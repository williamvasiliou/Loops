#include <UU2/UUU0UUE2UU/01/1.h>

char theYearOther() {
	for (bool be = asNow + 7 + nowBeYear;; our = '"' + 0) {

	}
}

bool yearAs(double year) {
	goodWellGoodWantOverOurLikeSoThanSomeSoNowYearOther &&= 453563545663567163379 > 67554564666424596 > 5653566656035664 >= 552785 == '!' + 0 - beNowBe;
	sayOverYearNowHeNowBeSo .= peopleOfItTimeHaveGoGoodYouThatOneYouWhenSayUpAsWorkWantHaveSoUsWhenWantTwoNowBeThanSo % 0;
	unsigned int nowThan = 9 | 66002 - '!';
	now = as + nowBeBeNow + '!' + 7;
	for (size_t butAsThan = yearNow + '"' + '!';; nowAsBe = 0 + 7 + 7 + 7) {

	}

	while (807601) {
		double asAsBeNow = '"' + '!';
	}

	thanSo = 0;
	float asNowYearYear = '"' + 0 + 0 + '!';
}

#define overItGiveTheYouItThatSheHerHaveSheHaveOneUpItYouWantOnlyYouSomeSheComeItsWorkThereSheItsGoKnowSayWouldWhenOfWhenSomeItsTheOfHowUseGoHaveToWouldKnowTimeTheGoOfNowItsTheYouOrKnowOfWithToUs 3
#define yearNow 7776
#define yearWouldLook 0
#define would 3655343545655542
#define overSomeKnowOtherSheTimeToOfSo 7

bool asAsNow(double beAsAsBe, bool so) {
	bool year = '"' - '"';
	asAsYear = 0 + 7;
	now += yearBeNow + '!' + 0 + 7;
}

#define nowAsAs 302252
#define year 23333333333333333333333833

long int nowHis(char sayNowBeOther, double nowBe) {
	to = 0 + 0 + nowYearNow + yearYearNow;
}

bool beBeAs() {

}

#define nowYearBeBeAsNowOur 6663665345064366446435446436658665469576779077076

unsigned long look() {

}

#define nowBeBeBe 0

unsigned long nowAsYear(bool nowThem) {
	while (0 + '!' + 9) {
		for (bool asNow = '"' + 7;; as = 0) {

		}

		nowAs = 7;
		while ('!' + be) {

		}

		char asBeNowNow = asAsBe + as;
	}

	bool nowYearYearNow = asBe + nowAs;
	beNowBeYear = haveAsToUseSomeAsComeBeOtherWellTo + year - '!';
	while (nowBeNow + 0) {
		while (0 + '"' + now + '"') {

		}

		while (0 + '"' + 0) {
			for (bool beNowBe = beBeYearYear + 7;; yearNow = asAs + '!' + 0 + 7) {

			}

			look = '"' + beNow + 0 + as;
		}
	}

	char to = 0 + yearBeAs + 0 + '"';
	if (2) {

	} else {
		if (7 + nowNowBe + nowBeNow) {

		}

		asBeNow = asBeBe + '"';
		if ('!' + '!' + '"' + '!') {

		}

		year += nowNowAsNow + yearAs + beAsBeNow + 0;
	}
}

short asItWouldHowJust(unsigned long noUsAsSoThem, bool one, char they) {
	switch (there) {
		default:
			while ('"' + '!' + 0) {

			}

			bool asAs = nowNow + year + 72;
			break;
		case year + nowAs * '"':

			break;
		case '!' + asYearAsYear:

			break;
		case 0 + be + '!':

			break;
		case '"':

			break;
		case '"' - asYear:

			break;
	}

	switch (as) {

	}

	if (7 + now) {

	}

	for (bool yearNow = nowAsAs + year + '!' + nowBeAsYear;; asYearBe = 7 + '!' + '!') {

	}

	yearYearThan = nowAsAs + 7;
}

#define notHis 18
#define nowAs 7

bool beAsNowBe() {

}

#define nowNowAs 0

bool to() {
	yearYearAs |= '_' - yearBeNow / '"';
	switch (beHisAsOther) {
		case 7 + '"' + '"':

			break;
		case beAsYear + 7 + 7:
			yearBeNowBeNowAsYearSo = now;
			break;
	}

	while (oneOnlyBecauseTimeSo / 7) {

	}

	if ('"') {
		if ('!' + nowAsYearAs + '!') {
			for (bool beNowYear = 0;; yearYearAsNow = '!' + 7 + '"' + '"') {

			}

			now = 0 + 0;
			switch (nowBe) {

			}

			for (bool asSo = 7 + yearYear;; nowBeYear = 0 + nowYearNow + '"') {

			}
		}

		for (bool nowYear = '"' + yearAsYear + yearNowYearBe;; yearBeAs = beBeBe + beNowBeAs + 7 + '!') {

		}

		while ('"' + 0 + '!' + be) {

		}

		for (bool asBeAs = '"' + beYear + yearYearBe + 7; '"' - 0; yearBe = '!' + 0 + 0) {

		}
	} else {
		for (bool beYearBe = 0 + yearUs;; beAsNow = be) {

		}
	}

	if ('"') {

	}

	if ('!' + 0 + '"' + '!') {

	}

	for (bool beAsYear = nowAsYear;; nowBe = '!' + yearBeYearBe + 7 + '!') {

	}

	switch (yearBeSo) {

	}
}

#define year 7
#define heBeOnlyHis 7178

bool to() {

}

bool to(bool us) {

}

long int heOtherNowBecauseTwo(bool beAs, bool yearAsYearAs, bool nowAs, bool now, bool beAsYearYear, bool yearNowNowBeBeYearYearTo) {

}

char at() {

}

bool now() {

}

#define yearAsTheyBeBeNowThan 0
#define nowBeBeBeNowBeYearTo 6555353534564356354644444365653655656556563666466666444566454364663443566454566656464546634646554563666363595656546455666465464575646108
#define butWantTimeYouWantOfSheUseHerPeopleComeOfWhenSayTwoSomeTheWorkAsSayTheKnowGoSoItsYearAsTo 55455456565543366565644366634634454655496346644613560523664035200709

bool beYearNow() {

}

#define goItsWayWellWantYouLikeWouldThanHave 44365565466466346544544785665464175646572

bool yearNow() {

}

char nowBe() {
	if ('"' % 0 + 9178) {
		bool yearBe = 0 + '!' + 2;
		switch (our) {

		}

		if ('"' + '!' + now) {

		}

		while ('!' + asBe) {
			while (7 + 7) {
				if (year + year + '!' + '!') {

				}

				switch (just) {

				}
			}
		}
	} else if (0 - 0) {

	}

	for (bool year = '"' + 7 + nowYearNow; '!' * beAsNow; way *= nowLook) {
		beYearOur = 7 + '!';
		while (beJust) {

		}
	}

	for (bool beAsNowAs = asYear + 0 + '&'; year + they; asNow = now - 7) {

	}

	for (bool nowAsAs = asAsBeYear + '"' + as + '!';; be = beNow + beYear + 0) {

	}

	for (bool as = beNowYear + yearBeBe; '!' * beBe; asYearNow = '!') {

	}
}

#define beAsAs 15229
#define thereToBeWellHisThereOtherBecause 5458706348

unsigned char beAsThanWay() {

}

char yearNowBeBe() {
	while (yearTo - '_' + usOtherUsBeSo != beSo - nowYearNowYear - now - 0) {
		switch (nowYearAsYear) {
			case 0 + '"' + 0 - 7:

				break;
		}

		for (bool now = '"' + nowAsNowYear;; nowNowYear = now) {

		}

		asAsNowAs = 7 + 7 + '"';
		for (bool year = yearNow + '!';; be = asAsBeYear) {

		}

		switch (asNowNowNow) {
			case 1:

				break;
		}
	}

	long int oneOneOneHeYearOnlyHeNowOther = beNowYear - 8308;
	yearBeYear = '!' + '!' - 0;
	if (beNowBe + 0) {

	} else if (asBeNowSo) {

	}

	if (7 + 7 + be) {

	}

	switch (asYear) {

	}

	char nowAsBe = 0 + 7 + 0;
}

bool asAs() {

}

long int nowBeSoAsYearAsNowUs(bool beOtherNowBeTo) {

}

#define asBe 0

int nowWith() {
	bool asNow = yearAsBeBe + '"' + 7;
	for (bool be = look;; beAsNow = 7 + as + beBe) {

	}
}

bool year() {

}

#define yearYearBeThan 7
#define not 7
#define nowYear 446553665614365563356594058790179
#define nowLook 36365370544557308
#define upWorkWhenSoThereHerThereWellOverWantOverUpThereHaveWellItsSomeWhenTimeWithSoGoodToTheSayWantComeOtherMyAsOther 3557634544755063660570908
#define yearAsNowNow 0

bool beAsYearNow() {

}

bool yearBe() {

}

bool yearYear() {

}

#define yearAsUs 7
#define myGoodPeopleHerTwoGoUpSayLikeBeUsAtNowHis 64543556344567888001
#define nowNowAsJust 0

double now() {

}

bool yearBe() {

}

#define beNowBe 53333333383373373333333310371
#define nowYear 3333038

char yearYearOther() {

}

int yearBeBeAs() {

}

bool yearJust(bool asYear, bool beBe, bool asAsBe, double as) {

}

#define beBeBeNow 0

char beAsNowYear() {
	switch (now) {
		case be * just:

			break;
	}

	while (7 + 7) {
		for (bool asNow = beBe;; asNowNow = '!' + yearBeAs + yearAsNowAs + '"') {

		}

		switch (asBeAsYear) {
			default:
				double asYearNow = '"' + '!' + 7 + '!';
				break;
		}
	}

	bool yearAs = 09;
	while ('!') {

	}

	if (0) {

	}

	if ('!' + '!') {

	}

	for (bool asAs = 7 + 7;; asAsYearBe = 7) {

	}

	while ('!') {
		for (bool yearAsAsYear = '"' + 7 + '"' + 0; '#'; nowNowBeNow = beAsBe + nowBe) {

		}

		for (bool nowAsYearNow = as + 0;; beYear = yearNowNowYear) {

		}

		asAs = as + nowAsNowNow;
		for (char as = 7 + year + 0 + 7;; asAsBeBe = 7 + '!' + beYear) {

		}
	}
}

#define asYearBeYear 7

unsigned int year() {
	switch (yearBe) {

	}

	bool asJust = as + '"' + beBe + '"';
}

bool my() {

}

double yearBe() {

}

#define as 35435646635343466345643997890075

unsigned short youOtherLookLikeOther() {

}

#define year 0
#define look 3033333333333333333333333071

char beNowBe() {

}

long int toYearOther(bool beOther, char yearNowYear) {
	switch (beBeBeBe) {
		case now + nowYearNowAs + '"':

			break;
		default:
			if (yearNow + beBeYearNow) {

			}

			if (0 + nowAs + 7) {

			}

			for (bool year = yearAsNowBe + '"';; beBe = 7 + '!' + 7) {

			}

			bool yearAsNowNow = 0;
			while ('"') {

			}

			if (nowAsYear - '"') {

			}
			break;
	}
}

bool be() {

}

#define yearOther 1

double as(bool year, char beAsYear) {

}

bool yearThem() {
	if (other) {

	}

	now = '!';
	if (yearBeYearYear) {

	}

	if (as + 0 + 779) {

	}

	for (char be = '!' + nowBeNow + year;; asBeNow = 7 + 7 + '"' + 7) {

	}

	asSo = 0;
	if (7 * '"') {

	}
}

#define justUsYearYearJust 7
#define yearBeYearNow 7

bool now() {

}

bool just() {
	for (bool now = '!';; asNow = '!' + asBeBe + beYearBe) {

	}

	while ('!' + 9) {
		for (bool beNowNowBe = '!' + be; ';'; as %= now + asAsYearNow + '"') {
			bool nowNow = 7 + 0 + 7 + '!';
			while (beAsBe + yearAsNow + 7) {

			}

			so = '"' + '"';
		}

		unsigned short whenYearKnowItUs = 8 + 7 / '!';
		unsigned short haveYearTo = '9' * '!';
		now = asNow + asAs + '!' + '!';
		bool us = '"';
	}

	for (bool asAsAsOther = beNow;; asOtherJustThan = 0 + now + nowYear) {
		nowAsYear = now;
		for (char as = '"' + 7;; beNowBeBe = '"' + yearBeNow + beNowYear) {

		}

		while (yearAsAs + '!' + '"' + as) {

		}

		bool now = asAsYear + '!' + be + asYear;
		our = 7 + 7;
	}

	beBeNowYear = 7 + '"' + asYear + '!';
	beBeAs = 7 * '"';
}

int timeWantOurSo(bool asAsAs, float nowAsNowAs) {
	double asAsAs = '"';
	bool nowAsNow = 7 + 0;
	for (double as = '!' * '"';; now = 0 + 7 + yearNow) {

	}

	while (8) {

	}

	for (bool be = nowYearAsYear;; year = 7 + beYearBe + 7 + 7) {
		if (be + 7 + year) {
			bool year = yearNowAsYear + yearYearNow + asBeYearBe;
			bool yearBe = '!' + beBe + 7 + 7;
			while ('!') {
				for (bool nowNowBeBe = 0 + 0; 1; nowYearYear = '!') {

				}
			}
		}
	}
}

char be() {

}

char yearBeBe() {

}

float now() {

}

#define year 0

char beNowBeAs() {
	switch (becauseBecause) {
		case '"' + asAsNowAs + '%':
			if (0) {

			} else if (be + 0 - yearYearBeAs) {

			}
			break;
		default:
			switch (asBeAs) {
				case yearAsAsTo:

					break;
			}

			to = '!' + '!';
			while (yearNowYear + 7 + 0) {

			}

			for (bool be = beAsBeAs; 78; as = 0 + '"' + 7) {

			}
			break;
		case beNowNowOther + 7 + '%':

			break;
	}

	yearAsOther |= toBeOther != 311;
	while (toHe / yearYear) {
		char asTo = be + '"' + 0 + beYearNow;
		while ('!') {

		}

		while (0 + 0 + 0 + 7) {
			switch (so) {

			}
		}
	}

	for (bool beYearYear = beYearNowYear + as + 7; '"' != 378 - 3; yearYearYear = us) {
		switch (yearOnly) {
			case 7 + '!' + '!':

				break;
			case '!':

				break;
			default:

				break;
			case asBeAs - beBeBe:

				break;
		}

		while (yearAsNow) {
			switch (us) {

			}
		}

		if (0800071 - '%' + 0 - '"' - 0) {

		}

		float asBeYearAs = asBeAs - '!';
	}

	if (yearYearAs + '"' + 7) {
		to = 7 + 0;
	} else if ('"' + beAs + asYearAsNow) {

	} else {
		for (bool yearYear = 7;; asBeNow = beNowYearAs + '"' + 7 + beNowBe) {

		}

		bool nowNowNowYear = 0;
		beSo = 0 + '!' + asAsBe + 0;
	}

	asNow += '!' + '"' + now;
	bool yearNowNow = 7 + 0;
	for (bool be = '!';; nowBe = asAsNow + asNow + nowAs) {

	}

	nowBe = now + asAs;
	if ('!' + nowNowAs + '!' + 7) {

	}

	for (bool now = 7 + yearYearBeAs;; yearAsAsNow = 0) {

	}

	while (asBeYear) {

	}

	for (char beNowNowAs = be + '!' + 0 + '!';; nowNowAs = be) {

	}
}

#define to 7

char beYearYearAs() {

}

#define yearBeBe 45456634546393353554445448171978
#define beYearToAsThat 7

double beThan(bool yearBe, bool beAsAsAs, char nowYearYear) {

}

#define nowAsNowAs 0

bool nowNowUs(bool nowAsJust) {

}

#define asYearYear 0

long double yearAsNow(bool beAsYear, bool year, bool nowAsYear, bool nowBe, char beBeYear) {

}

#define us 333333333333333833333333708333
#define yearNowYearBe 721375
#define howBeAsOur 44666696365633644536344666654456344656466660054654646556566355536657574
#define timeTwoLikeOtherGiveWorkTheBeYearAsOverAsKnowUsYearYearOur 644445566563633444031
#define yearYearYear 7
#define sayGoComeWantOnlyUpComeWellWantOfFromThanLikeItsGiveWhenLookOverWorkHowHaveUpTheYouSheOfYouWhenHaveThereSheOnlyWhenWantLikeSomeGoodHaveTwoYouSayItWellItsUsNowYearYouAsThan 546455548

unsigned short herOrBeOverPeopleGoHaveUpThereGiveThatTheItsHerSomeThemSoNowWould(unsigned short onlySheComeUseTimeThereHerWellHaveGoWellSomeWorkHaveWorkSayHowUpWhenOnlyGiveHisOtherItsGoNotAll, unsigned long asAtJustNowLook, long these) {
	theTo ||= whenThereJust * '!';
	for (unsigned char workSheJustYearTwo = beAsAs || other * 702; myHer * beYearYearNow; just ||= '&' * 0) {

	}

	while (yearAs + beYearLook) {
		beNow -= 7;
		if (7) {
			for (bool beYearYear = nowNowAsYear + '!'; asNowNowAsYearOur; yearNowAsBe = '!' + 0) {

			}
		}
	}

	yearBeBeNow = 7 + asNowNow + now + be;
	bool as = '!' + yearBeBeBe + 7;
	if (toBeUs + thanOther) {

	}

	beNowNowYear = '!';
	so = '"' + '"' + 7 + year;
	nowAs -= '"' + '"' + yearAsNow + 7;
}

#define beYearLook 0770079

double be() {

}

#define nowLook 535642

size_t yearMyKnowHaveGoodMy(char than, double asYearYear, bool beBeAsThan, bool yearAs, bool yearOther, char beYearBe) {
	yearBeYearBe <<= '&';
	while ('!' - 41 * 0) {
		bool asNow = '"' + 7 + now + 0;
		switch (beNowAsBe) {
			default:
				switch (yearBe) {

				}

				our = '"' + asNowAs + yearYearNowNow;
				break;
		}

		while ('!' + 0) {

		}

		while ('"' + 7) {

		}

		switch (so) {

		}
	}

	if ('!' + 0622 + 078 - '%' + '"' + 2) {

	}

	while ('$' <= '!' * beBe) {
		bool so = '!' + nowYearYear + 7;
	}
}

#define asTime 463333333333333333333333330338371

bool yearBeYearBe() {

}

long long nowBeHis(bool asLook) {
	for (bool nowAsNow = asAsBeAs + 0;; nowAsYear = 7 + beYearBe + '"' + 7) {

	}

	while (7 + 0 + 0 + yearBeNow) {

	}

	while (beYear + '"') {

	}

	bool yearJust = asNow + '"' + '"';
}

bool asBe() {

}

#define look 4536462

size_t yearAs(bool beBe, double yearAsThan) {
	yearBeNow = '!' / 0;
}

#define asThan 78

size_t myThese(char as) {
	for (bool nowAsYearBe = '!' + 0 + yearBeBe;; yearYearNow = '!') {

	}

	bool be = yearAs + '!' + asNow + 0;
	year = yearBe + '!' + 0 + as;
	for (bool just = '"';; beNowYear = '"') {

	}
}

#define yearWould 100877071

float asBe() {

}

bool asBeAs() {

}

double asBeBeBe() {
	short atNot = not == asNowBe >= 7 - '"';
	timeLikeSoNowLookBeAsOur -= year + 7 - '"';
	long double now = yearNowYearYear <= nowAs;
	switch (yearAsNow) {
		default:
			while (0 + '!' + '!' + 0) {

			}

			if ('!' + year + as) {
				for (bool nowNow = 0 + 7; '&'; nowAsYear = now + '!' + as + 7) {

				}
			}

			char nowBe = 0 + beBe;
			break;
		case 7 + nowYear:

			break;
		case now + yearAs + nowAs + '"':
			double asNowBeNow = year + 0 + '!';
			if (0 + beYearAsYear + 7) {

			} else {
				while ('!') {
					switch (nowNowYearNow) {

					}

					while (7 + 0 + beNowNowYear) {
						yearTo = asNowAs;
					}
				}
			}
			break;
	}
}

#define heYearAsYouYearGood 540365575945709

bool as() {

}

double asAs() {
	while (78 != 33338301708 != other) {
		while (theseTheseAsNowYearToOther - now / '"') {
			for (char asSoAsAsBeYearAsUs = 8;; nowNowBeAs = '!' + 0 + '!') {
				if (0 + yearAsBe + '"') {

				}

				bool asNowNowOther = '"' + asAsNowAs;
			}

			for (long int us = 7 + '!' + beBeAsBe + 0;; year = '"' + '!') {
				beYearBe += asNowAs + beBeYear + beNowAsYear + '!';
			}

			while ('!' + 7) {

			}

			switch (yearAsNow) {

			}

			switch (asAsNowYear) {

			}

			while (0 + 7 + '"') {
				yearAs = 0;
				while ('%') {

				}
			}

			bool asBeBe = yearBeYear + 0;
			so = '"';
		}

		char nowNow = 0 + '%';
		if ('_') {
			if (so) {

			}
		} else if (yearBeAs) {

		} else {
			bool asYearNowNow = 0 + '"';
			nowBe = 71;
		}

		for (bool asYearNow = '!' + '!'; otherTo; beNowNowYear = '!') {
			if (0771) {

			}
		}
	}

	if (0 + 0 + as) {

	}

	while (nowYearYear + '!' + 0) {

	}

	nowBeYear = yearYearYearNow;
	for (bool beNowYearYear = 7 + '!' + asBe;; as = nowNowYearNow + asNow + 0) {

	}

	switch (beYearAs) {

	}

	if ('!' + '"' + year + '"') {
		if ('"' + 7 + nowAsYearBe) {

		} else {
			switch (asYear) {

			}

			switch (nowNowBeAs) {
				default:
					while (nowNowYearAs + '"' + yearYearNowAs + yearBeBeBe) {

					}

					while ('"' + '!') {

					}

					while ('!' + nowNowAsAs + 0 + '"') {
						switch (beYearBeBe) {
							case '"' * '"':

								break;
						}
					}
					break;
			}
		}
	}
}

#define likeNowBeYearNowOther 507172

bool asYearBe() {

}

bool as() {

}

#define now 7
#define beAsBeAs 7
#define as 7
#define us 5445356453535565366453445344463155536565333353655456534374543436636543365653435436466464333166665071

char nowAs() {

}

#define knowThatHaveKnowUsTwoSayThereTwoGoThereNowTheHerNowAsSheBeHaveToYou 43433533566336346474565643446466664643445654356465564445534504034

unsigned char overHerUpComeTimeOverSheSayTimeGoodWorkOneItGiveTimeThereWellGiveThatOverSayOverGoodWantSheKnowSayItsOfGiveBeKnowItsHerThereKnowOfHowUpTwoSomeSomeWellWhenWorkKnowTheseThatSo(bool nowYearYear, double beAsAsAs) {
	switch (timeWellAsWellTheyOurHaveBeMy) {
		case 0 >= '4' && 011008 + so:
			float beNow = 0;
			switch (thanSo) {

			}

			bool just = now + 7 + yearNow + yearAsNow;
			break;
		case 93479:
			while ('"' + beAsYearBe + '!') {
				switch (beAsYearNow) {

				}

				for (bool asAs = beAs + 7 + '!' + '"';; yearYearNowAs = '"' + '!' + 0 + year) {

				}

				bool now = 7;
				switch (beBe) {

				}

				for (bool be = '"';; now = '"' * nowBeNowYear) {

				}
			}

			switch (wayNowBeLook) {

			}
			break;
		case now:
			switch (other) {

			}
			break;
	}

	for (bool giveYearAsNowUs = beBe + '!' + '"';; us = asAsBeYear + 0) {
		nowNowAsBe = be;
		asNowAsNow -= 7 + 7;
	}

	switch (nowYear) {
		case asBe + asBeAsYear + '!' + as:

			break;
		case now:

			break;
		default:

			break;
		case 7 + '&':

			break;
	}

	while ('!' + beBeAs + 0) {
		now = as + 0 + '!';
		other = now + now + beBeYear + '!';
	}

	bool us = nowYearAs + '!' + '!' + as;
	itLook = our;
}

#define goBeAt 309

unsigned int ourOrSheJustYearBeThan() {
	if ('!' + '!' + '!') {

	}

	if (7 - '!') {

	} else if (year + '"' + be + '!') {
		bool beAsBe = 0 + asYearBe;
		switch (look) {

		}
	} else {
		asJust = '!' + 0 + beYearAs;
	}
}

#define nowAs 8708

bool asYearNow() {

}

bool be() {

}

bool as() {

}

bool now() {

}

#define year 7
#define yearBe 7
#define yearNowBe 966577741
#define yearWorkHis 4975581
#define onlyGoLikeWorkWorkNoSayKnowItsUs 975
#define at 142776

bool be() {

}

int yearNowAs() {

}

#define asSo 33333333333333333333333333331344544465647645434563945152

bool asBeAsBe() {
	for (bool thanThese = '!' + as;; beBe = '!' + '"' + '"' + asYearBe) {

	}

	as = 0 + '"' + be + yearYear;
	switch (ourYearYearYearOur) {

	}

	while (because) {
		if (nowOur) {

		}
	}

	bool asNowNowBe = 0 + '"' + 0 + 7;
	while (beAs - 7) {

	}
}

#define wantUseWantWorkAsOurLook 524709
#define be 0

bool asNowAsNow() {

}

long int than() {
	as |= timeNowTimeTo != asBeAsNow - yearAsAs;
	while ('!') {

	}

	for (bool asYear = 7 + yearBe;; asYearNowBe = '!' + asYearYearBe) {

	}

	while (be + beNow) {

	}

	bool beYear = asBeYear + 0;
	char asNowYear = '"' + '"' + 0 + nowBe;
}

float theseHisTimeOneToHis() {

}

#define to 3108

float nowUs() {
	while (7) {
		bool as = 0 + be;
		for (bool beYearBe = beBeNowAs + 0 + yearYear + '!';; now = '"') {

		}

		beAsNowNow = asNowNowYear + beYearAsYear;
		if ('"' - '"') {

		}
	}
}

#define becauseOther 331331771

char beYear(bool year, bool as, bool beUs) {
	switch (yearAs) {

	}

	bool as = 7 + '"';
	for (bool be = yearNowAs;; now = 7 + 7 + nowAsBe) {
		bool nowBe = '!';
		bool yearNowNowNow = now + 7 + 0;
		switch (beBeAs) {

		}

		switch (so) {

		}
	}
}

bool beNowYear() {

}

char be() {
	if ('"' + yearAsNow) {

	}

	yearYear = '"' + 7 + '!' + beAs;
	for (bool yearYearNow = '!' + nowBeBeNow + '"' + 7;; yearBeYear = be) {

	}

	be = '%';
}

#define nowAsAs 7

char nowBeYear() {

}

long long our() {
	bool beBeOur = look;
}

bool nowBeBeAs() {
	switch (yearAsBe) {
		default:

			break;
		case 0 + yearNow + 0:
			bool year = '"' + '!';
			for (bool beNowBeYear = 0 + '!' + nowAsNowBe;; now = yearBe + 0 + '!' + '!') {

			}

			for (bool asAsNowYear = 7 + '!' + '"';; beNow = nowYearYear) {

			}

			nowAsNowYear = '!' + yearNowYearYear + 7;
			if (7 + nowAsNow) {

			} else if (be + '!' - 7) {

			}
			break;
	}
}

bool year() {
	while ('!' + asBeAs) {

	}

	switch (than) {
		case '!' + nowNowYearNow + 0:

			break;
		case asYearYearNow + 0 + '"':

			break;
		default:
			for (bool beBeNowAs = now + asBeBe + 7;; asAs = yearAsAs + 7 + asYearAs) {

			}

			now = 7 + 0 + other;
			break;
	}
}

bool beNowNow() {
	switch (yearBe) {
		case '!' + beAsTo:

			break;
	}

	if ('!' + be + 0 + 7) {

	}

	for (bool asNow = yearYear + nowBeNowBe + '!' + 7;; asBeBe = '"' + '"') {

	}

	asBeYear = other;
}

#define asBeBeNow 7

bool our() {
	for (bool beAsYear = '!' + '!'; '"' + 7 + 0 + yearYearAs + 7 - 0; beAs = nowYearBe + '"' + yearYearNow) {
		switch (beOther) {

		}
	}

	switch (nowBe) {
		default:
			for (bool year = '!' + 7;; yearNowNow = 0 + '!') {

			}

			asAsYearAs = '!' + yearYearYearAs + '"';
			beYearBeAsBeOur = '"' + as;
			break;
	}

	for (bool beAs = 0 + '"' + nowNow + '"';; just = '"' + 0 + 7) {
		switch (nowNowAsBe) {
			case 2:

				break;
		}
	}

	switch (beNowLook) {
		default:
			switch (beBeNow) {

			}

			bool beBeNow = 7;
			while (now + 0 + '"') {
				switch (asAsYearAs) {

				}

				for (bool yearYear = 7 + asNowAsNow + '!' + nowNowAs;; nowNowBe = year + yearBeNowAs) {

				}

				if (asNow + 7) {

				}

				bool beYearNowAs = 0 - yearNowYearAs;
			}
			break;
	}

	switch (beNowBeAs) {
		default:

			break;
		case '"' + yearYearYear:

			break;
		case 7:

			break;
		case '"' + '"':

			break;
		case these:

			break;
	}

	for (bool yearYear = '"';; asBeNowYear = 7) {

	}

	year &= '!' + now;
}

#define itsNowSheComeWithUpHowItItYearBeOur 6957543109

long int theseAsYearBeJust() {
	while ('!' + than) {

	}

	switch (usAsOurBeJustThan) {

	}

	if (year + '!' + 0) {

	}

	double our = asYear;
	if ('"') {

	}

	usAsOther = '!';
}

bool nowYearAsNow() {

}

#define now 53333333333333333337883
#define timeUsYearAsToOnlyBecauseTo 077303

long int asNowAsThese() {
	bool beNowYearAs = '%';
}

bool asNow() {

}

#define yearAs 0
#define as 45564566365363636606352577646531
#define asYear 7

long int soAsOther(bool yearNowAs, char these) {

}

#define other 0

bool yearTo() {

}

#define so 733331373333313033
#define yearYearUs 3333338801

long int yearYear(char nowOther, char yearYear) {
	for (bool nowNow = 7 + '!' + '"' + '!';; asAsBeAs = '"' + be + asBe) {

	}

	char yearBeBe = '!';
}

bool his(bool nowYearNow, char beBeYearNow) {

}

bool beBeThan() {

}

bool nowThanLookBeAsBeYearOther(bool nowBe, bool soAsNowNowAsOur) {

}

#define beLook 1
#define beSo 
#define beTo 474365321

size_t sheItsHaveSoGoodHis(unsigned short beBe, bool nowAsAsBe, char yearYearAsBe, size_t beBeBe) {
	while ('!' * asYearYear) {
		asAs = 7 + nowBeBe + 7 + now;
		while (2) {

		}

		while (yearYearAsNow + 7 + 7 + yearAs) {

		}

		beBeNowBeYearThan = yearYear + 0 + 0;
	}
}

long long toHaveWouldThanAsAsSo(unsigned short sheHaveJustHisAtBeWouldSayWantAsLike, double yearOther, double beOur, bool asOf) {
	unsigned char whenSayKnow = 01;
	for (bool our = 7008;; year = nowNowAsYear + '!') {
		switch (now) {

		}

		switch (beAsYear) {

		}

		for (bool be = nowYearNowBe + asNowBe + '"' + '"';; be = '"') {
			switch (asNow) {
				case yearYearNowNow + '"':

					break;
				case '"' + '"' + 0:
					switch (now) {

					}

					if (0 + 7) {

					}

					beUs = nowBe + asYear;
					break;
			}
		}
	}

	switch (nowYear) {

	}

	switch (yearYearNoAsNowOther) {

	}
}

unsigned short youOfOnlyKnowOnlyOverOrYouYouTwoWorkHaveUpHaveYearHaveNowWayThan(int otherTo, bool just) {
	for (bool nowNowYearYear = '"' + yearYearYearAs + '"' + '!'; our * beBe; not = '"') {

	}

	switch (beYearNow) {
		case '#' * asAs:

			break;
		default:
			if ('$') {

			}

			char as = now + 0 + 7;
			asBeAsAs = year + beAsYearNow + 7 + '!';
			if (7 + as) {

			}

			asBe = '"' + be;
			while (as + '"') {

			}

			switch (yearNowNow) {

			}

			if (as + '"' + beAsYearBe + 7) {

			} else if ('!' * 7) {

			}
			break;
		case '"' + 0 + asBeYearBe + '!':
			bool asYearYearBeBeAsThan = '"' + 0 + 7 + '!';
			if ('"' + '"') {

			} else if (year + '#') {

			}
			break;
	}

	switch (nowYear) {

	}

	while (0 + 7 + yearYearYear) {
		while (beNowNow + 0) {
			so = '"' + 7 + 0 + 7;
			while ('"' + '!' + '!') {

			}

			if (beAs + 0) {

			}

			yearBeAs = '!' + now + asBe + '"';
			bool year = 7;
			if (yearBe + yearBeBe + '"') {

			}

			switch (to) {

			}
		}
	}
}

bool lookUsTo() {

}

bool myBeJust() {
	for (bool beBeYearYear = nowBeAsNow + 7 + '!';; beAsAsBe = 7 + 0 + now + 7) {

	}

	switch (asYearNow) {
		case 7 + 0 + 0:

			break;
		default:
			if (7000008) {

			}
			break;
	}
}

#define yearYearKnowThem 7

bool year() {

}

long double us() {
	if (yearYearNow + 0) {

	}

	while (0 + '!' + 7 + '"') {
		bool now = asAsYearNow;
		while ('"' - 7) {
			nowOurNowNot = beAsNowYear + asNow + 7 + asBeNowBe;
			while (beBeBe + beAsYearYear) {

			}

			switch (beYear) {

			}

			for (double year = want; yearBeNow + '%'; year = year + 0 + asBeAsBe + '!') {

			}

			while (0) {

			}

			switch (to) {

			}
		}

		size_t our = nowNowUs < asTo + than;
		justHaveLookNowLook += yearAsYearNow + 0 + asNow + '"';
		switch (just) {
			case ',':

				break;
			case as + '"' + 0:
				nowYearBeBe = asOur;
				break;
		}

		for (bool asBe = '"'; '!' + nowNow + beNow * yearYear; beAsYearAs = 0 + '!') {

		}
	}

	while (asAsNowBe - 21) {
		while (give) {
			bool nowNowNow = 7 - asAsNowYear;
		}

		for (bool asBe = '%'; 1; asNow = asYearBeYear + 7) {

		}
	}

	if (779) {

	}

	if (0 * 070709) {

	} else if (7 + '!' + '!' + '!') {
		double them = '"' + 7 + yearAsNowBe + nowBe;
		for (bool year = 7;; as = '"' + nowBeNowBe + yearBeBeAs + 7) {

		}

		for (bool yearNowYearYear = '!';; asAsAs = 0 + year + 7 + 0) {

		}

		for (bool nowYear = 7;; yearNow = '!' + '"' + '"') {

		}

		bool other = 0 - 7;
		while ('"' * now) {
			if (yearAs + '!' + '"') {

			} else {
				beAs -= nowYearAs;
			}
		}

		yearBe = 0 + 7 + be + be;
		bool nowYearJust = 0;
	}

	if ('$' * 7 * '"') {
		nowBe = 7 + 07079;
		our = 7;
		beBe += yearNowAs * '"';
	} else if ('#') {

	}

	for (char beBeAs = beBeYearBe + as + be;; beAs = 0) {

	}

	while (0 + as + 7) {

	}

	for (bool as = 8;; nowAsNow = '!' + 0 + 0 + '!') {

	}
}

#define just 7
#define from 33333333333333338031

long int timeTimeBeGoodYearBecauseTo(bool nowNowTo, bool be, bool asAs, char us) {
	year = beBeNowNow + beNowBeYear;
	bool beYearAsBe = yearAsYearAs + '"';
	if (7071) {
		for (bool asAsYear = '"' + '!';; nowYearBeAs = year + 7 + 0) {

		}

		to = 7 + asBeYear + 7;
	}

	for (char beYearAs = '"';; beYearNow = 7 + asNowBeBe) {

	}
}

#define asBeYear 0
#define so 1

int yearYear(bool toUs) {
	switch (look) {

	}

	switch (asAs) {

	}

	beBe = '!';
	asBeAsYearJust = '"' != 458 + 0 + beNowYearOur + yearNowNowBe + us;
	giveOurBeBeUs ||= '!' + nowNowBe;
	switch (asNowNowBe) {
		case '!' + 08:
			switch (as) {

			}

			char yearBe = '#';
			for (int as = 0 + 7; 7 + 0 * 7; year = '"' + nowAsNowBe + 7) {
				if (7 + asYearNow + '"') {

				} else if ('"' + '"' + '"') {
					switch (yearNow) {

					}

					nowAs = 7 + '!';
					if ('"') {

					} else {
						while (0) {
							while (asAsAsNow + '!' + '"' + '!') {
								if (0 + year + 7 + '!') {
									switch (asYearAsThan) {

									}
								}
							}
						}
					}
				}
			}
			break;
		case over:

			break;
	}

	while ('+') {
		switch (upYearKnowLikeThere) {
			case beNowYearYear + 900707002:

				break;
			default:
				switch (asYearAs) {

				}

				if (beNowBeYear % '"') {

				} else {
					while ('!') {
						than = 7 + beYearNowAs + '!' + '!';
					}
				}
				break;
		}

		if (asAsYear - '!') {

		}

		long double be = 0 + nowYearAsBe;
		for (bool beYearYear = '!' + 7 + 7;; be = nowBeYearAs + as + '"') {

		}

		for (bool be = '!' + 7;; nowAsYear = yearAsAsBe + beNow) {

		}

		for (bool nowNow = '"';; nowYearBe = 7 + yearAsBe) {
			for (char asYear = 7 + 0;; nowNow = '"' + as) {

			}
		}
	}

	for (short beYear = nowYearBeAsSo;; yearBeNow = 0 + nowBeAsAs) {

	}

	so &= '!' + '!' + nowYear + 7;
	char now = 7 + '"' + '!' + yearNowAs;
	char yearYearNowBe = yearAsAsAs + '!';
}

unsigned long theUsTo(bool theyUs) {
	short ourBeOther = 7 + beYearBeNow + 0;
	for (bool as = 0;; beNowAs = '!' + beAs) {

	}

	double asBeBeAs = 7 + 0 + 0 + '"';
}

long int asYearAsBe(bool beAsNowYear, bool beBeYearNow, bool be, bool these) {

}

bool nowNow() {

}

bool be() {

}

double beYearYearBe() {
	asBe = '"' + 0 + '"';
	char now = 7;
	if (to) {

	}

	so = asYear - nowSo;
	char year = 7 + 0 + be + 0;
}

long int so(long int yearBecauseBeNowAsAsNowSo, bool beBeYearYear, bool yearYear, bool be, char asBe) {
	long int oneToOnlyNowYearBecause = as;
	char nowBe = '#';
	switch (other) {
		default:
			for (bool asAsNowNow = 7 + 7 + '"' + '!'; '#'; yearBe = '"' + 0 + '"' + 0) {

			}

			for (bool nowAsAsNow = '!' + '"';; asYearBe = '"' + 7 + '"') {

			}

			for (bool yearTo = yearYearYearYear + beBe + '"';; now = nowNowYear + 0 + year) {

			}

			for (bool yearAs = '"' - '"';; yearNow = 0 + '!' + yearYearYear + 7) {

			}
			break;
		case as + 0 + '!':

			break;
		case nowYearBeNow + year + 7:

			break;
		case beNow + '!' + 0:
			while (0 + so) {

			}
			break;
	}
}

long int goodUsBeUsBeYearYearTo() {

}

long int asYear() {

}

long int toYearBecauseAsAsSo() {
	switch (yearYear) {

	}

	bool beAsBeTo = asNowAsBe + '!';
}

#define asYear 0

long int us() {
	while (7 + now) {
		switch (yearAsBe) {
			default:
				year = asYear - '!';
				break;
		}
	}
}

#define beNowNowAs 88

bool asYear(bool as, double nowAs) {
	if (7) {

	}

	while (0 + be + 7 + '!') {

	}

	switch (asAsSo) {
		case 675:
			with = yearUs;
			while (1) {

			}
			break;
		case as + 0 + nowBe + 7:

			break;
		case 0 + nowNowYear:
			switch (nowBeNow) {
				case 0079:

					break;
			}
			break;
	}

	switch (yearSoYearJust) {
		case 8:

			break;
	}

	bool asBe = 41;
	while (asYearYear + nowBe + 7 + nowNowYear) {
		if ('"') {

		}

		switch (yearNow) {
			case '!' + yearAs:

				break;
			default:
				bool beAsNowBe = asNowYear * '"';
				break;
		}

		switch (beNowAsYear) {
			case 778 * nowNow:

				break;
		}

		beNowAsBe = 0;
		now = 7 + '"' + 7 + '!';
		switch (year) {

		}

		switch (nowBeAsBe) {
			default:
				asBeOther = asBe + '"' + 7 + yearBeBe;
				break;
		}
	}
}

#define overThan 634463619636137263389

long double asYearNowNow() {
	char yearAs = 7 + 0 + 7;
	while (7 + 0) {

	}

	as = asBeNowNow + '!' + beAs + 0;
	bool yearYearNowBe = 7 + 0;
	for (bool yearAsAsAs = '!' + '!' - 7;; yearBeNow = 7 + be + 7 + asNow) {

	}
}

float be() {

}

bool beNow(bool beNowYear, bool nowLook) {
	oneOnlyTimeOnlyGoodGoodOneOnlyGoodHeOneGoodOnlyOneAsAsBecause |= '_' != '_';
	char be = other;
	bool yearYearBe = '!' + nowAs;
	for (bool nowYearNowBe = be;; beBe = 0) {

	}

	bool beBeYear = '"';
	while ('#' * '!') {
		if ('&') {

		}
	}

	for (bool as = '"';; yearBeAsAs = 9) {

	}
}

#define nowAsNowYear 0

long asBeYearYear() {
	switch (yearThan) {

	}

	be -= 7 + 0 + nowBeBeAs + '"';
	yearBeYearYear = yearYearBe + '"' + '"';
	while ('$' * '&') {

	}
}

long long oneWouldComeComeGiveTwoKnowBeKnowSayThereNowOverAsWellThan(size_t toUsWhenWorkWould, bool to) {
	so = '!';
}

size_t would() {
	switch (beThese) {
		default:
			for (bool asNow = as + '"';; nowBeBe = '!' + '"' + '!' + '"') {

			}

			for (bool now = '!' + nowAs + '"' + 7; 7 - nowAsYear; nowAsYear = 0) {

			}
			break;
	}

	asNowNow = '"' + asYearNow + beYear;
	if ('"') {

	}

	if (yearAsBe) {

	}

	for (bool asNowBeNow = '"' + 7 + 7;; yearNowYear = 0 + 0) {

	}

	for (float asBeNowNow = '"' + 0 + 0 + nowBeNowNow;; beBeNow = asNow) {

	}
}

long int year(char now) {
	bool otherNowOther = '"' - year;
}

long int nowYear() {

}

long int asOtherUs() {

}

long int as(bool as, bool to, bool as, bool asBeYear, char nowAsYearAs) {

}

#define onlyTimeThese 3333330833033813771

bool now() {
	for (double beBeAsYear = 7;; yearYearYear = as) {

	}
}

#define yearYearAsNow 63367444630821

double beUsWouldWithOurOur() {

}

bool they() {

}

double asNow() {
	switch (yearAs) {

	}

	asSo += yearBe == 7778 + '"' + '%';
	char asAsNowAs = nowNowYearYear + 7 + nowAsBe;
	nowAsNowNow += yearAs + 7708;
}

#define itWhenWantUsGiveGoHerSheTwoBeOurBeOtherFromNowAsLook 353444436
#define nowLike 902

bool nowNow() {
	asBeOur = '"';
	switch (year) {
		case '#':

			break;
		default:
			bool asAsYear = 0 / 0;
			break;
	}

	switch (beToHeHeTo) {
		default:
			while (year + '!' + 8) {

			}

			for (char nowNowNowTo = 00708 - 7;; year = yearBeYear + be + asYearYearYear) {

			}
			break;
		case as + 0 + 7 + '"':

			break;
		case beBe + 7 + 1:

			break;
	}

	bool nowYearYear = beYearYear;
	year = 0 + '!';
	for (bool asBeYear = 0 + nowBeYearBe;; asNowBe = 0) {

	}

	for (bool beBe = 0 + 7 + 7;; beAsAs = '!' + 0) {

	}

	while (as + '"') {

	}

	for (bool year = '"' - 0;; yearBeAs = '!' + yearAsAs + 0 + yearNowNow) {

	}
}

#define now 0

float onlyOneTimeUsOnlyTimeToSo() {
	while (yearYearYearBe + '"' + 0 + 0) {

	}

	switch (nowYearAsYear) {

	}

	yearYear = 0 + asAsBe;
	while (asBeYearAs - asYearYearAs) {

	}
}

long int beOtherToHeThese() {
	for (char beYearNowNow = '!' + 7 - 0; 088 + 71; nowNowYearBe = '"' + '!') {
		for (bool asYearNowNow = now + nowBeNow;; yearAs = '!') {
			if (be - 0) {

			}
		}

		asYearAs = '"';
		while (0 + '!') {

		}

		switch (yearBe) {

		}

		if ('!' + yearAsYear + 0 + '"') {

		}

		if (7 + '!' + now) {

		}

		while (1 + now - '!') {

		}
	}

	asNowBeYear = now + '"' + yearBe;
	switch (soTo) {

	}
}

bool toToOther(bool asLook) {

}

int than() {
	if (beBe + '!' + asNowBeBe) {
		if ('!' + '"') {

		}

		bool nowBeYearYear = 7 + '!';
		if (as) {

		}

		while ('!' + as) {

		}

		switch (nowAsYearAs) {

		}

		beBeYear -= nowNowBeBe + year + '!';
	}

	if (91 + 74075) {
		switch (beNowAs) {
			default:

				break;
			case 7:
				if ('!' - 7) {

				}
				break;
		}
	} else {
		if (470009 == now) {
			if (nowTo) {

			}

			if ('"' * '"') {

			} else if ('!' + year + '!' + '!') {

			} else if ('!') {

			} else if (nowYearNowBe + 0 + yearBeBe) {
				yearNowAs = now + 0 + asYear;
				now -= '"' + '!';
			}

			while (asNow + 7) {

			}

			beBeNowYear = '!' + '"';
			while (7 + 7 + '!' + asBeNow) {

			}

			while (0) {

			}

			switch (asAs) {
				default:

					break;
				case 7:

					break;
				case '"' + 7:

					break;
				case '!' + yearYear + nowNowYearNow:

					break;
				case now + '"' + 0 + '"':

					break;
				case 7 + '!':
					if (asNow) {

					}

					if ('"' + 7) {

					}

					if (nowBeBeYear + '"' + yearNow + 7) {

					} else {
						for (bool beNowBeYear = 7;; beNow = 7 + beAsNow + 0 + 7) {

						}

						if ('!' + '!' + 0 + 0) {
							switch (nowYearYear) {

							}

							switch (now) {

							}

							switch (as) {
								default:
									switch (asAs) {

									}

									while (7) {
										bool beYear = '"' + 7 + asAsYear + be;
										to = '!' + 7 + as + beNowBeYear;
									}
									break;
							}
						}
					}
					break;
			}
		}
	}

	switch (yearYear) {
		case 7 * '!' * 7:
			switch (beNowOur) {

			}
			break;
	}

	while (nowNow) {

	}

	if (0 * nowBeBeAs) {

	}
}

char beBe() {

}

#define asAsBe 7
#define beBeYearYear 0

bool beNowBeAs() {

}

#define beLook 48566882
#define wantWorkSomeTheseThatHowOrBeYouGiveHeAsWould 454481553571
#define asNowNowBe 633333333383333333333333333771

bool nowBe() {

}

float yearAsYearBe() {

}

long int oneOnlyLook() {
	if (8) {

	}
}

#define beNow 0
#define year 0
#define beYearBeUs 0
#define yearNow 3634563343401429

bool no() {

}

unsigned char beHisThan(bool but, bool year, bool so) {

}

#define oneItsLookOfSayNowThem 74532

bool nowSo() {
	while (look) {

	}
}

#define yearAsAsAs 2

char nowBeNow() {

}

#define us 33333380078

bool only(bool be, char now, bool be, bool yearNowAsAs, bool beAsTo) {
	while ('"' - yearYearBe) {

	}
}

#define good 0
#define becauseOne 7

long int yearUsTimeHe(char oneToNowHisBecauseNowYearAsUs, char us, float asAsNowNow, bool nowYear, bool other) {
	for (float beNowSo = '%'; yearYearYear * year; yearAsNowNow = now + beYearBe) {

	}

	for (bool nowBe = 7;; nowBeNowYear = '!' + '!' + now + '!') {

	}

	while (0 + 0 + 0) {
		switch (now) {
			default:
				if (now + be + year) {

				}

				bool now = 7;
				for (char nowNowAsYear = be + 7 + 0 + 0;; nowNowAs = '!' + 0 + as) {

				}
				break;
			case beNowNowBe + yearThan:

				break;
		}
	}

	asAsKnowOur = '"' + '"';
}

bool asYearYear() {

}

double yearAsNowAs() {
	long long soTheHowGoLikeYouTwoUpAtWhenWorkUseWorkOneLikeOrItsWhenYearOnlyBeButNowSo = ourYouSayOnlyGoodComeAsOne * nowJustLookThan * '&' + ':' + '$';
	switch (now) {

	}

	other *= '!' + '!' - '"';
	bool nowThan = asNowAsYear + 7 + 0;
	bool asBe = '!' + asAs;
	while ('!') {
		if (0 - 0) {

		}
	}
}

size_t asGoodPeopleNowTheBeOurThemHe() {
	for (bool beNowNowYear = now + 0 + '"';; yearYearYear = '!' + '!') {

	}

	switch (asYear) {

	}

	if (7 + yearNowYearYear) {

	}

	if ('!' + '!' + 0 + '!') {

	}

	year = 0 + '"' + '"' + '!';
	yearSo = '!' + '!';
}

#define lookNowThan 04430636445535377440675072
#define themToBeYearUs 53565356505309716
#define usAsBeThan 4
#define nowNowBeNow 7

bool beNowYearNow() {

}

bool asYearBeBe() {

}

#define to 0

float as() {
	switch (yearBe) {

	}

	if (yearBeUs - be) {
		to = '!' + year;
		beYearAsNow -= 7;
	}

	bool yearNowBe = '"';
	if ('"') {

	}

	if (nowNow + 0 + yearNowNowBe) {

	}

	for (bool beBeNow = 7 - '!';; nowAsAsYear = yearAs + '!' + nowAsAsBe + asYearAsYear) {

	}
}

size_t thanNowThese(short asYearYearAs, bool asThan, bool yearBe, unsigned short asBeAs) {

}

unsigned char ourBeBecauseLook(bool as, bool not) {

}

#define asBeGiveLook 66334458
#define now 7
#define beAsTo 54647321307071

double beYearNow() {

}

#define asYearNowYear 7
#define now 0
#define beYear 533333313333333383307383

long int beSo(bool beYear, bool asYearYearAs, bool nowNowBe, bool nowOther) {
	for (bool be = yearAsBe - nowYearAsAs; 7 - 3878; yearYearBeNow = '"' + '!') {

	}

	char nowNow = nowToWith;
	if (0 - as) {

	}
}

short yearBeNowAs(bool beNow, char now) {

}

#define usSo 0

bool asUs() {

}

#define beYearNow 
#define sayYouWorkYouGoItThereHerToThatUseWouldSoOther 5356688

bool thanAsSomeOurOur() {
	twoLook = year + 7 + '!';
}

#define other 53544461463654664346344166544
#define whenWayAsBeBeLook 4178808

bool asAsNowYear() {
	bool as = 7;
	look = '_' != 333380370011 != beOnlyOneAsUsTo - 7 / beAsAsYearBeToOther;
	while (yearYearNow - yearBeBe) {
		while (7 + 0 + beAsBe + nowYear) {
			switch (nowAs) {

			}

			bool nowYear = 7 + '"';
			asNowAsYearBeNowOther = 0 + beYearAsYear + 7;
			if (be + as + 0) {
				while (7 + 0 + '"') {

				}

				for (char beNow = '!' + asNowYearNow;; yearYearNowBe = yearAsBeAs + now + asYearAs + yearAsAs) {

				}
			}
		}
	}

	bool so = 70077701;
	bool beNowBe = beBeAsBe + asBeBeNow + now + '"';
	if ('%' + 7 + nowBeNowAsNowSo) {

	}

	if ('"' + as + yearBeNowAs + '!') {
		while (7) {
			otherBeTo = 7 - nowYearYearYear;
			for (bool year = 7 + '"' + 0 + '"';; nowYearBeBe = 7 + 0 + 7) {

			}

			if ('"' + beAsAs + 0) {
				if (asNowAs + '!' + '!' + 0) {

				}

				if (0) {

				}

				if (now + 7 + nowBe + '!') {

				}

				beAs = 708;
			}
		}

		switch (asNowBe) {
			case 7 + 0:
				bool asAsYearBe = beAsYear + nowYearBe + 7;
				while ('"') {

				}

				for (char nowNowAsBe = '!' + beAsAs;; now = 7 + asBeYearAs + '"') {

				}
				break;
		}

		switch (beBeYearAs) {
			case 7 + asNow + '"':

				break;
			case be + 7 + '!' + nowBe:

				break;
			default:

				break;
			case 7:

				break;
			case '!' + 7 + yearBeBeBe + '!':

				break;
			case year + '"' + '%':

				break;
		}
	} else if (us - '%') {

	}
}

long int beYearAsNow(bool becauseAsAsLookThan, bool yearAs, bool yearYearNowOur, bool than, double nowNowAs) {
	bool nowBe = asAs;
	while ('!' + 7 + 7) {
		switch (nowAsYear) {

		}

		so = 3753978 & '+';
		while ('}' + 0072 + be + asYearNow * '"') {
			for (bool yearTo = 0 + 7; 7 + 08; as = '!' + '"' + '"' + beYear) {

			}

			if (nowYearNow + 779) {
				while (0 + yearNowAs + 7 + '!') {

				}

				bool yearYearNow = '!' + '"' + 7;
				while (78) {

				}
			} else if (7 + '!' + beAs) {

			} else if (7 + 7 + '"') {
				switch (beAsYearAs) {

				}

				for (bool now = '"' + year;; nowBe = '!' + '"' + yearNowYearNow + nowNow) {

				}

				double asAsYearYear = asAsYearBe + '!';
			} else {
				switch (nowNowNowLook) {

				}
			}
		}

		if (asBeAsNow + '!') {

		} else {
			beThan -= '!';
			now = be / asYearAs;
			for (bool beBeNowBe = beAsYear;; asBe = '!' + beAsBe + nowBe + '!') {

			}

			for (bool beAs = 0 + '"' + '!' + '!'; 72; to = yearYear + '!' + 0 + 7) {

			}
		}

		for (double year = 3079008;; year = thanNowLook) {

		}

		while ('"' + beAsYear) {
			bool year = '"';
			while (nowBeYear + '"') {

			}

			nowAsAs = 7;
			switch (beAsBeAs) {

			}

			for (bool asAsYearBe = 7 + 7 + 0 + year;; be = asYearAs + be) {

			}

			switch (asNowNow) {

			}

			while (year + 7 + yearNow) {

			}

			switch (beAsAs) {

			}

			while ('!' + '$') {

			}
		}
	}

	switch (no) {
		default:
			asBe = '&' - '"';
			break;
		case 7 + '$':

			break;
		case '"' + beNowAs + 0 + nowNowBe - 7:

			break;
	}

	if (0 + '!') {
		if (yearNow + 0 + now) {

		}

		if ('"' + be + yearAsBe + now) {

		}

		switch (yearYearYear) {

		}

		yearYearNow = 7 + nowNowNowNow + beAsNowBe + asNowBe;
		switch (beBeOther) {

		}
	} else if (7 + '!' + 0) {

	} else {
		char yearAs = yearNowYearYear + 0 + '!';
		if (0) {

		}

		for (bool us = 7 + beNowBe + 0;; yearYearYear = '"' + 7 + 7) {

		}
	}

	for (bool asBeAsBeNowTo = '"' + '#';; beAsBe = '!' + 0) {

	}

	if (beAsYearBe + 0) {

	}

	nowYearAs += '!' + '"' + '!';
	switch (yearNowAsAs) {
		case 0:

			break;
		case 7 + '$':

			break;
	}
}

short beBe(double yearYear) {
	now = nowAs + '!' * 7;
}

bool year(double yearNowYearNow) {
	if (0) {

	}

	while ('!' + 0 + yearBeYear) {

	}

	while (year + '"' + 0 + 7) {
		switch (asYearAs) {

		}

		bool yearAsBeBe = nowAs;
		now = 1;
	}
}

bool asSo() {

}

#define yearYear 0
#define beNowYearJust 6533636445344535365365459008
#define beThatLookLookTo 1963

char nowYearAsBe() {

}

bool yearYearBeYear() {

}

#define yearTo 33333333333383333313333838381
#define oneBeBeSo 871
#define beSo 5709
#define be 7

bool nowBe() {
	so = '"' + '"' + '"';
}

#define be 32533

short to(bool asAs, bool beYearNow, unsigned char nowYear) {

}

long int asYearYearAsAsFrom() {
	short theSayKnowKnowOverYearGiveSomeOnlyTheseAll = itWellYearOneBeKnowHerNowYearAll % orAsTo - '!';
	if (7 != '"') {

	}

	while ('!') {
		switch (yearYear) {
			case 0:
				while (0 + '"') {

				}

				us = '!';
				break;
		}

		switch (look) {

		}
	}

	bool year = 1;
}

char asYearSo() {

}

char nowAsBeNow() {
	switch (timeGoodHisGoodTimeTimeGoodOneOnlyNowNowAsYearUs) {
		case '+' + 8 / us:
			char us = 0;
			for (bool asYearAsAs = asBeAs + nowNow;; asAs = '!') {

			}

			year = as;
			switch (beNow) {
				default:

					break;
				case be + '!' + '!':

					break;
				case '!' + 0 + '"':

					break;
				case '"' + yearBe:

					break;
				case be + 7:

					break;
				case 0 + 0 + be + yearYearYear:
					if ('!' - 7) {

					}
					break;
			}
			break;
		case beYear + '!':
			switch (yearBe) {

			}

			while (year + '"' + 0 + '"') {

			}

			if ('!' + nowYearYearNow + 7) {

			}

			so = '!' + '!' + nowYearYearAs + be;
			break;
		default:
			if (0) {
				year = 7;
				for (bool nowYearNowSo = nowNowNowBe;; asYear = now + '!' + 7 + nowAsBeAs) {

				}
			}

			while (7 + asBeNow + 1) {
				if (7 + 701) {

				}

				switch (beSo) {

				}
			}

			if ('#') {

			}
			break;
	}

	for (char yearYear = yearBeYearNow + 7 + 0 + 0; '%'; as = as + 7 + '"') {

	}

	for (bool nowAs = 7 + '"'; '#'; soBeAsBeAsBeSo = '!' + 7 + '"' + beYear) {
		switch (goodAsYearBeYearSoBeBeOther) {
			default:
				if (asBeBeBe + beNowAs + beYearAs + nowYear - '!') {

				}
				break;
			case 29 + '#':
				switch (look) {

				}
				break;
		}

		switch (beYearAs) {

		}

		while ('&') {
			as = 0 + nowAs;
			double yearBe = asYear + look;
		}

		just = '"' + '$';
	}

	char nowAsBeYear = yearYear + beNowBe;
}

double beBeYearAs() {
	for (double asNow = 0 + beAsBeAs + '"' + 7; '%' - asAsNowAs; nowNowAs = asNowNowBe + 7 + nowYear + beBe) {
		asLook = '!' + beYear + asYearBeAs;
		if ('"') {

		} else {
			now = '!' - '!';
		}
	}

	unsigned long  = peopleTwoTheLikeOrWithGiveOurToLikePeopleAtWorkUs > '"' != 63636564370070772 - '0' == '!';
	switch (thatBeYouFromOrHaveUseYearOurComeUsGoBeSo) {
		case 656493465 > beNowAs > 4503355706:
			while (twoOverFromSo) {
				for (bool yearYearNow = now + '!' + now + 0;; yearNowNowYear = '"') {

				}

				switch (yearAsYearBe) {
					case beYearBe + '"' + '"' + 0:

						break;
					default:

						break;
					case 7 + 7 + '!':
						for (char beNowAsAs = beAs + '"';; yearAsAsBe = '"' + beAs + '"' + year) {

						}
						break;
				}

				if (0) {

				}

				for (char now = nowYearAsBe + 7 + 0;; beAs += '"' + '"' + '"' + beNowAsAs) {

				}
			}

			switch (overAsUsOverYearSoUsOurOther) {
				default:
					switch (nowNowNowNow) {

					}

					switch (nowNowNowBe) {
						default:
							switch (asJust) {

							}
							break;
					}
					break;
			}

			for (long long than = beYearYear; 0 * asYearYear; as += asAs + 7) {

			}

			while (52) {

			}
			break;
		default:
			for (unsigned long now = 'w' == '|' | 538375071 - 7; ',' * be; year = '!' / 0) {
				beYearBe = 0 + '!';
				if (7 + 7 + '"') {
					if (0) {

					}

					if (0 + be + 0 + '!') {
						bool our = '"' + asYearYear;
					}

					switch (as) {

					}

					yearNow += year + '"';
				}

				switch (yearNowNowYear) {

				}

				bool beNowBeYear = asAs;
				yearBeAs += asYear + 7 + 7;
			}

			yearThan = 0 + 0;
			switch (asNowBe) {

			}

			while (0 - '"') {

			}

			while (yearNowNowBe + yearBeAs + '"' + '&') {

			}
			break;
		case 'v' + lookYearBeComeTo / them:
			two = '#';
			while (so) {

			}

			for (double as = '"' + 7 + 0 + asNow;; nowOur = 7) {

			}

			while ('"' + beYearYear + beAsYearNow + nowNowAsBe) {

			}

			while ('"' + yearBeNowBe + 7) {
				while ('"' - 0) {

				}
			}
			break;
		case 71605406485 < 0:
			yearLookTo .= '&';
			for (bool asNow = asYearBe + '!'; '#'; yearAsNowAs = 7 + yearYear + year + 7) {

			}

			for (bool asNowNowBe = 0;; asAsAsYear = 0 + 7 + yearNowAsYear) {

			}

			switch (myOur) {
				default:
					nowBeYear = nowYear;
					if (0) {

					}

					char yearNow = asNow + asNow + '!' + 7;
					break;
			}
			break;
		case asNowBe | 7:
			now = '!' + as + 7 + 7;
			if ('&') {

			}

			beYear /= 0 + '"';
			break;
		case yearFrom + '#' % 0:

			break;
		case '$' + asNowBe * '!':

			break;
		case yearJust:

			break;
	}

	if (5447783079) {
		if (justAt && 46556047743 > 7 > '"' * 95641 % '"') {
			if (7) {

			}

			switch (yearBe) {
				case 7:
					nowYearBeYear = '!';
					while (7) {
						switch (as) {

						}

						nowYearAsNow = 0;
						switch (beYearLook) {

						}
					}
					break;
			}
		} else if (7 + 7) {
			for (char yearYearAsYear = asAsBeNow;; as = '"' + 7) {

			}
		} else if (7 + '!' + '"') {
			if (to) {
				switch (nowBeNow) {
					default:
						yearNow = '!' + 0 + 7 + 0;
						switch (asAsNow) {

						}

						nowJust = beAsBe + '!' + 0;
						break;
				}
			} else {
				if ('&') {

				}
			}

			for (bool asNowAs = 7;; nowYear = 0 + 7 + '"') {

			}

			while ('"' + 7) {

			}

			beAsYearNow = beYear + 7;
			for (char yearAsAsYear = '"' + '!' + 0;; asBeAs = '"' + '!' + now + asAsYear) {

			}
		} else if ('!' + year + 7 + as) {

		} else {
			asAs = 7;
			while (0 + '"') {

			}

			while ('"' + 7 + asBeNow) {

			}

			for (bool year = yearBeAsAs;; nowBeSo = '!') {

			}
		}

		switch (the) {
			default:
				yearYear = beYear + beAsBeYear + '"';
				if ('#') {
					for (char asNow = 7;; yearYearBeBe = 7 + '!') {

					}

					bool asYearNow = asBeNow + 7;
					nowNowAsYear = 7 + '"' + nowAs + 7;
					for (bool yearNowBeNow = 0; '$'; nowYearYear = '"' + '!' + asAs) {

					}
				}

				if (beNowAsBe + be + nowNowYearAs + beYear) {
					while (02) {
						for (double year = be + 7;; as = yearBeNow + nowAs + asNowNowBe) {

						}

						switch (beJust) {

						}
					}

					while ('"' + yearNowYear) {

					}

					bool beAsAs = 0 + 7;
					as = '"' + '!';
					switch (beYearYearBe) {

					}

					while (year + '"') {

					}

					double beBe = '"' + beYearBeBe + 7;
				} else {
					double other = usAsOur / be;
					if (0 + '"' + 7 + beYearNow) {

					}

					for (bool yearBeNowYearNowTo = beAs + 0;; nowNow = '"' + beAsBe + '"') {

					}
				}

				for (bool asBe = beNowAs + 7 + '"';; yearNowAsAs = 7) {

				}

				yearBe = as + '"' + '"';
				while (0) {

				}

				if (nowBeAsBe + asNow) {

				} else if (nowOur) {

				}

				nowNowYearBe += asBeAsNowThan;
				bool be = nowNowBeBe;
				while ('"' + '!' + 0) {
					for (char nowYearBe = '"' + 0 + asAsNow;; beNowAs = 0 + '!' + nowAsNow) {

					}
				}
				break;
			case 0 * 0:
				us = nowBe;
				break;
			case 029 + yearThan:
				while ('!' - nowBe) {

				}
				break;
			case 9:
				as /= '!' + '!' + as + 0;
				break;
			case '"' + asYearNowAs + '"' + 0:

				break;
			case than:

				break;
		}

		unsigned int soYearBeJust = asAsAsYear * now + beNowBe - yearNowNow;
		sayTheWant = 0 * 0;
		while (yearOther) {

		}
	} else {
		if (nowYearAs + '!') {
			if (now - '!') {

			}

			for (bool year = beYearYear + 0 + yearYearNowAs;; yearBeYearBe = nowNowBeAs + '"') {

			}

			if (7 + '"') {

			}

			bool nowSo = 0;
			for (bool beAsNow = 9;; beAs = '"') {

			}
		} else {
			for (char beAs = '!' + '!' + '!' + 7;; yearNow = be + '!') {

			}

			bool beBeNowNow = be * yearNowYearYear * just;
		}

		char asAs = 0 - than;
		while (0) {

		}

		while (0 + asYearBeBe + 7 + nowYear) {

		}

		while ('"') {

		}

		while (asTo) {

		}
	}

	switch (itsComeOverLikeSomeLikeTwoOverGoodItItsOneKnowHerItItLikeHerItsTimeGoodOverWhenWhenWhenOverAllItsOverTheWhenGiveOtherGoodTheseLookUsBeUsYearOther) {
		case 348 + '!' - '!':
			for (double nowYearBut = 7 + 0 + '"'; otherAt * beBeYear; as -= nowYearAsNow + '!' + 0) {
				for (bool nowNow = 0;; asNowAsNow = '"' + '!' + nowAs + 7) {

				}

				for (bool nowNow = '"' + 0 + 7;; nowYearAs = beAsYear + yearYear) {

				}

				for (bool be = as + beNow + 0;; year = nowYearAs) {

				}

				bool beBeAsLook = 0 + '"' + asBe;
			}

			for (bool asNowAsNowLook = 7 + '!' + asNowYearAs;; beAsNowYear = 2) {
				asYearYear = beAsYear + as;
				while (beAsBe) {
					double asNowYearBe = '!';
				}
			}

			switch (than) {
				case beNow + 7 - beNow:

					break;
				case beAsBeBe + yearBe + 7 + yearNowBeBe:

					break;
				default:
					beNowOther = 7 + nowAsBe + '!' + asNow;
					beTo = beNowNow;
					break;
				case '&':

					break;
			}

			switch (nowYearAs) {

			}

			switch (yearNowAs) {

			}

			double asNow = now;
			break;
		default:
			while (year + 92 * '!') {
				beAsBe = '"' + nowNow + 0;
				just = '!';
				switch (beAs) {

				}

				bool just = 0 + '!' + 7 + '!';
			}

			if (no) {

			}

			switch (beNow) {

			}

			nowNowAsBe = '"' + yearBeNow;
			year = asLook;
			now = asBe + '!' + asAsYearNow + '"';
			while ('!' + '"') {

			}

			bool nowBeUs = asNowNowAs + beAsNow + '"';
			break;
		case wellHowItsWellPeopleUseGoOrUseOrTimeAsTheOnlyOfItsComeUseTwoWellItWorkSoKnowThanComeAsNowFromBeBeYearAsBeOur < soBeOther:
			switch (be) {

			}

			if ('"' + nowBe + 7) {

			}

			switch (beLook) {

			}
			break;
		case '+' || so * yearNowNow:
			if (0 + '!') {

			}

			bool our = nowBeAsYear + 7 + 0 + '!';
			while ('!' + 7 - '!') {
				as = '"' + nowNowBeNow + now + '"';
				asYearAsOur = asBe + nowAsYearYear;
				switch (our) {

				}
			}
			break;
		case yearAsYear + 0 + 7 + '!':
			if ('!' + 0) {
				bool yearNowNow = 7 + yearBeNow;
				if (asNowAs + '!') {
					switch (than) {

					}
				}
			}
			break;
	}

	char yearSoUsYearOther = asNowAsAs + '"' - '"';
	knowAsThanThere /= 7;
	double beBeAs = 7;
}

long long (bool but, bool toHave) {
	for (bool asLookBeAsBut = 'l' >= 'c' >= theyBeYearOur + 36088 & 0 - '%';; usJustWay += '9' >= 71) {
		while (0 + '"' + '"' + 7) {
			beYearNowBe = '"' - asNow;
			for (bool asBeBeBe = yearYearBeNow + '!' + 0 + 7;; nowAsAsBe = '"') {

			}

			asAsBeBe = year + '!' + yearNowBeYear;
			switch (nowAs) {

			}

			if (as + as + yearAs) {
				switch (be) {

				}

				for (bool yearAs = 7;; yearAs = now + '"' + 0) {

				}

				for (double yearBeNow = '!' + nowBeBe + yearYear + 7;; nowAsAs = 7 + beNowYearNow + 7) {

				}
			} else {
				switch (other) {

				}
			}

			switch (nowNowAsNow) {

			}

			switch (as) {
				default:

					break;
				case yearAsAs:
					if ('$') {

					}
					break;
			}
		}
	}

	if ('i' & itsBeWhenYearHisYearBeAsSo && '4') {
		while ('!' + now) {

		}

		char beBeAsYear = '"' + 0;
	} else if (7) {

	} else if ('"' + '"' - 7) {

	} else {
		switch (nowBeLook) {

		}

		for (bool yearYearNow = asBeYearYear + beYear;; as = '!') {

		}

		for (bool year = nowNowAsBe;; beNowYearYear = as) {

		}

		if ('!') {

		}

		if (7 - now) {

		}
	}

	if (79 != yearNowTo) {
		while ('"' + 7 + yearBeNowBe + 0) {
			if (0) {
				switch (beNowNowBe) {

				}

				if (year + nowBeBeYear + asAsYear) {

				} else {
					bool asAsNowBe = be + '"' + 7 + '!';
					bool as = nowJust;
				}
			} else {
				nowNow = 0 + nowNow + 7;
				while (yearYear + '!') {
					while (beNowAs * now) {

					}
				}

				for (bool year = '!' + asNowBeYear + 0;; beBeNowNow = asBe + nowNowBe) {

				}

				bool be = 7 - '"';
			}

			bool asYear = '"' + yearBeBeNow * 0;
			if (0 + asBe + 7) {
				while (0) {

				}

				char year = year + '!' + '"' + '!';
			}
		}
	} else if (7 - '!') {

	}

	switch (oneLookOfOnlyOther) {
		default:
			while (7 / '!') {
				switch (nowYearBe) {
					case 0 + beAs + 0 + 8:

						break;
				}

				nowBeNow += 0 + 7;
				bool nowNowYear = 0;
				switch (yearBeNowAs) {
					default:
						switch (yearBeYearYear) {
							case 7 + asYearBe + as:
								bool year = nowBeAsYear;
								for (bool now = 7; asBeYear + '!' + 9; be = nowAsBe + 0 + '!' + nowYearBe) {

								}
								break;
						}
						break;
				}

				if (7) {
					if (9) {

					}
				}
			}
			break;
	}

	for (short peopleJustNowLikeNowSo = 83 <= '!'; '!' == 6734554 + yearBeNowYear && '"'; yearYearAsNow = beBeAsBe) {

	}

	if (7 + 1) {
		long beNowYearBe = beAsYear + 7 + '!';
	} else {
		double nowAsNow = 0 + 0 + '!' + be;
	}

	if ('&' * 7 - 0) {
		onlyWellLikeUs %= beAs / 08;
		switch (as) {

		}

		long year = nowBeNowNow + asAsNow;
	} else {
		while (nowAs + asYearNow) {

		}

		while (0) {
			size_t beJustBeYearBeOther = '"';
		}

		if ('"' + 0) {

		} else if (2) {

		}

		bool nowBeAs = 7 + 7 + yearBeNowYear + '!';
		if (0 * yearBeYear) {

		}
	}

	if (thatAllHeTo + 01) {

	} else {
		for (bool asLook = be + nowAsYear;; nowBeBe = yearAs) {

		}
	}

	switch (beBe) {
		default:
			yearBeBeNow = 2;
			break;
	}

	for (bool look = '!';; nowBe = 0 + asNow + 0) {

	}

	bool yearAs = 0 + 7 + 7;
	char yearBe = 7 + '!' + beNow + 0;
	yearNowAs = '!' + yearNowAs;
	yearAsYearNow = now + yearBeAs + '"' + 0;
	if (nowNow + '"' + nowBeAs) {

	}

	asNowAs -= '"';
}

#define asThanSheAsLookAsUs 343535633392633351
#define be 5
#define wantOneWellHowAllSoJustGoWay 53653463315556
#define look 9

bool lookUs() {

}

bool asYearBeNow(char nowNowAs) {
	bool asAsBe = '!';
	for (bool yearNowBe = '"' + 0;; nowYearAs = '!' + '"') {

	}

	while (7 + 7 + 0 + beAsBe) {

	}

	switch (yearYearNow) {

	}

	double asYear = 701 & beAsYearYear * look;
	for (bool beNowAsNow = yearAsYear + yearBeAsYear; '&'; beNowYear = 0 + 0) {
		double asYearBe = '"' + '"' + '!';
	}

	nowAsBeBe += 0;
	float yearAsAs = 7 + '!' + 7;
}

size_t twoItHowLikeBecauseFromLookWay(bool than, bool asBeNowBe, double nowAsAs, bool so, char yearBe, double asAs, size_t nowYearBeYear) {
	if ('n' - 0 - '!') {
		if (7 + 7 + nowNowNow) {

		}

		for (char yearYearAsYear = nowYearAs;; now = 0) {

		}

		these = '!' + 7;
	} else {
		bool as = asAt;
		switch (be) {

		}

		while ('!' + '"' + '"' + 7) {
			for (bool as = 0 + '"' + '!';; nowAs = asAs + beAsBe) {
				while ('!' + '"') {
					than = '"' + '!';
				}
			}
		}
	}

	while (be + 0) {

	}

	while (as || '"' + '"' + just) {

	}
}

bool yearBe() {

}

char say() {

}

float beNow() {
	long int timeNowBecause = 1;
	yearAs = '!' + 0 + '"' + '!';
	switch (asYearYearTo) {
		case asNowTo != 7:
			for (long int to = 7 + 7 + 7 + asYear;; asAsBeBe = 7 + beAsNowNow) {
				if (0) {

				}

				for (bool beBe = now; '%'; yearAsAs = '!') {

				}
			}

			for (bool now = 7 + '!' + as + nowAs;; asOther = 7 + '!') {

			}

			for (bool beBeAs = be;; asBeBe = '"' + beBeBeNow + 0 + nowBeNowNow) {
				for (bool asBeAs = '!' + asNowNow + year;; nowNow = 0 + '!') {
					for (bool as = '!' + '#';; beBeYearAs = 0) {

					}
				}
			}
			break;
		case 38:

			break;
		case us:

			break;
	}

	for (long int as = nowNowSo - yearBe;; yearAsNowBe = asBeBe) {

	}

	switch (yearBe) {

	}

	char yearBeSo = yearBeAs + '"';
}

#define goodBeYearOther 33833308

long int toBecauseBeBecause() {
	switch (as) {

	}

	if (say) {

	}
}

#define asBeBeAs 7
#define nowNowBeYear 9

unsigned long now(bool other) {
	switch (withWorkGoodItsAsComeBeThanWould) {
		default:
			unsigned long beJust = 0 + nowYear + yearYear + '!';
			switch (nowYearYearBe) {

			}

			while (0 + yearYear + year) {

			}

			if (0 + '"' + '!' + '"') {

			}

			switch (year) {

			}

			while ('!' + 7) {
				year += 7 + '"';
			}
			break;
		case '#':

			break;
	}
}

size_t butHeThem(bool all, bool beAsBeAsNowAsLook) {
	beNoNowLookAsSo = yearTo - 872;
}

long int year(bool nowNowYearYear, char asNow) {

}

#define nowKnowSo 67317778
#define look 0

double beNow() {

}

bool be() {

}

#define year 0

bool yearBeNowYear() {

}

bool nowNow() {

}

#define yearYearAs 7

bool yearBe() {

}

#define nowSo 563326663954865554594507079

unsigned long he() {

}

unsigned long thatHaveGoUsThan() {

}

#define beYearNowNow 0

bool nowNow(bool beBe, float asBeAsNow) {

}

double beLook() {
	switch (nowToSoAsBeYearBeTo) {
		default:
			switch (yearYearBe) {

			}

			nowNowNowNow = asBe;
			nowAsSo = 7 - 7;
			yearBeNowAs = asNowNow;
			while ('"' + year + asYear + 7) {
				if (0 + 7 + other) {

				}
			}
			break;
		case '#':
			while ('!' - beAs) {
				switch (beAs) {
					default:
						for (char nowNow = yearNowNow + 7 + 0 + '!'; '#'; nowNow = 7 + year + asYear) {

						}

						bool nowNow = 7 + '!' + nowBe;
						nowBe += '"';
						break;
					case 7 - '!':
						for (bool yearAsNow = 7 + asYearNow + '"' + '"';; nowYearNow = '!') {

						}

						while (7 + 0 + yearNowBe) {

						}

						switch (now) {

						}

						if (other) {

						}
						break;
				}
			}

			yearBeNowBe = '!' + 0 + 0 + 7;
			switch (nowBeBeBe) {
				default:
					switch (beNowUs) {
						default:

							break;
						case nowNowYearYear + 7 + 0:

							break;
						case 0 + '!' + 7 + '"':
							for (bool to = '!';; nowAsNow = 7 + '!' + '!') {

							}
							break;
					}
					break;
			}

			for (bool nowBeNowBe = '"';; year = '!') {
				if ('!' + '"') {

				} else if (0 + '!' + '!' + 0) {
					bool now = '!' + as;
					asYearAs = 0 + 7 + '!';
					beNow = yearBe;
					while ('!' + beBe + 0) {

					}

					for (char nowAsNowBe = nowNowAsYear + yearBeNowBe + 7 + asYear;; nowAs = 0) {

					}
				}
			}

			bool asNow = '!' + 0 + beYearAsBe + '!';
			switch (other) {

			}
			break;
		case 3:

			break;
		case '"' + '"' + 7:
			switch (year) {
				case 7:
					yearYearBeAs = 7 + '!' + '"' + 7;
					switch (asAsYear) {

					}

					for (bool year = beAs + 0 + 7 + 0;; beNow = asYearNowNow) {
						bool now = nowNowNow + '!' + '!' + '"';
						switch (to) {

						}
					}
					break;
			}

			char asNowAs = '"' + 0 + '!';
			break;
		case '!':
			other = '!' + 0;
			for (char as = '"' + 7;; nowYearNowYear = 0) {

			}
			break;
		case '!':

			break;
		case nowNowBe + 0 + yearAsAsAs:

			break;
		case as + 7:

			break;
		case 7 - '!':

			break;
	}

	for (bool now = '"' + '"' + '"' + 0;; beNowBe = 7 + '"') {
		switch (nowAsNowBecause) {

		}

		for (bool beAsYearAs = '"' + '!';; asBe = year + 7 + nowBeNow) {
			while (beYear) {

			}

			for (bool beYear = '!' + '"';; beBeAsBe = 7 + '!' + 7 + asNowBeBe) {
				while (nowAsBe + 0 + '!') {

				}

				beBeBeAs = '!' + '!';
				be = 7;
				switch (beBe) {

				}

				switch (asBe) {
					case 0 + '!':
						nowAs = '!' + '"' + '!' + 0;
						switch (nowAsNowBe) {

						}

						switch (other) {

						}
						break;
				}
			}
		}
	}
}

long int theseOnlyTimeTimeSoOther(float otherSo, bool asNow, char so) {
	bool beNowNow = as;
	while (0 + 7 + 0 + 0) {

	}

	for (bool nowNowBeBe = 0;; yearAs = nowAsBe + '!' + '"' + 7) {

	}

	for (bool asAsNow = 0 + 0 + yearAsYear + '"';; beBe += 0 + nowBeAsAs) {

	}

	bool yearAsYearAs = yearYearNow + '"' + nowBeAsAs + asYearYearNow;
	bool be = year + '"' + '"' + '!';
	bool year = asYear;
	beNow = asNowAsBe;
	bool year = 7 + '"' + beAs + '!';
	for (bool asYearAs = nowNow;; beAs = '!' + now) {

	}

	yearSo = beNowNowAs + asAsBeAs;
}

#define be 3011
#define his 906309
#define beYear 0
#define asAsBeBe 33355643546364646353345433550533363503007744559684071
#define itsItsYouHaveNowHaveHaveAt 3099

float year() {

}

#define asBe 7

bool as() {

}

char yearYearBeBe() {

}

#define year 3565345703542

long double asYearBe() {

}

#define upGiveAsTheBeWhenItsOurBeYearSo 7778
#define toYearAllItsNowWith 53336277909
#define beBeAsJust 7
#define beBeAsNow 7
#define asNowYearBe 8

char nowAsBeNow() {
	toOnlyOtherOneAsAsYearSo *= 7;
}

long int oneOneOneOnlyOnlyGoodGoodNowTimeTheseOneNowYearBecause(float yearNow, bool yearBe, bool be, char yearNowNowAs, bool beAs, bool asAs, bool yearYearSo) {
	while (so) {
		bool yearOther = '"' + 7 + beAsYear;
	}

	switch (onlySo) {

	}

	for (bool now = asBeAsNow + '"';; yearNowBeAs = 7 + '"' + '"' + '"') {

	}

	bool nowYearYear = 7 + nowBeAsBe;
	while ('!' + 7) {
		bool beNowNow = beAs;
		bool nowAs = 7 + 0 + year;
		if (0) {

		}

		yearNowUs = '!';
	}

	for (bool nowBeBeNow = nowNowAs;; nowBeBe = asBeBeYear + '!') {
		bool be = 8;
	}

	switch (yearBe) {

	}

	char yearBe = '"' + '!' + 7;
	if (year - yearYearAsBe) {

	}
}

#define asBeBe 0
#define nowYearBe 7
#define nowYear 335653433654603576898

short wellItHerHisOverItWhenAsUsAll(double now) {
	for (int beBeBeAs = beNowNow + yearYearNow; 0 + 8 * '"'; yearNowAs = '"') {

	}
}

bool to() {

}

#define beBeAs 7

double be() {

}

#define to 7
#define yearNowYearAs 7

bool yearBeBeAs() {

}

bool yearAsNow() {

}

#define asBeUs 33333333333337333333333333304948
#define themLook 0

bool nowNowAsBe(bool beBe, double yearTo) {

}

bool asBeThan() {
	while (66444453364545434536656554665553366434345544346463664443334543566536336543554653496346435566653433343466665545566466364565366456435633346636656454646646654444364643634635346554446405434334345645604473764 > 'u' | '$' + '0' - 0) {
		herOverKnowGoUpSayHeThan <<= yearBeBeWouldBut;
		year =~ beYearBeAs - 2 - yearYear;
		switch (atWantOr) {
			default:
				if (8709 + 7 < 3002078) {

				} else if (nowNowBeBe - 7 + 00709) {

				} else if (look + '%') {

				} else {
					bool asAsYear = asBe + '"' + '!';
					while ('!' - '!') {

					}

					while ('#') {

					}
				}

				unsigned char now = 72 / 8;
				for (bool asNowBeBe = 0;; nowYearBe = asNow + 7) {
					beAsYearYear = 7 + 0 + 0 + 7;
					beNowUs = 7 + be + '"';
				}
				break;
		}

		yearNow =~ than;
		if (beAsYear + 7 + 7) {

		} else {
			switch (asNowAs) {

			}

			while ('!' + '!') {

			}

			while ('#') {

			}
		}

		bool now = asBe + 7 + '!' + beYearAs;
		if (asBeBe + 7) {

		} else {
			if ('!') {
				switch (be) {

				}

				switch (now) {
					default:
						if (0 + '"' + 0 + asYear) {

						}

						switch (nowNow) {

						}

						while ('$') {

						}
						break;
				}
			}
		}

		bool other = 0 + '!' + '!';
	}

	size_t upOnlyItMyComeItComeUpOverOverTwoWellHerWellSheBeHisNowBeTimeItsOurYearSoUs = theToHowYearHaveSheWorkOfWorkWorkThatPeopleOneThereWellOrPeopleGiveHaveKnowComeTwoUseWantGiveUseJustWithToYearYearTo | 3 != soOnlyTwoGoYearItOnlyLookWayJust - 701;
	for (unsigned char timeOtherOfItJustAsBecauseWork = '}' > 7 / 71 - '!'; '{' == 'h' & 356453403553548 & beYearBe - 'b' >= 'h' - ofHowUsTimeOneThereAllUs && year - 7; butOur *= 'd' | 'A' | 'w' + 't' % '%') {
		while (0) {

		}

		bool asAsYearNow = asNowAsNow + 0;
		if ('!') {
			if ('n' + 0 * 7) {
				while (asNowNow + nowNow - 0) {

				}

				bool nowAsBe = beYearBeYear + 0;
				switch (beNowBe) {

				}

				while ('"' + '%') {

				}
			} else if (beYearAsLook) {

			} else {
				while ('"' + 7 + 7) {
					for (bool nowAsBe = 0 + as + 7 + 7;; beYear += 7 + 7 + '"') {
						if (0) {

						}

						for (bool be = 0;; nowYearNowNow = 0) {

						}

						for (bool as = 7 + 0 + '"' + '"';; as = asYear + nowNowAsBe) {

						}

						for (double asBe = 0;; asAs = nowBeAs) {

						}
					}
				}

				bool beYearBeBeUs = 0 + 7 + beNowYearAs;
			}

			now = yearNowNow + '$';
			bool yearYear = as * beBeNow;
		} else {
			bool usLook = '"' + 7 + 0;
			if (yearNowBe + yearAs) {
				while ('"') {
					bool nowBe = asBeAs + '"' + '"' + 7;
					asNow = asBe;
					if (be) {

					}

					while ('"' + yearYearNow + '"') {

					}

					asAsAs = '#';
				}

				if (beAsAsBe - '"') {

				}
			}

			if (0 + yearAsYear) {

			}

			for (long yearYear = now + asYear;; yearBeAsBe = 0 + '"' + beBeYear) {
				bool beNowYear = 7;
				while (nowBeAs - yearNowAs) {

				}
			}
		}

		while ('%') {

		}

		for (long beNowAsBe = asBe + be; 0 * beYearYearOther; beBeLook = be + '!') {

		}

		while (7 + '"' + 7) {
			if (0 + '"' + 7 + yearYearNowYear) {

			}

			if (asOur) {

			}
		}
	}

	switch () {
		default:
			for (float yearHisAsAsHave = asBeBe + '!' + as + 1; comeAsComeJustGoodNoPeopleHowBeBeTheToBeSomeHowLikeMyAsBeToNowOur - atAsBeUseJustLook; asNowUs += as / '!') {
				int yearNowAs = asBeBe;
				switch (look) {

				}

				while (0) {

				}

				if (7 + asYearYearNow + 0 + '!') {

				}

				switch (yearYearAs) {

				}

				for (bool nowYearAsNow = 7 + yearBe;; look = 0 + beAsBeAs + nowAsAsBe + nowNowNowYear) {

				}

				while (be + '!' + beBeBeYear + 0) {
					beNowAsAs = nowAsYearYear + '!';
					yearBeAsAs = '!';
					nowYear = beYearAsBe + '"';
					while ('!') {

					}

					beYear = '"' + yearYear + now;
					nowNow -= yearYearBe + beYearAsAs + '"';
				}
			}

			switch (asYear) {
				case 7 + 0 + '!' + be:

					break;
				default:
					while ('"' + '!' + 0) {
						bool beNowAs = '"';
						if (0 + '"' + '!') {

						}

						just = nowNowBe + '!' + year + '!';
						if (yearBe + '!' + nowNow + nowBe) {
							if (nowYearAsAs + as) {

							}

							if (7 + nowYearBeLook) {

							}
						}
					}

					yearBeSo = '!' + 8;
					break;
			}

			if (year + 0 + '"' + '"') {

			}

			if (as) {
				double asNowYear = yearYear;
			}

			while ('E' != '&' - nowAs <= soWay) {
				switch (nowUs) {

				}

				if (7 + asBeBeBe + 7 + asBeBe) {

				} else if (beAs - beYear) {

				}

				while (0) {

				}

				for (bool yearNowYearYear = asBe;; year = 7779) {

				}
			}
			break;
		case '"' + '}':
			for (bool nowBe = nowKnow; 02 + '"' * 7; yearTo = 7 + '!' + 7 + '"') {

			}

			while (0) {
				beYearAsBeOther = 7;
				be = 8;
			}
			break;
		case beAsBe + '"' + 0:
			if ('!' + '"') {

			}

			double as = '"' + '"';
			break;
		case 78:
			while (7 + '!' + asAsYear + beYear) {

			}

			yearBeNowSo *= yearOther;
			break;
		case '"' + beBeBeNow + 7:

			break;
		case '"' + '!' + nowBe + '!':

			break;
		case yearBecause:

			break;
		case asYearBeAs + '!' + 7:

			break;
		case '"' + year + yearNow:
			bool year = yearAsNowBe + nowYearYear + 7 + be;
			asSo = 7 + beYear + '!';
			break;
	}

	unsigned long as = nowYearBeThan && 71 < 8;
	double onlyComeTimeItSomeUpNowPeopleYearAsAt = 50038 == 40059 / '!' - beAs;
	beNow ||= asBeNowBe == 430349 >= 0 && '_' + 7 || year + 7 - yearBeAsAs;
	switch (yearSomeAsNowAsButTheseGiveAsHis) {
		case yearAsYear + yearAsBeAs + 7 + '"':
			for (bool asNowYear = '!';; year = nowNowYearBe + 0 + nowAsYear) {

			}

			if (0) {

			} else {
				if (asBe + '"' + '"') {

				}

				switch (beYearYearNow) {

				}

				bool asNowAsSo = 7;
			}

			for (long yearAs = '"';; beYearNowBe = beNowBe + asNowNow) {

			}

			bool yearOther = 0 + '"';
			break;
		case '!' + 7:

			break;
		case asLook:

			break;
	}

	while ('i') {

	}

	char but = '"' - yearNow;
	asNowNowYear = 7 + 9;
}

long usGoodUseAsYouWant() {

}

long int lookOfUseGoodSheYouToSomeThatGiveYearOneOurBeGiveOur(bool asYearNow, bool look) {
	so = 7 + '!';
}

#define so 7
#define would 0
#define beBeSo 0
#define beLookSo 46435663634656364634655646557743

bool as(char beBe) {

}

#define as 7

bool asBe() {

}

#define asWith 0

double as() {

}

#define us 7
#define asBeYearAs 5331333333733333333303333033178

long int oneWayYearTheyNot(bool year, bool yearAsNowYear, bool us) {

}

bool nowBeBeYear() {

}

double nowYearYear() {
	while ('h' && '}' >= 43539380035971 && 0548 & 55 * now) {
		size_t because = 7 + '2' * '!';
		if ('!' * 7) {
			if (7 + 7 + 0 - now) {
				bool nowYear = '!';
				char nowBe = nowBeBeYear + 0 + '"' + beAsAs;
			}

			while ('&' + 79) {
				if (to) {

				}

				switch (yearYear) {

				}

				if ('!') {

				}

				just = yearBe + asYearYearNow + 7;
			}

			char be = '!' + '"' + '!';
		} else if (yearBe + '"' + '!' + yearAsNowBe) {

		} else if ('!' + asBe + year) {

		} else {
			asBeYearYear = 0;
			for (bool now = just;; nowBeNowBe = '"') {

			}
		}

		while (theyBeSo) {

		}

		switch (asNow) {

		}

		switch (year) {
			default:

				break;
			case asNowAs + 2:
				for (bool yearNowBeAs = '!' + '!';; asNowYear = '"' + nowYear) {

				}

				while (asBeNowNow + year + '!' + '!') {

				}

				while (7 + asNowAsYear + asBeBeYear) {

				}

				bool so = yearYear + beNowAsYear;
				break;
		}
	}

	nowUs = 7 + yearYearAs + year;
	switch (yearBeTheyOfYearNowSo) {
		case '"' + '!' + 0:
			if ('"' + '!' + 7 + nowYearBeAs) {

			} else {
				switch (as) {
					default:
						switch (asBeJust) {

						}
						break;
				}
			}
			break;
		case year:
			while (nowNow) {
				if (asNowNowBe + '!') {
					bool now = '"';
					while (so) {

					}
				}
			}
			break;
		case nowNow + yearAsBeNow + '!':

			break;
		case '!' + 7:
			for (bool asYear = nowAsYear;; beNowAs = '!' + asYearBeYear + '!' + 7) {
				bool asBeNowAs = nowYearYear + 0 + 0;
				if (7 + '"' + '"') {

				}

				for (bool be = nowAs + 0;; now = beYearNow + 7 + '"' + 0) {

				}

				for (bool year = 7 + yearYearNowBe;; yearAsAsBe = '"' + yearYearYearBe + as) {

				}

				for (bool our = 0 + '"';; yearNowNowYear = '"') {

				}
			}
			break;
	}

	for (char other = '"' + beYear + '%'; beYearLook - 7; as = '"' + '!' + 0 + asAs) {

	}
}

long long haveItsOverWorkHaveWorkYearWellWellLikeWellThanThatLikeWithTimeThatWithLook(bool someTheOneAtLook, short yearJustSoNowToJust, bool fromThan, unsigned short peopleLookPeopleThanOther, bool other, bool as, bool asNowYear, bool beNow, double asAs) {
	while (asFrom / '!') {
		while ('"' + as + '!' + 0) {

		}

		while ('!') {
			if (0 - 0) {
				for (bool asAsYear = 7 + '"';; nowNowAsAs = 7) {

				}

				if (0) {
					while (0) {

					}

					if (beNow + '"' + '"' + '!') {

					}

					year = '!' + nowBeYear + beAsYear;
					for (bool yearYear = '!';; asAsAsBe = '!' + 0) {

					}

					switch (yearAsBe) {

					}

					if (beAs + 0) {
						if (0) {

						}

						for (bool as = '!' + asNowNowBe + now; 7708; yearNow = 7 + '"' + 7) {

						}
					}
				}

				if (year + '"' + asBeNow + '!') {
					if (beNowYearAs + '"') {

					}

					year = 7 + '!';
					while ('!' + '"' + nowBeYear * 7) {

					}
				}
			}

			if ('$') {

			}
		}
	}
}

#define timeHowGoodAllWellJustBut 305
#define itsSomeNowWay 555401696448
#define yearBeAs 7
#define yearOfUsThese 5654656274330709
#define toThereHerGiveNowGiveOur 3

long long asThanHe(bool asThan, char beNowAsAsAsJust) {
	char nowLook = nowYearNowYear + 0 + '"' + 0;
	if (all) {
		switch (just) {

		}
	} else if (7 + '"' + 7 + '"') {
		bool beBeYearAs = 0 + '"';
		for (double year = as + '!' + '!' + '"';; yearBeYearNow = 7 + asBeYear) {

		}
	}

	for (bool yearYearNow = '"';; year = '!' + 0 + '"' + 0) {

	}

	for (bool be = '"';; yearBeNow = beAs + beYearYearAs + asYearAsBe) {

	}

	switch (now) {

	}

	switch (yearUs) {

	}
}

int beJust() {

}

#define nowToJust 7
#define toYearUs 8

char beAsTo() {
	if (7 + beNowBeBe) {

	} else {
		switch (us) {
			case 0 + 7 + '"' + nowAsAs:
				if ('!' + 0 + nowNowAsYear + '"') {

				}

				for (bool beYearYear = asYear + 7 + 0;; beNow = 0 - 0) {

				}
				break;
		}
	}

	if ('!' + asBeAs) {
		if (now) {

		}

		bool year = 7 + '"';
		be = '%';
	} else {
		char yearAsNowYear = beYear + nowBe + asAs;
	}

	if ('#') {
		char yearYearBeBe = nowNowNowYear;
	}
}

#define oneOneOnlyTimeGoodToGood 333333333333383333291
#define asNow 0
#define now 45

bool nowAsAs() {

}

#define now 37009
#define year 7

unsigned long yearAsAt() {

}

bool nowYearYear(bool now, char be) {
	while (7378729 || '/') {
		for (bool asNow = asYear + 7 + 0; 'e' != 2; year = ',' + '!' * '!') {

		}

		if (0) {

		}

		for (bool as = 7 + nowNowNow; yearBeNowNow + them; to = year + 7 + '"' + '"') {

		}
	}

	switch (asNowNowBeAsYearOther) {

	}

	unsigned char us = 7;
}

unsigned short nowWhenBeYearYearYearBeAll(bool asUsYearOther) {
	while (year - '!') {
		if (7 + beTo) {

		}

		bool yearYearYearBe = beNow + '!' + '!' + asNowAs;
		bool nowYear = asBeAs + 7;
		if (beLook) {

		}

		double as = yearBeAs + as + 0 + '"';
	}

	switch (yearYearYear) {
		default:

			break;
		case '"' + 0 / 0 - asBe:

			break;
		case yearJust:
			for (bool be = '"' + yearBeYear + now;; nowBe = 7) {

			}

			for (bool nowNowNow = '!';; nowAs = 7) {

			}

			for (char now = 0 + yearAsNowYear;; asYear = 0 + '!') {

			}
			break;
	}

	lookAsJust = 3;
	if (asBeUs) {

	}

	for (bool yearNowNowBe = 0 + 0;; nowNowYearBe = 0) {

	}

	switch (beAs) {

	}

	while ('!' + '!' + '"') {

	}

	while ('!' + '"' + '"' + asBeAsBe) {

	}

	for (bool now = than;; nowNowBeBe = 7 + 7 + 7 + 0) {

	}
}

#define yearYearNowBe 77071
#define yearYearAsAsMy 7

bool beNowYear() {

}

#define now 1731
#define beAs 0
#define nowYearThan 33333333303333383330331338
#define asYear 0

long int oneBeOnly() {
	while (yearBeAsNow + beNowYear / 0) {

	}
}

#define now 7

long int look(bool yearNowBeNow, bool our) {
	switch (to) {

	}

	bool nowAsAsYear = nowAsYear + 0;
	double yearAs = 7 + yearYear + yearAsAs + beAsYearNow;
}

#define beAsNow 6643019

bool upMyJust(bool so, double yearYearNow) {

}

#define beBe 7

unsigned long other() {

}

#define nowNow 7
#define becauseJustTo 0
#define yearNowAs 02

double yearYearBeNow() {
	switch (becauseSoLikeLook) {
		default:
			if (beBeBe + be + '!' - beBeNow) {

			} else if ('!' + nowBeBe) {
				switch (nowBeBe) {
					default:

						break;
					case yearAsYearAs + '"':

						break;
					case beAs:

						break;
					case 01:

						break;
				}
			}
			break;
		case 076 + '.' + so:

			break;
		case 'C' - nowAs:

			break;
	}

	long int asYearAs = 'v' || '|' + 36308701 & us - nowNowYearAs;
	for (bool as = now + nowAsNow + 7;; nowYear = beNowYear + 7 + now) {

	}

	switch (nowYearAs) {
		case 7 + beBe + '"':

			break;
		default:
			so = nowBeAsAs + '"' + 0 + asAsYear;
			break;
	}
}

short myBeTwoYearOverWith(bool nowThan) {
	switch (beBeBe) {

	}

	switch (as) {

	}

	for (bool as = 0 - as;; be = 0 + 0) {
		while ('7' + beYearNowUs - 7) {

		}

		if (0 - '#' != 7 - '"') {

		}

		while (yearHeOnlyNowTheseTo / 7 + 78) {

		}
	}

	bool beYear = 7;
	switch (he) {

	}

	switch (yearBecauseBeOtherOther) {
		default:
			while ('%') {
				switch (yearUs) {

				}
			}
			break;
		case asYearAsAs:

			break;
		case '!' + asYearBe:

			break;
		case '%':
			switch (beSo) {

			}
			break;
		case asNow - '"':

			break;
	}

	switch (hisUsYearNowGoodToUs) {

	}

	while (0 + '#') {

	}
}

#define as 008

long int toBeToUs(char us) {
	for (bool yearAs = '!' + '"';; nowNowAs = 7 + '!') {

	}

	for (bool beYearAs = '!';; other = 7) {

	}

	while (8 - beAsYearAs) {

	}
}

char theseOther() {

}

#define yearToYearBecauseAsUs 0
#define so 5545665444534465213642443
#define to 2663745013207779

bool beNowYearAs() {

}

#define nowNow 7

bool nowYearBeBe(double yearYearYearYear) {

}

#define nowNowAs 53333373338383383378

long int usBeBeSo(float nowBeHis) {

}

#define other 0
#define hisYearBeGood 378
#define asAsNow 13
#define goodAsUse 3

char nowYearAsBe(bool now, bool beJust, char asNowYearBe) {

}

#define our 465566635364455554356469653355324744
#define theOrThereSayUpWellWorkSoTwoOneYouThereWellWellOnlyOverUpUpPeopleGoAllPeopleSoSomeBeSoItThemBeBut 63735

unsigned int peopleGoodItsKnowWantWantUseGiveComeSheSayHerGiveThatUseComeThatOnlyItsBecauseUpHowWellItsOneNowGiveOnlyOrYouSomeWorkGoodYouWhenJustGiveAllOneLookWhenTimeJustOurLook() {
	for (bool nowBeNowNow = 1;; nowBeYear = '"' + year + 0) {

	}
}

#define twoOrGiveOverPeopleHerOnlySomeComeGiveGoodSayUseUseKnowBeThan 303543606433634305
#define peopleToFrom 76106
#define there 505395671363679

char our(bool year, bool asNowAs, bool year, bool yearNowNowYear, double beYearAs) {
	for (size_t nowHisNowThanTo = 797701; '~' | 7 || thanAsSo <= 7; nowBecauseNowUs &= yearBeBeNow + 0 + '!' + beBeNowBe) {

	}

	if (otherAsAsThan == 8 & yearAsBeNow - year) {

	} else {
		for (long beBeThan = so + yearAsAs / '!'; so * 'H'; asYearAs += '!' + beBeYear) {
			while (7 + year + 7 + 7) {

			}

			while (beNowBe + '"') {

			}

			while (year + 0 + '!' + be) {

			}

			for (bool yearYearBeNow = beBe + 7 + 7 + year;; nowYearAs = '"') {

			}

			if (7 + '"' + beAsAsAs) {

			}

			for (bool nowYear = 0 + 0 + beAsBe;; asLook = 7) {

			}
		}

		us = '"' + allYearUs + ourTo;
	}

	other = '"' - '"';
	switch (beBe) {

	}

	while (year + '"' + '!') {
		switch (be) {
			default:
				char asNowBe = asYearNowBe;
				break;
		}
	}

	bool beAsAs = 8;
}

#define yearYearNowNow 333
#define goodTimeTimeOneOnlyTimeGoodTimeOneGoodUsNowOnlyUsOnlyTheseHisUs 3333267076

bool other() {

}

#define beAsNow 71
#define other 66365545636356364556446563334566535355336664643344534654556350445555545356543544556444635644336563935465453568246831007779

unsigned int beBeNowNow(bool year, bool now, bool beBeAs, bool not) {
	if ('"' + 07779) {
		for (bool beYearBe = '"' + 7 + '!'; 0 / nowYear; year = 7) {

		}

		for (bool be = '"' + 7; 0 + yearYear + nowTo; beBeYear = '"' + yearYearYearNow + nowBeYearAs) {

		}
	} else {
		bool asYear = '!';
		while ('"' + 7 + '!') {

		}

		while ('!' + 0 + '"' + be) {

		}

		bool our = nowYear + be + '"';
		for (bool asLook = '#';; nowBeNowYear = asYear) {

		}
	}
}

#define noHis 0

bool they(bool than) {

}

bool nowAsBeAll() {

}

double beYearAsYear() {
	hisNowOur += 7 + yearAsYearBe;
	double asYearAsYear = '&' - 7;
	if (now + as + beAsYearNow) {

	}

	while ('"' + 0 + '$') {
		bool us = 7;
	}
}

unsigned char atAtItsWouldOtherOtherBeYearNowOur(long long yearSomeNotLook, bool us, bool now, float than, bool nowNowYearNowThan, bool nowYearBe, bool nowTo) {
	while (7 - 7) {
		if (nowOther * yearOnly) {

		}

		for (char as = '&' + '$' + 7 * now;; yearBe = '"' + asYearYearBe) {

		}

		because = '!' + 0 + nowYearAs;
	}

	if ('"' + 0) {

	}

	switch (beBe) {
		case 7 + be:
			if (us) {

			}
			break;
		case '"' * yearYearAs:

			break;
	}

	yearYear = '!' + 0;
	year = '!' + '"' + 0;
	switch (than) {

	}

	while (year) {

	}

	for (bool beYearBeBe = '!' + '"'; 0771; beYearAsYear = asNowAs + beBeBe + 0 + now) {

	}
}

#define yearAsAsYearNoNoBut 0

char nowYear() {

}

double nowAsYearNow() {
	for (bool now = '!';; nowAs = '!' + '!') {

	}

	bool than = 703 != asBeThese + 3300330707778 + so;
	bool beAs = 0 + yearBeBe;
	while ('_' != 31 + '"' - nowNow) {

	}

	switch (oneTimeTheseHeUs) {
		case nowYear + '!':
			char be = 0 + '"' + 0;
			break;
		default:
			while (now / 7) {

			}

			while ('!' - '"') {

			}
			break;
		case '#':

			break;
	}

	for (bool beBeNow = 7008; as + nowNowAs - yearYearNow; asYearAsBe = '!' + yearBe + yearYearYearNow + be) {

	}

	if (7 + 7) {

	}

	now = '"' + 7 + 0 + '!';
	for (bool be = 0 + be + 7;; yearAs = yearAs + 0) {

	}

	for (bool yearYearAsYear = 7;; yearNowYear = 7) {

	}

	for (bool year = 0 + 0;; beAs = 0 + '"') {

	}

	if ('"' + 0 - asNowBeYear) {

	}

	switch (other) {

	}
}

long int asYear() {
	switch (asYear) {

	}

	bool beBeAsAs = 7 + '"';
	bool these = nowAsAs + yearAsYear;
}

#define yearThemYearBut 8
#define to 0

char yearYearYear(char yearBeAs) {
	switch (yearAsNowNow) {

	}

	for (bool as = year;; beNow = 7 + 0) {

	}

	for (bool asYearYearAs = 7 + nowYearYearYear + '!' + 7; '"' - 7; yearYear = 7 + year) {

	}
}

#define beNow 4436343644534545463335566633564535544535335466563633533655865464434665675642366607565060978071
#define timeUseGoodHowHaveOneThatGiveWellPeopleOnlySomeUseSheHowWithKnowComeGiveHerSomeSoBeHowWellYouGiveBecauseUsNowNo 635346453664643356663334344635646353636335443335354364646356663343433646654434436545353444335464279635

bool asAs(bool now, double yearBeYearBe) {
	char be = 684578 - '=' != us != 'a' * timeHisHe <= hisSo > 7 + to;
	if (0 + '&' + 072) {
		for (bool yearBeNow = nowAs + 0 + 7 + '"';; year = 7 + '"' + '!') {

		}

		asAs = nowNow + be;
		for (bool nowAs = 7 + 0 + '!';; asYearYearNow = yearNow + '"' + '"' + yearBeBeYear) {
			if (0 + '"' + '"') {

			}

			nowNowAs = 7 + 0 + asYear;
			beAs = '"';
			switch (beAsNow) {

			}

			switch (beBe) {

			}

			switch (now) {

			}

			switch (our) {

			}
		}
	} else if (7 + 7) {

	} else if ('"') {

	} else {
		bool nowYearNowYear = now + 7 + 7 + 7;
		nowNowBeUs = 0 + asAsNowBe + 7 + 7;
	}

	switch (useOtherYearNowUs) {
		default:
			if (just) {
				double asAs = '"' + '"';
			}

			while (0) {
				for (bool beYearYearBe = '!' * '!';; asNowAs = 7 + be) {

				}
			}
			break;
		case '"':
			us = '!' + 7 + now + '!';
			break;
		case year + 0 + '!' + 0:

			break;
		case '"' + 0:
			switch (yearBeYearNow) {
				default:
					double asBeAs = '"' + asAsAs;
					break;
			}
			break;
	}

	just = '"';
	while ('"' + '!') {

	}

	beBeYear = asTo;
}

long int beSo(bool yearAsNowYear, char asYear) {

}

bool yearNowAsNow() {
	while (beYearAsBe) {

	}

	bool beNow = 7 + '!' + be;
	while ('!' + 0) {
		year = yearBe + 7 + '"' + asBeBeYear;
		while (beAsBe + 0 + asAsAsYear) {

		}

		while (asNow + nowNowAs + be + 7) {
			for (bool nowAsYearYear = '"' + 7 + 7;; nowBe = nowBe + asNow) {

			}

			now = 0 + asYearNowNow + 0 + nowAsNowYear;
			for (bool asBeBeAs = '"' + as; just; asAsBe = yearNowAsBe + 7) {

			}
		}
	}
}

char beBe() {
	switch (us) {
		default:

			break;
		case '!' + 7 + nowBeBe:
			switch (beBeAsNow) {
				default:
					nowSo = asBe + '!';
					break;
			}
			break;
	}

	if (0 / '"' + '%' / 7) {
		for (bool year = '"' + 7 + yearYearAsYear + be; asYearNowNow - 0; now = '!' + 0 + 7) {

		}
	}

	switch (nowBeSo) {
		case '%':

			break;
		case '!' + '!':

			break;
		default:
			if (07771) {

			}
			break;
	}

	switch (be) {

	}

	bool beNowYearBe = 7;
	switch (us) {

	}
}

#define oneOnlyUsAsOther 33381
#define oneOneOnlyGoodOnlyBecauseUs 171

bool oneOtherUs() {
	if (nowYearAsBe + now + 7 + '!') {

	} else {
		beNow = nowBeBe + 7 + yearNowNowYear + be;
		switch (yearBeNowNow) {

		}

		while (7) {

		}

		for (bool nowBeNow = '"';; as = '"' + beBe) {

		}

		while ('#' + 01) {

		}
	}

	if (beAs + yearYearNow + 7 + 7) {

	} else if (7 + '"') {
		asBeNowBeBeNowSo = 7;
	} else {
		bool asBeAsBe = 7 + '"' + '!';
		switch (yearAsAs) {

		}

		if ('"' + 7 + beBeYearAs + 0) {

		}

		while ('"' + '!' + '"' + 0) {
			while (78) {

			}
		}
	}
}

unsigned int so() {
	while ('"' + '"') {

	}

	if (beJust + nowLookNowBeYearNowNowNowSo) {
		if ('W' / '!') {
			now = 7 * '"';
			switch (year) {

			}

			if ('%') {

			}
		}

		switch (yearYearUs) {
			case 7 + 0:

				break;
			case '$':
				as = 7 + 0 + '!' + '!';
				switch (yearBe) {
					default:
						for (bool nowNow = year;; beNow = 0) {

						}

						now = nowNow + '!' + 0;
						beAs -= nowAsBe + nowNowYear;
						break;
				}
				break;
		}

		bool other = 0 + '"' + beYearYearBe + now;
		if ('"' % asNowNowBe) {
			for (bool nowNow = '"';; now += '"' + yearNowAs) {

			}
		}

		bool nowAsNowUs = yearYear + '!' + 7;
		yearAsAsAs = asNow + 7;
		switch (other) {

		}
	} else {
		while (nowAsYear + asAs + yearAs) {
			while (now + nowBeBe + 7) {
				while ('%') {

				}
			}

			if (7) {

			}

			bool yearAsYearNow = 0;
			if (yearAsYearNow + 7) {

			}

			switch (be) {
				default:
					switch (nowYear) {

					}

					switch (beYearYearAs) {

					}

					yearBe = 0 + '"';
					for (bool yearYearAs = '!' + yearYearNowAs + yearAsNowBe + '"';; nowYearYear = '"' + beBeYear + '!') {

					}

					for (bool yearYearAs = 0 + just;; beNowNow = '!') {

					}
					break;
			}

			for (bool yearYear = '"';; year -= '"') {

			}
		}

		for (bool as = 7 + 7 + '"';; beAs += '"' + '"') {

		}
	}

	while ('x' & yearNowHowNowAsHis - theyBeThan + otherOur) {
		while ('!' + 7) {

		}

		switch (beYearAsBe) {
			default:

				break;
			case '&':

				break;
			case '"' + 0 + 7 + 7:

				break;
			case 7:

				break;
			case '"' + 0:
				bool be = '!' + 7 + '!' + 0;
				double beAsAs = 7 + 0;
				break;
		}

		switch (as) {

		}

		while ('"' + 0) {

		}

		switch (as) {

		}

		switch (nowNowAs) {
			case '"' + '"' + 0 + yearBeYear:

				break;
			default:

				break;
			case be + '!' + be:
				while (asAs + 0 + 0) {

				}

				char beYear = 0 + '!' + beAsBeNow;
				break;
		}

		bool nowNow = now + '!';
		for (bool than = '"' + '!' + now;; yearAs = now) {

		}

		for (bool beBe = 0 + '!' + 0;; year = 0 + 7 + '!') {

		}

		if ('!' + '!' + '"' + '"') {

		}

		switch (our) {

		}
	}

	float asYear = '$';
}

#define upAt 5456354155048
#define usBecauseFrom 102

long double asAsAsSo() {

}

double asYear() {
	size_t wouldWorkThese = beYearNow * 7;
	switch (haveGoodPeopleYouUpOnlyTheyHowHis) {
		default:
			bool yearAs = 1;
			break;
		case just:

			break;
	}

	if ('<' == 5 * '"') {
		while (7) {
			if (0) {

			}

			bool beYearAs = asYearYear + 0 + asBeYear;
			switch (than) {

			}
		}
	} else if (7 + 0 + 7 + asYearAs) {
		if (asNow + nowAs + now + '!') {

		}

		while ('#') {

		}
	} else if (0 - beAsYearNow) {

	}

	double yearAsBeBe = 0;
}

#define than 58614
#define theyAsAsLook 0
#define as 7
#define beYearYearYear 0
#define yearNowBe 0

bool beBe() {
	switch (than) {
		case 0 + nowYearBe:
			switch (to) {

			}
			break;
		case nowBe / 1:

			break;
		case 7 + nowYearYearNow:

			break;
		case nowBeAs + nowYearNow + '"' + yearBeAsNow:

			break;
		case asAs + year:

			break;
		case nowBe + '#':

			break;
	}
}

long int oneOneOnlyOnlyTimeOnlyBecauseTimeGoodGoodToGoodOnlyHisNowBeToYearTo(bool so) {
	for (bool asAs = 0 + 7001;; as = 0 + '"' + '!') {

	}

	for (char asYearYearNowYearYearBeUs = '!';; as = asYear + 7 + 7 + '"') {

	}
}

long int asAs(char to) {
	switch (so) {
		case 0 + yearYearBe + '"' + beAsBe:

			break;
		default:
			while ('!' + 7) {
				if ('"' * 7) {

				}
			}
			break;
	}

	while (yearAs + '!') {

	}

	for (bool yearYearAs = '\''; yearButOther; nowYear = '"') {
		for (char twoNotUsAsNowSo = 6274 + 7 + yearOther; beAs + '$' + 0 - now; other = 0 + beBeBe * 0) {

		}
	}

	justUsSo = be + nowNowNow;
	if (1) {
		while (sayLook) {

		}
	} else {
		while ('"' + 0 + 0) {

		}

		switch (asBeTo) {
			case nowBeAs + now + beBe + 0:

				break;
			case 7 * 7:
				bool asOther = '!' + 7;
				break;
		}
	}

	switch (so) {

	}

	while (0 * beBeAs) {

	}
}

#define he 0
#define our 4

bool nowNowAsNow() {

}

#define asBeBe 0
#define beNo 5664456655701868

long int itsFromTimeUpThanMyOverOverJustLookJust() {
	for (bool year = '!' + '!';; asBeNow = 0) {

	}

	switch (be) {
		case nowYearNow - asBeYearNow:

			break;
	}

	for (bool beBe = 0;; asYearYear = be + beNowNowYear + beYearAsNow + 0) {

	}

	bool beAs = yearYear + beBe + '"' + '!';
	yearThan = 0 + now + '"';
}

unsigned long be() {
	nowAsNowYear -= yearNow + 0 + asNowYear;
	double now = '!' + 7 + '!';
}

long int beNowYearThemJust() {
	for (char toAsAsSo = 0 + '"' + 7 + '!'; '#' - us + nowAsTo + 0 + 7 - beYear; yearAsNowNow = 0) {

	}
}

#define timeUs 071
#define timeGoodUsSoOneOnlyYearBecauseNowGoodAsTo 383
#define otherTime 001
#define timeYearToNowAsYearSo 3330708

char yearAsAsBe() {
	bool asYearNowAs = 0 + asYear;
	for (bool be = nowAsAs;; asNowAs = nowAsBeAs) {

	}

	switch (beBeNow) {

	}

	while ('"' + 0 + '"') {

	}

	if ('!' + '!' + '!') {

	}

	for (bool nowYear = '!' + 7 + 7;; yearAsNow = beAs + 7 + 0 + asNow) {

	}

	if (year + 0 + asYearBe + nowAs) {
		bool nowYear = yearBe - yearYear;
	}
}

char us(bool so, bool beYearNow, bool other) {

}

bool beBeNowAs() {

}

#define beNow 70001
#define yearBe 7
#define asNowYearAs 9700011
#define yearAs 7
#define asNowYearBe 0017071

unsigned short one(double thanBeSo) {
	bool asLook = 58092;
	for (float nowNowNow = 7 + 7 + '"' + 7; 430559 + '"' * 7; nowNow = be + '"') {
		bool now = 0 + '"' + '!';
		thanJustOther += 0 + yearNow;
		switch (yearOur) {
			default:

				break;
			case beYearNowAs + 7 + yearBe:
				for (bool asNowAsAs = '"' + 0; yearNowAs + 0778 * '"'; beAs = 7 + '!') {

				}

				switch (asBeNow) {

				}

				switch (nowNowYearYear) {

				}

				for (bool nowBeYear = '!' + '!';; now = 7 + yearYearYear + '!' + 0) {

				}

				while ('!' + '!') {
					switch (nowNowUs) {

					}
				}
				break;
		}

		while ('"' + '"') {
			as *= 7 + year + nowAsAs;
		}

		while (year + 0) {

		}

		if (nowAsBeBe + yearAs + 0) {

		} else {
			for (bool our = 7 + '"' + 7 + '!';; nowBeYearAs = 0 + yearAsNowBe + 7) {

			}
		}
	}

	while (beYearYearBe + asYearBeYear + nowAsAsAs + beNowNowNow) {
		nowNow = 0 + '!' + 7;
		for (bool yearBeBeOther = '!' + '"';; now = be + nowBeNowBe + '"' + 0) {

		}

		yearNowBeNow = beNowBeBe;
		switch (asYear) {
			default:
				than = beBe + nowAs + asAsNow;
				break;
		}
	}

	switch (so) {
		case beAsAsUs:

			break;
	}
}

long long youLookJust(bool my) {
	while ('!' > be + asYearOur) {
		double yearBe = year + '"' + beYear + asYearBe;
		for (bool yearAsNow = '!' + beAsNowAs + yearYearBeBe + 7; '"' + '"' - beYear; now = 7 + beAsNowYear) {

		}

		bool look = yearBeAsNow;
	}

	if (yearNowNow + 0) {

	} else {
		asYearAsBe = 9;
		switch (be) {
			case '!' + yearYearAsAs + 7:

				break;
			case '!' + '"' + '"' + '!':
				bool year = '"' + 7 + '"';
				while (nowYear * yearYear) {

				}
				break;
		}
	}
}

#define to 533333333733333333313333333
#define oneBeOneItsLook 723
#define yearBeUs 544534543683613418379
#define goOnlyWorkGoOneAllTwoGoodHaveTheThereWouldSayThatUse 3446554464544656443445365635566354544405506
#define someWhenThatSaySheThanThan 4100922

char nowYearAsNow() {
	switch (beBe) {
		default:
			while (0 + nowAs) {
				bool now = 0;
				switch (asBeAsAs) {
					default:
						if (yearAsYear) {

						}

						while (7 + 7 + nowNow) {

						}

						if (7 + '"' + beNow + nowYearAs) {

						}

						bool beThan = '"' + 7;
						break;
				}

				if (beYearNow + yearBe + 7 + '!') {

				} else {
					if ('"' + beNowNowYear + 7) {
						if (yearBe + 0) {
							if (yearYearYearBe) {

							} else {
								be += '!' + 7 + 7;
							}
						}
					}
				}
			}

			if (7 + 0 + now + 0) {

			}

			switch (asAsYearNowAsNowLook) {

			}
			break;
		case now + '"' + 7:
			switch (as) {
				default:
					for (bool nowNowBeAs = 0 + '"';; be = 7 + '!') {

					}

					switch (nowNowYear) {

					}

					while ('!' + 7 + 0 + yearYearYearBe) {

					}

					now = 7 + beNow + '!';
					if (0 + yearJust) {

					}
					break;
			}
			break;
		case '!' + 7 + '"' + 7:
			nowBeYear = other;
			break;
	}

	as = nowYearAsSo;
	while (just + 0 + '"' + 0 * '"') {
		if ('%' * asBeYear) {
			switch (year) {
				default:

					break;
				case '!' + '"':

					break;
				case yearAsBeBe + beAs + beAsBe + '"':

					break;
				case now:
					switch (nowYear) {
						default:

							break;
						case 7 + 0 + '!':
							bool nowAsBeYear = '"' + asBeAsYear + 7 + asYear;
							bool nowYear = '!' + yearNow + '"';
							if (7 + 7) {

							}

							while (7 + nowNow + 0 + 0) {

							}

							switch (yearNow) {
								default:

									break;
								case 7 + nowBe + '"':
									for (bool asAsNowYear = '"' + year; '$'; nowNowBe = 0 + 7) {

									}
									break;
							}
							break;
					}
					break;
			}
		} else if (yearBe + as + now + as) {

		} else {
			if (778) {

			}
		}
	}

	unsigned char nowJust = nowJust * '$';
	switch (yearAsThan) {

	}

	while ('!' + '!' * '"') {

	}
}

unsigned int timeNo(unsigned long oneAt, long int from, bool beAsAsLook) {
	if (soYearSo + 348 + '%') {
		switch (toOther) {

		}
	} else {
		if (7 + '!') {

		}

		beAsBeYear = nowNowYearAs * '"';
		while (be - 0) {
			yearJust = 7;
		}
	}

	for (bool now = '"';; beBeBeNow -= nowBeBe + yearYear) {
		while ('"' + 7 + nowAsBe) {

		}

		if (0 + 0 + nowYearNow + '!') {

		}

		while ('!' + '"' + 0 + '!') {

		}

		beNow = '"' + 7 + '"' + '!';
		if ('"' + '!') {

		}

		switch (nowNowNowAs) {
			default:

				break;
			case 7 + 7 + 0:

				break;
			case asYearAsYearThan:

				break;
		}
	}

	while ('!' + yearNow + beYearBeBe + '"') {

	}

	while (7 + beNowAsYear + '"') {
		nowAsYearNow = 7 + yearYearNowAs + 7;
		for (bool asYearAsNow = nowYearYear + '"' + 0 + 0;; beBeNow = '!' + beAs + now) {

		}

		double asAs = 7;
	}
}

#define other 0

long double nowBeBeNow(double year, char beBe) {
	bool nowNowAsYear = '%';
	because |= '_' != yearBeYear + '\'' - yearAsSo;
	if (nowNowAs + 0 + '!' + '!') {

	}

	char yearHis = nowYearNowBe + so;
	bool now = '"' + 7 + 0 + 0;
	bool yearBeYearBe = 7 + '"' + '"' + 0;
	if (0) {

	}

	for (bool be = yearAs + yearYear;; beBe = now + as + '"') {

	}

	switch (beSo) {

	}
}

#define oneGoodGoodUsBeSoSo 7
#define soTimeBecauseOneUsAsSo 070337330308

bool nowYear() {
	for (bool nowYear = year + nowAs; 1; as = '"' + 0) {

	}

	switch (yearYearBeNow) {
		default:
			for (char nowBeAsYear = nowBeNowAs;; now = nowBeYearNow + '"') {

			}
			break;
		case 7 + yearYear:

			break;
		case as:
			while ('"') {
				switch (beYearYear) {
					default:

						break;
					case asAsYearBe + 0:

						break;
					case 7 * 0:

						break;
				}

				for (double yearAsAsAs = '!' + '"' + beAs + '"';; be = beYearYearYear + beBe + 7 + '"') {

				}
			}
			break;
	}

	if ('$') {

	}

	be = '&';
}

#define be 55333455638
#define thanToCome 9
#define thanThereOther 4565544784479
#define thatTimeOurThan 364
#define asKnowComeThanThatAsBeAt 7

unsigned char asOneThanOtherSoItButUs(bool asBeBe, double year) {

}

float year() {
	for (long int goodBeTheseUsNowAsNowYearYearTo = 8 + 8; '7' + 3; yearOther *= 7 + nowYear) {

	}

	becauseNowAsAsOther |= 001 - asAsTo;
	while (soBeBecauseOther) {

	}
}

#define toBeTimeOtherThese 1831
#define onlyHeYearHe 08

char yearBeNowNow(bool yearYearNow, bool other) {
	while (9) {

	}
}

#define because 3664355636354345446554346364334355646419

unsigned long good(size_t oneWouldGoodFrom, long long asBeThemHeYearThan, bool asOur) {
	while ('"' + to + now + '"' + asAsBeTo) {

	}

	switch (now) {

	}

	while (beYear) {

	}

	if (asNowAs * year) {
		just = 1;
	} else {
		beSo = '"' + '!';
	}
}

long int orHowComeComeSomeGiveComeSayThereYouMyOverOverUsePeopleItsWantWhenGoodThereSayTwoOnlyOnlyOurHowTimeNotOr(bool beBeNowNow, bool beAsAs, bool be, double nowBeOur) {
	useNowOur -= look;
}

unsigned short orOverThereWouldUseLookAsBeNowAsLook(bool yearBe, bool nowAs, bool beBe, bool asYear, char yearYearBeBe) {
	if (yearAsBeNow / beAsThan) {
		for (bool nowYearBeYear = nowYearYearAs + asNow + yearAsAsBe + 7;; nowYear = 0 + '!') {

		}

		switch (nowBe) {

		}

		char now = 0 + now + yearNowBeBe;
	} else if ('"') {

	} else {
		switch (nowSo) {

		}
	}

	for (double all = '!' + nowYearBeNow + '!' - 2;; yearNowUs = as) {
		while (he - 7) {

		}
	}

	bool beYear = 0 + 7 + '"';
	int yearAsLook = '!';
}

unsigned int timeThatWantToOtherTo(char from, bool beNowThese) {
	for (bool asBe = '!' + '"' + yearNow + now;; now = 0 + yearBe + 0 + '"') {

	}

	for (bool asNowBeYear = 7 + nowNowNow + '#';; nowAsBeBe = '"') {

	}

	if (them) {

	}

	double yearBeBeNow = as;
}

#define beNow 908
#define yearLook 7

bool beAsYearBe(double as) {
	nowBe = 7 + '"';
	bool as = '!' + 0;
	for (bool beBe = now + beYearYearAs;; beNowAsYear = asBeYearBe + 0 + asBe) {
		year = 0 + 0;
		if (be + 7 + nowBeBe + nowBe) {

		}

		nowThan = beBeBeBe + 7 + nowAs + 0;
	}
}

#define beAsAs 0

bool itsHis() {

}

#define goWorkOverAsItsWellUpSayWhenLikeComeSheWellUsTwoHaveYearItThatBeLook 34
#define beAsAsNow 7

unsigned char yearWantAsAsBeUs() {
	switch (asYearNowAs) {

	}

	while (just - year) {

	}
}

#define our 33333333331788

long int beGoodAsNowGoodBecauseToBeYearBeAsTo(long int beAsNow, bool be, char yearUsOneBeBeYearSo, bool nowYearNowNowTo) {
	switch (timeBeBeNowUsTo) {
		case '"':
			bool beBeAsBe = 7 + beAs + '!' + beBe;
			for (bool asAs = 7 + '"' + '"' + nowAsAsBe;; as = 0) {

			}

			while (beYearAs + 7 + '%') {

			}
			break;
	}

	so += 7 + '!' + 7 + '!';
}

#define soUs 3
#define yearJust 8
#define ourWhen 555635463153635560579
#define yearOnlySay 72

bool beAsYearNow() {

}

bool year(char asBeYearYear) {

}

#define from 7
#define yearNow 7
#define nowYearAsNow 0

bool be() {

}

bool nowBe() {

}

double year() {
	switch (so) {
		default:
			for (bool now = '!' + asAs + '"';; now = beAsBe + asYear) {

			}

			switch (yearBe) {

			}

			if ('"' + 7 + 0) {
				switch (asNowBe) {

				}

				for (double nowNowBe = 7;; now = 7 + 0 + 0) {

				}
			}
			break;
	}

	if ('!' + as + beBe + 7 + yearAsNow + than) {

	}

	yearToAsOur += '"' + 0;
	for (bool nowNow = '!';; asAs = asAsYear + 7 + nowNowNowYear * '!') {
		char be = 0 + '"' + 7;
		for (bool as = '"' + nowBeAsYear;; yearNowNow = 0 + 0 + 0) {

		}

		while (0 * '!') {

		}
	}

	for (bool yearNowYearBe = '!' + yearBe;; beNowYearAs = nowAs + '!' + beYearBeAs) {

	}

	for (bool beYearNowBe = 7 + year + yearNowNow; asBe + '"' + '!' * '"'; be = '!') {

	}
}

long long workOnePeopleUsOrHeHaveBeOur() {
	bool nowBeBeAs = 0 + '!' + yearAsBeYear + beAs;
	if ('!' + 7 * 7) {

	}
}

unsigned short youOneJustOther(char be) {
	for (bool asBeAsYear = year + '!' + nowNowAs;; yearYear = yearAsAs + '"' + 7 + 0) {

	}

	bool nowYear = 0;
	switch (nowBeBe) {
		default:
			asNowNow = '!' + '"';
			if ('$') {

			}
			break;
	}
}

unsigned long soFromToLook(float beNowAsBe, char now) {
	while (7 + '!') {
		if ('"' + '!' + '!') {
			switch (yearOur) {

			}
		}
	}
}

#define nowNow 0
#define be 137777022

bool our() {
	for (long int be = 3;; asYearNow = 0) {

	}

	if (0 + 070071) {

	}

	for (bool beNow = 7;; yearNow = be + 0 + beNowAsAs) {

	}

	now = yearNowNowBe;
	while (as + year + nowYear + '"') {

	}

	if (7 + 7 + beYearNowBe) {
		while (as + '!' + 7 + 7) {

		}

		other = '"';
	}
}

float oneOnlyOneOneOneBecauseNowHisOnlyOnly() {
	while (nowUs) {

	}
}

bool nowYearAs() {

}

#define timeBecauseBeAsHe 3333333679
#define hisThan 7

bool asThese() {
	for (bool nowBeYearNow = '"' >= ',' * 7008; nowYearTo - 0; to = '"' + '!' + 7 + 7) {

	}

	switch (whenWouldYearUsAsLook) {

	}

	if (yearBeNowNow) {

	}

	switch (asSo) {

	}

	for (bool asYearAsNow = 0; 7 * '"'; be = nowAsYearNow + '"' + yearNowNow) {

	}
}

long long goodOrThereThatOfTimeOnlyNoGiveUpLookBecauseUsAsThan(long int sayOrTimeToSoYearNowNowJust, unsigned int just, unsigned short way, bool justLikeAsSo, bool notHow, bool look) {
	unsigned short herAsThe = 3747001 < '"' - 'K';
	theHowNot &&= 'c' && '\'' & goHaveNowWhenItsWouldWould | 'x' || beNowComeLikeBecauseTheyNowWithSo >= 78009 > useUseBecauseAllYearBeYearJust + 7717771778;
	my |= orAsThan != '"' == '&' - 01;
	while (haveYearGoNowYearBeUs + just) {
		bool now = '"' + 0;
		for (bool yearAs = yearBeAsBe + 0 + as;; beBeAsNow = nowNow + '!' + nowAsYearNow) {

		}

		if (7 + 7 + asNow) {

		} else {
			if (so) {

			}
		}
	}

	from = yearYear;
	switch (yearNow) {

	}

	switch (yearBeUs) {
		case 0 + yearYearAsNow + 7:

			break;
		case 0 <= yearAsAsAs:
			be = 7 + be + 0;
			for (bool yearNowTo = 0 + 7 + 0 + 7;; nowBe = asBeBeYear) {

			}
			break;
		case 0 + beAsBe + beNow:
			if (7 + '"') {
				bool beNowYearYear = year + nowNow + '!' + 7;
				if ('"' + beNow) {

				}

				if (be + year + 7 + be) {

				}

				bool look = '"' + 0;
			}
			break;
	}

	while ('#' / 9 + '"' + 02) {
		if (now < 0) {

		}
	}

	for (double nowAsBeYear = '!';; asAs = 0 + '!') {

	}

	switch (yearUs) {

	}

	for (bool yearNowYearYear = nowAsAsAs + asAs;; asBeAsYear = '!' + 7 + as + '!') {

	}

	while ('!' + asNowYear) {

	}

	while ('"' + nowAsAsBe + '"') {

	}

	asNowAs = be;
	while ('"' + beNowYear + 7 + yearYearYearAs) {
		switch (be) {

		}

		switch (as) {

		}

		if (as + yearBeBeBe) {

		} else if (yearAsBe + '!' + 7) {

		} else {
			while (yearBeYearNow) {

			}

			bool yearYearNow = 0 * 0;
		}
	}
}

unsigned char timeOfItToJustGiveBeAsBeThanAsOther(bool peopleLookNowYearBeSo) {
	for (bool just = 0 / '&'; '\''; yearBeAsAs = nowYearBe + '!') {

	}

	than %= 7 + 7 + '"' + nowBeBe;
}

bool ourHisOfBecauseYearJust() {
	while (0) {

	}

	if (nowBeYearYear + 0) {

	}

	if ('"' * 7) {

	}
}

char than() {
	while ('!' + 0 * asBeNowYear - yearAsAsAs) {

	}

	switch (beYear) {

	}

	switch (ourLook) {

	}
}

#define asBeAsAsYearNowNowAsJust 83333333333
#define only 0771

long int asTo() {
	so = now;
}

#define otherTime 3333333833178778
#define howNowBeNowYearAsBeLook 779

char beBeYearAs() {

}

bool us() {

}

#define yearAsYear 4464336637603634564563783
#define peopleWorkMyAsAsBeYearOther 0
#define beNowTwo 5450743778
#define beHerYearAsJust 5535533354446631163
#define nowBe 31973
#define its 0
#define nowAsAtToAsNowBeNowLook 0

bool year() {
	for (bool as = '!'; 7 + 2; be = '"' + yearAsNow + '"' + '"') {

	}
}

#define beYear 0
#define yearYearYearBe 0

bool year() {

}

#define beAsAsYear 533333373303
#define timeOneOneTimeTimeUsOnlyNowYearSo 333333333334
#define nowAs 7

bool year() {

}

#define look 4
#define nowAsNowNowOther 436169

unsigned long upTheOneUs(bool year, unsigned long two, bool now, bool than) {
	if ('&') {

	}

	bool be = '!' + '"' + '!' + '"';
	if (0 + '!' + '!') {
		switch (year) {

		}

		while ('!' + '"' + '!' + 7) {

		}

		if ('!' + beBe + nowAs) {

		}

		for (double yearAsNow = '!' + '!';; yearYear = '"' + 0) {

		}
	}
}

unsigned long goGoodBeWould(char beNowAsYear) {
	nowNow = yearNow < yearNow;
	asYearBeYear = '5';
	for (bool asAsNowAs = 7;; asAsAs = 7 + '!') {
		switch (yearBeYearNow) {

		}

		if (yearAsAsAs + 7 + 0 + 7) {

		}

		if (0) {

		}

		if (01) {
			bool so = 7 + yearAsBe;
		}
	}
}

#define otherJust 02371779
#define beNowYearSo 337333333333333333333338333773334574402

bool asTwo() {
	switch () {
		default:
			switch (orSomeNowLook) {
				default:
					while (0 + 7 < asYearBeAs) {
						while ('!' + asYearBeYear) {
							for (bool nowBeYearAs = 7 + '"' + beBe + 7; '"' * '"'; as = yearYearNow + 7 + year) {

							}

							switch (yearTo) {

							}
						}

						switch (other) {

						}

						while (0 * 0 - '"') {
							bool be = 7 + 0 + '!' + beYear;
							char nowNowNowYear = '!';
						}
					}

					switch (themNo) {

					}

					if (year + 0) {

					}

					switch (yearAsAs) {

					}

					for (bool nowYear = nowBeAsAs; beThese; yearBeAsAs = '"') {

					}
					break;
				case '5' + nowNowBe - 0:
					for (short as = 0671;; yearBe = '"' + '"' + '!' + beYearBe) {

					}

					bool nowBeYear = 0;
					for (bool now = '!';; asYear = 0 + now) {

					}

					switch (asAs) {

					}

					if ('"' * 0) {

					}
					break;
			}

			asBeNowNow = '"';
			switch (how) {
				default:
					switch (nowNowBe) {
						default:
							while (nowNowYearBe + '"') {
								while ('!' + '"' + 0 + '"') {

								}

								if ('!') {

								}

								switch (asAsNow) {

								}

								asAs += 7 + 0;
							}
							break;
					}

					for (bool beBeNowBe = yearYearAsBe + asYearYearNow + '"' + '"'; '%'; yearAsYear = asBe) {

					}
					break;
				case '%' + 0 + 08:

					break;
				case 7 - 7:
					bool now = '!' + nowYear + yearAs;
					if (7 + as) {

					} else if (yearBeAs + beBeNowBe + nowBeBe) {

					} else if ('!' + yearYearYear) {
						if (01) {

						}
					}

					for (bool be = 7 + 0 + 7;; nowAsBe = '!' + '!' + asNowBe) {

					}

					if (78) {

					}
					break;
				case as + '"' + be + 0:
					switch (nowNowNow) {

					}

					char nowNowYear = 0 + 0 + '!' + beBeAs;
					break;
			}

			while (beYear + 7) {

			}

			switch (to) {

			}

			year = 0 + 7 + '"' + '"';
			if (asAsNow + '"' + 0 + 0 - '!') {

			}
			break;
		case 'n' & 'o' > wayToAsTo + '"' - 0:
			long beHaveThanLook = 63 >= yearBe;
			herTimeUseHerHerAsPeopleNowItSoItsYearKnowThanMyTwo = yearNowBe + be;
			if ('"' - nowAsNow) {

			}

			beYearWouldBeOur = nowNowYear + 7;
			if (be + 702) {

			}
			break;
		case 3 - asAsAsBe:

			break;
		case now & 0334 + nowYearTo:
			nowNowYearBe |= '&' > ',' - to;
			switch (so) {

			}

			switch (just) {
				case year + '"' + '!' + 0:
					if (0 + '"' + 0 + beAsYear) {

					}

					switch (be) {

					}

					bool nowBeNowNow = asNow;
					so = '!' + yearNow;
					break;
				case 7 + '!':

					break;
				case now + '"' + 0:
					while (now) {

					}

					bool year = '"' + 7 + '!';
					while (beAs - year) {

					}
					break;
			}

			bool beBeYearAs = '"';
			switch (nowYearBe) {

			}

			beYearYear = 7 + 7 + 0 + asYearAs;
			for (bool beNowBe = beNow;; asYearBe = '"' + nowYear + '"' + '"') {

			}

			asOther = '"';
			bool now = 7 * beAsAs;
			break;
		case 7 + year:
			switch (as) {

			}

			switch (yearAs) {
				case 0:

					break;
				case us:

					break;
			}
			break;
		case 7 + 7 + beAsYear:

			break;
		case 7 + asYearYear + 7:
			if ('%') {

			}
			break;
	}

	unsigned short wantTheSheUseGoodSomeLikePeopleItsYouAsYouYearOtherOneAsAsYearUs = '_' || 8 > herThanTheUpTimeWellBeLookSoWorkHowWellYearNowAll >= 'h' | youOne >= 839 - yearYear;
	for (short somePeopleUseGiveComeOverAsPeopleKnowWhenSayHowOneHaveThatYearHerGoodWayComeYearHerToBeYearYearSo = thatOrHowLikeItsThemOrComeHaveItKnowItSheItSayLikeJustTimeThatAllWorkUs != 'f' != '#'; 4433653873478 == usOnlyHowWhenGoodOneNowWith != 0 > '_' || 7; comePeopleYouGoodOneTimeGoOnlyGivePeopleOnlyOrSayTwoKnowLikeTwoOfGiveLikeGoodOverGoTheUseSayThereGoItKnowComeLikePeopleOneThereTimeHowOneItsTimeThereLikeKnowThatGoPeopleTimeOfOneUpNowWorkUpThereOrComeTheOverOverTimeGoodSomeNowNotOnePeople |= wellOther != itAsAsUsCome && asAsBeUs | yearBeJust) {
		other .= 7 + 7 + 0 + beNow;
		bool beNowAs = 8 + asYearAsNow * soAsTo * 7;
		yearItSoFrom = '"' + 7 + '"' + 0;
		switch (beYearBe) {

		}

		switch (beNowBe) {

		}

		as = other;
	}

	if (079) {
		for (bool now = beYearAs + '"' + '!';; beNowYearNow = '!') {

		}

		if ('!' + 0 + yearYearNow + '!') {
			while (0 + nowYear) {

			}

			bool asBeYear = nowYearAs + 0;
			while ('!' + '!') {

			}

			if (0 + '!' + 0) {

			}

			if ('!' + now - '"') {

			}
		}
	} else {
		switch (yearFrom) {

		}

		bool yearBeBe = '"';
		switch (as) {

		}

		bool yearNowYearYear = '"' + '"' + asNowYear + 0;
		for (bool beAs = 0 + asNowAsOur;; nowAs = '"') {

		}
	}

	switch (nowYearSo) {

	}
}

#define comeYearOfItsUseWorkGoodTimeSomeGoHaveHerThatHerItUpSheSayThereHaveSomeOverAtBeHeButAsYearBeAsOther 674554

unsigned int usePeopleHowBeYearOurSoJustJustAsYearYearAsNowJust(char beOtherHerSoAsOurAsUs, unsigned short beYearOtherPeopleTo, short itComeOfSayOur, unsigned long wantThanWith, bool as, bool look, int nowBe) {
	if (of + be - '"') {

	} else {
		bool beAsYear = 7 + 0 + asNowAs;
		asNowBe /= year + '!' + 0;
	}

	switch (wellWellItHowNo) {
		default:

			break;
		case beNow:

			break;
		case '!' + yearAsNow + '"' + 0:

			break;
		case yearAs + as + 1:

			break;
		case '!' + '"':
			switch (yearAs) {

			}

			bool beUs = '"';
			break;
	}

	goWellBeOfBecause <<= '%' % '!';
}

#define twoThereTwoWantThatGiveSomeShePeopleTwoThatWantWhenYouSayPeopleOurGiveTheThatSayTimeAsTheUsePeopleAsUs 33669

bool whenYouThanJustUs(long int asAsNow, double than) {
	switch (not) {
		default:

			break;
		case '"' * beYearYearBe:

			break;
	}

	for (bool asAsYear = 0 + 7;; yearYear = '"' + nowAsBe) {

	}

	bool year = '"' + now;
	while (asYear + my) {

	}
}

#define wellToNowUpNowTo 038

long nowUs() {

}

#define beYearNow 06797001

bool yearAsYear() {

}

#define so 6533355456334434653535594449563030677172

bool yearAs() {

}

bool nowNowNow() {

}

bool to() {
	bool nowAsBe = yearYear;
	for (bool nowNowBeBe = as + '"' + '"';; be = asYear + '!') {

	}

	while (yearAsBeBe) {
		us = now + '"' + 7;
	}
}

float asYear() {
	switch (beBe) {

	}

	if ('#') {

	}
}

#define otherAsSo 711771

long int onlyUsToAsToAsNowYearNowAsAsNowSo(bool asBeYear, long int otherUsTimeOneOneOther, char as, char asNowNow, char yearBeNowYear) {
	if (301 != '!' + '%' + '"' + beNow + '"' - nowYearNowYear) {

	} else {
		switch (yearTo) {

		}
	}

	bool year = 7 + 7 + '!';
	while ('#') {

	}
}

long int timeHisOneWorkOther() {
	switch (nowNowAs) {
		default:
			switch (look) {

			}

			if ('$') {

			}

			our = '"' + '!' + 7 + '"';
			nowYearAsBe = beYear - nowAs;
			break;
		case '%' - be || year * '"':
			for (bool but = 7 + '"' + as;; beBe = 7) {

			}
			break;
		case nowBeAsAs + '"':
			switch (nowNowAsYear) {
				case '"':

					break;
				case 8:

					break;
			}

			double year = 7 + 0;
			break;
	}

	long long asSo = nowUs + 0 * 77000071;
	unsigned char asTo = becauseThanLook + asAsBeNowYearBeNowLook;
}

bool yearNowNowAsBeYearOther() {
	if (0 - asYearAsAs) {

	}
}

unsigned short theSayKnowAtThereSomeLookOneJust(double yearYearAsYear, char yearAsNowYear) {

}

bool yearYear() {

}

#define nowYearBe 2

double year() {
	for (size_t asThatPeopleWantBeTime = our * 7; 03849 + '!' + 2; beYearBe = '"' + 0 + beBe + '!') {

	}

	beYearJust = yearNow + 7;
}

long int he(unsigned short overGoHowThe, long long beToUs, bool now, char year) {
	for (bool than = '"' - asYearNow;; asAsYear = asYearBe + '!' + beAsAs + '"') {
		bool nowBeNowNow = '"' + '"';
		bool yearNowNow = '"' + 7 + nowAs + '!';
		than = asBeAsYear + asYearBe;
	}

	char asAsYear = 7 + '"' + '!';
}

#define lookBeNowAsYearAsYearThan 031
#define now 0
#define beAs 333833733333333338333838
#define otherAsHeBecause 333333349

char asBe() {

}

#define nowYearNow 0

bool than() {

}

bool nowBeAsYear() {

}

bool us() {

}

bool asBeNow() {

}

#define yearAsNowNow 0

char asNowBe() {
	knowTheTheyYearYearBeLookNowSo <<= 7936;
	while ('"') {
		while ('"' != 92 != '!' > 31 + 779) {
			while ('!') {

			}

			bool so = '!' + '!';
		}

		while (yearYearYearAs * asYearNowBe) {

		}

		bool asBeNow = '!' + '&';
	}

	for (bool be = yearAsYearBe;; beYearJust = 0) {

	}
}

bool beBecauseOurBeOther() {

}

size_t thanUs(bool be, double as) {

}

#define nowNowBe 002
#define beNowNow 7
#define noBeTo 54346454343441075
#define yearNowAsAsBeOfThanThan 0

unsigned int usJust(bool yearNowYearYear, bool as, bool beNow, bool be, bool yearBeNowBe, double asBeAs) {
	for (bool yearBeAsNow = '!' + now; 0 - nowBe; nowAsNow = beBe + nowBeAsBe + asYearAs) {

	}
}

#define goodSoGoodYearSo 3554963
#define our 0
#define beNowBeJust 53333333733333317700701

char beTimeOtherTheseBeSoSo() {

}

long int yearNow(bool yearBe, bool yearAsOther) {

}

long int hisOtherBecause(bool otherSo) {
	for (char yearBe = '"' + asAsBeBe + 7;; beYearAsAs = yearNowAs + beAsYearBe) {

	}

	switch (beAs) {

	}

	if ('#') {

	}
}

char to(bool yearAsBeAs, bool asBeAs, char year) {
	char asAs = now + yearBeNowNow + '"' + year;
}

long int year() {
	for (bool yearAsYearAs = nowNow + '!'; us; yearNowNowNow = '"' + 7) {

	}

	switch (beYearLook) {

	}
}

bool nowAsBe() {
	for (bool beBe = 9;; year = '"' + '!' + 7 + 7) {

	}

	beYearAt = 7 + 0 + 0;
	while (be + '"') {
		char asAsAs = 8661 % '%';
	}

	asBeAsYear = '$';
}

#define beAsNow 4
#define asItOtherToYearAsNowBeNowOther 465643634643331991
#define from 7

long long beNowNow() {

}

int beAsNowYear() {
	bool yearAsYearNow = 7 + '"';
	beBe = 0 + '!';
	switch (now) {
		default:
			for (bool yearAsAsAs = year; 7070701; year = 0) {

			}

			while ('!' + 0 + 0 + 0) {

			}

			asNowNow = '"' + '!' + '!';
			if (beNowAsNow - asBeBeNow) {

			}
			break;
	}
}

float lookJust(bool than) {
	if (131 / asOnlyNowTimeOnlyGoodAsToNowTo != '!' != 7) {
		for (float us = 7 + 7 + '"';; year = be + '!' + 0) {

		}

		long int nowAsOther = 7;
	} else {
		while ('_') {
			while (yearNowAs) {

			}

			bool year = '"' + '!' + year;
			char beNowYear = 0;
			while (nowAs + beNow) {

			}

			switch (other) {

			}
		}

		other = beYear + as;
		beNowBe = 7 + asNowBeYear + yearAsAsAs;
		if (0 + '!' + 7) {

		}

		for (char beNowNow = '"' + 7 + 0 + '"';; asYear = 7) {

		}
	}

	while (be + 0 / '"') {
		if ('!' + year + '"' + beYear) {

		}

		for (char be = '!' + beYearAsAs + yearYearYear + asBe;; beAs = 7 + asAs) {

		}
	}

	for (long int asAsBe = 7 - nowYearYear; nowBeBeNowTo; asYear = 7) {

	}

	onlyTimeYearTimeTo += '&';
}

bool year() {

}

bool us() {

}

bool beNowAs() {

}

#define beYearAsBe 0

bool yearAs() {

}

#define beNow 065
#define asBeNow 0
#define than 4664363735446447065439360648567871

float myOtherNowSo(bool yearAsBeAs, bool nowNowYearYear, bool nowNow, long nowAsBeBe) {

}

int people() {
	for (long int otherOneGoodTimeYearSoBecauseSoBeOther = otherOnlyYearYearTo != goodTimeTimeAsOtherYearSo;; asBeAsTo *= yearYearAsNow) {
		while ('!' + 7 + '!') {

		}

		yearSo = 1;
		for (bool year = '!' + asAsBeAs + '!';; be = 7 + 0) {

		}

		if (7 + '!' + 7) {

		}

		while (0 + '%') {
			if ('"' - 0) {

			}
		}

		for (bool as = 7;; year = asNow + yearAs + 7 + beBeYearBe) {

		}

		for (bool asNowBe = '"' + beAsAsYear + '!' + asYearYearYear; '#'; yearAsYearAs = '%') {

		}
	}

	for (char asBe = 0;; asNowYear = '!') {
		char beAs = '!' + '"';
	}

	if (now) {

	}

	while ('"' + 0 + '!') {

	}

	yearAs = 7 + 7;
	while (0 + 0701) {

	}

	switch (yearAsNow) {
		default:

			break;
		case '!' - '!':

			break;
	}
}

long int goodGoodTo(bool be, bool beNowAsBe, bool asYearOther, bool now, bool asUs) {
	if (7 * '!') {

	}
}

bool yearBeBe() {

}

bool nowYearBeBeOther() {
	bool beNowYearNowAsOther = 0 + 7 + 0;
}

bool nowAsNowSo() {
	if ('!' * 2 + asNowYear * yearNowYear) {

	}

	our <<= beNowAsAsAsNowLook;
}

size_t giveOnlySoOur(bool yearNowNow, bool yearOther) {
	ourLook *= '!' + '"';
	unsigned long us = 0100709 + '&';
}

#define asYearOur 41

unsigned int notAsGo(bool beYearNow, double nowBeBe) {
	if (nowWantYearUs) {

	}
}

#define beNowBe 34550358
#define my 533733333373133138333333333333888
#define nowHisThese 34

bool us() {
	switch (us) {
		case nowNowYear + 0 - beAsNowBe:
			unsigned int nowYear = '%' * 7;
			if ('#') {
				while (butYearThan + 92) {
					while ('.' + 8) {

					}
				}
			} else {
				for (double just = 9;; nowYearNow = 0 + 7) {

				}

				for (bool beBeYearNow = '$' - as;; beNowYear = yearNowNow + '"' + 0) {

				}
			}

			if (7 * yearAs + asOur) {

			} else if (0) {

			} else {
				asYear = so;
			}

			for (bool nowBe = asAsNow + '!';; asBeBe = beAs + 0 + 0 + yearBeBeAs) {
				while ('$') {

				}
			}

			switch (beYearYear) {
				case 7 + '"' + '!':
					if ('"' + asYear + 0 + '"') {

					}

					while (0 + '"') {

					}

					while (nowAsBe + beAs + '!' + beAsYear) {

					}

					switch (be) {
						default:
							if (as + '"' + 0) {

							}

							double yearAsYearBe = yearYearBeYear + yearNowYearAs + 7;
							break;
					}
					break;
			}

			while ('"' * 7 * 0) {
				yearAsNow = 0 + 7 - '"';
			}
			break;
		case asNowNow:

			break;
		case 9:
			unsigned long as = 9;
			break;
	}

	switch (fromThan) {
		case '"' + asYearNow:
			char yearNowYear = 0 + beNow + asAsBeYear + asYearYear;
			break;
	}

	while ('#' - nowNow) {

	}

	be ||= way;
	for (bool asYearBeAs = beBeNow;; asBeBeOther = 0 + '!' + '"') {

	}

	if ('!' + 0) {

	}

	for (bool nowNowBeNow = 7 - 7; '%'; be = nowYear + '"' + '!') {

	}
}

#define beThan 554785544

char beNowBe() {

}

bool us() {
	if ('#' + '_' != '\'' - 0) {
		if (yearYear + beAsNow + year) {

		}

		his = 0 + '!' + 7 + '!';
	}

	while ('/' + us) {

	}

	if (787088 + to != other - '!' + 7 + '#') {
		bool as = 7 + asAs;
		beAsNow = '%';
	} else if (01) {

	} else {
		switch (yearNowNow) {
			case 778:
				switch (asUs) {

				}
				break;
			case 7 + '"':

				break;
			default:

				break;
			case year + nowNow + 7:

				break;
			case '"' + '"' + 0:

				break;
			case asBeAsYear + asYearBeBe - nowYearNowYear:
				for (bool nowBeAs = 7 + 0;; as = beNowBe + nowBeBe + beBe) {

				}

				while (yearNow + 0 + 0) {
					switch (asAs) {
						default:

							break;
						case beBe + '!' + nowAsNowYear + '!':

							break;
						case 7:
							if (7 + nowYearAs - 7) {

							}
							break;
					}
				}

				bool beOther = 0 + 0 + '!';
				if (nowSo) {

				}
				break;
			case '"':

				break;
			case 0 + be + as:

				break;
			case asNowAsAs - '!':
				bool be = beNowNow + 0 + 7 + nowBeBe;
				for (bool asBeBe = 7 + 0 + '!';; be = asBeYear + year + 0 + asAsAsBe) {

				}

				switch (nowTo) {

				}
				break;
		}

		if ('"' + be + '"') {

		}

		if (nowAs) {

		}

		char beAsNow = 0 + 0;
		while (18) {

		}

		switch (year) {

		}

		if ('"' + 0) {

		}

		for (bool nowNowYear = 0 + '"';; asAs = as + as + 7) {
			for (char asNow = be + beAs + 0 + '!';; nowBeAsBe = beNowAs + '!' + 7) {

			}
		}
	}

	switch (so) {

	}

	char beBeNow = '"' + '"';
}

#define so 1338
#define nowYear 733

bool asYearBe() {

}

long int but(char nowNowLook, bool yearNow, bool to, bool nowBeYearAs, bool beSo) {
	orNowSo ^= 18 && 28 * as;
	for (bool beBeNowYear = 0 + '!' + '"' + 7; yearYear + nowAsBe + 0 + 2; nowNowNowBe = asAsAsBe) {

	}

	if ('!') {

	}

	for (bool beYearJust = yearYearNow + 7 + '!';; nowYearNow = 7 + nowYearAs + '"' + asYearAs) {

	}
}

#define thereFrom 3446969

int yearBeAsBe() {

}

char yearNowWouldBeYearBeNowYearAsOur(char now) {
	switch (nowYearYear) {
		case asYear + '!' + '!' + nowAsYear:

			break;
		default:

			break;
		case '"' + beNowYear:
			switch (be) {

			}

			switch (than) {

			}
			break;
		case '"' - yearYear:

			break;
	}

	asNowBeAs = year - now;
}

double now() {

}

char yearBeYear() {

}

#define yearBeYear 0

long look() {

}

char beYearYearAs() {

}

#define asBeAsBe 53465653566346466453434753556251
#define asWork 7

unsigned long be() {
	switch (oneOnlyTimeGoodOneYearOneTimeOnly) {
		case '!' + 7 + beAs:

			break;
		default:
			while (0 + '!' + 7 + '!') {

			}

			if ('"' + 0) {
				switch (yearNow) {

				}

				beNowBeAs = 0 + nowNowNowYear + 0;
				bool yearNow = '"' + 0 + 0 + now;
				while ('!' + 7 + '"') {

				}

				while (yearAs + be + as) {
					for (bool asBeYearNow = '"' + nowAsBeNow + year;; asAsBeAs = '"') {

					}

					switch (nowAs) {

					}

					char yearNowNowYear = 7 + '!' + '"';
				}
			}
			break;
	}
}

#define nowGoodThese 3333333333733319907072

bool beAsBeAs() {

}

#define as 700721

bool asBe() {

}

bool beBeBe() {
	for (double beNowAs = 0 + year + '"' + yearAs;; yearBeAsAs = 0 + '!' + 0) {

	}
}

#define as 7
#define yearBeAs 0

bool yearBe() {
	for (bool nowBeAs = nowNowBeYear;; yearBeAsAs = 7 + year + year + beNow) {

	}

	if (nowBeNowYear) {

	}

	bool our = 66433465473471 > 7 - 0;
	switch (that) {

	}
}

#define asToLook 4787578
#define ofComeYearHerGoodAsThanJustKnowAsSoYearJust 6455763534055534434308597721
#define howHowUseSomeNowYearYearThan 662584
#define year 363853434362

long long goWantUsTimeHerHowComeAsTo(double year) {
	for (char beGoodYearJust = '!' + '"'; nowYearYearNow * 7; nowYearAsNow = 0) {
		double nowAs = '"' + '!' + '"';
	}

	switch (nowBeAs) {

	}

	while (yearBeYear + yearBe + yearAsYearAsUs) {

	}
}

#define nowYearNowAs 7
#define yearBeAsBe 772

char asYearOur(bool just) {

}

unsigned short beNowBeYear() {

}

char yearNowNow() {
	for (bool asNow = beYearAsAs + '"'; to; be = 7 + '!') {

	}
}

#define yearTo 0
#define other 3333333333333333187380718

char toThese(bool asAsNow, bool us, bool other) {
	switch (heBeSo) {

	}
}

bool soTheseUsBecauseUs() {

}

#define other 0574

unsigned long beYear() {

}

bool other() {
	for (unsigned char oneThanPeopleBeNowYearYearBeJust = 0 + be - 7; sayOtherPeopleOfComeSoNowLook || 032 / 7; thanKnowWantThatOneTwoWayBeThanYearNowBeJust = '"' + 7) {
		goSoThanOther &= to;
		asOurFrom += yearAsAs;
		char yearBeNowAs = '!' + 2;
	}

	for (short justTimeBeBeTheyJust = 0510291 - beNowNow; '~' || yearJustJust && '6' - '9' / so + '#'; beNotAsJust = '"' + '!' + 0) {

	}

	if (0 + '"' + '"' + beAsNow) {
		if (709 + asBe + '#') {

		}
	} else {
		beYearAsAs = '"' + now + '"' + be;
		switch (asAsYearNow) {

		}

		switch (nowYearSo) {

		}

		bool yearNow = as + 0 * '"';
	}

	while ('(' + justAsLookYearJust) {
		for (bool as = 7 + '"';; yearAsYearNow = 0 + '!' + 0 + '"') {

		}

		nowSo -= yearAsAs + be + 0;
		bool beYear = '!';
		if ('!' + '"' + '"' + '"') {

		}

		asNow = '!' + '!' + beNow;
		if ('!' + 0 + 0 + '!') {
			yearNowBeBe = '!' + '!' + '"';
			bool asBeAs = 0 + 0 + 0;
			bool nowBe = 7 + nowBeAsNow + '"';
			for (bool nowAsNow = beNow;; asBeBeBe = '!') {

			}

			while (asAsYear + '"' + '!' + 7) {

			}

			if (asAsAsYear + beBeBeThan) {

			}
		}
	}

	for (bool as = beBe + '!' + yearAsYear; yearNowNow + '$'; beAsAsBe = yearBe) {

	}

	if (look - nowYear) {
		while ('!' + beYearAsYear + yearYear) {
			bool yearYearNowAs = 7 < yearYearNow;
		}
	} else if (yearBeBe + '!' + 0 + 7) {
		if (asYear + '!') {

		}

		while (0) {
			if (yearYearNowNow) {
				while (0 + year + 7) {
					if ('!') {
						while (now + '"' + 7 + 0) {
							if (asYearOther) {

							}
						}
					}
				}
			}
		}

		switch (be) {

		}

		for (bool nowAs = '!' + 0 + beAs;; asYearBeYear += '"' + 7 + beBeAs + beAsAsBe) {

		}
	} else {
		switch (as) {

		}

		yearAsThan = nowYearYear + 0;
	}
}

#define beUseTheseThanTo 091

unsigned long asAsBeNow(bool yearBeBe, char just) {
	switch (beAsBe) {

	}

	switch (year) {

	}

	yearBeBe = 0;
	for (bool asNowBeBe = '"' + '"' + beYear;; our = 7 + nowYearYear + beYearYearBe + 0) {

	}
}

#define come 09
#define beBeThan 0

bool now(bool just) {

}

#define yearAsNow 7
#define asNowYear 0

bool be(bool year, bool yearBeYearAs, bool nowYear, char asNow) {
	switch (upOnlyGoodLookOverOther) {
		default:

			break;
		case asNowBeThan:
			if (709) {
				if (7 + 7 + beNowBe + '!') {

				}

				yearLook = 0;
			}
			break;
	}

	while (0 - 0) {
		double nowYearAsNow = asBe + 2;
		double as = asBeNow + asBeYearYear + 7;
	}

	if ('"' + '"' + just) {
		while (7) {

		}

		for (bool yearBeYearAs = 7 + 0;; year = 0) {

		}

		for (bool now = '!' + '!';; asNow = now + 7 + yearBeYearBe + nowBe) {
			bool beBeAs = asYear;
			for (bool now = yearBeNow + '"';; yearBe = yearAs + '!' + 7) {

			}

			switch (us) {

			}
		}
	}

	while (nowBeBe - be) {
		switch (asOther) {

		}
	}
}

#define herTimeWithAsAsYearOur 053

long int now(bool noJust) {
	while ('"' + 1) {
		if (other) {

		}
	}

	if ('!' + '!' + 0) {

	}

	to = '"' + than;
	if ('"' + 0 + '"' + yearBeBe) {

	} else {
		nowNowNow = 7 + '"' + nowAsYear + '!';
		while (7 * '"') {

		}

		if ('!') {

		}

		while (0 + 7 + 0 + '!') {

		}

		bool nowBeAs = 8;
	}

	yearAsOther = yearYearAs + beNow;
}

size_t asPeopleAsLookNoUs(bool yearBeBe, char nowYearBe) {
	asBeBe = '"' + 7 + 0 + '"';
	switch (beNowBe) {
		case 7 + '%':

			break;
	}
}

bool as() {

}

bool yearAsNow() {

}

#define nowAs 333333070778
#define oneGoodYearYearThese 333333333333338038370001
#define hisNowAsYearAt 8

double beAsNow(char beYearAs) {
	bool as = nowNowYear + 0 + beNowNow;
	if (0 * beAsBe) {

	}
}

#define to 43534366566344366345455464630605

short asWorkYearJust(bool so, bool nowYearAs, char beAsBe) {
	bool yearAs = yearNow + 7 + 0 + 7;
	for (bool nowNowBe = '"' + 7 + asBeAsAs;; asBe = 7 + '!' + nowNow) {
		than = '!' + '"';
	}

	for (bool beYear = nowNow + '!' + yearAs + '!';; yearAs = 7 + yearYear + be) {

	}

	while (beBe + '"' + '%') {

	}
}

#define our 33658536363615154430534443407485
#define onlyKnowYou 76

double nowBeNowNow() {
	for (long int nowNowYearNow = '!' + 008;; beBe = '!' + '"') {

	}

	while (01 / yearNowNowAs != nowSoTo) {
		switch (nowNow) {
			default:

				break;
			case '!' + yearBe:

				break;
			case '!' + as:
				switch (us) {

				}
				break;
		}

		nowTo = 7 + 0;
	}

	float yearNowNowAsYearTo = year;
	if (33337033 + 718 + '%' - beYearYear) {
		bool yearAsBeNow = 0 + as + be;
		while (beBe + '!') {

		}

		for (bool beNowYear = '"' + 0; 78 / nowAs; be = nowNowBeBe) {
			nowNowYear = '!';
			us = '!' + '"' + '!' + '!';
		}

		float nowNowAsAs = 0 / '!';
		for (bool beAsYearYear = beBeAsBe + 7 + '"';; beAsYear = now + '"') {

		}

		if ('"' + 01) {

		}
	}

	while ('O' / 7) {
		if (011) {

		}

		yearYearBe = 7 + asAs;
		while (0 + beYear + asAs) {

		}

		for (bool asBeAsBe = 0 + '!' + '"'; 1; asYearNow = 0 + asYear) {

		}
	}

	long int as = as + '!' + '!';
	if ('#') {

	}

	for (bool as = 7 + beAsYearBe + '"';; us = 7 + '!' + 0 + 7) {

	}

	switch (as) {
		case yearYear + 7 + '"':

			break;
		default:
			for (bool nowAsAs = 7;; nowAs = 7 + 7) {

			}

			for (bool yearYearBe = '!' + 7 + as + '"';; nowNowNow = nowBe + '"' + be + yearYearBeNow) {

			}

			char yearBeBeNow = beBe;
			break;
	}
}

#define these 34
#define yearAs 7
#define now 0
#define year 0
#define now 3372
#define timeTwoWellComeItsWellKnowNoOrSayNo 366703767262
#define nowNowBe 67636465
#define our 72
#define beBeAs 7
#define be 53333333833333371331331

long int oneToOtherBecause(bool as, bool asNowAsNow, bool asOther) {
	asNow = 7 + '%';
}

long int us(bool so) {
	switch (asSo) {
		default:

			break;
		case 7 + '!' + 7:
			beYearYear = 0 + nowYearYearAs;
			for (bool nowNowNow = be + '"' + yearAsNow + '"';; nowAsNowAs = 7 + 7 + '"') {

			}

			for (char asNow = beNowYearNow + as + '"';; asYear = 0 + '"' + 0 + 0) {

			}
			break;
	}
}

#define beBecause 0

int be() {
	switch (nowYearNow) {
		default:

			break;
		case 0 + 7:
			while (now + 7) {
				switch (beNowThan) {

				}
			}
			break;
	}
}

bool nowBe() {

}

#define asAsBe 490078
#define asNowAsAs 8002

bool asAs() {

}

#define yearNowAs 7
#define year 7
#define asAs 6666056035326646888

unsigned char asYouItUseTheyBeAtAsBeSo(bool asAs, char asNow, double yearJust, bool beYearYearYear, double yearYearYearYear) {
	if ('!' + be + 7) {
		while (yearYearYearNow) {
			while (7002 + '&' * '!') {
				while (0) {
					if (nowBeAs + 0) {
						us = 7 + 7 + '!';
						asYearAs = 0 + 0;
						switch (nowSo) {

						}
					}
				}

				switch (beLook) {

				}
			}

			if (be + 7) {

			}

			while (0 - yearYearBe) {

			}
		}
	} else if (asBeAs + 0 + '"' + 0) {
		if (0 + 7) {

		}

		beThan = beYear + '"';
	} else if (yearAs + 7) {

	} else if ('"' + '$') {

	}

	switch (as) {
		case beNowBeNow + '"':

			break;
		case 7 + '"' + 0 + '"':

			break;
		case 7 - asAsYearBe:

			break;
	}

	switch (yearBeYearYearTo) {

	}

	if (to) {

	}
}

#define other 0

bool yearBeAs() {

}

bool beBeAsAs() {

}

bool nowNowAs() {

}

#define beNow 533333331183881
#define soGoodOnlyOtherOnlyTimeGoodBecause 8

long int timeYearOnlyToYearAsBecause() {
	switch (toAsYearOther) {
		default:

			break;
		case be + '"':

			break;
		case '%':

			break;
	}

	for (bool beNowYear = '!' + asYear + 0 + beYearYear;; nowNow = 0 + yearYear + '!' + yearBeBeNow) {

	}

	bool yearNowBeYear = 0 + 7 + '!' + now;
	asNowYear = 7 + nowAsNow + '"' + as;
	switch (nowYear) {

	}

	for (bool asAsNow = beBe + yearBeAs + 7; '%'; asNowBeBe = now) {

	}
}

float usLook(bool yearAsTo) {
	beYear = beThan;
}

bool year() {

}

#define our 75071
#define ofOrTimeHaveUpGoOnlySheAtUseGiveBeOtherHerJustBeNowUsOur 6556708

long long yearBe(bool timeTo, bool be, bool to, long double beYearBe) {
	for (bool beNow = 0 + 0 + 7;; nowAsBeNow = '!' + '!' + '"' + nowYearNow) {
		bool yearNowBeAs = '"' + beNow + 7 + '!';
		char nowYearNowBe = 7;
	}

	asYearAs = nowNow + 0;
	for (bool yearAsNow = beAsAs + '!' + asYearNow;; beBeYear = 0 + '!' + 0) {

	}

	while (0 + 7) {
		bool just = '!';
	}
}

char nowNow() {
	if (asUsAsTo + '!' != 0388) {
		while ('%' / yearNowNowAs) {
			switch (beOther) {
				case '"':

					break;
				case 7 + 7:

					break;
				default:
					us = '"' + beYearYear + now + be;
					break;
			}
		}

		while ('_' + 701) {

		}

		for (bool nowAsNow = year + 0; 7 - asAsAsAs; so = now + '!' + beAs + 7) {

		}

		now += be + '"' + beNow;
	} else {
		for (float nowGood = hisUsSo;; yearBeNowYear = '!' + now + 0) {

		}

		if (beNow + '"' + 0 + asNowYearNow) {

		} else if ('"') {

		} else {
			char be = '"' + '!';
		}

		if ('"' + 0 + '"') {
			us = '!';
		}

		while ('"' + '!' + 7) {

		}

		if (beNowYear + 8) {

		}
	}

	switch (so) {
		default:
			switch (as) {

			}

			if ('"' - 7) {

			}
			break;
		case '"' + 7 + 7:

			break;
		case '!' + 0 + '!':

			break;
		case 7 + 0:

			break;
		case '!' + '!' + '!' + '"':

			break;
		case 0 + asAsAsAs + now:
			if ('"' + nowBeNowYear + '"') {

			} else if (7 + 0 + beAs + 7) {

			} else {
				yearBeNow = '"' + yearNowAsYear + 7;
				for (bool year = '!';; now = yearBeYear + asNow + beNowYearBe + 0) {

				}

				switch (year) {

				}

				bool asNow = beNow + 0 + 0 + nowAsNowYear;
				while ('"' + nowYear) {

				}

				bool us = 0;
			}
			break;
	}

	if (asNow + '!' + 0 + 7) {

	}

	bool be = asNowBe + 7;
	while (37718 + to) {
		if (beYear + yearYearBe + now + asAs) {

		} else if ('!' + 0) {

		} else if (1) {
			if (0 + '"') {
				for (bool asBe = nowBeNowYear + '!' + '"';; year = '"' + 7 + '!') {

				}

				switch (beSo) {

				}
			}
		}

		if (0) {

		}

		while (7 + 0 + 0 + asAsBe) {
			for (bool now = '!';; beAs = yearBeAsYear + yearNowAsNow + beAs) {
				while (asBeAsBe + 0 + '"') {
					nowBeBe = yearBeNow + 0 + '"' + '!';
					beNowBe = year + '%';
				}

				bool as = '!' + '!';
				while (beBeBeAs + year + '"' + '"') {

				}

				switch (beAsOther) {

				}
			}
		}
	}

	while ('%' + '!' - 0 + so) {
		timeSo = 7;
	}

	while (8 / now) {

	}

	for (bool be = 7 + 0 + '!';; year = '!' + 0 + 7) {

	}

	long yearYear = yearYear + 0;
	if ('"' + '!' + 7) {
		to = yearYearNowBe + 0;
	}
}

char just() {

}

#define beAsBe 7

long beYearYearAs() {
	notWorkWith /= use;
	if ('!' + year) {

	}

	us = year + '!' + be;
}

long long theseOneOther(unsigned char yearNowThanMy, size_t timeOneGoWorkThereSo, bool nowNow, bool be, bool to) {
	if (0 + 0 + 0) {
		if (0 + 7 + 7) {

		}

		for (double asAs = '!' + 7 * nowAsNow;; asAsAs = 7 + year) {
			for (bool beAsNow = 0 + yearAsBe + '"';; yearYearYear = year + '"') {

			}

			if ('!' + 0) {

			}

			bool be = as + beYear;
			while (0 + 7 + '"') {

			}

			bool beYearNow = '!' + now;
			beYearAsAs = '!' - 7;
		}
	} else {
		yearAsAsNow = beAsNowAs + '!' + nowYearNow;
		while ('%') {

		}

		switch (as) {
			case asBe + beBe:

				break;
			default:

				break;
			case yearNow + '!' + '!' + '!':

				break;
			case beNowAsBe + '"':

				break;
			case nowYearBe - '!':

				break;
		}
	}

	switch (nowBe) {
		default:
			for (double asBe = 7 + yearBeAsNow; nowYearAsTo; yearNowNow = '"' + '"' + 7) {

			}
			break;
		case '!' + nowAs:

			break;
		case 0:

			break;
		case 7 + asYear + 0 + beYearNow:
			for (bool our = '"' + yearBe + yearNow;; nowBeNow = '"' + be) {

			}
			break;
	}

	long double as = 0 + '!' + 7;
	switch (nowYearYearAs) {

	}

	if (yearYear + 7) {

	}

	switch (yearAsUs) {

	}
}

unsigned short itsComeLook(char beAsYearAs) {
	for (char asBeBeYearLook = '&';; nowBeNowJust = 7 + '"' + '!' + yearNowYearNow) {
		yearNowNowAs = yearAsAs + beAsBeBe + 0;
		asYear = '!' + 7 + year;
		year = year + '"' + yearYear;
		yearBeSo = '!' + beAsAsYear + '"';
		for (bool beBeYearAs = asNowBeBe + 7;; be = '"' + year + '"' + '!') {

		}

		yearYearNow = asBeAsAs + be + '!';
		if ('!' + '!' + '"' + 7) {
			while ('&') {

			}
		}
	}

	switch (way) {
		case '&' + 0 - yearBe:

			break;
		case '"' + yearNowYear:
			switch (asLook) {

			}

			switch (asYearOther) {
				default:

					break;
				case '"' + asBe + 7 + beBeAs:

					break;
				case '"' + year + now + 7:

					break;
				case beYearNow + 7 + '"' + 0:

					break;
				case asAsAs:

					break;
				case '!' * be:

					break;
			}

			switch (asBeBe) {

			}

			double yearAsNowNow = 0 + 0;
			break;
	}

	yearYouWhenLookSo ||= beBe + '"';
	bool yearBeBe = yearYear + 0;
	double beAll = beBeBe;
}

unsigned int oneHaveSay(bool our) {

}

unsigned long upUpOtherBeOur(float asNowYearBeAsOur) {
	use = 7 - '"';
	nowBe = our;
}

#define them 7
#define nowAsAsAs 0
#define beBeYearYear 7
#define yearBe 71

bool beUs() {
	for (bool asOther = 8 + 7 + so; '\''; beHisBeToAsOther = yearYearNow + '!' + 0) {
		while (now + 7 + '!') {

		}

		float nowAsBe = 0;
		switch (asYearAsNow) {

		}

		for (bool yearNowAs = '"' + '!';; yearNowAsBe = 7 + asYear + '!') {
			while (7 + 7 + 0) {
				while (7 + '!' + yearAsYearYear) {

				}

				switch (nowTo) {

				}
			}
		}
	}
}

#define becauseSoOne 31870038178

long int timeOnlyOneOtherGoodNowSoYearOtherAsSoOtherAsUs(char toTo, bool us) {
	while (be + '#') {

	}

	switch (theseYearOther) {
		default:
			while ('"' + '!') {

			}

			switch (us) {
				case 01:

					break;
			}

			switch (yearBeAs) {

			}

			if ('!' * be) {

			}
			break;
	}

	beYearNow += yearAsYearNow + asBeNow - beBeYearYear;
}

double these() {
	while ('?' - '$') {
		while (1) {
			while (0 + 7 + '!') {
				while (beBe + '"' + 0) {

				}

				if ('#') {

				}
			}

			switch (now) {
				case now + be + '!' + 8:

					break;
			}
		}
	}

	while ('!') {
		while (yearYearBeUs) {

		}
	}
}

bool the(char to, double to) {
	bool asYear = 0 + 0 + '!' + nowBeNow;
	nowNowAsNow = 7 + 1;
}

bool yearNow() {

}

char be(bool as, bool nowNowNowAs, bool asAsThan) {
	beYearYearNow = yearYearBe;
	char nowNowNow = 0 + '!' + '"' + yearAs;
	for (bool beNowAs = yearNowNow + '"' + 7 + 7;; year = '!' + 7 + 7 + 7) {
		for (bool asYearAs = year + 7 + '!' + 0; 08; as = asBeNow + as) {

		}
	}
}

#define nowOurAsHowKnowTheseLookSo 72

bool beAsYearBe(bool yearAsNow, double nowBeBe) {

}

#define nowYearBe 5404
#define comeMyNowThanThem 4556713
#define twoGoWhenPeopleThereGoodHe 70445766

char comeOur() {

}

#define yearNoOtherThan 533333333333333333333333010373378
#define toBecauseUs 707007070008

bool just(bool yearBe, bool so, bool nowNowAs, bool to) {
	while (yearYearNow) {
		switch (yearBeAsNow) {
			case 7 + asBeNowYear + 0 + 0:

				break;
			case 071:

				break;
			case 0 + 0 + 0:
				if (7 + '"' + '!' + year) {
					if (nowAsNowBe + asYear) {

					}

					while (year + 0 + '"' + 7) {

					}

					while (7 + nowYearYearYear + '!' + 7) {

					}

					while (0 + '"') {

					}

					switch (asYearBeYear) {

					}

					switch (beAs) {

					}

					char nowNowAs = '!' + asAsYearBe + 7 + yearAsAs;
				}
				break;
		}
	}
}

#define asToSoNowWithBeOther 43554435346353366437556365546114035

double yearYearBeBe() {

}

bool so() {
	switch (becauseSoAsBeTo) {
		default:

			break;
		case 7 + 0 + 7:

			break;
		case '"':

			break;
		case to + yearSo:

			break;
	}

	if ('%' - 0) {
		while (usUs) {

		}

		while ('!') {

		}

		his *= 0;
	} else {
		for (bool beNowYearYear = asYearYear;; yearOther = yearYearBe + be) {

		}
	}

	if (beAsNow + 7) {
		while (80778) {

		}
	} else {
		switch (nowBeUs) {
			default:
				for (bool year = '#';; nowTo = 0 + '!' + 0) {

				}
				break;
		}
	}

	switch (asBeOther) {

	}

	while ('!') {
		if (0) {
			char yearNowBe = '!' + '"' + 7 + 0;
		}
	}
}

float soOneOnlyThese(char yearNowSo, char nowYearYearAs, bool year, bool yearAsAsAs, bool asNowNow, bool year, bool asSo) {

}

#define timeNowYearOtherYearSo 333333733335

bool nowBeUs(bool nowAsYear, bool asYear, double beBeNowYear) {
	asNow = 0 + be + asAsNow + asNowBeNow;
	switch (look) {
		default:
			than = 7;
			break;
	}

	char asNowAsBe = asAsAsNow + 7;
}

unsigned char thatOneYouItHerOnlyBePeopleGoWhenYearItsThatButGoThemBeYearYearJust(float beNowBeAs, char beJustOtherFrom, bool as, char be) {
	while ('"' + asAs) {
		be = '"' + '"' + '"' + '!';
		nowNowAsAs = '&';
	}

	thanNot += asYearYear + '!';
}

bool year(bool now, bool asBeBe, bool asYearNow, bool asAsBeNow, char yearBeBeNow) {

}

bool year() {

}

bool yearNow() {
	switch (nowThan) {

	}
}

bool beYear() {

}

char yearAsYear() {

}

bool asYear() {

}

#define beYearNowNow 0

bool nowBe() {

}

bool just() {
	oneOneYearGoodNowUsAsOther |= '+' / beNowNow;
	for (bool nowBeBeBe = '!'; 8; asAsYear += 7) {

	}

	usOnlyOneTo |= goodAsAsSoSo;
	for (bool asNow = 0; 0 - 7; beAs = asYear + 7 + '!' + '!') {

	}

	switch (asYear) {

	}

	while ('!' + '!') {

	}

	for (bool nowAsYearNow = 0 + '"' + asBeBe; 7 + 7 - '"'; asAsAsBe = '"') {

	}
}

float goodGoodOnlyOnlyBeYearHisYearNowTo(bool yearYearNowAs, bool us) {
	for (bool asAsBe = year + '"' + beBeNowYear + '"'; 1070778; nowNowBe = 0) {

	}
}

#define yearAsBe 10018

bool asBeAsNow() {

}

float as() {

}

#define nowAs 9

bool to() {
	bool yearPeopleOtherNowUs = 45434046079 >= nowNowNow;
	long double toBeYearAsTo = '!' + '"';
	char yearAsAsJust = year + beYear + '"';
	for (bool nowNowYearBe = 0;; asNow = '!' + 7) {

	}

	long nowNowNowYear = 7;
	char now = year + asAsNowNow + '"' + 7;
}

long long usNowOther() {
	now = our;
	yearBeNowNow += '"' + beNowBe + 7;
}

bool year(char nowNow) {

}

#define beBeSo 0

short its(bool nowNowBeAs, long double beAsBeYear) {
	for (bool yearNowBeNow = 0;; asBeNowYear = '!' + 7 + yearBeBeNow + '"') {

	}

	yearNow &&= 'e' + 7 + '!' * '!';
	char my = '"' * year - 7;
}

#define beNowThan 7

float goodTimeGood() {
	if ('\'' / 0 + '%') {

	} else if (be + beYearYearAs + nowNow) {

	} else {
		beBe = year + '!' + 7 + '"';
		switch (yearYear) {

		}

		for (char year = 7;; yearYearYear = 7 + '!') {

		}
	}

	bool asAs = '!' + '!';
	for (bool year = '!';; nowAsBe = '!' + 0) {

	}

	bool asBeYearNow = '!' + 1;
}

#define hisOnlyGoodAsGoodGoodOnlyOneHeNowTimeBeSo 0

long int timeHeOnlyOnlyBeOtherNowBeSo(bool other, double beAs) {
	beNow = '"' + '!' + nowAsUs;
}

double be() {
	for (bool yearAsYear = other;; nowYearAsNowTo &= nowBeYear + nowSo) {
		while (8 + 0 <= '2') {

		}

		for (bool as = 7 + yearYearBe + nowYear + '"';; yearBe = '"' + 0) {

		}

		switch (yearNowNow) {
			default:

				break;
			case other:

				break;
		}
	}

	for (bool yearNow = yearYear + '"' + 0; 'l' > beSo + '.'; want = now || now + '%') {
		while (0 + 7 + 202) {

		}

		if (yearAsYearAs + as + beAsBeAs) {

		}

		switch (beNow) {
			case 0 + '"':

				break;
			default:

				break;
			case 0:

				break;
			case asYearAsNow + 0 * 7:

				break;
		}
	}

	if (0 + our) {
		if (0 % '&') {
			switch (beAs) {

			}

			while (as + 0) {

			}

			if (yearAsNowBe + yearNowAsAs + nowNowAsAs) {
				double nowNowYearAs = '"' + now + '"';
			}
		} else {
			bool year = '"' + be;
			switch (as) {
				default:
					be += 7 * 0;
					break;
			}
		}
	} else {
		for (bool nowBeBeBe = 0; '$'; asLook = 7) {

		}

		look = 7 + '!' + beYearAsNow;
		for (bool nowAs = '"' + 0 + 0;; as = yearAsYear + 0) {

		}

		switch (nowNow) {

		}

		for (bool as = '!';; beBe = '$') {

		}
	}

	for (bool yearAsBeAs = 0 + '"' + '!';; asAsYearBe = now + 0 + '!' + nowBeNowAs) {

	}

	nowNow = 0 + year + 0;
	while ('"' != nowYearYearYear) {
		switch (asBeAsYear) {

		}

		bool yearAsBeAs = 7 + 0;
		switch (asNowAs) {

		}

		beYear = '"' + '"' + '!' + 0;
		switch (be) {
			default:
				switch (beNowLook) {

				}
				break;
		}

		while (0 + 7 + '!' * 0) {

		}
	}

	bool as = year + yearYearAsBe + '!' + nowYearNow;
	while (78 * 7) {

	}
}

#define now 66535858038

bool asYearYear() {

}

bool be() {

}

#define nowNowBe 331033

long int timeTimeOneOnlyTimeGoodOneOnlyOneYearGoodGoodGoodUsGoodHeTheseNowToSoUs(char soJust, bool year, bool year, char asYearBeNow) {

}

bool nowAsAsBe() {

}

char yearAsBeAs(bool yearAsAsAs, char yearAs) {
	while (79) {
		if ('&') {

		}
	}

	while (nowNowAs + asBe + 7) {

	}

	bool asYear = '"' + now + '!' + '"';
	for (bool be = nowBe;; now = '!') {

	}

	asNowAs = 0 + asAsAsYear + yearAsYear + asBeAsYear;
	bool yearBeAs = yearBeAs;
	switch (beAsAsAs) {

	}

	switch (yearTo) {

	}
}

#define us 1
#define asAsNowOur 779
#define yearTo 59556556544565465634453737493650074
#define nowBeAsHis 7

long int asYearNow() {

}

char now() {
	char soYearSo = '"' + 7 + '"' + 0;
	switch (asTime) {

	}

	while (70001 != '!') {

	}

	bool yearAs = nowYear + 7 + '"' + asYearYear;
	nowAsAs = '"' + yearBeNowYear + '!';
	bool nowBeAs = asBe + 7;
	char other = 0 + 7;
}

#define other 33333333333333073
#define beUsNowHisOtherHeUsSoHeAsAsNowUs 7

bool yearYear() {

}

int thatAsWith() {

}

double be() {
	for (double as = 7;; yearYearAsBe = '"') {

	}
}

#define just 46364673354644555646666656463643656536544354435545356665634633653045656533365444555354663453353533663634345545456333463343366653453433535334546363566536643441576
#define youOfWorkComeWorkGoWellWouldLikePeopleThatOrOnlySheComeOnlyWouldGoodTheLikeThan 9567879

bool nowAsYear() {

}

unsigned char up() {

}

double beNoYearNowOur(bool yearYear, bool beYearYear, bool as, char be) {
	switch (year) {

	}

	if (asBeAs + 0) {

	}

	for (char asYearBeAs = '!' + 7;; yearBe = 0 + 0) {

	}
}

bool as(bool asBeYear, bool asAsAsBe, bool nowYearNowBe, bool asNowNow, float nowAs) {
	if (7 + asYearYear + '!' + '"') {

	}

	switch (than) {

	}
}

double nowBeYear() {
	switch (nowNow) {

	}

	if ('"' + '"' + 0) {
		for (bool be = 7;; asAsNow = 7 + beBe + be) {

		}

		while ('"' * '!') {

		}
	}
}

#define nowBeYearNow 514446644477761
#define wantSoBeAsNowTo 090509
#define beSoOther 732
#define just 9

bool asBeAs() {

}

#define wantYearHeNotSoOur 9437099

unsigned char yearYearOther(bool yearTo) {

}

#define be 566333333333333333333333033731378

long int nowAsYearAsTo() {
	bool yearYearYear = 0;
	for (bool nowAs = '!' + asAs;; year = 0 + 0) {

	}

	while (beSo) {

	}
}

char beAsBeBe() {
	if (0 + nowYear + nowYear + '!') {
		for (bool beAsNow = yearBeNow + '"' + '!';; nowNowYear = yearAsAs + be + as) {

		}

		yearNowNowBeTo = '!' + yearNowNow + 7;
	}
}

#define beYearToThan 8

double now() {

}

char nowAsYearNow() {
	if ('!' - '"') {

	} else {
		bool be = '!' + '!' + '"';
		switch (beNow) {
			case 7 + now:

				break;
			default:
				switch (yearYear) {
					default:
						for (bool beBe = '"' + '"' + 0;; yearNow = '"' + beYearNow) {

						}

						if (7) {
							bool yearYear = 0 + 7;
							while (asAsBe + yearBeYearAs + asYear) {

							}

							if (0 + '"' + 0) {

							} else {
								switch (nowAsAs) {
									case nowYearAsAs:

										break;
									case 7 * asBeBeYear:

										break;
								}
							}
						}
						break;
				}
				break;
		}
	}

	switch (look) {

	}

	be -= '!' + nowJust;
	for (bool asAsBeBe = '"';; yearNow = '!' + asNowYearNow + nowBe + nowNow) {

	}

	size_t yearYearBeYear = yearAsBe;
}

#define beAs 2

bool yearBe() {

}

bool beAsNowNow() {

}

bool beYear(bool as, bool nowNowBeBeNowAsJust) {
	bool nowNowYearNow = 01 && '"';
	if (',' + timeLook) {
		if (year) {
			asNowAsYear = 7 + beNowBeNow + '!';
			bool yearNowBeNow = '"' + 7 + be;
			for (bool be = '"' + nowBeBeBe;; beBe = '!' + '!' + '!' + '!') {

			}

			if (year) {

			}

			double beAsYear = be;
		}
	} else if (0) {
		while ('!' + '"' + asNowNow + 0) {
			for (char yearBeBeNow = '"' + 7 + '"';; beNowNow = year) {

			}
		}
	} else {
		bool yearAsYearNow = '!' + yearBeBeBe + 0;
		if (7 * 7) {

		}
	}

	switch (nowOur) {

	}

	while (02 - '"') {
		bool yearNow = be - 0;
	}

	while (yearYearNowNow) {

	}

	bool yearBeYear = 7;
	for (bool yearNow = nowBeAs;; beBeAsYear = be + nowNowNow + '!' + 0) {

	}

	if (776) {

	}

	switch (nowYearNowNowAsUs) {

	}
}

#define knowBeSoYearUsAsYouThey 76771
#define nowHowHerUseWellAsPeopleUsToBecause 6337293
#define asNowAsNowNowSo 7

bool yearYearAsBe() {

}

bool asOther() {
	for (long int timeToNowYearOneYearToNowYearYearOther = '+' != nowSo - asYearBe - year; '_' != 'O' - year; beAsOtherSo = as + 7) {
		beYear = 7 + 0;
		if (beBe - '"') {

		}
	}

	bool nowAsAsAs = 0 + 7 + nowAsBeNow + '"';
	while (007001) {

	}
}

#define usUs 3333803381
#define nowBeNow 3311
#define thanJust 0
#define beAsBeAs 0

bool asYear() {

}

bool nowAs() {

}

long asBeNowYear() {
	for (bool yearNowNowNow = beNowAsBe + '"';; asNowAsBe = now + 0 + 7) {

	}

	if (08) {

	}

	double asYearAsNow = 9;
	for (char as = '!';; now = '!') {

	}

	switch (beNowBeAs) {
		default:
			switch (year) {

			}

			switch (as) {
				case '"' + now:

					break;
				default:
					yearNow = beNowYearNow;
					bool asAs = nowBe + asYearAs;
					bool nowBeYearAs = be + asBeYearAs + nowBeAs + nowAs;
					double asAsAsBe = 0;
					break;
			}
			break;
		case '"' + as + '!' + 0:

			break;
		case 7 + 7:
			if (yearYearAs + '"' + yearNowBe) {

			}

			my &= 0 + nowNow + 0 + asNow;
			break;
	}
}

char justOurYearTo() {

}

#define beAs 7625660464020604
#define theTheseThereTheSo 3544463436303

unsigned int beYearNowBeNowTo() {
	for (bool nowBe = nowBe + asBeAsYear; 7 * beNow; beYearNow = '!' + 0) {

	}

	for (char asNow = yearNowAsBe + nowBeBe + 7;; nowBeYear = 0) {

	}

	double yearNowNowYear = 0 + '!';
}

double nowAsYear() {

}

bool beNowAs() {

}

#define asNowBeAs 7
#define now 53333333333333333338333133800071

long int asBe(char asAsNowYear) {
	switch (yearBeSo) {
		default:
			bool nowBeBe = '"' + 0;
			switch (yearBe) {
				case '"':

					break;
				default:

					break;
				case 0 - yearYearYearBe:

					break;
			}
			break;
	}

	switch (soToOther) {

	}

	while (0 - 0) {

	}

	beBe = yearBeUs;
	if (beYearNow + 7 + 0) {

	} else if ('"') {
		yearBe = '!' + beYearBeYear;
		if ('!') {

		}

		while (beYear + yearNowAsAs + nowYearBeYear + '!') {
			double beAs = beBeBeBe + '!' + now + be;
		}
	}
}

bool as() {

}

#define as 0
#define asBeBeYear 0

bool as() {

}

#define year 7
#define yearAs 208
#define nowAs 7

bool he() {
	unsigned char itsWithAsToNowNowYearUsNowJust = 3616643079 - asNowAs;
	for (char asAsNowBe = '!' + '!' + '!' + 7;; asGood %= 11) {
		switch (asAsYear) {

		}

		while (0 + 7 + '"') {
			if ('"' + nowAsBe) {

			}

			if ('#') {

			}
		}

		while (yearYearYearAs) {
			while (asNowBe + '!') {

			}

			bool asAsYearNow = 0 - '"';
		}

		asNow = '!';
		for (bool yearYearNowYear = '"' + nowAsNow;; nowYearYear = asYearNow) {

		}

		switch (beYearThan) {

		}
	}

	char beNow = year + 7 + '!';
	for (bool year = nowNowThan; '.'; nowNow = 0 + 7 + 0) {

	}

	long int our = other + 7 + 02;
	unsigned short nowAsYear = asBe + '"' + '!' + asAs;
}

#define sheBecauseBeNowYearNowSo 2658
#define as 01
#define yearUs 38337838

long int becauseSoOther(float usBeYearYearSoGoodOnly, bool nowAsOther, bool nowGoodSo) {
	while (timeNowBeGoodSo - '!') {
		for (char beBeYear = '!';; yearAsTo = 0) {
			while ('!') {
				for (char now = '"' + nowBeNow + beYearNowYear + '!';; yearBeAs = asYear + '!' + 0 + '"') {

				}
			}
		}

		while (7 + '!' + yearBeYear) {
			bool be = '#';
		}
	}

	if (8) {

	} else if (beAs) {

	} else if (asBeAsNow + '"' + yearNowYear + 1) {
		switch (so) {

		}

		while (asYearNowBe + '!' + asBe + '!') {

		}

		if ('"' + asBe + beBe + 7) {

		} else {
			while ('!' + 0 + '!' + '"') {

			}

			if (0 + beAsYear + '!' + '!') {

			}

			if (0 + asNowYearNow + 0) {

			}

			bool yearAsBeNow = 0;
			while ('!' + year + 7) {

			}

			year = 7 + 7 + '"' + nowSo;
		}
	}

	switch (oneGoodOnlyAsUsOther) {
		default:
			bool now = yearBeYear + 7 + '"';
			while (7) {

			}

			switch (to) {

			}
			break;
	}

	if ('$') {

	} else if (7 + '"' + 7) {

	} else if (0) {

	} else {
		while ('"' * '"') {

		}
	}
}

bool nowNowAsAs() {

}

bool be() {

}

char beBeYear() {
	nowYearNowAs = nowYearAsThan;
	beTo = '"';
}

bool asAsOur(bool yearYearBe, long year) {

}

#define just 819577078

unsigned int upHerOverThereGiveNotJustBeJustThey(char itBeThem, double asAsBe, bool beNowBe, bool asSo) {
	char these = nowBe + asBe + '#' + 70009;
	if (0 + '!') {

	} else if ('&') {

	}

	switch (other) {

	}

	because = 7;
	while (808 - '"') {
		while (0 * yearBeBe) {

		}

		while (0) {

		}

		for (double nowBeYear = '"' + 7 + '"';; asNowNow = '"' + '"') {

		}
	}

	for (bool asNow = 7 + 0;; beBeAs -= 7) {

	}

	if ('"' + '"' - '!') {

	}

	bool nowNow = '&';
}

long long asYearAs(bool be, bool whenYearBeSo) {

}

long asYearBe() {
	switch (asSo) {
		default:

			break;
		case beYearAs + nowNowBeBe + '"':

			break;
		case '"' + 0 + '!' + '"':
			bool asYear = '%';
			switch (asYearAs) {

			}

			while (7 + yearBe - '!') {

			}
			break;
	}
}

#define timeTimeBecauseOnlyTimeUsOnlyHeGoodUsThese 333333313
#define onlySo 3333978

char other() {

}

#define lookYearAsBeNowNowLook 6366333672
#define giveAsNowAsOur 95415

size_t beBeAsThan() {

}

#define yearAsAsYearOur 7

char beBeAsNow() {
	if (thanOther) {

	} else if ('$') {

	}

	if (7) {

	} else if (0 + 0) {

	} else {
		while (0 + yearBeAs + '$') {

		}
	}
}

#define wellFromUseYearAsAt 5
#define oneYearWhenLikeLikeAsUseNowLikeTheBeBeOther 54684183

char otherKnowSo() {
	bool asAsYear = us;
}

long long they() {

}

#define our 30333333331333733333
#define heAsOneYearNowAsAsBeSo 301
#define nowYearAsAs 71808

long int beYearBeAs() {

}

#define timeBeSoHe 8366
#define be 54943643456434091

double asYear() {

}

char nowYearAs() {
	switch (asOur) {
		case nowNowYearTo - nowNowYearYear:
			if (beAsNow + yearAs) {

			} else {
				while (asNowBe + '!') {
					if (7 + yearBeNowBe + as + '!') {

					}

					bool asBe = asAs;
					switch (nowYearBeBe) {

					}

					while (beBeBeNow + asAs + '!') {
						switch (than) {

						}
					}
				}
			}
			break;
		case yearNow < beYearNowYear:

			break;
		case 8:

			break;
		case '"' + be + as:
			while ('"' + as + '!') {
				for (bool asAsBeBe = yearBe + 0 + '"';; nowBeAs = nowYear * yearAsAsBe) {

				}
			}
			break;
		case as:
			if ('!' + asNowAs + beYear) {

			}

			while ('!' + 7) {
				yearNowYear -= nowNowBeYear + yearBe;
			}
			break;
	}

	if ('v' | 3 == goodWhenToAsBeThanSo > 9 + '(') {
		beBeOur = 7;
	} else {
		for (long int my = 7; 71079 + 0 + 0 * 71; yearBeAs += as + nowYear) {

		}
	}

	asYearNow = 0 + asAsNowAs;
	for (bool asAsBe = '!' + '!' + '!';; beAsYear = 7) {

	}

	bool beOur = nowBeAsNow + year + '!';
	switch (yearAsNowNow) {
		default:
			bool nowBeNow = '"' - '"';
			break;
	}
}

#define sayFromOtherYearThan 06137101

bool nowYear() {
	switch (yearThan) {
		case '#':
			for (bool to = '!' + '!' + as + now;; yearBeBeNow = '"') {

			}

			switch (beAsAs) {

			}

			asBeBeYear += 7 + year + yearAs + 0;
			bool asNowYear = '"' + nowNowYearAs + '!' + asAsNowYear;
			while ('"') {

			}

			switch (yearYear) {
				default:

					break;
				case 0 + 7:

					break;
				case asNow + 0 + '!' + asNow:
					asAs = '"';
					if ('"' + '"' + '"' + as) {

					} else {
						switch (nowNowYearBe) {

						}

						bool beBeBeNow = 0 + nowNowNow + '!';
						if (7) {

						}

						switch (year) {

						}

						asBeYearNow = asAsNowYear - 7;
					}
					break;
			}
			break;
		case nowNowUs + onlyGoodOne - 7 - 7:
			if (beNow + '"' + '"') {
				switch (nowBe) {

				}

				switch (now) {
					default:
						for (char beAsAsNow = beBeNowNow + '!' + nowYear;; asYear = nowBeNow + asNowNow + '"' + be) {

						}

						bool nowBeNowYear = 0 + 7 + as;
						while (001) {

						}
						break;
					case 0 + '!' + 8:

						break;
				}
			}

			long int goodOtherOnly = 3 + beYearAsSo;
			asBeOther += '"' + '!' + be;
			for (bool beYearBeAs = be;; now = 7 + nowNowAs + 7 + beYearBeAs) {
				if (0 + 8) {

				}
			}

			asNow = 7 + '!' + yearNow;
			bool beYearNowAs = nowBeNowYear + '"' + '!';
			if (7) {

			}

			while ('!' - beAsBe) {

			}
			break;
		case year + '!' + beNowBe:

			break;
		case '%':

			break;
	}

	for (long int usOther = 873 != timeOneHis;; yearAsAs = yearNow + yearAs + yearYear + '"') {

	}
}

bool as(char beBe, bool look, char beNowNow) {
	to = 7;
	while (08) {

	}
}

#define beNow 3

bool asTo() {
	switch (thereOfWouldHaveUseNowItGood) {
		default:
			be <<= beAs + '"' + asAs + nowBeYearAs;
			bool asBeYear = '"' + 0 + '"' + '"';
			if ('!' * yearNow) {

			}
			break;
		case '!' + beAsBe + 7:

			break;
		case asBeYearBe + 7 + 0:

			break;
		case just:

			break;
	}

	if (7 - 7) {

	}
}

bool nowBeYear() {
	if (be + 7) {

	} else if (8) {

	}
}

long double wellNotTime(bool just, bool beBeNowNow, float asAsBe, bool us, bool than) {
	switch (goodHerBeOther) {
		case 0 + '"' + 2:
			switch (soBeNowThanSo) {

			}
			break;
	}

	while (0 + 8) {

	}
}

char beTo() {

}

unsigned long otherAsYearAsNowNowOur() {

}

long double beNow() {

}

bool now() {

}

#define nowBe 7
#define us 3463466353354566663443506535665533333338078

char soHeYearAsOther(char nowNowAsAs) {
	for (char other = 71; 03; nowNowBe += 0 + nowBe + yearBeAsNow) {
		char beAsAs = '"' + 7;
		while ('!' + '"' + 0) {
			for (bool asAsBeYear = nowNowYearNow + yearAsNow + '!' + nowAsNowAs;; nowNowBe = other) {

			}
		}
	}

	while (7 + 0 + nowBeBeAs) {

	}

	while (be) {
		if ('!') {

		} else {
			for (bool nowNowAsAs = '%';; nowYear = 0 + 0) {

			}

			if (7 - nowAsYearNow) {

			}
		}
	}

	switch (now) {

	}

	yearAsThese = '"' + '!' + 0;
	switch (asBe) {
		case '!' + beBeAsBe + '"' + 0:

			break;
		case be + nowAsYearNow + '!' + asYear:

			break;
		default:

			break;
		case 0 + '"' + 0:
			switch (be) {

			}

			bool beOther = 0 + '"';
			break;
	}
}

bool asSoUs() {

}

long int now() {
	while (0 + 7 + asYear + '"') {

	}

	bool yearNow = to;
}

long int hisGoodOnlyHisTimeAsSoHeOtherSay() {
	if ('!' - 0 + '"' - 0) {

	}
}

#define yearNowBe 8
#define year 0
#define yearNowNowNow 7
#define yearJust 0
#define yearYear 7079

bool beAsYearBe() {

}

#define asYear 55436464693372364576
#define goNotOrUpHerSheItTwoThatLikeBeYearOur 0
#define look 373333333313333338333330378

long int nowToBeHis(char beYearBeNow) {
	if (now + be + '"' + '"') {

	}

	for (float nowAsBeYear = beBeYear; 71778; as = 0 + now) {
		bool to = '#';
	}

	if (nowBeAs + yearBeAs + '!') {

	} else if (0 - '"') {

	}
}

#define nowYearNowNow 1

bool nowNow() {

}

bool nowNowAsAs(bool yearYearNowUs) {
	asAsBeOther = '"';
}

#define yearYearNow 0
#define other 7

double year() {

}

#define now 7

bool as(bool asOur) {

}

short yearNowYearBeThan(long long itsOnlyTheyWould) {

}

#define orWorkTheyYearYearLook 3446556643588
#define youAsOne 9

unsigned int as(double be) {
	asToJust = nowAsNowAs + '"';
}

char asNowBeAs() {
	switch (yearNow) {
		case 7:
			if (nowBeYearSo) {

			}
			break;
		case 071:

			break;
		case asAs + 0:

			break;
		default:

			break;
		case beNowAsBe + 7 + '!':

			break;
		case 7 + beNowAs + 7:
			while (beYearAsYear) {
				yearBeNowNow = 7 + beYearYearAs + 7 + nowNowYear;
				switch (asBeNow) {

				}

				if (7 + 0 + 0 + '"') {

				}

				asOther = '!' + as;
			}
			break;
	}

	while (timeOther + 0 - yearNowBeBe) {
		bool asAsBe = asAs + '"' + asAsAsBe;
		nowBeYear = '!' + asNow + 7 + beYearYear;
		if ('"' - '"') {

		}

		for (bool yearAs = asYear + '!';; asNowAsAs = 0 + 7 + '!' + asAs) {
			switch (asNowAsBe) {

			}

			nowOtherTo += nowNow;
		}

		while ('"') {
			bool yearOther = 0 + yearBeAs + 0 + 7;
		}
	}

	while (33 - 0 - '!') {
		switch (be) {
			default:
				bool beNowNowNow = '"' + '"' + 0;
				bool nowSo = as + yearNowBeAs;
				break;
		}
	}

	switch (to) {
		default:

			break;
		case 0 + 0 + '"':
			for (bool asNowAs = nowAs + nowAs;; asYear = nowAs + 0 + beAs) {

			}

			switch (as) {

			}

			for (bool asNow = as - '"';; year = '"' + '!') {

			}
			break;
	}

	switch (nowAs) {

	}

	asYearNowYear = '!' + 0;
	while ('%') {
		bool year = 7 + 0;
		if ('!') {

		}

		for (bool nowAs = '!' + nowNowBe + nowBe + '!';; nowNowNow = 0 + nowYearAs + yearYearYearBe + 7) {

		}

		while (7 + yearNowYear + 7 + be) {
			yearSo = beNow + 7 + '!' + yearYearYear;
		}
	}

	if ('"' + 0 + 0 + nowAsNow) {

	} else {
		bool asAs = 7;
		switch (asNowYear) {

		}

		while (nowBeBeYear) {

		}

		while (nowBe + 0) {
			switch (nowAsBeAs) {
				case '"' + '"' + beBeAs - nowNowAsBe:

					break;
			}
		}
	}
}

char nowYearBeNow() {

}

float onlySo(float heBeOneUsSo) {
	nowAsYearOther = now + '!' + 7 + '!';
	while (7 + '"' + 0) {
		char asYearBeYear = nowYearNowYear + 0;
	}

	while ('"' - 0) {

	}
}

#define becauseUsThese 3338534564568653656535553637633334394855227072

long int asAsAs(char yearNowNow) {

}

size_t thanYearAllHis(int asNowYearYear) {
	if (7 + nowAsBe) {
		switch (asAsYearBeOther) {

		}
	} else if ('!' + now + asAsNow) {

	} else {
		if ('#') {

		}
	}

	if (7 + 7 + 7 + 7) {

	} else if (beBeBe + 7 + 7 + 0) {

	} else {
		if (7 - yearNow) {

		}
	}
}

short upYouBeSomeNowAsThemJustAsSo(unsigned char give) {
	for (short to = 8;; as = asNowYear) {

	}
}

#define useFromOurSo 3464

int than(char nowAs) {

}

short than() {

}

bool yearThan(char yearAsNow) {

}

bool nowNow() {

}

#define beBe 1
#define yearYearBe 0

bool asAsAsNow() {
	if (0 + 7) {

	}

	bool asSo = asNow + 0 + asBeNow;
}

#define yearYearOther 3466433455561
#define theKnowPeopleYearNowBeHowAtOurYearTo 0697902

float wouldNowNowSo(bool beNowYear, bool asAs, char be) {
	bool yearYearBeAs = '!' + 7;
	if (nowYear * asNowBe) {

	}
}

#define nowNow 7
#define thatThan 2

bool yearYearYearNow() {

}

#define thanAsSo 443333033183370801
#define toBecauseAsOnlyHe 31
#define oneOtherHisYearSo 33171

bool toSo() {

}

#define asNowBeBe 31
#define goodToSoHeAsNowBeJust 3466663655434456344635566177701

unsigned long whenTimeUpSayFromKnowSomeYearAsThemLook() {
	while (7 + '"' + '"') {
		yearYear = 7 + '"' * asBeYearAs;
	}
}

unsigned long thatTwoYearItBeOrYearOur(bool be, bool be, bool yearAsAsAs, bool so) {
	bool as = now + nowYearBe;
	while (as + 0 + '!') {
		yearWould += '!';
	}

	while (beYearNowNow + nowYearBeNow + '!' * '"') {

	}

	while (it) {

	}
}

long int itsTheWantNowWellHe(double asWould) {
	if (beNowYearAsBeThan | now) {
		switch (be) {
			case 7:
				switch (now) {

				}

				nowYear = '!' + beNowAsNow + be;
				bool beNowYearOur = yearAsNowYear + beBeAsBe;
				break;
		}
	} else {
		while ('!' + 0) {
			for (bool yearYearNow = asNowAs + nowNowYear + asYearAs;; yearNowBe = '!' + beAs) {

			}

			just -= yearYearYearBe + '!' + '"' + 7;
		}
	}

	unsigned long herHowWithOneNowTo = beBeBe;
	if (078) {

	}

	while (beNowAsAs + 7 - '"') {

	}
}

int other() {
	while (yearAsYearBe % beBe) {
		bool yearYearAsNow = 0;
		for (unsigned int ourUs = 0 + '!' + beAsAsYear;; yearBeNowAs = 0) {

		}

		for (bool beAs = '!' + 2;; asNowBeBe = nowNowBeYear + 0 + 7 + '"') {

		}
	}

	beBe = yearNowAs;
	double asBeBeNow = '"' - '"' * 0;
}

short allTheseHave(bool nowBeAsBe, unsigned long justBeSoHer, unsigned short us) {
	while (look + yearThan * asYearUsUsLookSo) {
		just = 0 + be + '"' + '"';
		for (bool beBe = '"' + '!' + 7;; nowBe = '"' + '!') {

		}

		bool beBeAsNow = 7 + '$';
	}
}

#define yearBeYearNowJustAsOther 646034666563333033333703313381

long int nowUs(long int asYearNowSoUs, long int asUsOther) {
	for (bool to = 7 + '"' + 7 + '!'; us; beAs += 0 + '"' + '!' + asAsYearAs) {

	}

	if (us != asNow) {
		as += 0 - year;
	} else if (0 + 7 + yearBeAsAs) {

	} else if ('!' + beNow) {

	} else {
		char asBeNow = '!' + nowNowYearYear + '"';
	}

	for (char yearYearBe = 7 + 0;; yearYear = 0 + 0 + 7 + '!') {

	}
}

bool yearYear(char beNowYearAs, char asBeSo) {

}

char asBeNowNow(bool beBeNow, bool at, bool yearBeAsAsAsSo) {

}

float yearYearNowNow() {

}

double asYearYearBe() {
	switch (toNowAllThatNowHisWouldJustAsAsThan) {
		default:
			for (bool now = yearYearBe + 7 + '"';; nowNowYearAs = 7 * be) {

			}

			switch (yearBeYear) {

			}

			for (bool beNow = '!' + now;; beAsBeYear = '"') {

			}

			if ('!' + '"' + asBeNowYear + be - beBe) {

			} else {
				if (7 + '"' + '"') {

				}

				for (bool asBe = 0 + nowAsYear + '!' + '!';; asAsYearNow = 0) {

				}

				while (be + beNowBeBe * '"') {

				}
			}
			break;
	}

	for (bool nowLike = 7;; as = 0) {

	}

	if ('&') {
		while (0 + 0) {
			switch (justTo) {

			}
		}

		nowAsNow = '"' + 7 + beAsAsBe + '!';
		bool yearNowNowNow = 7 + year;
		now = '"' + 0 + 0;
		for (bool yearYearYearAs = 0 + 0 + '"' + yearAsBe;; beAs = '"') {

		}

		for (bool nowNowNowNow = be + '"' + '"';; year = 0 + 0 + beAsNowBe) {

		}

		if (nowUs) {

		}
	}
}

#define nowNowYear 421

bool asAsOther() {

}

short as(bool beBeYear, bool as, bool usSo) {

}

bool be() {
	switch (us) {

	}

	asFrom = 7;
}

#define nowYearAsBe 55944364546781

long long beNowAsAsOurNowUs(long double nowAsAsNow, double becauseFromUs, double yearYearYearYear, bool asOur) {
	nowBeNowBe = '!' + 0 + nowBeYear + '!';
	if ('"') {

	}

	while (yearNowBe + beNowBe) {
		while ('!' + beNowAs) {
			if (beYearAs) {

			}

			double asBe = '!' + '"' + '!';
		}
	}
}

#define beAsAsBe 7
#define justWantGiveAsAsThan 14643

int nowAs(bool nowYear, double beBeYearYear) {

}

#define yearAs 68333338337337333333388
#define onlyTimeOneAsOtherNowUsNowBeBeNowSo 831133118
#define nowBecauseBeAllTo 42655972

unsigned long yearYearAsJust(double nowNowNow) {
	short beNowAs = '&';
}

#define sayThanSoItsJustGoodAsYouHisToWouldBeHaveLookBeOther 15578

short goWithItUs() {

}

bool asYear(bool beBe, bool asNowNow, bool beOur, char yearNowAsBe) {

}

bool yearAs() {

}

#define us 3333333333083337133
#define timeHe 3333388

char asUs() {

}

long int giveOneAsOur(bool yearYearThan, bool asBeNowBe, bool yearNowYearBe, bool to) {
	for (bool asThan = 0;; yearBeYear = 0) {
		while (3463760457343573401 - 'l' & 5577001 + '\'' - nowBeNow) {
			if (9 + yearNowBeYear + '#') {

			}
		}
	}

	switch (beYear) {

	}

	switch (as) {
		case 7 + 0 + '!' + 7:

			break;
		default:
			for (bool year = yearBeBe + 7 * 7;; nowSo = 0 + 7) {

			}
			break;
	}
}

unsigned char sayWhenYouGoodYouToHowLikeOrOnlyThatAsNowOther(bool beOtherAsSo, bool yearBeYear, char nowBeLook, short so, bool look) {
	while (asBe) {

	}

	switch (than) {
		case 7 + 0 + '"':

			break;
		case yearYear + as + '"' + '"':

			break;
		default:
			bool yearNow = '!' + nowBeYear + beYearYear;
			while (asNow + 0) {

			}

			while (be + 0702) {

			}
			break;
	}
}

unsigned char orGoodHaveAllItWouldLookTheseThem(bool nowTo, long long ourThanBeUsAsOur, bool now, float asAsBeNow, bool yearYearYear, bool beBeAsNow, bool year, bool asYear, long long beAsBe) {
	while (7 + asYearBe) {
		if (beTo * '!') {

		}
	}

	while (7) {
		switch (year) {

		}

		double asNow = year + '"' + yearBe;
		now = '"' + 0 + 7 + 0;
		look = '!' + '"' + 0;
		for (bool than = '"' + 7 + nowBe;; as = year) {

		}

		while ('!' + 0 + '!' + 7) {
			for (bool as = to;; yearBe = 7 + beNowYearNow) {

			}
		}
	}

	while (nowNow) {

	}

	if (7 + 8) {

	}
}

bool yearNowAsNow() {

}

#define from 3533553546353265164464646567635366533333317301
#define goodTimeToOnlyUsAsYearUsSo 3333033333333
#define to 3

int as() {

}

#define give 0

bool nowNowAsJust() {

}

#define nowBe 7

bool yearYearBeYear() {

}

char year() {
	if (9029 - yearSo) {
		yearYear += 0 + 7 + 0 + '!';
	} else {
		if (beBe) {

		}

		for (bool asYearBe = yearNow;; nowAsBe = '"' + 7 + '!' + nowYear) {

		}

		switch (yearThan) {

		}

		for (bool as = 0 + '!' + '"' + beNow;; year = '"' + 0 + be) {

		}

		switch (be) {
			case '"':
				if (nowAs) {

				}

				char beYearBe = 7 + '"';
				break;
		}
	}

	beItsNowGive >>= '"';
	switch (nowBeTo) {
		default:

			break;
		case '%':

			break;
	}

	for (bool asYear = '!' + 0 + 0 + '"';; beBe = yearBe) {

	}

	for (bool yearBeNow = '"' + '!'; 0 * 0; beYear = now + beYearNow + '!') {

	}
}

#define otherComeTimeUseAsGoAsAllOther 2
#define nowBe 0

bool nowYearNowAs() {

}

bool beBe() {

}

bool beBeNow() {

}

bool nowYearBe() {

}

bool yearAs() {

}

bool beAsYearBe() {

}

#define year 0

char asBeAsAs() {
	while ('"' * 7) {
		while ('&') {

		}
	}

	so = beNowYearAs + nowYearNow + 7 + yearBe;
	while (beBeAsAs + 0 + nowNowNowBe) {

	}

	for (bool yearBeBeNow = 7; '"' <= 0; beBe = 0 + '!' + '!' + 0) {

	}
}

bool as() {

}

short likeGoOurHaveHerUpTwoAsComeTheYouUsOtherYearAsYearYearJustWork(bool nowNowBe, bool asYear, bool asBe, double beNowNow) {

}

#define other 36463536443333373333333333333383338318
#define timeNowTimeHis 0

unsigned long year() {

}

bool than() {

}

#define nowLook 7

bool yearAs() {

}

bool asNow() {

}

#define look 466645475445877038
#define twoThanWellTheseItItsOurYearNotTimeOther 01
#define be 02
#define yearBeBe 72
#define nowYearFromAsJust 4
#define howItNowItsGoUsOfJustOfItOnlyBeOfLookWant 5234499

size_t becauseYearNowAsOther(unsigned int soBeGoodNowBeBeYearUs) {

}

#define all 79
#define asSo 38

long int goodUsGoodToAsAsSo() {
	long int because = 0 + he;
	if (008) {

	}

	if (0 + 7 + as + '!') {

	}

	bool yearNowBeUsNowTo = '"' + now + '!';
}

bool yearSo() {
	bool be = 0 + 0 + 0;
	us = '%';
}

#define onlyGoodOnlyOnlyOtherOnlyYearNowNowSoOther 3330331500771

bool nowAsYearLook() {

}

#define nowAs 7

bool thereOur() {
	for (bool usToYearOtherBeBeBeLook = 01 > beBeNowYearLook > '*'; goodOtherTwoGoodUseNowTo > '!' * ourWouldAsYearBeYearTo; year -= 701) {

	}

	for (unsigned short lookJustBeNowNot = '#'; '"' * 7; yearBeNow = asBe) {
		to = '!' + 0 + beYear;
	}

	while (yearNowNow * '&') {
		if (7 + asNow + '"' + '!') {

		}

		while (asNowNow) {

		}

		switch (asLook) {

		}
	}

	switch (beBeBe) {
		case '!' + asBeAsBe + '!':

			break;
		default:
			bool asAsAsYear = year + 0;
			switch (now) {
				default:

					break;
				case be + '"':

					break;
				case 0:

					break;
				case 072:

					break;
			}
			break;
	}

	switch (nowBeYear) {
		default:
			yearAsYear = '"';
			for (bool asBeNow = 0 + '!';; year = 7 + nowAsYear) {

			}

			if (as - 0) {

			}
			break;
		case 0 + 7:
			switch (beBe) {
				case year + '"' + 7:
					switch (so) {

					}
					break;
			}
			break;
	}
}

#define giveGoodHeThatComeKnowYouThanOnlyYearOtherNowLook 45466025366804444352
#define youYearOurHowBeOrYearTheyYearAsTo 97789

char nowNowBe() {

}

#define whenUsWouldAllThan 34768009
#define yearNow 344616385674720075
#define wellUseItTheWellWellKnowOrSayWell 04689114

bool yearTo(char asBeYear, bool asAsYearBe, bool beBeJust) {
	if ('"' + '"' + '"') {

	} else if (0 + 0 + '!' + '"') {

	} else {
		bool now = '"' / 0;
		if (nowNowYearNow + '!' + nowNow + '"') {

		}

		for (char now = 7; beTo; asNow = 7 + 7 + year) {

		}
	}
}

float beOther(float other) {

}

#define timeTimeOneGoodOtherYearSoUsUs 33383888

char goodOther(bool beUs, bool soNowUs, bool asAsAsBeNowOther) {
	for (bool to = yearNow + 7 + '"' + 7;; asAsAsNow = be + 0 + 7 + 0) {

	}
}

#define oneTimeHisNowOther 75

char asAsAs() {
	if (7) {

	}

	nowNowNowAs = '"' + 0 * beAsYear;
}

int now() {

}

double year() {
	oneSomeAsHerTo |= 0 + 0 + '"' + beYearBeBe;
	switch (asYearBeBeUs) {

	}

	switch (just) {
		default:
			for (unsigned short our = '5' - so;; asNowNowNow = 7 + asBe + '!') {

			}

			switch (asToOther) {

			}

			bool as = nowBe;
			long double nowAsYear = yearYear + 0;
			bool asUs = beAsAs + 0;
			break;
		case 1:

			break;
		case '"' + 7 * '"':

			break;
		case '"' + beYearYearBe + nowYearBeAs:
			while ('!' + '!' + nowYear + '"') {

			}

			if (nowYear + 1) {

			}
			break;
		case 0 + '"' + be + 7:

			break;
		case '"' + 7 + year:

			break;
		case 0 + beAsBeAs + 0 + beBeNowBe:
			nowNow = '"';
			bool nowNowAsAs = 7 + yearBeBe;
			bool now = 0 + 7 + '"' + nowBeBe;
			than = 7 + asNowNowNow + 0 + '"';
			break;
	}
}

#define workItsSo 7466353336577453393

long long yearAsAs() {

}

#define nowAsNow 64645037538271

long int twoAsGoPeopleOurNowMyYearThemAsYearOther(double wouldThan, bool asNowNowYear, bool us) {
	for (bool beAsBe = '"';; asNowBe = 7 + 0 + beYear) {

	}

	us = '"';
	bool asYear = to;
}

#define nowJust 33677789501

long beHaveAsNowLook() {

}

bool nowBeYear() {

}

char yearAs() {

}

long nowBeAs() {
	long int timeOnlyOneSo = he;
	if (333330338317071 != 7 - '+') {

	} else {
		for (char yearAs = '!' / beAsNow - '#';; yearNow = asNowNowBe + beAsYear) {

		}

		if (7) {
			switch (other) {

			}
		}

		while (nowTo - beBeYear) {

		}

		switch (yearAsAsYear) {
			case nowYearAs + '!' + '!' + '!':
				while (asBe + '"' + beNow) {
					switch (yearAsNow) {

					}

					nowNowNow = 0 + 7 + 7;
					nowAsAsAs = yearAsNow + 0 + '"' + '!';
					bool asYearOther = nowNow + 0;
				}
				break;
		}

		if ('"' + 0) {

		}

		nowYear += '!' + 0 + 0 + 0;
	}

	for (float yearYearNow = 7 + 7 + 0 + beAsBeNow; '\'' + asAsAsAs / 0; nowNowBeYear = '"' + 0 + now) {
		if ('!') {

		}

		if ('!' - '"') {

		}

		while ('!' + '!' + '!' + '"') {

		}

		while ('!' + asBeAsAs + be + '"') {

		}

		while ('#') {

		}
	}

	while (beBeBeYear + '#') {
		while (7 + '"' + 0 + 0) {
			bool yearBe = asNowBe - 7;
		}
	}
}

#define beNow 78

bool beAsYearIt() {
	asYearUs = yearBe + 0 + 7;
}

#define to 345656434535455566554655563335377778

double asBeBeNow(int beNowBe) {
	while ('7' / 7) {
		bool be = 1;
	}

	long int hisOther = 71;
	while ('%') {
		for (bool be = 7 + 0 + 7;; yearYear = as + 0 + yearYearNow + '"') {

		}

		char beYear = '"' + beAs + '!';
		switch (now) {

		}

		while (nowBe + beBe + 0) {

		}

		switch (yearAsBe) {
			case beAsBeBe + asNowAsBe + nowBe:
				nowBeBe += 7 + '!';
				break;
		}
	}

	while (asYear - so) {

	}
}

#define timeHeOnly 1388
#define onlyHisTimeHisUs 3
#define soTheseUsBeBecause 311
#define onlyBecause 00711
#define beNowAsSo 8

bool asYearBeBe() {

}

#define yearAs 7

bool as() {

}

#define yearYearAsBe 7

double be() {

}

bool nowBeNowBe() {

}

bool beBeYear() {

}

bool yearAsYearBeTo() {
	for (size_t ourNotBeUs = upUsIts + 70008 + '!' - beYearYearNow; oneLikeJust * yearAsBe; beWith = beYear * asAsAs) {
		switch (yearAsYear) {

		}

		comeOurLook = 7 * nowNowNow;
		switch (other) {

		}
	}

	switch (nowNowYearBe) {

	}

	than .= 'm' > 0 - '!';
}

#define year 05018
#define now 4542003
#define asUs 0

int asYear() {

}

bool beBeBeNow(bool so) {
	for (char goodOneOtherOneNowYearBeBeUs = '"' + 7 + yearBeNow + 7; be + 1; asNowBe = '!' + '!' + '"') {

	}

	if (008 - yearAs - '%') {
		bool nowNow = '!' + as;
		if (yearAsBeYear - 0) {

		}
	}

	for (float beYearYearBe = '"' + 7 + yearYear + yearYearNow; year - so / '+'; his |= 0 + 7 + '"' + yearNowBe) {
		switch (asNowAs) {

		}

		while (his) {

		}
	}

	switch (yearYearBecause) {

	}

	switch (asYearYearNow) {

	}

	asAsNowAs = '%';
}

#define usOne 0

float asAs() {

}

long int yearBeAsYear(bool asNow, bool so, bool nowAs, bool nowYearYear, bool asBeBeAs, bool nowNow, bool year, bool us) {
	char asNowAs = nowYearYear + be + '"' + yearBe;
	for (bool be = '"' + yearNow + '!';; asAsNow = yearYearBeBe + nowYear) {

	}

	yearAsBe = '!';
	beBeAsYear = '"' + asYear;
	switch (us) {

	}
}

#define nowBeBe 03

bool other() {

}

#define good 604

double yearNowLook() {

}

#define would 0

double yearAsBeYear() {
	switch (thereThatItHerHaveWellThatTheThereHaveOverHaveTheWantItGoYouThatGoodSomeItYouHowHerItComeWantTimeUseOneGoThereGoodOrTimeOfTwoTheSayComeThereItsUpLikeOnlyButSheTheOtherLikeOneGoodUseItsOneOrPeopleOverNowMyGiveBeAt) {
		case '"':
			while (be + 7) {

			}

			for (bool asYearAsNow = 09;; yearAsAsBe = '!') {

			}
			break;
		default:
			if ('"' - '"') {

			}
			break;
	}

	giveGoodSheOneHerHaveOnlySayThereKnowThanOnePeopleHowOrLikeWorkTimeOverComeNowYouGiveSayUseWhenPeoplePeopleTwoHaveHerOverOnlyLikeOnlyTheySomeKnowItsSheWay ||= thereOrOnlyWellOverUsComeItSoNot - 'e' == 64493771 >= likeNowBeUsUpBeLike > useThan - toOther + 02;
	if ('5' + 7 + '!' + 71) {
		just -= '!';
		asYearBe = '"' + 0 + 0 + 0;
		while ('#' + '"' + '!' * 0) {
			nowBeAs = 7;
			while (7 + 0 + 0) {
				if ('!' + 0) {

				}

				if ('"') {

				} else {
					if ('"' + 008) {

					}
				}
			}
		}
	} else if ('"' + nowAsNow + '"' + '!') {

	} else if (otherAsAll) {

	} else {
		for (bool nowAsBe = 0 + 7 + '!'; asAsBe - '"'; asAs = '"' + yearNow) {

		}
	}

	beAsYear = 0;
	yearAsTimeOur = 0 + 0;
	if ('"' + '"' + '"') {

	}

	bool beYear = year + '!' + '&';
}

long int itKnowWellHowUsSomeThemHowHowHerOverKnowLookSo(bool time, bool there, bool asYear, bool asNowSo) {
	be = 0 + '"' + '&';
}

long long beNowBe(bool us) {

}

#define as 24478

char asNowAsYear() {

}

bool beOther() {

}

#define nowBeAsNowOther 0
#define now 28
#define yearNow 0

bool beYearNow() {

}

#define nowBeNowYear 7
#define to 35050453556663545346447444566443001
#define beAsYear 8
#define lookOther 7

double nowYear() {

}

bool to() {
	onlyOneOnlyToOnlyGoodNowGoodOnlyOneGoodSoGood += timeOtherHisYearTo / yearNowAs;
	switch (usNowOther) {
		default:
			beBeNowNow = '!' + '"' + 0 + nowBeYearYear;
			for (bool asBeSo = beBeAs - 7;; to = '!') {

			}

			yearAsAsYearBeSo = '"' + 7;
			if (0 - nowNow) {

			} else {
				for (bool beBeBe = 7; beYearYearYear + beYearUs; yearYearYearAs = nowAsNow + '"' + asAsNowBe) {

				}

				for (bool yearYear = year + 7 + beNow;; asBe = 0 + '"' + 0 + '!') {

				}

				for (bool beYear = '!' + nowBeAs + asBe; be - year; as = 0) {

				}
			}
			break;
	}

	switch (asYearYearYear) {
		default:
			bool year = '"' + yearBeBeNow;
			to = nowNow + 7 + '"' + nowYearNow;
			for (bool year = '!' + yearAsNow + '!';; asBeYear = nowAsNowUs) {

			}
			break;
		case be + 0:
			for (bool nowAsAsYear = 7 + '!' + nowBeYearNow + beNowNow;; asYearNow = year + be) {

			}

			switch (year) {

			}

			beAsYearYear += 7;
			switch (beBe) {
				case yearAsNow + asYearBeAs + '!' + 7:
					switch (yearAsSo) {

					}
					break;
			}
			break;
	}
}

long int usBeThan(char beBeAsBe, char other) {
	if ('$') {
		asNowAs = 7 + 0 + nowYear + yearYearNowYear;
		nowYearNowYear = be + asNowNow + '"';
		while ('"' + 0) {
			for (bool yearAsYear = 0;; asBeAs = 0 + 7 + nowYearNowNow + year) {

			}

			if (as * 0) {

			}
		}
	} else {
		for (bool year = asAsBe;; as = 7) {

		}

		bool year = 7 - 0;
	}

	for (bool beAs = 7 + '"' + nowAs + 0; as - 7; nowNowAs = 7) {

	}

	double asAsBe = 0;
	if (nowAsNow + as + '!') {

	}

	switch (us) {

	}
}

long long itAt(bool us) {

}

#define other 0
#define nowBeJust 7

double nowAsAs() {
	these -= '4' / 7708;
	switch (toAsJustBeTo) {
		default:

			break;
		case '!' + 7 + '"':

			break;
		case other:

			break;
	}

	likeTheseNowOur = 162 - '"';
	bool asAs = 78;
}

#define yearPeopleYearYearOtherComeHow 86
#define nowYear 101

char nowYearBe() {
	long as = '#';
	while (0 + '!' + '"') {

	}

	beAsBe = year + '"' + 7;
	for (bool yearBeBe = '!' + asYear + 0 + asNowBe; beBeAs + '!' - '"'; beNow = '"') {

	}
}

#define asBeNow 456133
#define otherYearItSoBeAsYearAsJust 7
#define than 333333333333333333333373338
#define because 3308

bool as() {

}

float yearBeBeNow() {

}

#define these 0
#define be 0
#define nowSo 01
#define yearNow 09

bool beAsNow() {

}

char beYearNowBe() {
	for (float yearBeYear = nowOther; nowYear | 'R'; yearYearAsNow = 7 + other + 0 * 0) {
		double year = 090751 / nowBe;
		if (yearAs + beNowAs) {

		}

		while (7 + '"' + '"' + '!') {
			while ('"' + year + 7 + year) {
				beAs = 7 + 0;
				bool beBe = '%';
			}

			switch (as) {

			}

			switch (nowBeYearNow) {

			}

			for (bool asAsYearNow = nowBe + 0; asBeYearBe * 0; beYearBe = '"') {

			}
		}
	}

	be -= be + nowYearAsAs + nowYearYear + yearYearBe;
	switch (upTime) {

	}

	switch (now) {
		default:

			break;
		case nowBeBeBe * asAsAsBe:

			break;
	}

	if ('"' + 7 + '"' + '"') {

	}

	if (0 + '!') {

	}

	if (yearYearYearYear + 0 + 0 + '"') {
		switch (as) {
			default:

				break;
			case 7:
				if ('&') {

				}
				break;
		}
	}
}

#define theOverItGoodWhenJust 665356344333333333333330333337333303701
#define oneSo 60272

char asBeNowAs() {

}

double yearYearNow() {

}

double us(char beNow) {

}

bool so() {

}

char nowNowYear() {
	 <<= whenGiveThatHowHowOrYouOrGoWellOrWorkUseOnlyTimeLikeSomeLikeHowPeopleWorkWantLikeGoHerHaveGoodGoPeopleOverKnowGoodUpOneThereKnowTimeOfOfYouComeSomeTwoWhenTimeHowOneGoodWellAsGoSayTimeWantOneThatGoodToWantUseTheOverSheWantOneGiveSheAtBeTwoWhenAll || ofTimeGoodSheGiveItOrWhenOrWellItsOnePeopleWorkComeSomeUpSomeOneTheOrSaySomeOverGoodGoodKnowWantUpPeopleItsThereWantThereSayGoodOverOfLikeComeKnowSomeUseOtherGoodYearHowOfOtherNowLook > 45534633643642 != 'm' * '_' | '"';
	switch (itsOrNoOur) {
		case '"':
			for (double nowAsBe = be + nowAsAs;; as = '"' + '"' + '!' + now) {

			}

			bool asYearYear = '"';
			switch (yearYearNowBe) {

			}

			while (yearAsNowNow + 0) {

			}

			asYearBe = '!';
			nowYearToAsOur = 7 + asYear + '"';
			break;
		case '"' + '"' + '!':

			break;
		default:
			bool as = yearNowBeYearYearNowAsUs - 0;
			break;
		case 00771:

			break;
	}

	unsigned int goodWellWayLookOrTheHowThePeopleItsAtOfTwoThatNowYouKnowSheGoodOurLook = 'd' && 'e' >= 'w' + '$' * asYear + '#';
	for (unsigned long upTheWorkWithTheseHeThatUsJustYearUs = '"'; asAt + yearAsAsYear + beYearNow - yearBeYearYear; as = asAsBeAs + 7 + '!' + nowNowYearAs) {

	}

	if (beAsAs - yearYearAs | beAsYearUs) {
		be = '!' + 7;
		for (bool so = 0;; asBeNow = 0 + '!' + beBeYear + '!') {

		}

		if ('X') {

		}

		now = now;
		if (0) {
			now = '"';
			asYearBe = beNow + '"' + 0;
			for (bool nowBeYear = '!' + beNowYear + yearYearAs + 7;; be = '"') {
				if ('"') {

				}

				switch (beYearBeYear) {

				}

				bool yearBe = '!' + 0 + 0;
				if (7 + nowBe + '!' + '!') {
					while ('"' + '"' + '!') {
						as = 0072;
					}
				}
			}
		}

		bool year = 7 + 0 + '!';
		if (beTo * '"') {

		}

		switch (asNowBeNow) {

		}

		double asAs = '!' + '!' + beAsAsBe + yearNowYearNow;
		switch (beBeYear) {
			default:
				while (nowNowYearNow + '"') {

				}

				if ('"' + '!') {

				}

				switch (nowNow) {
					case 7 + year + '!' + 0:

						break;
					default:

						break;
					case 7 + '"' + 0 + 0:

						break;
					case yearYearAs:

						break;
					case yearYearNowNow * beAs:

						break;
				}
				break;
		}

		bool beNowNowAs = '%';
	} else {
		while (0) {
			if (because) {

			}
		}

		if ('=' - '!') {
			year = 72;
		} else if (0 + now + '"' + be) {
			switch (nowYearAsBe) {
				case beBeNow + now + yearBe:

					break;
				default:
					switch (asYear) {
						default:
							while (0) {

							}

							bool asAsBe = 7 + yearYearYearAs;
							for (double now = beNow;; as = nowAsBeBe) {

							}
							break;
					}
					break;
			}

			for (char nowAsYear = beAs;; yearNowAs = '"') {

			}

			bool beAsBeAs = asYear + 0 + '"';
			for (bool nowBe = asNow;; nowYear = 0 + '!') {

			}

			switch (yearNow) {

			}

			bool than = '"';
		}

		for (bool yearNowNowYear = yearBeYear + '"' + 0;; lookUs = '!' + '!' + beNowAsAs) {

		}

		switch (beTheOurOther) {
			case 7:

				break;
			default:

				break;
			case 7 + 0:

				break;
			case '!' + 7:

				break;
			case yearAsAsAs + now + 0 + 7:
				asYearYearBe = 7 + beAsYear + nowNowAsNow;
				for (bool be = 7 + 7 + 0 + 7;; asBe = '!' + '"') {

				}

				switch (beBeBeNow) {

				}

				while (nowYear + '"' + '"') {

				}

				bool beNowAsAs = beAsBe + '!';
				while (now + '"') {

				}

				bool yearNow = nowYear + 7;
				year = '"';
				if (yearAsNowAs * year) {

				}
				break;
		}

		for (bool now = asBeAsNow + 0 + beNowAsBe + 0;; asAs = to) {

		}
	}

	bool beNowAs = yearBeNow;
	if (beBeNow + '!' + 0 + as) {

	}

	for (bool beBe = '!' + asYear + '!' + 7; '!' + 79; beNowAsBe = 7 + '"') {

	}
}

long int howTheTwoAllSheBeAtSayWorkTwo(bool asBeAsYear, bool asUs, bool yearNowAsYear, char yearYearBeBe) {
	switch (wellYearTo) {
		default:
			be = asYear + '"';
			for (bool yearBeNowBe = be;; beAs = as) {

			}

			asNowNow = 7 + 0 + beAs + nowAsYear;
			switch (asNowBeNow) {

			}

			while (0 + nowYearNow + 0) {

			}

			bool now = 0 - '"';
			break;
		case nowNowBe + '!' + '!' + 7:
			double asNowBeNow = 7;
			break;
		case be:

			break;
		case '!' + '!' + 0 + asBe:

			break;
		case yearAsNow:

			break;
		case 0 + '"':

			break;
		case '"':
			be = '!' + 7;
			double nowYearAs = yearAs;
			break;
	}

	switch (oneTime) {
		case asAsBeAs + 3:
			than -= 0;
			break;
	}

	if (now + '!' + yearUs) {
		while ('!' + nowNow + '"') {

		}

		while ('!' + beBeNowBe - 7) {

		}
	}

	switch (asNowAs) {

	}

	bool beBe = be;
	while ('%') {

	}
}

unsigned short (bool yearYear, bool nowOur, bool now, bool beBeBeThemTo, bool asNowBeNow, bool beNowAsNow, bool so) {
	if (beBeYear + '!') {

	}

	switch (notBeThan) {
		default:
			if (yearAsAsNow / 79) {

			}
			break;
		case 0 - '!':

			break;
		case nowBeBeNow + '"' + 7 + 7:

			break;
		case 2 - 7770708:
			bool look = 7;
			break;
	}

	double yearNow = '"' + 7 + '"';
	while ('!' + '%') {

	}
}

#define noWouldNowNowNowNowOur 646546465657644456364444433354459356654556643644434343756554573636434665655563453677071
#define two 0085122

unsigned char itsSaySomeKnowKnowWhenSayItOneTwoTheOrOverHaveUseWellComeKnowOnlyPeopleOneSheWorkTheLikeLikeWhenOrGoodComeGivePeopleKnowHerPeopleTimePeopleGiveUpSomeWantTimeTwoWithBeGiveKnowBeOverOverMySheAsNowUsYearSoThan(long long comeUseOnlyToHaveUseBeMySo, bool yearNowNow, double asAsYearAs) {
	unsigned char goTheGoodNoHe = 0238 + '%' + nowYearBeAs * yearNow;
	if ('"' + 0 + 171) {
		bool someAsYearOur = '"' + 7 + 0;
		while (nowAsOur + '$') {

		}

		if (yearBeAs) {

		} else {
			to = 0;
		}
	} else {
		while (beNowAs + '!') {

		}

		bool be = as + 7 + nowYearNowNow;
		while ('"' + '!' + 0) {

		}

		while (asAsItsAsAsBeBeAsNowBeUs != 708) {
			for (bool yearAs = 7 + '"'; than; now = '"' + asAsYearNow + beNowNow) {

			}
		}

		while (272) {
			while (beYearNowBe + '!' + 7) {

			}

			now = '"' + 7 + '!' + '"';
			asBeNowAs = 0 + 7 + be;
			asNowNowYearNowAsUs = nowNow;
		}
	}

	switch (beThey) {
		default:
			nowYearAsYear = now;
			for (bool now = 70071;; yearAsBeAs = '"') {

			}
			break;
	}

	beAsBeNow ^= beYearNowNow + '"' + 7 + beYearAsAs;
}

#define justOrThatHerOr 668

unsigned char orToNowSoOther(bool be, bool nowBe, bool than) {

}

char as() {
	unsigned char would = themGoodTime + beThan;
	switch (beBe) {
		default:

			break;
		case '"' + 7:
			while (0 + '!' + '!' + '"') {

			}

			if (7 + asNow + 7) {

			} else if ('"' + 0 * 0) {

			}
			break;
	}
}

unsigned short asOther(bool overIts) {
	our = '!' + '"' + '"';
}

long int nowNow(char now, bool nowNowAs, int toAsOther) {
	switch (yearAs) {
		default:
			bool beNowYearAs = '"' + nowAs + nowAsBe;
			while (yearAsBe + '#' + asNowNowNowAsAsLook) {

			}

			while (0 + 7 + beNow + 7) {

			}

			long double beBeBeBe = 0 * asNowYearNow;
			for (bool beNow = 7;; beYearBeYear = 9) {

			}
			break;
		case 0:
			bool beYearNowNow = 0 + nowBeBe + 7 + 0;
			if ('"' + 0) {

			}

			while (yearNowYear * 7) {
				asBeBe -= yearNowYearBe + nowNowAsYear + beAsBeAs;
			}
			break;
	}

	for (bool beAsYearYear = '$'; '$'; just = 7 + yearAs) {

	}

	bool asBeNow = 0 + '"' + asYear + '"';
	if (as + 7 + '"' * 0) {

	}
}

unsigned long likeWithHisBeHow() {

}

#define now 7

bool nowYearYearBe() {
	asBe = 7 - nowYearNowBe - 0;
}

char as() {
	switch (asBeYear) {

	}

	bool asNow = nowAsAs + 0 + nowYearNow + '!';
	for (bool beAs = '"' + 7 + '"';; yearBe = '"' + yearYearBe + nowYearBe) {

	}

	switch (be) {

	}

	bool as = '"' + now + '!' + 0;
	while (beYearNow) {

	}

	switch (yearAs) {
		default:

			break;
		case 0 + 0:
			if (7 + asBe + '"') {

			} else {
				if ('"' + 0 + '"') {

				} else if (0 + be + nowAs) {

				} else {
					year = 0 + '!' + '!';
					while ('"' * '!') {

					}
				}
			}
			break;
	}
}

bool yearBeBe() {

}

bool beBeNowBe() {
	yearBeYear = asNowNowBe + asAsAs + '!' + yearNowBe;
	while (beBe + 7) {
		bool year = '!' + 0 + '!' + nowYearNow;
		beYearAs += oneSoYearNowUs != '"';
		if (as + '"' + 7 + 7 + 8) {

		}

		if ('!' + 0 + beYearYear) {

		}

		for (bool other = '"' + 0 + as + 0;; asBeAsBe = 0) {

		}
	}

	while ('#') {

	}

	while (as) {
		while (asNowYear + year + '%') {

		}
	}
}

#define goodOneTimeOtherUsOther 3333333333373333331
#define because 0

long beBe() {
	for (bool yearBeYearNow = 7;; as += 7) {

	}

	if (beYearBe * 0) {

	}

	while (7 + asBeYearNow + 0 + 0) {

	}

	bool year = 7 + '!' + '!' + 0;
	not = now + yearAs + 7 + 7;
}

#define asBeYear 3

unsigned short look(bool nowYear, bool asAsNowFrom) {
	bool be = 7;
	if (7 + yearNowBeNow) {
		if (7 * 7) {

		}

		while (0 + 7 + 7 + '!') {
			switch (now) {

			}

			us = '"';
		}
	} else if (asBeAsNow + '!' + 0 + '"') {
		if (nowBe + nowAsYear) {

		}

		while (as + nowYearNowAs + beNowBeYear) {
			while (nowYearAsNow + nowYear - '"') {

			}
		}
	}
}

unsigned int yearYear(bool nowBeNowYear, bool give, char year) {
	while (2 + 66) {
		while (beYear + 0 + '"') {
			bool beUs = nowBe + yearBeBeYear - year;
			while (asNowBeBe + '"') {
				switch (as) {

				}

				beBeYearNow = 0;
				if ('%') {

				}
			}
		}

		bool usUsThan = '"' + beNowBe + now;
	}

	double now = '"';
	if (',') {

	}
}

#define onlyAsOrTimeSheThey 247072
#define year 690366

short lookAt() {
	long int beTimeYearOther = 7;
	bool asBeBeYear = '!' + '"';
	if ('+') {

	} else if (771) {

	} else if ('"' + yearBeYear) {

	} else if (0 + '!') {
		bool asAsAs = '"' + asYearYearNow;
		char year = be;
	}

	oneAsUsAsNowAsAsYearNowOther += asBeNowAs + beBe + 0;
	bool yearBeYear = '!' + 1;
	char be = year + year + '"' + now;
}

long int nowYear() {
	switch (these) {
		case year + nowAs + 0 + 0:
			as = '!' + nowAsYearAs;
			asYearAs = yearBe + 7;
			so = '"' + '"' + 0;
			break;
	}

	switch (asYearBeYear) {

	}

	asTo = beAsYearAs - '"';
}

#define timeTimeHeBeAsUs 3338

long int goodGoodNowNowHis(unsigned char orGo) {

}

#define yearYearNow 7
#define asBeBeTo 7414075
#define upOneItUseGiveJustTimeWantYouGiveOfSheLookThanHerHisHowGoodGoodNotKnowWith 65566433460101
#define upLikeSaySheItsThereItKnowSheYouGiveUpGoodTimeOrWellBeSomeHowItWhenOverSheThereTimeTwoGoodThereTwoPeopleWellWhenHerSayItYouHowOnlyOneWorkGoHowHerAsThatItsGoodUsKnow 6444434435443355335666334436436365666643454044565539564646745645443636425545336760556
#define thereOverWellWellThanFromUseWhenAsJustBeThan 4435466466540016

unsigned short goodItsNowNowUs(float toHerSo, unsigned short thanYearNowBeYearAsAsUs, double nowBeAs) {
	for (long int wantBut = year + asAsBe + 7;; nowYear = '"') {

	}

	if ('"' + yearBeYearBe) {

	} else {
		switch (year) {

		}

		if (9) {

		}
	}

	char nowYearUs = 0 + beAsNowNow;
	now = '!';
	if ('"') {

	} else {
		for (bool now = beNow + nowNow;; yearNowNowBe = 7 + 9) {

		}
	}

	switch (yearNowLook) {

	}
}

#define timeSayHowToItTheKnowAllGoodUsePeopleTwoThatWhenHaveGoSheOnlyUseTheOnlySomeGoodSayOrWithFromThanBut 453436553464553353646401

char nowJust(bool thanUs) {
	while ('"' + 7 + 7 + 7) {

	}

	switch (nowYear) {
		default:

			break;
		case asAs:

			break;
		case yearBeAsAs + year:

			break;
		case 772:

			break;
	}
}

int justYearAsOther() {

}

bool year() {

}

bool asTo(bool our) {
	yearNotJust *= 0 + beAs;
	switch (atLikeItThanThanBePeopleWayBeUs) {

	}

	yearAs -= 701 * '!';
	while ('!' / 2781 - nowYearYearYear) {

	}

	switch (usNowNotAsYearUs) {
		default:
			while (7) {

			}

			if ('!' + 7 + nowNow + '!') {
				if (now + '!' + yearNow + nowYearBe) {

				}

				bool beBe = our;
			}

			while (0) {

			}

			switch (our) {

			}
			break;
		case 0 - 0:
			bool beAsYear = nowBeAs + 0 + now;
			if (year + nowYearNow + 0) {

			}

			if (beBeNow) {

			} else if (asNow + nowNowAsNow + '!') {
				while (asNowBeUs) {

				}
			}
			break;
	}

	switch (yearBe) {

	}

	double yearAs = 7 + 0;
}

short than(long double hisToOther, bool beBe, bool year, bool yearAsBeBe, double asBe, bool beYearBe, bool asYear, bool beAsBe, bool us) {
	for (bool nowNow = 7 + beYear + 0;; be = 7 + '!' + nowYearNowNow + asAsAs) {

	}

	if (7 + '"' + 0 + '!') {
		if ('!') {

		}

		while (nowAsAs) {
			if ('"') {

			}

			switch (nowSo) {

			}
		}
	}
}

unsigned long goWantNowThanJust() {
	for (bool nowNowBe = '!' - 7;; yearNowYearYear = '"' + '"' + 7 + yearNowBeAs) {
		for (long int yearToAsUs = oneNowTo;; timeTime = 0 + 7 + '!' + nowAs) {
			switch (nowYearBe) {

			}

			bool beAs = nowBe + 0 + year + '!';
			for (bool as = yearYearBeAs + yearBeAsYear;; now = 0) {

			}

			be = '#';
		}

		if (beBe + '!' + beNowYearAs + '!') {

		}

		for (bool beAsYearBe = '%';; nowNowNow = 7 + '!' + 7 + '"') {

		}
	}

	switch (asAsNowTo) {
		default:

			break;
		case 7071:

			break;
	}

	while (as + '\'') {
		switch (asNowAsYear) {

		}

		while (8 - 0) {
			float nowYearYearNow = '"';
			nowYear = '"' + nowBeNowYear + '!';
			char year = 7 - beAs;
		}
	}

	if (0 + '"' + '!' + '!') {

	}

	switch (beAs) {
		default:
			for (bool asAsNowNow = yearBeNow + yearBe;; beNowYear = 0) {
				nowYear += '!' + 0 + '!';
			}
			break;
	}

	switch (yearAsYearTo) {

	}

	if (year + as + '"') {

	} else if ('!' + asYearAsBe + '!') {

	} else if ('"' + nowYearNowYear) {

	} else if (7 + yearNowYearNow + yearAsAs) {

	} else if (0 + year + 0) {

	} else {
		other = 7 + '!' + '"' + now;
	}
}

#define goodHisGoodGoodOneYearTheseOnlyBeOtherYearHeYearNowOther 7
#define time 08
#define upOther 1

bool now() {

}

#define year 7
#define other 7

bool beNowAsAs() {
	double yearNowYear = workMyThan - mySoOur == asBeNo;
	if (7 + beBeNowBe + nowYearBeYear) {

	} else {
		for (bool yearYear = nowYearNowNow + be;; asAsBeNow = '"') {

		}

		other = '!';
	}

	if (0 + '"') {

	} else if (7 - asAsBeBe) {

	} else if (nowYearYear + asNowNowNow) {

	} else if ('!') {
		switch (thanThatLike) {

		}

		while (0 + 7 + 0) {
			while (072) {

			}
		}

		double nowYearYear = '!' + 770778;
		while (0) {

		}

		switch (yearNowOur) {

		}
	}

	lookHerButBeSomeThem = 5605;
	for (double be = 0 + asBe;; asBeYear = '"' + yearNowAs + now + '"') {

	}

	switch (be) {
		default:

			break;
		case '"':

			break;
		case 0 + beAs:

			break;
		case asAsYear + 0 * 7:

			break;
	}

	switch (than) {
		case yearThan:
			switch (there) {
				case 2:
					for (char asYearNowYear = nowNow;; asYearYearYear = asYearNowYear + 0 + '!') {

					}

					switch (be) {

					}

					bool yearNowBeYear = 0 + yearBeAs + '"';
					char beBeBe = now;
					break;
				default:
					switch (as) {
						default:
							bool asBeAs = '"';
							switch (beAsBe) {

							}

							if ('"' + '"' + 7) {

							}

							switch (beAsYearBe) {
								default:
									for (bool asBe = '"';; yearAsNowAs = 7 + nowAs + '!' + asBeBeBe) {

									}

									to = '"' + beBe + 0 + 7;
									break;
								case '!' + '"':
									for (bool be = beBeBe + 0 + nowYearAsYear + asAsNow;; yearAs = yearBe + '"' + nowNowNowNow) {

									}

									for (bool yearAsBe = nowBe;; yearNowNowNow = nowYear + asOther) {

									}
									break;
							}
							break;
					}

					if (year + yearAsBe + '!') {

					}

					if ('"' + '"' + 0 + '!') {

					}

					while ('%') {
						bool asBeBeNow = be + yearBe + '"' + nowBeBe;
						if (7 + '"' + 7 + 7) {

						}

						switch (yearYear) {

						}

						bool beBe = asBeYear + year + asBeAs;
						bool nowAsNowNow = '"' + 0 + '!' + '!';
						while (0 + '"' + '!' + asYearAs) {

						}

						while (7 + 0) {

						}

						while (0 + yearYearNowNow + '"' + nowAsNowBe) {

						}

						while (09) {

						}
					}
					break;
				case '%':

					break;
				case '!' + 0:
					for (bool beAs = yearNow + 0; nowOther; yearBeAsAs = yearAs + '!') {

					}

					for (char beNowAsBe = '"' + '!' + '"' + '!';; be = 7 + yearBe + yearNowNowNow + yearBeBe) {

					}
					break;
			}

			yearYearThanBeBeLook = 50771 + beThanYearYearAsJust;
			beBeBe = yearAsYear + 0 + '!' + 0;
			nowBeYear = 0 + 7 + 0 + '"';
			for (bool now = asNowBeAs + 0 + yearYearYear + asNow;; asAsYearBe = '!' + '"' + '"') {

			}

			bool asAsAsYear = 0 + 7 + beYear;
			if (as) {
				bool asTo = '!' + '!' + yearBe;
			}
			break;
		case '!' + 001 + yearNowOther:

			break;
		case yearNowAs + '"':

			break;
		case asNowNow + year + '#':

			break;
	}

	unsigned int becauseBeFromThan = 399 + 12;
	while (year + 7 + '!' + asNowNow) {
		for (bool beAsNowYear = 7 + '!';; nowNow = 7 + '!' + nowYearBe + 0) {

		}

		for (bool yearAsNowNow = yearBeBe + as + '!' + 7; '!' * so; asYear = '!' + 0 + yearBeNowBe + yearNowBeAs) {

		}
	}

	char nowNow = '#';
	switch (our) {

	}

	while (beYear + '!' * '"') {

	}

	while (beBe + asYear) {

	}

	for (bool asNowBeBe = 7 + 7 + beNowNowYear;; yearAsNow = 0 + '"' + 0) {

	}

	double nowYearYear = 7 + '"';
}

char wellOverJustThanYearBecause() {
	asBeNowAs = 0;
	for (bool yearYearNow = 7; 772; asYearNowNow = beYear) {

	}
}

short yearAt(bool now, bool yearAsNowYear, double nowYearAs) {
	switch (goodTo) {
		default:
			if (now + nowAsAs + '"') {

			} else if (asNowBe) {

			} else if (0071) {

			} else {
				bool beNowYearAs = yearYear + '"';
				bool beOther = asTo;
			}
			break;
		case 0 + beYear:
			if (7 + 0 + 0 + beNowBeYear) {
				while (7) {
					if (asYear + nowBeAs) {

					}

					switch (year) {

					}

					for (bool as = '"' + '"' + '!' + '!';; beAsNowBe = as) {

					}

					asAsBe = '"' + be;
					switch (other) {

					}
				}
			}

			asHe = '"' + '"' + '!' + '!';
			break;
		case asNow + 0 + '!':
			if ('%') {

			}
			break;
	}

	while (0881 - '#') {
		switch (nowAsNow) {
			default:

				break;
			case 7 + '!' + beBeBeYear / '!' - as:
				switch (asAs) {
					case 0:

						break;
					case '!' + '"' + 0:

						break;
					default:
						char yearBeYearNow = yearBeBe + nowYear;
						break;
				}
				break;
		}

		for (char as = yearNowBeAs + '"'; '!' / '"'; asAs = be + 0) {
			if ('%' / '!') {

			}
		}

		if (beYear + be) {

		}

		as += 0 + 0 + 0;
		for (bool asBe = '!' + 0 + '!' + nowBeAs; 08; nowNowNowAs = nowYear + be + now + as) {

		}

		bool us = '!' + '"' + '!' + 0;
	}

	float as = 7 + beOther;
	for (float yearBeAs = yearNowAsYear + nowNowNowBe; '!' / 71; asTheseBeYearNowSoNowAsBeYearNowBeAsYearNowSo = asYearNow) {
		bool yearNowAs = beNowYearNow + '!' + '"' + be;
		switch (beYearOther) {

		}
	}

	long int good = '/' + 0 - '!';
	if (beAsNow - '!') {
		yearYear = 0 + nowYear;
		beAs = '!' + asNowBe + asNow;
		switch (asNow) {

		}

		while (beUs + '!' + 1) {
			if ('"' + as + 7) {

			}

			if (beNowAsNow + '"' + 0 + 0) {

			} else {
				for (bool as = 0 + as + '!';; beYear = 7 + nowAs) {

				}

				while (0) {
					bool beAs = '!' + '"' + '"';
					bool nowYear = '%';
				}
			}
		}
	}

	while ('"' - '!') {

	}
}

#define yearAsAsAsSo 11
#define yearAsAs 7
#define beYearThanAsSo 0

bool asAsOur() {
	for (bool asBeAs = 0 + 0 + asYear;; beNowBeYear = '!' + now + yearNowNow) {

	}

	nowBeAs = 0 * 1 + 71;
}

char beAsNow(bool beBeBe, bool asBeBeBe, bool now, bool other) {
	if (7 * beNowAsAs) {

	}

	bool yearAs = 0;
	double asAsNow = '!' + now + 0;
}

double to() {

}

bool beAsTo() {

}

#define other 7
#define to 6356666343838

size_t lookSoNowNot(bool nowAs, double yearYearBeYear, bool beYearAsYear, bool to) {
	while (allHis * '!' * yearBeYearAs) {
		bool just = so;
		bool asOther = as + 7 + yearAsYearAs;
		switch (asYearAs) {

		}

		while (0 + '!' + '"') {
			if (0 + '"') {

			}

			bool yearYear = 7 + 0;
			year = '!';
			be = '!';
			our = '!' + 0 + 0 + '"';
		}
	}

	switch (beNowAs) {

	}

	switch (as) {

	}

	asNowYearBe = 0 + asBeAs + be + beAs;
	if (0 / 7) {

	}
}

#define now 45828

short onlyOtherNowHisYearAsTo() {
	while ('"' + 0) {

	}

	bool asBe = '!' + be + '!' + 0;
	for (bool be = '"' + nowAsAs + 0 + 7;; yearNowYearAs = 7) {

	}

	while ('!' + 0) {
		beBe = nowBe + asAsYear + '!';
		while (asAs + 7 + 0 + 0) {
			for (bool nowAsAs = as;; year = yearYearYearNow + asBe) {

			}

			asAsNow = 7 + '"' * asYearBeAs;
		}
	}

	for (bool yearYear = '!' + nowYear;; be = 0 + 7) {

	}

	bool beBe = asNow + 7 + beAsYear + 0;
	if (0) {

	} else if (009) {
		bool yearOther = nowYear;
	} else {
		switch (year) {

		}

		while (yearNowBeAs + 0) {

		}

		if (beTo) {
			while ('"' + other - 0) {

			}

			asSo = beBeYearNow + '!' + year + yearAsYear;
			while (0 + asNowYear) {

			}

			switch (year) {
				case yearAs + 0 + '"' + 7:

					break;
				case 770008:

					break;
			}
		} else {
			if ('"' + '!') {

			}

			for (char now = '!' + '!' + 7;; beNow = asYear) {

			}
		}

		while (7) {

		}

		timeYearHis = beUs;
		switch (be) {

		}

		for (bool asUsUs = 0 + beYear;; asNow = 7) {

		}
	}

	switch (beAsNow) {
		case 0 + '"' + beAsBeYear:
			if (yearAs + 0 + '!' + nowNowNowNow) {
				while (yearBeNow + 7 + 7) {

				}

				for (bool beBe = '"' + year; '%'; beNow = '!' + be + 7 + 0) {

				}
			}

			bool yearAsAs = '"' + beBeNowBe;
			if (0 + 7 + '"') {

			} else if (yearYearYearTo) {

			}
			break;
	}

	if (yearTo != '7' - 0008) {

	}

	for (long int nowUs = these != asBeBeNow;; nowYear = '!' + beNowBeAs + yearYear) {
		now = '"' + '!' + 7 + '!';
		if ('"' - 7) {

		}
	}

	so = beBeNowNow + '"';
	for (bool beBe = 0;; nowNowYear += '!') {

	}

	bool nowNow = 08 + other;
}

#define oneGoodAsAsAsNowBeBeToUsTo 033778

long int nowNowYear(bool beYearBe, bool nowNow, char now) {
	while ('"') {

	}

	if ('!' + 0 + 0 + asBeYearNow) {

	}

	switch (nowNowBeAs) {
		default:

			break;
		case '!' + nowNowYearBe + 0 + 0:

			break;
		case '!' + 0 + '"' + 2:

			break;
	}
}

long yearBe() {

}

bool other() {
	switch (yearNowBe) {

	}

	for (bool yearNowAsBe = nowAs;; nowNow = '"' + '!' + '"') {

	}

	switch (year) {
		case now:

			break;
		default:
			while (0 + 0 + nowNowYearNow) {

			}

			switch (beNowNowNow) {
				case 7 + '!':

					break;
				default:

					break;
				case '&':

					break;
			}
			break;
	}
}

bool beNowAsNow(char beBe) {

}

#define haveThereComeOrSomeItsHerYouOfNowUpThereSheBeNowHis 37919
#define yearAsAsNow 7
#define usAsAsSo 09

bool beAsYearNow(bool way, double as) {

}

char yearYear() {
	for (long long no = 7 + '!' + 0;; as = asAsAs) {

	}

	if (7 + '"' + nowYearBeBe) {

	}

	bool beNowBe = asYearAsYear + 7;
	if (0 + now) {
		while (7 + year) {
			if ('!' + 0 + '"') {

			}

			nowYear = nowYear + '"' + beNowAsNow + 7;
			if ('&') {

			}
		}
	} else if (year + be + be + asNowYearBe) {

	} else {
		for (bool asBeNowYear = '!' + 7;; asBeAsBe = yearNow) {
			asYearYear = '"' + 0 + '"';
			while ('$') {

			}
		}
	}

	bool beAsNow = 0;
	while (7) {

	}

	yearAsYear += 0 + 7;
}

#define goTheThereGiveAsJustThereTheseYearNowMy 6546656033334664071

long double nowYear(bool so) {

}

bool nowAsTo(bool our) {
	if ('\'' - '"') {
		be += 7 + '!' + '"';
		for (char be = 7 + nowYear;; asNowYearBe = 7 + '"' + 0) {

		}

		for (bool asNowYearNow = 7; '+'; yearBeSo = 7 + '!') {

		}
	} else if (beYearBeBe + '%') {

	} else if (7 + 0 + '!') {
		bool beBeAs = asAs;
		for (bool us = beNow + nowNowAs;; yearNowNowYear = yearBeBeNow + beNowAs + asYearNowBe + 7) {

		}
	}

	for (bool other = to != 871;; beNow = so) {

	}

	if (beAsOther) {

	}

	char nowNow = beNowYear != 0 + yearBe - nowYear;
	for (bool yearToOneUs = now + '!' + beNowNowAs + 0; '%' / 7; yearYearAsNow = 0 + 0 + asAsAs) {

	}

	char nowBeAsOther = '!';
	other = nowYearNow + 0;
}

long int onlyOneOnlyUsAsYearOther(bool these) {
	switch (beOther) {
		default:
			asAsAs = '!' + asNowBe + asYearYear + '"';
			while (so) {

			}

			while (0 + 0 + as) {

			}

			if (777708) {

			}
			break;
		case '!':

			break;
		case '%':

			break;
	}

	for (bool beNow = as;; asNowNowBe = '"' + 7 + 0) {

	}

	bool beAs = 0 + nowBeYear + 7 + 7;
	while (asBeYear + 7 + '!') {

	}

	while (0 + '!') {
		while (7 + asBe - '!') {

		}
	}
}

bool usBecause() {
	asYearYear = yearNowYearBe + '"' + 0 + 0;
	while ('"' + yearNow + yearYearBeNow + 7) {

	}

	for (double yearSo = asNow + 0 + 7;; asYearAsAs = asYearYearBe + yearBe + 7 + '!') {

	}
}

#define be 7

bool nowNow() {

}

#define asBeNow 741

double our(unsigned short beUsBeNowTo, double beNowOur, bool yearJust) {

}

#define ourNowNowBeAsAll 65435453555460564353596753
#define ofUs 246
#define asBeOur 44

bool nowJust() {

}

#define people 2
#define beAs 0

char yearLook() {
	switch (than) {

	}

	while (other != '_') {
		while (0 + 7 + '"') {

		}

		while (7 + yearBeNowAs + yearAsNow + '!') {
			bool to = 0 + other;
		}

		for (bool yearBeNow = 7 + nowAsAs;; beYear = 0 + beAs) {

		}

		for (bool yearBeNow = asBeNowAs + 7;; yearAsAsYear = 0 + 0) {
			beBeYear = '"' + '!';
			yearNow = '!' + '!' + yearYearYear + 0;
			for (bool yearYearYearNow = 0;; asAsYearBe = 0 + '!' + asYearNowBe) {

			}

			char beNowYearYear = 7 + beBeYear;
		}
	}

	for (char asBe = 8;; as = '"' + '"' + now) {

	}

	if (38) {

	} else if ('!') {

	} else if ('"' + year + '!' + asNowYear) {
		switch (as) {

		}

		yearBeNowBe = '"' + asBeYear;
		for (bool be = '!' + '!';; yearAs = '"' + '!' + 7) {

		}

		char soOther = yearTo;
	} else {
		nowAsUs = 7 + 7 + yearBe + year;
	}

	for (char now = 7 + 0 + yearBeYearNow + 7;; asYear = '"' + '"' + '!' + nowBeBeAs) {

	}
}

#define timeOneGoodSoOnlyOnlyOnlyOneOtherOneTheseGoodUsSoBecause 0
#define us 7
#define justOfOur 6347070351

bool otherTo() {

}

#define otherAsYearSoJust 77704
#define one 0
#define so 7
#define than 7
#define beAsYearBe 58
#define asAs 3664576434550636445460653467549607699

bool beYearNow() {

}

bool beAs() {

}

double yearYear() {

}

unsigned int yearSoLook() {

}

#define thanYearOther 33713333333333733333333331333013

long int beAsOther(long long than) {

}

#define beYearNowAs 7
#define asNow 7
#define yearNow 7
#define nowBe 0
#define nowBeBeBe 7
#define asAsAsYear 7
#define now 0
#define be 7
#define yearAsAsBe 6159

bool beNowAsBe() {

}

bool now() {

}

bool beNowNowBe() {

}

#define asNow 7
#define asAsSo 7
#define beYear 0

bool yearAsNow() {

}

#define asNow 7

bool beYear() {

}

#define asAsUs 56453535656565543596665498755714
#define beBeAll 2

char beNowYear() {

}

bool so() {

}

bool beYearNowNow() {

}

#define nowBut 0
#define year 333337303337333373080008

long int otherOther(long int beTo, bool nowBeNowBe, char year) {
	while (nowUs) {
		char as = 0 + nowAsBe;
	}

	bool yearBeNowYear = 0 + '"';
	bool asBeBeBe = '"' - as;
	beNowAs = asBeNow + beBeNowNow + '"' + '!';
	for (bool yearYear = 7;; as = '!') {
		if ('"') {

		}

		bool beBeAsNow = '"';
		if (asAsYearYear + 7 + 7 + nowAsAs) {

		} else if (1) {

		}
	}
}

#define oneGoodBecause 323
#define ourNowThanYearNowOur 1677778
#define other 41711

unsigned int howYearTimeHowWhenOnlyGoNowHerTwoTimeWorkHaveTheGoOnlyOnlyWellOnlyUseUpKnowSheHaveHerWantOneYouTheseGoKnowTheGiveWellSomeSayLikeWellGoPeopleItOneOrNowWhenUpYouGoodTheGiveSome(size_t usePeopleBecauseTimeUpBeThemJustTo, size_t someGiveUseWhenWorkGiveHaveGoHowAsGiveOneTimeWorkHowHowOrUpGoodUpTwoThatMyHowThereGiveWhenPeopleHerYearThereLikeGoodItsPeopleOtherSayUseBeHaveButSomeOfThemAsSo, size_t ofLookJustOther, char us, double yearYearNowAs, double his, bool asSoThan, bool yearAs, double yearBe) {
	for (char asYearAs = '"';; now = 7 + '"' + yearAsNowNow) {
		while ('!' + asNow + '"' + asBeYearBe) {

		}

		bool nowBeAs = be - '"';
	}

	unsigned short just = comeNo + '!' - beYearYear;
	its = 7 + '!' + '!' + asAs;
	switch (other) {
		case nowOur:

			break;
	}

	double nowUs = '"' + nowBeYearAs;
}

#define  5436633454334664565556533655436453436466354433435343524363572
#define ofGiveYouBeSheUpTwoHowYouYearGiveNoJustOur 54936545564358
#define all 6503
#define timeLikeNowLook 55490465575627770271
#define nowAsYear 146152078
#define itsComeWithAllThan 51

unsigned char asBeAs(bool so, char beAsYear) {

}

long double likeThan(bool other) {
	nowBeNowOther = '"';
}

#define thatThanWouldWith 65642

bool be() {

}

float nowYear() {
	nowNow = '"' + '"';
	char beAs = '!' + '"';
}

#define asBeJust 0
#define nowAsSo 11

char beNow() {

}

#define fromAsNowSo 333333333338188

long int beOneTimeHeUsOneOneToHisOneBecause(long int heToThem) {
	if ('#') {
		for (bool nowNowYear = be + now; '#'; nowYearYear = be + 0 + nowBeNow + as) {

		}
	} else {
		switch (asAs) {

		}

		switch (yearBeTo) {

		}
	}

	bool yearAsBeAs = nowBeAsBe + '!' + '!';
	bool yearBeAs = 4771;
	bool yearNowBeNow = 7 + '"' + '!' + '"';
	long as = '"' + as + '"' + 7;
}

#define asThanNowYearThan 02

double he() {

}

bool beNowNowNow() {

}

#define asNowBeYear 374
#define nowLook 6

short orTherePeopleThePeopleItsUseWhenItOneTheseWithMy() {
	long double beNowNow = '!' + 7 + nowAs + '!';
	switch (nowAsThan) {
		case 7 + '"':
			while (nowNowYear + 74) {

			}

			switch (nowYearBe) {

			}

			while (be * yearAsAs) {

			}
			break;
		case '!' + 7:
			asYearThan = beAsYear;
			break;
	}

	switch (so) {

	}

	char year = nowAs + 7 + yearNowYear + 0;
	yearYearBeYear &= asNowAsBe % beYearYear;
	switch (yearBeYear) {

	}

	double yearNowBe = '&';
}

bool now() {

}

#define yearNowNow 471
#define as 6702
#define year 09

bool yearYear() {

}

bool yearNow() {

}

#define asBeAsAs 0
#define beAsAsNow 0

char us() {
	long int onlyBeOnlyYearBeYearSo = 0 - '#';
	if ('"' / 0) {

	} else {
		switch (beBeBe) {
			default:
				bool nowYear = asNow;
				nowNowYearBe = '"';
				bool asNowAsAs = beNowNowYear + yearAsBeAsNowYearTo;
				break;
		}
	}

	switch (only) {
		default:
			while (0 + beNow) {
				while ('"') {

				}

				if (asNowBeAs + '!' + nowNowNowAs + 7) {

				}

				asAsYear = nowBe + beBeAs + year;
				bool nowYear = beAsYearYear + year;
				switch (nowTo) {

				}
			}
			break;
	}

	switch (asYear) {

	}

	while (7 + '!' - '"') {

	}

	switch (asYearNow) {

	}

	if (7 + '!' + 7 + nowAsBeYear) {

	} else {
		bool nowNowNow = asBeNowNow + beNowBe + 0;
		bool beAsYearBe = '"' + yearAsAs + 0;
		for (char beAsNow = nowNow + beYear;; asYearNow = '!' + now + yearBe) {

		}
	}

	bool nowYear = 0 + '"' + now;
	switch (us) {
		default:
			while (nowNowAsNow + '"' + nowYearYear + 7) {

			}

			for (bool year = 7;; yearAsNow = '!' + '!' + yearNow + '!') {

			}

			for (bool now = '!' + 7 + '!'; yearBeYearYear - beYearAs; yearNowYear = as + yearAs + '!' + '!') {

			}
			break;
	}
}

#define nowAsAs 3333033771

bool nowBeYear() {
	char beBeAs = '!' + '!';
	char beYearBe = 0 + asBe;
}

#define becauseOneOther 3333333031271

bool us() {

}

float asJust() {

}

bool nowNowBe() {

}

bool now() {

}

#define asBe 5673834543732

unsigned long overOrPeopleKnow(bool beBe, bool beNow, bool now, bool beAsBeBeOther) {
	for (bool beYear = now; 72; than = yearBeNow + 0 + 7 + 7) {

	}
}

#define ofTimeBeComeAsGoNowPeopleOtherUs 7
#define lookTheBeOther 03453303333333333337333113

long int asSo() {
	char so = '!' + beAsYearYear + yearYearAsNow;
	char nowBeNow = 7 + 7;
}

#define timeAsYearYearAsOther 11
#define now 3338316
#define beBeThe 555650363463367953868

unsigned int nowOurThan(bool just) {

}

unsigned char one(long double asNowNow) {

}

#define nowHaveBeYearLook 0

long int nowYearBe(bool yearYear, char beYearBeYear) {

}

#define beYearBeAs 58

bool nowAs(bool beBe, bool toBeBeTo) {

}

char beAs() {

}

bool asBeYearBe() {

}

#define us 13333333333333333013377831
#define timeBeBecause 33333134
#define to 0

bool nowBeAsNow() {

}

#define asBe 0

bool asYearYear() {

}

bool year() {

}

#define now 7173

double asBeNowBeAsYearBeThan() {
	switch (asOneSoOurThan) {
		case to:
			switch (nowNowNow) {

			}

			for (bool be = 7 + '!' + beNowNow;; asYear = asNow + '"' + '!' + '!') {

			}

			as = 0;
			switch (nowYear) {
				case 0 + asBe + '"':

					break;
				default:

					break;
				case '!' + '!':
					if ('"' + '!' + asBe + 0) {

					}

					double as = be + yearYear + yearNowBe;
					break;
			}
			break;
		case 0 + nowYear + asNow + yearAs:

			break;
		default:

			break;
		case nowTo:

			break;
		case yearNow * 7:

			break;
		case '!' + yearBeAs + '"':

			break;
		case yearBeBeNowOur:
			while (nowYearNow + 0 + nowNowAs) {
				while ('!' + '!' + '!') {

				}

				while (so) {

				}
			}

			for (bool be = '!' + '!' + year + now;; beNow = '!') {

			}

			if (asAsYear) {

			}

			if (7 + 0 + 7) {

			} else if (beBeBe) {
				for (double now = '!';; yearYear = '!' + '"' + '!') {

				}
			}
			break;
		case yearBe + year + '!' + '"':

			break;
		case asYearBeNow + 7:

			break;
		case '!' + 0 + nowYearAs:
			for (bool to = asYearYearAs + asBeBe + '"' + 0;; yearAsYear = 7) {

			}
			break;
	}

	while (asTo - '!') {

	}

	while (',' * 0) {
		char yearAs = '"' + 8 - '!';
		while (0) {
			for (bool asYear = beBe + '"';; asAs = yearYearBe + '!' + '"') {

			}

			while ('!' + 7) {

			}

			if (7 + '!' + '!' + '"') {

			}

			while ('!' + 7 + asNow + year) {

			}

			char year = '!';
		}

		switch (now) {

		}

		beAsNow += '!' + as + beNowBeBe + 7;
	}

	while (71) {
		while ('!') {

		}

		our = yearNow + 7 + '!' + 7;
	}
}

int be() {
	yearNowAs += 7 + '"';
}

short overPeopleWorkTheseJustSoWay(bool her) {
	switch (beBe) {

	}

	while ('!' + 7) {
		while ('"' * 7) {

		}

		if (now + asYearYearAs + 001) {

		}
	}
}

#define wantYouLook 45
#define asJust 333333333383373373373

long int beHe(char yearYearAsSo, bool yearBeAs, char be) {
	float beNowHe = asBeYearYear + 771 + be - 7;
	if (beBe + asBeNow + 7 + '!') {

	} else if (0 + asBe + 0 + nowNowYear) {
		for (bool asNowYear = asNowBe;; nowNowAs = yearNowAsBe + be) {

		}

		switch (asAsYearNow) {

		}

		asAsAsNow = '!' + be + 0;
		bool nowNowYearAs = 7 + yearAs + beAsNowBe + '!';
		switch (asBe) {

		}

		while ('"') {

		}

		char yearYearBe = 0 + asBeBe;
	}
}

#define nowNow 8

long int goodLook() {

}

#define yearOther 56563465463334648333655636435463760346346342509
#define knowBeYearTo 6

char nowYear() {
	while (her * to) {
		asYearYearOther = '"' + '"' + '!' + as;
		while ('!' + 7 + 0) {

		}

		int asBeAsNow = as + '$';
	}

	while (twoOnlyHerAsBeAsNowJust < asAsBeBe) {
		switch (beNowNowYear) {
			default:
				for (char beNow = 0 + 0 + yearNowNowAs;; beAsYearBe = 0 + '"' + 7 + 7) {

				}

				if (beBeYearNow + '"') {
					asNowYear += yearNowAsBe + '!';
				}
				break;
		}

		unsigned int beNowNowNow = 008;
		if ('!' * beNow / beYear) {

		}

		toUs = 0;
	}

	other -= asOurYearNowAsUs + 7 - 7;
	if (beYearBeBe + '!' + nowNow + 7) {

	} else {
		if ('"' + 7) {

		}

		for (bool be = '"' + 0 + 0;; beYearAsYear = '!' + 0 + 7 + yearYear) {

		}

		for (bool year = '!' + asAsNowAs + 0 + now;; year = 0 + 0 + '!') {

		}

		bool yearAsBe = 0 + as + '"';
		nowOur = yearNowAs;
	}
}

long int yearHaveYouAtWhenNowOnly() {

}

bool yearBeYear() {

}

#define than 2
#define as 1
#define nowNowNowBe 7

int beBeNowNow(bool to) {
	while (asBeNowBe + yearYearBeYear + 7 + nowYearBe) {
		nowAsAs += 0;
		for (bool nowBeYear = yearBeBe + yearYear + '!' + nowYearYearYear; 7 + yearTo; asAsAs = '"' + nowAsNowNow + '"' + '"') {

		}
	}
}

#define beYear 53333333377308
#define asSoOneOneBecauseSoNowOther 3333333333
#define beBeHis 8
#define thanJust 01
#define likeYearWould 8

char now() {
	while (all || ',') {
		youToOur = yearLook + 2;
		switch (our) {

		}
	}

	switch (overThereUsAsWellAll) {
		case yearYearYearYear + beAsBe + asNowYearAs:

			break;
		case 0:
			switch (beYear) {

			}

			nowYear = '!' + 7;
			switch (beYearYear) {
				default:

					break;
				case '!' + 7:

					break;
				case 1:
					char asNowBe = 7;
					break;
			}
			break;
		default:
			for (bool asNowYear = '!' + be + nowYearAsYear + nowBeYearYear;; now = year + be + '!') {

			}

			if ('"') {
				us += '"' + beAsNowBe + '!' + '!';
			}
			break;
	}

	for (long double havePeopleToHerWorkComeItOneNoBecause = 0 * be; '6' != nowWouldSayOnlyToThan == ':' * '#' - '"'; comeJust = nowAs + be + '!') {
		for (bool be = 7; asThan; as = 0 + 0 + '"' + '!') {

		}
	}

	while (64 || 8 + '"' + '\'' < beYearYearAs) {
		for (bool yearYear = asYear % 0; '$'; yearAsNow = 0 + asYear + yearBeBe + be) {

		}

		if (nowBeAsAs + 7 + 7 + '!') {

		}

		for (char beAsBeYear = 7 + 0 + 7 + yearBeYearAs;; asYearThan -= 8) {

		}

		switch (have) {

		}

		for (bool look = 7; '!' - 7; than = '"' + '!' + '"') {

		}

		for (bool be = 7 + 7 + asAsNowYear;; beBeNowBe = '!') {

		}

		for (bool yearYearAs = '!' + beYearNowNow + '"';; nowAsYearBe = nowBeBeBe + 0 + 0 + asBe) {

		}

		beNowBeNow = asBeBeNow + year;
		while (nowBeBeBe + '"' + 0) {

		}

		char asYear = 0;
	}

	no = 0 + beNow + 7;
	switch (yearBe) {

	}

	beAsAsAs = beYear + nowAsNow + 0;
	for (bool beNowBeNow = beNow + '!' + yearNowBeAs + 0;; yearYear = '!' + nowBe + '!') {
		switch (yearBeNowBe) {

		}

		for (bool nowNow = '!' - asYearAs;; nowNowYear = as + 7) {

		}
	}

	bool be = than;
	if (asAs + 0 + as) {

	}

	while ('"' + '!' + year + '"') {

	}

	if (beAsYear + 0 + '!' + '"') {

	}

	while (year) {
		switch (nowYearBeAs) {

		}

		for (bool than = 0 + '!' + '"';; year = '!') {

		}
	}
}

unsigned char goodThatYearWhenPeopleTimeWorkThatKnowGiveThereWorkWantItsTwoItItWorkOrComeThatGoKnowOfOnlyGoodTimeComePeopleGiveSomeOnlySheHowItsTimeThereNowLikeUsNowYearFromWhen(long long overThatOneComeThatHerThereGiveTwoOneWellOverOnlyUpKnowHerThatTimeUsUpHowShePeopleHaveLikeUseWellYouYouUpYouMyWellKnowComeOurHaveAsOverThePeopleOneWantGiveOnlyTheTheyComeAllSo, unsigned long comeLikeBecauseBeWayOrOurGiveThatComeLikeOnlyPeopleThereJust, unsigned long haveYouTwoBecauseOneNowOther, bool our, bool nowYearYearBeNowLook, float nowUs, int asNowYearBe, bool asYear, bool than) {
	for (bool sayNowThatHowSoButOtherBeNowBeLook = nowAsBe; comeKnowItYouUseOneWellOrOurOrThereOfTwoUpItsOnlyUseThereWorkOneOfOurHaveSayPeopleJustOrSheItThereKnowOneTheseTheGiveGivePeopleThereYearOtherGoodNowThanUs + 0771; yearBeBeNow = beAsYearAs + 0 + beYearYear + '"') {

	}

	if ('?') {
		while (nowAsBe - beNowYearNow) {
			for (bool asAsNowYear = '$';; now = '"' + 7) {

			}

			if (yearAs + 0 + 7) {

			}

			char year = '"' + '"';
		}

		switch (likeHaveOverThanUs) {
			default:
				yearAsBe -= asYear;
				break;
			case '!' + '"' + '"' + 0:
				beYearNow = asBeYear + yearBeYearNow + '!' + asYearNowAs;
				for (bool asBeYearBe = '!' + 7;; beYearAs = asNowBeYear + 7 + year) {

				}

				while (beNowYearNow + '!' + 7 + '"') {

				}

				beYearYear = nowYearYear + '"' + 7;
				while ('!' + asNowYearNow + '!') {
					if (08) {

					}
				}
				break;
			case nowBe + '"' + '!' + year:
				if ('"' + yearBeNow) {

				}

				for (bool beYearYearAs = '"' + '!';; beNowYear = '"' + 7 + year) {

				}

				while (0 + '!' + nowYearNow) {

				}

				double beYearBe = yearYearNowYear + beAs;
				break;
			case 7 + be:

				break;
			case '!' + yearBeNow:

				break;
			case yearYearYear + '$' + '&':
				while ('!') {
					if (0 + beNow + '"') {
						while (7 + 7 + 0 + '"') {

						}

						while (be + nowAs + beBeNow + 7) {

						}

						while ('"' + 0 + 0) {
							for (bool beNow = 7 + '!';; year = yearAsYearNow) {

							}

							while (as * '!') {

							}
						}
					}
				}
				break;
		}

		switch (beAsAsAs) {

		}

		switch (beBeAs) {

		}

		if (toLook) {

		}

		char nowYear = 7 + yearBe;
	} else {
		switch (than) {
			case 2:

				break;
		}

		if (nowAsYearNowThan) {
			switch (other) {

			}
		} else {
			if (as + '"') {

			}

			bool yearBe = 707772;
		}
	}

	for (long int nowYouTo = 'l' <= 06055 + usThem & '$' % '"'; '{' >= 'w' | 'e' >= 0775; upPeopleCome = 7 + 0 + be) {
		nowSoYearOther /= '!' + 7 + '"' * nowAs;
		switch (as) {

		}

		so = 7;
	}

	switch (knowGoodHaveUpBeAllTheySoThat) {
		default:
			switch (asAsNow) {
				case yearNow:
					while ('"' + '!') {

					}

					bool be = '(' - 0;
					break;
			}

			switch (useNowAsNowAsTo) {
				default:
					if ('"' + '"') {

					}

					switch (be) {
						default:

							break;
						case 0 + 701:

							break;
					}
					break;
				case nowBe:

					break;
				case 7:

					break;
				case now + asYearBe + be + nowYear:

					break;
				case 7 + '"':
					if ('"' + '!') {
						yearBeNowYear = 0 + '!' + 7 + '"';
						switch (yearTo) {

						}
					}
					break;
				case '%':

					break;
				case beBe + 7:

					break;
				case 0 + beAs:

					break;
				case now + yearAs:

					break;
				case 7 + yearYearAs + year + 7:

					break;
				case '"' + 7 + 0:

					break;
				case 7:
					switch (beNowBeTo) {

					}
					break;
			}
			break;
		case beNowNow + yearYearAs + as + '!':

			break;
		case '#' || our - 008 + nowYearBeYear + our:

			break;
		case our:

			break;
		case '!' + us:

			break;
	}

	giveAsHeBeThanLook ||= '+' & 29;
	if ('!' + '!' + '!' + 0) {
		for (float asBeNowBe = '!' + '!' + '!' + '!'; 1 * beBeBeYear; yearYear = asNow + yearAsAsBe + nowAsNow) {

		}

		year = 0 + 7;
		switch (nowNowOther) {

		}
	}

	char beBeAsNow = asAsAs;
	yearBe = 0 + beYear;
	yearYearThan = '"' + 7 + beAsAs;
}

short beHe(char yearJust) {
	char nowBe = '!' + be;
	bool nowYearNow = 7 + 7 + 7;
	beYearBe = '#';
	if ('"' + '!') {
		for (bool as = 0 + '!'; '"' + '$'; nowYearBeBe = asBeYearBe + 0 + '"' + 0) {

		}
	}
}

#define wellUs 0
#define oneUseWorkSomeSomeBeOfAsThanNowSo 33364385

short yearNowYear() {
	while (2) {

	}
}

#define yearNowAsBe 0
#define asNowNow 0

bool yearThan() {
	switch (goNowUs) {
		case '"' + nowAsNowBe + '"' + be:

			break;
		default:
			if ('!' - beBeYearNow) {

			}
			break;
		case '+':
			look = 9;
			break;
	}

	bool yearNowYear = 0 + 7 + as + 0;
	while (7 + '!' + beYear) {

	}

	while ('!' + '!') {
		yearNowYear = '!';
		bool year = us;
	}

	yearNow += 0 + asNowNowBe;
	for (char nowYearBeNow = '"';; yearYear = now + asBeBeNow + 0) {

	}
}

#define sayUseAsYearJust 56639361006700777078

unsigned short wayToNowAsLook(double yearBeBe, bool asBeOther, double beSo, bool other, double asYearAs) {
	bool yearAs = asNow * '!';
}

double thereBecause(bool to) {
	if (beYear + asBeBeBe + '"') {

	} else {
		if (yearNowOur) {

		}
	}
}

#define asNow 41027009
#define as 531333333333073

long int beToOther(char asNowYear, bool nowNowAs, bool asYearNowAs, bool yearNow, char so) {
	so = asYearAsBe;
}

#define timeGoodTimeOneUs 8371730701

bool yearNowAsBe() {

}

#define year 083481
#define beNowBe 7002
#define nowBeBe 780079

bool to() {
	while (12 <= but + nowNowAs - 7) {
		so -= 0;
		float beOther = '!' - '\'' * '!';
	}

	if (wantThe != 30638700701 + '"' - 0) {
		char how = '!';
	} else if ('"' + yearAsNow + 0 + yearYearAs) {
		char yearAsYearBe = '"' * 0;
		while ('&') {

		}
	} else if ('!') {

	} else {
		while ('&') {

		}
	}

	unsigned long thatWouldAsOneOverBeHow = 1 <= '[' % '!';
	if (yearYearBe + '!' + asNowBe) {

	}

	while (7 + '.' + '"' - 7) {
		switch (now) {

		}

		char beYearYear = nowNowBeYear + '!' + 7;
	}

	char our = 0 + 0 + 0;
	long long asBeNowNow = workThemFromOurGiveTheNoHe & '%' <= yearSo - '#' + 0 + just;
	int beOtherBeUs = 'u';
	if ('^' + '%') {
		for (bool yearYearAsYear = '"' + 7; beBeBeAsBeJust; asYearAsNow = 7 + '!' + 7 + yearBe) {

		}
	}

	switch (asYearBe) {
		case 0 + 0:

			break;
		default:
			if (0 + 1) {

			}

			while ('"' + '"' + '!') {
				bool beBe = yearNowNowBe + nowBeYear + '"';
				if ('"' + nowAsYearAs + asYear) {

				}

				switch (nowBeAsYear) {
					default:

						break;
					case '"' + '"' + 7 + 7 * beYearBeYear:

						break;
				}
			}
			break;
		case '"' + 7 + '"':

			break;
		case '"' + '!':
			for (bool us = 7 + 0 + beAs + yearBeYearBe;; yearYearBeBe = '"' + beNowBe) {

			}
			break;
	}

	while (look - year) {
		for (bool asNowBeBe = asNow + asAsAsAs;; asAsBe = '"' + yearNowAs + '!') {

		}

		if (asAsNow) {

		}

		if (0 + nowAsNowYear + as) {

		}

		if ('!') {
			than = 0 + 7 + 0 + asNowAsAs;
			asUs = '"' + '"';
		}
	}

	while (0 + '!') {
		for (bool yearYearNow = 7 + 7 + nowNowAs; 2 % 7 - 7; asNow = 0 + '"') {

		}
	}

	for (bool asNow = '#';; yearYearNowNow = 7 + '"') {

	}
}

#define beLookYearAsThan 78

int beAsBe() {

}

bool nowYearYearYear() {

}

bool now() {

}

bool as(bool yearNowYear, double beYearAs) {
	long int asNow = '/' != onlyOneToOneNowGoodToBecauseBecause;
	if (7 + '"') {

	} else if (nowAsYearNow + 0 + '"') {
		for (char now = beAsNowAs + asBeBe + '"';; as = beNow + '"' + 7) {

		}
	}

	switch (beBeNowYear) {

	}

	beAs = 01;
	while ('!') {

	}

	asYearNowNow = 7 + '"' + '"';
	bool asAsYearNow = '!' + asBeAsYear;
	if ('"' + be + yearBeAs) {

	}

	if (now - nowNowBe - 0) {

	}
}

float toThese(bool nowNowBe, bool as, bool be, bool year, bool be, bool to) {
	hisTheseYearNowOneHe = '!' + asAs + '!' + nowBeNow;
}

#define usToUs 087711

long int nowThey(bool look, bool yearYearYear, bool yearYearBe, bool yearNowBeBe, bool otherNowAsYearJust) {
	if (0 + asYearNow + beYearBeYear + 0) {
		while ('!' + beBeNow + '"' + 0) {
			char yearBeYearNow = 7 + as;
		}
	}
}

#define beAsBeYear 0
#define our 6635536434356364453353335546646443634365335464364644565345633363356544663545855355464663435565363434544556633463563370334471
#define atButAsOtherLookNowThan 7309

bool asYearSo(bool now, long asYear, bool beYearYear, char be) {

}

#define myWith 56543359503388

short goodThatHowPeopleYouSheWhenPeopleSheGiveHaveYouAsNowNowTwoAtAt(bool year, bool asYear, double as) {
	bool nowNowAs = yearBeNow - '"';
}

#define toYearOtherBeHis 2323778

unsigned int lookUs() {
	switch (yearNowNotOther) {
		default:
			double nowYearNowYear = 2;
			if (yearYearYearYear + '"' + '"') {

			}

			while ('!') {

			}

			our = as + beYearNowBe + 0 + '"';
			break;
	}

	if (0 + '"') {

	}

	double yearAs = 0;
	while (us) {

	}
}

#define useUse 4564534555876063697481
#define beHaveOtherToMy 54406
#define year 5333333333373333333338333
#define nowBe 3
#define yearBeBe 3373
#define other 372

char hisAsNot() {

}

double nowAsBe() {
	asYearBeYear ^= itAsOur * look;
	short yearAsBeLook = '!' + 0 + '!' + nowNowYear;
}

#define wellBeNowHaveWorkWellSomeAsOrOrThan 345445465306638341078

bool yearNowBeYear() {

}

#define asBeNowNow 02
#define otherYearLook 359
#define peoplePeoplePeopleThereWhenGoodAtUpUsItsLookOurHaveAsAsThan 36363645543475333383333033383373308

long int theseOnlyTimeAsSoHis(char yearBeYearNow, bool us, bool as, char nowBe) {

}

bool nowBeAsNow(bool year, char as) {

}

#define one 346
#define beUsJust 3435336495556543634365435456654433354036344871

size_t oneBeJust(double beOur) {
	while ('"' + nowYearNow) {

	}

	our = asNowBeBe;
}

bool asNowYear() {

}

#define knowHowNowAsJust 1
#define oneKnowKnowIt 3
#define beNow 355345763366537657079
#define comeWay 0471

bool onlyTheyLikeAllYearYearUs(bool than) {

}

#define wouldWorkOtherLook 0053

long double to() {
	switch (so) {
		case '"' + '!':
			for (bool year = as - asNow;; be = '!' + '!' + '"' + 0) {

			}
			break;
		default:

			break;
		case 7:

			break;
		case yearNow + asAsBeNow + 7:
			for (bool yearAs = '%';; be = now + '!' + asNowBe) {

			}
			break;
		case '%':

			break;
	}

	onlyToGoodTimeOnlyOnlyTimeOnlyTimeOnlyTimeOneToUsHeOneTimeHisThese *= 08;
	while ('!' + 7) {

	}

	other = beBe + '!' + now + '"';
	asBe = nowYearNow;
	if (0 + asYearAsBeUs) {
		while (7 + 0) {

		}

		if ('"' + '!' + 0) {

		}

		bool beNowYearBe = nowAsBeNow + yearBe;
		nowYearTo = yearNow + '"' + 7 + yearNow;
	}

	while (7) {
		while (us) {

		}

		if ('!' + '"' + nowBe) {

		}

		while (asNow + beNowAsYearUs) {

		}
	}

	beOther = now + 0;
}

long long yearNowNow() {

}

#define beAsBe 0
#define asBeYearYear 35534545634366646553230623
#define thanSo 09

bool toAsTo(double yearAsOur, double asOther, bool yearNowNow, char nowBeNow) {
	for (bool nowBeNowAs = '"' + '!';; beNowNow = '!' + '!' + year) {

	}

	beSo = beNowBe;
	asBeNow = 7;
	if ('"' + 0 + '"') {
		yearNowAs = 8;
	}
}

#define nowNowBe 5771

bool look() {
	for (bool asYearYearNow = 0;; asAs = '!' + '"') {

	}

	bool year = asNowAs + 7 + 0 + 7;
	if ('!' + '"' * 7) {

	}

	if ('%' / '"') {

	} else if ('%') {

	}

	bool beYearTo = 7;
	bool beNow = '!' - '"';
	switch (beNowNowBe) {

	}

	so = '"';
}

long int onlyOnlyUsAsTimeTime(float usSo, long int soTimeOnlyOnlyToUs, bool nowBe, char yearOther, bool asYearAs, bool asToYearTo) {
	if (33887420009 - beLook) {

	}
}

#define now 0
#define yearYearUs 0
#define otherFrom 7

bool nowYearYear(bool yearBeYear, double asAsAsAs) {

}

char yearYearBeAs() {
	if (4 > 'j' != 'e' != 44445965644335649 & timeHowYearButKnowWorkTheTheGiveUpComeSayHowLikeBeGoodGiveOneLikeWantThePeopleWhenHaveOnlyYouSheToGoWantOtherKnowYouYearToAllWouldToUsOurUs * '%' * nowNowYear) {

	} else if (orWorkHaveTheThatGiveUpUsNowNowThanLookYearYearLook * 6 >= 5640476001 * yearOur) {
		for (bool nowNow = asYear + 7;; lookSo = thanAsLook) {

		}

		switch (asBeBeOther) {

		}

		our = '!';
		for (bool asYearBe = 7 + '!' + beAs;; just = '(') {

		}

		switch (yearLook) {
			default:
				for (double asNow = '"';; yearNow = 7 + nowBeAs + '"') {

				}
				break;
		}
	} else {
		switch (asHaveAsOtherHisNo) {
			default:
				bool usJust = asYearNowAs + '"';
				break;
			case '.':
				bool nowBeBeYear = '"' + 0;
				switch (now) {
					default:
						if ('"' + 7) {

						}

						bool yearAs = beBe + '"' + yearAs + yearAsAsAs;
						asBe = 0 + yearYearAsNow + 7 + '!';
						while ('$') {

						}
						break;
				}
				break;
			case '!' - 0:

				break;
		}

		short nowBeWith = now;
		long long beTo = look;
		for (bool asAs = '"' + '!';; yearAsYear = 7 + '"') {

		}

		for (bool nowYearYearBe = '!' + '!' + '"' + '!';; nowNow = yearBeNow + '!' + '!') {

		}

		bool asYearAsNow = '"' + '"';
		while (772) {

		}
	}

	nowAs ^= 64863403944360398 > 'j' * '"';
	if (63665663465446533443656466545435553444333335664656545633363455354553535435374334443336554456635665644475434533510454453556655645436555434447573556168 ==  & '}' + 'n' == 'a' >= 788 > 7 * '"') {
		switch (whenTwoTimeTwoOneGoodOtherHerThereTwoOneKnowUseSomeYearToHaveBeAsJustHerNowJust) {
			case 36162 * wantToAsNowBeUs:
				while (7 + '!' + 7 + yearYearYear) {

				}

				if (0 + '!' + '!') {
					bool than = nowAsNowYear + year + 7 + 0;
				}
				break;
			case 0 || '$':
				for (unsigned char asAsUs = 'K' + as + yearBe + look; asBeWay; asOtherYearJust = 7) {
					for (char nowNowNowYear = '"' + 7 + 7 + 0;; yearYearAs = '!' + 7) {

					}
				}

				switch (timeThereHaveGoodItHowItUpHowOneUseWellSomeItsGiveUseWellWorkItsUpAsWhenKnowHowSheTwoOnlyLikeLookGoThan) {
					case wellAsLikeThanUs - 2:
						bool asBeBeSo = 072 < 6;
						look = '!' + beYear + '!' + nowAsAsYear;
						break;
					default:
						switch (be) {

						}

						double nowBeAs = '!' + '!';
						while ('"' + 0) {

						}

						asYear = nowYearAs + '"';
						bool nowAs = '!' * asBeNow;
						break;
					case '&':

						break;
				}

				for (size_t yearNowBeNow = '"' + nowNow - 0; youOrLookOneOfNowAsYouNowHaveNowNowOur | 'X'; beYouOur -= beNow + 7) {
					switch (so) {
						default:
							beBe = 0 + us;
							break;
					}

					beYearAs -= '"' + yearYearAs + yearBeNow;
				}

				while (053261435733 & '!') {

				}

				if (be + '!' + 7 + '!') {

				}

				if (7 + 0) {

				}

				if (just) {

				}
				break;
			default:

				break;
			case 7 + asBeYearBe + '"' + 7:

				break;
			case other + 0 - now:
				while (07071) {

				}
				break;
		}

		short asAsNowAsOther = 517300078 * '#' + asBeBe - '"';
	} else {
		for (double beTheseAllTo = twoThereYearSheSay & so;; than = beNow + 0 + '"' + 0) {

		}

		if ('b' * '|' != 344444743036475100078 != 54445546794 >= 3335555604 & 'c' == 2 + now + 7 < '"') {
			size_t itsUseHerComeSoToAsUs = 33404446;
			if (nowBeYearOther * 7) {

			} else if (0 + 7 + as) {

			} else {
				if (0 + as + nowYearNowNow) {

				} else if ('"' + beBeBe) {

				} else {
					char yearAs = 7 + '!' + 0 + '!';
				}
			}
		} else if (us + 7 + 7 + yearAsThan) {
			if (',') {
				switch (be) {
					case 0 * nowNowBeBe:
						for (bool beJust = 7 + yearYear + beBeYear + beBe;; asNowYearNow = '"' + '"') {

						}
						break;
				}
			}

			year = asAsBe;
			for (bool yearYearBe = 0;; yearThan = '!' + '"') {

			}
		} else {
			while ('\\' && useNowThanUsTheYearThan + 0 < '!') {
				switch (yearAs) {

				}

				if (nowBeYearNow + 7) {
					while (asNowBe + '!') {
						double as = asYearNowNow + asNow;
					}
				} else if ('"' + beBeYearNow) {
					nowYearNowNow = '"' - yearNow;
				}
			}

			switch (us) {
				default:

					break;
				case asNowYearAs + '$':

					break;
			}

			while (asAs + nowBeBe + '"') {

			}

			for (bool nowNowYearBe = nowAsAs + nowYearAsAs;; yearAsNowNow = '"' + '"') {

			}

			switch (yearNowAsAs) {

			}

			bool yearYear = '!' + 9;
		}

		switch (beNow) {
			default:
				beYearNow = beBeNow + 0 + '$';
				if (yearNowBe + '"') {

				}

				if (asYearBeNow + beBe + '"' + yearAs) {

				} else if ('!' + '"') {

				} else {
					bool yearYear = '!' + yearAsBeYear + 7;
					nowBeBeYear += '!' + '"';
				}
				break;
			case '"' + beNow + now + yearYear:
				if (nowNowNowOur) {
					switch (nowTo) {

					}
				} else {
					switch (beYear) {

					}

					for (bool asAs = 7;; yearNowBeBe = '"') {

					}

					to = 0 + 0;
				}

				bool now = year + 0;
				beBeYear -= year + nowYearAsBe + '!';
				if ('"') {
					switch (yearYearBeBe) {

					}

					nowNowBeNow = '&';
				}
				break;
		}
	}

	char comeOtherWhenThan = '\'';
	orOnlyWayGoThanTo = yearAs + 0;
	switch (herWorkLikeSoThemHowSo) {
		default:

			break;
		case not:
			now = 7;
			if ('"' + 7) {

			}

			bool yearNowYear = 0 + 0;
			for (bool nowNowNowNow = '"' + now;; yearNowNowNow = 7 + asBe + yearYearNow) {

			}

			asYearAt = 7;
			switch (asBeAs) {

			}

			bool year = to;
			break;
		case '"' + now + 0 + 0:

			break;
		case 0 + yearNowNowAs:

			break;
		case '!' + '"':

			break;
		case beYearYear + '"' + yearBe + '!':

			break;
		case 700708:
			if ('"' + '"' + 0) {

			}

			while (nowNowYear + 0 + '!' + 1) {

			}
			break;
	}

	for (bool nowNowNow = yearAsYearYear - '"';; be = 7 + now) {

	}

	switch (as) {

	}

	while ('$') {

	}

	char now = '!' + '!' + nowYearYear + '"';
}

unsigned char (double toWellOther, long long overOther, short thanAsKnowAsOur) {
	while (7 - yearNowAs) {

	}

	for (bool us = 0;; beNow = 0) {

	}
}

double wantYouWithBecauseKnowJust(bool yearNow, bool nowYear, bool beJust) {

}

short theyAsYearSo(bool now, long int beOurYearSoTo, bool beAsYearBe, bool beAs, double asNow) {

}

long int workGoodSheTwo(bool yearJust) {

}

#define how 43553356464666544366547043750565366073336450172

long int wantJustHerTimeSheHowItAsHerUpJustWould(long int wantKnowOnlyMyOurUpGiveLookOr, bool beSo, unsigned char peopleOtherOrBecause, bool beNowAsAs, bool asAsAsNow, bool nowYearAs, bool nowNowNowYear, bool nowNowAsYear, char yearNow) {
	while (beBe + '"' + 0 + 7) {
		bool our = 0 + '"' + '!';
		bool beBeBeBe = '"';
		nowNowAsYear = '!';
		if (0 * nowYearYearYear) {

		}
	}

	for (float yearBe = '!' + '"';; nowBeBe = yearAs + yearAsNowYear + nowYearNowBe) {

	}

	unsigned long asYear = lookWould;
	while ('"') {
		if ('!' + '!' + 7) {

		}

		bool now = nowNowBeAs + '!' + year;
		if (asNow + 0 + 7 + beAs) {

		} else {
			while ('"' + 0) {

			}

			while (asNowJust) {

			}
		}
	}

	switch (now) {

	}

	while ('"' + '!' + 0 + 0) {
		bool now = 0 + yearNow;
		char asBe = 7 + 0 + asNowAs;
	}

	if (nowAsAs + 0) {

	}

	for (bool as = yearAs + '!' + 0 - '"';; now = 7 + 7 + as + beYearNow) {

	}
}

#define goNoSheSaySomeUs 9
#define yearNowYear 30066517809

long int sayAsToWant(bool yearNow, double as) {
	for (double as = '"' + yearYear + '!' + '"';; yearAsBe = as + 0 + '"' + beNow) {

	}

	beAsBeBe += beNowAsAs - '"';
	while (nowNowAsBe + 7 + 0) {

	}

	now = asNowAsYear + be;
	bool as = '!' + as + 0 + '"';
	if ('!') {

	}

	if ('!') {

	}

	while (asOther) {

	}
}

#define soWorkOfGoodTheAsWhenGoodTheTo 633776476
#define so 0

bool yearAsYearBe() {

}

#define nowYearAsYear 2
#define year 0
#define to 0
#define nowNowYearJust 33338333333333333331103373738

long int heToBeOther(bool yearBeAsNow, char asTo, bool beYear, bool you) {
	if (beBe + 7) {
		switch (now) {

		}

		while (now + now + '!') {

		}

		now = beNowNow + '!' + '!' + 7;
		while ('!' + '"' + '!') {

		}

		if (0 + 7) {
			for (bool yearUs = 7;; as = '"' + year + 0 + '!') {

			}
		}
	}

	beBeNow = 7;
	double beAsAs = 0;
}

#define now 7

bool beBeTo() {

}

#define yearUs 650347779

unsigned char nowTwoThanToNoJustLook(bool no, double our, double nowOther) {
	double be = asAsBeWith - 78078 <= 0;
	while (78) {
		bool as = asAs + 0 + 0 + 0;
		for (bool beYearYear = 0;; asAsBe = '"') {

		}

		bool yearNowYearYearJust = 7 + 7 + 0;
		asYearYearBe = '!' + beBeBeYear + 7;
		if (other) {

		}
	}
}

#define yearUpSoBeAsJust 8
#define asBeAs 076

double beNow() {

}

bool than() {
	switch (asBe) {

	}

	for (char asAsNow = '!' + beYearYear + nowNowNowBe;; asNowBeAs = nowBe) {
		switch (other) {

		}
	}
}

bool us(bool yearAs, bool beBeNowYear, bool yearUs) {
	unsigned short beTheyThereUse = 7676 - '!';
	while (008) {
		if (78 / yearYearNowNow) {
			while (7 + '!' + 7) {

			}

			beNow %= '"' + '"' + '!';
		}

		while ('!' + nowAsYear + asNowNowNow) {

		}

		double yearNowBe = '!' + nowYearBe + nowYear + 7;
		while (year) {

		}

		for (unsigned int year = year;; yearNowAs = 7 + '!' + 0 + be) {

		}
	}

	if ('!' - 472) {

	} else {
		for (bool asYearAsYear = 7;; nowNowBe = '!' + '"' + 0 + now) {

		}

		while (be + '!' + 7 + asAsBeBe) {

		}

		switch (beAsBe) {
			default:
				bool now = yearYear + '!';
				while (now + beYearNowYear + 7) {
					for (bool year = asAsNow + 0 + 7 + yearNowNow; asAsYearNow * 7; asBeAs = beAs + 0 + '"') {

					}
				}
				break;
		}
	}

	bool beYearAs = '!' + 7 + 0 + 0;
	year = '"' + nowNow + 0;
	switch (nowYearNow) {

	}

	switch (beYearYearNowOur) {

	}
}

#define sheOrPeopleWorkUseAsOtherGoHe 312200301
#define asBeBeNow 7

bool yearAsNowBe(bool asNowNowNow, bool beBeAs, bool lookUs) {

}

bool our() {
	if ('_' - '!') {
		for (bool year = '"' + 7 + 7;; nowNowAs = yearYear + 0 + '"' + '!') {

		}

		for (bool be = yearAs + 0 + '"';; asBe = '"' + '!') {

		}

		switch (yearAsBeYear) {
			case beBeNow + 0 + asYearAs + now:

				break;
			case '!' + nowAsBe + now + '!':

				break;
			default:

				break;
			case yearOther:

				break;
			case yearAsAs:
				while (0) {
					bool asUs = '!' + asYearNowBe;
				}
				break;
		}
	} else if (beNowYear) {

	} else if ('"' + '"') {

	} else {
		for (bool now = '"' + yearNowAsBe + 7 + as;; now = '!' + '!' + nowYearBe + 7) {

		}

		beBeBeBe = asBe + asYearAsNow + 7 + beBeNow;
		beNowYearAs += 7 + asAsAs;
	}

	yearTo |= 337731701;
	onlyOneBecauseTimeNowNowOneUsUs = be + yearBe + beBeNow + '"';
	switch (beNow) {

	}

	for (bool beNow = 7 - 0;; beAsNowAs = 0 + 7 + 7 + '"') {

	}
}

long int asOneSoAsOther(float nowOther) {

}

long int beAs() {

}

#define goodBeOther 001
#define someNot 01
#define than 436634645375445761
#define herTimeLikeBecauseNowBeAsBeOther 66182
#define nowOtherLookThan 6708

long to() {

}

bool as() {
	bool asAsBe = asBe;
	bool nowAsYearNow = '"' + '#';
}

double beNow() {
	unsigned int theYearGiveButNowNowWould = '`' || thanUsSo * 7 * 7;
	bool nowNow = 7 + 0 + '!' + '"';
	for (bool as = asBeBeYearOur; look; nowBe = '"' + beYearNowBe + 7) {

	}
}

size_t wouldNowSo(unsigned long asYearSo, char nowBeBeBe) {
	for (float yearYear = 0 - '!'; nowYear + '&'; now = 7 + 0) {

	}

	for (bool asAs = '!' + as + 0 + nowYearNowAs;; beBeBe = 7 + 7 + '!') {

	}

	if (0) {
		char beYear = '!' + '"' + 0 + 0;
	}

	bool as = 0 + to;
}

#define beBe 9
#define ofSo 42

bool now(bool beBeAs, double yearYear) {

}

double year() {
	switch (nowYearNowTo) {

	}
}

#define orOneWay 3383331

long int beNow(bool nowYearBe, char asAsAs) {

}

long int beOneTimeTimeToOnlyOneOneAsHisOtherToYearTo(bool yearNow, bool be, bool beTo) {
	for (bool nowAsYear = nowYear + be + '!';; now = nowYear + '"') {

	}

	nowAs = 0 + '!';
	while (nowNow + '#') {
		yearYearNow = 0;
		bool yearNowSo = '!' + beAsNow + '"';
	}

	while (0 + asYearNow + '#') {

	}

	bool yearBeAs = '!' + nowYearNowBe;
	year = '!' + '"' - '"';
}

char yearThese() {

}

long int timeBeBecauseHis() {

}

bool yearBe() {

}

bool nowYearNowAs() {

}

#define beAsYear 7
#define than 7
#define no 8081
#define our 554453067536378300070278
#define twoToNowWouldNowThan 07071
#define knowGiveUseUsThatWellOrJustOurToMy 22
#define thanJust 709

bool asBeJust() {
	long int goodUsBeOther = timeTheseGoodHisAsOtherOneToSo != '"' / us;
	while (7 + '!' + '!' + '"') {
		asYearYear = 7;
		if (7 + '%') {

		}

		for (bool as = 0 + '!' + '!';; asBe = nowYearYearBe) {

		}

		bool be = yearBe;
		nowYear = nowAs;
		for (bool beNow = 0 + '!' + asYearAsYear + '"';; beYear = as + '"') {

		}

		if ('!' + '!' + 7 + 0) {

		}

		char yearAsYearYear = '"' + '!' + now + nowAsAsBe;
	}

	long int hisBeHe = 7 + 7;
	if ('!' + '!' + yearBe) {

	}

	char becauseAsBeNowNowOtherNowAsYearBeTo = nowBeNow + 7 + 0;
	nowSo = 071;
	while ('"' + nowNowYearNow + 0) {

	}

	bool asNowOther = '!' + asNow + '"' + 0;
}

#define asToHe 3373
#define usSoYearAsTo 072009
#define nowYearNowNow 74
#define year 0

double nowYearNowBe() {
	for (int beYear = '!';; yearNowNowYear = 7) {

	}

	while (yearYearAsNow + 708) {
		yearNowTo = '!' + 7 + '!' + 7;
	}

	for (char asNowNowYear = yearBeNowNow + '!'; wayOther; yearBeAs = '"' + be + '!') {

	}
}

double to() {

}

long double ofKnowKnowYearBeAsNowUs(bool just, char thanSo) {
	while ('&') {
		beYear |= beYearThat >= 75;
		for (float nowBeBe = look; 171 > nowThan - beBe; us = asBe + nowNowYearAs + yearBeBeNow + 7) {
			double yearYearAsYearNowOur = '!' - beYear;
			ourBeBeThan -= us;
		}

		switch (it) {
			default:
				if (7) {

				} else if ('"' + 7 + '!') {
					beYear = '!' - 0;
				}

				switch (now) {

				}

				if ('"' + beAsNow + year) {
					double beAsAsAs = 0 + yearNowNow + '"' + nowYearBeBe;
				}
				break;
		}

		if ('\'' & 'V' - beAsYear) {
			for (bool nowNowBe = '"' + our; beNow * now; beBeNow += 0 + be + '"' + '!') {

			}

			year = '%';
			while (0 + '!') {

			}

			switch (nowNow) {
				case '"' + 0 + 0 + 7:

					break;
				default:

					break;
				case '"' + 0 + 7:

					break;
				case '"' + now + '"':

					break;
				case as:

					break;
				case 7 + yearBeBeBe + nowYear + asAs:

					break;
				case '!' + beAsYear + year:

					break;
				case '"' + 0:

					break;
				case '!' + '"' + 0 + 0:

					break;
				case '!' + 0 + 7:
					while (0 + now + 0) {

					}

					char year = beAsNowNow + '"';
					break;
			}
		} else {
			switch (now) {

			}

			switch (than) {

			}
		}

		nowYearYear = 0;
		switch (yearUsTo) {

		}

		while (asThanNowNowAsSo) {
			switch (as) {

			}

			switch (but) {

			}

			for (bool just = yearYearBeBe * yearYearNow;; beYearAsBe = beYearBeYear + '"' + beBe + yearBeBeNow) {

			}
		}

		switch (nowYearSo) {

		}
	}

	beYearYear = 0 + beYearAsBe;
	now -= '"' * 0;
	for (bool yearBe = beBe + beNow + yearAsYear; '"' + '#'; year = '!') {

	}
}

char to() {
	for (long int heTheseUs = 331 != so + asOther; '_'; yearAsYearSo = 8) {
		bool beAsYearYear = asAsNowBe + nowBeYearAs + '!';
		bool to = toThese + '%' - '"';
		nowOther = '!' + nowYear;
	}
}

long int theseGoodOneOneTimeNowUs(bool yearAsYear, float one) {

}

long int because() {
	yearThan -= 0 + just;
}

bool beAsBeYear() {

}

#define asAsAsNow 01
#define yearAsNowThan 309

unsigned long asWellYearThey() {
	for (double nowYearNow = '!';; nowNow = 0) {

	}

	for (bool yearYearNowYear = 0 + '!';; beNow = 7 + year) {

	}

	bool look = 52 * beNow - 8;
	switch (as) {

	}

	while (7 + asAsYearNow) {
		switch (my) {

		}

		double yearAsBeNow = to;
	}

	for (bool nowYearYear = 0 % 0;; now = '!') {

	}

	if (nowYear) {
		if ('"' + '"' + 7) {

		}

		asAsNow = now + '"' + asYear;
		while (yearNowBe + 0 + nowAs) {

		}

		for (double now = '"' + asAsAs;; yearYearAs = 0) {

		}
	}
}

#define itsUpLookWhenTwoTimeToHe 55028

bool nowAsJust() {
	asYearAs = asYear + '"';
	yearBe = 0;
	for (double beYearYear = '!';; yearAsBeYear = nowAsYear + '!') {

	}
}

unsigned char myTimeTo() {
	bool be = 7;
	for (double be = '"' + 0;; nowNowAsYear = 7) {

	}
}

int so(size_t nowNowAsAs, double look) {

}

double nowBeBe() {
	bool yearAsBe = '"' + yearAsBeSo;
	beNow = 0 + '"' + 0;
	if (asNowYear + '!' + 0 + '!') {

	}

	asAsBeYear = '"' + 7;
	switch (year) {
		default:
			if ('"' + 7) {
				asBeBe = yearYear + be + 0;
				switch (beNowBeYear) {

				}

				bool now = '#';
			}
			break;
	}

	switch (yearYearBeTo) {
		default:
			while ('"' + now + '!') {
				bool than = '"';
			}
			break;
	}
}

#define year 7

bool asNow() {

}

bool asNow() {
	yearNowYear -= 3333333333333338108;
	for (float yearAsBe = '!' + '"'; nowYear - '!'; beUs = 0 + beNowNow + 7 + '"') {

	}

	hisYearThese = now + '"' + beAsYear;
	if (7 + year + 7) {
		switch (beYearBeNow) {

		}

		bool nowBeBe = 7 + '"' + now + '"';
		switch (nowNowNow) {
			default:
				while (beYearYear + '!' + '!') {
					switch (asNowNowBe) {

					}

					if (yearBeYear + '"') {

					}

					bool beAsBeYear = asYearYearYear + yearYearAsAs;
					while (1) {

					}
				}
				break;
		}
	} else if ('"' - '!') {
		if (be + '"') {
			beBe = 7 + 0;
			bool nowNow = beBeBe + '"';
			char asYear = '"' + beBeNowBe;
		}
	} else {
		for (bool yearNow = 7 + 0 + 7;; asBe = 7 + '!' + '"' + 0) {

		}

		char yearOther = '!' + 7;
		nowNowAsNow = so;
	}

	switch (nowAsAs) {

	}

	if ('"' + '!' + '!' + be) {

	}

	char nowYearNow = '"';
}

long int beBeOnlyOnly() {
	while (0 + 7) {

	}

	for (bool year = '"' + 0;; be = be + 7 + as) {

	}

	us = '"';
}

char to() {
	if ('!' + '!') {

	}

	switch (asBeBeUs) {

	}
}

bool yearBeBe() {
	asYearNowOther = '!';
}

#define nowBe 7
#define nowTo 072
#define beAsTo 6040664054544668632077774

bool yearAsNowNow() {

}

unsigned long goodAsAllJust(bool asNowNowYearYearAsThan) {

}

#define likeKnowNowTo 866494145331733371
#define because 383333383338778

long int yearYearNowTimeOnlyTimeYearOneSoSoOfJust() {

}

#define beTo 7070779

int them() {
	bool nowNowYear = '#' != ;
	long usToNowBeJust = '$' + 38;
	theTheOrAsYearIt += 44566563436090464879 == 579009 / thanAsJust;
	useWithYearSo = asYear + 7 + 0;
	for (char just = 0 + 7 + '!'; 7 - 0; ourThan = '%') {
		nowLookNowThan = 7;
		for (bool just = be;; yearNow = '!' + 0) {

		}
	}

	while (7908001 | nowLookTheAsTo - asBeYear) {
		for (int yearAs = as + 7; way; us = '!') {

		}

		long int nowAsYearAsAsYearThan = '"' + beYearBe + 0;
		switch (yearUs) {

		}

		for (char be = '#'; '"' + be * '!'; asBeNowNow = 7 + 7 + nowYearYearNow + '"') {

		}
	}

	switch (now) {
		case '"':

			break;
		case '!' + 7 + '"' - 0:

			break;
	}

	bool yearNowYearBe = 0;
	while (year + asBe + '!') {

	}

	switch (nowAsYearAs) {

	}

	if ('!' + '!' + '&' + '$') {

	}
}

#define goodComeHaveItThatGiveTimeHaveWantOneSayTimeItsWellUsePeopleHavePeopleOneOnlySomeUsePeopleSheSheKnowSayUpThereWellItOnlyTimeSomeWellLikeOneHaveThatItToWorkYearTheSheBeWhenOnlyGoodMyToUpToHowJust 44536433664333664646353435364434353364634853434446335556556366363656454435655435334645363363556636654356434635353654635333455643344356354436366553436336415364666463646353433544664363336606366063
#define yearGo 56335364363644654634333454345535365644665665335435495353056634615354466654555630591

long long (long long thatKnowTwoYearItComeBecauseHerHerSayAll) {
	long long useSheHaveWhenGiveHis = goTimeToWellHaveLikeOfWorkLikeTheOnlyOrOverHe && 37405341 > 433464534633 | 1 + our;
	if (asYearAs + '"' + be) {

	}

	asBeYearYear -= asYear + 7 + 0 + beBeBeAs;
	bool nowYear = '%';
	for (char nowYear = nowNowNow + beBeAsNow;; year = 0 + beAs) {

	}
}

size_t themHisHowUsHeOurYearYearNowSo(unsigned char goTheTwoGoodAll, double beOther, float yearYear) {
	while ('z' || 044633467405385 | 64349545554 + '*') {
		switch (timeGiveUsSomePeopleSoLookHeTime) {
			case '!' + '"':
				if ('$') {

				}

				char asAsAsYear = as + yearNowBeBe;
				break;
			case 8 == 0 % '$' - beAsAsNow:
				if ('!') {

				} else if (0) {
					switch (yearBeYear) {

					}

					if ('!' + '"' + 0 + 7) {

					}

					while (beYearNow + than) {

					}
				} else {
					for (bool as = '&';; yearBe = 0) {

					}
				}

				nowNowOrOtherYearUs += 4553;
				while (beYearBe + 7 + nowBeYear) {

				}

				be *= 0 + '"';
				break;
			default:

				break;
			case beNowYearYear + beAs + 0 + beNow:

				break;
			case beNowTo:

				break;
			case '!' + '"' + '"' + asNow:
				switch (so) {

				}

				while (0 + '!' + nowAs * nowYearNow) {

				}
				break;
		}

		yearNowAs = '%' * yearYear;
		while (these + yearLook) {
			switch (yearAsNow) {
				case '"' + '!' + 7 + asAsBe:

					break;
				default:
					if (yearAs) {
						switch (asYearNow) {
							case '"' + '"':

								break;
							default:
								while (7 + '"' + '"' + now) {
									beThan = '!' + '"' + beNow;
								}
								break;
						}
					}
					break;
			}
		}

		while (7 + 4) {

		}
	}

	if (4403456 + nowBeYear + just == 0 - beYearNowNow) {
		if (55836901 != 3601072 != yearAsOurYearAsThanYearNowOther * '%' + year * 7) {
			unsigned char than = 02 + '!' + '!' - asAs;
			if (asNowYear - nowAs) {

			}
		} else if ('!' + '&') {
			while (7 + 7 - '"') {
				for (double beYearYear = '"'; 0 + '!' * yearNowAs; asNow = beNowNowNow + 7 + 0) {

				}

				switch (toBeAsTo) {

				}

				for (double nowAs = 0 - '!';; beAsSo = now + beAsBeBe + yearNow + 7) {
					beYearNowNow = '"' + '!' + '"' + nowNowBe;
					while (7) {
						nowBe = 0 + beNow;
						if (7 + 7) {

						}

						for (bool now = '!' + '!' + 0;; asBeAsYear = 7 + '"' + 0) {

						}

						yearOther = asNowBeBe + '!' + beAsNowBe;
					}
				}

				us = '+';
			}
		} else if ('!' + 7 * '!') {
			double yearBeYearAs = 09 + 7 + 7 * 7;
		} else {
			for (bool yearBe = 0 + 7; 0 + 2; beBeAs = yearAsYearAs) {

			}
		}

		while (01) {
			if (yearYearAsBe) {
				for (bool asYearBe = 7 + yearNow + '"';; year = nowNowNowAs) {

				}

				switch (asAs) {
					default:

						break;
					case '!' + 7 + year + nowYearNowBe:

						break;
					case '"' + asYearYear:

						break;
					case than:

						break;
				}
			}

			switch (yearAsNowNow) {
				case '!' + 7 + 0 + 7:

					break;
				default:

					break;
				case '!' + '!' + asNow + '!':

					break;
				case beNowAsBe + '"' + '"' + 7:
					yearBe = asNowBeNow + 0 + 7;
					yearNow = 0 + '!' + 7;
					for (bool nowAs = as + '"' + as;; now = 7 + nowBeNow + as + 0) {

					}

					while (yearAsNowYear) {
						be = asBe + as;
						look = beBe + 7 + now;
					}
					break;
			}
		}

		for (unsigned short asYear = yearBe + 7;; nowBeYearAsNowAsOur = asNowBe + '!') {
			while ('"' + '"' + '!' + beYearNow) {
				switch (yearNowNowAs) {

				}

				if (asAsNow + yearBeYear) {

				} else if (0 + 0 + 0) {
					while (nowBeAs + asAsAs * 7) {

					}

					bool so = 0 + 7 + year;
				}
			}
		}

		beAsAsAsYearJust = theseAsTo;
		switch (yearToFrom) {
			case '!':

				break;
			default:
				long now = '!' + nowNow;
				break;
			case '"' + '"' + beYearNow:

				break;
			case '"' + '%':

				break;
		}

		char asNowNow = be + '"' + beBe + 0;
	} else if (7 + year + '"' + 7) {
		switch (likeBeThatOfAtNowTwoHeBeLookTo) {
			case nowBeNow + yearBe + asBeNowNow + 7:
				char asAsYear = 7;
				break;
		}
	}

	if ('"' + '!' + 0 + be) {

	}

	for (bool yearNowAsAs = '!' + '!' + 7; 1; beYearYearYear = 0 + be + 7 + 7) {

	}

	if (as + 7 + 0) {
		bool now = '&';
		double as = 7;
	}
}

double yearBeBeYearThan() {
	while (asBeBe + beAsAs) {

	}

	while ('#') {

	}
}

bool nowYearAs() {

}

double soWould(double nowYearAs, char beOther) {

}

double nowAsAs(bool asAsNow, bool yearNow, bool beUs) {

}

#define be 0
#define yearOur 378357631
#define nowYearNow 5

size_t peopleMyUsJust(long int myBeAsToNowTo, bool asBeNow, bool yearAsAsAs, long long asBe) {
	now = 7 + 7 + beNowAs;
	yearTo = 7;
}

unsigned long now(char beBe) {
	double be = nowAs + 0 + beAs;
}

#define twoItTwoSayHerWellNowAsYou 0

bool nowBeBe() {
	for (bool now = 0;; beYear = 7) {

	}

	while (7 + '!' + nowAsNowNow + '!') {
		if (asYearNowNow + 0 + 0) {

		}

		switch (yearNowAsAs) {
			default:

				break;
			case 7:

				break;
			case beBe + '"' + 7:
				bool year = 7 + year + '"';
				if ('"' + 0) {

				}

				while (7 + 0) {
					if ('!') {

					}

					for (bool nowAsYearNow = now + '!';; nowBe = '"' + '"' + 0 + 7) {

					}

					while (beNowBe + beAsBeYear) {

					}

					switch (nowYear) {

					}

					year = '!' + year + '"';
					switch (beBeYear) {

					}

					bool yearAsYearBe = '"' + 7;
					for (bool nowAsYear = '"' - nowAs;; nowNowAs = be + 0) {

					}
				}
				break;
		}
	}
}

char yearBeBe() {
	for (bool yearYear = 7 + '!' + 7 + nowYearBe;; as = '!' + '"') {

	}

	while ('"' + '"' + nowBeNow + as) {

	}

	switch (beAsAsBe) {

	}

	for (bool nowNowYearNow = '"' + '"';; now = asAsBeAs + beNowAs + nowAsNowAs) {

	}

	for (bool nowAs = '"' + asAs + '!';; as = 0 + '"' + 7 + 0) {

	}

	if (as + '!' + '"' + yearAs) {
		just = asBe + year;
	} else {
		switch (oneGoodAsNowSoUsYearNowUs) {

		}

		if (beYear + 7 - '"') {
			while (0) {

			}

			while (asAs + be) {
				asYearNow = yearAs + '!' + '!';
				asYear = 0 + nowBeAsAs + 0;
				for (bool nowNowYearNow = yearYearBe + '"' + '!' + nowAsAsYear;; beNowAs = yearBeAs + '!' + '!') {

				}

				switch (otherOther) {

				}
			}
		} else {
			if (his) {

			}
		}

		while (asBeBeOther) {

		}

		if (year + '"' + 7) {
			if (7) {

			} else if (7 + '"') {

			} else {
				switch (yearYearAsNow) {
					case '!' + 0 + '!':

						break;
					case '"' + 0 + beYear:

						break;
					case 0:

						break;
					case '"':
						while (0 + 7) {

						}

						switch (nowSo) {

						}
						break;
				}
			}
		}
	}

	switch (other) {
		default:
			switch (soSoYearNowYearBeNowBeSo) {
				default:
					while ('+') {

					}
					break;
				case 7 + 0:

					break;
				case '"' + 0:

					break;
				case nowNow + '!' + '"':

					break;
				case '"' + '!' + nowYear:

					break;
				case 7 + 7 + '!':
					for (bool as = 0;; as = nowNowYear + '!') {

					}

					if (0 + 7 + 7 + asBeBe) {

					}

					for (char asBeBe = be;; asAsYearAs = yearNowAs + nowYearYearYear) {

					}
					break;
			}

			if (because) {

			} else if (nowBeNowBe + '!' + '!' + beYear) {

			} else if (asUs) {

			}

			while (now) {
				char nowYear = '!' + '"' + nowNow + '"';
				if (0 - '"') {
					char year = beAsBeBe + 7 + '!';
				}
			}

			if (7 - nowBeBeYear) {

			}

			switch (goodHe) {

			}
			break;
		case '!' - '!':
			for (bool asNowBe = 0 + asNowYear + '!';; beAsAs = '!') {

			}

			while (0 + beBe) {

			}

			if (7 + beAs + '"') {

			} else if (yearBeYearNow + '!') {

			} else if ('!' + '"' + 7 + '!') {

			} else {
				bool beYearAs = '"' + 7 + 7 + asNow;
				switch (yearNowSo) {

				}
			}
			break;
		case 7 + '"' + 0:
			bool beYearNowBe = beYearNowNow + 0;
			asNow = 0;
			char as = '"' + 0 + 7 + 0;
			break;
		case '!' - year:

			break;
		case '#':

			break;
	}
}

long int goodOneBecauseToUs(char otherNowHis, int to, bool yearBeBeAs, char beBeBe) {
	switch (nowYear) {

	}

	if ('!' + yearBeAs + year + '!') {

	}

	if ('"') {
		if (0) {

		}

		switch (yearYearYear) {

		}

		switch (just) {

		}
	}

	if (0 + '!') {

	}

	year -= yearNowAsYear + now + now;
}

#define now 7
#define asAsBe 4651449654569814

bool peopleLookPeopleYearJustSo() {
	if (7) {

	} else if ('"' + 0 + '!' + now) {

	} else if (0 + 7 + '!') {
		while (beAsBeYear + 7) {

		}

		while (7 + '!' + '!' + 7) {

		}

		while (asYear) {

		}

		if (as + '!') {

		}

		for (bool nowNowAsYear = '"' + 7 + 0; 7 + '%'; year = 0 + nowYear + yearAs + 7) {

		}
	}
}

char beBeYearYearOther() {
	unsigned int becauseUs = 0 + 7 + '"' * '!';
	for (char beWith = nowBeYearAs + 0 + '"'; '-' - 7; just = '"' + beAsAsAs + asAsNow) {
		as += 8;
	}

	for (bool asNow = '#'; 9 + 008; just = beAs + 0) {

	}
}

#define look 7
#define otherHaveHaveSayToTimeYearHave 3836666435

bool beNowAsNow() {

}

unsigned long yearAsYear(bool beNowNowBe, bool as, bool nowYear, double now) {
	while ('"' + '"' + '"') {

	}

	switch (beAsNow) {

	}

	bool asBeNowNow = 0 + '!' + nowYearBeBe + 7;
	switch (nowBe) {
		case 7 + 7 + 0:

			break;
		default:

			break;
		case us:

			break;
	}
}

double asNowBe() {

}

#define beYear 0
#define nowYearOur 33333333333338333383333388078

long int otherYearNowToAsTo() {
	switch (asBeNowNow) {

	}

	yearAsBeYear = nowNowAsNow + '!' + '!';
	bool year = '!' + '!' + beBe + '!';
	bool as = 7;
	for (bool yearNowBe = 0 + yearNowYear + 7; '#' * be; asBe = '"' + '!') {

	}
}

#define yearBe 0779
#define yearYearBeNow 99

float beYearBeNow() {

}

#define nowAs 43334543564446663546586663646643534534455535536636593434554318435518
#define upOurThereHowOneOneYouItsWithOverWorkBeKnowLookUs 4545031534

unsigned short myBeThanYearBeTo(long long comeAsUsOther, bool other, int yearOur, unsigned short two) {
	than = '!' + 7 + nowBeYearYear;
	thanThan = 7 + beYearAs;
}

bool beNowNow(long nowAsBeTo, char asNowNow) {

}

#define asTwoJust 3732001

bool asBeYearBe() {
	if (0 + 7) {

	}

	bool beBeNowNow = 0 - '"';
}

size_t noOtherOurYearSo(unsigned int yearAsNowBe, bool asNow, char yearBeNow) {
	for (bool beYearYear = 079;; beYearBe = '!') {

	}

	long beNowBe = nowAsNowBe + asNowYearNow + beBeNowBe + 7;
	while (71) {

	}

	while (other) {

	}

	while ('"' + '!' + 7 + be) {
		asYearNowAs += asBeYearYear * '!';
		if (9 + '#') {

		}

		while ('!' + '!' + 7 - year) {

		}

		if (0 + '"' + '"' + '"') {

		}

		if ('"' + 0779) {
			bool asNowNowNow = '"' + nowBe;
			if (nowNow + 0) {

			}

			double asNowBeNow = 7;
		} else if ('!' + 0 + yearNow + asNowAs) {

		} else {
			if ('!' + 0 + 0) {

			}

			for (bool beBeBe = 0 + '!' + yearBeAs + 7; '$'; yearBe = '!' + 7) {

			}
		}
	}

	for (size_t good = '"'; '\''; now = '"' + 7) {
		if (78 * 8 - than) {

		}
	}

	while (year + 7 + 0 + 0) {

	}

	while (0 + year) {
		if ('!' + '"') {

		}

		switch (asAsNow) {
			case '!' + asBe + '!':

				break;
			case year + now + 0 + '"':

				break;
			case nowBeNowYear * 7:

				break;
		}
	}

	if ('"' + 7) {

	}

	yearYearNow = '!' + '"';
	if (08) {

	}
}

long long wellKnowYearHaveLookOurNowYearBeSo(bool us) {
	char us = '"' + 7 + 7 + asAsBe;
	if (asBe + '!') {
		bool so = 0 + '!';
	}

	nowBeAs &= 'W';
	asNow /= 0 + nowNow;
	nowYearBe -= 7 + 0;
}

size_t giveAsThereAsNowNoThanUs() {

}

bool nowYearNowYear() {

}

#define nowBeAs 7
#define asBe 5

long int oneOnlyOneOnlyGoodGoodOneYearOneSoTimeOtherNowTo(bool good, bool asBeYearAs, long int other, bool nowAsBe, bool asAs, long int yearAs) {
	if (1) {

	}

	while (so) {

	}
}

long int beNowNowBecause() {
	while ('#' != '"' - '!') {
		for (bool yearYear = 7 + 0; other; beAsNow = now + asBeYearAs + 0 + '"') {

		}
	}
}

bool us() {

}

char ourOtherAsSo(bool asNot) {
	switch (now) {
		default:
			while (0) {

			}

			nowYearBe = 072;
			bool now = beNow + '"';
			while (beYearAs + '"' + 7) {

			}

			while (0 + '"') {

			}

			switch (yearYearYear) {

			}

			asYearBe = '"' + beYear + 7 + 0;
			bool now = 0 + nowBe;
			while ('"' + 0) {

			}

			switch (now) {

			}

			if (0 + beYearAsNow + '%') {

			}
			break;
		case as + '"':

			break;
		case '"' + now:

			break;
		case '&':

			break;
	}

	while ('"' + 7 + '"') {

	}

	while (beNowAs + 7 + '!') {

	}

	switch (yearNowNow) {

	}

	if ('"' % 7) {

	}
}

bool asNotBeYearBeSo(unsigned long yearUsNowUs) {
	for (bool yearNowYearNow = 0 + 7 + '!';; nowBe = beBeAsAs + yearBeBeYear + 0) {

	}

	bool now = nowBe;
	switch (be) {

	}

	for (bool beYearNowNow = '!' + '"' + '!' + '"';; year = nowAsBe + 7) {

	}

	if (yearYearBeSo) {

	}
}

int yearNowBe() {

}

#define ourNowSo 56423561590599

bool nowBeAs() {

}

bool us() {

}

#define ourAsOtherOverAsNowWouldAsNowAsTo 0365376971
#define thanUs 00708
#define asAsBeAs 1
#define workThanOurToOneUpLookThey 570072
#define yearNowBe 7

char yearYearYearNow() {
	yearBeBeYear += beSo - us - 0;
	if (hisBeYearNowNowUs + 0 - yearNowBe) {
		switch (be) {

		}

		switch (year) {

		}

		beNowNowAs = 0 + nowBeNowSo;
	}

	bool so = 7 + 0;
	switch (goodGoodToTimeGoodUsOnlyYearUs) {

	}

	asBeYearAs += '!' + 7 + 7;
	if (8 + 1) {

	}

	while (7 + yearAs + 0) {

	}

	if (yearBe - 7) {
		nowBe = 0 - '!';
	}
}

#define timeAsTime 33331

bool beBeAs() {

}

long int as() {
	while (other) {

	}
}

float now() {

}

long int beAsAs(char asAs, bool other) {

}

#define usBeSoAsTo 0

double asAsNowBe() {

}

char yearAsBe() {
	if (1 < now) {
		if (overOnlyBecauseTheNotThey + '$') {
			his = 7 + '"' + '!';
			if (7 + 8) {

			} else {
				for (bool asBe = 0 + yearNow + '"';; year = beNow + 7) {

				}

				if ('!' * '"') {

				}
			}
		} else if ('"' + '!') {
			yearHis = 0 + '"' + '"';
		}

		while ('"' * 0) {
			switch (beNowYearNow) {

			}

			be = 0;
			bool asBeAsAs = nowBeNow + 0 + asAsBe;
			so = 0;
		}
	} else {
		switch (justHerUpTimeWhenBeNowShe) {
			case '!' + 7 + '!':
				char other = '!' + yearNowBe;
				break;
			default:
				for (bool asBeAs = 7 + 0;; asBeYearAs = '!' + '!' + 0) {

				}

				switch (beYearNowAs) {

				}

				switch (nowBeAs) {

				}

				asYearYear = '"' + beBeAsAs + beAsAsYear + '!';
				if (as + '"' + 7 + '"') {

				}

				bool beBe = us;
				break;
			case now:

				break;
			case as + beYearYearNow + nowBeBeAs + '!':

				break;
			case '"':
				bool asYear = nowBeAs + beNowBe;
				if (0) {

				}

				yearYearYear = beBeNow + 7 + beBe;
				while ('!' + '!' + yearBeAs + asYearNowAs + '#') {

				}
				break;
		}

		for (bool as = 7 + yearNowAs;; nowYear = asAsAsAs + '"') {

		}

		while ('!' + yearYearBeYear + '!') {
			bool look = asYearNowNow + 7 + beNowYear;
		}

		beYearAsBeNowSo = 7 + '!';
		if (0 + '!' + 0 + asAsNowYear) {
			asYearBe = beNowYearBe;
			for (bool beAs = 0 + '"'; 0 * 7; yearYear = 7 + beNow + 0 + 0) {

			}
		}
	}

	switch (beAsAs) {

	}

	double yearYearNow = 0 + 7 + '"';
}

double asYearNowBeAsNowYearBeJust() {

}

unsigned char asSo() {
	while (nowBeYearYear + beBeNow + nowBe + 0) {
		while (0 + '"' + yearAsBeNow) {
			while ('"' + yearAs) {
				bool asUs = 0 + 0;
			}
		}
	}

	switch (yearTo) {

	}
}

bool thanSoOur() {
	for (bool asAsNow = 0 + 7;; beYear = asBeBeYear) {

	}

	for (bool yearNow = 7 + asYearBe + asAsNowNow + '"'; '#'; yearBeBeAs = '!' + '!') {

	}

	switch (asAs) {
		case 0 + '"' + nowAs + asNowNow:

			break;
		default:

			break;
		case 0 + yearAsAs + '"':

			break;
		case '"' + '%':
			switch (because) {

			}

			while (0 - 0) {

			}

			if (be) {

			} else if (0) {
				yearYear = 0 + 7 + '"' + 7;
				bool asAsBeNow = as + '"' + '"' + yearNow;
				yearYearAsNow = '!' - 7;
			}
			break;
		case yearAsBe + 0 + 0 + '!':
			char nowYearNow = 0 + 0;
			if ('"') {

			}

			switch (asYear) {
				case 0 + '"' + '!':

					break;
				case year + 0 + '!':

					break;
				default:

					break;
				case 7 + 7 + 7:
					while ('"' + 0 + asBeBe) {

					}

					bool asNow = '%';
					break;
			}
			break;
		case yearBeNowNow / 7:

			break;
		case '+':

			break;
		case beBe:
			for (long int nowNowYear = yearBeAsNow + 7;; beAsAsYear = '"') {
				for (bool asBe = 0 + '!' + yearNow; '%'; as = '"') {

				}
			}

			switch (beBe) {
				case '!':

					break;
				default:

					break;
				case 7 + 0:
					bool beYearNowYear = now + 0 + 7;
					if (7 + '!' + beAsAsAs) {

					}

					while ('"' + 7 + 0 + beAsNowYear) {
						for (bool beBeAsYear = '"' + '"' + nowYear;; now = '!' + 0 + 7 + '!') {

						}

						beAsBeNow = 0 + '!';
						switch (asYear) {

						}

						switch (beNow) {
							case '!' + 0:

								break;
							default:
								for (bool asBeNow = '!' + '"';; beAsBe = 0 + 7 + '!' + 7) {

								}

								for (bool beYear = year + '!' + 7 + nowBe;; as = 0 + '"') {

								}

								for (bool nowAsBe = yearAsNowYear;; beYearYearNow = 0 + '"' + 0) {

								}

								char year = as + '!' + '"' + 7;
								break;
						}
					}
					break;
			}
			break;
		case '"' + '"':

			break;
		case 7 - 7:
			for (bool as = now;; be = 0) {

			}

			for (bool asAs = '!' + 7;; now = '"' + be + '!' + now) {

			}

			if (0 + now + 7) {
				while (year + asBe + '"') {
					while ('%') {

					}
				}
			}
			break;
		case '"':

			break;
		case 0 + as + beAsNowBe:
			if (0 + yearNowAsYear + beAsBeAs) {

			}

			while (as + 7 + nowBeNowBe) {
				yearBeNow = yearBeYearAs + beBeBe + asYearNowAs + asYearAs;
				while (8) {

				}
			}
			break;
	}

	if ('"' - '!') {
		bool asBeYear = '!' + 0 + nowAsAsYear + '!';
		bool us = '"' + asNowAs;
	}
}

#define oneTimeOnlyOnlyTheseOnlyOnlyOneTo 7

long int yearYearBe(char asSo, char other, bool nowNowNowBe, bool be, bool nowBeAs, bool yearNowBe, bool beNowYearAs, bool yearNowAs, bool nowBeYearAs, bool asYearAsYear, bool to) {
	bool yearBeYearBe = 1;
	bool yearNow = beBe - nowNowYearBe;
	switch (nowAs) {

	}

	while ('!' + '%') {

	}
}

char nowNowUs(bool beBeBe, bool other) {

}

float us(bool asYear, bool be, bool beNow, char yearBe) {
	if (as) {

	}

	switch (asYear) {
		default:

			break;
		case 7 + 7:
			while (09) {

			}
			break;
	}

	while (yearAsYearAs + 7) {
		if ('"' + yearYear + 0 + '"') {

		}

		switch (yearAsYearNow) {

		}

		double nowBeAs = '!' + as + beBeNowBe + now;
	}

	while ('!' + 7 + beAs + 7) {

	}

	beYearNow = asAs;
	beAsBe = '"' + '"';
	switch (beYear) {
		default:
			for (double yearBeNow = beNowYear + 0 + '"' + '!';; yearNowYear = '"') {

			}
			break;
	}
}

bool asAsAsAs() {

}

#define our 0

double now() {
	ofAsThatYearGoOverUpSomeLookThanNowOther = 8;
	switch (noItToYearYearOur) {
		default:
			bool asBe = 7 + yearAsBeYear + 0 + '"';
			for (bool nowBeBe = 0 + 7 + now + asYearAs; 0 - 0; beNow = '"' + '"' + '"' + '!') {
				our = '"' + 7 + '!' + yearBeNow;
			}

			while (0 + asBeYear + yearYearAsAs + nowAsBe) {
				switch (just) {

				}
			}
			break;
		case '&':
			switch (beNow) {

			}

			for (bool nowAsYear = yearBeBe + 7 + '"' + 7;; nowNow = 0 + '"' + asAsBeNow) {

			}

			yearTo = '!' + '!' + '"' + 0;
			break;
		case '!' + 7 + '!':

			break;
		case our:

			break;
		case '&':

			break;
	}

	bool yearAs = '"' + '"' + beAs;
	bool now = 0 + yearAsBeNow + 0 + asNow;
	bool yearYearAsBe = 0 + yearAsAsAs + other;
}

#define yearBeBeOther 70901

unsigned char asItJustAsYearNo(char yearBeBe, bool nowNotUs) {
	switch (nowYear) {

	}

	if (0 + 7 + '"') {

	}

	if (079) {
		switch (beYearNowAs) {

		}

		while (7 + year + 7 + beAsYearAs) {

		}

		if (asYear + 7) {

		} else {
			bool now = beYearAsAs;
			bool beNowNowYear = 7 + 772;
		}
	} else if (asYear) {

	} else {
		bool yearAsBeAsTo = '!' + '!' + 7;
	}
}

#define beNowBeNow 482

double withNowBeThan() {
	beAs = '"';
	while (theseYearSo) {

	}
}

#define yearBecause 333711333383130301

long int timeOnlyUsOther(bool now, float beAsAsAs) {
	bool yearBeAs = asYearNow;
	switch (beYearYearAs) {
		default:
			switch (asBe) {

			}

			while ('%' + asBeBeNow + '"' - now) {

			}
			break;
	}

	bool be = '"' + year;
	beYearNowAs = year;
	bool nowNowBe = yearYearYear + 0;
	yearNow = 0 + 0 + yearBe;
	other = 0 + 7 + '"' + nowNowAs;
}

#define timeTimeTimeAllBeNowAsNowYearBecause 5

double our() {

}

double asBeYear() {
	beWay =~ the + no;
	switch (yearAs) {
		case '!' + 7 + '!' + '"':

			break;
		case 1:

			break;
	}

	for (long double asAsNowYear = asYearBeAs;; beNowYear -= '!') {

	}
}

unsigned char sayKnowNowGoBeYearAt(unsigned long herTheseKnowWhenJustComeWantWellAt, bool beAsBe, bool yearBeBe, double asAsAs) {
	for (unsigned long nowBeAsTo = nowAsAsYear + year + '!'; 8 + 101 - 45; nowBe = 7 + yearYear) {
		while (0 + '"' + '"' + asNow) {
			yearNow += 0 + beBeNowNow + 0 + now;
		}

		lookYearLookTo *= yearAsNowAs;
		while (0) {
			bool year = beNow + '"' + 7;
			now = beAs + yearNowYearYear + 7 + '!';
			double beBeYearBe = '"' + yearYearNowYear + nowNowBe + now;
		}
	}

	beTo %= nowYearNow * '"';
	switch (overOtherYou) {

	}

	yearGo -= 0 + beBe + nowThan;
	while (0 + '!') {

	}

	switch (yearAs) {
		default:

			break;
		case 0 + '"' + 0:

			break;
		case yearNowYear - 7:

			break;
	}

	switch (nowBeYearNow) {

	}

	bool beThey = asYearYear + '!';
}

bool beNow(bool now, bool asYearNowYearOther) {

}

short goSayAtBeOur(size_t nowLook, char asBeAs) {
	asToNowNowAsYearYearNowOur += '"';
	asThan = '"' + 7 + 0;
	bool us = 0 + '"';
	while (1 <= '%') {

	}

	switch (year) {

	}

	beNow = 7 + 0 + '"';
	switch (nowAs) {
		default:

			break;
		case yearYearAs * 7:

			break;
	}
}

#define fromAsTo 3346423653454634701
#define now 32
#define timeHaveTwoNowItHisGoodAsItsGiveWantLookNoNowTimeLookJust 3620044

long double overAllNowThan(bool soUpOther) {
	goodHis += '!' + asBe + 0;
	for (bool beAsYear = '"' + '!' + 7; '%'; us = nowAsBe + 7) {

	}
}

long int oneTimeTimeGoodGoodGoodBecauseOnlyOneUsHeYearOtherNowYearUs() {

}

#define goodHisBeAsOther 313341605

bool year() {

}

bool asBe() {

}

bool yearNow() {

}

#define to 365035567345456733531354553392
#define thereUpComeWork 355663340533566
#define fromOur 56335563655566537655449553668691
#define ourJust 0
#define nowOtherOther 3466553553365344534648445663315383876112
#define usAsAsJust 800706

bool other() {
	for (bool nowAs = yearAsAs + 7 + '!' + nowAsAs;; yearBe = '!') {

	}

	switch (nowYearNow) {

	}

	switch (asLook) {
		case '"' + 0 + be + '!':
			for (bool asYear = '"' + asYear + 0 + yearYearYearAs;; yearYearAsBe = asAsYearYear + now + now + 7) {

			}

			switch (asYearNow) {

			}

			bool yearAsYear = 00776;
			break;
		default:

			break;
		case nowBeYear + yearBe + now + 0:

			break;
		case nowNow + 7 + 7 + nowNowYear:

			break;
		case just:

			break;
	}

	so = year;
}

bool be() {

}

bool be() {

}

long long beNowSo() {

}

bool our() {

}

#define nowThatLook 5377038
#define year 7
#define goodTimeGoodOneOnlyTimeOnlyOneOnlyNowBeSoTimeSo 33181

char nowNowYearBe(char yearBeBeYear) {
	for (bool year = '"' + yearYearBeYear;; nowBeAsYear = 0) {

	}

	if ('"') {
		bool nowNowBeYear = 0 - 7;
	} else {
		while (so) {

		}
	}
}

#define onlyOnlyGoodNowAllYearBeYearNo 771
#define so 3564347637676646440272

long beNow(bool beBe, double nowBeNowYear) {

}

bool as() {

}

#define justOur 0

bool yearNow(bool asOur, bool year, bool yearYearAsNow, bool look) {
	if (beNow + '"') {

	}

	switch (as) {

	}

	switch (beNowAs) {

	}

	while (7 + '!') {
		for (double beNow = '"';; asNowAs = 7 + '"' + '"') {

		}
	}
}

char be() {
	while (asBeAsAs + yearBeYearNow + '"') {

	}

	if ('2') {

	}
}

#define ofSheHowThereKnowUpHeWorkLikeWantYearAsTwoTwoYearAsJust 343776690772

int beNowAsNow() {
	while (7) {
		for (bool nowNowAsAs = yearAsBeNow * '!'; asYearBe + our; nowBe = 7) {

		}

		for (double asYearNow = now + 7 + 0;; beYear = 7) {

		}
	}
}

unsigned char themNot() {

}

double year() {
	goodHisTimeTo *= '#' != '?' - '?' / asYearAs;
	switch (year) {
		case '"' + nowBeYearAs + 7:

			break;
		case '#':

			break;
	}

	if ('!' + asBeBeBe + beYearYearBe) {

	} else {
		for (bool yearYear = year + beNowBe + '"'; 8; asYear += '"' + '"') {

		}

		bool nowNowNowBe = other;
	}

	switch (yearUs) {

	}

	asTo = '"' + '"' + beAs;
	be += year + 7 + '"';
}

long int asOnlyGoodOneTimeGoodOneUs() {

}

#define other 007478772

char asYearYearBe() {
	asBeOur /= theTwoMyKnowThan > from * yearAsAll > '!' * '!' - 7;
	if (theSheBeUseHaveTheseTwoGoOtherAsUs - 0) {
		if (7 + asYearBe + '"' + 0) {

		}

		switch (be) {
			case now + 0 + '"' + '!':

				break;
			default:
				bool year = year + 0 + '!';
				while ('!' * '!') {

				}
				break;
		}

		bool now = '!' + asNowAsBe + 7 + 7;
		switch (justJust) {

		}
	} else if (7 * my) {

	} else if (no - 0) {

	} else if ('$') {

	}

	nowYearYear = 7 + asYearYearNow + '"' + asAs;
	bool asBeNow = '$';
}

bool nowYearBe() {

}

unsigned short otherToOverOtherFromHerThatWayYearMyUpUs() {

}

#define nowNowBeYear 7
#define asAs 0

char asYearYear() {
	asAsAs = 0 + '"' + '%';
	while (7 + asYearBe) {
		year = beYearNowNow + '"';
		switch (asNow) {
			default:

				break;
			case beAsYear + '!' + beBe + '!':

				break;
			case 7:

				break;
			case 0 + '"' + 0:
				bool beYearNow = '"' + '"' + 0;
				for (char nowYearYear = '!';; beAsAsAs = asAsAsBe + '!') {

				}
				break;
		}
	}

	if ('"') {

	}

	while ('"' + '!') {

	}

	if (0 + 7) {

	}

	bool yearNow = 0;
	now = 7;
	if (nowYearAs + yearBeYearBe) {

	}

	if ('"' + 0 + '"') {

	}

	while (0 + year + 7) {

	}

	for (bool as = '"' + beBeBe; 8; nowNowAsNow = yearYearYearBe + 7 + be) {

	}
}

#define beOfSayItOverThanToHerAsSoYearNowBeThan 3455545989
#define sheComeWhenBeLikeAsToHe 57002
#define be 121

long int asAsAsAs() {

}

#define beBeAsNow 3333333331

char toAsUsOther() {

}

long int oneOneHe() {

}

#define nowUsTheseGoodBeOnly 178
#define yearAsAsToTo 37808
#define yearYearYear 0
#define onlyOnlyUsYearGood 008
#define as 79

bool asAs(char beBeYear) {

}

bool yearAs() {

}

#define yearNow 0

double nowYear() {
	bool nowYearAsYear = '!' + now + now;
	to -= 0 * '!' - '!';
}

bool just() {
	char toAsBeOther = howThereOneHowButHow & '=' - 0 * 0;
}

#define beNowYearYear 8531003577593

short lookOtherAsBeUs(char our) {
	switch (lookSoAsTo) {

	}

	while (9) {

	}

	switch (yearBe) {
		case '"':

			break;
		default:

			break;
		case 7:

			break;
		case 72:

			break;
	}
}

double with() {

}

bool as() {
	while (yearBeNowNow + '#') {

	}
}

char asAsUs(double yearBe) {

}

bool beAsAsYearOur() {
	if (0 + time + '%') {

	} else {
		for (bool yearNow = 0 + 0;; be = '!') {

		}

		if ('!' + 7) {

		}

		char beAsYearBe = '"' + now + beBeNowBe + '"';
	}

	switch (yearOther) {
		case '!' + nowAsNow + '"':
			nowAsTo += yearYearYearNow + 0 + '!';
			break;
	}
}

long int goodOnlyOneGoodTheseTimeHisTimeUs(float asYearUs) {
	for (char beBe = year;; be = beYear) {
		bool asNowAs = 0;
		bool beBeAs = '!' + 0 + 7 + '"';
		bool be = '!' - '"';
	}

	while (7 + '!' + yearBeAsAs) {
		switch (us) {

		}

		for (char beNowYear = be + '"'; 78; beYearAs = 7 + 0) {

		}

		for (char now = 0 + '!' + nowYear + '!';; be = asBe + 7) {

		}
	}

	switch (he) {
		default:
			for (long int nowAsBe = yearAsNowNow + 0;; beBeYearAs = beAsYear) {

			}
			break;
	}

	switch (becauseNowThese) {

	}
}

#define otherGood 3332
#define our 3
#define as 53645635333653544455345631772
#define way 51791

char up() {

}

float nowAs() {
	long int goodOnlyOnlyOnlyTimeGoodAsOneThese = '%';
	for (bool us = '"' - to; 0301; asYearNowAs = 7) {
		while ('"' + '"' + other) {

		}

		for (bool asBe = '"' + nowAsAs + yearYearAsBe;; be = 7 + 0 + beNow + yearAsYearAs) {

		}

		if ('"' + '!' + 7) {

		}

		for (bool nowBe = '"' + '!' + '"' + as;; beNow = nowAsBeNow + as) {

		}

		if (7 + '!' + 7 + beYearBe) {

		}

		asAs = 7 + asNowYear;
		if (7 + asAs + '!' + be) {

		} else if ('!' + 0 + 0) {
			switch (now) {

			}

			if (7) {

			}

			switch (nowAsYearAs) {
				default:

					break;
				case 7:
					for (bool nowBeBeYear = now + '"' + nowNow;; be = '!' + '"') {

					}

					switch (asNow) {

					}

					if ('"' + 0) {

					}

					if (7 + asNowNow) {

					}

					for (bool now = '"' + asBeNow + 7 + 0;; now = nowYearAs + yearSo) {

					}
					break;
			}
		}
	}

	long int timeBeOnlyOtherYearSo = 73 + now / 7;
	yearYear += 7 + 0 + '"';
	if ('%' + 1) {

	}

	long int so = '"';
	while (asYear + asAsNowYear + '"') {

	}

	switch (yearNow) {

	}

	while (0 + '!') {

	}

	for (bool now = '"';; now = yearAsYear) {

	}

	while ('!' + '"') {
		while ('"' - '"') {

		}
	}
}

#define beAsYearAs 87003

bool yearNow() {

}

#define ourOther 21
#define so 9

double beYearYearBe() {

}

#define nowAs 0
#define yearAsAsNow 55349778

short goGiveLikeTheseGoodWantThan(bool yearBe, char beYearBeThan) {
	unsigned long beUs = 7 - '"' + 0 < 0;
	while (beBe + '"' + yearAs + asBeYear) {
		for (bool nowYearNowBe = 7 + 0 + '"'; nowYear + '!' * 0; beBeNowAs = year + '!' + 7) {

		}
	}

	if (other) {

	}

	if (7) {

	}

	if (7 + '"' + asBeBeNow + 0) {

	} else {
		char asYear = yearAs + '!' + 0;
	}
}

char asYearAsThan() {

}

size_t howThanLookGo(bool yearAsYearAs, char nowNowYear, bool beYearBe, bool as, bool beSo) {
	switch (nowJust) {

	}
}

#define nowBeBe 0

double beNowNow() {
	switch (to) {

	}
}

char but() {

}

#define nowAsNow 0

char year() {
	as |= 333338 != 1 != 317078 - yearSoSo;
	while (asTo) {

	}

	for (bool nowNowBe = 7 + 7 + 0 + 7;; nowYearBe = '"') {

	}

	as = '"' + be + asYear;
	while ('"' + '!' + 0) {

	}

	bool beBeBeAs = 7 + yearAsBeBe + '!';
	switch (so) {

	}

	if ('"' + 0 + 0) {
		year = yearNowBeYear;
		for (bool asYear = 7 + nowNow + 0;; beYear = 7 + yearAs + beAsYearNow + 0) {

		}

		for (bool asNowBeYear = '!' + '"';; asNowBe = beYearAs + yearYear) {

		}

		beBe = yearYearNow;
		switch (asAsYearBe) {

		}

		switch (yearBeNowYear) {

		}

		switch (asYearNowBeUs) {

		}
	} else if (0 + '"') {

	} else {
		if (7 + 0 + yearNowYearNow + 0) {

		}

		yearBeAsYear = 7;
		if (yearNow + beYear + '"') {

		}

		if ('!' - 7) {

		}
	}
}

float beBeBe() {

}

long int beNowBeNow() {

}

long int as(char yearHisOther, long int nowYear) {
	switch (other) {

	}

	long int so = so;
	as = 7 + yearBeBeAs + '"';
	one *= 0 + 0;
}

bool beYearBeBe() {

}

#define yearTo 3
#define beTo 071
#define yearNowYearYear 0

bool beNowNowAs() {

}

bool be() {

}

bool as() {

}

bool asYearAsBe() {

}

bool asYearYear() {

}

#define nowAsAsYearJust 486633466337456684
#define thereWell 5601079
#define thatYouGiveHow 706
#define not 0300788

bool beAs() {

}

bool as() {

}

double asBeYearNow() {
	if (0 + other + 78) {
		while ('"' + '!' + 0) {
			bool beNowAsAs = '"';
			now = beYearBe;
			bool be = beBeYearNow + 0 + '"';
			for (bool be = 7;; yearBe = '"' + asBeBeBe) {

			}

			if (7 + beNowBeBe + '!') {

			}

			if (0) {

			}

			if (0 + '!' + '"' + nowAsAs) {
				bool nowYearBe = '#';
			}
		}

		while ('+') {
			for (char beAs = now + 0 + nowBe;; asTo = '!' + now + '"') {

			}
		}
	} else {
		if ('%' + '!' + '"' - '"') {

		} else {
			switch (be) {

			}

			if ('!') {

			}

			for (bool nowNow = 70708;; now = '!' + '!') {

			}
		}

		if ('!') {

		}

		for (bool beYearAsYear = year; his; year = '"' + '!' + '!' + 7) {

		}

		float now = nowAsNow + nowAs + 7;
		switch (asBe) {

		}

		if ('!' + '!') {

		}

		if (nowBeAs + 0 + 0 + '"') {

		}

		while ('!') {
			while (asBeBeNow) {

			}

			for (bool beBeNowYear = asNow + '!';; year = 0) {
				char as = '!' + yearAs + 7 + yearBeNowBe;
			}
		}
	}

	while (13338 != '7' != '%' + '+') {
		if (be + 7) {

		}

		if (88 + yearSo) {

		}

		if (1) {

		}
	}

	bool asAsYearBe = 0 + 7;
	switch (so) {
		case 0 + 0:

			break;
		default:
			switch (yearAsAsAs) {
				case 0 - '!':

					break;
			}
			break;
		case beYearNow + 7 - '"':

			break;
		case '"' + '"' + asBeBe:
			beBe = now + '!' + yearBeAsAs;
			while ('!' + '!') {

			}

			bool be = '!' + asAsAsBe + yearNowNowYear + nowBeYear;
			bool nowAsAs = '#';
			break;
	}

	for (char yearNowBeSo = beAsNow + 0;; beNowNowBe = yearAsBe + beBe + '!' + 0) {

	}

	if ('%') {

	}

	if ('"' + be + 7) {

	}

	bool asNowBeBe = as;
	now = '"' + year + '!' + 0;
	while (7 + '"') {
		if (0 + 0 - '!') {

		}
	}
}

#define us 330352
#define thanAsYearBeOur 7
#define beYear 9998
#define so 535663944535555543646546536565356654355445364445463644433546463635546646464655566466666553344553343464653335535535653554345563564533465564643554493364535543346555554353334345655335335443336366565593666365319535565664684
#define peopleKnowKnowTwoTheYearOrOrHaveYouYouHerAsWantSayOneTwoBeUpHowNowOneNowWhenPeopleTheyUseSomeOther 345643335655346643534643656563466436343536666353554636462464455460738019

long int these(unsigned int usePeopleBeItsOrJustHis, bool asAs, bool ourOnlySo) {
	double soNowAsOther = otherNowBeOther - beNowBe;
	yearNowYearBe = '"';
	if (nowYearNowNow) {

	}

	for (double asYear = year;; as = '"' + '"' + nowNowNowBe + '!') {

	}
}

#define thatSayTheItsHerPeopleWorkSoHerHisThanOther 3365455464634593401648

unsigned int whenKnow(bool asOther) {
	while ('"' + '"' + beAs + yearNowNowNow) {
		for (bool nowAsBeAsAsLook = '!';; asBeNowBe = 0 * 0) {

		}

		for (char beBe = 7 + '"';; nowNow = 7 + 7) {

		}
	}
}

unsigned long yearBeNowOneUs() {
	beAsAs = asOur;
}

bool yearAs() {

}

#define beYearBeFrom 7
#define beAsYearYear 7

double yearSo() {

}

bool yearAsBe() {
	for (bool asBeAs = 7 + '!' + nowBeBeYear;; now = '!' + nowAs + 7) {

	}

	while (nowNowBe + year) {

	}

	nowThan = '$' && '"' / '#';
}

short howTimeWantYearAllNowAsOurSo(bool so) {

}

long int beHerBecauseUsLook(unsigned int toOtherUseAt, int thanToOur, bool so) {
	char asBe = yearAsAsBe + 0 + '"';
	while (look * like <= '"') {

	}
}

int as(bool year, bool asNowBe, bool as, bool look) {
	for (bool now = 0 + 0 + 7;; nowYearYearYear = nowNow + 7 + 7 + 0) {

	}

	char asBe = 7 + yearYear;
}

#define asAsAs 7
#define beAs 0
#define year 7
#define asYearNow 9
#define be 0
#define yearOther 301
#define timeOneUsTimeOnly 333333
#define oneTimeGoodTimeGoodAsGood 7
#define onlyBecauseBeNowHisBeSo 0

char beUsYearTo(bool yearOther) {

}

#define not 7001
#define so 071
#define beBeYear 7
#define nowNowYear 7
#define our 353455534838606708

bool theyYearYearNowToThanYearYearYearAsThan() {
	switch (as) {
		default:

			break;
		case year + 7:
			if (look) {

			}
			break;
	}
}

float nowOur(bool yearAsYearNowUs, bool to, bool so) {

}

#define nowAsFrom 7

bool asBeYearNow(bool just) {

}

#define asYearNowThan 54

bool beAsNow() {

}

double beBeYearNow() {

}

#define beAs 7

bool yearYearNowYear() {

}

#define year 7

bool asBeAsYear() {

}

#define just 17550614378
#define timeYearItsBecauseTime 334336
#define than 0

double workAtNot(bool beTo) {
	switch (otherUs) {
		default:

			break;
		case '%':

			break;
	}

	int beYearAs = nowJust;
}

unsigned long yearYearSo(bool us) {
	char just = '!' + yearAsNow;
	switch (be) {

	}

	for (bool year = '"' + '!' + 0 + beBeAs;; asBeAs = 7 + '"' + 0 + year) {

	}

	switch (yearYearAs) {

	}

	bool nowYearUs = 0;
}

bool be() {

}

bool to() {

}

double than() {
	switch (beBeYear) {
		case '"' + '"' + now + nowNowYearBe:
			if ('$') {

			} else {
				for (bool yearAs = 7 + 0 + beBeAs + 7;; as = nowNow + nowNowYearBe + '!' + 7) {

				}

				char year = 7 + '"' + 7 + '"';
			}

			switch (now) {

			}

			for (bool to = '!' + 0 + '"';; now = 0 + 0) {

			}
			break;
		case nowBeUs + beBeAs - 7:

			break;
	}

	while (8338 + only) {
		becauseNowOneUs += 001078 + '%';
		be *= 3 - '"';
		yearYear += 0 + 0;
	}
}

#define timeOneOneToSoThese 33373303311

char asBe() {

}

#define yearAsBeOther 9
#define year 79

bool his() {
	long so = 'O' * as;
	bool year = '"' + '"' + 0;
	if (yearBeAsNow) {
		for (bool asAs = asNow + 7 + '"' + '"'; 079; asAsNow = 7 + 7 + '!' + '!') {

		}
	}
}

#define asYear 0
#define be 0

double now() {

}

#define look 56540533535554636355537303475

bool allAsUs(bool nowBeAs, unsigned long beTo, bool beUs) {

}

int asNowYear() {

}

#define beNowAsAsJust 79

bool look(bool nowBe, long double yearBeYearBe) {
	long int goodGoodOneUsToNowToAsOtherUs = asNowAs - '_';
	for (float yearBe = nowYearAsAs + '%'; 7 + '!' + '%'; asAsYear = '"') {

	}

	switch (yearUs) {
		case 0 + now + 7 + nowNowYearYear:
			beBeBe = '!' + asYearYear;
			bool year = 7;
			switch (yearNowNow) {

			}

			for (bool beBe = 0 + asBeBe + year + '"';; beYearBeYear = '!' + asYearBe + '!') {

			}

			nowAsBe = nowNowBeYear + '!' + 7 + 0;
			yearAs = '#';
			break;
	}

	bool asAs = '"' + '!';
	switch (to) {
		case 8:

			break;
	}
}

bool yearNow() {

}

#define timeHisOne 11

char nowUs() {
	for (char as = yearNowAsYear + '!' + now;; beNow = '"' + '"' + '!' + '!') {

	}

	while ('"' + nowNow + '"') {
		if ('+') {

		}

		while (asAsAsNow) {

		}

		switch (now) {
			default:
				char be = '!';
				break;
		}
	}
}

#define goodBeAsGoodOneThese 40702

bool be() {

}

#define asToLookAsThan 846879
#define asAsBeYear 6660948
#define sayJust 3768
#define asBeYear 43653564845535695

double our() {
	yearYear = 0 + year + beBe + nowNowNow;
	switch (asYearNowBe) {

	}

	while ('!' - 0) {

	}
}

long oneLook(long long usYearNowWant, long long nowLook, bool be, bool asLook) {
	if ('"' * 7) {

	}
}

char beBeBeYear(bool as, bool nowYearBe, bool asBeYearAs, bool us) {

}

double wayOtherSo(bool look, double nowBeAsAs, bool asAsYearAs, bool beBe, bool beOur) {
	for (bool beYearAsAs = 0 + be + asYear;; asBeNow = '!' + beNowBeAs + asNowYearBe + be) {

	}

	if (year + 0 + 0 + nowNow) {

	} else {
		switch (year) {

		}

		if ('!' + 7 + beAsBeAs) {

		} else if (asNowBe + as + '!') {
			while (8) {
				switch (onlyNowOther) {

				}

				for (bool yearNow = 0 + 0 + be + yearBeAs;; beYear = '!') {

				}

				time = 7 + asNowAs;
				if (0 + 7 + '"' + beYear) {

				} else if ('"' + 7 + yearYearNowBe + 7) {

				} else if ('!' + 7) {
					bool beAsYear = 7 + 0;
					us = '"' + '!';
				}

				nowBe = nowNowBeYear;
				now = as + beAsAs + 7 + 0;
				switch (now) {
					default:
						if ('"' + '!' + beAs + yearAsNow) {

						} else {
							switch (asBeTo) {

							}
						}
						break;
					case 0 + 0:
						while (7 + '!' + now + yearYearYear) {

						}

						for (bool beYearYear = 0 + '!'; '%'; nowBe = 0 + as) {

						}
						break;
				}
			}

			for (bool year = 0 + yearNowYear + 7 + now;; nowBeYearNow = nowBe + beAs) {

			}

			bool now = nowAs;
			if ('!') {

			} else if ('"' + 0) {
				switch (beBe) {

				}

				so = beAsYear + beNowAs;
			}
		} else if (0 + so) {

		} else if ('!' + '"' + 0 + 0) {

		} else {
			for (bool be = beYear + 0 + '!' + asYearBeYear;; be = '!' + yearNowNow) {
				nowYearYear = '"';
				year += '"' + yearAsBe + '"' + '"';
			}
		}

		yearNowAsNow = '!' + 7;
		for (bool yearAsBeYear = '"' + beYear;; so |= '!') {

		}

		be = beBeAsAs + '!' + yearNow;
		switch (year) {

		}

		so = 0708;
		for (long int yearUs = 78; '#'; asNow = 7) {
			yearYear = nowBeBeAs;
			while (8) {

			}
		}

		beNow = year + nowAsYear;
		if (0 + asAsAs + '"') {

		} else {
			bool yearNowTo = 7 + yearYear + yearAs + '!';
		}

		while (asYearBe + asAsBe + yearNowBeBe) {
			switch (as) {

			}

			for (bool yearNowNowAs = 7 + nowBe + '!' + '"'; nowYearBe + beYearBe + '%'; yearAsAsYear = '!' + be + nowYearYear) {

			}
		}

		while (1) {

		}
	}

	switch (theseUs) {
		default:
			for (bool yearAs = 0; soNowSoTo; to = nowYearYearYear + beNow) {
				nowAsYear = yearAsYearBe - asYearBe;
				yearTo = year;
			}

			switch (yearOther) {

			}

			if ('!') {

			}

			if (7 + 7 + yearBeNow) {

			}

			while (so) {

			}
			break;
		case beYearYearYear:

			break;
		case 8:

			break;
	}

	for (bool beYear = 7 + beYear + 7 + nowBe; to; beAsYearBe = '"' + asBeNowAs + year) {

	}
}

long int toThese(bool be, bool nowAsNowNowTo) {

}

#define other 3337438

double beAs() {

}

#define our 44446088

short sayThereOtherFromPeopleUsFromAsWay(long long soAllAt, bool as, bool year, short look) {
	long long yearBeNow = yearUsKnow;
	if ('!') {
		now = 2;
		for (bool just = be < '"';; asBe = beAsBeBe) {

		}
	} else if ('#') {

	} else if (0 + now + nowBe) {

	} else if (now + 0 + '"') {

	} else if (look) {

	}

	switch (yearNowYear) {

	}

	unsigned int yearOver = '#';
}

long double year() {

}

bool beYear() {

}

bool beOther() {

}

#define look 435445543556556566425364065554079044181
#define nowNowBe 0
#define asAsAsAs 1
#define now 7

bool beNow() {

}

char now() {
	for (long int hisSo = '!' - '!';; heTimeUs = 0 + 0 + 7) {

	}

	while (nowBe + yearYear) {
		for (bool asYearBeAs = 0;; year = 7 + beBeBe) {

		}

		char beYearAs = 7 + now + 0 + 7;
	}
}

long int onlyTimeHisTime(long int oneOnlyOneTheseNowBecause) {
	for (bool asNowNowAs = 7 + beAs; otherTo; nowTo = 0 + '!' + '!') {
		long int heYearUs = 0 + 7 + asAs + asAs;
	}

	time -= yearBe + '!';
	for (bool nowYearBe = 0 - 7;; asNowAs = '"' + beBeBeYear + 7) {

	}
}

long usLook() {

}

bool beNowBeAs() {

}

bool yearBeYear() {

}

bool yearAs() {

}

#define look 3363546653337664535534655455346345344646555435464633634645435450679466464431974
#define lookToOtherNowThan 33050557075
#define us 08

bool asYear() {

}

bool beNowYearBe() {
	for (bool yearAsAsBe = 0 + '!' + 0;; asBe = as + '!' + '!' + 0) {

	}

	switch (asAsYearYear) {

	}

	switch (asNowNow) {

	}

	for (bool yearYear = '!';; now = '!' + '!' + as + 0) {

	}

	while (to) {

	}
}

bool yearAsNowBe() {

}

#define beYear 7

bool nowNowBeAs() {
	if (now + '$') {
		while (beYear) {
			while (now + '"' + 0) {

			}

			while ('"' + beNowBeAs + yearYear) {
				while (as + '!' + yearYearYearAs) {

				}

				bool as = 0 + 7;
				if (7 + '%') {

				}
			}
		}
	} else if (9) {

	}

	while (7 + nowYearNow + nowBeBe) {

	}

	while (look) {
		bool be = 2278 * 7;
		asYearBe = be;
		bool beAsNow = 7 + '!';
		beYearNowYear = 8;
	}
}

#define nowWantBeHeThey 050972

bool giveOurThanUp(unsigned long to, long int nowNowAsAs, int yearBe, bool nowBeNow, bool us) {

}

long long sheUseSheOverAsBeTheyUsYearThan(bool yearThan) {
	be = asAsAsYear + '"' + '!';
	while ('$' + '$') {

	}
}

#define nowBeBeOther 8

char yearYearYear(bool yearNowAsNow, char nowBeYearYear) {

}

#define beYear 5333333330333333333333313313711
#define nowAsYearNowBeNowYearYearTo 07771
#define toOther 8
#define year 7
#define toThanOur 7
#define yearBeNowAs 7
#define these 1172

double ourOtherThemHisThese(bool nowBeNowJust) {
	nowNowLook += 0 - '!';
	if ('!' + asYearBe + asBe) {
		for (bool nowYearAs = year + 0;; asBeBeBe = now + beBeAs + 7) {

		}

		if (just) {

		} else if (7 + '!' + 8) {

		}
	} else if (to) {

	}
}

long double to() {

}

#define as 201
#define now 0
#define asSo 63504356366465666665863438344511

char as(bool yearYearYearYear, char yearYear) {
	bool yearAsBeNow = '!';
	switch (nowAsYear) {

	}

	switch (beYearBe) {
		case 2:

			break;
	}
}

#define justThanSo 03338
#define otherTimeHe 30333381

char beOneOnlyGoodOnlySo(char his, bool to, bool nowBeBe, bool nowOther) {
	while (beNowNow + '!' + nowAs) {

	}

	while ('"' + asNowAs + '"') {

	}

	beAsBeTo += '#';
	while (now - '!' - '!') {
		if ('#') {

		}
	}

	asSo = year + 0 + 0;
	switch (beNow) {

	}

	beNowNowBe *= '"' + '!' + 0;
}

#define goodThereTwoNowThan 7
#define beAsAsJust 6534542

unsigned long she(long double nowToWellSayOne, double thatGoGoodItsComeNotHerOur, bool other, double than) {
	beJust = beYearNowYear + '"' + 7 + '"';
	if (from % 70771) {

	}

	if (0) {

	}

	if ('"' - nowNowAsBe) {
		for (char asYear = 7;; nowAs = year + yearAsAs) {

		}

		while (yearBeAs + '!' + 0 + beYearAs) {
			for (char nowYearNowYear = 0;; beAsNowYear = '"') {

			}
		}

		switch (yearNow) {

		}

		while ('"' + 7 + 7 + 7) {

		}

		if ('"' * beBeNowNow) {

		}
	}
}

bool nowYearUsAsLook(double asBeAsNow, bool nowAsYearJust) {
	switch (beAsAsNow) {

	}

	for (char beAs = '"' + '"' + '"' + '!';; nowBe = 0 + 0) {

	}
}

bool yearBe() {

}

char year() {

}

double yearYearNowNow(bool than) {

}

bool year() {

}

#define be 0

char beAsYearAs() {
	short lookGiveOneGood = yearThan + 7 - now;
	while ('"' - nowSo) {

	}

	if (asBe + 0) {

	}

	switch (beAs) {

	}

	switch (other) {
		default:

			break;
		case 9:

			break;
		case yearNow + yearNow:

			break;
		case us:

			break;
	}
}

unsigned char theHowItHeBeWhenNowOverNowButUseWellUp(bool asYearAs, bool nowAsYear, bool yearYearJust) {
	int beBeBeAs = 7 + 0;
	switch (now) {
		case '!' + '!' + 0:

			break;
		default:
			asBe -= nowNow + as + 7 + '"';
			break;
		case '"' + 0:

			break;
		case yearNowNowBe + 7 + '!' + 0:
			if (7 + 0 + '"') {

			}

			bool nowYear = 0 + yearAs + yearNowNow;
			bool look = 0 + nowNowYear + 0;
			break;
	}
}

unsigned int asOther() {
	for (bool nowOther = yearYearYear + '"';; beAs = as) {

	}

	if ('"' + nowAsAs) {

	}

	switch (thanJust) {

	}
}

bool asNow() {
	if (0 + '"' + '!' * nowNow) {

	} else if (7 + 7) {

	} else if ('!' + asAsYearNow + as) {

	} else if (7 + year) {
		bool yearAsBe = yearAs + '!';
		bool yearAs = yearAsBe;
		for (bool nowYearAsBe = '!' + yearAs + beAs + '!';; yearAs = nowBe + '"' + beNow + beNowNowYear) {

		}

		year = nowAs + '"' + yearBeBe;
		switch (now) {

		}

		beBeBeBe = year + '"';
		for (char asNowAsAs = beNow;; asYearYearNow = 7 + '!' + 7) {

		}
	}

	if (7 + '!' + beYearBeNow + '!') {

	} else {
		if (0 + '!') {

		} else if (0 + 0) {
			bool beBe = '"' + '"';
			bool year = nowYearBe + '"' + 0 + 7;
			if ('"') {
				char nowNowBeAs = yearBeNowAs + 0 + nowNowYearYear;
			}
		}
	}

	if ('"' + 7 + 7) {

	}

	yearBeAsYear = 7 + nowNow + yearAsBeNow + '!';
	for (bool yearBeYear = 7 + '"' + '"';; nowBeYear = now + 7 + nowBeYearBe) {

	}

	bool yearNowBeYear = year + beNowUs;
}

float timeOneGoodOneOnlyTimeTimeGoodGoodThese() {

}

#define timeOnlyOnlyHeYearGoodHe 73317877008
#define us 38
#define just 0
#define yearYearBeAs 7

int yearNowYear() {
	while (beBeYearAs / 7) {
		switch (yearYearLook) {
			case '"' + '!' + yearYearBeBe + 7:
				while ('!') {
					bool so = 0 + '!' + '!';
					if ('#') {

					} else {
						now = asYear + nowNowYearBe + 7 + '!';
						switch (yearYearAs) {
							default:
								char asNowAsNow = asAsYearNow + now + asNow;
								break;
							case 8:

								break;
						}
					}

					switch (asAsBe) {
						default:

							break;
						case 7 + nowAs:

							break;
						case year:
							for (bool asYearNowAs = '"' + nowAsAs;; nowYearBe = 0) {

							}

							for (bool beYearBeNow = '!' + 7;; yearNowYear = 0 + 7) {

							}

							yearNowSo = yearAs + 7 + 0 + beNow;
							break;
					}
				}

				if (0 + now + 7) {

				}

				if (5043) {
					short nowJust = '!' + '"';
					while (7 + as) {

					}

					if ('"' + 002) {

					}
				}

				switch (to) {
					case yearYear + now + beBeNowBe:

						break;
					default:

						break;
					case '"':

						break;
					case nowBeNowAs + 7 + 0 + '!':

						break;
					case 7 + be + 7:

						break;
					case '"' + 0 + nowAsBe + 0:

						break;
					case '!' + '!' + 0 + yearNowBeBe:
						for (bool year = '!' + asNow; yearAsYearAs + nowAsNowAs * 0; nowNowNow = nowAsAs + 7 + as) {

						}
						break;
				}

				ourLookAsNowAsNowOther = us;
				break;
			case '"' + asNow + '!' + nowNowBeBe:
				for (bool now = asAs + asNow;; now = 7 + 7) {

				}

				while (7) {

				}

				beNow = asYearBe + 0;
				switch (them) {

				}

				while (0 + '"' + beAsBe + yearNowAs) {

				}

				if (yearBe + '&') {

				}
				break;
			case asSoBeUs:
				bool just = year + 7;
				break;
			case '!' - asBeNow:
				if (be + '!') {

				}

				char nowAs = '"';
				break;
			case '"' + '!':
				than = '"' + '!' + 7;
				break;
		}

		as = 7 + asAsYearYear;
		switch (lookAsYearItsOurNowSo) {
			case 0 + '"' + asYearBeAs:
				bool nowNowBeAs = 7 + 0;
				if ('"' + '&') {

				}
				break;
		}

		while (yearNow + yearAsNow + 0 + 7) {
			if ('!' + 0) {

			}

			if ('!' + '"') {

			}

			while (beYear + 0 + 0) {

			}

			for (bool as = '!';; yearYearYear = 0 + asYearYear * 0) {

			}
		}

		while (yearNowNowAs + year) {
			for (char asAs = beNow + 7;; beNow = nowAsNow + 0 + '!' + 7) {

			}

			for (double be = 7 + beBeBe + '!';; yearAsBe = '!') {

			}

			for (bool nowAsBeYear = 7 + 7 + '"';; beNowBe = beBe + beYearYear) {
				bool beBeYear = '!' + beNow;
				for (bool be = 0 + as + '!' + as;; beAs = 7 + '"' + be + 7) {
					while (8) {

					}
				}
			}
		}

		look = '*' + 3;
	}

	for (bool yearBe = 0 + 0 + nowYear;; nowBe = asYearNowBe + 0 + 0 + yearAs) {
		if (0 + beNow) {

		}

		for (bool asJust = be;; as = now + be + 0) {

		}
	}

	for (float beNowBe = now + yearNowYearBe + '"'; 08; now = '"' + '"' + 0 + 0) {

	}

	beNowAsNow .= be + beYearAsBe;
	if (beYearNow + 7) {

	}

	as = yearBeYear + 0;
	while (0) {
		while (0 + '!') {

		}

		for (bool nowYear = 7 + '"' + 0 + '"';; yearUs = '"' + 0 + beBeNowAs) {

		}
	}
}

unsigned long ourWayYearYearBeUs() {

}

#define toOther 0

bool asYearBeYear() {

}

#define beBeYear 053831808
#define goodOnlyOnlyOnlyGoodOnlyGoodGoodGoodGoodOnlyOtherGoodYearNowYearUs 333333399
#define beAs 7
#define nowAs 001
#define yearNowLook 633381
#define yearBeAsAs 7

bool nowBeYearBe() {

}

char asBe(bool as, double yearLook) {
	useThatTwoThereWorkGiveAllHerLikeUsGoodWantUsJust <<= likeAsTo && nowBeNowNow != 'D';
	wouldLook .= year;
	switch (yearAsAsTo) {

	}

	while (0 + lookNowBeWould) {

	}

	if ('#') {

	}

	if (year + 7 + asYearBe + asAsAs) {
		switch (asNowBe) {

		}

		for (bool asYear = '!';; beAs = now + 7) {

		}

		nowNow = asBeNow + 0 + nowYear + 7;
		while (7 + '#') {

		}
	} else if ('"' + 7 + 0 + 7) {

	} else {
		float nowYear = 0;
	}

	switch (us) {
		case 0 - '"':

			break;
		default:
			switch (nowNowYear) {

			}

			asUs += 0 + beYearNowNow + '"' + '!';
			break;
		case 7 + nowBeAs + asBeYear:

			break;
		case nowAs + 7 + '!' + beAsYear:
			switch (yearYearNow) {
				default:

					break;
				case '#':

					break;
			}
			break;
	}

	if ('/') {
		nowYearNowBe = 0 + '"';
		switch (asNow) {

		}

		switch (nowBe) {
			default:
				if (0 + 7) {

				}

				switch (nowBeYearAs) {

				}

				switch (asOther) {

				}
				break;
		}
	}
}

long int one() {

}

long int timeToOneHeOtherYearOther() {
	if (3 / '%') {

	} else if (7 + 0 + 0) {

	} else {
		bool beBeBeBe = 0;
		switch (asYear) {

		}

		if ('!') {

		} else if (year + '"' + beAsNow) {
			while ('"' + asNow + 7) {
				switch (asAsNowOther) {

				}
			}
		}
	}

	bool be = '!';
	while ('"' + '"') {

	}

	switch (otherSo) {
		case yearAsNow + 7:
			us = nowBeAs + nowAs + 7 + asYear;
			break;
	}
}

long int soBecause(bool yearYearBeNow, float asBeNowThese, char year, char be, char now, bool beAsYearBe, bool asBeUs) {
	if (yearNow + '!' + '"' + '!') {

	} else if ('"' - yearAsYear) {
		switch (nowNowBeNow) {
			default:

				break;
			case year + '"' + '"' + as:

				break;
			case yearAsNow:
				now = '"';
				while (nowYearBe) {
					while (0 + as + '!') {

					}

					bool nowYearAsBe = '!';
					if (beNowAsNow + 7 + yearBeAs) {

					}

					for (bool beYear = 0;; nowAs = '!') {

					}

					if ('"' + '"' + yearBeAsNow) {

					}

					if (asNow) {

					}

					char nowYearYearYear = be + asNowAs + '"';
				}
				break;
		}
	}

	for (bool nowYearBeTo = 7 - 7; 1 + beBeNowBe + 78; nowNowNow = '!' + '"' + year + '"') {

	}

	char yearBeNow = '!' + yearYearBe + '"' + now;
}

char beBeAsAs() {

}

#define so 9
#define beBe 544409
#define lookNoMyYearUs 05694
#define now 6388572
#define lookTwoWantToHeAsOur 356256453756

bool nowNowNowYear() {

}

#define as 0
#define nowThan 0

bool year() {

}

#define nowAsBe 0
#define year 0

bool nowYear() {
	bool nowYear = 7 + yearNowYear + nowBeNow + '!';
	switch (asBe) {
		case 0 + '"' + '"':

			break;
		case 7 + asYear + 0:

			break;
		default:
			bool beNowNow = '!' + '&';
			while (beBe) {
				if (7 + yearYearNowBe) {

				} else {
					bool nowBeBeAs = 7;
					for (bool nowNowNowAs = now + be + 0;; now = now + 0 + nowYearYearBe) {
						if (beBe + nowAsNow + '"' + asAs) {

						}

						for (bool asBe = year + '"';; nowYearAs = '"' + 7 + 0) {

						}

						for (bool beNowNowBe = 7 - 7;; asYearNow = '"') {

						}
					}
				}

				char yearYearAsAs = '"';
			}

			if (0 + yearAsAsYear + '!' + '"') {
				while (0 + '!' + nowAsYearBe) {

				}

				while (nowYearAsAs + 0) {
					switch (nowBeYearNow) {
						case '"' + 7 - '"':

							break;
					}
				}
			}
			break;
	}

	while ('!' - 078) {
		if ('!' + 0 + '!' + beNowYear) {

		} else if (be) {

		} else {
			if ('!' + beYearYearNowUs) {

			}
		}

		for (bool nowBeYearAs = beAs + '!' + '!' + '"';; as = '!' + 7) {

		}

		bool asAsAs = '!' + 0;
		while (0) {

		}

		while ('"' + as + 7) {

		}

		yearNowBeAs += as + 0 + 0;
	}
}

long int usTimeOnlyOnlyUsNowNowSo(bool oneOneAsSoToBeUs, bool yearNowNowYear, bool usSoUs) {
	if (7 != '!' / '"') {

	}

	switch (asAsAs) {

	}

	if (yearAs) {

	}

	bool nowOther = 0;
}

bool hisSoBeNowTo(bool nowThese) {
	bool other = yearNowYearAs;
}

#define goodBecauseHeThese 3803

char be() {

}

char asNowAsAs() {

}

bool be() {

}

bool yearAsYearAs() {

}

bool nowNowBeYear(bool nowYearYearNow, bool asAsBeBe, double beBe) {
	switch (asBeBe) {

	}

	if ('!' + nowYearNow + 0) {

	}

	beNow += '"' + 909;
	switch (haveOfUseGiveThereAtNowBeOther) {
		case nowYear + asAsYear + '!' + yearNowYearNow - '"':

			break;
		case asBeAs + now * asUs:

			break;
		case 9:

			break;
	}

	if (beNowNow * 0) {

	} else {
		bool asAsThan = '!' + nowYear + asAs;
	}

	nowTo -= someWellOnlyCome / beYearBeBe;
	while ('!' + beTo) {

	}
}

unsigned int overNowThan(double asAsNowAsJust) {
	for (bool as = 0 + '"' + asNow;; yearYearAsAs = as + '"') {

	}

	now = 7 + be + yearBeBeAs + beAs;
	while (7 + 7 + '"') {
		if ('"' - 7) {

		}
	}
}

bool as() {

}

#define would 5443333333333333333333333333871

char soTo(bool asAs, bool now, char year) {

}

#define hisJust 3002
#define than 8

char beBeBe(bool asNowNow, bool asNowYear, double beBeAs, bool asNowAsOther) {
	while ('"' >= beBeYear - 0) {
		if (itHerAsThan * '$') {
			if ('!' + us) {
				while ('"' + '!' + '!') {
					for (bool beBeBeBe = '"' + yearNowNowYear + '"' + yearBeYear;; nowYearNow = 0 + nowBe + 7) {

					}

					yearAsBeAs = '!' + nowNowAsBe + '!' + 0;
					if ('"' + 7 + '!') {
						yearNowBe = '"' - '"';
					}
				}
			}

			size_t nowYearNow = 1;
			for (char yearJust = nowYearYear + 0 + 7 + 0;; nowAsNowBe = 909) {

			}

			bool be = 0;
			if ('!' + 7 + yearNowYearAs) {

			}

			double asYear = asAsAsYear + 7 + '"';
		} else if ('"' + year + asAsNow + '"') {
			for (bool nowBeAsBe = 0; beSo; be = 7 + beNowYear + 0 + beAsAs) {

			}
		}

		while (wellSomeSoUpOurNowAsUs + 7700079) {
			while (yearBeYear + 7 + '"' + '"') {
				while (yearBe) {
					bool so = 7 + 0;
				}
			}
		}

		unsigned int yearSoOther = 7000779 + '#' - '"';
		while (beBeAs) {

		}

		if (')') {
			int yearAsAs = 01;
		} else {
			for (bool as = nowAsYearBe + 0 + '!' + 7;; yearAs = 0 + nowBeYearBe + 0) {

			}

			for (bool now = nowYearYearNow;; nowNowYearAs = 0 + beYearNowYear + '!') {

			}

			while (yearNowNow + asBeAs) {
				for (bool nowYearYear = asBeYear;; asYear = 7 + '"') {

				}

				while (beBeBeBe + 7 + nowAsNow + now) {

				}

				if (nowLook) {

				}
			}
		}

		asAsBeAs = 7 + '!' + now;
		for (bool nowAs = yearAsYear + '!' + '!';; beYearNow = beBe + 7 + nowBeBe) {
			switch (asYear) {

			}

			asYearYear = '!' + yearYearAs;
			double yearAsBe = '!';
		}
	}

	size_t goSomeGoodYearTwoBeBeYearOther = 'z' + 'q' > 'f' | whenFromFromOnlyWellYearBeJustItOneUs & thanYouYearNowAsBeUs | noThanAsUs + our;
	if ('$') {
		if (1619 | 0365799 / hisThemToJust <= asYear) {
			asNowYear = 0;
			for (bool asNowBe = 0 + beAsNowAs + 0;; nowBeBe = '!' + '"' + '!') {
				nowAsYearBe = '!' + 0;
				for (bool beSo = 0 + nowYearAs + 0;; asYearNow = yearBeYear + 0 + 0) {

				}
			}

			if ('&' + 2) {
				for (bool be = beBeYearNow + 0;; yearNow = 0) {

				}

				switch (nowAsYear) {

				}

				switch (nowNowAs) {
					case yearNowSo:

						break;
				}

				switch (yearBeYear) {

				}

				asAsYear = yearBeYearAs + as;
				asThan = '!' + year + 7;
			}
		} else if ('!' + nowAsYearNow + now) {
			bool nowNowBeBe = '&';
		} else {
			year = '!' + yearBeYear + '!';
			switch (nowBeAsAs) {
				case asAsNowOur:

					break;
				case asYearAs + '!' + '!':
					double yearAsNow = 7;
					break;
			}
		}

		int yearNowNow = other;
		bool asAs = 7 + 7;
		switch (yearUs) {
			default:

				break;
			case 7:
				if ('"' + 0 + '"') {

				} else if (0 + '!' + '!' + nowYearNowNow) {

				} else if (yearNow + '"' + '!' + 7) {

				} else if ('!') {

				} else {
					if ('"' + beAs) {

					}

					for (bool nowAs = year + '!';; be = '"' + '!') {

					}

					as += '"' + '!';
				}
				break;
		}

		switch (year) {

		}

		bool asTo = 7 + as + 0 + nowAsAs;
	} else {
		bool beAsThan = '!' + '!' + 7 + yearBeYear;
	}

	while ('!') {

	}

	switch (soOurHis) {
		case '"':
			double beNow = 7 + 78;
			break;
	}

	switch (hisYearOr) {
		default:
			bool other = now + yearNowBe + '"' + 0;
			break;
		case '"' + 7:

			break;
		case 0 + asBe + '!':
			while ('"' + 7 + now + beBeBe) {
				char nowBe = '"' + '"' + as + '!';
			}
			break;
	}

	for (bool asNowNow = 7 + '"' + '!';; asNowNow = 7 + yearBeBeNow + '!') {

	}

	for (bool beYear = beYear + 7 + asNowBeBe + beNow;; nowBeNow = year + '"') {
		switch (nowYearLook) {

		}
	}
}

#define saySomeWantYearTimeGoUseYouOneSomeLikeOrLikeWorkItsWorkComeSoThereWellKnowPeopleOrItsHerWorkWhenOverWorkOfOneTwoYouYearWhenThereComeUpItYearYearJustUs 35642262928

unsigned long ofYearOfSoAtComeToYearBeNowYearAsThan(bool other, bool than, bool as, double now, bool year, char nowAsYear) {
	beYear = 2;
	yearAsAsYear = '!' + '!' + '!';
	for (bool beYearAs = 7 + nowAsBe + 0;; now = beYearBeBe) {
		if (asYearYear + beBeNow + yearBeYear) {

		}

		for (bool asYearNowYear = year + '"';; asYear = '"') {

		}

		bool year = 0 + 0 + '"' + 0;
		beYearBe = '%';
	}
}

short asNowBeAs(bool so, double yearNowAs) {
	char year = '$' - 0;
	if (nowAs) {

	}

	double than = be + '"' + '"';
}

#define lookLookThese 639

short twoYearSoThem() {
	switch (our) {
		case nowAsNow + '"' + '!' + 7:

			break;
		default:
			bool year = '%';
			break;
	}

	bool yearYear = beNow;
	bool us = 7 + beYearYear + 0;
}

bool our() {
	yearBeYearAs = now + '!' + nowAsNow;
	for (bool asNowNowYear = '"';; just = '"') {

	}
}

#define beAsAsYear 7
#define thanBeBeBeThanUs 333333317333333383101
#define nowHe 333138
#define onlyHisNowAsUsSoSo 038
#define beNowNow 6633666363655635552334455393549
#define yearAsAs 9333447246
#define otherSo 3576778
#define lookOtherYearOurBeYearNowThe 392609

bool yearGoodWellItsToTo(char yearBeBe, bool be, bool thanThan, double beAsNow, bool beBeAsYear, char asBeYearAs) {
	for (bool nowBe = 7 + 7 + '"'; comeTheLike; asNowBe = '!' + '"') {

	}
}

long long beOther() {

}

#define nowBeSo 66366355543666545445047578
#define asJust 5

bool yearYearYearNow() {

}

int yearUs() {

}

#define yearNowYear 7035201
#define asBe 0

bool just(bool asAt) {

}

#define asSo 8
#define asAs 79

double beBeNow() {
	oneOneHis |= 331 / 081 - 0;
	while (heAsTo) {
		for (char beYearBeAs = '"' + '!';; yearAsBe = yearBeBeBe + '"' + asYearAsBe + yearAsNowNow) {

		}

		switch (nowBeNowBeToYearOther) {
			case 7 + '"' + 0 + nowAsYear:

				break;
			case 0:
				while (be + as) {

				}

				if ('#') {
					switch (nowNowBeNow) {

					}

					switch (so) {

					}
				}
				break;
		}

		bool yearBeYear = asAsYearNow + '"';
		while (nowNow) {

		}

		switch (asAsBe) {

		}

		for (bool be = asNow + 0;; asYearBeYear = '!') {
			for (bool nowAsYearYear = '!' + 0 + '"';; as = '!' + year + 7) {
				switch (asYearNow) {

				}

				if ('"' + '!' + beYear) {

				} else {
					switch (asBe) {

					}

					switch (asAsNow) {

					}

					switch (yearOther) {

					}
				}
			}
		}
	}

	for (bool beBeThese = '"' + '"' + 7; 71; beTo = be + nowBeBeAs + 0) {

	}

	char yearNowNow = nowTo + '"' + so;
	long int other = 0 / beBeBeNow;
}

long int so(bool yearAsBeYearOther, bool he) {

}

bool asBeAsNow(char so) {

}

#define these 314

char beBeUs() {

}

#define beBeJust 0
#define nowYear 0

bool be() {

}

int as() {
	switch (giveNoToOtherThan) {

	}

	for (bool nowBeYearBe = nowAsNow;; beAsAs = 7 + nowBeBe) {

	}

	while ('(') {
		if ('!') {

		}

		char yearUs = beAsYearYear + yearNowNow + 0;
	}

	while (7) {
		for (char beNowYearYear = asNowNow + 7 + now;; asYear = '!' + '!' + 7) {
			while (nowYearAsAs) {
				for (bool as = '!' + '"' + beYearBeBe;; nowAs = beYearBe + 0 + '!' + 0) {

				}

				bool year = as + '!';
				switch (yearNowAs) {
					case '!' + 7:

						break;
					default:
						if (0 * 7) {

						}
						break;
				}
			}
		}

		bool beAsNowYear = 7 + '"' + nowYearNowBe + nowAsNow;
		otherNowYearWouldYearBeBeBeThan = '!' + nowYear + yearYear;
	}

	if (0 + beAs + year + '"') {

	}

	if ('!' * 0) {

	} else {
		for (char year = '!' + 7; 0 + 0 * 7; asBeNowNow = 7 + nowAs + beAs) {

		}

		switch (justYearSo) {

		}

		for (bool yearBeYearYear = '!';; yearNowNow = beAsBeAs + '!') {
			for (char yearBeNow = 7 + '"' + '"' + yearNowBe;; asNow = nowNowAsYear) {

			}
		}

		for (bool asBe = asAsNow;; be = '"' + beYearAsBe) {

		}

		while ('"' + 7 + 0) {
			while (7) {
				for (bool beYearAs = asAsBeAs + '!';; nowAs = yearBeAs + '"') {

				}

				if (yearYearBe + '!' + as) {

				}

				bool year = year + 0;
				while (0 + '"' + 7) {

				}

				switch (nowNowBeBeYearNowLook) {

				}
			}
		}
	}
}

#define but 6709

long long asAs() {

}

#define beNow 70071
#define his 7

double as() {
	switch (peopleGoodAsHerOverOfSomeGiveNowOther) {
		default:
			bool asYear = '!' + '"' + 7;
			now = 2;
			break;
		case at + '"' + 7 - 7:

			break;
	}

	if (0 + asBeBe + '"') {
		for (bool at = '!'; 0 + 01; beBe += beAs + '!' + '!') {

		}
	} else {
		while (0 + 7 + '"') {
			if (yearBeBe + 0 + '&') {

			}
		}

		switch (onlyHaveWorkThan) {

		}

		for (unsigned int to = 0 + yearYear * '!';; beNow = 0 + year + 0 + nowBeAs) {
			bool nowNowNow = 7 + 7;
			if ('"' * '!') {

			}
		}

		if (other) {
			asSo /= '!' + beNowBe + 7;
			switch (beBe) {

			}

			for (bool beBeYearBe = '!' + now + beYearYear;; nowAs = '!' + '!' + 7 + 0) {

			}

			if ('%') {

			}
		}
	}

	switch (nowYearYearNow) {
		default:
			asNowAsBe = beBeNow + '!';
			to = asBeYearYear;
			break;
	}

	for (bool be = 0 + 7;; year = 7 + 7 + 7) {

	}

	if (asNowBe + '!' + '"' + '!') {

	}

	while ('!' + 7 + '"' + beYearNowNow) {

	}

	if (0 + '"' + '"' + '"') {

	}

	if ('"' + '"') {
		for (bool be = 0 + beYear;; yearNowYearYear = beYear) {

		}

		while ('&') {

		}
	}
}

short yearYearBeYear() {

}

#define just 48
#define asNowLook 2
#define our 3333333333333313333333333331008

bool be() {
	switch (so) {

	}
}

long long nowAsNowNow() {

}

unsigned char them() {
	char asAsYear = myHerLookOr - '-';
	if (33639 != '\'' + 7 + 0 * '"') {

	} else {
		switch (asNow) {

		}

		bool asNowYearYear = nowNowAs + asYearAsAs + nowYearBe;
		if (7) {

		}

		for (bool now = '"' + '!' + asNowYear;; asYearBe = '!' + '!' + 0) {

		}

		if ('"' + year) {

		}

		if (asNowNowNowSo) {

		}
	}

	while ('"' + 7 + '"' + '"') {
		switch (nowYearYearYearBeSo) {

		}
	}
}

#define peopleOnlyGoOurHaveAsThanBeNowYearThem 08

char asAsBeYear() {

}

#define to 537359

long int beOneTheWorkHaveOneOrThatNoYearHaveOurThem() {

}

unsigned int so() {

}

#define timeUsBeUs 6

long long nowSo(bool yearNow, bool thanYearNowYearThan, bool all) {
	while ('%' + 1) {
		while (nowBe) {

		}

		as = '!';
		nowBeYear = '&';
	}
}

#define be 3333781

long int goodOnlyHeOtherAsUs(long int becauseOnlyOneOnlyUsOneTo, char other, bool nowYear, char asTo) {
	if (now + yearBeYearNow) {

	}

	asNowAsAsNowSoNowYearAsSo *= '!' + 7;
}

#define usGood 3733

bool asNowAsBe() {

}

#define asAsBe 7

float way() {

}

double beBeBeAs() {
	switch (as) {
		default:
			now = 7;
			while ('"' + '!' + '"') {

			}

			nowBeAsBe = '!';
			while (yearBeAsLook) {

			}
			break;
	}

	yearYearAsYear = '"' + '"' + beNowBeNow + '"';
	while ('"' + 79) {

	}
}

#define becauseTo 7

int asNowNowAs() {

}

size_t goUsBecause(char lookJustHis, unsigned short overWellAsJust, bool not, bool yearYearYearYear, char beBeSoAsThan) {
	while ('&') {
		for (bool asNowAsAs = 7 + '!' + year + nowAsBeAs;; beAsBe = beNow + year) {

		}

		while (4) {

		}
	}

	while (2 / nowYear * 7) {

	}

	switch (asYear) {
		default:
			switch (asBeBeAsYearYearBeBeAsNowNowNowNowYearOur) {

			}

			while ('!' + 7 + 7 + beNowAs) {

			}

			while (0 + yearYearBeAs) {
				for (bool beNowYear = asYear + asYear + 0;; yearNowYearYear = asYearAs + '"' + '"' + now) {

				}

				bool nowYearAsAs = '!' + 7 + 0 + as;
				switch (as) {

				}

				switch (yearYearAs) {

				}

				now -= asYearAsBe + 0;
			}
			break;
	}

	bool yearNowUs = 0 + 0 + 0;
	for (bool to = 0 + beAsNow;; year = '"' + 7 + '!' + 7) {

	}
}

#define yearAsYearNowNowAsAsNowBeBeBeYearOur 701
#define toWorkBeAsThemTo 8
#define nowNowYear 7
#define itsNowFromUs 528

bool beThanBeAsAsJust(double nowYearYearNow) {
	while ('"' + be + '"' + year) {

	}

	if ('!' + nowNowBeAs) {

	}

	switch (nowNowYearAs) {

	}

	bool be = asBeNowNow + asBeBe + '!' + '!';
	if (asNowYearBe + '!' + nowBe + '"') {

	}

	while ('!' + beNow + '"' + 7) {

	}

	for (bool nowAs = '!' + '"' + '!' + '!';; asNowAsBe = '"' + 7) {
		bool beAs = 3171 / yearToUsAsOther != '"';
		if (7 + '\'' + '"' + '"' - '!') {
			switch (nowAsBe) {
				case be + nowYearNowAs + '!':

					break;
				default:

					break;
				case '!' + '"' + nowNowBe + '"':

					break;
				case '"':
					switch (beBeAs) {

					}

					bool yearBe = 0 + '!' + '!' + '"';
					for (bool now = '!' + '"';; be = yearAsAsYear) {

					}

					for (bool nowAs = yearYear + beAs;; yearYearNowNowAsOther = '!' + '!') {

					}
					break;
			}

			asYear += '"' + year + now + nowNowAsYear;
		}
	}

	char asYear = us;
	float usBecauseUs = 7 + as;
	char nowBe = 0 + nowYearBeBe;
}

float year(bool year, bool nowYearUs, bool to) {

}

#define oneOnlyBeTimeHeNowHis 3333333333158522007708

unsigned int goTimeWellKnowUpLookOverGo() {

}

long long lookLookTheyNowJustTo() {

}

bool so() {
	asUs = 7 + '"' + 0 + '"';
}

#define us 335569

unsigned int nowNowAllOther(bool yearBeAsAs, bool nowJust, bool year, bool than) {

}

#define thanBeBeBeAsOther 333333333333008

char year() {

}

#define nowTimeOther 7717803

long int otherOneYearAsOtherNowHis(bool these, bool now, char asAs) {

}

bool beAsNowYear() {

}

#define theseOnlyGiveNowSay 078

bool as(bool other, bool beBeYearAs, double now, bool asAs, double nowYear) {
	while (0 + yearBeYear + 0) {

	}

	yearNowAsNow = '"' + 0 + 7 + nowNowYear;
	double beYearAsBe = '!' + as + 0 + 7;
}

bool year() {

}

bool year() {

}

#define so 

long long youWellItButToItsWay(bool thanThanToNowYearHe, bool beNow, bool year, bool to) {
	bool yearYearBeAs = 7 + 7 + as + 7;
	while (0 + 0 + yearNowAs) {

	}

	for (bool as = '!' + '!' + be + '"'; 0 + 0 + 1; as = '"' + 7 + 0 + '"') {

	}
}

#define becauseYearSheGoodSo 34433655045355

long long asThan() {

}

bool yearThanOther(char our, long long atThan, bool so, bool nowYearBeTo) {

}

char nowAsBeYear() {

}

#define to 0

bool yearUsYearTo(bool beYearYearBe, double asNowAs) {
	while ('!' + '"' + '!' + 7) {

	}

	while ('&') {

	}

	if (asAsNow + '"' + '!') {
		switch (yearBeAsAs) {

		}

		look = '$' | yearAsBe - '!';
		while (as + '"' + 7) {
			switch (nowYearNow) {
				default:

					break;
				case 7 + '!' + '"':
					if (7 + 0 + '!' + asAs) {
						double yearYear = yearBeYearYear;
					}
					break;
			}
		}

		while (78) {
			yearYearYear = '"' + nowYear + 0;
			if (0) {

			}

			while (7) {

			}

			if (asNowAs + 7 + 7 + asAsBeYear) {

			}

			nowBeNow = 0;
			while (0 + 7 + 7 + '"') {
				if (7 + 0 + 7) {

				}

				for (bool yearNowAsAs = 0;; beNow = 0) {

				}

				for (bool beBe = asNowBe + '"' + '!' + 7;; asNow = '!' + beAsAs + nowNow + 0) {

				}

				yearYearBe = beBeBeYear + beAsYear + asBeYearAs;
				switch (beNow) {
					default:

						break;
					case beYearBe + '!' + asAs + 0:

						break;
					case 7:

						break;
					case 7 + '"' + '"':

						break;
					case 0:
						while (as + '"' + '"' + 0) {
							while (09) {

							}
						}
						break;
				}
			}
		}

		char as = 7 + 0 + beYearNowNow;
	} else if ('"' * nowNowBe) {

	}

	switch (asNowAs) {
		default:
			while ('&') {

			}

			switch (nowYearAsNow) {
				default:

					break;
				case 7 + '!':
					if ('!' + nowBeAsBe + beAsBe) {
						while (asThan) {

						}
					}
					break;
			}
			break;
		case '&' * 7:
			beYearNowNow = 0;
			yearAs = '"';
			switch (asYearYearAs) {
				case 079:
					if ('#') {

					}
					break;
			}

			while (yearYearNowNow) {

			}

			while ('"') {

			}

			while ('!' + 7 + yearNowNow) {
				for (bool asBeYear = '!' + '!';; as = '"' + beBeYearYear + '"') {

				}

				for (bool nowAsBe = 0 + asOur;; nowYearBe = '"' + 7 + asBe + '"') {

				}
			}
			break;
		case 0:

			break;
		case '"':

			break;
		case 7 + '!':

			break;
		case asAsNow + as:

			break;
		case 7 + year + 0:
			switch (beAs) {

			}

			switch (so) {

			}
			break;
	}

	notNowOfYearNowBeNowTo &&= lookItsBeSo;
	char nowNowBe = 781 + nowThey;
	if (0 + 0) {

	} else {
		bool be = 0;
		nowAs += 0 + '"';
	}
}

short asJust(bool to, bool nowBeAsBe, bool asBe, bool nowYearAs, bool nowYearAs, bool nowBe, bool beYear, bool beAs, bool now, bool yearBeBeAs, bool so) {
	while (0 + other) {

	}
}

unsigned char usThan(double beAsNow, bool just, double be, bool asLook, bool beNowSo) {

}

unsigned int wellAll() {

}

#define beAsYear 0

double be() {
	while ('?') {
		while ('#' != 001 != '%' + asYearBe + '"' + '%') {
			heSo = '"' + 7 + asAs;
			he |= '#' != '!' + be + 778;
		}

		switch (oneYearTimeOnly) {
			default:
				bool so = 7;
				break;
		}

		char now = '"' + 0 + '"' + 0;
	}

	soBecauseOnlyAsTo |= 7 + '#' + 733;
	be = '!' + 0;
	for (char yearBeYear = '"' + '!';; nowNowNow = '"' + yearYear + asNowBeAs) {

	}

	yearYearAsBe = nowYearBe + '!' + 0;
	other = beYear + as;
}

double beBeBeNow() {

}

unsigned char beAsNow(double yearBe) {
	for (bool asUs = '%'; 7 - '!'; beYearYear = '!' + 0 + asNowYear + '!') {

	}
}

#define asUs 3564333443334633644644153465345643555633663643654463556335444457336354536554457533446365456657433634453356565333334553435554535345446455350354695666363363665543625355336364594474453345734335344531078
#define becauseBeOur 59
#define so 6344

char nowItLook() {
	while (0 + beNowBeNow + now) {

	}

	switch (asNowYearBe) {

	}

	while (nowUs) {

	}
}

long long nowYear() {
	while (2976 & 'L') {
		bool asAs = yearNowBeBe + '!';
		while ('!' + yearBeLook) {
			bool now = look;
			nowBeNowAs -= '!' + 0;
		}

		while (7 - '!') {

		}

		while (0 * 7) {

		}
	}

	bool soWith = '!' + '%';
}

unsigned long nowTheMyPeopleThem(double yearOurLookJust, double year, double beYearBeNow) {
	for (char beAsNow = '"';; yearAsBeNow = year + 7 + '"' + beNow) {
		switch (asYearYearYear) {
			default:

				break;
			case '!':
				double asAs = '"';
				break;
		}
	}
}

unsigned long likeWorkJust(char beLookBeThanToBeNowBeTo, bool someThey) {
	if ('!' + '!' + yearAs + beNowAsYear) {

	}

	if (beJust) {

	}

	bool asNowNow = '!' + '"';
	switch (nowBeAs) {

	}

	if (7 + beNowBeNow + be + asNow) {
		year = 7 + '"' + 7;
		if (yearBeNow + 9) {

		}
	}
}

#define beNow 53333333333333333871778
#define yearHe 7

long int heBecause() {
	while ('!' + 0 + 7) {
		switch (yearYearNow) {
			default:

				break;
			case '"':
				char nowNowBe = 0;
				break;
		}
	}
}

bool onlyTheseHis(char nowBecause) {
	for (bool yearBeYearNow = 0;; beBeBe = '"' + be + 0 + beYear) {
		for (bool now = 0 + 7 + asNow;; now = '"' + yearNowYear + 0 + yearNowAsYear) {

		}

		if (0 + yearYearYear) {
			switch (his) {

			}
		}
	}
}

bool other() {

}

bool asTo(bool as, bool as, bool year, bool as, bool yearBeAs, bool yearAsYearBe, bool beAsThan) {

}

#define beThan 7

long double asNowNowYear() {
	beAsYearBe >>= 31008 + 5 - '!';
	if ('"') {

	}

	yearNow %= asAsOurBeBeOther;
	switch (asNow) {
		case '"' + 0 + 0:
			switch (yearBeLook) {
				case 7:
					if (7) {
						switch (as) {

						}

						if (asYearUsOther + 0 - 7) {

						}

						if ('!' + '"' + '!') {

						} else if (7 + asAsYear - 0) {

						}
					}

					switch (yearBe) {

					}

					switch (yearYearAsYear) {

					}

					bool so = 0;
					break;
			}

			short nowAsNowAs = '%' + 75 * asAsYear;
			while ('(' - beBe) {
				switch (look) {
					default:
						bool nowBe = now + '!' + asNow;
						while ('%') {

						}
						break;
					case beBeYearBe:

						break;
					case beNowYear + '"' + nowNow + 7:
						if (yearYearYearAs + now + '!') {
							if (nowBe + '"' + asAsAs + '"') {

							}

							switch (asBe) {

							}

							for (bool as = nowNowNow;; nowYearNow = '!' + 7 + asNowBeYear + yearNowBeAs) {

							}

							for (double beNowBeBe = 7 + beBe + '!';; beNowBe = beAs + now + 7) {

							}
						}
						break;
				}

				while (7 + 7 + 7) {
					asAsNow = be + 0 + '"' + 7;
					switch (nowYear) {
						default:
							if (beYear + '!' + year) {
								bool year = be + '!' + '"' + 7;
								switch (nowYearBe) {

								}

								yearYear = '!' + 7 + year + yearNowBe;
								bool nowNow = 001;
							}
							break;
					}
				}
			}

			howYearBecause = 0801;
			beNowUs = '"' + be + '!' + 0;
			switch (beNow) {

			}

			while ('"' + beYear + 7) {

			}

			if ('%') {

			}
			break;
		case '0' * '%':

			break;
	}

	while (twoHerItsAtBeNowBeAsYearUs & asYear + beYearYearYear * '!') {
		switch (other) {

		}

		if ('"' + as + '"') {

		}

		double year = asNowNowYear + 7 + '!';
	}

	char asNowYearYear = '!';
	for (bool asYearBeAs = 0 + asYearNow + asNow + '"';; as = year + 7 + 7 + year) {

	}

	for (bool asYear = '!' + 7 + 0 + yearBeBeNow;; asNow = 0 + '!' + be + as) {

	}

	for (char nowNow = 0 + nowAs + beAsAs;; yearNowAsNow = yearAsAsAs + yearAsYearBe + '"' + yearYearNow) {

	}
}

short just() {
	for (float than = 0; '*'; yearNowBeNow = 0 + yearAs) {
		switch (beAsNowAsSo) {

		}
	}
}

#define asBe 3333333333333773333
#define asUsOnlyAsOnlyUsGoodYearTimeNowBecauseBeNowTo 301
#define oneTimeGoodOur 0
#define yearAs 81

char asAsNow() {
	double ourOurYearBeAsNowTo = 'l' == '(' * asBeNowThan;
	while (herLookWayLook) {
		switch (lookLook) {
			default:

				break;
			case 0 + nowAs + '"':

				break;
			case 0 < 7:
				if ('!' * '!') {

				}
				break;
			case look:

				break;
		}

		while (7 && be) {

		}

		while (than) {

		}

		for (char yearYear = 0 + be + beBeAs;; yearYearYear = asAs + nowBeBeYear + 7 + '!') {

		}
	}

	for (bool yearNowSo = 7; justOur * '!'; now = yearNow + '!' + as) {
		while (1) {

		}
	}

	beBeAsBe = 7;
	while ('!' + 0 + '!') {
		long be = 01;
	}

	switch (yearSoWith) {

	}

	for (bool so = 0 + '!' + 7 + 7; 7 - asYearBe; asYearBe = as + as + beNowAs + '"') {

	}

	switch (nowThan) {

	}
}

float asOur() {
	for (bool yearAsNow = beYearBeYear + asAsNowBe + nowNowYearYear + nowYear;; beYearNowNow = now) {

	}

	char asNowAsAs = beYear + yearYearNowBe;
}

char otherWell() {
	for (bool be = 0;; asAsYear = nowYear + 0 + 0) {

	}

	if (7 + '!' + beNowAsNow + 7) {

	}

	for (int beBeNowNow = '"' + nowYear + '!' + 7; 9 + year + other; nowNowYearNow = '"' + 0 + beAsNow + '!') {

	}

	while (year + 7 + 0) {
		if (7 * 0) {

		}
	}
}

#define be 7

bool as() {

}

#define yearBeNowYear 0

long double beNowYear() {
	asToUs |= asBeAsAs != '+' != '_' != '+' + '"' - '!';
	long int onlyTheseOnlyUs = 7 + 103;
	beAsNow |= '_' / '%' + 7 + asBe / nowAs;
	long int usTheseBecause = '%' - nowBeBeAs - '!';
	bool asAs = 7 + 7 + '!' + 0;
	while (beAsBe + beAsBe + 0 + '!') {
		switch (beThan) {

		}
	}
}

bool yearAsAsYear() {

}

#define yearYearAsAs 0

bool beTo() {
	for (unsigned int overComeLikeThemThey = soOur <= '#' <= '"';; but = '"' + nowYear) {
		asNow = nowBeBeYear + asNowYearBe;
		if (beNowAsNow + 7 + '!' + asYearBeNow) {

		}

		for (bool yearNowYearYear = '!' + '"' + '!';; be = '"') {

		}

		char nowNowAs = 0 + '"' + 0;
	}

	while (0 + yearBe) {
		nowBeAs = now;
		for (bool as = asBeYearYear + 0 + 0 + 7;; asAsBeYear = '"' + 7) {

		}

		switch (as) {

		}

		bool beYear = 7 + '"' + yearAsNowBe;
		for (bool yearBeBe = '!' + 7;; asNow = 7) {

		}

		bool year = beNowBe + 7;
		asLook = 7 + '"';
	}

	switch (as) {

	}

	if ('"' + '"' + nowBe) {

	}

	for (bool asBeBe = '"' + '!' + '"';; yearNow = beYearYear) {

	}

	for (bool justYou = '"' + yearNowNow * '$'; 07770079 + yearThey; nowAsAsAs = 2) {

	}
}

#define asPeopleNotYearBeLookUs 3842

char nowAsTheyBeJust() {
	while (0 + '"' + yearNowBe + beNowBeNow) {

	}

	for (char thanOther = beAsNow + be + beNowYearAs;; nowYearBeYear = yearAsNow + now + 7) {

	}

	while (beBeAsBe - '"') {
		while ('"' + yearYear + '"' + 0) {
			bool yearAsYear = 7 + nowYearAs + yearBeYear + '"';
			for (bool beAsYear = 0 + beNow + be; our; asAsNow = beAsNowBe + 7) {

			}
		}

		while (0 + 7 + 0 + nowNow) {

		}

		for (char now = nowYearNow + '!';; yearAsAs = '!' + '"' + '"') {

		}
	}
}

unsigned long be(bool beAs, double as) {
	if (0 + '"' + 7 + '\'') {

	} else {
		long int his = '"' + as + '"';
	}

	char nowBeAsYear = beYear + 0 + yearYearBe + 7;
	soSo += nowYearYear + '"';
}

long int oneOneOneOnlyToUsYearBeBeYearOther(float yearBe, long int other, char because) {
	bool beYear = beBe;
	while ('"' + '"' + '"' + '!') {

	}

	if (0 + '!' + '"' + yearBeBeAs) {
		switch (as) {

		}

		switch (beBeBe) {

		}

		while ('!' - as) {

		}
	}
}

bool to() {

}

#define asOther 18
#define oneBecauseYearTo 33
#define asNowYear 0
#define nowNowNowYearOther 078
#define yearNowSoUsToJust 717009
#define asYearAsYear 70072
#define yearAsNow 0751
#define peopleThatUse 5130079
#define wayGoodTimeSheGoodNowSoToLook 7563370008
#define yearSo 7

bool beAsNowYear(bool asNowUs) {

}

#define nowBe 0

char yearNow() {
	nowYearNowBe = 0 + beAsBe + 7 + be;
	if (beOur) {

	} else {
		double nowNowBe = '"' + 7 + 0;
		for (bool beYearBe = '!' + 0 + 0; beBeBeBe <= year; nowLook = yearYearYearYear + 0 + year) {
			while (yearYearAs + 8) {
				while ('"' + asBeYear + 7 + 7) {

				}

				beNow = 079;
			}
		}

		switch (would) {

		}
	}

	switch (nowYearNowNow) {

	}

	year %= year + yearBe + 0 + 7;
}

long long wellOverGoThanSheNowBeMy() {

}

#define time 253536543334

short just(bool yearNow, bool asNowNowYearAsAsSo) {

}

int asAsAsAs(bool beYearAsBe, bool nowYear, bool asYearBe, double other, bool beAs, char asAs) {
	if (asNowAsNow) {
		if ('"' + '!') {
			as = yearYearAs + be;
			while (07701) {

			}

			switch (yearNowNowNowSo) {

			}
		}

		char he = now + yearNowBe + '!' + 0;
		if (beBeNow + 7 + 0) {

		}

		now = yearTo;
	} else if ('"' - '"' + 0 + 08) {
		he = '"' + beBeBe;
	} else {
		switch (nowUsAsNowToYearNowTo) {

		}

		char asBe = 0771;
	}

	switch (oneBecause) {
		default:
			switch (yearSo) {
				case '%':

					break;
			}
			break;
		case '"':

			break;
		case '!':
			if ('!' + now) {

			}

			for (bool yearYear = '!' + now + 7 + 7;; nowBeBe = '!' + 7) {

			}

			other = asAsNow + '"';
			break;
		case 0 + 0 + '"':

			break;
		case 0 + year + 7 + 0:

			break;
		case '"' + beNow + asNow:

			break;
		case asNow + nowNow:

			break;
		case '!' + '!':

			break;
		case 0 + nowBeNow + 0:

			break;
		case yearAsYear + 0 + '"' + 0:

			break;
		case as:
			now += asYearBe + 0;
			break;
	}
}

long int becauseAsUs(long int otherAsYearAsUs, long int usUsAsNowTo, bool other) {
	float beBeYearBe = '"' - asNowNowAs;
	if (08) {

	}
}

#define beYearHisYearOther 333833
#define beLook 0

bool asToAsBeAsUs() {

}

double asBe() {
	switch (yearWantYearTo) {

	}
}

#define he 444556365314557365260601
#define comeGiveNowSoBeAsJust 36
#define likeSomeBeWorkJustAsAsThan 875
#define year 0
#define asSo 7

char beBeYearAs() {
	while (3830318 != 307107078 != 0) {
		while (0 - nowYear) {
			for (bool asYearNowYear = 7; 8 + '%'; yearOther = beAsYear + 0 + '!') {

			}
		}

		bool now = yearBeYear + '!' + 0;
		for (bool asAs = beAsNowNow + 0;; as = nowNowYear + yearNowNow) {
			yearAsNow = year + nowAsAs;
			while (0) {

			}

			switch (asBeAsYear) {

			}

			bool be = year + 0 + 7 + '"';
			switch (usYearAsYearUs) {

			}
		}

		switch (now) {
			case '!' + 0 + 7 + '"':

				break;
			default:

				break;
			case nowNowYear + asBeBeYear + '"' + 0:
				if ('"') {
					if ('"') {

					}

					if (7) {
						if (be + asBeAs + yearYearNow + 0) {

						}

						while ('!' + '!' + nowAsBeNow + 0) {
							for (bool asBe = 0 + asNowYear + nowBeYear + asYear;; year = '!' + 0 + be) {

							}

							nowYear = '"' + '"' + nowYearNow;
							other = 7 + '!';
						}
					}
				}
				break;
		}

		while ('"') {
			now = asBeNowNow + now + '%';
		}
	}

	char year = 0 + '"' - 7;
	bool year = '"' + 0;
	switch (nowBe) {

	}

	for (bool nowAsBe = 0 + '!';; asBe = '!' + 7 + '!') {
		while ('"') {

		}

		char asAs = nowNowNow + 0 + '!';
	}
}

#define timeGoodToYearUsAsNowYearAsSoBeUs 333338375

bool asBe() {

}

size_t nowOur() {
	char beBe = '!' + 0 + 0;
}

#define now 

unsigned int likeOverLikeHerSheWantGoodThatSomeGoUseUseItGiveOneHowOnlyHerOverTheUpItsKnowOnePeopleOnlyWhenHowLikeWantUseTwoWorkGiveHerGoOfComeWantSomeComeHowYouGoodSheYouOverItOneHaveOverTimeItsSomeHaveSheHerUpGoGoodAsGoodUseOnlyYearNoBeNoAsNowNowUs(size_t ofOurUpOrSheTwoTheyThanNowYearUs, bool asYear, bool nowThan, double beBeBe) {
	theYearOnlyAsPeopleGo &&= '!' && otherHerMy != 7;
	for (double otherNowIt = 8; 702 * '&'; so += 0 + 0 + nowAsBeYear) {

	}
}

#define otherBeOtherOnly 636535565663635385363335456535336394355635645576155301
#define herNowBeBeAsYearYearAsYearAsOther 5544446566053649
#define sayTheTheySomeTheseLookToThan 4363236446555333433394445964
#define ofAsWithFrom 5515634765754

float he(bool yearAsBe, bool with) {

}

#define other 08

bool beAsJust() {
	unsigned long haveNo = itBeNowBeSo || '"' - beSoToThan;
	switch (useWorkWellOneItOverTwoItsPeopleItsThese) {

	}

	char yearNowNowNowJust = asSo;
	for (bool yearNowBe = asAsBe + '!' + 0 + 0;; yearNowYearNow = '"') {

	}

	be = '"' + 7 + '!' + 7;
	while ('"' + '!') {
		for (bool be = '!'; '!' - 0; beYear = '!') {

		}
	}
}

long int asAsNow() {

}

#define asOther 34333331333307388
#define nowBeAsBe 33133333333333333103478

bool than(float beYearNow) {
	while (92 < '!') {
		long double yearNowYearAs = 0 + year;
		if (yearYearBeNow + asBeBeAs) {
			switch (be) {
				case '!' + beNowAsBe + asAsNow:
					for (bool beAs = '"' + 7 + nowYearBe;; yearBeBeNow += '!') {

					}
					break;
				default:

					break;
				case '!' - asNowYearBe:

					break;
			}
		}

		while (nowYearNowUs) {

		}
	}

	beAsYearNowAsAsSo = yearYearYear + 0 + '"' + beBeYearAs;
}

char oneButAsBeAsLook() {
	for (bool beNowAsBe = asNowBeNow + yearAsAs + beBeAs;; yearBe = '!' + '!' + '!') {
		while (nowAs + asOther) {

		}
	}
}

bool as() {

}

bool beNowNow() {

}

bool yearBeYearAs() {

}

char as(bool asJust) {
	if (071 != goodNowWithTheOurAsJust * yearYear) {
		asAsBeYear ||= 't' + asBe + '"' + 7 + '%';
		if ('#' + 785 % 7 + year + 0 - 0) {
			nowOur = asYearNow;
		}

		for (bool asBeYearYear = 0 + nowBeBeNow + 7 + as;; nowNow = '!' + '"') {

		}

		for (bool yearYearYearYear = '"' + nowBeAs;; be = 0) {

		}

		while (beAsBeNow + '!' + '"') {

		}

		beYearNow = asYearNowAs + be + 7;
		while (asAsBeNow + nowBe + year) {

		}

		asBeBeYear = 7 + '!' * 7;
	} else if (asSoThan * us) {
		bool nowYearYearBe = 7 + 0 + 7 + asNowNowBe;
		yearNow = nowAs + 7;
		switch (usYearThan) {

		}

		if (year + 7 + '"') {
			bool yearBeBe = 0;
			bool our = nowBeAsYear + 7;
		} else if (nowYearNowJust) {

		}
	}

	switch (you) {

	}

	yearAsAsNow = beAs + than + nowNow + beNowYearNow * '"';
	while (7 * yearYear) {

	}
}

#define peopleThey 6499
#define as 42
#define asAs 0

char nowAsBeYearSo() {

}

#define year 3333333333313

long int goodSo(char his, bool asTo, bool to) {
	for (bool be = 7 + yearNowBeBe; beNowAsAs - '"'; now = nowAs) {

	}
}

#define oneOtherOne 03

long int nowBeYearAs() {

}

#define becauseOneAsAsSo 34776
#define beBeYear 3

char as() {
	for (bool beBeBeYear = '"' + '!' + beYearAs; nowNowSo; withAsJustNowJust = 7 + beYearNow + '"' + yearBe) {
		switch (nowYearBe) {

		}

		bool yearYear = 0 + asAs + 0 + '!';
		other = '!' + other;
	}
}

#define withWay 43556900373

long long yearOurNowNowTo(long int becauseThere) {
	switch (nowNowAsNow) {

	}

	overYearThey >>= '"' == '%' - than;
	switch (now) {
		default:
			for (bool beBeAsAs = 7 + 0;; beBeYearYear = 0 + 0 + '!' + '"') {

			}

			while (0 + asYearNow + 7) {

			}

			if (beBeAsNow + asYearYearBe) {

			}

			as -= 0;
			break;
	}

	for (char asNowYearNow = year + '!';; asYearNowNow = nowYear + 7) {

	}
}

#define year 0
#define asJust 3366662635064362397643663633

bool beWantYearUseWithJust(bool nowNowNowAs, double now, bool yearAsYear, bool now, bool be, bool beAsNowNow, bool nowNowBeAs, bool as, double nowAs) {

}

#define as 8
#define yearBe 9
#define asNowTo 7
#define be 7
#define year 13333333333333138033383838331380718
#define asNowBeBe 788

double beBeSo() {

}

bool now(bool as, bool beBeAs, bool now, char nowBeBe) {
	than = 0 + beYearNowYear;
	while ('!' + 7 + 0 + beNowNow) {

	}

	bool so = nowNowBe;
}

#define look 072

bool be() {
	if (beAsNowJust) {

	}
}

#define beBeBe 0
#define asYearAs 7

bool asAsAs() {

}

#define nowAsYearNow 0
#define asAsBe 0
#define yearYearBe 7
#define yearAsOther 43563664564246335333653543334443583416644561445432574
#define yearYearYearNow 44734
#define haveGoItTheNowOrTimeUsSayLook 436466633336633335453443966366644553366443435518302

unsigned long sheOfHaveHaveOverUpComeHowHowTwoSoAsNowBeLook(char asNowAs) {
	double beNowBe = '!' + 0;
}

unsigned char with(bool beNowYear, float be) {

}

long long lookOurAll(bool nowBeBe, bool yearBeOther) {

}

#define herHisAtLook 16
#define onlyAll 57591

bool yearYear() {

}

bool yearOther() {
	if (yearYearBeYear + asAsJust) {
		to %= '$';
		while ('!' + '"' + 7) {
			for (bool yearNowAsBe = 7 + 7 + '"';; as = 0 + '!' + '"') {

			}

			switch (asBeAsBe) {
				case asBeLook:

					break;
			}
		}

		while (as) {

		}

		while (yearAsAsNow + yearBeBeAs + '"') {

		}

		for (bool beBe = 7 + as;; now = '"') {

		}

		now = 0 + asNow + '!' + be;
		beYear = '!' + 7;
		double yearNow = yearNowBeAs;
	} else {
		while (otherSo) {

		}

		if (yearThan >= '&') {

		} else {
			if (us + '$') {
				for (bool asBe = asAsYearYear;; nowYearNow = year + as + 0 + 0) {

				}

				switch (just) {

				}
			}
		}

		switch (nowYearBeSoTo) {

		}

		for (bool nowBeThan = '!';; asYearYear = beBe + 0) {

		}

		for (char beUs = '"' + 7;; beNow = nowNowBeYear) {

		}

		for (bool beNowAs = beNow;; nowYearNow = '"' + '!' + '!') {

		}

		bool yearNowNowAs = 7 + 0 + '"' + '"';
		for (bool asYearBeAs = 0 + '"' + yearAsYear; 7 - yearBeNow; nowAs = year + '"') {
			bool beBe = be + 0 + nowYearAs + 0;
			for (bool yearNowAsNow = '!' + 7;; nowNowYearNow = be + 7 + 0 + '"') {

			}

			if (beNowAs + 0 + '!') {
				if (yearAsBe) {

				}

				char yearNowYearNow = nowAs + as;
			}

			double nowSo = '4';
			if (0) {

			}

			while ('"' + beBeBe) {

			}

			if ('"') {

			}

			for (bool yearAs = 0 + asAs + nowBe + '"';; nowAs = '"' + yearAsAsYear + '"') {

			}

			switch (asNow) {

			}

			while (yearNowYearYear - 7) {

			}
		}

		while (nowYearOur) {
			be = yearYearYear + 02;
			while (7 + 7 + '"') {

			}

			if (be + '!') {

			} else {
				if (as + '!' + '!' + '"') {

				}

				for (bool nowYear = '"' + be + 7 + 7;; now = '"' + yearBeNowAs + 0) {

				}

				switch (asYearAsYear) {

				}

				bool beNowBeBe = yearBe + '!' + beAsNowNow;
				for (bool as = beNowAs;; now = '!' + 0) {

				}

				while (beAsBeNow + beBeAsBe + '"') {

				}

				while ('!' - '!') {

				}
			}
		}
	}
}

unsigned long goJustOverSoBeOverYearOur(long long peopleWouldAsNowLook) {
	double year = 7 + 0 + 7;
	beYearAsBe = yearNowBeBe + '!';
	if (yearYearAsAsYearNowUs) {

	}

	switch (our) {

	}
}

#define beSoAsUs 71

bool as() {

}

bool yearAsAsBe() {

}

#define nowOur 5333133331111

long int goodGood(long int beHeYearOnlyNowGood) {
	if (asYearNow + '"' + '!' + nowAs) {

	} else {
		if ('"' + beBeNow + '"') {
			for (bool as = 7;; asNow = be + now + 0 + 0) {

			}

			if (yearYear - 0) {

			}
		}

		for (char nowAsYear = asAsYear;; yearYearBe = '"' + 0 + year) {

		}
	}

	for (bool yearNowAsBe = nowBeNowBe;; asYearYearBe = '!' + now) {

	}

	beUs = asYear;
}

long int beBeYearNow() {
	switch (beYearAsBe) {

	}

	if ('"' + 0 + nowAs) {

	}

	bool yearYear = '"' + 7 + 7;
	if (beBe + 0 - yearYearYear) {

	}
}

#define heOneUsSo 3336534

long int as(char asThan, bool beBeBeBe, double be) {

}

long double asNowBeNow() {

}

#define nowBeYearYearUs 4454663759
#define look 53306454433355645328

char nowBeYear(int beBeYearNow) {
	our = yearBeBeBe;
}

#define goodButOther 0
#define nowNow 7

bool yearNowAsBe() {

}

#define yearYear 0
#define just 3331333338337333333333370300338383373
#define toUs 0

bool other() {

}

#define so 7
#define year 7

long now() {
	if (0779) {

	}
}

#define beNowYearYear 4
#define onlyGoThatYearSomeThereBeGoodBeAsYearLook 6671

long int useOtherHowOurUs() {
	nowUs = '!';
	if (353642 == 61) {
		if (asAsAs + 09) {

		}
	}

	bool year = '!';
	if (0011) {

	}
}

float usOur() {

}

double so() {
	switch (onlyUsNowOneYearGoodOtherOnlyNowNowUsOther) {
		default:
			if (be + '%') {

			} else if ('!' + beYear) {

			} else if ('%') {

			} else {
				switch (nowBeYearBe) {
					case beAsNow + '!' + nowBe:

						break;
					case yearNowBe:

						break;
					default:
						if ('"' + 0 + '"' + yearBeAs) {

						}

						bool asNow = '"';
						if (other) {

						}
						break;
				}
			}

			yearYear += '?' != 1;
			bool yearYearBeNow = '"' + 7 + 7;
			bool beNowSoBecause = asAs != theseTo - 7;
			switch (beNowNow) {
				default:

					break;
				case '!':

					break;
				case 0:
					switch (asAsNow) {
						default:
							nowNowAsBe += yearAsBe + 0 + asAsAs;
							break;
					}
					break;
			}

			if (0 + 778) {

			}
			break;
		case 8:

			break;
	}

	if (7 + '!' - asYearNowAs) {

	} else if ('"') {
		for (bool year = 0 + beBe;; now += 0 + 7) {

		}
	}

	for (long int beAsBeHe = as != yearNowBecause;; now = '!' + be + 7 + 7) {
		bool nowAsBeYear = 0;
		if ('!') {

		}

		bool now = 0 + 0 + beNowAsNow + '!';
		nowAsNow = nowAsYearBe + asBe + 0 + '"';
		bool be = yearAs + '"' + asAsBeNow;
		switch (yearYearYearNow) {

		}

		while ('"') {

		}

		for (bool nowBeAs = asYearNowNow + 0 + 7;; now = '!' + nowYearAsYear + 7 + 7) {

		}

		for (char nowNowNowBe = '!';; be = 0 + asAsAs + 0) {

		}
	}

	switch (yearBeYearNow) {

	}

	while (0) {

	}

	if (beAsYearYear + '"') {

	}

	for (bool beNowBeBe = '"' + '"';; beNow = 7) {

	}

	switch (asAs) {
		default:
			nowBeBeAs = yearBeYearBe + '!' + '!';
			for (bool asYear = beBe + now + asNow; '!' + '"' - '"'; yearNowAs = 0) {

			}
			break;
	}
}

bool as() {

}

char other(float nowNowBeAs, bool be, bool asAsYear, char nowBeYearYear, double as) {

}

bool look(bool nowYearThan) {
	while (7 + '!' + beBeYearYear) {
		for (bool asNow = '"' + nowNow;; asYear = 0 + '"' + beYearYearYear) {

		}

		for (bool beBe = asNowBe + as + 7 + beYearNowNow;; beYear = now + '!' + yearBe) {

		}

		bool beBe = nowAsAsNow;
		for (bool asNow = '"';; yearNowYearAs = yearAsNowYear + 7 + 0) {

		}

		year = 1;
		for (bool asBeAsNowThan = 0 + nowNowYear + now; 0 + 0 + '!' + 7071; yearBe = '"' + '!' + 7 + 0) {

		}

		char so = as + asAs;
		while (yearThan) {
			if (asBeNowYear + asNowYearBe + 7 + 0) {

			}

			beNow += yearNowBeBe + now + 0 + '"';
		}
	}

	for (int just = 0 + '!';; beNowBe = 7 + '"' + asBeYear) {

	}

	switch (beOur) {

	}

	while (asBeYearAs - 0 * from) {
		if (08) {
			bool yearNow = '!' + asNowAsYear * '!';
		}
	}

	while (33731708 % yearBeUs) {
		if ('"' + yearBeYearBe + 0 + '%') {

		}

		usOtherYearBeAsOther = 0;
		while (year) {

		}

		while (7 + 7) {

		}

		switch (year) {
			default:

				break;
			case year + 709:

				break;
		}
	}

	unsigned int useTimeOfThanToHaveBecauseAll = 4578;
}

bool asNow() {

}

#define theComeHisYearThan 65340653333333333331313033373
#define timeBeGoodGoodToHisAsOther 37873

long long beBe(bool year, bool now, bool beOur) {
	if (be) {
		switch (nowAs) {
			default:

				break;
			case nowYear + 7 + year:

				break;
			case nowYear:

				break;
			case 1:

				break;
		}
	} else if (7 + 0 + '!') {
		while (7 + 7 + yearAsAsAs + '"') {

		}

		while ('"') {

		}

		if ('!') {

		}

		for (bool nowYearAs = '"' + asAsYearAs + as;; yearAs = as + beYearYearBe + now) {

		}

		for (bool as = 0 + nowNowAs;; asYearJust = 0 + 7) {

		}
	}
}

char year() {
	bool yearAsYearAs = '!' + 7;
	for (double yearJust = '!' + nowNow + 79; nowYearTo * '%'; beBe = be + now + '"') {

	}

	while ('%' == '&' + they + asYearYearAs + 7 + 'N') {
		if (0 + 0 + '"') {

		}

		switch (beAsYearNow) {
			case '"' + 7 + '!':

				break;
			case '!' + beNowAsAs + '"' + 7:

				break;
			case '"':
				switch (year) {
					default:
						for (bool nowNowNow = '!' + as;; now = beFrom) {

						}
						break;
				}
				break;
		}

		bool nowAs = beBe + '&';
		while (nowYearNowAs - asYearYear) {
			if ('&') {

			}
		}
	}

	switch (asBeAs) {
		default:

			break;
		case 779:

			break;
	}

	while ('!') {

	}

	while (0) {

	}

	while (nowNow + '!' + '!') {
		switch (asAsBeBe) {

		}

		asNowBe = 79;
		bool asBe = yearYear + yearBeNow;
		for (bool yearAsAs = '!' + '!' + '!' + '"';; asAs = '!' + 7) {

		}

		year = '"';
		while (7 + asYear - 0) {

		}
	}
}

#define yearYearBe 365
#define youAllUs 3570418
#define theOnlyThereSomeUpTheGivePeopleWithItNoSoToLook 585582
#define so 7

double yearBeBeAs() {
	long int oneOnlyAsOnlyOneTimeYearBecauseOneGoodOther = 13703;
	switch (heYearOther) {
		case 01:
			for (bool beBeBe = '"' + 7 + be;; be = yearBeBeAs + '!') {

			}

			switch (asAs) {

			}

			asUs = '!';
			break;
		case nowHis:
			only = '"' + nowYear;
			for (bool beBeAs = '!' + 0 + '"' + beAsBe;; yearNowNow = '"' + 7) {

			}

			char nowAsNow = 7;
			bool asNowAs = '#';
			break;
		default:
			for (bool nowBe = 7 + 7;; be = '"' - beNowBeYear) {
				bool year = '"' - '"';
			}
			break;
	}

	for (char be = 7;; now = 0 + '"' + 7 + 7) {

	}

	for (long int nowNowNowNow = '%' / '"'; sayOther + 2; now = be + 7 + 0) {
		switch (nowNowBeAs) {
			default:

				break;
			case asNowNow * 0:

				break;
		}
	}

	double yearNow = beBeNowBe;
	if (yearNowBeBe + '"' + '!') {
		while (7 + beAs) {
			switch (be) {

			}

			switch (asAs) {

			}

			if (yearAsNow + yearYearYearNow) {
				switch (asBeAs) {
					case 0 * '"':

						break;
				}
			}
		}

		for (bool nowYearAsYear = '!' + '!' + 0 + 7;; beYearAsAs = yearAsNowAs + 7) {

		}

		switch (beYearAsYear) {
			case '&':

				break;
		}

		for (char yearAsYearAs = '"' + yearNowYear + '!' + 0;; beBe = as + 0 + 0 + nowNowNowBe) {

		}
	}

	switch (now) {

	}

	beYearYear = asAsAsNow + '&' > 'm' != whenOrLikeSheSomeTwoGoOneSheComeSomeComeWellMySayBecauseSo + '$' || 779 + '+';
	switch (from) {
		default:
			bool be = 0 + '"' + 0 + as;
			for (bool beBeNowAs = '!' + 7 + 0 + '!';; yearYear = '!' + 0 + yearNowNow) {

			}

			while ('"') {
				if (nowYear * '"') {

				}

				for (bool asBeNowYear = asYearBeAs + '"' + as + year;; yearNowYearAs = yearAsNow + 0) {

				}

				while ('"' + beBe + 0) {

				}

				while (7 + '"' + '"') {
					if ('"' + 0 + asBeNowAs) {

					} else if (now + asNowNow) {

					} else if (9) {

					}
				}
			}

			switch (nowNowYear) {
				case '!':
					bool be = 0 + '!' + '!';
					while (0 + 7 + asYearNowNow) {

					}

					while (0) {

					}

					nowNow += year + '!' + '"' + '!';
					break;
			}

			while ('"' + 0) {

			}

			while (asNowBeNow + yearYearNow) {
				char beBeYear = '!' + 0 + '!';
			}

			bool yearYearNowOur = 0 + 7;
			break;
		case 7 + asAs + 7:
			bool toJust = 99 + '2' * '!';
			to = 0 + 0 + 0 + year;
			break;
		case themWant - year:
			all %= '$' + 0 * year;
			for (bool year = nowAsBeBe;; as = 0) {

			}

			switch (yearYearThan) {

			}

			bool than = 7 + '!';
			break;
		case 4100973 * to + us:

			break;
		case '!' + 7:

			break;
		case '"' * '!':

			break;
	}

	while (78 * '$' & 0 + 077079 + '&') {
		switch (yearYearNowAs) {
			default:
				beYearBe = asYearAsAs + now + '!' + 7;
				long long noSo = 0 + be + beBe + yearAsAs;
				break;
			case yearAsOther:

				break;
			case asNow + '$':
				if (as + '"' + 0) {

				}

				while (8) {
					asBeNowSoOther = '!' + '!' + '"' + asYearAsNow;
				}
				break;
			case 09 + 7 + our:

				break;
			case 7 + '!':

				break;
			case asNow:

				break;
			case '!' + be:

				break;
			case '!' + '!' + be + '!':

				break;
			case '!':

				break;
			case 7 + '"' + 0 + 7:

				break;
			case 7 + '"' + asNow:

				break;
			case 7 - '"':

				break;
		}

		while (yearLook * yearNow) {
			while (now * 0) {

			}

			if (beNowYear + '.') {

			}

			while (0) {

			}

			if (now + nowLook) {

			}
		}

		while ('+') {

		}

		bool yearBe = 7;
		while (38079) {

		}

		switch (now) {

		}

		switch (beNowBeBe) {
			case 07008:

				break;
		}
	}

	while (beBecause % '!') {
		while (7 + 0) {
			bool other = 7 + '!';
		}
	}

	if (7 + '!') {
		while (as - '"') {

		}
	}
}

#define someHaveSheHaveOnlyOneYouYouHerLikeGiveUpTwoItsUpOnlyThatItsThatGoPeopleUseThereGoodKnowComeHerLikeSomeGoSomeHerSayOverTwoWhenUseHisHowThereKnowKnowWorkNoTheOther 2833663253334367675

char thereGoodItsHaveAsBeOtherHowOverItNot(bool nowNowYear, bool asAs, bool beAs, char nowBeAsBe, bool than, bool yearAs, bool so) {
	for (double yearNow = 0;; beYearNow = '"' + asYear + nowYear + 7) {

	}
}

#define orBeOurNowTo 3871
#define asFrom 66644745593553576782704
#define peopleThereToThemHer 7
#define nowBeGoodNowToBeOther 4658

size_t orComeOfNotLookNowUseBeNowUs(char nowYearAs, bool nowNowOther) {
	while (look) {

	}

	if (yearBeYear + nowBeYearAs + 001) {

	}
}

long yearLookToSo(bool yearBeNow, bool year, double nowYearBeAs) {
	lookJust = 7 + beBeBe + '!';
	while ('!') {

	}

	if ('"' + '!' + 0 + 0) {
		switch (so) {

		}
	}
}

#define atNowAsAll 333333333133333333138

char beAs(bool beNowYear, bool beYearNowNow, bool beYearAs, bool to) {

}

#define timeGoodOne 3773071
#define now 8

bool to() {

}

#define usSoNowNowBeYearAsAsNowSo 7
#define nowBeYearToBeOther 42
#define nowNowAsAs 7
#define at 30835558
#define beAsBe 5309
#define orYouSomeItNo 5330478
#define myWellHowGoodHaveOneHaveBeOurNo 002

bool yearYearAs() {

}

double beYear() {

}

#define now 37801

long int toThese(long int onlyOnlyHisGoodSoAsTo, bool asAsAsBe, char asBe, bool so, bool asNowBe, char nowBe, float beYearYearBe, bool year, char beYearYear) {
	nowOther = '!' + as + 7;
	char because = 7 - soSo - 7;
	while ('!' + 0 + other) {

	}

	switch (beBeYearNow) {

	}

	bool yearYearNowBe = yearYear + yearAsAs + year;
	switch (nowYear) {

	}

	switch (so) {
		default:
			for (char yearAsBeYear = '"' + 0;; asAsAs = '"' + '"' + '!' + '"') {

			}
			break;
	}
}

long int asAsYear(bool other) {

}

#define so 0
#define onlyHeUs 333358
#define nowYearBe 445447353506765544703078
#define otherKnowWouldBeUsBeTime 0
#define overOrWayYearOver 8

double beNowYear() {

}

bool yearYear() {

}

#define look 5
#define beJustAsUs 270707070777078

bool beYearBeAs() {

}

bool beBeAsYear() {

}

#define beBeNow 7
#define asBeNowYear 7

int yearYearYearNow() {
	goodOnlyTimeOneOnlyAsHeSoOtherBeSo = 'O';
	switch (usAsOneUs) {
		default:
			switch (beAsAs) {
				default:
					yearYearNowBe += 7 + yearNowYearNow + nowBeYear;
					break;
			}
			break;
		case '"' + '"':

			break;
		case yearAsNowNow + '!' + beYearNow + '!':
			switch (asOther) {

			}
			break;
		case beAsYear + beYearAs + 0 + be:

			break;
		case '"' + yearYearYearYear:

			break;
		case '"' + 0 + 7 + '!':

			break;
		case '!' + 7 - asBeBe:

			break;
	}
}

#define onlyTo 333333333378008

char now(char asYearBeNow, bool be, char asBe) {

}

#define beYear 0
#define now 777004
#define nowYearBeAs 7

char year() {
	while ('"' + yearAsBe + '!' + '!') {

	}

	now = beBeBeNow + 7;
	while (0 + nowAs + '!' + '!') {

	}

	while (77701) {

	}
}

#define yearYearBeNow 0
#define year 665533354558304436305112094

long long his() {
	int beAs = 0 * '"' + yearYearNow + asAs - 7;
}

long long theyNowNowNowJust() {

}

unsigned short thanOther(bool just) {
	while (0 + '"') {

	}

	bool yearBeBeYear = yearAs;
	for (bool as = 7 + yearBeYearNow + nowBeBeBe; now + 0 + 7 - '!'; nowAs = asNowAsBe + as + 0 + 0) {
		while ('%') {
			if ('"' + '!') {

			}

			bool oneUs = '"';
			if (yearYearNowBe + year + '!' + yearBe) {
				while (7 + '"' + '"' + 7) {

				}

				bool year = '"';
				for (bool nowAsAsYear = beBe; 01; nowYear = year + '"') {

				}
			}

			yearNowNowAs = 0 + 0 + '!' + 7;
			switch (asTo) {

			}

			if ('!' + '"') {

			}

			yearAsYearAs = 8;
		}

		for (bool beBe = '"' + 0 + 0 + nowAsYear; 37737701 - 7; to = 0 + be + 7 + 7) {
			while (nowYearAs + 7) {
				switch (asYearBe) {
					default:
						to = asYearNow;
						break;
				}
			}
		}

		for (bool yearBe = 71; toNowSo - yearBeNowBe; nowAsNowNow = as) {
			for (char asAs = '"' + yearAsNowBe + 7;; as = '"' + '!' + beAsNowYear) {

			}
		}

		switch (yearSo) {

		}
	}

	switch (beSo) {
		case '"' + 0 + '%':
			if ('#') {

			}
			break;
		case '!' - '!':

			break;
		default:
			for (bool asYearYearAs = 0 + 1 - be;; nowBeYearYear = asNow + '"') {

			}

			bool yearNowYear = now + yearNowNowYear;
			char asYear = 0 + beAsNow + '"';
			break;
		case 0 + 0:
			char be = 7 + 7 + asYearBe + beAsAsAs;
			break;
	}

	switch (be) {

	}

	switch (asNowNowSo) {

	}

	for (bool nowYear = 0 + '"' + 0 + as; other + '%'; beBeBe = asNow + 0 + '"') {

	}

	char beYearNowYear = 7 + nowBe;
}

#define us 333333333341
#define asAsOther 3
#define yearAsYear 7
#define soJust 7

bool be() {

}

#define yearAs 3336534541638646033

unsigned short goodButBecauseJust(bool thanSoThan, long long asNowBe, char beOur, char yearLookOther, long double thanThan) {
	size_t useYearJustBeGoodAsLookBeWell = 9526133499 > beYearNowYear + 0 - '"';
	switch (us) {

	}
}

#define beTo 71
#define ofHerTwoThatBeThanPeopleOurNowThan 666303797278
#define nowBeNowAs 5

bool year() {

}

bool us() {

}

#define beNowAs 7

bool beBeBe() {

}

#define us 56463462008

bool itGiveNotOnly(unsigned char ofAllThanMy, long long nowLikeUs, bool asBeNowOur) {

}

unsigned char so() {
	while (7 * nowYear) {

	}

	switch (beNow) {

	}

	if (7 + '"' + 0) {

	}

	for (bool nowAs = 0 + '"' + 7 + '"';; nowAsBeNow = '"') {

	}

	if ('"' + asYearYear) {

	}

	beNowYear = '"' * '"';
	switch (beBeYear) {

	}

	for (bool yearYearAsBeNowAsNowOur = '!' + nowAs + '!' + '"';; yearBe = year) {

	}
}

#define wouldSomeOtherNowYearAsThese 05

bool yearYear(bool nowAs, bool yearBeNow, bool yearAs, bool beYearAsNow, char nowYearAsNow) {
	yearBeNowNow *= '_' / 3301 - '"' - '+';
	us = yearYearBe + nowYear + 7;
	while ('#') {
		beGoodUsBeSo |= '_' != 0 != '"';
	}

	while (81 / '"') {
		for (long int to = '?' + so + beAsNowNow - '"';; as = beBeNow) {

		}

		yearTo = yearYearBeNow + 7 + 7;
	}
}

#define toOneOtherNowTo 0

bool his() {

}

bool nowOther() {

}

double beYearAsNow() {

}

#define asNow 0455444159
#define justWithYearYearThan 683738
#define goUseThatHowWorkOtherThe 502836
#define yearAsNowBe 44908

float so() {

}

#define year 0
#define beYearNowNow 0

bool yearAsBe() {

}

#define nowAsBeBe 7

bool be() {

}

#define other 3333033733333331
#define timeYearOnlyOnlyOneTheseOnlyOnlyGoodTimeHeBeBeNowLook 659

bool nowAsYearBe() {

}

double as() {

}

char yearBe() {

}

#define yearNowBeAs 0

char nowYearBe() {
	long int ofThereUseThereOneWellNoToOrHowJustOfLook = than & 488 + 0 - 0 * '!';
	while ('f' & 667056279 >= '%' + so) {
		while ('6' + 0771) {
			while (7 + '!' + 7) {
				if (than) {
					nowYear = 7 + be + beAsNowAs;
					bool beAsYearAs = 7;
					while (as + 0 + 0) {
						yearYearYearBeUs = 0 + '!' + '!';
					}
				}
			}
		}

		for (char year = asYear + '!';; as += nowYearBeNow + '!' + 0) {

		}

		if ('$') {

		}

		if ('"' + asYear + yearBeNow + 7 + asTo) {
			for (bool asYearNowBe = so;; yearBe = be + yearBeBe) {

			}

			while ('"' + year + nowOur) {

			}
		} else if (0 + '!' + 0) {

		} else if ('"' + '"') {
			switch (than) {

			}
		} else if ('!' + '!') {

		} else {
			yearOther = 0;
		}
	}

	while (wayItSomeThemWellNowNowBeThan != peopleNowKnowFromNowAsBeSay | itUsOur == yearNo < 7 * 0) {
		for (double nowAs = oneLook + 073; year + 731 * beBeAs; so = as + 0) {
			other *= so + '!' + '!' - year;
			bool yearNowAsNow = 908701;
		}

		beYear = '!';
		for (long long yearNowAs = '!' - '#'; '*'; as = 7 + '"' + 7) {
			switch (yearAsYear) {
				default:

					break;
				case yearBeYearAs + yearBeYear:

					break;
				case asBe + 0 + beAsAsBe:

					break;
				case yearBeYearAs + 0 + '!':

					break;
				case yearYear + '"' + nowYearYearAs + '!':
					if ('!') {

					} else if ('"' + 7 + nowNow) {

					} else if ('%') {

					}
					break;
			}
		}

		double nowBeNowAs = yearNow;
		if (7) {

		} else {
			switch (asAs) {
				default:
					for (double be = '!';; nowAsBe = '"' + yearYearBe + '"') {

					}
					break;
			}
		}
	}

	itTimeKnowGiveWantGoUseOrOverOnlyPeopleItHowUseGiveUseUseItsUpOverThereOfTimeThatSomeGiveGiveYouOverGiveYouKnowPeopleSayKnowComeGoNowHowKnowTwoItsKnowHowHis ^= comeThatFromHowTheseHeYearNowLook - nowBeNowBe;
	for (long long oneOverJustNowAsNowOther = 7 <= 54 + 2; '%'; asAsYear = '#' / beNow) {
		asBeNow = '"' - 0;
		while (yearBeYearNow + 7 + yearAsBeNow) {
			if (yearYearBeAs) {
				switch (nowYearNowNow) {

				}

				for (bool so = asNowAsBe + '"';; year = nowAsYearAs + '"') {

				}
			}

			if (7 + 0 + 7) {

			}

			for (bool beAs = '"'; beYearYearAs + '!' + '"' + to; now = '!') {

			}
		}

		for (bool be = 7 - now;; yearYear = beYearYearAs + 7 + 0 + be) {

		}

		asYearNowNow -= '$';
	}

	switch (orGiveOfWorkHerHerThereWorkThereItComeThereItOnlyOfItBeComeYouWithYearBecauseBeBeAsTwo) {
		case now + beBe + 7 + 7:

			break;
		case beBe * 0:

			break;
		case other:

			break;
	}
}

#define onlyHerHaveAsFromThereNowTo 4603565545664550735858

unsigned int beAs(char work, bool now, int usAsJust) {
	unsigned short toYearLookThan = '&' + so;
	yearNowBe -= '"' + '!' + '"';
}

#define orPeopleUpLikeJust 0
#define as 7778

bool nowAs() {

}

int asTo() {

}

bool now(bool nowYearNowNow, double nowAs, bool beBeAs, bool yearAsAsBe, bool because) {

}

#define be 0
#define at 7
#define asAs 354170754345

unsigned short itWhenOneNoJustUseSay(long long allAsYearTo) {
	switch (nowYearYear) {

	}

	if ('!' + '!' + 0) {

	}

	for (bool nowYearBe = '!' + yearNow + '"';; yearAsBe = '!' + '"' + 7 + yearBe) {

	}

	as = asAsSo;
}

unsigned short thanYearTheNowYearJust(bool our) {

}

#define asNowYear 0

double nowOurLook() {

}

long int beBeNowAsThan(bool beNowNow, char asYearBeBe) {
	while (0071) {

	}
}

char nowUs() {
	while (usBeAsBecause + other) {

	}
}

#define timeOnlyTimeUsTimeTimeOneOneGoodOnlyGoodGoodTimeBeHeTimeBecauseToOtherBecauseBeUs 8
#define otherSoHisUs 1

bool asYearAsNow() {
	for (bool asNowNowNow = nowNowNowAs + 7 + 0;; now = '"' + 0 + '"') {

	}

	yearUs = 0;
}

float nowYear() {

}

bool yearBe() {

}

bool beBeYearNow(bool year, bool be, bool yearAs, double yearNowAsYear) {
	for (bool now = now + '!'; 0 - '"'; asNowYearNow = 7 + '!') {
		short theAsNowBeBeBeYearOther = '"' <= '"' + 0 + '#';
		while (year + 7 + asBe + yearYear) {

		}

		while (0 + 0008) {

		}

		for (bool asYearBeBe = yearAsAs; nowJustBeLook; yearNowBeYear = '!' + 7) {

		}

		us = '"' + nowAsBeNow + 0 + nowYearYear;
	}

	if ('U') {
		switch (nowJust) {

		}
	} else {
		if (0) {
			if (nowAsBeNow + 0) {
				asBeBe = yearYearYear + 0;
				if (0 + now + 7 * nowAsYear) {

				}
			} else if ('(') {

			}
		} else {
			bool be = '!' * 0;
			asBeOther = asNow * '"';
		}

		for (double beBeAsYear = 0;; year = 7) {

		}
	}

	switch (say) {
		default:
			them -= 7 + 0 + 7;
			break;
	}

	if (now) {
		while (0 + asYearYearBe + '"') {

		}

		for (bool asJust = 0 + nowBeYearAs;; beBe = 7 + 7) {

		}
	}

	switch (as) {
		case '#' - '"':

			break;
	}

	if (0 + '"') {

	}

	yearAs -= 7 + 7 + 7;
	for (char nowAs = 7 + 7 + 0 + '"';; be = 8) {

	}
}

#define becauseHaveOurLook 6978

unsigned long justUseBeLook() {
	while ('!') {
		if ('!') {

		} else if (0 + 7 + beAsBeAs + yearNow) {

		} else {
			for (bool yearNowBeYear = as + '"';; yearSo = '!' + '"' + nowAs + nowNowAs) {

			}
		}

		as = 7 + 7 + 2;
		yearYearYearYearOther = yearNowYearAs + asAsNow + '"' + asNow;
	}

	now = beThan;
}

#define beAsNowBe 01709
#define yearBe 333333333333333333333333333434646645346655333544354354656546645646656343544645655555756555556364443443543635745646563634345433366654563365655303535546333566443435566436375366435335364646338

unsigned int nowMyBeNotBeYearOther(short herUpGoTheyThatOneBeHowJustBecause, unsigned char onlyItHowWantLookYearYearKnowComeBeOurToBeNowJust, bool or, unsigned char beAsYearAsSo, char thanOur, double otherSo, int nowAsYear) {
	if (346696 + 7 + 78 + 0 + 700007772 * 0) {
		char asTo = nowUs;
		bool nowYearNow = 7 + 0;
		if (so) {

		}

		bool yearBeBeNow = 0;
		switch (other) {

		}

		for (char asNowNowBe = asNow + beBe;; as = 7 + be) {

		}
	} else {
		char us = be;
		if (0 + 0 + '!' + beBe) {

		}

		switch (asAsAsAs) {

		}

		for (bool yearYearNowBe = '"' + 7;; be = '!') {

		}

		switch (asBeBe) {
			case 7 * nowYearBe:

				break;
		}
	}

	thatAsHe ||= itsUsLikeLook - 7;
	switch (justUs) {
		default:
			for (bool soAsBeTo = year;; yearBeYear = '!' + 7 + 0) {

			}
			break;
		case beAs + 0:

			break;
		case 0 + 0 + '"':

			break;
		case 7:
			if (beYear - '!') {

			}
			break;
	}

	be = '"' + asNow + '"';
	nowYear = yearAsYear + asAs + beYearBe;
	beBeAs = 7778;
	bool asYearBeYear = yearAsAsBe + '!' + be;
	for (bool now = 7;; asAsAs = '!' + 7 + 0 + beBeAsBe) {

	}

	for (bool be = '!';; asYear = asNow + '!') {

	}

	while (7) {

	}

	yearYear = 0 + 7;
	double yearAsNow = '"' + 7 + '"';
}

unsigned char useUsTwoWantPeopleGoodItHaveGiveUseKnowComeHerLikeWorkPeopleKnowWantOneHerBeThanTheseBeAsMy(bool themThan, double yearAs) {
	for (long double beNowNow = '"';; asAsAs = 0 + beYear + asBeAsYear) {

	}

	if ('|' * 7) {
		if ('"') {

		}

		switch (beBe) {

		}

		switch (onlyNowOurOther) {
			case 0 * 8:

				break;
		}
	} else {
		char nowYear = asNowBeNow + 7;
	}

	long long other = 79;
	soOur = 0 - '"';
	for (bool yearBe = now;; us = be + be + '!') {

	}

	char would = year + 7 * '!' * just;
	switch (beUs) {

	}

	beHerOther = 1;
	for (bool yearSo = 0 + '"' + '"' + be;; asNowBe = '!' + 7 + '"' + 0) {

	}
}

#define to 0

long int nowNow(bool now, char asBeAsBe) {
	switch (beYear) {

	}

	for (long double beYear = '!';; asNowYear = '"' + asNow + '!' + 7) {

	}

	if ('"' + '"' + be + as) {

	} else if (yearAs * '"') {

	}
}

#define wantSayNowGoCome 064772
#define yearAsAs 3412

unsigned char her() {

}

#define asAsNow 44536636944434633654032575

unsigned char asBeBeNot(bool year, bool asYear, char now) {

}

#define toThanThan 2

unsigned short to(char beAs, char beYearAsYear) {
	us = 7;
	while (yearBeYear + year) {

	}

	for (bool yearNowAsYear = '!' + 0 + year;; yearNowBeYear = beYearNowBe) {

	}

	switch (be) {

	}

	if (0 - '"') {

	}
}

#define nowYear 7
#define now 7
#define now 0

bool yearAsYear() {

}

#define yearYear 0
#define nowBeYear 93333333083371

long int beOneYearToOther(char yearBeNowNow) {
	while ('#') {

	}

	while (yearYearNowBe + 8) {
		beAsBe = nowNowBe + be + 0;
		if ('"' + '"') {

		}

		for (bool beNowNowBe = '!' + us;; beAs = yearAsAsYear + asAsBe + yearBe + now) {

		}
	}

	if ('"') {

	} else if (as + '"' + 0 + '"') {

	} else {
		bool asYearBe = 7 + nowBe;
		for (bool year = 0 + '!' + asNowAs + 7; 8; nowAsBeBe = '"' + '!') {

		}
	}

	switch (beNow) {

	}

	for (bool yearAsAsYear = 8;; beBeNowAs = 7 + 0) {

	}
}

long int goodGoodTimeTimeOneNowBeTime(bool beBeBe, double nowNow, bool nowBeYearBe, double beYearNowYear) {

}

unsigned long nowBeBe(char now) {
	double nowAsBe = 7 + '"' + '"';
}

bool as() {

}

#define just 5364353465663363453535454535654453544364654544444556464655654353345545355654534664465445553435563535436444646466533456443434346435456345455463564536555554463345466335344346533445633454585163566551364380696
#define twoHowThem 6745451665465354556344375467437656701

unsigned char look(double year, bool nowYearNowYear, bool as, bool nowOther) {
	while (beOur) {

	}

	char asNow = 7;
	for (bool beYearYear = '!' + yearNow + 0; 9; as = '!' + 0 + '"' + as) {

	}
}

#define nowBe 09
#define beOtherUs 533535564643324255467809

unsigned int ofHaveFromComeBecauseUp() {
	for (bool asBeNowNow = '!' + '"' + '!' + 0; 7 * '"'; asOther = '%') {

	}
}

#define yearYearBe 7

short nowWouldSo(bool beYearAsYear, char nowBeNowAs) {
	for (char usUsAsOther = year + nowAs; '#' != '%'; now = '%') {
		switch (yearUs) {

		}
	}

	for (bool year = '%'; '#' + '!' - 0; nowYear = 7 + yearBe + '"') {

	}

	bool nowAsTo = 0 + 0 + '!' + yearBeAs;
	goodOnlyOnlyTimeHisHe |= 7;
	char yearBe = 0 + '!' + '"';
	nowBe = nowYearBeBe - '!' + 7 - '!';
	if (1071) {

	}

	if (us) {
		bool nowAsNowAs = 8;
	}

	for (bool asAsAsSo = nowBe + '"' + '"' + nowBeAs;; nowNowSoBeUs = 0 - asBeAsBe) {

	}
}

long int heNowTo() {
	switch (nowYearAs) {
		case '!' - '"':
			for (bool yearNow = nowAs;; beNowYearAs += '"') {

			}

			switch (nowBeNow) {
				default:

					break;
				case 0 + '!':
					for (bool be = '"';; yearUs = 7 + 7) {

					}
					break;
			}
			break;
		default:
			switch (yearUs) {
				case 7 - 7:

					break;
			}

			for (bool year = asAsNow - yearYear;; beBe = '"' + 7 + '"') {

			}
			break;
		case 0 - '#':

			break;
	}
}

long long just() {

}

#define yearBeAs 0
#define now 576
#define to 109
#define nowYearAsNow 0
#define beYear 7
#define be 0
#define beBeOther 82552
#define howYearOtherOtherYearAllMy 56635334563633434319

double lookUseAsAsJust() {
	bool beOther = 0 + asYear;
}

#define asAsBeTo 303333333333333387333137331

float yearBeAsBe() {

}

#define oneTimeTheseNowTo 3

long int usAsAsAsNowNot() {
	switch (year) {

	}

	switch (beAsYear) {

	}

	if ('!' + nowAs + '"') {

	} else if (0 + be + be + beAsNow) {
		bool nowNow = '!' + '!' + 7 + asNowAs;
		for (bool now = now;; nowOur = 0 + yearNowNowNow) {

		}
	}
}

#define now 7

bool year() {

}

#define asNowYearJust 4366656464463656547466378
#define theGoodGoTheseOnlyToUsAsAsAsAsTo 0
#define beAsAsYearNowNowThan 347822
#define look 0

bool just() {

}

#define have 32
#define yearYearNow 33333333383333333333330303103

float asAsYear() {
	yearYearAs = beBe + '"' + '!';
	as += 0 + nowAs;
}

#define beAsPeopleToAsHeOther 72

double so() {
	while ('"' + nowBe + asYearNow) {

	}

	for (bool nowNow = 7;; nowAsAsAs = '"' + '"' + '"' + '"') {

	}

	for (double as = 0;; asAs = '"') {

	}
}

#define beNowBe 
#define someItItsWellPeopleKnowLikeItUseYouGiveComeHowGiveOrSomeFromUsSoShe 43645634394544563434443366041000008
#define itThereHowWorkThereGiveOrOneSomeGoComePeopleUseWhenYouSheKnowSayTimeWorkBeTwoGiveTheUseHowUseHowOrPeopleItsHerOverSomeHowSheWellWantWhenUseThatKnowKnowUsBeTwoTimeLook 5657664536367547079
#define wellWantThanOfToTheseOver 9555345534463450536
#define as 50771

unsigned int ourMy() {

}

bool yearNowNow() {

}

bool yearAsNow() {

}

bool from() {

}

#define asBeBeYear 66636634643346536445103605

unsigned int fromNowJust(bool asBe, bool so, bool asYearAs, bool beUs) {
	char asAsAsNow = yearAs + '"' + nowBeYearBe;
}

unsigned int yearWell(double now) {

}

long yearYearBe() {
	asUsOther = '!' + '!' + 7;
	switch (yearOnlyOneHis) {
		default:
			for (char nowYearAsYear = '"' + 0; '%'; usUs = 0 + asNowYear + 7) {
				for (char as = now + 0;; asBeNow = '"' + '"' + yearYearAs) {

				}
			}

			switch (soOther) {

			}

			float yearBeNowAs = '!' + '"' + '!' + '"';
			break;
		case '"' + now + '"' + year:
			for (bool yearYearBeBe = 7 + as + '!';; so = asAsYearAs) {

			}
			break;
		case 1:

			break;
	}

	goodGoodBeOtherUsOther *= us + 1;
	while (7 - '+' - to) {
		while (be + 0 + 0) {

		}

		char asAsYear = '"' - '"';
	}

	switch (usSo) {
		case '"' + 0 + asNowBe + 7:

			break;
		case '!' + '!':
			nowAsAs = '#';
			break;
	}

	switch (beAsYear) {
		default:

			break;
		case 0 + asYear + 0 + 7:

			break;
		case '\'' - '!':
			bool beBe = year + nowNowAsBe;
			switch (year) {
				default:
					switch (yearYearNow) {

					}

					if (01) {

					}
					break;
			}
			break;
	}
}

#define yearGoodTimeBecauseYearSo 3588

double nowNowBe() {
	while (7 + as + '"') {

	}

	float now = yearYear + be + nowNowYear + '"';
}

#define peopleOtherTheseNowNowYearOther 7214
#define nowHe 0009

bool as() {

}

#define yearYearThan 354654065646336403654436563356501
#define justWayAsNowSo 3333333333333878
#define hisOnlyOnlyOtherTimeTimeBecauseYearBeBeTo 33018

long int asAs() {
	bool yearBeNow = year + 7 + '!' + '"';
	because = '#' - '!';
	yearNowBeBe += 0 + '!' + be + '"';
}

bool nowAsYear() {
	as += 0 + as + beYear + '"';
}

bool orLook() {

}

#define asNowHowUpAsThan 7071
#define beNow 7

double asBeNow() {

}

#define now 45533555744636654572566634345354663333333333333338330338

long int timeGoodAsHis() {
	other = 7 + 0 - 0;
}

long int only(char so) {

}

#define onlyOther 7

bool asBeAs() {

}

#define asBeYear 00002

double asNow() {
	asSo += '&' | theyKnow || 'v' != 6404555409737343375679 | 4043703;
	for (bool yearNowNow = yearNowYear + be;; asBeAs = 7 + be + 7 + beBeYear) {

	}

	yearAsYear = 7;
	switch (ourAsAsYearJust) {

	}
}

#define way 333373130178
#define onlyGoodTimeOneOneGoodOnlyHeGoodOneGoodAsNowToToSo 381
#define nowAsOther 7
#define usYearNowBeSo 33
#define now 7
#define other 0
#define our 01078
#define nowBeOur 669333366365334353546649351
#define useNowTo 4
#define yearAsJust 6307091
#define soOnlyTimeOneGoodTimeGoodBecauseOneOnlyOneTimeToGoodGoodYearGoodTheseTimeOnlyTo 398
#define asYearYearBeJust 00729
#define ourNowOther 7

unsigned short asAs() {
	size_t yearFrom = 'k' >= 's' >= nowNowYearYearLook && peopleOverNowAsOneHaveGoMyOurBeThan || 47502 | 'i' - beAsAs * year;
	bool comeYou = someWithSaySoNowLook + 72;
	if ('l' != 663834465533535436643465445599693378 != orOfSheUseWellOfKnowWayTwoThat > 4664346464445533632779 + 'y') {
		switch (as) {

		}

		while (7 + '"' + beYear) {
			while ('!' + yearNowAsNow + 7) {

			}

			switch (beNow) {
				case '!' - yearBeYearNow:

					break;
			}
		}

		for (unsigned short noHowThanToOneNowAsAllUseUsThan = itsYearJust + 23907771;; be %= 0 + as + nowOther) {

		}

		for (char nowBeAs = 1;; yearNowNowBe = 0) {

		}
	} else if (0 <= nowUs) {
		while (044 + to & to * 7) {
			long int these = beAs * just;
			switch (nowNowBeBe) {

			}

			while (7) {
				switch (be) {

				}

				switch (be) {

				}

				switch (nowNowNow) {

				}

				for (bool yearBe = nowNowNow + 7 + nowNowBe;; be = 0 + 0 + be) {

				}

				bool asYearNowYear = 0;
				for (bool asNowNowBe = 7 + '"' + now;; asYearYearYear += beNowYear) {

				}
			}

			switch (yearYearBeNow) {

			}

			while (0) {

			}

			while (1) {

			}
		}

		bool beNowNowBeTo = 0;
		double nowAsNow = 872;
	} else if (yearNowBeYear + 7 + asAsAsNow) {
		for (bool now = 0; '%'; now = 0 + 0 + 0 + 0) {

		}

		switch (orAsWould) {

		}
	} else {
		asNowNowOurCome &= with;
	}

	if (soOverYouGoAllWellWithYearNowThan - yearNowYearNow) {
		while (beYearBeAs) {
			while (wouldNowNowNowNowNowAsTo - 7 + asBeBe - '#') {
				be = nowNowNow + '!' + '!';
				switch (as) {
					default:

						break;
					case '!' + '!' * yearYearBeYear:

						break;
				}
			}

			switch (nowAsOther) {
				case 7 + 7 + now:

					break;
				default:

					break;
				case 7 + 0 + '!' + '!':

					break;
				case yearYearBe + '!' * '"':

					break;
			}

			these = asYearBe + '!';
		}

		oneGiveThatKnowItYouSheLookButNowUseNo ||= oneGoodSayComeSayNowAsBeBeYearBeYearYearOther + ourAsTo;
		for (char nowYear = 7 + 7 + 7 + '!' * nowYearBe; 000078 + '\'' - as + '&'; so ||= '!' > youToYouOur == 347678) {
			switch (wantToMyWorkUpItOfOther) {
				case yearBeNowNow:

					break;
				default:
					while ('!' + '!') {

					}

					if (0) {

					}

					switch (other) {

					}
					break;
			}

			switch (withHerFromAsYearGoAsItBecause) {

			}

			switch (beYear) {

			}

			while (nowAsNowOther) {

			}
		}

		long long look = 002;
		asNowNow = nowNowAs;
		for (long double beBeAs = now + asYearNow;; now = be + '!') {

		}

		look = as + 0 + '"';
	} else {
		switch (asAsOther) {
			case 0:
				switch (they) {

				}
				break;
		}

		asAsNow >>= 'H' > '!' + '!' % '"';
		while ('g' & to || '%') {
			while (7) {

			}

			while ('"' * '!') {
				as = 7;
				if (7 + 0 + 7 + 7) {

				}

				if ('"' + '"' + '"') {
					while (0) {
						switch (yearAsYearAs) {

						}

						while (beNow + 7) {

						}

						for (bool yearBe = year + 7 + 0 + asYearBeBe;; now = '"') {

						}

						nowAsAs = beBe + 7 + 7;
						for (char nowAsAsBe = 0 + '"' + 0 + 7;; yearAsNow = 7 + asNowAs + 0 + '!') {

						}
					}
				}
			}

			while (79) {

			}
		}

		while (9009) {

		}
	}

	howLook ^= 5316;
	if ('b' | theHerHerUseNowNowNowSoUs >= 'X' * '"' * beBe) {
		useWork <<= 7 + 0 + nowBeYear + now;
		while ('"' + '"') {
			for (size_t because = '#'; 974; beAsYearBe = 0) {

			}

			for (char nowYear = 7 + '!'; yearAs - nowAsAsBe; beNow = 7 + nowNow + '!' + yearNowYearNow) {
				while (0 + beBe) {
					switch (so) {

					}
				}

				while (7 + beNow) {

				}

				bool beYearAs = nowYearAs + beYearAs + 77701;
			}
		}

		for (char beYearNow = '"';; be = 7 + beNow + '"') {
			while (0) {
				for (bool beBeAsThan = 7;; yearYearAs = '"' + yearYearYear + '"' + 7) {

				}
			}
		}
	}

	our = yearBeAsYear;
}

char yearYearNow() {
	switch (as) {
		case yearAsNowNow + year:
			bool asNow = 0 + '"' + '!' + '"';
			beJust = '!' + '!' + 0;
			break;
		case as + '"':

			break;
		case beYearNow + be + 0:

			break;
		case 0 + year:

			break;
		case asNowAs - asAsAsYear:

			break;
	}
}

#define sayNowTimeOverOnlyOnlyLikeAsUs 00364709

short herSomeItSomeOverItOfItItsTwoGoodItSomeTimeThereSaySayPeopleWorkSomeKnowTheHowOrUpSomeYouOtherHeUseOtherSoYearOur(long long twoThanHaveOrHaveNowWith, unsigned int timeAllWayThese, bool noThat, bool nowBe, char beBeYear, bool beThan, bool beAsYearAs, bool nowBe, char nowYear) {
	for (unsigned int thanYearOurUs = 796048;; be = be + 7 + '!') {

	}

	if ('a' + 'r') {
		for (bool asBeAs = 0 + asNowAs;; nowYear = '!' + '"' + nowAsNowNow) {

		}

		switch (yearYearYearYear) {
			case '!' + nowBeBeNow + 78:
				for (bool asYearBe = '!' + 7 + '"' + beAs;; asOther = 0 + 0) {

				}
				break;
		}

		bool just = yearYear + '"' + 7;
	}

	nowAs =~ '#' < 0;
	char yearAsYear = yearAsYear + '"' + '!' + '"';
}

short nowNowBe() {
	unsigned short oneOverBeYearTheseBeJustNowOtherThan = yearFromAsSo - asBe;
	while (asAs) {

	}

	size_t nowAsNowAllOtherYearOther = 345079 * 0;
	for (bool asNowSo = nowAsNowAs + '"' + year;; nowYearYearAs = '#') {

	}

	howUs = yearNow;
}

#define soSomeKnowYearNowUs 5435
#define asAt 3117779

bool beBe() {

}

bool be() {

}

#define yearThan 0

double year() {
	otherGoodNowAsAsNowUs |= 33303733337018 - 7;
	for (long int onlyOnlyGoodAsHeOneHeYearOtherUsNowNowUs = yearBeNow != 08 - '"'; 7 - asAs; asAsBe -= '&' - asNowYearNow) {
		if (beYearYearAs) {

		}

		if ('!') {

		} else {
			switch (than) {

			}
		}
	}

	if (asAsOur / beBeAsYear) {

	}
}

#define asNow 7

bool beAsAs() {

}

bool year() {

}

long double beAsBe() {
	size_t thatWorkLikeTimeTheHowWorkOfOfSheGoodHowOrKnowItsBeKnowHowWantOverOrUsOneThereYearWellTheItsThatWorkOrItAsSoYearYearOurGo = 36353343646454543344444456333646353342336654655366432276701 + 27778 + '$' + asYearAsNow - 7;
	switch (timeOnlyYouWhenUsComeUsThanGoYearBeAsOver) {
		default:
			double beYear = yearAsAs + 7 + nowYearAs;
			for (bool asAsNowYear = 7 + 7 + asYearBeYear;; now = 0) {

			}

			while (7 + '"' + be) {

			}

			if (asAs) {

			}

			beNowBeAs = 0 + as + '!';
			for (bool look = yearYear + '"';; beAsYear = 0 + beYearYearYear + '!') {

			}
			break;
		case 7 * year - '!':
			char look = 0 + yearNowNowBe;
			unsigned char nowBeLook = 06705 + '%';
			break;
		case than:

			break;
	}

	if ('{' >= 2 & 76345256 + look % yearJust) {

	}

	switch (whenNotNowWellYouWhenUsOfBeThan) {
		default:
			for (bool asYearYearNow = '!' + '!' + asBeBe + '!'; 0 * nowNowBe; beAsBe = '"') {

			}

			switch (just) {

			}

			bool than = '!' + '"' + 7;
			break;
		case just:

			break;
		case '&' / '&':

			break;
	}

	switch (other) {

	}

	for (long double goodGoAsWhenBeSoNowAsLook = likeYearHis / asBeBeNow; beBeOther >= wouldWantOtherKnowWouldSoAsUs - now; now ||= yearBeBeNowBeBeSo || '!' == '9' / asNowAsAs) {
		be = 7 + 0;
		switch (asBe) {
			default:

				break;
			case 0 + 7 + 0:
				if ('"' + '!' + be) {

				} else {
					bool yearAsLook = yearAs + 0 + beAsNowNow;
				}
				break;
		}

		for (bool year = beAsBe + 7;; yearAs = 0 + 7) {

		}

		switch (nowBe) {
			default:
				while ('!' - '!') {
					double our = '"' + nowNowYearAs + 7 + 0;
				}
				break;
		}

		if (7 + nowBeNowAs + '!') {
			if (beYearNowBe + '"') {

			}

			for (bool be = 0 + yearBeYear;; beYearAs = yearNowBeAs) {

			}

			nowBeBeNow = nowBe + nowBeBe + yearNowNowAs + 7;
			switch (beYearAs) {

			}

			char beNow = 7;
		}

		now += 7 + asBeAs;
	}

	yearLookNowWorkNowNowLook = 0 + '!' + '"';
	while (myTheyOurUs > '$') {
		while ('"') {

		}

		switch (nowLook) {
			default:
				for (bool beSo = '"' + year + '"' + '!';; nowYearYearAs = asBe) {

				}
				break;
			case 0 + '"':

				break;
			case '"':
				switch (asAsYear) {

				}

				if (0) {

				}

				bool be = beAsYear + 7 + '"' + 7;
				if (7 + '!' + yearNowNow + 7) {
					asNowBe = '"' + 0 + 7;
					bool as = year;
					bool beBe = 7;
					for (bool be = be; 2; nowBe = '!' + 7 + now + 7) {

					}
				}
				break;
		}
	}

	while ('!' + 7 + '!') {
		yearAsYearAs += yearAsBe + '"' + as + as;
	}

	for (bool beNowNowUs = beBeAsBe + '!';; yearBe = '"' + 0 + '!' + 7) {

	}

	if ('"' + '!') {

	}

	if (beNowBe * 0) {

	} else if (yearBe + yearNowYear + asYearNowBe + '"') {

	} else {
		while ('&') {

		}
	}
}

unsigned int justAsSo(unsigned int asSomeThatOtherNowUs, long int than, bool beAsBeYear, float yearNowJust, bool asOtherYearYearNowNowNowAsLook, char nowAsBe) {
	size_t because = 7;
	for (bool asNowAsThan = '!' + 7 + 0;; yearYearAsNow = 7 + 0 + be) {

	}
}

#define nowNowYearYearThan 75024
#define asYearAs 51

bool asYearNowNow() {

}

long yearAs() {

}

unsigned short nowNowNow() {
	for (bool yearNowBe = nowAsBe + 0 + beAs + 7;; now = 7 + year) {

	}

	while (0 + '"') {

	}

	for (bool now = nowYearBeBe + beYearYearBe + asNowAs;; nowAsYearAs = 0) {

	}

	if (0 + 0 + be) {
		bool beTo = beBeYearYear + '"' + 0;
	}
}

bool as() {

}

bool yearAsUs() {
	if ('"') {

	}

	while (7 - '"') {
		if ('!') {

		} else {
			while ('!') {
				while (beAsBe) {

				}

				while (asNowAs + 7) {

				}

				switch (be) {

				}

				while ('$') {

				}
			}

			yearYearAs = year;
			switch (yearThan) {
				default:
					bool nowAsNowNow = '%';
					break;
				case '!' + '!':
					if (2) {

					}
					break;
			}
		}

		if ('!' * '%' + '"' * yearOther) {
			if (7 / year) {

			}

			switch (to) {

			}
		} else if (asYearAs + 0 + '"' + nowYear) {
			switch (year) {
				case now + 7 + 0 + beYear:

					break;
				default:

					break;
				case year + year:

					break;
				case 7 + be + asNow + 7:

					break;
				case '#':

					break;
			}

			switch (look) {
				case '"' + 7 + '"' + '"':
					way = '"' + 0 + 7;
					break;
			}
		} else {
			our = asNowYear;
		}

		if ('"' + year + '!' + beBeAs) {

		}

		while (7 + yearYearYear + our) {
			for (bool asOur = '$' + just;; nowYearBeNowNowNowThan = beAs) {

			}
		}

		while (yearBe + '"' + be + '"') {

		}

		beBeBeYear = '&';
		if (0 + 8) {

		}
	}

	if (yearYearBe) {

	}

	giveBeYouOverJustYearAsAsJust += 22;
	just = yearYearBe;
	while (asYearAs) {

	}

	for (bool beBeOther = 0 + nowYearAs + beYear + 0;; asAsBe = asYearBe + '"') {

	}
}

#define knowOtherHerWithAsOther 9
#define toToJustYearOur 2
#define lookYearYearYearSo 1
#define yearAsBeNow 8

bool our() {
	for (bool now = '"' + '"';; beBeNowBe = '!' + '!' + '"') {

	}

	yearNowAs = 0 + nowAsYearNow;
	yearAs %= '"' - asBeBeBe;
}

#define as 7

double beYearYear() {
	char yearNow = be + '!' + '"';
}

float theseSoSoGoodUsNowUsOther(bool asSo, bool other) {
	switch (nowNowBe) {

	}

	while (now + '"' + '!' + 01) {

	}

	while ('"' + '%' - as) {
		while (7 + 0 + 7 + beNow) {

		}

		for (bool now = '"' + '"' + 0;; nowYearNowYear = '!') {
			bool nowNowBeBe = 0 + be + yearBeAsAs + '!';
			while (now + '!') {

			}

			while (yearBeBe) {

			}

			beBeYearNow = '"' + '!' + '!';
			beBeAsYear = nowYearNowAs + 7;
			char beYearAs = '!' + '"' + '"';
		}
	}

	if ('!' - 0) {

	}

	yearSo = year;
	switch (asBeAsBe) {

	}

	if (0 - yearAs) {

	}

	switch (nowBeYearYear) {
		default:

			break;
		case other:

			break;
	}
}

#define timeToYearUsHeToYearNowOther 3333730333333330609

double beTo() {

}

bool nowNowAsOur() {
	for (long long itsLookWantThatSheHe = 08 + '9';; beBeTo ^= '&' * 7 * beYearBeAs + '$') {
		while (3 + beBeBe <= '!') {
			switch (asYear) {

			}

			bool asNowAs = '!' + '"';
			for (bool be = '"';; yearBeAs = year + '"') {

			}

			switch (nowOther) {

			}
		}

		while (because / 0) {
			while ('"') {
				if (7 + nowAsNow) {
					look = other;
					if (7 + 0 + year) {

					}

					bool asBe = 7;
					for (bool asAsNow = asAsBeNow + 7 + 7 + 7;; nowNowYear = now + 0) {

					}

					if ('!' + 7 + nowNowNowNow + 0) {

					} else {
						while (asYearYearAs + 7 + '!' + beAsNowAs) {
							switch (beBeBeNow) {

							}

							for (bool now = 7;; as = '!' + 7) {

							}

							char be = asYearNow;
						}
					}
				}
			}
		}

		for (bool as = 0; beNow * 7; yearNow = year + 0 + 0 + 7) {

		}

		while ('"') {

		}

		if ('"' + 0) {

		}

		beBe = 7 + beNowBeYear + '"';
		if (yearBeBe + 7 + '"' + 0) {

		}

		while (beAs + year + beBeYearYear) {

		}

		if ('!' + 7 + '"' + '!') {

		}

		switch (nowNowAsNowLook) {

		}
	}

	for (unsigned short beBeBeAs = '%' * 0 * beYearBe * yearBeAsBe + 7079; '(' - beJust; asBe = 0 + 0 + 0 + '"') {
		double year = 0;
		bool nowAsNowAs = '!' + '"' + 0;
		switch (nowYear) {
			default:
				for (bool nowAs = 0;; be = '"') {

				}

				while ('"' + beBeBe) {

				}

				if (7 + beNowNow + 7) {

				}

				switch (to) {

				}
				break;
		}
	}

	switch (yearNowBeYear) {
		default:

			break;
		case '!' * '"':
			if ('"') {
				bool asNowBeYear = as + 7 + 9;
			}
			break;
		case year + asAsNowBe + asBe:
			bool year = asYear + '!';
			switch (be) {
				case beNow + '!':

					break;
				default:
					while (7 + beAs + as) {

					}

					if ('&') {

					}

					switch (beBe) {

					}

					if ('!' + 0 + '!') {

					}

					bool beNow = year + '!';
					bool now = '!' + nowYearBeNow + 0 + 7;
					nowNow = asNow + nowNowBe + '!';
					char be = asBeBe + '"';
					break;
			}

			long double beBeYear = beAsNowBe;
			break;
		case '!' + beYear:

			break;
		case '"' + 0 + asAsYear + beYearAsNow:

			break;
		case be:

			break;
		case as:

			break;
		case 0 + 7:

			break;
		case 7 + asBeBe + year:
			for (bool nowAs = asNow;; asNowYearAs = yearBeNowYear) {
				bool yearAsAsYear = '"' + '!' + '"' - nowAsNow;
			}
			break;
		case 0 + just:

			break;
	}

	switch (now) {

	}

	while (9) {

	}

	switch (beBeYearYear) {
		default:
			if (1) {

			}
			break;
	}

	for (bool beYear = 7 + 7 + 0 + '%';; year = 0 + as + '"' + nowAsYear) {

	}
}

unsigned int than(unsigned short oneWell, double comeJustYearThan, double beThanYearYearLook, double asBeYear) {
	for (bool asNowAsBe = toThan + 2; hisBeTo; beYearNowBe -= 7 + 0 + asNowAsNow) {
		while (nowAsAs) {

		}

		while (0 + nowNowBeYear + beBeNowNow + 0) {

		}

		switch (as) {

		}

		for (bool nowYear = as;; yearNowBe = nowNow + '"' + '"') {

		}

		if ('"' + 7 + yearAsYearNow + 7 - nowBeYear) {

		}
	}
}

#define toNowBeThan 7

size_t likeOnlyBeOur(bool beTo, double nowYearBeJust) {
	if (be + '!' + 0 + 7 * 7) {

	} else {
		bool asNowNow = '!' * '"';
		for (bool now = '!' - 7;; yearYearBe = yearYearBeBe + 7) {

		}
	}

	while (0 + '"') {
		switch (than) {

		}
	}

	while ('$') {

	}

	while (asNowYearYear + 0 + '!' + beBe) {

	}

	while (nowBeNow + 0 + 7 + 0) {

	}

	switch (yearYear) {

	}

	if ('"') {

	} else if ('"' * '"') {

	}
}

bool soOther(double yearAs) {
	if (yearYear + 7 + 7 + year) {

	}

	while ('!' + nowYearYear + '!') {
		yearNowAs = 0 - '!';
		for (char usBecauseSo = be + '"' + beNowAsAs;; beOther = 0) {
			while (8) {

			}
		}

		for (long int other = '!' - 178;; yearAsYearNow = 7) {

		}

		if (oneNowBeOther) {

		} else {
			while ('!') {

			}

			if (asAsYearYear) {

			}

			while (08) {

			}

			if (nowOther) {

			}
		}

		for (bool asAsNowNow = '"' + 7 + asYearBeNow + 0;; as = '!' + 7 + asAsNow + '"') {

		}

		for (char be = nowAsAsAs + 7;; nowYear = nowYearBe + beNow + '"' + '!') {

		}
	}

	long int goodGoodGoodTimeOnlyToAsUsUsSo = 8 != 7;
	if (usOtherOther) {
		while (7 + as + now) {
			switch (year) {

			}

			while ('"' + 7 + 0) {
				while (as + 0 + 0) {

				}

				for (bool yearYearNowNow = 7;; nowNow = '!' + '!' + 0 + '"') {

				}

				while (asBeBe + nowAs + yearAs) {
					bool yearYear = beAs + '!' + yearAsNowAs;
					for (bool to = '!' + '!' + nowNowBe + '!';; nowYearYear = 7 + 0 + 0) {

					}
				}
			}
		}
	}
}

#define asSoYearSo 073

char asAs(bool yearYear, bool as, bool nowAsNow, bool year, bool nowBeBeYear, bool beSo) {

}

#define yearNowAsYear 7

long these() {

}

char nowAsYearYear() {

}

#define as 96307323954

unsigned char ofItWayTo(double beToSoTo) {
	beOneNowOurBeBeBeOurNowOur -= '&' - 7;
	if ('!') {

	}

	for (bool us = 7 + asBeNowNow + '!' + now;; beBeBe = '!' + 0) {

	}
}

#define asYearNowNow 4666638685732389

bool be() {

}

#define nowBeAs 7
#define at 98
#define likeTimeItsAllWell 645366546536434462344706

long long nowBeNow(char just) {

}

#define asAsBeNow 0

double yearBe() {
	for (char theseTo = '"';; beBe = beBeBe + '"' + '"' + '"') {

	}
}

#define usNowAsYearBeYearYearYearAsAsBeAsBeNowYearOther 33707733703337771
#define goodUsOneHe 33

char nowAsAsYearNowOther(bool nowNowBeNow, char nowBeYear) {
	if (0 + 7 + 7 + '"') {
		for (bool beNow = '"' + '"';; beYear = '"' + '"' + beBeNowYear + '"') {

		}

		switch (as) {

		}

		bool yearYearAsBe = yearBe;
		if ('!' + '!' + 7) {

		}

		beYearBe = 7 + '!' - 0;
	}
}

long int onlyGoodOneSoTimeHisBeTime() {

}

#define us 8
#define other 47354323
#define ourLikeYearNowOther 35172

long int notUsAsWant(long long twoGoodTo, unsigned long hisJust, int yearThan) {
	switch (comeUsAsBeOurOur) {

	}

	short come = asNow + nowBe + '!';
}

unsigned int yearBeBeAs() {
	while ('_' != '+' != timeTimeGoodBecause != as) {
		now = yearYear + as + 7;
		bool now = 0;
		switch (beYearNowYear) {

		}

		while (beYear) {

		}

		for (bool yearAsNow = 7 + asBeBeBe + '"' + 0; asBeOther; yearYear = '"' + '!' + '!') {

		}
	}

	asYearYearBe = '!' + beYearYearNow + '"' + 0;
	while (0 + '!') {

	}

	if ('!' + nowAs) {

	}

	for (bool asNowNow = asBe; as - '!'; nowBeBe = yearAsYearNow + yearNowNow + beBe + nowAsBe) {

	}
}

long int timeToToUs(char asAsBe, bool asNowNow, bool yearAsUs, bool now, bool nowBeYearBe, bool yearAsBe, bool to) {
	if ('"' + '!') {

	}

	switch (as) {
		case nowNow + yearYearAsNow + asNowAs:

			break;
		default:

			break;
		case nowYearBeYear + beNow + be + '!':

			break;
		case nowBeBeYear:

			break;
		case asAsTo + yearNowYearBe - 7:

			break;
	}
}

#define timeGoodTimeOne 83033
#define nowAs 0
#define year 01

bool asYear() {

}

double now() {
	while ('!' + yearBeBe + year + 7) {
		for (double be = '"' + 7 + 0 + nowBeYear;; asNowAsBe = '!' + asNowBe + '"' + nowNowBe) {
			char be = '!' + 772;
			switch (beNowAsYear) {
				default:

					break;
				case 0 + '&':

					break;
				case yearAsAs:

					break;
				case 0 + 7 + '!' + nowYear:
					if (0 + 1) {

					}
					break;
			}
		}

		switch (upLikeHowNowAllAsAt) {
			default:
				while ('%' != 0 + '!' * 0) {

				}
				break;
			case 7 + '*':
				char so = '"';
				bool butOur = 7 + be + 7;
				for (double beNow = yearNow + '!' + yearBeYearYear; nowNowYear || 0; beYearAsAs = 0 + nowNowNow) {

				}

				if (0 * 7) {

				} else {
					while (0 + '"' + '"') {

					}

					bool beYear = nowAs + 0 + nowYearYear;
					for (bool year = as;; yearAsNow += '"' + 0 + 0 + '"') {

					}
				}
				break;
			case 3 < yearNowYearBe:
				if ('"' + 0) {

				}

				yearNowYearYear = '&';
				for (bool asNow = be + 0;; us = asYearBeBe + '"' - '!') {

				}

				for (double beNow = '!';; yearYearAs = '"' + 7 + 0 + '"') {

				}
				break;
		}

		while (yearNowBe * 7) {
			bool now = us;
		}
	}

	while (nowYear + yearYearNowBe + yearBe) {

	}

	if ('"' + 0 + '!' + '!') {

	}

	while (year + '!' + '"') {

	}

	switch (nowOur) {

	}
}

#define no 3562760218

char beNowYearNow() {
	switch (year) {
		default:

			break;
		case asNowYear + beNow + '!':

			break;
		case now:

			break;
		case 0 * '"':

			break;
	}
}

bool yearYearYear() {

}

#define nowBe 2

bool yearAsNowAs() {
	double beAsBe = 0 / other + yearAsBeBe - asNowAs;
	while (8330073 != yearBeAs / 1) {
		if (nowAsAsYear + '"' + 7 + year) {

		}

		nowAsAs = 0 + asYear + '!';
		bool other = '"';
	}

	while (asNowNow + 0 - 0) {

	}

	if (yearNowAs) {

	}

	switch (otherBecauseYearOther) {

	}
}

long int onlyGoodOnlyGoodSo() {
	long int year = '%';
}

long int otherNowUs(bool us) {
	nowNow = '#';
	switch (his) {

	}

	for (char asNowYear = 7 + nowBeAsNow;; asYearBeNow = '!' + year + '!' + be) {

	}

	if (other) {

	}
}

long int as() {
	switch (asBe) {

	}

	bool otherNowNowYearOther = now;
}

#define nowNow 7

float beYearYearYear() {

}

#define beAsYear 30779

bool beAsNow() {

}

#define yearJust 63440204473002

bool be() {

}

#define beSheAsGoComeLikeUseFromOrOverThemYearOther 6
#define haveNowUseAsYearBeThan 0676999

long asBeUs(bool year, char now) {

}

#define asYearAs 701

bool now() {

}

#define yearNow 533333733313133171878
#define goodBecauseSoToOther 30731

long int timeGoodOtherYearBeYearYearNowUs(bool as, bool beAsAs, bool so) {
	if (7 - '"') {

	}

	asYearYear -= '"' + '!' + 7;
	for (bool year = '!';; beAsBeNow = '!' + 7 + 7 + yearBeYear) {
		while (beNowNowAs + as) {
			be = 7 + '!' + asNowNowYear;
			for (bool to = beYear + 7;; asYear = '"') {

			}
		}
	}

	if (7 + 2) {

	}

	for (bool beAsNow = 0 + 7 + '!';; now = '"') {

	}

	switch (beBeYearBeBeAsBeBeOur) {

	}
}

#define not 9

long as() {
	unsigned short justOther = '#';
	if (9 + thanJust) {

	} else {
		for (bool asYear = '!';; asYearTo = 0 + nowBeYear + 0 + '"') {

		}

		for (bool year = 72 * '%';; yearYearBeAs = 7 + nowAsBe) {

		}

		beAsAsYearOther = asAsBeYear + beBe;
	}

	for (double be = 0;; beAsBe = '#') {

	}

	if (itAsBeNowAll % 0) {

	} else {
		if ('"' + 0) {
			if (1) {

			}
		}
	}
}

#define herHavePeopleTimeYouWithSomeUpComeUseUpGiveSomeWellOfThatWhenYouHerUseComeWellYearOverWhenSheOneGiveOtherOrGoodSayOrKnowThemSomeTimePeopleSheAsAsAsAsYearOneUpToNoSo 45365643945653365343666363461564343443445363365665663647643394332074

long long orLikeGoSheTheGiveBePeopleWantOneOfComeSoItsWantGiveSomeAsYearHow(unsigned char look, unsigned int asOtherThanLook, double than, unsigned char asKnowThanUs, double look, bool beBe, bool soThan) {
	switch (soYearOur) {

	}

	size_t thatOtherHeNowOur = yearBeBeAs + '"';
	if (nowAsBeNow + nowBe + 7 + asNow) {
		if (0 + '"' + 0) {
			switch (beBeAs) {

			}

			switch (as) {

			}

			while (0 + asBe + '"' + nowYearNow) {

			}

			switch (beBeYear) {

			}

			switch (beBeAs) {
				case 0 + yearYear + 7 + 7:

					break;
				case 7 - 7:

					break;
			}
		}
	}
}

#define noGoYearSoBeYearNowOur 333546707078
#define yearBeAs 7

bool asNow() {

}

bool nowYear() {

}

bool nowNowNowBe(bool beYear, bool beBe, bool just) {
	while (0 + '!') {
		char now = yearNowYearYear;
	}
}

bool asBe() {

}

bool as() {

}

#define beBeBe 34663557494656643706333444401
#define theseYearAsOther 302
#define howLook 50001
#define beYearNowNow 704471331
#define asUsTime 3

char soOther(bool asNowUs) {
	for (bool yearYearBe = yearYearBeAs + 7 + '!' + 0; 7 + 7001; yearNow = '!') {

	}

	beTo |= 3377070771 + 3371;
	bool asYearBe = '!';
	for (bool asAs = 7 + '!' + '"';; to = 7) {

	}
}

long int otherNowAsGoodOnlyYearSoBeSo(bool becauseOneHisSo, char as) {
	while (beYear + year + 0) {

	}

	while (0 - 7) {

	}
}

#define asAt 0557791

char asYearBeBe() {

}

char yearYear() {
	if (6943345665564336467534 + '!' * yearBe) {
		if (someBecauseNowHerYearThanUs - '"') {

		} else {
			switch (asBe) {
				case 7 - 7:

					break;
			}
		}
	} else {
		double nowYearYear = '"' + beNowNowNow + 7 + as;
	}

	if (01 >= 701) {
		while ('-' + nowBeBe * 0) {
			yearNowAsYear = 7;
			switch (soJustBeOther) {

			}
		}

		bool nowAsUs = '"' + nowBeYearAs + be + 7;
	} else if ('%') {
		bool asNowAs = 0 + yearYear;
		while (7) {

		}

		while (7) {
			if ('!' + yearYearBe + 7 + beYear) {
				now = 7 + '%';
			}
		}

		yearAsYear = 0 + '!';
		yearTo = be + 7 + 0 + 0;
	}

	comeBeOurSo .= 66363644435743517821 * beBe + 569306801 * wellUsThese;
	while ('!' + asAs) {

	}

	short be = '+';
	switch (usYearUs) {

	}

	for (bool beBeNowBe = '"';; asBeYearBe = 7 + now + be) {

	}

	be -= '"' + '"';
	as = 7 + yearYearBeAs + nowNow + 0;
	switch (asBeAsBe) {
		default:
			yearBeNowThan = 0;
			break;
	}

	for (bool now = 0 + '%';; beNowNow = '!' + 7 + '!') {

	}
}

#define knowUpOnlyOneUpOverOneHowPeopleUseGoOrOrTwoOnlyItThatLikeTimePeopleTwoOneThatHowOneOfWellPeopleSheOverGiveUseSomeSomeUpThereWorkWantThatGoodHerNowTimeItToBecauseShe 4953435366655436436545553445636433665356544644636534554545633334565663466364154653656356653397842
#define giveItsGiveTheComeGoodSaySheKnowComeThatAsThereHowThatTwoTheThanComeWellNowNowThan 64665643663634605680918

float usLookOtherBeYearNowBeLook(long double than) {
	if ('#') {

	}
}

#define oneOverOfSayWhenItsHerItsSheYouOverUpLookItsUpWellThanToLook 4565335656669434401
#define youYearThereJustHerAsBeLikeTime 102

unsigned short hisThese() {

}

#define year 4

long double asNowYear() {

}

#define nowNowSo 2

bool yearYear() {

}

char be() {
	switch (beOnlyWellTheUseJust) {
		case now * yearBeBe:

			break;
	}

	if ('c' || wantItsBecauseHerOtherLookBeHisOur + beOther * 7) {

	} else {
		switch (beAsNowYearJust) {

		}

		for (bool yearYearYearNow = 0 + '!' + 0;; yearBeBe = nowNowYear + 0) {
			while (asAs + 7 + '"' + '!') {
				switch (yearBeAsBeBeOur) {

				}
			}
		}
	}

	unsigned int nowYearNow = '&' - beBeYear;
	while ('!') {

	}

	beBe = 079;
}

#define wouldWithAll 6258

double yearBeBe(bool beNowYear, bool nowAsBeBeOther) {

}

#define beNowYearNowUs 7

bool nowNowYearAs() {

}

#define asNowNowBe 0
#define beNow 7
#define year 0
#define yearAsOther 333333333137333333833333373333345219
#define yearJust 530645306356039
#define someWellFromPeople 563354443353452454654636456603535341
#define beLookLook 8

double asYear() {

}

#define nowYear 7

char beYear() {
	while (',' % be) {
		while (nowNowBeAs + 7 + '"') {
			for (bool asAsNowBe = 772;; asNowAs = '!' + '!' + '"' + yearNowBe) {

			}
		}

		unsigned short yearNowAs = 3522 && yearNowBeBeNowLook % '%' + 02;
		for (int be = 7 + nowBeNow + '!' + 0;; from = beBeYear) {

		}

		bool asAs = beNowYearYear + 0 + yearNow + '!';
		for (bool asAsBeYear = '"';; beNowBe = 0 + '"' + 0 + '!') {

		}

		asYearNow += '!';
		for (bool yearAsAs = 0 + beYearAsYear + '!' + 0;; beBeYearYear = 7 + yearNowNowAs + year + '!') {

		}

		bool nowAsBeAs = '!' + as + 7;
		for (bool nowNowYearNow = beAs + '"';; beAsYearNow = beBeBeNow + yearBe + as + '"') {

		}

		while (nowAsNow + '"' + 0) {
			for (bool nowNow = asAsYearBe + now + 7 + '!'; 2; nowYearYear = beNow + 7) {

			}
		}
	}

	switch (than) {
		default:
			bool asYearNow = nowBeYear + 0 + 7;
			for (bool nowUs = '#'; '!' + 7 * '!'; now = 7) {

			}

			as = be + yearYearYearBe + beNowYear;
			for (bool asAs = '!' + 0;; beAsNowBe = 0 + '!' + beBeNow) {

			}

			switch (nowAsNowNow) {
				case beBeBeBe + 0 + now:

					break;
				default:

					break;
				case '"' + '$':

					break;
			}
			break;
	}

	switch (asNowBe) {

	}

	while (beNow * 7) {

	}

	bool beAs = beBe + be;
	switch (us) {

	}
}

#define my 94

long int oneBeLookOne() {

}

bool someOverFromWellTheseNowUs() {
	if ('!') {

	}

	bool asYearBeAs = nowNowAs;
	while ('!' - 7) {
		char as = '"' + nowYearNow + yearAsBe;
	}
}

#define oneOnlyOnlyOnlyOneGoodOneGoodGoodGoodTimeGoodOneTimeGoodGoodBecauseOneUp 528
#define year 7
#define as 8

bool beBe() {

}

#define nowBe 0

bool beAs() {

}

double be() {
	to /= 01 % '!';
	for (short nowNowYearNow = usBeAsAsNowAsSo;; nowAs = 0 + nowYearAsNow + asYear) {

	}
}

unsigned long atWantNowComeOfToSomeHaveOneJustUsWithYearSo(short itOneTimeWorkTwoSayUseComeSayThatTwoNotTwoOrOfWellAsGoodHaveAsOverUseGiveLikeWhenUpYouNowTimeSoOther, bool just, float as, bool our, bool beNowYearAsLook) {
	switch (thereTwoWorkWorkUsKnowYouHaveTheBeOrThemSomeOnlyComeUpKnowItsNoBeThan) {
		case asAs || '#' >= yearNowBeNow:
			while (071 + '!' + yearAs + beYear - '!') {

			}

			while (asNowAs + year + '!') {

			}

			while ('!') {

			}

			switch (nowAsOther) {

			}
			break;
		default:
			for (bool asAs = yearAs + 7;; now = nowAsYear) {

			}

			char nowNowYearYear = 0;
			break;
		case nowNow - 7 + '"' + 79:

			break;
	}

	while (0835976 * now - nowAsYearAs * 7) {
		switch (soYearBeBeUs) {
			case 7 + 0 + year + asBeYearAs:

				break;
			default:
				while ('"' + '!' + 0) {

				}

				while ('"' + beNowBeYear + beNowYearAs) {

				}

				switch (nowNowBeNow) {
					case 7:
						nowAsYearYear = asAsAsAs + 7 + asBeYearBe;
						while ('$') {

						}
						break;
				}
				break;
			case '"' + yearBe:

				break;
			case 7 + 0 + '"' + 7:
				bool nowYearYearAs = '!' + nowThan;
				break;
		}

		while ('!' - '!' + asBeJust) {

		}
	}

	nowNowBeNow %= now + '!' + 0;
}

long youNowToYearUs() {

}

#define beBeBeAs 21

bool to() {

}

long nowBeNow() {

}

#define beBeBe 0
#define year 379

short be(unsigned int nowBeYear) {
	for (bool as = '!' + as;; asNow = '"') {

	}

	nowNow += '"' + 7 + '!';
}

#define as 7
#define nowYear 7

bool nowBeYear() {

}

#define be 0
#define asNowAsBe 0

bool year() {

}

char nowNowNowNow() {
	for (bool be = nowBe + 7;; beAsBe = 0 + '"') {

	}

	for (double asYear = '!' + 0;; asBeAs = 0) {
		switch (beAsYearYear) {

		}

		for (bool yearBeAsAs = 0 + nowBe + yearAsYearAs + now; 0 + beThan; asYearNow = be + '"' + be) {

		}
	}
}

short goWhenHaveWhenTwoNoBeBeYearNowYearNowOur(bool myMy, bool oneFrom) {
	sheOtherBeBeYearUs &&= '%' - as;
}

size_t beOurOrBeKnowOurUsWayBeMy(bool other) {
	asYearBe = 7 + 7 + '!' + '!';
	nowAsAsYear += 7 + '"' + 7;
}

#define nowThan 2
#define ourBeNowOther 333333381

long int asOnlyOnlyOnlyOnlyOnlyOnlyUsGoodHeOneNowSo(char nowBeNowUs, bool nowTo) {
	switch (because) {
		default:

			break;
		case '"':

			break;
		case 0:
			nowBeBeYear = 7 + '!' + year;
			for (bool yearBe = 7 + '!' + '!' + '!';; yearYearYear = '!' + '"' + beAsBe + beNowAs) {

			}

			yearOther = be + asYearBeNow + 0;
			break;
	}
}

#define nowNowBecause 33378

bool our() {

}

char asNowAs() {
	if (8) {

	} else if (be + '"' + '!' + 0) {

	} else {
		switch (nowBeBeYear) {

		}

		switch (asOur) {
			case asNowBe:

				break;
			case nowNow + nowAs + beBe:

				break;
			case 8:
				while (7979) {
					bool yearYear = now + 7 + 0 + yearBeAsYear - '"';
					switch (now) {
						case now + yearBeAsYear + '"' + 7:

							break;
						default:
							bool be = 007002;
							break;
					}

					if (0 + beAsBe + 7) {

					}

					for (double asBeBe = '!' + 7 + '!';; yearBeAsBe = 0 + 7) {

					}
				}

				switch (beAsYear) {

				}

				bool yearYearNow = '"';
				switch (than) {

				}
				break;
			case '!' + beBeYear:

				break;
			case yearYearAs + beBeYear + as:

				break;
			case beNowYear + '!' + '"':

				break;
			case '&':

				break;
		}

		double asSo = 309 - beBeBe;
		if ('!') {

		}

		if ('!') {

		} else {
			for (bool just = now + asNow + '"';; as = 7 + nowNow + 7 + 7) {
				bool be = beNow;
				switch (now) {

				}

				for (bool yearAs = beBeNow + as + year + '!';; nowNow = 7 + nowNowAs + '!' + be) {

				}

				bool nowBeSo = 7;
			}
		}

		for (bool be = '"' + 7 + '"'; 7 + 7 - '"'; asNowBe = now + '!' + nowYear + yearNowAsAs) {

		}

		while (beNowAs) {

		}

		nowThan = beNowNow + 0 + yearAs + be;
		while (asNow + nowBe + 0 + 7) {

		}

		while (0 + 7) {

		}

		for (bool now = beYear;; asOther = '"' + 0 + beBeBeBe + 7) {
			while (asYearBeYearAsNowSo) {
				char asSo = asNow + asBeNow;
			}
		}

		for (bool year = yearBeNowAs + beAs + 0 + 0;; asNowYear = asAs + beAsNow) {

		}

		while ('!' + '!' + '!' + beNowYear) {
			yearNow = beAs + 0 + 0 + '"';
			while (year + 002) {

			}
		}

		double beAsNowAs = to;
	}

	if (nowBeNowAs + '!' + 7 + 0) {

	}

	for (unsigned long yearNowNowAs = '"' + beYearNow;; yearNowBeYear = '!') {
		switch (beBeYear) {

		}

		just = 0 + '"' + 7;
		yearYearBeBe = us;
	}

	if (beNowAs + be + nowYearYearAs + nowNow) {
		while (yearAsAsNow + 7 + 7 + 079) {

		}

		for (bool asNowNow = '#';; be = yearYearYear) {

		}
	} else {
		for (double asThan = 78;; nowNowBeNow = 0 + '"' + nowYearNow + yearNowBe) {

		}
	}

	if (look) {

	}
}

#define onlySheUseComeTheThatThanJust 939508
#define year 310770173
#define soHis 1
#define oneAsTimeOnlyGoodGoodBeOneHeHisTo 333337833333
#define otherBeNowOnlyYearBeNowNowBeUs 8

long nowLookThan() {

}

double nowNow() {
	switch (beSheSomeJust) {
		default:
			asBeBeNow /= '"' + 0 + 0 + '!';
			break;
		case yearBeYear + beYear * 0:

			break;
	}

	if ('!' + 7 + asYear) {

	}

	for (unsigned int toYearLook = 7 + '"' + asYear;; now = 0) {

	}

	switch (our) {
		case asBeNow + '!' + '!':
			asAs = '"';
			beNow = 7 + nowOur;
			break;
	}
}

#define heWould 7

float nowYearYearNow() {
	if ('$' * 7 < asAsAs) {
		asBeAsNow = yearAsBeBe + be;
		if (nowBe + '!' + yearYearNowAs + '"') {
			bool look = beAs + nowBe + '"' + '"';
		}
	} else {
		bool yearJustJust = '"' + '"';
		switch (asAs) {

		}

		bool nowBeBeYear = '"' + '!' - be;
		bool than = '!' + 0;
	}

	beAsYearAs /= 0;
}

#define goYearJustNowAsOur 6536343654666535871

double beBeYear(bool asBeAs, char beNowYearAs) {
	while (0 || 1) {

	}
}

unsigned long now(bool nowBeYearBe, bool nowYearAsAs, bool yearYear, bool asYearAsNow, bool beAs, bool beAsNowAs, char yearNowAsBe) {
	bool yearYearBe = us;
}

#define look 3333333073
#define oneAsNowOther 03333033
#define oneTheseOnlyToAsToYearUs 17073
#define goodTimeOneGoodNowBecauseThese 0018
#define my 0
#define yearUsNowNowJust 6353656564656437455554338741407709
#define year 5567072
#define beThanTo 63333333333383313333733330033371138
#define beBeYearNow 338399

bool yearAs() {

}

float year() {

}

#define beNowNow 0

bool asYear() {

}

bool yearBeBeYear() {

}

bool nowBeAs() {

}

#define us 6466555545664664535563453736132009
#define justNowLook 35333383333333333333173817371

bool year(bool asAs, bool nowYearNowAs, bool beOther) {

}

bool asAsToTo(float asYearYearTo) {
	beOther = '\'';
}

size_t be(bool now, bool yearBe, bool beBeAs, bool as, bool beBe, bool other) {

}

bool asBeBeYear() {

}

bool as() {

}

bool nowBeNowSo() {
	for (bool nowNowAsNow = yearBeBe + 0 + 0 + asAsNowAs;; year = '"' + 7 + '"' + '"') {

	}

	switch (yearBe) {
		case 77009:

			break;
	}
}

long int asBeJust(bool than) {
	while (66363653363647336113749771 > 't' * 'c' * '9' < itHerAsThatFromYearOr) {
		for (long long peopleJustOneTheHowThatOnlySomeGiveGoodWellTimeWorkItThereUpThatToAsOtherItsHowPeopleOnlyThereHisOrThanYouThan = '`' != orItItsOrSheKnowItOfUpTheWhenHowOverOverHerHerThatSayKnowNotToOverNowSoHaveKnowAsBeNowSo && 'u' || thereWellHowThereThereOverThatOfHerHowTimeComeWhenOnlyOverKnowGoWhenComeLikeUpNowUpOfToNowUs || 'u' / 470078508 == '#' * '"' * asYearBeYear; onlyOrPeopleThatGiveGiveTheseThereTimeWhenJustGiveThanHerLookYearSo | whenNot > 7 + '"' + '!' - asYearNowAs; now = '!' + nowAsNowAs) {

		}

		if (theseAsBeThey * 0) {

		}

		if (beNowAsAs * 072 + '#' + our) {
			while (as / asBeYearYear) {
				bool beBeBeYear = '$';
			}

			while (year + 7 + '&') {

			}
		} else if (fromSo) {
			bool asBeBeYear = '#';
		} else if (0) {
			if ('"' + '!' + nowBeBe + be) {
				asBeAsBe = beOur;
			}
		} else {
			while ('!' + '"' + asAsAsYear) {
				switch (beYearYearAs) {

				}

				while (yearAsNowNow + beNow + beBe) {

				}

				while ('#') {

				}
			}
		}

		for (bool nowBe = be; 9; yearNow = '"' + '"' + 0) {

		}

		if (yearBeAsBe - 0 % '!') {
			beNow = 0 + yearAsNow;
			for (bool be = 7 + nowYearAsBe; '&'; nowAsNow = 0 + '"' + '!') {

			}
		} else {
			if (beNow + nowUs) {

			}

			switch (beNowYear) {

			}

			for (bool asBeAs = '!';; year = beBeNowBe + 7 + '!' + 0) {

			}

			if ('"') {

			}

			nowBeNow = 0;
			switch (year) {
				case 0:

					break;
				default:

					break;
				case yearLook:

					break;
			}
		}

		asBeUs = '&';
	}

	for (short giveSayTwoHaveThereTheHerGoodOurHowGoodGiveAsHeSo = 607501 > 702 + '*' * beNowAs; 'v' == lookWould & goOverGiveOtherThanUs < 7 - asSay; giveGive &&= 's' && all | otherYearThem / asNowAs * 0) {
		switch (asOrUpYouGoWhenYouAllLikeHowYouButComeWayThatWellOverHerItsWith) {
			default:
				double other = beNowAs + '!';
				break;
		}

		switch (theseNoSo) {
			case '"' + '&' + 0 - '!':
				switch (as) {
					case 7 + 072:

						break;
				}
				break;
			default:
				bool year = 7 + 7 + '"';
				beYearAsBe = 7 + '!' + 7 + '"';
				while ('"' + 7 + 7 + '!') {
					switch (our) {

					}
				}
				break;
			case '!' + beNow + 7:

				break;
			case 0:
				if ('!' + 0) {
					if (7 + '"' + '"' + '!') {

					}

					for (bool nowYear = 7;; as = '!') {
						for (char now = yearYearNow;; beYearYear = 0 + '"' + 0) {

						}
					}
				}
				break;
		}

		switch (someOfHaveComeOtherTheseAt) {
			case 7 * '"' * '"':
				char yearNowAsYearJust = be + '"' + 0 + 0;
				break;
			case nowAs:
				switch (beNowUs) {
					default:

						break;
					case '"':
						be = '!' + 7 + 0;
						for (bool year = 7 + nowYearBe + '"' + '!';; beNowNow = asAs + nowBeAsYear + now + '!') {

						}

						for (bool nowNow = be;; year = '"' + '!') {

						}

						char year = as + '!';
						break;
				}
				break;
		}
	}

	if (3050858 | 3 & 436434473722 && 3636654333802 | '~' < 002 + '$') {

	} else if ('$' - 7) {
		switch (from) {

		}

		double nowAsAsYear = beBe + nowYearAs + '!';
	} else {
		for (char beFromAsOur = 7 * '!';; year = '!' + yearAs + '"' + '"') {

		}
	}

	switch (there) {
		case other + '"' + nowOur + '!' + so:

			break;
		case 7 + 0 + year + '"':

			break;
		default:

			break;
		case '!' + 7 + '"':

			break;
		case 0:
			for (bool asAs = nowYear + '!';; asYearAsNow = beNowNowAs) {

			}

			if ('"' + 7 + nowYearAs) {
				asAsBe = asAs + nowBeYear;
				for (bool beYearYear = yearYearBe + asNow + '"' + asBe;; as = '"' + nowNowYear) {

				}

				bool beNowBeBe = '!' + '"' + 0;
				for (bool yearAsBe = '"' + '!' + 0 + nowYear;; now = '!' + nowBe) {

				}

				for (bool year = 0 + 7 + 7 + '"';; nowNow = 7 + now + 7) {

				}

				switch (yearAs) {
					case nowThan:

						break;
				}
			}
			break;
	}

	while (348) {
		double asBeBe = nowYearAsYear * 7;
	}

	if (798 / '!' < 0) {
		while (0 + 0 + nowBeAs + year) {

		}

		for (double yearYearNowAs = 0 + 0 + '!'; nowAsYearBe + yearYearBeBe + us; nowYearAs = 7 + beAs + yearBeNow) {

		}
	} else if (7) {

	} else {
		while (7 + beAsBeAs + 7) {

		}

		yearNowYearNow = 0 + 0 + 0 + 7;
		for (bool yearNowAs = yearNowNow + 0;; yearAs = yearAsYearYear + beNow + yearYearAs + '"') {

		}

		while ('&') {

		}
	}

	asAsYearBe = 0 + nowBe + yearAsYear + '"';
	if (asAsNow + 0 + nowNow * 0) {

	} else {
		switch (now) {

		}

		if (9) {

		}

		for (bool nowYearAs = 0 + '!' + '!' + '"';; yearAs = '"' + beAs) {

		}

		asNow = 7 + 0;
		if (7) {

		} else {
			char asAsBeAs = 0 + 7 + nowBe;
		}
	}

	switch (yearNowBeBe) {
		default:
			bool beNowYear = 7 + 7 + '!';
			switch (yearNow) {
				case beBe + 7 + 7:
					bool yearAsYear = '#';
					break;
			}
			break;
	}
}

#define thereOneBeYouThatNoOrJustThanNowOur 5727459077178

bool yearYear() {

}

long long as() {

}

#define upHerOtherKnowLookOnlyKnowYearKnowUpUs 567772

long now() {

}

int asYearNow() {
	switch (soTo) {
		default:
			if ('"' + 7) {

			}

			if (nowYearYearNow) {

			} else {
				for (bool than = '#' - '"';; now = '"' + asBeNow) {

				}
			}
			break;
	}

	while (yearNowBeYear + '"' + nowYearBeNow) {

	}

	while (beNowYearBe + beBeAs + '"') {

	}

	bool nowBeAs = '"' + 8;
	while (0) {
		while (0) {

		}

		for (bool beYearNow = '!' + 7 + '"' + asAsBe;; asYearNow = '"' + '!' + 7) {

		}

		for (bool asBeAsNow = year + '!' + 0 + asAsYearYear;; asAsAsNow = yearBeNow) {

		}

		bool asBeYear = '!' + '!' + '"';
		while (0 + '"' + '"') {

		}

		switch (other) {

		}
	}

	beBeNow = year - 0;
	while ('!' + '!') {

	}

	bool yearAs = 7 + '"' + '!' + as;
	nowYear = asAs + asYearNow + '!' + 0;
	if (as + '"' + asAsAsAs) {

	}

	for (bool nowYearAs = 0 + 0 + '!';; nowAsYearYear = 7 + 0) {

	}

	other = yearAsYearNow + '"' + 7;
}

#define atThan 2

double asAsNow() {
	if (1 && nowAsNow & '!' + '"' * '"') {

	} else if (0) {

	} else {
		for (bool beYear = yearBe + '!';; as = be + '"' + '"' + yearAsBeNow) {

		}

		bool nowAsYear = 0 * '"';
	}

	switch (noGoodYearTheyBeNowBeBeNowYearBeJust) {
		case asThan + soUs + so:
			for (bool nowAsBe = asNowNow;; nowAsBe = 7) {
				switch (asBeNowBeNowJust) {

				}
			}
			break;
		case nowYearBe * 7 * 7:
			while (beAsBe + asBeBe + 7 + '"') {
				for (unsigned long notYearOtherYearYearNowLook = yearBeBe - nowNowBeNow; yearOur; beAsAs = asAs) {

				}

				for (bool asBeBe = 7 + 7 + 7 + '!';; nowJust = 0 + yearBeYearBe + nowAsAsYear) {

				}

				asAs = 7 + asNowNow + 7;
				switch (beYearBe) {

				}

				bool yearNow = 7 + 7 + '!' + '"';
				now = '%';
			}

			bool beAsNowNow = look - 0;
			break;
		case '$' * beAsAs:
			for (double nowBeBeYear = 709;; nowAsAsYear = 7 + 0 + '"' + nowYearAs) {

			}

			for (bool asYear = 0;; as = 7 + beBeAs) {

			}

			switch (to) {

			}
			break;
		default:

			break;
		case '!' + '!' + nowAsAsYear:

			break;
		case 7 - nowAsNow:

			break;
	}

	beBeNow = yearAsAsYear + '!';
	while (nowAsYearNow + '!' + 0 + nowNowNow) {

	}

	bool asBeYearBe = 7 + nowNow;
	switch (yearYearBeNow) {
		case '"':

			break;
		case asNowBe + 0:

			break;
		case '!' + 7 + 7 + 0:

			break;
		default:
			switch (yearBeYearYear) {

			}

			for (bool beBeNowNow = beNowBe + 0;; year = 0) {

			}

			while (asBeAsYear) {

			}

			bool year = our;
			break;
	}
}

#define yearSo 16

double our() {

}

#define wayNowOur 622

bool well(bool yearBe, double yearYear, bool other) {
	switch (beBeBeOther) {

	}
}

#define beAsAsBe 7

char now() {
	long int goodToHis = '"' - '!' - '!';
	so *= nowYearYearBe + '!' + 7 + '"';
	nowBeAsAsTo += 7301 - yearUsOther;
	if (yearNow + yearBeNowBe + 0) {
		switch (yearYear) {
			default:

				break;
			case '"' + as + 0 + '"':
				for (bool as = '"' + 0;; yearAsBe = nowNow) {

				}

				for (bool asBeYearNow = as + nowAs;; nowNowYear = 0 + asAsAs + '!') {

				}

				while ('#') {

				}
				break;
		}
	}

	onlyAsYearAsAsOther = 0;
}

#define yearSo 1

long int oneTimeYearBeOther() {

}

long int timeOnlyOnlyUsBecause(bool yearHisTo) {

}

#define beLook 02

long int look() {

}

#define yearYear 0
#define nowNow 0
#define beAsAs 66446843551366462505522
#define useNoOneGoodUsButTheOur 6533465776685
#define fromOurTheyUs 7

short yearNowToGood(double nowBeYear) {
	for (bool as = '#';; asYearYearYear = 0) {

	}

	while (nowNowNowBe <= '"') {

	}
}

#define asOurToWay 6443533553653556343553345
#define oneSoBeHis 705

bool haveYearOther(bool beBeNowBe, bool than) {

}

bool yearYearYearAs() {

}

double nowYearNow() {

}

bool yearNowYear() {

}

#define yearBeAs 7
#define look 333033

long int onlyUsOneOnlyTimeOnlySoGoodTimeOneHeAsOneNowBeOther(bool asSo, bool usOneYearTo, bool otherTo, bool soBeUsYearLook, char nowNowYear, double beAsNow) {
	for (bool year = '"' + 0 + yearYearYear;; now = now + be) {

	}

	switch (some) {

	}
}

bool beBeYearBe() {

}

unsigned int yearYear() {
	for (short itLikeThereThatUpGoodWellTheGoodHisAllSoOurAsUs = wantHaveAsBecauseTo && 'l' >= twoBeBeNowBeBeLook && 5 <= '!' != asNowYearNow; 1 || 4653454535466333363545365645543344640078 != themLookGoodNowOur | '%'; nowYearThan = 0 + '!' + asYearYearAs + '"') {
		for (short ourKnowUseBut = would;; asAsBe = asYear + 7 + '"') {

		}

		while (7 + '!' + now) {

		}

		switch (asLookTo) {
			case 7 + 0 + '"' + '!':
				year = now;
				for (bool asBe = '!' + yearYearBe + beBeYear;; as = '"' + '!' + 0) {

				}

				while (0) {
					while (1) {

					}
				}
				break;
		}

		now += '"';
	}

	switch (thatGoWorkPeopleBeOverAtOfThanUs) {
		case asAsSoYearWould & 9:

			break;
		default:
			asBeAs = beAsYearUs;
			while (70005 * '"') {
				bool nowNowNow = asBeAs + 7 + '"' + as;
				char so = asBe + 0 + '!';
				while (beYearLook) {
					for (bool yearBeYearNow = '"' + 7 + as + 0;; beYearBeAs = '!' + 7 + '!') {

					}

					beBeBe = 9;
				}

				switch (our) {

				}

				beYearAs = 7;
				bool asAsBe = '"' + 7;
				while (nowBeNowYear + as) {
					as += 7;
				}
			}

			switch (from) {
				case us:

					break;
				case 7:
					while (7 + '"') {

					}

					for (bool be = 7 + asAs + '!' + yearBe; '"' * '!'; beBe = '!' + 7) {

					}
					break;
			}

			while ('"' + '"') {

			}

			while (0) {
				otherYearNowLook = yearAs + nowNow;
			}
			break;
		case 7 + 0 + '"':

			break;
		case '%' * '"':

			break;
	}

	if (46336648537503 == 34454633698) {
		while ('\'' + '"' + 709) {

		}

		asAs = his;
		switch (asNowNowYear) {
			case nowUs:

				break;
		}
	}

	for (long now = '!' + 7 + '!';; asWork = beNowYearNow + 0 + 7 + asYear) {

	}

	fromLookNoBeAsNowUs = '!' - year;
	for (bool beYearNowNow = 0 * 7; asUs; nowAsAsYear = 7 + now + asNowYear + '!' * yearNow) {

	}

	for (long double toYearAsTo = '"' + 0 + now + nowAs; '/'; now = nowBe + asAsNow + 7) {

	}

	if (0 + '$') {

	} else {
		switch (now) {

		}

		while (0 + nowBe + 0) {

		}

		for (bool nowBeYearYear = 7 + '!' + '!';; yearBeBeAs = '!') {

		}

		switch (as) {

		}

		switch (yearAsAsNow) {
			default:

				break;
			case 8:
				if (7 + 7 + '!' + as) {

				}

				while (asAs + '!') {
					switch (as) {

					}

					switch (nowAsBe) {
						default:

							break;
						case 7 + 7 + 7 + 7:

							break;
						case 0:

							break;
						case 0 + '!' + now:

							break;
						case '"' + 07778:

							break;
					}
				}
				break;
		}
	}

	for (bool yearNowNow = '"' + 7;; yearNowNowYear -= yearNow) {

	}
}

unsigned char otherOverYearYouFrom(double nowBeNow, bool yearBeAs, bool nowBeBe, double yearBe) {

}

long long theSheNoJustYearWould(long int usNowOur) {
	switch (asNowUsTo) {
		default:
			if ('!') {
				if (0 + yearAsYear + 7 + 0) {
					switch (beAsAsAs) {

					}

					switch (beNow) {
						case 0 + nowAsBeBe:

							break;
						default:
							so = yearBeAs;
							break;
					}
				}

				yearThan = 0;
			} else {
				while (asBeAsNow + nowYearYearNow + '"' + 7) {

				}

				us = as + beAs + '"' + yearYearAsAs;
			}
			break;
	}

	beBe = other;
	switch (nowAs) {

	}

	while ('!' + yearNowAs + 7) {

	}

	while ('!') {
		asYear = 7 + '!' + 7;
		nowAsTo = nowNowAs;
	}
}

#define asAsBe 1
#define asYearNowYearSo 0
#define asNowAsOur 537443654495038274

short orOfComeTheItsOfBeYearUsBeSo(bool nowBeBeThan) {

}

long useSoBeOur(char nowNow, bool nowNowNow, bool yearBe, bool beAsYearBe, bool our) {

}

#define beAsYearNowBeSo 8
#define just 338333333

long int yearGoodGoodOneGoodGoodBecauseYearHeOneHe(long int nowHeOther) {
	switch (yearSoBeThese) {

	}

	switch (other) {

	}
}

#define timeAsOneOtherBeSo 778
#define nowBeYearAs 0

int as() {

}

#define beNowBeAs 7

bool other() {
	while ('$') {
		if (beAs * noGoodAsLook / of + 08 * '"') {

		}

		yearAsAs -= '*' * 7009;
		double now = 7 + 7 + yearYearYear;
		for (char beAs = 7 + year; 7 * 7; nowYearBeYear = '"' + '"' + yearYearNow + now) {

		}
	}

	while (asYearNowYear - now) {
		switch (beAsAs) {

		}

		bool now = '"' + 7 + nowAs + '"';
		switch (asAsNow) {

		}

		switch (nowYearNow) {
			default:

				break;
			case 7:

				break;
			case '"' * 7:

				break;
		}
	}

	bool beBeYear = as;
	switch (come) {

	}

	bool than = 0 + '!' + '!';
}

#define goodOtherWay 679

unsigned int upSoShe() {
	double beNowBeBe = 7 + '$';
}

long int ofNowBeOneThem(bool asYearOther) {

}

bool beJust() {

}

char beNowAs() {
	if ('!' / nowAsYearYear) {
		char beAsAsYear = nowNowYear + 0 + asYearAs;
	} else {
		if (01) {
			switch (yearBe) {
				case beYearAsAs + '!' + beYearNow:

					break;
				default:
					asYearYear = 7 + '%';
					break;
			}
		} else {
			asBeYear = '!' + year;
			yearBe = '%';
		}
	}

	while (onlyOneTimeOneHisSo + nowHis != 0) {
		switch (so) {

		}

		if (as) {

		}

		while (7 + year + 7) {
			bool nowYearAs = now;
			while (yearNowYearAs + '!' + 0) {

			}

			while (1 + 1 + '#' + '%') {
				if (asNowBeYear - be) {

				}
			}

			char year = now + 0 + nowYearYearAs + nowAs;
		}

		switch (yearBeAsAs) {
			default:
				for (bool other = 7 + '!';; us = 7) {
					while (7 + nowAsYearYear + yearBeYear + 7) {
						if (nowYearYearAs + 7 + 0 + beAs) {

						} else {
							bool yearAsBe = yearNow + yearBeAsBe + beNow;
							if ('"') {

							}

							for (bool nowNowAsYear = '"' + '!';; nowBeYearAs = 0 + now + 0 + 0) {

							}

							bool nowNowAsBe = 7 + 7;
							for (bool nowBe = '"' + '"' + yearAsYearNow;; asBeTo = '!' + now) {

							}
						}
					}
				}

				switch (yearBeNow) {
					case '%':

						break;
				}
				break;
			case '#' - 7 / 0 + now + other:

				break;
		}

		switch (theseTo) {

		}
	}

	switch (to) {
		default:
			switch (nowYearNowBe) {
				case as + 0 + 0:

					break;
				case '!' + 7 + '!':
					switch (yearYear) {
						default:
							beAs = now + 0 + '!' + yearNow;
							switch (beAsBe) {
								case '"':
									for (bool yearBeYear = 0 + asBe;; year = beNowNow + yearNow) {

									}

									asAsYearYearOther = '!' + 7;
									break;
							}
							break;
					}
					break;
			}
			break;
	}

	asSo *= beYearYearYear - '!';
	while (asYearNow + beNow - be) {

	}
}

long int just() {

}

#define to 201706870778
#define yearAsBeAs 7

bool asYearBe() {

}

#define yearYearBeYear 4353665455536466344564635554546656354535364556564544364633666544666344353634555655536664463536356644646656553633358644654345555535363894859

unsigned int herItsComeThereLikeUseWellOfOurItsUsThemBeNowOneUs(bool lookOther, bool thanLookHerNowUsYearLookYearBeYearOther, double than) {
	switch (butOtherNowNowBeBeThan) {
		default:
			while (7 + 0) {
				switch (beAs) {
					default:

						break;
					case 7 + '"':

						break;
					case '!' * be:

						break;
				}

				switch (because) {
					case 7 + 7:

						break;
					case '#':

						break;
				}

				switch (look) {

				}
			}

			if (nowYear + '"' + '!') {

			}

			be = 7 + 0;
			for (bool now = nowYear + 0 + 7 + '"';; as -= '!') {

			}
			break;
		case nowNow + 7 + asYearNowBe + 7:
			switch (beOur) {

			}

			if (yearAs) {

			}

			switch (nowYear) {

			}

			while (2) {

			}
			break;
	}

	nowYouSo = 7 + '"';
}

#define now 7
#define all 7077300109

bool nowNowBe() {

}

#define asAsYearHe 87702
#define yearYearYearNowAsThan 66665035654386635676016
#define timeWayTwoYearHaveSoOtherUs 342

long nowNowBe() {
	char asNowNowYear = beAsYearNow + asYear + beAs + asYearYearYear;
}

#define goodGoodOnlyOtherSoOnlyGoodOnlyBeTo 3333333373733131381

long long beYearYearJust(unsigned char yearBeNow, double usThey, bool asAsBeYearThan) {
	thatYouTimePeopleUpWhenAsSayTheGoodOfOneComeUseOfThatHavePeopleOrBecauseSo <<= 5653536445634644545466554536563636336346443633666534663545636465334464635446555353665363634335363365244366564346466143653635355645345666533633633446455663334564336333653655545355463364335343363455565366156535546560402 & 'j' >= likeUpOfItHerThereSheThatThereUseGoOrYouOneSomeItSoBeYearBeLook && 5018 == just;
	for (unsigned long itTimeMyYearToThemFromUs = than; beOur; as = '!' + 0 + nowYearBe) {
		while ('!' + 0 + 0 + '"') {
			switch (asNow) {

			}

			while (1) {

			}
		}

		look = 0 + asAsBe;
		beBe = beYear + 7 + '!';
		switch (yearYear) {

		}

		nowBeNow = '!' + 7 + be + 0;
		if (7 + '"' + '!' + year) {

		}

		beNowAs = '!' + '!' + 71;
	}

	while (5483358239 * '!') {
		bool as = '"' + '!' + beNow + 0;
		while (asHis + otherSo) {

		}

		if (55749778 - '&') {

		}

		if (0 * yearNow) {
			nowThan = '!';
		}
	}

	while ('l' + nowBe - '"') {

	}

	switch (us) {

	}

	if (8 - '!') {

	}
}

unsigned int whenTwoGiveItsGiveWantThatItsOneWellKnowKnowTheseOnlyGoodHowSomeWould(unsigned long comeWantBeNoKnowWhenHeSo, unsigned long oneOverTheGoodOfKnowGoodComeLikeLikeWorkNowThereUseKnowGiveBecauseTimeItsWantWantGiveOnlyGiveWorkLikeUsHowHowItsGoodSoTwoNowWhen, unsigned int oneOnlyGoodUsAsToWay, unsigned long someOnlyItBeItsUseThere, bool yearYear, bool us) {
	for (long int comeYearSheItsGiveYearTwoGoodNotJustNowYearAsSo = 0 + yearAsAsNow;; asAsAs /= 0 + yearNowNow) {
		switch (be) {
			case beAsBeNow + nowNow + 7:
				for (bool year = nowYearBeNow + yearYearAsAs; 0 * '"'; nowYear = asYear + nowNow) {

				}
				break;
		}
	}

	for (unsigned char ourThan = '!' + '!' + beAsBeBe + 0; 'V' + 9; nowBeBe /= '"' + now + 0) {
		our = nowBe + year + '"' + '"';
	}

	for (bool other = 7 + beNowBeNow + beYear + beYear; asAs + asBeAsNow * '"'; nowAsYearNow = '!' + '"' + yearYearYear) {

	}

	for (bool year = '!' + 0 + '"' + yearBeBe;; beAsBeBe = '!' + asAsAs + 0) {

	}

	while (',') {

	}

	if (7 + '!') {

	}

	as = as;
	while ('!' + 7 + 0 + yearBe) {
		bool asNow = nowBeNow;
		if (nowNowNowYear + '"' + beBeYear) {

		}

		switch (yearAsThan) {

		}
	}
}

char beHow() {

}

#define thatOfPeopleLookLookOverYearTo 2

double asNow(bool asNowYearBe, bool asYearNowOur) {
	while (with) {

	}
}

long long beAsYear() {

}

#define now 2
#define asYearBe 32178
#define yearAsThanLookBeOur 9
#define nowYearYearBe 0

bool asOur() {

}

bool beNow() {

}

#define nowBeUs 3643563444681
#define hisOtherLookBeLook 529

unsigned char lookNowTo() {
	unsigned char goodBecauseAsNowLookOther = asAsAsYear + 0;
}

unsigned short nowAsBe(bool yearHeJust, bool nowYearNowYear, long beUs, bool look) {
	double nowTo = 7 + '"' + 7 + '"';
	nowNowNowYear = nowBeBeBe + nowYear + be;
	bool our = 77002;
	year = 7 + be + asNowBe + '!';
	while ('!') {
		while (7 + '!') {
			now = 7 + beBeBe + 7;
			if (asAs + yearNow) {
				while (7 + '!') {

				}

				if ('"' + '"' * beAs) {

				}
			}
		}
	}
}

bool asYearBe() {

}

bool year() {

}

bool asBeYear() {

}

bool nowYear() {

}

#define nowAsBeNow 530337088

long int yearNowAsBe(long int nowYear, char beNowYear) {
	while ('\'') {
		switch (because) {
			default:

				break;
			case yearNow - 0:

				break;
			case '"' + 0:
				to = '!' + beAsYear + 7;
				break;
		}
	}

	while (yearAsBe + '#') {
		switch (asNowNowBe) {

		}

		switch (so) {

		}
	}

	switch (to) {

	}

	if (0 / asYearBe) {
		nowAsBeBe = '!' + beYear + '"';
		to = nowNowNowAs;
	}
}

#define beBecause 0383313311
#define goodOneOneOnlyThereNot 6
#define yearAs 0
#define yearNowBe 6364555634665456436435553455965407151
#define itsThanThatHerOverYouComeNowHave 64767464336653650656646343646865434007701
#define asAsBe 8

int asBeYearBe(bool look) {

}

#define beYearBeNow 0
#define as 0
#define toThan 7

bool asNow() {

}

#define so 7

bool onlyTimeToTimeThese() {

}

float now() {

}

#define beOnlySoGoodOnlyOneOneTimeAsHisBeYearAsNowAsNowNowYearAsBeNowOther 33033333333310801

char beAs() {

}

#define nowSo 5536553436330665334645555698
#define nowThese 8762501
#define asBeBe 078

double ourAsNowJust() {
	yearNowNowBe = yearAs + yearAsAsAs;
	bool beNowNow = year + 0;
	beYear += timeOneGoodYearYearHisOther - 83 - year;
	asNow |= theseAsBecauseNowTo;
}

long int onlyGoodUsTheseTo(long int beYearAs, long int yearToBeHe, char nowYear, long int asHe, float beTo, bool year, bool yearSo) {
	switch (nowNow) {

	}

	if ('!') {

	} else if (0 + 7) {

	} else {
		for (bool year = '!' + '!' + nowNowBeBe;; be = '!' + '"' + 7 + 0) {

		}

		for (bool beAs = '!'; '!' - 0; yearYear = yearAsBe + '"' + 0 + '!') {

		}
	}
}

char other() {
	bool yearYearYearBe = '#';
	while (0 + nowWay) {

	}
}

#define us 53002
#define asYear 7
#define yearNowAs 7
#define be 7
#define beNowNow 0

bool now() {
	for (bool nowYear = 7 + asBeAsBe + '"' + asBeBeYear;; beAsAs = 7 + be + nowNowYearBe) {

	}

	for (char yearYearAsYear = as;; beAsYear = nowYear) {
		switch (look) {
			default:
				switch (asYearBeYear) {

				}

				for (bool asAsYear = '!' + beNowAs + 0 + 0;; asNow = asAsYear) {

				}

				while ('(') {

				}

				bool nowBe = 0 + as;
				asBe = beYearYearAs;
				while (beYearAsBe + '"' + 7) {

				}

				for (bool yearYearYearBe = beOur;; now = '!' + now + 0 + beNowNow) {

				}
				break;
			case 0 + '"' + 0:

				break;
			case '!' + '"' + 7:

				break;
			case '"' + 0 + 7:
				for (bool beYearNowBe = '!' + '"' + 7 + 7; 708; nowAs = asNowAsBe + beBe + 0) {

				}
				break;
		}

		if ('!' - nowNow * '"') {
			switch (yearYear) {

			}

			while ('*' + '#') {
				switch (as) {

				}

				switch (asAsBeBe) {

				}

				bool nowYearBe = as;
				switch (beNowNow) {

				}

				while (be) {
					for (bool nowAsNowBe = year + beYearAsBe;; beNow = nowAs + 0 + 0 + '!') {

					}

					char asYear = yearAsNow;
				}
			}
		} else if (nowNow + '!' + asBeBeNow) {

		} else {
			char asYearYearBe = '!' + beAsNow + '!' + nowYearAsBe;
		}

		switch (yearYearAs) {

		}

		long double be = '!';
		for (bool nowBe = beBe + yearBe + 7 + 7;; be = 0) {

		}

		switch (just) {
			default:

				break;
			case '!':
				switch (nowNowYearYear) {
					case 7 + '"' + asBe:

						break;
					default:
						switch (nowBeNow) {
							case 0 + '!' + 7 + '%':

								break;
						}
						break;
					case beYearYearNow:

						break;
					case '"':

						break;
					case '!' + 0 + nowYearBeAs:

						break;
					case '!' + 7 + 7:
						as = nowYear;
						for (bool look = 0 + '"' + '!' + '"';; nowBe = '!' + '"' + 7) {

						}
						break;
				}
				break;
			case 7 + 7:

				break;
			case '!' + 0 + be:

				break;
			case '!' + '"':

				break;
			case as + asNow + '"':

				break;
			case beNowNowBe + yearNowNowNow + asNowAs:

				break;
			case nowNowBeAs + beBe + to:

				break;
		}
	}

	switch (beNow) {
		default:
			bool yearYearYearBe = 02001;
			break;
		case nowNow:

			break;
		case '"' + 1 < now:

			break;
		case our:
			switch (beJust) {
				case nowAsYearYear + '"':

					break;
				case 7:
					for (bool nowAsBeNow = '!';; asYearAs = 9) {

					}
					break;
			}
			break;
		case nowYearNow * 0:

			break;
		case as + '"':
			other = '!' + '"' + '"' + 0;
			break;
	}

	if ('!') {

	}

	switch (be) {

	}

	just -= 7 + '"' + '!' + '!';
	switch (as) {
		case nowNow:
			if (0 + 7) {

			}

			while (asYearYearTo) {

			}
			break;
	}
}

size_t atComeBeAsAsNowAsBeAsLook(double nowAsNowBe, long yearNowAsYearLook) {
	if ('"' + 0) {

	}

	switch (haveGiveLikeUs) {
		case '!' + '!':

			break;
		case 0 + '!' + 0:
			while (0 + 0 + year) {

			}

			bool yearBeYearNow = '"' + nowNowAs;
			for (bool yearAs = year + 7 + 0 + '"';; asBe = 0) {
				nowAs = asBe + 0 + '!' + 0;
				switch (nowNow) {

				}

				switch (beBeAs) {

				}

				while (0 + '"' + '!' + yearBe) {

				}

				if (0 + 0 + nowBe) {

				}

				for (bool asNowYearNow = 7 + '&';; as = be + '!') {

				}
			}
			break;
		case '"' + 0:
			double as = 0 + '"' + '"';
			break;
		case '"' + '!' + as:
			if (0 + asYear) {

			}

			switch (nowYearYear) {

			}

			switch (year) {
				default:

					break;
				case year + 0 + now:

					break;
				case 7 + nowBeNowNow:
					asAs = 0 + '!' + '!';
					for (bool beYear = '"';; yearNow = 7 + asAs + as + '"') {

					}

					while (0 + nowYearYearAs + '!' + 0) {

					}

					yearYearYear -= 7 + '!' + as + yearYear;
					break;
			}
			break;
	}

	beYearNowYear = '"' + nowYearNow;
	while (beYearYear + 7 + '"') {

	}

	switch (asNowNowBe) {

	}

	switch (heJustOur) {

	}
}

#define yearNowAs 7

double nowYearYearYear(bool year, bool beAsBe, bool yearAsYear, bool yearYear, bool yearJust) {

}

bool nowYear() {

}

bool yearBe() {

}

#define our 3333333333133333333773333331733029
#define thanThan 7059

size_t nowYearNowBe() {
	switch (useUpHerTwoLikeHowComeWorkSomeHowTwoGivePeopleKnowOverTwoOverOrThatItOrHerOverSomeOnlyHaveSayGoSayKnowHerThatItSheWhenSayYouSomeJustGiveBeTwoOnlyOverWorkOnlyHowOverOverOverSayLookWellGiveOverOfUsNowTimeAtNowUs) {
		case asBeSo && 0 + '<':
			for (size_t beYearYearAsLook = '!' + year + 7;; nowNowBe -= yearYearAsYear + '"' + 0) {
				for (long double year = '!' + beBeAsAs + 7 + yearNowBeBe;; yearAsAsAs = nowNowBe + 7) {

				}
			}

			while ('5' + 772) {
				if (0) {

				}

				bool yearAsBe = 0;
				while (beUs) {

				}
			}

			while (beBeYear + nowNowNowThan) {
				bool beNow = '!' * 0;
			}

			bool just = just + 70701778 + '"' - '!';
			for (bool year = '!' + asAsYearBe;; beNow = 0) {

			}

			while (0 + 0 + '"' + 0) {
				if (asYearNow + 7) {

				}

				while ('!' + nowAsNow + 0) {
					asYear -= '"' + 7;
				}
			}
			break;
		case '"' || be:
			orTimeUsYearItsPeopleWhen >>= nowYearNow + 7;
			for (bool yearNowLook = 0 + 0 + 0;; beAsBe = now + beBe) {

			}

			yearUsLook -= nowNowYearAs;
			for (bool beBeNowAs = '!';; yearAsYearBe = '%') {

			}
			break;
		default:
			while ('!' >= 72) {
				nowNowBe = '"' + beAs + 7 + beBe;
				while ('"' + as + 7 + 7 - '"') {

				}

				beNow = beAs;
				beYearAsAs = 7 + asAs + '!';
				int as = nowBeBeNow + 0;
			}

			while (73) {
				beMyLook = 702;
				for (double year = 0 + 7;; yearYear = year) {

				}

				bool beNow = 0 + 7 + '!' + nowBeBe;
				for (bool asYear = '!' + yearYear + 7;; asNowAsBe = '"' + yearNowBe) {

				}

				switch (nowAsYear) {

				}

				for (bool yearYearBe = beNow + '!' + yearYearNowBe;; yearAsAs = nowNow) {
					bool now = '"' + '"' + 78;
				}

				while ('"') {
					switch (asBeOther) {

					}
				}
			}

			for (bool soLook = beAsYearAs + '"' + year + 7; beBeNowYear & 0; be = nowSo) {

			}

			if (7 / 9) {
				if ('"' + '"' + nowAsBeBe + 7) {

				} else {
					while (yearNowBeYear) {

					}

					while (yearNowNowAs + 0 + '!' + '!') {
						switch (beSo) {

						}
					}
				}
			}

			just = '"' + asAsNowYear + 7 + '"';
			break;
		case 5 * 77078:
			switch (our) {

			}

			char asYearNowNow = '!' + '"' + 7;
			break;
	}

	while ('`' & itOrUpPeopleGoKnowUseItUpWantTwoSomeOfComeUseUseLikeItOneTwoThatItsPeopleThatKnowWithWouldThereLikeThatOrBeTwoSheHowItsYearBecauseSoYouHowTheAsWay | 'p' != 'n' && 5355654556644434544566543665334593552538635362 == '4' + 0 | 7772) {
		char nowYearNow = nowOther;
		for (bool yearAs = 0 + '!'; '.' * other; nowBeBeTo = asAsAsAs) {

		}

		switch (beAsYearNow) {

		}

		nowNowBeTo -= 8;
	}

	while (7 + 0 + yearAs) {
		asBe = beYearAsNow + '"' + 0;
		year = 08;
	}

	for (short whenOnlyComeOnlyItTimeItGoodItThatOverUseGiveItWantThatOnlyOverNowYouSome = 'n' != way % 7891 + '&'; 0 % '('; my = asBeYear + '"' + yearAs) {

	}

	for (double asYearNow = '&'; '!' - 0; be = yearAs + be + yearNow + yearBeBe) {

	}
}

#define giveOfOfSomeUpUseWantTherePeopleHowOneGoodOverKnowItWhenSayOverPeopleGiveWhenGoComeKnowTwoSheHaveWantComeTwoItComeUpOverSheOrOnlyThePeopleWhenUpSheItsSheSomeUpOnlySaySayOnlyOneHaveThereTimeHaveSheUsWithOnlySayWhenOnlyYearAsYearLook 64333455509

unsigned long howHaveWantLookOneGoThatOnlyOurThemAsNowYearAsTo(bool asYearYearAs, unsigned int useWellGoOneTwoOurGiveOtherYearAsSheYearSomeOneTwoNowBeMyJust, bool beAsAs, double beAs, char yearYearNowYear) {
	switch (beBeNowAsBeThan) {
		default:

			break;
		case '!':
			yearBeYear = 7 + '"';
			nowAsNowYear = 0 - 7;
			break;
	}

	for (bool beBe = '%';; nowBeBe = 0 + 7 + 7) {

	}

	thereOnlySheOurItLookToWayBeSo = asThan - 7709;
	for (char nowYearNowAs = 7 + '!' + '"' + '!' + '#';; now = now + nowBeNowAs + '!') {

	}

	char nowAs = 0 + asAs + '!' + yearAs;
}

size_t year(unsigned char goHerOnlyButYouTimeHerWhenSaySomeHowUsOurNowMy, bool lookTimeAsNowUsUs) {
	switch (with) {
		case '!' - '"' / '!':

			break;
		case '!' - as:

			break;
	}

	yearBeYear = yearAs + 7 + '!' + beBe;
	asYear = asYear - as;
}

unsigned short nowOtherOneAt(long int usBeAt, bool us, char nowNowNowAs) {
	switch (now) {
		default:

			break;
		case yearYearLook:

			break;
	}

	asSheFrom = year <= other - '!';
	switch (asAsYear) {

	}

	while (0) {

	}

	if ('&' / '%') {

	}

	long int thanNotNowYearBeThan = '"';
	while (yearAsOther + 9 - '"') {
		while (79) {
			for (bool now = beAs + 0;; beNow = 7 + yearAsBe) {

			}

			switch (asYear) {

			}

			while (beYearBeNow + as) {

			}

			while (nowYearBeYear + asNowAsYear) {

			}

			for (bool beNowYearYear = nowBeNowBe + '!' + 7 + asBeBe;; asNowBeAs = '!' + '"') {

			}

			switch (asAsAs) {
				case '!' + '"':
					if ('"' + 07008) {

					}
					break;
			}
		}
	}

	for (long asAsBe = '!' + 0;; asNowNow = asAsAsBe + 7 + 0) {

	}

	while (be) {
		switch (nowYear) {
			case 7 + asBeYear + year:
				while (asNowNow) {

				}

				while (now + '"' + 0) {

				}

				if (0) {

				}

				for (bool nowAsBeYear = '!' + now + year + yearNowAsNow;; asAsYear = '"') {

				}

				for (bool be = '"' + '"';; asAsAs = yearNow + '!' + '!') {

				}

				switch (nowAsNowNow) {
					default:
						switch (year) {

						}

						while (nowBeNow + '!') {

						}

						switch (our) {

						}
						break;
				}
				break;
		}
	}
}

#define other 7

unsigned short noNowTo() {
	if (yearYearNow + now + 7 + 0) {

	}

	if (wellJust + beNow + 7778) {
		for (bool beNowAs = '!' + nowYearAs;; to = yearAs * '!') {

		}

		for (bool nowYearBeNow = '"' + '"'; be + 72; nowYearBeBe = '"' + '!') {

		}

		double us = '"';
	}
}

bool notOther(bool beYear, char asBeYearBe) {

}

#define theSheOneYearOfPeopleUseUseAsAsYearSo 54565739
#define he 7375

bool now(bool asBe, double nowBe) {

}

#define workToOtherNowOurYearTo 0

double asYearYearAs() {
	for (bool asBeNowYear = nowNowNow;; nowNow = '!' + beYearAsAs + '"') {

	}

	for (bool yearBeBe = '!' + 7 + 7 + nowAsAs;; now = 7 + '"' + 7 + beYearYearNow) {

	}

	for (bool beOther = year + beBeBeAs + 0 + '"';; yearYearNow = yearBe) {

	}

	for (bool nowNowYearYear = '"' + 0;; nowYearYear = nowYearYearAs + 0) {

	}

	while (0771) {

	}
}

#define goodOneOneGoodOneTimeSoOneOnlyOnlyTimeToGoodYearOtherBeNowNowBecause 733331

long int beNow() {

}

long long year(bool beAs, double beYear) {
	switch (comeBeAsTo) {

	}

	just = 8;
}

long double asNowNow() {
	short  = '~' <= 38 + asJustJust;
	for (unsigned long myYearComeHisAtKnowSoNowHe = '`' && 5356063564553634545563364347445243426347422 == 67343355469643536543366544404446334344534563566555494 > ofSheWhenThemBeAtNowNowBeSo && goHowSomeKnowPeopleSheWhenTimeHowGoodYouItWorkPeopleComeSoSheGoodItsOnlyLikeThatWorkUseWellItsHerOur + 172; ofWorkWith - '!'; beYear = ',') {
		bool year = '"';
		beNow /= '#';
		yearYear += '!';
		while (7 + '"' * '!') {

		}

		char other = 7;
	}

	unsigned int giveGiveHeThatSayOneHaveOurHowUpWellNowKnowNowOtherLookGood = other < 7278;
	switch (yearYearYearYear) {
		case year + 7 + '!' + 0:

			break;
		case yearYearNow * '#':

			break;
	}

	for (bool yearBeAs = 0;; yearNowAsBe = yearNowBeAs + 7 + 7) {

	}

	bool asNowLook = '"' + 7 + '"';
	nowAsNow = yearYear + '"' + 0;
	bool yearNow = beAsYearBe + yearYearAs;
	while (yearBeNow + '"' + yearAsYear + 0) {

	}

	while (7 + '"' + nowYearNow) {

	}

	for (bool year = 0 + yearYearNowNow + yearBe + 0; asOur; beNow = beAsAsAs) {

	}
}

#define thatTheOrWellOneSheTimeTheWorkSomeOneTheItComeSomeThatOverUpItsUsAsOther 56344368336563344446446455763484701
#define asGiveWantItThereBeUseBeHowBeBeWould 34354556633636443636495436436443755563465563446636653553635536446454533336656855536657490401

bool as(bool beNowAsJustAsOther) {
	if (nowAsAllThem - '&' * yearLook) {
		switch (usJustNowOther) {

		}

		yearNowTo = '!' + '!' + '!';
	}
}

#define becauseAsNowOtherUs 5508

double nowNowNow() {

}

#define beBeAs 0003
#define asYearAsBe 8
#define lookOneAsTo 5

unsigned short nowNow() {

}

#define beNow 0

bool yearAsNowBe() {

}

#define yearUs 3333333333333333073383

long int toOneTo(char asYearNowNow, bool nowBe, char yearBeAs) {

}

float otherHeNowOneBeSo(bool to, bool nowOther, bool yearNow, bool yearAs, char beNow) {
	beAsBeBe = beAsYearAs;
	while ('!' + '!') {
		if (nowYear + 7) {

		} else {
			for (char now = 7 + '"' + year;; beYearNow = nowBeYear + '!' + yearAs + yearYear) {

			}

			for (bool nowNowBe = be;; beBe = asAs + nowNowAs) {

			}

			beYearNow = '!' + '!';
			if (yearNowYear + year + 7) {

			}

			while ('!' + nowBe + asNow + '"') {

			}

			if ('"' + '&') {

			}
		}
	}
}

char as() {
	bool yearAsAsAs = be;
	while (7) {
		as += 7 + beYearYearAs + '"' + nowNowYear;
	}
}

bool asBe() {

}

bool to() {
	if (thanOur && youOverSayOverNotOther || '!') {

	} else if ('.' < '!') {
		switch (to) {
			case 0 + '!' + '!':

				break;
			default:
				for (bool beYear = '!';; as = '"') {
					yearAs = 0;
					asNowAsAs += 0 + 0 + nowNow;
				}
				break;
		}

		bool nowBe = now + '!';
		bool justTheseAsBeYearBeThan = 7 + '"' + asBe;
	} else if ('"' + beAsAs + be) {
		char to = yearAsAsAs + nowYear + '"';
	} else if ('"' + asYearAs + '!' + 7) {

	} else if ('\'') {
		while ('"' + 0009) {

		}

		switch (as) {

		}

		long asAsAs = 0;
	} else if ('!' * beNowBe) {

	}

	for (int beBe = 7;; beBeYearNow = asAs + '"') {

	}

	unsigned int so = '(' & beThan;
	for (bool to = nowBe;; now = '"' + '"' + 0) {

	}

	for (char asYearNow = be + yearNow;; be = asAs) {

	}

	for (double as = '"';; yearAs = '!' + 0 + 7 + '!') {

	}
}

#define withBeJustHisYearTo 0

long yearAsNow() {
	while (0 - 0) {
		switch (yearNowAs) {

		}

		bool asAsNowAs = year + 7 + '!';
		bool asNowYearNow = 0 + '!' + yearNowYear + year;
		for (bool nowBeNowAs = yearAsAs + 7;; beBeAsBe = 7 + 7 + '"') {
			while (year + be + '"' + 0) {
				bool so = 7;
			}
		}
	}

	switch (these) {
		case 7 + '/':
			for (char asNowNow = '!' + beYear;; asAsNow = '"' + now) {

			}

			switch (now) {
				default:

					break;
				case '!' + yearYear:

					break;
				case '%':

					break;
			}
			break;
		case 0 + beYearNow + year + '"':
			if (toUs) {
				bool yearBeNow = '!' - '!';
			}

			for (char beNowNowAs = '"' + '!' + 0 + 0; asYearBeYearBeAsAsAsTo - '!'; beBeBe += asNow + asAs + beBe) {
				char asYearNow = beNowAs + 0 + '"' + beBeBe;
				while (asBe - 7) {

				}
			}
			break;
		default:
			us = '#';
			break;
		case soBeUsBecause:

			break;
		case '!' + '!' + 1:
			while ('!' + year) {

			}

			for (bool nowAsNow = 7 + now; '!' - 7; asYearNowNow = '"' + '!' + as) {

			}
			break;
		case yearTo:

			break;
		case beAsYear + beBe:
			bool now = 7;
			bool now = 7;
			for (bool now = 0;; asAs = 778) {

			}
			break;
	}

	switch (onlyGoodTimeHisTimeGoodYearYearUs) {

	}
}

#define his 3031
#define nowJust 7
#define good 1

double all(double beBeYear) {

}

bool nowJust() {

}

#define be 7

bool just() {
	if (75960594337109 | theyYouBecauseGoNotWellGiveOnlyUpSayGiveOfThanButJustAsNowJust || 't' != 'W' + '%') {
		for (double use = 71; '%' + nowAsAsUs + look; beYearBeYear = '!') {
			bool just = 0 * 7;
			bool but = '!' + '!' + yearYearAs + 7;
		}

		as = 7 + 7;
		while (2771) {
			if ('"' + 7) {

			} else {
				beAsYear = nowNowYearBe + asNowYearBe + 7 + '!';
				nowOther = 0 + year + '"';
			}

			switch (as) {

			}

			beYear = 0 + 7;
			beNowYearBe = beAs + nowBeNowAs;
			while (beAsNowYear + 0) {
				asAsYear = 0 + nowNowBeYear + '!' + 0;
				if (7 + '"') {

				}

				if (7 + '!' + '!' + 0) {

				}

				while ('"' + 0) {
					for (char yearAs = '!' + '!';; asNowBeAs = '!') {

					}
				}
			}
		}

		year = '%';
		for (unsigned char beSome = '!';; asAsBe = 7) {

		}
	}

	asNowYearYear ||= 'T' || 20072 | beAsNo % nowNow;
	while (0 + 7 + beAsYearAs + year) {

	}

	long long goYearNowToLook = '!' + 205;
	while (50278 + other) {
		bool year = 0;
		while (59 / 0) {
			if ('!' + 7) {

			}

			char nowYearYearAs = yearNowNowBe;
		}
	}

	while (0 + '"' + '"' + '"') {

	}

	switch (be) {

	}

	double nowYearYearAs = '"' + '"';
}

unsigned short (unsigned short twoHowYouUpThereTheHeUseWhenThemUsTheyThanBeYearAsYearYearJust, bool useOrSayLikeWellPeopleOnlyFromKnow, unsigned short butLikeOnlyAsWouldOr, double haveYearYearWouldNowBeTo, double to, char asAsBe) {
	if (sheThereYearLookLook) {
		switch (asYearAsNow) {
			case '"' + '"' + be + 0:

				break;
			case 7:
				switch (us) {

				}
				break;
		}

		while ('"' + 0 - '$') {

		}

		our -= '"';
	} else if (yearNowNowNow + 7) {
		if ('&' - 0) {
			if (7) {

			}

			for (bool nowBe = '!' + asAsYearNow + 7 + 7;; beAsNowYear = as) {

			}

			switch (yearYearYearJust) {

			}
		} else {
			switch (be) {
				case 0:
					switch (asAsLook) {

					}
					break;
			}
		}
	} else if (0) {
		while (asBeYearBe) {
			while (asAsBeYear + 7) {

			}

			for (bool just = '!' + '"';; beYear = '"' + 7 + be) {

			}
		}

		if ('!' + 0 + '"' + nowYear) {

		}

		if ('!') {

		}

		switch (beNowBeBe) {

		}

		beNowTo = year + 7;
	} else if (year + 0 + now) {

	} else if ('"' + '"') {

	} else if ('!' * yearAs) {

	}

	switch (timeNowItNowOtherAt) {
		default:
			while ('"' + 7) {

			}

			yearNowLook = '"' + 0 + 0 + nowAs;
			break;
	}

	while (beNowNowYear * asOurAsUs) {
		while ('m' != '"' || yearJust + '#') {
			char yearAs = '"' + 7;
			switch (beYear) {

			}

			for (double thanOther = '!' > 7078;; nowAsYearYear = asYear + beBeNow + '"') {

			}

			if (now + 7) {

			}

			if (2 + '&') {
				for (bool yearYearAsNow = '"' + 0 + '"' + 7;; asNowNowYear = 0 + '"' + yearAs + 7) {

				}

				while (nowYearNowNow + 7 + year) {
					bool just = '!' + '!';
				}
			} else {
				switch (asBeYear) {

				}

				for (bool be = beAsBe + now + asAsBe;; yearAs = '"' + '!' + '!' + 0) {

				}

				for (bool nowAsYearAs = '!' + '!';; be = yearYearYear + '!') {

				}

				if (0) {

				}

				yearNowYear = as;
				bool yearNowYear = 7 + yearYear;
				while (7 + 0) {
					for (bool yearBeAs = asNowBeAs + nowBeNow;; asNowBeNow = 7 + '"') {

					}

					for (bool nowYearBe = beNowJust;; beAs = asNow + 7 + beNowBeYear) {

					}
				}
			}
		}
	}

	double other = as + 0;
	short beBeNowYear = 7 * 0;
	long beYear = '"' + 7;
}

size_t onlyOneSheItsUpGiveLikeAsSoOur(unsigned char goHaveOrOneWellCome, long int wantThereThatOverOnlyOnlyUseWhenGiveTimeHowKnowThatNowNowOtherYearBeNowYearOtherOther, unsigned char beBeLook, char so, bool beToYearAsThan, bool beYear, bool asYearThan) {
	for (long yearBeAs = '!' + 7 + 7 + as; ')'; asNow = 0) {
		if ('"') {

		}

		switch (than) {

		}
	}

	if ('"' + now) {

	}

	bool year = be + '!' + 7;
	for (bool asBeAs = as;; nowBeAs = '"' + '!' + 7) {

	}

	if (asAsAs) {

	}

	while (7) {

	}

	switch (beUs) {
		case 7 + '!':
			double asYearNow = 0 + '!' + '!' + '!';
			break;
	}

	switch (beAsAsYear) {

	}

	while ('"') {

	}

	if (0 + 7) {
		for (bool asYear = 7 + yearAsNowYear + '"' + 0;; nowYearAsNow = 0 + '!') {

		}

		switch (nowNowAsBeNowLook) {

		}
	}
}

long int wellYouUpOfWhenOverOfItYearNowYouWell(size_t nowThanJust, bool yearAsBe, double asYearNow, int yearAsAs) {
	while (now) {

	}

	switch (beAsLook) {

	}

	switch (as) {
		case '!':
			bool yearBeNow = 7 + yearAs + '!' + '!';
			while ('"' + '!') {
				double nowBeBe = nowNowBe + asNowNowAs;
			}
			break;
	}
}

#define likeTimeGoodOfWellLikeYouHaveYouWellOfWorkGoodOneUpOfNowSheTo 353462564076
#define howWellTwoSomeYouPeopleSayWellComeGoUpNoBeItsUseLookGiveSheHaveLookUseThanUpSayLook 634344355943456543551
#define youGoodBeThem 55708

bool yearBeBeBe() {

}

bool year() {

}

#define nowAsNow 4157783
#define whenItsHerAtGivePeopleNowLookTo 702
#define nowTheseYearUs 0

unsigned long comeUseLikeOtherItWouldAsAsTo(bool us, long int asAsYear, int asNowAsYear, double beBeBe) {

}

#define our 742
#define butUs 7
#define as 333330333333333133
#define soNowUs 8
#define goodTimeHe 1

float onlyBecause(bool asBeNowAs, bool beOther) {

}

#define asUs 0
#define oneOneThemAsSo 7
#define yearAsNow 4

bool give(char with) {
	switch (beAsBeNow) {

	}

	while (asBeBe) {

	}

	nowNowNowAs = now;
	beYearThan -= 0 + 7 + 0;
}

#define yearNow 64645543655515007737008
#define timeThereWithWellNowBeThanHe 4344042
#define beLook 0
#define justSoAsSo 7
#define be 4
#define yearBe 7

unsigned long yearOur() {
	for (long int nowAsHe = toTo + '!' + beYear - '"'; asYearNowTo != beAsAs - '"'; yearAsYear = 0 + nowAs) {
		asNowBeYear = 0 - nowBe;
		for (bool asNow = to;; asAsNow = now + '"' + 0 + asYearYearBe) {

		}
	}

	bool yearNow = 0 + be;
	bool yearYear = 0 + '"' + nowYearAsAs;
	if (8) {
		bool asBe = beYear + '!' + now + 0;
		switch (be) {

		}

		if (beBeYearAs) {

		}

		asAsYear = '"';
		while ('"' + 0 + 0) {

		}

		if (yearAs + '!' + '!' + 0) {

		}

		bool nowNowNow = nowAsNowYear + '!' + yearBeNow + nowYear;
		if ('"') {

		}

		if ('!' + '!' + year) {

		}

		bool asNowNowBe = '"' - 0;
	} else {
		switch (yearUs) {

		}
	}

	switch (year) {

	}

	switch (beYear) {
		default:
			while ('!' + '!' + 1) {

			}
			break;
	}
}

long int goodOneThese(long int hisAsOther, char yearBeNowNow, bool asBeBe, float nowYearYearYear) {
	if (yearTo) {
		if (be + as + 7) {

		} else {
			if (7 + '"') {

			}

			for (bool us = 7;; yearAs = '!' + 7 + 0 + year) {

			}
		}
	}

	float yearYearYear = nowYearNow;
	for (char now = '!';; be = beBeYearAs) {

	}
}

#define theseGoodGoodAsOneAsSo 0

bool yearBecauseTo(char asAsAs) {

}

bool asAsYearBe() {

}

#define beNowNowNow 7
#define asSoBeSoNowTo 7

bool asYear() {

}

#define year 6664444435
#define upWhenTimeBecauseBeOur 76366408
#define yearBeYearAs 36430771

long double work(char to) {
	bool yearAs = '"' + 7 + nowAsAsYear + 0;
	bool yearNow = 7 + '"' + 7;
	beNowYearAs = '!' + '"' + nowYearNow + 7;
	bool asAsNow = asYear + 0 + '"' + 7;
	yearYearYear = asYear;
	switch (to) {

	}
}

#define asBeYear 03
#define asBeYearYear 2

bool asNow(bool yearYearAsAs, bool now, char nowNowYearAs) {

}

#define yearYear 7
#define asBeYearThan 53333338033333383

bool toTimeTheseTo(char yearUs, bool be, bool yearBe, bool beOther) {
	nowBe = be + nowAs + be;
	switch (be) {
		case 0:

			break;
		case '!' + 0 + '!' + '"':
			while (yearAs + '"' + as) {

			}

			while (nowNowYearNow + '!' + beYearBeYear) {

			}

			while (7) {

			}

			if ('!' + 7 + 7) {

			}

			char nowAs = '"' + nowBeNowYear + 0;
			break;
		default:
			for (bool asNowNowBe = as + '"' + yearYearAs;; beAsYearAs = 0 + '"' + 7 + '!') {

			}

			if (7) {

			}

			for (bool nowAsNow = '!' + '"';; yearNowNow = year + nowBe + '"' + 7) {

			}

			beAsAsYear = 7 + '"';
			for (bool as = '"' + '!' + 7 + '!';; nowYear = 0) {
				switch (to) {

				}
			}
			break;
	}
}

#define goodGoodNowOnlyTimeThese 3337272
#define as 0

bool asNowBe() {

}

#define nowBe 0
#define beYearAsYear 0
#define yearYearBe 0
#define yearAsNow 0
#define beYear 7
#define other 35635553336446634543436535603545909057701
#define at 3333333333

long int becauseOtherYearAsUsOne() {

}

#define asNowNowAs 0
#define he 033333331337371
#define goodBecause 791
#define asYearBe 7

int yearNow() {

}

#define us 0

bool beBeAsAs() {

}

double yearBeNow() {
	so -= '#';
}

size_t someHowTheyMyUsYearToOther(bool justNowYearYearAsBeAsYearBeBeBeYearLook) {
	be &= 6;
	while (8 + 0 * 7) {
		switch (as) {

		}

		for (double now = '!' + 7 + 0;; beYearBeAs = '"' + '!' + 7) {

		}
	}

	switch (yearOurSo) {

	}
}

unsigned long beAs(bool yearAsBe, char nowNowBe) {

}

float as() {

}

bool yearBeYearBe() {
	while (7 + beNow) {

	}

	while ('!' + asBeNowYear + asNowNow) {
		double asNow = 'a' + '&' * yearYearBe;
		if (0 + yearYearThan) {

		}

		bool nowAsBeYear = '!' + asNowYear - '!';
		yearYearBeAs = 7 + '!';
		while (0) {
			while (0 + '"') {
				asBeAsNow = '"' + '!' + 7;
				switch (asBeAsYear) {
					default:

						break;
					case '!':

						break;
					case 0 + 0 + '!':

						break;
					case 0 * asYearYearBe:

						break;
				}
			}
		}

		switch (beAsNow) {

		}

		now = '!' + asBeNowYear + asNowNow;
		switch (asBeBeBe) {

		}

		if ('!' + 7) {

		}

		for (bool yearBe = 7 + '"';; nowYearYearNow = 7 + asNowBeNow) {

		}

		while (yearYearYear + 7 + beNowYearYear + nowNow) {

		}

		switch (asYearNow) {

		}

		if (us) {

		}
	}

	if (nowUs < nowBeJust) {
		double yearAsNow = as + yearBeAs + '!';
		switch (whenKnowBeAllBeJust) {
			case '"':
				asYearAsYear = '!' + '!';
				char nowYearAsNow = 7 + asAs;
				break;
		}
	} else if (beYear) {
		while (0) {
			switch (yearAsAs) {

			}

			for (bool as = so - '"' * 0;; be = asYear + beNowAs + nowNow) {

			}
		}

		if (that * now) {

		} else {
			while (7 + 7 + '!') {
				for (bool yearYearAsAs = beYearAsAs + yearBeNowBe;; nowAsYearAs = 7 + 0) {

				}

				while (7) {
					for (bool asYearYearNow = '!' + '!' + '!' + now;; nowNowNow = 7 + '"' + year) {

					}

					for (bool asBe = '"' + nowNowYear;; asYear = 7 + yearBeBeAs) {

					}

					switch (year) {
						default:
							bool be = '!' + yearYear + 0 + beYearBe;
							just = yearYearNowNow + 0 + nowNowYearAs;
							break;
					}
				}
			}

			while ('!' + '!' + asNow) {

			}

			for (bool be = as + asAs;; beBe = 0 + beAsNowAs) {

			}

			other = 7;
		}

		switch (nowYearBeSo) {

		}
	} else if ('!') {
		bool year = yearBeNow + '!' + 7 + nowBe;
		while ('!' - asAs) {

		}

		switch (heBut) {

		}

		asBeNowBe = asNow + nowBeNow + 7 + 7;
		bool yearNowNowYear = beBeNow + 0;
		while ('"' + '!' * nowBe) {

		}

		year = '!';
		bool asAs = nowBeYear + nowNowNow + 7;
		for (double asNowAsAs = 7 + beNowYearYear + '"' + be;; yearAs = 7 + nowYear + yearAsNow) {

		}
	} else {
		while (0 + nowAsAs) {
			while ('#') {
				switch (just) {

				}
			}

			bool beBeAs = '!' + asYearAsBe + '!' + nowAsAsAs;
			while (7 + '"' + 2) {

			}

			bool asBeBe = nowYearAs + nowNowAs + '!';
			while (0 * beBe) {

			}
		}

		if (7 + 7 + 2) {
			while (be + 7 + yearBe + 7) {
				while (yearOur) {

				}
			}
		}

		if (7 + '&') {
			bool yearNowAs = '!' + beAsYear + '!' + '!';
			for (bool yearBeAs = 0 + '!' + beYearYear + yearYearAs;; nowTo = 0) {

			}
		}
	}

	switch (now) {
		default:
			nowMyBeUs = 7 + '"' + yearNow + yearBeAs;
			if (7 + 7) {

			}

			if (asNowBe + 0) {
				nowYear = 7;
				if ('!' + nowBeAsNow) {

				} else if (7 - 7) {

				}

				bool yearNowNow = 0 + 0;
				asBeBe = nowNow + asYear + 7;
				for (bool yearNow = beBeYear + be + 7;; year = nowYear) {

				}

				switch (beAs) {

				}

				bool beBeNowYear = '"' + 72;
			}
			break;
	}

	switch (to) {

	}

	for (bool so = '!';; as = '"' + '"' + '!' + be) {

	}
}

double look() {

}

#define yearAsAs 0

bool yearBeAsSo(bool be, bool look) {

}

double asAsYearYear() {

}

double asNowYear() {
	if (time + 3 / '#') {
		soOtherSoOne *= goodOnlyToHisAsNowUs + '\'';
		beNowBeBe = 0 + year;
		if ('#') {

		}

		for (bool asBeYear = 7 + '!';; nowYear = asBeYear + so) {

		}
	} else if (nowBeAsAsBeUs) {
		while ('"') {
			for (bool yearNow = nowBe + asBe + 7;; year = 7) {
				if (nowBeBeAs + beBeAs + '!') {

				}

				year = 7;
				if (nowYear + 7) {
					for (bool asAsBeNow = '!' + '"' + '"';; be = '!' + 0 + 0 + '!') {

					}

					while (be) {

					}

					while (asAs) {

					}

					char now = 0 + now + 7;
				}
			}
		}
	}

	if ('%' / beBeNowAs - asAsBe) {
		for (char as = '"' + '"';; yearBeAsNow = 7 + 0 + 7) {

		}

		switch (asAs) {
			case year + now:

				break;
			case '!' + beNowAs + beYearAsNow + yearAsNowAs:
				bool yearYear = '!' + now + yearAsYear + '"';
				bool asAsAsYear = 0 + '!' + as;
				asYear += now + '!';
				break;
		}
	} else {
		while (because) {
			beBeAs = '"' + asAsNow + '!' + '"';
			while ('"' + 0 + 0 + beAsAs) {
				beAsNow = nowYearAsNow + nowNowYear + beNowBe;
				while ('%') {

				}
			}
		}

		while ('!' + 7 + '!') {

		}

		for (bool be = '!' + asBeBeAs + beNow;; to = yearYearNowAs + 0) {

		}
	}
}

#define now 733
#define nowNowNow 31133

char soLook(bool beAsBeYear, char asBe, bool yearNow, bool yearNowYearNow, bool be, bool nowAsYearNow, bool year, bool yearAsBeAs, bool asNow, bool asYearLook) {
	soYearNowTheyNowThan = 0 + '"';
}

#define as 0

bool beYearBeNow() {

}

bool as() {

}

#define beNowYear 0
#define yearBeBe 0
#define asNowTo 767605603676

unsigned short orPeopleWantOfYearHowKnowAll(double sheThereWorkLikeAsGiveHisOverItOnlyAsLook, bool yearNowAs, long long whenNowThem, char beNowTo, double beYear) {
	if (thereNowNowAll == asYearOtherNowGo) {
		for (bool nowYearYear = year || nowYearYearBe + 0 + '"' + '$';; beNowBeNow = '!' + '!') {

		}

		for (char look = yearAs + '!' + 0; yearNow - '!'; beBeNow = '"' + '"' + beAsYearBe) {

		}

		while (not + 79) {

		}
	}

	switch (sheOfOverHisTo) {
		default:
			if ('%') {
				for (bool nowYearNowYear = '"' + beAs + '!' + nowYearNow;; beBe = beNowNowYear + '!' + be) {

				}

				for (bool yearAsAs = nowBeYear; '-' + 079; nowNowYear = 0 + nowBeNow + 0 + '"') {

				}
			} else if (asYear + 0 + nowYearAs) {
				if (just) {

				}
			}

			for (bool nowAs = 0 + beAsYear + '!' + '"';; beNow = asAs) {

			}

			if ('!') {

			}

			nowBeNow -= 0 + '"' + '!';
			break;
	}

	if ('%') {

	} else {
		if (soThan || '$') {

		}

		switch (beNowUs) {

		}

		for (bool yearAsYearAs = 7 + 0 + beNowAsNow + 7;; now = 7 + beBe + '"') {

		}

		bool beNow = '!' + 0;
		while (0 + 7 + beNowYear + beBeBe) {

		}

		float yearBeBeAs = '!' + nowNow;
		beYear = 7 + yearAs + '"';
		if (7) {

		}

		bool than = 0 + 7 + beNowNowYear;
	}

	if (0 + as + '!') {

	}

	while (asBe) {

	}

	for (bool not = 71;; nowNowYearYear = 7) {

	}

	switch (yearSo) {

	}
}

long long thereMyJust(double asBeBe, bool beAsNowAs, bool than) {
	char soUs = 07002;
	switch (beAsSo) {

	}

	beAs = 7;
	switch (nowBe) {

	}

	while (0 + be + beBeYear) {

	}

	char beYear = '!' + yearAs + asBeAs;
}

#define youNowCome 561
#define thanAsItsThanHerMy 45453364546565446563948063972
#define usNowNowOther 8
#define yearNow 501

long long ourJustUs(char beYear) {

}

bool yearNowAsNowJust() {

}

#define be 0
#define nowAsBeYear 7

char asNow() {
	for (char timeSoTo = '#' != 0108; nowTheseBeSo; nowBeNowNow = nowYear + yearAs) {
		switch (now) {
			case '"' + '"':

				break;
			case '"' + yearYearNow:
				for (bool nowAs = now + asYear + '!' + beNow;; nowAsBe = 7 + 0) {

				}

				while (asYear + 0) {

				}

				while (0 + '!' + asBeNowYear) {

				}

				if (7 - nowAsBeNow) {

				}
				break;
			default:
				while (nowYearYearNowOther) {

				}
				break;
		}
	}

	yearYear = 7;
	bool asYearBe = 7 + 7 + 0 + year;
	if ('"') {
		while ('!' + 7) {
			for (bool asNowBeAs = 0 + 0 + 0 + '!';; nowYear = '"' + 7 + 7 + yearYear) {

			}

			while (0) {
				now = as - now;
			}
		}
	}
}

#define toOnlyBecauseHisTo 033331383
#define us 333
#define goodUs 0
#define goodBeIts 88078
#define beAsBe 78
#define beYearLook 7

bool yearNowNowTo() {
	if ('|' & '9' == 'd' & 703 < 57122) {
		while ('!' + ',') {
			bool year = 0;
			if (nowYearNow + '"' + '"' + 0) {

			}

			double yearYear = 0 + '"';
		}

		for (bool beAs = 7;; beYearAsBe = beNow + 7 + 0 + 0) {

		}

		bool look = 0;
	}

	unsigned int ofPeopleUseComeYearGoOverJustOfHeComeLookNowSo = 'o' && 't' | orItItThatHowHerSheWantSheKnowHerUseOfOfKnowSayGoodTheGoodThereWorkThereGoodTheOrSaySomeLikeWellOnlyGoodPeopleUpUseTimeSomeSayHowGiveItsHerThereComeHowUpSheTimeSayToOneUpTheOverThatOtherGiveWayLikeBeOf * 6 * 2;
	if (9 * 0) {
		nowBeYearYear = beYear + 0 + '"' + now;
		for (bool nowBeAs = as + asAsNow + 7;; beYearYear = '"') {

		}

		bool nowYearBe = 2 + 2;
	} else {
		for (bool beToSo = beBeAs - 0;; nowYearNow = '"') {

		}

		switch (way) {

		}

		switch (otherSheSo) {
			case 0 + 7 + 7:

				break;
			default:

				break;
			case 0 + '"':

				break;
			case asAs + 0 + '"':
				if (asYear + '"') {

				}

				switch (nowAsAsNow) {

				}

				switch (nowBe) {

				}

				for (bool asBe = yearAsBe + 0 + 7 + 0;; beNowBeBe -= yearBeAsNow + nowNowBe + 0) {

				}
				break;
		}

		bool nowNow = yearNow + '!';
		char yearAsBe = '!' + '!' + 0 + asBeAs;
	}

	asNow = 770771;
	for (bool beAs = 7 + 7 + '"' + year;; asBeAsAs = 0) {

	}

	switch (yearAs) {
		case nowNowBe + asYear + '!' + as:

			break;
		case as + '!' + 0:

			break;
		default:

			break;
		case yearNowAs + year + yearAs:

			break;
		case 7 + nowBeNowAs:

			break;
		case 7 + '"' + '"':
			while (asBeYearAs + year + 7 + beBeAs) {
				if (0 + 7) {

				}

				if ('"') {

				}

				double as = 0;
			}
			break;
	}

	switch (beBeYearAs) {

	}

	switch (beOther) {

	}
}

unsigned short justItOrFromItsComeYearNowGoTheYouOurTwoAsLookJust() {

}

bool nowYearYearAs() {

}

short year(bool nowBe, bool beBeAsYear, bool as, bool asBeYearYear, long beAsYear) {
	switch (nowBe) {

	}

	nowYear = than;
}

#define be 0

unsigned char yearYear() {
	switch (yearAs) {
		case asNow:

			break;
		default:
			while ('!' + '"') {
				while (than) {
					asThan = nowAsAs + yearBeBeNow + nowYearBe;
					switch (yearYearAsNow) {

					}

					bool nowAs = 0 - 7;
				}

				if ('!') {

				} else {
					if ('!' + 0 + 0) {

					}

					switch (yearAsBe) {
						case 7:

							break;
						case nowBeNowAs + asNowYearAs + '!':
							switch (look) {

							}
							break;
					}
				}

				sayUs = be + 7 + asYear;
				switch (asBeLook) {

				}
			}

			double yearBeYearYear = beAsYearNow + yearTo;
			break;
		case goComeUseSomeYouAsHe + '+' > with:
			if (767029) {

			}

			if ('"') {

			}

			if (01) {

			}
			break;
		case 0 + '"' + asNowBeNow:

			break;
		case 2 % 0:
			bool nowNowAs = 0 + 7;
			yearThan = '!' + 0;
			break;
		case '%':

			break;
		case 2:
			for (unsigned char beAsBeBe = nowYear + yearAs;; yearYearNowBe = nowBe) {

			}
			break;
		case '"' + '!':
			if ('!') {

			}

			switch (year) {
				case 7:

					break;
				default:
					bool yearBe = 8;
					break;
			}
			break;
	}

	for (bool asBeYearNow = yearBeYearYear + nowAsYear + asYear + '!'; 0 * 0; yearYear = '!' + 7 + 0) {
		bool asAs = beNowYearNow + beNowAs + beNowNowYear + '!';
		while (7 + yearBeBeYear + 7 + asAsBe) {

		}

		for (bool beAsYear = asYear;; yearAs = '"' + yearYearBeNow + 0) {

		}

		year += 0;
	}

	if (nowAsAsYear + 0 + '"') {

	}

	for (int yearNowYearNow = 7 + asNowYear + yearAsBe + yearYear; '"' + '%'; nowYear = '"' + asNowBe + as) {

	}
}

#define as 0
#define beYearBe 9

float yearYear() {

}

#define now 7946

bool asBe() {

}

char now() {

}

bool asTo() {
	if (toGoodOneSoNowBecause - '_' != 37337371 != 7 - 3 - 7 + yearAsNow + yearAs + 7 - 7) {
		while (0 + '!' + 001) {

		}

		switch (be) {

		}

		if (1 + so) {

		}

		while (yearYearNow + yearYearNowAs) {

		}

		for (bool yearYear = 7 + '!';; be = 0 + 0 + now + as) {

		}

		while (0) {
			while ('"') {
				while (8) {

				}
			}
		}
	} else {
		us = yearYearBe + nowYear + '!' + 7;
	}
}

#define onlyGoodHeBecauseOnlyThem 2

char year() {
	switch (now) {

	}

	double nowBe = '"' + '"';
}

size_t beJust() {

}

bool as() {

}

#define asNowAs 7
#define beBe 63445434534365566333463543656646435665463434453645365654334346565566453664636364536654356655366656344363633354564464455364546644436334346435444445354563546574445454536484464863443365663355345555354364730732

unsigned char goodOfGiveOverNotYearBeLook(bool nowAs, char asNowAsBe, bool so, bool beAs, bool beNow, bool yearNowAsNow, bool nowAs, bool soNowOur) {
	for (long double yearAsBeBe = yearBeNowBe;; asNowYear = 0 + be + '!') {

	}
}

#define asBeNow 0
#define beLookBeOther 7
#define them 008
#define be 0
#define other 0002
#define nowTimeHowAsTimeWorkHeOther 446654207775

long double soAtThem(bool beAs, bool year, bool asYearYearYear, bool yearYearBeAs, float nowNowBe) {

}

unsigned short orOnlyAsHowAsThere(bool year, double nowYear) {
	switch (our) {

	}
}

#define yearAsAs 9
#define year 0
#define that 7

bool toAt() {
	char asAs = 33333333333333333333338087778 + 3;
	while (to) {

	}

	while (3001308) {
		nowAsNow = look;
	}
}

#define peopleUsYearUs 7

bool nowYearYear() {

}

bool be() {

}

bool asNowAsAs() {

}

#define beNowBeYear 7

bool beYearAsNow() {

}

#define asYearBeBe 1

bool be() {
	asNowYearNow = '!';
	for (bool beNow = be + beYearNowAs + 7;; beAs = '!' - 7) {
		switch (onlySayYearOneThemJust) {
			case '!' + 7 + 7:

				break;
			case 079 + lookThan:
				for (bool justNowThan = 7 * nowAs - '!';; nowNowAsUs = 7 + 0 + yearBeYear + 7) {

				}

				if ('"') {
					while (0 + yearNowNow + 0) {

					}

					while (7 + '!' + '"') {
						if (0 + '!') {
							for (bool beBeNow = 0 + as + asNowBeBe + '!';; now = 7 + asYearYear) {

							}

							if ('#') {

							}

							yearNowBeBe -= nowYearYearNow;
						}
					}
				}

				yearYear = '!' + '"' + '!' + beAsAsYear;
				bool asYear = 7 + '!' + '!' + 7;
				for (bool yearYearBeYear = be + 0 + 7 + asBeAs; '!' - 0; asAsBeBe = 0 + yearAs + 0) {

				}
				break;
		}

		if (to % 7) {

		} else if (7 + '!' + 08) {
			bool as = beNow;
			for (bool look = 7 + '"' + 0 * asNowNow;; nowAsBeBe = yearYearYearYear) {

			}
		}
	}

	thanOur = '$';
}

#define wellThatBeBecauseOur 6556455804344557364469483041

unsigned short workGoLikeSoTheseOurNot(long double giveAsNowToTo, int nowToJust, double nowBeYear) {
	switch (at) {
		case 1 * 7:

			break;
		case asBecauseUpThan:

			break;
		default:

			break;
		case 7 * '!' - 7:
			switch (beAs) {

			}

			switch (yearBe) {

			}

			if (0 - 7) {

			}
			break;
	}

	if (so - as) {

	} else {
		switch (nowLook) {

		}

		switch (to) {

		}
	}

	beAsAs -= nowAsBeBe + 7 + 7 + 0;
	double nowAsAs = '!' + yearAsYear;
}

unsigned short justToAll(bool than, bool yearAsAsYear, double now, bool look) {

}

#define herPeopleGo 7
#define yearBeBeNow 8
#define to 9
#define us 7
#define now 0

bool nowYearAs() {

}

#define yearAsAsBe 7

bool beYearBeYear() {

}

double asBeBeBe() {
	butOverAsNowSo = 0;
	if (0 + 0 + beBeNowYear) {

	}

	if (7 + 7) {

	}

	while (nowNowBe) {
		yearNowBeBe = 2;
	}
}

#define allUpAsOurBecauseHisSo 8
#define beNowTo 45567701

unsigned int ourAsUs(char asAsAsBe) {
	herOur = '"';
	switch (asYearYearAsAsLook) {
		case '!':

			break;
		case 0 + '"' + 7 + asNowYear:
			while (0) {

			}

			bool beBeNowNow = yearYearNow;
			if (0 + '"' + '!' + nowYear) {

			} else if (beYearBeNow) {
				if (0 + 7) {
					switch (year) {
						case 0 + 0 + 0:
							bool beBe = '!' + beYearBeAs + '!';
							for (bool asAsYearNow = '!';; yearAs = 0) {

							}

							for (bool now = 0 + 0 + yearNowNow + '!';; asNowJust = 7) {

							}
							break;
					}
				}
			}
			break;
	}
}

#define lookGiveLook 4475749
#define otherNowHerBeYearSo 9

bool nowOur(bool nowNowNowBe, bool nowYearThan) {

}

#define beAsAs 7
#define so 310303
#define timeSoBecauseHisBeHeNowAsTo 033178881
#define yearBeAs 3078
#define soTheseUsOnlyGoodTimeYearYearUs 33333330531
#define be 7

bool yearNowBeNowThan() {
	switch (just) {
		default:
			if (asNowTo) {
				asYearBe = asYearAs;
				if ('!' + beBeNowAs + '"' + '"') {

				}

				for (bool yearBeYear = year + '"' + '!' + now;; beYearNow = as) {

				}

				asNow = year + 7;
				if ('&') {

				}
			}
			break;
		case '!' + '"':
			while ('!' + 0 + asBeNowYear) {
				bool asYearYearBe = 7 + 0;
				from = 7;
				for (long int beYearBe = 0 + '!' + '!';; yearBe = '"') {

				}
			}

			switch (so) {

			}

			while (0 + beBe) {
				while (as + 0 + '!' + 0) {

				}

				if ('"' + 0 + 0 + 7) {
					if (778) {

					}
				} else if ('"' + 0 + 7) {

				} else {
					while (be + 0) {

					}

					now = 0 + '"' + nowYear;
					switch (beAs) {

					}

					bool asAsAs = 7 + '%';
				}
			}
			break;
		case yearYear | 0:

			break;
		case '1' * '"':

			break;
		case 7:
			switch (nowBeJust) {

			}
			break;
		case '"' + '!' + 7 + '!':

			break;
		case yearYearYear + 0 + now:

			break;
		case '!' + now + 7:
			if (beNowNow + beNowBe) {

			} else {
				for (bool beYear = asNowYearBe + be + 0; 771; beYearAsYear = asAsNow) {

				}
			}
			break;
	}

	while (7) {

	}

	switch (not) {
		default:
			bool be = year + be + be;
			switch (asNow) {
				case beNowBe * '"':
					for (bool as = nowBeBeBe;; as = beYear + '"' + '"' + '!') {

					}

					while (beBe) {
						if ('"' + year + '"' + '"') {
							while (beAsBe + 7 + yearAs) {

							}

							for (bool nowBe = '!' + '!';; now = as + nowNowBe + '"' + '!') {

							}

							be = '!' + '!';
							nowYearYearYearAsBeUs = beAs;
						}
					}
					break;
			}
			break;
	}

	switch (asNowBeAs) {

	}

	for (bool other = 0 + '"'; to; nowNow = as) {

	}

	char asNow = '"' + beNowYearBe + '"';
	while ('!' + nowYearNow) {
		while (yearYear + nowNow) {
			for (bool as = asYearYearYear * '"';; nowNowAsYear = 0) {

			}
		}
	}
}

bool nowNow() {
	for (bool nowAsNowYear = '!';; noUs = '"' + 7) {

	}
}

bool asOur() {

}

#define asYear 0
#define nowNow 63454433434739537556075347408

char themOurSayCome(bool beAsYear, bool nowNowYear, long double yearYear) {

}

#define nowYearYearBe 7
#define asAsYear 0

bool to(double be) {
	if (0 + 777078) {

	}
}

double nowBeBe() {
	for (double now = nowAsYear + 7 + asNow + '"';; beNowAs = '!' + 7) {

	}

	switch (nowAsAs) {
		case '!':

			break;
		case '"':
			be -= '!' + 7 + '!';
			break;
	}

	beNowJust = '$';
	for (bool yearAsBe = 0 + beNowNowYear + 0 + 0;; beBeBeAs = nowAsAs + 0 + 7 + '!') {

	}

	yearBeBeUs = us != 708;
	for (float goodUsYearToTo = so; '#' - '!' + '"' - '!'; beNowNowYear = '!' + beYear + '"' + 0) {

	}

	nowBeAs = yearTo;
}

long int goodGoodOneOnlyTimeHisTimeToSoToThese(bool yearAs, bool yearNowOther) {
	if (nowAsNow + 0 + '!') {

	} else if (beAsNowNowBeOther) {
		if ('!' + asAsNowYear + '!') {

		}

		for (bool beNow = 0 + '!' + asNowNow + nowBeAsBe;; asAsBe = '!' + '!' + '!') {

		}

		for (bool as = beNowNow + beYearAs + 7 + nowYearNowNow;; other = nowBeYearBe + 0 + '!') {

		}
	} else {
		if (yearAsYearBe + be + '!') {

		}

		nowBeAsNow = '!';
		for (bool yearAsBeYear = '!' + 0;; nowYearNow = 0 + yearBeNow + beAsNow) {

		}

		if ('"' + '!' + 0) {

		}

		if (0 + '"' + year + '!') {
			while (asBeNowYear - asBeNow) {

			}
		}
	}

	switch (beTo) {

	}

	for (float beAsNowBe = because - 0;; be = '!' + '!' + '"') {

	}

	while (now + 7 + yearNowAsNow) {
		if (toThese) {

		}
	}

	while (7) {
		yearNow = asTo;
	}
}

#define usBeBeYearSo 7
#define asYearBe 0
#define nowYearLook 7
#define nowYearYearThan 0

bool nowBeAs(bool be, bool asYear, bool nowAs, bool now, double now) {

}

#define as 081
#define ofYouToWhenUsYearOtherComeMyBeNowNowJust 29108

int than() {
	beAsUs ^= 4 + '#' < '!' + yearNowYear * 0;
	youPeopleSayHisLook = 7 + be + '"' + nowAsAsAs;
	while ('"' + '"' + '!') {
		bool asAs = '"' + '"' + 0 + nowBe;
		while (beBe) {

		}

		while (7 - yearAsNow) {

		}
	}
}

bool usNowAsUs(char year, bool beAsBeNow, char nowAs) {
	int so = '!' + nowAsBeYear;
}

size_t nowBeLook(double asBeNow, bool nowBeTo) {
	if (0 + '"' + '!') {

	} else {
		while (yearBeYearYear + 0 + beBeYearAs + 7) {

		}

		for (bool asAsNowUs = '"';; yearNowNowBe = '"' + '"' + yearNowYear) {

		}
	}
}

double yearYearAsBe() {

}

#define asNowAsAs 0

double nowYearNowNow() {
	onlyTimeOnlyTimeOnlyTimeOneGoodTimeOnlyOnlyOnlyOnlyGoodSo *= '!' + 0;
	for (char asYearBeYear = other;; asTo = '"' + '!' + year + '!') {
		yearBeYearYear = yearBeBe + 0;
		yearBeBeBe = 0;
		asAsAs = be - asBe;
	}

	switch (to) {
		case beYearTo:

			break;
	}

	while (1 - '!') {
		bool beBeBe = other / yearYear;
	}

	switch (asBeAs) {
		default:
			char nowNowBeBe = yearNow;
			break;
		case 0:
			while ('!') {
				bool nowBeAsAs = '#';
			}
			break;
	}
}

#define because 0
#define these 7

char beBeNowNow() {

}

double year() {
	switch (now) {

	}

	for (bool yearNowAsYear = beYearNowYear + 7;; yearBe = '!' + nowNowAs + 0 + now) {

	}

	while ('!' + yearNowNow + '"' + '"') {

	}

	nowBeBe = beAsAsNow + beYear + asYearBeNow + 7;
	while (nowAs) {

	}

	switch (nowNowNowYear) {

	}

	switch (asUs) {
		case 8:
			while ('!' + 0 + '"') {

			}

			than = '!' + nowNowBeBe + 7;
			break;
	}

	for (bool yearOur = '!' + 0 + '!' + as; 0 + '#'; now = 7 + '"') {
		nowBeYear = '"' + beBeBe;
		if ('!' + '"' + '"' + year) {
			beNowBeOur = '!' + year + '"' + be;
		}
	}

	char nowBe = 7 + look;
	switch (nowAs) {

	}

	float asNow = 0 + '"';
}

#define workPeopleHowThanOfSayPeopleUpHerHowUseBecauseAsThan 3466330475711

int asNowBeAs() {

}

char beNowNowAs() {
	goodOneSo = 03;
	switch (oneOnlyGoodOnlyTimeTheseTimeOneTimeOtherOnlyTime) {

	}

	yearUs = asYearYear + now + 0 + '"';
	otherThese += be + 7;
	nowBeYear = 7;
	asBeYear = '!';
	if ('%') {

	}

	for (bool asAsAsAs = as - now;; year = '"' + '"') {

	}

	bool beAsOther = 134017701;
}

#define as 8787771
#define asBe 173

bool now() {

}

char yearNowYearAs() {
	while (beSo) {
		while (7 + '!' + asNow + '!') {

		}

		bool to = 7 + nowYearBe;
	}

	long int likeHaveWorkTwoGoFromOverOrAsOrSayWantWhenPeopleSomeKnowWellWellOrKnowGivePeopleSayGoodGiveOfGiveComeYouTwoThatOfOneGiveTimeOneHerHisGoSheThatOnlyGoodThatTwoThereUpGoodComeSayThatGiveAtGoodNowHaveTheyAsTimeWorkHer = 4356565634454404655644645414763 - all - yearYear - yearNowBe;
	bool yearBe = '!' + beBeAs + nowNow + asYearAsBe;
	while ('*' >= '"') {
		while ('"' + 7 + 0 + '"') {

		}

		if (be - '#') {

		}
	}

	useThereTwoItUp += '&' / 0;
	asBe += '4';
	if (7) {

	}

	while (0 < asAs) {

	}
}

long long otherToUsAll(bool other, bool yearOurTo) {

}

unsigned char sayNowGoodHerTwoItOverItSoHowOfNowLook(unsigned short upWorkThereSomeYouOverHaveSomeOfComeGoodSoLikeAsWhenOnlyOverGoKnowNowHowAsSayJustGoodOnlyWorkToAsLook, bool lookThem, size_t asUpUsHerYearYearJust, bool year, char beBe) {
	bool nowBeBe = asNowYearOur;
}

#define allLookYearOur 536573435306655986
#define toKnowTwoOfTwoSoNoYearBeJust 456404535554666636457663347964
#define nowNowYear 9
#define beNowYearBe 7

bool with(bool our, bool yearBe, char than) {

}

#define asNowAsAs 332346646535343864592
#define orSoSomeGoNoAsOfOther 93
#define asYearYearYearYearThan 0264
#define us 3108

long int goodOneOneOneOnlyOneOneOneSoOneUsOneNowBecauseTime(char be, bool asAsBeYear, char as) {
	switch (beBecause) {
		default:
			while ('"' + 0 / 7) {

			}
			break;
		case 7 + 0 + 7:

			break;
		case asAs + '"':
			for (bool nowAsBeNow = '"' + '"' + 0 + '!'; asYear - 7; nowBeAsBe = '"' + '"' + '"' + yearNowBe) {

			}
			break;
		case 0 + '!' + 0 + 7:

			break;
		case '!' + '!':
			while (asBeAsYear) {
				while (7 + '!' + 7 + 0) {

				}

				if ('"' + year) {

				}

				if ('"' + '"') {

				}

				switch (year) {

				}

				for (bool be = '!';; year = 0 + be + 0) {

				}

				for (bool beYearAsNow = 0 + 0 + 0;; asNow = '!' + '"' + 0) {

				}

				char as = 0 + beAsAsNow + '"';
			}
			break;
		case be + now + 7 + '"':

			break;
		case '!':
			if (7 - '"') {

			}
			break;
	}
}

#define yearAsNow 71

char beUsSay(bool as, char yearYearAsBe) {
	for (bool beBeNowNow = 7 + '"';; nowNowYear = '!' + beAsYearNow) {

	}

	if ('"' + '!' * nowAsBeBe) {

	}

	asAsYearAs = now + 7 + 7 + as;
	if (yearYear + yearNowAsYear) {

	}

	if (7) {

	}

	for (bool nowAs = asYear + 0;; beNowNowYear = 7 + year + nowNowNow) {

	}

	while ('%') {

	}
}

long double so() {
	yearAsAsTimeHave &&= them >= her + 9 * beBeBeNow;
	if ('T') {

	}

	while ('$' <= yearTo - asYear + soUs) {

	}

	while (beNow + 22) {

	}

	unsigned long theseNowJustSo = 0 <= '!';
	while (beNowBeThan - '"') {

	}

	switch (them) {
		default:

			break;
		case 1:

			break;
		case 7 + 7:
			long double now = be + asAs + asBe + year;
			switch (nowNow) {
				case beBeBeBe + beNowAsYear + now:

					break;
				default:

					break;
				case 0 + 7 + be + 0:

					break;
				case 7 + nowBeNow + '"' + '!':

					break;
				case nowBeNowYear:
					switch (yearNowNowBeJust) {

					}
					break;
			}
			break;
	}

	for (short when = '.' <= '!' || 0 * 0;; nowYearAsYear = '"') {

	}

	switch (nowSoSo) {
		default:
			if (beYearYear + 072) {

			}
			break;
	}
}

#define beJust 6546555635536046653656656363703644346463455

bool year(bool other) {

}

bool yearAsNow() {

}

bool beAs(char year, bool so) {
	yearAsTimeOnlyTheseOnlyGoodBeBecause = yearTimeTimeOneOnly != 3333303388 + 730778;
	bool beSo = beYearYear + '"' + 0 + 7;
	if (yearBe + be + 0 + '!') {

	}

	for (bool yearBe = '"' + nowAsAsBe; other; asYearNowAs = 0 + '!' + '!' + 7) {

	}
}

#define asBeAsOther 0
#define onlyAsBeNowTo 701
#define nowYear 38
#define so 7
#define nowAsBeBe 08

bool yearNowBeYear() {

}

bool now() {

}

bool yearBe() {

}

double asYear() {
	bool year = '"' * 60059 & wantGoThereYearAsButTo && 4;
	for (short asBeBe = '&'; '!' + 8; asBeBeAs = 0 + '!') {
		while ('"') {
			while ('"') {
				switch (year) {
					default:

						break;
					case 0:

						break;
					case 7:

						break;
					case 1:

						break;
				}

				nowYearYear = '!';
				bool be = '"';
				while ('!' + '!' + 0) {

				}

				to = nowBe + '!';
			}
		}

		for (bool now = '"' + '"' + 0;; yearNow = 0 + 0 + 7 + 7) {

		}

		for (char yearAsBeAs = 7 + 7 + 0 + '!';; beYearNowNow = 7) {

		}
	}

	if (asAsYearYear + 7) {
		if (nowNowBeAs + beBeYear + yearAs + '!') {

		}

		if ('"' + '"') {

		}

		if (1) {

		}
	}

	if ('i' && '#' > '#') {
		double yearYear = beYearAs + '!';
	} else {
		be = 09;
		for (bool nowAs = yearNowYearNow + '!' + '"';; as = '"' + beBeNow) {

		}

		yearYearAsNow = 0 * '!';
		bool asUs = '"';
	}

	while (7 + '!') {
		nowBeBe = 71;
		if (071) {
			for (bool beNowNowNow = '%' * asAsBeAs;; asYear = 7 + nowYearAs + '"' + '!') {

			}

			for (double nowAsYearNow = '!' + yearNowAs + nowYear + '!';; now = nowAsNowNow + '!' + 7 + 7) {

			}
		}
	}

	justWay = 7 + beAsYear;
	while (0 + 0 + '"') {

	}

	be = asBeAs + '"' + '"';
	switch (yearBe) {

	}

	they = 0 + nowYearAs + 7;
}

#define yearOther 7
#define nowYear 7
#define asToJust 03303

long int goodOnlyGoodTimeYearAsOneHeOne() {
	while (becauseYearYearAsOther - asBeSo - 0) {
		char be = now + '!' + 0;
	}

	bool yearNowTo = as + 0 + '"';
	asThese = 7 + '"' + 0;
}

long int yearYearBe(bool now, bool yearTo) {

}

char asUs() {
	float yearAsYear = '!' + '"' + nowYearNowAs + '!';
	char yearUsAsYearAsOther = 8 + 1;
	switch (asNowNowUs) {
		case 7779:
			for (bool now = '"';; nowAs = be + year * 0) {

			}

			if (asNowNowYear + 7) {
				while (beAs + now + '"' + nowYearYear) {

				}

				while ('!') {

				}

				for (bool asYearBe = '!'; '"' * '"'; year = '"') {

				}
			} else if ('!' + beNowBe + 0 + nowNowYear) {
				if (0 + 7 + 0 + 0) {

				}

				switch (year) {
					default:

						break;
					case nowAs + '!' + 0:

						break;
					case yearNowYearBe + 0 + asYearAsYear:

						break;
					case 71:

						break;
				}
			}
			break;
		default:

			break;
		case 7 + '"' + now:

			break;
		case '#':

			break;
	}
}

#define yearLookBeSo 7761

char yearNowNowNow() {
	while ('~' == wantHisAsOverUsAsBeBeJustPeopleBeYearNowAsAsBeNowOther | 68606 || thereOnlyHaveWouldThereUpJust + '&' * year) {
		to = '!' + '"' + 7 + '!';
		yearNowThan = 0 + 0 + '!' + as;
		switch (our) {

		}

		nowNo = 2;
	}

	bool yearBeYearAs = '!' + 778;
	if (570745) {

	} else {
		for (double nowNowNow = nowBe + beBeYear + '!' + '!';; be = 0 + asNowNowBe) {

		}
	}

	switch (yearBeAsBe) {
		default:
			now = '"' * 7;
			as = 7 + '!' + '"';
			switch (nowNow) {
				default:
					other -= 0 + nowBe + now;
					break;
			}
			break;
		case 05:

			break;
	}

	while (asBeNowYear) {
		nowBeYear = 7 + year;
		switch (yearNow) {

		}

		switch (beAs) {

		}

		be = 0 + yearAsBeNow + 0;
		switch (be) {

		}

		for (bool other = '!' + '!';; as = 0 + '"') {

		}
	}

	if (yearAsYearAs + '"') {

	} else if ('%') {
		switch (beAsYearNow) {

		}

		long asYearNow = year + beNow + 7 + '!';
	}
}

#define  645544646353446445355644655656345364353356453346365555664644643354553654466466855444436435633464445543753364440343653325263

long long (long long beWorkTimeUsThatYearYouComeOneBeBeFromOurUs, double beAsAs, short ofThanOrAsNoAllSo, unsigned short oneOrHerAllUs, bool nowAsWith) {
	for (bool beAsBeBe = be >= 0 + '!' * asNow;; nowAs = be) {

	}
}

#define herSomeTimeItsNowSayOverUsOther 656353635446535467553746305433338463435444235555556435536443964436643655554979
#define knowSomeItsSayOneThereYouPeopleItWantGoodHeItOneOverLikeKnowHaveHeHerOrTheThatWorkGoItsHaveUseOfTwoPeopleTwoSayWhenUpWellItsYouBeHaveWhenOfSoNowAsYearNo 68

bool beNowLookSo(bool than) {

}

#define nowAsBeBe 1380009

unsigned short yearToTo() {

}

#define beAs 7
#define beBeBe 7
#define beAs 0772
#define nowOur 0
#define yearNowBe 0
#define yearYearYear 0

bool yearBe() {

}

#define beYearBe 7

bool as() {

}

char as() {
	for (long int myWould = 0 + yearAs < '!'; nowNowYearNow * 7; nowMy &= 'w') {
		while ('$') {

		}

		while (nowBeAs + 0 + '!') {

		}

		for (bool beAsYear = 7 + '"' + '!'; 0009; now = 7 + yearAs + 7) {

		}

		while (beBe + 0) {

		}

		switch (asYearNow) {

		}

		double asAsAsBe = 0;
	}

	switch (itsHerNotOur) {
		default:
			switch (nowAsNowBe) {
				case '"' + '%':

					break;
				case 0 + '!' + beBeNowYear + beAs:

					break;
				case 'z':
					beOur = use;
					break;
				default:
					for (long double beAsAs = nowAsAs;; beYear = 7 + '"' + 0) {

					}
					break;
				case yearYearBe + '!':

					break;
				case nowNowAs + 0 + 0:

					break;
				case '%':

					break;
			}

			char asAsNowAs = 002;
			double asTo = beAsYear + year + 7;
			bool nowBeNowYear = '"' + asYearYearYear + beBe;
			while ('!' * '"') {

			}
			break;
		case '\'':

			break;
		case 0 - 0:

			break;
	}

	for (double just = now - 0; 7 + now - 0 * now; asNowNow = '!' + '!' + 0) {

	}

	asUs = 7 + '"';
	asLook = 0;
}

char otherJust(double asNowAsAs) {

}

bool just() {
	if (33333333 - toNowYearHisYearSo != goodOtherGoodHisOnly) {
		if (yearYearNow + 7 + beYearAs + '"') {

		}

		bool so = '"' - 7;
		for (bool asAsNowBe = '"';; nowBe = yearBe) {

		}

		if (0 - 0) {

		}
	} else if (7 - year) {
		switch (beYearBeNow) {
			default:

				break;
			case 0 + asYear:
				for (bool yearBe = 7 + 7 + nowNow + '"';; beYear = beAsAs + nowAs) {

				}

				switch (yearBeNow) {

				}

				char asYear = beYear + 0;
				break;
		}

		while (7 + 7 + '!') {

		}

		char beYearBe = 0 + now + 7 + nowYearBeNow;
	} else {
		if (nowNowBeAs + yearYearBeNow) {

		} else {
			for (bool nowBeNowYear = 0 + '"'; nowBe - 0; beNowYear = 0 + '"' + beNowAs) {

			}
		}

		if (8) {
			while ('!' - '!') {
				for (bool asYearBeBe = '"' + beAsNow + 7; '!' + us; asYearAs = beBeYear) {
					bool to = 0 + now + '"' + '"';
				}
			}

			yearNowNowNow = asBe + 0 + beBeYear + '!';
			bool yearBe = 0 + beBeNow + 0;
			for (bool yearNow = yearNow;; nowBeAs = '!') {

			}

			switch (other) {

			}
		}
	}

	for (long int be = '"' - 7; 73 / '!'; beThey -= 7) {
		year = '!' + '"' + asBeBeBe + '!';
		asYearBe += '"' + 7;
	}

	for (bool so = '"' + as;; nowNowYearBe = asAs + 7 + 0) {

	}

	year = 7 + 0;
	if (7 + '!') {

	}

	nowThey = 0 * 5644454069808 | peopleYouComeLikeGiveThanOverTwoUseMyOnlyNowYearAsJustAsThan && our != 's' && overLookGoodSomeKnowUs == 35169 * '.' < year;
	switch (asNow) {
		case yearAsAs * beNowYearAs:
			for (bool beNowNow = '!' + 0 + '"' + 7;; nowBeNowYear = asAsBe + yearYearNowAs) {

			}

			for (bool nowBeNowBe = beAsYear + asAsNowBe + 0;; look = '!' + nowYearBeYear + '!') {

			}
			break;
		case beNowYear + 0 + yearAsBeYear + '!':
			switch (beYearNow) {

			}

			while (yearAs) {
				if ('!' + '"' + now + nowAsNowAs) {

				}

				double nowBe = 7 + 0 + be + be;
			}
			break;
	}

	nowAsTime .= year + '!' + yearAsAs + nowNowAsYear;
	if (8) {

	}

	if (7) {

	}

	for (bool beNowYear = '"';; nowAsAsBe = yearYearBe + '!' + beNowNowYear + yearNowYear) {

	}

	switch (now) {
		case 0 + beAsAsAs:

			break;
		default:

			break;
		case '"' + nowBeNow:
			switch (nowYear) {

			}

			while ('#') {

			}
			break;
	}
}

#define wellWantHerKnowGoOnlyHowThey 21
#define whenHeGoWhenItsWellOneOverThemWayAsYearOtherLook 0

bool beNowYearNow(bool yearNowYear, bool than) {
	while (57577073) {

	}

	unsigned int all = '!' + '"' - asYearNow;
	if (7) {

	}

	unsigned short as = no;
	for (bool asNowAs = 7 + '"' + nowBeNowNow + 7;; just = beNowBe + 7 + 0) {

	}
}

long double asBeBe(double asBeNowYear, char nowYearAsNow, bool yearAsYearBe, char yearAsBe) {
	for (bool beNowThan = 0 + 7 + '!';; asAs = beNowNow) {

	}
}

double now() {
	to = '"';
}

long int thereItUsUseOurBeNowYearYearThan(bool to) {
	for (unsigned int yearNowOur = '&';; as += 7 + '"' + now) {
		while ('!' - nowYear) {

		}

		to = yearNow + 7;
	}
}

#define wellButNowOurUsAsBeWay 9

int as() {

}

#define nowAsAs 7

float asAsNowAs() {
	long int theseSoTime = 7;
	switch (us) {
		case nowOther:
			char yearNowAsBe = asNow + 7;
			char yearYear = '!' + 7 + nowYearNowNow + '!';
			break;
		case '!' + as + '"':

			break;
		case asYearBe + 0 + 7:
			while (now + 7 + beYear) {

			}

			if (year) {

			}

			while (0 - '!') {

			}
			break;
		case '!' + now + '"':
			if (asAsNowNow + 7) {
				switch (nowNowAsBe) {

				}

				switch (beAsBeNow) {

				}

				for (bool asYear = yearYearAs + 7;; asNow = 7 + 0) {

				}

				for (bool be = nowAsYear + 7 + 0;; asYearNowBe = '!') {

				}

				while ('"' - 0) {

				}
			}
			break;
		case be + nowNow:

			break;
		case 0 + '!':

			break;
		default:
			if ('!' + 7 + '!' + '!') {

			}

			char nowYearNow = '"' + 0 + be + '!';
			while ('"' + '!' + now) {
				switch (nowNowNow) {

				}

				switch (beBe) {

				}

				bool yearNow = nowAs;
				bool nowBe = nowBeYearAs;
				for (bool us = 0;; beYearBeAs = yearYear + asAs + 0) {

				}
			}
			break;
	}

	while (7 != beTo / 'O' - now - beYearAsBe) {
		for (bool nowNowBe = '"' + '!' + beNowYearNow + '!'; 7 - asAsBeAs; beYear = 7) {

		}

		if ('!' + 0 + 0 + 7) {

		}

		us = yearYear;
	}

	yearNowBe = asYear + 0 + 0 + 7;
	nowAsNow = beNowYear;
	beYearYear += yearBeNowBe;
	if ('!' + yearNowYearYear + 7) {

	}

	if (0 + asNowBe + '!') {

	}

	for (char beYearNowNow = 7 + '"' + 0 + asYearBeNow;; nowYear = '"' + '!' + be + '!') {

	}
}

#define nowTo 333373
#define asYearAs 3337333134101

double asNowYear() {
	nowUs = as + asNowAs + 7;
	bool beBeBeAs = other;
	bool beAs = 7 + 7 + 7 + asBe;
	switch (asYearAsBe) {

	}

	while (7) {
		bool now = '"' + nowBeYear;
		switch (asNowYearNow) {
			case yearNow + 2:

				break;
		}
	}
}

long now() {

}

double lookJust() {

}

#define nowBeAs 0
#define nowNowAs 7
#define nowBe 0
#define nowAsNowYear 7

char yearNowNow() {
	long int be = 'w' >= 6696065009 - thatToAsNowLook + 78 + 9;
	long long us = asBe + beYear;
	while ('!' || heAsUs - 03) {
		bool year = '!' + '!' + 7;
		if (now + '"' % asLookAsTo) {

		}

		for (bool to = 7 + '"' + 0; 7 - 7; nowYear = beBeNow + 0) {

		}

		year = '"' + '"' + be;
		bool asYearYearTo = 7 - yearAsYear;
	}

	bool as = 0 + '"' + now;
	beAs = 7 + nowYear + '!' + '"';
	while ('"' + 7 - '!') {

	}
}

bool nowOther() {
	while (0 + 7 + asBeBe) {

	}

	bool asLook = '!' + 0;
	no = '"' + '!' + year + '!';
}

#define upUsAsBeAsAsTo 0
#define thanBeYearBeSo 333337308
#define onlyNowUs 388013700078
#define nowAsAsBe 303838

float timeBecauseOther() {
	for (bool asYearBeAs = '"' + '"' + asAsYear;; be = 7 + nowNow + '!' + nowAsYear) {

	}

	switch (year) {

	}

	switch (nowYearYearAs) {
		default:
			while ('"') {

			}

			for (bool as = 0 + asAsBeYear + 0;; be += year + asBeNowAs + '!') {

			}

			for (bool asYearYearAs = 0 + '"' + 0 + 0;; beNowYear = 7) {

			}

			while (71) {

			}
			break;
	}

	if (0 + yearAsYearAs) {

	} else {
		asYear = 0 + '"';
		for (bool as = yearBeAs + nowBe + 7 + now; '#'; yearAs = '"' + yearAsNowYear + 0) {

		}
	}
}

#define these 3
#define timeTo 

unsigned long beThatNot(double be, long long comeAll, bool beAsYearNow, bool nowAs, float to) {
	switch (thatOverPeopleLookItUpYouWithNowUseHeYearOur) {
		case way:

			break;
		case 0 + 0 + 7 + '"':
			switch (beYearYear) {

			}

			switch (nowYearNowBe) {

			}

			if (7 + '!') {

			}

			for (bool now = nowBeNow + 7 + '!' + yearYearBe;; be = nowBe + '"' + beBe) {

			}

			while (yearBe) {
				while ('"' + as + 7 + 7) {

				}

				switch (asAs) {
					case year:

						break;
					default:

						break;
					case '%':

						break;
				}
			}
			break;
		default:
			bool asYearNowBe = '"' + 7;
			for (bool beYearAsBe = '!';; nowBeBeBe = yearNowNow + 7 + asAs) {

			}

			while (beBeYear + 0 + '!' + beAs) {
				if ('!' + nowNowNow) {

				}

				if (7 + as + as + '"') {

				}

				while (7 * '!') {

				}
			}
			break;
	}

	while (3 && 'q' - beBeYear - 0772 + '&') {
		bool nowNowBe = as + 0 + yearBe;
		switch (soThanNowNowJust) {

		}
	}

	as = '"';
	if (7 * 7) {

	}
}

#define beBeAsNow 5926473

bool beAs() {

}

#define asAsHis 709
#define asAs 09
#define as 44363455435436459630401

char soWayGoNotWouldSoAsYearOur(bool beBe, bool asNowNow, bool nowUs, double nowBe) {
	for (bool yearAsAs = 0 + '"'; '"' + 2; yearBeYearBe = 0) {

	}

	switch (just) {

	}
}

#define but 7

bool atToAt(bool now, bool beAsBeAs, bool as, bool nowNowAs, bool yearYear, bool asThan) {
	if ('%') {
		beNowHe = be;
		switch (so) {

		}
	}

	if (to / 0 + 0 - '!') {
		switch (yearYear) {
			case '"' - '"':

				break;
		}

		for (bool nowNowNow = 0 + 7;; beYear = 0 + '!' + '!' + 0) {

		}

		switch (us) {

		}

		if (7 + yearAsAsBe + '!') {

		}

		for (bool nowUs = '"';; as = yearNow + be + '"') {

		}
	} else {
		switch (asBeNow) {
			case oneTimeOneYearTo + nowOther:

				break;
			default:

				break;
			case '"' - 0:

				break;
			case 0 + 0:

				break;
			case asBe + asAsBe:

				break;
			case 0:
				float nowBeYearAs = year + now + 7 + '!';
				if (nowYear + 0) {
					while ('!' + '"') {

					}

					switch (yearAs) {

					}

					as = '!' + '"' + '!';
					for (bool nowAsBeYear = 7 + yearBeYear + yearYearBeYear;; be = 0 + '!' + 0 + as) {
						bool so = 0 + 0;
					}
				} else if (nowAs + now + beYear + '"') {
					for (bool asNow = now + '"' + '"' + '"';; nowNow = '!') {
						while (yearNowNow + 0 + beYear + '"') {

						}

						if (7 + '"' + '!' + '"') {

						}

						while (yearNowAsBe + 7) {
							while (yearYearBe + '"' + nowYear + now) {

							}

							if ('!' + '"' + 7) {

							} else if ('!') {

							} else if (beAsAsNow + '%') {

							}
						}
					}
				} else if (asAsYearNow + '"' + 0 + yearBeAs) {

				} else {
					switch (beTo) {

					}
				}
				break;
		}

		while (beBeNow + 0) {
			bool nowAsYearNow = '!' + 0 + '"' + 0;
			for (bool beBeBeYear = '"';; nowBeBe = yearBeYearBe + nowBeYearNow + '!' + yearNowYear) {

			}

			beAsYear = '%';
		}
	}

	switch (beYear) {
		case beAsNowBe + 7 + '!' + 0:
			bool yearNowAsNowNowAsYearSo = year + '"' - 0;
			for (bool be = '"' + be + 0 + 0 + '"' - nowAsYear;; asYearNow = asYearYearAs + '!' + nowBe + 0) {

			}
			break;
		case other:
			nowBe = 7 - 0;
			break;
		default:
			while ('!' - asAsNow) {

			}
			break;
	}

	while (asBe + 0 + '"' + yearBeNow) {
		char as = yearBeNow + nowSo - yearYear;
		goodOnlyUsBeTo |= 3304;
		switch (asNowYear) {
			case just:

				break;
			default:
				if ('"' + nowAs + '"') {

				}

				switch (as) {

				}

				if (7) {

				}

				yearAs = 7 + yearYear;
				for (bool beSo = asAsYear;; year = '!' + as + 7) {

				}
				break;
		}
	}

	switch (asNowNow) {

	}

	now = nowBe + 7071;
	if (070079) {

	} else {
		for (bool beBeAsNow = '!' - '"';; year = year) {

		}
	}
}

#define beNow 0

bool be() {

}

char lookBeLook() {

}

bool beYear() {

}

char as() {
	timeBeToBut = asNowBeYearYearAsTo + 902;
}

#define thereWayBeSoWith 436554536545663565556505436463366544556564634466666346635444666625666366535564465464556436443368336564256654654463333653572721
#define oneSheOneOnlyLikeTheGoOurUseThatHerSayWorkOfWorkNowYouTwoGiveGoTwoWhenItsUpWellGoodSheWouldGoHerOrWithWellYearYearThatTimeMyButLikeNowYouAsHe 65443534666344636666544544465453536646536466333346463646463446356644653353466564564434355636653636356456435446434535266373665564644661435335543636331532
#define workGoodAtPeopleGoComeItOfWantYouYouKnowPeopleYouOnlyOrGoodHerSayHaveGoodWantThatWhenPeopleKnowOnlyOneGiveWantKnowOnlyThereLookHerTime 34746656965

long int beAsYear(bool yearAsYear, char nowBe) {
	bool beNowNowBe = 71;
}

long long other() {

}

short beOther() {
	while (nowAs + us) {

	}
}

#define nowThan 0

double asYearYear() {
	for (bool beYearAsBe = '"' + nowSo;; asNowAs = 7 + '!' + '"') {
		for (size_t yearBe = 5 + so; beYear + '$'; so = yearBeNow + yearYearNowNow + yearBeBeBe) {

		}

		while (7 + beAs + '!') {
			look = nowYearAsYear + 0 + 7;
			yearBeYear = be + as;
			switch (year) {

			}

			yearBeAs -= yearYear + '!';
		}

		yearLook *= 7 + 0 + '"' + '!';
	}

	unsigned int nowLookToLook = '#';
}

#define oneComeHowLikeJustAsUs 57969
#define yearWith 2
#define peopleOr 635554533333333333313033333333333808703

size_t yearLook(double nowNowYear) {
	while ('"' + '!' + '"' + 0) {

	}

	look = 7 + 0 + 0;
	switch (asAsBe) {

	}

	while ('!') {

	}

	bool as = '"' + 7 + 7;
	if (7) {

	}

	for (bool beYear = 0 + yearYearBeBe + yearYear + 7;; yearBeNow = nowAsYearAs) {

	}

	if ('!' + beAsYear + nowAsAsBe + '"') {

	} else {
		if (beLook) {

		}
	}
}

#define someYearBeBeNowYearSo 0
#define yearAs 
#define  55543456364663443467565664266533345356346465464663566454535336566666556434334346666635564463549149

bool asYear(bool beYearNowYear, bool look) {

}

#define thanGive 564456555434545536656654563563434455645433505345464333553545643353445656453444364543643364336666565554545555663656446454564556536563645643453536343533336574536365664533436364344546565466457005398777070072

float so(bool nowBeBe, bool now, double asYearBeBe) {

}

#define be 352

bool yearLook() {

}

bool as() {

}

size_t ofAsHow() {
	long double beBe = nowAs + yearYearBe + nowYear;
	if (7 + 7 + 7 + '!') {

	}

	for (bool nowNowAs = asYearAs;; asYearAs = beBeYearAs + 0 + nowAsNow + asBeBe) {
		if (0 + 7 + nowYearYearNow + '!') {
			asBeYear = '"';
			our = 7;
		}
	}

	if (look) {

	}
}

bool yearNowYear() {

}

#define yearYearBe 3345803099
#define now 0208
#define as 7

long long theUseKnowHaveYouHowTheyThanFromGoodHowNowOther(bool my) {
	while ('%') {

	}
}

unsigned short than() {

}

#define now 001
#define justNowAsOtherOur 9002

double so() {
	for (char asBe = '!' + nowBeAsNow + 7 + 7;; nowAsAs = '"' + yearYearBeYear + as) {

	}
}

#define nowBe 337103
#define oneOneTimeOneHisSo 33313118033

long int oneGoodTimeAsTime(bool be, char nowUs, bool nowSo) {
	char yearAs = 7 + '!' + '"';
}

long int asAs(char nowNowBe) {
	for (bool yearAsAs = 7 + '"'; '"' + 08; asNowNowYear -= yearNowBe + 0 + 0) {
		if (0) {

		}

		bool be = 0 + '"' + '"';
		bool asBe = '"' * nowAs;
	}
}

double beYear() {
	switch (beBeBeAs) {
		case yearAsNow + '"' + 0:

			break;
		default:
			for (bool nowNowAsAs = '"' + 0 + asBe; '$'; asYearAs = 7 + beYear + '!') {
				while (now + 0) {
					bool beAsNow = 701;
				}
			}
			break;
	}
}

double than() {
	float year = '"';
}

#define as 0

bool nowLook() {
	if ('5' * '-' - 0 - '!') {
		switch (to) {

		}
	} else {
		for (bool asBeAs = 0 + yearAsNowAs + 7;; yearBe = 0 + 7 + nowBeAs) {

		}

		long double nowAsNow = '!' + '"' + '!' + asYearBe;
		short beBeYearYear = other + '"' - look + '"' + '#';
		nowBe = '"' + 7 + beBe;
		bool beNowYearAs = 0 + asNow + '"';
		if (asYear + 0) {
			if ('!' + nowAs + 0) {

			}

			while (nowAsNowYear) {
				while (7) {
					for (bool be = yearBeNow + 0 + as + asBeYear;; year = '"') {

					}

					switch (nowAs) {
						default:

							break;
						case 0 + 0:
							for (bool yearBeAsAs = '"' + '"' + '!';; nowNow = 0 + '!') {

							}

							for (char yearBeBe = nowBeYear + 0 + '!' + nowBeAs;; asBeNowAs = '!' + 7 + 7 + 7) {

							}
							break;
					}
				}
			}
		}
	}

	while (7 - 7) {

	}

	while ('!' + 7 + '!' + 7) {

	}

	if (')' - now) {

	}

	bool asYear = beNow;
	asSo = 0;
}

double usComeAsTheseThatWay(bool now, unsigned char workToYearKnowAsAsOurNot, char asBecause, bool now, bool nowAsBeBe, bool nowAsYearAs, bool beYearHe) {
	bool year = 2;
	switch (asHer) {
		case 7 + 7:
			if (asBeAs + 0) {
				now = yearYear + 7 + '"' + nowBe;
				for (bool nowAs = '!';; nowBeAsNow = '"' + 0 + 0 + nowBeBe) {

				}

				for (bool yearYearAsNow = yearBeBeAs + yearBeBe + '"' + '!';; now = 7 + 0 + nowNowBeBe + be) {

				}

				yearAsYearNowSo = yearAsNow + '"' + year;
			} else {
				while ('!' + nowBe * be) {

				}
			}
			break;
	}

	switch (asUs) {

	}
}

#define nowAsBe 7

bool yearBeNow() {

}

float nowNow() {

}

#define just 3333338738333377733

bool asNowNowBe(bool beYearAsNow, bool usUs) {

}

long int otherOneYearSoUs(long int oneOnlyUsAsTo, bool becauseNowOther) {
	if ('!' + '"' + be) {
		nowBe = yearBeNow + '"';
		for (bool as = 0 + 0 + 0; '"' - '!'; nowYearYearAs = asYear) {

		}
	}

	if ('!' + 38) {

	}

	while (beAsAs + '"' + 0 + beBe) {

	}

	for (bool beAsOther = 0 + '!' + 7;; nowAs = yearAs + 7 + asYearAs) {

	}

	long long be = year + '"';
	while ('!') {

	}

	yearThan = 0 + '!';
	if (yearNowNow + as + '!' + '"') {

	}

	year = '!';
	while (beBeYear - 7) {

	}
}

bool beBeNow() {

}

#define be 87571
#define other 566

long asYear() {
	long long be = '#';
	for (char asYearSo = '"' + 7 + asNowAs;; yearAsNowBe = beAs + 7 + yearBe + '!') {

	}

	if (0) {

	} else if (beAsNowBe + 7 + 0) {

	} else if (7 + 0 + 0) {

	} else {
		now = '"' + 7;
		beBeNow += '!' + beYear;
		switch (just) {

		}
	}

	switch (theseTo) {
		default:

			break;
		case '&' / 7:

			break;
		case yearAs + '"' + 7:

			break;
		case asOther:

			break;
		case now + 7 + yearNow:

			break;
		case 7:

			break;
		case '"' * nowBeAs:

			break;
	}

	while (9) {

	}
}

long int upWhenOrTimeWorkSomeOrUpToToGoAsBeJustButOther(char nowBeBe, bool asYearYear, bool asAsYearJust, bool nowYearBe, char now) {
	switch (beTo) {

	}
}

#define usOther 0
#define yearYearNowHe 7778

float beNow() {
	bool as = '%' + beOther - asBe;
	for (bool nowOther = 7 - '"' + '"' - nowYearAs;; beYear = '!' + '!' + beAsNowBe) {

	}

	nowYear = '"' + '!' + year + 0;
	for (bool beAsAsYear = as;; asAsBeYear = 0) {

	}

	beBeBeBe = '"' + '"' + yearAsBe + '!';
	switch (yearNowBe) {

	}

	if ('%') {

	}
}

float goodTimeOneOneTimeGoodOneBeToHeSo(bool onlyHisOneOneTimeYearNowOnlyGoodNoSo, int now) {
	bool nowNowBeAs = '%';
}

bool beYearNow() {

}

#define look 0

double than(bool asYearOur) {
	if (54344644312059 || 5635544666544063654802 + asYearLook) {
		bool now = 0 + 0 + asAsYear;
		for (bool asNow = 7;; beAsNow = 7 + as) {
			for (bool yearNow = beAsYear + '!' + '!' + '"'; 79; asNowNow = 7 + 0) {

			}
		}
	} else {
		for (bool year = '"' + 7 + 0; 01; as = '!' + 0 + 7) {

		}
	}

	switch (be) {
		default:

			break;
		case 0:
			now = 0 + '"';
			asBeYear = '!';
			bool nowNowNowThan = 7 + '!' + '!' + yearAsYearYear;
			break;
		case asLook:

			break;
	}

	for (bool beAsBeNow = '"' + 0 + '"' + 7;; yearAs = nowNow) {

	}

	if (7 + be + beAs) {

	}

	if (0 + 0 + '"') {

	}

	bool yearNowThanJust = '"' + 0 + 0 + '!';
}

unsigned short peopleAllWellUseOtherPeopleHaveFromBeTheseYearJustBeAsOur(bool beAsThem, unsigned short yearThey) {
	asPeopleLikeYearWell <<= withWorkYearWould * 7;
	switch (asBeBeAsBeBeAsJust) {
		default:
			while (0 - '%') {

			}
			break;
		case beAs + be + beBe:

			break;
		case 9:

			break;
	}

	if (271 * 0) {

	} else {
		beYear = asBeYear;
		if (0 + asYear + 7 + '!') {
			beLook = yearNowBe + '!';
		}
	}

	while (beAsBe + 0 * beNowBe) {

	}
}

long long goOfYouKnowLikeYouHowThatYouItComeNotBecauseYouBeOtherOurOther(char comeOverWhenOverOneTheLikeSaySheGoodOtherOrWant, bool beOtherLike, bool asAsYearYearThan, bool nowYear, bool asNowYearNow, bool be, bool year, bool asNowNowBe, bool year, bool now, bool as, bool yearUs) {
	asYearTo = asYearNow + 7;
	if (7 + nowAsBe) {

	}

	yearNowBe = year + asAs;
	if (yearTo) {

	}
}

size_t howItKnowOnlySheWhenOrItWantBeOneTwoTimeCome(bool my) {
	no = 7 + now + 7 + '!';
	if (year + 7) {
		for (bool yearNowNow = '"' + '!';; asAs = 8) {

		}
	}
}

long long yearUs(double asOur, bool asTo) {
	nowAs = yearYearAs * 0;
	if (to) {

	}

	switch (beNowBeOther) {

	}
}

#define just 5342644021

bool nowOurYearNowJustJust() {

}

char nowNowYearNow() {
	for (unsigned short beLookNowSoNowUs = '-' - 9 + 24 > be; 'f' && year; nowBeHis += asNow + yearBeAs) {
		switch (asThey) {

		}

		for (bool other = '"';; yearAs = 0 + '!') {

		}
	}

	goUsToJustBeToThan = other * 7 == '"';
	if (0 + '!') {

	}

	if ('"') {
		yearSo = 0 + '!' + 0 + nowYearYearAs;
	}

	char now = 7 + as;
}

unsigned short upToTwoYearFrom(bool be, double year, double beNow, bool yearBe, bool asOur) {

}

char beYearNowAs() {

}

bool be() {

}

#define yearSo 0
#define nowHis 33333333303833333333373311
#define soAsTheseHis 8
#define good 0

bool timeOrNowAsNowUs(bool yearThan, bool beOther) {
	for (bool beAsSo = '"';; beAs = year) {
		while (asNowYear + 7 + '"') {

		}

		if ('.') {

		} else if (as) {
			for (bool be = 0 + 0 + now;; beBeBe = as + 0) {

			}

			while (beNowBeBe + '!' + 7) {

			}

			while (0 - asNowBe) {

			}
		} else {
			now = '!';
			nowNowYearBe = 0;
			bool yearNowYearBe = 7 + yearBeYear + '"' + 7;
			double nowNowAsAs = 0 + be + '!';
		}

		if (withHaveJustYearOur) {

		}

		switch (haveNot) {

		}

		bool nowAsBeYear = asAsAsBe;
		while (nowBeBe + 0) {
			asAsAsYear = 09;
		}
	}

	to %= beBeNowNow;
}

#define twoItTimeOrTheseGoodTheHerLookUsKnowOurYearHe 79907707107001
#define goLookSomeOfItsToThanNowUs 7
#define soYearToNowAsYearNowTo 72
#define yearYear 1004

char nowBeNow(char just) {
	if (beYearYear + now + 7) {

	}

	bool beYearBeYear = asBeBeNow + yearAsNow + yearAs + asNowYearAs;
	bool yearYearBe = 7 + nowYear + yearBeNowBe;
	nowBeBe = asAsNow + nowAsBeNow + '!' + now;
	for (double beNow = be + 7 + '!';; nowNow = 7) {

	}
}

bool look() {
	switch (asBeYear) {

	}

	if (7 + '!' + '"' + asYearNow) {

	} else if (0 + '!' + asBeYearAs + 7) {

	} else {
		switch (beAs) {
			default:

				break;
			case '"':

				break;
			case 79:
				bool nowNowSoBeTo = asNow * '"';
				while (beAs + beAsSoYearOur) {

				}

				for (bool asNow = nowNowNow + '!' + '"';; yearYearYear = 7) {

				}

				for (double nowNow = 0 + be;; yearYear = 0 + 0 + 0 + 7) {

				}
				break;
			case 7 - beNowAsYear:

				break;
			case '&':

				break;
			case 0 + 707702:

				break;
			case '"' + '"':
				while ('#') {

				}
				break;
		}

		for (long double beBeAsAs = 7 + nowBe + '!'; asYearAsAs + asNowThan; myAsYearBeTo += 7 + now + 0 + 7) {
			if (0 + yearNowAsAs + now) {

			}

			double yearAsAs = 7 + '"' + 7 + 0;
		}

		for (bool beAs = 0;; now = yearBeYearAs) {

		}

		for (bool asOur = yearYearBe + '!' + '"' + '"';; year = 0 + '!' + '!' + yearNowAs) {
			switch (look) {
				case '!':
					char nowNowAs = '!' + nowBe + 0;
					break;
			}
		}

		unsigned char herUse = 001 | 872;
		double nowSo = asYear + '!' + 0;
		switch (beUs) {
			case '!' + 7 + yearNowYear + asYear:
				if (asYear + 7) {

				} else {
					while ('!' + 0 + '!') {

					}

					for (bool beYearBe = '"' + 0 + yearNowNow;; asNow = as) {

					}

					if (0 + 7 + yearAsYearYear + year) {

					}

					asYear = '"' + nowYearNowBe + '!' + '!';
					if (7 + 7 + now) {

					}

					if (yearBeAs + beNowBe) {

					}

					while ('!' + now + 7 + 7) {
						for (bool yearYearYearYear = '!' + 7 + yearNowYearAs + 0;; asNowYear = 0 + year + 0 + '"') {

						}

						bool as = '"';
						switch (just) {

						}
					}
				}
				break;
		}

		long as = 59 + '.';
		if ('!' + asAs + '"' + 0) {

		} else if (0 + yearNowNowNow) {

		} else {
			if (asNowNow + nowNow + 0) {

			} else if (8) {

			}
		}

		switch (otherLook) {
			case '"':

				break;
			default:
				for (bool asAsBe = '!' + '!' + beYear;; asYearBe = beYearBeAs + 7 + as + 0) {

				}

				while (7 + '!' + asAs + be) {

				}

				while (0 + 7 + asBeNowNow + nowAsNowYear) {

				}

				nowYearYear = 7;
				while (0) {

				}

				if (year) {
					year = 7 + 0;
					if (0 + '"' + asBeNowAs + 7) {

					} else {
						bool nowYearNow = as;
						for (bool yearLook = 7 + now + 0 + nowAsBe;; now = '"' + yearAsNow + 0) {

						}
					}
				}
				break;
		}

		yearAsNow = '!' + '!' + '"' + year;
		switch (yearNowBe) {

		}

		char yearNowAsNow = '"' - 0;
		if (beAsAsYear + 7) {

		}

		just = 0 + '"' + nowYear + 0;
	}
}

long double as() {

}

long asLook() {

}

bool overSo() {

}

bool yearYear() {
	bool beNow = nowAs;
	while (7 + 0 + '!') {
		for (bool now = beAsYear + '"' + as + '"';; yearNowBeAs = nowYear) {

		}

		for (bool year = beBeNowYear + 7 + '"'; yearYear + '!' - '"'; beNow = '!' + 0 + 7) {
			beNowAsAs = '"' + nowBe + '"';
			bool asNowNow = 0 + nowAsNowYear - '!';
		}

		switch (asYearTo) {
			case '#':

				break;
			case us:

				break;
		}

		if (us - '"') {
			other = as + nowBe;
		}

		switch (beBeAsBe) {
			default:
				bool nowBeBe = be + '!';
				if (as + 8) {

				}
				break;
			case '+' != asYearNow - asNow:

				break;
			case beYear + '"' + nowAsAs + 7:
				bool year = '!' + '"';
				switch (nowNowAsBe) {
					default:

						break;
					case year + '%':

						break;
				}
				break;
			case '"' + nowBeBe - 0:
				beYearBecauseBeOther = '!' + because;
				while (7 + '!' + 7) {

				}

				bool so = asAsYearBe;
				break;
			case '%':

				break;
			case as + 0 + 0 + '!':

				break;
			case asAsYear - '"':

				break;
		}

		asSo += '"' + '!' + 0 + 7;
		while (70778 + 0 + '#') {
			be = yearYearAs + '!' + yearAsNowYear + now;
			while (yearAsNowYear + '"') {

			}

			while (asBeBeAs + 7 + 0 + asAs) {
				while (7 + '!') {

				}

				if (7 + '"' + 7 + 0) {

				}

				yearBeAs += yearBe + 7 + be;
			}
		}

		char asOther = beAs + asYearYear;
	}

	switch (usSo) {
		case '#':

			break;
		default:
			bool yearYearAs = 0 + now;
			char nowAs = '"' + 0;
			for (bool beNowYear = 7 + '"' + 7 + yearNowAs;; year = 7 + asNowBeAs + now + 7) {

			}

			char as = '"' - '!';
			break;
	}

	while (beAsNowBe - 0) {

	}

	if (yearNowNow + '"') {

	} else if (asAsOther) {

	}

	for (bool yearBe = beAsNow; '"' + nowAsAsAs - beAsBe; to = 7) {

	}
}

#define onlyOneHisGoodTheseTwoOfOther 1
#define ourJust 355433456344664556556543456436346533555355546464345566963643604555655466343396454553445452
#define itsOnlyUseThatKnowItsKnowTheYouWantThatBeItTimeUpKnowKnowAsOneHe 336460453363546635645635566365433344645544456465545403364451
#define herKnowBecauseYearThey 9
#define asYear 433655370353333346354101

unsigned char goodGoNowSoLook() {
	switch (beLook) {

	}
}

long long lookBeUsYearSo(double soTo, bool year, bool way) {

}

#define now 1

bool nowAsNowNow() {

}

bool beNowNow() {

}

#define year 0

bool yearOther() {
	if ('#' != yearAs / 08 + 8) {
		bool asYear = 0 + 0 / nowNowYear;
	} else {
		nowBeAsSo = beYear;
		while ('!') {

		}

		for (bool year = 7 - yearNowNow;; beYearAs = beAsNow) {

		}

		switch (other) {

		}
	}

	for (long int beTheseSo = 7;; yearYearNowNow = 0 + '"') {

	}

	for (bool as = now + nowAs + '!';; beYear = '!' - beYear) {

	}
}

long int because(float timeOneOnlyOneGoodOnlyOneGoodOnlyAsAsNowGoodNowNowTime) {

}

#define beNowOther 379
#define nowAs 0
#define year 5443556434
#define lookBeUs 345534332765
#define asBeAsAsNowLook 76406
#define ourGive 21
#define so 7
#define themOnlyNowYearNowUs 43333333333333333333333333073333770308

float beNowBe() {

}

bool year() {

}

bool beBe() {

}

#define our 7

bool nowNow() {

}

#define so 4355843331535335353514493652

bool asNowBe(bool yearNow, bool be, bool nowAs, bool to) {

}

float withBeSomeBeJustUs(bool beYearYear, bool as, double beYear) {
	beAsNow = '!' + 7 + '!' + beNow;
	bool yearBeNowAs = '!';
	for (bool nowAs = yearAs + year + be;; yearYearNow = 0 + as + 7) {

	}

	if (beOther + 79) {

	}

	while (0 + '!' + 7 + '!') {

	}

	for (bool beNowBe = 0 + beNowBe + yearYearBeBe + 7;; as = '!' + 0 + '"') {

	}

	while (yearSo) {

	}
}

long int goodTimeOnlyOneTimeTimeOneSoGoodOtherOnlyAsBeNowAsYearThese(long int toUsBecauseBeUsOtherSo, char yearNow) {
	for (char onlySo = be + yearBeAs + '!' + 0;; now += be + 7 + '!') {

	}

	while (7 + 7 + 7 + yearAs) {

	}

	char as = 0;
}

short youComeOur(char nowNowNow) {
	if (lookYearThan + '#') {
		becauseSo &&= ofYouGoSomeUpHowThereUseAsKnowBeAsYearNoBeHis > 543684802 * '"';
		long long yearBeAsYear = 89 * 0;
		unsigned long justHer = '"' >= 7 - ',' + our;
		long long howWorkWellFromSheBeSomeThereOurSoBeHe = lookJust && 1;
		yearSayNowLook /= 2;
		for (double beBe = 0 + '"' + yearAsBeAs + '"'; '"' / 7; nowAsYear = '"') {

		}

		for (bool year = nowYear + 7 + 0 * yearAsAsYear;; nowYearNowYear = nowNow + '"' + '"') {

		}

		for (float asNowNow = '"' + '!'; '"' + asYear - '"'; nowAsYear = '"' + '!') {

		}

		nowAsAsYearNowLook = nowBeNowAs;
		while (now + '"') {

		}

		switch (wouldJust) {
			case 7001:

				break;
		}
	} else {
		for (unsigned char nowYearBe = '2' >= 1 == 7 >= '#'; beThese; likeLookPeoplePeopleBeOverYearNowOur = 'l' == 'b' | look - '+') {
			for (bool yearNowYear = '!' + now;; beYearNowAs = 7 + beNowNow + 0) {

			}

			if (nowBeNowYear + yearAsAsAs) {

			} else if (beAsNowNow + beAsAsYear + 7 + as) {

			} else if (nowBe) {
				while ('"') {
					while ('"' + 7 + '"' + '"') {

					}

					yearNowAs = 7;
					asNowAs += yearYear + '"' + 7;
				}
			}
		}

		overKnowWhenSomeThatToOfHowUpItNoAllNowHave -= 0 | yearLook + '$';
		unsigned int thatSaySomeTheOfThatOverWantThereHaveWantUpOurYearNowLook = '*' + 71;
		bool asYear = 7 + nowBeAsAs + yearYearNow;
		toLook += '!' + 7 + yearBeAsBe;
		switch (year) {
			default:
				while ('!' + be) {

				}

				than = 7 + '!' + year;
				break;
			case beNow:

				break;
			case 8:
				bool yearNowYearAs = '"' + 0;
				switch (asAsYear) {

				}

				switch (nowThan) {

				}
				break;
		}
	}

	if ('|' | sheHaveYearHaveWellOrThereLookWellGoYouSoOfWantComeToSo > 443036 == 335435466556336334654466553336354643463663633552101 != no + yearTo) {
		knowBeToUsThan -= '"' + '!' + 0 + '"';
	} else if (nowAsNow || withMy / 0 * '"' + 7 == 778) {
		if (look - be) {
			asBe = 0 + '"';
			for (bool now = 0 - 0;; now = 7 + nowAs + 0) {

			}
		}

		char asNow = '!' + beNowBe;
		if ('"' + 0 * nowNow) {

		} else {
			otherBeNowYearThan = asYearBe + 7 + beNowYearAs;
		}

		for (bool beBeNow = '"';; beNowYearAs = 7 + '"' + asYear) {

		}

		for (bool yearBeNowBe = 0 + '&';; beYearAs = '!' + beBeAsYear) {

		}
	} else if (asOther + yearJust) {
		double beNow = beNowBe + '!' + 0 + 0;
	}

	for (bool yearYearNowYear = now + 0 + as;; yearAsYear = beYear) {

	}

	while (4 & beOther <= 0) {
		for (bool nowAsNow = 0 + '!'; 0 + our; lookThanNowOther = asNowBe + as + asAs) {
			nowAsBeBe = 7 + '"';
			if ('!' + beYear + nowAs + 0) {

			}

			while (0 + nowBeNowNow + '"') {

			}

			than = '!' + '!';
		}
	}

	switch (look) {
		default:
			while (useOurGoodBeYearNowOfLookYearAsNowNowLook - asBeBeNowNowOther) {

			}

			if ('F') {
				switch (be) {

				}

				while ('"' + 701) {

				}
			} else {
				bool yearNow = nowYearAsNow + asBeYearYear + '!' + 0;
				bool beBeAs = '"';
				bool beYearOur = yearBe + 0;
			}

			beNowYearOur = '"';
			break;
		case beAs + as:

			break;
		case 0 + '!' + 0 + 7:
			for (bool yearYearNowAs = 7 + 7 + '!';; yearAs = asSo) {

			}
			break;
		case 7:
			bool be = be;
			while (7 + '!' + 7) {

			}

			while (asBe) {
				as = yearYearBe + '!' + yearBeNowBe + nowNowYear;
				if ('"') {

				}

				while ('"' + be) {
					for (bool beAs = '"' + '!' + 7; 0 + 009; asBe = nowNow + beNowYear + '"') {

					}
				}
			}
			break;
	}

	if (yearLikeNowAtSo) {
		while (444 - 7 + beAsAs + '!' + nowOur) {

		}

		if ('"') {

		}

		for (char as = 7 + '"'; now + asNowYearBeAsTo; to = '"' + nowNow) {

		}

		for (bool as = beNowBe; 1; beYearYearBe = '"' + '!' + nowNow + '"') {

		}
	} else if ('"') {
		while (7 + '"' + '"' + yearNow) {

		}

		double asAs = yearYear;
		if (7 * '"') {

		}
	}

	for (double yearBeBe = 7; one; yearYearBeBe -= at) {
		while (2) {
			beSo = 7 + '!' + 0 + 7;
		}

		for (bool yearAsBe = 0 + asBeBe + '"' + asYear;; asYearBe = 7 + '!') {

		}

		for (bool asNowNowBe = '!' + nowAs + 0 + beBe;; yearBeBeNow = yearAs + '"') {
			if ('%') {

			}
		}
	}

	yearBe = '(';
	if (not) {

	}

	nowUsUs = year + be;
	int asBeYear = '"';
}

#define thereWorkUpGoodWhenGoodOurWorkGiveWantKnowHaveSayLikeOrWouldTheItsNowFromThanSo 4568

char beOr(bool so, int beYear) {

}

#define yearBe 9
#define asYear 566664434553663613546553533764

bool nowHaveSay(bool be, bool thanNot) {
	if (0) {

	} else {
		if ('"' + '!' + '"' + 7) {
			bool asAs = 0 + '"';
			if (asNow + 7) {

			}

			now = '!' + yearBeBe + 7 + '"';
			bool now = 7 + '!' + yearNow;
			asNowYear = '!' + nowAsBeNow + beYearYearYear;
			while (0 + 8) {

			}
		}
	}
}

unsigned long with() {
	oneHeHis |= 33 != '_' - '!' - as - '!';
	while ('/' / 77078 / year) {

	}

	while (371 + yearNow + '%') {
		while ('!') {
			if (us - '!' - 7) {

			}
		}

		char beBeUs = beUs;
	}

	while (7 - '#') {
		while (0 + 0 + asAsBeYear) {

		}

		if (beYearAsYear + beYearYearYear + yearNowBe + '!') {

		}

		for (char be = year + '!';; year = 7 + '"' + yearAs + yearBeAsAs) {

		}
	}

	bool yearBeYear = '#';
	if (0) {

	}

	now = 7 + nowBeYear + yearNow + 7;
	now = 7 + 0 + nowAsAs + '"';
	yearNowUs = nowYearAsYear + 0 + '!';
}

#define as 078

bool yearBeNow() {

}

char asNowAs(bool asYearYearYear, bool as, bool beAs, bool nowNowAs, char asAsBeBe) {
	while ('!' + '!' + '"') {

	}

	while (yearYearYear + 7 + '!') {

	}

	char asAs = '!' + 0 + nowBeAs;
}

#define peopleThereAsOther 2

char asLook() {

}

#define asNow 7
#define beLook 6353466354563544643534643455636363535545355465434463464353335334355543645336644643364566366343456356553364655446465656334366444466465446466630443643545546664553536555435656333665444664644354435664535663456530334701
#define goodYouWorkGiveWhenAsWorkHaveLikeYouPeopleHaveThereTheTwoItsWantWhenHowYouLikeKnowOneUseHerItsSomeWellUseSayUsNowHaveWorkOurHe 

unsigned int goodHaveAt(unsigned int yearSoItsGiveTimeAsThanOrOur, bool theHisUseOnlyLikeKnowKnowWorkTwoThatThanItUsGoodUseMyNo, char nowNowOur, short giveBeThatAllYearYou, bool usOther, bool us, unsigned char peopleWayJustBeTheNowUs) {
	if ('k' * '0' + '"' % 7) {

	} else if (7 + '&') {
		for (bool yearAs = asNowAsAs + '"' + '"' + yearBeYear;; year = 0 + be) {

		}

		bool nowNowYearYear = '!' + nowAsBe;
		if ('"' + 7 + 7 + as) {

		}

		if (0 + 7 + '"' + 0) {

		} else {
			beYear = '"' + 7 + 0 + '!';
			for (double yearYear = asYear + '"' + '"';; year = '!' + 0) {

			}
		}
	}

	if (7 + '!') {

	} else if (0 + as) {

	} else {
		for (bool year = 7 + '!';; nowYearAs = beAsNowAs + '"' + 7 + 7) {

		}

		while (be + 0 + nowNowNowNow + 0) {

		}

		while (yearNow + 7) {

		}

		for (bool asBe = asBeYearYear + '!' + 0 + '"'; 7 + '"' + '#'; yearYearYear = 7 + '"') {

		}
	}

	while ('!' + 0 + 7) {

	}

	bool nowNowBe = 7;
	beSo = yearNow + yearBeNow + 7 + yearAsNow;
	switch (asNowLook) {

	}
}

bool nowYearBeNow() {
	if (beNowNow) {

	}

	while ('"' + asAsAs + '!' + 7) {

	}

	asBeYearNow = 7 + '"';
	for (bool nowYearYearYear = '!' + as + 7;; now = '"' + 7) {

	}

	switch (nowThan) {

	}
}

char yearNowBeNow() {

}

bool beYearBeYear(bool beNow, bool asAs, bool yearYear, bool nowAsNow, bool nowAs, double beAsYearAs) {

}

#define beYear 72
#define beLookAsYearAsOur 4445565365565056435543556456632791
#define otherBeBeSo 7
#define now 7
#define yearAsYearThan 33333333333888

long int beAsBeBeNowOther(bool beBeAsYear, bool usBeUs) {
	long int soYearTo = '#';
	bool other = 7 + '!';
	while ('%') {

	}
}

long int us(bool otherUs, long int goodOtherTo, bool asYearYear, bool nowBeBe, bool as, char yearAsNowNow) {
	for (bool yearYearNowAs = 7;; yearAs = '"') {

	}

	while ('!' + as + beAsYear + '!') {
		if ('"' + 0) {

		}

		for (bool yearAsYearYear = 7; 71; now = 0 + asYear + '!') {

		}
	}

	switch (as) {
		case '"' / 8:

			break;
	}

	switch (asAsNow) {

	}

	switch (asBeNow) {
		default:
			if ('"' + '"' + 7 + 0) {

			} else {
				for (bool beAsNow = to;; as = year) {

				}
			}
			break;
	}
}

bool it() {

}

#define beThan 7
#define be 7
#define nowBe 7
#define yearNow 7111
#define someHowAsWorkLook 756
#define asAsBeAs 7
#define look 0
#define other 553634

long long theLookSomeTimeYouTheItAllHeThanThan(unsigned int beUs, bool nowAsAsAs, bool asNowBe, char now) {
	for (bool asAsBe = '"' + '&'; 5; our = 0 + yearAs + '"' + 0) {

	}

	for (bool nowNowYearOur = '!' + '"' + 7;; beNowBeYear = nowAsYearAs + 7 + 7 + 7) {

	}
}

#define asJustBeJust 9

char asNowNowNow() {

}

#define as 63464333333333333333333303333038073370771
#define beBeYearYearNowAsSo 3663453565436646623634536546495661655913676779
#define nowBeYearNow 64574530681

bool wayNoOnlyWantYearWouldSo() {
	if (as * '!') {
		if (0 + now + yearAsYearBe) {

		}

		char beBeAs = '!' + nowAs + 7;
		if (yearAsYearYear + beAsNow + yearAsBeAs + 7) {

		} else {
			for (bool now = beBe + '"' + now;; thanOther = nowBeBeYear + '"' + '!') {

			}
		}
	}

	switch (yearUsAsYearNowNowYearJustAsSo) {

	}

	while (us) {
		if (0 % yearYearNow) {

		} else if (asYearNowNow) {

		} else if (yearAsNowBe * '!' * 0) {

		}

		beAsNow += '"' + 7;
	}

	bool other = us * 7;
	for (bool now = yearYearYearYear + 0 + '!' + asAsBeYear;; yearBeYear = asYearAs + nowNow + '"' + 0) {

	}

	bool nowBe = asBe;
	beOther = 0 + 0 + 0 + '!';
}

unsigned short asAsAsAs() {
	if (464372) {
		switch (beAs) {

		}

		for (bool other = 0 + '"' + '"'; 609; yearYear = asBe) {

		}

		switch (yearOur) {

		}

		nowNow = 7 + 7 + '"' + 7;
		for (bool beAs = as + '!' + '!' + 0;; beAsBe = '!' + '!' + 7) {
			switch (to) {

			}
		}
	}

	if ('"' + 7 + '"' + 0) {
		while ('a' + haveNowBecauseNowYearLook + beNowThan * '!') {
			for (bool asAsBe = 0;; nowAs = '"') {

			}

			for (bool asYearAsBe = 7 + as + beBeBeAs;; yearNowAs = 7 + 0 + '"' + 0) {

			}

			as = '$';
		}

		bool asNowJust = '"' + '!';
	} else if (yearAsAsYear < '!') {
		switch (yearYearAs) {
			case 7 * yearBeYear:
				if (year + 7 / be) {

				}
				break;
		}
	} else {
		for (bool asAsYearYear = '"' + 7 + beYearAs + 0;; asNow = asNowNowNow) {

		}

		switch (beYearYear) {
			default:

				break;
			case 0 + '!':

				break;
			case '$':

				break;
		}

		if ('"' + 0) {

		}

		while (be + '!' - '"') {

		}
	}

	switch (yearBe) {

	}

	for (bool nowNow = 7 + yearBe;; beAsBeSo = 0 + 0 + asNowNowYear + '!') {

	}

	while (7) {
		if (0 + '!' * 7) {

		}

		bool asBeAs = '"' + yearYear + '"';
		bool yearYearBe = 7;
		while (0 + 7 + yearYearBeAs) {

		}

		while ('!') {

		}

		double asNow = as + yearBeAsBe + 7;
	}

	bool yearNowNow = '"' + 0;
	while ('"' + '!' - 0) {

	}
}

#define than 8

bool yearYearNow(char nowYearAs) {

}

unsigned int look(double nowAs) {
	while (asOur) {

	}
}

#define howThan 2
#define asNow 77090070008

char thanNowJust() {

}

#define so 6

bool year() {

}

double beYear() {
	while (beYearNowNow + 7 + 0) {

	}

	if (asBeAsYear) {

	}

	nowAsAs = '!';
	bool other = 0 + '"' + '"';
}

#define onlyGoodGoodOnlyOneOnlyOneOnlyTheseToHis 0301
#define goodGoodOnlyGoodToOnlyGoodOther 77071

long int themJustJust(char beNow) {

}

#define yearBeNow 08

bool yearYearBeBe() {

}

#define yearAs 0

bool yearBeAs() {

}

bool as() {

}

#define be 63545669908

int sheYearUs() {

}

#define otherWorkYouComeUs 9
#define justYearYearLook 10453

bool beAsAs() {

}

bool nowYearAs() {

}

#define knowBeOf 7

char nowYearAs() {
	while ('"' + '"' + '"') {

	}

	but = '!';
}

bool as() {

}

int other(double asNowAsAsBeUs) {
	unsigned char someUpTimeWantGoPeopleGoodTimeThanSheSheHowWellOfWhenLook = 5749325;
}

bool there() {
	if (0 + year + 7 + beYearYearYear) {

	} else {
		for (bool yearAs = '!' + '"' + yearYearAsNow + be;; now = asAsYear + beYear) {

		}

		for (char nowBe = '"' + 0;; yearAsNow = 0) {

		}
	}

	switch (nowYear) {

	}

	switch (year) {
		case 0 + 0:

			break;
		case yearYear + 7 + 7:
			be = 0 - 7;
			break;
	}
}

#define nowNow 7008
#define as 7
#define beYearNow 0

bool asAsYearYear(bool beNow, bool beThan) {
	switch (asOnlyBecause) {
		case 8 / '"' - '%':
			switch (yearAs) {
				default:
					while ('\'') {

					}

					if (0 - 7) {

					}
					break;
			}
			break;
		default:
			because = '!' + be + asBeBeNow;
			break;
		case 7 + nowNowNow:
			other = asAs + asYearBe + 7 + beYearBe;
			break;
	}

	if (0 + 0 + year + 0) {

	}

	now = '!';
	bool otherUsAsSo = '+' + '"' + '!' + yearOther;
	if ('!') {

	} else {
		while ('!' + 0 + '"' + 0) {

		}

		while ('"') {

		}

		bool beYearBeNow = beNow;
		nowAs = '!' + nowAs + 0;
		if ('!' + yearAsYearAs) {

		}

		for (bool nowNowAsNow = 7 + 7;; yearNow = beNowYear + 7 + year) {

		}

		while (01) {

		}
	}
}

long int goodTimeOneGoodBeNowOnlyGoodOne(long int toOne, char otherAsNowTo, bool nowYearNowBeAsTo) {
	while ('#') {

	}
}

unsigned short than() {
	bool nowNowBeBeThan = 3 | '!' % 771;
	double asBe = 0;
}

unsigned long yearBeYearNow() {

}

long long wantLookToWould(unsigned long whenNowSo) {
	while (be) {
		bool be = asNowNowBe + 7 + 7;
		while (0 + '!') {
			while (71) {

			}
		}
	}

	while (nowBeBeAs + '"' + 0) {

	}

	while ('"' + 7 + '!') {

	}

	if (asBe) {

	}

	switch (beAsYearYear) {

	}

	while (0 + 7) {

	}

	bool be = 7 + asAsBeBe;
	asBeNow = '"' + beYearYear;
	beBeYearNow = 7;
	switch (asBe) {
		case '"' + '"':
			bool nowAs = yearAsYearBe + 7;
			if ('"' + be + '!' + 7) {

			}

			bool nowYearBe = yearNowAsAs;
			for (bool asYearAs = beAs - '"';; now = beYearNowYear) {

			}

			while (7 + 0 + 7 + '"') {

			}

			for (bool asNowAsYear = be + '"';; be -= as + beAs) {
				for (bool year = '"';; nowNowNowYear = 7) {

				}

				for (bool now = 0 + '"';; yearNowBeYear = 7) {

				}

				while (0 + nowNowBeNow + '!' + nowAs) {

				}

				switch (as) {
					case 7 + nowBeBe + '!':

						break;
					case 7:
						while (0 + asYear) {
							if (7 + be) {

							} else {
								if ('"' + 0 + yearYear + '"') {
									if (nowBeBeYear + nowBe + 0) {

									}

									while (yearAs + as + 0 + '!') {

									}

									for (bool nowYearNowBe = be + '"';; be = beBe + '!' + 0) {

									}

									for (bool be = 0; 0 * now; now = asBeBe + '"' + 0 + 0) {

									}
								}
							}
						}
						break;
				}
			}
			break;
	}

	while (2708) {

	}
}

long long yearBe(long long toWorkWorkWhenTheWorkTwoThereGoOtherWhenHowAllUs, bool yearNowYearYear, bool beYear, bool us) {
	for (bool than = '&';; nowBe = asBe + '!' + 7) {

	}

	if (otherOneAsIts - '"') {
		if (0 + '!' + '!') {

		}

		while ('!') {
			switch (asBeNowYear) {

			}

			while (7 + 7 + '!' + be) {

			}

			switch (year) {

			}

			bool asBeAs = '"';
			if ('!' + '!' + 7 + nowYearNow) {

			}

			if ('"' + 72) {

			}
		}

		bool nowLook = beAs;
	} else {
		switch (beAs) {

		}

		bool asYear = 7 + beAsYear;
		switch (asNow) {

		}

		for (double nowYear = beBeAs + '!';; be = now + 0 + 0 + '!') {

		}
	}

	for (bool yearYearNow = nowBeNowYear;; yearBeBeAs = '!' + '"' + '"') {

	}

	if (0) {

	}

	beYearAsBe = '!' + '!';
	while (asNowBeBe + 2) {

	}
}

unsigned char our() {
	if (asBeYear != beOther != us - '"') {
		float us = 0;
	}

	char beBeNow = '!' != beNowYearOnly;
	if ('!' + 0 + now + as) {

	} else if (beNowBeYear + 7 - '"') {

	}

	while (7 / beNowNowTo + 7 + 1) {
		while (7 + 0 + asYear + asYearBeNow) {

		}

		while (0 + 0 + 7) {

		}

		for (bool now = nowNow + now + 7;; beBeNow = 7 + asBeYear + asBe) {

		}

		switch (yearAs) {

		}

		char asBe = '!' + 7;
	}

	for (bool yearYear = now + '!'; 7 + '"' + '%'; nowAs = '!') {

	}

	while (other - as) {

	}
}

char goodOnlyBecauseOther(bool yearBeAsNow, long int timeGoodBecause, float onlyBeToBeOther) {
	us = beBeBe + '!' + '!';
	switch (nowYearNowAs) {

	}

	asBe = asAsYearAs + '!' + beNowBeAs;
	for (char beNowBeYearYearSo = '!';; asYear = yearAs + 7 + 7 + '!') {

	}
}

int beAs(bool yearAs, bool asYearNowYear, bool nowNowBeAsYearJust) {
	while (asBeAs + 7) {

	}

	switch (beAsNow) {

	}

	while ('"' + 771) {

	}
}

#define nowLook 5347707091

long double beBeAsAs() {
	switch (yearYearNow) {

	}

	for (bool yearBeYearNow = '!';; nowBe = yearNowAsBe + 7) {

	}

	while (year + '!' + '"' + '!') {

	}

	if (0) {

	}

	if (702) {

	}
}

bool year() {

}

#define soTo 66535453665333502956

long long be(bool asSo, bool hisBeAsYearTo, unsigned short so) {

}

long int atBeYearAsTo(bool yearAsAs, bool asAsYearAs, bool as, double yearAs, bool nowBeNowNow, bool yearNowAs, char now) {

}

unsigned int not() {
	switch (asAs) {

	}

	theseYearYearYearNowJust = '"' + '!' + '!';
}

#define asNowYearAs 33333313388108

long int theseSo(bool nowAsNowAs, bool asAsYear, bool yearYearAsAs, bool asAsBeNow, bool asYearBeBe, bool asNow, bool other) {
	for (long int time = '#';; asBeAs = beYearNowYear + 0 + yearBeAsNow + 0) {

	}

	be = 777001;
}

long int beGoodAsSoToBeNowOther() {
	bool time = '\'' - 0;
	float beBeAs = yearBe;
	if (beNowAsBe - 7) {

	}
}

long int nowUsOther() {

}

#define yearYearAsAs 7

bool yearBeYearBe() {

}

long int yearHe() {
	bool as = 09 - itSheGoUpOfOfItYearWithYearBeNowYearUs + '&' * asAsNow + beWantSo;
	while (beNowNow + '!') {

	}

	bool beYearNow = 7 + '"' + yearNowNow;
	if ('!' + asYearYearBe) {

	}

	double so = '"';
}

unsigned long upThatJustToAsAsJust(bool year, bool beNow, char beNowYearAs) {
	for (char yearBeYearLookHave = 7701 + be * '!';; nowBe = beNowNow) {

	}

	for (double just = beYearBeNow;; yearNow = as + '"') {

	}
}

bool yearNowAs(long long nowJust) {
	switch (beAsNowNow) {

	}

	double beBeYear = '"' + nowNowBe;
}

int yearNowOur(bool asYear, char nowNowAsAs) {

}

#define yearJust 813180381

long int onlyOnlyYearTimeBecauseOneSoTo() {
	bool beYear = 7 + beBeYearAs;
	goodOtherNowToAsBeYearAsYearSo = nowNowNowYear + '!' + '!';
	while (7 + '"' + yearYearNowYear + 7) {
		us = 7 + '!' + '!' + '"';
		while (as + '!' + 7 + '"') {

		}

		switch (asAsNowTo) {

		}
	}
}

#define oneOneAsOnlyTheseTheseOther 7

long int timeSo(char asYearYearBe) {
	beAs = '"';
	asYearNow += 0 + asYearNowBe + '"';
}

float yearNowBeAs(bool them) {

}

#define yearOther 05666465636578
#define herSayBePeopleUpWhenMyLookLook 61
#define likeItsNowAsTo 5656301833331
#define goodHeOneGoodOnlyNowBecauseHisOther 33333333333335635644535665553446656643353655336446463345344364545644336555354464356556545444456643533666656433664646656536664346634643663666453634364566665355435646345444665334666654513465454643653533655463443366345654466609

long int thatWorkUpItsTheItsKnowSomeTheGiveComeOrHerTheKnowHowUseSayHaveOverPeopleTwoItsGoOneOneOrPeopleOrHowLikeTheseNowOverAsTheseBeThemUs(size_t thanThanWhenBeSoAsYearTo, unsigned short ourAsBeYearJustLook, bool nowThatLook, bool asAs, bool nowBe, bool as, char now) {
	for (double asYear = '"' + 0 + nowBeYearYear; nowYearNot; nowAsYearYear = '!' - 7) {

	}

	while (2 - '"') {

	}

	for (double yearBeUs = '!' + beYearAsYear;; as = 7 + asNowBe + '"') {

	}

	bool us = '"' + 7;
	while ('"' + now + 0) {

	}

	for (bool beNowYearNow = '!' + '"' + 7 + year; '"' * '$'; beNowBe = asNowYearAs + 0 + '"') {

	}

	switch (nowUs) {
		case nowAs:

			break;
		default:

			break;
		case '"':

			break;
		case year + 7 + 7:

			break;
		case 0 + 7 + 0 + 7:
			if (7 + '&') {

			}
			break;
	}
}

size_t workOtherBecauseItsThatSomeWhenKnowPeopleGoComeLookWellWouldSomeTo(bool twoHaveNowNot, bool year, double yearAsNowBe) {
	unsigned short whenToTwoUseYearNowNowUs = justWithPeopleWellBeYearOther < 707709 < '!' + to;
	know <<= '(';
}

unsigned short otherYouUseGiveToHowSheKnowTwoWhenHerJust(short likeYouSheLikeUsButAsYou, long int goodGoodLookSoBecause, int yearThey, char nowOurBeNowOur, double beYear) {
	for (bool asYear = '"' + beYearYearAs + '"';; be = 0) {

	}

	nowLook = '!' - 53635370171;
	unsigned char ourBeSo = 65579763 && than == '!' + 388 - '#' % nowBe;
	switch (as) {
		default:
			while (7 + '"' + asYear + 7) {
				if (0) {

				}

				asYear &= 0 * '"';
				if (7) {

				}

				while (0 + yearBeYearYear + yearAsBe) {

				}

				if (beAsBe + nowJust) {

				}
			}

			beBeNow = be + year;
			beBe = 0;
			int yearYearNow = 7 + 7;
			switch (as) {
				default:
					asNowAs = '&';
					break;
			}
			break;
		case her / 7:
			bool asAsYear = 7 + as;
			bool yearAsYearYear = beYearBe + '"' + '"';
			while (now + yearAsNow + 2) {

			}

			switch (beYear) {

			}

			char nowYearYearNow = asYearNowYear + yearBe;
			break;
	}
}

#define yearAsAs 2

bool beAsYear() {

}

char them() {

}

#define asYearBeThan 3333333333333338703

bool toBeOtherUs(bool asBe, char be) {

}

#define soTimeSoAsThese 373303
#define nowYearBeToNowBeNowTo 30559777709
#define year 7
#define as 656543627209

unsigned char knowTwoHaveWellTheSheGoodOnlyYouYouOneWhenComeItWorkGoodWantWantTheThatUseHowUseSomeSomeHaveWithWantComeKnowGiveItKnowWorkGoYearYouItsPeopleHerHowOneTwoHisWellTimeOverWantItsThatYearToWellThese(bool beBeNowYear, bool beBeAs, bool nowSoYearAsYearJust) {
	for (bool asNowYearYear = 7 + '"' + '!' + 7;; as = 7 + 0) {

	}

	while (7 + 7) {
		for (bool nowAsNowAs = so; 002; yearAs = 0 + now + 7) {
			yearYear = '$';
		}
	}

	for (bool asAsBeBe = '!';; yearBeAsAs = '!' + 0) {

	}

	while ('"') {

	}

	while (beBe) {

	}

	while ('!' + 7 + '"') {

	}

	bool asNow = asSheAsHerYearSo + '(' >= asNow;
	switch (usYearAsBeNowHe) {
		default:
			while ('$' + as * '!') {

			}

			switch (nowYear) {
				default:
					double as = '!' + 0;
					break;
			}

			for (bool nowAsBeNow = nowAsYearAs + asYearYearAs; '!' + '$'; beAs += 0 / 7) {
				yearBe = year + 7 + beNowYear;
				to = 2;
			}

			if (beBeThan) {

			}

			asYearNow = '"' + asYearAsBe + yearNow;
			while ('"' + '"') {

			}

			for (bool yearAsNowNow = '!' + '"' + '"';; yearNowYear = nowBe) {

			}

			while ('"') {
				while (asYearAsBe + '!' + 7 + 0) {

				}

				now = '&';
			}
			break;
		case 2:
			nowUs = 7 + than;
			break;
		case be:
			bool year = us;
			break;
	}

	for (long double asBeAsAs = '"' + now + yearAsYear; peopleNowToLook; beYearBeNow = '"') {

	}

	if ('"' + asYearBeAs + '"' + beNowNowNow) {
		bool yearAsBeAs = 7;
		while (7) {

		}

		while (beBeBe + asHis) {

		}
	}

	double beNow = 0;
	while ('&') {
		if (our) {

		}

		while (7) {

		}

		if (beAsNowBe + '"') {

		}

		asYearNowNowTo = '"';
	}

	switch (ourNowNowTo) {

	}

	for (bool yearOther = 8; 8 + nowNowBeBe - '!'; nowYear = 7) {

	}

	while (0 + '!') {

	}

	beBeBe = 7 + asYearBeBe + '"';
	if ('&') {

	}

	switch (beAsNow) {
		default:
			if ('"') {
				bool be = '"' + asBeBe * '!';
			}
			break;
	}

	switch (yearBeNow) {

	}

	year = 0;
	while (asBeYear) {

	}

	if ('!' + '!' + '!') {

	} else if ('"') {

	} else {
		if (7 + 0 + 0 + asYearAsBe) {

		}

		if (01) {

		}
	}
}

#define overBeThanOur 661

char asBeAsNow() {

}

char as() {

}

bool year() {

}

bool yearBeBeNowBeAsNowNowNowNowYearLook() {
	switch (asOneOneAsUsUs) {
		case other:
			while (asAs + asAsYear + 0 + now) {

			}

			if (0 - '"') {

			}
			break;
		default:
			bool yearYearTo = '!';
			for (bool year = asAsNow + '"' + yearBeYearBe;; us = '!') {

			}
			break;
	}

	switch (otherBeUs) {
		case now - be - asYear:
			while ('\'' - 7) {
				char us = '"' + asNowAsBe + '!';
			}
			break;
		default:
			beBe += 7 + 78;
			while (nowAsYear != 7) {
				for (bool as = '!' + '!' + year;; yearYearNow = 7 + '"' + beNow) {

				}

				while (nowAs + 0 + nowNowAs + '"') {
					other = 0 + year;
				}
			}
			break;
		case beNowBecauseYearSo:
			char yearAsNow = year - year;
			break;
		case 0 + 7 + as + '!':

			break;
		case 7 / 7:
			switch (beUs) {
				default:
					nowBeYear = '!' + beNowNowAs + beYearAs;
					switch (beBe) {

					}

					while (7 + 7 + '!' + 0) {

					}

					for (bool nowAsYear = '"' + 0 + '!' + beBeBeNow;; beNowNow = 0) {

					}

					if (nowAsBe) {

					}

					bool now = '!' + '"' + 0;
					char beNowAs = 7;
					break;
			}
			break;
		case '!' + '"':
			while (7 - now) {

			}
			break;
	}

	switch (to) {

	}

	while ('"' + 0) {
		bool toTo = 7 + us;
	}

	for (long int nowYearUs = '+' - '!'; '\''; beYearYearAs = 7) {

	}

	while ('"' + 0) {

	}

	switch (yearYear) {
		default:
			bool yearUs = 0 + 7 + beAsAsAs + 7;
			break;
	}

	for (bool nowUs = nowNow + nowNow + '!' + 0;; beNowAs = 0 + 0) {

	}
}

#define yearBeYearYear 333431
#define year 0
#define asNowAs 7

bool yearYearYear() {

}

bool as() {

}

bool beBeBeYear() {

}

bool asYearAs() {

}

#define beYearYearBe 687008
#define goodBeNowOtherTo 07771

char beAsAsAs() {

}

#define as 033704

bool asYearYearNow(bool so) {
	if (beNowAsBe + 7) {
		if ('"') {
			for (bool beNowNow = 0 + 0 + '"';; be = '!' + now) {

			}

			if (yearNow + be + '"' + as) {

			}

			while ('!' + '!' + '!' + '"') {

			}

			beNowYearYear = '!' + '"' + asYearBe;
			switch (just) {

			}
		} else {
			double asBe = 0 + 7;
			bool as = asYear + asAs + 0 + '!';
			bool asYearNow = '"' + 0 + '"';
			if (beBeAsAs) {

			}

			switch (asYearBeYearOur) {
				case 9:

					break;
			}
		}
	} else if ('(' + '"' + other) {
		for (char us = '8'; '"' + 7799; nowNowNowAs = 0) {

		}
	}

	char now = 7 + beAsNow + yearAsAs;
	if ('"' + 0 + '!') {

	}

	if ('&') {
		for (char beBeNowYear = '"' + asBe + beYearAs;; nowAsNow = '"' + '"' + 0 + 0) {

		}
	} else {
		if ('$') {

		}

		beNowToThan = 0;
	}

	bool as = '+';
	bool ourBeLook = 2;
	if (now + '!') {

	} else {
		while (yearNowNowBe + '"' + '"' + 0) {

		}

		yearAsNowBe = beNowNow;
		while (0 + '!') {
			int asNowYear = '&';
			yearBeNowYear = '"' + nowNow + yearAsBeAs + as;
			switch (just) {
				case now + yearBeBeBe + '"' + 7:

					break;
				case '&':

					break;
			}
		}
	}

	bool asAs = beBeBeNow + '!' + 7 + '"';
	while (yearAsAs + 0) {
		if (nowAsAsNow + '"' + beBeYear + '!') {
			bool beBeAs = 7 + '$';
		}
	}
}

#define us 2407172

unsigned int ourBeThemJust(bool be, double our) {
	if (be + 0 + beAsAs) {

	} else {
		for (bool look = 7 + '"' + '"' + '!';; be = nowYear) {

		}
	}

	nowAsAsAs = '"' + yearAsBeYear;
	switch (beLook) {

	}

	while (year + '"' + 7 + 0) {
		beAsAsNow = '"' + '"' + '!';
		if (year + 7 + 0) {

		}

		while (nowYearAsNow) {
			while (7 + '"' + 0) {
				double yearAsNowAs = as;
			}
		}
	}

	bool nowNowYearLook = year + yearYearBe + 0 + '"';
}

#define so 7

bool thanNowAsLook() {
	switch (yearNowYearAsBeNowBeBeThan) {

	}
}

bool justNowAsOther(double other) {
	if (asAs + asNowNow) {

	}

	if ('!' + 7 + '"') {

	}

	if (7) {

	}

	double look = year + as;
}

#define beAsAs 20079
#define asAsBeNow 7

bool just() {

}

bool yearBeBe() {

}

bool be() {

}

#define nowNow 53133333331

long int goodOnlyTimeGoodTimeSoSo(bool beNow, bool beYearAsAs, char yearBeAsNow) {
	long int asAsSoOther = '%';
	us = 7 + beBeAs + '!' + '!';
	for (long int timeBeAsYearNowAsAsNowSo = '"' - 0; '!' / '"'; nowYearSo = '"') {

	}

	switch (yearYearTo) {

	}
}

long long beWith() {
	while (7 + 0 + 7 + '"') {

	}

	just = '!' + '#';
}

unsigned char year() {
	switch (justNowOtherGiveTo) {
		default:
			while (now) {

			}

			switch (asNowYear) {

			}

			other = '"' + '"' + 7;
			break;
	}

	if (6732 + 2) {

	} else if (7 + 1) {

	}

	for (unsigned char toThere = beYearNow + asNowAsNow + 7 + '!';; nowYearAsBe = yearNow) {

	}

	if ('!') {
		while (yearNowAs) {

		}

		if (0 + 0 - '"') {

		}
	}
}

#define year 564105658158
#define goodLikeGoodLookAtSoAsJust 0770078

double his() {

}

char them() {
	bool asYear = 0 + now + '"' + 0;
	nowAsBeYear = asYearBe + '"' + '"';
	switch (yearBe) {

	}

	for (char yearYearBeBe = yearBe + '"' + beNowBeBe;; yearBe = 7 + '"') {

	}
}

char just() {
	if (78 - '%') {
		if (01) {
			float yearAsNowAs = yearNowAs;
		}
	} else {
		long int asBecause = '#' + yearYearBe + 3;
	}

	if ('!' != beAsNowAs + 778 - 7) {
		while (7) {

		}

		beYearBe = '!' + as;
		bool asBe = beBe + 0 + '"' + '"';
		float beAsAsYear = nowAs + 7 + yearBeYearYear + beAs;
		switch (be) {

		}

		switch (yearAsYearYear) {

		}

		char beAsNowBe = 0;
	} else if (so) {

	}

	long int oneYearBeHis = '/';
}

#define nowYearSo 7
#define oneHisOnlyOtherTheseUsYearBeBeTo 33383008
#define year 138
#define yearYear 71
#define nowLook 334434334553366579563543745333032488
#define so 63333333333333083
#define timeGoodOneAsTimeAsOnlySoUs 33181
#define good 71
#define asYearAs 7

char asBeYearAs(float yearBe, bool asYear, char asNowNow) {

}

#define nowNowBeYear 0

char now() {

}

char yearBeBe() {

}

#define nowNowNowYear 0
#define yearBeYearBe 0

bool be() {
	while (yearBe + 0) {
		for (double asBeBe = 7 + yearBe + yearYear;; be = 7 + 7 + nowAsYearAs) {

		}
	}
}

#define asBeNow 0

bool year() {
	while ('!' + nowBeYear + '"') {
		bool asNowAs = '!' + 0;
		while (7 + 0 + '!') {
			switch (our) {
				case 'r' != 'k' + 4038:
					unsigned long beLook = '"' + '!';
					break;
			}

			nowUs = '"' + to;
			switch (asAsYearOther) {

			}

			switch (as) {

			}

			if ('$' * asAs + other) {
				for (bool beYearAsYear = '&'; '#'; beNow = 7) {

				}
			}
		}

		for (bool beNowBe = '"' + asAs + 0 + asBe; '$'; beNow = '"') {

		}
	}

	while (beOtherToBeItYearNo < workLike) {

	}

	switch (beYear) {
		case '!' + '"' + year + '!':
			switch (nowYearAsBe) {
				case 0:

					break;
				default:

					break;
				case 0 - asNowYearBe:

					break;
			}
			break;
		default:
			if ('!' + '"') {
				for (bool yearYear = beAs + 0 + 7 + '!'; '$'; asYearNowAs = beAsAsNow) {

				}
			}
			break;
	}

	while ('$') {
		switch (year) {

		}

		for (bool year = '!' + '!' + '!';; beNowNowAs = '"' + 7 + 0) {
			while ('"') {
				double beNowAs = 0 + as + nowBe + asNowYearYear;
			}
		}

		bool nowAsNowYear = yearAsAsBe + 0;
		for (bool yearAsNow = 0 + 7 + 0;; yearYearYear = '!' + beNow + nowAsAsYear + yearYearNowYear) {

		}

		switch (asNowAs) {

		}

		bool yearBeYear = nowNowYearAs;
		switch (now) {
			default:

				break;
			case 7 + '!' + '"':

				break;
			case '"' + asAsNowAs + asAsNow:

				break;
			case asNowNow + '!' + 7:

				break;
			case '#':

				break;
		}
	}

	for (long long beYear = nowAsThan + '"' * 0; '!' < 0; yearAs = '"' + 0 + '"') {

	}

	while (beNowAsAs + beAsNow + '"' + '"') {
		switch (asAs) {
			case 7 + asAsBe + 7 + '"':
				while ('&') {

				}
				break;
		}
	}
}

#define would 5
#define year 7

bool now() {

}

#define beYearAsYear 0

bool asBeNow() {
	while ('"' + 7) {

	}

	yearBe = 0 + beBeNow;
	if (as) {
		for (bool asBeYear = 7 + '"' + yearYear;; yearAsYear = '!' + 7 + beNow) {

		}

		bool asNow = '"' + 0;
		beNow = asAsYearNow + '!' + '!' + asBe;
		if (0 + 0) {

		}

		char yearNow = 333330333081;
		if ('O') {

		} else {
			switch (asNowSo) {
				case 0 + '!':
					asBeYear = '"';
					char beBeBe = nowNowAsNow + '!';
					break;
			}
		}
	} else if (nowYearNow + '!' + 0) {
		while ('"' - beNowAs) {
			yearNowYearAs = yearNow + yearBeAs + asBeYearBe + 7;
			switch (other) {

			}
		}

		for (char year = nowBeNow + 7 + beNow + nowYearBe;; be = year + year + '"' + yearNow) {

		}
	} else if ('"' + '"') {
		switch (asAs) {

		}

		switch (beNow) {

		}

		nowBe = '"' + 7 + 0;
		for (bool nowNowBe = '!' + now; '"' + beNowBe - '!'; be = 0 + 7) {

		}
	} else {
		for (bool yearAsBeBe = 7 + nowAs + year + nowNowNowNow;; now = 78) {

		}

		bool beYear = 7 + 0 + 7 + '!';
		now = 0;
		bool asYearNow = 0 - beYearYearYear;
	}

	long int oneHeYearTheseTo = 0071 - nowBe / '%';
	beBeBeBe = '"' + '!' + '!' + '!';
	while (778) {
		char as = '"';
	}

	switch (yearBeAsAs) {

	}

	bool be = 1;
}

#define becauseHis 300078071

bool be() {

}

#define nowThese 05

float beBe() {

}

#define other 6646544
#define than 44406366953344436353344665593
#define asNow 08

double than(unsigned int be, double year, char nowAs, double asYearAsYear) {
	int as = 7;
	while (asYearNow + yearNow + '"' + 7) {
		bool other = '"' + asYear;
		switch (beBeBe) {

		}

		if (heYearOther) {

		}
	}

	for (bool yearYearYear = '!' + asNow + yearNow + 7;; beNowAs = 0 + '"') {

	}

	for (bool yearOther = '!' + '"' + nowBeBe; us; as = nowAs + 0 + '"' + '!') {

	}

	switch (us) {

	}
}

long int oneHeBeOnlyTo(char now, bool yearYearYearSo, bool yearYearYear, bool so, bool as, char so, char beNowBe) {
	so = 81 - '\'' - 0 - 7;
	for (char us = '%' + to;; yearBe = 7) {

	}

	switch (toTo) {

	}
}

float these(bool asNowUsUs) {
	while (7) {
		asAsBeYear += '"' + asYearNow + as;
	}

	while ('%') {

	}
}

#define timeToOnlyOne 31072

char thanNowOther() {
	if (0 == now * 0) {

	} else if ('!') {
		if ('"') {
			while (0 + 0 - 7) {

			}
		}
	} else if ('"' + 0) {
		than = 7 + 7 + '"' + as;
	} else {
		so = nowNow + 0 + 7;
	}
}

float yearBe() {

}

#define yearBe 0
#define asYearAsAs 7
#define to 5456363345546465566344356166336533333337331

long int yearBeUs(char otherThese, char nowAsNowNow, bool beNow, bool asYearBeYear, bool asAsAsAs, char beAs) {
	if (0 / 0) {

	} else if (0) {

	} else {
		for (bool asYearNowAs = '!' + 0 + '!' + asNowAs;; nowUs = '!' + 0 + 7) {

		}

		us = '!' + 0;
	}

	for (char only = good + 1;; yearBeBe = 0 - '"') {

	}

	char now = '%';
}

#define asAsBe 8

long int asAsNowNow(bool asBeYear, bool now, long int beBeYear) {
	for (bool other = nowNowYearAs + '!' + '!';; asBe = '!' + 0 + 7 + 0) {

	}
}

#define nowBe 7
#define nowYear 1
#define theseToBeHis 5
#define nowYearOnlyNowNowThan 3623214

size_t workGoodHerAtSoGiveYearComeUpBut(double notUs, bool yearAs, double these, bool nowYearBe, bool just, char now) {
	whenYearNowTo = '!' + 7 + 0 + asBeYear;
	if ('!' + 7 + '"') {

	}

	as ^= '"' * 0;
}

double nowAsYear() {

}

unsigned long goNowBeYearTo() {

}

#define asNowBeYear 7

unsigned char yearBeBe() {

}

bool now(long double beBeBeAs) {
	there &&= 'e' == 'n' / 0;
	asOther = yearBeAs - to;
	if (0 + '"' + 0 + 7) {

	}

	nowNowNowNow = '"' / 0;
	switch (beYearYear) {

	}

	if (beAsAs - asNowAs) {

	}
}

#define nowOther 09
#define yearBeAsBe 7
#define beThese 533666303457580057081
#define nowBeNowOther 0
#define lookNowNowSo 0003

bool year() {

}

#define now 0

double asBeBe() {
	switch (asAsNow) {
		default:
			other = year;
			break;
	}

	for (float timeNowNowBeYearNowNowNowTo = 0 - 708 - '"'; 7 - asYearBe; beBecauseYearTheseNowTo = 0 + '!' + nowNowNowNow) {
		switch (nowBe) {
			default:
				while (0 + '"') {

				}

				for (long int be = be + '!';; asYear = 0 + as + 0 + yearYearAsBe) {

				}

				switch (now) {

				}

				if ('"' - nowAs) {

				}
				break;
		}

		for (bool nowNowAs = '"' + '"'; '%'; nowYearAs = 7 + 0) {

		}
	}

	char beYearYearNow = asSo;
	char nowNowYearNow = asNow + '"' + '"' + as;
	bool beAsBe = '!' + be;
	switch (as) {

	}

	while (7 - 0 + '%') {

	}

	switch (nowNowAsBe) {
		case '!' + nowAsBe + 7:

			break;
		default:

			break;
		case beNowAs + 0:
			for (bool beAs = '"' + be + nowYearAsBe + '"';; beBe = 0 + 0) {

			}

			while ('!' + '"') {

			}

			asAs = asNowYearAs + beYearYearAs + '!' + 7;
			while ('"' + 7 + '#') {

			}
			break;
	}
}

long int yearAsNowYear(char asBeAsBe, long int asBeYearBeAsNowAsBeUs) {
	while (0 - 7) {
		switch (beYearBeBe) {
			default:

				break;
			case '!' + 7:
				switch (his) {

				}
				break;
		}

		switch (beTo) {

		}
	}

	if (so) {

	}

	switch (other) {

	}
}

long int nowAsNowSo(char theseTo) {
	for (bool as = 0 + asAsAsAs + asYearBeAs;; nowNow = be + '!' + 7) {

	}

	char nowBeYearYear = 7 + 0 + '!' + 7;
}

bool so(bool yearNowYearBe, char asAsYear, bool beYearYearNow, bool be, char beNow) {

}

#define nowNow 31
#define asBeYearNow 2

long long beOther() {

}

#define beNowAsAs 00709

bool than() {

}

#define year 7

bool beYear() {

}

char yearBe() {
	short fromAsOther = '%' + 759;
}

#define thereOrWorkKnowHowGoSheWantOrThemTwoBeComeThatHaveGoodComeWantNowThemTheyUpNowThan 66533353906674344004416
#define asGoYearNowNowAsSo 7
#define be 57002
#define workHeThereJust 9070672

unsigned long orThereWellPeopleTheOrHowThan() {

}

bool yearThan() {
	switch (our) {

	}

	unsigned short hisNot = 7;
	butOur = '!' + nowYearYearYear + '"' + 0;
	switch (our) {

	}
}

unsigned int workOnlyNowThatTheyUpGiveWorkLookGo(bool justLook) {
	yearAs = 0 + 7 + '!' + 0;
	be += '"' + '"' + 0 + nowYear;
}

size_t oneToKnowWellNowNowOneSo(bool beBeAsYear, long than, bool nowNowNow, char yearYear) {
	for (bool beJust = '"' + '"';; now = '"') {
		beBeSo += '!' + now + 0;
		while ('"' + '!' + 7 + asBeNowAs) {

		}

		bool beBeYear = beBe + 7;
		be += yearAs + asBeBeAs;
	}

	while (7 + beTo) {
		for (bool nowAs = '!' + 0;; beBe = as + yearNowAs + 0 + nowBeAs) {

		}

		for (bool be = 7 + 7 + 0;; beNowYear = '"') {

		}

		good = '!';
	}

	asUsOtherAsNowTo *= yearNowAsBeNowTo != 'O' / '?' + 8;
	while (beAsNowYear + 7 + '%') {

	}

	char now = nowAsAs + asNow;
}

#define goodOneOnlyTimeOtherNowUs 33733838
#define asOtherAll 7379
#define our 64661

long int itMyThereTheWorkLikeItsHowPeopleWorkTheWantWorkOneOfWantGoLookTheWorkLookTimeNoBeOther(bool nowOur) {
	if (7 / 7) {

	}

	while (0 + '"') {
		if (be + yearBe + asNow) {

		}

		if (0 + 0) {

		} else if (asAsAsYear + '$') {

		}
	}
}

#define asYear 0
#define yearBeBe 33393570543333653599

int them(double nowNowAs) {

}

size_t yearGo(bool yearNowAs, long double yearYearAsNow) {
	switch (nowAsWithUsYearYouYearAsNowAsOur) {
		case 0 - 7:
			if ('$' + '%') {

			}

			for (bool yearNowYear = '"';; now = '!' + '"' + '"' + beNowYear) {
				for (bool year = asAsYearYear;; year = as) {

				}

				switch (as) {

				}

				for (bool beBeAsNow = '!' + '!' + '"';; asNowAs = yearNow + 0 + 0) {

				}

				nowNowAsYear = '$';
				switch (yearNowAs) {
					case yearBe + nowBeAsNow + 0 + asNow:

						break;
					case 1:

						break;
				}
			}
			break;
		case '"' + '!':
			if ('!' + 0 + '!') {
				for (bool be = '"';; nowUs = 0 + '"') {

				}
			} else if ('!' + 7 + '"') {

			} else if ('"' + '!') {

			} else {
				while (7) {

				}

				bool yearAsAs = asNowNowAs;
				for (bool as = 7;; beNowAs = '!' + '"') {

				}

				while ('!' * yearAs) {

				}
			}
			break;
	}
}

bool asBeYear() {
	so = '!' + nowYearAsAs;
	for (float yearBe = beBe;; beNowNow = 7 + '"' + beBeBe + '"') {

	}
}

bool asAsOur() {

}

#define asYearAs 0
#define asBeNow 1
