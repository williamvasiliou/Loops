#include <UUUU2U0002.h>
#include <UUUU0v00.h>

long nowYearAsYear() {

}

#define year 7072

bool yearYearSo() {
	if (itPeopleTheSomeGiveOnlyOrHerItUseOverGoodWhenThereSomeGoHowUseGoSomeOneOfOfHaveTheSayItHowGiveThatWellGoWorkKnowItsHeSomeTwoUseItsOnlyUpItsLikeSomeTwoSayMyItLikeUpOfGoodSayHerOnlyLikeOfGoOfPeopleWorkWantOnlyHerUseKnowKnowHerUpItBeLookWould ==  != 6034564146250171 | orSayAsWayOurJustAsWith != 5343664029453519 < ':') {
		size_t oneKnowItsItsUseGiveSheItWellPeopleOverHowOnlyNowBeOneHowLookGoWellJust = whenAsThanTwoBeGoodWhenToBeOrGiveBeYearBeBeThan || 33361646635335476 >= 7755 && 'f' + beWithSo + '!' + beJust;
		switch (orNowThemToTo) {

		}

		thanSoYearOur ||= '=';
		switch (knowLookThanOneUsYearYearLook) {
			default:
				switch (asNowAs) {

				}

				while (beNow & 7) {

				}

				while (nowNowNowYear + asYearAsBe) {

				}

				bool yearNowAs = '!' + yearAsYear;
				while (asNow + asAsAsNow + '$') {

				}
				break;
		}

		if (')') {

		} else if ('"' + beYear + '"' + as) {

		} else {
			yearYearNow = 0 + beYearYearNow;
			while (7) {

			}

			bool year = 0 + '!' + '"';
			if (7 + '"') {

			}

			char nowNowBeNow = 7;
		}

		bool nowNow = asWould;
	} else {
		if ('t' | 01 != '#') {
			if ('[') {
				switch (nowNowAsBe) {

				}

				switch (us) {

				}

				char asBeBe = '"' + '!' + '!';
			} else if (0 + 0 + '"' + 7) {

			} else {
				now = '"' + 0;
				switch (our) {
					case asAs + 0 + 0 + 7:
						switch (just) {

						}
						break;
				}
			}

			bool yearBeYear = 02;
			for (bool nowAs = yearBeNow + beYear;; beAsYear = 0 + 7) {

			}

			for (bool yearNowNow = '$';; nowYearYear = beAsNow + 0 + '!' + 0) {

			}

			while ('!') {
				switch (asNow) {

				}

				bool be = just;
			}
		} else if ('#') {

		} else {
			bool nowYear = beAs;
			if (year + as + '"' + as) {

			} else if ('"' + 7) {

			} else if (0 + now + be) {

			} else {
				bool asNowBeNow = '"' + nowBeAs + '"';
				if (yearBe + 7 + '"' + '"') {

				} else {
					if (nowNowAsYearTo) {

					}
				}
			}
		}

		if ('\'' + 7516 + 9) {
			while (0 + 7 + asBeBe) {

			}

			if (7 + 0 + asBeNowYear + beAsBe) {

			} else if (as) {
				bool other = 0 + 0 + '"';
				asYearAsBe = '!' + 0;
				bool nowYearYear = '"' + nowYear + yearAs;
				asYearAsYear = 7 + 0 + yearNowYear;
				switch (to) {

				}
			}

			beYearBe = 0;
			switch (asNowYearBe) {
				default:

					break;
				case asBeBe + nowAs * 7:

					break;
			}
		} else if (nowNow) {

		} else {
			bool year = 0 + beBeAsBe;
			for (bool now = 0 + 0;; year = asBeNowYear + '"') {

			}

			long long us = 0 + '%';
			switch (yearYear) {

			}

			bool beYear = 7 + 0 + be + beYearYear;
			bool yearSoNowYearYearNowSo = 0 + yearYearNow + 7 + 7;
		}

		for (double asBut = other;; nowBe = '"' + 0 + '!' + yearBeNow) {

		}
	}

	bool us = 5;
	if (54732412 == thereGoHerPeopleHaveSayPeopleOrPeopleWhenSheHowSayWouldKnowSheNotJustYearNot) {
		switch (useGoTheOrWhenUpTheYouAsToOurThan) {
			default:
				switch (lookOur) {
					case nowNow + yearNowBe:

						break;
					case beYearAsAs:
						while (as + '!' + 0) {
							while (yearThan) {

							}

							year += '!' * as;
						}
						break;
				}

				float now = 0;
				nowBe = 1;
				if (770701) {

				}
				break;
			case 7 + nowNowNowYear + 7 + be:

				break;
			case '!' + '!' + 0:

				break;
			case 7 + '!' + '"' + 7:

				break;
			case asAsYear + '!' + '"':

				break;
			case 71:

				break;
		}

		if (beThem) {
			for (long asNow = 7 + '"' + '!';; asAs = be + '"' + '!') {

			}
		} else {
			switch (come) {

			}

			workWould = beAt;
			now = 0;
			for (float beBeYear = 78;; yearNowYearAs = yearNowAsYear + 0) {

			}

			for (bool beBeYearAs = '!' + 7 + '"' + asBeAs;; yearNowYear = '"' + '"') {

			}

			for (long long yearBeAs = us;; beBe = '"' + 0 + '!' + nowYearAs) {

			}
		}

		if ('"' % yearAsBe) {

		} else {
			float nowYearNowAs = 0 + '"' + 7 + 7;
		}
	} else if ('b' >= yearBeAs & '-') {
		while ('N') {
			switch (well) {
				default:

					break;
				case '&':
					for (bool nowAsAsNow = '"' + '"';; be = '"') {

					}

					asNowBeAs = now + nowBeAsBe + be;
					while ('"' + as + 0 + 0) {

					}

					char year = '!';
					break;
			}
		}

		while (asBe) {
			if (1) {

			} else if ('"' + '"') {

			} else {
				now = 7 + 0 + yearBeBe;
				if ('!' - 7) {

				}
			}

			for (bool be = as + 0;; as = 0 + '"' + nowAs) {

			}

			for (bool beBe = '!' + 8;; yearNowAs = '"' + beNow) {

			}
		}

		if (7209) {

		}

		bool yearUs = 7 + '!' + 0;
	} else if ('$' - ',') {
		if (beTheyLook * 9) {

		}
	} else if (asNowNow + asYearThat + '&') {
		for (bool to = 0;; asBeYearBe = nowBe + '!' + 0) {
			while (0) {

			}

			switch (yearAs) {

			}

			switch (beBe) {

			}

			for (bool beYear = 0 + 0 + yearYear;; year = '!') {

			}

			year = 0 + 7;
			switch (beNowAs) {

			}

			bool asAsYear = 0 + '"' + 7;
			switch (beAs) {

			}

			if ('!' + '!' + '"' + 7) {
				for (bool nowBe = '"' + 0 + beBeYearNow;; beBeYear = '"') {

				}

				while (0 + 0) {
					while (yearBeYear - 7) {

					}
				}
			}
		}

		char beYearAsYear = yearBeBe + '"' + 0;
	} else if (17002 + yearYearAsAs - '"') {
		bool year = yearYear + now + '"' + '"';
		double nowYear = asBeBe;
		switch (beBe) {

		}

		beNow = 7;
		year = 0 + as;
		for (bool nowBeNow = 0 + 7 + '"';; nowYearYear = '!') {

		}

		yearNowYearNow += '!' + beBe + '"';
	} else if (knowThereAsWant) {
		asBeBe = 7 * year;
	} else if (yearAsBecause) {

	} else {
		bool be = 7 + 0;
		switch (as) {
			default:
				for (bool asYear = '!' + nowOur;; as = '!') {

				}
				break;
		}
	}

	while (asAsNowNow + yearNowBe % '!') {
		our <<= 's' * yearNowBe - at;
		at ||= yearOther + 77003 + '(';
		for (bool likeSoPeopleUpAsWithBeOther = 07800009 || yearAsNowBe - 7781 - year - '"'; 7 | '"'; beAsOther = 0 + '"' + 0 + yearAsAs) {
			if (nowYearNow + 0 + '"') {
				for (bool asNowYearNow = nowYearNow + 0 + '!' + '"';; beAsBeBe = '"') {
					bool to = '!' + 0;
				}
			}
		}

		for (bool as = '"' + nowAsYearYear + as + 7; beYearYearYear * 'P' * 7; beAsYearNow = 7 + '"') {

		}

		while (as + 0 + '!' + beAsAs) {
			nowBe = asYear + '"' + as;
			if (other) {

			}

			beAsNow = yearAsYear;
			for (bool beBeBe = 0 + 0 + 0;; asYearBeBe = '#') {

			}
		}

		while (62 / 0) {
			bool asYearBe = 7 + '!';
			while (7 - asBeBe) {

			}

			for (char now = 0 + '!' + '!' + '"';; nowAs = '!' + yearNowNow + '"' + beYear) {

			}
		}

		nowNowAs %= 0;
	}

	if (howSheNotHaveOrUpWithHisYearOtherOur & '.' + 0 + 1) {
		unsigned long asAsYearWay = 238;
		nowUs = '!' + 0 + 0 + asNowAsNow;
		unsigned long asYear = '!' + '!' + 7 + '!';
		beNowNow = 0;
		asAsYearAs = 7 + '!';
		yearYearNowBe = nowBeNowNow + '"' + '!' + beYearNowBe;
		bool yearAsYear = '"';
		for (bool beNowNowBe = '!' + beYear + 0 + 7;; nowBe = yearAsBe + asYear) {

		}

		for (bool asBe = 2;; nowBe = year + 7) {

		}
	} else {
		switch (beAsYearYearAsAsTo) {

		}

		while (317077028 + not - '"') {

		}

		switch (wayUs) {
			case nowBeYear + now + us:

				break;
		}

		bool yearBeBeNow = '.' - 0;
	}

	size_t nowTheyOurYearYearSo = 4475605007079 & 33338 >= 40209 + 58 / beAs;
	while (us % yearAsBe + '!' + '&') {
		beBeBeNow = '"' % yearYearNowAs;
	}

	for (bool nowAsBe = '!' + '"' >= '&'; heThereThanHaveGiveOur * 709 - '"'; year = yearAsBe + 0 + 0 + yearBe) {

	}

	while (asBeBeBe + 0 + beAsYearAs + 0) {
		if (asYearYearYear) {
			yearBe = '"' + asAsBeBe + '!';
			if ('!') {

			}

			while (8) {

			}
		}
	}

	switch (nowYearAs) {

	}

	beTo = as;
}

#define  536345563334536564453636345646654434465645939
#define wellTimeOtherTimeAsSome 0
#define our 1365304
#define haveYearThereBeNowSayLookAsAsNowOther 373
#define justSoTo 88
#define from 41

unsigned int from(bool be, bool beYearNow, char yearBeAsBe) {
	allUsOther = '&';
	while ('!' - 7) {
		for (bool asAs = 0 + 7;; as = '"' + '!' + as + 0) {

		}

		while ('"' + 7 + 0) {
			for (char beYearBeYear = 7;; asAsBe = 0) {

			}
		}

		double as = yearNow + beAsAsYear + 7 + '"';
	}

	if ('"' + beNow + 0) {
		for (bool nowYearNow = asAsAsNow + 0; asUs; asNowYearYear = 0 + yearNowYearAs) {

		}
	}
}

#define peopleYearJustOrOfItsOtherThanBeAsTheseGive 68317008

double upComeGoWayNoOurGoodButTimeAsSoYearYearLook(bool nowNowTo) {
	switch (yearAsNow) {
		default:
			if (yearBe >= asYearNow) {

			} else {
				if (yearBe) {

				}

				switch (so) {

				}
			}
			break;
	}

	while (nowBeNowNow) {
		if ('"' + beBe + 7 + '!') {

		}

		while (nowNowAs) {

		}

		now += asYearYearNow + nowYearBe;
	}

	if (7) {
		asYearBeNow = 8778 + 009;
	} else {
		if (now + beAsBeYear + '"' + 0) {

		}

		while ('"' + 0 + yearNowNow + 0) {
			asNowNowNow += beYear + now + nowAsNow;
		}
	}
}

char beBeYear(bool asOther) {

}

bool nowSo() {

}

#define lookTo 1

bool asNowNow() {
	if (8) {

	}
}

bool asYear() {

}

bool asYear() {

}

#define asBeAs 63665345541466534456554493059

char nowOtherBeYearJust(bool my, long now, bool beBe, double beAs) {
	switch (other) {

	}
}

#define yearYearAsBe 90709

double nowYearAsNow() {
	switch (oneAsOtherBeNowTo) {
		case '!' + 7:

			break;
		case '#':
			while (0 + nowBe) {

			}

			while ('"' - '"') {
				asNowAs = '!';
				if (7 + 0 + be) {

				}

				for (bool yearNowAs = '!' + '"' + 7;; yearNow = 0 + 7 + 0) {

				}

				while (yearNowNowYear + 7 - nowAs) {

				}
			}
			break;
		case '"' + '%':

			break;
	}

	long int usTimeOther = '_' != 083 != 178 - yearBeBe - '"';
	if (7 != asBecause + asOther) {
		long int beYearAsBe = yearAsBeBe + nowYearNow + '!' + 0;
		switch (be) {

		}

		while (0 + '"' + '"') {

		}

		for (bool yearBe = yearNow + '"' + '!' + beBeNowBe;; be = '!') {

		}

		asAs = '"' + 0 + nowAsYear + 7;
		for (bool now = '"' + 0 + '!';; yearAsBeNow = be) {
			if ('"' + yearBeYear + '!' + 0 - 7) {

			}
		}
	} else if (nowAsAs) {
		switch (asNowYearNow) {

		}

		char now = 7 + 0 + beAs + beBeBe;
	} else {
		switch (be) {
			default:
				if ('"') {

				}

				if ('"' + 7 + '"') {

				} else {
					asBeBeAs = yearAs + 0 + 7;
					while (yearAs + '"' + '"') {

					}

					switch (asNowAsBe) {
						case 0 + '"' + nowYearNow:
							while (beAsYearAs + 7 - '"') {

							}
							break;
					}
				}
				break;
		}
	}
}

float soNowTo(bool onlyOurYearYearNowAsThan, char nowAsBeYear) {
	while (7 + yearBe + asAsAs + '!') {

	}

	switch (nowBeAs) {

	}

	now = nowNowNow - 0;
	for (bool nowNowAsNow = yearNow + 0 + nowYear + 7;; as = '"' + 7) {

	}

	if ('%') {

	}
}

bool with() {
	while (asBeNow + now) {
		switch (beBe) {

		}

		for (bool yearNow = '"' + '"' + yearBeNow + yearYear;; yearBeBe = '"' + '"' + 7 + year) {

		}

		bool asYear = 7 + 7 + 0 + '#';
	}
}

#define yearBeAsAs 7

bool as() {
	for (bool nowNowAsAs = 0 + beBeNowNow;; beAsAsYear = '!' + now) {

	}

	for (bool asBeAsYear = 0 + '!' + beYear + '!';; yearYearBe = '"') {

	}

	while (7 + '!' + asAsBeBe) {
		if (nowBe + yearAs + be + 7) {

		}

		bool beNowBe = now + now;
		if (7 + '"') {

		}

		while (0 + as) {
			while ('!') {
				double nowYearYearAs = 7 + '!' + 7 + '"';
			}
		}
	}
}

#define nowNow 7

bool as() {

}

double now() {
	while (46708 != 'o' & 'h' >= 613889722 < so || as + yearOurBeYearJust) {
		 >>= timeUseGiveGoodWantOnlySomeOrHowHowGiveGoItItTwoWith || 55334466574951 + thanSo + 5;
		long int whenWhenKnowUseHerGoodTwoWhenUpYearPeopleHowAsUseGoodTherePeopleYouAsHe = ',' | justLook % beYearBe > nowYearAsNow * yearYearNow;
		switch (nowBe) {

		}

		if (asAsNowAs) {

		}

		as = 0 - nowBe;
		switch (yearUs) {

		}
	}

	if (comeOrUseOneWantUseWorkOfTimeYouGoodLikeItGoodSayWorkOverKnowGiveTheTheSheOverTheyUpSomeThatSomeComeTimeThatToItsOtherWouldThey > 'g' | 540034445656680638 > 'd' >= 7 / '!' * asYear) {

	} else if (to && no) {
		if ('%' * 09) {

		}

		beAsBe = asBeNow - '!';
		year = beYearYear + beAs;
		if (now + 0 + '"' + '!' * 7) {

		}
	} else {
		if (beAsBeYear + '!' + '"' + 7 - '!') {

		}

		switch (asYearTo) {

		}

		switch (nowYearYear) {
			case be % 0:
				bool as = 7 + '!' + beAsNowYear + '"';
				bool yearBe = 0 + beOur;
				switch (that) {

				}
				break;
		}

		for (bool yearBeYearAs = 7 + yearBe + '!' + 0;; asBeAs = '!' + beBeNowNow + '!' + '!') {
			switch (nowNowYearNow) {
				case '"' + be:

					break;
				case 7:
					char yearAs = '!' + '"' + 0 + '!';
					break;
			}
		}

		beYearBe = 7 + asAs + year;
		for (bool nowBeNowYear = beNowAsAs;; asAs = '!' + 7 + year + '"') {

		}

		if (now + 0 + yearYear + yearBeNow) {

		} else if (7 + nowNow) {
			nowAsYearBe = be + year + '"';
			while (as + 0 + nowNowYear) {
				switch (asNowAs) {

				}

				switch (yearAsNowNow) {

				}

				for (bool as = nowYearYear + '!' + 7; 772; nowAs = 7 + beAs + 0 + '!') {

				}
			}
		}
	}

	yearYear = '"' + 7 + 7 + 0;
	if (1) {
		while ('!') {
			if (beAs) {

			}

			while (0) {
				for (bool nowAsNowBe = now * beNowBeYear;; yearBeNow = 7 + '"' + '"' + '!') {

				}
			}
		}
	}

	switch (yearAs) {

	}

	long yearYearNow = year + '"' + yearNowAs;
	switch (beYearYearBe) {
		case 9:

			break;
	}
}

#define thereSayHaveKnowOnlyHerHowSheWellGiveOverItOnlyTheItTheSayOrNowThatSayTimeHowUpItsLikeTwoTwoWhenSheOfHaveOfHowSoWorkThanOrItTheseGoodToUsUsAsHeSo 556365
#define have 634451074457068
#define nowNowYearNow 300872
#define nowJust 233584566335385455845347655777704
#define nowAsAsBe 9

char asBeYearYear() {

}

#define other 02
#define noOrJustNot 1

bool nowAsYearNow() {

}

bool asAs() {

}

bool asAsNow() {
	for (char now = beNowBeAs + asAsAsNow + nowBeNowAs + 7;; asAsAsYear = '"' + 0) {
		oneOnlyGood |= '?';
		for (bool yearBeYear = now + '!' + 7 + '"'; '%' + be + 81; beYear = now + asYear + be) {

		}
	}

	so = '#';
	long int timeOnlyToUsYearOther = '+';
	switch (because) {
		case '!':

			break;
		default:
			bool nowAsBeYear = beBeYear;
			while ('#') {

			}
			break;
	}

	long int hisOnlyOther = beOnlyHis;
	switch (yearNowBe) {
		default:

			break;
		case 7 + 0:

			break;
		case nowBe + yearYearAsYear + year:

			break;
		case 7:

			break;
		case 778:

			break;
	}

	switch (asNowYear) {
		case '!' + '"' + '"':
			if (0) {

			}

			switch (asNow) {

			}

			char asBeYear = be + 0 + 7;
			break;
	}
}

#define us 0778
#define asHisAsOther 3037654
#define nowWantNowThan 5544406200777707701

long likeWouldAllJustWantUsGoNowSo(bool asBe, float nowAsNowNow) {
	switch (beTo) {
		case '*' + beNow + 8:
			if (asYear + '!' + year) {

			}

			while (0 + 7) {

			}

			if (7 + as) {

			} else {
				switch (other) {

				}
			}
			break;
		default:
			if (0 + now) {

			} else if ('!' + '!' + 7) {

			} else if (nowYear + 7 + yearYear + '"') {
				switch (asAsBe) {
					default:

						break;
					case yearNowJust:

						break;
				}
			}
			break;
	}

	nowYearThemSoNowOur = yearYearYearNow + 7 * yearAs;
	for (bool nowYearAsNow = '.' + yearNowAsBe + '&';; than = '"' + beAs) {

	}
}

#define yearNowAs 0
#define nowYearAsNow 0

unsigned int beBeBeBeSo() {

}

long beBeYear() {

}

bool as() {

}

bool yearNowBe() {

}

#define to 333333331038377338

long int becauseSoBeOneToOnlySoBeHisAsHis() {

}

#define nowHe 33371
#define asAs 1
#define nowTo 0
#define goodThemHis 15709778
#define useThan 036643648494624404460016709

unsigned int fromUseNowNowSo(char as, bool beNow, bool yearNowYearBe, long yearNowAsBe) {

}

#define us 7
#define withOverBeWhenItsNowYearAsOtherOur 0

double nowAs() {
	switch (goodGoodOnlyOneAsOneUs) {
		case '!' + asBeYear + 0 + 7:
			if ('"' - asAs - 7) {

			}
			break;
		default:
			if (7) {

			}

			switch (nowAs) {
				default:

					break;
				case 8 - '!':

					break;
			}
			break;
		case 0 - 7:

			break;
	}

	switch (nowBecauseSo) {

	}

	if ('+' != 38 + us + '%' + '!' + 8) {

	} else {
		long int oneOneNowNowNowUs = '#' + '%' - 7;
		for (bool asNowYear = '%'; 7 - 7; yearYearYearYear = '#') {
			while (7) {
				asYearAs = 0;
				if ('!' + 0 + '!' + 7) {
					bool nowAs = asYear;
					for (char beYear = '!' + asNow + yearBeYearNow + yearBeNow;; year = '!' + '"' + '!' + '!') {

					}
				}
			}
		}
	}

	while (otherBeOther + '%') {

	}

	if ('R' + '"' - asBe) {

	}

	switch (to) {

	}

	switch (asAsNowNow) {
		case nowBeNow + 0:
			switch (yearNowAsYear) {
				default:

					break;
				case beNow + '%':

					break;
			}
			break;
	}

	bool yearNowBeNow = '!';
	bool nowBeBe = 7 + 7 + yearNow;
	while ('"' + '"' + 0 + '"') {
		bool nowBeAsYear = '$';
	}

	while (beNow + yearNow + 7 + '"') {
		switch (asUs) {
			case '%':
				switch (our) {

				}

				bool than = '!';
				break;
			case yearNowYearNow:

				break;
			case 0 + yearNowAsYear:

				break;
			case '"':

				break;
			case 7 + '"' + 7 + '!':
				for (bool beJust = 0 + beBe;; yearNow = yearAsAs + 0) {
					bool nowYearAsAs = '"' + 0 + beNowYearAs;
					for (bool nowYearBe = be;; beBeBeYear = beAsYear) {

					}

					switch (yearAsBeYearAsAsJust) {

					}
				}
				break;
		}
	}

	beThanNowNowThem |= lookAsOnlyLook * '$';
	for (bool yearNow = '!' + 0 + 7 + as;; be = 0 + as + yearYear + 7) {
		while ('"' + '"' + 7 + 7) {
			while (yearAsNow + 0 + as) {
				switch (asNowAsYear) {

				}

				while ('%' + beAsBe + '"' + 7 - year) {

				}
			}
		}
	}
}

#define wayTimeOur 3454490675

short theTwoWorkJustPeopleItsItsJustSoWant(bool asThan, short thereKnowHis) {
	while (asOther) {
		if ('"') {

		} else {
			bool beYearAs = other;
		}

		for (bool beNow = yearNowBe - '!';; nowBeBeAs = '"' + nowAsNow + 0) {

		}
	}
}

#define soBeAsOneTheseThan 36
#define asOur 7

char just() {

}

bool asAsAsNow() {

}

bool asBe() {

}

bool nowBeYear() {

}

#define be 776
#define overOneOneOnlyGoodBeUsPeopleSoThereNoNowHaveOrNowYearMy 1
#define peopleWellThese 794

bool yearSay(bool our, double other, double nowNow, bool to) {

}

#define asNotAsUsJust 355333373333333333
#define beOneBeTo 773
#define asAs 1
#define goodBeTo 3333311
#define beGood 3871
#define so 74
#define year 7

char us() {

}

char asBe() {

}

#define to 03

bool asUs() {
	while (0) {

	}

	than = 7 + beAsYearNow;
}

#define asBeJust 33454466645465564563335417076809

bool asYear() {

}

#define whenYouBeBeAsSoUs 505

char nowNowNow() {
	for (char beAs = '"';; yearYearNow = nowAs + yearYearBe + beNow + '"') {

	}

	bool now = '"' + '!' + '"' + '"';
	switch (asAsYear) {

	}

	while ('!' + now + 7) {

	}

	for (bool year = 0;; beYearNowAs = '"' + '"' + '"') {

	}

	for (bool nowBeNowNow = 0 + nowNow + as; yearBe + 0 - as; yearYearBe = '!' + 7) {

	}
}

#define onlyOnlyOnlyTimeTimeHisOnlyOneYearBeBecause 3

long int timeOneTimeTimeOnlyOneBecauseUpJustAsFrom(unsigned char asNowNowAs, char beNow) {

}

#define be 

long int knowWorkWorkWhenOrTheWorkItsSomeHerBePeopleKnowNowAll(unsigned long overGoUseAsWantItsWellGoTheItOneOverOverWellYouItHowGoHerKnowUpWellAtWantGoGoOrWellPeopleTheThereWantGoodLikeOverComeKnowPeopleHerGoComeWorkAsGive, unsigned short herThemHowSomeGoWantBecauseGiveTwoAsOnlyWellWantHowTwoSomeUsePeopleLikeItsUpThanOrSomeOneYouToSo, double justNowOther, bool yearJustThan, unsigned int beWith, bool asYearYearBe, bool as, bool us) {
	switch (soKnowBeUseThanYouWellWantTheyHowGoHowThatFromThe) {
		default:
			while ('!' + 71 * thanAsBeBeBeSo) {
				switch (upWouldAll) {
					case '!' + other:
						if ('!' + 7 - nowAsBeYear) {
							switch (yearBeNowBe) {
								default:

									break;
								case '"' * 7:

									break;
							}
						}

						for (long double now = beBeNow + beNowNowNow;; yearAsJust = beYearYear + 0 + 0 + 0) {

						}

						bool year = beNowAs + 7 + 0;
						while (yearYearYear + 0 + '!' + nowNow) {

						}

						for (bool asAsYearNow = 0;; nowBe = 0 + 7 + nowBe) {

						}

						while (nowNowYear + beAs) {

						}

						switch (yearAs) {
							default:

								break;
							case year:
								for (double beNow = '"' + '!' + '!' + 7;; nowNow = '!' + '"' + beBeNow + asBe) {

								}
								break;
						}
						break;
					case yearNowBeNow:

						break;
					case 7 - 0:

						break;
				}
			}

			for (long int asYearNowNowJustYearJust = '"' + 75 < yearAsBe; '"' - '!'; asNowYear = 0 + now + yearNowNow) {

			}

			for (long int whenBeKnowJust = 850809 && thereBeLookAsOur - '%'; '-' > '*'; yearYear = yearYear + '!' + '!' + as) {

			}

			if (0 + 7) {
				for (bool asBeAsAs = 0 + '"';; yearYear = 0) {

				}

				nowYearYear = '#';
				for (bool nowAs = 7 + nowYearAs; 7 * now; now = beYearAsNow + 0 + 0 + '!') {

				}
			} else {
				switch (than) {

				}
			}

			for (bool yearBe = now + '!';; yearAsAsNow = 0 + 0) {

			}

			switch (yearNow) {

			}

			if ('"' + '"' + '"') {

			}

			for (bool yearYearBeBe = '"' + '"';; look = 7 + as + nowYearYear) {

			}
			break;
	}

	switch (ourWhenYearKnowGoNowYearBeBeAsOur) {
		case '"':
			while (yearThan) {

			}

			for (bool nowAs = nowYearNow;; year = nowBeYearYear) {

			}

			if (1) {

			}
			break;
		case '.':

			break;
		case ')' - beYearBeNow:
			if (as + '!') {
				while (7 + year + year + nowBe) {

				}

				bool beYearYear = '$';
			}

			switch (just) {

			}
			break;
		default:

			break;
		case year + yearNowOther:

			break;
		case nowBe:

			break;
		case '!' + yearNow + '"' + beBeAsAs:

			break;
		case 7 + '#':

			break;
	}

	while (them + 0976589 / yearYear) {
		our += '"' + now + nowBeNowBe + 7;
		if ('$' + 7 + 079) {

		}

		bool to = '!' + '"' + beNow + be;
		bool now = '"' + 7 + '!';
		bool than = 0 + 0 + be + asBeBeYear;
	}

	switch (theHowPeopleTimeGoTimeOverItsTwoSheLookNotUs) {
		case '&' + '!' + '!' * as:
			switch (beNowAsBe) {
				case '&':
					if (yearNowBeNow + 7 + '"') {

					}

					bool be = asNowAsUs;
					break;
			}
			break;
		case giveToUs >= 7 - yearNow:
			char yearNowThereNowOther = 0 * '!';
			while (7 + to) {
				for (bool nowNow = asNowAsAs + yearBeYearBe + 7;; asYearYear = 7 + '!' + 9) {

				}
			}

			switch (yearOtherYearJust) {

			}

			if (asBeAs + be + 0 + now) {
				if (beAsNowBe + '!' + 0) {
					switch (beNow) {

					}

					while (yearNowAsAs - 0) {

					}
				}
			} else if ('"' * beAsAsNow) {

			}

			while (year + 7 + beBe + 7) {

			}

			switch (nowOur) {

			}
			break;
		default:
			for (bool yearBe = nowNowYearAs + asBe;; nowAsAsNow = '!') {

			}

			if (7 + '!' + 7 + '"') {

			}

			if ('!' + 0 + 0 + nowYearAsYear) {

			}

			while ('"' + '"') {

			}

			yearYearBeYear = '!';
			for (bool yearYear = '"' + beAsNow + 0; beYear - '!'; asYear = '"' + nowYearYear) {

			}
			break;
		case 6801 || '#' * '"':

			break;
		case year + nowAs + '!':

			break;
		case 7 + asBeNowAs + yearYearNow:

			break;
		case 7 + '"' + '"':
			bool nowAsJust = '"';
			break;
	}

	switch (nowYearNowOur) {

	}

	for (bool way = 0 + asAs; 02; yearYearBe = be + yearAs + 7) {

	}
}

#define itYouOverWantComeOrOnlyWhenThereLikeHaveWorkOneYouComePeopleWhenWhenKnowGoodTwoComeHowPeopleOverThanThanGoodItsGiveGoOfTimeOneWorkOrTheLikeHerOfUpComeWellSomeTimeJustOfThatGoHaveGoodWorkItKnowGoGoodKnowHerWorkThereWantPeopleTo 4366363344552223

long long knowSomeUpWantWhenItKnowTheSomeItsWantOverUseUseWorkThePeopleComeOverThereGiveTheSheGiveButUseComeUpHerGoWantUsOverLikeOverOfHerHaveGiveThereUseOurGoodHaveToTo(long long sayTwoOrGiveGoodWorkWhenThatItItOnlyTwoWhenThereTwoSayTimeUpUpGoodHowOnlySayGoodSheLikeHerComeThatSayComeKnowItHowItSayGoodGiveHerOnlyPeopleHaveBePeopleKnowUseUseThereOfWantThatItsYouYouTheLookKnowWayThemAsWouldComeHerThan, long lookAsOur, long double other, bool yearOther) {
	if (047092 / yearNow + 01) {
		while (0 + 0) {

		}

		if (7 + '"') {

		} else {
			for (bool now = be;; nowNowBe = 7 + '"' + 0 + '"') {

			}

			year = '!' + 0 + year;
			switch (be) {
				case '"' + 70709:

					break;
			}
		}
	} else if (yearSo + '&') {
		while ('%') {
			while (be + beBeAs) {

			}

			for (bool yearBeBe = '!' + 0 + '!' + as;; nowYearBe = 0 + yearBe + asYear + '!') {
				for (bool asNow = 7;; be = 0) {

				}

				switch (beAsBeBe) {

				}

				beNow = 7 + yearNowYearYear + beBeNow + 7;
				while (nowYearYear + 7 + '"' + 0) {
					if ('!') {

					}

					while (beAsYear + '&') {

					}
				}
			}
		}
	} else if (asNowOurYearBeBeNowYearNowAsAsYearUs * '"') {
		while ('!' + beYearAtOther) {

		}

		if (butLook + look) {

		}

		for (char be = year + 7;; yearNowAsAs = nowYear + year + '"' + 7) {

		}
	} else if ('&' < would - '"') {

	}

	asNotOtherOrThanMyWayOurAsYearSo .= '"' * 7 - 7;
	switch (nowThatAt) {
		default:

			break;
		case beNow + asAs + year:

			break;
		case 0 + 7 + 0:

			break;
		case '$':

			break;
	}

	bool that = '!' + 7 + '"';
	for (size_t as = 'R' - be | beYearHis + '$';; beNow = be + 0 + '!' + be) {

	}

	nowAsNow = beBeYear + '!' + 7 + be;
	switch (than) {
		case '"' + 0:
			bool yearNowYearBe = '!' + asNowYear;
			year = 7;
			if ('"' * 7) {

			}
			break;
		default:

			break;
		case '&':

			break;
	}
}

#define useWhenWorkGoTheUseItsComeTheUseItOfPeopleOrOverGoComeSayHaveKnowPeopleTimeOverHaveTheYouOverNowItYouHowOnlyOverOtherNowTo 3536476342756446735014

unsigned short otherThan(char asYearLook) {

}

bool asNowBeYear() {

}

bool wellOur() {

}

bool look() {
	for (bool asNowNowNow = '%'; '!' * nowAsBe; asBeNowBe = 7 + 0 + '!' + '!') {

	}

	if (9) {

	}
}

#define theAsGoThatWithWellNowBeItsPeopleItsUsAsPeopleNot 54965279

unsigned int asAs() {

}

#define theyToMyWithAsOther 0

char asNowWould() {
	while (7 + 7 + 0 + 7) {

	}

	for (bool be = now + '!';; now = '"' + '"' + 0 + 0) {

	}

	while ('"' + asBeNow + '!' + '!') {

	}

	if (asYear + 7 + beYearBeBe + year) {

	} else {
		switch (nowNowYearNow) {

		}

		if (0) {

		}

		yearNowAsYear -= '!' + as + 0 + '"';
	}

	if (7) {

	}

	double asNowBeAs = beNow + '"' + asNowYearAs;
}

bool beNowAsNow(bool beYearNowTo) {

}

#define than 65333333331801313303

long int yearBeBeBe() {
	for (bool asNowAsYear = 7 + 0; '%'; yearNowBeNow = 0 + '!' + 0 + '"') {

	}

	while (nowYearYear + 7 + '"') {
		if (beNowNowYear) {

		}

		if ('!' + 7 + '"') {
			for (bool beAsNowYear = beNow + nowBe + asNow + 7;; asNowBe = 7 + 7 + '"') {

			}

			if (as) {
				char be = 7 + '!' + 0;
			}
		}
	}
}

#define asHeYearNowOneYearBeGood 333107338

float beNow() {

}

#define asTo 0

bool yearBeSo() {

}

#define onlyTo 1

double nowAs() {

}

#define as 354566363634446

unsigned short two(long beYearBeYear, char year) {

}

unsigned long yearSayYearWould(bool the) {
	switch (be) {
		case 7 + yearAsBeAs:

			break;
		case 0 + '!':

			break;
		case nowAsBe / 0:

			break;
	}
}

#define now 466
#define she 643333333333333338
#define other 8333100830081
#define yearTo 33333177378

bool yearYearYearBe() {

}

bool other() {

}

#define beUs 7

char as(double yearAsNow) {
	for (bool beAs = yearYearNow + '"' + '"' + 0;; year = nowYearAsNow + '!' + 7) {
		if (01) {
			bool nowNow = 0 + 01;
		} else if (7 + '"') {

		} else {
			for (bool asYear = '!';; beBe = '"' + '!' + asYearBeAs) {

			}

			bool as = 0 + 7 + '"';
			bool nowBeYear = '"' + 7 + year;
			switch (other) {

			}
		}

		if ('!') {

		}

		if (7 + 0 + '!') {
			for (char yearNowYearBe = '!' + year + beAsBeNow;; yearAsAs = beNow + '!') {

			}
		}

		switch (nowAs) {
			default:
				look = asNowYear + '!' + '!' + asAs;
				break;
		}

		for (char yearBeNow = '"' + 2;; be = 7 + 7 + asNowYearAs) {

		}
	}

	switch (howNowHisBeOther) {
		case soGiveOtherTo + than:

			break;
		default:

			break;
		case '!':

			break;
		case '!' + yearAsBeYear + '"' + yearNowNow:

			break;
		case as + 0 + '!' + as:

			break;
		case 0 + '!' + '!' + year:

			break;
		case '!' + 7 + 7 + 7:

			break;
		case 9:

			break;
	}

	switch (beNoHerUpUpYearBeBeBeBeBeOther) {
		default:
			if (0) {

			}

			for (bool yearBeAsNow = nowAsBeBe + 0;; asBeAsAs = 0 + '"' + '!') {

			}

			switch (be) {

			}

			for (bool nowBeAsBe = yearNowAs;; as = yearNowYear + '!') {

			}

			beAsNowYear = '!' + '!' + 7 + '"';
			if (7 * nowBe) {

			}
			break;
		case '$':

			break;
	}

	if ('!' + yearAs + now + 7) {

	}

	if (beBeNowBe + '!') {

	}

	switch (look) {

	}
}

#define likeOnlyOfAsUsTheseShe 141295
#define people 9

double yearBe() {
	bool now = asAsYear + '"' + yearYear;
	just = '"' + asAsBe + be + '!';
}
