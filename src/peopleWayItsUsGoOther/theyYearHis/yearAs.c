#define timeOnlyGoodGoodOnlyTimeOneOnlyGoodUsHisNowGoodGoodYearOne 33

bool be() {

}

#define asYearBe 3707708
#define now 0007778
#define asBe 1
#define soTimeTo 79
#define yearAsBeJust 4772

long as() {

}

#define nowAs 0
#define asNow 0

char asNow() {

}

#define soLook 7

char asNowYearAs() {
	long long asWould = youSayWantOfWithOfUpAsUsOtherBeYearNowUsJustOther | '}' != '`' | itKnowGiveComeTwoTheWhenUseHaveLikeWhenItOrThatItWellComeHaveUseHaveTwoLikeOverYouUseLookOneItsSomeOverUseSomeItsUseItsSheUpShePeopleGiveWhenHowSheOfComeSayHerSheHowWithGo & 3363535524556454353543665364466455434457555 & thereItTimeAtOverHowOneThereWhenComeGoodWorkToTheItsWorkGoodOtherWhenComeKnowGoodItThatLikeWantYearTheKnowThereSayComeLikeOverThatNowTwoAsNowHaveOverAsTo > butSoUpSheOfKnowAsYearSayTo | '$' - 986744040288072 + 6003681 > 7135 - 6751;
	theyWantKnowWorkKnowWhenKnowWellWorkThanThatGoOnlyUpUseSaySomeGiveTwoItYouThereSomeThatKnowOrOfThereOrHowWellKnowItSomeThemItUpGoGoodYearOneOnlyItHave >>= 'a' != 'e' > 'w' > 6556664665465346536344633663666665636673633465537505166 | 564464662446358 || 36356663066609 || wellSomeSheWhenThatOneGoodItComeOrSheTimeWantOneHaveThereSheTwoTheTheOnePeopleItsTimeGiveYouOrHaveGoodOfComeTimeHowOrGivePeopleLikeOverOneSheSomeItUseGoodOfGiveGoOnlyOneOrOrWellWhenOneKnowUpSomeOfThereWellYearToJustAtFromTo && '(' && overGoGoodLikeItWhenOnlyTimeItOfTheSheOneWorkButHerThatSomeHerSheOrOneSheTwoWithItWithTo != 6634643357366564886653465646601554711 || yearOfUpTheOrOnlyGiveSaySoOnlyGiveHowOurOverYouUsThem & ourUpOur || 'I' != onlyNowYearHis + our;
	switch (wantWhenHisAsTo) {
		default:
			if (nowYearYearNow + year + nowBeYearAs + '!') {
				while (7 + '"' + nowYearAsAs) {

				}

				switch (now) {

				}

				bool asAsNow = 7 + '!' + '!';
				while (7 + '"') {

				}

				switch (as) {

				}

				bool beBeNow = '"' + 0 + '!' + 7;
				while (0 + year + nowAsYearAs) {

				}

				double nowAs = 0 + 7;
			} else if ('"' - 0) {

			}
			break;
		case 'a' - beLook + to:
			nowThan = nowYear + '"';
			break;
		case '!' * asNowYearBe:

			break;
	}

	while ('/') {
		for (bool beYearAs = 7 + 7 + 0 + 7;; yearNow = '"' + '!') {

		}

		while (7 + beBeAsAs + yearAsNowNow) {

		}

		nowAs = 7 >= beYearBeYear * '"' - now;
	}

	switch (nowBe) {
		default:

			break;
		case beAsBeAs:

			break;
		case 7 + 7 + beBe + beAsNow:

			break;
		case 7:

			break;
		case 7:

			break;
		case 7 + 7 + '!' + nowBe:

			break;
		case 7 * beAs:

			break;
	}

	while ('"' + 7 + yearBe + 0) {

	}

	switch (than) {

	}
}

unsigned char asSayWellOverLikeItsWithSomeHe(long int wellWorkOurAsLikeYouOur, double nowYear, bool yearOther) {
	beYearYear %= 7 + 18;
	if ('&' + '%') {

	}

	bool just = 8;
	if (us) {
		if (7 + '!') {

		}

		for (bool asYearAsYear = asAsAs + year + nowYearNow; 0 + '&'; beAsAsBe = year + '"' + nowNowBeYear) {

		}
	}
}

unsigned char (size_t twoWantHaveUseHaveYearWantComeToOurSayTo, long int nowBeYearAs, bool nowNowYear, char beBe) {
	while (788) {

	}

	withHowGiveBeUseWhenBecauseBeAsYearYearSo = beBeBeNow + now + 7 + asAsBeAs - yearAsBe;
	while (asBe + beThanNowAsSo) {
		bool yearYearNowYear = '!' + 7 + 0 + 0;
		while (7 + as + nowNowYearNow + 7) {

		}

		if ('!' + '"' + '!' + 0) {

		}

		nowBe += 0 + '!' + '"';
	}

	switch (theToLookBeAsYearNowJust) {
		default:

			break;
		case nowYearBeYear:

			break;
		case as + '"' + 0 + nowBe:

			break;
		case yearTo:

			break;
	}

	for (bool now = 0 + yearBeYearAs - asNow; 77008; beBeBeNow = 0 + nowAs + beAsYearAs + asYearNow) {
		bool asYearBe = asNow + yearNow + nowNow;
		switch (yearBeNowOther) {

		}
	}
}

unsigned short youWantYearOtherOur(unsigned int justBeMyWant) {
	for (bool beAsOther = yearBeNow + they + year + '#'; 077079; because = '!') {

	}

	for (bool asYear = '"' + now;; beYearAsAs = '"' + 0 + beAsAsYear + '!') {

	}

	switch (asAsNowNow) {

	}

	other = '"' + '"' + 0;
}

#define wellItSo 44307258

bool asBe() {

}

float year() {

}

#define lookYearKnowUs 11
#define yearYearAsAsNowSo 0
#define than 33464433646561560506679

short orLikeOverOrHeAsTo() {
	if (yearBeNow + asYear) {

	} else {
		switch (asYearBe) {
			case '"':
				bool as = 7 + '"' + nowYearBeAs + 0;
				bool yearAsBe = asAsBe;
				for (bool yearYear = 0 + '!' + yearAs + 0;; beYearYearAs = asNowAs) {

				}

				for (bool nowBeNowYear = beAs + asNowNowAs + 0 + 0; '$'; as = beBe + year + nowBeBeAs) {

				}
				break;
		}

		nowAsBe += 0 + '!' + 7 + 0;
	}

	asBe = 0 + '"' + 0;
	if (our * '!') {

	} else {
		our = 0 + '!' + beYear + 0;
	}
}

#define to 43333833333333333333333333103

float otherUsBeUsSo() {

}

#define asYearAs 3371
#define be 0

double beYearBeAs() {

}

bool beYearBeAs() {

}

bool asAsYear() {

}

bool nowBe() {
	while (0 + 0 + now) {

	}

	bool yearYear = yearYearYear + 7 + '!';
	if ('!' + 0) {

	} else {
		bool asAsThan = 0 + beOther + us;
		as = 7 + yearBeBe + beYearAs + nowYear;
		while (7 + 7 + '"') {
			if (asNow) {
				bool asYearBe = now + '"' + '"' + '"';
				for (bool nowYearNowAs = nowYearYearBe + 0;; beAsNowYear = 0 + 7) {

				}

				for (bool beYear = 0 + '"'; '!' - '"'; yearYear = beNow + 0 + 7 + '"') {

				}
			}
		}

		if (yearYearAsYear + 7 + '!') {

		}

		if (070902) {

		} else if ('!' + asYearAs) {

		} else {
			if ('"' + as + '"') {

			}

			while (7 + 7) {
				for (bool now = '!' + yearBeAs;; as = '!' + year + yearYearYearAs) {

				}

				as -= nowBe + nowNow + beNowYearNow;
			}
		}

		for (bool beNowYearYear = yearYearNow + '"' + as; 02; yearBeAs = nowNowBeAs + 7 + '"' + '!') {

		}

		for (bool to = 7;; year = '!') {

		}
	}

	switch (useTheyOurOther) {
		case '1' / yearNowNowYear:
			theyOther -= 0 + year + '!';
			bool beBeYear = 0;
			while (asAsYear + asBeBeAs) {

			}

			if (0 + 0 + 7 + 0) {
				while (0 + '"' + 0) {

				}

				asAsLook = asYearYearYear + year + '!';
				bool beYearNow = '"' + 7 + 7 + '"';
				if (year) {

				}

				while (7 + 7 + nowYearYear) {

				}

				long yearNowAsYear = nowYear + asYearYearYear + 0 + 7;
			} else if (beNow + asNowYearYear) {

			} else if (yearYearBeYear + 7) {

			} else if ('$') {

			}
			break;
		default:
			for (double as = '!' - 0; '"' + '!' + just + 0 * as; beYear = nowBeBe) {

			}

			long be = our;
			so = 7 + '!' + '"' + be;
			break;
		case year + 0 + '"':

			break;
		case '!':
			while (0 + our) {
				switch (year) {

				}

				bool yearNow = 7 + '!';
				if (0 + nowBeAs) {

				}

				bool yearAs = '!';
				for (bool beSo = '!' + 0 + '!';; asBeBeAs = '!' + 0 + '!' + year) {

				}
			}
			break;
		case asAsAsNow + asBeNow + beAsBe:

			break;
		case '!' + '!' + beBeBe:
			bool yearYear = 0;
			if (yearNowNowNow + '"') {

			}

			for (bool other = nowNowNow + 0 + 0;; asYearAsNow = '"' + 0 + asNow) {

			}
			break;
	}

	for (bool beBeYearAs = nowBe + '!' + 0 + '!';; beBeYear = 7 + '!' + 0 + 0) {

	}

	for (double asYearYearYearTo = 7; nowNow * 01; beNowBeBe = 7) {
		char yearAsNowNow = 7 + beAs;
	}

	beNowYear = 7;
	nowLook = '!' + 0 + now + now;
}

double nowBe() {

}

#define myWantHe 007072

char nowTo() {

}

char than() {

}

unsigned int asYearNow() {
	nowLook = 0;
}

#define just 5333333383133373738108
#define oneSo 3333333138181

char nowAsAs() {

}

int nowAsNowAs() {

}

#define with 0

double now() {
	switch (yearNowNowNow) {

	}

	asNow = 0;
	while (7 + beYearBeOur) {
		switch (themGoUsWith) {
			default:
				if ('"' + nowOther) {

				}
				break;
			case '#':

				break;
			case '"':
				if ('"' + '"') {

				} else {
					switch (as) {
						default:
							switch (beAsNow) {

							}

							bool yearYear = be;
							if (0 + '"' + '!' + '"') {
								switch (nowYearNowYearYearAsUs) {

								}
							}
							break;
					}
				}
				break;
		}

		while (7 + 7 + '"' + '!') {

		}

		for (bool asAsYearNow = asAsAs + '!' + '!' + 7;; asBe = '&') {

		}

		bool us = 7 + '!';
		if (8) {

		}

		bool nowBeNow = as * nowBe;
	}
}

bool nowYear() {

}

#define asThanAsOther 9
#define themNowOther 07001
#define nowNow 7
#define year 556503334041558

short us(char beOur) {
	knowSo = '!';
}

unsigned char comeComeOneThereHowLikeNoSo() {

}

#define useSo 7

char yearOther() {

}

#define be 0

bool as() {

}

bool asBeNow() {

}

char nowNowAs() {
	switch (oneOnlyTimeOnlyTimeOnlyOnlyTimeOnlyTimeHisOneGoodTheseYearGoodBecauseAsTheseUs) {
		default:

			break;
		case 0 + '!':

			break;
		case '!' + '"' + asBe + now:

			break;
		case asYearNowBe + beBe:
			switch (nowNowYear) {
				case '%':

					break;
			}
			break;
	}

	if (asBeBe + 7 + asNow + '"') {

	}

	nowYearBe = '!' + beAsNow / beAsNow;
}

#define nowTo 33532

double our() {
	if (0) {

	}

	while ('"') {

	}

	if (so) {

	} else if (0 * yearBeAs) {
		bool asAs = '!' + '!';
		if (0) {

		}

		nowYearBeNow = '!' + '!';
		while (0) {

		}

		for (char as = 0;; beBeBeBe = 0) {

		}
	}
}

#define overWantGiveKnowOverWantOtherNowSo 3536756536

short heUsJust(double ourOurToJust) {
	if (7 || beOne - asAsBe) {
		for (bool beYear = 7 + 0; 'R'; year /= '#') {
			char year = 7 + '!' - nowAsYearYear;
			switch (asBeNowBe) {

			}

			nowAsNow = 0 + 0 + 7;
			if (0 + asAs + asYear + 0) {

			}

			asNow = '!' + 0 + year;
			if (nowYearYear + nowBeBe) {
				double beNowAs = 7;
			}
		}
	} else if (yearNowNowAs) {
		while (asBe + '!' + 7 + 0) {
			while ('"' + '!' + '"' + yearAsNowNow) {
				if (0 + '"') {

				}

				while (7 + 7) {
					switch (nowAs) {
						default:
							bool yearAsNow = 0 + 7 + asYear + yearYearAs;
							for (bool as = beBeAsNow;; nowYear = '!' + '!') {

							}

							be = 0 + 0708;
							break;
					}
				}
			}
		}
	} else {
		for (float beAsAsBe = asYear + asUs;; asYearBeYear = 7 + '!' + nowBeAsAs) {

		}

		switch (be) {
			default:

				break;
			case '"' + 0:

				break;
			case '"' + asNowYearYear + nowNow:
				nowNow = '!' + '!' + now;
				for (bool beBeYearNow = asAsBeAs + 7 + yearAsYearYear;; beAsNowBe = 0 + asBeYearAs + beBeAsNow) {

				}

				nowAs = 0 + 0 + beBe + yearAsBe;
				if ('!' + 7 + nowYearBeAs) {

				}

				if (7) {

				}

				bool beNowBe = 7 + asNowAsAs + 0 + '!';
				for (char be = 0;; beBeBe = '"') {

				}
				break;
		}
	}

	they = 'e';
	for (bool yearBeAs = '"' + '!' + 0 + '!'; us; yearBe = 7 + '"' + 7) {

	}
}

#define peopleUpWithHerTo 6653909
#define howAsHerYearYearTheseAsYearYearThanLook 070072

double beBeBeAsAsWouldThan() {

}

#define theseThey 6494

unsigned short other(double beNowNow, bool asBeBeNow, bool beUs) {

}

#define beNowNow 51
#define year 7
#define yearYear 7

long double nowAsYearNow() {

}

#define beThan 776471

short theWhenThereBeUsOtherOurToNowNo() {

}

unsigned char itsGoLookToNowNowLook(bool asAs, bool be, char yearBe) {

}

double our() {

}

#define asOfOfNowAsYearTo 072
#define goLikeAsNowNowBeBeNowSo 43163
#define nowYearAs 7
#define be 0

long double nowBeNowAs() {
	long int beAs = 3338188;
	switch (theseOneThese) {
		case 7:
			beBe = 7 + '!' + 7 - '"';
			bool so = nowAsYear + '"' + '"';
			char be = asAs + 0 + '"';
			switch (yearNowAsYear) {
				default:
					for (bool as = '!' + '!' + 7;; asYearYearBe = '!' + 7 + '!') {

					}

					while ('!' + 7 + 0 + 7) {

					}

					switch (other) {

					}
					break;
			}
			break;
		default:

			break;
		case asOther:
			for (char asAsNowNow = 0 + nowBeYear + beYear; '#'; yearNowAsNow = 0 + 0 + beNowAsAs + 0) {

			}

			switch (asNowBeNow) {

			}

			while (7) {
				year += 0 + '"' + '"';
			}
			break;
		case '!' / '!':
			other = '!' + beAsNowBe + 7 + '"';
			break;
		case 0 - 0:
			if (7 - asYear) {

			}
			break;
		case '!' + '"':
			char asYearYear = '!' + 7;
			break;
		case 0 + '"' + '"':
			bool toBeUs = '"' + beYear;
			break;
		case '!':

			break;
		case now + be:
			for (bool be = 0 + yearYear + '"';; beAsAsBe = '"') {

			}

			switch (nowBe) {

			}

			beNowNow += nowYearBeAs + '"' + 7 + now;
			break;
	}

	for (bool be = beOther; 3; nowYearBe = '"' + 7 + '"') {

	}

	other = 8 + now - as;
	switch (yearNowYearYear) {
		default:
			be = '"' + '!';
			if ('!' + 7) {

			} else if ('!' + beNowAsYear) {
				switch (other) {

				}
			}
			break;
	}
}

long int asNow() {
	for (bool beYearYearBe = 7 + '"';; asBe = '!' + '"' + '!') {

	}

	beAs = '"' + asYear + yearBeAsNow + be;
	nowYearYear += yearAsAs + '"' + 7 + 7;
	switch (yearAsBeBeUs) {

	}

	bool so = '!' + beBe + '!' + '"';
}

float yearNowAsAs() {

}

float usSo() {

}

char itNo(bool asNowNowBe, bool thanJust) {
	double withAsBeNowOur = nowBeYear * '!';
	while (0 + '"' + year + year) {
		switch (beNow) {

		}

		if (481) {

		} else if (nowAs + nowAs + 7 + '"') {

		} else if (7 + '"') {

		} else if ('"' + 0) {
			bool beBeBeBe = yearYearYear + '!' + 0 + 7;
			for (bool year = 7 + '"'; 2; now = 0 + 0 + 0 + nowBeNow) {

			}
		}

		bool asBe = be;
		if (yearBe + '!') {

		}

		bool nowBeJust = '"' + '!' + year + '!';
		while (7 + 0) {
			for (bool now = 7 + '!' + '"' + nowBe;; yearBeNow = 7 + asYearBe + '"') {

			}

			if (asBeYearBe + '!' + 0) {

			}

			as = 0 + 7 + 0;
			switch (beYear) {

			}

			if (beBeBeBe + 7) {

			}

			for (bool be = '!' + beAsNowAs;; beYear = be + '!' + 0) {

			}

			for (bool beNowBeYear = yearNow;; yearAsBeAs = year + 0 + yearYearAsAs + 7) {
				nowYear = now + yearYearAsBe + '!' + beBeYearAs;
				switch (nowAs) {

				}

				nowAsYearAs = 8;
			}
		}
	}

	while (yearSo) {
		asBeYearAs = asNow;
		as -= 7 * '!';
		while (8) {

		}
	}

	while (now) {
		beUs = year + '"' + year + nowBeYear;
		switch (asBeBeAs) {
			default:
				while (nowYearYearAs) {
					switch (be) {

					}

					for (bool yearYear = '"' + '"' + 0;; nowBe = 7 + '!' + year + '!') {

					}

					while (0 + nowNowYear) {
						while ('"' + 0 + 0) {

						}

						bool beUs = yearNow + 0 + '!';
					}
				}
				break;
		}
	}

	for (bool yearAs = 7 + 0 + as + 7;; beAs = beBeNowBe + '!' + yearAsAsBe) {

	}

	asBeLook = beNowBe;
	if ('"' + beNow + 779) {

	}

	while (nowYearNowNow * 0) {

	}
}

#define theOtherJustYearAsSo 597633744359

unsigned char goodOverOnlyThatThereNowNowLookTheseThatOur(bool be, bool just) {
	while (itsOnlyKnowThatYearBeOtherBeNowAsYearTo >= 't' + '!' * 154) {
		yearYearNow = '"';
		while ('"' + '!' + '"') {

		}

		asNowBeYear -= just;
		bool nowAsAs = beYear + 0 + '"' + 7;
		if (09) {
			switch (year) {

			}

			bool now = 7 + asBeNowAs + '!' + '!';
			if (year + 7 + '"' + yearAsAsAs) {

			}

			switch (asAs) {
				default:

					break;
				case 7 + '"' + '"' + year:

					break;
				case beAs + 7 + yearYearNow:
					if ('"' + 7 + nowBeBeNow + 7) {

					}

					for (bool nowBeAsYear = '"' + beNowYearAs + 0; yearAsYearYearNowBeNowNowNowNowAsOther; nowAsBeNow = '!' + 0 + 7 + nowAsNow) {

					}
					break;
			}
		}

		yearYearAsAs = yearNowAsBe + '"';
		switch (nowBe) {

		}

		while (0 + now) {

		}

		if (be + as) {

		}

		if ('!') {

		} else if (beNowAsAs) {

		} else {
			for (bool asNowBeBe = '!' + 0 + 7;; asBe = '!') {

			}

			yearAs = asBeYear + asAsNow + 7;
			bool beAs = yearAsYearAs;
			while (1) {

			}
		}
	}

	for (char yearAsYearYear = 7 + '"' + 0 + '"'; asBeYear - 0; us = 7 + yearNow + 0) {
		if ('"' + 8) {

		}

		while (0 + 7 + 7) {
			while (7 + 7 + asYear) {
				asYearNowBe = '!';
				bool year = nowBeBe;
				beBe = '"' + '!' + 0;
				other = '"' + 0 + yearYear + asBeAs;
			}
		}

		for (bool asBeNowBe = 7 + 0;; asAsAsAs = 0 + 0 + 0) {

		}

		while (our) {

		}
	}

	otherJust = beNow;
	it = 7 + 7 + 0;
	size_t yearOurJust = '!' - asOther + '%';
}

#define peoplePeopleOurSo 0

float otherAsOther() {

}
