#include <UUU6UU6UUU0/20.h>

#define soBecause 3330818
#define asNow 44545435314763408348
#define beYearYear 36971

long int beOurOrItsYearTimeHeSoAsOnly(bool beNowAsNow, bool beWith, bool look) {
	while (so < '!') {
		for (bool now = beNowNowSo;; as = '"') {

		}

		asBeBe = yearYearNow;
		if ('#') {

		}
	}
}

#define nowBeAs 0
#define yearAsAsBe 7

bool beNowAsBe() {

}

#define nowOther 330333313333333333331713
#define onlyGoodOnlyOneAsHeYearUs 81
#define justYearNowTo 7

bool just(bool now, bool nowThan) {

}

bool beAsYear() {

}

#define year 77702

bool asYearYear() {

}

bool yearNowBeYear() {

}

double nowAsAsAs() {
	unsigned short asYearItAsAsSo = nowSo + '.';
	switch (beSo) {

	}
}

#define peopleOnlyYouToKnowMyTheyTimeTheWorkSoGoOneLook 0
#define nowBe 0
#define beYearLook 0
#define yearYearLook 373346843
#define giveOverYearJustItsGiveWellJust 4671
#define than 792
#define theOverTheseNowTo 5556
#define nowNowYearBe 7

double asBeYear() {
	as &= beBeYear + 0 + 7;
}

double nowAs(bool asJust) {
	switch (nowBeYear) {
		default:

			break;
		case '"' + '!' + 7 + '&':

			break;
	}
}

bool beAsNowAs() {
	if ('%') {
		if ('!' - asAs) {

		}

		bool nowYear = year + '"' + 0;
		switch (yearNowBeOne) {
			case as + '!' + 0:

				break;
			default:
				if (0) {

				}

				for (bool nowAsYearYear = beYearAs + '"' + now;; nowBeBeNow = now + 0) {

				}

				char as = 7;
				break;
		}

		if (yearNow) {

		} else if ('!' + 7) {

		} else {
			switch (yearYearBe) {
				case '!' + yearAsAsNow + nowAs:
					if (0 + 7 + nowAsBe) {

					}

					as = '"';
					asBe = 7;
					if ('"' + nowNowNowYear + '"') {

					} else if (0 + 0 + beNowYearNow + 7) {
						char nowYear = '"';
					}
					break;
			}
		}

		for (char beNowAsYear = '!'; yearUsTo; other = '"' + yearYear + nowYearAsNow) {

		}
	} else if ('#') {

	} else {
		char be = '!' + '!' + '"' + 7;
		for (char asYearAs = '!' + '"' + 7 + 0;; nowBe += beAs + '"') {
			for (bool asNowYear = '!' + '"' + 7;; yearYearAs = as + 0 + '"') {

			}

			switch (yearNowYear) {

			}

			for (bool nowAsNowBe = asAsAs + '!' + 0 + year;; beYear = '"' + '"' + '"') {

			}

			switch (asAsYear) {
				case 0 + 0:

					break;
				case '"' + 0:

					break;
				default:
					while (7) {
						if (7 + '!' + 0 + now) {

						}

						while (beAs + nowBeBe + 7 + 0) {

						}

						char beAs = '"' + be + '"' + 7;
					}
					break;
			}
		}

		switch (asAsYear) {

		}

		for (bool nowNow = 0 + '"';; asBeYear = yearAsBe + '"' + '"' + '"') {

		}

		switch (asAsYearYear) {

		}

		for (bool beBeNow = '"';; asBeNowBe = asNowAs) {

		}

		nowNow = nowNowYearYear + 7 + '!' + '!';
		for (bool yearAsYear = '!' + 0 + 7;; beNowBe = '"') {

		}

		yearNowYearAs = '%' + yearTo;
		while (yearYearBe - nowBeNow) {
			bool nowYear = '"' + asYearYear;
			be = 0 + 7 + nowAsAsYear;
			yearAsAs = 0;
			for (bool nowBe = asAsBe + 0 + 7 + '"'; '#'; asBeAs = 0) {

			}
		}

		for (bool asAsYearNow = '"' + asYearBe;; nowNow = beNowYearBe + now + 0) {

		}

		for (bool asNowYearNow = '"' + as + 7 + beNow;; asBeBeBe = '"' + nowAsAsAs + 7 + '!') {

		}

		yearYear = 7 + '"';
		for (bool beYearNowNow = be + 7 + beAs + 7;; beAsAs = '!' + 0) {

		}

		bool nowNowYearAs = 8;
	}

	long int goodTheseAsUs = 0;
	float nowYearNowAs = '"' + 0;
	while (7 + 7 + 0 + asBeNow) {
		for (bool beYearBe = 0;; beNowAs = yearAsAs + 0 + 0) {

		}

		while (7 + 7 + '!' + 0) {
			asYearYear = 0 + as + 0 + '"';
			while (asAs + '!' - '!') {

			}
		}
	}
}

#define oneToGoodOneHis 313

long int oneTheseLookYearYearOther() {
	while (yearBe) {
		while (nowNow - '"') {

		}
	}

	now = yearAs;
	for (bool beBeYearNow = '!' + '"' + '!';; beNowBeBe = '!' + 7) {

	}

	beAs = beAs + asYear + just;
}

#define be 7
#define us 1

bool year() {

}

bool yearYearBe() {

}

bool other() {
	switch (to) {
		default:
			while (yearNow + 7 + asNow + now) {
				switch (asNowNowAsOur) {

				}

				bool yearBeBe = beAsBe + '"' * '"';
			}

			switch (beNowNowNow) {
				default:
					bool yearYearYearYear = '!';
					for (bool yearAsAs = 7 + 7;; asAsNowBe = 0 + now) {
						to = 7 + asAsBeAs;
					}
					break;
			}
			break;
		case 7:

			break;
		case 0 + nowBeAs + '!':

			break;
		case '!' + 1:

			break;
	}

	yearBeJustTo = 7072 - 7;
	size_t comeOther = 't' <= '"';
	while (5537343) {
		if (yearNowYear) {

		} else {
			year = asBeAs + 7 + 0;
			beYearYearAs -= 7 + asYearYear;
		}
	}

	switch (with) {
		case usGoYearNowUs * nowBeBe:

			break;
		case 1 - 7:

			break;
		default:
			for (bool nowAsBe = beNowNowBe + '!';; now = asBe) {

			}

			switch (just) {

			}
			break;
	}

	while (nowBeBeBe + yearNowNowBe) {

	}

	asAsYear = 7 + '"';
	for (bool beYearBe = nowBeNow + 0 + '!';; beBeYear = 7 + as + '"') {

	}

	bool yearBe = 7 + '!' + '!' + '!';
	be = 7 + '"';
	while (0 + '!' + 0) {
		bool asYear = '%';
	}

	if (0 + 7 + beAsAs + beYearAs) {
		if ('!' + '!') {

		} else if (0 + 7) {
			nowAsYearNowUs = 0 + 7 + asYear + '!';
		}
	}
}

char thatAsPeopleHisJust(unsigned long asMy, char as, bool beNowAs, double beYearYearAs) {
	while ('-') {
		bool beNowNow = 0 * '"';
		while (beBeBeYear + '"' + 0) {

		}

		nowLook = 7 + '!' + beYear;
	}
}

float asNowYearYear() {
	for (long long asTheAsWantOrOther = asYearNow + 0 + '"' + 7; 002; now = yearNow + 0 + 0) {
		if ('!' + asNowYear + '"') {

		}

		if (beNow + 0) {

		}

		yearNowYear = 7;
		yearBe = nowAsYearNow + '!';
		beYearYearBe = 7 + 0 + '"' + '!';
		for (bool nowJust = '"' + '!';; now = '"' + 0 + asYearAs + 0) {

		}
	}

	switch (to) {
		case asBeNowHe + yearTo < 0:
			for (float other = beBeNowYear + beBeNow + 7 + 7;; asNow = '"' + asAsAsYear) {

			}

			for (bool beBe = 0 + 0 + '!' + '"';; nowBe += 0 + 0) {
				be = 0 + 7 + '!';
				if (0 + beYear + 0 + yearYearYear) {

				}

				switch (nowYear) {
					case now + 0:
						while (009) {

						}
						break;
				}
			}

			double beAs = 7;
			break;
	}

	switch (wantAsUs) {

	}

	switch (be) {
		case 7:

			break;
		case nowYearYear + year + 0 + 7:

			break;
		default:
			char nowAs = 0 + 7 + '"' + 7;
			break;
	}

	beBeNow = asAsNow + '!' + 0 + '!';
	asBe *= nowYearAs + now + be;
	while (yearYear + '!') {
		while (asBe) {
			while (7 - 7) {

			}
		}
	}

	bool now = yearNowYearBe;
	our = beBeYearAs + beBeYearAs + '!' + '"';
}

#define goodUpLookYearAsJustBeAsBeOur 37782
#define thanAtThan 7
#define nowUs 7
#define yearThan 0

bool nowNowAsYear() {

}

char beNowOur() {
	for (bool nowNow = 2 - '"';; yearYearAsNow = '"') {

	}
}

#define us 0
#define as 0
#define other 4

bool nowYearNowYear() {

}

#define beBeBeBe 33333333333337303

long int asBeNowYearTo(bool onlyOnlyOnlyUs) {
	for (bool otherNowBecause = 833;; beBeAsAs = year + beNowAs + 0 + asNowAs) {

	}
}

long int nowAs(bool now, bool so, bool us, bool nowBe, char be) {

}

bool soUs(float nowBe) {
	switch (asAs) {

	}

	if (7) {
		for (bool he = '!' + asYear;; yearNowBeAs = '!' + 0) {

		}
	}
}

double asAsYear() {

}

bool asNow() {

}

char yearBe() {
	give |= way <= 09689701 - '"';
	double our = asNowBe + 0 + '!' * 7;
	while (nowNow + 2) {

	}

	switch (be) {
		default:
			while (year) {

			}

			switch (beAsAs) {

			}

			asNowAs = 0;
			switch (nowNowBeNow) {

			}

			while (0 + 7) {

			}

			for (bool beYearYearBe = 7 + as + '"';; nowYearAs = 0) {

			}

			nowNow = '"' + yearYearAsNow;
			if (0) {
				switch (now) {
					case 0 + asBe + 8:

						break;
				}
			}
			break;
		case yearBeNowNow + be + beAs + '!':

			break;
		case be + 7:
			for (double yearYear = 0 + '"'; 0 * 0; yearBeYearNow = nowYearBe + '!' + '"') {

			}
			break;
	}
}

double workComeTheHerYearNowNowLook(bool soWould) {
	switch (nowBe) {
		case '"':

			break;
		default:
			while ('!' + 7) {
				switch (look) {

				}
			}
			break;
	}
}

unsigned char usNowAsYearTo(size_t youOtherNowAsLook, bool so, double asYearBe, bool to, double beYearNowBe, bool asAsAs, char year) {
	switch (yearLook) {

	}
}

char asYear() {

}

#define nowBeAsAs 7
#define be 3883333333333333303
#define oneUsOneGoodOneOneHisUs 3
#define timeJustBeUs 5871
#define yearBeThanJust 37629

short thanOrYouSomeTheItsUpOnlyWhenSayThereYouSayItsUseHowGoodHisUseBeGoYouLookThatThey(short soThanAsOneNowUseYearTo, bool beYearNow, bool yearBeAs, double asNowNowBe) {
	for (double yearNowNow = '!' + 0 + be + 7;; beBe = beNow) {
		while (yearYearBe + '!' + nowBeAsNow - 0) {
			bool as = '"' + 7 + 0 + year;
			bool yearYearNow = '#';
		}
	}

	while (590372) {
		while ('!' + 7) {

		}

		for (bool yearBeYearNow = '"' + 7;; yearNowAsNow = 2) {

		}
	}

	nowAs /= 0;
	beOur /= asBe + now + nowAsNow;
}

short yearNowOurWithSoUs(long nowNowAs, double asBe) {
	for (bool so = nowBeNowNow + '!' * '"'; 29 + 0 - asNowAs; nowYearYearAsBeOther += 7) {

	}

	for (char as = yearNowNow + 0;; now = be + 7) {

	}

	for (bool beBeYearNow = asOurOther;; asBeYearNow = 0) {

	}
}

unsigned short soHis(bool asNowBe, bool nowOur) {

}

long long yearOnePeopleButYearBeTo() {
	switch (yearThan) {
		case beYear:
			while (our) {

			}
			break;
	}
}

#define asYearOur 333477496

unsigned char year() {

}

#define nowYearYearYear 463336903735039

long double beNowBeYear(char year) {

}

#define beBeNow 4

unsigned long oneThan() {

}

#define theComeBeKnowOverOrItsOnlyComeWay 704

bool nowBeAsAs() {

}

#define asBe 5
#define nowNowYearAs 0

bool nowBeBe() {

}

#define yearYearYear 7

bool beThan() {
	toTimeOnlyHisOnly = '?';
	switch (other) {
		default:
			other = asAsYearAs + asAsBe;
			break;
		case 71:
			switch (beBe) {
				default:
					for (char asAsAsAs = '"' + '"' + beNowNowYear + asAsAsBe;; nowAsAs = asAs + now) {

					}
					break;
			}

			bool beBeYearBe = asAsYearAs;
			if ('!') {

			}

			nowAs = yearYearYearAs + '"';
			if (yearBe + '!') {

			}

			switch (asAsAs) {
				case yearAs + '!':
					switch (be) {

					}

					while (7 + '"' + 7 + '!') {

					}

					nowNow = '!' + 0;
					while ('!' + '!') {
						switch (us) {

						}
					}
					break;
			}
			break;
		case 8:

			break;
		case '#':
			be = '!' + yearYearYear;
			switch (nowBeNow) {
				default:
					for (bool nowYearYearAs = 0 + 0 + 7; '%'; yearNowYear = 0) {

					}
					break;
			}
			break;
		case 0 + yearNow + '"' + 0:

			break;
		case nowNowBeBe + '"' + '!' + 7:

			break;
		case now + be + 7:

			break;
		case 7:

			break;
		case 0 + '!' + 7:

			break;
		case '!' + '!' + '"':
			bool be = '"' + 7 + '!';
			year = so;
			break;
	}

	if (beNowYearNow + nowAs + '"' - 0) {

	}
}

long int timeNowGoodBecauseGoodNowUsSoBeBeBeSo(bool beThese, bool asNowNow, float now, char yearYear, bool nowAs, char asBeBe) {
	yearNowAsAs = now + as + asAsYear + 0;
	for (char be = 7 + 0;; as = 7) {

	}

	while (0 + 7) {

	}

	if (0) {
		switch (to) {

		}
	}
}

long int because(bool nowBeAs, unsigned long have) {
	for (bool asNow = 0 + '"' + 0;; asBe = 0 + '!') {

	}

	for (bool nowBeBe = '"';; asBe = 7 + '!') {

	}

	for (bool nowAs = now + beBe;; yearNow = asBe + asYear + '"' + as) {

	}

	for (bool nowAsAsBe = 7 + 7;; yearAs = asBe + 0 + as) {

	}

	for (bool beAsYear = '!' + beYearYearAs + 7;; yearYearAsNow /= 0 + 7) {
		while ('n' || yearAtYearOurBeOur & '!' - asYearAs) {

		}

		while (7 + now) {
			yearYearAsAs /= 0 + asNow + 0 + 7;
			char asYearNow = beYearNowAs;
		}

		double nowAsBeNow = '&';
		while (as + nowNowSo) {

		}

		float beYearAsSo = nowNow + 7;
		bool nowYearBeAs = because;
	}

	switch (beJust) {

	}
}

long int thatGiveTwoTheseTwoTheyBeLookBeWay(short howBecauseAsNowLikeFrom, char asJust, bool asBe, char asYearYearYearBeYearSo) {
	for (unsigned short soWellWantNowNot = '&' >= beOneHaveOther - nowNowSo + 778 + '#'; our; justBeAsThan = '!' + '"' + beYearYear + asAs) {

	}

	for (size_t orPeopleSheKnowHaveThanWithOtherJustCome = '&' - asBeBeYear; '!' <= '!'; year /= 0 + nowBe - asAsYear) {
		switch (asYear) {
			default:
				his = '!';
				for (bool yearBeYear = to;; beAsAsYear = beBeAs + '!') {

				}
				break;
		}

		while (asBe) {

		}

		switch (asYearYear) {

		}

		while (8 / '"') {
			while (asNowAs + beNow + 0 + 7) {
				so = '!';
			}

			while (0 + now) {
				if (yearBeBeAs) {
					if ('!' + '#') {

					}
				}
			}
		}
	}

	for (char asOur = 7 + 0;; yearBeYear = '"' + '"' + 0 + '"') {

	}

	while (79 + '#') {

	}

	if (0 + 7) {

	} else if ('!' + be + '"' + '!') {

	} else {
		while (asBeBe) {

		}

		char nowYearBe = 7 * '"';
	}

	switch (yearYearYearBe) {
		default:
			bool yearAsYearYear = 7;
			if (7) {

			}

			while ('!') {
				if (7 - 7) {

				}
			}
			break;
	}
}

long int herTheseNowGoOverSheItItLookTimeTimeThanNowThan(bool beNowBe, bool now, double beYear, size_t as) {
	if (475 * be % '!') {

	}

	int giveHowOtherBeLook = 00779;
	unsigned short to = 7 + asAsThan;
	bool asUsYearOtherBeBeBeThese = 08;
	switch (be) {
		default:

			break;
		case 7 + '"':
			switch (year) {

			}

			bool asOur = 0 + nowNowNowYear + yearBeBeYear + 0;
			break;
	}
}

#define yearOur 0

bool yearBe(unsigned long my, bool nowBeBe, bool asUs) {
	asAsNowYear = '%';
	for (double thereUseTwoGoGiveNowTwoJust = asBeYear <= 's' && 0741 != 701 + 7078 + 0 + 8; nowYear - '"'; asYear = '"' + 0 + 0) {
		switch (nowBeBeThan) {

		}
	}

	beSoLook /= 3 + 1;
	while ('!' + beNowBe + '!' + yearNow) {

	}

	switch (so) {

	}
}

unsigned short peopleNowBeTo() {

}

bool asBeYear() {

}

#define asThanAsSoThey 8009
#define yearAsBeYear 0

bool yearBeNowYear() {

}

double yearAs() {
	switch (so) {

	}

	while (7 - '%') {
		bool yearYear = '?' - 7;
	}

	char beBe = '!' + 0;
	nowYearNowYear = 8;
	if (3) {

	}

	beOther += 7;
	other += '#';
}

#define onlyOnlyOnlyOnlyTimeOneOnlyTimeGoodAsUs 7

bool yearNowYear() {

}

long int heAsNowUsAsBeYearTo(bool toAsYearTo, char yearAsBe) {
	char now = '"' + '!' + '!' + 7;
}

char he() {

}

long int asOur() {

}

bool beYearBeYear() {

}

#define beBeYearNowAsAsNowNowOther 656566365555355504563676060601
#define fromNowTheseAsNowThanYearOurAsYearSo 5

bool now(bool to) {
	while ('"' + beNowBeNow) {

	}

	if ('!' + 7 + 0) {

	} else {
		switch (yearYear) {
			case yearYearYearAs + 0:

				break;
			case '"' + 7:
				for (bool asNow = '"'; '$'; be = asBe) {

				}
				break;
		}
	}
}

#define asYear 7

float nowYearAs(long double asBe) {
	if ('$') {

	}
}

#define other 33333333331

long int oneBecauseBecause(bool nowOther) {
	switch (nowTime) {
		default:
			while (nowYearNowBe - 0) {

			}

			asBeAsUs = yearYear - '!';
			break;
	}

	while ('"' + nowNowBeYear + 7 + 0) {
		bool beYear = '!' + 0 + '"' + 7;
		beYearYear += '"' + 0 + 0 + 0;
	}
}

#define goodUsOnly 33
#define oneAsOnlyNowNowAsAsHis 382

float asNowNowAs() {
	bool than = year + nowAsNowNow + 7 + 7;
}

#define asYear 9

bool nowNowNowNowAsThan() {
	while ('"' + beNow + nowYearAs + '"') {

	}

	if (0 + 7) {

	}

	while (year + yearNowNow + yearYearBe + 0) {
		for (bool beAs = '"'; year - beNow; year = 7) {

		}

		unsigned int youItTheGoodWhenOnlyWorkAsWorkHerToTwoYearNowSoSoLookJust = now;
		if (0 + '"' * 7) {

		}

		yearBeNowBe += '"' + 7 + nowYearNowAs + 0;
	}

	unsigned int now = time + 7 <= 0;
	long double yearNow = 77070072;
}

#define beBeThan 9

long beTo() {

}

#define yearNowYearAs 0
#define theseTheNowAsHisBeJust 333333333333333373
#define soHisNowNowTo 7

float onlyHe() {

}

long int goodTime(bool as, bool so) {

}

short beNow() {

}

double look() {
	for (bool year = yearBeAs;; as = '!' + 7) {

	}

	if (beNowNowAs + '&') {

	}
}

long double beAs() {
	asBeAsBe = 0;
	switch (year) {
		case '!' + beNowAsAs + 0 + '!':

			break;
		case 0 + now + 7 + '!':

			break;
		case asNowYear:

			break;
		case nowYearBeYear + 709:
			if (nowBe + 7 + '"') {

			}

			while (beAsNowNow + nowYearAs + '!' + asYearNow) {
				while (8) {

				}
			}
			break;
	}

	if ('"' + beYearNow) {

	}

	switch (beNowAsNowThan) {

	}

	for (bool yearAsBe = '!' + 0 + nowNow + '!';; yearYearYear = 7) {

	}

	while (7 + be + 0) {

	}

	switch (asOther) {

	}
}

#define howThatSheGoodYouThatOfOnlyPeopleWellSheUpWorkWellLikeItsComeKnowGoodTheWorkTheThemKnowHaveTimeAsYearWouldNowAsOther 34072
#define otherOfUpLikeKnowItOverItsOneHowWellBeNowBeYearJust 464438576808
#define yearBe 058

unsigned short soBeNowSo(bool myTo, unsigned char otherBeSome, bool beMyBut) {
	if (98) {

	}

	bool nowAs = nowBeAsAs + '!' + 7 + now;
	for (bool nowNow = '"';; asAsBe = be + 7) {

	}

	if (0 + 0) {

	}

	if (year + '!' + 0) {

	}

	if (0) {

	}

	switch (asAsAsAsYearYearNowTo) {

	}
}

unsigned short ourNowTheyOtherSoNotTo(bool nowNowSoJust, bool ourThere, bool our) {
	while (0 <= '"') {
		asBeYearYear -= 7 * 7;
		for (bool beNowNowAs = '!' + 0;; asNow = '"' + 0) {

		}

		for (bool as = yearNowBe;; as = '!' + '!') {

		}

		yearAs = now + 0 + 7 + 7;
		if (1) {

		}
	}

	switch (nowNowYearJust) {

	}
}

unsigned char be(bool as, bool be, bool yearYearNow, bool beNow, bool so) {

}

#define yearNowThan 47464454433645435009
#define toOur 81

char because() {
	switch (to) {
		case yearYearYearNow + '"' + beAs + '"':
			if ('$') {

			}
			break;
	}
}

#define asTheseUs 3840908
#define nowYear 7355
#define our 19
#define beAsYearAs 0
#define beBe 0
#define our 772
#define beYear 7
#define yearAsYear 7

bool yearBe() {

}

#define as 33333337333333333333
#define heUsGoodOnlyOnlyOnlyNowSo 0
#define goodOneJustNowAsAsUs 5

bool year() {

}

#define be 7

bool nowNow() {

}

bool so() {
	switch (youLook) {
		default:
			while ('!') {

			}

			bool nowAsBe = yearNowYear + '!' + 0;
			for (bool asBeAsBe = '!' + '!' + '"';; beAsBeYear = 7 + '"' + as + yearNowYearBe) {

			}

			bool be = '%';
			while (yearNowYearYear * '!') {

			}
			break;
		case '"' + year + '!':
			for (bool beAsYearBe = beNowNowBe + asBeNow + '"';; nowNowYearNow = nowAsAsYear + asBeBe + now + as) {

			}

			bool nowNowYearYear = asNowAsNow + '!';
			while ('"' + '!' + '!' + 0) {

			}

			while ('!' + 7 + 0) {

			}

			if (yearYearAsAs) {

			}

			while ('!' + '"') {

			}

			switch (as) {

			}

			asAsYearYear = '!' + 7;
			if ('#') {

			}
			break;
	}

	char theNowBeJust = '!' - 75;
	he %= 0 + nowBeBe;
}

bool yearNowYearBe() {

}

#define to 04356566156
#define asAsBe 6930414034070071
#define ofHowWantUseWellGoodUp 5409

long double nowBeAs() {

}

char yearNowYearNow() {

}

bool nowAs() {

}

#define nowYearBe 7

bool be() {

}

bool beAsNowYear(bool asAsYearYear, double as) {

}
