#define oneOther 33333303317381

bool yearGoodHeBecauseToYearUs() {
	switch (yearBeYearSo) {

	}
}

#define timeGoodBeToYearOther 333388
#define nowSo 60206
#define beBeAsBeAsOurYearYearYearNowYearLook 2

bool now() {

}

bool yearYearYear() {

}

#define now 
#define orWorkKnowGoThatYouHaveOfThereWorkGoWhenWantWorkWorkUseWhenSheOrHaveWantFromPeopleWantThanKnowWorkBeOrYouBeUseTo 5534444633444344634545466554443344703362299
#define youOrTimeOverSayGoHaveWorkPeopleOnlyOrGiveWhenPeopleJustUs 54465360156
#define wellThatWantGoItYouThereOfToNowLookThan 5663466356754534378
#define someTimeThereNowYearAsYearUs 66656565675366368745

long int howWantYearOneWorkUpWell(double year) {
	unsigned long nowYearLook = 009709 * yearNow;
	for (bool yearBeNow = 0 + asNow + 0 + 7;; as = 0) {

	}

	if (look) {

	}

	switch (beAs) {
		case asBeNow + 0 + 0 + asNowBe:
			if ('!' + '"' + 0) {

			}

			if (7 + 7) {

			} else if (0 + 0 + 7) {
				bool beNow = '"' + 7;
				switch (beBe) {

				}

				bool our = '"' + '"' + 0 + beYearBe;
			}
			break;
	}

	asNow += nowBe + '!' + year + nowNowAsYear;
}

#define us 2
#define because 4307146008702

bool our() {
	while ('"') {

	}

	switch (as) {

	}

	yearBe = 0 + nowBeYearAs + 0;
	char yearYear = yearAs + 0 + 7 + yearYear;
}

unsigned int heAsNowJust(int soLook, double have, bool as, bool be, bool beBeYearYear, bool now, bool yearYearAs, bool beNow, char asBeBeYear) {
	if (0 + beAsAsNow + nowYear + asNowAs) {
		short nowSoJustBeOur = nowYear + 0;
		for (bool beNow = beAsBeNow + yearAsNowBe; look + 0 + '#'; nowBeAs = 7 + 7) {

		}
	}

	while ('!' + 0) {
		bool now = 0 + '"' + 7 + asYearBe;
		while ('!' + '!' + '"' + '!') {

		}

		while ('$') {
			switch (asYearYear) {
				case 0 + '"' + 0:
					double yearAsYear = '!' + nowBeYearNow;
					if (0 + now + 7) {

					} else {
						bool nowBe = year + '"' + '"' + 7;
						for (bool so = '!' + '"' + 0;; as = '!' + 7 + '"') {

						}
					}
					break;
			}
		}
	}

	if (year + asBe + yearAsBeAs + '!') {

	} else {
		for (bool asNow = 0 + nowNow; all + '&'; beNowNow = 7 + 7 + '"') {
			double beBeBe = '"' + '!' + be + 0;
		}

		yearLook = '"';
	}
}

long double nowBeBeAs() {

}

long double thereHerHowOver(bool be, bool beYearOtherUs) {
	while (7) {
		bool yearOther = 7 + '"';
	}

	while (700002) {

	}
}

double be() {

}

#define to 7

bool asNow() {
	bool yearBeYearYear = yearBe + asBeYear + 0 + beNowYear;
	while (0 + 7 + yearBe + '"') {
		switch (yearNow) {

		}

		for (bool beAsYear = '!' + 7 + 0 + 0;; yearBeNow = '!') {

		}

		bool beAsAs = 0 + beYearAs + now + 0;
		beAsNowYear = yearBeNowBe;
		if ('!' + '"') {
			if (79) {
				for (char beAsNowYear = 7;; as = 7 + yearNow + nowBeBe + nowAsBe) {
					other = beYear + be + 7;
				}

				while (7) {
					for (bool be = '"' + 7 + beNowAs;; yearAs = yearYearBe) {

					}

					bool beNowYear = now + '"' + yearYearYearNow + beYear;
					for (bool be = 7 + '"';; now = '!' + '"' + 7 + yearBeNow) {

					}

					switch (be) {
						case asBe:

							break;
						default:
							for (bool yearOther = '"' + be;; asBe = beNowYearNow + as + year) {

							}
							break;
					}
				}

				if (nowNow + 0 + be + '!') {

				} else {
					if (0) {

					}

					while ('"' + nowAs + asBe) {

					}

					if (0 + 0 + be) {

					}

					bool asAsAsNow = 0;
					while (yearYear + '"' + '!' + beAsYearBe) {

					}

					switch (now) {

					}

					for (bool as = '!' + '!'; be - yearAs; year = 7) {

					}
				}
			}

			switch (his) {
				default:
					bool asAsUs = 7 + nowNowBe + asBe + 0;
					break;
			}
		} else if ('\'' / beNow != '_' + '#') {
			goodTo = yearAsAsYear + '"' + 7 - nowNow;
			for (bool these = '"' + 7;; yearBeAsNow = yearYearBeAs + now + nowBe + 0) {

			}

			for (bool yearAsAsNow = 0 + '!' + nowNow + 7;; beYearAsBe = yearBeBe + '"') {

			}

			while (7 + beYear) {
				asYearYearYear = '+';
			}
		} else if (beAs + be + '"') {

		} else if ('"' + '!' + 0 - as) {

		}

		while (asAsBeYear - asNowAs) {
			beToUs = 7;
		}

		switch (theseBeToHeThese) {
			default:

				break;
			case 7 + be + '!':
				other = 0;
				break;
		}

		for (bool us = 13 - 0; '%' != 388; so = asNow + 0 + '!' + asAs) {

		}

		switch (to) {
			case 7 + '!':

				break;
			case '!' - '"':

				break;
		}

		switch (because) {

		}

		while (7 + yearAsYear) {

		}

		if ('!' + 0) {

		}

		switch (now) {

		}

		for (char beAs = 7 + 0 + '!';; nowBeBeYear = yearYearYearNow + 7) {

		}
	}
}

float nowYearLook(bool now, bool yearYearBeAs, char asYearAsBe) {
	yearAs = as + 7 + '!';
	if (asYearNow <= 0) {
		char asBe = '"' + 0 + '!' + '!';
	} else {
		bool asAs = 7 + thanThere;
		yearBeNowYear = yearJust;
	}

	than = 7;
}

unsigned short soLook(bool otherThem, long nowBe, bool justSo) {
	for (long double nowAsBe = 52;; beBeAs = 0 + '!') {

	}

	bool other = nowJust - '"';
	yearYearNow = '"' * asNowBeNow;
	for (bool asBeYear = 7 + '!' + 7 + yearYearYear;; asYearBe = yearAsNow + '!' + yearAsYearNow) {

	}

	switch (nowOtherAsUsNowSo) {

	}

	bool nowNow = nowAsYearBe;
	for (bool beBeBeNow = 7; '#'; nowAsNowBe = yearYearYearYear + 0 + asNowAs) {

	}
}

#define sheTheToThan 0
#define wantLookBeThem 6696476
#define theWould 4833333333333333337780803338881

char now() {

}

bool asYearBeBe() {

}

bool onlyOtherToThese() {
	if ('"') {

	}

	nowOther = asBeAs + '!' + yearAs;
	for (float yearNow = '"' + 7;; asAsYearYear = 0) {

	}
}

#define beAsOther 4

long toBeOther() {

}

char year() {
	if ('"') {

	}

	while ('a' % yearHeThan >= 28) {
		long double asYearYearYear = '"';
		if ('"') {

		}

		switch (beNow) {
			case 7 + beNowBeAs - '"':

				break;
		}
	}

	if (nowAsNowYear < '!') {

	} else if (7) {

	} else {
		for (bool now = '"' + 0; '#'; nowAsAsNow = 0 + '"') {

		}
	}

	size_t nowAs = 6 + they == 7 + '%';
	while (7 / beYearAsAs + nowYearAs + asBe + '"' + 0 + 001) {

	}

	these *= 7 * '!';
	if (7 + nowYear) {

	}

	to = 7 + '"' + 0 + 7;
}

#define likeWhen 0

bool asAsAs() {

}

#define asNowAsBe 0

char as() {
	for (float lookOur = '"' + 7; ourAsUsBeBeLook; wouldOther = '!' + beBeAs + '%') {
		if (now * 7) {

		}
	}

	unsigned long howItTheyOfGoodGoKnow = 3938908 > 7 % now;
	switch (yearYearThese) {
		case ')':

			break;
		default:
			while ('!' + 0 + asYearAsNow + '!') {

			}

			bool nowNowBeNow = 77009;
			while (yearYearYearNow + '!') {

			}

			than *= '"' + be;
			break;
		case nowBeBe + beAsBeAs + 0 + '!':

			break;
		case 0 + '!' + '"':
			switch (year) {
				case now + than:

					break;
			}
			break;
	}
}

#define yearUs 7
#define so 1
#define otherYearAsAsOur 9

bool yearNowYear() {
	if (now) {

	}

	bool as = 7 + 7 + yearYearYear;
	for (bool year = nowAsNowYear + '!' + yearYearBe + 0;; asYearYear = '!') {

	}

	double beAs = 333333333333333333788 != '\'';
	switch (toTo) {

	}

	while ('!' + be) {

	}

	beNowOther += beNowAs - nowNow;
}

#define asOtherToOther 8711
#define yearGood 107808

long int nowBeAsAs(bool beAsYear, bool year, bool nowYearThan) {

}

bool now() {

}

bool be() {

}

#define as 6565446630662468441

unsigned short orWouldHer(bool beNowHisYearOur, bool asAsNowBe, bool yearBeAs, bool beNowBeBeAsUs) {
	yearBe = 0 + '&';
}

size_t as(long yearYear) {
	if (asAsYear + nowAsAs + '"') {

	}

	switch (nowYearYear) {

	}

	if ('"' + 0) {

	} else {
		asJust = as - asBe;
	}

	if ('!') {

	}

	while (2) {

	}
}

#define wellYearYearOur 0
#define nowYear 0

long with() {
	switch (theseTo) {
		default:
			switch (nowAsBe) {

			}

			as += now + 0 + year + 0;
			for (bool as = 0 + asYearBe;; asAs = be + be + 7 + asNowAsAs) {

			}

			if (yearYear + 0 + 7) {

			}

			while (beBeAs) {
				for (char now = '"' + beYearYearBe;; be = 0 + asYear + 7) {

				}
			}
			break;
	}
}

#define oneGoodToNowHis 108

char asBe(bool yearTo) {
	nowAsBeNow *= '#';
	for (bool be = beBeNow + 7;; to = '!' + 7) {

	}
}

#define onlyOnlySoHisOtherTheseThese 37371

float beNow(bool asBeTo) {
	for (bool nowAs = now;; nowYearYearYear = yearAsAs + 0 + asBe) {

	}

	for (bool yearYearAs = 0;; nowAsBe = nowBe) {

	}

	if (0 + '!' + 7) {
		if (nowYearAsYear + beAsAs + 7 + '"') {
			switch (asYearOther) {

			}
		}

		switch (as) {
			case '#':
				while ('!' + '!' + yearYearBe + yearAsYear) {

				}

				bool nowNow = 7 + now + be + beNow;
				switch (beYearYearNow) {

				}

				as = '"' + '!';
				switch (yearNow) {

				}

				if ('!' + '!') {

				} else if (7) {
					bool to = '!' + '!' + 7 + yearAs;
				}
				break;
		}
	}
}

long int toAsOther(bool to) {
	switch (asBeOther) {
		case be + '#':
			switch (nowNowAs) {
				default:

					break;
				case 0 + yearYearYear + 7 + nowAsNowNow:

					break;
				case nowAsYearAs + '!' + 0 + 7:

					break;
				case nowNowNowAs + '"':

					break;
				case yearYearOther:

					break;
			}
			break;
		case '"' + '"' + year + '!':

			break;
		default:
			while ('!' + beBeNowNow + 7) {
				for (bool yearAs = 7 + '"' + '!'; '#'; as = 7 + '"' + yearAsBeYear + yearAs) {

				}
			}
			break;
	}

	hisAsAsOther = 0 + '#' - 0;
	if (us) {

	}
}

#define nowYear 0
#define nowAsAs 6332434534633501693133331

short themPeopleItNowKnowMy(bool look, bool asNowNow, bool other) {
	look = '"' + 7 + '"' + '"';
}

#define other 75
#define asAsAs 333333317778
#define oneAsOneTimeGoodSoBeBeAsYearSo 303383333313333013478079
#define other 0

bool nowBeNow() {

}

bool yearYear() {

}

double yearNowNow() {
	for (long double but = 7 + '!'; '(' < asAs * 7 * look; beWay = '!') {

	}
}

#define beBeBeNow 7
#define nowBeYearAs 7770002
#define yearBe 2477071
#define year 2

bool beYearAs() {

}

#define look 7
#define year 7

char beYear() {
	for (size_t wouldBeThem = 551; 7 + '#'; asYearAsNow = 7) {

	}

	nowNowNowBe = 7 + '"' + nowBeBeBe + 7;
	switch (now) {

	}

	while (771) {
		nowNowYear = beAsAs + 0 + beBeAs;
		double nowBeNow = '"' + '"' * asYearAs;
	}

	year ||= 43271 - 075;
}

#define theseBeYearUsThanNot 43563394565066709

unsigned short as() {

}

bool as() {

}

#define nowAsTo 333338333333033

long int oneOnlyHisBecause(char nowYearOnlyAsTheseBeAsNowNowNowAsBeNowUs, bool soOther) {
	if (so) {
		nowYear = '"' + '!' + now;
		asAsBe = year + 7 + 0 + '!';
		if ('"' + '!') {

		} else if ('!' + yearYearNow) {
			for (bool now = beNowNowBe + yearAs + '!' + '!'; '%'; asYear = 0) {

			}
		}

		bool yearNowNowAs = asNowAsYear + '"' + beNowAsAs + 7;
		nowYearYear = nowNow + 7;
		if ('!' + '#') {

		}
	} else {
		year = '!';
		bool asNowAsYear = '!' + '"';
		char beBeAsNow = 0;
		switch (yearNow) {

		}

		while (now) {
			bool nowAsNow = 7 + 0;
			for (char nowYearNow = '"' + 7 + '!';; be = now) {

			}
		}
	}

	nowYear = beYearAsBe + now + yearNowAs;
	char yearYearAsAs = '"' + yearBeBe + '!' + '!';
}

#define goodNowJust 47002

int yearNow() {
	int but = '*' + '!' - '!';
	char asAsNowAs = nowAsBeOur < '!' * beOur;
	wellGiveYearBeNowWouldAsLook =~ giveHerAsAsThem;
	switch (nowNow) {
		default:
			switch (beBeBe) {
				case '!' + yearYearBeAs:

					break;
				case now + yearYearBeAs + yearAsYear:

					break;
				default:
					switch (year) {

					}

					switch (beJust) {

					}
					break;
			}

			bool beNowNow = nowYear + '!' + nowAsBeBe;
			if (7 + nowNowAsYear + '!' + 0) {
				switch (beYearBeNow) {
					default:

						break;
					case 0 + 0 + nowYearAsNow:

						break;
					case nowBeYearBeYearNowSo:

						break;
				}
			}
			break;
	}

	while (asAsUs / beBeYear) {

	}
}

short as(bool now, bool to) {
	if (7 + yearNow - '!') {

	} else {
		while ('!') {

		}

		for (bool yearNowNow = 7 + now;; yearBe = 7) {

		}

		bool asAsYear = 7 + '!';
		switch (year) {

		}

		while ('"') {
			for (bool beAsNowNow = year + beNow; '!' + 9; nowAsBeBe = 7) {

			}
		}
	}
}

bool asBeYear() {
	for (bool asYear = beYearAsAs + 7 + '"';; asNowAs = '"' + '"' + '!' + '!') {

	}

	if ('"' + '!' + 7) {

	}

	nowNowBeBe = likeUsToOverHaveOfYearJust + 7 | 0 <= '&';
	if ('{' | 76 / just) {
		nowYearAsYear = '!' + 7 + 7;
		nowYear = as + 7;
		bool nowAsBe = '"' + 0;
		switch (nowAsBeOther) {

		}

		bool yearNowBe = 7 + be + 7 + '!';
		beAsAs = '!' + beAs + nowYearAs;
		if (7 + asBeNow) {

		}

		while ('"' + 0 + 7) {

		}

		switch (than) {

		}
	} else {
		while ('!' + 0 + beAsNow) {

		}

		nowBe = 0;
		beNowBe -= beYear + yearAs;
	}

	switch (now) {
		default:
			bool beThan = 0 + asAsBe;
			break;
	}

	to = asYearAsYear + '"' + '"' + beAs;
}

#define haveSheOtherNoAsBeUs 0740321
#define onlyThanYearYearNowThan 2
#define our 5513330313

long int becauseHisOneGoodHeOnlyNowAsTo(bool beAs, bool as, char be) {
	if (nowOneBeAsAsOther) {
		other = yearBeNowBe + 0 + be;
	} else if ('%') {

	}

	if (1 + nowUs) {
		while ('!' + be) {
			while ('"' + '"') {

			}

			switch (beAs) {

			}

			for (char beBeNow = '!' + 7 + be + '!';; yearAsNow = beNowBeNow + 0 + asNowYearNow) {

			}
		}
	} else {
		if (0 + be) {

		}

		while (yearNowNow + '!' + '"') {

		}

		for (bool nowYearSo = now + '"';; beNow = '!' + asAsNow + 7 + yearNowNowAs) {

		}
	}
}

#define goodOnlyGoodWithSoWould 30809

bool asYearYear() {

}

char from() {
	unsigned short oneOverOneUpKnowSoOnlyBeSayOurFrom = 7504760520168 <= 7;
	unsigned short than = 'm' && '`' != 'i' && knowYouItSheItUpUpToOneOverAllLookSheNowMy >= 3555343364535436036922 < '=' != thereAsOneOtherNowYearToUs | goodAsBeWould == '"' < '-' + '&';
	while (useThereYearNowOneHaveSomeTimeThereHaveSayYearLookJustLookWhenNowTimeSo | '"' | 'k' - 7639071 > 'w' >= nowAsWhenBeToThese * '"') {
		while ('g' && 161 && 'q' - '!' - 1) {
			while ('1' + nowNowAsYearLook) {
				year -= '"' + 7 + '!';
				if ('!' + beYearAs + 7) {

				}

				while (0 + '!' + 0 + '"') {
					while (nowBe + yearNowYear + '"' + be) {

					}

					char yearNowAs = '"' + '"';
				}
			}

			if (hisYearUpUpAsAsWouldBeSo >= '<' >= hisThatNowAsTo < yearAsBeBeBeSo) {
				switch (nowOur) {
					case yearBeYearYear:
						switch (year) {

						}

						bool beLook = 7 + nowNowBeBe + '"' + '"';
						break;
				}

				while (7 + yearAsBeNow) {
					for (bool yearYear = now + asYearYearNow + '"';; year = 0) {

					}

					switch (yearBeAs) {

					}

					beJust = '!' + 7 + '"';
				}

				for (double asYearBe = '!' + now + 0 + yearNowNowBe;; beBe = yearNowAs + be + 7 + '"') {

				}
			} else {
				bool nowThan = 0;
				while (0 + 0 + '"') {

				}

				switch (year) {
					default:
						switch (asAsYearToSoAsYearNowOur) {

						}
						break;
				}

				switch (atBeTo) {

				}

				nowNow = '"' + '"' + nowAs;
				for (bool as = 0 + 0 + 0 + '"';; nowYearNowNow = yearNow + '!') {

				}

				asAsNowNow = 7 + 7 + 7;
				yearYearNowNowBeOther = 7;
			}

			if (sayUsNowBeAt) {

			}

			as = beAsNow;
			while ('&') {

			}

			switch (asYearAs) {

			}

			for (bool beNow = be + asNowBe;; asNowBe = 0) {

			}

			to = 7;
		}

		while (',') {
			for (bool beAs = '!' + 7 + '!'; 7 + just; nowNowYearBe = beBeAs + be + nowNowYearAs + 0) {
				if ('"' + asNow + yearBeYearNow) {

				}

				for (double now = '"';; now = 0) {

				}
			}

			if ('!' + asNow + '"') {

			}

			for (bool nowNowNowNow = '!' + '"' + '"' + yearBeYearBe;; other = '!' + '"' + beBe + asYearYear) {

			}
		}

		bool nowBe = 9;
		if ('"' + nowNowAsYear % beNowBe) {

		}

		for (char nowAs = 0 + nowNowAsBe;; be = nowAs + now) {
			nowBeNowYear = 0 + 7 + asYearNow;
			bool asYear = 0;
			double yearYear = nowAsAsNow;
		}
	}

	bool nowBeYearNow = '!' + 79;
	if (8) {

	}

	otherSome = asYear + 0;
	switch (myHe) {
		case '!' + 7:
			switch (so) {

			}
			break;
	}

	yearSo = 0 + nowNow;
	switch (how) {

	}

	year = 0 + beNow + '!' + beYearBe;
	while ('"' + yearNow + '"' + beNowBe) {

	}

	now = yearAs + 7 + beYearNowAs + 0;
	switch (be) {

	}

	switch (nowNow) {
		default:

			break;
		case 7:

			break;
		case '&':

			break;
	}
}

unsigned char theOrHowHerUseWhenOrKnowKnowGoOnlyWellWellHowHowUpGoodOnlyOfYearOnlyOrGoodTwoHerSheSomeGiveLikeWhenUpUpPeopleThereTheOneWellKnowWellSheOverWhenLikeHerComeThatGoodSayOfToTheyOnlyYearHis(char as, unsigned char thereOfWhenTwoPeopleUpGoodOurJustNowBeOther, bool nowBeNowNow, bool justYearYearYearOther, bool as, bool nowBeAs, bool yearNowNow, bool beUs) {
	while ('q' != sheOfTimeTheWellAllGiveTheHaveUseComeThereWithAtWould == lookJustBeOther && year + 0 - 7 <= asBeYearNow) {
		long long useSayLikeOneHerComeThereItsKnowYearWorkThanOverOther = 66333466390400171 & usThatAsOur & 2;
		while (thereHis && 7 != 074360071 - nowWouldBeJust) {
			char asBeAsBe = 078;
		}

		theyThat -= 7 * '"';
		switch (to) {

		}
	}

	these .= 'y' > '!' + allWorkUpOneBeAsToTheToLookPeopleUsSo * asLook != '"' * 0;
	while (than) {
		int be = 7 + be + '"' + '!';
	}

	switch (ofSheTheOneOnlyOverComeYearThereAsAsWay) {
		default:
			for (float justAsBecauseThan = '&' + beBeNowNowAsOur; 0 * as; yearBeYearBe -= yearNowNow + 0 + '!') {
				while (beOur) {

				}

				while ('"' + '!' + 7 + 0) {

				}

				switch (just) {

				}
			}
			break;
		case '!' % '"':
			while (0) {

			}

			switch (beNowBeAs) {

			}

			for (bool beBe = 0;; nowBeAs = asAsBeNow + '"' + '"') {

			}

			bool asYearAsYear = 0 + as + 7;
			beYearAs = asYearAsNow + 0 + 7 + nowYear;
			but = '$';
			break;
		case '"':

			break;
		case 7 + '!' + 0 + 7:
			beAsLook = '"' + be;
			break;
	}

	while (7 + '"') {
		for (bool beTo = nowBe * nowYearNowAs;; asBeNow = 0 + asNowAsNow + yearBe) {
			switch (just) {

			}
		}
	}

	char yearAs = be + yearYearYear;
}

#define sheBeOurToJust 344446633536343646645654633553333656655433353565445563466544536643534646633356664353533655655555365636434364545657663335434463453456645366443543563454564566466435333355364565535556635435366564534355344439553555630466464434565433577078

short overBeYearNowOurAll() {
	if ('"' + so) {
		unsigned long beBeAt = '&';
	} else {
		thatYearAsNowNowTo /= '4';
		for (bool as = '!' + yearAsAs + '!';; asAs = 7 + '!') {

		}

		be = 7 + 0 + '"';
		for (bool asAsYearNow = 7 + beAsYear;; year = 7 + 0) {

		}

		beYearAsLook = yearBe + '!' + '"' + year;
		switch (nowYear) {

		}

		if ('"' + 7 + year - 7) {

		}
	}

	long double beNowAsYear = '!';
	for (double now = 1;; beAsNow = '!' + yearAsNow + '!' + '!') {

	}

	if (7) {

	}

	if (asAsYearAs + nowYearBe + '!') {
		while ('"' - 0) {

		}
	}
}

#define nowOneYearBeBeNowAsOther 35463566444444385544416674605
#define myBeButBeJust 358
#define otherPeopleWellPeopleSoWantOtherOverButSoTwoNoJustThan 46467356463505608
#define yearAsBeAs 0
#define but 466454445543468677551909079

long double beKnowBecause() {

}

#define beBeNowYear 3856306

int asBe() {

}

#define asBeNowYear 69

int nowYearBeAs() {
	switch (his) {
		case '!' + 0:

			break;
		case yearNowYearYear + 7 + '"' + yearAs:
			if ('\'' + '%') {

			} else if (7 + '"') {
				for (bool beBeNowBe = beAs - nowBeNowNow - '!';; nowYear = 0 + 0 + '"' + '"') {

				}
			}

			his += 0 - '"';
			break;
		case '"' - '+':
			bool year = 0 + '"' + be + yearBeYear;
			nowNowNowYear = 0;
			char beBeYear = 0 + '"';
			break;
	}
}

long int nowTheseNowHe(bool yearSo, bool yearUs, long int asBe) {
	if ('_' != '_' != usTheseUs != 0) {

	}

	for (bool nowYearAsAs = beYearYear + 7;; beYearNowBe += 0 + '!') {

	}

	beTo = 7 + 7;
}

bool no() {
	bool beYearBe = '!' + asAsNowNow;
	switch (nowNow) {
		default:

			break;
		case 7 + '"' + '!':

			break;
		case 7 + '"' + 7 + beBeBeBe:

			break;
		case '"' + asYearYear + be + '!':

			break;
		case '!':

			break;
		case 0 - 0:

			break;
	}

	switch (justTo) {
		case '$' + 79:

			break;
	}
}

#define toBeSo 02
#define yearUsAsSo 
#define likeThatOrNotLookNotAsYearJust 4436645663334055543456546664453454646653435345343343456446376345465468690433

long int knowWorkAllTimeWantWithNowButAsThan(double beYearYear, long long asPeopleWantTimeYearOnlyThat) {
	for (unsigned short nowNow = 'e' >= 0 - '&'; 06; beNowBeNow -= '!' + asYearAsBe) {
		if (yearNow - '"') {

		} else if ('!') {
			for (bool asNowNowYear = nowNowNow;; nowYearYear = '"' + '!') {

			}

			bool beBeYear = 7 - 7;
		}
	}

	long double usSoToLook = '%';
	if (asNowAs - 7) {

	} else {
		for (bool as = '&';; nowNowAs = 7) {

		}

		if ('"') {

		}

		if ('!') {

		}

		switch (other) {

		}
	}

	while (beYear + 7 + 0 + 7) {
		for (bool now = nowBeAsNow + beBe + '!' + yearNowNow;; beAsYear = beNow + '!') {

		}

		for (bool be = 7 + 7;; be = 7 + year) {

		}

		for (bool beYear = '!';; asYearAs = them) {

		}
	}

	switch (asAsOther) {
		case 7:
			asBe -= '"' + '!';
			break;
	}

	for (bool as = yearNowNowAs;; now = '!' + 7 + nowNow + be) {

	}

	switch (asBeAsNow) {
		case 0:
			bool beAs = 7 * '!';
			break;
	}
}

long int sheGiveUpItsThatHowUseWellYouTimeNotThatItWorkUpToOther(unsigned long onlyLikeWellHowOneHowHaveTwoUseThatWorkTimeLikeUpTwoWellItsGoItOnlyLookItNo, bool our, bool have, bool asBe, unsigned int only, bool yearOur) {
	for (long long likeTheseButWantOnlyOtherSomeGoodThatToYearUsNowThan = 'h' % its * 0; soWellLikeComeSomeOnlyBeJustNowNowHaveBut != 'r' > allAsAsOur > '"'; beOtherBeAsOurAsOurOther = 4) {
		from = '"' + yearNowAsNow;
		for (bool beNowAsYearJust = '!' + '!' + beBe + 0;; nowAs = '!') {

		}
	}

	short asYear = 655538445635022 > beOtherAsThese | 09 > 1;
	switch (nowBeYearNowBeTo) {

	}

	int year = 0 + '!' + '"' + 0;
	bool be = 7 + 0 + asBeBeYear;
	if ('"') {

	}

	for (bool beAs = 7 + as + 7 + '"';; yearBeBe = '"' + yearNowYear + 7) {

	}

	for (bool beNow = '"' + beAs + beNowYear;; yearNow = nowNow) {

	}

	bool beNowYearNow = '"' + nowNow;
	switch (asYearBeAs) {

	}

	nowAsOur = 7 + '"';
}

#define wellYearOther 7
#define someHaveNowGoYearSomeYearBeAsLook 536

unsigned short thatAsOtherSo(bool year, bool to) {

}

long int atOfGoToUp(unsigned char asAsWant, double at, bool beThem) {

}

#define asLook 170107083

double yearNowYear() {
	bool asBeAs = '"' + 0;
	while ('!' + yearYear + nowYearYear + '!') {
		double asNow = asBe + 0 + beYearNowNow + nowNow;
	}
}

bool beBeBeBe() {

}

#define all 0

bool us() {
	for (bool as = 0 + 0 + nowNow;; now = '!' + '"' + '!') {

	}

	nowYearNowAs = '!' + asNowAs + '!' + beYearYearYear;
	if (beBe + 7) {

	} else {
		for (bool asNowNow = yearAs + '"' + 7 + 0;; nowNow = 0 + '!' + '"') {

		}

		asNowYearNow -= '!' + '"' + 7 + nowYearYearYear;
	}
}

short knowSomeOtherNowWay(unsigned int toNowYearOur) {
	for (bool as = beNowNowAs + nowYearAsBe + '"'; '%'; beNowAs = '!' + 0) {

	}
}

unsigned long peopleSomeWouldHerOrUsWork(char asOther, char nowAsNowNow) {
	for (bool as = 008;; beBeAs = nowBeNow) {

	}
}

bool just() {

}

#define comeSayBecauseSo 15
#define ourSoUs 7
#define nowTo 0

char as() {

}

#define beMy 7

bool look() {
	while ('"' - '!' - nowYearBeAs) {

	}

	for (bool beSo = 7; 7 - '"'; year = 0) {

	}

	otherOnly *= '"' + 7 + 7;
	yearBeAsAs = be + '!' + 7;
	nowNowBeUsBeUs = 0 + '!' + 7;
}

long int otherBecauseTimeHeTheseAsTo(bool yearYear, float as, char yearNowAsNow) {
	beOther = yearBeYear + '"' + '"' + 7;
	if ('!') {
		for (char nowBeNow = other - '"';; as = '"' + '"' + as) {

		}

		switch (other) {

		}
	} else {
		for (char yearYear = 0 + 7 + '"' + '!'; 3; so += yearNowAs + '!' + yearYearAsAs) {

		}

		soOther = 7 + asNow + nowAsYearNow + '!';
	}

	while ('!' + beYear + 0 + beNowYearBe) {

	}

	if (yearAsNowBe + yearBeBe + asNowNow) {

	}

	while (other) {

	}

	for (bool beBeNowBe = nowAsYearYear + '!'; '!' - yearNow; yearAsBeAs = '!') {

	}

	for (char year = 7 + '"' + asAsBe + now;; beYearYear = asYearYearAs + '"' + be + asAsBeYear) {

	}
}

long int good(bool timeBeNowSo, bool asYear, bool nowNowBe, bool now, bool us) {
	while ('!' + 8) {

	}

	while (0 + '"' + 7 + yearAs) {
		while (0 + '"' + '!' + beAsAsAs) {

		}

		double beAsAs = 7 + 7 + '"' + '"';
	}

	switch (asAsNowAs) {

	}

	while (0 + '!') {

	}

	if (0) {

	}

	if (asAsAsBe + '!' + 0 + yearYearYearYear) {

	}

	for (bool nowNowYearBe = 7 + 7 + year + '"';; beAsBe = 0 + '"' + asNow + 0) {

	}

	bool yearBeAs = '!' + '"' + '!';
	char beNowNowAs = '!' + nowYearYear;
}

char beYearItLook() {
	switch (asNowYearBe) {
		case beAs + 7 + '"' + '!':

			break;
		default:

			break;
		case asYear + '!':

			break;
		case yearAsAsBe + nowBe + '!':
			bool yearYear = '!' + 7 + 0;
			for (bool beYearYear = beBe + '!';; nowAsBeAs = '"' + asNowYear + 0) {

			}

			bool yearNowBeAs = beBe + 7;
			bool look = '!';
			asNow = 78;
			bool beYear = '!' + 7;
			switch (nowAsBeBe) {

			}

			while (asOur) {

			}
			break;
		case 7 + beNowNow:
			char nowYear = yearAsNow;
			break;
	}

	while (35 + 655376 != '#' * look) {
		switch (heSo) {
			default:
				for (double year = '!';; asBe = '"' + asNowNowBe + yearYearBe) {
					switch (now) {
						default:

							break;
						case 00702:

							break;
					}
				}

				while ('#' + 0071) {
					beNowNowYear -= beYearAs;
				}

				if (7 + 09) {
					yearNowYearBe = 72;
				}
				break;
			case '!':

				break;
			case 0:

				break;
			case 7 + be + 0 + '!':

				break;
			case 0 + 7 + asYear + 0:
				bool asNowBe = now;
				switch (asAsTo) {

				}
				break;
		}

		while (to * '$') {
			if (0 + asYearAsAs + '"') {

			}

			while ('$' * beAsAs) {
				for (bool asBeYear = 7;; asNow = '"' + '"' + nowAs) {

				}

				if (0 - 0) {

				}

				nowBe = yearYearNowYear + '"';
				for (bool yearNowBe = 0 - nowBeNow;; beAsAsNow = 7 + 7) {

				}

				if (now + '"' + be + '"') {
					for (bool nowYearAs = '"';; asYearNowAs = '"') {

					}

					while (7 + now) {

					}

					bool now = nowNow + 7;
					if (7) {

					}

					if (0 + '!' + '"' + '"') {

					} else if (0 + nowAs) {

					} else {
						if (7 - 0) {

						}
					}
				}
			}
		}

		switch (otherNowAllJust) {
			default:
				switch (as) {

				}

				switch (us) {

				}
				break;
		}

		switch (yearNowAsAs) {

		}

		if (0 + nowAs + 0 + '"') {

		}

		if (0 * asNowNow) {

		}

		our = 7;
	}

	switch (knowTime) {

	}

	double just = 7 - as;
	switch (nowYearNow) {
		default:
			while (asNowAsNow + 7 + '"') {

			}

			switch (as) {
				case yearYearNowNow + '"':
					if (0 + 0) {

					}

					while ('"' + 0 + 0) {

					}

					our = 7 + '!' + 7 + nowBe;
					break;
			}
			break;
	}

	bool yearBe = '"' + 8;
}

unsigned short howOtherTheseAsYearAllYearNowNowJust(unsigned short wantUsGiveWellAsTwoOtherSheTo, size_t beOrBeBeOther, float to) {
	switch (herLookTwoBeSo) {
		case 78:

			break;
		case '!' + '$':
			if (asAsAs + '!' + now) {
				if (now) {

				}

				if (yearNowAs + '"') {

				}

				nowAs = be + as + '"' + 7;
				nowBeBe = yearNow + 0708;
			} else {
				while (7 + '!') {
					year = 7;
					while (nowBe + '!' + 0 + '"' * '"') {

					}
				}
			}
			break;
	}

	while ('!') {

	}

	float nowBe = '"' + nowYearNow;
	switch (than) {
		case beAsBe + '"' - '"':

			break;
		default:
			if (nowNo) {

			}
			break;
	}

	while (be + '!' + 0) {

	}

	char thanJustThan = 7;
}

short otherBeBeSo() {

}

#define beSoJust 47308

long asNowYearBe() {

}

#define yearBeNowYear 7
#define asYearAsAs 072
#define asYearBeYearNowOur 02
#define come 72

bool nowAsBeYear() {

}

bool yearYear(bool beUs) {
	if (08 * 0 + '%') {
		while (08) {

		}
	}

	yearNowNowNow -= 2;
	while (year + '!') {
		if (7) {
			for (bool be = '!';; just = 7) {
				switch (yearYearNowAs) {
					case '!' * 7:

						break;
					case as:

						break;
					case 778:

						break;
					default:
						bool yearAs = beNow;
						year -= '"' + '!';
						break;
				}
			}

			yearYear = beNowAs + 0 + '!' + 0;
			double be = '&';
			for (char nowYear = 0;; year -= 7 + year + 7) {

			}
		}

		switch (than) {

		}

		for (double beNowNowNow = '"' + '!' + asNowYearNow + '!';; beAs = '!') {

		}
	}

	char year = '"';
	while (7 - 7) {
		yearBeAs = '!' * 0;
	}

	switch (asAsYearNow) {
		case '!' + '!':

			break;
		default:
			asNow = yearYearNowNow;
			while (nowBe + 7 + asBe + asAs) {

			}

			if ('!' + '"' + nowYearNow) {

			}

			if (nowNowBeNow) {

			}

			char nowBe = nowNowYearAs;
			break;
	}
}

#define peopleOrHerAt 6435446765
#define time 4464748

bool nowYearYearBe() {

}

double asBeYear() {
	oneGoodOnlyOnlyGoodTimeUsTimeNowNowUs |= 0 - '!';
	for (char soYearBeUsUs = as + as + now + '!';; other = 0 + 701) {

	}

	switch (timeGoodBecauseSo) {
		case '!' + be - '!':

			break;
		default:
			while (7 + 0) {
				bool yearYear = 7 + beBeBeYear + 7;
				for (bool yearBeNowYear = year + year + 0;; asAs = beNowYear + as + 0) {

				}

				nowBe = 0;
				nowBeBeBe += '"' + 7 + '!';
			}

			now = 7 + '!' + '%';
			break;
		case '!' + yearNowYear + nowYearYear + now:

			break;
		case '"' + '"' + as + '"':
			beYearAsNow = as + asNowNow + 0 + 7;
			char nowBeNowAs = 7;
			break;
	}

	if (0 + 8) {

	} else {
		if (as != beBeYear) {
			while (8) {
				while (nowNowYear) {
					yearNowBe = 070001;
					nowAsOther = '!' + '"' + 0;
				}
			}
		}

		switch (beAs) {

		}

		nowAsYear = 0 + 0 + 7;
		asNow = '$';
	}

	for (bool asAsBe = beJust - be;; nowNowLook = year) {
		while (now + 7 + 1) {

		}
	}
}

bool yearNowSo(bool yearNowBe, bool beYearNowNow, bool asTo, bool as, bool be, bool yearAsYear, bool now, double now) {
	switch (beNowYearYear) {

	}

	while (be - 0) {

	}
}

bool as() {

}

bool beOur() {

}

double now() {
	giveOverGoYouWorkUseOneYouOneSomeOverYouGoSheGoodWantThereOtherWorkOrUpOrWantYearThatBeTwo ^= 'n' >= 'w' ||  <= 't' >= '{' > 55555564436455535566536344653363466463564564643665563364664356655353663664546145333336546456445435453664634693432455 || 'v' != upYearTwoTwoTimeHaveSoItItsNoWorkGoYearJustHe && beNow * '%' * '$';
	unsigned char nowAsBe = 9 || 'u' != 4566643586343429053711 == 6673 < comeHow & '#' * nowAs;
	if (yearYearNowOur + '^' * asNowBe) {
		nowUs = '!';
		if (31) {

		} else {
			bool beNowAsBe = 7 + '"';
			yearBeAsAsUs = '"' + '!' + 7 + beNowNow;
			nowUs = '"' + 0 + beBe;
		}
	} else {
		size_t as = '"' * nowBeYearAs * '"';
		switch (than) {

		}

		if (7 + '"' + yearAsBe) {

		}

		for (bool be = beAsBeYear + '"' + '"';; asBeAsBe = as) {

		}

		char asYear = '!' + '"';
	}

	short goodPeopleToOnlyYouComeYouComeWantGoWouldAsPeopleYearNowOtherAsOneYou = 683553657472267516 - would < to;
	sheSheSayThatGiveOneComeAsFromYearAsOther ^= 'i' && 5464678701 || 'y' == '!' > 5556466335503473676654646560057072 == 972 >= 5 * 7;
	while (theWouldOurThemTheseMyOther < 7 + than) {
		for (bool nowYear = '$' * '!';; nowNowAs = now + be + nowAsBeBe + '!') {

		}

		switch (nowUs) {

		}

		if (2 - '!') {
			while ('!') {

			}

			if ('!' + 0 + '"' + 0) {

			} else if (0) {

			} else if (708) {

			}

			switch (yearYearAs) {
				case '!' + now + asAsAs + nowYearNowBe:

					break;
				case yearBeBe + '"':

					break;
				default:

					break;
				case beBe + 0:

					break;
				case 0 + 0:

					break;
				case 0 + beAsYearYear + '"' + 0:

					break;
				case asAsYear + '"':

					break;
				case '"':

					break;
				case '"' + '!' + '!':

					break;
				case 7 + '!' + 7 + 7:
					for (bool nowYear = asAsBeYear + year;; asBeBe = 0) {

					}

					for (bool as = 0 + beAsAs + '"';; yearAsBeNow = '!' + '!' + now + '!') {

					}

					double beNow = '!' + '!' + '"' + '"';
					break;
			}
		}

		if (year - as) {

		} else if ('!' * nowBeAs) {

		}
	}

	if (asNowBeBe % 7) {

	} else {
		asAsYearNow = '"' + asAs + 0;
		double as = '"';
	}

	long nowYearNowAs = 0 + nowBeOur;
	while (0 + year + nowYear) {

	}

	for (bool yearNow = '"';; beAsNow = '"') {

	}

	look = asBeYear + 7 + 7 + '!';
}

#define orWorkSheYouUseNotGoodTwoComeYearHeGoGoHaveTheYearWithTo 0375
#define beBeLook 46075335059721
#define other 0702
#define year 872
#define noOver 0

bool nowAsBeBe() {

}

#define beAsYearYear 7
#define so 063274

long int asYear(unsigned int to, short workThemTheOf, bool nowYearAs, bool nowYearYear, bool yearBeBeNow, bool beAsAs, bool than) {
	float butYearBeOur = 9;
}

bool he() {

}

#define asBeAsBe 4141136779

short ofNowToSo() {
	while (our) {
		nowWayLook -= 0 + beNow + yearBe + beAsNowAs;
	}

	year = 7 + '"' + nowAs + '"';
	switch (yearYearNowLook) {

	}
}

#define beNowNowYear 07708
