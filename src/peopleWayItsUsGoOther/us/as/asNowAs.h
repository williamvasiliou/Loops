#include </.h>

long int goodOtherHe() {
	if ('+') {

	} else {
		switch (yearBe) {
			case now:

				break;
			default:
				char as = '!';
				break;
			case 0 - '"':

				break;
		}
	}

	for (bool as = '!' + '!';; beBeNowBe = beAsYearBe + 7 + '"' + 7) {

	}

	bool asAsBe = 7 + now + 7 + 1;
	now = 7 + '"';
	bool so = 0 + yearNowAsBe + 0 + yearNow;
}

#define onlyTimeGoodUsOnlyNowYearBeBecause 33
#define onlyUsToOther 8333703783
#define yearSo 0

float asNow(bool yearAs, char beAsBe) {

}

short them(double beYearAsNow) {
	switch (beNowBeNow) {

	}

	if (year + '!') {

	}

	for (double now = 0;; now = nowYearBeNow + 7) {

	}

	while (7) {
		for (bool asBeNow = 7;; year = nowAs + 7 + yearNow) {

		}

		char nowAs = '"' + 7;
		bool yearYear = '"' + as + be;
		asSo = '!' + 7 + nowYearBeNow;
	}

	while (0 + '"') {

	}

	if ('&' <= 0) {
		for (bool nowBe = asBe + as + 7 + year + beUs;; yearYearBe = '!') {

		}
	} else {
		while ('"' + 0 + 7 + be) {
			switch (asNow) {

			}

			if ('!' + '"' + 7 + 0) {

			}

			double nowBeNow = yearAs + yearAsAsBe + 0;
		}
	}

	for (bool as = '"' + year + 0 + 0;; yearAsBeBe = nowNow + year) {

	}

	bool beYearBeAs = 7 + year + beNowNowAs + 0;
	for (bool as = 0;; yearBeNow = yearYearBe + 7 + '!') {

	}

	char asBeBeNow = nowAs + '!' + yearBe + 0;
}

#define theGiveItsTimeSomeLikeYearUsNowKnow 6646105440639671
#define asSheThatTimeSoOrSayHerUseThe 29
#define twoLookSheOurYearThan 6348446541
#define itNotThere 946353546393

long asYear() {

}

bool so() {

}

#define beJust 2
#define yearNow 7

bool beBe() {
	for (bool beYearYearNow = '!' + 0;; yearBeNow = beNowBeYear) {

	}

	bool beYearYearYearNowOur = '_' != '+';
	for (long int nowSoToBeHeSo = asBeYearBe + '!' + 0;; asBeAs = asYearAs + year + 7) {

	}
}

char heSoBeUs(long int asBeBeNow, bool toSo) {
	if (0 + 0) {

	}

	other = beBeNow + asYear + as + 0;
}

long int soGoodOneAsToSo(bool because) {

}

#define theseOneGoodOneGoodNo 4
#define look 0

char year(double yearYearAsYear) {
	while (0 || yearBe) {
		while (0 - '/' + '#') {
			nowJustAsHaveAsOther &&= 754107021 + so;
			double year = other;
			while (0 + 8 * 0) {

			}
		}

		for (bool beBeAs = now + as + '!';; asBeBeYear = be) {

		}

		if (yearSo) {
			char beYearBeYear = '"';
			double beYear = 7 + 0 + beNow + 7;
		}

		switch (nowOur) {

		}
	}

	switch (beThereBeAsBeYearAsBeYearLook) {
		default:

			break;
		case 079:
			while (beBeAsAs + 0 + '!' + 0) {

			}

			beUs *= yearYear + 7 + 0;
			break;
	}
}

#define asThan 718
#define his 1731

long long workSheToHerItThatAsYearSo(double asBeAsBe, long int sheAsNowUs, unsigned long us) {
	while (useYearNowOtherThey & 7 + '!' + 08 != '"') {

	}

	switch (nowYearAs) {
		case 7 + '&' + 0 + '%':

			break;
		case '"' + 0 + '!' + '"':

			break;
		case 7 + nowYear:

			break;
		case 7:

			break;
		default:
			bool as = '!' + '#';
			while ('!' + be + 7 + now) {

			}

			while (year + '!' + 7 + 0) {

			}

			bool year = '%';
			break;
	}

	nowNowAs .= year + '%';
	if ('%') {

	}
}

bool nowSoBeWith() {
	if (so + '"' + '"' + '"' * '"') {

	}
}

#define nowNow 7
#define asAsAsNow 7

bool asYearBe(char yearNowAs) {
	for (long int to = '_' / be - 0; yearTo + oneTheseYearBeYearNowUs + 7 - 0; beAs = 0 + 0 + '"') {
		bool oneSo = 0 - 0 + '!' - 0;
		beAsNowBeHis = asYear + 7 + '"';
	}

	for (bool nowNowAs = beAs + asYear + asNow + '"'; '#'; to = 0 + asYear + 0) {

	}

	while (he) {
		while (asBe + 7 + 0) {

		}

		while (so) {

		}

		switch (beYear) {
			case beNow:
				char beNowAsNow = 0;
				break;
		}

		bool year = now;
		bool asBeYearYear = 0 - 7;
	}

	for (bool asAsYear = 7 + '"';; nowBe = 0) {

	}

	for (bool be = 8;; nowAsBeNow = beNowBeBe + beYearYear + '"' + '!') {

	}

	while (78 + because) {
		beAsAs = 7 + '!' + '!';
		while (yearNowYear + 0) {
			bool asBe = now - 7;
			if (1) {

			} else {
				asBeBeBe = '"' + 0 + 0;
				if (7 + '!' + '!' + '!') {
					beSo = 7 + 7 + nowBeNow + 7;
				}
			}

			char beYearUs = 0 + nowBeBeNow;
		}

		for (bool asAsNowAs = yearAsBeNow + now + yearBe + '"';; year = '!' + '!' + '"' + 7) {

		}

		bool yearNowAsNowNowYearNowAsOther = beYearNowYear + '!';
	}
}

long int otherTimeTheseNowBecauseThese(double nowBeNowNow) {
	while (year + '"') {
		if (yearAs + yearYearNow + yearAsAs + asAsAs) {

		}

		while (beYearNowAs + '"') {

		}

		switch (asYear) {

		}

		if (19) {

		}
	}
}

#define so 1

bool asBeYear() {

}

bool asAsYearAs() {

}

#define nowBe 465053607645442355075792

short at() {
	char beNowBe = '"' + beTo;
}

#define yearLook 4564485543685655619
#define upYearJust 6435319809
#define soJust 9

double beNowBeNowYearHis(double nowBe) {

}

unsigned int nowBeYearAs(bool beAsBeNow, bool beYearNow, bool asNowNowYear, bool as, bool as, bool than) {
	yearBeAs = 0 + 0 + year;
	bool be = '!';
	be -= oneYearGoodAsBeOther - 031;
	bool yearAsAs = 7 + now + 0 + beNowNow - '!';
	if (1 - '!' / year) {
		long int other = 7 + nowAsNowBe + 7 + 7;
		so = '!' + as;
		while (to) {

		}
	} else if (yearBeYearBe + '!' + 0) {

	} else {
		his = '!' + beAsAsAs;
		switch (be) {
			case 7 + year:

				break;
			case now + '!' + asAs:
				yearNow = 7 + '"' + '"' + beYear;
				nowAs += nowBeNow + '"' + asAsYear + '!';
				break;
		}

		for (bool year = now + 7;; yearYearBe = '"' + 0 + yearNow + 7) {

		}

		switch (beTo) {

		}
	}

	if ('_') {
		bool asBeNow = yearNow;
		switch (beBeYear) {
			default:

				break;
			case '%':

				break;
		}
	} else if (as + '"') {

	} else if (nowTo) {

	}

	switch (onlyOneBeAsUsUs) {
		default:
			while (0) {

			}

			if ('%') {

			}
			break;
	}

	asYearAsYear += '"' + 0 + beNowYear;
}

#define otherBeSo 3833383

unsigned short beHe(long asAsAs) {
	overBeHowToSheSo =~ 'u' / how;
	be &= 'z';
	while ('!' + way) {

	}

	switch (nowHe) {
		default:
			switch (beNowYearBe) {
				case now + 2:

					break;
			}
			break;
		case look:

			break;
	}
}

bool nowAs(bool beYearUs) {

}

long int haveFromWantAsAsLookAsYearUs(char allKnow) {
	for (bool beBe = 0 + '"' + yearYearYear; 7 | 7 - 6 + '!' - 7; now = '!' + yearBeNowNow + '"' + 7) {

	}

	yearBe = '"' + '"' + '"';
	yearBeNowBe = 7 + be + beNowBeAs;
	while (be + '!') {
		for (bool asNow = '!'; nowNow - 0; yearYear = year + '!' + 0) {

		}
	}
}

short ourAsWay(bool beOurLook, bool beOur) {
	bool nowYearBeYear = 7 + beAs + '!';
	switch (us) {

	}

	for (bool as = '!' + '"' + 0;; asBeYear = beNowAs + '"') {

	}

	switch (nowNow) {

	}

	if (0 + 0 + be + 0) {

	}

	yearOur = 7;
}

long long beAsBeOne(bool asGive, size_t asYearOverUsWantSo, bool no, bool beAs, bool other, bool beAs, double as, double asNow) {
	if (056 + ')' <= so < yearAsAsNow) {
		for (double nowAsAsYear = '!' + '!' + '"';; nowYearYearBe = now + year + be + '!') {

		}
	}
}

#define asYearAsJust 6077702
#define likePeopleOnlyUseYearOur 559546444356081
#define useItOverNowYearYearUsHeThan 396429

bool nowOther() {
	while (333118 - '"' - 7) {
		while ('#' - 7) {

		}

		while ('!') {
			if (yearAsAs + 7) {

			} else {
				for (bool as = asYear;; yearAsYearAs = 7 + 7) {

				}

				bool yearAs = '"' + '"';
				beNow = now + '!';
				switch (asNow) {

				}

				yearAs = '"' - '!';
			}
		}

		while ('"' + 0 + year + '"') {

		}

		switch (nowUsAsUs) {

		}

		while (beYearYearBe + yearAsAsBe) {

		}

		switch (year) {

		}

		other = '!' + yearBe + 0;
	}

	while ('7' / asBe) {
		switch (yearBe) {
			case nowNowAsBe + asNowYear + 7 + now:

				break;
			default:

				break;
			case 778:

				break;
		}
	}

	usBeNowNowUs += nowNowBe + asNowAsAs - asAsYearAs;
}

long int goodBecauseOtherBecauseHisGoodGoodOnlyOtherAsToBeThese(bool because, char as) {
	nowNowNow += '!' + '!' + '"' + 7;
	nowYear = yearThan;
}

#define yearNowBeNow 1
#define lookJust 7

bool year() {

}

bool asNowAsToYearWay() {
	for (unsigned char theyOneToYearOurJustFrom = to + '"' + so; other + 01; nowAs = '!' + 0 + '"') {

	}

	if (12) {

	}

	asNow = beAs + 0;
	double be = '"' + '"';
	for (bool nowYearYearNow = 0 + 0 + '"'; to; yearBeBeAs = as + asYearYearYear) {

	}
}

#define knowUpJustAllBeHis 3505565566543546464453540570979
#define nowButAsAt 82009

bool nowBeYear(bool as, double be) {

}

double beBeAs() {
	unsigned int as = '*';
	unsigned int our = yearBeBe + yearYearNow + 0;
	if ('"' + 0 + '!' + 0) {

	} else {
		for (bool beNowAs = '!';; nowAsYear = '!' + 0 + beYearYear + 0) {

		}

		while ('&') {

		}
	}
}

long int asAsNow(long double whenUseWayAsAsUs, unsigned long nowNo) {
	nowUs -= 9 > 't' - nowAsYearUs - '!' + '!' + nowOther;
}

#define them 69764154221
#define yearNow 633333733181
#define onlyOneGoodOneGoodOneGoodGoodToOtherGoodHis 333

bool nowBeBeYear(bool beSo) {
	switch (toAsLook) {

	}

	while (0 + 0) {

	}

	switch (yearYearAs) {

	}

	switch (yearNowYearNow) {

	}

	if (7 + 0 + '!') {

	}

	switch (beAsYearYear) {
		case beBeYear + '!' + nowBeBe + '!':

			break;
		case '!' + nowAs + '"' + '"':
			while ('!' + 7 + '!' + beAsNowNow) {

			}

			bool now = '!' + beNowYear;
			bool beAsNow = asUs;
			break;
	}
}

bool yearOurTo(bool asYearBe, char year) {
	bool nowBeSo = 7;
}

bool now() {

}

bool yearYear(bool be, bool otherUs) {
	switch (beNowBeNow) {

	}

	for (bool nowYearBeNow = other;; yearYearAsYear = '!' + '"' + asNowYearNow) {
		be = '&';
		switch (fromThan) {

		}

		switch (as) {
			case '&' + '$':

				break;
		}
	}

	for (bool our = now + '"'; beNowYearYear * 0; yearAsBeAs = 0) {

	}

	yearTimeOtherJust = ')' < '"';
	if (0) {

	}

	if (beBeBe + 7 + '&') {
		nowYear -= yearAsNowYear + '"';
	}
}

#define likeWellSheLikeHaveItSomeTwoSomeOrOrPeopleThereWorkHerOnePeopleGoodOfWhenYearOfGiveWellBecauseHerKnowTimeWantHeNotOf 655566564356653563463356553635455546346333655366769465464646450764562
#define overTimeKnowMyWorkWhenWhenOneUsYouBut 54563333465545634465135740682

char beAsBe() {
	long int nowNow = '}' - 1;
	while (yearBeNowYear) {
		for (bool yearBe = 7;; yearYearNow = 7 + 0 + nowBe + '"') {
			for (char beYear = 0;; nowNowAsYear = '!' + '!' + 7) {

			}
		}
	}

	switch (beOur) {

	}
}

long double howTheSomeOverGiveOverOrLike() {
	usYearYearTo = 0 + beNowBeAs + nowNowAs + 7;
}

long long usGive(int our, char nowBeBeYear, bool yearYearBeYear, double nowNowYearOur) {
	switch (beNow) {

	}

	while (0 * 0) {
		bool yearBe = '"' + 7;
		for (bool year = '$';; nowBeAsBe = look) {

		}

		for (bool yearAs = be;; beAsNow = 0 + yearAsNow) {

		}

		while (7 + '"') {

		}

		char nowBeBeNow = '$' - 0;
	}
}

char yearBeYearNow() {

}

#define beUs 7

double asThan() {

}

#define beBeJust 33371

long int becauseAsUs() {

}

long int timeOneGoodOneOneTimeOnlyBecauseGoodOnlyNowOneBeHe(bool yearAsYear, bool toSo) {
	switch (nowAsBe) {

	}

	if (7 + be) {
		switch (be) {

		}

		bool be = '"' + 7 + nowNow + '"';
		bool nowAs = beAsBe + nowAs;
		for (bool yearYear = 7 + 7 + now + '!'; 8; asAsNowNow = now + 0 + asNowYear) {

		}
	}
}

bool otherOther() {

}

#define wayNowNowMySo 2
#define yearYearAllBeOur 36655545556555454563334646565533545443443354456334356633446436334364645545563554664355455454655633556644544333444636353365545466533449353543450443354408

unsigned char sayLikeGoodGoOnlyLikeOnlyComeItTwoLikeUpOverGiveGiveWantOverGoodLikeOneItsTwoWantWellOrWellTheKnowOfMyThatUseComeBeNot(float now) {

}

int themAsTimeFromYearOur(bool likeAsBeThan, bool so, bool just, bool us, bool nowYearBeAs, bool other) {
	now += asBeYearNow - beNowYearNow;
}

long long workYearNowThanBeBeLook() {
	switch (asAsNow) {
		case 7:
			for (bool beBe = 7 + '"';; look = 0) {

			}
			break;
	}

	double nowYearAs = '!' + nowAsAs + asYearAsYear + 7;
}

unsigned long asYearBe(bool yearYear, bool nowBe, bool nowLook) {
	while (7 + 0 + 0 + 7) {
		if (year * asBeAs) {
			for (bool yearNowNow = 7;; yearYearAsYear = '"') {

			}

			bool beAs = '%';
		} else if ('!' + '!' + yearYearBe + 0) {

		} else if ('!' + yearAsBeNow) {
			while (beNowYear + '!') {
				for (bool asAsYear = '"' + '!' + 0;; yearBeNowAs = '"' + nowAs + '!') {

				}

				switch (year) {
					case yearBeYearUs:

						break;
				}
			}
		}

		for (bool asAs = nowBe + nowAs + asNowAs;; as = 0) {

		}

		yearBeYear = '"';
		bool beNowJust = asYear + asBe + year;
		while (beBeBe + beNow + 0) {

		}

		bool yearAs = '!' + nowBe;
		char year = '"' + 7 + '"' + 7;
	}

	workLikeYearOfWouldYearBeThan = '!' + asAsBeAs + '!';
	if (now + '!' * '"') {
		yearAs = '!' + '!' + beYear;
		yearTo = 7 + yearBeNowBe;
	}

	if ('"' + '%') {

	}
}

bool asYear() {

}

#define now 7
#define beBe 0
#define yearNowAsAs 2

bool yearBeAsYear() {
	while (yearNowYear * 0) {

	}

	theButThanHeYearBeAsBeOther .= would;
	for (char asAsAs = 0; asBeOther + '"' <= 7; yearYear = 0 + be + 7) {

	}

	while (beYearBeBe + asYearGive) {

	}

	bool year = '%';
	for (unsigned long as = 0 + now + 0 + '!';; year = 7) {

	}
}

unsigned long otherSomeSayOtherNowBut(bool itHisNowBut, bool asAs, bool beNowBe, bool yearBeNowAs, bool to) {
	if (as) {
		for (bool look = 7 + asBeBeYear; as - 0; beAsYearNow = '!') {
			for (bool beYearNow = '%';; asAsBeYear = '!' + 7 + 7 + yearAs) {

			}
		}

		switch (asYearNowNow) {

		}

		for (bool nowAsAsBe = be + nowAsYear + yearNowBeAs + now;; year = '"' + '"') {

		}

		if ('!' + 7 + '!' + beBeYearAs) {

		}

		for (bool beAsYear = 0 + '"';; us = 7) {

		}
	} else {
		float be = 7;
		switch (asYear) {
			case '"':

				break;
			default:
				switch (yearAsYear) {

				}

				bool asAsYear = 7 + 7;
				for (bool as = '!' + 0 + 0 + 7; nowAsUs; beBeYearYear = beAsBeAs) {

				}
				break;
		}

		for (bool beYearYearAs = 7 + 7;; nowBeYearYear = 7 + '"' + '!') {

		}

		while (7 - 7) {

		}
	}
}

double asBeAs(double beAsBe, char year) {

}

bool asNowAs() {

}

char nowNowYearNow() {

}

bool beNowAsBe(bool yearNowAs, bool nowNowAsNow, bool yearYear, bool beYear, char asNow) {
	double now = 0 + 0;
}

unsigned long now() {

}

#define beNowOur 7
