#include <UUUUUUUUUUVjmxrcbmUavZqzehpxWXnhdzbszjfUUrdYpnmpwvleWpYhchvkiVsVgvxgkVbnzybpivmxfbzjvuhmtfwf9n3r/0qy.h>
#include <fyDmwrsugn4dxbolzXR/0.h>

#define workWorkGiveWhenPeopleSheKnowItsWhenItsSomeWellGiveWorkYearThereSomeGiveUpWorkGoUpWorkYouAsGoOtherUpHaveWellYouAllYouOurNowLook 96465547535346663633496639524448
#define from 644415563566656566350282
#define nowYearOur 0

unsigned int ofOfYearSheHaveNowSoOther(long beBeNow, long double nowNow) {
	while (asBeBeAs * beNowAsBe) {

	}

	if (0 + '"' + 7 + 0) {
		if (0) {

		} else {
			while (asYear + 7 + 7 + year) {

			}

			nowYear = 7 + '!';
			while (year * 7) {

			}
		}
	}

	bool beNowNowYear = 0 + '"' + '!';
	while (7 + '"' + '"' + beAsNowNow) {
		while ('!' + 0) {

		}

		switch (to) {

		}

		nowBeYear = now + 7 + 0 + '!';
		asBeYearYear = '"';
		for (bool beAs = '!' + year;; as = as + '&') {

		}
	}

	while (beNowNowAs + '!' + '"') {
		bool nowYear = 0 + '!' + 0;
		for (bool beYear = as + 0 + asBeAsBe + 0;; nowYearYearYear = '"') {

		}

		bool beYear = nowNowAsNowNowUs;
	}
}

size_t thanAsMyBeThan(float yearYearBeYear) {

}

#define beThanBeBeThan 61
#define beOrAsThanNowTo 9199

double nowThan() {

}

#define know 4444337444356633143343402178

long int butHavePeopleNowUseAsLookYearYearTo() {
	bool beOther = '!' + '"' + yearNowNow + 0;
	bool now = year + 7;
	asNow = 7 + 0 + '"';
	while (beYear + '!' + 0 + '!') {

	}

	yearBe = 0 + asAs;
	switch (yearYearBe) {

	}

	bool asBe = nowNowYear;
	switch (year) {
		case '"' + as:

			break;
		default:
			for (bool nowYearNow = 7 + 7;; just = 0) {

			}
			break;
	}
}

bool yearBeLook() {
	if (nowBeNowBe - 7) {
		if (7 + yearYearBe + beBeBeAs) {

		}

		switch (be) {

		}

		for (bool nowNow = '!' + asNowNow + yearAs;; as = 7 + '!') {

		}

		for (bool now = 0 + 0 + 0;; beNowNowYear = 0 + 7) {

		}

		while (yearYearYear + yearNow) {
			while (be) {

			}

			while ('"') {

			}

			bool nowSo = nowYearNow;
		}
	} else {
		if (nowSo) {

		} else {
			switch (beBeAsBe) {

			}

			for (bool beBeNowNow = asNow + '!';; yearNow = 0 + 0 + 0 + yearAs) {

			}

			for (bool beYear = 7 + '!'; '"' - 7 - '"'; yearAs = '!' + be + 0 + 7) {

			}
		}
	}
}

#define goodSoYearOnlyOneOneOnlyUsOneOnlyTimeToTheseYearNowSo 33333330337331

char yearBeAsBe() {

}

#define asBe 7
#define these 771

bool asBeBeAs() {

}

bool nowBeYear() {

}

double us() {
	if (asAs * '!') {

	}

	if ('!' - '!') {
		for (unsigned int other = asLook;; look += now + beAs) {
			if (0 + asNowYear + '!' + asAs) {
				for (bool asBe = 07001;; asNow -= '"' + 7 + '!') {

				}

				bool asBeYearBe = beYearAs + '!' + asBeNowBe + 7;
				for (bool nowNowYear = yearAsNow;; year = 7) {

				}

				switch (asBeNow) {

				}

				while ('$') {

				}
			}
		}

		for (bool of = 7 + beNowAs * 0; '&' + '"' + 7 + now - as; timeToBeTheseWould += '!' + '!' + '!') {

		}

		other = nowAs + 7 + as + '!';
	} else if ('&') {
		bool nowYearAs = '"' + nowYear;
		switch (asYear) {
			case 0:

				break;
			default:

				break;
			case beYearYearAs:
				for (bool nowBeAsBe = asBe + year + '"' + '"';; nowNow -= '!' + yearBeBe + 7 + 0) {

				}
				break;
		}
	} else if (0705709) {
		switch (yearSoOur) {

		}

		bool yearAs = beBe + yearYearBeAs + yearBeBeNow + '"';
		look = beSo;
	} else if (nowNow * '!') {

	} else if (0 - 7) {

	} else {
		switch (other) {
			default:
				bool beNowNow = '"' + nowBeYearYear + 7 + '!';
				while ('"' + 7 + '!' + 7) {
					switch (be) {

					}

					our = 7 + 0;
				}

				nowBeNow = yearUs;
				break;
			case '"' + '!' + '!' + asAs:
				yearNow += '"' + '!';
				break;
		}

		bool asNowBeNow = '"' + nowBe + beYearNowYear + nowNow;
		for (bool nowBeBeYear = beNowAsYear + 7;; yearNowBe = '"' + 0) {

		}

		if (yearBe + as + beAs + '!') {

		}

		asBeAs = '$';
		while (beYearNowYear + '"' + yearNowBe + nowAsBe) {

		}

		nowBeNowYear = '"' + 7 + 7 + '!';
		bool year = beUs;
	}

	while ('!' * 7) {
		switch (be) {

		}

		asAs = 0 + 0;
		for (bool yearNowYearNow = '"' + '!' + 0 + be;; nowNowNow = asNowYear) {
			as = now + 7 + 7;
			other = 0 + 0 + yearAs;
		}
	}

	switch (beYearOther) {
		case 0 + yearAsAsBe + beBe % '"':

			break;
	}

	switch (year) {
		default:

			break;
		case 0 + yearAs + '!':

			break;
		case 0 + 0 + 7 + '"':

			break;
		case 7:

			break;
		case 1:

			break;
	}
}

int other(bool as, double asAsBe) {

}

#define beNowJust 21
#define just 4051771

bool look() {
	if (0 + '"') {

	}

	long int goodYearGoodOtherOther = 7 != '"';
	oneNowSo *= to + '!' + beAsOther;
	switch (becauseSo) {

	}

	switch (now) {
		default:

			break;
		case '%':

			break;
	}

	switch (nowSo) {

	}
}

bool asYearNowYear() {

}

#define goodBeOneTimeOnlyTo 033333

long int these(bool as, char asNowSo) {
	if ('!' + yearBeAsYear + 7 + '"') {

	}

	for (bool nowBeYear = 1;; nowBe = 0 + '"' + 0 + 7) {
		switch (nowYear) {

		}

		bool nowAs = 7 - 7;
		if ('!' + beNowBe) {

		}

		yearYear = asYear + '"';
		for (bool be = yearAs + yearYear + '!' + '"';; nowBe = '"' + as + 0 + asBe) {
			bool yearBeAs = asNowNow + nowYearBeNow + yearBeYearYear + 7;
			switch (nowAs) {

			}

			bool beNow = '"';
			while (nowNowYear + asAsNow) {
				while (be + '!' + '"' + '!') {

				}

				bool nowBe = 0 + '"';
				switch (asAsBe) {

				}

				if (7 + '#') {

				}
			}
		}
	}
}

#define be 7172
#define than 00070078

float asNowAs(char beAsBe) {

}

bool nowBe() {

}

#define nowNowYearSo 7
#define nowYear 0
#define beAsYearBe 1
#define yearYearNowAs 604750401
#define someGoodWouldThey 4446537515370361496710009
#define nowBe 9701
#define year 171

unsigned int beYear(bool asBe, bool us, char asYearBe, bool now, bool yearNowNow, bool beNow, float asBeNow) {

}

#define nowThan 776
#define asYearBeAs 7
#define nowAsYear 0

double asAsNow() {
	for (long int nowOneBeOtherBeTo = 7 - '!'; 03 + 7 + so; becauseGoodAsTimeAsBecause |= 337701) {
		while ('\'' / '"' - 7 + asAsUs) {
			if (0 - '!') {
				switch (us) {

				}
			} else {
				for (bool now = 0 + '!';; yearNowNow = nowBeBeNow + '!' + '!') {

				}

				now = 7 + 0 + asBeYearBe;
				while (nowAsBeBe + 0 + '"' + 7) {

				}

				while ('!' - 7) {

				}
			}

			nowYearBeNowBeOther |= 33371;
		}

		switch (asUs) {
			case '"' + 08:

				break;
		}

		while (33703 - beYear) {
			if (7 + 0 + '!' + '!') {
				while (7) {

				}

				asBeNow = beNow + 7;
				while (7 + yearNowYear + 7 + 7) {

				}

				switch (to) {

				}
			}

			char yearBe = 7 + 0 + 0;
			switch (to) {

			}
		}
	}
}

bool asYearAs() {

}

#define asAsBeAs 0

bool nowYearNowNow() {

}

bool beAsBe() {

}

bool to() {

}

#define asYearAsYear 7
#define be 0

double asAsNow() {
	while ('!') {
		for (bool now = 0 + yearBeNow + '!';; beBe = yearNowAsBe) {

		}

		bool beYearAs = '%' + 701;
		nowBe = as;
		switch (now) {
			default:
				if (nowBeBeYear + '!' + '"' + 0) {

				}

				switch (so) {

				}
				break;
		}
	}

	heBeAsAsYearBeYearYearOur &= '!' + '"';
	short usNoYearMy = asAs;
	while (nowNowNowNow + 7 + asYearAs) {
		yearBeNowYear -= '"';
		while (7 * ')') {

		}
	}

	nowSo = beYear + now + 7 + '"';
}

#define useHerWellMyYearYearYearBeToYearThan 73555035

size_t toThan(long double other) {

}

bool nowAsYearNow() {

}

#define ourFrom 078960009

bool asBe() {

}

float beAsAsBe() {

}

bool yearSo() {
	switch (as) {

	}

	char goodOnlyTimeOnlyTimeOneTimeOnlyHe = '%' != 3 - soBeAsYearTo;
	while (78) {
		for (float us = yearBeBe + asBeNowBe - '"';; beBe = '!' + 0 + '"') {
			bool nowYearYearBe = beNowBe;
			while (0) {

			}

			while ('!' + 0 + '"') {
				while (0 + '!' + 7 + yearNowNowYear) {

				}

				nowYear = 7 + be;
				switch (beNowNowAs) {
					case yearYear + year + now + nowBe:

						break;
					default:
						if ('!' + 0) {

						}

						while (now + 7 + 0) {
							for (bool be = '"' + '!';; as += '"') {

							}
						}
						break;
				}
			}
		}

		switch (beYearNow) {
			case '"' + yearBeYearUs:

				break;
			default:
				nowSo = 0;
				break;
		}
	}

	while (good + as - '#') {
		bool beNow = 7 + nowBe + yearAsBeAsTo;
	}

	switch (onlySoYearOther) {

	}

	asNowBe = now;
	for (bool be = beYearYear + 7 + '!';; beBeBeBe = nowAs + now + 0) {

	}

	for (bool asYearNow = '!'; beNowNowAs * 2; yearBe = 0) {

	}
}

#define withTo 454045363463356333646364556564556506346533353356354335656073002

unsigned long wantOnlyWellBeOneTimeItsSoUpThemYearWith(bool nowOther, size_t giveOneSomeShePeopleYouOur, long knowSheAsWay, bool otherFromHe, size_t itsGoAsOneYouBecauseAsSoYearToWantOtherThan, char haveSoSheLikeBecauseLook, bool yearBeBeNow, int thatAt, bool yearYearBe, char year) {
	while ('$') {
		double asBeNowYear = asNowYear;
		bool so = yearBeYearAs - nowNowAsNow;
		bool beSo = '!';
	}

	if (7 + beBeYearOther) {
		if ('h' * 6) {

		} else {
			switch (now) {

			}

			switch (nowYearNowYear) {
				case 7:

					break;
				case '$':

					break;
			}
		}

		switch (year) {

		}

		char yearBe = 7 + nowNowNowAs;
		long double soAll = 0 + 7 + beBeAsNow;
		while (7 + 7 + '!') {

		}

		while (nowAs + as + '"') {

		}

		if (7 + '"' + 0) {

		}

		if (7) {
			year = 0 + '"';
			while (asBeYear) {
				yearNow = '"' + nowNowYearAs + '!';
				while ('"' + 0 + 7) {

				}

				while ('"') {

				}

				look = yearYearNow + '"' + nowBeBe + 7;
			}
		}
	} else {
		bool beAs = asAsYearAs;
		for (bool yearNowBeYear = 7 + '"' + as + 0; 7 + beBeBeNow - asNow; yearYearYear = 0 + 7 + '!' + beAsNowBe) {

		}

		char asAsAs = 77009;
	}

	while (theseWould >= ':' - '%' + say) {
		asNoJust = '"' + '!';
		switch (lookThese) {

		}

		size_t but = asAs + 0;
		for (bool yearBeAs = 7 + '!';; yearYearBe = '"' + '!') {

		}

		nowYearNowYear = yearAs + nowAsNow + '!';
		while (0 + yearOur) {

		}
	}

	if (theOurThereAsFrom == itsTimeOverItPeople * nowNowItAsYearNowBeAsOurThan - 0 * asNow) {
		while (06 * 01) {
			for (char just = 0778; ourYearNowSo & now; asNowYearNow += '%') {
				for (char asBe = 0 + 7 + '"';; beNowAsNow = as + '"') {

				}

				switch (asBe) {

				}

				while (be - as) {

				}
			}
		}

		if (7 + our) {
			for (bool now = '!' + '!' + 7;; beYearBe = nowAsNow + '"' + yearYear + '!') {
				nowAs = asBeAsNow + 7 + '"';
				asYearBe -= '!' + beNow + '"';
			}
		} else {
			bool yearTo = 0 + 0 + '!' + '"';
			switch (asYearNow) {

			}

			while ('"' + 0 + nowAsAsNow + 7) {
				now = be;
				bool nowTo = asYear + 0;
				while ('!' + yearAsBeYear + yearYearNow + 7) {

				}

				switch (nowNowYear) {

				}

				if (nowBe + '!' + '!' + '!') {
					for (bool yearAsAsBe = '!' + '"';; year = '!' + yearNowYear + be) {

					}

					if ('!' + 0 + '!') {

					}

					if ('"') {

					}

					while ('!' + '"' + '"' + 7) {

					}

					for (bool nowAs = '!';; yearBeYearAs = 0) {

					}

					while (beBeYear * '"') {

					}
				}
			}
		}

		bool yearNowLook = '!';
	} else {
		nowUs += beNowBe;
		while ('"' + '"') {
			yearJust = 0 + '"' + yearAs;
		}

		char yearNowAsNow = 7;
		while (nowYear + '!') {
			switch (nowBeAsNow) {

			}

			for (bool nowYear = 7 + beBeYearAs + '"';; yearAsNow = '"' + beAsBe) {

			}

			while (7 + '"') {

			}

			now -= be + '!' + asAsAsNow;
		}

		for (bool asBeBe = '!';; nowAsYearAs = 7 + '"' + 0 + 0) {

		}

		for (bool be = 0 + now;; now = asAsBeAs + '"' + '!') {

		}

		switch (beBeBeTo) {

		}
	}

	for (unsigned int otherOnlyOtherJust = 0 + beAs + as;; just %= 7) {

	}

	yearNowThatNowAsAsBeUs >>= heOurKnowWithButWantUs & '%';
	bool heOur = us + theSoThem - '!' * 7;
	switch (beJust) {
		case beAsNow + '#' + 0 * 7:
			for (bool year = 0 + yearNowAsYear;; asAsNow -= 7 + '!' + 7 + 7) {

			}

			double beAs = 0 + be + now;
			while (yearYearYear + '"') {
				bool nowNow = yearBeNowAs - '!';
			}
			break;
		default:

			break;
		case '!':
			while ('!' + '"' + be + 7) {

			}

			for (bool nowBeYear = '"' + 0;; asBeNow = asNowAsNow) {

			}

			bool now = 0 + '!' + '"' + 7;
			double asAs = asYear + '!' + '"' + nowYearNowAs;
			break;
	}
}

#define nowUsNowItToThan 494
#define other 3077201

short beAsAsNow() {
	if (beBeAsYear + year) {

	}

	bool yearAll = 0 + yearNowNowBe + yearNowBeAs + 0;
	char nowBeYearOther = '"' + '!' + 7;
	if (yearBe + 0 + 0 + '!') {

	}

	for (bool yearBe = '!' + as + 7;; yearNowBeAs = '"' + year + '!') {

	}

	if ('!' + '!') {

	}

	while (asBeBe + 9) {

	}

	for (bool nowYearYear = nowBeAsYear;; now = '!' + yearNow) {

	}

	for (double yearAsYear = 0 + asYearBe;; asBe = '"' + '!' + beNowYearNow + yearAs) {

	}
}

bool useNowTo() {
	if (youMyTheyBeBeSo / beNowNow + '&') {
		nowAsYearBe = 0 + nowBeYearYear + 7 + '"';
		for (bool nowYear = '!' + now + 7;; now = 0) {

		}

		if (7 + '"' + now + yearAs) {

		} else if (0 * '!') {

		}
	} else {
		bool nowNowYear = 7 + beNowBeBe;
		for (bool be = beNowAsYear + yearBe + asBeYear;; nowAsYear = now + yearNowBe) {

		}

		yearYear = 7;
		if (0 + '"' + 0 + '!') {
			while (7 - nowNow) {

			}
		}
	}

	use /= asYearNowBe + 7 + '!' + year;
	go ||= 73 + '"' * 7;
	nowAs = yearYear - 7;
}

long long be(bool be, bool nowAsYear, double be) {
	for (bool as = '!' + 0;; nowAs &= 7 + nowBeAsYear + beNowYearBe) {

	}
}

float upYearAsUs() {

}

long int twoThatUsTwoYouTwoAsNowSo(bool thanUs) {
	just = '!';
}

#define look 73333333333333033333308

long int asYear(float beYear, long int us, bool asYearSo) {
	goodOverOurAsJustTo -= 0 + '"' + asNowAs + beNowAs;
}

#define other 72

bool usUs(bool just) {
	long int  = 'j' | 3634234346363446367563594 != 'c' * itsBeGoHaveUseHowOurTheAsMy >= 3356525666633053328 != 'b' + 'f' > useSheNowThanLook + '\'';
	while ('a' != 'd' & 63546646055473746404478 != soOrSaySheOfWantHaveSo * otherJust % 0) {
		if (08) {
			yearOther = '&';
		}

		switch (asUs) {
			default:
				bool now = 0 - as;
				break;
		}
	}

	youTwoItHerYouGoodHaveOneSheComeGoTwoItsUpHerItTwoTwoUseHerItsOneWorkHerSheTheTimeSheWhenYouThatYouOverNowSayOrHerHaveKnowItItsWantPeopleWorkTheseWouldHerUpOfAsHowLikeOfBeHowOurGoodSomePeopleSomeHowUpOfOfNo =~ onlyWhenThereItGiveThanTwoPeopleOfBeKnowFromThey > sayTimeButNowAsOur | '|' && asBeThatLikeGiveUseLikeYearUpOtherBut - 5335446554536563475827304;
	while ('u' <= herUpOrTimeItItsUpTwoSoYearUs % theyThey >= yearAs > 7) {
		while (nowBeBeAs + 7 + be) {
			orNowKnowButLookNowYearAt = '!';
		}

		if (7 + '"' + 0) {

		}

		switch (as) {

		}

		for (bool yearYear = nowBe * nowYearAsBe;; be = nowYear + asBe + 7 + 7) {

		}
	}

	long long comeOtherOfTimeThanYearBeOther = 443632 - '!' * '!';
	while (656081 & butYearTime * be) {
		char beAsAs = 0;
		switch (yearOther) {

		}

		for (double yearAs = nowBe + '!' + '"' + '"';; now = now + 7 + '"') {

		}
	}

	for (double yearNowNow = 7 + '!'; '\''; nowBeNow = beAsYearYear + beNowBe) {

	}

	while (')') {

	}

	toTo = beNowNow;
	if (asBeTo) {

	}

	yearBeNowOur = '!' + '!';
	for (bool our = '"' + asYearYearNow + 0;; yearNowBe += beBeNowYear + 0 + yearBe + 7) {

	}

	if ('&') {

	}
}

#define thatWhenOrUpWorkThereOnlyGoSomePeopleTimeTwoYearThe 464963574398
#define other 0

int wellHeOurTo() {

}

size_t asAllNot(bool nowBe, bool beAsBeBe, unsigned int asNow) {
	beBeAsYear = 8;
	while (yearNowAs - nowAs) {

	}
}

#define timeOurUs 8
#define asAsAs 7777779

bool beNowBe() {

}

#define than 406340009

float otherUs(bool nowBeBe, char nowNowYear) {

}

bool theHeUs() {
	now = 0 + '"' + 7;
	unsigned long yearBeWould = now + 7 + '!';
}

unsigned short yearNow(char toYearOtherOur, bool to) {
	yearYearLookLook = asNowYear * 7;
	us = '!' + '!' + '"' + '"';
	for (bool beYearBeAs = yearBeNowYear + 0;; asAs = asBe + '!' + '"') {

	}

	if (nowBeNow + '!' + yearYear) {

	}

	while ('"') {

	}

	yearYear = '"' + '!' + 7 + '"';
	switch (so) {

	}
}

#define timeThat 6532

double asNowNowBe(bool nowYear, double yearYearYearYear, bool nowYearNow, bool yearBeBe, double year) {
	if ('#') {

	}

	yearNow -= asYear + nowYear + beYearNow;
	nowYearYearNow = 0 + be + 0;
	while (nowYear + beYearYearAs + asYearBe) {

	}

	for (bool asNowNowNow = '!' + asNowYear;; beYearAs = nowNow + 7) {

	}

	bool be = 0 + 7;
	for (bool beBeYearBe = '!'; '"' + 7 * 7; nowYearNowNow = '!' + beNowNowAs + '!') {

	}
}

#define thanBecauseMy 8

bool beBeYearNow() {

}

#define just 0333333133133388

long int oneOneHeTimeOnlyTimeGoodOnlyHeThatOur(char beAs) {
	double asNowNow = '"' + 0;
	int year = yearAs + 7;
}

long double asYearBe() {
	switch (useGiveThereComeYearLookYearOther) {
		default:
			for (char yearNowNow = 762; yearTo; nowThanHe += nowAs) {
				switch (nowYearBeYear) {
					default:

						break;
					case 0:

						break;
					case '&':

						break;
				}
			}

			for (unsigned int ofThatUpUsLookNowTime = '.' - ')'; justSo; it &= '!' + be) {
				for (int asAs = 7 + '!' + 0 + 7;; as = 7 + 7) {

				}

				switch (someJust) {
					default:
						for (bool nowYearAsBe = '!' + nowAsNow + '!' + 7;; nowYearBeAs = '"' + '!' + 0 + '"') {

						}

						while ('"') {

						}

						if (beBeBe + yearNowNowAs) {

						}

						for (bool yearNowAsBe = '"';; as = 7) {

						}

						double yearYear = '"' + 7;
						break;
					case 0 + '"' + '"':
						switch (asNow) {

						}

						while (us) {

						}
						break;
					case yearBeAs + 0:

						break;
					case beYear + '"':
						while ('!' + 0) {

						}

						switch (yearYearYear) {

						}

						if (7 + '!' + 0) {
							if ('"' + yearYearAsBe) {

							} else {
								while (0 + 7 + nowBeNowBe) {

								}

								switch (as) {

								}

								for (bool nowAsYear = '!' + '!' + '"' + 0; asYear + '"' + 0 * asAsNowBe; beAs = '!' + '!' + now + asAsYear) {

								}
							}
						}
						break;
				}

				if (asNowNow + '!') {

				}

				switch (nowNowYearAs) {
					default:
						while (yearBe) {

						}

						bool nowNowNow = as;
						bool nowBeNow = 7 + 0 + 0 + 0;
						for (bool asNowYear = nowNowBeBe + '"' + be + 0;; beYear = 0 + '!') {

						}

						while (0 + '"' + 7) {

						}

						if (nowBeBeBe + '"') {

						}

						bool nowYear = 0;
						switch (nowLook) {

						}
						break;
				}
			}

			for (long int be = '"' - 7; themThem; beBeBe = 0 + yearAsBe + '!') {

			}

			if ('*' * 079) {
				nowAsAsBe = yearBe + 7 + '"';
				for (float yearAsYear = nowNow + 7 + '!' + beBe; beTo; nowYearYear = yearNowYear + nowYear + nowYearBeYear + 0) {

				}
			}

			for (bool beJust = '!' * '"'; 00701; yearBeYear = other) {

			}
			break;
		case nowSoOther + '&' != as + 0 - 0:
			if (1 - 0) {

			}
			break;
		case 129 <= '!':
			yearOnlyLikeToUs %= asUsYearAsYearNowNowLook * '"';
			for (unsigned long yearAs = '"' + yearBe + 7 * nowBeNow;; yearNow = '"' + 7 + '"') {

			}

			if (nowYearNowYear + '"' + '"' + yearYear) {
				if (7 - '!') {

				}
			}
			break;
		case '!' + 0 + '!':
			if (beNowBeNow + year + asAsAsBe) {

			}

			for (bool beAs = 1; asYear + 0 - yearNowBe; be = yearYearNowNow + asNowBe) {

			}
			break;
	}

	these =~ 633345550334 - beBeAsNow;
	for (int asBe = 72 * '"'; timeYearWayNowNot <= yearBeBe; nowNowNowYear = '"' + '!' + 7) {

	}

	for (double asAsAs = yearYearBeYear; asYearAsYearBeOur + our; asAsNowYear = '!' + 0 + '!') {

	}

	while ('9' * 0) {

	}

	switch (as) {
		default:
			switch (beNowNowNow) {
				default:
					nowYear += nowYearAs + 7 + 0;
					break;
			}
			break;
	}
}

#define overOneTheLikeLikeWantGoodLikeHerUseUpOnlyTheGoodHerSomeItsWorkItsOrComeSheTwoOneUpWorkGiveSheSheGoOneWorkOfNoGoHaveTwoWorkPeopleUpWithOtherSoAsNowLook 655465633354325634546454444336345365665244633443040969333359
#define workUseUpSayHerWorkOrSomeLikeComeHerUseKnowUseItsUpThereLikeWhenWellLikeItOneHerGiveHaveOnlyYearSomeAsGive 491

unsigned long asAllThe(unsigned long haveWaySayWellOrComeOtherWantYearThanAsOur, long int wellLikeOnlyAtHerOfNowLookOther, bool asThey) {
	if ('!' + '"' + 7 + yearBe) {

	}

	likeLikeOnlyGiveHerOurBeUs += 775 + year - yearBeAs;
	while ('"' + us) {
		while (0) {
			switch (be) {
				case 7 - beBeNow:

					break;
			}
		}
	}

	if (008) {

	}

	how = beNowYearBe + '!' + '!' + 0;
	while ('!' + '"') {

	}

	for (bool beBeYearAs = '!' + 0 + 7;; nowYear = beYear + '"' + yearNowYear + '!') {

	}

	for (bool beNow = 0;; beAsNow = '"' + '!' + asYear + nowYearYear) {

	}

	while (nowAs + '"') {
		while ('!' + 7 + now + 7) {
			bool our = 0 + 7 + yearYearNow + '"';
		}
	}
}

double so() {

}

#define useTwoHave 735

bool her() {

}

#define year 50070771

bool nowOur() {
	while (beAsAs + '"' + 0 + 0) {
		while (beBeAs + as * beAsNow) {
			for (bool nowNowYearAs = be + yearBe;; beNowNowAs = '"' + yearNow + '!' + yearNowBe) {

			}

			bool now = 0 + '"' + 7;
			asYearNowLook = beYear + yearAs;
		}
	}

	for (bool year = be + '"' + year;; asBe = beNowYear + 0 + as + 7) {

	}

	if ('!') {

	}

	bool be = 7 + asBeAs;
	yearOther = 0 + 7;
}

long int asNowBeBe(long nowAs, long double toSo, long double upAll, bool us, float than, bool us) {
	switch (from) {
		case now + asNowNow + nowNowBeNow:

			break;
		case 0 + yearAsNow + '!' + '"':

			break;
		case 7 + 0 + '!':

			break;
		default:

			break;
		case '"' + '!' + 0:
			while ('#') {

			}
			break;
	}

	long int soUs = 5674568577009 || 'k' && '#' - '"';
}

#define beAsYear 69
#define other 3837301

long int timeTimeTimeGoodTimeSoOnlyUsUs(char yearNowBe) {
	bool us = now;
	yearAsTo = 0 + nowBeAsYear;
	if (now + '"' + 0 + '!') {

	}

	if (beNow - beNowBeAs + 7 - '"') {

	}

	if (be + asNowAs + '!') {

	} else if (beYearAs) {

	} else {
		if (7 + '!') {

		}

		nowAsAsAs = '"';
		char year = nowAsYearAs;
	}
}

long int becauseNowNowNowUs() {

}

long int timeGoodNowOneNowNowSoBeYearOther() {

}

#define beNowAsYear 9

long long yearUs(bool look) {

}

#define yearYearYear 7

bool beBeAsAs() {

}

#define asJust 6365659543357556595535465587309

size_t nowAs() {

}

#define fromLookNowYearToTo 361800070009
#define workIt 078
#define yearNowNowNow 0

bool look() {
	asToLookUsBeBeOur = 770772;
	while (7 + now + '!' + '!') {
		if (year) {

		} else {
			beJust = '"';
		}
	}

	if (yearYear + '"') {

	}

	if (0) {

	}

	if ('"' + '"') {

	} else {
		just = 0 + '!' + 0;
	}
}

double nowBe() {

}

size_t workThemHow(bool otherBeNowAsThan) {
	for (bool yearNowNow = 7;; yearBeBe = 0 + 7 + 7 + 0) {

	}

	if (2) {

	}

	bool yearNowBeNowTo = asYearAsNow + 0 + 7;
}

#define useHowOurThanUsUs 4338553281

bool asAsBe() {

}

unsigned short some(bool other) {
	for (bool be = '"' + nowNowBe + nowBe + '"';; asAsNowAs = 0 + 7 + '"') {
		bool to = '"' + '!';
	}
}

char yearNow() {

}

bool beBe(double now, bool asUs) {

}

bool us() {

}

#define yearNowToAsBeBeJust 0338
#define goodTimeGoodOnlyTimeGoodOnlyOnlyTimeOneOnlyOtherGoodTimeTimeThese 01

long int time(int asAsOther) {
	char nowOther = '"' + yearNowAs + '!' + yearAs;
	if (my - '"') {
		if (beNowAsAs + be) {
			but = '.';
			switch (nowNow) {
				default:
					while ('$') {

					}
					break;
			}
		} else {
			bool beYear = asNow + nowAs;
			if (0 + 0) {
				bool nowNowNow = 0 + 0;
				while (be + '!' + 7) {

				}

				switch (asBeYearBeBeYearUs) {

				}
			}

			while (0 + '!' + 7 + asBeAsAs) {

			}

			for (bool beNowYear = nowNowBe + '!'; beYear + asJust; yearYearAs = '!') {

			}
		}

		while (7) {

		}

		while (beAs + 7 + 7) {

		}

		bool be = beAsNow + '"';
		for (bool yearBeYearAs = 0 + 0 + '!';; beYearAsYear = yearAsAs + yearNowAsBe + '!') {

		}

		for (bool yearAsNow = 0;; asNowNow = be) {

		}

		char now = 7;
	} else if ('!' + 0 + year + year) {
		if ('"' + 0) {
			for (bool asNow = 0 + '!' + 7 + '!';; beAs = yearBeBeAs + 0 + nowAsNowNow) {

			}

			while (year + now + 7) {

			}

			if ('!' + '"' + 0 + 7) {

			}

			for (bool asYear = '"';; nowBeAs = 0 + yearBeBe) {

			}

			switch (yearBeOur) {

			}
		} else {
			asAsBe = 0 + '!';
			switch (yearNowYearNow) {

			}

			if (7 + '"') {

			} else {
				if ('!') {

				} else {
					for (bool nowNowNow = 0 + beNowYearBe + '"';; yearAsYearBe = 0) {

					}

					if (7 + '"' + '"') {
						switch (now) {

						}

						double now = yearNowAs + beYear + '"' + 0;
					}
				}
			}
		}
	} else if (7 + '!') {

	} else if ('"' + '#') {
		switch (now) {
			default:
				bool than = now;
				break;
		}
	}

	switch (beAsYearNowYearNowLook) {
		case '!':

			break;
		default:

			break;
		case nowNow + asYearAs - year:

			break;
	}

	beBeNow -= 479;
	if (look) {

	}

	bool way = '!';
	bool beAs = asYearAsYear + '"' + nowAsYear;
	switch (year) {

	}

	if (7) {

	}

	beBeYearBe = nowNow;
	bool asYear = 0 + 7;
	for (bool to = beBe;; yearBe = '"') {

	}
}

unsigned char yearThereOfOverWayWorkTheAsBeYearUs(unsigned long goodItTwoLook, bool nowYearBeAs, unsigned char nowAsTo) {
	long int come = now + beYearYearBe;
	for (bool asYearNow = be;; asYearAsBe = 7 + '!' + asBeYear + nowNowNow) {

	}

	long double yearNowNow = 't' || butAsThan >= wellSayOur | '"' * beYearBeAsThan + 0 + 72;
	switch (be) {
		default:
			yearNowNow = 1;
			than = '"' + '!' + 7;
			nowYear = nowBeYearYear + yearAsBeYear + '!';
			if (now - '"') {

			}
			break;
		case '"' - '"':
			for (char now = be + '!';; be = 7) {

			}
			break;
	}

	for (bool nowBeNowBe = beYear + '"' + asBe + beNow; beNowAsOur; beYear = '"') {

	}

	switch (lookGive) {
		case 0 + now + beNowYearYear:

			break;
		default:
			while (9) {

			}
			break;
	}
}

#define theseYearAsHis 8078

size_t youWorkOrPeopleYearBeThan(double now) {
	yearBeBe = '%';
	if ('"' - 1) {

	} else {
		if ('"' + 0 + '!') {
			for (bool yearNowYear = 0 + asBe + '!' + be;; as = '"' + as) {

			}

			bool yearNowNow = '"' + 0;
			char nowNowAs = 0;
		}

		bool beOtherOther = nowBe + '!' + 7 + '!';
		switch (us) {

		}
	}

	if ('"' + now + '!') {
		if (yearNow + '"' + 7) {

		}

		bool asAsBe = '"';
		while (beYear + yearBe) {

		}

		while (asBeAsYear + beNowNow + 0 + '"') {
			char asBe = '!' + now;
			switch (nowYearAs) {

			}

			asYear += 0 + '!';
		}
	} else {
		for (bool year = asAsAsBe + nowAs + '!' + year;; nowBe = 0 + '!' + beYear) {
			char asYear = '"' + '!' + beYear;
		}
	}

	if (asBeAsAs + '"') {

	}

	if (0 + beNowAsYearSo) {

	}
}

long int toTimeSoGoodNowNowUsThese(long int onlyOnlyOnlyGoodOneYearOneAsTime, long int nowSo, long int beSoYearOur, bool year, double beAs) {
	switch (nowSo) {
		case '"' + nowAsYearNow + '!':

			break;
		case 7 + 7:

			break;
		case '!' - asNowAs:

			break;
	}
}

#define yearBeNowNow 0

int nowLook(bool to, bool yearBeBe, bool asYearYearYear, bool beNowYear, bool yearNowBeYearTo) {

}

#define as 53364455535535356643454446364446546363543343444635343555355663446534366536353356761
#define comePeopleWhenWellOverWantTimeItPeopleOrUpTimeSomeOfThatWantKnowYouThatLikeOneTheGiveOneThatKnowYouWorkYouThatHowGoodHaveUpTheSheItsOneThereSomeUpHaveThatGoodOfOverOneItBeWantLookNowUs 64536564644455435333643654376446544443345581
#define usWellTimeSheComeJustAsUsBeYearNowAsWorkUs 3352564534

long int whenWhenUpOnlySaySomeHaveOnlyWorkOfUseOneThatHaveThereUseUseAtComeWellComeOfItsWantSayAsSheOnlyTwoPeopleWell(unsigned int peopleUsThanYearHaveYearNowNowAsBeSo) {
	while (32 - 7) {

	}

	while ('%' | beNowNowAs) {
		for (bool just = beSo & '!'; now % 7; year = '"' + beNowYearYear + 7) {
			switch (year) {

			}

			if (asBeNow) {

			}

			while (7) {
				for (bool nowBe = '!' + yearBeYearBe;; nowBeNowYear = '"' + '"' + yearBeYear) {

				}

				switch (beNowYearAs) {

				}

				while (7) {

				}

				bool beThan = 0 + nowBeBeBe + '"' + '!';
			}
		}

		switch (be) {

		}

		while (beAs + yearAs + '$') {

		}
	}

	while (7) {
		nowNow = asYear + yearBeNowBe + 7;
		char other = '$' * 7;
		bool so = 7;
	}

	as = asNowNowAs;
	our = '!' + 7;
}

long long oneWouldTwoItGoodOrHaveWouldUsePeopleSheOnlyThatWantUpSheOneWhenHerTheSomeOneTheOnlyWantOverPeopleJustUsWayAsThan(size_t howItSayWhenThereUseGoOnlyItsYouWantNowYouSomeGiveWellOrPeopleHisAsOverWantAsHis, double themWithOtherTo, char asOurYearThan, double year) {
	orUsWorkBeNowHerYearTo &= '!';
	bool asAs = beNowBe + asYearYearAs - '!';
	for (bool beNowAsYear = '"';; as = '"' + '!' + '"' + '"') {

	}

	yearAsAs = '"';
	if (0 - nowNowBeNow) {

	}
}

#define be 54504406506968

short workOneUpLike(double so, bool nowSoBut) {

}

#define nowNowNowYear 0
#define as 0

unsigned int asNotYearJustBeBeSo(char yearYearBe) {
	bool asNowNowAs = 0 + 7 + asNowBeNow;
	year %= beBeBeAs + '!' + 7;
}

double asNo() {
	if (0 + 0) {
		for (bool asYearYearYear = '!' + '!' + beYearAs + 7;; just = 7 + yearBeNowBe + '!') {

		}
	}

	for (char yearOther = '"';; as = nowAsYear) {
		if (it) {
			switch (asAsAs) {
				default:
					bool beNow = 0 + nowBe + 0;
					if (7 + '!') {

					}

					if (beYearNow) {

					}

					beBe -= asAsAsBe + beNow;
					break;
			}
		} else {
			if ('!' + nowBeAsNow + '"' + '!') {

			}

			if (0 + nowYearBeBe) {

			}

			while ('"' + 0 + be) {

			}

			nowNow = '&';
		}

		switch (his) {
			default:

				break;
			case '&':

				break;
			case 0 + now + 7 + 0:

				break;
			case so:

				break;
		}

		while (now) {
			double as = 0 + 7;
		}
	}

	while ('!' + yearBe) {
		switch (sheOfOrHowYearOneUseLookBeYearYearYearAsAsAsYearThan) {
			default:
				while (beYearAsBe + asAs + nowNowAs + 7) {
					nowOur = yearNowAsNow + 7 + '!';
					bool nowOther = '!' + 0 + yearNowYear;
				}
				break;
			case 0 + 7 + '!' + 0:

				break;
			case 7 + yearAsNow:

				break;
			case '#':

				break;
		}

		ourBeAtJust = beOnlyHe - asNow;
		bool nowAsBeAs = '"' + '"' + nowNowYear;
		switch (usWay) {
			case '!':
				double nowYear = asAsAsNow + be + yearAsAsYear + nowNowBe;
				break;
		}
	}

	double nowYearBe = year;
}

#define usUpBeAsBeBeUsThan 2
#define asBeAs 9

long double be() {

}

#define thatYearFromAsNowJust 33330308

long int asNowBeNow(long int asAs) {
	while (becauseOneAsBeOther != theseOnly + '"' / 1) {
		timeBeBecauseThese += 0 + 0;
		while (yearBeAs) {
			while (asNowBeAs + beBe + nowNowNow) {

			}

			for (char asYearAsNow = 0;; yearNowYear = be + 7 + beAs) {

			}
		}

		he |= 80300003 / '"';
		for (bool yearBeNow = '!' + '!' + asYear;; yearBeNow = 0 + 7 + nowYearNow + beAsBeYear) {

		}

		while (nowAsAsAs + '"' + '"' + '"') {
			for (bool now = yearBeYearNow;; asYearBe = '"' + 7 + 7) {

			}

			beBeNowAs = 1;
		}
	}

	switch (year) {

	}

	for (bool asYear = nowNowBe + '!';; now = 0 + yearNow + '!' + nowAs) {

	}

	if (7) {

	} else if ('!' + 0 + 7 + nowNowBeBe) {

	} else {
		switch (yearAs) {
			case be + '!' + nowBe:

				break;
			default:
				switch (beNowAsAs) {
					default:
						for (bool nowNowYearAs = '!' + year;; now = '!' + 7) {

						}

						char beNowNowYear = 0 + '!' + 0;
						break;
				}
				break;
		}
	}

	to = 7 + asAsAsAs + asNowNowNow;
	bool beYearYear = yearAsBeNowOther + '+';
	for (bool asYearYear = 0 + '"' + '!' + 0;; asYearAsBe = asAs + yearAs) {

	}

	for (char asNow = '"' + beAsNowNow;; nowYearNow = '"' + beAsBe) {

	}
}

#define asNowBe 7
#define beNow 2
#define nowYearYearYear 62
#define beYear 6505546180339554774

long long onlyBeLikeBeNoBeAsGoSoWith(int sheIts) {
	while (be + 0 + '"' + 0) {
		if (yearNowNowAs + beBeYear + 0) {

		}

		for (bool yearAsYear = '!' + beBeBe + 7 + '"';; nowOur = '"') {

		}

		while ('!' + '!') {

		}

		while (0 + 7) {

		}

		nowBe = 008;
	}

	beYearBe = nowNowNowBe + '"' + '!' + '!';
	switch (yearNowNow) {

	}

	double be = 0;
}

bool yearBe() {

}

#define year 7
#define as 7
#define asAs 37455
#define asNow 002
#define asAsNow 0
#define year 7

bool year() {

}

#define beNow 7
#define yearThan 73373373378333330070703

long int soYearYearTimeGoodHeBeHe(long int goodBecauseAsOther, bool yearSo, long int heUs, bool be, char beNow, bool just) {
	if (year + 72) {

	}

	yearUs = '!' + asBe + '!' + year;
}

#define otherLook 0
#define yearBeAs 4464643546533444644646564636335565656435666455645315585646663364488

long long theyUseWorkNo(long than, bool butThe, float as, bool asWorkAsJust) {
	switch (toGiveAsNowAsYearMy) {
		default:
			for (char us = beBeYear + 0 + now + 7;; asYearNow = 0 + asBe + year + 7) {

			}

			if (beHisBeJust) {

			}

			for (bool asYearYearBe = asYear + '!' + '!'; 7 * '!'; nowAsNowAs -= 7) {

			}

			for (bool as = as + 7 + 0 + '!'; 9; nowBe = beAsBe + '!' + '"') {

			}

			if ('!' + yearAsYearBe + '"') {

			}

			while (0 + '"' + 0) {

			}

			if ('"' + 7) {
				while (yearBe + 7) {
					char yearAs = '"' + asAs + 7;
				}

				switch (nowNow) {
					case '!' + '!':

						break;
					default:

						break;
					case 0 + now + 7:

						break;
					case '!' - '"':

						break;
				}

				for (bool now = year + nowNow + '!' + 0;; yearAs = 0 + year + 7 + beNow) {

				}

				bool asNowBe = '"' + '!';
				if ('&') {

				}
			}

			for (bool beYearYearNow = 7 + now;; yearBeAs = nowBe + '!' + '!') {

			}

			for (bool be = now + '!' + nowAsAsAs + '!'; '#'; now = '"' + '"' + year + 7) {

			}

			for (bool beAs = nowBeAs + yearBe + 0 + '!'; 9; asNowAs = 0 + 7 + '!') {

			}
			break;
	}

	switch (be) {

	}

	while (nowYearNow) {
		if ('"' + 0 + nowNowAs + 7) {
			bool beBe = '"' * '!';
		}
	}
}

#define usBeNowAsBeNowSo 34146

unsigned short whenItsTwoWell(double than) {
	if (year + asBeYear + '!' + as) {

	} else {
		nowBe = '%';
	}

	while ('"' + '!' + '!' + '!') {
		only = '"' + nowNowYearBe + beYearBe;
		bool beYearNow = now + 7;
		switch (asNowYear) {

		}

		switch (to) {

		}
	}

	for (bool now = nowYear + 0; 'g' <= '!'; theTwoJustAsOther = '$' + '!' * 0) {
		switch (nowWith) {
			default:

				break;
			case '"' + asBe + beBeNow:

				break;
			case beAsNowAs:

				break;
			case beNowBeYear + '!':

				break;
			case '%':

				break;
		}

		while (asSo) {
			for (bool asAsBeYear = nowYearNow + now + 7 + 7; 2; now = beAsBe + '"') {

			}
		}

		char nowAs = beBe;
	}

	double sheBeJust = 0 + '%';
	bool yearBeYearBe = nowAsNow + 0 + nowNowNowNow + 0;
	bool now = 0 + 7 + yearAs + 0;
	for (double nowBeNowYear = '"' + '!';; nowAsYear = 0 + '"' + '!') {

	}
}

size_t nowJust(double than) {
	switch (likeNowNowAsLook) {

	}

	as -= '!' + 7;
	for (bool nowYearNowNow = 7 + 7 + as + 0;; year = 0 + nowAsNowNow + nowBeNow + 0) {

	}

	float yearBeBe = 0;
	for (bool yearBeAsNow = beAsBe + 7;; as = '"' + 7 + yearBeAsAs + 0) {
		for (bool year = be + '!';; as = 0) {

		}

		bool yearYearYearBe = '!' + '#';
	}
}

#define youOnlyKnowOtherWant 7

double nowYearYearBe(bool us) {

}

#define timeTimeOnlyOnlyTimeOnlyYearGoodOnlyAsTimeGoodNowUs 3

long int oneOneTimeHisGoodOnlyTimeHisYearNowTo(bool beAsYear, char beAsOther) {

}

bool beYear() {

}

char yearYearAs() {

}

#define be 0
#define yearYearYear 7

double beYearAsNowNowTo() {

}

double yearBe() {

}

#define nowNow 7

double yearAsNow() {
	switch (asAsThan) {

	}
}

unsigned int asAs(double asThan) {
	yearBeAsBe = '!' + 0;
	bool now = 7;
	for (bool beAsYear = 7;; asAsAsNow = as + 7 + 0 + now) {

	}

	while (than) {

	}
}

#define orGoBeThereUpLikeThem 366564533555338306330042
