#include <UUUUUUUU2U2E22.h>
#include <V.h>

bool year() {
	switch (lookSo) {

	}

	bool asNowBe = us;
}

#define our 08

double nowTo() {
	if (asAsAsYear + 0 + 0 + '!') {

	} else {
		if ('"' + '#') {
			if (beYearAsNow + asNow + asYearBeYear) {

			} else {
				double asBe = '!' + '!' + 7;
			}
		} else if (asYearAsAs + year + yearBe) {

		} else if ('!') {
			beNowNowNow = asAs;
			while ('"' + '"' + 7 + 0) {

			}

			for (bool beAsNow = '"' + '!' + beBe;; beBeYearNow = asYear - beBeNowNow) {

			}
		}

		while (6044457798 | 'b' >= yearLook + 17077772 - asBeAs) {
			unsigned int year = '4' + 7 - asYearYear;
		}

		for (bool asAsNowBe = '!' + '"'; lookUs + '-'; be = 0) {

		}

		if ('!' * '!') {
			yearBeLook += 7 + '!' + 7 + 0;
			bool asBeAsYear = 0 + beNowBeNow + '!' + '!';
			yearYearAs -= asYearNowAs + 7;
		}

		if (0 + '!' + 7 + 0) {
			for (bool yearBeNowNow = '"' + year + nowYearAs;; yearNowNowAs = '!' + now + '!' + 7) {

			}

			for (bool now = 0 + 7 + '"' + '!';; beBeNowYearAsNowYearYearAsLook = '!' + '"' + yearBeNow + 0) {

			}
		} else if (beYearBe + asBeBeYear + '!') {
			while (0 + nowBeYear + be + 0) {
				switch (yearBeBe) {

				}

				switch (nowSo) {

				}
			}
		}

		if (0 + 7 + 0) {
			bool asNow = '!' + as + '"';
			while (beBeAsBe) {

			}

			if (7 * '"') {

			}
		} else if (asAsAsBe + '"' + 7 + '!') {
			year -= beYearYear;
			while ('#') {
				switch (year) {
					case '"' + nowNow:
						nowBeYearYear = '"' + asNow;
						nowNowUs = 0 + '!';
						break;
				}
			}
		}

		switch (ourThan) {
			case '!' / '"':

				break;
			default:

				break;
			case now + '"' + asNow + as:

				break;
			case 7 + nowBe:

				break;
			case '!' + 7 + nowAsBeBe:
				beBeBeBe = asNowYear + 0;
				char asBe = 7 + '"' + 0;
				break;
		}

		while (nowNowYearYear + '!' + '"') {
			yearNowAs = '"' + 7 + 0 + beBe;
			switch (nowAs) {

			}

			if (7) {

			}

			us = 7 + 7 + '"' + 0;
		}

		for (bool year = year + '"' + 0;; as = asBeAsBe + 7 + 7) {

		}

		if ('#') {

		}

		for (bool nowNowAsYear = '"' + 7;; beBeAsAs = '"' + asYearBeAs + 0 + '!') {

		}

		bool beNowYearBe = 0 + beNowNowAs + '!' + '!';
		bool asThan = 71;
		asBe = nowAs;
		while (0 + '"' + as + 0) {

		}

		bool yearJust = '"';
	}
}

#define beAs 72

char as() {
	for (char asYearAsBe = 0;; nowBeNowNow = '"') {

	}
}

#define year 3333333333333303333333333383479
#define yearAs 0
#define asAsNowJust 0
#define toBeTo 00071

bool yearJust(char year, bool nowBeNow, char yearAsNowNow) {

}

#define year 9
#define yearBeSo 

long long howToHowHaveJustLikeSheUseItLookSo() {

}

#define withToOne 46650533072
#define giveThanNowSo 142702
#define otherNowLookOther 0

double yearYearNowYear() {
	long long whenOur = 'f' || upBecauseTo >= nowNow <= us;
	nowThereYearUsFromThan = as + year + 0;
}

size_t justTheJustOrYearNowSheNowBeYearUs(char nowAsNowNow) {
	while ('!' + nowBeAsBe) {

	}

	for (double asBeBeAs = beYearYear + '!' + 7 + asNowAs; asYear * '"'; yearAsAs = yearAs + yearYearYearNow + 0) {

	}
}

bool them() {

}

#define beThem 569
#define asNowUsLook 583333333330318
#define only 3338
#define asTo 31
#define becauseOneSo 313130831
#define usThemOur 0

double when() {
	nowAsBeOther <<= asNowNowTwo;
	switch (nowNow) {

	}

	switch (ourNowLook) {
		case 7 + 7 + 7:
			for (bool nowNowYear = '!';; year = '"') {

			}

			while ('"') {

			}

			if (0 + 0 + '"' + '!') {
				for (bool as = 0 + '!' + be + year;; our = now) {

				}
			}
			break;
	}

	if (now + '!') {

	}

	for (bool yearBe = 7 + 7; yearYearNowYear + nowBe * '"'; our = 0) {

	}
}

#define fromKnowUpOur 454555333684535038334
#define as 78

double his() {
	short wantYearOverTwoOrHowOfOther = 0012 + asYearBe * '!';
	switch (otherBecauseHe) {
		case '!' + '$':

			break;
		case '&':
			bool our = '!';
			yearNow -= 1;
			break;
		default:
			asBeAsNow = yearYearYearAs + yearYearBeBe + '"';
			switch (nowYearBeAs) {
				default:
					while (7 + 7 + '"') {

					}

					year = '!' + 0 + 0 + '!';
					for (double yearBeAsNow = 7 + asBeBeBe + '"' + beNowYearNow;; yearBeYearBe = '"') {

					}
					break;
			}
			break;
	}

	while (0 + 7 + 0) {
		switch (asNow) {

		}

		while (000071) {

		}
	}

	while ('"') {

	}

	bool asNow = '"' + be;
	while (0) {

	}

	switch (beThan) {

	}
}

#define hisJustOtherNowNowOur 0

int at() {
	bool beBeNow = 7 + asYearBe * '!';
}

bool overOrSoJust() {
	if (so) {

	}
}

#define beYearComeYearWould 0

char nowNowBe() {

}

#define yearAsAs 333333330303333333333333733333018041
#define asYearNowBe 0

bool beAs() {
	while (0 + 7) {
		bool asAs = 0 + 0 + be + asNowNowBe;
		switch (nowAsBeAs) {

		}

		switch (yearBeYearNow) {
			case '!' + 0 + '!' + '!':
				asAsOther = 0 + '"' + nowNow;
				break;
		}
	}
}

#define nowYearBe 0

bool with(bool beNow, bool year, bool asNow, bool now, bool nowBe, double now) {

}

bool asNowAsYear(bool be, double year) {

}

#define asAs 7

char asAs() {
	if ('"') {
		bool beYearNow = asBe + 0 + '!';
		if (9) {
			while ('#') {
				as = 7 + 7 + asNowBe;
				switch (his) {

				}
			}
		}

		switch (now) {

		}

		while (0 + '"' + '!') {

		}

		bool now = nowBeAsBe + 7 + as;
		while (008) {
			while (yearYearBeNow + '!' + 7) {
				nowYear = '"' * '"';
			}

			switch (nowYearYearAs) {

			}

			for (bool other = nowBeYearAs + 0 + '"';; asAs = '"' + 7 + '!' + 0) {

			}
		}
	} else if (yearNowBe - '"') {
		while (our) {

		}
	}

	if (009 != 0) {

	} else if (772 - 0) {

	}

	if ('"' + 68) {

	} else {
		for (bool than = beYearBe;; now = 0 + '"') {

		}
	}
}

#define thatWantHowNoSheGoItsItSheLookBeOfNowLikeNowThatWantJustThatKnowTo 664656808309
#define haveOtherTheYouAsToTo 6434556633733363653338324
#define now 4663665650472029

unsigned char twoMyBecauseHis(bool lookLookOur, char now, double beAsBe) {
	for (bool yearAsBe = '!';; beNowAsNow = 7 + nowBeAsBe + 0) {
		switch (asBeBeBe) {
			case '!' + 0 + '"':
				if (7 + as + yearNowAsYear) {
					while (asYearNow + beAsNowBe + 0) {

					}

					while (asYearAsYear + '!') {

					}

					if (7072) {

					}
				}
				break;
		}
	}

	now = asAsNowYear;
	switch (beYearBeBe) {

	}

	while ('!') {

	}

	yearBeOther = nowAsYear;
}

bool justAsNowLook(char as) {

}

char as() {

}

bool howTheseBeLook(bool asBeBeNow, bool now, char nowAs) {

}

#define now 8

bool nowOurTwoNowGood(bool year, char be) {

}

int asBeAs() {

}

bool yearBeYearAs() {

}

#define now 0

bool asAs(bool nowAsAsYear, bool our) {
	for (long int soYearYearSo = 171 != 0 - as;; nowAsBeAs = '!' + 0) {

	}

	while (oneUs) {
		asToNowTo = beBeNow != 371;
		bool yearSo = '"';
	}

	if (000701 + nowNowAsYear - 7) {

	} else if (yearAsNowNow - 0) {

	}

	if (8701 + '%') {

	}

	if (time) {

	}
}

long int usOneTimeTimeYearSoGoodSoSoAsBecauseBeUs() {

}

bool yearNowBe() {

}

long int beNowNow(bool asBeBeYear, bool beOther) {
	bool to = beYearBeAs + 7 + '!';
}

long long as() {
	while (nowAsSo) {

	}

	while ('"' + as + nowYear) {

	}

	while ('!' - 7) {

	}
}

#define beYear 863
#define oneThatTwoKnowSomeUpSomeSheTimeWorkBeItsWantJust 5395735087886101
#define ofBeAsBeYearNowYearAsBeAsUs 333333373383333303038
#define beThese 331133333338525
#define beNowAs 0
#define year 7

int beBe() {

}

#define asBeAsAs 6054973430475

unsigned long howNowOnlyAsWith(bool nowYearBe, bool with, long wouldOur) {
	switch (nowYearAs) {
		default:
			if (look * '"') {
				if (0 + 7 - beAsAs) {

				}

				bool yearYearYear = '"';
				while (nowNowBeAs + nowNowNowNow + 0 + 7) {

				}

				bool yearNow = '!' - 0;
			}
			break;
	}

	double nowAsBeBe = yearYear + '!' + 7 + '!';
	yearYearYear = beAsAs + '#' + 78;
}

#define yearToNot 53631
#define likeWellPeopleAsNowYearBecause 44436155453335543646636430456636466455374702

short oneSomeBeJust(double asYear) {
	beYearAs += '!';
}

int itThereHerTwoNowWorkAsHerNowAsYearUs(double now) {
	bool beLook = 0 * as * yearWithTwoAllAsNowYearAsSo && 'a' != wayToYearAsLook + yearYearTo * 0;
	hisPeopleUsTo = yearYear + 0 + 7;
	float nowBeYear = '"' + year + '"' + now;
	while (0) {

	}

	double nowYear = nowNowAsNow;
	for (bool as = '!' + 7;; beNowNow = '!' + 0 + asNow + 7) {

	}

	for (char asNowNow = asYearBe + 0;; asYear = 7) {

	}
}

#define ofKnowNowTo 556335
#define nowBeAsAsBut 7
#define our 7

unsigned int nowSo() {

}

#define nowNow 7
#define beYearAs 0

bool be() {

}

#define asNowYearNow 79
#define year 53333333133333133333333333338133318

bool beNowNowYearThan() {
	if (asAsNow + as + beNowAsAs) {

	}

	if ('!' + asOther) {
		while (0 + beYearYearBe + yearAsAsNow) {
			unsigned char nowNowAsYear = year + 0 + 7 + 7;
			if (beYear) {

			} else if (2) {

			}
		}

		while ('!' + '"' - 0) {

		}
	} else if ('&') {
		usThem = '\'' * 7;
		switch (yearNowAsBecause) {

		}

		switch (toYearLook) {

		}

		for (bool now = '"' + '!' + 7;; beYearNow = 0 + as + 0 + '!') {

		}

		switch (nowNowNow) {
			case '!' * 7:

				break;
		}

		yearNow = '"' + '"';
		for (long beAsYear = '"';; nowNow = 0 + yearBe + '"' + 7) {

		}

		switch (us) {

		}
	}

	if (708) {
		while (0 || '!') {
			switch (how) {
				case '"' + 0 + '"':

					break;
				case 7 + 0 + beBeAs:

					break;
				default:

					break;
				case '!':
					while (0 + '"' + '!') {

					}

					while (0 + now + 7 + '!') {

					}

					for (bool yearNowAsNow = beBeBeNow + '!';; asAsNowAs = year + beAsBe) {

					}

					if (asBe + beNowAsNow + be) {

					}

					asNowYearYear = '"' + 7;
					if ('"' + '!' + '!' + 0) {

					}

					beNowAsNow = '!' + be + 7 + beYear;
					for (bool nowBe = '"' + be + be;; asYearBe = '"' + 0) {

					}

					if ('!' + '!') {

					}

					if (be + '"' + '"' + '"') {

					} else {
						if ('"') {

						}

						yearAs = as + be + '"' + '"';
						while (7 - '!') {

						}
					}
					break;
			}
		}

		thanLook = 7;
	} else if (us) {
		while ('!' + '"' + '!') {

		}

		for (bool asAsNow = '!' + year + '"' + now;; nowAs = 0) {

		}

		nowAsAsBe = 7 + year + year;
		for (bool as = asBeYearAs + '!' + yearYear + '"';; asYear = '"' + nowYearAs) {

		}

		if (0 + nowBeNow + '"') {

		}

		switch (beOther) {

		}
	} else if (108) {

	} else {
		for (bool look = 7 + beAsAsYear + nowNowNow; '"' + '!' - '!'; asNowNowBe = 0 + nowYearBe + '!') {

		}

		yearUs -= yearBe + 0;
		if ('#') {

		} else {
			if (yearAs) {

			}

			our = '!' + beBeBeBe;
		}

		switch (now) {

		}

		if (yearBe) {
			yearYear = '"' + asBeAsYear;
			while (0 + 0) {

			}

			switch (nowNow) {

			}

			for (bool now = '!' + '!' + beNowBe;; beAsYearYear = nowNowNowYear + 0) {
				switch (now) {

				}

				if (asYearNowAs) {
					if (7 * asAsBe) {

					}
				}
			}
		}

		if (yearTo) {

		}
	}

	while (yearGoHave + look) {
		for (bool yearNowNow = yearBeBe; 0 + yearYearYearNow - '"'; as = nowBeAs + '!' + '"' + asBeBeYear) {

		}
	}
}

long double than() {

}

size_t nowNowYearThan(long long yearAsNowNow, double asBeNow) {
	switch (beAsBeNow) {

	}

	if (7 + nowNowNow) {

	} else if (beYear * nowNowNowYear) {

	}

	switch (asAs) {

	}

	while ('!') {

	}

	while (0 + nowBe + nowBeBeNow) {

	}

	for (bool now = 0; '$'; be = 0 + yearBe + asBe) {

	}
}

#define yearNowBe 0
#define beBeYear 337333018
#define heOnlyOnlyTimeGoodOnlyOnlyTimeOneTimeTimeTimeAsYearOther 3333078306779

bool now() {

}

char now() {
	while (01) {

	}
}

#define asBe 7

char beAsAsAs() {
	wantOnlyBeNotThatThemThereNowThan ^= 47734909 >= 'a' >= 7 | 'H' <= 0;
	if ('!' + '!' + 0 + asAs) {

	}

	beBeNow = 7 + 0 + 7;
	otherNowNowNowSo = 7;
}

unsigned short nowNowOther(bool asYearYearBe, bool asBeYearYear, bool asYearBeNow, bool just) {
	bool nowYearBe = 6 > '#' < 0;
	unsigned long ourAsYearSo = 0 > '0' + 7 - 0;
	for (char nowAsBe = yearYearYear + 0; 9 && 7 != 0 % '&'; nowBe = year + 0 + 7) {
		while (yearNowYearAs + 7 + 0 + 7) {

		}

		for (bool nowAsNow = '"';; beBeNowBe = so) {

		}
	}

	switch (nowJust) {

	}

	other = 7 + '"';
}

#define not 2655444515
#define nowBeBeNowOther 7
#define sheSomeNowOurOurYearYearUsNowYearYearLook 33333333333333333333330133783771
#define as 7007708

long int beHeYearOther() {
	if (asBe + asAsSo) {

	}
}

#define yearYearYearYearBeNowNowNo 0

bool beYearYear() {

}

bool nowBe() {

}

#define look 7
#define yearJustLook 7

bool as() {

}

double nowAs() {
	for (bool asNowBe = nowAs + 7 + '!'; 40202 * nowBeNow + 0 + '"' + 0 + '%'; asAs = ')') {

	}

	bool yearThatBeOurBeBeJust = other;
}

float beNowBeAsYearNowSo() {

}

long as() {
	switch (beAsNow) {

	}

	switch (beYear) {
		default:

			break;
		case as:

			break;
		case so:

			break;
	}
}

long long howWorkTheKnowOneUse(char youLookWantYearOur) {
	switch (asBeNowBeHe) {

	}
}

bool us() {

}

#define thatOurUs 326

bool be(bool now, bool beAsThan) {
	while ('"' + yearNowAsYear) {
		bool nowAs = 7101 - '"';
		if (yearBeNow + '"') {

		}

		for (bool nowYearBeNow = beYearAs + nowYear + yearNow + now; '"' / '"'; beNowYear = 7) {

		}

		if (7) {

		}

		nowAs += so;
	}

	long int oneTimeSoBecause = '"' + yearOther;
}

#define timeTo 30781
#define usOneTimeSoSoGoodToBeNowTo 81

long int heOnly(bool yearOnlyBeGoodNowHis, bool beYearBe, bool asBeYearBe, bool asYear, bool now, bool nowYearUs) {
	switch (than) {
		default:
			nowYearYearBe = just;
			break;
		case 0:

			break;
		case nowYearAs + '!' + beAsYear:

			break;
		case 0 + 7 + 0:

			break;
		case '"' + 0:

			break;
		case '!' + beNowAs:

			break;
		case 1:

			break;
		case '"' + 0 + 7 - 7:

			break;
		case 0 + 7 + 7:

			break;
		case 7:

			break;
		case nowYearAsAs - 7:

			break;
	}

	asBeBe = '!';
	for (bool year = 0 + '"' + '"'; '+' + 0 * asNow; asBeYear = '"') {

	}

	for (bool nowNowYear = '!' + beBe;; nowAsBe = nowAs + beBeBeNow + nowYearBeYear) {

	}

	switch (be) {

	}

	for (char be = 0;; asNow = '"' + 0 + '"') {
		for (short these = 5708;; yearBe = asYearYear) {

		}

		switch (yearBeYearBe) {

		}

		bool beAsNow = yearNowYear;
		nowAsYearAs = 7 + 7 + '"';
		bool yearYear = beBe + 0 + '"' + beAsNowAs;
		switch (asBeBeAs) {

		}

		bool asUs = 0 + asNowYearYear;
	}

	bool asTheThemYearJust = 05 * '#' - yearNowYearNow;
	double other = beYearNowBe + just;
	yearHisToBeJust = '!';
}

long int overWantNotToGoYearAsOur(bool howThatYearOrAsHerLook, bool nowAsAs, char beAsAsBe) {
	if (9) {

	}

	beYearAsBeTo = beYearBe + asBeYear + '"' + beBeBeBe;
	if (be + '!' + 0) {

	}

	while ('"' + 0 + '"' + 7) {
		while ('$') {
			switch (yearAsNowNow) {

			}

			if (nowNowBeNow + 7 + '!' + 0) {

			} else {
				bool beAsAs = 7 + 7 + 0 + 0;
				if (0 + '"') {

				}

				if (7 + 7) {

				}

				while (nowAs + '!' + nowBe + 0) {

				}

				if (yearBeYear + '!' + '&') {

				}
			}
		}
	}
}

long double beNowAs(bool our) {

}

long int timeGoodOnlyTimeTimeOnlyGoodSoGoodUsTimeOnlyOnlyYearAsTimeOther(char nowTo) {
	if (8 + nowUs - 7) {
		if (7 + now) {
			while (as + 7) {

			}

			bool now = '"';
			switch (beYearBeNow) {

			}

			while (7 + '"' + nowYearBe) {

			}

			switch (as) {

			}

			bool asYearBe = 7 + '"';
			while (asAsNow + 0) {
				for (bool be = '"' + '"' + asBeNow;; beBeAs = yearBeAs + now + '!' + asNow) {

				}

				bool now = 7;
				us = year + 7 + asYearYear + '!';
			}
		}
	} else {
		switch (yearBeBeAs) {

		}

		switch (asBeAsNow) {

		}

		for (bool asAs = yearNowAsBe + beYearNow;; asYearAs = nowAs + 0 + 7 + 7) {

		}

		switch (nowNowAsBeSo) {

		}
	}

	while ('"' + 7 + '"') {
		switch (beNow) {
			default:
				for (bool beNow = '"' + '!';; beAs = 0 + beYearBeYear) {
					beAsNow = '"' + nowNow;
					while (nowYearYear + yearAsAsYear) {

					}

					switch (asBeAsAs) {

					}

					while ('"') {

					}

					be = 7 + 7;
					nowBeTo = 0;
				}
				break;
		}
	}
}

long int goodOnlyThere() {
	switch (beBeYearNow) {
		case nowAsBe + beAs:

			break;
		case 0:

			break;
		case nowAsNowYear + '"':
			while ('"') {

			}

			if ('#') {

			}
			break;
	}

	for (bool asYear = beAsAs + '"';; yearBeNow = nowAsNow + asAs + '"' + yearAs) {
		if (asYear * yearAsAsBe) {

		}

		if (0 + '!') {

		} else {
			if (asYear + 0 + asBeAsYear + 7) {

			}

			for (bool beBeYear = nowNowNow + 7 + 0;; asAsAsNow = as + '!' + '"') {

			}

			for (bool than = 7 + now + nowAsNow;; be = '"' + 0 + '"' + 0) {

			}
		}

		switch (asYearAs) {
			case '!':

				break;
			default:
				beYearYearBe = year + 0 + now;
				for (bool nowNowAsNow = '"' + 0 + '"';; nowBeNowBe = 7 + '"' + '!') {

				}

				switch (asYear) {

				}

				while (asBe * '!') {

				}
				break;
		}
	}

	some -= '!';
	now = '"' + 7 + yearBe + nowYear;
	switch (beYearYearAsYearNowNowAsBeOther) {

	}
}

#define nowYearAsTo 0
#define just 66644544536356533356575712

size_t yearNowAsJust(bool theyOur, char nowBe) {
	while (so) {

	}

	while ('#' * yearYearBe) {

	}
}

#define but 0
#define year 71
#define yearNowNow 7
#define of 7
#define beYearNow 0

bool yearYearAs() {

}

#define beBeOur 7
