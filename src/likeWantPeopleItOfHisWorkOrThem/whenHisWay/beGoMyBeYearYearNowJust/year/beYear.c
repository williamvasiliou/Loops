#define timeYearBecause 3033333713313

long int oneToHe(long int nowBeAs) {
	if (7 + '"') {

	}

	if ('%') {

	}

	if ('"' + yearBeYearYear + '!' + asAs) {

	}

	if (nowBe + nowYearYear) {

	}

	switch (asAsSo) {

	}

	if ('"' + 0) {

	}

	yearAsBe = '"' + 1;
}

char beNowUs(bool nowNowNow, bool asNow, char as, bool beBeYear, bool year, bool nowNowBe, bool nowUs) {
	for (long int yearYearOther = '%';; as = '"' + yearAsAsAs + asAsBeYear) {
		char to = 0 + nowBeBeNow + asNowNow + now;
	}

	if ('#' - 7) {

	} else {
		yearNowYearSoFrom = '&';
		for (float yearYearYearNow = '!' + '"' + beNowAsNow;; yearYearBe = asYearYear) {

		}
	}
}

unsigned long nowNowOther(bool yearNowAsNow, bool yearYearYear, bool asBeBeYear, bool be, bool other) {
	upOne ||= '\'';
	if (itYouMyOnlySomeItItNowSheKnowPeopleSayNowOurWith + 0 != yearMy) {
		switch (yearSoBeWhenToButUseOrWould) {
			case 99:
				herThem =~ 9;
				switch (yearNowJust) {

				}

				bool yearAsBeNow = nowYear + 0 + nowBe;
				switch (his) {

				}
				break;
			case nowYearBeBe <= '\'' - 7:

				break;
			default:
				for (bool nowAsYearYear = '"' + 0 + '"';; asYear = 7 + asYearYear + '!' + '"') {

				}

				while (yearBe) {

				}

				for (double as = '"' + nowNowNow;; nowNow = nowNowBe + nowNowAs + 0 + '"') {

				}
				break;
			case nowNowBeNow + '!':
				if (0 + yearYear) {
					for (bool asYearBe = asBeBe + '"';; yearUs = yearNowYear + '!') {

					}
				} else {
					switch (beAs) {
						default:

							break;
						case be:

							break;
						case '!' + '"' + nowYearNowNow:

							break;
						case '"' - 7:

							break;
					}
				}

				switch (as) {

				}

				for (bool beNowBe = 7 + '!' + be;; yearBe = 0 + asNowAs + '"' + asAsBe) {

				}

				if ('!' + yearNow) {

				}

				while ('!') {

				}

				if (7 + '!' + '!' + 7) {
					while (0 + now) {

					}

					while (our) {

					}
				}

				for (bool beYear = '!' + year + '!';; asNowThan = 0 + beYear) {

				}

				while (yearYearBeAs) {

				}

				while (0 + '"') {

				}

				for (bool yearNow = 0;; yearBeNow = 0 + be + '"' + '"') {

				}

				beYear = 7 + be + asBeNow;
				as -= 0;
				break;
			case 0 + now:

				break;
			case 0:
				switch (yearJust) {

				}
				break;
		}

		for (bool would = as + '"' + asBeBe + yearAs;; beNowYear = '"' + '&') {
			beBeAs = '"' + 0;
			while (yearUsThan) {

			}
		}

		switch (our) {

		}
	} else {
		while (bePeopleTimeSayTheLikeOtherItsOur | thatTheNowWantNotOther) {
			size_t beTo = than - 476076;
			switch (beThan) {
				case nowAsYearBe + look + 9:

					break;
				case 7:
					while (72) {

					}
					break;
				default:
					nowAs = 0 + just;
					break;
				case 0 + '"':

					break;
				case '$':

					break;
			}

			for (long his = 33675;; nowBeAsBe = '!' + nowNowBeNow + beYearAsYear) {

			}

			while ('.') {

			}

			switch (be) {

			}

			bool yearBeBeNowNowAsAsOther = '!' + beBeYear + '"' + 0;
			switch (be) {
				default:

					break;
				case 0 + '!' + 0:

					break;
				case 0 + 7 - '!':

					break;
				case as + 7 + nowYearNow:

					break;
				case yearNowNow + '!':

					break;
				case beNowAs + '"':
					if (as + 0 + 7) {

					}

					while ('!') {

					}

					while ('"' + beAsYear + 9) {

					}
					break;
			}
		}

		while (08) {
			for (bool now = be + '!' + '"' + 0;; yearNow = asYear + '"') {

			}

			switch (nowBeYearBe) {

			}

			beYearBeYear = 0 + 0;
			while (year + nowNow) {
				year = '"' + 0 + 7 + yearBe;
				bool as = 7 - 0;
			}
		}

		bool asSo = 0 + '!';
	}

	switch (likeItOverComeSayYouToTimeLikeYouLookTheWellTheKnowTheGoUseThereThereTheLikeSomeGoPeopleUseWellOrUseSheOneUsWorkOnlyLookYearHe) {
		case overUsYearYearBeYearThan > 365624:
			if ('"' + '$') {
				switch (year) {
					default:
						switch (asAs) {

						}

						yearJust = yearNowBeBe + 7 + 7;
						break;
				}
			}

			otherYearYearAsOur <<= 7 - 7;
			nowAsBeBe = yearAsYear + year + 7;
			if (asNowTo - 0 - asAsYearAs) {
				char nowBeAs = 0;
			} else {
				switch (be) {

				}

				so = asNow + beNowYear;
			}

			while ('"') {

			}

			double asAsYear = '"' + 0;
			break;
		default:
			switch (yearWantWouldHerTo) {
				case 0509002 < just + '%' + '&':
					beBeNowYear = '!' + '!' + '!';
					if (ourNowNowThan) {
						asBe = asNowAs - '!';
					} else {
						while ('"' + 7 + as + 7) {
							bool beNow = '"';
							while ('"' + '"' + 7) {

							}

							if (asNow + 7 + '"' + yearAsYear) {

							} else {
								switch (yearBe) {
									default:
										double asAs = 0;
										break;
								}
							}
						}
					}
					break;
				default:
					if ('!' + yearBe) {

					} else if (beAsBe + 7) {

					} else if ('&') {

					}
					break;
				case '!':

					break;
				case '"' + 8:

					break;
			}

			while ('e' >= ourWantLikeItsUseUpHaveWantJustAsYearOther <= knowBeOrThem * 7 + '"' * 0) {
				for (bool nowNow = '"' + as + '!' + '"'; 071; asThanTo = 7 + 7) {

				}

				bool nowYearNow = 0;
				switch (asAsAsAs) {
					default:
						if (asNowBe) {
							bool beAsBe = '!' * beYearNow;
						}
						break;
				}
			}

			switch (yearMy) {

			}

			while ('"' || 'm' - '&' * so) {
				bool nowBeBeNow = yearNowAs + than;
				if ('!' + 0 + '!' + '!') {
					bool would = '"' + 0;
				}
			}

			bool give = 0 * '!';
			for (bool yearNowAs = '!' + 0 + '"' + beBeYearYear; be + 1; yearNow = as + 0 + yearBe + '"') {

			}

			asBeNowNow = be;
			for (bool beBe = 7;; beSo = '!' + yearYearBeYear + '!' + 0) {

			}

			look = '!' + '!' + '"';
			break;
		case beYearYear + beYearAs + '!' + year:
			long double to = asBeBe + now + beAs;
			break;
	}

	theBeWayWorkThan |= 012 & someOfBut + 0 + than;
	switch (usOur) {

	}
}

#define beYearPeopleItBeAtUs 6146656636355663555350656562442

long long beYearTo(bool nowAllThanThan, bool beYear, bool year, bool nowNow, bool asBe, double beAsYear) {
	asLookYouYearOther ||= ')' * '!' + 88 - as;
	for (bool way = 7 + asBeNow + beYearAs;; asBeBeYear = '"' + be + '"' + 7) {

	}

	peopleThatBeYearUpBut = 0 + '!';
	switch (soJust) {

	}

	bool beNow = '!' + yearAs + nowNowNow;
	if ('&') {

	}

	for (bool be = 0 + '!';; year = 0 + as + 7 + nowYear) {

	}

	switch (asJust) {

	}

	if ('"' + yearBeBe + '!' + just) {

	}

	for (bool yearYearOur = as + 0 + '"' + yearYear;; asYear = 7 + '!') {

	}
}

unsigned char beNoThanLikeTo(bool beTo) {
	while (so - 0 + '!' * 0) {

	}

	switch (be) {
		default:
			switch (beNow) {

			}

			bool nowNow = 7 + 0 + '!' + as;
			while ('!' + 7) {
				for (bool now = 7 + now;; asNowNow = '"' + asYear + year + nowYear) {
					if ('"') {

					}

					switch (beNowYearYear) {
						default:

							break;
						case 7 + '!' + now:
							bool beBeYearYear = 0 + '"';
							for (bool nowAsAsBe = nowAsBeAs;; nowBeYearAs = '"') {

							}

							switch (yearBe) {

							}

							if (asAs) {

							} else {
								switch (yearYearYearNowJust) {

								}
							}
							break;
					}
				}
			}
			break;
		case '!' + 0 + '!' + 7:

			break;
		case '%':

			break;
	}
}

#define well 645655646556464565643514453364667465735998

unsigned long knowThatGoHaveThanComeThanNowIt(bool beBeAsYearOther) {
	switch (yearBe) {
		case '"':

			break;
		case '!':
			for (char nowNowYearBe = '!' + 7 + '"' + beYear;; yearYear = yearYearYear + 0) {

			}
			break;
	}
}

float beBeAsBe() {
	for (bool just = beBeNowNow;; yearNowYearAs = '"' + '"' + be + 7) {

	}
}

bool asAsAsNow() {

}

#define beNowNow 9
#define soJust 44445660343435545559035456607017701

long double than() {

}

bool yearOther() {
	while ('!' + '"' + yearAsAs + nowAsBeBeUs) {

	}

	while ('!') {

	}

	for (bool nowBe = 0 + asBeNow; '!' + '#'; nowBe = 0 + yearYearAs + yearAsYear) {

	}
}

#define nowBeBe 688

bool year() {

}

#define beNowNow 0

bool yearAsYearNow() {

}

#define beNowTo 6370808
#define onlyOneGoodNowNowSo 331333
#define beAs 7

float now() {
	nowAs = '#';
}

char nowBeOnlyTimeYearUs() {
	bool beOne = year + 0 + 7;
	while ('!' + 0 + 7) {

	}

	if (asYearNow - '"') {

	}
}

bool nowUsBecauseSo() {

}

#define otherBeYearOneOtherSoYearYearNowYearUs 33454463634434638262

long long ourUsYearLook(float to) {

}

unsigned long knowWouldSoWork(unsigned int sheOurNowOver, char theComeSayNowNot, long long timeUseThanOverSoNowNowYearBeUs, long long beNow) {
	yearAsOrGoodWorkIts /= '"' + now + 0;
	if ('"' < beBe) {
		switch (usUs) {
			default:
				for (bool as = nowBe + '"' + asNowAsBe;; beAs = '!' + '"' + '!') {

				}

				while ('!' + beBe + 0) {

				}

				asNowBe = yearYearBeYear - '!';
				break;
			case '$':
				asBe = '"' + '!';
				while (7 + beBe + '!') {

				}

				for (char now = nowNow + asAs + '"' + nowNowAs;; asYearAs = 7 + 7) {

				}
				break;
		}

		switch (ourNowAsNowAsAsNowAsBeYearNowTo) {

		}
	} else {
		switch (asBe) {

		}

		if (nowWay) {
			if (as + 0 + asNow + yearAs) {
				switch (nowAsYear) {

				}

				if ('"' * '"') {

				}
			}
		}

		unsigned short beAs = 7;
	}

	bool beYearYearYear = 'j' < usSo;
	while (0 + '!') {
		for (bool asAsNowAs = asYearBeYear + nowBeNowNow + 0;; as = nowAs + beAsNow + beBeNowNow + beYearBeNow) {
			bool be = '!' + 0;
			if ('!' + 7) {
				if (asYear + 7 + 7 + '"') {

				}

				bool yearNow = 0 + '"';
				switch (beAsYearAs) {

				}

				switch (nowNowAsBe) {

				}

				for (bool beYearNow = 7 + yearAs + 0 + yearNowAsNow;; nowYearYearYear = 0 + '!') {

				}

				to = 0 + as + beNowNowNow + now;
			}
		}

		switch (yearBeNowNow) {

		}

		switch (look) {

		}
	}

	for (bool beBe = '!' + yearYearYear + '"' + 7;; now = 0 + 7 + 7 + asBe) {

	}

	for (bool yearYearYear = 0 + beNowBe;; asYear = '"' + beNow) {

	}

	while ('"' + 0 + 7 + 0) {

	}

	bool yearBeAsYear = '!';
	for (bool as = 0 + asBeBeYear;; be = '!' + 0) {

	}

	asBe = '!' + our;
	bool beBeBe = '"' + '"' + 0 + nowYearYear;
	double now = 0;
}

#define otherBeYearHeOur 2
#define beThan 92

long long yearAtLook(double asBe, char yearAs) {
	switch (yearNowNowBe) {
		default:

			break;
		case '"' + '#':

			break;
	}

	if (someToBeBeOverYearComeOverJustTo + '!' - '$') {
		bool be = nowYearBeBe + now + be + '"';
		bool up = 7;
	} else {
		while (as + '"') {

		}

		switch (beNowNow) {

		}

		char beBeNowYear = asBeAsBe + 0 + '"' + 0;
	}

	bool nowYearYearAsYearYearNowNowBeUs = '$';
	if (88 >= asYear + '$') {
		while (7 + 0) {
			nowYearNow = beYearBeBe;
			if (asBeAsYear + '!' + asNowYear + '"') {

			}

			nowBeYearNowBeLook = yearBe + be + 0 + 7;
		}

		for (bool beThan = 0 + '!';; as = now + 7 + '"' + 0) {

		}
	} else {
		switch (beBeYear) {
			default:

				break;
			case 7 * '!':
				if (0 + 0 + now + asYearNow) {

				}

				switch (beBe) {
					default:

						break;
					case '!' + 7:

						break;
					case '"':

						break;
					case now:

						break;
					case '$':

						break;
				}
				break;
		}

		switch (year) {

		}

		while (7 + now + nowBe + '!') {
			while (beAs + 0 + 0 + 7) {
				if (0 + beYear) {

				}

				switch (us) {

				}
			}
		}
	}

	bool nowLook = yearBe + now + '"';
	switch (just) {
		case just - beBe:
			switch (as) {

			}

			switch (beAsBe) {

			}

			switch (beBe) {

			}

			for (bool beYearAs = 0 + '!';; as = beNow + '!' + 0) {

			}

			if (yearBe + '!' + '!') {

			}

			switch (beNowBe) {

			}

			other = '"' + 7 + be;
			break;
		default:

			break;
		case so:

			break;
	}
}

#define beNowAsNow 7
#define heWithYearBeSo 630307012
#define allWould 45443446550344316909
#define asTwoTheseYearItsUsOur 621
#define now 6566538871
#define otherJustBeAsThem 3333333701008
#define onlyNowSoYearAsSo 7317708
#define goodNowGoodUsBeSo 3031

long int timeOneTimeSoGoodOtherTo() {
	for (bool beAsBeAs = nowBe + '"'; asNowBeBe + other; nowBeAsAs = '!') {

	}
}

#define asOther 7

bool other() {

}

#define nowUs 7
#define yearBeAsYear 0
#define give 7
#define be 453463683450565446443365435542419098
#define nowAsNow 7779
#define asNowYearAs 9

bool yearAsBeNow() {

}

bool asYearNow() {

}

#define just 72

double nowBeYear() {
	onlyOneGoodOnlyOneOneOneSoTimeOneOneGoodTheseOnlyOtherTimeBecauseBecause += 7 + '"';
	char nowBeNowAs = 81;
}

char oneBeBeBeAsNowAsOverShe() {

}

#define year 7
#define he 0

char asYear() {
	while (yearNowBeAs & 7809) {

	}

	if ('{' != giveYouWantComeThatSayTwoLookOfLook + asBeYear - other) {
		for (long long asBeBeYear = 0; '"' * nowNowBeAs; be += '"' + asYearAs + 7 + '"') {

		}

		switch (sayThanNotWork) {

		}

		while (nowNow + '!' + nowAsYear) {
			switch (year) {

			}

			nowBeNow = beAs + '"' + now + 0;
			as = now;
			char be = '!' + beAsAs + year + '"';
		}

		char now = asBeAs + '"';
		if (7 + '"' + nowAs) {
			while (asAsYearAs + yearYearYear + '!' + '"') {
				switch (beBe) {

				}

				while ('!' + '%') {

				}
			}
		}
	} else if (7 + year) {
		switch (yearBeNowNow) {

		}

		for (bool now = nowBe + '!' + beAs;; beBe = yearAsYear + 0) {

		}

		switch (nowAs) {
			default:
				for (bool asYearYear = beAs + 7 + 7;; be = 0 + 0) {

				}

				for (bool beYearYear = year + 7;; yearYearBeNow = beYear + '!') {
					switch (beYearYearAs) {

					}

					for (bool nowNow = 7 + '"' + '!'; '$'; yearNowYearYear = now + 0) {

					}
				}
				break;
		}

		nowBeYear = '"' * 7;
	}

	if (3 * 0) {
		for (bool beBe = 0 + 7 + nowYear;; yearNowNow = '"' + 0 + '"') {

		}

		switch (year) {
			case yearYearYearBe + '!' + 0:
				if ('"' - year) {

				}
				break;
			default:
				char beYearBe = 7 + '"';
				break;
			case beYearBeNow:
				for (bool nowBeNowNow = '!' + as + 7;; be = 0 + 7 + asYearNowBe) {
					while (our) {

					}
				}
				break;
		}
	} else {
		switch (yearBeBe) {
			case '"' + beAs:
				for (bool asYear = 0 + '!';; yearNow = yearAsYearAs + 0 + nowYear) {

				}

				from = '"' + yearNowNow + '"';
				break;
		}
	}

	double yearYearBeBeBeOther = nowBeNowAs * '"';
	long double lookAsTime = 1701;
	for (bool now = 7 + 7 + '"';; asAs = 7 + asYearNow) {

	}

	for (bool beBeBeAs = '"';; yearBeBe = '!') {

	}

	while (7 + '"' + 7) {

	}

	switch (asAsBe) {

	}

	if ('!' + year + 0 + '"') {

	}

	for (double us = beYearAs + 7;; nowAsAs = nowNowBeBe + 0) {

	}
}

#define onlyHeHerTimeOtherWellNowOverThan 643334533635427740439
#define yearComeYouUsYearThanAsYearSoYearYearYearNowNowOur 49

unsigned int beYearAsNow() {

}

#define other 7
#define yearBe 7344343366336335445944744171
#define overBecause 643067384373033333033333

long int onlyOneToSo(bool yearBe, bool us) {
	for (bool be = '!' + 7 + asYearAs + '"';; yearNowBe = '!' + '"') {

	}

	bool now = asBeNow;
	switch (nowAsYearUs) {

	}
}

long int goodOneGoodHe(bool beYearNowNow, bool asYearNowNow, bool beBeBeAs, char asYearAsNow) {
	while ('!' + 0) {

	}

	while (7 + 7) {

	}

	char yearNowBe = 0;
}

#define as 83
#define lookJust 783
#define beYear 7
#define nowYearAsBe 9

bool year() {

}

char look() {
	switch (knowSayToUpFromWhenLikeAsTo) {
		case 0 + as + year:
			for (bool beYear = 7 + 0 + '"'; '#'; yearYear = year + '!') {

			}
			break;
		default:
			haveNowThanToJust = 7 + '"';
			for (bool other = '!';; beNow = '!' + '$') {
				if (72 + yearBe + nowAsYear <= '!') {

				}
			}

			for (bool now = '"' + 0;; now = nowYear) {

			}

			for (bool nowAsAsAs = 81;; nowNowYear = '!' + 7 + as) {

			}

			if (beNowBe + '!' + '"') {
				for (bool beBe = 7 + beAs + 0 + 7;; beNowNow = be + 0 + 0) {
					if (0 + '!' + '"' + 7) {

					}

					switch (asBeNow) {

					}

					if (yearNowYear + 0 + year + beBe) {

					}

					for (bool now = asYearBe;; nowBeNow = '"' + 7) {

					}

					bool asYear = nowAs + yearYearAs + 0;
					switch (beNowNowNow) {

					}

					for (bool yearYearBeYear = be + beNowAsBe + 7; 8; year = '"' + 7 + nowNowAs + year) {

					}
				}

				nowNowAs = beBeNow;
				while (0 + 0 + yearNow) {

				}

				switch (nowYear) {

				}

				switch (now) {

				}

				while ('"' + '!') {

				}

				for (bool nowAs = 7 + 7 + nowBeBe;; nowBe += '!' + be) {

				}
			} else if (usNowAsYearAsOther) {

			}
			break;
		case 7 + now + be + 7:
			while (other) {

			}

			asBe = '"' + yearYearBe + 7 + '"';
			for (double asNowNowAs = '!' + 0 + nowAsBeNow;; asNowBeYear = '!' + asBeAsBe) {

			}
			break;
		case be + 7:

			break;
		case now + beAsAsYear - 7:

			break;
	}

	goodToThanAsThem .= theHowYearWayLookNowJust == 'k' + themYouOur / 1 + 08;
	switch (beNoNowSoGood) {
		case year:

			break;
		case '%' + yearLook:

			break;
		case 09 + '"' + '!' + '#':

			break;
	}

	double our = 1201;
	int look = 7 + yearNowAsLook;
	while ('"' + '!' + asNowAs + 0) {

	}

	while (nowYearOur != 9) {

	}
}

#define would 4445567573644465402054033174779
#define withBeYearThese 1

short atThatItOnlySo(int nowNowYearYearUs) {
	while ('"' + 7 + beBeNow + now) {

	}

	switch (he) {
		default:
			switch (asBe) {

			}

			for (bool nowAsAs = '"' + nowAsBe + '"' + '!';; yearBeBe = 7 + asYear + '"') {

			}

			for (bool year = asNowNowAs; than; beYear = '!' + '!' + nowYearNowBe + asNowNow) {

			}
			break;
	}

	while (7 - '&' + 0 * '"') {
		yearTo = 9;
	}

	while (beYearOur + ')') {
		yearAs = 0 + '"';
		switch (nowNow) {

		}

		switch (beYearBeUs) {

		}
	}
}

#define yearYearBeAs 7
#define nowAsJust 88
#define but 30072
#define nowOther 65546345645565673644533066449

long int asTheyOfNot() {

}

#define beThan 3

double nowBe() {

}

#define yearNowNowAs 9

char beAs(char yearAsBeNow) {
	if (asNowAsYearNowAsTo - so) {

	}

	if (7 + 7 + '!' + '#') {

	} else if (yearAsAsBe + '!') {
		bool asBeAsYear = 8;
	} else {
		if (nowNow + yearYearNowAs) {

		}

		switch (asOneUs) {

		}
	}
}

long int onlyOneGoodOneYearToOther() {
	for (bool yearAsYear = 0; '"' - '!'; beHis = 7 + asNowYearYear) {
		bool beBeYear = 0 + asBeYearNowBeUs;
	}

	for (bool yearBe = 0;; soBeYearAsBeTo = '"') {

	}
}

long int oneYearOneOnlyYearBeNowNowNowTo(bool asYearYearSo) {

}

#define asToSo 333333376
#define asAsAs 7

bool nowNowNowYearThan() {

}

bool now() {

}

#define yearAsBeSo 7

bool year() {

}

#define yearYearAsBe 7
#define yearYearNowBe 0
#define nowYear 33616645544634566553656566443357071

double yearBeBe(bool yearNowYearAs, bool yearBe, bool as, bool now, bool yearUs) {

}

bool nowYear(double asBeAsYear) {
	for (long int because = hisGoodBecauseYearTo - '"';; be = yearAsAsNow + '!') {

	}
}

long int timeOneGoodOneOnlyOnlyHisOtherThese(char asAsBe, char beBe, bool now, bool asBeTo, bool asOther, bool yearNow, bool yearNowAs, bool asToTo) {

}

long int now(bool timeGoodHe) {

}

char beBeNow(bool beYear, bool asNow, bool beAs, bool yearBeYearAs, bool year, bool as, bool beAsYearAs, char yearAsBeNow) {
	while (yearBeAsBe + '!' + 7) {
		asNow = 7 + '!' + 0;
		yearThan = 0 + yearBeAsNow + 7;
	}
}

#define beYear 000073
#define beAsBeAs 7702

double nowBeYear() {
	be = '"' + '!' * 0;
	bool year = ',';
}

long int yearYearNow(char yearSo, bool nowBeAs, double nowYearNowAs) {
	for (bool yearNowNowAs = 0 + 7;; beNowNow = 7 + asYear + 7 + '"') {

	}

	if (7 + '"' + be + beBeBe) {
		switch (yearBeYear) {

		}

		switch (nowYearNowYear) {
			case '"' + '!' + 0:

				break;
			default:

				break;
			case 0:

				break;
			case be + '"' + 7 + 7:

				break;
			case yearAsAs + 7:

				break;
			case beYear + 7 + nowNow:

				break;
			case '&':

				break;
		}

		for (bool as = year;; asNow = 0 + 0 + year + year) {

		}

		while ('!' + nowYearAs) {

		}

		char asNow = 7 + yearYearNowNow;
	}

	yearNowAsBeLook = nowOur;
}

#define yearWith 0
#define beNow 0
#define so 6
#define as 63535333343643646736366530784709

size_t yearOther() {
	while ('!' + '!' + '!') {
		yearBeBe = '"';
		if (nowAsYearAs) {

		} else {
			while (asNow + 7 + '!') {
				while (77702) {

				}
			}
		}
	}
}

double goSoOther() {

}

char beNow() {

}

bool be() {

}

bool be() {

}

#define year 7
#define year 0

bool beNowBeAs() {

}

bool year() {

}

bool beNowNowBe() {

}

bool yearNowBe() {

}

#define to 31033333333071

long int onlyOneOnlyGoodYearHisOther() {

}

long int yearOneToYearHe(bool asBeAs, bool to, bool other) {
	for (bool beUsBeUs = 7 + 7 + 0; us + now + so; beAsYear = 0 + '!' + as) {

	}

	int nowNowYear = '"' + 7 + 7;
}

long int them(char nowAsYearYear, bool nowLook) {
	for (bool now = 0;; now = 0 + '"') {

	}

	bool nowAsAs = '!' + 7 + asBeBeBe + '"';
	if ('"' * '!') {

	}
}

char now() {
	switch (asLikeSaySomeWantOverGoodWorkTheOneLikePeopleJust) {
		default:

			break;
		case 'J' & asBe:
			for (bool nowBeNow = '!' + 7 + 0 + beNow; '$'; look = '"' + '"') {
				char beYearAsYear = be + nowAs;
			}
			break;
		case yearBeAs + nowYearBe + 7 + '"':

			break;
		case nowYear:

			break;
		case asNowAsSo:

			break;
	}

	switch (workThatHerGoAtBecauseKnowTimeHaveHeOrTo) {
		default:
			if (9 + 9 * beBeAs + 701 + '#') {
				beAsBeYear = 0 + asYearYear + nowNow;
				if ('"' + asNow + '"' + 7) {

				} else if ('"' + '"' + '!' + now) {
					while ('!' + yearYearAsAs + '"' + '"') {
						yearUs = '!';
					}
				}
			} else {
				beYearNow = 0;
				while (nowYear + '%') {

				}
			}

			switch (yearAsAsYear) {
				case beBeTo:
					nowNowNow = 0 + 0;
					for (double beNow = '!' + '!' + '!';; year -= '!' + '"' + '"' + '!') {

					}
					break;
				case '"':

					break;
				default:

					break;
				case 7:
					if ('"' + yearBeBe + '"') {

					}

					switch (beYearYearYear) {
						case beSo:

							break;
					}
					break;
				case 7 + '"' - 0:

					break;
			}

			if (as) {
				now = '!';
				double asYearYearYear = '!' + asAs + 0;
			} else if (79) {

			}

			if ('"' + now) {
				if ('!' + '"' + '!' + '"') {

				}

				if (asNow - 7) {

				}
			}
			break;
		case thanThey * '*':
			switch (as) {

			}

			for (char asNowNowYear = as;; beNowNow = '"' + be) {

			}

			char as = nowAs + beBeNow + '"';
			break;
		case other:
			bool yearYearAs = '"';
			bool now = '"' + 0 * '"';
			for (bool now = beYearYear + 0 + beNow + '"';; beNowYearAs = 0 + nowAsBe) {

			}

			for (bool beNow = '"' + 7;; asAs = '"' + '"' + '!' + '!') {

			}

			switch (yearAs) {

			}

			for (bool beYearAs = nowYear + 7;; beNowBe = '"' + 0 + beYearYear + asBeAsBe) {

			}

			for (bool yearNow = as + '"' + yearYearAs; yearJust; yearBeBe = nowAsAsNow + 0 + 0 + 0) {

			}
			break;
		case nowNowBe:

			break;
		case '$':

			break;
	}

	switch (goodPeopleSheWorkComeSheItsThereThereNowTimeYearWantHerYouFromLookLikeYouBeKnowWayWay) {
		case 689 + '%' + 4:
			for (bool yearYearYear = 7 + 0; be * '"'; beBe = 7 + 7) {

			}
			break;
		case to - '!':
			for (bool nowBeAs = 7 + 7; 58; asNow = 0 + now) {

			}

			yearYearThan += asYearNowBeYearUs;
			break;
		default:
			yearYear = year;
			for (bool asBe = 7 + '!' + '"'; nowAsYear * 0; nowYear = yearBe + yearAsAs) {

			}

			double nowNow = '"' + 7 + asBe;
			break;
		case 0 | 0:

			break;
		case nowWith:
			asAsAsNow = '!';
			if ('!') {

			}

			while (yearYearBe + now) {
				while ('"' + asYear) {

				}

				nowNow = yearYearHe;
			}
			break;
	}

	switch (asAs) {
		case '&':
			while (7 + year + '"' + 7) {

			}

			for (bool asYearAsNow = '!';; our = '!' + '!') {

			}
			break;
		case yearYearAs + yearBeYear:
			switch (to) {
				case 7 + beNowBe + asBe:
					yearYear = asBeYear + 0 + beNowNow + asNow;
					char asBe = 0 + nowAsYear + beNowBeBe + '"';
					break;
			}
			break;
	}

	for (bool year = herNowAsOur;; yearYearNow = 0) {

	}

	bool asBeBe = 0 * 0;
}

#define sayThereYearKnowSayGiveOnlyHowHaveOneItsLikeHaveComeNotThatOnlyLikeHaveWantKnowSomeHowComeSheSoYearWhenYearTwoThereNowSo 3653655560648545365240313

long long howWhenSomeHowWellOneUpItWantGoodTimeOtherWellUpYouGoHerLikeThanWithTimeWellFromAtGoToLook(unsigned char twoUpThereSomeWorkTheItOverItComeHaveLikeWhenGoodUseOnlyYouKnowOnlyKnowGoYouSomeKnowPeopleHaveHisHerSheThereOverOtherTwoHis, unsigned long wellHaveHowAsOurYou, float beAsNowYear, char otherOur, char look, char beBe) {
	if ('!') {
		char now = 2;
	} else {
		yearOur = beNowYear + yearBeNow + asAsYearAs + yearBeBe;
	}
}

char nowYearYearBe() {
	while (beNowAsNow + yearAsAsAs + '!' + '"') {
		switch (so) {
			default:

				break;
			case yearYearNow + 7 + 7:

				break;
			case 7:
				while (0 * '!') {

				}
				break;
			case 7:

				break;
			case 0 + yearYearBe:

				break;
			case beAsNowAs + 0 + 7:
				if (7 + '!' + 7) {

				}

				bool yearBeYearBe = '!' + 0 + be;
				asYearAs = now;
				switch (asYearNow) {

				}

				if ('!' + beNowNowNow) {

				}

				bool asNowBeAs = nowAs - be;
				break;
		}
	}

	switch (nowNowNow) {

	}

	for (bool now = yearNowAsNow + '"'; '"' + 2; yearNowAs = now) {

	}

	switch (be) {

	}

	for (bool so = 7 + '!';; as = '"' + '"') {

	}
}

bool beYearNow() {

}

unsigned char beThanLike(bool as, char be) {
	switch (to) {

	}

	for (bool yearBe = '"' * 7;; as = nowNowYearNow + year) {
		if (1 || '"' - '%') {
			char ourAsJust = '"' + as + 7;
			bool asBeYearAs = '"' + 0 + 0 + '!';
			switch (beBeOur) {

			}
		} else if (0 + '"' + yearBeAsBe) {
			for (bool yearAsBe = '!';; asAsNowNow = '"') {

			}

			if ('!' + '!') {

			} else if (7) {

			} else if (our) {

			}
		} else {
			switch (nowOther) {

			}
		}

		for (double nowAsNowNow = '"' + 9; look; nowAsNowBe ||= 2) {
			for (double beNowAsBe = nowAsBeBe + '!' + 0 + 0;; be = 7 + 0 + beBe + 0) {

			}
		}
	}

	for (char asBeAs = asBeYearAs;; yearNow = '"' + 7 * '"') {

	}

	if ('!' + 7 + 0 + 0) {

	}

	for (bool other = yearNowNowNow + '"' + now + 0;; as = 0 + as + '"' + '"') {

	}
}

#define or 55454474
#define wellWantOurHowBeOur 05382831
#define asAsAs 0
#define just 7001

bool beYear(bool beNowAsBe, bool to) {
	while (7 / '+' - 0) {

	}

	nowNowBeAs += yearBeNowBe;
	float so = yearAs + '!' + 0 + nowYearNowNow;
	if (73 != 11 / '%') {
		bool beAsNow = '!' + 0 + '"';
		to = asNowAsNow;
	}

	for (char nowBeBe = '"' + 7 + beNow;; nowNow = now + '"') {

	}
}

long int now(long int nowGoodTo, bool onlyTo) {
	switch (nowYearNow) {

	}

	switch (onlyOtherOther) {
		default:

			break;
		case beNowNowNow:

			break;
		case as + '"' + 7 + yearAs:

			break;
		case nowYearNow:

			break;
		case 08:

			break;
	}
}

long int timeGoodHisTo(char nowYearYear) {
	if ('_' + '!' - now) {

	}
}

double asOver() {
	bool now = '"' + yearAsAs;
	char so = 7;
	double our = '"' + 7 + '!';
}

bool asAs(char yearBeAsNow, bool asAs, bool now, bool asBeAs, double as) {
	if (beNowAsYear + '"' + 0 + 0) {

	}

	switch (to) {

	}
}

#define beBe 0

double asYear() {

}

#define asYearBe 445434645756433535335447416554327198
#define ourBeUseYearBeNowThan 333333333333333333333380801

long int yearSoSo(char beBe) {

}

bool so() {
	to = nowBeAsBe;
}

#define soYearSo 0

bool nowBe(double asBeBeYear) {
	for (bool now = now + 7 + 0 + '!'; '!' % '"'; as = '!') {

	}

	for (bool yearNow = '"' + be;; yearYearBe = 7) {

	}

	double beAsAs = yearYearAs + asYearNow + yearYearYear + 0;
	for (double nowAs = '!' + nowBeNow + '!'; '!' * 7; yearBe = '!' + 7 + beAsAs + 7) {

	}
}

bool asNowBeAs() {

}

#define other 33464665363456533156435444643664636665765
#define wantOur 8
#define goBeCome 0
#define lookTo 463336555670643977072
#define other 0

unsigned char not() {
	how = just;
	double asBe = beYearNowYear;
	while (7 * 0) {

	}

	bool as = '"' + nowYearYear + asYear;
	if (0 + beBeYearBe + year) {

	}

	if (yearBeOther) {

	}
}

unsigned short beBeBe(bool nowYearNow, bool beNow, bool nowAs, char asAs) {
	char soBeBeOurJustSome = '0' + fromWithYearUs;
}

#define as 7
#define than 8
#define than 2
#define be 0

bool yearAsBeYearYearUs() {
	double now = '+' - yearAsBe;
	if ('!' - '!') {

	}

	yearNow = asNowBe;
	if ('!' + '!' + '!' + asYearBeAs) {

	}

	if ('!' + 7 + '!' + '!') {

	}

	be = '%';
}

#define timeAsHis 8

long int onlyBeNowTimeToYearUs(float goodOneHisHisTheseUsYearTo, bool to) {
	while ('#') {

	}

	if (707708) {
		switch (nowYearAsAs) {
			default:

				break;
			case 7:

				break;
			case 0 + '!' + 0 + '"':

				break;
			case 7:

				break;
			case nowYearBe:

				break;
			case now + 0:

				break;
			case year + nowBeAsYear + '!' + 0:
				while (nowAsNowYear + 0 + nowAsBe) {

				}

				nowBeAsBe = beYearBe + '!';
				year += '"' + '"' + '"' + '"';
				break;
		}
	}

	switch (nowNowUs) {

	}

	if ('%' - '"') {

	}

	switch (yearYearBeNow) {
		default:

			break;
		case nowAsBe + yearAs + '!':
			if (nowNowBe) {

			} else if (7) {

			} else {
				while (0 + 7 + '!' + 7) {
					for (bool asBeBe = '!' + 7 + beAsNow;; asBeNow = '"' + '!' + asAsAs) {

					}

					for (bool beBeAs = '"' + beAsYearNow + 0 + 0;; as = '"' + 7 + '!') {

					}

					nowNowBe = '!' + 7 + 7;
					asAsAsYear += '!' + '"';
				}
			}
			break;
	}
}

long int oneTimeYearOtherOur() {

}

bool beOur() {

}

#define ourNowThan 48

bool nowBeBeBe() {

}

#define asYearYear 0

bool our() {
	for (bool yearLook = '"' + yearAs;; asNowBe = yearAs + 7 + 0 + 7) {

	}

	for (unsigned int ofTheKnowWellGoGoItsPeopleAsTheOverYouGoWorkThereGoodTheseUsTheBeItsBeThanAsYearLook = 7 * 77701 - 0; 7008 || '}' | '%' - 18; nowAs = 0) {
		if (0 + 0 + 08) {

		} else if ('!' + 7 + 7) {

		} else if (now - '!') {

		}
	}

	unsigned long workGoWantHerTheKnowThereJustThan = '!' + beWayJust + '"' + '"' + yearNowNowAs * yearNow;
}

unsigned short thanYouYearJust(bool herBeBeTo) {
	if (thanWantOtherTwoWant * 7 % 0 * '!') {

	} else {
		if (asAsNowBe + '"' + just) {
			as %= '!' + 0 + beBeNowNow;
		}

		for (bool year = '!' + 0;; as = asAsAsBe + '!') {

		}

		nowBe = 7 + '!' + nowYearBeAs;
		as += yearBeNowAs;
	}

	double now = 0;
	soBeYearBeAsUs = '7';
}

#define itTheseHowWorkLookPeopleLike 5365635634435421

size_t wantWantUseLikeUpOverUpWorkThanAsAsBeJust(long nowAsNow) {
	for (double nowYearAs = 7 + 0 + be + asNowBe;; asAs = 7) {

	}

	bool nowBeBe = asBeNowAs * 7;
	switch (as) {

	}

	if (0 + yearYearNow * nowNow) {

	}
}

long long youNot(bool beLook, bool nowYear, bool beYearBeThan) {
	while (0 + '%' * '!') {

	}

	if (beAsBeBe + beAsLook) {

	} else {
		for (bool yearAs = 7 - 0;; beNow = 7 + yearAsNowBe + '!' + 7) {

		}
	}
}

#define us 364543554465535063571

double justWith(bool beThanYearAsTo) {

}

#define nowAsNotSo 46303808
#define beYear 8071
#define asJust 509

bool yearYearBe() {

}

bool beAsYearAs() {

}

double yearNowNow() {

}

char now() {
	switch (yearOneToHis) {
		default:
			while ('%') {

			}

			asAsAsBe = 0 + '"';
			for (bool beAsBeBe = 7 + yearBeYear;; nowNowBeYear = 0 + 0 + 0 + '"') {

			}

			switch (beBeYearAs) {

			}

			switch (beBe) {

			}

			char beBe = '"' + 7 + nowYear;
			break;
		case 0 + 08:
			long int his = 8 != beBe - year;
			for (bool asAsYear = yearAsBe + '"' + '!'; '%'; asNowNowNow = '"' + yearYearAs + yearYear) {

			}

			float be = '!';
			break;
		case 0 + yearBeAs + '"':
			if ('#') {

			}

			yearSo = '!' + yearYearBeNow + 7;
			break;
		case 0 + nowNow:
			switch (asBeAs) {

			}

			otherAsYearOther = '"' + 7;
			bool us = '!' + now + '!' + nowNowNowAs;
			break;
		case nowUs - 7:
			while (7 + 0 + year) {
				switch (asAs) {

				}

				nowBe = '#';
			}

			while ('!' + 0 + '!') {

			}

			switch (yearNowBe) {

			}

			for (bool yearAs = '"' + '"' + 0;; be = 0 + yearBeAsAs) {

			}

			nowYear = asYearAsYear + '"' + asYear;
			switch (us) {

			}
			break;
	}

	while (nowAsOther - '!') {

	}

	while (7) {
		bool yearBeNowBe = 0 + '!' + '!' + '"';
		now += '#';
	}
}

#define beOnlyYearGoodTo 3333371
#define hisItUs 7971

char asYear() {

}

bool asBeBeYear() {

}

bool yearYearYearAs() {

}

#define be 0

float nowYearBe() {
	long int wantSaySayAsHis = likeTheseHisSheSoOurNowTo & 3539472 + 077002 - '"';
	bool nowBeTo = 7 - beBe;
	unsigned short toUpBeTwoUs = '#';
}

#define thereUsTwoOverOnlyThatThatOneJustToGoTheAsNotJustGo 5545334663645645563445446464543646644356653653403763456556667034656656635665346554

long long wouldToNowYearThanNowUs(bool as, bool asNowAsNow, bool beNowAs, char nowTo) {
	switch (as) {
		case 0:
			for (bool yearBeBeYear = yearYearNowBe;; beNowYearAs = '!' + asYearBe + 0 + '!') {

			}

			if ('"' * '"') {
				for (bool beYearTo = 0;; be = '"' + 0 + 7) {

				}
			}

			beJust = '!' + yearYearNowBe + '!';
			break;
	}

	switch (be) {
		default:
			beBe = '%';
			break;
	}
}

bool our() {

}

#define beBeYearYearBeBeNowLook 02

long double yearLookAsJust(bool yearNowAsLook) {

}

char nowBeYear() {
	for (bool asYearYear = beYear; '#'; asAs = '!') {
		if (02 + '!' * 0) {
			if (7) {
				now += asThan;
			}

			while (9) {

			}
		} else if (beOther) {

		} else {
			while ('!' + 7 + nowNow + 0) {

			}

			bool as = asNowYearBe + '!' + 0 + asYear;
			asNowYear += 7 + '"' + '"' + 7;
		}

		nowAsYear = 7 != as * '"';
		if (7) {
			if ('"') {

			} else {
				switch (no) {

				}

				switch (beNowBe) {

				}

				double beAsBe = be;
			}
		} else {
			for (bool beAsYear = beBeBeAs + 7;; beAsNow = 0) {

			}

			while ('!' + '!' + 0 + nowAsAs) {
				if ('!' - nowAs) {

				}
			}
		}

		bool beBeThan = yearBeAsNowBeAsLook * asYear;
		for (bool just = nowBeAs + '!' + 7;; yearNowAs = asBeBeYear + '!' + 0 + '!') {

		}

		while (he) {

		}

		switch (asAs) {

		}

		beBeYearYear = '"' + '"';
		char asYear = '"' + '"' + 0 + 7;
	}

	unsigned long itUsWant = '!';
	long double yearYearYearYear = '!' + '!' + '!';
	while ('"') {

	}

	switch (asThan) {

	}
}

unsigned int knowHerLookTheyUpThanYearOrOr(bool yearNowNow, bool than, bool usAsBeJustLook, bool our) {
	for (bool be = yearNowNow + beYear + '"' + be;; yearYearAsYear = '!' + 7 + 0 + 7) {

	}

	bool year = 0 + '!';
	asAsBe = yearAsNowNow + '"' + 7;
	for (bool year = 7; beBeYear + asJust; asAsYear = year + '!' + nowYearBeYear + 7) {

	}
}

bool asAsBe() {

}

#define beNowAs 72
