#include <UUU/UUEU//UUU/U6.h>
#include </.h>

bool these() {

}

unsigned char be(bool asAsNow, double asNow) {

}

#define now 0

bool beBeNowNow() {

}

#define beAsBeTo 50245747070002
#define otherYearBeThanJust 55

double year() {
	for (unsigned long yearSay = '(' * nowBeAsNow; '!' < '!'; year = as + 7 + '!' + now) {

	}

	for (bool year = '"';; asNow = 7 + asAsAs) {

	}

	yearAs = 7;
	bool be = asNowAs + 7 + 7 + '!';
	beAsBeNow = asAs + '!' + 7;
	char nowNowNow = '"' + 7 + as;
}

#define likeOverThemToAsNowToAll 480432507529
#define butItsHaveTheBut 4443055533333333333838078

bool beBeSo() {

}

#define goodTimeGoodOne 03738
#define heTo 30801
#define timeOtherSoOther 033
#define as 7

bool asBeNow() {

}

double nowYearYear() {

}

#define asBeBeOther 360655

unsigned short asBeGoodGoAsUsLookYearThan(double my, double yearNowAsNow, double yearBe) {
	while (70772) {
		asBeAs = '"' + '!' + '"' + beBeAsYear;
		if (beNow + beAsBe) {

		} else if (yearBe) {

		} else {
			for (bool yearYearBe = 0;; beAsNow = '"' + '"') {

			}

			bool nowAsNowNow = asNowYear + '"';
			bool yearAsAsOur = yearBeYear + beNow + '!' + 0;
		}
	}

	toHeNowLook &&= 56701708;
	unsigned short as = '&';
	for (short asNowYear = 7 + '!' + '"' + nowNowAsYear;; nowNow = beAs) {

	}

	for (size_t nowNowBeAs = '!' + yearNowAs + '"' + '"'; asYear - 0; asBeYearNow = 7) {
		for (bool than = 0 + asAsAs + 0;; yearYear = 0 + '"' + nowYear + asBeAs) {

		}
	}
}

unsigned long our(double nowAsYearAs, bool us) {

}

#define nowBeBe 0

bool as() {

}

#define hisAsLook 33337333333038

long int soGoodTimeYearTimeGoodOneTimeOtherUsSo(bool year, char beBe) {

}

#define soNowBeHe 08

float goodAsOne() {
	for (bool yearAsBeAs = year + '"' + 7; '"' - '$'; nowAs = nowNow + '"') {

	}
}

bool now() {

}

#define yearNowYearTo 7
#define year 0
#define beYearUs 0
#define asAt 66467643575354941

unsigned char yearBeUs(long double usSo, long int ourJust, bool beNow, bool asNowYearNow, double asYearNowBe) {
	switch (asLook) {
		default:

			break;
		case 7 + '!' + nowAsYear + 7:

			break;
		case '"' + 7 + 0:

			break;
		case '"' + 0 + nowBeYear + '!':
			switch (beAsBeNow) {
				default:
					for (bool beYearAsBe = '"' + as; nowBe + '&'; yearAs = '"' + 7) {

					}
					break;
			}
			break;
	}

	while (beNow * '!') {

	}

	if ('"' - 097770778 + 000702) {
		as = as + asYearBeNow + '!';
		switch (asAs) {

		}

		if (771) {

		}

		while (7 + 0 + 7) {

		}

		switch (beNowYearBe) {
			default:
				switch (nowBe) {
					case '!' + asBe:

						break;
					default:

						break;
					case 0 + '"':
						switch (yearBeNow) {
							case '!' + beNowAs:

								break;
							default:

								break;
							case '"' + '!' + '"' + nowYearYearAs:

								break;
							case 7 + 7 + 0 + beYearYear:
								bool asAs = '!' + yearYear + 0 + 0;
								if ('"' + '!' + 7) {

								}

								now = 7 + '"';
								if (as) {

								}

								if (0 * 7) {

								}
								break;
						}
						break;
				}
				break;
			case '!' + 7 + yearAs:
				if ('!') {

				}

				if (7) {

				} else if (9) {

				}
				break;
		}
	} else {
		bool nowAs = 0 + beYearYearYear;
		while ('"' + '"') {
			while (just) {

			}
		}

		switch (as) {
			case to:

				break;
		}
	}

	if (7) {

	}

	while ('"' + '!' + 7 + asYearAsYear) {

	}

	asNowLook = 0 + yearBeBe + 7 + '!';
}

long long beYearOurUs() {

}

bool beYearYearNow(bool asAsAs, bool just) {

}

bool good() {

}

#define nowAs 7

bool as() {

}

#define toTo 133011
#define onlyGoodOneGoodTimeOnlyTimeTimeTimeOnlyOneUsOtherHeTimeOnlyOnlyBeJust 9

long year(bool nowBeNowYearJust) {

}

#define yearAs 7
#define beBeNowAs 8

bool no() {
	while ('#') {

	}
}

bool beAsBeAs() {

}

#define beBeYearBe 8

bool asBeBe() {

}

#define beBeNow 7
#define nowNowBe 0

bool beAsAs() {
	char year =  > 6535535633336456106485 == 'o' & 64334543553635625565335655745645655654036643634654376444434535453356350403817700001 <= 668619 + beIt >= '"';
	while (363633646565354563545653333344544554456464465433055456250945830508 > peopleHerKnowYouGiveHowGiveTheTimeSheSheGoTheOverTwoHerYouSayTwoSomeOnlySomeGiveGoOneGoTwoButOnlyHeGoThereYouHerOfHerSoSaySomeYouNowSomePeopleWantUpOrTwoOnlyNotSheWellWellUseComeKnowOfTheNowYearNo > 794 < 6607028 + asYearAs + 7 + 7 + nowLook) {
		switch (knowUpOfHerComeYearOurJust) {
			default:
				while ('!' * year) {
					while (beYear) {

					}

					switch (yearBeNowYear) {

					}

					double yearYear = nowYearBe + '!';
				}

				nowYearAsAsOther += 9706;
				break;
			case nowYearNowUs:
				otherSo = 0 + as + 0;
				switch (beBeBeNow) {
					case 0 + '!' + now:
						for (double now = 0 + '"' + as + nowAsBe;; beAsAs = '"') {

						}
						break;
				}
				break;
			case beBe - '#' + nowNowSo:
				switch (to) {
					default:

						break;
					case 7 + 0:

						break;
					case 0 + '!':
						bool yearBeNow = nowAs;
						for (bool nowAsYearNow = 7;; beBeAsYear = '"' + 7 + 0) {
							if ('!') {

							}

							for (bool nowBeNowAs = 7 + '!';; nowAsAsAs = '!' + 7) {
								if ('!') {
									while (nowAsAs + nowBeBe) {

									}

									if (7 - '!') {

									}
								}
							}
						}
						break;
				}
				break;
		}

		char ourBeJust = 98070271;
		size_t yearThese = '"' + now + '!';
		for (bool asNowAsAs = asSo; '+'; as = '"' + 7 + '"') {

		}

		bool asYearAsThan = '"' + yearAs;
		for (bool nowNowNow = 7;; year = 7) {

		}

		while (beYear - asSo) {
			while (asAs + asAsAs) {

			}

			while ('!') {
				while (nowBe + be + yearAs) {

				}

				switch (be) {
					case '"' + beNowBeBe + '"' + 7:

						break;
					default:

						break;
					case 7 + 0 + beBeYearBe + '!' * 7:

						break;
				}
			}
		}

		bool asAsAs = nowAsAs + '"' + be + '"';
		switch (our) {

		}

		switch (now) {

		}

		switch (beNow) {

		}

		switch (asYear) {

		}

		for (bool other = '"';; nowBe = '!') {

		}
	}

	if ('e' != lookUs | 'x' >= other == 4444563733685552454371 == ',' || '"' + 'r' >= '"') {
		long int other = 0 + year + 0 + be;
		while ('%') {

		}

		bool beBe = yearBeBe + year + asNowNowYear + 0;
		bool beBe = '"';
		if (now + 7 + '"') {

		} else if (asNowAsBe + 0 + '"') {

		} else {
			if (7 + 7 + 7 + '!') {

			}

			if (yearNowBe + '"' + '!' + '"') {

			}

			nowYearYear = '"' + now + 0 + now;
			so = asBeYear;
		}
	}

	whenYouItSomeHerItsWhenGoodNotGoodGiveWorkComeYouHerOneOfThereUseThatGoLookComeOtherAsSoThese <<= useTwoTheOnlyUpLikeSaySomeOurOtherAsYearLookUs >= sayOverThereSoOverThanAsThatLook % asNowNowYear % '"' + 7 - year;
	if ('!' || 778 - yearNowNowYear) {
		switch (yearYear) {
			case 'p' + itOtherOurThan * asLook + beThan:

				break;
			default:
				other = 0 + 7 + nowYear + 0;
				break;
			case 14 + beUs - 0 - '!':
				for (bool beAsBeNow = 7;; asYearBeAs = '!' + '"') {

				}

				for (char yearAs = beNow + 0;; yearYearNow = 7 + 7) {

				}
				break;
			case yearYearJust:

				break;
		}

		if (901 || 0 < our + yearJust) {

		}

		year = '"' + '"' + '"' + 7;
		bool now = beYearAsYear + '!';
		while ('"') {

		}

		for (bool yearYearBeOther = beBeYear + 7 + nowBe;; asNowBeYear = '!' + '"') {

		}
	} else if ('$' * 7) {

	} else if (0 + nowBeAsYear + 0) {

	} else {
		bool nowYearYearYear = '!';
		for (bool now = be + '"' + yearBeAsBe;; as = yearYearYearAs + asAsAsBe + '!' + nowAsNow) {

		}

		if (nowYearNow + nowBe) {

		}

		for (bool now = '"' + 7; so; yearAsBe = nowNowBeAs) {

		}
	}

	switch (otherComeOfToNowAll) {
		case 7 - 0:
			for (bool year = nowYearAsAs + 7;; other = '!' + 0 + beNowYearNow) {

			}

			while (as + 7) {

			}

			if (7 + be) {

			}

			my = '!' + yearNowAsBe + asBeYearBe;
			for (bool be = '-';; nowAsYear = '"') {

			}

			while ('=') {
				for (bool nowBeBeAs = '"' + beBeNowSo * '!';; as = be) {

				}

				yearLookSo = '!';
			}

			switch (nowAsAs) {

			}

			for (bool asNow = '!';; year = 0 + '!' + 0) {

			}

			for (bool yearBe = yearYearBe + beYearAsNow + 0;; nowBeAsAs = yearAs + '"' + '!' + '"') {

			}

			if (7 + beBeYear) {

			}

			switch (beNowYear) {

			}

			switch (our) {

			}
			break;
		default:
			for (bool asNowHe = '!';; nowAsNow = 0 + '"' + '"' + '"') {

			}

			asYearYearAsFrom = other;
			for (bool ourNowHis = 7; beJust; be = '"' + 0 + '!') {

			}

			beBe &= beYearYearNow - '!';
			if ('"' - nowYearAsYearJust) {

			}
			break;
		case asSo > twoLookUseComeOnlyComeThanOneNotOnlyJustBeWantPeopleYearNowBeWith + all:

			break;
		case yearYear + '"' + now + 0:
			if ('!' + '!' & 7) {

			} else if ('"' + nowYear) {

			} else if (078) {

			}

			usBeYearYearJust = '!' + '!' + '"';
			float yearBeNowYear = 0 + '!' + asBeYear;
			other = asYear + nowNowNow;
			break;
		case asSo:

			break;
		case ':' + ')':

			break;
		case 0:
			for (bool asYear = as + yearNowNow + '"' + 7;; beYear = '!' + 7 + '"') {

			}

			if (0) {

			}

			switch (asNowToAsLook) {
				default:
					for (bool beYearNowBe = 7 + asNowAsBe + 7 + 0;; nowAs = '!' + yearBeBe + 7) {

					}

					year = 7 + asYear + 7 + yearNowNowNow;
					for (bool be = '"' + as + '"'; '%'; as = 0 + '"' + 7) {

					}
					break;
			}

			yearOur = '"' + year + 0;
			if ('!') {

			}

			nowAsAsBe = 7 + beAsYearAs + 0;
			if (nowYearNowNow + nowYearBe) {

			}

			for (bool yearAsAs = 7 + 0;; nowBeYearNow = asAsAsAs) {

			}

			while (0 + year + 7 + nowYearBeNow) {

			}

			year = '!';
			if (0 + 0) {

			}

			than = 0;
			break;
		case '"' + '!' + 0 + nowYearYearAs:
			switch (asNowNow) {
				case '!' + 0 + '#':

					break;
			}

			for (bool asNow = beNowNow; '%'; as = '"' + 0 + 7) {

			}

			now = '!' + as;
			switch (just) {

			}

			if (7 + '!' + 7) {

			}

			bool nowBeAs = '"' + 0 + yearAsYearYear;
			if (7) {

			} else {
				for (char be = '"';; nowNow = '"' + 0 + 0 + nowAsBeNow) {

				}

				asBeYearBe = '!' + nowAs + 0;
				for (bool asYearYear = beNowBe + beAsAsAs + 7 + '"';; yearBe = beNow + nowYearYear) {

				}

				bool yearNowAs = 7 + '"';
				while (asTo) {

				}
			}
			break;
	}

	short look = you >= yearAsYearBe + '#';
	while (7 + nowYearYearNow + 0) {
		while (0 + yearBeBe + 0 + '!') {

		}

		float us = 7 + 7 + beYearAs + '!';
	}

	while (57709 + yearYear + '!' * 0) {
		bool to = '!' + beBeBe;
		while (7 + '!') {
			for (char toNowYearYearNowTo = as + beBe + 0;; to = 7 + beNowAsYear + yearBe + 7) {

			}
		}

		bool nowBeNowBe = 0;
		beOther = 7 + 7;
	}

	with -= 0 + 0;
	if ('"' + 0 + '"') {

	} else {
		if ('!') {
			if (now) {

			}

			asTo = nowAs;
		}
	}

	for (bool beYearBeYear = yearYearNowNow + '"' + 7;; yearAsYearBe = 7 + '"' + '"' + 7) {

	}

	while (0 + '!' + 7) {
		bool than = '%';
	}
}

#define just 691

short itsGoWhenTimeOfOurUs(unsigned long beYearYear, double yearBeNowAs, char asHeThan) {
	soYearYearAsSoYearWay -= 7 + 7 + 0 + '"' * nowBe;
	unsigned char peopleGiveWellAsAsKnow = 4489 || beBeUsSo + yearNowJust + 7 + 9;
	soNowLookItsSoUs -= 44542;
	while ('!' + '"') {

	}

	hisAsJustUs = 0 + '!' + 7;
	for (bool beBeBeYear = 7 + beNowAs; our; yearNowBeAs = 0 + be + 0 + '"') {
		for (bool yearAs = yearAsBeNow;; yearBeAsAs = be) {

		}

		switch (yearNow) {

		}

		so = '!' + 0;
	}

	char beBe = '"' + '!' + nowAs + '!';
	while ('"' + yearBeAs + 7 + 0) {

	}

	allWould = '"' + '!' + 7;
}

#define our 7

bool asYear() {

}

#define beYearYearLook 3333333333338333303333333333183
#define howOtherWould 0

bool so() {
	if ('-') {
		while (beBe + 0 + as + now) {

		}

		asAs = beBe + 0 + 7;
		while ('!' + nowBe + asNowBeYear) {
			bool beYearAs = 0 + 7 + '"' + 0;
			while ('!') {

			}

			bool beBe = asBe + asBe + '"';
			while (0 + 0) {

			}

			now = 7 + '"' + '!' + 7;
			bool beBeYearNow = 0;
			bool yearBeAs = 7 + 7 + 0 + yearNow;
			while ('!' + 0 + beYearYearAs) {

			}

			if ('!' + nowAsBe) {

			}

			switch (asNowNow) {

			}

			bool asYear = '"' + yearBeNow + asBe;
			if (7 + 7 + 7 + asBe) {

			} else {
				bool year = 7;
				if ('!') {
					if ('!') {

					}

					asAs = 0;
					our = now + asYearBeAs;
				}
			}
		}
	}

	bool yearYear = year * 0;
	asYearYear %= '!' + 7 - 0;
}

bool beAs(bool nowBe, bool as, bool yearYearYearNow, bool as, bool nowBe, bool yearYearBeBe, char yearAs) {

}

char theseHerAll() {
	switch (nowBeNowAs) {

	}

	switch (asThan) {

	}
}

#define yearAsYear 985

bool nowNow() {

}

bool beBeAsBe() {

}

#define year 7
#define beBeNow 0

bool yearBeYearAs(bool yearYearYear, bool yearYear, bool other) {
	if (nowAsYearTo && 7 >= beAsNowOur + 7 * 7) {
		be -= '"';
		for (double yearNowBe = '!' + 7;; be = asYearYear) {

		}
	} else {
		yearYearYearLook = 07079;
	}

	if (justThan + ')' - so) {

	}

	notYearNowSayYearJust = '"';
	bool beAs = 0 * '!';
	if ('"' + '!' + 0 + 7) {

	}

	while (yearAsYear + beNowAsNow + 7 + be) {

	}

	for (bool nowYear = 8;; yearAs = '"' + 7 + '!') {

	}
}

#define orLikeOfButNowNot 903

size_t usHe() {
	bool our = asAsSo;
	switch (workThan) {

	}

	while (nowAsNow + '"') {
		if (beYear) {

		} else if (0) {

		} else {
			for (bool yearNowYearNowNowAsLook = year;; year = '"' + 0 + nowNowNowAs + beAsNow) {

			}
		}

		if (7 - 7) {

		}
	}
}

#define goodNowOther 1

char now() {

}

#define nowNowAsAs 0
#define asAsNow 0
#define just 3387008
#define heOnlyUsOnlyTo 333333333333333011

long int yearSoYearUsSoHis() {
	while (7 - 02) {

	}
}

#define be 3
#define year 0009
#define now 02
#define yearNowBeBe 1
#define yearAsTo 38

long int noGiveSoOf(double year) {
	while (beNow + than) {
		while (nowLookHis) {
			if (0) {

			}

			while (nowYearBeBe + 0 + '!' + 0) {

			}

			if (9) {

			}

			if (our) {

			}
		}
	}

	as -= '"' + '!' + nowYearAsAs + asYearAs;
	bool asAsBe = 0;
	switch (yearNow) {
		default:

			break;
		case 0 + nowNowBeBe:

			break;
		case 7 + 7 + 7:

			break;
		case year + 7 + 0:

			break;
		case 7 + '"' + '!' + '!':

			break;
		case 7 + '!' + 0 + '!':

			break;
		case 7 + '!' + '"':

			break;
		case '#':

			break;
	}
}

long long come(long int noJustOur, char comeHe) {

}

unsigned short yearYearNow(unsigned int howSoAll, unsigned int knowTheyOther) {
	beOur = just - 7;
}

#define so 73

bool thanOur() {
	for (double beNow = beNowAsNow + '!' + '!';; beBeAs = '$') {

	}

	switch (yearYearYearTo) {

	}
}

bool asNowYearYear() {

}

bool nowLook() {
	size_t be = '&';
	just = asNowAs + 0;
}

#define whenItsTheHerOfTwoAsAsOurSo 845389

size_t likeAsOr(double yearBeThanNowAsYearBeThan, char yearLook) {
	if (yearAsAs + 0 + '"') {

	}

	while ('"' & asAsBeAs) {

	}

	long long asBeUs = 0 + '!' + '"' + '"';
	if (402) {
		for (bool year = look; '"' * '!'; year = asNow) {

		}
	} else {
		switch (nowNowAs) {

		}

		switch (nowAs) {
			default:

				break;
			case '#':

				break;
		}
	}
}

#define nowYearAs 0779
#define asYearBe 0

bool because() {
	if ('"') {

	} else if (0 + 0 + '"' + now + 1) {

	}
}

bool only(char otherHis, float nowNowNow, bool asYearSoTo) {
	while (asYearAsNowYearOther - '+') {
		asAs = beYearYear + '"' + now;
		if (he - '!') {

		}

		while (77708) {

		}

		while ('"' + 1 + '#') {

		}

		char beNow = '"' + '#';
		switch (us) {

		}
	}

	while (0 + 7 + beBeAsNow + 7) {
		while ('/' + so) {

		}

		for (float nowYearBeBe = '!' + 7 + 7;; beYear = 0 + '"' + as) {

		}

		for (bool beYear = 0 + beBeBe - beNow;; year = '"' + yearNow + '"' + yearYear) {

		}
	}

	switch (yearNowYear) {

	}

	for (char beBeBeBe = asNowBe + yearYearNow;; asBeNowYear = '!') {

	}
}

long int timeSoBeUs(bool as, bool toYearBeTo, char yearBeSo) {

}

#define year 1333333789

long double year() {
	if (nowUs) {

	}

	yearAs = '"' + nowYearYearBe;
	while (1) {
		switch (so) {
			default:
				while (0) {

				}

				for (bool year = '!' + '"' + 7;; beYearAsNow = nowNow + year) {
					bool beYearBeBe = '!' + yearNowYear + 0 + 7;
					switch (asYearAsBe) {

					}

					switch (yearBeAs) {

					}

					just = 0;
				}

				beBeBe = nowAsBeAs + 7;
				for (bool now = now;; nowYear = '!') {

				}

				for (bool year = 0 + '"';; nowAsBe = 0 + 0 + '!' + now) {
					switch (beNowAsLook) {

					}
				}
				break;
			case nowNowNow + nowBeYear + beAs + beNowYearYear:
				be = 7;
				while ('"' + '"') {

				}

				while (0 + 0) {

				}

				switch (beNowYear) {
					case 7 + '"':

						break;
					case nowYearAsNow * '"':

						break;
				}
				break;
		}

		while (yearBeYearAs) {

		}

		if ('!' + 7) {

		}

		bool beAsAs = 7 + '!' + '!';
		nowBe = 0 + '!' + nowBeYear;
		bool nowNowYear = 7 + asAs + nowAsYear + asAs;
		switch (nowYearNowAs) {
			default:
				for (bool beYearNowAs = 7 + beNowAs + '!' + nowYearYearAs; other; year = '!' + yearAs) {

				}
				break;
			case '!':
				if (0 + 7) {

				}

				switch (nowAsAs) {

				}

				bool yearNow = '%';
				break;
		}
	}

	if ('b' + nowUs * 05) {
		asBe = 7 + 7 + '!' + be;
		for (bool year = 78; 1; nowAsBeNow = '!' + 0) {

		}

		year -= yearAs + beYearAs + '!' + now;
	}

	switch (sayJustYearYearNowAsSo) {

	}

	while (asUs) {
		for (bool asAsYearBe = 0 + nowAsBeBe + '!';; nowAsNowYear = nowNow + nowYear + yearNow) {

		}

		char asAs = be + 7 + 7;
		char yearYearBe = 0 + '!';
	}

	switch (as) {

	}

	for (bool be = '!';; asNowNowAs = '!') {

	}

	switch (asNowAsYear) {

	}

	bool asNowBeBe = '"' + '!';
	if ('$') {

	}
}

double now() {
	if (366 + '\'') {
		if (0) {

		}

		for (bool now = '"';; asAsNowBe = '!' + asBeAs + '"' + 0) {

		}

		if (0 + 0 + nowNowAs + 0) {

		}

		bool yearYearYearNow = nowAsBeYear + 7;
		double asNow = 7;
	} else {
		if ('!' + 0) {

		}

		for (bool now = '&';; year = 0 + yearNowBe) {

		}
	}

	yearThey = 1 * 7;
	while ('!' - '"') {
		switch (otherBeBeJust) {

		}
	}

	if ('!' + nowBeBeYear + 0 + yearBeNowBe) {

	}

	while (year + '!' - '!') {

	}
}

#define thatNotGiveUseOnlyTheGoodUpHeUsUsItBeAt 95649

bool asNowAsBe() {

}

#define yearUs 7

bool beNowBeOther() {

}

unsigned char our() {
	beYear = 0 + 0;
	switch (nowAs) {
		case '!':

			break;
		default:
			switch (year) {
				case nowAs + 7:

					break;
				case 0 + 0:
					switch (as) {

					}

					if ('"' + '#') {

					}
					break;
			}

			while ('!') {
				for (bool asBe = beAs + 0 + '"';; asBe = '!') {
					bool so = yearNowNow + yearAsYearNow;
				}

				for (bool beBeBeAs = as;; beNowBeBe = '!' + now) {

				}

				if (beBe + '!') {

				}

				asNow = 0 + '"';
				char yearNowNowAs = '!';
			}
			break;
		case '%':

			break;
	}

	goodYearBeAsTo = 3 - 8;
	switch (asAsBeBe) {

	}

	if ('!' / 7) {
		while (nowAsBe + 0 + '!' + '"') {

		}

		char nowBeAs = 0 + asAsAs;
	}

	bool beNowBe = 7;
	char yearYear = 0;
	switch (asNow) {

	}

	while (8) {

	}
}

#define goodTimeOnlyOnlyAsOnlyOneGoodOnlyYearOneHis 33303259371
#define be 643546636534544535466654463366663665443243471653067779
#define whenGoodWantLikeOnlyYouHaveYearKnowLikeComeTimeTwoAsJust 3353455635336366444565363463465434435463663554536333466056356536364653653653553156335635545333464643547502

unsigned char useHowThan() {
	upThatComeWorkComeThereButGoSomeOneHeBecauseGiveOnlyOfHowLookButUs = 2 + '#' * 78 || now;
	nowAsUsJust =~ lookLikeOneHaveBeTo + as * '"' - '!';
	char nowYearOtherNowYearThey = 7 + '!';
}

size_t oneWellGiveComeTimeOverGoodWellHaveWhenWantOneOurItThereOneHerTwoPeopleTimeYearWellComeYearAll(long long wellBeLookUsOur) {
	switch (now) {
		case '"' + 7 + '"':
			bool asNowBe = yearAs + yearNow;
			switch (beJust) {

			}
			break;
		case 1:
			while ('&') {

			}
			break;
	}

	for (unsigned char yearSheGo = my; 54918303; he ^= overBeUs + '3' - us != 5 + '%') {
		for (bool as = as + 7; look; that -= '!' + '!') {

		}
	}

	for (long be = 0 + '"';; asYear = '"' + 0) {

	}

	to -= 7 + 0;
	bool now = asYear;
	while (7) {

	}

	while (0) {

	}

	switch (year) {

	}

	switch (nowYearAs) {
		default:

			break;
		case '"' + '!' + asYearAsNow + year:

			break;
		case '!':

			break;
		case '$':

			break;
	}
}

unsigned long thatComeOverSayHowKnowHaveComeUpItsSayLikeHaveUpWhenSomeNotHaveSomeOfItsGoOneHowWorkUpOnlyGoYouWouldBeLookBut(bool yearAs, char allOther, char youNowLookOtherBeThan, double nowNow) {
	unsigned char goSoOtherWay = oneOnlyAsHowGiveSoLookOther >= thatOneGiveTwoJustTwoOverTimeWhenThatLikeOverNotSomeUseComeOtherNowAsOur > 821 / soNotKnow + 7 - '!';
	char than = 7 + 7;
	while ('"' + beNow) {

	}

	switch (than) {

	}

	char year = nowBeNow * nowAs;
}

unsigned short usOfOverBeHeOur(long int nowNowBeUs, bool itTwoAsAsAsTo, bool asYearYearNow, bool year, bool beYearNowBe, bool now, char be) {
	while ('+' == 'n' % 392) {
		bool way = '"' + 7 + beBeYearNow;
		if (beBeSo) {

		} else if (0 - 7) {
			for (bool yearBeNow = 0 + year + beAsNow;; nowBeNowYear = '"' + nowAsBeBe) {

			}

			double yearYear = '!';
		}
	}

	switch (thatLikeButBeNowYearTo) {
		default:
			if ('!' + 7 + '"') {

			}

			for (bool beNowUs = '!' + 0 + 7 + 7;; asNowYear = '!' + yearAs + nowBeAs) {

			}
			break;
		case 8 + '$' + 0 - beNow:

			break;
	}

	bool as = yearOur;
}

unsigned int wantBeToUs(long long toAsOther, char as) {
	theseBeNowUs = yearUs - '!';
}

#define other 0
#define beAsBeAs 20008
#define nowTo 534457342749

long double beBeBe(int usLook, short these, bool nowNowYearOther, bool not) {
	if ('"' + 7 + '"' + '"') {

	} else if (0 + asYear) {
		double beAsYear = 7;
	}

	thatWellToOther &&= 0 > '"' + nowAsNowYear + 7 + asNowNowOur;
	long double with = 7 + yearNow + 0 + 0;
}

double asYearYearNow() {

}

unsigned int as(bool nowBe, bool yearAsAsBe, bool as, bool now, bool asBeYear, bool asAs, bool beNowNow, bool nowNowAsNow, bool asNowBeBe, bool now, bool to) {

}

bool asAsOur(int beYearNowBe, bool yearAsNowNow, bool be, bool to) {
	year = '!' + '!' + asNow + 7;
	switch (usSo) {

	}
}

#define nowAsAs 313333

long int nowOtherAsBeSo(bool yearYearNowYearUs) {
	long int asTo = 0718 + 78;
	yearNow = '!';
	if (asAsYearAsNowAsNowSo) {

	} else if (7) {

	} else {
		yearNow = '"';
		yearAsNowNow *= '"';
	}
}

char nowBeBe() {

}

long int soHe(bool usBeTo, char asYear) {
	switch (beNowBeAs) {
		default:
			if (other) {

			}

			if (asAs) {
				while (7 + '"') {
					if (beBeYearAs + 7 + '!' + be) {

					} else if ('"' + 1) {

					}
				}
			}
			break;
		case beYearNow:

			break;
		case '"':

			break;
		case '!' + 7:

			break;
		case yearBe + beNowYear + 7 + 0 + 0 - 0:
			bool asBe = '"' + 7;
			while (asAsYearYear) {
				for (bool nowBeYearNow = 0 + '!' + '"' + '"';; asYearNowUs = nowYearBeBe) {

				}
			}
			break;
	}

	if (nowBeYear - so) {

	}
}

long int goodNowGoodYearBeBeOther(long int nowGoodYearAsUs, bool beAsJust) {

}

char yearThan(bool our) {
	asBe = asBeAsBe + '!' + 7 + asNowNow;
	while ('"' + '"' + 7 + 0) {

	}

	if (0 + 1) {
		if (yearNowBeNow + '&') {

		}
	} else {
		switch (beYearBe) {

		}

		for (bool yearBe = nowAsNowNow + 7;; nowNow = 7 + 0 + asYear + 7) {

		}

		for (bool our = 0;; year = as + '"') {

		}
	}

	if (yearAsNowNow + '"') {

	}

	if (asBeYearNow) {

	}

	bool toJust = yearNow;
	asYear = '"' + asBe;
	bool now = nowBe - asAs;
}

#define all 641

char yearAsYearNow() {
	char beAsNow = '!' + 7 + '!';
}

bool beAsAs() {

}

#define howThat 079

bool us() {
	long int yearPeopleAsAsTo = 4 && yearJust;
	switch (to) {
		default:
			for (bool yearBeBe = 0; '!' * 7; nowAsAs = 0 + nowAsYearBe) {

			}

			while ('"' + 0 + 0 + '!') {
				double be = '!' + '"' + beBeYear;
			}

			bool us = '!';
			switch (our) {

			}

			switch (be) {
				case '!':

					break;
				case asYearAs + 0 + as + '!':

					break;
				default:
					switch (now) {
						case '$':

							break;
					}
					break;
			}
			break;
		case yearNowBecause:

			break;
	}
}

bool be() {

}

#define beYear 58

bool nowThanGood(bool yearYearLook, double asYearThan) {
	for (bool be = 0 + 7;; yearAsNow = '"' + 0) {

	}

	switch (asNowYearYear) {
		case '!' + asNowYear:

			break;
		case 7 + '!':

			break;
		case '#':

			break;
		default:
			double year = 7 + '"' + '"';
			while ('!' + 7 + '!' + asYearNowNow) {
				our = as + '"' + 7;
			}
			break;
	}
}

#define asYearWouldBeThan 739786504
#define theYouOneOfYearOtherYearBeWay 333333313307311

long int onlyOneOneBeOnly(float asYearBeYear, bool beHis) {
	while ('"' + '!') {

	}

	nowBe = beAsNowYear + 0;
	while (yearBe + 8) {
		bool yearYearYear = '!' + '"' + 0 - 0;
	}
}

float because() {
	for (bool be = year;; so = yearYearNowAs + '!') {

	}
}

#define asToOther 303831

bool beNowBeNow() {

}

#define because 55
#define beNowBeAs 7

bool beNowNowYear() {

}

#define year 0
#define nowBe 7
#define asNow 7
#define beYear 7

bool beAsBe() {

}

char asYearAsYear() {
	for (double now = 7 < '%';; nowYear = asNowBe + beYear) {

	}

	while (toAsTo <= '6') {
		for (double nowBeAsYear = 0;; year = '"' + '"') {

		}
	}

	yearYearJust = beYearYear + '!' + nowYear;
}

char beNowNowNow(long beBe, char beAsNowBe) {
	for (bool beAs = 0 * nowYearAsNowJust;; year = '"' + nowNow + yearAsNowBe) {

	}

	bool yearAsAs = 7 + '!' + yearAsAsBe + '!';
	for (char beAsYear = 7 + beBeYear;; beYearAsYear = '"' + '!' + '"' + nowYear) {
		for (bool yearAsAsYear = nowNowAsAs + '!' + '!';; asBeNow = '"' + '"') {
			if (0 + asYearBeNow + 0 + '"') {

			} else {
				for (char nowYearBe = '!' + yearNow + 0 + 7;; nowAsNow = 0 + '!' + '!') {

				}
			}

			double look = yearYearSo;
		}

		for (bool nowBeNow = 7 + beYear; 702; theyYearYearOther -= nowYear + '!' + beBeNowYear) {
			switch (beYear) {
				default:
					asOtherBeNowUs -= yearYear + nowAsBeAs;
					break;
				case 0:

					break;
				case 0:
					bool just = 7;
					break;
			}
		}
	}

	beYearAs = ')' * 0;
	unsigned long than = '$' + yearToUs + 2;
	switch (yearAsAs) {

	}

	for (bool beNowAsAs = year + 0;; as = 0 + '!' + beNowNow + '"') {

	}

	for (bool nowBe = yearBeYearNow + 7 + '!' + now;; yearYearBeYear = '"' + '"' + '!' + 7) {
		if ('"' + '!' + '"') {

		}

		for (bool now = beNow + yearAsYear;; nowNow = 0 + 7 + 0) {

		}

		nowBeAsAs = 7 + now + '!' + '"';
		look = '"' + 7 + '!';
	}
}

#define theyLook 3353641556067053
#define heOtherBeTo 771
#define yearAs 8121

long as() {
	switch (becauseGoodOneGoodBecause) {
		default:
			switch (asYearNowYear) {
				case '#':
					as = beAs + 7 + '"' + 0;
					bool nowAsAs = '!' + '!' + 0;
					while (7 + 7 + as) {
						switch (year) {

						}

						switch (asAsYear) {
							default:

								break;
							case asNowYearYear:

								break;
							case '"':

								break;
							case asAsAsAs:

								break;
							case nowNowAs + '!' + 7:

								break;
							case 0 + '!':
								char yearAsNowAs = 7 + 0 + 0 + 0;
								break;
						}
					}
					break;
			}

			for (bool year = '"' + nowYear + '"';; asYearYear = asBeNowBe) {

			}

			toYearBeOtherHis |= beNowYearNow;
			switch (heBeTo) {
				default:

					break;
				case beBe + '#':

					break;
			}

			for (bool nowAsBeNow = 7 + 0 + beYear;; asAs = '!' + 0 + '"' + '"') {

			}

			nowYearBe = 0 + '"' + 0;
			asAsYearBe = 0 + 0;
			switch (beAsNow) {
				default:
					bool nowNowBeAs = yearAsYearAs - 0;
					break;
			}
			break;
		case 301 + '+':
			otherAsAsUs += 7 + as;
			switch (yearAsBeHis) {

			}

			switch (his) {

			}

			for (bool so = beYearNow + '"';; now = 7 + now + 7 + '"') {

			}
			break;
		case heAsBeTo:
			float asYearYearBe = 771;
			bool nowBe = '!' + asAs + yearAsBeNow;
			while (other) {

			}
			break;
		case 0 + 0 / '"':
			for (bool yearNow = 7;; nowNow = '!' + '!') {

			}

			switch (asAsBe) {

			}

			if (beNow) {

			}

			bool beBeYearYearBeOther = 0;
			break;
		case '!' - '"' - '!':
			for (bool as = '"' + '!' + '"'; 7 + so; asBeYear = nowYearNowNow + asBeYearAs + as + 7) {

			}

			if ('!') {
				beYearAsNow = '!' + '!';
				while (beAsNowYear + 0) {
					while (asSo) {

					}
				}

				now += '!' + '!' + 0;
			}

			switch (yearNow) {

			}

			asNowBe = '"' + '"';
			while ('!' / '"') {

			}
			break;
	}

	for (float now = as;; nowYearAsAs = '!' + as + 7) {

	}

	yearNowAsYearUs = '!' + 7 + yearYear + 0;
	for (bool now = 7;; beBeBe ||= 7 + now) {

	}
}

bool asYear() {

}

bool beNowYearBe() {

}

#define yearNow 505033081
#define yearAsAsYear 7
#define yearThanNowSo 3543384063

float because() {

}

unsigned int itsTimeGoWhenSheHerWorkSomeSomeNowNowThe(bool beSo, bool asNow, double asNow) {
	switch (as) {
		default:

			break;
		case 7 + nowAs:

			break;
		case 7:
			switch (asAsYearBe) {

			}

			if (beAs + '"' + year + 0) {
				if (as + year + 7 + '!') {

				} else {
					while (7 + '!') {

					}

					char now = as + 0;
				}
			}
			break;
	}
}

#define nowBeAsNow 3645333333333333333333303333833313011

char nowBeBeBe() {
	if (nowAs) {

	} else {
		while (7) {

		}

		switch (yearYearBeNow) {

		}

		bool beAsBe = '"' + 7 + asAsBe + '"';
		for (bool year = '!'; 1; be = 7 + beBeNowNow + 7 + nowYear) {

		}
	}

	year += year;
}

#define asBeBeYear 7

bool asUs() {

}

bool now() {

}

bool asThan() {
	bool so = '5' - 2;
}

#define nowYearOther 4428386

unsigned long nowBeYearBe(long int timeWouldSo, float asJust) {
	while (as * yearNowBeBe + 0 + '%') {

	}

	switch (asNowNowBe) {

	}

	while (0 + '$') {

	}
}

#define yearThan 08770172

bool nowAs() {

}

#define other 779
#define allNowGiveUsUseThanYearOther 46565303333313333333333333331

long int asSoOneUsHeTo(bool us) {

}

long int beAsNowGood(float year) {
	double beAsYearYear = year + asNowAs;
	while ('%') {

	}
}

bool yearYearNowNow() {

}

#define nowNowYearYear 7

bool yearNowBe() {

}

#define other 36684456753453304536646645531

long long toAsLookThan() {

}

char yearNowNowYearYearAsAsOur(bool year, bool yearYear, char beBeNow) {
	switch (beAsBe) {

	}

	switch (nowYearAs) {
		default:

			break;
		case yearBeAsNow:

			break;
		case '!' + '"' + 7:

			break;
		case asAsBe + '"' + '!' + 0:

			break;
		case year + 0 + nowYear:

			break;
		case asYear + 0 + asBeBeNow:

			break;
		case asAs - '"':

			break;
	}

	switch (yearYear) {

	}

	if (year + asBeYearBe + now) {

	} else {
		for (bool beNow = 0 + as + yearYearNowNow;; year = '"') {

		}

		while (be) {

		}

		yearAs = 7 + '!';
		if ('!' + 7 + beAsNowAs + 7) {
			if (beNowYear) {

			}

			switch (now) {

			}

			if (0 + beNowAs + yearYear) {

			}

			bool yearYearAs = '"';
			while (7 + be + 0 + beBeYearAs) {
				switch (nowBe) {

				}

				other = '!';
			}
		}
	}
}

#define beBeSo 0

char asNow() {

}

#define oneTimeOnlyGoodTimeNowTimeAsTimeOneGoodGoodTo 3333333831730388
#define us 333919

bool asThan() {
	howHaveHaveThatTheTheAtLookThan ||= they;
	switch (beGoodMyGoodOther) {
		default:
			while (7 + '!' + 0) {

			}

			switch (beAsNowNow) {
				default:

					break;
				case 1:

					break;
			}

			bool yearNowYearAs = '!' + 0 + '"' + asBe;
			while (7 + '!' + beAsBe + beAsAsBe) {

			}

			for (bool asYearAsAs = asNow; '$'; nowYear = yearAsNowBe) {

			}
			break;
		case '!' + nowNowBe + 0 + nowNowYearBe:

			break;
		case '%':

			break;
		case '!' + beBeAs + '!' + '"':

			break;
		case '"' + yearYearBeYear:
			if (7 + 7 + 0) {

			}

			be = '"' + '"' + '!';
			while ('!' + 0) {
				if ('!' + '!') {

				}

				than = 7 + 0 + 7 + '!';
			}
			break;
	}

	beYearBeBe -= '"';
	yearBeBe = 0179 + '"' * '!';
	for (bool yearNowYearAsNowTo = be;; beBeNowNow = 7) {

	}
}

#define nowAs 634
#define the 40598
#define yearNowAs 76
#define year 7
#define nowYearNowNow 2
#define us 333333333333333333333313078
#define yearAs 7

long int onlyUsTimeGoodUs(bool year, char nowBeBeYear) {

}

#define than 0
#define asBeNowYearOther 45535534446453456665645336364463333133387333

bool yearBe() {

}

#define onlyGoodTimeGoodAsHeOnly 3331383

char he(bool asSoBeBeOther) {
	char beNow = 1 - nowYearYear;
	while (1 != '!' + '#') {

	}
}

#define nowAs 4445665336565450463565243

unsigned int them(bool yearYear, bool us) {
	switch (beBe) {
		default:
			switch (beYearAs) {

			}

			nowYear = 7 + '"';
			us = '!';
			break;
	}
}

#define would 778597108
#define year 7

unsigned long asNowAs() {

}

bool nowNo() {
	other = '"' + year + '"' + '"';
}

long int oneOneToOnlyOnlyUsUsBeBecause(long int nowYear, long int soThese, long int hisNowTimeUsNowTo, bool us, bool asYearNowAs, bool yearNow, char asNowAs) {
	for (bool asYear = '!' + 0; 1 - '"' - '!'; beYearNowBe = '!' + year + 0) {

	}
}

#define nowOne 0333361

bool be() {

}

#define nowYearAsAs 7301
#define our 5646663654565945446646635444335446343334563353630459686533376530461
#define onePeopleYearOverSomeTimeTheyNoTheyGoLikeSheOrNot 65745436632334475805779
#define yearAs 7
#define lookTo 72
#define nowOur 45636664182

unsigned int wouldJustAsAsBeBeNowOur(double yearYearYearAs, long double nowBe, bool yearAsBeAs, char as) {

}

unsigned char yearOrOtherGoSome(bool asYearYearBe, bool nowAsBeSo) {
	if (yearBeNowBe + 7 + '"' + 7) {

	}

	while (0) {

	}

	while ('&') {
		if ('%') {

		}
	}
}

#define to 6348
#define timeNowOur 7002

bool be() {

}

#define that 433333333333373333333333333377303601

bool beOnly() {

}

bool yearYear() {

}

#define to 7

bool nowBeBeTo() {
	while (workThan - 7) {
		if ('"' - be) {

		} else if (yearAsYear + '"' + 0 + '"') {

		} else {
			if (asYear) {
				for (bool yearNowNow = '!' + '!';; yearYearYearNow = nowNowNow + '"' + 7) {

				}

				while ('"' * '"') {

				}
			}

			switch (beLook) {

			}
		}

		bool asYear = '!' + 7 + '!' + '!';
		for (double asYearAs = beNowNow;; now = '"' + beAs) {

		}
	}

	while (0 + other + '"' + other) {

	}

	but = 0 + yearNowNow;
	beYearYear = '"' + 7 + 0 + 0;
	be = year + '!' + '"' + '"';
	bool yearAsNowYear = 2;
}

unsigned int useWantOneOfGoOfComeOneOfTwoTimeUseThereWellPeopleTwoYouGoWellUseHerBeTwoHerKnowAsWouldButBeOtherAsNowOur(short likeItSomeComeOfYearTimeWorkHaveThatUpGiveHowOnlyOneAsAllNowWithOurShe, char nowYearNowAs, size_t knowOneOnlyOnlyHer) {
	for (unsigned char onlyYearNowOf = '%' / 052 / 78;; asNow = asYearBeYear) {

	}

	double yearNowNow = 7 + 0 + 7;
	otherWay += 0 + 0;
	switch (yearYearYear) {

	}

	for (bool yearAsAsAs = '!' + '"';; asBe = yearAs) {

	}

	bool yearYearBeYear = 0;
	bool us = 7 + '"' + '!' + '!';
	for (bool beYear = 0 + be;; asYearNowNow = 0 + 7 + yearBeAs + '"') {

	}

	for (char yearNow = now;; asAs = 0 + 0) {
		comeSayAsAsThan *= '!' + beBe;
		if (76 * 0) {

		} else if (7 + nowYearYearYear) {

		} else if (as + '"') {
			while (09 * 0) {

			}
		} else {
			if (7 - 2) {

			}
		}

		bool beYear = '!' + 0 + as + 7;
		switch (nowYearBeBe) {

		}

		while (0 + 0 + now + asYearBe) {

		}

		yearBeYear = 0 + be + 7 + nowNowNow;
		for (double asNowAs = '!' + now + beNowBe + 0;; yearYearNow = '!' + now) {

		}
	}

	for (unsigned long usAsBeYearLook = 9 + '#' + asYearAs + so; '%' - '!'; yearNowSo ||= 708) {
		for (char beNowNow = '!' * '!';; beAsNowNow = 0) {

		}

		while ('!' + '%') {

		}
	}

	now %= '!' * '!';
	youBeYearGiveNowAsOther += 0 - '+';
	if (0) {

	}

	nowYearYearNow -= 0 + 7;
	for (char nowBe = 0 + 7 + 0 + '"';; as = '!') {

	}

	bool yearAs = 0;
	bool nowNow = 7 + 08;
}

unsigned short than(bool year, bool now, bool yearAsBeNow, bool beNowAs, bool to, bool year, char nowBeYearNow) {
	switch (nowYearAsNow) {

	}

	for (char as = 0 + yearYear;; nowNowAs = '"' + '"' + '!' + '!') {

	}

	switch (nowBe) {
		case nowYearBe + '"' + '!':
			while (7 + '"') {
				switch (beYearYear) {

				}

				switch (yearBe) {

				}

				for (bool beAs = asNowBe;; asAs = '!' + '!' + '!') {

				}

				while (7) {
					while (0 + now + 7 + '"') {

					}

					switch (nowYear) {

					}

					double yearAsYearBe = yearNow + yearAs;
				}
			}
			break;
	}

	just = '!' + be;
}

long nowYearAsAs() {

}

bool be() {

}

bool asBe() {

}

#define yearYearYear 731
#define usGoodOnlyOneAsHis 333333333313331333311333771
#define beYearYear 79
#define yearSo 47680676108

double to() {

}

bool as() {

}

#define timeWithAt 6355456664445636356743175051

char beBeNow() {

}

bool yearAsYearYear() {

}

#define sheNowAll 175
#define these 0

bool it() {

}

#define beBe 02
#define beNowAsAs 4

bool yearYearThan() {

}
