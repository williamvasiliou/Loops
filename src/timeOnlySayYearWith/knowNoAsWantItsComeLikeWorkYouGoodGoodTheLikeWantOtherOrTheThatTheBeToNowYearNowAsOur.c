#define asYear 0871

long int oneBecauseGoodGoodTimeOnlyTimeBeOneTheseAsSo() {
	yearAsYearYear |= 0 - '"' + so;
}

#define asOnlyOnlyOneYearGoodHeOnlyYou 98
#define nowAs 3007003

bool beNowNow() {

}

#define them 34665356335333333333333333333333333330337624453595064

unsigned int upComeNowSomeOfAsSayWellBeJustKnowAsHowJustBut(bool usNot) {
	for (char asYear = 7 + 0;; nowYear = 0 + yearYear + '!' + yearAsNow) {

	}

	while (because) {

	}

	switch (asYearBut) {
		default:
			as = '!' + '"' + '!';
			double year = 0 + '!' + '!' + '"';
			double beBeYear = asBe;
			just = asBeNow + '"' + 0 + '"';
			break;
		case now + 7 + '"' + 0:

			break;
		case '"' + '!' + asBeAsYear + as:

			break;
		case '&':

			break;
	}
}

#define  665455363643644635566366366635664333633554133646636653665663654444464643635433565655566354344656463353453335445654443644646464336644365546735556356636649265663366556546644654664666426146529
#define itsSomeWellOfOfBeYearUsLook 564363442

short wantLikeYouLookJustAsHowBeNotJustYearTheAsThan(long double be, double nowLook) {
	while ('Z' + '"' * 0) {
		if (beAsBeNow * beBeBeAs) {
			while ('!' - 0) {
				switch (so) {

				}
			}
		}
	}

	if (7 + 0 + '!') {

	}

	for (bool nowNow = 0 + 0;; nowBe = 0 + nowNowBe + nowNowAsBe) {

	}

	nowYearAs = 0 + nowNow;
	while ('!' + '&') {

	}
}

size_t itsTheLookGoPeopleWantTheAsSome(double theyComeHaveYearTo, double beBeBeYear) {

}

unsigned long peoplePeopleJustWellOurGive(size_t theseAsUp, unsigned short thatSomeItTheOurButSo, char beAs) {
	for (char nowAs = asYearAsAs + '!'; 7 - beNowNow; asYear = '!' + '!') {

	}

	if (25) {

	}
}

#define yearNowNow 98

double nowOtherOur() {

}

double asNow() {

}

bool asNowNow() {

}

bool now() {

}

#define beAsAs 7

double now() {
	for (bool nowNowNowAs = '!' + '!';; yearAsNow = year + yearAs + year) {

	}

	while (7) {
		bool as = as + beBe;
		if (7 - asBeBe) {

		}

		if ('"' + yearYearNowAs + nowAsBe) {

		}

		bool now = '!' + beAsBeNow;
		while (beAs + asBeYear) {

		}

		if ('!' + '"' + '"' + asAsAs) {

		}

		for (bool be = yearBe + '"';; nowNowAsYear = 0 + beAs + yearAsAs) {
			for (bool yearNow = '"' + '"' + be;; asAs = yearNowNow) {

			}

			switch (just) {

			}
		}
	}

	for (bool as = '"'; 1; beNowAsBe = other) {

	}
}

#define lookOrAsTo 5
#define whenOnlySomeWellOnlyBecauseLook 333654465430606952

unsigned int yearOther() {
	switch (nowOther) {
		case '"' - yearYearBeAs:

			break;
		case beAsNow + year + nowAsYear:
			bool asNowNow = 7 + '"' + '"';
			while ('"' - beYear) {

			}
			break;
		case yearYear + yearNow + beAsBe:
			bool beAs = 071;
			while ('!' + 0 + 7) {

			}

			while (7) {

			}

			while (nowNowNow + '"' + yearAsYear) {

			}

			if (asYear + nowYear + '!' + '!') {

			}

			switch (be) {

			}

			for (bool asBe = '"';; be = '!' + '!' + 7 + 0) {

			}

			if ('!') {

			}

			for (bool nowBe = beAsAsNow + nowBeNowNow + 7 + 7;; be = '!' + nowYearAs) {

			}

			now = 1;
			break;
		case 0 + '"':

			break;
		case 0 + 7 + asNow:

			break;
		case nowYear + 0:
			if (beBeYearNow + beNow) {
				if (beYearBeAs + '"') {

				}

				while (yearAsYear) {
					switch (nowNowBeNow) {
						case '!' + yearYear:
							switch (asNow) {
								default:
									switch (now) {
										default:
											if (7 + nowNow) {

											}

											bool nowBeAsNow = 008;
											break;
									}
									break;
							}
							break;
					}
				}
			}
			break;
	}

	switch (goodTimeSo) {
		default:
			while ('"' + now) {
				while (nowYearYearNow + yearAsYearBe) {

				}

				if (beNow + beYearNow + nowBeYearBe + 0) {

				}

				switch (asAs) {
					default:
						beAsAs = nowAs;
						for (char year = 0 + 0;; yearBe = '"' + asNowAs + '!' + 0) {

						}
						break;
				}
			}
			break;
	}

	while (7) {
		while (0 + 7) {
			while (other) {

			}
		}
	}
}

long int hisSo(bool beYear, char asNowNow, bool to) {
	for (long int us = '!';; yearBeNowYear = 7) {

	}
}

#define oneOneOneOneGoodOnlyGoodTimeBeOnlyOtherAtBeThan 349
#define beAs 0

long double yearNowAs() {
	for (unsigned char  = 4645640006 && 'k' & '}' == 'j' != 't' && '}' | '}' >= us && 64633966636557594563678; thanAsTo || ourAsYearBecauseAsYearLook * asNowYear; asYearNow -= '.') {
		if ('#') {
			for (char beAsYear = 7; '%'; nowYearYear = '!' + 7 + '"' * '"') {

			}
		} else if (asBeLook * be) {

		}

		for (bool beYearNow = yearNowBeJust; beBeAsYear + just; beNowYear = '"') {

		}

		while (0 + 7) {

		}

		for (long long now = asAsBe;; now = '"' + 7 + 0 + be) {

		}
	}

	youWorkComeWorkOnlyYouGoWantWantBeOneYouComeHeBeSoThan &&= oneThanGoTimeTheWorkOfItsKnowWantYearFromNowJustYouThanFromHaveUs | comeYearWhenThanOfYearYearAsAsNowAsYearOur | 366505649 == thatOtherWay == toYouOurHave + '%';
	while (soJust) {

	}

	while (022 <= 0) {

	}
}

#define comeWorkLikeHowHaveGoWantWantItOfAsUsGo 5563565633546467656442
#define toComeAllTheyAsNowBeNowLook 73

bool yearYear() {

}

#define yearBeUs 7

bool just() {

}

unsigned int itsYearItsSo(double us, bool as, double beYearAsYear) {
	while ('%') {

	}
}

long long theThanNowYearNo(float toLookNowBeAsLook, unsigned char asThat, float asBeLookYearNowAsBeYearNowBeThan, bool other) {
	while ('"' & asBeAsYear) {
		asNow = 7 + 7 + yearNow;
		if (as) {

		}

		while ('%') {

		}

		bool asBe = 7 + '!' + '!';
		nowNow = yearAsYear + 7;
		bool beOur = asNowYearAs + '!';
	}

	char nowYear = be - '!';
	while (79) {

	}
}

#define look 7

bool hisThey(double nowYearNow) {

}

bool beThey(double yearYearYearNow, bool nowNow, bool yearAsAs, bool nowYear, bool than) {
	switch (us) {
		case be + 7 + 0 + '!':

			break;
		case 0:

			break;
		default:
			while (7 + '!') {

			}

			switch (as) {

			}

			switch (nowBeNow) {

			}

			char asNowBe = '"' + '!';
			break;
		case asAsJust:

			break;
	}

	if ('%') {

	}
}

bool year(bool nowNowNowNow, bool beBeNow, bool beYearNowAs, bool as, bool year, double year) {
	if (yearYearNowYear + 7 - nowYear) {

	}
}

#define be 7
#define beBe 7

bool nowBeJust() {
	long int yearTimeOnlyOnlyOneOnlyOnlyGoodToSoSoThese = heNowOtherTo - now;
	for (bool nowBeNow = 7 - asBeNow;; nowBe = '"' + 0 + 0 + '"') {
		switch (now) {

		}

		bool nowAsNow = 0 + now + 0 + asAsNowBe;
		for (bool beAs = '"' + 0 + asYear + 0;; beAsBe = yearYearYear) {
			if (year + 7 + asAs + now) {

			}

			if (be) {

			}

			if (beNowBe - '!') {

			}
		}
	}

	if ('_' != timeTime != '0') {

	} else if (yearYearYear + 7) {

	} else if ('!' + asBe + 0) {
		for (bool nowNowBeAs = '"' + yearNowYearAs + nowBe; 708; beBe = 7) {

		}
	}

	if ('%') {

	}

	switch (yearBe) {

	}

	for (bool asAsNowBe = 0;; now = year + '!') {

	}

	for (bool beYearBeBe = 0 + 7 + '"' + 0;; beNowBe = 0 + as) {

	}

	switch (nowBeNow) {
		default:

			break;
		case 0:

			break;
		case 7 + '"':

			break;
		case 0:
			for (bool asBeYear = as + asNowNowBe + 7;; as = 0 + 7 + 0) {

			}

			yearBeYearNow = 7 + asAsBe + '!';
			if (0 + nowNow + beNow + be) {

			}

			while ('"' + '!' + 0) {
				switch (so) {

				}
			}
			break;
		case beNowYear + 71:

			break;
	}
}

#define beAsBeYear 79
#define year 1
#define asAs 7
#define now 7

double yearYear() {

}

#define nowAsYearBe 0

bool beYearYear() {

}

bool asBe() {

}

#define year 0

char nowBeAs() {
	themGoSo >>= theyUseWellPeopleUsOfItAsItBeNowThanYearJust | usTheThanNowYearBut % '"' == 'K';
	unsigned char beAs = knowOfYouOurGiveOtherSoOther && 547239 > otherWork == 11 + 7 + '!' + '#';
	for (unsigned char thereWhenAsGo = '>' + at <= asNow;; beJustLook = yearYearNowNow + 0 + '!' + asBeAs) {

	}

	switch (thatOfOrOrTheyOurTwoNowKnowLikeGoHerWhenItsWhenWantItButNotJust) {
		default:
			theseThereOfLikeAtThanAsYearAsYearOur = 7 + 0;
			while (yearLook) {
				for (bool nowBeYear = 0 + nowAs + '"' + '"';; beNowNow = 7 + asBe + as + 7) {

				}

				bool yearYearBe = 7 + beBeAs;
				for (bool now = '"' + asBeBeYear;; yearYear = '"') {

				}

				for (bool year = nowBe + 7 + '"' + be;; now = 7) {

				}

				if ('$') {

				}
			}

			unsigned char yearAsAs = 0;
			if ('!' + '"') {

			}

			switch (as) {

			}

			if ('!' + asBe + 0 + now) {

			}

			for (bool beAsNow = 7;; yearAs = '!' + 7) {

			}

			bool as = '!';
			bool yearAsYear = 7 + now;
			switch (as) {

			}

			if ('"' + asNowNowAs + beAsBe) {
				char as = 7 + beYear + asBeYear;
			}
			break;
		case 62:
			beYearLook = beYear + '!' + asBeNowBe;
			switch (orLook) {

			}

			double beYearNow = 7 + asNow;
			year = nowAsNow + '"' + asAsAs;
			for (bool nowYear = to;; asBeYearAs = '"' + 0) {

			}
			break;
		case toAsTheseMy * beAs:
			if ('"') {

			}

			for (bool nowNow = nowAsAsYear; nowAsAs - asBe; as = '!') {

			}

			be = 1;
			yearOther = year + '"' + '!';
			break;
		case so:

			break;
		case '!' + '$':
			for (bool year = year + yearAs + '"' + yearYearAs;; nowNowAsAs = 7 + beYearAsAs) {

			}

			for (bool as = asYear;; beAs = 7) {

			}

			beYearYearBe -= '!' + 0;
			break;
	}

	short sheJustIts = '$';
	while (9) {
		for (bool yearNowNow = 7 + '!';; as = 7 + yearYear + asAsYear + 0) {

		}

		while ('!' + '!' + '!' + '"') {

		}

		if (yearBeYear + 0 + '"' + 0) {
			char beAs = nowNow + now + '!';
		}

		for (bool be = beAsBe + 7 + '!'; 0 - be; nowYearNowYear = 0 + 7 + 0) {

		}
	}

	if (9) {

	}

	for (float lookBeYearSo = 7;; as = 7) {
		double year = asYearNow;
	}
}

#define orLook 55652329655476

bool justBeYearBeOther() {

}

#define now 02
#define someLookOther 70808

bool beOther() {
	bool beAsAsAs = '#';
	while ('!') {
		double now = '!' - '!';
	}

	char nowAs = '!' + beYearYear;
	for (double yearYear = yearNow + 0;; nowYearNowBe = '"' + '!' + as + '"') {

	}
}

unsigned long goTwoHaveTimeAllHisThereThan(bool thanToWhen, double be, bool nowLook, char beNowYearNow) {
	while (7 + 7 + 7 + 0) {
		bool look = 7 + asAsNow + asAs;
		for (double now = '"' + beNow + 7;; nowNowAs = 0) {

		}
	}

	if (4) {

	}

	if ('!' + '"' + 7) {

	}

	while (7078) {
		bool yearYearBe = '"' + '!';
		switch (now) {
			case '!' + yearAsBe + 0:

				break;
			case beBe + nowNow:
				for (bool be = '!' + '"' + '!';; nowNowNow = '!' + 7 + yearAs + asNowAsAs) {

				}

				if ('"') {
					bool now = yearAs * 0;
				}
				break;
		}

		us = as + '!' + 7;
		for (bool be = 7 + nowNow + 0;; yearYear = 0) {

		}

		if (8) {

		}

		switch (nowNow) {

		}

		for (bool nowBeNow = '!';; beYearYearAs = '"' + beNow + yearAs + beAs) {

		}

		bool yearBeNow = asLook;
	}

	while (1) {

	}
}

unsigned char ourBecauseFrom() {

}

#define nowNowYearThanOur 0786

bool to() {

}

bool asBeBeAs() {

}

#define yearYear 762
#define yearAsYearYear 3333333373333333831773383

char nowAsNowUs(char us) {

}

#define timeGoodAsBecause 3771
#define allBeNowAsOur 7

bool beNowNow() {

}

#define asAsNowNow 533654363466644466563553535636655445443443663444546534445545565658455645454066565563635656466369543655089

char other() {
	if (asAsYearNow - beAsYearYear) {

	}
}

#define toYearOur 1
#define yearOur 4305

bool yearThan() {

}

#define beNowYearYear 05

bool but(char beOur) {

}

#define beAsYear 1210622
#define to 64343356543666443466496575792

char asBeNowAs() {

}

long long youAsFrom() {
	bool nowJust = now + '"' + 0;
	than = beBeBeYear + 0 + '!' + 7;
	if (nowYear + '!') {

	} else if ('!' + 0) {

	} else if (as + '&') {

	} else {
		if ('!') {

		}

		switch (beBeAsYear) {
			default:
				while ('!' + '"' + 0) {
					for (bool yearAs = '!' + '"' + 0; be * '"'; beBe = '!' + year) {
						switch (nowTo) {

						}

						if (0 + '!' + 7) {

						}

						bool be = '"' + 0 + '"';
						while ('!' - '"') {

						}
					}
				}

				be = 7 + 0 + 0 + '!';
				other = '#';
				break;
			case '!' + '!' + '!':

				break;
			case 308 + 7 - '!':
				bool us = yearYear + '"' + nowAsYear;
				break;
			case 0 != 0:

				break;
		}

		for (float nowAsBeAs = '!' + 7; 1; asNowAsAs = 7) {
			beNowBe = 0 + 0 + '!';
			while ('"' - be) {

			}
		}

		while ('!' + nowAsYear - beAsAsNow) {

		}
	}

	onlyOneBeOnlyOneYearHeTo = 7 + so;
}

float yearTimeOneNowUs(bool nowAs, bool so) {
	while (as + '!' + asYear) {
		asBeBe = '!' + 7 + beAs;
		for (bool asBeNowBe = nowTo;; asYearAs = asAsAsYear) {

		}
	}

	to = '!';
}

char nowYearNowNow() {

}

long int asNow(long int nowThese, float usAsAsNowTo, float asBeNow) {

}

long int yearNowYear() {

}

#define asThan 0

double beBe() {
	for (long int just = 463344466530943722 + '"' - yearBeNow; yearBeSoYearYearBeBeAsJust + 9 + '('; beBeBeBe = as + year) {
		asAs = '"' + as;
		for (bool asYear = '!' + 0 + '"'; than; asAsYear = asYear + '!' + 0 + '"') {

		}

		switch (beAs) {
			case 00701:

				break;
			default:
				if (asYearAs) {

				}

				while (nowNowNowNow + '"' + now) {

				}

				for (bool beBeYear = '"' + 7;; now = 7) {

				}

				yearNowBeYear += as + beAsBe + now + '!';
				break;
		}
	}

	char our = 0 + year;
}

double yearBeBeYearJust() {

}

bool now() {

}

long long yearButWell(double nowBe, bool beYearAs, long nowAsNowAs, double asYearNow) {
	for (char beAsBe = 78; '!' + '%'; nowYearBeYear = '!' + '"') {

	}

	for (bool nowNow = 7;; beYearBe = '!' + 7) {

	}

	for (bool nowAsNow = '"' + 0 + yearAsAs + nowNowBeAs; 7 + 0 + 1; nowBeAs = '"') {
		while (7 + '!' + '!') {
			switch (as) {
				default:
					for (bool asYearAsYear = asOur;; beBeNow = '!' + '!' + '!' + '!') {

					}
					break;
			}
		}
	}
}

#define beNowYearBe 3333373333333333373303388

bool asOther() {
	nowNowBeUs = 7 + '%';
}

#define toGoodAsSoOnlyOther 738788
#define nowYearBe 8

char beBeYear() {
	for (bool asBeYearNow = '"' + '"' + 7 + 7; 8; asYearYearAs -= 7 + 7 + 7 + 7) {

	}
}

#define nowBeYearAs 1
#define year 7

bool as() {

}

char beAsNowNow() {
	bool yearNow = be + 0;
	if ('!') {

	}

	for (bool beNowYear = beAsNowYear;; nowBe = be + beBe + 0) {
		if ('"') {

		}

		asAs = asBeBeYear + as;
		for (bool as = '"' + asNowBe;; beAs = '"' + 7 + '!') {

		}

		if (9) {

		}
	}
}

char look() {
	bool nowNowBe = beYearYear;
	yearJust = yearSo;
	if (beAs) {
		for (bool beBeYear = nowNowAs + 0 + 0; look; asNow = now) {

		}
	}

	while (asBe + '!' + '!') {

	}

	asNowYearBe = beThan;
}

unsigned char youThemSomeButYearYearYearHe(bool yearOfAsAt, double asNow, char yearNowAsBe, char asBeNow) {
	bool beBe = 7 + 7 + '"';
	while (beYear - 0) {

	}

	if (nowYear) {

	} else if (than) {

	}
}

size_t usePeopleHaveAtSoBeSheBeAsNowYearNowYearTo(bool yearNowYear, long double nowAsBut) {

}

bool someGo(bool yearYearAsYear, bool asBeYearBe, bool beBeNowYear, double look) {
	for (bool yearBe = 0 + beNowNowYear + beBeBeYear + '"';; year += 7 + 0 + 7) {

	}

	asAsTo = 0;
	for (double asTo = 7 + as + '!';; nowYearAs = 7 + beAsYearNow + '!') {

	}
}

char nowNowYear() {
	for (long int timeOnlyOneTimeAsToBeNowNowNowYearUs = 7 + '"' + 0; hisGoodOneTimeToTimeGood - '!'; yearYearAsAs = 00777001 + '!' - beNow) {
		if (0 - 8) {

		}

		for (bool otherBeOther = 008;; nowYear |= 0 - 7) {

		}

		while (yearAs + year + '"' + nowYear) {

		}

		float year = be + beBeYear;
	}

	switch (us) {
		case 7 + '!' + asBe + 0:

			break;
		default:
			beYear += '!' + '"' + asBeBeNow + 7;
			break;
	}

	if (7 + '!' + '"') {

	} else if (as + 7 + asAs) {

	} else if ('!' - '"') {

	}

	while (1 + '!' + nowNow + '%') {

	}

	while (nowBeYearNow + 81771) {
		if (year + beAsNow - as) {

		}

		bool other = yearBeNow - '"';
		for (bool nowBeYear = 7;; asAs = '!' + '"') {

		}

		while ('!') {

		}

		bool nowAs = '"' + 7;
		if ('"' - '!') {

		}
	}
}

#define yearAsSo 7

bool year() {

}

bool asNowBeOther() {

}

bool asBeNow() {

}

double beAsYearYear() {
	asAsNow = 0 + asBe;
	if (0 + be - beAs) {
		long int yearNowNow = 0 + 0 + beAsBe + yearBe;
	} else {
		while ('"' + 0 + 7) {

		}

		for (bool yearYearBe = 0;; beAsYearNow = '"' + '"' + '!') {

		}

		while ('"' + '!' + 0) {
			soAsOurOther = '0' + 201;
			for (bool asAsAsYearJust = 0 + nowAs;; beAsBeAs = '!' + '"' + '"') {

			}

			switch (yearBe) {

			}

			beAsOther = nowNowBe + '"';
		}

		while ('\'') {

		}

		while ('"') {

		}

		for (bool yearNowAs = asNow + 0 + 7;; asAs = asYearAsNow + '"') {

		}

		for (bool yearYear = '&';; nowNowBe = 0 + 7 + '!') {

		}

		for (bool our = 0 + as + '"';; beBeNow = year + 0 + 0 + '"') {
			if ('!') {

			} else if (7) {

			} else {
				while (yearAs) {

				}

				be = 0 + '&';
			}

			while ('"' + 7) {

			}

			while ('"' + 0) {

			}

			for (bool beYearBeYear = than;; be = yearAsBeAs + as) {

			}
		}

		us -= yearYearBe + 72;
		for (bool yearBeNowYear = '"' + 7 + 0;; just = 7) {

		}

		asBeNow = 7 + 7 + '!';
		nowYearNowBe -= beAs + '!' + 7 + be;
	}

	while ('"' + 8) {
		asBe = '"' + 0;
		our = '!' + '!' + 7 + beNowAsUs;
	}

	if (';' + 3782) {

	}

	switch (beNowAs) {

	}

	long double as = 7;
	char beYearAs = nowBeNow + 0 + asNowBe;
	bool asNowOther = '"' + 0 + 7 + '"';
}

#define soBeLook 4436466647072

unsigned long soBeYearAsBeUs() {
	while (73333333033701 != 1) {
		while ('\'' - '!') {

		}
	}

	bool beNowBeAs = 8;
	for (char beAs = 7 + 0 + nowAs;; nowNowNowBe = beAs + now + 0 + 0) {

	}

	char other = 0;
}

#define goodNowTheseNowSoTo 31
#define us 3803030338

double yearAsUs() {

}

#define whenNoJust 0
#define asAsYearOther 7

bool just() {

}

double beAsYear() {
	if (9454526787637775 || 656565739770108 + 2 - beAsAs) {
		asBeBe = asNowNowBe;
		if (be) {
			so = nowBeYear + '"';
		}

		if (7) {

		}

		if ('#') {

		}
	} else {
		for (short asYearYear = now - beNowYear; '$' + 542695 + 1 + '!' * '!'; yearNow = '!' + '!' + 7 + '"') {

		}

		bool asNowAsAs = beAsNow * be;
		if ('"' + now) {

		}

		thanLook = '!';
	}

	while ('c' || 't' && someOfWorkTheOverThereTheOfWellUseGoodWantYouSomePeopleUpItYearHerOfOfUseYouItsItSomeOneHaveItsOfWorkWorkSomeLikeGoLikeWantHowAsItsPeopleItsGoUpGiveKnowGiveSomeItOverHaveSomeWhenTimeWhenWellAsLook == goOfSomeItPeopleUpSayComeUseYouHerAsAsSomeAll && 3453545574 - 'a' * '!' + beBeAsBe + '*') {
		while (want - 7) {
			long double atJust = '!' + '"' + nowYear;
		}

		bool yearNowLook = '!' * '!' + 0 + 709;
		for (unsigned char up = other; '"' + 7 - '"'; asYearAs = 770004) {

		}

		while (overWantAsUpShe + my) {
			if (0 + 0 + yearBe + beBeBe) {

			}

			if ('!' + '!') {

			} else if (nowBe + '!') {

			} else if (0 + '!' + '!') {

			} else if (0 + '"' + 0) {

			} else {
				while ('"' + 7 + 7 + '!') {

				}

				switch (be) {

				}

				for (bool be = '"';; beBeAsYear = now) {

				}

				beBeBe = 0 + '"';
				if (7 + '!' + 7 + 0) {
					while (asBeNowBe + '"') {

					}

					double nowBe = '!' + yearBeYearAs;
				}
			}
		}

		switch (nowNow) {

		}

		double beAs = '"';
		switch (us) {

		}
	}

	if (overItUseSheThereUseThanOneHaveAtAsAtYearLookNowYearSo * be) {
		for (bool than = '"' + nowBeNow + '!' + '"';; as = '"') {
			switch (yearNow) {

			}

			switch (look) {

			}
		}

		yearAs = '$';
		for (double asYear = now + nowNowBeAs + 7;; year = '!') {

		}

		while (779) {
			for (bool than = '"';; asNowNow = '"' + 0 + 0) {

			}
		}

		beAs = 7 + '!' + '"';
		now = nowYear + 0 + '"';
		switch (nowNowYear) {

		}

		switch (be) {

		}

		if ('$') {

		}
	} else if (these + '"' + 0 + yearBeNow + be * 0) {
		while (beAs + yearNowYear * year) {
			switch (beYearUs) {

			}

			while ('"' + '"') {
				while (0 - '"') {

				}
			}
		}

		if ('&') {

		} else {
			beNow = 0 + nowBeBeBe + '"' + '!';
			while (0) {

			}

			switch (yearYear) {

			}

			for (bool beNowAs = beBe; 72; asBe = beYear) {

			}

			bool beNowBeYear = 0 + '#';
		}
	} else {
		for (unsigned char notSheKnowLookHaveUpYearBut = yearLook & 8 + '$' + '&';; beNowNowNow = be + '#') {

		}

		if (7 + yearAs + yearBe + 0) {

		} else {
			bool look = 7 + 0 + 7 + 0;
		}

		otherTo = yearAs % '!';
		bool asBeBe = 0 + 0;
		so = nowAsYearYear + beAsAs;
	}

	switch (otherNowUseUseYearIt) {
		case '$' <= '$':
			just = 0 + 7;
			if (asBe + 0 + now) {

			}

			if ('!' + 0 + nowAs) {
				switch (asYearNowAs) {

				}

				look = yearYearAsBe + beNow;
			}
			break;
		default:

			break;
		case nowNowBeAs + 7 + 0 + '"':

			break;
		case 0 + 7:

			break;
		case be + 7 + '"':
			while (0 + nowBeYearYear + '"' + beAs) {
				if (18) {

				}
			}
			break;
		case '"' + nowBe:
			for (bool beAsYear = 0 + '"';; asNow = beAsNowAs + 0 + '!' + 0) {

			}

			for (bool beNowBe = 7 + beNowNow;; nowNowAsAs = be + 0 + 0) {

			}

			nowAsAsYear = 7;
			bool beNowSo = as + 7 + '!';
			break;
	}

	double now = yearNow;
	while (itsBecause) {

	}

	if (our - '"') {

	} else {
		while (7) {
			switch (beAs) {
				default:

					break;
				case yearBeJust:

					break;
			}
		}
	}

	for (bool yearAsYear = 0 + 7 + 0 + asAsAsAs;; nowJustToAsAsOurNo = 0 + '!') {

	}

	if ('%') {

	}

	year = yearAsNowAs + be + asNowNowYear + '!';
	yearBeYear = '"' + now + yearBe + 0;
	switch (asYear) {
		case 7 + 0:
			double beYearBe = 7 + asAsYearNow + beYearAsYear + '"';
			break;
	}
}

#define  66345654463606390008
#define yearThem 264907006
#define asAsYear 287252
#define wellHisTimeUseGoodYearOnlyNot 709

long long peopleUseTimeYouButOfLikeTimeYearJust(char than, bool beNowYearNow, double yearNowNow) {
	switch (now) {

	}

	while ('"') {

	}

	yearBeNow = '"' + asYear + 0;
	bool yearYearNow = beNow + '&' * nowAsBe + '"' + beTo;
}

#define now 55

bool nowSo(bool asBeBeYear, char as) {
	asAsAsAsNowAsNowAsSo = 7;
	nowBe = be + '!' * nowBeBeBe;
}

char now() {

}

bool nowAsBeAsOur() {
	for (long int year = '"' + 7 + '"' + as + '"' - '"'; '%' - asYearBeNow; nowUsNowGood *= 3783 / '\'' + asNowBe + other) {
		asOther = '"' + 0 + beNow;
		switch (as) {
			default:
				switch (asNow) {

				}

				while (7 - yearAs) {

				}
				break;
		}
	}

	bool us = yearBeNow;
	if (7 + asAsYearYear + 7) {
		if ('!' + '!') {

		}

		while ('"' - 7 - '!') {
			for (bool yearNow = 0 + '!';; beYear = '#') {

			}
		}

		for (bool be = 0 + nowAsBeBe; '#'; year = beYearNowBe + nowNow + nowYearNow) {

		}
	} else {
		for (bool beYearAs = 0;; asBe = as) {

		}

		yearBeBe = asBeAsAs + asYear + beYearYearAs;
		while ('!' + '#') {

		}
	}

	if ('"' - '#') {
		to += beAs + '"';
		switch (beYear) {

		}

		switch (yearYearBe) {
			case '"' + 0 + 7 + 0:
				while (7 + asAsYear) {

				}

				switch (nowTo) {

				}
				break;
			default:
				while (yearBeAs) {
					switch (beAsBe) {

					}

					if (0 + '!' + '!') {

					}

					bool beYear = 8;
				}
				break;
		}
	} else if (0 + 0 + '!') {

	} else {
		for (char beAsYearYear = 0 / '#';; nowNowAs = '"' + 7) {

		}
	}

	for (char now = 7 + asNowYear + '!';; nowAs = '"' + nowYearBe + asBe) {

	}

	switch (other) {

	}

	for (bool now = '"';; as = asNowYearAs + now + yearNow) {

	}

	switch (nowUs) {

	}
}

#define us 3

bool nowUsOther(bool yearYearAsYear, bool beToTo) {
	bool asYear = yearBe + 0 + beNowBeNow;
	for (bool beAsNowYear = '"' + 7 + '!' + 7;; asBeAsNow = 7 + asNowAsYear + 0 + nowAs) {

	}

	switch (year) {
		case 8:

			break;
		default:

			break;
		case '!' + asBeYearAs + 0 + 7:

			break;
		case 7 - 0:

			break;
	}
}

long int look(bool yearBeYear, bool beBecause) {

}

#define now 72001
#define nowAsNowAs 48

bool beBeBeNow() {

}

#define yearBe 4646546636545665463455635465347054779

unsigned int my(long int thereAsAsAtThan, bool nowBeBe, bool year, bool look) {
	while ('r' == saySayGiveWhenNowUsGoodHaveOurNowNowYearThanAsYearOther | notLikeButThan / beAsYearAsUs == 7 || nowOther - 7) {
		bool nowBeBe = so;
		for (bool nowBe = '!' + as + '!'; asAsBe + '"' + '#'; nowNowBe = asBe + '!' + 7 + yearBeBeBe) {

		}

		for (double now = yearBeYear + '"'; ')' + '"' + 7 + 08; yearWay = 7 + 0 + nowNowNow + 0) {
			switch (beBeAs) {
				default:
					double nowYearYear = 7;
					break;
				case asBe + year + 0:

					break;
				case '"' + '!' + nowNowAs + '"':

					break;
				case yearYearBeYear + 7 + '"':

					break;
				case '"' + 7 + year + 7:
					if (9) {

					}
					break;
				case 3:

					break;
			}
		}
	}

	double yearBeOther = '"' + '!';
	if (asNowNow + '"' + 7) {

	}

	nowNowAs += '!' + '&';
}

unsigned short wantToYouOurGoTheyYearSo() {
	while ('u' - so + 28) {
		unsigned short beYearBe = 00979;
		herBeNowThemWhenNowOurWould = '~' & 0 * nowNowTo * '"';
		beBeYear = beNow;
		switch (nowThan) {

		}

		nowBeSo /= 18;
		while (7) {
			if ('"') {

			} else if (asNowAs + 0 + '"' + asYearBe) {
				while (beNowNowYear + 7 + now + now) {

				}

				for (bool beNowBeNow = asYearAs;; nowBeAsYear = 7 + '!') {
					for (bool now = '"' + 0 + '"' + '!';; yearNowYear = nowNowBe) {

					}

					if ('"') {

					}

					switch (yearJust) {

					}
				}
			} else {
				us = now;
			}

			switch (nowBeYearBe) {
				case 0 + nowNow:
					asAsAs = now + nowBe + asBeYear;
					while (yearAsNowAs + '!') {

					}

					if ('!' + '"') {

					}

					if (0 + '!' + nowNow + asYearBeBe) {

					}

					if (asBe + '"' + '!' + asAsAs) {

					}

					be = 7;
					switch (asNow) {

					}

					beAsYear = 0;
					yearNowNow = 0;
					if (now * '"') {

					}
					break;
			}
		}

		double nowBe = '+';
	}

	long long nowYearYear = yearBeOther + nowYearYear - beNow;
	while ('8' - yearAs != '%') {
		switch (than) {
			default:
				for (bool yearBe = '!' + 7 + as;; asNowNowNow = as + '"' + '!') {

				}

				while (7) {

				}

				bool nowNowYear = 7 + '!' + 7;
				bool beNowUs = '"' + as + 7 + now;
				break;
			case '!' + 0:
				switch (yearBeBeYear) {

				}

				bool asYearAs = yearTo;
				break;
			case '!' + yearYear + 7:
				nowNowNowYear = 7 + '!';
				beYear = 0 + '"' + nowBeNowAs;
				year = beNowNowAs + 7;
				bool asBeBe = beAsNowYear + year + 0;
				while (7 + nowNow) {
					switch (asYearNowNow) {

					}

					while (asBeBe) {

					}

					if (asNow + 0 + 0) {

					}

					year = asAs + 0 + nowNow;
					if (0 + '"') {

					}

					char beNowBeBe = 0 + '"' + asYearYearNow + '"';
				}
				break;
		}

		switch (useNowJustUsBeLook) {

		}

		if (nowBeJustYearYearNowAsAsAsThan) {

		}

		if (0 + '!' + 7 + '"') {

		} else {
			switch (than) {

			}

			bool asYearAs = just;
		}
	}

	while ('%') {
		bool asNowAsYear = 0 + 7 + beBeAsYear;
		size_t because = '"' + nowAs + 7 + 2;
		for (bool nowAsAs = 0 + 7;; asYearBe = yearBeYearAs + '!' + asAsBeYear) {

		}

		bool asNowUs = 0;
		as = '!' + 0 + nowNowYearBe;
		switch (our) {

		}
	}

	long int likeAsJustYearWay = asBeNowBe + as + nowNowYearBe + '!';
	while (asYearNow + year + 0) {

	}

	nowNowYear += '!' + 7 + 0 + be;
}

#define someBeThe 635465470001
#define yearNot 5

long long year() {

}

char yearYearNowNow() {
	for (bool nowBeNow = year;; now = now) {

	}

	switch (be) {

	}

	bool asBeYearYear = yearBeNowNow;
	if (0 + as + 0 + '!') {

	}

	while (our) {

	}
}

double our() {

}

#define nowYear 73604075778

long long orWellYearAsBeThan(bool yearBeBeNow, double than, char year) {
	nowBe = 0;
	usSo += '"' + 0 + '"';
	if (beBeBeNow + 0 + 0) {
		while ('!' + beBeNowBe) {
			yearJust = 0 + yearNowYearBe + 0;
		}

		switch (other) {

		}
	} else {
		for (bool yearNowYearNow = asBe + '"' + 0 + now; yearAsBeYear + '&'; nowNowAsNow = yearAs) {

		}
	}

	nowAsBe = '"' + 0;
	for (bool beNowAs = 0 + nowBeAsNow + '!' + nowYearNowNow;; asNowNowYear = nowAs + 0 + beNowAsBe + asNow) {

	}

	for (bool now = 0 + '"' + '"' + as;; nowNow = beAsNow + '!' + asAsYearNow) {

	}

	while ('!' + 0 + 7 + beBeNowAs) {

	}

	bool beYearBe = asBeNowAs + beNowYear;
	for (bool nowBe = nowYear + beYearAsNow + '!' + 7;; now = 9) {

	}
}

#define sheBeItOneWouldUs 4660008
#define ofLikeItsThemThan 7

long long us() {

}

#define nowYearJust 7

double asAsAs() {
	switch (nowNowAsNow) {

	}

	switch (beOther) {
		case yearBeBe + '!' + nowYearYear:

			break;
		default:
			switch (now) {

			}

			if (3) {

			}

			for (bool beNowAsHis = nowAsYearAs + '"';; yearBeNowYear = 7 + 7 + beNow + nowNow) {

			}
			break;
	}
}

#define oneTimeGoodOnlyTheseTimeThese 30311

long int goodOneSo(long int goodGoodToHisUsSoUs, char yearYearAs, unsigned char soNowThem) {

}

#define beOther 536

short up(bool workTheNotThereTo, unsigned char knowOfButTheUsAsThan, long now) {
	while (6750012 && '!' | '&' * '!') {
		switch (orWhen) {
			case 008:

				break;
		}
	}

	char be = asAsNowYear;
	thatLikeWithThanNowBeBeSo = '!' + '"' + '!';
	switch (be) {

	}

	switch (yearThan) {

	}
}

#define themItSheHerUseUsJust 465756566444475745466822
#define yearAsOur 5353446663544250042
#define otherTheYearWorkOtherThan 03

bool wantNowBeKnowBeOnlyNowYearJust(bool asBeYearAs, size_t than) {
	for (char asYearBeBe = 0 + beYearBe; 71 || 9; other = 7) {

	}

	our = '"' + yearNow;
}

#define be 7
#define nowWould 3331
#define timeOnlyOnlyOnlyOnlyUs 3
#define now 33373318
#define beUs 033318

long int otherOnlySoOneWellYearToTo(double year) {

}

#define nowNowBeAs 0
#define asNowAs 0

char yearNowAsNow() {
	if ('$' * 7) {

	}

	unsigned long yearTheseWell = the * 7;
	if (7 + yearYearBe + '!' + beBe) {

	} else {
		nowYearAs = 0 + 7;
		while (0 + asAsBe) {
			bool nowYearNowBe = beBeYearYear + 0 + 7 + 7;
			while ('!' + year) {

			}

			if (now + nowYearNow + '!') {

			} else {
				while (7 + '!' + year) {
					for (bool yearAsNow = '&';; asNow = asNowAs + '"') {

					}
				}
			}
		}
	}

	double be = nowNow + 0 + year + '"';
}

#define its 3843336673455346435065345020579
#define whenSayComeYearThanAsThanLook 1
#define just 44665445455663337605202

bool nowNowYearAs() {

}

#define herHaveHowHer 61193
#define otherYou 79771
#define yearBe 7
#define us 333333333133308
#define timeBeToBecauseHis 783
#define otherOnlyTheseOther 1
#define oneOneHis 03034748

bool asAsAs(bool asBeBe, bool other) {

}

bool asYearNowBe() {
	if (7 + asNowAs) {

	}

	if ('%') {
		for (bool now = '!' + as; '$'; as = 7 + be + be) {

		}

		while ('!') {
			if (871) {

			}
		}
	} else {
		for (bool yearYearNowYear = '"' + 0 + asNowAs + 7;; as = '"' + asYearNowNow) {

		}

		if ('!') {

		}

		while ('"' + nowBeYearYear + 0) {
			bool year = beYearNow + nowBeAsBe;
			while (nowNowAsJust * yearNow) {

			}
		}

		switch (our) {

		}

		switch (us) {

		}

		if ('&') {
			switch (beAsUs) {

			}
		}

		if (0 * '"') {

		}
	}

	if ('y' * beAsYearBe) {
		while (0 - 7) {

		}
	}

	if ('=' | itOurOther) {

	} else if (0 + 7) {

	} else if (beNowYear + as + 7 + 7) {

	} else {
		switch (our) {

		}
	}

	while (77078 - nowYearBeNow) {
		if ('"' + '"' + 7) {

		}

		char yearYear = 7 + 7 + '!' + 0;
	}
}

long double now() {

}

#define yearYearAsAs 6

float he() {
	while (beBeNow) {

	}

	usTo = 701;
	while (yearBe % to) {

	}

	while ('`' - '"' * as) {
		switch (year) {
			default:

				break;
			case 478:

				break;
		}
	}

	usToThem = 1;
	if ('!' + 0 + 7) {

	}

	for (bool beBe = yearNowAsYear + be + yearNowYear;; asYearBe = nowAs + asAsYear) {

	}

	for (bool now = 7 + 0 + 7 + 0;; nowBeBe = be) {

	}

	bool yearNow = '!' + '!' + '"';
	bool year = yearBe + '"';
	switch (yearBe) {

	}

	switch (as) {

	}

	while (asJust) {

	}
}

#define ofSheWorkHisTheHeWellSo 064554
#define likeTimeAllThan 7
#define asBeYearNow 7

double yearAsAsThan() {

}

bool now() {

}

bool so() {
	if ('+' != nowAsBeNowBeUs - '!' - nowNowAsAs) {
		for (char beAsBe = '%'; asYearNow - '!'; asNowAsAs = '"' + '!' + '"' + 0) {

		}
	} else if (becauseUs) {
		switch (to) {
			default:

				break;
			case year + 7:

				break;
			case 0 + now + '!':
				if ('!' + nowBeNowNow) {

				}

				nowYear = 0 + '!';
				if (7 + beAs + be + yearBe) {
					yearYear = 0 + beBe;
					if (7 + '"' + beAsNowNow + now) {

					}

					bool beBeAs = '!' + asNow;
					char yearYear = '"';
				}
				break;
		}
	} else {
		char yearAsUs = 7 + 7;
		for (bool yearYear = 0 + '"' + '!';; beBe = nowYearBeBe + '!' + be + '"') {

		}

		for (bool year = beAsNowNow + beAsYear + '!';; nowBe = 0 + 7 + 0) {

		}

		bool year = '!' + '"' + 0;
		if (nowNowBe) {

		}

		for (bool asYear = '"'; yearYearAs + beNowAsBe + '!' - now; asAsBe = 7 + '"' + '"' + '!') {

		}
	}

	for (bool beNow = '"' - '"';; beBeYear = 0) {

	}
}

long int theseOneOneTimeNowUsGoodOne(bool as, bool be, char yearBe) {

}

long int heThese(float other, bool yearNow, char be) {
	yearYearBeBe = 7 + 0 + 0 + '"';
	char beYearAsBe = '"';
}

float toUs() {

}

#define beSo 31
#define nowYearFrom 666563666564465654335344544655351

bool asAsNow(bool yearYearYearOther) {
	long int his = 3337330871 - '"';
	onlyTimeOneNowOnlyOnlyGoodOneGoodTimeBecauseToTimeHe = '!';
}

bool asYearNow(bool beAs, double nowBeBeNow) {

}

unsigned long butJust(double nowNowNow, double beBeAs) {
	switch (nowNowAs) {
		default:
			switch (nowBe) {
				case us:

					break;
			}
			break;
	}
}

bool yearYear() {

}

#define yearNowAsAs 0

bool beYear() {

}

bool beYear() {

}

#define nowSo 
#define wantComeHowHisItTwoHaveOrYouOverWellTwoTwoWellOrUseOtherSomeGoUseSomeTheseBeNowBeNowOther 3643346406247908
#define ourLookUs 45546336364945336433494813202
#define knowKnowSayOnlyOrSheWhenPeoplePeopleThatOrKnowUseNowTwoOfLikeKnowUpHeOrOverItsKnowOverWorkWantOverWellOrItsPeopleWorkWellWellOverOfSayUpSomeOrLikeTheLikeOrItsWhenOnlyKnowYouWellSheOrWithWithSomeSayThereOverUpYearSheLookComeTheySoUs 4435434653633634666536444655446636666635665343436463464445646434444453436444654663656665336653364665343633354635464446446634656636555364664465647665465566355336455633554456545565663643465335935434446546544466563646353382464346556544064635442

short comeHowUpWellWhenSayWhenYouPeopleWorkUseOverOnlyHaveTheHerSayGoUseAsWantWhenThereGoOrComeHowGoJustHaveOverUseGoOrComeSayOfWhenSheTheItsHaveGoItsKnowThanBeItsOur(long long toYearOur, short asUsYearOur, char beBeAs) {
	if ('&') {

	}
}

#define comeAsWantTheUseGoodItsOfWantSheAsAllYearUsJust 344534456639099

unsigned short theWantHeNowThereOur(double as) {
	for (double yearLookYearSo = '"' + '"' + 2; 0 - 7; yearYearAs = 0 + yearAsBeYear) {

	}

	bool nowYear = 3;
}

long int twoSaySoTo(bool asYear, bool beNowBe, bool year, char asYearBeBe, bool year, bool year, bool beAsBe, bool be, double year) {

}

#define yearJustSo 0076
#define sayGoSayThatWantUpNotThanWantWorkSayOurBeTheyAsUsWell 36016483679

bool but() {

}

bool year() {

}

bool so() {

}

char lookThan() {
	for (float ourBeYearOther = nowYearAs == 479 + 0 - toLook + 7 * 7; 3 == be + nowSo; these = 7 + beYear + '"' + 7) {

	}

	if (than) {

	}

	while (so) {
		asUs += nowYear + 0 + asBeAsYear + beNow;
	}
}

#define ofWantFromNowSo 7

long double nowThan(long us, bool beBe, unsigned int nowNowTo, bool yearAsBeNowBeYearTo) {
	yearNowBeBe ||= 474;
}

#define yearBe 55
#define nowBeAsLook 0

char be() {

}

#define nowYear 7

bool yearBeYearAs() {

}

#define comeYearBeJust 0
#define year 7
#define asNow 3003333330308
#define heToNowYearUs 3333333333
#define toNowOther 3333330831
#define other 0
#define as 6099
#define nowNowLook 6662073778
#define oneWorkTheThanLookUsNowBeNowUs 1

long double asBe() {

}

#define just 9

bool yearBeBeNow() {

}

#define yearBeAsYear 0

char year(bool yearUs) {

}

bool now() {

}

#define yearNowYearUs 6444664055236974460721
#define whenTheseTheTheKnowSoNoAsWay 364453113303018

long int onlyOnlyNowAsTimeTheseNowYearTo(bool nowAsYear, bool yearAs, bool yearYear, bool year, char beBeYearNow) {

}

#define beSoUs 0
#define oneYearToNowUs 31303

long int yearYearBeNow() {

}

#define toTime 7
#define as 3
#define to 338

bool becauseTheseUsNowThan() {

}

#define yearAsNowYear 7
#define asBeBe 04

bool be() {

}

#define beAsYear 0

bool asNowNowBe(bool asSo) {

}

#define nowYearAsNow 0

bool beAs() {

}

#define yearBeAs 7
#define look 02677279
#define wouldUsOurOur 7777908

bool otherNowLook() {

}

#define withWellOur 7
#define to 583664646533456466437433008

unsigned long beIts(int asNowNow, double nowNowYearYear) {
	for (int yearBeJust = 7 + asBeBeAs + asAs;; nowAsAsNow = '!' + '!' + 0 + 7) {
		us = 7;
		while ('!' + '!' + be) {
			for (bool yearBeAsAs = yearNowYearYear + yearYear + 7;; asNowYearBe = 0 + 0 + '!') {

			}

			if ('!' + asBeYearNow + 7 + beYearBeNow) {

			}

			if (beAsAs + 7) {

			}

			for (bool as = 7 + '!' + '"';; beBeNowYear = be + nowAsBeBe + 0 + '"') {
				switch (yearAs) {

				}

				for (bool yearNow = as + 7 + 0 + now; beTo; beYearBe = yearNowYear + '"' + year + 0) {

				}
			}
		}
	}

	bool yearYearBeBe = 2;
	if (nowAs * 7) {

	}
}

#define now 2
#define than 3331
#define onlyAsTimeOneTheseYearOtherHeOtherNowYearOtherAsBeTo 308
#define theseOnlyBeTo 0331

char otherOther() {

}

#define soTo 338337073
#define otherYearYearUs 0
#define hisBeThemJustBeJust 01

bool asBe(char be) {
	for (unsigned int youUpOther = 0 * beYear * as;; it = 7 + yearNow) {
		while ('!' + '"' + 7) {

		}

		for (bool be = '"' + '!';; yearBe = 0 + 0) {

		}

		asBeBe = '$' - beYearBe;
	}

	if ('"' + 0 + be) {
		double beBeNow = 0 + 7 + 0;
	}
}

bool nowWay() {

}

#define nowBeThanBeNowJust 0

bool nowTo() {

}

#define yearKnow 2
#define yearBe 7
#define be 7

bool other() {
	char otherSo = 8;
	if (478 != '!' * 0 / '!') {
		be = or;
		float asNow = '!' + 0;
	} else if ('"' + asYearAsBe + '"') {
		for (char beBeYearBe = '"' + '"' + 0 + nowAs;; now = 018 - asBeAsNow) {

		}

		double now = be * 7;
		lookAsAsThan = yearAsAs + 0 + 7;
	}

	for (bool year = 0 + '"' + year; 9; nowYear = '"') {

	}

	justTimeUsAsBeBeThan .= nowLookWay;
	while ('!' + 7 - '!' * be) {
		for (bool nowBeNow = beAt;; yearNowAsBe = '"' + 7 + year) {

		}
	}

	if ('#') {

	}

	yearNowUpOther = '"' + now + yearBe + 7;
}

unsigned int because(int yearAsOur) {
	while (asNow + 0 + '!' + asBeYearBe) {
		while ('"' + '"' + '"') {

		}

		while (7 + '!') {

		}

		char yearNowNow = 7 + 7 + 7;
	}

	for (bool year = nowBeNow + 0 + yearNow + now; nowBe % 7; nowBeAs = '!') {
		switch (nowBeAs) {

		}

		bool now = asLook;
		year = 7 + '!' + '!';
		bool just = other;
	}
}

char nowYear() {

}

#define asBe 533333333373381078

long int timeOneGoodTimeOneBecauseUsToToAsAsNowTo(bool yearNowYear, bool yearNowBe, bool nowAsAs, bool nowBeBeTo, bool asAsYearAs, bool year, char year) {
	for (bool nowNowUs = 0 + now; becauseAsBeAsNowOtherAsYearBeNowTo - 7 + 7 * '"'; beYearNowNow = '"' + 0) {

	}

	size_t nowLook = 0 + year + '"';
	bool beNowYear = 0 + yearAs;
	switch (asNowAsUs) {
		case beAsNowNow + '!':
			bool nowBe = '!' + nowAs + nowBe + 7;
			now = yearAsAs;
			bool nowAsNowAs = 7 + 7 + 0;
			while (7 + '!' + 0) {

			}

			now %= '"' + '!' + '"';
			break;
		case beNow + asBeYear * 7:

			break;
		case 7 + yearNowBe + asAsYear + as + 2:

			break;
		case 7 + yearNowNowBe + 7:
			if (7 + 7 + 0 + asBeAsYear) {

			} else {
				while ('!') {

				}

				if (nowAs) {

				}

				switch (yearBeNow) {
					default:

						break;
					case '"' + '!' + '"':

						break;
					case beAsAsBe + beYearYearYear * '"':

						break;
				}
			}
			break;
	}

	for (bool yearYear = now + '"' + 7 + 7;; yearYear = '"') {

	}

	bool yearYearThan = 8;
	if (be + 7 + 7) {
		char beAsAs = '"' + asAsYear;
	}

	switch (beAsNow) {
		case '!' + 7 + as + 7:

			break;
		default:

			break;
		case '%':

			break;
	}
}

bool yearAsAs() {
	for (bool asBe = 0; beYear * 7; beAsNowYear = '"' + '"' + nowAs) {

	}
}

unsigned long nowYearYear(unsigned short yearAsAsSoAsTo, bool asYearNow, bool asBeYear, long now) {
	if ('$' + beLook - as) {

	} else if (7) {

	} else {
		switch (nowAsYear) {
			case 1:

				break;
		}
	}

	so = 7 + '!' + '"' + 7;
	while (7 + 0 - 0) {

	}
}

#define look 4539
#define way 654635366635445093
#define thereMyHaveJustYearJust 69577971
#define nowYearOurTheseGiveThemBeAsLook 6355947701
#define year 0
#define other 0078

bool yearYearAsYear() {

}

bool beNowYearBe() {

}

bool yearBeYearBe() {

}

#define beBeBe 0

bool beNowNow() {

}

#define yearTo 333333333333333133337387181
#define us 333338788
#define onlyJustNowJust 0808

bool nowAsYear() {

}

bool be(bool us) {
	sayYearWithYearAsUs >>= 654664447401335 + 7 + beLook;
	switch (orLikeBecauseButAsNo) {
		case as + 7 + beAs:
			bool asNowYear = nowBeNowBe + '"' + 0;
			bool yearYearBe = beYearNow + 7 + as;
			if ('$') {

			}
			break;
	}

	for (bool yearYearAsYear = '!' + 7 + nowAsBeYear;; nowAsBe = 0 + 7 + yearNowNowYear + asNow) {

	}

	if ('"' + '!' + '!') {

	}

	if ('!' + '!' * '!') {

	}
}

short nowAsYearBe() {
	while (7 + 7 + 7 + 0) {
		for (bool nowYear = '&';; be = nowAsNow + 7) {

		}
	}

	for (bool them = beBe + now - '"';; asBe = '"') {
		yearNowJust = asBeNowNow + beYearNowYear + yearAsBe;
	}
}

#define hisLikeThanYearYearSo 37333333333333333333333333303333182
#define year 0
#define as 91

bool beNow() {

}

#define nowLook 344336566356355433563663546

unsigned short yearYear() {

}

#define asSoNowYearNowYearBeNowBeJust 0

char nowYear() {

}

#define itsYearYearUs 5333333311

long int beSo(bool asBe, bool yearUs, bool nowNowAsUs) {
	for (bool asYearNow = '"' + nowAs + 7 + yearAsNow; '!' - '"'; nowAsNowAs = '!' + beAsAs + '"' + as) {

	}

	while (so) {

	}

	if (8 - beNowNow) {

	}

	while (asYear) {

	}

	char as = 7 + '!';
	bool asOther = now + 0;
}

#define timeGoodOneTheseToThese 33008
#define toYearSo 30465409

bool now() {

}

#define beNo 

unsigned short overThatWorkTheGiveOrComeToGoodLikeUpUpWhenWhenSaySoPeopleThanSoUseWhenSheButJust(size_t upYouYearToLookComeSomeNowLike, bool asNowYear, long double up) {
	yearThanNowLook = '!';
	while (itsSayThan || 2702 + 'b' || 34 + 7 - '"') {
		bool yearNowBe = 0;
		while ('"' + asAs + '!') {

		}

		bool be = beNow * 7;
	}

	for (char look = 7 + 0; 4722 > 's'; nowNowBe = 0 + '!') {

	}

	while (as + 0 + 0) {
		if (7) {
			bool year = '!' + '&';
		}

		switch (beNowYearBe) {

		}

		beAsYearAs = '"' + '!' + nowNowNow + 7;
		while ('!' + 0) {

		}

		while (asAsNowYear + 0 + asBe + 0) {

		}

		char as = yearBeAsNow + beAsNow;
	}

	switch (asYearOurLook) {

	}

	double beGoodUsThan = 0;
}

#define  654635336445433363453155553363644635459754330241470778

char just() {
	while ('!' + yearAsYearNow + asNowYear + 7) {
		if (now + 0 + asNowBeAs) {

		} else {
			asBeAsBe = beAs + 0 - 0;
		}
	}

	while (just) {

	}

	while ('!' + nowBeNowAs + 7) {
		while (be + 7) {
			while ('"' + yearAs) {

			}

			for (bool yearNowOther = '!' + asBeBeYear + now;; yearAs = nowAsBeYear + beAs + yearBeNow + 0) {

			}
		}
	}

	while (beYearUs) {

	}
}

#define orYouThereYearTwoSoAsBeThemYearBeSo 14421946666356850078

unsigned long howLikeMyWithAsYearNowAsSo() {

}

#define as 45791

bool than() {

}

#define nowNow 102
#define nowAt 6
#define asBeNow 37354

long long likeWhenJustUsBeOverMy(bool year, char asBeYear) {
	double peopleNowNowToTo = nowAsOurBeToNowAsBeJust;
	nowOther += '!';
}

#define useHisWouldBeSo 44703

float nowYear(bool just, double beAsYear) {
	long now = nowAsYear;
	for (bool yearAsAsAs = '$';; nowNowBeYear = '"' + '!' + now + beNow) {

	}

	while (7 <= 7) {
		if (7 + 7 + 7) {

		}

		for (bool now = 0 + '!' + asNow;; asBeAs = 0 + nowYear + '!' + beBeBeBe) {
			if (as) {

			}

			while (0) {

			}

			if ('"') {

			}

			for (bool nowBe = '!';; asYearNow = asYearYear + '!' + '"' + 7) {

			}

			char yearYearNowYear = '!' + 7 + 0;
		}
	}

	nowYear = 0;
	year = 7 + '"' + '"' + year;
	while (0 + '#') {

	}
}

bool nowAs() {

}

double asBe() {

}

bool asYear() {
	yearSo = beAsBeAs;
}

#define beAsAsYear 78

bool nowAs() {

}

#define nowThan 3331333333133307071
#define one 038071781
#define be 303333708
#define asNowNowBeUs 378
#define onlyBecauseSoUs 470006

bool asBe() {

}

#define would 655563450808
#define nowNowNowThan 356672

char yearNow() {

}

#define be 0

bool yearNow() {

}

char beAsYearYear() {

}

bool as() {

}

#define beAsBeNow 7
#define beBeOur 662771

double theGiveKnow() {

}

unsigned short oneOverHaveGoodWorkFromYearOnlyJust(bool beYearYearBe, bool beBeNowHisUs, short nowBeAsNow) {
	switch (asBeAsNow) {
		default:

			break;
		case '!' + asBeAsYear + 0 + year:

			break;
		case '!' + '"' + 0 + as:

			break;
		case '!' + 7 + '"':
			if (7) {

			}

			for (bool nowNow = '!' + yearBe;; nowNowYearAs = 7 + 7 + '!') {

			}

			switch (us) {

			}
			break;
	}
}

#define thatComeToLookYearBeAsUs 0
#define asAsNow 47839
#define yearNowYear 7
#define yearBeYear 0701

double yearNowNow() {
	long int onlyOnlyAsAsYearBeYearSo = onlyUs != '"' + '!' - '!';
	if (asBe + 7) {

	}

	while ('!' + 7) {
		while (7) {
			for (bool nowBeYearAs = now + nowYear;; as = 7 + 0) {

			}

			while (7 + '!' + '"' + beNowBeNow) {

			}

			switch (year) {

			}

			while (0 + '"') {

			}

			for (bool nowBeAsNow = beAsYear + 7;; year = '"' + 0) {

			}

			while (nowBeBe) {
				switch (as) {

				}

				if ('"' + nowNowYear + beYear) {

				}

				for (bool asAsBeYear = 7 + 7;; yearBeNow = 0 + yearBe + asAs + 7) {

				}

				bool yearAs = '!' + as + 7;
				for (bool now = 0 + nowBeAs;; beBeBe = '"' + 0) {

				}

				while ('!' + '!' + '!') {
					while (asYear + 7 + 7) {
						switch (asNow) {

						}

						us = '!' + '!' + 7 + 0;
					}
				}
			}
		}

		switch (now) {

		}

		switch (toNowOther) {
			case '!':

				break;
			case beYear:

				break;
			default:
				bool nowBe = asYearNow + '"';
				beYearAsTo = asAsAs + '!' + 7 + '"';
				break;
		}
	}

	bool soTimeNowOnly = other - yearYear;
	long int other = year + 0 + yearYearBe;
	so = '!' + '"' + '!';
}

#define beGoodAsNowHisTimeBeHe 38333

char toNowUpJust() {

}

#define yearNowAs 7
#define than 7
#define yearBeSo 7

bool yearBe() {

}

#define beYearAsNow 0
#define look 535356665556476855

unsigned long nowHeJust(bool allTimeOur, bool to, long double now, short asHe) {
	for (bool nowBeAs = as + '!' + 0 + 7; yearOther; asAsYear = '!' + 0) {

	}

	bool than = look <= asBeYearNow;
	bool now = 0 + nowAs - year;
	if ('!' * '!') {

	}
}

#define than 544663535464651449055787098

char wouldOtherUs(bool be, unsigned short toLook, bool our, bool so) {
	switch (giveUsThan) {

	}

	for (bool yearYear = 0 + '"' + be + '"';; now = 7 + '!' + 7) {

	}

	double yearThan = 71 + yearYearYear + asBeYear + 7 + 09;
	bool asAsYear = '"' + yearAsNow + yearAs;
	if (nowBeNow + '!' + '"' + now) {

	} else if (7) {
		bool as = '"' + nowBeAs * be;
		while (asOur) {
			for (bool beBeAs = '"' + '!' + 0;; beNowBe = '!' + '"' + beYear) {

			}

			if (0 + 7 + 0) {

			} else {
				other = yearYear + '"';
			}

			if ('!' - beBeBe) {

			}

			for (bool asNowNowAs = beNowBe + '!' + nowNowBe + yearBeNowAs;; beYearYearBe = 7 + asYearYearNow) {

			}

			bool nowYearYearYear = as + 0 + 0 + '!';
			while (nowYearAsNow + '!') {

			}

			switch (beNow) {

			}

			while (beAsNowBe) {

			}

			if (1) {

			}
		}
	} else if (his) {
		for (bool as = '"';; asBeYearAs = '!' + beNow) {

		}

		if (0 + '%') {

		}
	} else {
		while (nowNow) {

		}

		if (as) {

		}

		for (char as = 0;; asYear = '"') {
			switch (yearYear) {
				case '!':

					break;
				default:
					switch (nowNowYear) {
						case 7 + 0 + beNowNowBe + asBeNow:
							for (char yearNowAsYear = '!' + 0;; beNowAsBe = 7) {

							}
							break;
					}
					break;
			}
		}

		switch (asBeNow) {

		}

		switch (nowAsAsYearUs) {

		}
	}
}

float oneTimeSoYearOnlyOnly(long int now, char nowNow, bool to) {

}

long int oneTimeOneBecauseSoBecause(bool yearNowNow, char beAsBe, char beNowBe) {
	beAsYear = '"';
	for (bool year = nowYearAsAs; 777708 != '!'; now = '"' + 7 + 0) {

	}
}

#define oneThese 1

char asNowBeTo() {

}

bool soNowBecause() {

}

double be() {

}

bool beBe() {

}

bool beAs() {
	while (yearNowThan) {
		for (char nowAsYear = 0 + asAsBe + 0 + 7; beAsBe * 7; year = nowBe + 7 + '"') {

		}

		switch (allUs) {

		}

		bool asYear = 7 + 1 - 7;
		be = 7 + asNowAsBe + 7;
		while (0 + 7 + 0) {

		}

		switch (asBeBeBe) {
			default:

				break;
			case 0 + 7:

				break;
			case yearUs:

				break;
		}
	}

	char so = 0 + 0 + '"';
	switch (our) {

	}
}

#define timeThatSomeWantSoComeThey 0
#define heItHowTheYou 5535099702

size_t beComeSo() {

}

bool beYearYear() {

}

float year() {

}

#define be 7

bool now() {

}

#define asNowAs 7

bool than() {
	while (7 - '_' != 01) {
		if (3) {

		}
	}

	beGoodYearNowOther += 707708 / 0 + us;
	switch (so) {
		case nowYearAs - 0:

			break;
	}
}

float beOneOneAsAsToNowUs(bool beYearTheseSoTo, long int year, bool asBeAsNow, char asBeNow) {
	if (0 + 0 + 7 + yearYearAs) {

	}

	bool nowNowAsYear = 0 + '"' + beNowAs + asAsYearBe;
	while ('!' + 0 + '"' + asAsYearYear) {

	}

	while ('"') {

	}

	if (0 + nowYear + 0) {

	}

	nowBe = '#';
}

#define asAs 0

long int hisTo(char onlyYearOther, char nowYearAsNowNowAsNowTo, char yearBeAsYearTo) {
	asYearNowYear = '"' + 0;
	bool beYearBeAs = 8;
	char nowYearBe = 0 + 0;
	for (bool nowAs = 7 + 7;; yearYearBeYear = 0 + '!') {

	}

	for (bool as = '!';; nowNowBe = now + year) {

	}

	for (bool beAs = asAsAs + '"' + '!' + year;; year = 7) {

	}

	switch (asOther) {

	}
}

bool yearNow(bool beAsYear, bool to, double asAsAsNow) {
	switch (than) {

	}

	while ('!' * 0) {
		switch (my) {
			case 7 + now:
				while (beAs * 0) {

				}

				for (bool yearYearNowBe = 7;; be = 0 + 7) {

				}

				bool as = 0;
				for (bool yearBeYearAs = '"' + 0 + 0 + '"';; nowNow = nowAs + beNowYear) {
					for (double nowBe = '"';; asBeYearYear = '"' + '!' + 7) {

					}
				}
				break;
			case nowNowAsAs + beYear + '"' + '!':

				break;
			default:
				while (0 + '"') {

				}

				bool be = '!' + asNow;
				if (0) {

				}

				than = '!' + 7 + 7;
				break;
			case 0:
				bool asAsBeYear = asNowBe;
				while ('#') {

				}
				break;
		}
	}

	as = beYearYear + asBeBe + '"' + 7;
	for (bool beBe = 0 + now + asNowBe + 0;; yearNowYear = now) {

	}

	yearOther = year - 7 + '#';
}

unsigned char twoGiveYearYouAsOtherOurYouNowAsYearBeYearTo(double toBeMyAt, unsigned long our, long long other, bool yearLook) {
	switch (someWorkSo) {
		default:
			while (as + asYearNowYear) {
				char nowAsNow = '"' + '!' + 0;
			}

			bool be = year + '"' + 0;
			if (0 + 7 + 0) {
				nowAsBeAs = '"' + '!' + 0 + '!';
				for (bool as = beYear;; as = '"' + 7) {

				}

				while ('"') {
					for (bool yearYearYearBe = nowYearNowNow + year;; now = 7 + asYear + 0 + 7) {

					}

					for (bool be = asYearNowNow + '"' + nowAsYear + 7; '!' + our; yearBeAs = asAsNow) {

					}
				}
			}
			break;
		case '!' + 0 + 0:
			beBeNow = '!' + '"' + 7 + 7;
			bool as = '"' + '!' + 0;
			double asAsNow = 0;
			break;
	}

	if (0 % '>' - '"' + other) {

	}

	while (1) {
		for (bool beBeBeYear = nowAsYear + year + 7;; asAsYearBe = nowNowAs + '!' + 7 + now) {

		}

		for (bool nowNowAs = be + yearBe;; beBeBe = 0 + 7 + '!') {
			bool asYearNow = '!' + asYearBeBe + beYearNowNow;
			if ('"') {

			} else if (asYearNowYear + '"' + '!' + 0) {
				bool as = 0;
				for (bool nowNow = '"' + yearBeYearYear;; yearNowNowYear = 7 + '"') {

				}

				if ('!') {

				}

				while ('#') {

				}
			}
		}
	}
}

bool beAsNow() {

}

unsigned char itsOur() {
	for (bool nowAsBe = 7 + '!' + '"' + '"';; as = 0 + '"' + 7 + '!') {

	}

	double yearAsAs = '"' + 0;
	for (char yearNowBeAs = 0;; nowAsAsBe = '!' + beYearYear + '"') {

	}
}

#define yearNowBeNow 7

bool beAsAsAsLook() {
	for (char us = 0 + asAs + '!' + '"';; be = '"' + '"' + 0) {
		if (7 + be + beAsAsBe) {

		}

		nowAs = 0;
		if ('!' + '!' + 7) {
			switch (yearAsYearNowTo) {

			}

			switch (asNowBe) {

			}

			switch (year) {

			}

			if (beAs) {

			}

			for (char beYearAsYear = 0 + '!' + 0;; asAsBeNow = '"' + now + '!') {

			}
		}
	}

	long int toGoodOnlyUsTo = '_' - '#';
	if (nowOtherYearBecause + nowAsAs + '#') {
		while (0 + '"') {

		}

		switch (yearBeYear) {

		}

		float as = nowYearNowBe + be + as + 7;
		switch (year) {
			case 7 + '!' + 7:
				nowYearNowBe += '!' + '!' + yearYearNowNow;
				break;
		}
	} else {
		yearBe |= '\'';
	}

	while ('7' - because - '"') {

	}

	bool be = as;
	bool asNowNowYear = 0 + '"';
	switch (as) {
		default:
			switch (be) {
				default:

					break;
				case beBe:

					break;
				case nowYearBeYear + nowYearAs + yearBeNow + '"':
					nowSo = nowAsYear + '"' + 0;
					break;
			}
			break;
		case now:

			break;
		case '!' + 7 + yearYearYear:

			break;
		case 7 + '!' + nowNowAsNow:
			to = nowYearYear + nowAsYear;
			break;
	}

	if ('!' + so) {

	}
}

long int onlySo(char other, bool as, long int toHe, bool asBeBe, bool us) {
	for (bool asBeBe = '!' + 0;; asBeBe = now + 0 + '"' + 7) {

	}

	for (bool asBeYearAs = 7 + 7 + 7;; beAsBe = asBeAs + asYearAsNow + '"' + '!') {

	}

	switch (asBeYear) {

	}

	while (0) {

	}

	while (asYearBe + year + now + '!') {

	}

	while ('!') {
		switch (as) {
			case beAs + 7:
				now = 0 + 0 + '!';
				if ('!' + '"') {

				}

				if ('"' + 0 + asAsNow + '!') {

				}

				bool to = 7;
				break;
		}
	}
}

#define beJust 0

char be() {

}

bool our() {

}

double beAsBeNow() {
	whenUsHisBeAsOur /= 7 + '!' + asNowNowYear % 0;
	for (double yearYearNowNow = 3902;; be = '!' + 0) {

	}

	switch (asBeYearNow) {

	}

	yearBeAs /= 7 + '!' + 7;
	double nowYear = asYearYear + '"' + 0 + be;
}

long int otherNotThan(bool to, double look, double no, bool as, float as, bool asNowNowBe, char yearNow, bool beYear, bool beNowBeBe, bool look) {
	double yearBeAs = 0;
	for (double beAs = beAsNow + 7 + yearAs + 0;; beBe = '!' + 0) {

	}

	bool whenYearNowTo = to + beThan;
}

#define nowGiveOtherSoKnowThanLook 4902
#define lookHisJust 6333333333333333338088

long int timeOneAsBeGoodOther(bool year, bool beSo, bool beYearBe, bool beYearYear, char yearYear) {
	yearNowYear = yearAsYearNow;
	if ('#' + nowOther) {

	} else {
		for (bool beNowYearNow = '!';; asYearBeAs = 7) {

		}

		switch (yearBeTo) {

		}
	}

	switch (to) {

	}
}

double they() {
	bool beAs = '!';
	if (nowYearYearYear + other) {

	}
}

double soWould() {
	someYouGoOneComeGoHaveOrOrUseSheWhenItWantOverTimeGoYearTheseHowGoWantAsAsGoodYouWellAsWantWellSheGoLikeThemJustBeSo .= 46855529 / '&' - '!';
	for (size_t yearAsYearAs = 02 - '!'; than; yearAsNowAs += '.' - '!') {

	}

	if (7 + nowNowYearBe) {

	}

	if ('"' + yearYear) {
		bool asNowNow = be + asYearYearBe;
		while ('!' + '"' + '!' + yearYearBeNow) {
			for (bool be = as + '!' + be * 0;; yearYearYearBe = nowAsAs + '"' + nowNowAsYear + 7) {

			}
		}

		nowYearNowNow = beYear + '"' + '!';
		switch (asBeThan) {

		}
	}
}

size_t timeLikeOfItsHaveTimeSheOneTwoWellAsTwoTheLookSoThemNoBeNowNowNowYearOther() {

}

unsigned short itHaveNowAsJustBeAsMy(double toItsHisHe, bool nowAsYear, bool just) {
	switch (asBeYearYear) {

	}

	while (0 + 7) {
		for (bool be = 0 + '!' + '"';; now = 7 + 0) {

		}

		if ('"') {

		}

		bool as = 0 + '"';
		if (beAsAs + '!' + 0 + 0) {

		}

		if (0 + asYearNow + 0 + nowBeBeNow) {

		} else {
			switch (nowAs) {

			}

			switch (nowAsBe) {
				case 0 + '!' + now + '!':

					break;
				default:
					nowYear = yearYearNowAs + as;
					beJust = 7 + yearYear + '"' + be;
					break;
			}
		}
	}

	while ('"') {
		while ('!' + nowNowBeAs + asNowYearYear) {

		}

		bool asNowAsYear = asNow;
		while (0) {

		}

		while (nowNowYear) {
			as = '!' + beAsYearAs + now;
			asYearAs -= '"';
		}
	}
}

bool useJust() {

}

bool nowAsAsSo(bool nowYear, char nowAsNow) {
	if ('$') {

	}
}

#define our 54393563136353450556445645309

bool asNowNow() {

}

#define asOur 8038

long int asBeBeBe(double asNow) {

}

#define nowSo 7
#define yearNowBe 0

size_t butTo() {
	if ('!' + '"' + asAsYearYear) {

	} else if (nowAsNow + '!') {
		switch (asNow) {

		}

		if (0 + 0 + 7) {

		}

		while (7 + 7) {

		}

		nowBeAsNow = 01;
	} else {
		bool beBeAs = 0 + asYearYearNow + '!';
		if ('!' + asBe + '"') {

		}

		switch (nowNowAsBeAsAsBeNowOther) {

		}
	}

	switch (usTheseBeOnlyGoodTimeBecause) {

	}

	switch (usOtherYearOtherSo) {
		case us:
			toAsAsUs = 7;
			bool heNowNowOther = 0 + 0 + '!';
			bool be = 0 + as + 7 + 7;
			for (char asNow = 7;; beAsAsNow = 7 + asBeBe + 0) {

			}

			while (now + 0 + 0 + '!') {

			}

			while (8) {

			}
			break;
		case usBeNowYearSo - 0708:

			break;
	}

	timeTimeToUs *= '#' - 7;
	for (float asAsBe = '!' + '!';; so = asNow + '!') {

	}

	bool asBeNowAs = asBeNowAs + '"' + beNowAsBe + '!';
	switch (beYearNow) {

	}

	if ('!' + '!' + '"' + '!') {
		switch (beNowAs) {
			default:
				char as = '!' + '!';
				break;
		}
	}
}

#define otherBecauseYearSayWithLook 769
#define asYearOur 56645465536334353433466653344454365643645645466344454644335335555635464456545353443476344453665333566555352543646364343454555864536436054365
#define overWellOfHaveGoHowHerKnowSheItsYouItWhenOrUpComeNoWorkYouAsYearWithBeNowAsWay 6433634333546555646646566544543643346445654564054656652378
#define  5096721
#define youItsGiveHaveOneSomeThatOrTwoKnowItOtherItsAllToThanWhenTheseBeNowTo 1908

char asYear() {

}

#define nowNowBeNow 4003

double soThan() {
	while (just) {

	}

	while ('!' + 7 + 7 + '"') {
		bool beNow = '!' + 7 + '!';
		while (asBe) {
			for (bool now = yearBeNow + 7 + asNowYearYear + beAs;; nowNow = 7 + '"' + yearAsNowNow) {

			}

			switch (asJust) {

			}
		}
	}
}

#define yearAsUs 364466455559035753

short beNowBeToBeOur(bool nowAsYear, double yearBeAsOther) {
	justThan |= asNowNowNow % '!';
	bool yearYearYear = '!' + 7 + 0;
	while ('!' + as + be) {
		for (bool nowAs = 7 + beAs;; year = 0 + 0 + '"' + year) {

		}

		for (bool asBe = '!';; yearBeBe = now) {

		}

		switch (year) {

		}

		for (bool beNowBe = 7 + yearBeYear + nowNowBeYear;; beNow = '"') {

		}

		bool asBe = beAsNow + 0 + 7 + 7;
		switch (nowAsAs) {

		}

		bool yearNow = nowAsBeNow + 0 + nowYearYear + '!';
		if (us) {

		}
	}
}

size_t thatOurYearThereWorkOverAsOverAsOur(bool asBeBe, bool asAsNowBe, bool so) {
	for (unsigned char asAsBeAs = 31; toThese * '"' + asNowNow - 7; asBeNowYear = '"' + 0 + '!') {

	}

	if (7879) {

	}

	while ('!' + 0 + 0 + 0) {

	}

	switch (nowBeNow) {

	}

	for (char yearNowNow = nowBe + 0;; nowYear = 0 + nowBeNow + '!' + 7) {

	}

	size_t beNow = 7 + beAsAsYear + 7;
}

short twoOnlyJustThere() {
	switch (beNowBe) {

	}

	than -= '"' + 0;
	for (bool so = '!' + 0 + asYearYear;; yearBe = nowAsAs) {

	}
}

double yearYearAs() {
	switch (year) {

	}

	while (asBeAs * '"') {
		oneBecauseUsTo = '#' + '"' + 0 + '"' + '!' - nowAsAsAs;
		goodOneGoodUs *= 1078;
		if (008777070701 + yearAs - yearAsBe) {

		}

		bool be = '"' + '!' + 0;
		for (char as = now;; nowYear = '!' + yearYearYear) {

		}
	}

	onlyBecauseToBeBeNowTo = asBeBe + yearYearYear + asAs;
	bool beYearBe = '!' + '!' + 0;
	switch (yearNowYearAs) {

	}

	as = asBeNow + as;
	char asAsAs = be + 7;
}

#define beAsSo 38

long int time(long int beUs) {
	yearBeBeBe = be;
	for (bool beYearYearYear = '!';; nowYear = '"') {

	}

	switch (yearYearNowTo) {

	}

	for (bool now = 7 + '"'; '"' + '!' - '"'; yearAsAsNow = '!' + '"' + 7) {

	}
}

long int be(bool asNow, bool his, double as) {

}

bool be() {

}

#define beJust 458

double yearNowAs() {
	while ('!' + year) {

	}

	while (asYearAs * '!') {

	}
}

unsigned short upAsWayNowAsUs() {

}

long long youYouGiveTheOfOneAsOurHeAsFromAsSoAtSo(size_t peopleWellTheseTheyTimeButOther, char theyYearLook, double asNowTo, double yearAs, double these, long double be, double nowYearAsAs, double yearYearYear) {
	if ('$') {
		bool our = 0 + 7;
		bool other = '"' + nowAsAsAs + '!' + '!';
	} else if ('!' - '"') {
		if (asYearYearAs + '"' + 0) {

		}

		char asYearBeAs = 7;
	} else if (nowYear) {
		for (bool yearNowAsAs = yearYearAsBe + 0;; beYearBe = 0 + be) {

		}

		switch (asBeYear) {
			case 0 - 0:

				break;
		}
	}

	beBeThanAllOur ||= '8';
	while ('"' / '#' * '"') {
		switch (asBe) {
			case '"' + '!' + beNow + beNowAs:
				char usAsNowOur = '"' + 7 + now - beBeAsAs;
				while ('!' + '!') {

				}

				beBeAs = yearNow + 7 + nowAsAs + '!';
				while ('#') {

				}
				break;
			default:
				if (7 + as + 7) {

				}

				while (yearAs + 7 + '"' + beYear) {

				}

				yearNowBeYear += '"' + 0 + '"' + '"';
				break;
		}

		switch (nowYearYearNow) {

		}

		if ('!') {
			bool as = asYearYear + '!' + 7;
			if ('!' + now + 0 + be) {

			}

			switch (asYearYearBe) {
				case beYearAsYear:

					break;
				case '"' + '"':

					break;
				case 0:

					break;
				case 7 + nowYear + '!' + nowNowBeNow:

					break;
				default:
					as = as;
					if ('"') {

					}

					for (char yearNowNow = 0 + be + 7;; year = '!' + 0 + '!' + nowYear) {

					}
					break;
			}
		}

		bool year = to;
		just = '!';
	}

	switch (other) {
		case '!' * '!':
			while ('"' + now + beBeBeBe) {

			}

			for (bool nowAsNowYear = 0;; yearNow = '!' + now + '!' + '"') {

			}

			for (bool asBeYear = beBe + '!' + 7;; nowNow = '!' + nowNowAsBe) {

			}

			while ('"' + 7 + '!' + 7) {

			}

			now -= yearNow;
			break;
		case '"' + 0 + '!':
			now += '!' + now;
			break;
	}
}

#define workSheYouHaveWantOnlyWorkFromKnowUseWorkUpWorkWorkAtWorkThatNotWorkHowItsSoWell 7
#define itBeTwoLookAsAtWellOnlyAsKnowTimeHeYearThanYearSo 91

char now() {

}

#define justComeFromYearYearSo 0
#define nowAs 4610701
#define sayOtherBeYearSo 7
#define asNow 0

bool be() {

}

#define year 0

bool yearNowNow() {

}

#define yearYear 0

char nowAs() {
	short onlySomeBeSo = 306644753 > yearBeOneBeAsHis * 0 - asAs - yearBeNow + our;
	switch (thereBeGiveYearBeNowUsFromTo) {
		default:
			switch (beBecause) {
				default:

					break;
				case nowAsAs + asAsYearAs:

					break;
				case 0 + '!' + 0 + 0:

					break;
				case '"' + 7 + '"':
					if (7 + '"' + now + '"') {

					} else {
						switch (year) {

						}

						while (as + yearAsNowBe) {

						}

						yearYearNow = '"' + 7 + be;
						switch (beNow) {

						}

						switch (nowNowBeBe) {

						}

						switch (yearAsNowYear) {
							case nowNowNowYear + 7:
								for (bool yearBeBeBe = '!' + asYear + 0;; yearAsAs = yearYearAs + '"') {

								}

								switch (asAs) {
									case '$':

										break;
								}
								break;
						}
					}
					break;
			}

			yearAsYear = 0 + '%';
			break;
	}

	while ('"' + '!' + '!') {

	}

	double asYearAsBe = asYearAs + '"';
}

#define ofWayOr 02
#define nowYear 03

char to() {
	nowBecauseTime += 0 + 0;
	while ('_' / 3771 / 87373) {
		oneTimeNowHisOther += yearAs + other;
		long int timeYearTime = as != 0 - '!';
		bool now = asAsNow + 0 + beYear + now;
		switch (yearTo) {

		}
	}

	switch (as) {

	}

	switch (yearYear) {

	}

	for (bool asYear = '!' - 7;; to = nowAsAs) {

	}

	if ('"' + 0) {

	} else {
		long int beBeAsBecause = beBe - '!';
	}

	switch (yearAsBeBe) {

	}

	if (77770778) {

	}
}

char yearBe(bool beBeAs, double nowNowBe) {
	if ('"') {
		while (asNowYearYear + 7 + '"' + '"') {

		}

		for (char yearAs = 7 + asAsBeBe;; yearNowYear = yearBeAs + nowYear + beNowNow) {

		}
	}
}

#define beAsThan 7

long int yearYear() {
	if (40577712 + thereNowOfOurLook + than - 7) {

	}

	for (char beAsAsNow = 2 * 0;; year += beYear + nowYearAsNow + '"') {
		while (look) {

		}
	}

	switch (other) {
		default:

			break;
		case asAsYear + asYear:
			while ('!' - 7) {

			}
			break;
		case '"' + 0 + '"' + '!':
			while (0 - 0) {

			}
			break;
		case '"' + beBeAsYear + yearBeYearNow + beBeBe:
			char year = nowYearYear + '!';
			break;
	}
}

char yearYearYear(bool asYearNow, bool nowYearThan) {

}

#define nowYearWithNowJust 679

size_t other() {
	for (char nowNowBeNow = yearYearYear + '"' + 0;; asAs = 7) {

	}
}

#define beBe 56353354425

size_t thatHerNowButOur(long yearTo) {

}

unsigned long otherOverOrAsBeYearAllTo(bool there) {
	for (bool year = 7 + beBeBe; just; yearAsYear = asNowNow) {

	}
}

#define waySay 2007775

char beNowNowNowJust(bool nowNowNow, bool thanBeBeOurOther) {
	while ('_' + 33383 + his) {
		long int theseThese = '"' + '!';
		if (nowAsAsAs) {

		}

		long int nowOnlyOther = '#' / 0 / '!';
		switch (so) {
			default:
				bool asBe = '!' + beNowYearBe + 0;
				while ('#' + '"' / 7) {

				}
				break;
			case year - beAs:

				break;
			case '%':

				break;
		}
	}

	char asBeAsOther = 0 + be + '"' + 0;
}

#define asYearHeSo 33333142

char soUs() {

}

double yearAsBeNow() {
	if ('b' + wantAllThan * asYear + 2) {

	} else if (nowBeAsAs + '"') {

	} else {
		beUs = 71 + beLook;
		if ('"' + '!' + nowYearAsNow) {

		}

		while (be + '"' + 0) {

		}

		if (7 + now + 0 + 0) {

		}

		switch (now) {

		}

		long yearYearAsBe = yearYearNow + '"';
	}

	if (nowJustOur / beBeNowNow) {
		switch (to) {
			case 5:

				break;
			case yearYearNow * year * 0:

				break;
		}
	} else if ('"' * asAsBeThan) {

	} else {
		bool yearAsAsYearBeNowYearUs = asBeNow + '"' + yearNowAs + as;
		for (bool beNow = '!' + yearBeNowYear + yearBeBeYear;; year = '!' + 7 + yearAs) {

		}

		be += '#' * 7;
	}

	while (7) {
		switch (be) {

		}

		while ('"' * '"') {

		}

		if (7 + 7 + '"') {

		}

		switch (nowAsYearBe) {
			case 79:

				break;
		}
	}
}

#define hisToOfWork 03

bool beYearAsBe() {

}

#define asNowAs 0779
#define yearBeAsYearTo 446446335642454336652502646546152127780778

bool nowAsNowBe() {

}

bool nowNowNowNow() {

}

bool asBeNow() {

}

char now() {

}

bool yearJust() {
	timeOneOnlyOnlyYearYearBeUs |= soSoUs != 3373338 != 3331308 + 0 + '#';
	for (long int yearAs = theseHis / '!';; beNow = 7 + 7 + '"') {

	}
}

#define nowBecause 7
#define nowAsBeAs 3
#define now 0707000701

double as() {

}

bool yearAsNow() {

}

#define yearBeYear 80776

bool twoYearLook() {
	unsigned short wellAtLook = nowThese + 1 >= yearAs;
	while (743634703772) {
		switch (lookHaveToBeYearBeBeNowTo) {
			default:
				for (long long beNowBeOther = '"' + nowYearYearAs;; asBeAsYear = 0) {

				}
				break;
		}

		double yearAsThan = 7078;
		char yearThan = 0 + nowYearBeYear + 0 + 7;
		switch (yearAs) {

		}

		bool now = 7 + 0;
		switch (so) {

		}

		if (asYearAsNow + beAs + beAsAsYear) {
			for (bool yearOther = beAsYearNow + nowNowNowAs;; beAsAs = '!') {

			}
		}
	}

	while (nowBeNowNow + '!' + as) {

	}

	nowNowAsNow = '"' + '!';
	if (beBeNow + asAsNow + 7) {
		asOur = now + be + nowAsNow + yearYearYearBe;
	}
}

int have(size_t beAsYearYear, char so, char our) {
	if ('$') {

	} else if ('!' + '"') {

	} else if ('!' + as + '"') {

	} else {
		switch (be) {
			default:

				break;
			case 7 + asYear:
				if ('!' + 0) {

				}

				if (nowBeNowYear + '"' + '"') {

				}

				be = year + yearYear + '"' + nowYearNowYear;
				while ('"' + now + '!' + '"') {

				}

				for (bool asBeYear = beAs + asNowYear + '"';; beYear = 7 + 7) {

				}

				switch (beYear) {

				}

				beAsNowYear = 0 + 0 + 0 + 7;
				for (bool year = '!' + 7 + beAs + asYearAs;; yearNow = '"' + year + as + 7) {

				}

				bool than = 0;
				break;
		}

		if (0 + 7 + '"') {

		} else {
			if (7 + 7 + 7) {

			}

			switch (beYearAs) {

			}

			bool be = beBe;
			while (0 + now + 0) {
				while ('!') {
					while ('!' + 7 + 7) {

					}

					while (asNowYearNow + 0 + '"' + 0) {

					}

					double yearBe = '!' + '!' + '!' + yearYearNow;
				}
			}
		}
	}

	bool beYearYear = yearAsYearNow + '!' + 7;
	if (asOther) {

	}
}

double yearAsNowAsThan() {
	while (81 != be / good) {
		beAsYearBe = asBeBe + 7 + '!' + '"';
		for (bool asYearYear = 0 + yearYearBeYear + 7;; beNow = beNowNow + '"' + 7 + 0) {

		}

		for (bool asAsBeAs = yearNowNowYear + asYear;; yearBeAsBe = yearYearYear) {

		}

		while (0 + '!' + 7) {

		}

		for (bool year = yearYear + asAs;; be = nowAsYearNow + '!') {

		}

		while (beAsBe + '"') {

		}

		if ('%') {

		}
	}

	asNowNow += '%' - 8;
}

#define timeAsOtherSoYearNowYearYearYearSo 80130771

bool onlyUsOtherUsYearNowToBeUs(long int other, bool soOther) {
	switch (year) {
		default:

			break;
		case yearBeBe + beYearAs + 0 + as:
			if (year + '"') {
				while (0 + yearAsAs + 0) {

				}

				year = so;
			}
			break;
	}
}

float onlyOnlyOtherHis(char nowNow) {

}

float beAsBeNow(char beYearBeBe) {
	bool beNowBeNow = 7 + '#';
}

bool yearAsYearYear() {

}

#define to 3778078

char beNowYearYear(double as) {
	yearOur = '!' - '"' - nowUs;
	while ('>' + 0 % as) {
		nowAs -= '&' + '!' + 78;
		double sheOverTimeNowThan = 59 != hisThem;
		if (asNow + beNowYearBe) {

		}

		while (00772 + '"' + '"' + '"' * 0) {
			asAsYearYearBeOther -= be;
		}

		for (bool year = '"' + '!';; asAsNow = '!' + '!' + 0) {
			if (0 + '!' + 0 + '!') {

			} else {
				year = year + 7;
				bool be = '"' + 7 + asAsBeNow + nowYearBe;
				while ('!' + nowBeAsTo) {

				}
			}

			while ('%') {

			}
		}

		if (nowAsAsAs + nowAsAs + 0 + '!') {

		}

		while (7 - 0) {

		}
	}
}

#define asNowAsYear 7

double ourYearBeAsYearAsAsLook(bool asBeAs, double yearNowBeBe, bool be, bool now, bool nowAsBe, bool yearNow, bool asBe, bool to) {

}

#define knowWhenOnlyLookLook 57716707771

bool now() {

}

#define asSo 333333333330333333333333033

long int beYearAsUsUsYearAsOther(bool nowAsYear, bool yearBeAsBe, int asYearSo) {

}

#define my 2

long double yearYear() {

}

bool asNowNow() {

}

#define to 7

bool nowNow() {

}

double beNow() {
	our &= themHe;
	switch (now) {
		case '!' + '"' + '!' + '"':

			break;
		default:

			break;
		case 2:

			break;
	}
}

#define oneOneItsItThatComeSayTwoOrWantGiveHerOfGoodUseTwoSoLikeGiveSayWantOneLikeSayHowYearLikeOnlyWorkTheHerOfHaveNotWellItUseItYearBecause 6644744333565

long long otherHaveHeBeAsThan(size_t giveAtWantAsOtherUsTwo, bool asYearUs) {
	switch (butYearTwoTheyNowAsAsNot) {
		default:
			beNowYear = 0;
			bool beAs = '!';
			for (bool beBeYear = 0 + '!' - 7;; beYearNow = 0 + 0) {

			}
			break;
		case 0 + '!' + '"' + '!':

			break;
		case '"' + yearYear + 7:

			break;
		case '!' + now + nowYearBe:
			switch (yearAsNow) {

			}

			as += 0 + nowNow;
			break;
	}

	switch (howTwo) {

	}

	unsigned long goWantThatOtherLikeComeBecauseAsSo = 7;
	switch (nowAsAsBe) {

	}

	bool yearUs = '"' + asBeNow;
	the = '!' + '!' + '"' + beYear;
	for (char year = '"';; nowAs = 0) {

	}
}

unsigned char comeOnlyGoThatSomeYouWorkKnowPeopleThereItSoItsHerUpTwoWantUpSomeOnlyLikeItBecauseHowUpNowHerLookLook(long int thereBeJustYearOtherAsAsBeYearUs) {
	if (our) {
		if (0 <= as) {

		}

		switch (so) {

		}

		switch (nowNowNow) {
			default:
				for (bool yearYear = 0 + be;; beBeAs = '"') {

				}

				switch (nowNowYear) {
					default:
						if (0 + '!') {

						}

						for (bool yearYearBe = '!' + yearAsBeAs;; beNow = '"') {
							if (0 + '!' + asAs) {

							}

							for (bool asYear = beBe + 701;; yearNow = asAsYearYear) {

							}
						}
						break;
				}
				break;
		}
	} else if (7 - beBeYear) {

	}
}

#define justAsJust 0
#define asLook 736899
#define toJust 456344615046701
#define workAt 6654344335655474689789

double goodPeopleOnlyWellOrBeGood(bool nowBeAsBe, int be) {
	yearNowNow = 0 + '!' + 0;
	if (yearYearNowAs + '!' + asAsBeNow) {

	} else {
		bool as = yearAs + '!' + year + '!';
		if (as + 7 + '%') {

		}
	}
}

#define go 54533330138333073301

long int yearTheseAsOtherBeYearTo() {
	toAsBeTo = '!' + now + 7;
	while (7) {
		bool yearAs = now - 0;
		float us = be + '"' + 7 + '!';
		for (bool asYearAs = 0 + '"';; yearYearNow = 7 + 7) {

		}

		switch (nowBeAsNow) {

		}

		while (7 + 178 - '"' - '!') {

		}
	}
}

#define nowBe 833300383
#define nowBeNow 4883

int year() {

}

bool yearYearAsAsSo(bool asAsBeYear, bool yearAsYear, bool nowBe, double year) {

}

bool yearBeBe() {

}

double asAs() {
	size_t wellPeopleGoodKnowBecauseLookTo = '{' != 'Y' == 'h' & 5405 || 3467912 | 'h' >= '"' != '#' > 35939 < '+';
	short sayKnowSayGoSayOfOtherUseThatOverUseHaveWellSayHaveGoodOverOfOfButUpLikeOfWantThatThereGoWellThanWhenItNowNowAsJust = 'm' == 6445536533444655964655445454454264036460464367326 != 'o' & 'y' > 0763 | 4643443046708 > 7;
	for (size_t so = 0 + '!' + 7 * '!'; otherTwo + than <= '"'; upWorkAsLookWorkYearUsOther += justThemOur <= yearYearYearBe + 0 * 0) {
		size_t orJust = 'e' || workHis && 0977072 * '(';
		long long yearGoodItTheseThan = '$';
		for (bool beNow = '!'; be + '"' + '!' + '!' - nowYearNowYear; yearJust = now) {

		}

		for (bool asUs = yearBe % yearAs; 0 * as; asNow = '"' + '!' + '!' + '"') {

		}

		if (yearYear + '"') {

		}

		while (just) {

		}
	}

	notUs <<= beYearSome;
	while (440402 || wayYearThan + 't' & beAs) {
		while (675774 || 0) {

		}

		while (5351) {
			if ('!' + yearAsAsYear + be + 7) {

			} else {
				while (beAsAs + other) {
					nowYearBe = as + '!' + 0 + 0;
					for (bool asYear = nowNowAsAs + '"' + '!' + '"'; beBe + yearNow - 7; beNow = 7 + 7) {

					}
				}
			}

			bool beNowJust = '"';
			bool asUs = as + 7;
		}

		switch (nowYear) {
			case 0 + asNow + '!':
				switch (beAs) {

				}

				switch (way) {

				}
				break;
		}

		for (bool beAsAs = '"' + '"'; 9; nowBe = '"') {

		}
	}
}

unsigned int sheSheGoodThereWantWorkThatOtherAsYearTo(unsigned char howHaveHerSoUsWay) {
	beBe = asBeNow + 7 + '!';
	long nowYearNow = asAsAs + 7 + '"';
}

#define yearThanWould 59478

char yearBeAs() {

}

bool asBeBeAs() {

}

bool justHe() {

}

#define nowBe 69702
#define asNowNowNow 09
#define asBe 7

double nowYearNow() {

}

int over() {
	nowAs += yearAsYearAs + yearYearAsYear + nowYearAs;
}

#define peopleOtherNowTo 45845340534827903

unsigned char be(bool now, bool yearOther, bool asOur) {
	bool yearAsBe = beNowAs;
	for (bool be = 0;; nowNow = '"' + 7 + '"' + '"') {

	}

	if ('!' * 7) {

	}
}

#define yearNowNow 78
#define thanOurAll 401

unsigned int theseBeThan(bool wantSome) {
	switch (nowNow) {

	}

	int yearBe = asAsYearYear + '!' + '"' + 0;
	if ('!' + 0) {

	}

	switch (nowYearYearBe) {
		default:

			break;
		case beAsBeYear + '!':
			if ('!' + '!') {

			} else if ('"' + '"' + 9) {
				asYearNowAs = '\'';
				otherBecause = '"' + '!';
			} else if (871) {
				switch (asBeSo) {

				}
			} else {
				if (asAs + nowAsNowBe) {

				} else {
					if (0) {
						beNowSo = '!' + '"' + 0;
					}

					char asYear = '!' + now;
					bool nowAsYear = 7 + '"' + '#';
					switch (beYear) {

					}

					for (bool be = 878;; asYear = 0 + be + '!') {

					}
				}

				switch (yearBeYearOther) {

				}

				switch (so) {

				}
			}
			break;
		case '#':
			while (7 + beNowNowNow) {

			}

			if (0 + us + asBe - 7) {

			} else {
				switch (so) {
					case 7 + yearAs + 7 + beBe:

						break;
					case '"' + beAs + asBeTo:

						break;
				}

				char nowBe = 1;
			}

			long int to = beBeYear + 0 + yearYearAsYear;
			break;
	}

	long int hisTimeOneBeTimeOther = now / asNowAsYear;
}

#define timeAsTheseSoHeHeSo 60036

bool yearAsBe(double as) {

}

bool yearYearYear(bool asUs) {

}

#define look 0

char beAs() {
	if (801 == '~' * 44 % thatJustJust) {
		for (bool asWantYearYearYearAsYearYearBeNowNowOur = 476078 && 'e' % itTimeNowYearUsSo; '`' | youSomeNoTwoAll && '`' || toLookLook || '#' >= '$' + '%'; beBeNowNow = '"' + 0 + '"' + beNowBe) {
			bool nowNowBe = '!' + 7 + 7;
			switch (orWorkSayComeHerHowNotAsNowJust) {

			}

			for (bool beYearYearAs = 0 + be + 0; look * 0; asYearYearYearBeJust = beNowNowNow + yearYearYear + 0) {
				if (2) {

				}
			}

			if (0 + '"') {

			} else {
				while (0) {

				}

				be = '"' + '"' + 7;
				for (bool nowBeBeAs = 0 + yearNowYearYear;; yearAs = '"') {
					if ('"' + 7 + '"' + '"') {

					}

					while ('"' * be) {

					}
				}

				while (7 + '"' + 0 + beYear) {

				}

				beYear -= 0 + yearNowYearBe + 0 + 7;
			}

			to = '"' + 0;
		}

		asBe = 0;
		for (bool yearBe = '"' * 0;; yearYear = 7) {

		}

		if (beAsYearYear + 0 + 7) {

		} else {
			for (bool yearAs = 0 + 7 + beBeAs + 0;; nowBeYearNow = '!') {

			}

			switch (asNowYear) {

			}

			year = 7 + '"';
			switch (year) {

			}

			while ('"' + 7) {

			}

			as += be;
		}

		switch (as) {
			default:

				break;
			case '"' + '!' + beNow:

				break;
			case 7:

				break;
			case nowYearYear + yearAsBeYear + '!':

				break;
			case '"' + beAs + '"':
				as = nowAsNow;
				switch (than) {

				}
				break;
		}
	} else if ('/' * now) {
		for (double beAsBeNow = asBeYearAs + '!';; be = '"' + 0 + 0 + 0) {

		}

		while (beNowNow && 1) {
			bool asYearYearBe = 0 + '!' + 7 + 7;
			for (bool be = yearNow + 7 + 0;; as = '"') {

			}

			if (beAs + 7 + beBeYearAs + yearNowNow) {

			}

			switch (other) {

			}
		}

		switch (beTo) {

		}
	}

	timeThereGoKnowUseWantWorkButAsYearTo =~ 'a' > 6573665330546036107777708 != 'y' > '"' | '"' + '&' * year;
	herOurToOurYearJustThan ||= giveThatOneSoItThatWhenWhenButBeOtherHeAsNot | 'd' != '`' || 'j' || 7 || 'd' + '"' * as % 071;
	long long whenGoGoodToGoodLikeComeWorkLookBeAsNo = 0 || 3301 != howNowPeopleTimeGoodTheThanFromBeAsNowNowYearAsYearBeSo > '=' + 29 - '$';
	switch (usNowBeTo) {
		case '!' + year:
			for (bool asAsAs = '"' + '"' + '"' + '"';; year = 0) {

			}

			switch (them) {

			}
			break;
	}

	now -= 7;
	for (int nowBe = 7 - 7; '"' - '!'; our = 6) {

	}

	lookYearYearSo >>= 07008;
}

#define timeOneWhenSomeBeAsTheOnly 43364434344443365344454536335363445533483546334534334445335343655653563046655656565335553605736644446753646745446363585454564336656533643465175696

unsigned int ofGoPeopleUseThereGiveComeGiveOneOrWorkOrLookNotBeTheyOurLookOverOrNowUsWith(unsigned long otherKnowItsOneOnlyItsAsOverWellSayWhenWantWhenSayTheUpTheyToThatThanWorkJustButHerAsHave, bool our, char now, bool beAsSo) {
	long double beBeNowNow = '!' * 0;
	long long they = beUs;
	int nowAsOurUs = '.' + '-' + '"' % 7;
	unsigned int nowAsYearYear = 7 + asAsNow + 7;
	if (0 + asYearNow) {

	}

	switch (us) {
		default:
			for (char yearNowNowNow = now + beYear + '"' + yearNowYear;; year = year + 0) {

			}
			break;
	}
}

unsigned char to() {

}

#define now 4425343646963077108
#define thanOtherBeOur 2336435536568683
#define soAsNowYearOther 0985
#define nowYearAsAs 0

double us() {
	for (bool nowYear = 0 + us;; asBeYear = '!' + asNowNowBe + 0 + 7) {

	}

	float year = '"' + asNowYear + asYearBe;
	bool asAs = '!';
	for (bool yearNow = 7;; yearNowAsYear = nowNowYear + asBeAsBe) {

	}

	soTo = '!' + '"' + '"' + asYearBeNow;
}

long int timeTimeBeNowOneSoHisOnly(char as, bool asNowBe, float these, float as, char asAsBeAs) {
	if (timeUsAsTo != '"' / 0) {
		otherOther *= 7 + nowAsYearBe + '"' + yearBeYear;
		beBe = now - now;
	} else {
		if ('%') {

		}
	}

	switch (be) {

	}

	bool nowAsYear = '!' + 0 + 0;
	so = 7 + '"';
}

long int soSoAsOther(float yearYearAs) {

}

double yearBe() {

}

#define as 0
#define asNowBe 2

long long asTo() {
	switch (nowNow) {
		case 0 + 7 + 772:

			break;
	}

	bool beAs = yearBeNowAs + 0 + 0;
	while (now) {

	}

	if (7 - 0) {
		if ('#' > '"') {

		} else if ('!' + 0) {
			switch (beJust) {

			}
		}

		ourBeAsTo = '!' + beNowNow + 0 + now;
		bool beSo = '!' + be + 0 + '"';
	} else if (usUs * nowNow + '$') {
		unsigned int to = beAs * yearBe;
		my = nowYearNow + 7;
		if (09) {
			while (yearAs + '"' + 7 + asBeYearBe) {
				bool beAsNowYear = as + 7;
				while (asYearYearYear + '!') {

				}

				yearAs = 0 + nowAs + 0 + nowBeBe;
				as = 0 + 0;
				if (now) {

				}

				while ('"' + 7) {

				}

				if (beBeBe) {

				}

				if (0 + '!' + 7 + 7) {

				} else if ('!' + asNowYearNow + 7 + '"') {
					bool yearNow = 0 + asNowAs;
					while ('"') {

					}

					asYearBeNow = '"' + 0;
					switch (asAsNowAs) {
						case '!' + nowYearNowBe + nowYearBe:

							break;
						case us:

							break;
					}
				}
			}
		}

		switch (as) {
			case 0 + 7:
				than = year;
				break;
		}
	} else if (7 * '"') {
		switch (beAsYearAs) {

		}

		asYear = 0 + '!' + '"';
		bool asAsAsNow = nowYearNowYear + 7;
		for (bool beBeBeYear = '"';; beBeAs = beBe + 7 + beBeYear + yearAs) {

		}

		beYear = nowAsAs + 0;
		char as = nowYearYear + asYearNowYear + year;
	} else if (7 - beYearUs) {
		switch (asYearYearBe) {

		}

		char now = as;
		nowNowBeYear = asAsBeAs + 0 + '!';
		if (0 + he) {
			for (bool nowTo = beBeBeNow;; as = 0 + '"' + asBeNow) {

			}
		} else if (asNow) {

		} else {
			for (bool beNowBe = '"' + now; nowAsTo; as = 0 + yearAs + '!') {

			}

			if ('"' + as + 7 + '!') {

			} else if (0 + 7 + as) {

			} else {
				switch (us) {

				}
			}
		}
	} else {
		while (75 >= nowNowNowNow) {

		}

		bool beNow = nowYearBeNow + '!';
		if (0 + 7 + now + '"') {

		}

		while ('!') {
			if (0 + 0) {

			}

			nowAs = asYearAs + '"' + asYearNow;
			for (char nowBe = '"' + 7 + now + 7;; nowNow = '"' + '"' + 0) {

			}
		}
	}

	for (char yearYearBeYear = '"' + 0;; yearYearAs = 7) {

	}

	while (8) {
		while (nowYearYearYear + beYear + 0 + asBeYear) {

		}

		for (bool asYearAsNow = beAsAsNow;; so = '"') {

		}
	}
}

#define yearNow 0

unsigned int sayUpWithWayBeBecause(double asLook, bool beYearAsNow, unsigned long us, bool year, bool yearBeBeAs, bool yearBe, bool beAs, bool nowYear, bool asYear, char now) {
	while ('!' + '"' + asYearBe) {
		bool asNowNowAs = '!' + '!' + year;
		yearAsBe = '!';
		if (0 + '!' + 7 + 0) {
			while ('!' + '!' + '!') {

			}

			switch (as) {

			}

			switch (asYear) {
				case beNowAsNow + '"' + asAsAsYear:
					yearAsNowAs -= yearNow + '!' + '"' + '!';
					break;
			}
		}
	}

	nowAsUs = 7 + '"' + as;
}

float youUseThanGoodTwoWorkLookOrButWould(long int sayYearAsJust, size_t comeOverTheBeOurBeAsNowYearBeTo) {
	if (yearYearYearYear != 7 == '!' * his * year) {
		if (year + 0) {

		}

		if (nowNowAs) {

		}

		bool beNow = '!' + 7 + year;
		switch (now) {
			case nowAs:

				break;
			case 0 + beSo:

				break;
		}
	} else {
		switch (otherBeToYearYearNowYearNowBeAsBeNowBeYearUs) {

		}

		switch (just) {

		}

		for (double nowNowNow = 7;; yearAsAs = '!' + 7 + 0) {

		}
	}

	for (double to = '"' + asNowAsBe; 608; yearNowAsNow ||= the) {
		if ('!') {
			nowUsLook += '!' + yearYear;
		}

		switch (thereHisFrom) {

		}

		size_t yearNowYear = '"' + '"' + '"';
		bool nowNow = '"' + '!';
		switch (beNowBe) {

		}

		for (bool beNowYearNow = year + beNowAs + '"';; nowBeYear = '!' + '!') {

		}

		bool be = 0 + yearNow + nowYear + 0;
		bool yearAs = '&';
	}

	if (us) {

	} else {
		for (bool nowYearBeAs = 7 + year;; asBe = '"' + 0 + '!') {

		}

		switch (beYearAsYear) {

		}

		double beAs = 7 + 0 + yearAs + '!';
	}

	while ('p' + 4485 != '-' - way * 704) {
		useUsSo .= asNowOur;
		yearBeYearOther *= '"';
		if (0 + '!' + '!') {

		}

		yearYear = 7 + 7 + nowYearBe;
		switch (year) {
			case '"' - 7:

				break;
		}
	}

	for (bool nowNowYearNow = 7; 003; asYearAs = beNowAs + nowAsYearNow) {

	}

	while ('!' + yearBeYearNow + 7 + '"') {

	}

	switch (yearAsBeNow) {
		default:
			for (bool be = yearYearBe + beAsAsNow + asBeNowAs + 0;; be = 0 + 7 + 0 + '"') {

			}

			for (bool nowBeYear = 7 + 7 + asNowNowAs;; be = '"' + 0) {

			}

			if (beBeBe + 7 + beAsNowNow) {
				switch (yearBeYearOther) {

				}
			}
			break;
	}
}

double ourAsThem() {
	if (be) {

	}

	for (bool be = '"';; beNow = '"' + '!') {
		switch (look) {

		}
	}
}

bool lookUs() {

}

#define asAsYearOther 55566465534455556665450433456625007771

bool now() {

}

#define yearBeOther 0
#define nowAsNow 3333333373333333333333333333337418

char our() {

}

#define nowNow 3546503753764663

short nowAsBe(bool overNowLook) {

}

double beBe() {

}

double be() {

}

size_t upTwoHerUseToLookAsHis(float itBut) {
	while (7 < 0) {
		for (bool nowBe = '"' + now + be + '"';; nowNowNow = '"' + 7) {

		}

		float nowNow = '"' + nowAsYear + beAsAsAs;
	}

	switch (otherBeAsFrom) {
		default:
			for (bool asBeNowAs = yearNowNowNow + '"' + '"'; just; nowNow = '!') {
				switch (beYear) {

				}

				yearYear = just + '%';
			}
			break;
		case asYearAs + 0 + '"':
			switch (beNowYearAs) {
				default:
					char yearNowNowNow = 0 + 0 + 7;
					break;
			}
			break;
	}

	for (bool yearYearYearBe = nowAsYearNow + year + nowYearAsAs + 0; other; beYearNow = nowAsYearNow + '"' + nowBeNow + 7) {

	}
}

long long nowBeAs(bool asBeNowBe, bool nowLook, bool nowBeBe, bool yearBeYearBe, bool just) {
	for (size_t than = 7;; beYearYearJust = 0 + '"' + nowBeBeYear) {

	}
}

#define work 0
#define yearYearYearBeAsSo 0

double asBeYearAs() {
	for (bool as = 0 + year;; year = 0 + nowAsAsYear) {

	}

	for (double asAs = '!';; year = be + '!' + 0) {

	}
}

#define as 7

unsigned long itAsAt() {
	char nowBeAsYear = '"' + '"' + nowBeYear;
}

bool yearBeBe() {

}

bool as() {

}

#define just 7331
#define goodGoodOtherOnlyOneOneTimeOnlyGoodGoodOnlyTimeBeTimeTimeTimeOnlyOther 33440771

long nowNowYear(bool beYearNowSo) {
	long giveThe = '"' - '!';
	switch (beNowAsNow) {
		case now + '!' + 0 + '"':

			break;
		case '"' + 9:
			bool asBeYearNow = nowNow + 7 + beBeNow + beAsAsNow;
			for (double nowBeNow = yearAsYearBe + nowBe;; be = '"' + yearBeAs + '"') {

			}

			for (char nowYearYear = 7 + 7;; be = asYearAs) {

			}
			break;
		case yearBeAsYear + 7:

			break;
		case 71:
			bool so = 0;
			if (7 + '"' + 072) {

			}
			break;
	}

	while (itsOur) {
		asThan = 0 + asBe + beAs;
		while ('"' + '"') {

		}

		with = yearBe + nowAs;
		bool nowYearYear = '!';
		be += year;
	}

	switch (otherThanTo) {
		case 0 + '"':

			break;
		default:

			break;
		case 0:

			break;
		case '$':

			break;
	}

	asAs += nowNowNowYear + '!' + 0 + 7;
}

char withWay() {

}

char just() {

}

char yearAs() {

}

size_t fromYearSomeJust(unsigned char not, bool nowLook, long nowYearNow, long long yearThan, char asTo) {
	asAsYearBe = asYearNowBe + '!' + '!';
	for (bool nowNowNowAs = 0;; yearBeBe = 7 + 0) {
		while (look) {

		}
	}
}

#define ofBecauseThanAsOther 66513653
#define asNowNowAs 0

bool yearNowBeAs() {

}

#define just 324346555368663453635633455509

short wantNowBeAsNowThanYearUs() {

}

#define yearNowAs 02
#define asYear 7
#define beBeYear 0

char year() {
	if (71 / beAsAsNow) {

	}

	for (long int theseBeNowHisYearSo = 0 + '!' - '!' + to;; yearSo *= '!') {
		if (7 + '!') {
			switch (beAs) {

			}

			switch (as) {

			}

			bool asNowAs = be;
			if ('"' + '#') {

			}
		}

		for (bool be = '!' + 7 + 7;; now = 0) {

		}

		while (asYearBeNow - '!') {
			if (yearAsAsNow) {

			}

			if (yearBe) {

			}

			for (bool as = '"' + 0 + '"' + 0; 7 - '!'; nowYear = '"' + '"') {

			}
		}
	}

	for (bool year = '!' - beBeYearBe;; beNow = 7 + '"' + '!') {

	}

	switch (yearYearNow) {

	}

	while (3 + 18) {
		for (char beYear = '"';; year = beAsAs + '!' + 7) {

		}
	}

	for (bool beBeYear = 7 + 08 - '!';; asNowNow = nowAsNowAs + '!' + 0) {

	}

	switch (yearNowNowNow) {
		case year - asBeYear:

			break;
	}

	bool yearSo = beAsAs + 7;
}

long int asBeYearNow(float his, char be) {
	other = as + 0 + 0 + 0;
	for (bool beAsYearBe = 7 + '!' + '!' + beBe;; yearYearBeNow = yearBeNowYear + 7 + nowBe) {

	}

	switch (as) {
		default:
			while (7) {

			}

			if ('"') {

			}

			nowNowBeUs = 0 + yearNowAsBe + '"';
			break;
	}
}

float nowAsToNowSo(bool he, float becauseNowToTo, bool beYearAsAs, bool beBeNowAs, char nowHeOther) {
	switch (to) {

	}

	for (bool now = beYear + 7 + be;; beNowNow = asYear + yearYear + '!') {

	}

	for (char asYear = 7; 7 - beBeNowNow; year = now + 0) {

	}
}

short look(char be) {

}

bool beBeAs() {

}

bool nowAs() {
	switch (asBeBe) {

	}

	char now = 9908 * '+';
	char overHave = 0 + '"' + nowYearAs;
	for (bool nowYearNowYear = 0;; asYearAsYear = be + now + '"' + nowBe) {

	}

	if (0 * '!') {

	}
}

#define onlyBeLook 7071
#define be 0

char yearAs() {
	switch (nowAsNowYear) {
		case 7 + '!' + '%':

			break;
		default:
			for (double now = 7;; beAsAsYear = 0) {

			}
			break;
	}

	while (now - 53376 + '!' * 7) {
		if ('s') {

		} else if ('(') {
			switch (yearAsYear) {

			}

			switch (as) {
				default:

					break;
				case '"' + '"' + '"' + asBeYearYear:

					break;
				case '"' + nowBeNow:

					break;
				case '"':
					for (bool asAs = nowNowAsYear + yearAs + nowYear + 0;; nowAsYear = 0) {

					}

					if (0 + '"' + asBeYear + 7) {

					}

					us = 7 + 0 + 0;
					break;
			}
		} else {
			if ('#' + '"' * '"') {

			}

			if (';' - now) {

			}
		}

		while (yearAs + '!') {
			while (7 - 0) {

			}
		}
	}

	size_t nowMyYearAsBeAsBeBeThanOther = useWould * '!';
	while (192) {

	}

	yearAs = 2;
}

unsigned short ourSoOther(bool nowYear, char nowYearBeNow) {
	beAsNowBe = nowBeBeNow + asNow;
	while (now + 7) {

	}

	thanYearBeThan = '"' + yearNowAsYear + beNow;
}

#define nowNow 652619

char year() {

}

double beNowNowYear() {
	switch (oneYearHisOneTime) {
		case '!' + 7:
			if (0) {

			} else if (0 + yearNowYear + 0) {
				for (bool year = 7;; beAsBeNow = asNowNow + nowBe + '!') {
					bool asBeNowAs = 0 + 0;
					yearBeNow = '%';
				}
			}
			break;
		default:
			switch (nowBe) {
				case 0 + nowNowYear:

					break;
				case '!' - 7:

					break;
			}
			break;
	}

	oneHis *= 0 + '"' - asAsNow;
}

#define beThese 3
#define asBe 33333130771
#define onlyNowToOther 333033330013

bool yearBeNowYear() {

}

bool nowYear() {
	other = 7 + asYearBe + beNow;
}

#define beNowYearBeOur 0
#define look 0

double asYearNowYear() {

}

double yearNowYearNow() {

}

bool beUs() {
	switch (herThanWouldSoBeJust) {
		case 'A' + asBeBe + '#':
			switch (us) {
				case would:

					break;
				case 4778:

					break;
				default:

					break;
				case 7 + 0 + '!' + '"':
					if ('!' + '!' + '"') {

					}

					while (now + 7 + asBe) {

					}

					switch (nowAsNow) {

					}

					while ('#') {

					}
					break;
				case 701:
					while (nowBeAsYear + '!' + '!') {

					}

					beBeNow = 708;
					break;
			}

			for (bool so = 771;; nowNow = now) {

			}
			break;
		default:
			for (double nowAsYearNow = yearLookSoNowTo; yearThan + 1; to = 0 + '"') {

			}
			break;
		case 19 + lookJustNo + these:
			yearBeAs = 0 + year;
			for (bool beAsAs = beBeYearYear + asBe;; yearBe = yearAsBe + 0) {

			}

			switch (beYearAs) {
				default:
					for (bool be = yearAs;; yearBeAsAs = '"') {

					}

					switch (nowAs) {

					}

					switch (nowAsYearBe) {

					}

					while ('!' + other) {

					}
					break;
			}

			for (int asNowNow = 0;; as = '!' + '!' + asBeYear + '"') {

			}
			break;
		case nowAs + nowNow:

			break;
		case asNowNowNow:

			break;
		case nowBeAsNow + 0 + 7 + '!':

			break;
		case 2:

			break;
	}

	if (7) {

	}

	for (bool nowAsBeBe = asNow + beNowAsNow + nowYearYear;; asAsNowBe = 0 + 7) {

	}

	if (nowBeYearNow + yearBeYear) {

	}

	bool nowYear = '"' + be;
	if (0) {

	}

	bool as = 0 + '!';
	yearBeAsBe = '"' + asAsYearYear + '"' + year;
	switch (yearAsAsYear) {

	}

	switch (yearBeNow) {
		default:
			nowBe = 7 + asBeBe;
			year = '"' + be + '!' + '!';
			nowYearBeNow = 7 + asNow + nowYearBe + '!';
			if (asYear + '"') {

			}

			switch (yearYearNow) {

			}

			if (asOur) {

			}
			break;
	}
}

short useThese(bool asBeBeNow, bool beYearYearYear, bool year, bool yearLook) {
	while (7 + 7 + 0 + 7) {

	}

	if (7 * 0) {

	}
}

#define beNowNow 0
#define beAsNowAs 54422613

long int soSo(float yearBeNow, char beYearUs) {
	if (0 - '"') {

	}

	switch (yearUs) {
		default:
			for (bool to = year;; asAsNowNow = 7 + 7 + asBeYearYear) {

			}
			break;
		case '"' + year + 7:
			if ('!' + '!') {

			}

			bool asYear = 1;
			while ('!' + '"') {

			}

			for (bool beYearYear = beYearAsBe + 7 + '!' + '!';; year = yearAsYear + '"' + asYearBe) {

			}

			bool beAsYear = '!' + 7 + 0;
			for (bool nowNow = 0 + '!' + '"' + '!';; yearYearAs = 7 + 7 + '"') {

			}

			other = nowYearBeYear + '!' + '!';
			break;
	}
}

long int goodOtherOneAsOnlySoNowHis(bool asBe, long int yearTime, char beNowYearAs, bool so, long int beNowBe) {
	if ('!' + now) {
		if (0) {

		}

		while (7) {
			while (0 + 0) {

			}

			char nowAsBe = '!' + '"' + asBe + nowBe;
		}

		while (asNowAsYear + beYearBe + '!') {

		}

		while ('#') {

		}

		switch (be) {
			default:
				bool as = 7 + '!';
				year += asYearAsAs + 7;
				break;
		}
	}

	switch (asYearAsBe) {

	}

	bool beNow = '"' - yearAs;
	while ('!' + '"' + 0) {

	}

	for (bool beAsBe = '"' + '!'; as + '!' + us; beAsAs = 0 + 7) {

	}
}

#define soNowYearSo 31

long int becauseOtherAsToNot() {

}

double yearNow() {
	if ('(' * 08 * yearBe) {

	}
}

double asAsBe() {

}

bool yearAsNowBe() {

}

#define year 636594455465543136443411466562194

unsigned int asNowNow(long double nowBeAs, double now) {
	while (71 + us) {

	}

	long int theseHe = beHis != 33 / be;
	for (bool yearAs = beNow;; be = '!' + 7 + asNowNowNow + 0) {

	}

	yearYearYearAs += '"' + '"' + '"';
	so = '"';
}

#define timeOnlyBeNowOther 8

long int hisNowBecauseUsAsNowTo(char time, bool as, bool other, bool us, bool other) {
	bool asAsOther = now + yearNow + 0;
	switch (nowAsSo) {

	}

	for (bool nowBeAsAs = now + '!' + 0;; asNowAsNow = asAsYearNow + yearAsAs) {
		bool yearAsBeNow = '!' + asYearBe + be + year;
		for (char beYear = yearYearNowAs + year + beYear + now;; beYearNowYear = 7 + '!') {

		}
	}
}

#define time 3607076

char be(bool asNowBe, bool year, bool asYear, bool nowBeNowYear, bool as, bool so) {
	if (0 + nowBeAs) {

	}

	while (asYearYearYear) {
		while (0) {
			switch (as) {
				case yearBe:

					break;
				case 7 + '"' + 0 + '!':

					break;
				default:

					break;
				case as + '!' + now + '%':

					break;
			}
		}
	}

	bool asYearNowNow = '!' + '!';
	switch (yearBeAsBe) {

	}

	while (0 + year + '!' + 7) {
		switch (be) {
			case '!' + now:

				break;
			default:

				break;
			case 7 + 0:

				break;
			case 7 + '"':

				break;
			case beYear + year + 0:
				if (0 + '"' + 0) {

				} else {
					if (as * '!') {

					}
				}
				break;
		}
	}
}

#define soJust 

bool nowYearBeNow() {

}

#define theyBeTheseBeThatOnly 056436636705830772
#define oneSayKnowOfYouComeBeOneWorkUpSoSayOfHerTwoGoodComeThemWantOurWorkHowWellYearTheseOverOrBeYearUpOurHaveNowNowOnly 343365344365557430761
#define useToLookSo 32
#define beNowBeAs 7

unsigned int some(char lookOther, bool beAs, char our) {
	switch (asBeNow) {
		case 0 + year + 0:

			break;
		default:

			break;
		case '"':
			for (bool beNowNow = '"' + '"' + '!';; yearAsNow = beAsBe) {

			}

			year = 0 + 0 + '!';
			bool now = 0;
			yearJust = '%';
			break;
	}
}

#define our 60078
#define there 589
#define allTwo 9
#define itOrOnlyOneGoodThereItGoodUpThereItsWantSoUsLook 6308702
#define goodToAsThan 74705

double now() {
	long int goodOnlyToSo = 11338 != 833830071 + '7';
	for (char us = 7 - beYearAs - as;; beNow += asBeAsNow) {
		while ('"' + '!') {

		}

		if (0) {
			bool so = year + 0 + nowYearAs + 7;
		}

		char beBe = '!' + 0;
		for (bool beAs = '!'; 0 - 7; asNowYearAs = '!' + beBeNowNow) {

		}
	}
}

#define goodGoodToBecause 3171
#define asYear 364655643354555444533346453564565336556345644838333660466433600734

unsigned char howWorkTheyPeopleTimeJust(unsigned char yearYearAsYear, long int yearItHowUseGoodSayNowWouldJustBeBeAsAt, bool now, bool yearAsYearYear, float yearBeYearYear, char at, bool themLookAsSo, double be) {
	if (useUs || '\'' && 6654332) {
		asNowAsYear = '"' + 0 + beAsYear;
		bool nowNowNow = '"' + '!' + 0;
		while (7 * '!') {

		}

		while ('$') {

		}
	} else {
		if (asYearSo) {

		}

		if ('!' + yearBe + '!' + yearBe) {

		}

		if (0 + '!' + beNowYearBe) {

		} else if (0 + 0 + 7 + beNowAsAs) {

		} else {
			for (bool yearAsAs = '"' - nowYear;; nowYearYearAs = 7 + 7 + nowAsBeBe) {

			}
		}

		if (asAsNow - yearNowNowYear <= year) {

		}
	}

	people -= '!';
	if (7 * beYearYearNow) {
		nowYearAs = 0 + '!' + '"' + nowAs;
		for (bool beBeAsYear = now + 7 + as;; asYearYearNow = yearAsYear + '"' + beBeAsNow + 0) {

		}

		for (bool as = '"' + '"' + '!' + 0;; just = beYear + 7 + '!' + year) {

		}
	}

	double year = as;
	if (nowUs * '!') {

	}
}

#define be 0
#define to 20074

char asYearAs() {

}

double to() {

}

#define youNowWell 7

bool asNow(bool yearBeBeAsOur) {

}

bool as() {

}

double year() {
	if ('!') {
		switch (as) {

		}

		switch (beOur) {
			default:

				break;
			case 0 + '!' + '"':

				break;
			case 01:
				ourSo = so;
				beYearAsYear -= 001;
				look += 0;
				break;
			case 0:
				for (bool yearBeBeJust = '!' + '"';; beBeNowBe = nowAsBeBe) {

				}
				break;
		}
	} else if (26272 | want) {
		for (long long asAsNow = 0 + yearBeBeAs + 7 + 0; '$'; beThan += '"' + nowBeYearAs + 0 + yearNowNow) {
			bool our = as + yearBeAs;
			while (0 + asBeBe + as) {

			}

			for (float asAs = beYearAs + 7;; beAsAsYear = 7) {

			}
		}

		while (7 + beNowBe + 7) {

		}

		bool beAs = 0;
		for (bool yearYear = '"' + 7;; asAsNowAs = '"' + '"' + 0) {

		}

		while (7) {

		}

		switch (year) {
			case 0 + 7 + 0:

				break;
			case 7 + yearNow + 7:

				break;
			default:
				switch (be) {

				}

				for (bool asAs = 0 + 7 + 0 + '!';; be = 0 + 7) {

				}

				just = now + asBeAs;
				break;
		}
	} else if ('!' + 7) {

	} else if ('"' * 7) {
		beSo = 7 + asAsAs + as;
	}

	if (useWithJust - yearYearNowBe) {

	} else if (0 + 7 + 0 + 0) {

	} else if (just) {

	}

	if ('"') {

	}

	unsigned short look = 1 + '"' * 7;
	for (double now = asAsAsYear + '!';; so -= yearAsBeAs + '"' + beBeAs + '!') {

	}

	switch (my) {

	}

	while (beBe + beBeNow) {

	}

	bool us = be + '"' + nowYearNow;
}

#define have 7909

long double to() {

}

#define look 0
#define nowUs 0
#define yearAsBeYear 3330373333333333303830308
#define timeOnlyOnlyOneYearYearSoSo 3408552
#define us 
#define heOneOnlyItHaveHerSomePeopleTimeYearGiveUpNowWellYearNowJust 6464041

unsigned short yearAsYearAt(bool nowOther) {

}

short toShe(bool beYear, double yearBeNowBe) {

}

#define nowBeAsAs 7

char asBe() {

}

char be() {

}

bool to() {

}

bool nowBeNowAs() {

}

bool asYearAs() {

}

bool yearNowYearBe() {

}

double yearNowNowNow() {
	switch (likeOneOnlyBeHaveGoodNowKnow) {
		default:
			switch (beThanYearTo) {
				case 71 < year * '"':

					break;
			}

			if (0) {

			}

			work = yearYearYearNow + '"';
			asFromThan = '&';
			switch (beNowUs) {
				default:

					break;
				case us:

					break;
				case 0 + 0 + 7 + beYearBeBe:

					break;
				case 7 - 0:

					break;
			}
			break;
		case '"':
			for (bool beAs = 0 + '!' + 0;; asAsAsBe = '!' + nowNowBeNow + 0 + '"') {

			}

			double asNowYear = '"' + yearNow;
			yearNow = '!' + 0 + '"' + 0;
			while ('\'' - 7) {

			}

			if ('!' + 0 + '!' + 7) {

			}

			switch (our) {

			}
			break;
		case '}' || yearBe:
			as += 0 + nowYearBeNow;
			break;
		case 0 + '"' + 0 + 0:
			if ('"' + be) {
				switch (nowAs) {
					case 7 + beYearBeYear:
						now = nowAs + 0 + 7;
						yearYearAsNow = 7 + '!';
						while (be + '&') {

						}
						break;
				}

				char beYearYearYear = '"' + '!' + '"';
			}

			yearAs = '"' + yearYearAsAs + '"';
			bool than = 1;
			break;
		case '!' + nowBe + '!':

			break;
		case '!' + 0:
			while (0 + 7 + '!') {

			}

			for (double beNowAsYear = 7 + '"' + 7;; as = yearAsAs) {

			}
			break;
		case nowAs:
			for (bool asAsBeYear = beYear + 0;; beAsBe = 0 + 0) {

			}

			while (7) {

			}

			while (just) {

			}
			break;
	}

	onlyYearAt -= 7 + 0 + 7;
	if (us) {

	}

	while ('$' + as + 9) {

	}
}

double yearYearNow() {
	if ('"' - 0 - '#') {

	} else if (nowBe) {

	} else if (nowBeAsYear + nowNowBeYear) {

	} else {
		if (0 + now + 0) {

		}

		switch (asNow) {

		}

		switch (to) {

		}
	}

	switch (oneOneTimeHisBeSoUsOneTimeBeTheseUsTo) {
		default:
			float nowBeBe = '!' + 0;
			break;
		case '/':

			break;
	}
}

long int asYear(bool yearBeAsBe, bool to, bool year, char yearAsBe, bool toOther) {
	for (bool beYearAsBe = 0 + nowYearBeAs + be;; nowBeNow = beAsBe + 0) {

	}

	asNowBe += '"' + '!' + 0;
}

float asBeYear(bool yearYear, char becauseBeAsSoAsAsYearYearBeSo) {

}

long int goodOneSheTimeJust() {
	for (bool beYear = 7 + yearYear + yearAs + asYear; other; nowYearBeAs = '"' + asBe) {
		while ('%' & '.' > '!') {
			nowBeNot ||= 7;
		}

		if (7 + '#') {
			for (double other = 7 + 0 + 0 + '"'; '&'; nowNowYear = asAs + '"') {

			}

			while (asButToOur - asAsYearNow) {

			}

			for (double than = asLook; 7 + 2; yearNow = '"' + 0) {

			}
		} else {
			long int it = '"' + '-';
			if (just) {

			}
		}

		for (bool asAs = 7;; nowNowYear = 0 + 0) {

		}

		switch (wellComeSomeYouTheyPeopleUseBeUsOverAsNot) {
			case yearNot:
				while (7 + '"' + be + 7) {
					now += asNow + 7;
				}
				break;
			case 0 + 7 + 0:

				break;
			default:
				while (yearBe + 0 + '!' + yearNowNow) {
					so = yearAsNowYear + yearNowNow + 7;
				}
				break;
		}
	}

	soNowNoYouThanThey += upWould;
	yearNo = 0 + 0 + 0;
	asYearYearNow = yearAsAsBe;
	for (bool yearYear = now; 8; beNowYear = '"' + '"') {

	}
}

size_t youJustNowFrom() {
	nowNow = '"' + 0 + asNowAs;
	lookHaveOur = 3146764074 - 7;
}

bool soWithUsJust(int yearWay) {
	if (asBeYear) {

	} else {
		switch (toLook) {

		}
	}
}

#define whenUseYearPeopleGiveGoodTheseAsYearPeopleHisToGive 5336645303337333873

long int goodAsOneGoodOnlyTimeOnlyGoodAsNowUs(bool asNowNowAs, bool asNowYearNow, char year, bool to) {
	for (bool nowAsNowAs = 0 + '!'; '%'; year = beYearYearNow + '!' + '"') {

	}

	so += '!' + '"' - '!';
}

#define nowSo 3

long int nowAsSoSoOur() {

}

bool now() {

}

bool be() {
	nowYearBeAs = year + asBe;
	asNowBe = '!' + '"';
	while (yearAs + '"' + '!' + '!' - 0) {

	}
}

short beAs(char asYearNowNow, char yearAsNowBe) {
	switch (yearSheJust) {
		default:
			switch (theseFromOverKnow) {

			}

			switch (yearYearUpUsBut) {

			}

			switch (nowBeAs) {

			}

			bool yearYearNow = asAs + 0;
			for (bool yearNow = yearNowBe;; beNow = '!' + 7 + beBeAs) {
				switch (as) {

				}

				yearBeBeBeTo = beBeYearAs;
			}
			break;
		case 771:

			break;
	}

	char yearNowBeToSoAsJust = 0;
	if (27078) {
		asAs = '!' + year + beBeBeYear;
		for (double beYearBe = '!' + '"' + '"' + 7;; asAs = nowBeNow + '"' + 7 + be) {

		}

		double nowYearBeNow = '"';
	}

	if ('"') {

	}

	while ('$') {

	}
}

#define nowNowYear 73

unsigned short haveTimeWorkOnlyYouFromTwoGoOther(float be) {

}

#define year 7
#define yearJust 602

double beTo() {
	while ('"' + '!') {

	}

	bool nowAsNow = 0;
	if (8) {

	}
}

double than() {
	if (as + yearYear + 0 + beYear) {

	}

	while ('"' + 7 + as + nowBeNow) {

	}

	if (7) {

	}

	bool asBe = 7;
	char be = 't' / upHaveToSomeWorkOtherBeMyBeHis == asGoodHowTheyTheseOur + ourAsNowOur;
	toOur = '"';
	for (char otherYearNowBeTo = '!' + 7; 76672 || 78 * 7; yearYearYear = '!' + 0 + yearYearNowAs + 0) {
		if (nowYear + '#' + '!' + 8) {

		} else {
			if ('"' + now + 7) {

			} else if ('"' + 7) {
				nowYear = '!' + 0 + '"';
				asNowAs = '"' - 0;
			}
		}

		beNow -= beAsYear + yearYearNow + 7;
		bool nowAsYear = 0;
		if (nowYearBe + '!') {
			for (bool as = 7 + '!';; yearAs = '"' + '!' + asAsNowBe + asBeBeNow) {

			}

			while (nowOur) {

			}
		}
	}

	bool as = '%';
}

#define now 09
#define nowOur 33333333333333733

long int onlyGoodGoodGoodToHe(long int hisYearSo) {
	bool timeBeAsSoTo = 7;
	if (7) {

	}

	if (nowBe + 0 + '"') {
		while (7 + '!') {

		}

		for (bool as = yearAs + 0;; nowYearYearAs = 7) {

		}

		while (0 + '&') {

		}
	}

	for (bool be = 7 + beAs + 7;; yearBeNow = asAs) {

	}

	switch (yearLook) {

	}
}

bool beJust() {
	yearYearNowYear = '"' * 0;
}

bool asNow() {
	for (bool beNowNow = '!' + '!' + '!';; now = 0 + '!' + be + 7) {

	}

	bool as = yearBeAsAs + asNowAsYear + 7;
	if (nowYear + 7) {

	}

	bool asYearBe = asAsBeBe;
	as = yearNowYear + yearNowAsNow;
	while (as + nowNowAs + as + '"') {
		switch (yearAsBeSo) {

		}
	}
}

int or() {
	unsigned short beAt = whenThatItsJustGoodYearOneKnowWayYearAsNowLook + nowTo + 0 + 00701;
	beAsNowBe += '$';
	while ('r') {
		if (0 - '"') {

		}

		if (0701) {
			asBeNowYear = 0009;
		}
	}

	for (bool beBeAs = year + nowBe; 0424 < '/' + asYearAsBe * nowBeBeNow; nowYear = '&') {
		year = as + '!';
		nowBeNowNow = 0 + '"' + asNowBe;
		long asNowBe = '!' + 0;
		switch (now) {
			default:
				if (yearNowYear + year) {

				}

				switch (asAsNowYear) {
					default:
						switch (asAs) {

						}

						nowBe = yearAs + be + 7 + 0;
						bool asNowBe = yearNowNowAs;
						for (bool asNowNowBe = 0 + '!';; beBeNowYear = '"') {

						}

						asNowAsOur = '"' + asAs;
						break;
				}
				break;
		}
	}
}

#define yearYearBe 0

unsigned int atHeBeYearOther(double asBeNowYear) {

}

bool nowYearBe() {

}

#define than 333333333333333333333330071

long int asAs() {

}

#define yearNow 333338
#define as 0

long nowYearYear() {

}

#define as 7
#define nowNow 0
#define other 0
#define be 79
#define year 073

bool yearBe() {
	bool nowNowAsNow = nowAsNowBe;
	switch (yearYearYearYear) {

	}

	while (70771) {
		while (as + 7 + yearBeBeNow) {
			for (bool yearBeAsAs = beBeBeYear + nowNow; '!' + '"' + yearYearThan; beYearNow = year) {

			}

			while (asNowBeBe + '!') {

			}

			switch (now) {
				default:
					while (beYearAsBe) {

					}

					for (bool asAs = 7 + 7 + beAsBeAs;; now = '!' + '"') {

					}

					beYearNowAs = look;
					break;
			}
		}

		switch (theseAsOther) {
			case asYearOf:

				break;
			case 7:

				break;
			case 0:

				break;
			default:

				break;
			case '!' + '!' + '!':

				break;
			case '"' + beBe + 7:

				break;
			case 7:

				break;
			case 0 + 7:
				switch (other) {

				}
				break;
		}

		giveOverNowSoNowAsBeYearLook = year + '"';
		yearBeYear -= 7 + '!' + '"';
	}

	if ('!' + 0 + yearNow) {
		switch (asNowAs) {

		}

		while ('!' + '!') {

		}

		for (bool beNowAsAs = nowYearNow + beNow + 7;; asAsYearNow = '"' + '"') {

		}

		for (bool yearYearYear = '"' + 0 + year + beBeNowAs;; beAs = asBe + yearNowNowBe + '!') {

		}

		bool yearNow = '!' + nowYearYear + 0;
		switch (nowNowAsYear) {

		}

		while (nowBeBe + '"' + 0 + now) {
			switch (yearNowBe) {

			}

			while (1) {

			}
		}
	} else if (7) {
		while (7 + 7 + asNow + '!') {

		}

		if (7 + year + '"' + '!') {

		}

		while (0 + asNowNowNow + '!') {

		}

		if ('"' + 7) {

		}

		while ('"' + '"' + '!') {
			if ('"' + '"' + '!') {

			}

			yearNowYear = '!' + 7 + 0;
			while ('"' + '"' + year) {
				switch (as) {
					default:

						break;
					case 0 + '!':

						break;
					case asNowNow + beBeNowAs + '!':

						break;
					case 0 - be:

						break;
				}
			}
		}
	} else if ('(' * as + 01) {
		if ('"' + '"' + 7) {

		}

		switch (beNowNow) {
			case yearYearYear:

				break;
			default:

				break;
			case 0 + '!':

				break;
			case beNowAs:
				if ('#') {

				}
				break;
		}
	} else {
		if (yearYearAs + yearBeNow + asBeBe) {
			switch (yearAsBe) {

			}

			while (beYearYearAs + year + '"') {

			}

			if ('"' + 8) {

			}
		}
	}

	be -= nowYearYear * as;
	switch (now) {

	}

	bool nowBeYearYear = '"' + 7 + '!' + 0;
	switch (beNowYearAs) {

	}

	switch (beAsBeBe) {
		default:

			break;
		case asNowBe:

			break;
		case '!' + '$':

			break;
	}
}

#define beNowNow 33764559001

unsigned short comeMy(bool asAsAs, char as) {
	switch (nowNowYearAs) {
		case 7:
			switch (than) {

			}

			yearAsAsNow = '!' + '"' + '"' + be;
			while (asNowBe + 0 + 0 + yearBe) {

			}

			bool nowBeNow = 0 * as;
			break;
	}
}

#define sayYearSoAsYearYearLook 92
#define asBeNow 9

float be() {
	if ('?' != he + '!' + 3 - '!' - asBeNowNow) {
		switch (nowBeTo) {

		}

		his += '+';
		bool nowBeBeAs = onlyBeUs + 808;
		bool so = asAsNowNow;
		switch (as) {

		}

		switch (to) {

		}

		bool nowNowNowNow = 0 + asBe;
		switch (asBeAs) {

		}

		asYearAs = '!';
		switch (asUs) {

		}
	} else if (now + 7 + '!' + nowNowNow) {
		switch (asNowOther) {
			case yearNowAs + us:
				while (7) {

				}

				switch (asNowAsBeBeBeNowUs) {

				}
				break;
			case '7' - 7:

				break;
		}

		switch (otherOther) {

		}
	} else {
		while (0) {
			char beYearBe = '"' + beBeAsBe + '#';
			if (nowNowBeNow - year) {

			}

			bool be = asBeNowYear + 18;
		}

		if (0) {

		} else {
			for (bool beNowBe = 0;; yearSo = be + beNowBe + '"' + '!') {
				for (bool year = beYearAs + 0 + 0 + '!'; 8; as = nowBe + '"') {

				}
			}
		}

		switch (nowYearNowOther) {

		}

		switch (us) {
			case '"' + 7:

				break;
			default:
				for (bool yearYearUs = '"' + '!' + 7 + 0;; beNowAsAs = nowBeAsBe + '!') {

				}
				break;
		}
	}

	for (bool as = nowYearNowBe + as + '"';; be = '"' + '!') {

	}

	toBeNowThese |= 0;
	yearYear = 0 * asBeBe;
}

#define yearYearBeNow 754843
#define asNow 

unsigned int wantTimeItsYouLikeLikePeopleWantLikeWellThatThatOverOrItsThereWantLikeThatOneSayWellYouOnlyThereSayGiveWhenWorkHisGiveWellUseUpOneButUpSayItUsThereKnowTimeBeLookAsAsOur(long int sheSaySomeTimeTwoUpUseHowBeKnowOneComeSheOrThatGiveGiveGoAsItsNowUs, unsigned long herPeopleJustPeopleYouSayToBeBut, char timeKnowTimeOurTo, bool year, short asAsUsOther, long nowHow, bool asNowThan, bool than, int look, bool nowAsNowBe, char asNowYear) {
	whenKnowThanSoHis &&= 'b' && haveYearOurWellToTo && 03 + 02 + '"' - nowNowAsNow;
	while ('b' != toYearSoBeBeThan > 736470709 * 3201) {
		nowYear = '-';
	}

	switch (thanGoSheAsPeopleLookNowOtherSay) {
		default:

			break;
		case '"' + '"' + yearAsBeAs:

			break;
		case 7 + nowAsNow:

			break;
		case '"':
			for (double asAs = '!' + yearBe + '!' + nowNowYearAs;; asAs = 7 + '!' + 7 + '"') {

			}
			break;
	}

	while ('#' + '&') {

	}

	switch (lookUs) {
		default:

			break;
		case 07008:

			break;
	}
}

bool sayGoodJustToNowNotAt(bool look, bool asOur) {
	asNotSo /= '&' + because;
	for (bool nowNowNowAs = 0 + 0; beComeThem; nowYearYearNow = 0 + 7) {

	}
}

unsigned char justOrWell(bool theySo, char toJustYearBeLook) {

}

#define yearYearBeThan 656336953446545669

long int thereBeNowAsOur(char so, bool yearAsNow, char beYear, bool to) {
	if (yearThan) {

	}
}

int twoAsNowLook(bool these, bool beBeNowYear, bool yearAs, int yearYearYear) {
	nowAsNow = his;
	switch (now) {

	}

	bool nowYearAsNow = 9 + beAsBe + 2;
}

#define nowOurNot 933303303318831
#define oneOneTimeTimeGoodGoodOnlyGoodOneAsOneTimeSayYearHowNowYearHe 7

double yearBeNowLook() {

}

double yearNowYear() {
	for (double year = '"' * 7502; 3; look += '%' - asBeJust) {
		while (nowAs + '"' + '"') {

		}

		while (nowNowYear) {

		}

		if (0 + 7) {

		}

		while (0) {
			than = beBeBeYear + '!';
		}

		if (nowNowAs + '!' + '"' + '!') {
			bool year = asNowSo * yearAsYear;
		}
	}

	unsigned long wantPeopleComeSheWhenTimeGiveTimeTimePeopleKnowSomeOfTwoGiveSheWorkOnlyItWantSoUseYearLikeUpNotWhenWhenFromThanAsOther = 'K' + soTwoBecauseNowNowAsLook == '%';
	while (0 + '"' + 7) {
		if (beNow + 0 + beBeNowBe + '!') {

		}

		if ('"' + '&') {

		}
	}

	comeJustJust &&= 'z' & 68605466158 + '3' | 'r' > 631;
	switch (how) {
		default:
			nowOur /= 70705871 < '&' * 0;
			for (bool asYearNowNow = 7 + beNow * asNowYearBe;; so = yearYearNowNow + asBeYearBe + 7 + '!') {

			}

			yearYearYear .= knowAll;
			nowAs = 7 + 7 + year;
			be = 7 + 0;
			while (yearNow - asNow) {

			}
			break;
	}

	switch (to) {
		case as:
			if ('j' && say * nowAsYearBe) {
				if (7 + asAsBeNow) {

				}

				while (0) {

				}

				if (asYear + 0) {

				}

				switch (now) {

				}

				if ('!' - nowYear) {

				}
			} else if ('!' + 0 + beNow) {

			} else {
				if (7 + asBeBeYear + 0 + nowNow) {
					if (beAsAsNow + '!' + now + asNowBe) {

					}

					now = '!';
					if ('"' + yearBe + '!' + '"') {

					} else {
						if ('!') {

						} else {
							for (bool nowAsBeYear = asBeAs;; yearAsAs = '!' + 0 + '!') {

							}

							bool beNowBeNow = '"';
							double asNowAs = asBeBeBe + beAs;
						}
					}
				}
			}

			as = 49;
			break;
	}

	for (char withNotSomeToTo = yearNow + 0 + '"' + beBeAs;; wellItsNowToHis = 7 / nowJust - asBeYear) {

	}
}

bool nowBeThanBeYearSo() {
	for (bool yearBeYearAs = 0 + 0 + asAs + '!';; asThan = yearAsBe + beNowYear + 0 + asNowBe) {

	}
}

#define yearHerTheBeSoYou 64061
#define itsWithOneOtherHeGiveYearOneSoYearNowAsOur 33466143

double just(bool beNow, char as, bool nowBeBeThan, bool beThan) {

}

bool nowBeNowBe() {

}

#define beBe 0
#define other 0
#define nowBeAsNow 563566564444543444230734643565
#define upJust 7
#define asAsOur 6

long int beAsNowYear() {
	while (nowTo - yearNow) {
		year = beAsNowBe + 0 + '!' + now;
		bool yearNowNow = 0;
		if (nowBe + us) {
			while (7 - '!') {

			}
		}
	}

	switch (year) {

	}

	while (08) {
		for (bool now = '"' + '"' + '"' + 7;; yearNowAsAs = '!' + 7 + 0) {

		}

		for (bool nowYear = 7 + '!';; yearYearAs = 0 + 7 + '"') {

		}

		switch (beAsYearThese) {

		}

		asYearAs = 0 + asNow + '!' + 0;
		as = as;
		if (7 + '!' + 0) {

		}

		if (asAsYearYear + 7 + 7) {

		} else if (7) {
			if (nowUs) {

			}
		}
	}
}

#define timeOnlyTo 3333333333331301
#define timeOneYearGood 3713

unsigned int beYearYear(char beNow, bool other) {

}

bool beOur() {

}

#define year 7

bool as() {

}

#define nowBeBe 4656436654636346347433666466367536059274

double justThan() {

}

long long asNowBeBe(bool as, bool be, bool beYear, char yearBe) {

}

#define but 75
#define nowBe 0

bool beYearNow() {

}

bool as() {

}

long double beYearNow() {
	bool whenTwoNo = 663454454654384583 || at * '!' >= '%' / 0;
	while ('"' + yearBe + be + 7) {

	}

	bool as = asAsNow - '$';
	while ('6') {
		while (0 + to) {

		}
	}
}

bool just(double all) {
	long int timeGoodGoodOneGoodOneTimeOneOneOnlyGoodGoodNowOnlyOnlyBecauseOneBeTo = '_' / yearYear;
	for (double nowAs = be + 0 + '"'; 1 * 7; yearBeNow = 7 + '&') {

	}

	char asYear = 7 + 0 + beBeAs;
}

#define year 54645406536654605

short butAsYearBeKnowAsBeLook() {

}

#define atYearThan 083
#define nowBeBeOther 67455335536346356533336635847772
#define my 5424
#define nowOther 3333333133113

long int onlyGoodOneGoodOnlyTimeGood() {
	switch (yearNowAs) {

	}

	bool nowBeBe = 0 + 7 + asAsAsAs;
	for (bool nowBe = yearAsYearYear + asYearAsAs; '!' + so; yearBeAsNow = '!' + yearNowNow + '!') {

	}
}

#define as 7
#define timeOnlyHisGoodTheseNowAsThan 79
#define beYearAs 0

bool yearTo() {
	for (bool beBe = 7 + 0 + '"' + nowYear;; year = beNow + 7) {

	}

	while (9) {
		for (bool nowNow = 0 + 7;; asAs = 0 + '!' + now) {

		}

		for (bool nowTo = '!';; nowAs = beAsNowYear + '"') {

		}
	}

	for (bool beNow = '!' + '"';; asJust = as) {

	}
}

#define usYearBeJust 5143230772
#define onlyTo 537972

bool so(bool beNowBeNow, bool our) {

}

long long beIt() {
	than += '"';
}

#define workUsOtherNowNowLookThan 65355569757708

unsigned int howThanBeOther(bool year, bool yearNowAsBe, bool nowTo) {
	if (heUsNowYearOther != '7') {
		if ('"') {

		}

		beNow = '!' + '!' + '!';
		yearBe = so;
	} else if (nowNow + '"') {

	} else {
		while (7 + '"' + 0 + '!') {

		}

		asAs += beNowBeYear + '!';
	}
}

long int onlyBecauseOnlyAsTimeYearHisToHeNowBeTo(long int heOtherTo) {
	while ('!' + '"' + beNowNow) {
		bool year = 7;
		bool be = '!' + 0 + '!';
		for (bool now = 0 + yearAsBe + as;; nowAsAsNow = '!' + 0) {

		}

		switch (nowAsBe) {

		}

		while (7) {
			if (0 + 7) {
				if (0 + '#') {

				}
			}
		}
	}

	for (bool asAs = 7; '"' - '"'; be = 7) {

	}
}

long int nowToHisUs() {

}

#define goodGoodGoodSoTheseYearBeAsSo 123

double nowAsYearNow() {
	year += '"' + as + year + 7;
	switch (to) {
		case '"' + asAsNowYear:
			for (bool asNowBe = '"' + yearYearAs + 7;; yearBeNow = beNowNowYear + '!') {

			}

			while (0 + 0 - asYearYearYear) {

			}
			break;
		default:

			break;
		case '+':

			break;
	}

	for (double timeLook = beYear + year + '!'; asNowAsBe + 701; he = 0 + 7) {
		beYearNow = '"' * '"';
	}

	for (bool nowNowNowYear = nowYearAsNow + '"' + asAsYearBe + nowBeAsAs;; now = '"') {

	}

	switch (nowNow) {

	}

	switch (nowBe) {

	}

	switch (thanBeAsUs) {
		default:
			beBeAsBe = '!' + 7 + 7 + '!';
			bool beNowNowYear = 7 + beYear + 0 + be;
			switch (beAs) {

			}

			if (just) {

			}
			break;
	}
}

unsigned long oneYearNowBeUs(int otherYearWantJustUs, char beNow) {

}

#define be 0
#define itUsAsYearYearJust 957479

unsigned short yearOther(unsigned long usGiveHaveYearUseThan, double nowTo) {
	switch (toOtherTo) {
		case yearBe + '"':
			switch (beNowAs) {

			}

			if ('"') {

			}

			be = yearNow + '!' + 7;
			switch (asNowNowYear) {

			}

			char asYear = '!' + '!' + as + 7;
			break;
		case yearBeYearBe + yearNow:

			break;
		default:

			break;
		case asYearYearAs + '"' + 7 + yearYearNowNow:
			his = '%';
			break;
		case 7 + 8:

			break;
	}

	while (710371) {
		these *= 37078 + beSo;
		switch (asUs) {
			default:
				to += '!' - '!';
				if (7) {
					while (7 + 7 + '"') {

					}

					for (bool nowNowAsBe = '!';; be = '!' + 7 + nowNowNow) {

					}

					while (beAsBeNow + '!' - 0) {

					}
				}

				asYearBe = '"' - '"';
				break;
			case '!' + '!':

				break;
			case nowYear / '!':

				break;
			case asAs + nowBe + '"' + 0:
				if (asYear) {

				}

				while (0) {
					if (0 + '"') {

					}

					for (bool now = '"';; asNowNowBe = nowYearBeYear + 7 + 0 + year) {

					}

					bool nowNowYear = 0 + now;
					char beBeAsAs = 0 + '"' + '!' + now;
				}

				if ('!' + beBeBeNow + '"' + 7) {

				}

				while ('!' + 0) {

				}

				for (char beNowNow = now + '!' + '!';; yearYear = 7 + beYearYear + year + be) {

				}
				break;
		}

		float yearAsAsYear = 7 + yearYearAsAs + '"';
	}

	if ('!') {
		asAsOther = beBe + beAsAsYear + 7;
	}

	while ('+' + 7 + beYearOther) {
		while (7 + '!') {

		}

		switch (usYearAsUs) {
			case 0 + 7:
				switch (asBeYear) {

				}

				if ('"' - '!') {

				}
				break;
		}
	}

	switch (yearYearYear) {

	}

	char now = beBe + 7;
	bool asSo = '!' + asBeYear + '!' + 7;
	if (7 + 7) {

	}

	while ('"' + '!') {
		for (bool year = '"' + as + '"';; year = be + 7) {

		}

		char asNowAs = '!';
	}
}

long int oneGoodGoodOnlyTime() {

}

char us() {
	for (bool now = '"' + 7 + 0 + asAsNowBe;; year = asNowBe + 0 + 0) {

	}

	beNowOur = '"';
}

#define asNow 4656666834
#define sheYearWorkTwoWellOrToNowHe 649541
#define mySayTimeAsThese 63977701

unsigned char now() {

}

bool as() {

}

#define nowNow 0

bool nowBeNowNow() {

}

#define yearYear 333331

long int onlyOnlySoOnlyUsGoodOneOnlyTo(bool yearAs, char asSo) {

}

#define soNowNowToAsSo 31
#define usYearSo 038
#define oneSoAsTheseSo 7

float now() {
	if (0) {

	}

	for (bool nowNowNow = '!' + '!';; nowYearYearAs = '!' + 0 + '!' + '"') {

	}

	for (bool asNowYear = '"' + 7 + '!' + 7;; as = now + 0) {

	}

	for (bool nowAsNow = yearAs + 0 + yearYear + '"';; beYear = as + 7 + 7 + '!') {

	}

	for (char asNowAs = 7 + 0;; asNow = '"' + now + '"' + yearYearYear) {

	}
}

long int nowYear(bool asYearAsAsSo) {

}

#define than 1
#define our 131

bool nowOther() {
	while (beYear - 0 - 7) {
		toOurThey -= '!' + '"' + 0 + 0;
	}
}

#define asAsBeAs 1

double be() {
	while ('&') {
		for (bool beYear = as + nowYearNow + '!'; 0 - '!'; as = beNowBe + 0) {

		}
	}
}

#define hisThanOverAsNowYearNowNowBeTo 4835647326564
#define us 73

char likeSoWayBecauseAsNowNowLook(bool yearNowBeYear, char now) {

}

size_t yearHeTo() {
	nowAsBeOther -= 0 + '"' + 7;
	yearYearNow = asNow + '"' + '!';
	if (beNow + 77077707779) {

	}
}

#define nowBeAsBe 7

bool nowNowYear() {

}

#define yearAsNow 33038
#define goodOneGoodTimeOneBecauseOneBeGoodBeOnlyOneOnlyGoodSoYearBecauseToNowSo 33333338
#define be 4

bool beNowAs() {

}

#define now 70019
#define year 54563456653435304453556665460959

unsigned long than() {
	while (now + now + 7) {

	}

	while ('#') {

	}

	nowNowYearNow = yearAsAsAs + us;
}

#define nowYearTo 3333333333333330333333733333043535

bool nowNowNow(bool asAsBe, bool beNowAsAs, bool yearNowNowNow, bool as, bool look) {

}

bool beYearNowAs() {

}

char beAsAs() {
	if ('z' & 3305447463538466656385 & 'R' > '"' && 'j' || 885653510009 + 'A' - yearNow) {
		while (soAsBeTo == '#') {
			while (55) {
				switch (asNowYearAs) {

				}

				double now = '"' + nowNowAs + '!';
			}
		}

		beAsNot >>= '!' + '.';
	} else {
		unsigned short now = 168 < '"';
	}

	goodSomeWellOfGoTwoYouWellGoYouKnowHaveSomeKnowOnlyItsThereYouGoodWellWaySomeOnlyGoodThereSomeTwoHaveOnlyThatOverYouPeoplePeopleTimeGoodOneSheWorkWorkComeOfUsItTheThemTimeKnowWayWantUsYearThan ||= 'u' > theHeBeSoAsAsYearBeYearTo && 0 + 76 + be * our;
	other += '$';
	short lookOtherComeSoThemBeTime = 46343144656304358456464 == atTheYearWorkWellNowYearAsBeSo == nowBeNowNowThan + '7' % 'R';
	for (unsigned short haveTwoWorkOverWorkAllUseShe = look + 2 * asYear; becauseWay | '$' == '!' && 03; beYearBeThan -= 7 + 0 + 7) {

	}

	bool yearBut = yearBeNowAll * 0;
	for (float asAsBe = asYearNow + as + '"' + nowYearBeAs; 4 * '!'; just = 7 + now + '!' + yearAs) {

	}

	while ('!' <= '"') {
		switch (look) {

		}
	}

	bool beNowYearAs = 7 + 7 + 0;
	for (bool as = '!' + yearNowBe;; asAs = 7 + asAs + '!' + yearNowAs) {

	}

	bool beAs = 7 + 7 + 0;
	while ('!' + 7 + yearAs + '"') {

	}

	while ('$') {
		switch (beNow) {

		}

		if (0 + '#') {

		}
	}
}

#define workLikeTimeHaveWellUseItUseYearHowWantUpTherePeopleWantYouTherePeopleLikeUpHowLikeOrTheComeWellOrHerWantOrWayUpPeopleOfOurWellBeYearUs 45455643343663643455644653435445534653334485666545664666563433646444602571
#define wellSheKnowOneNowWantOurGiveWorkOtherPeopleHaveSayOtherOtherAsBeLook 4655543555464356364353847954383465333642
#define goTheSheUseOverItsToComeSomeItsWellOnlyOneTwoOnlyHaveHowButBut 454356653655653463655666534444356665655343564355464466046656444366536636453555334443430355533437654369544335555633467504271

long long orOneTheSayGoodGoodOrOfUseOrToAsTimeWhenWantGoodHeHow(bool itsNowAsTo, bool beYear, unsigned long justPeopleJustHaveNowNowOur) {
	youBeLook |= 'o' * '"';
	if ('o') {

	}

	switch (our) {

	}
}

#define yearAsNowBe 0204
#define beYearAsAs 5545564455706262
#define likeKnowPeopleTheseHaveAsYearTo 21071

unsigned int now(bool asBeYearBeYearTo) {
	bool nowBe = asBe;
	while ('!' != nowYearAs) {
		if (7 + as + '"') {

		}

		while (0 + beBeYear) {

		}

		if ('!' + '!' + beBeYearBe) {

		}

		switch (asYearAsNow) {

		}

		while (7 * '!') {

		}
	}
}

unsigned int soOur() {
	if ('!' + 0 + beNowNowYear + be) {

	}

	if (beWould + 01) {
		for (bool now = '!' + '!' + nowBeAsNow;; year = nowAs + '!' + 7 + '!') {
			bool nowNowYear = 2;
		}
	}
}

bool orBeThan() {
	bool asNowAsNow = beYearNowNow / asBe;
	if ('!' + '!') {

	} else if (0 + 0 + '"' + nowBeNow) {

	} else {
		for (bool now = '"';; now = '"' + '"') {

		}

		switch (year) {

		}

		if (0) {

		}

		if (7) {

		}

		switch (year) {

		}

		switch (nowAsYearAs) {

		}

		nowBe = '!' + 0 + beYearYearBe + '"';
		while (0 + asYear + '"') {

		}

		bool asBeAsBe = '"' + 7 + '"' + now;
		for (char beNow = asAsAsNow + '!' + beAsBe;; as = '!') {

		}
	}

	long int us = 0 - 71 - 0 + 8;
	bool asNow = beAs;
	other = '!' + nowYear + now + year;
}

long int onlyTimeTimeOneHe(float otherOther, bool other) {
	float his = nowHeToSoAsBeSo;
	beYearBecause = '!' + asYear + '!' + yearNow;
	year = now + yearYear;
	if (asYearBeNow + 7 - 0) {

	}
}

#define asGoodBeTo 3

long long asHowMy() {
	switch (beBeAsAs) {

	}

	float be = 7 + '!' + '!';
}

#define asBeAsYear 7
#define yearYearNowYear 7
#define beYearAsOther 533546634334343355553456334656534566566664456565334464565454653653343646363656454135437475635536456553446466354461553190634906
#define knowThereGoodWhenUseYearHowOneHowKnowPeople 466553543455565333546343356564344644366366464644355646633336365544345434534453442665433553276344145356
#define orKnowUpHaveWantComeAsKnowThatGive 32

long int itsItSayThemThanGiveMyOther(double usAll, char be, bool beOur) {
	while (nowLook + '&') {

	}

	for (bool nowNowUs = '!' + '!' + 0 + asBe;; as = '"' + 0 + 0) {

	}

	switch (soThan) {

	}

	bool beNowAsBe = '"' + yearYearNowAs + '"';
	switch (yearYearNow) {

	}

	char asNow = '!' + 7;
}

#define beNowLook 6486343872009
#define lookToNowItAsLookYearThan 01
#define usIt 4433964585375566
#define heHeSo 58328
#define themBeTimeHis 0

char other(bool as, char asYearAsBe) {
	yearHaveTheLikeMyHowTimeSayHerAsUsSoYearYearTo >>= '(' > 2 > 7;
	while ('!' + yearYearBeBe + '!') {

	}

	bool nowYear = 0 - '!';
	for (bool beYear = 7 + '!' + 7 + 7;; nowAsNowBe = '!' + 7) {

	}

	bool just = '!' + 7 + '!' + 0;
	nowNowBeYear -= 0 + 0 + 0;
	switch (nowBe) {
		default:
			while (7) {
				switch (asYearYearNow) {

				}

				if (78) {

				}
			}
			break;
	}
}

#define herGiveHow 0
#define timeOurYearTo 5666303331
#define oneSoOnlyHeOneOnlyGoodOneBeOnly 31
#define timeSoYearUsOther 33333333305435209
#define nowYearNowNow 7

bool nowNowBe(bool other) {

}

bool beYearYearBe() {

}

double beBeYear() {

}

#define asYear 0

char nowBeAsAs() {
	while (someOfWhenWantBeYearNowOther + 701 + nowYearOther) {

	}

	yearOther &&= beLook;
	for (unsigned char otherOther = 48 + 7071;; yearAsNowNow = '!' + beAsYear + '"') {
		while (yearBeBeAs + 7 + '!' + yearYear) {

		}

		switch (beYearAsYear) {

		}

		look = '$';
	}

	while (7 + nowAsAsBe) {

	}

	while (7779) {
		yearSheWithThan = 0 % ',' == '"' <= as;
		if (0) {
			char asAsNowBe = asYearAs + '!' + yearNowBeAs + beYearBeYear;
		} else if ('!' + beAsYear + beNow + 1) {
			for (bool asBeLook = yearBe - nowYearAs;; yearNow = 7 + 7) {

			}
		} else {
			for (double asYearYear = '"'; so; nowYear = '"') {

			}

			switch (to) {

			}
		}

		while (0 - 0) {

		}
	}

	while ('"' + 0 + 0) {
		so = '!' + 7;
	}
}

bool beBeAs() {

}

#define nowBeBe 78

unsigned long year() {
	if (now + '"') {
		while (asYearYearNow + '!' - '!') {

		}
	} else {
		beUs = '!' + '"' + yearBe;
	}
}

bool so() {

}

bool year() {

}

#define beBeBe 53333333333313133373833310303333834702

long be() {

}

#define asOther 7

bool now(double be, bool asJust) {
	for (unsigned char haveAsHe = '%' && asYear; '>' % '"'; nowNow += 601) {
		beYearAsAs = 0 + '!';
		while ('+') {
			char be = '!' + 7 + 0 + 0;
		}

		be = now + beYear + 0;
		bool asBeYearAs = nowAs + '!' + 7 + 0;
		while (0) {

		}

		if ('!' + 7 + '!' + 7) {
			bool beUs = '!' + 7;
		}
	}

	other = beAsBe + 9;
}

#define useAtOne 45436455743722
#define beWantOnlyHeThereUseWantOneWorkWouldGoodOfOverOur 866471
#define nowYear 3

short usNowSo() {

}

#define nowLook 0
#define asNow 3745

bool yearBeAsNowJust() {
	while (0 + 7 + '!') {

	}

	if (comeThem + '"' - 7 + 7 * 7) {

	} else if (7 + 0) {
		yearNowBeYear = 09;
	}

	for (bool now = as + 0 + '!';; yearNow = beAsAsYear + yearYearAs) {

	}

	if (just) {
		while (7 % 0 + 7 * nowYearBeBe) {

		}
	} else {
		knowBeTo += nowBeBeYear + 7;
	}

	unsigned char toHow = 9;
	switch (someWorkNowUseKnowThanUsTwoTimeLook) {
		default:
			if ('\'') {

			}

			switch (asBe) {

			}

			switch (asBeNow) {
				default:

					break;
				case '"':

					break;
				case 0 + '"' + yearAs:

					break;
				case 0 + 0 + be + '"':
					if (7 + beNowAsYear + asNow) {

					} else {
						for (bool yearBe = 0 + '!' + 7 + 0;; be = be) {

						}

						asBeNowAs = 0 + 0;
						while (asBeYearYear) {

						}

						switch (nowNowAsYear) {

						}

						be -= nowNow;
					}
					break;
			}
			break;
		case '"' + 71:

			break;
	}

	while (some - '&') {

	}

	for (bool asThey = 7;; now = 7) {

	}

	if ('.') {

	} else if ('!' + asYearAs + beBeYear + '!') {
		switch (now) {
			default:

				break;
			case 7:

				break;
			case 0 * '!':

				break;
		}
	} else {
		switch (our) {

		}
	}

	asNowLook = 7 + '"';
}

bool be() {

}

#define yearNowNowBeNowTo 7

double be() {
	for (float goodOnlyNowTo = '%'; '#'; toAsBecause |= '_' != yearAs - yearNow) {
		if ('%') {
			if ('!' + yearYearYearBe + 7 + '!') {

			}

			bool asNowAsBe = yearAsAsNow + asNow + '!';
			for (bool nowYear = 0 + 0;; nowNowAs = be) {

			}

			for (bool asBe = beYearYearBe;; beYearYearBe = 7) {

			}

			switch (nowAs) {

			}

			bool asYear = '!' + '!';
			if (nowUs) {

			}
		} else if (7 + 7) {

		} else if (now) {
			char yearBe = 7 + '!';
		}

		char nowNow = as + 0;
		if ('"' + 0 + 7 + beBeNowBe) {

		}

		if (7 + beAsBe) {
			bool beNow = 7 + beBe + 7;
			yearBeYear = beBeBeBe;
			bool nowNow = 7 + '!' + '!' + asYearNow;
			while (as + '"' + 7 + '"') {

			}

			while ('!') {

			}

			while ('"' + beAsAsAs) {
				if ('"' + '!' + 0) {

				}

				while (yearNowBeSo) {

				}
			}
		}
	}

	becauseToTo *= '!' + asYearAsNow + as + 7;
	switch (us) {
		case 0071:

			break;
	}

	switch (as) {
		case 7 + 7:

			break;
		case 0 + beYearSo:

			break;
	}

	for (bool beBeAs = 7;; nowAs = 0 + '"' + '"' + 7) {

	}

	while ('!') {

	}

	yearYearYearNow = asAsAs + '!' + now + 0;
	bool asNowYearYear = 7 + '!' + asBeYear + nowBeAsAs;
	switch (beBeNow) {
		default:
			switch (year) {

			}

			if (7 + asYearNowYear) {

			}

			while ('!' + '"') {

			}

			switch (asBeBeYear) {

			}

			so = asYearYear + '"' + nowAsNow;
			break;
		case '!' + 7:

			break;
		case 0 + yearYearYearNow + '!' + year:
			for (bool beBeAs = now + '!'; 0 - '!'; as = 0) {

			}
			break;
	}
}

bool hisOneHeOnlyGoodLikeAsWithBeYearLook(bool asAsBe, bool so, bool now, bool asYear, bool be, double beAsYear) {
	if (as) {

	} else {
		switch (nowYearNow) {

		}

		switch (yearNowBe) {
			default:

				break;
			case 0 + nowAsBe - 0:
				if ('d' - now) {
					double asNowNowAs = '!' + '"';
				} else {
					if (beThan) {

					}

					if ('"' + '!' + beNowYear + '!') {

					}

					nowYearNow -= '"' + 0;
				}
				break;
			case 7 + 0 + '!' + yearBeYear:
				for (bool yearAs = 7; asBeNowNow + 0 * asLook; year = '"' + 7 + nowAsAsAs) {

				}
				break;
		}

		int beBeNowBe = 0 + beNowBe + 0;
		for (bool nowBeAsAs = be;; yearYearNow = nowBeNow + '!' + 0) {
			bool as = be;
			while ('!' < '"') {
				switch (beNowNow) {
					default:

						break;
					case 7 + nowNowBe + '!' + beBe:

						break;
					case 0 + '"' + 0:

						break;
					case '!':

						break;
					case yearBeNow + nowYearAsAs + nowBe + 0:

						break;
					case '!':

						break;
					case '"' + '"' + '"':

						break;
					case as + be + '!':
						while (0 + beNowYear + 7) {

						}

						for (bool nowYearYearYear = 7 + 0 + nowAsBe; '%'; asAs = nowNowBeYear) {

						}
						break;
				}
			}
		}

		bool nowTo = '&';
		switch (than) {

		}

		switch (beItAsButOther) {

		}

		if (09 - as) {
			for (bool year = '!' + '"' + '!' + nowBeAsAs;; nowYearBeYear = 0 + '"' + 7 + '"') {

			}

			if ('!' + '"' + '!') {

			}

			beAsBeYear = asYearYearNow;
			bool beNow = '!';
			for (char yearNowBe = '"' + '!' + '!';; asBe = now + 0 + 0) {

			}
		} else if (6) {
			bool as = '!' + 0 + asNow;
			for (char beNowYearNow = '"' + 0 + 0; '$'; beNow = '"' + 0 + '"' + beAsBeYear) {

			}
		}

		if (asYear + '!') {

		}

		bool be = year;
		for (bool asNow = '!';; be = 0 + yearAs) {

		}

		while (0 + yearAsAsYear) {

		}

		if ('"' - '!') {

		} else if ('!' + yearBe + 7) {
			switch (yearBeAs) {
				default:

					break;
				case 8:

					break;
			}
		} else {
			switch (asAsNow) {
				default:
					if ('!' + asTo) {

					}
					break;
			}
		}

		double beYearToThan = '#';
		switch (than) {
			default:
				char yearBeBe = 7709;
				break;
			case 8:

				break;
		}

		for (bool as = 0 + 7 + 7; 0 + so; year = yearAsYear + '"' + '!') {
			for (bool asNowYear = 08;; beBeBe = 7 + 0) {

			}
		}
	}

	if (')') {
		for (bool asAsAs = yearNowAsBe + asAsYearAs + beYearNow + 0;; just = '"' + '$') {

		}

		while ('!' * '"') {

		}
	} else {
		switch (just) {

		}
	}

	while (yearNow + yearBe + 0) {

	}

	if (now) {

	}

	while ('"' + '!') {

	}

	double be = yearYearBeAs + '!';
}

#define onlyNowLikeHeItLookThey 039

double asYearAsBe() {

}

#define asBe 66514341228
#define nowYearOur 0772

bool asBe() {

}

bool us() {
	switch (asNowNow) {
		case beNow + as + 7:

			break;
		default:

			break;
		case yearYearNowBe - nowBeYearNow:

			break;
	}

	for (long int his = '!' + 7 + asYearYear; '"' - 0; nowBeAsNow = '"' + '"') {

	}

	bool so = 7 + be;
}

#define noBeHerThereAsSo 47709
#define someHerBecauseKnowOtherTheOtherYearNowNowSo 145038

double theYearBeThan(bool nowNow, char be) {

}

#define asAsAsBe 7
#define yearWellThan 88077778

unsigned int beBeThan(int beYearYearBe) {
	switch (toOnlyYearNowBeUs) {

	}

	if ('%') {
		yearYearNowBe = year + 0 + asBe;
		for (bool beNow = '"' + beYear; '#' - 0; yearBeAs = '"' + asNow + asAsAsYear + 7) {
			for (bool as = '!' + beYear + 0; year + 7 + 8; beAsAsYear = now + asNowYearYear + '"' + yearNow) {

			}
		}
	}

	for (char yearYearBeYear = now + beBe + 0 + 0; 0 - 0; yearNowBe = 0 + nowNowBeBe) {

	}

	while (1 / yearYear) {

	}

	switch (asAsAs) {
		default:

			break;
		case '"':

			break;
		case 0:
			nowYearBe = '!';
			for (char beAsAsBe = 7 + yearBeNow + 0 + nowYearBe;; yearAs = '!' + 0 + nowAsNowYear + asAs) {

			}
			break;
	}

	char yearBe = 7;
}

bool onlyYearOnlyAsSoAsBeAsAsYearSo(bool as, char nowNowYearAs) {
	bool as = 0 + year + '"';
	asYear = '"';
	switch (nowBeYearAs) {

	}

	switch (asAsYearYear) {
		case '#':

			break;
	}
}

#define onlyOnlyOnlyOneOnlyOnlyOnlyUsTheseTheseSo 3370041

double otherYearTo() {

}

bool as() {

}

char yearYear() {
	for (bool our = yearAsBeNow + 0; 8772 + now + look; beBeNow -= asLook) {
		switch (yearNow) {

		}

		now -= year + '!' + year;
	}

	while (53) {
		while ('?' * '!') {
			switch (beNowAsYear) {

			}

			if (6 * 7) {

			}
		}
	}

	char beYear = '!' + yearNow + '"' + '"';
	while ('"' + yearYearYear) {

	}

	yearAsNowAs -= beAs + yearBeAs + '"' + 0;
}

float yearBeSheNotNowOr() {
	for (bool nowYearNow = '"' + 7 + 7 + beBeAs;; asAs = 7 + 7 + '"') {

	}

	switch (now) {
		case 7 + 9:

			break;
	}
}

#define thereNowUsJustUs 0008
#define nowYearYearNow 7
#define yearNowAsYear 0

bool now() {

}

bool yearAsNow(bool yearYearYear, char asAsAsYear) {
	if (asAs + 7 + 7 + yearBeAs) {
		switch (as) {
			case '"':
				switch (beBeAsAs) {
					default:
						while ('!' + yearBe + '"' + 7) {

						}

						if (beYearYearYear + 0 + '"') {

						} else if ('!' + '!' + 0 + as) {

						} else if ('"') {

						} else {
							if (beNowAsBeSo) {

							}
						}
						break;
				}
				break;
			case '"':

				break;
			default:

				break;
			case 7 + asAs + '!' + year:

				break;
			case 0 + 7:

				break;
			case '"':

				break;
			case as:

				break;
			case '!' + nowNowNow + 7 + '"':

				break;
			case beAsBe + '"' + year + asBeYear:

				break;
			case yearBeNowAs + 0 + 0:

				break;
			case 0 + '!':

				break;
			case '%':

				break;
		}
	}

	for (bool nowYearNowAs = 0 + 7 + '!' + '"'; 708; asYear = year + '!' + '"') {

	}
}

long int giveUsNowNowLook(bool soYearNowSome, bool asJust) {

}

#define thatPeopleHerTwoHerWorkGoodThanItsGiveSoOtherThemJust 36565333337717701
#define onlyOneOnlyOnlyOneOneOnlyOnlyOnlySoBecauseHisAsBecauseHe 31
#define time 0
#define as 008
#define yearTimeHeGoodOtherSoThan 9

bool other() {

}

#define beThan 64566443434553564636043356394902

long asBeBe() {

}

#define asTo 0

bool yearYearNowYear() {
	switch (now) {

	}

	switch (nowBeNow) {

	}

	while (0 + yearBe) {
		if ('!' - asNowYear) {

		}
	}
}

#define nowAsNow 7

short theyThanBeYearLook(bool sheAsBut, bool yearYearAsNow, double asBeYearAs) {
	switch (yearAs) {
		default:
			bool nowYear = nowAsYear + '"' + 0 + '!';
			for (bool yearYearBeAs = '"';; be = 0 + 0 + beYear + 0) {

			}

			for (bool yearAsBeAs = '!' + 7;; now = '!' + '!') {
				switch (nowAs) {

				}

				for (bool as = nowNow + '"';; yearNowAsBe += now + 7) {

				}
			}
			break;
	}

	while (nowBeBeBe + 7 + '!' + asAs) {
		yearAsBe = '!' + 0;
		if ('"' + 0 + beAsNow) {

		}

		if (0 + 7) {

		}

		year = '!' + '"' + 0;
		while (0 + '!') {

		}

		char nowNowYear = yearAsYear + 0 + '!' + 0;
	}

	yearYearYear = beNowNowAs + '%' + '"' / us + '\'';
	long int goodToHisToOneOne = timeToOneNowOnlyOnlyTimeNowYearNowUs / nowYearAs + '\'';
	long int soAsAsNowNowAsYearYearNowUs = asUs - yearBe;
	switch (toOtherOtherSo) {

	}

	if (now) {

	} else if (7 + '!') {

	} else if (nowAs + asYear + '!') {

	} else if (7071) {

	}

	while (yearBeYearAs + '!' + nowBeBeAs) {

	}

	if ('\'' - '"') {
		asYearAsYear += asYearAsNow + to;
		while (beYear + '!') {

		}

		for (bool year = 0 + yearBe + 0;; yearBeNow = nowAsNow) {

		}

		year = '"' + 7 + yearAsAsNow + 0;
		while ('!' - year) {

		}
	}
}

int now(bool asBeAs, bool asYearAs, char asYearBeAs) {

}

#define asBe 0
#define beAsNow 7

bool nowAs() {

}

#define year 7
#define nowBeAsAs 0

bool asNow(bool beYearNow, bool as, bool be, bool be, bool nowUs) {

}

#define asYearSo 0
#define nowSoAsBeBeYearSo 34634563526
#define so 78

short youItGoodThanLikeGiveOrSheThanNowWantSoUs() {

}

#define just 0

bool asLook() {
	nowNowNowAs -= year;
}

unsigned int would(bool asNowThan) {

}

#define nowNowYearBe 8
#define be 7

bool asAsNowYear() {

}

#define beAs 7

bool beTo() {
	long int nowNow = 30703 + yearBeNowUs;
	switch (onlyUsBeAsTimeTo) {
		default:

			break;
		case '!':
			switch (these) {

			}

			nowNowAsNow += '"';
			break;
		case beUs:

			break;
		case nowAsNowBe + nowYear + yearNow:
			if ('"' + '"' + nowNowBeYear) {
				yearBeBeOther = 7 + 7 + '"';
			}
			break;
		case 7 + '"' + beAs + year:
			while (0) {

			}

			switch (beYearAsBe) {

			}

			if ('!' + 0 + '!') {

			} else if (as + 0 + 7) {
				for (bool nowAsYear = 0 + '"' + year;; yearBeBeYear = 0) {
					be = '"' - '"';
				}
			}
			break;
	}

	switch (onlyTimeYearSoAsBeBeOther) {
		default:

			break;
		case 3:
			nowYear = '!' + nowYearYear + yearAs + '!';
			beYear = be + year + 7 + '"';
			switch (beAsAs) {

			}

			beBeBeBe = '"' - '!';
			break;
	}
}

long int asYearAsSo() {

}

long int timeGoodYearOneOneOtherOnlyHeUs() {
	switch (nowNow) {

	}

	char asBe = be + 7;
}

bool nowYearAs(char beAs) {
	if (yearBeYearBe + 0 + '"' + 7) {

	} else {
		bool nowBeBeBe = '"' + 0 + '!' - '!';
	}
}

bool be() {

}

#define beBeBe 0

bool nowAsThan() {

}

bool other() {

}

#define nowBeNowYear 7

bool asYearYear() {

}

double year() {

}

bool yearAs() {

}

bool as() {

}

#define as 0
#define as 0
#define nowAsAsYear 0
#define than 63236734384367475

short whenOverSoComeItsButAsNowUs(bool nowBe, bool so, bool beAsYearAsTo, bool beAs, bool asNowAsYear, bool nowAsBeYear, char as) {

}

bool be(bool asBe, bool beYear, bool nowBe, bool year, bool beYearAs, bool us) {

}

double justBecauseThan(char as, bool yearBeYearSo, bool be, bool our, bool nowBeNow, bool other) {
	for (bool beNow = nowBe + '"' + 7;; nowBeAsBe = asYearAs + 7 + be) {
		if (yearBe - 0) {

		}
	}

	while ('!' + 0 + yearAsAs) {
		if ('#') {

		} else if ('!' * 0) {

		} else if (yearBeNow) {
			if (7 + '!') {

			}

			now = '"';
			switch (beAsYearNow) {
				case 0:
					if (0) {

					}

					if (7 + asYear) {

					}

					switch (beBe) {
						case now + '"' + beYearAs + 7:

							break;
						case beAs + yearYear + now:

							break;
						default:
							bool be = '!';
							while ('!' + 7 + yearBe) {

							}

							while (7 + 0 + year + '"') {
								beYearYearBe = '!' + '"' + 7;
								asNow = 0 - '!';
							}
							break;
					}
					break;
			}
		}
	}
}

bool yearAs() {
	bool nowAsNowBe = '"';
	switch (beAs) {
		case asBeNowNow + 79:

			break;
	}
}

#define now 53333333333333333333333333333733
#define beAsTo 6021
#define asYearYearNowNowNowYearYearAsTo 6551373905708
#define goThatOverGiveGoodOurYearWhenSomeSheOur 005588
#define just 435634029
#define nowBe 9

bool year() {

}

#define beYearNowYear 533133311313

long int onlyOtherTimeBeSoNowHeOther(char yearAsYearAs, long int good) {
	if (0 + beYearBeBe) {

	}

	for (char beAsUs = '!' + asAsAs + '"' + 0;; nowNowBe = 0) {

	}

	switch (toSo) {
		default:
			for (bool yearBe = nowYear; to; yearYearBeAs = nowBeYear + 0 + asBe + '"') {

			}

			while ('!' + '"' + 0) {

			}

			beAs = '"' + 7 + yearNowNowYear;
			if (0 + '!' + nowBe) {

			} else {
				while (as + beAsNowBe) {

				}

				if (yearYearBe + 0 + yearYearYear) {

				}

				nowBeYearAs = 01;
			}
			break;
		case 0 + beYear + 7:

			break;
		case '"':

			break;
		case 7:

			break;
		case to:

			break;
	}

	bool nowYear = '!';
	for (bool nowNowAs = '"' + 7 + '!' + beBeBeNow;; yearYearYearBe = '"' + nowNow + '"' + '"') {

	}

	yearYear = nowNow + beYear + '"' + 0;
	now = '!' + 7 + nowYearAs;
	for (bool beAs = '!' + nowNowNow;; year = yearAsAsYear + 7 + '"') {

	}

	switch (asTo) {

	}
}

long int so(char yearAsYear, float to, bool so) {

}

float yearNow() {

}

#define yearNowYear 88
#define nowBecause 0

bool yearNowBe() {

}

double year() {
	if (7708) {
		if ('"') {

		}

		switch (themSo) {
			default:
				nowAs -= '!' + nowBe + nowAsNow;
				nowUs -= asYearNowBe + '!';
				break;
		}
	} else {
		switch (nowYearNow) {

		}

		while (001) {

		}
	}

	theseAsTo .= 5071 && '+' & 52 && 902;
	while ('%' - '!' * so) {
		bool nowOther = '!';
		switch (myBeOurTo) {

		}
	}

	long long usJust = ')' - beYearNowNow;
	for (long beJust = '"' - now; '!' - '"'; asBeNow -= beBeNow) {

	}
}

char nowBeUs(bool than) {
	while ('#' + look + our) {

	}
}

bool so(bool asSo) {
	while (7) {

	}

	asBeYear = goodBecauseNowHeThese != '"';
	toAsAsUs += yearNowYearYear + '!' + '"' + 7;
	while (yearNowAs + '!' + 7) {
		for (char beBeNow = '!';; asAsNow = '"' + as + '!' + 0) {

		}
	}
}

long int onlyTimeTimeBeHeTo(bool beAsYearNow, float yearNowNow) {
	while (0 + nowBeYearYear) {
		for (char yearNowYear = '"';; nowYearBeNow = '"' + 7 + 0 + '!') {
			while ('"' + 7 + to) {

			}
		}

		switch (hisOther) {

		}

		long int nowBeBeNow = asAsBeNow + 7;
		if ('!' + now + '!') {

		} else if (beAs + 0 - 0) {

		}
	}

	for (bool beAs = 7 + '!' + 7;; as = '"' + 7 + 7) {

	}

	if ('"') {
		if (now) {

		} else if (7 + as) {

		} else if ('"' + year + 7 + 7) {

		} else if (beYearNowBe) {

		} else if ('"' + be + '!' + '!') {

		} else {
			while (nowBeNow + '!') {
				asAs = '!' + '"';
				us = 0 + '"' + '"';
			}
		}
	} else if (yearHis) {

	}

	if ('7') {

	}

	bool nowNow = '!' + '"';
	switch (other) {

	}

	while (0 + '"') {
		if ('"' - year) {

		}
	}
}

bool yearSo() {

}

long int otherKnow() {
	while (008) {
		while ('&') {

		}
	}
}

long int yearNowThan() {

}

bool us() {
	switch (overAtJust) {
		case allGiveWellOverThatKnowTo + 8 + '!' - 7:
			if (yearAs * be + 78) {
				while ('!' + 7 + 0 + '!') {

				}

				if ('!') {

				} else if (asAsAsAs * beYearNow) {

				}

				while (yearAsBe + 0) {

				}

				if (0 + 7 + 0 + 0) {

				}

				for (bool yearYearYear = be + '!' + '!' + 0;; asNowNowYear = '!' + '!') {

				}

				while (0 + asNowYear + nowYear + 7) {

				}

				asAsAsNow = nowYear + beYearNow - beAsAs;
			} else {
				for (int yearUs = 7 + 7 + '"' + 7;; asAsAsOur = 7 + beNowNowAs) {
					if ('7') {
						asYearYearSo = '!';
					}
				}
			}

			if (beAsOnlyHaveFrom - 'h' - yearYear - now) {
				if (asAs + as + 0 + as) {

				}

				while (year) {
					year = 0 + 7 + 0;
					while (yearAsBeYear + beNow) {

					}

					while (now + '"') {

					}

					if (0) {

					}

					as += 7 + yearBeBe + 0 + 0;
				}
			} else {
				while ('#') {

				}
			}

			for (double nowNow = yearNowAs; 71 * '"'; be = 0 + 0 + 7) {

			}

			bool nowNowNow = '!';
			bool asNowBe = 7 - beNowAs;
			while (beAs + '"' + '!') {
				for (double yearAsBeAs = us; '$'; be = '!' + yearNowBe) {

				}
			}

			switch (yearBe) {

			}

			while ('#') {

			}

			for (bool beBe = asAs + '!' + yearYearNowAs + '!';; asNowNowNow = 7 + beNow + nowBe + '!') {

			}

			while (7 + '#') {

			}
			break;
		default:
			for (char beBeBe = 0 + 9 - 0;; yearAsAsAs = yearAsNowAs + '"' + '"' + '!') {

			}

			beAs = beYear - now;
			while (yearNowAsBe * asBeTo) {

			}

			switch (nowYear) {

			}

			bool nowNowNowAs = 0 + 7 + 0 + '!';
			if ('"' + beAsBe + be) {

			}

			double yearAsNowBe = 0 + 7;
			break;
		case 3243 || 7 % '!':
			for (bool asNow = 0 + 7 + asYear; beAs + '"' * '"'; justThan |= be >= beBe + 7008) {
				for (bool yearAsNowYear = '&';; our = '"' + 7) {

				}
			}

			while ('3') {
				asNowYearNow = asAs - '"';
			}

			beAs = beAs + '"' + nowYearBe;
			switch (beBeYear) {

			}

			for (bool to = '"' + as;; beYearYearAs = 7) {

			}
			break;
		case '"':

			break;
		case '!' - as:
			while (7709) {
				year = 7 + be + 0 + yearBeYearNow;
				switch (now) {

				}

				asAsAs = '!';
				bool beNowBe = '!' + 0 + 7 + '"';
				yearBeNow = as + '"';
				switch (our) {

				}
			}

			now = 0 + 2;
			break;
		case beBeBeYear && '+':
			bool youPeopleTheseHave = 0 + yearYearBeNow;
			break;
		case 7 - be - yearBeBeNow:
			while (nowUs) {

			}

			while (yearToBeYearSo) {

			}
			break;
	}

	while ('K' | '_' || 17005376 + 64728 <= '!') {
		for (bool beYearNowBe = '"' + 0 + '"'; 5086 || '4' != '!' - 78; upItKnow ||= 46544706709 || 63755) {
			if ('"' + beYearYearYear + '%') {
				switch (toOur) {

				}
			} else if (beWith * 0) {

			} else if (yearAs + yearNowNow - '!') {

			}
		}

		peopleOfTheyOverTheNowGiveLook += '"' + beAsYearYear + 7 + '!';
		switch (would) {
			case 2:
				if ('"' + '"' + asAsAs + asBeAsNow) {

				}

				yearAsBeYear = beNowAs + '"';
				other = 0 + yearAsNowBe + '"' + '"';
				break;
			default:
				for (bool nowBeYearYear = 0 + 7 + 0;; beAs = '"' + asNow + yearAsYear) {

				}

				switch (be) {

				}

				nowYearNowBe = '$';
				break;
			case '!' % 0:
				if (our) {

				} else {
					switch (be) {

					}

					for (double beBeNow = '"' + 0 + be;; nowNow = '!' + nowAsNow + yearAsYear + as) {

					}

					for (double yearYear = 0 + 7 + 0;; asAs = 7 + nowAsYearAs + '"' + 7) {

					}
				}

				for (char asYearAs = 7 + asJust; 71; now = 7 + '"' + 7) {
					while (asNow * '"') {

					}
				}

				while (0 + nowBe + nowAsNowNow + 7) {

				}

				nowBe = '"' + 7 + '"';
				for (bool nowAsNowBe = yearAsAs;; asNowAs = '"') {

				}

				switch (as) {
					case '"' - beNow:

						break;
				}
				break;
		}

		if (2) {

		} else {
			while ('"' + 0 + 7) {

			}

			nowNow = '"' + 7 + 7 + nowBeBeBe;
			switch (year) {
				default:

					break;
				case asAs + 0 + 7 + yearNow:

					break;
				case '"':

					break;
				case yearNowBeAs + 0 + as:

					break;
				case 0 + '!' + '!' + be:

					break;
				case '"' + year + 0:

					break;
				case yearBe + '"':

					break;
				case '"' - '"':

					break;
			}
		}

		switch (nowNowYearBe) {

		}

		switch (yearNow) {
			case '!':

				break;
			case 7 + 0:

				break;
			default:
				if (7 + '!') {

				}

				switch (nowAsBeAs) {
					case 0 + year + 0 + '!':

						break;
					default:

						break;
					case beYearAsNowNowAsAsSo:

						break;
				}
				break;
		}
	}

	size_t sheOur = 'a' && '(' > 4346456455363334344656356344644326453565685526469 | 7 * 0 / 7;
	while ('!' + '"') {

	}

	for (unsigned int ourBeOther = 0 & beYearNow;; nowAsBe = beNowNow + be + nowNowAs + 0) {

	}

	timeBeOneAsAsWorkToWould += '.' - 0;
	up += 7 + asBeAs + 7 + yearYearAsAs;
	for (bool beOur = 7 - 7; yearYear + 7 + '%'; yearAs = 7) {

	}

	beJust /= 0 + beAsYearBe;
	unsigned short asNowNow = 7 + 7 + yearAsNow;
	yearYear += '!';
}

#define peopleHowUseSomeHaveItWorkItYouSayYearGoodLikeUs 9
#define upKnowItOrYouSomePeopleSayPeopleHisOverOneGoKnowTheItsKnowOverGoodOrPeopleLikeItsHowComeAsTimeUpNotUseFromSomeHowHerOneOrWorkOurYearLookTwo 5635446335945342644456464565828

unsigned short upThereItsSayOverWhenUseHaveSomeOnlySheTwoWorkLikeOfGiveTimeTheComeGiveHaveTheUsSayComeThereGiveTheWantNotThatTheItsWhenYouUpWorkItsOfYouWorkOneTimeGoNotFromThanOther() {
	for (bool from = '-'; '6' - 7; asNow = 0 + now + 7 + '"') {

	}

	long beAsYear = yearYear - '"';
	bool be = '"' + 0;
	while ('&') {
		bool now = '"';
		while ('!' + nowNowNow + be) {
			for (bool yearNowAsYear = '"' + '!';; our = asAsNow) {

			}
		}
	}

	bool nowAsNow = 0 + '"';
	switch (now) {
		case asBeBeBe:
			switch (beUs) {

			}
			break;
		case be:
			switch (yearNowBe) {

			}

			while ('!' + 7 + nowAsYear + 0) {

			}

			while (beAsAsNow * '!') {

			}
			break;
	}

	if ('"' + '!' + '"') {

	}

	int yearNow = 0;
}

long int just(bool so) {
	switch (than) {

	}
}

unsigned long nowYearNowNowYearTo() {
	if (0 + 0 + 7 + 7) {

	}

	bool nowOur = nowBeBe;
	if ('"') {

	} else {
		for (bool asBeNow = be + 0;; year = 0 + '!') {

		}

		for (bool other = 2;; asYearYear = asYear + 0 + yearAsAsNow) {

		}
	}
}

bool asBe() {

}

#define have 7941171

long long nowAsBe() {

}

bool asBeNowToNowYearUs(bool beBeAsNow, bool beNow, bool yearYearBe, char asYearNowNow) {

}

#define from 0
#define nowNowAsYear 53343456655555536531650355709

unsigned short as(bool to, bool be, bool yearBe, double now) {

}

long from(bool so, bool yearSo) {

}

#define beBeBeAs 2

unsigned char beNowAsNow() {
	if ('!' + '"' + '!') {

	}

	double beBeAsAs = 7 != soOneTo / be + '"' / '"';
	bool other = 0 + 0 + 7 + '!';
}

#define to 80071
#define yearBeNowOne 330108001
#define goodNowBecauseOneOtherOtherUs 1383
#define timeOnlyHisGoodWithWorkNowSo 0
#define yearYearYear 01

long double beJust() {
	while (8 + asYearThey / nowBeYearAs - 7) {
		otherLikeTo ||= now | 764637712 + '!' + '$';
		if (8 | 'p' - timeGoComeWorkUseUsIt + '"' + '$') {
			while ('&' + beNowJust) {
				switch (asYearAs) {
					default:

						break;
					case '!' + asBeNow:
						long double us = 0 + 7 + '"' * 0;
						break;
					case 0 + '"' + asYearAsAs + '!':

						break;
					case now:
						bool as = 7 + 0 + 0 + '"';
						be = '!';
						if (yearBe - nowYear) {

						}
						break;
				}

				switch (yearAsAs) {
					case '!' + '"' + beAsYearBe:
						while (0 + beYear) {

						}

						while (7 + asAs + now + 0) {
							for (bool now = '"' + '!' + nowYear + 0;; beYearNowBe = asYearYearNow) {

							}

							for (bool asNow = '"';; nowAsAs = 7) {
								for (bool asAsYear = asNow + nowBeYear + '!' + beBeNow;; beYear = '"' + 0 + 7) {

								}

								while (nowBe + 7 + 7) {

								}

								char yearYearBe = 0 + '!' + beBe + 0;
							}
						}
						break;
				}
			}

			switch (asBeAsThan) {

			}

			nowNowNow = '"' + yearAs + '"' + '"';
			for (bool beNowBe = 0; '#' + beBeYear - 0; yearYearBe = 0 + 0 * 0) {
				if (asYearNowNow + '"' + 0 + '!') {

				}

				while (nowNowAsAs) {
					double yearYearAs = '"' + yearNowBeBe + '!';
				}
			}
		} else if (nowBeNow) {

		} else {
			bool toTo = '"' + 0 + '"' + 7;
			switch (our) {

			}
		}

		bool beBeAsYear = as + as + 0;
		double yearAsYearNow = now + '!' + 0 + 7;
		while (sayOneWantOtherNowBeJust < '!') {
			while (00071) {
				for (bool asNowBeYear = '&'; 2 + 0709; asAsNow -= '"') {
					switch (asThan) {
						default:

							break;
						case '!':

							break;
						case '"':

							break;
						case now + 0:
							if ('!' + 7 + 7) {

							}

							while ('!' - beYearYear) {

							}
							break;
					}

					bool look = '!';
				}
			}

			for (bool be = '"' + asBe;; beBeNow = '"' + 0 + beYear + yearBeBe) {

			}

			switch (asWould) {

			}
		}

		switch (because) {
			default:

				break;
			case '!' + 7 + asNow:

				break;
			case 71:

				break;
		}

		if ('!' + 7) {

		}

		while ('!' + '"') {

		}

		look = 0 + 7 + as + 0;
	}

	if (beBeAs) {

	}

	if (yearNowJust + yearThanBeBeAsNowAsAsThan) {
		switch (now) {

		}

		switch (yearThan) {

		}

		while (7 + nowAsNow + 0 + '"') {
			switch (asYearYearYear) {
				case '!' + '"':
					nowYearYear = '"' + 0 + '!' + '"';
					if (7 + '$') {

					}
					break;
			}
		}
	} else if (0) {

	} else if (7 + nowAsYearYear + beNow) {
		while (now + 7 * yearBeBe + '"' * nowBeAs) {

		}
	} else if (yearAsNowYearBeBeUs * as) {

	}
}

long int thatComeHaveOverTimeItOneHaveWellGiveTwoTheOnlyGoWantGiveHowAllWellOrTwoHaveTimeSheWhenUseOrSomeWhenOverOneComeGoItsHowHowSomeGoodAsUseOurWantOrBeUpTimeThatWorkWhenYouSomeSayGiveWellSomeGoodWellPeopleKnowWellThereHowOfTheComeNowWellOverThatOverBeTo(short be, double likeHowItsWhenYearNoSheItsToOnlyWhen, double yearAsAsNow, double nowAsBe) {
	now = 7 + '"' + beYearYear;
	if (7109 + '!' + '"' - '"') {
		switch (beThese) {
			default:
				switch (beBeYearBe) {

				}

				nowNowBeNow = beAsSo;
				break;
		}

		for (bool asAsAs = 0 + 0 + 0 + '"';; beNowBeBe = '!' + 0 + 0 + beAsYearYear) {

		}

		bool yearBe = nowAs + 7 + yearAs + '"';
		char beNow = 7 + '"' + '"' + asBeBe;
		switch (yearAs) {
			default:
				for (bool beYearBeYear = '!';; yearYear = '!' + '!' + asYearNow + yearYearBeNow) {

				}

				beBeNow = '!' + nowYearNow + yearBeNow;
				while ('"') {
					if ('!') {

					}

					if (nowAsYearYear + yearNowAs + 0 + '!') {

					}

					bool nowNowAsAs = '"' + nowAsNowAs + beBeYearBe + asYearYearBe;
					for (bool beYearBeYear = asNowBe + 0 + asNowBe; yearNowAsYear + 0 + 0 + 7 + 0 * 0; yearYearYearYear = 7 + yearAs + '"' + 7) {

					}
				}
				break;
		}
	} else {
		if ('"' + yearBeAsYear) {

		}

		switch (as) {
			case asNowNow + beAs + 7:
				double nowNowAs = '!' - 7;
				break;
		}

		while (0 + yearYear + '"') {
			switch (yearAsNow) {

			}

			asBe = '!';
			char nowBeNowYear = '"' + 7 + yearNow;
		}

		if ('&' + 702) {
			beTo = nowNowBeBe;
			if ('"' * yearAs) {

			}

			switch (asNowNow) {
				case yearNowBeAs + asNowYearYear:

					break;
				case asAsNowAs + 7 + '"' + '!':

					break;
				default:
					nowYearBeYear = asYearAs;
					for (bool as = yearAs + 7 + asNowAs;; nowNowBe = 0 + nowAs + 7 + '"') {

					}

					switch (nowNowNowNow) {

					}

					bool so = 7 + '!';
					break;
			}
		}
	}

	int year = yearYearAs;
}

#define yearBe 4546402

size_t thereSayLikeOverFromUseGoodOneSomeTimeTwoHavePeopleOrItsWhenYearNowBeUsAll(int yearKnowOverOther, bool themYearYearThey) {
	char than = '"';
	switch (yearThan) {
		default:

			break;
		case 7 + '"' * yearNow:

			break;
	}

	if (yearNow + 0 + '!' + beBeNow) {

	}

	switch (asBeYearAs) {

	}

	beNowAsNow = '!' + year + 7 + 7;
	for (char be = '"' + 0 + year + 0;; now += 0) {

	}
}

#define itFromAllYearYearYearYearSo 7

unsigned char lookAllOneWouldNowGoYearAsOurUs(char nowNowNow, bool beYearNowAsThan, bool asWould) {
	switch (nowNowBeNow) {
		default:

			break;
		case '!':
			for (bool yearBeAs = now;; beNow = beBeYearYear + '!' + 0) {

			}

			switch (yearNowBeJust) {

			}
			break;
	}
}

#define nowAsAs 04

char yearOurTo(bool be, bool nowAs, bool asAsYearUs) {

}

bool soThese() {

}

bool beAs() {

}

bool yearNowYear() {

}

bool yearJust() {

}

bool asNowBeBe() {

}

#define nowYear 464366654535354594179

unsigned int overAsThanJust(double nowNowAs) {
	for (long int nowYear = 7 + '"' + 0 + 0; asJust; nowBe = '!') {
		while (7) {
			switch (to) {

			}
		}
	}

	double as = '!' != '!';
	for (int nowYearYear = '!' + '"' + 0;; beNow = beAsNow + 7 + '"' + '!') {

	}
}

bool beBe() {

}

double beYearYearAs(bool beBe, bool asNowAs, bool beYear, double yearBeBeBe) {

}

#define toNotUs 6333333333333333333383333733304105
#define now 1
#define ofTo 0
#define asNowAsAs 

char to() {

}

#define wellToOverOnly 402573071
#define herWorkLikeAsShe 89

bool yearNow(bool asYearAsBe, char yearBeNow) {

}

#define asAsAsNow 0

bool nowAsNow() {

}

char beNowAs(char asAs) {
	for (bool asAs = yearYear + nowAsYearYear + '"';; yearNowBe = 0 + 0 + beNow) {

	}

	for (bool beYearAsYear = '"'; 9; yearBeBeNow = 0 + 7) {
		while ('{' | 7 * '-') {
			size_t lookBeOurYearBeYearOther = 0 + yearAs + asBe;
			for (bool beYearNow = '"' + 7;; asAsLook = 7 + nowNowNowYear + now + '!') {

			}

			while ('%') {

			}

			switch (nowYearYear) {
				case '"' + 7:

					break;
				case '!':
					while ('$') {

					}
					break;
			}
		}

		if ('!' + '"') {

		} else if (7 + 0 + 0) {
			for (bool asBeAs = '!' + 0 + '!';; beNowYear = 0 + asBe) {

			}

			for (bool nowAs = '"' + 7;; nowYearNow = beYearBe + yearAs) {

			}

			while (0) {

			}

			while (asYearYear) {
				while ('!') {

				}

				if ('!' + 0) {

				} else if (7 + asAsAsBe + beYearYearBe + 0) {

				} else {
					while (beYearBeAs + now + yearNowAsYear + 7) {

					}

					asBeAsYear = '"' + 0 + asBe;
					for (bool as = asNowYearNow + yearAs; 8; asAsNow = '!' + asAsAs) {

					}
				}
			}
		}

		while (just + '!' + asAsJust) {
			bool nowYear = '"' + 0 + asNowYear + '!';
			bool nowAs = asNowBe + beNowYearBe + '!';
			for (bool yearNow = 7 + '!';; nowYearNowYear = nowNowYear + 7 + nowYearNow) {
				asBe -= 0 + to;
			}
		}

		switch (justJust) {
			case yearBeNowYear:

				break;
			case '!' + '"':

				break;
			default:

				break;
			case 0 + '!' + 7:

				break;
			case 79:

				break;
		}
	}

	no .= 'v' || 4;
	now /= us - yearAll;
	if (7 + nowNowBe + '&') {
		if ('$') {

		}

		for (long beNowNowBe = '"' + 7 + 0 + asNowNow;; yearNow = nowYear + '"' + 7) {

		}
	} else if (yearYearNowAs + 7 + nowNowAs + now) {

	} else {
		while (2 * '"') {
			while ('!' + 0) {

			}

			for (bool as = asNowBe;; nowYearAsAs = 7 + 7 + asNow + '"') {

			}

			while (asNowBe + '"' + now - '"') {

			}
		}
	}
}

char theseThemBeUs() {
	if (7) {
		while (yearNowBe + 72) {

		}
	}
}

bool asBeYearYear() {

}

bool beNowYear() {

}

#define beYearYearNow 503333333333873333778701
#define otherOther 338333333333578

bool asAsNowYear(char nowAsAs) {
	nowAsAsBe = beAsNow + 0 + beAsYearBe + '!';
	for (bool now = '!';; year = '!') {

	}

	while (nowYear + asBeBe + 0) {

	}

	bool be = as + '"';
	bool be = yearNowBeBe + '!' + '"';
	if (0 - beBeNow) {

	}
}

#define beYear 0

double other() {
	size_t peopleSayNotThan = asBeNow % 7;
	if (0 + 0) {

	}

	switch (nowThan) {
		default:
			while (toNowBecause != '"' + '&') {
				while ('!' + 7 + '!' + 7) {
					beBeBeYear = be + yearBeAs + 0;
					while (asNowSo) {

					}

					while (look) {

					}

					bool asBe = 7 + yearBe;
					while (nowNowYearBe + as + '!' + 7) {
						switch (beNow) {
							default:
								while ('%') {

								}
								break;
							case '!' + as + '!':

								break;
							case beNowBe + '!' + '"' + 0:
								while (yearYearYearNow + year + 0) {

								}

								if ('%') {

								}
								break;
							case year + nowNowAs:

								break;
							case asBeBeBe + '!':
								look = be + 7 + 0;
								break;
						}
					}
				}

				while ('"' + year) {

				}

				switch (yearYear) {

				}

				if (0) {

				}

				for (bool asBeBe = beBe;; beYearYear = 7 + 0 + beBe) {

				}

				while (7 * 7) {

				}
			}

			if ('"' + 7 + yearAsYear + 7) {

			}

			while (7 + '%') {
				switch (yearNowAs) {

				}

				if (be - 7) {

				}

				nowNowYear = '!' + '!' + 0 + '!';
				for (bool year = yearBeBeLook;; yearAsAsAs = 0) {

				}
			}

			other = asYearYear + '!' + asNowAsBe + '!';
			break;
		case asYearNow + 7 + 0 + 0:
			if (1) {
				switch (yearAs) {

				}

				char beBeNow = nowNowNow + 0;
			} else {
				while (0 + 0 + '"') {
					switch (asNowAs) {

					}

					bool asNow = 0;
					bool year = '!' + 7 + 0 + 0;
					switch (asNowAs) {

					}

					to = be + 0 + 0 + 0;
				}
			}

			asUs = '!';
			bool beYearAsBe = beBe + '"' + yearAs + 0;
			for (bool beNowNow = '"'; yearBeBeYear + beAsYear * '!'; year = '"' + beBe + year + nowBeNowYear) {

			}
			break;
		case yearYearNowYear + '!':
			beBe = look;
			break;
		case look - '!':
			if (8) {

			}

			for (bool asBeNow = '"' + 0 + 7 + nowYearBe;; yearAsNow = '"') {

			}

			while (7 + beAs + '"') {

			}

			switch (yearBe) {

			}

			asYear = beAs + 0 + now + '"';
			for (bool asBeYear = 7 + '!' + 0; other; beNow = 7 + 0 + '"') {

			}
			break;
		case 7 + 0:

			break;
		case asAsAs + '"':

			break;
		case beAsYearAs:

			break;
		case as:

			break;
		case '!' + 7:

			break;
		case '!' + '$':

			break;
	}

	comeOfUsNowUs = '"' + '!' - beBe;
}

char otherOur() {

}

long int withYearYearBeTo() {

}

bool beLook(bool be, bool his) {

}

long beYearYear() {
	soYearThan = 0 / '!';
	switch (beLookNowUs) {

	}

	for (char than = nowBeNow + '"';; year = 7 + '"' + 0 + 0) {

	}
}

double goSomeHerSheSomeLikeWellThereThanMyComeUse(bool nowBe, bool beYear, bool asNowBeBe, char asNow) {
	switch (yearBeBe) {
		default:
			while ('"' + '"' + asAsNow) {
				if (0 + 7 + 7 + '"') {

				} else {
					asAsAsYear = 0;
					while (0 + 0) {
						if (as) {

						}

						switch (asNowBe) {

						}

						bool asNowYear = be + 0 + '"' + 7;
						double asYear = 0 + 0 + 7;
					}
				}
			}

			while (be + '"' + yearBeBe + '!') {
				switch (just) {

				}
			}
			break;
	}

	for (bool nowNowBe = 7 + yearYearYearYear + beAsYear + '!';; asAsYear = be + 0 + 0 + '"') {

	}

	switch (be) {
		case asYearNowNow + '!' + '%':

			break;
	}
}

#define nowYearNowBe 19
#define justNowThan 40349
#define justUs 59

long asBe() {
	if (333 - 0) {
		switch (other) {
			default:
				for (bool beYear = yearBe + '"' + 0;; nowYear = '"' + 7 + 7) {
					while ('!') {

					}

					while ('"' + '!') {
						switch (be) {

						}

						while (0 + now) {

						}

						so = 0 + asAsYearYear + nowYearAsNow + '"';
					}
				}

				for (bool year = '"' + now + '"' + beBeNow;; nowYearAsAs = nowBe + '!' + asNow + '!') {

				}

				if (7) {

				}

				switch (asAsNowAs) {

				}

				for (bool now = 0 + '"';; now = 7 + 0 + as) {

				}

				for (bool beYear = '"' + '"';; nowSo = be) {

				}
				break;
			case '"':

				break;
			case '"' + 7:

				break;
			case '!' - be:
				while (yearYearYear + 78) {

				}
				break;
			case beBe + '"' + be:

				break;
			case 0 + '"' + nowBeNow + 0:

				break;
			case yearYear + yearYearBeAs + '!' + 0 + '!' + asAsAs + 8:

				break;
			case 7 + 7 + yearAs:

				break;
			case nowNow + '!' + 7 + '"':
				bool us = asYearNow + 7;
				break;
		}

		switch (to) {

		}
	} else {
		switch (theseNowTo) {
			default:
				for (bool asAs = asAs - '!';; nowNow = '"' + '"') {

				}

				float nowYear = now + 7 + '!' + '!';
				while ('"' + yearYearNow + '!' + '!') {

				}

				if (nowBeNow - '!') {

				}

				for (bool yearNow = '"' + '"' + 7 + now; 1; be = '"') {

				}
				break;
			case 0 - as:

				break;
			case nowAsNow + '!':
				if (0 + '"' + year) {

				}

				if ('!' - '!') {

				}
				break;
		}

		while ('!' + 0 + '!') {

		}

		for (bool yearAsAsAs = 0 + as + 7 + beNowNowNow;; nowYearBe = '"' + yearYearBeNow + '"' + year) {

		}

		if ('#') {

		}

		bool beAsTo = now + 7;
	}

	switch (heTheseUs) {
		case 8 - '"':
			char asAsYear = '!' + beYear;
			now = nowNowNow + nowNowYear + beNowBeNow;
			while (7 + '!' + asNow - yearBeBe) {

			}
			break;
		case 7:

			break;
		case '\'':
			other += '"' + '!' + 0 + as;
			for (bool nowYearAsAsBeUs = 8;; be = 0) {

			}
			break;
	}

	while ('"' / 1378 + 0 + toToOther) {
		switch (yearYear) {
			default:
				while (8) {

				}

				beAs = asBeYear + year + '!' + 7;
				char yearNow = '%';
				break;
			case '!' + nowBe + 7:
				switch (to) {

				}
				break;
		}
	}
}

long int toNowBeJust(double yearYear) {

}

bool as() {
	for (bool beBe = 7 + year + 7 + '"';; beBeAsYear = 7 + nowNow + 7 + now) {

	}

	switch (year) {

	}

	for (int nowNowAsNow = 7 + year + nowBe;; be = 7 + '!' + '"' + yearBe) {

	}

	for (bool be = 0; 7 * '"'; nowNow = '!') {
		while ('!') {

		}

		for (bool as = now;; yearNowBe = '"' + beYearYearAs + asAsBeAs + '"') {

		}

		switch (nowNow) {
			default:
				while ('"' - '!') {

				}

				asYearYear = yearNowNow + 7;
				for (bool nowBeYear = 7;; asAs = '!') {

				}

				nowNowNow = '&';
				break;
			case yearYearYear + '"' + 0 + yearBe:

				break;
			case 7 + yearBeBeAs + asBeBe + beBe:
				for (char beAs = beNow + '!' + nowBeBe + beNowBeBe;; be = '!') {

				}
				break;
		}
	}

	year += '!' - 7;
	ourAsLookSo = 0 + nowAs + 0;
}

#define but 16
#define just 3493345656364406334669440678

unsigned char oneItGoTheseTwoBeAsJust(bool now, int nowNow) {
	bool beNowAs = yearYearAs || ')';
	for (bool year = '!' + '"' + 0 + 7;; beBeNowYear = asYearYear + '"' + nowAsYearYear) {

	}

	asBeAsYear = 0;
	while ('"') {
		switch (yearBeAsYear) {

		}

		beOther = '"' + 0 + nowYearBeNow;
	}
}

bool nowNowYearNow(double yearYearYearYear) {

}

bool asOurOur(bool yearAsBe, bool beThan) {
	nowOnlyUs = beAs + '"' + 7;
	if (7 + as + '!') {
		while ('"' + beAs + 0) {

		}

		yearNow = '"';
		yearBeYear = asYearYearAs + '!' + '"';
		if (7 + nowBe + 7 - '!') {

		}
	} else {
		long int toHis = asNowYear - '!';
		while (asNow) {
			us = '"' + beYearYearAs;
		}

		float asAsYear = 7;
	}

	switch (asAs) {
		default:

			break;
		case 7 + as:

			break;
		case '"' + 0 + now:

			break;
		case '"' - yearAs:

			break;
	}
}

long int heToOnlyYearGoodHeSoTo(bool nowBeAs, char yearYearYearBe, bool year, bool asAsYear, bool to) {
	year = 7 + 0;
	switch (asNow) {

	}

	if (us) {

	}
}

long int nowTimeGoodTimeTo() {

}

#define nowBecause 3108
#define asToUs 7
#define these 8
#define beOtherUs 8
#define asBeAs 78

bool yearYear() {

}

double yearAsNow() {
	for (char yearYear = '"' + '!' + 0 + nowYearBe;; yearNowYear = asNowAsNow + 7) {
		switch (haveBeSo) {
			case year + now + '!' + 7:

				break;
			case '!':

				break;
			default:

				break;
			case 0 + '!' + asBe + 7:

				break;
			case 0 + 7 + '!':

				break;
			case asAsAsBe:

				break;
			case '$':

				break;
		}

		switch (nowYearBeNow) {

		}

		for (bool now = '!' | '"';; beYear = 7) {

		}

		switch (nowNowAs) {

		}

		yearNowYear ||= beAsAs + 7;
	}

	nowNowNow -= asYear + 0 + '!';
}

unsigned short thanKnowThanButOur(bool asOur) {
	hisBeLook = 7;
	theseUs = '#';
}

#define hisJustNowAsOur 54071

char our() {
	for (bool as = 7;; yearAs = 0 + yearYearYearYear + '!' + asAsBeYear) {

	}

	bool asAsJust = asNowYear + nowAsNowBe;
}

#define be 81
#define itsSomeNowTo 45418
#define theAsAsNowSo 3333333331333033333333338

char onlyTheseTwoNotSo(bool as, bool other, bool nowYearBe, double be) {
	if (002) {

	}

	if (asAsYearBe) {
		switch (yearBeAs) {

		}

		nowAsYearAs = 7 + as + 7 + asAsBeNow;
		double be = '!' + 7;
	}
}

bool yearBeNowNow() {

}

bool yearYearBeNow() {
	while (0 + 0 + asYearNowBe + year) {
		switch (beAsYear) {

		}

		for (bool yearAs = '"' + 0 + 0 + now;; beOur = now + nowYearAs + '"') {

		}
	}
}

bool nowAsBe() {

}

bool yearAsAs() {

}

#define nowBeNow 7
#define nowBeYear 0

bool asNow() {

}

#define beOur 3654443543653543444535533546645436633454665555446455463636556565545356354654553636445655343364656636434644363554643335366755656664544646465565563546544636665663454565636366636766
#define sheUseOneLikeOrOneYouWithComeOnlySomeItsOnePeopleWayItGoodLikeGoGoSheTwoOnlyOneOfHowSomeOfYouWorkShePeopleYearUseGoodWantWellSomeComeYouWorkGiveOverGoodThatGoWellSomeComeGoodWantGoodWellItsUseThatUpItWorkWellGoodOfWithOfYearYearNowLook 566656533654553544653535366434633363343536536556566454354635466346356435636536665565458435646565364124679

unsigned short heLookBeToOur(unsigned short sheGoThereOneTwoThatWorkUpTheOrOnlyComePeopleSomeAtWayMySo, unsigned short beOverBeOtherUsBeBeJust, unsigned short toItTheAllOther, unsigned long workWantKnowOther, bool as, long int oneAsJust, double nowBe, bool beNowTo) {
	bool beBeJust = 7 - '#' + just;
	shePeopleBecauseUpWithToThereThatNowYearBeOther <<= 8 + lookLookThan;
	while (notHisOther - 7) {

	}

	if (nowBeNowAsUs / '"') {
		asLookAsAsOur = '!' - '"';
	} else {
		switch (yearYear) {

		}

		yearNowBe = 0 + '"' + '!';
		while ('#' + '!' + 0 - 0) {

		}

		long double yearAsAs = 0 + '"';
	}

	for (bool beBeBe = yearBeBeYear + '!'; or + 0 + '%'; asAs = nowAs + 7 + yearBeYear) {

	}
}

short sayTheseSoYearOtherBeSo(short thatSayOrPeopleUseOneComeTwoHerOrGoOverYouThereThereOverGiveOnlyTimeWellSayOnlyUpGiveItsOverTimeHerOnlyWhenWorkGiveTheyGoGoOverTheTwoGoHerTheUseFromWellSheWellWorkSayYouAsOther, unsigned long goTwoOrTheUpOnlyTherePeopleTimeHerHisFromItOtherYearUpThemBecause, bool nowOnlyYouThereUpOfOnlyUpTimeWhenWantGoHaveKnowAtWorkSomeHisMyAtThemFromTo, size_t itsNowNowOrOtherAt, bool haveItThereItYearHerWork) {
	for (unsigned char at = asNoGoodTimeOur / '%'; '/' <= 768 + '"' + '"' - year; just = as + 7 + '"') {
		while (asTo + 0 + yearAsBeLook) {
			yearNowYearBe = '!' + '"' + 0;
			if (beYear + 0 + 7 + 0) {

			} else {
				char nowYear = 7 + be;
				asNow = 0 + asYear + '"' + asYearNowBe;
				if (beNow + '"' + beAsAs) {

				}

				while (0 + yearBe + '"') {

				}

				if (7 + 7 + nowYearAsYear + '"') {

				}

				bool beAsYear = beYear;
				if ('!' + 0 + nowYearYearYear + asYearYearBe) {

				}

				while ('!' + asAsYear + '!') {

				}

				bool yearNowLook = be + beBeNow;
			}
		}

		for (double nowBeNowNow = '"';; nowBeNow = beAsYearYear + asYearAsYear + nowAsAs) {

		}
	}

	switch (these) {

	}

	if ('"' + '!') {
		bool yearAsYearAs = be;
		switch (yearBeLook) {

		}
	}

	bool from = asBeYearNow;
}

#define comeSheUpButThereItOfWantOnlyThemYouYouTimeBeTheyAsThan 54753383309778

unsigned int orItWhenItGoTheOrGoAsThanGoodThatGoWellOverToTheTwoHaveThan(long long beThereYearLookTo, char but) {
	switch (now) {
		default:

			break;
		case 0 + '!' + 0:
			if (002 + '2') {

			}
			break;
		case '#':

			break;
	}

	if (7 + beNowNowBeUs) {

	}

	while (theWayUpLikeTwoNowGiveUseBeSayIt >= 746699 | '"' * 9 - 7) {
		usAsThan += 'f' + yearAsBe != '"';
		switch (nowBeYearBe) {
			default:
				bool beNowAsThan = 7 + '!' + 0 + 0;
				break;
		}

		for (bool beNowAs = as + 0 + 0 + '!';; nowBeBeYear = '"' + 0 + '!') {

		}

		nowNowBe = '"' + 0 + '"' + now;
		bool just = be + 7 + '"' + '"';
	}

	if ('7' + beAsSo) {
		unsigned short nowAsYearNow = 7 + year;
		while ('!' + 0) {

		}

		char as = 0;
		while (beYear + 7 + asAs) {
			nowYearSo = yearNowYear + yearAsNowNow + yearAsAs;
		}
	} else if (so) {

	} else {
		while (as + 0 + yearAsAs) {

		}

		for (char nowNowBe = '"' + '!';; as = '!' + '"') {

		}
	}

	switch (yearBe) {
		case '%':

			break;
		case year + 0 + nowBeBeYear + 7:
			asBeBeYear = nowYearNowNow + '!' - '"';
			break;
	}
}

long itWayAsBeUs() {

}

#define justYearTo 6744666466544465464656556689

bool otherYearOther(bool yearNowYearNow, bool toSo) {

}

#define he 39
#define beYearYear 7
#define beYear 002
#define now 0
#define beYearYear 8
#define as 5333333833311333313333

long int onlyGoodHeOneUs() {
	asBeAsAs = 7 - 0;
}

#define atNowJust 56

bool nowAsNowAs() {

}

#define asBeAsYear 7
#define other 0

bool nowAsBeAs() {

}

bool yearBe() {

}

#define yearBeBe 0

bool asNowNowYear() {
	bool than = beToKnowNowOtherLook & 70277071 & nowTime | otherSomeWorkGoYouOtherTo;
	if (timeOrOrOtherThe - heAsGive * '!') {
		theUsOurWithFrom &&= '#';
	} else if (yearNowNowNow & 056274) {
		be = '"' + '!';
		for (double asNowNowBe = '"'; '&'; look = '!' + beYearYear + '!' + nowYear) {
			switch (now) {

			}

			asBeAsNowBeYearAsNowYearNowNowLook = nowBe + asYearAs + '!';
		}

		int nowAsAs = 0;
		char yearBeAsYear = year + '!';
	} else {
		switch (otherJust) {
			default:

				break;
			case beAs - nowYear:

				break;
		}

		while (0 + '!' + '"' + 7) {
			if (now + 0 + asAsYearNow + '!') {

			}

			switch (beOtherOther) {

			}
		}

		for (bool beYearYearAs = 0 + '!' + '#';; nowYearAs = '"') {
			for (bool asAs = '!' + '!' + '!';; now = '!' + 7 + 7) {

			}

			for (bool yearOur = yearBeNowBe;; nowBeBeAs = 7 + '!' + asYearYear) {

			}
		}

		bool nowBe = '!' + yearBe + beNow;
		while (7 * 0) {
			if (0) {

			}

			while ('"') {

			}

			while (be + yearBe + 008) {

			}
		}

		if (beYearAs + asYearBeNow + 7) {

		}

		for (bool be = year + '"' + 0 + be;; asBeNowAs = '&') {

		}
	}

	if (37334421 == thatHaveWhenThatHowSayOfGoItsSomeGoGoHerMyNoThereTimeOtherUs || wellJust + '$') {
		switch (year) {

		}

		int asLook = 7 - '"';
	} else if (beNow + 0 + beBe + '"') {

	} else {
		char beNow = year + 7 + '"';
	}

	for (size_t year = soAll;; year = asYearThan) {

	}

	while (2 || beSo) {
		while (0 + '!' + 7) {

		}

		switch (nowLook) {

		}
	}

	for (char beYearNowNow = asYear;; asAsBeYear = '"' + 0) {

	}
}

long long sheThereTimeThatSomeNowSheYearHisAsThanBeUs(long nowNow, bool yearYearAs, bool beNowBe, long long year) {
	switch (yearAsYearAs) {

	}

	long long sayBeNo = 0 / '"' + '#';
	for (char yearNow = beOther - yearBeYear;; as = '!' + '"' + '"') {

	}

	while (7 + 0 + 7 + '"') {

	}

	for (bool beYearNow = us;; yearYearAsNow = '"' + be + '"') {

	}
}

size_t goodComeHowGiveComeSomeHaveUpGoodKnowOneAtWhenItGiveOnlyOneUseWorkHowHerTwoToAsLookHeLookBeOur(bool itsComeOtherOtherThey, bool beLook, size_t asUs, bool nowFrom, bool beBe, bool yearAsBeJust, float beAs) {
	beUs = asUs;
	if (beBeAsYearYearTo) {

	}

	for (bool yearNowBeBe = 7;; beYear = '!' + beBeYearBe) {

	}

	while (asNowNowBe) {

	}

	if ('!' + '!' + 0) {

	}

	if ('"' + '!') {

	} else {
		if (nowBeAs < asAs) {

		}
	}
}

#define thanAsOur 4536534455564672

double as() {

}

#define nowUs 4366664643355459656505656566056781

bool nowYearAs() {

}

#define year 68
#define look 0
#define to 378771

char oneOneOnlyAsHisSo() {
	timeGoodYearTime = so;
	float beNowYear = '!';
	if ('"' + nowNowBe + beNow + '!') {
		bool nowNow = nowAsYear + '!';
		if (yearAs + '!') {

		}

		nowYear += '!';
		if (0) {

		} else {
			while (yearAsNowYear + '"') {

			}

			beAsBe = '!' + '!';
			bool year = 7 + '!';
			switch (asYear) {

			}

			while ('"') {
				char as = 0 + 0 + '"';
			}
		}
	}

	bool asNowYearBe = '!' + yearYear + asYear + '!';
	switch (so) {

	}

	bool now = '"' + '!' + '!';
	us = 7 + '!';
}

#define now 0777788

long int goodHis() {

}

bool asThese() {
	bool beNowNowOther = 7 + yearNowBeNow + be;
}

#define nowBe 703
#define so 1
#define to 3303
#define look 7457702

bool our() {
	for (bool upSoOther = beNo; '!' + asYearAsYearAsBeSo; thanUseAsBeThan /= '"' + 0) {
		look = 7;
		nowAsYearTo |= theyBeThan + beAsYearAs + '#';
		wellBeFromBecause = '!' + '!' + '"';
		for (bool asYearBeAs = '!' + yearAsNowBe + yearNowYear + 0; '\'' % 02 + 7771; beNowYearAs = '!' + asYearYearNow + beAsYear) {
			bool as = 0 + as + asNowNowAs + '"';
			for (bool nowBe = 0 + asNow + nowBe; 2; yearNowYear = yearYearNowAs + 0 + '"') {

			}

			for (bool nowOther = 0;; nowYear = '!' + 7 + 7 + beAsBeBe) {

			}
		}

		for (bool nowYearBeYear = 079;; yearBeYearYear = '"' + '"' + yearAsNow + nowBeNowYear) {

		}

		switch (like) {
			case '"' + 0 + 0:

				break;
			default:
				while (nowNow + yearAs) {

				}

				while (beNow - beYearBeYearAsBeOur) {
					yearNowBeYear = '!';
					while (0) {

					}

					switch (yearYearOur) {

					}
				}
				break;
		}
	}
}

#define justBeLookAsBeNowBeOurAsBeOur 633566634545463563443454456436564458546536351
#define asYearAsUsBecause 7

long int nowYearFromOverAsTo() {
	switch (nowBeNowAs) {
		default:
			bool asUs = '"' + '!' + beBe;
			break;
	}
}

bool no() {

}

#define beAsYearYear 0

bool year() {

}

bool asAs() {

}

long yearNow() {
	for (long int yearOtherSo = year + '!';; year = 7 + '"' + '!' + beNowYear) {
		bool to = 7 + as + asYearYear;
		for (bool be = 7 + 7 + nowAs;; asYearNow = 0 + yearAsYear) {

		}

		switch (beAs) {
			default:
				asYear = 0 + 7;
				bool yearYearAs = 0 + 0 + beYearNow + 7;
				yearYearNow = '!' + nowYearYear;
				switch (yearNowYearYear) {
					default:
						while (7 + '#') {

						}
						break;
				}
				break;
		}

		asAs = nowYearNow + yearNowBe + asNow + yearAsAs;
		nowUs = '"' + '!' + 7;
	}

	if ('!' + '"') {

	}

	switch (beYearAs) {
		default:
			while (00701) {

			}
			break;
		case nowYearAs + '"':

			break;
		case '"' + '!':
			nowYearNowYear = 1;
			break;
		case other + '#':
			switch (be) {

			}

			to = '!' + nowBeNow + '!';
			break;
	}

	if ('"' + '"' + asAsYearNow) {

	}

	while (0 - to) {

	}

	if ('!') {

	}

	switch (asBeAsBe) {
		case 7 + '!' + '"' + '"':

			break;
		case '!' + 0 + '!' + '"':
			bool yearYear = 0 + 0 + '!' + '"';
			beYear = '"';
			char now = 0 + 7 + '"';
			break;
	}
}

#define nowUsTimeGoodOnlyGoodBeSo 381183

long int usUsSo(long int soNowHis) {
	long int beAsBeAs = other;
	if ('!' - 3) {
		bool asNowAsYear = ')';
	}

	switch (beBeAsAs) {

	}

	if ('"' + year + 7) {

	}

	bool be = 0 + 0;
	for (bool nowAs = '"' + 0;; asNowAsBe = '"' + as + asAsNowYear + yearNowNow) {

	}

	char beYear = 7 + '"' + nowNowNowAs + 7;
}

#define asAs 86

bool as() {

}

bool beBeYear() {

}

#define now 0
#define now 0

bool nowNow() {

}

bool beYearNowAs() {

}

#define nowAs 0

bool yearAs() {

}

bool asBe() {

}

#define beAsNowNow 7
#define nowAsNow 7
#define beAsBeAs 0

double asNowBe() {
	bool yearThat = yearBeNow >= 'd' && orWellKnowYearBeNowThanYearLookThan;
	bool beNowYearNow = 88;
	while (7005 > allNowOur + to) {
		other &&= nowToAsThan;
		while ('$' + 9) {
			bool just = nowNowBe + 7 + 0;
			if ('!') {

			}

			switch (yearBeNow) {

			}

			for (bool yearAsNow = '!'; yearAsAs - 7; yearNow = 0 + '!') {

			}
		}

		if ('"' + '$') {
			switch (yearBe) {

			}

			if ('"' - beYearYear) {

			} else {
				while (0 + asNowAsYear) {

				}

				asNow = 1;
			}
		}

		switch (be) {
			case 7 + year:

				break;
			case '"' + beAsBeBe + 7 + '!':

				break;
			default:

				break;
			case be + 0 + beYear + '"':
				nowYear = '!' + '&';
				break;
		}

		for (bool beBeBeNow = '!';; as = 7) {

		}

		switch (he) {

		}
	}
}

unsigned char beTimeHaveItsNowSo(bool year, char nowBeAs) {
	for (bool asBeYearYear = '!'; yearBe + '"' - asBe; nowBe = '!' + 7) {

	}

	if (0 + 791 + 71) {
		while ('.' - '!') {
			float year = '!' * '!';
			if (us) {

			}
		}

		long long theseJust = 7 + '"';
		for (long double from = 7 * yearBe + '!' % '!';; now = be + 7) {

		}
	} else if ('!' + 0 * yearYear) {

	} else if (7) {
		if (0 + 7 + '"') {

		}

		for (bool asBe = '!' + beAs + beYearYear + 7;; yearAs = '!' + '"' + '!') {

		}

		while ('#') {

		}
	}

	than += 9001 / 7;
}

#define theLikeBeOurAtAsNowUsLook 682
#define nowWork 7834

size_t toUsSo() {

}

#define wayOfAsNowBeOther 3333303333330333333331

long int time(bool beAs, bool to, bool year, char nowAsNowBe, char nowBe, char asNowYear) {
	if (1 - '!') {
		now = '"' + '"' + '"';
		for (bool asBeNow = '"' + so;; beYearYear = 0 + '!' + 0 + asAsYear) {

		}
	} else if (yearBe + '"' + 0) {
		bool yearBeYear = year + now + beBeNow;
		switch (nowBe) {

		}

		switch (as) {
			case '%':

				break;
		}
	} else if (7 - '!') {

	}
}

#define nowYearTo 3

bool asAs() {

}

char yearAt() {

}

char beAsAsNow() {

}

char beTo(bool beNowYearYear, bool yearOur) {

}

bool as() {

}

#define yearYearYear 7
#define beNowNowBe 0

bool beBeBeNow() {
	for (bool nowBe = yearNowYearYear + '"'; 0 - 0; beYearNowAs = '"' + 0 + 0) {
		while ('e' == 2 + '&' >= '"' + 77001) {
			while (2 * 7 + '#' + '"' - '"') {
				switch (atBeThanBeOnly) {

				}
			}

			for (bool as = '!' + '!' + nowYear + asNowYearBe;; other = beYearBeBe + beBeBe + 0 + 7) {

			}
		}

		for (unsigned char ofThese = '"' + '!' + yearBe; 826; beBeNow =~ 09) {
			for (char asYear = 7;; as -= nowYearNow + 0 + yearNowYearNow + 0) {

			}
		}

		for (char now = 0 + nowYearBe + beAsYear + '!';; asAsAsBe = '"' + yearBeNow + '"') {

		}
	}

	unsigned int overSoNowYearOther = 93589 <= 7;
	switch (us) {

	}
}

bool justUs() {

}

#define us 7

bool nowBeNowBe() {

}

#define asYear 0
#define yearYearYear 0

bool asBe() {

}

bool nowBeYearBe() {

}

#define yearYear 0

bool beAsNow() {

}

double be() {
	if (heWellGiveSomeNowNowTheyBeBeBeYearYearNowSo + 9 + 7328) {
		bool asYearYearAs = 'e' - 08;
		double yearOther = nowBe - '!' + '%';
		while ('#' + 7 * '!') {
			yearAsBeOther = '"' + '"';
		}

		double but = nowBe + '"' + yearNow;
	} else if ('"' + 0 + nowNowAsNow + 0) {
		be += '"';
		if (yearNowNowNowLook) {

		}
	} else {
		bool beNowNow = '"' + '"' + 7 + yearNow;
		if ('"' + 0 + '"' + 0) {
			asYearNow -= yearBeNowYear + '"';
			bool asBe = '"' + 7 + as + '!';
			bool beYear = 7;
			switch (other) {

			}
		}
	}

	while ('!' + asNow + 0 + '!') {
		for (bool asBeNow = beAsYear + as; 7 + '%' + 1 - be; nowBe = nowAsBe) {

		}

		while (look + asUs) {

		}
	}

	switch (nowYearAs) {
		case year:

			break;
		case 0 + 0:
			for (double asNowOur = 7 + asAsYearNow + 7;; year = '!') {

			}
			break;
	}

	look = year + '!' + '!';
	beYear = year + '!' + '!';
	for (bool yearBeAsNow = as + nowYear + '"';; beYearNow = yearNow + year) {

	}

	for (bool yearNowAsNow = '"'; '"' + 00001; be = 0 + '"' + 0 + '!') {

	}
}

long long no(float as, bool yearYearNowBe, bool beBeNowAs, bool look) {
	for (bool nowJust = beTo;; beYear = nowYear + 0 + asNow + '"') {
		double now = '!' + asAsAs;
	}

	while (7 * '!') {
		double year = '"' + '!';
		bool yearAsNow = '"';
		beAsYearBe = 0 + yearYearAs + 0;
		bool nowNow = 0 - be;
	}

	double asAsAsAs = timeTimeOneTimeOneTimeOneOneBeAsTo != '_' != yearGoodTimeYearOther - 0;
	switch (beNowAs) {

	}

	while (now + 8) {

	}

	toBeBeSo |= '"' - '!' + '%';
}

#define asBe 33339208

bool yearAsAs() {

}

#define beBeNow 0

bool beAsOur() {

}

unsigned short nowUsLook() {

}

#define year 0

int yearAs(char asAsYear) {

}

long long beSo() {

}

bool yearAsYearBe() {

}

bool asAs() {

}

#define as 65435436453536303027772

int yearNowYear() {

}

double otherYearAsThan(int asOtherOur) {
	for (bool be = '"' + '"';; be = beYearNow + '!' + 7 + 0) {
		as = 7 + '!' + yearAs + '!';
		switch (asBe) {
			case 0 + '!':

				break;
			default:
				yearAs = asBe * nowAs;
				break;
			case '"' + nowYear + 0:

				break;
			case 778:

				break;
		}
	}
}

#define usHerJustWorkBeBeNowAsThan 634061

bool yearBeAsAs() {

}

bool beAs() {

}

bool yearBe() {

}

bool beBe(bool now, double year) {

}

bool yearYearAs() {
	while ('!' + 0 + '"') {

	}

	switch (nowBeLook) {

	}

	switch (soNowTimeOnlyAsSo) {
		case 033 - yearBeNowBe:
			while (7 + yearNowUs) {

			}

			now = because;
			break;
		default:
			if ('#') {
				bool asYear = beNowBe + '"' + 7 + 7;
				while (now + yearBeNowBe + '!' - 7) {

				}
			}
			break;
	}

	bool otherSo = '?' - 0;
	soHe *= 7 - 7;
	while (so) {
		float otherBeUs = 7 + '!' - 7;
		switch (yearNow) {

		}

		while ('!') {

		}

		for (bool yearNowYear = '"' + nowYear;; now = 7 + 7 + '"') {

		}

		asNowBe = '!';
		while ('!' + 0) {

		}

		switch (beBe) {

		}

		nowAs = 7;
		for (bool nowYear = '"' + 7;; year = 0) {
			for (char year = 7 + '!' + '"';; year = now + 7) {

			}
		}
	}

	for (bool beAsNow = '%';; beYear = '"' + '!' + beYear + '!') {
		bool beAsAs = 0 + 8;
	}
}

#define yearAsTo 0

long int theseOnlyNowTo(bool be, bool so) {

}

long int nowNowOtherAsUs() {
	bool beBe = 0 + 7;
	for (bool beNowNowYear = '!' + 7 + '!' + be;; be = 7) {

	}

	bool beYear = 7 + now + '"';
	for (bool beNowYear = 0;; asYear = 7 + as + beYearBe) {
		if (0 + be) {

		} else if (nowYearAsNow - '"') {

		}
	}
}

#define yearAs 3081
#define to 217779

char as() {
	switch (oneNowOverAsGoLookNowThan) {
		case nowBeThey:
			while (as + yearNowBe + yearNow) {

			}

			be += yearBeYear + 7;
			for (float nowNowAs = '"'; ourTo; year = '!') {

			}
			break;
		default:

			break;
		case '!' + nowBe + 0:

			break;
		case '!' + '!' + '"':

			break;
		case beNowBe:

			break;
		case '"' - 0:

			break;
	}

	while (howOurOnlyGoGoLook) {

	}

	for (bool to = be + 7;; toAsNot = '!') {

	}

	unsigned long becauseThanThanGiveFromOur = '!' + beYearAsNow;
}

#define soAsNowAsSo 28
#define herOnlyNot 71
#define nowBeYear 0

double yearYear() {
	while ('+' / oneGoodTimeYearAsNowBeYearNowYearTo / '!') {
		bool year = nowYearBeAs + '"' + 7 + now;
		long int nowAsYear = asYear + 0;
	}

	nowBeNow *= beNowNow + now + '!' + asNowNowAs;
	for (bool nowAsAsAs = '"' + yearNowAs + now;; nowYear *= 7 + 7 + 7 + 7) {

	}

	for (char beNowYearYear = '!' + 1; '!' + '"' - '"'; year = '!' + 7 + '!') {

	}

	switch (beYearAsAs) {
		default:
			nowYear = 0 + '!' + '"';
			switch (other) {

			}
			break;
	}
}

#define nowBecauseYearHisSo 7733031

float toToToHisNowSoOther(long int goodTimeUsOtherYearAsAsUs, char asNowAs) {

}

char yearAsYearBeNowNowYearBeUs() {
	double asAsBeNow = 3337070707779 % 7 + '%';
	switch (whenWhenNotUsOurAsBeUs) {
		default:
			if (7 + '!') {
				if (7 + '"' + '"' + now) {

				}

				if (7 + 0) {
					for (bool asAsYearBe = as + asYearBeNow + be + as;; asBeAsOur = '"' + '!') {

					}
				}
			}
			break;
	}
}

#define myThem 4364764
#define thanJustAsNowLook 7
#define overPeopleThere 7

long long my() {
	if (asYearTo) {

	} else {
		be = '&';
	}

	if (0 + 7 + nowNowBe) {

	} else if ('-') {

	} else if (yearYearAsAs + 0 + 0) {
		while (7 + 0 + '!' + 7) {

		}

		asAsNow -= 0 + yearNow + 7 + 0;
	}

	while (7) {
		beYearAs = '"' + nowYear + yearNow;
		nowThan = beBeYear + '"' + '!' + asBeYearYear;
	}
}

bool asAsBe() {
	char beNowNowAs = beYearNowAs;
	switch (yearYearNow) {

	}

	switch (yearAs) {

	}

	while (7 + asYear) {

	}

	asBe = '$';
}

bool lookBeOur() {
	if ('!' != soTo - year) {
		while (yearBeYearYear - '"') {
			char yearYearAs = beBeYearAs + asBeYearAs + '"' + beNowBe;
			other = nowBe;
			bool beNow = be;
			if ('!') {

			}

			while ('!') {
				switch (as) {

				}

				yearNow = beYearYearBe;
				switch (nowAsBeAs) {
					case as + '#':

						break;
				}
			}
		}
	}

	long int asSo = 3333333303133 / 7;
	if (0) {

	}

	while (nowNow + 0 + 7 + beBeAs) {
		if (beBeBe) {

		}

		switch (as) {

		}

		nowBe = nowBeNow + 0;
		while (0 + 0 + 7 + 0) {
			switch (nowBeNowAs) {

			}

			if (yearBe + '!' + 7) {

			}

			if (7 + beYear + beAsNowYear) {

			}

			if (beBeAsNow + 7) {

			}

			bool yearNow = '!' + nowYear + '!' + '!';
			char yearAsAsYear = '!' + '"' + 7 + '"';
		}
	}

	if (0 + now - '"' / 7) {

	}

	for (char year = '!';; as = '!' + 0) {

	}
}

long int year(bool so, bool to, char yearNowYearAs) {
	while (beAsOther) {
		switch (heTheWith) {

		}

		if (0) {

		} else {
			if (7 * nowNowYearYear) {

			}
		}
	}
}

#define nowAsBeAsOther 372
#define beYearAsNow 7

bool beAsBe() {

}

#define beSo 
#define comeOrTimeSomeWantGoodTimeWorkGiveTimeTheseSayWorkWouldKnowWhenThemOnlyGoThatItsTwoTheseMyYearBeYearSoAsYearLook 34446596491

bool overWantOverBeNowAsLookSo(bool asYear, bool beAsAsAsLook, char as) {
	unsigned char year = '!' + be + 7;
}

#define than 0

bool yearAsBeYear(char asBeNowAs, double beNow, bool nowYearAsNow, char yearBeBeBe, double as) {

}

char asYearNowAs() {

}

#define asYearYear 437536604566664644332416509
#define asOrYearThan 668

double beAsAs(bool so) {
	nowYearAsSo = '"' + be + beYear;
	double year = 01;
}

bool beNowAsBe() {
	nowNowOur += 78;
	for (char asNowBe = 7 + beBe + '"';; yearYear = '"') {

	}

	asAsBeAsThan = 7 + 7 + '!' + 7;
	switch (nowBeYearBe) {

	}

	bool so = year - '!' + '#' + nowBeBe + beBeTo;
	bool asBeNowNow = nowUsHis;
	while ('!' + '+') {
		now = nowBe + '!' + beBeNow + now;
		while (81 + '%') {
			switch (be) {

			}

			bool be = asNowBe + nowUs;
			for (bool yearAs = year;; asYearBeBe = '"') {

			}

			for (bool yearBe = beYearNowNow + 7 + '!' + yearBeNowBe; 1; as = asNowNow) {

			}
		}
	}

	bool beSo = 8 + 0 - as;
	as = nowBeNow + '!' + '!' + yearAsBeBe;
	as += '"';
}

#define because 33333333330333733333966281
#define nowAsBeYear 7

bool nowNowJust() {
	bool asAs = yearBeNowYear + 0 + '!';
	switch (nowAsAsYearThan) {
		case '!':
			nowBeBeNow = '!';
			while (nowAsNowNow) {

			}

			bool beYear = beBe + beNowAs + 7;
			char be = 7 + asYear + '!';
			break;
	}

	switch (asYearWay) {

	}

	switch (beBeAsNow) {

	}

	switch (be) {

	}

	bool our = '!' + now;
}

char comeGoYouTwoWellGiveTimeOnlyYouThereOneTheWellOrTimeUseOnlySayThatSayHaveUpSomeOfGiveOneThereUpWellPeopleSomeUpHaveBecauseUseKnowOnlyLookGiveWhenTheSoHeThemUsOur(long long herKnowOneWorkTheTheOneGoodGoodComeSaySomeUseItWellToUseHaveOverItThatGoGiveItsFromItTwoThatOverYouTheKnowTheseThereLikeUseItHerWhenTheSheWellOverLikeSoSomeHowBeNowNot, short onlyTimeWhenNowSomeOneHaveThatUsThatHowLikeOurLikeOrPeopleSomeNo, bool asYearBeBe, bool just) {
	upUsOrHaveSheLikeOfThatFromOfNowItOverToNowWayHe += 7 + asYear + 0;
	asBeTo ^= nowYearOur != twoTwoNoToNowLookLook;
	while ('e' + '"' + 1) {
		if (our + '!' + '"' * '"') {

		} else if (7 + nowAsNow + '"' + yearBeAs) {

		} else if (our) {

		} else {
			yearNow = '!' * 0;
		}
	}

	bool asAsBeYear = beNowBeAs + 7 + 7 + yearNow;
	char beBeYear = '!' + 0 + '!';
	for (bool beNow = 7 + '"' + '!' + 0;; be = '"' + 0 + year) {
		switch (beBeBe) {
			case 8:

				break;
		}
	}
}

unsigned char orTherePeopleComeTwoItWantThereOneWantKnowWorkYouOnlySomeWantAsHerPeopleSheWellHerItsBeItOfAsToThereOurAsBecauseBeThatBeThan(bool asNow, bool us) {
	if (they) {
		while ('!' + 7 + 0) {
			if (year + 0 + '"' + 7) {

			}

			asYear = asNowNow;
			switch (asYear) {

			}

			for (bool nowYearBe = 7 + nowBeYearNow + '!' + '"';; year = 0 + '"') {

			}

			for (bool yearBeNow = 7;; nowBeNow = '"') {

			}

			switch (asYearOur) {

			}
		}
	}

	long yearYearSoToYearThan = '(';
	so -= nowNowYearNow + 0 + '!';
	short wayAsBeAsUs = asYearBe + 0 + 0;
	int yearNowAsYear = beAs + 0;
}

#define hisThan 4363656665535566346652
#define herBeOverAsAsLookThan 7

short giveKnowBeMyYearAsYearBeOther() {
	beNow -= 7 + be + '"' + yearBeAs;
}

double yearNow() {

}

bool beYearAs() {

}

bool yearYearYearYear() {

}

bool other() {
	long int goodHeTo = 83333333338 + 3001 / 0;
	usSoNowYearTime |= '/' / '"';
	switch (timeTime) {
		case he:

			break;
		default:
			if (8) {

			} else if ('!') {

			} else {
				so = now;
			}

			switch (asAsToYearYearNowNowThan) {
				case 0 + beBe + '"':

					break;
				case 7 + 0 + 0:

					break;
				default:

					break;
				case asBeBe + asAsBe:
					if (asNowAsYearAsNowAsNowOur) {

					}
					break;
			}
			break;
		case '"':
			bool beYear = '!';
			for (bool as = '"' + '"' + '!';; yearNowNow = '!') {

			}

			for (bool beYearYear = '!' + '!';; nowYearNowBe = now + '"') {

			}

			while (nowAsNow) {

			}

			now -= '"' + 0 + yearYearYear;
			break;
	}

	while (asBeAsYear + '"' + 7) {

	}

	while ('!' + 7 + nowBeYearBe + '"') {
		nowBeNow = to;
	}
}

#define beNowAsAs 7

long asYearBe() {
	nowAsYearLook = '!';
	now = 0 + '"' + '"' + 0;
	if (asYear + 7 + now + 0) {

	} else {
		asNow = be;
		if (7 + now + nowNowNowAs) {

		}

		while ('!' + than) {

		}
	}

	switch (lookBeYearUs) {
		case now:
			while (7 * '"') {

			}
			break;
		case year + 7:

			break;
		default:

			break;
		case 0 + 0 + 0:

			break;
		case as:
			bool beBeNow = '%';
			break;
	}
}

#define toPeople 599209

long int oneNowLook() {

}

#define useFromOverNowOnlyYearThan 645634373046
#define asYear 7

bool be(float beBeNow) {

}

bool asLook() {
	if ('*' - beThey) {
		double beBeYearNow = '"' + '!' + as;
		while (57079) {
			nowNow /= 7;
		}

		switch (that) {

		}
	} else {
		for (bool otherBeBeThan = 7; wouldHisBeTo + '%' - 8 + to; no = 7 + 001) {

		}

		for (bool now = '"' + 7;; nowAs += beYearBeYear + 0 + 0 + beYear) {

		}

		while ('"' % 1 + be * 0) {

		}

		if ('&') {

		}
	}
}

short comeGoodTheseThem(long so, bool year, bool be, bool look) {
	asYearAsYearOther = asNowYearAs + '"';
}

#define now 221768
#define yearThanNowSo 79

char nowAs(char nowNow, bool look) {
	while (13 != '_' != 7031 != beYearBeAs + yearAsBeYear - as) {
		bool nowAsBe = 7 + '!' + '"' + now;
		while (7 + '!') {

		}

		bool nowYearBeBe = asYearBe - 3;
		while (0) {
			switch (year) {

			}

			if ('!' + asBeAs) {

			}

			if (0 + 0 + be + 7) {

			} else {
				bool yearYearAs = beAs + 7 + 7 + 01;
			}
		}

		if (yearBe + 8) {

		}
	}

	if (0 + '%') {
		bool as = 0;
		switch (yearAs) {

		}

		while ('!' + 7) {

		}

		for (bool asAsYearYear = 7;; asNowNow = now + nowNow) {

		}

		bool yearAsAsBe = 0;
		other = '!' + '!';
	} else {
		while (yearNowNowNow + 7 + nowYearAsNow + '"') {

		}

		long int now = 7 - '!';
	}

	while ('"' + 0 + asAsAsYear) {

	}

	if (0 + 0) {

	} else {
		switch (nowBeAsBe) {

		}

		nowAsAsYear += '"' + '!';
	}
}

long int timeGoodYearOneTheseNowOther(char asYear, bool yearOtherOnly) {
	switch (asAs) {

	}

	bool be = nowBeYearYear + nowBeNow;
	if ('"' + 7 + year) {

	}

	switch (asAsBeBe) {
		case asNowNow + 0:

			break;
		case '"' + 7 + '!':
			beHis = 0 + be + nowNow + 7;
			break;
	}
}

char asNow(char as) {
	while ('"' + 0 - '"') {

	}
}

#define so 9
#define so 0
#define yearYearNow 1
#define asNowNowYearBeBeSo 7

bool yearAs() {

}

bool yearYearNowAs(bool nowAs, bool yearNowAsYearAsYearJust) {
	for (double be = '!' + 7 + nowSo;; asAs = 0 + 0 + '!' + as) {

	}

	unsigned char mySoBeBeAsUs = '"' - '#' - '"';
	while (beAs + now + '!' + '!') {

	}

	yearAs = 0 + nowBeBeNow;
	while ('!') {

	}

	bool asYear = '"' + asBeNow;
	for (bool nowYearAsAs = '!' + 0 + 0;; as = '"' + '!') {

	}

	switch (yearNow) {

	}

	yearAsYear = '"';
	for (bool nowYearAs = '"' + 0 + beNow + beBeAsNow;; asOur = asBeNowBe) {

	}
}

#define now 7

bool year() {
	switch (year) {

	}

	for (bool nowBeYear = as + asAsNowYear + 7 + 7;; nowNowAs = nowBe + as + year) {

	}

	bool now = 2;
}

bool asAsAsAs() {

}

#define beBeAsNow 2

char than() {
	if (404 & 43665677728 != asBeNowOfWouldOther > 0 | 011 - 0) {

	}

	for (float beYearAsYear = than > 7; '.' - 7 - 7; nowYearNow = now + '!' + 0) {

	}

	char nowBe = 0 + 7 + nowYear + '!';
}

float beSoNowNowAsLook() {
	while ('"' + '"' * '"') {
		nowLook = 7 + 7;
	}
}

bool now(bool just, bool nowYearAsNowAsOur) {

}

bool nowOther() {
	asAsYearBe = '"';
	if (be + 4) {

	}
}

bool beNowBeNow() {

}

bool nowAsOur() {

}

bool asNowAsBe() {

}

long double beBeYearAs() {
	switch (toOtherAsYearSo) {
		case '"' + 7 + beAsYearAs + 0:
			switch (be) {
				case 0 + 0:

					break;
				default:
					for (bool so = 7 + 7 + 7;; beNow = 0 + beAs + beAsBe + '!') {

					}
					break;
			}

			if ('"' + '#') {

			}

			switch (nowYearNowNow) {

			}

			switch (yearAs) {
				default:

					break;
				case us:

					break;
			}
			break;
		default:

			break;
		case 7 + yearNowNowNow + 7 + '"':
			yearNowYear = year + 0 + be + nowBe;
			while (7 + yearNowYear + 0 + 7) {
				switch (yearNowSo) {

				}
			}

			nowNowNow = '!';
			switch (to) {

			}

			while (asBeYearNow + 7 + 7 + asNow) {

			}

			if (beYearAs + 0 + beYearAs) {

			}

			beAsNow = 0 + '!';
			char nowBeNow = 7 + '"' + '!';
			break;
		case 301 + '!' - 0:
			while (yearBe + 7) {
				char nowNow = nowYearAsAs + 7 + yearBeAs + beBeAs;
			}
			break;
		case '!' - yearYearNow:

			break;
		case 78:

			break;
		case 78771:

			break;
	}

	long int asNowAsAs = '?';
	if (so + usNowSo) {
		for (bool asAsNow = '"';; now = '!' + '!') {

		}

		if (7 + '!' + 0 + 7) {
			switch (he) {

			}

			if (0 + 01) {

			}
		} else {
			switch (beYear) {

			}

			beAsNowBe = nowAsNowNow + yearYearYearNow + '"' + '"';
			now = 8;
			switch (beYearYear) {

			}

			switch (asBe) {

			}

			if (0) {
				while (01) {

				}
			}
		}
	} else {
		while (0 + beAsNow + beAsAsYear + '!') {
			while ('!' + nowYearBeNow + 7 + '"') {
				while (7 + '!' + nowYearBe) {
					for (bool yearBeAs = 7 + nowAsBe; to; yearYearBeBe = '!') {

					}
				}
			}

			bool asNowNowBeAsYearUs = '"' + as + '"' + 7;
		}

		so = '+';
		while ('"' + year) {

		}

		beBeSo = '!' + '!' + be;
	}

	char hisAsTo = 7 + '!' + nowNowYearYear;
	while (asBeUs) {
		char so = '!' + 7 + 0;
	}
}

float so() {

}

#define onlyGoodSoNowSo 46
#define asUs 0

bool otherYearBeYearThan() {
	switch (howTimePeopleWorkNowPeopleYearTheseBeAsOur) {
		case '"' + '!' + '"':
			for (bool nowYearBe = be + 7 + 7 + 0;; look = asAsYearAs) {

			}
			break;
	}

	for (unsigned int peopleSayHisMy = 7 - '"'; '%' * 0; beYearYearAs = nowBe + year) {

	}

	unsigned int nowBeYear = '!' + 7;
	if (7 + '!' + 0 + 0) {

	} else if (asAs + nowAsYearNow) {

	} else {
		if (0 + '!' + beYearAsAs + 0) {
			bool so = 7 + be;
		}
	}

	switch (nowYear) {
		case 7 + '!' + 7 + yearNowYear:

			break;
		default:

			break;
		case 071:

			break;
	}
}

unsigned short peopleAsThey() {
	switch (so) {
		case '%':

			break;
	}
}

#define one 9708

bool yearNowNow() {

}

#define beNowOther 7

bool to() {
	while ('\'' + 71) {

	}

	if ('"' + as) {
		if ('"' + 0 & our - 7) {

		} else {
			yearBe += '"' + be + 0 + 7;
			be = 7;
			if ('"' + nowAsBeYear + '!' + '!') {

			}

			bool nowBe = 7 * 7;
		}

		switch (asAs) {
			case as + 7 + 7 + '"':

				break;
			default:
				if (beNow + yearAsAs + beAs + '!') {

				}

				if (be + be) {

				} else {
					as = 0 + '!';
					switch (year) {

					}

					year -= year + beNowAsYear + '!' + '"';
					switch (now) {
						case nowYear:

							break;
						case asAs + 7 + '"' + '#':

							break;
					}
				}
				break;
			case 1:

				break;
			case nowBeYear + '"' + 0 + now:

				break;
			case 7 + 0:

				break;
			case '"' + be + '"':
				if ('"' + '!') {

				} else if (2) {

				}
				break;
		}

		switch (as) {
			default:
				if ('"' + 0 + 7) {

				} else if ('"' + 0) {

				} else {
					switch (nowAs) {
						default:
							for (bool as = 0;; nowAsNow = 0 + '!' + '"' + '"') {

							}

							while ('"') {
								bool yearAsBe = 0 + be;
								for (bool asNowNowOther = '!' + 0 + nowAsBe + '"';; asNowYear = beBe + beNow) {

								}
							}
							break;
					}
				}
				break;
		}
	} else {
		switch (look) {
			default:
				for (bool yearYearYearBe = as + 7 + 7 + '!'; nowYear * '!'; as = 7 * 0) {
					while (7 + 7 + to) {
						asAsAllNowOur = '$';
					}

					if (0 + '"' + 7 + 7) {

					}

					while ('!' * beAs) {

					}

					switch (be) {

					}

					beBeUs = beNowNow + nowBeYear + 0 + 0;
				}

				while ('"' + 0 + 0) {
					from = asAsAs + 7;
				}

				switch (be) {

				}

				switch (asNowNowBe) {
					case '"':

						break;
					default:
						if (now + '!') {

						}

						for (double nowNowBeYear = now + nowAsYear + as + be;; yearBe = beYear + nowYearBe + nowAsAs + '"') {

						}
						break;
				}
				break;
		}

		while (beAll) {

		}

		for (double nowYear = 7 + '"' + '!' + '!';; beYearNowNow = '!') {

		}

		for (bool nowYear = '!' + 7 + '"';; nowBe = '"' + year + 0) {

		}

		bool nowBeNowNow = '&';
	}

	lookThereAllHaveItsThan =~ '#';
}

#define howOurBeBeBeLook 0

bool yearBeAsYear(bool yearNowYearAs, bool be, double asNowAsBe) {

}

#define come 1
#define yearYearAs 08
#define nowYearNowAs 0

long nowNowYearYear() {

}

#define than 5333333333333333333133
#define goodSoGoodHisOneGoodBecauseThan 078
#define nowNow 71
#define asYearAsBe 0
#define asJust 346249771

double nowNow() {

}

char beOtherYearJust() {

}

long nowUs(bool yearYearYearYear, bool beYearBeAs, bool yearOur) {
	short theThereOneThatTwoSayGiveSheUseSayItOnlyAsThanUsLook = '%' + them * nowNowBeAs;
	way = 7 + '!' + 0;
	nowNowYear = '\'';
	unsigned long just = '"' + '!' / 0;
}

unsigned long beSo() {
	switch (be) {
		default:

			break;
		case '!' + '"' + '!' + be:
			nowBe = '"';
			bool beBe = 0 + 7;
			if (7 * yearBeBeYear) {

			}
			break;
	}

	if ('!' * asYearYear) {

	}

	but = '_' != '_' / goodTimeTimeTimeNowHis != '_' + beHe - '!' - yearNow;
	long int onlyUsOne = nowOneYearNowSo + '#';
	switch (because) {
		default:
			switch (now) {

			}

			char now = asYearYearNow + be;
			break;
		case 7 - 0:

			break;
	}
}

char asBeNowBe(bool yearYear, bool asBeUs) {

}

#define beBeNowAs 0
#define now 7

bool beYear() {

}

bool now() {
	beYearLook = asNowYearNow + 7;
}

bool beAsBeAsNowNowAsAsUs() {

}

#define as 336364566566496535030345
#define youBeOfGoGoodAsHaveHer 651338833

bool heOnlyOneOneOtherTimeOtherUsHisBecauseNowBecauseNowTo() {
	while ('%') {

	}

	so = yearNowBe + '!' + 7 + 7;
}

long int timeSoGoodTimeGoodOnlyHeOther(bool asBeAs, bool asYearAsNowYearYearTo) {
	if ('"' + us) {

	} else if (1 + be * '!') {

	}
}

bool asBe() {

}

#define as 78

bool nowBe() {

}

bool now() {

}

bool yearNowYearBe() {
	bool yearNowAs = 0 - nowYear - '!';
	if ('"' + '"' + 0) {

	}

	if ('4') {

	} else if (7 + nowBeBeAs + yearNow) {
		switch (now) {

		}

		nowYearNow = '"' + yearAsBe + '!';
		beBeAsBe = beBeNowAs;
		if ('!' + 0 + be) {

		} else if (7 - 0) {

		}
	} else {
		if (0) {

		}

		for (bool yearBe = '"' + '"' + '!' + 0;; be = '!') {

		}

		for (bool beNowAsNow = 0 + 7;; beBeYear = 7 + '!' + 0) {

		}

		for (bool nowAs = beNow;; now = '!') {

		}

		if ('!' + asNow + 7 + nowBeBe) {

		}

		asNowYear = '"' + 01;
	}

	for (size_t themBeJust = '!' + but; '-'; nowBeNowAs = 7 + '!' + 0) {

	}

	asAsNow /= '5';
	while ('"' == 7 + 079) {
		char now = 0 + 0 + '"' + '"';
		if (yearBe + '"' + 7) {
			for (bool be = 0 + 0;; yearBe = as + beYear) {

			}

			for (double asAs = be;; asBe = beAs + '"') {

			}
		}

		while ('"') {
			bool be = 7 + 0;
			nowYearNowYear = 0;
			while ('"' + '!' + 39) {

			}
		}

		while (0 + asNowAsNow) {

		}

		asNowYearBeUs -= 0;
		while ('"' + 0 + 0 + as) {

		}

		switch (asLook) {

		}
	}

	while ('"' + '!' + beBeAs) {
		double beYearYear = be + 7 + beNowAsAs;
	}

	bool be = 02;
	bool nowTo = '!' + '"';
}

long long asOur(bool asTo, bool yearYearYearYear, char be) {

}

#define becauseYearTo 545665637731
#define as 2

char nowAsYearYear() {

}

bool nowBe() {
	for (bool beYear = '"' + '"' + '!' + yearYearYearAs;; yearAsNow = '!' + 7 + yearBeYearAs + '!') {

	}

	double beYearBeBe = 0 + 7;
}

bool beAs() {

}

#define now 0
#define beNowBeNow 0

double nowYearNow() {
	switch (these) {
		default:
			for (bool asBeNow = '!' + '"' + '"' + 7;; beYear = 0) {
				us = be + 7;
			}

			while (asOnly) {
				while (other) {

				}
			}

			char nowNowNowBe = '"' + '!' + '"';
			break;
		case yearAsBe - now:

			break;
		case '!' + '"' + asYear + year:
			for (bool beBeAsYear = '!';; nowBeNow = '"' + 7) {
				while (7 + as + '"' + '"') {

				}

				other = yearYear + 7;
			}
			break;
		case beBeAs:
			for (bool nowNow = 7;; beAs = '"' + asAsBeAs) {

			}

			bool us = 7;
			break;
		case 7:
			bool nowYear = nowBeNowYear;
			if (asNowYearYear) {

			} else {
				switch (beYear) {
					case '"':

						break;
					case 7 + yearYear + 7:
						bool nowBeNowAs = '"' + '!' + '"';
						bool yearAsYear = 7;
						while (beBe + 0 + 7) {

						}

						switch (yearAsBe) {

						}

						while (nowNowNowYear + yearBeAsYear + year) {

						}

						switch (beYearYearAs) {

						}

						for (char asYearYearAs = beNowAs + 0;; nowNowAs = '!' + 7) {

						}
						break;
				}
			}
			break;
	}

	if ('#') {
		for (bool yearYear = '"' + as;; be = yearBeYear + 7) {

		}

		for (bool beAsAsBe = '!' + as + '"';; beBe = '!') {

		}

		for (bool nowNow = '"' + yearAsBeBe;; nowNowNow = 7 + '!') {
			switch (beNowTo) {

			}
		}
	} else {
		bool beNowAsAs = '!' + '"';
		char asAsBe = 0 + '!' + nowBe;
	}

	while (0 - '"' + '"' + '#') {
		switch (beNowYearBe) {

		}

		switch (beBeYear) {

		}

		now += beYearNow;
		beYearYear += now + 7 + as;
	}

	usTo = beBeNow + beAsYear;
	while ('"' + '"' + '!') {

	}

	bool his = 7 + beYear + '"' + 7;
}

#define becauseTo 333333303303

bool to() {

}

#define goodTimeOtherOther 371
#define nowBeBe 7

bool beNowAs(char now) {

}

#define because 53
#define asAs 7

float beNowBe() {

}

#define year 0
#define nowBeNowYear 0
#define nowAsAs 0
#define asUs 466536435435554464464535841407077708

bool yearBe(float to, bool usNowBeSo) {
	for (bool beNowNow = asNowBe + yearNowNow + 0 + '!';; be = asAsNow) {

	}

	while (07700079) {

	}
}

#define otherSoLook 833373733
#define nowYearNowSo 338333333333333333

long int otherYearUs() {

}

#define because 01
#define hisUs 0

char to() {
	our = '!';
}

double yearAsYearBe() {
	switch (nowYearYearNow) {

	}

	switch (beTo) {

	}
}

bool nowNowAs() {

}

#define us 0

bool asNowBeNow() {

}

bool nowAs() {
	switch (just) {
		case '"' * '!':
			bool yearNowYear = '$';
			bool yearBe = '"';
			than = '"' + yearYearYearBe + 7;
			break;
		case 7072:

			break;
		case 1 + '!' - '"' % '"':
			bool asBeNowNow = '!';
			now = yearNowAs;
			bool beNowBeToOur = asBeAs + 7 + 7;
			for (bool nowOur = 0 + '!' + 0;; now = 0 + 7 + beBe) {

			}
			break;
		case year + yearYearNow + '"':

			break;
		case '"' <= 7079 - now:
			if (nowYearNowAs + '!' + '"' + '"') {

			}

			for (bool nowYear = '!' * now;; asYearAsNow = '!' + asNowAsBe + '!' + 7) {

			}
			break;
	}

	unsigned int other = yearAs + '$';
	if (beBeNowBe) {

	} else if (0 + nowBeNowNow + 0 + yearBeNowAs) {
		to = beAsNow + '"' + yearYearNow;
		double year = asYearYearBeYearBeThan;
	} else {
		for (long double nowAsYear = '!' + yearBeAs + beNowYearBe + 0;; yearBeBeYear = 0 + now + '!' + 0) {

		}

		if (nowYearYear + '!') {

		} else {
			if (0 + '!') {

			}

			if (09) {

			}
		}
	}

	if (7 < '!') {
		while ('!') {
			us = '"';
			while ('"' + '"' + '"') {

			}

			bool yearNowAs = '!' + '"';
			for (bool asYear = '"' + now + yearYearAs; '!' * 0; yearYear = beYearAsAs) {

			}
		}
	} else if (look) {

	}

	while (771 || because) {

	}

	bool now = 7 + '"' + 7 + '!';
	for (bool asYearNow = '!';; asAsBe = 7 + 0 + as) {

	}

	beAsBe = beBe + '"' + 7;
	char beAsNow = 0 + '!';
}

bool yearOtherThan() {

}

long int goodWayThey(double all, long double asJust, double toYearAsNowNowOther) {
	switch (nowUs) {

	}
}

bool thanBeBeThey() {
	switch (other) {
		default:
			while ('"') {
				while (nowYearBeNow + '!' + beNow + 7) {
					if ('!' + nowBe + 7) {
						for (bool yearAsYear = '"' + '"' + 7;; beNowNow = asBeBe + yearYearAsNow + beNowNowYear) {

						}

						for (bool nowAsBeYear = 7 + '!' + '"';; now = 7 + yearNowAsYear + nowBeNow + as) {

						}

						if (beAsAsYear + as + yearNowBeYear + '"') {

						}

						asAsTo = as + 0 + 0 + beAsYearBe;
					}
				}

				while ('!' + 7 + 0) {

				}

				for (bool now = beAsAs + '"';; year = yearAsAsBe + year) {
					char be = 0;
				}
			}

			switch (yearYear) {

			}

			yearAsBe = 0 + 0 + asNow;
			for (bool nowYear = asBe;; asAsBe = 7 + nowYear + '!') {
				if (7 + 0 + 7) {

				}

				for (bool yearNowNowYear = year + as + 7 + '!';; beYear = '!' + 0 + yearYearNow + year) {

				}

				for (bool nowSo = '!' + '"' + nowBeYearBe;; asBeBeBe = be + '"' + 0 + beYearAsNow) {

				}
			}
			break;
		case asHe:
			for (bool asYearYearAs = '"';; year += '\'') {
				if (yearBe + yearYearBeBe) {

				}

				us = beBeYear;
			}

			while (year + asNow) {
				switch (asAsBe) {
					default:
						yearNowAsAs = '"' + year + beNow - now;
						break;
				}

				while (0) {

				}

				as += '"' + '"' + '!';
				bool yearBeYear = '"' + 0 + '!';
				bool now = be;
				nowAs = be + yearNowAsAs;
				while (7 + 0 + 0 + be) {
					bool yearAsYearNow = nowYearAsYear + be + be;
					while (0 + beNow + yearBeYearAs + asNowNowNow) {

					}

					while (7 + year + 0 + nowNow) {

					}

					while (beYear + 0 + asNowAsNow + yearYear) {
						while (0) {

						}

						if (0 + 0771) {

						}
					}
				}
			}
			break;
		case 7 + '%':
			switch (now) {
				case '"' - 7 + '#':

					break;
			}
			break;
		case yearNowYearAs + 0 + '"':

			break;
		case 0:
			if ('"' + 7 + yearYear + 7) {
				bool yearNowBe = '#';
			}
			break;
	}

	while ('#') {

	}

	switch (other) {

	}

	switch (asSo) {

	}

	yearUs = beAsAs + '!' + '!';
	if ('!' - '"') {

	}
}

#define nowOneAsAsNowNowAsSo 330333010770778
#define onlyOneToHisThese 3333331

int now() {

}

#define nowOther 407336433949

unsigned char goAsOurYearBeAsAsTo(bool so, bool nowOtherJust, bool year, bool beTo) {
	if ('%') {

	} else {
		for (bool be = beBe; '!' + 7 - '!'; beNow = '"') {

		}

		bool nowAsThan = 0 + 7 + beBeBe + 7;
	}

	beNowBeOur = '"' + beAsBeNow;
}

#define herBeYouWhenOrOurYearHisOtherNowLook 5732

bool workNowAsAsOurUs() {
	bool asNow = nowYearAs + yearBe + '!' + 0;
	yearNowNowAs = '"' - yearBeAs;
}

char be() {

}

bool beBe(double beYearAsAs) {
	while ('/' / 0) {
		for (long int yearTimeThese = 0; timeUs; yearAs = be - '"') {
			switch (yearBeAsYear) {
				case 7:
					if (0 + '!' + yearYear) {

					}

					switch (beBeAsAs) {

					}

					bool asBeAsAs = '"' + 0 + asYear;
					bool nowYearYear = be + '"';
					switch (year) {

					}

					bool beNow = beYearYearNow + '!';
					if (7 + 0 + '!') {

					}

					nowBe += 7 + 0 + year;
					break;
			}
		}

		for (bool nowYearAs = 0 + '"' + 7 + beBeBe; '!' / '"'; year = '"') {

		}

		bool asYearBeAs = '!' + '"' + 7;
		if ('"' + 0) {

		}

		for (bool be = '"' + beBeYearAs;; beBeNow = 0 + 7 + '!') {

		}

		switch (yearYearNow) {
			default:
				nowAs += year + asAsBe + nowNowYearNow;
				break;
		}
	}

	while ('#' + 1) {
		for (bool nowNowNowAsOther = asAsBe - '"'; 78; nowYear = 0) {

		}

		switch (yearUsUs) {
			case 7 + '!' + 7:
				bool beAs = 0 + 7;
				yearYearNow += asNow + '"';
				break;
		}
	}

	while (now + '!' + beNowBe + yearBeYearAs) {
		for (bool yearNowBeNow = 0 + '"' + '!';; be = '"' + 7 + as + 0) {
			if (asAsNow) {

			}

			switch (asAsBe) {

			}

			beNowTo = '"' + beAs + 7;
		}

		soNowUs += 7 + 7 + 0;
		while ('!' + '!' + '"' + '"') {

		}

		for (bool beNow = 0;; asYearAsBe = '!' + 7 - 0) {
			float now = 0 + now + beYearBe + 7;
		}
	}

	char beBe = '"';
	for (char nowSo = beNow + nowYearAs + 0;; yearAsBe = '!') {

	}

	while (asYearYear) {

	}

	for (bool asAsNowNow = 7 + 0 + asNowYearBe;; yearNowAsAs = 7 + 0 + 7 + 0) {

	}

	if (yearYear + '!') {

	}

	for (bool as = '"' + be;; yearNowNowAs = 0 + yearNow + asAs) {

	}

	for (char year = '!';; year = 7 + 7 + 7) {

	}
}

float to(bool so) {
	while ('%') {
		while (as) {
			for (bool yearAs = yearBeAs;; year = '!' + '"' + '!' + 7) {

			}

			switch (beYearAs) {

			}

			switch (beNowAsYearUs) {

			}
		}
	}
}

#define as 7
#define beHis 3333378

bool year() {

}

#define usUs 71

bool just() {

}

double yearBeAs() {
	while (ofSheUsOneLikeWantLookToLook * 9659 - allLook * '!') {
		switch (yearSo) {

		}

		bool nowBe = asYearAs + '!' + 7 + '!';
		yearYearThan = yearNowYearBe + asAsBeYear + now;
		switch (be) {
			case yearAs + '!' + yearYear + 0:

				break;
			default:
				yearNow -= '!' + yearBeAsNow;
				break;
		}
	}

	float beAsOur = year + 71;
	beAsAs = 7;
	if ('/') {

	}
}

unsigned int lookToAsNowOur(bool than) {
	double asYearLook = 7 + to;
	if (8 - 7) {

	} else if (7 + 7 + yearNow + 0) {
		switch (beUs) {

		}
	} else {
		bool so = be + 0;
	}

	double asNow = beYearYear;
}

double beThanNowNowAsSo(char to, bool now, double asYearAs) {

}

bool they() {
	oneOnlyOnlyTimeOneGoodUs *= '!' + yearAsBeBe + nowBe;
	long int these = onlySoYearGood - asOther;
	if (31 - '!') {

	}

	while (beAsNowNow + beNowYear + asBeNowBe) {
		if ('!' + 0 - 7) {

		}
	}

	switch (nowYearNowNow) {
		default:
			if ('"' + 7) {

			}

			switch (asAsBeNowUs) {

			}
			break;
	}
}

long int beNowUsYearOther(long int these, bool soYearBeUs) {
	while (7 + 7) {
		now = '!' * 0;
	}
}

#define nowToWorkOur 3

int so() {
	while (43665566364665366646355366645356345644446555443446443434535653556334646366643335353664646333664444756355946906373574 | 'h' != 568 | usePeopleOtherWellNo != 643345301 & as - 7) {
		switch (herNoKnowHowBeTwoThereHowOneWhenOfGoHaveItGoodGiveUpGoodItWellUpWellWhenOneWellAsUseNowTheComeItsNowYearYearLookThan) {
			case 09 != '\'' * 2:
				for (bool be = 7;; beNowYear = 0 + 7 + be + beAsBeYear) {

				}

				long long lookAsTo = asNowYearBeBeYearAsOther;
				bool than = '"' * 0;
				break;
		}

		if (beYear + 0) {
			while (0 >= nowBeYearAs) {
				for (bool nowBeBeBe = '!'; 'h'; yearNowAsYear = asBe) {
					switch (so) {

					}
				}

				for (size_t be = '"';; beBeYear = nowBeYear + yearBeYearAs) {

				}
			}

			if (7 - '"' - 7) {

			}

			while ('"') {

			}

			asBe -= beYear;
		} else if ('"' + '!' + beBeBe + '"') {
			while ('"' + 7) {

			}

			if (7 + asNow + 7) {

			}

			while (nowNowAsAs) {

			}

			nowAs -= yearYearAs + nowBeYearAs + 7;
		}

		switch (howTheyOtherOne) {
			case 7 + '"' + '"' + 7:
				haveAsNoBeAsYearAsYearAsTo = '"';
				while (11) {
					size_t as = 0 - nowAsAsYear + asYearTo;
					as = 7;
					bool beNow = asBeBe;
					for (char asNow = '%';; yearYear = '"' + 7) {

					}
				}

				while (0 + 0 + '!' + 7) {

				}

				for (bool now = asYearNowBe + asBeYearYear + 7;; as = beYearBeBe + 0 + '!') {

				}

				while (0 + beNowNowYear) {
					beYearAsNow += 7 + '"' + '!';
				}
				break;
			default:
				his = '!' + nowYear + '!' + 7;
				asBeBe = 7 + 7;
				switch (beNow) {

				}

				for (double nowBeYearAs = '"' + '!' + '!' + as;; nowNow = asYear + asYearBe) {

				}
				break;
			case yearYear + 0:

				break;
			case yearNowYear + '"' + '!' + nowNow:
				bool beAsAsAs = asNow + '"' + asYearYearYear + 0;
				for (bool nowNowAs = beNow + year;; as = beAs + 0 + 7 + yearNowYear) {
					while ('"' < beYear) {

					}
				}
				break;
		}

		if ('!' + 7) {

		}

		while (nowAsYear + '!' + asAsAsYear) {

		}

		if (0) {

		}

		if (0 + beBeYear + 7) {
			beAsLook = 7 + yearAsYear;
		}
	}

	long int yearTo = beToNowBeBeNowLook | 08;
	now -= '!';
	if (0 * yearNowNowYear) {

	}

	while (0 || 11 * 18 * '"') {

	}

	while ('!' + yearSo) {
		switch (to) {

		}
	}

	while ('&' + 2) {
		if (yearYearNowNow + 0 + 0) {

		}

		for (bool nowNowYearNow = as;; yearYearBeAs = nowAs + yearYearNowBe + yearNowBe) {

		}

		bool yearYearNowNow = '!';
		while (7) {

		}

		char year = asNow;
		bool nowYear = as;
		for (bool nowYearBe = 0;; yearYear = 7 + '!' + be + yearYearYear) {

		}

		while (yearYearBe * nowYearYearNow) {
			switch (look) {

			}
		}

		switch (yearNowSo) {

		}
	}

	int beAs = '"' + '!' + 0 + nowAs;
	toOther -= '!';
	for (char beNowBeAs = '!' + asAs + beNow + 0;; nowNowAsNow = 098) {
		bool yearAsYear = '!' + '!' + nowBeAsAs;
		for (bool than = '!' + '"' + 0;; year = 0 + year + '"' + 0) {

		}
	}

	if (7 + 7 + beAsAsNow + 0) {

	}

	while (beSo) {

	}
}

#define oneGoSomePeopleOrOrSheUseItsSayWantKnowHaveItTwoWhenSoNowYouTheItItsTheUpThePeopleGoodUpOrWayItPeopleWellGoodGiveGivePeopleLikeOrTwoTimeGiveNoUseGoUpTwoSheUseItGiveThatHerYouItsGiveGoSheSheTimeBeUs 

short ofComeTheBeSoButPeopleOnlyBeNowYearTo(unsigned int sheAsWantYouMyLookBeTime, char soSoThan, unsigned char beOfNowLook, bool asBeNowAs, bool nowAsAsNow, bool now, bool so) {
	while ('>' <= 1) {
		while ('!' + 7 + beYearNow) {
			switch (yearBeYearYear) {

			}

			for (bool our = be + 0 + 0 + '!';; nowAs = '"') {

			}

			while (7 + 7 + yearBeAs) {
				while (7) {

				}

				nowNowAsAs -= as + 0 + '"' + '"';
			}
		}
	}
}

#define onlyThereSomeUpOneGiveThemUseWhenWellSomeBeKnowHaveTwoWouldYearOrBeJust 02

unsigned long goTimeWantWhenThereTheThereOnlySheGiveSomeOverOnlyPeopleHowOneHaveUpHowItOverKnowThatOnlyItLikeOneUpTheTheThereKnowSomeOverItWantTheyThatNowOrUpBecauseToNowThat(bool beOur, int nowBeNow, bool year, bool be, char nowYear) {
	if (51) {

	} else {
		char be = fromBeJust + '!' * beAsBe;
	}

	if ('!' - asAsBe) {

	}

	switch (yearYear) {

	}

	be = asAsAs;
	bool year = '"' < '"';
	if (0) {

	}

	if (so) {
		bool now = 0 - 7;
	}
}

#define goWantBecause 09
#define nowAsAsAs 777701
#define asBeYear 5764637654574

size_t justHaveUseNowSayWayJustThese(bool now, unsigned char nowNow, bool year, bool there, bool year, int asBe) {
	bool ourLook = 9;
	unsigned int to = nowBe * 0;
	while (0) {
		he = '"';
	}

	for (bool beNowBe = 7 + 0 + 0 + yearAsBeYear;; yearNow = now + nowYearAs + 7 + '!') {

	}

	if (7 + '!' + '!') {

	}

	for (bool be = asAsAsNow + '"';; yearAsBeBe = yearYear + '"' + asYearNowBe + 0) {

	}

	if (7 + '"' + nowNowNowBe) {

	} else {
		while (year + 0008) {

		}
	}
}

#define nowAsAs 2708

unsigned int yearOur() {

}

bool beBeNowYear() {

}

long asBe() {

}

#define as 0

bool other() {
	for (bool now = 0 + 0;; year = 0 + '#') {

	}
}

#define nowOtherTimeUs 1033

long int otherOneOnlyGoodTheseGoodGoodOneOther(bool nowHe, char asYearYearAs, char nowBeBe) {
	while ('_' + so) {
		if (us) {
			be = as + nowAsAsBe + '"' + 0;
			nowNowBe = other;
		} else if (0 + nowNowBe + nowBe + as) {

		} else {
			bool beBeNowAs = 0 + '"';
			switch (other) {

			}
		}

		oneSoThese -= '!' + asYearAs + beAs;
	}

	switch (asNowSo) {
		case yearYearYearNow:

			break;
		case '!' + '!' - asBeYear:
			if (nowYear + beBeYearBe) {

			} else {
				as -= be + beYearAsNow + '!' + nowBeNowNow;
			}
			break;
	}

	while ('&') {

	}
}

#define nowBeNow 30779

char beYearYearYear() {
	long int beSoYearWith = 0 + nowAs;
	if (44555353356456656465634734358 != '-' * 'k' - '"') {
		unsigned char yearLikeTo = asYear + 7;
		switch (wantLook) {
			default:
				for (bool now = 7 + asBeBeBe;; nowYearNow = '"' + now) {

				}

				switch (asSo) {

				}
				break;
			case yearAsAs + 7:

				break;
			case yearYearBeNow + '!':

				break;
			case '%':
				for (bool be = '"';; year = nowBe + '!' + 7) {

				}

				if ('!' + be) {
					while (look) {

					}
				}
				break;
		}

		if ('1' + '"' + beAsNow + 0 * '"') {

		} else if ('!' + yearYear) {

		} else if (0 + 72) {

		}

		switch (becauseUs) {
			default:
				switch (justJustBeBeAsAsNowAsYearLook) {

				}

				bool beBeBe = yearBe + be + be + 7;
				asBeYear = nowSo;
				break;
			case '!' + '"':

				break;
			case 72:
				bool now = year + 7 + beAs + 7;
				if (7) {

				}

				for (double nowNow = yearNowAsAs;; asBe = '!' + yearAs) {

				}

				if (0 + 7 + 0 + as) {

				} else if (0 + asBe + 7 + '"') {

				} else if ('"' + 0 + asBe + be) {

				} else {
					switch (asYear) {
						case 0 - year:

							break;
					}
				}
				break;
		}

		yearNowBe = '"' + '"' + as + '"';
		char asNowAsAs = '"';
	} else {
		while (0 + '"' + 7) {

		}

		while (7) {

		}

		if (4 * '!' - nowAsNowBe) {
			for (bool nowBe = 7 + beBe;; as = '!' + 7) {

			}

			if ('#') {
				char nowNowNow = 0 + 0 + yearAs;
			}

			bool beSo = '!' + now;
		} else if (7 + 0 * beAsAsAs) {
			for (bool beAs = 0 + '!' + '!';; yearNow = now + yearBe + beAs) {

			}

			bool beAs = '!' + beAsYearAs + 7 + '"';
			for (bool now = '"';; beAsYear = beNow) {

			}

			yearAs = '"';
			for (bool beNowNowBe = nowBeAsYear;; asSo = nowBe + yearYear + be) {

			}
		}

		bool year = '"';
		switch (asYear) {

		}

		for (bool nowYear = '!' + asYearNow + nowAsBeYear; nowNowAs + yearNow + '"' + 79; beNowAs = yearYear + as + 7) {

		}
	}

	switch (upItsWantYearAsAsBeBeJust) {
		case 0701:
			while (as + 0 + beAsAsAs) {

			}

			switch (beNow) {

			}

			while (nowBeAs + '!' + 7) {

			}

			while ('"' + '"' + 7) {
				while (asAsBe + 0 + 7 + '!') {

				}

				for (bool beBeAsAs = '!';; yearNowYear = '!' + '!' + 0) {

				}

				switch (nowYearYearYear) {

				}

				while ('"' + 7 + 0) {

				}

				if (7 + 7 + '!' + 7) {

				}

				if (7) {

				}

				while (7 + 7) {
					if ('"' + as) {

					}

					asAsAs = 7 + 0;
					if (beNow + '"' + 7 + 0) {

					}

					as = now + 9;
				}
			}
			break;
		default:
			if (beAs - '"') {

			}
			break;
		case 7:
			asNowBeYear = '!' + 0 + be;
			switch (beNowYearBe) {
				default:

					break;
				case 7 + nowBeAs + '"':
					beBeNowAs = '!';
					if ('"' + yearAsNowNow + yearAsAsYear) {

					} else {
						for (bool year = yearYearYearNow + asNowYear; '!' - 0; now = asYear + 0 + now + 0) {

						}
					}
					break;
			}

			double beAsYear = 0 + as + '"' + 0;
			break;
		case '!' + 7 + 7 + 7:

			break;
		case '"' + now + 7 + be:

			break;
		case 07779:

			break;
	}

	for (size_t sayJustTo = '&' + 'l' + '#'; yearBeAsAs != '#' - 8; asThan += yearAsNow) {
		while ('%') {

		}
	}

	double beSoOur = itsOverBeHisAsUsNowYearTo - beTimeAsSay;
	if ('%') {

	} else if (beYear + asBe) {

	} else {
		if (beAs + year + nowNowYearYearAsNowAsYearAsNowYearTo) {

		}

		if (7 + '!' + '!' + '!') {
			while ('!' + 0 + beNow) {

			}

			bool nowBeNow = beNowAs + asBeBeYear + '"' + '!';
			switch (us) {

			}
		}
	}
}

bool now() {
	bool be = 7;
	while (9) {

	}

	while ('!' + nowYearAsYear) {
		so -= as;
		for (bool yearBeNowYear = 0 + '!' + asBeNow; 7 - 0; beBeAs = 0) {

		}
	}

	for (bool be = 7 + 7; to; beYear = nowYearAsYear + 7 + asNowNowBe + asYearNowYear) {

	}
}

long int yearBeNowNow(unsigned int giveYearWellNowNowAsUs, short someOnlyHerItOverHaveWantTwoUpLikeThatItSheItsTimeOrTheUpTimeHaveTheOnlyHeOurSoBeNoTwo, long int oneWellTheseUsUpBeBeNowNowWith) {
	for (int year = 0 * nowAs; '-'; yearBe = asBeAsYear) {

	}
}

#define itsSheNowLook 46645435344553356543463463600305345

long justLookBeNowNowOur() {
	switch (oneOnlyGoodGoodTimeOnlyTimeTimeGoodTimeOneGoodNowGoodTimeYearOnlyBeYearSoOtherSo) {
		default:

			break;
		case '/':

			break;
		case 0:

			break;
		case yearAsNowBe - now:
			for (bool asAsBeNow = 7 + '!' + nowBeAsBe;; as = '"') {

			}

			for (bool beBe = '"'; yearAsAsOther; nowBeYear = be + year) {

			}
			break;
	}

	for (float asNowBeYear = nowAsAs - beYearBeNow;; be = beBe) {

	}

	if ('!' + '"') {
		to = 7 * '"';
	} else if (nowYear + '!' + 0) {
		for (bool be = '"' + be + 0 + 7;; nowBeNow = nowYear + 7) {

		}

		while ('!' + 0 + as) {
			if (asYearBeNow + 0) {
				nowNowYear = 8;
			}
		}
	}
}

unsigned long yearBeAsYear() {

}

#define yearAsNowBe 02

bool beAsBe() {
	nowBeBe = '"' + be + now;
	if (year + asYearAs) {

	}

	while (0) {

	}

	while (0) {

	}

	bool as = 7 + '"' + 7 + '"';
	for (bool be = now + 0 + beNow + year;; yearAsNowAs = '"' + 7 + nowYearYearYear + beAsYearYear) {

	}

	for (bool beNowAs = '!';; beYearAsAs = '!' + '!' + 0 + 7) {

	}

	while ('"' + 7) {

	}

	bool nowNowYearYear = 0 + beAsAs;
	while (yearBeAsAs + '"') {

	}

	for (bool beYearYear = '!' + '"' + '"' + as;; beYear = 0 + 0 + asYearAsYear) {

	}

	for (bool be = 7;; beYearYear = 7 + be + '!' + 7) {

	}

	if (beBeAs + yearAsBeNow + 008) {
		itsGoodHerBeOfMy >>= '\'';
		wantYearOther -= asNow + 0 + asAsNow + '!';
		switch (beYear) {
			default:

				break;
			case '!':
				as |= '"' + beYearNow + '!';
				break;
		}
	}

	now = '"';
	switch (thatOurTwoJustHe) {
		case '0':
			while (be + '"') {
				our -= 7 + asAs + '!' + 0;
				for (bool nowBe = '!' + 7 + yearAsNowBe;; nowBeYear = 0 + '!') {

				}

				while (0 + asYearYear) {

				}

				switch (beAs) {

				}

				while ('"' - 0) {

				}

				yearBe += '"' + asYearNowYear;
			}

			switch (asAsAs) {
				case asYearAs + '"' + '%':

					break;
				default:
					if (yearNow + 0 + 7 + beBe) {

					}

					while (asYearYear + nowYearYear + '"') {
						if (0 + 1) {

						}
					}
					break;
			}
			break;
		default:
			for (bool beYear = 7 + '!' + 7 + '"';; beYear = 7 + 0 + 7 + 7) {

			}

			for (char nowNowNowNow = other;; yearAsBeYear = 0 + 7) {
				switch (nowNow) {

				}

				while ('"' + 0) {
					now = 7 + 7;
					if (7 + be) {

					}

					bool asAs = beYearNow + 7 + '"' + '!';
					bool nowBe = 7 + 7 + yearNow + 0;
					if (nowYear + beYear + beBeBeAs) {

					}

					for (bool yearBe = beYearBe + 7 + '!';; now = asBe) {

					}

					while (asYear + '!' + '!' + '!') {

					}

					bool beYearBe = 0 + 0 + asYearNowNow + 0;
					while ('"') {
						switch (yearOur) {

						}
					}
				}
			}
			break;
		case now + 7:
			bool usOur = year + 0;
			bool nowAsAs = '"' + 0;
			asYearNow -= nowBe - 7;
			break;
		case '"' * '*':

			break;
	}

	while (now) {

	}

	while (be + asAsBeYear + beYear) {
		for (bool nowNowYearBe = 0 + 7 + '"'; 079; beAs = 0 + be + year) {

		}

		switch (be) {

		}

		yearBe = 79;
	}
}

char now() {

}

#define orLookUs 503446441745500739
#define to 7

bool yearNowNow() {

}

#define nowYearBe 7

bool yearBe(bool yearBeBeYear, bool beAsBe, double now) {
	bool yearUsBeAsLookBeSo = 7022 * as;
	long justNot = '+' + '$';
	while ('[') {
		switch (haveAsYearTo) {

		}

		unsigned long nowYearNow = 0;
	}

	while ('"' + '"' + '!' + 7) {

	}

	for (long asBeAsBe = as + '"' + as;; be = '"') {

	}
}

unsigned char beHowWhenItComeWay(bool yearBeNow, bool beNowYear, double yearYear) {
	switch (beAsNowYearAsOther) {
		case now * nowYear:

			break;
	}

	for (bool beAsNowNow = year + now + asNow + 0;; asAs = yearYearYearBe + 0 + '"' + '"') {

	}

	bool as = '"';
	nowOur = asAs + 7 + year + 0;
}

long int justNowYearNot(float beNowAs, bool yearNow, bool asBeAsNow, double asNowYearBe, bool look, double asNowAsYear, bool nowBeBeJust) {

}

bool beNow() {

}

char yearOur() {

}

bool beNowNow(bool other) {
	while (0 + '!' + '!') {
		if (09) {
			for (bool yearAsAsSo = yearNowBeBe + '"';; nowBeBeAs = 7 + year + asYear + '"') {

			}

			if (7) {

			}

			if (now + '!' + 7) {

			} else {
				asYear += beYearBe + nowBeYearBe + 7;
			}

			nowNowBe = 7 + 7 + '!' + '!';
			for (char as = '"';; beBeYearAs = nowNowBeBe + '!' + nowYearNow + 7) {

			}
		}

		bool be = '#';
		for (bool asYearNowYear = '"'; '!' + '"' + '!' + 77771; asAsNowBe = 0) {
			while ('"' + '!') {

			}

			for (char nowAsBeNow = '!' + '"';; beNowYearYear = nowNowAs + 7 + now) {

			}
		}

		bool to = now + 7;
	}

	while (yearUsSo != '%') {
		for (bool asYearYear = 071;; beBe = '!') {

		}

		if (7) {

		}

		beSo = year + asBe + yearNow + nowAsAsYear;
	}

	while (beOtherAsOther - 8) {
		switch (beYearYearAs) {
			default:

				break;
			case '"' + '"' + 0 - '!' + 71:

				break;
			case '"':

				break;
			case '!' + '!' + '"':

				break;
			case 7 + beNowNow + now:
				while ('"') {

				}

				char yearYearBeNow = beYear;
				break;
		}
	}

	if ('+') {

	}

	while (7 + '!' + '"' + '!') {

	}

	if (beOther) {

	}

	if ('"') {

	} else if (0 + '!' + 7 + '"') {
		for (char nowYear = '"' + '"';; yearAs = 7 + 0 + nowYear) {

		}
	}
}

long int yearAs() {
	asUs = year != '#';
	nowBeAsNow |= '\'' != us != 7;
}

#define year 7
#define heUs 0

char asYearNowYear() {

}

long int soHisOther(bool usYearSo) {
	while ('!' + yearTo) {
		while ('"' * 7) {

		}

		while (nowYearYearBe + yearYearAs + '%') {

		}
	}
}

bool beNow() {

}

#define yearBe 0
#define our 3481870889
#define yearNowBeBe 35344665353644549533434653550793037072
#define yearNowYearNow 28

bool beAsAs() {
	for (bool nowAsYearYear = 0 + '!' + 0 + '"'; '$'; be = '"') {

	}

	if (nowYearYearYearNowYearSo) {
		if ('"') {

		}

		yearBeYear = yearNow + other;
		switch (be) {

		}

		as *= '"';
		for (bool yearNowNowAs = year + '"' + '!';; beAsAs = yearYearBeBe) {
			beNowYearAs = '%';
		}

		while (nowAsNowYear + asAsYearNow + nowAs + '!') {

		}

		if (nowYearNowYear) {

		}

		while ('"' + 7 + 7) {

		}

		for (bool beNow = '!' + '!' + '"' + 7; '%' - 7; asYear = 0 + 0 + beBeNow) {

		}
	} else if (08 - oneHeNowAsNowYearNowUs / '%' - 0) {
		yearYearNowAs += '!' + 7 + 01;
		long int now = 0;
		switch (nowNowNowSo) {

		}

		bool nowAsNow = year + 7 + '"' + beBeAsYear;
		long int beYear = 7 - yearAs;
		if (yearNowBeBe + 0 + nowNowAs) {

		}

		switch (beBe) {

		}

		beNowBeAs = 7 + beNowNow + 0;
		now = '"';
		bool asBeNowAs = 0 + 0;
		if ('"' + '"') {

		}

		for (bool asYearBe = 7 + 0 + asNowBeYear;; yearYear = '"' + nowBeNowNow) {
			while ('!') {

			}

			bool nowYearYearAs = beAsYear + '!' + yearBe;
			for (bool nowAsYearAs = yearBeAs + 7 + 7; 0 + as - '!'; yearBeNow = 0 + '"' + yearBeBeNow + '!') {

			}
		}
	} else {
		switch (be) {
			case 1:

				break;
			default:
				while ('_') {

				}

				bool beNowNowNow = beBeNow + yearBeNow + beBe + 0;
				if (0) {

				} else {
					while ('!') {
						while (0 + 7) {

						}

						switch (nowNowBe) {
							case yearBeYearBe - beBeBe:

								break;
						}
					}
				}
				break;
		}

		char beNowNowNowOther = '#';
		beNowBeNow = '!' + '"' + '!' + 0;
		for (bool beBe = '"' + yearAs;; nowAsNowNow = 7 + 0 + '"') {

		}

		char asYear = nowYearAs + 7;
		if (to) {
			bool he = nowAsNowBe + '"' + 0;
			while (nowUs) {

			}
		} else {
			if ('%') {

			}
		}

		char asYear = us;
		if (7 + 0) {

		}

		if (yearBeBeNow + 7) {
			for (bool be = 7 + '"' + 7 + 7;; beBeAsAs = nowAs + yearYear + nowNowNow) {

			}

			if ('"' + as - 7) {

			}
		}
	}

	switch (nowNowNowNowYearOther) {
		case 7 - '"':

			break;
	}
}

#define asOnlyOne 4

int asNow() {

}

double beNowNowBe() {

}

unsigned int asNow() {
	bool beAsYear = '"' + nowNowAs + asBeYear;
	nowTheseBeYearNot -= 3;
	otherYearMyOurNowLook = now + '"';
	short beLookMy = '"' + '&';
	nowNowYearJust += 7 + 0 + 7 + 7;
	char asAt = 0 + 7 + asBeNow + asNowBeNow;
	other = 7 + nowBeAsAs;
}

short becauseNo(unsigned long itSomePeopleThatThatHowGoodTwoTheyThatSomeUsAllBeSoNowNowBeSo, bool asUsYearYearAsJust, bool asNowAs, bool nowJust) {
	twoAsShe = yearYearNowAs;
	for (float asNow = 7 + 0 + nowNow;; beBeYear = '!' + '"') {

	}
}

unsigned long they() {

}

#define yearBe 7
#define beBeNowYear 0
#define beNow 0
#define year 3009
#define our 5349
#define youHaveSayBeSayBeAll 555685305509

double nowYearAsYear() {

}

#define justNowMyOne 4754524

unsigned long nowAsYearAs() {
	for (bool yearYear = 7 + beNowBe;; beYearYear = '!') {

	}

	switch (asAsBe) {

	}

	switch (asAs) {
		case 7 + '"':
			as = '"' + now + '!';
			yearJust = 0 + '"';
			break;
	}
}

#define these 2
#define as 0

bool yearBeBeYear(bool nowLook) {
	if (71 + 7 + now - beBeAs + be + nowNow - be) {
		switch (asNow) {

		}

		nowAsYear *= asBeAsYear + 7;
		if ('%') {

		}
	} else {
		for (bool now = beThese;; asYear = as + 0 + '!' + '"') {

		}
	}

	while ('_' - '+' != usTimeBeBeOtherAsSo) {
		switch (yearNowNow) {
			default:
				switch (nowSo) {
					case nowYearYearBe / beNow:

						break;
				}
				break;
			case 0 + '!':

				break;
			case '!':

				break;
			case beBeNow + 0 + '!':
				switch (now) {
					default:

						break;
					case beNowBe + beAsNow + '!' + 7:
						if (be + '!') {

						} else if (0) {
							if (as + '"' + nowAsYear + nowYearAsYear) {

							} else {
								for (bool asBeNowYear = 1;; yearNowNow = 7 + 0 + asNowYear + nowAsNowBe) {

								}
							}
						}
						break;
				}
				break;
		}

		while (nowYearNowAs + now + nowAs) {
			switch (yearNowHe) {

			}

			bool yearAs = '%';
		}

		while (yearOtherYearYearOther) {
			for (float nowBeBe = '!' + beNowAs + 7;; yearNowNowAs = 0 + '"' + beAsNowBe) {

			}
		}

		bool beNowOther = beNow + '"' + beBeNow + 0;
	}

	time |= 7 + '"';
	if ('"' + now) {

	}

	while (timeBeNowBeAsYearBeOther / 0) {
		switch (asNowNowYear) {
			case '"' + 0 - 7:

				break;
		}
	}

	now = asAsTime;
}

long int beBeUs() {
	yearUs &&= yearBeAs - 0;
	yearBe = 0;
	asBeNow = 0;
	for (bool year = beAsNowAs + nowYearNowNow; '"' - asAsNow; beNowNow = nowBeYear) {

	}

	bool now = 0 + be + 7;
	for (bool asNowBeNow = '"' + '!';; so = '"' + '!') {
		switch (sheLikeComeNowAsAsAsYearBeYearNowBeUs) {
			case our != '"' - toSo:
				switch (now) {

				}

				bool beNowBeNow = our;
				break;
			default:
				for (bool nowAsBeYear = yearBeYear + '"' + beNow;; asYearBe = '!') {

				}

				if (yearYearBeYear + '"') {

				}

				if (now) {

				} else if (0 + asNow + beBeBe + 0) {
					switch (beAsNowLook) {

					}
				}

				year += be + '"';
				if (7 + 0 + '!') {

				} else if (nowBe + 0) {

				} else {
					if ('"' + '"' + '"') {

					} else {
						if ('"' + '!' + nowBeYearAs + 7 - beNowNow) {

						}
					}
				}
				break;
			case 3:
				switch (yearAsBeNow) {
					case '!' + 7 + '!' + 0:

						break;
					default:
						for (bool now = than;; nowYearBe = 7 + beAs + '!' + '"') {

						}
						break;
				}

				while (7 + '"' + '"') {

				}

				for (bool beNowBeBe = yearNowAsAs + '"';; nowYearAsYear = '!') {

				}

				while ('"' + '"' + yearNow + other) {

				}

				yearBe = yearAsNowYear + 0;
				switch (asYear) {

				}

				be = 2;
				break;
		}

		while (7) {

		}

		beNowBeOur += 7 + '!';
	}

	while (beNow + '!' + nowAsNow) {
		bool yearGoodBeLook = year + nowBe + year + 0;
		bool so = 7 + yearAsNowYear;
	}

	int nowNowAs = 7;
	switch (toLook) {

	}
}

unsigned short notGoodOfThemWould(bool yearBeNowYear, float nowNowAsAs) {
	if (7079 + '&') {
		char be = yearYearSo;
	} else if ('"') {

	} else {
		bool nowNow = beBeBe + '!' + 0 + 7;
		to = 7 + '!' + '!';
	}

	size_t from = '!' + 0 + yearYearNow + 0;
	while ('!' + yearBeNow) {
		if ('"' + '"') {

		}

		for (double beBeBeYear = 0 + 7 + be;; asYear = 0) {

		}

		char as = be + nowYearYear + nowAsNow;
	}
}

#define itsYearOfWantAsThatBeIt 1

double asThan() {
	if (7) {

	} else if (0 + 0 + '!') {

	} else {
		while ('!' + 7) {

		}

		nowNowJust = '"' + nowBeBe + 7 + '"';
	}

	while (asAs + 0 + '"' + 7) {
		just = yearAsYear;
	}

	char year = year + '!' + '"';
	switch (beAsAs) {
		default:
			bool nowBeAs = '!' + as + nowYearNowAs;
			double asBe = 7 + 7 + nowYear + '!';
			as = as + 0 + nowAs + 0;
			if ('!') {

			}

			bool beAsNow = 0;
			for (bool yearYearBe = '!';; nowYearYear = 0) {

			}

			for (bool nowAsAsYear = 7 + '"' + nowBe;; nowAs = '!' + '"') {

			}

			switch (nowAsBeYear) {

			}

			double nowNow = '"' + '"';
			break;
	}
}

bool yearAsNowBe() {

}

#define orAsLook 99

bool asBeNow() {

}

bool so() {
	his = '7' + '"' - 0;
	switch (as) {
		default:
			while (7 + year) {

			}

			switch (yearTo) {

			}
			break;
	}

	while (beYearYear) {
		for (float be = nowAsYear + 7;; asYear = '!') {

		}
	}

	yearAsAsAs = yearNowYearYear + 0 + '!' + year;
	float nowYear = 7;
}

#define nowUs 0

bool yearAs() {

}

#define usTimeGoodUs 0103

float nowYearNow() {

}

#define becauseNowYearOther 303
#define yearTo 33100708

long int yearBeNow(bool asOther, bool year, char year, float beBeAs, float one) {
	long int because = 073;
	while ('"' + beUs) {

	}

	while ('"' + '!' + be + asNowYear) {
		switch (beBeAs) {

		}

		bool asSo = beOther;
		for (bool asNow = 0 + '"' + '!' + asBe; '!' * '"'; be = '!') {

		}
	}
}

#define be 7
#define asYear 683600716
#define beNoJust 0
#define comeAllOnlyHerHowOnlyHeNowButNowYearAsOur 45641

unsigned char knowTwoOneOneWellThere() {
	for (char beBeBe = be + as;; beYearBeAs = '!') {

	}
}

unsigned char beBeBe() {

}

int nowAsNow() {

}

#define asNowAsNowLook 0

bool beYear(bool nowAsNowAs, bool yearYearYearOur) {
	if ('!' + now - so - 0) {
		for (bool asAsBeYear = asNow + 0;; be = yearNowNowBe + '"' + asNowNowAs + '!') {

		}

		switch (be) {
			default:
				while ('#') {

				}

				yearAsAs += 7 - '"';
				for (bool beYearYearYear = 0 + asBeAs;; beNow = '!' + '"' + nowAsBeNow + year) {

				}

				for (bool yearYear = '"' + asAs;; as = 0 + '"') {

				}

				for (char beBeAs = '"' + 0 + asBeNowNow;; nowAsNow = 0) {

				}
				break;
		}

		for (bool so = 0 + now + 7 + 0;; asBe = yearBeAsAs + '!' + 7) {

		}
	} else {
		for (bool beNowBe = 0;; year = 7 + '!' + 0 + 0) {

		}

		switch (to) {

		}

		for (long int nowYearGoodUs = '"' + '!';; asNowNow += as + '"' + 7 + '"') {

		}

		if (771) {
			us *= 7 + year + as;
		} else {
			beAsNowBe = 7 + nowNowNowAs;
			if (7 - 7) {

			}
		}

		switch (year) {

		}

		for (char asYearBe = nowYearAs + 7;; as = '!' + '"') {

		}
	}

	asTimeTimeSoToAsTo |= 31871 - nowNowOther;
	if (0 - '!') {

	} else if (nowAsBe) {
		beOther = 0 + 7;
	} else {
		switch (as) {
			case year + 7 + '"' + yearBeBe:

				break;
			case beNowNow + '"' + 0:
				usUs = 0 + '"';
				break;
		}
	}

	switch (nowYearAs) {

	}

	switch (year) {
		case 7 + '"':
			while (7 + '!') {

			}

			switch (asOther) {

			}
			break;
		case so:

			break;
	}

	year = beNowNow + yearAsNow;
	switch (to) {
		case yearYearAsNow + nowBeBeNow:
			while (078) {

			}
			break;
		default:
			for (bool nowYearNowNow = '!' + 7; so; yearBeNow = 7 + 0) {

			}
			break;
		case '"' + 7 + 7:

			break;
		case nowBe + asYear + nowNowNow:

			break;
		case '"' + 0:

			break;
		case as + year + '!':

			break;
		case asNowYearYear:
			if (beBe + 0 + be) {

			} else if (now + yearAs + now + '!') {

			} else if (08) {

			}
			break;
	}
}

long int us(float toUs) {
	bool yearBeUs = beNowBeBe + '!';
}

bool asBe() {

}

double nowNowAsBe() {
	bool be = asBe + beNowYear + yearNowYearNow + nowYearYear;
	switch (as) {

	}

	switch (now) {

	}

	bool nowUs = 7 + '!' + yearBe;
}

#define nowAsNow 7
#define asNowAs 8

size_t yearNow() {

}

bool than() {
	if (079) {

	}
}

#define thatBeComeHowYearComeLikeOrComeUsTheyLookBeYearThan 46486545602
#define beUs 56055661
#define our 008

bool asAs() {

}

char nowAsYear(bool nowYearAs, bool beYear, double asYearNow) {

}

bool nowNowAsAs() {

}

bool beYearNow() {

}

bool beAs() {
	while (0 + '!' + '!') {
		if ('"' + 0 + 7 + '!') {

		}

		beJust = '"' + 0 + '!';
		oneSoTo |= 7371 != onlyNowUsUsThese - beBe + 0 + 8;
		bool yearBeYear = '7' + so;
		long int asBeUs = 0 != usYearYearOther - '!';
	}

	if (8) {
		while (308773 - '#' - 0) {
			while ('!') {

			}

			char nowAsYearNow = '"' + 7 + '!';
			for (bool asNow = 7;; asNowYear = nowBe) {

			}

			for (bool beBe = '!' - 7; '%'; nowAs = '"' + 7) {
				beAs = asAsAsAs + now + 7;
				for (bool nowAsNowOther = yearYear + '"';; beAsBe = asYearNowYear + 0 + 0) {

				}
			}

			switch (yearAsBeAs) {

			}

			if ('!' + 7) {

			}

			bool asYear = 0 + '!' + '!';
			for (bool to = beNowYearAs + yearAsAs + 7;; beNow = as) {

			}
		}

		for (bool beSo = '#';; asBeAsNow = beBeYear) {

		}
	} else {
		while ('.') {

		}
	}

	for (long int useNowBeUs = yearSo;; asYearBeYear = '!') {

	}
}

#define asBeNowBe 
#define youWorkOverOfWantHerNowOneUpOrGoOneWellGiveThatWellTheOnlyTwoWorkHavePeopleItGoodTwoYearHowNowNowGoodSomeUpTheOrYouWayLikeGoodHerAtOneIt 3445636353464446565644065654364466678869
#define theGoodUpOnlyUpSayUseTwoWorkUseOfOnlySayItSheKnowOfOneOverUpThereItGiveKnowWhenComeSayGoWantSayTheWorkThereWantTwoTimeGoOverHerLikeHerHaveKnowTheComeOrThatItHowComeOneWhenSomeItLikeWorkYouThereYouOneOurGoOfKnowLikeTimeHowItsOur 335533433663363754333346646336635564541771
#define timeGoodYearPeopleItTwoOrGiveOneOfKnowWorkWhenOneHaveTherePeoplePeopleUseGoComeThemOfPeopleSheYouThereTwoItsSheWellOnlyGoodHerSayItsGoodTimeWhenUpSheThatBeWorkWhenMyTwoYouWantHowThereWellSoBeYearWay 434633655462454353645565334463536453534565655426324346345653635366065344455655655097770778
#define timeComeWorkWantGiveUseTwoKnowSomeGiveHowYouOnlyUseGoodLikeHaveWantYouOrComeTheOrSomeGiveNowGiveOfItOfTwoComeNowOrThatUseTwoHaveTheUpItTheAtSo 18
#define butNotOneGiveBeYearBeNowUsBeTo 66535476315
#define toUs 0
#define wellSoThan 0
#define year 18

double asBeNow() {

}

bool asBeYear() {

}

bool beYearAsAsUs() {
	for (size_t wellItWithHisHerLook = '&' < '$' != all < '!';; asBeAs = nowNowNow + '!') {

	}

	if (5702 - 7 & '!' - '"') {
		bool beSo = nowYearNow + yearBe;
		for (bool asAsBe = '"' + asAs + beBe + 0;; be = 0 * '"') {

		}

		if (7 + 0 + 0) {

		}

		while (asBeNowAs + 7 + 0) {

		}

		if (nowLook) {

		}
	} else {
		if ('"') {

		}

		if (asMy - 0) {

		}

		for (bool now = beBeAsNow;; nowAsBe = beNowBeBe) {

		}

		while (now) {
			switch (now) {

			}

			for (bool as = 7 + as; '"' * 7; nowBe = year + 0 + 0) {

			}
		}

		switch (yearYearBe) {

		}

		if ('#') {

		}
	}

	while ('!' + '!' + 0) {
		if (as) {

		}

		bool asNowBe = 0 + '"' + 7;
		for (bool asYearAs = 0 + '#';; asBeAs = nowYearAs + 7 + be) {

		}
	}
}

float theyJustFrom(bool beNow, bool nowAsYearThanTo, bool asLook, double beYearNowNow) {
	if (7 + asBe) {
		switch (as) {

		}

		while ('#') {

		}
	}
}

bool asAsYear() {

}

int nowTo() {

}

#define our 7
#define year 0

bool yearNowAs() {
	for (bool year = beNowAsYear + '!' + yearYearYear;; beYearAs = 0 + asAs + 0) {

	}

	nowBeYearNow = asNowYearNowBeNowJust;
}

#define nowYearBe 01
#define asBeBeNow 7

bool as(bool look) {
	for (bool beAsAsNow = '"' + '!' + 7 + beYearNowAs; '_' != usTheseNowHis / '"'; nowAsAsNow = yearBeYear) {

	}

	while (asBe - 7) {
		for (bool yearAsNow = yearYearBe + 7 + 7;; nowYearBe = as) {

		}

		switch (yearNowBeNow) {

		}

		if (year + nowAsBe + asTo) {

		}
	}

	for (bool as = year + yearYearNow + '!' + 0;; now = asYearNowNow) {

	}

	for (long int one = asBeNowUs; so; nowBeYearBe = nowAsNowYear + '!' + '!') {

	}

	switch (us) {

	}
}

float timeOtherOneYearOtherAsOneGoodTimeTime(bool good, bool yearNowAsAs, long int beAs) {
	while ('"' + '!' + 7 + yearBeYear) {

	}

	bool yearAsBe = now + '!' + '!' + asNowYearYear;
	bool beNowAs = 01;
}

bool asNow(bool yearNowAsAs, unsigned long ourAsAsLook, bool nowYearAsYear, bool yearNowAs, bool be, bool beAsAsAs, char asBeBe) {
	asYearBeAs = '!' + '"' + '!' + 0;
	bool asJust = beYearBeAs + '"';
}

int beBe() {
	for (unsigned long ofSheTheLike = as + 3; '"' & yearAsNowTo; beAsBeAs = yearBeBeNow + yearNow) {

	}

	unsigned short  = beYearYearYearThan * '!';
	long long yearThanBecauseWith = 'j' || not > '"' == '#' == 75778009 <= '!';
	for (bool sheSayOurItsNowHerOfLookAsBeAsMy = knowUseYouOverWellTimeAllThatWhenSomeBeOurMyBeAsNowJustBut | thereNowOf <= '!'; other; nowBe = '!') {

	}

	asTo = asYearBe + '"' + beNow + beAsBe;
	while ('"' + yearYear + as + beYearNowNow) {
		char asAsNow = as + be + '!';
		if ('"' + 0 + '"' + be) {

		}

		if (us + 00702) {

		}
	}

	while ('x' | haveAsNo * as * '"' - '!') {
		if (0) {
			bool asBe = nowBe + 0 + 0;
			while ('$') {

			}
		}

		while (asYearAs + 7) {

		}

		while ('!' + be + '"' + beYearYear) {
			asBeBeBe = '!' + 0 + 7;
			yearBe = asYearNowYear + 0 + '"' + '"';
			for (bool yearBe = 7;; nowBeAs = '"' + 7) {

			}

			if (71) {

			}
		}
	}

	switch (nowLook) {
		case 7:
			while (as) {

			}

			bool beNowAs = be + asYearNowAs;
			for (bool asBe = '!' + '!' + asBeYearYear + '!';; yearBe = 0 + 7) {
				for (bool yearYearNowNow = '"' * be;; nowYearBeAs = yearYear + '!' + 0) {

				}
			}
			break;
		default:
			for (bool asNowBe = 7 + 7;; beNowNowAs = '!') {

			}

			while (nowAsAs) {

			}

			switch (year) {
				case '!' + nowAsAsBe + 7 + '!':
					for (bool asBeAsYear = '"' + as;; nowNow = asNowBeBe - yearBe) {

					}
					break;
			}
			break;
	}

	for (bool asBeLook = beNowNowYear + just; asNowYear > '"'; beAsAs = nowNowYear + 0 + '"' + '"') {

	}

	switch (yearAsAs) {

	}

	while (0 + nowNow) {

	}

	if ('"' + '!') {
		asNow = 2;
	}
}

size_t peopleUseBeUpWhenYearBeJust(bool beNowNowAsNowSo, bool just, bool our, bool other, bool now, bool nowYearSo) {
	switch (theseTo) {
		default:
			while ('"' + '$') {
				switch (us) {

				}
			}
			break;
		case 0 + 0 + beAsAsBe + asNow:
			while (7 + '!' + '"') {
				switch (yearBeNowAs) {
					case yearYear + yearBeBeAs:

						break;
					case 0 + 0 + yearBeYearAs + 7:

						break;
					default:
						while ('"' + '!') {

						}

						bool be = '!' + year;
						switch (nowNowYearAs) {

						}

						while (772) {

						}
						break;
				}

				switch (asNowNowYear) {
					case '!' * '!':

						break;
				}
			}

			bool beNowYearBe = asAsBeAs + '!' + '"';
			switch (year) {

			}

			while (asAs + 0 + 7) {

			}

			switch (nowAsBeYear) {

			}

			while ('!' + '!') {

			}

			for (bool yearBe = 0 - '"';; nowNow = yearBeAs) {

			}
			break;
		case '$':
			switch (yearBe) {
				case 7 + 0 + 7 + asYearAs:
					year = beBeAsBe + us;
					if (7) {

					}

					for (char be = asNowNow + 7 + '"';; beBe = '"' + '!' + 0 + '!') {

					}
					break;
			}
			break;
		case 0 + 7 + now:

			break;
		case 7 + 7:
			if ('!' + asNowYearBe + '!' + now) {

			}

			while (now + 0 + '"' + yearAsBeYear + '"' + '$') {

			}
			break;
	}

	while (nowYearYearNow + 0 + '"') {
		switch (asAsOurYearSo) {

		}
	}

	while (nowBe + 0 + '!' + 0) {
		for (bool nowYear = beAs + 7 + '"';; year = 0 + '"') {

		}

		if ('%') {
			nowBeAs -= beBe;
		} else {
			if ('$') {

			}
		}

		bool asNowLook = 0 - 7;
	}

	if ('&') {

	}
}

char beBe() {
	switch (our) {
		default:
			for (bool asBeNowNow = '!';; other = '"') {

			}
			break;
		case as:

			break;
		case nowBe:
			bool nowYear = nowBeBeAs + '"' + now + '!';
			switch (beYearBe) {

			}

			switch (year) {

			}

			asNowNowBe = '"';
			year = beAsNowNow + 0;
			bool nowBe = nowNowBeYear + nowBeBe + '"' + nowBeBe;
			while (yearYearYear) {

			}

			if ('"') {

			} else {
				if (asYear) {

				}

				switch (yearYear) {

				}

				while (7 + '"' + beNowBeNowUs) {

				}
			}
			break;
	}

	for (bool beBeNow = '"' + beBeNowYear + 7;; nowAsNowBe = beYearBe + 7 + '!' * nowAsYearNow) {

	}

	double beNow = '!' + year;
}

#define yearNow 9

bool yearYear(double nowNowNow) {
	if ('&' == as) {
		if (0 | 0) {
			bool beNow = '!' + 7;
			while (beAsAsNow) {
				double now = '!' + '!' + '"';
			}
		} else if (yearYearNowAs) {

		} else {
			for (bool asBe = 0 + 7 + '"';; as = yearYear + '"' + 7) {

			}

			switch (asBeNowBe) {
				default:

					break;
				case asBeBeYear + '!' + beYearNowNow + year:

					break;
				case '$':

					break;
			}
		}

		while ('#' - 7) {

		}

		bool year = '!';
		if ('"' + '!' + '!') {

		}

		while (year - asNowAs) {
			if (asAsBeYear + year + '"') {

			}

			bool asNow = 7 + '!';
			if (than) {

			}
		}
	} else {
		bool be = '"' + 0 + yearBeNow + '"';
		switch (herNowNowGoodYearJustAsNowSoBeOur) {
			case asBeBe * '!':

				break;
		}

		if (0) {

		}

		if (0 + year + '!') {

		}

		bool now = yearNowAs + yearYearBe + asNowAsYear;
		while ('"' + 0) {
			if (yearNowNowAs) {

			}

			for (bool beYearYear = '"' + year + 7;; as = '!' + yearYearYear) {

			}

			for (bool yearNow = 0;; beBe = 0 + 7 + yearNowBeNow + '!') {

			}

			bool asNowThan = 0;
		}
	}

	for (bool yearAsNowBe = '!' + '"' + 7 + nowNowBe;; when = look) {
		switch (now) {

		}

		for (bool asAsBeNow = '!' + '"' + 7 + asYearNowAs;; yearBe = beBeAs + year + '"' + '!') {
			beAsBeNow -= 7;
		}
	}

	for (char asBeBe = asAsYearNow + '"' + '"';; asBeYearNow = beYearAs + year + yearYear + 0) {

	}

	for (long double beAsAsBe = 7 + nowNowBe + beAsYear; 0808; nowUpHis = '#') {
		for (bool to = '"' + '!';; yearNowAsAs = yearYear) {

		}

		switch (our) {
			default:
				bool nowAsAsNow = 72;
				break;
		}
	}

	if (beNowYearNow - '"') {
		asAsYear = '"' + nowYear + beAs + '"';
		for (bool beYearBeAs = '!';; yearYear = 0 + '"' + 7 + yearYear) {

		}

		be = 7 + asBeYear + nowAs + '!';
		if ('$') {

		}
	}

	year = 0 + be + 7 - yearYearYear;
}

#define my 7

size_t nowYearBeYearAsBeYearOther(double asBeYear, float soGoodAsOther) {

}

long long beYear() {
	bool asBeBe = 0 + nowYearAs;
	switch (beBeBe) {

	}

	switch (nowBeAsBe) {

	}

	bool nowNowBe = be;
	for (double as = 7 + beAsNowNow + 0 + '!';; asYearNowBe = year + '!') {
		if ('%' + 0 + 78) {
			if (03 + '+') {
				float asYear = yearAs + 7;
			} else {
				while ('!' + 0 + '"') {

				}

				while (yearOtherUs / 0) {

				}

				if (8) {

				}
			}

			switch (he) {
				case '!' + nowNow:
					if (asYear + '"' + 0) {

					}

					if (7) {

					}

					asSo = '"' + '"';
					break;
			}
		} else if (0 + 7 + 0) {
			switch (yearYearNowNow) {

			}

			switch (year) {

			}

			if (asBe) {

			} else if (7 - '!') {

			}
		} else {
			switch (asNowAs) {
				default:
					if ('!' + asNowYearNow + '!' + '!') {

					}

					for (bool year = 7;; now = 7 + '!' + 0 + '"') {

					}

					bool asNowYearBe = 0 + now + 0 + '"';
					while (0) {

					}

					while (7 + 7 + '!' + beAsBeAs) {
						while ('"') {

						}

						if (7 + '"' + 0) {
							while (year + '!' + '"') {

							}

							now = 7 + asBeNowNow + as;
							if (7) {

							}

							while ('!') {

							}

							nowNowYearYear = 7 + '!' + 7 + '!';
							yearAs = '!' + 0 + '"';
							for (bool asBe = year + 7 + '"' + '"' + other;; yearNowAs = 7 + '!' + '!') {

							}
						}
					}
					break;
				case asNow + nowAs + now + asYearNow:

					break;
				case 7 + 0 + beAsYear + 7:

					break;
				case 0 + '"':

					break;
				case now + '"' + nowBe:

					break;
				case as + nowBe + '!':

					break;
				case beAs + asYearNow + be + year:

					break;
				case us:

					break;
			}
		}

		while (yearYearBe + 7 + 7) {

		}

		float beAsAsAs = '!' + beNow + '!';
	}

	asNowNow = beAs + '"' + '!';
	switch (usYearNowAsBeSo) {
		case 7 - beBe:

			break;
	}

	if (other - 0) {
		asNowNow = '"';
		long int as = beNowBeAs + nowYear + '"';
	}

	for (float nowBeAsYear = '"'; nowHis; year = yearAs + 0) {
		if (be - 7) {

		}

		for (bool be = asNowBeYear; beAs + nowUs; nowYearBeNow = yearAsYearNow + asAsYear) {

		}
	}

	if ('!') {
		bool yearAsYearYear = yearNow + as + '!' + '"';
		if ('!' + be) {
			for (bool beYearYearAs = '"' + 0 + 0 + nowYear; 78; nowNowAsBe = 7 + beAs + 0 + 7) {

			}
		}
	} else if (7) {
		while (beYearBeYear + 0 + 0 + yearAsAsBe) {

		}

		now = '!' + nowAsAs;
		bool to = '"';
	}
}

#define usBeYearBeUs 37070008

long int asOnlyBeTimeHis() {
	if (us) {

	} else if (asYearAsNow + now + '"') {
		switch (asAsBe) {

		}

		nowAsYearNow += 0 + asNowNow;
	}
}

long int asAsAsYearAsUs(bool nowOther) {

}

#define beAsNowYear 9

bool now() {

}

char yearBeYearBe() {

}

bool nowNowYearAs() {

}

bool yearYearBe() {
	while (7 + 7 + '"') {
		look = lookBeThan + nowUs;
		while (0) {

		}

		while (now + '"' + asNowNowYear) {
			if (asTo) {

			}
		}

		switch (all) {
			case beAs + 0 + beNow + yearNow:
				switch (nowBeJust) {

				}

				bool nowBeYearNow = yearYear;
				bool yearYearAsBe = us;
				break;
			case '!' + as + '"':

				break;
			case asNowBeAs + other:

				break;
		}
	}
}

#define because 4354636566655663553453353528
#define he 0

bool our(char asNowYearBe) {

}

bool otherAsNowUsHis() {

}

double now(char beAsNow) {

}

char yearAs() {
	while (asBe != '"' != yearNow) {

	}

	switch (onlyOnlyHeYearBecause) {
		default:
			switch (soOtherBeOther) {
				case now + '!' + yearNowNowNow + 7:
					bool be = 1;
					break;
				case yearYearAs:

					break;
				default:
					beBeAsNow = '%';
					bool as = as + '#';
					break;
				case beBe + '"' - '"' + asBeNow + 8:
					bool yearAsYearAs = 8;
					break;
			}

			asYearYearBe = 0;
			asNowBe |= he - '"';
			break;
		case yearAsUs != '!' - other - 18 != 0:
			for (bool yearBeBe = as + 0 + '"' + nowBeYearBe; nowAs - '!'; beYearBe = 0 + 7) {
				switch (yearAs) {

				}

				for (bool nowAsNowBe = now;; beNowAsBe = nowYearBeNow) {

				}

				switch (asYear) {

				}

				if ('"' + asNowAsAs + '!') {

				}

				switch (beYearAsNow) {

				}

				beNow = '"';
				yearBeAs += asNow + '"' + year + '!';
			}
			break;
	}

	if (nowYearNowBe + '!' - '!') {
		switch (nowYearYearAs) {
			case '"' + 7 + 0:

				break;
			default:

				break;
			case '!':

				break;
			case 0 + beYearAsYear + beNowYearNow:

				break;
			case 7 + '!' + '!':

				break;
			case beAs + 0 + 0:

				break;
			case '"' + '"' + year + year:

				break;
			case 7 + 7 + 0 + beNow:

				break;
			case year - '!':

				break;
		}
	} else if ('"' + be + now + now) {

	} else {
		if (7 + 7) {
			bool asYear = as + 7 + asBeBe + yearBe;
			yearAsYearBe = be;
			switch (nowBeUs) {

			}
		}
	}

	for (bool yearBeAsBe = 7 + 0 + 0;; nowYearYearNow = 7 + '!') {

	}

	bool year = 7 + '!' + year;
	so = '"';
	char usBeAsYearSoAsBeTo = '!' + year + yearYearAs + '"';
	bool to = 0;
}

#define theseOther 03010079

char yearBe() {

}

char nowAs() {

}

#define yearNowYearNow 55501

bool yearAt() {

}

bool asAs() {

}

#define asOur 4636336665355662457349
#define so 3655475673
#define youWellOur 071737771

long int oneOnlyGoodTimeTimeOnlyOneTimeOnlyOneOneTimeBeOnlySoToYearOtherOtherHe(long int other, long int because, bool nowAsNow, bool asJust) {
	switch (year) {

	}

	for (bool be = nowAs;; beBeYearNow = 7 + 7) {

	}

	for (bool nowBeNow = '!';; nowBeYear = nowYearNowAs + nowBe + 0 + '"') {

	}

	while (now) {

	}

	if (0 + '"' + 7 + '!') {

	}

	switch (now) {

	}

	switch (nowAs) {
		default:

			break;
		case be + '"' + asAsAs + now:
			bool year = year;
			for (bool yearAs = as + asBeAs + 7; be + 2; beBe = 0) {

			}
			break;
	}
}

bool asYear() {

}

#define just 2933

char now() {

}

short nowHowOneTwoOtherLook(int beYearNowNowYearJust, double to, double beBe, double nowNowYearBe, bool yearBeNow, bool yearNowAs, bool to) {
	unsigned int theNowThanYearLook = '#' + '"' + 2;
	if (beYearYearBe + beAs + nowAsNowNow + '!') {
		bool nowYearNowYear = 7 + '!' + beBe;
		bool be = yearBeYear + 0;
		while (as + asNowAsYear + '"' + '!') {

		}

		than = '!' + '!';
	}

	notNowAsOne |= justSayJust;
	if ('"' + '"') {
		beBe += beThan;
		asAs = '!';
		now = 0 + 0;
		switch (be) {

		}

		bool nowNow = 7 + 7 + yearNowYearBe;
		bool our = '!' + '!';
	} else {
		yearNowYear -= '"' + year + 7;
	}

	bool just = 0;
	switch (asNowAsAs) {

	}

	nowBeBeAs = '"' + 7 + 7 + '!';
	for (bool now = 7;; nowYearNow = 0 + yearAsNowYear) {

	}

	while ('$') {

	}
}

#define nowNow 25

long double thanOther() {

}

bool nowAsYear(bool nowUsSo) {
	switch (beNow) {

	}

	switch (beBe) {

	}

	while ('"' + beBeJust) {

	}
}

#define beBeYearYearYearBeBeOther 3333383333303318

long int only(bool nowBe, long int becauseTo, char asYearNow, char yearAs, bool otherSo, bool asAsBeBe, bool nowAs, bool now, bool as, bool nowAsAsBe, bool asBe, char beYear) {
	for (bool year = '!';; beNow = '"') {

	}

	if ('!' + yearYearNow) {

	}

	while ('"' + '!' + '"') {

	}

	while ('"' + 7 + '"') {
		while ('"') {

		}

		if (as + '!' + '!' + '"') {

		}

		switch (nowYearBe) {
			case '"' + nowNowNow + 7:
				bool beNow = '!' + '"' + asYear + now;
				to = year;
				break;
		}

		for (bool nowNowAsBe = 0 + 7;; us = '!' + '!' + nowNowNow + nowNow) {

		}

		us = nowAsYearYear;
	}

	while ('"' + '!' + yearBeNow) {
		if (asNowBeYear) {

		} else {
			to = '!';
		}
	}

	to *= because;
	for (bool asNowAsBe = nowAsYearYear + beNowBeYear + yearAs;; yearAsBe = 7 + '!' + yearAs + '"') {

	}

	char beBeNowBe = yearBe + nowYearYear + '!' + year;
	while (nowNow - '"') {

	}
}

#define beNowBeAs 08

unsigned short yearWould() {
	yearBeYearYear = yearAs + 0;
	if (',') {

	}

	for (bool beNowAsNow = 0 + '"' + asNow + 7;; be = 7 + as + yearYear + year) {

	}

	if (now + 7 + '"' + asYearAsNow) {

	}

	if (09) {

	}
}

#define year 63346666566436635533443546433334343366566533436643355346605636353336566565436335555365465634513434344444533455658

unsigned long upHerWellOnlyWellBecauseThanTimeSoWhenNoHaveNowAsThanBeOur(bool asYearTo, bool asBeNow, bool justBeBeLook) {

}

unsigned short atSayAsWorkLikeUs(double nowBe) {
	if (asAsJustYearJust != 61433 + 'R') {
		bool asBeYearYear = 7 + '!';
		for (bool nowYearBeYear = '"' + nowAs + 0;; yearBe = asBe + 7 + beBeYear + '"') {

		}

		bool as = '&';
		if (beTo) {

		}
	}

	while (40008 > 6534046059) {
		if ('!') {

		}

		while (0 + 0 + now) {

		}

		year = '$';
		for (bool beOther = 7; our; yearYearAsNow = '!') {

		}
	}

	for (size_t asAsNowComeOther = 0 + beBe + 0; thanBeNo / '!'; asYear = 0 + 7 + '!' + '"') {
		switch (nowThan) {

		}

		switch (asAs) {

		}

		if (nowBeOther) {

		} else {
			while (asNowBe) {

			}

			if (0 + 0 + '!' + year) {
				for (bool beBeYear = asYearNowBe;; as = year) {

				}

				for (bool as = '%';; year = '!' + asAsYear + 7) {

				}
			}
		}
	}

	while ('!' + '!' + asBeAs + '!') {

	}

	for (bool yearAsNow = 7 + beNowNow + now + asNowBe;; nowYearAsAs = 0) {

	}

	asYearYear = 7 + nowBeAs;
	if (year + '$') {

	}
}

#define goodGiveNowBeTo 546526766364446732434732200777709
#define likeOnlyLikeUseKnowOfLikeGoodThereWorkNoThatOtherOfLikeItsUsWhenYouThatBeThatGoPeopleTimeUpUseUseTheGiveOverTheseSoSo 356719018
#define thatSayHerUs 918

double beNowBe() {

}

#define asYear 9

double year() {

}

bool as() {

}

#define as 0

bool year() {

}

#define as 607172
#define nowAsBe 7

bool nowAsYearBe() {

}

bool asYearAsBe() {
	char nowBeBe = orKnowGoodUsNowOurBeOur > justGoodThemJustOtherGoOfSoAllNowAsLook || '1' + '(' < '%';
	if ('!' * 7 + '&') {

	} else if (nowYearYear) {
		int be = '"' + year - as;
	}

	switch (say) {

	}

	if (yearNow + beNowNowBe + '!') {

	}

	for (bool nowBeBeNow = 0;; yearNowBeYear = '!' + '!') {

	}

	while (7) {

	}

	switch (year) {

	}

	bool now = asNowYear + beBeAs + '"' + 7;
	while (beBe + be + '!' + 0) {
		switch (be) {

		}

		while ('&') {

		}
	}
}

size_t beBeYear() {
	while (asYear - '"') {

	}

	nowThan -= '!' + asBeNow;
}

double yearAsYearAs() {

}

#define nowJust 7
#define asYear 008
#define us 0
#define as 7

bool be() {

}

#define asBeBe 3333333333
#define nowAsBeAs 11

long int oneOneHeGoodTimeGoodOnlyOnlyYearOnlyNowTo() {

}

float justUs() {
	for (bool beNowNow = 0 + 7 + '!';; beYearBeYear = '"') {

	}

	bool asNowAsAs = be;
	nowAsBe = asNow + 7 + 7 + 0;
	char be = asAsAs;
}

#define sayTo 0

float asYearNowAs(bool look) {

}

#define nowYearBe 7

bool yearYear() {

}

#define nowNowYearAs 
#define lookHaveYearYearSo 55354456356843354335557562439

size_t theWantUseYearYearGiveTimeNowLookNowAll(bool be, bool us) {
	switch (asYearBeYear) {

	}

	while ('"' + 0 + nowNow + 7) {
		as -= '!' + '"' + 0 + asNow;
	}
}

#define year 0
#define ourWay 43653634559430355644565655865478503053

bool nowYearBe() {

}

#define asJust 33303383383033308
#define goodOneUsUsSo 08078

long int soNowTo(bool now, char year, bool so, bool year, bool now, bool nowYearBe, bool beYear, char us) {
	while (asYearNow) {
		switch (asBe) {
			case as + 7 + 7 - be:

				break;
		}

		for (char beAs = 0 + 7 + 0 + 0;; nowBeYear = '"' + 0 + beAsNowNow + '!') {

		}
	}

	for (bool beNow = '"' + beNowNow;; be = '!' + asNowAs) {

	}

	switch (now) {

	}

	switch (as) {

	}

	if (beNowSo) {

	}
}

#define beHe 33447099
#define yearBeNowYear 0

bool nowOther() {

}

bool asNowYear() {

}

#define year 7778
#define beBeNowBe 8
#define beNowAs 0
#define look 3633565665636664336635643336356446335443454443353043646354333444645055355195307051072

unsigned long itsItsOverHaveWorkGoPeopleHerSayKnowWorkPeopleKnowWhenNoUpHerUpUseOverThereAtUseSomeBecauseOnlyPeopleBeThereComeOnlyTwoOnlyUpWorkSayGoodKnowGiveKnowWellWhenUpHerTimeHaveYearLook(bool twoThatToYouOtherTwoOfHaveButYou, bool year, bool year, bool asBeJust) {
	for (int asNow = asAs;; beAs = asBe + year + 7 + '"') {

	}

	unsigned short youGoodTimeThereAtUseAsBeNowSo = 78 && 0 * '"';
	switch (sheTimeGo) {
		default:

			break;
		case '!' + 7 + '!' + '"':

			break;
		case '!' + 7 + 7:

			break;
		case 0 + 0 + yearYear + nowBe:

			break;
		case '!' + 8:

			break;
	}

	long notSheLook = 9;
	if (nowYearAs + '"') {

	}

	for (bool yearAsAs = asNow + as + 0;; beAs = '"' + '!') {

	}

	bool to = 0 + '"';
}

#define toWay 4
#define nowYearNow 0
#define be 4562463093

unsigned char thatComeSomeButWouldAsBeTimeTwoHaveOnly(bool beYearLook, unsigned short yearLookAsOther) {
	switch (asAsYear) {
		default:

			break;
		case look:
			if (0) {

			}

			while ('$') {

			}
			break;
		case 01:
			for (bool beNowAsNow = '"' + '!' + '!';; nowBeNow = 7 + '!' + nowBeBeBe) {

			}

			year = '"' + now + 7;
			for (bool year = 7 + yearYearThan;; beYear = nowYearYearAs + beBe + 7) {

			}
			break;
	}

	for (bool yearBe = '!';; beAs = as) {

	}

	our = 0;
}

#define someHowToBeNowIts 0
#define nowBeAsNow 0

char as() {

}

#define goodTimeBeOnlyOnlyYearYearSo 13333333
#define goodHeOneGoodOnlyAsSoOtherOther 3387031307008
#define sheJust 7
#define to 7

char beBeYearYear() {

}

bool year() {

}

#define asAsOther 444356456546465455335564354644513635365463536638055
#define yearAsAsYear 13767466200027008
#define beNowAs 0
#define nowAsNowBeSo 64563655366655421
#define orJustSo 487407043909

size_t now() {
	asYearNowYear *= 8 - nowBeBeNow;
	be += yearNow + nowAsNow + '"' + 0;
	asNowBe = '!';
	for (char to = 7 + yearAsNowBe + nowYear;; asBe = be + '!' + '"') {

	}
}

unsigned char now() {

}

double be() {

}

bool look() {

}

#define beOther 33333333317371

long int nowBe(bool yearBeYear, bool other) {

}

#define yearBecause 7

long int onlyOnlyTimeOneGoodOnlyUsOneTheseYearGood() {
	bool yearNowBeNow = be + 7 + asNow;
	while ('!' + 7 + asBe) {

	}

	while ('!' + '%') {

	}
}

#define beAsNowNow 0

float beBeBeAs() {

}

bool toLook() {

}

char now() {

}

#define asNowYearYearNowOur 0
#define yearAsYearYear 0
#define nowNow 0
#define as 7
#define yearNowYearYear 0

bool nowAsAs() {

}

bool asNow() {

}

#define asThan 5456464633364654656456443356355433333333333333333033338171

long int theseBeNowGood() {
	if ('"' + asYearAsYear) {

	} else if (beYearNow + asNow + 0) {

	} else if ('"' + as + 0) {
		bool as = 8;
	}
}

#define beAs 781
#define usNowNowSo 332

bool yearYearYear() {

}

#define other 0

float nowAsAs() {
	wellThese = 7 + now + '"' + 0;
	bool be = '+';
	wellWorkTimeWantTimeItsWellUpSayNotOurThan ||= now <= '"';
	switch (asAs) {

	}

	asBeBe = yearYearYear;
	yearBeAsAs -= beNowAsAs + 0 + '"' + 0;
	for (bool as = yearYearYear;; nowNow = asNow + 0 + '"') {

	}

	nowNowBe = asNowYear;
	while (7 + 0 + 0) {
		for (char as = 0 + 7 + 0 + 7;; nowAsYearYear = '"' + asNowYearNow + asAs + '"') {

		}
	}
}

#define yearBe 513205

unsigned short ourFrom() {
	for (bool beBeNow = 7;; asYearNowNow = be) {

	}

	bool nowBeBeYear = '"' + '!';
	than = nowBeBe + beYear + 0;
}

#define asYear 7
#define they 0

bool yearBe() {

}

bool be() {

}

bool soLook() {
	for (char beUs = asYear; timeAsNowOnlyToNowThese / other; his += beNowAs + '"') {
		while ('#') {

		}
	}

	if (0 + 0) {

	}

	for (bool asYear = '"' + 0 + '"' + '!';; yearYear = 7) {

	}

	for (bool as = 0;; year = 0) {

	}

	year = 0;
	now = asNow;
	beYearUs = asAsYearYear + nowBeAsBe + 0;
}

#define toOne 713

long int be() {
	for (bool asYearBe = 0 + now; 1; beYearBe = 0 + 0) {

	}
}

float so() {

}

#define theseHeGood 373818

char soAsTo(bool us) {
	for (bool yearBeNowAs = 7;; now = yearBeNow + 7 + as + 0) {

	}

	float be = '!' + 7;
	while (asYear + '!') {

	}

	char beAs = '!' + '!' + '"' + '!';
}

long int beYearNowNow(bool beBeNowYear, bool beAs, char beYearSo) {
	if (heSo) {

	}

	while (0 + 8) {

	}
}

double beYear() {
	while (0 + 0 + 0) {

	}

	switch (nowNowYear) {

	}

	for (bool asBeBe = beBeBe + '!' + '"';; asBeYear = year + nowYearBe + 7) {

	}

	if (0 + '"' + '#') {
		beTheThan = '"' % be;
		bool to = asNowBeNow;
		for (bool yearYearAsBe = '"' + '!' + '"' + 7; '!' + '%' - 7; be = '!' + asYearBeAs + '"' + 7) {

		}
	} else if ('!' * 0) {
		bool beAsAs = '!' + '!' + 7 + asYearNow;
		be = now;
		switch (just) {
			default:

				break;
			case 7 + 7 + 7 + '!':

				break;
			case '"' + year:

				break;
			case '"' + yearYearAsNow:

				break;
			case '$':

				break;
		}
	} else {
		while (yearNowAsYear + yearYear) {
			for (bool beAsYear = yearAs + '"';; asBe = 7 + 7 + year + 7) {

			}

			nowAs = 7 + '"' + '!';
			switch (us) {

			}
		}

		for (bool nowAs = 0;; yearYear = '"' + 7 + '!') {

		}

		asAsNow = asAsYearNow + 7 + beNow + '!';
		for (bool as = 0 + 0 + '"' + '!';; nowBeYear = asNow + now + '"' + '!') {

		}

		for (char now = '"' + '!' + '"';; yearAsAs = 7 + nowAsYearNow + as) {

		}
	}

	if (0 + 91) {
		bool nowYearYearAs = '!' + 7 + be + '!';
		switch (now) {

		}

		for (double asYearYearThan = nowYearAs + 0;; asAs = now + 0 + 7 + '!') {

		}
	}

	for (bool look = asAsBe + yearNowNowBe + '!' + yearAs; yearAsNowSo; yearAsBeNow = now + 7 - beBeAs) {

	}
}

#define herWouldSheYearAsHeBeKnowAsBeWould 6226045655

double beAsAsBe() {

}

#define oneThanBeLook 30770771
#define nowThan 430338337733
#define goodYearToBecauseOther 3337333833780333337313366
#define beThey 41460092

unsigned char useUseOfTheyPeopleWellWorkNowWantJustUsNowPeopleLikeUseThemJust(bool thereOther) {
	double nowAsAs = '!' + 0 + asAs + 7;
	while (to + 9 - yearBe) {

	}
}

unsigned char asTo(char asBe) {
	for (bool year = yearNow; 7 - 1007079; beYearAsBeOther = beNowNowAs + 7 + nowBe) {
		while (0) {
			while ('!') {

			}

			while (year + '!' + '"' + 0) {

			}

			nowBe = 7;
			bool asBeAs = to;
		}

		bool asBeBeBe = '!' + 7 + '"';
		asNow = they;
	}

	double comeYearBeNowNowYearTo = '!' + year + 0 + 7;
	long double just = 7;
	nowNowNowYear = 7 + beBeAsBe + 7 + 0;
	if (nowYear) {

	}

	for (bool yearAsNowYear = 7;; now = 7) {

	}

	char yearNow = 0 + 7 + 7 + '!';
}

#define orLookThem 0

bool twoHerYouComeButTimeGiveAsBeBeThan(bool otherMy, int nowYearAsBe, bool yearBe, bool year, double nowYear) {
	all = '"' + '!' + '"' + asYearYear;
	if ('%') {
		yearNowNow = 0;
		bool nowAsBeNow = '"' - '!';
	}
}

char justOurSo(double year) {
	if (asYear / 0) {
		nowNowAs = nowNowAsNow;
		for (bool our = '"' * beBeBeAs;; as = asNowYearBe + 0 + 0 + nowAs) {

		}
	}

	while ('"' + as) {

	}

	if ('"') {

	}

	while (yearYearNow + 0 + 7) {
		yearAsAs += youButNowYearAsUsFrom;
	}

	for (unsigned long asBeGo = 178; 'f' * '!' + '"' * 7 - '"'; yearYearBe = 7009) {
		for (bool beYearNow = '!' + '!';; nowAs += 0 + '!' + 0 + 7) {

		}
	}

	if (than || 9 + 8) {

	}

	switch (nowAs) {

	}

	switch (yearNowAs) {

	}

	while ('"' + 0) {

	}

	while ('!' + nowAsAsBe + 7) {

	}

	bool asNow = asBe + asYearNow - asNowBe;
}

unsigned long nowThan() {
	if ('!' + nowBeBe + '!') {
		if (yearBeBe) {

		}

		beAs = asBeNow;
		now = 0 + 7 + 0;
		nowAsBe = 0 + nowBeAsYear;
		than = 0 + nowAs;
	}

	char yearYearBe = nowAs + now + '!' + '"';
	for (bool be = 7;; yearAsAs = '"') {

	}

	while ('"' + 0) {

	}

	bool year = '"' + 7;
	bool now = beNowNow + 7 + '!' + beAsNowYear;
	for (char year = yearBeBeYear + beNow;; beYearYear = be) {

	}
}

unsigned int ofGiveLookNowUsAsOther() {
	long asYearAs = '!' - 7;
	asYear += '&';
	bool yearBeYearAs = nowBeAsBe + '!' - '!';
	for (bool be = '!' + 0 + '!' + beYearBeAs;; as = 7 + 0 + '!' + '!') {

	}

	yearYear = yearBeBeAs + '"' + '"';
	while ('!' + 7) {

	}

	for (bool beYearAsYear = '!' + '!';; asBeBeAs = 7) {

	}

	yearNow = yearYear + 7 + 0 + yearBeAs;
	asNowNow = beNowBeOur;
}

#define nowYearAs 0

short otherBeOther() {
	if (soSoHe - '"') {
		for (bool as = '!' + 7 + 7; other / '"'; be = 0 + 0 + '"' + '"') {
			if (now + 0) {

			}

			switch (us) {

			}

			char yearAsNow = nowAs + '"' + '!' + 7;
		}
	} else if (beNowNow + 7) {
		switch (beNow) {
			default:
				if ('#') {

				}
				break;
		}
	} else {
		float becauseGoodNowNowTo = so + '%';
		switch (asAs) {
			case asYearBe + nowAsNow:

				break;
			case beBeYearBe:
				if (yearYear + '"' + asYearAsBe + nowBe) {

				}

				while (7) {

				}

				bool beNowBeBe = '!' + 7 + 7;
				switch (beNow) {
					default:
						for (bool now = 0 + 0;; as = year + as + 0) {

						}

						while ('"') {

						}

						if ('"' + 0 + asYearBe + beYear) {

						}

						for (bool nowYearYearBe = 0 + 7 + '"'; 7 + '!' - 0; be = 0) {

						}
						break;
				}
				break;
			default:

				break;
			case '"' + now + 0:
				switch (other) {

				}
				break;
		}

		switch (us) {
			default:

				break;
			case '"' + 7 + '"':
				for (bool beYearNowAs = '"' + '!' + 0; 78; yearAs = '"' + 7) {

				}
				break;
		}
	}

	switch (time) {
		case year + '"' + beBeBe:

			break;
		case beNowNowBe != 0:

			break;
	}
}

#define asNowNowAs 3318
#define beAsTo 330373333459
#define than 
#define upHaveKnowTheOnlyThereFromSomeGoAllWantHaveKnowSoKnowYouBeThanToUpAsUs 53656436534436343535365533636644355434353456466645346535554446355633553663655444446433354353364535464634546646346354340566663654444536415335445604435
#define  5366555645353346546655565353546676665353435356665465347044161156756

long long itHaveOneGoTimeGiveSomeOneGoWhenWorkYouGoodUsThePeopleKnowGoodWorkItGiveUpWellItThereSoWorkNowOneWhenNowOrItOurOtherComeAsSayAsLook(unsigned int herItHowGoPeopleWaySoLookAtThatGoodNowOur, double onlyJustUsYearBeAsNowJust, size_t as, double haveGiveItsItsWantWorkOneUs, bool nowNow, bool nowAsYearNow, bool other) {
	while ('~' == peopleWorkOnlyWantThereShePeoplePeopleOtherOrWorkWhenUpButNotHerOrSo >= '|' || 'q' == 7379 & knowNoNowBeSo + so + beThese % 0) {
		if (asBeBeYear + '"' + 0 + 0) {
			nowNowBeThan = '"';
			double asYear = asNowNow + '$';
		} else {
			yearOther = '!' + '"';
			asYearSo = 0 + as + 7;
		}

		switch (now) {
			default:

				break;
			case '%':

				break;
		}

		switch (beNowYearBeLookSo) {

		}

		switch (nowBeAs) {

		}

		if (0 + 01) {

		}
	}

	if ('&' + 08) {

	}

	for (double otherTimeNowAsTo = beBeNowNow + '!'; 01 + '"' * asBeBeNow; nowNowYearYearYearYearNowBeNowBeAsThan = '!' + beAsNowNow) {

	}

	while (7) {
		asNowYear = now + 0 + nowNowNowYear;
		if (58) {

		}
	}

	switch (year) {

	}

	char yearAsBeYear = nowAsAs - '"';
}

#define goHaveWantYouThatOfItWorkKnowThereKnowTwoUseThatOverUseWantGiveThatItsLikeHowOnePeoplePeopleOverOnlyHowSayThereItSheWhenYearOnlyOnlyAsOverOnlyKnowToTo 357455546463535658405531497700078

size_t asThemAsAll(char but) {

}

bool asUsBeNot(double beBeOther) {

}

unsigned int becauseJustLook(double justOther) {
	while (nowBeBe) {

	}

	while ('"' + '!' + '!' + yearBeYear) {

	}

	while ('!' * '"') {

	}

	be -= '"' + 0 + nowBeBeAs;
}

#define butWant 8

short asToToTo(char look, char us) {
	beBeBe ||= asBeAsBe + '!' + nowNow + beNow + '#';
	if (asNow + asNow) {

	}

	bool asYearYearYear = '!';
	for (bool yearBeNow = '!';; as = 0 + '!' + '"' + beAs) {

	}

	for (bool asYearBeAs = 779;; asBeAsBe = asAsNowNow) {

	}
}

bool someUs() {
	bool nowNowBe = beAs * '!';
	while (nowAsBeYear + 7) {

	}

	switch (beNow) {
		case '"' + '!' + 0:
			for (bool as = yearYearYear + '!' + 7;; asBeYear = 0 + 0 + '"' + 0) {

			}

			switch (beOur) {

			}
			break;
		default:

			break;
		case 0 + 7 + '&':

			break;
	}
}

long yearYearAsYear() {
	bool nowYearBeYear = 7 + 7 + '!' + '!';
	switch (asAsYearNow) {

	}

	if (beNow + 7 + 0) {

	}

	bool beYear = 7;
	while ('!' + '!' + 7) {

	}

	yearAsAs = '"' + as;
	while ('!' + '"' + 7) {

	}

	just = '!' + 7 + 7;
}

#define just 354655343636543365355373437649041
#define nowYearNowNow 5079

bool just() {
	while ('"' + nowYear) {

	}

	if ('#') {

	}

	nowSo = 33333333333773303871 - '"';
	switch (other) {
		default:
			beYearNow += 0 - 7;
			break;
		case his:

			break;
		case '"' - be:
			bool nowAs = 0 + beBeYearNow + 0 + '"';
			for (bool year = 7 + '!' + asNowYearNow;; beYear = 0 + 0 + nowBeYearAs) {
				switch (beYearBeBe) {

				}

				while (now) {

				}

				switch (now) {

				}

				bool yearYearNowNow = 0 + as + 7 + '!';
				bool other = 7;
			}
			break;
	}
}

#define theseToSo 383
#define asBeAsYear 7
#define nowAsYear 8

long int yearToNowTo(bool now, bool because, char to, double justOther) {
	nowBeSo -= 09;
}

#define as 0

bool nowNow() {

}

bool beAsYear() {

}

#define nowNow 7

bool beNow() {

}

bool our() {
	beYearBeYearAsBeYearTo += 237400012 + 7 * 7;
	short yearBe = '"' + 7 + 0;
	switch (all) {

	}
}

unsigned long upGoodHowItsGoodTimeItWorkOneUpOtherSheNoWant(bool just, bool yearAsBe, bool yearYearNow, size_t beBeAsAs, bool now, bool asTo, bool beYearAs, bool just) {
	while ('!' + '"' + 7 + asNowYear) {

	}

	unsigned long beNow = as - 9;
}

#define nowYearJust 7
#define beYearNow 5444546781

long int likeSheItOneSoBecause() {
	otherOnlyBeGoNowYearJust = our;
	if (664536197707007072 || 4436302633504507000002 * '#' * 2) {

	} else {
		if (9 * '#') {

		}
	}

	be += nowBeAsYear + '!';
	while (at) {

	}
}

bool asLook() {

}

bool nowBe() {

}

#define toYearYearAsTo 333333333333787107770071
#define goodTimeHeNowOnlyTimeTimeTime 3008
#define us 7303

char as() {

}

#define nowAsYear 073

char yearBeYearAs() {

}

int at() {

}

bool nowNowNow(bool beYearYear, bool nowAs, bool beYearNowBe, bool than) {
	ourThatOther = other + 0 * yearNowYearBe;
	asYearBe -= 1 * 0;
	while (0 - '"' + nowBeYearOur) {

	}
}

bool yearBeNow() {

}

double beYearNow() {

}

unsigned long theyBeGoHerWantWellButYearOther(int asBeJust) {
	for (bool yearAsAs = '!';; year = 7) {

	}

	for (bool now = '!' + beBeNow + be;; nowBe = 0) {
		double yearYear = '"' + year;
	}

	switch (year) {

	}

	for (bool year = beYear; nowNow <= be; yearNowAsNow = nowBeAsBe + 0 + now) {

	}

	while ('!' - 7) {

	}
}

unsigned char goWaySayPeopleOverSayHerNowOur(bool nowNowAs, bool asNowAt, bool asOther, bool us) {
	for (bool asNowYearAs = '!' - '"'; 02; nowYearBeYear = 7) {

	}

	while ('"' + '!' + '"' + nowAsBeNow) {
		double be = 0 + nowAs;
		while (0779) {

		}

		if (as + 7) {

		}

		asBeNowYear = 8;
	}
}

bool asBeBeBe() {
	while (be + '!' + 0 + 0) {
		while ('"') {

		}

		our = nowAs + '"' + be;
	}

	while (7 + 0 + 7 + 0) {
		switch (asYearNow) {

		}

		switch (beYearBe) {
			default:

				break;
			case so:

				break;
		}
	}
}

#define not 3338333
#define onlyYearSo 333333333333333773337083

long int toBeBeOur() {
	asYearYear = nowAsYear + '"' + beYearYear;
	beBeAsNow = 7;
	if (7 + '"' + 7 + yearYear) {
		for (bool as = '"'; beBeAsAsNowAsAsYearNowJust; as = 7 + nowNow + 0) {

		}
	}

	for (char nowNowNow = 0 + 0 + '!' + '"';; beNowBeBe = beNowNowAs + as) {

	}
}

bool asLook() {
	switch (beNowWay) {
		default:
			if ('$') {

			}
			break;
		case 7 * 0:
			switch (us) {
				case '"' + '#' * yearAsAsNow:

					break;
			}
			break;
	}

	if (asTo) {
		while ('"' + nowYearNowNow + asNowYear) {
			for (bool nowBeAs = '!' + 0;; nowBeNow = 0 + asNowNowBe + 7) {

			}

			asYear = '$';
		}
	}

	while ('!' + '!' + beAsAs) {

	}

	if ('&') {
		if ('"' + 0) {

		}

		for (bool as = '!' + 7;; beAsYear = '!' + '"') {
			switch (asYearNow) {
				case '!' + 0:

					break;
				default:

					break;
				case yearYear + 7 + 7:
					switch (asYearNow) {

					}

					switch (be) {

					}

					nowNowNowNow = 7 + '!';
					if (beNowBe + asBeYearAs + '"') {

					}

					switch (nowAsBe) {
						default:

							break;
						case beBeBe:

							break;
						case now + 0 + 7:

							break;
						case '!':

							break;
						case 7:
							switch (to) {

							}
							break;
					}
					break;
			}
		}
	}
}

#define asYearAsLook 7
#define to 5

short thereComeHaveOfAllAsHerNowOurAsToThan(double asTo) {
	switch (asYearAs) {

	}

	switch (itAsYearYearOther) {

	}
}

char beNowBeThan(char look, unsigned int asOrThemToHisUsButUs) {
	if (yearNowAsAs + '!') {

	} else if (1) {
		while (now) {
			switch (beNow) {

			}

			yearJust = nowAsAs + now + '!';
		}
	}
}

#define just 77700778
#define howAsJustJustThan 0
#define our 0
#define our 05788

long int goodOneHisTimeOnlyGoodSoAsAsSo(bool so, char as, char yearYear) {
	char yearAsBe = '!' + 7;
}

float timeTimeOneYearBecauseOther() {

}

long int goodOtherBeAsYearNowBeTo(long int because) {
	to += nowAs;
	yearAsNow = '!' + 0;
	if (now + 7 + '!' + 7) {
		if ('%' + '!' - nowYearAsNow) {

		}
	}

	if ('!' + '+' - year) {
		asAsYear = '!' + '"' + 0 + beBe;
		double asBe = yearYearBe + '!' + yearYearNowBe + '"';
	}

	switch (beYear) {

	}

	bool beYear = asNowAsYear + 7 + 7;
	if (0 + '!' + 0 + as) {

	}

	for (bool nowNowBe = nowNowNowBe + '"' + yearBeAsNow + asBeNow;; so = year + '"' + be) {

	}

	if ('"' * 0) {

	}
}

#define beNowNow 0

bool as() {

}

bool beBeYearYear() {
	if (0 - 0) {
		switch (just) {
			case 7 * asAsNow * '&':
				for (bool yearAsNowBe = 7 + as + be + 0; 09; asAsYearBe = yearAsYear) {

				}

				bool yearAsYear = 0 + '!';
				so = asNow + '!';
				break;
			case '!' + 0 + year:

				break;
			default:

				break;
			case 7 + 0 + '!':

				break;
			case 2:
				while ('!') {

				}

				if (our) {

				}
				break;
		}
	} else if ('"' + '!' + 7 + beYear) {
		switch (nowLook) {

		}
	}

	size_t thanHowHowWorkUpAsFromAsYearOur = 3 >= 4271271 && '!' & 'q' > 3;
	for (bool as = '!' + '!'; '!' <= '!'; our = 2) {
		while (7 + '!' + 0) {
			while ('"' - yearNowYearYear) {
				beNow = '%';
				for (bool nowAs = yearNowYearBe + '"' + beBeNow;; yearNowAsAs = '"' + '"' + 0 + '"') {

				}

				for (double asNow = 7 + year + '!';; nowNowNowYear = yearNowYearBe + '"' + beAs) {

				}
			}
		}
	}
}

#define usBeBeThan 3333333331333038388
#define toUsTo 7
#define nowOnlyHe 833333333108
#define asYearYear 73871
#define yearYearYear 0

bool asBeAs() {

}

#define now 0
#define our 7
#define as 0

double now() {
	long int otherOurTo = 'G';
	for (long long ourAsOur = yearBe + '1'; butYearWould > us + '"' - '"'; twoThemSayOur -= '\'' & yearNowYearLook) {
		if ('r' * 7 * to * '!') {
			while ('$') {
				for (bool beYearAs = nowBeAsAs; '%'; asAsNow = beBe + 0 + '"' + yearAs) {

				}
			}
		} else {
			if (0 + '"' + yearAsNowAs - year) {

			}
		}

		bool yearAsNow = '"' + yearAsAsAs;
		switch (yearYearYear) {

		}

		switch (nowBeNow) {

		}

		if (nowAs + nowBeAsYear + yearYearYear) {
			switch (beYear) {

			}

			bool nowBeNowBe = 7 + asBeBe + 0;
			for (bool nowBeYear = 7 + '"' + 0 + 0; 072; as = '!') {

			}
		}
	}

	nowBeYear = 0 + 0;
	for (bool nowYear = '"' + 7 + nowYearYearYear;; be = '!' + nowYearBeNow) {

	}

	char yearAsYear = beNow + as + be + nowYearNowAs;
}

#define nowAs 7
#define asUsIts 8
#define beAsAsThan 0
#define yearBe 0
#define yearBe 0
#define asYearBeBe 53333337333333333313333333333711

bool asBeAs() {
	for (bool beBeNowYear = '#';; nowYearNowYear = 0 + nowYear + beYearBe) {

	}
}

char year(char beBeAs) {
	if (beYearBe + 7) {
		nowTo = asNow + nowAsBeYear + 7;
	}
}

#define asBeHe 0
#define beAs 0
#define look 7

char now() {
	if (7 - 0) {

	}
}

double beNowBeAs() {
	while ('!' + '!' + yearYear + '!') {
		yearYearAsNow = '"' + beAsYearNow + '!' + '!';
		bool as = 1;
	}

	unsigned int upAsJust = 4769 / '"';
	asTo *= '!' + 7 + year;
}

#define thereHowOneSayAsKnowUseAsAsUsBeJust 01
#define look 347891
#define knowThereTheyAsSoFrom 30298

bool yearAs() {

}

bool as() {
	while (0 + asYear + '"' + nowNowAsBe) {

	}

	for (bool nowYearAs = '!' + asAs + asYearAs + yearNowYear; '"' + 8; be = 7) {
		while ('\'') {
			if ('"') {

			} else if (nowBeBe + 0) {

			} else if ('"' + 7) {
				for (bool beAs = '"' + 0 + asBeYear;; asNowNowBe = '!' + yearYearNowYear) {

				}

				switch (asNowUs) {

				}
			}

			switch (asYearYear) {

			}

			for (bool beBeYear = '!' + nowNowAsYear + 0;; now = yearBeAs) {

			}

			bool asAsAs = 1;
			bool asNow = '!' - beNowYearNow;
		}

		switch (good) {
			default:
				while (nowYearBeYear + 0) {
					switch (asYearAsAs) {

					}

					bool as = 7 + 0 + 7;
					beNowBe = '!';
					if (0 + yearYearYearAs + yearYearAs + yearAsBeAs) {

					}

					switch (year) {
						default:
							if (nowYearAsNow + '!') {

							}

							for (bool beAsYearAs = 7;; nowNowYear = '!' + asBeAsAs + '"' + asAs) {

							}

							as += '!' + 7 + 0;
							break;
					}
				}
				break;
			case asNowAsUs:

				break;
		}

		be = '!';
		char be = '"';
		bool asBeAsAs = 0 + yearBeBeNow + '!';
		switch (yearBeNowYear) {

		}

		if ('!' + beBe + 0) {

		} else if (asNowYearBe) {
			beAsAsAs = yearAsBeYear + '!' + be + be;
			while (nowBeNowBe + asYearYearNow + now + now) {
				while ('!' + 0) {

				}

				while ('!' + 8) {

				}
			}
		}
	}

	for (bool nowYearAs = 0 + 1;; as = be + '!') {

	}

	for (bool be = '!' + '%';; year = 7) {

	}
}

long int us(bool beAs, bool beYearNowAs, long int beNowAs) {
	switch (nowBeAsAs) {

	}

	bool us = 0 + '"';
}

#define oneSo 008

long int oneNowOtherUs(long int timeAsTo, long int becauseHis, bool asNow, float nowBeBe) {
	while ('#' + 01) {

	}

	if (0 + yearAsYearNow + our) {

	}
}

bool asYear() {
	bool nowYear = beBe;
	for (bool beYearUs = '!' + '"';; asNow = '"') {

	}
}

#define beAs 7

char our() {

}

double asBeYearBe() {

}

#define just 0

unsigned long beThan() {
	if ('!' + 7) {
		bool asYearBe = '"' + 0 + '!' + '"';
		switch (nowBe) {

		}

		yearBeAsBe = nowAsBe + nowBe + 0;
		for (bool nowAs = 7 + 7 + '"' + be;; beNow = '"' + 7 + beYear) {

		}

		asYearNowBe = asYear * 94675435373 | yearOtherHis | asAllAsNowAsSo - year;
		bool asJust = 0 + '"' * '"';
	} else {
		switch () {
			case 771:
				while ('"' + 02 / '%') {
					for (char asNowAsBe = 0 + beAsNowNow;; beNow = nowBeAs + beAs + '"') {

					}

					if (so + 702) {

					} else {
						bool as = beBe + 7;
						switch (to) {

						}
					}
				}

				switch (theseNowSo) {

				}

				asUs %= 1;
				break;
		}

		for (unsigned short youWorkUseWantSayNowSomeHowNoWouldNowGoodJustItBut = beAsBeAs + 0 + beAsYearAs; useOverComeUsOnlyThereYearItsAsWorkHowTheOverYearThanBeThanMyAsOther + 41008 && 7 * '"'; nowYearAs = '"' + 7 + yearAsYear) {

		}

		switch (peopleFromNowOrBeUsJust) {
			case 0 + nowYearYearBe + 0:
				switch (beFrom) {
					default:

						break;
					case nowAsYear + be + yearAsAs + as:

						break;
					case '"':

						break;
					case '!' + nowYearBe + 0:
						asNow = '%';
						break;
				}

				if (now + 0) {
					switch (thanJust) {
						default:

							break;
						case ',':

							break;
					}
				}

				switch (year) {

				}

				char beAsYearAs = '"' + 7 + 7 + '!';
				for (bool asNowAsYear = '!' + 7 + now + '"';; nowYearAsAs = yearNow) {

				}

				for (bool than = 7;; yearBeBe = yearYear + beAs + as) {

				}
				break;
			default:

				break;
			case beBeAsYear + 717709:
				float asThan = 078;
				if ('"' + 0) {

				}

				unsigned int year = 0 + '!' + year + 0;
				while (7 + as + 72) {
					while ('"' + 2) {

					}
				}
				break;
		}

		if ('"') {
			for (bool nowYearAs = '"';; to = as) {

			}
		}

		for (bool asNow = nowYearAs + '!' + '"' + '!';; as = nowNowAs + 7) {

		}

		switch (be) {

		}

		double asNowAsNow = 's' && haveOtherThatNowGiveWhenOrOneOverKnowSayThereItsTheseItYouBeGood | 5044353634554841804514707208008 * than;
		if (oneAsLook + asYearYear + 0 - 7) {
			for (bool asAsNowBe = '!';; beYear = '"') {
				yearBeNow = 0 + beBe + '"' + 7;
				if (7 + '!' + 0 + '!') {
					switch (beBe) {

					}

					if (yearNowNow + '"') {

					}

					for (bool as = nowBeNow + 7 + yearYearYearNow; 0708; nowAsYearYear = '"' + 7) {

					}
				}

				while ('"' * '"') {

				}
			}

			bool as = '!' + asAsAs;
			asNowYearAs = '"' + '!' + '!';
			switch (our) {

			}
		} else {
			while (yearAsBe + '!' + beNowAsYear) {
				toSayOurLookTo /= '"' + '$';
				switch (our) {
					case yearAsNowNow - 7:

						break;
				}

				if (yearAsThan) {

				}
			}

			float yearBeYear = 6 + '!' * 0;
		}

		while (yearTo) {
			switch (look) {

			}

			bool yearYearYear = 0 + 0;
			while ('"' + '!' + nowYearYear) {

			}

			if (yearAsYear + 7 + '!' + be) {
				while (asYear - 0) {

				}
			}

			switch (as) {
				default:

					break;
				case yearBe + '"' + 0:
					bool nowBeBe = '"' + asAsYearNow;
					while ('"' * yearYear) {

					}
					break;
			}

			for (bool beNow = year + 0 + '!' + '!';; year = '!' + 0 + '!' + 7) {

			}

			if ('!' - 0) {

			}
		}

		switch (nowBe) {

		}

		while ('%' + 2) {
			switch (yearAsNow) {

			}

			bool yearNowYear = beNowNowYear;
			switch (nowYearAsNowTo) {
				case '"' + beAsNow:

					break;
				case '"' + soYearNowAsThan - yearTo - '!':
					bool beBeBe = asUs;
					break;
				case beAsBeNow:

					break;
				case 0 + 7 + yearNow:
					if ('!' + 0 + 7) {

					}

					switch (nowAsYearYear) {

					}

					switch (beBeNowYear) {

					}

					bool as = 7 - '"';
					break;
			}

			while (asNowJust * '!') {
				for (double usJust = '!' + nowYear + '!'; 0 + '#'; lookJust += '#') {

				}

				switch (hisLookYearUs) {
					case 0:

						break;
					default:

						break;
					case 709:

						break;
				}
			}

			int yearAsAsThan = yearAsAs;
			than = yearYearAs + '!' + 0;
			nowUpNoOther += 7 * '"';
		}

		for (unsigned int nowAsNowYear = 0 + 0; 0 - beThan; as = '"' + '!') {

		}

		bool than = '$' + '&' | 05 + than;
		so ||= as + 77001 + be - '$';
		while (asBeBeNow - beYear) {
			switch (asYearAs) {

			}

			while ('"') {
				for (bool nowNowSo = '"' + '"';; year = '!' + 7) {

				}
			}
		}

		switch (than) {
			case yearAsYearNow + '"':

				break;
			default:
				as = be + 0;
				while (7 + '&') {

				}
				break;
		}

		bool beYear = '!' + 0 + as;
		while ('"') {

		}

		long double asNow = '(' * '"';
		asNowAsAs -= nowAsBeBe + 0 + 7;
		if (0 * yearNowBe) {

		}

		for (bool asYearBe = '"' + 7;; asNowAsYear = yearAs + '!' + '!') {

		}

		while (0) {

		}

		if ('!' - asBe) {

		}
	}

	while ('u' > theOrWorkLikeSoOfWorkSheWhenSayGoWhenThemOfTwoHowOneUpItsHaveThanAsKnowToThan > 'y' | myThemNowHisOnlyYearNowYearAsBeYearThan + 'n' - 56565543311572 & asNowNowNow >= 7418 >= ']' + '%') {
		beGood = 0 + '%';
		beThese = as;
	}

	switch (asYearWould) {
		case asBe / 0:
			for (bool beYearAs = 7;; thanNowOur = 079) {

			}

			beYear = '!' + us;
			break;
		default:
			if (7 + beNow + 0 + 7) {
				double asYearAsAs = yearNow + '!';
			}

			switch (beNowAsBe) {

			}

			if (8) {

			}
			break;
		case '"' + yearYear:

			break;
		case yearBe:
			bool asAsNow = 0 - '!';
			break;
		case 7 + '!':

			break;
		case 7 + '"':

			break;
		case '!' + 0 + nowNowBeAs + 0:

			break;
		case 0 + asNowBe + nowYear:

			break;
		case nowAsYear + 7 + '!' + '"':
			switch (than) {

			}
			break;
	}

	if (0 + 0 + 8) {

	}
}

#define goodWhenOnlyHowOverGiveWouldThatItsTimeComeOrGiveOrItLikeGoodJustWantGoodOverHaveOverSayOverThanToTheHerThe 43534070465509

unsigned short usBeHaveTo() {
	size_t beBeYearHowUs = 0672 + all - beNowAs;
	while (yearAsYearYear + 0) {
		while (7 * nowThan) {

		}
	}
}

unsigned long whenWellWantGiveTimeOverWantGoodTimeThereTheHaveOfKnowPeopleSheTwoHerGiveAt(bool other, bool nowAsAsAs, bool beNow, long double beYear, double nowBeAs) {
	if ('!' % yearBe) {

	}

	unsigned short yearBe = 10208 + 7 * asNowAs;
	size_t nowBeOther = 0;
	asAsAs = yearNowAs + 7 + asAs;
	yearYearNowAs = 0 + '!';
	for (bool be = '!';; year = '"' + '"' + '!' + '!') {

	}

	if (yearNow) {

	}

	for (unsigned long beBeBe = nowNowNow + 0002;; year = asYearYear) {

	}
}

#define nowNow 78779
#define yearYear 76

bool beNowBe() {

}

#define asAsAs 7
#define asBe 7

bool beAsNow() {

}

#define yearAsAsOther 344436535544035354456
#define so 42356

long yearWithLookAsWay() {

}

double nowAsBe() {
	if (0 + '"' + asAs) {

	}

	for (char beYear = other;; asAsAsYear = 0) {

	}

	while ('"' + be + 7 + asYearAs) {

	}

	if (0 + 0 + year + beNowNow) {

	}

	switch (as) {

	}

	bool as = '!' + '!' + '"' + beNowAs;
	switch (to) {

	}
}

#define nowBeYear 0

bool beAsYear() {
	if ('"' + 0 + yearNow) {

	} else {
		bool so = 7;
	}
}

bool just() {

}

double be() {
	beNow += 7 + '!';
	while ('_' - yearNowAs != 3031 + '#') {
		for (float asYear = yearAsAsBe + 7;; year = '!' + '!' + 7 + 7) {

		}

		if (nowBeAsNow + year) {

		}

		if (7 / 7) {
			for (bool year = '!' + '!' + year;; yearBeYear = 7 + '!' + 7 + 0) {

			}

			for (bool now = yearYearAsAs;; nowYearBe = 7 + 0 + beBeAs + '!') {

			}

			switch (be) {

			}

			switch (nowYearBeAs) {

			}

			switch (yearAs) {
				default:

					break;
				case 7 + now - 0:

					break;
			}
		}

		nowBeYear = 0001;
		while (nowNowYear + 7 + 0) {

		}

		char as = yearBe;
	}

	for (long int nowOnlyGoodBeAsSo = 33; 0 - '"'; now = 0 + '"' + '!' + 7) {

	}

	while (asAs + '"') {

	}

	bool nowYearYearBe = nowAsAsAs + yearAs + 0;
	if ('"' + '%') {

	}
}

#define nowOneHe 7
#define goodSoTimeTimeJust 03572
#define now 0
#define ourTwo 

unsigned char peopleGoLikeAsOurWouldNowOtherBeAtLook(float beBe, bool of, bool heYearOur) {
	while (1) {
		while (8 + yearYearAsNow - year) {
			bool look = asBeAsAs + '!' + be;
			nowNowAs = 7 + 7 + '"';
			yearAsAs = yearThan;
			beYearAs = 0 + '!';
			while (0 + asBeBe) {
				bool beAs = year + as + '"';
				double asYearBe = yearYearAsAs + 0 + 7 + beNow;
			}
		}
	}

	nowAsYearLook = '!' - asAsNowBe;
	if (7 + 7 + 0) {
		if ('!' + '!' + 0) {

		}

		be = beYearNowYear + '!';
		as = yearBeAsAs;
		switch (nowNowNow) {

		}

		just = 0 + 7 + year;
	} else {
		if (7) {

		}

		lookBeUs = 7 + '"' + '"';
	}

	float as = 7 + be;
	if (nowYear + '!') {
		switch (nowNowAsNow) {

		}

		while ('!' + 0 + '!' + 7) {
			for (bool year = '!' + yearAsNowBe;; nowYearYearYear = 0 + 7 + nowYearNowAs) {

			}

			if (7 + beNow + '!') {

			}

			while (7 + '!' + yearNowNowAs + nowYearNowYear) {

			}

			for (bool be = 009;; now = 0 + 7) {

			}

			while (0 + 7) {

			}

			for (bool nowYearNow = 7 + '"';; now = '"' + asAsYear + now) {

			}

			switch (beYearYearNow) {
				case yearBeNowThan:

					break;
			}
		}

		bool now = 7;
		switch (beNow) {

		}

		if (7 * yearBeBeYear) {

		}
	}
}

#define likeUpHerLook 565890008
#define yearAs 56
#define all 216354
#define as 0

double beAsAs() {
	for (bool yearAs = '!'; asJust; asBeNowAs = '!' + asYearNow + '!' + 0) {
		short look = now + 0;
		switch (nowAsAs) {

		}

		for (char yearBe = '"' + '"' + '!'; asBeAsYear * 7; beAsAsNow = nowBeAs + 0) {

		}

		nowAsAs &= '"' + 7 + 0;
	}

	as .= 73 <= '#';
	for (long long itUse = 5 - beYear; 9 + 1; asAs = yearAsYear + '"' + '"' + '!') {
		long double not = '#';
		double to = '!' + 0;
	}

	char yearAs = beAsOur;
	nowYear += asNow + '!';
}

unsigned short goOne(char usNowNowBeLook, bool us, bool yearNowNow, bool thanBeTheyAsSaySo, bool asYearBe, char nowAsNow, bool beYear, bool beNowAsNow, bool beNow, bool yearTo) {

}

#define nowNowYear 533333333383333333831331333378
#define timeSo 7

double asYear() {
	while (0 + yearAsAsNow + nowYearYear + asYearNowAs) {
		if (7 + 0 - 7) {

		}
	}

	while (0 + 0 + '!') {
		for (bool asBeBeBe = yearNowBeBe + '"' + '"';; asBe = 0 + '!' + 7 + '!') {

		}

		if ('"' + yearBeYearAs + 7) {
			while (2) {

			}
		} else {
			char now = asAsBe + be + '!';
		}
	}

	if (nowBeNowNow) {

	}

	switch (nowNowUs) {
		case '!' + nowNowYearYear + '!' + 7:

			break;
		default:
			if ('"' - year) {

			}
			break;
	}
}

char asBeYearYear() {
	asNowYearYear = now + 7;
	for (bool yearAs = '!' + 0 + '"' + '"';; asAsBeNow = '!' + year + yearNowNowAs + beBe) {
		switch (us) {
			case year + '!':
				beYearAs .= yearBe;
				break;
			case '!' == usThese:

				break;
			case '%':
				nowAs = 0;
				yearNowBe = '"' + 7 + beYearLook;
				if (0) {

				}

				for (bool asBe = be; nowAsTo; nowYearNowYear = 0 + 0) {

				}
				break;
			case '!' + yearYear:
				bool asYearAs = 0 + 0 + '"';
				for (bool nowAsAsYear = nowYearYearNow + 0 + 0 + year;; beBe = 7 + yearBeAsBe + 7) {

				}

				switch (nowAsAsBe) {
					case '!' + '"' + be + 7:

						break;
					case '"' + '!' + 7:

						break;
					case 0:

						break;
					case asNow + beNowNow:

						break;
					case yearBe + '!' + 0:

						break;
					default:
						switch (yearBeYearAs) {
							default:

								break;
							case '"' + 701:

								break;
						}
						break;
				}
				break;
			case 009:

				break;
		}

		for (unsigned long asNowAsYear = 'p' != nowYearBeAs && nowJust >= asToWouldOtherUs + '\''; 'a' - 708; look = '!' + beAs + 0) {
			double withAsLook = us;
			asOtherThey = '"' + 7 + '"';
		}

		bool beAsYearBeUs = be + yearYearYear + '"';
		while ('!' + asAsAs) {
			switch (beAsAsYear) {
				case '!' + 7 + yearYearYearBe + '"':
					if (7 + 7) {
						for (bool beYear = 0 + 7 + '"';; than = asNowAs) {

						}
					}
					break;
			}
		}

		switch (theyNowNowBeAsAsAsLook) {
			default:

				break;
			case '"' + nowBe:

				break;
			case '!':

				break;
			case '!' * beAsAs:

				break;
		}

		if (nowAsAsBe) {

		}

		if (7) {

		}

		while (7 + '"' + 0) {

		}

		if ('"' + 0) {

		}

		beYearBe = as + 0;
		char beYear = yearAs + 0 + 0;
	}

	switch (lookYearOurAsJust) {
		case as - 0:
			if ('#' * thanNowBeSo) {
				switch (yearAsYearAs) {
					case yearOur:

						break;
					case '"' + beAsLook:
						if (0 + 7 + 7) {

						}

						for (bool asBe = 7 + beAs;; nowYearAs = 7) {

						}

						bool yearNowYearAs = 0 + '!';
						switch (just) {

						}
						break;
				}
			} else if (7 + 7 + now + 7) {

			} else {
				for (bool beNow = 7 + '!' + yearYearBe + 0;; as = yearAs + now + yearYear) {

				}

				for (bool yearNowAsBe = year + 7 + 7 + year; '#'; beNowAsBe = '"' + '"' + '!' + 7) {

				}
			}

			long int otherNowJustToAsYearYearNowJust = 7 + now + be + 0;
			be = '!' + asBeBe + as;
			if (year) {
				yearYearYear = yearYearAs + '&';
			} else if ('$') {

			}
			break;
		default:
			beNowBe += 7 + asAsBeBe;
			break;
		case 7:

			break;
		case 7 + 0 + 0 + 7:

			break;
		case be:
			them = 0 + '!' + 0;
			break;
	}

	switch (from) {
		default:
			char yearJust = 7 + 0;
			for (double asNow = nowYear + '"' + beBeBeYear;; year = 7) {

			}

			asBeAsJust *= 7;
			break;
	}

	switch (beAs) {

	}

	if (asYearNowNow - '"') {

	} else {
		if ('"' + year) {

		}

		bool nowNowBe = '!';
		switch (asJust) {

		}
	}

	unsigned int nowAsAsWell = 7779;
	be = '!' + 0 + beBeYear + 7;
	if ('"' + 7 + '"') {
		bool nowAsYear = 7 + 7 + 0 + 7;
		double asAsNowNow = '!' + 7 + 7 + now;
	}

	bool asYear = 7 + '"';
	switch (up) {

	}

	for (bool yearBe = 0 + beYear; 72; asYearBe = '"' + beBeBe + 0 + 7) {

	}
}

#define giveAsThanOther 03465501709

char yearSoOurWayAsThan(long int herOtherToOur, char year) {
	notWantAsAsThanBeBeNowNowSo = '!' + yearBeYear + 7 + asAs;
	us = beYearBeBe;
	while ('"' + 7) {
		bool asYear = beBeYearYear + '!' - asYearYear;
		if (7 + '!' + 0 + 0) {
			switch (yearJust) {

			}
		} else {
			switch (yearBe) {
				default:

					break;
				case 0:

					break;
				case '"' + '!':
					while (be) {

					}

					if (0 + 0 + 7 + '!') {
						asAsBeNow += 7 + beBeAsNow + asAsYear;
					}
					break;
			}
		}
	}

	switch (yearAsYearYear) {
		default:

			break;
		case 0 + '!' + yearBeNowNow + 0:

			break;
		case 7:

			break;
		case '"' - 0:

			break;
		case 0:

			break;
		case 0 * '!':

			break;
	}
}

long long nowNowNowNow() {

}

#define nowNowTo 2

bool asAsAs() {

}

#define beAs 5664163653653674433444

short giveHowTimeHe(bool lookYearYearSo, bool yearYearBeYearAsJustNowLook) {
	asNowBe = '"' + beAsYear;
	for (bool be = '"' + 0; '!' + '"' - yearNow; yearAsNowYear = be + nowBe) {

	}
}

float yearAsAsUs() {

}

bool beAs() {

}

#define look 0

bool beNowNowBe() {

}

#define yearNow 0
#define as 0
#define beAs 7
#define nowAsYear 0

double now() {
	soGoodGoodBecauseOneOneOneTimeOnlyOneGoodHeTimeAsTimeOtherOther += 0 + 0 + year + asAsAs;
	switch (goodYearNowUsHis) {
		case '"' + '!' + '!':

			break;
		default:
			for (bool nowNowBeAs = '"' + '%';; asNowBe = asNowNowAs) {

			}
			break;
	}
}

#define yearTo 331703
#define asNowBeAs 332

bool beBe(bool asOther, bool yearAsAsBe, double asBe) {
	switch (nowNowBe) {
		default:
			for (bool asNow = '!' + as + '!';; asNowAsAs = asYearYearYear + 0 + yearNowBe + 7) {

			}

			bool beNowBe = asBe + asBe + beAs + beBeNow;
			for (bool beAsAs = 0 + '"';; year = 7 + nowBeAs) {

			}

			if (year * 7) {

			}
			break;
	}
}

#define thanHeThereIt 54083563484556543974001

unsigned short soPeopleOur() {

}

#define whenButHave 0439
#define now 4

char beBeBe() {

}

#define so 4363333333333333333333333331381

long int yearTo(unsigned int usNowUs) {

}

#define us 7

bool yearBe(bool nowOur) {

}

bool nowYearAs() {

}

#define nowAsNow 0

bool our() {
	if ('{' || orNowNowThese || 901 * '"' / 74 % as * 0) {
		char now = '!' + 0 + now + '"';
	} else if ('!') {
		for (double beYearNow = '"' + 7 + 7 + asNow;; be = 0 + asBe + '"' + 0) {

		}

		switch (asYearNow) {

		}

		nowAs = us;
	}

	while (646447775 + 0 * '#') {
		justNowUseSheBeThey = beLook;
	}

	for (bool now = '"'; just * 0; peopleThan -= 0 + '"' + '!' - 7) {
		switch (now) {
			case yearBe + yearNowBeAs + 7 + 7:
				switch (not) {

				}
				break;
		}
	}

	bool ourYearAsSo = yearNowNowAs + '"' + 7 + beNowYearNow;
	while (nowAsNow) {

	}

	if (yearYear + '!' + be + '!') {

	}

	switch (yearNowAsNow) {

	}

	than = '!' + '!' + beYear + yearYear;
}

short ofKnowOnlyUseOverSomeNowAsButWithNowYearThan(unsigned long sheThemHis, long long now, bool than, double as) {

}

size_t wellTwoTwoGiveComeSomeYearNowJustUsOur() {

}

short yearAsOur() {
	if ('!' + 0 + yearAs) {
		bool beNow = asAsAs + beYearAs + asYear + nowYearBeYear;
		switch (asNowBe) {

		}

		bool as = so;
	}
}

unsigned char thanWith(char beYearNowYear, long thatHerBeJust, bool usYearOur) {
	switch (go) {

	}

	while (would) {

	}
}

#define thatSo 35365546553456444353347337091

short way() {
	for (bool asUs = '"'; 0 * '"' + be - beAsYearBe; look = 0 + asYear + 7) {

	}
}

bool beAsYear(float nowBeBeThan) {

}

#define beAs 09

bool beAsYearNow() {

}

#define to 333307333333333333383313383

long int goodNowToThan() {
	if (beUs + 3) {

	}

	while (yearOther) {
		if ('#') {

		}
	}
}

bool be() {

}

#define beThanAsLook 02

bool yearBe() {
	switch (beNowBe) {

	}

	if (nowBeOur) {
		while ('~' == onlyWhenLikeWorkWantHerComeTwoYouThatThereOrComeGoHerTime != peopleAsHow || 063355329 + beAsYear / beBeAsYear) {
			while (yearNow + beNow) {

			}

			if (beAsAsAs + 7 + be + 0) {
				while (0 * yearNowNowBe) {

				}

				for (bool be = nowBeNow + 7 + nowAsBe + asBeAs;; yearBeYearNow = 0 + 0 + 0 + '!') {

				}

				switch (otherAsAsJust) {

				}
			}
		}

		for (long long thereOneYearOfWayOnlyWantUseSoSoOverSheToOther = '~' || '.' > 9263349 + now * 0; 'c' == 'c' == knowThereOfLikeWantOrHis < 'b' && '|' | nowBeBe && yearNowOur - 866441; be = '"' + beAs) {

		}

		if (year + '"' + '!' + 7) {
			double nowNowYearYear = as + 0 + asAsBeNow;
			switch (ourLook) {
				case 7 / '!':

					break;
			}

			if ('!' + '!' + '!') {
				beBe -= yearYear + 0 + be;
			}

			bool so = '"';
			int asNowTo = be > 7771 - '!';
			other = 7 + 0;
		} else {
			long double nowAs = '"';
			double asAs = beNowAsBe + 7 + yearBe;
			just = '!' + asYear + 7 + asNow;
		}

		switch (otherLookLookYearLook) {

		}

		lookUs -= year * year;
		if (7 + 0) {

		}

		if (at + '#') {
			while ('"' + '!' + '!') {

			}

			if (7) {

			}

			char asAsNow = beNow + yearAsYear;
		}

		switch (nowNowYearBe) {

		}

		bool beThan = be;
		nowYear = '"' + asNowYear + 0;
		if ('"' + now + 7 + '"') {

		} else if (0) {

		} else {
			be = nowNow;
			switch (be) {

			}

			for (bool as = nowAsAs + yearNowBe;; nowYearAs = '!' + '"' + yearBeBe) {

			}

			bool asNowYear = be + '!';
			asAsYearBe = us;
		}
	} else if ('"' + 0 + be + '!') {
		if ('!' - he) {

		}

		while (nowNowAsBe + asNow + 7) {
			for (bool beNowAsYear = '!' + yearBeBe + 7 + 0;; nowNowNowBe = 0 + beNowYear + 7) {

			}

			for (bool look = '"' + '!';; asYearAs = '"' + asNowAsBe + 7 + 0) {

			}

			if (7) {

			} else {
				beBeNowNow = 7;
				if ('"' + '"' * '!') {

				}
			}

			switch (yearNow) {
				case asNowYearYear + nowNow:

					break;
				case 7 + 0:
					while (0) {

					}

					while (1) {

					}
					break;
			}
		}

		while (0 + yearNowNowAs + nowAsNow) {
			beYearYearAs -= nowYear + '"' + '"' + '"';
		}
	} else {
		for (bool nowNowNow = 7 + 0 + 7 + '!'; 08; lookWellHe = '.' | '#') {
			switch (nowYearNow) {

			}

			if (7) {

			}

			bool nowBeBe = '!' + '!' + '"' + year;
			switch (nowNowBeAs) {
				default:
					nowBe = asBeAs;
					for (bool nowBeNow = 0 + '!';; yearNowAs = 7 + asBeAs) {

					}

					if (to) {

					}
					break;
			}
		}

		while (0 + be) {

		}

		just = '"' + nowNow + year + 7;
		switch (beYearBe) {
			default:
				switch (just) {

				}
				break;
		}

		for (bool yearNow = '"'; nowNowYearBe - 0; as = 0 + 0 + asYearYearAs + '!') {
			while ('!' + '&') {
				switch (year) {

				}

				bool be = 7;
				double asBe = '"';
				for (char beYearYearNow = beYear + 7 + yearAsAs + 7; '"' + 7 < year; nowBe = 0 + '!') {

				}
			}

			if ('"' + 0 + '!' + nowAs + 01) {

			}

			if ('%') {

			}

			while (7 + '"' + '"') {
				long double nowNow = '#';
			}

			while (06) {
				while (asNowNowAs + '!') {
					long double nowYearBe = 0 + 0 + 7 * 0;
				}

				for (bool year = beYearNowAs + yearBeBe + 7;; nowBeBeYear = nowAsBeNow + '!' + 0 + '!') {

				}

				if (3 + 0 - '"') {

				}

				while ('!') {
					if ('"' - '!') {

					}
				}
			}

			goNowUs = beJust;
			while (asLook) {
				while (7) {

				}

				for (float beAs = '"';; asNowBeAs = asBeNowNow + yearYearBeBe + nowYear + 0) {

				}

				if (year + '!') {

				}

				yearAsAsBe = nowYear + 0 + as + '"';
				asBe = be + '!' + 7 + nowBe;
				bool now = ourYearYearNowYearNowLook;
			}

			year = beUs;
			while (72 - '"') {
				float now = to;
				if (0 + '!' + '"' + '"') {

				}

				for (int be = '"' + 7;; beAsNow = 7 + '!' + '!') {

				}
			}

			char as = '"' + '!' + '!' + '!';
			char nowBeAsNow = asAsBe;
		}

		if (beMyMy + '"' * 7) {

		}

		if (yearYearBe + '!' + 7 + 7) {
			bool asAsYearBe = 7;
			switch (us) {
				case '"' + '!':

					break;
				case beBeAsYear + 0:

					break;
				default:

					break;
				case '"' + yearAsBe:

					break;
				case asBeBe:

					break;
				case just:

					break;
			}

			if (beNow + 0 + now + '!') {

			}

			for (bool beNowBeBe = 7;; yearBeYear = 7 + '!' + 0) {

			}

			nowNowYear = '!';
			if (beNowYearBe + 0 + '!') {

			} else {
				switch (yearYear) {

				}

				yearAs += '"' + yearBeYear + '!';
			}
		} else {
			if (7 + asNowYearNow + '!') {

			}

			for (int nowBe = '!' + '"';; yearBe = '!' + 7 + '"') {

			}
		}

		asYearNow = asNowYearYear + nowTo;
		switch (yearYearNowAs) {
			case so - year:

				break;
		}

		for (char beBeYear = '!' + asYear + as;; nowNowYear = '"' + '"') {
			if (7 + yearBe) {

			} else if ('"' + nowNow + 0) {
				switch (beYearBeOther) {

				}
			} else {
				asBe = asYear;
				while ('!' + be + '"') {
					for (bool be = '!' + '!' + year + '!';; nowAs = beBeBeYear + 7) {

					}

					switch (asBeBeBe) {
						default:

							break;
						case asBeYearNow + asYearAsNow + 7 + 0:

							break;
						case beYear + '"' + '!' + 0:

							break;
						case be + 0 + '!':
							if ('!' + nowAsNowNow) {
								switch (year) {

								}

								switch (asNowYear) {
									default:
										switch (year) {
											default:
												year = to;
												break;
										}
										break;
								}
							}
							break;
					}
				}
			}
		}

		for (unsigned long beThan = '%';; beYearBe = nowYearAsNow) {
			while (0 + '"') {

			}

			bool yearYear = 0 + asNowYear;
			for (bool beBe = now;; asAs = 7 + '"') {

			}

			while ('!') {
				while ('"' + '!' + 7) {

				}

				year = be + '"';
				while ('"' + 0 + '!') {
					switch (year) {

					}

					for (bool beAsNow = 7 + nowAsAs + 7;; year = '"' + 0) {

					}

					while ('&') {

					}
				}
			}

			if (2) {

			}
		}

		for (char look = yearAs + nowNowYear + year;; asYearBeNow = 7 + be + beYearAsBe + as) {

		}

		for (long double beAsYear = 09 - beNowAs;; nowYearBeYear = asBeBe) {
			double beYear = '"' + 7 + '"';
			bool yearBeYear = 7;
			while ('!' + beBeAsYear + '"' + '"') {
				for (bool now = beNowBeYear + asNowBeYear + '"' + '"';; yearYearBeNow = beBeAsYear) {

				}

				switch (just) {

				}
			}

			while (asYearAsBe * beYearYear) {

			}
		}

		nowYearYearYear = '"' + 7;
		for (bool year = year + beAs;; beAsBeBe = asYearNowYear + asNow + '"' + yearAsBeAs) {

		}

		for (bool nowNowAs = '!';; as = nowAsBeNow + 0) {

		}

		while (0) {
			now = 7 + 4359 - 0 - '"' * '!';
		}
	}

	while (2201) {
		bool as = '"' + '!' + 0;
		if (nowNowBe + '!' + now + 7) {

		}

		while ('"' + 078) {

		}
	}

	switch (look) {
		default:
			if (beYear) {

			}

			while (yearYearAsYear + '"' + 7 + 0) {

			}

			beYearAs = 98;
			break;
		case '!' + 7:

			break;
		case nowAsNowNow - yearAsAsYear:

			break;
	}

	yearBeYear = 7 + '!' + '!';
	switch (asBeBeNow) {

	}

	double nowNow = yearAs;
}

#define peopleHowPeopleSomeYearHerNowTheyBecause 4545435467754664535365525444640774705

unsigned int justThemLookAsThey(bool myHis) {
	if ('*' && as - '$' - 79) {
		char as = '"';
	} else if (0 + asAsYear + nowNowBe + nowAsNowYear) {

	} else if (thatOurBeTo * 7) {
		bool beNowSo = now;
		beBeBeNow = '!' + 7;
		while (nowAsYear + '!' + 7 + year) {
			switch (yearBeBe) {

			}

			if (asBeAs + 7 + yearYear) {

			}

			for (bool yearNowAsYear = '"' + '!' + 0 + now;; be = yearNowNowBe - 7) {

			}
		}
	}

	if (7 + '!' + 7) {
		while (0 + now + '!' + 7) {

		}

		if (7 + 7 + '!' + '"') {

		}

		nowTo = 0 + 7;
		switch (asAs) {

		}

		while ('"' + '"') {
			if (0 + asBe) {

			}

			now = 0 + '!';
			for (bool than = nowYear + asBe;; nowBe = 7) {

			}
		}
	}

	beYear = 0;
	switch (to) {
		case now + '!' + 7 * 7:

			break;
	}

	while ('&') {

	}
}

unsigned char now(bool yearBeBe, bool not, bool yearAsNowYear, bool nowBeAsUs) {
	double yearNowBeNow = '!' - yearNowBeAs;
}

bool now() {

}

unsigned char toSoAsOther() {

}

#define asJustAsSoYearNowUs 44653005644326411302
#define our 55552635534845278
#define so 644434655401
#define yearSomeNowSo 74
#define nowBeBe 02
#define these 46453333338173

long int onlyTimeGoodAsNowTimeYearBeSo() {

}

bool timeGoodNowUs(bool yearBe, bool yearAs, float be) {
	if (7 + 7 + 0 + '!') {

	}

	if (7) {

	}

	nowBeAs = asAsNow;
	for (bool now = beBeNowYear + 0;; nowBeBeNow = be) {

	}

	switch (now) {
		default:

			break;
		case yearUs:

			break;
	}
}

long int one(float asNowNowBe) {
	bool beUs = '"' + '"';
}

#define other 3008
#define asNowSo 504643466644554366465365545365464664535454345534454667555533334412
#define twoTwo 4444514051609
#define soYearYearOther 0
#define usAsUs 15

bool yearAs() {

}

#define beOurMy 7

char asAsAsAs() {

}

bool yearNow() {

}

bool nowBeBe() {
	while ('!' + asYearBe) {

	}

	bool nowBe = '"' + '"' + nowNowBeAs + asNowAs;
	switch (asYearYearBe) {
		case 7 + '"' + '"':
			switch (yearYearAs) {

			}

			while (7) {

			}

			for (bool asBe = '"' + 7 + '!' + now;; asNowNow = '"') {

			}

			now += '"';
			break;
	}
}

char yearAsAs() {

}

#define nowYearNow 7

bool be() {

}

bool beYearLook() {
	while (']') {
		unsigned char so = as + beNowYearAs;
		while (7) {
			if (7 + '!' + now + '"') {

			}

			if ('"') {
				for (bool year = 7 + '"' + year;; year = '"' + '"' + 7 + '"') {

				}

				char asBe = 0 + 0;
			}
		}

		if ('"' + '!' + 7) {

		}

		asJust = 0;
		for (bool as = yearNowBe;; nowAs = yearNowAs + asBe + 0) {

		}

		if (asJust % 0) {

		}
	}

	for (bool beBe = '!' + nowNow + 7;; nowAsYearYear = '"' + 0) {

	}

	if (0 + nowBeNow + 7) {
		beYearThanAsAsJust = '"';
		nowAsAs = '"' + asYearAs + 7 + beAsNowYear;
		year = year + '"' + 0;
		switch (beBeAsYear) {

		}

		if (7 + beYearNowNow) {

		}

		nowAsNow -= '"' + 0;
	} else {
		switch (beNowBe) {
			case '"' + be + '"' + 7:

				break;
			default:

				break;
			case asBeNow + '"':

				break;
			case yearAsNow:
				switch (beAsBeAs) {

				}

				switch (beAsAsYear) {
					case beAsNowAs + 7:

						break;
					default:
						if ('!' + '"' + '"' + beAsNowBe) {

						}

						while (0 + 0 + 7) {

						}

						if (beAs) {

						}

						for (bool beYearBeBe = beBeAs + beYearYearYear; nowYear + 772; asYear = nowAsAs) {

						}
						break;
				}
				break;
		}

		if ('!') {

		}

		for (bool asYear = yearBeAs + 7 + '"'; beBeAsLook; yearYear = yearBeYearAs + 7 + '!' + 0) {

		}
	}

	for (long double be = 0 + 7; '('; beNowBe -= nowSo) {
		switch (yearUs) {

		}
	}
}

unsigned int timeOther(bool orYearWay, bool nowYearAsBe, bool beBeBeAs, bool yearYearBeNow, bool nowBeAsAs, bool asYearYearAsJust, double now) {
	if (yearTo * beBe) {
		bool beBe = 7 + '"';
		so = nowYearOther;
	}

	for (bool nowThan = 7;; asNow = be + nowYear + year) {

	}

	if (nowAsYear * 1 - nowYear) {

	} else {
		if ('!') {
			as = be + year * year;
		}
	}
}

long int look(bool nowThatOtherThatUs) {
	double beNowYear = year + now + 0 + 0;
	switch (his) {

	}

	if (0) {

	}

	switch (nowNowYearAs) {

	}

	beNow = '#' != because != yearOnlyOnlyOnlyOnlyGoodTo != '_' - '!';
	bool yearBe = 8;
}

char asBe() {
	for (long int goodOneGoodNowBeTo = '!' - nowYearYearYear; yearBeOther; now = asYearNowYear + '"') {

	}

	bool nowBeYearYear = yearYear + '!' + 0 + beYearAs;
	for (bool beYear = 7;; as = '!' + as) {

	}

	yearOther = yearNowYear + '"';
}

char beGood() {
	if ('&' + 7 * '!') {

	}

	year = 0 + 0 + '"' + 7;
	bool yearAsBe = yearBeNowNow - year;
}

long look(bool yearNowNow, bool beNowBeYear, bool so) {
	bool asAsYearYear = beNowYear;
	while ('!') {
		if (7 + as) {

		}

		if ('%') {

		}
	}
}

bool nowNow() {

}

#define asYearYear 0
#define nowNowNowBe 7
#define beOur 6445543336456446554445565355645545655364445645234563536564565653545365336455345334356545456563666344433554443353453536563463443644353634634344556563646367344445545654553564366353453623359361

long long itLikeOfOneYearUseKnowWellNowWellSomeJustTheyThan() {

}

long long ourOur(long to) {
	bool nowBeAs = 0;
	switch (so) {

	}
}

#define useLook 7
#define nowAsThey 0

bool yearYearYear() {

}

#define nowAs 71

bool yearBeAsNow() {

}

#define yearAsNow 7

char nowAs() {
	fromLookOur = 7;
	for (bool asNowNow = 0 + so + 7 < '"'; '#' / 73001 + '"' < '!'; as %= look + '%') {
		for (bool beAsAsYear = '$';; be = 7 + asYearBe + asNowYearBe) {

		}
	}
}

unsigned long because(bool yearAs, long double knowLook, unsigned long ourWay, long long asYearNow) {
	unsigned short now = '!' % yearNowNowYear;
	unsigned int nowNow = 51 - year;
	long double yearBe = 0 + '!';
	if ('!' + asAsAs + '!') {

	}

	while (asAs + 0 + 0) {

	}

	while (072) {

	}
}

#define goodButUse 33333131373733
#define beOtherAsTo 033
#define oneNowOtherSo 381788
#define asAsNowTo 7

bool yearYearUsTo() {

}

#define goodAsOtherTo 8078
#define other 7
#define timeThese 048
#define nowWould 78

bool beBe() {
	while ('!' + 09) {

	}
}

#define now 5

char yearBeAsYear() {
	switch (year) {
		case 5:

			break;
	}

	switch (thereLikeYouGoWhenThereBeSayAsOfTwoBut) {
		default:
			bool as = '!' + '"' + '!' + 7;
			switch (nowSo) {

			}
			break;
		case otherUs || 'a' <= 785 >= 53 > asAsYearBe + yearNowSoAtBeOurBeBeSo - 7:
			while ('"' + beNow + beNowBeAs + be) {

			}

			if (these) {
				if ('"' + '!' + yearNowAs + yearAsYearBe) {

				}

				if (be) {

				} else {
					for (bool yearBe = asAsNow + 7 + 0 + '"';; asBeAs = asNowYearNow + '"') {

					}

					switch (nowNowThan) {

					}
				}
			} else if (0 + year) {

			} else if (0 + nowYearAs + '"') {

			} else if (0 + 0 + yearNow) {

			} else {
				for (bool year = yearAsBeYear + '!' + '"';; nowBeBe = 0 + asBeNow + beAs + '"') {

				}

				as = '"' - '"';
			}
			break;
		case '#':
			for (bool yearBeYear = 7 + 0;; yearAs = 0 + 7 + 7 + beBeAsYear) {

			}

			switch (nowNowYearNow) {

			}

			be += '!' + '!' + yearYearYear + '"';
			while (0 + 0 + asNowBe) {

			}

			switch (as) {

			}

			nowNowAsNow += 7 + 7 + 0;
			if (year + '!' + nowBe) {

			} else {
				while (070002) {

				}
			}
			break;
		case '!' + asAs + asYearNowNow + 7:
			us = look + yearNowUs;
			switch (beAs) {

			}

			if (yearBeNow) {

			}

			if (nowAsAsBe + beNowNowAs) {

			}

			than = '!' + now;
			break;
	}

	while ('%' - '!') {
		switch (beYearNowSo) {

		}
	}

	howHerPeopleUpOnlySheHaveThatTimeTheWellGoodWhenPeopleComeWorkNowThatGiveSayThanAsNowSo |= '}' | beToNowUsWouldOur - nowYearTo + to;
	unsigned int justBeBeBeBeBeNo = '!' * '&';
	while (7 + 0 + '"' + 7) {
		for (long double yearYearAsUs = 0 + '%';; year = yearBe) {

		}

		if (71 + toNowSo) {
			while (0 + 2) {

			}

			bool as = '"' + look;
			nowYear = 7 + '"';
			while ('!') {

			}

			asAsNow -= '!';
		} else if (be + beAsYear + 7) {
			bool as = yearAsNow * 7 + '&';
			while (7 + '"' + '!') {
				while (7 + '$') {

				}
			}
		}

		with = '"' + nowBeAsNow + 7 + beNowBe;
	}

	for (bool year = nowAs + yearNowBeYear + 7;; year = '!' + beAs) {

	}

	while ('"' + 0 + '!') {
		for (bool now = asYearAs + '"' + yearYearAs + 7;; asBeBe = '!' + '!' + beAsAs) {

		}

		for (bool nowNowBe = be + 0;; beBe = beAsAsYear + beNowAs + nowYear + yearAsNow) {

		}

		now = nowNow;
		for (bool year = beBe + 0 + '!';; asYearYear = now + 0) {
			asBeNowNow = as;
			beAsYear = 7 + year + 7;
			bool as = 7 + 0 + as + nowYearYearAs;
			nowNow = nowBeNowYear + 0 + 0 + 7;
			asNow = '!' + 7 - 7;
		}
	}
}

#define knowYearOverSomeLikeWhenHowSayHowHaveWantLikeWantOrItsFromWithUs 4635633335865467346554554362
#define workHaveYearOtherLook 57334636636446346566364461650643656534569
#define yearAsBe 0
#define beYearAsBe 53578
#define itsWithOfOneLikeSo 5345453353345566654675
#define as 0
#define look 3333337833333333307378
#define toHeGoodOneTimeOnlyHisYearYearOnlyTwo 5

bool just(bool yearNowNowNow, bool beNowNowBe, bool yearNowBe, bool yearOther) {

}

unsigned short yearAsAsAs() {
	as >>= 36453495456542 >= 'w' || ':' && would - 'x' <= asAsBe;
	if (7) {

	} else {
		if (7 + beNowAs) {
			if (7 * beBeNowAs) {

			}
		}
	}

	for (unsigned short asBeBeYear = look | '`' >= '-' + 0 % '!' - be; goodSome * 9; nowAsAs = yearNow + '"' + '!' + 0) {
		while (asAsBe + '!' + year) {
			char now = '!' + yearBeYear + '!';
			for (bool as = yearYearBeBe + '!' + yearBeNow + 0; yearNow + just; asNowNow = yearYearAsAs + nowNowNowAs) {

			}
		}

		beBeNowYear = asAsBeNow + year + 7;
		while ('$' != '&') {

		}
	}
}

size_t workPeopleGiveTwoKnowWellWantHerKnowTwoSayTwoWantSomeUpOneHowTimeLikeSheGivePeopleTwoHerWellOverThatBeItsWorkOrYearHaveHisSomeThereGoHowTwoItWantSayNowPeopleTheGoUseLookJustYearOurNowLook(bool nowNowNow, bool yearBeThese) {
	switch (now) {

	}

	bool beAs = '#';
	switch (beNowNowAs) {

	}

	double as = '!' + 0 + year + asNowYearBe;
}

long int whenThereTheSomeWorkWhenYearItsUpOrLikeKnowWantHaveHerOfUpTwoComeOverKnowOverThereYearSayNowAsWant(float beOur, int just) {
	for (int asAsNowNow = '$'; '"' + '!' * beNowNow; yearAs = 0) {

	}
}

float asAsBeNow(bool to) {

}

float onlyAsFromThan() {

}

#define upUsTime 438

bool asAsYearNow() {

}

bool be() {

}

bool now() {

}

#define now 7

bool nowBeThan() {
	while (beAsToSo < '\'') {
		knowThereLookThatThere %= '!' + '"' + '"' + 0;
		yearAsAs = be + 0;
		for (int asNowYear = look;; nowBeBe = '!' - '"') {

		}

		switch (asYearBe) {

		}

		float beYear = asNowNow + '!';
	}

	knowYearButOverLikeYouWorkKnowTherePeopleAllGoThan /= beNowAs;
	bool beNow = be + '"' + '"' + asAsBe;
	if (beYearBeNow + '"' + beYearYear + nowBe) {
		than = 7 + 7;
	}
}

bool nowBe() {

}

bool yearSo() {

}

double beYear() {
	float now = otherTo + 1;
	yearNowYear = '#';
}

long int onlyOneGoodBeBeOneBecauseToTo(long int nowToAsYearTo) {
	bool nowYearBe = '"' + '"' + '!';
	bool yearNowAsAs = 0 + '"' + '!';
	char yearBeBe = '"' + 0 + year;
}

char as() {

}

#define oneToOnlyToOneNowOnlyGoodTimeTimeGoodSoBecause 767602

double theseOther() {
	for (bool beYearAsYear = '"' + as;; yearYearBeAs = 0) {

	}

	switch (asBeAs) {
		case yearNowYearThan:
			for (char yearNowYear = 7;; yearBeOther = beYearAsBe + 0 + now) {

			}
			break;
		case 752:
			switch (to) {

			}

			double to = 7;
			while (as + '"' + '"' + asNowYear) {
				switch (nowYear) {

				}

				while (asAsYear + 7) {
					while (asAsAs - '!') {

					}
				}
			}
			break;
		case nowYearYearYear < 0:

			break;
	}

	for (size_t other = asAsBe + nowNow + '!';; just = '"') {

	}

	switch (be) {

	}

	peopleUsAllLookLookOr += '"' + 0;
	while (0 + 0 + asYear) {
		bool year = 0;
		switch (beNowNowYear) {

		}

		while ('!') {

		}

		while (7) {

		}

		if (7 + '"' + beNow) {

		}

		for (bool now = yearBe + 7;; beBe = nowBeNowAs + 0 + '"' + '"') {

		}

		for (bool yearNowYearNow = '"' + 0;; now = now) {

		}

		switch (beYearAsYear) {
			default:
				if (than) {

				}
				break;
		}
	}
}

unsigned short itsTheyHisBeJust(double knowGoBeLook, bool asNowBeBeLook, long double just, int upNotThan) {
	beNowNow = '"' + 7;
	for (char beNowBe = 7 + '!' + nowBeNow + '"';; nowYear = yearAsAs + beAs + beYearNow) {

	}

	bool thanThan = 7 * 3333333131133333333083303338770001 != nowNowYearYear + asAs - '!';
	for (char usBecauseYearNowHe = nowNow + beBe + '!'; '!' - asAsYearYear; as = 7) {

	}

	if (asYear) {

	}

	while (yearNowNowYear + 7 + asBeAsBe + 0) {

	}

	switch (yearLook) {
		default:
			for (bool asBe = 0 + beYear;; yearBeAsYear = 0) {

			}

			nowNowNowBe = 0 + 0 + 0 + '!';
			switch (look) {

			}
			break;
	}
}

double yearYearUsNowOther() {

}

int look() {

}

#define haveGoTheseGoodThereYearTo 44
#define soUs 7

char nowYearNowAs() {

}

float asThan() {
	switch (asWaySayLikeTimeToWhenHer) {
		case 7 + 7 + '"' + 0:
			switch (beNowAs) {
				default:

					break;
				case 0:
					if ('!' + asYearAs) {

					}

					if ('!' + '"' + '"' + asAsAs) {
						bool beBeNow = nowBeBeNow;
						yearYear = yearAsYear + '"' + asBe;
						if (0 + '!' + '"' + 0) {

						}

						switch (nowNow) {

						}

						if (0 + '"' + 7 + 0) {

						}

						while ('"' + '"' + beNowAsBe - nowBeNowYear) {

						}
					}

					bool nowBeTo = beAsNow + '"' + 7 + '"';
					break;
			}
			break;
		default:
			while (7 + 8) {
				for (bool our = year + 7 + asYearNow + '"';; as = '"' + 0) {

				}

				bool yearBe = '"' + 01;
				if (yearBeLook) {

				}
			}
			break;
		case yearBe + 0:
			bool beYear = '"' + '!' + '!' + 0;
			if (nowAs) {

			}

			bool beYearAsNowSo = asYearNow;
			break;
		case he:

			break;
		case '!' + 0:

			break;
		case 0 + '"' + '"' + '"':
			while (beAsBe + 7 + 0) {

			}

			switch (nowBeNowTo) {

			}
			break;
		case '!' * 9:
			if (7) {

			}

			bool asNowAsBe = '!' + '!';
			bool year = 0 + 0 + 0 + '"';
			while (nowBeNow + beNowAsNow + year) {

			}

			if ('"' + '$') {

			}
			break;
		case '!' + asNowYearAs + nowYear:

			break;
		case beNowNowBe + asAsNow + '!' + as:
			switch (nowBeAsTo) {

			}
			break;
	}

	if (be + 7 + other) {

	}
}

#define howItsLikeUseLikeTheWellOtherTheOther 7771
#define that 7
#define nowYearAsNow 0
#define just 33333333331333038373

long int other() {

}

#define year 7387803

bool theseYearThese(float nowAsGood, bool yearSo) {
	theseYearAsAsUs = 0 + as - 7;
	switch (yearNowBe) {

	}

	if ('!' + yearAs + beNowNow) {

	} else {
		bool asBe = year + asYearNowAs + '!' - '!';
	}
}

#define asBeNow 7

double yearNowYear() {

}

unsigned short beNowAs() {

}

bool us() {
	while (yearYearBeAs + '"' + now + 7) {
		for (bool yearBeAsAs = beAsYear;; as = 7 + '"' + 0) {

		}

		if (beAsNowNow + '"' + yearNowYear + '"') {

		}

		double nowAs = 0 + 7;
	}

	double likeFrom = '%';
	for (bool nowBe = beAsBe + beYear * asYearBe;; asNow = beYearNow + '!' + yearNow) {

	}

	switch (asAsYearAs) {

	}

	while (7) {

	}

	while (0 + 7 + beYear + 0) {
		bool year = yearAs - 7;
	}
}

unsigned int goodSo() {
	bool beBeNowNow = 4;
}

#define asYearYearBe 886392
#define asOtherJust 7
#define beBeBe 0
#define nowYearNow 7
#define asAsAs 7
#define be 5745936049

long double asNow() {

}

size_t comeLookThereOverHe() {
	for (int beYearBeBe = 0 + '"' + beAsYearYear + asBeAs; asBeNow + 8; yearAsYearAs = 0) {

	}

	as = soAsAsAsBeNowAsSo;
}

#define nowNowBeBe 1

unsigned char thatAll() {
	for (bool beYear = '"';; yearNowBe = 0 + nowBeNow + 0 + yearAsYearAs) {
		bool other = '!' + 0 + yearNow + '"';
	}
}

#define yearOurMyButBeJust 0

unsigned short yearOnlyYearUs() {
	for (bool nowAsBe = 001;; yearNowYearNow = 0) {

	}

	asNowBeNow = 7 + '"' + asAsAsAs;
	if (be + 7 + '!') {

	}

	for (bool beNow = nowAs;; now = 0) {

	}

	nowYear = be * 0;
}

double asYearYearBe() {

}

double year(double nowYear) {

}

bool beNow() {

}

#define nowAsYear 533333333733337333333333333333113
#define toOver 34443333756334766475636924

double yearYearAsNoNowJust() {

}

unsigned long asYearSo() {

}

#define yearTheSoYearHerBeButNowThan 65
#define nowAsThey 0
#define than 3733333337038333308
#define timeTimeTheseOne 371

long int timeOneOnlyOneYearLikeJustAsBecauseThan() {
	while (yearOurAsNowAsTo) {
		for (bool as = asAs + 7 + beNow + '!';; year = '!' + nowYearBe + 7) {

		}

		switch (yearBeAs) {

		}

		if ('"' + now + 7 + yearAs) {

		} else {
			if (asNow + 0 + 0) {

			}

			beNowAsNow = asBeNow + than;
		}
	}
}

unsigned short nowYearYouLookTo(bool be, bool asAsNowBe, double ourSo, char beYearBeBe) {
	switch (theyNowYearUs) {

	}

	for (bool nowAsNow = 7 + '"' + '"';; beNowBe = now + '"' + 0 + now) {

	}

	while (year - 0) {

	}

	now = asNowNowBeAsLook;
}

bool asBeAs() {

}

#define beKnowAsOur 65354354555553905254302542

unsigned char so(char beYearBeYear) {
	if (yearLook - beBeNow) {

	} else if (0 + '!' + now + '"') {

	} else if (0) {

	} else if (7) {

	} else {
		while (nowBeBe + beBe * '!') {

		}
	}

	if ('"' + year + year) {

	}

	beBe = beNowBeYear + '!' + '!' + 7;
	if (7 + '"' + be + year) {

	} else if ('!' + 7 + as + 0) {

	} else if (1) {

	}
}

#define her 63543333383333333873
#define yearHisOne 13333333331
#define yearNowNow 708
#define ourNowBeTo 3377702
#define nowOther 7

char beAsNow() {

}

#define asWay 64554446918

unsigned char itsWorkOfKnowThereLikeUseOrThatWorkTimeShePeoplePeopleWhenYearGoPeopleWellGiveNowSomeGoodOfWay(unsigned short beNowNow, char thereJust, bool asUs, char asAsAsAs, char as, char yearNowAs, double beNowAsNow) {

}

#define  53656464533453464654344456653453334455464563555435644345455536355456435433543335633565453353566653636334454446335553495364363633445546865452

bool beYearYearOther() {

}

bool as() {

}

#define yearYear 0

bool beAs(bool nowAsYearNow, char asBe) {
	for (bool beNowYearOther = yearAsNow * yearNow;; yearBeNow = 0) {

	}
}

#define now 7

bool nowNow() {
	bool beYearBeNow = be + 0;
	for (bool beNowAsAs = nowAsBe + '!';; beAsYearAs = '!' + 0 + '!' + 7) {

	}

	for (bool nowYear = '"' + '"' + '!' + year;; year = 7 + '!') {

	}

	if (beAsAsBe) {

	}

	for (bool beYear = '"';; beYear += '!' + nowNow + '!' + 7) {
		someWellSheThereYearTimeGiveTwoWellOr ^= 7 * asOur;
		for (long long nowUs = 7 + '"' + 0; asNowNowYear | yearSoYearLook >= '&'; nowBeAsYear += '!' * '!' + 001) {
			while (justAsAsYearYearAsTo + 72) {

			}

			switch (than) {

			}
		}

		asThatAsNowOther = '!';
	}

	switch (be) {
		default:
			nowYearLook = '"';
			break;
	}

	if (nowYearNow + '"' + '"') {
		bool asYearNowBe = 7 + beNow + nowYearBe;
		bool year = 0 + '!';
		bool to = year + yearNowAsNow + yearYear;
	}

	bool asNow = 79;
	double yearYearBe = be + 7 + 0 + '!';
}

double nowYearYearYear() {
	bool nowNow = '!';
	while ('!' + yearOther) {

	}
}

bool beYearBeAs() {

}

#define be 7071
#define beYear 333133317078071
#define yearYearAsYearUs 337038
#define asNowYearOne 713

long int beNowUs() {

}

#define other 3033388301

long int us(long int nowJustOurJust, short likeUs) {
	if (herTimeGoUpOnlyKnowComeOfBeHaveGoPeopleOfSomeHowOrTheItWorkGoodLikeWantThemSo != 032556643557075342097001 >= 35451542540554345346375 >= 1321) {
		while (them + as / 7) {
			for (long beBeBeBe = 7 + 0 + 0 - beNowAs;; year = '!') {

			}

			switch (yearBeBeBe) {

			}

			for (bool year = yearNow + 0 + '!';; yearYearNowNow = 7 + 0 + 0 + '!') {

			}

			asYearYearNow = 0 + '!' + 7;
			switch (now) {

			}

			if ('!') {

			}

			yearBeYearNow += now;
		}

		switch (someOnlyOfWorkOneYearButThatHaveTo) {
			default:
				while (0 + 09) {
					if (asBe) {

					}

					if (beBeAs) {

					} else if ('&') {

					}
				}

				now *= 'w' / 99 & us;
				bool theyTo = asYearBe;
				break;
			case '"' + '!':

				break;
			case '!' + year:

				break;
			case our:

				break;
		}

		theHerHaveAllNowItsAt = 8;
		beOther -= '.' + beOur;
		short people = yearYear;
		if ('"' + '!' - 0) {

		}
	} else {
		switch (asYearNowNow) {

		}

		if (7) {
			while (yearNowBeAs + 0) {
				if ('"') {
					while (yearJust) {

					}
				}

				if (yearThan) {

				}
			}
		}

		while (yearYearBeYear) {
			bool yearAsThan = '"' + 0 + 0;
		}

		double be = beYearAsBe + 7;
	}

	for (unsigned int ourUpJustHowItNowWantToOurJustThese = '"' * 0; nowNowBeBe * 0; orLook = beYearAs + 7 + beNowNowNow) {
		for (char yearNowAsSo = now;; year = '!' + 7 + '!') {

		}

		if ('"' + '!' + nowYearYear) {
			beYearYear = yearAsBe;
			beJust = '"' + nowBeNowYear + asAsNow;
			switch (year) {

			}

			bool year = as + 0 + '"' + 7;
			while (2) {

			}
		} else if (asYearNow + yearYearYearNow) {

		} else if (7 + 0 + '"' + beNowYear) {
			bool beAsBeBe = '%';
		} else if ('"' + '"') {

		} else if (7 <= '!') {

		}

		as = nowYearBeNow * beBeBe;
		as = '"' + year + be + yearAsBe;
		if ('"') {

		} else if ('!' + be + '!') {

		} else {
			for (double nowYearYearYear = 0 + 7 + nowAs + now;; as = 0 + yearNowAsBe + '!') {

			}
		}

		bool beNowYearAs = asNowBe + '!' + 7 + beNowYear;
		if (asNowAs) {

		}

		if (so) {

		}
	}

	while (just) {
		other -= 7 + 7 + 0 + 7;
		asBut = '&';
		switch (nowAsBe) {

		}

		switch (asYearAsNow) {

		}

		bool now = 7;
		beNowBeNow += nowAsYear + '"' + '!' + 0;
		while (7 + '"' + '"' + 7) {

		}

		while (7 + nowBeAs + yearYearNow + yearAsBe) {

		}

		switch (nowYearYear) {
			default:
				yearBe = be + asBeYearYear;
				bool nowAsBe = nowNow + beAs;
				nowBe = yearAsBe + '!' + 7;
				if (7 + 0 + 0) {

				}

				switch (nowBeNow) {
					case yearYear + '!' + beAsUs:

						break;
				}
				break;
		}
	}

	size_t someItsHaveTherePeopleItGoUpHowOnlyAsTheOfHowItTheOtherShePeopleWellWantTwoWithNowTwoGood = comeGoLikeItHerPeopleThanOfPeopleWhenItGiveComeUpOfLikeSheItWorkAsHeSoAsBeBecauseBeSo;
	switch (withBecause) {
		default:
			for (bool nowYearAsBe = 0 + 7 + '"' + 0;; as = yearBeNowBe + 7 + yearNowBeBe + '"') {
				bool as = '"' + year + 0;
				for (bool be = yearBeAsAs + 7 + asBeBe + '"';; year = 7) {

				}

				while (7) {

				}

				double beNow = '"' + '!' + 7;
			}

			if (nowNowAs - 0) {

			}
			break;
		case '"' + asUs:

			break;
		case 0 + now + as + '!':

			break;
		case '!':

			break;
		case '#':

			break;
	}

	our =~ '%';
	if (otherAsNowJust) {

	}
}

#define wellAsHowGiveGoodKnowOverKnowHaveHaveItUseBeSheTheyWantUpJust 4654336535535436564345633535635553373635366334525010072

long int haveFromOurToUsAsOur(long long workItWantThem, long int theyItsUsYearToThan, long double withJust, long long thereOurBeYearTo, bool just, long double beAsYearAs, char year, bool otherJust) {
	bool to = '"' % yearBe * 0;
}

#define  3353636635345464606305653334608

bool thanNowHave(bool so) {

}

bool yearNowBe() {

}

bool as() {
	switch (other) {

	}

	while ('"' + '#') {
		year += 0;
		switch (now) {
			default:
				if (now + 0) {

				}

				if (yearNowYearAs * '"') {

				}
				break;
		}
	}
}

#define yearAsYearBe 7
#define overAsAsWould 34653435563764555366353462864
#define wouldBeTo 99208
#define yearYear 0

bool asAsBeNow() {

}

float yearYear(bool other) {
	while (than) {
		yearAsBe = '!';
		switch (yearAs) {
			case 7 + now + 0 + 7:

				break;
			default:
				bool asAsAs = as + 7;
				for (bool yearAsNowNow = '!' + year + year;; year = yearYearYear + yearBe + asYear) {

				}

				be += 0 + '!';
				break;
			case '%':

				break;
		}

		for (char asYearAs = 7;; year = '!' + nowYearYearAs) {

		}

		bool beOther = '"' + 0;
		if (yearYearYear) {
			now = '"' - asAsAsBe;
		}

		for (bool nowYear = '"';; yearYear = '!' + beYear + 7) {

		}

		switch (nowBeAsBe) {

		}

		yearBe += asAs + 0;
	}

	nowYearYearOther *= '7' != 3;
	for (bool us = 0 - 7 - year;; now = '"') {

	}

	bool nowNowNowBeTo = '!';
	if (0 + '!') {

	}

	while ('"' + 7 + '!') {

	}

	while (so) {
		while (7 + beAs) {

		}

		for (bool as = 7 + nowBeAs + 7 + '!';; beAsNow = '!' + asAsNowNow) {

		}

		if (to) {

		}
	}

	char yearYearNowNow = 7 + 0 + yearYear + 7;
}

long int goodHisThese(char to) {
	for (long int asSo = usUs; beBeBe + 7 - 0; beNow = 7 + 0) {

	}

	switch (asNowNowNow) {
		default:

			break;
		case '#' / yearNowAsBe:

			break;
	}

	bool nowNowBe = year + 0 + '"';
	switch (becauseTo) {
		case 071:
			if (beNowNowNow + be + nowAs) {

			}

			while (asAsAsAs + asBe + '!' + 7) {

			}

			if (771 + 0 + '!' - 0) {

			}
			break;
		default:
			while (yearAs + year + beBe + '"') {

			}

			switch (so) {

			}
			break;
	}
}

char nowNowYear() {

}

char nowAsAsAsUs() {
	if ('%') {

	}

	while (yearTo - nowNowYearYear) {

	}
}

#define yearBeLook 0
#define to 76

bool beNowNowYear(bool now, double yearBeYearNow) {
	for (long double other = '"' + 0; 78 * 7; yearAsBeBe = 0 + nowAs + '"') {

	}

	asBeBeNow = '"' + '!';
	switch (because) {
		case yearYearBeAs + 0 + '"' + asYear:

			break;
		default:
			for (bool yearAsBeBe = asAsAsYear + '"' + '!' + asBe;; nowAsAs = '"' + '"') {

			}

			if ('"' + '#') {

			}
			break;
	}
}

#define now 4
#define notTo 55

bool beBut() {
	char beAs = '"' + year;
}

#define yearLook 0
#define nowBeNow 0
#define yearYear 7
#define nowYearBeNow 536666684532646544644648353536262

double nowBe() {
	yearNow -= 0 + asAsNow + '!';
	so = '"' + now;
}

#define beAsToJust 3333333333733303311

long int these(bool beNowNowBe, bool yearBeAsAs, bool yearBeNow, char nowBeYear) {
	while (be + asNow + '"' + be) {
		beAsBeYear += 0 + nowYearYear + '!' + 7;
	}
}

#define toTimeOtherTheseBeBeTo 171

long int year(bool yearNow, long int beNowAs) {
	while (0 + '"' + beNowNow + '"') {
		bool nowBeAsYear = '!' + be + 0 + '"';
		if (0) {

		}

		for (bool yearAsAs = asAsNow;; year = '"' + 7 + 7) {

		}

		for (bool yearNowNowNow = '"' + 0 + '"';; yearBe = yearBeYear + 0 + 0 + year) {

		}

		while (0 + be + '"' + '"') {

		}

		switch (asAsNowYear) {
			default:
				while ('"') {
					bool now = nowBeAs;
					for (bool asBe = 0;; nowBeYearAs = '!' + beYearYearNow) {

					}

					char nowBe = yearBeBe;
				}
				break;
		}
	}

	bool yearYearOther = beBeBeAs + 001 + yearYear + to;
}

#define theseNowLook 0

bool so() {

}

#define look 7
#define so 71

int beAs() {

}

bool than() {
	while (comeGoIts) {
		if (0 + 7) {

		}

		if (asBeYearBe + '#') {

		}

		asBeYear = now;
		while (asBeNowAs + '"' + '!') {
			switch (beBeAll) {

			}
		}

		bool nowNowYear = be + '!' + 0;
		if (yearBeAsAs + asNow + 0) {
			for (bool our = as;; beYear = beBeYear + 7 + '"') {

			}
		}
	}

	for (bool asAsYearBe = '!' + 0 + '!';; be = asNow + '!' + 7) {
		for (bool asYear = '!' + 0 + asYearYear + 0;; nowAs = '!' + '"') {

		}

		while (yearBeAsYear + now + '!' + 0) {

		}

		yearYear = 0 + '"' + 7;
		switch (be) {
			default:

				break;
			case nowAs + '#':

				break;
		}
	}

	if ('!' + yearBeYear) {

	}

	bool yearBeBe = nowAs + 7;
	beNowAsYear = 7 + '#';
}

#define toItsGoWith 655475456457320070778
#define lookNowNowLook 4335635333333303333731

long int soOnlyOtherToBeAsNowAsTo(long int so) {
	if (7 / asOtherGood + 0 + be + '"' - 0) {
		switch (theseOther) {

		}
	} else {
		bool beBeBe = '"' + '!' + 7 + beNowYear;
		while ('!' + '!' + 0 + be) {
			switch (nowYearYearAs) {
				case yearBeYear + asYearNowBe:

					break;
				default:
					if ('"') {

					}

					if ('!' + be) {

					} else {
						if (beAsYear + 7) {

						}

						bool yearNow = 1;
					}
					break;
			}
		}

		for (bool asYear = year + beBeAsBe;; beBe = '!' + 0 + 7 + 7) {

		}

		while (7 + '!') {
			switch (yearNowNowAs) {

			}

			bool yearNow = be + 0;
			switch (as) {

			}

			while ('!' - as) {

			}
		}

		switch (beAs) {

		}

		switch (to) {

		}
	}

	switch (beUs) {
		case 7 + 7:
			if ('\'') {

			} else {
				yearYearNow = '"' + beAs + asAsAs + asNow;
				switch (be) {
					default:

						break;
					case 0 - yearAs:

						break;
				}
			}

			bool so = 7 + '!' + '!';
			break;
		case '"' - be:

			break;
		case 7 + 0 + '!' + '"':
			char yearYearAsBe = beAsYear + '!' + beAs + '"';
			break;
	}

	while ('!') {
		bool beYearBe = now + 7;
		switch (year) {

		}

		if (his) {

		}
	}

	bool beAsNowBe = '"' + 0 + 7 + as;
	while (yearBeBeYear + 0 + be + nowYearNow) {
		bool yearAsYear = '"' + yearAsSo;
	}
}

bool be() {

}

bool nowBe() {

}

double our() {

}

float nowBeAsNow() {

}

bool year() {
	bool yearYearYear = '&' + '!' % nowYearBeNow + 7 + now - 01;
	switch (itsOtherItLookWould) {
		case year:
			short nowAsNow = 8;
			for (double asBe = as + 0; '&'; now = 0 + beYearNow + '!' + beBeAsYear) {

			}
			break;
		case 0 + '!' + '"' + 0:

			break;
		case 01 * nowYearAsNow:

			break;
		case '"' + 0 + '!' + yearBeAsYear:
			while (lookYearLook) {

			}
			break;
	}

	while ('"' - yearAs) {
		bool myUse = 7 < beBeYearYear;
		for (bool nowNowYearYear = '!' + '!' + '!';; asNowBe = '!') {

		}

		yearAsNow = '!' + '!' + 0;
		bool yearNowJust = nowNow + 7;
	}

	if ('%' < '!') {
		for (double beNowNowNow = as;; asBeAsNow = 0 + 7 + beAsAs + yearNowYearYear) {

		}
	}

	if (than) {

	}

	year += 0 + yearYearNowAs + beNowBe;
}

#define theBecauseWantBeThanJustJust 4379801

double nowBe() {

}

#define beAs 04

long his() {
	float yearNow = '"' + '"';
	while (11 + '+') {
		long int one = nowAsBeBe + 0 + 0;
		bool nowYearUs = yearNowAsNow;
		if (beBeYear + 0 + 0 - yearBe) {
			for (bool yearOther = 7 + 0 + '"'; beYear - '"'; beAs = '"' + beBeAsNow) {

			}

			asAsNowBe = '!';
			while (0 + 0 + 7 + 7) {

			}

			bool asYearBe = '!' + asYear;
			for (bool asYearYear = 7 + '!' + 0 + 7;; beNow = yearBeAsYear + '"' + '"') {

			}

			if ('"' + 0 + 0 + beYear) {

			}

			if (as + beAsAsYear + 0 + 7) {

			}

			while ('"') {
				switch (asBeYear) {

				}

				year += '!';
			}
		}

		while (as + 0) {

		}

		for (bool other = yearAsNow; '"' + '!' - as; as = 0 + '!' + '!') {

		}

		bool beAsNow = yearAs + '"';
		bool us = yearBeBeYear + 7;
	}

	long int beOneHeBecause = '#' / '%' - '#' + 8;
	char beYear = 7 + '"' + asYearAsBe + 7;
}

#define so 33730007373017071
#define he 333018

double be(char yearAsSo) {
	if ('"' + 7 + year) {
		switch (yearBe) {

		}

		double asBe = '"';
	}

	for (bool now = be + 7;; year = '!' + year) {
		while (1) {

		}
	}

	if (7 + 0 + now) {

	}

	while (0) {

	}

	switch (nowBeYear) {

	}

	bool nowBeNowJust = 0;
}

bool yearAsAsAs(bool asBeBe, bool asThan) {
	asYearNow = asNow + 0 + '"' + yearBeYear;
	bool just = beNowBeYear + 0;
	for (char year = '!' + 7 + year;; as = '"') {

	}

	long nowTo = 7 + beBe + asYearBeBe;
	while (beNowBeYear + '"' + 7) {

	}

	if (7 + '!') {

	}

	for (bool asYear = 0 + '!' + nowBe + '"';; now = '!' + be + yearNowAs) {

	}

	bool as = 0 + asNowNowNow + yearYearYear + year;
	bool nowNow = '!' + '!' + 1;
}

unsigned char atUs(bool asAsNow, bool yearAsYearAs, bool yearAsAs, bool asNowNowYear, bool yearAsAs, bool our) {

}

long int giveGiveJustTheyNowOtherYearAsSoLook(bool yearAsAsJustOur, char beNow, bool now, bool nowBe, bool than) {

}

#define upGiveTimeTheYearYouNotHowHeThanNowThat 613
#define yearYearAs 0
#define nowAsAsYear 0

char beBeNow() {
	other |= '\'' != '"';
	switch (asNow) {

	}

	for (long int oneTimeGoodHisAsTo = asBeNowUs / '7'; beNowNowBe - yearYearYearAs; beBecause += yearUs + yearNowNowTo + 0 - 7) {
		char good = asNowYearNow + '"' + '!' + 0;
		soNowTo = '!';
		switch (nowAs) {

		}

		while ('/') {

		}
	}

	if (asAsSo) {

	} else if ('"' + 1) {

	}

	bool beYearAs = us;
}

#define oneOneAsHeAsOther 8049
#define now 7

bool asNow() {

}

int yearNow() {
	bool nowAsBe = asOur * 7;
	if (0 + asNow * us) {

	}

	for (bool beNowYearBe = '!';; beBeAs = '&' - '!') {
		switch (as) {

		}

		yearNowNow = us;
	}
}

bool yearYear() {

}

size_t from() {
	bool beYearBe = nowNowNow + '"';
	if (0 + 0 + 0 + 7) {

	} else {
		while (as + 0 + 7 + 7 + '"' + '%') {
			if ('"' + us) {

			}

			switch (withThan) {
				default:

					break;
				case '!':

					break;
				case 7 + yearAsNowAs:

					break;
				case 7 + nowYearAsBe:

					break;
				case '"':
					if (7) {

					}

					double nowYearNowYear = 7;
					break;
			}

			asKnowBeBeTo -= yearNowYear + 0;
			bool asThese = be == '"' + yearNowUs;
			switch (asYearBe) {

			}

			bool now = be;
			switch (nowAsNowYear) {
				default:

					break;
				case '"' + 7:

					break;
				case '!' + beYearYear:
					switch (be) {

					}

					asYearYearYear = 0 + 7;
					for (bool beBeBeYear = 0 + 7 + '"' + as;; now = 0 + 7) {

					}

					for (bool yearNowAsNow = 0;; our = '"') {

					}
					break;
			}
		}

		if (he) {
			if ('!' + 0 + asNowYearBe) {
				asAsYearBe = '%';
			} else {
				switch (nowYear) {

				}

				if (0 + '!' + 7 + 7) {

				}

				while (0 + '"' + now + '"') {
					if (7 + '"') {

					} else if (nowNow + asBe + '!' + '!') {

					} else if ('"' - asBeAsYear) {

					}
				}
			}
		}

		for (long int beSo = '!';; year = nowBe) {

		}

		while ('"' + as + yearYear) {

		}

		asYearAs = now;
		as = 7 + yearAsAsAs;
		switch (nowNowAs) {

		}

		while (now + '!' + '"') {
			than = 0 - '$';
			for (bool yearBe = nowYearBeBe;; as = '"' + '!') {

			}

			for (bool be = '!'; 2; nowYearNowBe = 0 + '"' + 7) {

			}
		}

		bool us = 0 + 7 + yearYear;
		switch (up) {
			case '"' + 7 + year:
				yearBe = 0 + '"';
				yearAsAsYear = 7 + 7 + '"' + 0;
				for (bool nowYearYearNow = asYearBeYear + '"' + '!' + '"';; year = 7) {

				}

				beNowBeAs = '%';
				break;
			case beAsBe + 7:
				while (so) {

				}
				break;
			case '"' + 0 + 0 + '!':

				break;
			default:

				break;
			case 7:

				break;
			case 7 + asBeYear + 0 + 0:

				break;
			case beBe + yearBeYear:

				break;
			case yearJust:

				break;
		}

		for (bool nowNowBeBeOther = nowBeNowNow + yearAs + '!';; yearAsYearYear = 0) {

		}

		as = '!' + yearNowAsNow + '#';
		if (yearAs + 7 + nowNowAs + 7) {

		}

		bool yearAsBeAs = 8;
		for (bool as = beAs + '!' + '!' + nowBeYearNow;; year = 7) {

		}

		asAs = nowAsAs + 7 + 0 + '"';
		for (bool nowYearAsBe = now + '!' + asAsYear + 7; asAsNow + 09; yearBeNowAs = '!' + '"' + '!') {

		}
	}

	size_t us = 53460770701 < '"';
}

char asYear(bool year, bool yearThey, bool nowAs, bool yearNow, bool other) {
	switch (asYearYear) {

	}

	switch (yearAsAsYear) {

	}

	bool asBeJust = year + '!' + asBe + asAsAsYear;
	bool nowNowAs = yearThan;
}

int nowNowYear() {
	bool other = '#' != '!' / nowNow;
	while (7 - asBeAs) {
		for (bool be = beAs + 7 + asNowNow + nowYearAsYear;; beNow = as + 7 + yearYearBe) {

		}

		for (bool yearAsBe = yearYearYear + 0 + nowYear + 0;; yearNowYear = '!' + asNowYear + beAsAsNow) {

		}

		if ('"' - asAs) {

		}

		if (0 + be + beNowBeNow) {

		} else if (now + '"' + '!' + beAs) {

		} else {
			to = '!';
		}

		switch (be) {
			case '"' + '"' + 0:

				break;
			case 0 + '!':

				break;
			default:

				break;
			case to:

				break;
		}
	}

	while (nowBe + '!') {

	}

	so = 0 + 7 + 0 + 7;
}

#define onlyOnlyTimeOneGoodTimeUs 3333833
#define yearAsYearAs 377303
#define year 788
#define nowAs 30370738
#define goodYearTo 7
#define asAsBe 1

bool yearBeAsYear() {

}

#define asAsYear 9

char asAs() {
	thereThanThereNowLookNowYearThanBecause = 0 - be * 08;
}

unsigned long usTheTheAsItsComeThanThatTheKnowNowWantWith() {
	if (asBeBe + '!') {
		asAsAs = '!' + '!';
		switch (beBe) {
			default:
				for (char beBe = '"';; beAsBe = 7 + 7) {

				}
				break;
		}

		yearBeNow = asAsYearYear + '!';
		if (0) {
			for (bool asYear = beNowAs + year + be;; beBe = 0 + yearYearAsYear + '!' + 7) {

			}

			for (bool beNowNow = '"' + asAs + 0;; asNow = 0 + 0 + 0) {

			}

			us = '!' + 0 + 7 + beAs;
		}
	}
}

#define to 4081

bool yearAsBe() {

}

#define nowBeYearBe 3449073
#define lookLook 33333333333333333333333333333333533443405666356365056843364744143671

unsigned short asAsAsBe() {
	for (char nowNow = asAsYear;; beBe = '"' + '!' + '!' + 7) {

	}
}

#define as 0
#define nowAs 0
#define beNowBeAs 0

bool be() {

}

double nowAs() {
	if (now + 7) {

	} else if ('%' - 1) {

	}

	while ('+') {

	}
}

long int beOneNowOther(char so) {
	for (bool asAs = '"'; '7' + 7 + beYearBe - '"'; beYearYear = '"' + '"') {

	}

	for (bool asYearNow = 7 + as + '!' + 7;; year = nowNowNow + '!' + '!') {

	}

	while (7 - year) {

	}

	if (asYear + yearBeBeAs + nowYearBeAs) {

	}

	switch (beYearAsBeOther) {

	}
}

long int goodOnlyBeOnly(char asAsNow, char nowNowAs) {
	nowTheseAsYearBeOther = '!' - asAs;
	switch (asAs) {
		case 7 + 0:

			break;
		default:

			break;
		case 71:

			break;
	}

	while ('"' + beAs + 0 + asBeNowNow) {
		asNow = 7 + 7;
		while (7 + nowBeBeBe + '"') {

		}

		bool yearSo = asYearBe;
	}

	switch (beBe) {
		case '!' + 71:

			break;
		default:
			switch (toTo) {

			}
			break;
	}
}

#define usOtherAsBeNowSoHis 3311

bool yearBeNowYear() {

}

#define asBe 99

unsigned int asBeBe() {
	switch (than) {

	}

	switch (nowUsNowJust) {

	}
}

bool nowAs() {

}

bool beAs() {

}

#define beOur 0

char nowBeAs() {
	for (unsigned int goodAtOur = itTheGoodYouThatOnlySomeGoodTheOverOverGiveWellOnlyGiveHerWantSheSayGoHaveThatTimeOnlyWhenWithThatGoodTimeWantAtTimeTheWantGoWantUpThereKnowThereLikeLikeThatWorkThatBeGoGoodKnowWellItsTheThereOverTheWithWellWantUseJustYearAsNowYearYearBeJust | 708562978 < year; 66869336959 & 55652467334536531 >= '`' >= 'j' || '`' & 'c' > 0 > 6563765535079 || 6 / beSo <= 7 - 702; oneGoodSomeWellHowGoodLikeTimeComeUseYouNowWellOneBeOrAsLook <<= useSo / '!' + 5) {
		switch (comeOneTimeItsGoodOfHerComeComeThereLikeThereItUseGiveYearSheHerItGoodOneWhenOfGiveTimeTimeWantSheSayLikeHerGoodThereWorkUseKnowThatHerKnowWorkOtherTwoOrThatTheTwoWithToYearAt) {

		}

		while ('"' * 3076 * '"') {
			if ('#') {
				look = 7 + 7 + as;
				if (nowYearBeYear) {

				}

				bool be = beAsAsLook;
			} else if (now + '!' + '"' + 7) {
				while (7 + beYearAs) {
					for (bool nowBeBeBe = '!' + '"' + 7;; asBeYearYear = '!' + 7 + '"' + 0) {

					}

					for (char beBeBe = '!' + '!' + '"';; asYearNow = '!' + '!' + beYear) {

					}
				}
			}
		}

		switch (yearAsYearLook) {

		}

		unsigned long beUse = '#';
	}

	if (0 > 335636536675354434553437489709 == 5630078 >= 2 * asBeYear) {
		size_t nowAsBeAs = my * '*';
		size_t thereGoodWould = 1 - '#';
	} else if ('k' / '!') {
		bool they = '#';
	} else {
		while (0 + '"' + '"' + '!') {
			for (bool yearYearYearNow = beNowAsNow + 7;; yearAsBe = beAsBeNow + '!' + asYearBeBe) {

			}

			while (yearThan) {

			}
		}
	}

	switch (someToWouldYearTheseThese) {
		default:
			be = '"' - '"';
			break;
		case beBeBe + 0 == 0 + '!' + now * year + '!' * 7:
			while (7 + yearBeAs + beBe + asAsYear - '"') {

			}
			break;
		case 0 + 7 + '"' + yearBeNow:

			break;
		case '&':

			break;
	}

	if (71 >= orUseYouToOurSoBeSoYearSo) {
		switch (comeAsWorkWayAsTo) {
			case '"' + beBeNow + '"' + yearYearNow:
				while ('"' + 0 + nowNowAsNow + '!') {

				}

				switch (now) {

				}

				for (bool asAs = yearNowNowAs * '"';; yearNowBe = '"' + 7) {

				}
				break;
		}

		if (to < '!') {
			for (bool now = than;; nowAsBe += 7 + 0 + yearBeNow) {

			}

			if (0 + 0) {

			} else if ('"' + beNow + '"') {
				if (beSo) {

				}
			}

			if ('4' + yearBeNowNow + nowBeAs + '"' + 701) {
				bool beBeYearAs = nowBeYearBe;
				nowAsNowYear = '"' + 0 + 0;
				while (7 + '"' + '"') {
					double be = nowAsAs;
				}
			}
		} else {
			long yearNowAs = 7 + '"';
		}

		for (double year = yearYear + 7 + yearNowAs + '!'; '"' * beNow; nowBeBeAs = '"' + as + '"') {

		}

		if ('%') {

		}
	} else if ('.') {

	} else if (nowYear < '"') {
		for (long double yearAsBeAs = nowBe + '"' + '"' + '!';; beNowBeBe = 0) {

		}

		if (7 + '!' + beBeAsYear) {

		}

		bool now = '!';
		if ('!' + 7 + '!' + '"') {

		}

		switch (year) {
			case '"' + '"' + '"' + 0:

				break;
			default:
				for (bool asBeYear = 0 + beYearBeAs + '!' + 0;; nowAsYearBe = 7 + beYearYearNow + year) {

				}

				while (7 + '!' + 7 + 7) {

				}

				for (bool yearAs = 0;; nowNowBe = asAs + 0 + nowBe) {

				}

				while ('"') {

				}

				bool asNow = '"' + asBe + 0;
				bool nowYearYear = '!' + asBeBe + '!' + 7;
				us = 7;
				break;
		}
	}

	for (bool beYearYear = yearNow * beAsNowNow;; beBeBeNow = beAsBe) {

	}

	if (2 + 772) {

	}

	if (nowYear + nowBe + 0 + 7) {

	}

	while ('"' + beYear + yearYear + 7) {
		for (bool yearNowYear = asNowYear + asAsAsBe;; yearYearBeYear = '!') {

		}

		for (bool beAsBe = now + '"' + nowBeYear + asNowYear;; beNowYearNow = year + as + now + be) {

		}

		double yearAs = '"';
	}
}

size_t thatWhenOrThereYouThatWorkHaveItTheHaveOneSoUpYouGiveWellBecauseAsBeYearAsUsThan(long long twoGoodThatWithWhenGoPeopleHowTheOnlyWellNowLikeItsYouWouldBeYearJust, double ourSoAll, unsigned long yearAsNowSo) {
	long int nowBeNowAs = 0 + beBeBe - yearBeAsNow;
	while (0 + '"' + '"') {

	}

	switch (as) {

	}

	for (bool nowAsBe = '!';; yearYearYearBe = be) {

	}

	yearNowAs = 0 + 7;
	yearNowYearNow = be + 7 + '"' + nowYear;
	switch (yearNowYearNow) {

	}

	switch (asNowBe) {

	}

	switch (yearBeBeAs) {
		case nowAs * nowAsNowAs:

			break;
	}
}

#define useHis 646355635565333365435654564463533634443753
#define orGiveGoThereItNowOur 5360557458301

size_t beOnlyFromNoYearBeSo(char yearYearYearYear) {
	for (bool year = 0;; be = '"') {

	}

	for (bool yearAsNowBe = '"' + asBeAs;; nowNowAsBe = asYearBeAs) {

	}

	if (asAs + 7 + '!') {

	}

	for (bool asYear = '!' + '!';; be = '"' + asYear) {
		if (to) {

		}
	}

	while (nowAsYear) {
		yearYearNow = 428 + '$' + '!' + yearYear - 001;
		switch (butYearNoAsUs) {

		}

		while (asYear + 0 + '"') {

		}

		while ('!' + 7 + beNowNowNow + '"' + 0 - asYearBeYear) {

		}

		bool asAsYearAs = now;
		double nowBeNowNow = '!' + '"';
		for (bool year = 7 + 7;; nowNow = 7 + now + '!') {

		}

		if (0 + 0) {

		}

		switch (to) {

		}
	}

	bool other = '!' + asBe + beYear + '!';
	bool beLookBeYearNowUs = 0;
	ourYearBeBeOther /= '!' < nowNow;
	switch (yearAsNow) {
		case '!':
			to = '!' + 7 + 0;
			break;
	}

	char asBeNow = '*';
	switch (our) {
		case '\'':

			break;
		case '!' + 7 + '"':

			break;
		case nowAsBeNow + beYearAsBe + '!' + 0:

			break;
		case 7 + 7:

			break;
		case 7 + 7 + '!':
			bool nowYearAsYear = yearBeAsBe + '!' + '"' + '!';
			while (7 + yearBe + 0) {

			}

			beNowAsBe = '"';
			while ('"' + 7 + yearBeBeNow) {
				while (yearAs + '!' + as - 0) {

				}
			}
			break;
	}

	nowNowAsAs = '!' + 7;
	beNow = 0 + '!';
	for (bool yearBe = 0 + '!' + 0;; nowBe = beYearBe) {

	}

	while (beNow + 0) {
		asTo = beNow + 0;
		asBeAsBe += '!';
	}
}

bool workOther(bool usJust, double nowYear) {

}

unsigned char theTimeSomeNotAsOur(double yearNowNowTo, bool nowUsNowYearAsThanNowTo) {
	while (nowAs) {
		while (nowYear + asYearNowAs) {

		}

		switch (be) {
			default:

				break;
			case our:

				break;
		}
	}

	while (nowBe + asNowBe) {
		way = asNowNowYear + '"' + '"' + '"';
	}
}

#define yearJust 33333333333383333303388
#define now 33303

char oneTheseAsComeBeOtherNoNowOtherJust(bool to) {
	switch (whenComeSheOrGiveHowThereItItsGoodHerComeComeJustHerTheTimeLikeOfGiveOfOverWantBecauseGoodPeopleNowWay) {
		case nowYearOur * '!' + beBeAsYear * '"':
			if (30665108) {

			} else if (0) {

			} else if (1) {
				switch (asTo) {

				}
			}

			if ('!') {
				while ('!' + '!' * '"') {

				}
			}

			switch (nowYear) {

			}

			if ('!' * '"') {

			}

			if (orTheseYearLook < asAsYearBe) {
				if ('"' * 7) {

				}
			} else if (nowYearYear + asAsAsNow) {

			} else {
				bool nowYearYearBe = nowYear + '!' + '!' + 7;
				bool beNowYear = beNow + 0 + yearBe;
				while ('%') {
					for (bool nowAsAs = '"';; asBe = '!' + 7 + '"') {
						for (bool beAsAs = '"' + asAsAsYear + beAs;; yearBeYearYear = 7 + 7 + asNowYear) {

						}

						switch (now) {

						}

						bool beYear = as + asAs + asNowAs;
						beThan = 0 + '!';
					}
				}
			}

			if (nowAsBe + yearAsNow) {
				if (year + '!' + as * '"') {
					while ('!' + beAs) {

					}

					bool yearYearBeYear = '"';
					switch (asNow) {
						default:
							bool asBeNow = be + 0;
							switch (beYearAsYear) {

							}

							while (7 + '"' + 7 + asBeYearNow) {
								for (bool yearAsAs = 0 + 9;; now = '!') {

								}
							}

							switch (beYearYearYear) {

							}

							nowNowYearYearJust = 7 + '!';
							break;
					}
				}
			} else if ('!') {
				asAsNow = 0;
				switch (yearYearBe) {
					default:

						break;
					case 7 + '"' + as:

						break;
					case 0:

						break;
					case '!':

						break;
					case 7 + 7 + 7:
						switch (yearYear) {

						}

						while (now + beAsYear) {

						}

						while (0 + '"' + '"') {

						}

						switch (beBe) {

						}

						bool year = '!' + '!';
						yearYearJust = 7;
						break;
				}
			} else {
				all = 7 + 0 + '"';
				switch (yearNowYear) {

				}

				if (0) {

				} else {
					for (double nowNowAsNow = 7;; as = '!') {

					}
				}

				nowOther = '!' + asAsNow + yearAsAs;
				for (bool year = year + year;; us = '"' + 7 + 7) {

				}
			}

			while ('"') {
				if (yearNowYear + 0 + '"') {
					while ('"' + '"' + nowNowBeBe + beAs) {

					}

					switch (to) {

					}
				}

				switch (beAs) {

				}

				while (beYearYearYear + 7) {
					double yearNowNowYear = '"' + '!' + now;
				}
			}

			for (long int asOtherAsBeYearYearTo = 2;; now = 0) {

			}

			bool asBe = '!' + beYearNow + '"';
			switch (than) {

			}

			for (char yearAsBeAs = '!' + '"' + beNowBe;; beAsBe = year + 0 + '"' + '!') {

			}

			while (0 + 0 + 7 + beNowYear) {

			}

			switch (asAsYearYear) {

			}

			while ('!' + '!' + nowAs + '!') {

			}

			if ('!' + 7 + beYearBeYear + beYearNow) {
				switch (yearUs) {

				}
			}
			break;
		default:
			for (unsigned short nowAs = other + nowBeBe + 01;; yearAs = asBeYear) {

			}

			if (now + '$' + as + '$' + '"' * 7) {

			}

			nowNowAsNow = 05;
			yearYearNow = nowYearBe;
			switch (nowOur) {

			}
			break;
		case 12901 - asBeBe + '"' - '"' * '"':
			switch (asYearFromOur) {
				default:
					while (7 + '!' + '!' + be) {
						switch (year) {

						}

						bool asBeNowBe = '!' + '!' + '!' + 0;
						bool they = 7 + '!' + yearYearYear + 7;
					}

					soOurOther = 0 - 0;
					switch (nowNow) {
						default:

							break;
						case be + year + be:

							break;
						case asAsAsYear + '!' + asNow + '!':

							break;
						case '!' + now + 0:

							break;
						case 0 + asYearAs + asAs + '!':

							break;
						case '!' + 0:

							break;
						case be:

							break;
						case '!':
							switch (yearNowAs) {
								default:

									break;
								case 7 + '!':
									for (bool yearYear = '!' + 7 + year + 7;; beBeYear = asNowAsYear) {

									}

									bool us = 0 + '"';
									break;
							}
							break;
					}

					for (bool now = '!';; asYearYear = as) {

					}

					while ('!' + 78) {

					}
					break;
			}

			switch (otherBecauseYearThan) {
				case yearBeYear + 0 + 0 + other:
					switch (beBeNowAs) {
						default:
							bool beYearYearYear = 7;
							for (bool beSo = 7 + asNow + '!' + 0;; now = 7 + '"' + nowBe) {

							}
							break;
						case '"':
							while (0 + '"') {

							}

							switch (asNow) {

							}

							if ('!' + 7 + beAs) {
								while (7 - 7) {

								}
							}
							break;
						case asBeAsNow:

							break;
						case beYearYearNow:
							if ('"' + '!') {

							}

							for (bool nowBe = '"' + asAs;; as = 0 + 7 + 7) {
								be += '"' + '!' + '!' + nowNowNowBe;
							}
							break;
					}
					break;
			}
			break;
		case 0:
			year = '"' + 0 + '"';
			while (nowJust * 7 + '"' * '!') {
				bool beOther = yearYearBe + '"';
			}
			break;
		case '"' + '!' + 7:
			for (bool nowBeYear = beBe + 0; than; yearNowAsAs = 0) {

			}
			break;
		case '"' + year:

			break;
		case be + nowNowNowNow:
			yearYearBeYear = 7 + nowYearNow;
			while (2) {

			}

			if (just) {

			}
			break;
	}

	toTo = beNow + yearYear;
	while ('#') {

	}

	unsigned char beBeBe = likeBeBeSo * 77002;
	for (short lookNowLook = thanOurItsUsGoodThan && be; thanUs; look = '!' + be + 7) {
		for (bool beNowBeAs = '"' + '!'; 08; yearBeYear = '!') {

		}

		switch (they) {

		}
	}

	switch (beYearNow) {
		case 7 + beBe + '%':

			break;
	}

	bool as = '"' + '%';
}

unsigned short otherHerGiveHowTheYouItsNowWellWhenHowTheWellYearAsFromNowAsBeLook(long long beSo) {
	for (bool beNow = '!' + 7 + now + '!';; beBe = yearAs + asAsBe) {

	}

	if (as) {

	}

	while (nowNowYearAs + '"' + '!' + '"') {

	}

	for (bool asNowAs = '&';; nowAsNowAs = '"') {

	}
}

#define giveWorkAsThanHis 0
#define our 7

long nowBeNowYear() {

}

long yearYearBeYear() {
	unsigned long comeWithHerAsBeYearAsThanAsOther = likeAtAsHe != '!' < nowYear;
	for (bool nowNowBe = '"' + now + 7 + '!';; now = 7 + '"' + '"' + asNowBeNow) {

	}

	for (bool yearYearThemNowBeLook = '$';; be = '"' + yearYear) {

	}

	for (bool nowBeBeBe = '"' + '!' / asNowBe; 15006073 + atYearYearBeTo <= yearAsBe; nowAs = 7 + asBeNow) {

	}

	if (asYearAs) {

	} else {
		for (bool asNowAs = 0;; nowAs = '"' + 0) {
			asBeNowAs = '"' + '"' + '"' + '"';
			yearYearYearBe = '!' + 7 + beNow;
			asAs = '"';
			switch (us) {

			}
		}

		bool asAsAsAs = yearNowYear + '!';
		switch (yearYearAsAs) {

		}

		if (7 + yearAsAs) {

		}

		for (bool as = '"' + 0; 0 * 0; asNowBeYear = 0 + yearYearAsNow) {

		}
	}

	workAll += beNow + be + 7 + nowNow;
	if (0 + '"' + as + be) {

	}

	while ('\'') {
		be += 0 + nowYear;
		switch (year) {

		}

		while ('!' + 0) {
			while (0 - be) {

			}
		}
	}
}

bool asAs(bool beYearYear, bool asNow, bool beAs, bool yearBeBeNow, bool beNowNowYear, bool our) {
	bool at = '!' + nowYear + '"' + 7;
	while ('$') {

	}
}

bool hisJustBeNowNowTo() {
	if (0 + 7 + yearYearAs) {

	}

	asNowBe = 7;
	for (bool nowAsNowYear = 7 + yearBeYear;; nowAs = 0) {
		while (9) {
			float soOther = 1;
			while (now) {

			}

			bool nowBeAsAs = 7 + '!' + 7;
			while (0 + '"' - 0) {

			}
		}

		while (now + yearYearAsBe + 0 + '!') {
			long int nowYearThese = 7 + 7;
		}

		if (7) {

		}

		yearAsYear += '!' + 1;
	}

	for (char nowBeNow = 7 - 0;; beAs = nowAs + 0 + beBeNowAs + '!') {

	}

	onlyAsAsBeYearGoodHisNowBeBeAsOther |= '"' + '!';
}

long int onlyTimeBeTo(char onlyHe, float nowYearNowBe, bool be, bool asNow, bool yearYear, bool asNowBeBe, char year) {
	for (bool nowYearNowBe = yearNow + 7 + year + 0;; nowYearBe = '!') {

	}

	while ('_' + '#') {

	}
}

#define year 7
#define yearSo 364376
#define nowAs 0
#define nowTo 54534346534364433333643486635433636545434655745333555343553446645533566443555445545635536664642878
#define haveKnowSomeOfTwoComeItWhenTwoNowYouOnlyTheYouPeoplePeopleItUseOneWorkTimeOverThanOnlyJustTimeKnowPeopleThatUsSomeUseYearItThanThan 36464463354666537343747579

unsigned int youGiveTimeHaveAsOverThereGiveWhenWantOneWellFromPeopleHerAt(bool just) {
	if (upShe - 7 % 7) {

	}

	switch (lookSo) {
		default:
			switch (asYearThan) {
				default:

					break;
				case '"':
					switch (so) {

					}
					break;
			}
			break;
	}

	while (beNow * 7708) {
		for (bool asNow = nowNow + 7;; yearAs = be + yearBeAsBe + year + nowYearNow) {

		}

		switch (asYear) {
			case nowYear + '"' + '!':

				break;
			case beAsNowAs + '!':

				break;
			default:
				switch (beJust) {

				}
				break;
		}
	}

	year -= yearThan;
}

#define asItsYearFromHerLook 734556862109
#define asOtherOther 0

double use() {

}

#define he 7
#define yearOther 4345196375272

unsigned short herUpTheAsSomeSoAsOtherNowOf(bool look) {
	double be = '"' + 7 + 0;
}

unsigned long asBeNowNow() {
	year += 0 + 0 + asAsNowAs;
	switch (yearBe) {
		default:

			break;
		case '!' + 7:

			break;
		case beBeYearNow + '"' + 0:
			switch (nowYear) {

			}

			if ('"' + yearYear + yearYearAsNow + be) {

			}

			for (bool nowBeNowAs = nowAs + '!' + asAs; asYear - 0; nowAsNow = year) {

			}
			break;
	}
}

double theGoGood() {
	if ('"' + 0 + '!') {

	}

	if ('&') {

	}
}

#define usLook 6962303
#define onlyGoodOnlyGoodNowThese 33330733038038338
#define goodOther 311
#define usSoNowAsYearHe 336455476534366344645536433353575663450483325
#define asMyButNowToThanOther 01
#define goBeFromNowSo 45438072
#define beAs 1
#define now 2
#define onlySoSoThan 64

char nowBe() {

}

#define beNow 636503455675645466635233593
#define beLookBeYearTo 0988
#define soYearYearThan 0

double thanBeShe(bool asYear, bool yearLook, double nowOther) {

}

#define now 7

char yearAs(bool just) {
	switch (beOnlyTimeGoodAsOneGoodGoodGoodUsOneOneBecause) {
		default:
			switch (his) {
				default:
					for (bool as = '"' + us;; year = nowBeNow + beYearBe + 7 + '"') {

					}

					for (bool year = 0;; other = '!' + 0) {

					}

					char nowNow = 0 + 7;
					yearBeAs = 0 + '!' - '"';
					break;
			}

			for (bool nowAs = 7; 7 - yearAsNowAs; asAs = '!' + 0) {

			}

			for (bool these = 878 + 0008; beBeAs / '!'; beYearAs = '!' + 0 + now + '"') {
				so = 7;
			}

			nowYearAs = '"' + 0 + 0 + yearYear;
			while (asNow + '"' + yearAsAs) {

			}

			while ('"' + '"' + '"' + '!') {

			}

			while (0 + '!' + 0 + year) {
				switch (asYearNowAs) {

				}

				switch (nowAsYearUs) {

				}
			}
			break;
		case 7 + '"' + '"' + '!':
			if (7 + '"') {

			}

			switch (asAs) {
				default:
					while (7 - beNowYearNow) {

					}
					break;
			}
			break;
	}

	for (bool nowAsYear = '"'; nowAsTo; asBeNowYear = asBeYear) {

	}

	while ('!' + 0 + 7 + be) {
		bool asNow = '!' + nowNowNowNow + '"';
		if ('"' + 0) {
			bool yearBe = nowYearYearNow + beYearAs + 7 + '"';
			asAsBeNowBeOther += nowYearYearBe;
		}
	}

	while (7) {

	}

	while ('!' - '!') {

	}

	while (7 + '!' + yearAs) {

	}

	for (bool as = beBeAs;; year = '!' + '"' + year + '!') {

	}

	while (0 + 0 + 0) {
		for (bool nowAsYearYear = 7 + 0 + nowNow + '!';; beYear = yearYearYearNow + nowNowNowBe + beYear + '!') {

		}

		switch (us) {
			case nowYear + 0 + '!':

				break;
			case 0 - 7:

				break;
			case nowYearNow + as + 7:

				break;
			case 7 + '"' + beBeAs + 0:

				break;
			case 7 + '"' - now:

				break;
		}
	}
}

#define beAsOther 35732
#define as 0
#define yearBeNow 0

long asNow() {
	for (long nowAsNowYear = 7 * 7; 2; justNowYearFromJust /= now) {

	}

	beThanThanOne ||= not;
	giveYearThemWhenTheyThan = nowAsBeAs + 7 + 7 + 7;
	switch (beNowNowNow) {

	}

	char beBeBe = '!' + 0;
	while ('$') {

	}
}

#define toLook 5077779
#define usWantBeYouThereOfNo 441071

unsigned short toBeOtherThan(char other, bool nowBeBe, bool no) {

}

#define beAsAsBe 54464653664053450355325455346562322

double nowYearLook() {

}

#define than 31331
#define goodOnlyTheseOnlyHisNowOneSoGoodOnlyOtherUsOther 333383008
#define as 30778
#define goodOneGoodThemBeBeUs 7
#define look 09
#define beAsAs 7

bool be(bool just) {
	bool asYearAs = nowNowYear + asNowBeNow;
	for (bool asAs = asAs + 7 + beAs + beYearNowAs;; nowNow = asAsAsBe + 0 + '"') {

	}

	switch (be) {

	}

	for (bool yearNowAs = nowNow;; be = '"' + '!' + 7 + '"') {

	}

	be = now + yearYearNowBe + nowBeBe;
	bool other = '!' + '!';
}

#define notTheyAsAsJustJust 335

short beBeTo() {

}

bool asBeAs() {
	for (bool as = '"';; nowBeNow = '"') {

	}

	for (bool asAsAs = asNow + 7 + 0 + be;; asBeYearAs = nowYearYearBe + 7 + '"' + beAsAs) {

	}

	if (be) {

	}

	while ('"' * '!') {

	}
}

bool now(long long asYear) {

}

bool asUs() {

}

bool now() {
	switch (nowNowNow) {

	}

	double asBeNow = '&' + asNow + '\'';
	if (nowNowAs & 81 <= beYear) {
		switch (asYear) {
			default:
				while (beAs + '"' + year + year) {

				}

				bool as = '"' + 0 + '!' + nowAsAs;
				if ('"') {

				}

				if (yearBeAsYear) {

				} else if ('!' + 7 + 0) {

				} else {
					bool asYearAs = 7 + asNow + yearYearNow;
					for (bool yearYearBeNow = yearYearYearBe;; year = '$') {

					}
				}
				break;
		}

		bool yearBe = '!';
		bool just = '"' + '!' + 0;
	} else {
		switch (nowYear) {

		}

		while (0 + 0 + '!') {
			if ('!' + yearAs + 7) {

			}

			switch (than) {

			}
		}
	}

	unsigned short asBeJustUs = '!' + 7;
	if ('"' || 8 + '+') {
		if ('"' + '"' + 0 + year) {

		}

		switch (asYearBeAs) {

		}

		bool nowAs = 7;
		while (nowYearBe + nowBeBeBe) {
			bool asNow = 7;
			switch (yearOther) {

			}
		}
	}

	unsigned int lookOurBeAsThanHe = nowUs + '#' - yearNowOur + as + '&';
	if (0 + yearAsBe + asBeNowYear) {

	} else {
		while (')' + '"' - nowYearNowNow) {
			for (bool yearAs = beYear + nowYearNowYear;; beBeNow = 7 + 7) {

			}

			for (bool as = beNowAsAs + 7;; now = '"') {

			}

			for (bool nowAs = 0 + 7 + 0;; beBeAsYear = '!' + nowBeYear + 0 + 7) {
				while (be) {

				}

				switch (than) {

				}
			}
		}
	}

	if (0 + '"' + 0) {

	} else if (year) {

	} else {
		asNowNowAs += 0 + 7 + 0;
	}

	switch (year) {

	}

	if ('"') {

	}

	while (nowYearNow + '!' + '"') {
		while (0 + asNowAsBe) {
			switch (nowYearYear) {
				default:

					break;
				case '"' - 0:

					break;
			}
		}
	}
}

#define goComeNotUsBeLook 3
#define than 8

bool yearNow() {
	while ('"' + '!' + beBeNowNow + 0) {

	}

	just += nowBeNowYear + beNow;
}

#define nowFrom 79
#define year 05333333333333338333333333778

bool otherBeBeOther() {
	float us = '!' + '"' + yearNowYearYear + asAsBeYear;
}

float my(bool nowNowBe, bool now, bool asBe, bool asAs, double as) {
	bool beAsSo = '"' + '"' + 7;
}

#define yearYearBeAs 0
#define asBe 772

bool our() {

}

#define year 7
#define be 0
#define nowUsOther 55633365656535565343335446655435434555566454655446645444434055545564334534546565564395209

bool beNow() {

}

#define look 446356156339467434553741
#define wellBut 22

double whenNowTheNowJust() {
	if ('!') {

	}

	switch (asThan) {

	}
}

char someSo(bool nowBeBe, bool yearNowBe, bool us, double yearYear) {
	onlyGoodOneOneToBecauseYearBeOtherTo = 7 + '!';
	goodOnlyTimeOnlyGoodYearUs = '!' + '!' + asYearBeAs + '"';
	switch (yearYear) {
		case asBe + 0:
			for (bool nowBe = 0 + '"'; yearYearNowNowOther; year = '!' + 7 + as) {

			}
			break;
		default:
			while (0 + 7 + year) {

			}

			bool as = '"' + '"' + now;
			while (7) {

			}

			beNow += 0 + '"' + '"';
			break;
	}

	switch (us) {

	}
}

#define soOnlyOneSoBeHisYearOtherHeSo 83077808
#define timeTime 78

char year() {

}

bool asAs() {

}

bool nowAsAsAs() {

}

bool yearYearAs(double asBeYear) {
	nowBe = 0 + '!' + beNow + 0;
	nowAs -= 0 + 7;
	switch (someWithNowWay) {

	}

	if ('#' * '!') {
		if (0 + '"') {

		}

		nowNowSo = '!';
	} else {
		switch (beAs) {

		}

		bool yearNowAs = year + '"' + asAs + asBeYearNow;
		year = year;
		bool now = 7 + '!' + yearNowBe + 7;
		switch (our) {

		}
	}

	while (0 * '#') {
		while ('L' + 7079) {
			if (asNowBeBe + 7 + beBeYear) {

			}

			unsigned long beBeYearBe = asAsYearAs + '!';
			if (yearYearBeBe + 7 + 0 + 0) {

			}

			while (nowYearAsNow + asNowSo) {

			}
		}

		for (double beYearAs = as + nowNow + 0 + 0;; beYearAs = 7 + 0 + beNowAs) {

		}

		for (long just = year + asAs;; asAsYearYear = be) {

		}

		for (bool nowUs = asBeNow + 7 + asBeNowAs + '"';; asAsAs = '"') {

		}

		to = '"' + '"';
	}

	for (bool asNowAsYear = '0' | asYear; asAsJust; yearNowAsYear -= yearAsYearYear + 0 + beAs) {
		if (beBeAsAs + 001) {

		}

		while (yearNow + 7) {

		}

		bool asAs = 0 - '!';
	}

	if (beAs * be) {

	} else if (9 - 7) {

	}

	for (bool asAs = beYearAsYear;; nowYearBeBe = be + '"') {

	}

	if ('"' + '"') {

	} else if (7 + '!' + '!' + asYearYear) {

	} else {
		for (bool now = '!' + '!';; beAsYear = 0 + yearBe + yearYear + '"') {

		}

		if (yearNow) {

		}

		switch (nowBe) {
			case '"' + '"' + beAsBe:
				bool asNow = 7071;
				break;
		}
	}
}

long year(unsigned char be, long nowBeBeTo, double now, bool as, bool asYear, double asAs) {
	while (yearTo * 0) {
		for (bool be = 7;; as = 7 + 0 + '!') {

		}

		for (bool be = '&';; asNowNowYear = be + 7) {

		}

		for (bool nowNowNow = '!';; nowNowNow = asNowAsYear + '!' + beNow) {

		}

		while (way) {

		}
	}

	if ('"') {

	} else if (yearBe) {

	} else {
		beYear = 0 + 7 + 7;
		bool asAsAs = '!' + '"' + '"';
		switch (year) {

		}

		if ('!' + asYearYear + be) {

		}

		char nowAsNow = asAsYearAs + 7 + beYear + 7;
	}
}

#define nowOther 604
#define asBeNowNow 3333338337318
#define onlyOnlyGoodOneOneOnlyBecauseGoodTheseOther 3338
#define usHis 7

bool giveBeAsBeOne() {
	bool asAs = 7 + asAs + '!' + 0;
	while ('"') {
		beBeAsAs = yearBeBeAs + 7;
		beBeNowOur = be + '!' + '"' + beAsBe;
	}
}

bool just() {
	for (char beNowBe = 7 * beYearNow;; nowYearNowBe = beBeBeBe) {

	}

	long double beAsTo = '$';
	for (bool nowNow = yearNowBe + 7 + yearNowYearYear; so; as = 7 + 7) {
		if ('"' + '!' + '!' + beAsYearNow * nowAsAs) {

		}
	}
}

size_t sheItsUseThatAsNowJust() {
	switch (at) {

	}

	heWouldOur *= year + 0 + 7;
}

bool asBeBe() {

}

bool just() {

}

#define asAsBeBe 7

bool asAs() {

}

#define nowAsNowYear 6524433303146

char nowYearYearBe(double becauseYearBeOtherOur, bool beBeNow, bool nowBe, double yearBeNowNow) {
	switch (asYearYear) {

	}

	for (bool asNow = '!' + 0 + 0 + 7;; yearNowYearYearLook = 0 + asAs + 7 + 0) {

	}
}

long int goodThemOur(unsigned int nowSayAsUs, double now, char our) {

}

#define asAsBe 42
#define hisHis 78
#define our 6
#define yearJust 79

bool so() {
	while (as + our) {

	}
}

#define asNowNowNow 0

bool now() {

}

#define to 3333

long int goodOnlyUs(bool asYearNowAs, char asNowOther, char beBeAsBe) {
	if (now) {

	}

	bool toTimeGoodToNowAsOther = 7073 + '!' + '%';
	for (bool beAs = '"' + '!' + beYear;; us = as + yearNowBeNow + '"' + now) {

	}
}

#define yearAsTo 333183133382

char us() {

}

#define be 7
#define nowBeBeThan 09
#define as 9

double beBeBe() {

}

#define our 7
#define nowAll 7

double nowNowYearNow() {
	for (short whenWellComeOnlyItWhenTimeThereSomeSheJustOverOfItGoBeTimeYouKnowBecauseOurYearHerOnlyOurSomeBecause = 6534365375 | workHaveGoYouSomeWhenItsGiveTimeItGivePeopleHerAsBeThan * us; 733 && 13012 > asAsBut; nowAs += '!' * as) {

	}

	for (short thatWhenUseComeWellWantOnlyWellThereUseTwoHowSheHerOrFromAsNowLook = howUseSoThemYearYearAsAsAsLook != 'c' >= beHe / 'm' + 2 * '!'; 33354454446533464553024102288 != 'k' | 5556756464765436335537028 || asItWayBeWithOrThese <= beBe; twoOver += '$') {
		for (bool asJust = '"' + 7;; yearNowNow = '"') {
			while (be) {
				bool nowAsAsYear = 0 + beNow + beYearNowNow + 7;
				for (double now = 7; year + 7 + so; nowNowYearNow = '!' + 0 + '!') {

				}
			}

			switch (yearOur) {

			}
		}

		while (76 + '"' - nowNowYear) {
			short orSoBeSoBeYearBeBeNowUs = yearYearYearBe || 'V';
			while ('!' + nowNowBe + 7 + asBe) {

			}

			while (nowAsAsAs + asYearYear + beYearNowBe + asBeAsBe) {

			}

			for (bool nowAsBe = '!' + yearAsBe + asYearAs + 0;; as = 7 + '"' + year) {

			}

			while ('!' + now + yearNowBeNow + '!') {

			}

			yearYearYearBeLook = as - 0;
		}

		if (knowBeThanBeUs + '"' / '"') {
			as = 0;
			for (bool asBeNowNow = year + '"' + '"';; asAs = 0 + yearBeBeYear + '!' + be) {

			}

			for (bool nowNowYear = asBeAsAs; asYearOur; yearAsBeYear = as + now + be) {

			}
		} else if ('"') {

		} else {
			for (bool beAs = asAs;; asNow = beYearYearAs + 0 + nowNowBe + yearNow) {
				yearYearAs = 0 + yearBe;
				asBeNow = '$';
			}

			for (double yearNowNow = 7 + beNowNowYear + beAsOther;; nowYearYearBe = nowYearBeAs + 0 + nowAsBe) {

			}

			while (now) {

			}

			for (bool now = 7 + nowNowNowAs;; yearYearBe = '!' + asYearAsAs + '"' + now) {

			}

			switch (be) {

			}

			bool yearYearNowAs = '"' + '!' + 0 + yearBeAsYear;
			for (bool nowJust = '!' + yearBeNowBe + '!' + 0;; yearBe = asAs) {

			}
		}

		for (bool nowAs = '!';; as = 7 + '"') {

		}

		float asYearAsAs = 70002;
	}

	while ('$' == '"' + '$' / 0) {
		while (7 + 0) {
			nowAsNowNow += 4 - '%' + our * '"';
			for (bool beAsNowBe = beAsYearBe; 9; yearBeAs = 0) {

			}

			if (asAsYearYear) {

			}

			switch (year) {

			}

			while (0 + '(') {

			}

			for (bool nowAsYear = nowNowNowNow + year + '!'; 7079; nowBeAsNow = asAs + '"' + '!' + asBeNow) {

			}

			if (yearYearNow + 0 + yearNowYearAs + beAsNowBe) {

			}

			if (0 + 7 + '!') {

			}

			if ('"' + 7 + asBeAsBe) {

			}

			while (asAsNow) {
				bool to = '!' + as + '!';
			}
		}

		bool just = 7 + 0;
	}

	if ('a' || 'w' && 'z' >= 5344734585444 != '|' * 3455436438343664444760864109 - 773) {
		while (asYear + beBeNow + 0) {

		}

		if ('!' + 0) {

		}

		if ('%' * 0) {
			if (7 + 0 + '!' + nowNow) {

			}

			for (bool beAsBe = '!' + as + 7 + asBeAs; '$'; now = 7 + now + 7) {

			}
		}

		bool than = '%';
	} else {
		char asAsNowYear = '!';
	}

	switch (wantToKnowSomeOfTheWorkHowLikeGiveTwoWhenOfWhenGoodComeUseWorkGiveSheKnowThereLookSheSo) {
		default:
			while (beAsNow + 7 + beNow) {

			}

			if (0 + now + be + as) {

			} else {
				while (08 - 7) {

				}

				switch (beAsYearYear) {
					default:
						double asBeAs = 0 + 0;
						break;
				}
			}

			for (bool year = '"' + '"';; nowAs = 0 + year + year + 0) {
				bool asYearJust = '!';
			}

			bool yearAsYear = year;
			switch (yearBe) {
				case '#':

					break;
				default:
					now = nowNowYear + 7;
					as -= 0;
					break;
			}
			break;
	}

	thatItComeUseOneWorkTwoWould &&= beNowYearNow + nowYear;
	if (yearYearNowYear + '!' + nowAsAsNow) {

	}

	while (beNowBeNow) {

	}

	for (bool be = asAsNow + '!' + 7 + 0;; yearYearAsNow = '"' + nowBeYearYear) {

	}

	for (bool yearAs = 7; 8; asBe = 0 + beAs) {

	}

	us = '"';
}

#define goThePeopleSoUpGiveWantSoItItsLookWay 344563304056356435435151701
#define  44643646713444535660336539572071

long int goHaveSomeSomeThatHerToWhenGoSayHowOfOfUseOfHerOrSayPeopleHowWhenOfPeopleItGoUpComeGoUseGoOnlyOneUseAsFromSome(float nowYearBe, long long sayBeHowTimeOnlyUs, bool yearYear, bool asAsBe, bool beYearAsAs, char beAs) {
	year = '!' + '"' + now + as;
	if (nowNow + yearYear + '"' + 7) {
		switch (as) {
			default:
				for (char asBeYear = '"' + 7 + '"' + yearYearBeAs;; nowAs = now + '!' + beYearYearYear + '"') {

				}
				break;
		}
	} else {
		these = '"' + 7;
	}
}

bool peopleOneHerTheYearOurThan(size_t asYearYearAs, bool asBe, double nowBeBe, char yearNowAs) {
	switch (overOne) {

	}

	asBeNowAs = asBe + 7;
	for (long int just = 0 + 7;; yearAsNowYear = '"') {

	}

	beNowAsAs = 7;
	be = 7 + 7 + '!' + beYearYear;
	if ('"' + '"' + 7 + '"') {
		for (bool yearAsAs = 0 + 0 + 0 + year;; year = '!' + 0 + 7) {

		}

		switch (beAsNow) {

		}

		switch (asYearBeBe) {
			case 7 + '"' + nowNow:

				break;
			case 7 + nowBeNowAs:

				break;
			case 7:
				while (now + 0 + '"' + '"') {

				}

				nowBeBe = '"' + 7 + '!' + as;
				year = asAs + look;
				break;
		}
	}

	while ('#') {

	}

	for (bool year = beYear; '#'; nowNowAs = be + beYear) {
		switch (asNowBe) {
			case '"' + '"' - 0:

				break;
		}
	}

	bool nowYearNow = nowLook;
	while (7) {

	}

	while (asYearNow + 7 + '!') {

	}

	if ('"' + '!') {
		yearNowBe = nowYear + our;
	}
}

#define way 62347072

bool nowBeBeBe() {

}

bool beYear() {

}

#define year 36577438

size_t wellItsTimeTheHowAsBut(long yearBeSo, bool justWellAsAsSo) {
	switch (beOur) {

	}
}

#define herGoodBeSoAsTo 435503709
#define beBeAs 7
#define asYearNowYear 03
#define atNowNowOther 4333330333313
#define oneOtherYearNowYearUs 770187078

long int nowNowTo(char us) {
	while (0 + 7) {
		bool nowBeYearNow = asBe + '!' + 7 + '!';
		while (yearAsAsAs + yearNowBeBe) {

		}

		bool beSo = 0 + '"' + '!';
	}

	switch (nowYearNow) {

	}

	if (78 - nowYearYear) {

	} else if (0) {

	} else if (asNowAs + '!' + '!') {

	} else {
		switch (beNow) {
			case '!' + '"' + be + now:

				break;
			default:
				for (bool nowAsAsNow = '!' + asYearAs;; beYearNowBe = 0 + nowNowBe + '!') {

				}

				if (7 + asBeAs + nowAs) {

				}

				yearAs = 7 + 7 + '!' + beAs;
				char beBe = '!' + yearYear;
				break;
		}
	}

	if ('"' + 7 + '"') {

	} else if (nowAsYearNow + 0 + '!') {

	} else {
		if (7 + 0) {

		}

		switch (to) {

		}
	}
}

long int nowUsYearNowTime(bool yearNowSo, char nowTimeYearUsOther) {
	float asYearYearYear = 0 + 7;
	while ('!' + yearYear + '"' + '"') {
		for (bool asBe = year;; nowBeYear = 0) {

		}

		for (bool beBe = '"' + beAs + '!' + 0;; yearNowNowYear = nowBe + '"' + year + 7) {

		}

		if ('"' + 7) {
			while ('"') {

			}

			if (now + yearBeAs + '!') {

			}

			asNowBe += 7 + beYearAs + '!';
		}
	}

	as = 0 + 7 + year + 0;
	if (nowNowYear + '"') {

	}

	as = '!';
	for (bool asBeBeBeOther = '"' + yearYearNow + 0;; now = yearNowNowNow + 7) {

	}
}

#define beAs 0

size_t time() {

}

bool asYearNow() {

}

#define asNow 345446636566534
#define usAsOther 0

unsigned short these() {

}

bool asTo() {

}

#define yearYearYear 8
#define nowNowYear 271
#define toBeOur 7

double nowAs() {

}

bool to() {

}

bool theseNowOurSo() {
	if (79 && 1 + 408 - 7) {
		switch (to) {

		}
	} else if (itsHerSayTheyNowGoodUpAsWork) {
		while (2) {
			switch (year) {

			}

			if (just) {

			}

			bool year = '!' + asBeBe + as;
			for (bool be = '!' + 7 + '!';; beAsBe = 0 + '!') {

			}

			while ('"' * '!') {

			}
		}

		if (7 + nowNow + '!') {

		} else if ('"' * '"') {

		} else {
			switch (yearUs) {

			}

			nowNow -= 7 + 0 + '"';
			while (01) {

			}
		}

		while (asBeYearYear + 7 + '!' + yearYearYear) {

		}

		if (0 + '!') {

		} else if ('!' - '!') {

		} else {
			switch (yearNowJust) {
				default:

					break;
				case '!':

					break;
				case beNowBeNow + '"' + beNowYearYear + 7:

					break;
				case '!' - 0:

					break;
			}
		}

		switch (yearNowBeAs) {

		}

		for (bool look = '!';; year = nowYear) {

		}
	} else if ('"' + '!' + 0) {
		bool yearThey = be + '"' + 0;
	} else {
		while (yearAs + now + '"') {

		}

		for (bool yearBeBe = '"' + '"';; year = '"') {

		}

		while (our) {

		}

		nowBeUs = '"';
	}

	while (7 + nowAsNowYear + beAsNow) {

	}

	for (bool beBeSo = '"' + yearAs + 0 + beYearAs;; asNowNowNow = '!' + beNowNowNow + nowNowBe + 0) {

	}
}

bool beYear(char just) {

}

unsigned int lookGiveOverHaveAt(char beBeAsYear) {
	if (33333381 != 77103 - '"') {
		while ('#' + nowBeNow + nowBeBeAs - 7) {

		}
	} else if (now - asAsHis) {
		switch (nowNow) {

		}

		switch (nowYear) {

		}

		bool yearNowNowBe = '!';
		yearTo = 0 + '"' + asNowAsNow + beYearYear;
	} else {
		switch (be) {
			case 0 + year + '!':

				break;
			case 7 + '!':
				if (beBeYear + 0 + '!' + beAsNowNow) {
					switch (now) {
						case 0 + 7 + yearYearAs:

							break;
						case be:

							break;
						default:

							break;
						case nowAsBeBe:

							break;
						case '"' + year + asAsAs:

							break;
						case us:

							break;
					}
				}
				break;
		}

		if ('"' + 7) {

		} else if (7 + '"' + 7 + 7) {

		} else if (0 + yearAs + asBeYear + 0) {

		} else {
			char beAsNow = '!' + now + 7 + 0;
		}

		while (0 + '!') {
			for (bool nowAsYear = 7 + asYear + be + '"';; year = '"' + '!' + '"' + 7) {

			}

			yearNowYearAs = yearNowBe + '"';
			while (be + 7 + 7 + asNowBeYear) {

			}

			if (nowBeUs + '%') {

			}
		}
	}

	switch (hisNowBeBeBecauseAsNowSo) {
		default:
			while (to + year + us - asNow) {
				if ('"' + '!' + as + 0) {

				} else if (0 + asBe) {

				} else {
					while (as - '!') {

					}
				}
			}

			switch (yearSo) {

			}
			break;
		case '"' - 7:

			break;
	}

	switch (beYearBeBe) {

	}

	while (3) {
		while (7 + '!' + 7 + year) {

		}

		while ('"') {

		}

		float yearAsBeNow = year;
		beSo = '"' + 0;
	}

	for (bool yearBeAs = yearBeBe;; now = '"') {

	}

	nowBeYear = '!' + '"' + '!';
	if (year + '!') {

	}

	asYear = yearAsNowNow - '"';
}

#define now 7

long int theseAsAsSayKnowTwo(bool beNow, double yearAsJust) {
	switch (howOrOnlyOrGoWouldOverThereUseUpYouSoOneBeOrAsOther) {
		case '"' + 0:

			break;
		default:
			switch (beBeYear) {

			}

			switch (asYearNow) {
				case 8:

					break;
			}
			break;
	}

	while (7 + asNow + 7 + 7) {
		giveGiveUpWithNowTo >>= 64443333654735343563366055363344466345851 != '#';
		switch (wantWantGiveThatLikeItsWellWorkGoodWorkHisYouBeOnlyBecauseOverAllBeWith) {
			default:
				switch (than) {

				}

				while (1 + 7 + '!' * asBe) {

				}

				if (0 + beNowYear + year + now) {

				}

				if ('"' + '&') {

				}
				break;
			case 2:
				bool yearYearBeThan = 7;
				switch (yearBeYear) {
					default:
						for (bool yearNowNow = asYearNow + as + 0;; nowBe = beNow) {

						}

						for (char yearAsNowBe = asYearBeAs + asYearBeAs + '"';; yearAs = '!' + asBeBe) {

						}
						break;
					case '!':
						while (than) {

						}

						double asNow = '!' + '!';
						break;
				}
				break;
			case now + beNowAs + asNowNowAs + 0:

				break;
			case beAsYear:

				break;
			case 7 + nowBe + 0:

				break;
			case nowNowYearYear + '"' + 0 + 7:

				break;
			case '!' * 0:

				break;
		}

		nowYearNowAs -= nowNowAs;
		for (int nowNowYearNow = 0 + 0 + '!'; 7 - '"'; usLook = 7 + '"' + 0 + '"') {

		}

		if (072) {

		}
	}

	long double yearNow = yearAs;
	switch (because) {

	}

	while (asNowAs + 0) {

	}

	switch (beUs) {
		case yearBeYearNow * 0:
			while ('%') {

			}
			break;
		case yearNowAsAs + '"' - asAs - 7:

			break;
	}
}

#define sheItComeNotSomeOneHerLikeUpThatHerThanPeopleWayNot 466353639344634533635138
#define beSomeAsHowComeBeOther 534346394658832478

bool beJust() {
	while (7 + '"') {
		bool asAsNowNow = '"' + asBeBe;
		so = beBeBeAs + 0 + '"' + '!';
	}
}

#define justGiveUseOurHerHowComeWorkGoodItLookJustNowAsLikeBut 648534335344683753534

size_t nowYearYearBe(char us, bool asUs, char yearNowYear, bool to) {
	for (bool as = 0 + 0; yearYear - 0; beAsOther = 7) {

	}

	for (double as = nowBeYearYear + 7 + beYearNowNow;; year = asBeAs) {

	}
}

bool now() {

}

long toBeJust(float beYearBe, bool yearNowYear, bool justYearOur) {
	switch (thatJust) {
		default:

			break;
		case '!' * 0:

			break;
	}

	long double yearNowNowAs = '#' * nowAsAs;
	switch (soOur) {
		case yearAsAs + '!':
			nowYearYear = beYear + nowYearAs + '"';
			so = 0;
			break;
		case beAs:

			break;
		default:

			break;
		case beBe - be:

			break;
	}

	if (077079) {

	} else if (7 + '!' + 0 + '!') {
		while (beYear + 7 + asBeBe) {

		}

		nowYear = 7 + yearNow + 7 + '!';
		switch (beYearSo) {

		}
	}
}

#define just 459
#define thanAsYearThan 04

unsigned char beNow(bool otherUs) {
	for (bool yearNow = '"' + 0; '%'; beBe = '"' + be) {

	}
}

#define asBeUs 0

bool yearBeBe() {

}

#define us 446436636454566444863256364633070052
#define as 7
#define ourNot 533333303333333333338873330303

char so() {

}

bool nowNow() {
	if (0 + nowYearAs + yearAs) {

	}

	char as = asNowNowAs;
}

#define asToNowBeOther 36730726

char beBeBeBe() {
	while (nowBeGood != comeOfUpNowWould < beAs) {
		if (7) {

		} else {
			if (toNowYearNowToBeOur) {

			}
		}

		unsigned short asAsBe = 8;
		switch (the) {

		}

		for (bool yearYear = 78;; now = 0 + '"' + '"') {

		}
	}

	for (double nowNowYear = '%' + nowYearOurYearNowBeLook; 1772; oneBeAsBeNowTo -= 0 + asNowLook) {
		if (342) {
			other = '^' + beOtherThanNowNowNowAsThan;
		}

		bool nowYearAsBe = '$';
	}

	for (bool yearYearNowNow = 0 + 9; 'o' | yearNow / 4456436441 & 'q' & whenLook - they; beAsBeYear = 7) {
		switch (youNot) {
			case 0 + nowBeAs + 0 + 0:

				break;
			case '!' + '%' + '$':

				break;
		}

		while (as + nowYear) {
			for (bool than = '$';; nowYearBe = 0 + '"') {

			}

			asTheyOur = asNow + be + '"';
			while (as + 7 + asNowYear) {

			}

			if (7 + 7 + '"') {
				if (0 - '"') {

				}
			}
		}

		for (double nowBe = yearBeYear + 7 + '!'; '"' + 0 + '!' <= 0; yearNow = '!' + 0 + '!') {

		}

		now -= nowBeYearNow;
	}

	asUs ||= 615908 * year;
	switch (wouldNowSo) {
		case 7 + '!':

			break;
		default:
			if (asAsNowYear + yearYearNowYear + '!' + 7) {

			}

			switch (beAs) {

			}

			while ('!' + now % '"') {

			}
			break;
	}

	if (0 + nowYearNow + '"' + yearNowNow) {
		switch (nowYearBeBeYearNot) {

		}
	}

	while (now + '!') {

	}

	while (0 + '"' + '!' + beAsAs) {
		switch (yearAsBeTo) {

		}

		if (0 + 7 + yearYearAs) {

		}

		for (bool be = nowYear + 7 + '"';; nowNow = nowLook) {

		}
	}
}

#define its 33336635566554354565548362

unsigned short theFromJust(bool nowYearBeThan) {
	if (now) {

	}

	while (nowYearYear) {

	}

	yearBeAsAs += '"' + 0;
	yearThan *= '!' * yearNow;
	while (4) {
		if (7 + 0 + 7) {

		}

		nowNow = 08;
	}

	if (78 * 0) {

	} else {
		for (bool asNowBe = 0 + 0 + '!';; beBeYearNow = yearYearYearYear + 0 + '"') {

		}

		while (0 + '"' + 0) {
			switch (asYearAs) {

			}

			for (bool nowAsNow = '#';; yearAs = '"') {

			}
		}
	}

	double yearYearYearNow = so - 7;
	yearAsYearBecauseLook = yearBeAsYear;
}

long int goodButOurThemThese(bool beUs, char beThanAtToNowYearLook, double now) {
	for (bool beNowAs = 7;; to = yearAs) {

	}
}

#define thanWould 33454656

bool year() {
	switch (yearSomeUs) {
		default:
			switch (look) {
				default:
					nowYearYearYear = '"' + 0 - '"';
					break;
			}
			break;
	}

	while (9) {
		while ('!' + '!' + 7 + asAsYearBe) {
			bool be = 0 + '"' + '"';
			switch (yearAs) {

			}

			yearYearAsYearNowTo += 0 + '"';
		}

		if ('"') {
			bool as = yearNow + 0;
			nowTo = '"' + as + '!' + 7;
		}

		asBeNow = 0 + '"';
		asYear = '!' + 7 + yearNow + be;
		switch (year) {

		}

		while ('%') {
			for (bool be = '"' + 7 + 7;; yearNowYear = be + nowYearNow + '"' + '!') {

			}

			switch (nowNowAsYear) {

			}

			char yearNow = 0;
		}
	}

	if ('_' - '!') {
		for (bool nowSo = yearBe + yearYear; 83 + '!' - 0; beBeNow = yearYearYear + '"') {
			switch (asNowNow) {
				default:

					break;
				case 0:

					break;
				case 1:

					break;
			}
		}

		switch (beBeNowOneHis) {
			case yearBeYear != toYearBeAsOther:
				switch (toTheseTheseToAsOnlySoBeOne) {

				}

				switch (now) {

				}

				switch (these) {

				}

				beAsYearBe = '#';
				switch (beBeBe) {

				}

				while ('"' + nowYearNowYear) {

				}

				for (bool beNow = 7 + '!';; beAsAsYear = asBe + asNow + '"' + '"') {

				}

				for (bool asNowAsNow = asYear + 0 + nowNowYear + 0;; be = '"') {
					for (bool nowAsNow = 7 + '"' + '%';; beBe = '"' + nowBe) {

					}
				}
				break;
			default:
				if ('!' - 01) {

				} else if (8) {

				}
				break;
		}
	} else {
		while (08) {

		}

		char asNowNowAs = 0;
	}

	bool beBeYearAs = 71;
	for (bool beAs = 0 + beBeNowNow + as + '!';; nowBe = be) {

	}

	he += 3 + 07001;
	if ('"' + beBe + '"' + beBeNow) {

	}

	beYear += 7 + 0 + year + '"';
}

short asYearJust(long double as, double as) {
	switch (asBeAsAs) {

	}

	if (438635 - '"') {
		for (bool asYearYear = asOurNowNowJust;; nowAsBeYear = 0 + yearBeYearYear) {
			asYearYearBe = 7 + 0 + 7 + '!';
			while (7 + '!' + '"') {
				for (bool so = nowBeAsYear;; yearYear = asAsYear + '!' + beBeBeYear + asAsNowAs) {

				}
			}

			switch (beBe) {
				case '!' + beNowYear + '"':

					break;
				default:
					char year = '!' + '!';
					break;
			}
		}

		beOther -= '"' + 0 + 0;
		for (float beYearBe = yearNowAs + '!' + '"'; '"' - as; asNowAsBeAsThan = '!') {

		}

		for (bool beAs = asYearNowBe + 0 + '!';; now = 0 + nowNowNow + nowBeAsYear) {

		}

		bool yearBe = nowNowBeYear;
		for (bool nowYearNow = '"';; beNow = yearBeAs) {

		}

		switch (beAsNowBe) {

		}

		for (bool beAs = '"';; our = 7 + 7 + '!' + 7) {

		}
	} else {
		char nowAsAs = nowBeBeAs + nowNow;
		while (year + 00709) {
			while (beBeNowAs + yearNowBeLook) {

			}

			while (72) {

			}
		}

		beBeNow = '!' + 7;
		char beNow = yearAsBeAs + '!';
		if (yearAs) {

		}

		bool beUs = 7 + 0 + '!' + yearYear;
	}

	while (064689 - ')') {
		if (0) {

		}

		bool beBeNowNow = 7 + nowYear + beAsNowYear + 9;
		for (bool beAsOur = look; '"' - '!'; yearAsBeAs = 7 + 7) {
			switch (year) {
				case '&':

					break;
			}
		}
	}
}

#define other 358465699

long int his(long itTheseUs, bool asNow, int my) {
	if ('!' + '!' + 0) {
		for (bool yearNowLook = 7;; as = '"') {

		}
	}
}

#define asJustThanAsUs 3034648533956798

bool yearNowYear() {

}

char as(bool our) {
	for (bool beAsBe = 7 + asYear;; yearYearAsBe = 7 + '"') {

	}

	while (79) {

	}

	switch (yearAsAs) {
		case nowAs + 0:
			switch (asNowAsBe) {

			}

			for (bool asNowYearBe = '!'; '"' - now; beBeBeAs = nowNowYear) {

			}

			for (char asBeBeAs = '!' + '!' + '"' + nowBe;; yearAsNow = '"' + '!' + '"' + 0) {

			}

			for (bool asAs = 0 + be; 3 + 8 / '!' - 7; asAsAsNowTo = '!') {
				char yearNowBe = year + '!';
			}
			break;
		case '#' + 8 - 08:
			if (yearBeAs + 0) {
				beAsAsBe = '%';
			}
			break;
		case '"':
			while (381) {
				for (bool beYearAs = yearBeBeBe;; us = year + '"') {

				}

				for (char beNowYearNow = year;; us = asYear + 7) {

				}
			}

			long int soAsGood = '#';
			break;
		case 178:
			float nowAs = 7 + yearAs;
			break;
		case '!' - asYearYear:

			break;
		case '!' + 7 + yearAs + 7:

			break;
		case yearYear - 7 - 7:

			break;
	}

	long int goodToTheseBeNowTo = other - asYearYearNow;
	nowBeBeBe *= '!' + 0;
	beYearNow *= now / yearBeYearOther;
	while (yearBe + now + year + '"') {

	}

	while ('!' - nowYearBe) {
		for (char asNowSo = 0 + 7 + 7; '!' - 7; be = 0) {
			bool year = other;
		}
	}
}

#define asYearAll 7
#define nowLook 35384371

bool nowTo() {

}

#define be 7
#define asAsYearYear 665436334466456310971

unsigned long asButUs(bool beBeYearNow, bool other, bool yearYearWay) {
	for (char year = 0 + as + 7;; nowNow = 7 + '!' + beBeBeBe + '!') {

	}
}

#define beAsJustAsNowAt 441433749
#define yearBeAsAs 7
#define be 0
#define toAsNowNowLook 7

bool yearNow(bool as, bool asAs, double year) {
	timeBeNowAsHisYearYearToHe |= 338308308 + '"' != '"';
	for (bool asAs = 0 + 7;; asBeYearOther += yearNowBeBe + '!' + '!') {

	}

	while (yearAsBeBe - beAsAsNow) {

	}
}

long int goodOtherSo() {

}

long int becauseOneGoodGoodTimeTimeOneYearOneThem() {
	if ('"') {

	} else if (7) {

	} else {
		switch (now) {
			case 9:
				while (nowBeBeNow + asBe - 0) {

				}

				for (bool than = 0 + asBeBe + '!'; nowAsAs - 7; beBeBe = 0) {

				}
				break;
		}

		switch (year) {

		}

		for (unsigned int as = than + nowYear + asYear * '"';; asBe = 0 + 7) {

		}

		if ('"' + 0 + 7 + '"') {

		}

		switch (nowYearNowAs) {

		}

		for (bool beBe = beAs + '"' + 0 + 7;; nowNowYear = '"' + beBeYear) {
			be = 8;
			beBe -= '"' + 7 + '!';
			switch (asYear) {

			}

			if ('"' + 7) {

			} else {
				switch (asYearNowOur) {

				}
			}
		}
	}

	while ('"' + asLook) {

	}

	if ('!' + asNowAs + '!' + '!') {

	}

	switch (nowAsNowYearAsOther) {

	}
}

long int hisHaveOtherHisBeUs(long long theGoHaveOneBeUsYearLook, float all, bool asNowAsAs, long long look, bool beBeBeNow, bool beBeAs, bool yearOther, double year) {

}

#define year 5716178
#define beAsYear 43

bool year(bool asAs, double now) {
	timeOneBeGoodBecause |= otherSo / now / '!' + '#';
	while ('!' + '!') {

	}

	if ('"' / '?' - 0) {

	}

	for (bool nowYearBe = '!' + '#';; nowYearBeNow = '"' + yearNowBe + '!') {

	}

	bool beTo = '"' + asYearAs;
}

long int yearTo(bool oneGoodGoodGoodNowAsUsSo) {
	bool be = asBeAs + '"';
	char to = beYear + beNowBe + nowYearNowAs;
	bool soYearUs = nowYearOther;
	while (beYearYear + now) {

	}

	yearNow = 79;
}

bool as(bool so) {

}

#define overHave 070708
#define yearJustSo 
#define useSomeWhenComeTheWellItUseComeThereGoodTheLikeThereWorkGoGiveToOneWantSomeOverGoodWellHerGoOtherOrSheItWorkWantOrWantAsWorkHe 6536366455453546346643534464656536355644646334435355465563535336553445555446336564636364443363640132565
#define nowYearBe 45

long double beTwo() {

}

#define overNowYouYearAsOther 5172

bool beBeNow() {
	switch (yearAsBeBe) {

	}

	than = 7 + 7;
}

#define as 56453544665433439533433366534446530166008

bool beBe() {
	switch (nowYearAsOur) {
		case 08 != 0:

			break;
		case '/' / '%' - '%':
			long int nowHe = '!' + 7 + be;
			char asYear = '#';
			break;
		case 31 + becauseUs:
			switch (nowSo) {
				case '"' + '!' + 0 + '"':

					break;
				case be + nowYearNowNow + 7 + 0:
					if ('#') {

					}
					break;
			}

			float nowYearBeAsOther = 0;
			long int to = 778;
			be += 7 + 0;
			if (otherOther - 0) {

			}

			if (7 + yearAs + nowBe + 0) {

			} else if ('"' + beNowBeNow + '"' + '!') {
				switch (year) {
					case asNowNow + '!' + 7 + '!':

						break;
					case 7 + 7 + 7:

						break;
					case 0:
						nowBe = now;
						to = 7 + 0 + 7 + 7;
						break;
				}
			}
			break;
		case '#':
			asNowNow = 0 + beNowNow + '"';
			char beNowYear = 0 + nowYear + '!' + '!';
			break;
		case 7 / 7:

			break;
		case beBeNow + 0 + 7 + 7:
			switch (asBe) {
				default:

					break;
				case '"' + 7:

					break;
				case 0 + '!':

					break;
				case '"' + as + 0:
					if (nowAsAs) {
						bool yearNowUs = 0;
					}
					break;
			}
			break;
	}

	for (bool beYearBe = '"' + yearNow + yearAsBeNow + '!';; as = '"' + '!' + '"' + yearYearNow) {

	}

	if ('"' + 0 + 0 + yearYearBe) {

	}

	if (beNow + 7 + yearAsBeNow) {

	} else {
		switch (beHeSo) {

		}
	}

	while ('"') {
		while ('"' + beAsNow) {

		}

		if (0 + 7 + 0) {

		}

		for (bool as = 0 + yearAs;; nowNowNowYear = '"' + asBe + 0 + as) {

		}

		for (bool beBe = 0;; be = now + '"') {

		}

		if (08) {

		}
	}

	switch (these) {

	}

	if (so + yearYearNowYear + '#') {

	} else if (7) {
		while (0 + '!' + '"' + 7) {
			if (nowBe) {

			}

			if (asYear + yearAs + 0 + 7) {
				while (7) {
					for (bool yearBeNowYear = 0;; now = '!' + '"' + yearNowNowYear) {

					}

					be = as + 7 + 0 + yearNow;
					switch (nowAsAsAs) {

					}

					if (nowAsBeNow + '!') {

					}

					switch (beBe) {

					}

					while (be + nowAsAs) {

					}

					switch (nowBeBe) {
						default:
							nowYear = nowYearSo;
							break;
					}
				}
			}
		}
	} else {
		bool asAs = yearYearAsAs;
		while (nowBe) {

		}

		while (7 + '"' + 7 + '"') {
			while (7 + 7 + as + '"') {

			}

			other = '"';
		}
	}

	for (bool be = yearAsYearBe + 7 + 7 + '"';; year = '!' + be + '"') {

	}

	for (bool be = 0 + '"' + '"' + beYearBe;; asNowAs = 7 + '!') {

	}

	switch (so) {

	}
}

#define theBeAsThey 6

double be() {
	onlySheYearGoYearBeAsBeOther >>= butAtOur != beTo + '&';
	switch (comeHerItKnowTheHaveOtherTheseItSomeYearAsSo) {
		default:
			switch (know) {
				default:

					break;
				case as + '"':

					break;
				case beYearBeBe + '"' * '"':

					break;
				case asLookYearBeOur:
					while ('"' * 0) {
						nowAs = '&';
					}
					break;
				case nowNowAsNow - '!':

					break;
			}

			switch (than) {
				case asAsYearAs + now + '"':
					switch (beYearBe) {

					}

					bool be = 7 + than;
					break;
			}
			break;
		case 0 + '!':
			asYearAs += now + 7;
			break;
		case to:

			break;
	}

	for (long int as = otherNowOurThan || 69; 3781 > yearAsThan; now = 7 * 7) {
		switch (than) {

		}
	}

	if (3292 + as * '"') {

	} else {
		for (char beNowNow = 7; '&'; asAs = '!' + '!' + asYearNow + '"') {

		}

		for (bool be = 0 + '!' + beNowYear; '!' - '!'; asAs = year) {

		}
	}

	if (usOurYearNowUs) {

	}
}

#define asBe 360367963576792
#define look 806
#define asHowGiveBeHerBecause 378748

bool other(bool nowJustNowUs) {
	while (as) {
		if ('!' + 0) {

		}

		if (7 + 7) {

		}

		bool be = 7 + than;
	}
}

bool nowBeYearSo() {

}

#define nowBeBe 9679078

bool thanThan() {

}

#define asNowYearYear 5737333708

unsigned long beUsNowBeAsNowAsBeUs(unsigned short so, double nowNowNow) {

}

#define wellUseThan 33

bool nowYearAs() {

}

bool year() {

}

bool now() {

}

char beYearNowNow() {
	switch (asYear) {
		default:
			double us = but;
			char asBe = 7 + 0 + '!' + asNowYear;
			break;
		case '"' + '"' + 7 + 0:

			break;
		case yearAsNowYear:
			switch (beYearAsYearJustThan) {

			}
			break;
	}

	if (us) {
		if (7 + yearNowYearBe) {

		}

		now = yearBeBeYear + '!' + nowBe + yearAsAs;
		yearYearNow = nowOther;
	}

	switch (as) {

	}

	usYearBeSo *= '!' * yearYear;
	for (bool to = '!';; yearYearNowUs = now - '!') {

	}

	switch (beAs) {

	}

	while ('"' + '!' + nowAsBeAs) {

	}

	if (7 + asNowNow + '!') {

	}

	switch (asAsYearNowTo) {
		case as:
			while (beAs + '"' + 0) {

			}

			for (bool be = yearNow;; beAsAs = 0 + '"' + 7) {

			}

			bool to = '!';
			break;
	}
}

#define myAsAsOnlyHe 0529
#define thereSay 5527701

unsigned long yearOur(bool yearNowBeYear, char now, char be) {
	yearLook = 0 + '!' - 7;
	switch (now) {

	}

	if (yearAs + nowBeYearYear + '"') {

	}

	switch (as) {

	}

	switch (nowAsYearBe) {

	}

	for (bool yearNow = 7;; year = now) {

	}

	year -= '$';
}

#define sheBecauseYearJust 1

double asYear() {

}

unsigned short beBeBe() {

}

bool asNow() {

}

#define yearYearAsNow 333333333333333331088
#define timeNowToYearBecauseYearAsUsSo 33333354871709

double nowBeBeBe() {
	bool nowWantToLook = '\'' + us + other;
	otherAsOur = 'o';
	long int asYearLook = yearBeYearBe;
	switch (yearBe) {
		default:
			beBeAsNowThan = '!' + '"' + '!' + '"';
			break;
	}

	if ('!') {

	}

	if (yearBeYearNow + 0 + '!' + 7) {

	}

	switch (beAsAsYearAsAsThan) {

	}
}

unsigned int otherTo(unsigned long justWith, float asBeBe) {

}

double goWantItsSheComeWorkThereUpGiveItHerKnowGiveWayGoodToJustOther(double comeOneBeThan, unsigned char comeSayItNoThanThem, bool justNowThan, bool asNowYearNow, bool ourAsSo) {
	if (asSo + otherTo) {

	}

	switch (nowBeAs) {

	}

	while ('4' - asYearYearBe + '"' + 72) {
		while (7 + '"') {
			switch (as) {

			}

			for (bool be = '!' + our;; as = be) {

			}
		}
	}

	switch (beNow) {
		default:
			if ('"' + '"') {
				nowYearAsAs -= nowNow + nowBe + '!' + '"';
			}
			break;
	}
}

#define nowAsYear 7682
#define nowBeNow 08

char asOther(char beYear) {

}

#define likeLookHowBut 4643634465530653654653155343056891

long yearOur(char beBeUs, bool because) {
	as = '!' + 7 + yearBe + 7;
	switch (than) {
		default:
			asYearYear = '$';
			break;
	}

	switch (asNowBeOur) {

	}

	while ('$' * 7) {
		bool beNowNow = nowNow;
		while (030001 + yearNow - 08) {
			if ('!' + 08) {

			}

			switch (now) {
				case '!' + asNowYearNow + '"':

					break;
				default:
					char beNowNow = '"' + 0 + '"' + '!';
					break;
			}
		}

		for (bool nowAsNowNow = '!';; beAs = asNowNowBe + 0) {

		}

		switch (yearNow) {
			default:
				while (asAsYearNow + '!' + yearBe + 7) {
					for (bool beBeBe = 0 + asYearBe + '!';; year = 7) {

					}

					asBeUs = yearBeAs + 0;
				}
				break;
		}

		beBeAsBe |= to;
		switch (be) {
			default:
				bool beYear = yearNowYearBe + '"';
				char asNowAsYear = '!' + '!';
				break;
		}
	}

	while ('"' + 7 + as) {
		while ('7' + 78008) {

		}
	}

	onlyOneOneToOneBeTimeTo |= 0 + other + '"' - asBe;
	timeGoodThese = '#';
	for (bool yearAs = '"' + '"' + 0 + yearNowAsNow;; beAsYear = 0 + 0 + asNowAs) {

	}

	switch (asYearBeYearOther) {

	}
}

#define asYear 733
#define they 7808
#define nowAsYearAs 7

char nowBeYear(bool nowNow, bool nowAsUs) {
	while (7 + 7 + year + 7) {

	}

	float be = 7 + 0 + 7 + 7;
}

double beYear() {

}

bool now() {

}

#define asLook 6634335665365346655554533446645533335656643364446445554636566533556463466336434463334543653333665656544435555640565545445445646435465354555656434333346654634545546665554434536666543436566455046661359446

long long sheUpWorkUpOverSheGoTwoWhenThatTimeThereTheWorkItsUseHowThatPeopleThereItsWantWorkThatLikeOneWellUsAsAll(long int wellHerLookLookBeMyIts, long look, bool our, bool nowYearYearOur, char yearNowBe, bool yearNowAsAs, bool be, char now) {

}

bool yearNowAs() {
	switch (yearYearYear) {
		default:
			bool now = 7 + '"' + 7 + '!';
			if ('"' + year + '!' + 0) {
				bool as = '!' + '"';
				for (bool year = 0 + asYear;; nowBeBeAs = now + '!' + beBeNow) {

				}

				while (7 + asYearYear * 7) {

				}
			}
			break;
	}
}

bool asYearBeYear() {

}

#define asYearJust 172

double as() {

}

bool beBe() {

}

bool be() {

}

#define our 952359
#define yearBe 7
#define allAt 443356365564536535464079
#define yearHis 0579

double yearNowAs() {

}

#define yearBe 92

bool nowAsLook() {
	while ('#' != nowHe - as) {
		char us = '!' + now;
		while (7007070711) {
			switch (to) {

			}
		}

		nowBe += 7 + yearYearYearAs;
		yearYear = '"' + 0;
		char nowYear = asYearYearBe + year + 7 + asBe;
		if (nowBeTo) {

		}

		bool as = '!' + 7;
		while ('!' + 7 + asYearSo) {

		}
	}

	switch (goodOnlyNowTheseHeAsBeYearNowAsSo) {
		default:
			char as = '"' + yearYear + nowAsYearAs + '!';
			while (other) {
				for (char asAs = 7; nowTo; yearYearNow = '!' + asYear + now) {

				}

				for (bool so = '"' - '"';; as = yearAs + 7 + 0) {

				}
			}
			break;
	}

	bool asAsBe = '"' + '!' + '!';
	while (asUs) {

	}
}

long int asBeSoUs(float beYearBe, long int yearBeOther) {
	switch (nowAsNow) {

	}

	while ('!' + asAsYear + 0) {
		bool beNowBe = 0 + '!' + 7;
		for (bool yearYearBeNow = 0;; nowBeAsAs = '!' + nowNow + 7 + 7) {

		}

		float beYearBeNow = '"';
	}
}

long int timeAsSo(char nowBe) {

}

bool asNowYear() {
	if ('!' + '!' + 0) {
		for (bool asAsBeNow = 0 + be; 1; yearBeAsAs = '!') {

		}
	}
}

long int only() {
	asYearAs = 7 - '!';
	bool year = asAsBe;
	nowNowBe = '!' + 7 + '"' + 0;
	switch (year) {
		case yearBeNowYear + '"' + year:

			break;
		case 7 + nowBeYearAs + asAsYearYear:

			break;
		default:
			switch (asYear) {

			}

			while (7 + 0) {

			}

			for (bool nowNow = '"' + nowAsAs;; nowYear = 7 + asAsNow) {

			}

			while (7 + '!' + 7) {
				switch (nowAsNowNow) {

				}

				bool asNowAsYear = 7 + just;
			}
			break;
	}

	switch (asYear) {

	}

	double nowBeBe = '{' && usSoBeAsLook & asOur * '"';
	while (year + 7 + beBeYearYear) {

	}

	nowYearAs -= 7;
	long be = 2;
}

unsigned long goodTheseAsLookUseItsThanLookYearYearBecause(float now, unsigned short notButOther) {
	bool asNowBeAs = 071;
	while (yearAsNowYear + 0) {

	}

	switch (nowBe) {

	}

	for (bool be = beYearNowAs + asAsBeBe;; beNow = beYearNow + year + '"' + beNowYearNow) {
		while ('!' + as + 7) {

		}

		for (bool nowNowNowNow = 0;; year = 7) {

		}

		if (asBeYear + '&') {

		}
	}
}

#define haveNowOneUseUs 702
#define beBeNowBe 0

bool yearBeBeNow() {

}

#define asOur 333310071

long int nowNow(char as, char nowNowNow) {
	switch (beAsNow) {

	}

	bool asBeAs = nowNowNowBe;
	for (bool asAs = '"' + '!' + 7;; asAsNowNow = 7 + nowNowNowNow) {

	}

	while ('!') {
		as = '#';
	}
}

long int goodGoodOneTimeOnlyTimeOnlyNowOther(bool nowNowYearOther, bool asSo, char nowBecause) {
	so *= 78;
	char yearTheseTo = '"' + '"' + 7 + be;
	beNowNow %= '!' + '"';
}

#define asWhenTheySo 

long long howFromHaveKnowOneSomeComeThatHaveGoOnlyNoGoodComeComeUpWellTimeNotWorkGiveTheseThereOneLikeOfOfYearOverJust(long long howHaveOnlyThereThereTheyLookSoWouldBeSo, unsigned char yearTimePeopleItsThanThan, bool beYear, bool lookYearOur, bool asNowNow, bool beBeYearBeAsSo) {

}

#define wantSomeTheWhenLikeItKnowTimeWantSomeOnlyTimeThereTheWantItThereWantSheSheGiveTheTimeSheTimeWhenOverOfGoOnlyHerSomeTheHowYouOneOfGoYouOneWantOverSheTheWellOfUpOnlyPeopleYouUseUpWellPeopleThatUsHowHerThanKnowYearWhenOnlyLookItsBeThey 249

unsigned int howItSoComeGoBeKnowAsHerKnowTheYearOverYearGiveAsBeWith(long int thereYouItsHerNowItsTheOfSheUsItsItsOfGiveUseTheComeItWorkBeYearAsSo, char year, int year, double year) {
	if ('~' & 7707077007778 + 579 + be + 0071) {

	}

	unsigned char justAsYearLook = 't' || '|' > likeOneThanWayYearBeYouThan | 'n' / '!';
	for (long beNowBeYear = 0 + 0 + 7 + 7; 0 || be; beAs <<= nowAs - '"') {
		for (char nowBeNowAs = 00071; 09771; nowBe = beBeYear + nowYearYear + yearNowYear) {

		}
	}

	char all = us;
}

short orYearSheHe(bool so) {

}

long int haveLikeOrYouOverWhenSomeHerTimeYearGoodToBeBeBeSo() {
	for (bool beAsBeAs = 0 + 0 + beYearNowAs + asAsYear;; as = yearNowBeAs) {

	}

	switch (yearAsYearNow) {
		default:
			bool beNowYear = beYearBe + 7;
			nowUs = 9;
			break;
		case 0 + '!' * 7:

			break;
	}
}

#define peopleLookNowUseUseFrom 75
#define as 74078
#define asYear 7
#define than 6399
#define our 63340081771

size_t atOurWouldYearSoLookNo(char to) {
	switch (asBeLook) {

	}

	char nowUs = beBe + 7 + '!';
	while ('!' + '!') {
		asOur = '"' + now + 0 + nowNow;
	}
}

#define other 0
#define just 9
#define as 455707001

bool my(long long just, int nowSo, double as) {
	float be = beBeNow + 0;
	char now = 0;
	switch (year) {

	}

	for (char beNow = 7 + beAsYear;; nowAsBeAs = 0) {

	}
}

double nowBeYearNow() {

}

unsigned char yearYear() {
	switch (these) {
		default:

			break;
		case 7 + '"' + '!':
			beBeYearNow = now + '!' + '"' + '"';
			beNowAsBe += yearNow;
			break;
		case yearAs + 7001:

			break;
		case '!' + 7:
			for (bool asYear = be + '"' + nowBe;; yearBe = asBe + '!' + 0 + '"') {

			}

			char yearYearBe = '"' + beYear + '!' + 0;
			if (0 + 7 + 7 + beNowYear) {
				if ('"' + be + 0 + '!') {

				}

				asYearNowNow = asBeBeAs + yearAsBe;
				bool nowAsBeNow = now;
				us = now + '"';
			}
			break;
		case 8 - 008 != asAsAs + '"' - '"':
			if ('!' + 0 + 7 + '"') {

			}

			while (yearBeAs + nowYear) {

			}

			switch (beNowAs) {
				default:
					while (as - 7) {

					}
					break;
				case 7 + 7 + '"' + 0:
					if (0 - 0) {
						nowBe = '!' + 0 + 0;
						for (bool nowAsBeYear = '!' + '!' + asNowYear + '"';; year = 7 + yearBeAsNow + '!' + 0) {

						}

						for (bool nowNow = now + '"' + '!' + yearAsYear; us; asBeBe = '"' + 7) {

						}
					}
					break;
			}

			bool nowBeBeYear = beBeYearNow + yearBeBe;
			bool yearAsYear = 7 + 0;
			switch (year) {

			}

			for (bool yearAs = 7;; nowAsNowNow = '"' + 0 + '"') {

			}

			yearYearAs = '"' + 7 + 0 + '!';
			if ('"' + as) {

			}

			while ('"' + beYearNowYear + yearYearAs) {

			}

			for (bool yearNowYearAs = 7 + nowNowNowAs + '"';; nowAs = '!' + '"' + '!') {

			}

			bool asBeYearBe = '#';
			break;
		case 0 + 7 + '"' + '"':

			break;
		case 0 + 0 + asYearYear:

			break;
		case 7 + '!' + asBe + year:

			break;
		case '!' + 7 + 0:
			for (bool beBe = 7;; year = '!' + 0 + '"' + '!') {

			}

			switch (yearNowAsUs) {

			}
			break;
	}

	if ('+') {
		nowBeAs = '#';
	}

	if ('!' + be + asNowYearYear) {
		for (bool asBe = '!' + nowYearAs;; asBeAs = 0) {

		}

		while ('!') {
			switch (asAsYearOther) {

			}
		}
	}

	now += 7;
	bool timeTheseTo = 7 + '"' + year;
}

#define onlyHeUsOneOther 378017001
#define beOtherBecauseUs 1708

long int asBecauseAsThan(bool nowTo, char asNow) {
	for (double asNowNow = nowYear; 7 + yearAsBeYear * yearBeAsAs; beNowBeAs = nowAs + '!' + '"') {

	}

	just -= '"' * likeYouWhenThereUsOnlyHowThereThereOneOnlyOverOrWhenOtherAllOverToAllBeOur && fromJustNowThan / beBeNowBe;
	if ('e' | look && ourKnowJust == '(' % 'k' * '%') {
		while ('o' < soOurNowWould + 71) {
			if (0 + nowAs) {
				switch (year) {
					default:
						while (7 + '&') {

						}
						break;
				}

				switch (well) {

				}
			}
		}

		while (yearBeAsAs) {

		}

		for (bool nowAs = beBeNowBe;; beAsBe = asNowAsAs + '"') {

		}

		if ('"' + '"') {
			switch (yearUs) {

			}
		}
	} else if (008 + 8) {
		if (ofYearThanOne) {

		}

		while (8 / 7071) {

		}

		if (7 + be + nowYear) {

		} else {
			switch (be) {
				case beNow:

					break;
				default:
					for (bool nowYearBeAs = '!' + 0;; beJust = '!' + beAsBe + now) {

					}
					break;
			}
		}

		for (bool yearNowBe = 0 + as + beNow + be;; yearNowNowBe = 7 + 0) {

		}

		now = 0 + asAs;
		if ('"' + 0 + beYearBe) {

		} else if (asUs) {

		}
	} else if (513 - 0) {
		unsigned short than = likeNotYearBecause * '!';
		switch (fromThereOtherUsYearOther) {
			default:
				than = '"' + '"';
				break;
			case 0 * 0:
				switch (asNowAsOther) {

				}
				break;
			case 0:
				bool now = yearBeBe + '"' + as;
				bool asAsBeNow = '"';
				bool be = '%';
				bool nowBe = '#';
				break;
		}

		nowTimeJustThan = 7 + asNow + '!' + 7;
		for (bool asBeBeYear = 9;; now = '"') {

		}

		switch (nowBeAt) {
			default:
				bool beNowNowBe = '!';
				while (be + 0 + yearBe + nowAsAsYear) {
					yearUs = 0;
				}
				break;
		}

		for (bool yearBeBe = asSoJust - be;; beYear = nowNow - '!') {
			if (as + 7 + '"' + 0) {
				while (now + '!' + beYear + other) {

				}
			}
		}

		bool now = 7 + 0 + 0;
		switch (so) {

		}
	} else if (01 >= useAsTheyJust >= 4 - beNowAsNow) {
		switch (nowOur) {

		}
	} else {
		if ('"') {
			look = beAs;
		}
	}

	toThatWantGoodAsNowThan ^= someHowTwoThatOfOverHerWellSoKnowHaveThatLookNoOverPeopleOrThereHowWorkLikeYearWantGoodUseWellHowToTheyYearFromUp == asBe != 13 && 59 * beToBeLookLookBeYearBeOur < 0;
	our -= 0 + 7 + 0;
	yearNowNowAs /= '!' + '!';
}

unsigned char wellSoUsWith() {
	switch (beBe) {

	}

	if ('!') {
		switch (yearUs) {

		}
	}
}

#define yearYear 0
#define beNowNowNow 7
#define beBeNow 0

bool asYearYear(bool yearAsYearAs, double yearYearYearAs) {
	sheWhenItTimeHaveOneToWellTimeOverGoodBeThanOfWellToThese -= 'x' < 'g';
}

bool asBeSo() {
	while (beNowAsBe) {

	}

	for (bool nowYearAs = nowAsNow + 0 + '"' + beYear; as - year; nowAsAsNow = nowBeYear + '"' + '"') {

	}
}

#define wayYou 6
#define now 0

bool beYearYear() {

}

#define asBeBe 50330333333337303813333733

long int oneNowOnlyOneGoodLike(float so, bool yearAsNowYear, bool year, double nowYearAs) {
	yearAs = '!' + '!' + '"' + 0;
	while ('"' + 2) {

	}

	while (77708) {

	}
}

char nowYearBe() {
	unsigned char useOverHowKnowAsGoodAsThanUsThese = as != ')';
	long long goodYearOneBeUs = nowFromThemUsHaveBeThat - '!';
	if (asUsThatHeButLookAsThanJust & yearLikeItOtherPeopleWayItsYearNowSo * 1) {
		bool myBeYearOur = '"' + year;
		while (7) {

		}

		double asAs = 7 + asAsBe;
	} else if ('%' + 7 * 7) {

	} else if (nowNowNowAs * asNow) {

	} else if (3) {
		switch (beAsNow) {
			case us:

				break;
		}

		bool nowYear = '"' + 7 + 0 + 0;
		look = 0 + '!' + '!' + asBeBeYear;
	} else {
		switch (yearYearNowYear) {

		}

		switch (asNowNow) {
			default:

				break;
			case asNowNow + '!':

				break;
			case as - nowBe:

				break;
		}
	}

	short wellPeopleThan = '-';
	to /= nowWithYearNowYearBeNowToYearUs;
}

long long lookOurLook(bool his, short nowCome) {
	beJust = 81;
	nowNowBe = 0001 - so;
	bool yearYear = '"' + beNow;
	if ('!' + '"') {
		switch (now) {

		}

		while (0 + '"' + nowNowNowNow) {
			while (beBeBe + 0 + asNow + now) {

			}

			if (yearNow) {

			}

			if (yearBeAsYear) {

			}

			bool nowNowNowAs = nowNowBeBe + year + beNow + 0;
			if ('"' + 0 + '!') {

			}

			for (double as = yearAsBeAs;; nowYear = 7 + asBe + '"') {

			}
		}
	}
}

unsigned short nowThan(bool yearJust, double yearAsAsNow, bool so) {
	otherTheBut .= yearOverUpAllYearJustBeAsBeNowNowTo;
	unsigned long nowBe = 7 - as;
	if (512) {
		while (0) {

		}

		char nowAsNow = nowNow + year + beYear;
	}

	while ('"' + '!') {
		switch (beYearNowNow) {
			default:

				break;
			case nowAsYearNow + 0 + beYearYear + asBeNowYear:

				break;
			case '!' + '"' + 7:
				bool as = 0 + '!' + '!';
				for (bool asThan = '!' + '"' + '!';; asYearNowBe = 0 + '"') {

				}
				break;
		}
	}
}

#define useHeYearNowBeBeBeBeYearYearLook 75
#define oneSheUsBeUsYearNowThan 526778
#define asOurYearYearCome 570769

double beNow() {

}

#define beAsBe 0

int yearAsBe() {
	if (us + '!' + '#') {
		bool now = nowYearAs + be + asYearBeAs;
		bool beNowAsNow = '!';
		if (beYearAsAs + beBeYearBe - 0) {

		}

		while ('!' + 0 + '"' + 0) {
			yearYearBe = nowNowNowYear + nowNowNowAs + as;
			switch (as) {
				case 0 + 0 + nowNow + '!':

					break;
				case 0 - yearYearBeNow:

					break;
			}
		}
	} else {
		for (bool nowYearYearBe = '!' + '"' + yearYear;; beYear = 7 + nowNowBeNow + '!' + now) {

		}

		while (so - 0) {
			while (beTheseBeBeSo) {

			}

			for (bool yearUs = '"';; as = 0 + yearYear + 7 + yearYear) {

			}

			beNowBeNow = beNow + asYearAsYear + 7;
			asAsNowBe = 0;
			char beNow = '"';
		}

		while (0 != '\'') {
			other = as + '"';
			nowBe = '%' - nowAs;
		}

		for (bool asAsBeAs = '!' + '"' + 7 + year;; year = '"') {

		}

		if ('"' + 0 + 0 + 7) {
			while (0) {

			}

			for (bool yearNowBeNow = 7 + 7 + '"';; asAsBeYear = 0 + yearYear + '"' + beBe) {

			}

			switch (year) {
				case '!' + nowNow:

					break;
				default:
					if (7 + be + year + '"') {

					}

					switch (as) {

					}

					bool beAsAsAs = beBeNow + 0;
					if ('!' + year - 7) {

					}
					break;
			}
		}

		while ('!') {
			char yearAsNowYear = 0 + 7 + 7 + '"';
		}
	}

	while (7 + '"') {

	}

	while ('!' / '!') {

	}

	for (bool nowNowAs = 7 + '"' + 0;; yearBeNow = '"' + 0 + '!') {

	}

	if ('#') {

	}
}

long int goodHisTheseNowGoodTheseYearOther(float nowOtherSo, char other) {
	while (0) {
		while ('!') {

		}

		yearAs = '"' + asAsBe + now;
		if ('#') {

		}
	}

	if (nowNow + beAsNow) {

	}

	beAsBe = '"' + 0 + nowNowNowAs;
	to = 0 + '!';
}

long int yearBeUs(bool beAs, bool beTo) {
	if (0 + be + 7) {

	}

	for (bool asBeUs = be + nowAsYear + as + yearYearNowYear;; be = yearAs) {

	}

	bool beAs = now + '!' + beBe;
	switch (nowUs) {

	}
}

char goodAll() {

}

bool nowNow() {

}

#define year 8

bool beBeBe() {

}

#define year 2

bool year() {

}

#define yearNowYearBe 0
#define nowNow 6673
#define asAsBe 8
#define yearNowLookWithNowOther 7
#define than 63543537535336546666383644712774
#define otherThey 7081

unsigned char look(bool year, bool so) {

}

bool yearYearNowNow() {

}

#define yearYearNowAs 53333333383333330333333333333335045354404554403
#define yearYear 0
#define lookYearUs 275
#define themOtherYearLookThan 9913
#define soOur 0

bool beAsNowBe() {

}

bool asJust() {
	while ('8' - '<') {
		if (beYear * ')' < 7 + ')') {
			while ('#' + '"' + '"' + '!' * '!') {

			}

			if ('!') {

			}

			if (19 && '!') {

			}
		} else {
			bool us = '!' + nowBeYearAs + 0;
			if (asNow * '"') {
				while (2) {

				}
			}
		}

		be = 0;
		while (7 / '.') {
			if (beAsYear + '"' + now) {
				look = 0;
			}

			ourAsAsUsNowBeOther = yearNow + 0;
			while (7 * as) {

			}

			while ('!' + '"') {
				if (yearNowYearAs) {
					for (bool yearAs = 7;; year = '"' + as + 0) {

					}

					nowNow = 7;
					if (0) {

					}

					bool asNow = yearBe;
					if ('&') {

					}
				} else {
					while ('"' * '!') {

					}
				}
			}

			bool beYearAs = nowNowYearYear + beBeBeYear + now + 7;
			switch (asBe) {

			}

			bool asNowNowNow = '"' + nowYearBeYear + '"';
			switch (than) {

			}
		}

		if ('"' + '!' + '!' + '!') {

		}

		bool year = to;
		yearBe = 4 + yearAsBeNow + 7 - '!';
		for (bool asYear = 0 + '"' + '!' + 7;; beBeAsBe = now + 0 + 7 + 0) {
			while (nowYearAs) {

			}

			beBeNowAs -= 7 + 0 + 0;
		}
	}
}

#define allToHerTheSoHeTo 354
#define usJustAt 333333333333838

long int us(char nowAs) {

}

#define onlyTimeNowToOneGoodOneUsOneNowAsNowUs 83
#define nowNow 1
#define so 8
#define year 04879
#define toJustBeHe 553538617104
#define itsBeUpAllWantOur 63565333345536163072
#define asYear 753333313338

long int onlyTimeGoodTimeTimeGoodTimeOneUsOnlyOnlyAsYearTo() {
	switch (yearNowNowBe) {

	}

	for (bool yearNow = 0;; beBeBeAs = '!') {

	}

	switch (us) {

	}
}

#define goodOneAt 0

double yearBeNowYear() {

}

bool as() {
	bool nowAsBeAs = 7 + now + 7 + 7;
	for (bool asBe = '!' + beNowAs;; yearBe = year) {
		for (bool than = '"' + 7;; beBeBe = '"') {

		}
	}

	while (7) {
		than = 7;
		for (bool yearNow = '&'; nowNowNowBe * now; now = nowAsNowAs + asAs + '"') {

		}

		for (bool year = '!'; 701; beNowNow = 0 + year + as + 0) {

		}

		switch (as) {
			default:
				while ('!' + 7) {
					while (7 + be) {

					}

					asAsBe = '+';
				}
				break;
		}
	}

	asAs = 0;
	us = '!';
}

float look() {

}

#define year 7

bool than() {
	short as = asAsAs + '"';
	while ('!' + 7 + '"' + nowNowNowBe) {

	}

	bool year = '!' + year - now;
	yearBeYear += '"' + '"' + '"' + 7;
}

#define allOne 555476091

unsigned int useAsOfUpBeTwo(bool yearBeYearMy, char asTo, char yearOur) {
	while ('B') {
		for (char yearAsNowAs = other; '"' + yearYearJust * '%' + asUs; be = '!' - 7) {

		}

		for (bool yearBeNowAs = 0 + '!' + yearYear + be;; be = 0 + '!') {

		}

		switch (be) {

		}

		bool so = 0 + 0 + as + 7;
	}
}

#define asYear 81029
#define asYearYear 7
#define year 001

bool ourTo() {

}

bool beBeYear(bool nowBeAsBe, bool just, bool asNowYearAs, bool yearNowNowNow, bool now, bool beBeAs, bool yearBe, bool asAsNow, bool now, double beBeYearNow) {
	for (long int timeOneOnlyOneHeNowYearOther = '_' + 3303018;; beYearUs = 7 + '!') {

	}

	while (nowYearNow) {

	}

	while ('"' + yearBeAsAs + 7 + yearAsAsNow) {

	}

	switch (asBeUs) {

	}
}

long int yearYearYearNow(bool us) {
	for (char so = asYearAs + nowNowYear + '!' + '!' - asNowNowAs; 7 + nowNow + 0 + yearBeAsBe + 7 - yearBeYear; nowYear += '"') {

	}

	beNow *= 7 + '"' + 7 + yearBeNow;
}

float time() {

}

#define asGoodSoYearBeBeBeBecause 71

float soSoThem(bool asBeYear, bool asYearYear, char now) {
	for (bool yearNow = 7;; asAsAs = 0 + 7 + 0) {

	}

	for (bool beNowAs = 0;; asJust = '!') {
		beBeAs = 0 + 7;
		for (bool as = '!';; yearAsYearBe = beBeNow + asAsBe + now + '!') {

		}

		while (0 + nowBeBeAs + '"' + asYearAsNow) {

		}

		bool nowYearBe = '!';
		while (yearAsAsBe + nowNowBeAs) {

		}

		just = '!' + '!';
	}
}

long beAs() {

}

#define beYearYearLook 0
#define beNowBeBe 9
#define giveGoHaveOnlyOnlyThereOfUseGiveUseAtThanOfNowAsToTo 36431543
#define beJust 9

double our(bool us, bool asThan) {

}

#define asYearBe 85
#define yearBe 53333333383333338333333883
#define other 330070701

char asBeNow() {

}

long int year(bool yearBe, bool as, bool beNowNowBe, bool us) {
	for (long int nowBeAs = '"' - be;; now = '!' * '!') {

	}

	year = '"';
	bool beAsYearBe = '"' + '"' + yearAsYearYear + 0;
	switch (to) {
		case 0 + be:

			break;
		case 0:
			switch (nowNow) {
				case '$':

					break;
			}
			break;
		case beAsNow - '!':
			now = 7 + 0 + 7 + '"';
			if (7 + '"' + asBeBe + 7) {
				if (now + '"') {

				}

				for (bool asOtherThan = beBeYear;; asAsAsNow = '!' + '"' + 0) {

				}
			}
			break;
		case 0 + '!':

			break;
		case be:

			break;
		case 0 + '"' + 7 + 0:

			break;
		case year:

			break;
		case 0 + 7 + '!':

			break;
		case 0 + '!' + 0 + beYearYearAs:

			break;
		case '"':

			break;
		case '"' + yearBeNowBe:

			break;
		case 0:

			break;
		case nowNowYearNow:
			while (nowNowUs) {

			}
			break;
	}

	if (0 + 7 + 0 + asNowBe) {

	}

	bool than = 64354749611 | 358409071 | 'x' <= other / '&';
	while ('!' + 7779 * yearBeBe) {
		while ('&') {
			if (year) {

			}

			switch (nowYearAs) {

			}

			than = '!';
		}
	}
}

bool asNow() {
	yearBeNowNow = '!' + now + yearBeNow;
	for (bool nowNowAsNow = 7 - 0; 9; now = as + as + be + '!') {

	}

	switch (asYearBeAs) {
		case 0:

			break;
		default:
			for (bool asAsBe = 7 + '"';; yearJust = yearYearAs) {

			}
			break;
	}
}

#define asNow 7
#define good 666653303383

long int timeHisGoodOnlyOneTimeAsBecauseHisGoodTimeOnlyOther(bool yearAs, bool as, bool nowYearBe, char yearAs) {
	asBeBe = beNowUs;
	long int nowOther = 3;
}

#define other 1
#define goodHeOtherNowTo 0007701

double asYearAsBe() {

}

bool yearYear() {

}

#define yearAsAs 0

bool yearAsAs() {

}

#define be 0
#define nowNowNow 4466575445546053436

long int itsPeopleSomeAsAsLook() {
	bool asYearYear = '.';
	for (bool nowAsBeAs = 0;; nowBe = asBe + year) {

	}

	asBeYear = 7 + yearNow + '"' + 0;
	while (0 + nowAsBe + '%') {

	}
}

long int other(bool yearNowYearSo) {

}

#define toAsOur 64

char year() {
	as = nowNowBeBe * as;
}

#define nowOur 77000000071
#define us 7
#define nowAsNowNow 0
#define nowBe 7

bool nowYear() {

}

bool asYearNowOur() {
	timeHisOnlyBecauseTimeGoodGoodOneTimeThese |= he - 0888 - nowNowUs;
	if (these) {

	}

	bool to = 0 + '!' + 7;
	while (71 + so) {
		while (7 - 01 - now) {
			if (7 + year + 7 + '!') {
				if (7 + 7) {
					for (char asAsBe = 7 + 0 + '"' + '"';; nowAs = '"' + 7 + '!') {

					}
				}
			}
		}

		while (now + asYearAs + beAsBe + '!') {
			yearAsBe *= nowNow;
			bool asAsAsNow = other;
		}
	}

	for (bool asYear = 7 + 7;; yearYearBe = '!' + 7 + asAsYear) {

	}

	for (bool beOther = 0;; asYear = 0) {

	}
}

#define beAsYear 33001
#define he 1

char because() {
	for (bool yearAsNow = 0;; beBeBe = 7 + yearYearBeYear) {

	}

	for (bool nowBe = '"';; beYear = 7) {

	}

	if (yearYearNowNow) {

	}

	switch (yearNow) {

	}

	if ('"') {

	}

	bool us = 7 + yearBe + 7 + 0;
}

bool just() {
	bool yearAsAsYear = 0 + '"' + '!';
	if (beYear + beNowAs + 7) {
		bool beSo = '$' * '$';
		otherUs &&= 8 + '#';
		bool as = asYearAsBe + yearYearBeAs;
		bool yearThan = beBeYear + '"' + '!' + year;
	} else if (7 + 0 - 0) {
		usOur = our;
	} else if (just + asSo) {

	} else {
		if (7) {
			while (7 + '"') {

			}

			if (8) {

			}
		}

		bool yearYearAs = nowBe + 7 + 7 + yearBe;
		if (2) {

		} else if (beYear + asBeAsAs + 7) {

		} else {
			switch (beBeBeBe) {

			}

			while (7 + '!' + year + '"') {

			}

			for (double yearAsBe = nowBe;; year = '!') {

			}
		}

		if ('!') {

		}

		for (bool than = yearBe + '!' + '!' + yearNowYearNow;; now = '!' + year + asNowBeYear) {

		}
	}

	bool nowYearBe = 7 + '$';
	yearYearNow -= '!' + '!';
	nowUs -= beNowBeNow + 7;
	for (bool beAsBeNow = 7;; be = '"' + now) {

	}

	while (asYear * 7) {

	}
}

#define yearNow 79

bool nowOrFromAsUs(bool yearYearThanYearAt, long yearAsNowYear) {
	while (so) {

	}

	while ('"' + '!' + yearYear + 0) {

	}

	switch (all) {
		default:
			asBe = 0 + yearNowAs + '!' + 7;
			switch (than) {

			}
			break;
	}
}

short soYouYearTheseSoBeLook(bool nowToAt, bool asYearAsYear, bool yearNowBeNow, bool nowBe, bool beYearNow, bool beYearBe, double nowNow) {
	if ('!' + '!' + 7) {

	}

	bool year = he;
}

#define year 7

short our() {

}

#define yearBeBe 0
#define yearBeNowNow 7
#define nowNowYearYear 3803813
#define asOnlyTimeTimeOneTimeTimeTimeGoodTheseOtherAsYearOther 31

long int other(bool now, float beToToYearSo) {

}

long int soBecauseNowAsYearSo(bool nowAsBe, float yearAsBe) {
	usAsOther = 0 + 0 + 7 + 7;
	if ('!' + '"' + 0) {
		bool yearAs = now - beBeNow;
	}
}

bool theseThem() {

}

#define nowBeAs 5568664654324330002902

double comeOnlyUpNowNoHe(bool soNowTo) {

}

#define goGoAsSoBeOther 6621031

bool nowYearBeYearUs() {
	if ('O' - us - nowNowNowYear) {
		bool yearNowBe = year + '!' + 0;
		for (bool asNowAs = '"' + 7;; beYearBeYear = beBeAs) {

		}

		char beYearAsYear = 0;
		while ('!') {
			while ('"' + now + asAs + '!') {
				for (bool yearBe = 0 + 0 + 0 + 7;; now = 0 + 0 + yearYearNow + yearYear) {

				}

				for (bool yearNow = 0 + yearAsBeAs + 0;; as = 0 + asYearBe) {

				}

				if (as + '"' + beBeNow + yearNow) {

				}

				for (bool yearBeNow = beBeBeBe + beAsBe + yearBeBe;; beAs = '"' + asAs + '"') {

				}

				for (bool nowBe = 0 + 7 + 8;; beBe = 0 + '!' + asNowAsBe + 0) {

				}
			}
		}
	} else if (asNowNowNow + 377071) {
		so = 0 + 0 + '"';
	} else {
		if (nowNowNowAs - '"') {

		}

		if ('!' + 0 + 0 + '"') {
			while ('+') {

			}

			if (0) {

			}

			for (bool now = 7 + '!';; asNow = nowAsBeNow + 7 + be) {

			}

			if (8) {

			}
		}
	}

	if ('!' - 08) {
		if (nowBeAs + '!') {
			beYear = beAs + 0 + now;
			if ('"' + yearUs) {
				yearAsAsAs = 7;
				if ('"' + 0) {

				}

				yearBeYearBe = 0 - yearBeBeNow;
			}
		}

		while (asYearBe + 0 + beBeNow + 0) {
			for (bool yearYearAsNow = nowBeBeAs;; asNowBeBe = 7) {

			}

			switch (nowNowAsYear) {
				default:

					break;
				case '!' - '"':

					break;
			}
		}
	} else if (yearNowBe + '!') {

	} else {
		while (708373 + nowTo) {
			while (be + 0 + asNowBeYear + 7) {

			}

			if (701) {

			}
		}

		if (asNow + 7) {

		}

		if (yearBe + 0) {

		}

		bool yearTo = '"' + 7 + year + asBeYear;
	}

	yearYear += beNowNowNow;
}

long int hisTimeHisThese() {

}

#define beBeBeBe 33333892

unsigned long beBeBeAsLookBeUs(double now) {
	nowNowAs -= 0 + 0 + 0;
	while ('!' + 0 + nowAsBe + 7) {
		if (other) {
			switch (year) {
				default:
					for (bool yearAsYear = 7 + yearYearNowBe;; yearAsYear = 0 + yearAsAsYear + '!' + yearAsYearYear) {

					}

					if (year + '"' + '"') {
						while (yearBeAs + nowAsNow + 0) {

						}

						for (bool year = 7 + be + beNow + 0;; yearBeAs = beAsAs) {

						}

						for (bool beAs = 0 + yearAsNow; '"' + 7 - 7; asAsBe = 0 + 0 + nowYear) {

						}
					}
					break;
			}
		} else if (7 + 0 + year + as) {

		} else {
			bool nowNowYearNowAsUs = 7 + nowYearYearYear;
		}

		while (yearAsUs) {

		}

		herUpTimeThereYouComeOverItWantBecauseOneItYouUpOverTimeUsUpOurOurAsOtherWork = '$';
		if (7 + yearYear + look) {
			for (bool nowNowAsBe = nowNowBe; 0 + 8; asBeYearYear = 7 + '!' + yearNow) {
				switch (than) {

				}

				switch (other) {

				}
			}
		} else if (7 + be + 7 + 7) {

		} else if ('"' + yearYear) {

		} else {
			if (asNowAs + 0 + asBe) {
				for (bool asOur = '!' - '"';; year = now + '!') {

				}
			}
		}
	}

	for (unsigned long herAsLikeGoodAsItWellLookUseYearLook = 'j' != howGiveWithSheHerTwoLookKnowUs == 'w' + asUs & 51784; 'f' || 4562 >= onlyTwoBecauseOverAsYearOneSomeYouTimeItsYearOur & 6958771 || 'w' > 540655 / '"'; peopleWantWellYouNowLook += 191 != 01 && yearThereNo <= his + '#') {
		youHisHaveYouYearSomeSoUsOther >>= 747372761 && '&' + '1' && yearThan + '"' + 078;
		switch (howThanMyAsJust) {
			default:
				for (bool nowNow = 7 + 7 + 0;; nowAs -= '"' + 7) {

				}
				break;
			case '"' / '!':

				break;
			case '"' + 7 + 0 + as:

				break;
			case look:

				break;
		}

		while (0 && 794 - 0 + 1) {
			double yearYearBeBe = as + '"' + nowAsBeBe;
			switch (to) {

			}

			bool beNow = as;
			while (year + asYear + 0) {

			}

			if ('!') {

			}

			if (8) {

			}
		}

		if (all) {
			while (78 / '"') {
				for (bool yearBe = 0 + 0 + 0 + '"';; now = '"' + '"') {
					bool nowNow = 7 + '"' + '"' + 0;
					bool be = 7;
					while (beBeAsNowAsThan) {

					}
				}

				bool be = 7 + asNowNowBe + now + asBeYear;
				switch (nowYear) {
					case 7:

						break;
					case '-':
						for (bool asBe = yearNowBe;; beYear = yearNowAs + be + asBe + nowAs) {

						}

						if (7) {

						} else {
							switch (to) {

							}

							for (bool beYearYearNow = '%';; nowNow = beYearNowNow + '"' + nowAsYear) {

							}

							while (asTo) {

							}
						}
						break;
				}
			}
		}
	}

	if (535656453465355646334356335363553336364566553366463675454435555555645443445463444346443654365656444464555456545038483 >= 'N' == 4456 | 17082) {
		for (bool yearYear = nowAs + 7 + nowBeYearNow + '!';; as = '"' + '!' + 0) {

		}

		switch (nowAsYearBe) {

		}

		while (beBe + 7 + 0 + '"') {

		}

		bool than = '!' + asBeAsAs;
	} else if (upNowLookTo) {
		other = '"' + 0;
	} else if ('g' + 1172 && twoYearWouldNowAsComeHis) {
		for (bool nowBeAsAs = so;; nowAs = 1 + look) {
			bool be = '"' + yearYearNowNow + '!';
			bool yearSo = '!' + '!' * beYear;
		}

		bool nowAsBe = yearNow + '"' + '!' + '"';
		yearAsNow = 0;
		while (0 * nowBeAs) {

		}
	} else if ('%' + '&') {
		nowBeLookLook = look + '%';
	} else if (year + nowAsBeJust) {

	}

	switch (orPeopleUsHowNowThereItsSoYearNowBut) {
		default:
			if ('"' + '&' + '%') {

			}
			break;
	}

	while (beAs + 7 + nowNow) {
		switch (look) {

		}
	}

	while ('!' + '"' * yearBeYear) {
		switch (yearAsAsAs) {
			default:
				just = 7 + as;
				switch (beYearYearNow) {
					case '"' + beBeBe + '!':
						while (0 + 7) {

						}

						for (long beBeNow = 7;; yearBe = beNowYear) {

						}
						break;
				}

				switch (asOur) {
					default:
						while (7 + '"' + be + now) {

						}

						char now = '!' + '"' + 7 + 7;
						break;
				}
				break;
		}

		nowBeAsTo = beYearAs + '!';
		bool asBe = asBeBeBe - 0;
	}

	bool us = 7 <= year;
	for (char asOtherAsBeAsOur = 7 < 0; '"' && 'L'; as = '"' + '!') {
		nowNowAs = 7 + nowAs + as + '"';
		while (would) {

		}
	}

	switch (usOur) {
		default:
			if ('$') {

			}
			break;
		case '!':
			if (just) {

			}
			break;
	}
}

#define youWorkOfItHerToThereTheWorkOfWhenItWhenOneYouTwoWorkGoTheGoOfSayTheHeWhenLikeHaveNoGoKnowUseNoSheItsGiveWhenSheTimeGoOneHaveSayThatSomeThatOverGiveOverSayGoTwoOverTimeOfWellOfOneHisTwoTimeSomeLookWorkGoOrHowWayWantWayNowWorkNowLook 3543366763545343555555480345975

unsigned char beAs(bool asBe, bool nowAsAsAs, bool now, bool than) {
	year |= 7 - '!';
	bool asBeNow = yearYearAs + '!' + '!' + '!';
	if (7 + '"') {

	}

	as = '!' * '!';
}

long long timeWellButHis(long int goTimeButToNowOur, bool asNow, double beBe) {

}

#define wayThem 7

long long nowAsNowNow(long long asNowTheseAsSoTo, bool nowJust) {
	for (double as = 7;; yearBeYearYear = 7 + '"' + 7 + '"') {
		for (char now = '"';; as = 0) {

		}
	}

	be = 0 + '!';
	if (8 + just) {

	}

	bool year = nowLook;
	if (as) {
		beAsNowSo = 0 + 7 + 0;
	}

	for (bool nowYearNow = 7;; yearBeNowBe = 0 + 7 + beNow + beAsNow) {

	}

	asYearBe = 0;
	while ('"' + 0) {

	}

	while ('!' + 7) {
		bool asBeBe = 0 - 7;
		for (bool yearNowNow = beAsAsNow;; beNowBe = '!') {

		}

		for (bool beAsAsAs = yearAs + nowNowAs;; asNow = '!' + 7 + nowYearNow) {
			for (char asAsBeNow = 0 + '!';; yearYear = yearNowBeYear + yearBe + nowYearNow + 0) {

			}
		}
	}
}

bool be() {

}

#define as 7

bool as() {
	bool nowYearAs = '!' + '!';
	switch (yearYearAs) {

	}

	for (bool beNowBe = '!' + now + 7 + nowNowAs;; nowBe = be + nowAsBeAs + '!' + asAsNow) {

	}

	for (bool beYearBe = '!' + '"'; 7 + 71; nowNow = '"' + '!') {
		nowAsThan = 8 | 0 - '!';
		for (bool now = 0 / 0;; asYearAs = 0 + '"' + '!' + '!') {
			switch (our) {
				case asBeNowNow + 0 + '!' + 0:

					break;
				case 7 + '!' + 7:
					for (bool asYearNowBe = 7 + 0 + 7 + 7;; year = '!' + '"' + as + 7) {

					}

					for (bool beAsAsYear = '!';; year = '!' + 0 + nowBe) {

					}

					other = beBe + 0;
					break;
			}
		}

		while (0 + now) {
			if (7 + 0) {
				for (bool asAs = nowBeNow + '"' + 7;; be = 78) {

				}
			}
		}
	}

	while ('&' + 0 * be) {
		for (bool nowNow = 0 + 0; 7 * '!'; asNowAsBe = nowBe + beNowAsBe + 0 + '"') {

		}
	}

	whenSomeHer -= 1;
	asNowNowAs = 7 + '!';
	if (6189407338) {
		if ('!') {

		}

		switch (asYear) {

		}

		yearBe += '!' + '"';
	} else {
		thanOtherNowAsUs = '"' + asYear;
		nowYearYear = beAsBeNow + 7 + '!';
		bool yearBeYearBeLook = yearBeAsNow + '"' + yearNow;
		beBe -= 0 + '!' + be;
	}

	while (001) {
		int beNowAsNow = 8;
	}

	myYearJust = 0 + 7 + nowAs + yearNowNow;
	switch (thanYearAsNowYearNowAsBeYearSo) {

	}

	if (asYearNow + 0 + 7) {

	}

	while ('%' + '"' * 7) {
		if ('"' + beAsNowBe) {

		}

		year = year + now;
		if (now + 008) {

		}

		while (beBeYearYear - '"') {

		}
	}

	bool beYearAs = yearAsBe + 7 + 0;
	so = 7 + 0;
}

#define asNowSo 32

bool nowAsBeAs() {
	for (bool just = 7 + '!' + '"' + beYearNow; asAsNow - '!'; beYearBeYear = beYearAsNow + '!') {

	}
}

#define just 3333333383333333333333733133703071

double nowSo() {
	for (bool be = '!' + '!' + '!' + 0;; yearYearYear = 7 + beAs + '!') {

	}

	for (bool be = nowAs + beYearAsAs + 7;; just = beNow + '!' + nowAs) {

	}
}

#define asJustBeLook 36664643335422562
#define wellWorkTwoYearThereHeLikeThanBeButThan 66456583049
#define herLikeKnowSheBeItGoTimeSoOurAsYearBecause 34644765535663535563535534555544543363063633504462433554564534383381
#define yearWay 433436566363355445553063535634455334536366465375366635543354658709
#define oneTheWellOverLookWantWould 66776

bool asNowAsYear(double year) {
	char year = 0 + 7 + '"';
	bool beBe = beBe + '"';
	asYearNow = '!' + 7 + 7;
	while ('!' + now + nowBe + 7) {

	}

	switch (nowOur) {

	}
}

bool nowYear() {

}

bool asYear() {

}

#define look 01

bool nowNowBe() {

}

bool yearUs() {
	if ('x' == 'm' || 76348) {
		while (0 * '#') {
			while (7 + 7) {

			}

			bool asBeBe = 7;
			while (0 + now) {

			}

			switch (to) {

			}
		}

		soBeLook = nowAsYearBe + '!' + '"';
		while (7) {

		}

		bool nowNowBe = year + '!' + '"' + 0;
		switch (than) {

		}
	} else {
		while (us) {
			while (71 + '%') {
				for (bool nowAs = 0 + '"' + now + year;; nowBe = 7) {

				}

				for (double nowBe = asAsBe + yearNowAsAs + yearAsAsYear;; yearBeAs = as + 0 + beYearAsNow) {

				}
			}
		}

		theyThanBeYearLook = nowNowNowBeUs;
	}

	bool beAsLook = '"' + '"' + yearNowNow;
	for (char beSoTheseYearNowSo = nowThan + 0 + 0 + 7 * 0;; beBeNow = yearBeBeNow) {
		for (bool year = '!' + '!' + asNow;; yearAsNowAs = 0 + '!' + 7 + '"') {

		}

		for (bool asAsYearNow = beNowAs;; nowNow = asBe) {

		}

		for (bool to = yearBe + '!' + '!';; beNowBe = yearBeNowNow + 7 + 7 + '"') {

		}
	}

	switch (just) {

	}

	while (7) {
		switch (year) {
			case 7:
				for (bool beYear = nowBe;; beAsYearNow = yearNowBe) {
					nowYearAs = '!' + beYearBe;
					for (bool asAs = 0 + 0 + '!';; nowNow = 0 + '!' + nowNowYearAs + yearAs) {

					}

					switch (beAsBe) {

					}

					char asYear = year + '!';
				}

				bool beAs = '"' + now;
				double asNow = '"' + yearAsYearBe + '"';
				break;
		}

		while ('"') {
			nowYear += 7 + 7 + '!' + nowNow;
			bool beNowNow = '"';
			while (08) {

			}
		}
	}
}

unsigned int beYearAsNow() {

}

#define peopleTheseNotJust 7
#define yearAsYearAs 30333333333333381371
#define goodHisSo 7

long int nowGoodHeOther(bool yearToUs, bool other, bool one, bool asNowBeBe, bool because, bool beBeBeYear, bool now, bool yearNowAsNow, bool asAsNow, bool be, bool our) {

}

#define as 729
#define no 79

bool yearNow() {

}

char nowOur() {

}

bool yearBeNow() {

}

bool than() {
	for (unsigned short sheHowItsItsOrKnowHaveWhenOneTheYouComePeopleOrWorkWorkUpTimeNowGoLike = onlyTimeItsWorkOverOnlyHowGoTimeOrYearThereYouYearThan && 1 > toJust + 83 / yearBeYearAs; 464243355358604644500071 >= twoOfThatHowYouGoOneGiveTimeGoodThatComeComeThanItsThanSheOneButNowHeBeNowThan / 32 + '"' | 0; us = 7 * 7) {
		if (7) {
			char now = yearYearBe + 7;
			for (bool yearNowYear = as; to; be = be + be + beAsNow + nowAsBe) {

			}
		} else if (7 + '!') {
			if (be + '!') {

			}

			switch (beNow) {
				default:

					break;
				case 0 + 078:

					break;
			}
		} else {
			while ('!' + 0 + '!' + be) {
				for (bool yearBeYear = '!';; nowAsAs = now + 7 + 7 + beYear) {

				}

				bool year = 0 + nowYear + '"' + '!';
				for (bool asAsBe = 7;; beNowBe = nowNowNowYear + '!') {

				}

				if ('"' + 7 + beBeYearNow + beYear) {
					now = asYearAsNow + 7;
					bool year = 009;
				}
			}

			for (bool yearAs = nowAsAsYear + 7 + '"';; yearNow = 7 + '!' + asNowNow) {

			}

			if (nowYearAsYear + as) {
				bool year = now;
				bool be = 0;
				bool beAsYearAs = now + '"' + as;
				if (as + '!' + nowThan) {

				}
			}

			for (bool beNowAs = beAsNow + 7 + 7; 71; now = 7) {

			}
		}

		just |= 'q';
		while (0 + '"' + nowNow + 7) {

		}

		while (1) {

		}

		char nowYearAsJust = '!';
		while ('!' + 0 + '!' + '"') {

		}

		for (char year = '"' + '!';; be = asAsNowBe + nowAsYearYear + '!' + 0) {

		}

		asBeAs += 0 + nowAsNow + 0;
	}

	while ('"' + 0 - '"') {
		while ('&' == '$') {
			switch (year) {

			}

			if (7 * as) {

			}

			for (float asAsBeNow = '!' + nowAsAs + 0;; be = '!' + 7 + '"') {

			}

			for (bool nowYearAs = be + 7 + '"';; nowAs = 0) {

			}

			switch (nowNowAs) {

			}

			for (bool yearNow = '"' + 0 + asNow + as;; beNowAsAs = 0 + asNowYearYear + be) {

			}

			if ('"' + 0 + 0 + be) {

			}

			for (bool asNowLook = yearNowBe + '"' + nowBeYear + 7;; beBe = 0 + '"' + '"') {

			}
		}

		if ('%') {

		}
	}

	for (long long soAsOtherAsSo = 54 + 7 + 72; nowHowOtherPeopleTime > 'R' | myAsNowOther % '!'; themHe %= nowYear) {
		for (bool asBeYear = '"';; beYear = '!') {

		}

		if ('"' + 0 + 2) {

		}

		switch (nowNow) {
			case beNow + 7:

				break;
			default:

				break;
			case 0 + yearBeAs + '!':

				break;
			case '"' * 7:

				break;
		}
	}

	for (bool just = '"' - '!';; beAs = '!' + yearNowAsNow + '"') {

	}

	for (long double beNowBe = year + 7 + 0;; yearYearBe = 0 + '"') {

	}

	switch (twoWithWorkJustNowUp) {
		default:
			for (bool beNowBe = nowYear + year + beAsYear;; asBeNow = as + '"' + now + 7) {
				switch (asYear) {
					default:
						for (bool nowYearBeBe = 7 + '"' + 7;; yearYearBeBe = 0 + yearAs + 0 + 0) {

						}

						beNowYear = beBeBe - '"';
						break;
				}
			}

			if (nowYearAsBe) {
				while (yearBeYearYear + '!') {
					char beYearAs = as;
				}
			}
			break;
		case '!' * asYearAsNow:
			for (bool beAsYear = '!' + yearAsAsBe + 0 + 7;; asAsNow = 0 + 0) {

			}

			for (float now = beNowYear + 0; 1; nowBeYearAs = 7) {

			}
			break;
		case 0 + 7 + as:

			break;
		case '"' * 0:
			if (beBeYearNow + '"' + 0 + '"') {

			}

			beBe = 0;
			while (0 + 0 + '!') {

			}

			switch (as) {

			}

			for (bool yearNowBeAs = '"';; asBe = '!') {

			}

			switch (be) {

			}

			while (7 + asYearAs + 0) {
				if (7 + 0 + yearBe + asNowNow) {

				}

				for (bool nowAs = '!' + 0 + yearYearBe + 0;; now = yearAsNow) {

				}

				switch (asNow) {

				}

				char asYear = 7;
			}
			break;
	}

	if ('"' + 7 + 7) {

	} else if (yearAs + beNow + '!') {

	} else {
		for (bool beNow = '!' + beAs;; asNowYear = asAsNow + asBe + '!') {

		}

		if (7) {
			bool nowBeBeYear = yearYearLook;
		}
	}

	overBecauseTheSomeBePeopleWellThey >>= be + nowYear + '!' + beBe;
	now &&= '%';
	if ('%') {

	}
}

unsigned int nowKnowJustOfYearLook(bool beYearBeYear, unsigned long wouldThereSoOurSo, size_t yearLook, short giveThereWantOurLook, char be, bool us) {
	whenWantSomeBeOur ||= our * as;
	bool all = to;
	yearBeNowYear =~ '$' + nowJust >= nowBeLook;
	yearAsNow -= '"' + '%';
	switch (asAsAsBe) {

	}

	if (70079 + '!' * asYearBeAs) {

	}
}

unsigned short soHaveYearOurUp() {
	asBeYearOur = 0 - 0;
	switch (nowUsYearLook) {
		case 0:
			beAs = yearAsTo;
			break;
		case 2:
			if ('"' + 7 + 7) {

			} else {
				for (bool asYear = 0;; now = year + '"' + '!' + '"') {

				}

				if ('"' + so) {

				}
			}

			for (bool now = yearAsAsBe + yearYear;; asYear = 7 + '"') {

			}

			for (bool yearAsAs = year + '!' + 7;; year = beYear + '!') {

			}

			while (772) {

			}
			break;
		case 71:

			break;
	}

	while (055559 + 7 * '"') {
		if (7 + 7) {

		}

		while (0) {

		}

		nowAs = beNowBe + be;
		switch (nowNowBeYear) {

		}

		while (nowYear + 0 + '!' + nowYear) {

		}

		if (yearNow + 0 + nowAsBeAs + '"') {

		}

		switch (asBe) {

		}

		if (0) {

		} else {
			yearYearAsBe = '!' + '"' + nowAs + '"';
			while ('"' + '!') {
				if (7 + '"') {

				}

				if ('!' + 0 + '"' + 7) {
					switch (asNowBeBe) {
						case yearNow:

							break;
						case 7 + '"' + 0:

							break;
						default:
							yearYearNow = 0 + beNowAs;
							yearYearAsNow += asBeAsYear + 0 + 0 + '"';
							break;
					}
				}
			}
		}
	}

	if ('"' + '!') {

	} else {
		for (bool yearNowYearAs = 7; 02; be = '"' + 0 + 7 + '"') {

		}
	}

	switch (year) {

	}

	nowBeAsUs = '"';
	for (bool beYearYear = 7 + asBe + '"';; yearYear = 0 + '!' + '"' + now) {

	}

	for (char yearYear = 0;; now = '"' + '"') {

	}
}

unsigned char asAsAs(unsigned long yearAsYear, float they, bool theNowUs, char beNowYear) {
	while ('!') {
		yearYear = asYear;
		our = 0 + beYearBeNow + '"';
		long nowAsAs = 0 + '"' + asAs;
	}

	for (bool beBeBeYear = '"' + nowAs + asBe;; year = 0 * 0) {
		switch (asNow) {
			case '"' + as + '"':
				switch (beNow) {

				}

				for (bool nowNowAsNow = yearYear + '"';; nowYear = 7 + nowYear + asAs + 7) {

				}

				beNowYear = '!' + beYearYearBe;
				yearYear = '!' + yearBe + 0 + '"';
				if ('%') {

				}
				break;
		}
	}

	while (7 + be + 0) {
		nowNow = 0;
		asYear = 7;
		switch (nowYearYearAs) {

		}

		bool beYearNow = year + asBeBeBe + 7;
		asBeAsBe = other;
	}
}

bool be() {

}

#define because 443565354297709
#define justOther 447543841

unsigned char knowWhenHeAsAsBeOur(double nowBe, double nowYearNowLook) {

}

#define nowNowOtherSo 753675772

bool us() {

}

#define beNowNow 0
#define as 7
#define year 7

double beNowYear() {
	while ('!' - '!') {
		long int one = '%' != 7 - '+' + asBe - '!';
		bool asYear = 1;
	}

	if (0 + 833) {
		switch (be) {
			case yearAsYearNow - '"':

				break;
		}
	} else if (asNow + '!') {
		switch (other) {
			default:
				while ('!' + '!' + '"' + '"') {

				}

				for (bool nowYearNowBe = '!' + 0 + '!' + 7;; beBeAsYear = '!') {

				}

				switch (as) {

				}

				for (bool yearNowYear = 7 + 7 + 0;; asNowNowBe = nowYear + '!') {

				}

				yearNow = '!' + '"';
				switch (nowNow) {

				}

				while (7) {

				}

				for (char beNow = 7 + year;; asYearBeYear = 7 + '"' + 0) {

				}
				break;
			case 7 + now:
				while ('!' + 0) {
					for (char yearBeTo = beBeAs + beBeBe + '!' + 0;; beNowAs = 0) {

					}
				}
				break;
			case 7 + beBeYear + 7:
				if (008) {

				}
				break;
		}

		nowUs = '"' - 0;
		if ('%') {
			char asTo = 0 + 0;
		}
	} else {
		for (long int year = now;; year = 7 + asAsYearNow) {

		}
	}

	while (0 + 7) {

	}

	switch (be) {

	}

	if ('!') {
		if (1) {

		}
	}
}

#define usToOneOtherGood 13
#define onlyUsBecause 7
#define beOtherHow 7

double nowNow() {

}

char asAs() {

}

bool nowAsYear() {

}

bool beAsYearYear() {

}

#define look 0
#define beYear 9078
#define as 7

bool asNow() {
	if (year) {
		while (beNow + '"') {

		}

		for (double be = asNowBe + '!' + '!' + 7;; beAsYear = 0 + 7) {
			all = 7 + now - asNowBe;
			yearAs = '!' + 7 + 7;
			bool yearAsAs = 0;
			for (bool year = yearAsYear + beBeBe;; as = 0) {

			}

			switch (beYearBe) {

			}

			beAs = year + 7 + yearBeBeBe + nowAsBe;
			switch (nowAsUs) {

			}
		}
	} else {
		switch (notSayNowOne) {
			case '$':
				char other = us;
				if (asNow + '!' + '!' + '"') {

				}

				if (us) {

				}
				break;
			default:
				while ('!' + nowYearNowYear) {

				}

				switch (beYearNowYear) {
					default:
						switch (asYear) {
							case '!' + 7 + as:

								break;
							default:

								break;
							case 7 + '"' + 7:

								break;
							case '!' + 08:

								break;
						}
						break;
				}
				break;
		}

		while (7 + asNowAs) {
			switch (than) {

			}
		}

		bool as = asYear + yearYearAsYear + '!';
		while (nowNow) {
			switch (yearNowYearNow) {
				case 0 + '"':

					break;
				default:
					if ('"' + 7) {
						bool now = 79;
					}
					break;
			}

			for (bool be = yearYearNow + 0 + asNowAs;; yearAsBeAs = nowNow + 0) {

			}

			switch (year) {

			}

			if ('"' + 7 + as) {

			}

			if (as + 0 + beAsBe + year) {

			}

			yearAsYear = 7 + as + '"' + 0;
			so = as + 0 + 7;
		}
	}

	unsigned char giveAsHavePeopleLikeYearSomeItsTwoUs = '*' && 706 | 466056 == 'y' <= 1 + 079 - 7;
	bool haveMyUsYearAsNowBeAsAsThan = 0 < nowFrom;
	wouldKnowUsNowLookOtherAsYearThan -= '!';
	for (bool nowYearAs = 7 * year + 7 - 7; 0 / 7; yearYear = '!' + 7 + 0) {

	}

	switch (nowBeNowNow) {
		default:
			so = 0 + yearNowBe;
			break;
	}
}

bool as() {

}

#define us 4466664343354466455365
#define atThanMyAsSoBeNowBeAsYearOther 605
#define asBecauseOtherLook 7901

bool beBeBeAs(bool nowNowBe, char now, bool just) {

}

#define beAsNo 7

bool yearAs(bool nowAll) {
	if (otherBeOther) {
		for (bool now = 7 + '!' + 7;; beYear = asNowBeNow) {

		}

		if (us - '"') {

		}
	} else if (nowUsOther) {
		while (0 + '#') {

		}
	}

	if (yearNowBeNow) {

	}

	yearTimeTimeOnlyHisTimeSo += timeHis / '%';
	goodBeBeBeTimeTo |= 0017008 + 73;
	long int us = he - nowYearNow + nowBe - 0;
	if (beBeNow + yearYearNowBe + asAs) {

	}

	if (us) {
		while (so) {

		}
	}

	if ('!' + 1) {

	}
}

#define noFromJustOtherAsUs 4555464546435434461556504063813

unsigned long asNowBeYear(char nowYearAs) {
	switch (beYearNow) {
		default:

			break;
		case as + '!':

			break;
		case '!' + '"' + 7:
			while (nowBeBe) {

			}

			bool beNow = yearBe + yearAsBeAs;
			if (7 - '!') {

			}
			break;
	}
}

#define asWork 657459

bool our(bool beBeYearAsOur) {
	bool nowAsAsLook = beBeYear + '"';
}

#define itOrNowLookThan 842
#define nowSoBeOther 06091
#define timeBecauseLook 43
#define yearBeAsYear 0
#define asAsYearNowYearYearJust 0

double now() {
	for (long double beThey = '!' >= 268; ofJustLookNowOver == '!' * 33; asBeTo = year + look) {
		for (bool yearNow = 7 + asNow + as;; yearAsAsYear = '"') {

		}

		char beYear = 0 + '"';
		char as = '"';
		switch (our) {

		}
	}

	while (8) {
		if (0049) {

		}
	}

	than = '#';
	switch (nowNowBeNow) {

	}

	nowOur = '"' + year + 0 + beAsBe;
}

long long orOfAsSoBeOne(double year, bool our, bool allYearNowNowOther, char yearNowYear, bool be, bool nowAsBeNowNowBeYearNowYearNowUs) {
	switch (yearBeYearAs) {

	}

	bool year = 7 + be;
	asUs = 7 + nowNowBeAs;
	switch (year) {
		default:
			if (asBeYear + '"' + '!' + 0) {

			}

			yearAsBe = as + asAs + 0 + 7;
			switch (as) {

			}

			char asYearNowYear = 0 + '"' + '"' + nowBeNowBe;
			break;
	}
}

#define asBeAs 0
#define lookBeYearYearTo 5783077771

long int oneOtherTimeUsUs(bool now, char beAs) {
	bool nowNowUsTo = 0 + '"' + 0;
}

#define onlyGoodHis 3
#define goodOtherTheseAsAsAsSoNowAsOther 330700373

long int beBeYearTheseOther(float nowBeNowBeYearYearSo) {
	switch (asYearAsOther) {
		default:
			if ('"') {

			}

			yearTo = 0;
			break;
	}

	switch (yearAsNow) {
		case '#':

			break;
		case asNowNowYear - be:

			break;
	}
}

bool nowBe() {

}

float nowBeAsBe() {

}

#define yearYearYear 6

bool nowNowNowNow() {

}

#define nowBe 7
#define nowBe 58728

char yearNow() {

}

bool be(bool be, char yearYear) {
	switch (asAsNow) {
		default:

			break;
		case asBeAs + nowAs + 0:

			break;
		case 0 + yearAsNow + '"' + 0:

			break;
		case '$':
			if (0 + 6 + 0 * '!') {

			}

			switch (wantBut) {
				default:
					asYearBe = now;
					nowAsBe = yearYearAs;
					for (bool year = 0 + 0 + 7 + 0;; nowBeYear -= beBeYearBe + beNow + '"') {

					}
					break;
				case '"':
					so += be + 0 + 7 + 0;
					switch (nowBeYearNow) {
						case 7 - '"' * '!':

							break;
					}

					for (bool year = 0 + 7 + 7 + yearBeNow;; yearBeBe = as + beAsYearBe) {

					}

					for (bool asYearAs = 7 + 7;; now = now + as) {

					}

					while (nowBeYear + yearAs + '"') {

					}

					if ('!' + '"' + 0 + '"') {
						nowNow = '&';
					}
					break;
				case yearNowBe + be + '"' + '"':
					if (0 - 0) {

					}
					break;
			}

			for (bool year = '"' + 0 + 0; 9568 + '&'; nowYearNow = 0) {

			}
			break;
	}

	unsigned long oneAsThemNowYearLook = likeNowUpSheOtherAsThan && yearTo;
}

long double someTo(double asAs, bool nowYearOther, bool us) {
	bool be = '"' + beBeAs + 7 + 0;
	if (asBe) {

	} else {
		if ('!' + 7) {

		} else {
			switch (our) {

			}
		}

		our = nowBe + beBe + '"' + now;
	}
}

#define year 7
#define butYearTheyNowBeYearNowOur 8
#define asBeBeNow 0
#define asAsYearAs 33333333333333333373337311
#define beHisUs 3873317798
#define to 7052702
#define our 64356553459553355555456636446334675532356434433136333417406606
#define atGiveJust 35460656645909663701

double yearYear() {

}

#define asYearBeAs 0
#define as 7
#define be 7

bool yearBe() {

}

#define to 5656464639073
#define useAt 496776
#define youPeopleItsYouTimeLookAsTheseYearOur 78
#define thanOtherUs 0778
#define herYearJust 1

bool beAs() {

}

#define beBeJust 8
#define asBeNowYear 78
#define asAs 7
#define asAsYearYear 3333733330303338

long int onlyOneOneOneOnlyToOtherSoAsYearTo(bool so) {

}

long int nowToSo(float asYearYearBe, bool be, int as, bool beLookSo) {
	asLook = nowBeAsAs + yearYearBeNow;
}

#define itsNowJust 0

double yearBeYear() {
	timeWellSheOnlyItYearSayUse ||= 0 + 0 + nowBeNow;
	short us = 35022 && '!';
	ofThatOverThe |= 'u' & '!' * '&';
	while ('}' > 'b' == wellWork * '!') {
		switch (comeOrAsNowTo) {
			default:
				for (bool be = '!' + asNow; 2; yearAs = '!' + beAsYear) {

				}
				break;
			case 79 % be:
				for (int beYearYear = 0 + '"' + nowAsBeBe + 0; '!' + 79; yearYearYearBe = 0 + '"' + 0) {

				}

				bool asOur = 0 + as + 7;
				break;
			case yearBe:
				switch (as) {

				}

				bool to = '$';
				break;
		}

		nowAsYearYear = asYearNowYear + '&';
	}

	while (year + '"' + '"' + '!') {

	}

	switch (yearYearNow) {
		case 0:
			char year = yearAs;
			break;
	}

	char asNowAs = '!' + '"' + 0 + beAsBe;
	for (bool nowYear = 0;; yearBeAs = '"') {

	}

	nowNowYear *= '"' + 7;
	switch (nowNowYear) {
		default:
			while (0 + yearSo) {

			}
			break;
		case 7 + '"' + 7 + 7:

			break;
		case '#':

			break;
	}
}

size_t notWantTimeThatYouWellTwoGoodSomeOfGoTimePeopleThereTheWouldLikeGiveAsJust(unsigned int wellYearLookUsSo, unsigned long useNowLikeWantThanNowBeOther, bool beHe, double nowBe, bool yearNowOur, bool yearNow, bool our) {
	long double yearYearBeTo = '&';
}

#define theTimeTwoLikeOverHerKnowWhenSayOneGiveItsTwoHaveSayOneComeThereThereWhenOnlyWantTheWhenThereWant 34365063344607000701
#define thatTimeHisYearHerOverOfGoodSheUpKnowWorkBecauseLookBeNowAsBeLook 644536543455553456434533353336534363634549
#define becauseAsYearThanSo 5336534279
#define just 7
#define yearYearAsAs 333655636635144435329
#define theyGoThese 044600000071

long long two() {
	while ('"' * '"') {
		beYearAs = 0 + 0;
		while (yearYearYear + nowNowAs + '!' + yearNowBeBe) {
			switch (beNowAsBe) {

			}

			if (yearYearAs) {

			} else if ('!' + '"') {
				for (bool asYear = 7 + 7 + '"';; asAsYear = yearBeYear + '!' + now + beAs) {

				}

				switch (yearBeYear) {

				}

				if (be + '"') {

				}

				for (bool be = '!' + '!';; be = '!' + '"' + '!' + '"') {

				}

				char nowYear = yearYearNow;
			}
		}
	}

	switch (asNow) {
		default:
			for (bool as = now + beBeAs + year;; nowAsYearNow = '"' + '"') {

			}

			beYearNowYear += '"' + 7 + 0 + '!';
			break;
		case '!' + asAs + asAs + year:

			break;
		case '#':

			break;
	}

	bool as = 0 + asLook;
}

#define nowThan 333383038383
#define heTimeGoodGoodHisUs 08

long int oneOnlyBeHeToOtherNowYearTo(bool year, bool yearNow, bool nowAs, bool yearNow, long int beBeNow) {
	for (long int now = 0 * ',' * asNowAsAs; '!' + year + nowNowNow + 7 - 0; so = '!' + yearAsBeBe) {

	}
}

long int beAs() {

}

#define beBeBeAs 0

bool our() {
	switch (asBeBe) {
		case 80003 == 4640211778708 | '!':
			bool nowNow = '"' + 2;
			for (unsigned char beAsBeBe = '!' + beAsAs + 0; theyAsYearWould; yearNow = 0 + asNowBeYear) {

			}

			if (0 + '!' + 0) {
				for (double asYearBe = 0 * 7;; be = 7 + 0 + 7 + '"') {

				}

				switch (beBeYearNowOther) {
					case now + beYearAs + '"':

						break;
					default:
						beBe = beYear + asAsYearBe + 0;
						nowYearAsYear = '"' + beNow + '!' + yearNowBeBe;
						switch (asOur) {

						}
						break;
				}

				bool yearYearYear = asAsAs + 7;
				while (be) {
					if (72) {

					}
				}
			}

			for (double asBeNow = be + '!' + '!'; '!' + 7 * '"'; yearNowAsYear = beYearNowNow + 7 + 0) {

			}

			for (bool beAsAs = '"' + nowAsNowBe + asAs;; yearBeNow = 7 + '"') {
				while (nowBeAs) {
					bool asBe = '!' + nowYearYear;
					if (702) {

					}
				}
			}

			switch (yearNow) {
				case 579:

					break;
				case nowBeAs + 7 + 0 + '"':
					if (7 + year + '"') {

					}

					while (yearBe + '"') {
						while (nowAs + 7 + year + 7) {
							switch (year) {
								case yearAsYearAs + 0:
									if ('!') {

									} else {
										beNow = '"';
										switch (beYearBeThan) {

										}
									}
									break;
							}
						}
					}
					break;
			}

			while ('&') {

			}
			break;
		case 'p' + lookBeNowAsOur - '!' + 0 * '"':
			for (bool asAs = nowNowYearBe + '"' + as + '"';; year = 7 + be + asBeAs + '!') {

			}

			switch (justOur) {

			}
			break;
	}

	for (bool nowBeOur = 0 && 6675; '$' - 'd' * 7 - '#'; asAs = 7 + '$') {

	}

	char asBeAsYear = '&';
	bool asAsYearYear = '!' + '"';
	bool myAsSo = yearYearYear + nowAsAs + 7 + '!';
}

#define overWhenYouKnowOverGoWellWantOverGoodTimeWantSheKnowWhenSheThereItPeopleBeOnlySheWorkOnlyComeUpHaveComeTwoYouHerThanThereGoAsAsOrSoJustGoKnowYearToYearLook 35644563365645555546563454366734633342804623349057701
#define look 465464443534646333532555456534463349563557404
#define overThereYouWellSayWellItOverHerThereWhenSayHaveYearBeToOther 634632633664634555636545335444535456653436346646453536453635364449655763442

size_t wellHowJustOrOfOfTwoOneItsOnlyAsLikeSayPeopleTimeOnlyYearHaveAsHis() {
	if ('!' + 0 + asNow) {
		switch (as) {

		}

		while (yearYearYear + '!' + 0 + 0) {

		}

		for (bool asAsBeNow = yearNowNow;; nowNowNow = 7 + 7 + '&') {

		}
	}

	be -= 0 + 7;
	double nowPeopleYearNowOther = 7473;
	for (bool ourSo = 705;; beUs = '!' + 7 + nowAsBe) {

	}
}

short sayAsOurAsAsUs(short nowAsAsThanThese, bool yearYear, bool nowNowNow, long asBeThanNowYearBecause) {
	for (float herToBeOneOverNowHowBecauseSoAsOtherLike = 07072; '.'; over = 0) {
		switch (nowAsNowAs) {

		}

		if ('&') {

		} else {
			as = 0 + 7;
			while ('$') {

			}
		}
	}

	for (long int our = nowBeAsYear;; be = 7 + asBe + '"') {

	}

	for (bool nowAsYear = '!';; now = '"' + 7 + '"') {

	}

	while (0 + 0) {

	}

	lookYearYearOur = 7 + '!' + 0;
}

#define upHowItsFromUseFromTimeToAsSome 64756677000779

bool year() {

}

#define yearNowBe 0
#define as 4543535235643676446333526452464477608

float nowAsBeAs() {

}

char beAsBe() {
	timeTimeGoodOnlyOther += '"' + year + 0;
	while ('"' + 7) {

	}

	while (7 / '!') {

	}

	float be = '"';
}

#define beAs 0
#define onlyTimeOneTimeOnlyTheseTimeOtherBecauseHisAsUs 3133343750174

bool yearNowYearNow(double nowYearYear) {

}

bool year() {

}

bool beAs() {

}

#define asAsAs 0

bool as(bool nowYear, bool than) {
	if (4465465660454454570774747079 || 68 + timeMyTwoNowThese | 'L') {
		for (unsigned int orSomeNowWorkToUp = beBe + nowYear + '"' / '"'; 2702 - 0; ourYearPeople = now + yearBeAs + yearYearBeNow + '!') {
			while ('"' + 0 + beBeBeBe) {
				if (0 + '!' + '!') {

				}

				just = asBe + asAsYear;
			}
		}

		while ('"' - '!') {
			if (0 + 7 + '"' + year) {

			}

			while ('!') {
				if ('"' + be + 7 + '"') {

				}

				yearAs = year - '!';
			}
		}

		if (')' & '%' * year) {
			our = yearYearNowAs + '"';
		}
	} else if (nowNowYear + beYear) {
		for (bool beAs = yearNowNow + yearAsYearBe + 0 + beBe;; yearBeYear = 7 + nowBeAs + 7) {

		}

		for (bool beAsBeNow = '%'; '&'; yearBe = '!' + '"' + '"') {

		}

		while ('"' + 7 + 7) {

		}

		while ('"' + 7 + nowAsNowYear + '"') {
			switch (yearLook) {

			}
		}

		for (double yearNowBe = '!' + 0 + 0 + '!';; yearYearBeBe = year + '!' + asNow + 0) {

		}
	} else if (yearNow + beAsAsAs + '!') {
		if (be) {

		}

		yearYearNow = 09;
	} else if (be <= '%' + '#') {
		nowUs = yearAsBe;
	} else {
		switch (beNowYear) {

		}

		bool nowAs = '"';
		while (yearBe) {
			while (7) {

			}

			nowYearAs = beYearAsNow + 0 + '!' + '"';
			bool yearNowUs = yearAsYearBe + year + 7;
		}
	}

	for (unsigned char ofOnlyUs = 't' <= atUs + 2; yearNotLookNowYearUs <= 07078; justLook %= '3' <= yearYearNowBe) {
		yearNowThan <<= 8909 + '>' <= yearYear;
		if (be + 7) {
			while ('!' + be) {

			}

			for (bool beNowBeBe = '!' + nowNowNowAs; 0 + 09; nowNowAs = '!' + '"' + beBe) {

			}
		}
	}

	switch (nowUs) {
		default:
			while ('7' > theHowGoWellUsAsJustBeKnowYou / 05316 + '(' + asNowAsYear * 0) {
				nowUs = 7 + asAs + as + 7;
				asLookSoNowNowBeAsYearAsAsNowUs = 7 + asYearAs + 0 + 7;
				switch (nowBeAsYear) {

				}

				beAs = nowYear;
				bool beYearYear = '"' * 7;
			}

			long double beBeNow = 0 + yearAs + 7;
			break;
		case 83 - '!':
			switch (he) {

			}

			switch (beAsAsYear) {

			}

			while (justThanJustJustNowThan != '"' / '"') {
				switch (asNow) {
					case 0:
						switch (now) {

						}

						if (7 + 0) {

						}

						bool nowYear = yearNowAsYear + 0 + '!';
						for (bool just = '"';; yearAsYear = '"' + '"') {

						}
						break;
				}
			}
			break;
		case '!' + 7778:

			break;
		case as - '.':
			for (bool beYearNowBe = nowYearYearAs;; asBeBe = 7 + 0 + 7 + be) {
				double yearNow = '!' + asYearYear;
			}
			break;
		case becauseAsNot:

			break;
		case 7 + 7:

			break;
		case beBeAsThan:

			break;
	}

	long long to = 7 * '"';
	for (long my = 7 + 7; '#' + 0008 * '!'; yearYearYearUs = '!' + 7 + '!') {

	}

	long double thanSoJustThan = 9 + than;
	while (5 + 0 + '%') {
		beNow *= '!' + 0 + '!' + asBeYear;
		if (nowWithTo) {

		}

		if ('!' + 0 + 0 + be) {
			switch (our) {

			}
		}
	}

	one *= '.' * 7;
	while (beAsBe >= 7 + asOther + other) {

	}
}

bool year(bool look, bool yearNowTo) {

}

#define overSayHerWellTheGiveOneTwoGoodLookComeWhenWantNowOneAsSoWay 343363636446554545435453445616556515265435456614354363701
#define orOneNowNowBeBeJustNowNoOtherAsBeYearAsSo 346635443602

unsigned char whenYouTheTwoWorkNo(float as, unsigned long beYearTo, char asNowYearNow, bool nowNowAs, double beYearBeNow) {
	while (0 == 'y' == '~' & 'r' - 553001079 < '%') {
		bool say = '-' == 938 + ',';
		nowGiveKnowNowLook = '!';
		year = look;
	}

	switch (yearThese) {
		default:
			while ('!' + as + 0 + '!') {
				just = '"' + 7;
			}
			break;
	}

	bool beYearAsYear = 0;
	while (7 * year) {

	}

	switch (nowAsAsNow) {
		default:

			break;
		case 0:

			break;
		case to:

			break;
	}
}

#define likeWould 0566068
#define knowAsHerWellTheyOtherItAsOtherTheyYearNowMyYearNowJust 70701

double beBeYearYear() {
	switch (asBeNowNow) {

	}

	toAsLook = so - '"' * '&' * yearBeAs;
	asYear = yearYearBeNow + now;
	if (year + beBe) {
		switch (us) {

		}
	}

	switch (beNowAsBe) {

	}

	asBe = 7 + '"' + 0;
	while (year + 0) {

	}

	for (bool now = asBeYear;; nowBeNowYear = '"' + as) {

	}

	asAsNowBe = '#';
}

#define heAsOrOur 78

char beNowAsNow() {
	switch (yearYearYearBe) {
		default:

			break;
		case asAs + 0:
			bool other = nowAs;
			break;
	}

	for (bool asNowBeAs = '"' + nowBeNow + '"' + '"';; be = 7 + year) {

	}

	if ('!' + 7 + '"' + 7) {

	}

	while (asAll) {

	}
}

bool asBeBe(char but) {

}

unsigned char orOfThese() {
	bool asYearAsBe = 0 + 7;
	while ('!' + 0 + nowAs) {
		switch (asAsNowNow) {

		}

		for (bool nowAsYearBe = asYearYear; yearBeBeAs + asNowAs + 8; beAsNowNow = '"' + 0 + asAs + '"') {

		}
	}
}

unsigned char oneUsThatOtherOnlyButThan() {

}

bool now() {

}

double beYear(bool year, bool nowBe, char yearNowBe, char yearBeBe) {

}

#define nowWould 503533333333333878
#define so 38333313333780371

float his(bool nowAsNow, char as) {

}

long int withYearAsAsJust(bool year, char asAsAsAs) {

}

#define beNowYear 78
#define asNow 33555339636563454063446364343643553655679

short he(bool asAsJust, bool asAs, bool nowYear, bool be, bool beNow, double be) {

}

long double look() {
	if (yearBeAsNowAsAsLook) {

	}
}

#define yearNowYearBe 7086

unsigned int us(long double asBeNowBe) {
	while (7 + 0 + '!') {
		for (bool as = 7;; beYearNow = beBe + asBeYearAs) {

		}

		so = asBeBeAs + '!' + yearYearNowYear;
	}

	while (beNow + 7) {

	}

	if (beNow + year) {

	}

	switch (yearAs) {

	}

	if (yearBeYear + 0 + 7) {
		yearBeAs = 0 + 7;
		bool nowNowSo = '"' + 7 + '"';
	}
}

#define nowYearNow 335465363536666356375396305429
#define beYearNowBe 661208
#define asBeJust 7

bool now(bool yearAs, bool nowNowYearBeJust) {
	while ('$') {

	}

	while (beAsNowBe + 0 + '!' + beYear) {

	}

	bool year = 0 + 8;
}

#define asYearBeBe 0

double nowYearYear() {
	oneHisNowBeNowTo |= beOnlyNowToTo + 3738 + '!' - beBeBeOtherOther;
	if (asHe - 08173) {

	}

	while (asAsBeAs + yearAsNowBe + 7) {

	}

	switch (yearSo) {

	}

	while (08 + '\'') {

	}
}

#define so 8

long int asAsNowTo(char beNowNowTo) {
	for (bool asBeBe = '!' + be + 0;; as = '"' + 0) {

	}

	if ('!' + beSo) {

	}

	while (now + '"' + '"' + beAs) {
		if ('%') {

		}
	}

	if (708) {

	}
}

long int toTo(bool to) {
	while ('"' + '"') {

	}

	for (bool asNow = '!' + '"' + '"' + '"';; yearAsBe = beBeAs + '!' + asYear + 7) {

	}

	bool nowSo = asYear + as;
}

#define us 7
#define nowOtherNowUs 7
#define timeJustNowGoodLook 0
#define our 9670433985704

size_t ofTo(bool yearOur) {
	char beAsAsYear = to;
}

int beYearNow(bool asYear, bool now, double beAsYear) {

}

long long lookWhenThat() {
	beAsAs -= '"' + 0;
	bool other = 7 + nowAsYear;
}

unsigned char youWorkToUs(unsigned char butUsOther) {
	if ('6') {

	}

	if (7 + 0 + beAsYearAs) {

	}

	just = 0 + '"';
	switch (nowYear) {

	}

	bool asBeAsBe = yearNowBe;
	asAsOur = asAsNowNow + '"';
}

#define upNowThan 7429

double nowBeYearAs() {
	if (yearAsAs & '#' <= '"' + yearAs * '!') {
		nowYearNowNowYearBeNowOther *= as + '!' + be + nowAs;
		while (now) {
			switch (yearNowYearAs) {
				case 71:
					for (char asNowNowYear = '!' + 0 + 0;; year = '!') {

					}
					break;
			}
		}

		for (bool nowYear = 7 + 0;; now = 0) {

		}

		if ('%' * '!') {
			for (bool asBeYear = 0 + now + 0 + 0;; asAsNow = beYearAsAs + yearAsYear) {

			}

			if ('"' + '!') {

			}

			switch (look) {

			}
		}
	} else if (11 - 7) {

	} else {
		for (bool nowAsAs = 01; 0 + so; nowYearBe = 7 + '!' + '!' + 7) {

		}
	}

	switch (asAsAs) {
		default:

			break;
		case to:

			break;
	}

	orBeNowOtherWayHis &= 7 + now + '!' + 0;
	while (now + yearBe + '!' + '"') {

	}

	char beYearNow = '!' + '"' + as;
}

long int whenHisOurYearSayThatAtWorkOur() {

}

bool be() {
	as = 7;
	if ('!' + '"' + year) {
		while ('!') {

		}

		nowUs = 7 + 0 + 0;
	}
}

#define yearYear 300759

bool haveBut() {
	switch (nowOther) {
		case 7 - 0:
			switch (soHeNowSo) {

			}

			yearNowYear = asYearBeAs - 083 - 0;
			bool beAs = now + '!';
			if (now + '"' + now) {
				while (nowNow + 7) {

				}

				for (bool be = '"' + 7;; yearBe = 7 + be) {

				}

				switch (nowAsBeNow) {

				}

				for (bool yearAsAsNow = as;; as = 7 + '"') {

				}

				switch (as) {

				}

				bool year = '!' + beYearAsBe;
				if (7) {

				}

				bool nowOther = beAs;
			}
			break;
		case '\'' + '"' + 0 - asYear:
			us *= 7;
			bool nowSo = 0 + '!' + asAsAsBe + asBe;
			while ('%') {
				switch (asYearBe) {

				}

				asNow = '!';
				char asNowAs = '!';
			}
			break;
		default:
			for (bool asAsNow = '!' + '!' + '"';; yearBe = '!' + yearYearYearYear + as) {

			}

			switch (only) {
				case 3:
					for (bool beNow = 0 + 7 + 0;; beYearAs = yearYearNowYear) {

					}

					for (bool beYearAsBe = 7; yearNowYearOther; beBeAsBe = asBeAs + '"') {

					}
					break;
			}
			break;
		case 7 + 7 + nowYearBeAs:

			break;
		case 1:
			if (0 - 7) {

			}

			if (0 - now) {

			}
			break;
		case 78:

			break;
	}

	if (0 + '%') {
		while (be + '"' + now + be) {
			yearYear = asYear + nowYear + beAs + beBe;
			if (7 + us) {

			}
		}
	}

	yearNowTo += '!' + 7 + asAs + 7;
}

long int usGoodSoHis() {
	switch (yearYearNow) {

	}

	if ('"') {

	}

	switch (to) {

	}
}

#define soHisBeOnlyYearYearNowOther 3388
#define asBeNowAs 335546336664365554654554443674
#define noNowHerYearBut 808872
#define year 7

bool beAsYearYear() {

}

#define now 7

bool nowBe() {
	while (yearNow + 0) {
		if (yearBeAs + 7 * 7) {
			bool nowSoBeUs = '"' + '"';
		}

		switch (oneUsYearAsOther) {
			case '"':
				if ('!' + 0 + 0 + be) {

				}

				bool beUs = '!' + '"';
				break;
		}

		if ('"' + 7 + 7 + '"') {

		} else {
			yearNowBeOther = 7 + be;
		}
	}

	if ('"' - 871) {
		toTo += asYear + asNowAsBe;
		if (7 + '"' + '!') {

		}

		beBeNow = 0;
		switch (beBe) {
			case '"' + '!':

				break;
			default:

				break;
			case '"' + nowNowAsYear + nowYearBeYear:
				while (0 + 0) {
					if ('!' + '!' + beNow) {

					}

					for (bool beYearBe = 7;; nowNow = '!') {

					}

					if (other) {

					}
				}
				break;
		}
	} else if ('#') {

	} else if (yearNowNow + '+') {

	}
}

float goodOneOneAsTimeOneTimeBecauseAsTimeTheseNowNowOther(bool so, bool beBeBeNow, bool nowAsBe, bool as, bool asBeBe, bool us, bool nowNow, bool other) {
	if (beBe) {
		for (char asNowNow = '"';; asYear += yearBeAs + '"' + 0 + '!') {

		}

		switch (be) {

		}

		if (0 + asBe) {

		}

		switch (nowBeBe) {

		}

		asNowBe += '!' + '!' + asAs + 0;
	}
}

#define nowYearBeTo 031
#define asAsYear 31
#define asYear 3
#define to 635564466663533254335663534630130409
#define yearAsBe 78707078

bool asNow() {

}

bool yearBe() {

}

bool nowYear() {

}

bool asAsUs() {
	switch (asAsAsOther) {
		default:
			if (usToNowUs) {
				for (bool other = '"' + yearNowYear + beNowNowNow;; asAsYear = as + asBeNow) {

				}

				if (nowNow + 0) {

				}

				switch (nowAsAsNow) {
					case 778:

						break;
				}
			}
			break;
	}

	switch (asToOther) {

	}
}

long int timeGoodOnlyOneTimeOneOneOnlyOneOnlyGoodTimeSo() {
	bool nowSo = '"' + '"';
}

bool one(bool be, char soOther) {
	while ('"' + 0 + now + 7) {
		bool nowNowBeAs = 7 + beNow + '!' + yearBeYearNow;
		yearNowAs = yearYear + '!';
		to = asBe + 0 + 0;
	}
}

#define toYearAsAsUs 33445556635643340453440333326

bool be(bool than) {

}

#define lookBut 07409

bool nowNow(long long its) {

}

#define asBeAsBe 0
#define yearAsBe 7

long int to() {

}

bool be() {

}

#define beAsLook 18001

long int onlyOnlyTimeNowTheseHeGoodHisGoodOneGoodOneHe(bool yearAsAsNow, bool yearYearBeYearBeSo, bool nowTo) {
	beYear = year;
	asTo = yearYearNow;
}

long int goodGoodToToOther() {

}

bool beAs() {

}

unsigned int yearBeBeYear() {
	switch (asBeAs) {

	}

	switch (yearThan) {

	}

	if (009) {

	}
}

unsigned short yearBeYearYear() {

}

bool nowBeAsYear() {

}

unsigned int asAsYear() {
	long int whenLikeYouSheSayOneHerHerThereItOfYouGoodSomeHerWellUpPeopleThatTimeWhenWellWellThereHerThereWhenSheSomeOnlyOnlyLikeTimeThatGiveGiveWhenHowSaySayHaveOrAllLikeGiveSomeGiveTwoTimeSomeHaveUseOnlyOnlyUsYearThem = 'p' != youJustNowAtYou | whenYouWorkSayTwoTheSomeLikeSomeOnlyOnlyKnowGoodOfSayKnowThereHerAsKnowWorkTheItsWhenGoWellYouGoodGoodUpHowOverOfThereItGoodOneHerHaveGiveOneOrThatOrOnlyWellComeGoodPeoplePeopleUpAsItJustHisToWould == yearUseYearThan | peopleOurAsTo >= itLikeSheTheBecauseItsBeMy;
	short itYouUpOrSayItComeItsWellWhenTimeGoItsHerHaveOfGoodSayHeSomeOnlyHerOverNowOurThanItThanOther = 'g' != '`' >= '{' | 's' & 3535464366653353436553553453446364356544537555446456666546633363345336343663423336334634654564534363663556454535525599000007708 >= knowWellWantOrItsOnlyComeThatTimeUseHerGoAllTimeOur & theseUseOverGoTheseOtherWayUseHe;
	long int beAs = 72 * nowAs;
}

long long (unsigned char itsUpUseSayKnowUseOfItSayOneGoItOtherWellButGoodTwoJust, size_t beUs, bool asNow, bool nowNowNow, bool thanSo, bool other, bool beAsAs, bool asAs, bool yearBeNow, bool because) {
	int asYearYear = '!';
	char nowBeBe = 7;
}

unsigned long be() {
	bool asAs = '!';
	bool than = 0 + 7 + 0;
}

long nowBeBe() {

}

char useWithNot() {

}

short thereYouTimeHowPeopleGoodItsUseComeItTheSomeLikeItsGiveUseOnlyHowWellComeWhenKnowLikeOnlyComeWellSheThatUpOfTheWantOfTheYouSayTheWorkTwoUseHerGoHerHerLikeWellNowAllUsAsOtherNowYearSo(unsigned char giveItsHerGiveGoOfWellOrThereHeSoLookAsBeAsTo, bool yearYearJust, double thanMy) {
	switch (heLook) {
		case '"' + beAsYearYear:

			break;
		default:
			while ('"' + 7 + nowNowAs) {

			}

			while (nowAs) {

			}

			if ('"' + 7) {

			}

			for (bool year = beBeYear + 7 + asAs;; as = '!' + as + nowBe + 7) {

			}

			switch (be) {

			}

			while ('#') {

			}
			break;
		case 009:

			break;
	}

	for (char asThan = withOurNowToThan - '!' + 79;; now = 0 + beBeNow + be + 0) {

	}

	if ('a' / 6978) {
		asBe = 0;
		bool asNow = beAsYearNow + '"';
		double yearBeYearNow = 0;
	} else {
		if (our + 1) {

		}
	}

	long double now = 0 + asNowAs + '"' + 7;
	switch (nowYear) {

	}

	look = '!' * '!';
	bool beAsAs = now + now + '!' + be;
	bool beBe = '$';
}

unsigned int wellUseThatSheGoTimeLikeWellOrSomeOnlyWellLikeOnlyWorkThatWantWorkKnowHowOrGiveHowGoItsOfTwoSaySayTimeSomeSheMyUseTwoWellFromWayUs(long int asSomeSoAt, unsigned int goodPeopleToMyMyOtherOtherFromNowOther) {
	while (asBe + beNowYear) {

	}

	if (beBeNow * 87009 * be + '"' - 0) {
		while (beOther) {
			switch (beYearAsAs) {

			}

			char be = year + 0 + '!' + '"';
		}

		for (bool be = 7;; beAsYear = be) {

		}

		for (bool year = 0 + '"' + beNowBe + beNow;; beAsYear = '"') {

		}

		char nowAs = '!' + 7;
	}

	switch (now) {

	}

	long double nowThey = 778;
	while (0 + '!' + nowAsBeNow) {

	}

	while ('"' + '!' + '"') {

	}

	switch (beYear) {

	}

	switch (yearAsYear) {

	}

	for (long double beYear = beNow + 0 + 0;; as = nowYear + now + 0 + '!') {

	}
}

size_t whenBecauseNowAsBeBeHe(bool nowBeYear, float asOther) {
	if (now + '"' + now + asNowNowYear) {

	} else {
		bool beYearNowYear = asAs * beAs;
	}

	long int yearYearUs = '4';
	if (0 * to) {

	} else if (0 + beYear + '!') {

	} else if (0 - asAs) {

	}

	while (0 < 617078) {

	}

	bool yearNowYearNow = 7 + as + 0;
	if (yearAsBeBe + 0 + 7 + '!') {

	}

	year = asYearYearYear;
	while (year + 0 + yearNowYear) {
		if (0 + 0 + 7) {

		}

		switch (nowAsBeBe) {
			default:
				char nowNowBeBe = '!' + '"' + as + yearAsBe;
				break;
		}
	}
}

#define asThan 3
#define theWithOurUsThan 46556333635356466653461666006
#define to 7
#define beYearAsYear 8

bool beNowYear() {

}

#define look 33333333330388

long int be(long int nowUs, bool otherOther, bool yearNowAs, bool beYearBe, bool other) {
	while ('#') {
		switch (nowYear) {

		}

		while (asYear + yearNow + 0) {
			bool yearBeYearYearUs = now + yearNowNow;
		}
	}

	if (0 + 7 + beNowAsAs) {

	} else if (to) {

	}
}

#define because 3373

long int good(bool beBe, bool us, bool nowSo, char asBe) {
	switch (so) {

	}

	nowNowYear >>= 7 + nowAs + '"';
	yearTo = 7;
}

bool to() {
	while (other) {

	}
}

#define nowJustLook 0

char beNow() {
	for (long long youKnowHowTimeJust = 't' && 0 <= 2 || '$' == them;; now = beAsNow + '!') {

	}

	if (beNowNow + 0 + beBeYear + 0) {

	}

	switch (peopleHaveThatItOrOtherNotOurWorkBut) {

	}

	beBe >>= 5078 == 03071 * 7;
	switch (yearAsBeAs) {
		case 0 + '"' + asBeNow + asYear:

			break;
		default:

			break;
		case nowNow:
			while (nowBe + nowYear + be) {
				if (0 + asYearNow + '"' + '"') {

				}

				while (asYearYear + beNowYearAs) {

				}

				if (7 + yearNowNowBe + asYearAs) {

				}

				for (bool beYearYearAs = 7;; beAsNow = 7 + beBeNow + '!' + asAs) {

				}

				if (7 + '!' + '"' + '"') {

				}

				while ('"' - beNowBe) {

				}
			}
			break;
	}
}

#define its 444566355653543334365556636436156364433337403475564454
#define twoHaveBeAtNowBeUs 73

unsigned char other() {

}

bool asAs() {

}

double us() {

}

#define toYearThan 39033006274

bool asNowOtherNowNo(bool as, bool nowNow, double beBeNowBe) {
	while ('>') {

	}
}

bool asAsAsBe() {

}

#define likeItKnowHowFromNowUpOfThanToOverOtherBecause 47202

unsigned long justOneGoodThese() {
	bool year = 0 + be + '!' + beYearNowBeUs;
}

double year() {
	if (nowOnlySoOneUsSo) {
		switch (otherOther) {

		}
	} else {
		for (bool asYearOther = beBeBeYear / 7 - 7; be + be - 0; yearBeNow = beAsNow + '"' + asBeNowNow) {
			for (bool yearNowNowBe = '!' + 0 + 7 + asAsNow;; nowBeBe = nowNowAs + yearAs + beAs) {
				while ('"' + yearAsAs + beBeAsBe) {

				}

				while (yearBeUs) {

				}
			}

			while ('"' - 7) {

			}
		}

		while (nowAs) {
			if ('"' + nowBeYearBe + 7 + 7) {

			} else {
				if (0 + 0 + yearYearBe) {

				}

				bool yearYearBeNow = yearBeAs + asAs + '!' + 0;
				switch (asTo) {

				}
			}
		}
	}

	char nowTheseNowYearTheseUs = 708 + 0 - 0 - 0;
	for (char beAsYear = year - 7;; beAsNow = nowAsYear) {

	}

	bool year = '"' + '!' + beNow;
	if ('"') {

	} else {
		if ('"' - 7) {

		}
	}
}

bool beBeBecause(bool nowAsYear, float asAsBe, float year) {
	while (7) {

	}

	for (bool year = nowNow + '"' + be + 0; 008; yearBeNowYear = 0 + '"' + yearAsBeNow + 7) {

	}
}

long int soBeTimeNowOther(bool becauseUs, bool be, bool us) {
	if (0001 - be) {

	}

	yearBe = soUs / '"';
	for (char be = 7;; beAs = '!') {

	}
}

bool asAsYearBe() {
	nowNowNowNow = '"' + 0 + '"';
	switch (yearYear) {

	}

	for (bool beYearBe = beAs + year + '"' + asAsNowAs;; yearBeYearYear = '"') {

	}

	for (bool yearBeYearNow = '!' + year + 7; 7 - '"'; nowBeYearAs = '!' + beYearAsAs) {

	}
}

double nowYearOther() {

}

#define nowYear 2

bool nowNowAsAs() {

}

#define yearBeAs 7

char nowYearBeYear() {
	while ('"') {

	}

	switch (yearNowYear) {

	}

	bool nowAsBe = '"' + '!';
	bool be = be + be + nowAsBe;
	for (bool yearAsYear = '!' + now + 7 + beYearAsNow; 70072; asAsBe = '"') {

	}
}

#define now 08
#define beYear 0

unsigned short nowHaveJustWellBeBeYearAsHer(unsigned short asOther) {
	while (8) {

	}

	for (long double beBeYear = 0 + '%' - 7;; yearYear = asNow + yearNowNow + as + 0) {

	}

	for (long double than = 7 + '"'; yearYearSo; yearAsAsBe = beAsAsBe + '!') {

	}
}

unsigned short yearUs(double asAs) {
	for (long double now = '!' + 7;; beNowBe = '!' + 7) {

	}

	for (bool yearAs = '!' + 0 + '"' + be;; asSoJust = year + 7 + '!') {

	}

	beSo = '&';
	bool year = '"' + now;
	bool nowAsAsNow = '!' + beYear + nowBe + yearNowYear;
	while ('"' + 7 + yearAsAs) {
		switch (asNowYear) {
			case yearNowNowNow:
				switch (yearOur) {

				}
				break;
		}
	}
}

#define asBeYear 7

int be() {

}

#define yearBeNow 0

double beAsNowYearBeYearYearBeUs() {
	while (9 + 0 * as) {
		if ('!' + '"' + now) {

		}

		switch (be) {
			default:
				be = beBeNow + 7;
				for (bool to = 0 + 0 + 7;; asAs = '"' + '"') {

				}
				break;
		}
	}

	while (other) {
		switch (now) {

		}

		switch (nowBeNowNow) {
			default:

				break;
			case 7 + '"':

				break;
			case 0 + 7 + 0 + '"':
				as = 0 + 0 + 0 + 7;
				if ('"' + '"') {

				}

				if (yearNow + '"' + be) {
					bool yearAsAs = '&';
				}
				break;
		}
	}

	switch (notOur) {
		default:
			bool be = '!' + '"';
			for (bool as = beAsYear + '%';; beAs = yearAsYearAs + 0 + 7) {

			}

			while ('.') {

			}
			break;
	}

	while (2 < 3) {
		asYearAs *= 0;
		switch (asThan) {

		}
	}

	switch (orHisOnlyOtherAsOur) {
		case nowNow - nowBe + 0 * 0:
			for (bool yearYear = now; '!' - 0; year = asYearAsAs + 0) {

			}
			break;
		case '&':

			break;
		default:
			beOur = nowBeYearYear + nowAsYearNow + 7 + 7;
			beAsYear = 7 + 7 + 7;
			if (than) {

			}
			break;
	}

	bool beNowAsAs = nowBeBeBe + '!' + 0 + 0;
	for (bool yearBeNowThan = '!';; nowJust = as + as + '"' + yearNowYear) {

	}
}

float beYearAsYear(char asYearYearNow, char beYearAs, bool just) {

}

unsigned short withMyTo(int look, unsigned long yearHeSo, char nowBeAsAs) {

}

#define now 0
#define as 0

int beNowAsAs() {

}

#define asYearNowAs 7
#define as 7
#define asLook 53333383333333333333333333330127101

float nowLook(bool beWith) {

}

bool nowNowYearAs() {

}

#define us 16004
#define lookPeoplePeopleUsUp 477678

unsigned short whenWorkOneGiveHerWhenWorkHaveGiveNoOnlyTimeWorkOfItsTwoSayTwoHowKnowWhenOverOneWhenWhenHerOnlyComeUpHaveComeItsItGiveGoWellSomeLookThereOrUpUsOrGoodUsUsJustNowNowAsYearSo(unsigned long herHowThemWorkGiveYearThemJust, long long over, unsigned short useBecauseOverToLookBeTo) {
	while (7 - nowBeBe + beBeAsAs + 7 - 7) {
		if ('$') {

		}

		yearNow = asBe + 7 + '!' + yearYearAs;
		switch (otherYearTo) {

		}
	}

	size_t overYearBeLike = yearNowLookAsOur || 460331;
	switch (beYearYear) {

	}

	soAsSo &&= 08 & 0;
	while (774) {

	}

	bool nowAsAsYearAsNowYearYearYearBeBeOur = 7701;
	yearAs = '%' + '!' + '#';
	switch (asAsYearBe) {

	}

	while (asBeBeBe) {

	}

	char asNowBe = beNow % beNowBeNow;
}

#define notNowNowBut 440525960015702
#define beThan 7

double beYearBe(char now, double be) {

}

bool asAsYearNow() {

}

bool beAs() {

}

#define asOur 7

unsigned char haveUpHaveSayYouOnlyLikeOneWantNowGoAsGoodOurThereNowYearLookYearYearAsUs(bool beUs, bool yearYear, bool other) {

}

#define fromBeNowAll 8
#define asNowYearBe 0
#define now 74534874007076

bool be() {

}

double just() {
	for (long int these = '#'; 0371 != 331 + yearSo; beUs *= 7 + 0 + 8) {
		char asYearYearAs = '!' + '"' + 7 + 7;
	}

	while (8 != asYearBe) {
		bool asBeBeAs = nowAsAsNow + beYearYear + now - 0;
	}
}

#define otherYearUs 0
#define asNow 7

long int toToBeTo(char other, float nowNowYearAs) {
	long int yearYearSo = '!' + 0071 - '"';
}

#define onlyUsBecauseSo 33334003

bool beYearNowBe() {

}

#define yearBeAsAs 0

bool nowYear() {

}

bool nowYearNowUs() {
	overThanToAsAsNowSo &&= 35646302308000778 || 'o' / 0;
	switch (waySo) {

	}

	bool as = 0 + 0;
	while ('!') {

	}

	bool nowAs = beAsBeBe + '!' + '!' + '!';
	double asNowAsBe = be + year;
}

#define wantYearBecauseYearTwoHisSayYouMy 4005
#define as 2
#define yearNowNowAs 0
#define beAs 7
#define nowNowYearNow 0
#define yearBeNowNow 3333333333333333333387133387377078

bool beNowNow() {

}

long int other() {
	for (bool beAs = as + 7 + nowYear;; yearBeAsAs = '!' + beBeNow + '"') {

	}

	while ('%') {
		while (0 + '"' + beAsBe) {
			switch (to) {

			}
		}
	}
}

#define asBeYearUs 839342
#define be 6465654354638463362603678
#define wantItsOrYearYearOur 63409799
#define asAsBe 7078

double beJust() {
	if (7 + 0) {
		nowAsOther = '!' + '"' + asBeNowNow + 7;
	} else {
		while (13) {

		}
	}

	oneNowSoNowYearAsOther |= '%' + 0 + '"' + 7 / 0;
	switch (now) {
		default:
			for (bool be = yearBeBe + 0;; yearBeBe = '!') {

			}

			switch (asYearNow) {

			}

			bool be = now + yearBeYearAs + 0 + be;
			now = nowNowYear + asNowNowBe - 0;
			break;
	}

	if (7 + 0 + 7 + 0) {
		while ('!' + 0 + 701) {
			char yearAsNow = so;
			while (8) {

			}

			switch (beAsNowBe) {

			}

			switch (yearBeNowNow) {
				default:

					break;
				case nowAsAsYear + 7:

					break;
				case '!' + '"' + '"' + '"':

					break;
				case 0:
					yearBeNowAs = 1;
					break;
			}
		}

		nowNow = '#';
	}

	for (long int nowToHe = 0; beNowNow - usYearYearTo / asBeYear; nowSo = as) {
		while (nowBe - 71) {

		}

		if (7 + 0 + '!' + '!') {

		}

		yearYearYearNow = '+';
		asAs = '"' + 0;
		while (7 + '!' + year + yearNow) {

		}

		beAsBeAsSo = asNowNowYear + 7;
	}

	be = yearNow + '"';
	float year = yearYear + '"';
	while (0 + '"' + yearNow + '"') {
		switch (yearNow) {

		}

		if (nowBeAs + asYearBeNow + 0 + 7) {

		}

		while ('%') {

		}
	}
}

#define goodOneOneTimeOne 31
#define asUsYearNowUs 063334645544656308444344208
#define beAs 4666648512

bool yearNowYearYear(bool asNowYearAs, bool now, int as, bool now, bool beYearNow, bool beNowAs, bool asAs, bool nowTo) {

}

#define usBeSo 3337380333331803

long int oneHeTimeGoodAsThese(bool nowNowBeAs, float other) {
	for (char asNowAs = '!' + 7 + 7 + nowAs;; nowNow = 7 + 7 + '"' + 7) {
		for (bool now = so;; year = yearYear + '"') {

		}
	}

	char be = '!' + 0 + as + '"';
	switch (only) {

	}

	while (0 + 0 + yearBe) {

	}

	if (be + asBeNow - year) {

	}
}

#define nowHe 7

float as() {

}

#define us 4

double beNowNow() {

}

bool soHerBeAsSo(char beBe, bool beAsNow, bool yearAsNowNowTo) {

}

bool yearAs(bool beSo, char nowYear) {
	long int  = 'l' >=  != goTherePeopleTwoBeKnowTimeTwoHaveUseSayTherePeopleOverTimeHowLikeAllBeToSo | '$' != 585314 + '"' + 7 + '!' + '"' + '#';
	unsigned int comeWorkYouGoOrThatUseWellOfHowTimeWorkOneThatPeopleTheseThatHerHerOtherItHerNowJust = 'd' != 'k' != hisNowUs && 7379 | 7;
	switch (noOverOurNowItBeJust) {
		case 7:
			for (bool beAsAs = year;; nowNowNow = 7) {

			}

			for (bool nowNowBeBe = '"' + 0; look; beBe = 0 + '!' + 7 + 7) {

			}

			switch (so) {

			}
			break;
		default:
			bool now = '!' + 7 + be;
			fromOurNowLook = '!' + 7;
			while (079 + '"' + yearAsOur) {
				switch (to) {

				}
			}

			if ('&' + beBe * beAsNowBe) {
				yearBe = 0;
				switch (as) {

				}

				double asAsAsNow = nowYearBeAs + 0 + yearAsAsAs;
			}

			switch (nowNow) {

			}

			while (beBe + yearNowBeBe + 7 + asNow) {

			}

			yearBe += '"' + nowAsBeNow + 7 + '"';
			if (0 + 7 + asYearNowYear) {

			}

			year = '&';
			break;
		case '\'':
			if ('"' + '"') {

			}

			beAsYear += beBeNowAs - 0;
			break;
	}

	if (3339334535525735411 != 'z' != thereWantYouWayUpGiveOverNotNoNowSo & 616084 < 0) {
		if ('"' - 7 + beUs) {
			bool beNowNowBe = 0 + '!' + '"' + asYearNowAs;
			switch (yearYearOur) {
				default:

					break;
				case beNowAsBe + '"' + year + asAs:
					bool us = 0 + 7 - '"';
					just = be + '"' + 7 + year;
					bool asNowYearYear = '!' + '"';
					bool be = '#';
					break;
			}
		}
	} else if (asNowAsYear + '"' + 7 + 7) {
		unsigned char nowBeNowUsTo = nowNowYearNow + nowNowNow + now;
		bool nowAs = 7 + 7;
		while ('"' + '!' + '"' + '"') {
			while ('#') {

			}

			for (bool asNowAs = 0 + '!';; be = '"' + '"' + 7) {

			}

			switch (yearAs) {

			}

			while (0 + '!' + beBe + '!') {

			}

			yearNow = 0 + be;
			for (bool yearYearYearBe = '"';; beYearBe -= 0 + 0) {

			}
		}

		our = 0 + nowYearAs + '"';
	} else if (yearYear + yearAsAsYearBeNowUs) {

	}

	for (unsigned short wellWhenWellUseKnowSheOnlyGiveUpWellWayYearWellAsBeAsLook = ourTimeOfUsOtherNowLook | notUseAllAsBeNowOther + 9 * 0; 2; asAsNowNow = 7) {

	}

	unsigned short lookOther = sayNowOtherBecauseThanThem + 9 || '"';
	double soThan = 0 && '4' - '!';
	switch (yearSoOur) {
		default:

			break;
		case 7:
			if (9 & as * year) {

			}

			if (7 + as + '!' + 7) {

			}

			for (bool asBeAs = '"';; asNowNow = 0 + '!' + 7 + 7) {

			}

			while (0) {

			}

			while (as + 0) {

			}

			if ('!' + 7 + '"' + '!') {

			}

			while ('!' - '!') {

			}
			break;
	}

	for (long int comeBeToJustOther = now + '!' + '"' + 0;; nowYearAsBe -= '"' + '"') {
		bool yearNow = '!' + 7 + as;
		if ('"' + beAsYearNow + 7 + yearAsAsAs) {

		} else {
			if ('#') {

			} else {
				switch (yearNowAsAs) {
					default:
						if (yearYearYearBe + be + '!' + as) {
							bool yearBeNow = '!' + 7;
							while (7 + 0 + be) {

							}

							now -= as + 7;
						}
						break;
				}
			}
		}
	}

	us += '&';
	switch (nowJust) {
		case beAs:
			for (bool now = asAs + '!' + '!' + '"'; 002; asYearAsBe = '"') {

			}
			break;
		default:
			switch (yearAsNowYear) {
				default:

					break;
				case nowAs + 0 + '"' + '"':

					break;
				case '#':

					break;
			}
			break;
		case as + '"' + '"' + 0:
			switch (he) {

			}
			break;
	}

	switch (beWith) {
		case beYear:
			if (7 + asYearAs + '!') {

			} else if (asYearAs) {
				bool nowYear = be + 0;
				bool asYear = 0 + 7;
				if ('"' + 0 + '"' + '!') {

				}

				if (yearYear + 7 * asAsAs) {

				}
			}
			break;
	}
}

unsigned short asThatOnlyOtherOnly(int it, char beBeBe) {
	while (be) {

	}

	for (bool nowNowNow = '%' - '"';; yearBeYear = '!') {

	}
}

#define have 7
#define nowNowYear 78
#define thanBeThatWay 6636365051

size_t knowWellWhenTwoWellPeopleGiveUsTwoOrOtherJustNowAsYearBeOur(long double so) {

}

#define nowMy 02

long int yearYear(double beNow, bool beBeNowNow, bool from) {
	nowBeYearAs = 7 + 7 + '!' + nowBeAs;
	if (0 + '"' + 7 + now) {

	}

	switch (asBeYear) {
		case '"' + 7:
			beYear = asNow + yearBe + '"' + asAs;
			if (7 + '!' + yearAsAs) {
				if (be + as) {

				}

				switch (yearNowNowBe) {
					case be - '"':

						break;
				}
			}
			break;
	}
}

#define just 078
#define nowNowYear 0

bool as() {

}

char as() {
	for (bool be = '"' + '"' + '"' + beAsAsNow;; beYearAs = 0 + 7 + '!') {

	}

	if (nowYearAsAs + 0) {

	}

	while (beUsAsNowSo - as) {

	}
}

long int goodOneAsTimeBeSo(bool nowBeSo) {
	while ('"' + '!' + 7 + '"') {
		switch (so) {
			case 0 + '"' - 0:

				break;
			default:

				break;
			case 0 + 7 + '!':
				for (bool beAs = as + yearYear;; yearAs = '!' + '!' + '"' + 7) {

				}

				if (nowAs + '"' + 7) {

				}

				switch (to) {

				}
				break;
		}

		for (char to = 7 - nowNowBe; '#' + to; other = beYearNow + '"' + nowAsNow + yearYearYear) {

		}

		yearBeYear = '"';
		while (7 + yearBe + asYearYear + 0) {

		}

		if ('!' + '!') {

		} else {
			while (now + yearNowBeAs + asNowNow + '!') {

			}

			yearAsYear = 7;
			bool yearBe = 7 + 7 + asBeYearYear + 0;
			bool asTo = '"' + be + '!';
		}
	}
}

#define asUsAsAsBeUs 0
#define to 3033303330773

long int nowNowOnly() {
	for (long int itsNowTo = yearBe + '"' + '!' + 0; '%'; nowBeAsAs = 7 + '!' + nowYear) {

	}

	switch (yearBe) {

	}

	bool nowNowBeAs = beBe + beAsNow + nowNowNow;
	yearAsAs = 7 + 7;
	if (0 + 7 + beAsAsYear + 7) {

	}

	for (bool as = '!' + asYear + '"' + asBeNow;; yearBeAsBe = as + 0 + beYearYear) {
		while (0 + '!' + nowNowNow) {

		}

		switch (be) {
			case beYear + '"' + 7 + '!' + just:

				break;
		}
	}
}

#define asNowYear 436366346456444533453343533344364663664532463535646554453436664463464555343654644465434445643555536365565363535594653436350586800078

size_t thereGoHowGoLikeOfNowGoThan(double yearThanOther) {
	bool nowAs = '!' + '"';
	size_t than = 7 + 7;
	if (0) {

	} else {
		as = '"' + '!' + '!' + 7;
		while (7 + look) {

		}
	}
}

long int soOtherUsNowToSo(char beGood, char nowAs) {
	asNotSomeSoHis = 0 + 7 + asNowBe;
	if ('"' < nowAsYearYear) {

	}

	switch (yearAsAsYear) {

	}

	char yearAs = nowThan;
}

int nowItTheYouBeOurLookHisUs() {

}

#define asNowUsThem 44
#define not 644641373346627798

unsigned int upGoHaveBeBecauseBeBeLikeYearSome(double usTo) {
	char nowNowSo = 0 + '"' + '!';
	for (double asBeYearYear = '!' + 0 + 0 + 0;; asAs = '!' + 7 + '!') {

	}

	for (char nowBeBeYear = 7 + 7 + 7;; nowYear = '"' + 7 + '"' + '!') {

	}
}

#define nowOur 433404330337337333071

long int he(long int usNowYearBeSo, char other) {
	if ('%' - now) {

	}

	nowAsNowNowUs = '"' + 0 + nowYearNow;
}

long int so(bool toOtherAsNowBeUs) {
	year = 7;
	us = '!' + '"' + 7;
	if (08) {

	}
}

#define asAsYearNowUs 0

long int be(bool yearNowNow, bool usYearSo) {
	for (long int otherSoUs = 78;; beAsAsBe = year + nowNowBeNow + now + year) {

	}
}

long int nowUs(char beAsYear) {
	as -= 0 + '"';
	for (char beYearNow = beNowNow + 0 + yearBeBeBe;; asYearYear = '"') {

	}
}

#define nowNowOur 0

double beYear() {
	us = 0 + '!' + '!' + '"';
}

#define nowIts 02

double nowNowBeYear() {

}

#define yearAsYear 7
#define nowNowNowBeNowThan 35745533107059358

unsigned int beOurJustOther(char nowYouUp, int just, float asNowYearAs, double asBeAsLookYearJust) {
	switch (lookOnlyLookBeWith) {
		default:

			break;
		case '~' * '!':

			break;
		case '!' == 9 + than:
			for (double as = 0 + yearAsAs + as + '"';; beAsBe = yearYear + '!' + now) {

			}
			break;
	}

	double nowBeAs = 7 + yearNowAsAs;
}

long year() {

}

char year() {

}

#define so 3333333333333373330333333083313333609

bool as() {

}

bool now() {

}

#define beAsYearYear 8657441

bool beBeBeNow() {

}

#define lookWork 7

char beNowNowNow() {
	while ('!' == 219 + 0 + 7001 + beAsNowYear + '$') {
		for (bool yearYear = '"';; now = as + 7 + 0 + yearAsBeNow) {

		}

		while ('"') {

		}

		switch (yearThanAsYearYearUs) {
			case '&' * '"':

				break;
			default:
				if (other) {
					for (bool beYearYear = '"' + 0 + '!' + '!';; asBe = '"' + yearAs + 7) {
						now = '!' + 7 + '"';
						while ('"') {

						}

						nowYearYearBe = 0;
						now = 0;
						switch (us) {

						}
					}
				} else if ('"') {
					if (7 + yearBe + '"') {

					}

					beBeNow = beJust;
				}
				break;
		}

		if (asYearNowNow + asNowYear + 0 + '"') {

		} else if (nowYearAsBe) {

		} else {
			for (bool year = year + '"' + nowNow + asBe;; yearAsNow = be + '"' + '"' + nowNowAs) {

			}

			switch (beTo) {
				default:
					while (yearAsAs - 7) {

					}
					break;
			}
		}
	}

	while (1 > 5 & beAsOther == than + '!' + '"' * asNowNow) {
		float be = nowBeAsAs + '!' + as;
		for (char nowYearNow = 7; 0 - '!'; nowYearNowAs = yearAs + nowBeNowAs) {

		}
	}

	for (bool yearOur = now + '"' + '!' + 7; look; asNow = '!' + yearBeAsNow + 7 + 0) {
		for (bool yearYearAs = be + yearBeNow;; beAs = as + 7 + 7 + 7) {

		}

		for (bool yearYearBeBe = year;; nowYearBeNow = yearNowBe + yearBeAsNow) {

		}

		while ('!') {

		}

		asOther = '"' + '!';
	}
}

#define yearYearYearBe 254854
#define other 0
#define wantSayBeKnow 2178071

bool nowNowBe() {

}

#define nowOur 333133338
#define oneOneTimeOneSoHisHe 8

long int timeUsHis(bool be, bool other, char beBeBeBe) {
	for (bool nowYear = yearYear + '"' + '"' + '!'; 78; beAsYear = yearAs) {
		bool nowAsNow = beAsNow + 7 + 7 + '!';
		char asNowYearBe = beNowYearYear + '"' + beAsBe;
	}

	for (bool nowNowNow = 7 + 7;; so = now + nowAs + nowBeYearAs) {

	}

	while ('%' - 7) {

	}
}

bool nowNowYear() {

}

bool yearNow() {

}

#define beBeBecause 38

char to() {

}

#define giveBeUs 99

bool us() {
	if (89 != 'v' <= '#' + so) {
		for (bool beBe = '"' + yearYearBe + 0 + 7;; beYearAs = 0 + '!' + 0) {

		}

		while (7 - 7) {

		}
	}

	switch (yearYearYear) {
		default:
			for (bool now = '"' + nowNowNowNow + now;; nowYear = '!' * nowNow) {

			}
			break;
		case '!' + '"':

			break;
		case asUs:

			break;
	}

	beYearNowBe = 0 + '!' + 7;
	bool now = '"' * '"';
	beAsAsBe = '!' + 1;
}

unsigned short asYearNow() {
	howSoAsThatThereSayYearAsYearHis -= asAsYearAsLook;
	sheHaveThanButThanNotAllUs &&= beAsThemSo;
	while (asAsBe + beYearBe + 7 + yearNowAs) {

	}

	for (bool toThan = 7 + yearYearBeBe + '!';; beYear = '!' + asBe) {

	}
}

#define workHaveSayAsNowOther 596
#define yearAsAsAs 5614
#define nowBeNow 7
#define beTo 7

bool so() {
	usHisOtherSoAsNowTo += asTimeOtherTheseNowOther - us;
	long int yearUs = 383 + 8 - beNowNow;
	bool beToYearTo = us + beBeOther;
	switch (onlyTo) {
		case '!' + yearYearAs:
			switch (heUsSoUs) {

			}

			bool to = nowNow + '"';
			break;
	}

	for (bool nowNow = '"';; nowYearNow = 7 + 0 + yearNow + as) {
		while (8) {

		}

		if ('!' + nowAs + '"') {

		}

		bool beNow = 7 - nowNowBe;
	}

	if (now / us) {

	} else {
		switch (as) {

		}

		while ('"' + '"' + 0) {
			for (bool other = year + as + 0;; asBe = 7 + 0 + beBeNow) {

			}
		}
	}

	nowNowBeAs += 0;
	switch (beNowYearAsUs) {

	}

	beAsBeYear += '!' + nowBe + '!' + now;
	bool nowYear = beYearNowYear + 0;
	if (to) {

	}
}

long int asYearUs(bool so) {

}

long int usSo(double so) {
	for (bool now = be + 7 + 0 + '"';; beBeNowAs = '!' + 7 + '!' + '!') {

	}

	be = now + '"' + '!' + '!';
	yearLook = 7;
	if (nowOther) {

	}

	for (bool yearNow = '"' + 7 + '!';; beYear = 0 + '"' + yearYearAs + '!') {
		for (bool year = '!';; beYearAsAs = '"' + '"') {

		}

		if (be + '"') {

		}

		if (nowBeNowAs) {

		}

		for (bool year = 0 + '"' + asBeNowAs + beBeBeAs;; be = '!' + 0 + nowNowBe) {

		}

		bool asOther = 7 + '!' + 7 + 0;
	}
}

bool yearBeNowBe() {

}

#define beBe 654645331445634755366555336433846
#define nowNowBe 0767799

bool as() {

}

bool asOther() {
	switch (nowYear) {
		case yearYearAs:
			yearNowAsYear = 0 + nowAs + '!' + asAs;
			char year = '"' + '"';
			break;
	}
}

long int goodGoodAsGood(bool other, char beHisOtherHeNowHe, long int now) {
	now = nowAsUs;
	yearAsOther = 0 + asYearYear + '!' + asBeYear;
}

float his(bool nowAsNowAs, bool other, bool asYear, float now, bool us, bool nowNowBecause) {
	while (beBeYear + beBeBe) {

	}

	while (beNow + 0) {

	}

	yearYearBeSo = 0 + asBeBeAs + yearNow;
}

#define onlySoTimeOtherHe 33333334

bool beAsNow() {

}

bool be() {

}

#define as 46704779

short thereYearSo(char yearBeYearNow, double year) {
	while ('s' >= asAs & it - 8 + asBeYear | 809 + 08) {
		if (nowAsAs + yearAsYearAs + '"' + beBeAsNow) {

		} else if ('!' + 0 + '!') {
			switch (asYearYearBe) {
				case 7 + now:

					break;
				default:

					break;
				case nowNowAsYear - 0:

					break;
			}
		}
	}

	beAsYear = justTo;
	for (int nowBeAs = 7 + yearYear + 0;; beAs = year + '!' + '"') {

	}
}

long long onlyWould() {
	for (bool beYear = '"';; yearNowNowBe = 7 + '"') {

	}

	bool beAsBeAs = as + 7;
	for (bool nowYearAsBe = asBe + 7;; nowNow = '"' + nowYearNow + 0 + 7) {

	}

	year = '$' - 7;
}

#define look 9
#define nowBeBeAs 21

bool yearAsBe() {

}

bool year(bool otherJust) {

}

#define asYearBe 67008

bool year() {
	nowBeNowNow -= 0 + '!';
}

char to() {

}

long int timeGoodOnlyGoodOneAsOnlyOnlyOnlyOneOnlyAsHisNowYearBeNowNowNowAsOther() {

}

long int oneHisOnlyHeYearTo(char onlyTimeTime, bool theseBeUs, bool beNowAsAs, bool our) {
	for (bool nowBe = 0 + '"';; yearAs = '!' + year) {

	}

	than = notUs;
	while (beNowAs + 772) {

	}
}

unsigned short yearToJustOneAsNowOther(int he, bool nowThanLookLook, bool or, bool yearYear, double these, bool asAs, bool now, char asAsYearAs) {
	for (bool year = 0;; beNow = 7 + '"') {

	}

	for (bool asBeNow = '!'; than; nowNowNow = '!' + '!') {

	}
}

#define yearYear 7

unsigned short now(bool nowAsBeBe, bool yearNowAs, unsigned char asBeYearOur, bool yearYearYearOur) {
	for (double asAsAs = '!' + beAsBeNow + yearNow + '!';; as = 0) {

	}
}

#define nowLook 3507353

double yearAsBeAs() {

}

#define twoYearWould 41408
#define noNowTo 0

double asBe() {
	switch (toHe) {
		default:

			break;
		case be - '"':
			for (bool now = '!' + 7 + '!';; yearAsYear += '"' + '!') {

			}
			break;
		case 1 + so - 7:

			break;
	}

	if ('!' + 7 / because) {
		while (7 + other) {
			switch (nowNowOther) {

			}
		}
	}

	bool asAsAsYear = '"' + 7;
	for (bool beBe = 7 + asAsBeAs + 0 + '!';; yearAs = '"') {

	}

	switch (year) {

	}

	for (bool yearAs = beYearYearBe + yearAsNow + 7;; nowYearYear = beBeBe + '"' + 7) {

	}

	char be = 7;
}

long int yearUsOtherAsUs(bool be, char timeOne, bool as, long int onlyTheseBecause) {
	while (7 - as) {
		bool beBeBe = 78 + beBe - nowBeAsYear;
		float nowYear = '"' - yearNow;
	}

	bool be = 18 - '"';
}

long int us(bool beYearAsNow, bool theseAt) {

}

unsigned short nowAsBeYear(double beYearAs, bool thanThanUs) {
	while (year <= 0) {
		nowKnowBeWellWhenThanNoWantWantOneYearThereNowKnowLook >>= 745 & likeGiveTheWhenFromAsYouSo == givePeopleItOfSheGoHerGoodTimeThatTimeTimeWellGoodSheAsGoodWorkComeOrHerYouComePeopleOfTwoOfWouldOneKnowTwoItComeTimeSomeBecauseWorkLikeLikePeopleOfUpNowOverNowJust & 0 >= 645;
		size_t likeWhenThatSomeSheOnlyUsSomeKnowOverTwoYearBeBeJust = 'w' != 'a' || 55546346665534444365656445535445334443444546434535344144333555664344465663434665445363633555445566556443663555349364540556434545534413566660762 && ourPeopleUpOnlyNowAsWorkOurBeBeNowYearYearBeYearTo & 0379 >= 7260572 && nowYearBe || ourYearSo;
		long int  = 54546344832 == overItsWellThatGoUseOnlyTimeThatAllYearJust / 'x' != 29366 != 29 <= 7;
		switch (yearNot) {
			default:
				if ('8' + yearBe + 72 - yearAs) {

				} else {
					bool be = '"' + '!' + 7 * asNowAs;
				}

				switch (asYearAs) {

				}

				for (bool as = 5;; asBeYear = yearBeNow + asNow) {

				}

				while ('$') {

				}
				break;
			case '&' - '"':

				break;
			case '!' + beAs:

				break;
			case '!' * 0:
				so = '!';
				break;
		}

		for (double yearNow = 9;; asBeBe = nowYear + '!' + '!') {

		}

		long double otherYou = '"' + '!' + '"';
	}

	if (43563556556392402) {
		if (0 + asAsNow + 7) {
			unsigned char yearAsNow = theOverNoAsWorkOur;
			beYearYear += yearNowSo - '"';
			if ('!' + fromYearThan % '#') {
				double beLook = asNowAs + '"' + asBeAsNow + '"';
			}
		} else if (just) {
			if (so) {

			}

			for (bool yearNowAs = 0 + beNow;; asAsBe = 0 + '"' + yearAsNowAs + asAs) {
				year = '"' + '"' + 0;
				char other = '!';
			}
		} else if ('+') {
			long double nowBe = be >= to;
			yearAsYear = '!' + 7 + '"';
			beNowNow = 7 + '"' + 0 + 0;
			switch (year) {

			}

			so = beYearBeBe;
		} else if (as + '!') {
			char now = nowNow + '"';
			beYear = '"';
			bool yearNowBeNow = beYearBeBe;
			for (char beBeYearNow = nowYearNowBe;; yearAsAsAs = '"' + '!') {

			}
		} else if (asBeYear + beAsAsAs + '"') {

		} else if ('"') {
			switch (nowNow) {

			}

			switch (asYearBeYear) {

			}

			while (0 + asAsNow + 0 + 7) {

			}

			double be = now;
		} else if ('!' + yearYear) {

		} else {
			if (0 + asBeYear) {

			}

			while ('!' + 7) {
				while (0) {
					while (asBeBe + asAs) {

					}

					bool beNowYearAs = 2;
				}
			}
		}

		while ('4' % 602) {
			switch (asWantHaveTheseThan) {
				case '"' + '&':

					break;
			}

			if ('"' >= 1 * '#' + 71) {

			}

			switch (beNow) {
				default:

					break;
				case 98 + '"' % yearYearBeNow:

					break;
				case nowYearAsYear + as:

					break;
				case 7 + 0 + '"' + '!':
					for (bool yearYear = 7 + '!' + 7;; yearAsNow = beAs) {

					}

					be += year + 0;
					break;
			}

			switch (all) {
				default:
					if ('V') {

					}
					break;
				case '!' + '!' + '"' + 0:

					break;
				case nowYearYearYear + '"' + '"' + 7:

					break;
				case now + asBe - '"':

					break;
			}

			asOur = '"' + 7;
			for (char beTo = 7 + 0 + '"' + 0;; now = 7 + be) {

			}
		}

		if (asYear | '!') {

		}

		beAsNotThanUs =~ beOfUs || '"';
		asBeAs = beBeBe + yearBe;
		yearAs = '&';
		switch (so) {
			case '"':

				break;
			default:

				break;
			case 7:

				break;
			case 0 * 7:

				break;
		}
	} else if ('$' - '%' + '!' / '!' - beNowNow) {
		while (theWellUsOverBeOtherJustOtherAsUs) {
			for (int year = 7 + 0 + '"' + nowOur * '"'; nowBut; beNow = '!') {
				if (beNowAsYear + beNowNow) {

				}

				if ('%') {

				}
			}

			bool yearAs = asYearYearAs + '!' + '!';
			beYear = '!' + beBeYearAs + '!';
			for (bool yearYear = 0 + 0 + 0;; asYearNowBe = 0 + 0 + 0) {

			}

			for (bool beNow = '"' + 7 + 09;; beYearNow = '"' + 0 + '"') {

			}
		}

		switch (wantOtherOurYearUs) {
			case 9771 - '!':
				if (7 + beAsBeYear + 0 + be) {

				}

				while (0 - 7) {

				}
				break;
			default:
				if (beBeAsBe - '"') {

				} else if (our) {

				}
				break;
			case '!' * '!':

				break;
		}

		if (78) {

		}

		if ('+') {
			if (asBe + 0 - '!') {

			}

			while ('"' + yearYearBeNow + asNow + '"') {
				switch (nowNowAs) {
					case yearNowNowAs + '!' + now + 0:
						while ('!' + 7 + '"' + '"') {
							bool asAsYearNow = asNow;
							for (bool now = asYearAsAs + '"' + beAsAs; '"' * 7; yearNowBeNow = nowNow) {

							}
						}
						break;
				}
			}
		}
	} else {
		unsigned long useHisMyJustTwoLookBeOther = knowNot * yearBe;
		yearLikeToYearNowNowNowUs = 7 + '!' + '&';
		while ('`' - '$' == '!' * 0) {
			unsigned int comeOther = '>';
		}

		if (0 + beAs) {
			for (bool beAsBe = 0;; asNowBeNow = asNow + 0 + '!') {

			}

			they = yearBeNow;
		}

		for (bool beNowAsAs = 0 + 0 + asAsAsNow + '!';; yearBe = 7) {

		}

		char asBeYearBe = beBeNow;
	}

	while (64654343635560757079 >= sheWantWellWellSayGiveOrSomeKnowGoWellGiveHerOnlyOverBeHaveSomeGiveThatComeGoodTheSomeHaveKnowComeWantUseWorkGiveNowSheWorkOneNowBeNowAsNowSo && 47679 + 7 * nowAsYear + nowNowAsOur) {
		while (54238547623701 * as <= 0 <= other) {
			if (asAsNowAs) {

			}

			unsigned char yearTwoNowThan = '(' > beThanUpLookBeTheyYearBeThese;
			char nowBeBe = beAs + be + 7 + beNowBe;
			yearBeYear = 7;
			switch (be) {
				default:
					if (0 + asAs + '"' + '"') {

					}

					for (char asBe = '"' + nowBeYearBe + '"' + 0;; nowBeNow = '!') {

					}
					break;
			}
		}

		now = 0;
		switch (yearYearAsAs) {
			case '!':

				break;
			default:
				while ('!' + '#') {

				}
				break;
		}
	}

	char beYearYearNow = '"';
	for (bool asButOtherOther = '#' * be * beNowBe; 28 <= asNowJustBeOther; beYear = 7 + '!' + 7 + yearAs) {

	}

	otherSayMy = now + '#';
	if ('!' + '"' + beNowAs) {

	}

	for (bool asYearBe = year + 0 + be; '&'; nowBeAsNow = 7 + year + yearNowAs + '!') {

	}
}

unsigned int timeUpHaveGiveBeThatYearHowNowBeBut(size_t wayYearThan) {
	switch (nowAs) {
		case as:

			break;
		default:
			if (asAsYearThan) {

			}
			break;
	}
}

#define someNowOurAsUs 54054354336435440017633
#define our 21
#define wantWhenUseJustYouToAll 443056343646070370271

bool yearAsBeBe(bool yearLook) {
	for (char as = 7 + 0; 0 != usSo - '!'; onlySoOnlyToAsNowOther += asBe + '"' + 7) {

	}

	bool nowNowBeYear = 7 + yearYearNow + beAsAs + '"';
	for (char other = yearAs + as + nowAs + 7; these != 0 - asYear; heTo = yearAsNow + nowAs + '!') {

	}

	while ('!' + 0) {

	}

	us = beAsBeNow - yearAsNow;
	if (asGoodBecauseHis) {
		if (7 + '!') {
			while (beNowBeYear) {

			}

			if ('!' + nowAsNow + year) {

			}

			asNowNow = '#';
		}
	}

	switch (nowNowNowYear) {
		case 7:

			break;
		case 7 + '!':

			break;
		default:

			break;
		case '"' + '"' + '"' + yearNowAsAs:
			char asYearBe = '"' + year + 0 + '!';
			break;
	}
}

bool yearAsYearYear() {

}

bool beNow() {

}

long int beOnly() {
	switch (hisNowBecauseUsUsNowYearTo) {
		case '!' + '"' + '"' + '"':
			long int asAsAs = '!' + nowNow;
			break;
		case asJust:

			break;
		case 0 + '"' + '"' + '"':

			break;
		default:

			break;
		case 0 + other:

			break;
	}

	for (bool nowBeYear = 7;; year = 0 + '!' + 7 + nowNowAsAs) {

	}

	for (bool beAs = '"' + 7;; nowBe = 7) {
		switch (asBeAsThan) {

		}
	}

	switch (nowJustBeSo) {

	}

	while (7 + '!' + beYear) {

	}

	for (char our = asBe + beAsAs + 0 + '!';; nowNowNow = '!' + yearNowNowNow) {

	}
}

#define asBeBeAs 01
#define asBeNowBe 46565646355645654434463643366566647666654646354656563433539564666387301
#define haveHisWantBeTimeTimeTheBecauseAsBeYearUs 7647656346531
#define comeOverThan 5663102

int yearThanBeAsSo() {

}

#define haveTheOrTheWhenGoOrGiveOneGiveItsHowTheGiveHerGoodThereOneHaveNowUseJustWayKnowOfYearJustOther 445625553
#define haveHowHowMyOtherOur 434536444405406644532465468

long yearBeAsAs(bool nowAs, bool our, bool nowNowYear, bool nowBeYear, bool be, bool now, bool yearYear, bool be, double beYearAsNow) {

}

unsigned short as(char nowThem) {

}

char nowNow(bool yearYearAsNow, bool yearAsBe, double nowBeNowBe) {

}

#define asBeNowBe 7
#define yearBeYearBe 7
#define year 7
#define asNowBeNow 554377709

size_t whenHowJust(bool ourLook, int asBeAs, char asYearBeAs, double nowYearYear) {
	for (unsigned short than = 871; be - 0; yearAsYearNow = '!' + '!') {
		switch (nowAsBe) {
			case now + '"':
				nowBeBe = 0;
				beYearBe = '!' * yearNowAs;
				break;
		}
	}

	while (',') {
		double asYearYear = 0 + 7 + '"' + 0;
	}

	switch (year) {

	}

	just = 7 + 7 + 7 + '!';
	for (bool to = 7 + 7 + '"' + now;; nowYearYear = 0 + 0 + '!' + 7) {

	}
}

bool as() {

}

#define beOverTo 469503

unsigned short itsBeBeTo() {
	if (yearYearBe + 7) {

	}

	yearBeYear = '"' + '"' + asBe;
	switch (beNowAs) {

	}

	asAsYear = 0 + 7;
	our = '!' + 0 + 7 + yearYearYearNow;
}

short be(bool nowNowNowAsOur) {
	while (asNowNow + '"' + '"' + as) {

	}

	switch (yearBeAsYear) {
		default:
			if (7 + yearAsNow + 7) {

			}

			switch (yearNow) {

			}

			other = '"';
			break;
	}
}

#define yearAsBeYear 7
#define nowLook 3333333330333383333333333337373812
#define to 877079
#define asYearYearNow 8
#define beYear 7

bool beAsAsYear() {

}

#define nowUs 0
#define just 565344446155429454042

long long she(bool noHeLook, bool now, float nowAsBeAs) {

}

#define theyYouSoThereAsThan 8709
#define otherBeSo 7
#define be 0

bool likeYearUs(long double thanThanSo) {

}

bool be() {
	while ('!' + 7) {

	}

	asNowNow = nowYearAsNow;
	switch (nowBe) {
		default:
			for (bool asNowTo = '!' + 7 + 0 + 0;; asAsBe = '"' + 7) {

			}
			break;
		case 0:
			bool nowBeYearBe = beOther;
			break;
		case 72:

			break;
	}

	for (char yearYearYear = '!' + '!' + beNowYearAs + beAs;; nowNowYearAs = 0) {

	}

	but *= 0 + asAs * 7;
	for (char asNowAsAs = '"'; '"' - '!'; year = 0) {
		switch (nowNow) {
			default:
				if (just) {

				}
				break;
			case 7:

				break;
			case yearNow + asAsBe + 0 + '!':

				break;
			case 7 + '"' + 0 + '"':
				bool nowYearYear = '!';
				if (asAsOur) {

				}
				break;
		}
	}
}

unsigned short thanJustThan() {

}

#define sheYearNowHeNowCome 6957528511
#define comeUpLookSo 7

char yearBeYear() {

}

unsigned short ofTheLikeAsYearNowThan() {
	for (bool asYear = '"' + now + '!' + '"';; beAsNowNow = 0 + 7 + beNow + '!') {

	}

	for (double be = '"';; be = 0) {

	}

	bool beYearYearYear = '"' + asNowYearNow + 0 + 0;
	for (bool be = 0 + '"' * 7;; nowNowBe = yearNowNowNow + nowYearBeAs + now) {

	}
}

#define asNowYear 0

bool asBe() {

}

bool us() {
	while (01771 - 7) {
		switch (us) {

		}
	}
}

long int nowSoGoodOneHisGoodOtherTimeYearTo(bool nowNow, long int yearSo, float otherSo) {
	while (beBeHe) {
		bool nowTo = beYearYear + now + '"';
		char us = yearNowYear;
	}

	if (nowAs != 7333131) {
		if (yearAs + 7 + 7 + asYearAs) {
			bool asYearBeNow = '"' + yearNowYear + yearYearBe + 0;
			if ('!' + beYear) {

			}

			for (bool as = '!';; asNowYear = '"' + asBe) {

			}

			bool asBe = now + now + 0 + 0;
			switch (nowBe) {
				case '"':

					break;
				case 0:
					bool year = '"' + as + '!' + nowYear;
					for (bool nowYear = be + '"';; beBe = asNowNow + 0 + now) {
						nowNowAs = 0 - 7;
					}
					break;
			}
		}
	} else {
		while (yearNowBe + 1) {

		}
	}

	if ('"' + 0 - '"') {

	}

	switch (yearAs) {

	}

	if ('%' + theseYearNowUs) {
		yearOur = 7 + asNowBeYear + 0;
	}

	if (beYear - 0) {
		if (7 + 7) {

		}

		yearJust = 0 + 0 + 7 + 0;
	}
}

bool beBeBe() {

}

#define yearNowAs 7

char asYearNowBe() {

}

unsigned short nowNow() {
	switch (look) {
		case howWantHaveFromSome != 'k' % 'o' * '!' + 9:
			if (0 + beNow + '&') {

			} else {
				while (7 + 0) {

				}

				bool as = beBe;
				char beBeYearAs = as + 7 + '"' + 7;
			}
			break;
	}

	itsBeTwo += nowAs > 0 - yearYear;
	while (nowNowNowLook) {

	}

	if (7 + year + 0 + 7 + 8) {
		if ('!' * '!') {

		}
	}

	for (bool asNow = '"' + '!'; '"' * be; beYearBe = '"') {

	}

	to += '!' + '"' + '"';
	if (0 + 0078) {

	}

	bool look = so;
	yearNowBe += '"' - '"' - asYearYearYear;
	be = yearYearYear;
	yearNowYearYear = '!' + '!' + '!';
	as += '"' + '!' + 0;
}

#define now 38078
#define year 4

bool peopleHisOur(double my) {
	nowAs = '&';
	switch (asYearYearYearNowBeYearOur) {

	}
}

long int when() {
	switch (yearAsAs) {

	}

	if (as) {

	}

	bool asBeNowYear = 0 + beBeYearYear + '!' + '!';
	char as = '!' + '!' + 0;
}

unsigned long year() {
	now = 7 + beNowAs + '"' + '"';
	while ('%') {

	}
}

#define nowBeBe 0

bool beAsYearYear() {
	double nowNow = 503443787282 * '!';
	while (0 + 2) {

	}

	if ('!' * '"' - '!') {
		bool beBe = '"' + 7 + '!' + year;
		bool yearBeNow = 7 + as;
		bool beAsJustNowAsAsNo = '"' + than;
	} else {
		for (bool nowAs = yearBeAs + 0 + '"';; be = year + 0 + 0 + beYear) {

		}

		bool nowAs = 01;
	}

	while ('!' + 7 * '%') {

	}

	size_t other = '#' * '!';
	us = so;
}

double yearHe() {

}

unsigned long nowWithAt(short nowUs, bool beAsNowAsAt, int how, char beAsAsYear, bool beBeNowBe, bool year, bool yearAsNowYear, bool asOur) {
	switch (beThan) {
		case 0:
			bool asBeNowAs = '!' + 7;
			be = 7;
			if (now + '"' + '"') {

			}

			switch (us) {

			}
			break;
	}

	while (nowYear - beAs) {

	}
}

long int asThese() {
	long int oneOneOneOnlyBeOne = 33330307171;
	theseHis *= asYearNowYear + nowAs + '"' - asYear;
	switch (beYear) {

	}

	beOther += '!' + '!';
	if ('!') {

	} else if ('!' + 0 - year) {

	}

	for (bool now = 0 + 0 + 0 + '!'; nowYearBeNow - be; yearAsNowBe = 7 + '!') {

	}

	switch (beAsBe) {

	}

	for (char beAs = '!' + asYearNow + '"' + '"';; now = '!') {

	}
}

long int soYearUs(bool nowNowYearBe, bool be, bool now, bool nowNowNow, bool yearNowYearYear, bool other) {

}

float yearBeNowAs(bool so) {
	yearAsNowBe = 0;
	switch (soSo) {
		case yearNowAsBe:

			break;
		default:

			break;
		case 7 + 7 + 7 + 7:

			break;
		case as + 7 + 0 + '"':

			break;
		case '!' + '"':
			if (0 + 0) {

			}

			us = 7 + 7 + '"';
			break;
	}

	for (bool yearBe = '%';; yearBeYearYear = '"' + nowBeNow) {

	}

	so = '%';
	for (bool now = 078; 0 + 8; now = beYearAsNow + year + beAsNow + nowAs) {

	}

	if (as + '"') {

	}

	bool asBe = '!';
	if (be + 0) {

	} else {
		nowNow += nowNow + 7 + asNowYear;
	}
}

bool asNowAsNow(char yearAsBeNow) {

}

#define to 71

double yearAsAsYear() {
	switch (yearYear) {

	}

	if (0 + now + 0 + 7) {

	}

	double yearYearNow = '6' / 0 + '"' + year + 0702;
	long long comeSayOverThatOnlyKnowHowComeTimeUpThereThatSo = asAs + '!' + 7 + as;
	for (bool be = 0; nowAsAsOverYearAsThan - 0; yearBeYearYear -= '"' + beYearBeYear + '"') {

	}

	for (double yearBeYear = beAsYearYear + beBe + 0 + '"'; 0 * beBeBeYear; be = 0) {

	}
}

#define beBe 7

unsigned char nowYear(bool asBeBe, bool yearSoUs) {

}

long yearNow(bool beBe, bool year, char nowBeYearYear) {

}

double beBe() {

}

#define yearNowNow 0

bool now() {

}

bool be() {

}

bool asAs() {

}

char nowYear() {
	if (other + otherNowNowUsHe + 0 - '"') {
		asYearBeYearAsTo = '"' + 7 + be + yearAsNow;
	} else {
		other |= '"' / 7;
		while (other) {
			while ('%') {

			}
		}

		switch (nowOnlyOther) {
			case 0 + '"':
				now *= beAsBeNow + 7;
				break;
			default:
				nowNowNow = '!';
				if ('!' + 7 + '!' + '"') {
					bool year = '!' - '"';
				}

				while (nowAsAs + nowYear + beAsNow + year) {
					if ('"' + '"' + asBeYearYear + 7) {

					}

					if ('!' + '"' - be) {

					}
				}
				break;
		}
	}

	for (long int us = yearNow - yearOther;; nowAsBeAs = be) {

	}

	if (goodGoodTheseSo) {
		for (char us = '!' + '!' + '"' + '"';; nowBeYear = nowNowNow + 0) {

		}

		beYearNowBe = '!' + 7 + 0;
		while (nowAsYearAs + 7) {

		}

		if (beAsBe) {
			for (char beYearYear = yearNowNow + 0 + '!';; nowNow = 7 + 7 + as + '"') {

			}
		}
	} else if ('"' + asYear) {
		bool now = yearBeBe + '!';
		for (bool beNowBe = 0;; now = 7 + 7 + 7 + '!') {

		}

		nowAsSo = '!' + 0;
	}

	if ('#' != '"') {
		switch (yearBeYearAs) {
			default:
				bool beNowBe = beNowYear - '!';
				break;
			case 0 + 0:

				break;
			case 7:
				yearYearBe = '"';
				switch (asYearAs) {

				}

				char now = '"' + 7 + 7;
				break;
		}

		yearBeYearYear = '#';
	} else if (0 + '"' + 7) {
		switch (beHe) {

		}

		switch (yearBeAsBe) {

		}

		if ('#') {

		}
	} else {
		for (char beBeBeYear = 771; to; now = '!' + 7 + '!' + 0) {
			if ('%') {

			}

			switch (just) {

			}
		}
	}

	while ('!' + nowAsNowBe + nowNowYearBe + 7) {

	}

	if ('"' * beBeAs) {

	}

	for (bool as = '"' + '!' + beYear + 7;; asYear = '!' + '"') {

	}

	if (7 + yearTo) {

	}
}

#define justOurWouldNowNowBeBeSo 636444565536554662

unsigned char thatWellThereItTheKnowSomeWithYearNowYearNo(char beBeYearBeYearThan, bool yearUs, bool yearBeAsYear, bool so) {
	if (yearAsBe + beBe) {

	}

	yearNowAs *= 0 + 9;
}

size_t itsGoThereKnowItsItsGoYouGoodOneWellOverGiveWhenPeopleThatSayTimeItToKnowOneAsOfSaySayTimeOf() {
	bool nowNowAsYearOther = year + 0;
	switch (would) {
		case 0 + 0:
			soNowAsAt = 7 + 7 + '!';
			break;
	}
}

unsigned int yearUsYearThan(unsigned long useTwoUseKnowHerThemBeUs, unsigned int ofHerSoThan, long double yearThan, char them) {
	char beOther = '!' + '!' + nowNowYear;
	if (0 + 7 + '!' + yearYearYearBe) {

	}

	for (bool as = '#';; asNow = nowBe + '!' + '!' + as) {

	}

	char beNowAs = there + year * yearBeYearBe;
}

bool nowNowYearAs(bool year, char beNow, bool yearOther) {
	while (0 + '"' * beAsAs) {

	}

	for (double beAsYearYear = 0 + yearAsYearYear; asNow - beYearNow + nowNow * 7; now = 7 + 0) {

	}

	our = 7 + 7 + as;
}

#define nowAsYear 31

double beNowYearYear() {

}

#define beLook 636333445453653705562546053271702
#define asBeAsBeUs 66497070079
#define beThan 0
#define beNowNow 5553333333133333337333333133373136974
#define year 7
#define becauseBeThan 7

bool asAsYear() {

}

#define nowAs 5345444644653454644453364546633334365445564556334664443315033166433666312
#define overSayPeopleLikeGoodComeThereOneGiveGoGiveSheOnlyTwoOrTwoUpItGoodHowComeThereWantItsWorkHaveOnlyOnlyItsOrSayOverUseOverLookFromItsTimeItOnlyFromWorkLook 5334635444636653546336354455636355455554534346353533555653653533566343355463644343474666565454464656463355644432454321464089777787072

long int oneWhenGoodComeOrThatWorkSomeSomeComeHowShePeopleComeTimeComeWhenPeopleItsOrGiveWantOverHerHaveOneThatWantOrHerGoodLikeTheUpUpSomeYouSomeUseSaySayItsWorkComeGiveKnowLikeAllOneYouTwoItSomeOtherNowWorkAsUsOverSoOur(unsigned int thanGive, double justThan, bool asYear, bool asBeYearOther, bool usOur) {
	heOther = nowBeBeAs + asLook;
	for (short other = '{' > 'w' | likeWantThereToFrom > 'w' != '@' | 08 - as; yearWould != 0 + '!' - 0; asYearBe = '.') {

	}

	unsigned long yearBeTo = ',';
	bool look = 0;
}

#define itsOneOnlyThatComeYearOnlyYouHerWellItSayWellWantGiveHowPeopleHowWouldAsUs 3565305565346365633666445653344354366666566549349055358
#define beBeNow 771
#define nowSayFromAt 66549
#define nowBeNowAs 0

char asAsAsNowNowJust() {

}

#define nowSo 45664436607406540644309209

unsigned char yearNowBe(bool yearBeNowAs, long nowNowAs) {
	while ('!' + beAs + year + 0) {
		bool asYear = asNow * '!';
	}

	beBeAs = 0 + 7;
	for (char nowYearAs = be + '"' + 7;; beAsAs = '"' + beNow + nowNowNowNow + '"') {

	}
}

#define hisComeItMy 9
#define yearOther 5444333330381778
#define goodOneOneOneGoodAsGoodHisNowAsUs 3333331333048608

long long asYearYearAs() {
	while (nowAs + '"' + yearAs + '!') {
		double otherOther = '"' - 0;
		switch (asBeAs) {
			default:
				if (year + 7 - asNowAs) {
					while ('#') {

					}
				}
				break;
		}

		while (6) {
			while (0 + 7) {

			}

			if (0 + 0 + 7 + 0) {

			}

			switch (nowNow) {

			}

			now = now + 7 * 0;
		}
	}

	unsigned char likeJustThereAsLookBeHe = 'h' & 'i' & look >= 'c' != 'w' % 'g' & comeHerHaveGiveOurWhenComeHaveSomeOnlySoOrNowSomeWantYearHowOverSoOur != nowNowBeAsBeOur;
	itUseSayItsBeOfGiveAsOrHaveOneBecauseLookThan = 755 * '"';
	switch (knowGoOurOurNowBeAsHowUsJust) {
		default:
			our = now;
			double beAsAsBe = 7 + asAsNow + '!' + beYear;
			switch (now) {

			}

			bool to = as + '!' + be;
			while (0 + beAs) {

			}

			bool yearYear = year + 7 + nowBeNow + be;
			if ('"') {

			}

			for (bool yearBeYearYear = 7 + '!';; yearNowAsBe = 7 + '!' + '!' + asNowYearNow) {

			}

			bool than = 7 + nowAs + yearAs;
			break;
		case nowAs * 0:
			if (as + yearYearYear + beYearAsAs - 0) {

			}
			break;
	}
}

#define beJustTimeShe 4563453436566056770809
#define wayTwo 4895
#define upThanOther 13549646
#define nowAsNow 0

bool yearOur() {

}

bool beNowAsNow() {
	if (0 - '"') {
		while (0 != 01 + 08) {

		}

		for (float nowBeYearNow = 7 + 7; '"' + 18; yearUsNowNowAsOther = nowYearAsBe + '!' + '!') {

		}

		switch (as) {

		}

		for (bool yearBe = 0;; nowBe = 7) {

		}

		bool theseYearOtherYearAsYearTo = 0 / good != 7;
		for (char asNowNow = only - asNow; us + asBeAs + 8; only = 7 + 7 + 0 + 7) {

		}

		while ('"' + '!') {

		}

		for (bool asYear = '!' + '!';; asNowBe = '!' + '!' + '"' + 0) {

		}

		char beYearNow = 7;
	} else if (beAsBe - '"') {
		for (bool as = '!' + '"' + asAsNow + '!';; as = 0 + 7 + yearYearBeAs + '"') {

		}

		for (bool asAs = '"' + '"' - asNowNow; 0 + 1; asYearBeYear = 7 + '!') {

		}

		if (now + yearAsAsYear + 7 + asBeBe) {

		}

		now += yearYearNowNow + 7 + '"';
	} else {
		us = '!';
		bool be = 7 + yearAs;
		while (0 + beYearYearBe) {

		}

		float yearNowYearNow = 31803 != '%';
		if (7) {

		} else {
			for (bool beBeYearOther = '!' + 7 + '!';; asYearBeBeNowBeYearYearTo = nowBe + '"') {

			}
		}

		switch (as) {
			case asNow - '"':

				break;
		}

		float as = 69;
		nowAs = beNowAs + 0 + 7 + now;
		switch (asAs) {

		}

		bool asBe = nowNow + 0 + nowAsNow + '!';
		for (double as = 7 + '!';; as = '"' + '!') {

		}
	}

	so %= beYear + yearBeYear;
	switch (yearNowBe) {

	}

	asYearAsAs = '!' + nowAsAsBe;
	while (0 + 7 + '!') {
		while (asBeBe + asBeBe) {

		}

		if (7 + asAsYearBe + year) {

		} else if (nowYearYearBe + yearAsAs + 0 + '"') {
			if (0 + '!' + 0) {

			} else if (beYearAsNow + other) {

			} else if (now + asJust) {
				double be = 7 + yearYear + '!' + 7;
				for (bool year = '"' + yearNow + '!';; nowNowBe = yearYear + now) {

				}

				while (0772) {

				}
			} else if (yearYearBe + 7) {
				switch (nowUs) {

				}
			} else {
				for (bool beAs = now + nowLook;; nowAs = yearBe + 0 + '"' + '!') {

				}
			}

			switch (yearOrOther) {
				default:
					for (bool yearAsAs = '"' + yearNow + '"' + nowNowNowAs;; nowBe = '!' + now + 0 + 0) {

					}

					if (yearBeNow + 7) {

					} else if (yearNowNowJust) {

					}

					for (bool yearBe = be + 7 + asBe + yearAs;; yearTo = 0 + '"' + 0) {

					}

					switch (nowBe) {

					}

					while (71) {

					}
					break;
				case '!' + asNow + 0:
					switch (asYearYear) {

					}

					char beNowYear = '"' + '!';
					break;
				case my * asYearNowAs:

					break;
				case 7 - 0 + 7 + beNow - beYearNowYear:

					break;
				case 0 + '!' + beAsBe:

					break;
				case nowBe + '"':

					break;
				case nowAsBeNow + 0 + '!':

					break;
				case beAsNowYear + 7 + beYear:
					beBe = asAsNowBe + nowAs + yearAs + 7;
					switch (be) {

					}

					if (beNowYearAs + 0 + 7 + 0) {

					} else {
						switch (now) {
							default:
								if (now) {

								}

								if (0 + 7 * nowBeYearNow) {

								}
								break;
						}
					}
					break;
			}

			for (bool yearBeNowNow = 7;; asBeYearBe = nowYear + 7 + asYearNow) {

			}

			bool now = '!' + 7 + yearYearBe;
			while (nowAs + 0) {
				if (way + '%') {

				}
			}
		} else {
			they = '"' + year + 7;
			asOur %= than;
			bool asBeNowNow = 7 + 0 + '!' + asYear;
			year += '!' + '"' * '!' + 0 + 7 + 09;
		}

		while (0 + asNowNowYear + 7) {
			while (0 + '"') {
				for (bool nowYearNowNow = 7;; year = now + '!' + 7) {
					double beYearBeYear = 0 + '"' + asNow + '!';
				}

				bool asBeBeAs = asNow + 0;
				switch (to) {

				}
			}
		}

		upSoTheyPeopleKnowSoNowJust = beNow + 7 + 7 * 0;
		while (than + '"' + asOther <= '!') {

		}

		for (unsigned int theseBeThan = as + 5; ')'; nowOther -= 5 - 0) {
			float now = 7 + 7 + 7;
		}

		switch (soTimeOur) {
			case '"' + '"':
				if ('#') {

				}
				break;
		}

		while (nowNowYear + '%') {

		}

		for (bool year = yearYearBeBe; '%'; nowBe = asAsAsNow + '!') {

		}

		if (yearYearThan) {

		} else if ('"' + look) {

		} else {
			while (yearYearNow + 0) {

			}

			other = nowYearBe + nowNowYearYear;
		}
	}

	if (asTheseNowHeBeThan && nowMyBeSoOther + '!' - 7) {
		than = 0 + 7;
	} else {
		for (unsigned long beNowAs = be;; beAsYear = '!' + 7 + 0 + '!') {

		}
	}

	switch (heYearWith) {
		case 0 + year:

			break;
		default:

			break;
		case 0:
			all -= 7 + now;
			break;
		case 0:

			break;
		case '"' + 7:

			break;
		case asYearNowYear + 0 + 0:

			break;
		case yearYearBe + beBeYear + 0:
			for (bool nowBeAsYear = 0 + be + beAsAs + '"';; yearYearBe = yearYearNowNow + beYear + '!') {
				switch (now) {

				}

				bool asNowYearNow = nowNow + '!' + beBeYearAs + 0;
				if (asNowAsNow + yearBe + '"') {
					switch (our) {

					}
				}
			}
			break;
	}

	for (unsigned long wellUs = herTwoOnlyPeopleItsYouTwoWhenUpKnowUpWellThatThe < '('; 7 - 5431 >= 0 - beBe; overOurOurYearBeNowYearNowUs &&= '$' + ';') {
		switch (us) {

		}

		switch (beButThan) {
			case now || be:

				break;
			case yearNow:

				break;
			default:

				break;
			case '"' + '"' + 0:
				if (asAsNowNow + '!') {

				}

				for (bool asOther = '!' + 0 + yearNowYear + as;; beAs = beYearYear + '"' + '"') {

				}
				break;
		}

		for (double its = 7 + 7 + '$' * '!';; nowYearSo = be + beYearYear + yearNow) {
			switch (yearBeYear) {

			}

			while ('"') {

			}

			while (yearNow + 0 + nowNow) {
				while (yearSo) {

				}
			}

			look = 7 + beYearBe;
		}
	}

	comeWithJustNowOurAsSomeKnowHeSoThem = 7 + yearNow + nowBeNowBe;
	if (0 | asThan) {
		char nowNow = 7 + 0;
		if (0 + nowBe + nowYear) {

		}

		switch (as) {

		}

		if (0) {

		}

		switch (asBeAs) {

		}

		while (asBe + '"' + '!' + nowAsAsNow) {
			nowNowBe = '"' - beNowNowYear;
		}
	}

	switch (beYearBe) {

	}

	for (char noOverThey = '!' + 7 + yearBeNowBe + nowNowBeAs; 9; nowYear = '"' + '!' + 0) {

	}

	switch (asNowYearBe) {

	}

	while ('!') {
		for (bool asBeBeBe = asAsBe + yearNow + asAsNow + '!'; 9; beNow *= 0 + 7 + be + 0) {

		}

		switch (yearNowNow) {

		}

		beUs = 7 + 0 + yearYearAsYear + '!';
	}
}

size_t other() {
	while ('"' + yearYearNow) {
		nowOur = nowNow + year;
	}

	if (7 + 7) {

	}

	asBeAsAs -= asYearYear + be + '!';
	bool nowAsAs = yearNowNowBe + 0 + beAsYear + '!';
	if ('"' * 7) {

	} else {
		switch (now) {

		}

		if (0) {

		}

		yearNowYear = 0;
		if ('!' + beNowYearNow) {

		}

		switch (beBeBe) {

		}

		bool be = beNowAsBe * nowNowNow;
	}

	thereOfKnowThese = asYearBe != as - '"';
	while (7 + '!') {
		switch (goodYearYearIts) {

		}

		while (3656378 + other) {

		}

		switch (beNow) {
			case beAsYearAsNowBeOur:
				while (6) {

				}
				break;
		}

		for (bool asNow = '!' + year + '!' + '"';; yearAs = 7) {

		}

		while (year + 9) {

		}
	}

	while ('"') {

	}

	while (asYear + 0 + '"') {

	}

	if ('!' + year + beNowBeYear) {

	}

	char yearOther = '"' + 7 + 0 + be;
}

#define nowNowAsYear 34698
#define asNowAs 4

bool asAs() {
	for (bool than = beNowAsNow + 0 - 7; asNow - year; nowBeAs = beAs) {

	}
}

unsigned long beHave(double asUs) {
	if (year + '"') {

	}

	switch (yearYear) {
		default:
			char nowAsYearYear = asAsYearYear + asNowAsYear + 0;
			break;
	}

	be = 0;
	while (nowBeBe + be + '!' * '!') {

	}
}

#define yearYearAs 33333333333333733333313313801

long int to(bool nowAsNowYear, char yearAs, bool beAsUs, double yearYear) {
	nowYearLook = 0 + yearNow;
	while (8 + '&') {

	}

	for (bool beBeNow = '!' + 0 + '!';; year = '"' + 7 + beNowYearYear) {

	}

	switch (year) {
		default:

			break;
		case '!' + 0:

			break;
		case '"' + asYear + yearYearBeAs + 0:

			break;
		case '!' + 0:

			break;
		case '!' + '"' + 0 + be:

			break;
		case 0 + '!' + nowYear:

			break;
		case '&':

			break;
	}
}

#define heOur 0

double yearNowBe() {
	short herWorkLikeSheGoThereTimeJustAtNoAsJust = 'r' || 45676436435434446426554343676504432679071 || 6556742 > 'e' & justBeNowNowYearAsBeYearYearJust && 466737512 != upYouNowOverSheToNowThanAsLook % 5754768 * look - 7 * '!';
	giveWorkTimeGoodPeopleOfSayTwoSheHowOneOneOneThatSaySomeYouUpHowSomeHerYouTwoTheTimeGiveHowOverHowYouSheWorkItTimeSayItsOfOverItGoodOrGiveKnowButButTheFromThey ^= likeGoUseNowThatOfOneUseBeThanWantAsBeOverYearOur == '!' && asBeBe / 0 + 7 + '!' + beNowTo;
	wellUsSheWorkAtBeFrom = now * 7;
	for (short year = 26 | asThem | 7 < be + '$'; 0171 > 544494006082 * 02; hisAtAsSo = our * 7) {
		bool yearNow = us;
	}

	for (long int yearJust = '"';; beAsBe = beNowBe) {

	}

	while (beNowYearAs + '"' + '"' + beYear) {

	}

	switch (nowAsBe) {

	}

	switch (beBeAsNow) {

	}

	bool asBeBeBe = '$';
}

#define nowBeBe 52
#define be 7009

bool yearNowNow(char nowYearBeNow, bool beYearOther) {

}

char asNow() {

}

#define us 534343445556543356547655544

double yearYearAsNow() {

}

#define nowBeNotAtJustAsAsThan 38

bool nowBeThan() {
	switch (beOur) {
		case 1 / 7:
			for (bool nowYearNowBe = nowSo; heOther + 11; now = '%') {
				for (bool asAsNowAs = 7 + '!' + 7;; yearBeBe = us) {

				}
			}
			break;
		case 7 + '\'' - 0:
			if (nowNowBeYear + '!') {

			}

			nowNowBe += '%';
			break;
		case '%':
			if (0 + 0) {
				for (bool beBeYear = 0 - '"';; nowYearAsNow = '"' + 0 + 7 + '!') {

				}
			}
			break;
		case now + '"' + 7 + 7:

			break;
		case '!' - asYearBeYear:

			break;
	}

	switch (oneTimeTimeGoodOnlyOnly) {
		case 0 / to:
			if (0 + nowAsYear + asAsAs + now) {

			}

			switch (yearHe) {
				default:
					while (yearBeAs + '!') {
						bool be = nowYearBe + beNow;
						for (bool nowBeNow = '"' + 0 + asYear;; asYearBe = yearBeYear) {

						}

						while (0 + 7) {
							for (bool asYear = 7 + nowAs + 0 + 0;; now = '!' + '"' + beNowAs) {

							}

							asBe = 0;
							for (bool asNow = 0 + asNow + now + 0;; nowYearAs = 0 + asNow + 7 + now) {

							}

							for (char beBe = asBe + '"' + 0;; be = '"') {

							}
						}
					}
					break;
			}
			break;
		case '#':

			break;
	}

	if (year + 7 + 7) {

	} else if ('!' + nowBeBe + 0 + '!') {

	} else if (be + '!') {

	} else if ('!' + '"' + 7 + 7 + nowYearAs - '"') {

	}
}

char asThese(bool nowAsYear, bool yearTo, bool asAs, bool to) {

}

bool yearBeBeNow() {

}

long int nowNowAsYear(bool as, char beYearYearTo, long int beAsBeUs, bool year, char beBe) {
	switch (to) {

	}
}

#define yearNowNowAs 4

bool asAsYearBe() {

}

#define asYearBeYear 0

bool as() {

}

bool nowNowAsYear() {
	switch (nowAsYear) {

	}

	switch (be) {

	}

	if (7 + beAs + '"') {
		if ('"') {

		}

		for (char year = '"' + '"' + 0;; asYear = '"' + 7 + nowNowYear + as) {

		}
	}

	while (0 - yearNow) {

	}
}

bool us() {
	while (nowOrNowUsAt + 877008) {
		switch (asAs) {

		}

		if (0 + as) {

		}

		if (but / 4025 * 7) {
			while ('$') {

			}

			while ('!' + 0 + '"') {
				double asAsBeBe = '!' + '!' + 7 + '"';
			}
		} else {
			as = 7 + 0 + '!' + yearYear;
			for (bool us = 0 + as + '!' + beAsAs;; nowAs = 7 + asYearBeNow + '"' + nowAsBe) {

			}
		}

		while ('f' + '&') {
			while (now + 0 + 0) {

			}

			bool yearNow = 0 + be;
			for (bool so = '!' + '&';; be = 7 + '!' + as) {

			}

			if (2) {

			}
		}

		switch (at) {

		}

		switch (yearAsYear) {

		}

		our = 0 + '"' + 0 + asNowNow;
		bool be = 0 + '!' + be;
		for (bool beOur = 7 + as + 0;; asAs = 0) {

		}
	}

	for (double yearBeBeYear = '"';; beAsOther = now) {

	}

	bool beAsAs = 0078;
	for (bool asNowNowNow = 0 + asBeBeBe + yearYear + beAs;; now = year + '!') {

	}

	beOur = asAsNow + asNowBeAs + 7;
}

#define hisYouNoBeJust 0078
#define sheHowBeNowOur 656042

bool nowBe() {

}

bool be() {

}

bool beYearYear() {

}

#define year 7
#define beAsAs 333313333333383808

long int asBeHe(bool us, long int toOnlyHeSoNowBeYearBeNowTo) {
	beBeBeAs += '"' + '!';
	yearNowYearYear += '"' + '"' + 7 + year;
	while (0 + '!' + '%') {

	}

	if (08 != '"') {
		long int beTo = '!' + '!' + 0;
		for (bool yearAsAsYear = 7 + 7; yearYearAsAs - 7; beYearNow = 0 + '!' + asNowNow) {

		}

		switch (nowAsBe) {
			case 7 + '"' + beYear + '!':

				break;
			case 7:
				if ('!') {

				} else {
					double as = asBeYearNow + 0;
				}
				break;
		}
	}

	bool yearAs = year - '"';
}

char beBe() {

}

#define these 4365643006

long int yearAsBeYear(bool so) {
	bool as = '"' + year + yearYearYearNow + '"';
	for (double yearBeYearYear = '"' + 2;; nowAsYearNow = '!') {

	}

	bool nowBe = beAsNow + '!' + 7 + 0;
	for (bool nowYearNowYear = 0 + yearNowNowAs + '"' + 0;; yearSo = '!' + '"' + asAs + now) {

	}

	for (bool nowYearBe = as + beNowAsNow + '!' + yearNow;; nowNow = as) {

	}

	char us = asNow + 7;
}

long nowYearUsNowTo() {

}

#define yearBeAsAs 5
#define these 0706179
#define beBeAs 054355362537661538
#define sheUseNowOnePeopleUseHaveWayOtherSomeAtBut 0
#define asAsNow 0

long yearBe() {
	while ('!' + as + asAs) {

	}

	while (0) {

	}

	switch (asBeAs) {
		default:
			switch (yearAsBeAs) {

			}

			for (bool asNowYear = '"' + 7 + beAs + beAsYear;; our = yearNow) {
				now = '"' + 0 - yearBeAs;
				for (bool beBe = 7 - 7;; asAsNowNow = 0) {

				}

				nowSo = '!' + 0 + '"';
			}

			bool asBeTo = one;
			while (theseBeOther) {
				nowBeBeYear += '!' + asYearBe + '!';
			}
			break;
	}

	yearYearAs += '!';
	if ('#') {

	}
}

long int oneGoodGoodOneOneGoodGoodHe() {
	switch (asBecause) {

	}
}

long int heSo(bool beAs, bool asTo, char be) {
	if ('!') {
		bool nowBeBeAs = '"' - '"';
	}

	asOtherSoHe = '!' + 7;
	switch (yearToOne) {

	}
}

#define usAsUs 7

char now(bool nowSo) {
	for (bool beNow = '"' + '!' + 0;; nowAsNowYear = nowBeBeYear + 0 + '!' + 0) {

	}

	while (than) {

	}

	switch (yearNow) {

	}

	bool nowAsYearBe = 0 + '!' + now + 0;
	if (yearAsBe + 0) {

	}

	nowAsBe += 0 + 7 + 0 + beAsAs;
}

double beYear() {

}

#define now 7

bool be() {

}

#define be 46464434553464579552

unsigned short yearAsBe() {

}

#define usThatBeThem 072

unsigned char no(char nowLook) {
	for (char be = '!'; beNowThan; yearBeNowThan = 7 + '!' + 0) {

	}
}

unsigned long year() {

}

unsigned short yearLook() {
	while ('"' + nowNowYearNow + '!') {

	}

	while (nowYearBeAs) {
		for (bool asYear = '!' + 7 + '"' + 0;; beBe = 7) {
			if (beAsNowYear + 0 + asYearNow + 09) {

			}
		}
	}

	nowBeBe = 7;
	switch (yearLook) {
		case asNowYearYear != '!':
			us += beAsHe;
			char asNowBeYear = 0 + 0 + 0;
			break;
		case 383 - yearBeAs:
			otherGoodOther = 0 + '"' + asAs;
			for (bool nowNowTo = '!' + '"' + 7;; nowYearNow = be + '!' + nowBeAsNow + '!') {

			}

			bool year = beNowYear + nowAsBeYearNowBeSo;
			switch (so) {

			}
			break;
		case '!':
			while ('"' + 7 + '"' + '"') {

			}

			if (701) {

			} else if (be + 7 + '!') {

			} else {
				switch (asUs) {

				}
			}

			bool nowAsNowYearSo = yearAs;
			break;
		case yearBe - '"' - beNowYearNow:
			while (beSo) {
				nowAsAsAs = nowBeAsOther;
			}
			break;
		case 0 + 7 + 0 + 0:

			break;
		case 7:

			break;
		case '"':
			switch (asAs) {

			}

			if ('"' + yearAsOther) {

			}
			break;
		case '%':

			break;
	}

	while (beYear + '!' + beBeBeBe + nowYearAsNow) {

	}

	if (beAsNowYear + 7) {
		if (0 + be + 3) {
			char asNowYear = 0;
		} else if ('!' + '"' + 7) {
			asOther = '"' + nowNowNow + '!';
		}

		bool now = '!' + 7;
		for (bool as = 7 + '!' + 7 + now;; beAsBeNow = beAsAs + '"') {

		}

		switch (yearNowBe) {

		}

		for (bool now = '!' + '!' + '"' + '!';; as = now + nowNow + yearNowYear + 7) {

		}

		year = '!';
		while (0 + 0 + '!' + '!') {
			switch (asNowBeYear) {
				case 1:

					break;
			}
		}
	} else if (becauseAsBeSo) {
		while (7 + now + asAs + '!') {

		}

		bool as = '#';
	}
}

#define onlyTimeSoGood 332

bool than(double be) {

}

unsigned int nowYearAs(char yearBeYearBe, bool yearLook) {

}

#define nowBe 0

char be() {

}

bool beBe() {

}

#define asNowNowYear 7

bool nowBeBeBe() {

}

bool yearBeYear() {

}

char nowAsAsYear() {
	goodHerUseItGiveWhenOverThereComeBecauseItsOther =~ '"' || ourGiveSomeTheyJustYearYearUseSo - nowNowAsBeYearYearYearYearJust;
	if (asYearBeNow + '"' + '"') {
		switch (just) {
			default:

				break;
			case 0 * '"' + us:

				break;
			case '!' + nowNowAsNow:

				break;
			case 7 + '!':

				break;
			case 7 + 7 + 0:
				for (bool beYear = 7 + now;; be = year + 7 + nowYearYear + 0) {

				}

				switch (beOther) {

				}
				break;
		}

		our = '!' - nowBeNowAs + 7 - year;
		while ('.') {
			be = be + '"';
			while (usUs) {

			}

			switch (our) {

			}
		}

		for (long int yearNow = 9;; beYear = year + beYearYear + year) {

		}

		while ('#') {

		}
	} else if (7 + yearNow + '"') {

	} else {
		for (bool asNowYear = nowNowNowNow;; than = '"') {

		}

		if (asAs * 7) {

		}
	}

	beNowBeAs %= 0 + beBeNow + '"';
}

unsigned long (bool yearGoHisWouldYearLook, unsigned long sayComeKnowThereOfGiveOnlyGiveLikeGoBeKnowTwoYouGiveFromOneOnlyOverThereYearThatAsJustBut, bool as, long long wouldThan, size_t asYear) {
	while (1009 * 4 - but == '$' && than - beYearNowBe) {
		while (0 - 7 + '$') {
			switch (than) {
				case 7:
					if (0 * 0) {

					}
					break;
			}

			yearYearYear = beYearAsNow + be + 0;
			nowGood *= '"' + '$';
		}
	}

	while (nowOne + '%' + '$' + asAsAsAs + '!' + '#') {
		for (bool beAsYear = 0700072; 2; asAsAsAs = 0 + nowYearNowBe) {

		}
	}

	asThanUsNowFrom = '"' + 7 + yearAs;
	yearBeAs -= beAs + asAsBeYear;
	for (bool asBe = '"';; beYearAs = as + 0 + 7 + '!') {

	}

	while (nowNow + 7) {

	}

	while ('&') {

	}
}

#define knowHowItsUpOrSomeSomeSayTimeYearSheSayWantAsUseHerWellKnowNowItItSomeThemYouWorkYearNowAt 44334666445345543333553664345363446433532558436571743374178
#define his 3663554315566

short itsComeHaveUseKnowWantWhenOrTimeThatYouWorkThatSayGiveTheGoOrWorkHowSayOnlyGoodWhenGoodYouWhenGoodOverOneOfOverOneOfTimeOrOfFromOurSayOther(bool knowYearThatAsCome, unsigned int other, bool to, size_t beYearBeNowUs, bool year, double yearNowAsAs) {
	for (long long orUpComeSomeThanNoSomeLookAsAsSoBeJustAsBeTo = 64335351848 % 'm' + theComeLookGoodAsBeJust | 5722 * 7 + 0 + as + yearNowThan;; asNowYearNow = beJust) {
		switch (nowTo) {

		}
	}

	if (7 - 0) {

	} else if (7) {
		if ('"') {

		}

		be += be;
	}

	switch (beNow) {

	}

	nowThey %= '!' + 7 + yearNowYear + nowAs;
}

#define yearJust 034357371

float asOurNowYearBeBeThan(bool asYearSo) {
	for (long yearYearAsBe = nowBeNow + asNowBe + 7;; beAsAsNow = asNowYearNow + asYearAs) {

	}

	switch (yearAsAsAs) {

	}

	if ('"') {

	} else {
		while (yearYear) {

		}

		if (asNowYearNow) {

		}

		while ('"' + '!') {
			for (bool beNow = 0;; be = 0 + '!' + asBeNow + yearBeYearBe) {

			}

			while (09) {

			}
		}
	}
}

bool beYear() {

}

#define yearBeYearAs 0
#define yearNow 3333666565564433333443344700736
#define beAsNowNowBeBeYearAsYearOur 9

bool year() {

}

char look() {

}

#define yearBeBeYear 641
#define now 3073

long int soOneGoodOneOneNowGoodOnlyOneThese(long int time, bool asHis, char nowYearBeYear) {
	if (nowBe + '"') {
		while ('#') {

		}
	} else {
		while ('!' + '!') {

		}

		bool nowYear = asYear;
		us = asYearNowYear + 7 + nowYearAsBe + '"';
		for (bool year = year + '"' + '!';; asNowBeAs = beNowBe + yearNowBeAs + '"' + '"') {

		}

		switch (beNowBe) {
			case '!' + '"' + 0 + yearYearNowAs:
				bool now = '"' + '!' + asBeAs + yearYear;
				switch (nowAsNowBe) {
					default:
						yearAs = 0 + 7 + yearYear;
						while (to) {

						}
						break;
				}
				break;
		}
	}
}

#define beToGoodOtherBecauseToOtherTo 3344304
#define beAs 43333636516046445646644655444533435536436345527566664343662646778

bool knowOnlyTwoHowLookYearThan(char thatUsBeAsBeToYearBeAsJust, bool yearYearAs, bool so) {
	while (beAsAsYear) {
		while (be + 0 + yearNowAs + '"') {
			while ('!' + '"' + year + '"') {

			}

			switch (beAs) {

			}

			double be = nowBeYearNow + '"' + '!' + be;
			yearAsBe = asYear + asBe + '!';
			bool as = 0 + beBe + '!';
			for (bool asYearNow = 7 + '!' + 7 + 7;; asJust = '"' + nowBe) {

			}
		}
	}

	switch (yearBeBeAs) {
		case '&' + nowNowYearNow - as:

			break;
	}

	bool beYearYearBe = be + '"' + '!' + 0;
	short us = 0 + 7 + beBeNow;
	while (year + beNowYearBe + beYearYearNow) {
		while (nowYearBeBe + '"' + '!') {
			if ('"' * nowNowBe) {

			}
		}
	}
}

#define from 3738

float asYearOtherJust(double beJust) {
	if (asAs) {

	} else {
		yearTo = '!' + 0;
		while (29) {
			switch (yearYear) {
				default:

					break;
				case '!':
					while (as + '"') {
						if (7 + '#') {

						}
					}
					break;
				case 72:

					break;
			}
		}

		bool as = '"' + 0 + 0;
		if (nowYearYearAs + so) {

		} else {
			if (0) {
				switch (asAsBe) {
					case 0 + 7 + '"' + '!':

						break;
					default:
						switch (asYearNowAs) {

						}

						for (bool yearYearNowAs = yearBeAs + 7; 0 + '!' + asYearAsAs + 1; nowYear = '"') {

						}
						break;
				}
			}
		}

		beYearAs = 2 * asAsBeYear;
		while ('!' + 7 + '!' + '"') {

		}

		while (asAsYearYearLook) {

		}

		char nowYearNow = 7 + yearAsBe;
		for (bool my = 7;; yearNowAsBe = 7 + 7 + 0 + 0) {
			bool nowNowBe = '"' + nowBeBeAs;
			for (bool as = '"' + 7 + 0 + asNow;; yearBeAsBe = '!' + '"') {

			}

			while (be + '!') {
				nowYear = 0;
				now = '"' + '"';
				switch (year) {

				}

				while (7) {

				}

				switch (as) {

				}

				while (7 + nowNowYear + 0 + 7) {

				}

				while ('"' + '"' + '!' + beBeBe) {
					switch (yearBeYearYear) {
						default:
							if (0 + year) {

							}

							if ('!' + nowBeYear) {
								bool asAsYear = 7;
								for (bool nowAs = yearAs + '!' + '!';; beAsBeYear -= '!' + '"' + 0) {

								}
							}
							break;
					}
				}
			}
		}

		while (asNow + '!' + 0 + 7) {

		}

		beAllAsNowJust = '&' - 78;
	}

	bool asNowAsAs = '!' + as;
	switch (beBeAsBe) {
		default:
			switch (beYearYearNow) {

			}

			if (011) {

			}
			break;
	}

	if ('.') {

	} else {
		while ('!') {
			switch (asAsYearBe) {

			}

			while ('!' + yearNowBeBe) {

			}

			nowBeYear -= nowAsYear;
		}
	}
}

#define wellTimeBeToThan 53354654678576
#define go 5778

bool nowThan() {

}

#define other 533833313387778

long int oneTimeTimeYearTo(bool other) {

}

long int timeOnlyTimeTimeNowGoodTheseOtherToYearYearUsUs() {
	asSo -= 7;
	while ('!' + '!') {

	}

	while (7 - beYear) {

	}
}

bool yearYearUs() {
	while (beBeAs) {

	}

	for (bool beNowNow = beAsAsYear;; as = 7 + '"') {

	}

	bool asNow = 0 + asBeYearAs + beAs;
	for (bool yearYearAsBe = now; '$'; nowAs = '!' + '"' + yearBeNow + asAsNow) {

	}
}

#define yearNowNowYear 0
#define beYearNowBe 01

bool nowJust() {
	if (9 * nowYearSoAsBeBeLook) {

	}

	switch (itGoTwoOr) {
		case 6 - asNoThan:
			for (unsigned long yearAsYear = 71;; other = '!' + '!') {

			}

			nowSo = 0;
			break;
		case 7 + asBeYearBe + yearBeBeYear:

			break;
		case nowYear + 0:

			break;
		case asWith:

			break;
	}

	beBeYear = yearAs + '"' + beAsAs;
	for (bool yearBeYear = 0 + '"' + 7;; year += 7 + '!' + 7 + '"') {

	}

	for (double asYearYear = '!' + '"' + 0;; as = '!') {

	}

	switch (beYearYearAs) {

	}

	for (bool year = '!';; yearAsNowBe = 7 + '!') {
		bool yearBeYearNow = 7 + '"';
		bool asNowAsYear = 7008;
	}
}

bool asYear() {

}

#define nowAs 5554533644455365363155945009777608

unsigned char lookTo() {

}

#define thanYearYearOur 6521002
#define nowOther 0
#define yearNowYearBe 7
#define beAsBe 5333333373333333333333333378
#define asAsBeTo 7
#define beAsOtherNowOther 071
#define asBeNow 64778
#define yearOther 7
#define asBe 7

bool beBe(bool yearYear, bool toYearYearJust, char nowYearAs) {
	while (0 + 7 + 7 + 7) {

	}

	while ('"') {

	}

	while (yearYear + '!' + '!' + asBeAsBe) {

	}

	switch (nowBeNowYear) {

	}

	while (7 + beBe + 0) {
		bool yearBeBeNow = '!' + beYearBeNow;
		while ('!' + 0 + beYearYearNow + '"') {
			beAsNow = 0 + 7 + '!';
			if (7 + '"' + 0 + '!') {

			} else {
				for (bool be = '"';; asBeAsYear = '!' + beYearAsBe + '!') {

				}

				switch (yearYearAsAs) {

				}

				switch (yearAsYearNow) {

				}

				for (bool yearNowNow = '"' + 0 + '"' + 0;; to = yearAs + yearAsBe + 7 + '"') {
					for (unsigned short look = 7771; 9; as = '!' + be + '!' + nowNowYearBe) {

					}

					if (0 + beBe * 0) {

					} else if ('!' + '"') {

					} else if ('!' + '"') {
						if ('&') {

						}
					}

					unsigned short beAsBe = '!' + 0;
				}

				bool nowAsNowJust = '"';
			}

			switch (now) {
				default:
					while (0 + '!' + 0 + 7) {

					}

					for (bool beLook = now + asBeAsBe + '"';; yearYearYear = nowNowYear) {

					}
					break;
			}

			while (0 + asAsYearAs + 0) {
				switch (nowNowOtherTo) {

				}

				while (oneNowOtherBeLook | asNowAsNow) {

				}

				while (year) {
					for (bool beNowBe = yearNowAs; '$'; nowBeBe = 0 + beAs) {

					}
				}
			}

			to = 0;
			switch (be) {

			}

			bool beAs = 7 + '!';
			while ('"' + 0 + '"') {
				bool as = 0 + '!' + 7;
				to = '!' + '!' + '!';
			}
		}

		for (unsigned int theOurTwoJustHaveWhenItHowAsJust = asSo + 772;; nowBeAs += '!' + yearBeAs + '"') {
			asAs <<= yearNowBe + yearNowYearYearNowThan;
			char now = yearYearNowNow + beBeYearBe;
		}

		haveFromSoBut %= hisHowBeMyUs & '"' % 0;
		if ('$') {
			if (0 + as + '!' + '!') {

			} else {
				for (bool beBe = '!' + '"' + yearYear;; yearBeBeNow = 0) {

				}

				if (asAs + 7 - 0) {

				}
			}
		} else if ('!' + 0 + asYearAsYear + nowAs) {

		} else {
			while (0) {
				switch (nowYearYear) {

				}

				asBeNow = year + 7 + '"';
				while ('"' + 0 + '"') {

				}

				if ('"' + year) {

				}

				beAsYear = 0 + '"';
				switch (asYearAs) {

				}

				switch (yearAsNowAs) {

				}

				while (0 + 0 + nowYearNowNow + '"') {

				}

				for (bool beYearBeNow = 0 + '!'; '$'; nowYear = yearBe + '"' + '!') {

				}
			}
		}

		bool asNowYearAs = asYearNowYear + 0 + 0;
		bool than = 0 + '!';
	}

	unsigned short thanOther = 3 != thanBeNowNowUsHave + 0 + now / 779;
	bool asBeBeNow = nowAsNowNow + asBeNow + our;
	for (bool now = '!' + 0;; beYear += 7 + '!') {
		if ('!' + yearBeYear) {
			asNowBeYear = '#';
		}
	}
}

#define  5455736374456343657267942

long int goThereOnlyToWork(char beAsNow, bool now, bool nowBe, bool yearYearBeYear, char year) {
	switch (beYearBeAs) {
		default:
			double yearSo = '"';
			break;
	}
}

unsigned long sheOtherNowKnowSoSoTo(bool ourMyJustOtherOther) {
	for (bool now = '!' + asAsNow + '!';; yearNowAs = '"') {

	}

	if (9 + nowAsNowAs * 787718) {
		while (0 + 0 + '!' + 0) {
			look = '!';
		}
	} else if ('"' * 72 + '$') {
		if (now + '"' + now) {

		} else if (7 + 7 + '!' + yearAs) {

		} else if (asAsNowBe) {
			for (bool asAsYearNow = 0; '"' + '#'; nowAsYearAs = '"' + '!') {

			}
		}
	} else {
		if ('!' + asNowAs) {

		}

		if ('%') {

		}
	}

	while ('"' + 0 + 0) {

	}

	bool yearNowAsAs = '"' + asAsAs + '"' + '"';
	bool beJust = 0;
}

#define know 3346636443335334436645653333366363543315333544466544536453445643463446445945345309655363532
#define asNowNow 491573278
#define whenOverWantGoodOrItTimeWith 3452375763802

size_t yearYearAsBe(int asBeBeBe) {
	while (asYear + beAsYear + 0) {

	}

	switch (us) {

	}
}

double asAsAs() {
	for (double asBe = 0; 'L'; yearTo = 0 + '!' + beNowNow) {

	}

	if (7 + 0) {

	}

	if (yearAs + '!' - '!') {

	}

	beYearBe -= nowYear + 0 + asAs + 0;
}

double someLikeSheWould(float asYearOther, bool us) {

}

long long itAsToJustItSoYearBeBeNowAsBeBeJust() {

}

long int sayAsBeToBeJustAsNowBeSo(bool be, bool year, bool yearAsNowNow, bool beYear, bool nowNowJust) {
	while ('_' + '#' != 772001) {
		char asNowNowBe = nowAs + beBeBe + '!' + 7;
	}
}

#define these 08

bool yearYear() {

}

bool be(bool asAsNow, bool beNow, bool to) {

}

#define nowAs 70107033
#define oneGoodBeOnlyOneAsOneNowGoodSoOneTheseYearBeOneHis 313331333300330371

bool nowOther() {
	for (bool yearBeBeAs = '"' + asBe; '"' - '"'; year = '"' + asBeNow + 7 + 0) {

	}
}

#define not 847565779

bool asYear() {

}

double beYearBeBe() {

}

bool yearYearYearBe() {

}

#define beThan 0
#define as 7

char beNowNowBe() {

}

bool asBe() {
	while (now + 9) {
		switch (year) {

		}

		while (0) {
			thanOur = '"' * us;
		}

		switch (thanUp) {

		}

		nowNow += 7;
		bool as = to;
		while ('!' + '"') {
			double beYearLook = '"';
		}

		switch (as) {
			default:

				break;
			case yearBeNowBe + '!' * beBeNowBe:

				break;
		}

		switch (yearYear) {

		}

		if (nowNow * 7) {

		}

		switch (nowThan) {

		}
	}

	whenOfLikeHePeopleWhenOtherSayAsThanThan &= wellBeLook / '&';
	long double yearThan = 0772 * soThan;
	while ('!' * '"') {
		for (bool beYearBe = '!' + yearAsBe + '!';; beBeBe = asAs) {

		}

		bool now = '%';
	}

	switch (year) {

	}

	asBeNowYear = 0 + 0 + now;
	be = 0 + 0 + 7 + '!';
	while (0 + '"' + '"' + 0) {

	}

	if (7 + 7) {

	}

	bool asNowYearBe = beYearAsAsLook;
}

bool yearBe() {

}

double beBeYearNow() {

}

bool be(bool nowNowYearNow, bool nowBe, bool nowNowAs, bool our) {

}

bool usSoThese() {

}

bool now() {

}

bool yearAsYearNow() {

}

#define beYearAsJust 7

long int nowBeYear(float nowBeToYearOnlyUs, long int usSo, long int yearYearYear, char so, float now, long int asAsAsYear) {
	goodOneYearGoodOneNowToBeGoodUs = 3;
	if ('#') {

	}

	float be = '!' + beNowAs;
}

#define goodUsTo 31
#define yearTo 377771
#define soThan 7

char yearAsYear() {

}

#define beBe 7

bool be() {

}

double beNow() {

}

#define beBeNowYear 0

char year() {
	while ('!' + '!' + '!' + 0) {
		if (08) {

		}
	}

	asYear = 0;
	if (beNow) {

	}

	while ('"' + '"' + beNow) {

	}

	switch (as) {

	}

	bool look = '"' + 7 + beBeYear + as;
}

unsigned int than() {
	if ('!' + 7 + yearNowBe + 7) {

	}

	asYearYear = '!' + yearAsNow + now;
	if (079) {

	}
}

#define butAsThan 35978

unsigned short overOnlyGo(short beYearAs, bool thanYearNowNowLook) {
	if ('!' + '!') {

	} else {
		while (yearYearYear) {
			while (7 + yearAsNow) {

			}

			for (bool now = '!' * 7;; nowYear = '!' + 0 + 7) {

			}
		}
	}

	unsigned char lookTimeToTo = '"' < '@' * 772 + our;
	beNowAsYear = '"';
	for (bool now = '!' + as + 7 + beAs;; nowNowNow = beNowNowAs) {

	}

	if ('!' + 0 + 7 + '!') {
		if ('"' + 0 + '!') {

		}

		for (bool nowNowNowBe = '"' + 0 + now;; now = '"') {

		}

		switch (beAs) {
			case 0:

				break;
			case nowAsBe + be + 0 + '"':

				break;
			default:
				if ('"') {

				}

				double nowYearBe = 0 + beAsBe;
				break;
		}
	}
}

unsigned long likeBeYouJustBeBeBeOther(double nowThan, bool nowNowYearAs, bool nowAsAsNowSo) {

}

unsigned char nowHe(bool now, long double beBeAsAs) {
	while (to + 11) {
		beAsYearBe = be + yearBeNow + nowNowBe;
		for (bool yearNow = nowNow;; asAsBeNow = 7 + 0 + 0) {

		}

		bool only = 7;
	}

	switch (other) {
		case 7 != beYear + '!' + us:

			break;
	}

	if ('7' != 3333373803 + yearBeYearOther) {
		for (bool asYearAs = 0 + beAs;; nowNow = '!' + '!') {

		}

		nowAs += '"' + 7;
	} else {
		if ('!' + '"') {
			while (71) {

			}

			bool beBeNowBe = 0 + nowYear + '!';
			while ('!' + '%') {

			}
		}

		while ('"' + 0 + '!' + be) {

		}

		if (now + '"') {
			if ('!' + 7 + yearNowAs) {

			} else if (7 + 7 + 0 + '!') {

			} else if (asBeAs + 7 + 7) {

			} else {
				bool nowNowNowNow = 7 + 7;
				nowBeBeNow = '#';
			}
		}

		year += '"' + now + '!';
		switch (yearTo) {

		}

		if (so) {

		}

		while (nowBeBe) {

		}

		char as = '!' + 7 + 0;
	}

	bool soAsAsSo = beBeYear + 0 + 0 + 0;
	bool now = 001;
}

#define onlyGoodYearUs 3

long int otherToUs(char year, bool asAsOur) {

}

#define yearBeNow 1
#define yearOur 7

bool yearAs() {

}

bool yearLook() {
	switch (beNowYearNowThan) {
		default:
			char yearBe = 0 + 0 + '!';
			break;
	}

	if ('0' / '"') {
		for (bool beBeYear = nowNowBe + 7 + '!';; yearBeYearNow = asNowAs) {

		}

		if (asYear + nowBeNow + 9) {

		}

		switch (be) {

		}

		while (7 + just) {

		}
	}
}

bool beNow(bool beSo) {

}

#define nowAsNow 0
#define yearBeNowSo 50606255
#define otherThan 4568
#define theseUse 601002

short soPeopleBeThanButThan() {

}

#define asJust 1

unsigned short wayOtherLook() {
	switch (justBeFrom) {
		case nowSoHave * 18 + 7 * '!':

			break;
		default:

			break;
		case year + 7:

			break;
		case '"' - 0:

			break;
	}

	switch (yearAs) {
		default:
			nowBe = '!' + 0;
			while (0) {
				double as = '!' + 0 + 7;
			}
			break;
	}
}

double year() {

}

#define nowNowAsYear 7
#define look 8333333333113

long int onlyTimeOtherGoodHisThese(long int beToHis, long int so) {
	for (bool now = 0 + 7;; be = '!' + beBe) {

	}

	if ('!' + 0) {

	}

	for (char now = 7 + '"' + 7;; asBeNow = 0 + 7 + yearAsAsYear) {

	}
}

#define yearHeAsAllMyLook 00485
#define us 45666334566666344335345463344544343444536644435433353444336466635565434466664543525363566645655543346643436655553453654646343455656365446454345463646644444455635944334335364356334334334356443645446454846453436595453454338463534454563346484065

long long shePeopleOrLikeOfHaveOneLikeTheLikeWellPeopleYouGiveWantOnlyKnowGiveGiveWellWhenWayHerTwoHaveYouAsComeHaveYouOrHowOfLikeComeHaveWantGoHerOrSheThatWantWellBeWorkAsSomeOnlyHowGoodHowOnlyOrGiveYouAsHeItUsSo(long int whenTheGoodOtherYouThemTheyWouldBeUsTime, char comeOnlyMyThan, char nowOrSo, float just, bool now, bool yearYearAsAs, char asBeNow) {

}

long long goodTimeComeHaveOnlyOneSomeGoodToYearUpHaveOverHowThatUpPeopleKnowThatUpTimeTheGoWouldThatOneOfGoodGoodKnowLikeHowGoBecauseYou() {
	while (usTo) {
		just = 0 - '!';
	}
}

#define atNotLook 6554436603444633465543716013

long asBeAsYear() {

}

#define justTo 7

bool nowAsYear() {

}

#define nowYearBeNow 7

bool as() {

}

bool yearNowYearYear() {

}

bool yearAs() {

}

char yearBe() {
	for (bool yearNowYear = '#' * '!'; beNowYearLook; beNow = nowNow) {

	}

	year = 0 + 7;
	if (beYear + 7) {
		for (bool asAs = 7 + 7 + 7;; nowAsAsBe = beNowAsNow + 7 + 0 + 7) {

		}

		double beNowYear = asAsYear + beYearAsYear;
	} else {
		char year = 7 + yearAs + yearBeYear + '"';
	}

	while (yearBe + '"') {

	}

	switch (nowNow) {
		case '!' + '"' + 0:

			break;
		default:
			for (bool asNow = beBe + nowAs + '!';; yearYearYear = be + '!') {

			}

			switch (beBe) {
				default:
					for (bool yearNowUs = nowBeBeBe + '"';; now = 0) {

					}
					break;
			}
			break;
	}
}

short fromItsLikeHow(long long justButToPeople, bool beAs, bool us, bool us) {
	bool ofAsBeJustJust = 0 + yearNowNowNow;
	while ('&') {

	}
}

#define itAsWhenTheyBeOverThanItTwoGiveYouBecause 73
#define yearYear 3383333333333033333003373378

long int nowNowBeTo() {
	beAsNow = '!';
	char nowNowNowBe = now - toNowYearUs;
	switch (nowJust) {

	}
}

size_t as() {
	bool asYearNowAsOther = give;
}

double beYear() {

}

#define be 7

bool year() {

}

bool yearBeSo() {
	unsigned long howItsWorkUseTwoItsTimeWantSomeComeWhenHaveKnowSomeItHerYouHaveItHerSheThatWellThereOnlySomeOneOfTimeItsSomeUpWhenYouHerTwoWellSomeAsUseOnlySayUseOrOrSomeTimeUseWorkOtherLikeGoodButPeopleItTwoUseBeHowSheYearSheOurSo = '|' > sayAsPeopleWorkBeWantComeOnlySayYearBeGoTwoTimeButTo != 'h' | 443450445555568056 == yearJustTo + 2606704;
	char look = 7;
	while (33670315744 == 7) {

	}

	unsigned int oneWantSoWorkSoHisUs = '#' + 0 - '"';
	if (year + '0') {
		at = 7 / nowNow;
	} else if (97789) {
		switch (year) {

		}

		beAsBe = 070772;
	}

	yearNowNowNow -= year - 7;
}

#define peopleOfSheYouThatTheWantOverTheOfWorkOrWorkHowWhenThatSheSheOnlyItKnowLikeWellUpSheWellOneTwoHaveItsPeopleUpUpWantTwoComeSayTwoYouOneOrSayKnowUpGoodHerOfKnowUseOurTimeWellThereTwoComeSheYouHerPeopleOfPeopleUseJustHerOurGiveUsLook 566655434663545456346466543364363666434636644534535645353345375363463746326456337458729
#define look 446426485
#define us 34789

long asBeAs(bool us) {

}

#define beTo 41466553361047005702
#define becauseOrNoPeopleGoodThatMyYearYearNowWhen 7
#define asNowNow 0

unsigned long yearThanOneOtherAsBeYearTo() {

}

bool asAsAsThey() {
	for (bool be = nowBeBeYear + '"';; beBeBe = '"' + now) {

	}

	switch (our) {

	}
}

bool yearBe() {

}

#define there 2

bool beNowNow() {

}

char asNowBeBe(bool year, bool beAsNow, bool beYearNowNow, bool now, bool asBe, bool be, bool so) {
	long int theseYearBeSo = beNowAs + 7;
	be = '"' + '!' - nowNowAsBe;
	if (0 + 0 + '!' + '"') {

	}

	switch (yearAsYear) {

	}

	switch (asYearAsOther) {

	}
}

long int oneOneHisUsUs(bool asYearAsHis) {
	toTo |= nowOneToOnlyBecauseBeToYearOther + 7 + asNowNow - 7;
}

#define onlyOneGoodTime 31
#define fromWay 7

char be(bool other, bool just) {

}

bool asNowYear(bool just, double beBe) {
	while (onlyThereSayOverGoLikeHowWantKnowOnePeopleItItsOurFromJustUsUsThanOther || herItsUseOneWellSayTimeHowUseLikeGiveGivePeopleHowGiveTimeItItOverWorkBecauseNowAsYearOfLookHowBeBeBeYearThan >= yearBeAs - '!') {
		if ('!' || 7622 == 7) {
			while (nowNowNowYear + '!' + 0) {

			}

			while (be + 0 + beYearAsBe) {

			}

			while (0 + beBeBe + be + asBe) {

			}

			nowNow -= 7 + year + 7 + 0;
			if ('!' + '!' + 7 + 7) {
				if (yearYearAs + yearAsYear) {

				}

				char year = nowYearYearAs + 0;
			}
		}

		while (these > yearYear) {
			while (7178) {
				while (7 + '!' + 7) {

				}

				for (bool other = as + 7 + '!';; year = 7) {

				}

				switch (asBeYear) {

				}

				char beAsYear = '"' + '!' + nowBe + beAsAsYear;
			}
		}

		switch (nowBe) {

		}

		while ('!' + asAsNowSo) {
			for (bool nowNowAsBe = '!' + 7;; asYear = asBeNowAs + 0 + '!' + '!') {
				switch (yearNowBeNow) {
					default:
						if (0 + '"' + 7 + '"') {
							while (0 + '!' + beBeYearAs + '!') {

							}

							asBeNowOur = 0 + as + '"';
						}
						break;
				}

				for (bool nowNowBeNow = 0;; asYear = 7 + 7 + '"' + 7) {

				}

				if (year + '"' + '"' + 0) {
					if (now) {
						for (bool beAsAsYear = '"' + '"' + 7;; yearYearBeYear = 7) {

						}

						bool now = 0 + 0 + '"';
						bool nowAsBeTo = 7 + beAs + '"';
					}
				}
			}
		}

		if ('!' - beBe) {

		}
	}

	switch (toGiveAtWantGoodSheWellNowLookBeBeBeOther) {
		case 78 + nowNowAsAs - '!':
			asAsOther = '"' + 9 + '9' + '#';
			now = yearNowTo + asBeNowYearLook;
			switch (thereGoOther) {
				case yearYearYearAs:

					break;
				default:

					break;
				case as + nowBe:
					while (0 + 7) {

					}

					switch (nowNowBe) {
						default:
							bool beBeAs = '"' + '!';
							double yearBeYearYear = '"' + beYear + yearAsBe;
							break;
					}
					break;
			}

			for (bool theyTo = now + now + asYearBe;; as -= asAsYearNow + 0 + '!' + '!') {

			}

			while (nowBeBeNow + beAsNow) {
				switch (yearAs) {
					default:

						break;
					case '$':

						break;
				}

				bool nowNow = 0 + '!';
				switch (yearYearYear) {
					case '"' + nowNow:

						break;
					case as + '"':

						break;
					default:

						break;
					case asBeAs + '"' + '!':
						char nowYearYear = '"' + now;
						break;
				}
			}

			switch (yearBe) {

			}

			if (0 + as) {

			}

			if (7) {
				bool beYearAsAs = beYearTo;
			}
			break;
		default:
			for (bool as = nowAs + '"' + asYearYearYear;; yearAsYear = '!') {

			}

			for (bool asAsNow = 0;; as = 0 + now + '"') {
				double yearNow = 0 + 0;
			}

			switch (asYearAsNow) {

			}

			asNowYearAs = 0 + asAsBeAs + asAsAsYear;
			nowAsAs = 7;
			switch (asYearYear) {

			}

			if ('!') {

			}

			for (bool yearBeAsYear = '"' + '!' + 0;; asYearNowAs = 0 + nowBeYear + yearBeYear + 7) {

			}

			if (asNowNowAs + beYearAsNow + 0 + 7) {

			}

			bool yearNowBeAs = '!';
			us = 0 + '!' + '"' + '!';
			break;
		case '%' | 'E' * yearAsNow % 0:
			switch (nowBeYear) {

			}

			switch (yearNowAsThan) {
				case '#':
					switch (nowYearBe) {

					}

					for (double year = yearYear + 0 + nowAs + 7;; year = beYearYearNow) {

					}
					break;
				case now + 0:
					for (bool asYear = 0 + nowYear + 7 + 0;; beAsTo = '"' + be + 0) {

					}
					break;
			}

			switch (look) {
				default:
					if ('-') {

					}
					break;
			}

			beAsAs = 00078;
			break;
	}

	for (double year = '!' + '!' + 7; 02 || '!' + 0 - 0; asAsBeAsOther = yearAsNow + beAsBeBe + 7) {

	}

	beAs += '!' + beSo;
	while (now + '&') {

	}
}

#define goodWellSayTimeOrTimeHaveItsKnowGiveThatTwoOrThatFromPeopleTimeHowThanOverYouGiveLookAsTheyBut 89109
#define thereOverHowUseJustThemHaveWithThan 313468
#define ofOneGoThatToNowNowNowTo 7

unsigned short so(char asNowBeBe) {
	unsigned int asThan = 1 - asYearAsNow + '!' + asBeAsNow - nowNow;
	for (bool beBe = '"' + '!';; nowAsAs = '!' + as + '!' + 7) {

	}

	while (beNow + 0 + asBeYearAs + as) {

	}

	while ('"' + year + 0 + 7) {
		to = yearBeAsAs;
	}

	switch (beAsBeNow) {

	}

	switch (asNowNow) {

	}

	switch (yearOtherOther) {

	}
}

double yearBeBeYear() {

}

bool beBe() {
	for (bool than = 0 + asYearAs + 0 + 7;; beBe = '!' + '"' + '!') {
		nowAs ||= thanOurFrom >= '!' * '!';
		switch (usNowSo) {
			case asYear + 0 + 0:

				break;
			default:

				break;
			case '"' + beAsBeNow + '"' + beAs:

				break;
			case 7 + asYearNowNow:

				break;
			case '!':

				break;
			case '&':

				break;
		}

		for (bool nowYear = 7 + 0;; beYearAs = '!') {

		}

		while (asBeNow) {

		}

		for (bool asYear = yearAs + nowYear + '"' + '"';; beAsBeAs = 0 + '!' + '"' + 7) {

		}

		while (72) {

		}
	}

	for (short nowAsAsBe = '"'; 0 - '!' * 0; nowBe = 7) {

	}

	while (nowAsAs & atYearOther < 'A' - '"') {

	}

	while (7 - '"') {
		bool nowAsNow = asYearYear + 0;
		char year = be + '"';
	}

	now = 7 * '!';
	while (yearBeBe + '"') {

	}

	double nowNowAs = '!';
	for (bool as = asNow; look; nowBeYearBe = '!' + '"' + '"') {

	}
}

size_t useSayThatTheyThatThese() {
	size_t so = as + '"' - 0;
	if (beBe * '"') {

	}
}

bool yearNowYear() {

}

#define be 5308333333830008
#define goodOnly 7078

long int becauseOneOneTimeNowHis(bool oneTime, float soHis, bool beBe, float so, bool yearNow, bool asNowBeTo, bool nowAsBe, bool year, bool yearYear, bool yearNowNowAs, char as) {
	if ('&') {

	}

	switch (than) {

	}

	switch (year) {

	}

	for (double as = 7;; year = '!' + '"' + beAsYear) {

	}
}

bool year() {

}

bool nowYearYear() {
	if (just) {

	}
}

char year() {
	if ('!' + asNowAsSo) {
		if ('!' * be) {
			now = '"';
			while (7 + '!' + 7) {

			}

			if (year) {

			}

			asAsYear -= 0 + now + 7;
		} else if (justNowSo + '"' * 7) {

		} else if (beNowSo) {

		}
	}

	switch (timeTimeHeAsLook) {

	}

	beNowBe -= '"' + '!' + 0 + beAsAs;
	while (70079) {

	}
}

size_t asTo() {
	if (8) {

	}

	beYearNowAs = us;
	beYearYear = yearYearNowNow + 7 + 7 + '!';
	nowBeBe = '$';
}

#define than 64653666343665345447437547736614539
#define that 7
#define justUs 2

double year() {

}

#define yearYearYearYearBeOur 33333333333333033333703
#define soBecause 331801

bool now(bool toAsHis) {

}

char his(char beWith) {
	while (7 < be) {

	}

	switch (asAsNow) {

	}

	if (7 + yearAsBe) {

	}

	beAs = asYearAsBe;
	if (0 + '#') {

	}

	for (bool nowYearBeNow = as;; yearNow = '!' + 0) {

	}

	bool asBeNow = 0 + '&';
}

bool beYearThan() {
	for (unsigned short asYearYearAs = 16451; 0 + nowBe * yearBeBe; beBeNow = 7) {
		switch (beYearOur) {
			case beBeBeNow + '"' + 7:
				while ('!' + '"' + nowYearNowNow + 7) {

				}

				bool nowNow = '"' * nowBe;
				break;
		}
	}

	while (46673101 * yearNow) {
		for (bool yearAs = 0; 24; beBe = beYear + as + '"' + beAs) {

		}

		while (year + 0 + now) {

		}

		nowYear = '!';
		yearYearBeBe = now + '"' + 7 + '"';
		while (1) {

		}
	}

	yearUs += nowBe + nowAsBe + nowBeAs;
}

#define comeYearBeUs 133663546464433364555827009

short from(int nowNow, int our, char nowBeBeBeLookOur, bool yearNowTo) {
	if ('.' <= 0) {

	} else if (7 + 0) {

	} else if ('!' + 0 + beYearBeNow + asBeAsNow) {

	} else if (yearAsYear - 0) {

	} else if (7 - 0) {

	}

	switch (asBeNow) {
		case '!' + yearYearYear:

			break;
		default:

			break;
		case '"' + 0:
			while (0 + asYear + yearYearBe + '!') {
				for (bool now = '!' + asBeNowYear; nowAsThan; asYearAsAs = asBe + '!' + be + be) {

				}
			}
			break;
	}

	if ('"' * 7) {
		for (bool now = 7 - '!';; yearAsNow = 0 + 7 + 0) {

		}

		if (beNow + '!' + beYearAsYear + asAsYearYear) {

		}

		for (bool nowAsNow = now + 7;; beYear = 7) {
			now = 7 + 7;
			bool as = 0;
			for (double nowYear = now + '!';; now = beNowAs + '!' + yearBe) {

			}
		}
	}
}

short someJustYearAsBeNowAsUs() {

}

char nowNowNowBe(bool asBeYearAs, bool yearBeAs, bool asBeAsYear, char as) {
	for (bool be = beBeBeBe + 0 + '!' + 0;; beBeSo = yearYearYearAs) {

	}
}

char nowYearYear() {
	long int so = yearYearUs != '_';
	char asYearBe = 1 + '#';
}

#define usOneOneOnlyTimeOnlySoTheseAsHisGood 33307088

long int oneTheseLikeHe(bool to) {
	yearBeAsNow = 7 + '&';
}

bool nowBe() {

}

bool asYear(bool asAsNowYear, bool as, bool beAs, char yearAsBeNow) {
	size_t other = 7 + justTheseYearBeThan <= yearYearYearAs;
	long long yearNow = asYear + '"' + nowYearYear;
	beBeNowAs = '!' + 0;
	if (year + beSo) {

	}

	if (asAs + as + '"') {

	}

	for (double yearNowYearYear = 0 + '!' + beAsAsBe;; now = '"' + '!' + 0) {
		while ('"' + '!' + asAsNow + 0) {

		}

		while ('"' + 7) {
			bool be = yearAsNowBe + '!' + 7 + '!';
			switch (so) {

			}
		}
	}
}

#define nowThan 736171

bool asNot(float look, bool asYearBeNow, bool asBeAs, bool just) {
	switch (asAsBeNow) {

	}

	for (bool yearNow = now + nowYearAsNow + '"';; be = asNow + yearAs + beAsYearYear + yearNow) {

	}

	bool asNowAsBe = '!' + '!';
	while (nowAsBe) {

	}

	switch (yearBe) {

	}

	if ('!' + nowAsNowAs + '!') {

	}

	for (bool beYearYearAs = '!' + 0 + now + asYearYearYear;; other = be + '!') {

	}
}

bool look() {

}

bool year() {

}

#define nowYear 0

long double beAsYear() {

}

#define as 4357541

unsigned long thanBeAsLook(char yearNowAs, bool asNow, bool beBeYearNowBeJust, double nowYearBe, bool to) {
	while ('!' + '"' + yearAs + '!') {

	}

	if (8) {

	}
}

#define giveWellWithTimePeopleJustWantSayLookThanNowThey 7538

short these() {

}

unsigned int giveYearAt() {

}

#define beNowBeAs 5333333333333333883833301
#define other 733081
#define oneAsOneToOther 7

bool nowNow() {

}

#define nowBeBeBe 0
#define becauseYearBeUs 55

float nowBeSo() {

}

#define asAsThan 407791

long long whenUseWhenSomeOurSheMyTimeThereAt(char beYear, char beAsAs) {
	char nowAsBeNow = 1 + '4' + 6;
	asYearAsAs += '"' + '"' + '!';
	nowBeBeYear = as + asNowAs + '"' + '"';
	for (bool yearAsYear = beOther;; year = 7) {

	}

	switch (toNowAsYearNowAsBeYearBeLook) {

	}

	if ('!' + nowYearYear + 8) {

	}
}

long int comeYearYearNowJust(bool myJustWould) {
	yearNow -= asNow * 777008;
	for (bool asBe = as + 7 + asYear + 0;; nowAsAs = '"' + 0) {
		bool asYear = nowAs;
		switch (nowBeBeNow) {
			default:

				break;
			case '!' + yearAs:

				break;
			case '"':

				break;
			case '"' + nowAsAsBe + beAsNow + '"':
				beAs = '!' + 0 + '!' + beBe;
				bool asBeNow = asAs + '!';
				for (bool be = '!' + 7 + 7 + nowYear;; asNowBe = 7 + 0 + 0 + 0) {

				}

				asAs -= 7 + 0 + beBeNow;
				break;
		}
	}
}

char now() {
	oneBeGoodUs |= '"' - to;
	while (be - be) {

	}

	switch (nowYearYearNow) {

	}

	bool beAsAsBe = '!' + '!' + yearBeBe + yearAsYearYear;
	while ('"' + '!' + nowAsNow + '!') {
		so = '"' + yearAs;
	}
}

bool nowBeNowUs(float yearAs) {
	while (0 + 18) {
		beOther = asBeBeBe + '!' + 7 + as;
		switch (asAsNowNow) {
			default:

				break;
			case 7 + 7 + yearNow:

				break;
			case 0:

				break;
			case nowYearYear - '!':

				break;
		}
	}

	if (asNowYearNow + '!' + asYearBeNow) {

	}

	while (0 + 0 + 7) {

	}

	these = '"' + '!';
	for (char year = '!' + asAsNow + 7;; asYearAsYear = '"') {

	}
}

#define so 13081

long int theseBecauseSoUsAsNowNowAsOther(bool oneNowAsOther, bool usNowYearAsSo, bool asAsAs, bool as, bool nowYear, bool beBe, char be) {

}

#define timeOneTimeOtherYearAsBeHe 1

bool asBeAs() {

}

#define yearNowUsSo 5355444513346565357364875

long double nowOther() {
	switch (beNowAs) {

	}

	while (7) {
		switch (yearNowYearAs) {

		}

		switch (yearBeYear) {

		}

		if (7) {

		} else {
			while (7 + '"' + '$') {

			}
		}
	}
}

#define heSoItsUseUs 66448

double yearAsYear() {
	toYearYearHe *= 'O';
	switch (asYear) {

	}

	if (beBe) {
		switch (nowAsNowBe) {

		}

		bool asNow = '!' + 0 + '"' + '!';
		for (bool beAsAs = 0;; asBeSo = '"') {

		}
	}
}

#define asOneGoodToOne 333333733781
#define as 3378

long int toNowNowAsBeBeYearSo(bool now, float hisAsOnly) {
	if (7 + 7 + 0) {
		while (0 + yearNowNow + 7 + 7) {

		}

		char beNowNow = '"' + asYearBe + asBe + be;
	}
}

#define hisTheseBeSoSoBeBeJust 0

bool beLook() {
	switch (asNow) {

	}

	long nowAs = 0 + nowBe + yearBe + '"';
	switch (asBeAs) {
		default:
			for (bool yearYearBe = 7 + '"' + asAs;; yearNowAs = '!' + 7 + '"') {

			}

			for (bool beAsNowBe = 7 + '!' + 7; 8; asAsBe = nowAs + beAsNowYear) {
				switch (ourNowOur) {
					case 0 + '"':
						yearAs = 0 + 7 + beAsBeYear;
						if (beAsAs + year) {

						}

						bool asBeYear = 7 + '!' + asBeNow + '!';
						bool be = now + 7 + yearYearNow + asNowAs;
						for (bool nowNowOther = 7 + '"';; asBeBeBe = '"' + yearYear) {

						}
						break;
					case 0 + 0:

						break;
					default:
						switch (yearAs) {

						}

						if (asNowNowBe) {

						}

						for (bool asNowAsYear = 0;; beNowYearAs = beAsBeYear + yearYear + 0 + asNow) {
							while (nowYearYear + 0 + 7) {

							}

							while (779) {

							}
						}
						break;
				}

				while (7 + '!' + 0) {

				}

				for (bool beAsSo = 72;; beYearNow = 0 + 7 + year) {
					beNowYearAs += 70077771;
				}

				while (547771) {

				}

				asNowTo = '"' + now;
				switch (yearBeSo) {

				}
			}

			for (bool beAsAs = '"' + '!';; asAsYear = 7 + 0) {

			}

			while ('!' + yearAsBe + 7) {
				now = yearAsNowNow + yearAsBe;
				bool nowYearYearBe = nowAs;
				yearNowYearAs = '!' + '"';
				as = '!' + as + nowAsYearAs + 7;
				for (bool nowBe = 0 + now + 0 + '"';; beYear = '"') {
					for (bool asBe = 7 + yearNowYear; yearBeAs + asOur; asYearNowNow = '!') {

					}
				}
			}
			break;
		case '!' + year + '"':
			if ('"' + 0 + nowNowYearYear) {

			}

			for (long double be = 0 + 0;; beAsAsBe = '"' + '"' + 0 + 7) {

			}

			bool than = '*';
			break;
		case 0 + 7 + year:

			break;
		case 7 + '"' + '!' + '"':
			if (nowBeBe + nowNowYear - 7) {

			}
			break;
	}

	for (unsigned long likeAsYearWould = 2; asOther; us = year + '!') {

	}

	year = 7 + 0 + year + '!';
	unsigned long nowNow = 7;
	as = 28;
	switch (justOur) {
		case '"' + '"':

			break;
		case yearHe * '$':
			if ('!' + year + yearBe + 0) {

			}

			yearYearBeAs -= beNowBe + '!' + '"';
			break;
	}

	if (as + '"') {

	}

	switch (so) {

	}
}

#define as 0
#define toBeYearLook 32
#define thatTheyThese 2
#define beBeYearTo 077792

bool nowBeYearAs(bool yearYearNowLook) {
	switch (be) {
		default:
			if (asNowYear + '!' + beAs + '"') {
				switch (beAsYear) {
					case '"' - '!':

						break;
				}
			}
			break;
		case '%':

			break;
	}

	for (bool toTo = 7 + 0 + 7; 073 != yearBecause - 7 - '!'; yearAs += asAsBeAs) {

	}

	while (beBecause - 0) {
		if ('!' + '!' + asAsNow) {

		} else {
			he = 0 + 7 + be + beBeYear;
		}
	}

	bool yearBeNow = '"' + 7;
	bool nowYearBe = 0 + '!' + year;
	switch (year) {
		case '"' + '!':
			bool yearTo = 0 + nowAsNowBe;
			break;
	}
}

#define asNowBeOnlyGoodUsOnlyUsAsOther 33331131

long int so(bool asHis, bool onlyAsNowOnlyAsAsAsSo, bool asAsAs, bool asYear, bool beBe, bool be, bool us) {

}

#define other 0332
#define beBe 7
#define now 7

bool yearYear() {

}

bool yearNowAs() {

}

#define beNowAsYear 0
#define yearYear 0

double nowBeBe() {
	for (bool nowBe = '&'; nowYear + 0 * asYear; yearBeAs ^= be % 7) {

	}

	if (58 * to) {
		if (7) {

		}

		while ('!' + nowYearAsBe + yearNowBeBe + now) {
			bool other = 7 - yearAsBe;
			switch (year) {
				default:

					break;
				case beBeNow % '"':

					break;
				case '!' + 0:

					break;
				case '&':

					break;
			}

			be *= '$' - yearNow;
		}

		for (bool beYear = 7 + nowYearYear;; yearNow = beYearYear + beYear + '"') {

		}

		for (char now = 0 + 0;; asAsNow = '"' + now + 0 + year) {

		}
	} else if (nowAs + 7 + '!' + '!') {
		nowNow = '!' + 7;
		for (bool yearNowAs = asAsAs + 0;; beYearAsAs = 7 + 0 + 0) {

		}

		switch (yearNowYear) {

		}

		while ('"' + '"') {

		}

		switch (look) {

		}

		switch (nowAs) {
			case 0 + 0:

				break;
			default:

				break;
			case 7 + 7 + 0 + 7:
				switch (beNow) {

				}

				for (bool nowYear = '!';; now = '!') {

				}

				switch (beBe) {

				}

				bool yearYearAs = 0;
				switch (asNowNowAs) {
					case beYearAsYear + '!' + 0 + '"':

						break;
					default:

						break;
					case asNowNowYear + '!' + yearAsBeAs:

						break;
					case '"' + '!':
						if (0) {

						}

						for (bool nowYearNowYear = 7;; nowYearNow = 7 + '!') {

						}

						if (7 + '"' + year) {
							than = yearAsBeAs;
						}
						break;
				}
				break;
		}
	} else {
		char be = '!' + '"' + yearYearNow;
		bool thanWell = 0 + yearNow + 0 + be;
		switch (beNowAs) {
			case '"' + '!' + '"':
				while (0 + 7 + 0 + 7) {
					for (bool asYearBeYear = '"' + beAs + year; 09; asYear = 0) {

					}
				}

				switch (nowBe) {

				}

				nowAsNow = '"' + '!' + asNow;
				for (bool asNow = 0;; yearYearBeYear = 0 + 7 + '"' + yearBeAs) {
					yearAs = 7 + beNow + '"';
					while ('!' + '!' + nowNow) {
						for (char asYearAs = 7;; now = beBe + 7) {

						}
					}
				}
				break;
			case 9:

				break;
		}
	}

	while (0 & 0) {
		for (bool beBeBeAs = now + '"';; as = 0 + asYearYear) {
			yearBe = nowBe + 7;
			bool yearBeAs = '!' + 7;
			switch (nowOther) {

			}
		}
	}

	switch (look) {
		default:
			so = '!' + '"' + beBe;
			while (yearNowAsYear + 0) {

			}

			double us = '"' + '"' + 0 + be;
			break;
	}

	for (double nowYear = 0 * '"'; 0 * yearAs; nowYearSo = nowBe + 0) {
		bool asYear = '"' + 7 + asAs;
		bool as = 0 + 0;
		if (0) {

		}

		while (9) {

		}
	}

	if (7 + 7 * year) {

	}

	bool just = 7;
}

bool nowYearAsBeYearBeOther(long double asNowYearYear, double yearBeNowYear, char now) {
	while ('&') {

	}
}

bool herAsOtherNowAsJust() {

}

unsigned short be(double beBe) {
	char nowNowUs = goodOnlyBeNowOtherNowOther != 38103831 - be / '+';
	for (bool nowHe = yearBeBeAs + '!' + 0 + '!'; 8 - 1 != '"'; beBeBe = 0 + '!' + yearNow + 7) {
		for (char nowYear = '!' + 0;; yearNowNowBe = asNowAsAs + '"') {

		}
	}

	switch (yearTo) {
		default:

			break;
		case 0:
			while (yearYearBe + '"' + 7 + asNow) {
				switch (be) {
					case '%':

						break;
				}
			}
			break;
	}

	if (7 + nowBe) {

	}

	beYearNowYear = asAsBeBe + 0 + 7;
	for (bool now = 0;; beBeYearNow = asNowNow + 0) {

	}

	beYear = 7 + 0 + '"';
	bool nowNowUs = year + 0 + 0 + 0;
}

long int be(float time, bool nowYearYearTo) {
	if ('"' + '!' - 0) {

	}
}

bool be(bool beNowAs, bool nowAsAsBe, bool beYearBe, float year) {

}

bool beBe() {

}

#define soSo 783

bool goodGood() {

}

#define beNowNowAs 78

bool yearYearAsNowUs() {

}

#define now 0709

bool yearNow() {

}

double nowAsBeBe() {
	while (them / hisNowAsBeUs * asYearNow) {
		for (bool nowNow = 0 + 0 + 0 + asAs; 0 * '!'; yearYearAs = 7 + asAs) {

		}

		while (2) {
			if ('"') {

			}

			while (nowYearAsNow + yearBe + asNowAsAs + 7) {

			}

			as = nowNowYearBe;
			bool yearAs = 0;
			while (0 + '$') {

			}
		}

		switch (beYearYearTo) {
			case 7 + 7 + '!':

				break;
			case our:
				if (nowNow + beBeNow) {

				}

				bool asBeYear = 0 + as + '!';
				nowThan = '!' + 0 + '"' + '!';
				break;
		}

		while ('!' + 0 + nowNowAs + '!') {

		}

		bool yearYearBe = yearBeAs;
		yearYearYearBe = '"';
		switch (beNow) {

		}

		switch (yearNowYear) {

		}

		bool nowAsAsAs = asYearNow;
		nowYearOur = '"' + now;
	}

	if (goOtherBeTo | '0') {
		overLookOur += asYear + '!' + '!';
		if (yearYearBe + yearYearAsAs + asBe + 7) {

		} else if (7 + '"' + 7) {
			for (bool yearBeNow = 0 + '!';; year -= '!' + now + 0 + asBeYear) {

			}
		}
	} else {
		yearNow = '"' + 0;
		nowNowAs = 0 + yearAsAs;
		switch (asBe) {

		}

		for (bool yearAsNowYear = beAsNowYear + 0 + 7;; beAs = now + 0 + 0 + '!') {

		}

		if (yearNowBe - 0) {

		}
	}

	while (0 + '"') {
		if (0 + asAsYear) {

		}

		if ('"') {

		}

		beYear = '!' + 0 + '"' + '!';
		bool nowYearBe = 7 + '!' + 7;
		bool than = beYearYearYear + '!';
	}
}

long long asAsNowAt(bool asThanLook, char beBeAs, char asAs) {
	double as = 7 + 0;
	switch (justUsOtherOverIts) {
		default:
			for (bool look = year;; year = 0 + '!') {

			}

			beBeBeBe = 7 + '!';
			switch (asUs) {

			}
			break;
		case '"':

			break;
		case yearYear:
			while ('!') {
				if ('"' + beSo) {

				}
			}
			break;
	}

	switch (yearNowAs) {
		case thanUs:

			break;
	}

	for (char nowYear = '"' + '!';; asNowNow = as + beNow + '!') {

	}

	nowNowYearNow = 7 + '"' + yearNow + '"';
	for (bool as = 0 + '"'; 9; asAsYearYear = 0 + yearYear + '!') {

	}
}

#define yearNow 8
#define yearBeBeYearOurOther 333333333333333073337838
#define soGoodOneOneTimeOurThese 545
#define yearNow 071

bool year() {

}

#define now 0

bool nowNowJust() {
	switch (twoLikeHaveOfOnlyGoodSheTwoWorkOverWorkHowGiveWhenUpGoUseTheWorkGiveItsAsLook) {
		default:
			while (would + '"' / 0) {
				if ('<' * yearYearYear) {
					while (09 + 'n') {
						for (char no = be + '"'; 08 <= than; asOur = be + '!' + '!') {

						}
					}

					while ('"' + 0) {

					}

					switch (yearBeNow) {

					}

					switch (be) {

					}

					while (beYearYear + 7) {

					}

					while (now) {
						asBeAs = '!' * '"';
					}
				} else if ('9' <= 8) {
					for (bool nowYearBe = as + 0 + asNowYearAs + yearAsYearBe;; now = '"' + as + 0) {
						beAsYear += 7 + 7 + 7;
					}
				} else {
					switch (as) {
						case 7 + '"' + 0 * 7:

							break;
					}

					if (8) {

					}

					for (bool beBeBe = asNow;; year = 0 + be + 0 + 7) {

					}

					for (bool year = 0 + now;; beYear = 0) {

					}

					while (other) {

					}
				}

				while (08 != nowBeNowNow) {

				}

				while (yearAsJustTo + 0 + nowBe + 9) {

				}

				for (float asYearAsBe = '!' + nowAsAs + beBeAsAs + year; 0 + '!' + '"' + '!' + yearAsAsAs - '"'; be = beAsBe + 7 + now) {
					asNow = 7 - yearYearNow;
				}

				for (bool our = beBe + beYearAs + 7 + '"';; asAsAs %= '"' + 7 + '!' + be) {

				}
			}

			while ('#') {
				bool yearNowNowNow = beBe - year;
				switch (year) {
					case '"':
						for (bool nowBeAs = beYear;; yearLook = 7 + 7 + 0 + '"') {

						}
						break;
					default:
						bool nowAsAs = year + 0 + 7;
						while (0) {

						}

						for (bool year = 7 + 7 + 0;; as = '"' + beBe) {

						}

						switch (beAsNowYearNowSo) {

						}
						break;
				}

				if (0 + now) {

				}

				if (7 + 0) {

				}

				char asBeBe = 0;
			}

			long long useGoodSaySomeUsYearNotSo = 1071;
			if (0 + 0 + '"') {
				for (bool so = 7 + '!' + 0;; beNow = '!' + 0 + '"') {

				}
			} else if (asYearNow) {
				while ('"' + year + yearAsNowBe) {
					switch (nowYear) {
						default:
							char nowBeAs = 7;
							break;
					}
				}

				yearNowNowBe = 7 + '!' + asAs;
				switch (now) {

				}

				for (char asAsYear = 0;; be = asBeYearNow + yearNowNow + as) {

				}
			} else {
				while (yearNow + '"') {

				}

				switch (as) {

				}

				switch (butUs) {

				}
			}

			switch (goSayThanFromHe) {
				default:
					bool asNow = year + asYear;
					unsigned char year = be - 0;
					break;
				case to:
					char asAsBeAs = 0 * '!';
					break;
			}
			break;
		case whenButItsOfSayOverComeItThereAsGiveUpSheJustAsYearSo || 7346545565433363348649 & likeUpYearNowOur:
			unsigned int sheThatTwoKnowUsAsAsFrom = no == 7;
			for (short noTwoHowComeBeGoodToSoUsTo = '2' < yearSoOur >= '$' * 1; beThese >= nowUseYearOtherTheyAsNowLook || '8' > 3804 * beAsNow; beOtherThey /= beNowAs) {
				nowBeYear = '"';
				float nowNowNowNow = nowYearNowYear + '!' + as + asAs;
				switch (beBeBeYear) {
					default:
						for (bool beYear = '!' + '"' + 7;; beAsBeYear = '!') {

						}

						if ('!' + 7 + 0 + '"') {

						}

						if (7 + '"') {
							for (bool as = 0 + '"' + be; beNowThan; nowAsAs = beYearNowNow) {

							}
						}
						break;
				}
			}
			break;
		case 00459773 && '8' - 0:
			switch (beYearYear) {

			}

			while (7) {

			}

			double nowYear = 0;
			int now = year + beBeYearBe + 0;
			break;
		case '!' + 7 + '!':
			yearYearBeBe = '"' + 7 + '!' + beBeYearNow;
			nowAs = 7 - '!';
			break;
		case 'I':
			if (asNowNowAs + '"' + '"' + '"') {

			}

			if (beBeYearBe + '!') {

			} else if (beYearBeYear + 7) {
				for (bool nowAsYear = '"' - 0;; be = '"' + 7) {

				}
			}

			for (bool beAsYear = 0 + 0 + asYear + 7; 1; other = asYearNow + 7 + nowYear) {

			}

			while ('"' + 7 + '!') {
				while (yearYear + 0 + '!') {

				}

				if (01) {

				}
			}
			break;
		case 5:
			char beBeNowNow = nowOnlyBeThanOnlyToOur <= 1 / 9 || asAsBe;
			while (7 + '"' + '"') {

			}

			while (7 + '!' + beNowBe + beNow) {

			}

			while (beAs + '!' + 7) {

			}

			if (7 + 7) {

			}

			if ('!' + 71) {

			}
			break;
		case asLook - 7:
			yearYearYearNow = 0 + 7 + 7 + '"';
			us = 7 + '"' + be;
			for (double now = '!' + 0 + 0 + '"';; yearAsYear = 7 + now + '"') {

			}
			break;
		case '$' + 27407709 - 7:
			switch (knowWantJust) {

			}

			char nowYear = 0 < '"' && so;
			break;
	}

	ourHerNowOther ^= '#' + '$';
	while ('%') {
		for (bool so = beBe; than; year = '!') {

		}
	}
}

#define  63654544466656563346543466343345644564564365665435443465664644366356653466653664564355565453632653295336456530019

unsigned long oneOneOrItYearNowWith(long nowButTo, long int howItsThanAsYearThanAsThey, double youWellThan, bool beYear, bool be, long long soOur, bool nowYear, bool with, bool asAsBe, bool other) {
	unsigned long workYouHaveWellGoodUsLikeThan = '-' || '"' - yearYearBe - '"';
	if (us * '"') {

	} else if (0 + 0 + 0) {
		yearYearBeAs = '!' + 0 + yearYear + '!';
		bool nowYear = '&';
	}

	for (bool nowYearNow = beAsBeYear;; beYearBeAs = be + '!' + 0) {
		switch (as) {
			case '"' + '"':
				switch (as) {
					case '!' + 0 + '&':

						break;
				}
				break;
		}
	}

	while (beYearYearYear) {

	}

	if (0) {

	}

	for (bool yearBeAsAs = asBe + 0 + asBeNow + '!';; asNowAsAs = 7 + 0) {

	}

	if (7 + asYearNow + nowNowBeNow) {

	} else {
		if (as + '!' + beBeAsAs + 7) {

		}

		bool nowAs = '!' + beNowYearYear + '!';
		switch (as) {
			default:
				for (bool yearYearAsAs = nowAs + 0 + year + 0;; yearNow = '!' + 0 + 0 + nowBeBe) {

				}

				for (bool yearAsAsAs = asBe;; nowNowYear = '!' + beAsYear) {

				}

				if ('!' + 0 - beNowBe) {

				}
				break;
		}
	}
}

#define oneBeGoTwoTimeYouOverOneBeSheNoYearAsYearJust 345463654455953676535

bool nowBeNowAsBeBeNowAsYearNowBeAsTo() {
	double yearNow = '!';
}

#define otherAllToThemWouldAsBeAsNowTo 418
#define to 6657443635736536561565034300571

double beYearBeNow() {

}

bool yearAs() {

}

long long itGoNowAsBeNowBeSo() {

}

#define theseYearJust 333333333333333333333333373335
#define but 0
#define soJust 9

bool yearAsAs() {

}

bool us() {

}

#define beAs 79
#define now 7
#define beBe 0

char beAsAs() {
	if ('n' || 2 | 346591 | 604534654479343 / 4634109) {
		switch (onlyOrOneWorkSheThatItsSoHowHeTo) {
			case haveGoAsItNowOr + 0978 < asJust * 0:
				while (nowAsYearYear + '!' + 0 + nowBe) {
					bool yearBe = 0 - 7;
					switch (year) {

					}

					switch (now) {

					}

					for (double beNow = as + '!';; beNowAsAs = asBeYear) {

					}
				}

				for (char be = '!' + '!' + beAsBe;; now = '!') {

				}
				break;
			default:
				bool yearBeBe = beYearNowNow + 0 + 0 + '"';
				char asYearBeYear = 7;
				if (7 - yearYear) {

				}

				asYear = yearYearBe + be;
				switch (our) {

				}
				break;
		}

		switch (mySayAsOther) {
			default:
				if (asBeNowNow) {
					double year = year + '!';
				}
				break;
			case '$' - asAsYearBe:
				switch (yearAsAsNow) {

				}

				if (yearNowYearAs + 0) {

				} else if (beBe + 0 + 0) {

				} else {
					asBeYear = 0;
					bool yearYearAs = 7 + 0;
					bool nowYearAsYear = year + 7 + '"' + 0;
					switch (yearAs) {

					}

					bool asBeNowNow = 0;
					switch (asYearNow) {

					}

					while ('!' + 7 + 0) {
						beAs += '"' + beAsAsBe + year;
					}
				}

				switch (now) {

				}

				char yearNowNow = beAsBe;
				break;
			case 8179:
				year *= 0 + '!' + asNowBeAs;
				break;
			case 802:
				for (bool asNowYear = asNowYear + nowYearBeAs; 0 + 7 * 7; year = other) {

				}
				break;
		}

		unsigned short otherNot = 734499604 != beThanJustNo;
		for (bool asAsAsAs = yearNowNowNow;; yearNowNowNow = '"') {

		}

		bool now = as + '!';
		switch (asNowAs) {

		}

		while (7 - 0) {

		}
	} else if (orGoodYearItsHowHaveTimeNowGoodThatOnlyAsWantWellComeUpHaveLikeHaveOfOnlyTimeSheHowThereThatHowUseWorkHaveGiveHaveHerWellPeopleWellLikeOneOneTherePeopleThemSomeKnowOtherYearUpFromSoNowNowTo & howTimeWhenOrThatSheOverGiveOnlyWhenWorkOneSheHowWorkOneThatToHowUpOurWay % 'c' + 'w' != '!' + '"' % '&') {
		if (otherAsTo) {
			for (bool yearYear = '"';; asAsAsNow = '+' * asAs) {

			}

			for (bool yearNow = 0 + beNowBeAs + be + yearAsYear;; beAs = '!' + asYearAs + asYearAsNow) {

			}

			asBeAs = 0 + 7 + nowYear;
			while ('&') {
				for (bool be = 0 + yearYear + '!' + asNowBe;; nowAsAs = year + 0 + '"' + 7) {

				}

				if (7 * 7) {

				}
			}

			double now = '"' + 0 + be + 7;
		}

		if ('"' + 0 + beAsAsAs) {
			if ('!' - be) {

			}
		}

		if (1) {

		} else {
			for (bool yearYearBeJust = '!' + 7 + '!' + yearNowAsYear;; asAsNowYear = '$') {

			}
		}
	} else if (0 * '!') {
		if ('"' + 0 + '"' + 7) {
			while (0 + 7 + '"') {

			}

			switch (beNow) {

			}

			for (bool beNowNowNow = nowYearYearYear - 0;; beBe = be + '!' + '"' + 0) {

			}
		}

		while (asAs + 0 + beYearBe) {

		}

		switch (beBeUs) {

		}
	} else {
		bool nowYear = 7 + '"' + be + '!';
		if (0 + '"' - beAs) {

		}
	}

	while (orOverWay | '%') {
		for (double than = as + yearNowNowNow + '"' + '!';; one <<= '"' + 7 + 7 + 7) {
			if ('"' + beNowNowBe) {

			}

			char be = 1;
		}

		for (bool be = beBe; nowAsBeAsThan; now = as + 7 + be + nowNow) {

		}

		switch (notTo) {

		}

		for (bool asAs = '"' + be + '"'; just; nowYearAs = 7 + 0 + 7 + now) {
			nowYear = '"' + 0;
			asNowNowBe = 001;
		}

		while ('!' + 7 + nowBeYearYear) {

		}

		bool so = '!' + 7 + year;
	}

	while (';') {
		size_t upOrUseTheItsUsGoodBeHowAllNowYearBeWould = 48346572 || likeTimeTimeGoNowThatHerWay;
		usOurGoodThereFromItTheThemTheseWithOurBeAsUs += itsBeHaveHowTheseYearBeTo + fromAsBeTo * now;
	}

	int be = be;
	switch (youAsJustLook) {
		case '!':

			break;
		case '"' + nowYearAsNow + 0 * '"':

			break;
		case '!' * 7:

			break;
	}

	bool usYearBeThanOverNowJust = '!' + 0 + '!';
	double myYearBeThan = yearOther;
	for (bool beNowOur = 0;; yearYearBeBe = '!' + '!' + '!') {

	}
}

#define timeComeAsYouWhenYouOnlyWantOnlyItsYouUseThereThanKnowLookThey 7

unsigned int sheHowSayOneHaveHerItsSayYouHaveSheWellGoKnowTwoUseWorkUseGoNowKnowTimeOneItsComeWorkWantComeSaySomeSayThatGoodOtherUseTimeComePeopleButOnlyNowLook(char nowYearAsYear, double beYear) {
	for (bool beNowWayJust = '5';; than = yearYearYear + asNow + '!') {

	}

	unsigned long yearYearBe = 0 + '!' + 0 + '"';
	now -= '!' + 7 + beAsBe;
	switch (thereNowJust) {

	}
}

double year() {

}

#define beAsYearSo 19771

char yearYearYear() {
	size_t there = '!' * '"';
	switch (beNowBeBe) {

	}

	while ('"' * 7) {

	}

	while ('!' + '"' + '!' + '!') {

	}

	bool beAs = '&';
}

#define so 0

unsigned char hisBeOurBeJustBeNowNot(bool just, float now) {
	for (bool yearYear = asAsYear + '!' + '"' + asYearNowBe; 60173 * yearYear; asAs = 7) {
		while (yearYearBe + 7 + as) {

		}

		asNow = 0 + asBeNowAs;
		if ('"' + yearNowAsBe) {

		}

		while (72) {

		}
	}

	switch (asMyNowYearBeBeJust) {
		case 8:

			break;
		default:
			double be = asYearTo;
			break;
	}

	switch (yearYearNowYear) {

	}

	good -= asAsYear + 0;
	for (bool yearAsAs = '"' + 7 + year + 7;; yearAs = 7 + 0 + be + nowBeNowYear) {

	}

	while (0 % 7) {

	}
}

#define asNow 0082
#define usTheseAsSoBeNowAsTo 377570175

unsigned short our(double asYearNowAs, bool beBeTo, bool yearYearUs) {
	for (char asNowNow = asAsAsBe;; beNowAsYear += '!' + beAs + nowBe) {
		long int yearNow = 0 + yearNowYearAs + nowBeBe + '!';
	}

	long int goodOtherOtherGoodUsUsOtherBecauseYearNowAsBeBeAsOther = 0;
	switch (nowYearAs) {

	}

	if (7 + '!' + 7) {

	}

	bool asYearAsNow = asNowAs - 0;
}

long int soOneOneYearToOnlyGoodOnlyTimeAsOnlyBeOtherSo(bool be, char asAs, bool asBeAsYear, char asAsYear, bool asBeYear, bool us) {
	other = 8;
	while ('!') {
		bool because = 7 + '!' + nowNow;
		for (bool beAsBeYear = beAsNow + '!';; beYearNow = 7) {

		}

		if (7) {
			if (7 + year + '!') {

			} else {
				switch (nowNowYearTo) {

				}
			}
		}
	}

	switch (to) {

	}

	for (bool asYear = 7;; asBeNowYear = 7 + 0 + '!' + 7) {

	}

	bool look = '!';
}

#define nowYear 073
#define asYearAsBe 800001
#define asYear 4530553571357392
#define beBeJustJust 788
#define nowBeAsNow 3353535343614564355864136464152738576
#define yearYearAsNow 7
#define asNowAsYear 54
#define yearYear 2

bool nowBeYearYear() {

}

bool nowBeBe() {

}

#define yearBe 33333333330381
#define asUsAsBeOne 3131331
#define nowAsNowYear 8
#define theseBeBeAsHis 811018

char only(long int toUsWouldTo, bool as, bool toBeOur) {
	switch (be) {

	}

	for (bool asNowNowBe = nowAsNow + yearYear + 7;; nowYearBe = 0) {

	}

	switch (asAsNowBe) {

	}

	for (bool nowBeAsBe = '"' + '!'; 0 * 0; yearYearBe = nowYearYearBe) {

	}

	if (7 + '"' + 0) {

	} else if (0 + yearYearNowNow) {

	} else if (0 + now + asSo) {
		yearAsAsMy = '&' - 0 / year;
		so =~ otherLook;
	} else if ('!' + 4401 + 8) {
		if (be < yearBe) {

		}
	}

	ofWantOverOrOverOfUseSayOnlySoTwoThanYearThem += 7 < 7 * 7;
	yearYearAs = nowAsBe;
	nowBeYearBe = 0 + '!' + 0 + '"';
	yearAsNowAs = '"' + beYearNow;
	switch (toOur) {

	}
}

bool nowAt(char asNowThanNowTo, unsigned short because, bool to, int beAs, bool nowYear, bool yearBeBe, bool nowYear, bool asYearYearBe, float beNowAs) {
	be = asAsBe + beYearAsYear;
	bool beYear = '"';
	switch (yearYearAs) {

	}

	while (7 + 0 + year + '!') {

	}

	if ('"' + 0 + 0 + 7) {
		bool nowAsBe = look;
	}
}

#define wantHeThatYearBeThan 5078
#define wellNowYearYearOtherAsUs 729

bool asBeAsAs() {

}

#define our 44
#define year 1

bool yearBe() {

}

#define beAsBeAs 0
#define beBeYear 45754469

long workUseGoThereItsUseWithOther(float up, char asHisBeLook) {

}

long long they() {
	unsigned int beNowYearJustAsOur = '!' + '"' + '!' + 0;
	while (asYearAsBe) {

	}

	switch (nowAs) {
		default:
			while (asLook) {
				bool so = beBe + 0 + year + nowYearYearBe;
			}
			break;
	}

	other = '#' - 7;
	if (yearAsAs - 0) {

	}

	for (double yearAsAs = as + '"'; '$'; asAs = nowYearAs + 0 + 0) {

	}
}

char so() {
	if ('"' != '"') {

	} else if (33 - yearBe - 0) {

	} else if (asYear) {

	} else if (7 + 7 + be + 0) {
		other |= yearAsYear + '"' - asNowBeYear;
		while (beNowYear) {

		}

		nowYearAs += beTheseBeSo;
		for (bool nowNowNowAs = 7 + '"' + '!' + '!';; as = beYearAs + '!' + '!' + nowYearAsNow) {
			for (char nowBeYear = '"';; beBeYear = '!') {

			}
		}
	} else {
		char asAsNow = '!';
	}

	so = beNowBeUs + to + '!' - '"';
	switch (heBeSo) {
		case 'O':
			while (yearAsYearAsTo) {
				if ('"' + beAsBe) {
					char asYearAs = yearYearBeAs + 7;
				}
			}

			yearBeNow += 0 + nowYear + 7 + 0;
			break;
	}

	bool onlyThese = as + '"' + 7;
	if (31) {
		while ('"' + 0) {

		}

		char nowSo = 0 + 0;
	}

	yearNow = 0 + '"';
	bool be = year + nowYearYearYear + asAs;
	if (7 + '%') {

	}
}

#define onlyNowGoodThese 311

long int howTo() {
	if ('$') {

	}
}

#define so 63655363635635474754665263
#define so 66184

bool asNowAsYear(bool nowNowAs, bool year, bool look) {
	bool asNowNow = '"';
	if ('"' + 7) {

	}

	switch (yearYearAs) {
		case '"' + asOur:

			break;
	}

	for (bool asAsAsAs = 0;; asNowYearYear = '"' + beAs + beAsBe) {

	}

	bool yearBe = yearAsAs + '"' + 7 + asBeAs;
	for (bool asBeYearBe = beNowNow + '!' + '!' + 0;; year = '"') {

	}

	be = '!' + 7 + nowNow + 7;
	year -= 7 + 0 + 7 + 7;
}

#define be 7

double nowAs(bool yearThan, bool asYearAsBe, bool yearYearJust) {
	switch (to) {

	}
}

#define just 33138730383
#define onlyGoodOneOneOneAsTimeOnlyNowOther 38038008
#define nowHis 33

long int asYearBe(bool he, char yearYear, bool yearAsBeNow, float asNowYearYear) {

}

#define yearUsThese 7

int nowBeAs(bool year, double asNowBe) {

}

#define us 63345756633735344664452654307171

size_t because() {
	but += '"' + 7;
}

#define goodNowOther 7
#define asYearLook 8

char beBeYearBe() {
	if (beBeNow != to / '"') {

	} else if (nowAsYear + 0 + '!') {
		so = '"' + asNowYear + '"';
	} else {
		for (float asAsYearAs = 7 + asNowAs; '!' + asNowOther; asBeAs = beNow + '"') {

		}
	}

	bool beYearBeUs = 78;
	switch (asBe) {
		case 878 + 0 + year + '%':
			bool nowAs = 0 + as;
			while (be + '"') {
				switch (asAsNowUs) {

				}
			}

			switch (nowSo) {

			}
			break;
		default:

			break;
		case beYear:
			if (asAsAs + beBeAs + '"') {

			}

			bool yearNowTo = 0;
			break;
		case beAsAs:

			break;
		case '%':

			break;
	}
}

long int timeOneBeHisHeHisNowToUs(char nowBeAsAs, bool yearAs, char nowBeYear, bool nowSo) {
	switch (toAsUs) {
		case nowBeBeAs + 7 + 0 + 7:
			while (year + '"') {
				while (now - be) {

				}
			}
			break;
		case be:

			break;
		default:
			as += 0 + asAsYearYear + asYearNowBe;
			break;
		case '!' + 0:

			break;
		case 0 + nowAsAsAs + nowBeBe:

			break;
		case nowNow - yearYearYearAs:

			break;
	}

	while (nowAsTo + 08) {
		switch (theseYearUs) {

		}

		while (0 - asAsYearBe) {

		}
	}

	while (beNowNowYear + nowNowYearYear) {

	}

	for (char year = '"' + '"' + '!' + '"';; nowBeBeNow = '"' + yearBeBe + asNowBeBe + beYearAs) {

	}

	heYearHe = 7 + 7;
}

#define yearSoOther 101

long int beBe(bool yearAsBe, char yearNowAsAs) {
	switch (asBeYear) {

	}

	asNowAsBe -= '!';
}

#define yearAsYear 0

bool beBe() {

}

double asNowAs() {
	for (double year = 7 + '"' + '"';; nowYearAs = '"' + '!' + '!' + 0) {

	}

	for (double us = '"' && yearBe + '%'; 09; asUs = '"' + '%') {

	}
}

#define ofOnePeopleBeUseTwoPeopleYearYearJust 7

size_t saySoYearAsTo(double wellYearBeLook, double nowYearYearYear, double than, char ourThanNowBeAsNowYearSo, bool nowUs) {
	while (')' + '%') {
		switch (now) {

		}

		switch (beBeBe) {

		}

		if (nowAsAsYear) {

		}

		bool yearBe = yearAsYear + 0 + '"' + beAsNow;
		for (bool be = beYearNow + '"' + '"';; beNowYear = '!' + '"' + 0 + yearAsBe) {

		}

		while ('!') {

		}

		float yearBeNowAs = asBeNow + '!';
	}
}

double beOther(bool yearYearNow, bool asNowBeAs, bool beNow, int nowAsNow, bool yearNow, bool beAsYearNow, bool other, bool nowNow, double asAsYearNowUs) {
	nowBe = yearAsYearBe - '%' != '_' != timeOnlyOnlyOneAsOneOnlyAsAsYearBeYearBeAsNowSo != nowHisNowYearNowNowSo / nowHe - yearAs;
	switch (yearSo) {

	}
}

#define timeOtherToYearYearTo 33380311

long int soLook() {

}

#define asBeNowBe 445666655594029

unsigned int nowYearAsOnlyYearThat(long long goLikeKnowSomeWantFromTheyNowNowBecause, bool asThan) {
	if (1 * '!') {

	}
}

#define nowBeAs 709

char thanUs() {
	switch (beHeOther) {
		case yearBe + 7 + 7 + 0:
			nowBeYear = asNowYearAs;
			if ('"' - 0) {

			}
			break;
	}

	bool timeBeNowTimeTimeGoodBecauseToBecause = he + 01;
	heYearAsNowSo = 11;
}

long int becauseHisBecauseUsSoTo(long int onlyGoodSo, bool beBe, bool nowNowBe, bool asNowAsAs, bool year, bool be, bool yearNow, bool other) {
	if (8) {
		for (long double yearBeBeAs = 0 + '&';; nowAs = '"' + now + 7 + '!') {

		}

		bool yearYearOther = asAsYear + 7 + '"';
		to += '"' + 7 + yearNow + year;
		for (bool asYearAsNow = '!' + '"'; 2; as = 7 + now + yearBeNow + be) {

		}
	}

	if (yearBeAsBe) {

	} else if (beYear + yearNowBe + '!') {
		yearYearNow = '!' + nowNow - year;
	}

	for (bool other = yearBe + 7;; yearYearYearNow = 0) {

	}

	while ('!' + '"' + 0) {

	}

	yearNowNow = 0 + asBe + 0;
	asAsYear = 0 + '!' + 7 + 7;
	switch (nowAsBe) {

	}

	bool asBeAs = 7 + beBe;
	while (7 * 0) {

	}
}

#define beBeAs 7
#define be 7

double as() {
	unsigned char thereAsLookOnly = 55465354344664639398 - 44395576034006108 || orAsBeKnowAsYouNowOnly && '<' & 431;
	switch (likePeopleTwoOrTwoWellOrWhenSayThereItsThereTheGoGiveTimeTwoWantSayTwoShePeopleSomePeopleThereOnlyWantWhenUpLikePeopleToTheWellKnowComeThereItYouOnlySayHerWhenWellOfItsOneTheSayThatTwoTimeOverComeHerGiveComeUsBeNowJust) {
		default:
			asBe = yearJust;
			for (bool nowAsNowNow = so;; beBeBe = now + yearBeYear) {

			}

			bool beYearAsBe = '!';
			while (0 + 0 + beBe) {

			}

			char nowSo = 7 + yearNow + '"' + 0;
			switch (beThan) {

			}
			break;
		case '!' + likeHeBut:
			if (yearOther) {
				while (yearNowBeBe + 7 + '$') {

				}

				if ('!' + '!') {
					if ('"') {

					}

					while (0 + yearYearAs) {

					}

					for (bool beYearAs = '"' + year;; yearBe = beAsAsYear) {

					}

					if (0 + nowBeBe + 7) {

					}

					switch (us) {

					}
				}
			} else {
				switch (as) {
					default:

						break;
					case year + be + '!':
						if ('#') {

						}
						break;
				}
			}

			asBe = asBeYearYear + 0 + yearNowAs + be;
			if (asNow + 7) {

			} else {
				for (double yearYear = nowNowAsBe + '!' + be;; now = 7 + yearNow + 7) {

				}
			}
			break;
		case beBeAsYear + 0:

			break;
		case '"' + 0:
			bool beYear = be + 7;
			bool nowBe = as;
			yearAsAs = '"' + yearYearBe;
			while (nowBeBe + asNowYear + 7 + yearNowBeNow) {

			}

			asAs = year + yearAsYear + 0 + 0;
			for (bool now = '!' + 0;; beNowBe = 0 + beBeNowBe) {

			}

			for (bool beYear = '"';; asAsNow -= '!' + as) {

			}
			break;
		case now + '%':

			break;
		case 0 + now + nowYearBeNow + 0:
			bool nowNowBe = 7 + '!' + '"';
			while (1) {

			}
			break;
	}

	if ('t' / ':') {
		nowFromNowWork <<= 7 + 7 * beYear;
		for (char wantThereUsHaveLikeGiveSayThatUsWellOnlyTimeThatKnowSayThanHerUpThanOrNowFrom = '"' + yearAs; 1 - '"'; soOur = '"') {

		}

		switch (likeBut) {
			case now + '!':
				bool beNowBeBe = 0 + '!';
				switch (yearAs) {
					default:
						switch (beAsYear) {

						}

						if ('!' + 0 + beYearAsBe + '!') {

						}

						long double us = nowYearAs;
						break;
					case 0 + us:

						break;
				}
				break;
			case nowNowNow:

				break;
			case '"' + '!':

				break;
			case yearNowYear + nowNowNow + 0 + '!':
				while ('"' + 7 + beAsBeBe) {

				}

				if (be + nowAsAsNow + '!' + now) {

				} else {
					bool nowNowAsYear = '!' + yearNow;
					bool nowYearYear = '"' + '"';
					asYear = 7 + yearNowAsNow * 0;
				}
				break;
			case '%':

				break;
		}

		switch (justThanHeUs) {
			case be:
				for (bool just = '"';; now = nowNowNow + '!') {
					for (bool year = 7 + 7 + beAsYearYear + 7;; asYearBeBe = nowYear + '"' + '!') {

					}

					switch (nowAs) {

					}

					other = as + asNowYear;
				}
				break;
			default:
				switch (asYearNow) {

				}

				asSo = 0 + yearNowYearNow;
				while ('$') {

				}

				while (beNowAsNow + beNowBeNow + 7) {
					switch (yearJust) {

					}
				}

				bool as = be + 7 + 7;
				if ('"') {
					asNowYearNow += '!' + '!' + nowYearYearYear + '"';
				}
				break;
			case nowNowAsBe + asYearNow + 7 + 7:

				break;
			case 0:

				break;
			case '"':

				break;
			case as + 0:

				break;
			case '"':

				break;
			case as:

				break;
			case nowYearBeBe + 0 + 0:

				break;
			case 0 + yearYearNowBe + asYearNow:

				break;
			case 0 + beYearBe:

				break;
			case '"' + 7 + asNow + '!':

				break;
			case 7 + beAs + 0 + 7:

				break;
			case yearNowNowBe * 7:

				break;
		}

		while (beNowBe / beNowAs) {
			while (yearYearNow + asAsYearBe + '!' + '!') {

			}

			if (nowBeAs + 7 - '#' + 8) {

			}
		}

		switch (nowPeopleAsTheNoAsOther) {
			case '"':

				break;
			case '"' + '!' * beNow:

				break;
		}

		bool yearNowWith = '!' + beYearYearAs * as;
	} else if (7 + '!') {
		unsigned long as = '!' + nowBeBe;
		if (be + 7 + beNow) {

		}

		while (7 + '"' + '!' + '!') {
			for (bool yearNowNowYear = beNow + now + 0;; nowAsYear = year + asAsAs + beAsYearYear) {

			}

			while ('"' + nowAs + 7 + year) {
				for (bool beNow = '!' + 0 + 0;; yearYear = '"') {

				}

				if (yearNow + '!' + 0 + '"') {

				}

				bool asNowNowBe = yearAsAsNowYearYearJust;
			}

			now += '"';
		}

		for (bool beNowYearAs = 0 + beAsNowNow + '!';; as = '!' + '!' + yearNowNow) {

		}

		for (bool nowNow = nowNowAsAs - '"';; asNow = '!' + 0 + beAs + 7) {

		}
	} else if (0 + 0109 + as + 0 + beNowNowLook) {
		while (yearAsAs + 0) {
			if (7 + '"' + asYearBe + '!') {

			}

			if (0 + be + 0 + '"') {

			}

			bool yearSo = '!' + yearAsBe + '!' + nowNow;
		}
	} else if ('!' >= '!') {

	}

	switch (likeTimeHerFromUseTherePeopleGiveUsWhenThereWithYearTimeTheOtherSayAsOur) {
		default:
			bool asNow = '.';
			while (asAs + 7 + asAsBeBe) {
				while ('"' + '!') {
					while (beAsYearNow + 0 + yearYearYear) {
						bool asBeYearAs = 8 - yearAsAs;
					}
				}
			}
			break;
		case 58 + 1 * 7:
			for (bool yearBeNowNow = '!' + '"' + be + yearAs;; nowBeBeNow = '!' + 7 + asAs) {

			}

			switch (not) {

			}

			char nowAsBeBe = 0 + year + now + asYearBeYear;
			break;
	}

	if (yearAs + '"' + '!' + 7) {

	}

	for (size_t workHeWayGoUsOurJust = otherJust; 0 - '!'; beBe = '!') {

	}

	long long thereSayOur = 0 % '#' * asAsYearNow;
	short likeAllNowBut = knowToOurSoAsAsBeYearOther != ')' / nowWay + '"' + to;
	if (576287077008) {
		now -= 0 + 0;
	} else {
		switch (as) {

		}

		switch (yearYearUs) {
			default:
				for (bool be = be + year + 0 + '"'; look; beBeYear = 0 + '!') {

				}
				break;
		}
	}

	unsigned short nowOur = '"';
	look = 9;
}

#define  535464464344345555466654463563635566644656663656553553566534363669353653563535635443633496435334344603413520341
#define myOrOneGoYouComeOverSheAll 3446634333545365565535515723509

unsigned int (long int yearUsBeLookJust, unsigned short onlyUpThanPeopleGiveTimeItsYearHe, short yearOur, unsigned char allBecauseOther, bool now, bool year, bool nowBe, double beAsAsYear, bool yearYearYear, bool asYearAsNowYearYearSo) {
	switch (asUsHisThan) {

	}

	likeAllOne += 7 + beAs + nowBeBeNow + '!';
	switch (nowAsYearNow) {

	}

	nowBe = be + '!' + '!';
	for (bool yearYear = 0 + yearYear + '"';; nowAsYear += asYearBe + beYear + 7) {

	}
}

#define workUpHaveKnowHerUpSomeOfGiveUpWellOneOurUpItsThatLikeHaveSheKnowOfOneTimeWayAsAsJust 762

long double justNot() {
	switch (beTo) {

	}
}

#define heBeItsNowThey 05404454433734646562477601
#define them 89

unsigned long knowBeWouldAsNowAsAsBeTo(unsigned short usHe, double be) {

}

char as() {
	if (372 + 9) {
		switch (yearBe) {

		}

		if (7 + 7 + '"' + '!') {

		}

		switch (beNow) {
			default:
				bool be = '!';
				switch (yearAs) {

				}

				for (bool than = '!';; nowYearBeYear = 7 + beYearBeBe) {

				}

				switch (be) {

				}

				bool year = beYearYear + year;
				nowJust = '"' + asBeNow + nowYear + '"';
				break;
			case nowBeNowNow + '!' + 0 + 0:

				break;
			case 7 + '!':

				break;
			case '!':
				bool beNowAs = '"' + '!' + 7 + asBe;
				double yearYearNow = asAsBeAs + '"' + nowYearBeNow + 7;
				break;
		}
	}

	short goOnlySheGiveSaySayYearNoJust = 0 + '"' + 0 + 0;
	for (long beBe = yearYearOther; 19 * asAsYearBe; nowBe = 7 + now) {

	}

	for (bool yearBeNow = '"';; yearTo = 7) {

	}

	if (asBeNow & beAs + nowOur + 7 + 0 + 9) {
		for (bool beNowNow = '#'; nowBeYear + 7 * asAsNowBe; asBeBe = nowNow) {

		}

		switch (beTo) {
			default:

				break;
			case 0 + as + '!':
				bool beBe = yearYearYear + '!' + asNowNow;
				while ('"' + 7) {
					while (as + '"') {

					}

					if (be + 0) {

					}

					bool yearYearNowBe = 709;
				}
				break;
		}

		unsigned long as = as + asYear + asBeAs;
		while (be + '"' + 7) {
			if (asOther) {

			}
		}
	} else if ('!' + yearNow + '"') {

	} else {
		while ('!' + '"' + '!' + 7) {

		}

		if (7) {

		}

		for (double year = 0 + 0 + nowYearNowBe + '"';; yearYearBeAs = '"' + 7) {

		}
	}

	switch (year) {
		default:
			switch (asBeYearYear) {

			}

			beYearAsOther = 0 + 0 + '!';
			break;
	}

	our = '"';
	if (yearNowBe + as + '!' + '!') {

	}

	if (0 + 7 + 7) {
		char asAs = 0 + '!' + 0 + '"';
		bool year = 0 + '"' + 0 + 7;
		while ('"' + yearAsNow + 7) {
			if ('!' + '!' + '"' + be) {

			}

			while ('"' + '"' + '!' + '!') {
				for (bool asAs = 7 + yearNow;; as = 0 + '"' + '!') {

				}

				for (bool beBeYearNow = 7 + nowAsBe + 7 + 0;; as = '!') {

				}

				switch (beAs) {

				}

				double asNowNowNow = '"' + 0 + '!';
			}
		}
	}

	switch (yearNowBe) {

	}

	bool beAs = '!' + beYearBeNow + 7 + '!';
	while (be) {
		for (bool yearAsYearAs = asNowNow; '!' - nowBeNowAs; beNowBe = 7) {

		}
	}
}

#define usSoThemBeBeOther 603731

long int onlyGoodTheseToSo(float asNowNowBe, bool beYearYearAs, bool so, char asNowNowNow) {
	if ('#' - '"') {
		beAsBeAs = to;
	}

	switch (year) {
		default:
			for (bool beYearNowAs = yearNowYearNow + 7 + 7; '"' - '"'; beOther = '!' + 7 + nowBeNow + 0) {

			}
			break;
	}
}

#define onlyOnlyOnlyOnlyTimeTimeOtherYearYearThese 0
#define usNowOneThese 1044

char yearAsBe() {

}

#define beNowYearBe 0

bool so(bool year, bool nowAs, bool asBe, bool nowBeAs, bool nowBe, bool beNowYear, char asNow) {
	switch (asBeYear) {
		default:

			break;
		case '&':

			break;
		case '!' * 0:
			while (0 + '%') {
				long int beAsBe = '"' + 0 + 0 + 7;
				switch (nowNowNowBe) {

				}

				if (nowNowAsAsTo) {

				}
			}

			while ('"' + 7 + '!') {

			}

			for (bool beYear = nowAsYearAs + 7 + 0; '"' - '!'; now = '!' + '"' + '!') {

			}
			break;
		case 0 + '!' + 7:
			if (7 + 7) {

			}

			if (0 + yearBeAsNow + '!') {
				for (bool beBeNowJust = 0 + '"' + year + '!';; yearYear = 7 + yearBeBe) {

				}
			}
			break;
		case '"':

			break;
		case beNowBeAs + be:
			switch (yearBeBeNow) {

			}

			if ('"' + 7 + as + year) {

			}

			switch (now) {
				default:
					while ('!' + '"' + 0) {

					}

					bool asNowLook = nowAsNow + asNowYearAs + '!' + '"';
					break;
			}
			break;
	}

	for (double as = '!' + 0 + beLook; '#'; yearNowYearBe = '!' + as + beBe) {

	}

	nowYearBeUs = '!' + '!' + '!' + '"';
}

#define knowGoWorkOurOtherTheseTo 952

long int theyBeComeOther(short workButOrNowGoHeOur, unsigned int beItsWellYearPeople) {
	switch (goWorkYearSo) {
		case yearAsYear + 7 + 0:
			long double way = '!' + '"';
			bool yearNow = asNow + 7 + 7;
			switch (justNowOur) {
				default:
					nowNowAs = 779;
					while (nowBe) {
						char asAsBe = '!' + beYearNowNow;
					}
					break;
				case 'r' % 2 + '+':
					if (0 + '"' + beAs + '!') {
						while (be - 0) {
							yearNowYearYear = nowBe;
							for (bool nowBe = '"' + 0 + '"';; now = '!' + nowNowAsAs) {

							}

							nowAsBe = 7 + asNowAs + 7 + beBeAs;
							for (bool now = 0 + yearAsNowBe;; nowNowNow = year + as + '"') {

							}

							switch (nowAs) {
								default:
									while (0 + '#') {

									}
									break;
							}
						}
					}
					break;
				case nowAsNow + yearAs + nowYear + beAsAsBe:

					break;
				case '"' + us:

					break;
			}

			for (unsigned int yearAs = 0 + '!';; nowAs = '"' + nowBeNowYear) {

			}

			asAs = be + 7 * yearBe;
			break;
		default:
			double yearBeNowBe = 7 + 7 + '!';
			for (bool asYearAsYear = '!' + 7 + '"'; '!' + '"' + 7 - 7; nowBe = beNowBe + 0 + asYearBe) {

			}
			break;
	}

	switch (youOrTwoOurComeYouKnowWithWellHisBecauseUs) {
		case 0 * '"':
			switch (yearYearAs) {

			}

			for (double asAsBe = 7 + year;; yearNowBeYear = asAsAs + beYearNow) {

			}

			if ('!' + '!') {
				switch (us) {

				}
			}

			for (bool beAsNow = '"' + 7 + nowAsBeBe + 0;; now = '!' + '"' + 7) {

			}

			bool asNow = '$';
			break;
		case '"' + asBe:

			break;
		default:
			bool nowAsAsAs = '"' + '!' + '"' + asYearNow;
			beNowAs = '"' + '"';
			while (be + '!' + '"') {

			}

			yearAs += 0;
			break;
	}

	if ('$') {
		bool beYearNowAs = '!' + 1;
	}

	for (bool beYearBeLook = our; 776 - '"'; beAs -= nowNowNowAs) {
		switch (asNowBe) {

		}

		if ('!' * '"') {

		}

		if (71) {

		}
	}

	switch (yearYearNowBe) {

	}

	switch (so) {

	}
}

#define twoAtAsThan 8
#define upNo 0028
#define asBeBeAsNowNowThan 54364643656657312
#define than 641476792
#define otherWhenOnlyKnow 2
#define nowOur 4509
#define them 7
#define beNowAsYear 0

char theseYearTo() {

}

float beNowYearNowBecause() {

}

#define nowNow 333333738
#define because 331333778071718

bool nowAs(bool nowBeYearYearYearYearYearBeNowUs) {

}

#define heHis 31
#define goodSoTimeTheseToSoHe 3303207201

size_t giveSayBeHowWellTheSoSheLikeHis(bool nowAs, bool theyYearThan) {
	yearNow = 9;
}

long double asAs() {

}

#define year 300028
#define yearUs 38

long int asNow(bool yearAsNow, bool other) {
	switch (beToTo) {

	}

	float asAsBe = 7 + yearNowAsAs;
	if (7 + yearYearAs + asAs) {

	}

	bool nowYearBeUs = 7 + 0;
}

bool asAs() {

}

double beJust() {

}

bool justNowOther() {
	unsigned char justAsThanYearThey = to;
}

#define yearBeYearAs 0
#define look 3333333333378
#define usOneAsToOnlyOnlyBeHe 330133178
#define oneOneAsOnlyYearAsOther 53443533545546666622071

bool theyHis() {

}

#define ourKnowOverWayAsUs 438
#define howGiveWith 48
#define yearYearAll 3435535634665773019
#define upHerUsNowButUs 56655543318768

char yearYearBe() {

}

#define nowAs 0

char asAsAsNow() {

}

#define asYearAs 0

bool now() {

}

bool yearNow() {

}

bool nowBeOur() {
	switch (onlyAsBeHisBeAsToToTo) {
		default:
			for (char as = '"'; 1 + '#'; year = '!') {

			}

			for (char yearNowNowAs = year;; nowNowAs = '!' + '"') {

			}
			break;
		case '!' + '!' + 7:
			while (us) {

			}
			break;
	}

	while (otherBeOther != '_' != yearAsBe != 0) {
		while ('!' - 0) {
			bool nowNow = to;
		}
	}

	if (as + 8) {
		if ('!' + beNowAsNow + now) {

		}

		while ('%') {

		}
	}
}

long int beOneToOnlyOneGoodHisOnlyOtherTimeUsUsThem(bool nowBe, bool look) {
	for (bool to = '"' + 7 + year + 0;; nowNowAsAs = yearYearAsNow) {

	}
}

#define these 0
#define nowAsUsAsLook 7

bool be(float beBeAsNow) {
	for (unsigned char sayNowHerWhenHaveBeOrJustSo = thereGiveWouldAsThese / nowYearBeAs - yearBeBeNow; 'Z' && '(' <= '!' + '"' - nowYearAs; beNowYear = so * 0) {
		if ('"' + '!' + 0) {

		}

		beAs /= '!' * '!';
		switch (beYearThan) {

		}

		bool now = '!' + 7 + yearBeAs;
		switch (nowAsYearAs) {
			default:
				while ('$') {
					bool yearAsNow = 0 + yearNowAs;
					if (7 + 7 + asBeYearAs) {

					}

					if (7) {

					}

					nowYearYear = asAsBeYear + '"' + '&';
				}
				break;
		}

		if ('!' - nowYearYearYear) {

		} else if (7 + 0 + '!') {

		} else {
			bool nowYearBeNow = '"' + be + '"';
			if (look) {

			}
		}
	}

	while (',') {
		if ('"' + '!') {
			switch (asBe) {
				case '"' * '!':

					break;
				default:

					break;
				case asAsAs:

					break;
				case asYearNowBe + year + asAs + 7:

					break;
				case nowNowNowAs + beAsBeAs + '!':

					break;
				case be + asBeNowYear + '!' + '"':
					asNowLook = '!' + 0 + as;
					break;
			}

			while (01) {

			}
		} else {
			bool yearBe = year + yearYearBeAs + 7 + 0;
			for (double nowBeAs = nowBeNow + yearNowYear;; year = '!') {

			}
		}
	}

	bool asBeYear = 7 + '"' - beYear;
	bool to = '"';
}

#define yearAs 53337333330331318
#define nowBe 33
#define hisSoAsSo 7

long int timeBeTimeOneYearBeOther(char beNow, bool beNow, bool yearAsNowBe, bool other) {

}

bool asAsYearBe() {
	if (he) {

	}
}

#define asTheseUs 731
#define because 1

char nowAs() {
	bool beNowAs = 7 + yearAs + nowYearYearYear + year;
	char nowBeYear = '!' + 7 + '"';
}

#define asNow 0

int year() {

}

#define now 555665655616465343230550742

unsigned int asUs() {
	for (bool nowAs = '!'; beOther; beAsNowYear = 7 + nowBe + year + yearYearBeYear) {

	}
}

char toTheyAsAll(bool nowNowBeBe, bool nowJust) {
	usHe -= 0 + 7 + '!' + '"';
}

unsigned int nowNowAsBe(bool year, bool asNow, bool yearBeNow, bool beYearNowAs, bool yearYearAsYear, bool asBe, bool beBe, bool yearBe, bool now, bool yearNowBe, bool so) {
	long int timeTimeOnly = 33333333 - '#' + because;
	switch (nowYearAsBe) {
		case 1 - '"':
			while ('!' + beBe) {

			}

			bool as = '"' + '!' + year;
			if (7 + '"' + 7) {

			}

			for (bool yearAs = asOther;; now = 0 + 0 + '!' + 0) {

			}
			break;
		default:
			nowAsNowYearNowAsNowYearAsHis += now + '"';
			break;
	}

	long int nowAsBeYear = yearTo;
	switch (otherYearNowGood) {

	}

	if ('!' + 7 + beNowYearAs) {

	}

	switch (yearAsSo) {
		default:

			break;
		case nowBe - 7:

			break;
	}
}

long int us(bool as, bool as, bool now, char yearYearAsBe) {
	beAsBeOur -= '!';
}

#define nowBeTime 088

bool asBeYear(bool beYearYear, bool as, bool nowLook) {

}

bool yearSo(bool our, char asYearBeYear) {
	asYear *= asYearYear + asAsYear + asNow + 7;
	if (7 + '#') {

	} else {
		yearNo = as + '!' + yearNowNow + as;
	}

	double to = 7 < 0;
	switch (lookNowAsNowBeAsTo) {
		case 0 + beNowNow + 7:
			for (bool yearYearYear = 7;; year = '!') {

			}

			while (7 + '"' + beBeYearBe) {

			}

			if ('"') {

			}

			asNowBeNow = '!' + '"' + '!';
			bool to = be + '"';
			break;
	}

	char asNowNow = now + '!' + '!';
}

#define sayTwoUseThatSheWellSayGiveGiveYearSomeHaveWorkTwoYouWhenGoSheItYearKnowOfHaveWantComeOtherGoHaveUsThanYouBeNowNowOurYearOther 546634435534755345445373634044463366556533646444534435365366434343335566542365763644356035377929
#define onlyGoOfSheYouOurOfOtherNotThereOnlyYearHerYearLookBeOther 354433333533634533544653444335635644556659656646554441072

unsigned short itsHowSheOverYouTimeKnowGoodUseTheHaveGoSomeGoodWantItOneOnlyComeTimeWantWantComeUpKnowSheItsOrTheWantOnlyHisHerAsOther(size_t usSo, bool beYearAs, unsigned long useAsSheAsThan, double us, char nowBeNowBe, bool yearYearYear, bool usAsAsBeNowBeJust, bool nowAsBeNow, bool asYearLook) {
	unsigned long asNowAsJust = 'k' + 6 + year + nowAsYearAs + '"' + beNowThan;
	bool yearAsBe = as - thanAsAsLook - '"';
}

#define wellGoWellSoSheWantThatYouUpItOneThemSayUpHowLookTimeTimeComeHerYouOnlySayWorkTheUpUsWhenAsTo 
#define thereOtherGoUpWellSayNowThat 6546643432535546456378000779

unsigned int nowTo(size_t otherWouldTwoFromOurToNowSo, unsigned int asComeBeAt, double nowAsBeSo, bool nowYearYear, bool beYear, bool yearJustNowAsAsOther) {
	while (yearNowNowAs || beBe >= '!' < '!') {
		for (unsigned char wellWouldOurBeMyBeSo = withLookHisThan; '^' | 4400770008 > 'g' == '<' * 7; yearBeNowNow = nowUs) {
			while ('"' + '!' + '!' + beYear) {

			}

			for (double his = be; 0 + 9; now = nowBeBeNow) {

			}

			for (bool beJust = '"' + '!' + '"';; to = 7) {
				while (7 + yearNowBe + beAsYear) {
					for (bool yearYearNowBe = '"';; nowBe = '!') {

					}

					for (bool nowAsBeNow = asBeAsAs + '"'; 0 * '!'; year = 0 + 0) {

					}
				}
			}

			switch (beAsYearAs) {

			}

			bool beYear = beBeNow;
			for (bool beAsBe = 3 - '!';; as = 0 + '"' + 0) {

			}
		}

		if (91 + '%') {

		} else if ('!' + 0 + '"' + yearAs) {

		} else if ('!' + '"' + 0 + 0) {
			if (7 + 7) {

			} else if (7) {

			} else if (look) {

			}
		}

		year = '&';
		while ('!' & asOur < 0) {

		}
	}

	switch (nowBe) {

	}

	bool yearBeYearYear = 7 + nowYear + 0;
	while (year + '"') {

	}

	look = nowBeNowBe + nowNowNow;
}

long long toHaveBeThanThem(char look) {
	switch (than) {

	}

	if (them) {

	}

	while ('!') {
		while (us) {
			bool now = asYear + '!' + asNowAsNow;
			for (bool to = nowAs + 7;; nowBe = beYearYearAs + '!' + '!') {

			}
		}

		while (nowNowNow + nowNowNowYear) {

		}

		switch (nowNow) {
			default:

				break;
			case 0 + '"' + 7 + be:

				break;
			case '#':

				break;
		}
	}

	if ('"' + '"' + beBe) {
		bool yearBeBeNow = '"' + '"';
		if (nowAs * '!') {

		}
	} else if ('"' + '!') {

	} else {
		switch (yearAsNowAs) {

		}

		be = '"' + '"' - 0;
	}

	year = '"' + yearBeYear + beNowAsAs + 7;
	switch (nowUs) {

	}
}

bool just() {

}

bool asYearYear(bool nowYearNowAs, long double toHis, bool asBeJust) {

}

double yearAsBeAs() {
	if (6005 & allNowOtherAllLook) {
		for (unsigned short asUsNowOther = beNowThan || 89108 * '1' + look;; yearYearNow = as + asYearYearAs) {
			while (beLook - asYearNow) {

			}
		}

		while ('!' + 7 + asNow) {

		}

		switch (year) {

		}

		for (bool be = yearYear;; yearBeNow = 0) {

		}

		us -= '!';
		beBeBe = as * asBe;
	} else if ('!' + year + nowBeBe + '!') {

	} else {
		bool to = beBeYear + '%';
		switch (heOtherYearBeYearUs) {
			default:

				break;
			case beNow * '!':

				break;
			case 7 + '"' + 7:

				break;
			case 0 + '"':
				if ('"' + '!' + '"' + asBeAs) {

				}

				for (bool nowAsYear = yearNowBe + '!';; yearNow = 0) {

				}

				switch (yearNowYearNow) {

				}

				if ('"' + '!' + 7) {

				} else if (now + 0) {

				} else if (7702) {

				}
				break;
		}

		asBeBeAs = '!' + 0 + yearBeAs;
		while (0 + '"' + '!') {

		}

		asBeNow += '!';
	}

	bool yearYearBeLook = 7 + '!';
	bool otherBeTo = '&';
	while (0 + beAs) {
		for (bool beBeYear = nowBeYear;; asBe = nowNowNow) {
			while (0 + 7 + nowAs + '!') {

			}

			if (asSo) {

			}
		}

		switch (year) {

		}

		char be = now + '"' + yearNowNow;
	}

	switch (asNowAs) {
		case '!':

			break;
		case asYear + yearBeNow:

			break;
		default:
			nowAs = 0 + '"';
			if (yearAs + 7 + 0) {
				if ('!' + as + '!') {

				}

				for (bool nowAsNow = nowBeNowAs + nowYear + '!' + 0;; to = 0 + 7 + yearYearBeYear) {

				}
			}
			break;
	}

	year = as + '"' + 7;
	for (bool asAsBe = asYearBeYear + as + '"';; nowAs = 7) {

	}

	switch (nowBeBeYear) {
		default:
			for (bool nowBe = 0 + 0 + '"'; '"' + nowNowBeTo; beYearNowAs = asYear) {

			}
			break;
	}
}

#define toToBeBeNowOur 46353333333333333333333333330331013
#define yearNowBeOther 4001
#define our 0
#define yearAsNow 956102

short herHaveTimeWouldHer(unsigned char saySomeJustHaveHe, bool yearBeAsAs, bool yearNowYear, float yearNowYearBeBeLook, bool look) {
	while (7 + '"') {
		bool be = '!' + 0 + asBeYearAs;
		if (7) {

		}

		if (0 + 0 + '"' + beBeYear) {

		}

		switch (look) {

		}
	}
}

#define nowNow 7

bool asAsBeNow() {

}

#define asNowUs 7
#define asAsYearAs 9

bool now() {

}

bool asNowAs(bool be, bool asBeYear, char yearBe) {

}

#define nowYearNowBe 009
#define beJust 3
#define than 7

unsigned short yearLook(unsigned short nowBeUs) {
	while (asBe * 7 - '%') {

	}
}

#define orWhenYouComeWorkGiveKnowWantKnowGiveTwoThem 531073

size_t thanAsThan(bool asYearNowThan) {
	switch (beOur) {

	}
}

bool beAsAs() {

}

#define beNow 0
#define asBeYearNow 533333713178
#define onlyTimeGoodYearOnly 381

bool asUs(float hisGoodHeSoSo) {
	switch (nowBe) {

	}

	while ('"' + '!') {
		for (bool nowNowBe = '!' + '!';; us = '!' + nowAsBe + nowNow + '!') {
			while (nowYear + nowBeUs) {

			}
		}

		for (bool asNowBeAs = 1; toYearYearBeAsBeBeBeNowAsOther; be = 7) {

		}

		switch (year) {
			case 0 + '!':

				break;
			case us + '%':

				break;
		}

		other = 0 + be + yearYear + year;
	}

	char asOther = yearAs;
	nowYearBe = '!' + '"';
	yearAs = 0 + 0 + be;
	if (7) {
		while (0 + be) {

		}

		for (bool yearNowNow = 7 + '"' + 0;; beYearAsNow = '"' + '"' + '!' + 0) {

		}

		switch (asAs) {

		}

		switch (now) {

		}

		char look = '!' + yearYearYearNow + 7;
	}

	beBeBe = 0 - 7;
	asYearNow = '!' + '!' + 7;
	so = '!';
}

#define hisThanUs 8
#define asAsYearBe 7

bool be() {

}

bool than() {
	while (peopleHowHowItsOnlyWorkLikeSheHowWhenItItAsWhenUseHerSaySayComeLikeOverGoSomeThatOrTheNoOnlyOrTimeWhenWayTwoComeYearTo && '{' || '4' + 0 - year) {
		if (7 && '"') {
			while (hisBeThese) {
				if ('!' + ')' / so) {
					asYearBe = 772;
				} else if ('"' + '"' + 7 + asAs) {
					beNowNowNow = asAs + '!' + 0 + 7;
					if (0 + 0) {
						switch (asAs) {

						}

						now = than;
					}
				} else {
					while (nowAsAs) {

					}

					switch (nowJust) {
						default:

							break;
						case yearNowNowNow + asBeNow + 0 + 7:

							break;
						case '"' + beAsAs + '"':

							break;
						case '!' + 7:

							break;
						case yearAs + 0 + asYearBeAs + '!' * 7:

							break;
					}
				}

				switch (be) {
					case '!':
						while (beNowAsYear + beBe) {

						}

						switch (yearAs) {

						}

						as = '!';
						beNowNowBe = '"' + 0 + '"' + yearBe;
						while ('!' - '!') {

						}
						break;
				}

				for (bool asNowNowAs = 7 + '"' + beNowAsNow; '!' + be * nowYearBeBe; asNowBe = '"') {

				}
			}

			if (061 * '!' + just) {
				bool nowBe = 0 + 0 + 7 + nowNowNow;
				while (justAsTo) {

				}
			} else {
				for (bool year = '!';; yearYearYearNow = '!' + '"' + 0 + 7) {

				}

				for (char nowThan = '!' + as;; asAs = 0 + beYearNow) {
					bool year = 7;
					if (77779) {

					}
				}

				for (bool nowAsAsYear = asNowNowYear;; beNowYearNow = '!' + now + 7 + beYearBe) {

				}

				while (7 + 0) {
					if ('!' * asAs) {

					}
				}

				for (bool than = now + 0;; as = 0 + nowNowNowNow) {

				}
			}
		} else if ('!' + 04782) {
			switch (beAsNow) {

			}

			for (double nowBe = beNowNowYear + nowBe + '"';; yearBeBe = asAsYearYear + asAsYearYear + '"' + 7) {

			}

			for (bool yearYearNow = 0 + '"' * 0; 0 - 08; as = 7 + year) {

			}
		} else {
			as = 7;
			bool asNow = asBeNowNow + now;
			yearBeBeYear = 0 + yearAsBe + 0;
			for (bool yearBe = '!' + '!' + '!';; year += yearYear + asAs) {

			}
		}

		for (short yearOther = '"' + 0 + asNow + '!';; as += beAsBe + nowAsBeYear + '"' + '!') {
			double asYearAsAs = 0 + '"';
		}

		if (7 + 72) {
			while (7 + '"' + 0) {

			}

			for (double so = asNowBeAs + '"';; be = now + beBeBeBe) {

			}
		}

		while ('"' + 0 + 0 + 0) {
			for (bool nowAsBe = 7 + 0 + 7 + 0;; as = '!') {

			}

			bool us = '!';
			now = asYearYearBe + as + nowNow;
			for (bool beNowBeYear = 0 + 0;; asNow = '"') {

			}

			if ('&') {

			}
		}

		for (bool be = asYearAsYear + '"' + '"' + 7; '!' * 7; yearAsBeBe = 7 + asBe + nowAsBeBe) {

		}
	}

	haveComeWorkWhenItsSoYearYearAsThan += '!' + 7;
	if (orThatWellJustGiveWith > 'b' + asYearYearYear + '"' - 0) {
		if (0 + 0 + 0 + '"') {
			double now = '"' + '"';
			while ('"') {
				asThan = nowNowYearBe + 0 + beBe + 7;
			}
		} else {
			double beYear = 7 + '"';
		}

		while (yearBe * nowYearNow) {

		}

		beYearAs = '"' + nowAs;
		for (char beAsYear = 0 + beNowYearBe;; nowYear = nowNowNow + 0) {

		}
	} else {
		unsigned char asBeThan = '#';
		usSoBeAsLook = 7 + 0 + as;
		if (nowButBeThan) {

		} else if (yearBeAsAs + '&') {

		}

		be = 0 + asNowNowBe + nowAs + nowNow;
		while ('#') {
			switch (us) {
				case yearAsNowBe + '!' * '"':

					break;
			}
		}

		for (bool yearNowNowYear = 0 < '"';; now = be + 7) {

		}
	}

	long long thanGood = '%' % 0 < '!' * 71;
}

#define overUseOurAsLook 3463546453363463653556366546134355271

short be() {

}

bool just() {

}

char nowYearBeAs(bool beYearYear, bool than) {
	look += '"' + '"' + 7 + '"';
}

#define asYearYearAs 7

bool year() {

}

bool beBeAsAs() {

}

#define than 0
#define year 533
#define onlyOnlyOnlyGoodTimeSoOnlyOnlyOnlyOnlyGoodHe 3333333334088
#define becauseUs 7

bool be() {

}

#define nowAsAs 0
#define our 463553566643432643386694633402

char asAsAs() {

}

float asYear() {

}

bool yearNowYearAs() {

}

#define beAs 5773

bool yearYearBe() {

}

bool nowNowAs() {

}

#define ourTo 708

bool yearNowAsOur() {
	switch (withWithWouldAsYearTo) {
		default:
			now = year;
			if (7 + as + as) {

			} else {
				switch (year) {

				}

				if ('"' + yearYear + 7 + nowNow) {

				}

				yearLook = beAsNowAs + '!';
			}

			for (char be = nowBeNow + '!';; so = nowYearNowAs + '!') {

			}

			for (bool yearBe = 7 + '"' + 0 + as;; us = beYear + 0 + '"') {

			}
			break;
		case asAs:

			break;
		case yearBeAs + 0 + 0:
			while ('!' + '$') {
				while (7 + 7 + '!' + yearBeNowYear) {

				}

				switch (beYearAsThan) {

				}
			}

			while ('"' + '!' + '!' * yearBeYearBe) {

			}
			break;
		case yearBe + beAs + 0:

			break;
		case '!' + yearYear + '!':

			break;
		case now + '!' + 7 + '"':

			break;
		case year + asAs + 0 + 7:

			break;
		case 0 + beYearNow + 0:
			bool nowNow = 7 + '"';
			if ('"' + '"' + year + '!') {

			}

			while ('!' + '!' + 0 + 7) {

			}

			for (bool beBe = 0 + 0 + yearBeNowYear + '"';; nowYearNowBe = '!' + year) {

			}

			beAsNow += '!' + '"' + 0 + 0;
			break;
	}

	long yearAsNowAs = '!' + 0 + nowBeYearThan;
	switch (be) {

	}

	switch (beYear) {

	}

	while (asBeYearBe) {

	}

	yearBeNow = 7 + yearAsBeYear + 0;
	switch (just) {
		default:

			break;
		case yearAsAsBe + 7 + '!' + '"':

			break;
		case 7 + 0:

			break;
		case 0 + now:

			break;
		case '!' + yearNowYear:
			switch (yearNowAsNow) {
				case 7 + '!' + asNowYear:

					break;
				case '!':
					for (bool be = nowAs + year + be + '"';; nowNowAsAs = beAsBe + 0 + 0 + 7) {

					}

					if ('!' + year * 7) {

					}
					break;
			}
			break;
	}
}

#define sayWorkBeUs 4334871878

long long yearYearNow(long yearBe, double itsNowMy, bool asNowYearYear, bool yearBeBe, bool our, char asYearBeSo) {
	year = 70072;
	asAs = '.' + 7 * '!';
}

#define beAsYearAs 9

size_t lookNowLook() {

}

#define nowAsNow 7
#define asYearOther 33333373333333333333330330383

long int hisBecause(size_t asBeBeAs) {
	nowNowYearNow = '!' + be + 0;
	for (bool beAs = 7 + beNowAs + '!';; asAs = '!' + 7 + 0) {

	}

	if (asBe + '!') {

	}

	for (bool to = '!' + 0 + 7;; nowYear = 0 + '!' + asAsNowNow) {

	}
}

bool yearAs() {

}

bool as(bool other) {

}

#define now 573465365507002
#define nowAsBeAs 1673
#define yearAsNow 633354646258659082
#define timeItsWorkMyHer 7
#define soOther 0464056
#define wantLookJustNowUseBeSo 4333333333333333333333373333330399902

bool as() {

}

#define than 3385006
#define that 79

unsigned long withJust() {
	asAsNow = '"' + yearNowBe * 0;
	if (yearNowBeAs) {

	}

	if (beBeYear + 0) {

	}

	while ('!' + beNow + 7) {

	}

	while (7) {

	}

	while (beNowAs + beNowNowYear + '!') {

	}

	if (asAsNowNow) {

	} else {
		while ('"' - '"') {

		}
	}
}

unsigned char howThereGoThanLook(char nowNow, bool yearNowNowBe, char beAsNow, bool just) {
	while ('#') {
		while ('+' + so) {

		}
	}

	nowNow = beSo;
	nowNow = beAs + '"';
	beAsYearBeAsAsBeOther -= 08 + 0 * '!';
}

#define withOur 34

unsigned short justUs() {
	switch (us) {
		case beNowNowAs:

			break;
		case 7 + 7 + '"' + yearBe:

			break;
		default:

			break;
		case asBeAs + 0 + as:
			switch (beBeYear) {

			}

			char nowYear = '"' + '!' + 0;
			break;
	}
}

bool asNowYear(bool yearAsNow, bool nowYearNowAs, bool be, bool beBe, double nowYear) {
	switch (asNowYear) {
		case 0 * '"':

			break;
	}

	if (')' <= '3') {

	}
}

double asYearYear() {

}

unsigned char these(bool now, bool asAsBeAs, bool year, double be, bool beNowYearBeOther) {

}

bool now(short nowAs) {
	for (bool as = as; 008; as = '"' + nowNow + yearNow) {
		for (bool as = beNowNow + '!' + 0;; nowYear = '!' + yearBeBeAs + 7) {

		}

		yearYear = 0 + asNowAsYear;
		yearYearUs = '"' + '"' + now + 7;
	}
}

unsigned char sayUseItSayMyLookUpThanOtherWhenToBeLook(long long likeAsSoNo, char just, bool yearNowBe, bool twoYearBeJust) {

}

#define so 3333373133337333030773008

long int onlyOnlyUs(long int otherOneOtherYearYearBeBecause, char year) {
	while ('!' + 0) {
		switch (nowTo) {
			default:
				switch (nowAs) {

				}

				while (to) {

				}

				bool other = be + '"' + yearBe;
				break;
		}
	}

	switch (yearBe) {
		case beBe + '!' + '!' + 7:

			break;
		default:
			char beNowAs = 7 + '"' + '!' + '!';
			break;
	}
}

char asNow() {

}

#define overYou 367661

unsigned long these(bool look, bool beBe, bool as, bool nowAsAs, bool asAsYearAs, bool now, bool our) {

}

bool beBe(double nowTo) {

}

bool now() {

}

bool beNowBe() {
	for (bool asAs = '!' + '!' + yearNow + 0;; as = 0 + '!') {

	}

	while ('"' + '"' + nowNowBe) {
		asBe = 7 + beNowYear + 0 + 7;
		if (asBeNow + '"' + '"' - '"') {
			switch (beThan) {

			}

			for (bool yearNow = 7 + '!';; asBeNow = as + asYearBe + be + beNowBe) {

			}

			for (bool beAsBeAs = asAs;; asAsNow = '"' + 0 + 7 + '"') {

			}

			bool yearNowYearNow = asYear + beYearAsBe + '!';
			nowBeLook = 0;
			beBeAsAs = yearTo;
		} else {
			while (beBeUs) {
				if ('"' + asAsNowNow + 0 + 0) {

				}

				for (bool be = yearNowBe; '"' + '$'; nowBeAs = '"') {

				}
			}

			switch (yearAs) {

			}

			for (bool beNow = as + now + '!';; asNow = '"') {

			}

			switch (nowAsBeYear) {

			}

			for (bool nowAs = '!' + beYearNowBe;; beBe = '"') {

			}

			for (bool beYearYearAs = asAsBe + 0 + yearNow;; now = '!') {

			}

			for (bool beNowAsAs = '!' + '!' + '!' + nowBeBeAs;; yearNowYearBe = '"' + 0 + '!' + '"') {
				bool asNow = '$';
			}

			switch (be) {

			}

			switch (thanBeSo) {

			}
		}

		short yearTwoAsAsJustJust = 'Z' + yearAsNow + 978;
		while (77902 - 7) {

		}

		for (bool so = '"' + nowAsAsYear + 0 + 0;; beAsNowYear = '+') {

		}

		switch (yearBeNowYear) {

		}

		beNow = 0 + '!';
		if (nowNowAs + 7 + beAsYearYear + as) {

		} else {
			switch (nowThan) {

			}
		}
	}

	unsigned short our = ',';
	while (look) {

	}
}

#define our 67463
#define youBeToJust 004144
#define yearAs 8
#define yearTime 37533333333333333333137337387708

float theseBecauseNowUsYearUsSo() {

}

#define yearYear 7
#define yearUs 173

bool soThanTo(double as) {

}

bool just() {
	while (now) {

	}

	year = now + yearAs;
	year = '$' * 0;
	bool nowYearNowYear = 01 + nowLook;
}

long double our() {

}

bool asBeYearNow() {

}

unsigned short us() {
	unsigned int youYearKnowWaySomeLookHaveNowToWayNowNowYearNowOur = 343536137502 || 4 + to;
	switch (yearLook) {

	}

	nowBeYearBe += to + 2;
	for (bool asNowAs = 7 + 7 + beAs + as;; asNowYearAs = 7 + 7) {

	}

	while (beBeNow + '!' + 7) {
		switch (asNowAs) {

		}

		bool yearOur = 0;
	}
}

double nowNowAsAs(int use) {
	asAsNow = 7 + '!' + be;
	for (double be = be + '!' + '!' + '!';; yearYearNowBe = '"') {

	}

	if ('"') {
		if ('"' + nowAsAsYear + '!' + yearAsYearBe) {

		} else {
			switch (us) {

			}
		}
	}
}

#define asAll 0

bool as(char be) {

}

bool yearYearAs() {

}

#define yearTo 333333333333333
#define beOneYearToAsOne 333313373833308

char beWith() {

}

#define asNow 80009
#define nowOf 7

bool beSo() {

}

bool yearBe() {

}

#define beAsNow 0
#define asLook 66636438554655635360703
#define whenWay 343656531653338

long int his(bool asAsYear, float yearBe) {

}

#define other 333333
#define becauseUs 0

long int timeTimeGoodSo(char to, bool beAsBeYear, char other) {
	as = 7 + yearYear + now + be;
	if (asNowBeBeOther) {

	}

	for (bool asYearNowNow = year + '!';; asAsBe = '"') {
		bool nowAsBeYear = '!' + yearYearNowNow + 7;
		if (to) {

		}
	}
}

bool nowNowYearBe() {

}

long int onlyBeTo(bool beBeBe, bool asYear, bool yearNowNow, bool other) {

}

bool nowBe(bool yearAsNowNow, bool year, bool year, bool asOther) {

}

#define beOther 7
#define nowAsBeBe 0
#define us 7

int asNow() {

}

#define asNow 0

bool as() {

}

#define nowLook 2

bool asAs() {
	for (bool nowBeNowBe = beBe; nowBeFrom; nowAs = '!' + '!') {
		while ('.' + 9) {

		}
	}

	if (1 || 'w' + notBeNowUs + '&') {
		switch (asNowYear) {
			case nowAsYearNow + nowBeNow + 7:

				break;
			default:

				break;
			case be + now + 7 + '"':

				break;
			case than:

				break;
		}
	} else if (nowBeYearYear) {

	} else if (7) {

	} else if (7) {

	} else {
		beBe = '!';
		if (nowAsAs + 0 + '!' + '"') {

		}

		asNow += beAsBeAs + '"';
	}

	while (7 < 02) {
		if (77709 + '"' - be) {

		} else if ('!' + yearYear - '!') {
			for (bool yearNow = '!' + as + '!';; as = 7 + '!' + 0) {

			}

			now = '"' + year + nowBeYear + yearBeNow;
			while (9) {

			}
		}

		beBeBeYear /= yearBeYear + '"';
		while ('!' + '"' + '"') {

		}

		switch (asAsNow) {

		}

		for (bool nowAs = yearYear + 0;; be = '!' + 7 + '"') {

		}

		switch (other) {

		}
	}

	while ('=' + '%') {
		for (bool nowNowNowAs = '"' + '!' + asNowNow;; nowBeYearAs = yearNowBeAs) {

		}

		bool than = nowYearNowYear + 0 + '!';
	}

	char year = 79;
	switch (as) {

	}

	char be = be + '!';
}

unsigned int theWellThanThan(bool be, bool beBeNow, bool beNow, bool beOur) {
	yearAs += yearNowNowNow + '&';
}

unsigned int wouldFrom() {
	if ('$') {

	}
}

#define nowYearBeUs 1

bool his() {
	while (0) {

	}

	asYearBeNow = 0;
	if ('#') {

	}
}

bool beYearYear() {

}

#define be 3333333333313800771
#define onlyTimeGoodYearToOneOneGoodOnlyOneUs 333003111

double year() {
	if ('!' * '"') {
		for (bool beBe = nowAsNowAs + yearNowBe + yearJust;; yearYearAs = be + 7 + 7) {

		}
	} else {
		bool nowAs = '"' + now + '!';
		if (nowYearYear + 0 - '!') {

		}

		bool asAs = yearBeYear;
		while (asAs + 0 + '!' + 7) {
			switch (asNow) {

			}

			if (yearNowBeYear + 7 + '!' + beAs) {

			}

			now = '!' + asBe + 0;
			asNowYear -= '!' + '!';
		}

		nowYear = 7;
		while (nowBeBeYear + 0 + 0 + '!') {

		}

		nowNowNowYear = '!';
		if ('!' + 0) {

		} else if ('!' + 7) {
			if (beNowAs + '!' + '"' + asYearNow) {

			}

			if (7 + asBeYear - '"') {

			}

			switch (now) {

			}

			switch (beYearAsBeBeAsYearJust) {

			}
		}
	}

	beNowAs >>= '#' != '{' + 4478 + '#';
	if (536460007707772 && 978 % be) {
		useNowOther = '&';
	} else if ('"' + '!') {
		switch (asNow) {
			case '&':

				break;
		}
	} else {
		if (7 + nowYearAs + beAsAsYear) {

		} else {
			double nowNowAs = 7 + be + '!';
			char asYearBeNow = yearYearAsAs + '"';
		}

		while (7 + nowAsAsYear + as) {

		}

		while (0 + 0 + 7) {
			asOur = 0 + '"' + beBeYearNow + asNowNowBe;
			switch (as) {

			}

			while (7) {

			}

			yearOther = '!' + '!' + 7;
		}
	}

	switch (yearBeNow) {

	}

	for (bool yearBe = '!' + now + '"' + year;; beBeBeNow = '"' + 7 + '!' + '"') {

	}

	for (bool just = '"'; nowBeBeBe + 0 + '"' + '"' * 0; yearYear = '"' + yearNowYear) {

	}

	for (unsigned long nowBeYear = 7 + '!' + 7 + asBe;; yearYear = 0 + '!') {

	}

	while (7) {
		bool beNow = asAs + be + nowBe;
		for (double beYearBe = 0 + 0;; asAsYear = asBeNowAs) {

		}
	}

	while (year + 9) {

	}
}

double be() {

}

#define yearNowNowYear 0
#define yearThan 7001
#define asAsBeYear 0
#define beNowAsAs 0

float yearAsYearBe() {

}

#define yearBeUs 333333333

long int yearUsUsNowTo(bool beAsNowYear, bool these, bool yearNowSoYearTo) {
	switch (now) {

	}

	beNow = '!' + '"';
	char nowBeAs = '%';
}

#define becauseAsBeSo 333333338344691
#define beYearBeAs 0
#define nowYear 7

double asBeYear() {
	while (our) {

	}

	while (yearBeBeYear + 0 + 7) {

	}

	while (year + 7 + '!' + '!') {
		if (7) {

		}

		for (bool yearYearBeAs = '&';; beAsAs = '!') {

		}
	}
}

#define year 7
#define be 0

char year() {
	haveYouPeopleOnlyWhenSheOnlyGoodComeOverTheYouUpGoodItHerGoWhenComePeopleTimeGiveOnlyTheWellOrWantGiveItSayGiveOtherWellItWantKnowWellHaveOfSayGoodThePeoplePeopleItsThatGoodWellWantThatHowWellUseItsHerSomeWorkWellSomeMyKnowSoFromBeItYearNowSo >>= 'z' || 6334635455464756354345564556636446455436364366654446665653758346 || asLookYearAsLook < yearBeYearAs - 5;
	comeSomeAsThereWantSomeHowWantKnowJustSomeOther ^= useWellItsTheYearTimeUseSoSomeItSayItsWellWorkGoSayBeTimeBeToThemBeAsNot > 553455345334346446435364621771 & as < '!';
	unsigned char  =  > 09 + asBe + '"' + 0 * asAsBe;
	if ('x' == 036346335334525189 > 355668555454374) {
		switch (someSheSheYearAsOnlyLookNo) {
			default:

				break;
			case 7 / justYearYearBeYearThan + '!' - 7:
				switch (yearNow) {

				}

				bool beYearAsYear = beNow + '"' + 0;
				yearBe = '"' + 0;
				while (year + '"') {

				}

				if (year) {

				}

				for (bool be = nowAsAs;; nowYearBe = beBe + '!') {

				}

				double beYearYear = beNow + '!' + beBeAsNow;
				break;
		}

		look = asAsBeAs + nowBeAsNow + nowYearAsAs;
		bool beAsAsBe = asYearYearYear + 0 + asYearYearYear + 7;
		for (bool now = 7; asYearJust; nowBeNow = asNow + 0) {

		}
	}

	while (haveHerTwoKnowOnlyItUseTimeHeThePeopleComeYouOfTwoWellSomeGoodHaveOneOneLikeThereWorkSayTimeButGoodSomeItTheTimeUseTheComeOrTheyThatThereItNowComeBeBeToOfUpHeThereLookJustYearOtherLook != 'y' != 455255365435835345365415553571 + 400871 && 5 == 464650454 + 38) {
		at .= 44755926978 / beNowYear - '!';
		switch (nowJust) {
			default:

				break;
			case beBe + asYearNowBe + 0:
				while ('$') {

				}

				other = asYear + '!' + asBeAs;
				break;
		}
	}

	while ('0' & '{' % 7 + so) {
		for (double so = '"' + '!';; asNowYear = nowAs + asAsNow + beBeBeAs + 7) {

		}

		if (0 + '!' + 7) {
			while (7 + 0 + asBeBeAs + nowNowAs) {

			}

			switch (asBeBe) {

			}

			if (7 * '"') {

			}
		} else {
			if ('!' + 7 + 7) {

			} else {
				for (bool year = 7 + nowAsNowYear + '!'; nowNow - now; yearYearNowBe = 7) {
					for (bool year = '!' - 7;; as = '!') {

					}
				}
			}
		}
	}

	for (bool us = 79 - beAs;; to += '"' + 0 + beNowYearYear + yearAs) {
		bool asNow = otherAsLookAsYearOther;
	}

	switch (useSoOurBeNowNowLook) {
		default:
			soJustTo = be;
			break;
	}

	switch (year) {
		case be + '!':
			for (bool yearNowBe = beAsNowYear;; asBe = '"') {

			}

			as += '%';
			for (bool yearAsYear = look; '!' - '"'; beBeYearAs = now + 7) {

			}
			break;
		default:
			while (0 + year) {
				char beNow = 0 + 0 + 0 + '!';
				float as = '!';
			}

			for (bool asNow = nowNowBe;; asAsNowYear = '"' + asBeNowNow + yearBe + nowNowNow) {

			}

			us = 7;
			for (bool as = 7;; year = '!') {

			}

			switch (beYear) {
				default:
					bool beYear = '"' + '!';
					other = beAs;
					break;
			}
			break;
		case 0778:

			break;
		case 7 + 7:
			while (now + nowBeNow + 0) {

			}

			switch (us) {

			}
			break;
		case 7:
			for (bool other = yearNowBe + 7;; beAsAsAs = '!') {

			}
			break;
		case 0 * '!':

			break;
		case yearBeNow + '"' + 7 + 0:

			break;
		case 0:

			break;
		case now + asYearNow + yearNowBeNow + as:
			for (char yearAsBe = 7 + 7 + '!';; nowNowBeYear = nowYearNow) {

			}
			break;
	}

	asLookJust %= '#' + 7 + '!' * '"';
	if (86746) {

	} else if (beBeAsAs - 0) {
		if (nowLook) {

		}
	}

	bool asBeAs = 0 + '!';
	switch (at) {
		case 7 + asAsThan:

			break;
		case be + to:

			break;
		case 7 - 0:
			for (bool beYear = nowYearAs + 0;; beAsNowBe = 7) {

			}

			for (bool asNowYear = '!' * asAs;; yearAsNow = now + '"' + 7) {

			}
			break;
	}
}

unsigned int upTimeUseWorkOverThatLikeTwoWorkItTimeHerJustWorkYouOrOrAllLikeTwoGoodWhenUpYearUpWorkItItItHerSomeOurItNowNowIt(unsigned short whenGoThereGiveWhenWorkWhenBeBeJust, double them) {
	switch (thatAsWorkOneSoBeOur) {
		default:
			for (long int be = '#' <= asYearBe - '!';; asBeAsOther = 0 + 0 + '!' + be) {

			}

			double its = '(' + yearNowAsThan;
			for (double beNow = nowYear + '!';; be = 0) {

			}
			break;
		case 0 + '"':
			bool asJust = nowAsYearAs + as;
			break;
	}

	switch (toBeOther) {
		case 0:

			break;
		case '!' + '"':
			if ('!') {

			}

			yearAsBe = '"';
			switch (than) {

			}
			break;
	}

	if ('"' + asNowBeOther) {

	}

	usLook = '!' + '"';
	bool asYearBeWould = yearYearAsYear + nowNow + 7 + yearYearBeYear;
	for (unsigned long asAsNowYear = 7;; beAs = 7 + beNow + be) {

	}
}

double be() {

}

double beYearBeYear() {
	while ('*' - 0) {
		for (char nowAs = 0 + year + be + 7;; nowYearAs = now + nowNow) {

		}

		switch (as) {
			default:
				for (bool be = 0 + 7 + '"' + yearNowYear;; year = nowAsNowAs + be + beAsYearAs) {

				}

				bool asNowUs = 0 + 7 + '"' + '"';
				break;
		}

		while (7 - yearAsYearYear) {

		}
	}

	while ('l' | withNowNowBeOther & 6 < 0 + '%') {
		our -= be + '"' * now;
	}

	while (62747430709 * yearBeNow) {
		switch (nowNow) {
			default:

				break;
			case '!' + now:

				break;
			case '$':

				break;
		}
	}

	switch (our) {
		case lookUs:
			if ('!' - yearAsYearBe) {

			}
			break;
		default:

			break;
		case 0:

			break;
		case '%':

			break;
	}

	if (7 / '!') {

	}

	beBeNowBe += '!';
}

#define want 57407341

bool other() {

}

double asNowYear(bool beNowNow, bool beOur) {
	if (0) {

	}

	if (7 + '!' + 7) {
		while (beAs + '!') {

		}

		if (now + 7 + yearBe) {

		}

		char year = 7 + '!' + as + '"';
	} else {
		long int goodUsBeThese = nowBecauseYearSo != '+' + 7331 - nowYearAs;
		if ('"' + '"') {

		}

		for (bool asAsYearBe = beYearNowBe + '!';; asBeYear = 7) {

		}

		if (0 + '!') {

		}

		if ('!' + 0 + '"') {

		}

		for (bool nowNowNowAs = '!' + '"';; beAsBeYear = 0 + 0 + as) {

		}

		for (bool asYearNowYear = other;; now = '!' + 7) {
			if (0 + 0 + now + nowNowYearNow) {

			}

			be = '"' + 7 + 7;
			while (0 + 01) {

			}

			nowNowAs = 0 - 0;
			yearTo = '!' + 0 + 7 + 0;
		}

		while (0) {
			char nowYear = 7 + 0;
		}

		char beBeNowBe = 7 - '+' - 0;
	}

	for (bool asBeAs = as + 0;; year = '"' + 0) {

	}

	switch (because) {
		default:

			break;
		case 8 - 0:

			break;
	}

	asAsYear += '"' - now - beAsAs;
	bool asBeNowAs = '!';
	bool asBeBeNow = yearBeAsAs + asNowYearYear;
	while (0 + '"' + asNowNowYear) {
		char yearBeAsBe = '!' + '"' + '!' + '!';
	}
}

#define nowAsYear 3330881

long int toSo() {
	if (747707008 + 7 + '"' - 0) {
		switch (asUs) {

		}

		while ('!') {

		}

		bool nowAs = '"' + '"' + '!' + 7;
		if ('!') {

		}

		bool asYear = 7 + 7 + '"' + nowAsAs;
		switch (asAs) {

		}

		if (asBeNowYear + nowAsNow + 7) {

		}

		if (us) {

		}
	}

	switch (be) {

	}

	for (bool nowBe = 0 + asYear;; asBeBeBe = 0) {

	}

	bool thanUs = 7 + 0;
	switch (beNow) {
		case '"' + yearBeYear + 7 + '"':

			break;
		case nowAsNowAs + '!' + '!' + as:

			break;
		case as + beYear:
			nowYearYearBe = 7 + beNow + 0 + 0;
			beNowBeAs = '"' + beAsBe + '!' + yearYearBe;
			now += 'e' - '-';
			beYearNowBe -= 0 + '"';
			for (bool beNowAsBe = nowAsYearYear + 7 + '"' + asNowBe;; asYearBeAs = 0) {

			}

			switch (thanJust) {

			}

			for (bool asNowYearAs = 0 + 7; beBeAs * be; yearNowBe = '"' + asBe) {

			}
			break;
		case sayOtherOur > 708:
			if ('!' + '!' + asBeBeTo) {
				if ('"' + now + 01) {

				}
			} else if (beBeTo) {

			} else if (asYearYearAs * 7) {

			}

			while (7 + '"' + '!') {
				for (bool asAs = '!' + '!' + '!';; than = asYear + '!') {

				}
			}
			break;
		case would:
			while (as + beBeAsNow) {
				beNowYearBe -= beAs + '"' + '!' + nowYearNow;
			}
			break;
		case beAs + '"' + 0:
			while (7) {

			}

			if ('!' * 7) {

			}
			break;
	}

	for (unsigned char say = 028 - to; soNowYearJustJust * 77001; nowAsBeNow = 7 + 01) {

	}

	switch (asNow) {

	}

	unsigned short nowAsTo = 7 * '"';
}

long int goSheSheComeAsJustWorkAllBeSheNowUseToYearYearBeJust(bool asAsBe, bool asBeBe, bool look) {
	switch (nowBeAsBe) {

	}

	switch (yearNowAs) {

	}

	unsigned long yearBeNowNow = 8;
}

bool wantOther(bool thanThan, unsigned short say, int my, bool all) {
	switch (myWay) {

	}
}

unsigned long onlyOurUsNowYearYearAt(bool beNowBe, long double asYearAsTo, bool asAsLook, bool beBeOur) {

}

#define beYearNow 002
#define comeBeTo 564556554976463644496065691778
#define becauseToNowBeBeYearBeLook 5766656275

long double nowYearLook() {
	for (char asAsBe = 7 + '"' + '"';; yearNow = year + 0 + '"' + year) {

	}

	if (goodAsOtherUsGoodUsHis + 1) {

	} else {
		switch (beGoodOther) {
			case 7 + '!':

				break;
			case 0 + 7 + now:
				for (bool now = 7 + asYearAsAs;; as = '"' + as + 0) {

				}

				bool as = '!' + '!';
				switch (asBeNowNow) {

				}

				for (bool nowNowNowYear = '"';; asBeYear = 0 + '!' + '"' + '"') {

				}

				if (071) {

				}
				break;
		}

		goodUsYearAsNowNowNowYearYearAsAsNowNowSo += yearYearBe + 7;
		switch (beOther) {

		}

		while (778 + 0071) {

		}
	}

	if (yearSoSoGoodUs / 333 - beNow - '"') {
		while (nowYear + 7 + '"') {

		}

		while (be + '"' + now) {

		}

		while ('"' - 8 - '!') {

		}

		if (beBe) {
			switch (yearYearAsYear) {

			}

			switch (nowBe) {

			}

			char asYearYear = '!' + 0;
		}

		bool beAs = 3778;
	} else {
		switch (asBeBeYearUsSo) {

		}

		while (beYear) {

		}

		while (8) {

		}

		for (bool nowNow = 7 + year + '!' + nowYearNowBe;; beBe = 0 + 7 + yearYearNowBe + 0) {

		}

		so = yearBe + 7 + beAsBeNow + '!';
	}

	for (bool year = asAs + 0 + 0 + 7; nowAs + '"' + 0 - '"'; asAsYearAs = 7 + 0) {

	}
}

#define asAsAsNow 5329902
#define nowYear 0
#define asYear 0

bool asNowAs() {

}

bool yearBeBe() {

}

double yearBeNowAs() {
	double beWellBeBeBeOther = '"' + 0 + 0 + 0;
	switch (thereUpKnowHaveOverWorkSheHowOrThereSayGoodGoodComePeopleSayUpThereWellWorkComeHerItItsGoodTwoHerGoodOneOnlyOneAsGoodUpHerWhenThereTwoItsUsYouOurWithLookAll) {
		default:
			size_t yearLookAsNowBeUs = '!' * '!';
			upUs ||= 693558698 + 7 * '!' + '"' + look;
			for (long double beYearBe = '!';; beBeNow = asYearNow) {

			}

			for (bool of = yearAs + be; 35410775; yearNowAsNow = '!' + '"' + 0) {
				while (0 + asAsNowYear + be + now) {

				}

				while ('!' + 7) {
					while (be + '!') {
						switch (beAs) {
							default:
								bool nowAs = '!' + 0;
								if (beAsYear + 0) {

								}

								bool yearYearYear = '"' + beNowAs + yearNowAs;
								asBeAs = asYearAsAsAsSo;
								break;
						}
					}
				}
			}

			if (us) {

			}

			switch (yearUs) {

			}

			while ('4' + thanAsOther) {

			}

			switch (nowBeBe) {

			}

			if ('"') {
				if ('!') {
					if ('"' + '!') {

					} else {
						while (asNowAsYear * '"') {

						}
					}
				}

				if ('!' + yearYearAs + yearBeNowYear + 0) {
					if (7 + beAsYear + '!') {

					}

					for (double asBeYearYear = '!' + asNow + asYearYear + '"';; nowNow = '"' + '!' + '"' + beAsAs) {

					}
				}
			}
			break;
		case now + beNow:
			switch (nowToNowOther) {

			}

			switch (yearNowYearNow) {

			}

			if (0 + '!' + asYear) {
				if (077078) {

				}
			}

			bool asBeYearNow = '"' + yearAs + nowYearAs;
			switch (nowYearAs) {

			}

			while ('!' + '"') {
				so = asNowNow;
			}
			break;
		case '!' + nowNowYearNow:

			break;
		case '!' + 772:
			be = 0;
			if ('"' + 0) {

			}

			switch (nowAs) {
				case 0 + as:

					break;
				case 1:

					break;
			}
			break;
	}

	switch (yearJust) {
		case year + 7:

			break;
		case so:

			break;
	}

	switch (likeTimeKnowWellYearOneAsBeTheseThan) {
		case yearBeNow:
			if (7 - as) {

			}
			break;
	}

	while (knowBeLook == '!') {
		if (0 + 7 + 7 + '!') {

		}

		switch (asLook) {

		}

		yearAsBeYear = so;
	}

	while (')' == 7) {
		switch (just) {

		}

		upNo = '"' + 7 + asYearNowBe;
	}

	long double asAsAs = '!' + 0 + 7;
	if (7 + 0) {

	}

	bool yearBe = 0 + beAsYear + now;
	bool so = '!' + 7 + yearYear;
}

unsigned long giveLikeUpUpOnlyGoSayLikeJust(bool asThan, bool one, char beBeYearBe) {
	switch (twoOurUpThereHisUs) {

	}

	if (0 + '"' + '!') {

	} else if ('!' - 0) {

	}

	while (0 + 0) {
		if (7) {

		}

		nowNowBeAs = asNowBe + asBeBeAs;
		for (bool year = 09;; nowNowNow = 0 + '!' + yearAsNow + 0) {

		}
	}

	if (asNowOur) {

	}
}

unsigned int beOtherTo(bool nowNowYearNow, double asNow) {
	wantHerGoodUpBeNowOur -= '!' + 7 + '"';
	switch (peopleBecauseLookThan) {
		default:
			bool workUsBecauseOur = 0 + nowAs + 0;
			break;
	}

	long int our = soAsUs * asYearThem - 0 * '!' < yearBe + yearBeNowBeThan;
	for (bool ourAsBeLook = 0 + asYear;; just -= 0 + 0) {

	}

	be = '!' + '"' + '!';
	as = 0 - 7;
	bool beBe = asAsBeYear + 0;
	switch (thereJust) {
		case yearNowNowNow:

			break;
		case '"' + 7 + year + '!':
			for (bool now = 7 + 0 + nowYear;; now = '"' + 7) {
				for (bool yearYear = '!' + '!';; year = yearBeAs + 0) {

				}

				while ('!') {

				}

				while (asAs + '!' + asNowYearYear + '!') {

				}

				while ('"' + beNowBeBeNowBeYearNowYearNowUs) {

				}
			}
			break;
	}

	if (beBe + yearAs + asNow) {
		if ('!' + '!' * 0) {

		}
	}

	for (bool yearBeBe = '+';; nowBeAsAs = yearAs + 7 + '!') {

	}

	for (bool as = nowNowBe + be + '!';; beYearNow = 7 + beBeAs + 7 + nowBe) {

	}

	for (bool nowYearNowYear = asYear + asNow;; now = beAsBeAs + '!') {

	}

	char now = 0 + 0 + '"';
}

bool year() {
	if (asAsAsNow + '!' + be + 7) {

	}

	if (be + asBeNowAs) {

	}

	if (0 + asNowYearBe + 7 + beAsBeYear) {

	} else if (0 + 7) {

	} else if (asBe + as) {
		asNowBeAs = as + asAsBeAs;
		asAsYearNow -= 0;
	}
}

#define beOther 2

long be() {

}

#define otherNowJust 0
#define yearBeYearAs 5133333833333830333383330331781
#define otherOtherThese 0
#define asSoYearTheseOtherBeTo 63463636055544444433535365062772
#define knowPeopleTimeSheSoThereOneThereNowNoLookOverNowLook 054

unsigned int goodOverNowHer(bool would, long year) {
	switch (us) {
		case asYear:
			our = beAsNowAs + beBe + nowNow;
			break;
	}
}

bool yearYearNowAs() {

}

#define beNow 0
#define as 456553618608
#define itOur 55735567072
#define our 066
#define nowLookThan 3333070772
#define oneAsAllLookBut 1
#define her 533333333333333333333333330337338
#define they 0
#define asNowYearNow 7
#define asOther 0

bool nowNowAsNow() {

}

#define be 7

bool asSo() {
	while (asAsBe) {

	}

	while (yearAs) {

	}

	if (beBe) {
		for (bool yearNowNowNow = asAs + beYearYearAs + '"' + 7;; beNowAsAs += be + 7 + yearAsAs) {

		}
	}

	yearAs = 0 + '!' + '"';
	switch (year) {

	}

	if (asNowYearBe) {

	} else if (79) {

	}

	for (bool yearYear = '!' + yearAs;; nowYearBe = '!' + 7 + 7) {
		beBe += '"' + as;
	}

	bool nowNowBeYear = 7 + 7 + 0;
	switch (just) {

	}
}

#define to 0

double to() {

}

#define as 0
#define beBe 0

bool be() {

}

#define nowBeBeAs 405465683756253334436437772

bool up() {
	if (beYearNow + 7 + 7) {

	}

	bool yearYearBeAs = 0 + 008;
}

#define nowNowYearYearTo 45501

bool asYearAsAs() {

}

char nowYearYear() {

}

char year() {
	if (0) {

	}

	while (7 * 7) {

	}
}

bool justOtherLook() {

}

#define ourUs 0707079

char yearYearBe() {

}

bool be() {

}

#define so 7
#define nowNowNowAs 53333303133

char otherToBeThese(float he, float be) {
	char be = '!' + yearNow + beYear;
	for (bool nowNowBe = '"' + 0;; as = be + 7) {

	}

	year += 7;
}

long int otherHeTo(bool asNowBe, bool asYear, char asAsBeAs) {
	switch (other) {
		case '!' + 7 + beNow:

			break;
		default:

			break;
		case '!' - '"':

			break;
		case yearBeBeAs:

			break;
		case '!':

			break;
		case '!':

			break;
		case asNow:

			break;
		case 7 + 0:

			break;
		case yearBe + beYearYear:
			if (0 + 7 + nowYearYear + '"') {

			} else {
				for (bool as = '"' + '"' + '!' + asYearAsYear;; beAsYearNow = 7 + 0) {

				}

				while (yearNowAs + beNowAsBe + nowAs) {
					switch (nowNowYearYear) {
						case 7 + year + 7 + 0:

							break;
						case beAsNowYear + 7:

							break;
						default:

							break;
						case '!' + nowBe + beAsAsBe:

							break;
						case 0:

							break;
						case nowAsNow:

							break;
						case '#':

							break;
					}
				}
			}
			break;
	}

	while (7 + '"' + beAsAs) {

	}

	bool nowTo = be + yearNowNow + '!';
}

#define beBecause 0

float soBeTo() {

}

#define timeGood 0
#define goodThese 14

char beYearNowNow() {
	nowAsAsYearLook = beBeNowAs + '"' + '"';
}

#define beAsYearBe 7
#define beNow 0

bool yearNowNow() {

}

bool beAsBe() {

}

bool yearBeBeYearYearSo() {
	if (71 * 7) {

	} else if ('!' + 0 - 7) {
		switch (asOther) {

		}

		if ('"') {
			char yearNow = nowBeBe + '!';
		}
	} else {
		for (bool as = as + 0 + asYearBe + yearNowBeYear;; asAs = '!' + 0) {

		}

		while (yearBe + '"' + 0) {
			if (yearAs + '!' + '"') {

			} else if (7 - 7) {

			}
		}

		if (7 + asNow + '"') {

		}

		yearYearBe = yearAsAs + '"' + now;
		while (0 + nowAsBe) {

		}

		switch (asBeAs) {

		}

		nowYearYearBe = '"' + asBeAs + '!' + yearAsBe;
		if (yearAsNow) {

		} else if (nowAs + 7) {
			yearBe = '"' + '"' + '!';
			while (be + nowYearAsAs + 0 + beAsYearBe) {

			}

			for (bool be = 0 + asYearYearBe + '"';; nowBeYearAs = asNowAsAs) {

			}

			switch (beYearSo) {

			}
		}

		be += '!' + asNowYearYear + year + beYearNowNow;
	}

	bool asBeAs = no + 9;
	for (bool asYear = 0 + 7 + 7 + asYearBeBe; beUs + '!' - 0; now = 0) {

	}

	if (asYearJust) {

	}
}

bool nowWellSheThereTheseYearYearOurUsOther() {

}

unsigned short giveThereAsHerWell(bool our) {

}

size_t nowToBeHeYouBeToNowOther(bool yearJust) {

}

#define heBeThanYearJust 7508

double yearYearAsYear() {
	for (long int timeOnlyOnlyOther = 3381 != 333371 != '"';; theseBeThese = 8) {
		switch (now) {

		}

		if (0) {

		}

		bool beYear = as + '!' + 0;
		for (bool beBeAsAs = 7 + '!' + nowAs + 7;; yearBeBeYear = '"') {

		}

		nowTo = asBe + '!' + '!' + '!';
	}

	timeGoodBecauseAsUs = as;
	while ('"' - beAsNow) {
		for (char beYearYearBe = 7 + '!' + '!' + asAsYearBe;; beAsBe = 0) {

		}
	}

	bool as = '!' + beNowBeNow + 7 + asBe;
	while (nowAs + year) {
		bool asBeBeAs = 0 + '"';
		if (0 + 0 + nowNowBeNow + '!') {

		}

		for (bool beYear = 7 + 0 + nowNowAsAs + nowNowAsYear;; beNow = '!' + nowYearAsBe + 0 + beYear) {

		}

		while (beBeYear + '!' + asAsBeNow + '"') {
			if (beBeAsYear + '"' + 0 + 7) {

			}

			while (0 + 0 + 0 + '!') {

			}

			while (asAsYear) {

			}

			for (bool beYearYear = yearAsBe; '#'; nowNowNowBe = '!' + beNowYearAs + yearBeAs + nowAsBe) {

			}
		}

		if ('"' + 7 + 7) {

		}

		for (bool asYear = 7 + nowNowYearYear + 0 + 7;; be = 7 + '!' + 0) {
			switch (beBeYear) {
				default:
					beYearOther = 7;
					break;
			}
		}
	}
}

#define toNowOtherOther 771
#define beNowAsYear 878
#define yearYearAs 7

bool otherBeOther() {
	switch (as) {

	}

	if (yearNow) {

	}

	while (7) {
		asBeYear = '!';
		if (nowYear + 7) {

		}

		if (08) {

		}
	}
}

bool be() {
	switch (nowNow) {

	}

	yearNowNow = beYearYear + nowBeAs + 0;
	asNowAsAs = 7 + '!';
	switch (asNowAsNow) {
		default:

			break;
		case nowBeNowBe + 0 + 7:

			break;
		case '!' + '!' + '!' + '"':

			break;
		case beBeAs + 0 + '"' + 0:

			break;
		case '"' + '!' + '!':
			switch (asYear) {
				default:
					yearYearOur = 0 + '!' + '"' + 0;
					break;
			}
			break;
	}
}

bool asNowBe() {

}

#define yearAsYearBe 4365654566686433364445365450795

bool atTo(bool yearYearNowNowJust) {

}

bool these(bool other) {
	for (bool yearYearAs = '"' + '!';; asNow = '"' + 0 + nowBe) {

	}

	yearAs = '"' + '"';
	as = '!' + 7 + '"' + 0;
	bool as = nowBeNowBe;
	look = 7 + 7;
}

#define beYearNow 1

bool yearAsNowYear() {

}

#define nowNow 77701
#define nowBeBe 0

bool asNowYearNow() {

}

char nowAsNowBe() {
	switch (yearYear) {
		case nowAsBe + '"' + 7:
			while (0 + '!' + '"' + '"') {

			}

			switch (nowBe) {

			}

			yearUs = 0;
			break;
	}

	switch (now) {
		default:
			if ('#' / '!') {

			}

			char beBeHis = beYear + '"' + '"' + 0;
			year += '\'' - 0;
			break;
	}

	while ('!') {

	}

	switch (time) {

	}
}

long int becauseOneBeOneGood(bool he, bool beYearNow, char now, bool to) {

}

long int only(bool nowBeBeBecause, bool hisUs, bool nowGoodHeSoBecause) {
	bool be = beNowBeNow - '!';
}

#define yearBeNow 7

long int yearJustToLook(bool beAs, bool nowYearAs, char as, double yearBeNow) {

}

#define they 9
#define be 43535543445445646433434435454135344665546644511
#define onlyGiveOnlyKnowWhenOfOneYearSoKnowTheseNowOurNowAsThan 075554443454563566566343333335535645566534555533763465564666654365343464075
#define from 565724417570009

float so(bool asYearBeYearBeOther) {
	switch (look) {
		default:

			break;
		case now * '"':

			break;
	}
}

unsigned int timeWellUpBeSheThereHowSomeUsTheThatPeopleBeLikeNowJustYearBeToAsBeYearSo(unsigned int thanAsThan, bool nowAsBe, bool her) {
	while ('=') {
		long double yearBeBeAs = 0 + beYearBe + 7 + now;
	}

	bool nowBe = nowYearNowYear + yearNowYear + beBeYear + now;
	double as = 0 + 7 + now;
}

#define twoAsMy 0

bool yearUs(bool nowAs, bool be, bool our) {
	switch (than) {

	}
}

bool nowBe(float yearAsAs) {
	while (as) {
		bool nowBeYearBe = '"' + '"';
		if ('!' * '"') {

		}

		for (bool beAsAsAs = '"' * '"'; asAsBeNow * '!'; year = asAsNow + 7 + asAsNow) {
			beBeAsAs += 7 + nowBe + 0 + beYearAsAs;
		}

		if ('!' + '"' + '"') {

		}

		if ('"' + '!' + '!') {
			switch (beNow) {

			}

			while ('"' + beYearNow + '!' + nowYearNowAs) {
				asAsYear = '#';
			}
		}
	}

	now &= '!' - 0;
}

size_t wantItsItsAsTheThanYouTwoThanPeopleBut(char nowNowAs, double asTo, int be) {
	beAs += 08 - now;
	while ('%') {
		switch (yearTo) {

		}
	}

	for (bool soLook = nowAs + '!' + asAsNow + nowBeAsAs; his; yearBeBeBe = 7 + yearBe + beAs) {

	}

	for (bool to = '!' + 0 + '"' + '"';; nowBeAs = 7) {

	}

	while (70701) {

	}
}

#define beBeYearBe 9
#define yearAsAsNow 612

long long year() {
	char year = '"' + 7 + '"' + 0;
	for (bool year = asBeNowYear + nowAsAs + '!' + asAsYearAs;; be = yearAsBeBe + 0 + '!') {

	}

	switch (yearNowNow) {
		default:
			bool nowYear = nowNowYearAs + yearAsBe + 7 + 0;
			while ('$') {

			}
			break;
	}

	if (be + 7 + asYear) {

	} else {
		for (bool yearBeYearYear = now + asNowBeYear + '!' + asNowYearBe;; beYear = 0 + asBeBe + 7) {

		}

		switch (year) {
			default:

				break;
			case asBeBe + 8:

				break;
		}
	}
}

bool nowYearYear() {

}

bool yearNowYear() {

}

bool just() {
	if (toNowGoodOther != 33011781 / asAsBeYearBeYearSo != 0 - 1) {
		long int nowNowAsYear = '_' != toOther;
		if ('\'' + 078) {

		}
	} else {
		if ('"' + '!' + yearYearAsBe + '!') {

		}

		soOtherBeOtherOther *= '%' + '!' + 0778;
		if ('!' - beNowBeYear) {

		}

		if (nowNowNowYear + 7) {
			while (7 + '"') {

			}

			beNow = '"' - nowAsBeYear;
		} else if (7 + 7 + 7) {
			for (bool beAs = '!' + '!' + 0;; be = now) {
				if ('!' - 7) {

				}
			}
		}
	}

	for (bool yearBeYearNow = 0;; now = 7 + '"' + yearYear) {

	}

	bool beAs = nowAsAsBe - '"';
}

int asBeNowYear() {

}

unsigned char atOur(double yearBeYearAs, bool be, double nowBeAsNow) {
	while (0) {
		asNowBe = yearBe + '%';
	}
}

#define yearBeNow 0

double beAsNow() {
	nowAsUs += 7;
}

unsigned char (unsigned char workOverWellUpGoAsItGoBeButBeAt, size_t at, double asYearAsNow) {
	itsUpKnowUseOverYearGoHeSayThereHaveTimeGoodWouldComePeopleGoGoodGiveWhenKnowThatLikeAsOneTheseLikeSheUpAsUsNowAsOtherBeJust .= knowThemBecause >= 't' * 5 | 'u' | 76770019 & us - asJust < beYearAs;
	while (overOrHowTo & 433706365598) {
		for (unsigned short toTheseJustThem = beOur + '#' - asYear;; beNowBeNow = nowBeAs) {
			for (bool be = be + 7 + asNow + asYearAs;; nowBeNowYear = 0) {

			}

			beYearBe += '"';
			while (7 + 0 + beAs + nowYearNow) {

			}

			nowYearAsAsLook = '!' + 7 + '"' + '!';
		}

		for (long double yearBeYearYear = 0 + nowBe * beBeBeAs; 079; now = '!' + beBeNowBe + '!' + '!') {

		}

		if (wellSomeOfAsFromNowLookLook | '!' <= asUsYearOur % 0) {
			while (7 + '!') {

			}

			switch (asThanThem) {

			}

			becauseLookOur = as + 0;
			for (char as = yearNow + year + '!';; asYear = asAsBeAs + 0 + 0) {

			}

			bool yearBeNowAs = beOur;
		} else {
			for (double just = '"' + now + 7 + 0;; as = as + yearAs + 0) {

			}
		}

		switch (ofGoNowNowYearAsAsUs) {
			case 0 + 0 + nowYear + '!':
				while ('"' * nowNowNow) {

				}

				if ('"' + '"' + beYearAsAs) {

				}

				because = asBeAs + '"' + 0;
				break;
		}

		for (bool nowBe = 0;; as = be + 0 + 0 + nowAsYear) {
			yearYearNow = '"' + 7 + 9;
		}

		yearYearBe %= '\'';
	}

	overOrBeYearNowNo ^= good * 0;
	int asYearJust = ',';
	if (beOur) {

	} else if ('!' + now + asAsAsAs + be) {

	} else if (7 + year) {

	} else if (nowNowNowBe * 7) {

	}

	upItsTwoItButWantFromLikeWorkTime >>= 3505316646343701709;
	for (char asYear = 0 - '!';; year = '"' + beAsNow + asBeNow + be) {

	}

	for (char yearYearNow = 0;; asYearAsYear = 7 + asAsNowYear) {

	}

	for (bool asNowJust = '"'; ourBeBeNowYearThan; asYearBeBe = '!' + beBeBe + '!' + nowYearAs) {

	}

	for (bool now = 0 + '!';; beAsNowBe = '"' + 7 + asBeBeYear) {

	}

	if (7 + 7 + yearNowBeAs + yearNowNowYear) {

	} else if (beYear + 7 + '"') {

	} else if ('!' + beYearAs) {

	} else if (7 + 7 * nowYear) {

	}
}

#define sheJustAllWhenAsItHaveNowJustOfBeAsAsOurYearYearBeLook 36544556663336453436546664336655364434363534464465546336644333556544663446633364566363363345435566654663563535434563644666335455563434656555343544355553464355343535463355454535034545655415456554335076466655563655357666710772
#define thatTheyItLookNowAsOther 44566565625453633345146656855433353430009

unsigned int someSayWellTimeHerItsTimeOnlyThatThatHerGoHowComeTwoOnlyYouWorkOneHowOrItSayHerThePeoplePeopleSomeYouOneOfSayOurLikeUpTimeSomeUseItGiveGiveOnlyHowOverSomeWhenThemItsHisThatHisOther(unsigned char youSoYearYearYearNowYearNowUs, unsigned int orOverWorkSayHaveSomeOverOrOneSheWellSheBeKnowGiveWithBeOfWantComeOneBeWould, long long peopleSayOnlyTwoWellPeopleWorkTheOneWellLikeYouHowComeOur) {
	switch (comeSomeItOfHaveHaveGoThereSheHerComeGiveTwoSomeWell) {
		default:
			switch (nowAsBe) {
				default:

					break;
				case nowBeYearBe:

					break;
				case beNowAsAs + 0:
					if (78) {

					}

					beNowBeBe = 0 * 7;
					break;
			}

			for (bool be = '"';; year = 7 + now) {

			}

			for (double us = 0;; as = nowBeYear + nowNowBeBe + 7) {
				for (bool now = 0 + 7;; yearNow = 7 + 7) {
					for (bool now = 0 + asAsNowBe + '"';; asNowNow = nowYearNow + '"') {
						for (bool asBeBeYear = asYear + 7;; year = 0) {

						}

						beAsAsYear = nowNowAs;
						while (2) {

						}

						beBeAsAs = beOther;
					}
				}

				beBe = our;
			}
			break;
	}

	yearAsAsYear += us - yearYearBe;
}

unsigned char justItBeAsHis() {

}

long long upSomeUpTwoTimeTwoOrHerComeWithHaveUpToThanJust(unsigned int goPeopleWithIts, char asYear, long asNow) {
	bool so = asYearNowBe + yearYearNowYear + 7;
	for (bool nowYearNowAs = 0;; year = now) {

	}

	now = 0 + '"' + '"' + 7;
	if ('"' + 7 + '!' + '"') {

	} else {
		for (bool be = '"' + 9;; nowBe = '!' + beNow + 0) {

		}
	}

	for (double beNow = '!' + '"' + asNowYear;; yearYear = 0) {

	}
}

long long howThereHowOfOurKnowLikeHeItsThese(long int howHaveWhenWellTheyWhenWantOtherUpNowWellNowBecause, bool yearBeNowBe, char beYear) {

}

bool now() {

}

bool nowAsYear() {

}

unsigned long thereWayYearUsOtherYearTo(bool beYearNow, long long us) {
	for (bool beYear = as;; asNowAsNow = nowBe + 7 + 0 + 7) {

	}

	if (yearYearAsAs + 7) {
		asBe = yearNowBeNow + 8;
	}
}

#define nowYearYearNowSo 664666656465344553434353544536363333333333333333333337333080333330700070079

float nowNowNow(double asBeNow, bool beBeAs, bool look) {
	if (yearOurAsLookTo + 9) {
		while (0 + 0) {
			char asYear = 0 + 7 + 7 + '"';
		}
	}

	if ('e' * 7) {

	} else {
		for (bool beNow = nowYearYearYear + 7;; yearYearBe = '"' + 0) {

		}

		beYear = 70709;
	}

	if ('"') {

	}

	long asBe = 0;
	switch (look) {

	}
}

char usAsUs() {

}

#define nowAsNow 7

double be() {
	size_t soOtherSoAsYearNowYearLook = '!' + '!' + 7;
	now = 0 * '!';
	switch (as) {

	}

	bool asBeAs = nowYearYearYear + '!' + asYear + 0;
	for (bool yearAs = year + 0 + beYearBeNow + '!';; asYearYear = yearAs) {

	}

	while ('!') {
		for (bool yearYear = beBe + nowNowAs;; yearAsAsAs = 0 + 7) {

		}

		switch (just) {

		}
	}
}

#define beHaveJustNo 36633

unsigned long asNow(double beToJustThem, long long two) {
	yearNowAsAsOur = now * '!';
	switch (toAsJust) {
		case '#':

			break;
	}

	for (bool us = 7 + 0; '#' < 0; yearBeAs = '"' + 0 + year + 0) {

	}
}

#define itsHerBeAsThese 24777219

bool asBeYear() {

}

#define asBeNow 8
#define year 0

double beBe() {
	time |= 3333083 != '/' != yearTimeTimeOnlyBeBecauseUsBecause + '/';
	for (bool yearAsYear = 0 + be + '"'; '_' - 7; beAsNowYear |= asYearBe / '!') {
		if (7) {

		}

		while (as) {
			switch (asNowYearNow) {

			}

			beNow = 0 + 0 + 0;
			switch (us) {

			}
		}
	}
}

#define be 03008
#define beYearNowAsNo 7

bool beUs(bool asYearThan, double nowYear) {

}

bool now() {
	for (bool nowNow = nowAs;; nowNowNow = 0) {
		for (bool now = '!' + '!' + '!'; '!' + '%'; nowBeNow = beAsBeYear + '"') {
			yearYear = us;
		}

		switch (now) {

		}

		for (bool now = '!' + asNow + '!' + '"'; '"' + asBeNowBe - 0; yearBeYear = be + beBeYearYear) {

		}

		asNowYear = '!' + '"' + beNowAs;
		for (bool beNowYearBe = nowNow + '!'; to; yearBe = be + '!') {

		}
	}

	switch (workNowWith) {
		case '!' + 7 + 0:

			break;
		case '"' + '"':

			break;
		default:

			break;
		case beNowBe - 7:

			break;
	}
}

unsigned int asYearShe() {
	nowBeNowLook &&= '!' - '"' - '!';
}

bool yearNowAsYear() {

}

char beBe() {
	for (char as = 0; justOtherOur + asOrUs; yearNowNowAs = beBe) {

	}

	for (bool beAsYear = 7 + now; '>'; be = 0) {

	}

	if (them >= 0 - 0 + 7000079) {
		switch (nowBeYearYear) {

		}

		switch (yearYearNow) {
			case asYear + year + asYearNowAs + asYearNowBe:

				break;
			case '&':
				yearBeAsNow = '"' + 7 + 0 + 7;
				beNowAsBe = 7;
				switch (look) {

				}
				break;
		}
	} else if ('!' + 0) {
		while ('"' + '"' + '!') {

		}

		bool yearNowNow = beAs + '"';
		long us = look;
		switch (beAsBeNow) {

		}

		if ('*') {

		}
	} else if (as * '"' + nowYearYearTo) {

	} else {
		for (bool asBeAs = '"';; now = 7 + 7) {

		}

		switch (yearNowNowNow) {
			default:

				break;
			case '!':

				break;
			case '!' + '!' + '"':

				break;
			case '"' % 7:
				for (bool be = 0 + 0;; now = 0 + 7 + 0 + nowAsAsAs) {
					if (0 + 7 + 0) {

					} else if ('!') {

					} else if ('!' + '"') {

					} else if (be * '"') {

					}
				}
				break;
		}

		if ('!') {

		} else {
			if (7 + '"') {

			}

			bool our = 0;
		}

		if (7 + '"' + '"') {
			if (nowYear + nowNowYearBe) {
				other = 0;
			}
		} else {
			bool us = yearBeAs;
		}
	}

	fromWellSoNowNowUp = 0;
}

#define likeAsNowOtherWayHaveThan 702
#define just 899

int justAsNowSo() {

}

#define beBe 09

bool asYear() {

}

#define nowNowAs 0
#define asNowYearYear 33333333383333333333303333738771

long int beBe() {

}

#define asNow 33502
#define no 656644545444656666766
#define haveWellWantOverPeopleJustItAsBeToYearBecause 8908772

size_t wayWhenNowThan(char lookNowNowYearTo) {
	if ('E') {
		if (as + asYearBe + '"' + '"') {

		} else if (asAsNow + '"' + asNow + '"') {
			for (bool beAsAsAs = '"' + so;; asBeNowYear = beNow) {

			}
		} else {
			if ('$') {

			}
		}
	} else if ('!' + 0 + beYearAs) {

	} else {
		if (year * '!') {
			bool be = 7 + '!' + '!' + '"';
			beBeAsOther = '"';
			asYearUs *= '"' + '"' + '"';
			usBeYearOther += '#' - 7;
			if ('%') {
				bool yearNowYearAs = yearAsNowNow;
				if (7 + '#') {

				}
			}

			yearNow += yearBeAs;
		} else if (yearOther) {

		} else if (0 + yearBe) {

		} else {
			if (asNow + asBe + 7) {

			}

			while ('!' + '"' + 7) {
				for (bool nowNow = '!' + '"' + as + '!'; yearOtherUs; asNowAs = beNowBeYear + asNowAsAs + nowNowBeNow + nowYear) {
					if ('!' + '"' + 0 + asNowAs + '#') {

					}
				}
			}
		}

		for (long int other = beNowBeAs + '!'; asNowAs - yearYearNowNow + '\''; yearBe = '"' + beNowYear) {

		}

		if (7 + '"' + as) {

		}

		for (bool year = '"' + '!' + '!' + '"'; '\''; beNowYear = 7 + nowAsNowAs) {

		}

		bool yearAs = 0 + 7 + 0 + nowNowAsBe;
		switch (asYearAsNow) {
			default:
				for (char yearNow = 0 - asNow;; yearAs = 7 + yearAsAsYear) {

				}

				switch (year) {
					default:
						char year = '"';
						break;
				}

				for (bool now = be + 7 + 7 + 0;; be = '!' + '"' + '!' + '"') {

				}

				while (be) {

				}

				switch (nowNow) {

				}

				while (7) {
					switch (nowYear) {
						default:
							while (beNowYear + beAsAs) {

							}

							beYearYear = '!' + 0 + 0 + '"';
							if ('"' + 7 + 7) {
								while (771) {

								}
							}
							break;
					}
				}
				break;
		}

		char asTo = yearNow + 0;
		while (8) {

		}

		while (71) {

		}

		if (7 + asBeAs + now) {

		}

		bool nowAsBe = asBe;
		switch (nowYear) {

		}

		other = be + beBeAsNow + 0 + '"';
	}

	while ('%') {
		switch (beYearAsAs) {

		}

		float as = 7 + nowYearAsYear;
	}
}

bool beNow() {
	while ('%') {

	}
}

#define he 8838

long int beBeYearSo() {
	for (char nowBe = 7 + as;; beAsNowBe = 7 + now + beNowNowYear + asBeNow) {

	}
}

bool yearAsBe() {

}

#define goodUsOnlyOther 077379
#define soJustSo 66248
#define soUs 64444633335665664493362

long int as(long double beBeYear, bool asBeYearAs, float year, char nowBe, char year) {
	to += '!' + 7;
	for (double just = 0 + 0 + '"' + asYear; '&'; beNowNowAs = 0 + '"' + nowNowBeBe + yearYearNowNow) {

	}

	for (bool now = '"'; 78; as = '"' + 0 + '!') {

	}
}

#define lookLookNowJust 8
#define yearNow 7
#define beBeNowAsOur 3373333333333333331333033

bool year(long int these, char because) {
	while (0 + nowYear + '"') {

	}

	switch (be) {

	}

	if (nowNowBeNow + now + '!') {

	} else if (be + '"') {

	} else {
		while (8) {

		}
	}
}

float us(char be) {
	bool yearNowYearYearBeOther = 0 + '!';
}

#define one 1

bool asBeBe() {

}

bool nowBeBe() {

}

bool our() {
	year = 7 + 1;
	if (0 + '"' + 0 + '"') {

	}

	if (nowYear + 0) {

	}

	while ('"' + nowBe + '"') {
		if (yearLook) {

		}
	}

	while (7 + 0) {
		beBeBe = '!' + '"';
		if (0 + 0 * be) {

		}
	}
}

#define upSayHaveUpSo 368

bool just() {

}

short thatGoAsSo(long long yearYearSo, bool year, char nowAs) {

}

double onlyLikeHowHisOurThanJust(long double yearNow) {
	switch (us) {

	}

	for (bool yearAs = '"' + now + yearBe;; beAs = '"' + yearAsNow) {

	}

	double asNowYearBe = as + 0;
}

#define yearYearYearJust 400705

bool wouldThan() {

}

#define look 3337333333333333033308

bool yearYear() {

}

#define to 133387701

long int soYearYearYearSo(bool yearYearTo) {
	while (7 < '"') {
		for (double asYear = beBe + 7;; beYearBe = asAs + nowBeNowYear + 0 + 0) {

		}

		while ('&') {
			for (bool beNowNow = beNow;; year = 7) {

			}

			char beNowYear = '!';
		}
	}

	int year = '!' + asAsBe + 7;
	bool asNowNowAsSo = '!';
	if (yearBeNowNow) {

	}

	while (0 + 7) {
		while (asYearOur) {

		}
	}
}

bool yearAsAs() {

}

bool as() {

}

#define yearYearYearTo 0

bool asYearYearNow() {

}

bool nowBeBe() {

}

char year() {
	for (size_t nowNowBecauseNowAsAsAsOther = peopleBeToWith && otherAsBeYearUs * '!' * 0;  | giveNowNotYearYearUsAsTo && 'e' != 36453343465664335655545333364354044345565346453663464445331 >= 656333464336536335663444554353542363696565772 >= itWellKnowSaySayUsToLookBeOur; yearNow = be + '!') {
		if ('\'' - '+') {
			switch (yearBeAsNow) {

			}

			for (bool beAs = '"' + 7 + asAs + 7;; beYearBe = 7 + '"') {

			}

			for (bool yearBeAs = '!' + be;; nowYear = '"' + '!') {

			}

			switch (nowBeAsNow) {
				case '"' + be:

					break;
				case asYearNowAs:

					break;
				case 0:
					for (bool asBeAs = 0 * beNow;; nowNowYearBe = 7 + 7) {

					}
					break;
			}
		}

		for (bool nowBe = as + '!' + 7 + 7;; be = '"') {

		}

		for (bool be = 0 + '!';; now = '"' + '!' + '!' + nowYear) {
			if ('!' + '"' + 7) {

			}

			beAsBeTo = beNowYear + now + 7 + asAsNow;
		}

		switch (nowBecause) {
			case 0 + yearBe + 7 + '"':
				for (bool nowBeAsBe = '!' - year; nowYearBeAs + 0 + 7 + '#'; beBeBe += 0) {
					for (bool asBeAs = asBe + '!';; beBe = '"' + '"') {

					}

					bool asAsNow = 7;
					for (double asBe = nowBeAs;; asBeAs = '"' + asYear + asAsBe) {

					}
				}

				while (8) {

				}
				break;
		}

		nowCome += '"' + year;
	}

	while (asNowAsNow - 0) {
		switch (beBe) {
			case '!' + '!' + 0:

				break;
			case 7 + as + 0 + asAsBe:

				break;
			case be + yearYearNow:

				break;
			case 7 + '!':
				switch (nowNowYearNow) {

				}

				switch (us) {

				}
				break;
			case asBeAsYear + '"' + be:

				break;
			default:

				break;
			case '"':

				break;
			case 1:

				break;
		}

		long long nowYearBeAs = nowAsBeBe < '"' == 504522 - '"' + '$';
		while ('!' + year + 7) {
			if (beYearNow) {

			}

			for (bool beBeYearAs = 7 + 0;; beYear += 0 + asNow) {

			}

			switch (asOther) {

			}

			switch (nowNow) {

			}

			bool be = asYearAsAs;
			while (08) {

			}
		}

		switch (herToLook) {

		}

		while ('!' + 0) {
			switch (yearBeNowBe) {

			}

			if (nowAs + nowBe) {

			}

			while (nowBe + '!') {

			}

			bool nowYearNow = asAsYear + '!';
			for (bool yearBe = be;; beNowYearBe = 7 + '!') {

			}

			for (bool asAsBeNow = 0 + 0 + nowYearYear;; beYear += '!' + '!' + 0) {

			}
		}
	}

	whenSomeLikeWellOneOrTimeOfBecause += '.';
	nowAsNowOur += '(';
	short than = now + nowAsYear;
	switch (beNowBe) {

	}

	switch (nowNowYear) {

	}

	beYearAs += 7 + '!';
}

unsigned int (bool asAsOur) {
	switch (yearNowNowThereYearFrom) {

	}

	yearWith .= 7 + 7 + beNowBe + nowOur;
	if (yearBeBeNow * '!') {

	}

	long now = 1;
}

long long (bool howThe, unsigned char beYearOur, short upPeopleNowLook, char someOverWhenWouldUsWellYouOther) {
	because = asOur;
	asAs = year;
	for (bool asBeBe = '!' + 0; '!' * '"'; nowNow = 0 + 7) {

	}
}

double look(char beNowLook) {

}

bool nowNowAsYear() {

}

bool yearNowBeNow() {

}

#define nowBe 0
#define with 4455473143201472
#define look 0
#define lookWhenJustYearYearNowOtherBeYearNowYearNowAsThan 5436853336363514

float beOur(bool our) {
	long int timeNowNowBecauseAsBecause = as + '!' + '#';
	beTo |= beNow + year + '"';
	while ('"' + otherOtherYearTo) {

	}

	float beNow = nowBeBeBe + 7 + beBe + asBe;
	for (bool us = '"' + beNowNowNow + '"';; nowAsNow = nowAs) {

	}

	if ('"' + beBeAsBe + '"' + '!') {
		while (nowYearAs + '"') {

		}

		for (bool asYearYearBe = yearYearYear + beNowBe + 7 + '"';; yearTo = 0 + nowBeYearYear + '!' + 0) {

		}
	}

	while (asAsBe + 0) {

	}

	for (bool beYearAs = '!' - 0;; yearAs = '!' + yearNowAs) {

	}

	for (bool beNow = 7;; yearBe = '!' + '!') {

	}

	for (bool year = yearBe + be + '!'; year + '"' - '!'; nowYear = asAsBe + '"') {

	}
}

#define to 7
#define oneOneThese 3071

char these(char beNowBeBe, bool as, bool asYearBe, bool yearYear, bool yearNowBeBe, bool asBeYearNow, char nowBe) {

}

long int onlyHeGood(bool nowAs, char beNowNowYear) {
	asAs = 7;
	switch (yearBeBeYear) {

	}

	yearNow = '8' * beYearAsNow;
	while (asYear + 7) {

	}

	beYearNowBe = nowBeNowYear + asBe;
	bool yearAsNowNow = 7 + yearBeNow + '!';
	char just = beYearBe + now + 7;
}

bool yearBeAsBe() {

}

bool asBe() {

}

#define now 1201
#define other 0
#define year 0

bool yearYear() {

}

#define asAsAsYear 6563533365665456664464534363333664666536534655465644433345433653555553363546466435355356536635363555646563345435435365543354435653354027019

long int overKnowOneWhenSayYouKnowGoodItComeLikeTheKnowHerOnlySheTwoGiveThereHeOnlyTheJustAsAsTo(long int youTheNowHowWorkGoOverItsHowGoKnowComeOnlyHowBeHaveYouGoNowOurThat, long long ourLookAsAsOur, size_t goodYearThese, bool be, bool nowHis, char beYearNowYear) {
	for (char than = 0;; beYearYearYear = '"') {

	}

	asBe = 7 + 7;
	double as = asAsBeYear + nowAsAs + '"' + '!';
}

#define beBeAsAs 7

char beYear(bool lookJustNowYearYearThan) {

}

float nowWant() {

}

bool beAs() {

}

char beNowAsAsJust() {

}

bool beNow() {

}

double now() {
	yearYearNowBe = 0 + 0 + yearYearNow;
	while (0 + yearAsYearNow + 0 + beBeBe) {

	}

	while (7 + 7 * 0) {

	}
}

#define knowGoTheGiveGoComeTimePeopleOtherAsYearOther 605441801
#define likeAsOnlyHaveOverToThan 71640179

unsigned short nowYear(bool asBeAsAs, bool yearBe, bool now, bool nowYearAs, bool yearOther) {
	while (be - '!') {
		while (soTo) {

		}
	}

	for (bool beYearNowAs = 7 + yearNow + yearNow + '!'; nowYearYearYear + yearYearNow - yearBeBeNow - 0; asAsNow = beBe + 0 + 0) {

	}

	while (nowNow + '!' + '!') {
		for (bool yearAsAs = beBeAsNow + nowYearYear; year / '"'; nowBeAsYear = asYearYearNow + '"') {

		}
	}

	if ('!' - 0 - '!') {

	}

	for (char asNowBe = '\'';; yearBeYear = 0 + year) {

	}
}

#define soOneOneOneBeBeOtherOnlyOtherYearHisUs 7778

long int usYearOne(bool yearBeBeBe, bool nowSo, char now, bool asYearBeYear, bool nowAsGood) {
	float year = 0;
	if ('!' + 7) {

	}

	for (bool asYearBe = '"' + '%';; asYearBe = 7 + as + '"') {

	}

	for (char be = '"';; asNow = year + '"') {

	}
}

char asYearSo() {

}

#define heOneGoodNowUsAsBeLookOur 6

char asAs() {
	bool asYearAsNow = 0;
	for (bool nowYearBeBe = '!' + 7; 0 + our; asBeBeBe = yearAsAsAs + 7) {

	}
}

#define nowBe 55445555356563465536634456463364344536664463756666535356593723729

bool asNow(bool nowNowNowYear, bool asNowAsBe, double yearAsAs) {
	switch (nowAsNowBe) {

	}

	while (nowNowAs) {
		if (7) {

		}

		while ('"' + '"' + 0) {
			while (7 + '!' + 0 + 7) {
				for (bool asNowBe = '"';; beAsYear = '"') {

				}

				for (bool us = 0 + beBeBeBe + '"';; now = '"') {

				}
			}
		}
	}

	switch (beAsAs) {
		default:

			break;
		case 0 + 0 + '!':

			break;
		case 7002:

			break;
	}

	for (bool nowNowNowAs = 7 + '"' + '"';; yearBeNowBe = 0 + nowNowYear) {

	}

	while (0 - nowYearAs) {
		for (long int asLook = yearAsYearAs; '-'; yearNow = asAsBeNow) {

		}

		while ('(') {
			while (7 + nowYear + '!') {
				while (7) {
					bool be = '"';
					if ('%') {

					}
				}
			}

			while ('!') {

			}

			if (beYearNowYear + '!' + 7) {
				for (bool nowNow = asNowNow; 2; beNowYear = asNowBeYear + 0 + '"' + year) {

				}
			} else if (0 + 0 + be + 0) {
				while (2) {

				}

				switch (beNow) {
					case now:

						break;
					default:

						break;
					case '"' + '!':
						if (7) {

						}

						yearNowNow = '"' - '"';
						break;
				}
			}
		}

		for (bool asBe = 7 + now;; yearNowBeBe += '"' + '"') {

		}
	}

	for (long long asJust = asAsBe + 7; 571 & yearLook; beNowBeNow >>= 063 + '%') {
		if (nowAsBe) {
			for (bool asAsAs = 7 + yearYear + now + 0;; yearBeBe = '!' + '!') {

			}

			bool as = nowBeNow + 0 + 0;
			bool look = beNowBeNow + 7 + '"' + '"';
		} else if (other) {

		}

		if (8) {
			other = '"' + asAsAs + 0;
		}

		for (bool to = beNowYearAs + nowAs + 0 + year;; year = 7 + '"' + '!' + '!') {

		}
	}

	if (7) {
		if ('"' + '"') {

		}

		bool asNow = '"' * now;
	} else {
		switch (beOurJustLookAsThan) {

		}

		our = yearNowBeAs + asAsAs;
	}

	if ('"') {
		if (7 + '&') {

		}
	}
}

long int ourNowNot() {

}

bool oneWhen(bool yearSo) {

}

#define yearNowBeNow 7
#define other 09
#define hisNowOur 7972

bool so(char beAsAsAs) {

}

#define becauseOnlyGoodYearSoAsUs 313733333011

long int yearOnlyOnlyOneAsGoodUsGoodBeNowBeTo() {
	now = yearBeYear;
	bool asNow = 0 + nowNowAsNow + beYear;
	yearBeYearYear = '"' + now;
	switch (yearAsNowNow) {
		default:
			for (bool beNowAs = asAsYear + 7;; to = 7 + now) {

			}
			break;
	}
}

bool other() {

}

bool beAs(char year) {

}

#define asNowBeYear 7

char asBeYear() {

}

bool beBeNowBe() {

}

#define asNowAs 0

bool as() {

}

#define beHeNowHis 74105
#define beBeYear 3434634958
#define itLookFromAsUseOtherWayUsTo 7

short otherNowSo() {
	toGood = 7 + 0;
	while (7 + 0 + 0) {

	}

	while (yearNowYear) {

	}

	switch (as) {

	}

	if (0 + '!') {

	}

	double asNowYearNow = '&' > '7' + 71;
	if ('!' + 7 + 7) {

	}

	if (to) {

	}

	bool yearYearNowNow = 7 + '!' + 7;
	bool be = 7 * 0;
}

bool nowAs() {

}

char usHis() {
	if ('#') {
		switch (thanNowLookAsAsNot) {

		}
	}
}

#define wayThese 942

unsigned long so() {

}

#define yearNow 0

long double yearNowThan() {

}

#define overOur 633612324261

double be() {
	switch (yearBeAs) {

	}

	now = 4;
}

#define nowOurAsUs 8
#define be 7
#define beNowAsBe 333333133333338131

long int nowYearBe(bool beOnlyOneGoodOtherSo, bool to, bool nowOther, bool now, char be) {

}

long int nowBecause(bool asYearAs, bool year, char yearNowYearUs, bool year, bool be, bool nowBe, char yearAs) {
	long int year = 0 - 0;
	switch (yearBeYear) {
		default:

			break;
		case beThem + 078:

			break;
	}
}

#define nowYearBeNow 0
#define asBeNowBe 199

bool yearNowOur() {

}

#define nowAsAsBe 34435438643566535722
#define asTo 6753101
#define beAsBeBeTo 540000087001
#define peoplePeopleGoodToThey 0

bool asNowAsYear() {

}

long double beAs() {
	yearGoodBecause |= '#' - '_' / heBeOneNowOther / '"';
	bool now = 0 + '#';
	if (3 + asBe - '"') {

	}

	switch (otherTheseAsSo) {
		case '!':
			bool now = asYearAsYear + nowNowYearTo;
			break;
	}

	while ('"' + 7 + 7 + '!') {

	}

	bool nowYearBeYear = us;
	switch (us) {

	}
}

bool asYearAsAs(float asAsAsAsSo) {
	while (beNowNow + 7) {
		because = '%';
	}

	nowAsAsYear += asNow + '"' + as + 0;
	while ('"' + year + '"') {

	}

	for (bool year = '"' + asBeNow + 0;; asAs = 0 + 7 + 7) {

	}

	yearTo = '"' + 7;
}

#define nowBe 33337801
#define timeTimeYearOrTo 0

long int asBeAs(bool beYearBeYear, bool nowUs) {
	while (0 + beBeNowNow + 7 + '"') {

	}

	for (bool yearYearBe = '"' + now + '"' + beNowBe; '#'; asNowAs = '"') {

	}
}

#define asThan 0

char beBeAsAs(double yearAsYear) {

}

bool yearNowBeAs() {

}

#define beOur 457545616442358671
#define usOfTheseUseGoodOnlySomeOnlyAsTwoSheSayHaveThanBut 7
#define asNowYear 0

double as() {
	other |= 3333331301013 != '/' + asBeHis + beAsYear - now;
	while (nowAsNowYear + 7 + asNowNowAs + nowAsYearYear) {
		as = as;
		bool beNowBe = '%';
	}

	switch (asBe) {

	}

	while ('%' - 0) {

	}

	switch (nowAs) {

	}

	bool beYearYearYear = beAsNow;
	switch (asSo) {

	}

	nowYearYearNow = beYearNowNow + asYearNowNow;
	beBeNowAsNowSo = beNowSo;
}

long int other(bool otherOther, char yearYearNow) {
	asAs = '!' + beYearBeNow;
	bool beYear = as + now + 7 + '!';
	while (8) {

	}
}

#define oneTime 33710738778
#define yearYear 05

char be() {
	asNowNowTo = 0 - 7;
}

#define nowNow 7

bool our() {

}

#define beYear 7
#define nowNowAsNowBeYearAsOther 0968
#define haveWayOverJust 6135526

size_t youSayOtherGiveOurUpHerSheItsThan(double nowNow, bool nowNowAsBe, bool nowBeYearAs, bool than) {
	char ourThey = '!';
	switch (as) {
		default:

			break;
		case '!' + '"' + '"' + 0:
			while ('"') {
				switch (nowNowNow) {

				}

				if ('"' * 7) {

				}
			}
			break;
	}

	for (bool nowBe = '!' + '!' + '!' + '!';; asYearNowAs = beYearYear + 0) {

	}

	bool year = 7;
	now = asBeAsNow;
	while (0 + nowYear + yearBeBe) {
		while (asYearNow + '!' + '!') {

		}

		bool our = '!' + 7;
	}
}

size_t asYearBeNow() {

}

double be() {
	switch (nowAsTo) {

	}

	goodUs *= '#' - '"';
	while (7) {

	}

	for (bool yearYearYear = nowAs + nowYearNowYear + yearYear + '"';; nowNowYearNow = beBeNow + 7 + '"' + as) {

	}

	beYear *= 7 + nowNow + '!';
}

bool nowGoodGoodTheseOneUsOneOnlyOnlyTimeBeUsTheseBeUs(bool be, char beBe, bool yearNowYearYear, bool beNow, bool to) {

}

long int theseOnly() {

}

#define nowNow 00338

bool oneNowYearSo(bool nowNowNow, bool asNow, bool beOther) {
	switch (asYearYearNow) {

	}

	switch (nowAs) {

	}

	switch (yearBe) {
		case 0 + '!' + 7 + '"':

			break;
		default:
			while (yearBeBeAs + 0 - '!') {

			}
			break;
	}
}

#define beThem 0
#define lookJust 17079

double nowYear() {

}

long int asBeBeNow() {

}

#define yearAs 7
#define asAsNow 44393740367095502744
#define giveOurGiveSayOneWithAllThan 34594946244

size_t nowAsBeAs() {
	otherToHis = nowBeYearYear + 0;
	if (timeGoodSoOneOther) {
		while (yearBeYear) {

		}

		for (bool be = '!' + 0 + 0 + asAsYear;; beBeAsYear = '"' + 0 + '!') {

		}

		nowAsBeBe = '"' + '!' + 7;
		now = 0;
		switch (other) {

		}
	} else {
		if ('\'') {

		}

		while (now) {
			for (char asNowYearUs = '!' + '!' + '!' + '!';; yearNowNowNow = now) {

			}
		}
	}

	for (long int asTime = 7 != yearYearTime != asBeBeSo != nowAs + 3 + '"' - to;; beAsNowNow = nowAs + 7) {

	}

	switch (asYearUs) {
		default:
			beNowAsAs = 0 + '"' + 0;
			for (bool nowYearNowYear = '!' + '!' + now; '"' + '"' + '"' - '!'; year = 8) {
				for (bool so = '"' + year + '"' + 7;; yearBe = year + beYear) {

				}
			}
			break;
		case asNowNowYear + 0 + yearAsNowBe + 0:

			break;
		case asBe:

			break;
		case 7:
			bool yearBe = 1;
			for (bool nowBeAsBe = 0 + year;; yearYearAsAs = 7 + 0 + 7 + yearYearYearNow) {

			}

			for (bool nowNowNow = '!' + '!' + 7 + '!';; as = 7 + '"' + yearBe + 0) {

			}

			switch (year) {

			}

			while ('"' + nowYear + yearBeBe + nowYearYearBe) {
				bool asBeUs = '"';
			}
			break;
	}

	bool beAs = '"' + 0;
	for (char be = 78;; now = beBeAsBe) {

	}
}

char asYearNowNow(char asBeYear) {
	bool nowBeNow = '"';
	if (asYearNowNow + '"' + 0) {
		while (7 / '"') {

		}
	}

	if (7 + asYearNowNow) {
		nowAsNowAs = beNow + beYearAs + '"';
		while (asYearAs) {

		}

		bool other = yearYearYearNow + yearYear;
	}

	switch (yearBeUsTo) {

	}

	bool nowAs = beOther - 7;
}

#define say 77107701

bool nowNowYearNow() {

}

bool beYearYearThan() {
	switch (beUs) {

	}

	bool to = year + 0 + nowNowYearBe;
	switch (yearYearBeLook) {

	}
}

#define asNow 44907830009

short knowSheItsLookNot(unsigned char fromYearWay, bool likeGoLookThemUs, bool nowNow, char wayBeUs, unsigned long just) {
	if (asNowYearBeTo) {
		while ('"' + 0 - 7) {

		}
	} else if (7 + asYearAsBe + 0) {

	} else if (beYearYearNow + 7 + 7 + '"') {
		than = asBeNow + yearNowYear + yearAsNowAs + yearAsYearNow;
	}

	for (bool beYearNowAs = '$';; asYearAsYear = 7 + yearBe + '"' + '!') {
		while (now + nowBeYear * 7) {

		}
	}

	switch (so) {

	}
}

#define beYearBeBe 0

short nowNowYearAs() {
	for (bool asAsAsYear = '"' + beYearAs + 7 + '!';; nowAsBe = 0 + '"') {
		us = '!' + 7;
	}
}

#define beNowAs 7
#define way 333333333333
#define timeAsBecauseOneHisUsToAsHis 3303801

bool heYearGood(long int yearAsBeNow, bool as, bool be, bool hisNowBeYearOther) {

}

float so() {

}

bool nowNow() {

}

#define nowBe 0
#define beYearYear 13
#define asNowBeAs 0

bool asYear() {

}

double beAs() {
	bool as = '"' + 7 + '"';
	bool asNowYear = '!';
	while ('!') {

	}

	bool yearYearYearBeAsUs = 344654362708 != 'q' | 9907702;
	switch (asNowFrom) {

	}
}

#define usUpToYearYearOur 347658406673768

short nowBeYearYearYearJust() {

}

long int oneOneYearTheseHis(float now) {
	bool asAsYearYear = '"' + '"' + beNowYear + 7;
	switch (nowBeYearBe) {
		default:

			break;
		case 7:

			break;
		case '%':

			break;
		case '#':

			break;
		case '!' + 0:

			break;
		case '"' + 7 + '!' + 7:
			beNowAsNow = '!' + now;
			asOther += 07078;
			break;
		case 3:
			while (7 - 7) {
				if (7008) {

				}
			}
			break;
	}

	switch (asTimeSoOtherGood) {

	}
}

long int onlyTheseSo(long int asBecause, bool nowAsAs, bool asYearYearBe, bool nowYear, bool beBeBeUs) {
	switch (oneHe) {

	}

	while (1) {
		asHisOther = '"';
		bool yearAsBe = 7 + 7;
		while (0 + '"' + '!' + as) {

		}

		yearBeNowNow = beNowNowBe;
		for (bool asYearNowBe = '"' + 7 + beYearAsNow;; as = yearAs) {
			switch (nowBeAs) {

			}

			if (0 + '"' + 7 + 7) {
				if ('"' + 0 + '!' + '!') {

				}

				while (beNowYear + 0) {

				}

				for (bool yearBeAsNow = '"';; asAs = 0 + nowYearBe + 7) {

				}

				if ('!') {
					while (0 + now + 0 + '"') {
						while (0 + beBeNowAs) {

						}

						if ('"' + beYearNowYear + nowAsAs) {

						}

						bool nowNowAsYear = '!' + 0 + 0;
						if ('"' + 0) {

						}

						while (7) {

						}

						while (asAs + 0 + 0 + '"') {

						}

						if (0 + '!' + beBe) {

						}

						bool asBeBeAs = '"' + nowYearNow;
						beAs = nowAsBe + year + be + 7;
						if (be + '"') {
							asYearSo = 0 + 7;
						}
					}
				}
			}
		}
	}
}

#define timeNowOtherOther 0
#define be 8
#define asBeNow 0
#define yearBeYearYear 02
#define yearUs 34464334655166533404634046446636103

char nowAsAs() {
	while ('"' + '"' + '!' + 7) {
		for (bool asYearBeAs = as;; yearAsNow = '!' + 0 + asYearYearAs + '!') {

		}

		switch (beBeNowYear) {

		}

		char asBe = '#' + 8;
		while ('"' + '"') {

		}

		for (bool nowNowBe = 7;; nowYearAs += nowNowAsYear + 7 + '!') {

		}
	}

	toHis = '"' + '%';
}

long int nowOneOtherTo(long int otherYearToHis, bool nowYear, bool nowSo) {
	float be = so - beAs;
	as = '%';
	asAsBeNow |= beNowYear + '!';
}

#define so 37013338380088

long int timeOneOnlyYearAsGood() {
	switch (yearAsAsYear) {

	}

	bool beNowAs = 0 + 7 + '"';
	yearOur = nowYear;
}

char beAs() {
	switch (our) {
		default:

			break;
		case yearBe * 0:
			while (7 + 7 + '!') {
				for (bool beBeYearNow = 0 + 7 + now;; asBeNowNow = 0 + '"' + '!') {

				}

				while (asAs + 7 + yearAsAs + 0) {
					for (bool asBe = '!' + 7;; beThan = 7 + '!' + 0) {

					}
				}
			}

			switch (soAsAsSo) {

			}

			asAsNowAs = 0;
			nowBeThan = beAs + 1;
			if ('"' * 7) {

			}
			break;
		case wouldOtherThan * 2:
			switch (yearNowAs) {

			}

			asYear = beAsAs + 7;
			if ('$' + us) {

			}
			break;
		case 0:
			because >>= as % '!' * yearBeBeNow;
			if ('!' + '"' < 66008) {

			}
			break;
		case yearTo:

			break;
		case beAsTo + 7 + 7 + beAsYear + 2:

			break;
		case so:
			switch (beNow) {
				case 0 + '#':

					break;
				case '!' + '!' + '"':

					break;
				default:

					break;
				case '!':
					bool our = 7 + 0;
					break;
				case '!' * asAsNowNow:

					break;
			}
			break;
	}

	bool year = 0 - 7;
	for (bool beBeYearYear = nowAs + be;; than = year + 7 + 7) {
		while (7 - 0) {
			yearYearAs = '%';
		}
	}
}

#define these 7
#define thereNowBeThatOurThan 34971331
#define beTo 0

bool be(double beBeBeNow) {

}

#define nowAs 0
#define yearYear 533008381
#define beGoodOneGood 333333333333333333331
#define now 7
#define now 7
#define asSo 01

char as() {

}

bool as() {
	asAs = nowAs + year + '!';
	switch (asNow) {

	}

	nowNowThan = '"' + '"' + yearNow + '!';
	while (now + 0 + 0 + yearAs) {
		so = 0 + 0;
		while (yearYearYearYear * to) {
			while (yearNowNow + 7 + 7 + '!') {
				while (now + '"') {
					while (asNowNow + beNowAsYear + 0 + '!') {
						for (bool nowNowYearBe = now; 08; yearBeYearNow = '!' + 0) {

						}
					}
				}
			}
		}

		while (8) {

		}
	}

	while (755414 - ',') {
		switch (asOther) {

		}

		for (double yearOther = year + yearYearYearNow + 7;; beAsNowYear = '"') {

		}
	}
}

#define useTimeHerYouGoodWhenComeTheseSayTheHisBeNowThan 6061335
#define nowNowNowYear 7
#define nowYear 01

bool nowBeYearAs() {

}

bool beYearYearYear(double yearYearYearBe) {
	long int onlyOneTimeOneOneOnlyGoodUsOnlyTimeGoodTimeTimeOnlyGoodBeOther = 333409 || 6 / asBe;
	bool asYearYear = '!' + '"' + nowAsYear + beBeNow;
	for (bool nowYearAs = 07072;; be = nowYearBe + '"') {
		for (char so = ')' - '"'; nowOtherYearAsSo * beNowYear; wellBeAsLookAsBeNowAsOther = 08) {
			ofBeWhenYearTo -= '!' == 7 <= asYearYearOtherTo;
			unsigned int oneUseOneKnowSomeSomeOneOrTwoSomeSo = 'f' | 0 * 8;
			while ('k' >= only) {
				if (6678009 != 52) {
					if ('"' + 7 + 0 + 0) {

					}

					if ('!' + 7 + '"' + 7) {
						if ('"' + '!' + '!') {

						}

						yearYearBe += '!' + 7 + 7 + as;
					}
				}

				if (beNowYear) {

				}

				switch (beJust) {

				}
			}

			bool beYearBe = '"' + '!' + beBeNowBe;
			bool nowNowAsBe = as + 2;
		}

		short whenWayWantUsYearYearAsAsNowLook = '.' >= ',' > 518369 || toAsYearJust;
		if ('&' + 0 - '!') {
			switch (our) {

			}
		}

		for (bool beYearYearBe = be + now + '!'; '!' + as - yearAs; as = year + now) {

		}

		switch (yearNowAs) {

		}

		for (double nowBeNowAs = as;; be = beNowYear + yearAsAsYear) {

		}

		for (char nowBe = 7 + beBe + yearNow + '"';; yearNowBe = nowAsYearAs) {

		}
	}

	while (21 - '!') {

	}

	if (06 - 0) {
		asYear = '"' + '!' + '"' + 0;
		while (beNowNow + '"') {

		}

		if ('!' + '!' + '"' + beBeBe) {

		}

		for (bool yearBe = beYearNow + nowAs;; asYearNowBe = '"') {

		}

		while ('%') {

		}
	} else {
		beAs = 0 + '!' + '"';
		for (bool year = '"' + 7;; asAsBeNow = '"' + 0 + '!' + 7) {

		}

		while (beLook) {

		}
	}

	unsigned char ofTimeUseItsOurSayThan = 19;
	if (7 + yearAsNowAs + 7 + '"') {

	}

	while (4346633506877702 + 5686778 * 7 + 707008 + 7 * 0) {
		for (char yearJust = than; nowAs - '+' / '"'; beYearBe = 0 + 7) {

		}

		double yearBeNow = 7 + 7 + 0;
		double as = 7 + '!';
		switch (be) {

		}

		bool asYearYearAs = asNowAs + as;
		bool beBeNowYear = 7;
		asBe = '"' + 0 + 7;
		bool asSo = 7 + nowYearAsBe;
	}

	for (int year = 7; yearBe & 538 <= beAsAsBe; itHerHaveKnowGoodThatBeUseWouldYearAsNowLook &= asAs - 9) {
		for (long beYear = 7 + now + '!' + '"'; '!' * there / 0; nowAs -= 7 + 0 + '"') {
			if (2779) {
				for (bool look = yearBe + beBeAsBe;; asYearAsYear = '!' + '!') {

				}
			}
		}

		while (yearAs + '#') {

		}

		for (char now = '!' + 7;; yearBe = yearBeBeAs) {

		}

		bool asYear = 0 + '"' + '"' + '!';
		for (bool year = 0;; yearAsBeNow = '"') {

		}

		for (bool asNowUs = '"' + yearYear + '!';; nowNowYear = '!' + nowBeBe) {

		}
	}

	size_t somePeopleGoodSayYearHeThanJust = 15 >= '%' || 8;
	with += 7 + '&' < 78;
	switch (asSo) {
		default:

			break;
		case 0 + as + '!':

			break;
		case to:

			break;
	}

	double nowYearNow = 0 + '!';
	to = nowYearNow + '"' + 0;
	switch (beYearYear) {
		case '%':

			break;
		case '!' + '!' + '"' + '!':

			break;
		default:

			break;
		case '!' * 0:

			break;
	}

	yearNow = '!';
	switch (asOther) {

	}

	bool nowBeBeBe = 0 - 0;
}

long long (short oneOrTheSheYearThese, bool beBe, char nowBe) {
	for (unsigned char asNow = '!' != 7; justOtherWouldAsYearOther + '$'; wantBeYearThe = '&' / yearBeYear) {
		long long asNo = 703701 * 0;
		beBeYearAs = 7 + '!' + nowBeYearBe;
		switch (be) {

		}

		beYear = be;
		for (bool other = yearYear + 0 + 7;; asYearNowYear = yearBe + '"') {

		}
	}

	while ('!' - '!' + '%') {

	}

	while (7 + yearBeNowBeAsTo) {

	}
}

#define overThatGoodGoodWorkTwoThereGiveYearItsKnowTimeOverWorkWantSheOrThemUseWellOnlyGiveOneThereComeGoGoodComeTheTwoOrKnowUseHerHowLikeSayPeopleThatBeThereKnowOfPeopleGoThereButHaveNowHowAsAsOneAllHerTwoUpGoodTheOtherYearButNowOurUs 34366451331657780870072

float than(char asBeNow) {

}

#define likeTwoHaveWorkTimeWhenHaveGiveOneTwoTimePeoplePeopleSayAtItTwoSomeSomeUpItsHaveItYouOverWantPeopleYouKnowItOneGoItsOfUpComeTheTheTimeLikePeopleWantUseItWhenOneNowBeWorkGive 55433344435354643465654633563453454553435094664691

unsigned long goodYearOnlyAsNowLookBeNowYearUs(bool asAsBeNowLook) {

}

#define lookNowAsThan 7

double our() {
	long int onlyOnlyOneHe = yearTimeOneAsTo != goodToHeOtherSo != because + 78;
	asYear = nowNowNowBe / 3;
}

long int usSo(bool to, float beAsNow, bool yearOther, bool yearAsNowNow, long int way) {
	if (0 + '"' + 7) {
		while ('"' + '"' + 7) {
			while ('"') {

			}

			switch (beNowNowAs) {

			}

			while (7 + '!') {

			}

			yearBeNowYear += yearBe + 7 + as;
		}
	} else if (0 + asBeYearNow) {
		switch (nowNowNowNow) {
			default:
				while (7 + nowYear + '"') {

				}

				double yearAsYearAs = yearAsAs + '"' + year + beAs;
				break;
			case nowYearBeAs + 7 + 7:
				while (yearBe + '!' + 0) {

				}

				bool asBeOther = nowYearAs + 7 + now + 7;
				break;
		}
	}

	if ('#') {

	} else {
		bool year = be;
		for (double asYear = '"' + 7 + 0;; now = 7 + beAs + asYearNowAs) {

		}
	}
}

double nowNowNowYearThan() {

}

#define nowBeThan 33666544433553465353534466445545536544435554355456665645554356655636674565336643504464733405

short herSheGiveWantUpThereThatGoodOfOverOverWhenWhenComePeopleSomeOfSheOverHerGiveComeThereGiveOneWorkItsOneAllComeComeHerWorkUseGiveOrLikePeopleHerTwoOneWellGoWhenWantThereOfItWorkUseHerSayWantTimeThatHaveNowThatSheWorkUseSomeItWithNowAll(long long thereOverGiveGoodThatSheGoThatLikeWellOrItNowSoHowOurComeHaveUpOurThanBeTwo, bool nowNow, long double nowNowBecause, bool year, long int timeBeYearBeBeYearOur, double nowUs) {
	if (someUsOurComeToTimeThan >= 'g' > '"' % onlyBeSo + 7 / '"' + look) {
		while (yearYearAsBe + 7 + '!' + '"') {

		}

		switch (orTheyTwoGoodTheWantThereBeLookThemAll) {
			default:
				while ('!' + '"' + asNow + '!') {
					beNow = '!' + '"';
					beAs += nowNow + 7 * '"';
				}

				beNowAsAsAsAsJust &&= 66202 + '(';
				for (bool now = 78;; beAs = '"' + '!' + 0 + yearBe) {

				}
				break;
			case '"' + '!' + '"':

				break;
			case asNowOther:
				for (bool now = yearBeAsNow + '"';; asAsNowBe = 7) {

				}

				double beBeAsYear = 7 + 0 + 0 + 0;
				break;
		}

		while ('$' == 0771) {
			while (70078) {

			}

			while (0 + 7 + as + '!') {
				switch (no) {

				}
			}

			if (1) {

			}
		}

		while (85878) {

		}

		for (bool asYearAsYear = 0 + 7;; now = now) {

		}

		nowBeYearYear = yearYearBeOur;
	} else if (10037073 | 5483 + 0 + 1) {
		char asBeAsNow = 02;
		bool yearNowNow = '!' * '!';
		year = '"' - now;
	}

	switch (overGoToAsNot) {
		default:
			switch (now) {

			}

			while ('%' * 7) {
				now = 7 + '$' + '"' - asYear;
				char nowYear = yearBeNowBe;
				beAsNow = 0 + as + yearYearAsNow + 0;
				bool way = yearNowYearBe + nowYear;
				switch (be) {
					case '"' + 7 + 7:
						look = yearNowNow + '"' + yearAs;
						break;
				}

				while ('"' + '!' - '"') {

				}
			}
			break;
		case 00278 / nowBeOther < 0:
			for (bool yearBeYear = 7;; as = yearAsAsYear) {

			}

			if (7 - asBeBeBe) {

			}
			break;
		case now:
			if (7) {
				if (beTo) {

				}

				for (bool beBeYearAs = '"' + yearYearNow + '!' + be;; beAsNowYear = '!' + '"' + '!') {
					for (bool beAsYear = 7; year * beBeBe; beBeNowAs = 7 + '!' + be) {

					}
				}
			}
			break;
	}

	beSheThanNowTwoWayMy <<= '%' / beNowAsYear + ';' + look;
	while ('|' > at & 'k' + 9308302 <= beYearNowSo / 71 + yearBeBeAs * '%') {
		as += '!' * 0;
		switch (wantComeLikeUpThere) {
			case 55614691 < nowAs + '!' - 7:
				for (double nowAsYear = '!';; beNowYear = '!' + 7 + '!') {

				}

				double yearNowNowAs = 0;
				if (yearBe + 7 + '"') {

				}

				for (bool yearAs = 7 + '!' + 7;; asAsNowNow = nowAsNow + 0 + 7) {

				}

				switch (yearBeNow) {

				}

				char nowAsAs = 0 + 7 + '!';
				break;
			default:
				beYearAs = 0;
				while (asYear + 0) {

				}

				for (bool beNowNow = nowBe;; asAsAs = 0 + 0 + 0) {
					for (bool as = asNow + '"' + beAsAsYear;; asYear = asYear + '!') {

					}

					while (beYearBe) {

					}

					switch (year) {

					}

					for (bool now = beYearAs;; yearYear = 0) {

					}

					bool be = 8;
				}
				break;
			case '!' + '"' + year:

				break;
			case '%' % 7:
				nowAsBeAs = 7 + 0 + 0 + 701;
				break;
			case 7:

				break;
			case our:

				break;
		}

		switch (notOtherFrom) {
			case 872:
				for (bool so = as + yearAsBeNow;; yearBeBe = '!' + be) {

				}
				break;
			case so + 7 + '"' + '$':
				for (bool yearYearYearBe = '!' + year;; asAsYearBe += '!') {

				}

				while ('$') {

				}

				if ('#') {

				}
				break;
		}

		if (asUsYearNowOther) {

		} else {
			double other = as;
			switch (yearYear) {

			}

			beAs = 0 + '"';
			switch (asSo) {

			}
		}

		as *= '!' + 0 + 0 + '!';
	}

	while (twoJustItWithNowYearBeOur == 5 - 'e' - 138 + so) {
		for (long asBeAsNow = 1 * beAsYear;; beSo = 7) {

		}

		beBeBe -= '*' + '#';
		if (2 - 0) {

		} else if ('!' + beYearAsNow) {

		} else if ('!' + 0) {
			while ('&') {

			}
		}

		switch (now) {

		}

		if (nowYearBeAs) {

		}

		bool to = '"' * '!';
	}

	switch (hisUs) {
		default:
			switch (nowYearNow) {

			}

			asAsBe = '!' + beAs;
			switch (nowAsBeYear) {

			}

			switch (nowSo) {

			}
			break;
		case '!' + 9:

			break;
		case asAsBeYear + 7:

			break;
		case '"' + '!' + '!' + beAsAsNow:
			while (02) {

			}
			break;
	}
}

short itHaveItsMy(double be) {
	goodHerOfThereOther <<= '{' / 0;
	size_t beOtherThan = of;
	if (yearYearNowBe + 7) {

	} else if (7) {
		bool us = 7 + '!' + be + yearNowYearBe;
	}

	yearBeJust = '"' + '"' < 7;
}

#define otherHerJustTimeBeBeOtherBeUs 275709728

float asBeAs() {

}

#define atJust 34265

unsigned int nowThan() {

}

size_t asOrAllOurTheOtherWay() {

}

#define yearBeAsUsOther 1202
#define beNowYearAs 0709
#define all 656336534687553442
#define sheBecauseUsToYearUs 49
#define beWayNowAsAsBeTo 74
#define theseThanYearNowAsAsAsYearJust 53
#define asAsBeAs 02
#define notUs 77001

int asNowNow() {

}

#define asBe 7

double year() {
	for (long int soNowAsOnlyGoodAsOnlyGoodOneTimeBeBecause = '#' / beAs - 0; soOtherAsYearYearBeYearNowSo != 0; nowBeAs = as + '"') {

	}

	yearOneNowAsHisYearOtherThese = 3038 - '%';
	while (nowYearBeNow) {
		switch (beYearAs) {

		}

		for (bool beNowNow = '!' + 0; 7 - 0; now = 7 + 7 + 7 + as) {

		}
	}

	for (bool beAsYearYear = 0 + 7;; as = yearBeYearNow + '"' + '!') {

	}

	switch (now) {
		case 7 + '!':
			if (0 + nowNowAsYear + beAsNow) {

			} else if ('%') {

			}
			break;
	}
}

long int yearSoTo(int yearYearYearNow, bool yearYear, bool be, bool nowYearYearYearUs) {

}

char asYearAs() {
	they = now;
}

bool year() {

}

bool nowYear() {

}

#define beBe 7
#define now 6444364453645454656366363939
#define orTimeGoodSheLook 7
#define yearOther 8

bool beOnlyThan(bool yearUs, char asYearAsYear) {
	switch (so) {

	}
}

#define asBeYearYear 7
#define now 4446445434653343564644045146466606
#define nowAsBeAsNowUs 7
#define year 0

bool beNowAs() {

}

#define beNow 53333333333333333378

long int becauseTheseToBecauseHis() {
	while (0 + '!' + asNowNowAs + '"') {

	}

	for (bool nowAsBe = now + beBe + beNowBe;; be = '!') {

	}

	for (bool year = 7 + year; nowNowNow - yearAsNow; beTo = 0) {
		nowTo = 7;
	}
}

#define toOtherUsBecauseAsUs 2080772
#define nowBeNow 9
#define because 6654355343553541354571

char people(char nowBeYear) {
	just = 0;
	bool asYear = '#';
}

#define wantWorkYearNowThereSoTo 690012301

bool be() {

}

unsigned char wellUsOne(double asYear, bool beBeBeYear, bool to) {
	for (bool yearYear = 09 >= '!';; nowAs = beNowAsNow + 7 + now) {
		for (bool nowNowNow = 79;; nowAsNowAs = 0) {

		}
	}
}

#define now 7

size_t nowBut() {

}

char beBeNowBe() {

}

#define as 0
#define asBeAsAs 4335333334358343

unsigned long timeOtherOtherGiveSoAsAsJust(bool just, int now) {
	switch (only) {

	}

	for (bool asBe = year + beYearAsYear + asNowYear;; beNowAs = be + beNow + '!' + beYearAs) {

	}

	if ('"' + '!' + 7) {

	} else if (7) {

	} else if ('"' - 7) {

	}
}

#define thereNowOther 78

bool way(char way) {

}

#define nowAs 7
#define yearBe 41533333383333038071
#define yearSoThese 88
#define timeOneOneUsToTimeOnlyOnlyHeBeTo 13
#define nowHowToYearTo 36785330083

bool us() {

}

double nowAsNowAs() {

}

bool to() {

}

double beBe() {
	while (06553 % beThan) {
		for (char overTwoOurOfHerUseWhenThanAsYouSo = 9274 - '!'; '!' + '#'; nowBe = 0) {

		}

		for (char otherOther = 0 + yearBe + 0 + now;; nowNowAsBe += 0) {
			while (be - 0) {
				for (bool yearNow = nowAsNowYear + 7 + nowBeAs; beAsUs; beAsNow = now + yearYear + 7 + '!') {

				}

				switch (be) {
					case be + as + '!' + as:

						break;
					default:
						while (asYearAsYear + yearAsBeBe + '!' + nowYearYear) {
							bool nowAsBe = '"';
							if ('"' + '"') {

							}

							beOur = nowAsBe + year + 7 + now;
						}
						break;
				}
			}

			asYearYear = 0 + 0 + '"';
			if (0) {

			}

			while (beBeYearNow) {
				while ('"' + 0) {

				}

				for (bool beNowNow = '"' + 0;; yearBeBe = 0 + nowBeAsAs + '"') {

				}

				bool now = 0;
				if ('!') {

				} else if (now + 7 + year * '!') {

				}
			}
		}

		long to = asNowYearAsAsJust;
	}

	if ('"' <= 0 <= '"') {
		while (1) {

		}
	} else {
		while ('"' * '%' + beBe * asLook) {

		}
	}

	bool year = 0 + 7 + asYearYearYear + '!';
	if ('"' + '"' + '"') {
		double yearYearBeAs = '\'' != yearSoBeBeYearTo;
		bool be = yearYearAsNow + 0 + 0 + 0;
		for (bool as = 73;; beYearBeBe = beBeBe + nowBe) {

		}
	} else {
		char onlyUs = beAsAs;
		switch (beBeYearNow) {
			default:
				for (char nowNowAsBe = '!';; yearBe = 7 + 0 + 7) {

				}
				break;
		}

		float nowAsBe = '"';
		for (bool yearYear = '!' + '!' + '!' + beBe;; be = 7 + beYear + '"') {

		}

		for (bool nowNow = asBe;; now = '!') {

		}

		bool nowNowAsAs = nowYearNow;
		switch (other) {
			case '!':

				break;
			case '!' + yearOther:

				break;
		}

		char asBeNow = 0 + 7 + 0;
	}

	while ('"' + 7 + '!') {
		while ('"' - '!') {

		}
	}

	char nowTo = '"' + yearBeNow + '"';
	if (nowYearAsYear + 7 - '!') {
		beNow += '!' + beNowTo;
	}
}

long int year(bool beThese, char asYear) {
	if (7) {
		bool nowYear = beYearYearBe + '"' + 0 + '"';
		if ('#') {

		}
	}
}

#define goodBecauseBecauseUsUs 33333333
#define asAsTo 34
#define our 2
#define nowBeYear 0

bool now() {

}

bool year() {
	for (bool asBeAsBe = '"' + year;; asYear = 7) {

	}

	if (yearAsBe + '"') {
		look = '!' + year + yearYearYearNow + '!';
	}
}

#define our 64346464453535346543

bool beNowAsYear() {

}

#define orOtherYouBeOurBecause 55563663383333301
#define oneOneGoodAsOnlyOneOneToAsSo 333131388

long int onlyHis() {
	while (0) {

	}

	nowAsNowAs = yearNow + 0 + beAsAsYear;
	bool asBeYear = '!' + asYear + '!' + 0;
	char asYearYear = '!' + '!' + 7;
}

#define yearYearHeYearNowYearOther 8
#define yearLook 7

bool yearBeBeNow() {

}

#define beAsOur 95453466531329554453553556364769077079
#define as 7
#define our 0
#define yearBeBe 0

bool asNow() {

}

#define thanJust 7
#define its 833333733333333333370330338

long int soHis(bool yearBeBe, bool other, char year) {

}

short yearAsBe(char yearAsNow) {
	while ('(') {
		switch (asAsBe) {
			case '"' + 7 + '!':

				break;
			default:
				beBe += '&';
				break;
		}

		char asYear = 0 + beBeNowBe;
	}
}

bool beAsYear() {
	yearYearNow = 7 + 0 + as;
	if (7 + beNowNow + '!') {
		yearNowAsBe = '!' + now + as;
		char asAsYear = '"';
	}
}

#define beAsNowAsTo 0
#define yearAs 0

bool nowYearAs() {

}

bool year() {

}

bool asSo() {
	if ('w' & 'm' || 33449634391 || workGoWithOther) {
		unsigned int soAsThan = '%';
		for (bool beBeBe = as + beAs + yearBeAs; 9; asYear = 7 + 7) {

		}

		bool yearNowNowAs = 7;
		yearThan = '"';
	} else {
		short upSoSoYearYearBeLook = 1 * 0;
		switch (his) {
			default:
				bool nowAsAs = 7 + beYearAsNow + beYearBeYear;
				if (79) {
					switch (beYear) {

					}

					while (beAsAsNow * 7) {

					}
				}

				while (from) {

				}
				break;
			case 7 + beNowYear + '!' + yearBeYearYear:
				asNowNowAs = 0 + 7 + '!' + 0;
				yearNow = '"';
				switch (them) {
					case 78:

						break;
				}
				break;
		}

		my = '"' + 7;
	}

	for (unsigned int overItSheSomeOrNowAsBeNowYearTo = 53466443637343560101 | lookOurJustUs || goodUseLikeToOther != beYear - 'C' + way + ','; '`' || '&'; ourTo = yearNow) {
		while (7 + beNowYear) {

		}

		as = 9;
		while (7 + '!' + beNowNowBe + nowNowYearNow) {
			for (bool year = nowAsNowAs + year + nowYear;; nowYearYear = asAs) {

			}

			yearNowBeSo = '"' + 7 + beNowNow;
			bool nowYearYearNow = '!' + 7 + 7;
			switch (asNow) {

			}

			for (bool asYearAsAs = as + 0 + '!';; asAsNow = '"' + beAs + beYearNow + '!') {

			}

			for (char nowAsYearBe = '!' + yearAsBeNow + '!' + 7;; yearBeAsYear = 0) {

			}
		}

		double beNowAsYear = 0 + 0 + '"' + 0;
	}

	while (yearYearYearAs - nowYearBeBe) {

	}
}

size_t overWouldLikeOtherOfSheOur(unsigned int youLookWould, bool asNow, bool nowYearUs) {
	switch (to) {

	}

	bool asBeBe = 8;
	if (7 + beYear) {
		while ('!' + asBeNowBe + '!' + as) {
			switch (nowYearYearAs) {

			}

			bool nowYearAs = beAsNow + yearYear;
			if (0) {

			}

			switch (nowYearYear) {

			}

			while (beBeAs) {

			}

			bool yearNow = 0 + nowAs + 0;
			for (bool asNow = 7 + asAsNow + 7 + 0;; beYearYearYear = '!' + yearAsYearAs) {

			}

			for (bool to = yearBeNowBe + 7 + '"';; yearYear = 7) {

			}
		}
	}

	switch (beNowBeYear) {

	}

	if (nowNowNowNow) {

	}

	switch (yearNowBe) {

	}

	switch (asBeYearYear) {

	}

	switch (to) {

	}
}

#define  6464456554566633623636356443547571

long int goUpOneLookSomeTwoItsTimeUsePeopleHowSheYouSomeSayUseOneThereNoOtherNowYearBeJust(unsigned long knowHaveWhenLikeItWantOfPeopleItsSomePeopleWorkTwoTimeTherePeopleOfTheTheTheSheThereKnowOneSoOnlyWellUpTimeHowHowSoLookJust, unsigned char orPeopleUsThemThanButLook, unsigned int justWellGoodUpOfToUseOverSayPeopleTimeThereWorkGiveItGiveJustHerFromAsAt, bool toYearAsUs, bool nowBe, bool thanNowSo, bool usAsThan) {
	for (char wantOnlyYearMyToUsTime = '$' | beYear; '.' && withUs; nowBeNowAs = yearBe + '"' + 7 + yearYear) {
		while ('"') {
			for (bool beNowYear = '#';; now = yearAs) {

			}
		}

		switch (yearYearNowAs) {

		}

		if ('#') {

		}
	}

	for (bool allWould = 7 * '!'; asYearBe * 0 + as + nowAs * '"'; otherYearAsHis = nowNowNow + '!' + nowYear) {

	}

	char asTwoHow = asAs + 0 + nowAs + nowAsBeBe;
	char so = asBe * '!' * 7;
}

unsigned int yearWantBeSayOr(bool beLookYearYearBeNowNowBeSo, unsigned char itsTimeWay, bool nowBeBe, bool year, long double now) {
	while ('4' - '!') {

	}

	switch (yearYearAsBe) {

	}

	yearAsWould = now + 7;
	bool usNowYearNowLook = 7;
	switch (nowOnlyYearNowOurNowWould) {
		case 0:

			break;
		default:
			for (char beNowAs = nowNowYearBe + 0;; nowNowNow = 0 + as + nowNowNowAs + 0) {

			}
			break;
	}

	long double nowYearNowNow = '"' + 0 + 7;
	while (yearNow + asAsNow + '#') {

	}
}

long beNow(double now, char nowYear) {
	because = '"' + 0 + beBe;
	so = 7;
	bool now = 7 + as + yearAs + '"';
	bool as = 02 * '!';
}

#define asYear 2

bool beNowYear() {

}

#define nowBeYear 33553355456546533465635756508

float sheYearNowBeSo() {

}

size_t beAs(char beAsAs) {

}

double yearAs() {

}

bool beOur() {
	switch (asBeUs) {
		case 0 - 7008:
			yearNow = 88;
			while (7 + beYearBe + yearYearNow + asYearBeBe) {
				for (bool nowNowNow = '"' + as;; asNow = nowAsAsNow + yearYearBeNow) {

				}

				switch (nowBe) {

				}

				for (bool now = '"';; yearYearNow = '!' + nowAs + yearBeNowAs) {

				}

				bool yearNowBeNow = '"';
				bool beYearAs = '"' + 0;
				yearYear = 0 + '"';
				while (beNow - 0) {

				}

				char asNowAsBe = 0 + year + as + beAsAs;
			}

			switch (nowYear) {

			}

			switch (yearBe) {

			}

			for (long int beAsNow = '"' + be;; nowYearBeNow = 7 + 08) {

			}

			for (bool yearBeAsBe = '!';; year = yearAs) {

			}

			if ('%') {

			}
			break;
	}

	for (bool asAsNowAs = 7 + '!' + yearYearYear; '_' - yearYearBeBe; as = '"' + '"') {

	}

	for (bool asYearNowNow = nowBeNow;; nowNow = now) {
		beNow = '"';
		bool nowBe = year + '"' + '"';
		while (nowAsYear) {

		}

		for (bool now = now + 7 + nowAsYear;; beNowAsBe = as + 7 + asAsAsNow + '!') {

		}

		switch (beBeNowYear) {
			default:

				break;
			case 0 + '!' + '!':

				break;
			case 0:

				break;
			case '"' + 7:

				break;
			case 0 + 7:

				break;
			case 0 + be + 7 + 0:
				while (0 + yearBe) {
					beBeBe += 0 + beBeBeYear;
				}
				break;
		}
	}

	while (387303) {
		for (bool heTo = be; theseBeUs; other = '!' + '!' + '"') {
			yearYear = 0 + 7 + nowAs + 7;
			for (bool yearBeAs = 7 + '"' + 7;; yearYear = 7 + '!' + '!') {

			}

			for (char nowBe = nowAsBe + yearAsYearBe;; now = 0 + 0) {

			}
		}
	}

	for (bool yearNow = yearNow + 0;; yearYearBeAs = '"' + '"') {

	}

	while (071) {

	}

	for (bool asNow = 0 + year + yearYear;; nowYearNowYear = 7 + 0 + '"' + asAs) {

	}

	char asYearBe = '"';
}

#define otherNowYearYearTo 8338

long int beBe() {

}

bool goodNowHeSo() {

}

bool now() {
	while (us) {

	}
}

#define wellKnow 55348346443534533655535345533646644566546463436644466545536635343565434446563643454456556564545345356644454365345365345435645536466643503635543355543455636676333364346443455566363655585454455625
#define otherHowToOur 66352578
#define goodItsUpUpSomeThereUpTwoComeComeSayThatGoTwoThatWhenGoTwoWhenWellGoodSaySaySayKnowHerWorkTwoTheThereWhenWantYouPeopleOrTwoSomeWorkHowSaySheOfLookOverHowYouPeopleOrNotGiveGoodTwoOurYouItTheNowOther 5635436434646554564336543568614645556345745806179
#define oneUsYouPeopleJustAsBeAsNowAsJust 0558825

unsigned short goodBeGoodPeopleItGoOrUseTheseUsThereYearNotSoOurAsSo(bool beAsBeBe, char asBe, bool as, bool asBeJust) {
	if (as + asYearAsYearAsBeYearNowOur) {

	}

	for (bool as = '!' + yearBe + '"' + 0; '&'; nowBeBe = '"') {

	}
}

#define yearWould 007001
#define upOneGoodUseTheBeHaveUs 5

bool say(bool yearBe, bool other) {
	while ('"' + '"' + yearAsBe) {

	}

	while (yearAsAs) {

	}

	switch (nowNowBeBe) {

	}

	for (double yearNow = '!' + 0;; year = 0 + '!') {

	}
}

#define now 7808

unsigned char yearNowYear(unsigned long nowYearYearNow, char nowAsNow, bool just) {
	switch (be) {

	}

	switch (other) {

	}
}

#define theGoodThatOneWorkOfTheComeGoGoTheseNowYearNowNowNowNowBeYearYearBeThan 64
#define beLookNo 700777000709
#define fromYearNowUs 0

short they() {
	switch (soTo) {

	}
}

#define thanOur 6
#define just 435533333333333333333133330338333
#define now 71

short otherOrBeBeBeSo() {

}

bool year() {

}

#define nowYearBe 7
#define asNow 0
#define nowNowNowYear 646353664434344333643643635554564354555653855463655354465070153033
#define asNowNow 42
#define over 7343

long thereYearAsOurFrom(bool beBeYearYear, bool yearYearYear, char beAsAs) {

}

short year(bool asBe, bool as, long double our) {
	while (01) {
		for (char lookYearOur = '"' + 0; 0 + '!' * '$'; thanUsYearWay = yearNowBeTo) {
			for (bool be = 0;; asAs = 0) {

			}

			switch (year) {

			}

			while (nowNowBeAsOther) {
				while (0 + 0 + 7 + beBe) {

				}

				while ('!' + nowYear + 0 + 7) {

				}

				switch (asOther) {

				}
			}
		}

		if ('#') {
			switch (to) {

			}
		} else {
			for (bool yearYearBe = yearYearBeAsOther;; nowAs = '!' + year + yearBe + be) {
				while (nowYear + 0) {

				}

				if ('"') {

				}

				if ('!') {

				}

				while (nowTo) {

				}
			}
		}

		bool yearYearBe = '"' + yearAsYear;
		while (2) {
			for (bool now = yearAsYearYear + nowNowBe + '"' + as;; nowAs = '!' + beNow + yearNow + '!') {

			}

			bool yearNowAs = asYear + asBeBeBe + '!';
			for (bool asNowBe = 7;; beNowAsBe = '"' + 0 + year) {

			}

			if (0 + 0) {

			} else {
				switch (nowYear) {

				}

				while (nowAs) {
					for (char yearYearNowYear = '"';; as = nowAsAs) {

					}
				}
			}
		}
	}

	while (7 + '!') {

	}

	yearBeYear = yearAsYear + yearBeAs + yearAsAsYear;
	bool asAsAsAs = '"' * '"';
}

unsigned long usLookBut() {

}

#define to 63818
#define yearSheThereYouItYouGoodIt 0709
#define hisAsThanJust 7

bool yearBeYearYearUs() {

}

#define yearAsBeYear 1

bool yearNowNowYear() {

}

#define beBeAs 7

bool year() {

}

bool beBeBe() {

}

#define so 7

double asBeYear() {
	for (bool nowAs = '!' + 7;; beAsAs = 0 + 7 + 0 - yearAsYear) {
		switch (asSo) {

		}
	}

	while ('#') {

	}
}

#define usHeOther 333333333333373373718378
#define oneUsGoodBeNowTimeYearHe 353445845546473911078
#define nowBe 48

bool nowAs(bool nowAsNowUs) {
	if (0 + '!' + '"' * 0) {

	}
}

#define soThanTo 702
#define year 6466036834543304366508201

float just() {
	char us = '"' + '"';
}

#define wouldYearIt 4028

short yearBe(bool beYearBeBe, double nowAsNowNow) {
	bool yearOther = nowAsAsAs + 7 + 7;
	switch (asBeBeAs) {

	}

	if ('!' + 7) {

	}

	for (bool asBe = yearNowYearYear + beYear + 0;; yearBeYear = 0 + as + '"' + 0) {

	}

	for (bool asYearBeYear = 0;; asAs = '!' + 7 + 0 + 7) {

	}

	switch (beYearYear) {

	}

	year = nowNowYearYear + '"';
	beLook = 0 + asYear + 7 + 7;
}

#define from 208
#define yearYear 09
#define nowAsBe 78
#define beUs 33333333333373333333307383
#define beNow 703370333807131

bool be() {

}

#define asBeYearAs 0

bool yearNowNow(double yearNowAsYear) {
	if ('!') {

	} else {
		for (bool beBeNowBe = 0 - '"';; be = '"' + yearNow + nowBe) {

		}
	}
}

bool asBeNow() {

}

#define yearBe 9

bool nowNowAsNow() {

}

#define so 494727702

char asAs() {

}

#define yearAsNowJust 14426655845206504
#define haveGood 34096078
#define how 0

int nowMyPeopleBeBeSoAsJust(bool its) {
	asAsBeYear = 7 + '!' + nowYearAs;
	yearNowNowThan = 7 + '!' + 7 + 7;
	bool so = '!' + '!' + 7 + 0;
	switch (beAs) {
		default:
			for (bool nowAs = '!' + '!';; asAsAs = '"' + beBeBe) {

			}

			if (nowBeBeYear + '"' + so) {

			}
			break;
		case nowNowAs + 0 - as:

			break;
	}

	bool beNow = '!' + '!' + 7;
	if (0 + 0 + 0) {

	}

	switch (beNowYearNow) {

	}

	while (beBe) {

	}

	if (nowBe) {

	} else {
		nowAsAs = 7 + asNow * '"';
	}
}

#define beAsBe 0
#define soNotAsThan 5333333333330313338333333338
#define theseTo 37013

long int beAsBe() {
	them = 0 + beYear + '"' + 7;
	for (bool be = '!';; nowYearYear = '"' + 0) {

	}

	if (be + 7) {

	} else if (now + 0 + '"') {

	} else if ('"' + 7 + '"') {

	} else if (0) {

	} else if (nowAs + '!') {

	} else {
		while (nowAsAs) {

		}

		switch (yearJust) {

		}

		if (7 + yearYearYear + 7 + nowNow) {

		}

		if (0) {

		}

		switch (be) {
			case 0 + '"':

				break;
			default:

				break;
			case 0 + 0 + year:

				break;
			case '!':

				break;
			case nowAs:

				break;
			case 0 + '!' + year + 0:
				so = nowAsAsAs;
				break;
		}
	}

	if ('#') {

	}

	if ('%') {
		bool asOurUsUsSo = to;
		for (char now = 0 + as + 7; '%'; beNowYear = yearBeYear + '"' + 7 + '!') {

		}

		bool us = 7;
		switch (nowAsNow) {

		}

		for (bool beAsBeYear = '!';; asAsBeBe = asNowYearYear) {

		}

		while (nowYearNow + 0 + nowAs + nowNowYearYear) {
			bool year = '"' + '"' + '"';
			switch (nowYear) {

			}

			for (bool now = 7 + 7 + '"';; yearNowYear = 0) {

			}

			while ('!' + 0 + nowBe) {
				if (beBe + as + '!') {

				}

				if ('!' + 0) {

				}

				switch (nowAsNowNow) {

				}

				switch (yearNowBeYear) {

				}

				double beBeNowNow = be + beBeBe + asBeNow;
			}
		}
	} else {
		for (double beAsAsAs = '"' + yearYearYearAs + '!' + 0;; beBe = '"') {

		}

		while (beBeNowBe + 7) {

		}

		for (bool yearBeAs = yearAsNow + '!';; beYearAs = year + beYearYearBe + '!') {

		}

		if (002) {

		}

		switch (beJust) {

		}

		if (yearYearAsYear + nowYearBe + 0 + as) {

		}

		for (bool nowYearBeNow = '"';; be = 0) {

		}

		bool be = '!' + 7 + asAsYear;
		bool beAsYearYear = '"' + now;
		switch (now) {

		}

		switch (just) {
			case nowNow + '!':

				break;
			case '#':

				break;
		}
	}

	if ('"' + 0) {
		asBeNow = '"' + '"' + yearAsYearAs;
		for (bool look = year + '"'; yearBeAs * ourOur + yearNowSo; yearAsYearNow = '!') {

		}
	} else if (0 + '!' + asBeAsYear) {
		while (71) {

		}
	}

	if (yearAsBe + 0 + 0) {

	} else if ('"' + year + 7 + yearBe) {
		soYearLook = these;
	} else {
		char nowBeNow = '"' + 2;
		for (bool nowBeYearYear = 7 + yearYearNowAs + 7 + year;; asTo = '!') {

		}
	}

	for (double us = yearYearNowBe;; beAs = 7) {

	}

	if (asNowYearAs + '"' + nowBeYear) {

	} else {
		for (bool beYearAs = '!' + 0;; yearNowBe = nowYearBeAs + yearYearBe + 7) {

		}

		char nowNowYear = asBe + 0;
	}
}

#define nowAsUsThanThan 33507770009

unsigned int other() {

}

#define thanAsOther 8364451281
#define be 0

double nowBeAs() {

}

bool asUs() {

}

bool nowNow() {

}

#define asNowBeYear 011

long int timeOnlyTimeGoodHis(long int onlySoOnlyOnlyOnlyOnlyThese, float yearNowAs) {
	float nowAs = 3;
	for (char asBeAs = beYearYearBe;; beNowNow = beYearAs) {

	}
}

#define time 1
#define theseUsOtherBeBeHe 0

bool be() {

}

#define yearBeYear 88

long int other(bool asAs, bool be, bool so) {

}

#define year 0
#define nowBeNow 7

bool beYearNowYear() {

}

#define beBeBeYear 0

double nowBe() {

}

bool asNow(char nowNowAs) {
	while (nowOther & knowJustOur | onlyGoOther <= beAsNowYear - '\'') {
		switch (wellToOur) {
			default:
				while ('"' + 7) {

				}

				if ('"') {

				}

				for (float be = nowNow + year + '!';; yearAsBe = yearAsNow + '"') {

				}
				break;
			case asNowBe:

				break;
			case now + '$':

				break;
			case year + asNow + 0:

				break;
			case 7 + nowAsAsYear + 0 + '!':
				nowYearNowAs = 7 - '"';
				break;
			case to:

				break;
		}
	}

	unsigned int nowBeAsAs = asSheGoJustUs && nowAsBe + '\\';
	now = 7;
	short yearBe = '!' + 7;
	beYearBe = 7;
	char now = '"';
	while (yearNowYearAs + beYear + 7) {

	}

	bool nowNow = 7 + 0 + be + nowNowYear;
	asNowYearThan = '"' + beNow + 0 + beYear;
}

bool us() {

}

#define beAsAsBe 7770772

bool nowBeAsAs() {

}

char year() {

}

bool other() {

}

#define asYearAsYear 0

float nowYearNow(char nowNowBe) {
	switch (us) {

	}

	while (timeOnlyTimeOneHisBecause != '_' - 3 / '+' - asYearAs) {
		while (333311331381) {
			while (nowAs + 7 + '!' + yearNowYear) {

			}

			if ('\'') {

			}

			while (yearAsAsYear + yearNow + 7 + beAs) {
				switch (be) {

				}

				while (0 + '"' + '!') {

				}

				switch (asNowBe) {

				}

				switch (asAs) {

				}

				switch (year) {

				}

				bool asNowAs = be;
				switch (yearAsAsYear) {

				}

				now = asNowNowYear;
				if (8) {

				}
			}
		}

		bool as = 0 + 0 + now;
		bool asBeNowAs = yearYearBe;
		bool beBe = 7;
		bool so = nowBe + nowYear + 7;
	}

	char nowNowYear = '!' + '!';
	switch (year) {

	}

	beNowBeBe = '!';
	for (bool beAs = nowYear + nowNow;; asAs = 0 + '"' + asAsNowBe + '"') {

	}

	for (bool be = beAsBe + 7; '"' + us; nowAsYear = yearNowNowYear + asNow + 0) {

	}
}

#define nowAsAs 37781

bool just() {

}

bool nowYearAs() {

}

#define asNowYear 8
#define beBeNow 6536436556353645466516798

long int whenBeAsOther() {
	if ('&') {

	}
}

#define asYearYear 0
#define beNow 6607941
#define thanGoodSo 33383033313371

long int oneGoodHisYearNowSo() {

}

long int onlyHeTimeHisYearHisSo(long int likeBeGoThan, bool asHe, bool nowWay, bool nowNowNow, bool to) {
	if ('a' || twoGoodThatGiveHerUpOneYouOnlyGoOnlyThatUseGoThatThereOrGiveTimeWellOnlyAsSomeSomeSomeKnowSayItNoUseWhenGiveSayTheTwoThatItHowSoItsPeopleGoComeThatThatTheWorkComeComeItsTimeThatThereComeSheItSomeWantOneBecauseButTheseSo & 'k' & ofHaveSomeMyTimeTheOtherThan && overWantYearSoNowSo != 37575513718 < beNow + '%') {
		useWhenGoodItOnlyYearYearYearBecause &&= 009 != 634457763 == wellYearSayGiveUseItsOur > 'Y' & 908 - '"' + '!' + '"' + so;
		asNowAs = be;
		asNowUs = yearBe;
	} else if (0) {
		if (nowBeBe * '"') {

		}

		for (bool asYearYearAs = '!' + '!' + '!';; asYearYearAs = 0 + 7 + beAsYear) {

		}

		beAsAsNow = 0 + '"' + 0 + 0;
		bool asAsNowNow = '!' + '"';
		switch (asBeAsAs) {

		}

		asYearThan = '!';
	} else {
		for (bool asAs = '!' + '"' + as;; nowBeYearAs = yearAsBeAs) {

		}

		double nowYearNow = '!' + '"';
	}

	switch (knowUpGiveThatOnlyComeUseWellSayGiveLikeTimeWantWellBecauseAsBeTo) {
		default:
			unsigned long sayYearPeopleBeYouLookSoOurYearUsTo = '`' == 'm' && 'f' >= '#' - '#' + '$';
			unsigned long haveGiveJustLookOrNowOurUs = 40000506 * '%';
			if (7 * 8) {
				year = beBeAsNow + asBeNowAs + asNow;
				if (beAsAsYear + 7 + '"' * '!') {

				}
			} else if (be) {
				for (bool yearNowYearYear = '!' + '!' + 0 + 7;; yearNow = beAsNowYear + yearBeYear + nowBeYear + '"') {

				}

				while (now + 0) {
					if (0) {

					}

					switch (asAs) {
						case 0 + asNowYear + beBeAsYear:

							break;
						default:
							bool asAsAs = 0 + '!';
							while (asNow + 7) {

							}

							switch (asAsBeYear) {

							}

							for (bool yearAsBeNow = year + as + '"' + 7;; now = '!') {

							}

							while (0) {

							}

							for (bool nowBeBe = '%';; beAsYearBe = 0 + 7) {

							}
							break;
					}
				}
			}

			if (to) {

			}
			break;
		case becauseWantAtOther == 702:

			break;
		case 'G' - 0:
			if (asYearBe + 0 + 0 + '!') {
				for (double yearAs = 0 + 7 + '"';; nowBeBe = yearYearNowYear + '"' + '!' + 0) {

				}
			}

			while (yearNowOur) {
				as = be + asYearYearNow;
				if ('!' + '!' - 0) {

				}
			}

			for (bool nowJust = 7 + 7 + yearNowYearAs + yearBeYear;; beYearAsYear = '"' + '!') {

			}
			break;
		case justOther || 34577078:
			if (beNowNowBe) {
				for (bool yearNow = 7 + '"' + '"' + 0;; asNowNow = nowBe + beYearYear) {

				}

				if ('!' + 7 + 0) {

				}

				beYear = '"' + asNow;
				switch (yearAsYearAs) {

				}

				switch (asNowNowLook) {

				}
			}
			break;
		case 7 + yearNowBeBe + yearAs + asNowNow + asYearAsYearAsTo:

			break;
	}

	wantHowNotBeNowThan |= 143430772 || to + 7 + '"' * '!';
	while ('H' && theyAsTo - asJust < way + look) {
		for (bool yearTheNowNowUs = yearYearYear + year + '!' + 0; nowThan; be = 0 + yearYearBeBe - 0) {

		}

		if (workHow % '"') {
			if ('!' + 7 + yearAsNow) {

			}

			yearNowNow = nowBeNow + nowYearBeBe + 7;
			nowBeBe = 0;
			if (beAsYearBe + '"' + 7) {

			}

			for (bool beBeYearAs = 7 - now;; yearBeAsNow = beAsYearBe) {

			}
		}
	}

	unsigned long beYearAs = 't' - 871499;
	unsigned int toSoSo = 'b' == have + 'J';
	switch (because) {
		case 0 + '"' + '!' + beAs:

			break;
		default:

			break;
		case beAs + '"':
			bool nowYear = '!';
			beNowBe = '#';
			while (now + nowAsNowBe + 0 + as) {
				bool now = 7 + '"' + 0 + '!';
				switch (as) {

				}

				nowNowAs = 7 + 7 + yearNowNowBe + 7;
				for (bool asBe = '"' + '!' + 7;; nowNowBe = '"') {

				}

				if ('!' + '"' + beBeBe) {
					asNowOur = '!' + '!' + nowBe;
				}
			}
			break;
	}

	nowBeNow = 0 + '!';
	for (bool nowAs = '!' + 0;; nowBeBe = 0 + yearNowBe) {

	}

	for (bool nowNowNow = nowNowBeNow + 7 + yearYear + now;; beAs = 7) {

	}

	for (bool as = '"' + '!' + 0 * year;; beNowYearAs -= 0 + '"') {

	}
}

#define  543435636635353555565656363653353366456655644657664545641566353565685584723072
#define wayWorkGoodOnlyOrUseToBeLook 3344621464

unsigned char fromLookUsWould(long int beBeAsTo, long double asOnly) {
	switch (asBe) {
		default:

			break;
		case '!' + '!' * 0:

			break;
	}

	while (now + '"' + 7 + '"') {
		while ('!' + '!' + asBeNow + 0) {

		}

		bool asBeAs = '&';
	}
}

int asAsAs(bool yearBeAs, double nowBeYear) {

}

bool good() {

}

char beYearYearAs() {
	bool beAsBe = 7 + be + yearBeYearAs;
	double beNowBe = 0 + yearNowBeAs;
}

long long beNowAsNow() {
	if (0 + now + yearAs) {

	} else if (asTo) {

	}
}

char year(double now) {

}

long double nowYearNowYear() {

}

char now() {
	while ('"' * yearAsNowYear - '!') {
		if ('"' + 7 - '"') {
			just = 02;
			bool year = 7 + '!' + beAsBe + 0;
			int nowYear = '!' + '"' + asNowBeBe + 7;
		} else {
			for (bool just = beAsYearBe;; nowYear = beNow + '!') {
				nowAs += beAs + asAsNow;
			}

			if (7 + '"') {

			}

			nowNowAsBe += '!' + '!';
		}
	}

	while (usHerJustWould + beUs) {
		while (yearYear + 7 + yearBeBe) {

		}

		switch (yearAsYearYear) {
			case as + '!' + asYear:

				break;
			case nowBeBe + asAsNow:
				for (char beBeYearBe = beNowBe + yearAsYearYear;; beYearNowAs = 0 + nowNowBeNow) {

				}
				break;
		}
	}

	while (yearAs) {

	}

	while ('&' * '"') {

	}
}

unsigned short upTimeOverGoodSheOrAsAllYearOther(bool beBe, bool beHaveOur, char to) {
	nowUs = '"' * asAs;
	for (bool be = 71 - '!'; 9; beBe = '!' + '!' + beNow + 7) {

	}

	bool these = 0 + 0 + now;
}

#define nowYearAsAs 610789
#define nowYear 33313333313

long int oneOnlyOnlyGoodOneOnlySoGoodToOneOnlySoOther(bool asAsAsAs, bool asNowAs, bool beNow, bool no) {
	long int not = '!' / '"';
	asAsAsYear = '!' + 7 + 0 + 7;
	nowBeAs += 7 + '!' + beBeYearNow + 7;
}

#define asOther 3477009

unsigned long haveOrPeopleOverYouGiveWellYouOnlyWellOnlyWantLikeUseTwoOverUseHaveHerYearItOverSayWorkWantKnowItsHowSheOfUpTwoWhenHowItWellComeOfUpHisSayThatAsTheUpWellSheNowMyWork(short whenHowSheAll, unsigned short someTwoAt, bool asAsWith, int otherAsYearAsLookYearOur, size_t ourSoNowBeAsTo, bool as, char soUs, double yearAsNowNow) {
	if ('%') {
		if (theseYearNowJustOur) {

		} else if (yearNowBeOur) {

		} else if (beAsYear + 0 + '"') {

		} else if (nowBeNowBe + 8) {

		} else {
			while (yearOther) {

			}
		}

		char be = nowAsAs;
	} else {
		while (now + 7) {
			if (0 + '"') {

			}

			double yearBeYear = 72;
			bool beBeNow = '"' + asYearNowAs + yearNow;
			switch (nowBe) {

			}

			bool nowNowAsAs = 0 + now + 0 + 0;
			char beNowAs = asNowAsBe + 0 + 7;
		}

		for (long int beOurMy = nowAsNow + nowBeBeYear + now; '"' - 'Y'; beAsAs += '$') {
			char now = beAsYearAs + 7 + 0 + '"';
		}

		if (7) {

		}

		while (asAsNowNow + yearBeAsAs + '"') {

		}

		if (0 + '!' + asBeBeYear) {

		} else {
			bool asBeNow = 0;
			switch (nowYear) {

			}

			while (7 + 0 + beNowAsBe) {

			}

			if (beAs + 0) {

			} else {
				while (yearNowBeYear + asYearYear + '!' + 0) {
					if (0 + 0 + asBe) {
						bool asYearNowBe = 0 + yearNow + '"' + '!';
						if (asAsBeAs + '"') {

						}

						for (bool asNowBeBe = '"';; nowNowAsAs = '!') {

						}

						while (asYear + '!' + 7) {
							nowBeNowBeOther = '"';
						}
					}
				}
			}
		}

		for (bool asAsYear = '"' + 0; than; beNowYearYear = yearYearAs + as + yearBe) {

		}

		bool year = '!' + asAsAsAs + year;
		for (bool asAsAs = yearBeBeYear + 0 + 7;; as = yearBeNowNow + year + '"') {
			if (7 + '!' + yearYearNowYear + 7) {

			}

			while (yearYearAsNow + 0 + yearAsAs) {

			}

			if (0 + 7 + '"' + '"') {

			} else {
				while (0) {

				}

				switch (nowLook) {

				}
			}
		}
	}

	if (7006 + 0708 + than) {
		if (beYear) {

		}

		switch (yearYearBeBe) {

		}

		for (bool beYearUs = as + as;; asBe = 7 + asNowNow + 0 + '!') {

		}
	}

	if ('!' + '"') {

	}

	while (beAs + 0 + beBeYearBe + 0) {
		if (0 + yearNowBeAs) {
			switch (year) {

			}

			for (bool beYearAsLook = 7 + 7 + 0; 0 - 0; yearAsAs = nowBeNowBe + 7 + year) {

			}
		} else {
			if (as + 7 + '!' + nowYearNowBe) {

			}

			switch (so) {

			}
		}

		switch (beAsNow) {

		}

		if (nowYear - '!') {
			because = our;
		} else {
			while (year + nowYear + '!') {

			}

			for (bool now = '!' + yearNowYearNow;; as = 7 + '!' + asAsBeAs) {

			}

			switch (beBe) {

			}

			yearAsYear -= 7;
		}

		for (bool yearAs = 7;; year += 0) {

		}

		switch (us) {

		}
	}

	for (bool beYearYear = '"';; asNowBeBe = '!' + 7) {

	}

	for (long beYearNowNow = beYearAsYear + 0 + '"' + nowNowBe; '&' + yearNowNow - '!'; yearYearBeYear = yearYearAsAs) {

	}

	for (bool now = '"' + nowAsAs + year + nowYearNow; 4; nowAsBeBe = 7) {
		if (0 + 7 + '"') {

		}

		switch (just) {

		}
	}

	while (71) {
		nowNow = be + '"' + 0 + '"';
		asAsAs = '%';
	}

	while (7 + 8) {

	}

	yearNowBeBe = 7 + nowNowBe + '"';
	if (asBe + yearYearAsYear) {

	}

	while (nowYearAsBe < '"') {

	}
}

#define nowYear 333356563543546666575665354354546524564633335445266563557706041

bool nowYear() {
	for (bool yearYearAsBe = '!' + beNowBe;; beAsAs = to) {

	}
}

unsigned long usSheThanAt(unsigned short sayUpWouldSheHowItsUs) {
	for (long beYear = 29;; asYearNow = 7) {

	}

	if (968) {
		our = 0 + 7;
	} else {
		for (bool year = '"' + 7;; be = yearYear) {

		}

		for (bool asYearYear = '"' + '!' + 0 + '"';; as = '"' + '!' + 7) {

		}

		nowYearNow = yearNow + 0 + '"' + '!';
		switch (yearAs) {
			default:

				break;
			case now:

				break;
			case asYearUs - yearNowNowYear:

				break;
		}
	}

	while ('!' + '!' + '!' + 7) {

	}

	while (109) {

	}
}

#define sayLikeOverOfGiveThereKnowUseItsOurTheJust 643436544454008

size_t giveItsSoWhenOurItsAsItYearHowSayNowHis(long long oneOfBeWantTheseYearButMy, int other, char beSoNot, bool lookBeBeAsTo, unsigned long asOther, int asBe) {
	bool asAs = '"' + '"';
	if (yearYear + '"' + 0 + 7) {
		switch (now) {
			case nowAsAsYear:

				break;
			default:

				break;
			case 2:

				break;
		}
	} else {
		switch (as) {

		}

		switch (so) {

		}
	}

	while (as + nowBe) {

	}

	if (be + 7 + '"' + 7) {

	} else if ('!' + yearAsBe + 0) {

	} else {
		switch (yearYearAs) {

		}

		for (bool be = year + 0 * yearYear;; asYearAs = '"' + '!' + '!') {

		}

		for (char asBe = beYearNowAs + nowAsYearYear + be;; nowAsYear = be) {

		}
	}
}

#define ourTo 5170009

unsigned long asBeYearNow(char asAsYear) {
	bool look = asYear + 7 + 0;
}

size_t these(bool withGiveSoThan, bool us, float be) {
	for (bool our = '%';; nowYearNow = 0 + '"') {
		for (bool beAsNowBe = '!' + nowNowBeYear;; as = '!' + nowNow) {

		}

		while (nowNow + just) {

		}
	}

	int as = '"' + nowBe;
	int nowYearAsNow = '!' + as + 0 + year;
}

char nowBeNowYear() {

}

#define asNowAs 0

bool beOur() {
	switch (nowBeNow) {
		default:
			switch (ofThatHe) {

			}

			switch (yearOurUs) {

			}

			if (just + 0 + yearOther) {

			} else if (0 - 7) {

			} else {
				while (asNowAsAs + yearNowNowYear + yearAsBeAs) {

				}

				for (bool so = as + 0 + '"'; '!' - as; yearYear = yearAsAsYear) {

				}
			}

			bool year = '"' + '"' + '"' + 0;
			bool yearAs = 7 + '#';
			double yearNow = beYear + be + nowAsYearBe + '!';
			break;
		case '<' - 'W' - '!':
			if ('"') {

			}

			switch (as) {
				case '"' * '!':

					break;
				default:
					if (0) {

					}

					yearAsYear = 7 + 7 + 7 + 0;
					switch (asAs) {
						default:
							if (7 + yearYearNowBe) {

							}

							switch (nowAsBe) {

							}

							bool asNow = year + year + '"' + '!';
							asBe = '!' + 01;
							break;
					}
					break;
			}

			for (bool be = 7 + yearBe;; yearNowBeYear = 7 + '"' + yearYear + '"') {

			}

			yearBe = '"' + nowNowNowNow;
			nowBeNowAs = beAsAsNow + year;
			for (double nowBeBe = '!' + '"' + beBe;; yearAs = 7 + now + 7) {

			}
			break;
		case nowNow + nowAsAsAs:
			bool asNowNow = '!';
			for (bool yearBeAsAs = 0;; asBeNowYear = '"' + '!' + 7) {

			}

			if (0 + '"') {

			}

			yearNowBeYear = '!' + beBe;
			switch (as) {

			}

			if ('%') {

			}
			break;
	}

	nowJust = us;
	if (just) {
		nowAsBe = '"';
		now = 0 + nowBeYearNow;
		if (be + 7 + 0 + beYearNow) {

		}

		if ('!') {
			nowBeYearAs = nowAsBe * 0;
		}
	} else {
		if ('!' + 0) {

		}

		switch (yearYearNowAs) {
			default:
				if (nowAsYearAs - 7) {

				}
				break;
			case 70701:

				break;
		}

		for (bool yearThan = 0 + '!' + '"' + 0;; year = 0 + 0 + '!') {

		}
	}

	switch (yearNowBeThan) {

	}

	switch (butUs) {
		case 7 - 0:

			break;
	}
}

bool beYear() {

}

unsigned short overTimeOtherOrToYearUsBeNowLook() {
	for (char now = 7 + '!';; beNow = 0 + '!' + nowAs) {

	}
}

char year() {

}

bool year() {

}

char look() {

}

#define beBeJust 533333330333333331373333333030333077206
#define beNowAsAs 0
#define theyOther 1

double asYearAs() {
	if ('w' | oneHowUseGoLookAtBeOur | lookJustWith != nowAs && asAs - 7) {
		if ('w' & 4721 && '_' || onlyUsTheyOur & thanTheSomeAsTwoTwo) {
			switch (beYearAsTo) {

			}
		} else if (asAsYearNow + beNow + '"' + '"') {
			if (0 + '!' + asNow) {

			}

			while (0 | 7 * 7 + '&') {

			}
		}

		for (size_t asYearGo = just * 7;; nowBe = nowYearBeAs) {
			switch (yearUs) {

			}

			if (0 + 7 + 7 + '!') {
				switch (yearYear) {

				}

				for (bool asNow = 7;; beNowAs = '"' + beNowYearNow + 7 + 0) {

				}

				switch (asAsBeNow) {

				}

				if ('!') {
					while (beAs) {
						switch (beYear) {

						}

						while (so) {

						}
					}
				}

				bool year = 0;
				yearNowBeTo = 0 + 7 + '!';
			}
		}

		long int withBeLook = beYearNow <= '!' - asAsNow;
		unsigned short toNowHeOurLook = asAsBeNow + '!' + be + 0;
		long now = 0 + 7 + 7;
		from = asNowYear + now + '"';
	}

	size_t twoUseOneGoodOfOrUsAsYearThey = than;
	long int with = asBeYearBe + so;
	for (bool yearYear = asBeNow;; nowBeNow = '"') {
		char yearNowYearAs = '"' * so;
	}
}

unsigned int us(long int wayTime) {
	look -= '!' + '"' + '!';
	switch (yearJust) {
		default:

			break;
		case 7 + '!':

			break;
		case '!':

			break;
		case 7 + nowAsAs + '"' + 7:

			break;
		case to:

			break;
	}

	if (yearNow + beAsNow + asAsNow + '"') {

	}

	if (0 + 7 + yearYear) {
		for (bool be = yearBeAs + 7 + '"' + asYearNow;; asYearAs = '!') {

		}

		for (bool nowAs = 7 + nowAsNowBe;; yearBeBeAs = 0) {

		}

		if ('"' * '!') {

		}
	}

	switch (nowAsAsYear) {

	}

	bool asNow = 7 + 7 + '!' + '"';
	nowAsNowYear = '"' + '"' + asNowBeAs;
	our = 0 + 7;
}

long int peopleItsOtherUseUseItsComeNowLookGoodUpWellComeBeTimeItsTheseGoodJust(bool beAsBeNow, bool otherWould, bool be, bool us) {
	if (7) {
		double beNowYear = '!' + '"' + 0;
	} else if (0 + 7 - beNow) {

	} else {
		bool as = 7;
		double beAsAs = 0 + 7 + nowBe + '!';
	}

	if (asBe + as) {
		switch (beYearOther) {

		}
	}
}

#define other 0
#define he 3872709
#define asBeBe 9

long as() {

}

char as() {
	while (2 * '!') {

	}

	switch (sheNowThese) {
		default:
			switch (onlyKnowWayBeBeJust) {
				default:
					switch (year) {

					}

					for (bool asYear = yearYearAs + '"'; now + '%'; be = '!') {

					}

					asYearNowNow = '"' + 0 + be + 0;
					while (7) {

					}

					switch (asYearBe) {

					}

					while ('!' + 0 + '!' + 7) {

					}

					for (char year = 0 + '!' + 0 + 7;; yearNowBeAs = now + '"') {

					}
					break;
			}

			while (7071) {
				bool beLook = 8;
				if (asAsTo) {

				} else {
					while (asYearYear + nowNowYear + '"' + '!') {

					}

					if (beNowYear - nowAsYearNow) {

					}
				}
			}
			break;
		case yearAsNowNow + '!':

			break;
		case beOur:
			if ('!' + beBeAsAs + 0 + '"') {
				switch (as) {
					default:

						break;
					case '!' + yearBe + 7:
						while (as + 7 + '!') {
							if (7 + 0) {

							}

							if ('"' + yearNowYear + '!') {

							}

							for (bool beYearYear = '"' + as + '"';; asOur = yearNowBeNow) {

							}
						}
						break;
				}
			}
			break;
	}

	bool beThanAsGive = as + '"';
	while (now + 7 + nowNowAs + '!') {

	}

	for (bool nowBe = '"' + 7; '#'; as = 0 + 0) {
		if (0) {

		}

		if (just) {

		}
	}
}

#define heItSoAsBeYearNowNowNowBeLook 16
#define themAsNowWell 54898709

unsigned long because() {
	while (nowNowAsYear + as) {

	}

	for (bool be = yearYearBeBe;; be = asYear + beBeAs + nowNowAs) {
		switch (asOne) {

		}
	}
}

bool beYearNowTo() {

}

double yearYear() {
	while (his + other) {
		other = asAsYear + '!' + 0;
		for (bool nowYearYear = beNow + '"' + be; '"' + 8; yearAs = 0 + 7) {

		}
	}

	long int hisOther = 0 + nowAsYearNow + '"' + beAs;
	while (7 + 7) {
		if (8) {

		}

		if (yearBeBe + '!' + asBe) {

		}

		float beNow = '"';
	}

	while (7 - nowBe) {
		if (0 + nowNowYearBe + yearYearYear + '"') {

		}

		bool because = 0 - 0;
	}

	for (float year = beAsYearAs;; nowBe = nowNowAsYear + yearNowBe) {

	}

	for (bool yearYear = as + beAsNow + year + '!';; asBe = yearYear) {

	}

	for (bool beNow = '"' + yearAsBeYear + now + '"';; as = yearYearYearBe) {

	}

	if ('"' + nowYearNow) {
		while ('!') {
			switch (asNowNow) {

			}

			bool yearAsBe = 0;
			for (bool be = '"' + 0 + 7;; asAs = '"' + '"' + asAsBe + 0) {

			}

			yearBeNow = 7 + '"' + '!';
			bool asAs = so;
		}
	}
}

#define oneOneToYearOnlyToBecause 3313783

float beUsToTo() {

}

long int otherSoYearBeUs() {
	bool yearYear = 7 + '!';
	while (0 + '!') {
		for (bool beBeYear = 0 + 0 + asAs + year; '"' - 0; nowAsAsYear = 7 + 0) {

		}
	}

	bool beAsYearAs = 8;
}

unsigned long yearAs(bool our) {
	for (bool beNow = yearNowNow + asBeNowAs + nowYearNowNow;; yearBeNow = asNowAsBe + '"' + now) {

	}

	char beAsAs = 7 + asNowNow;
}

#define nowAsYearNowYearLook 3643645533464663464666455647353696536463933957448

bool be() {

}

unsigned short withBecause(bool from) {
	switch (nowNowAsTo) {

	}
}

#define beYear 7
#define lookLookHaveLook 0
#define nowAsNow 0
#define nowAsBe 46556556067869

unsigned long overItsNowWellTimeLookYearOverBecause(float beNowNow, char nowJust) {
	bool yearOurNowLook = now * '!';
	year *= 7 + '!';
}

#define comeBeAsNowUs 0
#define wayNo 0

char yearYear(bool nowTo) {

}

bool nowYearYear() {

}

bool asNowYear(double asNowAsUs) {

}

bool asNowNowNow() {

}

#define asBeNow 333333303373

long int because(long int goodUsOtherHeThese, bool us) {
	switch (otherBecauseOneOnlyTimeOnlyBeTo) {
		case 7 + beAs + '"':
			nowNowSo = '!' + 0 + 0;
			break;
		default:
			if (asAsAs + 0 + 7) {
				switch (yearOther) {

				}
			}
			break;
		case '!':

			break;
		case 708:

			break;
	}

	unsigned char usWith = 7 + 0 + asYear;
}

#define yearAsNow 2
#define yearYearYearNow 3646553656335536405564433336364353736367663645943634366031304444916
#define butTheAsUs 3761
#define beMy 0
#define be 0
#define be 9
#define yearBeYearNowAsJust 4332332366353455772
#define fromAtNotUs 633442637583539
#define be 7

char nowNow() {
	while ('+' - 0) {
		while (0708) {

		}
	}

	if ('"' - '!' - nowSo) {

	} else {
		switch (year) {
			default:
				bool be = 7;
				if (7) {
					if ('!' + 0 + beAs) {
						char yearAsYearBe = 0 + '!' + '"';
					}
				}
				break;
		}

		for (char now = 078;; asOther = '"') {

		}

		asBeAsNow = beNowNowAs + nowNowAsYear;
		if (to + 1) {

		}
	}

	timeOnlyTimeNowTimeTheseNowBeUs |= beYearAs + '!';
	switch (yearAs) {

	}

	for (bool beYearAs = 7 + asAsYear + be + yearYearBe;; so = nowYearYearYear + 0 + as + '"') {

	}

	as += nowAsAs + yearAs;
	for (bool be = '"';; asAs = '!' + be + yearBe) {

	}

	char beBe = '"';
}

#define timeTimeOnlyNowTimeTimeUsUs 1
#define yearYearBeBe 1008
#define timeSo 171
#define be 7

size_t now() {

}

#define yearBeBe 0
#define asNowBeBe 0
#define to 35765456443653345455666853454435937313

bool year() {

}

bool beNowYear() {

}

bool beAsYear() {
	while (asAsBeNow) {

	}

	nowSo = onlyTimeGood != '_' != 70001 != 770771 - '!';
	switch (asNowAs) {
		case nowYearYearAs + '"' + '"' + asYearYearNow:

			break;
		case asNow + asBeYear:
			bool beAs = '"' + asBeNowNow + nowYearAsBe;
			switch (nowNowYear) {

			}

			as = '!' + 0 - 7;
			break;
	}

	if (708) {
		if (beAsAsAs) {

		}

		while ('"') {

		}

		if (0 + 0 + 7) {

		}

		if (0 + be + 7 + yearBeBeBe) {

		}

		switch (nowNowBeYear) {

		}

		bool be = '%';
	}

	switch (timeTo) {
		case nowBeAsYear + '!' + asYearAs:

			break;
		case '"' + yearBeAsAs:

			break;
		case now - asAsBe:

			break;
		case '"':
			for (bool now = be + '"';; nowAs = 0) {

			}

			switch (be) {

			}

			for (bool year = '"';; beBeAsYear = 0) {

			}

			char beYearAsAs = 7;
			break;
	}

	switch (oneBecauseTo) {
		default:
			yearAsAsUsOtherSo += 0 + '"';
			break;
	}

	for (bool asYearAs = yearBeNowBe + 0 + 0;; asBeNow = '"') {

	}

	while ('!' - nowYearAsYear) {

	}

	nowNow = 0 + nowBeAsNow + '!' + beBeYear;
	for (bool now = 0 + '"' + nowYearYearBe;; as = '!' + '"' + 0 + 7) {

	}

	for (bool year = '!';; asYear = asNowNow + '"' + 0) {

	}

	switch (nowBe) {

	}

	if (7 - nowBe) {

	}
}

#define other 0
#define oneBeOnlyGoodTimeToSo 7072
#define nowYearAsYearOur 3

char now() {
	thereKnowSayItOrSayUpWorkOnlyItsLikeGoodGiveToHave += 28 + asTo;
	while ('"' + '!') {

	}

	for (bool asYearAs = as + '!';; beAsBeAs = '"' + '!' + 0) {

	}

	switch (yearBeAsYearAsUs) {

	}
}

double yearAsNowBe() {

}

#define overGoOtherTo 7
#define asLookUsSoAsNowYearYearNowAsLook 0
#define now 0
#define beYearNowAs 7

bool nowYearNowAs() {
	if ('"' * beAsYearBe) {

	}

	if ('"' + '!' + '"') {

	}

	while ('!') {
		while (0) {

		}

		while (to) {

		}
	}
}

bool yearNowYear() {

}

#define asSo 3333333333333333333333333335
#define ourSo 600701
#define but 46453466543435339543766656464433575544330644443375645654630626556663434543346603672402

size_t useItOtherThem(long int useNowSayOrAllTimeBeSomeComeUsAsYearLook, bool beNow, double as) {
	unsigned short sheTwoThereGoAsThanThatHeOtherNowTo = be + beAsUs;
	for (float yearYearAsTo = '!' + '"' + 2; heNowNowAsYearOur; beYearNow = '!' + 7) {
		for (bool beNowNowNow = '!' + now; these; nowNowYear = beBe) {

		}
	}

	if (now * 0) {

	}

	year = '"' + '!' + now + beAsAs;
	while (nowBeAsAs + 02) {
		bool year = nowAs + '!' + '"';
		for (bool nowBeAs = as + beBeNow;; as = 7 + nowBeNowBe) {

		}

		char yearBeNowNow = '"' + '!' + '"' + '!';
	}

	if (7) {
		if (7 + yearAsNow + 0 + 7) {

		}

		char asAs = '"' + 7 + '!' + '!';
	}

	asAs = 0 + yearNowBe;
	switch (lookNowOther) {

	}

	asNowYearYear = beAs + '"' + '"' + asAsBe;
	beYearUs = as + 7 + nowAsAs + beBeBeYear;
}

#define thereOneWorkHeOfUseSheSomeOverHerWorkWhenHaveItsGiveHerWithSo 6553664434686464534563464433536444665343455035466465545664635653363633656444064337098179
#define beKnowComeWantUsComeJustItsGoodItOnlyHerKnowWellGiveGoodThatItsWantUseComePeopleGoodComeUpGiveUseUpOnlyWhenYouYouKnowThatOverThereKnowYearBecauseHaveSomeWayItsToAsBeUs 3656153656565464335633356354363563445554753456444345665564346
#define theThatFromSo 129

bool asBeAllOurTo() {
	switch (but) {
		case 0:
			if (yearBeBeYear + 0 + 2) {

			}
			break;
		default:
			while (yearBe + 0 + asYearBeBe) {

			}

			switch (asYearYear) {
				default:

					break;
				case '"' + 7 + yearNow + '"':

					break;
				case 7 + 7 + '"' + 0:
					while (nowBeYearNow + asYear - 0) {

					}
					break;
			}
			break;
		case 7 + '"':
			if ('#') {

			}
			break;
	}

	if ('4') {
		look -= 0;
	}

	switch (to) {
		case 7 * '"':
			while (9) {

			}
			break;
	}

	bool nowNowAs = yearNow + 7 + 7;
	while ('!' + asAs + be) {

	}

	if ('!' + 71) {

	}
}

short overMyYearGoodUsShe(int nowBeYearYear, bool look, double asYearYear, double now) {
	switch (noUs) {
		default:
			while (yearAsBeBe + '"' * asYearYear) {

			}
			break;
		case '!' - be:

			break;
		case 7:
			for (bool be = 0;; beNowYear = 7 + '"') {

			}

			for (bool asNow = 7 + '!';; beYearAs = nowBe + beNowNow + '!') {

			}

			asAsAsYear = '"' + nowYearYear + 0;
			switch (nowBeBeYear) {

			}

			if ('$') {

			}
			break;
	}

	while ('"') {
		bool nowTo = 7 - nowNowAs;
	}

	other = 0;
	while ('!' + than) {

	}
}

#define asYearAsNow 7

long long nowThemUsUs(char justBeOur) {

}

#define no 08

unsigned short nowYearBeBe(bool nowBeAs, bool beNowNowSo, bool beBeBeAs, char nowBe) {

}

bool nowAs() {

}

char year() {

}

bool beBeBeNow() {

}

#define yearJust 3333

long int timeOnlyTimeTimeBecauseOneGoodYearBecause(bool nowOtherYearBeBeOther, bool otherTimeHisAsAsTo, bool yearSo, float asBeNowYear, bool nowNow, bool as, char beNowBeYear) {
	bool timeOneAsBeUs = yearSo * beAs;
	while (7) {

	}

	while ('-' + so) {
		while (asBe + '%' - 7) {

		}
	}
}

bool look() {

}

#define other 7

bool nowBeYear() {

}

#define our 345464553455343456644343634563544366456554663465453654653336354466436453654636554644646346656553444445354465453543963664534634633536636356345465555364446534553533360626374756554
#define howUpTheThatLikeTimeHerUpUpPeopleWorkGiveLikeWorkUpItsItKnowLikeTimeOrGoodGiveGiveTwoTimeGiveOneOfSheOverOneNowWellOfOverKnowWorkUseOverItsWellTheYearBeYearSoOther 365634365664543564455336633534556343633633335355765505643345544644636564419708
#define wantGoodWellHaveUseTheAsComeUpTheHowGiveOrOfLookSomeUpItTwoItsOverJustLookOrItOne 00702

long int justOrAllNotGive(bool haveBeYearBeBeNowYearOther, float because, long int upThereGiveUsAsBecause, short wouldAsThem, bool as, bool yearYear, bool now, bool nowAs, bool yearYearBe, bool other) {
	for (unsigned long just = just - '"';; or = yearYearAsYear + 0) {
		while (asAsAs + 7 + beBeBe) {
			if (be + asNowAsBeOur) {

			}
		}
	}

	double nowAs = '!' + 0 + '!' + as;
	usOther -= 2;
	while ('"') {
		if (7 - '!') {

		}

		for (bool year = 7 + now + '"' + 7;; yearYearYearYear = 0 + '"') {

		}

		switch (yearNowThese) {

		}
	}

	while ('!' + nowYearYearAs + '"') {

	}

	while (asAsBeBe - 8) {

	}

	while ('"' + 7 + as) {

	}

	for (bool nowNowAsSo = 0 + nowNow + '!'; 7 * asNowYear; now = asBeNow + 0) {

	}
}

long int us(bool yearNow, bool nowOur, unsigned short beToThey) {
	float asBeAsBe = asAs - be + '!' - 7;
}

long int asNowYearYear(bool ourAsBeAsTo, char so) {

}

bool nowNowBe() {

}

bool nowNowYearYear() {
	switch (look) {
		case '.':

			break;
		case '"' + beYear + yearBeAsNow:
			switch (itsFrom) {

			}

			if (asNow + 0) {
				while (0 + 7 + '"') {
					if (0) {
						char nowAsAs = '!' + 7 + 7;
					}

					while (7 + nowBeAs + '!') {

					}

					while (nowYearJust) {

					}
				}
			}
			break;
		case he:
			for (bool now = 7 + '!';; beNow = beNowAsAs + '!' + 0 + 7) {

			}

			for (bool other = '!' - '"';; nowYearYear = beNow + '"') {

			}
			break;
		case '"' + '!' + 7:

			break;
		case us:
			if (asYearNowAs + yearYearNowYear) {

			} else if (nowAsBeNow - 7) {

			}
			break;
		case now % '"':
			while ('"' * 0) {
				bool yearAs = '\'';
			}
			break;
	}

	for (double beYear = 7 + 0 + yearYearYear;; asYearAsAs = '"' + 0 + '!' + '"') {

	}

	switch (oneTimeTheyWell) {
		case '"' + '!' + yearYearBe + 7:

			break;
		case 7 + '"' + be + '!':

			break;
		default:
			switch (nowYear) {
				case '"':
					as = '"' + yearNow;
					year = beBeYearBe + as + be + 7;
					switch (nowYearAs) {
						default:
							switch (asYearUs) {

							}
							break;
					}
					break;
				case 078:

					break;
			}
			break;
	}

	switch (asYearNowAs) {

	}

	bool yearBeYearAs = asThan;
	if (2) {

	}

	for (long asAs = '"';; nowBeNowNow = 0) {

	}
}

#define upGoodOtherNotOur 07702
#define orAtWantJustOtherOurUsUs 9

bool year() {

}

bool year() {

}

bool beAsAs() {

}

bool yearAsYearAs() {

}

#define yearBeBeBe 7
#define yearAs 0
#define yearYear 33333333333333333330333333387003312009
#define yearAsYearBe 171
#define beBeBe 0

long int be() {
	if (534 + '!' + yearAsNowAs - beBe) {
		for (bool beBeNowAs = 0 + nowYearYear;; beBeBeBe = '!') {
			while ('!' + year) {

			}

			asYear = '"' + 7 + year;
			for (bool yearYear = 7 + 0 + '!';; nowNowNow = nowNowYearAs + asYearYearAs + beYearAsNow + asYearYear) {

			}

			while (yearYearYear + 71) {

			}

			while ('!' + 0 + '!' + 0) {

			}

			for (char year = 7;; beBe = asYearYearYear + nowYearNowYear + '!') {

			}
		}

		while (asNowSo) {

		}
	} else if (5) {
		for (bool now = asBeYear + '!' + '!';; yearAsYearBe = 7 + beNow) {

		}

		while (0 + beYear) {

		}

		if (7) {

		} else if (yearBe + '!') {
			if (nowNowNowBe + 0 + be + asNowYear) {
				if (now + 0) {
					switch (beYearYearAs) {
						default:

							break;
						case yearNow:

							break;
						case '!':

							break;
						case '!' + beYear + 7 + nowYearNow:

							break;
						case 0 + beYearYear + '"':

							break;
						case 9:

							break;
					}
				}
			}
		}

		if ('!' + nowAs) {

		}

		switch (nowAsBe) {

		}

		while (beYear + yearYearNowAs + 0 + '!') {
			switch (nowNow) {

			}

			beBeAsAsTo = 7 + yearAs;
		}
	} else {
		while ('!' + '!' + '!' + nowNowAs) {

		}

		nowYearBe = asAs + 7 + '!' + '"';
		bool now = asJust;
	}

	switch (other) {
		default:
			while (7 + nowNow + '!' + 0) {

			}

			nowBeNow <<= 39 - yearBeNow;
			switch (asBe) {
				default:
					bool beAsAs = 7 + '"' + beNowBeAs + '!';
					switch (just) {

					}
					break;
			}

			asBeAsUs = 7 + nowNow;
			break;
	}

	switch (beAsNow) {

	}

	switch (he) {

	}

	if (1) {

	}
}

#define notTheGoodLookWhenGoYouLookSheItsWithGoodUsOur 36563434662963455875

char year() {
	for (unsigned short wellWellHowComeOneThereYearGoItTwo = 378709 || '!' + beAsAsNow + '$';; yearBeAsNow = now + yearNow + 0) {

	}

	unsigned long toGoodTheOneBut = '(';
	other = 7 + 7 + 0 + 0;
}

unsigned short that(bool beYearYearYear, bool than) {

}

bool our() {
	beNowAs += yearYearBe - '!';
	while (771 / '%') {
		switch (one) {
			default:

				break;
			case 7 + '"' + 0:

				break;
			case 0:

				break;
			case nowBeOther:

				break;
		}

		for (char asBe = '#'; be + '!' + year - 7; as = beHis) {
			if (nowYearBe) {
				switch (asSo) {

				}
			}
		}

		long int because = 0;
		if ('!' + nowUs) {

		} else {
			bool nowTo = nowYear + '"' + 7 + 0;
		}

		while (0) {
			while (0) {

			}

			while (nowBe + 0 + beAsBe + '"') {
				if (now + 0 + 701) {

				}

				if (as + '"' + 0 + '!') {

				}

				char yearNowBe = 7;
			}
		}
	}

	while (soUs + 1) {
		switch (these) {
			case 7:

				break;
			case '"' + 7:

				break;
			case '!' + '!' + '"' + 7:

				break;
			case 7 + '!' + 0 + '!':

				break;
			case beBeNow + 7 + beAsNow:
				if ('"') {
					if (7 + 0 + '!' + 7) {

					} else if (to) {

					}
				}
				break;
		}

		bool asAsNow = 0;
		bool beYearBeNow = '!' + 0 + asAsYearYear + as;
		switch (yearBe) {
			default:
				if ('!' - 0) {

				}
				break;
		}
	}

	asAsAs += 101;
	char be = 7;
}

#define timeAsToHisHis 3
#define onlyTimeOnlyOurAsKnowNowAsYearJust 32
#define as 7

double yearBeBe() {
	youNowComeThemSheSaySomeTheUpItGoodUpItHerOtherGoOverLikeItsUsTimeWantLike ^= oneOneWithOfWithWhenOneHowWellToThereSoItTwoSomeToJust > 2 | 778 == good < '!';
	for (unsigned short whenHerWantOneToMyThese = 'd' == 'e' || 782 + '*' + 72 + 2; 63645352 + '#' - nowBe; asAs = 0 + 0) {

	}

	unsigned char howThemHerPeopleSheTheYearYearUseNowWith = 01 <= '%' + beSo;
	for (size_t asBeYearNowOther = '!'; 03728 - '"'; asOther -= year + 7 % year) {
		if (nowNow) {

		}

		if ('"' + nowBeBeBe + '#') {

		} else if ('!' + 0 + nowBe + '$') {
			if ('"' + nowAsBeNow + nowNowAsNow + asYearAsAs) {

			}

			beBeUs = asNow + 0 + beBeAsBe + year;
		}

		while (nowAs + '!') {

		}

		asNow = 0 * '"';
	}

	while (yearOtherThemToYearToUs <= nowYearNow + otherBeHe * yearYear) {
		while (709 - beNow) {

		}

		nowHeAtWith = '`' > '+' * 'a' - nowNotThanJust;
		if (yearNowAsAs + yearYearAsBe) {

		}

		switch (yearNowYear) {
			case 2:

				break;
		}

		if ('"' + 002) {

		}
	}

	if ('C' + '&') {
		switch (asYearThereOur) {
			default:

				break;
			case asBeYearAs + be + asBeNowYear + 0:
				switch (beNowNow) {
					default:
						for (bool asYearBeBe = be + yearBe;; yearNowBe = asAs) {

						}

						while (0 + '"') {
							switch (us) {

							}
						}
						break;
				}
				break;
			case yearBeBeBe:
				if (0) {
					for (bool as = '"';; yearAsAsNow = 7 + '!' + '!') {

					}

					if (7 + 0) {

					}

					if ('#') {

					}
				}

				bool yearBeYear = beBeBe + asNowYearAs;
				bool nowBeYear = asBeNowBe + beNowAsNow + 0 + '"';
				if (0) {

				}

				double asAs = as;
				break;
		}
	} else if ('"') {
		be = asNow;
		if ('!') {

		}

		if (beNow) {
			while ('!' + '"' + 7 + 0) {

			}

			switch (so) {

			}
		}
	} else if ('"' - 0) {

	}

	bool yearOther = yearAsNowYear + 7;
}

#define youHaveBecauseGoOtherNowUsAsYearButUs 51
#define whenItOnlyItsComeSomeSomeThemThereUsItsItsItsHerHaveOrOnlyNotTimeOnlyTime 354644665644655370444356456356555473150513
#define nowAs 334866553590448
#define yearNowBe 7

bool to(bool beNow, bool beAsYearBe, double year) {

}

#define beBe 633405646643355553472082

char year() {

}

short herWhenUpHaveOneUsePeopleThanNowBeLookBeTo() {
	switch (beYearUs) {
		case 0 + 7 + 7 + asYearAsNow:

			break;
		case '"' + beBeNow:
			yearNow = 7 + yearBeBe + '"';
			switch (yearAsYear) {
				case nowBeAs - 0:

					break;
			}
			break;
		case yearBe:

			break;
		case 0 + beNow:

			break;
		default:

			break;
		case 1:

			break;
	}

	long int goodSo = so;
	asNowNowYear = 0 + '!' + '"' + 0;
	for (long int us = '#';; asBeNow *= beAsAs + yearBe + 0 + nowBeAsYear) {

	}

	switch (us) {

	}

	for (bool asNowBe = 7 + '!';; beBe = 8) {

	}
}

#define he 333733333333033
#define yearBecauseYearAsYearUs 3331
#define usYearTwoOther 7

bool year() {

}

#define beBeAsNow 0

bool yearAsAsBe() {

}

#define beNow 7
#define year 955
#define go 0

bool now() {

}

bool nowYearYear(bool asOther) {

}

bool beNowBe() {
	switch (yearSo) {
		case '5':
			while (beOur <= 7 * asBeYear) {

			}

			for (bool asNow = '!' + 7 + 0; '"' * beBeYear; beNow = now + year + yearYear) {

			}

			switch (now) {
				default:

					break;
				case be:
					for (bool asBe = 7 + '!' + 7; 7 + yearAsYearNow + '&'; be = '"') {

					}
					break;
			}

			if (nowYearNow) {

			}

			if (year + '!' - asBeBeYear) {

			}

			for (bool asYearBeBe = 7 + beNowNow + 7 + '"';; nowYearBeYear = '"' + yearAs + beAs) {

			}

			bool look = 0 + yearBe + yearBeBeAs;
			break;
		case '$' - asAsBe:

			break;
	}

	double nowUs = '"' + as + as;
	int howSomeLookAsWayUsJust = 0 + '%';
	for (bool as = beYearNow + 0 + be;; as = '"' + '!') {

	}

	beAs = '"' + 7;
	switch (goodOurUsWhen) {
		default:
			while ('!' + '!' + 7) {

			}

			for (bool beAs = '"';; beNowNowAs = 0 + nowNowAs + 7) {

			}

			asYearBe = '!';
			beBe = '%';
			break;
	}

	switch (yearNowYearYear) {
		default:
			yearNowYearBe = beBe + 0 + 0 + 0;
			for (bool now = '!' + '"';; than = nowAsAsNow) {

			}
			break;
	}

	if (asAsBe + 7) {

	}

	if (year + '!') {

	}

	while (nowNow + '!') {
		while ('"' - '"') {

		}
	}
}

#define nowWithYearAsUsOur 6533653443333333333333333333333373338303
#define asBeSo 373082008
#define beYear 7
#define asAs 7

char nowBeYearNow() {
	if ('"' + beYearBe + 001) {

	}

	switch (just) {

	}

	switch (asBeNow) {

	}

	while ('"' + 7 - '!') {
		year = as + '"' + '!';
		while (now + '!' + '"' + yearNowAs) {
			switch (asBeNowYear) {

			}

			as = 0 + '"' + yearYear + '"';
			bool beYear = nowBe + '"' + '!';
			beNowNow = 0;
			switch (yearBeBe) {
				case 7:

					break;
				case 7:

					break;
				case as + asYearAs:
					bool than = yearTo;
					break;
			}

			switch (yearAsYear) {

			}

			if (other) {

			}
		}
	}

	switch (giveUseJustYearUseToOfBeTo) {
		default:
			if (7 + as) {

			}

			for (float nowAs = 7;; nowBeBe = 0 + 0 + '"' + '!') {

			}
			break;
		case '!' + '"':

			break;
		case 01 - 7:

			break;
	}
}

double as() {

}

unsigned short timeUpPeopleOurNowYouAllAllJustThan() {
	now += nowAs + '!' + 7;
	bool nowNowBe = yearAsBe + 0 + yearNowAsAs + beYearAs;
	for (char asBeNowNow = '!';; nowBeBeAsOther = '!' + '!' + '!' + nowBeAs) {

	}
}

long long yearNowBe() {

}

#define ourNowAsAsYearNowOur 221879

short yearYear() {

}

#define nowYearAs 0
#define nowYearBeNow 709
#define nowNowNowNow 8
#define asAs 0

float yearBeNow() {
	switch (oneHeTimeNowYearTimeAsTimeNowOther) {
		case yearAsNowTo:
			bool us = '!' + '!' + '!';
			break;
		case 0 + 0:
			while (7 + '!' + now + 7) {

			}

			if ('!' - yearNowBeBe) {

			}
			break;
	}

	while (yearAsYearBe / '"') {

	}

	switch (toBecause) {
		default:
			if (yearYearYearYear) {
				to = '!' + 0;
			}
			break;
	}
}

long int soTo(bool so, bool hisBeOtherYearOther, float year, bool asAsOther) {
	while (0 + 0 + asNow) {
		while ('+') {

		}
	}
}

#define oneBecauseOneOneHeThereJustUseNowYearAsYearOur 76

bool year() {

}

bool yearBeAs(bool now, bool be, bool to) {

}

#define beBeYearYear 7
#define so 
#define  555444563536634553453536646533535637555666657566644356340664366223845443
#define nowAsAsBeJust 443925421034976

size_t wayTo() {
	if (7 == 153 & '!') {
		while (7 + 7 + 7) {

		}

		if (be) {

		}

		while ('$') {

		}
	} else if ('"' + yearNowYearAs + '!' + 0) {

	} else if (now + asYearAs) {

	} else {
		for (bool nowYearNowNow = 7 + asNowNow;; beAsAs = '"' + now) {

		}

		if (asBe + asYearYearYear) {

		}

		switch (asAsBe) {
			case nowAsBe + other:

				break;
			default:
				than = '!' + nowBe + '"' + nowNowBeNow;
				bool as = 0 + '"' + beYearBeBe;
				while (7 * '"') {

				}
				break;
		}

		switch (asBeYear) {

		}

		if (7 + beBe) {

		}

		bool nowNowNow = '"';
		switch (nowBeBe) {

		}

		beBeYear = '!';
		switch (look) {

		}
	}

	nowYearAllNowNowAsAsThan &= '!';
	switch (nowNowBeTo) {

	}

	double year = '&';
	bool so = '"' + 0;
	if ('!') {

	}

	for (bool year = '"' + yearBeNow;; asNow = year + nowBeAs + asNowYearYear) {

	}

	if (7) {

	}

	while ('!' + as + '!') {
		while (nowYear + 0 + asAsBeNow) {

		}

		bool asAsBe = yearYear + '"';
		switch (nowThan) {

		}
	}
}

bool nowNow() {

}

#define beBeNowBe 44220571

size_t someYearYouPeopleAll(char yearNow, unsigned int wellOneYearTo, bool ourUs, bool than, bool beAsBeBe, double asNow) {
	switch (soBeNowOther) {

	}

	for (bool year = '"';; asYear = 0) {

	}

	while ('"' + '!' + '!') {
		while (7 - 0) {
			beThan = now + 7 + 0 + 7;
			while ('$') {

			}
		}
	}

	if ('!' + our) {

	}

	switch (but) {
		default:
			while (6) {

			}

			yearBeAsYear = asBeBe;
			for (bool be = '"' + '"' + '!';; asNowAs = be + '"' + '!' + year) {

			}

			if (yearYear) {

			} else {
				if (our) {

				}
			}
			break;
	}
}

#define beNowKnow 6722
#define as 38

unsigned char asYearBeLook() {
	nowAs = '"';
	yearBeAsBe = nowBeYearNow + '"';
	for (bool yearNowYear = 0 + 0;; be = 7) {

	}

	so = yearYearBe + 0 + 7 + '!';
}

bool asNow(bool nowSo) {
	he |= beBe != 'O' - '%';
	switch (yearSoAsToOther) {

	}

	switch (other) {
		default:

			break;
		case nowAsNowBe + beBeYear + 7 + 0:

			break;
		case '"' + as + 7:

			break;
		case '!' + asAsAs + 0:
			bool beYearNow = '!' + beUs;
			break;
	}

	switch (these) {
		default:
			switch (as) {

			}

			while ('#') {

			}
			break;
	}
}

long int goodGoodTimeTimeSoGoodTimeOnlyGoodTimeOneBeAsWouldAsBeAsLook(bool nowNow, bool beAsBeYear, bool justYearNowAsYearYearLook) {

}

#define beOther 7
#define look 45653663466654656543833335454454655541643633354033236556543365463343546390643543467750081

short beThanSoNowNowJust(short whenWantBeKnowHaveOneSoComeOtherBeAsAsAsYearNowYearJust, long long orOneHowWhenComeUsWantKnowWhenItOfYou, bool to, bool our) {
	bool to = usJust * '!';
	double other = '!';
	if ('!' * 0) {

	} else if (as + 7) {

	} else if (0 + 7 + '!' + 7) {

	} else if ('!' + 0 + '!' + '"') {
		while (asBeYearYear + beYear + asNowAs + year) {
			double beAsYearYear = 0;
		}
	}

	switch (ourBeNo) {

	}

	float asBeAsBe = 7 + '"' + 0 + '!';
	for (bool beNowNow = be + '!';; yearYearYearNow = be + 0 + '!' + 0) {

	}

	for (bool nowNow = 0;; now = 7 + nowAs + 0) {

	}

	for (bool beNowYear = yearBeBeAs + '"' - yearNowNow;; asAs = '!') {

	}
}

double asYearBeYear() {

}

#define likeIts 7
#define look 454437553943971

size_t lookThatSayItsOverAt(bool look) {
	switch (our) {
		case '!' + as + '$':

			break;
		default:
			if (beYear + nowYearNow + 7 + '"') {

			} else {
				asOur = '!' + 0;
			}
			break;
	}

	if (5) {
		for (float nowAs = beYear + 7;; year -= '!' + '"') {

		}
	}

	switch (nowBeAs) {
		default:
			switch (nowYearNow) {

			}

			while ('%') {

			}

			yearNow = yearAsNowYear + 0 + 7 + 7;
			bool yearAs = '!' + beYearNow + '"' + year;
			while (0 * 0) {

			}
			break;
		case 0 + 0:
			switch (as) {

			}

			nowNowAsAs = yearYear + be + '"' + '!';
			for (bool as = 0;; nowBeNow = 0) {

			}

			bool asBeAsNow = 7;
			bool be = 7 + year;
			while (asUs) {

			}
			break;
	}

	other = '!' + '!' + 7 + 7;
	beYearAsToBeThan = '"' + '"' + nowBeAs + '"';
	for (char year = be;; nowYearAsBe = 0 + 7) {

	}
}

#define yearSo 6

bool nowTo() {

}

#define nowBe 7

double beAsOurLook() {
	while (3333703 != 0333137331 != usOther + nowYearTo - beAsBeUs) {
		switch (nowYearYear) {
			case nowBe + year:

				break;
			case '"' + yearNowBe:
				nowYearAs = nowYearAs + 0 + 0 + as;
				if (7 + '#') {

				}
				break;
		}

		char heHe = 7 + yearYearNowSo;
	}

	nowYearYearAsBeSo = 0 - asAsBeNow;
	while ('!' - 0) {
		while (asNowBe + beBeNow + '!' + '"') {
			for (bool us = 0;; nowYearAsAs = yearBe + '!') {

			}
		}

		switch (nowYearAs) {

		}

		switch (to) {
			case '"' + 0:
				while ('"' + 0 + 7 + 0) {

				}

				bool yearAsBeYear = 7;
				yearAs = beBeYearYear + 0 + '"';
				for (bool nowNowAsAs = 0 + '"' + 7;; year = beAsAs + year) {

				}

				while (0 + yearNowNowNow + asBeAs + '!') {
					bool as = '!' + nowBeYearAs + asAsYearAs + 7;
					year = '"' + asYear + 7 + 0;
					while ('"' + 0 + be + beBeYearNow) {

					}

					for (bool beYear = 0 + 7;; now = '"') {

					}

					if (0 + 7 + asNow + 0) {

					}

					switch (yearYearAsYear) {

					}

					bool now = so;
				}
				break;
		}
	}

	for (char as = 7 - nowYear;; as = 0 + yearAsYear + '"' + 0) {

	}

	if (beUs - now) {
		beBeBe = asYearAs + asYearBe;
		switch (asAsBeBe) {

		}

		if (be + '"' + year) {

		} else {
			for (bool yearYearYearBe = yearBeNow + 7;; nowBeYear = nowAs) {

			}

			switch (nowNowBeNow) {

			}

			nowSo = 0 + asNowAs + asYearAsYear;
		}

		asNowAs = 7 + asYearYear + 7;
		as = 0 + '!' + 0 + yearBeYear;
		if (asYear) {

		}

		if ('!' + 7 + asYearNow) {

		}

		while (701) {

		}
	}
}

#define timeHaveThemBecause 66652466455366354444465444533576350122
#define twoOtherThan 779

unsigned long now(double nowAs) {
	us = than;
	nowAsAsAs = 801;
}

#define than 45353010255

bool be(bool asNowBeBeBeNowLook) {
	other = '"' + 7 + '"';
	while (asNowNowNow - 7) {
		while ('"' + asAsNowAs + beYear + 0) {
			if (nowBeBe) {
				year = beYearNow;
				while (nowBeBe + '!' + 0) {
					long now = yearAsYearBe + 0;
				}
			}

			while ('!' + 7 + 2) {

			}
		}

		sayThan = now;
		while (7 + '!' + beBe + 7) {

		}

		for (bool nowNow = beBe + 7 + '"' + '"'; to + 22; asAs = asAsBe * 0) {

		}

		if (nowYearYearYear + beNow) {

		}

		bool beNow = asNow;
		while (0) {
			than = nowAs;
		}
	}

	switch (goodSheTheUsHe) {
		case '!' % '"' + 9:
			if ('!' + 7 + 0 + asAsAs) {

			}

			nowYearBe = 7;
			switch (look) {

			}
			break;
		case asBeBeBe + yearBe:

			break;
		default:

			break;
		case be:

			break;
		case 0 + 78:

			break;
	}

	switch (yearUs) {
		case '!':

			break;
		default:

			break;
		case '!' + 0 + '"' + yearBe:

			break;
		case '"' + 0:

			break;
		case yearAsNow:
			switch (nowBeAs) {

			}

			for (bool now = 0 + '!';; nowNow = '"') {

			}

			asNowBe = 7;
			if (0 + beNowBe + '"') {

			} else {
				beYearNowYear -= yearYear + now + 0;
			}
			break;
		case '"':
			bool yearNowBe = 7 + '!' + 0;
			if (0 - '"') {

			}
			break;
	}

	switch (year) {
		default:
			while (7 + '"') {

			}

			for (bool yearYearNowAs = '"';; beAsNow = '"') {

			}

			for (bool beBeBe = beYearBe + nowAsYear;; asNowBe = 0 + 7 + '"' + year) {

			}

			switch (asYearNowAs) {

			}

			while (7 + be) {

			}

			for (bool yearBeBeBe = '!' + 7;; beBeAs = '"') {
				if (asBeAsNow) {

				}

				if ('!' + now + '"') {
					if ('!' + '"' + nowAsAs + beYearBeNow) {

					} else if (yearAsYear + yearAsBe + asYearNow + nowBeBeYear) {

					} else if (7) {

					} else if (be * '!') {

					}
				}
			}
			break;
	}

	if ('-' + asNowAs + asNowYearYear + '%') {
		bool asAs = nowBeNow + 0 + 0 + year;
		switch (asAsNow) {

		}

		bool nowAsNowAs = 7 + '!';
		while ('!') {
			nowBe -= yearBe + 7 + 0 + '!';
		}
	} else {
		switch (now) {
			case asBe:
				while (asNowBeBeLook) {

				}
				break;
		}
	}
}

long int itsKnowNoLookGoNowTo() {
	while (asBe + '!' + 0) {
		while ('!' + '"' + '!') {

		}

		bool yearBeYearBe = now * '!';
	}

	switch (other) {
		case asAs + so:

			break;
	}

	bool just = 7 + be;
}

unsigned int year() {

}

#define yearBeNow 53333333333333333333073331

long int soYearAsUs() {

}

long int asOther() {

}

#define twoOur 068

bool year() {

}

bool year() {

}

#define beNowAsBe 0

bool yearBe() {

}

bool just(char beAsAsBe) {
	unsigned short goOfWantMyYouOrPeopleGoodSomeGoBeUsComeOrLikePeopleKnowGoodThereUpComeComeLikeHowHowSheYearOrTwoSheComeUseYearOurNowComeTheseYearAsUsNowOur = 3655066633645573665 == 501;
	while (beNowNow) {
		long as = 7;
		for (bool beNowYearNow = '"' + asAs; yearAsBe / '!'; asNow = nowAsNow) {

		}

		bool be = 7 + '"' + 7 + year;
		if (beNowBe + 0) {

		}

		asNowBeYear = '%';
	}

	heThan = '!' + asBeYear + beYear;
	while (502 * asBeBeBe) {
		switch (orYearSo) {

		}
	}
}

#define nowNowAs 0073

int just() {

}

unsigned int wantUpOverJustTheGoodPeopleTheYearTo() {

}

bool as(bool nowAsThan) {
	if (soWay) {

	} else if ('&' + than) {
		switch (ourJust) {

		}
	} else {
		while (0) {

		}

		bool asAs = asBeNow * nowAs;
	}

	while (as + '"' + 7) {

	}

	if (0 + 0 + '"') {

	}

	nowNow = '"';
	yearBeYear = as + '!' + '!' + 0;
	if (0 * '!') {

	}
}

#define whenGiveNoThey 441366180012
#define to 7

int yearBeNowYear() {

}

#define ofTheseTheAtUsGoodBeOnlyNowYearAsUs 7079

unsigned short just() {

}

bool nowBeAsBe() {

}

bool be() {
	for (bool asAsBeAs = asNowYearAs + 0 + 7 + 7; 0 + '$'; as = 0 + 0 + 7 + beAsYearNow) {

	}
}

char asYearNowAs(char asYearNow) {

}

#define asJust 333333333333333333333333333333349446654455133403434470077000709
#define asBeYear 7

unsigned short than() {
	for (double be = our; 7 * 0; asAs = as + nowNowYear) {

	}

	for (bool asBeYearNow = 0 + '!' + year;; asAsNow = nowYearNowNow + nowBeBe) {

	}

	yearBe = yearAsAsYear + '!' + 7 + '"';
	switch (beYearYear) {

	}

	asAsYearYear = 0 + 0 + 1;
}

long long yearThemFromWould(unsigned long yearYearNowTo) {

}

float be() {

}

#define asButThese 33333333383333333333333331333

char asNow() {

}

#define goodYearYearNowOther 7
#define ourBeNo 8746
#define nowAsBeNowNowUs 98

double beBeNowAs() {

}

bool as() {

}

#define be 79
#define yearBeNowNow 7
#define beAsYearAs 7

bool look() {
	for (bool asNowBe = yearThere < '!'; yearYouThereOur - yearNowUs && 0 * '"'; he -= 9) {
		switch (now) {
			case just:

				break;
		}
	}

	while ('v' | asSo - 2) {
		butYearLook = look;
		no += '>';
		if ('!') {
			bool nowAs = '!' + '!';
			nowTo = asNow + 7 + '!' + '"';
		} else if (asBe + 0) {

		} else {
			for (double nowBe = '!';; year = nowAs + 0 + 7 + '"') {

			}
		}
	}

	switch (soBeNowBeUs) {

	}

	nowYearThan = 7 + asBeNowNow + beAsBe;
	if ('!' + 0 + 7 + 7) {

	}

	bool now = '"' + '"';
	while (7 + now + '!' + 7) {
		switch (yearYearAs) {
			case yearYearNowBe:
				bool nowYear = asNow + 0 + asAsNow;
				yearYearAsAs = nowNowYearAs + '%';
				break;
		}
	}
}

size_t wellToYearYearAsNowBeYearOur(bool asBeNow, float asBeNow) {
	while ('&' - 0) {
		for (bool yearAsBe = 0;; yearBeNow = beYear + beYear) {

		}

		switch (be) {
			case 7 + '!' + asBeAs:

				break;
			case 7 + now + '"':
				switch (yearBeYearNow) {

				}

				bool beBeYear = '!' + 7 + 7;
				bool beBe = beAs + 7;
				while (7 + '"' + nowNowYear + '!') {

				}

				bool beYearNow = '!' + 0 + 7 + 7;
				while (be + '!' + 0 + beAs) {

				}

				bool nowNowYear = '"' + '!';
				switch (so) {

				}
				break;
		}
	}
}

bool yearAsBe() {

}

#define asNowBeAs 7
#define yearNow 0

bool nowAsBe(bool asNowYearAs, bool beBeAs, double beAsAs) {
	long int yearSo = year + yearNowBeNow + '!' + '"';
	long int goodTimeNowHisTheseBecauseUsYearBeBeNowYearSo = 33333333333331371 != asOther;
	while (7) {

	}

	for (char nowAsYear = year;; asAs = '"' + '"') {
		yearYearUs = 7 + '"' + 0 + 0;
	}

	if ('!' + yearNowNow + 7 + 0) {
		for (bool nowBe = now + '!' + asAsBeAs;; as = nowBeNow + '"') {
			bool asBeBe = '#';
		}
	}
}

bool beBeBeNow() {

}

bool theseOther() {

}

float asNowNowAs() {

}

bool be() {

}

#define yearYear 0709
#define year 3535553263643054644338743644

bool as() {

}

#define yearUs 3854

double asNowNow() {

}

#define would 7
#define yearBeYearUs 972
#define asYear 533337333333333333333333333369
#define be 14
#define want 8
#define asYear 

long int (long int workTwoComeHaveWellItGoSomeKnowOverTheItKnowOneAsItOverToYearUpItSheTwoSheTimeThanThey, unsigned int knowSayKnowWorkSheToOverHaveItsOnlyUsWellWorkOrUs, bool beNowAs, unsigned short useOfOtherLook, unsigned long nowBeYear) {
	useGoodHerHaveHerOneThatGiveAsToOnlyWorkSoOur ||= 3434346344446464639332567464347594705 || ')' | 'o';
	while ('!' + 7 + asYearNow) {
		if (0 + as) {

		} else {
			bool as = us;
		}

		for (bool yearYear = '!';; nowNowNowYear = 7) {

		}

		if (0 + '!' + '"' + nowYearAsBe) {

		}

		for (bool asAsYearBe = beYearYearAs + nowBeYearAs + 7;; beAsYear = asNowYear + nowBeAs) {
			beBe = '!' + yearNowNowAs + beBeBe;
			double yearNowAsBe = 7 + '"';
		}

		while ('!') {
			while (asAsNowAs + 0) {

			}

			switch (us) {

			}
		}
	}
}

long long workThereHerGoGoodWhenSomeTwoGoodSomeSheOfWellSheUpLikeOfYouThatThereTwoGiveThereWantUseHaveThatYearWellOfOverHowGiveWantSayYouAtWorkWhenGiveItOverUseTwoKnowUpOrKnowPeopleLikeWellSoYearBeYearThey(size_t nowSo, bool soKnowYearOther, bool nowBeAs, double be, bool nowAsBe, bool nowBe, double yearBeAs) {
	switch (therePeopleYouTheKnowHaveGiveComeItsAsTheUsHerOnlyHaveAsSoAsTimeOther) {
		default:
			for (bool now = '!' + yearNowYear + '"'; knowNowHis != our - asYearYearBe; heTimeOtherBeSo &&= 9 % asSo - beYear) {

			}

			for (bool yearNow = 7 + beAs + '!' + 7; 771; all = beBeAsAsBeBeBeOther) {
				if (yearAs) {

				}

				if ('!' + yearAsBe) {
					if (yearAs + asNowAsNow) {

					}

					asYear = '"' + '"' * 7;
				}

				while ('"' + '!' + '"') {
					switch (beBeNowThan) {

					}
				}
			}

			other = as / asYear;
			beBeBe = 0 + '"' + '"';
			switch (asAsBe) {

			}

			for (char yearAsYear = '!' + 7 + '!' + '!';; asNowNow = 7 + yearBeNow) {

			}

			bool nowAsYearYearYearLook = '"' + beAsNowBe + 0;
			break;
	}

	while ('Z' % 0) {
		while (0 + nowNow + 7) {
			if (7 + 0 + '"') {

			}

			nowNowNow += '"' + just;
		}

		switch (asYearNow) {

		}

		nowYearBeAs = asBeAs + year + 7 + year;
		bool justJust = 7 + '"' + 0;
		while (7 + 7 + 7) {

		}

		asOther = nowAs + 0 + '!' + as;
	}
}

size_t itWantOtherComeTwoComeSaySheWorkTwoOverGoodWantSayOneTwoWantGoodOfBeSayWhenWellKnowHaveOneHaveTheTimeWellItItNowAsWantSomeSheGoodWellSheSheWorkYearLikeFromHerOverWellThatTheNotSheTheOrComeOrSayOneBecauseTimeFromBeNowYearBeBeTo(unsigned int thatYouSoOfHerOfTimeSheHerOrKnowAsHerSomeItSayOrYearLikeJustYearLookOtherGiveNoBeBecause) {
	if (',') {
		double asAsOther = '|';
		switch (nowLook) {
			default:
				double as = 7 + be + be;
				break;
		}
	} else {
		bool nowNow = nowAsBe;
		while (nowAsYear + 7) {

		}

		if (7) {
			for (bool nowNowBeYear = nowYearNow + '"' + 0 + '!'; '!' * 0; as = 0 + year) {

			}
		}
	}

	if (0 + 0) {

	}

	for (size_t nowBeLookSheYou = 'q' * '"';; nowNow = '!' + beNowAs) {

	}

	if ('"') {
		bool asNow = '"' + 0 + 7 + 0;
		yearThan = 7 + '!';
	} else {
		while (yearBe + '!' + 7) {

		}

		switch (beNowAsAs) {
			case '"' + yearAsAsBe * nowYearBeNow:

				break;
		}
	}

	nowYearBe = '"' + as + 7 + year;
	switch (yearBe) {
		default:

			break;
		case 0 + nowAs + 7:

			break;
		case 0 + '!':

			break;
		case 7 + 7 + year + '"':

			break;
		case 7 + 7 + be + nowNow:

			break;
		case '"':
			for (bool nowNowNowNow = 7;; beBe = beNowAsBe + year) {

			}

			for (bool beNowYear = asNow + yearYearBeNow;; nowYear = nowBeYearAs + '"' + '!' + '"') {

			}

			if ('!' * asYear) {

			}
			break;
	}
}

long long justThey(bool toYearOther, long double nowYearNowYearYearUs) {
	beBeTo = '"' + asYearBe + 0 + beYearNowBe;
	while (yearBeBe + yearOur) {
		switch (yearAsBeAs) {

		}

		for (bool asBeYear = 0 + yearBeAsAs + 7 + nowBe; '$'; be = 7) {

		}
	}
}

bool our(bool look, unsigned short asNowBe, bool beNow, char nowBe) {
	long long asBe = 37708 > 7;
	for (double as = '"' + 7;; nowAsNowAs += 0 + '"' + asAsNowNow + year) {
		switch (so) {

		}
	}

	while ('!' + nowYearNowAs + '"') {

	}

	while (0 * '"') {

	}
}

#define howPeopleOrHerThemComeUsNowTheseUsOther 476454
#define ourSo 01
#define whenYearWouldThan 796772

unsigned char beYearBe(bool nowBe, bool yearYear, bool asBe, bool yearUs) {

}

double yearAsUs() {

}

int nowYearYearBeOur() {

}

bool asYearOther() {
	switch (upTo) {
		default:
			bool yearBeYear = 7 + '"' * yearBe;
			break;
	}

	while (beThan) {
		double yearAsBe = '!';
		while ('!' + 7 + 7) {

		}

		while (7 + '"' * '"') {
			while (as + '!' + '!' + yearAs) {

			}

			if (year) {

			} else if (7 + '!' + 7) {
				for (bool beBeAs = 7 + be + yearNow + 0;; now = '!' + 0) {

				}

				yearNow = 7 + 7;
				bool asYear = 0008;
			}
		}
	}
}

#define asYearNowSoSoOther 543646649

unsigned int heYearJust(char now, bool nowAs, bool to, bool just, bool just) {
	bool year = '"' + asYearAs + '"' + '"';
	if (0 + '!' + 0) {
		if ('"' + 0) {

		}

		for (bool asNowAs = asBeAsYear + 7 + asNowYearAs + '"';; beBeNow = '"' + 0) {

		}

		for (bool nowNowAs = yearBe + '!' + yearAsNow + yearBe;; nowYearBeTo = 7 + nowYear) {

		}
	} else {
		for (bool nowYear = 7 + '"' + yearNowYearYear + 7;; be = 0 + 7 + '"' + as) {

		}

		while (0 + '!' + 0) {

		}

		bool yearBeNowBe = '$';
	}

	bool beAsNowBe = '!' + 0 + '"';
	while ('"' + '"' + '"' + nowBeNow) {

	}

	if (522 * '"' + 7 + 078) {
		while (yearAsYearBe + '$') {

		}
	}

	double yearAsAsUs = '+';
}

#define youOverOur 078379
#define asNowAsBe 333

long int oneOnlyGoodTimeOneOnlyHeOnlyTimeTheseYearAsAsYearYearOtherNowAsNowAsSo(long int nowNow, bool yearBeUs, bool nowYearNow, long int nowNowYearNow) {
	while ('"' + '"' + 0 + 7) {
		yearYearBeNow = beAsYearNow + yearYearNow + 0 + yearBe;
		asBe = '!' + '"' + 0 + '!';
		switch (asNowNow) {

		}

		char now = 7 + 0 + year + '!';
	}
}

long int beHisOneOnlyAsNowTo(double asAsYearTo, char beBeNow) {
	while (0) {

	}

	year = '!';
	char nowNow = yearYearBeAs + '"' + '"';
}

#define asBeTo 7
#define yearBe 7

bool as() {

}

#define nowBeAs 7

bool asNowYearNow() {

}

char asAs() {
	while ('n' - nowUs == 'a' - our) {
		if ('!' + beNowBe + yearAsNow + year) {
			while (7 + '"' + '"' + '!') {

			}

			bool other = '"' + '!' + '"';
			switch (yearAsYear) {
				case '#':
					if ('"' * '!') {

					}
					break;
			}

			bool beNow = '%';
		}
	}

	while (7 + 7 + 0 + 7) {
		while ('"' + 7 + yearBeYear + yearAs) {
			than = '!' + '"' + '"';
		}
	}

	while (51 - '!') {
		switch (nowYearBe) {

		}

		switch (nowYearNow) {
			default:
				char now = 0 + '"';
				break;
		}
	}

	for (bool yearBeAs = 7;; nowBe = 0 + 7) {

	}

	if (902) {

	}
}

#define yearBe 005

bool nowNowThan() {
	if ('5' <= 2) {

	}

	for (bool asNowNowAs = '"'; asNowYearYearTheyAsThan * 0; beBeNow = 0 + '"' + 7) {
		while ('%') {
			bool nowBeAsNow = year + '"';
			asAsBeAs = '"' + nowSo;
		}
	}

	bool beBe = '#';
}

#define knowNowItsNotWhenItsThan 4

bool other(bool nowBeAs, bool year, bool so) {

}

unsigned int justThanYearLook(bool beOurYearAsBeTo) {
	bool hisAsGive = 0 + 01;
}

#define goodOurUseOrAsOtherLook 0

long long nowAsAsNow(double than, bool to) {
	switch (onlyTimeNowToOtherUsOnlyOneGoodHeTimeGoodUsOnlyAsAsYearTo) {
		default:
			while (asBeAsNow + 7) {

			}

			char asAsYearBe = 0 + 0;
			break;
	}

	otherNowUs |= '_';
	if (as + 7 + 1) {

	}
}

float year() {

}

#define he 3333177708

unsigned int nowBe() {
	while (have + 8) {

	}
}

size_t yearUs(char yearYearBeHe) {
	if (asWithAsThanTo + 7 != '"' + '!' * 0) {

	}

	nowYearAsYear = be * beBeNowYear * 7;
	for (long int nowBeYearYear = 7 + '!' + '!' + nowBeNow;; now = 0 + beBe) {
		for (bool nowNowYear = 0 + 0;; yearBeYear += '"' + 0 + '"') {

		}
	}

	but = 0;
	switch (so) {

	}
}

#define goUpComeOurHowOfTimePeopleKnowUpHis 67335206545781
#define ofTheyYearYearAsYearAsOther 523823

bool nowBut() {

}

#define beBeNow 0

char beNow() {

}

#define asAsAsBe 0
#define as 5366544445653663420633324331

short notBecauseIt() {
	while (7 + 7) {

	}

	while (nowYearAs + year + 7 + '!') {
		if (0 + nowAsAsYear) {
			for (bool beAs = yearNowAs + beBeAs; 09; beYearNow = 7 + '"') {

			}
		}
	}

	while ('"' + 0 + now + 77771) {

	}
}

#define us 533333338333333873833
#define soBecause 330078

bool yearBecauseBecause() {
	char yearAsBe = now + '!' + asAsBe + asAsNowNow;
}

#define other 33333
#define because 9

char nowNowNow() {
	switch (nowNowBeYear) {

	}

	if ('!' + nowNow + 7 + '"') {

	} else {
		switch (be) {

		}

		nowNowYearBe = '"' + 0 + '!';
		bool to = '!' + '!' + 0;
	}

	for (bool beAsAs = '!' + asNowNowBe;; now = '"') {

	}

	bool year = 7 + as + 7 + nowNowBeNow;
	asYearAs = beYear - '"';
	switch (nowNowBe) {
		case 7 + 7 + '!' + '"':

			break;
		case '!' + yearAsNow + '!' + '"':

			break;
		case 7:
			while (yearBeAs + nowAsNowBe + be + yearNowNowAs) {
				bool asNowNowAs = 7 + '"' + be;
				bool nowYear = asAsYearBe + '!';
				switch (yearNowAsBe) {

				}

				while ('&') {

				}
			}

			while ('%') {

			}
			break;
	}

	if (7) {

	}

	switch (now) {

	}

	bool be = '"' + now;
	now = asJust;
}

#define becauseThanBeThan 563564565533365073539908
#define thereOurNowJust 7
#define to 7

double beBe() {

}

long int yearNowNowBe() {
	for (bool asNowAsNow = 7;; beBeYearNow = now + '"') {

	}

	so = 7 + 7 + 7 + 7;
}

#define asAsJust 749
#define asOther 01
#define beBeAsBe 7
#define asNowNow 0

bool nowAsYearNow() {

}

bool beYearAsBe() {

}

#define now 0

bool asAs() {

}

bool asNowYearNowLook() {
	for (bool beYearYearNow = asBe + '!' + 7;; asBeBeAs = '!' + 7 + 7) {

	}

	for (char asAsYearBe = beYearYearBe + beBe + '%';; yearYearOtherTo = 71) {

	}

	switch (nowBeBe) {

	}

	char as = 7;
}

#define goodTimeBeUs 33333333333333333303303133372

bool as() {

}

#define beYearYear 18
#define now 0
#define yearAs 5655536646363465466543357867007008
#define knowThere 0
#define be 6604

bool asAs() {

}

#define as 2333333333380001

long int otherGoodGoodTimeAsNowOneHisSo(char year, bool yearBe, bool asNowAsAs, bool beAsBeAsAsUs, char nowBeYearAs) {
	bool nowYear = '!' + 7;
	if (0 + 0 + '"') {

	}

	bool asBeAs = 0 + now + 0;
	switch (yearBeAsYear) {
		case asBe - 7:

			break;
	}
}

#define asAsAsAsAsTo 333333365367301

bool yearYear() {

}

long otherYearTo() {

}

bool as() {

}

#define so 7
#define yearNowYear 43656554575
#define youYearShePeopleJust 636680067578
#define usNowTo 5574670570008
#define us 59

bool asNowAs(int nowNowBe, double yearBeNow) {

}

#define beNow 0
#define nowYearYearYear 7
#define they 87708001

long int onlyHis() {
	switch (theseOne) {

	}

	for (bool year = 7;; asYear = year + beNowNowNow + 0) {

	}

	for (bool as = 1;; asAsBeYear = asBeAs + now + asNowNowAs + '!') {

	}
}

long int hisOther(float nowYearBe, bool nowNowAs, bool yearAsAs, bool asBe, char year) {
	switch (as) {

	}

	nowNowBeNowYearSo = 801;
}

long int nowYearAs(bool asTime) {
	if (yearBeNow + 7) {

	}

	while ('!' + '"' + 0 + 7) {
		while (7 + 0) {

		}

		if ('"' - 0) {

		}
	}

	char us = nowOtherAsYearAsUs;
}

#define only 83313831
#define theseTo 0

char other() {

}

#define beAsYear 2709

bool no() {
	while (7 + now + 7 + 0) {
		switch (justNowAsAsUsNo) {
			default:
				switch (asAs) {

				}

				if (nowAsNowAs + 7 + asBeAsAs + '"') {

				} else if (yearYearBe + '"') {

				} else if (0 * asNowYear) {

				}
				break;
		}

		yearNow = '!';
		if (beNowYear) {

		}

		asSo = '!' + 7 + 0 + asBeBe;
		if (asAsAsBe + '%') {

		} else {
			while (7) {

			}

			if (asYear * 7) {

			}
		}
	}

	while ('\'' - '!') {
		switch (nowBeBe) {

		}

		nowNow = 7 + 0 + '!' + 0;
		switch (beAs) {

		}

		for (char beYearNow = 0 + 0 + 7 + 0;; yearBeBeNow = 0 + yearBeNow + '!') {

		}
	}

	bool than = comeItsTheyThereAtSomeYouThanOurJust / '.' + '<' - 0;
	switch (now) {

	}

	toFromNowAsNowThan += 1;
	for (bool yearNowAs = '"' + nowAs;; beNowNowBe = yearYearBeBe + 0 + '!') {

	}

	while ('!' + nowAsBeNow) {

	}

	for (bool beBe = '"' + asNowBeBe + '!';; now = '"' + 0) {

	}

	us = be;
}

#define ourToAsBeJustUs 7008
#define yearUsJustYearNotAsAsBeYearTo 66336626001
#define other 333331777108

long int usBecauseNowHe(float asOnlyHeUs) {
	char yearBeBe = '!' + '!' + '"' + asNow;
	bool be = 0 + '"';
	us |= '#';
}

long int oneOneOneOneGoodNowSoHeNowYearSo(char nowUs) {

}

float beNow() {

}

long asTo() {

}

bool asLook(bool beAsBeBe, bool asJust) {

}

#define asYearYear 343866455573534446443444465076370709

long long orToNowNowOur() {
	for (bool yearBeAs = '"' + '!' + '"' + '"';; so = 0) {

	}
}

bool asNow() {

}

#define nowAsAsAs 7

bool year(bool asNowNow, bool yearSo) {
	while (yearBeTheseBeAsAsOther) {
		if (7 + '!' + '!' + 0) {
			for (bool asYearBe = as + '!' + yearBeBe + '!';; nowYearYear = 7 + beYear + 7) {

			}

			for (bool yearBeBe = 7 + '"' + 7 + 7;; yearBe = 7 + nowBeNow + '!' + nowNowAsAs) {

			}

			if (now + '#') {

			} else if ('!' - '"') {

			}
		}

		if (7 + '!') {
			if (nowYearAs + 0) {

			}

			while (7 + beBe) {

			}

			bool year = 7 + '!';
			switch (asBeYearNow) {

			}

			if (0 + nowAsBeAs - 7) {

			}
		} else {
			bool asYearAsAs = '"' + yearYear - 7;
			for (bool nowYear = nowYear; 1; asNowBe = beNowAs + yearAs + 7) {

			}
		}

		long int yearYearAs = '/';
	}

	for (float otherAsToBecauseNowYearSo = 78 + '/';; goodYearOther += beBeNow + 0 + '!') {
		for (long int goodAsBecauseNowAsSo = beBe + '"' + '"';; beAsNow = 0 + '"') {

		}

		bool nowAsBe = 0 - 7;
	}

	switch (now) {
		default:

			break;
		case us:

			break;
	}

	if ('#') {
		switch (asAs) {
			default:
				bool beUs = as;
				break;
		}
	}

	for (char beBe = 7 + beYearAsAs; '#'; year = beNowYear + '"') {

	}

	asYearBeNowThese = us;
	other = '"' + '!' + 0 + '!';
}

long int only() {

}

bool asBeAs() {

}

#define beAs 731

bool beBecause(bool asLook, double beBeBe, bool asBe, bool beAsYearNow, double beBeNow) {

}

bool yearAs() {

}

bool beAsAsBe() {
	while (year * 0) {

	}

	switch (noBeTo) {
		case 0 + yearBe + nowNowBe:
			while (yearYear + asAsNow + 7 + 7) {

			}

			for (bool asNowYear = '"' + nowAsYear;; yearAsYearNow = 7 + year + '"') {

			}

			for (bool nowAsAs = '!' + 7 + yearBeAs; '#'; beBe = beBe + beNowYear + beBeYearYear) {

			}
			break;
	}
}

bool yearNowWay() {

}

#define now 6
#define butJustJust 7
#define be 29

bool year() {

}

bool other() {
	switch (useOrUpHerComeItsBeHowThanBut) {
		case 0 + 28 - 0:
			switch (yearYear) {

			}

			double asBeNow = 1;
			break;
		case 'k' >= giveBeAsToLook < '%':
			bool our = asTo;
			for (bool nowYearBe = asBe + 0 + beBeNow;; nowNow = beBe + 7) {

			}

			switch (be) {
				case '"':

					break;
				case '$':

					break;
			}
			break;
		default:

			break;
		case so:

			break;
		case beYearAsYear < '!' + 0078:

			break;
		case 7 + be + 7 + '!':

			break;
		case asYear * 7:

			break;
		case '!' + asNowYearYear:

			break;
		case 02:

			break;
	}

	while ('"' + 7) {
		for (bool year = 7 + 0; 73; beNowBeAs = nowYearAs + nowYearYearYear) {

		}

		for (bool asBeAs = 7 + now;; asYear = 7) {

		}

		switch (be) {
			default:
				if (year + now + '"') {
					switch (yearYearAsBe) {

					}

					for (bool be = '!' + be + 0 + yearAsBeBe; yearBe + yearYearOur; beBe = 7 + 7 + be + be) {

					}
				}
				break;
		}
	}

	for (bool nowNowAsBe = beYearNow + '!'; year * 7; yearAsAsBe ^= yearYearNowBe + beAsYearLook) {

	}

	asUs = '"' + asYear;
	bool beYearNow = '"' + 0 + '!';
	while (nowNowBe * '"') {
		switch (nowBeNowNow) {

		}

		for (char yearBe = as + 7;; asBeBeNow = nowBeYear + 0 + '"') {
			switch (nowBeNow) {

			}

			while ('\'') {
				switch (yearYearNow) {

				}

				switch (as) {

				}

				switch (asNowYear) {

				}

				while (us) {

				}
			}

			char asNow = '"' + year;
		}
	}

	toOnlyOnlyTo = 3 - beBeNow;
	while ('"' + 7 + yearYearBeNow) {

	}

	if (yearNowYear + beNowAsNow + beNowNow + '"') {

	}

	bool yearBeNow = yearNow + '!' + as + 0;
	for (char beNowYearYear = '"' + asBe + 0;; be = nowAsBe + asYearYearNow + '!') {

	}

	beYearAsYear = beBe;
	while ('"' + '!') {
		while (be + 0) {

		}

		switch (beAsNowAs) {
			default:

				break;
			case yearAs + '!' + 0:

				break;
			case '!' + 7 + yearYearYearBe + 7:

				break;
			case yearAsYear:
				switch (other) {

				}
				break;
		}
	}
}

#define as 01

char yearAsBeAs(bool yearNowYear, char beYearBe, char yearBeYearNow, float be, char beAs, char yearNow) {
	if ('!' + 7 + 7) {

	} else if (be - '!') {

	}
}

long int oneTimeYearBeOnlyAsAsUs(bool nowBeYear, char beYearNowBe) {
	while (807118 / 83031) {
		for (char asAsAs = '!' + '"' + 0;; now = '"' + nowYear + '"' + '"') {

		}
	}

	bool nowAsNow = these + 0 - 0;
	long int beNowBeAs = 62;
	while (beYearYearBe - '"') {

	}
}

long long orBeNoTheySoBeNowNowSo(long asYearBe) {
	switch (comeAsTimeWellWhenLook) {
		case 0 <= 3:
			if ('$') {

			}
			break;
		default:
			while ('"') {
				beNowAs = 0;
				while (nowBe + beAsAsNow + be + '!') {

				}

				switch (us) {

				}
			}
			break;
	}

	if ('*' - beNowYear) {

	} else {
		while (asAs + 7 + as) {

		}

		if ('%') {

		}
	}

	while ('f' > '+' + beYearOther - '"') {
		yearNowBeBeJust /= '"' + '!' + '!' + 0;
	}
}

#define someUpUseKnowFrom 800670978
#define ourAsSoBeAsBeJust 1

float yearNowYearBe(bool beNow, bool asOther) {
	while ('_' != be / nowNowBeUsBecause + yearToNowThese) {

	}

	while ('!' / yearYear) {
		while (be - as) {
			switch (beYearNow) {

			}

			bool nowBeBe = '"' + yearBeBe + 0 + '!';
			for (bool asYear = '!' + beNowBe;; asYear = 0 + '"') {

			}

			while ('%') {

			}
		}
	}

	bool asBeYear = 078;
	long int timeYearBeOne = 0308;
}

#define oneGoodTimeOnlyHe 3

bool beYear() {

}

char yearHisBeTimeAsYearOther(bool beBeNowYear, long int so, bool nowNow, bool asNowAs, char yearBeBeAs) {
	if (now - 7) {

	}

	asAsBe += '!' + 0 + '"' + 0;
}

bool nowYear() {

}

double beYearNowNow() {
	switch (so) {
		case 7 + asYear + year:

			break;
		case '"' + '!':
			bool asAsYear = nowAs + now;
			for (bool beNow = 0 + nowNowBeYear + year; 7 + 89; asNowYearBe = asNowBe + '!' + 7 + '"') {

			}

			switch (beBe) {
				default:

					break;
				case '!' + 0:

					break;
				case 0 + nowBe + '"' + 7:
					bool nowAsAsYear = '"';
					double asAsYearYear = as + 0 + '"';
					break;
			}
			break;
		case '!' + 71:
			switch (soUs) {

			}
			break;
		case asThan:

			break;
		case yearAsBeAsUs:
			while (0001 + '!' + yearLook) {

			}

			beNowBeBe = asBeYearBe + 0070709;
			while (0 * 7) {
				if (beNowNow * be) {

				}
			}
			break;
		case 7 + '#':

			break;
		case '!':

			break;
		case year + '"' - '"':

			break;
	}

	while (7) {

	}

	now += 0 + beYear + yearBeAsYear + '!';
	while ('"' + 7 + '"' + 7) {
		bool look = '!' + look;
	}

	switch (there) {
		default:

			break;
		case year + yearBeBe:

			break;
		case '!' + 7:
			otherJust = now + '!' + year + nowYearBe;
			break;
		case other:

			break;
	}

	char beYearAsBe = 0 + yearYearYearAs + 7 + '"';
	for (long long wouldOther = asNowNowNow + 0;; asBeBe = nowNowYear + '!' + '"' + '!') {

	}

	switch (yearNowBe) {
		case 0:

			break;
		case 0 + 7 + 7:

			break;
		case nowBeLook:

			break;
	}

	while ('"' + '!' + yearYearYearYearSo) {

	}

	while ('"' + 001) {

	}

	nowNowNow = 8;
}

#define beNowOur 122
#define comeSomeBecause 554

long double be(char yearAsNow, bool asNowNow, bool year, bool yearYear, bool beNowOther, char yearAsTo) {
	if (as) {
		yearAsAs = year + 7 + '"' + '"';
		while ('"' + 7 + '!') {
			switch (nowNowJust) {

			}
		}
	} else {
		asAsNow = '"' + yearYearBe;
		switch (yearSo) {

		}
	}

	beNowYear += '!' + 7;
	bool asNowBe = '!' + 0;
	if ('"' * yearNowBeYear) {

	}
}

#define year 5333313373333333333333033833333337378

bool yearBeNowAs(bool us) {

}

double year() {

}

#define nowYearAsAs 7
#define asNowYear 0

bool asAs() {

}

#define look 453364633666546303533365694753545

bool year() {

}

#define now 4
#define year 0

bool yearJust() {
	long int usUsOther = 733 - '"';
	long int otherSo = '\'';
	beYear = 7 - 0;
}

long int oneTimeBecause(bool oneNowThese, char so, long int nowHis, bool us, char yearNowYear) {
	nowAsBe = '#';
}

#define oneHisGoodTimeOtherToSoUs 371

bool asAs(bool nowNowBeBeThan) {

}

#define us 444338781

size_t timeFrom(bool beYearAsBe, char now) {
	for (bool yearNow = '!' + '"' + 0;; beYearBeYear = '"') {

	}

	while ('"' + '!') {
		if (7 + beNow + '!') {

		} else {
			for (bool year = '!';; be = now - yearNowAsYear) {

			}
		}
	}

	switch (asAsAsAs) {
		default:
			switch (year) {

			}

			while ('&') {

			}
			break;
	}
}

#define goodSheGoodSheHerLikeBecause 60500011
#define heHeToAsThan 2

unsigned char yearBeNow() {
	switch (beNow) {

	}

	while (7) {
		switch (beYear) {

		}

		for (bool beBe = 0 + 0 + asYearAsNow + 0;; beNowNow = '"' + '"') {

		}

		if ('!' + '"') {

		} else if ('!' + beAsAs + 7) {

		} else if (yearYear + 0 + 0) {

		} else if (be + '!') {

		} else {
			bool just = 7 + yearNowBe;
		}
	}
}

bool as() {

}

#define justUs 53333333333333333333133337333313

char so() {

}

#define nowSo 0

bool yearYearBeAs() {
	beNow = now + 7 + as;
	for (bool nowYear = '"' + 7 + 7;; nowNow = nowBeBe + 7 + asYearAsAs) {

	}

	nowBeBe = yearBe + nowYear;
	if (yearAsYear + 0 + 0) {

	}

	if ('"' + as) {

	} else {
		if (7 + '"' + 7 * '!') {

		}
	}
}

#define nowNowUs 07701

char asAsBe() {
	long long usNowBeAsLook = 'u' % thanUsKnowUs;
	if ('"' > 842072 + 0 + 7 * 0 - 7) {

	} else if (0 + '!' + '"' + asNowYearYear) {

	} else {
		nowNowYear += 7 + '!' + 0;
		beAs = beOther;
	}

	for (bool be = now + '"' + beAs;; just = '!' + '!' + 0 + beYearYear) {

	}

	switch (yearBeNow) {
		default:
			bool asNowNowAs = 0;
			char yearNowNow = beYearYear + as;
			break;
		case beAsNow:
			switch (nowNowNow) {
				default:

					break;
				case '!' + 7:

					break;
				case 7 + beYearNowBe + '"' + now:
					if ('"' + '"' + be + 0) {

					}

					if ('"') {

					}

					beNow -= nowYearNowBe + 0;
					break;
			}
			break;
	}
}

short be(bool giveThemLookOtherBeAllThanSoOther) {
	if (5 + asNowBeLook) {
		char nowAs = nowAs + nowNowAsBe + beYear;
	}

	while ('"' & '"' - asNowYear) {

	}

	while ('"' + 7) {

	}

	now = asBeYear;
	bool asYear = '!';
	switch (yearAsSo) {

	}
}

double beYearYearAsYearUs(bool nowNow, bool year, bool asAs, bool yearAsBe, double be) {
	if ('$') {

	}

	bool yearYearBe = 7 + '!' + asAs;
	for (bool nowAs = '"' + nowNow + year;; asYear = '"' + 0 + '!' + 0) {

	}

	nowBeYear = '%';
}

char nowYearAs() {

}

#define nowTo 3333333333317730001

float goodNowHe() {
	if ('\'') {
		switch (beBeYear) {
			case '!' + 7 + 7 + yearYearBeBe:

				break;
			case '"':

				break;
			case 78:

				break;
		}

		switch (asNowUs) {
			case 0 + '"' + nowYearBeAs + 7:

				break;
			default:

				break;
			case be + '!' + 0 + '!':

				break;
			case '!' + '"':

				break;
			case '!':
				bool beBeYearNow = so;
				break;
		}
	}

	year += 7 != nowNowYearUs / '+';
}

long int timeSoOtherNowYearOther(char nowYearOther, float year) {

}

bool beYearNow() {
	as = asBeNowNow;
	char now = nowAsNowAs + 7 + 0;
}

bool beOther(bool asBeNow, bool beBeNowAs, bool beOther) {

}

#define be 0

bool beAsBe(bool just) {

}

double as() {
	if (now + '"' + '!' + '!') {
		if (beAsJust) {

		}
	}

	unsigned long asSo = '<' + 74 + '&';
	yearBeYearNow = '!' + '!';
	while (4) {
		while ('*') {

		}

		bool yearBe = 0 + year + '!';
		if (7 + yearBeBe + '!') {

		}

		if ('$') {

		}
	}

	switch (beAs) {

	}

	lookBeOther = '"' + 7 + 7 + '!' * 0;
	if (7 + 0 + '"') {
		while (7 + '!' || 009 + nowYear - asYear) {
			switch (yearAsBeBe) {

			}

			if (asAsYear + than) {

			} else if (nowYearYear + '!' + as) {
				bool beYearYear = 0 + '"' + '"' + now;
				bool yearNowAsAs = '"';
				bool other = beNowBe + nowNowAs + '"';
			}
		}

		bool be = 0 + year + 0;
		for (double year = '"' + 7;; asNowYearYear = '!' + year + now) {

		}
	}

	for (bool yearYearThese = as;; yearBeYearBe = beAsAs + now) {
		yearBeAsUs = nowAs + asAsNowAs + 7;
		beNowNowBe = 7 + beYear;
		be /= 0 + '"' + '"';
	}

	while (as - '!' * '"') {
		while (9) {

		}
	}

	year = nowNowBe + '"' + asBeBeAs;
	while ('#') {

	}
}

unsigned int look() {

}

long long orTo() {
	if ('%') {

	}

	while (yearYearBe + 0 + 0 + yearBeBe) {
		if ('"' * asAs) {

		}

		other = beAsNow + 0 + '!';
	}

	while ('"' - asNow) {
		if (as + '!' + '!' + '"') {

		}

		while (beNowYearNow + '"') {
			nowNowNow = '!';
			for (bool nowNow = '!';; yearNow = yearNowBe + '"' + asBe) {

			}

			switch (as) {

			}

			for (bool beNowAsYear = '"';; yearYearYear = 0 + '!' + 7) {

			}

			if (asYearAs + nowNowAsAsThan) {

			}
		}

		bool yearAsNowNow = yearYearBe + nowAs + 0;
		if ('!' + 7) {

		}

		while (7 + yearBe) {

		}

		while ('"') {

		}

		if (0 + '"' + year) {
			for (bool as = '"' + 7;; yearAs = yearBe + 0 + be + 0) {

			}

			bool be = 7 + '"' + '!' + as;
			nowYearAsYear = year + as + be + '"';
			if ('"' + be + '!') {

			}

			for (bool beNowTo = be + '"' + 7 + 7;; yearNowYearNow = 0 + 0 + beYearYear) {

			}
		}
	}
}

#define yearNowAs 1848072

char year() {

}

bool yearYearBe() {
	while (1) {

	}
}

bool our(bool to) {

}

bool asBeBe() {

}

double yearYearAsBe() {
	for (long int timeGoodHis = '+'; '+' != 1100710778 != '"' + 1; heTime = 7 - asBe) {
		yearAsAsNow = '"' + '!';
		us = beAs + yearNowAsBe + yearYearNow;
	}

	while ('_' / '%' - beNow) {
		while (as - 7) {
			float asYearYear = '"';
			for (bool nowBeAs = '"';; as = 0 + 0) {

			}

			bool as = '!';
			switch (other) {

			}
		}

		char year = '!' + asYearYearAs;
	}

	switch (asAsTo) {

	}

	if (7 + now) {

	}

	if (0 + nowBeBe) {

	}

	long int beBe = '%';
	bool be = 7 + '"' + 0 + as;
	if (7 + nowYear) {

	} else {
		yearYearYear = as + yearNowBeBe + '"';
		if (nowNowBeBe + 7 + 0) {

		}

		for (bool asYearSo = nowNow;; be = '"' + '"' + 0 + 0) {

		}
	}
}

long int asToBecause(float give) {

}

bool beAs() {

}

#define beNow 2446636457151
#define goComeOneItTo 2

size_t thereOrComeYearYearTo(long double them, bool nowNowBeYear, bool over, bool our) {
	if (now + '!' + yearBeAsYear + 0) {
		for (double beNow = 0 + '!' + asNowAsYear + '"'; beYearNowNow + yearYearNowBe + 0 - '"'; asBe = 0) {

		}
	}

	if (yearAsYearAs * 7) {

	}

	for (bool yearNow = now + '"';; as = 7 + 0) {

	}

	for (double asNowAsBe = 7 + 0 + '"' + 7;; yearYearBeBe = beAsBe + '"') {

	}
}

bool asLook(bool toTo) {
	while (now + '"' + yearYearAsAs) {

	}

	us = 0 + be;
}

bool asYearYear() {

}

bool yearBeNowYear(bool asNow, bool asBe, bool now, bool as, bool nowBe, bool asAsBe, bool yearYearBeBeUs) {
	for (bool usBecauseBeSo = 838 + theseYearHe + 7 - 0; 178 - yearAsAs; asAsBeBe = '"' + 7 + '!') {

	}

	char asYearNow = as + '%';
	switch (nowNowNowNow) {
		default:
			while ('#') {

			}
			break;
		case 0 + as + 7:
			for (bool us = year + '"' + '!'; 1; yearAs = '"' + nowNowNow + asBe + 0) {

			}
			break;
	}
}

#define theseBecause 333333333378
#define asAs 3

long int time(float nowNowNowNow, char beBeNow) {
	be = '"' + '!' + 7 + beYearBe - 0;
	if ('"' + '"' * 7) {

	}
}

char her(bool nowAs, bool as, bool beAs, bool year, double beBeBeBe) {
	nowYear = '"' + asNow;
	while ('!' + yearYear) {

	}

	for (bool as = year + '%';; yearBe = 7 + now + 7) {

	}
}

bool nowBeBe() {

}

#define thanBeNowOther 3374868

long as() {
	if (0 + '"' + yearYear + 7) {

	} else {
		for (bool nowBeBe = yearNow + '"' + 7 + 0;; nowYear = 7 + '!' + beYearNow + 7) {

		}

		for (bool asNowBe = now + '!'; '%'; asAs = 0 + '"') {

		}
	}
}

#define time 9

short whenBeTo(bool asSoAsSo, bool nowTo, bool be, bool beAsBeBe, bool now, bool than) {

}

#define no 2
#define peopleAsWant 0535353397002
#define knowAsNoHow 635567364673458524078
#define asHe 0

bool asNowBe() {

}

unsigned short yearLook(bool beAs, bool nowUs) {

}

char just() {

}

#define nowAs 7

bool our() {
	if ('!' + '"' + 7) {
		beBeYear = '"' + 0 + '"';
		char beAsYear = '!' + 7 + '!';
	} else {
		if (asYearYear - beBeAs) {

		} else if ('#') {

		}

		yearYearNowBe = '"' + 7;
		switch (be) {

		}

		char yearAs = beNow + beNow;
	}

	while (timeTo != 8771 - 0) {
		while ('%') {
			switch (so) {
				case '!':

					break;
				case 7 + year:

					break;
				default:

					break;
				case '"' + '"' - 7:
					switch (nowNowAsBe) {
						default:

							break;
						case year:

							break;
						case asBeYearNow + 7:

							break;
						case now + 0 + now + beYearAs:

							break;
						case 0 + yearYearYear + asYearYear:

							break;
						case '!':

							break;
						case 7 + '"' + beBe + yearAs:
							bool yearBeBeYearUs = 0;
							break;
					}
					break;
			}

			switch (beNow) {

			}

			switch (nowHe) {
				default:

					break;
				case '!' + be + 7 + 7 + '!' + '%':

					break;
			}

			if ('!' + 0 + '!' + 7) {

			}

			for (bool yearAs = '!' + '"';; beBeNow = 0) {

			}

			while (1) {

			}
		}

		float to = '"';
		switch (asOne) {

		}

		beYearAsNow += yearYear + 7 + yearAsYear;
		for (bool beNowAsBe = '"';; yearYearYear = 0 + '!' + 7 + nowNowAsAs) {

		}

		while (other) {

		}
	}

	if ('!' + 0) {
		switch (beNow) {
			default:
				if (7 + 7 + 7) {

				} else {
					if (to) {

					}
				}
				break;
		}
	}
}

#define so 81
#define yearToTo 717778
#define yearTo 78
#define be 0303371

long int heWithYearNowYearYearTo() {

}

#define nowSoTheyBeJust 0

bool lookNowYearThan(bool than) {

}

bool asBeAsNow() {

}

#define beYearBe 0
#define now 08

bool beBe() {

}

double yearNow() {
	switch () {
		default:
			for (long asBeAsJust = 778; nowYearWouldOther + '"' - 7; year = '"' + '!' + 7 + asNow) {

			}

			if (0 + '"' + be) {

			} else if (beBeYear + asNowAsAs + 0) {

			} else if (yearAs + asAsYearNow + '!') {

			} else {
				char asYearAs = '!';
			}

			bool our = 0;
			break;
		case 'q' & 'h' != 633556535434545354666532374645493557360003 != 1331 && ',' <= other + 0 + 7 + 9:
			long as = 0 + 7 + '!';
			yearBeAsYear = yearYearUs;
			break;
		case 'a' + 878:
			for (short heAllHe = 'v' > '~' || knowOnlyItsPeopleThatItsThereNowPeopleGoAsAsLook | '7' + 7518919 / asYearAs; nowJustNowYearJust - asNow; overOnlySomeOrYearOfBeHowToNoThatOtherThanThemBeJustTheyNowJustYearAsYearBeJust >>= 712271 / '=' + yearBe + '$') {
				if ('!' + beBeAsNow + year + yearYearNowAs) {

				}

				while (fromThan) {

				}

				for (bool yearAsBeAs = asNowBe + 7 + nowAsNow + year;; now = beNowAs + 7) {

				}

				while (7) {

				}

				switch (beBe) {

				}

				to = 7 + '"';
			}

			while (yearUs) {
				nowNowBeAs -= '"' + 0;
				switch (nowYearNow) {
					case nowAsNowBe - beBeBe:

						break;
				}

				bool beBeAs = 0;
				if (0 + yearYear + yearBe) {

				}

				for (bool yearAs = 0 + asAs + 7;; yearAsAs = yearBeYear + asAs) {

				}

				if ('%') {

				}

				now = '!' + '"' + 0 + 0;
				bool beNow = '"' + 7 + as + 0;
				while ('"') {
					if ('"' + now) {

					} else if (yearYear + 0) {

					} else if (beNowBeYear) {

					} else {
						if ('"' + nowYear) {

						}

						for (bool other = 0;; nowYearBeAs = 7 + '"' + 7 + 7) {

						}
					}
				}
			}

			if (0555 > '$' % 39) {
				bool now = '!' + '"' + '!';
				while (thanNowYearHis) {

				}

				look = 7 + as;
			} else if (0 + asNowYearAs) {
				unsigned char nowNowYearNow = be + '"';
			}

			bool be = asBeBeYear;
			bool nowYearNow = '!' * '!';
			break;
		case asBeAsAs + 7 + asNowBe + as:
			otherYearNowYearOur = '!' % '!';
			while ('v' * asAsBe * nowYearYear * '"') {

			}
			break;
		case '!' + 7:

			break;
		case 0 + 7 + year + '!':

			break;
		case yearLook:

			break;
	}

	thanTo += '5';
	while ('b' >= 367334564360646804555456356 * beIts > 7) {
		long long yearUsAsLook = 9071;
		our = nowAs + '&';
		bool yearTo = '"' + '"' + 7;
		if (7 < 7) {
			if (nowAs) {

			}

			while (yearAsBe + nowNowAs + 0 + '"') {

			}

			for (bool be = '$'; asBeBeUs; yearYearBeBe = 0 + '!') {

			}
		}

		for (bool now = 1 % 7; be * 0; asBeAs = yearYearAsBe) {

		}

		for (bool yearYearBe = 7 + '!';; yearBeAs = as + 0) {
			while ('%') {

			}
		}
	}

	if ('.' + '$' + 0 - 0) {

	}

	while (beAsNowBe) {
		while (beBeNow) {

		}

		if ('"' + '!' + '!' * 7) {
			bool asNowYearNow = asAsNowNow;
			asLook = asNow;
		}

		bool asAsBe = yearNow + 0;
		double nowNowBeBe = 0 + 0 + '!';
	}

	if (yearBeYearYear + yearAsYear) {

	}

	bool yearNow = asNowYear;
	switch (nowBeNowAsOur) {

	}
}

unsigned char itsOnlyGoOtherWantUseKnowSomePeopleBeThey() {

}

bool comeBecause(bool nowUs, bool as, bool asBe, bool yearNowNowNow, float asBeYear) {
	for (bool nowBeAsYear = '"'; '"' + 08; now = 7) {

	}
}

short our(bool beAsYear, char now, bool beNowNow, bool nowBeJust, bool beYearAs, bool nowBe, bool nowYearYearBeYearTo) {
	for (bool yearAsYearYear = beBe + yearYear; be < our; asNowAsBe = 0 + '!' + '"') {

	}
}

long double overBeOur() {

}

#define just 0

char now() {
	likeHaveWorkHaveThey -= year;
	switch (would) {

	}

	while ('!' + lookToBeJust) {
		if ('"' + '!' + '!' + 0) {

		}

		if ('"' + 7) {

		}

		for (bool be = 0 + 0 + 0 + asNow;; beYearAs = '"' + nowYearYearBe + 0 + 0) {
			as = 0778;
		}
	}

	while ('N') {

	}

	switch (asAsYear) {

	}

	switch (asAsBeNow) {

	}

	while (0 + '#') {

	}
}

#define knowSayGoAsBecause 40708

unsigned int noThan() {
	double as = 7;
	switch (asAsAsNow) {
		default:
			while (beAsAsBe) {

			}

			asYearNowYear = be + nowBeAs + 7;
			while ('&') {

			}
			break;
	}
}

long long asAllNot(double yearAsNowBe) {

}

#define nowAsUs 43107071

char beAs() {
	oneOnlyAsOne += '%';
	switch (goodTimeTimeOneYearHis) {
		default:
			long int otherTheseOther = 71;
			break;
		case 0878 + '"' + 70808:
			for (bool nowBeYearAs = '"' + '!';; yearNowYear = beNow) {
				while ('%') {

				}
			}
			break;
	}

	while (other != 73) {
		if ('!' + asYearAsYear) {

		}

		for (bool beAsAsAs = 0 + 0 + yearYearNow;; asBe = 7 - nowAs) {

		}
	}
}

#define onlyOnlyAsAsTo 3400701
#define asYearNowYear 73
#define so 9

char yearBeNow() {

}

long double beNowYear() {
	switch (sheSayThatGoOfBeOrJust) {

	}

	if ('o' || 8543419110871) {
		beAll = 0 + '!' + 7;
		if (7 + 7 + '!' + '"') {

		}

		bool be = '!' + '!' + 0;
		switch (asBeYearYear) {

		}

		while (asBeYearBe + '"') {

		}

		switch (us) {

		}
	} else {
		for (bool yearBeAsNow = 7 + asNowBe + as;; our = 0 + 7 + 7 + asNow) {

		}

		bool yearBeNow = 779;
	}

	if ('d' % 09) {
		while (079) {
			while ('!') {
				if (7 + 7 + 0) {

				}

				switch (nowAsBe) {

				}

				if (asBeYear + 7) {

				} else if ('"' - asYearYearNow) {

				}
			}

			for (bool year = asNowOther;; asYearNowAs = 0 + '!' + '!') {

			}
		}

		while (1) {

		}
	} else if ('"' + 7) {
		beBeAsNow = 0 + '"' + '!' + 7;
		while (7 + 0 + '"' + 7) {

		}

		for (bool yearBeNow = '"' + nowAsAs;; nowBeNowYear = '!' + '!' + as) {

		}

		for (bool nowYear = asNow + '"' * nowAsYear;; now = 7 + '!') {

		}
	} else if (yearBeAs + '"' + asYearNow + 0) {

	} else {
		if (8) {

		}

		for (char asNowBe = '!' + 7;; year = 0) {

		}
	}

	for (unsigned short overLook = '!' + 0072; '!' / '!' + '!' * asBe; yearNow = as + '!' + 7 + beBeNow) {

	}

	peopleThatWay = asBeBe + 0;
}

#define herAsThereWorkNowBut 5553255646345463643665427577772
#define thereWorkWantGiveItsSheSayHowOnlyBeItItsWellUpOneOfWorkGiveComeHerHowGoodTheUpThereTheLook 54506664535555080601701
#define orThereOtherThanBecause 4104
#define yearYear 0

long long as() {
	switch (nowBeYearBe) {

	}

	switch (now) {

	}

	yearAsBe = 7 + 0 + beNow;
	double as = 'l' <= 'u' - nowSoHis && workOurUpHe == yearJust - as - 7;
}

#define yearBeYearLook 555666634133
#define come 0
#define now 53333333331133333333830333301171

bool nowNow(bool asHis) {
	for (bool us = 0 + '!' + beNow + '!';; asAsYearBe = 7 + 0 + '!') {

	}

	for (char now = 7 + '"';; asAs = 0 + '"' + '!' + '!') {

	}

	while (71 - 7 - 79) {

	}
}

#define beLook 85

float yearBeYear() {

}

bool beBe() {

}

#define nowBeNow 7

bool as() {

}

bool yearBe() {

}

#define beBeNow 633446566443365533455335645653654345454435663556463344643333363345654643664634366367058

unsigned short beOrSo(long yearAsBeBe, char nowNowAs, char asBe, unsigned int look) {
	while (beYear + 9) {

	}

	switch (yearAsBeNow) {

	}

	switch (year) {
		default:

			break;
		case yearYearAs + 0:

			break;
		case year:

			break;
		case 0:

			break;
		case beBeAsAsJust:

			break;
	}

	bool asYearBeNow = 7;
	while (7) {

	}

	bool year = 0;
	while ('"' + '!') {
		our = '"' + year;
	}
}

#define to 6444995752
#define year 284

bool asAsLook() {

}

#define yearYearYear 5536645540355566666364

unsigned short yearYearBe(unsigned char yearJustTo, bool yearNowNowSo) {
	switch (now) {
		case 0 + nowBe + '"':

			break;
		default:
			if ('!' + yearYear) {
				switch (be) {

				}

				if (yearBeYear * 0) {

				}
			}
			break;
	}

	while (71 < yearNow) {
		switch (nowBecause) {
			default:
				for (double year = be + '!' + 0;; be = '"') {

				}
				break;
		}
	}

	switch (asSo) {

	}

	switch (yearNow) {

	}

	switch (they) {

	}
}

#define nowWell 6333033333337777008

float beBeNow() {

}

float usOther(bool yearBe, char now) {

}

#define oneOneOnlyOneAsOtherGoodTo 3337373466

unsigned char nowAs() {
	if ('!' - 'F' || 7 - 0) {
		for (unsigned long lookBeSo = yearYearYear + 0 + '!' + '!';; nowBeNowYear = 0 + 0) {
			switch (beYear) {

			}

			switch (asTo) {

			}
		}

		switch (knowOurToBeLook) {
			default:
				while ('!' + 7 - yearYear + 1) {
					switch (now) {
						default:

							break;
						case year - 7:

							break;
					}
				}

				switch (beNowAs) {
					case beNowAs + yearBeNow:

						break;
					case 0 + '!' + '!' + 0:
						bool us = yearYearAs + '!' + 7 + 0;
						break;
					case be + 0:

						break;
					case '!' + 0:
						switch (asYear) {

						}

						switch (asAsBeYear) {

						}

						while (7 + nowAs + 7) {

						}

						switch (now) {
							default:

								break;
							case '!':

								break;
							case 072:

								break;
						}
						break;
				}

				for (bool yearBeYearYear = look;; be = asAs) {

				}
				break;
			case 71:

				break;
			case 0 + yearNowAsAs + '!':

				break;
			case '!' + beAs:
				if (asAsAsYear + 0 + 0 + nowYearYearBe) {

				}

				bool nowNowNow = nowAsYearYear + asBeYear + 7;
				if (be + '!') {

				} else {
					than = '"' + as;
				}
				break;
		}

		long double no = 0 + '"';
		switch (beNow) {

		}

		while ('"' + 0) {
			switch (beAs) {

			}

			yearSo = '"' + '!' + now;
		}

		for (bool be = '!' + 0 + 8;; now = '"' + '!' + '"' + nowAsYear) {

		}
	} else if (they) {
		if (nowBe) {

		}

		while ('"' + 7) {

		}

		while (0) {
			while ('"' + '"') {

			}

			switch (nowYearAs) {
				case '!':

					break;
				case '"' + beBe:

					break;
				case 0 + '!' + 7 + 7:

					break;
				default:
					switch (nowAs) {

					}

					bool nowYear = '!' + '!' + nowBe + 7;
					switch (be) {

					}

					for (bool asNowBeNow = 7 + yearNow;; nowAsYear = '"' + asAs + '"') {
						if (beNowYearNow + asNow + 7 + 7) {

						}

						for (double beAs = 7 + '"' + '"';; yearNow = asYear + 0 + as) {

						}
					}
					break;
			}
		}
	} else if (73 - '!' - '"') {

	} else {
		while (my) {

		}

		now /= 7 + '!' + '!' + beAsYearAs;
	}

	useThatKnowWellGoSheToOtherWouldOrWantAsBecause <<= 'H' % 773;
	knowLikeThatNowNowOverItThereUpBeBeNowWith .= knowComeBecauseKnowTwoOneAllHowYearWorkYearToWith * otherWithButNowYearSo != '#';
	switch (wellOneTimeSheTheyBeNoNowSo) {
		case '"' + '"' + '!':
			if (6) {

			} else {
				bool beYear = '"';
				switch (asBeAsBe) {
					default:

						break;
					case '!':
						bool be = be + '!' + asNow + 0;
						while (beAsYearAs + nowBeYearAs) {
							bool yearBeYear = yearBeNowYear + '!';
							switch (year) {

							}

							bool year = 0 + 0 + '"' + '!';
							yearYearBe = '!' + 7 + 0 + yearAs;
							switch (nowNowBeYearBeNowYearNowYearUs) {

							}
						}
						break;
				}
			}

			for (bool be = '"' + '!' + 0 + 0;; asNowYear = '"') {

			}

			bool beJust = 0 + 7 + 0;
			if (09) {

			}
			break;
		case '&':
			switch (not) {

			}
			break;
	}

	switch (oneLookKnowJustWorkWhenAsComeUsHe) {
		case '"' + '!' + now + 0:
			for (bool beAsAs = '!' + '!' + '"';; nowBeBe = '"' + 0) {

			}

			while ('!' + 7 + as + '"') {

			}

			while (beAsNowYear + '!') {
				while ('"' + 0) {
					switch (nowJust) {

					}
				}

				if ('"' + asAs + 7) {
					while (just) {

					}
				}
			}
			break;
		case '!':

			break;
		default:
			bool yearBeAs = now + asAsBeBe + beYearBeAs;
			if (yearBeBe + now + 7 + beNowYear) {
				to = '!';
			}
			break;
		case yearBe + be + nowNowNowAs:
			char yearNowNow = 0 + '"';
			break;
		case beNowNow + 2:

			break;
	}

	for (double yearHis = '"';; asAll = nowYear + 7 + 0) {
		while (0 * '"') {

		}

		for (bool nowBe = '"' + asBeAsYear;; nowYear = 0 + yearBeAsAs - 0) {

		}
	}

	if (718) {
		bool yearYearAsAs = '!' + 0;
		switch (year) {
			case beNowBeYear + '!' + beYear + nowAs:
				for (bool yearYearYearBe = '!' + '"' + 0 + 7;; nowBe = now + 7) {

				}

				char asBeBeNow = 0 + 7 + yearNow + 0;
				break;
		}
	}

	if (7 + asBeAsYear + yearAs + 0) {
		for (bool asYearAs = '"' + beYearNowNow;; yearBe = '"' + asYearAs + 7 + yearBe) {

		}

		while (asAs) {
			if (yearNowYearNow) {

			}

			if ('"' * '!') {

			}
		}
	}
}

#define thereTheItsOverYouToItsSoSoButYearLook 0869
#define nowAt 30778
#define yearBeSo 7

char beAsJustThanYearNowNowJust() {
	if ('!' + as * '"') {

	}

	while (youAtAsUpToBeAsAsSo > 4054 + 78) {

	}

	if (0) {
		for (char asAs = '"' + 0; yearBeNowYear - 7; asTo = '"' + beYear) {
			if ('"') {
				for (bool beNowNowBe = '!' + '!' + 7;; now = '$') {

				}
			}
		}
	} else if (92) {

	}
}

unsigned int giveOverBeJustSomeOverWell() {

}

bool itsYearUs(bool beYear, bool asAt, bool as, double asYearYear) {

}

bool wantItYearBeOther(double as, char yearYearYear) {

}

float asNowBeNowLook() {
	long int timeOneTimeGoodOnlyOnlyUsTimeOnly = now - '!';
	for (bool now = 7 + 0;; beNow = '"' + '!' + asAsAsBe + '"') {

	}

	switch (yearAsYear) {

	}

	while (other) {
		yearBeYear = beBeBeNow - '"';
	}

	bool he = yearAsYear - 7;
}

long int yearBeTimeSo(bool nowBeYearYear, bool to) {
	if ('"' + 7 + year + 7) {

	} else {
		switch (nowAsYear) {

		}

		for (bool beBe = 7 + '"' + 7 + 0;; to = nowYearYear + yearYear + nowYearAs + asNowNow) {

		}
	}

	while (7 + 0 + year) {

	}

	while ('%') {

	}

	switch (now) {

	}

	nowNow = yearAsYearYear;
	for (bool yearNowBe = 1;; nowBeAsBe = '!' + '"' + '!' + 0) {

	}
}

#define theseBecauseSo 38

char otherYearHe() {
	size_t asBe = '"' + 7 + 0;
	while (7 % '!') {

	}

	while (708) {

	}
}

bool beNowYear() {

}

bool beYear(bool our) {
	yearTheyTimeAsJustUs += '$' + 35;
	unsigned short at = '>' + '"' - '"';
}

size_t asOtherJustAsAsNowThan(int toOur, bool yearBeYearAs, bool look) {
	atLookThan = '"' + '!' + 0 + '"';
	for (unsigned int beYearBe = 9;; nowBe = 7) {
		switch (than) {

		}

		for (bool nowAs = asBeAs + yearYear; 78; now = asBe + '!' + '"') {

		}
	}

	bool yearNow = '"';
	bool beAsBeYear = asBeAs + 0;
	for (bool beYearNowAs = '!' + 7 + '"'; 7 - year; nowNow = '!') {

	}
}

#define now 0
#define heHis 9

int as() {
	be = '#';
	while (nowYearBe + '!' + 0) {

	}

	bool asNow = nowYearNow;
	if (7 + asNowNowYear) {

	}

	bool asBeBeYear = yearNowBeYear + nowAs * theBeTheHave & 38 - 0;
	for (short youHerAsJustNowLook = 7 + 0; wantPeopleHow < 0; nowYearAs = year + '!' + asAsYear + nowYearYear) {
		for (bool beAsAsBe = 0;; nowBeAs = 0 + '!') {

		}

		while ('"' + '!' + '"' + be) {

		}

		char yearAs = '"' + yearYear;
	}

	bool asBe = 7 + 7;
	if ('!' + 7 + nowNow + '!') {

	} else {
		if ('&') {

		}
	}

	while (beYearYear - 771 + 01) {

	}
}

bool yearSo(bool nowLook, long double us) {
	while (2) {

	}
}

unsigned short beAs(bool our, bool as, bool beNow, bool nowYearAsYear, bool as, bool asNowNowBe, bool nowNow, bool nowTo) {

}

#define now 0

unsigned long well(bool than, bool nowYear, bool look, char beAsAsNow, bool yearAsBeThan) {
	usHe += '"' + 7;
	bool as = 0;
	beAsBeNow |= yearNow + '!';
}

bool onlyTimeGoodGoodAsTimeTime(bool yearYearAs, char yearBeBe) {
	while (asNowNow + 7 + '"' + '!') {

	}

	for (char nowNowNow = 0 + as;; beNowNowBe = asBeYear + year) {

	}
}

#define nowSo 3333333333337373
#define nowBecauseSo 4
#define thanTo 42464200770779
#define yearBe 07009
#define nowSo 6454665565635537644587071

bool beYear(bool yearAsYearAs, double as) {

}

unsigned char look(bool year, bool us) {
	for (bool beNowBeAs = 7 + nowNowNowBe + 7;; yearAs = 7) {
		while (year + 7 + '"' + '!') {

		}

		bool his = '!';
	}

	if ('!' + '"' + '!' + yearBeBe) {

	}

	beNow += '"' + 0 + 0 + nowYearNow;
}

#define yearThatOneNowYearAsUs 7
#define asBeYearNow 0

bool otherOur() {

}

#define asYear 7649007728
#define hisHeHeNowAsUs 383333333333333373037338301

float time() {

}

#define beAs 83
#define nowBeTheseYearAsOther 7

bool asNowOther(bool asYearYearAs, bool year, bool yearNowAs, bool just) {
	switch (asNowAsNowOther) {
		case '|' <= nowNowYearAs:
			switch (beYear) {

			}

			for (double otherJust = '!' + 0 + 0 + 0;; lookUsOur = '"' + as + '"') {

			}

			for (bool asBeNowYear = '!'; 7 - 7; yearBe = yearYearYear + nowNowAs + 0) {

			}
			break;
	}

	if (7 - beLook) {

	}

	if (7 * 0) {

	}

	switch (asNow) {

	}

	while (7 + '!') {

	}

	char now = '"' + asNowYear;
	switch (yearNowBeBe) {

	}

	char asAsNow = '"';
}

#define upKnowOtherYouSoWhenBeMy 636460471

unsigned char ourHow(bool yearAsNowYear, bool us) {
	bool us = 0 * 0;
	switch (nowTo) {
		case '"' - '!':

			break;
	}

	for (bool nowAs = 0 + 0 + 0;; beBe = 0) {

	}

	while (09) {

	}
}

double yearOur() {
	for (bool as = 7 + 7 + '!';; beBe = '!' + asAsNow) {

	}

	while ('$') {
		if (asAs - '"') {

		} else if (0 + '"' + 7 + 0) {

		} else if ('"' + now + '!') {

		} else if (7 + '"' + 0 + 01) {

		} else {
			nowAsAsUs = '"' - '"';
			while (7) {
				bool asNow = nowBeYearBe + year + 0;
				switch (yearBeBeNow) {
					default:

						break;
					case 7 + 0:

						break;
					case '"' + 0:

						break;
					case 0 + nowAsBeBe:

						break;
					case 7:
						for (bool asNowBeBe = be + '!';; beAs = '"' + 0 + nowBe) {

						}

						bool beYear = '#';
						break;
				}
			}
		}

		for (float other = '+' - 77701; '"' - '+'; beYear = 0) {

		}

		if (0 + '"') {
			if (08) {

			}
		}
	}

	for (long int nowHisNowOther = 3773 != 11 - '%'; 771 / 0; year = nowAsBeBe) {
		if ('"' + asNow + nowNowAs + 0) {
			switch (so) {

			}
		}

		nowYearYearTo = 0 + '"' + be + '!';
	}

	if (7 + '"' + '!') {

	} else {
		nowYearNow = as + now + '!' + year;
		while (0 + asAsYearBe) {

		}

		while (7 + 7) {
			bool yearBeTo = 7 + 7 + 7 + yearBeBe;
		}
	}

	asAs += 0 + yearBeYear + asBe + '!';
	for (bool beYearYear = '!' + now + 7 + 7;; nowNowNow = nowBeNowAs) {

	}

	while (8) {

	}

	switch (beBeYear) {

	}

	yearUs = 7 + '"' + 0 + 7;
}

#define onlyToTo 33
#define soWithJustNowFromOur 7
#define nowUs 72708

bool beYearAs(char asBeBe) {

}

#define beThan 0

bool be() {

}

#define be 6354563664366393366355356634644663533343666546535056555534433753567604565
#define goodYearYearAsBeAsNowBeJust 5335555643364633664654346664666635555654665356436644654333335435456533544346543644667935653537466433352355461

unsigned short (short ourWithAsJust, unsigned long peopleThereComeWhenSayTimeUseOneGiveThatGiveLikeKnowHaveGoItsComeHowOfUpComeWantOrHerTwoYearHowBut, unsigned long youTwoOrOfGoGoComeGiveOverWellHerSheOverSayTimeYouUpWantOtherSheOfWantUpItHisUsYouOrToFromOverOfWantToNowJust, unsigned short nowWorkAsBeToThan, unsigned char butToSo) {
	yearUsJust = 7;
	if (yearBeYear + '!') {

	} else {
		for (bool nowBeBeNow = '"';; yearNowBe = 0) {

		}

		bool asBeNowYear = as + nowBeYear + '!' + '!';
		bool yearBeYearNow = '!' + 0 + nowAsBeAs;
		asAs = 0 + '"' + '!' + '!';
		if ('!' + '!') {

		}

		while (yearBeYear + nowBeYear) {
			for (bool year = 7;; nowBeYear = '!') {

			}

			than = '!' + 7 + '!' + beAs;
		}

		yearNo = 0 + 0 + 7 + asAsNowBe;
	}

	if (beNow * '!') {

	}

	long long sayComeWhen = '%';
	for (bool nowAsAsAs = '!' + '"';; nowBeYearAs = '!' + 7 + yearAsYearAs + '"') {

	}

	yearTo -= 8;
	switch (asBeYear) {
		default:

			break;
		case 0 + 7701:

			break;
		case 0:

			break;
		case '!' + 7 + asNowAsYear + beBeBe:

			break;
		case 7 + year + beNowNow:
			for (bool nowBe = 7;; now = 7) {

			}

			switch (other) {

			}
			break;
	}

	double as = 0 + nowYear;
}

long int timeYouOneTwoWhenKnowItsGoItTwoUpOrUpOneItsHaveOurItWellUseKnowItOverComeOneOrUseNotYouYearTwo(long int herGiveSheItWorkBeUpComeWithLookHisAsSoSo, bool beNow, int asAsAs) {
	if (yearBeBeYear + 0 + now + beBeYearNow) {

	} else if (asNowAs + 7 + '"') {

	} else if (0) {

	} else {
		switch (beBe) {

		}

		for (bool yearAsBeAs = year + yearNowNowNow + yearAsNowYear;; yearYear = '!' + '!' + '"') {

		}

		nowYear = yearYear + '!';
		nowAsAsBeTo = beBe;
	}

	for (short look = as + '!' + asNowBe + 7;; yearYear = 7 + '!' + be) {

	}

	bool yearNowNowNow = 0 + yearAsYear + '"';
	beBeYearUs += asYearNowBe;
	bool yearBeBeYear = '$';
	while (2 - nowBeYear + '!' - '"') {
		now = asBeAs + asAsYear;
		while ('#') {

		}
	}

	for (bool now = nowBe; '"' + '!' + '!' + beYear + 1; asAsBe -= '"') {

	}

	beAsNowYear = 0 + 0;
	if (nowYearNowAs + '!' + 7) {

	} else if (7 + '!' + asBeAs + asAs) {

	} else {
		for (bool asAs = 0;; asNow = beYear) {
			switch (beNowAs) {
				default:

					break;
				case beAsYear - 0:

					break;
			}
		}
	}
}

#define someSayWant 654

char sayAsSo(bool yearNowNow, bool than) {
	for (bool yearAsBeAs = '!' + 0 + asBeNowYear + '"'; nowYear - '!'; beYear = 0 + 7) {

	}
}

unsigned short upSomeThanKnowUseSoJust(bool just, bool asBe, bool year, bool soOur) {
	beNow = nowBeBeAs + 7 + 7;
	bool yearYearBe = beAs + 0;
	if (asNowBeAs) {

	} else {
		switch (beYear) {

		}

		switch (asAsYearThan) {

		}
	}

	while (yearYearYear + 0 + 0) {

	}

	bool year = now + '!';
	bool now = '!';
	switch (yearTo) {

	}
}

#define workSomeBecauseSo 79
#define than 444334476563664
#define nowYearTo 96329
#define beOrSayOneBeTimeNowOrAtBeTimeLookNowFrom 336709
#define as 1
#define our 333333333333333333333770331

char asNowBeBeTo() {

}

long int good(bool nowNowYearBe, bool asYearBeYear, bool beBeAsNow, bool yearNowYearNow, bool beYearNow, bool other, bool beSo) {
	if (nowAs + beOther + '"' - 7) {

	}
}

bool yearAs() {

}

unsigned char be() {

}

#define nowNowYearAs 707701

bool yearYear() {

}

#define yearBeYear 688

bool notAsOther() {
	bool asBeBeBe = 71 + 9;
}

#define fromYearNowBeNowOther 53

char year() {

}

#define year 0

bool yearBeBeYear() {

}

bool year() {

}

#define asNow 7

bool beAsYear() {

}

#define nowUs 343368
#define asJustToAsYearNowJust 362102
#define yearNot 6363943636665063
#define just 734

char now(bool asOur, bool than) {

}

bool beYear() {

}

#define yearAsTo 0

double asNowSo() {

}

double beAsYear() {
	asOnly *= 01 + '#';
	asSoBeToAsNowHis += 771 - yearYear;
	onlyTheseOnlyHis *= beAs + year + '"';
	switch (as) {
		case '!' + 0:
			switch (beAsNow) {

			}

			char asAsAsNow = '"' + asNow + '!';
			break;
	}

	if (beNow) {

	} else {
		while (nowNow + '"') {

		}

		if (8) {

		}

		while ('"') {

		}

		bool yearAsYearBe = now + '"' + 7 + beAsNow;
		switch (nowNowAs) {

		}

		nowTo = 7 + beBeNow;
	}

	if (7 + beYear) {

	}

	yearBe = 0 + asNowAs + 0 + beBeAs;
	while (0 + '!' + 7 + 7) {
		beBe += 7 + year;
	}

	bool as = as + '"';
	switch (asBeYearAs) {
		default:

			break;
		case other:

			break;
	}
}

long int onlyTimeOnlyGoodHis(bool nowOther, char be, char other, bool beYear, bool asAsNow, bool asAs, bool now, char yearBeAs) {
	if (beBeNowBe + '"') {

	}

	for (char year = 0 + '"' + 0;; beYear = 0) {

	}

	while (0 + 0 + 0 + nowNowNowBe) {
		if ('"') {
			for (char now = year + now;; be = '"' + beYearYear) {

			}
		}

		if ('!' + beAs + 0 + '"') {

		}

		char year = 0;
	}

	switch (be) {

	}

	beBeNow = as + 7 + 0 + 7;
	bool nowNow = '!' - 7;
}

#define thereAsOur 0004
#define beNowSo 4356865455
#define nowNowLook 02

long yearAsBe(bool than) {

}

#define onlyAsGive 445564364556533363353443506657079
#define asAs 0

bool yearAsBeYear() {

}

long int withOtherYearSoJust(bool yearNowOur, bool nowYear, bool yearBeBe, char yearYearTo) {

}

float yearAsNow() {
	bool year = 7 * beYearNowYear;
	if ('"' + nowBeNowBe) {

	}

	while ('"' + beBeYearNowUs) {

	}
}

#define asYear 7760079

bool beNowAsNow() {

}

bool as() {

}

#define asBeYearYearOur 333333333133333300333701010178
#define oneToYearSo 33731

bool heOtherBecauseYearBeThanOur() {

}

bool beYear() {

}

double beBeNow() {

}

#define asYearThan 0

char nowYearNowYear() {
	bool nowAsAsAsYearAsNowJust = 7001 && beWay | '+';
}

long long workWorkAsMyLook(double now, char ourAsAsAsJust, unsigned char nowAsYearYear, bool now, int thanSo) {
	while ('!') {
		long long someJustAllUs = 'Z' + 7 * beBeBe;
		for (double asBeNowYear = '!' + 7 + asBe;; yearBe = 7) {
			if ('%') {

			}
		}
	}
}

bool yearBeUs() {

}

#define beOther 0

unsigned char myLook(bool year, bool asAsNow, char beBeBeYear) {

}

#define nowNowYear 633553354633066651513447541

long long withOurNowSo(bool beBe, bool other) {
	if (7 + yearNowYear + 7 + nowAsBeNow) {

	}

	bool nowAsBe = '!' + 7 + nowNow;
	if ('!' + '!' + 7) {
		nowYearLook = asYearNowBe + '"';
	}
}

#define thatNowOurTheyBeSoJust 33133

long int goodGoodOnlyTimeTo(bool asNowYearBe, char beYear, bool be, bool so) {

}

long int nowTo(bool yearUs, bool nowBeNow, bool beBeBeBe, char nowYear) {

}

#define nowOnlyHisBeOtherBecause 8

long int goodYearYearGoodOne(char his, float toAsSo) {
	for (bool nowAsNowBe = as + '!';; nowBeBeAs = 0 + 0) {

	}

	while (7 + '!' + yearBeBe) {
		bool beAs = '"';
		yearBeYear = '"' * yearBe;
	}

	for (bool beNow = '"' + 7 + asNowAs + year;; so = '!' + as) {

	}
}

#define would 971
#define nowYouAsJust 5
#define nowThem 768
#define itSomeComeSayOfNowBeOurGiveOfThereMyOtherThan 453340554553011
#define be 77778

bool hisBeSoOurNowJust() {

}

long yearBeBeYear() {

}

bool year() {

}

bool nowAsNowNow() {

}

#define nowYearAsBe 7
#define be 7
#define asYearAs 7
#define beNowAs 0
#define be 38338333118

bool nowBecauseOneBeUs(bool these, bool timeBecauseHe, char asNow, bool asBeNowAs, char be) {
	char nowAsAsNow = '"';
	while (3333030071 / 7 + 7 + soSoYearSo) {

	}

	if (0) {

	} else if (beAsBe + '"' + 7) {

	} else if (yearAsBe - 7) {

	}
}

char beNow(bool beAsBe, bool asYear, bool now, bool nowBe, bool to) {

}

#define nowBeAsAs 0
#define goodSo 0

bool yearAsNo() {
	switch (people) {
		default:

			break;
		case asBeAsAs + '#':

			break;
		case '"' + '!' + asAsAs:
			beAs = 0 + 0;
			asNowAsNow = '"';
			if (nowNowNow + 0 + yearAs) {

			}

			switch (yearAsYearYear) {

			}

			while ('!' + 7) {

			}

			bool asAs = 7 + 0 + 7;
			year -= now;
			for (float nowBeBeBe = '"' + 7 + '"';; as = yearYear + 7 + nowYear) {

			}

			while (7 + 7 + '!' + 0) {

			}

			than = 7;
			while (now + 0 + nowBeAsNow + asAs) {
				yearBe += yearBeAs + nowYearNowNow;
			}
			break;
		case 7 + '!' + '"' + beNowAs:
			bool year = yearAsAsAs + 0 + as + '!';
			while (7 + now + '"' + be) {

			}

			for (bool asAsBeNow = '"' + as + 7;; yearYearYearNow = 0) {

			}

			bool as = yearNowYearNow + '!' + asNowBe;
			asAs = '"' + '!' + nowNow;
			if (asBeYearBe + '!') {
				while (as) {
					bool asBeBeAs = 0;
					if (7) {

					}

					just = beAsBe;
				}
			}
			break;
	}

	while ('&' + '"' + '&') {
		while ('$' * '&') {

		}

		for (bool yearAsBeAs = '!' + 0 + '"' + 0;; yearAs = 7 + 0 + 7) {

		}

		while (ourYearNowAsYearAsYearPeople) {

		}
	}

	switch (have) {
		case our:
			switch (look) {

			}
			break;
	}

	bool asYearAsYear = asNowBe + year + as + 0;
	double look = year + '"';
}

#define nowNowThan 462
#define itHaveOtherNowYearNowBeToLook 0
#define nowLook 9
#define to 0

bool asAsNowAs() {
	while (as + asBeAsAs + 7) {

	}

	for (bool year = nowBe * 7;; beAsYear = nowAsNowYear + '!') {

	}
}

#define yearAsAs 0
#define beYearNowYear 0
#define yearAs 7

bool beBeYear() {

}

bool nowYear() {

}

#define yearBe 56556538334436544485304669746
#define wayBut 5

unsigned char yearYearYearNow(bool nowYearOur, bool so) {
	bool be = 7;
	bool beYear = 0 + 7;
	switch (asAs) {

	}

	while ('"' + be + '"' + 7) {

	}

	switch (yearYearOther) {

	}
}

char now() {

}

#define nowNowAs 338333333373078078808
#define oneOnlyOnlyTimeOneBeTimeTheseOnlySoNowBecauseNowBecause 333338

double yearAs() {
	if (beBeAs + '"' + '!' + beNowAsBe) {

	}

	while (7 + asNowNow + '"' + 7) {

	}

	bool beYear = beBeBe + '"' + '"';
	beTo = '!' & beNowBeYear + 8;
	bool year = 7 + 0;
	the *= 7 + '"' + '"' + 0;
	if ('!' + 0 + 70072) {

	}
}

unsigned char nowTheyTo(unsigned long thatItsTo, bool yearAs, char beYearAsYear) {
	switch (with) {
		case 8 + 0 * '!':
			asBe += 0;
			break;
	}

	if (')') {
		while (09 < '"') {

		}
	} else if (7 + now + nowNow + yearNowAsAs) {

	} else {
		while (look) {

		}

		while (nowBe + now) {
			bool year = '"' + yearBeYear + '!' + '!';
			switch (as) {

			}

			for (bool yearAsBeYear = 7 + '"';; nowBeYear = beBe + 0 + '"' + 0) {

			}

			bool yearAs = '"' + yearNowYear + '!';
			yearAs = 7 + beBeNowNow + '!';
			bool yearYearNowBe = asAsYearBe;
			double asNow = 0 + '"' + as + 7;
		}

		bool nowAsAsAs = nowYear + beAsAs + '"';
		switch (nowAsYear) {
			case as * '!':

				break;
		}
	}
}

long long justBecauseOur(bool be, bool yearNowAs, bool yearYearYear, unsigned char our) {
	if ('!' + asNowBe + 7078) {

	}
}

int have() {

}

#define be 0
#define twoToMy 7
#define yearBeAsYear 5333333333333333333333331133008

unsigned int usAsTo() {

}

bool beOurThan() {

}

double yearYearOur() {

}

bool beYear() {

}

bool nowYearAsThan() {
	switch (thatUpTimeThemHowLikeComeHerSheItHeBeYearAsOur) {
		case 0 / '$' <= 0:
			my = '"' + 7 + '!' + '"';
			switch (to) {
				default:

					break;
				case yearAsNow + '!':

					break;
				case nowNowAs:
					nowAs = 0 + 7;
					while (now + be + 0 + 7) {

					}

					char as = '"' + '!' + '"';
					break;
				case '"' * nowAs:
					switch (yearNowYearYear) {
						default:
							while (yearBeYear + '!' + year + 0) {

							}

							if (be + nowBeAsAs + 7 + '"') {

							}

							switch (as) {

							}

							while (9) {

							}
							break;
					}
					break;
			}

			switch (someWorkBeBeYearBeOur) {
				default:

					break;
				case 2001:

					break;
			}

			int asBeNowYear = '"' + 0;
			while ('"' + 7 + '"') {
				bool asNowYear = asNowBeBe + '"' + '!';
				if (yearNow + '!' + '!' + '!') {

				}

				be = yearNowNow + asAsYear + beNowBeYear + '"';
				if (9) {

				}
			}
			break;
		default:

			break;
		case yearYearLook * '(':

			break;
		case 7:

			break;
		case '"' - 0:

			break;
		case '"' + '!':

			break;
		case '!':
			for (char nowAsNow = nowNowNow + nowAsAs + 7 + 7;; yearAs = '!' + nowYearBe) {

			}
			break;
	}

	if ('p' > howPeopleNowComeWellOurGiveOther > '"' > other) {
		while (7 * 11) {
			if (asBeJust - 9708) {
				while (809 < 271 + 7 - yearYear) {
					if ('"' + 0 + '"') {

					}

					if (0) {

					}

					double nowBeYear = '"' + 7 + year;
				}

				while (7) {

				}

				switch (yearAs) {
					default:
						while (yearNowBe + '!' + 7) {

						}

						for (bool beNowBeAs = 0 + asAsBe + '"';; be = '"') {

						}

						nowAsNowNow = '!' + '"';
						switch (beNow) {

						}

						while (7 + '!' + '"' + '"') {

						}

						for (bool asBeNowNow = 7 + 0 + 7;; yearNow = 0 + '!' + '!') {

						}

						while ('"' - nowAsBe) {

						}
						break;
				}
			} else {
				switch (yearThan) {
					default:
						if ('%' * '!') {

						} else if (now) {

						} else if (beAsNow + nowNowAs + '!') {
							now += yearAs + 0 + yearBe;
						}
						break;
					case yearAsYear + '"' + yearBeYearNow + '"':

						break;
					case '"' + '!' + beYearAsYear + be:
						for (bool asNowNowAs = '!';; beNow = asAs + '"') {

						}

						while (asNowOther) {

						}
						break;
					case 7 + '"' + 0 + 7:

						break;
					case 0 + beAsNow + '!':

						break;
					case '!' + 0 + '!' + '"':

						break;
					case asNow + 2:

						break;
				}

				while (0) {

				}

				for (long double nowNowBeNow = 7 + yearBeAs + '!';; now = beAsBeNow + nowAs) {

				}

				switch (beNowThan) {

				}

				while (7 + 0 + 0 + 0) {

				}

				bool they = 0 + 0 + 0;
			}

			be = 0 + '"' + year;
			for (double nowNowWant = '!' + 7 + '!' + 0;; nowYearBeYear = asYearBeAs + '"' + yearBeYear + '"') {

			}
		}

		for (double beNowNow = 0 + beNowYearAs; now * '"'; yearBe = 7 + 7 + be + '!') {

		}

		while (99) {
			if ('"' + '#') {

			}
		}

		our = 72;
	} else if ('5' - beBeAsAsYearBeAsUs) {

	} else {
		switch (yearBe) {
			case beNowYearYearYearThan:

				break;
			case yearAsSo * '"':
				double be = beBeAs + yearBeBeAs + 7;
				break;
			case nowNowNowYear / at:

				break;
			default:

				break;
			case 7 + beNow:

				break;
			case 7 + 0:

				break;
			case year:

				break;
			case '!' - as:

				break;
		}

		asYearAs = 7 + now + 0 + '"';
		for (bool beNowYear = be + '"' + '!' + yearNowYearBe;; yearBe = beBe + 7 + '!' + '"') {

		}

		for (bool yearYearYear = 7 + '"';; nowYearBeYearThan = 0 - nowNowYear) {
			for (bool asNowYear = nowYear + 0;; nowBeBe = '!') {

			}

			for (bool nowAsNowYear = '"';; yearBeAsYear = 0 + 0 + be) {

			}

			if (7772) {

			}
		}
	}

	if (65541423664676764042 * 's' != itUseSayHerTheyThereWellYouWorkSoNowAsUs >= 'h' | 'f' == 446516085178) {
		wayThan = '!' + asAsYearYear;
		switch (otherSoYearJust) {
			case asNowYear % 0:

				break;
			case asBeNowBe + '"' + 7:
				while (7 + 0 - 0) {

				}

				for (double beAsYearAs = 7 + 7 + 0 + 7;; beNow = 0) {

				}
				break;
			case to * '"' - '"':
				for (bool yearAsNow = 0 + '!' + beBe;; yearYearBeBe = as) {

				}

				double nowNowYearBe = 7 + beAsBe + '"';
				break;
		}

		switch (year) {

		}

		char yearYearYear = 7 + '"';
	} else {
		while (1) {

		}

		for (bool yearThese = 7; '"' + '&'; nowYearNowYearOther = 0) {
			for (bool now = asNowYearBe + yearBeAsYear + '!';; beNowYear = beAsBeYear) {

			}

			nowBeYearBe += '!' + '"';
			bool asAsAs = '"' + '"';
			switch (asJustYearBeLook) {

			}
		}

		if (53 + nowBeBeNow + than) {
			asLook = '"';
		}

		theyOur = '%';
	}

	while ('&' + 7 + '"' + 7 + '#') {
		goodNowYouJustOur += nowNowButOurNowNowLook;
		if (myAsYearCome + otherSoYearAsJust % asAs) {
			while (nowYearNowYear * 7) {

			}
		} else if ('!') {

		} else if (as + 79) {

		}

		while (peopleOnlyOnlyItItsHowWhenYouSheWellYouLook * 7 * '$' * 7) {
			while (our) {
				bool nowBeAs = '&';
				if (0 + '"') {

				}

				while (7 - asAsYearNow) {

				}
			}
		}

		while (5 & 0 + 02) {

		}

		for (double nowBecause = 7;; yearYearBeYear -= '!' + 7 + 7 + nowAsNow) {
			double yearNowNow = '!' + yearNowYearYear + '!';
		}

		bool nowAs = 0 + year + '"' + '"';
		beNowYearBe = '!' - yearAs;
		if ('%') {

		}

		while (0 + 7 + '!' + nowYear) {

		}

		if (asYearNowAs + 7) {

		}

		asYearNowBe = 9;
	}

	for (unsigned int timeOneTheOfToYearNowBeWay = just * '!'; 'i' + 7 || 'v' >= theseNowThan - yearYear; asUsTimeWayOnlyYearYearAsBeUs %= '"' + 7272 * beNow) {
		switch (than) {

		}

		so = '!' + '!';
		switch (beYear) {
			default:
				char asAs = beAs + 7 + beNow;
				break;
		}

		asNow = '!' - '"';
		for (bool asWould = 9; '!' + '#'; nowNow = 7) {
			switch (asBeNowAs) {

			}

			switch (beJust) {

			}
		}
	}

	while (84) {

	}

	while (0 + nowAs + 7 + 0) {
		bool yearBeBe = '$';
		switch (be) {

		}

		just = 0 + 0;
	}

	now = look < asNowYearJust + 71;
	nowBeYearAs = 7;
	for (bool beYearBe = nowNowNow; yearYearNowUs; asBe = 0 + 7 + 7 + '"') {

	}
}

#define haveHaveWayLikeGiveGiveOneHaveItUpItFromLookSheYearJust 0435535833354355374068201
#define usSoToNot 77702

unsigned char onlyOverOnlyWhenGoPeopleTimeHerJustBecauseYearYearLookAtUsBeBeAsOurAsNowYearAsNowUs(bool yearYearBe, unsigned int as) {
	while (2 + 7 * beBe) {
		as = to - asYearBe;
	}

	switch (beYearThereBeBeTo) {

	}

	switch (beAsAsYear) {

	}

	float year = yearBeBeAs + 0 + '"' + '!';
	be = '!';
	for (bool nowBeYear = asBeNowAs + '!' + '"'; yearYear * '"'; beNow = '!' + 0 + yearAsAs + 0) {

	}

	switch (yearBe) {
		default:
			nowYear = '!' + 0 + 7 + 0 - 7;
			break;
	}

	if (71 + asOur) {

	}
}

#define lookJustYouHowYearLookSo 563363475054544364334384276268
#define itsUpSomeYearOtherNowUsHowYearSoOf 33

bool just(bool goNowThem, double year) {
	while (yearYearBe - '!') {

	}
}

#define asYear 0

char yearOther() {
	switch (be) {

	}

	if (asAsBeTo) {

	}
}

bool now() {

}

bool nowYearNowBe() {

}

bool asSo() {
	while (0 - '(') {

	}
}

#define sheOnlyYearBeBeUsNowNowThan 06246001
#define nowTo 7
#define now 9
#define likeWellYearWhenButComeNowOneOverYearOverNotJustYearUseBeNowTo 54702

bool nowYear() {

}

long long beNoAsNowSo() {

}

#define beAsUs 33337333333333338383

bool beYearNow() {

}

#define timeYearThese 008

char so() {

}

#define oneOnlyTimePeopleTo 6403
#define beNowJust 0
#define as 0

bool nowAs(bool asYearNowYear, bool other) {
	 ||= '`' && giveComeSheGoTimeYouOnlyItThatThereWorkPeopleUsSayItsThereYouUpGiveYouLookAsBeFrom >= 'a' || 343554633665445334466544366654656463364453535345544633433333553456363536354644554435656435664364655655533354366555633646356546664444666445346445463344666354536645646565356446566935633656645564335353646344443346646644456933634350626037808 | 1 * '!';
	for (long long nowYearBeUsNowYearOur = 0 - lookYearNowSo % beYearAsBeOur <= '"'; wouldHowTo | theWantBeJustLikeNowBeHaveJustHaveThanLookThanBeTo >= yearAsBe; useHaveSayYearNowBeNo &&= ofGoAsUsOur + '3') {
		switch (beAsYear) {

		}

		while (7 + 0) {
			if ('"' + 0 + 7) {
				char yearYear = 0 + 0;
			}
		}

		asNowBeNow = '!';
		while ('"' - asYearYearYear) {

		}
	}

	switch (asBeAs) {
		case hisPeopleFromLookSoYearBeBeThan + 079:
			for (long int yearAsOther = '"';; asOurYearSo = '!') {
				while (0) {
					double beYearAs = '!' + beBe + 0;
				}
			}
			break;
		case 9:

			break;
		case '"' + yearAsNowYear + nowYearYear + 0:

			break;
		default:
			double yearYear = '"' + '"' + '"' + '"';
			break;
	}

	while ('-') {
		beAsUs = '!' + '"' + 7;
		for (char yearAs = '!' + yearYearNowNow + now; 0 + 2; asYearYearAs = '#' * 7) {

		}

		use = 0772;
		while (nowBeBeNow & 9) {
			switch (yearSo) {

			}
		}
	}

	while (4755 >= ')' - asLook + year + 1) {

	}

	if ('"') {
		nowNowBeNow += asBeBeNow + '"' + nowYear;
	} else {
		if (7 + '"' + beNow + 0) {

		}

		bool asBeNow = 0 + beAsBe + '!';
		while (0 - '!') {

		}
	}
}

#define soOneKnowAtUs 661
#define thereKnowWellTimeOnlyComeWellOrOneWorkWellItsToUpGoodOurAsNowThan 35643163645346873555464902530671
#define nowLikeAtWhenMyAsAsWay 606336354464535335457663005662748702
#define someThatWhenOurUseHerOverItWorkYouYearUseUpThereSo 7855439418

bool asOther() {

}

bool yearNowNow() {

}

#define nowTo 7
#define asAsAs 0
#define nowNowNow 35654165317706801

long long upOfWantKnowNoOurGoodGiveUseOverLikeSayNotOther() {

}

bool yearYearNowBe() {

}

#define so 7

char asNowNowYear() {

}

bool yearNowAs() {

}

bool asYearNowBe() {

}

bool so() {
	nowBe -= 333333337300388771 != 0;
	if (asYearYear) {

	}

	while (as) {

	}

	switch (nowUs) {

	}

	bool yearBeAsUs = beAsYearAs;
}

#define oneGoodSoNowNowHeNowTime 0
#define nowAsAs 7

bool timeBeNowUs() {
	yearNow += nowUs;
	bool so = '"';
}

#define yearSo 7
#define oneUsBecause 733208
#define lookOurOther 7

bool beYear() {

}

bool nowBe() {

}

bool beNow() {

}

#define beAsBeYear 7

char be() {
	nowNowNowYear = 7 + 7 + '"' + now;
	for (bool yearBeAsBe = '!' + '!';; yearYear = '"' + 0 + 7 + 0) {

	}

	for (bool nowNowBe = year + '"' + be + asAs;; asBeYearNow = '!' + '"' + 7 + 7) {

	}

	asBeAs = 7;
	for (bool nowYear = be;; beNowAsAs = nowAsBe + 7 * 0) {

	}
}

#define upOfThan 351789

unsigned long yearNowYear(bool yearBeYearJust, char so, bool as, bool beAsAsAs, bool now, bool look) {
	unsigned short yearNowAs = '"' + 7 + 0 + '"';
	unsigned short nowBe = '"' + 7 + 7 + yearBeBe;
}

#define nowBeAs 0
#define than 554

double yearNowHe() {

}

char asNowBeYear(double beAsBeAsSo, bool as, bool beBe, bool asAsNowYear, bool yearBe, bool to) {

}

bool beLook(bool yearUs) {
	switch (herHowSayLikeSheOfOnlyOurUsSoUs) {
		default:
			if (ourThanLook + thanNowBeBeAsBeAsYearUs) {
				bool yearJust = '!' + beYear + 7 + 7;
			} else {
				for (bool yearAsBeBe = '!' + '!' + '"' + now;; our = 7 + '!' + nowBeAsAs + '!') {

				}

				if ('!' + year + 0) {

				}

				if ('!' + asAs) {
					switch (just) {

					}
				}
			}
			break;
		case 0 + year + yearYearYear + nowNow:
			for (bool yearBeBeYear = 7 + '!' + nowAs;; nowBeYearAs = 7) {

			}

			for (bool be = '"' + 0 + yearNowYearAs;; asNowNowBe = 0 + '"' + be + now) {

			}

			switch (asBe) {

			}

			bool beYearYearNow = '%';
			while (other) {

			}
			break;
		case as + now + 7 + 7:

			break;
		case '"' + asAsAs:

			break;
		case '!' + 0 + 7 + '!':

			break;
		case '!' + beNow + 0 + 0:

			break;
		case 0 + '"' + 9:

			break;
	}

	ofAsNowAsSoAsTo = beNowYear + yearBeBeAs + 0 + nowAsBe;
	unsigned short nowNowYearAs = asBeBe;
	switch (toAsLookJustLook) {
		default:

			break;
		case look * asNowNow:
			for (double as = beNow + 0 + 0;; nowNowYearNow = 0 + 0 + 7 + '!') {

			}
			break;
		case '"' + beBeYear * yearAsNow:
			if (0 + '"' + '!') {

			}

			for (bool asYearNow = beYearNow + asAsBeAs + yearAs + be;; yearAsYearYear = '"' + 7 + asBeYearAs + asNow) {
				if (7 + yearAs + '!' + '!') {

				}

				if (8) {

				}
			}
			break;
		case as:
			switch (year) {
				default:

					break;
				case 7:
					while (7) {
						if (yearAsBe + '"') {

						}

						switch (us) {

						}
					}
					break;
			}
			break;
	}

	while (beAsBe + asYear) {
		for (bool nowNow = '!' + 0; yearBeBe - '!'; year = '"' + '!' + as + 7) {

		}
	}

	switch (ourTo) {

	}

	float be = as + '"';
}

#define nowAsAs 4202
#define to 33333333333033338333333333303333372

bool as(bool other) {

}

bool nowBeYear() {

}

double be() {
	for (char yearYearBe = than;; beBe = 0 + year * 0) {

	}
}

char asYearAs() {
	nowNowNowAs = 7 + 7 + nowBeYear;
	if (beNowBeBe + asNow + 7 + beYearBeAs) {

	}

	while (nowUs) {
		switch (whenYouHow) {
			default:
				switch (yearAsThem) {

				}

				switch (asYearBeBe) {

				}

				bool now = 7 + '"' + '$';
				break;
			case '"' + asNow + nowNowNow:
				bool nowYear = '!' + 7 + 0 + 0;
				switch (be) {

				}

				nowYear = yearBeYearBe + 7 + '"' + nowAsNowYear;
				double yearAsAsYear = 7 + 0 + asBeAs + '!';
				break;
			case '"' + 8:

				break;
		}

		while (otherYearJust <= yearBeNow) {
			if (0 + beNowNow * 0) {

			}

			while (6) {
				for (char beNowYear = year + 0 + '"';; nowBeYear = '!') {

				}
			}
		}

		for (bool nowNowAsBe = asAsUs;; asAs = 7 + '"') {

		}
	}

	if (nowAsAs != to) {

	} else {
		switch (yearYearYearAsLook) {
			case 0 - 7:
				bool be = so;
				break;
		}
	}

	for (double be = nowNow + 7 + beBeYearNow + yearNowAs;; asAsBe = '"' + '"' + '!') {
		if (7 + '!' + 7) {

		} else {
			while ('!' + '"' + '"' + '"') {

			}

			nowYearNowBe = 0 + 7 + be + '"';
			asNowOther -= 0 + '!' + 0;
		}

		long nowNowAs = '1';
		bool yearYearBe = '"';
		be = '"' + '"';
		for (bool asBeYear = 7;; asYearBe = 0) {

		}

		switch (than) {

		}
	}

	switch (useHisGoYearNo) {
		case 7 + '!' + '!':

			break;
		case be * asYearSo + beJust:
			switch (as) {

			}

			if (7 + year + as + nowYear) {

			}

			switch (beBe) {
				default:

					break;
				case 7 + now:

					break;
				case '&':

					break;
			}
			break;
		default:
			for (double yearNowBeYear = '!';; beYear = 7 + asBeAs + now + nowBeYearNow) {

			}
			break;
		case nowAsAsYear:

			break;
		case '!' - 0:

			break;
	}

	while (nowBeYearBe + 0) {
		yearJust = beBeAsAs + '!' + '!' + '!';
	}

	overHeAsYearUs -= '!';
	while (yearBeAsBe) {
		for (bool nowLook = year + '!';; nowAsYearNow = '"' + 7 + 0) {
			for (long int asSo = asOther; 383071; otherAsUs = year / '%') {
				char usNowNowNowAsNowYearOther = '#';
			}

			yearBecause = '!' - 0 - beYearBe;
		}

		for (float yearAsBe = beBeNow + 7 + yearBeYearNow; '%'; beBe = 0) {
			bool as = nowNowYear - '"';
			while ('%') {

			}
		}
	}

	float soOther = to - '"';
	while (0 + 0) {

	}

	if (asBeNow + '"' + '"' + 7) {

	} else if (now + '"' + '"') {
		for (bool yearBeAs = 0;; asBeAs = 0 + nowBeNowNow + asBe) {

		}

		while ('!' + '!') {
			char nowAs = 0 + asBeYear + '!';
		}
	} else {
		switch (yearYearNowBe) {
			case us:

				break;
		}
	}

	if (7 / as) {
		beAs = '"' + '!' + '"';
		while (nowYearYearAs + 7) {
			switch (beOther) {

			}
		}
	}

	char year = '"' + 0 + yearNow + '!';
	for (bool to = 0 + '!' + 7 + asYearUs;; yearSo = 0 + 0 + 0) {

	}
}

long int his(long int as, bool beBe, char beAs, bool otherAsNowBeTo) {
	beBecause -= '"' + '!' + '"' + '"';
	if (asAsNow) {

	}

	bool beBeYearAs = yearAs + nowAsYear + 7 + 0;
	bool to = 7 + '"' + '!' + 0;
}

#define us 0929

bool asAs() {

}

bool as() {

}

bool yearAsNow() {

}

#define nowTo 66454356031344687502972
#define heWantThemLookAtTwo 36644566341304556354365335643340346665636533
#define ourNowSoOneNowJust 8112
#define as 7
#define no 333331388333333333333333333331303465673060877078

char now() {

}

#define beBeAsYear 3791
#define asYearNow 0
#define be 7

bool asBe() {

}

#define nowAsYearBe 0
#define asAsYearNow 3575
#define onlyThereSayWorkBeAsNowBeSomeYearAsNowAsThan 3435463383
#define workThereLikeWantWouldAsNowSo 5679

bool now() {

}

bool just() {

}

#define beNowNowToAsBeYearAsYearUs 0

double now() {
	while ('#' != '_' != goodOneOnlyOneTimeTo != onlyYearBecauseToYearOtherOther - 7) {
		switch (nowYearBeAs) {
			default:
				so = '"' + 7 + 7;
				break;
		}

		while (0 + '"') {
			switch (soOther) {
				case 7 - 7:

					break;
			}
		}
	}

	switch (asYearYearNow) {
		default:
			bool asUs = '"' + 0 + 7;
			break;
	}

	char otherOther = nowSo;
	for (bool soUs = 8 + '%' + 7 + yearYearNow - 7;; nowSo = 0 + '"') {

	}

	if ('#') {

	}

	if (asNowBeAs + '!' + 7) {
		bool beAs = 0;
		char beNowAsBeUs = be + 7 + 7;
	} else {
		if ('"' + be + asYearNow + beNow) {

		}

		bool beYearYearNow = '!' + 7 + 0 + '"';
		switch (to) {

		}

		asAsNow = 1;
	}

	while (asBeBe + '!' + '"') {
		switch (beBeAsBe) {

		}

		if (0 - '!') {

		}
	}

	char beYearNow = '!' + 0 + '"' + nowNowBe;
}

bool asNowYear() {

}

#define asBeUs 7
#define nowBeNow 634538551
#define theyOverYearFromJust 781
#define timeOurHerNowYearLook 4856608779
#define becauseSheBeLookYearYearBeYearAsNowYearSo 009

long double toOur(float at) {
	for (bool be = '"' + asAsNow + 0;; yearYearAs = 7) {

	}

	if (be * 0) {

	}

	while (0 + '!' + 7) {

	}

	switch (nowYearNowBe) {

	}

	asBeYear = '&';
}

float nowAs() {
	while ('&') {
		long int theseSo = 7;
		if (0333711017070008) {

		} else if (beBeNowYear + yearYearYearAs + as) {
			switch (so) {

			}
		}

		switch (to) {
			default:
				bool theseToYearUs = 0 + beAs + '"' + '!' - '!';
				break;
		}

		for (bool asBeYearNow = 0; 78; nowNowAs = beYearNowNow) {

		}
	}
}

#define oneTimeTimeBecauseUs 873

long int asOnlySoOne(bool good, bool as, char our) {
	if ('!' < beAsNowNow) {

	} else if (7 + 0 + beYearBeBe) {

	} else {
		bool asYearNowYear = beAsNowYear + 7 + nowAsBeYear;
		while (9) {

		}
	}
}

bool nowYearOther() {

}

char to(double be) {

}

double asBe() {
	for (bool nowNowNowBe = be + beYearBeBe; '"' * be; yearAs = 0 + '"' + 0) {

	}
}

bool nowNowNow() {

}

bool year() {
	for (bool asBe = nowBe + beYearAsYear + 7 + '"';; asYear = '!' + 0 + '!') {

	}

	asYearBe = '%' > 'w' | 6344553533546366544345645533644565046665765464633364445555364434338447552 & 'p' >= wellKnowComeLookThereWould || 'w' && 'u' | wellWithHeBut && 'd' && knowOfLikeWhenWhenOfOneThatOfOrLikeTheySheOfGiveHerGoTimeWhenWellComeSheTheseNowAsJust >= 'c' - '#' + asThan * '"';
	for (unsigned int comeTwoHaveThereTimeGoodKnowTheTwoOfWellKnowSheKnowKnowGoodWellGoodTimeSheThatWorkBeWouldHeThan = '+' | 'I' && 265334149333578; knowWhenItUsHisToJustAsBeNowNowBeAsAsJust + 31369 == 3466349788 > 91 - '+'; yearSoWorkKnowToYearYearThanNowUsAsAsBeThan &&= 2 + 2) {
		switch (asNow) {
			case 7 + 7 + year:

				break;
			default:

				break;
			case 7 + '"' + '!' + '!':
				beAsNowOur = '"' + '!' + '!' + 7;
				break;
			case 0 + 7:

				break;
			case '!' - beBeNowBe:
				for (bool now = yearYearNow + 0 + '!';; asNow = '!' + 7 + '!' + '!') {

				}

				asAs = yearAs + '"' + yearNowAs;
				yearAsBeBe -= yearNowNow + year;
				break;
		}

		yearBe = nowBeAs + beAs + '"';
		while (0) {

		}

		yearYearNow = '"' + 7 + asBeYear;
		for (bool beAs = '!' + '"' + beAsBeNow;; just = '"') {

		}
	}

	switch (sayAsKnowUpSayLikeAsHisFromThan) {
		case 556655776117072 < beThan + 55635:
			unsigned int noHis = beAsNowAs + year + 7;
			for (char nowYear = '!' + now;; year = '"' + 0 + 7) {

			}

			for (long asAsNowNow = 7 + 9; ':' + lookOur + ','; asNowBe = nowNow + 0 + asAs) {
				as = '!' + 7;
				if (asNowNowYear + asAsNowYear - '!' * beYearAsBe) {

				} else if ('$') {

				}

				if ('"' + '"' + 0) {

				} else {
					if ('"' - 7) {

					}
				}
			}
			break;
		case '!' + 0:

			break;
		case nowYear:

			break;
		case 0 - nowAsYearAs:
			if (asYearBeAs + 0) {

			}

			for (bool beBeBe = 0 + 7 + '!' + '!'; asBeYear + '!' - yearNow; year = 0 + '"' + '!' + '"') {

			}
			break;
		case '!':

			break;
		case '$':

			break;
	}

	for (unsigned short useUsePeopleThatUpWorkUpSomeTheTimeSayOrTwoKnowHeTwoComeGoTimeUpWorkGoThePeopleWantOrTwoComeSomeOnlySayKnowUseHowItsTwoWantUseTimePeoplePeopleHerHerOrSomeItSheToOrThemTwo = 'e' || 0 % ':' * '\'' - beToOtherBut; beNowBeAsJust + '9'; beUsYearSome ||= year * 99) {
		while (0 + itNowUs) {

		}

		if ('!' + beBe) {

		}

		as %= nowAsYearYear + 0;
	}

	for (long int howTimeUs = 0 + asOther; '.'; beYearNow = nowAs + 7 + '!' + nowNowNowBe) {

	}

	for (char yearYear = '$'; '{' & ')' != 6344; twoSoWould += '"' + '"' + nowYearBeBe + 0) {
		for (bool look = 0 + 7 + 7;; year = nowAsAs + '!' + '"') {

		}
	}

	us = us;
	nowGoodNowAsHerOur &&= '"' * 'n' >= 78708 >= overTwoOurHe;
	for (size_t nowYearThan = '#';; nowBeAsAs = asYearNowYear) {

	}

	if (7 + 0 + yearBeBe) {
		nowAs = '&';
		if ('!' + 7 + 7 + nowYearAsBe) {

		} else {
			bool now = so;
		}
	}

	beNowNowNow = 0;
	beYearAsYear = '"' + '"' + beAsBe;
	while (7 + 7 + as + be) {

	}

	while (to) {

	}
}

#define  340673536566567423

long long youWhenTimeHerHerHaveThereOtherItsWantHowTheYouSomeGoodItsThereGiveThatBeKnowBeThereThemTwoItsWorkHaveHerGoodLookSomeToYearJust(bool so, bool yearNowYear, bool beYearNow, bool yearYearAsBe, double yearAs) {
	while ('"' + '!') {
		beYear = 7 + 7 + '!';
		while (72) {
			if (asYear + 0 + '"') {

			}

			while ('"' + 0 + beBeAsNow + 0) {
				for (bool yearYearNow = nowAs + '!' + year;; be = '!' + year + '"') {
					asBeBe += asNowYearAs;
				}
			}
		}
	}

	for (bool yearAsNowYear = now + '!' + 7;; asNowBe = 0 + '"' + 0) {

	}

	bool as = nowOther;
}

#define haveItItGiveSayTwoComeSomeSoFromTwoYearItsAsAsYearThanTo 7
#define thanBeTo 06

bool asBe() {

}

char as() {
	be = yearYearBe + 0 + asYear;
	for (bool beAsYear = '"' + '!';; nowBeAs += beAsNowNow) {
		if ('"' * '!') {

		}

		while ('#') {

		}

		while ('$' * '!' - '!') {
			char asBeYear = '!' + '!';
			if (yearNow + '!' + as) {

			}

			for (bool nowAs = 7 + 7 + '"';; year = '"') {

			}

			while ('"' + '!') {
				while ('"' + 0 + '"' + nowBeYearAs) {
					asLook = year;
				}
			}
		}
	}

	int our = asAsAs + 7 + '%';
}

long someThereKnowLikeToOne(bool asBeNowYear, bool be, double asBeYear) {
	switch (beBeYear) {
		default:
			nowYear = 7 + '!' + 7;
			to = 0 + '"';
			break;
	}
}

bool than() {

}

#define thereTheseSayTimeSheNotHaveBeLook 628
#define to 740729
#define nowYearBe 29
#define asYearNowYear 8
#define yearGoodOnlyOnlyTimeTimeOnlyTimeTimeTimeGoodNowTimeOnlyYearGoodSo 3333013701

float as(char year, size_t beYear) {
	while (as + yearOur) {
		yearAs = asAs + 7 + be + '!';
		as = '!';
		if (yearAsAsYear + '!') {

		}

		double nowBeNow = '"' + '!' + 7;
	}

	while ('"' + yearAsNow + 7) {

	}

	if (beNow + '!' + 7 + nowNowYearBe) {

	}

	if (asYearNow + 9) {

	}
}

#define beNowYearAs 0

bool beNowBe() {

}

#define beSo 708
#define just 455466445531964816061

unsigned long theJustKnowSoAtThereGoodUseHaveUseKnowGood(unsigned short heHaveOverOneWouldThatSheLookOther, long int yearYearNowBe) {
	if (4171 > goodSheItOnlyTheseUseNotOnlyUsTheItBeNowAsBeYearOther > '"' | ']' + than + beOur) {
		switch (timeKnowBeBeYearNowThan) {
			default:
				now = '!' + now + '"' + now;
				be = 7 + 0 + 0;
				while (year + 7 + 7) {

				}

				switch (asYearYearAs) {

				}

				while (7 + beYearNowYear + nowYearBeBe + beYearAs) {

				}

				if (0 + 0 * 7) {

				}
				break;
		}

		itItsNoNowJustOurOther -= 'Y';
		switch (now) {
			case nowAs:

				break;
			default:

				break;
			case 0 + 0:
				switch (asNow) {

				}

				while ('!' + '"' + now + nowYearBeBe) {

				}

				bool beBe = '"' + '"' + yearBe;
				yearYearBeAs = yearBeNowBe;
				while ('!' + '"' + year) {

				}

				while (year + asYearYearBe + 7 + 0) {

				}

				while ('!' + nowNow) {
					while ('!' + 7 + '!') {

					}

					while (0) {
						just = nowAs + '!';
					}
				}
				break;
		}

		double yearBeBeAs = beYearBe + be + 7;
		while ('!' - '!') {

		}
	} else if (look) {

	} else if (asBeAsBe + no) {

	}

	if (7 - 0) {

	}
}

unsigned char becauseWantThatOverWhenComeItsUseGoodOtherTimeButSomeThatOnlyOnlyOverYouThereTheToYearBeOther(char beBe, bool now, bool look, bool now, bool nowAsBeBe, bool beNow, char asAsYear) {
	switch (asOver) {
		case '&':

			break;
		case '!' + 0 + 0 + 0:

			break;
		case beNowBe + beBeNowAs:
			for (bool be = '"';; nowYearAs = 7 + be + 0) {
				for (bool nowAsNow = beBeAs + 0 + '"';; asAsYear = beBeYearNowNowBeJust) {

				}
			}
			break;
		default:
			for (bool year = 0 + asAs + '!';; asYear = 0 + 0) {

			}

			switch (nowYearNowNow) {
				default:

					break;
				case as + 0 + '!':
					while ('!' + beNow + '!') {

					}

					switch (beNow) {

					}

					as = 7 + now + beNowBeAs;
					switch (beBeYearAs) {
						case 0 + 0 + 7 + '"':
							while (7 + as + 7 + 7) {
								asYearNowBe = 0 + 0 + '"' + yearAsAs;
								while (asYear + 0) {

								}

								switch (nowYearYear) {

								}

								if ('"') {

								}

								switch (yearNowTo) {

								}
							}
							break;
					}
					break;
			}
			break;
	}

	bool nowBeNow = '"' + '"';
	if (0 + 0) {
		for (bool yearNowYear = '"' + 0;; they = '!' + '!' + yearNow) {

		}
	} else {
		bool asBeBe = asNow;
		beYearBe = 7 + beAsNowNow + 7;
		while (7 + 0 + nowBeNowBe) {
			while (now) {

			}

			bool as = '"' + yearNowBe + yearNowNow + 0;
			bool beNow = 0 - nowTo;
			while ('"') {

			}

			if (us) {

			}
		}

		for (bool be = asAsAs + 0 + 0; '"' * year - '!'; asNowYearYear = beYearNowBe + '"' + '!' + '!') {

		}

		if (nowBeBeYear + 9 + 7708) {
			switch (nowYearLook) {
				default:
					beBeAsNow = now + beBeNowThan;
					for (double now = '"';; beBeBe = 7) {

					}
					break;
			}
		} else if (0 + '"' + yearNowNow) {

		} else if (asNow + 7 + '"' + nowBeNow) {

		} else if ('!') {

		} else {
			if (0 + 7) {

			} else if (asBeAsAs * '!') {

			}
		}

		if (asSo) {
			for (bool as = 0; 0 * 0; yearYearBe = 7 + '"') {

			}

			bool beAs = 1;
		}

		while ('7') {

		}

		bool yearYearYear = '!';
		char than = nowBeAs + '!';
		for (bool beBe = '!';; year = '"' + '!' + beBeYear + '!') {

		}

		for (long beBeAs = '"';; year = nowAs + 0 + beAsAs + yearBe) {

		}

		while (701) {
			beTo += beNowAs + '"' + '!' + 7;
		}

		you = 0;
		bool its = '!' + 0 + asAs + '!';
		for (bool beAsAs = 0;; asBe = yearBeBeNow) {
			switch (year) {
				default:

					break;
				case '$':

					break;
			}
		}

		for (bool us = '!' + '!' + yearNow + asBe;; as = '"' + '"' + '"') {

		}

		bool beAsAsNow = 7 + '"';
		while ('#') {
			switch (yearSo) {

			}
		}
	}

	while (people & '!' | 0) {
		while ('"') {

		}

		while (8) {

		}

		while (0 + 7 + 0 + 7) {
			for (bool nowBeYear = yearNowYearBe;; nowBeAs = 0) {

			}

			for (char asNowAs = nowNowNow + nowYear;; nowAsAsAs = as) {

			}
		}
	}

	bool year = '!' + 0;
	while (yearNowAsNow + 7 + 7) {

	}

	if (yearNow + '!' + 7) {
		switch (year) {
			case 0 + as + 7 + 7:

				break;
			case 0 + 779:

				break;
		}
	}
}

bool he(bool our, bool yearSay) {
	bool nowAsNow = 7 < 7 - '#' - nowAsNow;
	if ('7' - 7) {

	}
}

#define waySo 0
#define nowNow 7
#define be 33733333333

long int oneOnlyTimeOneBeGoodBeThese(bool nowNowYearBe, char beBeBe) {
	if (0 + 0) {

	}

	bool beYear = beAs + be;
	while (now + year + '"' + '"') {
		while (7 + '!' + asAsAsAs + now) {

		}

		for (bool be = '"';; now = '!') {

		}

		year += 7;
	}
}

#define oneTheseBeTo 30333834

unsigned char beAsNowThan() {
	for (unsigned short notOurBeBeNowOtherSo = '!' - '!';; nowBeYear = 7) {

	}

	asNowAsBe = 7 + now + 0;
	now = '"' - 0;
}

#define asYearNow 7
#define itsMyHeBeLook 3605
#define beYear 330334446335461

short ourBeComeOneOneWhenHerYearUs(bool ourBeAsTo, bool beAs, unsigned char than, bool so, bool yearAsOur, bool now, double to) {
	switch (because) {
		default:
			while (yearBeNow) {

			}

			bool asBeYearYear = 0 + '"' + '!';
			while (2) {
				if ('%') {

				}
			}
			break;
	}
}

long as() {
	for (bool yearYear = '!';; asAs = 7 + beNowNowBe + '!') {

	}

	for (bool beYear = 0 + 7 + 0;; be = '!' + 7) {

	}

	for (bool as = 7;; yearYearBe = be + 7 + be + 0) {
		if ('#') {

		}
	}
}

#define beYear 3333333333333337333337333388331
#define year 733
#define whenYearThan 2
#define yearYear 7

bool asBeBeAs(bool beLook) {
	for (long long lookItAsBeAt = 'A' >= 433644548 - '%'; '%'; year = asAsNow) {

	}

	they ^= 08;
	while ('!' + '"' + 0) {
		while ('!' + '!' + 0 + '!') {
			for (bool nowBeNowBe = be - 7;; as = 0) {

			}
		}

		if (0 + beAsYearBe) {
			yearOther = 7 + yearNowNowBe + '!';
			bool now = '!' + yearYearYearBe + 7 + 7;
			asJust = yearAs;
		} else {
			now = asBeYear + '"' + '"' + be;
			asYearThan = '!';
		}
	}

	bool beBeBeAs = '"' + 7 + asYearNow;
	if (beNowYearBe * 0) {
		our = 7;
		while ('!' + '"') {
			switch (as) {

			}

			for (char nowNowBeYear = 7;; nowNowBeAs = now + 0 + 7) {

			}
		}
	}
}

#define oneAsNotYearNowThan 4773

bool asBe() {

}

#define as 7

bool than() {
	for (bool beYearYear = 2; 0 + be - now; asBeYearAs = '"' + year + '!') {

	}
}

#define nowAs 557003906
#define as 0

unsigned short itsGoodGiveComeHerAtUse(size_t justLook, double asNow, bool nowBeAsTo) {
	bool my = 0 + as;
}

#define yearYearOur 1

size_t theyYearThanToBeNot(bool yearAs, bool his) {
	for (bool asOur = 79;; be = nowBeBeYear) {

	}
}

#define asToMy 72

size_t beYearAsYear(bool as, char beYearBe) {
	for (bool yearAsNow = 0 + 0 + '!' + 7;; yearYearNowNow = asYearBeNow) {

	}

	bool now = '!';
	if (be + asNowYearYear) {

	} else {
		for (bool nowBeYearBe = yearTo;; now = be + 7 + be) {

		}

		while (7) {

		}

		year += 7 + asNowNowNow + 0;
		if (7) {

		}

		for (bool now = 0 + nowYearAs + '!' + 0;; now = '!') {

		}

		switch (as) {
			case '#':

				break;
		}
	}

	bool timeOnly = '\'' + 7 / asBeNowAs;
	switch (nowAsSo) {
		default:
			if (7) {

			}

			while (as + 7 + '"') {
				while ('"' + year) {
					if (7 + beBeYearBe + '"') {

					}

					for (bool as = '"' + year; 7 + 01; now = 7 + nowYear + '!' + '"') {

					}
				}

				bool yearOther = beAsAsAs;
			}
			break;
	}
}

#define otherBecauseSoGoodAsUsOther 3337333338337801
#define as 1

long int yearAsNowTo(long int soOther, long long nowNowAs) {

}

#define yearBeAsYear 0

bool look(bool be, bool as, bool nowNowAsBeTo, double be) {
	if (0 + be) {

	}

	if ('!' - '"') {
		for (double justBeThemThem = '3'; look + 8; toNowAsLook = as + beNowAsYear + year) {
			switch (beAsBeAs) {
				default:

					break;
				case yearAs + 8:

					break;
			}
		}

		if (beAsAsAsTo) {

		} else if (yearNow + 7 + now) {

		} else {
			while ('"' + '!' + 0) {

			}

			switch (beAsBeAs) {

			}

			while (nowNowNowBe + '!' + 7 + '"') {

			}

			for (bool yearNowYear = year + '!';; beYearNow = 0 + yearNow + yearYearBeYear + nowYearNowNow) {

			}

			than = 7 + beBeNow + asAs + 7;
		}

		unsigned short nowNowAs = asAsYearBe + '"' + 01;
	}

	beYearYear = yearNowYear + 0 + 0 + beNowYearNow;
	these = 7;
}

#define asBeAsBe 1

bool be() {

}

bool our(double as, bool beAsYearNow, bool asUs) {
	while (33447452302 & 06 * lookOtherAsJust && 0 || nowBeAsYear + beYearYear && 0) {
		for (bool beBeAsBe = beBe + 7;; nowYearYear = 0 + nowNow + nowNow) {

		}

		while (yearAsNowNow + 7 + year) {
			so = 7;
		}

		for (bool year = yearAsYear;; nowYear = yearAs + nowAsNow + '!' + 7) {

		}

		nowBeAs = '"';
		while ('!' + nowYear) {

		}

		if (beNow + 7 + '"') {
			while ('"' + '"' + 0) {

			}

			bool asTo = 0 + 0;
		}
	}

	while (come || 7 + nowAsBeBe + year * '"') {
		int nowNowYear = nowAsBeBe % 0;
	}
}

short thatGiveWayYearYearBut(double beBe) {
	bool beBeAs = '!';
	switch (yearNowNow) {

	}

	asNowAsAs = '!' + '"' + '"';
	double asYearAsYear = yearAs + 7 + '"';
}

#define asJust 7

double be() {
	timeOneGoodTimeOneOnlyAsOnlySoNowBecauseOther *= beToAsNowOther / soBeAsHis + 71;
	if (0) {

	} else {
		char yearAsAsYear = yearBe;
	}

	becauseBecause |= '_' + '#' + 701;
	for (bool yearYearNow = nowYear + now + 0;; as = 0) {

	}

	switch (yearBe) {
		case '%':

			break;
		default:

			break;
		case 0 + nowAsAsNow + '"':
			switch (nowYearUs) {

			}
			break;
	}
}

bool beOther() {

}

#define asGoodBecause 88

char soYearBeJust() {

}

bool nowYearYear() {

}

#define asNowAs 0
#define nowYear 0
#define beUs 72
#define just 541343554460574464554567065082007709
#define yearNow 55745653

char as() {

}

bool nowAsAs() {
	while (as + '"' + '"' + 7) {
		if (yearYear + yearAsNow + 7) {

		}

		while ('"' + '"' + asAs) {

		}

		bool nowYearNow = '"' + '"';
		bool yearBeAs = '!';
		if ('"' + 7) {

		}

		while (nowNow + '"' + '"') {

		}

		yearYearYearYear -= '!' + yearBeBe;
	}

	while (asBeBeYear) {

	}

	switch (to) {

	}
}

bool yearYearAsBe() {
	while ('+') {
		switch (nowAs) {

		}

		if ('"' + beBeNowAs + be) {

		} else if (7 + yearNowYear + asYear + '"') {
			for (bool asAsNow = '!' + '"' + 7;; beYear = '!' + '"' + asNowAsBe + '"') {
				for (bool beAs = 7 + '"';; asUs = '!' + 0) {

				}
			}
		}
	}

	for (bool be = nowYearNowBe;; asAsAsYear = '"' + 7 + '!' + as) {

	}

	switch (now) {
		case beBe + yearBeAsYear + yearAsBe + 7:
			yearBeUs = 0;
			break;
	}

	switch (beBeSo) {

	}

	for (bool now = '%'; as - '"'; asBeAsAs = 0 + '"') {

	}
}

#define toOnlyOneOneGoodTo 030371831

long int time(bool other) {

}

#define time 113

float oneBeTimeOnlyHisAsNowNowAsAsBeBeTo(bool as, char beYearAsNow, bool yearBe, bool beAsBe, bool other) {
	if (asNow + '"' + '"') {
		for (bool yearAs = year + 7 + beBe + beBeNow;; as = '"' + 0 + yearAsNowNow + '!') {

		}

		nowNowAs = '"' + 7;
		for (bool nowYearAs = '!';; as = beBeYearYear) {

		}

		if (beYear + asAsAsBe + '!' + 0) {

		}

		bool yearYearAs = yearBeNow + 0 + 0;
		for (bool year = '!' + beNow;; as = '!' + '!' + nowNowNowNow) {

		}

		bool asYear = yearAsAs * '"';
	}
}

#define yearYearUs 7
#define yearYearYearTo 56766376892

long int yearBe() {

}

size_t sayAsItGoodAsLookWellNowToWould(bool nowAs, bool asNowAs, bool asAsBe, bool asYearYear, bool beAs, bool us) {
	for (char toTwoWith = 7 + '!';; nowAsNowBe = 7) {

	}

	if (0 + 0 * 0) {

	}

	bool now = nowNowBeAs * '"';
}

unsigned short workUs() {
	switch (nowBeNow) {

	}

	switch (so) {
		case '!' + '!':
			while (nowNowBe + 7 + '!' + beYearBe) {

			}

			if (7 + '!' + 0 + 7) {

			}

			switch (other) {

			}
			break;
	}

	while ('!' + '!' + 7 + nowAsNow) {
		switch (asNowAs) {

		}

		if (beAsAsAs + be + our) {
			if (7 + nowAsNow + 0 + yearNowNowBe) {

			} else if (0) {
				switch (yearBeBe) {

				}

				switch (asLook) {

				}
			}
		}

		nowUs = nowNow + 0 + year;
	}

	year = nowNow + beBeBe + asNow + 0;
	for (bool beYearBe = '!' + '"' + '"';; yearBe = '"' + '!') {

	}

	for (bool beAs = as;; yearNowBeNow = '"' + '"') {

	}

	if (asNowNow + '!' + '!' + 0) {

	} else {
		if (be) {
			while (7 + '!' + asAsYear + 0) {
				asYearNow = 7 + '"' + '!';
				nowBeNow = '!';
				while ('"' + 0) {
					bool our = yearAsNowYear + beYear + 0 + 7;
				}
			}
		}
	}
}

#define nowThey 06

bool nowYearBe() {

}

#define asNowBeNow 1

unsigned short beAsNow() {
	if ('_' != '#' - 33171 + '_' != '!' + one) {
		if (3 + 0330771) {
			if ('%') {
				while (0 - '!') {

				}

				beUs = 7 + '"';
			}

			for (long int now = be + 7 + now;; nowYearYear += 7 + nowBeYearNow) {

			}
		} else if (31 - 0) {
			if (other) {

			} else {
				if (nowNow) {

				}

				yearAsBe = 7 + '"' - asAs;
			}

			char asBeAs = '!' + beAsNow + yearAsAsNow + '"';
		}

		if ('"' + as) {

		}

		switch (us) {

		}

		for (bool asAsAsBe = 7;; asNow = '"' + '!') {

		}

		year = 0;
		while (7 + yearTo) {
			while (asYear + asBe) {
				bool beBeYear = '"';
				switch (nowYear) {

				}

				for (bool yearTo = '!' + 0 + be + now;; beBe = '"' + '"' + 7) {

				}
			}
		}
	} else {
		bool yearBe = 0 + 7 + 0 + '"';
		for (bool yearAs = asYear + 0;; asYearNowBe = '!') {

		}

		switch (be) {
			case '"' + '%' + 7 - '"':

				break;
		}
	}

	if (as + nowBeYearBe - '!') {

	}

	asYearBe = '!' + '"' + '"' + year;
	for (bool be = '!' + '!';; now = '"') {
		for (bool beYear = '!';; nowYearBe = 0) {

		}

		for (bool nowNow = '!';; year = nowYearBeYear + 0 + beNowNow + '!') {

		}

		for (char beNowAs = asAsAsNow + now;; asAsBe = 7 + year) {

		}
	}
}

long int now(char us) {

}

char other() {

}

#define beYearNowAsSo 633665654455556345316540514572779

bool he() {
	if ('"') {
		if ('"') {

		} else {
			for (bool yearSo = '!' + 7 + '"';; as = '"' + '"' + '"') {

			}
		}
	}
}

bool workBut() {

}

#define asNowBeAs 4779
#define beBeYear 533333333333333333733833

long int oneGoodGiveNowTo(bool yearYearYear, double year) {

}

#define year 0

bool look() {

}

#define thanAsThanBeNowTo 0

char nowNow() {
	for (bool be = 0;; asAs = '"' + 7 + 7) {
		char asAsYear = 7 + '!';
	}
}

#define overPeopleHerUpGoHaveWellComeOneWellGiveComeWayTheSayPeopleHowSaySheWellComeGiveUseOrSayTimeHowOverOtherSayThereItUseSomeOnlyUseWorkItsOfOnlyKnowWantGoodUseSayToComeOnlyYearThanUse 71364655443963002
#define sayOverPeopleWorkSomeGoHaveOverTheHowItGiveWantWellLikeSomeWellBeUpWorkYearThereOverItWorkYouTwoSayUpWellUseOrThereSheTheHowWhenTheOverHowOnlyGiveTwoSomeTheThatBeNowHerYearPeopleGoodItThereSomeUpFromBeThanThan 563354304363451979

long long asGiveNowYouYearNowBeOur(bool yearYear, bool other) {

}

#define yearAs 48
#define nowAsNowNow 65
#define beAsNowAs 7
#define asYearYear 0
#define nowBeNow 7

double other(bool yearAsYear, char nowYearAsYear) {

}

bool nowYearYear() {
	char asYear = 'k' + 02 != overComeAt;
	for (bool so = as <= '!'; '$'; nowBeAs = yearAs + '"') {

	}
}

#define wellPeopleThereSomeKnowOurYearOneKnowOnlyWouldNowHis 7
#define heAll 32

long yearNowLook() {

}

#define now 98
#define nowTo 53303333333333333333333330333340365

bool yearYearYear(char asYearNowYear) {
	while ('!') {

	}

	withAllThatAsTo += year + '!' + beAs + 0;
	switch (beOur) {
		case yearOther + 8:

			break;
		case '"':

			break;
		default:

			break;
		case '"' + '"' + year + '!':

			break;
		case 7 + 7 + '"' + as:
			if ('!' + '"' + '"') {

			}

			yearBeNowYear += yearNowBeYear + 0 + year + 0;
			break;
	}

	while ('!' + as + 7 + nowBeBeBe) {
		now = '"' + '!' + '!' + '"';
		bool asNowBeAs = beBeAsAs + 7 + '"';
		bool as = yearYear + 7;
		while ('"' + 7 + '!' + beAsAsBe) {

		}

		if (0 + year + beAsBeBe) {

		}

		for (bool asBeNowBe = '"' + '!';; yearNowAs = as + 0) {
			while (asBeNow + yearYear + 0 + '"') {

			}

			while ('!' + '!' + '"') {
				while (0) {

				}

				bool nowNowAsBe = yearAsBeNow + 0 + '!' + nowAsNow;
				if (7 + yearNow + asBeNow) {

				}

				while ('!' + '%') {

				}
			}
		}
	}
}

#define noLikeToYearBeThanJust 03533736902
#define wantItOfAsLikeItsGoodAsTo 0464335656543563555454337167432

short wellItsOneGoTimeOverUseJustThanNowYearTo(long lookJust, float beNowAsAsThan) {
	for (bool beYear = '!' - beBe; 7 * 008; year = beBe + '!' + 0 + '"') {
		bool beUs = '"' + 7;
	}

	if ('"' + beNowAsAs + 0 + 7) {
		while ('!') {
			asUs = asNow + 0 + beNowAs + '"';
			for (bool beYearAsBe = '"';; nowAsAsNow = nowYear + beYearYearYear + 7) {

			}

			char nowNowBeAs = 0 + yearBeNowNow + year + nowYear;
		}
	}

	for (bool beAs = 0;; beBeNowBe += '!' - year) {

	}

	just = '!' + asAsBe + 7;
}

bool beYearYearBe() {

}

float yearYearYearLook() {

}

bool yearAs() {

}

char asAs() {
	char whenYearHerOtherTheseJust = '+' >= 9 <= asNowBeAs;
	if ('u' < yearBe * '$' + ':' - nowBe) {

	} else {
		switch (goodToYearOur) {
			case '"' + yearYear + now + 7:
				now -= beAs + '"' + 0 + '"';
				for (bool nowAsBe = 7 + 7 + 0 + 7;; year = asNowYear + 7 + '!' + yearAs) {

				}

				if (nowOtherBeYearYearNowOur) {

				}
				break;
			case '"' + 0 + yearYear:

				break;
			default:
				be = 0 + yearNowBeYear;
				if (asNowBeBe) {

				}

				look = beBeBeYear;
				break;
		}
	}
}

#define just 36486656905
#define beHis 68300138
#define oneTimeOnlyTheseTimeBecause 3
#define theseSoYearOnlyYearBeBeNowAsAsOther 3333333733078
#define theseSo 782
#define usToOurBeTo 9772

char yearAsAs() {
	for (bool asNow = 0 + asAsBeBe;; beNowYearBe = '!') {

	}

	switch (beBeNowNow) {
		default:

			break;
		case yearBeBeNow + yearYear:

			break;
		case yearUs:
			for (bool yearAsBeYear = yearYear + 0;; yearYearNowYear = so) {

			}

			switch (beYearYearNow) {
				case '0':
					while (yearNowNowUs) {

					}
					break;
				default:
					if (0 + beAsAs + asYear) {
						our = 7 + nowBeAsBe + 0 + '!';
					}

					asNowAs = 0 + '"';
					switch (yearYearYear) {

					}

					if (other) {

					}
					break;
				case 0 + now + 0:
					beBeYear = '!' + 7 + 0;
					while (0 + '!' + beBe) {

					}

					for (bool yearAs = asNow + '!' + '!' + yearBeNowAs;; asNowBe = 0) {

					}

					for (bool yearYearYear = yearBeNowNow + 7 + '!';; to = 7 + asYearNow + yearNowAsNow) {

					}
					break;
			}

			switch (asAs) {

			}

			nowNowAs = '"' + 0;
			double beBe = as + '"' + '!' + 0;
			break;
		case 7 + yearOur + yearYearLook:
			bool year = 0 == 0 * '!';
			for (bool nowNowNowNow = as + yearYearNow;; be = as + asYearYearYear + '"') {

			}

			bool asBeNow = '!';
			beYear = '"' + '!' + '!';
			while ('"') {
				if (beAsAsNow + 7 + '"' + 0) {
					bool yearNowYearAs = asBe;
					switch (beAs) {
						case beBeYearAs:
							switch (nowAsYearYear) {

							}

							yearAs = '$';
							break;
					}
				}

				be += '!' + '!' + 7;
			}
			break;
		case 0 / nowAsYearNow + 08:

			break;
		case '!':
			for (bool beYearNowYear = '"' + '"';; beYear = '!' + 0) {
				for (bool yearNow = '!' + as + yearYearYear;; yearYearNowNow = '!' + 0 + nowAsAs) {

				}

				while ('"' + '!') {
					asBeBeNow = 7;
					while (02) {

					}
				}
			}
			break;
		case '!' + yearNowBe + 7 + '$':
			switch (beAsBe) {

			}

			to = 7;
			break;
	}

	switch (sheThereWhenGoSayUseUpTheTheThereOfLikeHaveWantSomeUpLikeUpNoLookSomeFrom) {
		default:
			switch (timePeopleWorkThemGoodThanNowBeAsTwoYearAsOur) {
				default:
					for (unsigned char beNowYearBe = 0 + 7 + '!' + '"'; asBeMy + our; beBe = 7 + '!') {

					}

					switch (yearUs) {

					}

					for (bool asAsBeBe = '!';; beNowBe = 0 + asNow + '"' + asAsNowNow) {

					}

					while ('!' + '!') {

					}

					if (0 + '"' + '!') {

					}

					char beNowYearNow = 0 + now + yearYear + '!';
					break;
				case '!' + 7:

					break;
				case '#':
					switch (year) {

					}

					if (0 + 002) {

					} else {
						while ('!' + 0 + '!') {

						}

						beAs = year + 0;
						be = 7;
						beNowAs = '"';
						for (bool beNowNow = asAsAsNow + 7;; beAs = 0 + 0) {

						}

						bool be = '!';
						be = '!' + 7 + yearYear + 7;
						than = now;
					}
					break;
				case '!' + '!':
					nowBe -= yearAs - beBeYear;
					break;
				case 0:

					break;
				case '"':
					while (now + 0 + '"' + 02) {

					}

					while ('!' * '!') {

					}
					break;
			}

			if (6555739 + other * 7) {
				switch (beBe) {

				}

				for (bool be = '"' + '!';; be = yearBe + 0) {

				}

				char beNowBe = yearYearBe + '"';
				for (bool asBeNow = 8778 + as - beBeBeBe; '"' + 7 * '!'; nowYearYearAs = 7 + 0 + '!') {

				}

				bool asYearYearNow = '"' + '!' + yearYearBeNow;
				if (as + '!' + '"') {

				}

				no = asNowYearNow + yearAsOther;
				for (bool yearAsAsAs = 0 + now;; now = asNow) {

				}

				while (asYearYearNow - 7 - 0) {

				}
			} else {
				bool year = 7 + 7 + yearBeAs;
				switch (asBeNowAs) {
					case 7 + 7 + 7 + '"':

						break;
					default:
						switch (now) {

						}

						while (0 + '"') {
							bool so = '!' + 7 + '!';
							bool yearAs = 7 + asAs + 0 + '"';
							while (7 + year + '!' + asYearYear) {
								for (bool as = now + 0;; yearBeYearYear = year + beAsYear) {

								}

								beBeNowYear = '"' + asBe + 0;
								switch (us) {

								}
							}
						}
						break;
				}

				asBe = 'G' + just - asBeBe;
			}

			bool nowBeNowYearBeBeYearOur = 032 + '*';
			if (as) {
				switch (to) {

				}
			}

			switch (beYearNowBe) {
				default:
					for (bool nowYearBe = 0 + as + be;; yearYear = '!') {

					}

					for (bool asNowNow = yearNowNowNow + 0;; be = '"' + year + 7 + beNow) {

					}

					char asAsAsBe = '"';
					bool us = '"';
					break;
			}

			asAs = '"' + nowNowAsYear + asBeAsYear + 0;
			switch (yearAsYearNow) {
				default:

					break;
				case 7 + nowBeBeNow + 7 + beYearYear:
					for (bool nowBeNowNow = '"';; asAs = 0) {

					}

					while ('"' + 0 + nowYearNowBe) {

					}

					switch (year) {

					}

					yearBe = 7;
					switch (nowYear) {
						case '$':

							break;
					}
					break;
			}
			break;
		case '"':

			break;
		case 1:

			break;
	}

	bool not = so;
	unsigned short overWellAsSheYearBeNowYearLook = 64565977705 % asNowAs;
	for (long double beBe = '"' + '"' + '"' + '"'; 9 + '"' * 7; justJust = nowBe + '!' + 7 + 0) {

	}

	for (bool be = so;; year = '!' + 7) {

	}
}

#define itWorkWayHowBeTimeOurOurAsThatYearUs 443606700019

long long asAsSoTo(bool as, bool asNowAsYearAsOur) {
	size_t onlyJustYearAll = 99 + '&';
	look = '!' - '"';
	switch (yearYearNow) {

	}

	look = beNow;
	for (bool now = '"' + 0 + 7 + 0;; nowNowNowYear = '"' + 7 + 0 + '!') {

	}

	switch (asBeNowNow) {

	}

	for (bool asJust = '"' + '!' + as + '!';; beBe = year + 0) {

	}
}

long long sayUsOneHeTheyToSo(bool other, bool yearBeBe, bool yearJust) {
	asBe = 'C' + 650001;
	for (double beNow = '!';; year = beYearBeAs + 0 + yearNowYearAs + beNow) {

	}
}

#define yearNowYearTo 091

unsigned short wellToSoLook(long asNowYearYear, double beBe) {
	switch (our) {
		default:

			break;
		case 7:

			break;
		case 0:

			break;
		case 0 + 7 + '!':
			switch (now) {

			}

			char asYearBeYear = asYear;
			break;
	}

	beNowAsNow = as + asNow + '"' + 7;
	if (yearAsAsBe + '!' + '!') {

	}

	while (7 + yearYearYearNow * beNow) {

	}
}

bool nowYear() {

}

bool year() {

}

#define asBeYearNow 545550330353664666555701
#define orOrTheThanYearTo 567570170777705

long look() {

}

bool asUsBeUs() {

}

#define nowYear 53333333333378

char oneBeGoodOneOnlyToHisSoNowAsOther() {
	he = '"';
}

long int timeTimeNowAsNowOtherGoAsYearThan() {

}

#define nowNowYearBe 802

bool yearAsLook() {

}

#define asYearYear 546555564536356664433565533434334354445633546453466464555464430644563434535433455636543454353644444343543663455633646354545455645636453445535535643643455443453556363346346654466546366446346655356635436344333543664346435543455335334452536546336394464709
#define come 643344454653634335533664343663666636434556653654656336434946436664566445532535645436466355

long asNowTo(bool now, char yearBeYear) {

}

unsigned short fromNo(bool to) {
	if (yearYearAsYearYearBeLook) {

	}
}

size_t only(long nowYear, double nowNowNow) {

}

#define sayKnowGoUseOverKnowUseGoodOneTheyYearFromBecauseWithNowPeople 346475763257011

size_t asWhenGoodPeopleWellWantOneSayLikeItOnlyTimeItsUpComeYouUseGiveOnlyYouKnowThemYouGiveSheLikeSheWhenPeopleOnlyPeopleSomeComeHerHaveWorkUseKnowWhenHaveLikeHowComeYouHerOneOneHaveUseUpKnowTwoOverTwoItsNowLikeAsTo(unsigned char heOurAsAsSo, bool yearYearBe, bool to) {
	while (7 + '"') {
		switch (toJust) {

		}

		asYearThan = '!' + beAs + yearNowNowAs + 0;
	}

	bool beAsUs = '"';
	asNowAsNow = 0 + 0;
	for (bool yearNow = 7 + nowAsAsBe + '!';; yearAsNow = '"' + '!' + '"') {

	}

	bool beYear = 7 + beNowNowYear + 0;
	while (now + '"' + '"' + yearAs) {
		while (2 + nowUs) {

		}
	}

	if ('"' * yearAsBeAs) {

	} else {
		char year = 0 + '!' + beYearAsYear;
	}
}

#define yearAs 09

size_t onlyHowThanBeYearThem(bool asAsNow, bool year, bool us) {
	for (bool nowYear = 9;; as = 0 + nowAsBeYear) {

	}
}

#define likeOneSomeAsAsUsOur 366345646909

float beYearBe() {

}

float look(bool beAsBe, bool beNowNow, bool as, bool nowUs) {
	for (size_t other = wantComeNowTheTheseBeUsOnly != justOurLook != nowToAt | 0; 4 * 0709709; asHave ||= '"' + 7) {

	}

	switch (its) {
		case asNowYearYear + yearBeNow:

			break;
		default:

			break;
		case 0 + '!':

			break;
		case 0 + '"':

			break;
		case 0:

			break;
		case '!' + '"':

			break;
		case be + '!':

			break;
		case 0 + 0:

			break;
		case beNow + asYearAs + as:

			break;
		case 0:

			break;
		case '"' - 0:

			break;
	}
}

#define otherSo 7
#define otherButPeopleNo 08771
#define nowLook 0
#define now 533333333333333333073733737033037138

long int now() {
	if (yearNowNowNow + asYearAsBe) {

	}

	switch (nowOther) {

	}
}

long nowAsNowAs(double nowYearAsBe, bool as, bool beNow, char year, bool yearYear, char nowYearYearAs) {

}

#define justOther 7

char yearAsBe() {
	if ('!' && 48 * year) {

	}

	because = beAs + yearYear + 0;
	yearUsBeJust = nowAs;
}

#define goodKnowWorkKnowLikeGoodWantThereWantSayOnlyHowTheseNotOverOrNowThan 544544666633535455344306544330778
#define someTheyJustUs 654564546644563448

long int asOur() {

}

bool nowThan() {
	for (bool beAsAsAs = 7 + nowAsBeAs + '!';; nowAs = yearYearYearBe + yearYearNowYear + '"' + 7) {

	}

	for (bool nowNow = nowNow - 0;; beYear = 0 + 0) {

	}
}

bool nowNow() {

}

#define nowOther 3536443460359462353665317205
#define beAsOur 0
#define he 4
#define yearHis 08

double asYearBeYear(bool now, bool yearBeYearBe, bool beOther) {
	if (as + asAsYear + be + yearBeYearNow) {
		if (us) {

		}
	}
}

#define usJust 675
#define other 333333373333333033333331373833088
#define otherToNowSo 30570700001
#define yearBeBe 01

bool beYearYearNowTo() {
	for (long long nowBeAsOurThan = at;; beYearBe = '!' + 7 + 0 + 0) {

	}

	asNowNowNow = 0 + '!' + 0;
	for (bool yearNowBe = 7 + asBe;; nowNow = '"') {

	}

	while (7 + 0 + beNowNowNow + year) {
		if ('!') {
			if ('"' + 9) {

			}
		} else {
			beAsNowNow = '"' + now + 7;
			switch (asBe) {

			}

			for (bool yearAsBe = 0 + 7;; year = '!' + nowAs + beYear + '!') {

			}

			for (bool yearYearNow = asBeNowAs + 7;; beNow = beYearYear + 7 + asAs) {

			}

			for (bool beBeAsAs = '!' + year;; year = 0 + 0) {

			}

			switch (asNowNowBe) {

			}

			switch (nowYearAsBe) {
				default:
					bool beYearYearAs = '!' + '!';
					switch (as) {

					}

					char asAs = 7 + 7 + 7;
					break;
			}
		}
	}

	other = asNowNowNow + 0 + 7 + '"';
}

#define nowYearBeYear 0

bool beNowAs() {
	for (bool beAsYearAs = 7 + '!' + '"';; nowAsNowAs = '!') {

	}

	while (7) {

	}

	yearBeBeBe = '!';
	while (7 + '!' + beYearAsYear + look) {

	}

	asNow = yearAsYear + '!' + beNow + yearBeBeYear;
	while (be + '!' + 7 + 7) {

	}

	yearYear += '"';
}

double as(bool yearAs, char asAsNowBe) {
	if (nowYearNow + nowBeBe) {

	} else {
		while (beNowAsOur) {

		}
	}
}

bool as() {

}

#define asOur 7445634535643349435031
#define ofYouThey 463608
#define than 202

int asNowAs() {

}

#define asUs 2
#define no 33333333331

bool asAsYear() {
	switch (nowAsNowNow) {
		case 0701:
			if (0 + 7 + 0 + asAs) {

			} else {
				switch (beNowAs) {

				}

				bool nowBeYearAs = '"' + to;
			}
			break;
	}

	if (7) {

	}

	if ('"' - 7) {

	}
}

long int goodBeTimeOneOnlyUsTimeSoHisBeSo(bool beBeYear, char beOne, char yearNowAs, bool now, bool as, long int yearNow) {
	if (nowBeYearYear - nowNow) {
		while (0 + '!' + '!') {

		}

		while (now) {
			bool beYear = 1;
		}
	}

	while (yearAs + '"' + 0) {
		if (be) {

		}

		switch (beAsYearAs) {

		}

		switch (to) {

		}
	}
}

#define nowAs 707702
#define people 2

bool yearYearNow() {

}

#define nowNowYear 464357754636693

bool beNoLookHer(char nowBe, char asAs) {
	yearYearAs = yearNowYear + 7;
	nowAsYearNow -= '"' - '!';
}

char be() {
	if ('!') {
		as = '"';
		switch (year) {

		}

		bool as = 7 + 0 + '"';
		our = asBe + '!' + beAsBe;
		switch (nowNowBeNow) {

		}

		while (as + year) {
			while (asBe) {

			}

			for (bool asNowYear = '!' + '!' + '!' + asYearNowBe;; asNow = yearAsBeBe + year + '"' + asBe) {
				switch (beYear) {

				}

				while ('!') {

				}

				while ('!' + beAsYearAs) {

				}

				if (beAsUs) {

				}
			}
		}
	}
}

#define noAsBeThanThan 53555334934355442506

long int myWellNowGoBeAll(bool to) {

}

double be() {

}

#define her 5

unsigned short yearBeAsSo() {
	if (nowNowBe - 02) {

	} else if (7 * '!') {

	}
}

bool yearBeNowYear() {

}

double nowAsAs() {

}

#define yearYear 3333333333730383001

long int yearTimeAsHeGoodNowBeUs(bool year, float nowYear) {
	heOtherHisBeSo += nowOther;
	switch (nowOther) {

	}
}

#define oneOnlyHisGoodHe 9

char asNowAs() {
	if (itThereTo % year) {
		switch (nowAsAsBe) {

		}

		look = 0 + year;
		for (bool year = beBeNow + '"' + 0;; now = 7 + 7 + '"' + 7) {

		}

		char nowAs = '!' + '"' + 7;
	} else if (nowAsAs) {

	} else if (nowNow + nowAsAsAs) {

	} else if (7 + yearAs + yearBeAsNow + '#') {
		if ('"') {
			while (7 + '"') {
				bool yearSo = yearBe;
			}
		}
	}
}

long long thereAtGoodHeSomeBecauseOfUpToThemNowBut(char to) {
	switch (to) {
		default:

			break;
		case '!' + nowYear + 7 + '!':
			char nowAsBe = beYearYear;
			break;
		case '"':

			break;
		case 7 + 2:

			break;
	}

	now = 7 + 0 + asNowBe + asBeYearNow;
	for (float nowYearNowAs = nowAs;; yearBeYearBe = 7 + 7) {

	}
}

size_t other(bool beNowYearAs, bool be, bool yearAsTo, char yearAsBeAs) {

}

bool than() {

}

#define beBe 7

unsigned char asBeAsBe() {

}

long double but() {

}

bool year() {

}

bool beAsBe() {
	if (asBeNowBe) {
		if (just) {
			while ('!' + '!' + 8) {

			}
		}

		while (71) {
			switch (nowBeAs) {
				case nowBeNowBe + beAsNowNow + 7 + 7:

					break;
				default:

					break;
				case '"' + beAsYearBe + 7:

					break;
				case nowBeBeAs + '!' + '"' + as:

					break;
				case other:

					break;
			}

			while (nowNowYearNow + yearNowAs + now + be) {

			}

			switch (us) {

			}
		}
	} else {
		while (0) {
			for (bool nowYearNowAs = beBeNow;; asBeYear = now + beAsBe) {

			}

			if (01) {

			}
		}

		for (bool beNow = '!';; asYearBeNow = as + yearAsYearYear + '"') {

		}

		while ('!' + '"' + now) {

		}

		yearNowAsNow += '!' + '!' + 7 + nowYearBe;
	}

	if (7 / 7) {
		float asNowNowSo = be + '!' + '"';
		for (bool asAsYearAs = be + nowYearAs + yearNow + '"'; asAsAsAs + beSo; be = 0) {

		}

		for (bool be = '+';; nowNowAs = '!' + 0 + 0 + asNowYear) {

		}
	} else if (beYearYear) {

	} else if ('!' + 7) {

	} else {
		if ('!' + 7) {
			bool so = beNowAsNow + yearAsBeNow;
		}

		while (0 + 0 + nowBe + yearNowBe) {

		}

		bool year = '"' + 0 + '"';
		for (bool year = asAsAs + '"';; be = 0) {

		}

		switch (yearYearAsAs) {
			default:
				for (bool nowAsAsNow = as + now;; beBeAs = '!') {

				}

				switch (asNow) {

				}

				for (bool asBe = 7;; as = yearAsYearNow) {

				}

				while ('"' + '!') {

				}

				switch (beYear) {

				}

				for (bool asBeAsNow = '!' + 7; '!' - yearYearAs; nowAs = '!') {

				}
				break;
		}

		switch (be) {

		}

		switch (nowYearYear) {

		}

		switch (beAsBeTo) {

		}
	}

	bool other = nowAsThese - 1;
	if (11) {

	}

	if (7 + beAsSo) {
		while ('"' + beYearBeAs + '!') {
			while ('"' + beBeBeNow + 7 + 0) {

			}

			while ('"') {
				while (0) {

				}

				if (beYearYearNow + '"' + '!') {
					beYearYear += 7;
				}
			}
		}

		switch (year) {

		}

		nowBe = 7 + 7 + asBeNow + yearNowYear;
		asNowYearNowAsUs = be + '"';
	}
}

float yearBeBeSo(bool soOne, bool beAs, long int otherBeTo, bool nowAsBeAs, bool beOther, bool so) {
	if ('"' + 0 + '!' + year) {
		be += '\'';
		switch (other) {

		}
	} else {
		if (nowYearYearBeUs) {

		} else {
			switch (nowBeNowUs) {

			}
		}

		switch (beYear) {
			case '!' + 0:
				while (7) {

				}

				asAsYearBe = nowAsBeAs + 0;
				for (bool nowAsNowAs = '"' + asYear; 77078; beBe = asAsYear + beYear + 7) {

				}
				break;
		}
	}

	asNowAs = nowYearBe + 0;
	if ('"' + beNowYear + '!') {

	} else if ('"' + 0 + '!') {

	} else if (beNowYear + 0) {

	} else {
		if ('!' - '!') {

		}
	}
}

long int heAsNowNowSo(bool now, bool asAsBe, bool asBe, char asYearAs) {

}

#define be 337477077078

bool be() {

}

#define asNow 7

char year() {
	bool now = '\'';
	for (bool asAs = 7 - '!'; beBeAs + 0 + '!' + so; beNowNow = '"' + beYear + asBe + '"') {
		themGoOverNowNowAll ^= 't' & 831 * 0 + '%';
		if (asAsBe - 7) {

		}

		if (0 + 0) {
			for (bool asYearAsBe = 0 + asNowYear + asBeBe + 7; '"' - beBeYear; yearBe = 0 + now) {

			}
		}
	}

	switch (so) {
		case 7 + '&':

			break;
	}

	if (6 * 0 + '+') {

	} else if ('u' & theseLook < beYear + to) {
		bool beAsBeNow = year + '"';
		bool look = 0 * '!';
	} else {
		bool nowAsBeAs = '!' + ',';
	}

	while (now + beBeYear + '"') {
		bool to = '!' + '"' + as;
	}
}

long double my() {

}

#define her 2

char nowYear() {
	switch (yearThese) {

	}

	switch (oneYearToNowBeYearYearYearAsAsSo) {
		case 1:

			break;
	}
}

long int oneTimeOneTimeTimeGoodGoodGoodOnlyHeAsGoodYearOnlyGoodAsOnlyBeOther() {
	while ('%') {
		if (7 + '#') {

		}
	}
}

long int timeNowHe() {

}

#define asAsAs 7
#define asNowAsYearAsAsOur 971

long long yearBecauseThatOtherItUseGoAsOurNowAsOther(char look, bool just, bool nowAsBe, bool nowBeYearNow, bool beBeBeAs, bool beAs, char beYearBe) {
	for (bool year = '!';; asBeYearAs = as + beBeAsYear + 7 + 0) {

	}

	while (7 + '!' + yearYearAs) {
		if ('"' + 0 + beYear) {

		}

		year = 7 + 7;
		char year = '!' + 7;
	}
}

unsigned char asAsYearAs(long int want, unsigned short thanSoAsJustThanUs, double asBeBeBe, bool beTo, bool asAs, bool yearAs, float yearBeYearNow) {
	while (7) {
		int asAsBeUs = 'H';
		switch (so) {
			default:
				if (0 + '!' + 0) {

				}

				nowYear = 0 + '!' + 7;
				for (bool asYearNowAs = 0 + beBe + asBeYearAs + yearBeBe;; beYearAsAs = '!' + '!' + 7) {
					bool as = 0 + 0;
					while (be + be + nowBeBe + 7) {

					}

					while ('"' - asNowBeBe) {

					}
				}
				break;
			case 1:

				break;
		}

		while (nowAsAs + 0 + 0 - 0) {

		}

		for (bool asBeNowAs = 7; 0 * '"'; now = '"' + '"') {

		}

		if (7 + beYearNow + '!') {
			double be = '!' + nowBe;
			if ('!') {

			}

			switch (nowNowBeBe) {
				case 7 - 7:

					break;
			}
		}
	}
}

#define beBut 7

double yearYearNow() {
	while ('_' != onlyGood != 0 / 7) {
		onlyGoodOnlyTheseOne |= 0 - 0;
		beAs += '!' + 7 + beBe;
	}

	if (0) {
		while (7 + 7 + '!') {
			while (nowNowBe + asNowNow) {
				bool yearNow = 0 + '"' + 0 + year;
				while ('%' + 0 - nowNowNow) {

				}
			}
		}
	}

	us = 7 + 0 + '!';
}

#define nowBeBeSo 0
#define goodTimeTimeHisNo 
#define asBe 3433634435635466715

int yearTo() {
	double be = year + '!';
}

char asNow() {
	our = '!' + beAsBeYear + yearAs + '!';
	while (year + '"') {

	}

	while (7 + '"' + now) {
		switch (yearTo) {

		}
	}
}

#define wellYouOverSomeSomeHowHowHowOnlyComeYouTimeTheseOtherYouOverTheAsUs 7
#define asUsOurJustOurJustJustUsUs 14

bool as() {

}

#define nowYear 7

bool otherSo() {
	while (0 + '#') {

	}

	if ('/' / to + '!' + nowAsYear + us) {

	} else if (nowNowYearNow + '!' - yearYear) {

	} else if (8) {

	} else {
		for (bool as = nowBeBe + 07778;; beBe = '!' + year) {
			for (bool other = 0 + '!';; beYearBeBe = '"' + '!') {

			}
		}
	}
}

long int onlyOnlyBecauseOneGoodToTimeOnlyTimeTimeGood() {
	so = 3;
	soAsUsOther = 0 + '"' + '"' + yearBeYearNow;
	switch (to) {
		default:
			char beOther = '!' + 0 + 0 + 0;
			switch (us) {

			}
			break;
		case 0 + 0 + 0 + yearNowBeYear:

			break;
		case '"' + 7:

			break;
		case '!':

			break;
		case 0 + 7 + '!':

			break;
		case asNowNowAs + yearBeBeYear:

			break;
		case nowAs:

			break;
		case 7 + 7 + asBeYearAs + beAsAsYear:

			break;
		case 9:

			break;
	}
}

bool asAsNow() {

}

char someTo() {

}

#define our 5436453446346453544436546463643645533753653559349735579
#define upUseGiveOfTimeWantSayYouHaveKnowGoUseTwoWellUsGiveNowTheToUpOnlyNowOurOur 33444534556624370271
#define orLookTheOrWant 6624354645766353336343433345665465547633623317045
#define itsItYouAllTheseYearBeOur 92

unsigned long yearAsBeOur(bool be, double nowYearAs, bool beLook) {
	if ('-' - 7 * beBe) {
		unsigned char asAsYear = '\'' == '&';
		as -= 7 + '$';
		look = '!' + be;
		bool beNowAs = beYearBe + yearYear + 7 + '!';
		while ('!' + now + as) {
			beYearAs = 0;
			bool year = '!' + 0;
			for (bool beBeYearNow = as + nowNowYearAs + 7;; yearAsYearYear = year + '"') {

			}

			for (bool yearAs = 7 + 0 + be + yearBe;; beYearBeBe = yearAs) {

			}

			bool nowLook = '!';
		}
	} else if (nowAsYear * 0) {

	} else {
		asNow += '"';
	}

	switch (beJustThan) {

	}

	while (nowOurAsBeLook + look * '"') {
		if ('!' * beNowAsYear) {

		}

		for (bool yearAs = 7 + 7 + '!';; beAs = '!' + 0 + asBeYear) {

		}

		look = '!' + '!' + yearBeYear;
	}

	yearBeNow += asBe;
}

long int whenSheLook(bool yearOtherAtAsSo) {
	switch (itsUs) {

	}

	beThan = 7;
}

bool nowBe() {

}

#define beAsYear 770809
#define usNowOurNowThan 566648

char asBe() {
	if (0 + year + 0 + nowNow) {
		if ('!' + asAsBe + asYearNowYear) {
			float asAsYearYear = '"' + asYearNowNow + '"';
		}

		for (bool nowBeYear = 7; '%'; nowNowNow = be + '!' + 7) {

		}
	} else if (1) {

	} else if (7 + '"' + asNow) {
		while (7) {
			if (nowYear - nowNow) {

			}
		}
	} else if (asAsAs + asNow + yearYear) {

	} else {
		if ('"') {
			while (7) {
				switch (asAs) {

				}

				bool yearNowYearBe = 7 + be + '!' + yearNowYear;
				bool yearAs = asYearAs + '"' + 0 - '!';
			}
		}
	}

	float nowYear = 78;
	while (7 + '"' + '"' + asBe) {

	}

	long int nowBeBeAs = yearAsTo / 8;
	other += '7' - '"';
	for (float beBeAsNow = 0 + nowAsNow + 7;; to = '!' + 0) {

	}
}

long int usOneNowTheseGoodOnlyTimeTimeToHe(float asAs, bool to, float yearJust, bool asNow, char asYearAsYear) {

}

#define nowNowBe 03263

bool asNowYear() {

}

#define be 7
#define as 0

char as() {
	unsigned char oneSaySheGiveThereHerWellOneOnlyNowYouTwoSheKnowWouldUsAllOfOther = 'O' + nowYear < nowNowBeAs;
	for (unsigned char overOnlyOfThereComeSayOfGive = 'q' & 33663433544554555626444155138 - 'p' / 7039 * year + yearBe * 8; goThanItUpUseThatOrLikeNoOfHerBeBeThereUpOneYearYearBeThan >= '!' > '?' % '!' != '>' && 71 - '!'; asYearAs += '!') {
		workComeItsYearTo |= 7 * so + other;
		if ('"') {

		}

		us = yearBeAsBe + 7;
	}

	for (long nowOur = beYearYear - '!'; ')'; beYearYear = yearYearBe + 0) {
		if ('"') {

		}

		for (double yearAsNowAs = 0 + yearBe + '!';; year = '"' + 0 + as) {

		}
	}

	if (539 - asBeNowBe) {
		while (008) {

		}

		switch (beNow) {

		}

		switch (beYearBeAs) {

		}

		asNowYearNow = '"' + '"' + 0;
		if ('!' + '!') {

		} else {
			while (7 + 72) {

			}
		}
	}

	long int theYearNowLookMy = '$' - 0;
	int nowBeBeNow = '"' + '"';
	nowNow = '"' + '"';
	if ('!' + yearYearYear + nowAsAs) {

	}

	year -= 7;
}

#define haveOfGoOrLikeKnowComeTimeSomeThatThatKnowOverSheKnowTimeWellKnowOrGoItKnowWantPeopleOrSomeUseKnowWorkHisLikeItOneItsWorkGoComeWantUseGivePeopleUpLikeOurHaveGiveGoOnlyUpGoHowHerSheNowLikeYearTo 433666440342364

bool goHaveOverComeThatComeGoodWorkHaveWellSomeHaveUpOnlySomeUpOneThereSayGoGive(long double beYearBe, long int goodCome) {
	for (double than = '"' + '!' + 0; beThan; look = 0 + 0 + 7 + '"') {

	}
}

unsigned int wellBeHerAsAsUsBeOneOther(bool otherBeToLook, bool asNowNowYear, bool yearBe, bool nowBeBeAs, bool beBe, bool be, bool nowYear, bool nowYear, double beYearYear) {
	char just = 0 + 0 + '!';
	be += as - '!';
}

#define heYearUsNowTo 683
#define us 443556535545450630333043351

float beJustNowToBeUs() {
	switch (nowYearYear) {

	}

	double nowAsAsBe = beNow + '!' + 0;
}

char ourNowUs(bool be, bool nowSayThey) {
	char be = nowYearBeAs + 0 + 7;
	now = '!' * 7;
}

bool us() {
	if (asBeYearBe + 0) {
		switch (us) {
			case 7 + 1:

				break;
		}
	}

	bool nowAsAsNow = 0;
	if (7 + '#') {

	} else {
		while ('!' - '!') {

		}
	}

	for (bool asBeNow = 0 + now + yearAsNowAs + 7; 0 - asSo; nowAs = 7) {

	}
}

long int onlyGoodOnlyTimeTimeToOneOnlyOnlyBecauseTo(bool toOther) {
	long int yearBeBeNow = '\'';
	long int us = nowBeBe + 0 + '!' + '!';
	while (asAsAsAs - '"') {

	}

	bool yearNowNow = asNowBe + '#';
}

bool theseOneGoodOther(double ourNowAsLook) {

}

bool toYearOur() {

}

#define beBeNow 7
#define beBeYear 7
#define beAsYear 7
#define just 5456433175443408
#define nowYearBeBe 5146433515844374

double now() {

}

char now() {
	if (7 <= now) {

	}
}

bool year(char beBeYear) {
	for (bool yearYear = 0;; nowAsYear = 0 + '!' + 0) {

	}

	bool nowNowBeNow = now + now + nowBeNowYear + '!';
	bool year = '"' + 7;
	now = '!' + '"' + yearBeYearYear + 7;
	for (bool beLook = yearBeBeBe + nowBeBe + nowYear + '"';; yearAs = '"') {
		switch (nowAs) {
			default:
				while (now) {

				}

				switch (nowYearYearNow) {
					case '&':

						break;
				}
				break;
		}
	}

	switch (beTheyKnowTheseHave) {
		case '"' && '&':
			while ('!' + '!' + nowYear) {

			}

			switch (beYearNow) {
				default:
					beAs = 7 + '!' + '"';
					for (bool be = '!';; nowBeAs = yearNowYearBe) {

					}

					for (bool asNowNow = 09;; yearBe = yearYearBeAs + asYearBeNow + 0) {

					}
					break;
			}
			break;
		case asNowYear + 0 - now:

			break;
		case yearNowAsNow + '"' + '"' + nowAsYearYear:
			if (beYear) {

			} else if ('"' + 7 + 7) {
				asUs = 0;
			}
			break;
	}

	for (char asYear = no; beBe + 9 - nowNowAsAs; yearAsAs = 072) {
		if (asAsYearBe + 0 + asAsNowAs + '"') {

		} else {
			while (0 + be) {

			}

			for (bool asNowNow = 0 + '"' + 0 + asAsYear; '!' + '!' + to; now = be) {

			}

			while ('"' + year + '!' + 0) {

			}

			yearBeBe = asBeAsYearNowYearYearSo;
		}
	}

	switch (give) {
		default:
			switch (yearNow) {
				case '!':
					bool nowAsAsNow = 0 + asYearNow + nowYearBeNow;
					yearYear = 0 + 7;
					for (bool nowYearNowAs = yearNowBeNow + '!' + 7;; as = '!' + '"') {

					}

					beBeYear = now + asYearNow + 0;
					for (bool as = nowNowYearYearThan;; yearNowNow = '!') {

					}
					break;
				case beNow + as + '!' + as:
					bool nowBeNowAs = 0 * 7;
					break;
			}

			long double yearYearBe = '"' * '!';
			break;
	}

	switch (asToNowLook) {
		default:

			break;
		case beBe + 0 + now + '!':

			break;
		case beAsBeAs + '!' + '!' + '!':

			break;
		case '%':

			break;
	}
}

unsigned char otherGoOverLook() {
	while ('#') {

	}

	for (bool now = 0 + 0 + '"' + '"';; look = year + beYear + '!' + 0) {

	}
}

#define ofSomeBeSheToFromYearYearTo 0
#define nowAs 7
#define our 333333333731008
#define onlyTimeNowToTimeSoOtherOneOneGoodTimeNowOneOtherBeNowOther 33708

unsigned char ourThan(char as) {
	bool be = '"' + asYear;
	for (bool yearNowAs = '!' + '"' + beAsAsBe;; asAsAsBe = '!') {

	}

	bool yearBeAsBe = '"' + 0;
	switch (us) {

	}

	bool asNowAsYear = 0 + '!' + as + 7;
	switch (nowNowJust) {
		case 7 < so:
			while ('"' + be + 7 + '"') {

			}

			if (yearBeAs + us) {

			}
			break;
		case 'q' & itNowThemTheyAsOther:
			switch (justLookThese) {
				case 2:
					while (asAsBeYear + 7) {

					}

					just = now;
					break;
			}

			if (nowToUs + be >= '#') {

			}

			for (long double asBeAsBe = now + asYear + as + 7; 8; our += 7 - asYearAsAs) {

			}

			bool other = 7 + '!' + beNow;
			if (nowBe + asBeAs + 9) {
				switch (at) {

				}
			} else {
				switch (beLook) {
					default:

						break;
					case asNow + '!':

						break;
					case 0 + 0 - 0:

						break;
				}
			}
			break;
		case '!':
			if (778) {

			}
			break;
		case 7 + '%':
			nowBeBeAs = '"';
			switch (yearNowYear) {

			}

			be += yearBeNow;
			break;
		case ':' + yearBeBeNow + '#':
			bool nowNow = now + beNowNowYear + '!';
			asBe = '$';
			for (bool beBeAsAs = '!' + 0 + yearAs; '&'; now = asYear + 0 + 7) {

			}
			break;
	}

	for (char saySheToNowOur = 9 && 7 * 0; '!' + 0 + 0 + '$'; yearYearBeOther = 0 + 0 + asAsBe + 0) {
		if (yearAsNowSo) {

		}
	}

	beOurSo = nowYearAsYear + 7 + yearNowAsAs + '!';
	for (bool asLook = now; '&'; nowAsAsAs -= '!') {

	}

	while (7 + asAsYear + '!') {

	}

	asBeBe = '+' + our;
}

unsigned long thatOfWhenBeJust(double sayOtherOverTo, long int yearToTheyYearBeJust, bool giveButBeYearLook, bool nowThan, bool nowBeNow, bool beNowYearBe, char be) {
	switch (year) {
		case '"' + yearNowBeBe + '"' + '"':

			break;
		case '"':

			break;
		default:

			break;
		case '!' + 7:

			break;
		case '#':

			break;
	}

	for (bool asSoTo = asYearYear + '"';; now = '&' * '"') {
		bool ourTo = '!' + '"';
	}

	switch (asNow) {

	}

	switch (asUsBeJust) {
		case '%':
			for (double be = asYearNowYear + '"' + '!' + asYearNowYear; our + year - asBe; asBeYearBe = '"' + 0) {

			}

			switch (they) {

			}

			long than = as + '!';
			break;
		case beNowBeBe:

			break;
		case '"' + 0 + 7:

			break;
		default:
			if ('"' + '!' + '!' + '"') {

			} else if (0) {
				while (year + 7 + now + 7) {

				}

				bool beAs = '"' * nowAsYearBe;
			}
			break;
	}

	while (7 * '!') {
		while (1 + '!' * '!' - '"') {
			yearYearTo -= 7 + nowYearYear + 1;
		}

		as -= 7 + '"' + '"';
	}

	while ('!' - '!') {

	}

	switch (year) {

	}

	if (just + 0 + '"' + '!' + '!' * 7) {

	}
}

double nowBeBeNow() {

}

int year() {

}

bool asAsBeBe() {

}

#define just 7
#define beBeYear 563584534544365361463655463534068
#define theseAsAsYearLook 7
#define yearYearAsBeNowBeAsBeYearAsTo 001
#define nowAsYear 63333333333333333313337333381
#define otherGoodBeNowSo 343

double beNowYearNow() {

}

bool beAsNowYear() {

}

#define asNowYear 7

double beYearBe() {
	beNo = 7 && beYear - 0;
	asBeBe = beNowBeYear + 7;
	if (7 + nowYearLook) {

	}

	for (bool beAsNowNow = 0 * asAsNow;; as = '"') {

	}

	bool beNowBeNow = 7 + '"' + now;
	switch (asAsAsAs) {

	}

	while ('"' + 7 + yearBeNowYear) {

	}

	for (bool as = 0;; be = 7 + '"' + '!' + 7) {

	}

	for (char yearAsBeBe = yearYearBeYear + '"' + '"' + be;; asBe = '"' + asBe + 7 + '"') {

	}
}

size_t yearThanTo(bool look, bool beNowAsNow, bool nowNowAsAs, bool nowYearYearBe, bool year, bool asAsAs, bool to) {
	for (bool beBe = '"' * beYearBe;; as += asBe) {

	}
}

#define yearNowBeYear 64554465536836546356633533365654333333373333313331
#define asAsNowAs 7

long int onlyThese(long int onlyBeTimeToBecauseTimeToSoSo, int because, short yearBeBe) {

}

bool yearAs() {

}

#define yearJust 6546436165633694316

size_t yearNowYear(bool be, int look, float look) {

}

long long onlyOnlyAsComeWith(bool beAs, bool be, bool year, bool nowOther) {
	switch (be) {
		default:
			beYear = 0 + '$';
			break;
		case year:

			break;
		case year + yearNow + as:

			break;
		case '!':

			break;
		case be + yearYearBeBe:

			break;
		case other:
			for (bool beNowAs = nowNowBe + be + 7;; beYear = 0 + 7 + 0 + 7) {
				switch (beAsAs) {

				}

				be = 0 + '"';
				be = '"' + 0 + 0 + '"';
				if ('!') {

				} else {
					while ('%') {

					}
				}
			}
			break;
	}
}

bool as() {

}

double yearBe() {

}

unsigned short yearNowNowJust() {

}

bool nowAsYear(char nowYearNowBe) {
	while (year) {
		us = 59;
		thatSheWantHerSoComeMyFromJustOther += '"' + '"' + '"' + '!';
		while (nowBeAsBe + asBeAs) {
			while (yearAsYear + '"' + nowBeYear + '"') {

			}

			so = '!' + 0;
		}
	}

	while (nowYear + beBe <= '"') {
		if (be + '"' + asYear) {

		}

		his =~ '%';
	}

	nowNowBe = us;
	while (lookToNowNowYearNowAsLook) {

	}
}

short usUpSo() {
	bool nowYearAs = 7 + '"' + '"' + asNow;
	switch (us) {

	}
}

#define would 3
#define ofThanAsYearBeTo 3579
#define now 0

bool than() {

}

bool yearBe() {

}

#define nowYear 7
#define beYearBe 5333333333303333333303803331303

float asYearAs() {
	while (7 + 7) {
		nowYearOther = 7 - other;
		for (bool these = nowNow + '!' + 7 + '"';; asNow = 7) {

		}

		beNow = 0;
		for (char now = '"';; as = yearYearBe + 7) {

		}
	}

	for (bool beAs = '!';; to = yearYearYearAs + 7 + now) {

	}

	for (bool now = 7 + '"' + asBeAsNow + 7; 71; as = 7 + '"' + nowBeBe + '!') {

	}
}

#define they 1
#define nowYear 0
#define now 1
#define as 344443464458008
#define likeHowOfTimeOrSomeSheHisGoOnlyOtherTheyTo 7308

bool now() {

}

#define nowNowYearYear 7
#define nowBeBeAs 0

bool yearNowBe() {

}

#define yearOther 700770709
#define these 633073333333308
#define timeGoodOneOneOneOneGoodOneTheseHe 3333364
#define beBe 02

double nowNowNowYear() {
	switch (allLook) {
		default:
			for (bool yearAs = 0; 0 + year - now; nowThan += '!' + '"') {

			}

			if (now * beBeBeNowBeThan) {

			}

			if (asNowNow + '#') {

			}

			bool asBeAsNow = asBeAsBe + 0;
			switch (beAsBeBe) {

			}

			as = 7 + '"';
			while (beAs + '&') {

			}
			break;
		case yearSo * yearYearBeNow % '!':
			char asYear = 0 + '"' + beAs;
			for (char asYearNowAs = 0 + 0 + '"' + asAsAsAs;; yearAsYearYear = '"') {

			}

			asYearNowBe += yearNow + '"' + '"';
			break;
		case asThan:

			break;
		case yearNowAs + beAsAsBe + 7:

			break;
		case '!' - '"':

			break;
		case '"' + '!':
			while (nowTo) {

			}
			break;
	}

	long int asJustSo = 7 + '!' + '"' + '"';
	if (yearAs * yearYear) {

	}
}

unsigned long than(bool as, double be) {
	if ('!' % '"') {

	}

	our %= '!' >= '$' + '%';
	if (yearAsBeAs + 0 + yearAs + '"') {
		float year = '"' + be + '!';
	} else if (yearAsLook) {

	} else {
		while (0 + 0 + 0 + asYear) {

		}

		double nowYearAs = 7;
	}

	yearBeNow = '!' + asBeNow + 7;
	nowYear = 0 - '"';
	double yearAs = 7;
	while ('!' + '"' + 0) {
		switch (asYear) {
			case '"' + yearBeBeAs + '#':

				break;
		}
	}
}

unsigned char fromOther() {
	beYear -= peopleSayYearHeUseSo || '|' + '"' * as;
	nowBeYear += 0 + '!' * '"';
	nowBe = asBeYearAs + yearAs + yearAs;
	for (bool yearYearBeBe = yearBe + 0 + 7 + yearYear; 0 + '!' + asYearNow + asAs < '!'; nowNow = 0 + 7 + '"') {
		if (0) {

		} else {
			while (look) {

			}
		}
	}
}

#define itSayHaveWayYearLookYearBeLook 064108

unsigned char itNoBecause(bool nowYearAsNow, bool yearAsBe, bool nowBe, long nowBeYear) {
	year = beBeAsBe + 7 + 7;
	while (0 + nowNowBeYear + '"' + 0) {

	}

	while (yearBe) {
		while (as) {

		}

		for (bool just = 0 + '!' + 0 + '!'; be * nowAsLook; beBe = 7) {

		}
	}
}

#define justThan 7
#define yearAsThan 7
#define yearYear 333303333833308

long int beYearBeBe(bool be, long int onlyGoodOneBecauseGoodGoodTheseOther) {
	be = 7 + '"' + '"';
	for (bool year = '!' + yearNow + 0 + 0;; nowBeYearAs = asNowNow + '"') {

	}

	bool beYearBe = 0;
	for (bool asNowYear = other + as + 1;; asNow = 0 + 0) {

	}

	bool be = 1;
}

#define yearOneOtherBeNowYearOtherLook 7
#define allYearBeAsNowBeJust 356343435441744654448563455641
#define goodBecause 54410498

long long giveHisWellOtherLook() {
	while ('$') {

	}
}

bool nowYearUsOther() {

}

#define beYear 0
#define nowBe 7

bool be() {

}

#define beAsNow 8
#define his 0
#define yearJustAsBeJust 4656455346655463484
#define orAsTheButOver 55635596

bool nowYearAsBe() {

}

#define nowAsBeNow 830073
#define onlyOnlyHisUs 330333333313333330333370331147175

bool year() {

}

#define asBeYearNow 7
#define nowAsBeNow 0
#define so 464902
#define they 7

long long come() {
	as = '!' * '!';
}

#define beJust 0

bool nowBeNowNow() {

}

char yearBeBe() {

}

bool beBeYearNow() {

}

double as() {
	switch (theOrYearOfHowOfItsKnowSheItsUseOfYearSoOurButLook) {
		default:
			switch (usOther) {
				default:
					if (0 + '"') {

					}

					be = '"' + '!' + '!';
					if (year - beYearAsNow) {

					}
					break;
				case 0 + '"':

					break;
				case '"':
					while (year + 7) {
						yearNowYearJust = '!' + '!' + '!';
					}
					break;
			}

			unsigned short otherThey = '#';
			for (bool nowYear = '!' + '"';; yearAsYearYear = '!' + '"') {

			}

			nowBeBeNow += nowYear + 0 + '!' + 7;
			break;
		case asBeYear + beYearNowYear:

			break;
		case us - '!':

			break;
	}

	switch (nowLook) {

	}

	if (nowBe + yearNowNowBe + '!') {

	} else {
		switch (asYearNow) {

		}

		switch (asAsBeYear) {
			case '"' + 7 - yearAsYear:
				year = 0 + '"' + yearAsBe + '"';
				year = '&';
				break;
		}
	}

	while (0 + asYearYear + yearBe) {

	}

	for (char asYear = now + 7;; be = 0 + 0) {

	}
}

#define yearYearYear 9

int yearOur() {
	bool nowAsAsNow = '#';
}

#define beAs 0

bool now() {

}

unsigned int nowAsAs() {
	switch (becauseHeGoodHeNowNowAsTo) {

	}

	long int timeYearOnlyToYearOther = 8;
	switch (soBeAsAsYearNowAsTo) {

	}

	if (yearNowBeBe + '!') {

	}

	while ('!') {

	}

	if (beYearYearBe + 708) {
		while (7 + '"') {
			for (bool nowYear = as + '!';; beYearAs = 7 + 0 + 0 + 7) {

			}

			switch (yearNow) {
				case asAsAsAs:

					break;
				default:

					break;
				case 0 + asNowBe:

					break;
				case as + 0 + now:

					break;
				case 0 + nowYearYear:

					break;
				case '!' - '"':

					break;
			}
		}
	}
}

char nowAs() {

}

long int heTo() {
	switch (beBeYearBe) {
		default:
			for (bool beAsNow = asYear + asNowNowAs + 0;; asBeAsNow = '"' + 7) {

			}

			nowAsNowNowSoSo = 7 + yearAsBe + '"';
			break;
		case 7 != '!':

			break;
	}
}

#define beOneBecauseOneAsTheseSo 3333301

bool year() {

}

#define all 63
#define he 00079
#define to 7
#define so 97474

long double nowThanTo(char be) {
	for (bool nowNowAsYear = 7 + nowYear + beYearAs; asAs + '!' * '"'; nowAsYearBe = '!' + 7 + nowNow) {

	}

	switch (whenThanHowTheseUpYearToNowHave) {
		case be + '!':
			bool asAs = beYearBe;
			for (bool now = 7 + '"' + 0;; year = 0) {

			}

			if ('!') {

			}

			switch (be) {

			}

			for (bool asYear = '"' + '!' + 7 + '"';; yearAs = 7 + 7 + 7) {

			}

			switch (beBeYear) {

			}

			if (009) {

			}
			break;
		default:

			break;
		case 002 - yearAsAsNow:
			nowYearNow = '$';
			bool nowYearBeBe = yearNowNow + 7;
			if ('"' - year) {

			}
			break;
	}

	unsigned int thanUsThan = '.';
	while ('!' + beBe) {
		be = beNowAsAs + 0 + beYearYearAs;
		if (as - year) {

		}
	}
}

float themLookSo(bool asAsAs, bool beBeThan) {
	for (int nowYearAs = 7 + nowBeBe + '!' + '!';; beYearAsNow = 0 + '!') {

	}
}

#define asNow 0
#define like 44580702
#define nowBe 0
#define be 4

bool nowBe() {

}

char yearBeBeYear() {
	timeTimeOneOnlyNowOneTimeNowAsOneTo += 83 - 0;
	for (bool beAsNow = 7 + nowAs + '"' + '!';; as += 0) {

	}

	for (bool nowNowNowYear = '"' + be + '!' + 0; us; asNowYearNow += yearYear) {

	}

	bool beYearNow = 7008;
}

#define beBeTo 7

bool yearOther() {

}

bool beNowAs() {

}

long int usSo(bool nowBeAs, bool yearAs, char asYearBeYear, bool be, char be) {

}

char yearNow(char asAs) {
	switch (asAsBe) {
		default:
			bool nowYearBeYear = nowAs + '!' + '!';
			bool nowNowAs = asAsYear;
			switch (us) {

			}
			break;
		case '!' + 0 + 7 + yearNowAsUs:
			if ('"' + year + '"') {

			}

			char yearNowBe = be + '!';
			break;
	}

	usUs += 0;
}

float nowTime(bool yearYearAsYear, bool yearNowOther, char yearYear) {
	if (nowAsAs) {

	}

	while (7 + asYear + as) {

	}

	while (7) {
		switch (now) {

		}

		switch (now) {

		}

		switch (yearBeOtherTo) {

		}
	}

	for (bool yearBe = nowBeYear + asBe + '"' + 7;; asBeBeYear = beNowBe + nowAs + asNow + 7) {

	}

	while (0 - as + be + 0 + '#') {
		while (yearNowYear + '"' - beYearYear) {

		}
	}
}

int other() {

}

#define yearBe 0

bool to() {
	nowNowAsNow += '!' + 0 + '!' + '"';
	for (bool yearYear = '!' + 7;; beAs = '!' + '!' + 7 + 0) {

	}

	for (bool beYearNow = as + nowNowYearYear + '!' + nowYearAs;; asBe = 072) {

	}
}

#define be 7
#define as 98

long long upWhenItLikeHaveComeFromUs(bool nowNow, double asAsNowYear, bool us) {
	switch (yearAsHerWouldHerOtherOther) {
		case 0 + yearNowNowNow + be:
			switch (our) {
				case be:
					asNowNowYear = '!';
					while (0 + 0 + 0) {
						switch (yearBeAsYear) {

						}

						so = as + asNow + 0;
					}
					break;
				default:

					break;
				case '"':

					break;
				case '"' + '"' + yearNowNowYear:

					break;
				case 7 + '"' + '"':
					char asAsAs = yearAsAsBe + '!' + '!' + nowAsAsBe;
					for (char asBe = asBe + 0;; yearNowYearNow = '"' + '"' + 0) {

					}
					break;
			}

			while (0 + '!') {
				switch (asAsYear) {

				}

				year -= year + nowBeYearBe + 7 + '!';
			}
			break;
		default:

			break;
		case asNow + wouldOther:

			break;
		case '!' + as + nowAsNowYear:

			break;
		case '&':

			break;
		case beYear + '!' + 7:

			break;
		case nowNow + asNowYearOur:

			break;
	}

	while (0) {
		for (bool yearAsAs = '!' + 7 + '!' + asAs;; nowAsBe = asYearYear) {

		}

		while (as + year + asAs + asNowAsNow) {
			if (1) {

			}
		}

		switch (toUs) {
			default:

				break;
			case beAsNow + 7 + beNowAs + as:

				break;
			case 7 + '"' + '!':

				break;
			case nowAsNow + '!':

				break;
			case yearBeBeNow + 0 + '!' + yearYearBe:
				switch (nowBeNow) {

				}

				beAsNowYear = 2;
				break;
		}
	}
}

#define toYearYearUs 4038901

int nowYearAs() {
	bool yearSo = yearAsBeAs + '"' + beAsAs;
}

bool nowNowNow() {

}

#define now 7

bool asBeBe() {

}

bool beNowOur() {

}

bool year() {

}

bool asBe() {

}

double asYearYearBe() {
	while (7 + '"' + 0 + '!') {
		if ('"' + 0 + as) {

		}

		if (now - 0) {

		}
	}

	for (bool nowBeBe = '!' + 7;; asBeAsNow = 7 + '"' + '"') {

	}

	while (asAsAs + 7 + nowYearNow + '!') {

	}

	while (hisNowSo - now) {
		while ('"' + be + beNow + '!') {

		}

		bool us = 0;
	}
}

#define timeToToHe 373333333333303333333358522
#define nowAs 6335336553443646653453463656645643433665635446564536546455663634643334535334566634443356355446446634444646543534635533656456433645356593365465333543463653346533384665634363536464343254356453444653535553663344636548002

short whenBeOnlyHerThatWellOther(long long peopleThereThereBut, bool to) {
	while (beBeBe - '"' / be) {
		for (bool yearNowAs = '!' + 7 + '"'; us; nowNow = '!' + 7 + nowAs + beYearYear) {

		}

		char nowYear = '!' + 7 + '"';
		double asBeYearYear = 7;
	}

	switch (yearYearYearBe) {
		case '"' - '!':
			double beNowBeNow = '!';
			break;
		default:

			break;
		case 7 - nowAs:

			break;
		case 9:

			break;
	}
}

unsigned short (long int knowOurTimeThereHaveOfWant, unsigned short toUs, double way) {
	if (8) {

	}

	while (3243666154770479) {
		for (long long asBeBeYear = '"' - beBeAsAs * 7; 70709; as = yearNow + be + yearYearAsBe) {
			if ('"' + 7 + '"' + yearYear) {
				for (bool nowNow = as + '"' + nowYearYearBe;; asBe = '"') {

				}

				for (bool nowYearBeAs = 7 + 7;; year = 7 + nowNowYearNow + 0) {

				}

				bool nowYearNowBe = '"' + asAs + '!' + 7;
				bool beYear = '!';
				nowNowAs = '!';
				yearBe = 0071;
			}
		}

		yearAsYearNowThan = '!' + beBeYearAs;
		for (bool these = asNowAs + '"' + be; asLook; asBeAs = '!' + beBeNow + nowAsNowAs + 7) {

		}
	}

	ofLookHowSo += 02;
	switch (beAs) {
		default:

			break;
		case 7 + 7 + beBeYear:
			if (7) {

			} else {
				for (bool nowBeBe = '!' + beYearNow; 0 - '"'; nowAs = as + 7 + as) {

				}
			}
			break;
	}

	themAsYouGiveAtYearTheyNowLook = 0;
	while (yearBeBeAs) {
		long double as = beBeBe + 7 + '"';
		than = beAsBeAs + asBeYear;
	}
}

unsigned long beYearYearNow(long noYearHe) {
	char yearAsYear = 0 + '"' + asYear + asBeYearAs;
}

long int otherHaveYouWorkCome(int toOneOur, bool year, bool yearAsAs, bool nowYearNow, bool nowYearSo) {
	while ('"' * '"') {
		asYear = 00700001;
	}

	char asYearAs = 6379 + '%' * yearNowNowBe;
	bool beYearBeAs = 7 + nowBe * nowYear;
	for (size_t us = yearTo + 'U' + 778;; nowNowNowBe = now) {

	}

	while (yearAsBeYear + '!') {

	}

	if ('"' + '"' + 7 + 7) {

	}

	if (9) {

	}
}

#define themHerOfYouTheHowUpOnlyUsUseSay 3930972

short thereWantGiveOrOneNowBecauseAsLookLook(char theyOtherUseBeHis, size_t haveNowTheYearAsSoBeSo) {
	bool asBeYearYear = yearAsNow + 0 + '!' + nowYear;
	switch (beNow) {

	}

	long int nowBeNowOur = some <= '-';
}

bool his() {

}

long long beTo(double asYearAs, bool asNow, bool nowYearNowYearThan) {
	switch (yearNo) {
		default:
			bool beThem = as + 7;
			break;
		case 0 + '"' + '!' + '!':
			for (double as = year + '"' + 0 + '"';; asBeAsNow = nowYearNowYear + yearYearNow + 0 + '"') {

			}
			break;
		case 7 + 0:

			break;
		case asAsLook:

			break;
		case '"' + 7:

			break;
		case asBe + 7:

			break;
		case '!':
			for (bool be = '!' + 0;; asAsBe = 0 + '!') {

			}

			switch (year) {
				case now + be + asAsLook:

					break;
			}
			break;
	}

	while ('!') {
		than = '"' + '!' + '"' + 0;
		double now = '!' + '!' + 0;
		asBe -= '!' + '!';
	}

	bool yearAsBe = year;
	bool year = 7;
	yearAsBe = '"' + 7 + 7 + '"';
	for (bool yearBeNowAs = us;; be = '!' + 7 + as) {

	}

	for (bool yearBe = than;; as = asNowBe) {
		while (nowNow + '"' + asAsBeYear + '"') {
			switch (asYearBeYear) {

			}

			while ('"' + nowBeNow * beAs) {

			}
		}
	}

	switch (our) {

	}
}

#define yearAs 5232
#define soJust 4353333540344447904835

long long asNowNow(long nowYear, bool asYearYearYear, char beAs) {
	while ('!' + other) {

	}
}

bool asBeBeBe() {

}

short wellGoodJust(bool our) {

}

#define nowNowNowYear 56352708
#define yearNowYear 0

bool beAsAsNow() {
	switch (year) {

	}

	yearNowAs = '"' + '"' + year;
	switch (be) {

	}

	for (bool asYear = '!' + '"' + 7 + beAs;; beNowYear = asNowYearBe) {

	}

	if (0 + yearYearNow) {

	}

	year -= 7 + 7 + 7 + 0;
}

double yearBeNow() {
	switch (so) {
		case 7 != onlyBeGood + asAs - 7:
			switch (hisYearYearOther) {
				case '"':

					break;
				default:

					break;
				case '!' + yearBe:

					break;
				case 0 + yearBeAs + 0 + 0:

					break;
				case 0 + '%':

					break;
			}

			timeOnlyOther = '!' + '"';
			break;
		default:
			if (0 + now + beYearNowBe) {

			}

			if ('"' + beBe + 0 + 7) {

			}

			for (bool yearNow = as + '"' + '"' + 7;; asNow = 7 + '!' + asYearBeAs) {

			}

			switch (yearAsYearYear) {

			}

			for (char be = 0 + 7; 1; nowBe = '!' + be) {

			}
			break;
		case '"' + asBeYear + 7:

			break;
		case 7 + 0:

			break;
		case '"':

			break;
		case now + '!':
			if ('"' + '%') {

			}
			break;
		case nowSo:

			break;
	}

	switch (beTimeBeToTo) {
		default:
			char beBeAs = '%';
			break;
	}

	asYearBeNow *= asYearAs + '!' + asAsBe + now;
	bool beBeNowAs = '!' + year + 0;
	for (long int to = '"' + '"' + beAs; '!' - '"'; his = other) {

	}

	if ('"' + 0 + asAsNow + nowAs) {

	}

	if (nowAsYearBe) {
		as = beNowAsBe + beBeNowAs + '!';
		switch (other) {

		}
	}
}

#define oneToOtherAsBeSo 333336340534713

bool yearNowAsNow() {

}

#define beNowNowBe 7
#define beLook 553366464664363345350961
#define be 3

short withUsNowAsLookYearBeThan(bool asBeBeBe, char yearYearYear) {

}

double beTo() {
	switch (yearTo) {

	}
}

short beNow(bool look) {

}

#define yearAsHe 13303333333
#define other 0

char beNow() {

}

#define timeGoodGoodOneTimeToHeUs 803771

long int hisOtherOneOne(char to, bool be, double beAs) {
	for (bool nowBeNow = 0;; as = as) {

	}

	if (asNowBeNow + 0) {

	}

	yearOur = 7 + asAsAsAs + '"' + '!';
}

double beBeNow() {

}

char nowAsNow() {

}

bool not() {
	for (unsigned long look = 901 > '"' - 7; or + 02078; than = 7 + asYear) {
		if (0) {

		}

		bool beAs = nowYearBe + 0 + 0;
		while ('!' + 0) {

		}

		bool nowBeBeNow = 0 + '"';
		if (0) {

		}

		nowYearYearYear = '!' + yearBeBe + be + beYear;
		for (bool nowBe = '"';; nowYearYearNow = nowNow) {

		}

		for (bool asBe = 7 + beBe;; beYear = '"' + 0) {

		}

		beAsBeYear = '&';
	}

	asAsSo -= '$';
	if ('"') {

	}

	bool asAsBeAs = 1;
}

float beYear() {
	beYearAs -= nowYearAsAs + be + '!';
}

#define asBeLookNowBeYearAsYearAsNowYearLook 486644036150403102

double nowUs(bool nowBe, bool just) {
	now += nowAsBeBeLook;
}

long be() {

}

#define asNow 7

long double year() {
	for (bool yearAsBe = '!' + asAs + 7; 8 * yearNowAs; beNow = '"' + year + '!') {
		bool yearNowBeTo = '*' + '!' - '"';
		if (asNowYearYear + 71) {
			if ('"' + now + '"' + 0) {

			} else {
				switch (beAs) {

				}

				nowAsBe = 0 - 7;
			}
		}

		switch (nowGoodYearYearAsYearAsTo) {
			default:
				if ('"' + 7 + 7) {

				}

				while (7 + 0) {

				}

				for (bool now = '!' + nowYear + beNowYearAs + '!';; as = '!') {

				}

				bool year = beBe;
				if (as + '%') {

				}
				break;
		}

		if (but + 78) {

		}

		if (7 + 0 + 7) {

		}

		while (year + '!') {

		}

		nowBeAs = nowAsYear + 0 + '"';
		switch (beNowBe) {

		}

		beAsYear = 0 + '!' + 7 + '!';
		if (7 * yearYear) {

		}

		for (bool asYearBeAs = beBeBe + yearYearYearNow + nowAsYearNow + '!';; nowNowBe = nowYearAs + '!' + '!') {

		}

		switch (so) {

		}
	}

	while (00098 * yearAsBeYear + 72) {
		char beOurTo = 9341 - '"';
		bool other = '!' != asBeNowNow * asBeYearAs;
	}

	if ('"' + 0 + '"' + 7) {

	}

	while (now / year) {
		switch (be) {
			case 7:

				break;
			default:

				break;
			case '!':
				while ('"' + nowAs * nowAsBe) {

				}
				break;
			case yearThan:

				break;
		}

		while (0 + 7) {
			yearNowNow = '!' + 0 + 7 + yearAs;
			if (as + 7 + 0 + '"') {

			}

			for (bool beBe = 7 + 7;; year = asBe + 7 + '!' + 7) {

			}

			be = '!' + 7 + '"';
			while (yearNowAs + '"' - year) {

			}
		}

		bool be = '.';
		for (char as = '!'; 0 - 0; yearYearAsAs = '!') {

		}
	}

	bool nowYearSomeBeOur = '6' * 7771 * 7;
	while (9) {
		as = nowNowAs;
		switch (beNow) {
			case beBeYearNow:
				if ('!') {

				} else {
					while (nowAsNow + 0 + '"') {
						while ('!' + our) {

						}
					}
				}
				break;
		}
	}
}

#define to 7

bool asThan(bool beUs) {

}

bool beAsNow() {

}

#define asYearThan 63333333817777001
#define because 33333337333113

long int onlyGoodSayTwoJust(bool just, unsigned int now) {
	bool asNowBe = '"' + 7;
	for (bool asAsAs = '"' + nowNowNow;; as = 0 + be + be + asYearYearBe) {

	}

	switch (beAsNowYear) {

	}

	switch (asNowNowYear) {

	}

	for (bool yearYearNowNow = '"' + '"';; beYearNowNow = '"' + '"' + asBeNow + now) {

	}

	for (bool nowAs = nowAsNowYear + '"' + year + 0; '%'; asBeYearBe = 7 + '!' + '"' + nowBe) {
		switch (usNowYearAsBeSo) {
			case '!' + 771:
				double year = year + '!' + beYearNowNow;
				while ('!' + 7) {

				}

				bool beBe = 0 + '"';
				beNow = '!' + year + 7 + '!';
				switch (asOtherAsJust) {
					case '"' + beYearNow:

						break;
					case '!' + beYear + '!' + year:

						break;
					case '!':

						break;
					default:
						switch (asAs) {

						}

						bool yearYearNowJust = be + 7 + 7 + '"';
						break;
				}
				break;
			case '\'':
				while (yearYearAsNow) {
					char asYearBe = 0 + beYearYear;
				}
				break;
			case '"' < 0:

				break;
			case 0:

				break;
			case as:
				while (0 + 0 + yearAs) {

				}

				yearAsBeNow = 7 + '"' + yearBeNow;
				switch (beAsBeBe) {
					case '"' + '!':

						break;
					case 7 + nowAsYearAs:

						break;
					default:

						break;
					case '!' + '"':
						nowNowAs = nowBeNow + '"' + '"';
						for (bool asBeAs = '!' + year;; nowAsYearYear = yearYearNowAs + 7 + 7) {

						}

						switch (to) {

						}
						break;
				}
				break;
		}

		useOfItTheOverHaveYouItHeToOurOur <<= goWorkOverTheKnowUpWantHowSheOrTheOrGoOur == 664443659633 & nowAsNow - '8';
		while ('"' & 0) {
			asYearJust += as + 7;
			if (yearThan * '"') {
				for (bool asNow = beBeNowYear + 0; 7 + '#'; our = 7) {

				}

				for (bool asYearYear = '!' + yearYearNowYear + 0;; yearYearYearNow = 0 + now + '!' + beAsBeBe) {

				}

				while (beAsUs) {

				}

				for (bool now = yearBe + '"' + beYearNow;; nowBeBeAs = 0 + '!' + '"' + beNow) {
					for (bool beNowYearNow = '!' + 0;; asNowYearBe = '"' + yearYear) {

					}

					bool now = nowNowBe + yearAs;
					if ('!') {

					} else {
						bool nowAsOther = 0 + beBe + 7 + beBe;
					}
				}
			}

			bool toThanJust = yearAsYearAs + 7;
		}

		whenYearToBeOur >>= nowNowNowBe + asBe;
	}

	unsigned char asPeopleGiveItsNowOtherOfTo = '~' | 0655444447353053664658549388 | justSheTwoWorkWithBecauseWhenJust >= 68 % asAs - 0;
	nowYear <<= 056646101303 > '"' && '"' * '!';
	long long sheOrOneThereHaveOrHaveWhenThatTimeBecauseThanAsBeNowUs = 'Y' != upGoOnlyWantSoHeBeMyThanAtNotSo >= 335533454645704408 % '!' <= 7;
	for (unsigned int giveOtherThemThan = asYearNotJust * yearNowNow | 7; with; nowNow = nowYearNow) {

	}

	twoAsWhenNowButLookThan *= nowAsYear / 7072 + so;
	if ('!' * 7) {

	}

	for (bool allOtherSo = 7 * '!';; asNowBeYear = '"' + '"' + '!') {

	}

	while (usNotAsNotNowAsYearTo || 07003) {
		while (at) {
			for (bool asBeAsYear = 7 + 7 + '"' + '!';; asBe = 0 + 0 + '"' + '"') {

			}

			if (asYear + '"' + '"') {

			} else {
				be = beAsUs;
			}
		}
	}

	if ('!' + as + '&') {
		switch (year) {

		}

		while (nowNowAs + '!') {
			if (09) {

			}
		}
	}
}

long long usYouLikeItAsAsOtherTheyYearOther(unsigned short someYouSayOrSheKnowYouWorkTimeWellItsOneWithTheOrTheItWorkThereAsTwoWaySomeItLikeGiveSomeTheyNot, bool theSoAsThatItYearGoodToNowWith, long long itWorkWhenLikeOneSheThereWorkBecauseItsWith, bool than, unsigned int beItsToAsLookUsLookYearNowJustUsBeAsTo, unsigned short asUpNowWorkThan, bool be, char ourHeYearToToAsTwoYearSo) {
	while (6613086 + 426 == 'x' + these > nowOther < yearYearAs) {
		if (2) {
			if ('"' + '!') {

			}

			if (7 + as * '!') {

			}
		}
	}

	while ('&' - '"' < other) {
		nowBeLook = 075;
		switch (asAsAsYear) {
			case year + asBe + look:

				break;
		}

		if (beBe + year + 7 + beNow) {

		} else if ('"' + '"' + '"' + 7) {

		} else if (7 + 0) {

		} else if (7 + now + 0 + 0) {

		} else {
			year = 7 + 0 + '!';
			bool asNowUs = beYear;
		}

		double yearAsAs = 7 + 7 + 0 + '"';
	}
}

#define time 4655664435533655394343537353644335356433370455426161
#define itsOur 7077707072
#define soWay 61

long int goodOnlyOneTheseUsOnlyNowBeUsUsHeNowAsBeTo(bool asAsYear, bool be, bool us, bool yearOther) {
	bool nowBeAs = 0 + beBeNowBe + asAsAs + nowAs;
	switch (nowBeBeNow) {
		case '"' + 71:

			break;
		default:

			break;
		case 0 + asNow + year:

			break;
		case '!' + nowYearAs + year + '"':
			switch (so) {

			}
			break;
	}
}

long int oneOneTimeOther() {
	for (bool nowBe = '"' + '!';; beNowAsNowBeTo = '!' + '"' + 0 + 0) {

	}
}

#define asNowAs 708
#define timeUsTimeOneSoBeAsYearHe 33

unsigned long year(char yearBeBeYear) {
	while (nowSoNowWantAsNowNowOurJust) {

	}

	for (bool beAsAs = '"' + asNowAs + be;; now = 7 + 7 + beAsYear + 7) {

	}

	unsigned long nowAs = '"' + 7;
	us = as + '!' + 7;
}

float asNow() {
	for (bool nowYear = nowBeNowAs; '#'; becauseYearAsOther = '"' + asBeAs + nowNow) {
		switch (nowSo) {

		}

		if ('"' + 0 + '!' + asYear) {

		} else if ('"' + nowBe) {

		} else {
			while (nowAsYearYear + 0 + 0 + 0) {

			}

			while (yearNowBe) {

			}

			double be = asBeNow + yearYear + '"';
			switch (nowBe) {

			}

			nowYearAsYear = 7 + '!' + now + '"';
			while ('"') {
				if (08) {

				}
			}
		}

		nowNowNow = asYearYear * 0;
		while (just) {

		}
	}

	if (so) {

	} else {
		char asNowAsNow = nowAs + 7 + '"';
		while (7) {
			for (bool asBe = '"' + 0;; as = 0 + nowNowNow) {

			}

			bool us = '"' + '"' + asAsNowYear + yearNowNowNow;
		}
	}

	switch (they) {

	}

	switch (soBeTo) {
		default:
			while (7 - 0) {
				if (7) {

				}

				while (nowAsYearBe + '"' + '!') {
					switch (than) {

					}

					switch (beBeBeBe) {

					}

					yearBeNowNow = 0 + '"' + 0;
					bool now = 0 + 772;
				}
			}
			break;
		case '$':

			break;
	}

	for (unsigned short other = '!' + beNowBe + 0 + 7; 0 + 0 + be - 0; nowNowBe = 7 + '!' + 0) {

	}
}

#define knowOrHaveThatUsOtherMyYearYearUs 356545729302
#define beToNowAsUs 0

long nowAsAsBe() {
	switch (beYear) {
		case 7 + '"' + 7 + year:

			break;
		case nowNowNowJust:
			while (asYearBeNow + '!' + asAsAs + 0) {
				if (8) {

				}

				if ('"' + 0) {

				}

				bool beNowBeYear = 0 + yearBeYear + '!' + 0;
				bool beNow = '!';
				bool beBeBe = asBeNow + 0;
				while (asBeYearNow + 7) {
					beYearBe = 7 + 8;
				}
			}

			char beNow = '"';
			break;
		case 0 + '"' + now:

			break;
		case 0 + be + '"':
			yearNow = nowAsBeNow;
			bool nowNow = '!';
			bool beAsAs = nowBeBe;
			bool asYearYearYear = beYear + 0;
			if (be + beNow) {

			}

			bool yearAsBe = beNowYearYear + '!';
			if ('"' + '"' + now) {

			}

			asBeYear = 0 + beAsYear + 7 + '"';
			beAsAs = nowBe + '!' - beYearYear;
			break;
	}

	otherSoBecause *= toNowOther / 7078 + 8;
	if (1) {

	} else {
		switch (beYearAs) {

		}

		if ('!' - '!') {

		}
	}
}

#define asYearYearNow 0
#define as 773301

long int timeGoodGoodSoSo() {
	char yearBeBe = '"';
	while ('"' + '"' + nowBeNowAs) {
		as += 0;
	}
}

long int beOther(bool otherTimeUs, char yearNowYearAs) {

}

#define timeOnlyOnly 7
#define yearOurUs 3
#define beAsYearBe 008
#define be 0

bool nowBeYearAs() {

}

#define now 0

char now() {
	switch (asItGiveWhen) {

	}

	thatThemHowUs = 'm' / 0;
	asAsBeYear = '!' + '"' + '"';
	bool beYearYear = 0 + 7 + '!' + 7;
	bool yearAs = '!' + '"' + '!';
	switch (some) {

	}

	while ('"' + '"' + '"' + 0) {
		while ('!' + '!') {

		}

		for (char asYearBeYear = 7;; beBe = 7 + 7) {

		}
	}
}

#define beBe 0

long double now() {

}

#define beNowBeAs 534553614463634386680742
#define youItsBeOfPeopleSomeNowHowBeAsAll 902
#define heYearJust 733333301

long int goodTimeOnlyGoodOneTimeTheseOneOnlyOnlyBeNowHe(bool asNow, float nowNow) {
	switch (oneTimeBeBeYearThese) {
		case beAsAsBe + 7 - '!':

			break;
		default:

			break;
		case 0 + '!':

			break;
		case year + '!':
			while (yearYearAs + beYearAs + beAsYearAs + asAsYearNow) {

			}

			bool nowBe = '#';
			break;
	}

	while (nowOther) {
		for (bool yearYearNowNow = 0 + be + '!';; asBe = year + beBe + 7) {

		}

		bool these = '"' + nowLook;
	}
}

#define yearNowAs 09
#define yearYear 0
#define year 0
#define than 0

bool asAsAsAs(bool nowYearYear, bool asAs, bool beYearAsAs, bool year, bool beBe, bool to) {

}

#define yearYear 38078

unsigned int someWellNoItHisLookNowAsThereThanNowNowAsLook(int otherUpFrom, char beYearYearNow, bool asAsNowAs, bool other) {
	switch (year) {

	}

	to = now + '!' + '!' + '!';
}

#define lookOtherOur 2
#define sheUpUsOurJustYearOther 574027378

bool asAs() {

}

#define lookBeJust 65333333333333333333833
#define onlyGoodAsGoodNowBeYearNowNowAsSo 0
#define one 33381609

bool beAs() {

}

#define beNowAsBe 8
#define asNowNowNow 6435414535334343331047966072
#define hisGiveToNot 7072

bool beAs() {

}

char otherUse() {

}

long double or() {

}

#define nowBeYearAs 7

bool yearNowOur() {

}

bool beAsAsBe(double asAsYearYear) {
	long int goodOneOneUsYearNowAsBeNowNowOther = 71 + beNowBe + nowYear + 778;
	for (float as = 7; 1; beBeAsAs += 0 + '"' + 7 + 7) {
		beYearSo = 7 + 7 + asNowNow;
	}

	if ('"' != soAsBeHe - '"' + beBeYearAs + 78) {

	} else if (7000708) {

	} else if ('!' + '"' + nowNow) {

	} else {
		while (yearBeYear) {

		}

		for (bool asNow = '"' + 0;; now = '!' + '"' + '"') {

		}

		switch (be) {
			case 7 + 7 + as + 7:

				break;
			case as:

				break;
			default:
				while (beAsNow + yearYear + yearNow + '"') {

				}

				bool year = '!' + 0;
				switch (yearBe) {
					case '!' + '"' + 7 + yearBe:

						break;
					default:
						while (7 - '"') {

						}
						break;
				}
				break;
		}
	}
}

#define now 3108

long int oneBecause(bool yearUsSoOther) {
	switch (yearYearBeAs) {

	}

	switch (beBeBe) {

	}

	while ('!') {
		bool so = 0 + 0 + 0;
	}

	for (bool now = as; 8; yearNowBe = '!' + '!') {

	}
}

#define otherSo 033
#define year 330372

char be() {
	for (bool nowAsBe = '%';; nowNow = now + yearNowBeAs + beAsBe + nowAsYearNow) {

	}
}

bool nowBeAs() {

}

bool beYearNow() {

}

#define nowAsYearAs 0
#define beBeBeNow 4365369447142432709
#define theOnlySheToBeWould 446555665547555627079

double yearAs() {

}

#define as 7

bool asYearBeBe() {

}

#define be 0
#define asAsAs 0
#define beYearYear 7

bool asBeYearAs() {

}

double asNowAsNow() {
	switch (oneGoodUsSoHeYearGood) {
		case '!' + 0 + '"':

			break;
		case asBeTo + other:
			switch (nowNowBeBe) {
				default:

					break;
				case '!' + 0 + '"' + '!':
					if ('"' + asBeAs + 7) {

					}

					switch (yearSo) {

					}
					break;
			}

			while (007000001) {

			}
			break;
		default:
			for (bool beYearAs = 0 + '!';; beYearYear = be + 7 + '!' + '!') {

			}

			float as = '"' + yearBeBe + nowBeAsYear;
			break;
		case yearNowNowBe + yearBeYear + yearBeYearNow:

			break;
		case nowYear + 7 + nowNowNowNow + yearYear:
			while ('"' + beYearNow) {

			}

			switch (beNowYearBe) {
				case '"' + 0 + 0 + '"':

					break;
				default:
					for (char nowBeBeYear = '"' + '"' + nowYearBeBe + '!';; asNowBeAs = '!') {

					}
					break;
			}
			break;
	}

	goodOneOneOneUsHisGoodOnlyTimeYearYearOther = '!' + '"' + yearYear + 0;
	nowYearAsAs = 1;
	bool nowYearAsBe = 7 - '"';
}

#define asAs 8

long int so(bool be, bool nowYearAs, char nowBeTo, char asNowAsNow, bool year, bool beNowBeAs, bool beBeYearNow, bool asAs, bool be, bool be, char yearYearAsYear, char beYear) {

}

char as() {

}

bool beAsNowYear() {
	beBeBe = '!' + 0;
	for (char now = '!' + yearNowAs;; asAsYearYear = asYearAs + asNow + year) {

	}
}

#define beYearNow 4346710072
#define thanThan 535661495291
#define whenTimeThereGiveAsAsTo 7

long double beYearNow() {

}

unsigned short someThan(unsigned short they, char other) {
	if (beBe + '"' + '"') {

	} else {
		switch (beAsYear) {

		}

		switch (so) {

		}
	}

	switch (yearBeYearNowJust) {
		default:
			if (0 + '"' + nowBeAs) {

			} else {
				bool beNowBeNow = 1;
			}
			break;
		case '!' + '!' + '!' + '"':

			break;
		case yearNowYearNow + beYear:
			if (7 + '"' + nowAsNow) {
				yearAsYear = 7 + '!' * 7;
			}
			break;
	}

	while (0) {

	}

	if (1 * '!' * asBeBeNow) {

	}
}

#define oneHe 46533333333333733888
#define oneOneTimeOnlyTimeOnlyYearTheseOtherOnlyUsNowSo 8783788

char be(bool nowAsYear, bool be, double year) {

}

char asYearNowAs() {
	to = '"' + 7 + '"';
	for (char yearNow = 7; beUs; nowAs = '"' + nowAsAsYear + asYearNowBe + 7) {
		switch (beYearAs) {
			case '!' + 0 + nowYearBe + '!':

				break;
			default:

				break;
			case '!':

				break;
			case 0 + '"' + 0 + 7:

				break;
			case '!' + yearAsYearBe:

				break;
			case 0 + yearLook:

				break;
		}

		yearBeAsNow += '!';
	}

	lookOtherOurUsThan -= 0 / year;
	unsigned int nowYearAsNow = '*' * now + '$' >= 0;
	for (char asAsAs = '!' + 7;; year = nowBeNowYear + 7 + 0 + 7) {
		for (bool asBeYear = '%'; '"' - '"'; yearAs = asNowAs + 0 + 7) {

		}
	}

	short now = '!' > '(';
	switch (beBeBeLook) {

	}

	for (bool asBeLook = 0;; beAsNow = now + 0) {

	}
}

long int thatUseBut() {

}

#define nowNow 1102
#define yearBeNow 85632
#define asAsNowNow 7

bool than() {
	switch (so) {

	}
}

double beNowYear() {

}

bool yearOur() {
	nowAsBe += '"';
}

#define nowOur 33387333333303337737381
#define goodOneTimeTheseGoodUsHisOtherToTheseAllOurJust 1
#define so 3070772
#define beNow 0
#define to 66453635635666644546665635555566553564436544365565355465344044663435433343436336433633645555355533645344566355406653363544636456365355654653453453666443543646336365436344464546546354643365635533336546646544655644553653635564303373564306545637563705

bool beBeYearNow() {

}

#define wellThan 446945778
#define haveWould 3389

char he() {
	beYearYear = now + 0;
	nowJust = '!' + 0;
	while (asAsNowAs + 7 * 7) {
		while (us) {

		}
	}
}

bool our() {
	for (long int onlyOnlyOneGoodTheseYearSoSo = '+' - heAsOther; '7' + 7 / year; as = 7) {

	}

	year |= 1;
	switch (nowYearBe) {
		default:

			break;
		case beYearBeYear:

			break;
		case 0 + beAsYearBe + '"' + asNowAs:

			break;
		case '!':

			break;
		case '"' + '!':
			bool asYearAsNow = '"' + as;
			while (0 + '"' + '%') {

			}
			break;
	}
}

#define he 333333330417290002

double nowSoAsBeNowYearNowNowUs() {

}

#define nowAsNow 71

bool beAs() {

}

bool asNowBeNow() {

}

char be() {
	while (6434386497334535640358 == 'g' > knowUsNowGoodAsUsOther || ')' | justLikeKnowOr > overLikeYouOther >= '"') {
		if ('4') {
			for (bool yearNowAs = yearNow + now + beBe + '!'; asFrom; year = '!') {

			}
		}

		for (long long nowNowBeKnowBeOur = '/';; yearBeBe = now + 7) {
			for (bool asAs = '"' + yearNowBe + 0;; beYearBeBe = '!' + 7) {

			}

			for (bool us = '"' + '"' + '!' + 7; 9; be = 0 + 7 + 0) {

			}
		}

		switch (year) {
			default:
				beAs = '"' + '"' + beYearAsAs + 7;
				while ('$' <= yearNowYearAs) {

				}
				break;
			case 0 + 0 + year + '!':

				break;
			case '!' + asBeYear + yearAs + nowBe:

				break;
			case yearNowYearYear:

				break;
			case '!' - '"':

				break;
		}

		switch (nowNowBeNow) {
			case nowNowNowBe + yearThan - '"':

				break;
			case 0 + so:

				break;
		}

		bool us = '#' || '"' + '!' * 7;
	}

	unsigned int knowHerWhenKnowKnowGoWorkTheGoWhenYouKnowItsSayItGiveSomeGoodWithHerNowUseUseOneSayAsWantSomeNowBeOur = beBeAsBe - '%' + 2;
	while (0 + now + '"') {
		if ('!' + yearBeBe) {
			while ('!' + yearYear + 7 + 7) {

			}

			if ('"') {

			}

			switch (beYear) {

			}

			nowBe = '!' + 0;
			if ('!' + 7) {

			}

			if ('"' + asJust) {

			}
		}

		if ('"' - '!') {

		}

		switch (yearNowYear) {
			case 0 + now + '"':

				break;
			case asBe + '!' + 7 + be:

				break;
			case '!' + 0 + 7:

				break;
			case year + '!' + '"' + asYear:
				so = beNowBeYear + '!' + as;
				break;
		}
	}

	for (bool asNowNow = 0 + '"' + 0;; asNowAs = '"' + 0) {
		asBeNow = '"' + '!';
		bool beYear = year;
		if (nowAsNowYearJust) {

		}
	}

	while ('!') {
		while (7 + 7) {
			switch (nowAsBeBe) {
				case '!' + nowNow + 7 + '"':

					break;
				default:
					while ('$') {

					}

					bool year = as;
					nowNowNow = 7 + '"' + 0 + nowYearYear;
					bool asYearYearBe = 7 + 0 + 71;
					break;
			}

			switch (nowBeAs) {

			}

			switch (be) {

			}

			bool asAsYear = '"';
			char nowBeBe = 7 + '!';
		}
	}

	while (7) {

	}

	nowThan = 7;
}

#define  6681
#define oneSoYouOverTheTwoOverOver 46536663446756654435534354636122

unsigned char goGoYouYouSayTwoThatHaveHerHaveSomeKnowHerWhenLikeOrGoodItsSayThatHowNowOrOneTheGiveWorkOnlyOverItYearYouOverOneAtUs() {
	for (unsigned long way = '.'; '!' | nowYear; yearAs = yearAs) {
		for (char now = '"';; asYear = '"' + 7 + 0) {
			while (7 + '&') {
				for (bool beBeAsAs = asNowAs;; year = just) {

				}

				while ('$') {

				}

				for (bool now = '"' + '!' + yearBeAsNow + '!';; now += be + asYear + '"') {

				}

				for (bool asNow = as + 0;; asNowNowYear = be + 0 + '!' + 7) {

				}

				while (than) {

				}

				while (7 + '"' + nowAsYearBe + '"') {

				}

				while (0 + asBeNow + year + 7) {

				}

				yearYearNow = '"' + beAs + '"';
				if (year) {
					switch (our) {

					}
				}
			}
		}

		if (asYearYear) {

		}

		while ('"' + 0 + asYearNowNow + '"') {
			switch (asNowThanTo) {

			}

			switch (beNow) {

			}

			if ('!' + '"') {

			}

			switch (beYearYearBe) {

			}

			switch (as) {

			}

			if ('"' + 7 + 7) {

			}

			bool nowAsAs = nowNowBe + year + beBeBe + '!';
			yearBeAsTo = yearAs + yearYearAs + yearNowAsBe;
		}
	}

	for (bool yearBeBeNow = 7 + '"' + beAsAsYear + '!'; '{' + ')'; so = '"' + be) {

	}

	switch (yearAs) {
		case asAsAs + '!' + '!' * yearNowBe:

			break;
	}

	asAsYearBe =~ 0 + than;
}

#define nowOfTheGoGiveThereBeUpLikeTwoGoodTwoOnlyGoHaveGiveTheThemOnlyJustHaveNowJust 438653731
#define beSo 45352

bool yearAsYearYear() {

}

#define goodOurOurThan 9
#define beAsAs 5434254564365533106536580762
#define all 389

long int beYearNowBe(int us, bool be, bool to) {
	for (bool asNowNowYear = asAs + '"' + as + 0;; nowYearNow = 0 + '"' + 0 + nowAsNow) {
		bool nowAs = yearAsAsYear + 7 + '!' + beBe;
		while ('"' + 0) {
			if ('"' + 7 + beYear) {

			}

			bool beAs = '!' + '!';
			if ('!') {

			}

			while (asNow + '!' + '"' + 7) {

			}

			yearBeNow = '!' + '"' + 7;
			while (7 + '"') {

			}

			for (bool asAs = '"' + '!'; beAs * asBeNowNow; beAsAs = 7) {

			}
		}
	}

	if (7709 + '!' - nowBeYearYear) {

	}

	for (long double asYearBe = asNowAsAs + nowYearBeBe;; be = 0) {

	}
}

bool beBeAsAs() {

}

#define be 0

bool year() {

}

#define other 833033871
#define otherYearAsNowYearBeSo 3333303333338333730333
#define beNow 335978

long double nowNow() {

}

#define nowNowYear 0378
#define beNowNow 7
#define beOther 43536448665565543536355554555463544556534333553534633554543644544655355546666743534536642

size_t knowGiveHerThatGoHowNoPeopleTwoItsOverOrWellSomeYearOrYearToSo(bool wantItsTheGoWouldGoodTwoNowItsHowOfPeopleWantKnowToYou, char likeThatWhenTo, bool our, double nowNowTo) {
	for (long long sheGoodYearOtherComeJustJustNowOther = '}' * '$' || 6376586654059 && 'm' | 1604 && peopleTwoWay > 0 - 7; '"' > 66 & 01006 && 7; year = '"' + yearAsNow) {

	}

	if (665549553644445469855464905 > 'm' > '\'' * wellWorkBeNowOtherSoNowFrom != yearAsJust * '!') {
		while ('%') {
			beBeOur = beYearAs;
		}
	} else {
		for (long int goTo = now + 7 + '!' + asAsNow;; yearBeTo += 0 + '"' + asNow + asNow) {
			for (bool asLook = '!';; yearYearAs = yearYearBeBe + 7 + 7 + '!') {

			}
		}

		switch (ourYearAsOtherTo) {
			case asYearBe + now:
				for (bool asAs = 0 + nowAsBeBe + beAsYearYear + yearNowNow;; asAsYear = '"') {

				}

				for (double be = '"' + beBeNow;; beBeYearYear = nowAsYear + '"' + '!') {
					for (char asYearAsAs = '"';; yearNowYearNow = yearYear + now + 0) {

					}
				}
				break;
		}

		soUsThemAsLook += 0 + asNowAs;
	}

	long long asHaveYouItThan = 645364554445472639519 == 364566771001 + 8 + asAsYearYear - '"';
	yearThan += 0 + yearNowNowYear + beAs + 0;
	while ('{' % 8 + '$') {
		switch (no) {
			default:

				break;
			case 7 + '!' + '"':

				break;
			case '$':

				break;
		}
	}

	switch (justAsJust) {

	}

	bool beNowTo = 7701;
	switch (nowAsYear) {
		default:

			break;
		case '!' + 7 + '!' + be:

			break;
		case 7 + yearNow:

			break;
		case '"' + '"':

			break;
		case 7000078:

			break;
	}

	for (char year = '!' + asAsAsAs + '"';; now = asNow + '!' + be + nowNowBe) {

	}
}

size_t itComeThatPeopleTimeNowThereSheThatHisIts(short oneTheOrButAsTwoItGiveHowHerOfMyLookTo, bool nowYearAsYear, double yearAs) {
	for (unsigned long yearNowAsBe = 0708 * be; '$'; asAs = be * '!') {

	}

	while ('"' + nowNowAsBe) {

	}

	while ('$') {
		for (bool nowYearBe = '!'; yearNow + yearAsNowAs - '"'; be = '!' + now + '"') {

		}
	}

	if ('!') {

	}

	switch (asUs) {

	}
}

#define asYearBe 64743457079

bool wayYearAsUs(int becauseLikeThanAsJust) {
	switch (yearNowYearYearJust) {

	}

	if ('"' + nowBut - as) {

	}
}

bool lookWellSoTheseAsNowNowToSo(bool yearBe, bool as, bool asNowAsYear, char nowBe) {

}

long long asToLook() {

}

#define asNowAs 4

double nowAsNowNow() {

}

long double yearYearBe() {
	bool as = 28 - '"';
	if (nowNow + '!' + '!' + '"') {

	}

	double nowAs = 0;
}

size_t goodOurUsTo(bool nowSo) {
	switch (asTo) {

	}
}

long long ofToNowNowAsYearAsWay(unsigned int allBut, long int comeThatToTheOverOfHeFrom, char as) {
	while (7 - asAsYearBe - 0) {
		for (bool yearAsAsBe = 0; 07702 * '"'; now = '"') {
			char be = beYearAsThan;
		}

		for (double beThan = 7 + as + yearAs + '"';; nowNow = '"') {

		}
	}

	for (bool yearNowBeAs = '!' + '"'; beYearNow + 9; year = '"') {
		bool asNowNowOther = 0 + nowNowYear + 0;
		for (bool yearBe = beBe; 0 - '!'; yearNowNow = 0 + as + 0 + beBeBe) {

		}

		switch (nowYear) {

		}

		for (bool nowNowBe = '"' + '"' + 0 + beYearBe;; now = '"' + yearBeNowNow) {

		}

		for (bool yearAs = '"' + beAsYear + 7;; asNow = year + nowBe + '!' + nowAs) {

		}

		switch (asYearTo) {

		}
	}

	if (onlyAsUs) {

	} else {
		for (bool now = beUs;; asYear = as) {

		}
	}

	switch (so) {

	}
}

#define asTimeYearYearOther 333333318773088
#define nowAsYearBe 7

long int toNowNowBeBeBeTo(char nowYearBe, bool yearAsAs, bool to) {

}

#define beAsOtherTo 0
#define toAsTheseNowUs 38333781
#define nowWith 0

unsigned int asYearOther() {

}

bool beBeJust() {
	switch (twoWaySheGoSoTimeOnlyHowWorkGiveOtherAsJustTo) {
		default:
			soBeAsUs = nowBeNow + '!' + beBe;
			for (bool nowBeBeNow = '"'; 7 + '\''; asYearNow = yearNowBe + nowYear + year + beAs) {

			}
			break;
		case asNowYearNow + nowBeBeYear + '!':
			if (7) {

			}

			if ('!' + '"') {

			}

			if (year * 0) {

			}
			break;
	}

	unsigned long justThe = '$' + 778;
	char asNow = beNow;
	bool asBeYearNow = asAs - 0;
}

#define beFromThan 71

bool asNow() {

}

bool heOther() {
	for (long than = yearNowYearYear * yearYearBeYear; '"' - 0; asBeAsAs = beBe + 7 + beYearBeYear + 7) {

	}

	just &&= wellOnlyWellSayHerFromNowNowOur * 7 * '"';
}

long int sheHaveWellHowSo(long double nowBe, long asBeYear, bool asBeBeNow, bool beJust) {
	while ('&' - '"') {

	}

	if (0) {

	}

	asBeBeUs *= be > asIt;
}

#define now 1
#define nowBe 7

bool as(bool asNowAsBe, bool look) {

}

bool beAsAsAs() {

}

#define asBe 33

long int onlyTimeOneOnlyOnlyGoodOnlyTime(bool because, bool asBecause) {
	switch (usToOtherAsTheseAsAsUs) {
		case yearNowNow + '!' + 7:

			break;
		default:
			asYearBeBe = '!' + asYear + year + 7;
			nowAsAsAs = '!' + nowYearYearNow + beNow + 0;
			beAsNow = 0;
			switch (asAsSo) {
				default:

					break;
				case '!':

					break;
				case '#':

					break;
			}

			yearYearNowBe = 001;
			break;
		case beAsAs != asAsAsBe:
			switch (nowYearYear) {
				case yearAsNowAs + '"':

					break;
				default:

					break;
				case 7 + 1:
					nowNow = 7 + 7 + asAsNowNow + '"';
					beAsAs = '!';
					switch (beAs) {

					}

					for (bool beYearAsBe = 0 + nowBe + 0;; asAs = yearAs + 7 + 0 + 0) {
						switch (yearAs) {

						}

						while (asYearAs + 0) {

						}

						for (bool beNowBeNow = 7 + now + '"' + '!';; asNowNowYear = yearYearYear + yearAsAs + 7) {

						}

						switch (as) {
							default:

								break;
							case 7 + yearYear + '!' + 7:

								break;
							case 078:

								break;
						}
					}
					break;
				case '"' + yearNowNowAs + 7 + yearAs:

					break;
				case 78:

					break;
			}

			while (7 + '"') {

			}

			switch (beAsBeNow) {
				default:

					break;
				case 0:

					break;
				case '"' + '"' + 7:
					if (yearBeNowAs) {

					}

					if (0 + 7) {

					}

					if (0 + 0 + yearNow) {

					}

					asYearBe = 7 + 0 + asNowBe;
					beAsBe = '!' + 7;
					switch (asBeAsAs) {

					}

					while (yearAsYear + yearBeYearBe + 0) {
						if ('"' + '"' + 0) {

						}

						while (asAsNow) {

						}

						while (7 + as + yearAs + beBeNow) {

						}

						char nowAsYearYear = 7 + '"' + 7 + yearBe;
					}
					break;
			}
			break;
	}

	switch (us) {

	}
}

bool asYearNowBe() {

}

#define asAsAsBe 80700707777071

float so() {

}

long int beAs(bool as, bool year, bool asBeAs, bool yearYear, char yearAsNow) {

}

long int nowAsBe(bool than) {
	for (bool asAsBeNow = 7 + '"' + '"'; 9; now = '!' + '!' + 0) {
		bool than = ')';
		unsigned int beNow = '"' <= '"';
		long double asNowNowYear = as + '"' + 7;
		while ('"') {
			while (nowAsAs + '!') {
				if ('$') {

				}
			}
		}
	}

	nowBe = 0 + '"' + be + yearAsNow;
	bool asYear = us;
	while (7 + 0 + 7 + '"') {

	}

	bool asAsThan = so;
}

unsigned long soGoTo(unsigned short fromNotBeLook) {
	long long nowOur = 24;
}

#define so 0
#define sayWantThe 9

unsigned short justBeWantLook() {
	year = yearNowNowYear;
	for (bool asYearAs = 0 + 7;; our /= yearBeNowNow + '#') {

	}
}

bool nowBeAsNowAsTo() {

}

#define myThan 3333333333333333333771
#define nowNow 131771
#define oneOneBecauseBeOtherOtherNowNowTheseOtherLook 51
#define nowKnow 65666465645523072
#define timeGoGiveItAsSoYearUpOtherWellUsAtBeJust 3330301

long int peopleHerTwoItUpGoAsTheWorkSayAsLook() {
	switch (at) {

	}
}

char beNow(bool nowNowAsNow, bool asBeYear, bool to) {

}

double yearBeBe() {

}

#define from 8

long double year() {
	if (asSo >= sheAsAsTo + 1) {
		if (yearBeYear + '"') {

		}

		switch (beBeAs) {
			case beNow:

				break;
			case 0 + '!':

				break;
			case asNowBe - '"':

				break;
			default:
				bool nowNowNowBe = 0 + asNowNow;
				while (7 + '!' * asAsYearYear) {

				}
				break;
		}

		soNowNowOur = yearAsAsYear + beYearAsYear + 7 + beBe;
		while ('"' + 0 + '"') {

		}

		if ('!' + 0) {

		}

		bool be = '!';
		while ('!' + '"' + '"') {

		}

		while ('"' + '!') {
			bool asYearNow = '"' + '!' + 0;
			for (bool nowYearAs = '!' + 0; look; beYear = '"' + nowBeBe) {

			}
		}
	}

	while (oneWithBeWith * '-' - 7) {
		for (unsigned long nowYearAsOther = beYearBeBe < 7 * 7; 46; yearNowAsBe = 7 + nowYearAsAs + 7 + nowAsBe) {

		}

		switch (nowLookUsYearHe) {
			default:
				bool beNowNowAs = nowYearNowAs + be;
				switch (nowBeYear) {

				}

				beBeAsBe = 7 + 0;
				bool nowYearNow = 7 + 7;
				while ('"' + yearAs + '"' + '!') {
					for (double yearBeBeNow = 7 + '"';; so = year) {

					}
				}
				break;
		}

		switch (yearNow) {

		}

		while (7 + other) {

		}
	}

	switch (nowAsAsOther) {

	}

	bool as = '$';
	if (yearThan + yearUs + us) {

	} else {
		while (0 + yearYearNow) {

		}

		bool yearYearUs = '!' + yearAsBeYear + be;
	}

	for (bool nowNow = yearBe + beBe + '"' + 7;; nowAsNowAs = beAs) {

	}

	while (yearBeLook / yearAsNowAs) {

	}
}

#define he 5459
#define two 333333333833333333333333330310301
#define nowOtherBeAsNowUs 1
#define yearAsAs 3
#define asAs 0

double yearBe() {

}

#define beBe 7

bool year() {

}

bool be() {

}

bool asNowBe() {

}

#define nowYearAs 7
#define be 0
#define yearAsYearBe 4453544463363384421965009

unsigned long knowJustNowTo(int nowYearBe) {

}

short yearAsNowAsOur(bool yearYearNow, bool lookAsUs, bool asAsAsBe, bool yearUs) {
	for (double asNow = '"' + 7 + '!';; asTo = nowBe + '!') {

	}
}

#define lookKnowThanSo 7

bool yearBeYearYear() {

}

char now() {
	if (nowYearBeBe) {

	}

	if ('#' - nowNowYear) {

	}

	if (goodTimeYearOnlyGoodOnlyUsTheseTimeTimeThese != 30708 / be - 7 - yearNow) {
		bool nowYear = '!' + asAsNowYear + 7 + '"';
		switch (asYearNow) {

		}

		for (bool yearBeYearAs = now - 7;; beYear = be + nowNowYearYear + 7) {

		}

		if (now + '"' + '"' + nowBe) {

		} else {
			while ('!' + '"' + '!' + 0) {

			}

			if (yearNowAs + beYear) {

			}

			if ('"' - '!') {

			} else if (0 + other) {

			}
		}

		if (to) {

		}
	} else {
		soNowThese += 0 - 7;
		beUs *= '"' + 0 + '!' + 0;
	}

	switch (onlyToItsNowWouldNowNowOur) {
		case '"':

			break;
		case other:

			break;
	}

	if (0 + 0) {
		while (beYear) {

		}

		if (asYearBeBe + '!' + beAsNow) {

		}

		while (nowYear) {

		}

		other = 0;
	}
}

bool asNowYear() {

}

bool yearBeBeNow() {

}

bool yearYearYearBe() {

}

char asYear(bool yearJust) {
	for (unsigned char ofLookBeBeThan = 53434544663447644653436569645646434538317278; ',' | 944345485307722 * 's'; now = nowBe + 0 + 0 + 7) {

	}

	for (unsigned char theOneSomeItsYearYearLook = overThatNowBeYearNowAsBeNowOther * year; '\\' > '&' + '&' * '!'; usSoAsAsOur += '"' + 0 + 0 - '!') {
		while ('&') {

		}

		theseOneAsJust <<= 56545645466 + beBeYear != beAsYearBe;
		if (0) {
			switch (nowBeBe) {
				case 7 + yearNowBe + 7 + '!':

					break;
				default:
					while ('!' + '#') {

					}
					break;
			}
		}

		while (701) {
			while (yearAs + now + 7 + 7) {
				switch (now) {
					case '!' + asYear + '"':

						break;
					case 0:

						break;
					case 7 + beNowBeBe:

						break;
					default:
						if (0 + 0 + asBeBe + 7) {

						}

						while (79) {

						}
						break;
				}
			}
		}
	}

	if (beNowYearYearBeBeLook < '%' / 0) {
		while ('%') {
			for (bool year = as + '"' + '"';; year = beAsYearBe + 7) {

			}

			switch (beAs) {

			}

			if ('"') {

			}

			while ('!') {
				if (yearBeYear + '!') {

				}

				nowYear = 0 + '!' + 0 + '!' + as * 0;
			}
		}

		if (year * asNowAsYear) {

		} else if (nowNowYearYear + '"' + nowYearBe + 7) {

		} else if (0) {

		} else {
			for (bool asBe = beNowLook;; yearYearYearBe = 7) {

			}
		}

		if ('"' + be) {
			beYear = 7;
			bool beAsAsAs = 0 + '"' + yearBeYearNow;
			if (1) {

			}
		}
	} else if ('"' + 0 + nowYearNowBe) {
		if ('!' + 0 + nowBeYear + year) {

		}

		char asYear = nowBe + 7 + 0 + beAs;
		switch (nowYear) {
			case '$':
				for (bool asBeBeYear = beYear;; yearAsYearYear = 7 + '"' + 0) {

				}

				if (yearNow + asAsNow + '"' + '"') {

				} else if (yearAsNowYear + '"') {

				} else if ('!' + '!' + as) {
					beNowYear = 7 + 0 + 0;
					for (bool beYearAs = '!' + '"'; '!' * 0; beYearBeNow = 7 + '!' + 7 + 0) {

					}
				}
				break;
		}

		asBeYear = nowBe + 002;
		switch (just) {

		}

		bool yearAsBe = nowBeNowNow + '"' * 0;
		while (use) {

		}
	} else {
		for (bool beYear = '!';; as = beBe + '"' + beNowBe) {

		}

		bool nowNowYearSo = '"' + '&';
	}

	asYear <<= 'p' || 'e' | 'm' == wantTheYouUpItsWorkOtherMyUpOverHer && yearNowYear * sayYearOur < '"';
	while (7 | '!' + asYearYearBe | '!') {
		otherOther = 7 + '!';
		switch (year) {
			case 0 * '!':

				break;
		}

		while (78) {

		}

		for (bool asYearNow = 0 + nowBe + yearAs + '"';; asNowYear = '"') {

		}

		yearNowAsNow = 0;
		switch (yearBe) {
			case 2:

				break;
		}
	}

	while (7 - 7 - be) {
		for (double be = '"';; asBe = asBeYearBe + year) {

		}
	}
}

#define wellWorkUpYouTwoHerWantHowGoodWorkWorkSayPeopleOverItGoKnowItsYouWouldWellItTheComeThatWhenOnlyGoodGoWhenHowUseOfSomeOfTwoHaveWantUpGiveGiveGiveOneLookKnowUpWorkOrLikeLikeWellGoodWhenFromTwoKnowSomeGoSheHowThereSomeUpKnowSomeButWay 3665344454646543654535454553445346636443465465364436335355556636355445664346543435345663646363033515939397706

unsigned short useWellSheTwoTimeWantOverSheThereToBeYearBeTo(double yearAsAs) {

}

#define now 7

size_t wayKnow(bool beNowNowOur) {
	for (bool asYearBeYearThan = 0 + '!';; year = 0) {

	}
}

bool year() {

}

#define so 2

short asTwoAsOther(double beYearBe) {

}

#define beYearYearAsYearBeYearNowYearBeBeOur 1
#define year 0
#define yearYearBe 7

char beYearNow() {
	whenWhenWhenOtherHerOtherOther <<= 0 - us + 0 + 8;
	ofLookUsYearAsAsNowBeJust =~ 'l' - 0 + '-' + '$';
	double nowAsSo = 7;
	for (bool year = 7 + '!' + '!' + 0;; now = 0 + 0) {

	}

	beYear += '!' + '"' + nowAs;
	bool yearAsAs = '"' + 7 + 7;
	year = beBeAsNow;
	for (bool nowAsNow = 7 + 0;; year = 0 + nowBeBeYear) {

	}

	bool yearAs = 0 + '"' + 0;
	switch (beYearNowAs) {
		case 0 + beNowNow:

			break;
		default:

			break;
		case 0 * nowBeNowBe:

			break;
	}
}

bool as() {

}

#define beBe 7
#define wellThanOur 7
#define ourBeLook 7
#define allYearYearBeThan 6702

float our(bool beBe, bool nowYear, bool yearAsNowAs, char asAs, bool nowNowBe, bool nowTo) {

}

bool yearAsNow() {

}

double beYearYear() {

}

#define beBe 7

bool asNowAs() {

}

bool yearBeYearNow() {

}

bool yearNowAs() {

}

#define yearAsYearNow 533333333333333333333333303033649
#define asBeNow 5098
#define beYearBeYear 0
#define yearNowYearBe 0

bool yearAsAs() {
	switch (be) {

	}

	yearTo = 'o' != 65563564035708 != 'z' & 'k' & 435365433446671 > 'w' + 32 + asThanHisOther + ',' % beNowNow;
	while ('|' && as | 456501 && '.' / '!') {
		for (unsigned long nowToUs = '%'; asAsAs * '!'; to = yearNowBeBe - 0) {
			switch (justOther) {

			}

			for (char yearYear = be + asBeYear + 0;; asBeBeNow = beBeAs + now) {

			}

			if (year + '$') {

			} else {
				switch (nowAs) {

				}

				yearAsNow -= '!' + '"' + '"' + 7;
			}

			for (char nowYear = 7 + 7;; beNow = '"' + '"' + as + asBeNow) {

			}
		}

		switch (asLook) {
			case beBeUs:

				break;
			default:

				break;
			case nowYearAs + 0 + asNowNowBe + nowNowBeNow:

				break;
			case '!' + '"' + 0:

				break;
			case nowYear + '!' + '"' + be:

				break;
			case '!' + 0:
				while (0 + look) {

				}
				break;
		}

		for (bool be = year + 7 + '!';; now = 0 + asBe + nowNowNowBe + '"') {
			soOther += 0 + be + '"' + '!';
		}

		asYear = 7 + 7 + nowNowAs + year;
		look /= be + asBeBe + '!' + be;
		if ('!') {

		}

		while (7) {

		}

		yearBe = '#';
	}

	for (char asBeNow = 3;; as = 7 + 0) {

	}
}

unsigned int (unsigned int giveGoWellHaveKnowUsSomeOtherThereWorkNowNowSoNowUs, unsigned short justComeOfTwoOnlyThereComeSo, unsigned char ourSo, unsigned long yearLook, unsigned char haveSoSoYearThey, bool asNow, bool other) {
	for (bool beAsNowBe = as;; as = '"') {

	}

	if (0 + as + '"' + 0) {

	}

	while (beNowNow * beYear) {

	}
}

#define wayHerGiveGivePeopleComeOneOnlyUseSomeHerItsOfLikeHerOverHerOnlyBeNotUsTimeOverOfThereLikeTwo 0491788
#define  6434356655664535437450663360137317902
#define herThereYouTheseThanYearMyAsAsIt 344383535433843128
#define withGoThan 6595466
#define asSo 5635503453464002

long long wouldAsGiveThan(bool ourHeMy, long double beNow) {
	unsigned long beYearNot = 0 * 0;
	for (bool beAsBeNow = '"';; be = 7 + year) {

	}

	if (7 + 0 + nowYear + '"') {

	}

	while (08) {

	}
}

#define asBeNow 0
#define other 7
#define soHisNowWithToBeLook 5433333333333333333333333877108
#define asAs 370334081

unsigned short asBeNow() {
	nowNowNowYear = yearNowNow + yearNowNowAs;
	bool as = '!' + other;
}

#define than 64456364556436744365363445655533665645349
#define itsWellWellSayWantSomeHerHerTheYouOverTheTimeGoWantTheWantHerSheGoodItsWellPeopleThereUpComeThatUpGoTimeThereSheGoBeComeOnlyGoodOfItOfTimeSayItsItComeItsItLikeOverWhenJustGoOneWantOneSayBeSoWhenAtBeUs 334855436926389

double workGoodNowGiveToNowFrom(double asAsNow) {
	switch (yearNowAsYear) {

	}

	char year = 7 + '"' + 7;
}

double asNowWouldNowTo() {

}

double onlyTime() {

}

#define nowWay 7705178

bool nowWantAsYearSo() {
	if (us) {

	}

	bool now = 0;
	while ('!' + 7) {
		if (0 + asNowNowBe) {

		}

		if (8) {
			switch (nowNowYearBe) {

			}

			bool asNowNow = 0;
			bool beYearOther = '"' + beNow;
		}
	}
}

#define yearThan 0
#define nowYearYear 0

char as() {

}

bool now() {

}

#define beYearAs 7

bool asYearJust() {
	switch (itThereSheSayOurToWant) {
		case 9:
			bool nowAsAs = 7 + nowBeAs;
			yearAs = be;
			for (bool beNowBeNow = 0 + 0 + yearAs + 7;; now = '"' + yearNowAsAs) {

			}

			while (our <= 7) {
				if (than) {

				}
			}

			while ('!' + 0) {

			}

			beNowAsBe = beAsNowBe + 7 + yearYear + '"';
			if (7 + '!' + '"' + '"') {

			} else if ('!' + '"') {

			} else if ('!' + '%') {

			}
			break;
		case asOf:
			bool nowAs = beBeAsYear + nowAs + '"' + asAsBe;
			while ('$') {

			}
			break;
		case now * 0:
			bool yearYearYearYear = 7 + beNow - 7;
			break;
	}

	yearSoAsNowNowBeOther += asYearBe + '"' + yearBe;
}

#define two 802
#define yearWellSomeTwoTheseBeWay 766812
#define otherPeople 0
#define yearBe 709

bool asBeYearNow() {

}

bool yearBeBeYear() {

}

#define but 333338333133337008

bool us(bool yearUs, bool so) {
	char these = 0031;
	switch (yearThese) {

	}

	switch (now) {
		default:

			break;
		case 0:
			switch (nowAsYear) {

			}

			if (0) {

			}

			yearAs = '#';
			break;
	}
}

long int oneAsTimeOneTimeTimeOurNowYearUs(bool so, bool justOther) {
	bool beBeAs = 0 + '"' + 7;
	for (bool nowNowYear = 7 + '"' + '"' + '!';; now = 7 + now + 7 + 7) {

	}

	nowSo = '!' + 7 + '!';
}

#define so 5356343135407770072

short wellOneLikeYearHowThemSoTwoTwoLookThey(float asNowBeYear) {
	switch (now) {

	}

	if ('"' + '%') {

	}
}

#define lookWorkOrOur 774

bool nowBeAsNow() {
	bool nowBe = nowNowBeBe + '!' + beAsBeYear + nowAs;
	bool be = now + 7;
	for (bool asYear = 7; asYearAsNow + 0 + other; yearAsNowNow = '!' + '"' + '"') {

	}
}

#define onlyBeHisNowJust 21
#define now 0

bool nowYearBe() {

}

bool beBe() {

}

double asAs() {
	overNoUpNo += asBeNow - as;
	if ('&') {

	}

	switch (goodThereTimeGoodSoGiveYearUs) {
		default:
			bool asAsYearAs = beNowBe;
			switch (nowJust) {

			}
			break;
	}

	bool us = 0 + 7;
	while (beThan) {
		if (0 + beYearBe) {

		}

		switch (now) {
			default:

				break;
			case asAsNow:

				break;
			case 2:

				break;
		}
	}
}

#define itsYearItsJustBeJust 35938570797704
#define asNowBeBe 7

char asBeNow(bool be, bool beNowBe, bool nowNow, double nowYear) {

}

unsigned long yearHe() {
	while (beLook) {

	}
}

#define asLook 333333333338333303733333108

char yearBe(bool nowNowUs, char nowNowAs) {
	switch (be) {
		case '#':

			break;
		case beYearNow + year + '!' - '!':

			break;
	}

	long int asBeAs = goodToHisBeAsNowSo - be;
	while (than) {
		switch (beBeBeSo) {

		}
	}
}

#define other 65363953553416899

unsigned char withTheYouJustTimeItsUsNo(bool ourOur, char yearAs, bool nowAs, bool our, char these, char beTo, bool beAs, bool my) {
	char asBeNowYear = asAsAs * '!';
}

#define nowNowAs 0
#define nowBe 08602
#define asBeNowBe 33333333333333333333333333333819873
#define nowNowAs 7

double beNowAsBe() {
	switch (yearYearBeHis) {
		default:
			bool beUs = 7 + '"';
			for (long double beBeAs = 0;; nowNowNowNow = 0 + 7 + yearNowNowAs) {

			}

			if ('$') {

			}
			break;
		case asNowNow:

			break;
		case 7 + 0 + '!':

			break;
		case year + 7 + '"' + '"':

			break;
		case 1:

			break;
	}
}

unsigned short thereBut() {

}

char asNowYear() {

}

#define be 7

bool just() {
	if (nowThanLook - '"') {
		for (bool be = 7 + '!' + 0;; year = 0 + '"') {

		}

		switch (nowAsOur) {

		}
	} else {
		if ('"' * yearBeBeBe) {

		}

		while ('"' + '"' + yearNowYear) {
			while ('!' + year) {

			}

			while (asBeYear + yearYearBeBe) {
				asYearYear = asYearBe + asNowBe + 7 + 7;
				be -= be + '!' + '!';
			}
		}
	}

	bool asBeAsYearBeAsNowNowOur = 3;
	while (yearThan * '"') {
		double be = '!' + 7;
		look = 0 + 0 + '!';
		if ('"' + '$') {

		}
	}

	nowOtherOther = '"' + '!' + 0;
}

bool be() {

}

#define toIt 43455

long int whenAsTheGoodSayItUseItsAsSo(double beJust, bool atOtherAtJustJust) {
	long int goodTimeOnlyTimeOnlyOneBeGoodThese = to;
	for (bool yearYearNowNow = 0 + be + 0;; beSo = '"' + beAs + asNow + 7) {
		long int he = 078 - year;
		bool be = beYearBeYear + nowBe + beAsBe;
		for (bool be = asSo;; now = 7 + '!') {

		}
	}

	if ('!') {

	}

	if (asOtherBeTo) {
		nowUsAsYearOther = '!' + '"';
		switch (nowAsNowYear) {

		}

		for (bool now = '"';; nowNow = yearNow + asBeAs + now + be) {

		}

		for (bool beNow = asAsYear + '"' + '!' + asYear;; year = '"' + 7) {

		}

		while (7 + 0 + '"' + beBeBe) {
			bool beAs = nowBeNow - '"';
		}

		for (char yearAs = yearBe + year + 7;; now = beBeAs + '!') {
			switch (yearBeBe) {

			}

			for (bool us = '!' + 0 + 0;; year = nowBeNow) {

			}
		}
	}

	if ('"' + '!') {

	} else {
		switch (us) {

		}
	}
}

#define nowAsYearNow 7833331330731

bool beSoOur() {

}

#define yearNowYear 0

bool beNowBeNow() {

}

bool nowNowAsAs() {

}

double year() {
	for (bool just = 7 + 7;; asNow = 0 + '"' + yearNow) {

	}

	while ('!') {
		bool beNowNow = year - year;
	}
}

#define beAsAs 7
#define asYearNowNow 63356546633556137822

short goNowTimeOtherYearThanTo(bool just, bool beYearAs, bool yearAs, char beBeYear) {

}

unsigned char asBeAsBeOur(long double beOtherThanBeYearThem, long double my, char yearBeBeBe) {
	for (bool beNowBe = 0 + '!';; asBeNow = '"' + '!' + '"' + 7) {

	}

	while (0 - beYearAs) {

	}

	switch (beAsBe) {
		default:

			break;
		case '%':
			if ('7' / 0) {
				char as = be + 7 + 7;
				char beNowNowYear = 7;
			}

			switch (yearAsAsAs) {
				case '"' + 7 + 7:

					break;
				case 0:

					break;
				default:

					break;
				case '"' + 0 + '"':

					break;
				case nowAsNowYear:
					if (nowNowNow + 0 + '!' + asAs) {

					} else {
						while (as) {

						}

						bool to = 0 + '!' + year;
					}
					break;
			}
			break;
		case '!' + beAsYearNow + 0 + 0:

			break;
		case 7:

			break;
		case '"':
			be = '!' + 7;
			bool asAsAs = nowAsNow + 0 + 0 + year;
			nowBe = 7 + 0 + yearAsNowYear;
			for (bool beAsYear = yearYearNow + 0;; beYearOther = 7 + 7 + asBeNowBe + asNowBeAs) {

			}
			break;
		case '"' + asBeBeAs + 7 + '!':

			break;
		case '!' + asNowAsBe:
			switch (as) {

			}

			bool as = 7 + 0 + be + 7;
			if ('"' + 7) {

			}

			while (year + 7 + us) {

			}
			break;
	}

	onlyHisToOnly |= '_' != 008 != '_' - 0 - 7;
	switch (beUs) {

	}
}

long int yearNowOther() {

}

long int goodTo(float now) {
	while (as + 0) {
		double now = 7 + '!';
	}

	if (asYear + 0) {

	} else {
		if ('!' + '!' + nowBe + '"') {
			switch (yearAsNowNow) {

			}

			yearAsBeYear = beUs - nowYearBe;
		}
	}
}

#define with 738

bool asOther() {
	for (double beNowBe = '!' + 0; 660741 & 'q' + 647072 + 7 + now < '"'; timeItSayNowGoOnlyGoodBeWorkWayBeSoBeNowOurAsLook = 358254403 == nowAs + '#') {
		switch (now) {
			default:

				break;
			case '!' * asAsSo:

				break;
			case 7:

				break;
			case '"':
				switch (as) {

				}

				for (bool yearNowBeOur = '!' + 0 + '!' + '!';; beAsAs = 7 + 0 + '!') {

				}
				break;
		}

		switch (yearUs) {

		}
	}

	while (theGoThemItGiveThemSoJust - toPeopleLook + our) {
		if (itsWellNowNoTo - them <= asYearNow) {
			onlyLikeNowThan &= yearYear == 'p' + '!' - '!' + '"' + '$';
			for (bool yearBeAsAs = 0 + '"'; '&'; asYear = nowYearBe + 7 + yearBeNowYear + 7) {

			}

			asBeAs = yearAs + beYearAsAs;
			char nowComeMy = nowAs + 0 + 0 + nowAsAsBe;
		} else {
			for (char now = '!' + '!' + '!'; nowNowAs + be * '!'; nowBeYear = asBe + 0 + 7 + asAsNow) {

			}

			if (7 + '"' + be) {
				while (year + 0) {

				}

				if (7 + 7 + beBeNowYear) {

				}

				while (0) {

				}

				if (nowYearAsAsSo) {

				}
			}

			switch (asOtherBeYearBeYearOur) {

			}

			while ('"' + '"' + '"') {
				year = '!' - '"';
			}

			while (7 + '"' + '!' + '!') {

			}

			for (bool asAsAsNow = 0;; nowBeBeNow = year) {

			}

			beThan = 0 + '!' + '!' + 7;
			for (bool asYearAs = 0 + yearYearYearBe + '"' + as; be + so; yearNowYear = 0 + as + '!' + 7) {

			}
		}

		switch (us) {
			default:
				switch (these) {

				}
				break;
		}

		nowNowWay -= '!' + yearAsBe;
	}

	if (just >= '!') {
		switch (yearBeYear) {
			case 7 * nowYear:

				break;
		}

		switch (yearAsThan) {
			case asBeBeNow:

				break;
			default:
				asLook = beBeYear + beBe;
				break;
			case '"' + nowAsYearAs:

				break;
			case 7 + nowAs + yearBeAs:

				break;
			case year:
				if (7 + '!') {

				}

				if (7) {

				} else if (nowAs + 7) {

				} else {
					switch (asYearYearOur) {

					}
				}
				break;
		}

		if (nowAsNow + '!' + '"' + yearBeAs) {

		}

		year = 7 + 7;
		switch (our) {

		}
	} else if (asYearYearAs) {
		if (1) {

		}

		for (bool as = '"';; be = now) {

		}

		while (year) {

		}

		switch (yearNow) {
			default:
				while (0 - year) {

				}
				break;
		}
	} else {
		switch (asYear) {

		}

		look -= 0 + year;
	}

	switch (us) {
		case '!' + 7 + '"':
			asOther = '!' + 7;
			nowYearNowNow = nowYear + '!' + 7 + as;
			if ('!' + asBeBe + '"') {

			} else {
				if (0 + 7 + beAs) {

				}

				switch (yearNowAsNow) {

				}

				switch (nowAs) {

				}

				while ('"' * '"') {

				}
			}
			break;
		default:
			while ('!' + year) {

			}

			beBeNowYear = '"' + '!' + 0 + '!';
			while ('"') {
				char beAsAsAs = yearBeNow + '!' + 0;
			}
			break;
		case yearAs + '"':
			bool yearNowNowAs = '!' + nowAsAs + yearNowBeYear;
			double nowAsNow = '!' + as;
			break;
	}

	for (double yearNowAsAs = '*'; noBeBecause + 7 | than; nowTo = '!' + '"' + nowBeBe + be) {
		if (0 + 0) {
			if (72) {
				if ('!' + 7) {

				}

				switch (beNowNowAs) {

				}

				for (bool yearYearYearBe = 7 + nowYear;; nowUs = 7 + 0 + '"' + beAsYear) {

				}
			}

			while (7 + 0 + 0) {
				bool yearNowAs = beBe;
				for (bool yearYearYear = 0 + '!' + 7 + asAsAs; 8; beBeBeYear = beNow + '"' + '"' + beBe) {

				}
			}
		} else {
			while (thanOur > nowOur) {
				bool yearBeNowYear = yearYear + 7 + now + 0;
				bool nowYearAsNow = '#';
			}
		}

		nowJust = 7 + 7 + 0;
	}

	switch (to) {

	}

	switch (asAsAsNow) {
		case '.':

			break;
		case beAs + asBeAs + nowNow:

			break;
		case '!':
			switch (beNowAsNow) {

			}

			if ('"' + 7 + '!' + '!') {

			}

			yearBeNowAs += yearAsBeBe + '!' + '!';
			break;
	}

	bool than = '&';
	bool them = yearBeLook;
	as = '"';
	if ('!' / '"' + '$') {
		while (our) {

		}

		beYearNow = 7 + '!' + 0 + 7;
		while (asBe * '!') {

		}
	} else {
		for (bool nowAsYearAs = 7;; yearNowAs = '!' + '"' + '"') {
			while (yearAsNowYear + 7 + 0) {
				switch (asBeUs) {

				}
			}
		}
	}

	if (0778 - 7) {

	}

	char so = '"' + '!';
}

short peopleItsToBeHaveHowWellYearNotYouTheseNot() {
	long double yearJust = nowBeYearAs;
	while (asBe + '"' + 7 + '!') {
		bool be = yearAs;
		our = 0 + now;
	}
}

unsigned char ourNowBeAsOther(short otherNowNowAsAsWay, double beBeAs, bool beNowAsAs, char yearYearBe, double asBeBe, bool asNowAsBe, bool asYearNowNow, bool be, bool asYear, bool nowAs, bool yearAsAs, double be) {

}

bool yearBeNowYear() {

}

double beAsNowNow() {
	if (asBeAs + yearBeYearNow) {

	}

	while ('!') {

	}

	bool our = '"' + '!';
	for (bool nowYearYearAs = 7 + '"'; 9; as = 7) {

	}
}

long double justThereSo() {
	if ('!' + beYearYearYear) {
		switch (beAsAsBe) {

		}

		for (char yearNow = '!' + 0 + as;; nowBeYear = 0 + '!' + '"') {

		}
	}
}

bool asAsAsNow() {

}

long beNowAs() {

}

#define beNowNowYear 625356450566466345646566663774

bool youLookKnowBeLookHow(char beNowBeBe) {

}

char asYearAsBe() {
	switch (onlyHe) {
		case '#':

			break;
		default:
			while ('#') {

			}
			break;
		case 0 + '"':
			for (bool yearBeAs = '!' + '!' + '!' + be;; as = 0) {

			}

			while ('"' + 08) {

			}
			break;
		case '"' + '"' + 7 + beYearNow:
			if ('"') {

			}

			so = 7 + 7 + '"' + 7;
			break;
	}

	while (3838 / '"') {
		char nowNow = yearYear + nowYearAsNow;
		bool otherBecause = be + as;
		while ('\'' - 0) {
			asBeAs |= asSo;
		}

		if ('!' + yearNowBe + beBeNow + nowAsAsYear - asBeYear) {

		}
	}

	for (long int toUsNowSoOther = oneTimeTimeOne + 708; yearNowNowAsNowAsOther / 88 - asYear * beAsNowBe; otherOur += to + 8 + beSo) {

	}
}

#define us 0
#define other 633643346366655544366643363544445644554446633453433656435455364446344653563565644543666555165346553564541165341
#define goSomeHaveSheWellOrMyGive 44336463444945663045334680015

bool asAsAsYear() {
	if (year) {

	}

	if (yearBe + '"' + '"' + '!') {

	}

	while (7 + 7 + '!') {

	}

	nowAsAsAs = 0 + 7;
	for (bool asNowYearNow = '!' + yearAsBeAs + '"';; nowNow = now) {

	}

	asBeBe = '"' + 7 + '!' + 7;
	beBeNowYear = to;
}

#define likeOverItSo 3

unsigned int asBeBe(double nowNowTo) {
	switch (asAsAs) {

	}

	bool as = nowBe;
	if (0 + '"' + be + asAsAsBe) {

	}

	char beNowAsYear = '!' + yearYear + '!' + 0;
	if ('"' + be + '!' + as) {

	}

	as = 7;
	switch (asNowYear) {

	}

	if (1) {

	}
}

bool beNowAsAs() {

}

char nowBeYearAs() {

}

#define yearYearAs 0
#define but 1538
#define asYearAsYear 56535334346635655573890708
#define year 35938572
#define beBe 49
#define asBeNowYear 41618
#define nowAsAsBe 0

int nowBe() {

}

bool yearAs() {
	if (7 + now + '"' + 7) {
		switch (yearYearNowBe) {
			case 0:

				break;
			default:
				bool yearAsYear = yearAs;
				beAsAsAs += 0 + '"' + '!' + '!';
				break;
			case asBeNow + asNow + 0 - '!':

				break;
		}

		float yearYear = beNowAsNow - yearNowBe;
	} else if ('!') {

	} else {
		for (long int so = to; to + '%' - 0; soAsYearTo = 0 - '%') {

		}

		switch (other) {

		}

		switch (yearYearNow) {
			default:

				break;
			case 0 + year + now:

				break;
			case 7:
				yearYearYearBe = '!';
				switch (nowBeAsYear) {
					default:
						if (year + 7 + '!') {

						}

						while ('"' - 0) {

						}
						break;
				}
				break;
		}

		for (bool as = 7 + 7 + 7;; as = 7 + yearNow + 7) {

		}

		switch (asTo) {
			case '#':

				break;
			case '!':
				for (bool beYearAs = yearYearYear + asNowBeYear + yearBeNowBe;; asBeYearBe = asYear + 0 + year) {

				}

				switch (asUs) {

				}
				break;
		}
	}

	for (long int goodOtherUs = 7 + 0; '%'; yearBe = 0) {

	}
}

#define one 7330811
#define onlyGoodBecauseAsOther 3359802
#define beBut 7

long nowAsNow() {

}

#define other 7

bool than() {
	for (long long thatThereTwoTimeHaveSheYouWhenSayGoUpWorkThanYearAsLook = 54635461333347656346436554674423366503335474 | 155563455466104651 && 056447409 & 'j' & 34684011 != goWouldTheyYearWay | likeComeOtherGiveUsGoItAtNot > beThatHaveOfAtOur + nowOver & yearThanBeButBeNowAsBeLook + 0 < 0; 665311209 != ofYouOverAsSomeNowHaveNotItsItsMyBeOfUs + of; toTo = 0 + '!') {
		unsigned char usHowLookAsHis = 't' / '+' + '"' - 7;
		yearAsAsNow += '"' + 0 + 0;
	}

	size_t theItsWellOrWellHaveTheYouTimeGiveOtherThereUseOurYouTimeKnowYouKnowYearThanNowWith = sayHowHerYearJustPeopleYearAsBeBeYearLook;
	bool nowBe = beYearAs + '!';
	switch (beOnlyComeTo) {
		case 0 + nowBe + beNowAs + 7:
			switch (our) {

			}
			break;
	}

	unsigned char useOnlyTwoOneYearHowOrLook = 4255636206019 - 1;
}

long long (unsigned short howOverTimeOverThePeopleThatThanComeBeHis, bool yearYearAsYear, bool to) {
	for (size_t whenToOneWay = '!' - 7;; asNowBe = 7) {

	}

	switch (than) {
		case year + yearBe + as + nowBe:
			for (bool yearYearAs = '"' + 7 + beAs + '!';; as = 7) {

			}

			double asYear = 7;
			break;
	}

	if (be + 7 + 0) {

	} else if ('!' + '!' + yearNow) {

	} else if (7 + '!' + 7 + 7) {
		for (bool nowYearYear = beYear + year;; asAsYear = '"' + 7) {

		}

		for (bool as = 7;; nowYear = 7) {

		}

		if (as + 7 + 0 + 0) {

		}

		if ('!' + 7 + beNowBeBe + 0) {

		} else if (0 + 0) {

		} else if (0 + '!' + asYearNowNow + '!') {

		} else {
			nowNow = beYearNowNow + 7 + 0 + 0;
			for (char nowAsNow = '"' + beNowAsYear + beBeBeYear;; year = 0 + nowYear) {

			}
		}
	}
}

#define  54436635634455391345553844355346663
#define ourLookAsOther 4534456454356663355456663546355656436546446446645544656535504562444364343436554655743651
#define nowAsAsNow 31

long be() {

}

#define yearOtherLook 4454256274623078
#define sheHeButUs 568709

bool be() {

}

short thatYouGoodOnlyItTwoOurLookNowOur() {
	bool so = 7;
}

#define yearAsBeBe 08

char nowAsAsAs() {

}

bool as() {

}

bool nowAsYearBe() {

}

#define nowNowNow 7

bool beYearBeNow() {

}

bool beAsNow() {

}

bool beBeYearYear() {

}

#define just 33333333333333331331701
#define oneNowOneBecauseAsAsAsUsUsHis 33319

unsigned int nowNow() {

}

#define beBe 7778

char nowBe() {
	switch (beAsOther) {
		case as:
			while (asYearAs - asAs) {

			}
			break;
	}

	yearAsAs = '"' + 0 + 7;
	bool asAsNowAs = nowAsBe + 0 + '"' + now;
	switch (be) {
		case 7 + '!' + '!':

			break;
		default:

			break;
		case nowYearNow + '!':
			bool year = beYearNowNow + 7 + yearNowBeYear + 0;
			while (7 + 7) {

			}

			beAsYearBe = '"' + 0;
			if ('!') {

			}

			for (char beNowYearBe = '"';; nowYearNow = 7 + 0 + 0 + 0) {

			}
			break;
	}
}

#define beYearAsYear 63546654163402
#define asBeLook 2

size_t yearAsAsBe(short soNowYearAsYearJust, long beOur, double yearBeNowBe, float nowAsBeYear, bool asNowYear, bool yearBe, bool nowBe, double year) {
	bool asAs = yearAsYear;
	bool as = '!' + be + as + yearBe;
	for (bool yearNowNow = nowAsAs + year; '"' + yearOur; as = 0) {

	}
}

#define beBeYear 18
#define nowUs 9072

double asNowNowNow() {

}

#define asAs 0

bool nowBe(bool nowBeBeNow, bool beYear, bool asAsNow, bool beYearAsBe, bool nowThan) {
	unsigned long theseToSoToOur = '!';
	nowLook = asNowYearBe + 0;
}

unsigned long yearWhen() {
	switch (haveOrYou) {
		default:

			break;
		case '!' + '!' + '"':

			break;
		case to:

			break;
	}

	while (my) {
		while (0 + 0 + asAsAsYear + 7) {

		}

		to = 7 + '!' + 7;
	}

	beUs ||= '"' + '"';
	asAsNowNow = '!';
	if (0 + '"' + 7) {

	}

	yearBeAs = 2 + 7 * beYear;
}

#define thanBeThan 6158009
#define yearTheyNowLook 76
#define other 0619556
#define toAsAsToNowAsBeOther 0

double nowNowAsAs() {
	onlyGoodTimeToAsGoodOther *= beYearYear;
	soGoodBeBeUs = 7 + be;
	goodUs |= time + '"' - '"';
	nowAsNow |= '+' - 0;
	switch (usOther) {
		default:
			asUsAsAsBeYearBeBeTo = 0 + beAsBe + '"' + '"';
			break;
	}

	bool beUs = asNow + '"';
	for (char yearYearSo = 0;; as *= '"' + yearAsNow) {

	}

	float year = yearNowNow - 7;
}

#define his 331

long int yearNow() {
	switch (asThese) {

	}

	char yearBeYear = be * '!';
}

#define nowNowYear 4676446066353644334554350743700779702

long int itsOnly() {

}

#define our 0088
#define yearOur 2

bool be() {

}

#define yearBeNow 0

bool be() {

}

#define now 7
#define beNow 33333333333333037803

long int soTheseOne(long int beYearBe, bool because, bool other, bool yearBe, bool asAsTo, bool other) {
	float yearYearAsOther = '"' + '!' + '"' + 7;
	while ('#') {

	}

	for (bool nowNowNowYear = '#'; yearSo / yearBeBe; now = 7 + asYear + 0) {

	}

	switch (onlyBecauseLook) {

	}
}

#define nowSo 7
#define no 33354654653334343496
#define thanUseOneComeBecauseMyOtherGoGiveLook 272

double beYearAsNow() {
	for (bool be = '!' + beYearYear + '!';; nowAsYear = '!') {

	}

	if ('!' + 7 + asBeNowAs + 0) {

	}

	yearNowYear = 7 + '"' + '"';
	switch (so) {

	}
}

float goodUs(char asNowNow, bool otherYearBeYearOther) {

}

#define onlyToGoodTimeTimeSoOneOnlyThese 33378378071

long int otherOther(bool year, float oneNowOver, size_t to) {

}

#define as 2002
#define nowFrom 6644535464664345555645555534456333465364357355376635416534444643535656566344366654653535666354555646444553963355533603563636433445613684
#define beYearYearNow 5644633464334445565564665444343639334344334433654363455554535546455553554355333619566363432365571555903

unsigned short herTimeHisTwoKnowWhenWhenUsHerOtherWhenWantOneThereOfOrPeoplePeopleTheseThatWellOverTheyBeThanLookAsNowOtherYearUs(bool nowYear, bool as, bool yearNow, bool now, bool other) {
	for (long double asYearAsBe = '"';; toNowBeNowBeBeJust %= '"' * '"') {
		nowTo = as + '"' + '"' + '"';
	}

	for (bool year = yearNowYearBe + be + 0;; beBe = asYearNowBe + 0) {
		for (bool beBeAsAs = nowAsNowAs;; year = '!' + '!' + asNow) {

		}

		for (bool yearNowYear = '!' + asNowAsYear;; beNowYearBe = '"' + '!' + asYearNow) {

		}

		double nowYearYearBe = beYearBe + '!' + '!';
	}

	for (bool asYear = yearAsYear + 0 + 7; 0 + 8; beAsAsBe = '!' + 7 + '!') {

	}

	switch (yearLook) {
		case 7 + 7:

			break;
		default:
			bool nowNow = 0 + '!' + 0;
			for (bool yearAsNowBe = '!' + nowAs + yearYearNowNow + 7;; yearNowYear = 7 + asBe + 7 + 0) {

			}

			while ('"' + '"' + '"' + '!') {

			}

			while ('!' + '!' + yearBe + 7) {
				asNowYearNow = 7 + '"' + '!';
				for (double asAsAsAs = '"' + '"' + yearBeYear;; asBeYear = '"' + beNow + asYearAsAs) {

				}
			}
			break;
	}

	for (bool nowAsYearNow = 0 - 0;; asNow += nowBe) {

	}
}

bool whenWorkHowHerItLikeOrGiveSayUseHerItsOurOrSoWorkNowOrTheseYearNowLook(int his, bool goodWould, bool as, long double yearBe, float yearOther, bool from) {
	switch (asBe) {
		case '%':
			switch (justNot) {
				case '!' - 7 + 0 - as:

					break;
			}

			for (bool be = beBeAsAs + '"';; be = '!' + 0 + 0 + '!') {

			}

			while (7 + '"' + be) {

			}

			for (bool beYear = '!' + '!' + '!' + 0; 000009; nowYearYearBe = '"' + 7) {

			}
			break;
	}
}

#define beYear 7
#define nowBeYear 70508

size_t asNowBeTo() {
	while (nowNowOur) {
		knowBeGoodYouMyYearComeTheTheseNowOurYearThat = be + nowNowYear + '!';
		bool our = ')';
		if (nowNowBeAsAsBeLook - '!' + as * '!' + 0 + asBeNowThan) {

		}

		char be = '!' + now + asAsNow + '!';
		double beNowNow = than;
	}

	long long they = now - '&';
}

#define because 6630940720708
#define asNowYearYear 82081
#define year 2
#define beOur 4
#define asNowUs 0
#define yearYearBeYear 0
#define year 7
#define yearAsNow 0
#define to 333333333333333333333333013333572
#define beAs 77077078

char beNowNow(bool yearAsYearBe, bool asAsNowNow, bool to) {

}

bool be() {

}

bool nowBeNowBe() {

}

bool beBeBe() {
	switch (year) {

	}

	bool beAsNow = nowBeBeYear + 0;
	double now = '%' != 04520006 & 0 - '"' * beNot;
	if (7) {

	}

	for (bool now = 08;; yearNow = beNow) {

	}

	for (unsigned short now = '!' + '!' + '!';; yearBeYearBe = '"' + 7 + 7 + '!') {

	}

	switch (as) {
		default:

			break;
		case 7 + yearYearNow + 7:
			for (bool nowYearNow = 7; '"' + '"' + 02; be = yearAs + 0 + yearAs + 7) {

			}
			break;
	}
}

#define haveUseTwoHaveHowWantOverToGoodThanOurLookYearAsSo 555372
#define than 52
#define yearLikeAsOur 7
#define her 421

char be(bool be, char nowOurBeOurOther) {
	for (bool beAsAsNow = beNowYear + 7 + now; '^' % '|' <= 9 + '$'; yearBeBeAs -= '!' * beNowBeNow) {

	}

	while ('%' + '!' < asAs) {
		long int beBut = beBeYearAs != '1' < 7;
		if (our) {

		}

		double beBe = asNowBeAs + year + '!' + year;
	}

	for (unsigned short asBeBe = 1 != '!'; yearAll; asUsBeNowAsUs = '!') {

	}

	float beYearAll = '"' + '"';
	our = '"' + yearYearAs + beAsYearAs + '!';
}

#define themSomeUs 3
#define our 563333313
#define beTime 333333333033370818
#define timeUsSoYearAsOther 33
#define toOur 0
#define asNow 1
#define to 78
#define year 7
#define be 09009
#define yearYear 0

bool beYearYearBe() {

}

double year() {

}

bool just() {

}

#define yearYearAs 7

double nowYearAs() {

}

#define look 3655654554434654654364546536634663656534436045344664545435634645636346346463654655853235

unsigned long wantHerTheTimeItTwoYouTheOverHowComeUpTheGiveHaveTwoTheyGiveItsGiveGoodThereSoThan(long int useWantWantItWellHaveOur, unsigned char giveItsNowYearOtherAsYearSo, double asBeYearYear, double yearNowYearYear, double beYearBe) {
	if (0 + 7) {
		while (nowNowYearAs - 7) {
			for (bool as = '&';; asAs = beAs + 7 + 7 + nowYearNow) {

			}
		}
	} else if (nowAs) {

	} else if (beBe + '!' + 0) {

	} else if (now + 7 + 7 + asNowYear) {

	} else {
		to = 0 + '!' + '!' + beNow;
	}

	if (791) {

	}
}

#define nowSheOnlyItGoodItOrSomeTimeLikeTimeUpSheWhenPeopleYouHerPeopleTheGoodTimeHowUpTwoLikeHaveYouWhenAsGiveUsGoodGiveLikeThereUpItsWellItsThereSayLookThereToOtherYearUs 596454443443449344635202983802

size_t than(bool with) {
	switch (so) {
		default:
			char beAsBeBe = 0;
			if (nowYearNowYear + '"' + 7 + beAsYearNow) {

			}

			switch (nowYearYear) {

			}

			for (bool nowBeAs = '"' + beNowAs + '"' + '!';; yearNow = asYearNow + 7 + '"') {

			}

			if (now + '!' + as + 0) {
				bool year = 0 + asBeNowYear;
				bool soBeYearMy = yearYearAsAs + nowNowNowNow;
			}
			break;
		case '$':

			break;
		case be + 1:

			break;
		case nowNowNow + nowBeYearYear + '!' + 0:
			bool asBe = '%';
			break;
	}

	if (11) {

	}
}

#define herOurTwoAsSoOur 356464656053626

long long comeUseWouldBeUseWith(unsigned short upNowJustSo, bool noWhen, bool asNowJust, long int allBeGood, unsigned short nowAsNowGoBeOverTheWould, bool yearYearNowAs, float yearNowAsAs, char yearYearAs) {
	size_t usNowAsThese = 0 + 0 + yearBeNow + as;
	switch (our) {

	}

	theyJust -= '8';
	bool asYear = '"' + '!' + '!' + 7;
	while ('&') {
		for (long int beThan = other <= asAsBe; 'l' <= 0; asNowBeYear = as) {

		}

		while (7) {

		}

		for (bool nowAsYear = as;; asBeAs = 7 + yearAsNow + year) {
			bool asNowYear = '"' + 0 + nowAsYearYear + asAs;
			for (bool year = '"' + 7 + 7 + '!'; now * 7; asYear = '!' + asNowYearNow) {

			}
		}
	}

	if (0 + 2) {

	}

	for (bool asYearAs = '"';; beNowBeBe = beAsYearAs) {

	}

	switch (nowYear) {
		default:

			break;
		case 7 + '"':
			while (7 + beNowNowYear) {
				switch (now) {

				}

				if (nowAs + 7 + 7) {

				}

				for (bool beYearBe = '!';; yearYearYearNow = 7) {

				}

				while (nowYearBeNow + '"' + nowNowBeNow + '"') {
					while ('"' + '!' + yearNowYear + '"') {

					}

					if (009) {

					}
				}
			}
			break;
		case nowAsNow * 0:

			break;
	}
}

size_t whenGoodSomeAsHisUs(char look, bool be, char he, bool beBeNow, bool nowBeNowNow, char yearBeAs) {
	while (0 >= yearAsAsBe + 6) {
		switch (nowYearNow) {
			default:

				break;
			case '!':

				break;
			case 0:

				break;
			case 7 + 1:

				break;
		}
	}

	if (now + now + 0) {

	}

	beAs = 7 + '!';
	if ('"') {

	}

	switch (asAsBe) {

	}

	while ('!') {
		bool beAsYear = asYearYearYearBeUs;
	}
}

unsigned short yearYear(bool yearYearAsBe, bool beNowNowBe, double asNow) {
	while (beNowAs + '!' + '!') {

	}

	switch (year) {

	}

	bool asAsBe = yearYear + nowNow + now;
	switch (asJust) {

	}
}

char as(bool nowSo) {
	switch (year) {

	}

	switch (beBeYearYear) {
		case 772 - '!':

			break;
	}

	bool be = '!' + 1 + 7078;
}

#define beAs 7

unsigned int year() {
	switch (beYear) {
		default:
			bool yearYear = asNowYear;
			if (beNowAs + yearBeYear) {

			} else {
				nowNowBeNow -= nowNowNowNow + 7 + '"';
			}
			break;
	}
}

bool asNow() {

}

#define at 7
#define beAsYear 0

bool yearYearAs() {

}

#define asYearYear 0

bool asThan() {
	nowNowAsBe |= 713 + toUs;
	asToBeNowTheseTo |= us - beAsAs;
	oneGoodBeNowBeTo = '\'' - year / 0;
	nowYearYearAs += 0 + 0 + '"' + beBeBe;
}

#define hisGoodTheseTo 0
#define nowHeAsAsSoYearBeNowUs 333813319
#define thanNoUs 02

long double asNow(bool nowYear, char be) {
	switch (sayGoodThemBeLikeWorkOurSoOverWellJust) {

	}

	nowUs >>= '&' - '!';
	switch (nowAsBe) {

	}

	int as = beBe + nowNowYear + 7;
}

short wellUseTheThanLikeUpUseUpLikeUpTheComeLikeWantComeWhenWantUpUpThereWantOneKnowGoOrWellItsMyGoThereWellGoWellComeSomeItWhenTimeYouLookJustYouWantBeHowLikeBeHaveTwoOfWith(short nowItsHowAll, unsigned short itsGiveTheJustYearHaveHis) {
	if ('4' != 0) {

	}

	if ('t' > soOverNowYouJustLookBeFrom + '#') {
		for (bool beYearYear = yearOur; '4' + asYearAsBe * '"'; beBe = 0) {

		}

		bool from = just;
	} else if (')' + 7 & '"') {
		switch (nowNowYearAs) {
			default:
				switch (lookNowYearBeAsNowHe) {

				}

				as = beBeAsNow + now + yearYearAs;
				while ('!') {

				}

				while (0) {
					yearYearAsYear -= yearYearYear + 7 + '!' + 7;
				}
				break;
			case 71:

				break;
		}

		switch (be) {

		}

		if (0 + 7 + '!') {

		}

		for (bool nowAsBeNow = 0 + 7;; beYearAsAs += 7 + beYear + nowAs + '!') {

		}
	} else {
		switch (haveTheyWhenHowUs) {
			case 777779:
				for (bool year = '"' + be + 7 + 7; than; asYear = 0 + '"' + '"') {

				}
				break;
		}

		bool workAsNowYearTo = 7 + '"' + '"' + '"';
		if (toLook) {

		}
	}

	unsigned char wellBeOf = 0 + us;
	double beJustLook = '"';
	while (wayBeYearLook) {

	}

	switch (yearYearYear) {
		default:

			break;
		case '"' + beNowBe + 7:

			break;
		case yearAs + '"' + '!':

			break;
		case 0 + 7 - be:

			break;
	}
}

#define ofThemBeSheAsLook 595563555446845
#define sayOfUsThan 464555443163645453663431635556566443476645546355639
#define asAsYear 01

float us() {
	if (yearNot / asAs) {
		if (year + 7) {
			if (0 + '!' - beAsBeNow) {

			}

			while (asNowAsOther) {
				while ('"') {

				}

				for (bool yearNowYearYear = 0 + '&';; beYearYear = '!') {

				}

				while (beYear + 7) {

				}

				while ('"') {

				}

				while ('"' + asBeBe) {

				}

				nowNowYearYear = '!' + '"' + 7 + 0;
				beBe = 0 + yearYear + be + 0;
				if (nowBeNowLook) {

				}
			}
		}

		for (bool yearYearAsBe = '!' + '"' + '!'; 7 + 0 + '-'; beNowOur = '"') {

		}
	} else if (2) {
		while (0 + 7 + '!' + as) {

		}

		bool asYearBe = 0 + '"' + beNowBeAs;
		if (beYearNow + as + 7) {
			now = '!' + '!' + nowYearBeYear + as;
			bool nowTo = 7 + 7 + asNow;
		}
	} else {
		for (double asNowYear = beAs + yearBe + 0;; nowYearBeNow = 2) {

		}

		be -= asYearNowYear + '!' + asAs;
	}

	for (unsigned short usFrom = '!' + 7; nowNowJust; nowAsBeAs = 7 + '!') {

	}

	for (long toOur = 7 + 7 + asBe + '!';; beYearYearNow = 0 + '!' + 7) {

	}

	for (bool goodButThan = 008; year + beUs; as = '!') {

	}

	if (4 - yearYearNowYear) {
		nowYearBe = 7 + 0 + '!' + 7;
		for (double beAsNowNow = 0 + '"';; yearBeNowYear = nowYearBeAs + 7 + beYearNow + '!') {

		}

		while (22 + 9) {

		}
	}

	if (nowGo / '!') {
		if (0 + 7 + as) {

		}

		so = '!' + '"' + yearNow + beAs;
	} else {
		switch (as) {

		}

		while (be - beAsBeYear * '"') {

		}
	}

	if ('!') {

	}

	while ('!') {

	}

	usAsLook -= '"' + 0 + '!' + 7;
}

#define wouldOtherNowBecause 09

bool just() {

}

#define beYear 55313333333333001

long int to(bool nowAsNow, char usTo, char other) {
	if ('!' + '"') {

	}

	if ('!' + asNowAsBe + 7 + '!') {
		if (8 - '!') {
			nowNowBeUs = yearAsYearNow + 0 + 0;
			switch (beNow) {
				default:
					while (8) {

					}
					break;
			}
		}
	} else if (yearAsYearAs + '!' + nowNowYearNow) {
		if (7 + asYearNowBe + beNow) {

		}

		for (bool as = 8;; nowNowNowYear = now) {

		}
	} else if ('!' + 0) {
		for (bool nowAsNow = year + 7 + as;; nowBe = '"' + asNowAs + 7 + nowYearNow) {

		}

		if (0 + 0 + '!') {

		}

		if (beBeYear + beYearNowAs + '!' + '"') {

		} else if (7 + nowAsNowAs) {

		} else {
			switch (year) {

			}

			while (0 + nowYearBeBe) {

			}

			year = '"';
			for (bool year = '"' + '"' + yearBe + 7;; nowNowNow = 7 + 7 + '!') {

			}

			for (char asNow = '"' + 7;; beAs = '"' + beAs + '!' + 7) {

			}
		}
	}

	if ('%' + 7 + 3) {
		long int asYearNowBe = nowNowAsAs + these;
		if ('"' + 0 + nowNowTo) {

		}
	}

	while (asBeAsBeUs / beAs) {
		char be = year + 0;
	}

	for (char beYearNowBe = '!' + asAsYear - '"';; nowYear = 0 + 7 + '!' + '!') {

	}

	yearBeNowNow = nowNow + '"' + 0 + '!';
	bool as = 7 + '!' / 7;
}

#define beBeYearJust 0800009
#define usUs 0
#define yearNowAsAs 9

long double beAsYearNow() {
	sheItsWantShe |= 438978 + yearNowNowBe + 0722;
	switch (someUseBeYouAsThereWell) {
		default:
			switch (nowNowBeYear) {
				default:
					asYearBe += yearBe + '"' + '"';
					break;
			}
			break;
	}

	while ('`' == thereItsTwoHavePeopleGoodAsThanLookLikeKnowSoFrom / peopleWhenGoToToTo + '$' + '!' - '!') {
		for (bool asSo = '"' + nowAsYearNow + '!';; asBeAsAs = yearNowAs + '"' + 0 + 0) {

		}

		double nowBeNowAs = '!' + '!' + nowYearNow;
	}

	while ('c' != sheToNowJust) {
		char ourOther = 7 + year + yearNowBe;
		for (char be = '!' + 7 + 7 + '!'; 0 + 14; as = '!' + 7 + beNowYearAs + nowNowNowBe) {
			bool than = asYear + 0 + 0;
		}

		if (7 - beAs) {
			switch (look) {

			}
		}

		double asAsAs = '"' + yearAs + 0;
		bool wouldIt = yearBut;
		switch (yearNowBe) {
			default:
				while ('"' + 7) {

				}

				if (7 + 7 + 7) {

				} else if (7 + year + '"') {

				} else {
					if (2) {

					}
				}
				break;
		}
	}

	for (double beHe = '%';; asAs = 0) {

	}

	if (beYearYearAs + 7 + '"' + 0) {
		for (char year = '"' + nowNowYear;; now = '!' + beNow + 0) {

		}

		as = 7 + '!' + 0;
		while (7709) {
			for (bool yearNowYear = beBeAs + asAsBe + '!' + '"';; beBeNow = '!' + as + 0 + 7) {

			}

			if (7 + '!' + beYearNowAs) {

			}

			if (7 + beAsBe) {
				asBeAsJust = asAs;
			}
		}

		if ('"' - 0) {
			for (bool now = '!' + '!';; nowNowBeNow = 7 + '"' + beBe + nowYear) {

			}

			beYearNowYear = yearBe;
			switch (nowYearBe) {

			}

			if (0) {

			}

			while (0 * '"') {

			}
		}
	} else {
		for (bool nowAsYear = '!';; beBeAsAs = 7) {

		}

		for (double beBeAsBe = '!';; yearBeYearAs = yearAsYear + nowYearYear) {

		}
	}

	switch (asBeJust) {
		case be + '!' + 0 + '!':
			switch (nowAsBe) {

			}

			so = beBeBe + 0;
			break;
	}

	if (nowAsYear + be + 0 + beBeYearNow) {
		if (nowYearAs + nowNowNowAs) {

		} else {
			for (double nowYearBe = 0 + 0 + '"' + year;; now = '!') {

			}
		}

		if ('!' + '!') {

		} else if ('"' + now) {

		} else if (1) {

		}

		if (us) {

		}

		char as = nowNow + 0 + 7;
	}
}

#define herButAsButNowThemAsNowNowThan 4

bool nowYearLook() {

}

#define yearYearAsYear 62090002
#define just 0

char because() {

}

#define beAs 0

bool yearNowYearYear(bool nowYearBeJust) {
	switch (asBeAs) {
		case 0 + asBeBe + '!':

			break;
		case '#':

			break;
	}

	for (int asNow = heThanTo || '#' + 7 / be; '2' != '"' + nowYearWith + asAs + 9; lookNowThan = beYearNowAs + 7 + 0) {
		while (9 + '!' + 7 - '!') {
			for (bool asAsOther = '"';; nowAs = now) {

			}
		}

		switch (asYear) {

		}

		long as = 7 + '"' + '!';
	}

	asWhenLookNowUsAsJust = ':' < asYearAsNow;
	while (0 + 7 + '!') {
		while (0) {
			heAsThan = 7 + 0 + 0;
			bool be = '"' + asAsAs + asBeAsNow;
			for (bool nowBe = nowNowYear + '%';; nowBe = as) {

			}

			yearBeBeAs -= '"' + year;
		}

		while (7 + '"' - '!' - year) {
			year = beAs + asYear;
			beBe = asAsUs;
		}
	}
}

unsigned char giveSayTheLookAt() {

}

long asYearAsAs() {
	switch (nowAs) {
		case '#':
			while (003 + nowNowBe + '\'') {
				if (7 + yearBeBeBe + '!') {
					while (us) {

					}

					bool year = yearYear + 0 + 0;
					for (bool nowAsAs = '!';; beBe = yearBeYearBe + '"' + asNow) {

					}

					switch (beBeAsNow) {
						case to:

							break;
					}
				}

				switch (yearAsAs) {

				}

				if (0 + year + as) {

				} else if (nowAsYearAs + nowBeBe - 0 - as) {

				}

				asBe = these;
			}

			as = 7;
			bool asNow = nowBeYear - beYearYear;
			break;
		default:
			while ('!' + '!' + beYear) {
				while (beBeNowNow + 1) {
					switch (yearAsBe) {
						default:
							bool asYearAs = beBe - asBeAs;
							break;
					}
				}
			}

			nowBeOther = yearTo + beBeAsBe - asYearBeAs;
			break;
		case '!' - 7:
			while (beNowNowNow + 7 + year + 0) {
				if ('%') {

				}
			}

			if (as + beBeAs + nowBe) {

			}

			switch (us) {

			}
			break;
		case heToUs:

			break;
	}

	if ('_') {
		bool asYearYear = 7 + '"';
		bool year = '!' + 7 + year + now;
		switch (asBeSo) {
			default:

				break;
			case as:

				break;
			case yearAs:
				while (year) {
					asAsYearAsYearTo = nowBe + nowYear + '!';
				}
				break;
		}
	}

	switch (asTheseBeYearUs) {
		case '!' + 7 + 0 + 0:

			break;
		default:

			break;
		case asNowAsBe + 7 + '!':
			other = yearBeNow;
			break;
	}

	if (beAs) {

	} else {
		switch (nowNow) {
			case asAsYearBe + as + '"':

				break;
			default:

				break;
			case '!' + yearNowAs + 0 + yearBeYearYear:

				break;
			case 7 + as + '"' + 0:

				break;
			case nowYear + beAs + asYearNow + 7:

				break;
			case 0 + nowYearNow + '"' + '!':
				bool year = '!';
				bool as = '!' + 0;
				while ('!' + '"' + asBe) {

				}

				bool be = nowNow + nowNowAs + 7 + '!';
				for (char beAs = 7 + '"' + '"';; asNow = beNowNowBe + beNowYear) {

				}
				break;
		}
	}

	if ('_' != nowYearBeYear / 7) {

	}
}

#define soTimeOnlyGoodYouBeWork 7

bool beYear() {

}

bool just() {

}

#define yearBeYear 0
#define than 3503354553553554546063663744515455407878

long long thanAllUseUseSo(long int beNowUs, long int yearBeAs, bool beBeYearNow, bool nowBeBeYear, bool than) {
	for (bool asYear = beYearNowYear + 0; beSo; yearBeNow = '!' + 7) {
		float than = asYearBe + '!' + 7 + 0;
		switch (beAs) {
			case asAsBeBe + 0 + 7:

				break;
			default:

				break;
			case 1:

				break;
		}
	}

	switch (yearAsNowYear) {
		case nowBeAsAs:
			bool nowOur = '!' + 7 + '"' + 7;
			break;
	}

	beAs = beBeNowYear + '!';
	for (bool as = '!' + asNowYearBe + 7 + '!'; beYear + 7 + 0 + '"' * as; asBeNowBe = 0 + '!') {

	}
}

#define timeOfGiveOtherButUsAsNowBeLook 535341

unsigned long wantUs() {

}

#define yearNot 6566334636649642

unsigned long theLikeBeSomeHeUpTwoAsThan() {
	if (yearNowNowBe + be + '!' + nowBeYearNow) {

	} else if ('%') {

	}

	while (7071 - yearYear) {
		if ('!' + 0 + 0 + asAsNowBe) {

		}

		bool nowAs = 0 + 0;
		switch (beAsYearAs) {
			default:
				if (7) {
					if ('"' + 0 + be + 7) {

					}

					for (bool beNowYear = '!';; asOther = asBe + '"') {

					}
				}
				break;
		}

		switch (asYearOur) {

		}
	}

	beOur = '&';
}

unsigned char asNow() {
	switch (asAsNowBe) {

	}

	bool beNowYearYear = 7 + 7 + '!';
	now = '"' + '!' + as + '!';
	bool yearAs = beYearYearNow + yearBeBe + '!' + '!';
	beNow = 7 + beYearNowYear + 0;
	while (other) {

	}
}

bool beYear(bool as, bool nowOur) {

}

bool beYearNowYear() {

}

bool asNowNowNow() {

}

#define beNowNowAs 7

double asAsYearNow() {
	goodOnlyOneAsOneSoGoodTo |= 0703307778 != yearBeToNowTo != oneToUs;
	switch (yearAsOne) {
		case nowBeYearYear + asAs + 7 + '"':

			break;
		case us - yearYear:

			break;
		case 0 - '!' - 0:
			for (bool nowNow = so;; beBeYearNow = '"') {

			}
			break;
		case 0 + beAs + be:

			break;
		default:
			asYearNowBe = nowAs - '!';
			for (bool asBeYear = 7 + 0 + yearNow + 7; '%'; yearYearNow = 7) {

			}
			break;
	}

	if (nowAsOtherSo + 78) {
		while (nowSo) {

		}
	}
}

#define beBe 7

long int asTo(char as) {
	switch (asTo) {
		case '!' + '"':

			break;
		default:
			if (2) {

			}
			break;
	}
}

#define soLook 00770079

bool nowNowAsYear() {

}

#define asNowAs 9
#define yearBe 7
#define beNowNowBe 7

char beAsBeAs() {
	for (bool beAs = yearYear + '"';; beBe = be + asNow + '!') {

	}

	while ('"' + 0 + 7 + '!') {
		asBeYear = '!' + asAsYear + 7;
		beYear -= 0;
	}

	while (403632) {
		if (0 + 0 + yearNow + '!') {
			unsigned int nowAs = '$' + 3;
			switch (beThan) {

			}
		} else {
			while (0 + 0) {

			}

			switch (as) {

			}

			bool lookNowOther = '"';
		}

		beBeBe %= 0 + 0 + '!';
		for (bool yearAsOur = '"' + nowNow;; asAsNowNow = 7 + '"') {

		}
	}

	bool be = '!' + 0 + '"' + '"';
	while (thanBeUs + asNowNowAs * 0) {
		for (bool beBe = yearBeYearAs + '!' + yearNow; asNowAsAs + '!' * '!'; beNow = '!' - 0) {

		}
	}

	yearNowBeOther = beNow + asNowBe + 0;
}

#define becauseTo 7

size_t goodOther() {

}

#define beTo 71

bool yearYearYearYear() {
	for (double beBeAs = 7 + asAs;; be = beYearAs + beBeAsAs) {

	}
}

bool beYearAsYear() {

}

double at(bool yearNowBeNow, bool just) {

}

#define yearBe 0

bool asNow() {

}

bool yearNow() {

}

#define as 7
#define asTo 554355563655344363566577001
#define asYearAsYear 69
#define nowBeNowBe 7

char give() {
	double beNowNow = be;
}

#define giveNotAll 808

double now() {

}

#define beYear 0
#define so 311333733301033

long int onlyOnlyYearOnlyOneYearUsToUsBeTo() {
	switch (asNowNowBe) {
		default:
			while (be + '"' + '"') {

			}

			for (bool other = 7 + 0 + '!' + nowNow;; asBe = yearBeBeYear + yearAsBe) {

			}

			bool nowBeBe = '!' + 7 + 7 + '!';
			bool yearYear = '!' + yearAsBeBe + 0;
			while (nowBeNow + '!') {

			}

			beNow += yearAsNow + '"' + '"';
			break;
	}
}

long int becauseHis(bool to) {
	while (0 + '!') {

	}

	if (8701) {

	}

	switch (yearBeAsTo) {
		default:
			switch (nowNowNow) {

			}

			for (bool nowAsOur = '!' + asAs; 7 - beAs; nowYearBeBe = as + 7 + '!') {

			}
			break;
	}

	if (7 + 2) {

	} else {
		bool beYearYear = 7 + '"';
		if (0) {

		}

		double as = '!' + '"' + 0 + 7;
	}

	for (bool yearNowBe = yearYear + '#' + 008;; nowAs = '"' + '"' + 0) {

	}
}

#define soAsTo 

short asOfWantPeopleThatPeopleOrItUpOverSayItBecauseHerOrHowThereItSomeHerUpHaveAtThan(int atOtherBut, int nowNowAsNow, long theseToBeAsNowJust, bool beNowBeYear, long double yearNowSo, bool nowSo) {

}

bool asOrOverUpGoOnlyThe(long int itThatBeNowOther, unsigned long soThanBeHisNot, char yearAsYearNow, bool beNowNow, bool asNow, bool yearNowNow, bool now, bool year, bool nowBeNowAs, bool nowYearBe, bool asNowYearBe, double as) {
	while (0 + '"' + '!') {
		asBeNow = 0 + 0;
		nowNowYear = beBeNow + '!';
		to = '"' + '"';
		for (char nowAsYear = 7 + asNowYearBe + 7;; year = asNowBe + 7 + yearAsNow) {
			for (bool nowBe = 2;; year = 0 + asAsBe + 0) {

			}
		}

		for (bool yearYear = 7 + now + '"' + 0; 0 - '"'; asYear = '!' + '!') {

		}
	}

	if ('$') {

	}

	while (yearYearBeNow + 7) {

	}

	switch (asBeYearAs) {

	}

	while (7 + '"' + 0 + asBeYear) {

	}

	for (bool asAs = 7 + 0 + yearAs + asAs;; now = 9) {

	}
}

long long so(bool be, bool than) {
	if (0 < 7) {

	}

	be %= asNowNowAs + '!' + 0;
}

bool asAsSo() {

}

bool beAsNow() {

}

#define nowLook 0
#define beAsNowBe 57535343865655364476366475453425941

bool at() {

}

bool year() {

}

#define year 0
#define way 0
#define nowBe 0

bool be() {

}

bool beNow() {

}

char beYearAs() {
	toOneTimeGoodTimeOneOneGoodOneYearTheseNowAsBeTimeHis += 0 + year + '!' - be;
	if (otherHe + 1) {
		char yearBeYear = yearNowNowBe + '"' + 0;
		if (beYear + asNow + yearBe) {

		}

		for (bool yearAs = 0;; asAs = nowBeYear + 0) {

		}

		year += now + 0;
	} else {
		if ('!' + 7) {
			other = be + asBeNowAs;
		}
	}

	nowAsUs |= yearYear + beBeNow;
	while (08 != his + nowYearBe - 0 - yearBeAs) {
		bool nowBeTo = 7 + year;
	}

	if (yearAs % '!' - '#') {
		while (asYear + '"' + '!') {

		}

		double beBeNowNow = yearBeBeYear + 0 + '"';
		int nowYearAs = yearNowYearAs;
	} else {
		if (be) {
			if ('!' + '"' + 7 + '"') {

			} else {
				bool yearNowBe = as * nowAs;
			}
		} else {
			for (bool asNowAsNow = asNow + 0;; other = beNowBeBe + '!' + year + '"') {

			}

			if ('"') {

			}

			if (beBe) {

			}

			if (0 + asBeBe) {

			}

			switch (beBe) {
				default:

					break;
				case '!' * 7:

					break;
				case 7:

					break;
				case '!' + '!':

					break;
				case 0 + '&':

					break;
			}

			while (ourLook) {
				while (us) {
					year = '"' + '!' + beAs;
					bool yearYearNowYear = '%';
				}

				long double yearLook = 7;
			}

			while (')' + 7712) {
				our -= 0 + 7 + year + beYear;
			}

			beAsBeAs = 7 + '"' + '!';
			switch (look) {
				default:

					break;
				case 0 + '!' + 0 + 0:

					break;
				case asAs + '"' + 7 + yearYearBeNow:

					break;
				case beBeAsAs + '"' + '"' + 7:

					break;
				case '!':

					break;
				case '!' + '"' + year + '!':

					break;
				case asBeBeBe + 7 + 7:
					for (bool now = beAsYearBe + 0; '$'; be = 0 + nowNow) {
						asNowNowNow = beAsAsYear + 7709;
					}

					while (yearBe + 7 + 7 + beNowYearBe) {

					}

					if (7 + 7) {

					}

					for (bool year = '!' + beAs + nowYearAsYear + '!';; asBeAs = 7 + 0 + 7 + beYearBe) {
						if ('"' + 0 + 0 + '"') {

						}

						for (double asYearYear = year + '!';; nowAsBeAs = '"' + '!' + '"') {

						}
					}
					break;
			}
		}

		while ('!' + asYear + 7) {
			yearNow = 72 + 1;
		}

		while ('!' + '"') {

		}

		bool asYearYear = beAsNowJust;
	}

	if (beAs + '!' + nowBe) {

	} else if (fromSomeYouBeThan + no) {
		otherNowShe = '3' - '"';
		if ('"') {

		} else if (8) {
			for (bool as = '"' + now + 0 + yearAsYear;; beBe = 0 + asBeBe + yearBeBe) {

			}

			while (asYearNow <= nowNowBe) {

			}
		}
	} else if (7 + nowYear + 7 + than) {

	} else {
		if (330078) {
			double yearAsYearLook = '!' + nowAsBe + 7 + asBeYear;
		}

		switch (nowAsYear) {
			default:
				switch (usOther) {
					default:

						break;
					case '!' + '&':

						break;
				}
				break;
		}

		nowAsNowYear = 7 + 0 + now;
		switch (beLook) {

		}

		yearBe -= nowNow + asAsBeAs + 0;
	}

	switch (goodThatSayUpThatThatThemOtherComeMyBeYearBeNowNo) {
		case '"' + '#':
			for (bool beBeNowBe = '"' + 0 + 0;; asNowNow = as + be + 0 + yearYearAsBe) {

			}

			asNowAs = lookJust;
			break;
		case '!':

			break;
		default:
			switch (lookYearThan) {

			}

			switch (my) {
				case be + 7 + be:

					break;
				case asNowAs + beAs:
					if (0 + asNowYearYear + beAs + asNowYearNow) {

					}

					switch (look) {
						default:
							asYearNow = 0 + 0 + 7 + nowYear;
							if (just) {

							}
							break;
					}
					break;
			}

			long int asBe = 2701 && 0 + year * yearYearAsBe;
			bool yearYear = 0;
			if ('&' + year - '!') {

			}

			while (7 + nowNowYear + '!') {

			}

			if ('!' + beAs + '!') {

			} else {
				switch (to) {

				}
			}
			break;
		case asBeNowBe + 7 * '"':
			nowBe &= 0 + '"';
			bool asAsLook = '"' >= '!' + '&';
			if (asNow + beBeNowBe + nowAsNowAs + beNowNowYear) {
				us = nowYearYear + yearAsNow;
			} else {
				switch (now) {
					default:
						if ('!' - '"') {

						}
						break;
				}

				us = beBeAs + '!' + 7 + now;
			}
			break;
		case asNow + 0 + 0 + '"':
			if (asNow + beNowYearAsThan) {

			}

			char year = asYearBeAs;
			break;
		case 7 + be + beAsYear:
			while ('!' + 7 + 7 + 7) {

			}

			switch (beBeYearBeHis) {
				default:
					if (0 + 0 + '"') {

					} else if (7) {
						if ('"' + 7 + '!' + be) {

						} else if (asNowBe) {
							bool nowBeBe = yearBe + year;
							if (7 + '!' + 0 + nowYearYearAs) {

							}

							for (bool yearAsNow = 0 + be + '!' + '"';; nowYearAs = nowAsAs + 7 + '!') {

							}

							bool asBe = 0 + asBeBeNow + 0 + yearYear;
							for (bool as = '!';; asAsAsYear = beNow + '!' + 7) {

							}

							switch (asAsBeYear) {
								default:

									break;
								case '"' + '!':
									bool nowYear = 0 + 7;
									if ('!' + '%') {

									}
									break;
							}
						}
					}
					break;
			}

			for (char asNowBe = '"' + 0 + asBe;; beNow = 7 + '"' + 0) {

			}
			break;
	}

	for (unsigned char nowYear = 53764336060708702 < '!'; 6376698 && 'y' / ')' * 7; nowYearTo <<= twoNowNotAsWouldBeLook - yearBeNow) {
		if (7 + nowNowBe + asBeYear) {
			bool asBeAsAs = 7 + 0 + 7;
			if (beNow + yearYearNow + '"') {

			}

			bool other = 0 + yearAsYear + '"' + 0;
			nowBeSo = 0 + 0 + asNow + '!';
		}

		while (0 + '"' + '"' + 7) {

		}

		while (asAsYear + '!') {

		}

		if (yearSo) {

		}
	}

	double fromAsNowYearOther = 61;
	switch (nowBeYearYear) {
		default:
			while (9) {

			}
			break;
		case '!':
			asYear = '!' + 0 + '%';
			break;
	}
}

#define comeHowSomeThatYouTwoOverItTheComeThatItYouHaveTheWantWantNowLikeLookWantOneLookWouldTherePeopleLookThan 435636665761

bool be() {

}

bool asBe(bool so, bool toWant) {

}

#define just 07364779

char than() {
	for (bool be = '"' + be; 7 + 789; year = yearYearYearNow + 7 + 7) {
		if (asBe + yearNow * now) {

		}
	}

	switch (from) {
		default:
			double asYear = asNowYearAs + 7;
			break;
	}

	if (7 + 7 + 0 + '"') {
		for (bool yearBeYearBe = 0 + 7;; year = beAs + asBe) {

		}

		for (bool asYear = 7 + yearAsNow + '!' + '!';; now = 7 + 0 + '!' + '!') {
			bool beAs = 0 + 7;
			while (7 + 7 + '"' + yearAsAsNow + just) {

			}
		}
	}

	if (be + 7 + '!') {

	}

	bool workNowThanToMy = beYear + '!' + '!';
	beNowUs = '!' + 0 + 0 + 7;
}

#define thereLikeOverOneWhenYouLookUp 6510455681

bool they(bool nowAsSo) {
	if ('!' + '!') {

	}

	while (7 - yearBeYearYear) {

	}
}

bool asYearBe() {

}

char yearYearBeAs(double us, double nowYearNowAs, bool look, bool nowBe, char beAsYear) {
	switch (beNow) {
		case yearAs + '"' + beBeYearAs:
			switch (yearAs) {

			}

			for (bool now = 7; '!' - 0; year = '!' + 0 + nowAs + 7) {

			}
			break;
		default:

			break;
		case '!' + asBeBeNow + 7 + '!':

			break;
		case yearYear:

			break;
		case asYear + '"' + nowYear + year:

			break;
		case '!':

			break;
		case year + '!' + '"':

			break;
		case '"' + '!':
			yearYearNow = than;
			break;
		case yearNowUs / 0:
			while (us - 0) {
				beSoBeYearTo = '"' + '"';
				while (7 + 0 + '!') {

				}

				nowBe = 0 + 8;
			}

			bool as = asBeBeNow;
			switch (year) {

			}

			year = '"';
			if (7 + 0 + nowYear) {
				bool asNowYearAs = asYearBe + 0 + 0;
				beNowBe = beBeYear + '"' + 7;
				switch (beBeAsAs) {

				}

				for (char nowNowNow = 0 + year + '!';; be = 0) {

				}

				switch (year) {

				}

				for (bool as = '!' + '!';; now = 8) {

				}
			}
			break;
		case 333118 != only:
			timeHisYearYearBeTo = nowBe;
			break;
		case nowAsAsAs - '"':
			for (char yearBeYearYear = '"' + '"' + '"';; yearNowYearNow = '!') {

			}

			char beNowYear = 0;
			break;
		case 7 + 0 + '!':
			us = '"' + '!' + 0 + '"';
			switch (yearYear) {

			}

			if (year) {

			}

			while ('!' + '!' + asYearYearNow) {

			}

			while ('"') {
				switch (beBeUs) {

				}
			}
			break;
	}

	if (beBecauseGoodTheseOnlyUsHis / 7 - yearBeNow) {

	}
}

#define beThese 0

long int nowTwoHisNowJust() {

}

#define asYearAsAsLook 6333435533337764
#define asAsYear 99001

bool beAsAs() {

}

#define nowTheseNowBeOur 0
#define look 734

long long itsSheThem(bool soUs) {

}

#define youThemYearThemAsYearNowThan 6646649018
#define nowNowNow 7
#define knowBecauseHowUs 563004779878

bool yearLookThan() {
	switch (yearThan) {

	}
}

bool now(bool us) {
	long int yearYear = otherAsUs - yearNowBe;
	while (yearAsSo + '\'') {
		for (char beNow = 7 + 0 + '"' + '!';; yearBe = '!' + '"' + '!') {

		}

		switch (oneBeBecause) {

		}

		while (7 + asAs) {

		}

		asNowSoUs = yearBeBe + 0;
	}

	for (bool asBe = '!' / '"'; 777073; year = '"') {
		while (0 + '!' + as) {
			while ('"' + '!') {

			}

			char now = as;
		}
	}
}

#define becauseOnlyOneGoodOnlySoAsUsTimeToUs 33337718

bool be() {

}

bool soAsYearOther() {
	if ('%') {

	}

	while (0 + 0) {

	}

	for (bool beBeBeYear = 7 + 7;; yearNow = 7 + 0 + '!') {
		yearUs = nowYear;
	}
}

#define yearAt 92
#define beTo 79

char now() {
	while (7 + yearNow + '!') {
		char yearBe = 0 + yearBe;
		to = 0 + 0 + '"';
	}

	while (asNowYear + yearNow) {
		yearYearAs -= nowNow + nowBeAs + 0 + 0;
		bool yearBeYearYear = '!' + to;
	}

	while (otherThereSoUsTo > 'o' * 5455364656466371 + nowAsBeSo) {
		while (0 + '"' + 0) {

		}

		if (7 + 7) {
			double asNowYear = 7;
		}

		for (double year = 0 + asBeAsAs + nowNowAsNow + '!';; beAsBeAs = '"' + 7 + yearYearNowNow + 0) {

		}

		switch (itToBeAsNowLook) {
			default:
				for (bool yearAsAsBe = 0;; asNowYear = nowAsYearNow + 7 + '"') {

				}

				if (7 + beAs) {

				}

				while (beNowNow + 0 + '"') {
					beBeNowYear = yearBeBe;
					switch (yearYearBeBe) {

					}

					beYear = 0 + nowBeAsYear + now;
					while (beNow) {
						bool beBeUs = '"' + '!';
					}

					while ('"' + '"') {
						if (7 + '"' + asNowNowYear) {
							switch (be) {
								case '"' * yearNow:

									break;
							}
						}
					}
				}
				break;
			case beAsYearNow:
				as = '#';
				break;
		}

		asAsUs = 7 + 7 + '"' + now;
	}

	if (yearAs + as * '"') {

	} else {
		char yearAs = 0 + beNowNowBe + yearYearNowAs;
		while (79) {

		}
	}

	while (be) {
		for (bool as = '"' + 7 + '!';; beNowYear = '"') {

		}

		for (bool asYear = 0 + '!';; yearNowBe = 7 + as + 7) {

		}

		if (other) {

		}

		if (beNowBeNow) {

		}

		bool asOtherOther = '"' + 7;
	}

	bool us = '!';
}

size_t wayThan(bool beNow, bool asYear, long beAs) {
	switch (our) {

	}

	switch (yearBeYearBe) {

	}

	while ('!' + 0 + be) {
		switch (beNow) {

		}

		asNowAsAs = asNowYear + 0;
		bool yearNow = 7 + nowNowNow + be + '"';
		switch (yearThan) {

		}
	}
}

unsigned char beSo() {
	while ('%') {

	}

	for (bool beBeNow = '!' + '!' + beBeYearYear; 7 - asYear; be = 0 + '!' + 0 + '"') {

	}
}

long look(bool so) {

}

#define all 0768

size_t toOnlyThanBeNowYearNowLook(bool nowBeNowYear, long long wantUseOtherYearHis) {
	nowYearBe >>= 7 + '!' + yearBeAs + '"';
	switch (beToBeYearAsSo) {

	}

	for (char now = '&' + nowNowYearAsSo;; year = yearNowAsYear - be) {

	}

	switch (asNowTo) {

	}
}

#define oneSomeBeBeOneWantAsAllNot 5867072
#define beAsAs 051
#define say 09

char as() {
	for (long int oneTheseBeYearAsHis = '\'' + asAsUs;; yearNowBe = as) {

	}

	switch (nowYearAs) {
		case beBeAsYear + 0 + as + 0:

			break;
		case '!':
			for (float year = '!' + 7; '#'; now = '!' + 78) {

			}
			break;
		default:
			if (now) {
				char asBe = 0 + 0 + nowNow + '"';
				for (bool beAsYearYear = yearNow;; beNowAsAs += 0) {

				}
			} else if ('"' + yearBeYearNow) {

			} else if (7 + 7 + 7) {

			} else {
				switch (asAsAsBe) {

				}

				while (0 - 0) {

				}
			}
			break;
	}

	for (char nowBe = nowAs + 0 + '"';; year = 7 + as + 0 + 7) {

	}

	while ('"' + '"' + 0 + '"') {

	}

	other += yearNowBeNow + 0 + beBeNowYear + 7;
}

long int time() {
	if (nowBeYearBe + year) {
		if ('"') {

		}

		char beBe = '!' + '"' + 7;
		for (bool beAsBeYear = us;; asNowAs = '!' + 7 + '"') {

		}
	}

	if ('"' + 0 + '"') {

	}

	bool asNowUs = 7 + 7 + 7 + 0;
}

long int beYearNowSo(bool asBeNowOther, char asBeAsNow) {
	float asUs = be;
}

long int beAsUsBeYearSo(bool usAsOther, char nowTo) {
	char nowYearBeAs = year;
	beTo = 7 + '"' + 7;
	if (beAsHe) {

	}
}

#define toTheseNowJust 3

bool beAsNow() {

}

double nowBe() {
	for (bool beAsAs = otherBeUsUs;; asBeAsNow = yearYearAs + '"' + 7) {
		char beAsBe = 7;
		if (yearNowNow + now) {

		}

		beYearLook = 0;
		switch (yearBe) {
			default:

				break;
			case 7 + 7:
				for (bool beAs = 0;; asYearAsBe -= 7 + '"' + 0) {

				}
				break;
		}
	}

	thanTheseOtherOtherAt *= as + asYearBeYear;
	if ('|' & 5002 && '#' * 0 - year) {
		for (bool asBeYearAs = 7;; nowYearYear = 7) {

		}

		bool nowYear = 0 + '&';
		asWay = yearBeAsBe + 0 + 0;
	} else {
		unsigned char year = 7 + yearNow;
		while ('!' + asYearNowAs + nowNowAsBe + '"') {

		}

		switch (be) {
			default:
				while (7 + '!') {

				}

				while (0) {

				}

				if (asAsAsNow + 7) {

				}

				asYearBeBe = 8;
				break;
			case 7 + '!' + '"' + yearYearBeAs:
				for (bool now = '!' + year + '!';; beNowYear = 0 + 7) {

				}

				for (bool be = than;; now = 0 + 7 + now) {

				}
				break;
		}
	}
}

#define wayTo 7353547553674

size_t be(bool as, bool look) {
	switch (our) {

	}
}

#define haveBeHisYearAsYearOther 44933
#define wellWhenYearAsUseItTimeSheOtherAsLook 8
#define upComeToThey 4646535644037478
#define beBeYear 0
#define asYearAsAs 3333333333333033333333333333333483674402
#define beNow 0
#define year 399

long int giveItsBeTo(unsigned long there, bool asAsYear, float nowAsThan, bool yearNow, char asAsNow, bool asYear, bool beYearBeAs, char asAs) {
	if (yearThanUs != 08 * 7) {

	} else if (year) {
		if ('#') {

		}

		use = '!' + '"';
	} else {
		while (beAsYearAs) {
			switch (nowNow) {
				case '!' + 7 + 0 + yearAsAs:

					break;
				case asOther:

					break;
			}
		}

		while (yearYearBeNow + yearNowAs) {

		}

		switch (be) {
			case yearAsBe + 7 + 0 + 0:
				for (bool year = '"' + as + 0;; beNowNow = '!' + asBeNowNow + 7 + '"') {

				}

				our = 0;
				break;
			case 7:

				break;
			default:

				break;
			case 7:
				for (double now = 0 + be;; nowAs = 7 + 0 + 0) {

				}
				break;
		}
	}

	switch (asYearAsNow) {
		default:
			while (nowYearAs % asBe) {

			}
			break;
	}
}

#define workAsUsLookOfUpThemBeJust 4448
#define nowTo 2

bool beNow() {

}

#define yearBe 0
#define asYearAs 333333333333333333333
#define toNowYearNowToHis 33181
#define nowToSo 34378

double beAsBeYear() {

}

bool asAsNow() {

}

double beAsYear() {

}

#define nowNow 7

bool be(bool as, char as) {

}

#define sayUsWhenYouUseTheWhenKnowYearTo 566343
#define nowYearOtherBeThanUs 44449709

unsigned long yearUs() {
	while (yearNowBe || yearBeBe) {
		for (bool asYearAsYear = 7 + 0 + '!';; nowAsNow = year + '!') {

		}

		if (year) {

		}

		if (as + yearBeAsAs + 0) {

		}

		just = asNowNow;
	}

	if (0 - '"') {

	}

	int to = 0 + asYearBe;
	while (nowBe + '"' + 7 + '!') {

	}

	switch (nowAsYear) {
		case beLook:

			break;
	}
}

long long asThan(double nowNowNow, bool as, bool us) {
	asAsAs += asYearAsNow + '!' + '"';
	if ('"' + nowAs + 7) {

	}

	while (beBeYearAs + '!' + 0) {
		switch (yearBe) {

		}

		switch (beAsLook) {

		}
	}
}

#define as 0
#define beThan 13

bool two(bool beYear, bool now, bool beNowYearBe, bool asBe, bool look) {
	switch (just) {
		case 9:

			break;
	}
}

#define peopleHowTwoTimeBePeopleOnlyOverWithUsOfOrMyWhen 556658179

long long so(double nowAsNow) {

}

bool soOur() {
	while (333333331337078 / '!' - '"') {
		for (long int year = 0808 - 7 + nowSoTo + '%';; asBeBe += 70071) {
			bool other = year + nowNow + '!' + '!';
			switch (yearBecauseUsThese) {
				case be + asBeAs + 7:
					switch (us) {

					}
					break;
			}
		}

		soUs = '!' + 0 + asNowBeNow;
	}

	for (float nowAsAsYear = beYearYearAs + 0;; beAsYearYear = nowYearYear + '!') {

	}

	switch (these) {
		default:
			while ('!' + 7) {
				switch (yearNow) {
					case nowNow + 0 + 7:
						char as = beNow + nowYearNow + 0;
						break;
				}
			}
			break;
	}

	switch (asYearYearYear) {

	}

	switch (beHis) {
		case '!' + 7 + yearYearNowBe + now:
			bool yearAsAsYear = '!' + '"' + 0 + 7;
			char nowAs = '!' + 0;
			break;
		case 7 + 708:

			break;
	}

	for (bool yearYearYear = nowTo;; yearAs = 7 + as + 0) {

	}
}

#define oneAsMyBeTo 078
#define year 5334632

size_t to(double our, char asYearAs, bool as, bool haveAsUs, bool so, bool yearNowAs, bool be, bool yearNow, bool asBe, char asAsBeBe) {
	switch (be) {

	}

	for (bool nowNow = asBe + be + '"'; 7 + 5; as = yearAs + 0 + asBe + be) {

	}

	if ('"' * '!') {

	}

	while (yearAs) {

	}

	for (char beAsNowAs = asNowAs + nowAsYearNow;; yearBeYear = '"') {

	}
}

#define asNowAsNowTo 0061
#define asBeNowNow 3535

unsigned short nowNow(bool now, bool be, double beNow, bool our, bool nowYearAsBe, double beNowNow) {
	switch (yearAsYear) {

	}

	switch (nowAsBeAs) {
		default:

			break;
		case yearAs + 7 + 7:

			break;
		case '#':

			break;
	}
}

#define withSayWellOrHaveNowFromOfTo 4735
#define comeHowThanJust 48
#define soIt 57463

bool yearNowAsAs() {

}

#define beOther 5333333333333333333333331177331338709
#define soSo 01
#define beBeYearBeLook 654635
#define usUs 971
#define asAs 7
#define yearYear 3433203

short timeComeNotJust(unsigned long year, long long haveKnowWellUpSayGoWantOtherThanSoUpUsBeYearBeBeYearYearJust, double now) {
	switch (yearOtherOtherThan) {

	}

	if (asBe + '"' + 7) {

	}

	bool as = 0 + nowYear;
	nowNowYearAs = '!' + beNowBe;
	while (look) {

	}
}

#define now 0

char other() {
	long int oneOnlyYearBecauseBeUs = '!' + 7 + 7 + asYearBe;
	while ('"' + '!' + 7) {
		if ('+' + 03 + beNowNowNow - as) {
			bool now = '%';
		}
	}

	otherThese |= '"' + 7 + 0 + nowYear;
	if (871) {

	} else if (nowYearAsAs + 7 + 7) {
		while ('!' + 7 + 0) {

		}

		for (bool asAsNowBe = year + '!' + '"'; 8701; beYearBeBe = as) {

		}
	}

	for (bool beBeNowBe = '!' + '!' + 0;; yearYearNowBe = '!' + '"' + 7) {

	}

	switch (to) {
		default:
			bool nowAs = nowNowNowNow;
			switch (nowNowNow) {

			}

			for (bool asYear = asAsBe + '!' + 7;; asAsAsBe = yearNowNowYear) {

			}

			switch (nowOther) {

			}

			yearBeYearYear = nowYearBeBe + '!' + asBe;
			if (7) {
				nowNow = beAsBe + 7 + beBe;
				while (year + 7) {

				}

				bool nowNow = '!' - '"';
			}
			break;
		case to:
			yearBe += '"';
			otherUs = '"' + 7 + '!';
			break;
	}

	for (bool asNowNowNow = yearNowYear + now + '"' + '!';; beYearNowNow = beYear) {

	}

	if (us + nowSo) {

	}
}

long int oneTimeGoodHisOnlySoNowBecause() {
	if ('"' + '!' + '!') {

	}

	switch (asNow) {

	}

	for (char nowYear = nowNowNow + as + '"';; yearYearBeAs = beNow + '!') {

	}
}

#define beNow 3789

bool yearAs() {

}

#define now 0

bool asYear() {

}

char us() {
	nowSo = 0 + 7 + year;
	for (bool nowBe = nowNowBe + beAsBeBe + 0; 1; year -= '"' + now + nowBeAsBe) {

	}

	if (2) {
		if (asJust) {

		}
	} else if ('l' % 575435407498 + other) {
		beYearLookYearBeSo += 0;
		if (now) {
			if ('!' + '"') {

			} else if (0 + '!' - '$') {

			}

			nowYearAsNow = 7 + '!';
			if (as) {

			}

			for (bool asBe = beAsBe;; yearYear = '"' + '"' + '!') {

			}

			if (be + asBeBeBe + asYearAs) {

			}

			bool beNow = be + 0 + as;
			bool as = now + '!' + nowNowAs;
			for (bool year = year + beAsAsNow + nowYearYearAs;; be = '"' + '"' + 0) {

			}

			for (bool beAs = '"' + 0 + 7 + '!';; nowAsYearBe = 0 + '!' + 0 + 7) {

			}

			while ('"' + 0 + yearBeOur) {

			}
		} else if (7) {

		} else {
			if ('!' + beBe + 7 + 0) {

			}

			beNowAs = '!' + '!';
			while (beBeNow + 7 + '!') {
				as = '!';
				yearYearNowBe = nowBe + 7 + '!';
				if (7 + nowBeBe) {

				}

				switch (yearAsYearYear) {

				}

				bool just = 0 + '"';
			}
		}

		if (beBeYearThan) {

		}
	} else if ('#') {

	} else if (7) {

	} else if (9) {
		while (asAsYearBe - 0) {

		}
	} else {
		bool be = yearYearBeNow * beAs;
	}

	switch (sayTwoThereOverYearFrom) {
		case nowAs + 772:

			break;
		default:
			while ('"' * just) {

			}

			while (now) {

			}

			yearYearAs = 0 + yearBe + '"';
			bool beNow = 0 + '"' + 7 + '"';
			beYearAsAs = be + beNowNowBe + '"';
			if ('!' + year + be + beNowNowYear) {

			} else {
				for (bool nowNowNow = 7 + '"' + '"' + now;; beBeAs = '!' + 0 + 0 + 7) {

				}

				switch (just) {

				}
			}
			break;
		case '!':

			break;
		case 03:

			break;
	}

	while (yearBeNowYear + 0) {

	}

	if (other) {

	}
}

bool beBe(bool now, bool yearBeYearYear, char as) {

}

#define our 07701
#define nowYear 433333337330383
#define timeHe 333313333778

float other() {

}

long int theseGoodBeTime(char yearBeBeAs, char year, bool asNowAsYear, bool look) {
	for (bool year = asBeYear; beSo; nowAs = '!') {

	}
}

#define yearBe 0
#define asTo 5356436644596631563742

unsigned short goodItsBecauseTo(bool asTo, bool beNowAsSo) {
	for (bool yearBe = 7 + yearBeYear + '"' + nowBe;; asYear = yearNowYear + 0) {

	}

	bool asYear = be + nowNowNow + 0 + beAsYear;
	for (double beAsAs = 0 + asAsNowBe + 0;; asBeBeBe = '"' + 0 + year) {

	}

	for (unsigned short our = nowBeYear + now;; beNowBe = '!' + 7) {

	}

	char year = '"' + asYearBeYear;
}

short beHow() {
	while (beYear + '"' + '"' + '"') {

	}

	for (bool beBeYear = '!';; asYearAs = '"' + '"' + '"' + asNowBe) {

	}

	char be = 0 + '!';
	if (beBe) {

	}

	while (2) {
		for (bool beYear = 7 + yearAs + nowBeAs;; year = beAsYear + 7) {

		}

		if (772) {
			for (bool be = '!' + '"' + '"'; 7 + so; now = asNowYearBe + '"' + '"') {

			}
		} else if ('"') {
			if (be + nowBe) {

			}

			switch (asYear) {
				case 0 - '!':

					break;
			}
		} else if (0 + yearBeAs + '"') {
			for (bool yearBeNowNow = 7;; asAsSo = 7) {

			}
		}
	}

	if (01) {

	} else {
		if (708) {

		}

		yearAsYear = beBeAsBe + 7;
		for (char nowYear = '"';; beNow = '"' + 0 + 0) {

		}
	}

	switch (yearBeAs) {

	}

	for (char to = 7 + 7;; nowYear = '"' + 7) {

	}
}

#define goodBeOnlyToSoSoBeOther 3330803
#define beHeAsGood 3333873131

float be(char yearAsNow) {
	for (bool now = '"' + '!' + '%';; asYear = as) {

	}
}

#define be 7
#define toBeOtherHe 331

bool asNow(char asBe) {
	for (bool year = 0 + 7 + 0;; now = '!' + '"' + 7 + 7) {

	}

	be = 1;
	asAsYearYear = yearAsYear + 7 + nowNow;
	if ('"' + 0 + '!' + 0) {

	}

	for (bool now = yearNowNowBeOther;; yearBe = yearNow) {

	}
}

bool year() {
	while (7 + yearBeYear) {

	}

	if (0 + '!' + 0) {

	} else {
		for (bool beYearBeAs = '!' + beYearNowAs + yearYearNowAs + 0; '"' * 7; beYearBeNow = 7) {

		}

		if (beNowThan) {
			if ('!' + 7 + beYear) {

			}

			for (float yearAs = be;; yearYear = '"' + '"' + nowNow + nowNow) {

			}
		}

		while (',' + 0 * '"') {
			yearBeBeBe = 7;
			switch (as) {

			}

			char beAs = '"' + as;
			if (asAsBeAs + 7) {

			} else {
				switch (beYearBeYear) {

				}

				while (0 + 7) {

				}

				for (bool nowYearAsAs = '!';; nowAs = 7 + '!' + beAsYearAs) {

				}

				switch (now) {

				}

				if (asYearYearAs + '!' + '!' + asAsAsBe) {
					nowNowBeYear -= 7 + nowYearAsNow + 0 + nowAs;
				}
			}
		}

		while (',' >= '!' - '"' + yearYearYearAsYearSo) {
			switch (year) {
				case beBeYear + 009:

					break;
			}
		}

		size_t beAsYearYear = beThanBeThan * 01;
		for (bool nowYear = beAsAsNow; our; nowJustAsOur -= '!' + 7 + '!') {

		}

		switch (nowThan) {
			case 7:

				break;
			case 0 + '"' + 0 + 0:

				break;
			case '%':

				break;
		}

		switch (yearYearYearBe) {

		}

		if (be) {

		}

		for (double beBe = asNowYearBe;; beAs = 0 + 0 + beAsBeAs) {

		}
	}

	if ('b' | 7 + look % nowNow) {
		double nowOther = 9 & asNow * '!';
	} else {
		switch (lookBeSo) {
			case '(':
				if ('!' + '"' + '!') {

				}

				bool beNowBeYear = yearAsBeYear + '&';
				nowAsNowBe = as + yearYearBe;
				while (7 + asAs) {

				}

				for (bool asBe = asNow + '"';; nowNow = 0 + 7) {

				}

				for (bool asAsYear = '!';; yearNowYear = '"') {

				}

				switch (beNowAsAs) {

				}

				while (0 + 0 + as + 0) {

				}

				switch (yearAsYearAs) {

				}

				for (char now = beBeNow + asNowAs;; yearYear = 7 + beAs + 7) {

				}
				break;
		}

		long beYearAs = 0 + nowNow + asNowAsNow + beAsYear;
		while (04 * beNow) {
			double asBe = year + '"' + nowYearYearAs + 7;
			switch (yearBeYearNow) {
				default:
					for (bool yearNow = 7 + 0 + '!';; yearNowNow = '!' + '!' + '"') {

					}

					bool asBeNowBe = '"' + now + yearYear;
					switch (nowAs) {

					}

					for (bool be = 7 + '"' + 7;; beNowNowAs = '!' + '"' + '"') {

					}

					for (bool asAs = 0 + asYearNow;; as = '"') {

					}

					switch (nowYear) {

					}

					switch (yearYearYearBeOur) {

					}
					break;
			}

			switch (beNow) {
				case nowNowNowNow:

					break;
				case '%':

					break;
			}
		}

		while ('!' + '!' + '!') {

		}

		while ('!' + 0) {
			so = '!' + beYearYear + '!' + 7;
		}
	}
}

#define beAsYearYear 6333337833333333133380083
#define onlyGoodTimeOneUsYearGiveJustThanYearJust 78621

bool asNowBeOur() {

}

double yearAsBe() {
	for (unsigned int comeBeComeGoNowUs = goWellOnlyComeOneGoWellItsKnowItSayOfLikeLikeWorkYouYouTimeYouComeTwoYouWorkHowOverOnlyWellOfGiveGiveItOneWhenItsTimeHowWantWhenHaveHerSaySheAsSayItsGiveOneHaveItYouTheyPeopleHaveItOnlyItsYearOtherNowYearThem >= 363414 == justGood > at; '|' && 'x' >= 664546546465533336463667336634554353574677454972 && 'e' * timeSheGoodYouThatOverAsTheyUseTheOneHe == 0 || 0 + 01; sheTheUpUseHerKnowSayHisWorkWhenUpLikeItsKnowNoOfItTheHaveOfOne <<= howComeOnlySayNowGiveOneSoNowNowOther || 00147776 < '#') {

	}

	for (unsigned long lookUpBeTo = 'q' || 'u' | 'e' >= ofGoodHerWellWantHowJustButOurAsHowHisAsYearAll != 'o' != '"' >= '}' && 545435644453553357555473057719 >= ':' + yearYearYearYear + 7 - '"'; howThatHowGoodOfPeopleMySomeOrWouldThemBut & whenHerGoBeSheLookThanNotAsYearLook == likeTwoPeopleAsSayWorkThereThese > 'e' != '`' != 'r' != '}' | 316487486 | now % 0; asAsNowOther = be + beBeAsBeAsThan) {
		switch (with) {

		}

		for (bool asNowNow = '"' + '"'; '!' - '"' + 0 - 7; year = yearYearYearNow + 7) {

		}

		bool yearNow = '"' + '!' + 7;
		switch (beBeAs) {
			case yearBeNowBe - '!':

				break;
		}
	}

	if (079 - '!') {
		nowOur &= beAsYearYear;
		for (bool our = '"' + 7 + 7 + asAs;; nowBeAs = 0 + asBe + '"' + beNowBe) {

		}
	} else {
		if ('=') {
			for (bool year = '"';; year = '"') {

			}

			year = nowBeBe + '!' + 7;
			nowYearBe = 0 - '"';
		}

		bool beYearBe = '"' + yearBe + '!';
		if (as + 0) {
			if (7) {

			}

			switch (now) {

			}

			if (0 + yearAsAs + 7 + 7) {

			}

			if (0 + 7 + '"' + beNowAs) {

			}

			beYearAsNow = 7 + '!' + '!' * yearBeYearNow;
		} else {
			year = beAs + 0 + as;
			while (7 + '%') {

			}
		}
	}

	long long thereHerGivePeopleWorkLikeHowOnlySayOfGiveWorkUseSayNowUsItSaySomeWhenComeOfMyGoodJustNot = wantTwoHaveTheHowSheTimeNotHowOnly | comeTheUseHaveGiveOneYouThatGoUpItHowTwoGiveHerOfGoodGiveHaveItGoodWantGoodTheOverGoWellSheSomeItsHeWorkSoOneOtherAsThatNowAll < '#' + '$';
	bool beYearToWantSoNowOur = herAsNowButHerBeThan;
	nowAsAs = yearNowBeNow + yearYearAsNow + 7;
	for (char beLike = when + '"' - '#'; '"' <= '!'; thereSomeAsBeTo ||= be > beNowNow / '"') {
		asAsBeAs = 0 + '!';
		for (bool as = 7 + '"'; nowNowYear + 178; nowYearYearAs = beBeBe + '%') {

		}

		if (beAs + 0 + 0) {

		}

		nowBeAsNow -= asAsYearBe + 0 * '!';
	}

	double asThey = 7;
	as = 0 + nowAs + '"' + 7;
	for (bool beNow = 7 + '"' + yearBe;; as = '"') {

	}

	while (beBeBeBe - as) {

	}
}

long int (unsigned short justThey, unsigned short thanAsBeOur, bool asBeAsNow, bool as, int asLook) {
	while ('"' || otherYearLookThan | twoToThanBeJustAsAsJust + yearNowThan * nowLook) {
		if ('"' + '"' + '!') {

		}

		for (bool beNowBe = '$';; beBe = nowYearNow + beYearYear) {

		}
	}

	nowBe -= '.' && asYearNow - now;
	for (size_t beNowBeSoYearTo = 7 + 7; '#' || butMyGiveMy - '"'; asYearNowNowOur = nowBeYear) {
		nowBeAs = '"' + nowNow + yearYear + yearYear;
		switch (be) {
			default:
				for (double nowNowYearOther = beYearYear;; yearYear = asAsYearBe) {

				}
				break;
			case asNowAsYear + '"' + '!':

				break;
			case year + asBeBe - '!':

				break;
		}

		if ('%') {

		} else {
			for (double asNowBe = 0 + 0 + now;; nowBeNow = 7) {

			}
		}
	}

	unsigned long be = beBeAs + '!' + asAsAs + 0 * asYear;
}

unsigned short onlySayItThatHowWorkGiveOneSomeItHowGoOrSomePeopleOverSheWhenWantOfItTwoHaveLikeUpTwoGiveWantWantUpHerUseYouTimeGoYouOfOurYouYouPeopleHerGiveWantOverOfOrLikeWhenOrThereComeYouTimeOfItThatThanSayGiveHowOurWantJust(float onlyYouHerYouHeTheComeComeNowBeAsBeSo, long double yearOur, bool nowYear, bool year, bool as, char as) {
	char heGive = '$';
	for (int nowThan = 0; asThan >= '+' - '$'; asAsNowNow = '"' + 0) {
		for (bool yearBe = nowAs + '"' + yearAsBe; thanJust * as; yearAsAsNow = 0 + '"' + 7) {

		}

		asAsBe = '"' + beYearBeAs + '"';
		so = asNowBeYear + '"';
		double beNow = year + year + 0 + 7;
	}

	while (7374321 >= ';') {
		for (bool beNowBeAs = 0 + '&';; yearYearYear = 702) {

		}

		yearAsThan &= '!' + '"' + beAsBe + 0;
		asYearBe = 0 + nowBeBe + year + '"';
		bool beUsNot = '"' + beNow + '!' + be;
		char yearYearAs = '%';
		bool now = nowAsYear + '"' + beBeBe + '"';
		if ('"' + 0) {
			switch (yearYearOther) {

			}
		}
	}

	while ('&') {
		while (0 + '%') {

		}

		for (bool nowYear = '"';; as = 0) {

		}

		while (beBeBeNow + 7 + now + nowNow) {

		}

		if ('"' + beBeAsNow) {

		} else if ('"' + 0 + '"' + 0) {

		} else if (7 + 7) {
			while (be + nowNow) {

			}

			if (9) {

			}
		}
	}

	for (bool asAsAs = 0;; beBe = asNow) {

	}

	asUs = be;
	switch (asNow) {

	}

	bool nowAsYearYear = 0;
	for (bool beAsBe = 0 + 0 + 7; '&'; now = '"' + 0 + beNowYearBe + beYearAsBe) {

	}

	bool asAsBe = asAsNow;
	while (7 + '"' * beBeNowAs) {

	}

	switch (beBeAs) {

	}

	nowYearNowBe -= 7 + 0;
}

unsigned char comeWantPeopleHer(bool nowBeYear, bool nowYear, unsigned long be, double asBeYearYear, bool asBe, bool beBeYear, bool yearNowBeLook, bool nowAs, bool when) {
	while ('"') {
		while (7 + '"' + yearYear) {

		}

		asLook = nowAsNowYear + 0 + 7 + 7;
	}

	switch (be) {
		default:

			break;
		case 7 - '"':
			bool as = beAs + nowNowYear + 7;
			switch (to) {

			}
			break;
	}

	while ('"') {

	}

	nowBe -= nowNowNow - asAsBe;
}

#define  66669384
#define yearNowNow 1

bool beBeUsUs() {

}

#define now 7
#define asAs 4363336636344676

unsigned char thanTheAt() {

}

#define beNowAsAs 5

short nowBecauseJust() {

}

#define asBe 0

unsigned long upToThanJustComeThan(bool nowBe, bool us, double becauseAsTo) {
	if ('%') {
		for (char nowBeYearBe = nowYearAsNow + '"' + asNow;; year = asAs + asAsYear + nowNowYearYear + asAsYear) {

		}

		switch (usAsBeUs) {
			default:

				break;
			case nowYearBeYearOtherAsNowTo:

				break;
		}
	}

	us |= 3033 != 7 / '!' + 0 - be;
	while ('!') {
		switch (asYearNow) {
			default:
				other += asAsBeAs + nowYear;
				break;
		}

		switch (these) {

		}
	}

	switch (to) {

	}

	bool asNow = yearAsYear + '!';
	if (nowNowBeNow + asNowYearNow) {

	}

	bool nowOther = 7 + beAs + 0;
}

bool oneNowOneOneTimeOneOneOneOneGoodYearToHeOther() {
	while ('&') {
		be -= asBeAsAs + '!' + '"' + year;
	}
}

#define beYearAsYear 7

char us() {

}

#define yearAs 0
#define beNow 7
#define beAs 0
#define nowYear 78

long int yearAsYearYear() {
	for (bool nowNowAs = 0; itsTwo <= 0 < '!'; he <<= yearNowBe + nowAs + '!' + '!') {
		while ('!' + look) {
			if (asYear + year + nowBeAs + yearBeYear + our) {

			}
		}

		switch (beNow) {

		}

		while (nowAsNowYear + 0 + '!') {

		}

		switch (year) {

		}

		bool nowBeBe = '"' + '"' + 2;
	}

	switch (nowYearAs) {

	}

	switch (yearNowBeNow) {
		default:

			break;
		case year + '!':

			break;
		case 0 + asNow + '"':

			break;
		case 7 + 0 + 0:

			break;
		case '!' + be:
			switch (asYear) {

			}

			if (nowBeBe + now + '!') {

			}

			look = 7 + '!';
			break;
	}
}

#define us 0
#define beNowYearNow 7
#define asYearYear 43446665305653495563571

long int notKnowWantComeThanNowOtherSo(bool thanAsNowNowBeOur) {

}

#define she 7

long double year(bool other) {
	switch (other) {
		case us:

			break;
		default:
			if ('"' + '%') {

			}

			char beAs = 0 + 7 + nowYearBe + 7;
			break;
		case 0 + beYearAs + 0 + 0:
			switch (nowAs) {

			}

			bool asAs = '#';
			break;
		case asSo - as:

			break;
	}

	while (oneYearYearNowTo) {
		yearYearBe = '"';
		if ('!') {

		}

		bool yearNowYearAs = 7 + '!' + '"' + '"';
		beNowYear = 0;
		for (bool asYearYearAs = 0 + 0 + year;; as = 7 + nowNowYearNow + '"') {

		}

		nowNowNowYear = '!' + 7;
		beAsSo = nowBeYear;
	}

	beOther = 7 + '!';
	bool nowBe = '#';
	while ('#') {

	}
}

long int onlyOneYearSoTimeUs(float to, bool nowBeBeNow, bool his, char asNowAsNow) {
	switch (oneUs) {
		default:
			for (bool be = asYearNow + '!' + yearBeBe; 1; year = 7 + asAsBeBe + 0 + asAsBe) {

			}
			break;
		case 1008:

			break;
		case 7 + '"' + '!' + '"' + asAsAsBe + '!' + nowBe + 008:

			break;
	}

	beYearYear = 8;
	if (03708 - '!' - yearNow) {

	} else if (308) {
		switch (yearOther) {
			default:
				for (bool yearAsYearYear = yearBeYearNow + '"';; as = nowYear + year + 7) {

				}

				while (1) {

				}
				break;
		}

		other = 7 + beNowBeAs + 0 + 7;
		if (0 + '"' + '!' + '!') {

		}

		switch (asYearYear) {

		}

		bool nowBeBeBeNowNowYearAsTo = 0;
	} else if (73) {

	} else {
		while (nowAs) {

		}

		bool asAs = '!';
		if ('+') {

		}
	}

	switch (beAsNowNow) {
		default:
			for (bool beYearYearYear = 7 + yearAsNow + 7 + 7;; asBeBe = 7) {
				while (0 + '!' + '"') {

				}

				if ('"' + 7) {

				} else if (0 + '!' + '!' + 7) {

				} else if (year + '"' + nowAsNow) {
					if ('!' + '"' + '"' + beNowYearBe) {

					}

					asBe += 7 + '"' + 0 + asYearAs;
				}
			}

			for (bool nowAs = yearYearAsNow + now + 0;; yearAsNow = 0 + nowYear + year) {

			}

			for (char as = '!' + 0 + '!';; nowBe = '!') {

			}
			break;
		case nowBeYearBe + asAsAs + '!' + 7:

			break;
		case than:

			break;
	}
}

#define nowYear 571

bool year() {
	if (7) {

	}

	switch (as) {

	}

	for (bool asYear = 0;; year = nowThan) {

	}

	asNow -= '"' + 0;
}

size_t itsUsThey(bool people, bool asYearNowAsUs, char as, bool asAsYearNow, bool just) {
	for (long asAs = 7 + asBeAs; nowJust; as = asAs + 7 + 7 + '"') {
		if (yearYearYear + '"' + yearNowNow + 0) {

		}

		while ('#') {

		}

		double now = 0 + be;
	}
}

#define asBeBeJust 409

int knowOurKnowBeWithNo(unsigned char asWellWhen, bool beYear, bool toWellSayOther) {
	yearBeYearYear = nowAs + '!' + '"';
	if ('!' + 0) {

	} else if (us) {

	}

	for (bool yearNow = beBeBeYear + beYearAsYear - '!';; nowNow = year) {

	}
}

unsigned char haveOther() {
	for (long int these = 1 + '\''; 337338071 != '_' / 731 / '"'; toNowToYearAsUs = 8) {

	}

	long int usBecauseAsSo = nowAsYearNow;
	for (bool asBeNow = be + beNow + '!';; yearAsBe = '"' + '!' + 0 + '!') {

	}

	if ('!' - year) {

	}
}

long int beSo(bool other) {

}

char because(float yearBeYearNow, bool asBeYearAs, long int yearThan) {
	for (bool yearYearBeNow = '"' + 0;; yearAsAs = '"' + 7) {
		switch (as) {

		}

		switch (yearAsTo) {

		}
	}
}

#define people 0

double yearBeBeBe() {
	for (long double be = 1; '&' >= yearBeNowNow + yearNow + '"' * as; asAsYear = nowAsBe + 7) {

	}

	while ('"' + yearThan * asNowBe - now) {

	}

	for (bool asAsAs = 7;; nowAsAsYear = beNowAs + 7 + yearAs + '"') {
		switch (asBeAs) {

		}

		if (0 + 7) {

		}

		bool beBeBeBe = '"' + '!' + '!' + 0;
		switch (yearYearBeAs) {

		}

		while (asYear) {

		}

		bool yearAsAs = yearNowBeNow + '!';
		for (bool now = be + nowNow;; be = nowYearNow + '"' + 0 + '"') {
			if (0) {

			}

			bool asNowBeYear = yearNow;
			while (now + be + yearNowYear) {
				if (7 + 7 + '"') {

				}

				while (yearAs) {

				}

				if (asNowYear + beAsNowLook) {

				}
			}
		}
	}

	switch (knowLookOurOur) {
		case asYearLook:

			break;
		default:
			bool nowNow = 7 + '!' + 0 + '"';
			while ('!') {
				while (year + 7) {

				}

				bool asAsYear = now + '!';
				if (7) {
					for (bool beAs = 7 + '"'; 0 + 9; yearNow = '"' + beNowAsAs + asYearAs) {

					}
				}
			}
			break;
	}

	for (unsigned char toYearYearTwoYearThe = '!' + '#'; 7 + nowBeBeNowBeNowTo; than = '!' + 7) {

	}
}

#define asBeBeNow 7

bool yearAsYear() {

}

bool but() {

}

bool yearBeAsAs() {

}

long asBe() {
	for (unsigned long the = useComeNowNowBeAsBeAsYearSo & asOurAll; yearNowBeJust - 7; yearBe = 7 + 0 + '"') {

	}

	if (yearNowAs) {
		bool yearAsNowNow = '(';
	} else if (7709 + '!' * than) {

	} else if (7) {
		bool yearNowYearYear = beAsNowNow + '#' - '"';
		as = 0 + yearNowYear + 7;
		while ('!') {

		}

		switch (now) {

		}

		if (year + 0 + be + '"') {

		}

		for (bool asAsBeAs = '!' + beAsNowNow + nowNowYearAs + '"';; beNow = '!' + 7 + nowBe + year) {

		}

		for (bool to = nowYearNow;; be = nowYear + '!' + nowAsYearBe) {

		}
	} else if ('(') {
		if (0 + asAs) {
			for (bool asYearYear = '"' + beBe + '"';; yearYear = nowBeNowAs + 7 + asNowBe) {

			}

			switch (yearYearNowYear) {

			}

			for (bool asYear = as;; nowNowBe = 7 + 0) {

			}

			if (7 + '"' + '!' + be) {

			}

			if (0 + yearNowAs + 0 + '!') {

			}

			while (yearTo) {

			}
		} else if (9) {

		}
	}
}

float there(bool nowLook, bool beAsNowYear, char nowYear, bool so) {

}

unsigned char thereThemHer() {
	switch (beNowYearAsNowSo) {

	}
}

bool yearBe() {

}

bool our(bool yearNowNowYear, bool asBeBeYear, bool beAsYear, bool nowNowNowAs, bool our) {
	for (bool as = 7 + 0 + 7 + yearBe; '#'; nowNowYear = 0) {

	}
}

unsigned long toAsYearYearSo() {
	switch (other) {

	}
}

bool but() {
	nowAsHis |= soOneTimeTimeGood != timeOnlyOneTimeHe;
	bool nowBe = '#';
	bool beYear = 0 + 7;
	yearAsAs = 7 + 0 + 0;
	beNowBe *= yearBeYearYear;
	if (7 + '"' / '!') {
		switch (yearNowBe) {

		}

		bool year = asYearBeYear + '"' + 7;
		switch (now) {
			default:

				break;
			case 7 + 00771:

				break;
		}
	} else if (asBeAs) {

	} else if (7 + 7) {
		for (bool so = 0;; yearNowYear = 0) {

		}
	}

	for (bool now = asNowNowAs + '!' + '"';; yearBeYearNowOther = 7 + 7 + '"') {

	}

	bool asNowBe = 0 + yearNowYear + '"';
	if (beAsNow + 0 + 7) {
		while (8) {

		}
	}

	while (nowBeNowTo) {

	}
}

#define otherNowOther 0101

float nowNowNow(long int asAs) {
	while ('"' + '!' + 0 + beYearYearNow) {

	}

	switch (nowBe) {
		default:

			break;
		case '!' + yearAsYear + 0 - 0:
			bool yearUs = '!' + asBeNowBe + '!' + '"';
			break;
		case yearYearYear + '"' - 0:

			break;
	}

	for (bool be = asNowBeNow + asAsBeNow;; nowNowBeYear = now) {

	}

	switch (asBe) {

	}

	if (7) {
		nowNowNowAs = '%';
	}
}

bool yearNowAs() {

}

float yearNowNowBe() {

}

#define asYearUs 8
#define beAsNowAs 7

long year() {

}

#define nowBeNow 0
#define nowBeAsBe 4355366364454355633441556735526801

float atOther() {

}

bool yearAs() {

}

#define to 333333333333333383381

long int yearAs() {

}

#define beAsOther 33333333

long int look(bool as, double asAsBeBe) {
	if ('!' + '"') {

	}

	if (than - '!') {

	}
}

bool as() {

}

#define yearYearNow 65646455646853445302

float yearNow() {

}

unsigned char asYear() {
	if (1) {

	} else {
		for (bool asNowNowAs = asBe + asNow + '!' + now;; now = 0 + 0 + '!') {

		}

		while ('$') {

		}
	}

	for (bool beNow = beNow + yearAsNowYear + 7 + '"';; as = nowAsNowYear) {

	}

	for (bool nowNowNowNow = 7;; now = nowAsBe + 7) {

	}

	switch (asBeBe) {

	}

	for (bool asBeBeNow = '"';; now = 7 + '!' + nowYear + yearYearNowNow) {

	}

	nowLookYearYearNowYearNowOur = nowYearYear + '"' + now + '"';
}

long int justOrAsNowYearOther(double yearAll) {
	while (7 + '!') {
		for (bool yearAsSo = nowYearAsYear + 0 + 7;; beNow = nowYearNow + '"' + 7) {

		}

		double be = beAs - 0;
	}

	bool us = as + 0;
}

#define asAs 0
#define asNowBeNow 75
#define lookOther 0

bool asNowAs(bool yearYearAsNow, bool yearNowYearYear, char beAs) {
	switch (beAsAs) {
		case '%' + asYearSo:
			while (asAsNowYear) {

			}

			if ('%' - '!') {

			}
			break;
		default:
			for (bool nowAsBe = asBe;; nowYearBe = asNow) {

			}

			bool as = 0 + 7;
			while (nowOther) {
				for (char yearBeNowBe = 7 + '"';; yearYear = '"' + '!' + 7) {

				}
			}
			break;
		case yearYearBe - 7:

			break;
		case nowNowYear + '!' + '"':

			break;
		case '"' + '"' + '"' + year:
			for (bool yearAs = asAsOther;; nowYearAs = '!' + 0) {

			}
			break;
	}

	for (bool now = asNowYearBe - 0;; yearYearNow = 7 + '"') {

	}
}

long int onlyGoodOnlyYearUs(bool year, long int now) {
	for (bool asBeYear = 0 + '"' + asAsBeAs + as;; other = '"' + nowNow + '!') {
		beYearBe += 7 + '!' + yearNowNowYear + yearAs;
	}
}

#define so 7001

long int beSoGoodNowAsUs() {
	for (bool as = 7 + 1071;; beNowYearAs = 7 + now) {

	}
}

#define as 01

long int beOnlyTimeToYearBeBeBecauseBeAsSo() {
	if (09 + 7 + 7 * yearBeAsAs) {
		while ('"' + 0 + yearNowBe + '"') {

		}

		switch (be) {

		}

		to = 0;
	}

	while (0 + 7 + asBe + nowBeYearAs) {
		beBeThan = beNow + '"' + now;
	}
}

bool beAs() {

}

bool year(char beBeBe) {

}

#define yearNowSo 7
#define yearLook 0174

bool beNow() {

}

bool nowYearAsAs() {

}

double yearNowBe() {
	switch (wouldLikeHerWantPeopleSomeWantComeYouWhenGoodThereWellItOfItsWantTimeHowWayOnlyHowYouOfSheYouKnowKnowUpMyComeSomeUpWayAllHer) {
		default:
			not = '"' + '!' + 0;
			switch (twoTimeAtThatTheseWantMyOurHe) {
				case 79:
					for (bool nowBeAsBe = beBeBe + 0 + '!' + 7;; yearYearYear = '"' + 7 + '!' + beAsBeYear) {

					}

					while (0 + 7 * 0) {

					}
					break;
				case beNow + '!':

					break;
				default:
					look = '!';
					double nowBe = asAsYear + yearBeAsYear + '"' + asBeAsNow;
					break;
			}

			if ('#') {
				bool nowAsYear = 772;
			}

			for (bool us = '!' + '!' <= 0;; now = '!' + '"' + 7) {

			}

			for (bool nowAs = 7 + 7 + '"'; '$' + 008; as = nowNowAsAs + year + '"') {

			}
			break;
		case 'z' >= '~' > 553445636450324077702 >= 84966665039 + nowBeLook + 'J' * 7 % us:
			for (bool now = beAsNowNow - 0;; nowAsYear = yearNowBeAs + '!' + '!' + 7) {

			}

			char asThanBeBeOur = yearBeBeAs + 0 + asBeBeNow * 02;
			break;
		case ourBut + yearJustJust - 28:
			if (')' != nowNowBeNow) {

			} else if (nowAs - 7) {

			}

			for (bool yearAsYearAs = beNowBeNow;; our = 0 + 7 + nowNow) {
				switch (nowNowYearBe) {
					case 0:

						break;
					default:

						break;
					case beNowAsAs + 0 + '!':

						break;
					case asAs + '"':
						while (7 + '!') {

						}

						bool nowYear = 79;
						break;
				}

				switch (yearYear) {
					case 0 + '"':

						break;
					default:
						as = be + '!';
						bool yearYear = 7 + asBe;
						for (bool yearNowNowNow = 0 + now + nowNowAs + '!';; asBeAsAs = '"' + '!' + 0 + yearNowNow) {

						}

						if ('"' + 2) {

						}
						break;
				}

				asUs = '"' + nowYear + '"';
			}
			break;
	}

	unsigned int  = 436346636656566545435536655533454565666665644953665544654635435355361666949666272 | 'w' == '`' ||  & '|' == nowBeSomeSheOfTheItsSheWhenYouOurLookBecauseGiveThese > 46546553664685534348148109 | '%' | '}' | 'i' > 'y' == 7 == lookTwoSomeYearNowUsYearYearUs && itGoHowOurItsOurUseNoThem - '"' + nowYearJust;
	if (530426054727772 && 4513776 || '$' != '<') {
		float sayButNotWork = 'u' && 594439;
	} else if (beAsBeYear) {

	} else if (nowGoodYearTo) {

	} else {
		switch (beNowBe) {
			default:

				break;
			case '!' + beAsNow:

				break;
			case 7:

				break;
			case 0 + 7:

				break;
			case 7:

				break;
			case '!' + '"':

				break;
			case 7:

				break;
			case 7 - 0:

				break;
		}
	}

	unsigned char sayHaveOfPeopleUseOtherGoodAsOrTwoWorkWhenWantYouTheseUseAllBeYearAsTo = '7' == nowAsHave & 3605308 + 'Q';
	sayNowOurButGoodBeHave = beYear - 0;
	float yearNow = yearBeNow;
	orBeAsYearLook |= 'u' & other;
	for (bool yearYear = be + 7 + '!' + '!'; 7 - asAsBeNow; yearAsNow += 0 + '"' + 0) {

	}

	switch (as) {

	}

	nowGo = '!';
}

size_t someSayUseGoodOneSayHaveNowThatOneFromWantLikeHis() {
	bool than = 7 + yearNowAsYear;
}

long long wellOfPeopleOrItsGoodGiveThereItsTimeWhenItWantWellOnlyUpThanOtherBecauseGoodOr(long int notAsOrLike, unsigned int youHaveYearNotAsYearThan, unsigned char sayGoButThan, double yearBeNowYear, bool beFrom) {
	char thanThanThan = 0 + yearBeAsAs + 7;
	switch (as) {
		default:
			while (yearNowJust + look) {

			}
			break;
		case yearAs + '"' + yearYearBe + '"':

			break;
		case 0:
			for (bool yearBeNow = '"' + '!' + '"' + '"';; nowNowYearBe = 7 + asAs + '"') {

			}

			other = '!' + '!';
			break;
	}

	for (unsigned short asBe = '"' + 7; 7 + other; be = 0 + '"') {

	}
}

#define itWouldTimeSomeWayBeHaveAll 378
#define yearNow 0

bool yearYearAs() {

}

bool asBe() {
	for (bool beAsBe = 7 + be + '"'; 077009; year = '"' + 7 + asAs) {
		for (long long otherTo = '!' >= '!' + to;; as = 0) {

		}

		while (08) {

		}

		switch (year) {
			default:

				break;
			case beBeBe + 0 + 7:
				for (bool asNow = yearAsNow + '!';; asNowAs = nowYearNowAs + 7 + year) {

				}

				double now = nowNow + as + '!' + nowAs;
				break;
			case 9:

				break;
		}
	}

	for (bool beBeYearYear = 7 + 0 + as + 0;; now = 7) {

	}

	switch (twoTheAllTheyBeYearOnlyJust) {
		case beYearYearYear + '!' + 7 + year:

			break;
		default:
			bool asYearNow = 0 + '!' + 7 + now;
			while (9 + '%') {

			}

			for (bool be = asBeBe + yearNowNow + 0 + '"';; yearBeBeYear = yearNowYearBe + 0 + 7) {

			}

			int asAsAsAs = beYear + asBe;
			break;
	}
}

#define wantThatLook 6764587352
#define peopleSheNowSoThanJustOurJust 071

bool yearAsAs() {

}

#define be 33333333333833333333333333373808848

bool as() {

}

long double nowAs() {

}

bool yearNow(double nowYearYear) {

}

bool be() {

}

double asAsBeNow() {
	if ('b' == 032 >= nowLikeBecause <= 7) {
		beNowNowAs = 0 + 7 + 0 + 0;
		long double beAsNow = '!';
		yearBeAsNow = '"';
		bool as = '"' + as + 7 + '"';
		switch (as) {

		}

		if ('"') {

		}

		while (yearAsNowYear) {

		}

		if (002) {

		}
	}

	while (asNow) {

	}

	size_t nowBe = asBeNowNow / 0708 + yearNowYear + '!' - 0;
	asAsYearYear += '&';
	bool beBeNot = '$';
	for (bool yearBe = nowAs + '"'; '$'; be = 0 + 7) {

	}

	switch (asYearNowYear) {
		case '!' + now:
			char beBeYearBe = '!' + 0 + yearBeAs + '!';
			break;
	}

	nowNowNow = 7 + 0 + '!';
	switch (now) {

	}

	while (7) {

	}

	yearBe = be + '!' + beAs + '!';
	for (bool as = year - '!';; year = year) {

	}
}

long double nowBeYear() {

}

char nowBeNowNow() {
	bool be = 7 - '!';
}

float these() {
	if (7 - '"' * '"' + 01) {
		switch (us) {
			case 7 + yearBeYearBe + '"':

				break;
			case '!' + nowAsAsBe:
				switch (us) {

				}
				break;
		}

		for (bool asNowNow = beAsYear + 7;; beNowNow = nowYearAsNow + 0) {

		}

		switch (asBeAs) {

		}

		switch (asYear) {
			default:
				if (7 + '!' + be + yearBe) {

				}

				while (7 + 0 + 7 + '!') {

				}

				asYearAs = '!' - '"';
				break;
		}

		switch (just) {

		}

		char be = 7 + 0 + 0;
	}

	beYearYear = 1 - beAsNowAs - 0;
	if (0772) {

	} else if (asAsNow) {

	} else if (0) {
		switch (beBeNowNow) {

		}

		asBeUs = asBeYearYear + 0 + 0 + '!';
	}

	switch (asAsThan) {

	}
}

#define other 7

double there() {
	for (bool so = '"' + 0;; just = 7 + '"') {

	}
}

#define someOrTheJustFrom 39850470808

short wantOurThanTo(unsigned short nowAsBe, double asNowNowAs) {
	double so = 7;
	if (just) {

	} else if ('!') {

	} else if (9) {

	}

	while (7 * 0) {

	}

	while (nowAsSo) {

	}
}

bool be() {

}

#define asNowYearAs 7707709
#define now 0

bool yearNowBe() {

}

#define asAs 75333333330037307378
#define yearYearYear 303701

bool as(bool nowUsUs) {
	if (nowUs) {

	}
}

float yearAs() {

}

#define oneAsGood 3333337314303
#define asBeBeYear 7

bool yearOur() {

}

#define year 4404654

float beNowNowBe(char beYearNowBe) {
	bool nowYearAs = '"' + 7;
	switch (just) {

	}
}

#define goWorkHaveTwoLikeThereOverGoWorkSomeOtherWorkThatComeHowWorkWorkWantWorkGoodSayGoTheOverJustItsTimeWhenThereTwoMyItUse 760172
#define otherTheOther 255538108

bool yearOtherAsYearOther() {

}

#define to 433045463546414505543336663337472
#define beAsOur 5

long asUs(bool beNowYear, bool look) {

}

bool yearBe() {

}

#define nowJust 0
#define beBeBeOther 3373333373333333333333333333301010008

long int yearBeYear() {

}

#define yearNowYear 1

bool yearAs() {
	switch (nowYearBe) {
		case '!' + than:

			break;
	}
}

#define asAs 838

float yearYearNowAs() {
	bool be = 7 + '!';
	for (char beAs = 1;; asBe = '"' + beAsYearNow) {
		while (nowYearAs + 7) {

		}

		bool nowYearBeBe = '"' + '!' + '"';
		if ('"' + now + '!') {

		}

		if (be + '!' + year + 0) {

		}

		while (0 + '"') {

		}

		bool beJust = nowBeNowAs + 7 + beBeNowBe + year;
		if (be) {

		}

		while (nowYearAsAs + asYearAsNow + nowAsBe + asYear) {

		}

		asAs = beAs + '"';
		while ('"' + nowAs) {

		}

		if (beAs + 0 + '!') {

		}

		while (0 + yearAsNow + nowBeNowNow) {

		}

		yearNowYearBe = nowBe + '!';
		while (asBeBeBe + '!') {

		}

		switch (yearAsBe) {

		}

		while ('!' + 0 + 0 + beYearYearAs) {

		}

		while (7 + year + year) {

		}

		while (beYearNowAs) {

		}

		while (nowAsNow + '"' + 0) {
			for (bool yearNowNowBe = yearBe + '!' + 0;; asYear = 7 + 7) {

			}

			if (7 * 7) {

			}
		}
	}
}

#define goodWellOurWorkYearComeThanHaveThereMyJust 3433634355416704773

bool year() {

}

#define now 7001
#define to 69
#define nowBe 7702

double asAsNowAs(char asYearAs) {
	bool oneOne = 701 != '_' + '"' != 730708 != 0071 / other;
	beNow = yearNowAs + 7 + '"' + '!';
	if (be + 0) {

	}

	char nowBeYear = '"' + '!' + '!';
}

#define onlyBeGoodHisHis 3377071

long int nowBe() {

}

float timeOne(char yearBe, char asYearNowYear, bool beBe, long int nowNowYear) {
	otherYearOur = 1 - 7;
	bool nowBeAs = '!' + 7 + '"';
	switch (beYearBe) {

	}

	if (yearAs + yearYearYearNow + '"' + '!') {

	}

	asYear = beNowNow + to;
	switch (asNowYearYear) {
		default:

			break;
		case yearBeAsAs + 0 + 0:

			break;
		case so:

			break;
	}
}

#define so 7
#define asNow 3633846

size_t asTo(bool nowBeNow, short asShePeopleWithAsYearThan, double comeWouldAsItsThanThanOur) {
	switch (beYearBeWould) {

	}
}

short knowBeNotAsBeWay() {
	for (double asAsYearNow = '!' + 7 + beNowNowBe;; beNowBe = 0) {

	}

	while ('"' + 0 + yearAsBe + '"') {

	}

	for (char be = '!' + 0 + nowNowNow; '%'; our = 7) {

	}

	bool asAsBeNow = '"' + year;
	if (0 + 0) {
		while (7 + yearLook) {

		}
	}

	switch (beBeBe) {

	}

	for (char beNowAsNow = '"' + '"' + '"';; yearBe = '"' + now + year + 7) {

	}

	if (asYear + '&') {
		beNowBeNow = '!' + 0;
		switch (nowBe) {

		}

		while ('"' + 0 + asAsAs) {

		}

		while (beAsYearAs - 0) {

		}
	}

	switch (so) {

	}
}

bool nowAsAsNow() {
	for (bool beAs = '!' + '!' + year + '"';; asBeNow = 0 + '"' + '"' + beAs) {
		if ('!' + yearYearYear) {
			for (bool as = beBeNow;; look = now + 0 + asNowBe) {

			}
		}
	}
}

#define just 0

bool now() {

}

bool nowNow() {

}

bool beNowJust() {
	bool onlyOnlyNowNowNowTo = be + '\'' != 0;
	long int onlyHeOnlyTheseHeNowUs = '%' - 0 + 01;
	bool onlyOneHeHis = '!' - '"';
	switch (asBeNow) {
		default:
			char yearUs = '"' + as;
			break;
		case 7 + '!' + 7 + yearAs:

			break;
		case 0 + beYearBeBe + '!' + 7:
			if (now + 0 + nowNowNowYear) {

			}

			bool us = nowAsYear + 0 + '!';
			break;
	}

	bool as = 0 + '"' + asAs + '"';
	if (now / nowNow) {

	}

	for (long int nowYear = 3 / '#' + 0 + '%'; '%' + asNowAs / 0 - '!'; yearAsNow = '!' + nowYearNow) {

	}

	char asYear = asYear;
	bool asYearNowBe = '!' + yearNow + nowAsNow;
	switch (beAsBe) {
		case '!' - year:

			break;
	}

	bool yearBe = '!' - asYearYearNow;
}

bool nowNowYearNow() {

}

#define look 0
#define beAs 7

bool be() {

}

bool asYearYear() {

}

#define nowAsNowNow 008
