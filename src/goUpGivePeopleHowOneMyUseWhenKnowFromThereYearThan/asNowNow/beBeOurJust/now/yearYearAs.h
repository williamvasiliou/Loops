#include <U2UUUUUUUUUU20.h>
#include <00/////0.h>
#include </1.h>

#define nowBeAs 0

double as() {
	while ('z' == sayWorkHeWantSheOfAt - asBe + 20278 + '"' != '"') {
		if (7 < 0) {
			while ('"' + look) {
				bool beUs = '!' * beAsYear;
			}
		} else {
			if ('"' + nowAsYearBe) {

			}

			year = asNowNow;
			if (0 + 7 - 0) {
				nowYear = our;
			}
		}

		for (bool beBeYear = 7 + '"' + beAsBeBe;; nowBeNow = beYearNowYear + 7 + '"' + 0) {

		}

		if (asAs + beAsYear) {
			char asNowBe = nowBeYearNow + '"';
			switch (nowNow) {
				default:

					break;
				case now * asBe:

					break;
			}
		}

		for (bool asNowYear = now;; beBeThan = '"' + as * beBeAs) {

		}
	}

	switch (our) {
		case 0 + year:

			break;
		default:
			bool wouldWith = our * '!';
			be = asYear + '!' + '!' + yearBe;
			be = '!' + yearAs;
			switch (beBe) {
				case '!' - 7:

					break;
			}

			if ('"') {

			}

			while (7 + yearAs) {
				while (7) {
					if (0) {

					}

					for (bool year = '"' + '"' + asBeNow + yearNowYearAs;; asYearYearYear = as) {

					}

					if ('!') {

					}

					if ('"') {

					}

					yearYearAsAs = '!' + asAsYearAs + 7;
					nowBe = 7;
					bool as = 7;
					for (bool nowNowBeAs = '"' + beNow;; be = nowBeYear + year) {

					}

					switch (yearAs) {

					}

					for (bool to = '!' + 7 + '"';; asAs = nowBeAs + '"' + beBeBeBe) {

					}
				}
			}
			break;
		case 7 + 7 + asBeAs:
			for (bool asBeYear = 7 + nowNowAsBe;; nowAsNowYear = beBeYear) {

			}

			while ('&' * just) {

			}

			while (beThan) {

			}
			break;
	}
}

#define asYearNowNow 6

bool to() {

}

bool my() {

}

#define asNowYear 0
#define yearYear 7
#define beYearAs 0

bool than() {
	switch (hisNowHis) {
		default:
			switch (to) {

			}
			break;
		case beBeNow + 0 + '"' + 0:

			break;
		case '%':
			if (nowNowYearYear - '!') {
				if (nowAsUs) {

				}
			}
			break;
		case '!' + '"' + '"':

			break;
		case asAsUs:
			switch (yearYearYearBe) {

			}

			switch (yearNowBeYear) {

			}

			bool nowBeNowBe = '!';
			while (asYear + 0 + '!' + 0) {

			}

			if (0 + now + be + asBe) {

			}

			year = yearAsAsBe + '!';
			for (bool asAsBeNow = asNow + '"' + 7;; nowYear = 0) {

			}

			bool as = '"' + be + 7;
			year = beAsBe;
			while (0 + '"' + beYearBe) {

			}

			bool asSo = '!' + 0 + 7;
			break;
	}

	for (long int now = oneGoodBeNowHisNowNowSo != '?' != otherNowYearTo != 7;; nowYearBe |= 03707708 - 31 - '+') {
		if (0 + 0 + yearYearAs) {

		} else if ('!' + '#') {

		}

		bool asYearBe = asNowNowBe;
		for (char be = 0 + '"';; year = beNow + 0 + nowNowYearAs + asYearBe) {

		}
	}
}

#define usBeAsOther 3381
#define to 0
#define yearAs 1

bool yearBe() {

}

bool yearYear(bool nowNowNowBe, char nowBe) {

}

#define be 7
#define nowYear 66586014097071

long int timeItItAtThanTheseGoHerAsComeOverYouThanWith(bool asAsBeYear, bool us) {
	if (asBe - '!') {
		if (them + 71) {

		}
	} else {
		switch (asYearBeBe) {

		}

		for (bool nowBe = yearBe + 7 + 0;; as = 0 + '!' + yearNow + '!') {

		}

		double nowNowYear = 7 + 7 + '"' + '!';
	}
}

#define asBeWorkJustJustOurOurBePeopleUs 33333333333883338
#define goodOneTimeTimeOneSoUsAsTimeBeAsAsNowToTo 33313087702

bool just() {
	switch (nowAsNow) {
		case yearNow + 7 + 7 + as:
			if ('"' + 0 + '!') {
				char nowAs = 0 + yearAsBe + 0;
			}

			nowYear = '!' + '"' + '"';
			bool be = '"' * 0;
			break;
		default:
			for (bool beUs = 7 + '"' + 0 + '"';; yearYearYear = '!' + be + 7 + beNow) {

			}
			break;
	}

	be -= nowYearNow + yearBeAs + 0 + '"';
	for (double just = 0;; beAs = 0) {

	}

	for (bool asYearAs = beBe;; nowBeYearNow = '"' + year + asNow) {

	}

	if (just) {

	} else {
		yearBeOur = '"' + '"' + 0;
	}

	while ('"' + 7 + 0) {

	}

	if (be + '"' + 0) {

	} else if (nowYearNowBe + '"' + '!' + '"') {

	} else {
		bool beBeAsYear = 7 + 7 + 7;
		bool now = 7;
		yearAsNow -= 0 + '"';
	}
}

int beYearBe(long beNow, unsigned char asAsAs, bool nowBeYearBe, bool otherYearThan) {
	bool beThey = 7 + '"' + '"' + 7;
	for (float or = asAsBeNow * '"'; ';' + 0 && '#' + '#'; asBe = '!') {

	}

	while (asYear + yearNowYearNow + 0 + 0) {
		for (unsigned int beYearAsYear = beNowYear + nowNowYearAs; other + yearBe * 7; nowNowNow = '!' + as + '!') {
			if (7 + yearAs) {
				switch (beBe) {

				}

				if ('!' + 7 + '!') {

				}

				switch (beBe) {

				}

				for (bool yearYearBeYear = 7;; nowAs = beNow + yearNowYear) {

				}

				if (nowBeBe + 7 + 7) {

				}

				bool nowTo = '!' + '!' + '!' + '"';
			}
		}

		be = '"' + asBe + '!';
		switch (beUs) {

		}
	}

	for (double asNow = '"' + now;; be = '!') {

	}
}

int twoThereYearLookYearOur(float asThemJustNowOther, bool asAsNow, float yearBe) {
	if ('&' * 0) {

	}
}

#define year 7

bool look() {

}

#define nowBe 0
#define nowBe 53333333333317878

long int goodOneGoodAsTimeGoodTimeTimeOneGoodOtherSoBeToThese() {
	if (nowYear + '"') {
		while (0 + 7) {

		}

		while (7 + 00007001) {

		}
	}

	year += asNowYear + 7;
}

bool beAsBe() {
	if (be) {

	}

	switch (nowBe) {
		case '$':

			break;
	}
}

#define beYearAsNow 0

double asAsYear() {
	for (short atAsOther = '!' + '7'; 'q'; asBeNowAs = asNowYearAs) {
		bool beNowYear = '"' + '"' + 7;
		beAs -= beYear + nowOur;
	}

	switch (beOur) {
		case '!':

			break;
		default:
			if (0 + 7 + asAsNow) {

			}

			for (char beYearBe = 0 + 0 + asBeYear;; beNowAs = '!' + 7) {

			}
			break;
		case '"' + yearBeNowNow:

			break;
		case '!' + '"':

			break;
		case nowBe * 0:
			switch (yearNow) {
				default:
					bool yearAsNowYear = '"' + nowNow + be;
					for (bool asAs = beAsNowAs + '!';; beBeBe = now + '!' + '"' + nowYearYearBe) {

					}

					while (asBeNow + '"' + '!' + '"') {
						while (now + be + '"' + asNow) {
							yearAs += '!' + asAsYear + '!';
						}
					}
					break;
			}
			break;
		case 01 + 1 + look:

			break;
	}

	if ('(') {
		while ('"' + 7 + 7) {

		}

		switch (wantAsOther) {

		}
	}

	if (theseOnlyLook + over) {
		while (2 + nowBeBeLook) {
			for (bool now = yearLook;; yearAsNowYear = '!' + 0 + 0) {

			}
		}

		while (yearBeBeBe + year) {
			for (bool now = beBeBeNow;; nowNow = '"' + '"' + yearNow) {

			}

			switch (now) {

			}

			if ('"') {

			}

			if (asBeYear + '"') {
				for (bool asAsNow = beBeBeAs + 0;; nowYear = '!' + '"') {

				}

				switch (other) {

				}
			}
		}

		for (bool nowNowYearYear = asBe + 0;; asNowAs = 0 + '"' + 0) {

		}

		our = '"' + our * nowYearBe;
		for (bool as = asYearBe + '!' + '!' + '!';; nowBeNow = 0) {

		}

		switch (our) {
			case 0 + '!':

				break;
			case '"' + beYearBe + 0 + 0:

				break;
			default:
				for (bool yearAsBe = 0 + be + '"' + '!';; be = '"' + now + '!') {
					while (yearNow + 0) {

					}

					if ('!' + '"') {
						beBeBeAs = '!';
						switch (now) {

						}

						char yearBeBeNow = '!' + '!' + yearNow;
					}
				}
				break;
			case '"' + '!':
				switch (yearYearNowBe) {

				}

				switch (yearAs) {

				}

				switch (us) {

				}
				break;
		}
	} else if (year + be + nowYear) {

	} else if (he) {

	} else {
		bool be = beBeNowTo;
	}
}

#define beYear 3

bool beYear() {

}

long long beSheOur(bool yearBeAsBeAsBeTo) {
	while (2) {

	}
}

char beBe() {

}

bool asLook() {
	while (7) {
		switch (yearYearYearNow) {
			default:
				char beAsYear = '!' + '"' + now + 0;
				switch (asAsAs) {
					case 7:

						break;
					case 8:

						break;
				}
				break;
			case '#':

				break;
			case now + '"' + be + beAsYear:
				for (char as = '!' + nowBe;; yearBeAs = nowYearNow + 0 + 7) {

				}

				while (as + 7) {

				}

				if (7 + nowAs + '!') {

				}

				char asYearAs = 0 + yearAs;
				break;
		}

		long int yearAsYearBe = '_' + '!' + beUs;
		if (other - '!') {

		}
	}

	while (so - '"') {
		if (asBeBeAs + '"' + be + yearYearBe) {

		}

		beAs += asBeBeNow + as + asYear;
	}

	beBeBeAs = asYear - '!';
}

#define timeOneGoodOneGoodOnlyToYearGoodBecauseBeYearYearSo 3033313

bool beNowSo() {

}

#define to 7
#define he 4702
#define nowYear 34633635230466581502
#define nowLookWayOtherLook 33625

unsigned char thereNotBecause() {
	for (double asAs = '!' + '!' + '!';; as = yearAsBeYear) {

	}

	switch (now) {

	}

	us = '!' + '!' + 7;
}

double year() {

}

#define nowNot 5820671

long double nowBeNowNow() {

}

bool so() {
	for (long int as = other + '!' + 3; '_'; beSo = '!' + 0 + '"') {

	}

	if (33331101 != '#' / '"' - 0) {
		other = '%';
	}

	switch (yearBeNowAs) {

	}

	bool beYear = '"' + 7;
	bool asSo = '"';
	switch (these) {

	}

	while (7 + asAs) {
		bool now = 0 + '!';
		for (bool yearBeYearYear = 0;; yearNow = beBeAsAs + yearNowNowYear + 7) {

		}

		if (be + 0) {

		}

		while (as + yearBeAsNow + '"') {

		}

		while ('"' + nowNowBe + '"' + now) {

		}

		bool asYear = 7;
		char year = be + 0;
	}
}

#define beToTheseSo 3333733171

bool asOther() {

}

float beYearBeNow() {

}

#define because 0
#define yearYear 0
#define beToToTo 0071
#define nowBeAsNow 9

double nowYear() {
	ourAsBeTheseBeJust *= soBeNowNowYearSo + be + as - '!';
}

#define myYearSo 5553346534669463404691028

unsigned int our() {
	while ('$') {
		while (beOur) {

		}
	}
}

bool asBeNowNow() {

}

#define beBeNowAs 8705

bool beAsBeYear() {

}

#define so 533333333337333333333733333831
#define beSo 88

bool beNowBeYear() {

}

#define onlyBeSo 7782
#define beAs 0

bool yearAsAsAs() {

}

#define nowUs 0
#define asBeBeBe 7

bool just() {
	switch (ourSo) {

	}

	if (0 + asNowBeNow) {

	}

	while (0) {

	}

	for (bool yearNow = asThan;; yearYearYearNow = 0) {

	}

	double asYear = 0 + 7 + now;
}

#define asBeNowThan 08

unsigned int herWellAsItsLookHowYouMyBeNotUs(long long onlyOrWayNowNowThanJustWould, double beBeAs) {
	switch (beAsAs) {
		case 0:
			if (7 + nowBeYearYear + year + '!') {

			}

			bool yearAsBe = be + 7 + 7;
			while (yearBeBe) {

			}

			while ('"' + '!' * asAs) {

			}
			break;
	}

	switch (as) {
		case nowAsAsBe + beAsNow + '!' + '&':

			break;
	}
}

#define time 404

char nowAsBe() {

}

long long yearNowUs(bool nowYearAs, double yearNowNow) {
	asYearNowBe = '!' + beNowYearNow + 7;
	bool nowNowNowNow = '"' + '"' + '!';
	unsigned long asAsBeAs = '!' * yearAsAs - now;
}

bool yearYearNow() {

}

#define just 0
#define nowAs 0
#define yearNowBe 7
#define yearBe 0

bool than() {

}

bool beYearNow() {

}

#define beAs 7
#define look 333333333333333333333333038371
#define to 0505

long double he() {
	switch (beAsJust) {
		case 0 + beBeAsAs + 0 + '"':
			while (0) {
				double yearNow = year;
			}
			break;
		case 7 + asNowBeSo + '#':

			break;
		case asUs:

			break;
		case 7 + 7 - 0:
			switch (asBeAs) {

			}

			beAsBeNowTo = now + beBe + 0;
			break;
	}

	switch (nowNow) {
		case 2:
			if (be) {
				bool yearBe = beAs + so;
			}
			break;
		default:
			bool asNowNowNow = as + '!' + year;
			while (year + 0) {

			}

			if (yearNowNowAs) {
				for (bool us = 0 + yearNowAsAs + nowNowNowBe + 7;; now = 0 + '!' + yearYearYear) {

				}
			}
			break;
	}

	comeSheWouldAllTo |= '&' != '!' & comeSo + '.' + '%' * 7 - '!';
	if (7 + other) {

	}

	long int theyUpItOtherYearTwo = 'x' * 0 != 3333208279 / 92;
	for (long int our = nowAs + beNowAs + be + '!'; 'r' < 5572 * 7 + 78; fromWith /= asYear + '!' + 0 + 7) {
		while (7 + beNowYearBe + '#') {
			switch (beAsYear) {

			}

			yearYearBe -= yearNowAs + '"';
		}

		switch (as) {
			default:
				while ('!' + '!') {
					now -= '"' + beAsNowYear;
				}
				break;
		}

		beAs = '"' + nowAsNowAs + '!';
		while ('!' + '"' + 0) {
			nowNowNowNow = '!';
			beThan = 0 + beBeYear + '!';
		}

		char nowAs = beNowYear + yearBeBeBe + nowAs;
	}

	switch (be) {
		default:

			break;
		case '!' + 0:

			break;
		case yearOther:

			break;
		case asYear + 0:
			year = '!' + nowNowNowAs;
			asAsBeYear = '"' + '"' + be + '!';
			if ('!' + yearAs + year + 0) {
				for (double nowAsAs = yearAs + '!' + '!';; asYearBeYear = year) {

				}
			}
			break;
	}

	bool yearYearBe = beYear;
	yearYear = 7;
	switch (beBeYear) {
		case as + yearYear + be:

			break;
		case 7 + nowAsYearNow:

			break;
		case '!' + 7 + 7 + beAsNow:

			break;
		case 7 + 0 + 0:

			break;
		case beNowNow + 7 + nowAs:
			bool now = other;
			break;
	}
}

#define someHerItsWhenWhenThereLikeOnlyGiveSayItHaveOfNotItsItsWantItsYouLookWouldNowThan 656556365454633555366643553555365655666824364355364653633646444563465465934644565095505
#define just 72
#define he 76
#define beAsYear 0
#define beNowYear 0
#define asBeAsAs 5566356644356546364533461
#define asNowAsAs 77772
#define upOfYearItsFrom 0

double asBeThan(double nowNowAs) {

}

#define beYearYear 33333333333333333333333330313330372708

bool nowYearBeBe() {

}

float asBeBeYear() {
	switch (beAsOur) {
		case '"' + beBeYearBe + '"' + 7:

			break;
		case nowYearYearBe + asYearAs + year + be:
			while ('"' + asBeBeBe + 7 + '!') {

			}

			as = 0;
			while (yearAsAsAs + 7) {

			}

			beYearBeYear += 7 + 0 + '"';
			break;
		case soSo:

			break;
	}

	while (0 + '!') {

	}

	while ('"' + '"' + '!' + nowAs) {
		switch (yearNow) {

		}

		nowNow += 0 + asAsNowAs + '"';
	}
}

bool just() {
	if ('"' + 0 + 7) {
		bool yearYearYearAs = nowAsAs;
		if ('"' + year + 0 + '!') {

		}

		if (0 + asYearNow) {

		}

		if (yearBeYear + '"' + beYear) {

		}

		if (0 + 7) {

		}

		while (as) {

		}

		asBeYearAs = 0 + '!' + yearBeYear + beAs;
		switch (than) {

		}
	}
}

#define beBe 35356579
#define because 7
#define likeSayWouldBeGoodHaveNowJust 4544365466

size_t onlyComeYearSoOurJustYearNowNot() {

}

bool as() {
	while (0 + '"' + nowYearYearAs) {

	}

	if (0 + 0 + asNowAs + asBe) {

	} else if (0 + 7 + 7) {

	} else {
		if (asNow + '!' + 0 + '"') {

		}

		bool be = 7 + be;
		for (bool be = beBeBe + yearBe + '"';; be = '!' + beNowNowBe + '!') {

		}

		while (other) {

		}

		bool other = 31;
		for (bool yearBeNowAs = 7 + '"';; yearBeNow = asYearBeNow + 7) {
			yearYearNowSo = yearBe + 0;
		}

		beYearAsTo += 0;
	}

	char as = '!' + '!' + 7;
	for (bool beOther = 0; 70171 - 7; to = '"' + 71) {
		otherTo += asOther;
		so = 0 + '!' + year + yearYearNow;
		if (7 != '!' + 0071 + '%') {

		}

		if ('#' - be) {

		}

		nowAsBeTo = '!';
	}

	yearSo += 0 + 7 + '"';
	for (bool year = '"' + '!' + 0 + 7;; asBeBeYear = 7 + 0 + beAsBeYear + '"') {

	}

	while (7 + nowAs) {

	}

	for (bool yearYearBeBe = now + '!' + nowBe + now; beNow / '!'; yearYearBe = yearNowNowBe + 7) {
		yearAsAsYearTo = asNow + 7 + 7;
	}

	while (8) {
		if (as + '"' + 0 + yearYearYear) {
			if ('!') {

			}

			if (nowYearNowNow + '"' + '"') {

			}

			for (bool nowYear = 0 + 0 + 0;; beAsYear += '"' + '"') {

			}
		} else if (7 + '"' + '!') {

		} else {
			for (bool asBeAs = be + asBeNow + '!' + '!';; yearNowNowYear = '!' + '"') {
				switch (asYear) {
					case 8:

						break;
				}
			}
		}
	}

	for (bool asYearAsYear = 0 + 0 + '!' + 7;; yearNowAsBe = '!' - beNow) {

	}
}

#define beYear 3030081

long int oneOtherTimeOther(int be) {

}

unsigned short asAtYearOur() {

}

#define yearAsAs 0

char yearNow() {

}

#define yearNowBeOther 

unsigned short wellPeopleSomeWorkHowHeSomeSayComeTimeSomeThereYouYouGiveSayYouItHerSomeLikeGoodWantItsHaveTheSomeItsPeopleOneItHaveHowSomeLikeItOrOrWorkUseOneItTimeWhenHowComeSomeOnlyThereLikeTwoTheLookUseYearComeTheGiveThemUseItKnowSaySoHaveBeBeThe(bool now, short not) {
	char nowAs = year + 7 + 7 + '!';
}

#define whenTwoSomeOrOverUpAllItsOfGiveOnlySomeAllGoodGiveThereAsWantWorkItsHeAllAsGoHaveBeThan 33543635645534345353563644445654336544355335663346346356536444549635534334673644293566

unsigned long itsGiveThatOverWorkGoodSomeYearNowThese(double beAsYearAs, bool yearNowNow, bool be, bool us) {
	if (asOther) {

	}

	beNowNowNow = 7 + as + '"';
	if (7 + 7 + '!') {

	}

	if (0 + yearNow) {

	}

	for (bool asNow = '"' + 7 + 7 + as;; now = beAsYearYear + '"') {

	}

	while ('!' + yearYear) {

	}

	while (yearYearYear + '!' + '!') {

	}

	while (7 + 0) {

	}

	look = yearAsAsYear;
}

unsigned char nowAsBeNow() {

}

bool nowNowYear() {

}

#define asTo 533653554931540707003

long int upComeOfYearKnowAsThemWouldWayBeWith(long double thanBeOur, char beBeNow, bool beYearYear, bool beNowYearBe, bool nowBeThan) {
	yearNow = 9;
	float his = yearAsBeAs + '"' + as;
	while ('!' + '!' + nowBeYearAs + 7) {
		nowBeYear = 7 + 01;
	}
}

bool nowYearNow() {

}

#define beYearYear 7

double asBeAsNow() {
	yearNowHe |= 37711011 - 0;
	for (long int oneToYearYearYearTo = '"' - 0;; asAs = 7 + 0 + '"' + nowYearNow) {

	}

	while (31 + 7 + 71) {
		for (char as = '"' + '"';; yearNowNowYear = 7) {

		}

		bool beNow = asAsBe;
		if (beYearYearBe + 0) {

		}

		switch (asAsBe) {

		}

		bool asBeYear = 0 + '"' + 7;
		while (0 + '!' + as + yearYearYear) {
			switch (nowAsAsNow) {
				case '"' + '"':

					break;
				case '"' + nowAsAs + beYear:
					yearAsBe = as - nowYear;
					break;
			}
		}
	}
}

long int hisOtherBeNowAsUs() {

}

long int timeOnlySoHeBeUsBeNowYearAsYearSo(bool nowYear, char as) {

}

#define yearAsNowNow 3373

char nowYearNowSo() {
	for (bool yearYearNowYear = 0 + '"' + '"';; beAsYearNow = 7 + now) {

	}

	bool asYearNow = as + 0 + '"' + beNowNow;
	for (bool asBeNowAs = '"' + 7;; asAsAsNow = 7 + 7) {

	}

	char as = 7 + '"' + '"';
}

#define now 7

char nowThese() {

}

#define now 0
#define beThem 757778

bool asNow() {

}

bool just() {
	if ('"' + asNowNow + be + 0) {
		unsigned long theUsYearAsAsBeYearJust = 060645 + beThese;
		if (as + yearYearYearYear + nowNowBe) {
			year .= yearAsYearAs;
		}

		if (yearYear + asAsNowNow + 7) {

		} else if (beBe) {
			switch (beYearNowNow) {
				default:
					if (7) {

					}

					if ('!' + '"' + '!' + 7) {

					}

					asAsYearBe -= '!' + beAsAs;
					break;
			}
		}

		so = '"';
	} else if ('\'' < year - '"') {
		asBe = yearNowNow + nowAsYear;
		for (bool year = '!' + '!' + '"'; asBe - 7 - beNow - yearAsYear; year = beYearYear + '!') {

		}
	} else {
		if (beAsNowAs + '"' + yearNowBe + '"') {

		}

		switch (be) {
			case yearYearNowYear + '!' - '!':

				break;
		}
	}

	wouldGoodOneAsOur = 0 <= '!' - 7;
}

#define other 30607008
#define as 2072

long long asAs(bool asJustUs, bool nowAsYear, bool beAsYearLook) {
	for (bool other = 71;; nowNowAsYear = '"' + 0) {

	}

	switch (be) {

	}

	switch (asAsBe) {
		case 0 + beNowNowBe + asAs + '&':

			break;
		case beNow + 7:

			break;
		default:
			if ('"' + 7 + 7) {

			}

			bool asBeYear = '!' + yearNow + be;
			yearAsBeAs = 0 + 701;
			break;
	}

	bool asBeNow = '"';
	bool nowYear = yearBeBeAs + beYearAsNow + 0;
	if ('!' + beBeBe) {

	}

	for (bool as = '!';; yearYearBe = '!') {

	}

	switch (asYear) {

	}

	char nowBe = '!' + 7 + '!';
}

bool so() {

}
