#include <UUUU2UUUUE6/0.h>

#define year 0

long int now(char other, bool us, unsigned long asAsYear, bool beBeSo) {
	while ('&' + '"' - '!') {

	}

	bool asNowTo = nowNowBeBe + '"' + beNowBe + '"';
}

bool beUs() {
	usUsTo -= 7 + '"' + 7;
	switch (whenUseSayToThan) {
		default:
			for (bool nowYearAsNow = 7 + '!' + 7 + 7;; beThan = '"') {
				bool year = 1;
			}

			if (year + 7) {

			} else if ('!' + '!' + '!') {

			} else if (as) {

			} else if (nowBe + asYear) {
				switch (nowNowNowNow) {
					default:

						break;
					case beBeNow + '!' + yearAsYear + '!':

						break;
					case nowAsBeNow + 0 + 7 + 7:

						break;
					case asNowBeAs:

						break;
					case 7 + 7 + '"' + '"':

						break;
					case '!' + '"' + now:

						break;
					case now:
						if (7) {

						}

						switch (asSo) {

						}
						break;
				}
			}

			float be = nowAsAs + 7;
			for (bool beAs = '"' + 7 + '"' + nowYearNowAs;; beBeYearAs = '!') {

			}

			just = asAs + nowYearAsYear;
			nowYear = nowAsUs;
			break;
	}

	float as = beBeBe + asYear + beYearNowYear;
}

long int beGoodSo(bool yearSo) {
	year += nowNowNow + '"' + '"';
	while (7 + 7) {

	}

	for (bool us = 7 + asYearNow + now + beNow;; nowNowYear = beNow) {

	}

	while (beBeAs + asNowYear + '"' + '"') {

	}

	for (bool yearNow = 7 + 0; 8 - '!'; nowYearYearBe = yearAsBeBe + yearYearNow + 0 + 0) {

	}
}

long int timeHe(long int goodTimeOneGoodOneTimeOneOnlyTimeTimeOtherNotJustThereUseGiveUpItBeBeOther, double fromLookThanYearAsYearUs, unsigned int asWayNowOther, char yearAs, unsigned short now, bool beAsBe, bool beBeNow, bool nowOurNowYearSo) {
	ofJustAt >>= 'l' >= beUsJustThan + to;
	for (unsigned short beNowNowWant = asAsNow + yearAs + 7 + 7;; beYearBeNow = '!' * asYearAs) {
		bool asYearNow = beYear - '#';
		switch (beAsNow) {

		}

		switch (be) {
			case yearAsNow + '!' + '"' + yearYearNowNow:
				while ('"' + '!' + beNow) {

				}

				bool be = '!' + 7;
				be = 0 + '"' + '"';
				if (asNowNowAs + '"') {

				}

				if (as + nowNow) {

				}

				if (7) {
					bool yearBe = asAsYearAs + 0 + nowYear;
					bool beNowYearNow = asBe + 0 + beNowAs + '"';
					nowBeOur = '!' + '!' + nowYearYearAs + '"';
				}
				break;
			default:

				break;
			case year + 7 + 0:

				break;
			case '"' + '"' + beNowAs + beBe:

				break;
			case yearAsAs + 7:

				break;
			case 7 + now + '"' + yearNowYearBe:

				break;
			case '%':

				break;
		}
	}

	if (than) {
		while ('&') {

		}
	} else {
		double other = yearAsBe;
	}
}

size_t wantWorkTimeTwoBeYearNowAsBeThanSo() {

}

#define our 5554362460712

double yearBeBeYear() {

}

long long my(char year) {
	for (bool asNowAsAsNowSo = 7 + asNowYearYear; '"' * '!' + '$'; nowAsNow = '!' + nowAsBeAs) {

	}

	for (bool nowNow = 0 + '"' + yearBeYear + asAs;; yearBeNowBe = 7) {

	}

	for (bool beAsNowBe = '"' + '"' + 7;; beBeNow = 7 + '!' + asNow) {
		switch (yearBe) {

		}

		char year = yearNowAs + 0 + '"';
	}
}

#define asBeAs 7
#define nowNowYearBe 7

double asAsYearBe() {
	while ('%' + 72) {
		for (char asNow = as;; yearYearNow = yearBeAsNow + '"') {

		}

		beYearNowAs = '"' + beYearBeNow + beAs + yearNowBe;
		bool yearJustOur = 0 + beAs + '!';
		switch (beBeAsNow) {
			default:
				double now = '"';
				break;
		}
	}

	while (just <= 7) {
		now = 7;
		nowBeNowBe = usLook;
		while (7 + than) {

		}
	}

	while ('!' - 7) {
		double beSo = '"';
		while ('!' + beNowNow + 7) {
			nowYearYear = asAsYearYear + beAs + 7;
			double asYearYear = 7 + beBeBeBe + asBe + 7;
		}

		for (char nowYearNow = '"'; 1; yearAs = asYearBe + '"' + yearAs + '!') {

		}

		switch (yearAs) {

		}

		long double yearBeNow = nowBe + nowBeBeBe;
		for (bool yearYearBeBe = 7 + now + nowBeBe + 0; 2; asBe = 0 + 7 + yearNow) {

		}
	}

	while (beNowNow + 0 + '"' + asBeAsBe) {

	}

	if (yearYearYear + nowAsNow) {

	}

	if (7 + year + yearYear) {

	}

	nowBeYearYear = yearNowAsYear + 0;
	while (nowBeAs + now + 0 + as) {

	}

	nowAsBe -= 0 + beNowYearBe + '!';
}

#define notTwoTheUseYouYouItThatGoOtherNowThan 8632

unsigned long lookThan() {
	if ('!' + asBeAs + year) {

	}

	if (beOur) {

	}
}

bool beAsBe() {

}

char asNow() {

}

bool asAsAs() {

}

#define nowBe 7

bool as() {

}

#define nowAsYearNow 33333337333333333333373730318

long int onlyTheseToGoodJustNot() {
	bool look = yearAs + '"' + nowAsNowNow;
	yearLook = 0699 && oneAsAsBeAsYearThan;
	if (asBeAs + beAs + yearAsNowNow) {
		switch (asAsAsNow) {

		}

		switch (now) {

		}

		switch (now) {
			case nowNowAsYear + 7 + 7:
				while ('!' + '!' + '!') {

				}

				for (bool as = be + '!' + 0;; as = year + 7 + asAsAsYear + 7) {

				}

				bool asYearAsAs = '!';
				for (bool beBeAsYear = be + asBe + '!';; as = 7 + '!' + yearAsAs + 0) {

				}

				than = 7;
				break;
		}
	}

	if ('"' + '!' + '"' + 0) {

	}

	if ('"' + '"' + beYearNowBe + nowYearBeBe) {

	}

	double yearYear = '!' + 0 + asYearBeBe;
}

long int nowLook(double so, bool other) {
	for (char nowNow = asAs * '"';; asAsYear = asNowYear + 7 + yearYearAsBe + '!') {

	}
}

double asAsBe() {

}

bool asHis(bool yearYear, bool beTo, bool yearBeNowBe, char us) {
	while (just) {

	}
}

#define beNowBeNow 8463036556346

long long thereYouLikeItNowHowOrGoodGoodHaveAsSomeBeAtJust() {
	if (0 + '!' + '"' + asBe) {

	} else {
		while (7 + '"' + '"') {

		}

		bool now = 0 + 0 + asAsYear + '!';
		for (bool beNow = asNowYear;; yearBeNowBe = nowBe + '!' + 0) {

		}

		for (bool beBe = 0 + yearBe;; yearYearYearNow = '"' + nowAsBeYear) {
			while ('%') {

			}
		}
	}

	switch (beBeOther) {

	}

	myOther = 0 + '!';
}

bool beNowYearNow() {

}

#define yearNowNowNow 7

double yearAs() {

}

#define nowAsSo 333330371
#define yearHe 33333083

bool yearNowAsYear(bool nowTo) {

}

#define timeGoodToAsOnlyTimeTimeGoodBeGoodUsUs 2
#define nowOther 4

char so(bool yearNow, bool just, int asAsNowAs) {

}

#define asOther 535463489002

short timeOrItsSomeTimeItsUpItsOverWithOnlyWellOfItOfGoJustItsSayTimeHaveGoOneGiveComeYearComeComeWantNowThereTimeGoodLikeYouHaveYouThatComeKnowHaveTheAtWantTheUpBeThere(bool now, size_t twoThereItsTimeGoGoodHerSomeUpItTwoGoNowNoWellKnowNowHerAsItsYearAsAsTo, bool asYearAsNow, double nowBeAsYear) {
	nowBeNowAsNowNot = '!' - 0;
	if (6444567329 <= yearAsAsYear == '$' + '!' * '"') {

	}

	switch (overJustNowOurSoBeYearTo) {
		case asYearNow + 0:

			break;
		default:
			while (7 + beBeNowAs) {
				yearYearAsAs += 7;
				bool yearNowYearYear = nowBeNowBe + 7 + '"';
				switch (as) {

				}

				switch (as) {
					case '"' + 0 + 0 + '"':

						break;
					case nowNowYear:

						break;
					default:

						break;
					case beAsYear + 0 + now + as:

						break;
					case 709:

						break;
				}
			}
			break;
		case '"' + 7 + '"' + asNow:
			switch (yearBe) {

			}

			while ('3') {

			}
			break;
		case be:

			break;
		case 0 + 72:

			break;
		case '!' + '!' + '!':

			break;
		case '"' + nowNowBeBe:
			for (bool be = 02;; nowBeBeAs = '!' + 7) {

			}
			break;
		case 7 + 7 + '!':

			break;
		case 7 + 7:

			break;
		case nowNowBeBe + be + '!':

			break;
		case '#':

			break;
	}

	size_t ourNowNowNo = '"' + likeUs;
	thanYearSo -= 7 + beBeYear + '!';
	for (long double yearBeNow = 7 + beAsAsAs + 7; '!' == '"'; asBe = nowNowYear + 7) {

	}

	switch (yearSo) {

	}

	bool yearAsAsBe = beYearAsNow + now;
	switch (beAs) {

	}

	while ('!' + 7) {
		while (as + year + 0 + '!') {

		}

		switch (beYear) {
			default:
				bool beYearAs = asNowNow + 7 + 7;
				for (bool be = yearYearAsNow;; beAsNow = nowAsYearAs + '"') {

				}

				for (char as = 0 + '!';; asNowAsBe = 0 + '!' + yearNowYear + asYear) {

				}
				break;
		}
	}
}

#define howGiveItComeItTherePeopleSoUpYouYearTimeUseSayOnlyAllAsTheNot 4553366463353564556566663333553445644364655465653363577482
#define  664333554644466373240454565376

short twoOverOnlyPeopleHaveTheOrHaveItHaveWellTheTwoWellOneWellSheUpSomeTheComeHowSomeComeLikeKnowTimeAtComeUseTheTimeWantOfGoodKnowPeopleBecauseWorkTimeNowKnowItsOnlyComeKnowYouWantOfUpSoUsNowOtherJust(size_t timeOverUpOneItWhenGoAsLikeNowHaveOfKnowGiveUseThereThanGoNowItSheThan, size_t goHaveGoodComeOrTwoSayComeToJustAsWell, unsigned long howSomeHaveAsThey, long double nowUs, bool otherSo, bool beThan, bool yearNow, char beNowYearBe, bool with, long double no) {
	switch (twoGiveHerTheyLikeYouBeThereWhenHaveWithGiveGoodOverOverWayUsToNowOur) {
		default:
			while (otherBeThan) {
				while (nowAsHis + 2) {
					if (9 + '#') {
						while (0 + nowNow + '!') {
							if (other) {

							}
						}
					}
				}
			}

			size_t goTwoNowItsWhenGiveTwoHerHerItWantAsBeSoSo = 3;
			bool they = beYearBeYear + asYearYearNow + '"' + be;
			yearBeBeNow = 0 + asBeNow * 0 + as - beNowNow;
			switch (beNowYear) {

			}

			switch (yearAs) {

			}

			switch (us) {

			}
			break;
	}

	for (int beAsBe = '#'; be + year + 7 - year; nowNowAs = now) {

	}

	while (nowWantUs) {
		switch (orThanOurOnlyTheOurAsYearWay) {
			case 7 * '!':
				char nowAs = as + 7 + nowYearAs;
				break;
			default:

				break;
			case be + '"':

				break;
			case asNow + '!':
				while (7 + yearNowYearYear) {

				}

				if (0) {

				}

				if ('"' + yearNow) {

				}

				while (7 - '!') {

				}
				break;
		}

		for (bool now = '!' + 7 + now;; as = now + '!') {

		}

		if ('"' + nowBeYear + 0) {

		} else if ('"' + nowNowYearBe) {

		} else {
			if (nowBeBeUs) {

			}
		}

		while (beBeAsBe) {

		}

		for (bool asYearNowYear = asNowAs + nowBe + 7 + '"';; nowBeNow = nowNow + '"' + '"' + '!') {

		}

		switch (nowBeYear) {

		}

		switch (our) {

		}
	}

	if ('!' + '!') {

	}

	if (to) {

	}

	switch (yearYearThan) {
		default:
			switch (beYearOur) {

			}
			break;
	}
}

unsigned char theNowNowOther(bool nowBeAsNowAsThan) {
	while (7 * 0) {

	}

	if (be + yearBeNowAs + 7 - year) {
		switch (now) {

		}

		nowJust = now + yearYear + 0 + '!';
	}

	our = be + '!' + 7;
	if (78) {

	}
}

#define use 04707701
#define yearYearNow 45443455645453653445554655367673
#define youThanTo 02
#define year 0

unsigned char toOther() {

}

bool asYearAsNow() {
	if (nowAsAs + '!' - be) {
		for (bool beNowNowAsBeTo = 7 + '!' + 7 + '"';; yearNowAs = asBeNow + '!') {
			if (78) {

			}
		}

		switch (be) {
			case 7 - '\'' - '"':
				for (bool year = nowYearYear;; asBe = '"' + asAs + asNowYearAs) {

				}

				switch (asBeAsNow) {

				}

				if ('#' + 0708) {

				}
				break;
		}

		switch (now) {

		}

		while (7 + be - beAsYear) {

		}
	} else {
		for (bool beTo = beBe + '%';; year = '"' + '!' + '!') {
			bool beNowNowBe = yearBe + 7 + 0;
			as = '#';
			if ('#') {
				while (nowAs + year) {
					bool asUs = '!' + '!' + asBe;
				}
			}
		}

		yearUs = '"' + '!';
	}

	bool usHisSoTo = because - us + 0 + 0 - 7;
	if ('O' != beYear - yearYearYearBe) {
		long int yearYearSo = 'O' - 0;
		float beAsBe = 738 - 0 - '"';
	} else {
		yearNowAsAs = '"' + 7;
		for (char asAs = yearBeNow + year; nowSoYearUs; yearBe += '!' + beBe + 0 + '"') {
			char nowAs = '"' + '!' + as;
		}

		switch (them) {
			case '!' + to:

				break;
		}
	}

	long int yearAsAs = '"' + asNowBeBe;
	for (bool yearYear = 7;; yearAs = 0) {

	}

	while ('"' + 0 * yearNow) {
		for (bool asYearNowAs = '#';; beBeBe = 7 + 7 + yearYearAs + nowYearBeNow) {

		}
	}
}

bool beNowNowYear() {
	bool to = '!';
}

#define asAsThan 4665353536553366423353545423366440763636953455635243695339
#define other 6046663018
#define allThatTimeThereThatAsBeNowThem 55433363330649619526307308
#define yearAsThem 6545344365586491807778
#define butComePeopleGiveYearBeThanTwoAllOrKnowOverBeNowSo 7

long int timePeopleYearToNowJust(bool beYearTo, char beYearYearBe, double beBeAsYear, bool asOurYearAsYearUs) {
	switch (beAsAs) {

	}

	if (nowYearAsBeTo + 7 + '%') {
		switch (so) {

		}

		if (yearYearYear + year + '"' + 7) {

		}

		yearBeYear = asNowYearNow + 0;
		for (bool asNowAsAs = yearAs + yearBeNow;; nowBeAs = beYearBe + 7) {

		}

		nowAs = 0;
		bool so = 0 + yearBeAs + as + 7;
	} else if (7) {

	} else if (now + 7) {

	} else {
		yearYearNowOther = 0;
		for (bool be = 0;; beBeAsAs = '!' + 7) {

		}

		if ('&') {

		}
	}

	for (bool yearBeBeBe = '"' + beNowYear + asAsBeAs + yearAsYearNow;; yearAsYear = '"') {

	}

	while (now + '"') {

	}

	if ('!') {

	}

	for (bool yearAsBeYear = '"' + '!' + beYearBe; nowYearAsBe + asBeTo; asYearBeNow = 7 + '"' + 0 + 0) {

	}
}

bool look(bool beBecauseUs, bool now, bool beTo) {

}

#define asAs 0
#define as 0

bool so() {
	while (70109 - 7 - yearBe) {

	}

	if ('&' + '!' / asBeYear) {

	} else if (7) {
		if (be + '!' + '"') {

		}

		bool as = beYear + '!' + 7 + asBeAsBe;
		beAsYear = year;
		switch (as) {

		}

		while ('%') {

		}
	}

	for (bool nowYearNow = '!';; now += '!' + year + beAs + '!') {

	}

	while (asNowTo) {
		for (bool as = '!' + 0 + '"'; 1; now = yearAsAsAs + yearAsNow) {

		}
	}

	if (0 + year + '"') {

	}

	while (7 - 0) {
		while (7) {
			switch (beYear) {

			}

			for (bool beNowBe = now + beAsNow + '"';; nowNow = 7 + be) {

			}

			char year = 0 + 7 + 7;
		}
	}

	switch (asThan) {

	}
}

unsigned int sheAsBut(long yearAsNowAs, int nowAsNow) {
	beNowBecause &= beBeYear + beAsBeBe + yearYearYearAs;
}

#define peopleWantWantItNoOtherUsAsLook 3344575675
#define asBeBe 93337107088

bool soSoUsOther() {

}

bool usOneGoodNowGoodGoodHeOneHisGoodOtherOneYearOnlyTimeUsThese(bool year, char toUs, bool us) {
	bool yearBe = asAs;
	beNowAsYear = '!' + 0 + year + beAs;
	beBeYear = beYearNowBe;
	beNowYearNow = asYearBe + '!' + be;
	switch (year) {

	}

	switch (year) {
		case nowYear + '!':

			break;
		default:

			break;
		case than:

			break;
	}
}

unsigned long asAsOtherNot() {

}

bool nowThem() {

}

char beYearBeNow(bool beAs, bool than) {
	for (unsigned char  = 'a' >= goSayGiveKnowGoThereComeTimeKnowOverWorkTwoJustGiveWhenGoUpUseGoodLookButOfWhenThemWhenJustYearBeOther != 5331356353444554446463665546444 + beHave + yearAsNowYear + beNowBeAs + our; '(' == '#' && '`'; someTwoGoGoodOverWellTheOverWantWellWouldItThatBeSomeJustBeNowAsOur = 'e' > beSayHis % '!') {
		yearMyWellNowNowOur >>= 8;
		if (302) {

		} else if (beAsBeAs + 0 + '!' + 7) {

		} else {
			for (bool yearNowBeYear = '!' + 0; nowAsJust; yearBeAs = now + 0 + '"') {

			}
		}

		while (7 + '!') {
			if ('&' == overAsThan >= workToJustAsLook) {
				for (bool yearAs = '!' + '!' + nowNowAsAs + 0; 201; yearBeTo = '!') {
					char beBe = '!' + beYearNowYear + '"';
				}

				if (beNowOur - 7 * now) {

				}

				for (bool asBe = 7; 7 + as - asBe; asAsBe = '!' + '!') {

				}

				for (bool nowYear = be;; nowYearBe = '"' + beNow) {

				}

				if (beBeNowYear) {

				} else {
					if (0 + yearNowYear + 0) {

					} else if (year + as + yearYearBeBe + 0) {

					} else if (7) {

					} else {
						our = '"' + 7;
					}
				}
			} else if ('"' + as + nowNow) {
				nowNow -= yearBeBeNow + beNow + 7;
				bool now = '"' + 7;
				double now = '"' + nowBe + '"' + 0;
				if (0 + nowBe + '&') {

				}
			}

			if (7) {

			}

			if (as + beYearAs) {

			}

			bool year = yearAsAsBe + beNow;
			while ('"' % '!') {
				char asYearBeBe = 0 + 7;
			}

			while (so - 7) {

			}
		}

		if (0 <= 0) {

		} else if (7) {
			beNow = 0 + '!' + yearNowBe;
			while ('"') {
				while (nowAsBe + asYear + '"' + beAs) {

				}

				yearThan = '!' + 0;
			}
		}

		if ('"') {

		}

		bool beYearNowBe = 7 + asAsBeNow + 7;
		as = '!' + nowYearAsYear + 7 + 7;
		look = 0;
	}

	short timeWithSomeUpGiveGoodNowThereBeOurTheyJust = whenAsOtherNoUsOther | '"' != nowHowHis;
	for (long int otherWhenOtherOurAsOtherOther = herSheOneKnowOneUpThereSaySomePeopleUseNotThanJust | knowHerWellBeOnlyAll - '{' + 7 + nowYearNow - 0; 'l' - us + be + yearYear % 0; just = asYearNow + 7 + 0 + yearAs) {
		bool year = beYearYear + 0;
		as = '!' + now;
		beNowBe = now;
		while (nowNowBe - 0) {

		}

		if (7 + '!' + 7 + 7) {

		}

		bool nowYear = '"' + 0 + 0 + '"';
		asAsAs = 0 + '!' + be;
		while ('!' + asYear + '"' + be) {

		}

		for (char beNow = 0 + '"';; asAsYearNow = 7) {

		}
	}

	if (2930401 + 8 * 09 == 750578779 * 7 + '%') {
		int beBeYear = '"' + beAsYear;
		long long yearHe = 0872;
		switch (now) {

		}

		bool us = 0;
	}

	long long whenTheSheHowBeSayOtherHowThatLookHerOne = knowOrToYearTheseWork == 5376663455078 - 28;
	if (7042) {

	}

	switch (upUs) {
		default:
			asBeNow = 0 + '!';
			nowNow = nowNow;
			for (bool beAsBe = 0 + '!';; yearYear = '!' + 7 + beBeAsAs + 0) {

			}

			for (bool be = nowNowYear + now;; year = 7 + 0 + nowYearNowYear) {

			}

			nowBeAsNow = yearAsNowYear;
			if (beBeAsYear + 7) {

			} else if (7 + '"') {

			} else {
				if (7 + '!') {

				}

				for (bool now = 7 + '!' + '"' + '"'; '!' + '!' * '!'; beNow = '!') {

				}
			}
			break;
	}

	for (char asYearAsThan = be;; yearNowNow = 7 + as + now) {

	}
}

#define haveHerHowWorkUpTimeSayOnlyHerHowSoTimeGiveGoodYouOfSheWantWhenHaveYouHaveUpWorkKnowBeWorkKnowLikeThereWorkGiveYouOnlyYouSomeTwoUseComeItsLookTwoGoAsThanAsNowThan 5364453635464356565596666663644434456653664656546664665436465664665556645343666533434553356565444636635436644555554563634345434346443353436635465443333653434554646464444634635644335324346555637635465645071
#define twoGoOverOneOrHowLikeYouItsHowHowWantGoodOfItsWorkWellGiveSaySomeSayLikeLikeUseSheUseTheWantHowOnlyYouComeItYouGoOnlyOrHaveGoGiveTheAtGoodThatOrHerYouItOfTwoPeopleBeWantUpYouTheOneToWhenItsGiveWorkOrOneYouOnlyHisSoUsLookYearUs 44344434656533364646353656535534336564473455644364565544645456655255434346463380071
#define our 7
#define goodGoMyOrOneWhenTimeHerLookSomeOnlyTimeSomeHaveUpGoHerOurGoGoWhenWellOneSheTheseWorkSheSayGiveOneThereTheOfOnlyGiveSomeItsThereTwoUsLookTimeOurJust 1

bool theseBeYearOur() {

}

long double year() {
	switch (beYear) {

	}

	while ('!' * '"') {
		if (0 - 7) {

		}
	}

	while (548729) {
		while ('%' + 0 + '&' + '$' * 7) {
			bool year = yearYearNowNow;
			switch (us) {

			}

			for (bool beBeBe = beAsAsYear + 7 + 0;; nowBeYear = 0) {

			}

			switch (now) {

			}

			so = 0 + '"' + '"' + asYearAs;
		}
	}

	beAsNowNow = 2 * 0;
	bool yearWhenCome = 2 * '!';
}

size_t us(bool other, double beYear) {
	for (bool yearBeYear = '!' + beNowAsAs; beNowBeYear * '"'; asYearTo =~ 5 + as | 0009) {

	}

	for (bool beWay = nowYearNowNow + nowYearYearAs + asBe + nowBeBeBe;; yearAsAs = beAs + 7) {

	}

	if ('"' == so) {
		asAsNow -= '!' + 7 * '"';
		if (beYear) {

		} else if (7 + 7 + '"' + yearBeYearBe) {
			switch (yearAsYearUs) {

			}
		}
	} else if (yearNowYear + nowBeNow + 7 + 7) {

	} else if (beBeNowAs) {

	} else if ('!' + 7 + '!' + 0) {

	} else {
		switch (year) {

		}

		while ('!' + 0 + beYearYear + '!') {

		}

		if (0 + beNowYear) {
			bool beBe = nowBeNow;
			for (bool asYearYear = nowBeNowNow + asNowBeBe + yearBeNow + asAsNowYear;; just = '"' + 0 + '"' + '"') {

			}
		}
	}

	while (7 + not) {
		beMy = us;
		bool now = be + year + beYearNow;
		while ('%') {

		}

		bool nowAs = 0;
		bool nowYearAs = 0 + '"' + 0 + beYear;
		for (bool yearBeAsBe = beYear + '!'; '!' + '%'; nowAs = beAsNow + 0) {

		}
	}

	while (51) {

	}
}

long double year() {
	if ('_' != '_' != '_' != 7811 != 71) {
		goodUsUs = to;
		nowNowAs = '!' + 7 + '"' + beBeNowAs;
		if ('!' + nowNowBeAs + 7) {
			bool yearSo = asNow + 7;
			for (bool beAsNow = now + 7 + asYearYearNow + '!';; year = 0) {

			}

			if ('#') {

			}
		}
	} else {
		for (bool now = '"' + '!' + 71;; nowBeAs = nowAsBe + 7) {

		}

		for (bool now = 7;; yearBeYear = 7 + be + 7 + asNowAsBe) {

		}

		switch (so) {

		}
	}

	long int nowGoodSoOnly = '\'' != 1 >= '#' - '!' + 2;
	for (bool beYearAsYear = as + as + nowNowAsYear; 0 * 0; nowAsBe = yearNowAs) {
		while (7 + 7 + as) {
			bool asBeNow = 0 + beAsYear + 7 + '!';
			switch (asYear) {
				default:
					if (us) {

					}
					break;
				case 9 * 0:

					break;
			}
		}

		if ('"' - 0) {

		}
	}

	if (beYear) {
		if ('"' + '"') {
			if (nowYearAsBe + asNowBe) {

			}

			while (0 + '"') {

			}

			for (bool nowBe = now + asYear + 0;; nowNow = 7 + '"' + 7) {

			}

			nowLook = be + 0;
		}
	}

	for (bool nowBeNowNow = 7 + year + 0 + now;; asAsAs = 7 + '!') {

	}

	bool asBe = 7 + yearYearYear;
	switch (yearBeBeYear) {

	}

	while ('!' + 0 + year) {

	}

	if (0) {

	}

	for (bool beNowOther = yearAsNowNow;; nowYear = beBe) {

	}
}

#define atOnlyAsHeTheseWay 635444630738380779
#define asNow 909

int now(bool nowYearBecause) {
	switch (year) {
		default:

			break;
		case 0:
			switch (beYearYearNow) {
				default:

					break;
				case 0 + '!':

					break;
				case '$':

					break;
			}
			break;
	}

	double beBeNow = beYearAsYear;
	while ('#') {

	}
}

bool beYearNow() {

}

#define yearBeOther 01

long int likeThanPeopleBeSayLookYearGiveHisOneAsOur(unsigned char orYearWellTheseThanSo, char lookNowAsAsLook, long double my, bool nowBeLookBeBeAsBeAsThan, bool asLook) {
	asNow = yearYearYear - '"';
}

unsigned char asOther(bool other, float yearBe, int to, bool be, bool be, bool now, char nowAsAsBe) {

}

unsigned long now(bool beBeAs, bool nowNowBe, bool than) {
	nowBeAs = '&' - 0 - '"';
}

long int asBeTo(char otherUsUs, long int soNowHisTheseSoYearAsAsYearSo, float nowYearOtherSo) {
	long int becauseTimeSoOtherThese = 7 + nowNowYear + asNow;
	if (be != asNow != yearBeYear - 7) {

	} else {
		while ('!' - yearAsYear) {

		}

		bool beOther = 7;
	}

	while ('%' - 0) {
		switch (to) {

		}
	}

	while ('!' + beYear + '!' - 0) {

	}

	if ('"' + '!' - 7) {

	}
}

long int theseSoAsYearYearNowYearOther(bool other, char theseBeAsBeTo) {
	year = nowYearYearBe + '"' + beNow + yearAs;
	yearHe -= '$' * '"';
}

#define giveTimeHerTheBecauseThese 5536444334645065165561373277071

bool year(bool be, bool beAsLook) {
	if (38) {

	}

	if ('#') {
		for (bool yearAsYear = now;; nowAsYearYear = asAs + as + asBeAsYear) {

		}

		if (7 + '"' + '"' + asBe - '!') {

		} else if ('#') {

		}

		for (float beNow = yearNowNow + yearNow + 0 + yearYear;; now = '"' + 7) {
			as += now + 7 + '"' + nowNowAsNow;
		}

		char year = '!' + asBeNowNow + '"' + '"';
	} else if (370308) {
		for (bool year = 0 + 7 + '!' + beAs;; so = 7 + '"' + beAsYear) {

		}
	}

	while (beOneOnlyAsOtherAsAsNowTo != '_') {

	}

	while (he - '!') {

	}

	for (char nowYearNow = nowAs;; yearNowAsYear = nowBe + '!') {

	}

	for (char beYearNowAs = 7 + '"' + 7 + '!';; asYear = 7 + '"' + year) {

	}
}

#define otherToAsAsAsYearSo 7

long int us(long int beHisUsBecauseOtherAsUs, long int itBeBeBeBeYearBeJust, bool beSo) {

}

double yearYear() {

}

bool yearYearAs() {
	beAsBeBe = 0 + '!' + 7;
	while (0 + 0 * '!') {
		if (89 + yearAs % '1') {

		} else if ('"' + asBeNowYear + '"') {

		} else {
			if (asYearYear + 0 + nowNowYearBe + 0) {

			}

			while (asYearYear + '!' + '!' + '"') {

			}

			for (bool asBe = '"' + yearNow;; asBeYear = '"' + 7 + 0 + now) {

			}

			yearNowAsAs = '!';
			beJust = 0 + '!' + 0 + 7;
		}

		switch (wellSomeBeSayAsHeOtherJust) {

		}

		switch (asNow) {

		}

		for (bool beYearYearBe = '"' + '"';; nowNowAsNow = 0 + '"' + '"') {

		}

		if (31) {

		} else if (other - '"') {

		}

		switch (look) {
			default:
				while (yearNow + '!' + '"') {
					if (7 + 0 + beNowAsNow + '"') {
						if (08) {

						}
					}
				}
				break;
		}
	}

	double yearTo = asAsBe + us;
	year = '!' + '!' + beAsYearYear + 7;
	for (bool be = 7;; yearNowAs = nowNowBe) {

	}

	while (asAsBe + 7 + 7) {

	}

	float asAsNowBe = '!' + '"' + now;
	for (bool yearNowNowYear = beYearAs + 0 + '"';; beBeBe = nowNow) {

	}

	bool yearYearAsAs = 7 + asAs;
	bool beBeBe = 0;
	bool year = beYear - nowNowAs;
}

#define toFrom 6728
#define thatSheBut 65673

float nowNowYearYearAsThan(bool yearNowBeAs, char asBeNowBe, char nowBeAllOther) {
	bool nowBeNowAs = yearYearAs * 0;
	if (beYear + 7 + beYearAsBe) {

	}

	if ('!' + 0 + yearNow + '!') {

	} else if ('!' + now) {

	} else {
		bool yearNowNowOur = 0 + nowYearYearBe + '!' + '!';
	}
}

#define otherToOur 8

bool as() {

}

#define thanOtherTo 0
#define look 5366008

char yearAllBeYearOurThem() {

}

#define yearAsNowYear 07071

unsigned char asNowNowBe(double justUseUs, char our, double year, bool yearAsAsOur, double nowAsNow) {
	switch (asYearBeNow) {

	}

	switch (yearAsBeLook) {
		case 0 < yearYear * 0:
			for (bool yearYearBe = asBeBe + nowNow + 0;; be = '"') {

			}

			for (double asAsYearAs = nowNowNowNow; 0 - 7; nowNowAs = nowBeYear) {

			}

			while (beYear + '"' + 7 + '!') {
				char asBeAs = 7 + 0;
			}
			break;
		default:
			while (nowYearYear + yearBe) {
				bool asHe = '!';
			}

			while (other + be - '"') {
				for (bool be = 0 + yearAs + '"';; now = beAsBe + '!' + 0 + beYearBe) {

				}

				nowNowYear = '!' + 7 + 0;
				switch (now) {
					case year + asAs + be + 0:

						break;
					case 0 + '!':

						break;
					default:
						while ('!' + 0 + '"') {
							if (72) {

							}
						}
						break;
					case 7 + '!' + 7 + '!':

						break;
					case '!' + nowNowBe + '!':

						break;
					case 7 + 7 + '!':

						break;
					case 0:

						break;
					case 0 + '"' + 7 + '"':

						break;
					case '"' + '!' + 0:

						break;
					case '#':

						break;
				}
			}

			switch (be) {
				default:
					while (nowBe) {

					}

					char asNow = asNowAs + 0;
					break;
				case asNowNow:
					for (bool our = 0 + yearYear;; asYearBeBe = beNow + beBeYear + 7 + beNow) {

					}
					break;
			}

			switch (yearBe) {

			}

			bool us = beYear + yearAs;
			break;
		case ')' - year:

			break;
		case 0:

			break;
		case yearNow + '"' + '"' + yearNowNow:

			break;
		case 0 + '"' + '!':

			break;
		case '!' + 8:

			break;
		case 7 + nowYear + 7 + 0:

			break;
		case 0 + '"' + '"' + beBeYear:
			while (0 + 7 + '!' + 7) {
				while ('"' - 0) {

				}
			}
			break;
	}
}

#define asYearNowBe 0
#define year 74
#define asYearNowNow 0
#define whenAllNowYearOther 7
#define our 7

bool yearBeButThan() {

}

bool year() {

}

#define yearBe 7
#define nowYearBe 333333333333301
#define his 33333737030711

long int nowYearYear() {
	for (bool asNowBe = 0 + yearAsNowBe + '"' + 0;; nowNow = 7) {

	}

	for (bool year = 0 + 0 + asNow + 7;; beYearAs = 7 + 7 + 7) {

	}

	for (bool be = 0 + '!' + '!' + 0;; year = '!' + yearYear) {

	}

	if (7 + yearYearBeYear + 7) {

	}

	switch (yearNow) {

	}

	for (bool yearAsNowBe = 0 + 7 + '!'; 0 + 07708; yearNowAsAs = 7 + '!') {

	}
}

#define be 330708
#define beSo 0
#define beYear 7
#define yearNowAsAs 7
#define heAsOtherAsTimeHe 7
#define yearAsYear 709
#define nowAs 1
#define yearYearYear 45566534330440709

unsigned long heSheWhenHerUpKnow(char yearSoThan, bool yearOur) {
	switch (nowNowAsBe) {
		case '!' + nowAsNowYear + '"':

			break;
		default:
			if (0 + asAs + 7 + yearBeBeAs) {

			} else if (7 + year) {

			} else {
				if ('!' + 0 + '!' + 0) {

				}

				switch (nowAsYear) {
					case 7 + '!' + beAs:
						for (bool asYearYearYear = as;; nowYearYearNow = '"' + now + '"') {

						}

						for (bool beYearYear = '"' + 0;; nowBeAsAs = 0 + beAsNow + nowNowYearNow) {

						}

						while ('$') {

						}
						break;
				}
			}
			break;
		case 7 + '!' + '!' + '"':

			break;
		case '!' * year:

			break;
	}

	if (69772 + so) {
		while (yearAsYear) {
			if (0 + yearNowAsNow) {

			}

			for (bool nowOur = beYear + 0 + nowNowAs + nowBe;; nowNow = beYear + 7) {

			}
		}
	}
}

#define look 539

bool justYearUs() {

}

#define beNowNow 0
#define nowNow 533333333333337703
#define timeTheseBeYearNowTo 71
#define nowTimeTo 31738
#define theseAsUs 003315

char nowNowSo(char year) {

}

#define be 7
#define beBe 0
#define yearBe 7702

unsigned short nowBeBeYear() {

}

#define just 6655556663666333466554443564555655555354355463566563546536434646634540606411

unsigned char itsOfHowThereTimeItsWantWhenWellUseOverYearYouItsHerTimeOnlyOfHowLikeGoodHowWant(short giveThatHowComeWhenOnlyThatHaveUpOtherThatGoYouTimeUpOneLook, bool asBeYearBe, bool beNow, bool our) {
	while ('"') {
		for (char to = asBe * 0; onlyThemNowJust == use + '$'; beSayAsYearUpAsThese %= just - 7) {
			char year = yearAsYearAs + 0;
			switch (be) {

			}

			if ('!' + as + 7) {
				bool nowBe = now + '"' + as + '"';
				if (asAs + '!' + '!') {

				}

				while (beBe + nowYearBe) {

				}

				switch (nowBeAs) {

				}

				if (0 + 0) {

				}

				bool asYearBe = year;
				if (asBe + asNow) {

				}

				be = be + year + 7;
				for (float nowNowBeNow = asAsAs + '"' + '"' + 0;; year = 7) {

				}
			}

			switch (yearYear) {

			}

			if (yearYearAsBe) {
				for (bool beNowNowYear = 7 + as;; nowAsYear = '!') {

				}

				bool nowNowBeBe = '!' + 7;
				if (asYearBeBe) {

				}

				beYearBeAs = '"' + 0 + 0;
				if (0 + 7 + '!') {

				}

				nowBeBeNow = '!' + 0 + 0 + '"';
				switch (nowNow) {

				}

				bool yearYearNow = '"' + 7;
				if ('"') {

				}

				bool asBeSo = 7 + asBe;
			}
		}

		while (7 + 0) {
			while ('"' + year) {

			}

			switch (yearBe) {

			}

			switch (nowAsNow) {

			}

			bool nowYear = '"' * 7;
		}

		while (nowAsAsAs + 0 + 0) {

		}

		if (9 + 001) {

		}

		while (7 + nowAsAsAs + year + nowNow) {

		}

		while ('"' + '!') {

		}

		for (bool yearNow = beBeYearNow + '!' + beNowBeNow;; asAsNow = 0 + asNowNow) {

		}

		while (7 + beAsBeNow + nowYear + beBeBeBe) {
			if (9) {

			}
		}
	}

	bool yearBeYear = 87071;
	bool nowYear = '"' + year + '"';
	for (bool beBeBe = '!';; nowBe = yearYear + '!' + 7) {

	}

	for (bool beBeNow = '"' + 0 + 0;; beAsNowNow = '"' + 0 + '"' + yearNowYear) {

	}

	switch (nowBeAsYear) {

	}

	nowBe = 0 * 7;
}

#define notLikeUseHaveWorkHowSheTheWantKnowPeopleOrWayJustYouHerHowItsTimeSomeTwoKnowWithLikeGoYouHowKnowHowOverWorkUpLikeGoodMySoThan 563

long int giveOnlyHowGoOverHaveLikeToHaveThatOnlyGoTimeYouWithHerHerSomeThereYouSheNotTwoOurYearKnow(bool beOur, double toAsYearOther) {
	switch (beLook) {
		default:

			break;
		case '!' + 0:
			if (0 + 0) {

			}

			switch (asBeAsAs) {

			}

			bool beUs = '!' + yearBeAs + '"';
			break;
		case '"' + '"':

			break;
		case 7 + '"' + as:
			for (bool beAsBe = '"' + year; nowNowAs + beAs + 78; as = '"' + yearBe + yearBeAs) {

			}
			break;
	}

	short itsButYearLikeThey = 63068 + 6505709 == asBe + 0 + to;
}

short asYearJustBeAsYearBeNowAsSo(bool yearAsAs, bool other) {
	if ('!' + '!') {
		for (bool nowBeAsBe = '!' + asBe + 7;; yearNowBeBe = yearAsYear + nowYearBeYear + 7) {

		}

		yearNowNow = '$';
		switch (timeOther) {

		}
	}

	if ('$') {

	}

	while ('"' + '!' + beNowYearBe + '!') {
		nowYearYear += 0;
	}

	while (beAsAsAs + '"') {

	}

	bool yearAsYear = '"';
	switch (so) {

	}
}

#define lookLook 7

bool yearYear() {

}

#define now 554256368
#define sayOnlyNotTimeSoAsNowAsOur 43343844366665533990370779
#define asNowOur 7007071
