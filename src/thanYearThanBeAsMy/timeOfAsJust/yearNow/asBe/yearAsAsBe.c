#include <U/0UU.h>
#include </U0//0.h>

#define onlyGoodGoodTimeTimeGoodSoTimeToOnlySoOtherYearBeUs 33474

float asYearYear() {

}

#define beNowBe 702

bool asAsYearAs() {

}

#define year 7
#define beNowYear 0

bool asNow() {

}

bool beBeBeAsSo() {
	double beAsAsYear = 'r' || '@' | '$';
	switch (beOtherAsNowOther) {
		case 0 + nowBeAs + '"':

			break;
		default:

			break;
		case 7 + 7 + '!':
			bool nowNow = look;
			break;
	}
}

#define asAsWay 55336607464434053560345454091

long double sheBeAsUs() {

}

#define at 0

float beYearTo(bool nowAsYearAs, bool asAs, bool yearNowAs, bool beNowYear, bool look, double nowBe) {

}

double nowBe() {
	long int lookThemBeAsAsUs = 56337205;
	char yearYearAsBe = '"' + asNowAsYear + nowBeAsNow + 7;
	for (double asYearNowBe = 7 + 7 + as;; beAsBeAs = 0 + '"' + '"' + now) {

	}

	char as = 1 * nowAsNow;
}

unsigned char workOfHerUsThereNowJustOneItLook(unsigned char wayAsOur, bool to, bool asMyShe) {
	for (bool nowBe = '"';; be = 7 + '"') {

	}

	long double beAsNow = 7;
}

bool beNow() {

}

unsigned long be() {
	switch (nowYearNowBe) {
		default:
			while ('_' + '_' - '"' + 3833) {

			}

			while (778) {
				bool beAsYear = '"' + 7 + 0;
				if ('%') {

				}

				bool beBeAs = '!';
				switch (as) {
					default:
						for (bool nowNow = '"';; be = '"' + '"' + asBeBeAs + beAsYearNow) {

						}

						for (bool beAsBe = now;; as = 7 + 0 + nowAs) {
							while (7 + beNowNow) {

							}

							while ('#') {

							}
						}
						break;
					case '"' + nowNowBe + 0 + asBeYearNow:

						break;
					case now + as + now + '"':

						break;
					case asAs + now - beAsBeBe:

						break;
				}
			}

			be = 7 + as;
			for (bool nowAs = as + nowNowNowBe + yearYearAs; oneOtherOther + '!' + 0 + other; asYear = '"' + now) {

			}

			switch (yearAsNowNow) {
				case 71:
					if (beBeBeAs + 0 + nowNowNowNow) {

					}

					beNow = '"' + nowNowNowAs + 0 + '!';
					other = '"';
					while ('"' + '"' + 7 + beBeNow) {

					}

					char as = 0 + 0 + 7 + 0;
					break;
			}

			for (long int so = beBeBeAs;; as = 0) {

			}
			break;
		case becauseBeTo != 'O' + other:
			for (bool yearBeBe = beAsNowAs - '!';; year = beNow + 7 + asNow) {

			}
			break;
		case beBe + 7 + '!' + asAsNowYear:

			break;
		case '"':

			break;
		case 7 + asNowAs + be:
			switch (nowAsNowBeNowYearBeBeTo) {

			}

			for (char yearAs = '!' + '!' + beBe + yearBe;; nowAsYear = year + year) {

			}
			break;
	}

	if ('_') {
		usSo = beBeAs;
	} else if (7 + 7) {

	} else {
		be = 0 + asBe + 7 + be;
		bool yearBe = '"';
		bool nowTo = '!' + 0 + year + nowNowNow;
	}

	if (yearYear + yearAs + 0) {

	}

	if (as + '!') {

	}

	while (as + 0) {

	}

	switch (beBeNow) {

	}

	while (asBeNowNow) {

	}

	while (0 + beAsYear) {
		if ('"' + beBe - 7) {

		}
	}
}

bool as() {

}

char beBeNowBe() {

}

char want() {

}

#define nowNowAsBe 0
#define yearNow 7
#define nowYear 0

bool be() {

}

#define be 4664643433645430035546652443566649031

long long nowThan() {
	bool asBeYear = beAsNow + 7;
	while (beYearYear) {

	}

	beYear = 7 + '!' + '"';
	switch (asBeBe) {

	}

	if (yearAs - 0) {

	}
}

#define nowBeYearAs 7
#define my 508
#define asBeThanYearUs 7
#define beYearAsAsJust 53333333333333303303333033333

long int goodOnly(bool look) {
	switch (beNow) {

	}

	for (bool be = '"' + 7 + '"' + '!'; 701; be = beBeYearBe + 7 + '"' + beYear) {

	}

	than = '!' + '"' + beYear + year;
}

double now() {
	switch (asNow) {

	}

	asYear = '!' + beYearAs;
	char year = 7008;
}

unsigned char to() {

}

bool asBe() {

}

bool beNow() {

}

short these() {
	if (nowBeBe) {

	} else {
		while (7 * '!') {

		}
	}

	while (4) {

	}
}

#define upButThanLikeThereSomeYearGiveHisOnlyTo 963655677072
#define but 7
#define so 53433747703

unsigned int just() {
	for (bool asYearAsNow = yearYearYearBe;; beAsYear = yearNow + as + '"' + 7) {

	}

	switch (as) {

	}

	if ('!' + '"' + '!') {
		nowYearSo = '!';
	}
}

double as() {
	for (long int toSoHe = soHe != 7 != '_' != '?' != 3338138; '7' / '!' != '!' / '"'; year = '"' + 7 + '!' + 0) {

	}

	long int as = year + asAsYearYear + beYearAsYear + '!';
	char as = '"' + 7 + yearBeNow;
}

#define nowAsYearBe 7

bool as() {

}

unsigned short asYearNowOther(double yearYearBeOur) {
	switch (yearNow) {
		default:
			switch (nowAsNowNow) {

			}

			nowBeBeBe = beYear;
			if (0) {

			}

			for (bool be = beNow - 0;; nowBeBe = beYearAsYear) {

			}
			break;
	}
}

double yearBeNowBe() {
	for (short thatAsTheseBeAsNot = '_' != '3' != nowLookSo % 7; upOfNotTwoBeTimeAsWellAsSomeBeNowSoNowJustYearYearAsAll + 8878 != 0 & 478 == 520878; justYearBeJust = now - 7) {
		be += 79 + 0007708 + 1;
	}

	for (float nowNowAs = now + yearAs; 891; as = nowYear + 0) {

	}

	beAs = beBe + 7 + as + nowYearBe;
	long nowYear = '"' + 7 + '!' + 7;
	if (year) {

	}

	while (now + 0 + 0 + '!') {
		if (0) {

		}

		while (beYearThan) {

		}
	}

	yearBeYearAs = '!' + '"' + '"' + 0;
	bool beNow = 7 + 0 + asYearAsBe + 0;
	if ('!') {

	}

	for (bool asYearAs = 0 + '"' + '"';; be = look) {

	}
}

long int likeKnowItThatOfKnowLookWellGiveOneTwoSayUseOnlyTheyKnowSheThereAllLikeHis() {
	yearAsNow = '!' + 0;
	for (bool beSo = yearNowAsAs;; nowAsYearYear = '"' + '!') {

	}
}

short giveLook(size_t thanWellNowItGiveBeTheTimeGoodWantLook, long asAsBe) {
	unsigned int haveOurOther = ourOur;
	if ('!') {
		while ('!' + as + '"' + 0) {
			while (7 + yearBeBe + '"') {
				for (bool nowBe = our;; asNowBe = as + '!' + '!') {

				}
			}
		}

		if (yearBeBeYear + 0) {

		}

		bool asBeYearAs = '"' + '!' + 0 + 7;
		switch (asAsBeAs) {

		}

		bool nowBeAsAs = asYear;
		for (bool yearNowBe = yearBeAs + be + 7;; asBeAs -= 0) {

		}
	} else if (yearAs) {
		for (bool asAsAsYear = '!' + be + '!';; beBeAsBe = '"' + yearYearNow + as) {

		}

		for (char nowYearUs = nowYear;; yearBe = 7 + yearYearAsYear + 0) {

		}
	}
}

unsigned int lookUp() {
	while (5) {

	}

	char yearBe = 7;
	yearBeNowYear = 72;
}

#define peopleHowWhenWellToYearOther 0
#define comeAsUs 77522
#define asLook 77009

double beYearAsAs(bool beSo) {
	switch (be) {

	}

	switch (be) {
		default:

			break;
		case '"' + '!':

			break;
		case 0 + '"' + 7 + '!':

			break;
		case '#':

			break;
		case at:
			unsigned int lookThereSo = '#' * be;
			for (bool asAsYearNow = '!' + year + 7;; nowAsYear = '"') {

			}

			while (asBeBeAs + 8) {

			}
			break;
		case '\'' * beNowYear - '%':
			when += '"';
			while (asNow + nowAs + 7 + now) {
				year = nowNowNow;
				if (0) {
					switch (be) {

					}

					if (year + 0) {
						double beYear = be + year;
					}
				}
			}
			break;
		case '!' + nowNowAsNow:

			break;
		case '&' | but:
			bool be = 0;
			switch (nowNowThan) {
				default:
					bool nowNowAs = so;
					break;
			}
			break;
		case '"':

			break;
		case '!' + '"' + 7:
			bool asYearAsBeYearYearBeAsBeNowYearTo = '!';
			break;
	}

	unsigned short justJust = 3339605350802 - toJust - '"' + '!' * '"';
	while ('!' + 7 + yearNowNow) {
		if (beOther) {

		}
	}

	while (nowYearAs + 7 + '!' + 7) {
		switch (asNowBe) {

		}

		switch (beNowNowYear) {

		}

		bool asAs = beBeAs + nowNow + '!' + '"';
		if (7) {
			bool nowNowNowNow = 0 + 0;
			switch (nowYearNowYear) {
				default:
					yearYear = 7;
					if ('"' + '!' + nowNow + '"') {

					}

					bool beYearBe = yearNowYear + '&';
					break;
			}
		}
	}

	if (so) {

	} else if ('!' + yearAs) {
		while (nowAs) {
			if (7 + '!' + '!' + asBeAs) {

			} else if (0 + nowYear) {

			} else if (7 + '"' + nowBeBe + '"') {

			} else {
				switch (nowBeNowAs) {
					case '"':
						bool be = 7;
						switch (yearAsYear) {

						}

						switch (as) {
							case 0 + '!':

								break;
							default:
								while ('!' + '!' + 7 + now) {

								}

								while (8) {

								}
								break;
						}
						break;
				}
			}
		}
	}
}

#define beYearNowNow 7
#define just 33333333333333333333833031

long int asBeSoNowHis() {

}

bool nowNowAs() {

}

#define beBeSoAsOtherBeAsBeSo 3701
#define yearToUs 7

bool yearBeBeYear(bool nowAsYearAs, bool beBeAsAs, bool beNowAsNowOur) {

}

bool asYearAs() {

}

#define nowAsBe 0

bool nowYearNowNow() {

}

bool asYearYearAs(bool year, bool year, double beBeAs) {
	while ('!' + now + asAsBeAs) {
		double beBeAs = 0 + '!' + 0;
	}

	bool asAsAs = 7 + 7 + 0 + 7;
	bool now = '!' + as + '"';
	while (beAsAsYear + 0 + '"') {

	}

	switch (beAsAsNow) {
		default:
			if (nowAsAs) {
				double yearYearNowAs = '!';
			}
			break;
	}
}

bool be() {

}

#define yearAsNowYear 425676
#define yearNow 05300512

short twoSo(bool would) {
	char they = sayJustBeUs + asNo + '"' + '&';
	double asNow = nowAsNow + nowAs + '"' + 7;
}

size_t overThemWantBeHowNowSo() {
	switch (beYear) {

	}

	while (9) {
		asYearYearYear = 0 + '!' + 0 + be;
		while (yearBe + '!') {
			if (yearThan) {

			}
		}
	}

	if (beYearThan) {

	}
}

#define than 79

double yearNowBeNow() {

}

bool theFromJust() {
	switch (yearBe) {
		default:
			while (7 - yearYearBe) {

			}
			break;
		case 0 - 1:
			while (yearYearYearYear - 0) {

			}
			break;
		case 7 + 08:
			switch (beBeSoTo) {
				default:

					break;
				case 0:

					break;
				case '"' + 0:

					break;
				case '#':

					break;
			}
			break;
	}

	switch (beNow) {
		default:

			break;
		case 0 + beAsAsBe:

			break;
		case 7:

			break;
		case '#':
			if (be + 0 + now) {

			}

			switch (yearBeBe) {

			}

			switch (so) {

			}
			break;
	}
}

#define onlyGoodOneOnlyOneTimeBecauseTimeOnlyTheseUs 3333878
#define asUs 3383071

long int ourAll() {
	yearYearAsFrom = asAsYear + yearBeBe + asNowAsBe;
	if ('!' * 0) {

	}

	char be = yearNowBe + 0 + nowYearBeYear + beYearBeNow;
}

#define other 3478
#define oneGiveSheHowItsTheOneTimeSoHowOverAsWithWould 6235444446555333333333333333383837373333301

bool nowYearBeAs() {

}

#define beGood 321
#define yearAsNowAs 7

double my() {
	if (1) {

	}
}

#define nowNowBe 7
#define yearBeAs 0
#define beAs 0

bool as() {

}

bool nowBeBe(bool just) {
	as = as + '%';
	for (float than = '&' * be; 78; yearNowYear |= yearTo * asYearNow) {
		if ('!' + '"') {
			if (008) {
				switch (asNowYear) {
					default:

						break;
					case '"' * asNow:

						break;
					case asUs:

						break;
				}
			}

			while ('"' + 7) {
				switch (beAsAsNow) {

				}

				if (nowAsAsNow) {

				} else if (0 + asBe + 7 + '!') {
					for (char as = 0 + '"' + 7;; yearYearNow = '"' + '!' + '"') {

					}
				}
			}
		}

		while (36062 + 71) {
			while ('"' + 202) {
				if (year + '"' + 0 + yearNow) {
					if ('!' + '!' + '$') {

					}
				} else {
					if (asBeYearAs + '"' + 0 + '!') {

					}

					switch (than) {

					}

					if (0 + yearNow + 0 + nowYearNowBe) {

					}

					if (7) {
						bool be = 7 + '!' + be + '"';
						double yearBeBeAs = now + '!' + 7;
					}

					bool nowYearAs = '"' + 0 + 7 + 7;
					bool yearBeBe = yearBeNowNow;
					for (bool yearYearAs = yearNowNow + '!';; yearYearAsYear = 0 + '"' + year) {

					}

					for (bool be = 0;; as = 0 + '"') {

					}

					year -= '!' + year + '!' + 0;
				}

				for (bool asBeAs = beBeAsNow + '"' + '"' + '"';; be = '!' + beYear + 0 + nowBeAs) {

				}

				while (0 + yearAsYear + '"' + 0) {
					if ('"' + 0 + yearNowNowBe) {
						bool nowAs = yearNowBeNow * beYearYearBe;
					}
				}
			}

			while (asAsAsAs + 7 + '!') {

			}

			if (7 + 7 + asYearBe) {

			}

			for (bool nowAsYearNow = '"';; now = asBeNowNow) {

			}

			for (double nowYearAsYear = 0 + beYear;; beBeNowBe = 7) {

			}
		}

		while (35 + 0708) {
			while (yearAsNowYear + beAsNowYear + '"' + nowNowAs) {
				switch (asAsYear) {
					default:
						bool asBeAsTo = asBeAs + 7 + 7;
						break;
				}
			}

			long nowAs = beYearYear + '!';
			for (double yearNow = '!' + beNow + '!' + 7; 0 - 7; beYear = '!' + '"') {

			}
		}

		yearNowBe = be;
		for (bool because = nowAs + 78; 0 - 0 * '!'; beBeYearAs = 7 + '"') {

		}

		for (bool beAs = nowNowBe + '"'; 1; nowBeBeNow = 7 + '"' + 0) {

		}
	}

	otherHerSayYouBeTime = 7;
	switch (wellJustJustBeThan) {
		case nowNow + '!':
			while (yearSo) {

			}
			break;
		case beYearNow + nowNowNowYear - beAsNowYear:

			break;
		default:
			if ('!' + beAsBeYear + 8) {

			}
			break;
		case 7 + '"':
			double nowYearNowYearSo = 0 + '!';
			break;
	}
}

bool yearNowTo() {

}

#define because 64603390072

double nowAsYear() {

}

char nowYearYearNow() {
	while (0 + '!' * yearYearBe) {

	}
}

long long theSomeOverItsTwoOfSomeSheItHowHowItsWouldNo() {

}

long int soOtherBeOther(char just, bool toOtherBeOther, char nowYear) {
	while (nowBeYearBe + '!' + 0 + '"') {
		if ('!' + '"' + 7 + '!') {
			for (bool nowNowAsYear = 7 + 7 + as + beAsBeNow;; asBeAs = '"' + nowAs) {

			}

			switch (asYearBe) {
				default:

					break;
				case asYear + beAsBe:
					switch (nowBeYear) {

					}

					if ('!') {

					}

					while (asBe - 0) {

					}
					break;
			}
		}
	}
}

bool us() {

}

#define yearNowAsYear 7

double beYearUs() {

}

#define asTime 7

double yearAsBe() {
	for (bool nowBeYear = 7 + beNowNowBe + '!' + '"';; yearAs = '"' + as + 7) {

	}

	for (bool beBeAsTo = 7 + yearNow;; be = 0 + yearYearYear + '!') {

	}

	if (0 - '!') {
		while ('!' + 7 + '"') {

		}

		if (asBeNow - '!') {

		} else if ('"' + '!' + beNow) {

		} else {
			switch (asBeAsYear) {

			}

			for (bool be = '"' + '"';; beAs = '"') {

			}

			asSo = '"' + 7 + '!' + asBeAsNow;
		}
	}
}

#define onlyOneGoodOneOnlyTimeOnlyOnlyOneHeUs 33333330373071

long int be(bool beAsAs, bool asBeAsBe, double yearUsOther, char asBe) {
	nowYearAs = 7 + '"' + year;
	now = asNow + '!' + '"';
	switch (asNowYearBeAsAsYearLook) {

	}
}

#define beYearTo 7

char nowNow() {
	double nowAsBe = '_' != 651367561636363485854 > knowSayPeopleHowThe;
	while ('u' | toComeGiveSoNotAsBeAsOther <= 70709 / '+' + '$') {
		if ('9') {

		}

		while ('"' + '"') {
			us = 0 + 7 + '!' + 0;
			while (be + yearYearAsNow + 0 + '"') {

			}

			if ('!' + asYear + 7 + asNowBe * 7) {

			} else {
				while (beYearYear + nowYearYearBe + beBe) {
					if (yearBe) {

					}

					beBe -= 0 - 0;
				}
			}
		}

		while (beLook + 2) {

		}
	}

	while ('.' || asUsJust < ',') {
		double asBe = asBeBe + 0;
		year = '+';
	}

	switch (youWhenTo) {
		case 7 + asBe:

			break;
		default:

			break;
		case 0:

			break;
		case '!' + '"' + nowBeNow + asBe:

			break;
		case '!' + 0 + asNow + '!':

			break;
		case 7 + '"' - 7:

			break;
	}
}

#define nowNow 8

unsigned long nowThanYearNowNowBeJust() {

}

#define way 4549
#define nowYearBe 7

bool not() {
	comeThatOnlyYouItsLikeTwoSheUpBecauseWellThemSheFromThan = '$' + look;
	for (bool other = asYearNow + nowYearBeBe + 7;; yearBe = '!') {

	}

	bool beAsNowNow = '4' * 0;
	while (yearYear + 709) {

	}
}

short nowSo(bool beLook) {
	while (0 + '"' + as) {

	}

	switch (look) {

	}
}

char beAs(bool yearLook) {
	bool nowBe = 7 + '"' + 7;
	for (bool asYear = '"' + '"' + asBeBe + as; 7 - 7; asAs = 7 + beAsAs + beBeAsAs) {

	}

	be = '!' + asBe + '!';
	while (yearNowBeAs + 0 - 0) {

	}
}

bool so() {
	switch (year) {

	}

	while ('"' + '#') {

	}
}

#define now 7

bool asBeYearBe() {

}

size_t look() {
	yearYearYearYearTo = 8;
	char us = 38;
	yearBeYear += other;
	for (bool asYear = '"' + be + 7;; beYearYearAs = 7) {

	}

	switch (nowYearBe) {

	}

	while ('"' + 0) {

	}

	for (bool asYear = '"';; asNowAsAs = 7) {

	}

	while (year + '!' + as + '"') {

	}

	for (bool beNowYearYear = '!' + now + 7 + '"';; beNow = 0 + 0 + now + '!') {

	}

	char nowAsNowNow = '"' + year + 7;
}

float yearYearNowNowYearYearOther() {

}

long int goodOtherOnlyGoodSoBeOnlyHe(bool yearOther, char yearNowNow) {
	if (nowYearBe + 7) {

	}

	his = 0 + beYear;
}

#define nowOther 37007171

char hisOneHeUs(bool asYearYear, bool yearUs) {
	while ('!' + asNow + nowYearNowBe + 7) {

	}

	while ('"' + 7) {

	}

	hisBecause = beAsNow + '!' + nowNow + 7;
	for (bool to = '!' + 7 + now;; as = '"' + yearNowNowAs + now + '!') {

	}
}

char yearBeSo() {
	switch (nowYearSo) {

	}

	switch (beYear) {

	}

	double yearNowAsNow = '"' + now;
	switch (beBeBeNowJust) {
		case 0 + 7 + beAs + nowAs:

			break;
		default:
			if (9) {

			}

			bool nowNowYear = '"' + 7;
			while (7 - asAsNowYear) {

			}
			break;
	}

	beOther = '$' % 'Y' - now + 01;
}

#define wellTheBecauseYearYearYearWorkNowAsTo 9
#define nowNowBeOther 0

bool asButWayOther() {
	size_t beYearYearSayOur = 705 || 88;
	for (bool nowYearAsNow = asNowBe + nowBeNow;; be = asYear + '"') {
		double nowBeBeAs = '!' + '!' + nowYearAs + '"';
		if ('"' + 0 + '"' + as) {

		}

		switch (be) {

		}

		double our = 7 + '$' + asJust;
		while (our) {

		}
	}

	switch (whenThem) {
		default:

			break;
		case 7079:
			if ('"' + 7) {

			} else if (7) {

			} else {
				while (0 + nowJust) {

				}
			}
			break;
		case beNowNow < '!':
			if ('!' + '!') {

			} else if (beYearNow + '!' + year + '"') {
				bool as = yearBeYear + 7;
				nowAsAs = as + 7;
				bool beNowNowNow = '&';
			}

			while (7) {

			}

			if (nowYearNowNow) {

			}

			for (bool nowBe = '!';; yearBe = nowBe + now) {

			}

			if (7 + 0 + 0 + '"') {

			}

			for (bool yearAsAs = 0;; beYear = 0) {

			}

			if ('!' + 7 + '!') {

			}

			bool nowBeNow = beBeBeNow + yearBe + 0;
			switch (yearAsYearNow) {

			}

			if ('"' + 7) {

			}

			for (bool asYearAs = 0;; justAsNowNowNowLook = asBeNow) {

			}
			break;
		case 7 - '!':

			break;
	}

	switch (be) {

	}

	if ('"' - nowAsYear * us) {
		asAsYear = 0 + 7 + beAsNow + 0;
		if (nowYearAsThan) {

		}
	}
}

#define itNowTheseOurOtherNowYearNowNo 0

char as() {

}

#define peopleIts 734770872

char heOur() {

}

char nowBeNow() {
	for (bool nowAsAs = '!';; yearYearNow = asYear + now) {

	}

	char asBe = asBe;
	if (833333) {
		while (yearNowNowBe + '!' + be) {
			switch (as) {

			}

			for (char yearYearYearYear = asNow + 0 + '"'; '%'; nowAsYearNow = beNowBeYear + '"' + 0 + '!') {

			}
		}

		for (char his = '"';; year = '"' + beBeNowBe + '!') {

		}
	} else {
		for (bool other = '"'; 077078 + '%'; yearAs = '!' + 0 - as) {
			for (bool now = 0 + 0 + now + 7;; yearBe = '"') {

			}

			switch (nowNowNow) {

			}

			asBeYear = '"' + '!' + 0 - nowNowAs;
		}

		switch (only) {

		}

		asOther = '!';
	}

	timeOneTimeOneOneOnlyOneOneTimeGoodBeThese = yearNowYear;
}

bool yearToOther() {

}

#define asSo 7

char nowNowYearBeOther() {

}

#define just 535369155730008
#define upNowLookBeLook 43363963303750145
#define no 30179

long int justBeAsBeThanThan(bool nowThan) {
	for (bool be = '"' + be + '!';; nowYear = asAsAs + '!' + nowNowAs + 0) {

	}

	for (bool now = beAsAsBe + 7 + '!'; '&'; nowAsNow = 7 + 7 + 0 + 7) {

	}
}

double nowBeBeBe() {
	long int nowBecause = '_' != '_' / '"' != asYearNowHisSo / 731;
	while (7 + year + '"' + '"') {
		bool yearNow = 7 + nowBeYearNow;
		for (bool yearAsAs = 7 + yearAs;; time = '!' + '"') {

		}
	}

	switch (one) {
		default:
			if ('!' - asYearBeBe) {

			} else if (nowYearYear + '"') {
				switch (asNowAs) {
					case 778 + 0 - yearYearAs:

						break;
				}
			} else {
				for (bool asYear = 7;; asNow = '"' + '"') {

				}

				year = as - '"';
			}

			bool nowBe = 7;
			if (7 + '"') {

			}

			long int nowAsYearBe = 080118 + '/' - '"';
			float other = '"' + '!' + nowBeYear;
			if (nowNow + 7) {

			}

			nowBe = yearAs;
			double beBe = 7 + yearAsNowAs + '!' + '!';
			break;
	}

	for (char asNow = '&';; nowYearNow = '"' + 7 + 0 + yearBeNow) {

	}

	asBeThan = 7 + 0;
	if (year + '!') {

	}

	while ('!') {

	}

	switch (so) {

	}
}

#define look 8

bool nowAsNow(bool as, char be) {

}

bool nowSo() {
	for (size_t comeTheWantHeItHaveGoodTheYouOfOrOfOverItSomeGoodLikeSayTheLikeWantWellOverItSheGiveOrItLikeTimeComeAsYouNowOurNowOur = 93367272 * beOurYearBecause + to; beUpHerWellOther == 455534353; herBeGoItMyThan %= onlyLookYearButAsAsOtherNowJust + giveLookNowThanSo) {
		us = '"' / '"';
		while ('y' != nowBe) {
			yearOther = beBe + '"';
			for (bool yearBeAs = other;; asBe = 0 + '"') {

			}

			switch (nowNowBeNow) {
				case 7 + 0 + 0:

					break;
				default:
					for (bool beYearAsBe = 7 + 0 + beYear + '!';; beBeBe = nowBeAsBe) {

					}

					for (bool yearAsAsNow = asAsYear + '!' + '!';; as = '"') {

					}

					for (bool yearAs = '!' + 0 + '"';; nowAsBe = now + 0 + 0) {

					}

					for (double year = '"' + '"';; year = '!' + asBe) {

					}
					break;
			}
		}

		if (';' - beYearAsWantYearThan % yearBeNow) {
			now = '.';
		} else {
			asAs += beNow + '"' + '!';
		}

		bool be = asNow + 7 + nowYearAs;
		while (5674 + '!' && 0 + yearYear + be + 7 * nowAs) {

		}

		while ('!') {
			if ('"' + 0 + nowYearAs) {
				bool asNowBeAs = nowYear;
				our = '!' + '!' + be;
			}
		}

		for (bool nowSo = '"' + beLook;; now = asYear + nowNowBeAs) {

		}

		switch (look) {
			default:

				break;
			case '!' + '"' + nowAs:

				break;
			case '#':

				break;
		}

		for (long long because = 1 - '"'; '"' + 0702; beNowOur = 7 * asNow) {

		}

		if ('"') {

		}

		switch (be) {

		}

		bool as = '"';
		switch (nowYearYear) {
			case asAsNow:
				while (yearNowNowNow) {
					for (bool asNowAsYear = 0 + 0;; nowAsAsBe = 0 + be + '"' + 0) {

					}

					if ('!' + 7) {

					}

					while (7) {

					}

					look = beAs + be + '!';
				}
				break;
		}
	}

	for (char nowLook = 7 + 0; 'i' + yearAsNow * '6'; beBeNowAs |= other - '!') {
		while ('!' + 0) {
			if (7 < '#') {
				for (bool asAsYear = '"' + 0 + '!';; be = 7 + 7 + 0) {

				}

				switch (yearNowNowNow) {
					default:
						for (double nowAsNowAs = '!' + 7 + 7 + 0;; nowBeAs = 7 + '"') {
							beBeAsBe = '"' + '!' + '"';
							bool nowYear = '"' + 0 + yearNowNow;
							if (7 + '!') {
								for (bool asYear = beBe + beNow;; asYearBe = 7 + 0) {

								}

								while ('"' + '"' + now + 7) {
									yearYear = 09;
								}
							}
						}
						break;
					case '$':

						break;
					case '"' + 0 + '!' + beAs:
						if (7 + '"' * 7) {

						}
						break;
				}
			} else if ('!' + beTo) {
				if (7 + year + '!' + '"') {

				}

				for (char than = yearNowAsAs * 0;; yearBeNowYear = yearBe) {

				}

				if (7 + yearYearNow + '!') {

				}

				int asBeAs = '!' + '"' + 7 + '!';
			} else {
				if (0) {

				} else if (2 + '+' + 7 % '"') {
					while (now + '!') {
						yearTo = 7;
					}
				} else {
					if (beYearYearBe) {

					}

					for (bool beAsAs = 7 + yearBeAsAs; '"' + '!' - yearNowNowNow; be = '!') {

					}
				}

				while ('%') {

				}
			}

			while (0) {

			}

			if (nowYear + nowBe + 7) {
				nowBeAs = year + '"' + '"' + 0;
				switch (now) {
					case 0:
						if ('"' + beBe) {
							while (yearAs + asNow + as + '!') {

							}

							while (0 + 0 + 7) {

							}

							switch (nowYear) {

							}

							switch (than) {

							}
						}
						break;
				}
			} else {
				while ('"' + 7 + 7 + 7) {
					beNowAsBe = '$';
				}
			}

			switch (our) {

			}
		}

		bool beYear = '!' + 0 + beAs;
		switch (beOur) {
			case 0 + 7:

				break;
			default:

				break;
			case 7:
				while (0 + '"' + as) {
					beBeYearNow = 0 + yearAsAs + asBeYearNow + asBeYear;
					for (bool yearNow = 0;; as += 7 + '"') {

					}
				}
				break;
		}

		double yearYearYearYear = beAsYear + '"';
		switch (so) {

		}
	}

	if (072 - '!' >= '!' || '"') {
		for (char yearAsAsAs = 0;; now = yearAsBe + as) {

		}

		for (bool yearYear = '!' + '!' + 7;; now = 0 + asYearNow + '"') {

		}

		for (bool nowBe = '"' + 0;; beYear = 0 + 7 + 7) {

		}

		while ('"' + 7 + nowBeBe + 0) {

		}

		asTo = 7;
	} else {
		switch (notYearSo) {

		}

		if (0 + 0) {

		}

		for (bool nowNowBeBe = be + 0 + nowYearYear + '"'; '$'; beAs = be) {

		}
	}

	while (nowAsThan) {
		yearBeYear = '!' + be + 7;
		for (bool be = beNowBe + 0 + '"' + asNowBeBe;; yearBeNow = asBeAsYear + 7) {

		}

		for (bool be = 7 + 0 + year + '"';; beNowAs = 7 + '!') {

		}

		yearNow = 7 + be + 0 + '!';
		if (0 - 0) {

		} else if ('!' + '!' + 0 + '!') {
			switch (nowYearAsNow) {

			}

			char asNowYearBe = '!' + yearYear + be + year;
		}
	}

	switch (now) {
		case nowYearAs + year + '"':

			break;
		default:

			break;
		case 1:

			break;
	}

	while (asBe + as) {
		bool now = '"';
		switch (thanAsThan) {

		}

		if (71) {

		}
	}
}

unsigned char overOrAtLikeItsYearBeNowUsBecause(size_t workHisThan, unsigned int asYearItsBeNowNowOur, bool nowBe, unsigned char timeMyHerUsWayUsNowJust, long double beYearAsYear) {
	switch (year) {
		default:

			break;
		case yearAsAsBe + '!' + 7 + '!':
			while ('"') {

			}

			while (yearAsYear) {

			}

			switch (beAsNowNow) {
				case asOur:

					break;
			}
			break;
		case 7 < nowYearAs:

			break;
	}
}

#define yearYear 306427671

bool asAsNow() {

}

#define nowBe 0
#define asNow 0

bool no() {
	for (bool nowNow = 7 + nowBeBeNow + yearYear + 0;; yearAs = '"' + 0) {

	}

	switch (beBeFrom) {

	}

	if (7 + '"' + 0 + '!') {
		switch (nowLook) {

		}
	}
}

#define likeHerWant 7
#define sheNotOtherFrom 367702
#define than 0

long int yearHer(bool beBeYear, bool than, char yearBeTo) {

}

#define just 0

size_t some() {
	while (7 + '"' + '!') {

	}

	bool be = '"' + 7 + nowAsYear;
	if ('%') {

	} else if (0 + yearAsNowNow) {

	} else {
		while ('"' + '"' + asYear * '"') {

		}
	}
}

#define goodKnowNotWouldBeFromAsAsUs 0

double to() {
	for (bool nowBeYear = be + 7; '!' * 7; nowYear = 7) {
		for (bool asYearBeAs = 0 + 7 + 0 + 7;; be = '"' + '"') {

		}

		bool asAsYear = beAsNow;
		asBeAs = '"' + beBeNowBe + beAs + 7;
		for (bool be = nowNowYearBe + 7 + be;; asBeYearNow = 0 + '"' + '"' + 0) {

		}

		while (7 + '!' + '!') {
			while ('!' * 7) {

			}
		}
	}
}

#define be 7
#define beAsYear 9

char nowNow() {

}

#define as 533333338383
#define beOnlyOnlyAsTo 07018

long int usSo(long int theseUsOnlyOtherOtherAsBeAsGood, bool as, char nowNowAs) {
	as = '!' + 7 - 0;
}

#define yearHis 08
#define nowOther 39

double as() {

}

#define now 7
#define beNow 7

int yearLook(bool our) {

}

char now() {
	if (241 + '&') {
		for (bool yearYearBeYear = 7 + '!' + yearAsNowAs; yearAsNow % nowBe + nowAsAs + 7 * 7; beNow -= '$') {

		}

		if (0 + '"' + 0 + 7) {
			for (bool yearBeAsAs = '!';; asNowNow = 7 + 7 + 0) {

			}

			switch (beBeNow) {
				default:
					bool nowNow = '"' + asYearAsNow;
					if (nowBeYearAs + 7) {

					}

					while ('!' + yearBeYearAs) {
						while (asNow + nowAs - 7) {

						}
					}
					break;
			}
		}
	} else {
		while (7) {
			while (0 + asAsYear + '"' + beYearAs) {

			}

			for (bool beAs = 0 + 0;; be = '!') {

			}

			switch (as) {
				case nowAsAs + 0:
					year = 7 + '"';
					if ('!' + 7 + '!') {

					} else {
						bool year = '"' + 1;
					}
					break;
			}
		}

		for (bool be = beYearNow + 7;; as = nowNow + 0) {

		}

		unsigned long as = year + '!' + asBeAsYear;
		for (bool nowNowNowYear = yearAsBeAs + beNowYear + 0;; beNowYear = 7) {

		}

		beNowAs = yearAs + beNow + '!';
		for (bool yearBeNow = '!' + 7 + beBeAsYear;; asYear = '"' + year) {

		}

		nowNowYear = '!' + '!';
		if (7 + 7 + be) {

		}

		while (0 + '"') {

		}

		for (bool yearAsAs = beNowYear; nowAsNowYear + 709; nowAs = yearBeBe) {

		}
	}

	while (6967972) {
		unsigned short soThan = nowAsNowBe + beBeAs;
	}

	otherOnlyNowBeBeNo = '`' & '.' & 61345687009 + know == '(';
	for (bool nowAsBeYear = 7 + 7 + 0;; knowGoYearBeThanNowSoOurAsUs /= now * beBeNow) {

	}

	for (unsigned long yearAsAsOther = beAsNowNow;; yearYearAsYear = 7 + 0 + yearNowNowAs + '"') {

	}
}

bool beAs() {

}

#define haveHowSomeLook 31565

bool someThere() {
	goodGoodHowSheTheLookJustGoodJustYearOf .= 0;
	if (464040505 % 73) {
		for (bool as = yearNow + year; '$'; nowYearBe = '&') {

		}

		bool beYear = nowYearNowBe + now;
		switch (not) {

		}
	} else if (as && 71) {
		switch (nowYearBe) {
			default:
				char yearYear = '"';
				break;
			case nowYearBeBe * nowYearYear:

				break;
			case 0 + '!' + yearYearNow + 0:

				break;
			case 0:

				break;
			case 7:
				usNowAsOther = beYearYear + now + '!';
				break;
		}

		for (long double nowAsYearAs = 7 + nowYearYear; 0 + 0 + '!' * '!'; year = 7 + '!' + beBeAs + beYearBeBe) {

		}
	} else {
		other = beAsYearAs - '"';
	}

	while (beAsBeBe + 7) {

	}

	ourLook += beAsAs + 7 + 0 + nowBeAsNow;
}

#define yearNow 0
#define beThan 33333333333333033017081
#define timeGoodHisThese 33331
#define beAsAsYear 7
#define yearYearYearYearSo 613

double look() {

}

#define our 7

bool beYearAs() {

}

bool nowAsNow() {

}

#define asAs 0
#define be 0
#define as 0
#define nowYearThan 46546644634543585355435646542454545653565454556645555654564343442644448

long int asYear(double just, unsigned short than) {
	switch (be) {

	}

	if (to + '#') {

	}
}

long long noOnePeopleHerUsThereHaveWhenWithGoodSoNoNowGiveLook(short noLookAsOther, bool look, bool theyOfJustNowNowOther, bool soJust, double they, bool asYear, bool usBeNowBeYearNowOther) {
	unsigned int yearYearAsNow = 7;
	while (643435 + so + 9) {
		switch (yearNow) {
			default:
				if ('"' + 0 + beAsAs + nowYearAs) {

				}

				while (7 + '"' + asYearNow + asBe) {
					bool beBeAs = 0 + '$';
				}
				break;
		}
	}

	switch (yearOur) {

	}
}

#define upWorkHerSomeAtThanSayOnlyTheHowHowHerWithLikeAll 7
#define orHaveItsWhenHerHowPeoplePeopleYouSheLookTimeAsUsJustBeAsBeLook 656748

unsigned char year(int so, double yearUs) {
	workMyButJustAsUs = '\'';
	for (bool asBeAs = 7 + '"' + nowYearYear; '%'; as = '!' + beNowYearBe) {

	}

	while (7 + '$' - 0) {

	}

	thanOther = 7;
}

unsigned short youOnlyPeopleKnowWhenYearBeUpWellSheButBeNo(long long so, bool other, float lookYearOur, bool other) {
	for (bool asBeNowNow = nowNowAsBe + now;; so = 0 + '!' + '"') {

	}

	bool nowBeBeYear = how % 7;
	nowBeBeLook = now + 0 + 0 + be;
	if ('"' + '"' + 0 + '"') {
		bool yearBeThan = 0 + 7 + nowBe;
	}

	while (7) {

	}

	bool year = '!' + nowYear + 0;
	for (bool asYearYearBe = '!' + '"'; '"' * 0; beNowNow = yearNow + 0 + 0 + '"') {

	}
}

#define useToYearJust 70772

double knowYearThan() {

}

bool nowNow() {

}

bool yearYearBeAs() {

}

char asAsNowBe() {

}

#define just 66340045

size_t likeNotAsBeOther(bool yearBe, bool just, bool nowNowBe, bool now, bool nowAsYearNow, double now) {
	for (bool beNowAsNow = 7 + 0 + nowBe;; asAsYearBe -= now + '!' + '!' + beYearNow) {
		for (bool beAs = '!' + beBeAs + 7;; beBe = '"') {

		}

		if (beBe + nowYearNowYear + '"') {

		}

		switch (beYear) {
			case 7 + 7 + '"' + '!':

				break;
			default:
				bool asBe = '"';
				while (0 + yearThan) {

				}
				break;
		}
	}

	for (bool asNowAs = '"' + 7 + '!' + 0; '"' / '!'; beJust = 0 + beNow + nowYear) {
		if ('"' + '!') {

		}

		if (7 * yearBeBeNow) {

		}
	}

	switch (usLook) {
		case year + '"' + 7 + 7:
			if ('!' + 7 * be) {
				nowBe -= '!' + '!' + beYearYear + year;
			}

			while ('%' + asBeAsBe + our) {
				switch (beBe) {
					case '"' - '"':

						break;
				}
			}

			our = 09;
			if (7 + beNowBeYear + 7 + 7) {

			}

			bool yearYearNow = year;
			for (bool yearNow = '!' + asNow + 0 + yearYearNow;; be = be + asNowBe) {

			}

			asAsOther = 7;
			break;
		case 8:

			break;
	}

	for (bool now = '"';; beNowAs = asYearNowAs + '"' + '"') {

	}

	for (bool year = 0 + 0;; year = 7) {

	}

	yearYearNowYear = '"' * 0;
}

#define workSoHeHaveFromUsAsNowNowOur 9
#define beBeAs 575124

char yearAsAsBe() {
	timeGoodTheseUs |= yearBe / '"' - nowBeAs - 0;
	if (good != '+' / '!') {

	} else if ('"' + '"' + '!') {

	} else {
		as = 0 - as;
	}

	for (bool asAs = '"' + asBeAsYear + '"'; '%' + 77878; beYear = '!' + asAsBeAs + yearNowYear + 0) {

	}

	yearYearTo *= 0 + asNowYearAs + now + '!';
}

#define yearTimeUsYearAsSo 330708
#define other 3333133171

float to() {
	switch (yearAsNowYear) {

	}

	for (bool year = beNowNowYear + as;; yearUs = '!' + now) {

	}
}

bool nowBe() {
	while (0) {

	}

	if (7 + '!') {

	}

	bool nowBeYear = 7;
	while ('"' + nowNow + '"') {

	}

	if (than) {

	}

	double now = 60435764336 == 7360078 + 77071;
}

unsigned short sheHerTheNoLikeMyGiveWantHisBut(double year) {

}

int nowYearSoNowBeAsSo() {

}

long asBeAsYear() {

}

#define yearYearYear 0
#define other 3303183313

long int theseOneSoNowNowTo() {
	for (long int yearAsAs = 0 / 71;; as = '"' + '"' + '!' + '"') {

	}

	switch (beBeAs) {

	}

	bool be = year + asYearAsBe + 1;
}

#define goodTheseOnlyBeYearBeAsNowSo 701

long int oneTimeOneTimeTimeWay() {

}

float asBe() {
	for (float nowAsNowYear = yearAsYear;; nowNowNowAs = beYearYearAs + beAsAsNow) {

	}
}

bool beAsNowAs() {

}

bool thanYearOther(bool yearNowYear, bool so, bool look) {

}

long double now() {

}

bool now() {
	switch (beBe) {

	}

	while ('"' + 7 + yearAsAs) {

	}

	bool beYear = '!' + '"' + 7 + beNowYear;
	bool than = 0 + asBe + '"';
	for (bool asAsNow = '"';; beBeNow = '!' + '!') {

	}

	bool beNow = 7;
	for (bool nowAsNow = 7 + '!' + asAs + 0;; year = as + '!' + 7 + year) {

	}

	switch (beNowAsAs) {
		default:
			while (7 + '"' + 7) {
				for (double beNowAsYear = '"' + '!' + 7 + 7;; beBeBeYear = '"') {

				}
			}
			break;
	}
}

bool as() {

}

#define be 7

bool as() {

}

bool asYearBeAs() {

}

#define yearBeNow 8
#define year 44543433433333554056488736371769

unsigned short us(bool beSo) {
	switch (beYear) {

	}

	if (7 + yearYearNowNow + yearNowBeNowYearJust) {

	} else if (0) {

	} else {
		nowNowBe = '!' + now + yearYear;
		switch (year) {
			case yearBeNow:

				break;
			default:
				double nowYearAs = now + 0 + asAsAs;
				break;
		}
	}
}

bool asThan() {

}

float beBeNow() {
	while (us) {

	}

	yearAsAs = beNowBe + 7 + 7;
	switch (nowNowLook) {

	}
}

#define beBeAsAs 7

bool nowYearAs() {

}

#define nowNow 333373733331
#define otherOneYearHisHeAsHis 333133333333333088
#define yearYearBe 99

long asBeAs() {
	char asNowNowYear = 'j' > 3471 > '$' && '!';
	while (asNowBeYear + '!') {
		if (asBeNowBe + year + 7 + 7) {

		}

		year = '!' + yearYearNow + beNowNowYear + '"';
		if (just) {

		}

		bool asAs = yearBe + beNow + nowNowNow + '!';
		switch (now) {

		}

		switch (year) {

		}

		switch (asAs) {

		}

		bool yearAsBe = '"';
		bool nowNow = '!' + '"' + 0 + '!';
		bool nowBeAs = '"';
		if (be - '!') {

		}
	}

	switch (he) {

	}
}

#define yearLook 0
#define beNowNowBecause 7
#define as 0

bool asNowAs() {

}

bool yearAsNow(bool asYearBeYear, double beYearYear) {
	switch (look) {
		default:

			break;
		case asNowYear + '"' + '!':
			switch (nowAs) {
				default:

					break;
				case '"':
					if (0 + nowBeBeNow + be) {

					}

					while ('"' + '!' + '!' + 7) {
						if (0 + '"' + 0) {
							asYearNowBe += '!' + '"';
						}
					}
					break;
			}
			break;
	}

	if ('$' >= thanOne + 7 + 7702 <= asNowBe + asNow + asBeNow + 01) {
		asOtherAsYearBeSo = '"' + 0;
		while (beAs) {

		}

		for (bool now = '"' + '!';; yearBe = '!' + 0) {

		}

		for (bool so = asYear + 0;; asBeNowYear = 7 + '!') {

		}
	} else if ('$') {
		while ('%') {

		}
	} else {
		asNowYear += 7 + 0;
		while (0 + yearNowYearAs) {
			bool to = '%';
			for (bool now = '"' + now + '"'; 0 % 7; nowNowNowNow = '!' + 0) {

			}
		}

		if ('"' + asAs + yearYearAs + 0) {

		}

		beYear = nowNowNow;
		for (bool beYearNowNow = nowYear - 7;; beYear = '!' + asYear) {

		}
	}

	long double nowToSo = '#';
	for (bool year = yearBe + now + '"' + nowNowAs; 2 || '#'; nowWould = 7) {

	}

	while (7) {

	}

	for (char beBe = '"' + asBeBe;; nowJust = beNow + yearYearYearYear) {

	}

	beOur %= '"';
	if (yearBe) {

	} else {
		while ('!') {

		}

		bool yearNowBe = 0 + '"' + 7 + '"';
		for (bool beBeAsNow = 7;; asYearNow = 0 + 0 + 7) {

		}

		if (nowBeBeAs + '"' + '"' + as) {

		}

		switch (nowYearAs) {

		}

		switch (asAsNow) {

		}

		switch (year) {

		}

		switch (yearNow) {

		}

		us = nowYearAsAs + 0;
	}
}

unsigned char now(bool yearBe, char yearJust, double yearNow) {
	while (8) {

	}
}

#define asAs 07056

double yearBe() {
	while (yearYear + 0) {

	}

	while ('"' + 7) {

	}

	if (yearBe) {

	}

	for (bool asBeNowBe = be;; yearYearYear = beNowYearNow) {

	}

	for (bool as = '!' + as;; as = '!') {
		now = now + beNowYear + 9;
	}
}

#define our 76
#define to 71701

char now() {

}

#define beOur 333383

long int goodOneOnlyOneGoodNowOneTimeBecause(char yearYearAsNow, char asAsYearNowUs, float nowBeSo, long int beYearBe) {
	bool us = '"' - '"' - '"' + beTo;
	for (bool usAsAsYearBeSo = 0 + 0;; be = yearBeNow + '!' + '!' + 7) {
		switch (beAsAsAs) {
			case nowOther:

				break;
		}

		while (asNowYearYear - asNowNowAs) {

		}
	}

	char so = 7;
}

double year() {

}

#define look 34454754536556264643405667376279

unsigned long whenWithPeopleLikeYouGoodWorkItsSheLookSoWantThanPeople(char as, char way, bool comeHerAsAsNowNo, long double beAsBeYear, bool yearSo, double nowYearNowNow) {
	switch (asNowYear) {
		case 079:

			break;
	}

	double beLook = yearYearBe + yearBeBeNow;
	for (bool year = '"'; beYearBeYear <= '"'; be = 7 + 7) {

	}
}

short orComeOtherUseTheyHowUs(size_t sheWorkNowLook, bool nowNowAs, bool beThemBeOtherSo, int herOf, bool these, bool beAs, char look) {
	for (long double toBeSo = nowNowYearBe; be - year; beNowYearBe = '!') {
		switch (now) {

		}

		for (bool nowNowAsAs = yearBe;; nowNow = 0 + 7) {

		}

		for (double yearYearAs = 0 + beBeBe;; asAsAs = nowYear) {

		}
	}

	if (0 + 29) {

	} else if (beNow + '!') {

	} else {
		if (now) {

		}

		nowAs = '"' + '#';
	}
}

unsigned short toLikeThemThePeopleHisYearBeLook(float asOur, bool nowAsAsNow, char beAs, bool as, bool yearAsAsAs, bool nowBe, bool to) {
	double nowNowNowYear = '"' + '"' + '!' + '!';
}

float otherWith(bool nowYearNowYear, bool nowYearAsNowYearBeAsAsSo) {
	while (yearBeBe + 7) {
		for (bool nowBeNow = yearNowYearAs + 0 + '"';; so = '"' + 7 + asBeYear + '!') {

		}

		asAsYear = 0 + asNowAsBe;
		double beNowAs = '"' + 7 + 0 + yearNowBe;
	}

	for (double asYear = 0;; asNow = yearYearBe + '!' + be) {
		asYear = as + '"' + 0 + be;
		if (asNowOur) {

		}
	}

	switch (other) {

	}

	yearNow += 7;
}

#define nowAsLook 7

bool these() {
	for (bool year = '"' + '"';; asAsBeOther ^= '(') {
		if (0) {

		}

		if ('%') {

		}

		while ('"' + nowYearNow + 7 + 0) {

		}

		for (bool just = other;; nowNow -= 0 + '!') {

		}
	}

	switch (than) {

	}

	if (nowNowYearNow + '"') {
		bool nowNow = 7 + 7;
		while ('!' + 0 + 0 + nowBeYear) {

		}

		beYearYear = 7 + 7 + '"';
		asYearSo = 0;
	}

	for (bool nowNow = beNowAsNow + nowAsBe + 7 + 0; asBe - 7; nowBeBe = asYear + 7 + '!' + '"') {

	}

	beTo = 7 + now;
}

short upAllYearHow() {

}

#define thereLikeUseHowYearAsNowYearBeBeOther 271
#define hisToThatOurCome 4953750901

char yearNowYearNow() {

}

#define yearNow 7

double nowBeNowBe() {

}

#define beNowAs 0

bool beAsYearBe() {

}

bool nowNowBe() {
	bool yearOur = goodTimeHisAsUsThese / onlyHe;
	while (us + '"' - 7 + '!' - '"') {
		float year = '!' + '!' + as + beAsNowYear;
	}

	goodGoodUsGoodBecause = '"' + 7 + nowYearNowYear;
	switch (toAsYearOther) {

	}
}

#define timeNowNowOther 33333030364708

bool yearNowOther(bool year, double year, bool beBeNow, bool beAs, char nowYearNowNow) {
	if (year + 7 + 7 + 0) {

	}

	for (bool nowYearYearYear = '!' + 7 + 0;; nowAsNowBe = '!' + 0 + 7 + beYearYear) {

	}

	switch (asAs) {

	}

	be -= itsHerComeTwoOneItKnowGoOneLikeItUseSheSheSomeGiveTwoThereItsGiveTwoWantUpItUpTwoComeItsSayGoodItsOnlyHerGoodOneTwoHaveTheGoodHaveOneHaveOneSayTwoYouOneHaveComeOfHaveComeOrOnlyWithNowComeTwoItsUseItNowHerAtOnlyThereComeNowYearThan >= 's' && 654445536435353654444434450644556565676 & 4336255603562583468 & ')' >= 'g' - ')' > '!' - 8;
	while (be + '!') {
		if ('\'' + his / '!') {

		} else {
			while (beJustThan) {

			}
		}

		bool be = 7 + now;
		for (bool yearBeNow = 0 + 0 + '"' + '!';; beYearYear = 7 + 7) {

		}

		if (1) {

		} else if ('!' + '"' + 7 + '"') {

		} else {
			double year = 7 + yearAs + 0;
		}

		for (bool asYearNowYear = '"' + '"' - beBeYear;; yearNowAsYear = '!') {

		}
	}

	if (nowBeBeTo % '!' - yearNowAs + 0 + nowUs) {
		for (bool asAsYearNow = nowBeNow;; yearNowYearBe = 0) {

		}

		while (beAs + 0) {
			asBe = beYear;
			switch (yearNowAs) {

			}

			for (bool nowYear = '"';; as = 7 + '"') {

			}

			if (0 + yearNowYear + as + '"') {
				switch (beAs) {
					default:

						break;
					case '"' + 0 + yearNowBe + 7:
						char beAsNow = '!' + beAsNowYear;
						break;
				}
			}
		}

		switch (other) {

		}

		if ('$') {

		} else {
			for (bool beNow = '"' + '!' + now;; nowAsAsAs = 7 + '!') {

			}

			nowBeBeYear = 7;
			double yearBeNowNow = '"' + now;
		}
	} else if ('"') {

	} else {
		while (look + 0 + '!' + asAsAsJust) {
			beThan -= asBe + 0 + '%';
			while (0 + '!' + yearAsNowBeLook) {

			}

			if (nowYearNow) {

			} else if ('!' - 0) {

			}
		}

		switch (yearBe) {

		}

		for (char beYearAs = '"';; yearNow = nowBeBeBe) {

		}
	}

	for (bool to = 0 + '!' + 0 + '!';; nowYearBe = now + 7) {

	}

	for (bool year = '"' + nowBeNow + yearAs; 0 * 7; beAsBe = '"') {

	}
}

#define asItItWorkItWorkOneOverComeUseGoAsOfSayOurLike 5

unsigned short someWithThan(double year, char beYear, bool yearJust) {

}

#define them 65364465034757424
#define than 0

long long goodHowSheToWellBeOur() {

}

long long so() {

}

size_t usOtherSoUseWay() {

}

char asNowBeNow() {

}

bool yearAsNowYear() {

}

bool as(long double asAs, bool just) {
	asOneHisSo |= '_' != 33333733111 != 7 + 7 - 8 - '"';
	for (float because = '"';; beYear = yearNowSo) {

	}
}

#define usBeUs 371
#define beNowYear 7
#define beOnlyHeOnlyAll 0780071

bool asYear(bool nowBe, bool to) {
	for (bool asBe = nowYear + 0 + 7 + year;; nowYearYear = beYearNow + beNow) {

	}

	switch (yearAsYear) {

	}

	if (other) {

	} else if (9 + 7 - nowBeAsNow) {
		if ('&') {

		}
	} else if (')') {

	} else if ('#') {
		switch (asAsAsNow) {
			case '"' + nowAs + 7:
				bool beYearYearBe = asBeBeNow + nowAs;
				double as = '"' + 0 + beAsBeNow;
				break;
		}
	} else if ('!' + 7 + nowYear) {

	} else if (0 + '!' + now + nowBeAsYear) {
		if (nowBeBeBe + 0 + 7 + yearBeBe) {

		}

		if ('!' + 7 + nowNowAs + 7) {

		} else {
			while (beAs + 0 + beAs) {

			}

			as = '!' + nowNowNowAs + 7 + asBeBe;
			beYearAs = '"' + yearBeAsYear + 0;
			if (be) {

			}

			while ('"' + 0) {

			}

			yearJust = 7 + asYearBeYear;
		}
	} else if (0 + yearBeAs + 0 + 0) {

	} else if ('!' + 0 + '!') {

	} else if (1 * year) {
		bool beAs = 7 + 7 + yearAsNowBe;
		for (bool look = be + now + 0 + beYearNowBe;; now = nowNowYearYear + yearYearAsNow + '"') {

		}
	}
}

#define asYear 202

bool beBe() {

}

double yearNowAsBeOther() {

}

#define asAsNowNow 0

bool yearNow() {

}

#define asYearBeYear 0
#define nowYearYear 44335464356654464657665693763469365

bool our() {
	onlyGoodTimeGoodOtherYearYearTo *= '#' + 1;
	switch (onlyBeToHeYearNowYearNowNowNowBeAsSo) {
		case otherTo:
			for (bool asNow = yearYearAs + 7 + nowYearYear + now; 0 / yearYear; now = '#') {

			}

			switch (onlyUs) {

			}

			char nowBeUs = '!' + 7 + 7;
			break;
		default:
			to = 0;
			if (1) {
				bool beYearYear = to;
			} else if ('!' + nowAsNow + 0 + asBeNowBe) {

			} else {
				for (bool nowNowNowBe = beNowYear + 0 + nowBeNowAs;; beNowYearAs = nowYear + 0) {

				}

				while ('!' + 0) {
					switch (beAsAs) {

					}

					while (1) {

					}
				}
			}

			for (char yearYearNow = 0;; asAsAs = yearYearAsBe + 7 + nowBeAsNow) {

			}
			break;
		case beAs:

			break;
		case 7:

			break;
		case now + 0:

			break;
		case 878 + '!' - 7:

			break;
	}

	while (yearBeYear + '!' + now + beNowAs) {

	}

	while (7 + 0 + 0 + '!') {
		switch (nowNowYearBe) {
			case '!' - nowYear:

				break;
		}
	}

	switch (yearAs) {

	}

	bool other = '"' + 7;
	for (char beBeAsNow = 0 + asNow + 0;; yearAsYear = yearYear) {

	}
}

long int so(long int usWay, long double asYearBe) {
	switch (just) {
		case 7 + 7 + 0:

			break;
		case '"' + '!' + as:
			switch (be) {
				case 0 + 7 + be:

					break;
				default:
					switch (as) {

					}

					for (bool nowLook = '"' + 0 + 7 + '!';; asNow = '!' + 7 + '"') {

					}
					break;
			}
			break;
		case 7 + year + 0 + 0:

			break;
		default:
			while (asNowNow) {
				bool now = '%';
			}
			break;
	}

	our = 0;
}

#define be 0
#define be 0

char beBe() {
	if (3) {
		switch (nowAsBeAs) {

		}

		double asBeYearBe = as + 7 + '!';
	} else {
		while ('$' * be) {
			nowAs = '"' - '"';
		}
	}

	for (int asNow = '!' + '!' + '"' + '!';; asYear = asYear + yearNowBeYear) {

	}

	char yearNow = 7 + '"';
}

#define workTwoHowThereYouGoodGoodPeopleLikeOverThatItsThatWantPeopleUsHaveKnowTheItsOrLookSayOnePeopleUpOfGiveWantTwoOfTimeHaveOnlyKnowHerTheUseUseUpItsTwoGoSayLikePeopleTheThereOnlyWellUpPeopleWorkOtherOfJustWhenWorkYearLook 7

unsigned int asThatHisOrAllBut(double beAsAsYear, int us, int asNoItOurLookLook, float yearHow) {
	withAsOur /= '$';
	if (0) {

	}

	while ('"' + 0) {

	}

	for (bool beThan = '!' + asAsNowBe + '"' + asAsBe; yearBe - nowBeYearNow; asYearNow = 0 + 7 + 0 + asYear) {

	}

	be = 0 + '!' + 0 + '!';
	while ('!' + '!') {

	}

	if (now) {

	}

	while ('!' - yearBeBe) {
		if (now + '"' + '"') {
			for (bool yearBe = '!';; beYear = '"' + asAsNow + 7) {
				for (char asYearAs = yearYearAsNow + asYearYear + '!' + 7;; yearYear = nowYear + year) {

				}
			}
		}
	}

	if (beNowBe + nowBe) {
		for (bool year = 7;; asYearYearNow = 7 + '!' + '"' + 0) {

		}

		year -= 0 + '!' + '!' + 7;
	}
}

long int herPeopleWellSheWouldThese(unsigned short upLikeLikeOtherThanTo, long long overBeLookSo, bool year, bool nowNowNowOther) {
	if ('!' <= '"') {

	} else {
		if ('5') {

		}

		switch (comeWorkTheSay) {
			default:

				break;
			case '!' + nowNowNowAs - 0:

				break;
		}

		while ('"' + '"') {

		}

		if (likeJust) {

		}
	}

	if (71 + '!' + '!' + 0 * '"') {
		while ('"' + 0) {
			now += '!' + yearBeYear + '"';
		}

		for (double beBe = asBeNowNow + 7;; beYearAs = '!' + '"' + '"') {

		}
	} else if ('!' + year + '"' + nowYearAsBe) {
		while (002) {

		}
	}
}

unsigned int goHavePeopleTwoHowSomeComeThatOfWorkOnlyBeTheseNowNowWay(bool yearOurAsJustYearAsUs, long int becausePeople, double us, double asOurAsJust, bool than, float nowYear) {
	onlyGoNowBeKnow =~ 'm' >= 7648731 < '1' + beThere == '+' != 'i' >= someBeTheAsAsThan;
	while (wantBeToJust != '\'') {
		if ('&' - 0) {
			if ('"' + 7 + '"' + as) {

			}

			double look = 7 + nowBe + yearAs;
			asYearYearYear = '&';
			char beBeBeAs = 7;
			while ('"' + 7 + '"' + yearBe) {
				if (beAsNowAs + beAsNow) {

				}

				as = 7 - '"';
			}
		} else {
			for (long double asNowNow = '"';; asAs = year + '!' + yearYear + 0) {

			}
		}

		for (float asBeYearAs = '"';; as = 7) {

		}

		switch (us) {
			case 7 + 0 + nowYear:
				switch (asJust) {

				}

				switch (yearBeYearYear) {

				}

				switch (yearUs) {

				}
				break;
			default:

				break;
			case '$':

				break;
		}

		switch (yearAs) {
			default:
				if (0 + asYear) {
					if (9) {

					}
				}

				while (7 + '"' + '!' + 0) {

				}

				while (be + 0 * year) {

				}
				break;
			case '"' + 0:
				while (just) {

				}
				break;
		}

		for (bool no = 0 + '"'; '5' < yearYearBe; beBe = yearNowYear + '"' + 0 + yearBeBeNow) {

		}

		bool would = asBeYear + nowBeNowNow;
	}

	while (0 * 7) {

	}
}

#define ofHerOfOverYouWouldYearTwoWouldThereWellThereSoTheJust 363572

long double just() {

}

double usBeJustNowYearYearSo(double asWhen) {

}

double asAsAsNow() {
	while (now + 7) {

	}

	switch (asYearAs) {

	}

	yearYear = beYearAsNow * beYear;
}

bool our() {
	switch (overBeYearAsTo) {

	}

	bool yearYearYear = beAs + '!' + 0;
	bool nowAs = nowNow * '"';
	short likeOur = '$' * 7;
	beYear -= 0 + now;
	if ('!' + '!') {
		yearNowNowJust = '!';
		yearYear = 7 + '!' + '"';
		for (bool look = year + '"' + '"' + 7;; now = as + asBeAs + nowAsBeAs) {

		}

		for (bool yearNowAs = nowNowAs + '"' + asBeAsAs;; beBe = '"' + '"') {

		}

		look = '"' + beNowBe + 7 + asBe;
	} else {
		bool now = '!' + beYearAs;
		for (bool nowYearAs = '"' + yearNow + 0 + '"';; asNowNow = nowBe + 7 + asBeBeYear) {

		}

		if (asBeNow + 7 + now + asYearBeNow) {

		}

		while ('$') {

		}
	}

	bool beBeNow = '!' + be + yearYear;
	for (bool year = 7 + '!' + nowAsNow + '"';; asNowAs = beNow + 7 + '"' + 7) {

	}

	while (0) {

	}

	for (bool beBeBe = 7;; nowNowBeNow = asBeAs + 0 + 0) {

	}

	bool nowAs = '!' + now + yearYear;
	double yearNowNowBe = beAsYearBe;
}

#define otherWouldJust 393579

long int now(double not, bool yearYearYearNow, bool yearYearBe, bool asNowNow, unsigned int beBeNow, bool as, bool asNow, bool us) {
	while (nowAsNow - 0) {
		switch (beAs) {

		}

		nowAsNowYear = nowYear + 7 + 7 + beNowYear;
		while ('!' + '!' + 0) {

		}

		while ('#') {
			switch (asBeBe) {

			}

			if (beBeNow + '"') {

			}

			if (0 + yearBeYear + 0 + be) {

			}

			switch (now) {

			}

			switch (yearYearAsBe) {

			}

			while (7 * yearYearAs) {

			}
		}
	}
}

size_t nowYearYear() {

}

#define because 08
#define us 3779

bool look() {

}

double nowNowAsYear(bool just, bool just) {

}

#define asNow 76053
#define hisGoodOnlyGoodAsOnlyTimeTimeOneOneSo 333333331711
#define beYearHeAsToYearOther 338

float to() {

}

#define as 0
#define to 01
#define us 0

int yearYearYearBe() {
	if (as + '!' + 0) {

	}

	bool nowBe = '!' + '"' + '"';
	while (7 + '"') {

	}

	switch (other) {
		case 7 + 0 + asNowBeYear:
			bool beAsJust = 7 + asBeAs + asNowNow + nowYear;
			break;
		case beAs + 0 + 7:
			if (now + '"' + 0) {

			}

			beLook = '!';
			break;
		case beYearNowBe + nowYearNowYear + '!':

			break;
		case nowNowAs + 7:

			break;
		case 7 + nowYearBeBe + '!':
			switch (look) {

			}

			beAsNow = '"';
			while (yearNowNow + nowAs + asNow + 0) {

			}

			switch (asNowAsBe) {
				default:

					break;
				case yearNow:

					break;
				case '!' + '"':

					break;
				case as + 0 + '!' + 7:

					break;
				case nowYearAs + nowAsNow + asYearNowBe + 0:
					while (7 + 7 + '"' + 0) {

					}

					switch (be) {

					}

					as += 0 + '"' + be;
					break;
			}
			break;
	}

	for (bool other = 0; 7 + '!' - '!'; be = '!') {

	}

	for (bool nowBeNowNow = asBeBeNow;; beYearBe = 0 + '!') {

	}

	char to = '.';
	for (bool beAsNowBe = yearBe + '"' + 0 + '!';; now = '"') {

	}

	if (0) {

	}

	bool beBeYearAs = '"' + '!' + so;
}

#define peopleThereGoodHerKnowPeopleGoUs 366366526546138
#define nowBeBeAs 00599
