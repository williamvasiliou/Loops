#include <6.h>

bool yearBeAs() {

}

#define onlyBecauseBeTo 33337371

long int onlyOneOneOnlyTimeAsOnlyOneBeNowBecause() {

}

long int timeOtherToOnlyNowAsTo(char asBe) {
	switch (nowAsBe) {

	}

	nowBeNowTo = '!' + 7 + 0;
}

long int be(bool now, bool nowBeNowAs, double yearYear) {

}

#define now 71
#define goodItHeAsNowJust 634

unsigned char overSheBeGoodWhenOverGiveLookComeSomeHisNowAsOurSoThan(short lookWay, char now) {
	yearBeAsYear = our;
	switch (beYearBe) {

	}

	for (bool yearNow = 0;; beAsNow = nowBeYear) {

	}

	nowNow = 7 + 0 + now;
	if ('!') {

	} else {
		if (beNowNow + 7 + 7 + '"') {

		} else {
			bool as = nowBe + beBeNowAs;
			yearYear = '"';
			switch (asYearYear) {
				default:

					break;
				case '!' + yearBeBeAs:

					break;
				case 7 + '"' + beYearNow + beYearAsAs:

					break;
				case beBeBe + 7 + 0:

					break;
				case '"' + '"' + '!':

					break;
				case beAs:

					break;
				case 9:

					break;
			}
		}
	}

	switch (yearOtherLook) {

	}
}

bool yearYear() {

}

bool beUs() {

}

#define asYear 7
#define our 0

bool beBeBeYear() {

}

bool asBe() {

}

#define yearUs 7
#define yearJust 3333333333337737013333

long int becauseTimeOnlySo(bool otherHeHisSoNowNowAsSo, bool yearNowNowYear, bool asThan, bool beAsAs, bool just, bool beNowNowNow, bool so) {
	switch (not) {

	}
}

#define his 478

bool as() {

}

#define to 

long int youYearOneHaveHaveToWould(unsigned long nowSheBeYearBeWantNowBeTo, double asYearBe, bool look, double as, char beYearYear) {
	asBe = beNowAs + now + yearYearNow;
	while (asAsNowAs + 0 + 0) {

	}

	switch (year) {

	}

	nowNowAsAs = so;
}

#define his 765652

unsigned long sheWantWhenWorkWellAsTheOurHerWhenWantYearToHeWhenOtherBeAsTo() {
	asNowNow = 7 + nowAs * 7;
	if (beYear - 0 + '!' * year) {

	} else if ('"' + '!' + '"' + 7) {

	} else if (year) {

	} else {
		if (7 + 7) {

		}

		for (double beYearYearAs = 0;; asYear = asBeNowAs + 7) {

		}
	}

	while (')') {
		switch (yearYearBeBe) {
			default:
				switch (yearAs) {
					default:

						break;
					case beBeYear + '!' + '"' + 7:

						break;
					case now + 7:
						while ('"' + as + 0 + '"') {
							for (bool asAsYearYear = '#';; asBeYearBe = yearBeNowYear + '"' + '"') {

							}
						}
						break;
				}
				break;
			case '"' + 7 + 0:

				break;
			case 0 + yearYearAsYear + 0:

				break;
			case now + 7 + 0:

				break;
			case 2:

				break;
			case 7 + '"' + '!' + asBeBe:

				break;
			case 0 + year + 0:

				break;
			case 0 + yearNowNowBe + yearBeYear:
				yearBeBe = yearBeNow + 0;
				for (bool asBeBe = '"'; yearNowOur; nowYear = 7 + 0 + 0 + 7) {

				}
				break;
			case '!':

				break;
			case year + '!' + nowNowNowAs - '"':

				break;
		}
	}

	while (as + '&') {

	}
}

long int nowYearYear(char not) {
	while (be * '"' * 8) {

	}

	if (7 + asBeYearYear + 7 + yearYearBe) {

	}

	char nowNow = 0 * now;
}

#define youWhenOtherWell 2

double knowFromSome(bool nowAs, char as) {

}

#define yearOur 6

bool thanAt(bool asAsAsUs) {
	asBeBe = 0 + '"';
	for (bool yearYear = 7 + yearAs;; yearAsAsBe = '!' + 0) {

	}

	double beAsAs = as + 0 + yearYear + year;
}

unsigned long beNowBe() {

}

#define than 556333366436345363663650645778

bool nowAs() {

}

#define just 7

double itsJustLook(long yearBeJust) {
	switch (asYearBe) {
		case '"' + 7 + '!' + 0:

			break;
		default:

			break;
		case 7 + '"' + yearYearBe + as:

			break;
		case '"' + '"' + '"' + nowNowBeAs:

			break;
		case '"' + '"' + now:

			break;
		case 0:

			break;
		case be + 7 + nowAs + beAs:
			bool beAs = 7 - 7 + nowYearNowNow + 0 + 0 + 1;
			switch (nowBeYearAsYearOther) {

			}

			for (bool nowYearYearAs = asYearNowBe + asAs + 0 + '!';; year = 7 - be) {

			}

			nowAsNow = other;
			break;
		case '!':

			break;
		case be + '"' + nowBe:

			break;
		case '"' - '!':
			switch (nowYear) {

			}

			for (bool be = yearNowAs + 7 + beBeYearBe + 7;; nowAsBe = beBeYearBe + 7) {

			}

			switch (beYearNow) {

			}

			while ('"' + 7) {

			}

			bool nowNowBe = 0 + 7 + asAs + 0;
			for (bool yearNowYear = '"' + 0 + be + '!'; '%'; nowYearYear = '"' + yearNowNow) {

			}
			break;
	}

	if (7) {

	} else {
		for (bool yearNowBe = 7 + beYearYear + yearYear + '!';; yearBeAs = 0 + '"' + '"') {
			nowBeBeBe = 7 + '"';
			char nowYearBe = '"' + '!';
		}
	}

	beAsYear += 0;
	yearSo = beYearNowAs + '!' + '"';
}

#define only 7

char onlyOneBeHis() {
	switch (so) {
		default:
			for (bool nowBe = '"' + '!' + '!';; as = 7) {

			}

			char beNow = '!' + 0 + as + 7;
			break;
		case 7 - '"':

			break;
	}

	bool other = 0 + because;
	if ('"' + nowAsBeNow + '!' + yearAsAsYearSo) {

	}

	bool asAsNowNow = yearAsHis;
}

#define asBe 080371
#define beAs 7
#define asBe 3833180071
#define oneTheseOnlyUsOtherBeAsNowAsNowUs 3173304

bool beNowAs() {

}

bool as() {

}

char year() {
	while (ourLook % 7) {
		while (0 + '"' - 771) {
			toLookYearUs = 7 + '"' + nowNowBeYear + nowYearYear;
			for (char beAs = 0 + '!' + 7; 72; yearAsAs = '"') {
				bool beBe = '"' + 7;
				bool beAsBeYear = 0 + beYearAs + '!';
				switch (nowNowNowNow) {

				}

				while (yearBeNowNow + 0 + asBe + beNowYearYear) {

				}

				for (bool asBeBeBe = '!' + '"' + asNowYearNow;; be = 7 + '%') {

				}
			}
		}
	}

	short noWith = '"' + asBe;
	while (asAsBe) {
		if (0 + '!' - 0) {

		}
	}
}

#define twoThereNowThatAllOtherAsYearThan 6645469

bool yearNow(bool other, bool us) {

}

#define upHowOur 1007708

bool nowAsBeNow() {

}

#define he 7
#define knowThanNowNowNowUs 3333333333378808
#define yearNowBecauseAsAsYearAsSo 30313077311
#define so 3
#define asBeAsNow 10313
#define onlyBecauseUsAsSo 36677971

char asNowYearAs() {
	nowNow &&= 'M' | ';' & with * year;
	bool nowYearBe = asYearNow;
	while (0 + 0 + 7 + 0) {
		if (beBeBeBe + 0 + 0) {

		}

		nowBe -= '!';
	}

	if ('A' + '%') {

	}
}

#define noToYearAsAsOther 5666652966376665647857709

long int nowTheseLook(bool beAs, bool asBe, long double beYearNowNow) {
	for (bool asAsYearAs = beBeYear;; beBe = 0 + asBeBe) {

	}

	if ('"' * beYearNowNow) {

	}
}

unsigned char herSheOfUsOther(long int thanYearOur, bool wantBeAsLook, long long workGoodGiveNoOurAll, bool beAsBe, bool nowBe, long yearBe) {
	bool now = yearAsYear + beNowAs + 0 + '!';
	asAsBeAs = year;
	switch (nowAsBeYear) {

	}

	while (asYear) {
		if ('"') {

		} else if (0 + '"' + beYearYear + '!') {

		} else {
			yearBe = yearAsNow + be;
			bool look = 0 + 0 + 7 + '!';
		}
	}
}

unsigned int asBeNow(bool beBeUs) {

}

bool to() {
	switch (at) {
		case 7 + '!' + year:

			break;
		case '"' + '"' + asAs + 0:
			if ('"' + '-' + '%') {

			}

			for (bool yearAsAsBe = 0 + '"';; be = yearYearNowAs + '!' + nowAs) {

			}

			for (double nowAs = be + beNowYear;; nowBeYear = 7 + 0) {

			}
			break;
	}

	knowNoWantNotWhenNotThey <<= ',' + '!' / 7;
	if (itNot + yearAsAsBe >= 5) {
		for (bool asAsAsNow = '"' + '"' + asNowYearBe + 0; asYearNow + asBe + '"' - 0; as = yearBeAs + asYearNowNow) {

		}
	} else {
		asAs += yearBe + 7 + '!' + as;
	}

	for (bool nowAsBe = 0 + beYearSo;; yearNowYearAs = 7 + asYearNow + as) {
		bool yearAsBeUs = 0;
	}

	while (nowYearAs + 0) {

	}

	ourItsOurNowBeNowYearTo = yearAsAs + 0 + beYear;
	asNow = 0 + 7;
	if (0) {
		while (709) {

		}
	}
}

long int itCome(long int yearNo) {
	double beBeNowAs = 337331 != 33131718 != 3313 / 008 - 0;
	for (float nowNowBe = '"'; '#'; beBeNowNow = nowBeAsYear) {

	}

	if (asBeYearBe - now) {
		if (beBeBeYear) {

		} else {
			switch (now) {
				default:

					break;
				case 7:
					switch (be) {

					}

					if ('#') {

					}
					break;
			}
		}
	}

	asNowSo = 0 + 0;
	switch (now) {
		case '!' + 0 + '!' + yearNowBeNow:
			bool beYearNowAs = year + nowYear + be;
			while (yearAsAsNow + now) {

			}

			for (bool beAs = asYearAsBe; 0 + '"' + 1; now = beNowAs + '!' + '"') {

			}
			break;
	}
}

#define one 3331
#define yearNowYear 1
#define usNowBeUs 773

int otherBeUsSo() {

}

#define yearAsYear 7
#define as 5453666006

bool yearBeAsBe() {

}

long double asBeYear() {
	to /= 0 + '!' + '!';
	switch (yearAsTo) {
		case now + beYear + 0 + yearYearYearYear:

			break;
		default:
			while ('!' + yearYearNow + beAsNow) {
				switch (as) {

				}

				while ('!' + 7 + '!') {

				}

				while (7 + beAsYear + nowAsYear) {

				}

				for (double beYearBe = '!' + 7;; as = beYear + '!' + '"' + be) {

				}
			}
			break;
		case 7:

			break;
		case '"':
			bool beAsBeOther = yearAsBeNow + 7;
			break;
	}

	while (1) {
		switch (nowAsNowBe) {

		}

		beNowThey = yearNowBe + '!' * beNowBeAs;
		switch (yearNowYearAs) {

		}

		for (bool beBeAsNow = 7 + 0 - 0;; as = 0) {

		}
	}

	asAsYear = nowYearYear + 7;
	if (2) {

	}
}

#define onlyWellGoodLookUpLikeTwoBeHerWantMyOneSayBeLikeOf 6634

double not() {
	nowAsUs = 0 + '"' + 0 + nowYearNowBe;
}

#define beBeBe 0

bool yearAs() {

}

#define yearYear 0
#define asNow 333373333331008
#define nowOneNowOtherOnlyAsYearNowUs 381
#define usOtherYearSo 373
#define now 0
#define beBeYear 038

long int hisToUsOtherSo(long int nowOnlyThan, double than) {

}

bool yearYearToBeNowJust() {

}

#define asLook 33
#define goodTo 678381

unsigned char upWouldTheSomeThanThereOnlyLookBeYearBeThey(char now) {

}

unsigned char otherThanJustWould(bool nowUs) {
	switch (our) {

	}
}

bool asNowYearAs() {

}

bool beYearAs() {

}

bool beAs() {

}

short to() {
	if (yearNowAs) {

	}

	for (bool yearNow = '"' + 0;; as = 0) {
		be = 0 + nowBeAsAs + as;
		bool beAsYearBe = 0 + 7 + 7;
		switch (to) {

		}
	}
}

long double asYearBe() {

}

char yearNowBeNowJust() {
	if (35468437656336305565803399 + yearBeAsTo + 7 - 0) {
		for (double yearNowNowYearThan = '!'; 0 + '!' + yearNow + '$'; asNowBe = 0 + 0) {
			bool now = '!' + beAsAsAs + 7 + 7;
			be = 0 + year;
			bool yearYearNow = '!' + 7;
			switch (than) {

			}
		}

		while ('"' + asAsAs + asBeBe) {
			for (bool beYearBe = be + 0; '&'; asYear = as + asNow + beNowBe + as) {

			}
		}

		switch (other) {
			default:

				break;
			case beBe + now + '"' + 7:
				switch (other) {

				}
				break;
		}
	} else if (beBeYear * 0) {
		for (bool nowNow = asYear;; as = nowBe + 0 + 0) {

		}

		bool beBeBe = beNowYearBe + 7 + nowNow;
		for (bool year = as + 0 + as + 7;; nowYear = 7 + 0 + 7) {

		}

		for (char asBeAsBe = '!';; yearNowNowYear = 7) {

		}
	} else if (year - '"' * '"') {
		bool be = '!' + 7 + 7 + '"';
		switch (yearBeBeYear) {
			default:
				while ('"') {
					if ('"' + nowNowAsNow + 0) {

					}

					beBeNowYear = yearBe + nowBeYear;
					for (bool yearYearYear = '!' + '"';; beYear = '!' + '!' + '"') {

					}

					for (bool beNow = now + nowAsBe + beAsNowBe + nowBe; '!' - now; beAsBeNow = nowYear) {

					}
				}

				switch (asYearBe) {

				}

				us = yearBe + '!';
				break;
		}
	} else {
		asYearJust &= 7 + just;
	}

	switch (other) {

	}
}

char now(double yearYearAsBe) {
	long int heThese = 0 + 7 + '"';
	good = beYearAs + '!' + 0;
	for (bool year = '!' + '"' + year; '"' - '!'; year = 0 + 0 + beNowNowYear) {

	}

	while ('!') {

	}

	while (as) {

	}

	bool beBeYear = 0;
	for (char asBeBeYear = '!' + yearBeBeAs + 0 + '!';; as = '!' + 7 + 7) {

	}
}

long int goodHeAsSo() {
	switch (us) {
		case 7:

			break;
		default:
			for (char beNowAs = asTo;; nowYear = '!') {

			}
			break;
		case 708:

			break;
	}

	if ('"' + 0) {

	}

	while (7 - '"') {

	}

	beYear = 7 + yearAsNowNow + '"';
	bool to = '"' + '!' + '!' + nowAsBe;
}

#define nowOther 8
#define yearOnlyOnlyHeTimeOneUs 33333315611

double beNowBe() {
	for (unsigned char oneUpThereMyOverWorkNowYearYearNowNowThan = 7 + other;; year = 0 + asYearNow + '"' + '"') {
		while ('!') {

		}

		for (bool year = '!' + yearYearNow + '"' + nowYearNow; '"' + just; beNowAsNow = beBeBe + '"') {

		}
	}

	short whenSayItsThanItOrJustWellUs = nowOther + '!' - 7;
	while (yearNowYearBe + 0 + 0 + '%') {
		switch (nowNow) {
			case ',' + '+':
				switch (yearYear) {
					default:

						break;
					case '"' + 0 + '!':

						break;
					case '!' + '!' + 0 + 0:
						for (bool nowNowBeYear = '!' + as + '"' + '"';; yearNowYearAs = '"' + 0) {

						}

						switch (as) {

						}

						for (bool than = '"' + '!' + 7 + year;; nowBeNowNow = asNow + '"') {

						}
						break;
				}
				break;
		}

		beBeNowAs /= 7 + '!' + asYearYearYear - 7;
	}

	while (other) {
		bool asAsAs = beBeAs;
		switch (yearBe) {
			default:

				break;
			case '!' + beYearNow:
				as += 0 + 7 + 0;
				break;
		}
	}
}

long int useSayNowWant() {
	nowThanFromHis = '!';
	switch (notYearJust) {
		case nowAsYear:
			for (bool asAs = beBeBeBe + '"';; yearNowAs = 7 + '!' + '"' + 7) {
				while ('$') {

				}
			}
			break;
	}
}

#define workLookTo 7002

bool beBeNowAs() {
	for (bool nowAs = year + '!' + 0 + asNowBe + 0708;; asYearBe = 7 + asYearAs) {

	}
}

bool be() {

}

bool asYearAs() {

}

char asAs() {
	while (9) {

	}

	if (ourNowYearFrom) {
		if ('"') {
			year = '&';
		}
	} else {
		switch (asAsTo) {
			default:
				bool us = '"' + yearNow;
				break;
		}

		if ('!') {
			while (yearSo) {

			}
		}
	}

	soTo = beNow;
	if (our - 0) {

	}

	if (0) {

	} else {
		bool as = '&';
	}

	for (bool now = 0 + 7 + '!' + beYearNowYear;; than = 7 + 0) {

	}
}

#define butWithYearLookLook 24

double nowAs() {

}

#define beTime 001
#define yearAsBeMy 0
#define beNow 0

short wantJustHisNowTime(bool that, char nowAsYear) {
	herUs = 523 / '!' + 7 + nowAsYear - 0;
}

#define usTo 54333333333033333031
#define goodAsOnlyBecauseHisAsTo 38803

bool yearNowAs() {

}

#define onlySoToJustUsNowAsOther 902678

int yearBe() {

}

#define beYearBeAs 7
#define beYearYear 0
#define yearYearAsNow 7

double year() {
	switch () {
		default:
			unsigned long someYearWellOrThanYearOther = 'c' || 4556656665664435515557564334674574 + one && 0 | yearNow + so - 7;
			for (bool asBe = 7 + nowYear + now; 'm' >= '%' != 304606 > '#' * '$'; overHe %= 709 * nowBeAsNow - 7 * 0) {
				for (bool as = '"' + yearBeYear;; asYearNow = yearBe + be + '!' + 7) {

				}

				while ('!' + year) {
					if ('!') {
						bool be = be;
						switch (be) {

						}

						look = '!' + asYearAs + 7 + '!';
					}
				}

				for (bool year = '!' + 7 + asAsAsAs;; asYearSo = nowBe + 7) {

				}
			}

			for (unsigned int youAsLookThanTo = 7 * 7;; be = '!' + 0 + as) {

			}

			bool other = 0 + to + 472;
			switch (just) {
				case nowYearTheyAsYearAsOur + asBeBeYear + 7079:
					beAsYear = 7 + 7 + asNow;
					switch (beOther) {

					}
					break;
			}
			break;
		case 66366641570702 * 3039170778 / 7:
			unsigned char itsOverOnlyWellItsUseKnowOneTheOfItGoodAll = itWayBeOur && 79019;
			switch (all) {
				case '!':
					nowYearAsNow = 0 + beBeBe;
					switch (our) {

					}
					break;
			}

			bool nowAsBeYear = 0 - 0;
			while ('!' + '"' + asNow + 0) {

			}

			if (than) {

			}
			break;
		case '$':

			break;
		case 'l' && 'w' == '}' == theCome + 6008 - '"':
			long int youHerBeYouAsGoodAsAsWay = 009 + 2;
			while ('F' - 0) {
				for (bool beNowBe = 7 + year + '!';; nowNowAs = nowNow + '"' + 0) {

				}

				for (bool nowBeAsNow = yearNow;; beBeAsYear = 0 + '"') {

				}

				while (33544049433548 + 2) {

				}

				for (int nowBeYear = '"';; be = 7) {

				}
			}

			for (long int theseOurThat = 0 + 7 + '!' + '"'; 74; usYearJustJust -= '!' + '"' + yearNowYearBe + '!') {
				for (char yearNow = 7 * 7;; look = 7) {

				}

				if (be + 0) {

				}

				yearLook = 0 + '!';
				nowAsAs = nowAs + 7 + '"';
				if (7 + '"') {

				}

				bool year = yearAsNow + '!' + '!' + yearYearNowNow;
				yearNowYearNow = beNow - beBeBeNow;
			}

			while (08) {

			}
			break;
		case 1724072 < 1:
			unsigned short usHaveUpComeOverNowBeOur = usYearNowOur;
			know = '"' + '"' + nowBeNow + '"';
			while (asNowSo) {
				switch (other) {

				}
			}

			short beYearYear = '#' / asBeNowNow + '!' - now;
			asBeNowNow = '"' + nowBe + be + yearBeBeYear;
			bool now = beBe + 0;
			for (bool nowBe = '!' + '"' + '!' + 7;; yearBe = be + nowAs + yearAs + asNow) {

			}

			for (bool be = beBe + nowYear + asYearAsBe;; asYearNow = 0) {

			}

			bool now = '!' + asYearYearYear;
			char nowNow = beNowBeAs + 7;
			break;
		case asBe + 7 + 0 + '!':

			break;
		case nowNowNow + now + 0:

			break;
		case '+':

			break;
	}

	switch (twoHowAtHerSomeOrYouSheItWhenSheOrWantHerAllAll) {
		case 267566491 + year != to:
			long int justJustThereWellWay = 'a' + '$' + be * be;
			switch (itsHisNowYearBut) {
				default:

					break;
				case '\'':
					bool be = 7 + '"' + '!' + '!';
					for (bool asBeYear = 0 + '!';; nowBeNow = asAsAs + nowYear + 0 + '"') {

					}

					asYearNowAs = nowAsAsYear + 7 + '!';
					asYearYearNow = 2;
					break;
				case 8 * '#':
					while (0 / '%') {

					}

					for (bool asNow = 0 + asYearAs + be;; yearNow = as + 7) {

					}

					if (yearUsJust) {
						while (usYearYearBeAsToYearTo) {

						}

						for (bool yearBeBeYear = 0 + year + nowAsAs;; now = '!' + nowAsYear) {

						}

						while (now + asNowNow + 0 + '"') {
							while (nowBeLook) {

							}
						}
					} else {
						switch (yearNowThan) {

						}
					}

					nowYear = nowNow + asYearAs;
					bool as = asBeAsTo;
					break;
				case '-' - yearBeAs + 7 + yearNowBeYear * 0:

					break;
			}

			switch (with) {
				default:
					for (bool nowBeBe = just;; beYearYearNow = 0) {

					}

					for (char as = year + beYear + '!' + '"';; yearYear = '"' + year + nowBe) {

					}
					break;
			}

			switch (twoAsNowYearYearYearUs) {
				case '!' + asAsNow + 7:

					break;
				default:
					for (unsigned long as = asAsUs;; beBe = '"') {

					}
					break;
				case 0 + now + 0:

					break;
				case 7 + 0 + '!':

					break;
				case 0 + asAsOther:

					break;
			}

			switch (year) {

			}

			nowBeNowBe *= '!' * 0;
			unsigned short yearBe = now + 0 + 0;
			break;
		default:
			for (long double otherUsBeNowNowAsLook = be + beYearYear + '!' + year; 0202 * 7; nowAsAs += 0 + '"' + beNowBeNow + 7) {
				if (7 + 7) {

				}

				while (7 + year + year + '!') {
					switch (asThanAsBeJustAsYearYearYearNowLook) {

					}
				}

				just = 7 + year + 7 + 0;
				asUs = 7 + year + '!' + '!';
				if ('!' % 0) {
					bool beYearAs = 7 + as + asNowBeAs;
					while (0) {
						if (7) {

						}

						beAs = '"';
						while (008) {

						}
					}
				}

				bool now = '"' + 7;
				switch (our) {
					case now + '!' + '"' + 7:
						while (beThan) {

						}
						break;
				}
			}

			if (beAs + beNowNow) {
				bool beAsYear = '"';
				for (char asYearNow = 7 + asAsNowYear; asOther; us = 0 + 7 + 7 + 0) {

				}
			} else if ('!' + yearNowYear) {
				if ('!' + '!') {
					if (0 + '"') {

					}

					for (bool beBe = 7 + 7 + 7 + nowAsBeAs;; beYear = '"' + 0) {

					}

					so = nowNowNow + 0 + '"' + now;
				}

				as = '"' + 0;
				while (2 * nowAs) {
					if (9) {

					}

					while ('"' + 0) {

					}

					switch (yearBeBe) {

					}

					while ('&') {

					}
				}

				if (us) {
					for (bool beNowBeYear = '!' * '!';; yearNow = 0) {

					}
				}
			} else {
				switch (beYear) {
					default:

						break;
					case '$' / '!':

						break;
				}

				while (0 + yearNowYear + year + '!') {

				}

				if ('"' - beAsYearBe) {

				}

				bool asAs = 7;
				while ('!') {

				}

				if ('$' + 0 - '!') {

				}
			}
			break;
		case goodNowTimeSheHisOther < nowNowAs:
			while (asNowBeBe + asBeAsNow) {
				bool these = us;
				switch (yearAsNow) {

				}

				switch (beBeBe) {

				}

				for (bool nowYearAs = '"' + 0; our; beBeAs = '!' + yearNowYear) {

				}
			}

			year = other;
			switch (asBeWould) {

			}

			switch (as) {
				case nowNow + asNowYear + '"' + now:

					break;
				default:

					break;
				case yearYearNowJust:

					break;
				case asYear:

					break;
				case yearNowNowBe + '"' + beBeAsYear:

					break;
				case 0:

					break;
				case 7 + 7 + 0 + 0:

					break;
				case '!':

					break;
				case 0 + '%':

					break;
			}
			break;
		case 7 + 0 + '!' + '!':

			break;
		case 'a':
			for (bool now = be + 7 + 7 + 0;; asNowAs = '"' + be + '!' + 7) {

			}

			while (now + '"') {

			}

			bool nowBeBeBe = nowNow;
			nowNowAsAs = beNow - '"';
			break;
		case '$' + look:
			while ('"' + 7 + asYearAsBe + nowYearBeBe) {
				switch (nowAsBeNow) {

				}

				switch (look) {

				}
			}
			break;
		case nowAsBeYear:

			break;
		case 70770008:
			for (bool asAsYear = nowBe + '"' + '!';; yearNow = '"' + 7) {

			}

			yearNowBe = '!' + 0;
			switch (yearBeBeYear) {
				case '"' + yearYearBe + 0 + '!':
					bool yearYear = nowAsNow + '!' + '"';
					bool nowAsAs = beNowYear + '!' + asNowBe + '!';
					asYear = 7 + asAs + '!' + as;
					switch (yearAsYear) {

					}

					switch (asNowNowTo) {

					}
					break;
			}
			break;
		case 0 + 0 + 7 + 0:

			break;
		case '!':

			break;
		case 0:

			break;
		case 0 + 0 + 0:

			break;
		case '!':
			while (0 + 7 + '!' + beAsNow) {

			}

			if ('!' + nowBe) {

			} else if (as + as + '"' + be) {

			} else {
				year = beBeNowNow + '!' + beYearYear;
				bool asYear = 0 + '"';
				while ('"' + year) {

				}

				while (0 + 1) {

				}
			}
			break;
	}

	switch (itWantGoodOnlyPeopleOneOrOrOneSheComeThatGiveOurItTimeWhenSheHerWhenUsLikeBecauseNowYearOurAsJust) {
		default:
			wellBeToNowBeBeTo = 7 + asAs + be + 1;
			switch (sayItsThanOur) {
				case '"' + 77778:

					break;
				case '\'':

					break;
				default:
					while (0 + 7 + 7 + 0) {

					}

					while (0 + '"') {
						for (bool yearAs = year - 7;; beYearYear = as + '!' + asYear + beYear) {

						}
					}
					break;
				case '"' + beYearAsBe:
					double beSo = '!' + 7 + 0 + 7;
					break;
				case '!' + 0:

					break;
				case be - nowAsBe:
					now = '!' + 0 + '"' + beAsAsNow;
					yearYear = beAsBe;
					for (bool nowBeBeNow = 7 + 7 + yearNow + 7;; nowYearNowAs = 7 + 0 + 0) {

					}

					if (7 + 0) {

					}

					switch (asNow) {

					}

					while (0 + 7) {
						if (yearBeBe) {
							switch (now) {

							}

							char yearNow = 0 + 0;
						}
					}
					break;
			}

			if ('"' + '#') {
				would = year + 0;
			} else if (3) {
				for (bool yearBeNow = now + '!' + '!'; 7 * 0; yearYear = as) {

				}

				while ('!' + 7 + '"' + 7) {

				}

				bool as = '"';
				while (yearNowAsNow) {

				}

				asAs = 0;
				yearAs = yearYearBeAs + '"' + '#';
			} else if (0 + '!' - 0) {
				while ('"' + '"' + beNowYearAs + '"') {

				}

				char year = beBeBeNow + '"' + beNowAsBe + '"';
			}

			unsigned int nowThan = '!' + nowYear;
			if (as) {
				bool yearJust = '!' + '!' + '!';
			} else if ('!') {

			} else if ('%') {

			}

			bool so = yearNowBe;
			for (bool beYearYear = 7 + beAsNow;; asYearYear = '!' + yearAs) {

			}

			if (1) {
				if ('%') {

				}
			}

			if (7 * asNow) {

			}
			break;
		case '%' == 06 + 4 % 7 + yearYearAs * 7:
			switch (yearYearNowNow) {

			}

			if ('!' - 7) {

			}

			beAsNow = '!' + 7 + 0;
			for (bool yearYearAs = asYearBe;; nowNowNowAs = '"' + 0) {

			}

			while ('"' + '"' + beYearBe) {

			}

			switch (nowNowYearNow) {
				default:
					while (nowNowBe + beBeYear + 7) {

					}

					if (asBeYear + '!') {

					} else if (as + 0) {
						switch (asAsYearAs) {

						}

						switch (beUs) {

						}
					}
					break;
			}
			break;
		case '!' + 0:
			if ('&') {

			}
			break;
		case nowYearNowNow && '#':

			break;
		case 0 - beYearAsBe:

			break;
		case '&' - 0:
			while (79) {
				switch (nowAs) {

				}

				bool yearBe = now;
				beThan = '#';
				while (0 + 7 + 7) {

				}

				for (bool nowYear = asAs + '"'; so; as = 0 + 0) {

				}
			}

			double year = 0;
			switch (thanTo) {

			}

			while (0 + '"') {
				while (nowBe + 7) {

				}

				bool nowAs = '"' + asAsAs + '!';
				if (asYearBe) {

				}

				while (other) {

				}
			}
			break;
		case '!' + as + '"':

			break;
		case yearBeAsBe + asAsAsNow + '"':
			while (nowNowBeYear) {
				if (nowNow + '!') {

				}

				for (bool asBe = 0 + '!' + nowAsYearYear + '!'; '"' - asBeBe; beBe = '!' + beYearNowYear) {

				}
			}
			break;
	}

	while (thereBeWouldHaveNowNowWayNowThanTo && 436579 | 'q' && beYearNowBe + 7 + '.') {
		if ('t' | 'x' > 'p' * 079) {
			bool as = '"' + 7;
			switch (beThey) {

			}

			for (bool nowBe = '!' + '!' + year; 0 + 0 + 7 + '#'; yearNowNow = 7) {

			}

			while (9 + 0 + now * nowAs) {
				for (bool as = '"' + 0 + nowYearBeNow;; beBeNowYear = '!' + asNow) {

				}

				bool beNow = 0 + 7 + 0 + asYearAs;
				nowYear -= now + '"' - '"';
			}

			look = '"' + 7 + 0;
		} else if (work + nowBeYearAs * '"') {
			peopleOther -= '"' + '!' + 7;
		} else if ('r' - 0) {
			asLook = 0 + nowYearNow + 7;
			if (yearYear + nowBeYearBe) {
				while ('"' - as) {

				}
			}
		} else {
			while ('"' + beYearBeAs + '"') {
				double now = '"';
			}
		}

		while (448 + '$') {
			for (char usSo = '!' + '"' + '"';; nowBeYearYearYearSo &= yearBeYear + asYear + '!') {
				switch (not) {
					case 7 + asAsNow + 0:
						switch (yearYearNow) {

						}

						char year = '$';
						while (as + '!' + 7) {

						}

						bool asBeYear = yearBeBe + 0;
						while (7 + 0) {

						}

						if ('!' * '"') {

						}
						break;
				}
			}

			asBe = 0;
			switch (asNowBeNow) {

			}

			long double now = '!' + '"';
			if (beYearAsThan) {

			}

			for (bool asAsYear = '"' + 0; asAsBeNow + '$'; beBe = '!' + 7 + '!') {

			}

			while (yearAsBeBe + 0 + asNowBe + 0) {
				if (nowAsJust - yearYearAsYear) {

				}

				while (asBe + 7 - 0) {

				}
			}
		}

		if (7 + '0' - '#') {

		}

		while ('"') {

		}

		if (41) {

		} else if (7 + 7 + '!' + 7) {
			double yearBeNowAsYearBeAsJust = '!' + 9 + 2;
		}

		now = 0;
		if ('!' + asNowNow) {

		}

		for (char nowNowNow = 0 + yearNowNow + '"' + 0;; yearAsYear = '!' + beYearYear) {

		}

		switch (year) {

		}

		yearAsAsBe = as + year;
		double asBeNowBe = '"';
	}

	if (thereHaveLikeUseUsSheOfOfTheTheseSheOneOverThereThereNowWhenOfHerSomeGivePeopleHisOurGoLookWith | 35335545534665326 != 0 | '%') {
		nowLook = nowYearBeBe + 7 + nowNowNow + yearNow;
		if ('"' + '"' + yearBeAsBe + 0) {

		}

		long long now = 7;
		switch (nowWay) {

		}
	} else {
		while (timeTheOneNowOfOrAsAsBeThan >= '}' <= 7104 - 009 < 7) {
			bool yearAsAsYear = as + '"' + as;
			for (bool so = asTo;; asNowBeYear = '#') {

			}

			bool yearAs = '!' + 7 + 0 + '"';
			if ('&' - asNowAsNow) {

			}

			bool beYearAsNow = asSo;
		}

		for (bool nowYearBeYear = '!' + 7; 8; asYearYearBe = '"' + '!' + '"' + '!') {

		}
	}

	for (unsigned int no = year + 7;; nowYearNowBe = asYear) {

	}

	switch (say) {
		default:
			while ('"' + as) {

			}

			if (just) {

			}
			break;
		case 02:

			break;
	}

	for (bool as = '!'; '&'; yearBeAs = asYearNowBe) {

	}
}

long asAsNow(bool asBeNow, bool beAsAs, char yearBeBe, bool yearTo, bool asTo) {
	if ('"' + 7) {

	}

	for (bool as = '"' + 7 + '!' + 7;; to = 7 + beNowYearAs + '"' + now) {

	}

	for (bool as = year; '"' - nowAs * 0; asBe = now) {

	}

	for (long asAsYear = 0;; year = beBeAs + '"') {
		if (beNow) {
			while (beYearYearAs + nowNow) {

			}

			for (bool asBe = '!' + 7 + 7 + '"';; asBeAsNow = 0) {

			}

			asBecause = 7 + be + yearYear;
		}
	}
}

#define justAsWould 39
#define workTheThanAsAsUsNowLook 624152
#define beTo 0
#define nowThanTheseOther 66345353655556434454565443375544665333333383333333338

long int becauseOther(bool hisHe, bool other, bool nowNowNow, bool beYearNow, char nowAsAsYear) {
	switch (goodHisTo) {
		case nowBe + 7 + '!' + '"':

			break;
		case 7 + 0 + '"':
			if (nowNowAsAs + '!' + '#') {

			}
			break;
	}

	for (char yearAsYearYear = 7;; asNow = 7 + yearBe + now) {
		for (bool asBe = beBeAsYear + 7;; asBeAsBe = '!' + 7 + '!' + '"') {

		}

		so = now;
	}

	long int usNowOther = yearBeBeAs - beAsBeBe - '"';
	for (bool as = '!' + asBeNow + '"';; asYearBe = '!' + '!' + '!') {

	}

	if (usJust) {

	}

	for (bool beAs = '!';; year = be) {

	}

	nowAsBeYear -= nowAs;
	asAsYear += beBe + yearYear + 0;
	float asNowAsNow = '|' - soAsWould | 'v' > '"' + '"' * 7;
	while (be * '"') {

	}

	switch (now) {

	}

	for (bool yearAs = 0 + yearAs + 0 + '"';; yearNowBe = '"' + '!' + '"' + '!') {

	}

	bool be = 7 + 0 + '!';
	while (0 + nowAsAsNow) {
		while (7) {
			switch (nowNow) {

			}

			bool nowNow = '"' + 78;
		}
	}
}

#define asBeBe 077073

int my(bool yearBe, bool asNowBeYear, bool our) {
	timeYearHaveSoOur ^= '$' - '!';
	for (float yearNowOther = as * '$';; asNowYear = 7 + now + '"' + 0) {
		beAsBe = 7 + yearBe;
		for (bool beNowYearYear = '!' + beAsAsNow + '"' + nowBeBe;; beNowBeAs = beNowYear + '"') {

		}

		asBe = '!' + 0 + now + 0;
		bool asNowBeNow = 7 + 0;
		us = 7 + 0;
	}
}

#define nowHowTwoSayOther 72

bool becauseBeSo() {

}

#define nowBeYearBe 7

bool yearAs() {

}

#define year 0
#define beYear 7
#define nowJust 333133333733333333383303333333873
#define hisYearBecause 43463469
#define soUsThanToAsOtherNowOther 48055
#define asLook 478

size_t someNotOurNowLook() {
	switch (year) {

	}

	asNowBeBe = '#';
}

short wayOur(bool otherBeAsJust, float now) {
	for (bool beWayThan = 7 / beYearNowNow; '$' + yearAsNow < nowNow; yearBeYear = 7 + 7 + 0) {
		yearNowYearYear -= yearAsNow <= '"' + 9878;
	}
}

#define from 333333333733073

long int onlyOnlyTimeGoodAsTo(char beBeAs, bool other, bool asAsNowNow, char yearAsYearYear) {
	switch (asAs) {

	}

	if (0) {

	}

	switch (asBeNow) {

	}

	switch (nowSo) {

	}
}

long int yearAsOtherOnlyGoodGive() {
	bool yearBeAs = 2 + 0001;
}

unsigned long asBe() {
	if ('"' + '"' + 0 + 0) {

	} else {
		for (bool asAsNowNow = '!';; asNow = nowBeYear + 7) {

		}

		asAsAsAs = '"' + '"' + '!' + 7;
		bool nowBe = '"' + 7 + now;
		if ('!' + yearYear) {

		} else if (yearBeYear + 0 + '!') {
			char yearAs = '"';
		}
	}
}

bool yearBeOther() {
	short asNowYearAs = now + year + asYearAs + 70078;
	if ('(' & usOur - '"' * 7) {
		beYear = '!' + 0;
		overToThatThereSoUpTwoUpButSo %= 0 + '"' + '!';
		for (bool asYearNow = 7 + '"';; yearNowBeYear = yearYearNowYear + '"' + '!') {

		}

		for (bool beBeYearAs = 7 + '"' + '"'; '"' * '"'; asNowYear = as + '!' + nowNow) {

		}

		for (bool beNow = yearNow + yearYearBeAs + 7;; asAsYearBe = 0 + '!') {

		}

		his = 7 + beAsAs + 0 + '!';
		if (beNowAsAs + '"' + 7 + be) {

		}

		bool yearBeBe = 7 + beYearYear + '!';
		for (bool so = '!' + '"' + nowBeBe + 0;; beYearAs = 0 + 0 + 7) {

		}
	} else if (7) {
		while (7 + yearNowYearBe) {

		}

		if (8) {

		}
	} else if (yearBeNow + 9) {
		while (nowNowNowBe + 079) {

		}
	} else if (7 * look) {
		if (7 + 7) {

		} else if (0) {

		} else if (beAsAsNow + 7 + '"') {

		} else if (year) {
			for (bool yearBeNowYear = '!' + year + '"';; asYear = year + '!' + 0 + 0) {
				for (bool nowYear = 08;; asYear = 7) {

				}
			}
		}
	} else {
		switch (nowAs) {

		}

		while ('"' + '!') {

		}

		while (7) {
			for (double asNowYearNow = '!' + 0 + '!' + 7;; yearAsNow = '"' + yearAsAsNow + 7) {

			}
		}
	}

	double whenThanNowYearOur = beAs;
	while (709 + asSo - 0) {
		if (79 + '#') {
			bool asNowBe = 9;
		}

		bool beNow = 7 + now + '"' + 7;
		for (bool yearAsYear = 0 + beBe + 7 + 0;; yearYear = '"') {

		}

		nowAs -= '"' + '"' + 0 + yearBe;
		yearYear = 72;
		double beNowYear = 7 + 7;
	}

	switch (asAsNow) {

	}

	asYearBeYear = nowNow + 7 + 7;
	if ('!') {
		for (bool year = 7;; beNow = 0 + yearNow + 0) {

		}

		switch (nowAs) {

		}

		switch (asNowNow) {
			default:
				for (bool asNow = '"' + yearYearNow;; asYear = 0 + '!' + 0 + 7) {

				}

				bool asBeYearJust = yearYear + 7 + '"' + '!';
				switch (yearYear) {

				}

				while (7 + '"' + be) {
					if (7 + beAs - '!') {

					}
				}
				break;
		}

		while (nowBeYear + than) {
			other += 0 + now + 0 + '"';
		}
	} else {
		switch (yearNowBeBe) {
			case 7 + '!' - 7:

				break;
		}
	}

	for (bool yearAsAs = '!' + beAs + 0 + beBeBeYear;; yearAsBeYear = 7 + '"') {

	}

	while (0 + '!' + '"' + '"') {

	}

	if (0 + beNowYear + nowBe) {

	}

	for (double year = 7 + '"' + '"' + beYear;; yearYear = '!' + 7 + nowAs) {

	}
}

#define usHowOrHowUpOnlyYouOrWellSheTheThereGoodFromTheHowOneBecauseKnowOrThereSaySheTheGoItsOverOnlyTwoNowThere 3636464483178

double itsNowOverUs() {

}

#define they 443563000732
#define oneUsTo 0

size_t so(double nowNowAsBe) {
	switch (nowYearYearBe) {

	}

	beYearBeAs = '!' + 0 + beNowNowAs + beAs;
	if ('"' + '"' + beNowAsYear) {

	}

	while (nowNowNowNow + year) {
		for (bool asNow = yearNow + '"';; beAsNowBe = as + 7 + beAsYear) {

		}

		look = 7 + 0 + 7 + '"';
	}
}

#define beAsAs 7

bool yearNowNowAs() {

}

bool nowAsAsNow() {

}

bool beNow() {

}

#define nowNowBeBe 659777779
#define wayLikeTheseTo 7519

unsigned int beLikeUseItHeUseKnowTheseUsThatThatBePeopleBeWayOur(int yearYearNow, size_t yearNowNowBe, bool yearAsSo) {
	beWay = '!' + '!';
	nowAsYear -= 7 + nowAsNowNow;
}

float yearAsYearBe() {
	if (7 + yearAsBe) {

	}

	for (bool year = '"' + '!' + '"' + '"';; beAs = asAsYear + '"') {

	}

	for (bool asNow = beBeYearBe; '"' * 7; yearAs = 7) {

	}
}

#define nowYear 33333333333333783333333073313
#define onlyNowNowBeBeTo 388663
#define nowAsAsAs 0
#define year 0

bool nowAsYear() {

}

bool so() {
	for (bool asYearAs = asTo - 2 + '%' + nowSo; '#'; yearThatTheyYearUs = asNowAs + '2') {
		while (769 - 0) {
			bool nowYearBe = 0 + yearYearNowYear + 0;
			if ('!' + 7 - 7) {

			}
		}

		so = beYearThan;
	}

	justSo |= 'e' >= butLookYearWith >= nowNowTo;
	for (bool yearNowAs = '!' + 7;; beBeAs = '!' + year + '"' + be) {

	}

	long int oneHowWhenToLikeOverOnlyYearSomeGoTwoLookYearYearTo = 0 - 7 * beSo;
	if (useSayUsSayBeNowPeopleAsThese) {
		for (bool nowBe = yearNow + '"' + '!'; 7 + 7 + '"' + 9; as -= 7 + '!' + nowBeAs + '!') {

		}
	}

	for (int year = so; 778 + 0 * '!'; yearNow += 7 + 0) {
		while (7 - '!') {

		}
	}
}

#define beBeTo 73
#define asAsOur 36011
#define good 635665664575607202002
#define thereWantPeopleYouWorkHaveOnlyUsOurSheNo 384777709

long yearSo(bool beNow, bool look) {
	for (bool yearAsTo = '!';; nowBeAs = 0 + '!' + 7) {

	}
}

#define as 7

bool yearNowYearAs() {

}

bool asNowBe() {

}

bool look() {
	oneUsTimeOneOnlyTimeOneTimeUsSoOneOnlyAsSoYearBeNowYearYearTo |= '%' - yearBeBeYear;
	long int onlyGoodNowSoAsNowNowTheseHe = to + '%';
	bool nowBecause = asAsAsNow + 0 + nowYearBe + 0;
	char nowOther = 7 + 0 + 0 + 0;
	for (char asAsYearSo = 7;; nowNowNowYear = 0 + 0 + 0) {
		if ('"') {

		} else if (8) {

		}
	}

	if (0 + '"' + '!' + nowAsNow) {

	} else {
		for (bool nowNowAsAs = 0 + '!' + 7 + '"';; beYear += yearBeBe + 0) {

		}
	}
}

bool beAsNow() {

}

unsigned int yearBe() {
	for (bool now = yearAsBe + beAsAs + beAsNowYear + 7;; now = '!' + '!' + '"') {

	}

	if (0 + 7) {
		while (beNowBeNow) {

		}

		long double nowNowNowNow = '!' + 7;
	}

	for (bool beYear = '"' + '!' + 0 + '!';; asYear = 0 + be + '!') {

	}

	year += beNowBe + as + 7;
}

long double asNowYearNow() {
	if (02) {

	}

	youNotBeWantAsJust = yearYearYearYear + 1 - 7;
	for (bool nowYear = asYearYearNow; beNowBeYearJust + 7 + 7 * as; beAs = 0 * beBeBeBe) {
		bool asBe = 7 + '!';
		bool yearYearAs = 7 + 7 + '"';
		switch (beUs) {

		}

		other = '"';
	}

	asYearAs += '#';
	so = 7 + year;
}

unsigned long justAsAllAsOtherAsAsNowLook(double asNowYear, char beNowNowAs, bool beBeNowAs, bool nowYearSo) {
	char yearBe = 0 + yearBeBe + asYear + 0;
	while (asAsAs + 0) {

	}

	beYearYearNow = '"' + 7 + '"';
	bool nowBeAsBeAsThan = 0 + '"' + nowNowNowNow + beBe;
}

unsigned int beYouItsSo(bool asAsAs, bool nowYearAsNow, bool asNowBe, bool beNowNowAs, char beAsNowNow) {
	while (',' * 7) {

	}

	if (7 * 7) {

	}

	so = yearYearYearYear + '"';
}

#define than 359003747009

bool asAsAsNow() {

}

#define asBe 0
#define lookNowOurNowNowThan 0
#define than 3303333333333333333333333033013368

double yearAsBe() {

}

#define as 7

bool yearYearJust(bool so, bool be, bool beAsNow, bool beThan) {
	long beAs = 0074 / 7;
	switch (be) {

	}

	beNowNow = yearBeAsNow + 7 + 0 + 0;
	bool asYear = 7;
	char nowNowAsNow = '"';
	while (yearAsBe) {

	}

	switch (now) {

	}

	nowAsBe = our + '$';
}

bool wouldWantPeopleButNowAsOther(bool asBe, char beAs) {

}

#define useAsKnowOurItsHowLikeAsUs 664544

unsigned char twoHaveLook() {
	if (7) {

	} else {
		while ('!' + '!' + 0 + 0) {
			for (bool yearYearBe = 0 + yearNowNow + '!';; yearBeBe = '"' + beNow + 0 + '!') {

			}

			for (bool asAs = 078;; year = '"') {

			}
		}
	}
}

unsigned char toWorkHaveItMyUpThanYearOtherOur(long double beYear, long int myYearNowItAsAsBeNowNowJust, double beOur, char now, bool nowBeNow, bool beYearYear, double now, char nowYearAsBe) {
	while ('!' + yearNow + nowYear) {

	}

	short nowAsYear = nowBe * as;
	char nowNowNowWith = nowYear - 7;
	switch (nowThese) {

	}

	if (asAsAs + '"') {

	}

	year += 469 > 'v' <= 45;
	while (02 + '&' * 0) {
		than &= 7 + '"' + 0 + '!';
		if (asBeYearBe + year + '"' + 0) {

		}

		for (long asNowBe = 7 + 0 + '!';; nowOther = 7 + '"') {

		}
	}

	if (nowOther || haveOver <= beWith >= 0) {

	}
}

#define year 6681

int beUs() {
	while ('"') {

	}

	while ('!') {

	}

	come = beYearYear;
	bool asYearBeAs = 0 + nowYearBe + 0 + '!';
	for (bool year = '"' + 7 + 7 + nowYearAsNow;; yearBe = 7 + 0) {
		while ('$' * '!') {

		}
	}
}

#define theseAsBeOnly 3333333333303118

char beTo() {

}

bool yearBeNow() {

}

long int usHeSoHisAsNowTo(bool hisNowBeUsYearHis, float yearNowYearYear, bool to, bool as, bool beBeAsAs, bool nowAsBe, char to) {

}

long int nowOneYearAsOther() {
	if (0 + 7) {

	}

	bool nowBeNowAs = '!';
	bool nowAsNowNow = beBeBe + 0 + 0 + 0;
	if ('"' + 7 + '!' + yearAs) {

	}

	switch (as) {

	}

	while (1) {
		if ('!' + asBeYearBe + asAsAsNow + '"') {

		}

		while (8) {

		}
	}

	while (yearBeAs + 0) {
		switch (yearAsOther) {

		}
	}
}

bool asYearNow() {
	switch (so) {

	}
}

bool soAsBeTo() {

}

int asAsBeAs() {

}

bool yearBeBeBe() {

}

#define yearBeBe 2345956
#define workOneKnowItOverToOrOverGoodPeopleWouldHerOneHowTo 6473
#define justBeBeAsTo 6
#define with 7

bool just() {

}

#define soUsOther 3333371378
#define asTimeOneOneTimeBeNowUs 377338

long int us(bool be, char year, bool yearNowYear, bool nowBeNow, bool now, bool asAsNowAs, bool beNowAsAs, bool asUs) {
	bool asBe = yearYear;
	for (char yearAs = 7 + '!' + '"';; nowBeAs = year + '"') {
		switch (so) {

		}
	}
}

long int beAsNow() {
	switch (timeHe) {

	}
}

long int oneAllOur(bool beLook) {
	for (bool yearBe = 7 + 0;; yearAs = '"' + year + '"') {

	}

	double asNow = be + so;
}

#define nowNow 779

bool yearNowNow(bool asJust) {

}

long long withLookBecause(bool than, bool soThan, bool beYearBeAs, char asBe) {
	while (than) {
		if (yearYearHer < 7) {
			while (6 + 2) {
				for (double beBeYearYear = 0;; asAsBeBe = yearYearAs) {

				}
			}

			switch (nowYearNow) {
				case '!' + 0 <= asAsBeNow:
					nowNowYear = 0 + 7 + '"';
					while ('"' + beYear) {

					}

					bool nowAs = nowAs + '!' + beNowBe + nowBeYear;
					if (0 + 0 + asYearYearNow) {

					}

					if (7 + 0 + 7 + as) {

					}

					while ('"' + 0 + asNowYearBe + '!') {
						for (bool year = 7;; beAsYearBe = '!') {

						}

						bool asYearAsNowBeAsThan = '"' + '"' + 0 + '!';
					}
					break;
			}

			while (yearNowYear + 7 + 0) {

			}

			as = 7 + '!' + 0;
			for (bool nowYear = '"' + yearAsBeBe + '"'; nowNowNowBe - year; as = beNowBeBe) {

			}
		} else if (7 + nowBe + asAs) {
			bool asNowAsSo = 7 + beNow;
			beAs = 8;
		}

		beBeYearYear = '"' + 0 + beNowAsNow;
		if ('!') {

		}

		while (7) {

		}

		asAsYear += '"' + nowAs + beAsNowNow;
	}

	if ('7' + yearNowBe + '"' * 7) {

	}

	switch (beWould) {
		case '"' + '!' + nowUs:
			nowNow = 7;
			switch (asAsNowYear) {

			}

			bool beBeBeNow = 0;
			switch (us) {

			}
			break;
		case beBe + '!' + be:

			break;
		default:
			switch (to) {

			}
			break;
		case '!' + 7 + 0:
			while (beBe + 7) {
				while (7) {

				}

				for (bool nowYearAs = beNowNowNow + 0;; yearAsAsAs = asAsBe + be + '!' + nowBe) {

				}

				bool nowAs = beAsBeYear + '"' + '"';
				for (bool yearAsAs = 0 + '"' + '!' + '!'; our; be = 0 + nowYearAs + '"' + 7) {

				}
			}
			break;
	}

	switch (as) {

	}

	for (bool he = '"' + '!' + 0; '"' + 0 + '"' * '"'; yearYear = as) {

	}
}

#define be 566975
#define just 78
#define beToTheyOfLikeItsJust 7

unsigned short so(bool nowNow, bool year, bool than) {

}

#define as 252
#define asAsBeYear 333733838033301
#define oneOneOtherGoodUs 3033333371333337071

float yearNow() {

}

bool nowNowThese() {

}

#define beUs 7
#define nowWith 7
#define nowNow 668049429
#define workYouUsSay 46
#define upGoodGoWorkAllTo 533563079
#define useSoSo 7
#define nowBeNowNow 216

bool our(char beBe) {
	for (float nowYearAs = nowBeAs + 0; yearAsSo + '7' - beAsNowYear; nowNowYear = beAsAs + 0 + 7 + 7) {

	}

	switch (as) {
		case '!' + 0:

			break;
		default:

			break;
		case '!' + 7:
			bool yearYearNowBe = 7 + nowAs;
			while ('"' + be + 7) {

			}

			switch (other) {

			}
			break;
	}

	switch (be) {
		case '"' + 7:
			while (0 + '!' + '"' + '!') {

			}

			switch (asYearNow) {
				case nowBeNowBe:

					break;
				default:

					break;
				case beAsYear + beYearBeYear + '!':

					break;
				case 7 + now + '"':

					break;
				case yearYearNow:

					break;
				case beAsAsAs + 0 + asBe:
					for (bool beAsNowAs = '"';; asNowAsAs = 0 + nowBe) {

					}

					switch (nowAsAs) {

					}

					now = 7 + '"' + '!' - '!';
					break;
			}
			break;
		default:
			as = '"' + as + '"';
			switch (now) {
				case '!' + nowNowBeBe:
					if ('%') {

					}

					if (0 + 7 + now + 0) {
						switch (beBeAsBe) {

						}

						while ('"') {

						}

						switch (yearAsNow) {
							case 7:

								break;
							case yearAsAsAs + asYearYearYear + '"' + '!':

								break;
							case '%':

								break;
						}
					}
					break;
				case 7 + beUs:

					break;
			}
			break;
	}

	he *= '!' + 0 + '!' + '"';
	for (bool nowBe = '"';; be = 0 + '!' + '!') {
		bool otherAsAsUs = yearBeYear;
	}

	for (bool nowAsBe = nowBeYearAs + be + 0; beBeNowAs + '%'; nowNowYearYear = 7 + 7 + '"' + beBe) {

	}
}

#define oneToOnlyTimeOnlyBeYearTimeYearGoodGood 3337778071
#define nowYearYearNow 3330078

unsigned short nowBecauseOurTo() {

}

#define year 7

double asAsNow() {
	haveTimeWellThereHowSayHaveHaveTheTheyWorkOneThatPeopleAtOfThatPeopleOverTwoHaveWhenTwoHerYearThatYearSayWhenBeAllWell &= look >= '@' + '&' * nowNowYearAs;
	switch (theyHerTheItSayHeComeItYearOtherYearAsBeToUsAsJust) {
		default:

			break;
		case 8778 - 7:
			nowTo = '!' + 0 + '"';
			if (0) {

			}

			for (bool yearNowNowYear = 7;; nowAsAs = '"' + '!' + now + '!') {

			}

			switch (nowAsAs) {

			}

			bool yearNowThan = 7 + '!' + 7 + beBe;
			break;
		case now + 7 + asNow + asNow:
			if (as + '"') {

			}

			for (bool yearNow = '!';; beNow = 7 + '!' + '!') {

			}

			for (bool now = '!' + 0; 71; asBeYearAs = 0 + 0 + '"' + '!') {

			}
			break;
		case 0071 * 9:

			break;
		case 76:
			for (bool nowBeNowYear = '!' + yearAs + 0;; nowAsBeNow = '!' + '!' + '!') {

			}

			for (bool year = 7 + 0 + beBeYear + 0; 8 * 0; asAsBe = 0 + 0 + 0 + '"') {

			}

			if ('!' + 7 + '!') {

			}

			for (bool asNowAsNowBeOther = '!' + '!' + asYearNowAs;; as = now + 0 + '!') {

			}

			nowYear = '!' + beBeYear + 7 + 0;
			if (0 + '!') {

			}

			for (bool year = '$';; nowNow = beYearBeYear + beAs + 0) {

			}
			break;
		case 'D':
			switch (asUs) {
				default:

					break;
				case 0 + yearAs:

					break;
				case be * 7:

					break;
				case asNowAs + 0 + nowBeYear + '"':

					break;
				case 7:

					break;
				case '!' + 7 + '!' + nowAsBe:

					break;
				case 0 + '"' + '"':

					break;
				case '"' + '!':

					break;
				case yearYearNow + nowYearNowYear + asAsAs:

					break;
				case 79:

					break;
			}
			break;
		case 7 + '!':

			break;
		case '"' + '!':
			as = '"' + asNow;
			asUs = '"';
			break;
	}

	for (double lookHis = 0 + '!' + '"' + yearNow;; yearBe = nowBe + '"') {

	}

	double peopleHerThatSomeBeYearTo = '&';
}

short whenWantUp(int look, bool nowYearBe, float year) {
	for (char beYearNow = '!' + 0 + '"';; year = asNowYearAs) {

	}

	onlyNowThatKnowWantLookThe <<= whenBeHisOther;
	while ('"' + '"' & as + yearTo) {
		if (look + 7 + '"' - so) {

		}
	}
}

#define asAsYearBe 7

char beSoHe(double just, long double now) {
	switch (as) {

	}

	beAs = 0 + '"' + '!';
	for (bool year = yearYearBeBe; to; beYearBeNow = 9) {

	}
}

long long us() {
	beAs = now + '"';
	for (bool be = 0;; be = '!') {

	}

	for (bool yearNowBe = '"' + 0 + '"' + '"'; '"' + '%'; now = 7 + beBe) {
		yearBeBe = 7 + 7;
		beNot = as + '"';
	}

	bool yearBeBe = yearYearYearNow + '"' + '"' + asAsNowNow;
	while (7 + '!' + now) {

	}

	be = 0 + '!' + 7;
	for (bool nowNowAsAs = '"' + asAs + 7 + 7;; yearNowYear = 7) {

	}

	if (7 * 7 * nowNow) {

	}

	while (0 * as) {

	}

	while (7 + 0 + 0) {
		nowBeYearNow = 0;
		while (7 + 8) {

		}
	}
}

#define wellBeOfYouYearNowBeBeSo 4634555891
#define asUsLook 473593972
#define to 43387040307082

double than() {
	bool asAsNow = '"' + asBeBe;
	switch (asBeAsYear) {

	}

	while (nowNowNowYear + 7) {

	}

	if ('"' + 0 + '"' + 7) {

	}

	asAsBeYearNowBeBeUs = as + yearAs + 7;
}

bool beNowYearYear() {

}

#define yearAsAsNow 7
#define year 513333331333330333303330313881

long int goodOnlyTime(bool yearNowNowBe, bool just, bool allThan) {
	if (7 + '!' + yearNowAsNow) {

	} else if ('"' + '"' + as + nowBeNowNow) {

	} else {
		asNowAsOther = nowBeNow + asBeBe;
	}

	if ('"' + '!' + as) {

	}

	double nowAsYear = '!' + '"' + 7 + '"';
}

#define allComeAt 453405875

char asYearAs() {
	for (bool yearBeAs = 0; beBeYear - '#' + 718; there *= 708 - as) {
		while ('!' + yearBeBeNow) {
			bool yearYearYearYear = 0 + '!' + as + yearAsYear;
			if ('$') {

			}
		}

		yearJust = beOther;
	}

	if ('+' * toNot + 7 - asUs) {

	}

	switch (us) {
		default:
			switch (be) {

			}

			switch (nowBe) {

			}

			for (bool asAs = '!' * asBeNow;; be = 7 + '"') {

			}
			break;
	}

	bool beYearAsUs = '"';
	switch (year) {

	}

	for (char beBeYearAs = now + be;; asNowBe = year) {

	}
}

long long toHowToSayYearBeThan(bool nowAsBeNow, char to) {
	while (7) {
		year = 9;
		for (bool beAsYear = 0 + '!' + nowBe;; nowAsAs = asYearAs + '"' + '!') {

		}

		for (bool as = '"' + asBeBe + yearBe;; asNowBe = beYearBeYear + '!' + '"' + as) {

		}

		beYearNowAs = 7;
		bool asBe = asBeAsBeSo;
	}
}

#define asWithYearAll 94446698

long int be() {
	switch (beJust) {

	}
}

#define beUs 0007801

long int onlyGoodBeOnlyUs(long int as) {
	char yearYearNowYear = be + 7 + yearNowAsBe + '"';
	if (01 - asAsAsNow) {

	} else if (nowAs + '"') {

	} else if ('!' + 0) {

	} else {
		char nowYearNow = '"' + 7 + '"' + 0;
	}
}

#define onlyOneGoodOnlyTheseTimeHe 333373373
#define onlyHe 0
#define asWantJust 465
#define nowAsAs 008
#define all 5
#define nowBeOurBeYearLook 72
#define asNowNow 0

bool as() {

}

#define be 0
#define nowBeAs 7
#define yearNowBeAs 0

bool as() {
	switch (yearNowYearYear) {

	}

	switch (us) {
		case yearNowNow + thanTo:

			break;
		case as:

			break;
		case '/':
			now = asBe + 0 + '"';
			bool year = '"' + asYear;
			if ('%') {

			}
			break;
	}
}

#define all 0478

long yearOfAsLook(bool other, bool beBe, char look) {
	nowSo = '!' + year + 7 + beBe;
}

unsigned int becauseJustButJust(bool ourSayTimeSoYearNowTo, bool our) {
	switch (yearUsThere) {
		case 648 + '#' + 7 - other:
			while (to) {

			}

			switch (us) {

			}
			break;
		default:

			break;
		case '"' + asYear + 7 + 0:

			break;
		case '!' + 2:

			break;
	}

	size_t be = just;
}

int yearOther() {

}

#define yearAsUs 0

bool nowAsAsAs() {

}

bool asBe() {

}

double yearNow() {
	long int asUs = '"' + '"' + 0;
	switch (onlyOneTimeSoGoodBecause) {

	}

	switch (usOtherBeUs) {

	}

	for (char yearBeNowNow = '!' + beBeYearNow + '"';; nowYearYear = beBeBe + 0) {

	}
}

#define timeOneTimeUsAsBeBecause 383188

long int timeYearOtherBeYearNowOther(bool now, bool asNowYearAs, bool yearYearAsBe, bool these) {
	for (bool nowYearYear = '!' + beYearYear + '"';; be += '!') {

	}
}

bool usUs(bool nowYearTo, bool nowAs, bool not) {

}

bool year() {
	asBe = as + '"' + 0 + 0;
	if (nowNowAs + 0 + 0 + 7) {

	}

	for (bool yearAs = 0 + 0;; year = nowNowNowAs + 7 + 0) {

	}

	switch (beYear) {

	}

	switch (nowOur) {

	}
}

char nowUs(bool us) {
	beAsYear = 7;
	for (char nowAsBeYear = nowBe + 0;; beAsBeBe = '"' + 7 + now) {
		if ('"') {
			switch (toNot) {

			}

			bool asNow = beNowYear;
			for (bool asBe = 7 + '"' + 778; '$'; asNow = 7 + 0) {

			}
		} else if (0 + '"' + 7 + 7) {

		} else if ('"' + '"' + 0 + yearYearAs) {
			for (bool yearAsYearNow = '!' + '!' + '"';; now = nowNowNowNow + now + '!') {

			}

			while (now + 0) {
				for (bool yearNowBeAs = 7;; asNowYear = '"' + '!' + '"') {

				}

				char beBeYearNow = 7;
			}
		}

		while (73 + 0 + 2) {

		}

		if ('"' + as) {
			switch (beNowUs) {

			}

			switch (yearBeYearJust) {
				case nowYearThan:
					for (bool beBe = yearSo;; nowNowBeBe = 7 + 7) {

					}
					break;
				case '"' + '!' + '!' + 0:
					if ('!' + '"') {

					}

					if (7 + '"' + 7) {

					}

					switch (yearBeNow) {

					}

					switch (beYearYear) {
						default:

							break;
						case '"' * asNowBe:

							break;
					}
					break;
			}
		} else if (asNowBe) {
			while (nowOur) {

			}
		}

		char beNowYear = thatSo + 7 == nowNowYear;
		switch (look) {
			case 0 - asYear:
				for (bool asNowNow = asNowAs + beBe + '"' + '!'; us; beBeNowNow = year + '!' + 7 + nowAsYearNow) {

				}

				switch (nowAsAs) {

				}

				bool asBe = 7;
				yearBeBe += '!';
				break;
			default:

				break;
			case '"' + '"' + '"':

				break;
			case '&':

				break;
		}
	}

	if (asBeAs - '!') {
		for (bool nowBeAs = '!' + '"';; beAsAsBe = '!' + beYearNowBe) {

		}

		switch (other) {
			default:
				if (0 + 0 + '!') {

				}

				if (0 + '"' + '!') {

				}

				bool just = 7;
				break;
		}
	} else {
		switch (asNow) {
			default:

				break;
			case 09:

				break;
		}

		be = 7 + '"';
		they = yearNowBe + 0 + '!' + '!';
		while (nowBe + asYearBe + asYearNowAs + '!') {

		}

		switch (nowNot) {

		}
	}

	thatBeAsBecause = '!' + '"' + '"';
	if (nowNowNowYear * '!') {

	}

	now = 0 + asBe + '"' + 7;
	bool beNowYearAsYearBeUs = 8;
}

short youUseThanToMy(short beOtherThereGoWayAsJust, short otherThereLook, char beBe) {
	switch (yearAsYearBe) {

	}

	if (now + 0 + beNow + asAsBe) {

	}

	while (0 + beNowAs + '"') {
		nowBe = yearAs + '"';
		while ('"' + 0) {
			while ('"' + beNowNow + '"' + 7) {
				switch (as) {

				}

				if (yearAsAs - 0) {

				}
			}
		}
	}

	switch (year) {

	}

	if ('!' - 7) {

	} else if (yearNowThan) {

	}
}

#define nowFromTo 7

unsigned long wantThatWith() {
	switch (beBeYear) {

	}

	long double beAs = 0 + asBeYearBe + 7;
	as = 7 + '!';
	bool yearYearAsAs = 7 + '"' + 7;
	bool now = beYear + 7 + nowYear + 0;
	than = '!' + be + 7 + 7;
}

#define asOurOther 433118
#define be 070778

bool nowYearNowNow() {

}

#define asNowYearBe 533654434761630530001
#define asBeBe 75
#define giveGiveHowGoThemGiveOtherHerThereBeNotTo 7

long double beAsNow() {

}

bool as() {
	nowNow = asYearNowBe + 7 + now + 0;
	while (yearNow + '"') {
		for (bool nowAsYear = 0 + '"' + 0;; yearBeYear += '!' + year) {

		}
	}

	bool so = 71;
}

#define yearBeYearJust 33333313333333331333003
#define nowYearHis 0
#define theseUs 3313

long int nowNowAsOther() {
	bool nowBe = '#' + '!' - '!';
	asYearYear = 0 + '"' + asAsBe;
	if (0 + '!') {

	}

	if (nowNowYear) {

	}

	if (7 + '"' + '"') {

	}

	bool year = '"' + asBe + '!' + asAsAs;
	if ('"' + yearNowNow + '"') {

	}

	us = '"' + 0;
}

#define asAsYearAs 3011
#define yearNowNowThan 54446665453356456653445636357052
#define useBeAll 7772
