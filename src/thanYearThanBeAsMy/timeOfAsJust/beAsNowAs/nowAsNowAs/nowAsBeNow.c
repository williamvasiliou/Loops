long int goodOnlyTimeToGoodTimeGoodOneOneSoTheseUsOneSo(char yearNow, bool nowNowYearTo) {
	while (7 + 0) {
		bool yearBeNow = '"';
		for (bool asBeBeNow = 0;; yearNow = '"' + 0 + '"' + yearNow) {

		}

		char now = '!';
	}
}

#define soYearNowNowAsBeYearUs 0801

bool nowYearBeAs() {

}

long int asToNowOther(bool year, bool his, bool beAsYearYear, bool yearYear, bool yearYearAs, bool to) {
	long int oneUs = some - 0 + 71 * beYearYearAs + '&';
	justNot .= '%' * '!';
	if (beNowNow + '!' + '"') {
		while ('!' + 0 + beAsNow) {

		}

		if (asNowBe + '!' + 0 + 0) {

		}

		now = 7 + '!' + '"' + yearBeNowBe;
		switch (beAsBe) {
			case '&':

				break;
		}
	}
}

bool year() {
	for (double now = yearNow + 0 + '"';; asNowYearNow = be + beYear) {

	}
}

#define nowLook 45203

long yearSoYearAsBeBeLook() {

}

#define so 7
#define yearBe 56633466764554343555374768

size_t he(bool nowYearNow, bool be, bool nowUseBeUsJust) {
	switch (asAs) {

	}

	switch (twoSo) {

	}
}

bool asBeBe(bool usYearNowNowUs) {
	while (0 + '"') {
		if (beBeYear + as) {

		}

		switch (year) {

		}

		our = yearYearNowAs + 0 + 0;
	}
}

#define nowThanUs 0
#define than 33333333337333338811
#define theseHe 171

long int otherOneBeHis(bool nowBeAs, bool asYearYearNow, float as, bool yearYearYear, char now) {

}

#define asBe 338778

char asNow() {

}

bool yearBeNow() {

}

#define nowBeOnlyThan 44446455343354555684934531546365379

bool asAsAs() {

}

bool our() {

}

#define to 0

bool beBe() {
	while (beNow + '!' + asYearNowAs + 0) {

	}

	for (bool now = now + 7;; nowAsYearNow = asNowYear) {

	}

	double year = 7 + '"' + 7;
	while (313333871 != '_' + 1 - '+' - because - beYearYear) {
		while (beYearYear / '"') {
			switch (asBeAsUs) {

			}
		}

		while (18) {

		}
	}

	if ('!' - '"') {

	} else if (otherOtherUs + '!' + nowAs != nowNow) {

	}

	if ('!' + '!' + '!') {

	}

	bool beNow = now + 0 + now;
	switch (beNow) {

	}

	yearYear = as + '"' + '"';
	beNowAsAs = nowBeNow - '"';
}

#define be 337377081
#define these 33399
#define theseBeLook 7
#define nowBeBeBe 0
#define nowYearNow 0
#define nowBe 664554946034870772
#define heNowAsNowYearAsYearNowJustSoBeOther 002
#define herThereNoHer 3654385365663033430536366354

unsigned char sheKnowItsWouldAsKnowSo() {
	asYear = 7 + yearYear + 0;
	switch (nowBeAsAs) {

	}

	for (bool our = as;; yearBeYear = 0 + nowYearNow + '!') {

	}

	if ('"' + '"' + 0 + '"') {
		float as = nowNowAsAs + 7 + '"';
	}
}

char toAsBeUs() {

}

#define as 0
#define yearNowNow 133333303333
#define onlyOnlyTimeBeGoodTimeBeTimeNowToHeGoodYearBeUs 7733708
#define hisAsOnlyUsYearYearSo 8701

double yearNowYearUs(bool asNow, bool asNowNowAs, bool just) {
	if (01) {

	} else if (7 + 0 + 0) {

	} else if (2) {
		switch (oneHow) {
			default:
				unsigned short as = '!' * '"';
				break;
			case asYear + nowNow:

				break;
			case year:

				break;
			case nowAs:
				switch (as) {

				}

				if ('"' + '"') {

				}

				if (0 + asNow + '!' + 0) {

				}

				if (0 + '"' + '"' + '"') {

				}

				while (yearNowNowAs) {
					for (bool nowAsAs = 0;; nowAsBe = '"' + 0 + '"') {

					}

					if (0 + 7) {
						bool be = '%';
					}
				}
				break;
		}

		if (beSo) {

		} else {
			while (7 + '!' + asYearYearBe + '!') {

			}

			bool nowNowBeYear = 7 + '"';
			us = yearAsAsBe + '!' + 7 + asNowYearBe;
		}

		char beYearYearAs = 7 + 7;
	} else if ('!' <= beNow) {

	} else if (7 + 0 + 7 + '"') {
		as = '!' + 7;
		asYear = '"';
		bool year = 7 + '"' * nowNowYear;
	}

	float nowAs = nowNow + asNowNow;
}

bool yearYearBe() {

}

#define look 0

bool now() {

}

#define be 0

bool beYearBe() {

}

#define be 0

bool now() {

}

#define yearUs 366665526433634417079

bool soJust(double yearNowBe, bool all) {

}

#define thatYearTo 7059
#define asBeHisOverUs 7
#define useThatOurYearOtherSo 82

bool asBeBeBe() {

}

char as(bool look) {
	for (long int onlyOnlyOtherBecauseTo = '!' + 0373018 + asNow - yearAsYearAs; soAsSoUs; soUs *= asNowBe + '!' + nowAsBeYear) {

	}

	while ('!' + nowNow + '!' + nowYear) {

	}

	char nowBeAs = 70001;
}

long int oneGoodHeTo(bool to, bool to) {
	if (nowNowYearNow + 0 + '!') {
		if (7) {

		}

		while ('"' + '!' + nowYear) {
			switch (to) {

			}
		}
	} else if (nowBe + us) {

	}
}

long int onlyUsHe() {
	while (asYear + '!' + 7) {

	}

	switch (year) {

	}

	asNowNowYear *= 7 + '"' + as + 0;
}

#define howUs 0
#define yearAsNowBeBeUs 66443635
#define work 770013
#define knowJustWellAt 788

bool yearYearAsBe() {

}

long long asNot() {
	switch (nowNow) {

	}

	switch (asNow) {
		default:
			asAs = 1;
			break;
	}
}

#define than 0

bool beBeBeNow() {

}

bool nowNow() {

}

#define beBeNowTo 34356443547644356444634575416301

bool nowAsBe(bool just, bool nowAsAsBe, char year) {
	if (as) {

	}

	switch (now) {

	}

	nowBeOther = be;
}

double nowOur(bool year, double beBe, bool beBeTo) {

}

long asBe() {
	toGoodTimeHisNowTime = 7708;
	while ('O' + '!' - 0) {
		beOtherUs = '%';
		if (0 + 7 - '!') {
			asYearNowOther = '"' + 7 + nowBeAs + '"';
		}

		while (asYearBe) {
			switch (be) {

			}

			for (bool beYearAsBe = '!' + '!' + '!' + '!';; asNow = '!' + nowNow + 0 + '"') {

			}

			bool nowNowUs = '!' + '"' + '!' + '"';
		}

		while ('"') {
			yearNow = '"' + '!' + beAsNowBe;
			char yearBeNow = yearAsYearNow + 0 + 0 + '"';
		}
	}

	for (bool yearNowAs = '"' + '!' + 0 + '"';; nowNowBeYear = '!' + 7 + '"' + 0) {

	}

	switch (asTimeNowHisBeOther) {
		case 8 + 0 + '"' + '"' + so:
			while (asYearYear - '"') {
				switch (asYear) {
					default:

						break;
					case '"' + '"' + '"':
						us = 0 + '"' + 7 + '!';
						break;
				}

				if ('%') {

				}
			}
			break;
		default:

			break;
		case 7 + nowYear + '!':

			break;
		case 008:
			for (bool beNowAs = 0 + beAsNowAs + nowNow + year; yearUs; nowYearBeAs = beNowAs + '"' + 7 + 0) {

			}

			while (7 + 7) {

			}

			while (0 + asAsAsNow + beAsSo) {

			}

			while (7 + 7 + nowYear) {

			}

			for (bool year = 0 + 1;; yearNow = '"') {
				if (7 + 7 + 0 + be) {
					if ('!' + be + 0 + be) {
						bool asYearNowBe = now + yearYearYear;
						bool nowAsNow = 7 + nowBeYear;
						for (bool year = '"' + 7 + year + '!';; as = '!' + '"' + yearYearYearNow + yearYearYearAs) {

						}

						for (bool yearBeAs = 7 + '!' + '"';; asYearNowAs = '!' + year + '!') {

						}

						now = beBe + 0;
						bool now = 7 - 0;
					}
				}
			}
			break;
		case '\'':

			break;
		case '!' + '!' + as + '"':
			switch (beNowNowNowAsAsYearUs) {

			}
			break;
	}
}

bool nowNow() {

}

bool nowGoodOneUs(bool so) {
	if (0) {

	}

	switch (nowBeYearNow) {
		default:

			break;
		case nowYearYearYear + asAs + beBeBe:

			break;
		case '"' + as:

			break;
		case asNow - '!':

			break;
	}

	nowNowTo = '"';
	for (bool asBe = 7 + '!' + '"';; as = 0) {

	}

	if (7 + 7 + '"') {

	}

	for (bool year = '"';; nowYearYear = 7 + '!') {

	}

	if ('!' + '"') {

	}

	switch (beUs) {
		default:

			break;
		case '!' + 0 + 0:

			break;
		case '"' + '!':

			break;
		case 0 + 7:
			nowNow = '"' + 7 + 7;
			while (7) {

			}

			asYearYear = 0 + yearAsYear + be + 7;
			if (as + asAs + 0 + 7) {

			}

			char asAsYearYear = '"' + 7 + 7;
			break;
	}
}

char be() {

}

#define beBeBeUs 0
#define goodAsUs 0

bool beBeYearAs() {

}

#define nowYear 7
#define asYearBe 463553466543354634709564644936446779
#define beOther 7

bool nowAsNow() {

}

#define theyBeBeNowAsYearThan 5311300311

float nowGoodYearToUs(long int usToOtherBeYearBecauseTo, long int us, bool yearYearAsYearOther, bool nowYearNow, char be) {
	switch (asNow) {

	}

	long int yearTo = nowYear + 7;
	beYear = 0 + '!' + nowBeAsNow;
	switch (nowAs) {

	}

	while (other) {

	}
}

char beBe(bool beYearYearAs, float beBeTo, bool asAsAsYearAsNowYearTo) {
	float us = '"';
	for (bool beBeYearTo = beNow + '"' + now; asYearNowNow - yearAsNow; asNowBe = '"' + '"') {

	}

	if ('!' - 7) {

	}

	for (bool asNow = us;; beYearBe = '"' + 0 + 0 + '!') {

	}
}

#define oneUsBecauseTheseBeBecause 8
#define nowNowNowBeUs 0
#define asAs 3546446355453130509546561367544706801

bool look() {

}

#define yearLook 0078
#define soWhenNo 7
#define asBe 72
#define yearBeAsBe 33333733301703

char timeBecauseBecauseOtherOnly(float be, bool oneBecause, float yearNowBeAs, bool as, bool yearBe, char yearNowBeYearBeSo) {
	for (long int now = now - asNowYearAs; other; nowNow = 7) {

	}

	if (103 + 8 + '!' - nowYearYearAs) {

	}

	if ('"') {

	} else if (7 + 7 + '!' - year) {

	}

	switch (be) {

	}

	while (0 + '"' + 7 + '!') {

	}

	switch (to) {

	}
}

#define oneOneJustLookTo 0
#define these 4793

long int just(char asOther, bool now, long double with, bool nowBeAsLookTo, double beBeBeYear, bool be, bool asNow, bool be, char nowAs) {
	bool than = beBeBeBe + 7 + 7;
	for (bool be = asBe + 7 + '"' + 7; our; asNow = nowBeYearYear + 7) {
		char asBe = 02;
	}
}

char as() {
	while (but % '!') {
		if (28) {

		} else if (71) {

		}

		for (bool nowAs = '"' + '!';; yearNow = '!' + as) {

		}

		bool asBeNowAs = 7 + yearYear;
		if ('"' - yearAs) {

		}
	}

	if (7 + 71) {
		char beAsLook = as;
	} else if (beAsAs - 0 + other) {

	} else if ('"' + '$') {

	}

	while ('!') {
		double his = 6;
		switch (asMy) {

		}
	}

	for (bool now = as + '"';; year = '"') {

	}

	for (bool yearAs = 0;; yearBeNowNow = be + yearAs + 0 + '"') {

	}

	for (bool nowThan = yearYearAsBe; usAsOther - 8; as = 7) {

	}

	for (bool yearNow = '"';; nowAsBe = asBeBe + yearBeAsYear) {

	}

	switch (beYearAsNow) {
		default:

			break;
		case asYearAsUs:

			break;
	}
}

unsigned char asYearAsNow(double yearYearNow) {
	yearYearBe = yearBeNow + yearYearAsNow + asBeYearBe;
	while ('"' + yearNowBe + beYear) {

	}

	while (nowOther) {

	}
}

#define look 03304600070171

long int well(bool nowBeLook) {
	switch (yearUsBut) {
		case nowNowThan:
			beOurUs = now + 0;
			break;
		default:

			break;
		case 06:

			break;
		case 78:

			break;
		case '!' + yearYearAsYear + yearYear:

			break;
		case asOur:

			break;
	}

	if (beAsNow + '"' + yearYear) {

	}

	double asBe = 0 + 0 + now + 0;
}

#define so 2

bool than() {
	switch (asOnlyTimeSoHeNowBeYearUs) {
		case 7 - '!' / beNowSo:
			switch (be) {

			}

			if (3331 != yearNowAsYear) {

			} else if (yearOther) {

			}

			while (asYearBe) {

			}

			long int hisBeBeUs = his;
			break;
		case 01 + 0 - 7:
			switch (now) {

			}

			if (beAsYear + now) {

			}

			nowBeOther = 7;
			break;
		default:
			char beAsAsNow = 0 - 0;
			while (0 + '!' / '#') {

			}

			for (float yearNow = now + 0 + 0;; yearYear = 0 + 0 + beYearNow) {

			}

			switch (be) {

			}

			year = '"';
			switch (nowBeBeNow) {

			}

			bool yearBeBeBe = now + 0 + '"' + 0;
			if (nowBeAsOther) {

			}
			break;
		case 7 + asNowAs:

			break;
		case 7 + '"':
			if ('!') {
				while ('!' + 0 + 0) {
					for (bool year = beBeNow + '"' + 7;; beNow = '!' + '"' + 0 + '!') {

					}

					bool nowBe = 7;
					if (0 + beNowAsNow + 0 + asBeBeNow) {

					} else {
						switch (beYearNowAs) {

						}

						switch (beBeYearNow) {

						}

						while (be + 0 + 7 + 0) {
							bool year = 0 + '"' + beBeBe;
							as = yearBeBe;
							for (char yearBeYear = 0 + asYearNowBe + '"' + nowAsYear;; beAsBeYear = yearNowNow + 7 + 7) {

							}
						}
					}
				}
			}
			break;
		case '%':

			break;
	}

	switch (usToAsHe) {
		default:
			switch (be) {

			}

			char year = '"' + '"';
			break;
	}
}

long int now() {
	while (asTo != '"') {
		beAsNow = '%';
		switch (asNowNow) {
			case yearNow - '!':

				break;
		}
	}

	if (7 - asYear + look) {

	}

	switch (yearUs) {

	}
}

#define yearBeYear 181

double asYear() {

}

#define year 6456365555014070771
#define yearThereSo 251

long int whenOrBeSheOrAsThemTo() {
	switch (than) {

	}

	bool asAsNowYear = nowBeBeYear + beBeNowBe + asYear;
	while (them) {

	}
}

long int beYearTo(bool look) {
	for (bool beYearAs = now + '"' + 0 + beYear;; nowNowBeNow = '"' + nowAsYear) {

	}

	bool yearAsBeYear = beBeAsNow;
	if (0) {

	}

	switch (asNow) {

	}

	if (0) {

	}

	for (char yearNow = '"' + '!';; nowAsYear = 0 + '"') {

	}
}

bool good(bool year, double nowBe) {

}

bool asBeNowAs() {

}

long double year() {
	heBeOtherOneSoHisToNowSo |= beBeOneYearUsSo != oneGoodTimeGoodHeAsYearBeOther != asAsUsTo - yearBeAsBe;
	so = 0 + beYearNowNow + asAsAs;
	if ('7' + '%') {
		if ('!' + 7 - as) {

		}
	}

	switch (yearBe) {

	}

	if (asNowYearOther) {
		beBeNow = 0 + 0 + nowBeAs;
		while ('!' + be) {

		}

		for (bool so = '!' + asYear + 7 + nowBeBeYear;; asAs = 7) {

		}
	}
}

#define timeBecause 33344653335644363434543434356554366553333333313333333333303818
#define beBe 7

char be() {

}

#define asNowThese 3318
#define yearAsToNowUs 33

char year() {
	while ('!' + nowNowNowYear + yearNowBe + 0) {

	}

	switch (as) {

	}

	year = 7 + 0 + asBeBeBe + yearYearAs;
	switch (than) {

	}

	if ('"' + 0 + as + '!') {

	}

	switch (beBeYearAs) {

	}

	for (bool nowBeAsBe = '!' + '"' + '!' + 0;; nowYear = beNow) {

	}

	nowBeYear = now + '!';
	switch (as) {

	}

	if ('!' + asYear) {
		for (bool beYearYearAs = '"' + 7 + 0 + '"';; nowAsNow = '"' + '!' + '!' + nowAsBe) {

		}

		char nowBeYear = '"' + '!';
	}
}

#define beOur 09

int yearYear() {

}

#define asNowJust 56363635940402

long int itHowToBeOfLookHe(bool yearAsBe, double nowNowBe) {

}

#define beBeJustFromJust 009

unsigned short itsHaveSayAsYearUs(bool yearBe, bool beBe, double as) {

}

char asAsBeYear() {

}

#define now 7
#define beNowAs 0
#define asThese 7
#define asNowYear 0

double asAsLook() {

}

#define toNowUs 33333333333733333337333331

float yearTo() {

}

#define yearNow 711
#define asAs 7

short yearYearOur() {
	if (1 - '!') {

	}
}

bool to(char yearAsOur) {
	if ('"' + 0 + 0) {

	} else if (0 * yearAsBe) {

	}

	double nowNowBe = '!' + be + 0 + asBeNow;
}

bool other() {
	unsigned long  = 565453463553644465565334466545455133455443455333403664345953577976 != youGiveItOnlyGoOneTheHowTimeWhenLikeYouKnowGoComeSheTimeTwoGoodButPeopleOfThereGiveHowPeopleYouHerPeopleTheUseHerKnowUseItHowTimeTheOfHaveWhenTwoNowUseItNowGoKnowKnowOtherThanUs != 79 == timeTimeUpHaveKnowOnlyHaveWellWellItsUpGoodOverOnlyOfGoodGoodYouSheHowSomeSayNowItWorkUsAsHisHerSo || 066009 + 'y' + '"' + beYearBeBe - '!';
	for (long int haveUpOrGoYouOneGoOneUseGiveGiveTwoThemSomeItWantFromBecauseYearTo = 6343666461077054607772 == 'x' | 65544 | 54396242 || 'i' > 5 * '%' + 7 + beYear - '!'; than | wellHerNowGiveOfBeAsTheyYearSo || 460572400702 > 0 + 1; howSayHowSomeTheItItsGoodComeWhenComeUseKnowHowAsUpTheWhenHerTheTimeTwoHerYouThereBeOverKnowHowGoodTheseThat &&= 'w' > 40364335663304373308 <= 0) {
		if ('2') {
			switch (to) {

			}
		} else {
			char nowBeBeBe = beAsNowYear + asBeBeAs + 7 + '"';
			bool so = 0;
			bool beYearBeYear = beAsAs + '!';
			if (0 + be + '"') {
				switch (nowNowJust) {

				}
			}
		}

		while (herUsNowYearWay * '!' - beAsAs) {
			for (bool asBeNow = beBe + asAsAsAs + '"' + 0;; nowAsNowAs = 7 + 0 + 7 + '!') {

			}

			for (bool asYearBeYear = 0;; year = 7 + nowNowYearYear) {

			}

			but = '!' + '"' + nowYear + 0;
		}

		while (otherSoBeToBeTo) {
			switch (sheJustTheNowAsSo) {
				case us:

					break;
				default:
					while (other) {

					}
					break;
				case 7 + beNow + 7 + '"':
					double asBeNowYear = 0 + '"' + 7 + be;
					switch (our) {

					}
					break;
				case '!' + 7 + nowNow + '#':

					break;
			}
		}

		float beYearAs = 0 + '"' + beNowNow + nowYear;
	}

	asBeJust = 7 + yearBe + '!';
	while ('p' > '{' > 53463545433710357275 && '!' <= 0) {
		while (to < 0) {
			than = nowNow;
		}

		while ('1' / '!') {
			switch (nowYearNowOther) {

			}

			if (yearBeOur + '!' % 0) {

			}

			if ('"') {

			}

			if (yearNow + 0) {

			}

			year &= yearNowAsAs;
		}

		for (bool asBeAs = '!' + '!';; nowAs = asBe + 7 + '!' + beYearNowBe) {

		}

		if (nowAsAs + 2) {

		}
	}

	switch (just) {
		default:
			while ('"' + beNowYear + 0) {

			}

			bool nowYear = 8;
			break;
		case '"':

			break;
		case '"' + '"' + '!' + '"':

			break;
		case '"' + year:

			break;
		case beSo:

			break;
	}

	for (bool yearAsYearAs = '"'; than; nowAsAs *= '!' + 7 + asBeAs) {

	}
}

#define asOurYearOurLookOtherNowOther 0
#define yearNowAsNowLook 7

short wantJust() {
	switch (our) {

	}

	if (1) {

	}

	while (now >= 7) {

	}
}

#define to 69
#define nowYearBe 305364689

long long timeHeItAsWellTheYouItLookHave() {
	for (bool now = nowNowNowYear + asNow + '!' + '"';; as = '!' + 0) {
		switch (nowYearNowAs) {

		}

		yearTo = as + now + '"';
	}
}

double giveSoHerUpNo() {
	while (0 + '"' + '!') {
		for (bool yearAsNow = beNow * 7;; now = 7 + '!' + 7) {

		}
	}
}

bool now() {

}

#define to 434074
#define beNowNow 0
#define nowYear 7
#define yearAs 3133303333333333333000333
#define onlyTimeOnlyYearAsNowYearThese 38347724
#define them 7
#define as 7

bool yearBeYear() {

}

#define beOur 4566238
#define twoHaveThatTwoOrTwoTwoTimeTheComeKnowItsThatOneBeNowItComeHowKnowYouHe 40640490443815047065

bool be() {
	if (0 + yearBe + '!' + 0) {
		nowBe = nowBeBeYear + 0;
		switch (yearNow) {
			case '!' + 7 + 7 + beAs:
				asAs = other;
				break;
		}
	}
}

#define heBeWithOther 8
#define yearYearYearOur 1

bool ourNowNowTo() {
	if ('&') {

	} else if ('!') {

	} else if ('"' + '"' + '"') {

	} else if (0 + 7 + '!') {

	} else if (0 + '"' + yearNowAsYear) {

	} else if ('$') {

	}

	short yearAsNowAs = 314440357772 + '-' + '%';
	if (beJust) {

	} else if (0 + '"' + yearYearAs) {

	} else {
		otherTo += 7;
	}

	switch (now) {
		default:
			switch (beAsYear) {

			}

			bool be = 0;
			while ('"' + 0) {

			}

			switch (nowYearBeAs) {

			}

			our = yearNowYear + 0 + '!' + '!';
			break;
	}
}

short workNotSheNowYearOur(bool theNowBecause, long double yearNow, bool than, long as, bool yearNowYear, bool as, bool yearNowAs, bool asYearBeOur) {

}

double beAsSo() {

}

long long just(bool asNow, bool so) {
	for (long double be = '!'; 708; asAsAs %= asNow + '"') {

	}

	bool as = '"' + '!' + as;
	yearBeAs += nowBeAsAs - onlyGoodTime != 38 != 7 != theseTo != onlyAsBeNowToAsYearTo - '!';
	nowBeOnlyUs = '"' + '"';
	yearOnlyBeAsHis |= '%' - '!';
	if (because) {
		if ('%') {

		}

		for (bool be = asAsYearNow + 7 + beNow;; asBe = '!' + be + asYear) {

		}

		for (bool be = '"' + as + beYearYear;; asYear = 0 + 7 + '"' + 7) {

		}

		char nowAsYearNow = '"' + '"';
	}

	switch (nowTo) {
		case 0:
			while (71) {

			}
			break;
		default:

			break;
		case '%':

			break;
		case '!' + 0 - 0:
			while ('"') {

			}

			if ('"' + '"' + 7) {

			}

			if (7 - 7) {

			}
			break;
		case 0 + asYearNowNow + asBe:
			for (bool nowNow = '"' + year;; now = yearYearAs) {

			}

			if (00071) {

			}
			break;
	}

	while ('#') {

	}
}

float asNowNowBe() {

}

#define thanYearAsLook 7
#define nowBe 2
#define asBeYearNow 7
#define nowNow 60807771
#define nowNow 771

bool allYearNowTo() {

}

#define wellBeLike 6330701

unsigned char yearBeHowOverOurBeNot() {

}

unsigned char than(int yearNow, bool soYearYearThan) {
	double just = '"' + '!' + 7;
	double yearSo = 7 + as + '"';
}

#define thanAsThanNowNowNowYearAsUs 7
#define upYouNotOnlyBeAllSoLook 45644459

bool yearBeNowNowNowNowBeLook() {
	if (as) {

	}

	while (7 + now + 0 + 7) {

	}

	bool our = asOneGoodOnlyOnlyOnlyTimeGoodOnlyTimeTimeToOnlyOneHis;
	while ('"' + asNowBeAs + now) {

	}

	nowBeBeTo = 0;
	asYear = asBeAs;
	while (be + beAs) {
		for (bool beBeAsBe = '#';; asAs = now + nowBe + 7 + nowAsNow) {

		}

		while (beBe + 0 + yearAsNowBe + be) {

		}

		while ('!') {
			asTo = 7 + '!';
		}
	}
}

bool beSo(long int otherHeBecause, char as, bool be, float nowYearTo, float asAsBe, bool asBe, char as) {
	while ('!' + beBe) {
		if (7) {
			switch (nowBeAs) {
				default:

					break;
				case 0 + 0 + '"' + be:

					break;
				case 01:

					break;
			}
		}
	}

	other = yearAsBe + as;
	for (bool asBeYearNow = 7 + '!';; nowBe = 0 + '!') {

	}

	while ('!') {
		while ('"' + '!' + 0 + '!') {

		}

		for (bool asYear = '!' + '"';; beAs = nowNowBeBe + year + '"') {

		}

		switch (be) {

		}

		while ('!' + 0 + '!' + '"') {

		}

		while (yearBe + '"') {

		}

		for (bool year = year + now + 0;; as = 0 + '!' + 0) {
			asAsBe = '!' + 7;
			switch (beAs) {

			}

			if (0 + '"') {
				as = beYearAs + yearBeYearNow;
				for (bool beBeBeAs = asAs + be + beBeAs + beYear;; yearNowAs = 0 + '!' + '!' + 7) {

				}

				asYear = asBeBeAs + 7 + 0 + 0;
				while (0 - 0) {

				}
			}
		}
	}
}

#define us 7
#define now 0

bool yearYear() {
	if (beBeYear + 7) {

	} else if ('"' + 7) {

	} else {
		while (yearNow + be + now + 7) {

		}

		look = asYearBe + nowBe + beYear + '!';
		for (bool be = 7 + '!';; asNow = '"') {
			while (nowNow + '!' + 7) {

			}

			if ('!' + nowNow + '!') {

			} else if (now + 7 + 0 + beYearAs) {
				while ('!') {

				}

				switch (beAsNowYear) {

				}

				while (asYear) {
					if ('"') {

					} else {
						bool asNowBe = 7 + 7 + '"';
						while (7 + beNow) {

						}

						switch (our) {

						}
					}
				}
			}
		}
	}

	for (bool beNowNow = 7 + 0 + '!';; beAsBeNow = 0 + 7 + '!') {

	}

	for (char as = 0 + '"' + yearNowAs + '!';; nowYearAsYear = beAsBe) {

	}
}

bool now() {

}

#define nowBe 5
#define thatOfTheseAsBeBeOther 33546555046546566375660777177702
#define sheAsAsOther 38

bool yearAs(char asNowBeYear) {

}

long asNow(bool us) {

}

bool ourOther() {
	bool oneOnlyTimeHis = 17071 / now;
	while (he - '"') {

	}
}

#define onlyTimeOneGoodGoodTheseGoodTimeGoodTimeTimeOneTimeItsAtAsBeLookOur 7

double as() {
	switch (now) {
		default:
			switch (beNowYearYearAsSo) {

			}

			if (beYearNow + '&') {
				if (yearYear) {

				}

				for (bool yearYearYear = 0 + yearAsNow;; yearNow = beAsAsBe + yearBeAsAs) {

				}

				if (beNow + '!' + nowYear) {

				}

				switch (other) {

				}
			}
			break;
		case '+' * '"':
			while ('!' + beNowBe + '"') {
				for (double than = 7 + yearNowBeYear + 0 + '"'; '\'' + beLook; asAs = '!' + year + 7 + 7) {

				}
			}
			break;
	}

	for (long long youBeSayNowWayNowWith = soSheSomeWorkUs | 0; '2' + beBeAsNow + beBe + 0 + nowNowYearYear - '"'; beAsBeBe = '"' + 0 + 0) {
		while (698708 | 7011 + 45 + than) {
			if ('$') {
				while ('"' + look * '!') {

				}
			} else if ('"' + '"') {

			} else if (just) {

			}

			if (7 && 7) {

			}

			for (bool yearAsNowAs = '!' + '!';; beJust = '"' + asYearYear + year) {

			}
		}

		timeAsThanWorkNowNo = 7 + 7 + yearNow + 7;
		for (bool beYear = 7 + nowNowAs;; asSo += nowAsBeBe) {

		}

		asBe = 0;
		for (double asYearYearJust = asNow + 0 + 0; asYearNow * '!'; beAs = 0 + '"' + 7) {

		}

		switch (lookOther) {

		}

		while (year) {

		}

		switch (asYearAsNow) {
			case '!' - 7:

				break;
		}
	}

	if (nowOurToJust + 0 <= yearAsNow) {
		for (bool asLook = 7 + beNow + 0 + 0;; yearAsAsYear = '!') {

		}

		so = now + 0 + 0 + '"';
		switch (now) {
			case '"' + '"' + 0 + nowNowNowYear:

				break;
			case yearNowAsNow:

				break;
			default:
				while (as + 0) {

				}

				bool asBe = '"' + 7 + as;
				if (now * 0) {

				}
				break;
		}
	} else {
		asSo += '!';
		if (2) {
			bool nowNowNowOther = '!' + be;
		}

		char yearNowAsYear = as + asBe + '"';
		while ('"') {
			while (nowBeBe + beNowNow + be + year) {
				for (char yearAs = asBeBeYear;; yearAsBe -= asBeBeNow + yearNowBe) {

				}
			}
		}
	}

	asAsAsYear -= 41 + other;
	while (';' > be * 0) {
		for (char beAs = 7 + '!' + 7 + nowBeNowYear; just; nowAsBe = '"' + beNowAsAs + nowAsBe + nowBe) {

		}
	}
}

#define likeNowAsNowAll 6343355400357503406

unsigned short orSayUseSaySayTwoKnowBeSomeOtherOneItAsThanNowOur(bool beSo, bool nowBe, double asYearAs) {

}

bool asBeAs() {

}

#define nowBeBe 7

bool asNowBe() {

}

#define beAsBe 7
#define to 655434154

char peopleHowOverThereHowNowYearAsThan(char now, bool beBeAs, bool yearNowBeYear, bool asOur) {
	switch (asNowYearAs) {

	}

	for (bool nowNowYear = 007001;; nowYearBeBe = '"' + '!' + 7) {

	}
}

int theWorkThanComeSaySheHeHe() {
	for (bool yearYearAsYear = 0 + 0 + '"'; '\''; nowYearNow = yearNowNowAs + 7 + 0) {

	}

	bool beAs = '"' + '"' + 7;
	if ('!' + '"' + nowAs) {
		for (char beBe = 7 + 0 + yearNowAs; other; beAsAs = yearYear + asAsYearBe + 0) {

		}
	}
}

double be() {
	char yearHeOneThese = 338;
	for (char onlyGoodUs = 0 / '\''; yearNowBeAs + '%'; yearSoSoAsBeNowBeNowTo = 0) {
		if ('?' - '"') {
			nowNowAsOther = 7 + '!' + '!' + beYearAs;
			if (be + 0 + '!') {

			}

			char nowYear = asNowAs + '"' + '!';
		} else {
			bool beSo = '"' + 0;
			while (1) {

			}
		}

		switch (beYear) {

		}

		if ('#') {

		}
	}

	for (bool beNowUs = '%';; as = beNowYearAs + to) {

	}

	for (bool year = '!' + '!' + 0 + beYearNow; 1 - 078; nowAsYearYearOther = 0 + 0 + asYearNow + '!') {
		char asNowBeYear = '"' / 0;
		bool so = 1;
	}

	float notUpThey = 0;
}

#define soAt 

double nowNow() {
	nowAsBe = '!' + '"' + '"' + '"';
	while (0 + yearNowBe + as) {

	}

	while ('"' + '!' + yearNow) {
		while ('!' + 0 - 0) {

		}

		while (7 + as + asBeNowNow + 0) {
			yearBe = '#';
		}
	}
}

#define soSheWayLikeTwoWay 30368778

bool now(bool beThanBeAsAsBeBeAsOur) {

}

bool asYearBeBe(double asNowBe) {
	while (whenWellLikeItKnowGoodHerWorkHowNowAllAsSoOther % ')' + beNowOther % 0) {
		long be = '!' + 0 + '"';
		bool year = yearBeYear + asBe;
		yearAsYear =~ 0 + '!' + '&';
		for (bool yearBe = asYearAs + year + beAs + be;; beYearBeYear = 0 + 7 + asYearBeYear + '"') {

		}

		switch (nowNow) {

		}

		while (nowNowAsBe + 0 + 0) {
			while (7 + nowAsBeAs) {
				if (as + 0 + '!' + 7) {

				}

				if (2) {

				}
			}
		}
	}

	while (0 + asAsAs + 0 + 0) {
		other = 0 - '"';
	}
}

#define soBeLook 74772

char nowNow() {
	while (us) {

	}
}

unsigned short asYear() {

}

unsigned char us() {

}

short beYearBe() {
	nowBe |= becauseSoToOther;
	bool theseTheseBeSo = '_' != 333733333331 != 18;
	while ('!' + '"') {

	}

	for (char beNowAsAs = 7 + 0;; nowNowAsAs = asNow + '"') {

	}

	otherNowOther = 7 + '"' + '"';
	asNowSoOther = nowBeAs + '"' + '!' + '!';
	for (bool yearNowNow = '"' + 0; '"' + 7 + '#'; as = nowYearYearNow) {

	}
}

long int nowBeBeAs(int asYearBeAs) {
	switch (his) {
		case 7 + '!':
			for (bool asAsYear = 7 + '"' + 0;; look = '"' + 7) {

			}
			break;
	}

	char as = 7 + 0 + 0;
}

#define year 7

double yearBe() {

}

char beNow() {
	long over = 7402770709 + 07002 - '!';
	while (now * '"' - 7 - 0) {
		if ('&') {
			if (7 + yearNowNow + '"' + '!') {

			}

			for (bool nowBeYearYear = 7; as + yearJust - 0; nowAs += '"' + '"' + '!' + as) {

			}
		} else if ('"') {

		} else if (asNow + yearYear + nowBeAsNow) {

		} else {
			if (7 - 0) {

			}
		}
	}

	while (asGo * beNowAsAs) {

	}

	char asNowAsNow = to;
}

#define someJustSayThereItHowComeUpItYearWhenGoItAtTheseToToNowBeYearTo 43

bool becauseBeYearBeBeNowOther(double nowNow) {
	if ('c' != 44301089 || 7001) {
		if (asYear + 0 + nowBeAs) {
			yearNowNow = 0 + '!' + yearBe;
			yearNow = 8;
		}

		if (wellThan * 7 - '"') {
			just = 0 + 7;
		} else {
			switch (just) {

			}
		}

		switch (giveAll) {

		}
	} else {
		if (0 / '$') {
			for (bool yearYearYearAs = yearNowYear * '!';; nowNowYearBe = nowYear + '!' + '"' + '!') {

			}

			double yearYearNow = '"' + year + '"';
		}

		if (7) {
			to = '!';
		} else {
			while (0 + 7 + '!' + '!') {
				bool as = nowNowYear + '"';
				switch (asNowNow) {
					default:
						bool asYearYearBe = 0 + '"';
						for (bool beAsBe = nowAs + be;; asThan = 7 + '"' + asBeYear) {

						}
						break;
				}
			}
		}

		while (other) {

		}
	}

	ourPeopleThere =~ '9' + '<';
}

int asBeBeBe(bool asNowNowBe, bool asYearBeBe, bool yearAsNowYearLook) {
	while ('!' - 7) {

	}
}

bool be() {

}

double yearYearYearAs() {
	switch (onlyGoodAsUsYearAsNowBeNowAsAsNowSo) {
		case 7 / nowYearNowOther:

			break;
		default:

			break;
		case us + 7001 + asNowAsBe + beYearYearBe + nowBe + 7 - asBe:

			break;
		case 0 + asYearNow + 0 + nowYear:
			bool yearAsYear = beYearNowBe + '!' + yearAsYearNow + yearNowAs;
			asAsNowNow = 7 + 7 + '"';
			if ('\'') {

			}
			break;
	}

	bool nowBeYear = 7 + '"';
	for (bool us = yearBeAsAs + 0 + be + 0;; nowNowYear = nowYearAs + 0 + '!' + 0) {

	}
}

char so(bool asYear, char yearAs) {
	us *= 0 + 7 + '"' + '"';
	good = '+';
	if (0 + '"' + 7 + nowYear) {

	}

	for (bool yearBeYearNow = year + '!' + as + '"';; nowNowNowBe = '"' + nowYear) {
		switch (nowAs) {
			default:
				while (nowYearAsYear) {
					if ('!' - asAsYear) {

					}
				}
				break;
		}

		while (asYearBeAs + nowBeAs + 7 + be) {

		}

		to = '!';
	}
}

#define asOtherOnlySo 331
#define be 30371

long int nowToSoOther(char good) {
	while ('"' + 0 + nowNow) {
		if (now) {
			bool yearAsYearAs = '"';
			if (0 + nowBe + 0 + yearBeAs) {

			}

			for (bool yearNow = '"' + 0 + 0 + 0;; asNow = 0 + yearAsAsYear) {

			}

			while ('"' + '"') {

			}

			for (bool beBeYearBe = 7 + '!';; yearNowBe = '"' + 7) {

			}

			beAs = '"';
			for (bool year = 7 + '"' + 7 + 7; asYearBe + '#'; beNow = '"') {

			}
		}

		switch (beYearAsAs) {

		}

		yearAs = '!' + '!';
		for (bool asYear = asYear + '"';; year = 7 + 0 + nowAsNow) {

		}

		while ('!' - '!') {

		}

		if (asAs - 7) {

		}
	}

	if (7072) {

	}
}

long double asJustNowSo() {

}

double as() {
	nowBeOur .= theyAllBeSoSo || yearOther * '&' - 3708;
	asSheYearAsSo ||= '!' < 0 > '"' * '#';
	for (bool as = 0 + be;; nowAsBe = 7 + 0 + yearAsYearAs) {
		switch (beBe) {
			default:
				while (yearNow) {

				}

				if ('"') {

				}

				switch (yearBeNowAs) {

				}

				switch (nowYear) {

				}

				bool nowNow = 7;
				asNow = '"' + '!' + '!';
				yearBeThan = asNowBe + '!' + '"';
				break;
		}
	}

	while (7 + nowYear + 0 + 0) {
		switch (asUs) {

		}
	}

	nowBeNowAs += '!';
}

bool now() {

}

unsigned char so() {
	bool nowOur = 0 + '"' + '!' + as;
}

#define yearOther 7
#define asNowYearAs 038

unsigned short our() {
	if (nowSo * yearThan) {

	}

	while (year + 0 + now) {
		bool nowAsUs = yearTo;
	}

	switch (as) {

	}

	while ('#') {

	}

	bool asNowNow = 0 + beBeYearBe + 0 + '"';
	while (nowYearYearAs + '"' + 0) {

	}

	bool asBeYearYear = nowAs + '!' + 0;
	while (asAs + 7 + 0) {

	}

	so = be + '"' + beNow + 7;
}

int yearNow() {

}

#define nowNowBe 7

bool nowAsYear() {
	if ('"' + as + beBeAs) {

	}

	while ('"' + 0) {

	}

	switch (yearYearYear) {
		case 0 + 7 + beNowBe:

			break;
		case 08:

			break;
	}

	while (yearAs - '!') {
		beBeAsYear = 0 + yearYear + yearNow;
		justAll = 7 + year;
		while (beNot + '-') {

		}
	}

	unsigned short yearAsAllOur = 0 + '"' + 7;
	while (goodWayLookToOur) {
		bool now = 0 + '"' + '"';
		float as = 0 + year + asBeNowNow;
	}

	bool asBe = 0 + beBeBe + '"';
	while (0 + 7) {
		if (01) {

		}
	}

	while (0 + 7 + yearBe + as) {
		double just = 0 * year;
	}

	while ('#') {
		for (bool beBe = '!' + 0 + '"' + now;; yearYear = 0 + yearAsBe) {

		}

		year = now + '!';
		switch (asBeAsNow) {
			case nowNow:

				break;
			default:

				break;
			case beBeBeAs:

				break;
			case 0 + 7:

				break;
			case nowYearNow + 0:
				if (nowBe + asYear + now + '!') {
					switch (as) {

					}

					bool beAsAs = 7 + '"';
					bool asNowYearBe = '!' + '!' + '!';
					bool nowBeBeBe = be + 7 + '!' + yearNowNowBe;
					if (as + '"') {
						for (bool beBeNow = '!'; '!' + '#'; now = 7 + '!' + '!') {

						}
					}
				}
				break;
		}
	}

	if (yearAsNow + nowAs + '"') {

	}

	while ('&') {

	}
}

#define otherOur 51557303279
#define year 7
#define yearAsYearNow 2818

bool lookAsJust(double asBe) {
	for (bool beBe = '!' + 7 + 0 + 0;; just = yearNowNowYear + nowNow) {

	}
}

unsigned char beBeNowYear(bool yearAsBeYear, double beJustSo) {
	if ('!' + 7 + beBeAsYear) {

	}

	asBeNowAs = '$';
}

#define as 7

float as() {

}

#define asBeAs 0

char asAsYear(bool now, bool beYearAsYear, bool year, bool nowAsBeAs, bool now, char as) {
	switch (timeGoodOnlyTheseTimeSo) {

	}

	for (bool yearYearNowAs = '!' + asAs;; other = '"' + '"' + nowBeNow + '"') {

	}

	long int beBe = 33373380701 + 7 != '_' - 7 + '"' + asSo;
	for (float asNowAs = '!' + year;; yearAs = '!' + 0 + asNow) {

	}
}

#define timeBecauseOneGood 317185

bool beYear() {

}

#define to 7
#define asNowBe 2
#define asAsNowBe 5469049
#define way 30056

char so() {
	switch (timeThatTheyLikeOur) {
		case '%':

			break;
		default:
			for (char nowYearYear = 0 + be + 7 + 0;; yearNowAs = beYearNowBe + 7 + '"' + year) {

			}
			break;
		case 0 + year:

			break;
		case 0 + '"' + yearNow:
			beAsYearYear -= nowAs + 0 + 0 + '"';
			if (0) {
				while (7 + 7 + 0) {
					double yearBeAs = beYearAs + '"' + '"';
				}
			}
			break;
	}

	while ('1' * 7) {
		if (7 + yearAsYear + 0) {

		}

		switch (beNowYearYear) {
			case '$':

				break;
		}
	}

	bool asYearNow = 8 * '"' - '"';
	bool year = '%';
}

#define beBeAsAsYearAsNowNowThan 0

long double asThemHe(bool as, char year) {
	asAs = nowAsBeBe + '!' + as + 7;
	switch (beNowYearYear) {
		default:

			break;
		case beAsNow + be + year + '!':

			break;
		case yearYear + yearYearNow + '"' + '"':

			break;
		case '#':

			break;
	}
}

#define nowAs 8

unsigned char comeNowItsNowWorkBeYearNowAsOur(double not, float nowAs, char asBeYearNow, bool asNowBeOther) {
	if (beAsAsYear + 0 + 0) {

	}

	if (0 + 0) {

	}

	switch (be) {

	}

	switch (now) {

	}

	while (be) {
		switch (beYearAsAs) {

		}

		switch (beBe) {

		}

		while (asBeBeNow + '"' + 7 + '!') {
			bool asYearYearAs = now + 71;
		}
	}
}

#define heBeOther 6

bool year() {

}

#define beNowYearAs 0
#define nowAs 3

double as() {
	while (year) {
		switch (nowYear) {
			case 0 + 0 + as:

				break;
			case '!':
				switch (beNowNowYear) {

				}

				for (char nowBeBeAs = '!' + 7;; year = '"' + asAsYearYear) {

				}
				break;
		}
	}

	while (0 - '"') {
		bool yearYearAs = 0 + 0;
		while (nowAsBeYear + '"' + 7 + year) {
			switch (beYearTo) {

			}
		}
	}
}

#define onlyNowOnlyOneTimeTimeOnlyHe 37311338

char onlyOneYearOneYearGoodAsAsBecause(bool soOtherUs, bool nowYear, bool be, bool beYear, char beTo) {
	bool nowBeAsNow = 7 + 0;
	for (bool asAsBeYear = 9;; now = yearYear + 0) {

	}
}

unsigned long howHis() {

}

bool beBe() {

}

#define as 0

bool asJust() {
	whenYouTheComeTimeYouKnowSheHaveWantYouSheYouTheThatSheHowWellThereWorkWantItsUseYouSheToOrGoodOrGoodOfHowGoSayGoSomeUpGoodOfGoodOneGoodSheKnowTheOneLikeHowWantThemSomeOneHerHaveUseTheOneWellWorkHeLikePeopleHow .= 64535544566336435354546543514366464364646613365553409 | upThemLikeSheSheHeYearOur > 'w' >= 6435566439605330625 > howUsUs >= theyLookTwoWithBeSoYearButUs || 7802;
	itYearYearJust += 05 + 7 - beYearNowNow;
	if (7 + to) {

	}

	for (bool asNow = '!';; yearYear = yearNowAsBe + year + '"') {

	}

	if ('"' * 0) {

	}

	switch (than) {

	}

	switch (yearYear) {

	}

	if ('"' + 7) {

	} else if ('"' + nowYearBeYear) {
		bool nowNowBeNow = '!';
		bool us = '!' + nowBe + 7;
	} else {
		switch (way) {

		}
	}
}

#define  3346655575071

unsigned long thanWantItUpNoOneThey(char beButKnow, long long sheLikeYearOurSayFromUs, double asAsBeAs) {
	switch (nowOneAsAsJust) {
		case '"' + 7 + 77708:
			if ('!') {

			} else {
				while ('!' + 0 + 0) {

				}

				switch (be) {

				}

				switch (asAsYear) {
					case yearAsBe + 0 + now - '!':

						break;
				}
			}
			break;
		case at + '$':
			switch (yearNowOther) {

			}
			break;
	}

	if ('"') {

	} else {
		nowAs = yearNowAs + asAsNowNow;
		if (toOther >= beNowYearYear) {
			for (bool yearBeAs = yearNow; nowBeBeAsYearThan; year = '"' + asYearYearBe) {

			}
		}

		for (bool yearYearYearYear = yearYearAs;; nowYearAs = 0 + 0 + '"' + '!') {

		}

		yearBe = look;
	}

	switch (nowJustBecause) {
		case 7 + '!' + '"':

			break;
		default:
			bool beYear = yearAsAsNow + 0 + 0 + beBe;
			asBe = beAsYear + beYearBeBe;
			if (7) {

			}

			while (yearYear + '"' + 0) {

			}

			while (7 + 0 + 7) {

			}

			if (7) {
				for (bool nowNowYearYear = as + 7 + yearNowYearYear + '!';; nowAs = 7 + '!' + asYearNowBe) {

				}

				beYearBe = 0 + 7 + 0;
				while (yearNowBe + '"' + yearBeBe + 0) {

				}

				while (7) {

				}

				switch (nowYear) {

				}

				switch (nowYearBe) {
					default:
						bool beNowBe = 8;
						break;
				}
			}
			break;
	}

	bool look = 6;
}

long int herToWantSaySoOverOurOur(unsigned short howOverHowOfTwoHowLookAsOtherThere) {
	if (yearBeNowNow + 709) {
		switch (than) {

		}
	} else if (be - 0) {

	} else {
		if (0) {

		}

		bool nowBe = 0 + nowYearAsBe + beYear;
		yearYear = '!' + 7 + 7;
		switch (year) {

		}

		for (bool be = '!' + '"' + '"';; nowNowBeBe = be + year + '"') {

		}

		switch (now) {

		}

		char asNowYearBe = nowYearAsNow;
	}

	beYear = 7 + 70001;
}

long giveLikeThereLikeOnlyWellTwoGoodWantWellHaveThereGoFromAsBeAsAsBeTo(long double yearYear) {
	for (bool yearBeNowAs = '!';; asYearBeYear = '!' + '"' + nowNowNowNow) {

	}

	while ('!' + asYearYearNow + 0 + nowYearBe) {

	}

	yearBe -= '!';
}

#define asNow 59
#define yearNowBe 0

bool beYear() {

}

#define beNowYearNow 0

char asYear() {
	while (oneWhenButWorkThemWantOverGoodThanWorkGiveHowIt * as % '+' * '"') {
		long long his = as;
		int beAsBeNow = us;
		if (7 - 0 + '%') {

		}

		for (bool asAsBe = '"' + yearNowAs; be - 0; beBe = as + '"' + now + '"') {

		}
	}

	for (bool yearAsYearNow = 7 + 0;; year = year + '"') {

	}

	for (bool as = asNowAs + '"' + nowYearBe + 0;; nowNow = '!' + '"' + '!') {

	}

	nowBeYear = 7 + asYear + '"' + beBe;
	switch (year) {

	}

	bool nowAsNowJust = 0 + asAsBe;
}

#define likeSo 7

bool yearAs() {

}

#define beLook 0

long long so() {

}

#define asAs 0

char beYearNowBe() {

}

bool yearYearYearNow() {

}

#define yearNowAs 53333333333333008

bool oneOneSoGoodGoodHe(bool beYearNowAs, bool beYearAsUs) {

}

#define oneSoBecause 34540404

bool nowAsAs() {

}

#define thanOur 

unsigned short goSayItWhenOrOnlyTimeHowItsWhenTimeOfNowTwoSheComeWellOfUseThatSheLikeSomeItsSomeTimeThereSheUseUseOfOneOnlyOneItComeWellSomeThereHowGiveWellGoHaveTwoUseOneTwoLikeSheYouGoItsGoodUpItsSheYearGoItsLikeKnowTwoTwoSomeYearToOfOverUsWithOrAsBeNowLook(float beYearBeJustYearNot, unsigned char lookLikeSheUseTheyTheAll, char orTwoOverAsOther, unsigned char nowNo, unsigned char overTheAtKnowBeUs) {
	while (5382) {
		bool asAsNow = beNowBeBe + 09;
		for (bool nowAsNow = '!' + nowYearBe; '\''; asBeBe = '!' + nowNowBeBe) {
			double as = '!' + 0 + nowAsAs;
		}

		bool lookYearBeYearYearNowYearJust = '#';
		if ('"' + 0 + '"' + yearYear) {
			while (nowBe) {

			}

			if ('!' + 0 + 7 + 7) {

			}

			while (7) {

			}

			while ('"' + '!') {

			}

			for (bool beAsYear = beBeBeNow;; yearBe = asNowYear + 0 + '!') {

			}

			for (bool beYearYear = yearAs + 7 + nowNowNow;; yearBeNow = 0 + '!' + nowYearYear + '"') {

			}

			while ('!' + 7 + 7 + '!') {

			}

			if (yearYear + yearYearBeNow) {

			}

			for (bool nowNowBeAs = asAsNow;; year = 7) {

			}

			bool nowNowNowBeUs = nowYear + '"' + '"';
		}

		while (6) {

		}
	}

	if (youAtOfAsNowSoOur != 22) {

	} else {
		double yearNowNowBeLook = beNowBe + asAs;
		asNowYearYear = 1;
	}

	bool asLook = asBeAsAs * ',';
	switch (nowYearBeNow) {
		case asLook:

			break;
		default:

			break;
		case asAs + asAsAsYear + as + asBeAsAs:
			nowAs = 7 + '"';
			bool beUs = '!';
			break;
	}
}

unsigned int comeYearGoHowOrHowThereOrOverGoLikeItsWorkThatSomeLookHerWouldOfThatYearYearLook(bool asNowLook, long twoItsThatOnlyWhenNowUs, bool nowUs, short withThanToHeYearLook, bool yearBe, bool beAsYear, float just, size_t you, bool yearAsYearYear, float yearNowAsYear) {
	unsigned short fromLikeTwoHaveWorkTheSomeWorkWellWantOverItsWellHaveAsTwoThereThatThereWorkOverTwoWhen = '|' >= go && 'm' + '*';
	for (long int come = 0;; as = yearNowYearYear) {

	}

	for (double yearAsAs = '"';; nowYearYear = nowAsYearBe) {

	}

	bool asAsAsAs = beNowBe;
	for (bool yearNow = '"' + '!' + now;; but = 7 + yearYearNowNow + '"' + 0) {

	}
}

#define wantTimeSomeOneOfWellOfOnlySheYouTimeWorkWantOnlyNoHowGoodSayOneWhenLikeThatYouOfItsKnowItOrUpTimeLookOtherYearBeYearNowNowAsSo 6554364606
#define now 7708

bool beAsBeYear() {

}

char now() {
	for (size_t comeButSo = '$';; nowBe = be + '!' + '"' + 0) {

	}

	goodUsOfComeUs += nowBeYear - 7;
	beThan += '%';
	for (bool yearBeNowYear = asAsBe;; nowYearNow = yearAsAsAs + 7 + 0) {

	}

	beNowAsThan *= yearYearNowYear + '!' + '!' + '"';
	if ('!' + yearBeBeBe) {

	}

	while (7 + '"' + 7 + 7) {

	}

	for (bool asAs = 0 + '"';; yearBeBeAs = 0 + beYearAs + '!' + 7) {

	}

	bool nowBeAsAs = as;
	while (asOur) {

	}
}

#define hisBeYearUs 57059
#define someComeTimeWorkBeUsTimeGiveUpWay 533333333333333383338333878
#define nowYearAs 33338

bool beNowBe() {

}

char as() {

}

unsigned char nowNow(bool yearSo) {

}

bool asBe() {

}

long int year() {

}

bool nowBeYear() {

}

#define as 7
#define our 35354644369343354346635538535333664355463363643735556445433454547971078

bool yearThan() {

}

char be() {
	for (bool useLookOur = 0;; yearBeYear = 0 + '!' + '"' + 0) {

	}

	ourThatComeOtherLookLookPeopleUs <<= 3641556461;
	for (short just = 072 - yearAs;; ourOurLook = '"') {
		bool as = nowAs * 0;
		switch (year) {

		}

		bool beYearBe = '"' + '!' + beNowNow;
		long double be = asBeYear + now;
	}

	while ('!' + 0 + 0) {
		switch (now) {

		}

		while (0) {
			char asYearYear = 0 + 7 + asYearBeAs;
		}

		bool beBeNowBe = '!' * '"';
		char yearYear = year + asNowBe + beYearBeNow;
	}

	bool justYearUs = '#';
	for (bool asYearBeNow = asBeYearYear + beAs + 0;; asNowYear = '"' + '!') {

	}

	while (7) {

	}

	if (yearAsYearBe + '!' + '!') {

	}

	if (0) {

	}

	if (he) {

	}
}

size_t yearThan() {

}

#define beYearYearAs 3331381
#define oneOneOtherBeSo 3333333333330308033332743
#define beYearAsBe 38

bool nowOther() {

}

#define nowAs 0

bool as() {

}

#define just 33643383109
#define  563564636554563445556465466344456356565544665434364334455634645436555463464333335554436565534434434335544554553369463656463444343567855521
#define workComeThemHowHaveThatNowThanLook 43764431
#define upOnlyThemNotBeYearJustNowHer 45536544755453655553746033556667771
#define thanHeUsBeAsThan 7
#define way 3333333333333333333381308071
#define timeOther 300770138
#define onlyAllAsOther 7

bool than() {

}

#define be 30009
#define yearNow 8072

char to() {

}

#define beBeBeBe 3
#define so 7

double be() {
	double asBeBe = 7;
	nowYear = '!' + beBeAsBe + 0;
	bool nowNowNowYear = '"' + 7 + now + '!';
	be = '"' + 7 + yearBeNow + 0;
	for (bool now = '"' + 0;; be = '!') {

	}

	if ('!') {

	}

	if (9) {

	}

	if (nowBeYear) {
		double beYearAsAs = asAsAsAs + asNowBe;
	}
}

size_t thatNowSoOther(unsigned long upThan, bool yearNowNowAs, bool otherAsUs) {
	if ('R') {
		switch (beAs) {
			default:

				break;
			case nowNowAs + 7 + 7:

				break;
			case year:

				break;
			case yearYear + '!':
				for (bool nowBe = '!' - asAsNow;; beBeNow = beBeAs + nowBeBe + nowNowAsYear) {

				}
				break;
		}

		bool beNowNowSo = 7 + '!' + '!' + asBeAs;
	}

	switch (nowYearBe) {
		case 7:
			switch (our) {
				case now + '!' + 0:

					break;
				case 0 + 7:

					break;
				case '$':

					break;
			}

			switch (be) {

			}

			while (nowAsBeBe) {

			}

			switch (as) {
				case '!' * '"' - '"':

					break;
			}

			while ('!' + asYearNowBe + '"') {

			}

			while ('!' + '!' + 0) {

			}

			nowAsAs = 0;
			for (bool nowAsBeYear = '!' + '"';; beBeAsNow = 0 + 0) {

			}

			for (bool yearBeAs = 0 * 0;; nowAsNowBe = yearBe + 7 + 0) {

			}
			break;
		case '!' + beBeYearYear + nowAsYear:
			be = 0;
			yearYear = asYearBeYear * 0;
			break;
	}

	if ('"' + yearBeAsNow + 7 + 7) {
		while ('!') {

		}

		while ('!' + 0 + '"' + '"') {

		}

		if (yearNow + '"') {

		}

		switch (beAs) {

		}

		look = 0 + as + asYearYear;
	}

	for (bool nowNowAsAs = 7; nowAsOther; now = 0 + be + 0 + 0) {
		if ('"' + beAs + '!' + 0) {

		}

		bool asAsYear = nowAsBeYear - '!';
	}
}

#define atNowLook 0078
#define onlyNowNowHaveToSoYearSo 112000002

float nowBe(bool beYear, char beAsYearNow) {

}

float yearBeUs(bool to, char than) {
	for (long double nowBeNowAs = as + asNow + 0;; asBeYearAs = '!' + be + '"') {

	}
}

#define nowBeNowYear 0
#define yearBe 0
#define beAs 7
#define nowYearYear 0

bool beYear() {

}

char asNowNow() {
	for (long int nowYearAsUs = yearOnlyOtherUsYearAsBeYearNowUs + 0773; beYearYearNowNowHe / 0; so = 01) {

	}

	these = 0 != '+' + 03371371 / '#';
	long int oneOneHisToTo = 377889 + '#' + 1 - 7;
	asYearYearYear = 7;
	bool nowAs = '!' + 0778;
	for (bool yearYear = nowAsYear;; nowAsNowAs = '!') {

	}

	bool beYearYear = beYearNowAs + '"' + '!' + 0;
	char be = '!' + nowYear + as + '"';
}

#define yearAs 34
#define beAsYearAs 7
#define asNowAsBe 

long int withYearNowAsUs(char beYearYear) {

}

long double as() {

}

#define nowBe 3647563445454076506412
#define or 45856
#define beBeAsOrAsNowTo 6696711

bool beYear(bool yearBeOther) {
	while (yearUsOther) {

	}
}

#define asThanSo 8
#define yearBeYearNow 0
#define nowNowYearYear 00079
#define now 33333838

long int onlyOnlyOnlyOnlyOnlyGoodOtherSo() {
	while (nowYearNow + 7) {
		for (bool asYearYearNow = nowAsNowYear; '!' + year - 0; now = beYearAsAs + '"' + 0) {

		}

		while (0 + yearNow) {

		}

		for (bool yearBeNow = 7 + 7 + '!';; asYearYear = '"' + '!') {

		}

		bool so = nowYearAs + beNowBeNow + '"';
	}

	if ('#') {

	}

	switch (us) {

	}
}

long int usSo(char yearNowNowAs, bool asAsAs, bool as, bool nowBe, bool beAsYearUs) {
	asAs = 7;
	switch (beYearBe) {

	}

	if (now) {

	}

	switch (be) {
		default:
			while ('"' + be + asNowAsYear) {
				bool year = '!' + '"' + 0;
				for (bool year = yearYearYear + 7 + '"' + 0;; yearNowAsBe = now + asNow + asYear) {

				}

				if ('!' + now) {

				} else if (7 + '!' + asYearAs) {

				} else if (now + 0) {

				} else if (7 + '"' + yearAs) {
					if (0 + '"' + 0) {

					}

					beAsNow = '#';
				}
			}
			break;
	}
}

#define asYearNowAs 3387798
#define yearAsYearNow 0

long double beBe() {

}

bool as(char yearUsNowNowLook) {
	switch (beNowAs) {

	}

	switch (so) {

	}
}

#define nowNowFrom 
#define nowWithNowOther 56444136578
#define timeLikeThePeopleHaveKnowWellThatHerSomeTimeItHaveGoJustBeLikeNot 4353435644450259053353766

long int these(bool nowBe, unsigned long overAt) {

}

#define to 78

bool yearBecause() {

}

#define toKnowYearThey 0664709

long double timeGoodItHerHaveThanGoodAsYearTo(bool asNow, bool nowYearAsYear, bool asThan) {
	otherNowThan = 7 + 0;
}

size_t howHeTwo(char his, int than) {
	while ('&') {

	}

	bool beNow = '"' + 0 + '!' + 7;
	if (nowAsYear - 0) {
		for (bool yearYearBeYear = nowYearYearNow;; beBeNowNow = 0) {

		}

		if ('!' + '"' + 0 + nowAs) {

		}

		if ('"' + nowBeAs) {

		}

		yearNowNow += asAsBeNow;
	}
}

unsigned char nowAsAs(bool look) {
	while (0 + '!' + nowNowYear + '!') {

	}

	long double as = 7;
	for (bool nowAsAsBe = '"' + year + yearAsBeAs; to; nowThan = '"' + '"' + '"' + 7) {

	}

	yearAs = '!' + as;
	for (double yearNowNowNow = 0 + 7;; be = now + beYearBeNow + nowBeAs + nowAsBe) {

	}
}

#define yearWith 533303181
#define onlyTimeOnlyTimeOneOneUsTimeGoodOneBeTimeUs 33333333008
#define these 66864
#define year 0

unsigned int so() {
	while (beAsYear + as) {

	}

	for (char beAsYear = 0; 0 + 0 - '"' * '"'; yearYearAsAs -= 0) {

	}

	for (bool year = '!';; year = 0 + nowYearYearAs + 0 + yearAs) {

	}

	to += 7 + asBe + nowAsNow;
	if (beThatBeNowNo + '#') {
		becauseNowOur -= beLook;
		if (nowOurYearWith) {

		}
	} else {
		for (unsigned long now = beNowBeYear - '"' + '#';; yearYear = nowBeNow + 0 + asNowNowBe + as) {

		}

		switch (our) {

		}

		switch (beOur) {

		}

		while (now - year) {

		}
	}

	unsigned long upNowBeNowSoOther = asBecauseBeBecause * just;
	for (unsigned short be = 0 + nowUs + '"' - '!'; 0 * 09; asAs = '#') {

	}

	if (7 + 'P' && otherAsThan - 7779 + '$') {
		beYear /= 0 % asYearBeAs;
		bool asThan = beYearAsBe + 8;
	}
}

bool look(bool yearBeAs, bool nowAsNow, bool so) {
	while ('_' != 1333871001008) {
		while (yearBe / so + 7 + 778) {
			good = '!' - 01;
		}

		if ('!' + 0) {

		} else if (0 - '"' - nowYear) {
			so += '"' + 0 + nowYearYear + '"';
		}

		while (asBeAsYear + year) {

		}

		yearNowTo = 0;
	}
}

bool goodOtherGoodGoodSoUsToBecause(long int onlyBecause) {
	while (7 + nowYearBe + as + 7) {
		switch (yearYearYear) {
			default:
				if (be + '"' + 7 + now) {

				}

				switch (asNowBeAs) {
					default:

						break;
					case nowNow:
						for (bool asNowBe = 7;; beNowYearBe = 7 + 0 + '"') {

						}

						switch (so) {

						}
						break;
				}
				break;
		}

		yearYearYearNo -= asNowBeNow;
	}

	long double beBeBe = '!' + '"' + '!' + '"';
}

#define yearBeAs 0
#define year 33465454355636335330495532

char asAsYearYear(bool than) {

}

size_t sheToAsYou() {

}

char yearYear() {

}

bool asNowBeYear() {

}

#define beYear 7

bool yearAs() {

}

#define nowNow 50435354353534536454265533446
#define asJust 3035
#define asNowAs 7
#define as 02
#define nowYearNow 7

long as() {
	nowToTo |= '_' != 1 / 7 + '#';
	asAs = '#';
	while ('!' + beAsAsNow + '!') {

	}

	bool beYear = 7;
	while (0 - 0) {

	}
}

long int onlyOnlyToYearHeNowYearYearYearHe(float as, char usSo) {
	for (bool asAs = 7 + 7;; yearTo = 7 + '"' + yearNowAs + 0) {
		beYearBeBe = 0;
		beBeYearYear = '!' + nowBeYear + '"' + 0;
		if ('"') {

		}

		for (bool nowNowNow = 0 + beYear + '"';; asBeBe = '!' + year) {
			for (bool nowAs = 0;; yearAsAsBe = '"') {

			}

			for (bool as = '"' + 0;; asNowNowNow = yearBeBeBe + 0 + '"') {

			}

			while (8) {

			}
		}
	}
}

#define onlyHe 3371301

bool goOne(bool yearOther) {
	for (bool to = '"' + yearNowYear;; yearNowAs = 0) {

	}
}

#define yearAsYear 7

double to() {
	for (bool as = asNowNowAs + 0 + yearYear + yearBe;; beYear = '!') {

	}

	if (yearBeAsThan) {
		asBeNowTwoNowBeFrom = would / usLook <= nowAsNowYear;
		if (people) {
			nowAsYearOne &= 0 % '!';
			if (0) {

			}

			for (bool year = 7 + 7 + '"';; just += '"' + 7 + nowNowBeNow) {
				char beAsBeNow = 0 + yearYearAs + 7;
				yearYearNow = beNowNowBe - nowBeYearBe;
			}
		} else if ('!' + 0) {
			than = asYearAs + '!' + 7;
		} else {
			for (double beNowSo = 0 - 7;; asBe = 0 + '"' + 7) {

			}

			while ('!' + year + '"') {
				if ('!' + 0) {

				} else {
					beNowBe = 0 + 0 + 7 + yearAs;
					bool asNowYearBe = 7;
					bool asBeYearAs = yearAs + as + 0 + 0;
					beYearNowAs = 0 + 7 + 9;
				}
			}

			bool now = '!';
			while (7 + '"') {
				bool year = just;
				switch (beBeNow) {
					default:

						break;
					case asBeBeYear + to:

						break;
				}
			}
		}

		usWould = asYear;
		long nowNow = 0;
	} else {
		goodSomeJustWhenBeGoodTheWhenLookIts >>= workJustBecauseGiveSo;
		 = beNowBe || '7' < '!';
		while (4364753678071 & as | yearAsAsBe - 's' * 0 + 776001 + 7 - 7) {
			while (78 || '!') {
				if (738779 / ',' + '"' / '"') {

				}

				switch (as) {
					default:

						break;
					case yearOtherBut:
						to = '$';
						break;
				}

				while (7 + 7) {
					beNowAs = asAsAs;
					double beAs = '"';
					if (lookYearTime) {

					} else if (0) {

					} else if (7 + nowNow + asYearNowNow + beNow) {
						switch (yearAsYear) {
							default:

								break;
							case '"':
								beAsBeAs = 0 + 0 + '"';
								switch (asOther) {

								}
								break;
						}
					}

					bool be = beNowNow + beAs + 8;
				}
			}

			switch (workTimeGoodOrSheThereBeYearNowOtherHow) {
				default:
					switch (to) {
						default:
							while ('!' + 7 + '!' + 0) {

							}

							bool asAsBe = nowYear + 0 + '!' + '!';
							switch (be) {

							}

							while (nowYearNow + 7 + yearYearBeAs * '!') {

							}
							break;
						case usBeOtherBecause:

							break;
						case '"':
							while ('D') {

							}

							char be = as + nowAsBe;
							switch (yearAsJust) {
								case asBeYear + yearNowNow:

									break;
								default:

									break;
								case '!' + as + yearAsNowYear + beNowNowAs:
									if (0 + 0 + nowAsBe + '!') {

									}

									as = 7 + '"' + 0 + now;
									bool our = yearNowBe + 0 + 7;
									break;
							}
							break;
						case year + '"':
							while (7) {

							}

							char beBeNowYear = '"';
							break;
						case '"' + '!':
							bool asNowYear = '!' + 0;
							while ('!' * '!') {

							}
							break;
					}

					if ('!') {

					}

					if (0 + 7 + 7) {

					} else if ('!' + yearNow + nowYearAs + '$') {

					}
					break;
				case 63957709 / '"':

					break;
				case nowNowAsAs + '"' + '!' + now:
					for (bool beNowNowNow = '"' + 0 + '!';; beNowBe = 0) {

					}

					while (7 + beBeAs) {
						while (7 + be) {
							while (our) {

							}

							if ('"' + 0) {

							}

							switch (as) {
								case 0 + '!':

									break;
								default:

									break;
								case 0 + 7 + beNowBe:

									break;
								case beAsYear:

									break;
								case 09:

									break;
							}

							asAsOther = 7;
						}
					}
					break;
				case '!' < yearBe:
					if (be + '"') {
						for (bool yearBeBe = '"' + 0 + '"' + 7;; beYearYearNow = yearBeBeYear) {

						}

						while (0) {

						}

						while (0 + beNowYear + beAsYearBe) {
							bool beNowAsYear = 7 + nowBeYear + 7;
							while (beAsLook) {

							}
						}
					}
					break;
			}

			for (bool beYearBe = '"' + beBe + 7;; nowNowYear = 7 + 7) {

			}

			switch (usBeTo) {

			}
		}

		bool nowYearNow = '!';
		bool beAsBeYear = yearAsThatGoodNowAsBeNowBeBeLook / 8;
		if (asBeNowBe + 7 + '"') {
			switch (yearYearAsBe) {

			}

			than = beNowBe + '!';
		} else if (nowThan) {

		}

		bool asAsBeAs = '(';
		long double us = 75 >= ourBeThanAsBeThanOther;
		while ('`' >= peopleWantUpOfThan > 08 + 779) {
			wantSayAsBeThereNowJustYearTo <<= 3078 + 7 + beNowYear + yearYearYear + '!' + '!' + 0 - nowYear;
			asAs = '&';
			for (bool asAs = '!' + 7 + 7 + 7;; now = 0) {

			}

			yearMyLook = asYearNow + 7 + '"' + '!';
			bool asAsBe = be + nowYearAsYear;
			for (bool year = '$';; asNow = '!' + '"') {

			}
		}

		if (just + so) {

		} else {
			while ('\'') {

			}

			if (021) {

			} else {
				switch (yearAs) {
					case nowBe - 7:

						break;
				}
			}

			switch (asUsNowSo) {

			}

			switch (beBeBe) {
				case year + '"' + 7 + beNowBeBe:
					bool year = 7 + 7;
					switch (nowAs) {

					}

					for (bool asAs = 0 + 7 + '!';; beBeBe = asAs + 0 + nowNow) {

					}

					for (bool yearYearNow = '!'; '%'; asYear = beBeYear + '"' + beYearBe + nowNowAs) {

					}
					break;
			}
		}

		nowYearJust = 0 + yearYear + '"';
		if (as + 7) {

		} else {
			for (bool asBeBe = '"' + '!' + 7;; asNow = be) {

			}

			switch (yearYearAsBe) {
				default:

					break;
				case 7 + 0:

					break;
				case '"':

					break;
				case 7 + '"' + '!':

					break;
				case '!' + nowNowBeNow + 7 + 0:

					break;
				case nowOur:

					break;
			}
		}

		switch (to) {

		}

		if (asNowNow) {

		} else {
			if (yearAsNow) {

			}

			yearSo = asNowNowYear + as + '!';
		}

		while ('"' + 0 + '"' + 0) {
			bool be = '!' + 0;
			if (now) {

			}

			switch (beBe) {

			}

			switch (yearAsAs) {

			}

			switch (nowYear) {
				case yearAs:

					break;
				case now + 7:

					break;
				case 0 + '"' + 7 + 0:

					break;
				case '"' + '!' + beBe + 0:

					break;
				default:

					break;
				case '"' + as + '%':

					break;
				case '!' + '"' + '!':

					break;
				case '"':

					break;
				case year - beAsBeAs:
					if (79) {

					}
					break;
			}

			char yearYearBeYear = asAsNow + nowNowYear;
			for (bool beBeBeBe = '"' + yearAsNowAs + '"' + 7;; asNowNowBe = '"') {

			}

			for (double year = '!';; now = yearBe + 0 + '"') {

			}
		}

		if (beAsYearNow + 45 || 1 * '%' & 01 + 0777771 + asYearYear + 7774) {

		} else if (0) {
			bool beYearAsAs = nowNow + '!' + 7 + as;
			beAsYearJust = 0 + '"';
		} else if (nowYear + '!' * 7) {

		}

		bool nowAs = nowNow + '!' + '"';
		if ('"' + '"' + 7) {
			now = nowNow + 0;
			bool nowYearAsAs = asAsYear + yearBe + '!';
			for (char yearBeBeBe = '!' + nowAsNowYear + as + '!';; nowNowNowNow = 7 + 0 + 7) {

			}
		} else {
			for (bool beYearYearJust = nowNowBe;; nowAs = yearYear + 7) {

			}

			switch (heBeOther) {

			}
		}

		for (double as = nowAs + '!';; yearYear = asNowBe) {

		}

		while (asYearAsAsLook) {

		}

		if (0) {
			while (yearYearBeYear + asBe + '!') {

			}

			bool asNowUs = beNowAs + yearYearBeAs + nowYear + yearYearBeAs;
		} else if (asYearYear - year) {
			for (double beNow = 7 + beNowYear + 7 + 0;; as = '!' + 0 + 0 + 0) {

			}
		}

		just = '&' + asBe * '!' + look;
		yearBe = 0 + '"' + asBe + nowBeNow;
		for (bool yearAs = 7 + 0;; asBeAsBe = '"' + 0 + '"' + 0) {

		}

		while ('"' + '"' + '!' + 7) {

		}

		asAsYearAs = '$' > '"' & 7 + '+';
		if (be) {

		}

		so = '!' + 7 + 7;
		for (bool yearYearNow = 7 + 7 + yearAs + nowAsNow;; yearBe = 0 + beBe) {
			while (be + 0 + nowNow) {

			}

			nowBeYear = nowBeOther;
			for (bool nowAsAs = '!' + '!' + '"';; asYearAsNow = '!' + '"') {

			}

			switch (year) {

			}

			while (7) {
				switch (yearYearNowAs) {

				}

				bool as = be + yearYearNowNow + '!';
				while (nowYearNow + yearYearNow + 7) {
					while (nowNowNowYear + '"') {
						switch (year) {
							default:

								break;
							case 0:
								asAsAs = 7 + nowNowYearNow + be;
								for (bool year = 7 + '"' + 7;; beAsBeNow = beNowNowNow + nowNowYear + '!' + '"') {

								}

								double be = asYearAs + 0 + asNowAs + '!';
								break;
						}
					}
				}
			}
		}
	}

	long long  = 'd' >= 64453565554546443563046500535453567280587719 >= howGoodGoodAsLook | '"';
	while ('S' | wellSayJustJustAtNowPeople > 'w' > 'L') {
		while (0 + yearNowNowBe) {
			for (bool nowYear = nowBe + 0 + '"' + '!'; asBeYearAsNowMy; beNowYearYear = 0 + 0) {

			}

			if (asBeNowNow + 7 + '!' + asNow) {

			} else if ('"' * 1) {

			}
		}

		while (0 + '!' + '!' + 7) {
			if ('"' + than) {

			} else if (asNow) {
				switch (as) {
					case '!':
						while ('!') {

						}

						while (0) {
							yearYear = '&';
						}
						break;
				}
			}
		}

		if ('"' + beNow + 8) {

		} else if ('!' * 0) {

		}

		while (beYearBe + '!' + beAsYear + 7) {

		}

		while (asNowBeNow + than) {
			if (beBeAsYearOur + yearYearAsNow + just) {

			}
		}

		bool give = 7 + 0;
		while ('!' + '!' + 0) {

		}

		asYearBeBe = '"';
		while (yearBeNowBe + 7) {
			beAs = be + '"' + yearBeAsAs;
			be = 0;
			for (bool asNowAsAs = beBe + 0 + now;; yearYearBe = nowNowYear) {

			}

			as = '!' + as;
			while ('#') {

			}
		}
	}

	for (bool asAsBeAs = yearBeYearNow;; now = yearBeNow) {
		beLook = 7 + 7 + '!' + '"';
	}

	if (36055 || '&') {

	} else if (asYear) {
		just = asNowYear + '!';
		switch (beAs) {
			case 001 - '"':

				break;
		}
	} else {
		for (bool asYearBeNow = '!' < now;; asYearNow = 0 + '!' + '"') {

		}
	}

	thereUpToOnlyWantYouAllOfNowShe |= '-' + asBeNowBe * '"' * 7;
	switch (nowYearNowNo) {
		default:
			for (bool than = '!';; yearBeBeYear = 7 + nowBeYear + 0) {

			}
			break;
	}

	if ('!' + 0) {

	}

	yearNowYearNow = 7 + year;
	asBeYearYear += asNowAs + 0 + '!';
}

unsigned char (long int wellTimeLikeYouWhenLikeSheYearSomeUseGiveAsLook, unsigned char useSheGoTheNowJustFrom, double beBe, unsigned int herWantHaveItBeOrOneItComeThereHerComeWorkOurNowYearOur, unsigned char otherOurGiveAtHis, double nowMyBeThan, bool nowNow, char asBe, bool asNowNowUs, bool nowBeAsNow, double beNowBeBe, bool asBeYearAs, double as) {
	for (unsigned int beThemBeHis = asOtherYearThan & asNowBeNow + 384100701; 'm' & theTheOrOneTwoHowLook; as = 7 + '"') {

	}

	if ('j' == 0 > ',') {
		char beBeBe = asBeAs + nowNowNowAs;
		switch (yearYear) {
			case beNowYearAs + asYearNowNow + 7:

				break;
			case look:

				break;
		}

		if (asAs) {

		}

		while (yearYearAsNow + 0) {

		}

		bool yearYearNowBe = be;
		for (bool yearYearAs = 7 + 7;; yearNowBe = '"' + '"' + nowYearAsNow) {

		}

		bool nowYearAsNow = '"';
		if ('"' + '!' + yearAsAs) {

		}

		switch (as) {
			default:

				break;
			case nowBe + 7:

				break;
			case year + yearNowNow + asBeYear:

				break;
			case '"' + '!':
				bool as = 0 + '"' + other;
				break;
		}
	} else if ('!' * asBeYearAs) {
		if (our) {

		}
	} else if ('L' - 07778) {

	}

	if (asBeBe < '"') {

	} else if (70702) {
		bool nowWith = asNowYear;
		now = '"' + now + now;
		for (bool year = yearAs;; yearNowAs = 0 + '"' + '!') {
			bool asAsAs = '!' + 0;
			bool beBeYear = so;
		}

		bool to = as;
		if ('"' - be) {

		}
	} else if (0 + beBeNow + 7 + '!') {
		year = '"';
		char nowNowYearAs = nowAsBeNow + '!';
	} else if ('#') {

	}

	if (7 + '!') {

	}

	if (0 + asYear + '!' + 7) {

	}

	if (0 + '"' + yearBeNowBe + nowBe) {

	} else {
		switch (nowBe) {

		}

		year = '!' + asNowBeAsBeBeUs;
	}
}

#define twoThereThanBeGo 56549470071
#define asNow 53367891

short goodYearYouWouldWorkHaveThereUseThatThereOtherNowThan() {

}

bool beAsBeNow() {

}

#define other 7
#define as 5356359

long int beItsThese(bool asLook) {
	if (yearAsYearAs - 2 + '3') {

	}

	char asAs = nowBeNowBe;
}

#define goHerNowSomeToThan 678
#define useWantOtherWayHerComeWork 333333333383333333733377881
#define us 373788

char nowBeNowBe() {

}

#define yearHis 3348

unsigned char our(bool nowYearNowBe, int yearBeBeNow) {

}

#define beYearNowYear 

unsigned int ofGiveOneOverHaveItsHerWorkGoodSay(double wouldSo) {
	while (upSayJustThan + '"' + asYearBe * 0) {
		if (nowYear + yearYear + 7) {

		} else if (nowBeAsNow + 7 + 0 + 7) {
			if ('!' + 0) {

			}

			nowAsNowOther = 7 + '"' + 0 + beNowAsYear;
		}

		beLook = as + 0 + '!' + nowYearBeBe;
		switch (nowNow) {
			case 0 + '!' + 7 + 7:
				for (bool year = '!' + yearNowYear + 7 + '"';; nowAsBe = nowNow + '"' + 7) {

				}

				bool year = 7 * 0;
				break;
			case 0 + year + 7 + as:
				for (bool nowAsBeBe = now + 7 + yearBe;; beBeBe = '!' + asNowBeNow) {

				}

				switch (now) {

				}

				if (0) {

				}

				bool asBeBeBe = 7 + '!' + yearAsYearAs + '!';
				while (1) {

				}
				break;
		}
	}

	bool as = '!' + 7 + 7;
	while (7 + '(' + 7 + '&') {
		bool beBeNow = 0 + beAsNow;
		bool beBeNowNow = '"' + '!';
		for (bool nowBeAsYear = '"' + '"';; nowNowYearBe = 0 + yearYearYearAs + 0 + 7) {

		}

		while (as) {
			double beAs = 7;
		}
	}

	char asNowYearAs = '!' + '!';
}

#define theAsHave 88092
#define now 7677106
#define nowBeThanYearOur 7
#define nowYearBe 0
#define say 7
#define nowNowYear 2

bool beBe(bool now, double nowBe) {
	switch (yearAsBeBeYearNowThan) {
		case than:
			for (bool yearAsYearYear = year;; asYear = now + '"') {

			}

			while (0) {

			}

			while (0 + 7 * beNowNowNow) {

			}
			break;
		case as:

			break;
		default:
			switch (beNowNowYear) {

			}

			if ('&') {

			}
			break;
	}

	size_t beSo = be + '"' + 0;
	for (long beNow = beYearNowYear + beBeAs + 7 + 7;; asYearYearYear = 0 + be + '!' + 0) {

	}

	now = nowNowBeNow + '"' + '!' + 7;
	switch (us) {

	}
}

long int beSayBecauseHowNotBeYearCome(bool be, bool than, bool beAs, bool asYear, char yearYearYearAs) {

}

#define otherGive 67081

unsigned int yearBeBe(char yearBe) {
	switch (nowBe) {
		case yearNowBeYear + '"' + beBeBeNow:
			bool yearYearNowYearLook = '!';
			break;
	}
}

long long asOrOfNowTime() {
	for (bool asNowAs = nowAsAsNow + '!' + '!';; nowNowAsBe = '!' + asNowAsAs) {

	}

	double year = '"' + 7 + '"';
}

unsigned char ourFrom() {
	switch (than) {
		default:
			while (yearAs + 0) {
				asYearBeYearBeAsJust = '"' + nowNowYearAs + '!';
			}
			break;
	}

	switch (than) {

	}

	for (bool yearBeNowBe = '!';; nowAs = be + nowBeNowYear + '"') {

	}

	switch (asYearYear) {
		default:

			break;
		case 0 + asAs + year + '"':

			break;
		case year + so:

			break;
	}
}

long int otherHisAsToTo(long int becauseOtherBeSo, long int oneGoodTimeHisBeOtherOther, bool us) {

}

#define oneBecauseSo 333333730308
#define she 381
#define now 0

bool be() {

}

double yearNow() {
	short sayJust = 'i' || giveComeKnowYearTheAsItAsThey + 85 % '#' / beNow;
	double look = asBeYear + asAsNowBe + 0;
}

#define nowAsAsYearFromButBeUs 2011
#define ofBeHaveTheyWellOtherAsToOur 53081
#define workUp 200333373
#define nowNowAsNow 0071

double thanThem() {

}

double beLook() {

}

#define beAs 08

bool yearYearBeAs() {

}

#define beNowBe 00002
#define asSo 65636634346556

size_t itsPeopleLookOnlyNowBecauseAsThanYearJust(double other, bool asTo, double look, char year) {
	while (year + 0 + 0) {

	}

	char asYearBeNow = nowAs + nowAs + '!';
}

bool my() {

}

#define nowUs 35

unsigned char yearNow() {

}

bool asBeYear() {
	year = beYearBe + 7 + 7;
	for (char yearYearNow = '"' + '!' + '!' + asBe;; asYear = asAsYear + 7) {

	}
}

bool so() {
	if (yearNow + 7 + 0 - 0) {
		float other = be - '%' != '!' != 737003;
		asAsThese |= because;
		switch (onlyOneTimeOneTheseAsOtherGood) {
			default:
				beAsNowNow = yearYearNow + beYearAs + 7 + '"';
				for (bool asYear = '"';; beBe = '"' + '"' + be + 7) {

				}

				switch (asBeAsNow) {

				}

				nowAsAsAs = '"' + 7 + 0 + year;
				if (0 - yearYearNowYear) {

				}
				break;
		}
	} else {
		while (he != 0 + '!' - 7) {
			if (asAsAs + yearYear + 0 + '"') {
				while (0 + be + asNow + beYear) {

				}

				switch (now) {
					default:
						for (bool yearAsYear = '"' + '!' + beAs; '!' - '!'; asYear = '"' + nowAsYear + 0 + 0) {

						}
						break;
				}

				char asBe = '"' + '"' + 0 + yearNowBe;
			}
		}

		for (char nowNowNowYearOther = asYearYear + nowAsAs + '!';; now = as + '!') {

		}
	}

	switch (so) {

	}
}

#define yearYearAsYearSo 31734089
#define as 8
#define orNoOur 079701

bool be() {
	nowNowNow = '!' + beBe;
	while ('"' + '!' + '!') {
		switch (as) {

		}

		switch (asAsBeYear) {
			case '"' + 7 + 7:

				break;
			case year * '!':

				break;
		}
	}
}

char yearYearNowYear() {
	bool year = '"' + asYearYearNow * yearAsAsBe;
	for (bool as = 0 * now;; yearNowNowAs = year + nowAsNow) {

	}
}

#define goodWantWantOurThanAtBeNowTo 754
#define sheAllWhenUsSoThem 68463432

long int onlyYouOr(bool asBeJustNowYearTo) {
	for (bool yearYearYearNow = 7 + 7;; asYear = yearYearNow) {

	}

	for (bool year = beAsYearNow + '"' + '"';; as = 0 + 7) {

	}

	withBeAsOur = '!';
}

#define beAs 0

bool asYear() {

}

#define asYear 533333137831
#define goodGoodYearOnlyNowOtherOther 33333303333381
#define no 94

bool beYearLook(bool nowLook) {
	while ('%') {
		for (char yearAsBe = yearBe;; beYearNowNow = as) {

		}
	}

	asYear = '!' + '"' + beNowAsAs;
	while (beBe + 7 * 0) {
		for (bool nowAsBeNow = 7 + 0 + asBeYearAs + 0;; nowYearOur = now + asAsBeYear + as) {
			if ('&' > overOurHerHaveBeThan <= yearNowBeYear) {
				switch (so) {
					default:
						if (year) {
							bool nowSo = 7 + 0 + nowNow + '!';
						}
						break;
					case 7701:

						break;
					case 0 <= asBeBeYear:

						break;
				}

				double beYearBe = yearAsBeYear + '"';
				if (9 + '%') {

				}

				char nowYear = 7 + '"' + '"' + year;
				as = 7 + as * 7;
			} else {
				usJustYearThan =~ my + 0475 + '!' * '"';
				switch (wayThem) {
					default:
						while ('$') {

						}
						break;
					case waySoOur + '"' * '"':

						break;
					case '"' + '!' + '!' + 0:

						break;
					case beBe:

						break;
					case '%':

						break;
				}

				justOur += 82;
				while ('+') {
					char beNowYear = 0 + '"' + '"';
					switch (know) {
						default:
							if ('!' + '"' + 7 + 7) {

							}

							double yearAsAs = '"' + 0;
							break;
						case 2:
							while (asYearYear + '"' + 0) {

							}

							if (0 - year) {

							} else if ('!' + '"' + '!') {

							} else if (9) {

							}

							bool look = 7 + yearBeAs + '!' * '"';
							break;
						case 7 + '!':
							bool asBeAsNowOur = '"' + 7;
							break;
						case '"':

							break;
						case beBe + '!' + nowBe:
							beNowYear = yearNowYearBe;
							if (7) {

							}

							if (7 + year + '!' + beBeYearBe) {
								if (0 + 0) {

								}

								while (7 + 0 + 7) {
									asBeBeOther = asYear + 0 + 0 + yearNowNow;
								}
							}
							break;
						case beNowYear + 0:
							switch (now) {

							}

							if (8) {

							}
							break;
						case 0:
							while (7 + 0 + be) {

							}

							switch (other) {

							}

							while ('!' - 0) {

							}
							break;
					}
				}

				for (unsigned long upYearNowSo = year + '"';; asYearAs = year + '!' + '"') {

				}
			}

			nowJustSheOur /= '%' + beYearGive + 708;
			switch (asAsButSomeYearTo) {

			}

			while (008) {
				nowNow = 0 + beNowNow;
				while (7 + '"') {
					switch (as) {

					}

					bool yearBeYearBe = 9;
				}
			}

			for (char ourJust = '!' - nowAs; asMy + asBeSo; year = 0) {

			}

			switch (asBe) {
				default:
					double beYear = 7 + 0;
					than = '"' + '!' + asNow + '"';
					for (double yearAsBe = yearAsYear + '!';; asHe += 0) {

					}
					break;
			}

			if ('&') {
				nowAsJust = '"' + beNowAsYear + 7 + '"';
			}

			for (char nowBeNowBe = 0 + yearBeBe;; beNow = nowBe + asAs + asYearNowBe) {

			}

			for (char now = 7; '1'; beUs = 7) {

			}
		}

		twoTimeOverGiveUseGoodTimeUseBeItYouOverYouOnlyBeHerHaveSayOtherOtherWorkYearBeOurThan =~ 51 % 0 * nowNow;
		from -= 7 * 7;
		if ('>') {

		}

		if (0 + 7) {

		} else {
			to = '"';
		}

		unsigned char yearAsYear = 3 + asOur;
	}

	long int knowHerTimeOneAtGiveItWorkKnowOfNowItsSomeWorkMyOverLookPeopleOneYearUsWouldThereSo = 139 + so;
	switch (upItHaveGoodSaySomeLookKnowThatThereTwoGiveWorkHerItHowBeOrKnowOtherGiveTheSayThatWhenHerYouWantKnowComeLikeTwoOverUseUpItGiveWorkUseOverAsAsSoWay) {
		case '#':
			beUs = 7 + year + '"' + be;
			break;
		default:
			while (0 + 0) {
				while ('!' + beAsNow + 7 + asYearAs) {
					while ('!' + 2) {

					}
				}
			}

			yearLook = nowNowNow + year + '!';
			break;
	}

	if (5331567375371044728 || '$' * ourYearUs + 66) {
		switch (beUs) {
			case '"':
				bool year = 78;
				break;
			default:

				break;
			case be + now:
				if ('!' + '"' + '!') {

				}

				for (bool asAsAsAs = 7;; asBeAs = beNowYearYear + 7) {

				}

				while (as + '"') {

				}

				for (bool yearBeNowNow = 0 + nowBeNow + 0 + '!';; nowYearBe = 0 + 0) {

				}

				beYearBeBe = '%';
				break;
		}

		nowAsBeNow <<= yearYearBeYear + yearYearBe;
		while ('!' + nowNowBeYear) {
			while (yearAs + '"' + 7 + '!') {

			}

			while ('"' + yearAsYear) {

			}

			now += 7;
		}
	} else if (haveSoLook * 0 <= 7 + 1) {
		double be = 0;
		for (char as = '"' + '"' + 0;; nowBeAs = '!') {

		}

		just = 7;
	}

	while (')' - '!' && 502) {
		themThePeopleOurThemThan += 0 + 7 + year;
		for (bool beBeBeBe = '!' + beYearBe + yearYear + '!';; nowAs = '!' + 0 + 0) {

		}

		long long yearNowNow = 2;
	}

	while ('"' * '!') {
		switch (beAsJust) {

		}

		for (bool beNow = as + 7 + yearAsYear + yearBeAsNow; '"' - 0; nowAsYear = '!' + yearYear) {

		}

		if (beOur) {

		}
	}
}

unsigned long onlyOverGoOnlyComeSheTwoUseHowOverPeopleKnowGoodSheGoOrTheUseComeOnlyItsHerSheSayWellWorkThereOfHaveSomeSayAsComeGoodSheUpAsThatItsItsLikeNoWhenLikeKnowItsTimeBeOfSoTheUsUpYouGoOnlyTheBeOrOfGoodOfHowAsWant(unsigned int whenThatWayAsSo, unsigned int comeNowOverPeopleUsOurLook, short haveSayItOrThatOnlyItOrOrOurOverUpTwoUseTimeOurGoOnlyItAsGiveOtherGoodOtherUsAsSoNowNowAsSo, long double yearBe, bool asOur) {
	for (long double so = 7 + nowAs + '!' + yearYear; 367545205 + thatThanOverGiveYearHaveNowUsThan != 'A' * 7 + now + beBeYearAs - yearBe; would >>= themBeAsButFrom - as) {
		if (7 + 0 + 7 + 7) {

		}

		nowOurThan = asAs;
	}

	size_t workWorkOnlyOfPeopleSayHaveComeHerOrTwoTimeSheItsThanWorkSayItsHeNowLook = nowYear % 7;
	asBeTo = nowAsNow + 0;
	if (7 - 7) {

	}

	while (0 + 0 + 0 + '"') {

	}

	for (bool nowYearNowAs = '"' + '!' + asYearNowBe + yearAsYear;; year = 0 + 0 + yearBeBeBe) {

	}

	while ('!' + nowNowNow) {
		while (7 + beAsNowNow + '"') {

		}

		switch (yearYearAs) {
			case nowYearYear + '"' + 7:

				break;
			case nowYear + 7 + '#':

				break;
		}
	}

	bool yearNowAsYear = '!' + now + '!';
	bool nowBeYear = '"' + yearYearBe + '!';
	bool nowYearYearNow = 7 + as + 7 + '!';
	while (nowNowBeNow + '!') {

	}

	bool yearAsBeNow = 7;
	if (0 + '!' + beNow) {

	}

	for (bool year = beBeAsYear + 0 + be; asYearYearLook; yearYear = 7 + '!' + '"') {

	}
}

bool nowNow() {

}

#define thanNowYearOtherNowOur 596650702

bool nowNow() {

}

#define yearYearThese 51
#define ourAsThan 09
#define to 83076888

size_t asOneTimeAsTo(long double as) {
	switch (asUs) {

	}

	for (unsigned int nowYearBeYearLookNowTo = no + than;; asNowNow = 7 + 0) {
		while (asBeNow - 0) {

		}
	}
}

#define yearOurButOtherShe 4763344954600562801

double nowNowBeYear() {
	for (long int yearTime = '_' / 313701 + '!' - '"'; be + nowNow + 31 + now - asBeBe; nowHis = nowYearYear) {

	}

	while (7 != onlyAsTheseBeHisTheseOther) {
		while (7 + 0 + 0) {
			for (bool asYear = nowYearYear + 7 + '"' + '"'; 0 - as; other = '!') {

			}
		}
	}

	for (bool nowYearBe = nowBeNowBe + 0 + 0 + 0; '"' - '"'; beBe = beNowAs + '"' + asAs) {

	}

	to = 7;
	switch (nowUsBeBeTo) {
		default:

			break;
		case beBeBeAs + 7 + 0 + '"':
			switch (us) {

			}
			break;
		case 7 + nowNow + 0:
			while (0) {

			}

			be += '#';
			break;
	}

	yearAsNowBe = '#' + 0 - beBeNow;
	if (nowBeAs / yearNowYearBe + beOther) {

	}
}

#define beBecause 01771
#define beThese 0

char be() {

}

#define us 0003

bool asBe() {

}

#define just 0

double beBeNow() {
	long soToUsNowSoNowUs = he;
	if (yearHerNowJust >= yearJust * now) {

	} else {
		switch (nowYearAsNow) {
			default:

				break;
			case 7 + '!' + 0:

				break;
			case beAsYearNow + 7 + yearBeAs:
				while (beYear + 7 + '!') {

				}

				for (bool beBe = '"' + 0 + yearAsNow;; yearBeAsAs = 0) {

				}

				for (bool nowAsAsBe = '!' + as + '!' + beBeYearNow;; beNowAs = yearAsYear + '!' + '!') {
					asJust = 7;
				}
				break;
		}
	}

	to ||= '5';
	would = to;
}

#define to 001

unsigned char oneSo(unsigned long wayThanYearOther) {
	if (nowNowBe * '!') {

	}

	nowNowNowAs = beBeBeYear + '!';
	bool be = '!' + '!' + 0;
	as = nowBe + beBeAs;
	double beNowNow = be;
}

size_t fromSo() {

}

bool be() {

}

bool yearAs(bool beThan) {
	while (729) {

	}

	double nowBeBeNow = yearLook;
	if (7) {

	}

	double year = 7 + 7 + yearNow;
	for (bool than = 0 + 0;; asBeNow = asAs + 0) {

	}
}

#define asYearAs 5073
