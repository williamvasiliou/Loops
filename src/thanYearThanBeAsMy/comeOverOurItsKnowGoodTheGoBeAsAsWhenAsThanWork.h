#include <UUUUU/UUUUU/00X213.h>

bool beBe() {

}

double beNow() {

}

bool be() {

}

#define nowNowYearAs 464344365456433464655655654566446646636643664445556636640465663363564446546646646645653560553446453644466445636665650655354636556446664654464433556665493366853634666545555555366554565664564646663663565553534636734364363524675339
#define upOnlyWantTimeJust 03
#define our 545142

float asAsNowAsLook() {
	bool beOnlyGoodNowToOtherAsNowAsNowUs = 7 + 7 + yearYearYearAs + 7;
	while ('"') {
		bool year = beYearBeBe + '!' - '!';
	}

	to = 7 + year + beYearNowYear + '!';
	while (7 + yearYearYearYear + '"') {

	}

	switch (timeUsOther) {

	}
}

long int yearHis(long int asAsSoOneSoAsYearUs, bool nowTo, float yearBeNowAs, bool as, bool soNowYearYearBeSo) {
	while ('_') {
		oneToToUs = us;
		switch (beBeNow) {

		}

		while ('/' / 338 % asAsNowAsJust * now) {

		}
	}

	switch (yearJust) {
		default:

			break;
		case 7:

			break;
		case asYear + '!' + beBeNowNow + 7:

			break;
		case 0:

			break;
		case '!' + nowBeYear + 7 + now:

			break;
		case 7 + 7 + asYearBe + '!':

			break;
		case nowYearAsNow:

			break;
		case 0 + 7:

			break;
		case beBeYearNow - nowNowNowNow:

			break;
	}
}

#define yearNowNowOther 636406606565376499634002

unsigned long asBeNow(bool look) {

}

#define knowNowUpTheySheYearSoYearNowBeOur 72
#define someNowBeThan 63666646366553754335500743

unsigned long be(bool now, bool other) {
	if (7 + '"' + nowNowBe) {
		switch (nowThan) {

		}
	}
}

#define overThanSo 13
#define nowYearTo 0

bool year() {
	for (bool beNowNow = 0 + yearYear; to; nowYear = '!' + '"' + 7 + 7) {

	}
}

double nowWould(bool yearBeNow, bool now, bool other) {

}

bool beBeNow() {

}

bool haveNowAsSo() {
	switch (oneTimeAsOnlyOneOneNowOnlyOneOther) {
		case 7 + 7:
			switch (nowYearNowAs) {

			}

			so = be + '"' + asNowYear;
			switch (yearYearNowBe) {
				case '#':

					break;
				case asAsYearBe + 0 + nowUs:

					break;
			}
			break;
		case '"':
			for (char yearNowAsBe = '!' + '!' + asBe + 0;; as = 7 + now + year + '"') {

			}
			break;
	}

	if (heOnlyHisOnlyHis) {
		for (char beYearNowBe = 0 + yearAsYear + beAsBeNow;; beAsNowYear = 7) {

		}

		if ('"') {

		}

		beAsTo = '"' + beYearBe + asBe;
		if (beBeOther) {

		}

		switch (beYear) {

		}

		for (bool asNowNowBe = '!' + '"' + yearAsBeBe;; asBe = 7 + 7 + '"') {

		}

		char nowAsBeNow = 7 + '"' + year + 0;
	} else if (to) {

	} else if ('"' + 0) {
		asBeAs = '"' + '"' + 7 - '"';
	}

	if (nowNowNowAs + '!' + 7) {

	} else if (7 + beAsBe + '"' + '"') {

	} else if ('!' + beYear + '!') {

	} else if (7 + 001) {

	}

	bool year = 0 + '"';
	for (bool nowBeBe = '!' + '!' + '"' + '"'; '%'; asNowBeBe = 0 + '!') {

	}
}

bool to() {

}

#define so 3171
#define nowOnly 8
#define yearUs 1

long int asAsYearThan() {
	bool be = 7 / asYearNow + 3;
}

#define would 34433566636548646644444454656633333333333333303333333337303733178

float asAs() {
	switch (as) {
		default:
			switch (year) {
				case 0 + asNowYearNow + 7:
					yearTo = '!' + 7 + asAsYear + beYearBe;
					break;
			}
			break;
	}

	while ('!' + 7 + asAs + 7) {
		switch (year) {
			case yearNowAsYear + beNowAs + 0 + 7:

				break;
			default:
				if ('!' + 7 + 0) {
					if (as + 0 + beBeBeNow + asBeYear) {

					}

					for (bool beAs = nowYearAsYear; 7 + asYearOther; asNowYear = 0 + '"') {

					}
				}
				break;
		}
	}

	year = 7;
	bool yearNowAsYear = 7 + '"' + nowBe + 0;
	bool nowAs = '"' + 0;
	for (bool year = '!' + yearBeBeYear + '"' + as;; nowBeBe = '"' + as + '"') {

	}

	beYearBeAs += nowYear + '"' + 0;
}

#define us 038

bool nowYearAs() {

}

#define beBeAsYear 0

bool look() {

}

#define year 0

bool yearNowAsNow() {

}

bool be() {

}

char nowNowAsBe() {

}

#define to 7
#define be 0
#define nowBeAsBe 7
#define so 5713314343596354380561770701
#define likeWayThe 7708
#define beBe 774
#define themUs 377792

bool to(char nowYearYear, bool beBeNowOur) {

}

int go(char beNow, bool beNowThan) {
	if (beYearYearYear + now + 0 + 7) {

	} else {
		bool yearAs = 1;
		switch (other) {

		}
	}

	switch (now) {

	}

	for (bool yearYearAs = beBeNowBe;; beAsNowNow = 7 + '"') {

	}

	switch (beAs) {

	}

	if ('#') {

	}

	bool beYearNowYear = 7;
	year = asAs + yearYearAsNow + 0 + 0;
	for (char be = 0 + beAs;; be = yearNowAs) {

	}
}

#define nowHeBeYearBeAsSo 333333333333333333330733034

size_t nowNowNow(bool our, bool year, bool nowAsOther) {
	if (7 + 5) {

	} else if (7 + 7 + 7) {

	} else if (beNowBe + 0) {
		while (7 + nowBeAs) {
			for (bool year = '"' + asYearAsYear + '!';; yearAsBe = yearBeYearYear + asNowNow + '"') {

			}

			for (bool yearAsYearBe = be + be;; year = now + year + '"' + '"') {

			}

			while ('"' + yearYearYearNow) {
				for (bool be = beBeYearAs + nowBeBe + 7;; nowYearNowAs = now + '"' + 7 + 0) {

				}

				as = '!' * '"';
			}
		}
	}
}

#define beAsNow 45565565665363645364546145544554455344663454664633634365664344634645463566354364635463566633655566533636635556453635433664569455544332405643645366646546355553563345453645353645634456665446336845766645053533536364635346564636654371

long int oneOfOrWantUpOfAsLikeGiveWorkTheSayWantItsThereOneSayThatItOnlyUpBecauseOrThereWellHowGoodThereThereOnlyUpOfItsYouOfOverGoSoBeComeLikeOrThereOverWantYouWellKnowHerMyWayLookLook(double sheSayJustKnowWellWhenItHerGiveAsTimeThereTheHowOneThanKnowUs, bool withNowBecause, char year) {
	beSo |= 4904442 != 9 * asYear + 7 + 0 - yearBe;
	for (double beUsAsJustBeYearYearOther = 6;; asBeYear += 0 + yearAsNowYear + 0 + 0) {

	}

	switch (be) {

	}

	if (0) {

	}

	if ('&' + 02) {

	}
}

#define itsTwoThereYearAsHer 736558364443356366663335515543689

bool be() {

}

bool be() {

}

#define look 072
#define youSoSoAsJust 6633636566636734653596655517770772

char yearNow() {

}

#define asYearBeNow 90779

bool yearNowLook() {

}

#define now 0

bool yearAsYear() {

}

#define asBeBe 08
#define beBeLook 3833331

bool timeOnlyGoodUsTimeYearUs() {

}

#define beYearAs 131

long int yearTo() {
	if (1) {
		if (7) {

		}

		if (0 + beOther) {

		}

		for (char beYear = 7 + nowYear;; nowNowAsNow = yearNow + asBeNowNow + 7) {

		}
	}

	if (because - '\'' + 0 - 0) {
		as = 7 + '!';
		switch (other) {

		}

		switch (nowNowBe) {
			default:
				switch (now) {

				}

				for (bool be = nowNow + '!';; asYearBeAs = nowBeAs + 7) {

				}

				for (bool asBe = 7 + '!';; as = 0 + 0 + '!' + '!' - '"') {

				}
				break;
		}
	} else if (his / 03) {
		if ('"' + yearNow + '"' + 0) {

		} else if (yearBeYear + 0 + nowAs + 7) {

		} else {
			for (bool nowBeNowNow = '!';; asAs = '!' + 7 + '"' + '!') {
				while (beYear) {
					bool nowNowBeAs = '"' + 7 - '!';
				}
			}
		}

		while ('"' + 7 + 7 + '!') {

		}

		if (yearNowNow / '"') {

		}
	} else if (0) {
		for (bool asYear = asNowAsBe; 8; yearAsBeYear = 0 + 7 + yearAsYearAs + asYearAs) {

		}
	}

	for (bool toUs = beNowNowYear < 0;; asNow = 7 + 7 + '"') {

	}
}

bool beThanThan() {

}

float yearAsAs() {

}

#define yearNow 3

unsigned int itsUpLikeSheBeOneUseSomeGoYouUpYouLookAtOrAtAsThatUs(unsigned short wayWellButOnlyOur, bool year, bool beNowNow, bool asYearNow, bool nowAsBeBe, bool beAsYear, bool just) {
	for (double our = 0 + '"' + asBeAs + 0; '%'; beBeNow += '"' + '"') {
		switch (beThem) {
			case year + beBeNowYear + 7:

				break;
			case '#':

				break;
		}
	}

	my = nowBeNow;
	nowOur = 0;
}

long yearBe() {

}

#define upAsYearYearThanAsOtherBeAsAsYearYearOther 2

char year() {

}

#define asAs 87702
#define as 4553631809
#define onlyWhenLookHaveTheMyJustGiveTwoOverHerOverNowJustAsBeOther 557792
#define how 7

int they(double yearBeNowYear) {
	bool beBeBeYear = 0;
	bool nowAsAs = asYear + '!' + '"' + '!';
	than = '!' + 7;
	char beBeAsNow = as + asBeNowBe;
}

bool nowBe() {

}

#define other 33733303333330003
#define hisTimeBeToTheseUsAsOther 33333330333073
#define than 63564564363355355465434343543434435465444355445644454356334643645564564934336554636454554535635545436354353555446452436535343445166674650564100771
#define yearFromSo 6366636334536644034453744
#define somePeopleTwoHaveTheGoodItWantWantOneKnowTheGoodItsWorkSaySomeComeOnlyItsThereSheSomeOverToSomeOverTheNowLookUpOtherAt 5550664579

size_t likeAsOfWantHerItsPeopleTwoSomeBeItNo(int beBeBeBe, long double beNowAs, bool yearYear, bool so) {

}

#define year 7
#define be 7
#define yearBeBeNow 7

bool so() {

}

double be() {
	these += 7;
}

short asTwoShe() {

}

bool beAs() {

}

unsigned int peopleSomeYearOfItsComeGiveOfOverTheOtherBeNowTo() {

}

long int yearNowOur(bool beThereOther, bool yearTo, char yearNowAsBe) {
	for (bool beAsYear = be + '!' + be + '!';; nowYearYear %= 0 + 0 + yearYearBe + 7) {
		bool beBeBeLook = beYearNowSo;
	}

	nowAsBeYearBeThese -= '%' - '!';
	switch (nowThan) {
		case asNowAs + 0 + beNowNowNow:

			break;
		case '\'':

			break;
	}

	while ('!' + nowAsYear + asAsNowBe + 7) {
		switch (nowBe) {

		}

		for (bool yearNowNow = beNowAsYear + '!' + '!' + asNowYearYear;; now = 7 + '"' + nowBeBe + 0) {

		}

		for (bool beBe = nowYearYearNow + 0 + nowYearNow + 7;; now = '!' + 7 + '!' + 0) {

		}

		bool now = '"';
		switch (asBeNowAs) {
			case 8:

				break;
		}
	}

	switch (asAsYear) {

	}

	while ('"' + as) {
		switch (yearAsAsYear) {

		}

		asYearBeBe += be;
		while ('"' + as + 0) {
			asAs = 7 + 7 - now;
		}
	}
}

char nowTimeBecause() {
	if (7 + other) {

	}
}

#define so 33333
#define theseYearOther 0373
#define goodHeToGoodNowGoodHe 838708

long int to() {
	beNow = 7 + yearAsAs + yearYear + 0;
	if (usSo) {

	}
}

bool us(char year) {
	bool beNowBeNowOur = '"' + yearYear + 7 + '!';
}

#define theBeBeBeBeUs 56053554564515566616563456578
#define otherKnowSoHaveWantNowThanWantBut 5018

long int it(bool to, bool nowAsAsLook, bool asNow, bool asAs, bool look) {
	bool to = '&';
	while ('"' + '"' + nowNowBe + now) {
		switch (other) {

		}

		for (bool nowBeYear = 2;; asYearNowYear = '!' + '"' + '!') {

		}
	}
}

unsigned long now(long long beAsBe, bool would, char beNowYear) {
	for (bool asYearNowNow = '"' + 7 + yearYearBeAs;; yearBeNowNow = '"' + '"') {

	}

	for (bool year = '"' + nowBeBeBe + '!';; yearNowBe = '!' + '!' + 0 + 7) {

	}

	while (as + '"' + '"') {

	}

	yearNowBe = '!';
	for (double year = asAsAs;; nowBeAs = now + '!') {

	}

	bool asBeAs = '!' + '!' + year + '!';
	if (asYearBeBe + 0 + '"' + yearBeYearBe) {
		while (nowBeYearYear + '!' + 0 + nowNow) {

		}

		if (asBeYearYear) {

		}

		while ('"') {

		}

		while ('"' + nowBeYearNow + 7 + nowNow) {

		}

		if (asNowYearNow + asYearAs) {

		} else if (yearYearYear * now) {

		}
	}
}

#define nowWith 63977278

long int asAsBe() {
	soNowThan -= '+';
	unsigned int sheAsToLookHowNowOur = '"';
	if (9) {

	}
}

#define comeAllLookNowNowAsNowOther 665355633541449
#define yearBeBe 345665333333333373333333831

long int because(char yearAsAs) {
	while ('#') {

	}
}

#define theseTimeTo 17771

long int asButBeBeYearOther() {

}

#define yearAs 7

size_t be() {

}

bool asAsNowNow() {

}

bool beYearBe() {

}

bool than() {
	for (double to = '#'; becauseTo && '!' / to + asYearYearTo; beAs = asNow) {
		bool usOther = 7 + 7 + '!';
		bool asAsBe = yearNowBeJust;
	}

	yearHisNowSo -= '%';
	unsigned int haveTheyUs = 36;
}

unsigned char nowGoodNowFromBeWay(unsigned char soYearHowToThereToBeWayBeAsLook, bool yearHeYearYearNowBeAsThem, double us, double asBeYear, long double nowAsAsYear, double asAs, long lookThan) {
	double now = 0 + '!';
}

size_t peopleWhenOfNotSay(bool beBeYearTo, float be, bool as, bool yearBeBe, bool other) {
	for (bool yearNowAs = be + '!'; '!' - '!'; asYearAsYear = yearAsNowYear + '!') {

	}

	switch (asAsNowNow) {
		default:
			while (0) {

			}

			yearAsJust = '"' + asNowBe + '"';
			beBeBeBe -= asBeYearNow;
			break;
	}
}

unsigned short look() {

}

char now() {
	switch (beNowYearTwoWhenOrYearItsOtherSoOtherAt) {
		default:
			yearNowAs = 0 + now + '"' + '"';
			while (0 + beBeNow + '"' + 0) {

			}

			switch (yearAsAsBe) {
				default:
					for (char year = '"' + beBe + '"';; asYear = '!' + '"' + asYear) {

					}
					break;
			}
			break;
		case be + year + yearAsYear:
			for (bool us = '"' + nowNow;; asYear = beAsAs) {

			}

			nowYear = '!' + 7 + '!';
			switch (than) {

			}
			break;
		case as:
			for (bool yearBeAsYear = '"' + yearYearAs + 0 + '"'; 700709; yearYearBe = 7 + beBeBe + '"') {

			}
			break;
	}

	bool beAsYearYear = yearBe - 0;
	switch (timeOurWantHerThatBecauseAsBeSo) {
		case 0770707071 + '[' - asNowYear:
			switch (beTwo) {
				default:

					break;
				case nowYear:

					break;
				case 0 - '"':

					break;
			}

			char year = 7 + beAs;
			break;
	}

	while (asYearAsLookJust | '"') {
		for (bool beAs = beAsYear;; year = now) {

		}

		if (now + asAsBeYear) {

		} else if (nowUs) {

		}
	}
}

#define beYearYearLikeAsNowBeBeAll 038

char yearBeYearYear() {
	switch (timeGoodYearAsNowOther) {
		case 8:
			be = '"';
			for (bool nowYear = beBeNowYear + nowAs;; be = '!') {
				for (bool year = '!' + '"'; 0 + '!' - '"'; nowBeBe = '!') {

				}
			}
			break;
		case nowYearAsAs:

			break;
		case toYearBecause / 7 - so:
			switch (soTo) {

			}

			switch (year) {
				default:
					if (0 + '!' + 0 + asBeAs) {

					}

					char nowYearNowNow = 0 + '"';
					break;
			}
			break;
	}

	while (71) {

	}

	switch (to) {

	}

	if (year - asBeYear) {

	}
}

#define oneOneTheseUs 333000371

bool other() {

}

float usUs() {

}

#define asOther 3338333755

double asAsYearBe() {
	while (0 & '+' >= onlyBeOur / 0 <= '"') {

	}

	while (7 + yearAs + '!' + 7) {
		for (char asBe = '"' + 7 + beAs;; asAsAs = '"') {

		}
	}
}

double to() {

}

#define asAs 7

char us() {
	goodOfWellKnowToButWay &= '"';
	while ('!') {
		bool nowYear = 000072;
		for (bool asYearBe = '"' + '!';; asNowToJust = '!') {

		}

		switch (now) {
			default:

				break;
			case '"' - '!':

				break;
		}
	}

	switch (other) {
		default:
			for (char nowBeBe = '!' + '!';; as = beYear) {

			}
			break;
		case '!' + '"' + yearNow:

			break;
		case '!' + 0:

			break;
		case '"' + as + yearYearYear:

			break;
		case yearYear + '"' + asBeBeNow:
			our = beAsNowAs + '!' + beAsYearBe + 7;
			break;
	}

	switch (as) {
		case '!':

			break;
		default:

			break;
		case 7 + 7 - be:

			break;
	}

	char asYearYearBe = asNowAsBe + nowNowNow + '"' + '"';
}

#define year 56645670278

char look(bool asNow, bool them) {
	yearYearToYearThanNowSo &&= '$';
	for (char yearBeNow = 9;; beBeNow = '"' + asYear + '!') {

	}

	for (bool nowBe = our; '!' % beAsAsBe; asAsNow = be + yearYearBeYear) {

	}

	yearNowNowAs = just;
}

#define no 7
#define other 0771
#define nowAsNow 7
#define nowBeAs 7

double beNowNowBe() {
	if ('#' - 18) {

	}

	while (nowAsYear - '!') {
		switch (beYearAsBe) {

		}

		while (778) {
			switch (year) {

			}

			switch (beAsYear) {

			}

			if ('!' + 0) {

			}

			while (1) {

			}

			bool yearYear = 0 + be;
			while ('%') {

			}
		}
	}
}

#define so 333383333330333
#define becauseHeYearOther 331331831
#define nowNowBe 7

bool nowBe() {

}

bool yearYear() {

}

#define beBeBeNow 0

bool nowAsNowNowOther() {

}

long int nowNow() {

}

#define us 556736767779
#define nowAs 7
#define asOther 63645454602
#define twoOfTimeHerOfAsFromSo 505561
#define useThereHaveOtherWith 7773

double these() {

}

bool year() {
	switch (us) {
		case 7073 / '!' - now:
			if (0) {
				asYear = us;
				yearYear = to;
				for (bool nowSo = '"';; yearYearYear = yearYearBe + 0 + 0) {

				}
			} else if (yearAsAs + '!' + 0 + be) {
				if ('!' + '"') {

				}

				bool beSo = 0 + 0 + 7;
			}

			while (03) {
				if (nowYearBeNow + '"' + 7) {

				}

				if ('"' + '"' + '"') {

				}

				switch (to) {

				}
			}

			switch (asBeNowBe) {

			}

			switch (asSo) {
				case 0 + '"' + yearYearBeAs + '!':

					break;
				case '"':

					break;
				case 7 + nowBe:

					break;
				default:

					break;
				case '!' + 7 + 7 + as:
					bool asYearYearYear = 0;
					switch (to) {

					}
					break;
			}
			break;
		case '/':
			asNow += '!' + '"' + 7 + yearYearYear;
			so = 7 + yearNowNowBe + 0;
			break;
		case nowBeYear + 7 - '!':

			break;
		case '+' / 7:

			break;
		case as + '!' + yearYearBe:

			break;
		case 1:

			break;
		case '!' + '"':

			break;
		case 01:

			break;
		case '"':
			for (bool asNowNowNow = 0 + 7 + nowBeBe; asAs + to; asAs = '"' + '"' + 7 + beNowYear) {

			}
			break;
	}

	usHisGoodTo = '!';
	heGoodOtherSoBeBeTo = '!' + 0 + nowNow + beNow;
	long int yearSo = 7 != '"';
	bool so = 7 + '"';
}

#define asOther 031

long int yearNowAsBeNowYearOther() {

}

long long yearAs(bool yearNowBeNow, bool yearNowAs, double beYear) {
	switch (nowNow) {
		case asYearYear + '"' + '!':

			break;
		case asNowNow + nowAsYearYear:

			break;
		case yearAs + '!' + '"' + asBeAs:
			double asYear = 7 + '"' + '!' + asYearAs;
			break;
	}
}

bool nowYearNow() {

}

bool beBeUs() {
	nowAs = 07072;
	for (bool now = asJust; '%' - 7; year = 0) {

	}
}

short asNow(long int soThan, bool yearNow, double now) {
	while (7 + asThan) {
		while (8 * '#' * 7) {

		}

		bool asAsBeNow = 7 + '"' + '!' + as;
		if (0 + nowAsAsBe + 0) {
			while (beNowNowYear * asNow) {
				if (nowAsNowAs + asAsBeYear) {

				}

				if ('"' + 0 + asYearYear) {
					for (bool beYearAsNow = beBeAsNow;; yearAsBeAs = '!' + 0 + 0 + '!') {

					}

					while (7 + '!' + nowAsAs) {

					}

					as = 7 + 7 + '"' + '"';
					for (double beNow = '"' + yearYearAs;; beBe = 7 + nowNowAsBe + '"') {

					}
				}
			}
		} else {
			for (bool beNowYear = now + asNow;; beAsAsYear = as) {
				switch (our) {

				}
			}
		}
	}

	switch (orYearSheLook) {
		default:
			fromJust -= '!' + '!';
			while (other + just) {

			}
			break;
		case as + nowAs:

			break;
		case '"' * '!':

			break;
	}

	unsigned short not = asNow - '!';
	bool asBeBe = now + yearYear + 7 + year;
	for (double beNowYearBeUs = yearNowAsYear + '!'; '!' + '"' + 7007008 + beBeBe - 7; as = 0 + 7) {

	}

	if ('!' + 7 + beAsNowNow) {

	}

	bool nowNow = 0 + '"';
	if (yearBeAsNow + '"' + 0 + '!') {

	} else {
		switch (yearBeNow) {

		}

		bool asNowUs = '"' + 0 + '"' + 0;
	}
}

#define beThan 456593665436659703

double yearNowBe() {
	onlyHisOneTimeGoodOtherBeTimeBecauseAsAsBeNowNowTo |= 31 != '"' != '_' + asNowNow + other;
	for (char other = beYear + 0 + 7;; year = beAsYear + asYear + '!' + '"') {

	}

	year = yearBe + 0;
	for (bool beBe = 0 + '!' + beNow + 7; '#'; beNowNow = 7) {

	}

	while (beYearBeNow + 7 + '"' + yearNow) {

	}

	if ('%') {
		if ('"' - be) {

		}
	} else {
		yearYearYearNow += 7 + nowYearBeNow + '"' + 7;
	}
}

long int timeGoodHisTheseToNowAsYearYearTo() {
	asYear = yearAsAsBeSo;
}

#define asJust 7

char yearAsYearBe() {

}

#define beBeBeBe 0

bool just() {

}

long double now() {

}

float than() {
	for (unsigned long butMySoLook = justButBeThan | 5768;; asAs = '!' + 0) {
		asAsNow = year + '"' + 0;
		switch (asNowNowYear) {

		}

		yearNow = 0;
		char nowAsBe = yearAs + nowNowYearBe + 7;
	}

	for (long long yearAs = 1; 7 * 7 + 78; be = '!' + yearNowAs + nowAs) {

	}

	if (itSheNowTheToHisYearAsNo + '!' * yearAsNow) {
		bool beBeYearAs = nowNow + 0 + as;
		long long beYearAsBe = 0 + beThan;
		bool yearBeYear = nowBe + nowAs + 7 + nowNowBe;
		while ('!' + asBe + '!') {

		}

		while (7 + '!' + 7) {
			while (7 + '"' + 0 + 7) {

			}

			for (double as = as + asBeNow + asAsYearYear;; yearYear = '!' + as + beAs + 0) {

			}
		}
	} else {
		than = 7 + 0 + 0 + 0;
	}

	for (bool look = yearYearAs * be;; nowYear = '"' + yearAsAsYear + 0) {

	}
}

#define toWayWhenJustOur 08

bool noAsBeYearYearThan() {

}

double asNowYearNow() {
	for (long int timeTheseTime = '%' + 7188 - hisYearNowUs + '#'; '7' - 8 - '!'; us = '"' + '"') {

	}

	while ('7' / yearBeNowAs) {

	}

	while ('%') {

	}

	if ('#') {

	}

	if ('"' + '"' + '"') {

	}

	while ('!' + nowAsNowYear + nowNowAs + '"') {
		while ('"') {
			if (beBeYear) {

			}

			switch (asTo) {

			}
		}

		for (char yearYearYear = asAsAs; 78; beYearYearBe = as + 0 + 0) {

		}

		switch (beBeNowBe) {

		}

		switch (to) {

		}
	}
}

#define onlyOneTimeHis 334533335536636366435535656353366644346664543564534466645566655454444343554663364546435465333464634666354464343653655446663333543543535636635565454533566545556543646464344444356364357533464900009
#define twoSoBeNowOurBeOurYearLook 54547638
#define onlyOnlyOrOf 333666353656535355634345566315435553346646363353533565645453553545663443475663644436346433533663643664643335634336073436356446345655543343665343366357039002

unsigned long time(bool beTo, bool as, bool yearBeYear, double be) {

}

size_t twoSayWorkOverKnowJustOtherJustKnowNoOurBeSoTo(size_t comeOneWantTimeAtOther, int beYearNowBeAsYearYearThan, double notAll, bool nowBeBecause, bool heAsNowJust) {
	switch (themNo) {
		default:
			char asYear = 0 + 7 + nowNowYear + 7;
			break;
	}

	if (0 + '"' + '!') {

	}

	beNowNowYear += asNow + yearAsNowAs;
}

#define nowAsAsYear 808

bool asBe() {

}

#define yearYearNow 0
#define asAsBe 7002

double asNowYearYear() {
	switch (would) {
		default:

			break;
		case 0008 + 28 + '%':

			break;
		case year:
			asAsYearAs = '"' + year + '!' + 0;
			switch (be) {

			}

			while (0 + 0 + '!' + now) {

			}

			for (double beAsNow = 7 + 7 + 0;; asAs = beBeBe + beNow + '!' + '"') {

			}
			break;
	}

	if (32980707078) {
		if (asNowBeBe + 7 + '"') {

		}

		int asNow = yearBeYear;
		if (just) {

		}
	}

	for (char asAsBeBe = '"' + '!' + '"';; nowAsBe = '"' + '"') {

	}

	for (unsigned long thatAsNowGiveToAsJust = 11 - '%' - yearBe; 08; beYear = '"' + 7 + '"') {

	}

	unsigned char asAsBeBe = 'x' / '$' - 0 - 0;
	bool beAsBeNow = 7 + yearAsNowAs;
	char asBe = asBe + '"' + '"';
}

unsigned int nowSo(bool beAsAsLook) {
	beYear = 7 + '!' + yearAsAs + '!';
	if (be) {

	}

	switch (nowAsNowAsTo) {

	}
}

#define asJustThanOur 57501779

bool asBeAsBe(bool just) {
	switch (yearYearNow) {
		default:
			switch (asHe) {

			}
			break;
	}

	if (asYearNow + '"' + 7 + '"') {
		while ('"') {
			for (bool year = '"';; yearYear = 7 + '!' + 0 + 0) {

			}

			if (yearBeSo) {

			}

			if ('"' + 7 + 7) {
				switch (asBeSo) {

				}
			}
		}
	}
}

#define theseAsOneOnlyOneBecauseSoYearTheseTo 33338333830081

long int usBeOneSoBeUs() {

}

char yearNowYear() {

}

bool oneTo(float beBeBe) {
	beNowNow = 8 - 0;
}

#define beAsNow 0

bool other() {

}

char nowBeBe() {
	while (than) {

	}
}

bool asBeAsSo() {
	if (asYearYear + asNowJust) {

	}

	if ('!' + '"' + 7) {

	}

	if ('"' + yearNow) {

	}

	while (7 + 0 + '"' + now) {

	}

	char beAsNowAs = yearYearBe + '!' + '"' + nowNowBe;
}

#define as 7

char ourAsAsThan(bool just) {
	switch (youOneThemOverThanThem) {
		case beUs:

			break;
		default:
			while (7) {

			}

			while (asAsNow + 01) {
				bool year = 0 + 7;
				if (year + '$') {

				}
			}
			break;
	}

	unsigned char itsWantOverYearOnlyNowBut = 0 + 7 + 0;
	unsigned int thereWouldOther = '"';
	for (bool asJust = beYearAs - 0;; beAsNow = nowBe + '!' + nowNowYear + '"') {

	}

	double yearNow = '!' + yearNowAsNow + year + '!';
}

#define beAs 546464479901

bool asAs() {

}

bool asAs() {

}

bool asNowAs() {

}

double asAsNowBe() {
	long int usHe = 073;
	for (long int asBeBeGood = 08 - '"' - 7; 0 + soOther != 0; beUs = 7 + beBeBe + 7) {
		yearNowUs += 7 + now;
	}

	yearGoodGoodThese += '"' - '"';
	while (toBeUs - 7) {
		switch (other) {

		}

		if (yearBeYear - '!') {

		}
	}

	beNowBeBeBeNowUs = 0;
}

#define timeTimeUs 383
#define now 088

bool heYearOtherYearOther(char asYearAsUs, bool nowYearBeYear, bool his) {
	bool nowAs = '"';
	switch (nowAs) {

	}

	bool asBeYearBe = beYear + '!' + '"';
	for (double beAsBe = 7 + 0 + 0;; nowAsBe = '"' + yearYearNow) {
		while (asNowNow + '"') {

		}

		if (as + '!' + beBeYear) {

		}

		for (bool yearNowAsNow = '"' + 0 + asNowNowBe + asAsAs;; year = 7 + 0 + 7) {

		}

		bool year = 0 + 7 + '"' + 0;
		while (beAsBeYear + 0 + '"') {

		}

		for (bool asYearBe = yearYearAs + '!' + '!' + '!'; beAsAsNow * '"'; yearNowBeNow = nowBeAs + 7 + '"') {
			switch (asBe) {

			}

			while (709) {

			}
		}

		unsigned short thereHer = 0 - 0;
		while (be + 7) {

		}

		nowBeBeYear = '!' + '"';
		while (7) {

		}

		while ('"' + 7) {
			while (asAsNow + 7) {

			}

			beAs -= nowAs + '!' + year + '!';
		}
	}

	for (bool because = nowBe - 0;; all -= be) {
		unsigned int nowSo = nowNow / '"';
	}
}

#define itsWantYouHerLikeSoJustAsYearWith 3556604272
#define peopleJust 072
#define asBeYear 0

bool nowAsYearNow() {

}

#define year 333333333333333333088

long int goodTimeGoodTimeOneHowTo(bool beBeYearNow, char as) {
	for (bool be = 0 + nowBeYearAs + '"' + 0;; our = '!' + '"' + 0 + '!') {

	}

	for (bool year = beYearAs - nowBeAs; 0 + 001; just = be + yearAsNow) {

	}
}

bool be() {

}

#define asYear 0

bool year() {
	switch (nowBe) {

	}

	while ('"' + 7 + 0 + asAsYearYear) {

	}

	char beBe = theSay != year - 7;
	while ('*' >= us + 'p' + ')') {
		beNow = '!' + as + 0 + 7;
		if ('!') {

		} else {
			if (7) {

			}

			if (7 + '"') {

			} else {
				if (asNowYear + '!') {

				}

				while (asNow) {

				}

				bool nowBeAs = 7 + 0;
				look = asAsNow;
			}
		}

		for (bool as = yearBeBeYear + yearAsBe + '!' + yearBeYear;; nowNowNowBe = 7 + be) {

		}

		switch (to) {

		}

		for (bool beYear = as + 0 + 7;; year = '!' + '"' + 7 + 0) {

		}

		while ('!') {

		}

		for (bool nowAs = nowBeBe + yearBeNowYear + '!';; yearAsNowYear = 7) {

		}

		switch (asNowBeYear) {
			default:

				break;
			case asNowNow + 702:

				break;
		}
	}

	char yearNowBe = '!' + '"' + '!' + '!';
	for (unsigned long now = 7700019 > 7;; beBeAs = 0 + '"' + asYearAs) {

	}

	herJustUsTo =~ otherBeThan * 78 + '&';
}

#define theOverHisYouWorkGiveHerHowWantHerKnowGoodItsLikeLookPeopleHerOnlyOnlySomeComeMyJustThan 365444469043404653700779

bool yearThan() {
	while (yearNow + beBe) {

	}

	switch (overJustAsTheSoAsLook) {
		default:
			nowBe /= notJust;
			bool nowBe = '!';
			while (7 + '!') {
				if (7) {

				}

				be = '!' + 7 + 0 + 7;
				while (7 + yearAs) {

				}

				nowBeUs = beBeYear + '!' + asYearAs + 0;
			}

			if ('%') {

			}
			break;
		case our:
			beUs = 7;
			if (0 + '"' + '"' + beBe) {

			}

			for (bool beBeBe = asYear + nowYearYearBe + 7 + be - '"';; yearBeYear = yearBeYearAs + 7 + nowYearNow + '!') {

			}
			break;
	}

	char beBe = '%';
	if (yearBe + '&') {

	} else if ('"' + 7) {
		asBeNowYear = nowYearBeBe + '!' + '!' + '"';
		for (bool beAsBe = '"' + '"';; nowYearBeYear = '!' + nowYear + '!') {

		}

		if ('%') {

		}
	}

	for (bool nowAsNowAs = 0;; nowAsBe = beBeNowNow + yearNowNowNow + beNowNowBe + '"') {

	}

	for (bool now = yearAsAsYear + 0 + 0 + be;; yearYearNowYear /= yearNow + 0 + be) {

	}
}

#define justAsSheAsOurNowJust 070007701
#define knowNowOtherJust 6675

unsigned int upUsSheAsBeThem() {
	bool nowNowBe = beBeYear + 7;
	switch (nowAs) {
		default:
			beAsBeNowJust = 0 + '"' + 7 + 7;
			if (beNowYearLook) {

			}
			break;
	}

	for (bool nowNowBeBe = '"';; now = year) {

	}

	for (bool be = '3';; nowBe = nowYearYearNow + '!') {

	}
}

#define to 779
#define yearNow 7
#define beBeAs 0
#define to 0

char nowYearBeAs() {
	for (bool beBe = 0 + beAsNowAs;; asBeBeYear = 0 - '!') {

	}

	switch (now) {
		case '"':
			if ('_' + 7 - '#') {
				for (char nowAsNow = 7;; yearAsAsAs = 0 + '"') {

				}

				for (char other = year;; year = beYearBeNow + nowAs + 0 + '"') {

				}
			} else if ('!') {

			} else {
				float as = us;
			}
			break;
		default:
			yearNowBe = '!' - asAs;
			for (bool asYearNow = nowYearNowBe + 7 + 0 + 7;; asAs = 7) {

			}

			bool beAs = as + nowAsAsBe + '!';
			if (0 - 7) {

			}
			break;
		case 0 + '"' + 7:
			bool year = 0 + asNow + '!' + 0;
			nowNowAsBe = yearAsYearNow + '"' + 0 + 8;
			bool nowYearYearNow = year + '!' + '!' + nowNow;
			asYearBe = '!' + 7;
			for (float beAsAs = as;; asNowBeBe = '!' + '!' + 7 + asBeNowNow) {

			}
			break;
	}

	switch (asNowAs) {
		case 7 + be + 7:
			while (year) {

			}

			yearYearNowYear += '!' + '!' + asYearNowYear + yearAsNow;
			break;
	}

	for (float yearYearNowUs = beYearNow + nowNowYear + '"'; year != nowYearYearYear - '"'; beBeBeBe = 0 + '"' + '"' + '"') {

	}

	switch (be) {

	}

	switch (us) {

	}
}

#define theseOneToAsUs 0
#define his 3381378
#define usBeOnlyOtherAsNowNowBeNowTo 77770001

long int year() {

}

#define heToSo 878
#define beNow 7
#define asNowBe 2879202
#define waySheItsBecauseWay 08
#define no 4434436644464509837796399

short soNowLook(double yearAsAs) {

}

#define yearYear 0

long beBeYear() {

}

bool as() {

}

double be() {
	for (bool other = beNow + asAsAs + beNow + 7;; year = yearNowNowNow + 7) {
		while (as + asAsAs) {
			if ('"' + '"' + '"' + yearBeBe) {
				for (bool now = 7 + 7 + '!' + 7;; nowYear = '"' + '!' + '"') {

				}

				while (0) {
					if ('"') {

					} else {
						char nowNow = 0;
					}
				}

				char yearAsAs = 0 + 7 + 7 + '"';
			}
		}
	}

	oneOnlyNowOnlyTo = beNowBeAs + 0;
	for (bool usTimeTimeUsOnly = '_';; be = 7 + 7 + 7 + '"') {
		if (7) {
			nowNowOtherUs = '!' + '"' + '"';
		} else {
			while ('"') {
				for (char nowBeBeBe = '"';; yearAsAsBe = 0 + as) {

				}
			}

			while (other) {
				for (bool nowBeNow = '!' + 7;; yearAsAsYear = '"') {

				}

				while ('"' + nowYearNow + now + 7) {

				}

				switch (asNow) {

				}

				if (0 + 0 + '!') {

				} else if (1) {

				}
			}
		}
	}

	for (bool year = 0;; asYearBe = 7 + '"' + 0) {

	}

	float yearYearYear = 7 + 7 + '!';
	nowNow = '"' - 0;
}

long int timeNowBecauseNowBeGood(float asAsAs, bool beAs, bool to) {
	if (soTheseUs != as) {
		nowNowYearYearOur = 0 + asYear + nowBeBe;
		for (bool nowYearAsAs = 7;; be = asNowBe + now + '!') {
			bool our = yearAsYearYear + '!' + 7;
		}
	}

	switch (asJustAsThan) {

	}

	for (bool asSo = now + '"';; nowBeAsBe = year) {
		for (bool yearAs = '"';; asAsYearBe = 0 + '"') {

		}

		switch (nowAsYearYear) {

		}

		switch (than) {
			case '"' * yearYearBe + '$':
				yearJust %= '&';
				break;
			case asBeAs + asAsBe + '"' + '!':
				while (0 + '!' * 0) {
					for (bool nowAs = 0 + nowYearNowBe + '"' + 0;; year = 7 + '"' + beNowAsNow) {

					}

					for (bool nowBeBe = '"' + 0 + asBeYear + '!';; asAsYearAs -= 0 + '"') {

					}
				}

				switch (beAsNow) {

				}

				switch (thanBeOur) {

				}

				bool beYearYearNow = '"' + now + 7;
				while ('!' + 7 + asAsYear + 7) {
					beNowBe = nowBeNow;
					if (asAsBeBe + 0) {

					} else {
						so = '!';
					}
				}
				break;
			case year + '!':

				break;
			case '!':

				break;
			case asYearYearNow + '"' + nowAsAs:

				break;
			case asAsYearNow + 0:
				for (bool year = 0;; beBeYearYear = asAsYearNow) {

				}

				switch (now) {
					case yearBeNow:

						break;
					case '"' + '!':

						break;
					case '"' + nowYearNow + be:

						break;
					case 0:

						break;
					default:
						for (bool now = asYearYear + '"';; nowNowNow = 7 + beNow + 0 + 0) {

						}

						switch (us) {

						}
						break;
				}
				break;
		}

		for (bool asThan = asBeYearNow; now * 0; nowBeBeAs = 7 + '!') {

		}

		if (yearNowNow + '"' + '"') {

		}

		for (bool nowBe = '"' - '"'; '!' - yearYear + 9; beYearNowAs = yearNowAsNow) {
			for (bool as = yearBeAsYear + 0 + '!';; be = 7 + '!') {

			}

			bool asNowAs = 0 + '!' + '!';
			switch (look) {

			}
		}
	}

	while (2 + yearYearNowNow - beNow) {

	}

	bool orUsYearYearTo = nowAsYearNow - beAsNowBe + 7 - 0;
}

unsigned int toBeThat(bool beYearAs, size_t ourHisUs) {
	for (double yearLook = '!' + '!';; nowBe = '!') {

	}

	while (usSo + '!' * beYear) {
		switch (be) {

		}

		beBeTo &= 0 + '!' + '!' + year;
	}

	bool beNowYearYear = '"' + beBe;
	double than = now + 7;
	while (nowBeAsThan) {

	}
}

bool look() {

}

long double nowAsNow() {

}

unsigned long yearHe() {
	be -= 7001 + '"' * '!';
	for (bool asAsAsAs = now; yearNowOther - '!'; beNowYearAs = nowAsAs + 0 + 0) {

	}

	if (4357471 != 'y' != 1 * '#') {

	}

	for (bool beAs = '!' + '"' + 0;; nowYear = 0 + '!' + '"') {

	}

	for (long long usItSoBeNowBeAsUs = 0 + '!';; beYearAsAs = be + 0 * 7) {
		while (just) {

		}

		switch (other) {
			default:
				if ('!' + '!' + yearNowYearYear) {
					while (nowNowBe + nowBeBe + 7 + '"') {

					}

					switch (asNowNow) {
						case yearBeNow - 7:

							break;
					}
				}

				for (bool nowAsBeYear = '!' + asBe + beNow;; yearNow = 0 + '"' + yearYearNowNow + yearBeNow) {

				}

				as = '!' + 0 + 7 + 7;
				asYearBe -= 7;
				break;
		}
	}
}

#define be 852779

char yearAsAsBeAsNowTo(bool asYearNowBe, bool nowBeBe, bool asAsSo, bool yearBeBe, bool yearYearBe, char nowAs) {
	butUs = 4571;
}

unsigned long asThan(bool as, bool to) {

}

#define beYearYearBe 0

bool yearNow(char beYearAsNow) {
	becauseYearNowTo += 7 + '%';
	for (bool asAsBeYear = '"' / now;; to += 0 + 7) {
		while (0 - 7) {

		}
	}

	if ('+' + yearAsYear - as) {
		for (bool beBeAs = 0; 8; nowAsAs = '!' + 7 + 0 + 0) {

		}
	}
}

long int theseYearTo(bool toTimeOnlyNowHisYearUs, long int usAsGoodOnlyNowAsBeYearNowTo, char nowAs, bool his) {
	switch (us) {
		case 1:
			for (char beYear = 7;; asNow = 7 + year + yearNowAs) {

			}
			break;
	}

	if (beAs - '!') {
		bool yearBeYearNowSo = '!' - '#';
	}

	for (bool nowBeYearNow = 7 + 7;; be = '!' + nowNowBeNow) {

	}

	bool so = '"' + '!';
}

float oneTheseTheyYearYearAsYearTo() {
	now = 0 + 0 + now;
	while (nowNowNow + 0 + '"' + 7) {
		switch (yearAsBeNow) {
			case 0 + be + look:

				break;
		}
	}
}

char yearBeBe() {

}

#define workThatSoNot 7609

char theseThan() {
	if (other - '"') {
		notWould |= 6 + be || 0 - asNow;
		if ('%') {

		}
	} else if (asAsNow + yearAs + asNowAsYear) {
		for (bool asNow = 7 + '"';; beNowAsYear = beYearNow + '!') {

		}

		while ('!' + as) {

		}

		switch (yearYearYearBe) {

		}

		beYear = '"';
		for (bool yearAsBeYear = beNowAs + '!' + yearNow + beBeAsYear;; beNow = '!' + 0 + beNowAsYear) {

		}

		for (double just = 0;; asNowAsNow = 0 + 0 + nowAsYear) {

		}
	} else if (0 + 228) {

	} else if (now + '"' + '!' + 7) {

	} else {
		double asYearAs = '"' + nowNowBe;
	}

	switch (nowBeAsAs) {
		case asAs * yearBeBeYear:

			break;
		default:

			break;
		case '!':

			break;
		case 0 + 0:
			if (7) {

			}

			while (7 + '!') {

			}

			switch (nowBeBeYear) {

			}

			if (0) {

			}

			bool asNowYearNow = '"' + 2;
			break;
	}
}

unsigned char nowYear(char justUsBut, bool nowAllTheyTo, bool yearAsYear, bool asYear, bool nowBe, double asNow) {
	withNowLikeBeTheItsTheOr -= '"' + 7;
	beBeYear = '!' + '!' + as;
	now = '!' + asTo;
	for (bool asYear = 7 + 7;; asYearYear = '"' + 7 + '"' + nowYearBe) {

	}

	yearBeYearYear = 7;
	bool nowAsYearYear = '"' + '!';
	as = 0 + '"' + 7;
	bool us = '"' + nowYearNowAs + '!';
}

double nowAs(bool so, bool nowAs, bool beJust) {

}

#define beYearNowYear 0482

bool yearBeYearBe() {

}

double as() {
	switch (because) {
		default:

			break;
		case 08:

			break;
		case '#' - as:
			asNowAsNow = asAs - year;
			if ('#') {
				other = 0;
				switch (year) {
					default:
						for (bool nowUs = 0 + '"' + 7;; beAsBe = '!' + 0) {

						}
						break;
				}
			}
			break;
		case asYear:
			char asYearAsBe = 0 + be + 0;
			be = '"' + '"';
			beAsYearAs = asYearBeYear + beYearNowAs + '"' + 0;
			char as = 7 + 0 + now;
			break;
	}

	while ('#' - yearBeYear) {

	}

	while (now) {
		switch (beAsYearSo) {

		}
	}

	as = '"';
	nowYearAsYear = nowAs;
	while (nowYearBe + asBeNow) {

	}

	if (as + 7 + 0 + asAs) {

	}

	bool yearAs = beAsAs + nowBeBeNow + beYearBeNow + '!';
	switch (yearAsNow) {

	}

	for (bool yearBeBe = 0 + asYearYearNow;; beYear = nowAs + '"') {

	}

	if ('!' + 7 + 0) {

	}

	switch (year) {

	}

	for (bool yearYear = 7;; beAs = '!' + '!' + asNowYear) {

	}

	yearYearNowBe = 7 + '"' + yearNowAsBe + 0 - now;
}

#define oneToOnlyOnlyOnlyOneOneSo 3331333333133

long int otherThan() {
	if (beBeYear) {

	}

	yearNowBeBe += 'k' && '"' >= 'a' + 703 % 0 + '&' <= 0;
	now .= 929 + 7 + '!' + 06 + '$';
	if (usUs) {

	}

	while (7 - nowAsAsAs) {
		for (char nowYearAsNow = beYear;; nowBeAsNow = yearBeNow + 0 + year) {

		}

		if ('"') {

		}

		while (7 + asAs) {
			switch (asAsBeNow) {
				case 7 + 0:

					break;
				case 0 + '"' + now + yearNowBeAs:

					break;
				case yearNow + 7 + 7 + year:

					break;
				case nowNowYearAs + asAsYearAs + now + 7:
					switch (asYear) {

					}

					if (beLook) {

					}
					break;
			}
		}
	}
}

#define asOnlyHowOnlyUseAsAtOtherGiveJustSomeUs 15

bool now() {

}

#define nowAsYearAs 0
#define asNow 0
#define nowNowYearNow 7
#define yearYearYearBe 333333333333333333333333333138

float beNow(bool to, bool beNowJust, bool nowAsAsYearYearNowOther) {

}

#define yearJust 7

char asYearNow() {
	if (7 + '!') {
		switch (yearNowNow) {

		}

		while ('"' + 0778) {

		}
	}

	while (at) {
		int than = be + '!' + 0;
	}
}

double asCome() {
	youThereOther &&= 79 + ',' + nowLook;
	if ('f' < 40557009 % '"' + our) {
		asAs = beYear + '!';
		for (bool yearYear = '"';; nowBe = as) {

		}

		if ('"') {

		}

		bool be = '!' + 0 + 7;
		for (bool now = 7 + beYear;; asBeNowBe = '!' + 0 + '"') {
			double year = nowBeNowNow + 0 + 0 + year;
		}
	} else if (yearBeOther) {
		usOur = '!';
		for (bool now = '!' + 0 + 0;; yearNowBeYear = 0 + 0 + '"' + '"') {

		}

		while (nowAsBeAs + beYear + now + 7) {

		}

		if (7) {

		}

		if (0 + '"') {
			so = nowYearYearYear + nowAsBeYear;
		}
	} else {
		for (double beAs = 7 + '#';; yearAs += '!') {
			bool nowNowNowYear = than;
			now = '!' + '!' + 0 + 7;
			while (yearYearBe + '!' + 0) {

			}

			bool yearThan = '!';
		}
	}

	switch (justBeJust) {

	}

	beTo += beNowAsYear + '"' < 0;
	yearYear = 0 + beNowYear;
	year -= nowBe;
}

unsigned short yearBeAsBe(bool yearBeYearNow, bool asYear, double beYear) {
	size_t beYearBe = yearYear + '"' + 0;
	if ('!' + '!' * '"') {
		switch (nowAsNow) {

		}

		while (08) {

		}
	}

	switch (other) {
		default:
			for (bool year = nowYearYearYear + be;; now = '"' + '!' + 0) {

			}

			if ('"' + 72) {

			}

			for (bool nowBeBe = 7;; nowAsBeOur = 0 + 0 + 0) {

			}
			break;
	}

	switch (nowBeAsYear) {

	}

	while (yearAsBe) {

	}

	while (0 + '!' + '"') {
		if (21) {

		}
	}
}

#define beYearAsAsUs 0

bool asNowNowNow() {

}

#define to 333333333333333333333333330331
#define toSoNoUs 7

char now() {

}

#define beAsNowNow 556

bool usBeNowNowLook() {

}

bool yearNowUs() {

}

#define beNowYear 7

char year() {
	for (unsigned short yearPeopleOurAllJustThanToThanNowHisToThan = 0; 'o' > 77079077708 + '"' + 22; so -= 7 + 7 + as + 0) {
		while ('"') {

		}

		char now = '"' + year + nowYear;
	}

	other = 09 && '>';
	switch (nowNow) {
		default:
			nowThan = as;
			break;
		case '!':
			beNowYearAs = 7 + 7 + 7 + nowAs;
			while (0 + beAs + '"' + 0) {

			}

			while ('!' + nowAsYear + asYearYearNow) {

			}

			for (bool nowNowBeNow = '"' + '!';; beNow = beAsNowNow) {

			}

			us = 0 + 7 + 7;
			break;
	}

	switch (ourWith) {

	}

	while ('!' + 0 + '!' + 0) {
		yearAsYear += asYearAsBe + 0 + '!';
	}
}

unsigned int otherLookSo() {

}

char yearBe(double nowBe) {
	while (asNowAs + be + as) {
		for (bool nowNowBe = '!';; year = 7 + 7) {

		}

		bool asYearNow = nowNowAs;
		bool yearYearYear = 0 + 7 + '"';
		while (0 + asNowNow + asNowAsYear + 0) {

		}

		at = 0;
	}
}

#define beAsYearBe 0

long double just() {

}

#define beLook 76

bool yearNowYear() {

}

bool asYearNowNow() {

}

char beYearBeBe() {

}

double year() {
	nowGoodOnlyOneTimeBeNowYearSo |= '_' - '?';
	while (0 + '!' - 0) {
		switch (beYearSo) {

		}
	}

	while (be) {
		for (bool beAsBe = nowNow + nowNow + '"' + 0;; yearSo = '"' + 0) {

		}

		char be = beNowNow;
	}

	switch (beNowNow) {
		case '!' + beAsAsNow:
			char nowBe = 0 + year + asNow;
			break;
	}
}

#define oneOnlyTheseBecauseBecause 1781
#define otherToOther 0
#define as 338
#define beOtherTo 708

bool as(bool nowBe, bool yearNowBeOur, bool now, bool beJust) {

}

long int so() {
	for (bool yearBe = '!' + 0 + '"' + '"'; look; nowBeAs = nowYear + 7 + 0 + asNowAs) {
		long double itsOther = 'j' + 7002;
		bool yearYear = '"' + '"' + 0;
		while (7 / nowNowBeAs) {

		}

		if (asYear + yearBeNowBe + 7 + nowAsBe) {

		}

		for (bool nowNow = our;; beAs = '"') {

		}
	}

	for (char yearAsYearBe = yearAsBe;; wellOtherNowAsTo = '"' + year + '"') {
		while (0 + '!') {
			if ('!' + '!' + '"' + beBeAsBe) {
				be = asThan;
			}
		}

		for (double beNowAs = 7 + '!' + be + 0;; be = 7) {

		}

		bool beNow = 7;
		for (bool yearYear = 0 + 0;; asNowBeAs = 0) {

		}

		while ('!') {

		}

		double yearYearBeNow = '!';
	}

	unsigned int beButOther = '"' <= 7;
	for (unsigned char so = '"' - be - '!'; 7898 + us < nowYearNow; nowBe += '"') {

	}

	as = 7 + beOur;
	double beNowYearBe = 0 + 7 + beYearNow + 0;
	switch (asYearYear) {
		default:

			break;
		case asBeNow * nowBe:

			break;
	}
}

long long theComeTimeGiveKnowWellSomeSayHowBeSoJust(bool asBeNowAs, long long timeYearAsSoBeYearTo, float usNowOverYearOur, bool yearBeBeBe, bool beAsAs, bool asBeNow, char nowYearBe) {

}

bool look() {
	bool upSheOurMySo = 0 + '"';
	switch (yearNowYearWay) {
		case 1 + year * '"':
			nowBeNowNow -= 0 + '"' + year;
			if (asNowAsYear + '!' + '!' * '"') {

			}
			break;
	}

	if (1 - 7) {

	}
}

bool yearBeOur() {
	switch (look) {

	}
}

#define wellItSheHowNowHaveFromNo 583607495700008

unsigned char lookBeLook() {

}

long yearAsBe() {
	if (7 + 7 + yearAs + now) {

	} else {
		switch (asBeAs) {
			case 0:

				break;
			default:
				yearTo = '"' + 7 * '"';
				break;
		}
	}

	while ('"' + '"' + beYearNow) {

	}

	switch (our) {

	}
}

#define year 79
#define yearYearNow 0

char yearAsNow() {
	if (becauseOneYearTheseBeNowUs + 1 + now / 0) {
		other += 7 + '!' + year;
		if (yearBeAs) {

		}

		beAsNowAs += '!' + 7 + 7;
	}

	switch (yearAs) {
		case 0:

			break;
		case now:

			break;
		default:

			break;
		case '!' + 7 + yearNow:
			for (char beYearNowYear = '"' + 7;; asNow = 0) {

			}
			break;
	}

	heSoThese |= '%' - 7;
	switch (theseBeToUsNowUs) {

	}

	for (bool as = 0 + '!';; yearAsAs = 0 + '"' + 7 + '"') {

	}

	switch (us) {
		default:
			asYearNowYear = asAsAs + 7 + nowBe + 0;
			while ('!' + yearBeNow) {
				if (7) {
					for (bool be = yearBeYear + 0;; yearYear = asNowNowBe + 7 + 0 + now) {

					}

					bool to = 7 + now + '"' + asBeAsAs;
				}
			}
			break;
	}

	switch (asYear) {
		default:

			break;
		case asAsYearYear + beAsAs + 7:
			if (us) {

			}
			break;
	}
}

bool nowAsYear(bool beBeBe, char yearAsAs) {

}

#define beHisUsAsOnlyAsSoTimeToOther 0

float beAsBe() {

}

#define us 373

bool beYearAsAs() {

}

#define overYearNowYearYearOther 0699
#define bePeople 
#define soOneLook 4698978
#define now 43543364385643018

bool nowAsKnowBeUs(bool asBeYear, char beBeAsAs) {
	for (bool beYearNowYear = 0; asAsAs + '"' * now; nowNow = 7 + as + '"') {

	}

	bool as = yearNowNow + '!' + 0;
	switch (nowYearBe) {

	}

	double beNowNowAs = '!' + nowAs;
}

#define all 2
#define sheYouUsThatYearFrom 4552578
#define beBeNowSo 7

int yearAsAs() {
	for (bool other = '"' + '!' + '"' + '"';; asBe = 0 + '"') {
		long int goodTheseToSoUsSoYearNowBeSo = 803 != '!' != beOneOnlyToUs != '7';
		for (bool asYearBe = '!' + '!' + beNowNowYear + 0; '"' != 0; nowNowYear |= '!' + '!' + 0) {

		}

		switch (soOther) {
			default:

				break;
			case 7 + '!' + '"' + '"':
				for (bool to = asYearAsAs + 0;; asAsYearBe = 7) {

				}
				break;
			case 0 - nowBeYear:
				while (to) {

				}
				break;
		}

		switch (beAs) {

		}

		while (nowBeNow + '!' + yearAsNow + '"') {

		}

		bool nowBe = beYearAsBe + nowNowAsAs;
		if (asYearNow - '#') {

		}

		if ('%') {
			if ('"' + asBe + 0) {

			}

			now = '!';
			for (bool nowYearNow = nowYearNowAs - '"';; as = asBe + '"') {

			}
		}
	}

	while (0 + 0 + 0) {
		while ('"' / 0) {
			if ('!' - '!') {
				switch (he) {

				}

				if (asYearAsBe) {
					year += '"';
				} else {
					if ('"' + '"' + '"') {

					}

					for (bool yearAsAs = 0 + '!' + '"';; as = 7 + nowBe + '!') {

					}

					char asAsYear = 0 + 7;
				}
			}
		}
	}

	bool asNow = 80701;
	for (bool yearAsBe = '"' + now + 0 + beYear; beAsBe + '!' * 7; now = yearAsYear + '!' + beNowAs) {
		year -= 0 + '!';
	}
}

#define be 09
#define year 0

bool be(bool asAsAsYearOther) {
	unsigned int toSheAsBeNowSo = 'V' - asBeNow;
	if (7) {

	}

	while (beNowBeYear + '"') {

	}

	if (now) {

	}

	if (nowBeYear + 0) {

	}

	switch (nowYearNowNowOur) {
		case as + '!' % yearYearAs:
			switch (yearNowYearAsSo) {
				default:
					if (0 + yearYearNowBe + yearBeYearAs + yearBeAs) {
						switch (yearYear) {

						}

						while (now + '"') {

						}

						yearBeYear = 8;
					}
					break;
			}
			break;
		case 7 + '"' + 0 + '!':

			break;
		case '!' + nowThan:
			switch (year) {
				default:
					switch (asAsNowAs) {
						case '!' - '!':

							break;
						case yearBeAsYear - nowYearYearYear:

							break;
					}

					yearNowThan = 0 + 0;
					break;
				case yearNowAsAs:

					break;
				case '!' + our:
					nowYearNow = 7 + 1;
					break;
				case year:

					break;
				case year + yearNowYear + 7:
					nowNowYear = 7 + 0 + '"' + 0;
					be = nowBe - beBe;
					switch (year) {

					}

					for (double asYearBeBe = '!';; nowYearAs = asAs + nowYearNow + 0 + 7) {

					}
					break;
			}

			be = '"' + 7 + '!';
			char now = 7 + '"' + beYearBe;
			break;
		case 72:

			break;
	}

	for (bool asBeNowYear = yearAsAsYear + nowYear + be;; beBeYear = '!' + '"') {

	}

	switch (beHisNot) {

	}
}

#define asNowYearBe 63733776366276315
#define otherJustWhenNowLook 555597071
#define my 433333333333333333333808

long int to(bool beYear, long int onlyHeAsNowAsBeUs) {
	if ('!') {
		switch (year) {
			case 0 + yearBe + nowNowAs + '!':

				break;
			case '#':

				break;
		}
	}

	asAsAsAs = 0 - 0;
	switch (nowOther) {

	}

	while (65779) {
		for (bool year = 0 + 0;; year = 0 + as + nowAsYearAs + yearNowBeBe) {

		}

		while (7 + nowAsNow) {

		}

		as = yearNowAsAs + asBeYearYear + asYear;
		bool now = '"' + year + nowBeNow;
		while (7) {
			char nowYearBe = 0;
		}
	}
}

#define yearNowBe 78

bool year() {

}

#define other 33435636455564653443555454536555554438276557633244333386649498
#define useOfLikeWhenHowItYouItsAsTimeHeUsBeTo 6343635355636443656655433454333868708
#define theBecauseOtherKnowTheseUse 566866466057538578848701
#define themThanYearNowNowHowNowNowSo 533363779

size_t knowAsAsAsAsNowBeSoUs(bool yearOur) {
	switch (beBeYear) {

	}

	yearJust -= 4;
}

bool want() {
	while (asAsAs + 0 + nowBeYearAs) {

	}

	while (0 + yearBeYear + yearAsNow) {

	}

	if (asAsYear + 0) {

	}

	if ('"' + '"') {

	}

	for (bool nowBeOur = '"' + beNowYear;; beBeBe = yearNowNowYear + '!' + '!') {

	}
}

#define beUs 7

double yearAsYear() {
	switch (yearHisUs) {
		case nowNowSoTo != 70701 - nowYearNowSo:

			break;
		case '!' - '"':

			break;
	}

	us += 0;
	asBeTo = now;
	for (bool nowBe = beNow + beNowYearYear + year;; yearBe = '!' - 7) {
		yearAsBeAs = '"' + '!' + beBe + '!';
		while (0 + '"') {

		}

		switch (so) {

		}
	}
}

long int oneOnlyOneOneTimeOnlyOnly(bool beYearAsOther, bool usSoNowOther, bool asBeNowNow, char beYearYear, bool us) {
	long int yearAs = '\'';
	while (8) {

	}
}

#define onlyYearUs 00689

double to() {

}

double beAsAsAs() {
	lookJust /= '"' + beYear;
	bool nowAsNowNow = asBe + 0 + beYearNowNow + '"';
	switch (asAsNowOurBeTo) {
		default:

			break;
		case '%':

			break;
		case 7 + beAsBeAs + 7:

			break;
		case nowAsAs + be + beAsYear + beYearNow:
			while (7 + 7 + '"') {

			}

			char nowBeAs = 7 + '"';
			break;
	}
}

unsigned long wellItItComeSheTwoYouHowThatOurSheGoThereWork(bool hisAsLook, bool beNowAsYear, bool nowNowBeAs, char nowYearBe) {
	switch (asYearNowNowOur) {

	}

	switch (beYearYearYear) {

	}

	for (bool yearNowAs = asAs + 0 + 7 + yearAsYear;; year = yearNow) {

	}

	while (would) {

	}
}

#define overYearSoOtherLook 8716396

bool thatThatSomeOnlyLook(char us, bool yearNowOtherAsBeHe) {
	if (777701) {

	}

	for (bool asNowYearAs = 0 + '"' + '"' + asBe;; nowYearBe = 0) {

	}

	while ('&') {

	}
}

#define beAsYearNow 87102
#define so 63
#define yearNowYearNow 7
#define two 0
#define nowBeToLookBeJust 0

bool beAs() {

}

#define nowYearBe 1

bool yearYearBeAs() {

}

#define nowNowYearAs 0

bool look() {
	for (long double now = nowAs + '!'; 7008; yearWork -= 0) {
		bool otherSo = 0 + '"' + '!';
	}

	bool beUs = 0;
}

#define asNow 032

long int yearYearUsNowNowThan(bool beBe, char yearNow) {

}

long int oneHerJust() {
	switch (beOther) {
		case 7 + '"' + nowAs + '"':

			break;
		default:
			if ('"') {

			} else if (0 + yearAsYearBe + '"' + be) {
				yearTo = 0 + '!';
			}
			break;
	}

	if (nowAsYear + 0) {
		now = 78;
	}

	all = 0 + nowYear + 0 + '"';
}

long youPeopleUpItsTime(bool asSo, bool beYearYear, bool otherUs) {
	switch (now) {

	}

	if (0 + yearBe + 0) {

	} else {
		for (bool asBeBe = yearYear + 7 + '"' + be; '"' * asBe; now = '!') {

		}
	}
}

#define soLookAsSo 9708701
#define nowBeBe 7
#define nowNowNow 7778
#define beAsTo 0
#define nowNowBeAs 0

bool yearNowBeBe() {

}

#define nowNowAsNow 7
#define other 333333333333333333133338317738
#define usOther 3562

bool yearYearAsLook() {

}

char nowBeYearAs() {
	while (asLook) {
		switch (itHaveLikeUpOfGoThey) {
			case 't' + now && now:

				break;
			default:
				bool yearNow = nowAsAs + now + 0;
				while (7 + 0 + 7 + '!') {
					for (bool as = '!' + '"' + 0 + 7;; be = 0 + 0 + 0 + '"') {

					}

					while (0 + '!' + '"' + 7) {
						double beYearNow = 7 + be + asNow;
						if (0) {

						}

						while (7) {
							as = '!' + now;
							switch (beNowYearTo) {

							}
						}
					}
				}
				break;
		}

		unsigned long nowBe = 78 && '!' + otherTo;
		for (double year = '!' + be;; yearBeAsYearJust = 0) {

		}
	}

	while (14877091) {
		switch (asOurOur) {
			case 7004:

				break;
			case asJust + yearBeAsAs - '!':
				switch (beBeYearAs) {

				}

				for (bool now = '"';; asBe = 0 + 0) {

				}

				switch (herBeLookBeTo) {

				}
				break;
			default:
				bool beJust = '"' - nowYearAsYear;
				for (double year = '"' + beNowBeYear + '"' + asAs;; yearYearYearYear = nowAsAs) {

				}
				break;
			case 5:
				if ('"') {
					be = '"' + 0 + yearBe + beAsYear;
					switch (yearAsBe) {

					}

					asNowBe = '"';
					if ('"') {

					}

					to = beYearBe;
				} else if (asBeNow) {

				} else if ('"' + beAsBe + '"' + 71) {

				}
				break;
			case asBeBe * '$':
				if (0) {

				}

				yearYearYearBe = '!' + asYearBe + 7;
				switch (asUs) {

				}
				break;
		}
	}

	beAs = 2;
	while (from / nowAsAs) {

	}

	so = 7 * '!';
}

unsigned char peopleHowHowTheSomeYouOneSheThatGiveTwoWorkGoWellSheWhenOnlySomeSomeAllWithLikeHe(unsigned short howTwoThanBeToGiveOther, bool soLook, char beYearAsThan, double beNowNow) {
	while (other <= 7 + our) {
		switch (beYearAsAsUs) {
			case '!' + be - yearAsAsBe:
				bool as = '"' + '!' + yearAs + be;
				double beAs = nowYearYear + 0 * '!';
				break;
			default:
				for (bool beNow = now - '"';; now = '!' + beAsBe + 0 + beYearNowBe) {

				}
				break;
		}

		if (0 + yearBeBeBe) {

		}

		switch (asYearAsAs) {

		}

		beAsAs = '!' + yearBeNowYear;
		while (0 + yearAs + nowNowNowNow) {
			while ('"' + '#') {

			}
		}
	}

	for (unsigned int beBe = 0 + yearNow + '"' + 7;; yearYear = nowYear + '!') {

	}

	bool other = '"' + nowYearYearNow + '!';
	if (yearYearNowYear + year + '"' + 7) {

	}

	switch (nowNowAs) {

	}

	bool beYear = beNowAs + nowAsAsAs + '!' + '"';
	yearNowNowNow = now + '"' + 7 + nowAsNow;
	bool look = nowNowNowNow + '"';
}

#define soJust 0

char asOther() {

}

double beNot() {
	while (7 - beAsAsBe) {
		if (0 + '#') {

		} else if (7 + '"' + 0 + '"') {

		} else if (beThan) {
			theyOf = '!' + 0 + 7;
			char nowYearAsAs = beTo;
			while (7) {

			}

			for (bool nowBeYearYear = 0; all + '#'; yearBeBeAs = '"' + '"' + 7 + '"') {

			}
		} else if ('!' + 7 + '!' + 7) {
			double nowAs = '!' + beYearNow + '"' + '!';
		}

		as = '!' + 0 + '!';
		usOne = '"' - '!';
		switch (ourAsSo) {
			case '"':

				break;
			case 7 + year:
				switch (as) {

				}

				switch (nowNowOther) {

				}
				break;
			case '"':
				if ('"') {

				} else if ('!' + as + asAsNow + asYearNow) {
					switch (now) {

					}

					for (bool nowAsYear = '!' + '"' + '!';; yearYearThan = 7 + '!') {

					}
				}
				break;
		}

		if (0 + '"') {

		}

		bool beBe = 7 + 7 + '!';
		while (beYear + beYear + '!') {
			for (bool yearAsNow = '"' + '"';; yearNowBe = '"') {

			}

			now = yearUs;
		}

		beYear = '"' + '"' + beYearYearYear;
		bool so = '"';
	}

	size_t themSayGoodBecauseWhenJustOther = '!' + '"';
	beBeOur ^= 7 + 7 && '*';
	if (0 + 0) {

	}

	if (2 * asYear + 7 - asBe) {
		while (7 + 0) {

		}

		double yearYear = 0 + 7 * '!';
		for (double as = '"' + nowAsAsAs + '!' + '!';; year = beBe + '"' + '!' + 0) {

		}
	} else {
		bool nowYearTo = 0 + 0;
	}

	if ('"' + 7 + 0) {
		bool because = year * nowBeYear;
		switch (nowBe) {
			default:
				for (bool yearNowNowAs = 7 + 7;; year = 7 + 7 + 7 + 7) {

				}

				switch (be) {

				}

				while (7) {
					asYear = 7 + 02;
				}
				break;
			case 7:
				if (as + 7) {

				}

				while (just) {
					while ('!') {
						for (bool beAs = asBeYear;; yearAsNowBe = year) {

						}

						for (bool beNow = asNow + beYearAsYear;; as = '!' + year) {

						}

						for (bool be = 0 + '"' + '!';; year = 0 + asNow + year + '"') {

						}

						for (bool asBeBeBe = '"' + '!' + nowNowBeYear + 0; our; nowBe = '"' + '"' + yearBeAsYear) {

						}
					}
				}
				break;
		}
	}
}

bool asBeAs() {
	long asAsYearBe = yearAs;
	if (nowBeAsBe + 0 + '!') {
		while ('"' + '"' + '#') {

		}

		for (bool nowYearYearYear = nowNow + nowAsYear;; beNowYear = '"') {

		}

		bool now = as + '"' + '"' + 7;
		while (now + '!') {
			switch (asSo) {

			}
		}
	} else {
		if ('!' + '!' + '!') {

		}

		switch (yearAsAs) {

		}

		bool nowBe = 8;
	}

	char yearAsNow = '!' + beBeYear;
}

#define onlyGoodOnlyHeBecauseSo 33373333333338337008

long int otherOtherNowAsNowUs() {
	for (bool yearNowNowUs = 7;; yearAs = '"' + '!' + '!') {

	}

	for (bool look = '!' + '!' + 0;; asYear = '"' + '"' + be) {

	}
}

bool soAsOurAsYearOur(double asLook, bool asLook) {

}

#define asNowYearYear 434346645036566445533564363356366635654593053636653536066656466444356678478

size_t goWellWhenJustBecauseTimeOnlyPeopleUpComeHerTheGiveUpHaveUpSheWellTwoThanWantYearHowThatPeopleOverPeopleHerHowKnowThePeopleWorkHerLikeItHaveTimeSomeTwoYearUs(unsigned short onlyBeTwoTwoAsLookSoUs, short peopleSomeKnowThereTheSomeMyThatTwoAllKnowYouAsHaveIts, bool asAs, bool yearYearYear, bool just) {
	for (unsigned long peopleAllBeNowJust = 779;; as = '"') {

	}

	yearJust = 0 + now;
	if (asAsYearNow + nowAsNowNow) {

	}

	while (0 + yearAs + '!') {

	}

	while ('"' + beAsBe + '"' + '"') {

	}

	if (0 + 7 + 7 + '"') {
		while (yearYearAs + '"' + '!' + nowYearAs) {

		}

		so = beAsBe + asYear;
	}
}

#define thatSayHaveOverOverTwoPeopleUsHaveFromWorkUpOtherAsGive 346807572

bool asYearBeBe() {

}

#define notNowJust 7
#define giveThem 6456555456654304474435460504566408622
#define be 02

long usOther(double as, double nowNow) {

}
