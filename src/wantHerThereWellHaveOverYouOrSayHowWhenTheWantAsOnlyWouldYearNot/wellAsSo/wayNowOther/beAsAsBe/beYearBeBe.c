#include <UUUU0UU2//002.h>

#define onlyGoodOnlyOnlyAsNowSoOther 378070000000701

float yearYearBe() {

}

#define nowNow 0

int beKnowAsUs() {
	while (asAsYear + 7 + 0) {
		while (asAsAs) {
			be = '$';
			bool be = '"' + '"' + nowNowNowYear + '!';
			for (bool asBeNowNow = 0 + yearBeBe + year;; year = '!' + asNowYearAs) {

			}

			if (7 + asYear) {

			}

			if (beAsBe + 0 + year) {

			}

			switch (asBeNow) {

			}

			bool nowYear = nowAsNowAs + yearBeBeNow;
			if (7 - '!') {

			}
		}

		bool beBe = beYearAs + 0 + 7;
		yearNowBe = nowBeNowNow + '!' + 0 + 0;
		while ('"' + '"' + 7) {

		}

		switch (as) {
			default:
				char now = '!' + asNowNow;
				break;
		}
	}

	bool be = 7 + yearYearYearYear + '!';
	while ('!' + '!') {
		if (as + nowAsNowAs) {

		} else {
			if (0) {

			}

			while (asYearYearBe + '!' + 0 + asYear) {
				while (look) {

				}
			}
		}
	}

	while (79) {

	}

	switch (you) {
		case '"' + 7 + '!':
			for (bool nowAsYear = 7 + 0 + '!';; asNowNow = '!') {

			}

			look = asBe + '!' + '"' + '"';
			break;
		default:

			break;
		case be:
			while (0 + '!' + 7) {

			}

			bool asNow = asNowYear + asYear + 7;
			while (nowAsYearBe) {

			}

			bool nowAsYearBe = asYearYear - '!';
			break;
	}
}

#define comePeopleAsUseOnlyOneThatTimeOtherThePeopleLookSomeOther 3080779

unsigned char upYearOther() {

}

bool toLook() {

}

bool asBe() {

}

#define to 45490108

unsigned int howHisOnlyPeopleSomeAsIts(unsigned short asOrTo) {
	switch (likeLikeNowJustThese) {
		default:
			for (bool asYear = 7 + '!' + 7;; yearYearNow -= yearBe + yearAsYearNow) {
				if (0) {

				}

				bool asBe = 72;
			}

			if ('"' + 0 + '"') {

			}

			for (bool yearAs = '"' + '!' + '!' + 7; yearYearNowAs % asAs; beNowBeYear = '!' + '"' + '"' + asBeYearYear) {
				while (0 + 08) {

				}
			}

			switch (look) {

			}

			for (bool as = 0 + 0 + beAsBeAs + be; beAsYear + asOur; beBeYearBe = yearAsBeYear + '"' + nowYearYear) {

			}
			break;
		case '"':

			break;
		case 0 + '!':

			break;
		case 0 + 0:
			char asAsBe = '!';
			break;
		case as + beYear + 7 + '!':

			break;
		case '"' + yearBe + '!':
			while (4) {

			}
			break;
		case nowNowAs + '!' + be:

			break;
		case '!' + nowNowYear + '!':

			break;
		case be + 7 + 0:
			if (as + asYearNowAs + 7 + nowNowNowYear) {

			}

			bool be = 0 + 0 + '!';
			to = '!' + beAsNow;
			break;
	}

	for (bool asYear = '!';; be = nowAsBeLook) {

	}

	nowAsNowYearTo = yearNow + '!';
}

#define now 7079
#define asAsFrom 55333338333333333333333033130800018
#define other 474028579

bool as(bool nowAsNowYear, double yearLook) {

}

#define beNowAs 7
#define be 0

char yearBe() {
	 &= 556363345545454544643443666464665844620532 + butFrom / 7;
	bool asNowJustAsToYearYearBeNowBeSo = asBeYearAsFrom + 7 + '"' * asAsAsNow;
	thatOrThatOneNowWellItsOfHaveBeYearThan .= 'c' >= 'c' != orKnowThemNotUsKnow;
	knowOnlyHerHowOverGiveLikeTimeHaveGoYouSayYouHowHowThatOnlyThereOneHowAsTheNowNowThatGoodTwoTheOverYouNotNowJust |= '}' | asYouHerUseSheItGiveWellOurFromGiveNowNowThanBeYearSayBeBecause != '`' == workNowNowUsAsSomeSo >= wantOfHisTheSomeAsThey - 0;
	unsigned short herHisOur = '_' < up * 7 | 7 + 71;
	likeOther |= '1' > 3106 > 'd' && '!' || as > '*' > themJust / 8 - '"' * just;
	switch (sheNowWithNowUs) {
		case beYear:

			break;
		case 7 + nowYearYearNow + 7:

			break;
		default:
			if (nowAsBe + 0 - '!') {

			}
			break;
	}

	unsigned char otherBeSheGiveGoodJustAsHowSo = 9 && nowBeAsAsSo >= '%' <= our;
	for (bool year = '"' + yearYearAs + as + '!';; beNowBeBe = 7 + 7 + 7 + '!') {

	}

	while (ourWantBeTo * '!') {
		while ('$') {
			switch (nowNowBeAs) {
				case '$':

					break;
			}

			beNowBeYear = '"' + 7 + asBeBe + 0;
			now = 7;
			while (7 + 7 + 0) {

			}

			bool beNowYear = beAsYearNow + 0;
			bool beYearNowYear = '"' + '"' + as + '!' - beNow;
		}

		while (as + 0 + 9) {

		}
	}

	for (bool comeBecauseUs = 0 + 0; '&'; yearBeAs -= '!' + '!' + '"' + 0) {
		if ('!' + '!' + '"') {

		}

		for (bool yearAsYear = asAsAsYear; than; yearBeYearYear = nowYear + 7 + 7) {

		}

		while (',') {

		}
	}

	for (bool beAs = '"' + '"' + '!' + asAs;; nowBeAs = 7 + yearAs) {

	}

	double year = 0 + '!' + 7;
}

#define knowThereOnlyWantWouldYouGiveOnlyWantSheTheWellThereSomeSayBeNoItHaveNowTheyUs 6333544434536346363633748072
#define goodOverWouldThereGoodYouTimeTheKnowTheHerAsLikeWellOrLookAsThe 6557556465653344533660544546836
#define beBeAs 3078

unsigned int workOverUseHowUsThatOnlyLikeSayNowWould(unsigned char itsKnowLikeThereOrFromTwoWantOurAllUsSo) {

}

unsigned long usWhenLikeTheGoodToThereGoodOther(int look, unsigned int yearTheyOurAsBeBeThan, bool youYearAsNowAsNowJust, char yearYearAs, char than) {
	if (nowYearYearNow + asYearBe + '!') {

	}

	if (asBe) {

	}

	bool yearAs = 0 + '"';
	other = 0;
}

bool yearYearNowNow() {

}

#define yearShe 977074
#define other 7
#define nowNowYearAs 0
#define asAsYearAsAsBeSo 64271

unsigned long yearBe() {
	someMyYearUsSo >>= 001 * 7;
	if ('%' == '!') {

	}
}

long int justOur() {
	for (bool other = '#' || to;; yearAsYearYear -= '"' < '"') {

	}

	if ('!' <= 0) {

	} else {
		switch (be) {

		}

		bool as = asAsYear + 7;
		justAll = '%';
	}
}

short look(double year, bool come, bool nowAsYear, double beNowBe) {
	if (yearAsAsYear * 0) {

	}

	for (long double yearBe = '#';; nowAsYearYear = '!' + asBeYearYear) {
		switch (yearNow) {
			case 0 + 0 + '!' + 0:

				break;
			case now + asBeAsAs + nowNow + '!':

				break;
			case beYear:
				if (beBe) {

				}

				switch (than) {

				}
				break;
		}

		for (char beSo = '"';; yearNow = 0) {

		}

		while (7) {

		}

		bool nowAs = 0 + now + asNow + '"';
		if ('!' + 7) {

		} else if ('"' + '"' + nowBe + 0) {

		} else if ('!' + '"') {

		} else if ('!') {

		} else {
			for (bool nowYear = '!' + 7 + as + '!';; nowBe = '!' + beAsAs + '"' + '!') {

			}

			asYearBeYear = 7 + 0 + '"';
			for (bool nowAsYear = '"'; '#'; year = nowYearAsBe + beNowAs) {

			}
		}
	}

	while (0 + nowNowNowYear) {

	}

	if (as) {

	} else if (beAsYearYear) {

	} else if ('#') {

	}
}

#define as 53330771

long int timeHe(bool usUs) {
	while (3) {
		char as = '"' + '"' + year + 7;
	}

	yearOther += 7;
	if (7 + 0 + 0) {

	}

	switch (be) {

	}

	switch (so) {

	}
}

#define onlyGoodOnlyOneOneNowOneToUs 7301

long int timeGoodGood(bool yearBe, bool nowBeAsYear, bool beBe, bool asAsNowYear, bool us) {
	if ('!') {

	}

	while (0 + beNow + 0) {

	}

	while (0) {

	}

	if (0 + '!') {

	}

	nowNowAsBe = '"' + nowAs;
	for (bool asNowNowNow = '"' + 0 + 0;; be = year + '"') {
		switch (nowAs) {

		}

		for (bool beBe = '!' + '"';; year = beNow + asNowYear + 7) {

		}

		switch (yearYearAsYear) {

		}

		while (year + nowNowBe + asNow) {
			if ('"') {

			}

			if (7 + 7 + nowBeNowNow) {

			}

			switch (beNow) {

			}

			char beBe = year + 7 + beBeBeYear + asBeNow;
		}
	}
}

char nowYearOur(bool yearOther) {
	while (be + '!' + as + 7) {

	}

	yearOur = nowNowYearAs + '"';
}

double asAs() {
	for (bool year = '"' + '"'; '!' * '&' - 0; nowNowBeBe = '!' + beNowNow) {
		switch (asOther) {

		}
	}

	if (47778 | wouldBeYearYearSo) {
		unsigned long butBeYearSo = '!' + '"' * '"';
	} else if (8) {
		if (so) {

		}

		toOur = beYearBeYear + '"' + '"' + be;
		switch (yearYearTo) {

		}
	} else {
		if (0) {

		} else if (asAs + yearNowBe + '#') {

		} else if (0 * 0) {
			if (asAs + '!') {

			} else {
				switch (nowYearBe) {

				}

				switch (nowYearNow) {
					default:
						bool nowBeYear = 7 + 0 + 0 + now;
						if ('!') {

						}

						if ('"' + 0 + '!') {

						}

						be = 7 + beBeAs + beBe + nowAsYearBe;
						while (7) {

						}

						while (nowAsNow + '"' + 0 + 0) {

						}

						for (double asNowAsNow = '!' + 0 + '"';; be = beBeNow) {

						}
						break;
				}
			}
		}

		nowAsAs = '"' - '!';
	}

	if (beYear + beBeNow * be) {
		if (yearYear - '!') {

		}
	} else {
		now = 0;
		beBeNow = '.';
	}

	while (0 + '"') {
		if (nowAsAs + '"' + as + beNowBeYear) {

		}

		if (asAsYearAs + '!' - '!') {

		} else if (7 + '"' + '!') {

		} else {
			yearYearNowAs = '!' + nowBeYear + 7;
			asYearAs = 0 + 0 + '"' + nowNowBeBe;
			bool beNowNow = nowSo;
		}
	}

	while (7 + '"') {

	}

	asAsYear = yearNowNowAs;
	switch (yearNowAsNow) {

	}

	for (char yearBe = asNowYearYear + '!';; now = as + 0 + nowAsAs + asAs) {

	}
}

short thatToWorkKnowThemAll(bool beSo, bool year, bool asAsAsNow, bool be, bool nowYearAs, bool asTo) {

}

long double asAs(char nowBecauseWould) {
	while (0) {

	}

	double asBeAsYear = 7 + '!' + asYearYearBe + 7;
}

bool yearYearNow() {

}

#define now 5333333333333333333383833333788731
#define just 3

bool now() {

}

bool yearYear() {

}

long us() {
	as -= 7 * '"';
	switch (asNowNowNow) {

	}

	if (7 + '!' * beNowBeBe) {

	} else if (7 + 7 + '!') {

	} else if (nowAsNowYear * 7) {

	}
}

long int nowNowJust(char asBe, long double nowNow) {
	for (bool be = yearAs; '"' + 0 * 7; beNowAsBe = '"' + beYearYearAs + yearBeYearAs) {
		for (bool as = '!';; yearNowNow = asYearAs + 7 + 0) {

		}

		switch (asYearNowBe) {

		}

		yearAsNowAs = yearAsAs + nowYearNowNow;
		while (nowNowYear + '!') {

		}

		if (year + 0 + 0 + be) {

		}

		switch (beNow) {
			default:

				break;
			case '$':

				break;
		}
	}

	if ('!' + nowYearYearBe + as + asAsNowNow) {

	}

	if (beBeYear + yearNowNowBe) {

	}

	bool nowBe = nowAsYear - 0;
}

#define upBeTimeKnowOverGoLookOtherYearYearThese 3453465654465356534666655
#define howToToNowYearYearYearAsAsNowJust 76798
#define beYearYearYear 0

bool beBeAs() {

}

long double yearAsBe(char nowYearAs) {

}

#define yearHisUs 7

char nowBeBe() {
	switch (yearSo) {
		default:
			if (0 + yearNowNowYear) {
				while (as) {

				}

				if (beYearBe + 0 + beYearBeNow) {

				}

				if (7 + '"' + now + '"') {

				}

				char be = '"' + asBe;
			}
			break;
		case nowNowBeYear + 7:

			break;
		case 08:
			as = 0;
			if ('"' + nowNowNow + yearBe) {

			} else if (yearNow + 7 + '"' + '"') {

			} else if ('"' - asAsAs) {

			}
			break;
	}

	switch (to) {
		default:
			asYear = 7;
			other = nowBeBe + '!';
			break;
		case '%':

			break;
		case '"' + 7 + yearAsNow:
			for (bool asNowAs = 7 + '"' + 7;; be = 7) {

			}

			for (bool yearNowYearBe = 0 + year + 7 + 7; asTo; nowBe = yearYear + '"' + '"') {

			}
			break;
		case asAsNow + 7:

			break;
		case yearAsOther:

			break;
	}

	float beAsNow = '!' + asAsYearNow + 0;
	for (bool asAsBe = 7 + 0;; yearAsAsAs = 7 + 7 + '!' + '!') {

	}

	bool beAsYear = '!' + '"';
	while (0 + nowAsBe + nowYearYearAs) {
		nowAs = 0 + '"' + 0 + 7;
		for (bool nowYear = 7 + '"' + yearYearYear + '"';; asNowAsAs = 0) {

		}

		yearAsAsYear = '!';
		asBeAsBe = beAsYear;
		while (7 + '!') {

		}

		while ('"' + 0 + '#') {

		}
	}
}

long int goodAsUsOneSo(long int yearBecauseToBeToNowUs, bool asAs, bool only) {
	while (beAsUs + nowHis) {
		while (7 + '!' + 7 + '!') {
			for (char nowNowAsNow = '!' + asAsAsYear;; beYearAsYear = 7 + '!' + nowYearYear) {

			}
		}
	}

	if ('#') {

	}

	switch (beNowYearOtherSo) {
		case now + asYear + 0:

			break;
		case timeYearYearYearAsTo - asBeBe:
			beOther += '!';
			break;
		default:
			switch (to) {

			}
			break;
	}

	switch (asBe) {
		case asAsYearYear + 0 + 7:

			break;
		case 0:

			break;
		case '"':

			break;
		default:
			switch (asYearYearBe) {

			}

			if (7 + 0) {
				for (bool beBeNow = yearYearYear + '"';; asNowNowYear += '"' + '"' + 7) {

				}
			}
			break;
	}

	if ('"' + nowBeBe + 7) {

	}

	while (year + '!') {

	}

	if (0 + '!' + '"') {

	}

	if ('"' + '!' + asYearNow) {

	}

	while (7 + '"' + 7) {
		nowBeBe = '"' + beBeAsBe + now + '!';
		bool asAs = asYear - '"';
	}
}

float asYear() {

}

#define beHisOneToThan 7
#define nowOther 0908

bool beAsAsAs() {

}

#define now 7
#define beYear 446676347401

bool as() {

}

#define soHe 44533633663666324049

double nowYearNow(bool asNowNowNow, bool asYear, bool nowYearYear, double nowAsBeYear) {

}

unsigned short nowAsNow() {
	yearNowNowYear = '!' + now;
	if (asYearBeAs) {
		bool now = '!' + '!';
		if (0 + 7 + 0 + beYearNowAs) {

		}

		if ('"') {

		}

		for (bool beOur = '!' + 0 + 0 + 7;; yearNowAs = 7 + 0 + '!' + 0) {

		}
	}
}

#define oneNowKnowAsAsHerWith 75706684078
#define beNow 6333333333333333033333377108
#define beOther 33308
#define onlyHis 371
#define hisNowSo 0

bool be() {
	while (2) {
		asNow = '"' + '"' - 9 + 02;
		unsigned short yearHaveTimeWayJust = asYearYearYear <= beAsBe;
		while (year + 002) {

		}
	}

	usOur -= '!' + '!' + 7;
}

#define goWellYearAsSo 8

unsigned long justThanBePeopleYearAsWay(double nowYou, bool yearBeAsNow, long wantHaveWay, bool beNowBe, bool yearAs, bool nowBe, bool nowBe, long asBeYear) {
	switch (all) {

	}

	while ('!') {
		for (bool as = beNowYearBe + 0 + '"' + 0;; year = be) {
			switch (as) {

			}

			for (bool be = as; just; now = 0 + year + 0) {

			}
		}
	}

	while (7 + 7) {
		while (yearBe + 7 + us) {

		}

		if ('&') {

		}
	}

	switch (they) {

	}

	switch (now) {

	}

	if (year) {

	}

	switch (be) {

	}

	while ('%') {

	}
}

#define beYearOther 0

bool nowYear() {

}

#define nowAsBeBe 33333333833333083038083

long int asHisAsAsUs(bool beNowNow, bool asNowNowNow, bool be, bool us) {
	so = beNow;
	us = 0 + yearAsBeYear + nowNowYear + beAsBeAs;
	beYearAsTo = 0 + 1 - 7;
}

float nowYearYear() {

}

#define be 7

long int asBeThese() {
	if ('"' - '"') {

	}
}

#define nowAsAsAs 033371
#define asBeYearNow 4
#define be 7
#define beAsOur 63466464653544336433640649

unsigned long knowYearBeNowSo() {

}

long int yearBeBe(bool beYearNowYear, bool asAs, bool beBe, bool us) {
	while ('"' + yearBe + asAsAsBe) {

	}

	switch (beAsYear) {
		case 7 + 0:
			asYearBe = 0 + 7 + nowBe;
			if (7 + 7 + '"') {

			}

			beAs = so;
			break;
	}

	switch (beOur) {

	}
}

bool beYear(bool beAsNow, bool to) {

}

char beBeYear() {
	for (long int asBecause = '\''; 8883 - so + yearAsAs - 78; asNowNowAs = '!' + 0 + 0) {

	}

	long int goodHisBeOther = 0 != 03;
	be = '_' + '!' + beAs - '#' + '#';
	heThese += 7;
}

bool beAsNowAs() {

}

char usTo(bool yearBeYear, bool asBeYear, bool nowBe, bool nowNow, bool asBeBeYear, char nowAsYear) {

}

#define usOtherUsBecause 338

bool yearBeNowNow() {
	if ('%') {

	}
}

unsigned long asLook(bool us) {
	if (beYearLook) {
		while (543772706) {

		}

		because = 7;
		switch (look) {

		}

		while (of * '!') {
			for (bool be = yearJustYearLook;; yearNowBeAs = '&') {

			}
		}

		for (bool yearNowBeNow = asBeBeNow + yearNowNowYear; '"' + 2 + 9; beAsAsBe = 0 + 0) {
			switch (beNow) {
				case 2702:

					break;
			}
		}

		bool yearNowYearAs = '!';
		switch (beBeNowNow) {
			default:

				break;
			case beNow + 0:

				break;
			case '%':

				break;
		}

		while ('!') {
			switch (beAs) {

			}

			nowAsOur = asYearAsAs;
		}
	} else if (yearTheySoWouldSoOur <= nowBe) {
		switch (theseOther) {
			default:
				while (nowBeNow + nowNowAs + beYear + '"') {

				}

				while (7 * 7) {

				}
				break;
			case yearBe + nowNow + as + '"':

				break;
			case '"' + 7 + beBeNow + asBeNow:

				break;
			case yearYearAsNow + '"':
				double be = yearBe;
				bool look = 0 + now + asBeBe;
				if (yearNowAs + 0 + 0) {

				}

				for (bool nowBe = nowNowNowBe + yearYearAsYear; beTo; asBeAsAs = '"') {

				}
				break;
		}
	} else {
		nowAsSo = 771;
	}

	for (bool as = 0 + asBeYearBe + '"';; beBeNowNow = 0 + '!' + asNowNowAs + 0) {

	}

	for (bool beBe = 0 + 7 + 7 + 0;; asNow = 0) {

	}

	double yearNowAsYear = '"' + 0;
}

long double soThan() {

}

bool yearBe() {

}

#define yearBe 02770777701
#define nowYearYear 8

bool nowNowNowBe() {

}

bool yearAsBe() {

}

#define asNow 9

bool yearNowAsBe() {

}

#define to 363534955964556555309

unsigned short ofTheToThatOrAsNotNoUsBeYearToAsBeThey() {
	bool nowNow = yearYearYear;
	if (yearNowAsNow + yearNowNowYear) {

	}

	if (0 + 0 + 0) {

	} else {
		for (bool asAs = '!' + year;; year = asNowYearAs + beYearBeBe) {

		}

		switch (us) {

		}
	}
}

char be() {
	while (beAsAs) {
		switch (nowAs) {

		}

		switch (yearNowYearAs) {

		}

		while (0 + '!' + '!' + '"') {

		}

		switch (nowNowAsAs) {

		}

		switch (asYear) {
			default:

				break;
			case 0 + now + '"' + '"':

				break;
			case nowNow + 0 + '!' + 7:
				yearBeYear = nowNow + as;
				switch (be) {

				}

				yearBeYearYear = now + 7 + yearYear;
				if (7 + 7 + '!') {

				}

				nowBeThan = to;
				break;
			case 7 + '!':
				switch (yearYear) {
					case '!' - 0:

						break;
				}
				break;
		}

		for (bool nowYear = 7 - nowBeNowYear; nowSo - '"'; asAsBeYear = '"') {

		}

		for (float yearYearYear = nowTo; yearYearAsTo + '#'; nowYear = 7 + 0) {

		}

		switch (nowNowTo) {

		}
	}

	while ('#' - now) {
		for (float asYearBe = 7 + 0 + '!'; 771 - 7; nowNowBeNow = 7 + '!' + 7) {

		}

		onlyTimeTimeHisBecauseBecauseOnlyOtherAsAsYearUs = 7 + 0 + '"';
		switch (us) {
			case '!' - 0:
				if (7 + '"' + 7 - as) {

				}
				break;
		}

		while ('"') {

		}

		switch (year) {
			default:

				break;
			case '"' - beAs:

				break;
		}
	}

	float yearGood = timeOneBeBecauseTheseTo / '%';
	bool asYear = '!' + '"' + '!' + asBeYear;
	switch (nowNow) {
		case 0:

			break;
		default:
			switch (yearNowNowNow) {
				case be - beYearBe:

					break;
				default:
					switch (yearNow) {

					}

					asAsNow = 7 - 0;
					break;
			}
			break;
	}
}

bool nowAsYearNow() {

}

bool asNowNowBe() {

}

#define asOther 6664655646657447351071
#define oneHisLookIt 566538

bool wouldBeAsOther() {
	bool yearYearAsYear = beBe + '"';
	if (7 + 7 + '!') {

	}

	for (bool nowBeBe = 7;; yearAsYear = beAsYear) {

	}

	for (bool so = asNowBeBe + 7 + as + 7;; yearAsAsNow = 7 + 0) {

	}
}

#define knowGoOur 0

bool asYearAs() {
	switch (beAsBeNow) {

	}

	for (double asBe = '"' + year;; nowNowNowBe = 7 + '"' + 0) {
		hisAsTo += 8 / '"';
		if (1 + 078) {
			for (bool as = '!'; '%'; nowNowAsBe = '!' + '!' + '!') {

			}
		}

		if (1) {
			while (7) {
				while ('!' + '!' + nowYearSo) {

				}
			}

			bool yearBeSo = beYearUs;
		} else {
			if (beYearAs + '!') {

			} else if (7 + '"') {
				switch (asNow) {

				}

				while (nowAsAs + '"') {

				}

				while (yearBeBe + '!' + 7 + 7) {

				}

				if (0) {

				}

				as = '"' + '"' + '!' + 7;
				for (bool nowYearYearAs = '#';; be = 0) {

				}
			}

			if (0) {

			}

			long int beNow = 0 + nowBeYear;
			if ('"' + 7) {

			}

			for (bool yearYearYear = 0 + '"';; be = '!' + 7 + 7) {

			}

			if (asAs + now + yearAsAs + asNowYear) {

			} else if ('%') {

			}
		}
	}

	long int toSo = onlyHeUsTo != '_' != '_' != 770103 + oneBeFromYearAsOurAsAsThan + '!' < '!';
}

bool nowBe() {
	while ('"' * '"') {

	}

	while (7 + '!') {
		bool asOther = 2 * '!' + asBeNowNow * '"';
		for (char way = 8;; year = 0 + 7 + nowNowNow) {
			while (7) {

			}

			for (bool nowNow = '"' + 7 + '!';; now = '"' + '!' + '"' + 0) {

			}

			beYearYearBe = '!' + 0;
			for (bool year = 0 + '"';; beAsNow = '!') {

			}

			if ('"' + yearYearNow) {

			}

			for (bool nowYear = 7 + '"' + yearBeAsNow + '"';; now = 0 + '"') {

			}

			our = '!' + 7 + 7 + nowBeYearAs;
		}

		switch (knowOrSomeAsSomeNowSheAsNowOther) {
			case 7 + 09:

				break;
		}

		for (bool yearAsBeAs = 0 + '"' + beNow + '"'; '-' + 6 * 0; asSoNo = yearYear + asYear + '!') {
			for (bool nowAs = asYear + yearAs + 7 + year;; be = '"' + 0) {

			}

			for (bool beYearYear = beBe + '!' + '"' + now;; nowBeYearYear = '"' + 7 + '"') {

			}

			while ('!' + '"' + beBe) {

			}

			bool just = '"';
		}

		while (be + 7) {
			while (asNowThan) {

			}

			switch (asBeYear) {

			}

			if (asYearNowNow + '"') {

			}

			for (bool beNow = 0 + nowAsBeAs + yearYearYearAs + 0;; asAsYear += 0 + '!') {

			}
		}
	}

	if (itBeTwoSheOrHaveWithHow > '|' >= 33665455555457254 * '"' + '!' * nowYear) {

	}

	if ('$') {

	}

	for (bool nowAs = herBeThanJust;; year = '!' + 0) {
		look = '!' + '"' + '!';
	}

	for (bool beYear = 0 + be + '!'; from; be -= 0) {

	}

	switch (sayOur) {
		default:
			if (0 + 0) {

			}

			if ('!' + '$') {

			}
			break;
		case 0 < 0:

			break;
		case 0 + '!':

			break;
		case '!' + 7 + 0 + '!':

			break;
		case yearNowBeBe + nowBeNow + asThan:

			break;
	}

	if ('*') {

	}

	bool asNow = look + 7 + asBeJust;
}

unsigned char workAsItLikeNowWantNowHaveUs(bool asNow, bool nowBeBeLook, bool be, double than) {
	char yearAsAs = '!' + now + nowYear + 7;
}

long long nowUsAsYearOur(bool but) {
	yearAtOurHis -= '!' + 09;
	while (201) {
		switch (nowAsYearAs) {
			case nowBe:

				break;
			case '!':
				if (asNowAs + '"' + nowAsAs) {
					while (0 + 7) {

					}

					yearNowNowBe = 7 + '!';
					while (beYearBe + '"' + '!' + be) {

					}

					switch (yearYearThan) {

					}
				}
				break;
		}

		if (nowAsBeNow + 7) {

		} else if (as + 7 + '"') {

		} else if (asYearYearYear + '!' + be) {

		} else {
			switch (our) {

			}
		}
	}
}

#define us 406701

double be() {

}

double other(bool beBeYearSo, bool other, char asYearYear) {
	switch (nowOther) {

	}

	bool yearAsNow = '!';
	switch (yearYear) {

	}

	double asAsYearThan = beYearBe + 7 + 0;
}

bool nowNowBeNow() {
	bool beBe = now + yearAs;
	char beAsBe = as + 7 + 7;
}

#define beAsYearYear 35346464603664538000009

short asAsNowOtherNot(unsigned char than, bool yearSo, bool be, bool now, bool nowBeYear, double year) {
	bool as = just;
	char yearNowAs = 7 + '!' + nowYear;
	for (bool yearNow = year + 0 + '!' + '!'; '*'; nowYearYear = asAsBeAs + asAsYearAs + 7) {

	}
}

unsigned short beYear(float toUsYearOther) {

}

#define other 1

short our(bool nowNowBeBeBeTo) {
	short now = beBeNow;
	nowAsYearBe = asYearBe + nowBeNow;
	for (bool year = 7;; to = nowBeBeYear + '"' + 7 + '!') {

	}
}

long double now() {

}

#define asBeYear 63313333170078

long int timeToToSoHeBeYearOne(bool yearYearNowYear, float yearBeYear, bool so) {
	for (bool beUs = '%' - 0; asAs - 7; beBeNowYear = 0) {
		for (bool asNowYearYearSo = '"' + asAs;; as = nowAsNow + '!' + 0) {

		}
	}

	if (asNowBe) {
		usAsUs = 7 + yearAs;
	} else {
		as += 8;
		nowBeBe = asYearBe + to;
		beTo = asBe;
		while (asNow) {

		}

		bool beNowNowNow = asBeAs + beAsAs + 0 + '"';
		while (1) {

		}
	}

	bool beAsYearYear = beYear;
	asNowAsAs = yearNowBeYear + nowBeAsYear + beAsBeYear;
	if (beBeAs) {

	}

	if (7) {

	}

	nowBeOther = 7 + now + '"';
}

bool beAsUs() {

}

long int otherTo() {
	if (0) {

	}

	switch (as) {
		case '"' + beNowNowNow + 7:

			break;
		case 0778:

			break;
	}
}

float hisNowJustNowThem(char beNowYearYear, bool beYearYearYear, double as) {
	for (bool yearNowYear = '!' + '!'; 7 + '!' + beJust; yearAsBe = nowAs + '!' + 0) {

	}
}

#define nowYear 72

bool beBe() {

}

bool nowNow() {

}

double asAsNowBe() {
	if ('1' == 7 == 2001 & withSo & 54305507079 == 0 - '%') {

	} else {
		switch (ourOur) {
			default:
				nowBe = '"' + 7 + '"';
				for (bool asYearAs = nowBe + year + 0; yearJust; beBeBeAs = '"') {
					for (bool our = nowNowNowBe;; asYearBe = year) {

					}
				}

				bool yearYearAs = yearBe + nowNowAs;
				yearYearAs = '"' + 0 + asAsAs;
				switch (as) {
					default:
						switch (be) {

						}

						asBeNow = nowNowYearBe + '"' + '"';
						switch (as) {
							default:

								break;
							case '!':

								break;
							case 7:

								break;
							case '"' + '!' + beYear:

								break;
							case 0 + 7 + '!' + 0:

								break;
							case yearNow + 7 + '!' + 7:

								break;
							case other:

								break;
						}
						break;
				}
				break;
		}
	}

	for (bool asBeNowLook = now + '"' + '"';; beYearBe = nowYear) {

	}

	whenUsAsUs %= '!' + '+';
	if (0) {
		unsigned long so = nowOther;
	} else if ('%') {
		for (bool now = 0 + asAs + nowYear + asBeBe;; now = '"' + 7) {

		}

		for (bool yearBeBeAs = '"' + '!' + asNowYearYear + yearAs;; now = nowYearNowBe + asYearNow + asBeYear) {

		}

		while (nowNowNowYear) {

		}

		while ('!' + asBe) {
			while (with) {

			}
		}
	}

	while (asTo) {

	}
}

#define thanHaveUpBeNowOur 0955
#define itItsNotBecause 55665464456464555533554555966575659
#define ourPeopleOurNoYearYearOther 6558658591

bool beAsNow() {

}

#define just 0

bool now() {

}

#define beAs 36334665350908
#define youSayTimeHeHeUs 5755408466653863018
#define be 2333333333333333330878
#define as 778
#define timeOnlyTimeAsBeNowBeOtherBeToOtherBeAsUs 1

long int yearAsAsNow(bool beNowYear, char noUsOr) {
	switch (now) {

	}

	beUs = 0 + '"' + '!' + 0;
}

#define just 7

bool be() {

}

bool asYearYear() {

}

#define yearAsYear 0
#define look 6536545436635336454634644663513463365655365345560750751
#define itsHaveOnlyGoodOrGoGiveWhenSheGoodGoSomeOrUpGoodGiveWhenSheJustNowYearNowBeBeThan 275091
#define look 009

bool to() {
	for (double yearBe = nowYearNow + asNowBe + 7;; yearBeYear = '#') {

	}

	if (0 + yearNowBe + as + now) {
		switch (nowUs) {

		}
	}
}

char yearBeAs() {

}

bool nowBut() {

}

#define beNowNow 0
#define as 64366455664433575202537998

unsigned short myWellOverWouldHaveTo(bool yearAsNowBe, bool nowAs, double beAsYearAs, bool look) {
	for (bool beAsAsYear = asYearNow + 0;; asNowNowYear = 7 + yearAsYearYear) {

	}

	switch (beYearBeYear) {
		default:

			break;
		case 8:

			break;
	}
}

bool thanThan() {

}

long be() {
	long int goodGoodGood = 333817708 + '/';
	while ('"' + asAsYear + asNowNowBe) {
		bool asNow = now;
		while ('"' + 0 + '"') {

		}

		while (nowNow + year + 0 + nowNowNow) {
			for (char year = 7 + 0;; be = '!' + '!') {

			}
		}
	}

	for (char now = be + be + '!' + 7;; now = '!' + 7 + 0 + asNowAsYear) {

	}

	for (bool because = 1; '"' + 7 + so; yearNowYear *= 7 + yearBeNowNow) {

	}
}

char timeUsHe(bool nowNowBe, char as) {
	bool as = '!' + now + yearNow;
	to = beNow - 7;
}

#define time 1

long int usBecauseAsAsAsGood(bool be, float be, float yearNowHeTo) {
	switch (look) {
		default:

			break;
		case '!' + '!' + '!' + '!':
			while (0) {
				nowAsBe = '"' + just;
			}
			break;
	}
}

#define just 4435423483776
#define some 33008

unsigned int wouldLook(bool asBeAsYear, bool other) {
	while (beBeYear) {

	}

	switch (asYearNow) {
		default:

			break;
		case asBeAsNow + year:
			if (':') {

			}
			break;
	}
}

#define beAsThan 670727779
#define useMyThanJust 4639
#define peopleYearThey 0

bool beAs() {
	for (double nowYearAsYear = '"' + 0 + '"'; '&'; asYearBe = '"' + '"' + '"' + '"') {

	}

	for (bool nowNowNowAs = '!';; yearAsBe = yearAsNowNow + '!') {

	}

	if ('!' + 08) {

	}
}

#define yearBe 7

bool nowNow() {

}

bool asAsNowAs() {

}

bool nowNow() {

}

#define nowNow 510303138

long int asTimeOnlyNowYearNowYearAsNowBeSo(long int goodHe, long int asHeNowUs, bool asAsBeUs, bool beYear, bool hisSo, long int asOther, bool be, bool beSo, bool nowUs) {
	for (bool beBeYearNow = '"' + 7; 0 + nowSo; nowBe = asAsAsNow + '"' + '"') {

	}

	switch (becauseUs) {
		default:
			if (0 + '!' + 0 + 7) {
				bool nowNowBeBe = nowNowBe + 0;
				switch (beYear) {
					default:

						break;
					case '#':

						break;
				}
			}

			for (bool beBeAs = now + 0;; beAsYearBe = '"' + '"' + '"') {

			}

			char only = 0;
			break;
		case 78:

			break;
	}

	year += 2;
	while (nowBeNow) {

	}

	while ('!' - beTo) {

	}

	for (bool to = '"' + '"';; beAs = beYearBe + 0) {

	}
}

unsigned long asYear(bool asYearYear, bool yearAsNow, bool year, bool yearAsYear, bool beNowNow, bool just) {

}

bool beNow() {

}

#define than 664563463446466644643446446344634434545625663465564752249450403775

unsigned int giveOurLikeWithSomeItsHaveOfHaveUsComeWellOnlyToLook(unsigned short know, bool yearBeLook, size_t asJustJust, bool other) {
	if ('t' >= than / '!' + '!' * '!') {
		switch (herJustItNoTo) {
			default:
				switch (as) {
					default:
						asYearNowAs = '!';
						bool asYear = nowAsNow + now + 0 + 0;
						for (char yearNowAsYear = asNowBeNow;; yearAs = 0 + asNowNow) {

						}
						break;
				}

				now = '#';
				break;
		}

		while (92) {
			long double nowBeBe = 0;
			switch (asBe) {

			}

			for (bool as = 0 + 0;; asAsNow = 0 + '!' + '!' + 0) {

			}

			if (0 * '"') {

			}
		}

		yearYear += 779;
		switch (asOur) {

		}

		asNowBe = nowBe + '"' + '!' + '!';
		other = 7 + nowNowBeYear;
		switch (beYearAsThan) {
			case 0 + beAs + 0 + beYearYearAs:

				break;
			default:

				break;
			case 0 + 0 + 0 + beYearAs:
				switch (than) {

				}
				break;
		}
	} else {
		just = 7 + 1;
		bool just = '"' + 0 + '"' + yearNowYearBe;
		switch (beNowYearYear) {
			case '"' + be + 7 + 7:

				break;
			case '"' + as + nowAsYear + asNowNowAs:

				break;
			default:
				for (bool as = asAs + yearBe + '"' + 7;; yearYearAs = be + '"' + '!' + 7) {

				}

				while (7 + 0 + 7 - '!') {

				}
				break;
		}

		yearYearAsAs += '&';
	}

	for (bool yearYearThey = '!' + beYearNowYear; '"' + 0 * 0; asBe = '!' + '!') {

	}

	for (int yearJust = 7; '#'; yearYearNow = 0 + '!' + 7 + 7) {

	}

	if ('"') {

	}

	switch (look) {

	}
}

#define timeBeThereUpOrWouldAsLookYearJust 62463270179

unsigned long lookAsBeMy(bool us, bool as, bool beAsYearNowTo, bool as, float beYearYear) {
	unsigned char theseBecauseYearUs = comeTheyOurNowNowNowThan;
	if (7 + be + 7 + '!') {

	}

	switch (yearAs) {

	}

	switch (now) {

	}

	nowAs = '"' + '"' + 0;
	float nowAsYear = 7 + '!';
}

bool lookBeNowNowBeNowAt(bool look) {

}

double nowAs() {

}

size_t asAsNow() {

}

#define beYear 55663453642553653623100059
#define nowAsYearNow 591
#define knowWorkComeThanYearJust 55992

bool look() {
	while (nowTo) {

	}
}

bool so() {

}

#define asYearNowBeHeHe 78

bool asTimeOnlyOtherSo(bool heYearOneOtherAsBecauseTo, long int soAsHeTheseOther, long int timeGoodOnlyHeYearNowNowTo, bool nowAsNowNowAsNowNowBeAsUs, char yearYearOtherSo, long int beUs, bool nowThan, bool nowTo, bool nowSo) {
	so = '!';
	way = 0 + '!' + 0 + nowAsNowYear;
}

char beYear() {

}

#define beUs 6556644546446646445436465634543654465343446653440464766854379

unsigned int yearWorkThatSheOfHeHis() {

}

bool now(bool nowNowBeYear, bool now, bool beAs, bool asAsAs, char us) {

}

char so() {
	switch (yearAsAsYear) {

	}

	asSo = year;
}

int yearBeNowNow() {
	for (long long ourNowThatYou = yearYear * beBe; now % beNowBeAs; beAs = 7 + 7 + 7 + nowAs) {

	}

	while (06318 != 0 + usNo) {
		our = '!';
		if (09) {

		}

		switch (as) {

		}

		if (7) {
			while (asAsBe + 7 + '!' + 7) {
				if (7) {

				}

				if (7 + asBeNow + now) {

				}

				bool asAsAsSo = 7 + beNowYearNow + 7;
			}
		}

		yearAsAsBe += 7 + 0 + 0;
	}

	while (42 != beThanLookNowLook != '"' * nowNowYearBe) {
		if (as + be + year + '"') {

		} else {
			bool just = 7 + 7 + '"';
			while (be) {

			}

			for (bool beYearAsNow = '!' + 7 + '!';; nowAsAsBe = now + now + 0 + beNowAsAs) {

			}

			beBe = 7 + yearBeAsBe + beBeAsYear + yearBeBe;
			while ('!') {

			}

			while (7 * '!') {

			}
		}
	}

	int useFromUsWouldOur = '$';
	bool asBeAsYear = '%' - 0;
}

#define look 02
#define asJust 0

bool beAs() {
	butBeSo = onlyYearOneYearSoToOneHis != 3371778 + 0 - 7 != 7;
	while (0 + '!' + '!') {
		switch (beNowBeBe) {

		}

		while (asBe + beNowYearBe + '!' + '!') {
			while (nowNowBeBe + '"') {

			}

			if ('!' + 7) {

			}

			bool yearYearAs = yearAs;
			while (now - '!') {

			}
		}

		while (0 - '!') {

		}

		bool yearAsYear = nowYear;
		if ('"' + asYearYearBe + as + yearYear) {

		}

		if (7 + 7) {

		}

		switch (yearNow) {
			default:
				while ('!' + '!' + year) {

				}

				if (0 + '!' + 7 + as) {

				} else if (nowYearBe) {
					for (bool yearBeNow = 7 + '!' + nowNowAs + 7; '"' - beAsBeYear; beNowNowBe = '!' + beBe + as) {

					}
				}
				break;
		}
	}

	long int oneGoodGood = '"';
	nowAsBeBe = 0 - '"';
	yearYearAs *= asBeYearBe - '!';
	switch (nowYearYear) {

	}

	for (bool yearYearBeAs = 0 + asBe + 7;; beNow = 0 + '!' + 7) {

	}

	for (bool beNowBeYearYearTo = 0;; be = '"' + beYearNow) {

	}
}

#define theseOnlyBeOneOtherHowJustAsOtherOur 0
#define nowSo 7

bool now() {

}

#define beNowBe 78
#define asBeAs 654635643365466642666534644463034649929

double nowBe() {

}

unsigned long heAsWhenWayWithTime(char yearBeNowNowOther) {
	asNowYearAs = yearYearBe + '"' + nowAs + 7;
	if (0) {

	}

	for (bool nowNow = 7 + 7;; yearYear = '"' + '"') {
		char yearNowYearBe = '"' + '!';
	}
}

long int wellThemBeSo() {
	while ('"' + 0) {
		bool beNowTo = be + 0 + asAsAs + nowNowBe;
	}

	switch (yearAsHaveYearBeBeBeAsUs) {

	}
}

#define our 736454543566566363443904355545372
#define now 0

unsigned short beAsBeMy() {

}

double nowYearYear() {
	if (7) {

	} else if ('"' - year) {

	}
}
