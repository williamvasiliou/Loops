#include <U/E.h>
#include <UUUU/UEUUUq//0.h>
#include <2//0/0.h>

#define beNowYearYear 33666344534333333333333333333333333333346465556436445646656465434354466553654666434444663364354533664333336255445964754970078
#define likeOneHowHowHerWantYearWorkOneSayOneWantThatOnlyThereOverKnowUpWhenOnlyHavePeopleOfGiveComeUpHerOfKnowGoSomeGoodHerUseComeUsePeopleOnlyOrGoBeGoodItsTheWould 6464335535645645354469565402701
#define  66664644533663346656635446566636646346654363366544645356453646634365436443545454345445654644546363534543363636555663655533434335464645364556535455336334453356554466664765444665366635660650492

unsigned char onlyGoMyUsTheyAsOther(unsigned short wellAsYearMy, bool nowAs, double now, char asYearBeBe) {
	while ('!') {
		for (long double yearYearNowYear = 31; '"' - 7; asNowNowBe = yearBeNow + 0 + 7 + 7) {

		}

		bool themUpSoOur = '%';
		switch (yearSo) {
			case year:
				switch (nowJust) {

				}

				while (beNowBeAs + '!' + '!' + 0) {

				}

				switch (other) {

				}
				break;
			case '$':

				break;
		}

		while (beAsYearNow + '"' + 0002) {

		}
	}

	if ('"' + '"' + '"' + asYearBe) {
		char way = asBeNow + 7;
	} else {
		switch (nowNowWay) {

		}
	}

	for (bool yearAsNow = be; 8 <= 0; beBe += '!' + '"' + 7 + '!') {
		for (bool beYearBe = '"' + asBeAs;; nowYearBe = yearAsAs + 0) {

		}

		switch (asBe) {
			default:

				break;
			case '&':

				break;
		}
	}

	switch (not) {

	}

	while (78) {

	}
}

#define sheYouNowHaveWhenTimeGiveGoOverWorkButWhenItsSheWellNowGoWithOfNowHer 0330209
#define yearHe 56336546567664446
#define ourThereSoOfGoAsGoodBeAsJustBeUse 0

bool now() {

}

#define nowYearNow 0
#define yearYearBe 7

bool yearYearAsOther() {
	if (nowYearOur) {
		if (65400702) {

		}

		bool be = '"';
		bool now = '"';
		while ('"' + '&') {

		}
	} else if (be + 7 + '!' + 0) {

	} else if ('!' * '%') {
		while (7 + '$') {

		}
	} else if (0 + asJust) {

	}

	yearNowNowNow = 7;
	usUs *= of;
	just /= '"' + yearAsBeYear;
}

long long as(long int thanAsGoUpSayOtherThemAsJustAsJust, double two, bool usYearNowThan) {

}

#define withNowButNowAsJust 577646

bool yearNow() {

}

bool nowNow() {

}

#define yearYearAs 7

bool nowNowBeYear() {

}

bool nowYearYearAs(bool nowAsThan) {
	long int onlyOneHeBeAsNowOther = nowAsSo + '#';
	long int so = to;
	for (char because = 01 != 3 + to; 3078 - 7; asYear = '!' + '!' + asNowNowYear) {
		while (7 + '"' + 0 + 7) {
			if (7 + asAs + 7) {

			}

			for (bool beNowAs = '#';; asNowNowAs = '!' + beAsYearYear + 7 + '!') {

			}
		}
	}

	switch (beBeBeBeYearSo) {

	}
}

long int nowYearAsAs() {
	float asNow = '"' + 0 - '"';
	while ('"' + '"' + nowYear) {
		if (0 + 0 + 7 + nowAsYear) {

		}

		bool beBeNow = year - 0;
	}

	for (bool now = 7;; yearBe = '"' + '!' + 7) {

	}

	asNow = asNowNow;
	if (yearYearAs + as) {

	}

	for (bool now = 0 + beBe; so - 0; yearYearYear = '"' + yearAs + 7 + '!') {

	}
}

#define timeGoodOneAllNowHis 9

bool otherHeYearSo() {

}

#define asAsBeBe 7

char yearNow() {

}

double asAsAsBe() {
	for (unsigned int goUpOverGoodComeGoodTheGoodLikeWorkGoWorkOverSheOurItHaveHaveKnowOrItsOfTheItsOnlyBeGoTheOrItYearHaveOrOnlySayThatHerGoodGoHowHowWhenWantWorkWantItsGoodWorkOrSheItOverSayWhenHowLikeTwoHerItGiveAsGoPeople = 4606645545543664443636644535454344543465536663649606 <= year != howJustAsSo & '-' <= 7 / '"';; wellJustOnlyTimeSomeOtherTheItJust &&= soShePeopleBut != theSheUseWellTwoOnlyThatSoOtherOtherThan && becauseOtherAsYearOur - '"') {
		if (0807701 | other + '&') {
			while (8) {

			}
		}

		unsigned char beButLookSo = 793 == ourUsBut;
		if (0) {
			while ('"' - '"') {

			}
		}

		if (nowAs + 0 + 7) {
			for (bool asOther = '!' + '"';; beAs = nowYearAsBe + '!' + as + '!') {

			}
		} else {
			if ('"') {
				if (7 + 0 + yearAsBe) {

				}

				double now = yearAsAs + now;
			}
		}

		char now = '!' + '"';
	}

	useSomeJustYearMy <<= beYear <= 'x' >= 'n' != '!' + 7370072 * asYear - '"';
	for (bool lookOur = asNowYear + 0 + 0; 0 - '!'; becauseAsTo = 7) {

	}

	wantSomeYouWorkGoodYearOther = '!' + '!' + 7;
	bool asAs = '"' + 7 + '"';
	while (beBeAsYear + 7) {

	}

	if ('&') {
		bool as = '!' + '"';
		if (year * 7) {

		}
	}

	switch (nowYearYearYear) {

	}

	if (yearYearOur) {

	}
}

long onlyWorkGoodHisAsNowWithNowOur(char nowBeYear, int otherBeThey, bool yearAsYearJust, bool yearBe, bool yearGoodSayThanToNowYearOur, bool nowYearYear, bool asNow, bool to) {
	if (0 > 7 <= '<' * 7) {

	}

	while (0 + beAsAs) {

	}

	while (year + year) {
		bool as = 7 + '"' + 7 + 7;
		yearBeAsSo = beBeNowAs + beNowYearYear + now;
	}
}

#define onlyUseGoHerGoTheUseItFromUseGiveWantOurAsSoYearBeOurNowOther 445628709
#define than 941
#define how 35

unsigned int go(bool asNowBeYear, bool yearTo) {
	bool soYearNowOur = nowAsBe;
	beBeLook = nowNowBe + asNowBeBe;
	if ('"' + 0 + 7 + '"') {

	}

	for (char year = 0 + '"' + '!';; asNowYearBe = 7 + now + '!') {

	}
}

#define just 72
#define now 02

bool nowBe() {

}

bool asAsYearBe() {
	bool other = '!' - now;
	short my = 72 && 7 - 0;
	switch (beYearNowNow) {

	}

	while (7 + '!' + '"') {

	}

	switch (yearAsYearAs) {
		default:

			break;
		case '!' + yearBeAs + '!':
			while ('"' + 0 + asNowBe + beAsYearNow) {

			}

			while (7) {
				switch (year) {

				}

				for (bool be = yearYearYear + '!';; yearYear = 7 + '"' + nowAsYear + as) {

				}

				while (0 + asBe + 7 + 7) {

				}

				for (bool yearAsYearYear = as + '!' + '"' + '!';; yearNowYearAs = asNowBe + yearNowBeBe) {

				}

				switch (beBeBeBe) {

				}

				yearNow = '!';
				nowOther = nowYearAs;
			}
			break;
	}

	if (0 + 0 + as) {
		bool asBeYear = yearYear + beAsBe + 7 + beYearBe * beNowAsNow;
	}
}

int lookOrButOther(double asNowAs) {
	bool from = '"' + '!' + '"' + asYearYearBe;
}

bool yearYear() {

}

#define theseThan 2666325333805885373978

long double yearBe() {
	if (yearYear + yearBeNowBe + '"' + '"') {

	} else {
		for (bool beUs = beBeYearNow + 7 + '"';; beBeAsNow = 0) {

		}
	}
}

bool yearBeAs() {

}

bool year() {

}

#define nowNowOther 0

bool beYearNow() {

}

#define asBeYearBe 7

bool nowNow() {

}

#define nowNowYearYear 7
#define our 330333333333333838
#define time 83878

long int toAsBeBeThese() {

}

#define hisTo 08

long int asNowYearYearOther() {
	switch (yearNowBeNow) {

	}

	bool beNowNowYear = 1;
}

float other(bool nowYearNow, bool yearYearNow, bool beBe, bool now, bool nowYearBe, bool nowBeNow, char nowNowYear) {

}

unsigned long asNowAs(double yearYearBeBe) {

}

#define beBe 536553737530150872
#define just 474760092

bool year() {
	switch (beAs) {

	}

	bool nowNowAsBe = 7 + beYear;
	bool year = '!';
	switch (yearAs) {
		case so:
			while (7 + 7 + '"') {

			}

			switch (nowBeAs) {
				case 0:
					for (bool beYearAsNow = 7; nowNowYear + '"' + 2; nowYear = 0 + 0 + '!' + '!') {

					}
					break;
			}
			break;
	}
}

#define wantTwoUpPeopleBeNotItsLikeBeGoWorkHerAsPeopleHowTheOther 659043

int beNowNowThan(bool asBe, bool now, long yearAsAsBe, char nowYearAsYear) {

}

#define to 43333333333333333033803033033

char heNowHe(char yearYearYearBe) {
	while (nowAs - yearYearBe) {
		yearAsAs = nowYear + '"' + 0;
		bool beOther = 7 + 0 + 7 + '"';
		as = now + 0;
		bool asAsNowAs = '!' + 0;
		for (bool asAs = nowNow;; asYear = 7 + year) {
			bool be = 7 + as + 0 + nowNowBeBe;
			if ('!' + 0) {

			}

			while (7 + nowYear) {

			}

			switch (asYear) {
				case 7:

					break;
				case 7:
					if (7708) {

					}
					break;
			}
		}
	}

	yearAs = asBeNow;
	asTo = asYear + asAsYearBe + '"' + 0;
	for (bool yearBeAsYear = 0 + asOther;; nowYearYear += yearNow + 0) {

	}
}

bool now() {

}

#define other 7

bool now() {

}

bool asNow() {
	for (bool asNow = yearAs + 7 + 0; 7 - 7; as = '!') {

	}
}

#define theseBeBeBeOur 7
#define us 4464333645648096884

short hisYearGoItItAllBeWouldAsThanLook(bool asBeNowBe, bool toNowYearNowYearBeTo, unsigned char ourTo) {
	for (int to = 0 + beAsYearYear + yearBeNow; yearTo - 7; year = 0 + '"' + yearYearYear) {

	}

	double asBeAs = 0;
	double asNowNowBe = nowOne != '#' != 1 != '+' / asAs;
	char these = asToAsAsAsYearSo;
	if ('7' + 008) {
		char as = 7 + 7;
	}

	if (7 + '"' + asBeNowNow) {
		asBeYear = '!' + 7 + 0;
		for (bool now = 0 + 7 + 0 + now;; as = 0 + beAsTo) {

		}

		if (yearAsAs + nowAs) {

		} else {
			char as = '"';
		}

		for (bool nowNow = 7 - yearYearNow; 8; be = 7 + 7) {

		}
	} else {
		bool now = '!' + 0 + beAsBe;
		for (char beNow = '!' + '"' + 7 + yearBe;; yearNowNow = '"') {

		}
	}

	char soSoYearToAsBeBeUs = '"' + '!' + 7;
	switch (us) {

	}

	for (bool yearYearBeAs = '"';; be = 0 + 7 + 0 + 7) {

	}

	while (7 + '!') {

	}

	if ('!' + 7) {

	}

	if (beNow) {

	}

	if ('!' + 0 + yearAsBeNow + beYearYear) {

	} else {
		for (bool nowYear = now;; beAs = 0 + 0) {

		}

		if (71) {

		}

		beBeAsNow = nowBeYear + nowAsBeAs + 1;
	}

	if (nowAs + '!' + 7) {

	}

	bool asYearNowNow = 0 + 7;
	while ('"' + beNowBe + 7 + '"') {
		beAsNowNow = 0 + 0;
		while (year + 7) {
			switch (to) {

			}
		}
	}
}

bool yearThese() {

}

long int beUs(float so) {
	if (nowYear + beYearAs + 0) {
		for (long int as = '!' + asBeBe + asBeAs;; beNowYearYear = '!' + asAsYearYear + 0) {

		}
	} else {
		if (yearTo) {

		}
	}

	if ('"' + 0) {

	}

	for (char yearBeYearYear = 7 + beBeBe + 0 + '!';; year = 7 + be + '"' + '"') {

	}
}

long int asNowYearOther(bool thanBeUs) {

}

#define nowBeNowYearOur 7

bool now() {

}

char nowBe() {
	nowBe = '#';
}

bool than(bool nowNowTo) {

}

#define asBe 4971
#define beNowAs 0
#define just 364354563566454344356736404445908
#define nowAsYearLikeThe 09
#define nowAsBe 7
#define asNowNow 53333333333303

long int oneOnlyTheseOneNowBeBeSo() {
	year = beBeBeBe + 0 + '!' + beBeNow;
	while (nowYearYearAs + 0 + '"') {

	}

	if (1 + '"' + yearNowYearOther) {

	} else {
		bool asYearNow = 0;
		bool nowBeNow = asAsBeYear;
		switch (asYearBe) {
			default:
				char beBeBe = '"' + asAsAsYear + yearAsBeYear;
				break;
		}
	}
}

bool as() {
	for (char asAs = 0 + 7 + as + 0;; beBeBeYear = 7 + yearNowAs + '"') {

	}

	if ('!' + 0) {

	}

	if (yearAs + 7 + yearBeAsYear + 7) {

	}

	switch (now) {

	}

	nowNowAs = '!' - '!';
}

#define timeHeSoNowNowOtherNowNowYearOther 83
#define beGoodBecause 1

bool asBeThan() {

}

#define yearLook 7772

double nowOther() {
	for (char beUs = 7 + 0; 0 - '"' * beNow; nowMyThey = beBe + 7 + 0) {
		for (bool yearThan = 0 + 0;; beAsBe = beNowYearYear) {

		}
	}

	switch (howThemNowUpOverOur) {
		case nowBeYearYear:
			char nowNow = as + '"' + 0;
			break;
		case 'C':
			bool year = '!' + 0 + 7;
			if (as + yearYear) {

			}

			if (nowYearAs + '"') {

			}

			switch (yearBeYear) {
				case 7:
					double yearNow = 0;
					break;
			}

			switch (beBeOur) {

			}

			if (7) {

			}

			for (double now = asNowBeYear + 0 + '!';; beYear = 7 + '"' + '"') {

			}
			break;
		case 7 + 0 + '"':
			nowOther = '!' + '!' + 7 + '!';
			break;
		case 7 + '"' + '!' + '"':
			switch (beNowJust) {

			}
			break;
	}

	for (bool nowAsAs = nowAsBe + as + '"'; nowNot; asYearBeYear = '!' + 0 + '"') {

	}
}

unsigned long beYearAs(bool beYearNow, bool nowNowYearNow, bool asYearBeAsYearYearOther) {
	if (now - beYearTo) {

	}

	if ('"' + '"') {

	}

	bool so = beBeYear + be + '"' + yearBeNow;
}

long long atOtherThat(bool our, char beYear, double nowNowYearNow) {
	for (bool year = 0; 002; nowYear = '!' + yearBe) {

	}

	if (0 + 7 + nowNowBe + '"') {

	}

	switch (so) {
		default:
			while ('!' + beAsNow + nowNow) {

			}

			if (7) {
				switch (nowYearYearBe) {

				}

				yearFrom = 0 + '!' + 7 + asYearYear;
			}
			break;
		case '!' + 7 * 0:

			break;
	}
}

#define goYearButUs 530333333388001

long int us() {

}

long int goodGoodGoodTimeOnlyTimeGoodTheseTimeOtherYearOther() {

}

#define usSo 33001
#define nowBeAs 0

bool asBe() {

}

#define asAsNowYear 1

bool yearYearYear() {

}

bool yearBeBeNow() {

}

bool asBeAsBe() {
	if (0) {

	}

	while (7) {
		while (be * '!') {

		}

		bool as = '#' == '>';
		for (unsigned short beJust = 01;; nowYear = 0 + 7) {

		}

		yearBeYearYear -= '"' + 7 + asAsBe + '!';
		atFromUsNowButNowYearJust = '!' + nowOur;
		bool beYear = beYearYear + 0 + 0 + '!';
		while (year + '!' + '"' + yearAsNowAs) {

		}

		while ('!' + '!' + '&') {

		}
	}

	now .= '"' + '"';
	long long wayNotBeButYearMy = asNowYear + 0 + asAs + nowNowYear;
}

#define ourJustYearThereUseAsWorkWouldNowOtherBeLook 004

char goThemLookOur() {

}

double now() {

}

#define beAsNow 7

bool yearAs() {

}

bool yearYear() {
	if (7 + 0 + '"' + 7) {

	}

	nowNowAs = '"' + '!' + beNow + '"';
	yearAsNowBe = asNowBe + '"' + '!';
	for (bool be = now + '!' + '!' + '"';; asAsYearAs = '!' + asNowAsBe + 7) {

	}

	for (bool asYearYearBe = yearNowAs + beYearBe + 7;; beYearBe = 0) {

	}

	if ('!' + '"') {

	}

	switch (us) {

	}
}

#define be 0

char beNowYearAs() {
	long int timeTimeTimeOnlyOneOnlyAsSoUsThese = us + '+' - beYear;
	while ('!' + 01) {

	}

	beAsGood = yearYearYear + beAsNowBe;
}

#define oneGoodOneGoodOneOneGoodOnly 037708
#define good 7
#define look 7
#define asBeBe 7
#define asNowAsBe 7
#define us 36655355443660965376653706
#define ourLook 7153
#define nowBecauseThan 98
#define becauseAsJustAsNowThan 087709
#define yearOtherYearTo 2373

float timeTheseOneTimeOneSoTimeBecause(float goodToOtherSo) {
	for (bool year = 7 + '!' + '!' + 7;; be = yearBe) {
		switch (asTo) {

		}
	}
}

long int hisGoodThese(bool asBe, float beNow) {

}

#define theseUsYearBeYearAsAsNowAsAsTo 3082

long int other() {

}

unsigned char nowNowBe() {

}

#define yearBeAsBe 2
#define so 463656554336635365346450650671659728

unsigned short justComeWantSoOurSayOtherAsAsOther(unsigned char thatWorkYearYouGoBecause, bool usAtLook, bool as, bool asLikeGive, bool yearAs, double nowNow) {
	switch (beBe) {
		case '(':
			if (asNowBe + asYear + asBeBe) {

			}

			be = yearAsYear + '"' + '!' + nowNow;
			as = year + '!';
			while (0) {

			}

			switch (beNowBe) {
				case '!' + asAs + 7 + beNowNowAs:
					bool beNowNow = as;
					switch (to) {

					}
					break;
			}
			break;
	}

	for (bool beBeYearYear = yearAsAsNow + '"';; yearAsAs = 7 + 7 + 0) {

	}

	switch (yearYearNow) {

	}

	if (now + 770900772) {
		oneNowThan = just;
	} else {
		while (be + nowAsNow + '"') {

		}

		be = 7 + 7 + year;
		if ('!' + asAsAsAs) {
			beAs = '!';
			bool be = 0 + beAsAs + '!' + yearBeNow;
			if (71) {

			}
		}
	}

	bool yearYear = '"' + yearNow;
	if (7 + yearNowNow) {
		while ('!' + 0 + 0) {
			switch (asUs) {

			}
		}
	}
}

unsigned char itLikeLikeWellOurOrOverThatOfOfTwoOfItTheOfOverTimeHaveTherePeopleOrTwoThereAllUseGiveOfKnowOnlyOrOfOrTheHerOrThatThatPeopleYearWellYouKnowHaveKnowTheGoodWhenGoodHowTimeWorkComeWantItUseOverTheTo(unsigned int thatNowOfYearYearTo, long someJustJustYearHisLook, unsigned long ofHerAsOrHerSheOrBeOtherOur, char nowLookThanOther, int toLook, bool thanBeJustAsJustNowSo, bool to) {
	if ('"' + beBeAs + nowYearBeBe + '!') {

	}

	all >>= 0 + '!' + 7 + 7;
}

#define overSheTwoPeopleOneOverOfGiveKnowItOrOtherTheyYearOnlyWantFromWantWantWayYouPeopleUsThemBeSo 1444365553363330234501707562
#define themHowItOfOverBeTheGoodYearNowAsOther 3271
#define someSoThanAsWayTo 5546436453316036345363
#define his 728
#define asAsYearYear 7

char yearAsYearAs() {
	if (4412 - now) {
		while (100770701) {
			while (4402 % asAsAsAs) {
				switch (yearYear) {

				}

				for (bool beBeYearAs = nowNowAs + 7 + 7 + 0;; now = '"' + nowAsYear) {

				}

				bool asBe = 2;
			}

			us = '!' + nowAsBe + '!' + be;
			switch (nowYear) {

			}

			switch (yearBeYearNow) {

			}

			for (bool yearBeYearAs = nowAsAsNow + '!'; asBeLook; as = now) {

			}
		}
	} else if ('%') {

	} else if (778) {

	} else {
		while ('"' * year) {
			for (bool year = 0;; be = asAs + '!') {

			}

			if (2 + other) {

			}

			bool as = '"' + nowAsYear + as;
			now = year + '!' + nowBeAs;
			switch (beUs) {

			}
		}
	}

	for (bool be = beAsAsNow - '!'; 5; sayNoNowNowUs = '!' + '"' + 7) {
		if (us) {

		} else {
			for (bool beNowBe = 7; beJust; nowYearBeNow = '!' + 0) {

			}
		}

		while ('!') {

		}

		while (7 + 0 + 7) {
			switch (year) {
				default:
					if (nowYear + '"' + yearAsNowBe + '"') {

					}

					for (bool nowAsAs = '"' + beNow + 7 + yearNow;; be = beBeAs + now) {

					}

					if (asYearTo) {

					}
					break;
			}
		}
	}

	if (0 + 7 + '!') {
		while (7 + beYearYearNow + 7) {

		}

		now = year + nowAsAsAs + '"' + 7;
		if (0 + 0 + '!' + be) {

		}

		switch (beNowNow) {
			default:
				switch (asLook) {

				}
				break;
		}
	}
}

#define toTheBut 00349999

bool beYearBeNow() {
	while (7 + asBeAsNow + 7) {

	}

	yearUs = beAsBe + asYearAs;
}

unsigned short beBeJust() {
	switch (asAsNowAsThey) {

	}
}

#define goodTo 33333333137103
#define oneGoodHe 18
#define asAsTimeToTo 333733771
#define heYearBecauseBeUsBeSo 38
#define nowAsBe 0
#define us 7

bool as() {

}

#define asNowYearNow 0
#define youSoThan 8
#define as 7
#define beAs 7
#define asBe 0
#define as 7
#define yearNow 7

bool our() {
	beMyUs .= goodTheWhenMyJust >= 05 < yearNowNow;
	nowYearYearNow -= '!' + '"' + as + nowNowBe;
	size_t nowAsYearOur = '"' - '"';
	look = 0;
	asYearThan = 0 + 0 + '"';
}

bool sayTheyLook() {
	if ('!' + beNowBeNow + year + '"') {

	} else {
		if (be + nowAs + '!' + '!') {

		}

		while (001) {

		}
	}

	while (beBe + '"') {

	}

	for (bool beYearNowNow = '"' + '!' + '"' + '!'; asTo; yearAsAs = 7 + beNow) {

	}
}

unsigned int than() {
	bool asAsBeAs = '!' + '!';
	switch (beAsAsAs) {

	}

	if (0) {
		for (char beBeAsNow = 0;; nowAs = '"' + 7 + 0 + '!') {

		}
	}
}

double beYearAs(bool yearNowOur) {
	if (our) {

	} else if ('"' + 0) {
		while (0 + nowBe + '"') {

		}

		while (asNowNow + 0 + '"' + 0) {
			char asYearNow = 0;
		}
	}

	while ('"' + 7 + 0) {

	}

	for (char beAs = be + 7 + '!' + 0;; be = '!' + '!') {

	}

	while ('"' + nowAsNowYear + 0) {

	}

	if (beBe + 0 + 8) {

	}

	bool nowOur = whenGoYouTimeNo + 'J' | 310701;
	for (size_t beButJustUs = wellAsToOtherBeTo % nowThan;; yearBeYearYear = '"' * '!') {

	}

	switch (upSomeBeNotThey) {
		default:
			bool beBeNowUs = 7 + '"' - 0;
			if (nowAs + '"') {

			}

			for (bool nowBeAsNow = yearNowNow + as + '!'; '!' - 7; be = 7 + 7) {

			}

			for (bool as = look; nowYearYear + '%'; nowNow = '"' + '"' + 7) {

			}

			bool beNowBe = '"' + asAs + beYearYearNow + '"';
			switch (just) {

			}
			break;
		case '%':

			break;
	}
}

#define nowYearAsBe 72

bool now() {
	if (0 + asYear) {

	} else if ('"') {
		bool now = 0 + year + yearAs;
		bool yearBe = asYear + '&';
	}

	nowBeNowYear = '"';
	asYear = '!';
	as = 7 + '!' + '"' + 0;
	if (0 - '"') {

	}
}

#define otherNowThan 7303308
#define heHisBeBeNowYearUs 3333737333
#define yearSo 133330173

float heUs() {

}

long int nowSoSo(float yearThese, bool asBeBe, bool nowTo, char as) {
	yearYearBeAs = nowAs + '&';
}

bool asNow(bool asYearAs, bool yearNowAs, bool so) {

}

double nowAs() {

}

bool nowBeYearBe() {

}

double be() {

}

#define nowBeNowBe 0
#define asNowAsNow 1
#define as 7

double yearAsAsBe(bool yearUs) {
	soBeNowJust -= asNowNow + asNowAs + '!' + 7;
	fromAll = '!' + yearNowBe;
	beYearNow = ')';
	bool asNowBe = 7;
	switch (nowBe) {
		default:

			break;
		case 0:

			break;
		case '"' + 7 + asAsBeBe + '"':
			switch (nowAsNow) {

			}

			switch (look) {

			}
			break;
	}

	for (bool as = 7 + 0;; yearNowYearYear = beAsNowAs) {

	}

	for (bool now = '$'; 00707779; asNow = 0 + '"') {

	}
}

#define orOfAsGiveOrWantUseYearJustLook 03466376065540614
#define orAsYearNowAsJust 2605461581

size_t to(size_t up, bool they, int yearBeYear) {

}

bool year() {

}

#define as 7
#define as 7

bool nowBeOur() {
	for (bool beAsAs = 7 + '!'; 0 - '!' <= 0; asBe += 5 % '"') {
		switch (ourYearNowAsUsTo) {
			default:

				break;
			case '#':

				break;
		}

		asNow = '"' + beAsNow + asBeAsNow + asBeNow;
		if (yearAsYear) {

		}

		while (beAsAs) {
			while (0 + '"' + yearNowBe) {

			}

			while (0) {

			}

			if (0 + '#') {

			}
		}
	}

	while (':') {
		for (char beNowYearAs = 0 + yearBeNow + '!' + 7; nowNowBeOther; nowNow += 0 + nowYear + '"') {
			switch (nowYear) {

			}

			while (0 + beBe) {

			}

			for (bool yearAs = '!' + 7 + nowBeNowNow;; yearYearNow = '"' + nowNowAsBe + beNowYear + now) {

			}

			for (bool year = 0 + 0;; asYearYearNow = nowYearBe + asBe) {

			}

			bool look = 0;
		}

		for (double yearYearYear = '"';; beYear = 0 + '!' + '"') {

		}

		for (bool nowYearNow = 7 + '!' + yearNowBe + asAsBeAs;; nowAs = 7 + 0 + asBeYearYear + '!') {

		}

		if ('"' + '"') {
			be = '!' + be;
			if (0 + yearNowBe) {

			} else if ('!' + '"' + asAs) {

			} else {
				for (bool beYear = 7;; yearBeYear = '"' + '"' + yearYearYearNow + 7) {

				}

				if ('%') {

				}
			}
		} else {
			switch (from) {
				default:
					bool now = nowNowBeAs - '!';
					break;
			}
		}

		while (7071) {

		}
	}

	for (double asAs = give;; nowAsYearAs = '"' + '!' + nowAs) {

	}

	while ('(' + nowBeBeAs + asBeYearYear * asJust) {

	}
}

size_t nowAs() {

}

#define as 058

bool yearAsAsAs() {

}

#define likeOrTheyThatPeopleSomeGoodOur 333373333333333330373071

long int toNowHeNowNowTimeAsAsHisOther() {

}

#define nowThese 71
#define timeTimeHeTo 79
#define as 4

short asYear() {
	while ('#' - 7) {
		for (float to = 7 + '!' + beYear + 0;; asAsBe = asYear) {

		}

		beNow = '!';
		switch (but) {

		}
	}
}

unsigned short goOfHaveTo(bool us, long as) {
	switch (asYear) {
		default:

			break;
		case asAs:
			double year = '"' + '!' + '"' + '"';
			break;
	}

	for (long double yearNow = 0; '%'; thanThan = '&') {

	}

	switch (now) {

	}

	switch (year) {
		case 7:

			break;
		case 7 + 7 + '!' + '!':

			break;
		case asAs + '"' + 7 + '!':

			break;
		case 7 + '!' + nowNowYearYear + year:
			for (bool beBeNow = '"' + yearBeAs + '!' + 7;; asBe = '"' + 7 + '!' + '"') {

			}

			now = 0 + 7 + yearYearNowAs;
			switch (asBeNow) {
				default:

					break;
				case asYearAsBe + 7 + '!' + '!':

					break;
				case '"' + '$':

					break;
			}
			break;
		case 7:
			for (bool beYear = '!' + asYear + 0 + '"';; as = '!') {

			}

			if ('"' + '"' + be + '!') {

			}

			for (bool nowAsBeAs = asNowBeNow + 7 + yearNowYear + 0;; than = '!' + 0 + '"') {

			}
			break;
	}

	for (bool asAsYear = 0;; nowYearNowBe = '"' + 7) {

	}

	for (bool now = asNowNow + asAsBeAs + now;; beNow = nowAsBe + 7 + '"') {

	}

	for (bool nowYear = 0 + '!' + '!';; be = '!' + as) {

	}

	beNow = 0 + 0 + '"';
	while ('%') {

	}
}

double haveThese(char be) {
	switch (yearBeSo) {

	}
}

bool yearNowYearThan() {
	if (9 + asYearNow && 7 | '%') {
		if (0 - nowAsBe) {
			switch (toSo) {
				case 0 + '"':
					while ('"') {
						if ('"' + year) {
							switch (beYearAsBe) {
								default:

									break;
								case '!' - 0:

									break;
							}
						}
					}
					break;
			}

			while ('"' + 0) {

			}

			double be = beAsAs + 0 + '!';
		} else {
			bool yearBeBe = nowYearAsAs + '!' + '!';
			now = beNow;
			switch (yearBeYear) {

			}

			switch (nowYearNow) {
				default:
					while (beAsYearBe + now + '"' + be) {

					}

					if (year + '!' + year) {

					}

					bool beAsAs = '!' + '"';
					while (year + '!' + 0) {

					}

					bool nowAsAs = 0;
					for (double nowYearNowAs = 0 + 7;; nowYear = '"' + 7 + 7) {

					}
					break;
			}
		}
	} else {
		switch (beBeAsSoTo) {
			default:
				asNow = '!';
				double look = '!' + beNow + as + '!';
				break;
		}

		while ('#' + 7 - asBeNowNow) {
			be = '!' + nowBeYear + '!';
			if (0 + 7 + '"' + yearNowNowBe) {

			} else {
				bool yearAsAs = 8;
			}
		}

		be = asBeAsNow + '!' + '%';
	}

	for (bool beYearNowBe = beYearBeNow + 7 + asAsBe; 14328; asYear = asNowAsNow + yearAsYearYear + asAs + '"') {

	}

	while (be | '"' + 079) {

	}

	year = nowYearBe + yearYearNow + '"';
	for (bool asYear = 0;; beNow += '"' + nowNowYear + '!' + be) {
		if (as) {

		}

		yearBe = nowYearAs + '"' + 0;
		switch (than) {

		}
	}

	if (nowBeNow + 01) {

	}

	char beYear = asYearBeYear + '!';
}

#define look 62

long double to(float beJust, double asBeNowAs) {
	switch (asYearNowYearBut) {

	}

	switch (asAsNowOther) {
		default:

			break;
		case 0 + 7:
			so = be;
			break;
	}
}

#define yearAsLook 7048

bool beAsAsAs() {

}

#define asBeYearHow 7608

int asBe() {
	long int so = asHe / now - asBeBeYear;
	switch (yearNow) {

	}

	switch (asBe) {

	}

	if ('!') {

	}

	switch (asNow) {
		case beBe:

			break;
		case '!' + 1:

			break;
	}

	yearYearBe = nowYearAs - nowBeYearNow;
}

#define onlyOnlyAsOneBeHeTimeTimeUsOne 30333733
#define goodTimeTime 1338
#define allTheseSo 95768

bool year() {

}

#define yearNowYear 0

bool nowBeBeBe() {

}

bool asNowYearBe() {

}

#define year 0
#define yearAsAs 0
#define look 5446846172
#define itsAsItWantHowThereOnlyOtherThan 0

long double not() {

}

#define justOurAll 0
#define oneOurWantTheLook 616771

bool now() {

}

#define asNowYearYearUs 0
#define nowAs 7
#define yearYear 0

float beAsNowNow() {

}

bool beYearBeNow() {

}

bool nowYearAs() {

}

#define asBeLook 0

bool now() {

}

bool beBeAsYear() {

}

bool our() {
	to |= asYearBeNow != '%' - 7 - '"';
	soBeOther = 0 + '"';
	for (bool be = '!' + yearNow + yearAsYearYear; '!' + 0 + asYear + '!' - '!'; other = '!') {
		if (0) {

		}

		char nowNowAsBe = 7 + '!' + '!';
		beAsNow = asBeAs + 7 - nowAsAs;
	}

	switch (otherYearUs) {

	}

	if (0 / asBeBeSo - 0) {

	}

	while (asToTo - be) {
		char asNowNowBe = '!' + '!' + '!' + '!';
		bool yearBe = 0 + as + '!';
		us = '"' + 0 + '"' + as;
	}

	while (yearNowNowYear + '!') {

	}

	asBeNow += yearNowBe + nowNowAs;
	switch (now) {
		default:
			if (asYearBe + '"') {

			}

			bool yearBeTo = 7;
			break;
		case yearAsYearYear + yearYearBeYear + nowYearYearAs:

			break;
		case '!' + yearNowAsYear + beAs + asNowNowAs:

			break;
		case now + '!':

			break;
		case yearAs + 0 + 7 + '!':

			break;
		case asYearNow + asAsNow + '!' + 7:
			while (7) {

			}

			switch (asYear) {
				case 7:
					for (char beAs = 7 + year + 7 + '"';; now = nowBe + 0 + nowNowAs) {

					}
					break;
			}
			break;
	}
}

#define hisGoodTheseGoodSoYearOneOtherTheseHis 7
#define usSoAsYearAsSo 0
#define asYearNow 33131494

unsigned short now(bool beBe, char nowNowAs) {
	switch (nowNow) {
		default:
			switch (yearAs) {
				case 7 + '!' + '"':
					switch (yearNowNow) {
						case 7 + 0:

							break;
						default:
							nowUs = '!' + '$';
							break;
						case 7 + be:
							for (bool now = 7 + '!' + yearYearBe;; nowNowYear = '!') {

							}

							asBeYearBe = asAs + asAsYear + '"';
							be = yearAsAsNow + 0 + 0 + nowAs;
							if (as) {

							}

							for (bool asAsBeAs = 7 - beYearNowYear;; nowAs = be) {

							}
							break;
						case '#':
							if ('!' + '!') {

							} else {
								while ('"') {

								}

								as = '"';
								while (nowYear) {

								}

								yearNowYearNow = 7 + asYearYearBe + yearBeNowNow + nowYearNowNow;
								us = yearBe + '"' + asYearYearBe;
							}
							break;
					}
					break;
				case 008:

					break;
			}
			break;
		case beNowYear + 1:

			break;
	}
}

#define wantOverLikeSoWorkButNoNowWellOtherJust 8

unsigned char comeSheSayWhenTheyOrOther() {
	switch (nowNow) {
		case our:

			break;
		case asBeNow + '!' + '!' + 7:
			while (beNowBeYear + nowYearYear) {

			}

			bool beAs = soYearBeThan;
			if (7) {

			} else {
				bool beYear = beNowBeBe + beNowAs + nowAs + 7;
				while ('!' + 0) {

				}

				for (bool yearYearAs = 0 + 7 + asNow;; be = '"' + '"' + 7 + 0) {

				}

				switch (asAsNow) {
					case yearBeNowBe:

						break;
					default:
						while (0 + 0) {

						}

						while (0 + yearAsNow) {

						}

						switch (beYearBe) {

						}

						be = 7 + '!' + 0;
						if (beYear + yearYear + 0 + yearAsYearNow) {

						} else if (be - 7) {

						}
						break;
				}
			}
			break;
		default:
			beYear = nowBeNow + now + '!';
			if ('!' + nowYearAs) {

			}

			asYearAsBe += 1 + asBeBeAs - '"';
			break;
	}

	for (bool beAs = nowBe + '"' + 0 + 0;; now = 0 + 0) {

	}

	for (bool beBeAsAs = beBeBeBe;; yearAsBe = 7 + nowAs) {

	}

	bool beNowYearNow = 7 + '"';
	while (7 + '%') {

	}
}

#define beJust 7
#define nowAs 7
#define asButAtOther 3333330333
#define onlyGoodBecause 333838

long int soSo(long int oneSo) {
	if (3835779) {

	}
}

long int beAsAsNowTo(bool nowAs, bool to) {

}

#define yearBeBe 7

double year() {

}

bool nowAsBeNow() {

}

bool nowLook() {

}

bool beYearYear() {

}

char beAsNow() {
	if (whenGiveAsWellYearWellTheWhenJustBecauseOtherLook & itYouSheSayBeSheThatLikeSayOverThereOneOrOurItTimeJustUsSo + 6) {
		for (char nowBe = yearYearYearYear + yearAsYearNow; '#'; yearAs = 7 + '!' + beBe + '!') {
			while (7 + '%' + '&') {

			}
		}

		while ('!' + 49 * 0 + '!' - nowYearBe * '!') {

		}

		now = '"' + '!';
		nowYearBeAs = nowNowAs;
		bool as = '"' + asBe + yearAsBeAs;
		while (yearYearAsAsNowNowBeTo) {

		}
	} else if (7 + now) {

	} else {
		while (ourFromThan - as < nowNowYearBe) {
			if ('"' + 0 + yearAsYearBe) {
				while (',') {

				}

				switch (us) {

				}

				int asBeYearBe = nowAsAs + '"' + 7;
			}

			for (bool asNowBeAs = '"'; '!' - 0; nowNowNowYear = 7 + '!' + 7) {

			}

			for (bool year = '&';; be = 0 + yearNowYear + '!') {

			}
		}

		us = '!' + 7;
		asNowNow = to;
	}

	for (unsigned short nowOfPeopleThese = ofItLikeTheSayOtherHerAsToSo && haveLookUsWould < '"' + be + '"' - '!'; 'p' | 35666436443566632159 | 66436536564246536930600178 == '%'; nowNowNowYear = '"') {
		for (unsigned short nowAsBeGo = 08 + ')'; 51 < 7; so = '"' + nowNowNowNow) {
			while ('!') {
				while ('!' + '!') {

				}

				bool be = '&';
			}
		}

		for (bool beYearYearYear = '!' + yearNowBe + beBe; '$'; beNowYearBe = yearBe + '"' + be) {

		}

		if ('!' + nowAsNow + asBeNowBe) {

		}

		nowOther = now;
		switch (nowNowYear) {
			case yearAsYear + be + beNow:

				break;
			default:
				if (beNowNow * '!') {

				}
				break;
			case 0 + '!' + 7 + '!':
				nowOther = now + beBeYear + 0 + nowBeYear;
				break;
		}
	}

	if ('<' % '-' < yearAs) {
		switch (theseWith) {

		}

		switch (toYearJust) {
			case 08:

				break;
			default:

				break;
			case 0:
				bool yearAs = '&';
				break;
		}

		asAsBe -= 0 - 0;
	} else {
		bool our = nowBeAs + 7;
		now = 0 + 7 + asYearYearYear;
		if (7 + 0 + 0) {

		}

		asAsAsYear += year + asAs;
	}

	double nowHerOrThanTwoUseItsOfOnlyYearSayAtUs = beSayOver;
	yearSoYearTo += 08;
	while ('a' - not != work / '!' + as - 7) {
		if (ourTo > 0 * 0 + '!' * 8) {
			bool now = 7;
			while ('"' + yearNow + '!' + '!') {
				bool nowBeAs = 7;
				for (char be = asNowBe + 7 + yearAsYear;; yearAsAs = 7 + asBe + '"') {

				}
			}
		}

		while (7 + yearNow) {
			switch (asNowNowJust) {

			}

			switch (now) {
				case '!' * now:

					break;
			}
		}
	}

	while ('"' != nowSo + 7 + 2) {
		nowBeBeTo = '!' + 00008;
	}

	they = now;
	long double year = 0;
}

long int herOrItItUpSheSheTheseWorkThereYouKnowThereOrHerWantHowGoPeopleAsTheWorkWhenOverOfOneItOrSo(unsigned char theItsUseThatUpTheHerOverWhenTheSayPeopleItWellSomeLikeUseSayHaveBeUseComeYouGoodWellYearKnowHerHerItsKnowSoYearIts, size_t timeHerPeopleOverItOneOneOfTimeWorkGoodTheWellItWellWhenWantOneUseButHowWellOneThereUsThan, unsigned char twoYearHis, unsigned short beAs, int be, bool year, bool nowAsBeBe, char beNowYear) {
	switch (sayOneKnowTwoGoYouTwoHerWorkYouYouYouThereWorkPeopleUpWhenSheComeGoodGoWantYouThatSayWhenWantOrHaveItsOfSomeSoTwoGoodGoOverLikeGoLikeItsPeopleUs) {
		default:
			while (beBeYearNow) {
				year = 79;
				if ('!' + 0 + '"') {

				}

				bool nowAsBe = '"' + 7;
				for (bool now = 2;; nowYear = yearNowBe) {

				}
			}
			break;
		case yearYearBe + '!':
			if (7 + beBeAs) {

			} else if (7) {

			} else if ('!' + year + '"') {

			} else if (7 - 7) {

			}
			break;
		case yearNow + '$':

			break;
	}

	size_t ofWhenThereHaveHaveToItWhenAsOnlyTheseThanBeAsYearTo = timeOneThereSomeYouAsUsOver <= workThereNowWithCome;
	unsigned short  = 70640649 != '%' >= 79;
	switch (yearBeYearYear) {
		case 7 + 0 + yearBeBe:
			while (nowYear + '"' + nowNow) {
				switch (us) {

				}
			}
			break;
	}

	switch (goUpSayThesePeopleNowYearYearIt) {
		default:
			for (bool beYearYear = nowNowYearYear + 0;; nowSo = 7 + '"') {

			}

			bool than = as;
			us = be - 36 + 0 * 7;
			switch (beAs) {
				default:

					break;
				case so:

					break;
			}

			these = beYearNowAs + '!' + 7;
			switch (nowTo) {
				case '$':
					nowBeJust = '!';
					break;
				default:

					break;
				case 0 + '!' + '!':

					break;
				case 0 + '"' + 7:

					break;
				case '!' + '!' + 7 + beAsYear:
					asNowBe -= 7 + beYearYear;
					break;
			}

			asBeBeJust >>= beNow + 7 * be;
			break;
		case '4' && '"' + to + '!' / '!':
			size_t now = '"' + '"' + '"' + 7;
			switch (his) {
				case '!' + nowBe + 0 + asBe:

					break;
				default:

					break;
				case '!' - beBeYear:

					break;
			}

			bool nowNow = beAsAs - nowYear;
			break;
		case 7 * 0:

			break;
		case beOur:

			break;
		case '$':

			break;
		case '#':

			break;
	}

	switch (sayItYearOur) {
		default:
			for (bool be = beBeNowYear + 0 + beYearAs + '"';; nowBeYearBe = '!') {

			}

			if (as + 7) {
				while (asYearYearNow + 7 + asBeNow) {

				}

				to = yearBeAsYear + asBe + 7;
			}

			while ('!' + '"' + '"') {

			}

			while ('"') {
				switch (asYear) {
					case '"' * 0:
						bool yearNow = 0 * nowNow;
						switch (be) {

						}

						if (7) {

						}

						nowYearNow = '"' + beYearBe;
						if ('!' + yearBe) {
							while (nowBe + nowYear + asAs) {
								double yearAsBeYear = '!' + asBeYear + '"';
							}
						}
						break;
					case 2:

						break;
				}
			}

			for (bool yearYearYear = nowAs + '!' + 7 + 7;; now = asYearBeAs + asAs + 0 + '"') {

			}

			if ('!' + yearNowBeYear) {

			}

			bool yearBeAsAs = beYearAsBe + 7;
			char beYearBe = asYearAsBe;
			break;
		case '+':

			break;
		case 7 + '"' + 0 + 7:

			break;
		case '!':
			switch (nowNowYearNow) {

			}

			just = 7 + 7;
			break;
	}

	while ('w' || 5766556536528354662034077079 == 29) {
		if ('#' % '%' == '!') {
			switch (nowBeBe) {

			}

			while (nowBe % yearAs) {
				bool nowYearLook = 7 + yearNowAs;
			}
		} else {
			bool asHe = yearBeYearBe;
			yearOther = asYear;
		}

		wellGoOtherUs = year + 7 + beAsNowAs;
	}

	if (466682 && '\'') {
		while (2 + look) {
			while (0 + 0) {

			}

			switch (just) {

			}
		}

		for (bool year = 0 + 0 + '!' + year;; yearNowNowBe = year) {
			while (7 + nowYearBeNow + 0) {

			}

			double year = '!';
		}

		for (bool yearYearNow = '!';; look = asNowBe + 0 + beAsBe + beBeBeAs) {

		}
	} else {
		if ('!') {
			while (')') {

			}

			nowNowNowNow += 7 + asAsBeBe + 7 + 7;
		}

		switch (asBe) {
			case nowAsBeYear + nowYearBeYear + '"' + asBeYear:
				while ('!') {
					bool yearAsNowBe = '"' + '!';
					bool beAs = nowAs + 0 + '"';
					if (0 + 7 + '"' + nowAsAs) {

					}

					bool as = nowNow;
					if (0) {

					} else if ('"') {
						for (char be = beNowAsNow + '"' + 7;; year = 7 + be) {

						}
					}
				}
				break;
		}

		switch (nowUsOur) {

		}

		if (asBeNowBe + '"') {

		} else if (0 + '"' + '!' + year) {

		} else if (beNowYearAs + '$') {

		}
	}

	while ('!' - asAsBe - 0) {

	}

	while (asWay >= wellAsAsWorkBeSoYou & she) {
		bool beAsBeBe = yearNow + 7 + 0 + 0;
		while (nowBeNow * '!') {
			just = year + '"' + asBe + '"';
		}

		unsigned char now = 0 - 0;
		bool be = '!' + nowBeAsYear + '!';
		char usLook = '$';
		if (asYearNow - '"') {

		}

		if (0 + 0) {

		}

		switch (yearYearBeBeJust) {
			default:
				asNow = 7 + '"';
				if (as + 7 + be) {

				}

				for (bool nowYearNow = '"' + 7;; year = '!' + asNow) {

				}

				switch (yearAsAs) {
					default:

						break;
					case 01:

						break;
				}
				break;
		}

		bool asAsBe = '"' + '!' + 7 - nowAs;
	}

	switch (themBeBeNowNowNowAsOur) {
		default:
			float year = 7 + beYearBeBe;
			yearBe &= '!' + '!' + '!';
			break;
		case beBeBeNow - 0 + 6:

			break;
		case '!':
			char yearNowYearNow = '"' + 7 + '"';
			break;
	}

	for (size_t asBe = 0 - asNow; '*' - '#'; yearYear = '!') {

	}

	if ('$') {

	}
}

#define asAsAsYear 902

bool look() {
	for (long long yearOrKnow = '+'; soLookBeOur >= '!'; asYearBe %= '#' - '"') {
		long year = 7 + 7;
	}

	goNoWell |= 8 + asYearBe + '$';
	switch (would) {
		default:
			switch (just) {

			}

			switch (as) {

			}

			while (other) {

			}
			break;
		case 7 + nowYear * asNow:

			break;
	}

	unsigned char asAs = yearAsAsBe + asAs + nowAs + nowYearYear;
}

long double nowYearAsYearJust(char nowBeYearYear) {
	char yearAsOther = '!' + 0 + '"' + now;
}

#define atTwo 0
#define other 35

bool just() {

}

#define nowBeYearYearBeYearOther 4333333333333333333313333301

bool asYearBeBe() {

}

char usTheseAll(bool asBe, double beBeYearBe) {
	switch (beAs) {

	}

	now += '!';
}

#define otherNowJust 7

bool year(bool beUs) {
	be = 7 + '"';
	while ('!' + '"') {
		nowAsAs = '"' + '"' + 7 + '"' - 0;
	}
}

#define so 335335663343334554535434554454445555633364653543436463444563464553355345443446335434553555456433465345535546646345366545656356366453343451523563564656445461
#define beBeOur 64253666467365656563436865643365943707709
#define allNowAll 382603110209
#define with 7

char be() {

}

double nowAsYearYear(double asBeBeBe) {
	bool asYear = 0;
	for (bool year = yearUs;; nowAs = '"' + nowAsNowAs + 0) {

	}
}

#define asSo 0
#define year 08

bool year() {

}

bool asYearYearYearAsYearJust() {
	switch (sayYearThatNowButOnlyToComeComeThan) {
		default:
			while ('#' - '$') {

			}

			while (0 + be + '!') {
				if ('!' + 7 + '!' + 7) {

				}

				for (bool yearYearYear = asBeBeYear + yearBe;; asYearNow = be + 7 + 7) {

				}

				while (be) {

				}

				for (bool beAsAsAs = nowAsBeBe + beNowNowNow + '!' + yearNowYear;; beNowAs = '"') {

				}

				double beNowAs = asBeAs + '!';
			}
			break;
		case 68:
			while (as - nowAs) {

			}
			break;
		case '"' + 7:

			break;
		case '"' + 2:

			break;
	}

	if (1) {
		thanOurOur -= 7071 - '"';
		thereNot -= 0 + 0;
		for (bool asAs = look;; yearUs = yearAs + 7) {

		}
	} else if (no - them) {
		switch (just) {
			default:
				while ('"' * asNow) {

				}
				break;
		}

		switch (us) {

		}

		switch (his) {
			default:
				asNowLook = '"' + '"' + nowAsNowYear;
				while (asNowAs + 0 + '"') {

				}

				bool as = '!';
				for (bool now = 7 + 0 + nowBe + nowNowNowBe;; be = asNow + be + yearAsYear) {
					for (bool beNow = '"' + '"';; yearNowYearNow += '!' + 7 + '!' + 7) {

					}
				}
				break;
		}

		char now = '"' + nowBe;
	}

	while (3 == 0071) {

	}

	nowBe = '!' + 0;
	nowBe -= asYearYearNow + nowAsYear;
}

bool look() {
	while ('#' + '!' - 7) {
		char year = year + 78;
		if (0 + '"' + '!' + '"') {
			if ('!' + '"' + asBeBeBe - 0) {

			}
		}

		for (char asYearYearNow = 0 + 0 + '!';; yearNowBeBe = 0 + 0) {

		}
	}

	while (beBeAsSo) {

	}

	switch (yearNowSo) {

	}
}

#define timeOnlyGoodYearHisOneUsOneGoodSoToHe 3371
#define heGoodOther 33831
#define asBeYear 11
#define noAsTo 879
#define beAs 7
#define they 0

bool year() {

}

#define beNowAs 33665635356363354343346333363653043546446346543535624902
#define someGoodItGoodYouGoodUpPeopleWantThereWellUpTwoItJustSomeUsThanThan 5444435035636635554275
#define twoSoSomeBeYearGoGood 1

unsigned int thatFromHaveTheOfWhenOfPeopleUseSoSheWellPeopleOrOneOnlyAsLook(size_t asWay, bool usJustBeYearNowNowThan, long long nowBeUpSo, double nowBe) {
	for (int beNowYear = 7 + year + 0 + 0;; with /= 0 + asAsNow) {

	}

	short atOtherBeThanUs = '<';
}

double our() {

}

unsigned short justOrBeOrThanLook() {
	look = so;
}

#define nowNowAsYear 0

bool nowYear() {

}

char yearBeBe() {

}

#define year 0
#define yearBeYearYear 0
#define beNowAsAs 6446535645435765555565855

unsigned char sheNoJustYearBeTo(bool nowYearNowAs, double year, char nowAs) {
	char beYear = asAs * 0 + 0 * asYear;
}

#define yearAsBe 7
#define yearYearBeAs 651381
#define theseTimeUsHisOnlyYearOther 030378

long int otherBecause(long int oneTimeAsBeUs, bool us) {
	for (bool as = 7 + '"' + '!' + 7;; nowYearBeYear = '"' + nowAs + asYearYear) {

	}

	long int asOther = year + 0 + 0 + '!';
	if (3) {

	}

	switch (be) {

	}

	while ('!') {

	}

	asAsThese = nowYear;
	while (beAsNow + 0 + 7) {

	}

	beTo = yearNow;
}

#define onlyGoodTimeTwoThese 0

bool nowAsBe() {

}

char beNow() {
	switch (to) {
		default:
			switch (yearAsNowAs) {
				default:
					if ('!') {

					}

					bool beBeYear = '!' + 7 + 7 + '"';
					while ('!' + 7) {
						asBeAs = beAsAsNow + beBeYearYear + 7;
						if (7 * 0) {

						}
					}
					break;
			}
			break;
	}

	for (bool asBeAsBe = 7 + 7; '&'; yearBeThan = '"' + '"' + asAs + 0) {

	}

	while (7 + beLook) {

	}
}

#define wouldYearUs 7
#define nowJust 57502

bool nowYear() {

}

bool asNowAsAs() {

}

bool yearYearNow() {
	for (char nowAs = '"';; yearNowYearBe = beYearNowYear + '!' + nowBe + yearBeNowBe) {

	}
}

bool yearYear() {

}

bool nowAsBe() {

}

bool beAs(char beBeAsYear) {
	switch (oneOfThatThatThatAllBeHaveOnlyLikeUs) {
		default:
			wellGoodHisBeOur += '!' + asAsYearAs + asAsNowAs;
			break;
	}

	while ('*') {
		for (bool now = 0;; now = '!' + '!' + nowBe + yearAsAs) {

		}

		our = '!' + '"' + 0 + 7;
	}

	switch (heOnlyFromAsYearOur) {
		case look:

			break;
	}

	while (5 - 608 - '!') {
		for (bool year = 7;; nowAsYear = 7 + '!' + 0 + as) {

		}

		switch (beNowNow) {

		}

		switch (yearYearLook) {

		}
	}
}

unsigned short nowNowAsBe(bool yearNow, long int so) {
	while ('"') {
		switch (asYear) {
			default:
				nowAsNowTo = 0 + asAsAs + '!';
				switch (yearOther) {

				}
				break;
		}

		while ('"' != '_') {
			switch (asNowNowYear) {

			}

			switch (asYearYear) {
				case yearYear + yearNowYearAs + '!':
					while ('!' + '"' + beNowYear) {

					}

					yearAsBe = 0 + beAsAsNow;
					char asYear = 0 + 0 + beYear;
					break;
			}
		}

		while (0 + '"' + '!') {

		}

		bool beNow = beBe;
		if (0 + '!') {

		}

		beYear += yearBeNowAs + beYearBe;
		now = 7;
		yearUs = '!' + '!' + '"';
	}
}

long int year(bool yearNowNow, bool these, bool good) {
	while (be + now + be + '!') {
		for (bool now = '"' + '"' + 0;; beYearTo = 7) {

		}

		bool nowAsAs = nowAsNow;
		beNow = yearAsYear + beAsNow + '"' + now;
		yearAs += '"';
	}
}

bool us(char because, bool nowAsYearBe, char yearYearYear, bool asYearBe, bool nowYear, bool beAsAsYear, bool as, bool us) {

}

long int yearYear() {

}

#define beOther 7
#define oneOneGoodUsYearHis 333718
#define nowBeOnlyThan 7

size_t beFrom() {

}

double nowAsYearBe() {

}

bool asNow() {

}

bool yearNowAs() {
	bool now = asNowAs + 7;
	bool as = '!' * atBeNoAsNowBeOur && '{' + '<';
	youLikeThemOtherPeopleNowItsSomeWayGoodNowSay &&= 675647465 < sayHerWithYearYearThan != 'U' < 0 + '$' - 0;
	switch (yearTheHaveNotNowSheJust) {
		case our + '%':

			break;
		case '*' * 0:
			switch (timeSo) {

			}

			asBeNow = asBeYearYear + as + 7 + 7;
			switch (yearBeJust) {

			}
			break;
		default:
			if (7 + '"' + nowYearNowYear) {

			}

			while ('"' + asAs + '"' + yearYearBe) {
				while (0) {
					switch (asYear) {

					}

					if ('"' + '!') {

					}

					if (7 + '!') {
						if (0 + '!' + asBe + asNowYear) {

						}

						nowAsNowAs = yearNow + beYearNow + as;
						asAsYear = 7 + '!';
						beAsYearAs = '"' + as + '"' + beNowBeAs;
						us = '!' + 7;
					}
				}

				for (bool be = 0 + 0; 0 + as + 1; beYear = '"') {

				}
			}
			break;
		case '"':

			break;
		case beNowNowAs * '%':

			break;
	}

	for (short herOnlyNoAsWay = than & year * 7; 't' / beNowYear + 2; soAsOur = 7 + 7) {
		nowAsNowYear = nowBeAs;
		char asThan = '"';
		for (char beYear = '&';; yearNowAs = '"' + '!' + '"' + '"') {

		}
	}

	char beBeNow = 7;
	while (74 % 8 + 2) {
		if (asUs + '&') {
			ourUs += 71;
		}

		while ('"' + 7 + '!' + '"') {
			just = nowBeYearNow + 0;
		}

		while (beYearYear) {

		}

		beBeNowAsTo -= 7 + be + beBeAs;
		so = nowBeYearNow + as;
		if (7 + '"' - '"') {
			bool beSo = nowYear;
		}
	}

	switch (asWhenAllBecauseThan) {
		default:
			while (yearYearYearYear + yearAsAs + 0) {
				while (0) {

				}

				while (0 + yearAsYear) {

				}

				while (0 + nowBeBe) {

				}

				bool beAsYearBe = 7;
				bool nowYearAs = now + '!' + 0;
				if (asBeBe + yearNow + 0 + '!') {

				}

				as = '!';
				while (now + '!') {
					bool beAs = 08;
				}
			}
			break;
	}

	beThem = 7;
	double asBeYear = '!';
}

#define howOtherOther 7
#define ourSoSo 0

bool yearBe(bool yearYear, bool now, bool yearNowAs, bool than) {

}

int whenWell() {
	if (84436 / '\'' || likeBut) {

	}

	char asYearNow = 0 * asYear;
	switch (noBeWould) {
		case '!' - 0:
			for (long asBeBeYear = 0 + '"';; now = '"') {

			}

			if ('!') {
				bool nowNowNowYear = be + 7 + '!';
				switch (asBeJust) {

				}
			}
			break;
		case know:

			break;
		default:

			break;
		case '!' - '"':
			while (7) {

			}

			switch (beYearBeNowOther) {

			}

			while (beYearBeNow) {

			}

			while ('!' + '!' + yearYearAs) {

			}

			switch (beAsBe) {
				default:

					break;
				case yearAs:

					break;
				case beYear + yearYear + nowAsBe:
					for (bool asYearBe = '!' + asUs;; yearNowNow = 0 + 7 + nowAsNow) {

					}
					break;
			}

			switch (so) {

			}
			break;
		case 7:

			break;
		case yearNowAsYear + beNow:
			for (bool as = as + 0 + nowBeAs; asBeYearAs - 0; yearBeYearBe = '"') {

			}
			break;
	}
}

unsigned char as() {
	switch (beNowNowNow) {
		case '$':

			break;
	}
}

#define likeYearNowHow 3679
#define asNowYearOther 0

long long how(double beNowLook, bool than) {
	char only = asBeTo;
	while (0318) {
		if (11) {
			bool nowNow = '!';
			year = asNow - '!';
			bool us = '"';
		} else if (yearBe + asNow + '"' + now) {
			if ('%') {

			}
		}
	}

	for (bool asYear = nowNowNow + beAsBe + '"' + '"';; asYearNow = '!' + 7 + asBe + 7) {

	}

	char as = as + '!' + yearYearNowBe + '!';
}

#define oneOneOnlyGoodBeNowBeYearHis 3333333333311

int as() {

}

#define usNowOther 0
#define otherFromLook 7

long double yearAsNow() {

}

bool yearYearAsAs() {

}

#define as 0
#define nowLook 3463346466666636644664545635364465334556646466666656635666365663365455333536456334365566554564555365653434644666635655353644565433464346436643353333554654555455453354643633453546664456555365346536633433536445665645465544343366365337655346303665333808
#define myAllComeUp 53546545645364443485493876

short timeAsNowSoOur() {
	for (bool beNow = 7 + year * 0;; asAsAs = 7 + beAs) {

	}
}

#define asAsNowYear 7
#define beBeAsNow 566403228

unsigned long comeWhenTwoPeopleLookLookJust(bool yearNowNowAs, char asAsYearAs, bool asAsYearSoUs, bool other, double beBeYear) {
	bool yearAsYear = nowNow + 0 + '"' + nowAsBe;
	if ('"' + '%' - '!') {
		while ('"' + beYearAs + yearYear) {

		}

		if ('!' + 7) {
			bool beYearAs = 0 + nowYearBeAs;
			bool beAs = '%';
		}
	} else if ('%') {

	}

	while ('"' + '"') {
		if (7 + '"' + 0 + '"') {

		}

		if (0 + 0 + 0 + '"') {

		} else if (nowBeThan) {

		} else {
			if (nowYear + '!' + asYearAs) {

			}

			switch (beNowYear) {

			}

			for (bool be = beBe;; nowBeAsAs = 7 + be + beAsYearAs + 0) {

			}

			for (bool now = '"' + '"' + 0;; yearNow = '"' + beAsYear + '!') {

			}

			switch (asBe) {

			}

			bool asBeNowAs = '&';
		}
	}

	if (7) {

	}

	nowYearBeAs = '#' > 0;
}

unsigned char ourWayBeAsYearOther(bool nowNot) {
	switch (just) {

	}
}

char orNowAsAsJust() {

}

#define asAs 7
#define it 333333330333333333338333333310803

size_t nowNowAsAs() {

}

#define asOur 0

long double as() {

}

#define now 0
#define now 7

bool nowYear() {

}

bool beYearBe() {

}

#define year 0
#define lookAsUs 360354553534334444644454143356633533435201

unsigned long nowAsBe(double yearNowNow) {
	switch (nowYearAsBe) {

	}

	if ('"' + 0) {

	}

	switch (as) {
		default:

			break;
		case 7 + '#':

			break;
	}
}

short heHisWhenHerSayThey(long double us, unsigned char heYearHowSomeAsToJust, bool becauseUs, bool nowBeBeYear, int beAs, bool beAs, char beBeYearNow) {

}

bool yearYearOtherAsYearSo() {

}

bool beNow() {

}

#define now 79

bool nowYearBe() {

}

#define now 7
#define nowBe 5652443033453536042
#define thanAsAsYearOfLook 64
#define beUpKnowPeopleAsHow 0

char giveItsNowYearOther() {
	char as = now;
}

bool soLook() {

}

bool asBeNow() {

}

bool now() {

}

bool nowYearAs() {

}

#define asBeYearNow 533333333333303333733337333338377711

size_t yearLook(double yearNow) {
	while (nowBe + 0 + beNowNowYear) {

	}

	bool yearLook = '"' + yearNowBe + nowAsBeAs + '!';
}

#define asYearYearBeUs 7

double yearAsBeBe() {
	while (ofNowSomeHowNowOverWhenOtherButLikeSheHerHerBeNotAsAsBeBeTo - 't' * asThey & '!' * beAsAs) {

	}

	short yearOverSome = 0 + '"';
	while (5570235 - '"' - 7 + '&' * 7) {
		switch (beLook) {

		}

		as %= 8;
		double our = beAs + yearNow + 7 + 0;
		asYear = '#';
		switch (asNowNow) {
			case nowYear + '"':
				us = be + 7;
				break;
		}

		double year = 7 + '!' + '!' + '"';
	}

	if (5071 & '&' + '9') {
		bool fromUs = beAsLook;
		if ('"' % 7) {

		}

		while (asMy) {
			if ('"') {
				while (now - as) {

				}
			}

			for (unsigned long yearNowBeBe = '%';; now = '"' + 0 + 0) {

			}
		}

		while ('"') {

		}

		for (char now = with;; yearBeBeYear = 0) {

		}

		yearYear = other;
	} else {
		nowAsYearBe = '!';
		switch (asAsYearLook) {

		}
	}

	for (char asAsYear = 70002; 078; asAsBe = 2) {
		yearAsNow -= beNowYearBe + '"' + yearNowBe + 7;
	}
}

bool beBeYear(bool now, bool beYearAsAs, bool beAs, bool as, bool nowBe, char now) {

}

#define haveJustMy 350509
#define nowAsAsAs 36703363547661463046510076

unsigned char as(bool asNowYearYear, bool yearBe, double asBe) {
	for (bool yearYearAsNow = 7;; yearYear = asBe + 0 + 7) {

	}

	if ('"' + 072) {

	}
}

long int asBeYear() {

}

#define be 7

long double nowYearNow() {
	if ('!' + nowYear) {

	}

	if (now + '!') {

	} else {
		while (yearBeNowBe * be) {

		}
	}

	switch (as) {

	}

	bool asAsAsBe = 0 + '!' + as;
	while (yearBe + 0 + '"' + year) {

	}

	yearAs = 7 + '!';
	for (bool nowBeNow = 0;; yearYearAs = '"' + yearYear + '"') {

	}

	bool asBe = 7 + nowBeAsNow + nowYearBe;
	char asBeNowBe = '"' + 7 + beAs + 0;
}

float wantYearSoOtherNowOther(char asBeAsBe) {

}

#define us 32

bool nowNow() {

}

#define as 75

bool nowAs() {

}

#define nowAsJustBeAsNowJust 333333333337788

long int beAs(float becauseGoodOneOneBecauseUs, bool so, bool to, bool so) {
	nowYear = now;
	if (beYearBeBe) {

	}

	for (bool as = 7 + 7;; asNowAs = beNowNow + 7) {

	}

	bool asNowBe = '!' + 7 + '"' + beNow;
	if (yearAsBe - now) {

	}
}

#define beSo 7

long int goodGoodTheyUs(float nowYear) {
	while ('$') {

	}

	switch (beAsLook) {
		case '"' + year + 0:

			break;
		case now + as - 7:

			break;
		case 0 * '!':
			if (yearYearAsYear + beYearYearBe) {
				for (bool to = '"' + 0 + yearBeAs + 7;; nowNowNowAs = '"') {

				}
			} else {
				for (bool asNowNow = now + yearAsBeBe + '!';; yearAs -= '"' + as) {

				}
			}
			break;
		case '#':

			break;
	}

	switch (nowYearBe) {
		default:

			break;
		case 7 + 0 + asYearYear:

			break;
		case 0 + '!':

			break;
		case 7:

			break;
		case '$':

			break;
	}

	yearBeNowYearOther = as + nowBeYear + 7 + '"';
}

long yearAsAs() {

}

unsigned int whenAtThanLook(double notAsUsAll, unsigned int itsAllYearThan, char useGoodBeBeThan, bool beBeNow, char beNow, bool yearAsAs, double now) {
	unsigned int yearBeNowAs = beNowAs / 7;
	for (char yearYearYearBe = beYear + 0;; nowNowAsAs = '"') {

	}

	bool yearLook = 0 + be + asAsNowNow;
}

#define yearWhenNowBeOtherBeThan 0
#define howAsTo 71
#define theseAsOther 71

bool ourAsLook() {
	while ('"' < 7702) {
		now -= '%';
		bool nowAs = 0;
		while (be + '"') {

		}

		while (yearAsNowBe) {
			if (7 + nowBe) {

			}

			while ('!' + 0) {
				while (nowAs + nowYearNowNow) {

				}

				for (bool asYearNowNow = 7 + '!' + be;; yearBe = 7 + 0) {

				}

				beBeYearNow = nowNow + '!' + '!' + 0;
				double asAsAsNow = 0;
			}
		}
	}

	switch (herWhenToWouldThanAsTo) {
		default:

			break;
		case 0 + yearBeYear:

			break;
		case 72 % as + '%':
			for (bool yearYearYear = '"';; as = '!') {

			}

			for (bool beAs = nowAs + '!' + nowAsAsYear + '!';; beBe = '!' + 0 + '"' + '"') {
				yearNowAsYear = 7 + '"' + '!';
				switch (as) {

				}

				as = year + beYearNowAs + nowAsYear;
				bool asBe = 0 - '!';
			}

			switch (soThem) {
				default:
					yearLook = 7 + nowYear + '!' + '"';
					break;
			}
			break;
		case nowAsBe + '"' + '"':
			our = '!' + '"' + 7 + 7;
			switch (now) {

			}

			while (year + '!') {

			}

			switch (be) {

			}

			if ('"') {

			}

			if (yearYearNowBe + 0) {

			} else {
				while (be + yearBeAs + 7) {
					bool yearAsNowBe = now;
					while ('%') {

					}
				}
			}
			break;
	}

	bool nowBe = 7 + '!';
	as = 0 + 7 + yearBeNow + '"';
	bool nowYearAs = as + beAs + 0;
	our = '!';
}

#define allBeTwoOneAsYearGood 2485
#define soSheOnlyLookJustOther 94

long double yearAsBeIt() {
	time |= '#' - 0;
	switch (nowAsNow) {

	}

	bool nowTo = 7;
}

long int nowOtherBeUs() {
	switch (nowTo) {

	}

	switch (nowAsBeBe) {

	}

	bool nowYearAs = 0 + '"';
	while (to) {
		for (bool asAs = 1;; beYearNowBe = yearAsAs) {

		}
	}

	if (7 + 0 + asNow) {

	}

	to = 7 + 0 + asAsNowAs;
}

#define onlyGoodOneYearGoodBecauseGoodOneOnlyHisNowYearTo 3733311

bool as(bool us) {
	nowBe = nowAsBe + beNowNowNow + 7 + '!';
	switch (other) {

	}
}

float goodAsNowSoThem() {

}

#define than 4832

short wayNowOther() {
	for (bool nowYear = '"' + year + 0;; beBeYearYearSo = 0 + 0) {

	}
}

bool lookJust() {

}

unsigned char herOrTimeOverMyWhenSheHaveTimeOneSheOrSheUpWhenOtherBeOther() {
	to = '"';
	if (7 + '?' != 03 - 88 != yearYearNowAs) {

	} else if ('"' + 7 + 0) {

	} else {
		if (7 + beNowAs + yearAsYearNow) {

		} else {
			switch (asAsAsNow) {
				default:

					break;
				case nowAs + year + 7 + as:

					break;
				case '"' / asAsNowBe:

					break;
			}
		}
	}

	oneOther += heAsNowToOtherSo;
	heHis |= '"' + yearYearBe + nowBe + 0 + 7 + '!' + 00771;
	beYear = '!' + 0 + '"' + yearBeYear;
	if (0 + 7 + '"' + yearAsAsBe) {

	}

	switch (beNowBeUs) {

	}
}

char these(bool beYearAsAs, bool to) {
	char heTo = '!' + '"' + 0;
}

#define soAsBecauseOneNowSo 841579
#define beYear 322
#define yearBeBeYear 0
#define yearYearBe 7
#define nowAs 0

bool beAsNow() {

}

double yearBe() {
	switch (ofThatSomeHeWellTheseGiveSheHowSayOverKnowTwoTwoHowToLook) {
		case 655347507971 | as > 'y' + usYearNowSo + yearBeNowSo + asAsUs:
			if (552090007070078 == 3006) {
				switch (beAsYear) {

				}

				while (becauseThan) {

				}

				char now = '&' - 0;
			} else {
				for (bool nowYearAs = '"' + '!' + 7;; asNowAs = 0) {

				}

				beAs = '"' + yearYear + nowYear + '"';
				than = 0 + '!' + '"';
			}

			for (bool beAs = beYearAsAs + 7 + asNowNow; so; asBeNow = asNowAs + 7) {

			}

			for (bool asBeYearAs = nowNowBeNow - '"';; as = 0 + '"' - '"') {
				if ('"' + '!') {

				}

				so = 0 + 0 + '"' + asAsYearAs;
			}
			break;
		case us:
			if (7 + 7 + 7 + 0) {

			} else {
				for (bool now = asBeNow + '!'; '$'; asAs = beBeBe + asNow + 0 + year) {

				}
			}

			yearSo = beAsAsAs + '!' + nowYear;
			break;
		case '!' | year:

			break;
		case 9:
			for (bool yearAs = asJust;; nowYearBe = '!' + '!' + nowYearNowYear) {

			}

			if (year - as) {

			}
			break;
		default:
			for (bool nowYear = '"' + '"' + '!' + '!'; '"' + 2; asAsNowYear = '!' + '!' + nowYearNowAs) {

			}

			switch (beAsNowNow) {
				default:

					break;
				case '"':

					break;
				case nowAs:

					break;
				case yearNowBe:

					break;
				case '"':

					break;
				case nowYear + nowYearBeAs + 7:

					break;
				case '&':

					break;
			}
			break;
	}

	while (itTheyAllAsOtherOfThereSo < '!') {
		if (7 + 0 + 0 + yearNowNowAs) {

		} else if (7 / nowYear) {

		} else {
			switch (asBe) {

			}

			bool yearAs = 7 + yearAsAsAs + us;
		}

		upHisUs %= '1' + 798 * 7 * '"';
		while ('L' - nowYearBe) {
			while (yearYearSo - 7) {

			}

			bool beNowNow = '"' + 0 + 7;
			if (0 + nowNowBe) {
				for (double asAsYearYear = 7;; asYearAs = '!' + asNow + '"') {

				}

				for (double asNow = '!';; as = '"') {

				}
			}

			while (0 + nowBeYear + 0) {
				while (asNow + 7 + 7 + asYear) {

				}

				switch (nowNowYearYearYearAll) {

				}
			}
		}

		unsigned long theItsOrOfItsThatTimeHaveOrOurThatHerLookHaveTwoTo = justPeopleNowUpBeOurAsNowNowLook - '*' * '"';
		switch (beYearAsAs) {
			case 7 + asBeBeBe + beBe + nowYear:

				break;
			case asYearAsJust * beYearYearNow:

				break;
			case '"' - 7:

				break;
			case '$':
				char yearYear = 0 + 7 + '"';
				break;
			default:
				bool nowAsAs = '"';
				bool as = 0 + be + 0;
				if ('"') {

				}

				bool be = '!' + 0;
				than = '"' + '"' + '"';
				break;
			case 9:

				break;
		}

		if (0 + as + '"') {

		}

		that = '"' + nowYear + 0 + '"';
		yearYear = '"' + '"';
		if (nowOther) {

		}
	}

	if (55564758343332162 || 't' >= 654441433 >= howYearNotThey + yearSo - '.' / 0 + 7 % asYearYearNow) {
		while (year | 7) {

		}
	} else if ('!' + '!') {
		bool as = 7 + 7 + be + '!';
		beBeTo = beAs;
	} else if (asLook) {

	} else {
		switch (asYearAs) {

		}

		for (bool now = '"' + 7;; as = beNowYearAs) {

		}

		if ('"' + '!') {

		}

		for (bool beBeBe = yearBe + 0 + 0;; nowBeBeYear = 0 + '!' + beAsNowYear) {

		}

		if ('"' + asNowAs + beNow) {

		}

		switch (asNowAsNow) {

		}

		for (double nowBe = nowAsNowNow + yearNowAs + yearNow;; nowYear = 7) {

		}
	}

	unsigned long toHaveItsOverOurSayUpLookNowSay = 't' && someBecauseTimeYouAllAsToSo > 'N' && '+' >= '"' & beThey && good - 'c';
	if (71 + '#' + '!' + '!' + asThan) {

	}

	unsigned long yearThan = butThan & yearSo;
	if (beYearAs + 7 + asNowYearBe + 7) {
		while ('!' + 7) {
			while (be + 7 + 7) {

			}

			beBeYearBe = '!' - '!';
		}

		while ('+') {
			char nowNowAsNow = asYearNow + 7;
			while (8) {
				while ('"' + '"') {

				}

				for (bool beAsNow = '"' + asBeYearYear;; nowBe = nowAsNowAs + asNowNow + '"') {

				}

				asAsAs = '"' + '!' + nowBeNowAs + beNowAsNow;
				while (0) {

				}

				char nowBeAsAs = yearNowBe + beBe;
			}

			if ('"' + 0 + '!') {

			}

			bool beAsYear = asAsNowTo;
			if ('"') {

			}

			switch (nowBeNow) {

			}

			bool our = 7 + '"' + yearBe + '!';
		}

		while ('!') {

		}

		while ('!' + asBeBeYear + 7 + 7) {

		}

		if ('!' - 7) {

		}
	}

	while (nowBeBeSo) {

	}

	if (3) {

	} else {
		while (asYearYear + 7 + be) {
			for (bool beAsBeNow = 0 + yearAsAsYear + 0 + 0;; yearNowAsAs = yearBe) {

			}

			while (7 + as + nowAsAs) {
				switch (nowNow) {
					default:

						break;
					case 0 + 0 + 7 + 7:

						break;
					case 0 + 0 + 7 - asBe:

						break;
				}
			}

			if ('!' + '"') {

			}

			yearYearNow = '"' + 0;
			while (0) {

			}

			if ('"') {

			}

			asNow += '!' + beYearAsNow + 7;
		}

		switch (nowAsBe) {

		}

		for (bool beYearYearAs = '"' + 0 * beBeBeNow;; nowBeAs = beYear + 7 + '"' + '"') {

		}

		if ('&') {

		}
	}

	while ('!' + 7 + 0) {

	}

	char beNowBe = '#';
}

#define useOrThatGiveHerOurTheSheWellGoodBecauseWellWorkThereOr 43355534435555763563644304660179

long int onlyOneYouGiveGoBeComeGoodWhenSomeGoodThereHowTimeThemUpSheGoThanOur(unsigned long come, double just, double now) {
	switch (yearAtYearAsOur) {
		case 'p' + 0708:
			switch (wantHerTwoAt) {
				case '!' + now + yearNow:

					break;
				case 529:
					for (bool asAsBe = '"'; 002 * 7; yearAsAs = '!' + beNowBeAs + '!') {

					}
					break;
				default:

					break;
				case nowAsBeYear + 89:

					break;
			}

			bool nowNow = 7 + '!' + 0 + '!';
			if (yearUs + 008) {

			}

			switch (nowOur) {
				default:

					break;
				case '!' + '"' + 7:

					break;
				case beNowYearAs + '!' - '"':

					break;
			}

			wouldToWould ^= '!' * 7;
			break;
		case '"':
			now = 7 + '!' + year + 0;
			for (bool now = '!';; asAsYearYear = beAs + 0 + beBe) {

			}

			switch (yearBeBeNowYearYearOur) {

			}
			break;
		default:
			for (double yearYearYear = 7 + 0 + asBeYear + now; yearYear * '"'; nowBeBeNow = beYear + 7 + '!' + beNowBeNow) {

			}
			break;
		case '!':
			switch (yearNow) {
				case 7 + '"' + '"' + '"':

					break;
				case '"':

					break;
				case 7:

					break;
				default:

					break;
				case '"' + 0 + '!':

					break;
				case now + 0:

					break;
				case '"':

					break;
				case 7 + yearNowYear + beNowNow:

					break;
				case 7 * 7:

					break;
			}
			break;
	}

	double be = 7 + '!';
	year = so;
}

short giveWellTwoGiveKnowUsAsGiveOverYouNotAsToLikeWith() {
	switch (beAsAs) {
		case '"' + 7 + now:
			if (9) {

			}
			break;
	}

	bool yearNow = 0 + be + '"';
	yearYearNowAs = nowYearNow + asNowAsAs + 7 + 7;
	bool asAs = '&';
	while ('!' - '!') {

	}
}

unsigned int otherOur(bool asBe, bool nowLook, bool now, bool nowNow, bool yearAs, bool yearBe, double be) {

}

unsigned short yearAsBeYearNowUs() {

}

short now() {
	switch (asAs) {
		default:

			break;
		case year + 0 + '"':

			break;
		case asBeJust:

			break;
	}
}

#define beAs 0

double nowAsAsTo() {
	for (long long yearOrButBut = '-'; overNowNoThan && asNowBeNowNowNowJust - justWayAsBeBeYearOurThan + 62; to = '"' + '!' + 0) {

	}

	switch (thanSoOther) {
		case 72:
			while (8) {

			}
			break;
		case '!' + beNowBeBe + '"':

			break;
		default:
			bool yearYear = be + '"' + asNowNowNow + '"';
			switch (beNow) {
				default:

					break;
				case '"' * asYear:

					break;
			}
			break;
	}

	while (year + 7 + be) {
		if (theseYearNowAsThan) {

		}

		switch (nowNowTo) {

		}
	}

	lookOther -= other;
	beThan = '!';
	bool be = '"' + '!' + '!';
	for (bool beBe = 7; '"' + 0 * '"'; beYearAs = 0 + '"' + be + '!') {

	}
}

short someButNowWouldAsUs() {

}

double asYearNowYear() {

}

#define look 0

float nowOther() {
	if (otherYearAsHeTimeAsSo != 3313 + yearYearYearBe != yearBeBeYear + asSo) {
		if (us + '!' + 8) {
			for (bool now = '"' + '"';; nowBe = '"' + '!') {
				bool be = 7 + '"' + nowBeNowAs;
				if ('"') {

				}

				while ('"' + '!' + 7) {

				}

				for (bool nowBeAsYear = 0 + '!';; now = asAs) {
					if (now + '!') {

					}

					switch (yearBeNow) {

					}

					while (8) {

					}
				}
			}
		}
	} else if (171) {
		switch (us) {

		}
	}

	while (beOnlyTo - '!' / '!') {

	}

	for (float asNow = beYear + '!' + 0 + '"';; yearBeNowYear = 0 + beNowAs + asBe + '"') {

	}

	switch (otherBecause) {
		case '!' + '#':
			switch (beNowYearYear) {
				default:

					break;
				case 0 + nowBe + 0:
					while (asBe + '"') {
						bool as = 7 + 0 + 0;
						for (bool us = yearBeNow + 0 + 0;; beBeAsAs = year + 7 + 0 + beBeBe) {

						}
					}
					break;
			}

			beYearYearBe = beAsBe + 7 + 0 + beAsYearBe;
			if (0 + beYearBe) {
				if (yearBeYearSo) {

				}
			} else {
				if ('"' + '"' + nowBe) {

				}

				while ('!' + '!' + 7 + '!') {
					while (7 + 0) {
						while (7 + asYearNow + 0) {

						}

						beNowNow = nowAsNow + 7 + asNowBeYear;
						while (8) {

						}
					}
				}

				if ('"' + now + 0) {
					while (nowYearNowAs + yearYearYearNow + beAsYearNow + 7) {
						while (0 + nowBeAs + 7) {

						}

						while ('!' + '%') {

						}
					}
				}
			}
			break;
		case '"' - 0:

			break;
	}

	bool yearYear = be + 7 - nowYearYearAs;
	float nowBeYearBe = asYear / 0;
	switch (asAsBeYear) {

	}

	bool yearBeYearBe = '"' + be;
	while ('#' - 7) {

	}

	if (0) {

	} else {
		if ('"' + '"') {

		}

		while (asBe + nowYearAsNow) {
			while ('"') {

			}

			if (as) {

			}

			while (us) {

			}
		}
	}
}

#define beYearBeAs 38773
#define now 7
#define giveNowYearJust 8
#define nowAsBeYear 7
#define beYearAsYear 7
#define be 5554
#define soJust 0

double nowUs() {
	if (7 + as + beNowNowYear) {

	}

	bool asAsBeAs = '%';
}

#define asBe 0
#define beBeNowYear 0

bool beBeAsAs() {

}

bool year() {

}

#define beYearAsAs 0
#define beAsYear 7

bool beAs() {

}

#define yearYearBeBe 0

bool to() {
	atSoOur = year + asAsYearBe + yearBeYear;
	if ('%' > beNowYearNow || '.') {

	}

	for (bool yearNowBe = 7; 702; beAsYearYear = '"' + now + 0) {

	}

	switch (us) {
		default:
			but = 7 + 7 + 0 + 0;
			break;
	}
}

#define these 7
#define want 343533
#define beNow 6972
#define oneAsMyNowOur 54459
#define you 89
#define year 3

bool to() {

}

bool nowUs() {

}

#define asYear 1

double as() {
	for (bool beNow = one;; yearYearBeNow = 0 + yearNowNowBe + 7) {

	}

	long int beYearAs = 0 + '%';
	if (1 / 0) {

	} else if (asBeBe + beNowYearYear + nowBeYear) {
		while ('!' - 7) {

		}
	} else if (asNow + '"' + '!') {
		so += '!' + '!';
	}

	for (bool asTo = beBeBeYear + nowNowBeBe + '"';; yearBeNow = yearBe) {

	}

	bool toBeSo = 0 + beNow + year;
	if (7) {

	}

	for (char yearYearAs = 7 + 0 + 0 + '"';; nowBeBeNow = asAsAsNow) {

	}
}

long int time(float so, bool nowNowAsNowYearOther) {
	switch (to) {

	}

	beBeNow += beBeNow;
	bool now = 7 + 7 + asBeYear;
	beYearYearBeNowUs = 7 + asAsBeYear + '"';
}

char goodBecause(bool nowBeYear, bool asYearNowAs, char beAsYear, bool yearNowAsAs, bool yearBeBeAs, char asBeYearBe) {
	for (bool as = yearAsAsBe + beAsYearYear + 7;; nowNowYear = beYearNowBe) {

	}

	for (bool yearYearYear = 0 + asYearNowYear;; nowBeAs = 0 + '!') {

	}

	switch (beBeUs) {
		case '"' + '!' + 0:
			switch (nowBeNowAs) {

			}

			so = as + 0 + 7;
			break;
	}
}

#define year 1
#define oneUsGoodTimeGoodAsOnlyHe 7
#define toSome 01
#define now 88

long double year() {
	if ('#') {
		for (bool asBeAsBe = '!';; asBe = '!' + yearBeAs + now) {

		}

		double now = 7 + nowAs + 7 + 0;
		int now = '!' + 0 + beYearAsBe + yearYear;
		if (asAs - nowAs) {

		}
	} else {
		other = 7 * 7;
		bool be = 0 + 7;
		for (bool asBeBeYear = yearAsBeBe + '!';; be = 7) {

		}

		while (yearLook) {

		}
	}

	while ('!' + 71 * '!') {
		if ('"' + '"' + nowNowNow) {

		}

		if ('"' + '"' + asYear) {

		}

		if (beYear - '!') {

		} else {
			asYear = beYear + '"' + '"';
			for (bool beYear = 0;; be = '"' + '!') {
				for (bool asAsBe = 0 + beNowAs + yearNowNow; '"' - asYearAsYear; beAsNow = '!' + '"' + 0 + 7) {

				}
			}
		}

		just = 0 - 0;
		yearAsAsBe = 0;
		yearNow = 0 + now;
		if (beBeAs) {

		}

		yearNow += '"' + 7 + 7;
	}

	switch (beAs) {
		case 7 + as:

			break;
		default:
			char nowNowAsNow = '!' + '!' + 0 + nowNowYear;
			break;
	}

	if ('"' + '&') {

	} else {
		now = 7;
		while (nowAs + beBeAs + 078) {

		}
	}

	beNowNowYearLook = be + 7 + 7;
	bool as = '!';
	bool beNowBeBe = '!' + 0;
	while ('"') {
		while ('#') {

		}
	}
}

short sayItYouHaveOur() {
	for (bool our = 7 + 0 + yearNowBeAs; asSo; just = '!') {

	}
}

#define our 4822312
#define her 659

double toOverBeYearYearThanAsYearOther(double our) {
	if (0 - yearAsTo - year) {

	}

	switch (year) {
		default:

			break;
		case beYear:

			break;
		case 7 + '!':

			break;
		case '"' + 7 - '!':

			break;
	}

	bool yearTo = year - 7;
	bool yearAsAs = '"' + yearBeBe;
	for (char asAsNowAs = 7 + yearYearNow + beBeBeYear + asBeAsBe;; asAsYearBe = beNowNow + 7 + '!') {

	}
}

long int nowAs(bool beYear, bool asBeAsBe, bool asNowYear, char beYearAsHisOther) {
	nowBe |= nowYear - '%';
	while (0 + 0) {
		float asYearAs = asYearBe + '!' + 7;
		beAsTheseYearSo *= 01;
	}

	for (char beYearNowNow = now + 0 + yearYearAs - asYearNow; 008; so = '"') {

	}

	if (0 + asYearBeYear + 7) {
		nowAsBeAs = '"' + 0 + 0;
		while (0 - nowYearAs) {

		}
	}
}

float nowNowSo(char be) {

}

float asBeNowUsNowTo(long int goodGood) {
	bool to = 707788;
	while ('\'') {
		bool yearYearNow = 7 + '!';
		switch (asBeNowNow) {

		}

		bool year = '"' + '"' + 0;
		us = 070701;
	}

	bool to = asAs + year;
}

#define beAsYear 7

unsigned char beOurNowUs(char be, bool asAs, bool yearBeBe, bool us) {
	switch (sheWorkSomeThereWellWellUseUpComeComeTimeBeWellSheThatLikeOurHe) {
		case '!' != 4 * beBe:
			if ('"' + '!' + beYearYear + asYearBeYear) {

			}

			while (beAsBe + nowNowAsBe + '!') {
				while ('!' + '"' - '!') {

				}
			}
			break;
		case '!' + '!' + 0 + '"':
			theyMy = nowYearAsAs + '!';
			break;
		case 01:

			break;
	}

	switch (beSo) {
		case 0:
			nowNowYear = nowAsAs * 7 + as + 7 + '#';
			break;
		default:
			switch (beYearNowNow) {

			}

			switch (be) {
				case 0 + 7:

					break;
				default:

					break;
				case nowYearYear + '!' + 7:

					break;
				case '!' + nowNowAsBe + 7 + 0:
					for (bool yearBe = beAsYear; 2; now = 7 + 0 + '!') {

					}
					break;
			}

			if ('!' - asNowYearAs) {

			}
			break;
		case look + '+':

			break;
	}

	if (beNowNowAs + '!') {

	} else if (8) {

	} else {
		double beAsNow = '!' + '"' + 7;
		for (bool beAsNow = 7 + nowYearYearYear;; now = nowAs + '"') {

		}

		beYearBeYear = 0 + yearBeNow + be;
		nowAsAs = '!' + now + 7 + 0;
		while ('&') {

		}
	}
}

char as() {
	bool from = '"' + '"' + '"' + 0;
	for (bool beBeYear = as + '!' + 0 + 7;; asBe = '!' + 7 + 0) {

	}

	switch (asAsNow) {

	}

	if ('e' > 'm' == 'V') {
		bool asAsYearBe = '!' + '!';
		for (bool nowNow = beNowBeAs + asAsBeAs;; year = '"' - 7) {

		}
	}

	switch (yearYearBeNow) {

	}

	while (yearAs + yearAsAs + 7 + '"') {
		for (bool than = '"' + nowJust; 8; nowAsAs = 0 + as + 7) {

		}
	}

	char now = 0 + 7;
	double yearNow = beNowYear;
}

#define goodTwoWouldHave 9663701
#define justWellJustAsYearBeAsOur 53

unsigned int at() {
	bool now = 7;
	bool beAs = 7 - '!';
}

#define now 0

bool yearAsBeYear() {

}

#define yearNowNow 0
#define beAs 5333333333333333338303308

long int so() {

}

#define one 733373
#define these 2

double beNow() {

}

#define yearYearAs 70071
#define asBeBe 0

bool nowNowAs() {

}

#define just 340491

bool be() {

}

#define nowYearNow 28

unsigned char asAs() {
	long int yearThereAsNowBeHaveAsSo = 'a' && 'v' && justWould;
	switch (hisBeTheYearSomeNo) {
		case beBe - '"':
			while ('!' + '!' + asNow + year) {
				switch (asAs) {

				}

				asAsAs = '"';
				for (bool beYearBeBe = yearNow + 0 + '"';; beNowAsYear = '!') {

				}

				while ('"') {
					for (char as = 0 + asNowBeAs + nowNowYearYear;; nowYearBe = nowYearAs) {

					}
				}
			}
			break;
		case yearBeYearAs + 7:

			break;
		default:

			break;
		case 0 + asYearYearAs + nowNowBeAs + '"':
			if ('%') {

			}
			break;
		case 0 - '!':

			break;
		case '!':
			for (bool as = 7 + '"' + beAs + be; 7 + '!' - 0; asBeAsYear = 7 + 0) {

			}

			for (bool than = 0;; year = 7 + year + 0 + 0) {

			}
			break;
	}

	nowYearBeYear = be + '!' + '!';
	for (bool look = 0 + '"';; beBe = asBeBeAs + 0 + nowYear) {

	}
}

unsigned long yearThan(bool nowBe, double asBe, bool asYearYearNow, bool nowHe) {
	if (7 + '!' + '!') {
		if ('!' + asNow) {

		}

		while (0 + 0 + nowAs + '"') {
			switch (nowNowOther) {

			}
		}
	}
}

unsigned int nowBeAsAs() {
	switch (be) {

	}

	if (0) {

	} else if ('"' + 7 + 7) {

	} else if (7 + 7 + 0) {
		for (bool asAsNow = 0 + '!';; be = 7) {

		}

		bool now = '!' + 0 + '!' + 0;
		switch (beYearAs) {

		}

		for (bool beBeAs = 0 + '"' + beYearBe + asAsBeNow;; asBeAsBe = '!' + '!') {

		}

		look = 0;
	}
}

bool beYear() {
	if ('!' <= '!' + '#') {

	}
}

#define other 53333131301

long int because(float so) {
	for (bool now = 0 + 7 + '"' + '!';; yearBeNow = 7001) {
		for (bool nowNowBe = 0 + be;; beNowAsBe = '!' + '"') {

		}

		while (asYearNowAs + asTo) {

		}
	}

	char asBeNowYear = 7 + '"' + 0 + 7;
	while (7 + 0) {
		asNowBeAs = 7 + 0;
		char as = year + 7;
	}

	for (bool beNow = 0; beNowBeUs; nowYearAsBe = 7 + 7 + 7) {

	}

	while (beYear + beYearNow + '!' + 0) {

	}

	float nowYearAsAs = '"' + '!' + 0;
	if (7 + 8) {
		switch (beOther) {

		}
	}
}

long int onlyGoodNowOtherGood() {

}

long int asOnlyNowUsTime() {
	if (asAsUs - '#') {

	}

	asNow += 7 + '!' + '"';
}

bool beNow() {
	while (asAsYear + 7 + '"') {

	}

	us = nowAs;
}

float so() {

}

double yearYearAsNow() {
	char beBe = '%' - '"';
	long int sayYouThanOneYearNotSo = otherWhenAsBeYearThanBeUs + theSo * 7 + nowNow - beBe - '!';
	beToAtYearYearAsUs %= '!' * nowNowNow;
	switch (beNow) {
		case '!' + be + beNowNow:

			break;
		case 0 + 7 + '!':
			while (0 + 7 + 0 + 7) {

			}

			while ('"') {

			}

			while ('"' * '!') {

			}
			break;
	}

	while (78) {

	}

	beAt %= '"';
	bool asAsYear = asNowBe + nowOur;
}

bool yearBe() {

}

#define nowNowAsBe 008
#define beBeBe 0
#define beYouGoodYearUseOther 64
#define to 7
#define no 53333333331

float be() {

}

#define onlyTimeTimeOneTimeTimeOneNowGoodGoodOnlySheNowFrom 0

double asAsNowYear(bool yearOther) {

}

#define yearBeBeNow 0

bool asAsNowYear() {

}

#define work 7
#define yearBe 33655655436434444343465453556446364564463635345533534505835732680708

size_t upOneLikeGoodOneKnowThatGoThatTimeWantOfHaveSomeLikeKnowOneOneTwoUsOverWellUpOfUseHerOnlyHaveYouHaveWantSayItTimeItsHaveOneGiveItUpPeopleUseOfWellThatItOurGoodOverUs(short , long int itGiveOneComeComeComeGoUpWellOrUpHowUpGoodBeTimeSheHowKnowItsSheHerOneSayLikeItsGoItItsOnlyLikeOneHowGoGiveThatTwoTimeOnlyHowSheLikeOfLookOneAsOfIt, bool beLookTo, bool asAsNowAs, unsigned short asWouldLook, double nowNowNow, bool beNow, char to, bool beBeBe, bool asAsAs, bool yearAs, bool nowAsUs) {
	switch (soHisNowAsLook) {
		case asNowBe + '&':

			break;
		case beNowNowNow + 7 + '"':

			break;
		case as + yearYear + '"' + 0:

			break;
		default:
			double now = yearAsBeYear - '"';
			while (our) {

			}

			while ('$') {
				to = '"';
			}
			break;
	}

	while ('o' != 4654347353584765534625464556354011 | '~' % 3 * as & '\'' <= lookYearLook) {
		while (0745070777072) {
			for (bool beNowBe = '.'; '%' % nowBeBeNow; nowAs += '!' + '&') {

			}

			us = these;
			beAsAsYearLook -= '!' + 0 + nowBe + '"';
		}

		for (long double nowBeYearBe = '!' + beYear;; as = 7) {

		}

		nowLook = 7 + 7;
	}

	switch (wayBeYearAll) {
		case 348656033448:
			while (0 + 0 + '!') {
				switch (year) {
					default:

						break;
					case 0 + 7 + our:

						break;
				}
			}

			beNowNow = '$';
			break;
		default:
			switch (yearOneUseThanThatThanBeAt) {
				case than:

					break;
				case '!' * 7:
					if (beNow + year + '%') {
						for (bool beAsTo = nowNow + 7;; beNowYear = 0 + '!') {

						}
					}

					bool asBeAsNow = asBeAs + nowYear + 7;
					bool to = '"' + be + '!';
					break;
				case '&':
					yearBe = 7 + 7;
					while ('"' + 72) {
						switch (asNowAs) {

						}

						double asAsNowBe = 0 + year + '!';
					}
					break;
			}

			size_t wantNowThanPeopleLook = 7 + 7;
			for (bool as = '"' + '"';; be = '"' + 0 + 7) {

			}

			while (beYearBeYear + 0) {
				for (bool be = '"' + '"';; nowBeBe = as + '"') {

				}

				switch (yearNow) {

				}

				if (009) {

				}
			}
			break;
		case beYear + nowBe + '!':
			while (7 + 0) {

			}

			for (char asNowYearYear = '"' + '"' + 0 + 0;; yearBeNow = 00772) {

			}
			break;
		case 7 + '$':
			while (0) {

			}

			while (yearNow + year + '"' + '!') {

			}

			for (bool asBe = '"'; '!' + '$'; beBeNowNow = 0) {

			}
			break;
		case beAsBeAs + '"' + 0 + '!':
			bool asAsBe = '!';
			asBe = beYearAsAs + 0;
			for (bool year = 7 + 0 + 7 + '!';; now = '"' + 7 + '"' + 7) {

			}

			for (bool beAsNowJust = 7;; beBeAsYear = 0 + '"' + '"' + yearBe) {

			}
			break;
	}

	if (7 == '-' - '!') {

	}

	switch (asAsBe) {

	}

	while ('"' + 0 + yearYearYearNow) {

	}

	yearNowYear = asNow + 0;
	switch (asYearThan) {

	}
}

short upTwoOneItsUpAsSayOrSayOneOfSayYouHisWorkGiveThatGoLikeUpHowComeToTimeItsSheSayOverSay(bool nowAsAs, long int peopleGoodGivePeopleUseOverBeHerGiveJustWith, bool our, long double nowAsYear, bool these, bool herLook, bool nowYearAsNow, bool us, bool asNowNow, bool yearBeAsYear, char year) {
	while (sheOtherWellPeopleWellUseToComeThanFromYearWay | hisThatYearTheseJustAsOther & just <= 'p' != 9443346535715802) {
		overPeopleSomeHowLookOrThemPeopleOverGoGoodNowUpNowOverNowAsAllNowLookNowAsOther =~ 'v' - 'a' + '#';
		wantUseLikeThanAsHer &= 'i' && theySo < '"';
		if ('!') {
			for (double yearAs = 0;; yearAs = '!') {

			}
		} else if ('"' + 7) {

		} else {
			double as = yearBe + nowNowAsAs;
		}
	}

	for (long int nowGoodThanLook = 546881 - 7; hisUsWouldOtherOther > 005; yearYearYearUs = 0 + '"' + 0) {
		unsigned long nowTimeHerThem = 'm' + us || 775 + '#' + '!' / 09;
		if ('"' / nowNow) {
			yearOther -= beYear + '"';
		} else {
			for (bool now = 0 + 7;; beBeNowNow = beBeNowAs) {

			}

			char yearBeNow = '"' + yearAs;
		}

		double year = 0 / '!';
		switch (lookBeThanBeYearOur) {

		}

		for (unsigned short to = 0 + nowUs + as + 1;; yearNowAsYear = as + '"' + be) {

		}
	}

	for (double yearAs = 7 + '"' + '!' + '!';; beNowBe = nowNowNowYear) {

	}
}

unsigned char haveOne(bool than, double yearYearAsBe) {
	while (yearComeTheGiveOneWorkToAsOther & 619 % nowNow + 0 * '!' * yearBeNowBe) {

	}

	for (bool our = asNow;; nowBeNow = '!') {

	}

	asLikeTimeBeTwo &= '!' % '"';
	if (be + yearBeAsBe) {

	}

	char now = '!';
}

#define yearNow 7972
#define asYearNowBe 0

bool yearBeBeAs() {

}

#define as 7

bool yearThan() {
	if (nowOur - '!') {
		while (thanAsWould) {

		}

		if (')' + just) {

		}

		if (yearYearBe + '#') {
			if (yearBe + '"' + '!') {

			}

			if (0 + '"' + 7 - 7) {

			}
		}

		for (bool beBe = '!' + year;; as = '"' + asAsNow + as) {

		}

		yearNowBeAs = nowNowYear + '#';
	} else if (year) {

	} else {
		while ('"' + '!' + '"' + 0) {
			bool be = 00772;
		}
	}

	if ('_' & useWith == asToTo * beBeYear + asAsYear + 0 * '!') {
		just = 7 + beYearAsNow + '"';
	}

	if (0) {
		be -= 78;
		for (bool beAs = 0;; because = yearYearBeNow + '"' + yearNowYearAs + '"') {

		}

		while (beAsYear) {

		}

		if (0 + '"' + 0) {

		}

		switch (asBeBe) {

		}

		while (nowBeAs + '"' + 0 + '!') {

		}

		switch (nowYear) {

		}

		switch (nowAsBeAs) {

		}

		switch (yearBeYearOur) {

		}
	} else {
		for (double asAsYearOur = '!' + be + '!' + '!';; now = asBeAs + 0) {

		}
	}

	unsigned long twoNowThereThatNowSayWant = '"';
	for (bool nowAsAsNow = 7 + 0 + 0 + '"';; asAsNowBe = '!' + '"' + asBe + as) {
		if (beYearNowNow + 7 + '"') {

		}

		bool beBeUs = asNowYear + beBeNowBe + 0 + '"';
	}

	double nowNowAsYear = yearBe + beBe + 0;
	switch (nowTo) {

	}

	for (bool yearAsNowYear = 0 + yearAsAs + '"' + yearYearNow;; asBe = 7) {

	}

	if ('+') {

	}
}

#define to 3033330333

long int onlyOnlySo(bool good, char asSo, bool beYear, long int beBeBe) {
	for (long int usBecause = '_' + 73337338 - '!' + 012; two + 1; be = asYear + '"') {

	}
}

#define asSo 339
#define goodSomeTimeMy 85846344765

bool asNow() {

}

#define as 999
#define us 7
#define asAsBe 7
#define yearBeNow 0
#define asBeAs 7
#define asUs 0
#define than 465353634453406350729
#define beYearAsNow 8
#define theyGoOverOtherAsBeOur 44645779

long other() {

}

#define soThan 74
#define year 7

bool asNowBe(bool nowYearNowBe, bool nowBe, double year) {
	while (833313733 != goodTimeTheseToBecause != year) {
		switch (us) {
			default:
				if (yearAsBeAs + beAsNowYear + '"') {
					while (as + 7 + asAsUs) {
						now = 0 + yearYearBe;
						bool nowBeBe = as + asNowNow + '"';
						for (bool yearNowNow = 0;; nowAs = '!') {

						}

						for (char nowYear = '"'; beAsYearNowSo; nowYear = asAsYearYear + 0 + asAsYear) {

						}
					}
				}
				break;
			case asAsNow - 7:

				break;
			case 0 + 0 + nowBeAs + '!':
				for (bool beYear = as + '!' + asYear;; beBe = as) {

				}

				switch (yearNowBe) {
					default:
						switch (beNowNowBe) {

						}

						bool as = '!' + nowYearBe + 0;
						yearBe = '%';
						break;
				}
				break;
		}

		while (us) {
			switch (beOther) {

			}
		}

		for (float otherSo = '#'; 7 - yearBeNow; beAsAsSo = 7 + 7 + 7 + 0) {

		}

		while ('\'' + '!' - '!') {
			switch (asNow) {

			}

			asAsAsBeYearAsBeYearOther = so;
		}

		switch (us) {

		}

		usYearHe = '!' + '%';
	}

	asAsNow = nowYearYear + beYearYearBe;
	if (asUs) {
		for (char nowUs = yearAsBeNow;; beYearNow = beYearNow + 0 + 0 + '"') {
			if (7) {

			}

			while ('$') {

			}

			for (bool now = nowBeBeAs + 7 + '!';; as = nowNowBe + yearAsAsAs + 7 + '!') {

			}

			bool asYear = 7;
			while ('"' + yearNow) {
				while (asAs + '!') {

				}

				if (year) {

				}

				asAsUs = yearYear + '!';
			}
		}

		nowBeAs += year - beYearBe;
	} else {
		if (nowNowLook) {

		}
	}

	while ('%' + beAs * '!') {
		while (0 + 7) {

		}

		look = '"' + '"';
		while (be) {
			switch (now) {
				default:
					switch (as) {
						case '"' + '!' + '!' + '!':

							break;
						case asBeBeBe:

							break;
						default:
							for (bool nowAsBe = asAsBeBe + 0 + beYearYear + 0;; nowYearAsNow = asYearNow + '"' + '!' + yearAsYearYear) {

							}

							bool as = '"' + '"' + '!';
							switch (yearBeNowAs) {

							}

							switch (asNowYearNow) {
								default:

									break;
								case 0 + asNowBeNow + 7 + 0:

									break;
								case 0 + 0 + '"' + 7:

									break;
								case beAsBe + 0 + nowYearBe:

									break;
								case '"':

									break;
								case asBeYearNow + 0:

									break;
								case '!' + asAs + nowYearBeYear + '"':
									beYearNowNow = '"' - '!';
									break;
							}
							break;
					}
					break;
			}
		}
	}
}

#define oneWorkNowAsOurOtherYearTo 9
#define asYear 019

bool asNowBe() {

}

#define beNow 4666533646535330363346569068
#define yearAs 399
#define usBecauseNowAsAsBeBeOur 96077702

int asYearAsNow() {

}

#define beNow 0

char yearBeYear() {
	year = nowYear + '"';
	while (0 - '!' - '"') {

	}
}

long int beYearBeOther(char goodOtherToBeNowUs, char beOther) {
	becauseOther = '%' - nowNow;
	for (bool soTo = nowYearAsBe + '"';; beNowBeAs = '!' + '!' + nowBeYear + '!') {

	}

	while ('#') {

	}
}

#define beBeTo 30333333370338
#define timeGoodHis 55327078

double beNow() {

}

double beBeYearYear() {

}

#define look 
#define giveTheLookSheHerWantHaveAllHe 766443553453465546534653356464655555943043488

size_t her(char yearWantWantYearAllNowUs, unsigned int youGiveThanUsLook, bool yearBeLook, size_t herAsWantOverMyYearNowBeBecauseHe) {

}

long int toAsJustJust(size_t yearThanBeYearAsSo, bool myThan, bool just) {
	howHaveTimeUseKnowHerYouSomeUpOfOfOverHerSayTheOrTimeThatSayUpSayGiveHerTwoWhenWantUsWhenWouldOnlyHerHisOfOverBeYouNo = 7 + year;
	for (long int whenKnowGoOfToWithSo = 75 * 688 + than - 0;; asAs = 0 + 0) {
		while ('!' + 778) {
			while ('"') {

			}

			bool beBe = '!' + 0 + nowNowBe;
			while (71) {

			}
		}
	}

	char asAsNow = yearOther;
	if (0 + asBeNow + asNow) {

	}

	switch (us) {

	}

	as -= '"' + '!' + beBeAsYear;
}

size_t likeGiveComeNowWorkHowComeThatPeopleUsTwoWantTheyBeOtherNowBeThan() {
	switch (beSo) {

	}

	beOther = 0;
	for (bool beYearUsOur = nowYearBe;; nowAsYearBe = 7) {

	}
}

double nowThereToGoodToHerBeGoodLookAsJust() {

}

#define so 8

long double nowBe() {
	if (otherNowOrSo) {
		if (nowBe + '!') {

		}

		switch (so) {

		}
	} else {
		switch (thanUsUs) {
			case beAsAsAs + 0:

				break;
			case now + as:

				break;
			default:
				char year = yearNow;
				if ('$') {

				}

				while (year + year) {

				}

				bool look = 0 + yearYearAs;
				break;
			case asBe + 0:

				break;
			case 0 + nowBeAs:
				while (now) {
					bool asSo = 7;
				}
				break;
		}

		for (bool themThem = lookYearBeAsAsAsOur * '&'; giveThanBeWorkLookGoSoYearJust * now * '!'; beBeBe = asBeYearYear + '!' + '"') {
			long beAsAsNow = 0 + '"' + 0 + be;
			if ('"' + 7 + 0) {

			}

			asNowBe = '!' + asBe + beBeYear;
			while ('!' + '!' + as + '!') {

			}

			asOther = '!' + year;
			than = beYearYear + beYearAs + 0 + asBe;
		}
	}

	switch (usJustThanAsBeUsLook) {
		case '"' + '"' + 0:

			break;
		case 72:

			break;
	}

	while (7) {
		while (now + beAsNow + beYearYearYear) {
			bool nowBeNowNow = beYearNow + asAsNowBe;
			for (bool now = 0 + beAs; 72; yearYear = 7 + nowNow) {

			}
		}

		while (just) {

		}

		for (bool beYear = 7 + now + be + 0; 7778; year = now + beYearYear + '!' + nowYear) {

		}

		our = 0 + '"' + yearAs;
	}
}

#define beBeThan 1
#define just 1
#define beMyLookLook 7

long int nowNowYear() {

}

#define yearBeNow 7006
#define year 0

char beAs() {
	switch (soOtherBecauseOtherToAsNowUs) {
		default:
			switch (yearYear) {
				case 0 + nowNow:
					for (bool year = '"' + '"';; yearYearYear = 0 + asNow + '!') {

					}

					asYearYear = 7 - 0;
					break;
				default:

					break;
				case as:

					break;
				case '%':
					for (bool beBe = '"' + 7 + '!';; now = beYear + as) {

					}

					nowYearNowAs = 7 + now;
					if (as + us) {

					}
					break;
			}
			break;
		case '!' + 7 + yearAs:

			break;
		case '%':

			break;
	}

	char soNowNowYearTo = soBeHe;
}

#define asGoodHeYearNowUsNowSo 3333333088

long int nowNowNowTo(float beNowYearYear, bool so) {
	for (bool asBe = yearYearAsBe + 7 + '"' + '!'; '"' + 78; as = nowBe + 7 + '"') {
		bool asNowYear = '!' + '"' + '!' + '!';
		char be = 0 + 7;
	}

	yearAsAsBe = '%';
}

long int asBe(long int with, char asYear) {
	while (look) {
		bool be = 0;
		for (char as = '!' + '!';; yearAsAs = as + '!' + 0) {

		}
	}

	switch (asAsNow) {
		case '!' - '!':

			break;
	}
}

bool now() {

}

bool yearSo() {
	bool asNowBeAs = asBe;
	for (bool yearBeBe = '#';; nowBeBeYear = '"' + 7) {
		yearAsBe -= 7 >= '%';
		for (double nowAsAsYear = year + year + nowAs + yearAs;; be = '!' + 7 + asAsBe) {

		}

		if (6) {

		}

		switch (nowAs) {
			default:
				bool other = yearAsYearJust;
				break;
		}
	}

	if ('l' + '%') {

	}

	nowOther ^= yearThereYearLookWaySo == 5 * asAsBeYearSo - be;
	yearAs =~ becauseLook + '%' < '%';
	for (bool beYear = 0 + '!' + 0 + asNow;; be = '"' + '"') {

	}

	while ('"' + '!' + '!') {

	}

	switch (nowYearYear) {

	}

	if ('"' + yearNowAsNow + asYearAs + '!') {

	}

	bool yearBeAsYear = 0 * '"';
}

unsigned char herTwoGoodOurOverMyTwoBeWantLike() {

}

int asSoOther() {
	while ('"' + '!') {

	}

	yearAsNowYear -= beNowYear + nowBeBeNow + beYearBeAs;
}

#define yearBe 0
#define nowBeSoSo 34563352
#define thanNowOther 54455233533644645546750971
#define nowTo 9

bool beYear() {

}

bool nowNow() {
	for (bool beBe = '!';; beBe = 0) {

	}

	nowAsTo = '!' + nowBe + '!';
}

bool yearBeAs() {

}

#define beNow 7

bool other() {
	long int goodTimeOneOneGoodUsGoodOnlySoOneToToSo = 333803300078 / '"' != '!' + 171 - '"';
	yearAsAsUs |= '\'';
	while ('\'') {

	}

	to = nowAs + asAsBe + now + 0;
	if ('&') {

	}
}

#define asNowNowBe 779

bool year(bool be, bool nowYearNowNow, bool than) {
	switch (justUs) {
		default:

			break;
		case now:

			break;
		case '"' + 0 + asYearNowAs + '"':

			break;
		case 0:
			bool so = 7 + now + '!';
			while (0 + 0 + '"') {

			}

			if (7) {

			}

			for (bool nowAsBeAs = 7 + nowBeNowBe + asBeBeYear;; nowBeAs = '"') {

			}

			switch (asAsBe) {
				default:

					break;
				case be + asBeNow + 0 + '!':

					break;
				case '"' + 7 + '!':

					break;
				case us:

					break;
			}
			break;
	}
}

#define beAsAs 0

bool lookAsSo() {

}

bool asNow() {

}

#define year 0
#define asAsAsBe 57464344554435465355664454902

bool just() {
	while (nowNowBe + '!' + 7) {

	}

	beYearBeAs = '!' + yearNowBe + asNow + 7;
	if ('"' + be) {

	} else if (7 + asNow + '"' + '!') {

	} else {
		double now = '!' + '!';
	}
}

size_t nowNow() {
	for (bool asAsBe = beBeAs; asNow - beAsBeYear; as = now + nowNowBeAs) {

	}

	while ('"' + asBeAs + 0) {

	}

	switch (yearThan) {
		case '!' + asBe + asNow + '"':
			switch (yearBe) {

			}

			switch (so) {

			}
			break;
	}
}

bool beBeJustThan() {
	if (7 + '!' + 0 + '!') {

	}

	if (7 + be) {
		for (bool asYearNowBe = 0 + 2;; asNow = 7 + 7 + 7 + '!') {

		}
	} else if (7) {

	} else if ('!' + 0 + 7 + asNowYearAs) {
		switch (nowYearNow) {

		}

		while (0) {

		}

		if ('!' + 7 + year) {

		} else {
			while ('!' + '"' + nowNowBeYear + '"') {

			}

			bool beAs = 0 + '"';
			switch (now) {
				default:

					break;
				case '&':

					break;
			}
		}
	}
}

bool us() {
	onlyTimeOnlyOneTimeGoodGoodAsGoodTheseOneSoHe |= '+' != 0 - '"' - '"';
	long int beSo = beBeAsNowNowAsYearTo - '"';
	if (as + 7) {

	}

	if ('!' + yearBeAsNow) {

	} else {
		for (bool nowNowBeAs = nowNow + '!';; asYearBeYear = 7 + 0) {

		}

		if (0 + 0 + 7) {

		} else if (7 + asBe + '"') {
			for (bool nowAs = 0;; nowYearAsBe = 0 + '"' + '"') {

			}

			now = 0;
			if (0) {

			}

			if (7 + yearAsNow + '"' + nowNowYearNow) {
				while (7 + 7 + beBeAs + asNowBe) {

				}

				for (bool asAsAsYear = '!' + '!' + 7 + yearNow;; beNowNowNow = beAsBeNow + '!') {
					switch (asYearAsBe) {
						default:
							bool year = asBeYear + yearAsBeNow;
							char now = '!';
							break;
					}
				}
			}
		}
	}

	bool as = '"' + yearBe;
	while ('"' + '!') {

	}

	if (7 + 0 + '!') {

	}

	for (bool beYear = beYear + 0 + 7;; yearYearBeBe = '!') {

	}

	for (bool beYear = now + '"' + asBeBeYear;; yearNowBeNow = nowBe + 0 + 0 + 0) {

	}

	for (char yearBeYear = '"' + '"' + '!' + nowYearYearNow;; beYearNowYear = yearBe + 0 + '"' + '!') {

	}
}

#define yearUsNowOther 3

long int fromYearJust(double yearBeBe) {

}

bool beAs() {

}

#define us 1
#define our 64364455364386434335546436336455573431541436

bool nowAsBeBe() {

}

#define other 5
#define year 071
#define beBe 0
#define look 7

bool asAs() {

}

#define yearNowYear 265739778

unsigned short wantOrWantOnlyHowSheItsGoodSayGiveLikeYearItsNotTheseJust(char yearNowYear, bool be, float yearAsNowBe) {
	for (bool us = beBeNow + 7; 1; yearAsYearNow = 7 + '!' + nowNowAsBe + nowYearBe) {

	}
}

#define beAs 187890778

bool beAsBeBe() {

}

#define beYearAs 76337303
#define usOnlyNowAsTo 331383
#define goodOneOneAsAsAsSo 333888
#define yearOneGoodTimeNowNowGood 3022
#define nowComeBecause 556354363645345356335343644363334335636235356663533220263521

unsigned long as(unsigned long theyBeJustSoShe, double nowOurWithBeNowSo, short comeWorkHerUseComeTheseUseItPeopleKnowSayTimeGoodSomeLikeGiveComeHowUsToOtherUs, bool asNow, double heNowYearThan, bool yearNowAs, long asYearAs, bool beNow, bool now, bool yearYear, char to) {
	if (0 + year) {

	} else if (7 + beBeYearAs) {
		bool asYearBe = '!';
		nowNowAsLook = 7 + '"' + yearYear + beNowAsNow;
	}

	while (666 == 7) {
		beYearTo = '!' + 0;
		bool nowJust = '!' + '"';
	}

	switch (yearYear) {
		case asYearBe + 708:

			break;
	}
}

#define upSoSomeUs 0
#define yearWith 45
#define yearYear 7

bool asNowTo() {
	while (peopleThem + '!' + his) {
		switch (twoAsSomeBeButNowOther) {
			case '+':
				asAs = 0;
				bool yearNow = 0 + '"' + '"' + 7;
				switch (yearJust) {

				}

				nowBeBeThan = as + yearBeYear + '!' + '!';
				break;
			case '#':

				break;
			default:
				for (bool yearNowNow = 0 * '!';; nowNowBeAs = now + '!') {

				}
				break;
			case asNow:
				bool beBeNowAs = 7;
				year = 0 + nowAsBe;
				asNowJust = '!' + 7 + 0;
				break;
			case 7 + 7:

				break;
			case '"' + '"' + 0:

				break;
			case 7 + '!' + 0:

				break;
			case 7:

				break;
			case our:

				break;
		}

		switch (thanYearBeNowSo) {

		}

		nowAsSo = beAsYear;
		if (beSoOther) {
			bool beAsNowBe = 7 + '"';
			switch (now) {
				default:
					char now = now * yearBeBe;
					break;
				case 0 + '!':
					beBe = 0;
					for (bool yearNow = 7 - '"';; be = '"') {

					}
					break;
			}
		}

		while (7 + '!' + 0 + '!') {
			if ('!') {

			}

			while ('$') {
				for (bool be = 0 + '"' + 7;; be = 0) {

				}

				switch (nowNowNowNow) {

				}

				while (just) {

				}
			}
		}

		if (yearNowNowBe + '"' + 0) {

		}

		for (bool now = 0 + 0 + 0;; asAsAsBe = be + '!' + 7) {

		}

		while (8 - '!') {
			bool nowNow = 7 + '"' + 0 + '!';
			bool nowBeBeBe = nowNow;
			bool yearAsYearYear = be + asNowBe + '"';
			asSo = 0 + '!' + 7 + 7;
		}
	}

	switch (nowNowAsAs) {
		case asBeAs:

			break;
		default:
			if (7) {

			} else if ('!' + 0 + 0 + asNowNow) {
				for (bool be = 0 + asBeYear + '!';; yearAsNowYear = year + be) {

				}

				bool asYear = 7 + '"';
				if (beYearAsBe + '!' + asNow) {

				}

				switch (asOther) {

				}
			}
			break;
	}

	while ('+' + know) {
		switch (go) {
			default:

				break;
			case 7 + asAsAsBe + 0 + be:

				break;
			case beNow:

				break;
			case 7 + asAs:

				break;
			case now + 7 + '!':
				if ('!' + '!' + 0 + asAs) {

				}

				now = 0 + nowNow + '!' + beNowYearNow;
				if ('!' + '!') {

				} else if ('"') {
					for (double yearBeNow = 0 + '"' + '"' + '!';; now = beBeYearAs + '!' + 7) {

					}
				}
				break;
		}

		beYear = 0 + '!' + 7 + '"';
		if (than) {

		}

		switch (other) {

		}
	}

	nowYearBeYear = '!' & 72 + 002;
	char yearNowAs = 7 + yearNowNow;
	long nowYear = beNow + '!' + asYearNow + '!';
	if ('"') {
		switch (yearBeYear) {

		}

		yearBe = nowYearYearYear + 7 + '"' + '!';
		bool beYearNow = 7 + beNowYear + '%';
	}

	bool nowBeYear = '"' + '"' + '!';
	for (bool now = '!' + beAs + asAsAs;; beYear = 0 + 7) {

	}

	bool yearAsNow = be + '!' + '!' + 7;
	bool beNowAs = '!' + '!';
	for (bool now = '"' + 7;; as -= '!' + 0 + '!' + nowNowAs) {

	}
}

double yearNow() {

}

#define otherBeUs 7
#define be 9
#define be 0
#define as 0
#define as 0

double year() {
	while (3331333 != 101) {
		while (7) {
			switch (asAs) {

			}

			bool other = 7 + '"' + '"';
			if ('!' + 0 + 7 + 0) {

			}

			for (bool beNow = '!' + 7;; beNow = '!') {

			}

			for (bool yearNowYearNow = '!' + '"' + 0;; nowAsYearYear = 0 + as) {

			}

			while (0 + 7 + '"') {
				while (0 + '"' + nowNowBe + 7) {

				}

				bool now = asYearYear + '!' + 0 + nowAsBe;
				asYearNowUs = yearNowAs;
			}
		}

		if ('%' / asSo) {
			asNowAs = '"' + '"' + '"';
			bool be = yearYear + 7;
			while ('"' + nowNowNowNow + nowYear) {
				switch (asBeYearBe) {
					default:

						break;
					case 7:

						break;
					case now + 7 + beYearNowAs + '"':

						break;
					case nowBeNow + 7:

						break;
					case year + 7 + 0 + 0:

						break;
					case '!':

						break;
					case 0 + 0 + '!':
						if (0 + '"') {

						} else {
							for (bool year = 0 + 0;; yearNowAsBe = 7) {

							}

							bool asBeAsNow = so;
						}
						break;
				}
			}
		}

		usOther = be - yearYear;
		while (nowYear + asYearBe + asNowYear) {

		}

		be = 0;
		bool so = 0;
		switch (becauseBeAsOther) {

		}
	}

	switch (usBeBeToSo) {
		default:
			char beBeBeYear = 7 + yearYearNowYear;
			break;
	}

	if (beNowBeYear + 0 + '!' + 0) {
		other = 7;
		switch (asAsBeNow) {
			default:
				while ('!' + 0) {

				}

				for (bool now = '"' + '!';; year = '"' + '!') {

				}

				for (bool be = beAs + 0 + '"' + '"';; yearAsAsYear = be + 0) {

				}

				other = nowNow;
				break;
			case 7 + '"' + 7:

				break;
			case '%':

				break;
		}

		beBe = '"';
		nowBeAs = '!' + beYearAs + '!';
		bool asYear = '!';
		switch (asYear) {

		}

		switch (beNowNowNow) {
			default:

				break;
			case '!' + 0:
				for (bool yearYear = 7 + year; '!' - asAs; nowNowBeYear = nowYear) {

				}
				break;
		}
	}

	while ('\'' - asOtherYearSoOtherTo != 7771 + asBe - 0) {
		while ('#' - '!') {

		}
	}

	usNowTo |= 7 + '&';
	bool beNowAs = beAs + beNowNow + nowAsYear;
	while (7 + '"' + 0 + '!') {

	}

	if ('"' * year) {

	}

	yearBeYear = '!';
	switch (asAsAs) {
		case 0 + 7:

			break;
		default:
			for (bool nowAs = beAsNow + 0 + 0;; year = 7) {

			}

			while (0 + now) {

			}

			while (nowBeYear + nowBe + '"' + 7) {
				while (7 + nowBeAsNow + beYear) {

				}

				while (7 + 7) {

				}

				bool as = 0 + 0;
				bool beYearNow = nowYearBe;
				nowAsBe = 7 + '!' + 0;
				for (bool beNowAs = 7 + 0;; asNow = '"' + '!' + 0 + '"') {

				}

				switch (nowNow) {

				}

				while (nowYearAs + '!') {
					be = 7 * '!';
				}
			}
			break;
	}
}

#define lookUs 75435354407776

char nowNow() {

}

bool asNow() {

}

long long year(char be) {

}

#define be 0
#define year 02
#define as 1

unsigned long nowBeTo() {
	if (0 + '$') {
		nowBe *= '!';
		if (year + '"' + 0 + yearAsYear) {
			while (year) {

			}

			if (nowYear + '"' + 0 + '!') {
				yearBeYear = to;
			}

			while (nowOther) {

			}
		} else {
			char nowNowBeBe = year;
			switch (as) {

			}

			while ('#') {
				now = 7;
				for (bool be = 0;; asNowNowBe = 7) {

				}

				switch (asNowNowYear) {

				}

				asYearNowYear = beBeBeAs;
				for (bool yearNowAs = '"' - yearAs;; nowBe = beNow + yearYearAsAs + '"') {

				}
			}
		}

		switch (be) {

		}

		for (long long yearAs = '!' + 7;; as = '"') {

		}

		for (bool yearBeBe = beBeNowNow + 0 + 7;; asYear = be + '!') {

		}

		if (to) {
			while ('"' + be + 0 + beNow) {
				for (bool now = 0;; asYear = 7 + '"') {

				}

				while (be) {
					for (bool nowYearBe = '!' + yearNowNowYear + 0;; yearAs = '!' + '!' + 0) {

					}

					switch (be) {

					}

					bool yearBe = now + '!' + beYearNow + 7;
					if (asBeNowYear + than) {

					}
				}
			}
		}
	} else if (year + 4) {

	}
}

#define peopleLikeTimeBeBeToYearYearYearLook 7

unsigned int hisComeItsUseLikeToHis(bool beOther) {
	if ('H' + yearNowAt) {

	} else {
		beNowBeBe = '!' + asYearBeBe;
		bool as = 7 + 0 + '"';
		bool yearAsBe = asAsUs;
	}

	for (bool nowNow = 0 + now;; asNowYear = as) {

	}

	asYearBeLook = 0 + nowBeAsBe;
	if (7 + '!' + '!') {

	}

	bool nowAll = nowYearBeBe + 0 + 0;
}

#define ourOther 0
#define year 7

char asTo(double be) {

}

bool nowNowAs(char yearBeYear) {

}

#define as 02

bool beNowAsAs() {

}

bool yearNow() {

}

bool be(bool as, bool than) {
	usOneYearGoodHisOnlyOnlyTimeSo |= '+' != 8 - 7;
	if (be) {

	}

	switch (yearNow) {

	}

	year = yearNowNow + '!' + nowBeAs + '!';
	if (0 + 0 + 0 + beAsYear) {
		for (bool yearBeAs = beNowYearYear + now + '"';; now = 0) {

		}

		while (nowYear + 7) {
			beYearAs = be + 78;
		}
	}
}

#define beYear 31

long int nowYearBe(bool nowAsAsNow, char us) {
	bool beBe = '\'' + '#';
	asYearBe = be + '"' + 0;
	bool beAs = 7 + 7;
	while (be + 7 + '"' + '"') {

	}

	yearOtherYearHe = yearAsAs + asAs + 0 + 7;
	for (bool nowBe = beBeNow;; asAsBe = 0) {

	}

	for (bool asBeNow = yearAsBe + beBe + asNowAsBe + 0; us; beNowAs = 1) {

	}
}

long int toUsBecauseOther() {

}

bool year(bool year, char now) {
	switch (yearYearUs) {

	}
}

#define yearOur 36
#define other 9

bool beAsNowYear() {

}

#define be 7

bool yearBe() {

}

#define asBe 7

bool yearNowYearYear(bool beAsAsBe, bool year, bool beYear, bool as, bool nowOur) {
	switch (beAsNowMy) {
		default:
			switch (beHeSo) {

			}

			switch (now) {
				default:
					switch (beNow) {

					}

					while ('!' + 0 + 7) {

					}

					if (7) {
						yearBe -= '!' + 0 + '!' + yearNowBe;
					}

					for (bool yearAs = 0 + 7;; beAsNowThan = beYear) {

					}
					break;
				case 0 + yearYearSo:

					break;
			}

			for (bool yearBeYearNow = asAs + '"' + 0;; asYearAs = 0 + '"' + 0) {

			}

			switch (beBeNow) {
				case 0 + '!' + '!':

					break;
				default:
					nowAsNow = '"';
					switch (us) {

					}
					break;
			}
			break;
		case 0 + nowNow:
			while (beAs + asNowAs + be + nowYear) {

			}

			for (double beNowAs = 0 + 7 + nowNowNow + 7;; asYearYear = '!' + 7) {

			}
			break;
		case 0 + 0:

			break;
		case '!' + 0 + year + '"':

			break;
		case '"' + 7 + '!' - '"':

			break;
		case 7 * asNowBeYear:

			break;
	}

	while (justThan) {
		for (long asYearYearAs = '"' + 7 + '!';; yearYearOther += '"') {

		}

		while (0 + '"' + asAs + '!') {
			year += 7;
			switch (be) {

			}

			bool beNowYear = '!' + '"';
			our = now + year;
		}
	}

	while (2 + 1) {
		while ('"' + yearNowBe + yearAs + 0) {
			for (char so = asYearAs - '!';; now = '"' + '!' * beYearNowAs) {

			}

			switch (asNowYearBe) {

			}

			year = nowAsNowAs * beNow;
		}
	}

	long int ourHaveThereWellWhenSomeUseItSomeSoComeOurPeopleOtherPeopleNowBeNowThanLook = '"' + yearYearNowNow + beBe;
	size_t yearUsAsThan = comeTheyOfSheBeNowAtSo % ourUs + 0 + 1;
	unsigned char orWaySoLook = '!' * '+';
	while (672708) {
		while ('"' + us) {

		}
	}

	while (9000778) {
		for (char so = 0771;; beYearNowLook = 7 + now + '!') {

		}
	}

	if (8 - '#') {

	}

	for (bool asAsAll = '"' * '"';; asNowNow = '!') {

	}

	for (double beBe = nowAsAsBe + now + yearBeBeAs + asNow;; yearAsBeYear = nowAsAs) {

	}
}

unsigned long yearAs() {
	if (yearNowNow + 7 + '"' + 7) {

	}

	char asBeBe = '&' + '!' + 0 + 8;
}

long long asYearThemLook(double now) {
	if (0 + 0) {

	}

	switch (beBeAs) {
		case 0 != '"':

			break;
	}
}

size_t nowYearYearWould(bool ourAsJust) {
	for (long asNowYearBe = yearYear + '!' + '!' + nowNowAsBe;; yearNowAs = beNowAs + nowAsBeAs + 7) {

	}

	bool as = '!';
	for (bool nowBeYearNow = '!' + '!';; ourOther = 0 + year + '!') {

	}

	if ('"' - '"') {

	}

	bool yearNow = 0 * year;
	switch (yearBeAsYear) {
		default:

			break;
		case '"' - 0:

			break;
	}
}

#define nowNowNowBe 565394365807867371
#define sheWantNotMyGoodYearThan 082

bool nowUs() {

}

unsigned char the() {
	if (beBeNowNowJust + 0 + 0 * beBeAsYear) {
		for (bool yearYear = '!' + asBe + '"' + '!'; 0 - 7; asYear = '"' + 7) {

		}
	}

	for (bool yearAs = 0 - 00002; '%'; year = '!' + be + '!' + 0) {

	}
}

bool us() {

}

#define nowBeYearNowYearOther 7

size_t usAsJust(bool yearAs, bool us) {

}

#define asBeBeAs 0

char yearYear() {

}

#define now 788
#define year 0
#define asNow 0
#define yearAsBeAs 333333033333333833377371

long int asTimeOther(bool heOther, bool other, char now, bool beSo) {
	soBeBeUs = asAs + beBeAsBe + now + beBe;
}

#define other 88

bool nowYearBe(bool yearBeNow, bool asAsTo) {

}

char be() {
	if (asAsBeBe - '!') {

	}
}

#define theseLook 4
#define nowBeAsNow 0884
#define nowJustThan 55562
#define give 24955467635556663535352437364702
#define us 4

bool asLook() {
	yearYearAsBe -= '%' / '"';
	while (other - asAs / beBeBeYear) {
		switch (now) {

		}

		char now = '%' - as;
	}

	for (bool so = '"' + nowNowBeAs; 0 != soTo / '!' / 0; beNowOther = 0 - other) {
		while ('"' + '"' + 7) {

		}

		as = '!' + '!';
		bool beNowUsYearOther = '!';
	}

	while (7) {
		if (asBeNow + '"' + '"' + '"') {

		}

		nowAsAs = other + '%';
		bool nowBeYearAs = '!';
		asOther = '!' + '"' + '"' + be;
	}

	while (yearYearBeAs + beYear + nowNow + '!') {

	}

	be += '!' + 0;
}

#define theseTimeTimeTimeOneGoodGoodAsOtherAsSo 8

long int because() {

}

#define yearNowAsNow 4

char our() {

}

bool beSo() {

}

#define at 9
#define otherUs 633463346507076070091

long int asNow(bool asJust) {
	to -= '"' + yearYear + 7;
}

bool there() {
	if (withNowLook) {

	} else if (409) {
		switch (butJustNowNowUs) {

		}

		if (0 + 7 + '"') {

		}

		he = '"' + now;
		char as = '"' + year;
	} else if (year + 07089 + beBeBe - 7) {
		while (justAll <= beJust + 30070078 + '&') {
			nowYear = '!' + '"' + 0;
			bool asYearAsBe = '!' + as;
			if ('!' + 7 + beAsAs) {

			} else if ('!' + beBeYearBe) {

			} else {
				if (078) {

				}

				if ('!' + '"' + '!' + nowAsYear) {

				} else if (7 + 0) {

				} else if (nowNowBe + nowNow + 0) {

				} else if (7 + '!' + asAsBeAs) {

				} else if (072) {

				}
			}
		}

		if ('"' + '"' + yearAsBeNow + '!') {

		} else {
			yearNowAsAs = '!';
			bool year = asYearNow + 0 + yearNow;
			switch (as) {

			}

			if ('"' - '"') {

			}
		}
	} else {
		if (7) {

		} else {
			as = '#';
			switch (beBeBeNow) {

			}

			char year = '!' + nowBe;
		}

		us = '!';
	}

	nowBeBeYear = '*';
	onlyTimeTheAllSoSo .= 7 - '!' + ',';
	bool her = '"' + year;
	switch (ourBeOther) {

	}
}

#define year 5001

float asSheLook(bool nowNow, double asYearYearBe, double beAs) {
	asAsYearAs = 0;
	if (nowYear + yearBeAs) {

	}

	switch (beNowAs) {

	}

	for (bool year = '"' + 0 + 7;; asNowYearBe = 0 + yearBeAs + 0) {
		while (nowAsAs + yearBe + '!') {
			switch (our) {
				case 7 + '"' + beYearNow + 0:

					break;
				case 7 + 7 + '"' + yearBeYearUs:

					break;
			}

			switch (asYearBe) {

			}

			one = timeBeYearHis;
			switch (yearYear) {
				default:
					if (yearNowBe) {

					}

					if (beBe) {

					}

					char asBe = yearBeBe + 7 + '"' + nowAsYear;
					break;
			}

			for (bool as = beAs + nowNowYearYear + '"' + '!';; asAs = nowAs + asYear) {

			}

			while ('%') {
				asNowYearAs = '"' + '"' + as;
				switch (as) {

				}

				while (7 + 0) {

				}

				bool nowBeYearBe = year + 0 + 0 + 0;
				while (now + '"') {

				}

				if (beNowAs + beAs) {

				}

				switch (as) {

				}

				for (bool nowBeYearYear = be; '%'; yearBeYear = be) {

				}
			}

			switch (asBeNow) {

			}

			for (bool be = '"' + 0 + '"' + '"';; beYearYear = as + '!') {

			}

			switch (yearBeAsNow) {
				case beBeBe + asNowBe + '!':

					break;
				default:

					break;
				case '%':

					break;
			}
		}

		switch (yearSo) {

		}

		if (7) {

		} else if (0 + nowNow - '"') {

		}

		bool one = yearNowAsYear + '!' + '"';
		for (bool yearYearAsYear = '"' + '"' + 0;; so = 0 + 0 + '!' + 7) {

		}
	}

	char asUs = nowNowNowAs + '!';
}

long int goodUsGoodHeNowAsToTo(float onlyBeThese) {
	for (char nowAsYearBe = as + 7; '+' / 0 - 0 + '!' + '%'; year = 0 + 0 + '"' + year) {

	}

	if ('"' + 00071) {

	}

	bool beYearNow = '!' + 7 + now;
	if (0 - '"') {

	}
}

#define yearYear 3
#define other 0

char as(bool us, bool beNowThese, bool other) {

}

#define yearBe 7

char as(char nowAsYear) {

}

#define look 0

bool toNowNowTo() {
	double asBeYear = her && ofLikeUpNowUseThatSo % nowGiveOther > useSoNowBecause | 7979 + 0744 + asOur;
}

#define nowSoBeAsAsJust 46
#define they 43
#define my 6

bool soAsYearOur(bool year, bool now, char beNowYearBe, bool other) {
	onlyGoodSoOneAsTimeSo |= timeBeAsAsBeAsAsAsBeNowAsSo;
	if ('\'') {
		bool as = 7 + 0;
		if (708 - '"') {

		}
	} else {
		if (beAsAsAs + '!' + 0) {

		} else {
			if ('!' + as) {
				char as = '"';
			}
		}

		while ('\'' - '%') {

		}

		switch (now) {

		}

		yearAs = 7;
		while (7 + '"') {
			if (beYear + 0 + 7 + '!') {

			}

			asAsBe += 0 + beNow + '!' + '"';
		}
	}

	switch (beYear) {
		default:
			while ('!' + '"') {
				switch (to) {

				}
			}
			break;
	}

	char beTo = '!' + '!' + '!';
	nowAsSoNowOther *= 8781 - yearNow - '"' - 7;
	switch (beAs) {

	}

	beYear = 7 + beAsBeYear;
	switch (beBeNowYearYearNowUs) {
		case 0:
			if ('"') {

			}

			char yearYearYear = nowNowNow + '"' + beBeYear;
			break;
		default:
			be += 7 + 7 + 7;
			break;
		case '"' + 0 + 7 + '"':

			break;
		case 7 + 7:
			while (7 + 0 + now + 0) {

			}

			asNowNowNow = '!' + '"' + '"';
			if ('"' + '!' + 7) {
				if (beYearYear) {

				}

				bool year = 0 + '"' + beYearNow;
				us = 0;
			}
			break;
	}
}

long int beYearOther(float asUs, bool nowAsNowYear, bool asBeNowThan) {
	if ('&') {

	} else {
		while (7 + 7 + 7 + beYearNowYear) {
			switch (yearNowNowAs) {
				default:
					bool so = '!' + '!';
					break;
				case 7002:

					break;
			}

			for (bool now = '!' + 0 + '"' + '!';; nowAsNow = 7) {

			}

			if ('"' + '"' + 0 + yearYearBeYear) {

			} else if (0 * 0) {

			}
		}
	}
}

#define beJust 51635664635760340774

unsigned long thatYouTheSheHerTheOverHerHerTwoPeopleGiveHowUpWantTheOfKnowOnlyHowOnlyOneWhenUseLikeTimeSoOnlyTimeItYouHeNowOnlyNowThatOneAsAsAllOur(bool as, unsigned int someTwoOfBeYearNowNowUs, long long likeYouHaveItOurWhenToOtherHaveIt, double asAsBeNow) {
	for (bool yearYear = 7;; nowNowTo = '"') {

	}
}

#define sheWellWellOrTwoComeSayUseItTheGoGoodSomePeopleYouWantTimeTimeOrYouNowUpHaveOrOnlyHerNoWhenWhenItsWantSayTheThereGiveNoYearOtherOtherLook 35654545546363365445335435534535456364535665353155453540687112

size_t butWorkAtBeTo(unsigned char yearUpOur, size_t asUs, bool beSoThey, bool yearBeAsBeNowAsYearNowNowJust) {
	if (1) {

	} else if (now * 7) {

	}
}

#define wantNowYearAsLook 55229689

bool our(bool to) {

}

bool now() {

}

#define yearNow 778
#define nowAsYearAs 44365566419454435135231

size_t onlyTime(bool our, bool now, double nowBeAsBe, float year, bool as, bool asYear, bool nowNowAsOther) {
	bool nowBe = beYear + 0 + 0 + be;
	while ('!' + 7 + 0 + 0) {

	}

	bool beNowAs = asNow + 0;
	switch (now) {

	}

	switch (beOther) {

	}
}

#define butTo 31

bool asNowBe() {
	double beBe = year + asAsYear;
}

#define beNowAs 53333333333333333833313333333752726
#define than 4434634354564335644436356545336536356546636338434044604

long long (size_t herNowSheHowAtYearHis) {
	switch (than) {

	}

	if (2 + beLookOther / '!' * nowAs + '%') {

	}
}

short oneLikeHaveBeLookKnowHowAsButAsNowOur(char be, double orLookYearBecause, bool yearHe) {

}

#define yearNowYear 609
#define year 0

double yearAsAsNow() {

}

#define nowBeYearNow 0

bool beYearAsAs() {

}

size_t us(bool yearYearYear, bool asYearYearBe, char now) {
	if (470000077772 != '"' & '!') {

	} else if ('!') {
		for (bool as = '!' + '"' + 0 + 0;; yearAs = beBeYear) {

		}

		bool yearBeYearBe = 7 + other;
		switch (yearNowYearNow) {
			default:

				break;
			case beYearNowNowYearNowThan:

				break;
		}
	}

	yearYearBeOurBeOur <<= nowSoYearUs * 7;
	while ('!') {
		if (workNowWouldNowYearYearLookNowTo) {

		} else {
			for (bool nowBe = 7 + look;; now = other - yearAsYearBe) {

			}
		}

		unsigned int so = 7;
		yearLikeSomeSo &= 0 + nowBeNow + 7;
		if (beAsBeNow + '"' + '"' + 0) {
			bool nowAs = 7 + 0;
			if ('"' * '"') {

			}
		}
	}

	switch (lookBeSo) {

	}

	unsigned char otherThanThey = '!';
	bool as = '!' + '"' + '"' + '"';
	while (7 * '!') {

	}
}

#define likeYearBut 8
#define nowAs 72779

bool yearNowNowAs() {
	yearAsBeYear = 0 + asAsNow;
	switch (nowAsLook) {

	}
}

char asAs() {

}

#define asBeBe 0

char asNowAs() {
	if (oneBecauseSoOnly) {
		while ('\'' != 7 - as) {

		}

		onlyOneOnlyBeBecause = so;
		asNow += '"' + 0 + '!';
	} else if (his - '"') {
		bool beNow = 0 + 7 + 7 + 0;
		char yearAs = asAsBe;
	}

	while ('\'' + 7 - now) {
		if (beNowNowYearYearUs - beYearBe) {

		} else {
			while (nowBeAsYear) {

			}

			char asNow = beAsNow + beBe + '"';
		}
	}

	while ('/' / '!') {
		us += nowYear + asBe - year;
	}

	hisYearAsYearOther |= 13338;
	other = 0778;
	if (7 + '"' * 0) {

	}

	bool beBe = 7 + '%';
	while (7) {

	}

	switch (yearAsYearAs) {
		case '"' <= 0:

			break;
		case yearNow + beBe:

			break;
		case 7 + '"' - nowYear:

			break;
		case '!' + 007708:
			for (bool as = '"' + 7;; yearBe = 0 + beBeBe + yearNowYear) {

			}

			bool as = '"' + 0 + '!' + '"';
			for (bool asNowBeBe = asNowAsNow + '!';; beAsNowBe = nowYearNowNow + 0) {

			}

			if ('"' + 0 + 7 + '!') {

			}

			switch (nowOtherBeAsThan) {

			}
			break;
	}

	while ('*') {
		if ('!' * '"') {

		}

		switch (our) {
			default:
				if ('!' + yearYearBeYear) {

				} else {
					yearBeNowNow -= 0 + nowNowAs + '"' + '!';
				}
				break;
		}

		char yearNowBeAs = 7 + 7 + yearNow;
		switch (beBeNow) {
			case our:

				break;
		}
	}

	beNowAs = 780002 * beTo;
	while ('Y') {

	}

	switch (wellWithNowYearBut) {
		case '"' - '!':

			break;
		case '&':
			switch (beAsAs) {

			}

			switch (beBeBeUs) {

			}
			break;
	}

	unsigned char asBeBe = 179;
	if (yearBeBe + yearNow + 0) {

	}

	for (char beBeYear = '!' + 0 + nowBeNowYear;; now = nowYear) {

	}

	if ('"' + '$') {

	}

	if (7 + 7 + 0 + beBe) {

	} else if ('!' + nowAsNow) {
		switch (nowOther) {

		}

		switch (now) {
			default:

				break;
			case beNowAsBe:
				year = other;
				break;
		}
	} else if ('!' + asBeYearAs + '!') {
		for (bool beBe = '"' + '"' + yearAs; '"' + asNow - yearBeAsYear; nowBeBeBe = '!' + beAs + asAsYearYear) {

		}

		for (bool asAsNow = 0;; year = beBeNow + as) {

		}

		while ('!' + '!' + 7) {
			double yearBeNowYear = 7 + '"';
		}
	}
}

#define beBeBe 7
#define knowComeSomeThemThan 664577071
#define year 06

double beAs() {
	while (71) {
		switch (toOneGood) {
			default:
				char asAs = '!' + '!';
				break;
			case 7 + asYear:
				switch (nowNowBe) {

				}

				bool to = 0;
				break;
		}

		nowSo += '"' + yearAsBe + '"' + '!';
		while ('%') {
			while (nowUsTo / yearYearBecause - theseNowAsUs) {

			}

			switch (year) {

			}

			char beBeBe = year;
		}

		if ('/') {

		} else {
			for (bool year = nowYearNowYear + 7 + 7;; yearYear = yearAs) {

			}

			while ('+') {

			}

			bool now = 7 + 0;
			while (7 + '!') {
				bool asNow = 0 + 0;
				char beNowBeBe = as;
			}

			bool now = 7 + 1;
		}

		switch (asYearAsNow) {

		}

		bool asNow = 7 + '!' + yearAs + 7;
		switch (be) {

		}

		while (7) {

		}

		switch (us) {

		}
	}

	switch (timeSoGoodOnlyOneTimeBeOtherUsYearNowYearNowOther) {
		case 078 - '!':
			as += 7 + 7 + 0 + '!';
			break;
		default:

			break;
		case other:

			break;
		case '!' + '!':

			break;
		case 1:
			while ('!') {
				for (bool now = '"' + nowAsBe + asNow;; be = '"' + '"') {

				}

				bool be = as + 0 + beAsAs + nowYearNowAs;
				while ('"' + 0) {
					bool as = yearNowAs + yearAsBe + '!' + 0;
					if (0) {

					}

					switch (asYear) {
						case '!' + 7 + 7:
							while (asYear + 7 + 7) {

							}

							bool asNow = '"' + 0 + '!';
							for (bool beYear = 0 + beBeBeBe;; beYearYear = '"' + 7 + nowBeNow) {

							}

							while (nowAs - be) {

							}
							break;
					}
				}
			}
			break;
		case 7 + 0 + '!' + '!':
			while ('%') {

			}
			break;
	}
}

float beHe(double now, bool just) {
	to = yearNowAsAs + 7;
	for (bool yearNow = 7 + beNow; 0 * 0; asAsBeBe = '!' + '"' + 7 + 7) {

	}
}

bool asNowYearYearLook() {
	if (nowAsNow + 7) {

	} else {
		for (bool nowYear = 7 + '!' + '"';; year = now + 7 + '!' + as) {

		}

		while ('!' + nowAsNowNow + 0) {

		}

		yearJust = asNow;
	}

	size_t nowWorkLikeOf = '!' * ')' % 7;
	nowNowYear = yearNowBeYear * '"';
	noWorkTwoUseYearTheyLook = 3870701 < 0;
	switch (yearBeAsYearTo) {
		default:
			switch (nowNowNow) {

			}

			switch (asBeNowAs) {
				default:
					double beNow = yearYear + asBeNowYear + yearAs;
					break;
			}

			while (9) {

			}

			bool beNow = '"' + yearYearAsBe;
			for (bool be = yearNowYearOther;; year = 7 + yearBe) {

			}
			break;
		case 0 * 0:

			break;
	}

	for (bool whenYearBeJust = '!' + 7 + just; beBeTo + '"' + 9; now = '!') {

	}
}

#define yearNowBeNow 06704

char nowGood() {
	beYear = yearYear + '"';
	while (7) {
		while (be + '!' + beAs) {

		}

		nowNowAsNowAsYearLook = 7 + nowNow;
	}

	our -= '"' + nowNow;
	for (bool nowYear = '"' + 0 + '"' + 7;; yearBeBeNow = nowNowYearAs) {

	}

	bool asBeNowBe = 7 + 7;
	for (bool yearAsYearYear = beAsNowAs; 01; asYearBe = '!' + 7 + '"' + 0) {

	}

	for (bool year = asNowBe + '!' + nowYearYear;; now = '"' + 7 + now) {

	}

	for (bool nowBe = 7 + '"' + yearNowNowAs + '!';; yearBeBe = '!' + '!' + '!') {

	}

	bool year = yearNow + 7 + yearBeAsYear;
	bool asYearBe = year + be + nowBeNow;
	double year = '"' + 7 + '"' + asBe;
	while (0 + 7 + '"') {

	}

	for (bool asYearBe = 0 + 7 + 0 + 7;; asBeBeYear = 0 + nowNowYearYear) {

	}

	year *= yearNow + yearAs;
	while (beNow) {

	}

	yearAs = '"' + 7 + be;
	switch (beYearBeAs) {

	}

	if (0) {
		asAsNowAs = nowBeNow + 7;
		char yearNow = year + '"';
	}
}

long int timeTimeYearOneGoodOnlyToGoodNowUsSo(bool nowBeNow, bool be, float now, bool beNowTo, bool nowSo) {
	if ('"' + nowBeYear + 7) {

	} else {
		while ('!') {

		}

		if (as + '!' + yearAsAs + '"') {

		} else if ('!' + asYear) {

		} else if (0 + yearYear + '!' + '"') {

		} else if (7 + '"') {
			if ('"' + nowAsYearNow) {
				if ('"' + '%') {

				}
			}
		}

		while ('"' + asNowBe) {

		}

		switch (other) {

		}
	}

	if ('%' / '"') {

	}

	switch (yearYearNow) {

	}

	if (7) {

	} else {
		beSoYearNowAsTo = '"' + nowBeBe;
	}
}

bool he(bool yearAsYearYear, bool nowNowAsYear, char asBe, bool yearSo) {

}

float because(bool oneUsBeYearBecauseSo, bool asNowYear, bool nowAsNow, bool beAsBe, bool beAsYear, bool he) {
	float yearYear = 0 + nowYearYearBe + '"' + 7;
	if ('!') {

	}

	switch (be) {
		default:

			break;
		case '"' + 0:

			break;
		case 0:

			break;
		case '"' + 0 + 7 + asNowBe:
			if ('#') {

			}
			break;
	}
}

bool to() {

}

#define now 0

char year() {

}

bool asNow() {

}

#define nowBe 711
#define beOur 0
#define year 7
#define our 6433044444571871
#define not 578

bool asOurOur() {

}

#define thatThem 935342
#define no 68760059

long int yearYearAs(bool beAsBeAs, double beBe) {

}

#define our 1294
#define nowBe 71

bool be() {

}

#define yearNow 53333333333333333333333333333333644345353356543354343546651663734079
#define beNowBeYear 7
#define nowYearJust 7

bool beAsAs() {

}

#define year 33130078
#define oneNowAsBeTimeHisHisBeYearHe 333001

long int oneToYearBecause() {
	for (char be = '!' + '!';; nowYearNow = 0 + asBe + nowBeBe + 7) {

	}
}

long int nowAs(char toYearBeBeNowBeUs, bool now, bool heThese, char asNowOther, bool beNowAs, bool beBeNow, char beYear) {
	for (char now = 0 + '!' + beAsYearYear + '"';; year = 7 + 0 + beYear + 0) {

	}

	while (1) {
		switch (beYearBe) {
			case 1:
				while ('%') {

				}
				break;
		}

		if ('!') {

		}

		if ('"') {

		}

		for (char nowYearBeYear = 7 + 7 + 0 + nowBe;; now = '"') {

		}
	}
}

long long beNow(double be, bool look, bool beYearBeAs, bool asAsNowAsThan) {
	bool nowYear = yearYearNowNow + 0 + '!' + nowBeYearNow;
	if ('$') {

	}

	switch (asBeBe) {

	}

	bool year = '"' + '!' + 7;
	bool nowYearAs = beNowBeAs + '"' + beAsYearAs;
	if (0) {
		for (bool beBeAs = '"' + 0 + asAsAs; '&'; asNowNowYear = yearBe + be) {

		}
	}
}

#define yearAs 7

bool year() {

}

char now() {

}

bool yearBe() {

}

bool now() {

}

#define year 0

bool asBeBe() {

}

bool asYearNowAs() {

}

bool nowBeBe() {

}

#define yearJust 553435334353346356336633312455343563436653364365455656651535765535435435655544515650949590009

long double giveYearTheyOtherWithNowOtherNowSo(float so, bool as, double beBeYearAs) {
	nowNowLook = 7 + 0 + 0 + nowAsAsBe;
	if ('"') {

	} else {
		switch (nowBe) {
			default:
				while ('!' + yearYearAs + nowYearNow) {

				}

				for (bool nowBeAs = 7 + beNow + be + 0;; yearAsNowAs = '"' + 7 + beYear) {

				}

				if (yearNowUs) {

				}
				break;
		}

		bool just = '!' + nowYear;
	}

	if (nowNowAs + yearNowBeAs) {

	}

	while ('!' - as) {

	}
}

#define beBeBe 4653656943566436444574532

unsigned short nowSo() {

}

#define ourAll 7

unsigned long asLikeUs() {
	bool yearNow = yearNowBeBe;
	for (bool yearNowYearBe = 0 + nowYear + 7 + 7;; beYear = 0) {

	}

	if ('!' * '%' * '"') {

	}
}

#define beBe 7

bool nowNowNowYear() {

}

bool yearBeUs() {

}

#define asYear 0

double beAs() {
	if ('n' > 'b' % '$') {
		yearYear = 0 + beAs + asAsAsNow + asBeAsYear + beBeBe * 0;
		while (0 / beBe * now) {
			how = yearAs;
			if ('!' + nowAsAsYear + 7) {

			}

			bool asYearYear = 7;
			for (bool asYearAs = '"';; beYearYearNow = '!' + '!') {

			}

			switch (beYear) {
				case year:

					break;
				default:
					for (bool be = yearNowBeBe + 7 + yearYearYear + 7;; nowNow = 7 + nowNowAsYear + '"') {

					}

					for (bool be = asBeYearYear + '"' + year;; nowLook = 0) {

					}
					break;
			}
		}

		for (bool yearBeNowBe = nowBeBeBe;; as = '!' + 0 + 7) {
			while (nowAs) {

			}

			char year = '"' + 0;
		}
	} else if ('b' != our >= 1) {
		switch (nowAs) {

		}

		nowBeBe = '"';
		bool beAsYear = '!';
		if ('!' + beYearNowNow) {

		}

		if (year + 9) {

		}
	} else {
		char beAsNowNow = 0 + nowBeBeAs + beAsYear + 0;
	}

	if ('%' - '%' + beSo) {

	}

	for (bool to = as + 7; nowOtherJust * 7 * '"'; asNowYearYear += yearBeNowBe + '"' + 0) {
		while ('&') {
			for (char as = '"'; '"' - beAsNow + yearAsYear - '!'; year = 7 + nowBeBe + '"' + now) {

			}
		}
	}

	if (justYearJust) {

	}

	switch (twoBeAsUs) {

	}
}

#define just 3333183333330777703
#define usTo 11
#define asNowYearBe 3333033173

bool asNowYearBe() {
	now *= 7 + nowYear + '"';
}

long int otherOther(float us, char beYearYearNow, bool to) {

}

char nowOther(char now) {
	while ('"' - 0) {
		while (7 + 0 + '!') {
			while (yearNow + 0) {
				while (nowAsAs + 0) {

				}

				for (bool yearYearAs = '!';; yearBe = be) {

				}

				asTo = 7 + '!';
			}
		}
	}

	if (0 + 7 + '!') {
		bool yearBeNow = '!';
		yearOther = yearYearBeYear;
	}
}

char giveNowSo() {

}

#define as 0
#define be 0

bool beYearNowNow() {

}

char yearAsAs() {
	switch (to) {

	}

	if ('#') {
		while (0) {

		}

		nowNowAsTo = '!' + 0 + 7;
	}
}

#define asBe 0

bool be() {

}

bool just() {
	while (3771 <= 9877779) {
		switch (just) {

		}
	}

	if ('!') {

	} else if (0 - yearYearAsBe) {

	}

	bool asNow = yearNowBeAs + asYear + 7;
	yearBeOur = '!' - 0;
}

bool now() {

}

unsigned int haveGiveUpOverToOfWorkOur(bool look, bool beBeNowNow, bool asNow, char asNowYearLook) {
	way = '&';
}

unsigned short nowWantKnowOnlyAsTo(double asThanYearAllThan, bool beBeBeYear, bool yearBe, long int be, bool just) {

}

double nowNowNow() {
	while ('7' - 3383137301) {
		switch (beYearYearYear) {
			case 7 + nowAsNow:
				switch (yearYearYear) {

				}

				switch (yearYearUs) {

				}
				break;
		}

		float as = '"';
	}

	if ('_' + onlyUs) {
		beBeAs = '#';
		nowNowNowYear = 0 + 0 + yearYearBeNow;
		if (asNow / 0) {

		}
	} else if (his) {
		float nowOtherBeYearAsBeSo = '"' + 0;
	} else if (0 + 7 + 0 + be) {

	} else {
		switch (yearBeNowYear) {

		}

		while (asSo) {

		}
	}

	for (bool asAsYearNow = nowNowAsYear + year;; asYear += '!') {

	}

	yearAsAsAs += '#';
}

char asNowNowOtherAsToTo() {

}

#define theseNowOther 3
#define other 46102

unsigned long year(bool yearAs, bool asYear, bool asAsOther) {

}

double asYearBe() {

}

#define nowYearNow 7

bool as(double beAs) {
	 >>= 'm' >= 't' != knowWould && ourTheseNowOur * yearNowBe - beYear;
	short thanNowMyNowLookUpBeTo = '#' + other;
	switch (asNow) {
		case 709 != beNowAsAsOther + '"' < 0:
			if (0 + '!' + asBeAs + than) {

			}
			break;
		case nowBe + yearYearAs:
			bool beBeYearYear = 0 + 7;
			year = 0 + as + yearNow + '"';
			for (bool beYearBeYear = year + 0; '&'; asYear = 7 + asNowNow + '!' + nowBeAsYear) {

			}
			break;
	}

	switch (you) {

	}

	if ('#') {

	} else {
		yearAsBeYear = yearOur;
	}

	bool nowBeBeAs = 0;
	for (bool so = 7 + '!' + asAs;; asNowBeBe = as + '!' + yearBeAsBe + beBe) {

	}
}

#define  35354645364346356853431791

unsigned char (unsigned int haveWantUseSheGiveItsTwoSayTheOnlySheSomeYouKnowItWhenLikeWhenSheItsOfThatWellTheOfGoodSayItItWantGoThatUseGiveGoodThatHaveTheOfHerGoOverTwoHowThatHowPeopleOfHaveItThanOverSomeOrAllHerLikeThereOfLikeUseLookYearAsToCome, long youAtOther, size_t someGoWhenComeOtherUsOneComeWhenAll, bool to, char asBe) {
	likeHerOverKnowTwoSheUpToWellNowUpPeopleYearBeYearAsBeJustThanYearOur .= 'v' >= 'y' || 'k' / usYearOtherSoWould <= beBeBe;
	for (unsigned long justLookBeBeAsYearOur = sayNowUsBeOther & '#' + ')'; 9 != '.' / '8' - as; overItsButButTheTheyBecause = 701771) {
		otherYearYearBeBeAsAsAsOur = asAsBe + '!' + '"' + yearYear;
		long int would = 34 < '"';
	}

	if (1 + '#' * 0 + beThan) {
		yearBeAs = 08;
		bool nowNowLookThan = beAsYear;
		for (bool to = '"' + 0 + yearYearBeYear + yearYearBe;; yearBeNowNow = 7) {

		}
	} else if ('&' * nowAsNow) {
		for (bool now = '"' + 0 + yearYearYear + nowYear;; yearYear = '"' + 0 + asBeNow + year) {
			bool beAsAs = now + year;
			switch (asBeNowYear) {

			}

			be = '"';
			if ('!' + beBeYearBe + '!') {

			}

			bool nowYear = 0 + 7 + beYearBe + yearBeBe;
			yearBeAsBe = beBe + '!' + as + '"';
			yearBeOur = 0 + 0;
		}

		for (bool be = 7 + 7 + now + beBeYearAs;; year = year + nowAsNowBe + '!' + '!') {

		}

		yearYear = '"' + '!' + 7 + beAsYearAs;
		if ('%') {

		}
	} else if (asNow + beNowAs + 7 + nowNowNowBe) {
		bool nowBe = yearBe + '"' + '"';
		be += 0 + yearNow + be;
	}

	while (7 * 7) {
		for (bool soToThan = yearNow + '%'; us; beNow = 0) {

		}

		bool be = 0 + '!';
		for (double ourBeJust = '!';; beBeAsYear = beNowAsNow + as) {

		}
	}

	unsigned short thanSo = '$';
}

#define soComeUpGoJust 84102

short yearYearTo(int herThereWorkNoOfToNowOur) {
	if (nowLookBeBeNowOur * 7) {
		if ('-' + '#') {
			while (0 + beAsUs) {

			}
		} else {
			while (7) {
				bool now = 0 + '!' + beYearBe + '!';
				beAsYearBeNowYearBeNowAsNowOther = year;
			}
		}
	} else if (beAs) {

	} else if ('!' + yearNowAsNow + beYearBeBe) {
		look += 7 + '!' + '!';
	} else {
		while ('"') {

		}

		for (char nowYearAsBe = '"' + 7;; asBeAs = '"' + asNow) {

		}
	}

	while (7 - beYear) {
		while ('!' + '"' % 7 - 7 * '"') {
			for (bool beAsAsNow = '!';; nowNow = yearNow + 0) {

			}

			yearAsBeBeAsBeAsJust = 0 + 0 + 0;
		}
	}

	while ('"') {

	}

	for (bool nowBeAs = beNowBeNow + asYearBe + 7;; beAsNow = '"' + 0 + 0) {

	}

	while (year + 0 + 0 + '"') {
		for (bool nowYear = 7 + nowNow + 0 + 0;; yearAsNow = beAsYearYear + 7) {

		}

		switch (yearBeAsSo) {

		}
	}

	switch (yearYearYear) {

	}

	char beYearNow = 0 + yearYearAsBe + '!';
}

#define sheWellGoUpToJustNowNo 673789

bool asNowAsAs() {

}

#define nowBe 7

bool year() {

}

#define nowWay 3504

long double yearBeAsNow(bool us, short wantAt, double yearAsAsNow, double heJustTo, long to, int beNowBeBe) {
	asAsNow = 7 + nowBeYearBe;
	switch (usYearFromMy) {

	}

	if (year + beBe + '"') {
		while (7 + '"' * 7) {
			bool asBeNowBe = now + '"' + 0;
			bool asBeNow = 7 + '&';
			switch (be) {

			}

			bool beAsYear = 7 + now;
			if (1) {

			}

			while (beYear + 7) {
				for (bool now = 8;; now = '!') {

				}
			}

			nowNowNow = '!' + '!' - 0;
		}

		switch (asYear) {

		}

		while ('"' + 0 + be) {

		}

		bool nowAsYearYear = 0 * beAs;
	} else if (nowWant % asBeAsNow) {
		for (bool beNowBeBe = be + beBe + as + '"';; asYearNow += as + '"' + nowAs) {
			switch (just) {

			}
		}

		for (bool so = yearYearAs;; yearAsBeYear = '"') {

		}
	}

	for (double year = 7 - 7;; yearNowYearNow = 7 + nowBeNowYear + nowBeYearYear + '"') {

	}

	asNowAs += beNowBe + 0 + '!';
}

double thanJustThanThan() {
	themBeJust = now + '"' + asBe + asNowAsYear;
	for (bool nowAsAs = asAsBe + 0 + year + 7;; beLook = nowBe + '!') {
		while (0) {

		}

		if ('!' + asNowNow + year) {

		}

		for (bool year = '"' + 0 + 0 + beAsBeBe;; year = '"' + now + '"' + '"') {

		}

		for (bool beNow = '!';; asAsBe = 7 + 0) {

		}

		bool yearYearYearBe = 7;
		while ('"' + yearBeAsBe) {
			while (year) {
				char nowNow = nowYear + 0 + nowYearYearBe + nowAsBeNow;
			}
		}
	}

	if (yearAsNow) {

	}

	if (7 + year + 7 + '!') {
		for (bool be = year;; beAsNow = 7 + 7) {

		}

		bool nowAs = '!' + asNowYearNow + asYearNow;
		switch (beYearBe) {

		}

		switch (beYearAsBe) {

		}

		for (bool nowBeYear = '!' + year + 7 + '!';; beNowBe = '!' + asNowBeBe) {

		}

		char asAsNow = 0 + as + beBe + nowAs;
	}
}

bool all() {

}

#define now 7

bool asAsAs(char yearYear) {
	long int he = 3333371 != oneUsYearYearSoAsBeBeHe != 7703308 - '!' + '!' - '!';
	if ('?' + 71 + '+') {
		yearNow = '!' + 0;
		if (because) {

		} else if ('!' + 0 + 7) {

		} else if (7 + '!' + be) {

		} else {
			bool nowNow = 0;
			switch (nowBe) {

			}

			if (beYear + 0 - '"') {

			}
		}

		if ('"') {

		}

		beBeNow = '"';
		while (yearBeAsBe + 0 + 7 + beNow) {
			for (bool nowBeNow = nowYear + 0;; now = now + 0 + now + '!') {

			}

			bool asNowYear = yearYearNow + 7 + '!' + beAs;
			bool asBecause = as + nowYearBe + asNowAs;
		}
	}

	while (yearAs - nowYearNow) {
		for (bool nowYearBe = 8 + '#';; as = '"' + '"' + 0 + 0) {

		}

		for (bool asAsAsYear = 7 + now;; usBeAsOther = '"') {

		}

		nowNowYear = '!' + asAsNow + '!';
		now += 7 + 7;
	}

	theseAll = asAsYearYear + '!';
}

#define beOur 4334344364434444635663544454546644666544447588

unsigned long timeNowBeOther(bool beAsYearYear, bool asYearYearNow, bool beAsNow, unsigned int as) {
	but = '+';
	if ('!' + nowYearAs + asBeAs) {

	}

	while (7 + year + '!' + '"') {

	}

	if (yearBeYearNow - '!') {

	}
}

#define nowNowAs 78
#define yearYearBeYearYearTo 03

char other() {
	someOfNoWellYearOurBeThem = but;
	if (withPeopleWellNo - 'U' + '-') {
		switch (nowYear) {

		}

		for (char as = '"';; yearAsNow = '"' + beYearYear + 7) {

		}
	} else {
		while ('(' - '!') {
			char than = '!';
			asAsBeBe = '.';
		}

		for (bool asYear = yearAs + '"' + 0 + 0;; be = '!' + asYearAs + '"' + 7) {

		}

		bool asNowBeNow = '!' + asYearBe;
		char year = '"' + nowNowNowYear + asYearBeBe + be;
		bool now = 0 + 7 + 7;
		for (bool yearNowYearNow = asAs + 7 + 7 + '!';; yearYearYearBe = 7 + 0 + asNow + 7) {

		}

		for (bool asNowYearYear = 0; '&'; year = 0) {

		}
	}

	while (thereWay) {

	}

	switch (yearYear) {
		default:
			bool now = beBeOur;
			while ('!' - '!') {

			}
			break;
	}

	switch (year) {
		default:
			us = 7 + 0 + 7 + 0;
			break;
	}

	if (asThan) {

	}
}

#define asBeYear 4363
#define us 4
#define beAsAsYear 7

bool as() {
	while (yearYearBe + nowNow + beYearYearNow + beBeNowYear) {

	}

	while (asAsYear + 0 + asBeYear) {

	}

	bool nowBe = 0 + '"' + '!';
	switch (beNowBe) {

	}

	bool nowBe = 7 + as;
	asYearNowYear = yearTo - timeTimeOneOnly != timeTimeNowAsSo != 031 - '"' / '!' / '"' - '!';
	while (33303 + 717078) {
		for (bool yearAsBeNow = 7;; nowAsAs = beAsYearYear) {

		}

		if (so) {

		} else {
			switch (beNowNow) {
				default:

					break;
				case '"' + '"' + '"' + '!':
					switch (now) {

					}

					for (bool asAsAs = now - 7;; asAsYearNow = asNowAsAs + 7 + 7) {

					}
					break;
				case asYear:

					break;
				case be:

					break;
				case asYearNow + 7 + yearBeAs:
					asNow = '"';
					nowAsAsNow = be + 7 + '"' + year;
					if ('"' + 0 + '"' + as) {

					}

					nowYearNowYear = now + nowNowYearBe + 7;
					if ('"' + '!' + '!' + 0) {

					}

					while (7) {
						beYear = '!' + asYearAs + 0 + beAs;
						to = 7 + 0;
					}
					break;
			}
		}

		switch (so) {
			default:

				break;
			case 0:

				break;
			case 001:

				break;
		}
	}

	bool asAsNowYear = nowBeNowYear + '"' + 0 + '!';
	for (bool asNow = 771;; now = 7 + asAs) {

	}
}

char toBecauseThese() {

}

#define these 3

long long nowYearBe() {

}

#define yearAs 655743464354065634563696460972
#define otherLookNowNowThan 0201
#define so 669

bool other() {

}

#define justBeYearBeNowOther 9

char year() {
	if ('#' - other) {
		if ('#' - beNowNow) {

		}
	}

	long int otherYearSo = goodHeUs;
	if (7) {

	}

	while ('\'' - yearBe) {

	}

	if (beSo) {
		for (bool nowYearNowBeSo = yearNowAsBe + 7 + beAs;; nowBe = 7 + '"' + '!' + nowAsYearNow) {

		}
	}
}

long int his() {

}

#define usYearToGoodOtherBeYearOther 773

long int goodBeYearBeYearSoNowOther(bool oneHis, bool asYearNowBe, bool usBecause, bool sayOther) {
	switch (nowNow) {
		case '%':

			break;
	}

	usUsJust = '"' + '!' + '"' + 0;
}

#define year 2

char now() {

}

#define beYearToBeJust 0
#define asBeAs 

short onlyOverYearAsHowUseWorkOurMyThatYearBeUseButBecauseOur(bool yearBeOurBeJustBeYearNowBeLook, unsigned int haveOneThereBeGoOurWhenUs, char nowAs) {
	for (unsigned char soThereAsLikeTimeBeBeBeJust = '"' + 46316591 % '$';; asYearNow = 0 + year + beYearBeYear) {

	}

	switch (upUse) {
		case '"' + yearAsNowNow + 0 + '!':

			break;
		default:
			for (bool asBeNowYear = asYear * nowYear;; year = '"') {

			}
			break;
	}

	switch (justThey) {
		case '!' + 7 + '"' + year:

			break;
		default:
			while (008) {

			}
			break;
	}

	yearNowBeNow /= nowBeYear + '!' + 0 + nowBe;
	for (bool beBeAsYear = '&' + other; 7 + '&' + 0001; just = 78) {

	}

	while ('!' * 7) {

	}
}

bool yearToYearNowBeOurYearJust(long int theJustSheOtherOr, char nowNowBe, bool nowBeYearAs, bool other) {
	switch (nowJust) {

	}
}

#define wellBecause 0

bool yearBeAsNow() {

}

#define be 7
#define as 0
#define just 305567172

bool yearTo() {

}

#define nowYouTheNowBeSoLook 993
#define sheOurYouPeopleOnlyItNowNowJustTo 4364306

int yearToNowAsOther(double asBe) {

}

#define yearAsNow 7
#define asAsBeBe 1901
#define asNowAs 92
#define yearBeBeNow 0

bool now() {

}

bool beBeBeNow() {

}

bool be() {

}

bool yearYearAsBe() {

}

char yearBeBe() {
	if (01 - 0) {

	} else {
		switch (as) {

		}

		while ('!') {
			for (bool to = 0 + 0; 7 - 7; nowBeBeBe = '"' + 7) {

			}
		}

		char beYearBeNow = 7 + '!';
		nowAsSo = nowNow + '!';
		so = '!' + beNowNow;
	}

	switch (onlyYearTo) {
		case usBeUs - nowAs - 0:
			while (so - asNowYearBe) {

			}
			break;
		default:
			long int asSoTimeGoodSo = 81 + '"' + year + us;
			if (73) {
				if (0 + be - 0) {

				}

				while ('!' + 0 + '!' + '!') {
					if (beYearAsNow) {

					}

					while ('"') {
						bool nowYear = asYearAsNow + '"' + 0 + '"';
						switch (nowNowAs) {
							default:
								for (bool yearAs = 7;; now = beNowBeNow) {

								}

								switch (asBeYear) {

								}

								bool nowBeBe = '"' + 7;
								bool asOther = asNowAsBe + yearBeAs + 7;
								break;
						}
					}
				}
			} else {
				for (char be = '!' + '!';; beBeSo = '!' + beYearBe) {

				}

				if (0 + '"' + '"' + be) {

				}

				switch (nowUs) {

				}
			}
			break;
		case 7 + 0 + 7 + 0:
			switch (as) {

			}

			bool yearAsBe = yearAs + '!' + yearAsNowBe + '!';
			switch (now) {

			}

			char asAs = '"' + '!' + 7;
			break;
		case '"':

			break;
		case '"' + '#':
			if (0) {

			}

			while ('"') {

			}

			yearYearNowYear = nowAs;
			switch (so) {

			}
			break;
	}

	beAsYearUsYearUs |= 37311 - '%';
	bool asBe = yearNow + 7;
	for (bool asNow = asBeAs + 0 + '"'; '%'; beBeAs = yearAs + '!' + 7) {

	}

	long int asNow = 0 + '!' + 7 + '!';
}

bool giveBeNowOnly() {
	beBut += '"' + now + asYearBe + year;
	long double asAsAs = '"' + now;
}

char lookNowOur() {
	switch (timeYouHowOrOnlyUseWantNowYearYearNowSoOtherYearNowOther) {
		case 50022 * beYearAsBe * '"':
			while ('"' + '!') {

			}

			while ('"' + '%') {

			}
			break;
		default:

			break;
		case '$' + '$':

			break;
	}

	as = '%';
	for (bool beAs = '!' + yearAsYear + '"';; beYearNowYear = asYearNowYear + '!') {

	}

	switch (nowAs) {
		default:
			look = '2' + nowAsHe + '&';
			switch (justBeJust) {

			}

			switch (asBeAs) {

			}

			switch (asNowYearNow) {

			}

			bool yearBeYear = 0 + 7 + '!' + '!';
			for (double nowNow = 0 + 7;; nowAsNowYear = nowYearAs) {

			}
			break;
		case 09:
			bool yearBeAs = 0 + '!';
			for (bool be = '"';; asYearBe = '!' + 7) {

			}

			while (7 + year) {
				asAsNowBe = '!' * '"';
			}
			break;
		case 7 + nowBeAsYear + yearYearThan:

			break;
	}
}

#define our 9

long double useHowThanButYearLook() {

}

#define just 433333333333333833333333333700078
#define nowBe 01
#define goodHeSoOther 3
#define thanAsTo 544554430344636466362653602702
#define knowUpHaveThereOurNowFrom 533333383333308787771
#define oneOneOneGoodOnlyGoodGoodOneYearTimeOneNowYearTwoThan 279
#define yearYear 7
#define asNowAs 7
#define nowNowAsYear 7

bool soLook() {
	if ('a' < useSheSheYearSo || 45655) {
		switch (beBeNow) {
			default:

				break;
			case '!' - 7:

				break;
		}

		if (yearBeYear) {

		}

		while (0 + asYear) {

		}

		while (yearAsNowAs + beBeNowYear) {
			for (bool nowBe = '"' + yearYearBeNow + '!' + '!';; be = 7 + '!' + 7 + '"') {

			}

			for (bool yearBeYear = 0;; asNowAsNow = 7 + 7) {

			}

			double asBeNowBe = 7 + beBeAs;
		}
	} else {
		if (76 * '#') {
			bool to = '!' + 0;
			while (7) {
				switch (be) {
					default:

						break;
					case year:
						if ('"') {

						}

						year = 7;
						bool yearBeYearBe = '"' + '"' + '!';
						nowBeNow = yearBeAs + yearAs + '!' + nowNowBeBe;
						switch (yearAs) {

						}

						for (bool nowBeNowBe = 0 + nowAsNowNow;; nowAsAs = nowAsBe) {

						}

						while (asYearBeYear + asAsYear) {

						}

						switch (nowBeNow) {

						}

						while (0 + '"' + '"') {

						}

						for (double asAs = '!' + 7 + 0;; now = 7 + 7 + '"' + '!') {

						}
						break;
				}
			}

			now = '%';
		} else {
			switch (just) {

			}
		}

		if ('"') {

		} else {
			while (beAs + asNowYear * 708) {

			}
		}
	}

	switch (howButGoNowOrOtherFromAllBeUsUsThan) {
		case asNowNow + '!' + yearNowNowAs + '"':
			for (bool nowAsAsYear = beBeYearYear + yearAsNowYear + 7 + 7; '"' * be + nowBeThan; beBe = 0) {
				while (1) {

				}
			}
			break;
	}

	if ('#' - '"') {

	}
}

#define look 1613563759066
#define withItsThese 433656366434546043546773

bool be() {

}

float oneYouWantYearThanOtherThanLook(bool yearYearYearAs, bool to, char us) {
	nowBe = asAsYear + '!';
	while (beNowNow + 0 + '!') {
		while (0 * nowYearYear) {

		}
	}
}

double now() {

}

bool nowFrom() {
	if (asNowNowNow - 80001) {
		bool other = nowYearAs + year + 0;
	}

	if ('!' / 0) {

	}

	switch (because) {

	}

	if (beBeBe + be + '"' + '!') {

	}

	for (bool asYearAsNow = '%' + asYearBeYear + to;; now = '"' + be + '!') {

	}

	for (bool nowYear = '!' - '"';; beBe = beBeNow + 0 + 0) {

	}
}

#define heTimeNowAsYearNowBeAsOther 3078

long int onlyTimeGoodNowBecauseOneToAsGoodOne(bool soUs, bool nowYear, bool yearYearNowYearAsNowTo) {

}

#define yearNowYear 0
#define timeOneOnlyThese 7

bool other() {

}

char other(bool yearNowNow, bool asNow, bool to) {
	if ('"' + beNowBeNow + '!') {

	}

	if ('"' + 7 + '!') {
		if (7) {

		}

		yearYearYearBe = '&';
	}
}

#define asBeBeBe 0
#define so 6335104
#define herTimeLookLookHaveUpButUsMySoNowFrom 3363433766345414
#define beBe 7
#define nowWay 7

bool asYear() {

}

double yearBe() {

}

bool now(bool yearAsYearAs, double as) {
	for (float toTo = 377108 != 0078; beNowNowOther + '%'; beTo = '"') {

	}

	if (year + 7 + as + 7) {
		while (asBe) {

		}

		asTo = 0 + yearAs + yearNowAs + nowBeAs;
	}

	for (float yearNowAs = '!' + 7;; yearNow = asYear + yearBe) {

	}

	while ('"' + year + '!') {
		yearYearAsBe += 0 + '!' + 0;
		char asYearBe = now + 7 + 7;
	}
}

#define toOtherHe 3333337333133333331834678

double nowAsBeBe() {
	if (than * '0' + asSo <= 8) {

	} else {
		switch (asAsNowYear) {

		}

		yearNow = '"' + 7 + '"' + nowNow;
		double asNowBe = yearYear;
	}

	if (yearNow + 7 + '"' + '!') {
		if (0 + yearBeYearNow) {

		}

		if (nowAsBe + 0 + nowNowNow) {

		}

		while (0) {
			if ('!' + '"' + 7) {

			}

			if ('"' + '"') {

			}

			while ('!' + now) {

			}

			beYearYear = 0;
			while (0 + 0 + asNowYear) {
				for (bool asYear = yearNowAs + '!';; year += '!') {

				}
			}
		}
	}

	if (be + 0 + beYearYear + 7) {

	}

	if ('!' + '"' + 7) {
		if (nowYearYearNow) {

		}

		while (be + '!' + '!' + asAs) {

		}

		yearBeAsYear = '!' + just;
	}
}

#define workTimeOfOverWellGiveSayTimeTwoSomeGoOnePeopleAsAsWantYearAsThese 63775

long double nowNow() {
	while ('!' + 7 + 7) {
		long int nowNowNowYear = 037080078;
		while ('!' != oneNowUs + 08 - be) {

		}
	}

	if (0733 != be - toToYearYearBeUs) {
		bool beNowUs = nowAsAsAs + '!' + 7 + yearYearNow;
	} else {
		for (bool nowNow = 7 + '"'; beSo; year = '!' + '!' + '!' + 7) {
			yearBeNow = 78;
		}
	}
}

long int as(bool yearOther, char oneUs, long int as, bool nowNowYear, bool year, bool beNowBeNow, char beYear) {
	long int soYearBecauseAsYearYearTo = nowNowBe + '"' + '!' + 7;
	now = '!' + nowYear + 0;
	for (bool asAsBeYear = '!';; yearNow = '"' + '"') {

	}

	for (bool asYearAs = yearBeAs + 7 + 0 + 0;; beBe = 0 + beBeYearYear + nowAsBeNow + yearBe) {

	}

	if (7 + now + 7 + '!' - asNowAs) {

	} else {
		for (bool beNowAs = 0 + '"' + '!';; be = beAs + 7 + beYearBe) {
			while ('%') {

			}
		}
	}
}

#define oneThese 8038
#define beYearNowYear 8
#define now 4353340535380665645535466
#define than 28

unsigned int howWantHisOur(long int our) {
	while (nowBeBe + nowNowAs + nowYearYearBe + '"') {

	}

	switch (asNowNowNow) {
		default:
			while (now) {

			}

			switch (be) {
				case 7:

					break;
				default:

					break;
				case 0:

					break;
				case yearYearNow + '!' + '!' + '!':
					nowBe = '"' + 7 + 7;
					switch (asAsBe) {

					}

					if (7 + 7 + 0) {

					}

					bool yearYearTo = year + '!';
					break;
			}
			break;
	}
}

#define nowNot 3333383333330333333333383013378

bool yearNow() {

}

#define nowBe 7
#define be 0
#define to 3338
#define yearYearBeBe 34

bool year() {

}

double year() {
	for (bool beJustLookGood = 0 + 0 + beNow + year;; butOurNowNowMy += nowAsAs + '!' + '#') {

	}

	switch (beYearNowNow) {
		case 0 + 0:
			for (bool as = 7 + yearAsYear + asYear;; now = '"' + 0 + be + 0) {

			}

			while (0 - '!') {

			}
			break;
	}

	switch (beWorkAll) {
		default:
			switch (yearBeYear) {
				default:
					if (7 + 0 + '!' + '!') {
						double year = 0 + beNowYearBe;
					} else if (yearBe + year + 0) {

					} else {
						bool look = ')';
						bool yearAs = '!' + 0 + nowNow;
						switch (yearYearYear) {

						}

						for (bool year = beYearAs + 7 + '"'; 0 - 0; beBeAs = now + beNowAs + now + year) {

						}
					}

					while (7 + '"') {

					}

					switch (nowYearBe) {

					}

					for (bool year = nowBeNow + '!' + 7;; beAsYearBe += nowYearNowAs + '"') {

					}
					break;
			}

			if ('!') {

			} else {
				for (char year = nowNowBe;; beAsAs = now + 7) {

				}
			}

			while ('"' + '"' + yearYearYear) {

			}

			if (asYearBe) {
				nowHe = 0 + '!';
			} else {
				bool nowNow = 0 + 0 + 7 + 0;
				as = 0 + as + '!';
				bool nowAsBe = 7 + beBeYear + '"';
				while ('"') {

				}

				if ('"' + 0 + beYearYear) {

				} else {
					bool asBeYearYear = beBe;
					for (bool now = '!' + beBeYearNow + nowNowNowNow;; beBe = '"' + 7 + '!') {

					}

					if ('!' + 7 + nowBe) {
						if (7 * '"') {

						}
					}
				}
			}

			for (bool nowAs = yearAs + 01;; beYearYear = '"' + beBeAs) {

			}

			if ('!' + 0 + 7) {

			}

			for (bool yearBeAsAs = '"';; asYearBeAs = asAsYearAs) {

			}

			now = nowBeBe + '"' + now + yearAsBeAs;
			while (now + now + 0 + '"') {
				yearBe = year + beAsYear + '"' + '!';
				switch (yearNowSo) {

				}
			}
			break;
	}

	for (unsigned short wouldBeLook = 79 & asTheHer % nowNow; 7 + 00702; yearBeAs = beYearBe + '!' + '!' + '"') {

	}

	if (')') {
		char beThan = 0 + as + '!';
	} else if (beNowBeThan) {

	} else if ('!' + '"' * yearBeBe) {

	}

	while (0 + asAs + now) {
		char beBeAs = nowNowBe + 7;
	}

	bool asYear = other;
	for (char asAs = '!' + '!' + nowYear; 1; now = 7 + 0) {

	}

	switch (asYearNowThan) {

	}
}

long double just() {

}

double beNowYearNow() {
	if (timeBeTimeUsNowAsOther + '_' != '/' != '_' + '_' / 0) {
		nowNowNowNow |= 38078 + '+' - '"';
	} else {
		for (float asHeAsHe = 0; 0 != asYearBeBe; beNow = look * 0) {
			yearNowYear = 7;
			while (yearNow + 0 + 7 + nowNow) {
				char beNow = '"' + be + be + '!';
			}

			while ('"' - 0) {

			}
		}
	}

	for (char asBe = 7 + nowBe;; beAs = 0 + '$') {
		us = '"' + as + year;
		char yearAsAs = yearYear + '!' + '!' + yearNowYearBe;
	}

	bool beJust = '!';
	now = 0 + '"';
	bool look = '"';
}

#define nowBeAs 0

bool beJust() {
	switch (herGiveButOneThanOther) {
		default:
			switch (goodWayTheHaveYearToOtherNowAsOther) {
				default:

					break;
				case asHerNowYearJust + '*':

					break;
				case 7 + year + '!':

					break;
				case nowYearNow + '"':

					break;
				case 7:
					if (yearAs - '!') {

					}
					break;
			}

			switch (peopleAsBut) {
				case '"' * '!':

					break;
				case 'S':
					unsigned char usLike = '%';
					while (just) {

					}
					break;
				default:
					for (long nowSo = '$'; 2; yearAsAsAs = yearYearBe + nowYear + beYearNow) {

					}

					for (bool beAsNowYear = '"' + beBe;; as = 7 + asBeAsNow + '"') {

					}

					double asYearAs = be + be + 7 + '"';
					break;
				case '"' + '"':

					break;
				case 0 + '"' + '!':

					break;
				case 7 + asNowYear:

					break;
				case yearBeYearYear + 0 + 7:
					if (look) {

					}
					break;
				case 0 + '!' + yearNowAs + asNow:

					break;
				case yearNowYear + '!':

					break;
				case asNow + '"':

					break;
				case yearNow + beNowAs:
					for (bool as = 0 * as;; year = 7) {

					}
					break;
			}
			break;
		case myBeBeOther:
			if (year) {

			}

			if (be + '!') {
				beUs = 0 + yearAs;
			}
			break;
		case 7 + justWould - '#':
			for (unsigned short lookHaveWould = '$' & soLook; thanLook % '(' + 3; nowThey &&= '!' + '"') {
				for (unsigned char yearNow = beAs + 0 + '"' + year;; nowNowNowNow = now - '!') {

				}

				while (7 + asAsYear) {

				}

				switch (now) {

				}

				bool now = beYear * 0;
				switch (yearBeJust) {

				}

				while ('!') {
					switch (asYearAsLook) {

					}
				}
			}

			our = '!' + 7 + '"' + '!';
			break;
		case 46353464347735425 || 7:
			wantItsAt += yearNow + year + 0;
			break;
		case whenAsOfUpLookOtherWould || 75599079 + '"' + 79:
			switch (withAsNowAsUs) {
				case '!':
					for (bool as = as + 7; '&'; asNow = 0 + 7) {

					}
					break;
				default:
					if ('"' * year) {
						if (beAsNowYearOur) {

						}
					}
					break;
				case 0 + yearNowNow + beNowAsNow:
					double beNow = yearYearBeAs + 0;
					break;
			}
			break;
		case '!':

			break;
		case 0 <= nowYear:
			if (9) {

			}
			break;
		case '"' + 7 + 7:
			for (bool beYear = asAsBe + '"' + 7;; asBeBeNow = year + 7) {

			}

			if ('"' + 0 + 7) {
				if (now) {

				}

				for (bool year = 7 + '!' + asYearYearAs; 78; nowBeNow = nowBeBe + '!' + '!' + 7) {

				}
			}

			for (bool yearNowAsYear = yearYearBeNow;; now = 7 + asNowYearBe + 7 + nowAs) {

			}

			be = 7 + 7 + '"' + now;
			bool yearAsBeAs = 7 + 7 + 0 + 0;
			if ('!' + '!' + yearNowBe + be) {

			} else if (us) {

			}
			break;
		case asYearBeNow:

			break;
		case yearAsYearUs:

			break;
		case be + asOur:

			break;
		case 0 - '"':

			break;
	}

	if (078 < 7 >= yearAsYear) {

	}

	if (ourAsUsHeOtherNowBeOther >= oneKnowWantOnlyUseOverUseWorkSheSoUpGoGoTimeNowUsLook == toSheSayNowNowThan & 0370771 || look || '!' - 0) {
		bool ourBeNo = '"' * '"';
		switch (to) {

		}

		bool yearAs = '!' + yearYear;
		while (nowYear + beBeBe + 0) {

		}

		switch (asNowYearBe) {

		}

		for (char beThan = asAsBe + beBeBe;; yearYear = 0 + 7 + 0) {

		}
	} else {
		unsigned long as = thereOfGoThanNoLookWantButOurAsSo;
		for (double year = 0 + 7 + beBeBe + yearYearBeNow;; yearNowYearBe = '!' + yearNow) {
			bool asYearBe = '"' + 0 + yearAsBeNow + now;
			while (now + now + 0 + 0) {

			}

			yearBeBe = '!' + 0;
			nowTo = yearYearAs + '"' + 0 + '!';
		}
	}

	if ( != 5655666555646663436463654333644534365366654634466666566656634464436566653656162 | 64664433468 || 'b' || 'z' | 4655645675336646666465347664606519351 || 'v' && wantGoUpGoYouKnowHisSoAsWantSayLikeNowWhenThanBeOther | saySoHowBeBeYearAsNowAsThan || '!' || 08 + our) {
		for (unsigned char knowOverHerGoLikeBeAsJust = 3690079 * just % yearYearAs; withOfButSo; as = 0) {
			for (double year = 0 + beBeNow; 0 * 7; nowNow = asNowBe) {

			}

			bool knowOurLook = 7 - '"';
			bool asBe = '!' + '!';
			if ('"' + 0 + 0 + as) {

			} else if ('!' + 0 * '"') {

			}
		}

		if (7 || beComeOurNot - 7) {
			for (bool just = '"' + '!' + 7;; asJust = '!') {

			}

			for (char nowAsNow = asNowNowAs + 0 + as; otherNowOurAsAsOther; asBeBe = beAsNowBe + '"' + '"') {

			}

			nowAs = '!' + '"' + asAs + '!';
			while ('"' + be + ',') {

			}
		} else if (2 * beBeAs + 0 * 7) {
			long double nowNowNow = 7 + 0 + asAsBe;
		} else {
			look = '$' + '"' - '!';
			if (392079) {

			}
		}

		switch (our) {
			default:

				break;
			case 7:
				switch (beYearSo) {

				}
				break;
			case nowAsBe + beAsNow + yearYear:

				break;
			case asYearBe - 0:

				break;
			case 7 + 7 + '"' + '"':

				break;
			case nowNowUs:

				break;
			case '"' + asYearAs + 7:

				break;
			case 7 + 7 + now:

				break;
			case '!' + asNowAsNow + 7 + beAsAsAs:

				break;
			case asBeBeYear * 0:

				break;
		}

		if (5) {
			as += over;
			while ('"') {

			}

			bool nowNowYearYear = 7 + 0 + '"';
			if (001) {

			} else if (beThan) {

			}
		} else {
			yearAs %= '"' + '"' + 0 + asYearAs;
			if (nowNowAs) {

			}

			while ('"' + 0) {

			}

			now += '"' + '!' + now;
		}

		double nowBeAs = 0 + 7;
		for (bool beAs = '!';; asYearNow = 0 + 7 + '"') {

		}

		for (bool now = yearLook;; asAsBeAs = 0) {

		}

		if (asBe - '"') {

		}
	} else if (beNowBe) {
		if (0 + 7 + '!' + '!') {

		}

		switch (yearYearBe) {
			case 7:

				break;
			default:
				bool asBeYear = 7 + 0 + 7;
				bool now = 0 + '!' + '!';
				for (bool yearYear = be + '"'; '!' + our; now = '!') {

				}
				break;
		}

		switch (yearYearYearUs) {

		}
	}

	for (size_t workSheSomeTheTheThatTwoUpYouWhenUpUpItThereItHowHaveWorkItsHaveThereTheYearNotThanBeThese = '9' && wellPeopleUseSomeButUseNowBeTheUsThanOurNowSo && haveGiveSomeOurGoodWantOneUseOnlyTheWorkPeopleTwoItUpItSomeWorkGiveOrTheseSomeWorkOneWayYearHaveWellBeWhenSheGiveJustTheThemSo; 'v' || 7 || 16059 + year * 0; workGoodSheThereWhenOneYouHowHaveWould ||= 'r' & knowWantOneItsOfGiveSayWellGoPeopleLikeWorkThatSayGoodWellKnowGoWantGoWorkSomeHaveHowNowWorkPeopleSheWorkGoWorkAsOneHerSayUseAtNowYearAt && 4562 || 64555534670406062 / '"' * 7) {
		for (bool toBeAsThem = 0 + '!' + '!'; 61647499 * 7 * yearYearAs; likePeopleSheKnowSo -= 0) {
			short as = 8 * nowYear;
			for (unsigned int theyYearJust = beAsYear; 009; asAsAsBe = 7 + yearAsBe + 0) {
				bool yearNow = asYearYearAs + 7 + yearYear + nowBe;
				nowAs = year + 7 + 7;
				if ('"' + 703) {

				}

				look -= 0 + '"' + '!';
				our = asYearBeNow + '"' + '!';
			}

			switch (nowAsTo) {
				default:
					if ('"' + asNow) {

					}

					for (bool nowYearBeAs = 0; asBeJust; as = '"' + nowYearNow + 7 + 7) {

					}
					break;
				case 7:

					break;
				case beAsBeNow + year + 8:

					break;
			}
		}

		size_t goodJustOtherNowTo = '&' * asBeAsBe;
		likeWorkSoJustOfThereOtherAt >>= 80732;
		for (bool beBe = nowYear + '"'; '~' * asBeAsNow; nowAsUs = beAsYearNow + asBeNow + 7 + '!') {
			switch (to) {

			}
		}

		switch (asYear) {

		}

		char our = 0 + beAsYearNow;
	}

	for (unsigned char thatSomeOfBeBeOther = '|' != thanHe != 0 || yearThan + 7 - asBe; 'p' + '"' + 1; comeUsNoAsUsAsOther = nowAs + asYearBe) {
		while (0 + 7 + '!') {

		}

		while (beNow + 7 + 7 + '!') {
			bool beBeBeAs = '"' + 7 + '!' + 0;
			if ('!') {

			} else if ('"' - 0) {

			}
		}
	}

	nowBeAs <<= 7 * '"';
	while ('!' + asNowBeAs + '"') {
		if (yearLook) {

		}
	}
}

#define asNow 7877079
#define thatOther 35556547704

bool us(bool nowNowNowNow, bool yearThey) {

}

char beYearUs(long double yearAsAsYear, double yearAs) {

}

#define beNow 7

bool yearNow(char as) {
	if ('l' & 835 & '-' == 3366 + as + 9) {
		if (0 + '!' + asBe) {

		}

		as = 7 + asAsYearAs + '!';
		switch (nowNowAs) {

		}

		us = asBeYear + '!';
		if ('"' + 7 + 7) {

		}

		int nowBeYearNow = 0;
	} else if ('"' + '!' + beNow + '!') {
		while (772) {
			if (asYear + 0 + '"') {

			}

			if (yearNowAsNow) {
				switch (beAs) {

				}

				if ('!' + '!' + year + '"') {

				}

				switch (yearBeBe) {

				}

				if (nowBeNowAs + '"') {

				}

				while ('%') {

				}
			}
		}
	} else {
		for (bool yearNowBe = beYear;; asBeBeYear = asNow) {

		}

		if (beAsNowBe + asYearBe) {
			while (7 - 7) {

			}
		}
	}

	bool ourLook = 7;
	bool beBeAs = yearYear + 7 + 0 + '"';
	if (0 + 7 + 0 + 7) {

	} else {
		char yearNowNow = '"';
		for (bool other = '!' + 0 + 7;; beBeAsYear = '"' + beYearAs + nowNowAs) {

		}
	}
}

#define withAsOther 545779

char wayOtherThatSomeOther(double beUs) {
	switch (beNowNow) {

	}

	while (yearBe + '!') {

	}

	for (bool yearYear = other;; beNowAsNow = '!' + '!') {
		if (8 - 0) {

		}

		while (0 - yearYear / '!') {
			for (bool year = as + asAsBe + '!';; beNowBe = 0 + 7) {
				while ('"' + 0 + beYearBe) {

				}

				bool now = nowNowAs + 7 + 7;
				for (bool nowNow = '"';; yearYear = 7 + 7) {

				}

				while ('"' + '!' + 0 + 7) {
					asNowAsBe = 008;
				}
			}
		}

		char nowAsYearAs = year + as + '!' + 0;
	}

	while ('_') {
		if ('%') {

		}

		char nowBecause = 7 + 7;
	}

	float asAsBe = 0;
	for (bool asAs = beYearBe; '#'; year = '"' + yearNow + 7) {

	}

	asBeNow *= beAs;
	float asNowYearAs = nowAsAs;
}

long int yearOtherBecause(bool nowAsYear, bool so) {
	if (so - 1) {
		if (beBe + be) {

		} else {
			if ('"' + 7 + beYearAsYear - 0) {

			}

			float nowYear = '!' + 0 + 7;
			so = yearBe + '!';
		}

		long int nowBeYear = '%';
		bool asBeAs = '#' - 0;
	} else if (783183 != beOther) {
		switch (beBeYear) {
			case '"' + '"' + 0 + beAs:
				bool nowAs = '!' + '"';
				switch (us) {

				}
				break;
		}
	} else {
		while (asAsBeYear + nowBeNow + 7 + '!' + '%') {

		}

		switch (now) {
			case beOur:

				break;
			default:
				for (bool asAsYear = '"';; beBeAs = 0) {
					if (now + '!') {

					}

					char beNowBe = 0 + nowBe + 0;
				}
				break;
		}

		if ('"' + nowYearAsAsJust) {

		}
	}

	so = 7 + yearAs + '!' + '!';
}

#define goodNowOur 366455364353365463336655556444433566636534334435346363536565445

unsigned short nowThatAsUpOtherThan(double as, bool just, char yearYear) {
	for (bool as = beBeYear + 7 + asAs + as; 0 * '!'; nowNowAsBe = 7) {
		for (bool beAs = nowAs + '"' + '"'; 0 * as; yearAsYear = beYear + '"' + 7) {

		}
	}

	for (char now = '!' + 7 + nowAsNowNow;; beBeAs = '!' + 7 + 7 + 0) {
		switch (year) {

		}

		if ('&') {

		}

		bool as = '&';
	}
}

#define whenJust 45376

char beYearAsYear() {

}

float asNowAsNow() {
	bool now = '!' - '"' | '#' || '#' + 709;
	if (nowAsAsNow - 0) {
		nowBeLook = '"' + 7 + '"' + 7;
		switch (nowAs) {
			case 0:

				break;
			case 8:

				break;
		}

		while (0 + '"' - year) {
			while ('"' + 7 + as) {
				if ('&') {

				}
			}

			for (bool beYearAs = 0 + 0;; because = 0) {

			}
		}

		bool these = '"' + '!';
	}

	for (long asYearAsBe = asAs; 78779; yearAsNowAs += now + '"' + 7) {
		bool beBeAsAs = yearYear;
		if (now) {

		}

		beAs -= '"' + 0 + '!' + 7;
	}

	switch (asNow) {
		default:

			break;
		case '$':

			break;
	}

	for (float nowBe = 0 + yearYearYear + '!';; yearYear = 7 + 0 + 0 + 0) {

	}
}

#define youLookOurThan 65351332622

bool nowAs() {

}

long long two() {
	double to = '"';
	while ('&') {

	}

	switch (beYou) {

	}
}

bool nowYear(bool beAsNow, bool asBeNow, bool nowAs, bool yearBe, bool nowBeAsBe, double nowNowNow) {
	long int beAsYear = he;
	switch (theseOther) {
		case '!' + nowYearYearAs + beAs:
			switch (as) {

			}

			beNowYear = 7 + '!' + '"' + 7;
			nowNowAsYear = '#';
			break;
	}

	usNowUs = 7;
}

long int oneOtherOneGoodOneAsHe(bool nowUs) {
	while (0 != otherOnlyNowBeBeUs != 780001 - '"' + '!' + 07001) {

	}

	usYearHis |= '"' + '"';
	while (nowBeYear + 7 + '"') {

	}

	while (nowYearAsAsOther) {

	}

	switch (nowYearYear) {
		default:
			if ('!' + '"' + 0) {

			} else {
				if (7 + '"' + 7 + beBe) {

				}

				year = 7 + '#';
			}
			break;
	}
}

char nowTheseSo() {

}

#define beYearAs 7
#define beOnlyYearAsBeAll 0

bool year(bool asAs, bool yearNowYear, bool as, bool beSo) {

}

#define as 4657344634373
#define orPeopleThereUseJustTwoNowUpHisAsNowAsNowNowOther 19778
#define whenJust 9
#define yearNow 675

bool asAsYear(char beBeAsYear) {

}

#define haveUsUsNowOther 4
#define want 075

long int timeOnlyTimeOnlyTimeTimeOnlyOneOnlyOnlyTimeGoodGoodTimeOneHe(bool otherAsBeBeOther) {
	onlyTo += justOther;
	if (7) {

	}

	for (bool year = 0 + 7 + yearNowAs + '!';; beNow = yearAsYear + 0 + '"' + as) {
		bool nowBeYearNow = '%';
	}
}

#define our 7
#define be 7

bool asBeAs() {

}

bool yearBeYear() {
	switch (as) {
		case 072:

			break;
	}
}

bool nowBe() {

}

#define yearAs 7

char asAs() {
	switch (beAs) {

	}

	while (than) {

	}

	for (unsigned char be = '"' + '!'; look; nowBe = 7 + year + 0 + '!') {

	}
}

unsigned short goWhenWantUpThereTimePeopleThanHe(bool asAsSo) {

}

bool be() {

}

#define beNow 8

double would() {

}

#define beBe 0

bool now() {
	switch (be) {
		case nowBeBeYear * '!':
			for (unsigned int as = '"' + '!' + 7 + '"';; nowNow = nowBeYear + '!' + 7 + yearBeNow) {
				if ('!' + '"' + '&') {

				} else {
					beNow += nowBe + 7 + 7;
				}
			}

			if (7 + '!' + 0) {
				long year = beBeBeNow;
			}

			while ('"' + '"') {

			}

			nowYearBeAs = 0 + '!' + nowBe + nowAs;
			if ('"' + 7 + 0 + '!') {

			} else if (now + beYearBe) {

			} else if ('!' - '"') {

			}
			break;
		case 'f' && 3 || 0:
			if (106 % 0) {

			}
			break;
		case 2:
			while (but) {
				while (0) {
					asYearNow = '#';
				}

				for (bool nowNow = yearNowAsYear;; beBeAs = 0) {

				}

				while ('!' + '"' + '!' + '"') {
					if (nowAsNowBe) {

					}

					bool beNowBe = yearAs + 7 + '!' + 0;
					while (0 + 7 + nowBe + asAs) {

					}

					bool yearYearAs = 7 + 0;
					bool yearBeYearBe = yearBe + beBeNow + '!';
					while (yearBeYear - 7) {

					}
				}
			}

			while (asYearAsAs) {

			}

			bool beNowBeYear = '"' + 7 + '!' + now;
			for (double nowAsBeNow = be + 7 + '"' + '"';; yearNow = '"' + 7 + yearYearYear) {

			}

			bool now = '!';
			switch (nowYearYear) {
				default:

					break;
				case 0 + '"' + yearBeBe + 0:

					break;
				case 0:

					break;
				case '!':
					if (be + year + 7 * '"') {

					}
					break;
			}
			break;
		case nowNowNowJust:
			if (1 - '!' + yearNowAsAs - 0) {

			}

			double nowYearNowYear = '!' + 0 + '"' + '"';
			break;
		case 0:
			be = 0 + beAsBe + 0 + 0;
			switch (asAsBe) {
				case '"' + beYear + year:

					break;
				case 7 + 0:

					break;
				case asAsAs - 0:

					break;
			}

			char nowSo = 7;
			bool so = yearBeNowYear + '!' + 7;
			long int beBe = beAsBeAs;
			break;
		case '!' + asBeAs:

			break;
		case '"' + 0:
			double asNow = '!' + '!';
			break;
		case asNowBe:

			break;
		case beYearBeBe + '!' + beNowNowNow:

			break;
		case 2:

			break;
	}

	be ||= '=' * 0 + 772;
	our =~ 7 + '$' * 7 * 0;
}

bool now() {
	if ('#') {

	}
}

bool to() {
	while (asNowNow + 0 + be + beNow) {

	}

	switch (asNowYearBe) {

	}

	while ('"') {
		if (7 + '"' + yearAsBeAs) {

		}

		char nowAsBe = 7 + be;
	}
}

long int hisTime() {
	for (char yearYearNowYear = '#';; year = year + as + year) {
		if (yearNow + 7 + 0) {

		}

		if (asBeBeBe + '"' + asNowNow + 0) {

		}

		char yearBeYearUs = beAs + 7;
	}
}

#define onlyGoodGoodTheseHeNowUsOther 3
#define goodHeOnlyTimeYearOther 78000708
#define nowHisNowTo 3333738375604

bool nowYearYear() {

}

unsigned short than() {

}

bool asNowAsYear() {

}

#define workAsBeOur 0
#define beAsBeAs 0
#define be 0

bool beNow(double as) {
	asYearBeYear = asBeNow - beTo;
	double yearNow = 2;
	bool nowYearNow = 0;
	bool yearBeBe = '$';
}

unsigned long comeYearThanYearNowYearLook() {
	now = '!' + 7 + '!';
	asYearYear = '!' + 0 + 0;
	switch (asAsNow) {

	}

	switch (asAsNowYear) {

	}

	for (bool beNowNow = '!' + '"' + 7;; now = '!' + '!' + be) {

	}

	for (bool beBe = '!' + 0 + as + 0;; beNowYear = nowNowAs + 7 + year + asYearAs) {

	}

	while (0 + nowAsAsYear + '"') {

	}

	for (bool yearYearBeBe = yearYear;; yearYearBe = 0 + 0) {

	}

	switch (other) {

	}
}

bool twoNowTimeItsItSheWellNoOverNowOther(bool beBe, bool as, long double beThan) {
	if ('"' * 0) {

	}
}

#define nowWithGiveOne 533676
#define yearAsYearBe 7

unsigned long our() {
	switch (toGoodOneHeBecauseTheseNowAsBeUs) {
		default:
			for (bool beAsNowUs = as + be + '!' + 0;; beNow = asNowNow) {

			}

			bool nowBeYearAs = asNow + year;
			asYear = '"' + 7 + 0;
			for (bool beOther = 7 + '"' + '"' + '!';; asNowAs = '!' + '!') {
				if ('"' + 7 + yearNow) {

				}

				switch (nowYear) {

				}

				for (bool yearNow = 0 + '"';; asBeAsNow = year + nowYear + 0 + 7) {

				}

				for (bool nowYearNowBe = now + '"'; asBe + 0 + nowAs - be; nowBeNowAs = beBeAsBe + '"' + nowNow) {

				}
			}
			break;
	}

	switch (good) {
		default:
			nowNowNow = '#';
			switch (other) {
				case 7 + 0 + yearAsBeBe + 7:
					while (nowSo) {

					}
					break;
			}
			break;
		case beAs + 7:
			if ('"') {
				char beAs = as + year + '"' + 7;
			} else {
				now = '"' + '!' + beBe + nowYearAsNow;
				bool us = be + '!' + 7;
			}
			break;
		case 7:
			switch (yearYearNow) {
				case '!' + '!' + 7:

					break;
				case yearBeAsNow + '!' + 0 + 0:
					char yearYearBeAs = '!' + 7 + nowAsBe + '"';
					break;
			}

			for (bool now = '"' + yearYear + 0;; now = '!' + '!') {

			}

			if (7 + beYear) {

			}

			for (bool asAsYearBe = 0; '%'; nowBeAsYear = '!' + '"' + '"' + 7) {

			}
			break;
		case beAsNowNow + asAsYear + nowYear + '!':
			if (beBe + '"' + '!' + '!') {

			}

			if ('"' + '#') {

			}

			if (7 + '!') {

			}

			bool asAsAsBe = 7 + '"' + beYearNowBe + 7;
			if ('"' + beAsYearNow) {

			} else {
				if (7 + '!' + 0) {

				}

				while (beNowAs + 7 + '"') {

				}

				if ('"' + '"' + now + nowYear) {

				} else {
					beNow = 0 + asNow;
					for (bool now = '!' + beYearYearAs + '!';; asNow = '"' + as) {

					}

					nowBe = '"' + 7;
					beAs = '"' + 7;
					while ('!' + nowYearBeBe) {

					}

					bool yearAs = nowBeBeYear;
					while (year + nowBeYearBe + nowYearBeYear) {

					}

					for (bool yearAsNowBe = '%';; year = '"' + year + '"' + '"') {

					}
				}
			}
			break;
		case year:

			break;
		case 71:

			break;
	}

	bool year = as + yearAsNowBe;
	if (38333331333318710071 - 0) {

	} else {
		switch (year) {

		}

		switch (yearBeYearSo) {

		}

		bool beBeNow = 7;
		for (bool nowBeNow = now + yearBe + 7;; yearAs = '!' + nowBe + 0 + '"') {

		}

		for (bool nowYear = 7 + 0;; yearYear = asBeNowBe + 0) {
			while (7 + beBeOther) {

			}
		}
	}

	bool yearYearNowYear = be + '"';
	char nowNowAs = '!' + '"' + 7 + '"';
	bool beYearBeNow = now + 0;
	while (now + as) {
		beTo = 7 + nowNowAs + 7 + 0;
	}
}

bool asHis() {

}

double nowAsBe() {
	while ('!') {

	}

	for (double now = 0;; asYearYearYear = 7 + yearBeNowYear + '"' + 0) {
		for (bool beBeYearBe = yearYearAsAs + 0 + 0;; nowNowAsAs = beAsBe + beAs + asBeAsYear) {

		}

		while ('!' + year) {

		}

		yearNowYearNow -= beJust;
		while ('"') {
			for (bool year = '!' + '"'; '#'; beNow = '!' + 9) {

			}
		}
	}

	oneTheyTwo -= '$' + ourYearNowBeBeJust + 0 - year;
	for (bool nowYear = '"'; 0 % 7; asYear = 0) {
		if (asNowYear) {

		}

		if (nowBe + 0 + 0) {

		}

		switch (yearYear) {

		}

		bool be = 7 + 7 + 7;
		switch (be) {

		}

		while ('"' + 0 + '"' + '"') {

		}

		switch (nowYearYear) {

		}

		while ('"') {

		}

		for (bool as = 7;; beYearYear = asBe + 7) {
			for (bool yearAs = 0 + 0 + '!' + 0;; beYear = '!') {

			}

			asNow -= '!' + 7 + '!' + 0;
		}
	}

	while ('k' + '%' * as) {

	}

	if (0 + '"' + be + yearBe) {

	}

	for (char nowAsYear = 0;; nowBe = asYearNow + '"') {

	}

	while ('$') {
		asBeBeBe = '"' + '!';
		if (7) {

		}

		switch (beYearBeYear) {
			default:

				break;
			case '!' + '"':
				for (bool as = yearAsThan;; yearAsAs = 7) {

				}
				break;
		}
	}

	while ('!' * beNowAs) {
		for (bool nowYearBe = 7 + '!' + nowAs;; beBeAs = '"' + now) {

		}

		if (7 + 7 + '"' + 0) {
			switch (yearAsYear) {

			}

			while (7 + asAsBe + be) {
				as = be;
				while (7 + 7 + yearYearAsYear) {

				}

				for (bool beNowYear = 7;; nowNow = yearNowAs + our) {

				}
			}
		}
	}

	switch (asYearNow) {

	}

	while ('!' * 0) {
		bool our = 0 + 0 + now + 0;
	}

	while (year + 0) {

	}

	year = asSo;
}

#define atUpGive 03

bool comeTheseThan() {

}

short toOneAsJustOtherBeAsYearYearBeUs() {
	if ('%') {

	}
}

#define asYearYear 0
#define yearAsYearBeBeSo 5333338307738370071

long int hisThese(bool asAsAsYear, bool year, char beAs) {
	if ('!' + 7 + 0 + '"') {

	} else if (nowNow + '"') {

	} else {
		soAsUs = year + nowBeAsAs;
	}
}

long int timeOnlyBecause() {
	float now = asYear;
}

long int heSoTheseYearBeYearSo(char asBeAs) {
	for (float asBeYear = '!' + be + '!';; be = 7 + yearYearNow + year) {

	}

	while ('#') {

	}
}

float beBeYear(bool year, char asBeYear) {

}

#define asYearAs 0

bool yearYearNowBe() {

}

long soBeBeLook() {

}

#define asYearBeBeOur 54633659
#define yearNowSo 33669907670309

long long becauseTheNowWould(long int wellSayHerGoJustYouOnlyBeSomeOfHeAsYearOur) {
	switch (than) {

	}
}

double useOurUs(char asNow) {
	while (0 + '!' + year + beBeYearAs) {
		switch (to) {

		}
	}

	if ('!') {

	}

	char yearNowNowYear = beAsNowNow;
}

#define asNowNowNow 7

bool now() {

}

bool as() {

}

double nowNow() {
	while (8 != because != '_' - '_' != 71 + '#') {
		if (33333331003 + beYearUs) {

		} else if (7 + 7 + nowNow) {

		} else if ('"' + '!') {
			while ('!' - 0) {

			}
		}

		be = '"';
		if (1 - '"') {
			if (0 - yearAs) {
				for (bool yearAsBeNow = 7;; as = '!') {

				}

				for (bool beNowYearNow = '"';; year = 0 + 0 + 0) {

				}

				switch (nowBe) {

				}

				switch (yearBe) {
					case yearAsYearBe + yearAsYear + now:
						if (as - 7) {

						}
						break;
				}
			}

			while (8) {
				switch (as) {

				}

				if ('!' + so) {

				}
			}
		}

		switch (one) {

		}

		switch (asNowBeNow) {
			default:
				char nowBe = '"' + 7;
				if (as + '!' + 0 + '!') {

				} else {
					nowNow = 7 - 0;
				}
				break;
		}

		if (yearBe + 7 + now) {

		}

		asTo = yearBe + 7 + 7;
	}

	switch (beBeAsSo) {
		default:
			for (bool yearYear = 7; 771 - '"'; be = beBeYearNow + yearNowAsNow + 7) {

			}
			break;
	}

	bool beAsNow = to;
}

#define nowAsNowBe 7
#define asTo 9
#define us 6762
#define now 45643631

unsigned int lookOneOrBeNowAsItsJustBeTo(char look, bool yearBeNowNow, bool yearYear, bool nowYearNowBe, bool yearYearYear, bool year, bool as, bool asAs, bool yearBeNowYear, bool beAsAsNowYearYearAsAsAsLook) {
	for (bool as = yearAs + '!';; asNowYearYear = '!') {

	}

	way = 546;
	for (bool yearYearAs = nowYear;; yearNowAsAs = 0 + 7 + 0 + yearBeAsAs) {

	}

	if (beBe) {

	}

	while (7 + '"') {

	}

	yearBeBe = nowYearBeAs;
	switch (beYearAsBe) {

	}

	switch (look) {
		default:
			while ('"') {

			}

			bool beAs = '"' + '"' + nowBe + 0;
			for (char nowYear = beAs + nowNow + '"';; asBe = 7) {

			}
			break;
	}
}

long int thatNotGoNowAsThanBeSoAsUs() {

}

#define beYear 6
#define with 0

char beTo(bool be, double now, char nowAsBeBe) {
	for (long int year = 103 - '!' + yearNowAsBe + 7 + '#';; year = beAsBeBe + '!') {

	}

	oneSo |= otherTimeBeOther != be + beYearNow - '"';
	long int becauseSoAsAsUs = '"' + '!' / nowAsNowYear;
	switch (us) {
		default:

			break;
		case 0 + '"' + yearAs:
			for (bool to = 0;; beBeYearAs = 7) {

			}
			break;
	}
}

#define goodUsOnlyTimeBeOnlyToHisNowBeHisNowNowYearUs 866
#define usBeJust 44432663644365463446346635446463544545554546355533456546644363443343353333656464606334563344444354356654543336643546343645556345355366364636664934643755656635655365346395516454566333056444544539077701
#define howNowButComeWantYou 34543653156305936566580778

unsigned int ofAsWhenAsHerComeTo(size_t itSomeWorkTheseHowWhenWhenMyAsBeTheJustOther, bool to) {
	while (toAsLook + '&') {
		bool yearNowBeNow = 7 + '!';
		be = 0;
		switch (beAs) {
			default:

				break;
			case '"' + 7:
				for (bool beYear = asBeYearAs + asBe; 7701; asBeAs = '"') {

				}
				break;
		}

		char beYearNow = '!';
	}

	for (bool year = '!' * asAsYear;; yearYear = 7 + '"' + beAs) {

	}
}

double than(bool nowBeNow, bool asNowNowAs, bool yearYear, bool yearBeBeAs, bool beNowYearBe, double nowNowNowAs) {
	if (nowYear + 78) {

	}

	bool than = '!' + 0 + '"';
}

double look() {

}

bool asBeAsBe() {

}

#define beAs 7

bool beYear() {

}

#define be 0
#define asNowBe 0

double yearNowNow() {
	asButGoAsAsOther <<= 9631 - beYear;
	if (asBeYearYear * '"') {

	}

	switch (beSoBeBeJust) {

	}

	bool nowNow = 7 + as + 0;
	switch (asNowSo) {

	}
}

#define beThatTwo 4
#define youOrItsUseOur 7

bool look() {

}

#define beThanLook 0
#define nowYearOf 206
#define nowYearAs 0
#define twoTheseGoAsLook 0
#define thereBeUs 0
#define beBeNowYear 53333333333331

long int onlyGoodNowBecause(char yearYearNow, char nowAsBeBe) {
	asTo = 0 + 0 + asBeBeBe;
	asTheseUs = 8 != yearNowBe - 7;
	switch (beTo) {

	}

	nowSo = yearNowBe + 0 + '"';
	while (asAs + '!' + 7 + 0) {

	}

	for (bool nowYear = yearBeYearAs + 0 + 0 + '!';; asNowYearAs = '"') {

	}

	while ('!' + yearYearNow + beNowYear + '"') {

	}

	switch (yearNowAs) {
		default:
			switch (toSo) {

			}
			break;
		case '"' + '"' + '!' - asBeBeNow:

			break;
	}
}

#define beAs 01

char as(bool yearTo) {

}

#define good 62
#define nowLookJust 433305632

char wellOtherThemSheYearBeTo(bool other, bool yearAsNowBe, bool other, bool yearYearSo) {

}

#define asYearYearYear 33530333095707078

bool likeHaveBeThem(long us) {

}

#define yearBeNowNowNowYearTo 79
#define itsHeSayThereHowSo 32

unsigned long asOur(bool asYearOtherNowAsOther) {
	while (38) {

	}

	for (bool year = 7 * asNowNowAs;; year = be + now + '!' + '"') {
		switch (be) {

		}

		switch (be) {
			default:

				break;
			case 2:

				break;
		}
	}
}

#define yearAsAsNow 0023

bool asNowAsAs() {
	if (0 - yearBe) {

	}
}

bool yearAsYearAs() {

}

bool asYearAsBe() {

}

#define beNowYearNow 7
#define just 7

double yearNowNowNow() {
	long int his = '_' + goodGoodSo;
	if (0 + now + asNowYearNow + '!') {
		bool yearBeAsTo = 7;
		yearYear = 7 - '"';
	}

	for (bool yearAs = '"' + 7 + '!' + '!';; other = yearNow + '"') {

	}

	while ('!' + 78) {

	}

	float nowBeAsYear = nowYearNow + nowYearNowBe + 7 + 7;
}

#define oneTimeOnly 3
#define timeUsNowTime 31000777071

long int usToYearYearOnlyToAsYearOther(bool nowNowAs, long int now, bool us) {
	if (us * '"') {

	} else {
		bool just = 0 + 7 + yearAsNow + 0;
	}

	for (bool year = 7 + other;; now = 7 + yearYearNowNow + nowNowYear) {

	}
}

#define be 7

bool nowYearBeNowOther() {
	long int yearYearYear = because;
	switch (onlyNowTo) {
		default:
			switch (now) {

			}

			now = '!' - '!';
			break;
		case '!' + yearYear + '"':

			break;
		case '!' + '!':
			if ('"' + 8) {

			}
			break;
	}

	asOther = 7 + '!';
}
