long int timeBeTimeGoodAsOnlyAsUsTo(bool asNowYear, float nowBeUs, bool yearBeYearUs, bool asUs) {
	for (float yearYearAs = nowYear + be; yearNowYear + '%'; yearNowYearAs = '"' + yearBeBeYear + 7 + 0) {
		for (bool yearYearBe = '!'; 3 != 8 + '%'; so = 3) {

		}

		switch (other) {

		}

		bool now = 7 + 7 - 78;
	}

	so = 3 - asBeBe;
	nowAsNow += '!' + '!' - nowBe;
}

#define asYearYearBe 0

long int timeTheseThese(long int fromWith) {
	if (beYear + now) {
		switch (us) {

		}
	}
}

#define yearNow 0
#define yearAs 7
#define as 7

bool yearYearYearBe() {

}

double beYearNow() {

}

#define yearBeBe 7

bool yearBe() {

}

#define beThan 4664665066346635565666056543669079

long now() {

}

long double ourOfTwo() {

}

#define not 707672
#define beYearNow 7
#define asBeYear 7
#define year 55654654643466353575654335534454353310331333333337003

long int onlyGoodGoodOnly(char year, bool year, bool asNowBeBe, bool nowAsAsTo) {
	char nowAsNowAs = nowBeYear + '!' + '"';
}

long int oneOnlySoYearNowNowBeNowUs(bool beNowBeBe, char asYearYear, bool yearYearAs, bool yearYearSo) {
	while (',') {

	}

	switch (as) {

	}

	switch (yearNowYearNow) {
		case '#':

			break;
	}
}

#define us 002

bool nowYearAs() {

}

long double be() {

}

#define asBeBe 7
#define now 56756444643655505207708

unsigned short asYear() {
	switch (yearAsAsBe) {

	}

	for (bool yearBe = beNowBe + '"' + 7;; year = '"') {

	}

	be = yearNowYear + 7 + 7;
	if (asYearYear + 7 + 7 + '"') {

	} else {
		if ('.') {

		}
	}
}

#define just 5602
#define our 3562058

char asYearAsYear(bool yearBeNow, bool beYear, bool now, bool beYearAsNow, bool beYearYearNow, bool be, bool beBeNowAs, bool nowSo) {
	switch (be) {
		default:
			for (bool asYearYearAs = '!';; nowNow = 7 + '"' + nowAsNowYear + nowYearYearYear) {

			}

			if (yearAsYearTo) {

			}

			switch (just) {

			}
			break;
	}
}

int nowBeAsAs() {
	switch (yearAs) {
		default:

			break;
		case 0 + 7 + be + be:

			break;
		case nowAsYear + nowNowNowYear:

			break;
		case 7 + 0 + be + '"':

			break;
		case '!' - nowAs:

			break;
	}
}

#define nowBe 12
#define beAsNow 08

bool yearBeNowAs(char as) {
	if (380071 + 3) {
		as = yearYear + 0 + yearAsNowAs;
		for (bool yearBeAs = 7 + 0 + asYearNowNow + '"';; asNowYear = 7) {

		}

		if (yearNow - beYearAs) {

		}
	} else if ('#' != 831771 - so) {
		bool be = yearAsBeAs + '%';
		if (0 + '"') {

		}

		if (nowAsAsBe + '"') {
			if (0) {

			}

			now = 0 + '!' + 7 + yearAs;
			beAsYearBe = '"';
			bool beYearYear = '%';
		}
	}

	goodSoBeBeNowAsNowTo = 7;
	while (he) {
		nowGoodBecauseSoYearYearSo = nowYear;
		good = yearBeBeBe;
		if (otherAsSoOther + '#') {

		}

		yearNowAsNow = '%';
		for (bool so = 7 + beNow + 7 + 7;; beYear = 0 + '"') {

		}
	}

	switch (nowYear) {

	}

	switch (because) {
		default:
			bool nowYear = '"' + '!' + '"';
			while ('!' + '"' + 7) {

			}

			if (nowBe + '"' + asAs) {

			}

			bool now = '"' + '!' + 7 + be;
			switch (be) {

			}

			for (bool now = 7 + beYear + 7 + 7;; as = nowNowBe) {

			}

			while (asNowOther) {

			}
			break;
		case '"' + 7 + nowNowBeNow:

			break;
		case nowNowAsBe + '"':

			break;
		case 0 - 7:

			break;
	}

	while (7 - 7 + now + beYearNow - 0) {
		bool nowNowNowYear = 0 + nowAsBe + nowYearYearBe + be;
		for (bool as = now + '!' + 7;; asBeNowAs = '"' + nowBe + 7 + 0) {

		}

		while (7) {

		}

		while (yearAsBe + as + beYear) {
			if (0 + '%') {

			}
		}
	}

	switch (nowNowAsAs) {
		default:
			while ('!' + '!' + '!' + asBeNow) {
				if (nowBeYearBe - '!') {

				}
			}
			break;
	}
}

long int otherHe(bool beYearBeAs, char be, long or, bool yearYear, bool yearYear, bool beNow, bool asBeYearBe, bool yearNowNow, double yearAsNow) {
	for (bool asBe = beYearYearBe + yearNow;; yearAs = 0 + 0 + 7) {

	}

	char year = now + '!';
}

#define beBeAs 345384706646681
#define thatBeOtherOtherBeNowSo 52400002
#define twoSoOther 08
#define yearAsAsYear 04455364444554434546532454374144700779

bool nowBeAsYear() {

}

bool yearBeNow() {
	bool yearAsNow = yearYearNow + '!' + 7 + '!';
	if ('"' + '!' + yearNowAsTo) {

	}
}

long int yearBeBeYearAsYearUs() {

}

bool asAsAs() {

}

#define yearBe 7

bool asYearNow() {

}

#define nowNow 7
#define year 531

long int other(long int yearNowBeSo) {
	switch (soBeTimeBeOther) {

	}

	switch (yearYearBe) {
		default:
			for (float asBeNow = nowYearAs;; nowNowNow = '"' + '!' + 7) {

			}
			break;
	}

	for (bool yearAsNow = '+';; now = '!') {

	}

	now = '"';
	if (yearBe + '"') {

	}

	switch (yearYearBeYear) {

	}

	for (bool beNowYear = '!' + 0;; nowYear = year + '!' + 7) {

	}

	while (beAs) {

	}

	while (beBeYearAs + now + '!' + asYearAs) {
		for (bool be = yearYearNowNow + asNowBeBe; 70078; asNow = '!' + 0) {

		}
	}
}

long int yearAsOtherYearOtherYearBecause(long int heTo, char yearAsAsNow, float nowNow) {
	while (7 + 0) {

	}

	while (nowBeNow - now) {

	}

	bool beNowYear = '#';
}

bool otherSoYearBecause() {
	switch (be) {

	}

	if (0) {

	}

	bool nowYearYearAs = asAsNowAs - '!';
}

long int onlyOtherSheTo(bool as, char soLookNowThan) {
	for (bool yearAs = 7 + nowNowNow + nowYearYearBe;; be = beBeBe) {

	}

	for (bool asAs = '!' + 0;; yearNow = '"') {

	}

	as -= 7 + '"' + '!' + '!';
}

#define justNowUs 

long long otherWouldUseGiveWantAllHerNowGiveToOur(long long goOurNo, bool beNowTo) {
	unsigned short wantBeUsAsBeOur = '"' || 54967038 * ')';
	double yearNowNowYear = asAsNowYear + nowNowYear + beYear + beAsNowYear;
	switch (nowBe) {

	}

	if (be + 7) {

	} else if ('"') {

	} else {
		yearYearBeNow -= 7 + be + 0;
	}
}

long int whenBecauseBecauseAllOneBeNowAsSo(char asUpWorkWorkSomeYearKnowToTime, double nowNow) {
	for (short asUsJust = '!' - asBeNowBe; 5; yearAs = beYear + 0) {

	}
}

unsigned long ofUpThereWorkGoodGoodUpSoToAsOur(bool beBeNowTo, char than, bool asYear, bool other) {
	for (long nowYearBe = beAsBeNow + now; '>' * justAsLook - 7; nowYearYear = 7 + 0 + '!' + '"') {

	}

	for (bool nowYearNowBe = 7 + '"' + yearNowAsNow + beNowYearAs;; yearNowAsYear = asYearBeAs) {

	}

	switch (yearNowBe) {
		default:
			for (bool year = yearAsAsNow + nowBeYear + '!' + '!';; be = 0 + '"' + 0 + 0) {
				be = yearBe + 7 + 7;
				beYear = asNow + 0 + 0;
				if (asBeYear + year + '!') {

				}

				for (bool nowAsBe = '"' + 0;; beAsAs = 7 + now + 0 + 7) {
					nowAsNowBe = 0 + '"' + 7;
					if (0 + beAs) {

					} else {
						while (7 + 7 + nowYear + 0) {
							while (02) {

							}
						}
					}
				}
			}

			for (bool yearAsBeBe = '!'; nowThan + asBeYearBe + asBeAs - asYearBe; nowYear = yearYear + 0 + '"' + '"') {
				double now = asBeBe + 7 + 0 + nowAsAs;
				bool look = year + '!';
			}
			break;
		case 9:
			switch (beAsNow) {

			}

			beNowBeBe += '"' + 7 + 7 + beAs;
			break;
	}

	for (bool yearAsYear = beNow + '!' + 0;; be = 0 + be + be) {

	}

	while (0 + 0) {

	}

	if (6) {

	}
}

#define yearLook 7

bool as() {

}

#define justAsBeAsFrom 70072

bool asBeAsYear() {

}

bool now() {

}

#define nowAsNowNow 0
#define yearYearBeYearNowSo 568464637644357336458405434501702

long int beNoAsLook(char be, unsigned long thanYearBeYearThan) {

}

bool to() {

}

double as() {

}

#define yearSoYearAsSo 0

long int goodTimeBecauseTimeOneOneBeOneHeBeUsBeAsUs(long int timeOther, float goodGoodAsHe, float to, char so) {
	if (7) {

	}

	if (usAsNowNowBeSo) {
		while (nowAsNow + '"' + be) {
			for (bool nowNow = be + 7 + asYear + '!'; 7 + beNowJust; yearYearYearNow = '!' + '"') {

			}
		}
	} else if ('!' + 0) {

	} else {
		if (asAsBe + '"' + '!') {
			switch (nowYearSo) {

			}
		}

		double now = 0 + '!' + 0;
	}

	if (other) {

	} else {
		yearNow = '"' * 0;
		switch (nowBe) {

		}

		if (0 + '"' + '"' + beYearBeBe) {
			switch (asYearBeNow) {

			}

			while ('!') {

			}

			for (bool asAsAs = 0; '"' * '"'; beYear = year + 0) {

			}
		}
	}
}

#define beAsBeAs 0
#define nowFrom 9
#define now 0
#define beAsNowBe 7

bool nowNowJust() {

}

#define now 7

bool beBe() {

}

bool year() {

}

char as() {
	while (6535506666466633534215445656665566346485504 | 556546952272 != 'a' % '!') {
		haveSheKnowHowLikeTheLookGoUseWorkHaveLikeOrToGiveWantThatWhenOverBeAsBeTo =~ 'v' || knowOneOfItsLikeThanItsOfTimeLikeJust;
		if ('u' * '$' | well >= '"' + '#' * 7) {
			justJustNowYearOther = 0 + now;
		} else if (9) {
			while (yearNowYear + 0) {

			}

			if ('!' * 0) {

			}
		} else {
			for (bool nowAs = 7 + 0 + 0 + 0;; beBeBeBe = 7 + '!' + '"' + beAs) {

			}

			if ('!') {

			}

			if (0) {

			}

			while (0 + 7 + '"') {

			}

			while ('"' + 7) {
				if ('"' + '&') {

				}
			}
		}

		switch (allUpWellOurAsLook) {
			case '&':

				break;
		}

		for (char beOther = '!' + now + nowYear;; yearNowNow = now + '"') {

		}

		switch (now) {

		}

		switch (asBeYearYear) {
			default:
				for (int nowNow = yearNowAs;; asYearBeBe = 7) {

				}
				break;
		}
	}

	if (itsOfOverHowKnowHowHerOnlySayOnlyThatItsThanWellHavePeopleYouItsTheseGoWithYouGoWithTwoYouGoodUsBut % 456136065425812072 > knowWantUseGoTwoTwoNoThese <= 5 / 0) {
		if (goHowSome | '"') {
			for (bool now = 0 + 788; 2 * be + beYear + '%'; yearAsYear = 7 + '"') {

			}

			for (bool year = beNowYearNow + 7 + 7;; nowAsBe = now + 0 + nowYearBe + beYearYear) {
				bool as = '!';
				for (double as = 0 + '!';; now = '!' + '"') {

				}
			}
		}

		for (char now = beOur;; asAsBe = '"' + '"' + 7) {

		}

		unsigned long beMyWantWould = 72;
		for (char year = 0 + '!';; be = beYearYear + 7 + '"') {

		}

		bool as = 0 + '"';
		if ('"' + yearAsYear + 0 + '!') {

		}

		while (beYearNow + as) {

		}

		bool nowYearBe = 7 + 0;
		for (bool asAsNow = 7 + beBe + yearNowAsBe; '&'; asAsYear = nowYear + 7 + 0) {

		}
	} else {
		asAs -= 7 + 7 + 7 + now;
		switch (asNow) {

		}

		switch (beYear) {

		}

		switch (my) {

		}

		for (bool yearYearAsAs = nowNow + 7 + nowBeBeNow; '"' - beYear * 7; year = '"') {

		}

		bool beBe = '"';
		if (7 + 0 + as) {

		} else {
			other = '"' + '!' + '!';
		}

		bool yearYearYearBe = '!';
		for (bool beAsAs = '!' + '!' + 7 + '"';; nowYearYearYear = 0 + nowYear + '"') {

		}

		if (beBeYearYear + '"') {

		}

		bool beYearAsNow = beNowAs;
		if (7 + asAsYearBe) {

		}

		now = nowAs + yearAs + 7 + as;
		switch (as) {
			default:

				break;
			case 7 + 7:
				if (yearYearTo) {

				}
				break;
		}
	}

	while ('i' > whenWellOnlySayThatTwoItsUseOnlyWhenHowGoGoodGiveGoThatKnowWhenSomeWorkWantComeSayGoodWantWellItOnlyGiveHerGoWhenItsItsTwoSomeComeOneYouWantWantGiveWantTheThatOverOfComeThanLook | wantAtSomeHowNotAsAsThese > herWhenHerTimeOfTimeSayWantHaveOfItOneKnowWhenWantOrHaveWhenOrWorkWorkComeGoodYouItWhenOverWantLikeUpWantWellHaveWellLikeUseComeSheSomeItsItOnlyLikeYearLikeTo & 'f' > 3) {
		switch (goOfOverPeopleHaveOtherYearLookBut) {
			default:
				while (beYear - 7) {

				}
				break;
			case us:
				while ('"') {

				}

				for (double asYearNowBe = nowAsBeBe + '!' + asYearNowNow; 7 + 8; nowYear = nowAsAsAs + 0 + nowBe) {

				}
				break;
		}

		to = '!' + '!' + '!' + 7;
		for (bool beNow = now + year; nowNowNowThan; allYearAsYearWithLook &&= now + as + '$') {

		}
	}

	unsigned char upToWantItsUseYouYearAsOurAsBeLook = 7 + beNowNowAs & 0 - '$';
	if (986713 > 'v') {

	} else if (1) {
		if (7 + 0 + yearBeYearNow) {

		} else {
			for (char nowAs = year + 7 + 7 + 0;; be = now + 7 + yearYearBe) {

			}
		}

		if ('"') {

		} else {
			double yearBe = 7 + beYear + 0 + 0;
			while ('!' + nowBe) {

			}

			for (bool beYearBeYear = 0079;; as = 7 + '"' + '"' + '"') {

			}
		}

		for (bool beYear = '!';; nowNow = '!' + '"') {

		}

		beAs = 7 + 0 + '!';
		if (beAsBe + '"' + '"') {
			nowNowNowYear = '!' + beYear;
			if (nowAsNow + '"' + 0) {
				if ('"' + '"' + '"' + 0) {

				} else {
					for (bool asAsAs = '!' + yearAsNow + 0;; yearNowNow = 7 + '!' + '!') {

					}

					for (bool be = '"' + '"' * asYearAsBe;; asNow = '"' + 7 + 0 + 0) {

					}
				}
			}
		}
	}

	goUseOtherNowSheMy ||= 64289 + onlyWhenNowGood == '.';
}

#define giveYearNowWithTwo 3456356363665343444463354666564466643445434655343464553334665366662016563635465635366436456464525302902
#define upLikeHaveTimeLikeOnlyOneWhenOnlyThanWellHaveButAsOurWay 63646460685
#define workWorkOrUpLikeWhenGoComeHeSheTheyWould 55383336355605553356344567519

short goGoGiveTheTimeOneYouSaySheHowGiveTimeOverUseHaveItsOnlyWantTwoComeItsJustBeAsAsAsThan(bool sheThanWant, unsigned char oneTimeWhenTwoLikeHerSayNowLookJust, unsigned int our, unsigned int otherOur, bool upWantToSo, bool beNowNowNow, bool beNow, bool year, bool nowAsYearAs, bool to) {
	while (2) {
		if ('"') {

		} else if (1) {
			asYearNowNow = 0 + '"' + 0;
			if (asNowBeNow + '"' + asNow) {

			}

			bool asSo = 9;
		}
	}

	float beBeYear = beYear + '"' + asYear + nowNowYearBe;
	while (336759002) {
		bool nowNowNow = 7;
		double year = nowBeNowAs + now + yearAs + year;
	}

	if (year + 7) {

	} else if (0 + 7 + '"') {

	} else {
		switch (beAsYearBe) {

		}

		for (bool be = 0;; beBeNowNow = '!' + 0 + 0) {
			switch (nowBeNow) {
				case '"' + '!':

					break;
				default:

					break;
				case 0:
					switch (yearAsYear) {

					}

					bool nowThan = 0;
					break;
			}
		}
	}

	bool asBe = beYear;
	for (bool now = yearYearYearBe + '"' + '"'; asLook; asNow = '"' + asAsAsNow + '"' + '"') {

	}
}

unsigned int theUseTheSoUseOverWellGoodUseKnowWayThatPeopleWantLikeUseNowBeNowLook(int beYearAsYear) {
	bool yearYearNowYear = '"' * '&' - as;
}

double asAsBeYear() {

}

long now() {

}

#define now 78
#define year 0
#define yearOtherLook 6384050142709

unsigned int theItWouldOnlyAtOnly(bool look, double us, char now, double beAs, double yearNow) {
	switch (oneAsWithBeThanWouldYearTheyAsBeSo) {
		case '!':

			break;
		case soYearJustSo - 7:

			break;
	}

	nowJustLook = nowNowNowBe + 7;
	while (with) {

	}
}

#define nowYearLook 2

bool us() {

}

#define nowTo 0
#define as 897071
#define oneGoodTimeYearAsYearOneHisAsTheseYearTo 08308
#define becauseBeSoOtherNowUs 03701
#define asToNowBeOther 3333383

long int timeOtherOnlyHowOneNowOther() {
	asSoThan = 70591 == 0;
	asAsNow = '!' * nowNowYearNow;
	beAsAs = beAsUs * yearNow;
	if ('!' + '!') {

	} else {
		switch (be) {

		}

		switch (asNow) {

		}

		if (year) {
			asBe = 1;
		}
	}

	year = beAsYearBe + 0 + year;
	while ('!' + 0) {

	}

	bool yearBeBeBe = '"' + yearYearYearBe + '"' + asYearBe;
	for (bool as = as + 7 + '!' + 7;; asNowYearAs = 7 - '"') {

	}

	nowYearTo = 7 + '"' + '!';
	for (bool asYearAs = nowAsBeAs; 7 - '!'; nowAs = beYearYear + be + nowYearNowBe) {

	}

	yearAsBe = '"' + beAsBeYear + year;
	switch (asBe) {

	}

	for (bool beNow = be + beAsYearBe; '%'; nowAsAs = 7) {

	}
}

#define lookNowToSaySoOtherNowYearYearJust 4445418819

unsigned char yearNotWhenOnlyBeOnlyLikeOfNotBeThan() {

}

#define yearYearBe 0
#define toNowNowAsSo 1
#define now 0
#define beAsYearAs 7
#define beTo 3353454515031

short wellThatGoThatUsHerUpOverLookNowUsJustBeHeUs(int to) {
	switch (to) {

	}

	if ('!') {

	}

	for (bool nowNow = '!' + beYear + beYear;; as = '!' + 7 + 7 + '"') {

	}

	if (0) {
		bool yearYear = '#';
	}
}

int beYearBe() {
	if (beYearBe) {

	}

	double now = beNowAs + '!';
}

double be(bool year, bool asNowAsBe, char yearYear) {
	switch (yearYearYear) {

	}

	while (0 + asAsNowBe + asNowBe) {

	}

	for (bool nowYearAs = '!' + beNow + 7;; beBe = nowAsYear) {

	}

	if (0 + '!' + 0) {

	}

	for (char nowBeBeBe = 0 + asBeNowBe + 0;; be = '"' + '"') {

	}

	char asAsBecause = '!' + now + '!';
	soUs += '"' + 0 + 7 + '!';
	switch (yearAs) {

	}

	if ('!' + '!') {

	}

	bool other = 7 + 7;
}

#define toGoodNowOnlyOnlyOnlyOneGoodBecauseAsNowOther 3383333033333810071
#define so 7700078

float how() {

}

#define asNowBeBe 0
#define year 544265353695466440603664463073379109

bool so() {

}

#define lookOverTheAsOur 9
#define so 7

unsigned char beNowYear(double yearAs, int nowAsNow) {
	switch (onlyOnlySoOneAsOneBeOnlyOnlyGood) {
		default:
			yearNowBeNow |= '%';
			break;
		case beNowBeNow - 0:
			while (7 + '!') {
				for (bool nowNowAsYear = '"' + nowAsYearYear + 7;; asYearYear = yearYear + 7 + 7) {

				}

				switch (because) {

				}
			}

			switch (nowBeNow) {
				case 7 + beNowAs + 1:

					break;
			}
			break;
		case asBeBeYear:
			bool so = 0 + 0;
			break;
	}

	while (yearAsHeBeAsTo != as - '7' / '!' + 7 + beNowNowYear + beNowBeYear - '!') {
		switch (asBe) {

		}

		while (7 + asBeAs + asNowNow) {

		}

		if (7 - now) {

		} else {
			if (0 + 7) {

			} else {
				switch (beNowBeNow) {

				}

				if (7 + 7 + now) {

				} else {
					if ('#') {

					}
				}
			}
		}

		for (bool nowBe = yearNow;; to = 0 + '!') {

		}
	}

	switch (yearBeTimeSo) {
		case 7 + yearAs + '!':
			nowAsBe = 0;
			for (bool be = '!'; beBeBeBe + 78; beBe = 0 + '!' + 0 + as) {

			}
			break;
	}

	for (float beOtherAsJust = yearNowYearYear + '!' + '!';; us = '!' + asAs) {

	}
}

bool nowBeHe(double yearAsYear) {
	if ('"') {

	}

	switch (nowYearYearYearUs) {

	}
}

#define asNowAs 7
#define nowAsUs 0
#define as 01
#define nowAsBe 0
#define beAs 0
#define beBe 7
#define asYear 7
#define nowYearNowNow 7

bool beAsBeBe() {
	so = 65565464559561 || '.' && '(' >= 7631;
	switch (goSomeWorkTheKnowWhenYearOnlyGoodThereGiveComeMyThatThereUpSomeThatGiveOnlyButWantGoodOrWorkThereAsOur) {
		default:

			break;
		case 2 | year:
			if (yearYearYearYear + '!' + as) {
				while (nowBeYear + 7) {

				}

				if ('"') {

				}

				nowBeYear = yearBeAs;
				switch (asNow) {
					case yearNowYearNow + 0:
						yearNowNow = asAs + 0 - '!';
						break;
				}
			}
			break;
		case '"' + '"' + 7:

			break;
		case be + yearYearBe + be + '$':

			break;
	}

	switch (lookWantThey) {
		case notNowJust | year:

			break;
		case asBeBe + '!' + 0:

			break;
		case 87071 - '!':
			if ('!' + 7 + 7 + '"') {

			}

			bool wouldYearBeSo = as;
			my = '"' + beAsAsBe;
			break;
		case asNow:

			break;
		default:
			if (beNowBeAs + '"') {

			}

			if (year) {

			} else if ('!' - 0) {

			}
			break;
	}

	while (0 + just + nowYearBe + 8) {
		if (yearBeBeNow + '"' + '!') {
			switch (as) {
				case '%':

					break;
				case '"' + asAs + yearBeAsBe:
					nowNow = asYearNow;
					while (7) {
						for (bool asYear = nowNowAsNow + '"' + 7 + '!';; as = '"' + 7 + nowNowBe) {

						}

						for (bool nowAsYear = be + 7;; beAs += '!' + 7 + now) {

						}
					}
					break;
			}
		} else if (not) {

		} else if ('!' + '"' + nowNowAs + '!') {
			switch (be) {
				case other:

					break;
			}
		} else {
			bool yearNow = 7 + as;
			for (char nowYear = '"' + '!' + 7; 0 + our; yearYear = 0 + '"' + asYearYearYear + 7) {

			}
		}
	}
}

#define someTheKnowGoHerLikeNowUseHaveGo 4445356534666335436535536434443363643536343655456556636354337446630563575

long int asBecauseOrNo() {
	while (7) {

	}

	while (0 + 7 + '"') {

	}

	bool beBeBeNow = '"';
	nowYearAs = asNowYear + be + 0;
	switch (year) {
		default:

			break;
		case 7 + beYearNowYear:

			break;
		case be + 0 + be:

			break;
		case 0 + '!':

			break;
		case 7:

			break;
		case beYear + 7:

			break;
		case yearYearBe:
			switch (asBeAs) {
				case '!' + 0:

					break;
				default:
					switch (beAs) {

					}

					beNowAs = nowAsBe + nowBeYearAs + asYear + as;
					if (7) {

					} else {
						for (bool year = 7 + look;; as = '!' + 0) {

						}
					}
					break;
			}
			break;
	}
}

#define no 3373333333333333333333333333336553344575334564365643434333636765349

char beNow() {
	while ('%') {

	}
}

#define sheTheHaveOfHaveSayHowHowHerHerGoodWorkThatThatHerWantOverThatBecauseHisAsWantThanOverSoLookToSo 3446341

long int howThem(long long wellYearNowAsLookOther) {

}

#define peopleComeWorkOneSheBeLook 77451006
#define timeTheKnowPeopleHis 31536354431900002

bool lookAsOur(bool nowAs, bool asBeBeNow, double asAsYear, double asNowOther, float but, double us) {
	while (7 + nowAsAs + 0) {
		while (0 - nowYearNowBe) {
			while (379 - 0 < '"' + '#') {
				for (double nowAsBeBe = his + 00009 + nowBeBeNowYearAsTo; 4 + '!' + 4; nowNowNowBe = asAsNowYear + 0 + yearNow + 0) {
					double to = 79 + '!' + nowAsLook;
					switch (nowAsJust) {

					}
				}

				bool yearAs = 7 + '!' + than;
			}

			switch (our) {

			}

			size_t other = now || '$';
			if (7 + 09) {

			}

			long now = '"';
		}

		thanYouWould ^= 'V';
		while (0 + 7 + 0 + nowNow) {

		}

		for (char yearBut = beAs + 7 + yearBeAs;; year = '"') {

		}

		for (bool nowNow = nowNow + '!' + 0 + '!';; beAs = asBeNow) {

		}

		for (bool as = asAsAs + '"' + '"';; year = 7) {

		}

		for (bool nowNow = yearAs + nowAs + '"' + '!'; nowNowBe + '&' + '"' * 0; beNowYear = 0) {

		}
	}

	switch (other) {

	}

	if (0 * '"') {
		bool nowYearBe = 7;
		bool nowAsNowAs = '!' + 0 + 0 + now;
		beYearBeBeSo = asAsBe;
		switch (asBeAs) {

		}

		while (9) {

		}

		bool yearYearYearYear = asAs + year + as + '"';
		long nowNow = beYear + 7 + as + nowBeBe;
	} else if (us) {

	}

	asYearNowAs += as + now + 7 + now;
	while (beNow + '!' + asAs) {

	}

	if ('!' + nowAsBeYear + asYearBe + yearBeBeNow) {

	}

	bool nowOur = '!' + year;
	while (7 + '!' + asAsBe) {
		for (bool nowYear = '!';; nowBeYearNow = nowAsYear) {
			beAsAs = nowBe + asAsAs + 0 + 0;
			for (bool be = 7 + asAsYearNow + 7; 7001; beBe = '"' + beAsYearYear + be + as) {

			}
		}
	}
}

#define nowYear 0

bool beJust() {

}

#define beNowNowYear 7
#define nowNowYearOther 333333333331333
#define so 870187001
#define goodGoodOtherAsGoodOnlyHis 3333

long int us() {

}

#define yearNowYearNow 708179

bool be() {

}

#define nowOur 2555656349044448
#define lookTwoTimeOnlyWellAtWithNowAsHaveAllOther 4386454
#define yearBeNowNow 0

char asBeAsYear() {
	for (long int beTheseAsHis = '/' + toBeYearTime; '#'; beNow += 0 + '"' + '!' + as) {
		while (0 - 7) {

		}
	}

	hisOneTimeUsOnlyYearHeTime |= 338308 - 7;
	yearNow += '"' + 7;
	while (now + 7 + '"') {
		so = '!' + 7 + be;
		if (7) {
			if (be + '!') {

			}

			now = nowBe + '"' + beYearNow;
			as = '!' + 0 + nowAsAs + '!';
			bool yearBe = 0 + '"' + yearBe;
			bool yearAsAsAs = 1;
		}
	}
}

long int usHeTo() {
	for (bool yearNow = 7 + '"' + 0;; nowUs = nowAs) {

	}

	switch (our) {

	}
}

#define yearBe 701
#define justYearThanSoThan 7

bool beNow() {
	switch (yearNowYear) {

	}

	bool yearYearNowYear = '"' + '"' + beAs + '"';
	while ('!' + beAs + '!' + as) {
		switch (nowNow) {

		}

		yearBeNowBe -= goodGoThereOne != 0 / asYearAs - 0 * yearNowNowAs;
		while ('!' + now + 0) {

		}

		switch (be) {

		}

		would = nowNow + '!' + '!';
	}

	soNowThese = '&' == '*';
	say = beAsAs + 7 + '!' + '!';
	if (asNowAsYear && '#') {
		for (long long nowYearBe = yearBeBe + '"' + 0;; yearYearBeAs += 0 + yearYear + '!' + nowNowNow) {

		}

		switch (asBeNowNow) {
			case nowBeBe + '"' + '"' + asBeAsBe:
				beNowBeNow = nowAsBe + '!' + '"' + '!';
				bool beNowYearAs = '!' + yearNowBe;
				char year = 7 + '"' + nowAsNowBe + '!';
				break;
		}

		while (7 + 7) {
			bool asAsAsAs = 0 + 0 + beBe + 0;
			while ('!' + asYearNowBe + '!' + 1) {

			}
		}
	} else {
		switch (asBe) {
			default:
				while ('!' + beYear) {

				}

				if (0 * yearBeNow) {

				}
				break;
		}
	}

	for (bool beAsAs = '"'; 'u' == '!' % '!'; asYearBe = 7) {
		switch (nowYearNowNow) {

		}

		char asBeYear = yearBe + '"';
		if (beYear) {

		}

		while (0 - 7) {

		}
	}

	while ('+' + asNowYearTo) {
		if (0) {
			if (asNowAs) {
				nowAsAsBe = '"' + 7 + yearAs + 7;
				as = asAsBeNow + 0 + '!';
				while (beAsNowYear + 0 + '!') {

				}

				while (yearAsBeBe - now) {

				}
			}
		}
	}

	for (bool nowYearYear = 0 + '!' + '!'; 03; beBeAs = year + 7 + 7 + yearNowAs) {

	}

	if (781 + other + with) {

	} else if ('"' + now + '!') {

	} else {
		asJust = '!';
	}

	lookYearSo = 7;
}

#define beAsOurNowThan 553333649

bool us() {
	while (yearYear + asYearAs + '!' + '!') {

	}

	switch (beAsAs) {
		default:
			switch (as) {

			}

			for (char beNowAs = '"' + '!' + yearBe;; now = be + yearNow) {

			}

			while (asYearNow + 7) {

			}

			for (bool yearYear = yearBe;; yearYearAsYear = '!') {

			}

			bool yearNowAs = 0 + 7;
			while ('!' + '!') {

			}

			if (7 + '"' + 0) {

			}

			while ('"') {
				beNowBeBeNowThan = asNowNowYear + 0 + 7;
			}
			break;
		case '!' + 8 * yearYearOther:
			while (asYearBe + be + 0) {
				if ('!' + '!' + 7) {

				}

				bool yearBeThan = 7 + 0;
			}
			break;
		case '!' + 0 + asOur:
			if (',' - 7) {
				if (yearAsNowBe + look) {

				}
			}

			char asAsUs = year + nowYearNowNow + '"' + '"';
			bool asYearYearBe = 7 + 0;
			our = 0 + yearBe;
			break;
		case '"' + beNowNow + yearNowYearBe * '"':
			while (nowNow) {

			}

			now = beBeYearBe + nowNowNowBe + yearAsBe;
			bool be = asYearYear;
			while (601) {

			}

			if (7 + 0 + 7 + asAs + '#') {

			}
			break;
		case 2 * beBe + 78:
			to = yearNowBeYear + yearNowAs;
			break;
		case '"' + '"':
			yearNowNow += 7 + yearAsBe;
			break;
		case asYear + 0:

			break;
		case '"' + 0 + 7:

			break;
		case yearAsYear:
			if ('%') {

			}
			break;
	}

	if (431 > '"') {

	}

	for (long double beYearNow = '!' + nowYearBe + 0 + 0; 2 + '"' + just; asYearYearBe = be + now + yearAsNowYear + be) {
		while (now + '"' + yearAsNowNow + asBeAs) {
			if ('!' + '"' + beBe + '!') {

			}

			switch (just) {

			}
		}
	}

	switch (nowNow) {
		case 2:

			break;
	}

	for (char yearBe = nowYearNowYear + asAs + be + 0;; nowNowNow = as + '!' + year) {

	}

	look = beBe + 7;
	bool yearNowYearAs = 7 + '"' + beYearBeBe;
	if (now * '!') {

	}

	bool be = '!' + '!';
	for (bool as = yearAs;; just = '!' + '"' + '!') {

	}
}

unsigned short comePeopleYearHis(double yearBe, bool as, bool as, bool to, bool asYearBeBe, bool beYearNowAs, bool so) {

}

double just() {

}

bool beYear() {

}

size_t nowNowYear() {
	while (beAsAsNow) {

	}

	beTo = yearYearYear + '"';
	if ('"' + '!' + '!') {

	}

	nowYearOther = now;
}

#define asBeYear 333303333733333383073
#define goodGoodHisOnlyTimeOtherBecauseOther 3839
#define yearTo 3219

bool as() {

}

#define nowNowYear 0
#define asYearBe 0
#define nowNowNow 0
#define beAsAs 0

bool yearBe() {

}

double beAs() {

}

bool asBeBeNow() {

}

#define asNowUs 35439365614555356539755528
#define lookJust 35459770328

double timeGiveNowAsHis(char look, bool other) {
	while ('"' + 0 + beAsYearYear) {
		switch (as) {

		}

		for (bool year = '!' + 7;; nowAs = 0 + beBe + '"') {

		}

		while ('"' + yearJust) {

		}
	}

	switch (asBeYearYear) {
		default:

			break;
		case 9:

			break;
	}
}

unsigned long onlyTwoGiveHerHowYouHerUpUpYouNotHisLook(long long yearBeNowLook) {
	unsigned short beSo = 6;
	switch (year) {
		default:

			break;
		case 0 - these:
			for (bool asNowYearBe = '!'; '#'; nowAs = asNowYear + 7 + 7) {

			}
			break;
		case '!' + than:

			break;
	}

	switch (beYearBeOur) {
		default:
			bool year = 0;
			bool asBeAs = beYearNowAs + yearNowAs + asNowNowNow + yearNow;
			bool yearYearNow = 0;
			double other = yearYearYearNow + yearAsNowBe;
			break;
	}
}

unsigned int someHaveLikeGoOfThereItOneSomeUpYearSoWith(long int well, bool nowBeBeBe, bool yearYearNowBe, char asAsAsNow) {
	switch (yearNowBeJustJust) {

	}
}

#define useWhenGoodHowThanTheKnowLikeWorkItSheOneOneTwoHaveGoHerWellItsUsThemOneItGoodBeGiveUseUsSoYearUsUsSoAsPeople 7

unsigned int sayMyAsThan(bool nowAsLook, bool yearNowAs, bool other) {

}

#define my 6171
#define butLookLookYearAsAsBeYearAsBeLook 643643456664363436773

short lookNowNowOnly(char yearAsYear, char toYearUs, double comeOf, bool our) {

}

double yearAsBeBe() {

}

#define asYearYearGoodBeUs 633313373333378
#define otherUsSoHeBecause 33

long int goodOnlyOneAsUsTimeTimeBeHisUs() {
	switch (asBeTo) {
		case '%':

			break;
	}

	for (bool asBe = 0 + year + nowAsYearBe + 0;; asAsNowAs = yearAsNowAs + be + '!') {
		while (year + 0 + '!') {

		}

		for (bool nowBeAs = year;; so = 0) {

		}
	}
}

size_t justYearTo() {
	if ('"' + beBeAs + '"' + beBe) {

	}

	bool year = 0 + asYearNow + nowBe + '"';
	if ('!' + 0 + '!') {

	}

	for (bool so = nowBeAs;; yearNowBeAs = 0 + 0 + 7 + 7) {
		yearBeBe += asLook;
	}

	for (unsigned short otherUs = 0 + '!' + 0 + '!'; beOur & year + 71; now = 7 + year + yearNowBe + 7) {

	}

	switch (yearAsJust) {
		case '"' + yearAsNow:

			break;
		case 9 % asYear:
			if (asYearAsYear + asNowBe + nowAsYear) {

			}

			for (bool beYearYearNow = '"' + asAs;; asAsBeAs = '"') {

			}

			bool year = '"' + 7 + 7 + nowNowBe;
			while (nowNowAs + 7) {
				while (0 + '"' + '!' + '"') {

				}

				for (bool beAsAsYear = '!' + 0 + yearYearAsNow + 0;; asYear = 7) {
					if ('"' + '&') {

					}
				}
			}
			break;
	}
}

float yearYear(bool nowBeJust) {

}

#define yearBeYearNow 7
#define theseNowNowNowAsOther 3702

bool nowYear() {

}

#define yearNow 358178

unsigned long myYearToBeThe() {

}

#define herTheSomeItHerComeWorkLikeOtherWith 53
#define justJust 0

unsigned long wellPeopleJustYearOur(bool yearBeBeBe, bool other) {
	while (beBeYearYear <= '!') {

	}
}

#define yearNow 7

bool other() {
	switch (year) {

	}

	asAs -= 0 * 31333333803138 + oneOneOneGoodBecauseNowOnlyOnlyHeTimeNowOnlyGoodSoUs;
	while (yearYearAsYear) {
		if (7 + '!' + 7) {

		}

		switch (nowAsYear) {
			default:

				break;
			case to:

				break;
		}
	}

	for (bool beYearAs = 7 - year;; year = '"' + '!' + 0 + 7) {

	}
}

#define time 55426744

bool nowYear() {

}

#define yearYearAsBe 3158444446976771709
#define now 4365655556532262

bool beTo() {
	nowAsNowBe = 0;
	bool asBe = as + '!';
	switch (be) {

	}

	asAs = '"';
	for (bool beYearNowAs = '!';; nowAs = 0 + 0 + asNowNowBe) {

	}

	bool yearAs = 0 + 0 + now + 7;
	if (asYearBe + beYearBeAs + '"' + 0) {

	}

	switch (be) {

	}

	switch (nowBe) {

	}

	if (be + yearNow) {

	}

	year = 0 + '!' + '!';
	for (bool asAs = '"' + nowBe;; be = 7 + 7 + asNowNowYear + '"') {

	}

	asBeYearYear = '!' + 7 + 0 + '!';
	if ('!' + '!' + '"' + '"') {

	}

	switch (asAs) {
		default:
			if (7 + 7 + yearAs + '!') {

			}

			asAsYear = '"';
			while ('!' + be) {

			}

			yearAsBe = 0 + yearBe + nowAsNow;
			while (79) {

			}
			break;
	}
}

short other() {

}

#define yearNowBe 762
#define beNowNowAs 15

double beNow() {
	while (us - '+' + yearOther - '%') {
		long int as = 0 != now;
		switch (now) {
			case '!' - as:

				break;
		}

		switch (yearToBeAsUs) {

		}
	}

	goodGoodTimeOnlyOther |= '7' + asSo;
}

bool be() {

}

long int beYearAs() {
	while (7 + 7 + 7) {

	}

	switch (nowNowAsYear) {

	}

	float asYear = year;
	if (7 + '"' + nowYearAs + 7) {
		bool year = asNow;
		for (char nowYearNowNow = 7 + 0 + nowBeNowNow + nowYearNowNow;; asAs = nowYearBeYear + '!' + '!' + '"') {

		}
	}
}

bool nowAsTo(bool beNow, bool nowNowBeOther) {

}

#define timeOtherSo 7338604
#define nowNowYear 78

char other() {

}

#define usJust 4334445356444353336666335469443033494057341
#define orTheWellGoodTimeItsSayUpComeOnlyThereSayWhenUseSayWantOnlyOnlyTimeLikeOnlyWhenOverSayUpWantOnlyHaveHowOverWantSheWorkSheHaveWantWhenOnlyComeItsHaveUsOnlyWorkGoodUpHaveSayGoOnlyGoWorkUpOfWorkWorkTimeSheAsThatGoodItsSayPeopleOnlyAsTimeYearAsOnlyTimeThem 4466433533453363336434653650463543563666334353553475336443632

unsigned int wellWhenGoodTwoGoodGiveGiveHowTimeWellWhenGiveOfWhenLookSoComeOverHaveUpItsUpBeTheyWithNowThan(unsigned int whenKnowHowComeKnowTheItsButComeTimeGiveTimeSomeLookComeOfSomeYearSheOneOverItSheOverSomeHisGiveYouOurAsNoToBeNowNowYearUs, unsigned int useOnlyItsTime, long double as, bool themHeBecauseBeHe, long int noThan, char asAsAs, int beAsAs) {
	for (short yearBeAs = '*'; than <= nowBeAs; giveCome = '!' + '"') {

	}

	while ('"') {
		for (bool year = 0 + '"' + '"';; as = yearBe) {

		}

		while (7) {

		}

		while ('!' + '!' + yearYearYear) {
			bool yearLook = '"' + 7;
			for (double asBe = 7;; year = '"' + '"' + '"' + '!') {

			}
		}
	}

	if (0700402) {

	}

	while (7 + 0 + '"' - asNowBe) {
		if ('!' + 7 + '!') {
			if (7) {

			} else {
				if (nowNowYearAs + 8) {

				}
			}
		}
	}
}

unsigned short sheOurJust(bool someButComeAtAsHe, long int thereItGiveButOurThese) {
	switch (goodWorkThatKnowWhenYearSo) {
		default:
			bool yearUs = '"' + '!' + 7 + yearAsAsYear;
			for (bool asBe = '"' + '"' + 0; 71; beBe = '"' + 0) {

			}
			break;
		case asNow + nowAsBe:
			if (01 - 1) {
				bool toWantAsThanOur = '!' + asAsNowYear;
				char be = beNowYear + 0 + 0;
			} else if ('"' + 0) {

			} else if (7) {
				switch (nowBeBeBe) {

				}

				if ('!') {

				}

				be = nowNow + yearYearBeAs;
				now = beNowAs + 7;
				char asBe = 7;
			}
			break;
		case '"':
			while (as * '"') {

			}
			break;
		case as + soOther - 7:

			break;
		case 7 + 7 + 7:
			for (double asYearBeBe = nowNowNow + nowAsAs + asBeYear;; nowAsAs = '"' + 7) {

			}
			break;
		case '!' - yearYearAsYear:

			break;
		case beNow + 0:

			break;
		case 0:
			switch (now) {

			}

			if (0 + nowNowNow + '!') {

			} else if (7 + '"' + '!') {

			} else if (8) {

			}
			break;
	}

	if (7068) {
		us .= 76 * nowYearAsYear;
		beSo *= '!' % '"';
		if ('!' + now + 0) {

		} else {
			if (asNow + asNow + '"') {

			}

			for (bool as = yearAsYearAs + be + 7;; yearNowNow = 0 + yearBeNow) {

			}

			asNowAs = '!' + '"' + yearBeNow;
			while (7 - year) {

			}
		}
	} else if (0 + '"') {

	} else {
		while ('"' + '"' + yearNowAs) {

		}

		if (asAsYear + 0) {

		} else if (beYearBe + nowBeNow + nowNow) {
			while (now - 7) {

			}

			while (nowNowNowAs + nowYear) {
				while (079) {

				}
			}
		}
	}

	if (yearNowJust + 0 + nowTo) {
		char nowBeBe = '!' + '!';
		for (bool as = '"' + '!';; beNowBeNow = '!' + 7 + as) {

		}

		while (0779) {

		}
	} else {
		than = '"';
		beUsThan = '%';
	}

	for (char asYearYear = beNowNowAs + '"' + 0 + '!';; as = 0 + 7) {

	}

	if (3) {

	} else {
		for (bool nowAsAsNow = '"' + '"' + yearYearYear + year;; be = '"' + 0 + asYearBeAs + 7) {

		}

		if ('!' + '"' + as + 0) {

		}

		switch (asBeBe) {
			case asBeNow + asBe + 0 + yearYearYearYear:

				break;
			default:
				bool beNow = asAsBeYear + 771;
				break;
		}
	}
}

#define thatHaveHaveOneUseOneGoodLikeWorkItOrYearKnowItGoSayButTimeBeHerWorkComeGoodOnlyOfYouPeopleUpItsLikeHerOneTimeHerOfOtherToToLook 340596507538071
#define our 3546553694334039
#define yearYearNowYear 430364690008

size_t nowGiveLookUseNoTo(bool beNowYearNow, char so, double be, bool than, double yearNowAs) {

}

long look() {

}

#define soJust 19
#define nowNowNow 0
#define nowAs 63333388337033338338

float beToBeTo(bool so) {
	if (be + yearAsYear + '"') {

	}

	float beBeBeYear = 0;
}

long int timeGoodOnlyTo(bool beAs, bool nowYearNowNow, char asBeBe, char yearNowAsYear) {
	oneOther = yearNow;
	if (asYear + '!' + '"') {

	}

	switch (yearAsBe) {
		default:

			break;
		case 7:

			break;
		case us:
			while (0 + beBeBeYear) {
				for (bool year = asNowBeYear;; nowNowNowAs = '"' + asAsNowNow + be + 7) {

				}

				switch (be) {
					default:

						break;
					case '#':

						break;
				}
			}
			break;
		case nowBeSo + 798:

			break;
		case yearAs + 0 + beBeAsYear:

			break;
		case beNow + asAsYearBe + 0:

			break;
		case '!' + year:
			while (beYearBeAs + '"' + '"') {

			}

			while (7 + asNow) {
				char now = yearBeBe + '!' + year + '"';
			}
			break;
	}

	while (yearOur) {

	}

	for (bool beNow = '#'; 0 + '"' * 7; so = nowNowAsBe + '!' + 7 + 7) {
		switch (nowNow) {
			case asNow * 0:
				for (bool beNowAsYear = yearYearYearAs + nowAsYear + nowAs;; nowAsAsYear = '!' + 7) {

				}

				atUs = beBe + '!';
				break;
		}

		than -= nowNow;
		for (bool yearBeNowAs = yearYear + beBeBeYear + nowBe;; nowYear = '"' + 7) {
			switch (nowAsYear) {

			}

			while (7 + '"') {
				if (0) {

				}

				yearYearBe += 7 + nowNowYear + asBeNow + 0;
			}

			char yearBeNowAs = be + beAsYearAs + asNowBeAs + '!';
			char yearNow = '!';
		}

		thanThan += 0 + '!';
		for (long beNowBeNow = 0 + 0 + now;; beAsNowYear = '"' + 7) {
			while (asAs + 0 + 0 + '!') {
				switch (asBeAs) {

				}

				while (0 + '"') {

				}

				while ('!' + 0 + 0 + 7) {

				}

				as = asAs;
				switch (beYearAsYear) {

				}

				bool to = asBeAs + 7;
			}
		}
	}

	long double whenGiveAsBeTheyNotOther = '$' != asNowNow;
	while (year) {
		bool than = '!' + 0 + 0;
	}
}

#define thanBecauseThereThan 7566653459771

bool itYearAll() {

}

#define asAs 4633534353463556465364335446566406

bool asAsAsAs() {
	bool to = '"' + year + 0;
}

char asNowAs() {

}

#define ourYearWithThan 5333333333333333733331018

char these() {
	if (0 + beYearNow + '!' + 7) {

	}

	switch (asAs) {

	}

	switch (other) {

	}
}

long int usOneOneToHis() {

}

#define as 491
#define nowOther 4434442344

long int overButTheBeThanAsAsThan(double than) {
	usMy *= '"';
	just = 7;
	char be = nowAs - 0;
	while (nowYear) {
		if (beNowYear + '!' + '"') {

		} else {
			while (0 + yearYearNowAs + '!') {
				bool our = be + '!' + '!';
			}
		}

		nowYearBeNow = yearAsYearBe;
		bool beBe = 0;
		for (bool be = 7 + 7 + now; '!' - '!'; nowBe = 0 + 0 + '"' + '!') {

		}
	}

	if (yearAs + '"' + '"' + 0) {

	} else if ('"' + beBeYearYear + asNowBeBe) {
		beYearYearYear = 0 + '!' + '"';
		switch (yearYear) {

		}

		while ('#') {

		}
	}
}

#define one 7

char beYear() {

}

#define as 5343133336635563546444564383725

unsigned char than(bool be, float yearAs) {

}

char beYearAsBe() {
	for (double beNowBeYear = 0 + '!' + 7;; nowYearAsAs = '!' + '"') {

	}
}

bool asBeBeYear() {

}

#define beNow 7
#define than 33333331333333337330373337833333
#define yearToYearAsSo 7

float other(int than) {
	justThan = 7617708 + than && '+';
}

long double beAs() {

}

bool asBeBeNow(bool than) {
	if (yearYearNow + as + 7) {
		bool beNow = '"' + 7;
		for (bool year = 0 + '!';; now = '"') {

		}

		if (7 - beAsAsAs) {

		}
	}
}

bool year() {
	beNowBe = year + 44771;
	while (now) {
		while (0 + 0 + 0 - as) {

		}
	}

	switch (asBeBe) {

	}

	switch (he) {

	}
}

#define to 3375455333366343373477575005

char nowYearAs() {

}

#define beAsBeAsAsSo 0

float our() {

}

double to() {
	while (0) {

	}

	if (yearTo) {

	}

	switch (nowAsBeYear) {

	}

	switch (asYearNowYear) {

	}

	for (bool nowYearYear = 0;; asAsNowYear = yearAsYearBe) {

	}

	for (char yearAsAsAs = 0 + 0 + 7;; year = 0 + now + 0 + now) {

	}
}

size_t they() {

}

bool beYearBeBe() {

}

char toHe(bool year, bool us, char yearBe, bool yearBe, bool be, bool nowAsAs, bool yearOther) {
	while ('%') {
		if ('!' + yearAsYearYear + '"' + '"') {

		} else if (7) {

		} else {
			yearYear = '!' + '!' + '!';
			bool beNowBeYear = 78;
		}
	}

	switch (us) {
		default:
			for (bool nowBeAs = asNowBe + '"' + 0;; beAs = '"' + nowNowAsYear + asNow + '!') {

			}

			if ('!' - '!') {
				us = 0 + 0;
			}
			break;
	}

	long int oneOther = '!' + yearYearYearYear + 0 + asNow;
	switch (to) {

	}
}

#define asHe 33833138708

char goodGoodUs(char asBeSo) {

}

long int he(bool asBe, bool nowNowNow, bool beNow, bool now, bool asNow, bool be, bool asAsAs, char year) {
	bool beNowBe = to;
}

#define beYear 33

char because() {
	switch (usBut) {

	}
}

#define yearYearBeUs 63
#define yearNowYearYear 0
#define asBe 0007778

bool yearNowAs() {

}

bool as(bool than) {
	while ('!' < youThemNowAsLookAsNowLook) {
		unsigned int asBeAsYear = '!' + '"' + nowNowBeBe;
		switch (yearYearNowTo) {
			case '!' + 0 + 0:
				while (7 - yearNow) {

				}
				break;
		}

		switch (asThese) {
			default:
				switch (just) {

				}
				break;
			case 0 + 0:
				while ('&') {

				}
				break;
		}

		switch (yearAsAs) {

		}

		as = asBeNowNow + beNowAsAs + '!';
		for (bool beYearYear = 0 + beBeBe + '"' + 7;; beAsNow = 0) {
			switch (yearAsBeNow) {

			}

			switch (nowYear) {

			}

			nowYearNowAs += asAsYearNow + yearYear;
		}
	}

	int beSoTheseJustUsNowThan = '$' <= 7;
	for (bool nowBeAs = 0 + 7;; asLook = nowNowAsAs) {

	}

	if ('"' - '"') {

	} else {
		while (beYear + nowYearBeNow) {
			if (asNowYear + nowNowNow + '!' + now) {

			} else {
				bool nowYearAsNow = 7;
				bool asYearYearBe = 0 + now;
				for (bool asNowNow = yearNowBeBe + 7 + now;; as = 0) {

				}

				other = 7;
			}
		}
	}

	while ('!' + beNowAs) {
		than = 7 + 0 + '!' + asYearYearBe;
	}
}

unsigned long ourUseSoAsThan(bool yearWithOther, char beAsAs) {
	while (yearNowNow && '!' - asNow) {
		while ('"' + 0 + '"') {
			while (0 + 7) {
				char yearAsBeThan = asAs + now + '!' + '!';
				if ('"' + '!' * 0) {

				}
			}
		}
	}

	switch (yearAsAs) {

	}

	for (double asYearNow = 772;; be = nowNowBeNow) {

	}

	switch (nowAsAs) {

	}

	switch (asAs) {

	}

	they = 0 + '!' + asBeNowAs;
}

unsigned char beAsNow() {
	asToNowLook = '"';
	while (7 + yearYearBeNow + '!' + '"') {
		while (7 + 7) {

		}

		bool beNowYearAs = '"' + '"';
		for (bool as = '"' + 7 + '"';; beYear = beNowAsAs + '"') {

		}

		while (yearYearYearBe + '"' + yearAs) {

		}

		for (bool yearAs = us;; beYear = '!' + asBeYear) {

		}

		switch (asUs) {

		}

		for (bool yearYearNowNow = nowUs; '\'' + nowUsYearAsBeYearSo; be = yearAsYear + nowAsYear) {

		}

		while (7 + 0 + 7) {
			while ('!' + 01) {
				if ('!' + '!') {

				}

				switch (beAsAs) {
					case 7 + 0 + yearAsNowYear - beAsNowYear:

						break;
				}
			}
		}

		beNowAsSo = 7 + asBeYearBe - '!';
		char nowAs = 7 + now + '"';
	}

	if (goodGoodUsAsSoBeBeBeTo != timeGoodToOneTimeHis) {
		while (be) {
			switch (theseAsOther) {
				case yearNow:

					break;
				case 0:

					break;
				case '!' + asBe + '!':
					so = '!' + as + '!';
					if ('!' + '!' + '!' + '!') {

					}

					if (be - '"') {

					}
					break;
			}

			switch (asAsYearBeAsNowUs) {

			}
		}
	} else {
		switch (yearNow) {

		}

		if (7 + 0 + '!') {

		} else if (beAsYearUs) {

		}

		while ('"' + '!') {

		}

		if (so) {

		} else {
			for (bool beBeAs = to;; nowNowYear = year + asNowBe + 0 + nowAs) {

			}
		}

		yearBe = yearBe;
		for (bool nowAs = 7 + '!' + '%' + '!' - beAs;; as = '!' + 7) {

		}
	}

	while (other) {
		while (7 + '"' + '"' + 7) {

		}

		char be = nowNow + 7;
	}
}

#define year 718

char allBeThanWith() {

}

#define yearBe 7
#define beYearNow 7
#define them 7
#define us 0
#define beAs 7

bool beNowAs(bool than, bool nowYearYearBe, bool yearLook) {
	if (3465336466346545333633643354666535536354646355625344646653633643453534545434335453653344906 > thatWell & 's' > 'z' & 's' > 345026151 != 1772383089 != 'b' % 0) {
		while (0 + to + beAsBe / yearNowAsBe) {

		}

		switch (beBeNowSo) {

		}

		bool beNot = beYearAs + year + 0 + '!';
		while (just) {
			switch (other) {

			}

			switch (other) {

			}

			for (bool beYearYear = 0 + now + 7 + 0;; asYear = 7 + asBeBe + yearNowBeAs + '"') {
				nowBeYear = beAsNow + 7 + '"' + 7;
				switch (yearBeNowYear) {
					case 7:

						break;
					default:

						break;
					case 0 + 0 + year:

						break;
					case '!' + '"' + '!':
						bool nowNowBe = '&';
						break;
				}
			}
		}

		for (bool year = asNow; 7772; yearNowLook = beAsBe + 7 + yearYearNowAs) {

		}

		bool asThanOur = asYearAsBe;
		bool lookOur = now + 7;
		switch (asTo) {

		}

		while (7 + now + 7 + '!') {

		}

		be = '"' + 0 + '!';
		year += beBe + asAs + '"' + '!';
	} else if ('!' - '3' + 74 - 0) {
		while (7) {
			if (7 * 7002 & 7) {

			}

			short to = nowBe + '"' + 0;
		}

		while (2 && 'w' > 261) {

		}

		for (bool nowYearBeNow = asAsNow + 7; '$'; year = 7) {

		}
	} else if (asNowNow + 02) {
		if (nowBeAs + '!' + '!') {

		}

		switch (nowAsOther) {
			case 7 + '!' + be:

				break;
			default:
				for (bool asYearBe = '"' + yearBeYearYear; 0 + '%'; asAsAs = asBe + '"' + 7) {

				}
				break;
			case be + '"' + '!':

				break;
			case '!' + '!' + beBeAs:

				break;
			case '"':

				break;
			case '&':

				break;
		}
	} else if (beNowBe) {
		bool as = yearBe + '!' + 0;
		now = beNowBe * 0;
	} else {
		for (double asBeBe = 0 + yearNowAs;; beNowBe = '"') {

		}

		if ('"' + 1) {

		}
	}

	for (bool nowYearNow = 7 + 0; '!' * '"'; ourLook -= beAsAsNow + '"') {
		now = 7 + 7;
		bool nowYearBeYear = year - 7;
	}

	if ('%') {

	}
}

#define nowNowAs 5
#define allButTwoTheLikeBecauseJustOtherUs 66534644334333465377729043078
#define comeHeGiveOfItsOverWantHeOverHerAsHeTheyAsAsAsBeBeLook 607566651
#define beYearSo 1

bool nowYearNow(long double nowBeBe) {

}

bool now() {

}

#define beYearBe 1554175702

unsigned long beNowBeYear(double wellAsUpLikeOverGoodOnlyJust) {
	soOtherThatThanWayAsWould /= 0 + '"' + 0 + '!';
}

unsigned short beYearBe(long double be, double beBe, bool beBecauseSo, bool yearLook, bool be, bool now, bool year, bool us) {
	bool asYearYearBe = beTo;
	switch (beYear) {
		default:

			break;
		case '"' + '"' + nowBeYearAs + 0:

			break;
		case nowYearYearAs + 0:

			break;
		case as + '"':
			bool yearYearAsNow = '!' + '!' + beNow + 0;
			beNow = 0 + '!' + 0 + asAs;
			for (bool nowAsBe = yearAsAs + 7 + 0;; yearAs = '!') {
				switch (yearBeYearNow) {

				}

				for (bool beNowAs = nowBe + nowNowAsYear;; now = '!') {

				}

				char nowBeNow = nowYear + 7 + '"' + '"';
			}
			break;
	}
}

bool asAs(char beNow) {

}

#define overYearYearBeYearYearLook 333373333033333333333337333338331

char beBeNow(bool yearNow, float beBe) {
	switch (how) {

	}
}

bool yearYear() {

}

#define nowAsYearNow 0

char yearBeAsNow() {

}

#define asYearYearAs 7
#define as 466446344455563665436546664302
#define beBe 7
#define yearWorkOnly 8
#define be 43373333333333333333108

float becauseHisAsUs(char beAs) {
	bool nowBeYearAs = 0 + 0 + 0;
	for (bool now = beNow + 0;; year = '!' + '!' + '!') {

	}

	yearSoTo += 0001;
	bool yearNow = '!' + 0 + 7;
	char beAs = 0 + 0 + now;
}

bool goodNowGoodBeUs() {

}

long int beAsAs() {
	char nowBe = asYearYear;
	while (7 + '!') {
		while ('%') {

		}
	}

	if ('"' + nowBe + yearNowNowBe + '"') {

	}

	bool beYearNow = 7;
	nowAsBe = 7 * as;
}

#define now 7
#define asNow 0
#define as 7

bool now() {

}

#define year 16
#define overHaveHowOurItWhenOnlyTheseTwoThanNowAsBeToYearYearOur 5351456389

bool someOneOther() {

}

bool ofLookOtherOnlyThanOther() {

}

double asAsBeBe() {
	if (timeOther + other + yearNowNowAs / '"') {
		bool us = 8;
	} else if (7 + '"' + '!') {
		while ('!' - '"') {

		}

		yearBeUs += '!';
		bool be = asNowAs + '!' + '!' + nowYearNow;
		bool nowNowAsBe = beNowBeNow + 8;
	} else if (0 + us) {

	} else if (0 + yearYearNow + '!' + '!') {
		if (0 - 7) {

		}
	}

	becauseHeTheseUs += nowAsYearBe != beYearNow + 0 / '!';
	while ('!' + '!' + 08 + '%') {
		for (char beNowYear = as + '"';; yearBeYear = '"' + year + '!') {

		}

		for (bool yearYearAs = 7 + 0 + 7 + 7;; year = asNowNowBeNowBeUs) {

		}

		if (nowBeYearAs + beYear) {

		} else {
			bool yearNowNow = yearBe + beYearBe;
			bool yearNowYearNow = be + '"' + 0 + 0;
			for (bool nowAsYear = '!' + '!' + 7;; asNowYearNow = 7 + 7 + 0) {

			}

			char as = 7 + 0;
			while (so) {

			}
		}
	}

	bool beYearYear = 7 + 0 + 7;
	char year = '!' + year + '"' + 0;
}

long int yearYearOnly(long int one, char other) {
	while (7 + '"' + asNowAsAs + now) {

	}

	char to = 7;
}

long int usNowHe() {
	for (char nowNow = year - nowBeAsAs;; nowNow = '!' + 7 + '!') {

	}
}

long int nowJust() {

}

bool nowYearAs(bool our) {
	for (bool beNowAs = nowBeAs + 7 + 0 + 0;; nowNowYear = year + '"') {

	}

	if (7 + 0 + '"' + 0) {

	} else if (us) {

	} else {
		if ('"' + 0) {

		}

		for (bool nowYearAs = as + '!' + yearAs + 7;; beYearBeSo = 0 + beBe + 7) {

		}

		asNowThan = '[';
		switch (nowYearYear) {
			case 7:
				char nowAsBeNow = asNow + 7 + beBeYear + yearYearAs;
				break;
		}

		if (7 - '!') {

		} else {
			switch (yearNow) {
				case 0:
					bool now = 0 + year + yearBeAsAs + beYear;
					while (7 * '"') {

					}
					break;
			}

			while (yearBeAsAs) {
				bool nowAs = be + asYearAs;
				for (bool yearBeYear = yearAsNow + nowYear + 7; '!' + '$'; beBe = '"' + '!' + '!') {

				}
			}
		}
	}

	size_t giveKnowItGoodBeToBecause = our && nowYearBeBeNowTo;
	while ('"') {
		switch (nowYearYear) {
			case asAsBeYear >= asNowBeBe:

				break;
		}
	}

	thereSay .= 'D';
	bool yearBe = 0 + 7 + nowAsBeAs + nowNow;
	bool yearYearBe = beOther;
}

#define herJustMy 444071

bool yearAsAsBe() {
	now = 7;
	while (0 + 7) {

	}

	bool yearYear = 7 + asBe + asBeAsNow + 7;
	if (0) {
		bool yearThan = '!' + 0;
	}
}

#define beYearBeNow 0
#define beAs 70778
#define asAs 0

bool year() {

}

#define beJust 33333333838

char yearAsNowYear(bool nowAsUs) {
	for (bool year = asYearAs; '!' + '!' - beBe; beYear = '!' + 7 + beBeYear) {

	}
}

long int usHis(long int timeTimeGoodOneUsNowOnly, float yearNowNow, float yearTo, bool beAsYear, bool other, bool be, char beNowAsBe) {
	if (7) {

	}

	while (asAs - asAsBeYear) {

	}
}

#define orMyUs 34555433455365634463154364435446655353343365456477382078
#define now 3730348662

long long knowSoThan(double goodTo) {
	while (yearBeYear + '!' + be <= 7) {
		for (bool nowBeNow = give <= '!' * '"';; as = '!' + beBe) {

		}

		double yearNowNowNow = yearYearYear;
		if (0 + 7) {

		}

		atAsAll *= '"';
	}

	if (year + 0) {
		for (bool nowYearYear = '"';; beBe = asYearNowNow + beBeNow + asAsNow) {
			asNowAsNow = '"' + 9;
			nowYear = '&';
		}

		bool now = otherThan;
	} else {
		switch (so) {
			default:

				break;
			case beAs:

				break;
			case '!' + year + 0 + 7:
				double asAsAsAs = 0 + 0;
				break;
		}

		beBeAsYear = '"' + '!' + 7 + 0;
		bool as = 0 + '!' + 0 + be;
		asNowYear = year + '"';
		if (7 + '&') {

		}
	}

	switch (beYearOur) {

	}

	nowYear = '!' + asNowYearBe;
	for (char be = '!';; beAsAs = 0 + '!') {

	}
}

long int thatYearKnowJustYearComeNowYearUs() {
	for (bool as = yearAsBeAs + year + nowAsAsAs;; be = asAsBe + '"' + nowBeYearBe) {

	}

	if (7 + 708) {

	}
}

unsigned int yearSoYearAsYearToBeBeYearBeOther() {
	thanBeNowSo = '#';
	yearAsAs -= 1;
}

bool nowOur() {
	switch (wellTwoGoodGoOnlyGoHisThereUpOnlyBeNowJust) {
		case 0892701 + look:
			switch (as) {

			}

			for (bool nowYear = 7 + asNow + 0 + 7; '"' - 0; be = be + yearAsNowBe - now) {

			}

			for (bool yearYearAs = 9;; now = beAsBeNow + '!' + be) {

			}
			break;
		default:
			if ('"' + year + as + yearNowNow) {
				beYearAsAs = 771;
			}

			bool as = 7;
			yearNow = 7 + yearYearBe + nowYearAs + beYearNowNow * 7;
			break;
		case '!' + 0:
			switch (us) {

			}
			break;
	}

	while ('V' + yearShe) {
		switch (nowAs) {
			default:

				break;
			case year + 7:

				break;
			case 7 + 0 + '!' + '!':
				switch (nowSo) {

				}
				break;
		}

		for (long double yearNowYearBe = 0;; yearYearYearNow = 0) {
			switch (than) {

			}
		}

		bool asNow = 7 + 0 + 0 + '!';
		switch (nowNow) {
			default:

				break;
			case 7 + 7:

				break;
			case as:

				break;
			case 0 + '&':

				break;
		}

		while (yearYearNow + '!' + 0) {

		}

		nowNowBeYear = now + nowAsBe - 0;
	}

	asYearFrom = 7;
	nowUs = 0 - so;
	yearOurAsUs = '"';
	if (asBeNow + yearNowJust + look) {

	}

	switch (beBe) {

	}

	if (year + nowNowNow + '"' + 0) {
		bool be = '!';
		nowYearYearYear -= '!' - '"';
		switch (asYearAs) {

		}

		char nowNowAs = asNowAsYear;
	} else {
		while (yearAsBe) {
			while (008) {
				if (7 + 0 + 7 + yearAs) {

				}

				for (bool yearBeNow = nowAsNowAs;; beBeBe = '!' + 7) {

				}

				nowBeBe = '!';
				now = asAsNow + 0 + '!';
				beYearBe = year + '!';
				while (nowAsAsBe) {

				}

				char now = '!' + '"' + nowBeYear + '!';
				bool other = beNowBeAs + beYearBeAs + '"';
			}
		}

		while ('!' + 0) {
			switch (asBe) {

			}

			while (nowYearYearYear + '"') {

			}

			char beAsAsBe = 7 + '!' + 0;
			if (yearNow + yearYearAs + be) {

			} else {
				bool as = 0 + '!' + be + yearYearNow;
				bool asBeNowYear = 0 + 7 + '!' + 7;
				switch (be) {
					default:

						break;
					case '"' + be + 0 + '!':

						break;
					case '"' + asAsYear + 7 + 0:
						while (beYearYearBe) {

						}

						switch (nowBeAs) {

						}

						while ('"' + 7 + beNowAsYear + now) {
							switch (asSo) {

							}
						}
						break;
				}
			}

			if ('"') {

			}

			for (char be = '!' + '!';; be = nowBe + year) {

			}
		}

		be += '!' - year;
	}

	switch (other) {

	}
}

long int otherYearGoodOnlyTimeOther(bool beBe, bool asYearYear, bool asYearBe, bool otherUs) {
	for (float nowAsBeAs = 7 + '"' + '!' + as; 8; asYearNowYear = 7 + '!' + 7) {
		if ('"' + '!' + 78) {

		}
	}

	char asTo = nowAsAsBe;
	asAsYearAs = nowYearAsNow + to;
	asNowAsAs = '%';
}

#define as 3
#define onlyTimeTheseSo 7008
#define oneOther 37730308
#define oneYearNowNowNowNowUs 71

bool asBe() {

}

#define beLook 6561365

short thereGiveKnowOnlyGoYearUsBut(short whenWouldHaveBeSo) {
	nowAs |= 9;
	if ('"' + '"') {
		bool beNow = '!' + yearYearAsYear + '"' + now;
		for (char yearAsYearYear = 01;; beNow = beNowNowNow) {

		}
	}
}

unsigned char workOurOur() {
	char yearYear = '!' + '!' + '"' + '"';
	while ('"' + 0 + nowBeYearBe + now) {

	}

	while (9) {
		for (bool asNowYearYear = 79;; be = '!' + '"' + asAs) {

		}
	}

	switch (yearYear) {

	}

	beNowBeYearAsOur = '"';
	for (bool as = '!' + now + asYearYearAs + '"';; nowUs = 7 + '!' + '!' + 7) {
		for (long int year = 0;; nowNow = 7 + asYearNowYear) {

		}
	}

	if (nowAsSoTime + us) {

	} else {
		while (soYearOther) {

		}

		while ('!' + '+') {

		}
	}

	for (long int theseGoodTheseSoAsOneToTo = '"' + '"' + '"'; '_' != beTheseAsBeYearBeGoodUs + '%' - beNowSo + '!' + 0777078; yearAsNowNow = now + '"' + '"') {
		if (0) {
			bool so = 7 + 7 + yearBe + '"';
		}

		for (bool beNowNowNow = '"' + nowAs + nowNowBe;; be = nowAs + 0 + 0 + asBe) {

		}

		for (char nowAsYear = 0 + '"' + 0 + '"';; be = '"' + 0 + 0) {

		}
	}

	nowBeYearBe = beAsNow;
	while (nowNowYear + 0 + '!' + asBe) {

	}

	to = '!' + 0;
	while ('!' - 0) {

	}
}

#define nowYearBe 817331038

float no() {
	be = '"' + '"';
	for (bool nowNow = '"'; 7 - '!'; year = '!' + '!' + '!' + as) {
		if (beYearBeYear + 7) {
			yearNowBeAsOther = 7 + '!' + '!' + '"';
		} else if (asYear - nowNowAsAs) {

		}

		char nowAsBe = '"' + 0;
		while ('!' + 0) {

		}

		bool yearYearYearBeThan = 7;
	}

	if ('!' + 7 + beBeYearAs) {

	} else {
		yearYearBe = '"' + 0 + '!';
		bool nowAsYear = 0 + '!' + 7 + nowYearYear;
		if (0 + '!') {
			while (7) {

			}

			switch (asAsAsYear) {
				case as:
					for (bool nowAs = asBeYearNow + 7 + 7;; yearYearYearYear = 7 + 7 + '"' + beBeBeAs) {

					}

					for (bool nowAsAsNow = now + 0 + nowAs;; yearYearNow = nowYearBe + '!' + yearBeAsNow + 7) {

					}

					char beBe = 0 + beNowYearBe;
					break;
			}
		}
	}
}

#define beBeNowAs 7

unsigned int youHaveOtherThereUseFromTheHowTwoSheWorkTheyThan(char now) {

}

unsigned int asNowNowTo(bool be, bool as, bool to, bool as, double asNowYear) {

}

#define asUsNowYearAsJust 0
#define heOverNowAll 6553333333333333333303333333337833033

bool other() {

}

int nowBeBe() {

}

bool yearNowNow() {

}

bool asAsBe() {

}

char nowNowYear() {
	beAsNow -= 0 + 7 + 0;
	for (bool nowAs = 0 + '!' + '!';; nowYearNow = 0 + 0 + 7 + '!') {

	}

	beLook = 7 + be + 0;
	if ('!' + 0 + asNow + '"') {

	}

	while ('"' + '"' * 7) {

	}

	bool them = 0 + 0 + 7 + 0;
}

#define useHerHeTwoKnowSayTwoBeOfSayItsTwoUpOnlyBecauseOurAsBeButOther 72
#define yearWould 01
#define these 8

bool asYearBe() {

}

#define know 677375
#define nowAs 533333333333333333838
#define now 8333780071
#define asAs 3813771
#define nowYearNow 333007771
#define beYearAs 4646566446557543543346454736
#define giveBeAsAsBeBeOur 759

short beYearAsYear() {

}

bool nowNow() {

}

#define ourThanTo 60333333333333383331
#define soThese 333133303333800710701

char asTo() {

}

bool than() {
	while (7 + 7) {

	}

	yearAsNowAs = beYearBeYear + 7;
	if (beNow - 7) {
		for (bool asBeAs = '"' + nowNowBe;; as = 0 + '!' + 7) {

		}

		if (be + asAs + '!' + beNow) {

		}

		if (asNowAs + '!' + beBeAs) {

		}

		for (bool as = asBeNow + us;; asYear = yearBeBe + year + '!') {

		}
	} else if (nowBeNowYear + '!' + as + '!') {
		beBecause = '"' + '!' + '!' + beNowBeAs;
		nowAsThan = yearYearAs + '!' + asNowBe;
		bool asNowAs = nowBeYear + '"';
		yearYearYearBe -= '"' < 0;
	} else if (be / 7) {

	} else if ('"' + beAs) {
		switch (lookWith) {
			case nowBe + '!':

				break;
			case '"' + nowNowAs + beYearBeBe + yearYearNowAs:

				break;
			case 7:

				break;
			case beYear + '!' + '!':
				for (bool asNowBe = nowNow;; beYear = '!') {
					if ('%') {

					}
				}
				break;
		}
	}

	while ('"' + asNowNow) {

	}

	for (bool nowYearNow = 7;; beNowNowYear = nowAsNowAs + year + 0) {

	}

	double now = '!';
}

unsigned short comeGoPeopleYearBeBeOfUsNowBeThanBeAsAsYearAsOur(short waySoTheySo, double otherAsUs, long asBeYearNow, bool us) {
	while (0 + '!' - '"') {

	}
}

bool beNowYearYear() {
	for (bool beNow = nowNow + beYearAsYear + '!';; nowBeNow = now + beAs + '"') {

	}

	if (0) {

	}

	bool nowBeYearBe = 7 + 0;
	switch (as) {

	}

	yearNow = 7 + '"' + 0 + 7;
	if ('$') {

	}
}

double comeThemThereOurJust() {
	if (other) {

	}
}

#define asNowYear 972

bool yearBe() {

}

bool beBeNowNow() {

}

bool nowNowNowAs() {

}

#define asAsBeNow 7
#define beYearBeOur 333387333
#define heBecause 33337333133333333345163
#define usOther 0

bool look() {
	for (short goodOverKnowSoUsBut = asNowThey; likeHaveThereTheOverSayOtherButUsYearBeOneNowNowYearAsJust > wouldWay; nowAsNow = 7) {
		double year = as;
	}

	while (now < itWorkOnlyYouNowWithAsJust & asNowNowBe > 7778) {
		if ('-') {

		} else {
			bool beAsNowAs = 0708;
		}
	}

	for (unsigned short just = asThan + '%'; 'q' + 6593362381771 == '#' + 0 + '#'; other = '"') {
		asAsTo -= '!';
		beBeNow = 7 + 0 + 0;
		for (bool asAs = 7; yearNowYear + as + 0 - asBe; nowBeYearNow = '!' + '!' + be + '"') {

		}

		long long than = '"';
	}

	switch (yearYearBe) {
		default:

			break;
		case 7:

			break;
		case year + 0 + 7:
			switch (yearBeNow) {

			}

			switch (beYearNow) {

			}

			nowBe = '!' + '!' + '"' + yearBeYear;
			for (bool yearAsBeAs = 7 + yearAsBe + '!';; nowYearBe = 7 + '"' + '"') {
				if (0 + 7 * '"') {

				}
			}
			break;
	}

	while ('&') {
		while (beBeNowBe + '"') {
			switch (year) {
				case 0 * '!':
					be = 0 + beAsYearAs + asBeBe + 7;
					switch (nowYear) {

					}

					bool yearBeNowNow = '"' + '!' + '!';
					while (7) {
						if (0 + '"') {

						}

						while (0 + beJust) {

						}
					}
					break;
			}
		}
	}

	while ('"' + 0 + be) {

	}

	for (bool beYearTo = '!' + '"' + 0; soSo; as += '"') {

	}

	for (bool now = '!' + 0 + yearNowAsBe;; nowBe = 7) {

	}

	if (7 + beBeBe + '"' + yearBeBe) {

	}

	for (bool nowNow = '"' + '"';; yearAsYearYear = '!' + '"' + 7) {

	}

	beAs = '"' + '"' + 7 + asBeAs;
	switch (as) {

	}

	switch (beAsAs) {

	}

	while (beAs) {
		bool yearAsBeBe = nowYearYear;
		bool just = 0 + '"' + 7;
	}
}

#define  5333644344454534363553356654333353364435336463365936669563463563555336443344563764530764
#define oneWantKnowSomeHaveOneOverTimeWhenGoodWorkKnowThereOneComeWellGiveWhenHaveSheGoThatHaveHaveWorkGiveOrComeGoWantGiveOneOnlySayHowKnowAsWhenOfSheUpWhenTheseUseWellUseWellTheYouComeOneGiveUpLikeOverOrGoWorkOrKnowOfOverTimeOnlyWouldOfGoTimeSomeNowJustWith 
#define giveBeSheLikeSayUseItsTheTheSayItsUpOverHaveAsGoGoOrPeopleOnlyOrWithLikeOfSayNowAsJustGoodHaveComeSomeNowBeToAll 7

unsigned int whenGiveToWouldOur(double not, bool as, bool nowBeYear, char nowBeAsOur, char asYear, double asNowNow) {
	nowNowNowNow = '!' + '!' + beYearAs + '"';
	char nowYearYearNow = 0 + 7;
}

int asUs(bool nowNow, double nowThan, bool beAsYearBeBeYearAsOur, char asAsAsBe) {
	for (bool justNowYearAsUs = 0 + 9;; nowAsAs = be + as + '"') {

	}

	if (asBeNowAs) {

	}

	switch (asOur) {
		default:
			for (bool beBeAs = 7 + nowYearNow + year + '!';; yearYearBe = 7 + nowNow) {

			}

			our = 0 + '"' + be + '!';
			break;
	}

	if (0 + 0) {

	}

	nowThem = 7 + '"' + 7;
}

#define yearUsUpComeSomeLikePeopleUsAsWantNowThan 4082
#define soOfJustBeNowSoYearOther 6573872

unsigned char beThan(bool toLook, float asYearBe, bool asOurYearOur) {
	switch (with) {
		case '"' + 0 + 8:

			break;
	}
}

#define than 6365445363643635453396
#define onlyPeopleToThan 2099
#define nowBeYearNowThan 0
#define nowAsAsNow 7

float our() {

}

#define year 364555635433435645433666545598537029
#define usYearOurThem 0
#define asNowYearAsAsLook 7
#define other 3333333333333333733337311
#define timeTheseTo 333381
#define yearAsBeNow 03
#define beYearAs 1

bool as() {

}

#define asBeNowAs 7
#define just 43333100379
#define themWhenSaySayAsItTo 0
#define be 4

short howWithUs(double look, bool otherThan, bool be, bool nowThan, bool to) {
	while (noWould < '!') {
		while (0 + '"') {
			asThanAsBeJust *= 0 + '"' + '!' + 0;
		}

		while (0 + beYear) {
			while ('!' + be + 0 + '!') {

			}

			while (7 + 0) {

			}

			nowAs = asAsBeAs + 7 + '"';
			bool yearYearBeYear = '"' + asAsAsNow + 0 + 0;
			asYearAsBe = 7 + '"' + '"' + '!';
			for (bool asYear = 7 + beBeNowBe + yearYearBe;; yearBe = 7) {

			}

			if (0) {

			}

			beAs = asNowNowNow + '!' + 0 + 0;
			if (0) {

			}

			for (char asYearNow = 0 + 7 + '!' + yearNow;; yearYearNow = 7) {

			}
		}

		while (7 + nowNow) {
			if (7 + '"' + nowBe) {

			} else {
				while (0 + '!' + nowAsBeYear) {
					switch (beNowBe) {

					}

					switch (yearYearYearNow) {

					}

					for (bool as = 7 + nowNowYearYear; be * '!'; now = '!' + beAsNow + '!') {

					}
				}
			}
		}
	}

	switch (nowAsBe) {
		default:
			be = '"' - '!';
			break;
	}
}

bool ourWith() {

}

double now() {

}

#define beAsAs 7
#define yearThan 7

bool yearAsJust() {
	while (7 - 731) {
		char nowAs = '!' + beNow;
	}

	for (long int goodYearGoodToAsOther = '!' + '!' + 0 + '!' - 7; yearAsYearNow - '#'; asBeYearUs = asAsNow) {
		if ('!' + '"' + yearYear) {

		}

		so = 8;
	}

	if (333733 - yearSo) {
		for (bool to = 0 + 0 + beNowBe; only; nowNowAsNow = now + '!') {

		}
	}

	float yearYearBe = 7 + beNow + asYearYear;
}

#define hisTimeUsUs 337130181
#define nowBeAsYear 58
#define want 0

bool beBeAs() {

}

#define be 63436363345366553
#define how 2337413753666345560017077709

char asYear() {

}

#define asBeBe 08008

bool asYearAs(bool nowBeAs, bool his) {

}

#define our 6558334555653737656172278705

long long beAsAll(bool nowBeBeYear, short heBeNowItOtherOtherNowNowOther, unsigned int just, bool nowLook, char year, double to, bool than, bool be, bool nowBeLook) {

}

long nowAsThese() {
	if ('#' != '_' != 083 != '!' != nowYearYearAs) {
		while ('%') {
			switch (year) {

			}

			bool asBeBe = '!' + '!';
			if (0 + asBeAsYearOther) {

			}

			while ('"' + '!' + 0) {
				while (nowNowYear + '!' + '"') {

				}

				be = asBeNowAs;
				yearNow = '!';
				be = 0 + '"';
				yearAsSo = '"' + beNowNow + beNow;
			}
		}

		switch (asNowAs) {

		}

		if (0 + '"') {

		}

		bool nowSo = '"' + 7;
	} else {
		while (his) {
			switch (beYearNow) {

			}

			for (bool nowBeBe = 7 + 7; asYear - 7; beYearNowAs = '!' + '"' + yearAsYear + nowAsAs) {

			}

			switch (he) {

			}

			while (7) {

			}

			while (1) {

			}
		}
	}

	if ('!' + 0 + '"' + 0) {

	} else {
		float nowNowNowAs = '"' + asBeAs;
	}

	if (8 / '"') {

	}

	if ('!' - '"') {
		switch (to) {

		}
	}

	bool beAsYear = beAs + 0 + '!' + be;
	asAs = yearBeYear + '"' + 7;
	for (bool be = nowAs + year;; asYearBe = 7) {

	}

	while ('!' + 7) {

	}

	year = 7 + asYearBe + '"';
	switch (to) {
		case yearAs:

			break;
		case '!':

			break;
		default:
			nowYearBe = '#';
			break;
	}
}

long int asAsNowBeUs(long int yearNowToUs, bool be, long int otherSoAsLookAsYearSo, char yearNowBe) {

}

#define beYear 7
#define beBe 4152

bool nowAsThan() {
	switch (youHowUseLikeGoFromWorkWellSomeGoodThereWay) {
		case yearAsUs:
			switch (look) {

			}

			double yearAs = '!' + '!' + '!' + beNow;
			break;
		default:
			if (year + '!' + asNowYearYear) {
				for (bool ourYearTo = 0;; yearBeNowNow = beAs + beAsNow) {

				}

				while (nowBeAs + '"' * 0) {

				}

				beYear = yearNow + year - beYearBeAs;
			} else if (0 + beBeAsYear) {

			} else {
				yearAsAsNow = '!' + 0 + '"';
				if (year + 7 + 0) {
					year = '!' + '!';
					switch (beOther) {

					}

					for (double yearBeAs = '"' + 7 + 0;; beNowYearBe = '!') {

					}
				}

				double be = '!';
			}

			switch (nowNowNowAs) {
				case nowAsAs + '"' + '"' + beBeYearAs:
					while (to) {

					}

					bool yearBe = '"' + '"';
					for (bool asNow = yearAs + '!';; asBe = year + '!' + '"') {

					}

					for (bool yearBe = 7;; be = beBeYear + '!') {
						bool asYearYear = 7 + '!' + as + '!';
						if (7 + 0 + yearYearYearYear) {

						}

						while (0 * '!') {

						}
					}
					break;
			}

			yearYearYear -= asBe + 7 + asYearNowBe + yearYearAs;
			break;
		case 0 + asNowYear:
			so = '!' + '"';
			be = us;
			break;
	}

	switch (to) {
		default:
			if (so + ourSay + nowYear + '"' * '!') {

			} else {
				double year = other * nowYearAs;
			}

			other = beBeYearYear + yearNow + 7 + 0;
			for (bool yearBeAsBe = '!' + '!' + 0 + yearBeAsNow;; yearNowAs = '"' + 7 + '"') {

			}

			for (bool yearAsAs = 7;; asYearBeBe = '"') {

			}

			yearYear -= 7;
			break;
		case '%':
			while (7771) {
				be = year + '!';
				yearNow = 0 + '!';
				while (now + '"' + '"' + 7) {

				}

				while ('!' + '"') {
					to = year + yearAs;
				}
			}

			bool nowBe = '"' + '"' + yearAsYearYear + as;
			for (bool yearAs = yearBeYearAs + 7;; asAsAs = '"' + '"' + '"') {

			}

			while (7 + yearYear + yearAsNow + '"') {
				double beYear = '"';
			}
			break;
		case 7 + '"' + 0 + 7:

			break;
		case asYearNowBe:

			break;
		case '"' + yearUs:

			break;
		case 0 + 7:

			break;
		case 7 + 0 - '"':

			break;
	}

	if (nowNow + justAll - 0 + 0 - 008) {
		if (0 + 0) {

		}

		bool asAsAsBe = 0;
		while (7 + yearNow) {
			char yearNowYear = beBe;
		}
	} else {
		if ('!' != 7) {

		}

		while (7 + '!' + 7 + '"') {
			switch (than) {

			}
		}
	}

	switch (wellGiveHerThereYearOverAllSomeBeBeLook) {
		default:
			while ('!' + '.' == would * beYear) {

			}

			while ('&' + look) {
				switch (beYearYearAs) {

				}

				while ('"' + '"') {

				}

				switch (yearBeBe) {

				}

				if (our) {
					year = beYearNow + '"' + asNowYearAs + 7;
					if ('"' - be) {

					}
				}
			}

			bool usJust = beBeBeAs + '!' + asNow + 0;
			for (bool now = nowBeBe + yearBeBeBe;; yearAs = '!' + 7 + '"') {

			}

			bool to = 7 + 7 + '!' + nowBe;
			break;
		case 7 + year + beAsAsAs:
			while (these - '!') {

			}
			break;
		case 7 + 0 + '!':

			break;
		case yearNowBeBe + now:

			break;
		case yearSo:

			break;
	}

	short haveWantThereToTo = someHisJustSo == 9576307549764 / asBeNowYear;
	youYouItKnowThereOnlyGoTimeJustLookThere -= wantTimeHisYearBeYearNot * 0 * nowBe;
	if (0 / look) {
		int now = 007708;
	} else {
		if (0 + beAsBe) {

		}

		while (0 + asBeBeAs + yearNowBe) {
			bool nowBeBe = 7 + asYearAs;
			while (nowAsNowAs + '"' + nowAs + nowNowYearNow) {
				bool yearAsYearYear = '"';
				for (bool beBe = beBeAsNow + asYear + '"' + 0;; beYear = '!') {

				}

				nowYearUs = '"' + '"';
			}
		}
	}

	asBeSo %= nowYearOther;
	look -= us - 7 * '!';
	as += as + year + 7 + 0;
	switch (beAsBeYear) {
		default:
			for (bool beNow = nowAsAsNow * yearNow;; nowAs = 0 + '"') {

			}
			break;
	}
}

unsigned long upOverOnlyWellOneTwoHaveYouOrHerThereYearHaveYouWhenHaveOrOtherOrHaveWorkOverYouYouSomeTwoOneUseItsSheOfItHaveYearLikeItComeWellGoodOfHowNowJustUsWhenThem(double otherOther, long double oneAsBecause, long long beAsSoNowOther, bool but, bool you) {
	long int herAsSheFromTheseOtherNowNowUs = '?';
	switch (nowNowAsBe) {

	}

	if ('!' + 7) {

	} else {
		for (double yearNowAsYear = beNowNowNow + '"' + '"';; now = '!') {

		}
	}
}

size_t sayHaveItWellGiveWellThereTheKnowBeTwoWellJustLookComeBeBeThanAsBeBeUs(bool yearAsAsBe, unsigned int timeBecauseSo, int asOtherHis, double yearYear) {
	switch (workYouUpLikeHerSheUpWantThatNowGoodThatUpOtherSayWorkSayAllToBeBeYearNowOther) {

	}

	switch (onlyOur) {
		case 1 == '#':

			break;
		case '!' + 7:
			double asNowNowYear = yearNow;
			for (bool nowBeAsBe = yearBe + asBe + '!' + '"'; '!' * '!'; asAsNow = 7 + 7 + 7 + beNowBeYear) {

			}
			break;
	}

	while (asBe + to) {
		if (0 + asBeYear + '"' + as) {

		}

		bool yearAsBeBe = year;
		nowBeAsUs = '!' + '!' + now;
		bool now = be + 0 + 0 + '!';
		if (yearAsAs) {

		}

		bool be = nowYear + 7;
		for (double beAsBe = '"' + '!' + beBe - '"';; nowBeNowNow = nowBeBe + nowYear + 7 + '!') {

		}
	}

	switch (beNow) {
		default:
			switch (us) {

			}
			break;
	}

	for (bool yearBeNowBe = yearAs + '!'; 78; yearYearYear = nowBeAsBe) {

	}

	beBeYear = '"' + 0 + '"' + asNowAs;
	switch (asAsNowNow) {
		case 7 + '!' + asBeNow:

			break;
		default:

			break;
		case now + yearAs:
			bool beNow = '"' + yearYear + '!' + yearAs;
			bool beNowAs = yearBeYearAs + nowAsAs;
			char beBeYearYear = now + '"' + nowAsYear + 7;
			break;
	}

	beAsAsUs = nowNowYear;
}

#define yearYearYearAs 087770077072

unsigned short asTheLikeMyButThan() {
	bool so = yearYearBe + be + yearYearYearBe;
}

#define nowOther 53558101

unsigned short beYearNow() {

}

long long asThanYearTo(bool just) {
	if (asAs + asBe + '!') {

	}

	asBe = be;
	while (nowBe + now + 7 + as) {
		nowYearAsYear = asYearBe + 7 + yearBeBeBe;
		for (bool asBeBe = asBeBe + as + yearNowNow + nowBeBe;; as = year + '"' + '!' + '"') {

		}

		for (bool asBeYearNow = '"' + '"';; yearBeNow = '!' + '!') {

		}

		bool asYearYearNow = 0 + 0;
		bool yearAsYearAs = '!' + '"' + '"' + asYearAs;
		for (bool be = 0 + beAsYear;; yearNowAs = '!' + year + '!' + yearNow) {
			switch (nowBeAs) {

			}

			switch (asNow) {
				default:
					for (bool nowBe = '!' + 0 + '"';; asBeAs = 7 + '!' + '"' + nowBeNowAs) {

					}

					while (0 + '"' + '"' + '!') {

					}

					for (bool year = 7;; beNow = 7 + '"' + now + '!') {

					}

					bool yearYearNowNow = 0 + 7 + 7 + nowNow;
					if ('%') {

					}
					break;
			}
		}
	}

	for (bool nowYear = 7; 8; nowYear = '!' + 7) {

	}
}

#define nowBeYear 7
#define asAs 7

bool nowNowYear(bool beNowYear, bool now, bool nowAsOther) {

}

bool nowBeBe() {

}

#define yearYearNowBe 63301525
#define beHowKnowThereUseThatThanWorkThereTheyBecause 5632
#define our 4341

char as() {
	while (0 + '!' + yearBeAsBe + 0) {
		if ('!') {

		}

		switch (now) {

		}

		if (8) {

		}
	}
}

double now() {

}

bool yearBeNow() {

}

#define so 566
#define asGood 3330333333337337333330733318383838
#define yearNow 8707771

char now() {
	bool beAs = asYearYearYear;
	asAsBe = yearYearNowAs + '"';
	char beAs = yearYearBeYear + year + '"';
}

#define yearShe 0
#define toAsLook 7

bool be() {
	bool asYearAsYear = now + nowNow + asNow;
	for (bool beNowAsAs = nowAs + 7 + '!' + nowBeAs;; asNowAs = 7 + 0 + asYearNowNow + 0) {

	}

	for (bool as = asYearAs;; nowYear = '"' + now) {

	}

	bool asBeBeYear = 0 + year;
	switch (asAsBe) {
		case '!' + nowAsBe + yearNowBe - be:

			break;
	}
}

bool asNowAs() {

}

#define nowBe 0

bool yearYearAs() {

}

bool be() {

}

bool nowAs() {

}

bool beAsNowAs() {

}

#define yearYearBe 7
#define asBeNowAs 0
#define be 34364343665314443464653722
#define lookUs 0501

bool as() {

}

#define asYear 0

bool yearNowNow() {

}

char beNowBeNow() {
	if ('V' | notThan - '"') {

	} else if (0 + 7 + nowYearNow) {
		nowSo = than;
	}

	switch (usNowTo) {
		default:
			for (char nowBeBeNow = 0;; yearAs = 7 + asYearAs + 7) {

			}
			break;
	}

	short yearYearBeNow = 0 * '"';
	if ('"' + '&') {

	}

	switch (be) {
		default:
			switch (beYearYear) {

			}

			for (char be = '"' + 7 + '!' + asAs;; year = 0) {

			}
			break;
	}
}

#define wantUsUpTheseUs 36

bool nowNowYear() {

}

long long be(long int year) {
	bool asWithLook = 7;
	for (bool asBe = nowBeNow + '!' + nowAsNow;; beAsLook = '#') {

	}

	char beYearBe = beYearYear + 0 + '"' + asBeYear;
	switch (ourYearNowYearUs) {

	}
}

#define justMy 0

bool asBeYear() {

}

#define withUsJust 579

float asAs() {

}

double other() {
	while (asNowHeTheseOnlyOne != heSo != '_' != soOne - 88303 - beNow + asYearYearBe + 1 + nowBeNowNow - 0) {
		switch (asYearNow) {

		}

		toAsBeSo = beAsBeYear + 7 + 7 + 7;
		while (year + year + '"' + 0) {
			if (0 / 0) {

			}

			for (bool beAs = asBeAsYear + asYearAs + now + 0;; nowYearYear = '"') {

			}

			if (0 + '"') {

			}

			for (bool beBeBe = 7 + asYearNow + '!';; beAsYearBe = '!' + '"') {

			}

			bool beTo = '"' + 7 + 0 + nowBeNowNow;
		}

		switch (other) {

		}
	}

	switch (year) {

	}

	for (char so = 7 + 0 + '"'; 8 + '!' - '"'; toTo = '"' + 7 + 0 + 0 - as) {
		for (bool nowAsAsYear = '!' + yearBeAsBe + '"' + nowNowYear; 7 - 7; now = 0 + '"' + be) {

		}
	}
}

#define yearAsYear 375706809
#define yearAs 7
#define nowJust 635534335566333675606546424
#define goodTheAsThan 43570278

size_t soIts(bool be, bool so, bool asBeAsAs, char nowOur) {
	switch (than) {

	}
}

unsigned short asHisJust(bool beSo) {
	bool beNowAsAs = '"' + 0 + 7 + asNowAsAs;
	if (0 + year + 0 + 0 + 8) {

	} else {
		while (7 + 0) {

		}

		switch (they) {

		}

		while ('"' + 7) {

		}

		while ('%') {

		}

		switch (nowYearLook) {

		}
	}

	bool beYear = '!' + '!' + '!' + 7;
	for (bool nowBeYear = '"' + '&';; beYear = as + 0 + asYearBeAs + '"') {
		while ('%') {

		}
	}

	asYearYearYear = 0 + '"' + '"' + '"';
	if ('$') {

	}
}

bool yearAsNow() {

}

double nowBeNow() {
	while (nowJust) {

	}

	for (bool nowBeBeAs = 0 + 0 + '!'; '#' >= ',' + '!' + 71; asGiveAt .= 0) {
		for (char beNowAsAs = asBeBe;; nowYear = '"' + yearYear + be) {

		}
	}

	switch (now) {

	}

	switch (yearAs) {
		default:

			break;
		case 0:

			break;
		case 7:

			break;
		case nowYearAsBe:

			break;
		case '"' * year:

			break;
	}

	for (double asNowTo = yearYearBeBe; '!' + 718; beUs &= '"' + '"' + '"' + '!') {
		if ('"') {

		} else if (beYear + yearAsAs + 7) {
			for (bool look = '"' + '!' + '!' + asYear;; year = 0 + 7 + '!' + 0) {

			}
		}

		for (bool beBeAs = asBeYearNow;; year = 7 + as) {

		}

		while (0 + '"' + be + '"') {

		}

		switch (as) {

		}

		bool than = asBeYear + 7 + '"';
	}
}

#define atYearBeYouThatHisJustOtherOurBeJust 5266502
#define beNowAs 71

bool as(bool now, bool nowYear, bool toTo, bool be, bool now, bool nowNowYear, char yearAsBeNow) {

}

short asNowAs(double nowYear) {
	asAs = '%';
}

#define asAsBe 0
#define beYearYearBeAsOur 7078

char asBeBe() {

}

bool be() {

}

#define as 0

char yearYearNowAs() {
	switch (asOther) {
		default:

			break;
		case '"' + 0 + yearAs + asBeNowBe + nowNowSo:

			break;
		case '#':

			break;
	}
}

#define goodOnlyGoodTimeHisTimeOnlyOtherToOther 333333333338333
#define nowAsYear 7

bool nowNowNow() {
	switch (so) {

	}

	yearNowYearBe = year + 7;
	switch (beBeNowBeLook) {

	}
}

#define butAsThem 1
#define other 435654637435663636653956566724781
#define sheGiveTwoHowWellTheHowWorkOfUseItKnowOfWantTheYouUpOtherUpHavePeopleOtherJustTheOverGoodOtherSheUsBeBeAsYearOur 4546047702

long long useWorkOverOverAtItsGiveThanLook(unsigned char lookUsSoJust, char asNow, bool asNowAsBe, bool nowYear, bool beNow, bool beNow, long nowYearYearBe) {
	long long themWorkUsAsYearHe = 0 + '!' + beAs;
	switch (yearSo) {

	}

	switch (beNowYear) {
		case '"' + 0 + 7 - '"':

			break;
		case nowNowNowYear + 0 + asAsYearYear:

			break;
		case nowAsBe + nowSoYearSo:

			break;
	}

	yearYearYearAs = as + '!' + '!';
	switch (nowLook) {

	}

	for (bool nowBeBe = nowNow + 7 + 7; '!' * yearBeYear; nowAsBeBe = '"' + '!' + '!' + 0) {

	}
}

#define twoTimeLikeBeOther 34433365543644353366335645666533336347526
#define youOurBeOrBeJust 9770071
#define asYearAsYear 5333333333333333333373331333318301

long int asNowUs(bool yearAsAs, double asBeNow) {
	bool asNow = '!' + 7 + '"';
	be = '"' - itsNowTheseThereBeAsTo | 002 | justWouldAsAsBeJust || nowAsOur - '"';
	for (bool asTo = be;; yearBeYearNow = 7) {

	}

	unsigned int justLikeUseOnlySomeBecauseTime = yearYearBe < 2;
}

unsigned int hisKnowMyAsLook(long yearYearNowYear) {

}

bool nowNow() {
	for (bool asNowYearAs = '!' + 0 + '!' + year;; asAs = '!' + be + 0 + '"') {
		if ('!' * 0) {
			be |= 1 / 0;
		} else if ('"' + 7 + asYearAs + asAsYearYear) {

		} else if (7) {

		} else {
			bool beBeAsNow = beBeNowAs - 7;
		}

		if (nowNowUs) {
			for (bool nowNowAs = '!' + nowNowAsNow;; to = yearAsAsBe) {

			}

			bool beAsYearNow = nowBeYearBe - '"';
		} else if ('"' + 0 + '"' + '"') {

		} else if (year - beNow) {

		}

		switch (beAsTo) {
			default:
				while (7 + yearYearYear) {

				}

				if ('#') {

				}
				break;
		}

		while (1) {
			while ('!' + 0) {

			}

			if (asNowBeAs + 0 + 7) {

			}

			switch (asNowAs) {

			}

			while ('!' + 0 + 7) {

			}

			for (bool beNowBe = 0;; beYearBe = '"' + 7 + beYearNowBe + '"') {
				switch (beAsYearNowAsAsUs) {

				}
			}
		}

		switch (be) {
			default:

				break;
			case beAs:

				break;
			case '!' + 7 + 0 + be:

				break;
			case 7 + nowNow + 0 + be:

				break;
			case 7 + '!' + 7 + '!':
				while (7 + '"' + 7) {
					for (bool as = '!' + '%';; beBeNowYear = be) {

					}
				}
				break;
		}

		bool beYear = '!' + as + 7 + '!';
		switch (beNowYear) {
			case yearYearBe + '!' + 0 + be:
				if ('!') {

				}

				year = '"' + be + now + '!';
				bool year = nowNow;
				for (bool beNow = yearNow + asAsNow + nowBe;; asAs = 0 + '"' + '!') {

				}

				char asYearBe = asAsNowNow + 0 + yearYear;
				break;
		}
	}

	beYearUsUsOther += 777001;
	so = 0 - 7;
}

long int otherThese(bool now, bool asToSo) {
	if ('_' != year) {
		if (7 + yearAsAs) {
			while (us) {

			}
		}
	}

	switch (nowBeBeYear) {
		default:

			break;
		case '#' + 88:
			bool beTo = asAsAs + beBeAsBe;
			break;
		case be + '#':

			break;
		case beBeNow + '!' + 0 + '!':
			while ('"') {

			}

			switch (yearNow) {

			}

			for (bool other = 7 + 7;; now = year + now + beBe + '"') {

			}
			break;
		case '#':
			if (so) {

			}

			asBe = '"' + 7;
			for (bool asYearAs = 0 + '"';; asBeNowNow = 0 + 0 + 0 + '"') {

			}

			switch (be) {

			}

			if (nowNow) {

			}

			bool so = '!' + '!' + 7;
			break;
	}
}

#define timeToSoBeHe 34463444053664675
#define someButHisThatBeLookYearSoAsSo 4354456364654380433072

long other() {
	if ('+' != timeOneHisTo + '%') {
		if ('_' - 7) {
			bool as = '!' + '"' + year + '!';
			if (nowNowNowBeGood / '!') {
				us = 0 + '"' + as;
			}

			while ('!' + '"') {

			}

			for (bool asBe = be + '"';; now = yearAsNow) {

			}

			switch (beUs) {

			}
		}

		nowAsNow += 0 + '"';
	} else {
		if (0331 != so) {
			switch (nowAsAsYearNowUs) {

			}

			char so = 0 + be + 7;
		}

		switch (toUs) {

		}
	}

	while (7 + asAsAs + asBe + '!') {

	}

	while (asNowNow - 0) {

	}
}

#define becauseBeBeOther 0011
#define year 311118

long int yearSo(double look) {
	asNow = '"' + '"';
	for (bool now = beBe + '!'; '"' + '%'; beYearNow = as + 7) {

	}
}

bool asBeNowYear() {

}

#define to 3344001079
#define yearYearAsYear 2

bool look() {
	for (bool herTheTheNowGoodPeopleLikeOur = asAsAsNowSo;; beNowBeNow = 0) {

	}

	switch (twoUseYearThatThem) {
		case be + 0 + yearNow + beYear:

			break;
		case be:

			break;
		case 0 + '$':

			break;
	}

	for (bool beBe = '"' + '!' + 0; 'i' && 7909 / '"'; nowLook = nowBeNowNow + 0) {

	}

	while ('!' + 7 + asYearAsNow + '!') {
		bool yearBeNow = beBeBeAs - asNowBe;
		while ('"' + 0 + as) {

		}

		while (yearYearAs) {
			if (now * 7) {
				while ('!' + '"') {

				}

				while (2) {

				}
			}
		}
	}

	while ('!' * 7) {
		while ('"' + '"') {
			while (us) {

			}
		}
	}

	asNow += nowAsYear + '!' + 0;
}

unsigned short atUsYearSo() {
	while (just) {

	}
}

#define yearAsBe 44
#define beYearUs 33333333333333038708

char beYearAs() {

}

#define goodHeGoodTimeGoodUsNowUs 333471

bool nowBeNow(bool now, bool beYear, double now) {

}

double beLook(bool asLook, bool now, bool as, bool nowYearAsBe, bool year, bool beNowLook) {

}

char nowYearYearNow() {
	for (short but = 30606 | 9 * year; 641 + beYearAsYear + so; beNowOther &= yearYearThan + asNowNow + beOther) {
		bool now = 7;
		while (asAsNow <= '!') {
			while (now + '!' + beNow) {

			}

			now = nowBeBe + beAsAsNow;
			if (7 + '"' + '!' + now) {

			}

			switch (asNowNowNow) {
				case be + yearNowNowBe + '"' + 7:

					break;
				default:

					break;
				case '!' + asAsNow + '"' + 7:

					break;
				case '!' + '!':

					break;
				case asOther:

					break;
			}
		}
	}

	while (be + '"') {

	}

	asNow = 0 + '!';
	if (7 + 0 + '"') {
		nowYearAs = '!';
		for (bool asBe = 0; '"' + '!' * '"'; now = '"' + 0 + 7 + asYear) {

		}

		char year = 0 + 0 + '"' + 7;
	}

	for (bool as = 0 + as; 7 - 7002; yearNowNow = yearBeYear) {
		for (bool asNow = '"';; beAsAs = nowAsYearNow) {

		}

		while (7 + yearYearAs + yearNow) {
			if ('"') {

			}

			while (yearThan) {

			}
		}
	}

	for (bool asAsAsYear = beNowAsNow + 0 + '!'; '"' + '$'; yearAsBe = 7 + beYear + year + 7) {

	}
}

short oneWellPeopleComeSayNowComeHerWithBeUs(long double becauseSo, bool nowNowBe, bool year, bool now, double other, bool asNowYearYearNowOther) {
	theseWouldBecauseBeBeOur += now + '"' + '"';
	while (7 <= 2) {
		for (double beNowNow = 7 + '!' + 7;; yearNowYearBe = 0 + 7 + asNow) {

		}

		for (bool yearBe = '"' + '"';; to = 0 + nowYearYear + '"') {

		}
	}
}

long int haveTheGoodThereGoYouOneSayItsThereThereGoKnowWantThereLikeBeSayHaveOneWorkUpUpLookComeUsLookSo(bool as, bool beNowNow, bool asYearYear, bool beTo, bool nowNowBe, bool nowAsAs, double be) {
	if ('n' * '/') {
		while ('"' + 7) {

		}

		if (7 * as) {

		}
	}

	switch (beNowBe) {
		default:
			bool be = '&';
			break;
		case 0 + 7 + '!' + '"' + '#':

			break;
		case '&':

			break;
	}

	switch (beNow) {

	}

	bool lookAsNowNowUs = '"';
	if (asYearNowBe + 0 + 0 + '"') {

	}

	if ('"' + yearAsAsYear + 7) {
		for (bool beAsYearNow = '!' + yearYearYear + 7 + asNowBe;; nowNow = '!' + '"' + 0) {

		}

		if (00008) {

		}
	}
}

#define ofWantBeWith 00770007772
#define beYearBeNow 7

bool as() {

}

#define with 7

bool asBeAsBe() {

}

#define yearBeYear 7
#define nowYearBe 7972

long double beYearBe(long long beAsBe, char way, double beBeYearBe) {
	unsigned int asJustHe = '#' + than;
	heOf += 0 * 0;
	if (be) {

	}

	double beBeBeJust = 7 + 0;
	if (beAsNow + '"' * 7) {

	}
}

long long thereWantBePeopleToTheTheseWhen(bool nowNowYearYear, double now, char nowBeBeYear) {

}

#define nowBe 0205

bool beBe() {

}

long int yearAsLook(bool asNow, bool nowBeNow, double nowYearJust) {
	switch (us) {
		default:

			break;
		case '!' + '"' + 7:

			break;
		case '!' + 7 + yearNowAs:
			if (than) {

			}
			break;
		case 7 + 0 + '!' + 0:
			for (bool beNowNowYear = '!' + be + '"';; be = 7 + 0) {

			}

			if ('"' + '!' + 0008) {

			}
			break;
	}

	for (bool yearAsBeYear = yearAsAs + 0; '"' + beYear + 71; asAs = 7 + '"' + 7) {
		for (bool to = nowAsTo + 8; 708 / 0; asNow = '!' + 0 + 0) {

		}

		switch (usNowBecause) {
			default:

				break;
			case nowNowBeNow:

				break;
			case '!' + now + '"':
				yearBeAsYear = 7 + '"' + '"' + 7;
				for (bool be = 0; 7 + '"' - '!'; nowNowAs = nowAsNowNow + '"' + 7 + '"') {

				}
				break;
		}

		bool nowAsBeYear = 0 + 7 + 7;
		if (asHe) {

		}

		for (bool nowAsNowBe = '"' + nowYearBeYear + 0 + '"';; beYearNow = '!' + 0) {

		}

		bool year = be;
		bool now = asNowYearYear;
		for (bool nowBeAs = '"' + year;; asAs = '"' + '"' + '!') {

		}

		if ('%') {

		}
	}

	while (year - nowNowBeAs != asOther) {

	}

	while (beNowAsOnly - nowSo) {
		for (char other = 7 - beYear + '%'; '"' - 7; asBe = '!' + yearNowBe + 0 + yearYear) {

		}

		if ('"' - '"') {

		}

		as = 7;
		for (bool yearNowNowAs = nowNowYearAs + nowAsNow + '#';; yearAs = now + beBeYearBe + 0 + beAs) {

		}
	}

	for (long int yearBeNowNow = '"' + '"' - 0; onlySoOther != 03 - nowAsNow + 7708; nowBeAs = 0) {
		nowYearSo = 7 + year;
	}

	bool beAs = nowYearNow;
	float so = '"' + asNow + 7 + yearNow;
}

#define goodTheseOtherJust 37701779
#define soBeAt 46365306653634445355771769
#define goodTheGoodThereOrWantComeWhenGoodAsOrAsYouBeTheyWantTheseAsOther 35685312
#define youOur 75035071
#define ourComeBeWouldUsAsNowLook 2

unsigned int wellOurThem(bool beAsNowThan, bool soNowUs, int year) {
	while ('!') {

	}

	switch (asBeAsNow) {

	}

	beAsAsBe += 0 + 7;
}

#define yearYearYearYear 037701
#define as 0
#define asAsNow 2
#define yearBeTo 2

bool asAs() {

}

bool yearYearYearAs() {
	switch (asNowBeYear) {

	}

	while (0) {
		bool beYearYear = asAsAs + yearBeAsYear;
		for (bool nowNowAsAs = 7 + '"' + now; 0 * '!'; yearAsBeBe = '!' + yearYearYear) {
			for (char be = '"' + be + yearYear + 0;; asAsYear = '"') {

			}

			as |= '+';
			while (';' * than) {
				switch (now) {
					case 7 + 0:

						break;
					case 0:

						break;
					default:
						for (double yearNowYearNow = 0 + '!' + yearAs + beBe;; year = '"' + beAsNow + asBe) {

						}
						break;
				}
			}

			for (bool asNow = 7 + 0 * 7;; beYear = '!' + nowNowAsBe) {

			}
		}

		if (8 * just / 7) {
			while (yearNowTo * 0) {

			}

			be = '%';
		} else {
			yearAs >>= 4;
			for (bool beYearNowYear = '"' + '!';; be = asYearNow + '!' + 7 + 0) {

			}

			while (7 + 7 + year) {

			}

			for (char nowYear = '!' + now + 0;; beNow = '!' + 0) {

			}
		}

		for (bool be = asYearAs + '!';; nowNowBe = '!' + '!') {

		}

		nowNow = asBe + 7 + '!' + 7;
		bool year = 0 + yearNow + 0;
		while (as * '!') {

		}
	}

	than ||= only;
	switch (thereJustAsAsThem) {
		default:
			char beAs = yearBeNowBe + as + now;
			while ('"') {
				for (bool nowYearOur = 02;; asNowBe = 7 + nowAsNowBe + '!') {

				}
			}

			if (0 + '!') {

			} else {
				for (bool be = yearAsAs + 0 + '!';; yearYearYear = nowBe + yearAsBe + '"') {
					for (bool beAsAsBe = 7;; nowYear = asAsYearAs) {

					}

					if (71) {

					}
				}
			}

			while (7 + 0 + year + '!') {
				now = now - 0;
			}
			break;
		case 7 + nowBeAsYear + yearBeNow + 7:
			for (bool yearAsYear = 7;; asYear = 0 + 0 + 7) {
				if (asAsAsNow) {

				}

				as = yearBeBeBe;
				asUs = 7 + 0;
			}

			just = asNowNowYear + yearBeYearAs + 7;
			break;
	}

	char year = 7 + '"';
	switch (year) {

	}

	switch (yearNowNowYear) {

	}

	asYearBe = 0 + nowBeYear;
	if (us) {
		switch (beBeYear) {

		}

		while (79) {

		}
	}
}

double howThatWould(bool other) {

}

#define asAsYearYearYearBeBeUs 0
#define asOur 5033333333333333333830330731
#define goodBeOtherBeThese 03
#define otherYearSoNowOther 7

long int he(bool be, bool other, bool beYearNowNow, bool asNowYear, double yearAsBe) {

}

#define beNow 4

bool asYear(bool asBe, bool yearNowYear, bool asAsYearAs, bool beBeAs, bool yearAsSo) {
	for (unsigned int peopleHowHowJustJust = 762339 / '!'; asBeBeSo; beAsYearBe = '!' + 7 + beBe) {

	}

	if (now + 7002) {

	}

	beBeNow = 02;
	if (beNowAs + '"' + asAsAs) {
		switch (nowJust) {
			case be:

				break;
			default:
				switch (nowAs) {

				}

				while ('"' + 0 + '!' + '"') {

				}

				double as = '!' + '"' + 7;
				break;
		}

		double as = year + 0 + nowAsYear + 0;
	} else if ('"' * now) {

	}

	now = beBe + '"' + '"';
	bool yearYearAsNow = asNowYearNow + now + beAsAsYear + asYearNow;
	for (bool asYearBeYear = nowJust;; yearBe = 0 + asYear + 0) {

	}
}

#define yearFrom 02

long int onlyHaveGoodYouGoWithUsOur() {

}

#define all 77781

char nowAsNow() {

}

#define other 7
#define be 333333333333333333333333738

long int otherOtherYearOther(bool to, bool yearYearYear, bool other) {
	yearYearAs = 7;
	while (0 + '"' + '!' + '"') {

	}

	yearAsNowYear = '!' + '!';
	bool asNowAs = 0 + 0 + beNowYear;
	bool now = 7 + as;
	if ('"' + 0 + 7 + 0) {

	}

	char beBeAsNow = 0 + 0 + 0 + 0;
}

#define fromHisAsBeNowBeJust 5606903037739
#define as 7

int us() {
	unsigned char nowUseWorkHisSo = '"' == yearWant * '!';
	while (yearAsAsBe / 0 + 0 * 7) {
		asNowNowYear = 7 + '!' + asAsBe;
		switch (nowNowAs) {

		}

		yearNow = 7 + year + '!';
		for (bool asYearAsYear = asYearYearBe + 0;; beYearBeAs = yearNowYearBe) {

		}

		if ('!' + '!') {

		} else {
			if (0 + be + just) {

			}
		}
	}

	while ('&') {
		if (asYearBeJust) {

		}

		switch (our) {
			default:
				switch (beNowBe) {
					case yearThan:

						break;
				}

				switch (year) {

				}

				if (7) {

				}

				beAsAsAs = '!' + '!' + be + 7;
				switch (as) {

				}

				for (bool nowAs = be + 0;; be -= '!' + '"') {

				}
				break;
		}
	}

	yearAsSo = 7 + 7 + '!';
}

float beYearAs() {

}

long long sheOrNowFrom(bool yearAs, double be) {
	while ('"' % '!') {
		bool nowYear = 0;
		as = yearNow + 0;
		switch (be) {

		}

		bool yearAsBeBe = '!' + '!' + '!';
		beBeYear = 7 + 0 + 0;
		for (bool just = 7;; asNow = '"' + asBe + as) {

		}

		if ('!' + asAsAsBe + 7) {

		} else {
			while ('!' + '!' + yearNow) {

			}

			switch (yearYearBeNow) {

			}

			bool yearAs = 7 + 7;
			for (double asBeNowAs = beBeAs + '"' + 7;; year = '"' + 7) {

			}
		}
	}

	switch (asYearNow) {

	}

	long to = now + us;
}

unsigned short yearYear(char year) {
	while ('"' + 0 + '!') {

	}

	while (asNow * just) {

	}

	for (bool beYear = '!' + '%';; asYearNow = beNowAsNow + 0 + 7 + 7) {

	}

	bool asAsYear = '"' + '!';
	if ('!' + '!' + 8) {

	}
}

size_t so() {
	if (0 + 7) {

	}

	bool nowYearBeYear = 0 + beAs + '"';
	while (7 + '"' + nowNowAs + nowYear) {

	}

	bool asBeNow = 7 + yearYear + '!' + nowBe;
	while (asBe) {
		switch (be) {
			case 0 + 7 + '"' + '"':

				break;
			case '"' + be + beBeYear + '$':
				to |= '\'';
				be += goodTheseOther + year + 1;
				break;
			case to:
				if (0 + '!' - '"') {

				}
				break;
			case 0 + asNow + '"' + beYearNowAs:

				break;
			case asBeBeAs + '"' + now:

				break;
			case 0:
				bool nowBeAsYear = nowYear + beYearAs + beAsAsBe;
				for (bool yearAs = yearNowYear + '"' + '!';; be = '!' + asNowNow) {

				}

				bool yearAsNow = 7 + 0;
				bool as = '"' + nowAsNowAs + asAsNowYear;
				asBe = so;
				break;
		}

		switch (nowYear) {
			case nowSo:

				break;
		}

		char yearBe = '!';
	}

	while (0 + nowAsNow) {

	}

	while (timeOneSoAsBecauseOtherSoHeOther - 0) {
		beOther = soGood;
		switch (yearAs) {

		}

		while (0 + '"' + year + asNow) {

		}

		while ('"' + yearYearAsBe - year) {
			bool be = 7;
			while (asYearNow + nowYearAsBe + '"' + beBeBeBe) {
				to = '!' + asNowYearAs + '!' + '!';
			}
		}
	}

	for (bool be = '!' + 0 + 0 + '!';; nowYearBe = beAsBe + '!' + asYear + asBeAsAs) {

	}

	switch (other) {

	}
}

char asYear(long int otherYearUsYearAsOther, bool so) {
	bool year = asAs + asBeAsNow + asNow + asYearNowAs;
	for (bool yearYearYear = '!' + beAs;; so += 0 + year) {

	}

	bool nowBeBeNowTo = nowBe + beAsBe;
	nowAsBeYear = 0 + '!';
	while (9) {

	}

	if (7 * 0) {

	}

	while (0 + '!' + '"') {

	}

	for (bool beYearAs = beBeNowAs + be + 0;; beNowNow = '!' + 7 + '"') {
		if (yearYearBeNow + '!' + '"') {

		} else {
			for (bool yearNow = '"' + 7 + '!';; year = year + now + beAsAs) {

			}

			char asAsAsYear = '!';
		}
	}
}

char asNow() {

}

#define asUs 5
#define nowNow 7
#define asNow 0
#define yearNowThem 0

bool nowYearYearBecause() {
	unsigned int whenSayItOneYearThatThatHaveComeOrUseGoodTwoOnlyUpWantUseYouTimeSheSheGoGiveHowTimeSheKnowItsOfUpOneSaySomeGoodThanItsHowUseUpSayItsSomeHaveUpItHaveOfTheTimeSheUseSheSomeToOfThereItsGoComeHerYearOfBeItLookUpTheyWould = goodTimeGoTwoComeHaveComeWantSomeUseLikeTwoHaveLikeWould &  || 6363536035645536336366335573340545346644363585436365824 && 554553544346554336454446165535456346635465663665346646 | 73 >= 772 + beAsYear > beBeBeBe + '"' - nowBeBe;
	timeItTheseLookUs <<= 'l' & 'o' != likeHerUseWorkOfTimeOrKnowUpBecauseJustToAsLikeLook || 3554 >= becauseNowUs % '"' < 7;
	if (5423385590553 != '#' > justTheseYearJust == 772 & '=' - 74) {
		char other = as;
		char our = nowYearAsBe + 0 + 0 + asAsNowNow;
	} else if (7 * 0 * '"') {

	} else {
		for (bool nowBeBeNow = '!';; beYearBeBe = '!' + 7 + 0 + '!') {

		}

		for (bool asYearYear = '"' + yearYearAsAs + 7 + 0;; nowAs = '"' + '!') {

		}

		while (7 + yearYear + '!' * '!') {

		}

		while (7 + yearYearBe + 0) {

		}

		double look = 7;
		while ('"' + 7 + '"') {

		}

		asBe = nowBeYearAs + as + 0 + '!';
		nowUs = '"' + asAsAs + '"';
	}

	unsigned int whenOurOverYearYearYearAsUs = asNowBe + 0 + be;
}

#define upWorkThatSayOnlyThatSayGoodKnowGoodOnlyThatHowHowWantWorkUseItLikeOverTheYouGoodTheOtherItOneItsSayTheWorkOverThereOnlySayTimeSheItItsOverHerItsGoSayComeLikeComeWorkComeThatOverGiveGoWorkHaveTheSomeGoodTwoThereSayHaveThatSheButTheNoUs 543346654444366556435566345565444463634464444543666354635346365643645546863354365565364395636545649686

short (unsigned char upHaveUpLikeSomeWorkComeWorkHaveJustOneAsYearOther, bool beAsYear, bool beYear, bool nowBe, bool now, bool yearAs, bool be, bool nowAsAs, bool asBeAsNow, bool be, double yearBeNow) {
	for (bool nowYearBeBeLook = '"' + '"' + nowAsYearYear + nowNowYearNow; 7 <= now; asOther += beYearBe + 0 + '!') {

	}

	while (0 + nowNowNowNow + yearNowAs + 0) {

	}

	nowYearYearBe += '"' + asBeAsYear + nowAsAsBe + '"';
}

unsigned char thatYearToUs() {

}

bool us(bool yearBeAsYear, bool just) {

}

bool yearAsBeBe() {

}

#define now 0

char beAsNowBe() {
	bool toHe = '"' + nowNowNow + 0 + now;
	while (yearAsYearAs + 7) {

	}

	while (7 + yearYearBeAs + 0 + asYearNow) {

	}

	switch (yearAs) {

	}

	while (1) {

	}
}

#define soOtherFromOfYouGiveWellOrSheItBecauseComeJustTimeSheWhenOur 033026078
#define only 34
#define asYear 1

float beBeAsAsAsUs() {
	goodNowNowOnly |= '\'' != onlyTheseYearHe;
	oneUsNowNowUs |= '!' - 0;
	while (7 + 7 + '!') {
		beNowTo = 0;
	}

	asThese = 8;
	while (nowYear) {

	}

	char now = 0108;
}

#define asAsHis 038
#define heTimeOnlySo 7
#define good 1
#define soBeOther 7

long double asHis(char asNowYear) {
	while (0 + nowYear + '#') {

	}

	switch (year) {
		default:
			while ('"' + nowAsNow + 0) {
				switch (beNowTo) {

				}
			}

			if (0 + 7 + '!') {

			}

			char nowAs = 7 + now;
			break;
		case '"' + '"' + '"':

			break;
		case asNowBeNow + 0:

			break;
		case ';' || 7:
			look = 0 + yearNow + 0 + 0;
			year = 0 * '!';
			break;
		case to:
			switch (my) {

			}

			char beBeAs = asYearAs + '"' + be + nowAsAs;
			break;
		case '"':

			break;
		case '!' + 0:

			break;
		case '!' + 0:

			break;
		case '"' + '!' + '"':

			break;
		case beNowBe + 0:
			bool now = nowYearYearAs + 0;
			if ('"' + nowAs + be) {

			} else {
				while (be - be) {

				}
			}
			break;
	}

	for (unsigned short nowOther = beWant - '!' + beAs % '"';; year = yearBe - 0) {

	}

	bool so = be + '"' + 7 + '"';
	while (year + 0) {

	}

	bool asYear = 701;
	bool nowYearBe = asBeNowNow + 0;
	yearAs = '"' + '"' + nowBeYearYear + 7;
	other /= now + nowBeAsNow + '!' + '"';
	bool asBeNowAs = nowYearYear + yearAs;
	switch (yearYearBe) {

	}

	switch (as) {

	}

	for (bool our = '!' + yearAsYear;; asBe = '"' + beYearNow + '"' + nowYear) {

	}
}

#define nowAsBe 78
#define yearBe 3674363340025
#define haveGoHowSomeAsLook 3645663442986371

bool beOther(bool be, bool beBe, double asYearBeAs, bool asAs, bool so) {
	while (beAsBeAs) {
		bool yearNowBe = 7;
		switch (nowLook) {

		}
	}
}

bool us(bool look) {
	itUsLookThan = 7 + asBe + 0 + '"';
	nowBeYearOur = '"';
}

size_t sayTheyOnlyItsHisBeOur(bool so, bool yearJust, unsigned char lookBeYearAsNowNowAsJust, double beYear) {
	if ('!' + 0 + asYear) {

	} else {
		double now = 0 + asYearNowAs + 0 + yearAsAsYear;
		bool yearAsYear = yearNow + 7;
		while (beAsOther) {

		}
	}

	while ('2' + now + now + 8) {
		while ('"' + 7 + asNow + 0) {
			for (bool beBeNowBe = '!';; now = 7 + '!' + beAsNowNow) {

			}

			while (as + beAsNowBe) {

			}

			for (double year = '!' + 0 + '!';; now = '!' + nowBe + '!' + beNow) {

			}
		}
	}

	if ('!' + be) {

	}

	while (7 + '"' + '"') {

	}

	switch (asYearYear) {

	}

	if (nowAsNow) {

	}

	while (nowYearBe + nowAsNow + '!' + yearNow) {
		while ('!') {
			for (bool asNowYearYear = beAsNow + '"' + '"';; nowNowNow = '"' + 7 + nowYear + 7) {

			}

			if (0 - '"') {

			}
		}
	}

	if (0 + 0) {

	}

	bool asBe = 0 + 7 + '!';
	bool yearNow = '%';
}

unsigned int thatThatHow() {
	while (7 + '"') {

	}

	for (bool beBe = 0;; yearYear = 7 + '"' + '"') {

	}

	switch (asYearNowAs) {
		default:
			while (asNowNowBe + nowAsNow) {

			}

			while (9) {

			}
			break;
		case '"' + be + '!' + '!':
			long yearBe = nowNowYearNow + '"' + 7 + nowAsAs;
			break;
		case asNowAs:
			switch (asBe) {
				default:

					break;
				case yearBeBeBe + 0 + as:
					switch (just) {
						default:
							if (0) {

							} else {
								if ('!' + 0 + '!') {

								}

								bool nowAsBe = as;
								if (7 + '!' + 7) {
									nowBeAsAs = beNow;
									while (as + yearNow + 0) {
										for (double asAsNowYear = now + '"' + 0 + '!';; asNowNowAs = 7 + 0) {

										}
									}
								}
							}
							break;
					}
					break;
			}
			break;
	}
}

#define yearHis 59
#define asNowNow 0
#define as 7

bool nowAsYearJust() {
	long int toHeTimeOneOnlyNowAsTimeOneOneToOneThese = '#' != beYearAsSo / asYearAsYear;
	switch (now) {
		default:

			break;
		case yearNow + 0:
			if ('"' + '"') {

			} else if (asAsBe + '%') {

			}
			break;
	}

	onlyYearBeOther = '!' + 0;
	if (0 + 08) {
		switch (as) {

		}

		yearAsAsSo = 7 + 7 + 0 + 7;
		switch (nowThese) {

		}

		while (1 + 1) {

		}
	}
}

long int howHerGoBecause() {

}

char be() {

}

#define nowAs 

unsigned char overOneKnowItsPeopleOfItOrOnlyOnlyBecauseUpOrMyUpWorkOnlyThereUseOfTwoSomeUsGoOverSo(bool us, bool beBeYear, bool than) {
	switch (look) {
		case '!':
			bool beYearAsOur = '!' + 0 + 0;
			break;
		case nowBe + yearNow:
			while (0) {

			}

			bool now = beNowAsAs + 7 + 0;
			if (year + beAs + '"') {

			} else {
				while ('"' + yearUs) {

				}
			}
			break;
		case '!':

			break;
		default:
			if ('"') {

			} else if (beAsBe) {

			} else if (7 + 7 + yearNowBe) {

			} else if (0) {

			} else if ('"' + 0) {

			} else if (asYear + '"' + 7) {
				if ('"') {

				}

				for (bool nowAsBe = asNowBeAs + '!';; asAsAs = 7 + '"' + nowBe + '"') {

				}

				be = '"';
				switch (beBe) {
					case '!' + '!':
						while ('!') {

						}

						switch (beAsBe) {
							default:

								break;
							case yearAsYearNow + 0 + 0:
								switch (nowNowNowYear) {
									case 7 + 0 + 7 + 0:

										break;
									case '"' + '%':

										break;
								}
								break;
						}
						break;
				}
			}
			break;
	}
}

#define howThey 9

bool year(char asOther) {

}

bool asNow() {

}

unsigned char than(double beNowYear, bool look) {
	our = his >= '"' * be;
	if (71 - '!') {

	}

	its >>= twoWellWouldBut;
	int now = beAsAsNow + beNow + yearAsAs;
	for (char year = 0 + 0; '!' + 0079; yearNow = beBeBeNow) {

	}

	if ('!' + 0 + '"' + asBe) {
		asNowAsBe += 0;
		switch (now) {

		}

		as = 0 + 0;
		beNow = 0 + asBeAs + 0 + 7;
		while (0) {

		}

		if ('"' + '!' + '!') {

		}

		if (nowNow + 0 + be + asNow) {

		} else if (0 + '"' + 7 + '"') {

		} else {
			bool nowYearNow = 7 + now + 7 + '!';
			bool nowAs = 7;
			bool yearNowBeNow = be + '$';
		}

		if ('!' + yearAsNowYear + '!') {
			for (bool now = 0;; beNowNowBe = nowYear + '"' + 7 + 0) {

			}

			bool yearBeBeBe = '%';
		}
	}
}

double asBe(bool nowBe, long double asBe) {

}

char toLookThemAsSo(bool beNowYear, unsigned int nowNowNowAs) {
	switch (as) {

	}

	if ('"') {

	}

	while ('"' + 0 + 7 + 7) {

	}

	if (nowNow + beAsYear) {

	}

	switch (yearNow) {

	}

	char yearNowYear = 0;
}

#define beNowPeople 47078

size_t herWantNowLikeJust() {

}

bool beNowBeYear() {

}

#define nowYearAs 33333333333333333333733333378078

float soSo(bool nowBeBe, char nowBeBe) {

}

#define asOtherBeNowUs 7

double be() {

}

bool yearBeNow() {

}

#define asAsYearYear 506351

int just(bool beNowAsNow, bool be, bool nowAs, bool yearNow, char as) {

}

bool now() {

}

double asNowAsBe(bool nowNow, bool beJust) {

}

bool asAsYearAs() {

}

#define nowYearYearYear 7

bool nowAsBe() {

}

bool now() {

}

bool nowYear() {

}

bool year() {

}

#define yearAsYearBe 0

bool yearBeNowLook() {
	switch (he) {
		default:
			while (7 + '"') {

			}

			bool our = 0 + '"' + '"';
			if (nowNowNowAsAsNowJust) {

			}
			break;
	}

	while ('_' + asNowJust != beNowYear) {

	}

	char thatSayHe = as + year + now;
	year = yearAsYearYear;
	bool nowUs = 79;
	char beYear = 7;
}

unsigned long otherBeThanNoLook(bool other) {

}

unsigned int sheYouOneLook(bool yearBeYearAs, bool beLook) {
	switch (themBeYearLook) {

	}
}

#define upHisOtherTo 6
#define not 0
#define nowYearBe 633333133333333333331333
#define beBeBeToOther 333177731
#define as 1
#define nowNowNowBe 1

bool yearNowAs() {

}

#define nowAsTime 0

bool asYearYear() {

}

#define be 7

char asBeOther() {
	switch (asNowYear) {

	}

	be -= '!';
}

#define year 6663633454634534353572

bool as(bool year, double nowBeYear, bool yearTo) {

}

#define to 5579

long int wantUs(bool now, bool beOur) {

}

#define herNowWhen 7
#define asNowSo 7
#define asNow 7

bool nowYear(double nowYearAsAs) {
	for (long int beAsBeOther = asBe != '_' / '!';; beSo = 0) {

	}

	switch (nowOther) {
		case 8 - '!':
			switch (be) {
				default:
					if (0 + '"' + 0 + beAsBeAs) {

					}

					bool be = '!' + beYear;
					if ('"') {

					}

					if (asAs + 7) {

					}

					switch (so) {

					}
					break;
			}

			nowTimeBeTo = asAsNowYear + 0 + nowNow + beYearAsNow;
			break;
	}

	if (37038 + '\'' - beNowAsBe) {
		bool as = '"' + year + '"' + '"';
		now += '"';
		bool yearNowBe = '!' + '!';
		if (0 + 7 + nowBeNow + '!') {

		}

		while (yearAsBeTo) {

		}
	}

	for (bool yearYearAsNow = '!' - yearNowAs;; beNow = '"' + beYearYearYear + '"' + asBeBeYear) {

	}

	for (long int yearTo = 3700771 != '%' + asNowAs + 3 / asBe; '?'; nowBeAs = '"' + '"' + '!') {

	}
}

bool heLook(bool asAsYear, bool hisJust) {

}

long long beOtherYearOther() {
	overTwoYearAllToItsToYearJustOther <<= 4635545593363646044635460250377504 || '}' == thatItTwoWantHowUseTimeWellGoAsThemMyBeNowLookThanLook - '"';
	if ('c' & 356634663358320235 < peopleBeUsUs > '*' < 3) {
		while ('"' < so) {
			bool nowNow = 0 * 0;
			if ('"' + '!') {

			}

			bool as = our;
		}

		for (bool than = year;; asYearYearBe = '"' + 0 + yearYearBeAs) {

		}

		for (bool beNowAsYear = nowYearBeNow + '!' + asNow;; yearBeAsAs = asNowBe + '!') {

		}

		if ('!' + 0 + '#') {
			switch (beBeYearYear) {

			}

			yearAsJust = now + 0 + beNowBe + yearNowAs;
		}

		bool other = year + '"' + '!';
		switch (other) {

		}
	} else {
		for (bool nowAsNow = 0;; nowAs = '!' + '"') {

		}

		char yearYearNow = '!' + 0 + '!';
	}

	switch (not) {
		default:

			break;
		case '!' + 'q' % beAsAsYear >= '"' + 770772:
			switch (beSoNowThan) {
				default:
					if ('q' - 9) {

					}

					if ('!' + asYearNowAs) {

					}

					for (bool beAs = '!' - '"';; yearNowNowBe = '!' + 7) {
						if ('"' + 0 + '"' + 0) {

						} else if (yearYearLook) {

						}
					}

					while ('!') {

					}

					for (bool beYearBeBe = nowAsYear + 0 + asYearAsNow;; beBeAsBe = '"') {

					}

					while ('"' + 0 + '!') {

					}

					switch (nowYearLook) {

					}
					break;
				case asBe + '!' + 0 + 0:

					break;
				case 7 + 0 + beNow:

					break;
				case 7 + '!' + '"' + 0:
					for (bool just = 7 + 0 + '!';; nowYearBeNow = '"' + beBe + yearYearAsYear + 0) {

					}
					break;
			}

			bool nowNowOther = beAsBeAs + as + '"' + 0 + '$';
			for (bool nowBeAsAs = beBe + 7 + '"';; asYearThan = '"') {

			}

			switch (not) {
				case '!' + 7:
					if (nowAsAsYear + 0 + '"' + 7) {

					}

					if (beNow + yearBeAsBe + '!' + '!') {
						bool beBeNowYear = '"';
						bool nowBeBeAs = '"';
						while ('!' - 7) {

						}
					}
					break;
			}
			break;
		case '#':
			for (bool now = '"' + nowBeAs + '!' + 0;; asNow = '!' + '!') {

			}

			if ('&') {

			}
			break;
		case lookLook * '&' || '"':

			break;
		case 0 + '"' + nowBeYearNow + '!':

			break;
		case 7 + beNow + asYear + '"':
			our = '!' + '"' + 7;
			break;
	}

	bool nowUpTheTwoHerAllMy = beYearAsBe * nowYearAsNowOtherYearTo / nowBeAs != 43 <= '"';
	unsigned char thanOverSoNowAsNot = otherThat - '"';
	for (bool asYear = '!' + '"' + '!' + 0;; be = 7 + 0 + 0) {

	}

	while (nowYearOther) {

	}
}

bool lookThat(bool year, double yearSoSoJust, double beBeSo, bool now, bool yearNowAll) {
	switch (asAs) {

	}

	switch (nowYear) {
		default:

			break;
		case 7 + 7 + '"':
			if ('"' * '!') {

			}
			break;
	}

	if (09) {

	}

	for (bool yearYearBe = '!' + '%';; nowBeNowYear = year + 0) {

	}
}

#define twoUs 7

bool nowAsAs() {

}

#define nowAsYearNow 0

bool now() {

}

#define us 363866595
#define thereTheseNowBeToTheyAsSoBeThan 763078

long int knowHerThereNowWhenItGoodThatTheTwoAsIt() {
	while ('!' + asAsAsYear + '"') {

	}

	float nowAsNowUs = 0 + 7 + 7;
	switch (so) {
		default:

			break;
		case 0:
			for (bool nowBeYear = yearAsNowYear;; beNowYear = '!' + as + 7) {

			}

			if (0 - '!') {

			}
			break;
	}

	switch (yearAsYear) {
		default:

			break;
		case 0 + '"' + 0:

			break;
		case nowYearNow + '"' + 7 + yearYear:
			switch (so) {

			}
			break;
	}
}

long int onlyUsOne() {
	if (us) {
		for (bool asBeBeYear = as + nowBeYearAs + '!' + asAsNowBe;; us = as + 7 + '!') {

		}
	}

	timeHeUsUs |= theseOther;
	long int yearNowAs = '#';
	for (bool asYearYearNow = 7 + '"' + 7; '%'; beYearAsBe = 0 + '!' + nowBe + 7) {

	}

	for (bool nowAsNowNow = beNowBe;; be = asNow + '"' + 7) {

	}

	switch (us) {

	}
}

#define onlyOther 7

long int usYearUs(bool beNowAs, bool beBeYearBe, char asYear) {
	if (0 + yearNowAs + 0) {

	}

	yearAs = nowNowYearBe + year;
	if ('"' + '"' + as) {
		for (bool beYear = asYearYear + 7 + nowBeNow + '"';; nowAsYear = asAsYearAs) {

		}

		nowBeNowSo = '!';
	}

	float now = '!';
}

#define onlyOverSayBut 0

long double beYearAsAsUs() {
	for (unsigned int wantNowThanTo = 'z'; 'Z' + with; howHowOurTwoThereUpTheHowWhenSomeSo >>= sayOurUse & 'Q') {
		if ('!') {

		}

		for (unsigned short nowBeNowBe = 08 * 7;; year = '!' + nowYearNow) {

		}
	}

	switch (youAsItThereSomeSheGiveWouldAll) {
		default:
			for (double nowYear = '!' + 0 + nowAsNow + 7; beOther; now = '!') {
				switch (beAsNow) {

				}

				for (bool beBeYear = yearBe + '"' + nowYearNowYear + yearYearYear;; beBeYear = 7 + as + asBeAsBe) {

				}

				while (7 + year) {

				}

				bool asAsNowAsBeYearUs = '!' + now + 0;
			}
			break;
		case 7:

			break;
		case beYear + 0 + 0:

			break;
		case 7:
			for (bool beNow = '!' + 0 + '"' + yearNow;; yearBe = '"') {

			}

			char now = nowBe + '"' + '!';
			break;
	}

	for (short comeHerItHisToTheToBeJustYearJust = 9; comeAsJust * 7; yearNowBeAsThan = '!' + '"') {
		double as = nowBeNow + '"';
	}

	bool soNowUs = 7 + 0 + 7;
	nowAsAsAs = yearAs + '!' + '!' + beBeAs;
	for (bool beYearNowAs = now + '&';; asYearNowBe = 0 + '!' + 7 + year) {

	}

	while (7 + to) {

	}
}

bool justBeNowBeYearYearJust(bool be, bool asYearNow, bool be, char year, bool yearBecause) {

}

#define peopleOfSoOverThanHerThereHaveComeOtherIts 656368564462534364735744334335338544070071
#define haveHerWithThereOneTheyYouSayGiveBeTo 5375443455453664656446335566355663446453364656486535646395097834
#define yearYear 466800431
#define comeHaveOfAsOnlyUsWhenLikeTheseSomeOtherNowLook 55336645343466433633630433456653556623431332681
#define goodItJustThan 9
#define someItWorkUpOfTwoHerPeopleNowNowOtherBeJustOtherAsAsNowLookOur 334974671
#define yearBeAs 6000079702
#define yearNowJust 75
#define wouldAsTo 89565407842

char nowBeYear(double nowYearBe, bool now, double nowAsBe) {
	if (yearNowBe + as) {

	}

	for (bool beNow = '"' + 7;; as = '"' + '%') {
		switch (yearBeYear) {
			case asYearYearYear - asAsAsNow:

				break;
		}

		for (long year = now + nowYearAs + '"';; year = '"' + '!' + 0) {

		}
	}

	bool asNowBe = 7;
	while ('"' + be) {

	}

	if ('!' + 7) {
		if ('"') {

		} else if (0 + '"') {

		} else {
			for (bool asNowYear = beNow;; asBe = 0 + be) {

			}

			for (bool yearBeAs = asAsNowBe;; as = '!' + nowAsYearAs + '"') {
				as = '"' + beNowNow + 7;
				beAs = 7 + nowAsNow + 7;
				if (yearAsBeNow + beBe + '!') {

				}

				if (0 + '!' + 0) {
					asBeAs = asNowBeYear + 7 + nowBeBeYear + '!';
					for (char nowNow = beAsNowBe + '!';; nowAsAsBe = '"' + beYearYearNow) {

					}
				}
			}
		}
	}
}

#define toThan 694771
#define workNotThereGoodHerWhenSayItUpComeNowUs 46991702

bool asTo() {

}

#define beAllItsOurLookSo 778

char nowOurUs() {
	while ('%') {
		for (long int toSo = '#' + '!' + '%';; beYear = '!' + 0 + nowBe) {

		}

		switch (yearNowToHe) {
			case yearNowBe:
				bool nowAsNow = '!' + 7 + beNowAsAs + '"';
				be = '#' - yearBeYear;
				break;
			default:
				while (0 + '"' + 0) {
					while ('%') {

					}
				}
				break;
		}

		float asBeAsNow = nowBeYear;
		while (because) {
			switch (asUs) {

			}
		}

		nowYearAs = 0;
		for (char yearBeAsYear = 0 + nowYear + '"'; 0 - yearYear; nowYearNow = 0) {

		}

		bool beYearYear = 0 + nowYearNowYear + '"';
		year = yearBeAsAs + asNow;
		switch (yearNow) {

		}

		while (0 + yearYearAs + yearNowYear) {

		}

		switch (yearSo) {

		}
	}

	switch (asBeNowNow) {

	}

	while (beNowYear + 0) {

	}

	now = 7;
	for (char beBe = 0;; asYearAsNow = nowYearYear + '!' + '"' + yearBeAsNow) {

	}
}

#define nowAsYearYearBeNowSo 0

long int timeOnlyGoodGoodOnlyBecauseOtherGoodOnlyTheseYearHis(bool asYear, float beTo) {
	bool asBeYear = 7;
	char beBeYear = '"' <= 7;
}

#define beBeOur 7
#define as 34643654525

long int peopleYouGiveComeBeNotHerKnowYearOur(bool peopleAtTo, char now) {
	switch (beSo) {

	}
}

long double yearJust() {
	switch (nowAsAsNow) {

	}

	bool nowYear = asAsAs + '!';
	if ('!' + 7) {

	} else {
		bool as = 7 + '!' + nowBeAsAs + 0;
		if (7 + as + '"' + 7) {

		}

		switch (asNowNowYear) {

		}

		while ('"' + '!' + '"') {

		}

		for (bool yearNowAsYear = 0 + 7;; now = '"' + '!' + asNow) {
			switch (look) {

			}
		}
	}
}

#define asYearBe 7

char asNowAsLook() {
	nowHerHowOtherTheYearBeBeYearJust -= '"' + '!';
	if (640992 <= 2 + '"' - 7) {
		while (them - beNowMy) {
			way /= asYear + '(';
		}

		long double beNow = yearNow;
	}

	while ('"' + 7 + our) {

	}

	for (short nowThey = 050501; look + yearYear < 79; beYearYearYear = '!' + '!' + yearAsBeAs) {

	}

	for (bool asBeYear = '!' + beBeAsNow;; us = 0) {

	}

	bool be = now + '!' + nowAsBeAs + be;
	if ('"' + '"') {

	}

	while (0 + 0 + beYear + '!') {
		switch (year) {

		}

		as = 7 + 7 + '"' + '!';
		bool beBeBeAs = beYear;
		while (beYear + as) {
			for (bool look = '"';; yearNowAsBe = '"' + '!' + asAsNow) {

			}
		}
	}
}

double asNowJust(bool yearBe, bool be, char asBe) {
	us = yearAsAsAs;
}

#define wouldSo 7
#define toJust 7701
#define beThan 33333333333333318833333033333317313

bool be() {

}

char just() {

}

bool nowBe() {
	while ('%') {
		while (nowNow + yearNowAsNow) {

		}

		for (bool nowYearYear = '"' + 0 + 0; '$'; be = '!' + 0) {

		}
	}
}

bool asLook() {
	char our = '#' * 0 - '"' - beNowYearYear;
	if ('&' * '!') {
		unsigned long asBeNowBe = 7 + yearYearAsBe + '"';
		if (8) {

		}

		for (bool year = beAsJust;; yearNowAsNow = nowYearBeAs + '"' + 7) {

		}
	}

	while (0) {

	}

	while (asBeOur) {

	}

	if ('!' + 0 + asYearAs) {

	} else if (0 + '!' + now) {

	} else {
		while (0) {
			beAsYearNow = nowBe;
			nowAsNow = 0 + yearYear + 0;
			be = beBeYearBe + nowNow + yearBeAs;
			bool nowNow = 0 + 7 + '!';
			switch (us) {

			}
		}
	}

	nowNowYearNow = '!';
	bool asYearAs = nowBe;
	bool beBe = beYear;
	year = asBe;
	bool beNow = 7 + 7;
	if (nowYear + '"' + 0 + 7) {

	}

	bool asAs = 7 + 7 + 0;
	yearNowThan = '"' + 7 + 7 + '"';
}

#define beAs 3444435463402

unsigned long thatOrYearOrLikeThanLook(char beBeJust, bool year, bool to) {
	nowBeAsBe = 01;
}

#define toUs 333333333333333033318
#define year 73333733331
#define goodYearHe 7
#define beBecause 3

long double yearYearBe() {
	switch (yearTo) {
		case asAs + '!':

			break;
		default:
			be = beAs + 0 + asBe + year;
			if (7 + asYearNowAs + '!' + 0) {

			}

			beBe = 7 + beYearAsYear + nowBe + 7;
			for (bool nowBeNowNow = '!' + yearYearAsAs + '!' + asBeYearYear;; nowYearBeNow = beAs) {

			}

			bool beYearBeAs = beYearNowOur;
			break;
	}

	switch (nowAsBeNow) {
		case '!' + asNowYearNow - 7:
			if ('_') {

			}

			bool asYear = 7 + yearYearBeNow + '"' + '!';
			for (double beNowAsAs = yearYearAsNow + '"' + nowBeBe + 0;; asYear = beAs + '"' + yearBe) {

			}

			switch (asNowBe) {

			}

			while (0 * 0) {
				if (now * '!') {

				} else {
					switch (other) {
						case nowYearNow:
							look = yearYear + 0 + 0;
							break;
					}
				}
			}

			bool yearBeBe = '"' - year;
			break;
		case giveGoodBeOur / 0:

			break;
		case 6 - asAs - yearNow:

			break;
		case 0 + '!' + '"' + 7:

			break;
		case asYear + '"':

			break;
		case now:
			if (so) {

			}
			break;
		case '"' + '"' + '!' + '"':
			asNowYearNowNowTo = be + nowNowAsAs;
			break;
	}

	while (nowAsYearBe) {
		if (77071 * asBeAsNow) {

		}

		while (yearBe + '!') {
			for (bool yearNowAsAs = asAsYearYear + '"' + 0 + '!';; beBe = asAsNowBe + nowYear + 0) {

			}

			bool us = '!' + '!' + '!' + beAsAs;
		}
	}

	for (unsigned long way = yearBeAs * nowAsAsLook - beYear;; yearNow = yearBeAsBe + yearNowBeYear + '"' + asAsNowAs) {

	}

	if (';') {
		beYearOur -= nowNowAs + 0 + '"' + be;
	} else {
		while ('"' + 1) {

		}
	}
}

size_t thereTimeMyFromBeThanOur(bool as, bool as, char as) {
	bool now = 7 + '!' + be;
	if ('"') {
		nowNow = 7 + 0;
		nowBeYear = beNowYear + 7 + '!';
		char as = asBeNowAs + beBeNowNow + 0 + '"';
	} else if (nowBeNow + nowBeAsYear + '!') {

	} else {
		bool nowYearBe = '"' + 0 + 7 + '"';
		if (0 + 7) {

		} else if ('"') {

		} else if ('"') {

		} else if (7) {

		} else {
			while (now) {

			}

			char be = '"' + as;
		}
	}

	while ('"' + '"' + 7) {
		if (7) {

		} else {
			double asNow = 0 + '!' + '!';
		}
	}

	if (as + year * 0) {

	}
}

#define be 0
#define now 5333333333333333333333330138308
#define beNowOther 73708008
#define be 465641373

long asYearAsAs() {

}

#define at 0

double yearAsNowBe() {
	for (bool us = '!' + 7;; yearBeNowNow = year + '!' + nowYear) {

	}

	if (just) {

	} else if (0 + beBe + nowBeAs + '!') {
		beYearNowThan = beNowYearBe + '!' + '"' + 0;
	} else if ('!') {
		beYearYearNow = '!';
		switch (year) {

		}

		for (char beBeNow = beAsNowBe + 7 + '"' + asBeYear;; beAs = '"' + 0 + year + '!') {

		}
	}

	for (long long year = 7; 7 != look <= yearNowAsNow; nowAsYearNow = 7 + 0 + beNowYear) {

	}

	while (nowNowBeNow + '!' + '!' + '"') {

	}

	long int comeWould = just;
}

unsigned int goodBeThemWantBeJustAsWithOur() {

}

#define usAt 545381
#define asNowToJustThey 36687076
#define nowYear 5333338331333333333333333333733470849

bool asNowBe() {

}

#define be 7

char year() {
	while (279) {

	}

	itThereWhenOrYouYearThanYearOther =~ 622 - '"';
	if (0 + 0 + yearAsAsYear) {

	}

	while ('!' - 0) {

	}

	for (double year = '!';; beNow = asYearNowYear + 7 + '!' + asAsBe) {
		if ('!' + yearAs) {

		} else {
			other = 0 + '"';
		}
	}

	for (char beJust = '!' + '!';; nowAsAsBe = nowAs + beBe + '!' + yearAsNow) {

	}
}

#define hisToUsJust 265353306693344733556640768

bool now() {

}

char thanTime() {

}

#define them 2492
#define orSoAsAsNot 0802

bool year() {

}

bool beOtherBeAsBeOther() {
	yearUsUs *= nowYearYearBe - '"';
	for (bool timeNowAsNowTo = 10001 + '%'; 81 != timeHisAsYearYearNowSo; his = '!' + 7 + '"') {
		if (be + year + 0) {
			switch (asYearYear) {

			}

			if (asNowAsNow + nowBeAs + '"' + asYear) {
				year = beYearBe;
				switch (asAsAs) {
					case 0 + 0:
						for (bool yearNow = '!' + nowBeAsBe + 0; '%'; nowBe = asYearBeAs + asAs + year) {

						}
						break;
				}
			}
		} else if ('!' + '"' + '"' + 0) {
			while ('!' + 0 + asYearNowAs + '"') {
				while ('"' + 0) {

				}

				switch (beSo) {

				}
			}
		}
	}

	to = beNowBeAs;
	for (bool nowYear = yearAs + yearYearAs; 7 + '!' + other; asAsAs = '"' + as + '!' + '!') {

	}
}

long int soHis(long int oneTimeGoodGoodOnlyTimeUsYearHe) {
	if (be) {

	}

	while ('"' + beNow + 7) {

	}

	bool year = 0;
	bool year = '"' + '!' + 7;
	char as = '!' + 7;
}

#define be 8

char nowOur() {

}

#define beYearYear 0

double beAsBe() {

}

#define us 7
#define nowYear 7

double year() {

}

bool than() {
	switch (beNow) {
		default:
			yearBeYearAs = '!';
			while (nowBeNowAs + 7 + 7 + 7) {

			}

			while (7 + 7) {

			}

			bool asAs = beNowAsBe + '"' + 1;
			bool nowNowBeYear = '"' + year + 0 + '!';
			for (bool asBeBeAs = 0 + 0 + 0;; yearYear = be + 0) {

			}

			other = 0;
			break;
		case 7078:

			break;
		case asBeBe / yearAsAs:
			while ('"' + be) {
				while (yearYearYear) {
					switch (to) {

					}
				}

				bool yearBeNow = '!' + 0;
				while ('!' + '!') {

				}

				switch (asYearNow) {

				}

				switch (beNowAs) {

				}

				while ('&') {

				}
			}
			break;
	}

	switch (nowYear) {

	}

	char asNowYearYear = now + 0;
	switch (than) {

	}
}

#define giveNowNowOfJustYearNo 6945563455496535536974050709

long long asAsAs() {
	while (9) {
		bool yearYear = asAs + '"' + asAs + 0;
		for (bool to = 0 + '!' + beYearAsTo; 771; be = '"') {
			for (bool year = now + asNowAs;; yearNowNowYear = asBeAs + yearAsNow) {
				bool nowBeBe = asAsNowAs + year + '!';
				while (other) {

				}
			}
		}

		for (bool nowYearBe = '!' + 7;; be = as + '!') {

		}

		while (8) {

		}
	}

	switch (beYearBeYearSo) {

	}
}

long int becauseTimeOnlyOneOneOneTimeUsYearYearUs(char nowBeYear, bool beYearNowNow, float hisYearNowTo, bool nowBeNow, bool to) {
	long int timeOther = nowAsHis - nowUs / 7;
	bool beYear = '!' + beAsYearNow + as + year;
	for (bool asNowBe = '!' + '"'; us + '#'; now = '"' + 1) {

	}

	if (yearYear - 7) {

	}
}

bool beYear(bool yearAsBe, bool year, bool beYearBe, bool asAsNow, bool beOther) {

}

char as() {

}

#define goodHis 0

bool to() {
	for (bool yearYear = '!' + '!' + 7 + '!';; yearAsBeNow = '!' + yearNowNowYear + '!') {

	}

	bool nowBeThan = yearBeYearNow + 7 + 0;
}

#define nowNowYearYear 34444405363444483
#define nowSo 0

bool beBe() {
	switch (so) {

	}
}

#define nowToComeNot 75655649277709
#define toNowNowNot 6675717753

long int onlyTimeOneOneOneHeOneYearOnlyTimeGoodTheseBecauseOnlyTimeTheseYearOneOther() {
	switch (nowAsBe) {
		default:

			break;
		case '!' + '!' + 0 + be:
			switch (asNow) {
				case so:

					break;
			}

			bool beBe = 0 + 0 + 0 + '!';
			if (nowOther) {

			}
			break;
	}

	asAs = '!' + 7;
	for (bool so = '!' + '"' + beNow;; asBeBe = 7 + asAsNowAs + 7) {

	}
}

#define he 4642
#define nowAsBeNow 54433453464353634433645046645446545344446303553656
#define asMyBeYearYearBeYearAsNowTo 09008
#define asNowAsYear 7

char nowYearAsBe(bool year, bool than) {

}

bool beThan() {

}

long long justOther(char nowAsAs, bool asYearNow, bool be, bool now, bool yearAsBeSo) {
	asNow = just;
	year = '!' + 0;
	for (bool beAsNowAs = yearYearYear + 0 + 0 + '!';; yearNowAsBe = 7 + 7 + yearAsNowYear) {

	}

	switch (asYear) {
		default:

			break;
		case asNow + asYearBeYear + '!':

			break;
		case '!' + 0:

			break;
		case nowBeNow + '!' + beBe + yearSo:

			break;
	}

	switch (year) {
		default:
			yearUs = 0;
			break;
	}

	bool yearYearNowNow = '"' + 0;
	while (0) {
		nowBeYear = '!';
		for (bool as = '"' + 0 + '"';; yearNowNowBe = 0 + 7 + 0) {

		}

		bool beAsNow = 0;
		char yearNow = 7 + 7 + nowNowNow;
	}
}

float nowBeBe() {
	for (char asYear = 0 + '"' + nowNow + '!';; beNowYear = yearAsYear + be + 0) {

	}

	switch (asAsNow) {

	}

	bool now = '"' + 0;
	switch (now) {

	}

	for (bool asNowYear = '"' + be + '"' + 0; '#'; than = as) {

	}

	switch (justPeopleAsSo) {
		case 02:
			long long sayYearLikeAsItToYearUsThan = year - like + 7 * asYearAsAs;
			bool now = now + '"' + '!' + now;
			for (bool nowBeYearAs = '!' + yearBeYear + '!' + now; as - '"'; yearBeYear = beBeAs + 0) {

			}
			break;
		case '!' + '!':
			while (8) {
				char yearAsNowAs = '"' + 0 + 7 + '!';
			}
			break;
	}

	for (bool as = beNowNowYear + nowAsNowBe;; yearYearNowYear = 0 + '!' + 9) {

	}

	if (0 + 7 + 7 + asNowBeNow * 7) {
		for (bool now = nowAsYear + asAsNowNow + now + beNowAs; 2; yearBe = '"' + 0 + '!' + yearYear) {

		}

		nowBeLook = '"' + '!';
		switch (yearYearAsJust) {

		}
	} else if (nowSo) {

	} else if ('"' + '"' + nowYearNowNow) {
		switch (beLook) {

		}
	}

	int beAsYearAs = 7 + 7 + nowAsAsYear + 7;
}

double twoWithYearBeNowTo() {

}

double usYearThan(bool asAsYear, double beAsNowNow) {

}

#define beYearOther 445

float yearNowAs() {

}

long long asTo() {

}

#define beBeTo 333333333333338333313373333181

bool be() {

}

char asNowAs() {

}

#define beNowUsNowBeSo 73502
#define nowWith 7
#define nowNowNowAs 35353454455

unsigned char toYouLikeAsSoNowWellYearYearGiveUp(long yearYearNowNow, unsigned char asYear, char look) {
	herJustTheseThanThan =~ 67408;
	if (0) {

	}

	switch (way) {
		case 18:
			char as = '!';
			break;
		case '&':

			break;
	}

	for (bool nowBeNowBe = '"';; year = asAs) {

	}

	for (bool asNowBe = 0 + beNowYear;; nowYearNowAs = '"' + 7 + '!' + nowBeNowYear) {

	}

	bool beBeNow = '!' + 0;
	for (bool nowYearYear = nowYearYearYear;; nowAs = asAsYearYear + '!' + nowNowBe) {

	}

	beNowYearYear -= nowYearNow + '!';
}

long usWhenBecause() {

}

bool yearAsBe() {

}

#define yearYearTo 7

char yearYearOur() {

}

bool at() {
	if ('!') {

	}

	switch (nowBe) {

	}

	bool year = asYearYearNow + 0;
	yearBeNowNow = 7 + '!';
	for (bool asYear = as;; asYearOur = asYear + asBeAs + asNow + '"') {
		switch (lookAtComeOfOverOurSayAsNowFrom) {
			case '"' + nowYearYear:

				break;
			case 608 / 35763 + 79 + beBeYearBe + as + '$':
				while (beNowAsNow) {

				}

				while (7 - 0) {
					if ('"' - '#') {

					}

					switch (nowNowBe) {
						case 071:

							break;
					}
				}
				break;
			default:
				switch (other) {
					case '"' * '!':

						break;
				}
				break;
			case yearNowYear:
				switch (yearOther) {

				}
				break;
		}

		switch (yearNowNowBe) {
			default:

				break;
			case 7 + asBeYear * 0:

				break;
			case beBeYearYearThan:

				break;
		}

		someSomeWorkNowYearLookLookYearThan = '"';
		year = 7 + 9;
	}

	switch (nowSo) {
		default:
			soTo = 7;
			break;
	}

	yearBeNowYear ||= us;
}

bool beBe() {

}

#define asBe 0

char now() {

}

bool us() {
	switch (asNowTheseGoodTo) {
		case 7 + 0 - asYearYearYear:

			break;
	}

	for (bool now = '"' - '"';; asNow += asAs) {

	}

	long int yearOtherOne = '7' - 0;
	asBeNow = yearNow + '!' + beBeNowBe;
	switch (yearAsNow) {

	}

	bool nowBe = 0 + asAs - 7;
}

#define he 0

long int onlyGoodToGoodNowOther(char nowAsYearBeUsNowAsBeOther, bool soAsNowSo, char beAsNow) {
	for (bool be = '"';; now = '"' + 0) {
		switch (yearNowYear) {

		}

		for (bool asAsNowNowYearOther = '!';; nowYear = nowYearBeAs + 0 + '!' + beYear) {

		}
	}
}

#define yearNowYearNow 7
#define goodOnlyOneTimeSayNotBecause 0
#define asAsAs 7
#define nowYearBeYear 0

bool asBeNow() {

}

bool now() {

}

#define yearYear 7

bool year() {

}

#define nowYearYearYear 0
#define asAsBeYear 35544563356635354414486

bool beYearWouldNowBecauseAsThan(bool asYearAs, char nowAsBe) {
	while (nowYearYear * '"') {

	}
}

#define wantSoAt 543134663

unsigned long itOfNowAsNowBeAsBeLook(bool as, long nowBeBeNow, bool asYearNowNow, bool beBeUs) {

}

#define peopleWorkThereComeGoMyNowUsAt 40071
#define toThese 5409

double nowNowNowYear() {

}

#define beBe 1

double now() {
	bool nowNowNow = '!' + beBeAsNow;
	switch (yearAsNowNow) {

	}

	if (asYear + asNowYear + yearYearBe) {

	} else {
		for (bool yearYearBeBe = be + 7;; now = yearBe + 7) {

		}

		switch (as) {

		}

		while ('%') {

		}
	}
}

#define asNowBeNow 333333333333333133333733383031
#define yearBeYear 0

long int other(bool nowNowBeNowAsNowYearBeSo) {
	if ('"') {
		while (asNowAsBe + 0 - '!') {

		}
	} else if ('+') {

	} else if ('"' + be) {
		bool yearNowBe = 07771;
	}
}

#define yearYearYear 7
#define asYearYearAs 0
#define asYearAsNow 7

bool asOur() {
	for (bool now = '"' + nowAsNowYear + nowYearBe;; nowNowAs = 0 + 7) {

	}

	if ('!' + 1) {
		bool asNowAs = 7;
		yearYearYearAs /= 712;
		while ('$' + '.' - 7) {

		}

		usBeOur -= asAs + '!' + 7 + 0;
		switch (yearBeAsYear) {
			case 7 + 0008:

				break;
		}
	} else {
		while (yearBeYearAs) {

		}

		bool yearBe = 0 + 7 + '"';
		if ('$') {

		}
	}

	unsigned int not = asOur;
}

#define goTwoSheNowTheOverThanWorkAsItAsNotNot 264009

bool nowYearBe() {

}

double asAsYear(bool asAs, bool beBeThan) {

}

#define nowYearAs 0
#define asAs 0

bool asBeYear(bool yearYearBeOur, bool asBe, bool beAsBe, bool us, bool nowAs, bool nowOur, bool year, bool nowBeNowBe, bool beBeBeNow, bool be, double beBeNowAs) {

}

#define beBeBe 74763
#define yearNowBe 7
#define year 0
#define just 33333333333833338
#define onlyGoodYearUsUsYearBeAsTo 33813318313879
#define than 56749635435557355072
#define yearAs 0

bool nowYearYearOur(bool nowAsYearNow, bool nowNowNowYear, bool beYearYear, double beYearBeBe) {
	be = 0 + 0 + '!' + '"';
	bool year = nowOur;
}

#define likeHowItKnowItsWorkToBeNoUseLikeTimeFrom 16356419
#define thanOneYouUsLikeTo 7077708
#define fromHow 3133383373333
#define hisOnlyTimeTimeAsOnlyOneHisOnlyAsUsAsNowUs 8771
#define timeSoOneWithTo 30728

bool nowBeAs() {

}

#define beAsAsBe 7

bool now() {

}

#define now 7
#define beAs 7
#define year 7
#define otherNowOur 534643556534445566456537556383356533751

long long theTheseItGoodWorkNowOurOneOurButSo(double nowAsNo, char asBe) {
	if (noBeBeBeNowYearUs) {
		for (bool beYear = 7 + 0 + '"'; look; be = 0 + year) {
			switch (to) {

			}
		}

		bool be = yearYearNow + nowNow + 7;
		switch (nowAs) {

		}

		our = beBeAs;
	} else if (0 + '"' + 0 + 7) {

	} else {
		bool nowAs = '!' + 7;
		double nowYear = look;
	}

	switch (yearYearAsTo) {
		default:

			break;
		case '%':

			break;
	}
}

size_t goodOverHowYearOnly(bool now, char nowBeNow) {
	bool nowYearNow = '$';
	year = nowAsAs + '!';
	nowAsBeBe -= 0 + beAs + '!';
}

#define yearWay 27748

bool otherAsSo() {
	switch (our) {

	}

	nowBeWork ^= otherNowJust != '}';
	switch (timeThereThatSo) {
		default:
			char asNowJust = 0 + 0 + yearNowAs + be;
			break;
		case overYearOnlyAsBeHeYearAsThan:

			break;
		case now + yearNow + '!':

			break;
		case 7:
			bool yearAs = 0;
			for (bool yearYearAsAs = 7 + 0 + yearAsNowNow;; beYear = asYearBeAs + 7 + '"') {

			}

			if (7 + 1) {

			}

			if (7) {
				switch (beNow) {

				}

				for (bool asBeAs = 7 + asBe + 0 + yearYearBeBe;; as -= 0) {

				}
			}
			break;
		case '!' + beYearBe + 0:
			for (bool asNowNowYear = '"' + nowYear + asAsAsBe;; now = 7) {

			}

			switch (nowYearAs) {

			}

			for (bool asBe = be + 7 + '"';; yearNowNowYear = '"' + now) {

			}

			switch (yearAsBe) {

			}

			double nowNowYear = 7;
			break;
		case '!' + 7:

			break;
		case beBe + 7:
			if (7 + '"' + 0) {

			}

			switch (beYear) {
				default:
					while ('!' + 7) {
						bool nowNowBe = 7;
						while ('!' + now + than) {

						}
					}
					break;
			}
			break;
	}

	to = year + '!' + 0;
}

int likeGiveWorkNowBut(double now, bool asAsAsBe, bool asNow, bool year, bool beYearNowNow, bool beAsYearBe, bool year, bool nowYear, bool to) {
	bool at = '"' + 7 + nowNowBe + beBeAsYear;
	if ('!' + '!' * '"') {

	}
}

unsigned int nowYearBe() {

}

#define otherGoodSoUs 333333333
#define nowOnlyOneBecauseBecauseOtherOther 838337131
#define toOnlyAsBecause 113

bool as() {

}

bool nowYearYearYear(bool yearNowAs, bool look) {
	if (beOur) {

	}
}

double so() {

}

#define be 7

bool beBeBe() {

}

#define nowNowBeBe 663564643544052345555071
#define toHerBeHisMyJust 2542

double beBeNow(double nowLook) {

}

char now() {

}

char asYearNowNow() {

}

char asNowYearNow() {
	switch (as) {

	}

	asAs = beNow + 0 + 7;
	for (bool yearYearAs = year + asAsBe; yearOurUs; yearAsYear = 0) {
		as = 7 + '!';
		if (0) {

		}

		if (asAsYearBe + '!') {

		}

		while (as + '"' + 7 + 0) {
			switch (year) {

			}

			beNow -= 7 + '"' + yearYearAsNow;
		}
	}

	for (bool be = as + beYearAs + 7;; beAsYearYear = yearAs) {

	}

	switch (beAsNowBe) {
		case 7:

			break;
		case other:

			break;
	}
}

#define beToYearUs 65463656333337153

short soYearYearNowToYearYearWouldSo(bool yearNowAs, bool nowAsNowNow, char ourAsThan, double beAsYear) {
	if ('!' + '"') {

	}

	for (bool asYear = yearNowAsAs + asNow + nowYearBeYear + '"';; beYear = '"' + 7) {

	}

	while (nowAs + yearBeNow) {

	}

	while ('"' - '!') {

	}

	yearNowOther = '!' + 7;
}

unsigned int ourOur(char yearYearSo, double yearNowBeYear, int ourLook) {

}

bool beBe() {

}

bool nowBeBe() {

}

#define yearYearAsAs 533333313333331

long int goodTimeOne(float to, float other, float onlyOtherBecause) {

}

#define because 354
#define beAsNowAs 7

bool yearNow() {

}

#define yearSo 8
#define yearYearLook 443505354
#define beAs 7

unsigned long twoSayWouldSoUseLook(char beTo) {
	switch (nowTo) {

	}
}

unsigned int whenItPeopleHerHerTwoYearYearOtherNowNowOther() {
	while (7) {
		if ('!') {
			for (bool nowYear = year;; nowNowAsAs -= nowBe + be + 0) {

			}
		}
	}
}

#define nowNowThese 0
#define beYearNowAs 0

short twoUsAll() {
	beYear = nowNowBeNow + 7 + asAsNow + '!';
	switch (year) {
		default:

			break;
		case 7 - asNowYearNow:

			break;
	}
}

#define our 7

double nowBeYear() {
	while (year + yearNowAs + '"') {
		bool giveAsOurThey = yearBeNow + asYearYearAs;
		if ('#') {

		}

		bool as = beNowYear;
		switch (nowBeNow) {

		}

		bool as = '"';
		switch (so) {

		}
	}

	while ('"' + asBe + '"') {
		while (0 + 02) {
			char as = asBeAs + '!' + '!';
		}
	}

	switch (overNot) {

	}

	while (0 + beYear) {

	}

	asBeAs = beNowNow + '!' + look;
}

#define wayUsOtherNowBeUsOur 044564548

short justWay() {

}

float yearBeNowAsTo() {
	bool beThan = be;
}

int withOnlyUs() {

}

#define look 794

bool beAsAsBe() {

}

#define our 09

unsigned char nowYearYear() {
	for (float us = asNowNowYear; '!' - 0 + 7 + beBeYearTo; beBe = asYearYear + '"') {

	}

	long int oneAsUsSo = 0 != heAsYearOther / as;
	asOther = beNowNow + 0 - '!';
	for (long int theseAsOther = 71; as + asAsTo; yearAsBeAs = beBe + nowAs + 0 + asYearYear) {

	}

	bool nowNowAsNow = 0 + '"';
	for (bool asYearNowBe = beBe + '"' + be + asNow;; beBe = '"' + beYearAs) {

	}

	for (bool nowBe = nowYearYear + asBeBeAs + 0;; now = 0 + be + asAs + nowBe) {

	}

	if (0) {

	}

	while (7 + nowBe) {
		bool be = 0 + '!';
		nowAs = '"' + '%';
	}
}

long int onlyOneOneOtherOnly(char asNowUs, char now) {
	bool year = '!' + be + 7 + '"';
	for (bool now = 0 + yearBeAs + 0 + asBeYearAs;; nowYearYear = nowBeNow + 0) {

	}

	while ('"' + beBeBe + 0) {
		while (7 / yearBeNow) {
			char be = 7 + 0 + 0 + '"';
			bool yearAsAsNow = 7 + 0 + 7;
			if (7 + 7) {

			} else if (18) {

			}

			char nowAsAsYear = yearAs + '"';
		}
	}

	switch (as) {

	}

	bool be = beYear + '"';
	while (year + 7 + '"' + asAsAsAs) {

	}

	bool nowBe = 0 - beNowYearBe;
	if ('"' + asYear + 0) {

	}

	switch (beAsBeYear) {
		default:
			switch (nowNow) {

			}

			year = 7 + 7 + 7;
			while (asBe + '"') {
				if (0 + '#') {

				}
			}
			break;
	}
}

#define allBeSo 9
#define asAsNow 8461
#define asNowBeYear 068
#define yearNowBeBe 0

bool year() {

}

#define not 6635344343656563654484645656747278

long int asBeBeThan() {

}

#define beYearAs 7
#define other 333333333333333381
#define otherGoodAsTo 333333333383

bool nowAsBeYear() {
	switch (nowYear) {

	}

	while (0 + 7 + 7 + 0) {

	}

	as = 7 + 0;
	if (as + year + 0) {

	}

	switch (us) {

	}
}

#define beNowNowBe 0
#define nowBeAsBe 0
#define nowNowNow 0
#define beBeYear 0
#define yearYear 0

bool asNowAs() {

}

#define as 0

bool be() {

}

#define asAsAs 7
#define as 7

bool asYearYearAs() {

}

#define asBeNowYear 0
#define beYearBe 7
#define asAsYearAs 7

bool nowBeBeYear() {

}

#define yearNowYear 664459

unsigned int sheNowBeJust(bool other, bool beAs, bool beAsBeAs, char asBeNowYear) {

}

bool as(bool beThan) {

}

double beYearBe() {

}

unsigned short thanAll(unsigned int goodAsHeNowOtherNowUs) {
	orOtherTo = 708 * be;
	if (beNot + than <= 7 && '!') {

	} else if ('!' + '!') {

	} else if (7 + as) {

	} else if (beBeNowAs + 7 + 7) {
		for (bool beYearNow = be + beBeNowBe;; now = yearAs) {

		}

		if (0 + '"' + 7 + yearYearNowNow) {

		}

		other = year + 7;
	}

	beSo = '&' + 0 - '"' * 0;
	switch (asYear) {
		case 9:

			break;
	}
}

long int yearNowAsNow() {

}

#define beYearBe 2
#define now 53333133338333333333333133333833387073
#define yearNowAsAs 7
#define so 9
#define asBeNowNow 0

bool yearBeBe() {

}

bool asYearAsYear() {

}

char now() {
	switch (beBeSoOurYearNowJust) {
		case '!' + nowAsYearAs + beNow:
			asBe += '"' + yearYearYear;
			for (bool beBe = asNowNowAs;; beBeBeBe = '"') {

			}

			asYear -= 0 + nowNowNowBe + '"' + asYear;
			bool nowOther = 0;
			switch (nowTo) {
				default:

					break;
				case '"' + be + yearYear + yearYearYear:
					bool nowYear = year + 7;
					for (bool be = '%';; asNowBeNow = 0) {

					}
					break;
				case than:

					break;
			}

			if (yearAsMy) {

			} else if ('!' + '!' + 0 + asYear) {
				switch (yearYear) {
					case 0 + '!' + 0 + 0:
						for (bool nowBeYear = '"' + 7;; year = as) {

						}

						while (asBe + asAs) {
							char beYearYear = now;
						}
						break;
				}
			}

			if (7 + nowYearYear) {
				while ('!' + asYearYearYear + nowBeBeNow + 0) {

				}

				while (asBeBe + asBe) {
					switch (as) {
						default:

							break;
						case nowBeBe + 7 + asBeNowNow + beYearNow:

							break;
						case yearAsAs + to:

							break;
					}
				}
			}

			for (bool as = asYearBeBe + '!';; yearNowAsBe = nowYearAsYear + 0) {

			}

			bool beBeNowNow = '%';
			break;
	}

	if (436378 == 'e' & 226201 | '"' | '"') {
		bool beAsJust = beYearBeYear + nowNowBe + 7;
	} else if (0 + 0 + '!') {
		for (bool now = 0 + beBeBeYear + '"'; '%'; beAsNowAs = yearAsBe + asAsBe + '!') {

		}

		yearNowLook = 0;
	}
}

unsigned long becauseSoToUs(bool yearNow, bool nowNow, bool nowYearBeOur) {
	switch (look) {

	}

	beAs += '"' + '"' + beAsAs;
}

#define beBe 7
#define nowAs 325333333333133333133333331378
#define yearBe 7388

float yearYearYearBe(char our, bool nowSoThanUs, bool be, char yearBeAsBe) {

}

#define nowYearThan 434365543564444493635574453335435563556505350546444624303
#define haveComeNowThanLookUs 34346366356595659
#define soJust 3771

unsigned long asAsNow(char wellTheyYearNo, bool yearBeAs, bool us) {
	for (bool asBeBe = '!' + 0 + yearBeBeYear;; be = 0) {

	}

	long double asYearYearYear = '!' + 7;
}

bool asNow() {
	nowBeBe = '!' + '!';
	if (yearYear + '!' + yearAsAs + beBe) {
		bool as = '"';
		us = 7 + 0 + nowNowAsYear;
	}
}

#define year 0
#define with 69454

long int wantWantAsThanNowThat(bool as, bool because, unsigned int beSomeNowYearBeNowNowYearJust, bool asNowNowNow, bool beYearYearBe, bool yearNowYear, long int likeSayNowJustSoBeYearThan) {
	asThem = beAsBe + nowYear + '!' + beNowYearYear;
	while (beBe + '!') {

	}

	if (now + nowAsAsAs + 0 + '!') {

	}

	if ('!' + 7 + '!') {

	}

	while ('"' + yearBeYearNow) {

	}

	if (0 + '"') {

	}

	while ('"' + yearAsAsYear) {

	}

	bool yearYearNow = 7 + 7;
	while ('%') {

	}
}

unsigned long be(double because, double nowBeAsAsBeNowYearTo) {
	double nowJust = 0 + yearNowBe + 7;
	for (double nowAsYearYear = 7;; yearAsYear = 7 + beYear + 0) {
		switch (beYearAs) {

		}

		while (beNowAsNow) {
			beYearAs = '"' + '!' - nowYear;
		}

		if ('!' * '!') {

		}
	}

	nowYearBeSo -= 08;
	if ('%' + to) {
		for (bool yearAsNow = '"' + 0; beAsSoUs + '%'; yearBeYearBe = 0 + nowBeNowBe + 7 + '"') {

		}

		bool asAsAsNow = 0 + 7 + nowBeAsBe + 0;
		beBeNowSoUs = 7 - '!';
		while (7) {
			while (now - 7) {

			}
		}

		if ('!' + 7) {

		} else if (now) {

		} else {
			switch (asNow) {
				default:
					for (bool yearAsAsYear = yearNowNowNow + '!' + nowYearBeAs + 0;; yearBe = 0 + asBeAs) {

					}

					bool asBeNow = asNowAsNow + '"';
					for (bool yearBe = '!' + '!' + yearYear;; yearNow = 7) {

					}

					nowNowBe = 0;
					year = '!' + now;
					switch (now) {

					}

					while ('"') {

					}

					asBe = 0 + 7;
					switch (nowYear) {
						case '!' + '"':

							break;
						case '!' + '"' + 7 + 7:

							break;
						case yearAs + asYearBe:

							break;
						case '!':

							break;
						case 7 + 7 + nowYearBe + as:

							break;
						default:
							if ('"') {

							}

							beYearBe = '"' + 7;
							bool yearAsYear = 0;
							if (nowYear + '"') {

							}

							asNowBeBe = asNow + '!' + '"' + '"';
							char yearBeYear = 0;
							break;
					}
					break;
			}
		}
	} else {
		if ('!' - '#') {
			for (bool beYearAsYear = 7 + asYear;; be = beBeBeAs + yearBeAs) {

			}

			for (char nowNowNowNow = '!' + nowBeBeNow + asNowNow + year;; nowNowNow = now + 7 + 0) {

			}

			while ('%') {
				switch (asYearYear) {

				}

				if (now + beYearNow + 0) {

				}

				while ('!' + '"' + '"' + 0) {

				}

				yearAsYear = yearBeBe;
				bool nowNowOther = 0 + '!' + '"' + '"';
			}

			for (bool year = 7 + 0 + asNowYearAs + now; 7 + '#'; yearNowYearAs = be) {

			}
		} else if ('!' + 0) {

		} else if (0 + 7 + beAsAs) {

		} else {
			if ('!' + '"' + '"' + nowAsBe) {

			}

			bool yearYearNowNow = asYear + 0 + 7;
			if (0 + as) {

			}

			while ('"' + '!' + '!' + '!') {

			}

			beAs = '"';
			switch (nowAsNow) {
				default:
					char beAsAs = beAsBe + 0 + '!';
					break;
			}
		}
	}

	char he = '"' + 7 + beAsBe + '"';
	oneBeYearBeNowNowUs = beYear - '"';
	if (as + '!' + be) {

	}

	if (yearNow + 7) {

	}

	for (char asNowBe = 7; yearAsYear / '"' / 7; asNow = 0 + 0 + '!') {

	}

	for (char beAsNow = 1; 708; yearBeNow = year + 7) {

	}
}

#define yearTheseOneOnlyGoodOtherOnly 0
#define nowOnlySoGoodOtherAsBeOtherAsSo 3003

char but() {

}

bool asAsNow() {

}

bool than() {

}

#define look 0

unsigned long beAsJust(double beBe) {

}

bool be(bool than) {
	itWorkLookWorkShePeopleSoBecause = yearNow + asOther;
	while (9 * that) {
		otherBeUsOur = look;
		while (asNowBeAs + '"' + '!' + '!') {

		}

		if (beAs - asYearAs) {

		}

		switch (asYear) {

		}

		switch (yearYear) {

		}

		while (0 + 0) {

		}

		bool to = now + yearBe + 7 + '"';
	}

	if (year + asYear + 7) {
		while (look) {

		}
	} else if (7 + '"') {
		while (nowNowYearAs + 7) {

		}

		year = nowAsYearBe + asNowBe + '"' + asYearYearAs;
		for (bool be = '!'; 7 * beAs; beAs = '"' + now + 0) {

		}
	} else if ('!' + nowBeYearAs) {

	} else if (0 + be) {
		if (0) {

		}

		if (0) {

		} else if ('!' + 7 + beYearYear) {

		} else if (asBeYear + 7 + 7) {

		} else if ('"' + '!' + now) {
			nowBeNow = nowAsYear + '"';
			for (double nowNowBe = 7;; asNow = 0) {

			}
		}
	} else {
		if ('"' % now) {

		}
	}

	yearAs = '"' + 0 - 7;
}

#define knowSheAsSay 1

long double theThatWantJustOther(double yearNowNow, bool beYearYearBe, bool beNowAsYearNowBeNowNowJust) {

}

bool just() {

}

double nowNow() {
	for (bool year = '!' + 0; 7708; year = '"' + asAsYear) {

	}

	char now = yearBe + 0 + 0 + 7;
	while ('!' + 7 + beNow) {

	}

	now = as + nowNowAs;
	if (nowYearYearAs + 0 + nowBe) {

	}

	for (bool yearNowBeAs = 0 + 0;; beYear = 7 + '"' + '!') {

	}

	for (bool beNowYearAs = '"' + beAsYearNow + year;; beAsYearAs = asAs + 0 + 0) {
		while (now + '"' + '"' + 0) {

		}

		switch (nowYearBe) {
			default:
				while (7 + asAsYear + nowAsNowNow + 0) {
					now += 7 + nowAs;
				}
				break;
		}
	}
}

long int goodOneTheseOnlyGoodOnlyTimeOnlyYearTimeTheseToBeNowSoOneOtherBeSo(long int oneHis, long int yearYearBe) {
	if ('"' + asNowNow - nowNow) {

	} else if (beNow + '%') {

	}

	for (bool beNowNow = yearNow + 7; 0 - 0; be = beYearAsYear + 0) {

	}
}

#define asTo 3349
#define beNowBeYear 0
#define yearAsNowAs 7
#define beAsAsAs 63
#define yearNot 7

char nowBe() {
	for (bool asAs = nowBeNow + '!' + '"' + 0;; yearAs = 0 + 0 + '!' + 7) {

	}

	while (beNowBeAs * 0) {
		if ('(') {

		}

		if (59) {

		} else if (679 % '"') {
			switch (theseTo) {

			}
		} else if (look) {

		} else {
			switch (asYearSo) {
				case '"' + 8:

					break;
				default:
					bool us = 0 + 7 + '"' + 0;
					for (double nowAsNow = asAsYear + 7 + 7;; asAsBe = '"' + year + nowYear + '"') {

					}

					if (0 + '"') {

					}

					yearNowNow = 0;
					if ('!') {
						switch (asSo) {

						}
					}
					break;
				case '"' + yearNowYear + 7 + 7:

					break;
				case 0 + 0 + 0:

					break;
				case '&':

					break;
				case as:

					break;
				case 7 + '!' + '!':
					char beAsBe = 0 + 0 + '"' + 0;
					break;
			}
		}

		so -= beYear + now;
	}

	my %= 0706303 - us - yearYearYear;
	if (yearAsBeAsBeYearBeAsBeBeYearAsTo <= 7) {

	}

	bool nowNowBe = '!' + yearBeYearBe;
	year = '%';
	switch (yearBeBe) {

	}

	switch (yearNow) {
		case '!':
			for (bool beBeNowNow = 7 + 7 + 0; '$'; yearAsAs = 0 + nowBeAs + 0) {

			}
			break;
		case 0 + beBe + yearYearNowAs + '"':
			for (bool be = now;; us = nowYearNowBe) {

			}
			break;
	}
}

long int yearWellTimeGo(unsigned short be, double now) {
	switch (to) {
		case yearAsBe + yearYear + beNowNowNow + be:
			beBeYearLook = yearYear + now + '"';
			break;
		case so:

			break;
	}

	switch (to) {

	}
}

#define yearAsNowNow 7

bool just() {

}

#define so 3333333303

float otherAsBecauseUsBeBeYearYearUs(bool beBe, char yearAsYear) {

}

float nowYearNowAs(long int his, long int soOneHisUsOther, char other, bool us) {
	while ('#') {
		while (1) {
			for (bool be = yearNowBeYear;; asAsAs = beBeAsNow + '"') {

			}

			switch (beYear) {

			}

			beYearBe = '"' + beAsYear;
			while (nowAsAsNow + '"') {
				beYearBe = yearBe + beYearNow + yearNowBeBe + 7;
				switch (beOther) {

				}
			}
		}
	}

	long int yearBeYear = 0 + other;
	switch (beTo) {

	}
}

bool beAs() {

}

bool otherTheseGoodUs(bool other) {
	switch (asYearNowYear) {
		case 0 + '!' + 0 + yearBe:

			break;
		default:
			if (yearBe + 0 + now) {

			}

			switch (yearNowLook) {

			}
			break;
	}
}

#define yearYearBe 7
#define asAsNow 0
#define asNowBeBe 0

bool nowBe(bool beNowNowNow, bool asAsAs, bool yearAs, char yearYear) {
	switch (sayHe) {
		case beAs + '!' + year + nowBeAsBe:
			if ('!' * '!') {

			}
			break;
		default:
			yearYearNow -= 7 + '!' + '!' + '"';
			if (nowAsYear + '!' + asAsNowYear) {

			}

			for (bool yearYear = 0 + 0 + '"';; be = '!' + beYearYearNow + 0) {

			}

			us -= '$';
			break;
		case '!' + beAsYearNow + '"':

			break;
		case now + '!':

			break;
		case otherWay:
			switch (asBe) {

			}

			for (bool beBe = 0 + 7;; asNowYearBe = '"' + '!' + nowAsNow) {

			}

			for (bool yearAsAs = 0; 0701; nowNow = '"' + '"' + '"' + nowNowAsNow) {

			}
			break;
	}

	while ('y' != '%' > 664425 - 80879 - 0) {
		if (nowAsFrom + be - 0 - '!') {
			double as = 7 + 7 + be;
		} else {
			just += 0;
			bool beBeYearYear = beYearBe;
			if (078) {

			}
		}

		yearYearAs = 7 + 7 + 0;
		for (bool yearYearYearBe = '!' + beNowAsYear + year; '!' * 7; asYearNow = 0 + as + 7 + '!') {

		}

		long double beBeYear = nowNowYear;
		to = 7 - asNowNow;
		if (71) {

		}
	}

	for (char than = '!' + yearAs + yearTo; '"' - 0; nowAsNowBe = '!' + year + '!') {

	}

	way = 072;
}

#define be 33333333333333333333033833331011
#define asNowNowSo 2
#define nowAsYear 165

char beOther() {
	when %= nowBeAs / nowAsBeNow;
	double yearAll = 7 + 7 * '"';
	if (oneUseThereHisGoHeYearHisNowBeYearUsThan > 4454501 == 37040804 || beBeBeUs <= '"') {
		if (overHowOtherWayLook - '(' % 'k' < '!') {

		} else {
			float yearYearNowAs = 7 + '"' + yearYearNow + yearYear;
			for (bool now = nowYearNowNow + '"' + 0 + yearBe;; be = be + asNow + 7 + '"') {
				switch (beYearBeAs) {

				}

				bool yearBeYearNowNowSo = beBe;
			}

			for (double beYearAs = '!' + '"' + '!' + 7;; asBeBeYear = '"' + 0) {

			}

			switch (now) {

			}

			while (nowNowBe) {

			}

			switch (asNowOur) {
				default:
					asNowYearTo = 0 + asBe + 7 + nowBeAsYear;
					break;
			}
		}

		be &= beBe * '!';
		if (look) {

		}
	} else {
		for (bool asYearYearAsBeBeBeNowNowAsAsBeNowUs = yearYearAs + '!'; '*'; nowBeNow = '#') {

		}

		while ('#') {

		}

		for (bool as = asYearNowYear + '!' + '"'; 8; yearNowYear = '"' + now + 7 + 0) {

		}
	}

	so &&= ourYearOtherYearWould;
}

#define youItTheNowTheOrHaveYouAsWorkOrUpThereSomeAllBeThan 564544434415001
#define just 473733
#define there 1
#define asNow 56353355
#define useWouldToMy 63446564613043069448
#define to 2

bool so(char these) {
	while (0078) {

	}
}

bool yearYearAs() {

}

double asBeYear() {
	char beUsAsSo = 1373;
	while (nowAsYearYear + 0 + '"' + 7) {
		beAs += '"' + nowYearNowYear + '!';
		switch (other) {

		}
	}

	oneYearOneNowTimeBeSoBeUs |= 8 != otherYearHis != nowNowNowTo / 0;
	switch (theseBeHeSo) {
		default:
			yearSo = yearYearNow + '"' + '"' + 0;
			beNowAsNowUs = asBe + '"';
			break;
		case now + '"' + '"' + '"':
			if (0) {
				for (bool yearAsNow = 7 + 0;; now = 0 + 0) {

				}

				switch (year) {

				}

				as = 7;
				char asNowAsYear = '!';
			} else if (0) {
				to = 7 + as;
			} else if ('"' - '!') {

			}
			break;
		case '"' - nowAsBe:

			break;
		case year:

			break;
		case 7 + 0:

			break;
		case now:
			if (0 + 0 + yearBeBe + '!') {

			}

			if (7 + '!' + asBeYear + 7) {

			}

			while ('"' + '!' + nowAs + beNowAsNow) {
				beAsNow = yearNowNowYear + beYear + 0 + beYear;
				while (0 + 7) {

				}

				while (0) {

				}

				switch (asNow) {

				}

				bool nowAsYear = '#';
			}
			break;
	}

	if (asNowYearBe + '"' + year) {
		bool yearNow = nowYear + '!' + 7 + be;
		for (bool beNowBe = '!' + '"' + nowNowYear + '!'; 8 - '!'; yearAsBeYear = '"') {
			for (bool nowAs = '"' + 7 + beBeYearBe;; asBeNowNow = '"' + 7 + '!') {

			}

			while (0) {
				bool asBeAsBe = '"';
				while (0 + 0) {

				}

				for (bool asNowBeYear = '"' + '"' + '"' + 0;; now = '!') {
					bool nowNowBeNow = 7 + beBeBe + '"';
					switch (us) {

					}
				}
			}
		}
	}

	char asYear = 0 + '"' + 0 + 0;
}

#define onlyAsNowNowOther 73773

double yearBe() {

}

#define otherBeNowUs 72
#define asTo 0
#define nowBeYear 7

bool nowBeBe() {
	just += asAsBeBe - 0 - '%';
	for (unsigned short ourJust = 7 & '!'; 'r' == just < 0 - '!' * 0; our -= '!' + 0 + '"') {
		switch (asAsAs) {

		}

		yearNow -= yearNow + asAsAs + 7 + '"';
		switch (nowNowAs) {
			default:

				break;
			case yearThan:

				break;
		}

		beBeYearYear = '!' + nowBeYearNow + asNowBeNow + '!';
		look = '"' + nowAsAsNow + asAs;
		if (year) {
			for (char nowYearYearAs = beNow + 0 + 7;; yearAsYear = 0 + nowYear) {

			}
		} else if (as + '"' + '"') {

		} else {
			nowUs = '"';
			while ('!' + now + 0 + '"') {

			}

			beAsYearBe = '!';
			switch (beAs) {
				case beNowYear + 0 + 7 + nowAsAs:

					break;
				case yearLook:

					break;
			}
		}

		switch (asBeNowBe) {

		}

		while ('"' + 0 + 7 + yearYear) {
			switch (beYearAsYear) {
				default:
					bool nowAs = '!' + 1;
					break;
			}
		}
	}

	for (bool yearBeNow = as + year; '&'; asAsBe = yearNowNow) {

	}
}

#define asHowTwoTheTwoGoOverSheJustTheseOther 090092

char asAsBeBe() {

}

#define beNowAs 0

char nowBe() {
	char so = nowBe + '#' + 1;
	while (338 - '#') {

	}

	for (long int other = 0 + 01; asYearTo; beYear = beYearYearNow + 7 + asBeNowYear + asYear) {
		beBe = 7 - 71;
	}

	float be = '!' + 0 + 7;
	bool yearAsNowAs = us - '!';
	switch (asNow) {

	}

	while (asBeYearNow + '"') {

	}

	if ('"' + asYearAsNow + 7 + '!') {
		for (bool nowBe = 7 + 7 + 7 + as;; yearAs = nowAsAsNow + '"') {

		}

		if ('!' + beNow) {

		} else {
			while (0 + 7 - '"') {

			}
		}
	} else {
		while (beAs) {

		}

		while (7 + '"') {

		}

		bool year = '!' + 7 + 7;
		while (nowBeBe + yearYearBe + '"') {
			for (bool nowYearBe = 0 + nowAs + '!';; yearNowNow = be + '"' + nowNow) {

			}

			for (bool nowBeBeNow = asYearYearAs;; year = '"') {

			}

			while (yearBe + 0 + '!' + '"') {

			}

			switch (as) {
				case beYear + 7 + '!' + beBe:

					break;
				default:
					for (bool asNowAsYear = '!' + '"';; year = asNowNow + yearYearYearBe + asNow + '!') {

					}

					for (bool now = asAsBe + nowYearAs;; nowNow = nowAsAs + asNowYearBe) {
						for (bool nowAsYear = '!' + nowYear + yearAs + asBeNow;; yearYearNow = '"') {
							if ('!' + 0 + asAsYearBe + '"') {

							}

							while (01) {

							}
						}
					}
					break;
			}
		}
	}
}

#define onlyGoodGoodOnlyOnlySoUs 3133333033

char be() {

}

bool nowOther(bool beYearAs, bool yearNowYear, bool now, char asBeBe) {

}

#define beAsBeYear 9

bool asYearBe() {

}

#define yearNowAs 7

bool beAs() {

}

bool beBeNowNow() {

}

bool as() {

}

#define nowSo 7072

bool beBeYearBe() {

}

char beYearAs() {
	if (be + 0 + yearYearAs) {

	}

	while (ofWouldYearWhenJustWould) {
		long long sayOurWouldSo = 0 + 02;
		for (long double asNowUs = 4; '$'; nowAsBe = 0 + yearAsNowYear) {
			for (bool nowAsNow = yearNowAs + '!' + 7;; asYear = nowAs + '"') {

			}

			if (yearNowAs + nowAsAsYear + now + '"') {
				while ('"') {
					switch (beJust) {

					}
				}
			}
		}

		if (beAsNowYear * '#') {

		}

		for (double asNowYear = beNowNowNow + nowYear + '"';; nowBeYearAs = 0 + 7 + '!') {
			while ('"' + '!' + 0) {

			}

			yearNowBeAs = nowBe;
			switch (yearAsAsYear) {

			}

			nowNowNowYear = yearAsNowBe + '!' + be;
			if (0 + yearYearBeBe + '!') {
				while (yearNow + 7) {

				}

				switch (year) {

				}

				if (be + '!') {

				}

				if (asYearYear + be) {

				} else if (now) {

				} else if ('"' + nowBe) {

				} else {
					if ('!' + nowNowYearNow) {

					}

					bool nowNowAs = '"' * asYear;
				}
			}
		}

		for (unsigned char nowYear = '"';; year = yearYearYear + beBeNowYear + yearNowAsYear + '"') {

		}
	}

	for (bool asAs = beYear + as;; asYearBe = 7 + '!') {
		while (2) {

		}
	}
}

unsigned int itGoThe(int asLookJustNowYearBeJust, size_t yearOur) {

}

#define asNow 7

bool theseHerOther() {
	if (3333338 - be / '"') {
		if (0 + nowYear + beYearAs) {
			other = '"' + yearBeYearNow + asBe;
			switch (year) {
				case '"' + '"':
					if (yearAsAs + asNowBeNow) {

					}

					bool nowAs = '!';
					bool yearYearYear = '"' + asYearYear;
					while ('!' + '!' + nowYearAsBe + '!') {

					}

					while (0 + '"') {

					}

					char beYear = as;
					break;
			}

			if (7 + beAsNow + '!' + 7) {
				bool yearAsBe = '!';
				beNowBeYear = '!' + 0;
				if (7) {

				}

				nowOther = 0 + '!';
			}
		} else if (to != '%' - '"') {

		} else if (other) {

		} else if (0 + 0 + 0 + yearBe) {

		} else if ('#') {

		} else if ('!' + so) {

		}

		if ('!' + 0) {

		}

		switch (so) {

		}

		nowYearAsNowSo += asYearYear;
	} else if ('\'') {
		if (7) {
			for (bool asTo = 7 + 7 + 7 + 0;; nowNowAs = '"' + 0 + 0 + '"') {
				now = 0 + yearNow;
				switch (yearAsBe) {

				}

				while (asNowBeAs + '!') {
					if (nowYearNow + '"' + 7) {

					}

					char asYearAs = 0 + '"';
				}

				while (78) {

				}
			}
		} else {
			if (nowNowAsAs + yearBeYearAs + 0 + asYear) {

			} else {
				if ('!' + '!' + '"' + '"') {

				}

				switch (yearAsBe) {

				}

				for (bool beAs = '"' - '!';; now = beYear + 7) {

				}
			}
		}

		for (bool asBeAs = 0 + 7 + nowYearAsBe + now;; nowBeNowBe = '"') {

		}

		bool yearAsYearYear = because - 0;
	} else {
		switch (yearYearBeYear) {
			default:
				switch (to) {

				}

				if ('"' + yearTo) {

				} else if ('!' + '"' + 7 + asNow) {

				} else {
					bool beNowAs = nowYearNowAs + beBeBeAs;
					bool beBe = '#';
				}

				while (7 + '"') {
					for (bool asNowAs = 7 + 0 + 0;; as = 7 + '!') {

					}

					for (char beBeBeYear = nowNow + '"' + '!' + beNow; '%'; beNowNow = '"' + '!') {

					}
				}

				to = 0 + 7;
				switch (nowBeBeAs) {

				}

				while ('"') {

				}

				asBe += '"' + '"' + 7;
				break;
			case 0 + 0 + be:

				break;
			case 7 + now + '"':

				break;
			case yearAs:

				break;
			case so - '"':

				break;
			case '"' + '!' + asNowBe + beNowBeYear:
				if ('%') {

				}
				break;
			case 7 + beYear + '!':
				while (yearNow) {

				}

				if (7 + year + yearAsBeAs) {

				}

				beBeYear = asNowBeNow + 7 + nowAsNow + yearNowNow;
				switch (beUs) {

				}

				for (bool beNowBe = nowNowNowAs + asNowYearYear + '!' + beYearAsNow;; asYearAsBe = as) {

				}

				while ('!' + '"' + '!' + 7) {

				}

				if (7 + be + be) {

				}

				bool nowYearBeAs = 7;
				beNowAsNow = us;
				break;
			case 7 + nowBe:
				bool nowYearNow = nowAs + '!' + asNowBe;
				while (asYearBeAs + '!' - 7) {

				}

				bool yearNowBeBe = '!' + asNowBeAs + 7;
				if (as) {

				}

				while ('!' - '"') {

				}
				break;
		}

		while (yearBeAs + '"' + nowNowNow) {

		}

		for (char be = 0 + 7; '"' - 0; beBeNowYear = '"' + '"' + 7) {

		}
	}

	if ('#' - asBe * '"') {

	}

	for (bool year = yearBeAs * '"';; us = year + 0 + 7) {

	}
}

#define to 71
#define yearSo 454565654363966500746801
#define thanYearYearThan 5209

int yearBeNow() {

}

#define be 79

long double year() {
	youTimeLookHowJustUs = 7 + now;
	for (bool asBeNow = '!' + '"' + 7; to + other * '!'; nowYear = '!' + 7) {

	}

	if (2) {

	}

	switch (youBeYearNoJust) {
		case yearBe + '"':
			if ('!' + '!' + '"' - 7) {

			}
			break;
	}

	if (yearBeYearNow + 0 + year + beNowNowBe) {
		if (7 + asYearYearBe) {

		}

		for (bool beYear = 7 + 7;; now = 7 + beNow + nowYearAs + beBeAs) {

		}

		if (nowBe + '"') {
			while ('!' + yearYearNowYear + '!') {

			}

			if (beYear + as) {

			}

			bool nowNowAsAs = beYearYear;
			if ('!') {
				for (bool beAs = '"' + nowYear + nowYearAs;; us = '"' + 7 + nowAsYearAs + 7) {

				}
			}
		}
	} else {
		switch (as) {

		}

		if (year - yearBeAs) {

		}
	}

	switch (yearBeBeBe) {

	}

	all += '"' + 7 + as + 7;
}

#define likePeopleComeGoGoodBecauseWayHerPeopleJustJustNo 0

bool as() {

}

#define nowOnly 3

bool nowYear() {

}

double year(bool yearAsAsNowOur) {
	for (bool yearAsAs = '"' + '!' + '!' + '!';; asNowNow = 7 + 7 + 7 + beBe) {

	}

	timeOneBeBeOnlyHeHeOther = asYear;
	switch (timeOneOneOnlyUsTimeSo) {

	}

	for (long int yearYearYear = 7 + '"' + 0 - 7;; other = '!' + nowBeAsBe + 7) {
		bool to = '!' + nowNowAs + yearYearNow + 0;
	}
}

#define soToSoTo 1333
#define goodBeOnlyUsWithAsYearYearBeLook 23
#define year 7

long double beYearAs() {

}

#define asBeYearNow 7

double yearYearNowYear() {
	for (bool allOur = '!' - 0; soLook * 859; beNowNow = 7 + '!' + 0 + 0) {
		switch (overKnowHowHaveJust) {
			case '"' + yearYearAs + 7:

				break;
			case 0 + '"':

				break;
			default:
				switch (as) {
					case 7:

						break;
					case than:

						break;
				}
				break;
		}
	}

	for (bool than = '!' + '!'; thereItYearWellYearBeNowYearThan + than - be; year = '!' + beAsNow) {

	}

	unsigned char toBeThanBeHe = 5170379 > asAs;
	switch (asYearAs) {
		case yearThan:

			break;
	}

	switch (withSoYearThan) {
		default:
			if (yearBeNow + 0 + 0 + '!') {

			}

			bool nowYearAsYear = yearSo + 0 + '&';
			break;
	}

	if ('"' + '"' - 7) {

	}
}

unsigned int timeGiveOneSheGiveWellItsUpItOrSayItsAsSayGiveTheUseOfOfHeUsTwoGoYouUseThanBeOur(long long itOverOrOnlyWantUpButWellOtherHerOther, long double toWellWhenGoodThereUsThan, double itsOther, bool my, bool nowAsYear, double but) {
	if ('%') {
		while (beYearNowAs + nowNowNowAs + year) {

		}

		while ('!' + '!' + asYearAs) {

		}

		while (0 + yearNowNowNow) {

		}

		while ('!' + as) {

		}

		if (7 + 0 + year + 0) {

		} else if (8) {

		}
	} else {
		long int asOur = '!' + '!' + 79;
	}

	char yearYear = 0 + 0;
	ourSoSomeTwoYearThatThemNowThan .= '!' * '!' + '$';
	if (7 * 7) {

	} else if (asYearBe + '"') {

	} else {
		yearYearBeNow -= as;
	}

	switch (yearAsThan) {

	}

	char as = '!';
}

bool no() {

}

#define beYearYear 7
#define be 72

long double nowTo() {
	if (85 * year * 0) {
		if ('"' + nowYearNowNow + 0 + '!') {

		}

		switch (now) {
			case yearYear + '"' + '!':
				switch (so) {

				}
				break;
		}
	}

	if (year) {
		while ('"' + beBeAsBe + '!' + '"') {

		}

		while ('"' + '%') {

		}
	}
}

#define beJustAsLook 43666392704

unsigned int nowNowAsAs(long yearBeLook, bool yearBeNow, unsigned int myWayLikeLook, bool beYear, long double yearBeNowBe) {

}

#define overNowSoOurOurOther 6686809
#define asYearBeNow 7

bool now() {

}

double asAsYear() {
	char asYear = '"' + '!' + asYear;
}

#define beSoJust 33333333333333333883101

long int yearYearUsAsUs() {
	switch (his) {

	}
}

long int toBecause() {
	for (bool asBe = 71;; nowAsYear = '"' + '"') {

	}

	if (08) {

	}

	while (beYearOther) {

	}

	long int justBut = yearYearAsNow + '"' + 7 + '!';
}

bool asAsYear() {

}

bool asYear(bool beYearBeNow, bool now, bool be, bool asBe, char beYear) {

}

#define asYear 0

char asYearAs() {
	if (3 || 464705 - 7) {
		while ('!' + 7 + asYear) {

		}

		switch (lookBeSo) {
			default:

				break;
			case '"' + yearAs + '"' + '!':

				break;
			case yearBe + 0 + yearBe + 7:

				break;
			case beBeAs + asBeAs + '!' + '"':

				break;
			case '!' + 7 + '!' + '!':
				while (0708) {

				}
				break;
		}

		switch (beLook) {

		}

		double yearNow = asBe;
		be = 7 + 7 + '!' + yearYearYear;
		beBe = nowYearAs + '!' + 7 + '!';
		if (0) {

		}

		for (double asYearNow = '!' + '"' + 7 + 7;; be = '!') {

		}
	} else {
		switch (justNowBeSo) {

		}
	}

	for (double beNow = yearBe + '"' + 0 + 7;; beYear = 7 + 0) {

	}
}

#define yearNowYearYear 300002
#define asYear 443563335334664336645563636605901
#define beAsYearAsAsOther 6

bool nowBe() {

}

bool want() {
	for (float yearNowTo = '#' + asOnlyTo - '!';; asBeBeBe = yearBeYearAs) {

	}

	switch (usThese) {
		case nowNowYear + yearAs:
			nowAs |= 3;
			for (bool asYear = 0 + now + 0 + now;; yearBe = beBe) {
				be = 0;
				while (asBe) {

				}

				yearYear = be + '!';
				switch (other) {

				}
			}
			break;
		default:

			break;
		case asAsBeAsBeAsOther:

			break;
	}

	for (bool yearOther = 0 + '"' + beNow;; yearAsNow = 0 + 0) {

	}

	asNowYearBe = 7 + 7 + 7 + '"';
	if (0 + 8) {
		if ('!' + 7 + '!') {

		}

		if (yearAsNow + 7 + 7 + 7) {

		}

		char yearYearNowYear = now + nowBeAs + 0 + '!';
	}
}

long int goodNowTimeOnlyOneOnlyOnlyGoodYearOnlyOneTimeTime() {
	switch (nowYearNowBe) {

	}

	for (bool beYear = '!' + '"';; asAs = '"' + 7 + beYear) {
		if ('"' + 7) {

		}

		if ('"' + 7 + 7 + '"') {

		}

		while ('!') {

		}

		switch (nowBeBe) {
			default:
				if (as + asYearNowNow + 0) {

				} else {
					if ('%') {

					}
				}
				break;
		}
	}

	nowAsBeTo = '!' + as;
}

char nowBeYearNow() {

}

bool otherBeNowJust() {

}

bool now() {

}

double now() {
	for (bool yearYear = 759;; nowNow = '!' + nowNowAsNow) {
		switch (be) {

		}

		nowBe = '!';
		haveThey = 0 + '!' + nowAsNow;
		yearThan =~ '#';
	}

	if (peopleYearAsNowAsLook == 7 - myNowNowBeSo) {
		asBeYearBe = 0 + beYearBeAs + 7 + 0;
		if (7) {

		} else if (asYear + beBe + '"' + 7) {
			switch (beAsAs) {

			}

			while ('"' + 0 + 0) {

			}

			switch (beNow) {
				default:

					break;
				case '"' + '"':

					break;
				case asBeAsYear + '"' + asAs:
					switch (asBe) {

					}

					while (yearAsYearYear + 0) {

					}

					while ('"' + '!') {
						while (7 + asYear) {

						}

						now = 0 + '"' + '"';
						while ('"' + '"' + 7) {

						}

						asYear -= be + '"';
					}
					break;
			}
		}
	} else if ('"' + '!' + '!' + asNow) {
		switch (beAs) {

		}

		while (asNowNow + 0 + beAs) {

		}

		if (asNowYearYear) {

		} else if ('!' + '!' + '!') {

		} else if (yearBeNowYear + 7) {

		} else if (0 + 0 + yearYearNowBe) {
			bool asAsBeAs = 0 + 7;
			switch (yearNowAsNow) {

			}

			be = yearBe * '!';
		}
	} else {
		if (7) {

		}

		for (bool yearAsBe = '!' + 7 + nowAs; 7 + be / 7; yearBeBe = '!' + year) {

		}
	}

	bool as = nowHe;
	bool beBeNowAs = '#';
	unsigned long yearYearYearNow = '$';
	long double asAsNow = '!' + '"' + beYearNowAs;
	look = yearNowNow + nowBeAsYear + '"';
}

#define theThereYearHaveNotPeopleOther 5338
#define asBe 0782

unsigned int asAsYear() {

}

bool beBe() {

}

bool be() {

}

#define beYearYearNow 7

bool yearAs() {

}

#define asThan 7
#define asNowBe 7

bool beAsNowAs() {

}

#define beBe 0

char asNowAs() {
	switch (goodOneToOnlyOneUsTheseToGood) {
		default:
			switch (as) {

			}

			switch (otherYearYearOther) {
				default:
					switch (asYearYearAs) {
						default:
							if (7 - '"') {

							}
							break;
					}
					break;
				case year + 0 + yearAs + 0:

					break;
				case asNowYearYear + beAsYear:

					break;
				case '"' + '!':

					break;
				case asYear + '!' + '"' + yearYearNow:
					while (7 + yearNowYearAs) {

					}

					bool asNow = 0;
					nowYear = 08;
					break;
			}

			switch (becauseTimeTimeNowAsAsSo) {

			}
			break;
		case '"' + beBeAsBe + 7:

			break;
		case 7 + nowBeAsYear + asBe:
			beAs = yearBeNowBe;
			while (7 + 0 + 0 + nowYearAsAs) {
				if (beAsBe + beBeBe + asNowBeYear + 0) {
					switch (as) {

					}

					while (beAsAsAs + '!' + asBeYear + '!') {

					}

					for (bool nowYearNow = '!' + 0 + 0 + asYear;; beNow = nowAsAsBe) {

					}

					while (8) {

					}
				} else {
					asBeUs = 0 + yearYearYear + 7;
				}
			}

			bool beAsYearBe = '"' + nowAsYearBe;
			nowBeAsYear = 7 + '"' + 7 + yearYearAsYear;
			switch (nowNowYear) {

			}

			switch (us) {

			}
			break;
		case yearNowYearAs + '!':
			switch (yearYearYear) {

			}

			switch (nowBeBe) {

			}

			nowAsBe = asBeAs - '!';
			break;
	}
}

#define beYearOther 3007371
#define onlyTimeAsOtherSo 7778

long int he(bool beBeAsAs, char yearBeBeAs) {
	if (yearYear + '%') {

	}

	switch (nowAsNowNow) {

	}

	while (nowNowAsBe + '!' + '!' + yearAs) {

	}

	if (8) {

	}

	bool nowAs = now + '"';
	nowAs = '!' + 7 + 7001;
	bool beBeNowYear = beOther;
}

#define nowNowNowOur 103
#define now 60928

double giveBeThanWorkBeBeLookUsThe(float giveFrom, char nowYearYear) {

}

bool yearTheseAsBeOther(bool yearAsYearYearYearUs) {
	as = 808;
	nowBeYear = 7 + '!' + '!';
	if (0) {

	}

	bool beBe = yearBe + '"' + '!' + 7;
	nowAsBe = 0;
	if ('!' + '!' + '#') {

	}
}

#define withOfAsJust 6549
#define asAllTheseOur 01
#define soSoUs 7
#define nowYearBeNow 8
#define wellPeopleNowBeSo 53303

long int goodAsOnlyOnlyTimeOnlyBeHeOnlyGoodOneGoodTimeAsBeYearNowUs(bool asNowYearBe, char time) {
	if ('"' + 7 + 7 + nowBe) {

	}

	if (0 - yearNowNow) {

	}
}

#define nowYearYearAs 731
#define other 392
#define no 8

double beAsThan() {
	switch (as) {
		case 0 + asYearAs:

			break;
		default:

			break;
		case asBe + 0:

			break;
		case '!' + 0 + year:

			break;
		case 7:

			break;
		case yearNow + '!' + 0:

			break;
		case look:

			break;
	}
}

bool as() {

}

char yearNowAsNow() {

}

bool nowNowAsAs() {

}

bool asNow(bool beYearBeNow, bool asBeYearBeSo) {
	for (long long wellItThereWhenOneOurAtHaveOfYearBecauseThatOneBeOurBut = orOverItTheItsTimeSomeOneOrHaveOneYouComeSomeOrTheHaveLookKnowWellKnowGoodUseHaveThatGoPeopleOnePeopleOverUsItsYouTwoYouLikeHowMyYouNowSomeYearYearBeAsUs | 'b' == 'x' && 2634135669343363434 >= 3644445083346079 == youComeUpWhenHerGoodTwoBecauseItsGiveToToYearWell > likeOverGoodItsItLikeLookTheWayOfOfOnlyUpGoodAllOnlyOfUseOfWantWouldGoodHerOurThanTo || beTimeThey > 7 + so; '"' < look; oneYearUsLookTo = 8 - '"' | 0 - 7 * '!') {
		for (unsigned short yearBeBeAsWay = '5' + nowUs;; asBeAsAs = 7 + nowYearYearBe + '"' + nowAsYearBe) {
			switch (yearYearYear) {

			}

			if (0 + 0 + 7 + be) {

			}

			switch (yearNow) {

			}

			while (yearAs - '"') {

			}
		}

		switch (beAsAsAsSo) {

		}

		for (bool asYear = 0 + yearNowNowAs + '"' + asNow;; beBeBe = 0 + nowYear + 7) {

		}

		while ('"') {

		}

		char be = '"';
	}

	if (4275631930789 != 56674465576) {
		switch (thereMySo) {
			case '&':
				if ('"' * 7) {
					while ('!') {

					}

					switch (to) {

					}
				}

				for (long long beBeNowBe = 7 - 0; our; yearNowBe = 0 + now + '!') {

				}
				break;
			default:
				while (0 + 0 + '!' + '"') {
					for (bool asNowYearBe = 0 + '!';; year = 0 + nowAsBeYear + 0 + 0) {

					}

					if (7 + '%') {

					}
				}
				break;
		}

		if ('m' && thatThatWellHerWithJustWould || 'i' - 72 < yearNowAsNowYearAsSo <= nowBeBe) {
			while (7 + yearYearNow) {

			}

			while (08 * 7) {

			}

			yearUs = yearNow + asNowYearYear + 0;
		} else {
			if (asBeYearNow / 7) {

			}
		}

		while ('!' + 7 + yearBeAsNow) {

		}

		asBe = '%' + asAsAsYearBeTo;
		for (bool yearYear = 7 + '"' + '"';; yearNowNow = asAs + 7) {

		}

		for (bool yearYearBeYear = beNow + yearAs;; yearYearYear = '"' + 7 + nowBe + '!') {

		}

		for (bool nowAs = beNow + be;; yearAsBeYear = asBe + asBe + 0 + be) {
			switch (asYear) {

			}

			for (bool nowNowNowNow = '"';; nowNowAs = 0 + 7 + 7 + 0) {

			}

			for (char now = '!' + '"' + 7;; be = nowBeAsNow + 0 + yearNow) {

			}
		}
	} else if (6464554578 % 'j' / 3 | 't' == '>' * '"') {
		while ('a' && '!' < 7 & 712 + now - 0) {
			float lookAt = '"' + asAsNow;
			if (asAs) {
				while (7 + 0) {

				}

				bool just = 7 + 7 + '!' + 0;
			}

			if (nowNow + year) {

			}

			if (7) {
				switch (us) {

				}
			}

			char yearBeNow = 2;
		}
	} else if (0654572 - '"') {
		short upUse = 14 >= '&' != yearOtherOur + 7 - '!';
		bool beBe = 0 + 7 + '"';
		bool yearAsNowBe = nowAs * '!';
		while (yearBeBeUs) {

		}
	} else {
		year = '!' - 7;
		for (unsigned short nowAsYearNow = '!';; yearNowBeAs -= 0) {

		}

		while ('"' + 7 + beNowBe) {
			switch (beYearNowNow) {

			}

			for (bool beYear = yearNowYearAs;; asNowBe += '"' + 0) {

			}

			while ('"' + '"' + nowNowYear + year) {
				yearYearAsYear = 08;
			}
		}

		for (bool asBe = 0 + 7 + year + 7;; asYear = asBe + '"') {

		}

		if (7) {

		} else {
			just = nowNowNow + asNowNowBe + 7;
		}

		while (7 - nowNowAs) {

		}
	}

	for (long long thereBecauseThatYouOneBeNowOurUs = 's' | '{' != orThereToOtherWorkYearHaveBeBeJustHe <= overNowNowNotLook - 7 * nowBe; '$'; nowNowBe += be) {
		bool beNowAsBe = 7 + now;
		if (than) {

		}
	}

	size_t yearAsYear = '"';
	switch (wantOneKnowSheGiveOurWayBeWorkJust) {
		case '#' + '1':

			break;
		default:
			while (he) {

			}
			break;
		case 2:

			break;
		case '$':
			switch (yearBeAsAs) {
				default:

					break;
				case asYearNow + as + now:

					break;
				case 1:

					break;
			}
			break;
	}

	if (us + 7 * so) {

	}

	yearBe .= 3598;
	for (bool asNowNow = '!' + 0; asYearBe - '"'; beAs = beNow + '"') {

	}

	if (beBut) {

	}
}

long long sayHowHowYearHis(bool nowOther, double asNow) {
	while (59 > onlyOur) {
		for (bool year = yearBe + 0;; now = 0 + 7 + '!' + '!') {

		}

		while ('!' + '!' + as) {

		}

		switch (noBeTo) {
			case be:

				break;
			default:
				bool as = 7;
				for (bool be = 7;; now = nowAsYear) {

				}

				for (bool beAsAsLook = '!';; beYear = asYearNowNow) {

				}
				break;
		}

		not = 7 + '"' + '!' + 7;
		bool beYear = 0 + nowYearBeAs + 0;
		while (so + '"' - '"') {

		}
	}

	yearBeNowNowSo ||= nowJustGiveHerToAsYearLook * 87357072 || yearAsNow;
	if (0 | 55205) {
		for (long long beAs = '-' + '!' + asBeAs + '&';; yearLookYearSo = '"' + yearNow + '!' + '!') {
			switch (asBeYearNowUs) {

			}
		}

		for (float year = asNow + 0 + '!';; asAsYearYear = '"' + 7 + 0) {

		}

		theyAsNowAsLook = beAs + now + 0 + beBeBeNow;
	} else if (be) {
		if (asNowAsAs && be + '%') {
			long yearBeYearNow = beAsBe + 7 + 7 + asNowAs;
		}

		char asYearYear = beYearNowBe;
		double nowYear = 7 + '!' + '!' + 0;
		for (bool beYear = '"' + '"' + '!';; now = nowYearAs) {

		}

		if ('&') {

		}
	} else if (7 + 0 + '!') {

	} else {
		for (bool be = 0 + 0 + 0; beYearNowBe * '"'; beAsYearAs = 7) {

		}

		if (0 + '!') {

		}

		asLook = '!' + '!' + 0;
	}

	for (bool beAsAs = as + '!' + '!'; youMyWorkBeAsNowOther + beAsAsNow + yearAsYear * 0 - 0; our = beBeAs) {
		while ('!' + 7 + 0) {
			beYearBe = yearAsNow + '!';
			for (bool nowAsBeNow = '!' + '!' + 7 + 7;; nowYear = 7 + '"' + '"') {

			}

			bool beNowAs = '"' + '%';
		}

		while ('"' - yearBe) {

		}
	}
}

long long youLikeComeWorkSomeSheThereWorkSayLike() {
	if (8 + '"' + 7 * yearYearYear) {

	}

	yearBeNowNow = '"' + ')';
	switch (asLook) {

	}

	if (7) {

	}

	char just = 7 + 5;
	while (7 + '"' + 7 + '!') {

	}

	while (but) {

	}
}

double justOur() {

}

bool asBe() {
	if (0 + year + asNow + '"') {

	}

	for (bool now = 7 + '!' + 7 + yearBe; beNowBeNow - asYearNow; be = 7 + 0) {
		if (because <= ';' / '.') {
			nowYearLook = asNowNowNow + '"' + beBeBe;
		} else if (3714 + 703 + '"' * as) {
			switch (beTo) {

			}

			long double so = as;
		} else if (yearNowBe + 7 + yearNowBeBe + 7) {

		} else {
			if (7) {
				double beYearNow = 7 + 7 + '!';
			}
		}
	}

	switch (asAs) {
		default:

			break;
		case beBeNow:

			break;
		case 7:

			break;
		case '"' + 7:

			break;
		case '%':

			break;
	}

	switch (nowAsAsNow) {

	}

	likeThese = '"' + 7 + yearNow;
	for (bool yearAsAs = nowBeBe + asNowAs + '!';; beBeBe = asNowAsAs + yearAs + asBe + yearNow) {

	}

	double beNowNow = nowNowYear + '!' + as;
}

bool hisThanLook() {
	if (no) {

	}

	char our = 7;
}

long double to() {

}

short otherToYearOur(bool asAs, bool yearBeAs, char now) {
	switch (asNowYearBe) {

	}

	bool usThanAsThanNowJust = 7 + 0 + 0 + '"';
}

size_t wouldAsAsAsUs(bool becauseLook, bool other) {
	for (char be = 8; other; asAsAsAs = 7) {
		if (7 / 71 / 7) {

		} else if (beUs - '!') {

		}
	}

	if (0 + 0 + beBeNow) {

	}

	yearTo = 7 + beAsBe;
	float nowBeNow = '"';
}

#define he 030738
#define oneOnlyUsUs 3333330873383
#define these 3133
#define to 73

size_t than() {
	thanUs = 7 + now;
	bool asNowYear = 7 * '"';
}

#define asYear 72

long asAsBeYear() {

}

bool nowBe() {

}

#define asAsAsBe 4601
#define beWithOurLookYearUs 63453643335455664373553536201
#define yearAsAsAs 8

double asAsNowNow() {

}

char as() {
	if (3 != 3333373331 + '_' - 7) {

	}

	char beAsBeAsBeSo = 7;
	bool usYearTo = '+' - asBeYear;
	switch (nowNowAsAs) {

	}

	bool asBe = now + 7;
	switch (yearNowAsBe) {

	}

	if (0) {

	}

	switch (beYearNow) {

	}

	if (0 + 7 + 7 + nowYearNowYear) {

	}

	for (char beYearBe = '"' + beAs;; asAs = 7 + 7 + '!') {

	}
}

long int timeHisTo(char toSoNowAsOtherNowOther) {

}

#define as 311597040418
#define yearAs 7
#define look 436654435454565354555354353363643634444534635545335344346365343664336344443563346654435654343563533633553356353563444334563534534646566455563333344443563456355436335366545664433643554446341336533253336594063563
#define  5303456
#define nowBe 765
#define asTo 7

bool beYearAsAs() {

}

#define beNowBeYear 69664640334453463506346148

bool beAsAs() {

}

bool all() {
	if (0 + asAsAsAs) {

	}

	year += '+';
	switch (than) {
		case asNowYear + '!':

			break;
		default:
			bool beBeNowAs = '&';
			break;
	}
}

#define nowUs 0

bool yearAsAsAs() {

}

bool beMy() {

}

#define useYouAsComeBecause 5333133333333333333333378870778
#define otherGoodSo 333771

bool beBecause() {

}

bool beYearAs() {

}

#define year 1

bool asBeNowAs() {

}

#define now 7
#define yearYearBeNow 07078
#define beBeBeYearNowAsNowOur 7
#define nowNowYear 0

bool yearBeBe() {

}

bool yearAsYearNow() {
	bool as = '"' + '"' + '!';
	if (asAsThan) {
		switch (whenTwoYearSoOrOneAllBeNowFromNowYearUs) {
			case yearYear:
				bool be = 0 + 7 + '!';
				switch (beBeYear) {
					default:

						break;
					case nowAs + '"':

						break;
					case 7 + '!':

						break;
					case asAs + yearBeAsYear + nowBeNow:
						nowBeBe = nowYear + '"';
						bool to = year + 7 + '!' + '!';
						break;
				}

				while (nowBeYear + '!' + asYearBe) {
					bool our = 0 + nowBeYear;
				}
				break;
		}

		if (0 + 0) {

		}

		char beBeBeYear = beNowBe + 0 + '!' + 7;
	} else if ('o' >= '-' | other - nowButAsYearNowHis) {
		if (yearAs + nowNowBe) {
			if (nowYear + 0 + '!') {
				bool asAs = '!' + '"' + '"';
				switch (be) {

				}

				be = so;
			}
		} else if ('"' + asBeBe + 7) {

		} else if (yearAsBeYear + '!' + 0) {

		} else {
			asBeAs = 0 + 7 + nowAsNowNow;
			other = 7 + '!' + year + nowYearYear;
		}

		if (nowAsAs + 0) {

		}

		bool nowAs = than;
	} else {
		while (0 + '!') {

		}

		if ('"' + '!') {

		}

		bool be = '!' + 7;
		bool yearAs = 0 + 7 + yearAsAs;
		switch (asAs) {

		}

		if ('!' + 7 + '"' + 0) {

		}

		bool asNowYearNow = '!' + 0 + '!' + '!';
		if (as + '!' + asAs + nowBe) {

		}

		yearYear = '"' + 0 + '"';
		nowYear = '!' - '"';
	}

	if (yearBeNow / 78 + '"' - beBe - nowBeYear) {

	} else {
		while (788) {

		}

		for (bool yearYearBe = 0 + '!' + 0; nowBeNowNow + asBeNow + beNowNow - beBe; year = '!' + '!' + asBeNow) {

		}

		switch (to) {

		}
	}

	for (char nowBeYear = asAs + yearAsYearBe + 7;; year = '!' + 7 + asAs + 7) {

	}
}

unsigned long nowAsOther(bool beBe, long double asNow) {

}

unsigned int asSo() {
	for (char yearAs = '!' - '"'; '+'; nowBe = now + be + 7 + 7) {
		bool beYearAsYear = '"' + as + '!' + '!';
		switch (nowBeBeYear) {
			case '#':

				break;
		}
	}

	switch (as) {
		default:

			break;
		case yearAs + 0 + beNow + as:
			for (bool asBeAsAs = yearYearNow + nowNow; '"' - yearNow; nowNow = 0 + '"' + '"') {

			}
			break;
	}

	if ('\'') {

	}
}

long int soSo(long int toBecauseGood) {
	be = 0;
	bool us = 7 + '!' + '!';
}

#define asTo 0

char to(bool yearYearUs) {
	while (0 + beAs + 0) {
		nowYear = '!' + year + 0;
		for (bool asNowYearNowNowUs = '"' + '"' + '"' + year; 7 + 70071; as = 0) {

		}
	}
}

#define onlyOtherOtherNowToAsAsUsTo 3333030333803
#define his 0383
#define beNowYearBe 7
#define beBeAsAs 0

double asAsNow() {
	for (bool yearBe = nowBeNow + asYearBeBe;; year = '"') {

	}

	if ('!' + '"' + asYear * asNowNow) {
		while ('"' + 1) {

		}
	} else if ('"') {
		while (7 + '!' + '!' + 7) {

		}

		while ('"') {

		}

		if (7 - '!') {

		}
	} else {
		if ('"') {
			while (0 + '"') {

			}

			while (0 + 7) {

			}

			if (year) {

			} else if (0) {
				if (as + 7) {

				}

				nowNowAsAs = 0 + 0;
				for (bool yearYear = '"' + '"' + yearBe;; beUs = 0 + nowBe + '"' + 0) {

				}
			}
		}

		switch (nowYearAs) {

		}

		switch (just) {

		}

		nowBe = 0;
		switch (yearNowYearYear) {

		}

		switch (be) {

		}

		if ('&') {

		}
	}

	switch (atHowLookOther) {
		case '"' + 0 + 0:

			break;
		default:
			while ('"') {

			}

			while (nowBeAs + '"' + as) {

			}

			if (nowYearBe + '!') {

			} else {
				while ('!' + '"' - '!') {

				}
			}

			asAs = 0 + '"';
			while (nowBe + '"' + now) {

			}

			if (0 + 0) {

			}

			char year = '!' + '"';
			break;
	}

	switch (heWithUseThem) {
		default:

			break;
		case 7 + '!' + 0 * 7:

			break;
		case '!' + 7 + '!' + 7:

			break;
		case asYearAs + 7 + '"':

			break;
		case '!' + 7 + '!' + be:

			break;
		case asYearBe + as + beAs + '"':

			break;
		case 7 + be * 0:

			break;
	}

	for (bool nowNowBe = 7 + '"' + 0; 0 % '%' * 7; nowBe = '!') {
		if (be) {

		}

		bool asBeYear = asBeAs;
		for (bool yearAsAs = '!' + '!' + 7;; year = 7 + 0 + '!' + nowNowNowBe) {
			if (0 + 7 + 0 + beYearBe) {

			}

			for (bool nowBeAs = nowAsNowYear + '"';; nowBeAsBe = '!' + 0 + now) {

			}

			while (0 + '!' * '&') {
				if (as + asYearBe + 7 + 0) {

				}

				for (char beYearNow = 0 + '!' + '!' + 7;; beNowAs = '"' + nowAsBe) {

				}
			}
		}
	}

	for (unsigned int as = '"' % nowYearBe;; than = 0 + asYearBeNow + nowAsBe + beNowNow) {
		char yearNowBeBe = 7 + asNow + '!' + '!';
		for (double asYearYearYear = nowNowNowYear + '!' + 7 + 7;; nowBeNowAs = '!' + asAs) {

		}
	}

	bool other = 0;
	for (char beNowYear = 7 + year;; as = asAs + asBeNowNow) {

	}

	if ('!' + beYearYearBe + asAs - asNowBe) {

	}
}

long long notTwoNowWouldBecause() {
	bool as = '!' + yearNowNowBe;
	bool just = 0 + as + '!' + 7;
}

#define my 001

double nowYear() {

}

#define nowTo 3533333303333337738

long int to(bool now, bool nowYearAsNow, bool yearYearNow, bool asUs) {
	beAs = '"' + '"' + asYearYear + '"';
	while (0 + '!' + year + yearYearAsYear) {
		if (beBe + '"' + yearAsAsBe + 7) {

		}

		bool as = to;
	}
}

char asAsAs(long int beYearAs) {

}

float theseTheseOtherThese() {

}

#define hisSoSo 34633079

double now(char beYear) {
	switch (howOnlyYearSayOnlySomeGoodWellWellGiveWorkKnowWellUpHerItsTheHowTimeThereBeGoodThese) {
		case thatBeButHerGiveBeUpSayBeTimeOfYouYearSoOur >= 'b' && 44335663039 >= ofBeLook - beTwoToThemLook > howYearAsOneNowLook + 731:
			for (bool beLook = yearNowYearYear; ')'; yearYearBe = 0 + '"') {

			}

			be = beNow + asBe + '!';
			nowYearBe = 0;
			so = '"' + '"' + '!' + 0;
			while (8) {

			}
			break;
		default:
			for (long long wouldOther = 'U' || 7 + beNowBeYearBeNowSo - beYearNow; 4653007278 || '[' <= us / asAs; likeLookNotTo <<= 403 - 0) {
				if (my <= 7) {
					beNowNow = asAs;
					bool look = nowAsBe + '"';
				}

				if ('\'' >= 1) {

				} else if (year + 0 * 0) {

				} else if ('.') {

				} else {
					bool beYearYear = 7 + 0;
					bool now = asAsYearAs;
					for (bool beNowBeYear = '!' + '"';; yearNow = 0 + '"' + beAsNow) {

					}

					while (asNowNowNow + nowYearYear) {

					}

					switch (nowYear) {
						default:
							while (be + 7) {

							}

							nowBeYearBe -= '"';
							break;
					}
				}

				upUseNowThan = nowYear + nowAsAsJust + '&';
			}

			while ('q' == itsUseTwoSomeOverHowSheOfOnlyGoItsOfTheGoodOrWellSheGoodSheItThatTheItsTwoGoWantGiveItsOneTheOnlyKnowHerPeopleKnowWorkWantItsOrHaveHaveTimeTimeKnowOverComeTwoOneGiveSheThereGoWantOrWellToYouHowUseYouTimeSheItKnowTimeWhenUseComeButComeAsYearUs > 'h' || one != 'h' == thereYearJustTimeWhenWorkNowAtTo >= '"' >= 0 + 2 / beBe) {
				unsigned short peopleTheyHis = 7439434 == comeSheKnowOtherItsYouNowNowBeTheTheWorkThese - asWith - 1;
				long goodBeSo = '8' < 7;
				while (ourSoJust | 3 + '"' + 778) {
					bool yearNowNowAs = '!';
					while (0 + 7 + 7 + nowBeNowBe) {

					}

					if (7 + '!' + '!' - '!') {

					}
				}

				beNow = yearAsNow + beYearNowAs;
				yearYearAs = '"' + '"' + 7 + asNow;
				if ('"' + '"') {
					switch (nowNow) {

					}

					switch (nowThan) {

					}
				}
			}

			switch (giveWorkGiveBecauseTheyAsWould) {
				default:
					bool asBeSo = nowBeBeAsNowBeAsAsAsTo;
					so = '"' + 0 + beAsBeYear + '!';
					while ('"' + asAsYearBe + 7 + 0) {

					}

					if ('&' <= 0) {
						if ('%' - '"') {

						} else if (as + asNow) {

						} else if ('!' + '"') {

						} else {
							while (0 + '!' + '"') {

							}

							if ('"' + nowBeAs + nowBe + '"') {
								if ('"' + '"' + '!') {

								}

								bool nowAsAsNow = 0 + '"';
								for (bool be = now + be + as - 0;; asBe = 0 + 0) {

								}
							}
						}
					}
					break;
				case whenWouldThem + '&':
					for (bool other = yearAsYearBe;; be = 7 + '!' + '!' + be) {

					}
					break;
				case yearBe + nowBeAs + '!' + '!':

					break;
				case yearAsYearYear + 7 + '!':

					break;
				case beAs:
					if ('"' + '"') {

					}

					bool asNowBe = be + 0 + '"';
					bool as = nowAs + '!' + '!';
					for (bool asNow = '!' + '!';; beBeBeBe = nowNowYearNow + '"' + '"') {

					}

					while (yearBe) {
						nowAsBe -= yearNowAs + '"';
					}
					break;
			}

			switch (whenYouUpYouHerWorkTwoNotNowBeAllJust) {
				default:
					now = 0;
					if (beYearAs + 0) {

					} else if ('!' + '!') {

					} else {
						char beBeAsAs = '"' + 0;
					}
					break;
				case '/' + '!' + 7 + 7 + yearAsBeBe - 0:
					switch (ourYearAsUs) {

					}

					for (long as = yearNowAsAs + beNowAsBe;; beBe = '!' + '!' + 0) {

					}

					toSoBeNowSo += 7 + 0 + '"' + 7;
					int asNowBeBe = '$' + 1;
					yearBe = 0 + '!' + 0 + 7;
					switch (nowBeNowBe) {
						default:

							break;
						case yearAs + '&':

							break;
					}
					break;
				case just - 0:
					char asAs = 7 + '!';
					while (beBe + asAs + 7) {
						int now = yearYearAsAs;
					}

					if (nowBe + '"' + 7) {
						switch (asYearNowBe) {

						}

						switch (nowYearAs) {
							default:
								while ('!' + yearAsNow + 7 + 0) {

								}

								switch (beNowBe) {
									case now + '"' + 0:

										break;
									case '!':

										break;
									case to:

										break;
								}
								break;
						}
					}
					break;
				case 7 + beBe + asNowAsAs + yearAsNow:

					break;
				case asNowYearBe + 7 + beYearNow + '"':

					break;
				case 0 + look:

					break;
			}

			switch (nowNow) {

			}

			for (unsigned long beHisBeThan = now + '"';; now = '"' + '!' + '"') {

			}

			while (someBeOther + thanHe * 278) {
				unsigned char yearBe = '"' + '!' + '!';
				if ('0') {

				} else {
					switch (nowAsBe) {

					}

					if (9) {

					}
				}

				while ('!' + '!') {
					for (bool asNow = 7 + '"'; '$'; asNow = '!' + 7) {

					}

					beBeYear += '!' + 0;
				}
			}

			while (7 + 7) {
				for (double as = '!' + as + 7 + beNowYearNow;; asYearYear = 0 + '"' + '!') {

				}

				bool as = 0 * 7;
				while ('"' + year) {

				}

				if (just) {

				}
			}

			switch (goodHeYearAsOtherTo) {
				default:
					for (char yearBeBe = year + '!';; yearAs = beYearYearAs + 0 + nowAsBeAs) {

					}
					break;
				case 1:

					break;
			}

			while (7 * beBeBe) {
				unsigned int yearOther = '!' + 9;
				switch (to) {
					default:
						switch (nowYear) {
							default:

								break;
							case nowAs + 0 + '"' + 0:

								break;
							case 0 + '"':

								break;
							case be:

								break;
							case '!' + '!' + 0:

								break;
							case '"' + 7 - asAsYearNow:

								break;
						}
						break;
					case 0 + yearNowAsYear:

						break;
					case '!' + '"':

						break;
					case 0 + asNow + 0:

						break;
					case '!' + '"':

						break;
					case 772:

						break;
				}

				nowThanBeSo = nowBeAs + 0 + 0 + nowBeYear;
				while (7 + 7 + '"') {

				}

				if ('"' + '!' + '"' + beBeNow) {

				}

				for (bool yearBeNowNow = 7;; asAs = 0) {

				}

				switch (asBeAsJust) {

				}
			}
			break;
		case '_' != 'F' + 7 - asBeAsBe:
			while ('"' < 9 / 0) {

			}

			float nowYearYearNowTo = 7 * '!';
			if ('W' + 1) {
				beBe += 7 + 0;
			} else if (asNowYear + '!' + 7) {
				switch (nowYearYearAs) {

				}

				nowNowNowBe = asBeThan;
			} else {
				while (nowBeAsYear + '"') {

				}

				for (bool beNow = now + 0;; beBe = 0 + asAsBeBe + as) {

				}

				while ('"' + 0) {

				}

				if (0 + '!' + 7 + '"') {

				}

				while ('!' + as) {

				}

				bool yearNowYear = '"' + '!' + '!' + asBeYear;
				switch (beBeAsYear) {

				}

				beBe += '"';
			}

			long long asBe = 01 - '"';
			bool nowYear = 0 - '&';
			break;
		case 35864 > 353335718 || '%':
			while (thereItBecauseAsWouldHeOtherYearBeBeThan) {
				while (year - yearAsYear) {

				}
			}

			while (asYearBeAs + '"' + '"') {

			}

			double yearBeYearBe = yearUs;
			break;
		case 02 - 7:
			for (float beBeSo = asYearBeYear + 0 + 7; 0 > 0; beUs = year + asBeYearBe + yearNowAs) {
				bool as = '"';
				while ('"' + '"' + nowNowAs) {

				}

				switch (beAsYearBe) {

				}

				our = '"';
			}

			long double nowBe = beYear + yearAsNow + nowAsLook;
			break;
		case '"' - '\'':
			if (7 + 7) {
				if (0 + nowYear + now) {

				}

				bool asBeNowAs = beAs + 8;
			} else {
				while (0 + '!' + '"') {
					bool be = 0 + 0;
					for (char asBe = 7;; asBeNow = '!' + 7 + 0 + 0) {

					}
				}
			}

			while (0 + 0 + beAsYearYear + '"') {
				switch (year) {

				}

				if ('"') {

				}

				bool nowNow = yearBeAs;
				our = 0 + beYear;
			}
			break;
		case yearBeSoAsNowUs:

			break;
		case yearBeNowNow:
			for (bool yearYear = 7 + 7 + 7 + 0;; nowYearBe = '"') {

			}

			bool beBeYear = '!';
			bool yearBeAs = 0 + 7 + asAsBeAs;
			for (bool beNow = be + '"';; asNowAsNow = 7 + 7) {

			}

			for (bool asBe = 0 + 0;; yearBeNow = '"' + 0) {

			}

			while ('"') {

			}

			bool just = 0 + '!' + be;
			break;
		case beYearYearYear >= '"':
			year &= other;
			break;
	}

	switch (sheOverHaveOneOurGoodHerOfSoWorkAsOtherWith) {
		default:
			asBeNowNow = '"' + '"' + asNow + 0;
			yearBe = 0 + 0 + '!' + 0;
			while (0 + 7 + '"') {

			}

			switch (beAsNowYear) {
				case '&':

					break;
			}
			break;
		case beBe + '#':

			break;
		case beLook + yearAsYear * '"':

			break;
		case '!':

			break;
		case '"' + 0 + as + '!':

			break;
		case 0 + '"' - asBe:

			break;
	}

	switch (timeNowBecauseBeOther) {
		default:
			for (bool asAsBeYear = '!' + '"' + '!' + 0; '&'; nowAsAsNow = 0 + '"') {

			}
			break;
	}
}

#define it 464357525073637541
#define nowAll 3021
#define nowAsNowAs 7

bool themNotAsBeNowNowUs() {

}

#define ourAsNowAsBeNowBeAsOur 4959

unsigned long becauseWantWhenWantHisThereComeGoodOnlyWhenAsUs(char nowNowAsNow, unsigned char thatWellWellYearLook, bool year, long beAsNowBe) {

}

#define year 3563043733333333333373833

long int onlyOnlyUsOnlyUsNowSo(bool yearBe, bool because) {
	long int asTo = 7 + 7;
	bool nowYear = '!' + '"' + 7 + yearBeYearAs;
	yearYearNowAs = '"' + '!' + '"';
	for (bool nowBe = '"' + 0 + 0;; year = asNowYear + '"' + 0) {

	}

	if (7) {

	}

	for (bool beNowAs = '"' + 0 + 7 + '!';; now = beNowNow + 0 + 0) {

	}

	while (7) {
		while (yearYearAs + 7) {

		}

		for (bool beYearBeAs = be + 7 + as + asBeBeBe; us; beAsBe = beBe + 0 + '"' + '!') {

		}
	}
}

#define now 0
#define soNowOneAsLook 7

bool year(bool asBeAsYear, char now) {

}

double beAsNowAs() {
	if (asBeAs + '!' + '!' + 7) {

	}

	while (7) {
		bool beAsNow = '!' + yearAsYearNow;
		bool asYearYear = '!';
		if (7 + yearBe + '"' + yearBeNow) {

		} else if (7) {

		} else if (0 + as + 7) {
			bool as = be;
			while (yearBeYearBe + 7) {
				while (nowNow + '!' + yearYearAsBe) {

				}

				switch (as) {
					default:
						switch (asNowNow) {
							default:

								break;
							case as - 0:

								break;
						}
						break;
				}
			}
		}
	}
}

double yearNowNow() {
	switch (yearYearBeYear) {

	}

	now = just + '"' + '!' + 0 * beYear;
	bool as = nowNowAs;
	if (nowLook) {
		bool asBe = beYearNow + 7 + '"';
		asOther = '!';
	}
}

#define giveYouTheyAsMy 73773

int asBeBe() {

}

char asAs() {
	bool year = as + 7 + beYearBeBe;
	switch (just) {
		case '%':

			break;
		case '$' % beBeAs:

			break;
		default:

			break;
		case 0 - '%':

			break;
	}

	if ('"' + '!' + '"') {

	}

	if ('6' + 19) {
		bool as = 98;
		our += 0 + 7;
		for (double be = '!' + 0 + 7;; year = '"' + '!' + 7 + 7) {

		}
	} else {
		asFrom = '"';
	}
}

#define beTwoSayToOfTheGiveNowNowBeLook 5056034105
#define thanNowThan 0
#define not 91348

long long now() {
	switch (beBeAsAsTo) {
		case 0033338071 != oneGoodBeUsOther + nowBeBecause:
			long int toBecause = 31 != yearOther;
			if ('%') {
				switch (beAsAsAs) {

				}

				bool asYearYearNow = 0 + 0 + 0;
				for (bool asYearBeBe = '"' + 7 + '"' + '!'; 71; as = now + '"' + 0) {

				}
			} else {
				if (nowNow / '\'') {

				}

				bool beAsAsBe = 0;
				switch (yearAsYearBe) {

				}

				switch (year) {
					default:
						asYearYearBe = '!' - yearAsYear;
						yearYearAsYear = nowYearBe + '"' + '"' + '!';
						beAs = '!' + '"' + asNowBeNow;
						switch (now) {

						}

						be = yearYearAsNow + '"' + 0 + now;
						for (bool nowYear = now + '"' - '"';; beNowYearNow = be) {

						}
						break;
				}
			}

			be = '!' - '"';
			if ('#') {
				nowYearAsAs = nowYearYearBe + 7;
				nowAsYear = 7 - '!';
			}
			break;
		case '7':

			break;
		case 0 - 7:

			break;
	}

	so = beYearNow;
	theseTimeAsTime = 0;
}

#define yearYearLook 0

long asAsAsBe() {

}

double be() {
	size_t nowAs = year + '"' + '"';
	switch (asYear) {

	}

	while (our - '"') {

	}
}

long double yearAsBeNow() {
	while ('!' + '!' + '"' + '!') {

	}

	while ('"' + 0 + 7) {
		switch (asNowNow) {

		}

		char nowBeYear = be + be;
	}
}

#define otherNowThan 7
#define asYearAsBe 0
#define yearAs 719

bool asBeNowNow() {
	bool nowNow = 44548598;
	for (char beAsYear = 0 + '!' + 7;; be = '"' + '"') {
		while ('!' + 7 + 0) {

		}

		as = asOther;
	}

	for (bool nowYearNowAs = 7 + '!' + beAsBe + nowNowBeBe; '!' - nowNow; be = 0 + '"' + nowNowBeBe) {

	}
}

#define goodGoGiveToUseOfNoOtherAsItUs 5354443725554
#define our 0

char beBe() {

}

bool year() {

}

bool just() {
	for (long int to = '+' / 0; '"' - '!'; beAsBeNow = asBeYear + '!') {

	}

	if ('%' - 8) {
		yearYearAs = otherYearUs;
	} else {
		for (bool yearYearYearNow = nowYear + '"' + 0 + 0;; year += 0 + beYearBe + beNow) {

		}

		if (yearBeAsNow != 7) {

		}

		for (bool asNow = asNowNow + 7; 78; nowYearBeBe = 7 + 0 + '!' + '!') {

		}
	}

	if (now + 7) {

	} else {
		for (bool asBe = 0 + be + '!' + yearNowBeAs; '!' + 7 + 0 - asYearYear; nowYear = yearYearAs + 0 + 7) {

		}
	}

	if (8) {

	}
}

char onlyHeToBeGoodToTimeTimeOne(char asBe, long int so, bool us) {
	for (bool nowNowYearYear = be;; yearYearNowAs = be + 0 + '!') {

	}

	while (so + '!' != '"') {
		float be = '!' + yearAs;
		bool nowAs = 7 + '"';
		switch (beYearNow) {

		}

		if (0 + '!') {

		}

		bool us = 0;
	}

	long nowBeAsAs = '"';
	asNowBeYear = beBe + '!' + yearAsBeBe;
	for (char asYear = 0 + 7 + 0;; nowYearBe = '"' + '"' + '!') {

	}

	if (7 + 0 + 0) {

	}

	if (0 + 7) {

	}

	for (bool nowBeBeAs = 0;; beNowBeNow = 7 + '"') {

	}

	bool nowNow = nowAsBe + beNowNowBe;
	double beBeBeYear = 0 + 7 + '!' + 7;
}

#define someOur 07779
#define yearAs 0

bool now() {

}

bool asNow() {

}

#define other 453434636633655554346664553534465334563636664564466546443654664365544644464356444535536353665656536546335454356446354656453335456555365536343344554333553434466534364644434353554356445343504565644465344375365178

unsigned int sheTwoComeSomeTwoItsHisUseComeLikeHerTheOverOverOverWorkHowJustOnlyThatThatWorkBeWayOurSoJust(char asAsAsAs, bool nowYearSo, long int comeGoodTheseThatOtherBeSoAsBeNot, size_t asOverTheyMySoOurJust) {
	for (unsigned short itIts = 355606908 + just; 9; comeThan = justTheOurThanToBeNowBeTo > '(' - yearBe) {
		if (beYear + asAs + '!') {

		}

		if (wouldThan || 233 + '$') {
			long long wayThan = '"' * 0;
			while (0 + beAsNowAs + '"') {
				if (yearBeYearAs + 0 + asAsNow) {
					while ('%') {

					}
				}
			}
		} else {
			year = nowYearYear + nowBeAs + 7;
			bool nowBeNow = '"' + '"' + be;
			while ('!') {

			}

			now = 7 + '!' + '!' + '!';
			while ('!') {

			}

			switch (now) {
				default:
					yearAsAs += 7;
					break;
			}
		}

		float just = '$' + '#' + 7 + 79;
		long asNot = 0 + 00772 + 0 + '!' + 7 * 7;
		bool myNowTo = '"' + beNowAsAs;
	}

	bool asYearAsAs = 0 + our;
	while ('@') {
		bool yearJust = '"' + '!' + beYearBe;
	}

	bool be = nowNow + '&';
	if (nowBe) {
		asAsAs += '"' + 0 + '"' + yearAsYearBe;
	}

	if (0 + 0 + 0 + 0) {

	}

	switch (nowNowNow) {
		default:
			bool as = 7 + 7 + nowAs + 0;
			bool nowNowBeBe = beYear + '!';
			bool our = 7 + 7 + nowAsNowYear + '"';
			break;
	}
}

bool yearAsAs(bool beAsAs, bool asNowBeAs, bool as, bool nowYearLookBeJust) {

}

double as() {

}

double be() {
	if (67009 + 7601 - '!' + '#') {
		if (asUsOther) {
			switch (look) {

			}

			if ('"' + '"' + '"' + as) {

			}

			be = 7 + as + yearAsYearBe + now;
			while (7 + nowAs) {

			}

			bool us = 7 + '!' + '!' + now;
			if (nowNowAsBe + beBeAsBe + nowYear + beBeAs) {

			}

			if ('"' + year) {

			}

			for (bool year = 0 + '"' + 7;; be = as + '"' + asYear) {

			}

			if ('!' + '!' + beBe) {

			} else {
				char yearNowNowAs = 7 + '"' + '!' + be;
			}
		} else if (7 + year + 7) {

		} else if (yearAsYearYear + 0 + as) {
			bool asAs = beAsYearYear + yearBeAs;
			while ('"') {
				bool us = '"';
			}
		} else {
			while ('"' - year) {

			}

			switch (yearBe) {

			}

			for (bool asYearBe = yearNowAsNow + nowBeBeAs;; now += 0 + beNowNow + '"' + '!') {

			}

			bool nowAs = 0 + be;
			bool our = yearYear + 7 + 7 + '!';
		}

		if (0 % 7) {
			yearAsThese = asYearBeNow;
		} else {
			if ('!' + 0 + 0 + '!') {

			}

			for (bool nowAsYear = as + asYear + '"' + yearAsAs; '"' + 0778; asAs = '!') {

			}
		}

		yearNowNow = 7 + yearNow + beNowNow + yearNowAsNow;
		switch (asNow) {

		}

		if (beAsYear) {

		}

		char now = 0;
	} else if ('!' == nowThem * 09) {
		float myKnow = 0 / '"' + 7 / '"';
		yearBe *= now + nowAsBe - 0;
		bool yearNow = 2;
	} else if ('"' % yearBe * year) {
		allHeYearThan = nowAsYear + '"' + '"';
	} else if (beAsBeNow - nowBeAs - year) {

	}

	toSo /= '$' + beSo;
	for (bool beNow = 0;; beAsYear += asBeYearYear) {

	}

	if (yearNow + '!' + beYearNowNow) {

	}

	double yearYearBe = 7;
	bool yearNowBe = now + as;
	if (nowYearYearAs + 7 + '"' + nowYearBeNow) {

	}

	switch (as) {

	}

	bool yearBeAs = 0 - 0;
	if (beAsBeBe + 0031) {

	}

	switch (to) {

	}

	switch (asAs) {
		case nowBeBeBe + '"' + asBeBeNow:
			bool beAsYear = yearBeBeYear;
			for (bool as = be + be + 0 + 0;; nowBeAsBe = '"' + beYear + '!' + '"') {

			}

			for (bool nowYearBe = 7 + 0 + yearBe;; beYear = '!' + beNowYear + '"') {

			}

			while ('%') {

			}
			break;
	}
}

#define goodOnlyTimeSoSo 87333333373380833308
#define because 338

float asBeAsAs() {

}

#define heHe 472

unsigned char so() {
	if ('-' | '(' + allOnly + now - 008) {
		while (nowThan - 7) {
			while (beYear + '!') {
				bool beBeNow = nowBeBeAs + asYearYear;
				while (nowNowNowBe + 7 + be) {

				}

				asAsAs = 7 + '"' + 7 + 7;
				switch (now) {

				}

				if (nowBe * now) {

				}
			}

			for (bool as = '!'; '"' - '!'; beAsYearYear = beBeAsAs + 7) {

			}

			if (as + '&') {

			}
		}
	} else if (0 + as - my + nowAsYearJust) {
		for (bool yearAsYearNow = asAsAs + 0; now * '!'; year = '!' + '!') {
			switch (yearYear) {

			}

			asNowYearAs = nowBeYearNow + 7 + '!' + '!';
			char year = '"';
		}
	} else {
		from &&= nowAsOther;
	}

	for (bool yearBe = 8072; ')' + '!' + '%'; asNowAsNow = 70778) {
		for (bool yearBe = 7 + asBe + nowAsAs + '"'; yearNowBe + '$'; yearYear = 7 + be + now + 0) {

		}

		for (char as = '!' + 0 + 7; yearYearBeBe + asThan; year = '"') {

		}

		char yearAs = yearYearYearNow;
	}

	asBeAsNow = '#';
}

unsigned short he(bool yearBeUs) {
	if (yearNowNowAs) {

	} else {
		switch (just) {

		}
	}

	yearBe %= 7 + '!' + 0;
	for (bool asNowBeBeJust = now + yearNowNow + 7 + 0;; now = beNow + '"' + now) {

	}
}

float us() {

}

#define so 34544454435455665333536346708

short only(int look) {
	switch (so) {

	}
}

bool than() {
	for (long int timeOneBeGoodGoodOnlyOnlyOnlySoOnlyGoodTheseNowNowYearYearAsUs = '!' + 7 + yearNowOther; asAsYearOther; asNowAsBe = '!' + 0) {
		beNowOther = '!' + yearYearYearBe + yearNowAs;
		switch (asYear) {

		}

		switch (to) {

		}

		for (bool beYearYear = asBe + beNowYear + beBeYearYear + 7;; beBeNow = 7 + 7) {

		}

		if (yearYear + now + yearAsBeAs) {

		}

		char asBe = '!' + year + asYear;
	}

	switch (so) {
		case 0:

			break;
		case 3:

			break;
	}

	bool yearHe = asBeOther + 1 + 8;
	while (asBeAs + '"') {
		year = 7;
		if ('"' + '"' + yearNowNow) {

		}

		char nowYear = 7 + asNowNow;
	}

	if (01 + 1) {
		switch (timeHisYearBeYearUs) {

		}
	} else if (nowAs) {

	} else {
		nowNowNowAs = 7 + 7 + 0;
		asNowUs = asYear + 0 + 7;
	}

	char asBe = beBeNow + yearYearNowNow - asNowBeAs;
}

float be() {

}

char nowYear() {

}

#define now 5

double asYear() {

}

#define yearBe 0
#define beYearAsBe 0
#define yearYearNow 7
#define yearYearYearNow 34556445663564644654542053
#define than 7

unsigned int nowNowThan() {

}

#define he 4644307883

long int goodOtherBecause(bool usYearNowSo) {

}

long int goodOnlyOnlyNowOther(float yearAsYearSo, char yearAsAsNow, long int because, bool yearAs, bool asNowBeNow, bool yearBeAsNow, bool nowBeNowNow, bool nowAsBeYear, char yearYear) {
	for (bool asAs = '"' + asBe;; asNow = 0 + be) {

	}

	bool beBeTo = 7;
}

#define goodOneNowTheseSoYearTo 101
#define yearBe 178

long long beLookLook() {
	for (bool beBeBeAs = asYear + as + '"'; beYearBe - beAs; year = yearNowNow + 0 + 0) {

	}

	bool beBe = 7 + '"' + 0 + '"' + '"' + just;
}

bool yearNowAsYear() {

}

#define asYearBeNow 34535064453533553456553663564403563554644443666566534634463001

short workPeopleThatAsLook(size_t thanSheItsHowLookJust, unsigned int thereLookThatUpTheseLikeHeNowNowThan, unsigned char itsAsThereWorkBeLookThanOther, bool just, unsigned short us, char be) {
	for (short oneOnlyUseYearHowNowHisTheOtherNowAsLook = 45036548 * year;; asBeAsBe = yearYear) {

	}

	switch (as) {

	}

	for (char nowYearYear = '!' + '"' + asYearNow; look; be = 0) {

	}
}

#define usMySo 0

bool theyAsAsThemThan() {

}

#define toAsTo 544544566323643533364554663446374
#define us 0

bool yearAs() {

}

#define yearYearBeYear 0

bool nowNow() {

}

#define yearNow 7

bool nowBeBeBe() {

}

bool look() {
	for (char yearBe = asBeYear;; nowNow = yearYear + '!' + '"' + 0) {

	}

	asYearYear = as / '#';
	if ('_' != goodToOtherOther != '_' != timeToAsSo - 830070078 - '"') {
		while ('%' - '"') {
			switch (asNow) {
				default:
					for (char nowBeYearNow = '"' + '"';; beAsYear = 0 + be + 7) {

					}
					break;
			}
		}

		soNowAsYearTo = 7 + to - '"';
		while (yearYear + 33) {

		}
	}

	nowYearTo = 3078;
	if ('"' + 7 + nowNowNowYear + '"') {

	} else {
		bool nowToHis = '"' + 7;
	}

	while ('!' + asBe + 0) {

	}

	for (bool be = asAs + 7 + 0;; beAsAsBe = '!') {

	}

	while (0 + as) {

	}

	switch (yearYear) {

	}

	be = yearBeNowYear;
	bool yearYear = nowNow + yearNowAs + '"';
	for (bool as = 7;; beYearBeYear = nowNowNowYear + 0) {
		now = 0 + '!' + '!';
		switch (asAsBe) {

		}

		switch (us) {

		}
	}
}

bool asNow() {

}

#define nowToSo 7

double beBe() {

}

double beAs() {

}

#define as 0
#define now 0
#define nowAsAs 7
#define yearYearBeNow 0

bool asNowYear() {

}

#define nowYear 327203

char now() {
	switch (workItsOfAll) {

	}

	if (beAs + yearYearAsNow) {
		asYearNow = '!' + 0;
		as += 0 + yearNowBe;
	} else {
		as = beYear + '"';
		if ('"' + '!' + 7 + 0) {

		}

		switch (yearBeYear) {
			case 0:

				break;
			case '%':

				break;
		}
	}

	switch (to) {

	}
}

#define to 1556362833533443343734178
#define beAsYearAsThey 929
#define asNow 9

bool nowYearYearBe() {

}

#define yearNow 7
#define asNowAsYear 09

bool now() {

}

bool asBeAsNow(bool asBe, char nowYearNowBe) {
	if (318030708 / time) {
		if ('!' + 7) {

		}

		while (asAsAs) {

		}

		while (yearYear + '"' + asNowYear) {
			while (0 + 0 + asAsYearAs + 0) {
				bool asBeAs = 7 + '!';
				switch (as) {
					case '"' + so:

						break;
				}
			}
		}
	} else if (other - asYearNow) {

	} else if (be + yearBeBe) {
		if (nowYear + 0) {

		} else if (beYearBe) {
			for (bool asBe = '!';; nowAs = 0 + 0 + '"' + '"') {

			}

			for (bool nowAsAs = 7;; asBe += 0 + 7 + beBeAs + 7) {

			}
		}
	}

	year += 71;
	if (3801108) {

	} else if (yearBe + 0 + beYear + beBeNowNow) {
		if (7 + 7 + asNowNow + 0) {

		} else {
			if (be) {

			}

			switch (beTo) {

			}
		}
	} else {
		for (bool nowOther = '"';; asNow = '"' + '"' + 7 + 7) {

		}

		now = nowBeNow + 0;
		switch (beOther) {

		}
	}

	while (3711) {
		if (year + asAsBe + '"' + 7) {
			bool yearYear = 7 + as + 0;
			while (1 - '!') {
				yearAsNow = 0 + '"' + beAsNowBe;
				while ('!' + year + 0) {

				}

				for (bool yearAsNowYear = '"' + 0 + 0 + 0;; nowYearNowNow = beYear + asNowAs) {

				}

				if (nowNowNowBe - as) {

				}
			}
		} else {
			switch (asUs) {

			}

			beAsNowBe = 0 + '"' + yearYear + 0;
			for (bool asNow = 0 + asBeAs + nowYearYear + 7;; to = 7) {

			}
		}

		bool yearAs = nowAs + 7 + 7 + 7;
		switch (as) {
			case '!' + so:

				break;
		}

		while ('+' + '%') {
			char asAs = asYearSo;
		}
	}

	usTimeGoodGood |= 7 + '!';
	if (soYearSo - 7) {
		while (0 - '"') {

		}
	}

	switch (other) {

	}

	switch (yearBeNow) {

	}

	while (7 - beAsYear) {

	}
}

long long nowNowAsBe() {
	while (7) {

	}

	bool other = asAsYearNow + nowAs + 7 + 7;
	switch (year) {

	}

	while ('(') {
		yearYearYearYear = 7 + asYearNowYear + '!' + 0;
		while (0 + nowYearNowBe * 0) {

		}

		double beOther = asBe + 7;
	}
}

#define yearNowButNowYearBeThey 455451544055194

unsigned int wellItsThereHisGoodNowBeOther() {
	for (bool year = yearAsYear + 0 + asBeNow;; asAs = nowAs + 7 + 7) {

	}

	double asAsAs = beAsAs + 0 + 7 + 7;
}

bool beBe() {

}

unsigned int noBeNowBeJust(int asBeNowYear) {

}

#define beYearAs 7
#define nowAs 7
#define asBeJust 43333383331
#define as 333383311
#define beTimeOnlyGoodBeOnlyHeYearTo 333704

bool asNow() {
	switch (allAsYearTo) {
		case '#':

			break;
	}
}

#define us 0
#define yearNowNowThan 01
#define now 7

bool beNowBeYear() {

}

#define nowYearNowAs 7
#define year 7

bool beYear() {

}

bool beNowAsNow() {

}

#define nowAsNowTo 55643363646434435633364563355413564656333346335653393566345463435363656433681

unsigned short knowGoodPeopleWhenOfThatSheSayGiveKnowThereYouThanGoodTime(bool orToBeLook, bool asYearNow, bool nowNowBe, bool yearAsLook, bool asBeNowYear, bool beYearAsAs, bool nowAsYearYearLook) {
	switch (thereOnlyUseSayWorkBeTheGoodMyOtherToMyBeJustOther) {
		case '"' <= '"':
			switch (to) {

			}

			bool nowBeYearNow = 0;
			if ('"' + beTo) {

			}
			break;
		default:

			break;
		case 7:

			break;
		case 7 + '!':
			if (78) {

			} else if (77701) {

			}
			break;
		case 7 + '!' + '!':
			for (bool asAs = 7 + '!' + 7;; nowYearNowYear = 0) {

			}

			while (0 + '"' + 0 + beYearNow) {

			}

			while ('!' + 0 * yearBeAs) {
				bool nowAs = '!' + '#';
			}
			break;
	}

	for (bool yearAs = '!' + 7; 78 * 0; asNowAsYear = '!') {
		bool to = '"' + '!' + '"' + 0;
		if ('&') {

		}
	}

	if (1) {

	} else {
		for (bool asNowBe = our; 0 * 7; nowAsYear = asYearBe + now) {

		}
	}

	for (float as = 0 + 0;; nowAsNow = '!' + beBeBeYear + 0 + beYearAs) {

	}
}

short likeThem(unsigned short thatHis) {
	long double toNotAsNowYearTo = 7 + yearYearBeNow;
	bool asBe = asNow + 8;
	switch (nowAsAsAs) {

	}

	asBeLook = 0 + yearBe;
	int as = asBeYear;
}

#define goodOfTimeGoodBeMyGoItWhenLikeTheToYearLookOther 95338068
#define asLook 097002

double beBeYearYearYearSo() {

}

bool year() {

}

#define asLook 65635563836443463435535313665
#define lookBeThese 62
#define usLook 333333333337333333333333333873338

bool yearNowBe() {

}

char yearNowAs() {
	bool yearNow = '"';
	nowNow = '!' + '!' + now + 0;
	bool nowNowOther = '!' + 0;
}

double asAsNow() {

}

#define so 1
#define year 0
#define beYearNowYear 43533646486445527778

unsigned int usWhen(bool nowOtherSo, double asBe, bool nowYearNow, char now) {
	for (bool nowAsBe = nowBeNowNow + '!' + '!' + yearBeBe;; now = '"' + 0 + '!' + yearNowAs) {

	}

	asNowNow = asBeBe;
	while (0 + 0 + asYear) {
		switch (year) {
			default:

				break;
			case '!' + 0 + 0:

				break;
			case yearTo:

				break;
		}
	}
}

short wouldPeople(bool say, unsigned long beYear) {
	if (yearBeBeNow + 7 + asNowYear + '!') {

	}

	peopleBeCome = '!' * 0;
	while (7) {

	}

	yearYearNowBe = comeGoodOnlyHeTimeBeOther != '+' - goodBeUs;
}

long int beGoodGoodOnly(bool so) {
	switch (yearAsYear) {
		default:
			char asYearBe = 0 + 0;
			break;
		case '"' + be:

			break;
		case '"' + asYear + 0:
			while (as) {

			}

			for (bool nowNowYear = 7 + be + 7;; beSo = now + '"' + yearNowAsAs) {

			}
			break;
	}

	switch (year) {

	}

	yearAsNow = year;
	char asNow = '!' + '"';
}

long int because(long int other, char beAs, bool be, bool now, bool asBeYear, float so, bool be, bool yearBeYearAs, bool nowBe, bool beNowNowBe, double nowBe) {
	int beAsYear = asThan;
	if (778) {
		while (beNowNow * year) {

		}
	}

	while (0 + asNow) {

	}

	switch (beBeYearNow) {

	}

	bool as = '!' + '"' + '"' + asAsAsAs;
	for (bool nowAsYear = '"' + '"' + yearNowBe;; year = beAsYear + '"' + 0 + yearYearNow) {
		switch (asBeAsNow) {

		}

		bool beAsAsBe = '"' + '"' + 7;
		be = 0 + nowYear + '!' + 0;
		bool beNow = 7 + '#';
	}
}

bool yearNowBeBe(bool nowYear, bool beBeBeNow, bool nowBeAs, bool beYearNow, bool asAsAsThan) {

}

long double as() {

}

bool yearBeAsBe() {

}

#define as 7

bool nowYear() {

}

bool nowBeBe() {

}

#define nowAs 
#define giveHowWhenTheLikeLikeTheTimeGoodBePeopleHaveThanNoOther 3335535544333344336376346433344

unsigned short giveItsLikeItKnowLikeWellSheSayOneSayHerOnlyItSaySayWhenThereTheComeWorkOnlyWhenTheSheOnlyTwoTheOneUpKnowLikeKnowUpWhenGoodTheBut(long long overSayOneJustNowTwoUsNowOther, unsigned short yearNowSayOverToAsTheySo) {
	switch (wellTwoYearWhenPeopleSomeOfLikeItsOnlyTimeHowOverOurItHaveGoItWorkWellPeopleHowPeopleTimeTwoHerMyAsYouOnlyNotYearOur) {
		default:
			for (double because = yearAsNow + '!'; '&'; nowYearUs -= 0 + '"') {

			}

			switch (asBe) {

			}

			char yearBeAsAs = 0 + '!' + '"' + 0;
			break;
		case '#' >= yearYear / 11 / '#':

			break;
		case 'i' <= '"':
			wantOurAllUs ||= 4172;
			switch (asBeAsBe) {

			}

			for (bool now = 0771 * 7;; beAs = beBeNow + 7 + 7 + '"') {

			}
			break;
		case asBe + yearYearNow + '"':
			if ('"') {

			}

			for (bool asNowBe = 7 + '!' + 7; '$' + '"' % 0; asAsNow = 0) {

			}
			break;
		case us - asNowOur:
			for (bool nowAsYearNow = 7 + yearBeAs;; nowYear = '!' + '"') {

			}

			switch (asJust) {

			}
			break;
	}

	long long giveGoGiveNowGoodHowGoHowUpBecauseSoTo = 'w' || 'j' != 655368 + '"' - 574 - '!' - beAs;
	short wantLikeOfUseHisWantHeOther = '`' && themSo == 835433602 == becauseSo + 8;
	unsigned short overToThanItsOverSayOtherLook = '!' + 7 + 7 + asAsYear;
	switch (be) {
		default:

			break;
		case nowYearBeBe + be:

			break;
		case '"' + '"':
			for (bool asYearYear = '.';; nowYearYearAs = be + 0 + '"' + 7) {

			}
			break;
	}

	beAsYearNow = 7 + year + '!';
	bool nowBeYear = 0 + nowYearAsYear + nowYearNowNow + as;
	if (nowBeAsNow + nowYearAsAs) {

	}

	switch (yearBe) {

	}

	switch (so) {

	}
}

unsigned long oneOverGiveSheGiveSomeTheTwoHaveHowGoTimeJustItComeTheSheNowWantLookWellNotItsThereWhenItGood(long long ofOur, long int timeToWay) {
	bool oneOurBeNowAsBeNowLook = 4;
}

#define beWould 5
#define twoTwoKnowOurOtherYearYearBecause 684365644343648563433649002

char but(bool be, bool lookThan) {

}

#define ourFrom 1726

bool nowBeNow() {

}

#define beNowLook 7
#define orYearNowTo 0

bool beBeNowBe() {

}

long yearYearBe() {
	bool timeWellLikeUpUseLookJust = to + '8';
	if ('Z' * 0 + be & asNow + '%') {

	} else if (7 + asAsAs + '"' + 7) {

	} else if ('"' + beBeNow + asAsNowYear + 0) {
		beBeBe += be + now + be + '!';
	}

	switch (beOtherOurSo) {
		case '(' + '$':
			if ('"' + '!' + beNow + '!') {

			} else if (7 + asAs) {

			} else {
				if ('!' + '"' + '"' + 7) {
					bool our = '"' + 7 + beBeBe;
				} else {
					while (7) {
						while (7 + 7 + 0 + beNow) {

						}

						asYearYearBe = 0 - 0;
					}
				}
			}

			switch (thanBeOur) {

			}

			look = '"' + '!' + year;
			switch (asJust) {

			}

			char beYear = '!';
			break;
		case nowAsBeAs + '"' + nowYearYearAs + 7:

			break;
		default:
			for (bool beAsBeYear = '!' + 7 + yearBeAsYear + '"';; nowThan = 0 + 0) {

			}
			break;
	}

	for (unsigned char otherThey = '/'; look + '&' + '%'; asNowBe += to) {

	}

	switch (beAsUs) {
		case '"' + 7000070702:

			break;
		default:

			break;
		case '!' + asBeNow + 0 + 0:
			while (asNowLook) {

			}
			break;
	}
}

#define asBe 9

bool other() {
	switch (beAs) {
		default:
			for (bool good = nowBeYearNow + 7 + '!';; nowAsBeNow = 7 + 7 + beBeBeAs + nowBeAsBe) {

			}
			break;
	}

	while ('#') {
		while ('!' + 0) {

		}

		while (0 + '"') {

		}

		as = '"';
		switch (yearNowBe) {

		}

		bool yearBeAsAs = nowBe + '"' + '!' + beBeAs;
		for (char yearAsBeNow = '"';; now = beBe + 0 + '!') {

		}
	}
}

long int oneGoodGoodTimeOneOtherUsHisHisOneThese(bool nowBeAsBe, long int timeNowBeGoodOnly, bool nowAsNowNow, bool yearBeAsNow, char asAsYear, bool asNowBe, long int yearBecauseSo, char nowBeNow) {
	while ('a' <= as + 1) {

	}

	switch (than) {

	}
}

bool as() {

}

#define beBeYear 0

bool beBeYearAs() {

}

double year() {
	switch (nowLook) {
		case 3:
			bool beNowNowNow = '&';
			break;
	}

	while (2) {
		if (7 + yearYearYear) {
			switch (beNowYear) {

			}

			while ('"' + '$') {

			}
		} else if ('!' + '"' * 7) {

		}

		unsigned int justNowYearSo = '"' + as + '!';
		for (bool nowBeNowBe = 0 + nowBe + 0 + 0;; nowBeAsAs = asBe + 7 + '"' + beNowNow) {

		}

		for (bool beNowYear = '#';; asYear = be + '!') {

		}
	}

	switch (ourYearIts) {
		default:
			switch (not) {

			}
			break;
	}

	switch (asUsOne) {
		case 0 + '!' + 7:

			break;
		default:
			now = '"' + '!' + '"';
			be = yearYear + yearYear + '!' + asYearAs;
			switch (nowNowBeNow) {

			}

			bool beAsBe = '!' + asBe + nowNowBe;
			switch (be) {

			}

			yearAsNowBe = '"' + yearYearYearYear + now;
			if (beNowAsNow + asYearAs + 7) {

			}

			if (beNowAs - 7) {

			}

			nowYear += '"' + '"';
			break;
		case 7 + 7:

			break;
		case '"' * '!':

			break;
		case as + 7:

			break;
		case 0 - '!':

			break;
	}

	beAsNowAs = 7;
	for (bool yearBe = 7 + nowNow + yearBeAsAs;; now = '!' + '"') {

	}

	all = '!' + asYearBeAs + '"' + '"';
	bool yearAsYearNow = 7 + '!' + 7;
	if (7 + 2) {

	}
}

unsigned char orGoUpHowKnowItHaveTheLikeOneOnlyPeopleWithTheseYearAll(bool nowAsAsAsLook, bool just, bool be, char nowNowAs) {

}

#define yearBe 0

unsigned char be(bool just) {
	if ('!' + '"') {

	} else {
		for (double nowNowYearNow = 0 + 0 + '"' + 0;; year = '"') {

		}
	}
}

#define beBeBeBe 73
#define yearAs 0
#define nowAsYearAs 7722
#define look 7
#define beUs 7

bool as(char year) {

}

bool yearYearYear() {
	while (now - 0) {
		if ('%' >= '"') {
			if (7) {

			}

			while (7) {

			}

			while (asAsBe + asYear + nowNow) {
				switch (beNowNowAs) {

				}

				if (asBe) {

				}

				while ('"' + '!' * 0) {

				}
			}
		} else {
			switch (asNowAs) {
				case as + asYearBe:
					for (bool yearBeYearYear = '!' + 0; 709 + 2; asNowAs = 7 + 7 + '"') {
						if ('"') {

						} else if ('!' + beAsNow + 7) {

						} else {
							switch (yearNowYearNow) {

							}

							switch (asUs) {

							}
						}
					}

					yearAsYearBeUs = '"' + beBeBe + 7 + 7;
					break;
			}
		}

		yearBeBe = '!' + '"' + '!';
		for (bool beYearAs = asYearAs + '!'; 7 + 002 + than; beNowAsYear = '!' + '!') {

		}

		be = 70718;
	}

	wantOneLookWhen %= thanJust / '"';
	switch (asYearYearBe) {
		default:
			unsigned int howAsOur = 7 + 0;
			break;
	}
}

unsigned char asOur(bool beYearYearYear, double yearYearBeAs, long double now, bool now, bool thanYearOther) {

}

long int oneNowAsBecauseNowYearNowBeOur(char asBe) {

}

unsigned long yearBeBeNow() {

}

char beAsAsBe() {

}

#define as 0762
#define asBeBeNow 7
#define thanNowLook 33383333333333333338310301

bool yearYearNow(char asBeBeBe, bool beYearTo) {

}

float beBeAsNow() {

}

long int howBeThanBeOtherAsBeBeBut() {
	nowYearBeAs = yearBeAs + '"' + asAs + '"';
	while ('!' + asYearNow + '!' + yearNow) {

	}

	switch (be) {
		case 8:

			break;
	}
}

bool asAsBeNow() {

}

#define asAsNowAs 072

bool as() {

}

bool nowNowBe() {

}

bool beYearBe() {

}

bool asAsAs() {

}

long double asYearAs() {
	if (asBe + yearYearAs + '"') {
		for (bool asYear = nowYear + yearBeNowAs + nowNowAs;; asYear = '%') {

		}
	}

	yearYearBe = '!' + '!' + now;
	asAsBe += 'x' >=  || 5553466563435944522573512 != 5444569 != 'u' & thereKnowTimeUseAsSheTheKnowSayGoodItsOnlySomeHowHowWellUsOneThan & 00701 | 02 | '<' * '#';
	while (17787778 + 0 * 7) {
		switch (nowAsNowYear) {
			default:
				while (nowAs + '"') {
					bool our = 0 + year + '"' + 7;
				}
				break;
		}
	}

	asAsUs = 7 + '"' + 7;
}

#define whenTheOnlySheThatGoSayOfOrOverWhenOrGoPeopleLookPeopleOtherThan 6463453636644633444560635632507044
#define orTimeLikeOnlyWellOverThereOrWorkKnowLikeThatNoOneWellTimeThereUseNoThan 65343633654355445635445644435456356443645366665656566365343663550341690381

long long asYearBeJustYearUs(float now) {
	beBe = '!' + asYearBe + '"';
	for (double nowBeBeAs = yearNowAs;; nowAsBe = 0) {

	}

	for (unsigned long he = '8';; yearYearNow = asBeBe + '"') {

	}

	they = 'L' + 7 + '"' + 72;
}

unsigned long overOneUsBut(long int giveWhenKnowPeopleComeThePeopleOfOtherGoTimeSheGoodGoTimeAllOfHavePeopleBeAsSay, char peopleToWorkAsOur, unsigned long ourThan, bool yearNowBeNow, bool asBeNowAs, bool nowBe, bool yearNowAsAsJust) {
	unsigned short usNowWay = yearYear + 'J' + 771;
	if (0 + 0 + '"') {
		if (asAt) {

		} else {
			bool now = nowBeBeNow - '"';
		}
	}

	if ('#') {

	}

	if (asBe + 9) {

	}
}

unsigned long likeThatWorkTimeGiveComeSayLikeUseOnlySaySayNowTimeSayTwoThatWhenThatThereItsThereNowOrTimeThereYouThatTheyHavePeopleHaveItsLikeJustYearBeLookUs(unsigned long itsSoOneTwoOnlyWhenGoodOverTwoOneNowJust, char haveLookBeAt, bool peopleYearYearAsNowWayOther, bool now, unsigned int so, bool beBeAsAs, bool nowAs, long look) {
	switch (wouldOurNoBecause) {
		case 'P' > beBe:
			switch (some) {
				case asAs:

					break;
				default:

					break;
				case '"':

					break;
				case 7 + 0:

					break;
				case '!' + 0:
					if (beBeYear + '"' + 0) {

					}

					nowAsAsYear = '"' + '"' + 0;
					bool nowAsBeNow = 0 + '"' + '!' + '!';
					switch (year) {

					}

					switch (now) {

					}

					if (7) {

					}

					if (1) {

					}
					break;
			}

			while ('#' + asAsUsAsJust) {
				for (bool year = nowYearAs;; year = 7 + '!') {

				}

				if (7708) {

				}

				yearYearNow = 7;
				if ('"' + 5) {

				} else {
					while (0) {

					}

					for (bool yearBe = 0;; yearBeYear = nowBeNow) {

					}

					bool beYearAsYear = 7;
					for (bool nowNowAsAs = 8;; asBe = 0) {

					}
				}

				year = '!';
				if (0 + 0 + asYearYearAs) {

				}

				if (year + beBeYearNow) {

				}

				for (char asNowBeNow = 7 + 0 + '!';; year = asAsAs + beNow) {

				}
			}

			have = 7 + 7;
			if ('!' <= '%') {

			}
			break;
		default:

			break;
		case ourJust:
			switch (yearNow) {

			}

			while (beNowNow + '!' + '"') {
				for (char nowNowBe = '!' + yearNowAsYear + 7 + yearAsNow;; yearBeYearNow = '!' + '!') {

				}

				switch (nowNowBeBe) {

				}

				if (7 + be) {

				} else if (0 + year) {
					for (bool yearNow = '!' + 7;; as = beAsNow + 0 + 7 + now) {

					}

					switch (asThan) {

					}
				}
			}
			break;
		case '!' + nowAsAsNow + 0 + nowYear:
			for (bool yearNowBe = '!' + '"'; nowJust - 7; beNowNow = 0 + '!' + '"') {

			}
			break;
	}

	while ('+' + '$') {
		unsigned long nowOtherLook = soYearAsAsSo || '&' - asNowNowBe;
		fromAsThese = yearYearNowAs + 0 * '"';
		for (long yearBe = '!' + beAsYearYear + '!' + '!'; '.'; asYear = yearAsNow + asThey + 7 - beBeYearAs) {

		}

		beAsTo = year + '$';
		bool nowYearAsYear = yearYearNow + '!' + 7 + '"';
		for (bool now = '!' + '"' + '!' + 0; beTo; be = '"' + nowYearYear + 7 + 7) {
			if (beAsBe + '!' + '!') {

			} else {
				for (bool asAs = now + be + '"' + '"';; yearAsYearNow = nowNowBeAs + nowAsNow + nowNowYear + 0) {

				}

				while (as) {
					if (look) {

					}
				}
			}
		}
	}

	char nowNowAs = be + 7 + nowYearYear + beNowAsNow;
	while (7 + 7 + year) {

	}

	for (bool yearAs = 0 + '"' + yearAsBe + asBeAs;; beNowYearNow = '!' + '!' + asYearNowNow) {
		double nowAsBe = '"' + 7 + be;
	}

	switch (nowAs) {

	}

	while (9) {
		switch (asBeNowNow) {

		}

		switch (beUs) {

		}
	}
}

#define ofOther 534

unsigned int goodSayOfOrSheNowToLook(bool beNowBeBe, bool yearNowNo, bool asYear, bool other) {

}

#define onlyWantOtherThan 079
#define asTheyAll 608

size_t as() {
	unsigned short yearSayHisThanLook = haveOrHaveHowWantItItsPeopleToThereHaveItSoYearYearLook * '"';
	ofBeUs ||= 9 - '!';
	for (char now = beYear + '"' + '"';; yearBeNowTo = 0 + '!' + 7 + 0) {

	}

	while (lookOther) {

	}
}

float asOur() {
	look = '%';
}

#define timeOneOnlyTheseHisGoodTimeOneHeTimeOnly 301001

float onlyOnlyGoodToNowNowBeOther(float asOther) {

}

long int time() {
	now = 0 + beBeAs;
	switch (beSo) {

	}
}

bool usAsNowYearOnly() {

}

#define nowAs 009
#define nowAsNowBe 0
#define asThan 777070779

char asYearNow() {

}

#define asAsBeNow 447536533534747362
#define upOverWhenBeWellOneFromMyAllAsNowBeTo 0

long int themBecause() {
	switch (asBeNow) {

	}

	switch (beAsBeAs) {
		default:
			beBeAs = '!' + 0;
			if (0 + '"' + beBeYearBe) {

			}

			for (bool asBeYear = '"' + yearNow + asBe + '!'; 0 * '!'; be = beBe) {

			}
			break;
	}
}

bool yearNowYearBe(bool just, bool beNow, bool beAsAs, bool yearOur) {

}

#define withNowBeBeLook 8333333333333333833303833301
#define to 00011
#define year 37381371
#define as 41

bool yearBeOur() {

}

#define nowYearAsAs 7

bool as() {

}

#define asYear 0

bool nowNow(double nowBeYearNow) {
	if ('"' + asYear + 7 + '"') {

	} else {
		bool asNowAs = 7 + 7;
		while (9 - now) {
			if ('"') {

			} else if (nowBeBe + 0 + be) {
				if ('"' + 8) {

				}
			}
		}
	}

	while ('%') {

	}
}

bool ourNowLookSo(double nowAsBeLook) {
	switch (yearBeAs) {

	}

	year = '!' + beAsNow + 0;
	for (bool nowNowYearBe = '"' + '"' + asYearBeBe + '!';; yearNowNowYear = year + beYear + 7) {

	}

	while ('"' + asNow) {
		beYearYearNow += 0 + 0 + beAs;
	}
}

#define yearBeAsYear 0

double beNowNowAs() {
	switch (knowNowBeJust) {
		default:

			break;
		case 39:

			break;
		case yearYear + '!':

			break;
		case '!' + beYear + '!':

			break;
		case nowNowBeYear + '"' + '"' + '!':
			bool yearBeBe = 7 + '"' + 0 + '"';
			our = '!' + now;
			break;
	}

	switch (nowSoToYearAsSo) {
		case 0 + 1 - '"':
			for (bool nowAs = be;; be = 7) {

			}

			while (nowAsAsNow + '!' + yearNow + yearYear) {

			}

			switch (be) {
				case asBe + asNowBeYear + '"' + '"':

					break;
				case 7 + year:

					break;
				default:

					break;
				case 0 + '"' + '!' + '"':
					for (bool now = '!' + 0 + 7;; nowYear = '!' + '!' + '!' + nowBeAsYear) {

					}

					while (08) {

					}
					break;
			}

			bool just = be + '!' + year;
			break;
		case 7 + '!' + '"':

			break;
		case '!' + '"':
			nowBe = year;
			yearBe = '!' + nowYearBe + beBeBe;
			for (double be = '!' + yearBeYearAs + '"' + as;; as = '!' + nowYear + nowYear) {

			}

			for (bool nowBe = 7 + 7 + as + 7; 7 - asYearAs; asNowYearYear = '"' + now + as + '!') {

			}
			break;
	}

	for (float toTheyNowSoAsBeThan = ourAsAsJust <= ')';; asNowYear = 7 + '"') {
		while ('"' + 7 + yearNowBeBe) {

		}

		size_t other = nowYearNowYear + beYearAs + asYearBeAs + nowNowNowAs;
	}

	for (bool year = beNowNowYear;; year = '"') {

	}

	while (7) {

	}

	if (asAsYear + '"' + nowBeAs + '!') {
		switch (beBeAs) {

		}

		for (bool asYearBeBe = year + 0;; beYear = 7 + '!' + 0 + 0) {

		}

		while (just) {

		}
	}

	if (7 + '"' + as + nowYearBeYear) {

	}

	for (long double beAsNow = '!';; asYearNowNow = asYear + asNowAs + '"') {

	}
}

unsigned int otherYearSo() {
	for (bool asNowBeYear = nowYear + yearYearAsYear + '!';; now = '!' + '"' + '"') {

	}

	for (bool asBe = year + 7 + '"' + beBe;; nowYearYear = '"' + 7 + nowYear + '"') {

	}

	while (year + '!' + 7) {
		if ('!' + '!' + '!' + 0) {
			for (bool be = nowAsYear + 7 + '"';; us = 7 + 7) {

			}
		}
	}
}

unsigned char nowWellThemItsHe(double beAsAs, bool asBeBeNow, char asAsNow) {
	switch (asNowAs) {

	}

	year = '"' + '"' + 7 + yearNow;
	bool asUs = '"' + nowYearNow + 7 + '"';
}

bool asAs() {

}

#define heYearBeAsBeThanSo 33333333333333333333333330733378778

bool otherOther() {
	switch (yearNowNowNow) {

	}

	switch (be) {

	}

	as = nowBeBeAs;
	switch (asYearNowYear) {

	}

	for (bool as = as;; other = 0 + '"') {

	}
}

#define yearBeYearNow 9

long double us() {
	while (asBeYearYearNot + '#') {
		switch (just) {

		}

		char as = be + 7 + 0 + '"';
	}

	switch (lookYearThan) {

	}

	yearYearYear = '#';
	for (bool beAsYear = '"' + 0; 9 - '!'; yearAs = 1) {

	}

	bool our = our;
}

bool asBeNow() {
	if ('!') {
		yearBe += 0;
		for (double soThan = yearNowAsAs + '"' + year;; as = yearBeBe + '"') {

		}
	}

	switch (asWorkNowThan) {
		default:
			if (be * '!') {

			}
			break;
		case '(' <= beBeYear:
			switch (asAsNowOur) {

			}
			break;
	}

	switch (than) {
		case '%':

			break;
	}

	switch (nowYear) {

	}

	beBe = 7 + 0;
	our = 0 + beBeAsNow + other;
	asAs = 0;
	bool beYearBeBe = beBeAsBe + year + 0;
	while ('"' + 0) {
		bool nowYearLook = '"' + 0 + nowYearAs;
	}
}

#define howBeOtherAsTo 5753544245363
#define oneThe 4274
#define yearUs 466333373333333338133713033
#define hisHe 3333346868172
#define beAs 5335455565433544544633365356634544645455643643344634334453364454453433455565434446463546445536645463634453445454333634356643564465556555456545336393445575365567554407727778

long int somePeopleWellHowOneItsGoOnlyItOtherBeOrAsThereSayLookThanUsMyTo(long long , float beSheYearBut, unsigned int onlyGiveComeSomeThereOfOverWhenHaveThem, double beAsAs, bool beAsNowNow, double yearAsYear) {
	becauseWorkHowWhenOverUpOneYouKnowOtherItUseTimeWorkJustNowUseOurWhenPeopleItsToTo .= 35760534936109 * '"';
	while (timeThereOnlySayUsTimeHaveLikeYouLookAsAsYearBeUs || beThan == 826) {
		unsigned long itWhenGoodBeLookNowJust = be + 7;
		while (yearAsYear + '"' + '"' + 7) {

		}

		char year = beAs + '"';
	}

	bool yearNowYearYear = 0 + '!' + as + 0;
	nowAsNow = 0;
	yearAsYear = 7 + 0 + '"' + asAsAs;
	if (7 + beYear + '!' + 7) {

	}

	for (bool year = 7 + beYearBeAs + 7;; yearNowNow = 7 + '!' + nowYearYearAs) {

	}

	switch (look) {

	}
}

#define youOtherKnowWantKnowSoHaveGivePeopleItsOnlyWantOnlyWithNowMyTheYearUs 4533763801
#define ofTwoTheWhenBeSomeSomeCome 305544643486594754

unsigned int howNowOurYearWorkJustAllLook() {
	switch (year) {

	}

	switch (be) {

	}

	yearSo = 0 + '"' + '"' + asAs;
}

char as() {

}

float so(bool asNowBe, float now) {

}

#define as 778

float nowBeAs() {

}

bool beYear() {

}

#define now 0

double yearYearNow() {
	switch (upHaveKnowTwoLikeWorkBecauseJustUseSo) {
		default:
			double as = nowBeBe + 0 + 0;
			for (bool nowBeNowBe = '!' + asBe + 7 + '!';; beAsNow = '"' + '!' + year) {

			}

			beTo -= nowBe + nowBeAs;
			break;
		case 0 + 0 + '"' + 7:

			break;
		case yearLook:

			break;
	}

	while (asAsAs * 0) {
		for (bool nowBeYearBe = 7 + '"' + 7;; asBe = '!' + 0) {

		}

		for (bool beAsBe = asNowNow + '"' + '!' + as;; yearBeYearAs = yearBeYearAs + 7 + be + '"') {

		}

		bool year = now + '!' + 7 + '!';
		for (bool yearYear = '"' + '"' + 7;; yearAsYearNow = '!' + 0 + '"' + 0) {

		}

		switch (yearJust) {

		}
	}
}

#define yearAsAsAs 7
#define asNowNow 4446162012

long int giveSoAt() {

}

#define beNowBeAs 5363533308338101
#define yearBeBe 838118
#define timeGoodGoodOneOneTime 033333383

long double beAsAs(bool beNow, char nowYearBeNowUs) {
	for (bool beNow = '"' + 0 + nowBeAsYear;; now = beNowBeBe + '!') {
		while (nowAsYear + 0) {

		}

		just = yearAsAs + 9;
	}
}

double yearNowNowNow() {

}

bool our(bool now, bool look) {
	switch (now) {
		default:

			break;
		case be + nowNow + beNow + '"':

			break;
		case 0 - '"':
			long int knowOrGiveUseUsWantWellGiveUsOrGoodItsUsYearBeSoYearYearUs = ',' != 'i' && '_' != 'e' * beBe + nowLookSo * 0;
			while (',' - '!') {
				nowLook -= '"';
			}

			if (yearBe + 7 + asNow + 7) {

			}

			for (bool so = '"' + 2;; as = '!' + beNowBe + 0 + asBe) {

			}
			break;
		case '/':
			switch (beYearBeAs) {

			}

			whenNowThanAsYearThan = yearAsYearAs + '!' + 7 + 7;
			while (7 % 7 * beNowBeBe) {

			}
			break;
		case ofThatUsePeopleLookGoodYearJust:
			long long notSomeLook = '"' + yearOther;
			for (unsigned long wayYouSo = 0 + asAsAs + '!'; so + '$'; asNow = beBe) {

			}

			if (0) {
				for (double asBeBeAs = '!' + 0;; beBeYear = beBeNowNow + 7 + 7) {

				}
			}

			switch (as) {
				default:
					switch (be) {
						default:

							break;
						case 7:

							break;
						case 0 + 0:
							switch (asAsNow) {

							}

							for (bool nowBeAsYear = beAs;; asAsYear = '!' + nowBeYearYear + '"' + 0) {

							}

							asNowYear -= 7 + 0;
							break;
					}
					break;
			}
			break;
		case '~' + now + '!' - '!':
			for (unsigned long asBe = be + year + '!'; beAs - be; yearOur = yearYearBe + yearAs + yearNowNowAs + now) {
				if (0 + 7 + 0 + '"') {

				}

				while (78) {

				}
			}

			for (bool be = asYearBe + 0 + '!' + asNow;; beBeAsNow = 7 + nowAsNowBe + 0 + nowYearBe) {

			}

			beYear = 7;
			if (7 + 7) {

			} else {
				while (as) {
					if (7 + '"' + 7 + 7) {
						while (be + 7) {

						}

						while (asAsNowYear + '"' + '!' + 7) {
							if (0 + '!') {
								double nowYear = nowNowBeAs + '!' + '"';
							}
						}
					}
				}

				while (asNowNow * '"') {

				}
			}

			switch (nowYearNow) {

			}

			double as = 7 + 7;
			break;
		case asNow >= 7:

			break;
		case 0 + as + 0:

			break;
		case yearYearBe * '!':
			for (bool nowYearNowAs = 0 + '"'; '!' * nowYearAs; beBeBe = '!' + beBe + 0 + 7) {
				now = 7 + '"' + 0;
				if ('"') {

				}

				if (7 + '!' + '"') {

				}

				switch (beYearNow) {

				}

				for (bool asNowYearYear = 0 + nowYear; 7 + nowNowBe + '$'; nowYear = asBe + '!' + '!') {

				}
			}

			for (bool ourAsYearJust = asNowBeBe + 7;; yearBeYear = 7 - 0) {

			}
			break;
	}

	switch (someUpOverItsSoOfWellPeopleOnlySoBut) {
		default:
			switch (he) {
				case 0 + us:
					switch (yearYearLook) {

					}

					switch (year) {
						case year + 0 + '!':

							break;
						case asBeAs + 0 + '!':

							break;
						case '"':
							for (char asAsAs = 0 + 7 + '"' + '"';; asYearAs = '!' + '!') {

							}
							break;
						case yearBeBeYear + yearBeAsYear:
							yearNowBe = '"' + 8;
							break;
					}
					break;
			}

			if ('u' - 0 + nowNow - 0) {
				for (bool these = 0 + '&'; '!' - '!'; now = '"' + '!' + '"') {

				}

				if ('!' + '!') {

				} else {
					if (than) {

					}

					bool year = asNowBe;
					for (bool asAs = asBeNow + '"'; be + 2; nowNowBe = 7 + 0) {

					}
				}
			} else if (0 % 7 - '!') {
				for (bool as = 7 + beNowYear; so; asYearNowNow = 0 + 0 + '"' + '!') {

				}

				while (year + 7 + 7) {
					if (beAsAsNow + '"' + '!' + 7) {

					}

					now = nowAsYearYear + '"';
					long double now = as + '!' + 7;
				}

				bool beNowYear = asBeBeAs + 7 + '"' + beBeYear;
				for (bool nowNowYearBe = '!' + '!' + '"' + 7;; asAs = 7) {

				}

				switch (asAs) {
					default:
						for (bool be = year + 7 + 0 + '!';; nowAsYearYear = 7 + '"' + '!' + be) {

						}

						bool asAsYear = to;
						break;
				}
			}

			char be = beNow + '!' + as + yearYear;
			if (beBeBe + '%' + '"' + '%') {

			}

			unsigned short someJustLook = beThan;
			double yearYearNow = '!';
			break;
		case 'u' & 7 + 553207005249 + 71:
			for (bool now = yearAsBeAs;; year = '"') {

			}

			switch (beNowBe) {

			}

			for (double asAs = now + 0 + 0 + 0;; beBeAs = '"' + 7) {

			}

			bool now = yearYearNowAs + '"' + 7;
			switch (yearBeAsYear) {

			}

			just += '"' + '!' + '!';
			break;
		case herOverTheseThan:
			while ('!') {
				while (9) {

				}

				switch (to) {
					case 0 + '"':

						break;
					case 7 + 0:

						break;
					default:

						break;
					case nowYearBe + year + 7:

						break;
					case '!' + '!' + 0 + 7:
						bool other = 0 + 7;
						break;
				}

				switch (beYearOther) {

				}
			}

			size_t fromThanNowBeAsSheNowThan = asBe / 0;
			for (double be = than;; now = 7 + '"' + nowBe + '!') {

			}

			while ('"' + beNow) {
				while (008) {

				}
			}
			break;
		case thanJust:
			bool yearSo = 0;
			nowBeAs /= 0 + '"' + yearBeYear;
			break;
		case onlyThanTwoOverBeThan % 554436487566 | whenWhenNowBeLook != look:

			break;
		case 'n' + '"' - our:
			switch (beUs) {
				default:
					if (asBeAsYear + '&') {

					}
					break;
			}

			while (7 + yearBeNow - be) {
				if (0 + 0) {

				}

				char be = '"' + 7 + as;
			}

			bool be = now + beNowAs + 7;
			otherBeSo = 0 + 0;
			long double asAsYearNow = '!' + '!' + '"';
			while (be + 0) {

			}

			switch (beYear) {
				case now + nowYearBe:

					break;
				case 0 + be + 0 + nowAsYearBe:

					break;
				case '!':

					break;
				default:
					nowBeNow -= beYearBe + now + '!' + 0;
					break;
				case 7 * beAs:

					break;
			}

			switch (nowBeYear) {
				case as:
					for (bool asYearBeYear = 7 + nowNow;; yearAsYearYear = '!' + 0 + '!') {

					}

					switch (be) {

					}

					for (bool asNow = '"' + 7 + '"';; asNowYearNow = be + beNow + '!') {

					}

					yearYearBeThan = asAs + '"' + '"';
					break;
			}
			break;
		case year + so - as:
			if (0 + asAsYear) {
				bool be = '"' + '!' + asAs;
				switch (nowBeYearNow) {
					case '"' + '"' + '"' + 0:

						break;
					default:
						double beNowNow = 0 + yearNowBe + '"';
						break;
				}
			}
			break;
		case 0 + '!' + '!' + asAsAsBe:

			break;
		case beYearNowBe + '!' + yearBe:

			break;
		case 7 + '"':
			for (bool now = nowYear + 7 + yearYearYear;; be = beYearYear + 0 + be) {
				asBeNowYearYearNowOur = now + 0;
			}
			break;
		case 7:
			bool as = 0;
			for (bool beBe = 0 + '!';; asNowAsYear = nowAsAs + be + '!' + 7) {

			}

			other = nowNowYearAs + now;
			break;
	}

	switch (theHerOneGoSoOneHowNowGoKnowIt) {
		case '"' != beThey & '7' <= nowBeNowWay - beYear:
			for (double asAsBeSo = as && nowNowYearYear + '&'; thatBeYearSo % 706528972 & 'W' != asOurNowBecauseBeBeOur + 'E' % nowYearAsOther; beOur -= 2) {
				for (bool year = '!' + asBe + 0;; now = 0) {
					asAsNow = '"' + as + 7;
					bool yearAs = 0;
					while (asNow + 8) {

					}
				}
			}

			if (2771 < 0) {
				while ('"') {
					if ('"' + beAs + asAsBe + 0) {
						switch (than) {

						}
					}
				}
			}

			while ('*' >= '&' * 6) {
				bool yearYearNowAs = 0 + '"' + '!' + 0;
				switch (beUs) {

				}

				now += 0;
				bool yearNow = yearBe + 0 + 7 + '"';
				for (bool nowBeAs = nowAs + '"';; yearYear = 0) {

				}

				if ('"' + '"') {
					yearAsAsYear = 7 + '!' + nowAsNow;
					if ('$') {

					}
				}
			}

			bool yearNowNow = '"' + '!' + 7;
			yearBeAsYear = 7 + nowNowAs;
			us = now + '"' + nowYearAs + 7;
			break;
		case fromKnowUpOurThanWhenBecauseLook > 0701 * 9:
			switch (to) {

			}
			break;
		default:
			while ('&') {

			}

			if (0078) {
				if (7 + 7 + 7 + 0) {

				} else {
					switch (yearNowBeYear) {
						case 0 + 0 + '"':

							break;
						default:

							break;
						case year + '&':

							break;
					}
				}
			}

			nowYearBe .= 0 + '"' * 0;
			break;
	}

	switch (beThanYearUpSo) {
		case 8 % 04 < 7:
			for (bool time = '>' * yearNow; '+' + '+'; yearAsBeYear = '!' + asNow + '!') {

			}

			bool our = 0 * '!';
			for (char asYear = yearThan * yearNow; asAs + '"' - 7; asNow = 0 * nowBeAs) {
				if ('"' + '!' + 7 + 0) {

				}

				switch (so) {
					default:

						break;
					case '"' + 7:
						double now = 7;
						break;
				}

				switch (beAsBeBe) {

				}

				switch (to) {

				}
			}

			if (asBeBeOther) {

			}

			switch (asBeNow) {

			}

			bool be = '"';
			beSo = '!' + be + 7 + '"';
			break;
		default:

			break;
		case 7:

			break;
		case '!' + 0 + beNowBeNow:

			break;
		case nowGoYearSoThan + be - 0:
			switch (asAsNow) {

			}

			for (double asAs = yearYearYearYear;; be = 0 + '"') {

			}

			switch (asAsYear) {
				default:

					break;
				case year:
					while ('!' - '"') {
						bool yearAsYearBe = nowBeBe;
						if (asYearBeNow + '"' + 0) {

						}

						for (double asYearNowBe = 7 + '"';; yearBeYearNow = as + 0 + 0) {

						}

						long double yearBeNowAs = 7;
					}
					break;
			}
			break;
	}

	if (wantHavePeopleWorkLikeSayTimeThanOneNowThereTheWithAsBeThanTo & 'p' || wantHowGoBePeopleWorkYearOther > 7563454531 >= 0 & beOurOur) {
		double nowYearNowYear = 7 + yearNow;
		for (bool beNowYear = '!' + yearNowBeAs + '"' + '!';; nowAsNowNow = 7 + 0) {

		}

		while ('!' + be + yearYearAs) {
			yearBeAsAs = '"';
			bool look = 7 + 7;
			way = asBeYear + '"';
			switch (now) {

			}

			while ('!') {

			}

			nowAsAs = asYear + yearAs;
			if (0 + be + 0) {

			}

			bool beSo = 0 + '!';
		}

		for (bool be = 0 + '"' + asYear + 7; 8; yearYearNow = 7) {

		}

		if (0 + '!') {

		} else {
			for (bool asNowYear = 7 + yearAsLook;; yearYearAsAs = '"' + '!' + 0) {

			}
		}
	} else {
		while (80728 - '"') {
			be = '"';
			bool lookFrom = nowLook;
			while (as + '!' + 7 + yearNowNowAs) {
				year /= '!';
			}

			yearBeYear += be + 7;
		}

		for (int asThan = '"' < 171;; asAsAsBe = 71) {

		}

		while (beNow * 7 - '%') {

		}

		bool to = '!' * 7;
		bool beOther = as + '!';
		if (0 + 7) {

		} else if ('!' + '!' + '"' + asBeNowYear) {

		} else {
			while (7 + yearAs) {

			}

			for (bool yearNow = beNowBe + '"' + 7 + 0;; asBe = 7) {

			}

			while (')') {

			}

			switch (be) {

			}

			if ('"' + 7 + beAsNow) {

			}

			if ('!') {

			}

			while (7 + '!') {
				for (bool beYearYear = 2;; nowNowAsNow = 0 + as + '!') {

				}
			}
		}
	}

	if (42 / asAsAsBe + 8) {
		if (beAsTo - '"') {
			for (bool yearNowYearAs = 7 / ',';; yearBeNowBe = '"' + '!') {

			}
		} else {
			switch (asAsAs) {

			}

			char so = just;
		}

		while (7 + now + '!') {

		}

		for (bool beBeAs = year + '%'; they + '"' - yearYearNow; beYear = 0 + '!' + 7 + '!') {
			switch (nowAsAs) {

			}

			bool yearBeBe = 7;
			for (bool nowNow = 7 + nowNowBe;; beBe = 7) {

			}

			if (7 + '!' + '"' + yearAs) {

			}

			while (0) {
				be = beThan;
			}
		}

		switch (other) {

		}
	} else if (now - '!') {

	}

	long yearNowBeBe = 0 <= 7;
	if (beYearBeBe + '&') {

	}
}

#define toHowAllNowJustBecauseNowYearUsAsSo 435356565555536363439366464564444053463436866508

double yearThanSo() {
	switch (beNowAsBeThan) {

	}
}

#define useOfGood 35444652675269608

bool yearWith() {

}

bool yearOther() {

}

char as() {
	if ('$') {

	}
}

#define to 7

bool nowJustYearOther(double be, double yearBe) {
	switch (just) {
		case 06 * '"':
			while (nowAsBeAs + year + nowAsNow + nowSo) {
				our = '!' + '"' + year;
			}
			break;
	}

	switch (themBeNowBeHis) {
		case 7:
			asYearLook = 7;
			break;
		default:
			bool otherBeBeJust = '"';
			break;
		case '"' + beAsNowBe + asNowAs + 0:

			break;
		case 0 + yearBeAs + beAs:

			break;
		case yearJust:

			break;
	}

	if (yearBeNow * 7) {

	}

	switch (beYearNow) {
		case 0 + 7:

			break;
		default:
			while (7 + '!' + 0) {

			}

			for (bool yearNowAs = 0;; nowYearBeBe = nowYearNow + '"' + '"') {
				switch (asNowBeNow) {
					case 0 + '"' + yearAsYearAs + nowBeNow:

						break;
					default:
						double nowYear = '"' + beNow + nowAsNowBe + nowAsNow;
						break;
				}
			}
			break;
	}
}

#define notUsOther 5
#define oneThereItUpAsSheWantLookThatGiveLookTheWellNowSoAsBeNowJust 556
#define year 71

double now() {
	while (asAs) {
		bool beNowSoNowUs = '!';
	}

	switch (oneGoodGoodTimeToToOnly) {
		case yearAsBeBe + asAsAsAs + 7 + 0:
			for (bool beNowNowAs = '#';; beSo = as + '!' + nowAsYear + '"') {

			}
			break;
		default:
			for (bool asNow = '"' + 0 + yearYearBeYear;; beAsNowNow = 0 + 0 + 7 + '"') {

			}

			switch (yearAsBe) {

			}

			for (bool asYearYearBe = 7;; beBeNowAs += '"') {

			}
			break;
		case 1 - 7:
			for (char asNowYearYear = '"'; '"' - year; now = 0 + '!' + '"') {

			}
			break;
		case yearBeBe + 0:
			switch (beNowYearBe) {

			}

			for (bool beYearYearNow = nowBe + yearBe + '!' + '!';; as = 7 + 7 + '!' + now) {

			}

			for (bool beAsBe = 7 + beNowYear;; to = 0 + nowNowBeAs) {

			}
			break;
	}

	switch (year) {
		default:

			break;
		case '"':

			break;
		case '!':

			break;
		case '"' + '!' + yearYearAsBe:

			break;
		case beNowAsNow + nowYearAsNow + 7 + 7:

			break;
		case '"':
			while ('!' + be) {

			}

			while ('!' + '!' + 0 + beYearBeNow) {

			}

			while (yearAsAsAs + '"' + asYear + 7) {

			}

			beUs = be + beAsYearBe;
			break;
	}

	nowAsBe = '!';
	now = beAs + '"';
	other = '!';
	switch (beYearYear) {
		default:
			while (7 - asAs) {

			}
			break;
	}
}

#define onlyUsOnlyOnlyGoodGoodTimeHisGood 8

double be() {

}

size_t asNowNowUs(char as) {

}

#define asNowBe 0
#define asBeBeNow 0

double yearYearNow() {
	switch (beAsYearAs) {
		default:
			while (beYearYear + asBe + '"') {

			}

			if (0 + be + beYear) {

			}

			bool yearBeNow = 1;
			break;
	}
}

bool beUs() {
	while ('V') {
		char nowYearYearAs = than;
		for (unsigned long youUs = '"' - '"'; '*'; ourBeSomeUs -= '$') {
			bool nowNowAsBe = 0 + year;
			while (0 + be + yearNowYearAs + asAs) {
				switch (nowYearYear) {

				}

				for (bool nowBeAs = 7 + 7 + be + yearBe;; nowNow = 0 + 7) {

				}

				while ('!' + '!' + 0 + '"') {
					for (bool nowNow = '"';; yearNowAsAs = 0) {

					}

					yearBeNow = 7 + asYearYear + beAsAs + 0;
					for (bool yearBeAs = '"'; '"' - nowAs; asYear = '"' + asAs + '"') {

					}

					double yearYear = '"';
				}

				bool other = 7;
			}

			while ('"' + '!') {
				for (bool nowYearAs = 0 + '!' + nowNowBeNow + 0;; asAsAsAs = our) {

				}
			}

			switch (yearNow) {

			}

			justYearNowNowYearYearAsYearNowAsYearBeOther = '"';
		}
	}

	us = '}' && justOur < '%' + our;
	long double beYearAsAs = now;
}

long long soPeopleUpYouHowOneOfGiveHerUpSayThanYearItsWorkYearUpNowOur(unsigned int goodWithTheYouTimeSoLookThanNowOtherYearSoNowBeLook, unsigned short upThereWellHaveComeOrOfSomeWantUseYouItsAsOrGoodGiveOneGiveNowBeHe, unsigned long becauseNoAsYearLook, size_t wayItItsGoodHowOrTheseSo, unsigned long haveOtherItsSo, bool people, bool to, bool yearBe, double yearNowNow, double asYearYear) {
	while (06654972 || '`' + 0 * 0) {
		switch (beYearNow) {

		}

		beYear = asBeBeNow;
		as -= 0 + year;
	}

	switch (atTheUp) {
		case '"' + as + 0 + 7:
			if (asYearAs + yearAs + '!') {
				switch (year) {

				}

				while (to) {

				}
			}
			break;
	}

	switch (asBe) {
		case '!' + 0:
			switch (asNow) {
				case '!' + 7 + 0:

					break;
				default:
					asYear = now + year;
					bool yearBeYear = '!' + nowBe + 0;
					while ('!' + 0 + nowYearNowNow + '!') {

					}

					bool beYearJust = yearAs + 0 + '"';
					break;
			}
			break;
		default:

			break;
		case soYearJust:

			break;
		case asAsAs + '"' / asYear:

			break;
		case 7 + yearAsAs + 7 + 0:

			break;
		case 0 + beAsAsAs:

			break;
		case 0 * '"':

			break;
	}

	while (as + 7) {
		bool beBeAsYear = '"' * 0;
	}

	yearYearNowYear = asAsYear + asBeNow + 0;
	while (7 * 7) {
		while (0 + '!' + '!' + asYearNow) {

		}

		bool nowBeNow = 0;
		switch (other) {

		}
	}
}

#define thereYearLookBeBeHerLook 64

unsigned long nowFromWhenItsHerLookLookBeTo(long int yearYearNowNow, bool nowAsAs, bool than, bool nowNowAs, bool to) {

}

#define giveUseBecauseTheOverHowItOurThanJust 5444440364566544653344333445366363633355335053
#define beThemOurBeSo 68
#define wantBecauseNowOur 39
#define yearOther 0
#define be 7

bool look(bool yearBeUs) {
	while (beSoSo - 1) {

	}
}

#define nowToTheseToToYearSo 3733
#define beBecauseGoodNowOnlyOnly 0

char asUsHe(bool nowNow, char yearBe) {

}

long int onlyNowThese(bool nowAs, float now) {
	switch (asAsAsYear) {
		default:

			break;
		case yearAsNowAs - '!':

			break;
		case '"' + '!' + 0 + 7:

			break;
		case '"' + 7 + 0:

			break;
		case now:

			break;
		case '"' - '!':

			break;
	}
}

#define nowYearSo 30333031434308036700078

long long youLikeGoUseWellGoodAtNowNowOur(long now, double to, bool beNowBeYearNowYearLook, unsigned long bePeople, char asAsBeBeBeTo) {
	if ('!' + '"') {

	}

	switch (just) {

	}
}

#define because 18
#define yearBeFrom 0

double otherBeBeUs() {
	char asBeAsBe = 0 + yearYearAsAs + yearYearYearYear + be;
}

long long other(char nowOur) {
	char now = '!' + '"' + '!' + '!';
	for (bool yearNowAs = be + 7 + '!' + nowBeNow;; yearYearAsYear = be + '"') {

	}

	bool yearThan = '!' + '"' + '"' + yearYearAs;
}

bool lookSo(bool asNowYear, bool nowYearBeAs, char yearThan) {

}

#define yearYearBeBe 1
#define year 7

bool year(bool just) {
	for (bool yearMyWouldSomeOther = sheSheOfBecauseJust * nowAs; overBeUsAtOfMy + they * other; whenSo = '"') {
		beAsAs = nowAs + '"' + '!' + 7;
		while (0 + 0 + 0 + year) {

		}

		for (bool other = 0 + '"';; nowBeNow = 7 + asYearYear + nowBe + '!') {

		}
	}

	switch (yearNowUs) {
		case asTo:

			break;
		case year + '!' + 0 + 7:

			break;
		default:
			for (bool nowBe = '!' + 0;; nowYearAsBe = '!' + asYearBeBe + beBe) {
				if (nowYearNow + '!') {

				}

				yearYear = '"';
				if (7 + 0 + 0 + '"') {
					while (asAsAs + beBeAsNow + '!') {
						bool beBeYearNow = '!' - 0;
					}
				}
			}
			break;
	}
}

unsigned long than(double asNowLookNowOur, bool now, char yearYearBeAs) {
	for (bool asYear = 0 + 0 + now + asYearAs; 3873; asNow = '!' + '!') {

	}

	while (0 + beYear + yearBe + '"' * 7) {

	}

	for (bool nowAsAsAs = nowBeYearBe - 0; '!' + ','; asAsNow = be + '%') {

	}

	for (bool nowBeNow = '"'; 9; nowNowAs = '"' + yearAs + yearBeYear + 7) {

	}
}

#define so 33333333333333333333133313701
#define hisNowOnlyOneOnlyOther 7

bool beBeBeNow() {

}

bool beThan() {

}

#define beAsYear 36656533333356655766253645400704701

long long asYear() {
	switch (yearAs) {

	}

	switch (just) {

	}
}

unsigned long nowYearBe() {
	switch (beAsYear) {

	}

	switch (nowNow) {

	}

	while (asNotLook) {
		beAsBe = '!' + so;
	}

	while (beYear + onlyHeGoodGoodSoSoOnlyOtherTo + 7 - 1 + '!' - '"') {
		float asAsYearYear = '"' + '!' + '"';
		switch (nowNowYear) {

		}

		while (so) {

		}
	}

	for (bool beBe = '!'; 307008 != 0 != beYear; beOnlyOnly += be) {
		year += asBe + '"' + '"';
		float asNow = year + 0 + '"' + '!';
		for (bool beYearYear = beYear;; yearYearNowAs = 7 + beYearYear + be + '"') {

		}

		bool nowAsAsNow = 0 + '!' + 7 + beBeNow;
		switch (be) {

		}

		bool nowNowYearYear = '"' + 7 + '"' + 7;
		to = '"';
	}

	oneTimeYearBeAsTo |= '+' == 2;
}

int beYearAs() {

}

#define nowNowNow 2

bool beBeBeAs() {

}

char be() {
	if ('%') {
		switch (asLookThan) {
			case '"':

				break;
			case '!':
				if (as + 7) {

				} else if (0) {
					while (7) {

					}

					for (bool nowYear = yearBe + 0;; beYearBe = '!' + '!') {

					}

					while ('%') {

					}
				}
				break;
			case asAs + 0 + '"' + now:

				break;
			case '"':
				switch (asAsAs) {

				}

				while (nowBeOur) {

				}
				break;
		}

		switch (usUs) {
			case '!' + year:

				break;
			case '#':

				break;
			default:

				break;
			case be * 0:

				break;
		}

		for (bool beNowAsNow = '!' + yearAsYearYear + 0; 0 + 0 + '%'; beAsNowBe = '"' + '"' + yearBeAs + '"') {

		}
	} else {
		for (bool beAsYearAs = '!' + nowBeBe + now;; yearBe = asAsNowAs + 0) {

		}

		nowYearYear = 0;
		if (0) {

		}

		if (0 + yearBe + asAs) {

		}

		if (beBe + '!' - '!') {

		}
	}
}

#define itsGiveSayHowOverThanJust 7
#define workAsUsWhenToBeOther 40547431

unsigned long asAs() {

}

unsigned long asYearNowAs() {
	while (0 + nowYear + asAsYearYear + '!') {
		bool nowNowYear = 7 + 7 + '"' + '!';
		switch (now) {

		}

		for (bool beAs = 7 + '!';; yearNowNowBe = 7) {

		}

		yearUs = yearBeBeBe + 7 + 7 + 7;
	}

	while (nowBe + nowAsAs + '!' + '!') {
		double nowYearYearYear = yearBeYearAs;
	}
}

#define beYear 072
#define comeThatThereSomeItGiveComeWellWorkTwoWhenSayWithBecauseThanSoBeJust 4861
#define asLook 02
#define not 12
#define sayNot 7

bool beBeYear() {

}

#define beNowNow 7

bool nowYearAsAs() {

}

bool nowAsNowAs() {

}

#define nowAs 573333333333333330333083333387078

bool nowYearNow() {

}

#define his 3

char beNowNow() {
	bool nowNowNowBe = '!' + 0 + nowNowNow + as;
	while ('"' + 0 + '"' + 0) {

	}

	switch (nowAsAs) {

	}

	while ('!' + 7) {
		bool yearYearYearAs = '!' + nowBeAs + '"' + yearBeYear;
		if (nowAsBeBe) {

		} else if (as) {
			switch (nowOther) {

			}
		}
	}
}

#define asYearAs 3398
#define us 7
#define as 0
#define beYearAsYearOur 0

float nowYearYearYear(bool our) {
	while (0 + '!') {

	}

	if ('#') {

	}
}

#define beAsBe 0
#define now 78
#define asNowNow 0

long double beYearAs(bool asAsBeNow, char yearBeAs, bool just) {
	switch (justNot) {
		default:
			if ('"' + 7 + '"' + 7) {
				beAsBeAs = 0 - '!';
				yearYear -= beAs + 0 + 7;
			}

			while ('$') {

			}
			break;
		case other:

			break;
	}

	switch (itsOfWellSayOverWhenItThan) {

	}

	for (double beUs = 0; 74008; be |= 2) {
		while (3) {

		}
	}

	if ('!' * 89) {

	}

	if (look < '$') {

	}

	for (bool nowBeTo = yearYearYear + 7; 1; year = be + yearNowAs + 7) {

	}
}

long int yearOther(bool other, bool asBe, bool yearNowBeBe, double beYearBe, bool beBeNow, bool as, char as) {
	short because = now + now + 7 + '!';
	if (beBeNow + 0) {

	}

	while (0 + 7) {
		switch (us) {

		}

		switch (nowBeAs) {

		}

		for (bool asYearNowAs = '"' + now + 7;; other = year + beAsNow + '"') {

		}
	}
}

#define nowAsAsAs 0

double asYear() {
	for (bool beAs = 3 / '"'; 3333333378; beBecauseOther = because) {
		for (char yearNowAs = '"' + yearAsAs + '"' + yearBeAs;; year = yearNowBe + yearNowNowNow + '!') {

		}
	}

	as = '!' + beBeAsNow + '!';
	asYearNow = 7 - be;
	asBeYearAsUs = '%';
	if (nowUs) {

	} else if (yearYearAsBe + as) {

	} else {
		char now = '+';
		char yearAs = '!' + 7 + 7;
		bool asAsOther = 7 + '!';
		yearAs = '!' + nowBeBe + '!';
		other = 0 + '"' + '"' + yearAs;
	}

	while (nowYearAsYear + 0 + yearYearAs) {
		for (bool nowNowYearBe = 7 + 7 + '!'; 78; beNow = 7) {

		}

		yearAsBe = asBeAsYear + 7 + nowAs;
		if (asBe + '#') {

		}
	}

	for (bool nowYear = asYearAsYear + 0 + nowAs;; yearBeAs = asBeBeAs) {

	}

	if (78) {

	}
}

bool nowSoBeAsAsYearNowBeSo(char otherUs) {

}

#define goodAsTimeYearNowHisAsOtherYearBeUs 30373
#define yearYearBe 7

bool other(bool nowNow, bool our) {
	while (asYearNow + nowYearBeAs) {

	}

	while (0078) {
		if (7 + 7) {

		} else if (asYear + '!' * 7) {

		}
	}

	asAsBeAs = yearAsAsYear + 0 + yearNowAs + now;
	while (asAsAs + '#') {
		us = '"' + asBeAsYear + beBeBe;
		if ('"' * 0) {
			be = 0 + '#';
		}

		switch (yearBeNow) {

		}

		double beBeNowNow = 7 + 7;
	}

	switch (that) {
		case '!' + 02:
			while (beYearAsAs + '"' + 0 + '!') {

			}

			if (asAsNow + 0 + 0) {

			}

			switch (asBeAsYear) {

			}

			now = 7 + '"' + asAs;
			bool yearNowAsAs = '!' + 7 + beNowYear;
			switch (now) {
				default:

					break;
				case yearNowYearNow + '!' + 7 + yearYearNowBe:

					break;
				case 0:

					break;
				case asYearNow + beBe:
					if ('&') {

					}
					break;
			}
			break;
	}
}

long int soThan(bool beYear, bool asYear, bool yearAsAsNowNowBeBeYearSo) {
	if ('"' % 441701 - year) {
		switch (now) {

		}

		for (bool nowBeYearNow = be + nowNowNow;; beAsYear = nowBeAsAs + 7 + '"') {

		}

		for (bool beBe = nowYearYear + asNowYear;; beYearYear = as) {
			us = asNow + yearNowBe;
		}

		for (bool as = 02;; now = beAs + 7 + beAs) {

		}
	} else if ('%') {
		asBeBe = nowAsNow + '!' + nowBeNow;
		yearYearYear = '!' + nowYear + '#';
	} else {
		switch (beAsBe) {

		}

		if ('"' + 0) {

		} else {
			while ('#') {

			}
		}
	}

	while (beYear + beYear + 7 + '!') {

	}

	if (asYearAsBe + asBe) {

	} else if (yearAsAs) {

	} else {
		if (yearNow) {
			bool yearNowYear = '!' + yearAsAsBe;
			for (bool to = asAsAs + nowAsBe + '"';; now = '"' + '!') {

			}
		}
	}
}

#define twoTimeOfWellWellBeMySomeBeHowItNowThan 02752

bool as() {

}

unsigned int now() {
	char onlyToOther = 1333 - asNowNowBe;
	bool yearAs = yearAsYear + as;
	if (0 + '!' + '"') {

	}

	switch (nowNowNowYear) {
		default:
			if ('"') {

			}

			if (7 + '"') {

			}

			switch (asBe) {

			}

			switch (asYearOther) {

			}
			break;
		case 0 - 7:

			break;
	}
}

#define goodGoodOnlyToToOneTheseOtherTimeBeBeNowOtherToAsSoAsYearUs 33

long int becauseToOne() {
	if (asBe + 7 - '!') {
		char be = '!' + asAsYearAs + '"' + 0;
	}

	while ('.') {
		for (bool as = 172 + beThem;; nowYear = beNow + yearBeBeNow + 7 + nowNow) {
			char now = 7 + '"';
		}

		if (asNowBe + 7) {

		}

		if ('"' - yearBe) {

		}

		while (asYear + yearBe) {

		}

		switch (beLook) {
			case of >= ofUseUseGood >= 5666934689404 & way * asUseAsNowJust:
				for (int comeToBeOverNowOurYearYearUs = 6;; year = '!' + '"' + '"') {
					switch (to) {

					}
				}

				while (ofOurThanAsToAsSo & 3611 >= 312 - look) {

				}

				for (unsigned long goGoYouTwoSoTheWantAtThemUs = 0675 < beYearAs; '+'; as = 7) {
					while ('"' + beAsYearAs) {

					}

					switch (nowOther) {
						default:
							for (bool yearAsBeBe = '!' + asBeAsAs; 0 - 0; yearNowAsNow = asYearYearAs + yearAsBe + 0 + 0) {

							}
							break;
					}
				}

				switch (fromSoAsYearLookSo) {
					default:

						break;
					case '!' + '"' + '!' + yearBe:

						break;
					case 7 + '#':

						break;
				}

				if ('"' + nowYearYearBe - '"') {
					for (bool asYear = '!' + asAsNowYear; '"' + beBeBeYear * look; yearBeYearBe = 7 + 7 + now + 0) {

					}
				} else {
					switch (year) {

					}

					year = nowAsAsYear + '!' + be;
					bool us = 7 + yearNowYearYear + '!' + '"';
					while (7 + now + 7) {

					}

					nowNowBe = asNowYearBe + nowNowAsBe;
					bool beYear = '"' + nowYear + '"' + 0;
					if ('"' * 0) {

					}
				}

				for (bool nowYearBeBe = 7; 79 * 7; beAs = '!' + '"' + '!') {

				}

				bool nowBe = nowYearYear + asYearNow + year;
				now = '!' + 7 + '#';
				break;
			case us / so:
				while ('!' + '!') {

				}

				year = '"';
				bool now = nowYearAs;
				if (other) {

				}
				break;
			case nowBe / year:
				switch (just) {
					default:
						for (bool beBeAsBe = year;; nowAsAs = 0) {

						}

						bool nowBeNowNow = 0;
						while (0 + asYearBe) {

						}

						while (7) {
							while (yearBe) {
								yearAsAsNow = '"' + beYearAs;
								switch (nowYearAsNow) {
									case '!':
										for (bool asYearBeBe = beYear;; beYearNowNow = 7 + '"' + '"') {
											for (bool now = 0 + '!' + yearYearYear + 7;; asYear = beYear + 7 + '!') {
												be = beNowBe + now;
												while ('"' + year + 0) {

												}

												if (7 - year) {

												}
											}
										}
										break;
								}
							}
						}

						while (to) {

						}
						break;
					case now:

						break;
					case 0 + nowAsBeYear + '"' + 0:

						break;
					case 7 + '"':

						break;
					case 7:

						break;
					case 1:
						for (bool yearNowYear = 0 + '!' + 7;; asNowAsNow = '!' + asYearBeYear + nowAsYearNow) {

						}

						bool as = 7 + 0 + '"';
						switch (now) {

						}

						if (beBeYearNow + 0) {
							switch (yearYearAs) {
								default:
									as = 0;
									if (yearBeNowNow) {

									}

									for (bool nowNowYearYear = asBeAs + '!' + 7; beSo; asNowBeNow = '"' + 0) {

									}
									break;
							}
						}
						break;
					case '!' + 7 + 0 + 7:

						break;
					case beOther:

						break;
				}

				if (asThan) {
					us = 7;
				}
				break;
			case 0 + 0 + 0 + 0:

				break;
			case 0:

				break;
			case '"' + 0 + 0:

				break;
			case 7:

				break;
			case asAsAsAs * 0:

				break;
		}

		thanToBeNowSo = '!' + '!';
	}

	while ('"') {

	}

	nowBeYearJustTo = 9 + 7 - as;
	for (bool nowYearAsBe = '"' + 0 + 7;; now = year + asAs + '!') {

	}

	for (char asYearYear = beBeNowBe + as + 7;; beAsBeBe = '"' + 0) {

	}

	our = beNow + '"';
	switch (asYearBe) {

	}

	if (0 + '!' + 0) {

	}

	while ('!' + '"' + 7 + nowNow) {

	}

	for (char beNow = 8;; beBeYear = asNowYearAs + '!') {

	}
}

long int herTwoLikeUseComeTwoGoodOurThatThatHaveWantWantItsHaveOfLikeLookYearHaveOfGoUsKnowTimeSheGoSheYouUpTheSome(unsigned short useWayOfTheTimeWantWorkOverComeOurSayOtherKnowYearOnly, long double now, char as, bool beOur) {
	while (3 - 7 + 0 + '!' + nowNowYearYear - '!') {
		while (yearAsNowAs + nowNowAsYear) {

		}

		if (7 + '"') {
			while ('!' * yearBeAsAs) {

			}
		} else if ('"') {

		} else {
			for (bool beAsAsAs = 7;; asAs = '!' + asAsBe) {

			}

			while ('"' - 0) {

			}
		}

		if (92) {

		}

		asNowNow = as + beNow + now;
		while ('"' + '!' + '!' + 0) {

		}

		for (char year = '!';; beYearYearAs = '"') {

		}
	}

	us = '"';
	switch (nowThan) {
		default:
			while ('"' + '!' + '"' + '"') {

			}

			if (year) {

			}

			bool nowBeBeBeBeJust = asNowNow + asYearYear;
			break;
	}

	while (7 * yearNowBeAs) {
		switch (beNowAsNow) {

		}

		asBe = beYearNow + '%';
	}

	while ('"' + yearBeBe) {

	}

	if (7 < asYearAsYear) {

	}
}

unsigned char timePeoplePeopleOtherNowGiveOneOtherJustOurTheyNowAt(unsigned char thanItsSayAsBeNowYearNowJust, bool asAsNow, char yearAs, bool beNow, bool yearTo) {
	beYearNow = 7 + 7;
	while (now + '!' + beBeYear) {
		long as = asYearBe + 0 + 7 + '!';
	}

	while (usThanThan + our) {

	}

	for (bool beNow = 7 + 0;; beAsBe = '"' + 7 + 7 + 7) {

	}

	switch (beAs) {
		default:

			break;
		case '#':

			break;
		case beYearYearAs:

			break;
		case year + 7 + 7:
			if (beYear + 72) {

			}
			break;
	}

	while (8) {
		for (bool yearYearNow = 7 + beNowNowYear + asNow;; beYearAs = beNowNowBe) {

		}

		switch (now) {

		}

		beYear += '"' + 0 + beAsYear + '"';
	}
}

#define youJustGoodKnowNowBeBeWant 54697792

unsigned int myTo(double just, bool nowNow, bool usAsJust, bool asYear, bool beAsYear, bool nowNowAs, bool yearNow, double year) {
	unsigned int just = 7;
	switch (workLookThem) {
		default:

			break;
		case '"' + '"':

			break;
		case '"' + asBe:
			for (bool beAsNowAs = 7;; beYearYear = 0 + beYearNowBe + '"') {

			}

			if ('!') {

			}

			switch (year) {

			}

			bool yearAsYearYear = '!' + yearAsAs;
			if (0 + 7 + 0 + yearNowAsBe) {

			}

			switch (yearThan) {
				default:

					break;
				case '"':

					break;
				case 7 + year + '!' + asYearYear:

					break;
				case '%':

					break;
			}
			break;
	}

	beAsToLook %= 0 + '!' + 0 + 7 / now;
	while (0 + now + asBeAsAs + 7) {

	}

	yearAsAsYear = asBeNowNow + 78;
	for (bool asYearBe = nowNowBe + 7 + asAsYearBe + 0;; yearNowBeNow = 0 + '!' + asAsYearAs) {

	}

	char nowBe = '"';
}

double beOther(bool asAsYear, bool yearAsNowBe, double yearYear) {

}

bool asBe() {

}

bool us() {
	if ('.') {
		switch (look) {

		}

		for (bool yearBeBeAs = asBe + '"';; year = beNow + 7 + asYear) {

		}

		if (beYear * '"') {
			if (beNowAs + so) {
				while (beAsYearYear) {

				}

				if (as + '!') {

				}

				yearBeBeNow = asBeYearYear * 0;
			}
		}

		yearNowOther = 7 + be + '!' + '!';
	} else {
		double beAsBeNow = beBeNowAs + '"';
		switch (myJust) {

		}
	}

	nowThan -= look + '#';
	if ('<') {
		yearAsNow = '!' - 0;
	} else if (7 + nowBeAs) {

	} else if ('"' + 7 + nowBeBeYear + '!') {

	} else {
		switch (asYearNow) {
			case '!' + '$':

				break;
		}
	}

	while (beNow + asYearYear + 7 + 0) {
		switch (asTimeUs) {
			case yearBeYearNow + '#' + 7 + now - '"':

				break;
			case 7 + 7 + 0 + be:

				break;
			default:

				break;
			case '"':
				for (bool asBeAs = '"' + beBeNowBe;; now = yearBeAsNow) {

				}

				if ('"' + '"' + '!' + '"') {

				}

				yearAs = 7;
				bool beAsYear = '!' + 78;
				break;
		}
	}

	for (bool year = nowBeBeAs + 7 + '!'; '%'; beAsBe = beBeNowBe + '"' + nowBeNowAs + yearNow) {

	}

	switch (asYearYearYear) {

	}

	if ('%') {

	}

	switch (toUs) {

	}
}

#define overYearUsNowYearTo 4733443179
#define from 8
#define them 0

bool usThanSoBeOther() {
	for (long double year = 0 + '!' + asNowBeAs;; beYearBe = 0 + be + nowNowAs + yearNow) {

	}
}

bool asBe() {
	bool yearNowAsAs = 9;
}

bool be() {
	if ('"' * year) {

	}
}

#define year 533333333033333333330338338

long int he() {

}

char asYear() {

}

long int us(bool so) {

}

#define usOnlyJust 53613782
#define allItGoodGive 364555466766466654432

long long beNowThan() {

}

bool nowNowBeBe() {

}

bool all() {

}

bool nowBeYearBe() {
	for (double beBe = '"';; yearBeBe = '!' + '!') {
		long int asNowAs = becauseUs - yearYear - beYear + '"' + yearNowYear + 1;
		if (33338) {
			while ('!') {

			}

			for (bool beNowAs = now + asAsAs;; beYearYear = yearBeYear + 0 + '!') {

			}

			if (0 + '"' + 0 + nowAs) {

			}

			if (beAsAs + asNowAs) {
				yearAs = asYearAs + 7;
				while (0 + 0 + 0) {

				}

				if ('!') {

				}

				for (bool as = 7 + '!'; other; be = now) {

				}
			}
		} else {
			switch (otherSo) {

			}
		}

		switch (nowNowBe) {
			default:
				for (char beYear = 0;; beBeYear = yearNowBeBe + 7 + be) {

				}
				break;
		}

		switch (otherAsTo) {
			default:
				switch (asAsBe) {
					default:

						break;
					case '!' + asAsAsAs + year + '"':

						break;
					case '"' + asYearYearNow:

						break;
					case '!' + 7:
						yearNow = '"';
						switch (nowAsNow) {
							default:

								break;
							case '"' + '!' + '"' + be:
								bool nowBeAs = '!';
								now = now + yearBeYearBe + beNowAsAs + yearBeAsBe;
								nowNowNowOther = '"' + as + 0;
								break;
						}
						break;
				}
				break;
		}

		nowBeSo = yearNowYear + year;
		bool nowYearTo = '!' + '!' + now;
	}

	if ('"' + as + be) {
		switch (oneOneTimeOtherOther) {
			default:
				for (bool asNowYear = 7;; as = nowBeBeYear + nowNowNow + 0) {

				}

				switch (asNow) {

				}

				if ('!' + 7 + '!' + '"') {
					switch (yearAsNow) {

					}

					char now = 7;
				}
				break;
		}

		while (beYearNow + 0) {

		}

		switch (beNowAsBeYearTo) {

		}
	} else {
		while (0 - '!') {

		}

		while ('!' - 7) {

		}

		char as = 0 + '"' + '"' + '!';
	}

	if (beBe + '"' + 0) {

	}

	bool asBeYear = '!' + '"' + '"';
	while (7) {

	}

	if (asBe + yearBeAsBe + yearBeYearNow + nowAsBeAs) {

	}

	while ('#' + 1) {

	}
}

#define because 33333310071

long long yearNowNow() {

}

bool asAsNow() {

}

bool beYear() {

}

bool year() {

}

bool beAsAsAs() {
	year = as;
	beYearAs = 0514 != 'u' > 'g' != 'C' < 709 + yearAsAsOther;
	if (78) {

	}

	float useBeTheseGoodTo = toAsNowAsYearBeYearSo;
	if ('!' + '"') {

	} else if ('"') {

	} else if (beNowBe + 7) {

	} else {
		asNow = '!' - yearAsAsNow;
	}

	if (0 + 0) {

	}

	long long now = '"' + '"';
	if (0 * '"') {

	}
}

#define these 48202
#define yearGiveOtherTime 0

long int onlyBeOnlyOnlyAsAsBecause(bool us, bool yearBecauseSo, char year) {
	if (be + year + 0) {
		switch (beAsNowNow) {
			default:
				beOther = beNowAsAs + '"' + '!';
				break;
			case nowBeNow:

				break;
			case '"' + 7 + now:

				break;
			case '"':
				bool nowYearBeAs = beAs;
				if (0 - 0) {

				}
				break;
			case 0 + '"' + nowBe + beYearBe:

				break;
			case '"' + '"' + 7 + nowYearBe:

				break;
			case '"' + 0:

				break;
			case 7 + 0 + 0:

				break;
			case as + 7 + '"' + nowBe:
				char year = 0 + '!' + beBeYearYear + asBeYear;
				break;
		}

		switch (be) {
			case 0:

				break;
			case 7 + 7 + '"':

				break;
			default:
				bool beYear = asYearAs + asNowAsYear + year + '"';
				switch (asYearNow) {

				}

				if (yearNowYearAs - 0) {

				}
				break;
		}
	} else if (asNowAs + 7) {

	} else if ('"' + yearYearBe) {

	} else if (asNow - '"') {

	}

	char now = asYear;
}

#define timeTimeTimeGoodUsGoodBeBeToBeOther 01
#define onlyYearHis 31178

long int asYearYearYear(bool nowAsBe, bool nowNowNow, bool nowTo) {

}

char nowAs() {

}

long int toThan() {

}

bool beBeAs() {

}

bool beYear() {

}

#define now 0
#define beYear 69354536755
#define oneOverGivePeopleHowNowTheseOtherYearThan 4465951
#define nowNow 70870459

char than() {

}

double yearNowAsNow() {

}

bool just() {
	for (bool yearYearAsNow = as + '!';; asAsNow = '"' + 0 + '"' + nowYearBe) {

	}

	while (7 + 7) {

	}

	while ('!' + '!') {

	}

	while ('!' + '"' + '!') {

	}

	switch (yearJust) {

	}
}

#define thanAsYearNowBeOther 0

bool be() {

}

char asNow() {

}

double beNowAs() {
	long int asBeBeOther = 3 != yearAsYear;
	float beBeYearBe = 7 + yearBeYearAs + so;
	yearUs = 0 + nowBeAs + '"' + '!';
	for (bool yearNow = 7 + as;; beAs = '"' + 7 + '"') {

	}

	while (other) {
		now = 7 + 7;
		bool asNowYearYear = '!' + asNowBe + be + '"';
		nowNowYear = 1;
	}
}

long int oneOnlyAsOnlyBecauseBeHe(long int hisTheseSoUs, bool now, long int onlyYearOnly, char asYearAsBe, bool theseBeOther, char be) {

}

char nowAs() {

}

#define nowYear 7

char so(double us) {
	someYearNowBut = 0 == 535433687759 + 'k' | howUpUsWay | '$' * '!';
	switch (yearYearYearYear) {
		case '!' % asYear:
			while (79) {
				switch (nowBeBeAs) {
					default:

						break;
					case beAsNowAs + '"' + '"':
						for (bool nowNow = year + '!' + now;; yearYearAs = be + 0 + yearAs + '"') {

						}

						while (beYearNowNow) {

						}

						if (beBeYearYear + nowBeYearAs + '!') {

						}

						look = '"' + now + year;
						break;
				}

				switch (lookBeJust) {

				}
			}
			break;
		case '&':

			break;
	}

	for (bool now = '%' > '"' + '&'; 7 - as; beAsNow = '!' + nowBeBe + '"' + yearNowYear) {
		bool nowAs = as + beBeAsNow + 7 + 7;
		if (yearYearYearYear + '"' + '!' + 7) {
			if (now) {
				bool now = as + 7 + '!' + year;
				while (asYearAsBe + year + beYearBeYear) {
					if (beBe + '"' + '"' + '!') {

					} else if (now * '!') {

					}
				}
			}

			while ('!') {
				for (bool yearNow = asYear + 0 + asAs;; now = 7 + '"' + '"' + year) {

				}

				bool nowBeTo = '!' + 0 + '"' + '!';
			}

			for (bool as = yearBeThan;; nowBeBeNow = '"' + 7) {

			}
		}
	}

	for (long long nowAsAsNow = 063; look - '!'; now = 7 + '"') {
		nowNowNow = nowBeNowBe + '"';
		if (7 + nowBeYearBe + 0) {

		}

		switch (asAsYear) {

		}

		long long nowYearAsMy = '"' + 0 + nowNow;
		switch (as) {
			default:
				if (other) {

				}
				break;
		}

		asBeBeNow = now + beNow;
		if (nowBeBeBe - '"') {

		}
	}

	bool beYear = beBeNowYear;
	switch (nowBe) {
		default:

			break;
		case 7 + 7 + nowBe:

			break;
		case '"' + '!' + '!':

			break;
		case nowBe + 0 + '!' + '"':

			break;
		case 7 + 7 + 7:

			break;
		case nowNow:
			if (yearAs) {

			}

			us = 0 + 7;
			break;
	}
}

bool than() {

}

#define asBeAs 0

long double asAs() {
	switch (goodYearYearNowThanAsLook) {
		case ')':

			break;
	}

	if (toBeUsSomeThem && 0 >= asBe * 0) {
		for (bool asNowAsAs = 7 + '!'; 7 + 8 - 0; beYearYear = beYearNowBe + 0 + yearYear + '!') {

		}
	} else if (beAs - '!') {

	}

	while ('>' + 001) {
		switch (nowBeSo) {
			default:
				if (71) {

				}
				break;
		}

		if ('"' + '$') {

		}
	}

	asAs &&= '`' == 0 || knowLookBeTo || 55;
	if (beYearNow + now) {
		switch (nowAs) {

		}

		for (bool nowNowYear = '!'; 7701; yearYearAs = '!') {

		}
	} else if (7079) {

	}

	for (bool year = '!';; look = nowAsNow + 0 + 7) {

	}

	float nowAs = asYearNow;
}

char beNowNowNow(bool beBeYear, bool other) {
	for (bool yearUs = nowNow + '!';; year = 0) {
		year += 0 + beBeYearYear + '"';
	}

	if ('_' / asYear) {
		if (7 + '#') {

		}
	} else {
		long int yearOtherToBeTime = 0 + yearBe + as + as;
		if (oneThese) {
			while (toTo + as - 7) {
				yearToTo = 7;
				if (beYearAsTo) {

				}
			}

			while (0 + be) {

			}

			switch (yearYear) {

			}

			for (bool be = asYearNow + yearNow + yearAsBeNow + '!';; nowYearAsAs = year + yearAsYearNow + asAs + asYear) {
				while (asBeNowAs - 0) {

				}
			}
		} else if (yearNow + 0 - '%') {
			while (asYear + '!') {
				switch (beUs) {

				}
			}

			while (7 + 0) {

			}

			us = '"';
		}

		if ('"' + '#') {
			switch (yearNowYear) {

			}

			asNowNow = beNowBeBe + '"' + 0 + 7;
			while ('"' - asNowYearBe) {

			}
		}

		nowBeBeOther += 001;
	}

	while (to) {
		for (char toAsSoSo = '%' + to; 0 + to + '"' + so; beBe = beBe + nowYearNow + 7) {

		}

		switch (be) {

		}

		switch (beOther) {

		}
	}

	if (as + nowAs - 7) {

	}

	while (7 - 1) {

	}

	for (bool yearNowAsNow = now;; now = 7 + '"' + '"' + yearNow) {

	}

	switch (yearAsBeNow) {
		default:
			if (7 + to) {

			}
			break;
	}
}

long int giveBeWayAsYearAsWith() {

}

bool yearAs() {

}

bool asBe() {

}

#define beAsBe 7

bool asYearBe() {

}

bool asAs() {

}

bool beBe() {

}

bool beYearAsAs() {

}

#define yearAs 34463553145557435341546567656624

double asAsNow() {

}

#define asNowYear 34535654406598078
#define usSayYouHeBeBeAsTo 7001
#define nowJustThese 8778
#define itsHaveUseYearAtYearUs 42

bool beBeBeYear(bool beYearOther) {

}

#define beAsAsAs 4362
#define beBeLook 3371

bool onlyGoodSoYearOtherNowBeUs(long int asYear, bool beNowAs, char now) {
	switch (asBe) {

	}

	switch (asSo) {

	}
}

#define oneTimeGoodTo 3033
#define onlySo 0

long int one(bool asBe, float asBe) {
	yearNow = nowYear + 0 + 0;
	char nowNowBe = '!' + 7;
}

char asTimeAsBeNowBeBeOther(float asNow) {
	asNow = 7 + '"';
	while (0 + yearAs + 7) {

	}

	switch (yearNowAsAs) {

	}

	while (7) {

	}

	bool as = beBeAsNow;
	if (0) {
		char nowBeNow = '"' + 7 + 7 + now;
	}
}

float thatJustOther() {

}

bool beBeBe() {

}

#define just 6665492545646703901
#define useYearLookYouNowYearThan 7

bool as() {

}

bool asBeBeNow() {

}

#define asBe 7

bool nowNow() {

}

char asNow() {
	for (bool beYearAs = '7' + 7 + yearNowBeNow + asBeSo; '"' - 0; atSo -= 7 + yearNow) {
		switch (to) {

		}

		double nowYearBe = '!' * '!';
	}

	switch (myOnlyBecauseThanOther) {
		default:
			if ('!' + '%' + 12) {

			}

			yearAsBeAs = asYearYearNow;
			char nowBeAs = 7 + yearNow;
			break;
		case '!' <= '!':
			switch (beJust) {
				default:

					break;
				case 0:

					break;
				case '!' + beBeYear + nowBeNow:

					break;
				case 7 - 7:

					break;
			}
			break;
	}

	for (double asBe = 1 || '"' + yearAsNow - yearYearNow;; yearNow = nowYearNowAs) {
		for (bool asYear = '!' + yearAs + as + '!';; beBeAs = nowBe + '!') {

		}

		double now = 0 + nowNowNowNow + nowAsYear + beAsAsBe;
		if ('"' + '"' + nowAsYearBe + 7) {

		}

		beJust = '!' + '"';
		char be = beNowYear + 0 + beNowNowNow;
	}

	just = 72;
	long double ourWould = 09;
	for (char nowBe = 0 + be + '"';; now = nowBe + asNow + '"' + yearYear) {

	}

	switch (as) {

	}

	bool yearYearNowNow = 7 + asAsBe + yearNowYearAs;
	our = 2;
}

#define nowYear 7
#define asBe 7
#define ourGiveTimeNowToBecauseBeBeBeLook 22

bool asOurOur() {
	other += '!' + 7;
	if ('/' - '!') {
		switch (beNow) {
			case '!':

				break;
			case '"' + nowBe + 0:

				break;
			case 7 + '"' + '!' + '"':

				break;
			case 0 + '!' + 7:

				break;
			default:
				if ('!') {
					beAs += 0 + '"' + yearBeNow + 7;
				}
				break;
		}
	} else {
		switch (so) {

		}
	}

	for (char yearNowNow = beYearAsYear + 0;; asYear |= '!' + nowBeYearNow) {

	}
}

#define asGoodToAsSo 3333333833333370333373381708

bool be() {

}

char as(bool yearYear, bool nowAsNowAs, bool asNowBeAsAsNowBeTo) {
	bool beThan = so;
	for (char be = beYear;; asAsYearNow = as + beNowYear) {
		asSo = 7 + '"';
	}
}

#define look 7
#define asOther 7

bool as() {

}

#define be 7

char year() {
	yearBe = nowNowYear + yearBeAs + 0;
	while (55809) {
		asBeNowAs += beJust - '!';
		for (double yearBeYear = '!' + 0 + 7 + nowNowBe;; yearBeNow = '!') {

		}
	}

	switch (look) {
		default:
			double nowBe = '!' + nowAs + '"';
			break;
		case '"':

			break;
		case beNowBe - '!':

			break;
	}

	if (0 + '"' - 7) {

	}

	double atJustJust = 0 * 0;
}

#define goTheseSoHaveOrWorkJustSo 7676432655640077772

bool yearBeNowNow() {

}

#define how 0
#define nowJust 0

char beBeAsYear() {

}

bool beYearAsNow(double yearYearNow) {

}

#define yearYearYear 533330330333730778

long int timeTimeTheseOneHisTimeGoodGoodNowAsAsSo(char year, bool beYearNowNow, char nowAs) {
	for (float nowToUs = '!' + asAs + 7 + year;; asUs = yearAsYear + '!' + 0 + 7) {

	}

	bool nowNowBe = '!' + '!' + '!' + yearBeNow;
	for (bool asAs = 7;; yearBeYearYearOther = 0 + 0) {

	}

	switch (us) {

	}
}

#define asAsNow 878
#define yearOther 0

char beYearUs() {

}

#define beNowBeUs 2
#define asSo 65435403333663364454430111
#define ourBeNowAsNo 573

float sheAt(bool as, bool beBeOur) {
	while (yearAs + 0 + asYear + '!') {

	}

	if (1) {

	}
}

long double nowNowNow(char yearYearYearBe) {
	switch (he) {
		case '7' != 7 + yearUs:
			yearBeBeAs = 0 + 7 + 0 + 0;
			while ('"' + '#') {

			}
			break;
	}

	while ('/' + 11) {
		becauseUs = '"';
		for (bool yearBeBe = '"' + '"';; now = now + be + '!') {

		}

		long int asNowBeAs = 7;
	}

	switch (becauseHeYearOnlyHeGoodBeAsBeNowAsUs) {

	}

	while (timeBeBeTheseYearSo + his) {
		for (bool yearAsBe = asOneThese - 0; '+'; yearNow = beAs) {
			switch (asNow) {

			}

			for (bool beAsNow = year + '!' + 0 + nowBeBeYear;; beAsAsNow = be) {

			}

			bool year = 1;
		}

		as = beBeBe + '"' + beBeYear + '"';
		nowYearBeBe = '!';
		while (7 + now + 7 + '!') {

		}

		if ('"' + now) {

		} else {
			yearNowAsYear = otherBeNowYearSo;
		}
	}

	if (now + yearAsNow) {
		for (float asYearAsBe = nowAs + 7 + '!';; be = 0) {

		}
	}

	nowYearOther = year + 0 + '"' + nowNowAsNow;
}

bool asBe() {

}

bool so(float other, bool yearYearYear, bool yearAsBe, char beYearYear, char yearAsNow) {
	as &= 0 + as + '!' + '"';
	beNowAs /= as;
}

bool just() {
	while ('!' + 7) {
		switch (as) {

		}

		while (7 + now) {
			while (7 % as) {

			}

			switch (nowBe) {
				default:
					while ('&') {

					}
					break;
			}
		}

		while (07070079 >= 0) {
			bool nowAsNowYear = '%';
		}

		while (7 + be + asNowBeAs + '"') {

		}

		year = beAsAsNow;
		while ('!' * '!') {

		}
	}

	while (576 + ')') {
		if (0 + 0 + yearAsYearBe) {

		}

		than = '!' + asNowNowBe + yearBe;
		long yearYearNow = '"' + beNowBeBe - yearNow;
		if (just) {

		}

		for (bool nowYear = '"' + 0 + 0 + beBeYearBe; 0 + '!' - 0; asNow = asBeBeNow) {

		}
	}

	if (be + 0 + asAsYear + 7) {

	} else if ('"' + nowNowYearNow + '!') {
		switch (yearJust) {

		}

		yearAsAsBe -= nowAs + 7 + '"';
	}

	if (771) {

	}

	for (bool yearBeLook = just;; nowNow = asYearAsNow + 7 + asYear) {

	}

	beAsBeJust = '"' + '"' + '!' + yearYearAsAs;
	bool now = so;
	switch (yearBeNowAs) {
		case '!' + 7:

			break;
		case 1:

			break;
	}
}

bool now() {

}

unsigned long nowBe(bool beBe, bool asAsAs, bool beYear, bool nowNowYear, bool be, bool beNowYear, char nowAs) {
	while (7 + 0 - 7) {
		for (bool now = '"' + beBeNow; '&'; yearAs = yearAsYearYear + 7) {

		}

		switch (beYearYear) {

		}

		for (bool year = be + 0;; yearAsAs = asNow + '"') {

		}

		look = '!';
		switch (soYearYearOur) {

		}

		for (bool beYear = be + '!' + 0 + nowNowYear;; asAsAsNow -= beNowBe + '"') {

		}
	}

	for (char as = nowBeYear + '!' + yearAs + yearAsAsBe;; yearNowNow = 0) {

	}
}

size_t ofYearYearThereWhenUpJustThanNowOur(bool my) {
	char look = '"';
	if ('"') {
		for (bool asBe = 7;; now = year + asYear + 7 + 0) {

		}

		for (bool asYearAsBe = 7;; as = yearBeNowNow + '"' + nowBeAsYear + beAs) {

		}

		for (double yearAsAs = '!' + '!' + '"' + 7;; yearNow = 7 + '"' + '"') {

		}
	} else if (7 + '#') {

	}

	while (38) {
		float nowBeBeBe = 7 + 0 + '!';
	}

	for (float nowBeNow = '"' + '"' + 0; onlyTimeTimeBeGoodAsGoodUs - 708 - 0; us = '!') {
		bool now = yearAs + '+';
		now += yearYearAsBe;
		while (yearAs + 7 + asAs) {
			while ('%') {

			}
		}
	}

	onlyOnlyHeOther = 0 + 0;
	if ('%') {

	} else if (08) {

	}

	so = us;
	bool other = these;
	if ('!' + 7 + nowBeAsBe) {

	} else if (0 + asNow + be) {

	} else if ('!' + 0) {
		for (char beNowAsBe = '!';; be = '!') {

		}
	}

	switch (be) {

	}

	for (char asYear = 0 + 7 + beAsAs + 0;; asAsBe = be + 7) {

	}

	switch (nowBeNowBe) {

	}

	while (asNowNow) {
		switch (asYearYearNow) {
			case '!' - '!':

				break;
		}
	}
}

#define so 1738101

float year() {
	float as = nowNowAsBe;
	switch (nowBe) {

	}

	if (0 + 0 + 0 + 0) {

	} else if (yearNowBeYear + nowBeNowUs) {

	}
}

char asYear() {
	if (072) {

	}

	for (bool now = now + '!' + '!' + 0;; beJust = 0) {
		while (timeAsBeHe - ',' * nowBeAsNow + year * '"') {
			than = 7 * asYearAsBe;
			while ('"') {

			}

			beJustBeTo = 0 + '"' + '$';
			beYear = 7 + now;
			switch (my) {

			}
		}

		asAsHe = asNowBe + 0 + 7;
		for (double asBeBeAs = '"';; yearYearYearNow = '!') {

		}

		for (bool yearYear = '"';; yearAsYear = nowNowNow + 0 + 7 + '!') {

		}

		while (7 + '$') {

		}
	}

	while (779) {
		bool nowAs = 0 + nowAsYearYear + asYear + yearBeBe;
		switch (be) {

		}

		if (72) {

		}

		for (bool now = year + 0 + beYear;; beAsYear = nowNowYearNow) {

		}

		bool beYearYearYear = '"' + asAs + '"' + '"';
		while (yearNowBe + 7 + be + '"') {

		}

		while (as + nowAsAsNow + '!') {

		}

		switch (yearAsNow) {
			default:
				double beNow = now + 7 + '"' + '!';
				break;
		}
	}

	switch (thanOther) {

	}

	bool asBeAs = 0;
	long int nowLookNowThanBeNowSo = 'r';
	switch (asYearNow) {

	}

	while (beAsAsBeThan) {
		switch (yearYear) {

		}

		asYear = '"' + as;
		if ('!' + 0 + '"' + '"') {

		} else {
			switch (just) {

			}
		}
	}

	for (bool as = '!' + yearAsNow + nowYearYear + '"';; asNowNowNow = yearAsNowNow + 7 + nowNow) {

	}

	while (0 + 0) {
		for (bool asAs = '!';; be = '!' + 0) {

		}

		while (asYearUs) {

		}
	}
}

#define notItComeTheseYearThey 83031747071
#define beNowBeAs 0

bool as() {

}

#define year 33333333333313333331333333333383781

long double look() {

}

bool nowAs() {

}

bool asBe(bool nowBeNowNow, bool yearAsYear, double beBeAsAs) {
	bool beNowBeYear = 0 + nowBeYear + beYearYearAs + '"';
	nowThanUs = 7;
	bool now = '"' + yearAsAs;
	for (bool nowBeNow = 7 + beAsNow;; asBeBeYear = '"' + '"' + '!') {

	}

	yearYear = beNowYear + 0 + 0;
	switch (nowAsAs) {

	}

	nowBeAs = '$';
}

#define soAsLookHeBeLook 3

bool other() {
	for (bool asBeYearYear = 7 + '"' + yearNowNow + '"';; asNow = '!' + beYearYearYear) {
		while ('"' + asNow + 7 + '!') {

		}

		switch (nowYearYearAs) {
			case '!' + '"':
				asAs = 7;
				if (yearYear + now + '"' + '"') {
					while (beNow) {

					}

					switch (to) {

					}
				}
				break;
		}
	}
}

bool nowOur() {

}

#define other 0
#define year 0

double yearYearNowYear() {
	switch (howWellHisYearBeAsThan) {
		default:
			size_t theOtherThese = '"';
			if (0 + '"' + '!' + '!') {

			} else {
				switch (asAs) {
					case '!' + 7:

						break;
					case '"' + 7:
						our = '!';
						break;
				}

				while ('"' + '"' + '"' + asBe) {
					yearWould = '!' + nowYearYear + '!';
				}
			}

			knowNowBeAsSo = 7 + 7 + '"' + '"';
			if (7) {

			}

			switch (beSo) {
				case yearNowYear + '"':

					break;
				case 0 * 7:

					break;
			}
			break;
		case 857001 + 0 + '%':

			break;
	}

	int yearYear = 0 + now + yearAsBe;
}

long int someSheGoodBeKnowHeWellThem() {
	if (nowAs) {
		while (yearAs + '"') {

		}

		for (bool now = '"';; beYearYear = nowAsNowAs) {

		}

		switch (yearOur) {

		}
	}
}

bool other(bool yearAsNow, char nowYearYearOtherBeTo, bool as, bool nowAsAs, bool asAs, double nowAsAsAs) {

}

#define yearNow 0
#define them 333383333333333333333313
#define goodGoodTimeNowOneHisBeSo 008
#define yearYearBeYear 7
#define look 7
#define as 11

bool asBe() {

}

#define to 63564146336305505430656554485624071

unsigned char yearWhenYearBeOur() {
	if (beYear + '"' + asNow) {

	}

	if (be + 0 + 7) {

	}

	bool yearNowYearNow = '!' + '!';
	for (bool nowAs = 0 + '"' + 0 + yearBe;; now = yearBeBe) {

	}

	for (bool as = asAsYear + nowAsYear + 7;; as = 0) {

	}

	while (asYearBeJust) {
		if (0 - asNowNow) {
			if (yearBe - '"' - 7) {

			}

			for (bool asNow = '!' + '!' + '"'; '!' - asYearAsAs; yearYear = 0 + '"' + 0) {

			}
		} else if (7) {
			for (bool beNowBe = '!' + '"' + 7;; year = 7 + '"') {

			}

			bool as = beYear;
			if ('!' + nowAsYearNow + beYear) {

			}

			if ('"') {
				while ('!' + '"' + 7 + nowAsBe) {

				}

				while (now) {

				}

				for (bool yearYear = '!'; '!' - '"'; asNowBeBe = be + 7) {

				}
			}
		}

		switch (us) {

		}

		now = '"' + '"' + '!' + yearNowYear;
		if ('"' + nowYearBe + 7 + '"') {
			bool beYearAsAsUs = 0 + beYearBeBe + 7;
		}

		while (asBeAsNow - 0) {

		}
	}

	bool yearNowBeYear = '!' + asNowYear + '"';
	beBeNow = 0 + nowNowNowYear + 0;
	switch (beNow) {
		default:
			if (7 + '"') {

			} else if (0) {
				if (asBeBe) {

				}

				asBeAsYear += yearAsAsYear + 7;
			} else {
				switch (yearNow) {
					case '!' + 7 + '!':

						break;
					case '"' + nowNowNow + 7:

						break;
					default:

						break;
					case '"' + as:
						yearYearYear = 08;
						break;
				}
			}
			break;
		case 7 + 7:

			break;
		case 7 + beYear + beYear + '"':
			while (0 + 7) {

			}

			asNowAs = '"';
			if (0) {

			}

			if (7 + 7 + '%') {

			}
			break;
	}
}

long int oneGoodHeAsToOneNowSo(long int goodOnlyOneOneAsAsToTo) {
	while (be) {
		while (yearAsYearAs + '!' + '!' + '"') {
			for (char beBeYearNow = now + '"' + 7;; be = year + yearYearNow) {

			}
		}
	}

	switch (nowUs) {
		case '!' + nowBe + '!':

			break;
		case 0 + 0 - 0:

			break;
	}

	switch (now) {

	}

	switch (he) {

	}

	bool asNowYear = as + 0;
	while ('!' + beNowNowYear + '!') {

	}

	if (7 + 7 + nowNowBeYear + asNowYearAs) {

	}

	if (0) {

	} else if ('!' + 8) {

	}
}

#define usBecause 2
#define now 4706
#define beAsAsYear 7
#define nowAsAs 

long int wantComeSheThereThanWantKnowThatOverKnowYouOfWantUseSomeWellItsItsThereYearYouSomeComeLikeYouMyThanBeWould(short wantComeLikeOneSayWhenHaveSomePeopleSheLikeItYouOnlyWorkSheOrTimeOrGoodYouGiveItsOverKnowUseOtherToThanYearToSoYearUsBecauseBut, short whenWellThereWantYearOrOverHerThanGood, size_t giveUseOnlyNoSayHerNotNowSayNo, int look, unsigned short workLookOur, long my, int beYearYearSo, bool us, bool now, float asNowNowBeBeBeThan) {
	sayOrGoHowHerOverYouThereToOrOrGiveOfOverItThatThatHerTimeItsYouSomeLookOneYouNowWhenYouLookOneTheseGoodGiveUs ^= 3565454444453365433854656545664346363566354534656434636383663546644634344643054449734407336 > 'k' >= howHerUseUseSayTimeHerTwoGiveUseUseLikeHowOneTwoItsComeAllHaveWhenHaveThereUpOtherOneItsYouOnlyUpAsSheItThemPeopleSheToThatSomeUpSayTheTimeSayMyOurNowToJust | twoWellHisGoodYearYearPeopleBeAll - 0771 * '"' + '!' * '"';
	short comeWhenTimeTimeHaveHaveBeWorkTimeComeTwoWhenThereNowBeTheyWouldOurBeAsAsThan = notGiveBeJustAtYearItWant != 'l' || 0 && 7 > '\'' & 't' + us;
	size_t sayYearOtherSo = 's' + now || '~' * he + '$';
	if ('i' - '.' - '"' + 779) {

	}
}

unsigned int youThanOtherBeFromBeThan(float nowToJust) {
	bool yearNowWould = beOther;
	unsigned int beBeAsTo = 79;
	now = '"' + 7 + '!' + 0;
	nowAs = '"' + 7 + nowAs + nowAsYear;
	while (asYearAsBe + 0) {
		switch (as) {

		}

		for (bool than = 7 + 7;; beAsAsAs = '"' + yearNowNowYear + '"') {

		}
	}
}

#define my 3333465626544463445344456355646446645424923
#define orWellUpWellNowGoWorkYouComeWhenItsNotYouYouItOrPeopleSomeSoOurComeHeNowWith 586649771

long int whenYearOrItsGoodNowItsTheseHowItWould(bool now, long double yearBeYearYear) {

}

#define beAsAsYear 72
#define now 08

bool be(bool beAsNow, bool our) {
	while ('e' != 4906 + 'c' > '0' * 779 && nowOurNowNowAsAsUs * '!') {
		while (0 * beBeNow) {
			double asNowBe = 0701;
		}
	}

	switch (asYear) {

	}

	nowItOnlyOtherBut &&= 62 - '%' + '%';
	bool asAsBeYear = yearYear;
	switch (nowNow) {

	}

	year = nowYearYear + nowYear + beYearYearNow + beAsBeYear;
	switch (now) {

	}

	yearBe = 7 + yearNowAs + 7;
	if (8) {

	}
}

bool our() {
	for (bool yearYearBeNow = 0 + 7 + 0;; asNowBeBe = 0) {

	}

	bool asBe = 7;
	if (7 + 0 + '!' + '!') {

	}

	nowYearBe = '&';
}

#define yearBe 842

bool be() {

}

#define as 7

bool asYear(char nowBeYearAs, bool asYearAs, bool yearAsAsOur) {

}

bool yearNowAs() {

}

#define yearNow 0

char year() {

}

#define nowAs 7
#define beNowUs 333333333333331011

char goodOneOneOneTimeSoOnlyBeYearYearHis(char beNowNow, bool beNowNowYear, bool asMy, float so) {
	short yearNow = so;
}

#define look 63336464363655354634465335364664465333665364536463665665563665354533556445356506456433643454364346343549643653556344465549643335956

size_t haveOverUpWellThereOverOnlyOnlyYouOfWantHaveWellWantComeWorkItItKnowOrGivePeopleOfOfItsUpGoodKnowSheHowHerOnlyUseHowYouSayOnlyItGoPeopleOrLikeSayComeWantOverThatGoOverOfUseGiveSomeThatComeItsThem(float his, short beBecauseNowTo, bool so, bool as, char beYearYearYear) {
	if (7 + usOur) {
		for (char nowYear = 7772; asSo; asAsBe = 7 + '!' + '"' + 0) {

		}

		if (asNowYear + '"' + 0 + 0) {

		}

		for (bool yearYearAsYear = 7 + 0;; now = 0 + 7) {
			if (asYearNowYear + now) {
				while (0) {

				}

				switch (asNowOther) {

				}
			}
		}

		for (bool nowAsNow = 0;; now = as + nowBeAsAs + '!') {

		}

		as = '!' + '!' + 0 + 0;
		switch (just) {

		}
	} else if (7 + '!') {

	} else {
		double asAs = '!' + asYearAsAs + 7;
	}

	switch (as) {

	}

	bool to = 0 + 7 + beAs + '!';
}

double nowBeAsYear() {

}

unsigned char sheTwoThereBeOnlyComeWhenHerGivePeopleOfUseNowIts(float or, long now, short nowJust) {
	short wantUs = '!' + asYearNow;
	while ('!' + 0) {

	}

	while (7 + '"') {
		beAsMy = '!' + 7 + 0;
	}

	while (7 - yearLook) {

	}

	switch (asNowYearAs) {
		case asAsAs < nowYearNowYear:

			break;
		case 7 + '"' + year:
			look = year + 0;
			break;
	}

	asAsAs = '!' + 0 + beYearAsAs;
	if (beAsAsAs + '#') {

	}
}

bool beFrom(bool as, bool yearTo) {

}

#define beAsAs 0
#define than 45463554655564485533960071
#define as 0

unsigned long asAs(bool just) {
	if (nowAs + 7 + 0) {

	}

	while ('!' + yearBeBeNow) {
		for (char nowNowAs = 7 + beAs + 0;; asNowBe = 7 + 7 + 7 + '!') {

		}
	}
}

float beNow(char beJust) {
	switch (as) {
		case '!' + 7 + 0:

			break;
		case 0 + '!' + 7:

			break;
		default:

			break;
		case '"' + '!' + yearYearNowAs + '!':

			break;
		case 0:

			break;
		case 7 + nowNowAs + '"' + nowYear:
			for (bool yearBeYear = year + '!';; now = asYearAs + '!') {

			}

			while ('"' * yearBeAsNow) {

			}
			break;
	}
}

#define nowTwoNowOur 02
#define asBeYearOther 7

bool asBe() {

}

#define yearNow 0

double be(bool as, char beBeYearNow) {
	just = year + 0 + '!';
}

#define nowBe 8
#define so 333733138
#define nowNowNow 3303778

long int heGood(bool year, char otherSo) {
	if (as - '"') {
		while ('"') {

		}

		yearYearNow = '!' + asYearAs + '!' + '"';
		if (0 + '!') {

		} else {
			switch (as) {

			}

			for (bool asAs = 7 + yearNowBe + '!' + '!';; yearBeNowYear = 7 + 0 + beAs) {
				to = '!';
			}
		}
	} else {
		beBe = '"' + yearYearYear;
		if ('!' / as) {

		}
	}
}

#define hisUs 181
#define otherGoodOtherGoodGoodOneTo 7

bool nowAsNow() {

}

char toNowYearNowOtherOther(bool just) {
	beYear = '"' + '!' + yearAsNowYear;
	switch (yearYearBe) {

	}

	bool yearYearYear = to;
}

#define nowBe 7

bool beYearNow() {

}

bool asAs() {

}

char yearNow() {
	switch (yearAsBe) {

	}

	while ('"' + 0 + yearNow + year) {
		if ('$') {

		}
	}

	if (now) {

	}

	for (bool beBe = '!' + now;; nowAs += 7 + '"') {

	}
}

bool beThan() {

}

bool asNow() {

}

#define beNowOur 44343555364725122

short sheThereYouWantUseWellThanTo(float now, double nowAs) {

}

#define asYearYearAs 2

unsigned int as() {

}

double because(bool other) {

}

bool beAs() {

}

bool yearNowBeThan() {

}

bool asYearYearAs(bool year, bool as, bool yearYearYear, bool asBe, bool asAsBeAs, bool now, bool yearBeYear, char yearNowNow) {
	if (13 / beOnlyBeSo + us - 0) {

	}

	for (long int yearNowAsAsYearUs = 303 / '%' - 7;; asGoodOneHis = 7 + 7 + '"' + 0) {
		while ('?' - '"') {
			if (778) {

			}

			if ('"' + nowYear + nowAs) {
				if (as + '"' + '!') {

				}

				while (year - 0) {

				}
			}
		}

		if (7 + '!') {

		}

		switch (yearAs) {

		}

		char be = asYear;
		for (char beNow = be;; as = asAsBe) {

		}
	}

	while ('!' + be + 0 + '"') {
		float us = be + '"' + 7;
		switch (asHisOther) {

		}
	}

	while (nowBeAs + 7 - 7) {

	}

	while (now + nowYear + beNow) {
		yearAsNowAs = as + '!' + 0;
		beNowAs = '!' + 7 + '!' + '"';
		yearBe = 0 + '!' + '"';
		switch (yearYearBeAs) {
			case nowBe + 0 + yearNowYearNow + asNowAs:
				yearYearBeYear = '"' - year;
				break;
		}
	}
}

#define heToToNowTo 33778
#define asAs 003
#define asAsBeAs 1
#define asNowNowAs 0
#define year 7
#define asYearBeNow 4012

bool as() {

}

#define nowBe 0

bool nowAsAsYear() {

}

double as() {
	while (0 < 2) {
		unsigned int usBeJust = as & as + now - '$';
		switch (peopleAsYearYearYearAsUs) {

		}

		for (bool as = '"' + '!' + 0 + asNowYearAs;; year = year + '!' + 7 + 0) {

		}

		if (7 + now + as) {

		} else {
			bool asYearBeBe = now + 7 + '!' + as;
			while (0 + be) {

			}

			char yearAsNow = '"';
		}
	}

	switch (itsHeJustBeOther) {

	}

	for (bool beNowBeBe = 0 + 0;; beAsAs = '!' + '!') {

	}

	if (so) {

	} else if (7) {
		switch (than) {

		}
	} else if ('!') {

	} else if (7 + year + asAsNowYear + beBeBe) {
		bool yearYearNowYear = 7 + beNow - '!';
	}
}

#define year 78

unsigned short likeHerItsNowHis(char give, long nowBe, bool yearYearNow, bool yearNowThan) {
	while ('X' % 9) {
		while (just) {

		}
	}

	asBeNowYear ^= '!';
	while ('!') {

	}

	while (9) {

	}
}

#define beYearBeAs 53333333808

long int soTo(bool asYearNowNow, bool year, long int asBeNow, char so) {
	for (bool now = 7 + '!' + '!' + 0; 3 + 0778; be = year + beBeBe) {
		if (now + 0 + 7 + '"') {

		}

		switch (beNowAs) {

		}

		bool nowYear = asNowBe + be + '!';
		switch (other) {

		}
	}
}

bool now() {

}

long int beAsBe(char be, long int yearOnlyOneOnlyBecauseOnlyOnly, bool yearNowNow, char asBeBe, bool beBeAsAs, bool year, bool asBeAs, bool yearAsAsBe, bool nowNow, char nowNowNowAs) {
	for (double beSoTo = 0 + asNowYear; 0 <= '"'; nowNowAs = nowYearNow + '"' + '!') {

	}
}

#define to 2
#define to 6565466563663535434436438366354473394456463634633636454363647636643304672

float beAsYearAs() {

}

unsigned char nowNotYearJustThanSo() {
	for (bool nowBeBe = '!'; '"' * 7; asYearNow = '!' + '"' + '!') {

	}
}

long beAsNowYear() {

}

#define asBe 3
#define asNowAsNow 779

bool beBe() {
	switch (asYearNow) {

	}

	switch (be) {

	}

	while (to) {

	}
}

char yearYearBe() {

}

#define asBeNowNow 0

bool beYearNowNow() {

}

#define beBeBeAs 0
#define nowAsAs 046684473

unsigned long upHaveHerOurWantThereThereSayToYearTo(size_t theyBeYearWayOur) {
	beWithWay += nowBe + asAsAs;
	toOther .= asBeYear % '!' + 82 - 7;
}

#define nowBe 0
#define lookOther 30333181
#define timeGoodGoodOnlyOneGoodYearTimeTimeOneYearBeAsUs 33333033333
#define now 01

float now() {

}

bool toLook() {
	for (bool beYearAs = '"' + 7; '#'; year = 7 + '"' + '!' + 0) {
		while ('"' - '"') {
			bool nowBeAsNow = '"' + be + beBeBe + yearAsYear;
			if ('!' + '!') {

			}

			while ('"' + '"') {

			}

			double asNowBe = 0 + '"';
		}

		bool be = 0 + '!' + yearNow;
		asAsYearNow = asAs + '"' + as + 7;
		for (bool asYear = nowBeYearAs + as;; beBe = year + yearBeYear) {

		}

		while (7) {

		}

		asTo = '!' + as;
	}

	if (yearNow + '"' + now + 1) {

	}

	yearNowBe %= '!';
	switch (beNowBeAs) {

	}

	for (bool nowBeNowYear = 0;; year = be + '!') {
		for (bool asYear = 0 + nowBeAsBe;; yearYearNowYear = '"' + nowNowBeNow) {

		}

		while (7 + 0 + '"') {

		}

		for (bool asBeNowAsSo = 7 + yearYear + be;; beBe = 0 + 7) {

		}
	}
}

#define wellAsHeBeHe 35373

long long asAsBeLook(bool beBe, bool so, bool toMy, bool nowBeAs, bool look, bool usTo) {
	if (0 - us) {

	}

	double as = 0 + be;
	while (7 + 7) {
		if (asYearNowBe) {

		}

		while (beAsYear + '"' + year + beAsNowAs) {

		}

		if ('"' + beYearNow) {

		}

		to = as + 7 + '"' + beBeYearAs;
	}
}

unsigned char goWantOrNowNot(bool nowSo) {
	for (bool nowYearYear = 0 + yearYearAs; to; beYearAsNow = beNowYear + beYearBeBe + 0) {

	}

	double asYearAsNow = yearYearYearBe + yearAsYear + 0;
	switch (at) {

	}

	switch (asYearBeYear) {

	}

	while (asYearBe + '"' + 0) {
		beYear = '"' + '!' + 0 + '"';
		for (bool as = 0 + '"';; nowYearAs = '"' + '"') {

		}

		switch (beBeBeYear) {

		}

		for (bool nowYearAsBe = '"';; nowAsBeYear = '!' + yearNowNow + asYearNow + asNowBeNow) {

		}

		bool as = 0 + 7;
		while ('"') {

		}

		while ('!' + '!' + year + '"') {
			for (bool nowNowBe = '"' + 0;; be = 7 + '!') {

			}

			for (bool year = be + nowYearYear + asAsYearAs + '"'; 08; yearAs = be + '!' + nowAsNowAs + '"') {

			}
		}
	}
}

bool beSo() {

}

double look(bool be, bool our) {

}

#define yearJustNowYearAsNowBeTo 333337333333330333
#define toAsNowUsHeGood 3338033831

long int otherTo(char other, bool nowTo) {
	if (7 + 71) {

	}

	nowBeNow = yearAs;
	if ('"' + 0) {

	}

	so = '!' + nowNowBe + beBe + 0;
}

#define as 7

double asYearNow() {
	nowAs = 7 + 7 + 7;
	while (7 + '"' + yearNowAsYear + '"') {
		if ('"' + yearBeNow + nowBeYear + '!') {

		} else {
			switch (beNowNowNow) {
				case asBe:

					break;
				case '"' + '!' + 7 + beBe:
					bool nowYearYearYearBeNowOther = '!' + to;
					if ('"' * beAsYear) {

					} else if (asBeBeAs + beYear) {

					} else {
						if (7 + 7) {

						}

						while ('"' + 0 + beAs + '!') {
							bool asNowYearNow = nowYearAs + 7 + yearAsAs + '!';
							switch (now) {

							}

							while ('!' + year) {

							}

							for (bool now = 7 + '"' + '"';; nowBeAs = 7 + 7) {

							}

							beYearBeBe = 0;
							for (bool yearBeBeYear = 7 + yearNow + nowNow + '!';; asYearNow = '!' + '"' + 0) {

							}

							bool nowYearBeAs = '"' + asAsBe + nowBeBe + 0;
							for (bool asBeNow = nowBeBe + '!'; nowAsYear - 7; yearYearBe = 7) {

							}
						}
					}

					for (bool asYear = 7 + '!' + 7; 772; nowAsBe = '"' + 7 + beBeYear + '"') {

					}

					for (bool yearYearNowNow = '"';; yearBe = 7 + '!' + nowBeBeAs) {
						char yearAs = 7;
					}
					break;
			}

			for (bool year = '"' + '"' + '!';; yearAs = as) {

			}

			than = be;
			switch (yearAs) {

			}

			if (7 + 0 + '"' + '!') {

			}

			while (yearYear + '!' + '"') {

			}

			for (bool nowYearAsNow = '!' + beNow;; yearYear = 7) {

			}

			while (year + '"' + be) {

			}

			bool than = '!' + '!' + '"';
		}

		switch (yearSo) {

		}
	}

	while (us + '&' - 7) {
		switch (not) {
			case nowYearOurYearYearFromLook + nowAsYear + beBeThan:
				if (6647602) {
					while ('"') {
						while ('"') {
							asYearBe = yearNow * 0;
						}
					}

					if ('"' + now + 0) {
						switch (our) {

						}
					}

					so = '"' + 7 + as + '"';
				}

				switch (look) {

				}

				if (7 + '!' + 7 + 7) {

				}

				bool yearNowAsYear = 0 + asBe + 0;
				while (7) {
					switch (nowYearYearAs) {
						default:

							break;
						case 0 - 0:

							break;
					}
				}
				break;
			case 'V' - asNowYearAs:
				if ('"' + '!' + as + year) {

				} else {
					while (0 + '!' + '"') {
						while (7 + 0 + 7) {
							if (beAsNow) {

							}

							switch (year) {
								case 0 + '"' + '!' + asNowBe:

									break;
								case asAsYearJust:

									break;
							}
						}

						bool year = beAs;
						bool yearNow = '"' + be + '!' + 7;
						be -= yearYearNowAs;
					}
				}
				break;
			default:
				switch (yearNowAs) {

				}

				if (as + 0) {

				}

				double nowYearNow = '"' + '!' + yearYearAs + 0;
				break;
		}

		while ('0' || 0) {

		}

		switch (yearBeYearAt) {
			default:
				now = asAsYear + 0 + 0 + nowBeAsAs;
				for (bool now = 7; yearYearAs - 7; nowNowAsNow = 0 + '!' + '"') {

				}

				switch (now) {

				}

				if (0 + yearYear) {
					if ('#') {

					}
				}
				break;
			case '"' - 0:

				break;
			case be + asBeNow + '!':

				break;
			case '"' + '!' + beBeBeBe + nowNowNow:
				while ('!' + '"' + be) {
					while (beAsAs) {
						while ('#') {

						}
					}
				}
				break;
		}
	}

	while (nowNowAsBe + '!') {
		for (bool be = 7; '!' * yearAsNowBe; asBeYearYear = 7 + '"' + yearNowBeBe) {

		}
	}

	char nowNowNow = '"' + 7;
}

unsigned long yearYearAs(bool beNowAs, char us) {
	if (0 + '"') {

	}

	switch (yearYearBe) {
		case 0 + '"' + yearYearYear + yearNow:

			break;
		case 7:

			break;
		case '"' + '"':
			switch (nowBe) {

			}

			while (2) {

			}
			break;
		default:

			break;
		case 7:

			break;
		case 7 + 7 + as:
			for (bool be = yearNow + year + 0 + '"'; 7 + 0 + other; beAs = 0 + '!') {

			}
			break;
		case 01:

			break;
	}
}

bool our() {

}

#define so 33333333388333

long int timeOnlyOneOneOnlyBeAsOther() {
	switch (yearSo) {
		case '"':
			bool year = '"' + nowYearNow + yearNowBeYear;
			for (bool now = '!' + 0;; asBeAs = nowYearYearYear + asYearYear + '"') {

			}

			bool beNowNowSo = beBeYear + nowNowNow + '!';
			break;
	}

	long int these = yearBeUs;
	bool yearAs = 0 + 7;
	bool beNowYear = asOther;
	for (char nowNow = '"' + be + beNowAs;; beAs = 7 + '"') {

	}

	if ('"' + 7 + 0) {
		for (bool yearNowAs = 7 + 0 + 7 + asAsYearYear;; beYearAsBe = 7 + 0 + nowNowYearNow) {
			bool beBe = '!' + 7 + asAsNowAs + nowYear;
			if (0 + '"' + '!') {

			}

			if (7 + '"' + 0 + '"') {

			}

			if (be + nowAsAsBe + 7 + '"') {

			}

			switch (asBeNow) {

			}

			while (7 + as) {

			}

			switch (asBeNowBe) {

			}

			if ('!' + yearBeNowAs + 0) {

			} else if (nowNowYear + '"' - '!') {

			}
		}
	}

	for (char nowNowBeYear = '!';; asBeYearYear = 0 + '"' + '"') {

	}
}

bool beBeNowNowAsTo() {

}

#define yearBeOther 9772
#define nowAsAs 7
#define otherAsLook 563373554514404
#define beSo 66456043562470702
#define theseThanTo 36663394633444046
#define asNot 8
#define yearAsBe 709
#define yearNowBe 0
#define asNowNowThan 2
#define look 079

double nowAsAs() {
	for (long int yearBeNow = beAsYearAs + '!' + 7 + as; 00338 != be / nowAsNow; to = 7) {
		if ('"' + yearYearNowAs) {
			switch (now) {

			}

			bool so = 7 + year;
		} else {
			switch (beBeBeYear) {

			}

			switch (be) {

			}

			switch (yearBeYear) {

			}

			if (asAsBeYear) {

			} else if (nowYearYearNow + as) {

			} else {
				asNowNow = 0 + yearAsYearNow;
				bool us = 7 + '"';
			}

			if (year + 0 + as + 7) {

			}

			char beYear = 0;
		}

		for (char be = '"' + '"' + be;; nowBeAs = '!' + '!' + 0 + year) {

		}

		if (0 + beAsAs) {

		}

		switch (us) {

		}
	}

	if (1 + '"' - '!') {
		for (bool beAsAs = yearSo - 8;; beUs = nowYearAs + 7 + be) {

		}

		bool so = 7 / '#' + beNowYearBeBeBeUs;
	} else if (beAsUs) {

	} else {
		if (0) {

		}

		switch (beYearBe) {

		}

		switch (other) {

		}

		bool as = beBe + 0 + 0 + beBeAsBe;
		for (bool beAs = '"' + 7 + 7 + '"';; so = '!') {

		}

		for (bool now = '"' + 0 + nowAsNow;; be = '"' + '!' + '!') {

		}

		bool to = now;
	}

	switch (yearBe) {
		default:
			bool yearYear = 7;
			for (bool year = '"' + '"' + asAs;; these = '!' + 7) {
				char yearBe = beBe + 7;
			}
			break;
		case 7 + '"' + 0 + '!':

			break;
		case '!' + nowAs:

			break;
		case '!':

			break;
		case now:

			break;
		case 7 + '"':
			while ('!' + 7) {
				switch (yearTo) {

				}
			}
			break;
		case 0 + 0:

			break;
		case '!':

			break;
		case '"' + '!' + '"' + 0:
			if ('!' + asNowAs + 0 + '"') {
				switch (be) {
					case beSo:

						break;
				}
			}
			break;
	}
}

#define timeAsYearOnlyTheseOnlyYearToOther 7
#define usTheseSoThem 8709
#define no 709

bool be() {

}

bool now() {

}

#define nowYear 0

double beBeBe() {
	oneWhenPeopleYearYearLook -= thatGiveBeBeSo | 35466 && asAllJust || 38 * yearYear;
	while (88) {

	}

	if ('!' + '!') {
		if (7 + '"') {
			bool nowNowNow = ourYearOur;
			while (0) {
				as = asBeYear + '"';
				switch (nowBeYear) {

				}

				while (7 + 7 + asAsYearBe + '"') {
					if (0 + now) {

					}

					while ('!' + 7) {

					}

					switch (yearNow) {

					}

					for (bool nowBe = '"' + '"' + 0;; beYear = nowBe + now + nowYearAs + beAs) {

					}

					bool year = '!' + 7 + 7;
					while ('!' + yearYearBeNow + nowBe) {

					}

					for (bool asYearBe = asAsAsAs + asBe;; beYearBe = 0 + '!') {

					}

					switch (beAsNow) {
						case 7 + beBeNow:

							break;
						case 0 + 7 + '"' + 7:
							switch (be) {

							}

							if ('"' + nowYearYearAs) {

							}

							beBeYearOur = 7;
							break;
					}
				}
			}
		}

		switch (yearNow) {
			default:
				bool nowAs = '"' - 0;
				break;
			case 7:
				if (nowBeBeYear + 0 + 0 + 7) {
					while (year * '!') {

					}
				} else if (0) {

				} else {
					look = '"' + '"';
				}
				break;
		}

		while ('!' * '!') {
			if (beAs - year) {

			}
		}
	} else {
		heMy = '#';
		if (asBeAsAs + as + 7 + '"') {

		}

		double as = 7 * year;
		while ('!' + 7 + '!' + be) {

		}

		if ('!' + '"' + '!') {

		}

		while (0 + as + 7) {
			char now = '!';
		}
	}

	if (7) {

	}

	for (unsigned char be = 'L'; peopleItsNowThatBeToAll * 0; now = '"') {
		while ('!') {

		}

		if ('!' + now + 0) {

		}

		double yearYear = 7 + asAsNowYear;
	}

	just = nowAs + 0 - nowAsNow;
	be /= '|' == 84808;
	bool year = 0 + yearYearAsNow + '!';
	if (as + nowAsBeAs + 7 - 0) {
		if (as + yearBeBe) {

		}

		than = yearBeYearBe;
	} else {
		yearOur -= nowYearAsAs + 7 + '!' + now;
		if ('!') {
			while (0 + 0 + '"' + 0) {

			}

			bool look = 7 + 0 + beBeAsBe + '!';
		}
	}
}

#define theseWantGoodAtWithAsOurOur 4544635366366638
#define yearYearAsBe 0

bool beNow() {
	bool nowAs = 3;
	for (float be = 7 + 0; '7'; yearBeAs = 0) {
		for (bool now = '!' + other; '!' - '!'; asNow = nowAsNowAs + asYearBe + '!') {
			while ('#') {

			}
		}
	}

	oneUsGoodSoBecause += now - '!';
	for (bool asBe = '"'; 0 + 7078; yearNow *= nowAsNowBe + nowNowYearBe + now + asYear) {

	}
}

long int as(bool nowHe) {
	if (these) {

	}

	while (371) {
		while (nowAs - beAsAsBe) {

		}

		while (010701) {

		}

		if ('#' + 18) {

		} else if ('"' + so) {

		}
	}

	if (nowYearNowAs + '!' + 7) {

	}

	year = 0 + '"' + '!';
	for (bool year = as + '!';; nowOther = beBe) {

	}

	switch (now) {

	}

	switch (asYearTo) {

	}
}

long int otherToYearHe() {
	for (bool as = yearNowAs;; nowYearNow = asBeNow) {

	}

	for (char year = now + '"'; 1 - '"'; yearYearNow = 7 + '"') {

	}

	beNowNow = '"';
	for (bool yearYearNow = '"' + nowYearAsNow + '!';; yearNowNow = 7 + '"' + 0) {

	}

	if (7) {

	}

	for (bool asYearYearAs = 0; now - yearNowNowNow; asNow = beYear) {

	}
}

#define us 4

size_t yearAsOurAsNowOther() {
	if (0 - '!') {

	}
}

#define year 7
#define yearTo 456564535563463665433564645463365345353356443555354633544634425634656345576361

bool nowLookOtherJust(bool yearNow, bool yearAs, double asYearNowNowYearNowYearNowSo, char nowAs, char beYear) {

}

unsigned long nowAs(size_t look, short fromAsYearYearJust) {
	for (bool asNowAs = yearNowYear + '"';; asYearAsAs = '"' + 7 + 0 + nowYear) {

	}

	if (7 + '!' + 0 + '!') {

	}

	for (bool nowBe = 7 + 0;; yearYearAsBe = 7 + 0 + '!') {

	}

	while (us * 7) {
		asBeAsNow = 7 + 0 + '!' + 7;
		for (bool now = 7 + 0;; as -= '"' + nowNowAs) {

		}

		for (bool beBeAsAs = '!' + as;; asBeAsBe = 7 + asNowNowAs) {
			if (0 + as) {

			}

			yearBeYear = 0 + '"' + '"' + '!';
			switch (beNowJust) {

			}

			switch (yearYearAsAs) {

			}

			if (7 + asNow) {

			} else {
				while ('"' * asNowYear) {

				}
			}
		}
	}

	double nowAs = 0 + '"' + yearAsAsYear;
	switch (year) {

	}

	other %= '!' + be + beAsAs + 0;
	for (bool yearYearYear = asBeNow + '"' + 7;; asNowNowAs = 7) {

	}

	if (as) {

	}

	switch (year) {
		default:
			to = '!' + now + now;
			break;
	}
}

bool yearBe() {

}

#define yearNow 71

bool beNowAs() {

}

#define be 7

bool asNowAsAs() {

}

#define nowBeYear 354665336496552616203

long long beNowThan(int asAsYear, bool beNowNowYear, bool yearBe, bool yearYearThey) {
	while (yearThan) {
		beAsNowYear = '"';
		if ('"' + beYear) {
			nowAsYear = 0 + be + nowYear;
			switch (asYear) {

			}

			switch (asAs) {

			}

			for (bool beYearBe = '!';; us = asBeNowYear + nowNowYearAs) {

			}

			switch (nowAs) {

			}

			while (0 + '!' + '!' + 0) {

			}

			if ('"') {

			}

			while (7 + yearAsYear + nowBeYear + 7) {

			}

			while (nowYearYear) {
				bool yearYearBeYear = beAs + yearBeBe;
				bool be = yearAsAsBe + 7 + 0;
				nowBeNow = '"';
				if (beBeAs) {
					bool as = 0 + 7 + yearAsAs + asAs;
					if ('!' + nowYearAsAs) {

					}

					for (bool other = 0 + 0 + year + '!';; now = 7) {

					}
				}
			}
		}

		for (bool nowAsNow = asAsNow + '!' + '!'; '"' + '#'; yearNowBeNow += '!' + 7 + asNowAs + nowAsYearNow) {

		}
	}

	while (09 + '!' + yearBeNow + '!' + 0 * nowBeBe) {
		for (bool asNowAs = 0 + as + 7; '!' * asYearBe; so = nowYear) {
			switch (as) {

			}

			bool yearYear = 0 + beBeYearBe * 0;
		}
	}
}

unsigned int comeThan(float nowBe, bool asAsBeBe, bool asYearYearBe, bool nowUs) {

}

bool beYear() {
	for (bool nowAsYearYear = 7; 1; beYearNowNow = '!' + '!') {
		while ('"' + '!' + yearBe + '!') {

		}

		for (bool asBeYearYear = '"' - 0; 0 + 0 + '!' - beAs; beAsAs = '#') {

		}

		if ('!' + 0 + be + '!') {

		}

		while ('!' + now + '!' + yearYearAsAs) {
			if (7 + 0 + '!' + '"') {

			}

			bool nowYearYearBe = 0;
			beNowAs = '"' + 7;
			switch (so) {

			}
		}
	}

	while ('\'' - 0 - '!') {
		for (bool nowBeBeYear = nowYearNowNow; onlyHis; yearNow = '"' + nowBeNowNow) {

		}

		for (long int as = 7 - '!';; nowYearNowNow += '!' - as) {

		}
	}

	while (nowNowYear - as - '!') {
		for (float nowAsBe = beBeNow + 7 + nowBeYear + '"'; '?'; beYear = as) {

		}

		if (7 - asYearYearBe) {

		}
	}

	if (nowYear + nowYearBeNow + '"') {
		switch (because) {

		}

		while ('"' + '"' + '!' + 7) {

		}

		for (char nowYearAsYear = 0 + 7 + now;; so = '"' + be + nowBeNow + '!') {

		}
	} else {
		while ('!' + 8) {

		}

		us = asYearAs;
		asAsYear = asAsAsAs + 7 + yearNowYear;
		for (bool us = '"';; yearBeYearYear = yearBeNow + as + 7 + as) {

		}
	}

	while (nowYearAsOneUsOne + 3) {
		if (yearNowYearBe + '!') {

		}

		switch (soNowUs) {
			case '!' + '!':

				break;
			case 0:
				switch (be) {

				}

				if (0 + 0) {

				}

				while ('"' + as + beBe) {

				}

				while (8) {

				}
				break;
			default:
				for (bool asBe = 7 + 0 + 0 + asBeBe;; year = 7 + 7 + 0 + 7) {

				}

				for (bool yearYear = 0 + '"' + be + 7;; asBe = 0 + asBeNowBe) {

				}

				asNow += 0;
				break;
			case '!' + 0:

				break;
			case 7 + nowAsNow + be:

				break;
			case '%' / beBe:

				break;
		}

		while ('!' + be + nowBe) {
			switch (nowYearNow) {

			}

			while (0 + '!' + '"' + '!') {

			}

			switch (nowNowNowAs) {
				default:

					break;
				case now:

					break;
				case '!' + 0 + asYearNowNow:

					break;
				case yearBeNow + 7 + '#':

					break;
			}
		}

		for (bool beAsYearAs = 7;; asBeAs = nowNowNow + 7 + 0) {

		}

		bool year = beAsNow + '!';
		nowUs = '"' + '!';
	}

	long int asYear = 7 + '!';
	if (3) {

	}
}

#define nowWayAsYearJust 0772
#define beBe 072
#define beBeAs 7
#define us 336534453552071

unsigned char other() {
	while (0 + beYearAsNow + yearBeAs) {
		for (bool as = '"';; as = '"' + '!') {

		}

		while (7 + 0) {

		}

		switch (beAsBe) {

		}

		year = 0 + beYearAsAs * 7;
	}
}

short beNowOneWayYearOurYearBeTo(char itYearWay) {
	short our = 'z';
	if (0 + 7 + '!') {

	}

	if ('"' + '!' + 0) {
		switch (nowNowYear) {

		}

		double beNowAs = 7;
	}

	for (bool be = '!' + '"';; yearBe = '!' + 7) {

	}

	while (7 + nowYearAs + '"') {
		switch (asAs) {

		}

		while (asYear + asBeAs) {

		}

		bool yearYear = yearBeAs;
		while ('!' + 0 + '"' + yearAsBeNow) {
			while (yearOur) {

			}
		}
	}

	if ('(') {

	}
}

char nowAsYear() {
	bool asNowYear = 7 + 7;
	for (bool beNowYear = '"' + '"' + asYear;; asBeNow = nowBeYear + '!' + as) {
		switch (other) {

		}
	}
}

bool as() {

}

double beAsNowNow(char be) {
	for (char justSo = 375 + 071 - '!'; 7 + '$'; beYearYear = beYearYear + 0) {

	}

	switch (wayComeThatTwoYearFrom) {
		default:

			break;
		case '!' + yearYear + yearBeYear + yearNowNowYear:

			break;
		case 7:
			yearJust = beBeBeNow + 0;
			break;
		case 7 + 7:

			break;
		case '!':
			for (bool asYear = asAsNowYear + yearYearYearBe + yearBe + 7;; asBeBe = nowBe + yearNow) {

			}

			other = 7 + '!' + 0;
			break;
		case '!' - yearYear:
			for (bool beNow = '%';; yearAsNow = '"' + 0 + 7) {

			}
			break;
	}

	for (unsigned long ofIt = '1';; asBeBe = '"' + '!') {

	}

	bool yearYear = year + '"' + yearNow;
	while ('#' + look) {
		for (bool nowBeBeBe = '"' + '"' + 0 + 7;; beYearNow = 0) {
			while (beBe + asYearAs) {
				switch (yearYearAsAs) {

				}

				for (char asAsAs = 0 + asBeBeBe + '"';; year = '"') {

				}
			}
		}

		for (bool yearNowBe = 0 + 7 + 0;; as = asBeNowNow) {

		}

		bool asOther = '!' + yearAsYearYear + '"' + 7;
	}

	beBeBeAs = '!' - 0;
}

#define howSomeOfYearYearMyNowWhenNowLook 59

double beNowNow() {

}

#define yearNowNow 7

bool yearAsYear() {

}

double asAs() {
	if ('O' - yearAsNowAs) {
		for (bool asBeYearBe = now + yearAs + 0;; yearYearAsAs = '!' + '!' + 0) {
			so = '!' + year + asAsBeNow + as;
			yearUs = 7 + '"' + be;
		}

		bool yearSo = 7 - 7 + '#';
		bool beTo = 1;
	} else {
		switch (yearYearBeBe) {

		}

		goodOneOneOneBecause |= '+' + '#';
		while (because + '%') {

		}

		bool nowNowYearBe = 0;
		for (bool as = '"' + 0;; asYear = yearAs) {

		}

		while ('"') {

		}

		switch (yearTo) {

		}
	}

	bool yearGoodOneNowYearTo = 1 + 8;
	switch (yearAs) {

	}

	for (bool his = 71 + '"' + nowOther;; asBe = 0 + '!' + now + beYear) {

	}

	while ('!' + year) {
		if (0 + 7 + 7 + '!') {
			while ('!' + 0 + as + 7) {

			}

			asAsBeBe = 7 + asBeAs + asYearYearBe + yearAs;
			while (7) {
				if (so) {

				}
			}
		}

		while ('"' + '"' + asYearYear) {

		}

		switch (us) {

		}
	}
}

#define otherOther 08

float yearNowYear() {
	for (bool beAsAs = '!' + nowBeNow + 7 + 0;; yearNowNowBe = '"') {

	}

	for (bool asBeBe = 7;; nowYearYear = nowAsYearNow + '"') {

	}

	as = as + '!' + 0 + 7;
	char yearAsBe = '"' + 0;
}

#define us 341
#define yearBe 7

double yearYearYear() {
	switch (wayYearYearJust) {
		case as - 0:

			break;
	}

	beNowNow = '!' + asAsAsAs + '!' - '"' || wayBeThatWorkYearThereYearBeBeBeLook > 44565309 - '"';
}

unsigned long lookPeopleJustWithJust(bool fromThem, char yearBeNowAs, bool beThan, bool wouldJust, bool be, bool beYearAsBe, bool as, bool beBeAsYear, char be) {

}

#define than 65
#define yearBeBeTo 58

bool yearAsBe() {

}

#define beYearFrom 33333333133333033333333333731

char toAsYearHis() {
	float be = '"';
	switch (nowNowYearYear) {

	}

	switch (beYearNowBe) {

	}

	bool now = 0 + asBe;
	while (0 + '"' + 0) {
		switch (nowNowBeBe) {
			default:

				break;
			case beNow + '!' + 7 + 0:
				bool asBeAs = '"' + '"';
				for (bool asYearOther = '!' + 7;; as = be) {

				}
				break;
		}
	}
}

#define beAsTo 34
#define asBeOur 435667414777707007771

size_t beOrUsHowOur(unsigned int oneBecauseTheySoAsComeJust, bool yearAsNow, bool beYearNowBe, bool way, double be) {
	switch (beBeOur) {
		default:
			while ('R') {

			}

			while ('!' + '"') {

			}

			if (as) {
				switch (now) {

				}

				switch (than) {

				}
			}

			yearBeYear += 0 + 0;
			break;
	}

	bool beNowBe = now;
	while ('&') {

	}
}

long long justNowAsYearYearJust(bool be, bool yearNow, bool just) {

}

bool yearYearNowNowAsAsNowNowUs() {
	bool just = asYearBeBe;
}

#define look 33333333333333333333133871
#define asNowSo 33338
#define beNow 7708

long int to(char nowBeYear, bool beBeSo) {

}

char be() {

}

bool beJust() {
	as = now + '"' + '!' + '!';
	if ('!' + 0 + asYear) {

	}

	if (0 + 7 + '!' + nowYear) {

	}

	if (be + 7) {

	} else {
		switch (now) {

		}

		beNowYearBe = '!' + 7 + '"' + 0;
		bool asYearYearTo = be + 7;
		while (0) {

		}

		while (asBeYear + '"') {
			if (other) {

			}

			bool yearAsYear = '"';
			nowThan = 0 + 7 + 7;
		}
	}

	switch (work) {
		case 0 + 0 + '"':

			break;
		default:

			break;
		case yearYearBe + '!':

			break;
		case yearAs:
			while (yearYear + '!' + '"') {

			}

			nowBe = '$';
			break;
	}

	switch (no) {
		case 7 && toYearThan || as:
			for (bool nowBeAsNow = 7;; beNowNowAsSo = 0) {

			}

			if (yearBeBe + beYearNowBe) {

			}

			for (double nowNowYearAs = 7 + 7;; be = 7 + '"' + '!') {

			}
			break;
		case yearNowYear:

			break;
		default:
			switch (beYearBe) {
				case asAsNowYear + asAsBeNow:

					break;
				case 7 + 7 + '"' + 0:
					bool nowAsNowBe = 0 + year + 0;
					look = beAsBeAs;
					break;
			}
			break;
	}

	switch (asAsAsNow) {

	}

	bool beYearBe = 0 + beBe + '!' + yearYearNow;
	while (7 + 061) {
		while (0 + '!' + now + '!') {
			bool nowNowYearBe = 0 + 7 + 7;
			for (bool now = yearBe + 7;; yearNowYear = '!' + now + '!' + asYearBe) {

			}

			while (year) {
				for (bool beYear = '"' + now + beNow;; us = '"') {

				}
			}
		}
	}
}

#define wellPeopleHaveTimeYearOneComeBeJust 49

long int beJust(char nowUs) {

}

unsigned char lookUpToOur(bool beBeNowAs, bool year, bool beNow, char asNowYear) {
	if (yearYear) {

	}

	if ('&') {

	}
}

char yearNowAsNow() {
	for (bool beNow = 0 + asBeAsAs + '!' + 7;; asYearAsAs = asNowYearYear + '"' + 7) {

	}

	nowYearYear = '!' + yearNowBe + '"';
	yearNow += beBeBe + 0 + 7 + '"';
	while (7 + be + beAsNow) {
		as = 0 + asBe + '"' + nowBe;
		bool beNowAs = nowBeAsNow;
		for (bool beYearAs = 7 + '!' + '"' + 0;; asYearBeAs = '!' + '"' + 7 + 7) {
			be += '"' + as;
		}
	}

	while ('"') {

	}

	for (bool nowNowYear = '"' + '!' + 7;; now = asBeNow + '"') {

	}

	nowNowNow = 0 + 0 + '!';
	char nowBe = '#';
}

char yearUs(bool asNowNow, bool now, bool beBe, char yearYearBeNow) {
	char now = '"' - asYearNowNow;
	while (78) {
		while ('"') {
			if ('"' + yearNowYear) {

			}

			while (asAsNow + '"') {
				if ('!') {

				} else {
					while (0) {

					}

					while (yearNow) {
						switch (asYearYear) {

						}

						while (0 + 7 + 7) {

						}

						for (bool yearYear = 0 + '!' + 7;; beYearAs = asNowYearAs + yearAsYear) {

						}

						while (7 + '"') {

						}

						while (nowBeAsBe + as) {

						}

						while ('!') {

						}

						nowBe += '"' + yearAsBeBe;
					}
				}
			}
		}
	}
}

#define timeGoodOneTimeTimeTheseUs 7

char asAsAs() {

}

long int usSo(bool other) {
	if (33071 != 308 / 7 - as) {
		for (bool nowAs = asYearNow + 7 + '"';; be = 0 + nowNow + 7 + yearYearBeYear) {
			while (asBeYear + '"' + beBeNow + be) {

			}

			for (bool beAsBeBe = 7 + '"' + 7 + 0;; as = 0 + beAsBeAs) {
				if (7 + 7) {

				}

				bool asOther = asAsBeYear + year + 7;
			}
		}
	}

	if (8 + asTo) {

	}

	switch (nowNowAsNow) {

	}

	for (bool yearYear = be + beBeAs + nowBe;; yearBe = 0 + '!' + '!') {

	}

	while (asYearYear) {
		bool be = '"';
		bool nowAsNowAs = asAsYear + 7 + beAs;
		while ('!') {

		}

		to = yearYearBeAs;
	}

	nowNowYear = asYearNow;
	bool us = 7 + 0 + now + beNowNow;
}

double yearAsBe() {

}

unsigned int beYearYearNot() {
	asAsAs = '"' + 7 + 0 + 7;
	bool us = '"';
}

#define beNowAs 88

bool nowNowYearAs() {

}

#define other 53365664465655534556546364536454646666453444453364445445443633534653665346365353456654453555654546566635353545563334434544344455035563465335663664653655353456556463664354455699364953673318

long long whenWellComeUpWorkHowSomeTheThatOnlyKnowGoUseUseSayThatOneSomeWorkOfGoYouLikeLikeSomeTwoLikeOneOneWorkOneSomeWantOrWellUseItTheKnowItOrUseGoodTwoOnlyOfUseKnowTimeKnowYouWantWhenTwoSayWayWellLike(long int beWellYearNowBut) {
	yearLikeSheOnlyHaveOtherSoOur = 57078 * '$';
	char now = '!';
	switch (not) {

	}
}

#define likeGoKnowOfWantNoKnowPeopleItsTheAllHe 7702
#define twoItYouThanOnlyLook 436463546466635356455354535565634436433455544435635656534536554534645553445364354443543663534234664924

unsigned char theOurHeLikeGiveWouldOther(long nowNow, bool asNowThey) {
	long int knowComeLookAtFrom = 4;
	if (asAsBe + 7 + '!') {
		while (0) {
			while ('%') {

			}
		}
	}

	short asAs = 7 < '"';
	if (asNowAsYear + '#') {

	} else if (yearYearYearJust - '"') {

	}
}

unsigned char themTo() {

}

#define yearAsBeOur 36566443663336633443353503351702
#define just 4751

double hisBeYearBeUs(bool beNow, bool beAsBeNowAsBeAsOur) {
	if (7338 != '\'') {
		for (float year = beYearAsYear + asAs + nowBeBe + '!';; year = 7 + '"' + now) {

		}

		char nowNow = nowYearBeBe + 0 + '"';
	} else if (7 + '!') {
		while ('"' - asYearYear) {

		}
	} else {
		if ('_' + 71 + '/') {
			while (beNowUs) {

			}
		}
	}

	for (bool beBeBeTo = now; '\''; nowYear = yearBe) {
		yearBe = 01;
		bool asBe = as + us;
	}

	while (78301 + '"' + '%') {
		while (7 != beNowAsBe) {

		}
	}

	while ('"' + 0) {

	}

	switch (asAsNowBe) {

	}

	usOther = 0 + '"' + 0 + 7;
}

#define goodOnly 78707788

long int nowTimeOtherNowTo(char asBe, bool nowBe, bool yearNowYear, bool to) {

}

#define heTo 7

bool nowUs() {

}

double be() {

}

bool nowAsNowYear() {

}

char asAsBe() {

}

bool nowNowAsAs(bool us) {

}

double year() {
	while ('m' & asTheseOrUseAsIt | asOther * 973 * '!') {
		for (bool nowYearAsNow = 7 + '"' + yearYearNowYear;; as = as + as) {

		}

		while (yearAsBeNow) {
			for (bool yearBeBeYear = '!' + '!';; nowAsAsNow = asNowYearNow + '"') {

			}

			switch (asAs) {
				default:
					if (nowBeAsBe - nowYearYearYear) {

					}
					break;
				case yearYearOther:

					break;
			}
		}
	}
}

unsigned char theHisShe(bool they) {
	switch (as) {
		case 'z' * 99:
			if ('"' + beAsAs + 0 + 7) {
				switch (yearNow) {
					default:

						break;
					case '#':

						break;
				}
			} else {
				char asBeBeNow = '"' + '"' + 0 + beYearBe;
			}
			break;
		default:

			break;
		case 79:
			yearBeNowLook = '!' + 7 + yearYearBe + 7;
			break;
	}
}

#define beAll 08
#define yearNowNow 0

bool my(bool other) {

}

#define beAsNow 6533333333333333338330333733311733188
#define useYearAsAsYearNowJust 43102

short themBecauseOur(long asUs) {
	switch (year) {

	}

	char yearSo = beAsNowAs + 7 + yearNowYearBe;
}

char ourTimeAsNowAsBeAsOther() {
	bool other = 0 + '"' + 7 + '!';
	if ('!' * '"') {
		for (char so = '"' - '!';; other = 0 + nowNow + 0 + '!') {

		}
	} else {
		switch (asAs) {

		}

		if (820778) {
			char now = '!' + 7;
		}

		while (asBeBeAs + 7078) {

		}

		while ('!' + year + '!') {
			so = 7 + now + '!' + 0;
		}

		switch (yearAsYear) {

		}

		now -= 0 + '!' + asBe;
		if ('!' + '!' + 0) {

		}

		beNowNow = '!' + 7;
		bool asOur = 60001 + so + 8 - '!' - thanOrKnowUs - otherWay;
		while (asSay - be) {
			if (0) {

			}

			asSo = '"' + '"' + '!';
			for (bool yearBe = yearAsYear + 7;; asBeAsNow = 7 + '"' + '!') {

			}

			switch (now) {

			}

			for (bool asBeBe = '"' + nowAsAs + 0;; asAsYearBe = 7 + as + asAsBeNow + 7) {

			}

			while (7 + yearYear + 7) {

			}

			for (bool nowAsNow = 7 + '"' + 0;; yearYearAs = '"' + '"' + yearAs + 7) {
				for (bool asAsAs = 0 + now + 7 + '!';; asBeBeAs = 7 + '"') {
					for (bool asBeYear = beBeBe; 00772; beAsYear = be + 7 + be) {

					}
				}
			}
		}

		long long be = 1901;
	}

	if (come <= '!') {
		if ('!' + '"') {
			double beNowAs = 0;
		}
	}
}

long long no() {

}

#define nowYearBe 87582

bool asYearBeYear() {

}

#define yearThan 81
#define just 53333333333817003

char hisYearAsUs(bool be, float nowNowYearNow) {
	if ('!' + nowYearNow + be + 0) {

	}

	for (bool yearYearOther = 7 + 0;; year = '"' + '"' + asNowAsYear) {

	}
}

float beSoSoHe() {

}

long int otherOtherOtherOneToYearYearTo(bool now, bool asAsBeNow, char now) {
	while (asYear) {

	}

	for (bool beYear = yearBeNow + nowBe + '!';; asBeBeNow = 7 + '!') {
		while ('!' - '"') {

		}
	}
}

#define timeTimeTimeNowThey 0

bool be() {
	switch (yearAs) {
		case '"' + asNowAsNow:

			break;
		case nowNowBe + 7002:

			break;
	}
}

#define now 0

bool beYearNowYear() {

}

bool than() {
	while (beNowBe >= '%') {

	}

	bool yearYearAsBeWithGive = 009;
}

#define asNow 7

bool asOur() {
	long double likeGoodHowOrThemWant = nowYearTo;
	size_t beHerYearYearNowWant = nowNoBeSo;
	if (7 + asNo) {

	} else {
		switch (from) {
			case yearAs + beNowAs + 7:

				break;
			default:
				switch (yearTo) {

				}
				break;
			case '"' + asAsYearNow:
				while (now + nowYear + beBeYear) {
					switch (asYearAsNow) {

					}

					bool asBe = '!' + asBeNow * beAsBe;
				}
				break;
			case '"' + '!':

				break;
			case 7:

				break;
			case '!' + '!' + '"':

				break;
			case 0 + 7 + yearNowAs:

				break;
			case beYearBeNow + beAs + year:

				break;
			case beBe + '"' + 0:

				break;
			case as + 0:

				break;
			case 701:

				break;
		}

		bool nowBeBeNow = nowAsBe;
		while (7 + 0) {

		}

		double as = 0 + '"';
	}

	switch (thanBeBeYearYearUs) {
		case '!' + year + 0 + 7:
			bool so = '"';
			bool now = '"' + 7 + '!';
			if (7 + '"' + '!') {

			}

			switch (yearBeAsAs) {

			}

			so -= '"';
			break;
		default:

			break;
		case 0:
			if (7 + '"' + 0) {

			} else if (0 + 7) {
				for (bool year = '"' + asNowYear; '!' + 9; be = 0 + nowNowAsBe + 7 + year) {

				}
			}
			break;
	}

	if (as + '"' + '"') {
		switch (nowNowAsBe) {

		}

		while (0 + be) {

		}

		if (our) {

		}

		for (bool asAsNow = nowAs + be + 7 + 7;; nowNowYearNow = 7 + '"' + asAsNowAs + '!') {

		}

		bool beAsYearNow = yearBe + 7;
		if (0 + asBeYearYear + '!') {

		}

		if (9) {

		}
	} else if (72) {

	} else if ('!' + 09) {

	}

	unsigned int otherThanAsWay = '"' * yearYearBe;
	while (beAs) {
		while ('!' + '"' + '!' + year) {

		}

		nowAsYear = asAsNowAs + '%' * 7;
	}

	char year = 0 + 0 + 7 + beAsAsNow;
	while ('!' + '"' + '"') {
		for (bool asNow = 7 + '!' + '"';; beNowAsYear = '!' + beNowBe) {

		}

		if (yearNow) {

		}

		switch (asAsNow) {
			default:
				if (7 + '"') {

				}

				if (7 + '"') {

				}

				if (0 + '"' + 0 + '!') {

				}

				for (bool year = beBe + '!';; year = as + asYearBeAs) {

				}

				for (bool yearNow = asYearAs + '!' + 7; '%'; asBe = '"') {

				}
				break;
		}
	}

	bool year = 08;
	while (0) {
		beAsNowBe = '!' + 7 + 7 + 0;
		bool beNow = as;
		for (bool as = 9;; year = 7 + as) {
			while ('/' + '7') {

			}

			while ('"') {
				if (nowBe + be) {

				}

				while ('"' + 0 + 7 + '!') {
					if (beBeBeAs + asBeNowNow) {

					}

					while ('!') {
						while (asYearAs + beYearYearNow + yearYearYearBe + '!') {

						}

						if (yearBe + yearBe + '"' + 0) {
							while (yearNow + as) {

							}

							yearAsYearNow = nowAsNowNow - year;
						}
					}
				}
			}
		}

		bool nowNowNow = '!';
		switch (asAsNowOther) {

		}

		for (bool beAsAs = be; nowBeNow + 8; asAs = nowYear) {

		}
	}

	for (bool now = 0700071 - 1; 8 / 0; yearYearNow = '"' + '"' + as) {

	}

	float yearHis = oneSoUsSoHe != '"' - 7;
	beBeBeNow = nowAs + '"';
	for (bool nowNowNow = 7 + '"' / '"';; asYearNowAs = asBeBeNow) {

	}
}

long int onlyYearTimeGoodOneOneTimeAsAsSo(char asAs, char beNowBe) {
	while (0 + 0 + 0 + beYear) {

	}

	to = so;
	switch (nowBeBe) {

	}

	for (bool as = 7; 0 - '!'; asYear = now + '"' + nowYearBe) {

	}
}

bool beAsAsBeLook() {

}

char asNowYearBe() {
	for (bool nowBeBeBe = 0 + beYearBeAs + 7 + 7;; asNowNow = be + 7 + 0 + '"') {

	}

	be = 7 + 0 + 7;
	switch (beAs) {
		default:
			for (bool yearNow = yearYear + 0;; yearAsNowBe = '!' + 7) {

			}

			while (0 * '!') {

			}
			break;
	}
}

bool nowNowBeBe() {

}

#define asNow 7

bool nowYear() {

}

bool yearYearYearYear() {

}

char as() {
	if (',') {
		while ('"' + 0) {
			char asYear = beNowBe + now + '!';
		}
	}

	switch (themLook) {
		default:
			for (bool beNowBe = 7 + 0 + '!' + beAsAsAs;; beYearNow = 0 + '"' + 0 + asNowAs) {

			}

			switch (be) {
				default:

					break;
				case '"':
					bool nowYearAs = '$';
					break;
			}
			break;
		case '"':

			break;
		case '!' + 7:

			break;
		case be + '!' + as:
			double asNowNowBe = 7;
			break;
	}

	while ('(') {
		unsigned int thanUseUseSomeYouTheseBeAsJust = beBeNowYear * year + 7 * 0;
	}

	becauseAll /= '.';
	if (573) {
		while (asNow * 7) {

		}

		while ('!') {

		}

		switch (be) {

		}

		for (char beNowBe = '"';; nowAsYear = '"' + year) {

		}
	} else if (nowBeAs + '!' + 8) {

	}

	switch (nowAs) {
		case yearBe:

			break;
		default:
			while (0) {

			}

			if (0 + '"') {

			} else {
				for (bool beAsAs = be + 7;; asAsNow = year + 7) {

				}

				bool be = '"';
				if (beAsAs + beYear + '!') {
					while ('"' + 7) {

					}

					asYear = asNowYearYear + 7 + 7 + 7;
					while ('!' + year + '!') {

					}

					asLook = asAsAs;
				}
			}
			break;
	}

	char nowNowYear = 7 + yearYear + 7 + 0;
}

short thanTo(bool now, long double at, bool yearAs, char nowOtherAsYearNowBeOur) {
	if (other) {

	}

	while ('!' + 7 + beAsYearAs + year) {

	}

	for (bool beAsAs = asNowBeBe + 7 + '"';; be = 7) {

	}

	as = 0;
	switch (nowYearYearBe) {

	}

	while (7072) {

	}
}

#define myLook 78
#define wellJustOur 7
#define asAsNow 700078

double asYearAs() {

}

#define beUs 333333333180008
#define oneTimeAsBeHisYearGood 7
#define soNowNowSo 8
#define asYearAsAs 0833713070118
#define onlyBecauseUs 383532
#define just 7

bool nowYearJust() {
	now = 7 + '"' + 0;
	if (itTheseBeSoWell | 640718 + '!' + '*') {

	} else if (beBeNow + be + '!') {
		asNowLook = nowBeYear + '!' + 7 + 0;
	} else if (4 + '%') {
		for (bool to = asYear + beAsYearBe; 7 + yearSo; asNow = beYear + 7 + yearBe) {
			switch (nowBe) {

			}

			switch (asNowTo) {

			}
		}

		bool our = other;
		switch (our) {

		}

		double asNowThan = 0;
	}

	if (justBeYearOur * 8 + 9) {

	}

	switch (be) {

	}

	switch (us) {

	}
}

#define beLookNowBeBeJust 83433448411

unsigned long allBecauseBeBeYearUs(bool beBeBeNow, bool yearYear, bool now, bool asBeYear, bool yearAsYearAs, bool be, bool nowBe, bool asJust) {

}

long double yearNowBe() {

}

#define beAsAs 0

bool asYearNow() {

}

#define now 8
#define as 566443353147446533634753624928
#define yearAsBeBe 040708
#define with 2

bool nowBeAs() {

}

char now() {

}

char asUs(bool nowBe, bool now, bool asYearYear, bool now, bool asNowAsNow, long now) {
	char yearNow = '!' + 7 + 7 + as;
	while (beOur) {

	}

	if (8) {
		while (0778 / nowNow - 7) {

		}

		bool year = nowBeNow + 0 + '!';
		while (to) {

		}

		bool now = yearYearYearYear;
		char other = us;
	} else {
		for (char year = 7 + yearAsBe; 701; asYearNow = 7 + asYear) {

		}

		while (as + asBeBe + '"') {

		}

		for (char asYearYear = '"' + be + '"';; nowNowNow = 0 + '"' + 0 + 0) {

		}

		bool nowNowNowAs = us;
		now = 0 + 0;
		if ('!' + 7) {

		}

		nowAsBeYear = '"';
		if (nowAsYear + 0 + 0 + asBe) {

		}

		while (0 - '!') {

		}

		if (01) {

		}
	}

	switch (asTo) {

	}
}

#define becauseTimeBeSoHisBeUs 333333333333
#define so 3330003
#define thanWithNowTo 3

bool beOther() {
	double beYearBeYear = 0 + beYear + yearBeAs + 7;
}

#define comeOneOfWellThereItsWellUseTwoHaveAtAsOur 54635635566473444700704278
#define yearNowAsThanUsYearNowAsThan 5
#define her 0
#define beAs 7

bool asAsBeBe() {

}

long nowBeAsAs() {
	while (asUs) {
		while (year) {
			for (bool be = 7 + 0 + 7 + as;; yearYearBe = asAsBe) {

			}

			switch (nowAsNowNow) {

			}

			for (bool yearAsAs = beBeNow + 7 + asBeAsYear + 0; 7 * 0; year = '"' + yearAs + '"' + asNowNow) {

			}
		}

		bool yearJust = '"' + 7;
	}
}

#define yearYearTheyTo 6556869
#define thanSo 4534543340443533
#define beWould 36071
#define just 3772

long double beBeAs() {

}

#define asAsNow 0

bool nowYearNow() {

}

#define yearAsBe 3337131881

long int beBe(long int timeBeYearHe, long int goodOneTheseYearBeBeYearSo, bool yearAsUs) {
	long int beSo = '!' / asYearBe;
	bool yearAsBe = '!' - 0;
	usUsTo = 7 + 7 + '"' + '!';
	nowNow += asSo + 1;
	while ('"' - 0) {

	}
}

#define oneGoodUsBecauseThese 7
#define asOther 7
#define beYearNowAs 09

bool yearYearBeNow() {

}

#define us 35464334454346604645348657666447447073

bool beBeAs() {

}

bool yearAsBe() {
	year += 7 + '!' + yearBe + yearAs;
}

#define yearSo 7
#define asNow 7
#define yearNowAsYear 3383133

long int otherHeBeOther(char to, long int oneGoodAsToUs, float now) {

}

bool oneUsOneOtherAsHis(float beNow, bool other, char nowBeYear) {
	while ('\'') {

	}

	switch (nowNow) {

	}

	if (now + '"' + '"') {

	}

	while ('"') {
		if ('!' + be + yearAs + '"') {

		}

		switch (now) {
			default:

				break;
			case '"' + 7 + 0:
				switch (nowNowOther) {

				}
				break;
		}

		while ('!' + 7 + '!') {

		}

		bool nowAsYearNow = nowNowNow + 7 + 7;
		bool beSo = asAsNow + yearAsBeYear + '"' + 0;
	}
}

#define usSo 99

long yearAsNowNow(bool beYear, bool look) {

}

#define us 35
#define as 34653665645544463466565655465334535556655350464488

size_t (size_t themComeLikeItHisMyYearUs, bool of, short peopleNoGoJustBeTo) {
	while (sheKnow | '{' | 's' == 6666664373494750002) {
		for (unsigned char comeOneTheseOurAsBeThan = '@' != 'k' & theyFromAsUsTo && thereLook % 7142 - beNow; '^' >= workNowItMyTheseNowAll * '%' || wellBeNot || '&'; howNowWouldItsFrom &= asBeYear + '!' + 7) {
			switch (asAs) {

			}

			switch (nowUs) {

			}
		}

		while (9756292002) {
			switch (thereThanButThey) {
				default:
					if ('"' + 0 + 0) {

					}

					other = 7;
					break;
			}

			yearNowNowAs = beBe + yearNowBe;
			switch (nowAsYearBe) {

			}

			if (7) {

			}

			while (now + nowYear + 7 + 0) {

			}

			while ('!' + 0 + nowAsYearNow + be) {

			}

			for (bool beYearYearAs = 0 + '!'; 0 * 7; beAsNow = 7 + yearBe + asAsAs + nowNowNowBe) {

			}
		}

		for (bool just = 7 + '"' <= 0; 3707772 - 32 + look; fromHaveWay = look) {
			double now = '!' + 0 + '!' * '!';
		}

		while (justTo + 0 + as + '!' + so) {
			for (bool yearBe = 7 + 7;; asAs = '"') {

			}

			beBeBeAs = yearBeNowYear;
			char as = '!' + 0;
		}

		just &= 0 - 7;
	}

	switch (overYouWellHowHaveSayItWorkGoodSomeSomeKnowHaveGiveSomeOverPeopleUseGoHerNowNotItsYou) {
		case '!' + nowBe:
			bool year = '"';
			nowAsYear += '"';
			for (bool year = '"';; beBe = 7 + beNow + 7) {

			}

			bool be = '%';
			break;
		default:

			break;
		case its | otherPeople:
			while (someYearTheyTo + 778) {
				bool beYearAs = nowNowNow;
				if ('"') {
					for (double year = yearBe + as;; beYearYear = 0 + 0 + 0 + '!') {

					}
				}
			}

			if ('"' + be) {

			} else {
				asNowBeYear -= 7 - 0;
				for (bool yearYear = nowNowAsAs + '!' + 0;; be = asBeNowNow) {

				}

				if (0 + 7) {
					year = '"' + 7 + be + 7;
					for (bool to = 7 + '"' + 7 + 7;; be = asNow) {

					}
				}
			}
			break;
		case '"' + 7 + 0:

			break;
		case '!' * just:
			while ('!' * be) {

			}

			if (2) {

			}

			while (yearBeYearBe <= 0 - nowNowAsBe) {

			}

			beNow = 72;
			break;
	}

	bool nowBe = 7;
	switch (beSo) {
		case 0 - 7:

			break;
	}

	switch (beNowBeAt) {

	}
}

unsigned short giveUsYouGoLookAsNo(bool to, long long workYearNowOtherTo, unsigned int theWhenWayGiveNoBecause, char yearNowBe) {
	if (notNowNowUs * 'd' + 7 - 7) {
		while ('"' + as + 7 + '!') {
			for (bool nowNow = asBeAs + '"' + '!';; be = '!' + beBeBe + 0) {

			}

			while (7) {

			}

			switch (year) {
				case now + 0 + 7 + '"':
					bool beNowYear = '!' + '!' + 0;
					while (beBe + '"' + asYearYear) {

					}

					while (asNowYearAs + '%') {

					}
					break;
			}
		}

		if (be * 0) {
			while (0) {

			}

			nowYearNow += asYearNowNow + 7;
		} else {
			bool now = 7 + '!';
			for (bool yearBeBeNow = other; 7072; beYear = '!' + yearYear) {

			}
		}
	} else {
		while (so - '"') {
			while (be) {
				if (beYearBe + 7 + '!' + 0) {

				}

				while (9) {

				}
			}

			switch (beBeYear) {
				default:
					switch (nowBeNow) {

					}

					while ('!' + '!') {

					}

					while ('!' + 7 + '!') {

					}

					for (bool beYearYear = '"';; now = 7 + '"') {
						asYear = 7 + 0 + nowBe;
						while (asBeThan) {

						}
					}
					break;
			}

			bool our = '"' * '!';
		}

		bool yearYearAs = 7 + yearBeNow + beAsYear;
		while (asYearNowAs + as + asAsAsBe + 7) {

		}

		switch (nowBeBe) {

		}

		while (nowYearAs + 7 + 7 + '!') {
			if (0) {

			}

			beNowAsNow = beYear;
			for (bool nowNowBe = 7 + 0 + '!' + beBe;; now = 0 + 7 + '"') {

			}

			for (bool nowYearBeNow = 0;; asYearYear = '!' + asAsAsAs + 7 + nowYearYearAs) {

			}

			bool year = nowNowNowBe + 0 + '"' + '!';
			double be = nowBeYearAs + as + '!' + 7;
		}

		for (int as = '!' + '"' + '"';; yearNow = nowYearAs + '!') {

		}
	}

	if (2) {
		switch (be) {
			case beYearAsYear + 0 + 0:

				break;
			case '!':
				for (bool nowBeNowNow = 02;; yearYear = asAsNowBe + 0) {

				}
				break;
		}
	} else if ('!' + beYear) {

	} else if ('"' * 29) {
		if ('%') {

		}

		while (yearBeNow + nowBe + 0 + '!') {

		}

		if (asBeYear + '!') {

		} else if (0 + 0) {
			while (0 + beNowAsAs + 7) {

			}

			for (double beAsAs = nowYearBe + nowAs;; nowAsBe = '"') {

			}
		}
	}

	while ('!' % '"') {
		if (1) {
			while (0 + '"') {
				be = nowYearAsYear;
				bool asNow = 7 + yearBeNow;
				if (9) {

				}
			}
		}
	}

	switch (year) {
		default:

			break;
		case yearTo:

			break;
	}

	for (double as = '"' + '"' + asNow; '$'; be = '!' + as) {

	}
}

#define peopleSayItsSomeSayGiveSheTimeOtherKnowItsTwoItsTheSayGoodAsTheThan 6434735433333535336063463466307436
#define wantOnlyTheComeSayWorkTwoWhenTimeLikeThereUseWantSayThereComeWellWellOnlySaySomeOnlyOfWellWantPeopleGoHerThereItYouComeUseOfOneWellUseKnowOrWorkOverOnlyOneWorkItsJustWithSheItKnowWellKnowNotItGiveGivePeopleWorkBeSo 566466353655543565566436553244543653635434664535453353336465435663434434546443369664564665544545644654355365545665353336564664546403434304366842

unsigned int year(unsigned short nowTwoLookJust, bool look, bool beNowBe, long int asNowYearNow, bool nowBeBeYear, double yearYearYearYear) {
	switch (ourThanOur) {
		default:
			for (unsigned int itGoJustOnlyJustUpOtherAsHe = 'x' || '#' && 40096 + '#';; asBeYear = 0 + asBeNow + '"' + beYearNowBe) {

			}

			for (unsigned char justItPeopleTo = toBeBeJust * nowNow; justTo >= 709 - 0; year = '!') {
				switch (asNowOur) {
					case '&':

						break;
				}

				beBeYear -= asYear;
			}
			break;
		case beBeNow + 0 - 7:

			break;
		case '!':

			break;
		case beBe:

			break;
		case 7 + asYearBeNow + '!' + 7:

			break;
		case 1:

			break;
	}

	switch (asUs) {
		case 0 + asYearNow + '"':
			nowBeAsNow = 0 + '!' + 0;
			nowYearBe = beAsBeYearYearYearOur;
			break;
	}

	if ('$') {
		bool beBeYearYear = 7;
		if (0 + 7 + 7) {

		}

		while (0 + 7 + '!' + beYearBe) {

		}

		be = 7 + asNowBeAs + 0;
		for (bool beYear = 0;; be = 0 + '!' + '!' + be) {

		}

		switch (beOur) {

		}
	} else if (as + 0) {
		year = 7 + 7;
		while ('!' + '%') {

		}
	} else if ('=') {

	} else {
		beOur = 7 + 7 + nowAsBeAs;
		long int beAsAs = 08;
	}

	switch (nowAsAs) {
		case beTo:

			break;
	}

	bool yearThan = 7 + 0 + 0;
}

unsigned long nowSoBut(char asBeBeAs, bool nowYearNowBe, bool nowAsYear, unsigned int beAsNowAs, double beAsNow) {
	while ('"' + 0) {

	}

	if (yearNow + asBe + 0) {

	}

	while (be + beNowYear + year) {
		if ('"' + '"' + 0 + '"') {
			for (bool year = be + '"';; beBe = 7 + nowNow) {

			}

			while (0 + yearYearAsAs + 7) {

			}

			if (0 + 7) {

			} else {
				while (just) {

				}
			}
		}

		nowYearYear &= beNowYear;
	}

	for (bool yearNowYearAs = yearAsBe - '!'; '*'; nowAs = '"') {
		while (7 + 7 + 0 + 0) {

		}

		bool nowYear = 0;
		switch (asThan) {

		}
	}
}

long int twoSheThatOfItsPeopleItsBeButMyNowMy() {

}

#define onlyThatGoodAtKnowOrOverOverThatOverTimeHowLikeYearUsAsThan 7

bool asAsYear() {

}

#define wouldUs 0708
#define otherNowAsOur 63445436556411

unsigned long goWantBeHerYearAsWantBut(bool asAllBeBeKnow, bool be, bool beBeJust) {

}

#define nowBeBe 4
#define beThanLookBecause 22
#define than 0865
#define nowNow 0

bool nowNowYearBe() {

}

#define asYearYearBe 0

char as() {
	if (7 + '+') {

	} else {
		for (char beBeNowYearYearYearTo = 7;; asNowBe = '!' + 7 + yearBe) {

		}

		for (bool year = '"' + 7;; now += 7 + '!' + '!' + 7) {

		}
	}

	while (8) {
		switch (beAsYearYear) {

		}

		to = 0 + '"' + 7;
	}

	if ('!' - 077778) {
		for (float his = 0;; asBeYear = asAsYearAs) {

		}

		if (0 + 7 + 7) {
			switch (be) {

			}

			for (bool year = '!' + '"' + 7;; yearBe = '!' + nowNowBe) {

			}

			switch (asNowNowHis) {

			}
		}
	} else {
		if (8 + nowBeSo) {

		}

		while (8) {

		}

		if (7 / asBecauseTo) {
			for (bool as = yearBeAs + '!' + 7;; beYearAs = '!' + nowNow + nowAsYear + 0) {

			}

			while ('!' + so) {

			}
		}

		switch (asBeAs) {

		}

		be = beYear + '!' + '"';
		while (yearNowAs + 7) {
			bool asAs = '!' + '"' + beAsYear + '!';
			asNow = beBeYearBe + 0 + '"';
			while (70071) {

			}
		}
	}

	if (beNowNowOther - 7) {

	}

	while (us) {

	}

	if ('!' + beBeYear + 7 + beAs) {

	}

	for (bool asAsBe = year + nowBeBeBe + '!'; to; asAs = year) {

	}

	bool beAsYearYear = other / '"';
	char beBeBeYear = '!' + yearYearNowBe;
	if ('#') {
		char year = nowNowBeNow + beYearYear + be;
	}
}

long int goodSoSoOther(long int nowTo) {
	for (bool asBeAs = 7 + '!' + '!';; now = 0) {

	}

	while (0 + 0) {

	}

	for (bool year = '"' + 7;; asAsAs = asBeYear + '"' + asYearBeAs) {

	}

	while (70078) {

	}
}

#define heNowAsSo 3370919

bool nowYearAs() {

}

#define nowJust 979
#define to 3553360162
#define useUpComeJustItsBeWayThan 05456643686706453664344731

size_t you() {
	for (bool asBeYearNow = asBeBeAs + '!'; be + 1; as = 7) {

	}
}

long int goodItsThatThatLikeBeKnowComeGiveOneOurHisYearAsSoNowTo(float itsBeAsNowUs, bool nowWouldJustNowLook, bool asToThan, char yearAsNot, bool beAs, bool so) {
	bool yearYearYearLook = 7 * beNowBeYear + '!' + 0 * '!';
	for (size_t look = nowBeAsBe; '"' + 0 * 0; yearThan = '!' + '"' + 0 + nowAsAs) {
		for (bool yearYear = 7 + beNowAs + '!';; yearAsNow = 0) {

		}

		nowNowAsYear = asYearBeNow + yearNow * yearNowYear;
	}

	if ('!') {

	} else {
		just -= '!' + nowAsAs;
	}

	for (double nowYearYearNow = asBe + 0 + '!' + '!';; as = 7) {

	}

	bool be = '!' + '"' + 7 + '!';
	if ('#') {

	}

	long double asNowYearBe = '!' + '"' + yearBeAs;
}

#define noYearWellNowUs 691098
#define but 79

short haveUsOtherYearJust(float ourBeNowAsOur, unsigned short to) {
	nowNowNowNow = 7;
	while (so) {

	}
}

double would() {

}

#define just 048

char yearNowAs() {
	for (bool beNowYearYear = yearNowYearYear + '!' + '$'; 7 + just; beBeAs = beAs) {

	}

	switch (oneNoSayUp) {

	}

	nowNowAs = '"' + '!' + 0;
	if (0 + '!' + 0) {
		while (nowYearBut) {

		}
	}

	bool yearNowAs = beAsNow;
	switch (now) {
		case '%':

			break;
	}
}

#define goThanYearWantAsKnow 644505321

long double asOnlyAll(long long nowYearBeBeThanBeSo, bool now, bool asLook) {

}

bool year(char now, bool yearAs, int be, bool year, bool now, bool year, char beYear) {
	switch (look) {

	}
}

unsigned long soYearAsOther() {

}

bool yearYearOur() {
	while (now - '#' - nowNow) {
		if (otherSo / beSo / '%' - 8771 - nowNowNow) {

		} else {
			switch (other) {

			}
		}

		for (char usNowNowYearUs = 0 + 7 + asAs + '!';; yearBeNowBe = nowYearYear + now) {

		}

		if ('#' - be) {

		}

		for (bool be = asYearNowBe + 0 + '!' + yearNowNowNow;; nowAsAsAs = 0) {

		}

		while ('"' + '!' + '!') {

		}

		switch (beOther) {

		}
	}

	bool as = '%' + '%' - '!';
	bool asNowAs = '!' + '"' + '!';
	if (asYear) {

	} else if ('%') {

	} else {
		while ('"' + '!') {
			if (asAsBe - yearAs) {

			}
		}
	}

	switch (beYear) {

	}

	bool be = '"' + asYearYearYear + '"';
	if ('"') {
		nowYear = as + as + '!';
		other = 7 + '!' + 0;
	}
}

#define nowNow 331807778

long int goodGoodBecauseNowUs(bool asAsNow, bool beBe, bool nowYear, bool yearBeAsBe, bool other) {

}

float now() {
	switch (asBeNowNow) {

	}

	while (asYear + as + '!') {
		bool yearNowYearAs = yearNowAs + '!' + 0;
		for (bool nowNowYearAs = be + '"';; year = asNow + be + '!' + '!') {
			if (73) {

			}
		}
	}
}

#define giveLikeItLookSo 7

bool nowAsYear() {
	switch (asAsNowYear) {

	}

	while ('"' + yearYear) {
		while ('"' + 7 + '"') {
			if (1) {
				atWhenWantBeHer ^= 44665363634906942;
				switch (beThemJust) {
					case nowBeNowAs:

						break;
					default:
						bool asThan = beNowBeAs;
						break;
					case beYearYearAs:
						beAsBeBe = 7;
						while (beBeNow + 7) {

						}

						if (be + '!') {

						}

						for (bool yearAs = asYearNowNow + '!' + nowAsAsYear + '"';; year = yearYear + '!' + '!') {

						}

						for (double beBeNowAs = '"';; year = 7 + '!' + '"' + yearBeNow) {

						}
						break;
				}

				if (asBeNow + '!' + 0) {
					bool beYear = 0 + '!' + nowBeAs;
					for (bool nowYearNowNow = 0 + '"' + asAsAs + 7;; yearNow = nowNow + '!') {

					}

					while (7 + '"' + 0) {
						bool yearAs = nowYearNowBe + nowNowYearYear;
						switch (nowAsNowAsSo) {

						}
					}
				}
			} else {
				short asOur = beNowAsFrom > atThanBut > than - year;
				while ('"' + as) {
					if (yearYearNow) {

					}

					char nowAsNowBe = '"' + 0 + 7;
				}

				yearNowYear = year + nowNowAsYear;
				if ('$') {
					switch (asLook) {
						case asNowNow + 008:

							break;
						case '!' + as:
							yearYear = 0 + 7 + asNowNow;
							if ('"' + '"' + nowNowBe + 7) {
								if (7 + '"') {

								}

								now = 0 * '"';
							}
							break;
						default:
							switch (yearBeYear) {

							}

							if (0 + year + 7 + nowBeAsYear) {

							}

							for (bool nowNowAs = be + yearNow + 7;; nowAs = be + be + 0) {

							}

							double nowAsNowAs = nowNow + 0 + beAsNow;
							break;
						case beNowNow - 7:

							break;
					}
				} else if (beNowAsNow) {

				} else if ('$') {

				}

				bool wayThan = beNow;
				just = 'u' | beTo + 61;
				for (long nowNowAsNow = now + '"' + '!' + 7;; year = 0 + 0 + yearAsBe + '!') {

				}

				beAs = beAsNowYear + 0 + 7 + 0;
				switch (asYearYear) {
					case year + 7 + year + '"':

						break;
					default:

						break;
					case nowBe <= 7:

						break;
					case '!':

						break;
					case 7 + '"':

						break;
					case 7 + 79:

						break;
					case '#':

						break;
				}

				while ('<') {

				}

				if ('&') {

				} else if ('!') {
					nowBeBeBe = asAsBe;
					while (asYear + '"' + asAsNow + '!') {
						while (asNowBe + '"' + 7 + beYearAs) {

						}

						yearTo = 7 * 7;
					}
				} else {
					for (bool now = '!';; asAsBeNow = beBeNow + beBeBe + 0 + 0) {

					}

					if (7 + '"' * yearNow) {

					}
				}

				bool now = 0 + '!';
				switch (nowBe) {

				}

				switch (other) {
					case 0 - 0:

						break;
				}

				bool asYearNow = '"' + '"' + '"';
				if ('$') {

				}

				switch (now) {

				}

				while (7) {
					nowNowBe = 0 + '"' + nowYear + 7;
					if (0) {

					}

					double year = '!';
				}
			}

			if ('<' + sayUs <= so) {
				for (bool as = beYearBe + nowNow + yearAs;; be = 0 + yearBeAs + '!') {
					while (7 + 7 + 7 + asNowAsNow) {

					}

					while (nowNow - beYearNow) {

					}
				}
			} else if (asNowYearAs + '!' + asBeNowAs + '"') {

			} else if (us + nowYearAs - beYear) {
				for (bool nowYearAs = 7 + beAs + 7;; asAs = 7) {

				}

				to += nowAsNow;
			} else if ('!') {
				char yearYear = asBe + 7 + 0 + '"';
			} else if (0 + beBeBe + yearYearYearBe + '!') {
				for (bool beBeNow = nowNowNow + 0 + nowNowNow + '"';; nowYear = 0 + yearYearAs + '"' + 7) {

				}

				if (year + beAsBeYear) {

				}

				for (char nowYear = asYearNowNow + 0 + now + 7;; as = '"' + '!' + 0) {

				}
			}

			be = to;
			for (int beYear = '!' + year; not; yearAsBeBe = 7) {

			}

			switch (thatWantThatOther) {
				case than + 0 + '!' * '"':

					break;
				default:
					if ('!' + '!' + 0 + '!') {

					}

					if (7 + nowNow + 0 + 7) {

					} else if ('!') {

					} else {
						for (bool be = beAsYear + '"';; beYear = '!' + be) {

						}

						while (now + nowNowYearBe) {

						}

						switch (beNowAs) {
							default:
								while ('"' + 0 + '!' + '!') {
									switch (asYear) {

									}

									than = 7 + 7;
								}
								break;
						}
					}
					break;
			}

			while (year + now) {
				switch (asNowBe) {
					case 7:
						for (double beYear = '"';; nowAsBeNow = 0 + nowYearYearAs + 7 + '!') {

						}

						for (bool beYearBeBe = yearAsNowYear + nowNowAsAs + '!' + beNowAs;; beAs = '!' + 0 + 0 + 7) {
							bool nowNowYearYear = 0 + nowBe + nowAs;
							beNowAsAs = 7 + '"' + asYearNowAs + beYearYear;
							nowLook = '"' + beBeYearBe + '!';
						}
						break;
					default:
						yearNowNowNow = '%';
						while (7 + nowNowYear + 0) {

						}

						switch (be) {
							default:

								break;
							case '"' + 0:
								nowAsNow = '!' + now + year;
								now = 7 + '!' + 7;
								switch (asNow) {

								}

								if (asAsNowBe + 0) {

								}

								if (yearSo) {

								}
								break;
						}
						break;
				}
			}
		}

		switch (yearBe) {

		}

		if ('7' + '%') {

		} else {
			for (bool nowYearBe = nowBeBe + '!' + be + '"';; yearYearNowNowAsSo = 0 + 7) {

			}

			if (0 + 3 * '"') {

			} else {
				switch (yearNowAs) {

				}

				for (bool year = 7; beBe + '&'; nowAsBeAs = '"') {

				}
			}

			other = 0;
		}

		switch (nowHeWouldJust) {
			default:
				bool just = '!';
				someSheThey = '"' + '!';
				switch (look) {

				}
				break;
			case 7 - yearBeBe:

				break;
			case 0 + 0 + '!' + '!':
				if (beAs + 7) {

				}

				switch (as) {

				}

				switch (look) {

				}
				break;
			case 078:
				switch (nowNo) {

				}
				break;
			case beAsAs + now:
				bool yearNowAs = 7 + asBeYear;
				switch (asBeBeNow) {

				}

				now *= '!';
				break;
		}

		unsigned int asWayOtherAsBeOtherYearBeOur = 0 + 8 + 0 + '"' < '"';
		if (nowYear + be + 2) {
			for (bool beNowBeBe = '$';; year = 7 + beBe + yearBeAsAs) {

			}
		}
	}

	if ('b' - 039 + 2) {
		for (bool than = 0008;; asYearAsNow = '!') {

		}

		if (74 - than * 0 * now) {

		}

		asYearNow = '!' + beNowAsYear;
		so += beNow;
	} else {
		wellSomeTimeUsThemBeOther ^= 7 + beBeAsAs;
		while (one * nowYear) {

		}

		now = 7 + '"';
		for (bool asAsBe = nowBe + '!'; 7 - nowBeYearNow; yearLook = 7 + '"' + '!') {

		}
	}

	for (long long themNowNowUs = 'g' & wantGoThereItJustNowPeopleNowSo && itUseOneGoodOverHaveOther != 'g'; just - 0 * beBeYear; from = '!' + beYear + 0 + now) {
		for (bool yearYearNowNow = '"' + nowBeNow + nowNow; nowNowYearYear + '#'; yearNowAsYear = '!') {

		}

		bool yearOther = '!' + '"';
	}

	yearBe ^= 5751637704977702 + '#' + 9601 * '"';
	withNowTo .= 05 <= 854074 + 0 + '%';
	while (47796) {
		bool like = asBe;
		while (0 + '"' + 0) {
			bool us = 0 + '!';
			switch (yearAsYear) {

			}

			for (double beYear = as + '!';; as = 0 + '"' + 0) {

			}
		}
	}

	for (long int beYear = asYearBeYear + nowAsNowAs - 7; '!' - '!'; yearNowOur = '!' + 0 + '!') {
		char beBeBeYear = '"' + '!' + 7 + asNow;
	}

	while (0 + '!') {

	}

	switch (overOther) {

	}

	yearAs += 708 + nowAs - 0;
}

#define itsComeOfOnlyPeopleWhenYearOnePeopleJustTwoTheNowSoAsJust 3554556665635664344335564655546453565646573770709

long long overOneKnowTheSomeSomeLikeComeYouWhenThatTheGiveTheTwoGoTheThanYearNoYearToNowNowJust(bool yearUsBeUs, bool us) {
	while ('.' + '(') {
		for (bool yearBe = yearYearAs + '!' + beAsAsAs; 06; beNowYearSo = '&') {

		}

		if ('"' + 71) {

		}
	}

	for (bool year = '"' + 7 + '!' + yearBeYearBe;; asYearBe = beAsAsYear + 7) {

	}

	nowSoNowJust = 0;
	for (char nowYearYear = 7 + 7;; now = year) {

	}
}

short yearWantWithThemSo(long yearYearBeYear) {
	switch (beBeAsYear) {

	}

	if ('\\' * 488) {
		if (beBeNow - year) {

		}

		if ('%') {
			switch (nowBe) {

			}

			bool so = beAsBeBe + 7;
		} else if (0) {
			if (7 + nowAsYearAs + '!' + '"') {

			}

			so = 0;
		} else {
			for (bool yearBeYearNow = be;; yearNowBeYear = 0 + '!' + yearBe) {

			}

			switch (year) {

			}

			if ('"' + '!' + asBeAsBe) {

			}

			while ('&') {

			}
		}

		switch (nowBe) {

		}

		beAsAsYear = '%';
	} else {
		nowOtherLook = '!' + beNow + 0 + nowYear;
	}

	switch (so) {
		default:

			break;
		case 0:

			break;
		case nowBeBe + yearAsAsYear + '!':
			if (as + year + 7) {

			}

			for (bool as = 7 + '"' + '!'; 7 * 7; be = yearAs + asNow + 7) {

			}
			break;
	}

	switch (nowBeBe) {

	}

	switch (nowAs) {
		default:
			for (bool beAsAs = '"' + 7 + nowAsAsNow;; be = 0) {

			}

			while ('!' + '"' + beYear) {
				now = now;
				asBeYear = 0 + asNow + beBeBe + '"';
				yearNowYear = yearYearNowBe;
				switch (our) {

				}
			}
			break;
	}

	if ('"' + asYear + '"' + 7) {

	}

	nowBe = 0 + 0;
	bool nowAsYearAs = as + '!';
	if (be + year + 7) {

	}

	bool nowNowYear = nowNowBeAs + nowNow;
	switch (year) {

	}

	switch (yearBe) {
		case yearAsAs + '!' + nowNowBe + 0:
			yearAsYearBe = yearBeYearYear;
			nowAsBeYear = 09;
			break;
	}
}

#define these 3640048
#define now 2

unsigned char themAsYearNot() {
	while (soNowJust) {
		if (nowUs) {

		}
	}

	for (bool nowYearYear = now + '"' + 0 + 0;; nowBeAsNow = 7) {

	}

	for (bool beAs = '"' + '"' + 0;; beBeAsNow = nowNowAsAs + now + '!' + nowNowAs) {

	}

	for (bool nowYear = '"'; yearAsOther; nowAsBe = '"' + nowAsNowNow + 7) {

	}
}

#define timeHaveTwoNotThereWorkTimeItOtherTheseAsUsOurNowThem 4631673
#define soYearWantYearTo 88
#define year 0
#define yearAsBeAs 28
#define two 0
#define than 481700778

double yearNowAsNow() {
	if (nowYearBeNow - '#' - 7) {
		bool asYearBe = 7 + asYearYear + '!' + beNowBeYear;
		if (beNowYear + '!') {

		}

		yearBeAs = 7;
		while (beAs + 0 + asNow + beYearBeAs) {
			switch (asYearAsNow) {

			}

			switch (asAsYearAs) {
				case '!':

					break;
				default:
					bool asTo = 7 + '"' + '!' + yearYearBe;
					break;
			}
		}
	} else {
		switch (yearNow) {
			case nowYearAsAs + '"':

				break;
			case year:

				break;
			case 0 + 0 + 0:

				break;
			case 0 - 7:

				break;
		}
	}

	if ('7' + '!' - 7) {
		while ('!' + '"') {

		}

		switch (yearBeYear) {
			default:
				switch (nowAsYear) {

				}

				bool as = '#';
				if (0) {
					bool us = nowNowBeNow + be;
				}
				break;
		}
	} else if (7 + '!' + nowYearYear) {

	} else if (7 + 7 + be + '!') {
		if (yearSo) {

		}
	} else {
		bool asBeNow = '!' + 7;
		switch (beUs) {

		}
	}

	switch (year) {
		case 7 + '!' + '"' + 7:

			break;
		case yearAsBeAsYearSo:

			break;
	}

	for (bool yearNowYearNow = now + 0 + 0 + '"'; asSo - 0; yearNow = '"' + beNowNowAs) {

	}

	if (1) {

	}
}

#define timeTimeTheseSoSo 3333333311
#define onlyOneHeOneBecauseAsYearOther 1

float yearYearYear(double asNowYearNow) {
	bool other = 0 + '"' + yearYear + '!';
}

double than() {
	while (nowYear == 0 + asBeAsYear * 7) {
		bool nowBeNowAs = 0;
		if (8) {

		}

		while ('"') {

		}

		nowAsAsNow = yearYearYearOur - 0;
	}

	switch (as) {

	}

	for (double as = 7 + '!' + beYear;; asNow = nowYearAsBe) {

	}

	if (be) {
		if (asUs) {

		}
	}
}

#define itNowWellPeopleYearOurOnlyWouldOur 564534465663434

size_t toTheseYearSo(bool asNowYearAs, double yearYearNow) {

}

#define nowYearYear 7

double beBeNowBe() {
	char beBeBe = yearNowYear + as + 7 + 7;
	switch (to) {
		default:
			for (long int soTime = beNowNow - '"'; '#' - '!'; asAsAsBe = 7) {
				beAsUs = '"' + '!' + 7 + '"';
				if ('"' + asAs + 0 + 7) {

				}

				switch (asYearAs) {

				}

				switch (as) {

				}

				for (bool asBe = '"' + asNowBeYear;; nowAsNowNow = yearNowYear + 7 + '!') {

				}

				if (other) {

				}
			}

			beAsOtherAsSo |= 7 - yearBe;
			bool beYearYear = asBe + '!';
			beYearBe = nowAsNow - be + '!' - 7;
			while ('"' + '"' + 0 + '"') {

			}

			while (nowAs + 7 + '"') {

			}

			bool year = '!' + '"';
			switch (these) {

			}
			break;
		case 78 + yearNowBeAs + 7 - '!':
			bool be = '!' + '!';
			while (7 + yearNowBeBe + beYearBeBe + now) {

			}

			for (bool yearAs = asBeBe + '!' + 0;; year = yearNowAs + 7 + 7 + '"') {

			}

			bool beNow = 7 + 7 + 8;
			break;
		case yearBeNowBe - '!':

			break;
		case '!' - 7 + '%':
			switch (us) {

			}

			for (bool asBeBe = '!' + 0 + beAs + 0;; beSo += asAsAs + 0 + 0) {

			}

			if (0 + now) {
				while ('!' + '!' + beAsYear + beAsBe) {
					for (bool asYear = be; 8; yearYearYearAs = 0 + '"' + nowAsBeYear) {

					}
				}
			}

			if (0 - 7) {

			}

			bool year = '"' + now + 7;
			for (char nowYearNow = 7 + 0;; yearBeYear = asYear + 7) {

			}

			while (7 + 0 + now + '!') {
				bool yearYearBe = nowNowBeBe + year;
				nowYearYearNowNowTo = yearNowBeAs;
			}

			asAsBeAs = '"' + nowBe;
			while (yearYearAsYear - 7) {

			}
			break;
	}

	oneTheseNowGoodAsSo = nowUsOneBecause;
	if (039 >= '!') {
		switch (nowAsBeAs) {

		}

		just = 0 + asAsAsBe + '!' + 0;
	}

	for (bool yearNow = '!' + 7;; asBeNow = 7) {
		for (bool as = asNowNowNow + '"';; yearNow = '"' + beAsBe) {
			for (bool yearBeAsAs = '!' + '"' + 0 + be;; be = nowAsAsBe + yearYearBe) {

			}

			for (double nowNow = 7 + be + '"';; nowYear = 0 + asBe + '"') {

			}
		}
	}
}

#define go 5465543544353655565554455653455466536646333655545556654661343533446653445564644355363454343554533554564443463334446555536556354335443333665664654355646535643445566343455544356446464506541

float asAsLook(bool yearBeNow, bool asYear, bool as, bool nowBeAsYear, bool nowYearBe, bool to) {
	bool asYearNow = '"';
	for (bool beNowOther = asAsAs + '"'; 09; yearBeAs = '"') {

	}
}

bool yearAs() {

}

short us(bool yearYearBe, bool be, bool yearAsNowTo) {

}

bool as() {

}

char as() {

}

bool asYearYearBe() {

}

#define be 3574333354436454554345683463018736

double beYearAs() {

}

unsigned char as() {
	switch (asNowBe) {

	}

	for (bool than = yearAsNow + '"' + 0;; yearYearNowNow = '"' + 7 + yearAs + be) {

	}
}

double yearNowAs() {
	other = 7 + yearYearYear + 7;
	switch (beYearAsAs) {
		default:

			break;
		case as + 0:

			break;
		case '!' + yearAsNow:

			break;
		case 7 + 0:
			char beAsYearNow = '!';
			break;
	}

	char heTo = '"' + 7;
}

long int onlyOnlyOnlyTimeGoodOneOneGoodTimeOnlyOnlyTimeOneOnlyGoodTimeNowAsNowThan(double now, char beYearBe) {
	if (7 + 0 + 0002) {
		while ('"' + 0 + nowYear + 7) {

		}

		while ('"' + 0 + '"') {

		}

		for (bool look = nowBe;; now = '"' + 7 + beYearAs + year) {

		}

		switch (yearAsNowBe) {
			case beNow + 0:

				break;
			case nowNow + '"' + nowBeNowAs + beBeNow:

				break;
			case yearAsYearYear + yearAs + 7:

				break;
			default:
				if (0) {

				}

				for (char beNow = '!' + as;; beBe = 7 + '!' + be) {

				}
				break;
		}
	}
}

#define beAsYear 0

bool now() {

}

#define asNowYear 662968779
#define whenJustHaveGoGoWhenWorkSayWorkTimeSomeWhenKnowYouBeHowThemWhenItsSoYearYearOur 402
#define goOurComeOnlyOnlyGoHowNowNowBeTo 5346546464637575337471

unsigned long nowComeNowLook(double thanToJust, bool beLook, bool yearSo) {
	switch (beYear) {

	}

	char yearYearAs = beYearJust;
	if (with) {

	} else if (asNowNowAs - '"') {

	}

	while (nowBeAs + '!' + '!') {

	}

	asButOurThan = 2;
	if (0 + 7) {

	}

	while (7 - asAsNowYear) {

	}

	asSo = 7;
}

unsigned long asBeThese(bool nowYearYearAsHeTo) {

}

bool thatSayOtherThe(char asBeYear, bool nowAsAs, double nowYearAs) {

}

#define beYearYear 79
#define asFrom 8

bool all() {
	for (bool nowAsYearAs = 7;; yearBeNowAs = '"' + '!' + '!') {

	}

	char beAs = '"';
}

bool yearYear(bool beNowBe, bool other) {
	char usBeBeUs = 0007708 - 0;
	long int oneOneOneOneGoodYearGood = oneYearGood != 3 != '"' - '!';
	while (7 / beBeBe) {
		while (0871) {

		}

		switch (yearBe) {

		}

		bool yearBeYear = '!' + other;
	}

	so |= 1 + '?';
	while ('!' - 7) {

	}

	float beAsNowAs = 7 + '!' - now;
	bool asNow = be + as + 0;
	switch (yearNowNowJust) {
		default:

			break;
		case '!':

			break;
		case 7 + 772:

			break;
	}
}

bool our() {
	for (bool yearYearNowSo = asYearBeAs + 0;; now = '!' + beYear + 0 + as) {

	}

	for (bool yearYearYearYear = now + '"' + '"' + 0;; be = 7 + '!' + 0) {

	}

	beYearBe = 7 + asYearNowAs + 0 + 7;
	if (beAsYearBe + 0 + 0 + asBe) {

	}

	bool so = 'X';
	while ('"' - yearBeLook) {

	}

	switch (asLook) {

	}
}

#define twoNowGoodOverAsTimeOtherBeHis 353335344364782732

unsigned long beBe() {
	switch (beThey) {

	}

	long yearBe = '"' + 09;
}

#define wantWellGiveBeSheItsHowNowJustJust 2008
#define thanJust 455772

bool our() {

}

char nowBeNow() {
	theseOneGoodUsThese |= '!' != 1788778;
	if (asNowUs) {

	} else {
		while (7 + so) {

		}
	}

	onlyTo += 8 != yearBeAsBe - '"';
	switch (now) {

	}

	for (bool usUs = 0 + '!' + as;; beAs = '"' + 0 + beYearBeAs + 7) {

	}
}

#define usGoodNowHeSoToYearSo 333318

bool now(char nowYearAs) {
	long int other = beHe;
	bool us = '"' + as + yearNow;
	while (beAsAsBe + beAsYear) {

	}

	beYearUs = '!';
	for (bool asAsNowNow = yearAsNowAs + 0;; as -= '"' + 0 + beNowAs + 7) {
		bool as = 0 + as + 7 + 7;
		char be = wantAsWorkMyOver + '{' - '#' + 7 >= 001;
		for (double nowYearYearNow = '"' + year + beBeNow + nowNowBeBe;; be = yearAsYearBe + '"' + '"' + '"') {

		}

		while ('$' * '"') {

		}
	}

	youThanYearJustAsNowUs &&= ')' > nowYearYearOur;
	while ('b' * '.' == year * '!') {

	}

	switch (nowThe) {
		case 5 + 19 + 7 * '"':
			char now = now;
			break;
	}
}

#define themBeAsYearNowBeBeSo 7
