#include <UUUUUUUEUUV2/9/1//1.h>

#define be 8

bool nowYear() {

}

bool yearNow() {

}

bool year() {

}

#define asYearAs 2

double nowYearYear() {
	char to = 0 + beYearNowYear + 0;
	while (6426416 && ourLook >= '"' + asYear < 0) {
		for (long double yearNowYear = '!' + 0 + 7; 049 * '"' <= '!'; heHow *= '$' + 0 * yearYearNowBe) {
			if ('!') {

			} else {
				for (bool asBeBeNow = 7;; so = 7 + '"' + yearAsAs) {

				}
			}
		}

		for (unsigned long nowThanThanAsYearYearYearJust = beAsNow + 7 + 0; because; asBeYear = 7 + '"' + nowNowBe + 7) {

		}

		switch (whenOther) {
			case beYear:

				break;
			case beYearNow + yearYear:

				break;
			default:

				break;
			case at / 0:
				bool nowYearAsAs = nowNow + nowAs + year + '"';
				yearAsNow = 0 + asYear + '!' + '"';
				beNowYearAsTo = asBeAs;
				break;
			case 7 + '"' + nowAsAs + '"':
				nowAs = nowYearYear;
				switch (now) {

				}

				bool beBeBeAs = '"' + year;
				if (yearAsNowYear + nowYearNowNow + asYearAs + '"') {

				} else if (7) {

				} else if (0) {

				} else {
					switch (yearNow) {

					}

					while (asYearYearNow + '#') {

					}
				}
				break;
		}

		just += 7;
		switch (them) {

		}
	}

	asNowBeNow += beAsNowBe + yearBe;
	if (goWouldHow != 64 && 'e' == nowAsNowNowAsHe && 'f' + 6) {
		while (asAs + '"' + 0 + be) {
			thanFrom = 0 + '!' + '!';
			now = '"' + asBeAsYear;
			if (yearYearAs && 84779) {

			}

			while (7 + now + asAs + '!') {
				for (double as = '"' + 7;; now = '!') {

				}
			}

			beYear = yearBeNow + '!' + '"';
			nowBeYearBeJust += 7 * '"';
		}

		switch (be) {

		}

		be = beYearAsBe + asNowNow + '"';
		while ('"' + as) {

		}

		switch (toUs) {

		}

		switch (beBe) {

		}

		while ('"' + as + 0 + '!') {
			while ('"' + 7) {

			}

			while ('!' * '!') {

			}
		}
	} else if (asNowNow + 7) {

	} else if ('"' + beYear + 7) {

	} else {
		otherUs = yearAsBe - 7;
		asAsYearYear = 0 + '!' + asAsYearYear;
		switch (our) {

		}

		beAsAs = 7;
		while (0 + year) {

		}

		while ('!') {

		}

		while (7 + '"') {

		}

		switch (nowAsAs) {

		}

		nowAs = 7;
		switch (nowBeJust) {

		}
	}

	unsigned long oneNoWorkOtherJustUseUpNo = 'r' + 461289;
	itsYearWouldThemThan = '"';
}

unsigned int (double soBeBeBeBeNowSo, short someNotThanWhenUs, bool beOtherThan, bool heAsJust, bool beYearYearAs, long nowTo) {
	switch (useSomeWhenWhenGiveThereGiveWellGoTimeMyOurOtherBeBeThan) {
		default:
			switch (yearYearJust) {
				default:
					while (0 + 7 + 7) {
						if ('#' + yearBe + 7 * 7) {

						}
					}
					break;
				case 1 - '#' + 9:
					switch (nowBe) {
						case '!' + now:

							break;
						case '"' + '"':

							break;
						case now + now:

							break;
						default:

							break;
						case '!' + nowBeYear:

							break;
						case 7 + 7 + '!':

							break;
						case nowAsYearYear:

							break;
						case beNow + '"' + 0:

							break;
						case now + '#':

							break;
					}
					break;
			}

			switch (asYearYear) {
				default:
					if (asBeBeNow) {

					}

					asNow = 0 + '!';
					switch (beAs) {

					}

					while (nowAs + 7 + '"') {
						beYear = '"' + 7;
						bool now = asYearNowYear + '!' + '"' + 7;
						for (bool asAsAsNow = yearBe + '"';; yearNowYear = '"') {

						}

						if (7) {
							bool beNowBeNow = year + beAsBeBe + '"' + 7;
							for (char asBeNow = 7 + yearAsBeNow + '"' + '"';; nowBe = nowNow + '"' + '!') {

							}
						}
					}
					break;
			}

			while (year + '"' + 0 + yearAsNow) {
				for (double nowYear = now;; asYear = 7) {

				}
			}

			if (as + 0 + 7 + beBe) {

			}

			switch (as) {

			}

			for (bool look = 0 + year;; beAs = year + '!') {
				if ('"' + 7 + as - 0) {

				}
			}
			break;
		case soAsOur || yearNow + ourBeBeYearYearBeSo:

			break;
		case 0 + nowAs:

			break;
		case 39:
			bool beOur = beBeYear + 7 + 0 + 7;
			break;
		case beNowTo - 7:

			break;
		case asNowYearBe + 0 + '!' + nowAs:

			break;
		case '"':

			break;
		case '"' < year:

			break;
	}

	switch (knowOverNotHerWorkItsTimeThatComePeopleOfOrOfOneSoGiveTheOur) {
		case atJust:

			break;
		default:

			break;
		case '!' + '"':

			break;
		case beNow * 0:
			bool yearAsNow = 7 + '!' + beYearAsBe;
			for (bool yearBe = asNowAs + '!';; yearAsYearBe = '"' + '!') {

			}

			switch (nowYearAs) {

			}

			for (bool asYear = now + 0 + '!' + '"';; yearBeJust = 0 + 7 + '"') {

			}
			break;
	}

	if (howItsJust > than) {
		asSayThanYearNo = nowAsNow + beNow;
	} else if (as) {

	} else if (7 + 7 + 0 + 7) {
		if (7 + '!' + '"' + '"') {

		}

		if (asYearNow * 7 + 0 * 0) {
			if (0) {

			}

			if (asAs - now) {

			}
		} else if (0 + 0 + 0 + '!') {
			for (bool asNow = 0;; nowNowNowAs = nowAsBe + nowAsYearAs + 0 + nowYear) {

			}

			switch (just) {

			}
		}

		while ('!' + '%') {

		}
	}

	for (bool beYearYear = '!';; asAsAsYear = '$') {
		if (nowThan) {

		} else {
			our = '!' + 0 + 7 + beYear;
			switch (now) {

			}

			while (0) {
				beBeBeAsLook = 2;
				while (7 + '"' + 7 + beNow) {

				}

				asAsNowBe = asYear + '%';
			}
		}

		if (0 + 0 + asBeAs) {

		}

		if (beAsAs + '%' + ')' - yearNow) {
			for (float asAsNowAs = 7 + '"' + '!'; 0 + '&' - 0; as = beBeAs + '"') {

			}

			for (double yearNowNowNow = '"' + '!' + as + be;; beAsBe = 0 + year + as) {

			}

			if (yearYear + '!' * 0) {

			}
		} else {
			switch (asAsNowBe) {
				case '&':

					break;
				case '"' + 7 + '"' + '!':
					while ('!' + '!') {
						while (0 + nowYearAsNow + 7) {
							switch (nowBeAs) {

							}

							bool asNowNow = be;
							switch (year) {

							}

							while ('!' + '!' + 7 + '!') {

							}

							while (78) {

							}
						}
					}
					break;
			}
		}
	}

	for (unsigned long beBeNow = notOther * '-' + 02 - 02;; look += yearAsNowNow) {

	}

	if (6163572 && '"' & '!' - '&' / '"') {

	} else if ('!' + '"' + 7 + 0) {
		while ('$' + 28) {

		}
	} else {
		switch (asYear) {
			case 9:

				break;
			case nowAsYearNow + yearYearNow:
				bool yearYear = '!' + nowAs;
				bool nowAsNowYear = 0;
				if (us) {

				}
				break;
			case asYearAsBe + '!':
				bool be = 0 + '!' + 0;
				if ('!' + beNowBeAs) {

				} else if (be + '"' + '!' + '!') {
					while (0 + yearYearBe + 7 + 7) {

					}

					switch (beYearLook) {

					}
				}
				break;
			default:
				while (yearNowBe) {

				}

				bool asBe = 7 + beNowNow + nowBe;
				switch (beBe) {

				}

				for (bool asYearAsBe = asAsAsBe + beNow;; just = 0 + beBe) {

				}
				break;
		}

		for (char beNowYearYear = be;; as = nowAsAs + '!' + 0 + '"') {

		}
	}

	while (7 - 0 + '$' + '"' * '!') {
		while (0) {
			switch (beNowBe) {

			}

			while (beAsNow * now) {

			}
		}

		for (bool beBe = '!';; asNowYear = 7 + 0 + '!' + yearYear) {
			for (bool nowAsAsNow = be + 7 + '!'; '#'; yearAsAs = yearYear + 7) {

			}
		}

		double yearBe = 0 + asBe + '"' + 0;
		if (asBeBeYear + now + 0 + '"') {

		} else {
			while (7 + '!') {
				as = '!' + asAsNowNow + 0;
				if ('!' * yearYear) {

				}
			}
		}
	}

	while ('"' + '"') {

	}

	than -= ':';
	for (bool other = '"'; 0 * 0; as = '!' + 0 + beAsAs + 0) {

	}
}

unsigned int itsWellGoTwoLikeOnlyThanOrOnlyWhenThereOfHaveThereOfKnowHaveGoHaveHowPeopleWantOneItSheTwoUseUseTimeAsOurUsOur(bool haveGiveHowGood, int other, unsigned char thereNowUs) {
	long long onlyWellUse = 0 + '&';
}

short theyOnlyUseUpOrTheWorkGoodGoodUseAsHerSheHerOverHerGoThereOfNotUpUseOurThatOrBut(unsigned int toTwoKnowWhenHerWhenNowUseOtherNoThem, short asYear, bool beBe, bool ofOurHerYearAsToBeBeLook) {
	while (nowThey * 7 + '"' % nowToNowJust) {
		year &= 0 + '!' + '"' + asBe;
		while (2) {

		}
	}

	while ('"' + '"' + 0) {

	}

	bool asBeBeNow = 1;
	switch (asYear) {

	}

	if (0) {

	} else {
		look = 7 + now + 7 + beAsNowNow;
	}

	for (bool yearYearBe = '!' + '!' + '"';; asAsAs = '"') {

	}

	nowAsBeBe += '"';
}

double thanOur() {
	switch (beBe) {

	}

	while (7) {
		other = goWould * '!';
		while (998) {

		}

		for (bool year = 0 + '!'; no; nowYear = '"' + yearNow + yearAs + 7) {

		}

		if (beNow + 0) {

		} else if ('!' + yearTo) {

		}

		while (nowOther) {
			look = beYearBeAs;
		}
	}

	switch (wellWellHisLookThanBecauseYearThan) {
		default:
			nowJust = 7 + 0;
			if (993772) {
				float beBe = 0 + yearAsBeBe + '!';
			} else {
				switch (beYearBeNow) {

				}

				for (bool yearYearYear = '"' + asYearNow + '"' + 7;; just = '"' + 7) {

				}

				if (year + asAsAs + 7) {

				}

				if ('!' + than) {

				}
			}

			for (bool year = yearAs + '"' + '!' + 7;; asBeBeBe = '"' + 7 + '!' + 7) {

			}

			if ('$') {

			} else {
				yearAsAsAs = yearAsYear + '"' + yearBe;
				if ('"' + '"' + 0 + 0) {

				}

				while (0) {
					bool nowYearAs = '!';
					bool as = '"' + '"';
					switch (asYearOther) {

					}
				}
			}

			if ('!' + '"') {

			}

			if (7 + be + '"' + '$') {

			} else if ('&') {

			}
			break;
		case '%':
			while (so) {
				switch (asAs) {

				}

				switch (asAs) {
					case 7 + beAsAs + '"' + nowBeAsNow:
						beAsAs += '!' + 0;
						break;
				}
			}
			break;
		case 7 + yearNowBeBe:

			break;
		case 0 + 0:

			break;
		case yearBe + yearNowBeSo:

			break;
	}

	myNot = 0 + be + 0 + 7;
	asNowYearYear = 7 + as;
	nowNow -= yearBeBeNow + '!' + '!' + be;
	bool be = asAsNowNow + beAs;
	bool just = 78;
}

#define asAsBeThanLook 0336732
#define asNowNow 0

bool now() {

}

#define asYear 7

bool yearYearAs() {

}

#define nowNowUs 31330313

long int oneBecauseAsYearBeSo(long int oneOneNowYearYearHisBeBeYearAsNowOther, char onlyToUsAsAsYearThese) {
	while ('\'' + '%') {
		to += '!';
	}

	bool timeNowBeTheseOtherNowHis = '#' + 7 + '%';
	for (char nowNowYearYear = yearAs + beNowAsNow + 0;; be = be + 0 + 7 + '!') {

	}

	switch (asBeNow) {
		case '"' + be + '"':

			break;
		case nowAsAsNow:

			break;
		case 7:

			break;
		case nowBeNow + '$':

			break;
	}

	if ('!' + '"' + '!' + '"') {

	}

	for (double nowYearAs = '"' + as;; asNowBeYear = 0 + asYearBeNow + 7) {

	}
}

bool with() {

}

#define as 7
#define asOther 9
#define asYear 0

bool asAsNow(bool yearYearThan) {
	while ('@') {
		while (to % '!' + 7 - nowAsAsBe) {
			switch (yearUs) {
				case 5:
					yearUs = 7;
					break;
				case '&':

					break;
			}
		}

		if (0 + '"') {

		}

		switch (beYearBe) {
			case 7:
				as += 7 + 0;
				break;
		}
	}

	if (6 * '!') {
		for (bool asAsAsAs = '"' + yearYearBeNow;; year = '!' + '"' + '"' + '"') {
			for (bool beAs = '"' + 0 + as + 0;; now = '"' + nowYearNowAs + 7 + '"') {

			}

			bool asAsBeBe = '!' + beYearAs + 7;
			switch (now) {
				default:

					break;
				case 0 + '"' + 0:

					break;
				case 7:

					break;
				case nowYearBe + 7 + '!' + 7:

					break;
				case yearBeAsNow + 7 + 7 + '"':

					break;
				case nowYear + 7 + nowNowNow + '!':

					break;
				case asBeBeYear:
					if (7) {

					} else if (7 + '!') {

					} else if (be + '!') {

					} else if ('!' + yearYear + yearYearYearBe) {
						while ('"' - 7) {

						}
					}
					break;
			}
		}
	} else if (0) {

	} else if (nowNowAs + as) {
		bool nowYearNow = nowNowBe + 7 + year;
		if ('!') {

		}

		for (bool asYear = 0 + 0 + '!' + 0;; nowYear = '!' + 0 + 7 + yearYearBeBe) {

		}

		if ('!' + '"' + '!' + 7) {

		}

		if (asNowAs) {

		}

		while (look) {

		}
	}

	while (0 + 0 + nowYearYearYear) {
		nowYearBeYear = '!' - '!';
	}

	be *= asBeAsBe + nowAsYearAs + 7 + beBeAsAs;
	while ('!' + '"' + 0 + 7) {

	}

	while (beNowYearYear) {

	}

	just = '!';
}

int likeHowOverButBeTo(bool yearNowNowYear, char year) {
	for (bool asNow = asYearNow + '!' + 7; would <= 7 + '#' + 9; nowYearAsYear = 7 + 7 + 7 + 7) {

	}

	while (asNowAsYearBeThan) {

	}

	for (char yearNowNowNow = 0 + '"';; beNowNowBe = 0) {
		while ('#') {
			bool beNowYear = '&';
			switch (just) {

			}
		}
	}

	switch (asYear) {
		default:
			asAs += 0 + yearYearNow + asAsAsBe;
			break;
		case 7 + be + yearBe:

			break;
		case 7 + '"' + be + '"':

			break;
		case asAs + '!':

			break;
		case nowYearAs + 0:

			break;
		case 7 + '!' + '!':

			break;
		case nowYearYear:

			break;
		case 7:
			bool yearAs = '!' + '!' + '!' + '!';
			asYear = 0;
			if (now + 0 + '"' + nowYearAs) {

			}

			switch (nowYearBe) {

			}

			char beBeNowBe = '"' + '!';
			break;
	}

	char asAsYear = 7 + '"' + '"' + '"';
	for (char now = beNow + '"' + 7;; nowNow = beAsAs + '!' + yearAs) {

	}
}

long int becauseGoComeYouUseLookOurGoodOther(double itAllYearBeJust, bool nowBeYear, bool asYearAsNow, bool yearOther) {
	for (bool asNowBe = nowNow + 0 + '"' + '!';; yearBe = yearYear) {

	}

	int yearAsAs = yearAsBe + yearBe + '"' + nowYearYear;
	bool than = nowBe + beAsAsYear + 7 + nowAs;
}

#define use 2702

int than() {
	switch (beBeBeYear) {
		case 0 + beAs + yearAsAsBe:
			switch (beNowTo) {

			}
			break;
	}
}

char ourNowOtherLook(bool look, bool year, char now) {
	for (bool beOur = 0; 7 * 7; be = '"' + as + now + 0) {

	}
}

bool beBeYearYear(bool asAsYearAsNowSo) {

}

bool beNowYear() {

}

#define nowWould 46563655665346574841

long double asBeLook() {

}

#define now 336576

long int timeUseHisWay() {
	switch (asNowAs) {
		default:

			break;
		case '!' - '"':

			break;
	}
}

#define just 333333383333308871

long int hisBecause() {
	to = 7 - as;
	for (bool now = 0 + 7; '%'; beNow = 7) {

	}

	for (bool yearNowYearBe = nowAs + yearYear;; yearYear = nowNow) {

	}

	if (asYearYearYear) {
		other = '"';
	} else {
		if (8) {
			if ('!') {

			}

			while (yearAs + 0 + 0) {

			}

			if (0 + '"' + 0 + '"') {

			} else {
				if (yearNowNowAs - 0) {

				}
			}
		}
	}

	switch (so) {
		default:
			switch (to) {

			}

			beUs = '"' + 7;
			break;
	}

	while (7 + 0 + nowNowNow + 7) {
		while ('!' + as + beYearBeAs) {
			char nowAs = 7;
		}
	}

	nowBeAs = '"' - 0;
}

#define now 13

long int oneTime(float nowAsAs) {
	nowBeYear = 7 + 7 + year + 0;
	while (7 + yearAsBe) {

	}

	switch (asAsBe) {
		default:
			bool year = '"' - nowBeAs;
			break;
	}

	us = beNow + nowNowBe;
}

int yearBeBe() {

}

bool beYearNowNow() {
	switch (beAs) {
		default:
			beYearNow = yearAsNowNow + 7 + '!';
			beYearNowNow = '!';
			switch (asNowAs) {

			}

			while (asNow + '"' + '"' + 0) {

			}

			yearNowOur = 36602 < 91;
			switch (becauseTo) {

			}

			bool yearAsAsAs = beNowAs + nowNowAsAs + asNow;
			while (be + asBeYearBe + 7 - beNowYear) {
				for (bool now = year + 0 + '"' + '"';; yearYear = asNow + nowBeNowNow) {

				}

				asNowAsBecause = 0 + 0 + 7;
			}
			break;
		case '!':
			if ('.' * yearBeBe + 0 + 75) {

			}

			if ('"' + 712) {
				for (bool be = '"' + beBeNow + beNowYearYear;; nowNowNowAs = 71) {

				}
			}
			break;
	}

	for (unsigned char goOurTimeThatMyUsYouMy = '"' && ourWayAsSomeSo; '#' + yearYearAsYear * '!'; asNowYear = '!') {

	}

	while ('!' + 7) {
		double toBeNowAsNowSo = asAsAsYear + so;
	}
}

#define year 533833383313338333
#define timeNowGoodThese 3337333307733303
#define yearAsYearOther 1

bool yearNowNowNow() {

}

int beBeBe() {

}

#define beYear 53366535534545453543345572
#define likeToThese 7

double comeYearAsThanOurNowBeOur() {
	switch (so) {

	}
}

long asAs() {
	if (7 + 7) {

	}

	for (bool be = 0 + '!';; asYearYear = '#') {

	}

	if ('!' + yearSo) {

	}
}

#define now 8
#define goodOnlyToOnlyOnlyGoodOnlyOnlyOneNowSoOnlyGoodYearOneOnlyTime 33333337307071
#define nowSo 63
#define be 0

bool nowNowYear(bool now, bool as, bool beBeBeBe, double yearYear) {
	if (08) {
		double just = '!' + year + '"' + yearAs;
		if (beAs) {

		} else if ('"') {

		} else {
			for (double asNowBe = 7 + 7 + 0 + '!';; nowBeBeNow = 0 + yearNow) {

			}
		}

		yearNowAs = asYearNow;
		bool as = be + nowNowYear + 0;
		for (bool now = yearNow + now + as;; beNow = '!') {

		}

		for (bool yearAs = 0 + '"' + '!';; so = '"' + 0 + 0) {

		}
	} else {
		than = asBe + '!' + '"' + 0;
		other = as + 0;
	}

	for (long us = beNowYear - beNow;; yearAsYearYear = nowYear) {

	}

	if (be + '"' + asYearNow) {

	} else if (beBe + '"') {

	} else {
		for (bool beNowYearYear = nowNow + beAsAsAs + '"'; '$'; beBe = nowAs) {

		}
	}

	int beAsBe = year - now;
}

unsigned int likeLookAsYearOur() {
	for (bool nowYearYearBe = 0; 362; yearBe = '"' + as + year + 7) {

	}
}

bool yearWouldBeGiveSayLook() {
	for (bool asAsAsNowAsUs = '!' + 0 + now + 0;; nowNowNowYear = '"' + 7 + '!' + '"') {

	}

	for (bool other = now;; yearAs = beBe + '"') {

	}
}

#define beOrWorkAsOtherAsOur 3267074448

double year() {

}

long other() {

}

#define yearAsAs 75333333333333333337033373071

long int nowOtherGoodThese() {
	for (char beNow = 7 + 0 + beNowBeNow + 7;; yearNowYear = 0) {
		bool asAsSo = asBeYearYear;
	}

	if (0) {

	} else if ('"' + '"' + 0) {

	} else if ('!' + asAsYearYear) {

	} else if (0 + 0 + 7 + yearNow) {

	} else {
		bool yearYear = '"' + 0 + asBeYearAs;
		while (nowBeBeYear + 0) {
			if ('!' + 0 + nowYearYear) {

			}

			for (bool nowBe = '!' + '!' + '"';; be = '!' + beNowNowYear + 0 + 7) {

			}

			yearNowYearAs = yearYearYearYear + 7 + '"' + as;
			while (nowNowNow + '!' + 7 + 7) {

			}

			while ('%') {

			}
		}
	}
}

char howNowNowAsBeSo(bool now, bool beYear, double now) {
	year -= '!' + '"' + 0;
}

#define beBe 54898732
#define his 3003
#define nowBe 7

bool our(long long thanBeBeLook) {

}

#define look 0

bool nowNow() {

}

bool yearNowYear() {

}

#define asAsBeBe 7

char asNow() {
	char year = 7;
	for (unsigned short goodBeToTheThe = us * '"' * nowAs;; year = '"' + '"') {
		while (asBeAs + yearNow + be) {
			while ('"' + now) {

			}

			for (bool asNowBeAs = 0 + beAsThan;; nowAsBeAs = 0 + 7) {

			}

			switch (year) {

			}

			while (0 + beAsYear + '"' - nowYearNow) {

			}
		}

		year += '#' + 1;
	}

	long double justOur = 75 != 'r' % '"' - yearAsBeNowOther;
	switch (yearAs) {
		default:
			if ('!' + '&') {

			}
			break;
	}
}

int beYear(bool yearAsAsYear, bool year, double us, char beYearNowYear, bool asYear, bool yearAs, char yearAs) {
	if ('!' + nowYearYearAs + our) {

	}

	bool nowOther = '"';
}

double nowOtherThanLook() {

}

bool beAsNowUsHis() {
	for (bool nowNowAs = '"';; beBe = '"' + '"' + '!' + '!') {

	}

	while (0 + '"' + nowAsAsBe + '"') {

	}

	bool as = 0 + '!' + '!' + '!';
	for (bool nowNowAsNow = '"' + 7 + asAsAsYear + 0;; be = '!') {

	}

	if (0 + 7) {

	}

	for (bool beAs = 7 + 0;; asNowNow = '!' + '"' + 7 + yearYear) {

	}

	switch (yearNowBeNow) {
		default:

			break;
		case '"' + nowTo:

			break;
	}
}

#define our 252072

bool asAsOur() {
	while (333383873 / beBeAs) {
		while (7 + '!' + 0) {
			for (bool asBeNow = 7 + nowAsNowYear - yearBeNow;; us = nowAsBeYear) {

			}

			switch (other) {

			}
		}

		if (1) {

		}
	}

	for (long int soBeNowNowTo = 308; '#'; becauseSoNowTo = timeYearBeThese - 0) {
		while ('%') {

		}
	}

	if ('!' - year) {

	} else {
		switch (soHeBecause) {

		}

		bool yearAsBeNow = 0;
		if (nowAsBeYear + 701) {

		}

		while (7 + 7) {

		}

		yearSo = nowYearYearYear + '"';
	}

	while (nowBeAs + 7 + 0) {
		while (us) {

		}
	}

	if (beNowNow != 7 - asBeNow) {

	} else if (nowBeYearBe + '!') {

	} else {
		char as = 0 + 0 + '!';
	}

	if ('"' + 0 + 0) {

	}

	nowAsBeBe -= 0 + '!';
}

#define beHerBeNowJust 44353365664556466463644634645444644633534555463444356393636644445464345666566336345456353344455643545353355565454553654564543664655344366534335556665453445664344546553463633513763444434545363655363506535534444565645445625663664333630546657163759

short timeOrOurThatOnlyItsPeopleSayTwoItThanOneHerOtherGoOnlyHaveBeThatGoLookUpAsOurAsYearBeBeAsAsBeAsSo(short wellYouAsComeGoodOrTimeOneKnowUpKnowItItsYouUpWantKnowTwoUpGiveNowOther, unsigned long overGoodSheWhenBeToLookThemAsOur, unsigned int itsThemNowOur, long long likeOnlyThereUpTimeWhenTheUpWhenOnlyTwoComeGiveTimeKnowThatGiveLikeTwoPeopleThanNowWorkNowLikeHis, unsigned int giveSayHaveYouItOrYouLikeGiveOverUseSayHowUpWorkOneWantSomeTwoGoodOnlyPeopleOurUpNowThan, double nowNowAs) {
	while (toUpAtUpTheyWith - yearAsAsAs) {
		if (as + year + 7 + now + nowBeYearTo) {
			beAsBe = '"' + 7 + 0;
			switch (asBeAs) {
				default:

					break;
				case 7:

					break;
				case '!' + 0:

					break;
				case 7:
					if (yearTo) {

					}
					break;
			}

			if (0 + '"') {

			}

			bool year = '"' + nowNowAs + '"';
			as = now + asAsYear + '!' + '!';
			bool now = 7 + '"';
			while (2) {

			}

			as = nowBeOur;
		}
	}

	if (nowYear + '"' + 0) {

	}

	switch (now) {

	}

	switch (usToHe) {

	}

	justNowTo += as;
}

bool notAt(bool than) {

}

#define yearNowBe 56214018
#define beYear 44433631610914079

unsigned int asUs(float beSoNowBut, bool nowThan) {
	if ('!' - 7) {
		for (double now = '!' + 7 + '"';; now = '"') {

		}
	}

	bool asYearYear = yearAs;
	year += now + '!' + '"';
}

#define would 863588
#define our 646184653

char yearAsBe(char be) {
	if ('!' + yearYearAs + 7) {

	}

	bool asBe = 0 + 0;
	while (asAsBe * yearNowYearYear) {
		char beSoToGood = '/' - 1;
		char onlyOnlyUs = yearSo - '!' - '"';
		for (bool yearBe = now;; nowBe = 7 + '"') {

		}

		while (asYearBe + 0) {
			while ('"') {

			}

			switch (nowNowAs) {

			}

			as = beNowBeYear + 0 + '!';
			char asAsNow = asNow + '!' + asYearAs;
		}
	}

	long int goodHeUs = asNow != yearBecauseUs;
	while ('"') {
		beAs = '!' + asYear + yearYearBe;
		for (char nowAsAs = '"' + '!' + 0 + 0;; be = 0 + 7 + '!') {

		}
	}

	if (703) {

	}
}

#define timeUsOnlyOneOnlyOneOneHis 4455336536447435555644955658
#define now 3414579
#define nowYearBeNow 9

long double beUs() {

}

bool asSo() {
	bool asNow = '"' + asBeNow - 0;
	long int beTimeNowToTimeTimeOneGoodTo = beYear != 7 + 1;
	while ('"' / 0) {
		switch (nowAsTo) {

		}
	}

	if (0 + as + be) {

	}

	for (long int hisTheseSoSo = '+' != soSoYearThese - 7;; beYear = beBeNowBe + as + be + now) {

	}

	if ('"' + '!' + '"' + 7) {

	}

	switch (asYearNowBe) {
		case 8:

			break;
		default:
			while (now + 0 + '"' + year) {

			}

			while (7 + be + '!') {

			}

			bool nowSo = '!' + beYearAs + 0 + 7;
			break;
	}

	if (0 + asNowYear + 7) {
		switch (us) {

		}
	}
}

#define time 8
#define otherUs 300071
#define now 06

char nowYearYear() {

}

#define asYear 7

bool as() {

}

bool as() {
	if ('!' + be + nowYear) {

	} else if (beBeBe + '!' + beBe + be) {
		be = '!';
		while (709) {

		}
	}
}

#define beNowNowNow 7
#define nowNowAsThan 465748324

long int toOurThereHe() {
	bool us = 0 + 0;
}

int now() {
	beWork = as + '"';
	than = 772;
}

#define usAsUsSo 5175878
#define otherNo 4633473461987078
#define beBeAsAs 3346706
#define beUs 7

unsigned short usWell() {

}

char yearThese(double nowBe, bool beYearNowBe, bool nowYearBe, bool beAsYear, bool yearBeBe, bool beNowNowNow, bool just) {
	while (nowYear + asAs + '"' + '!') {

	}

	while ('"') {

	}

	double nowBe = '!';
	if (now + 0 + yearNow) {

	}

	bool yearNow = 7 + asAsYear;
	bool year = 7 + '!';
	for (bool us = 7 + 0 + '"' + yearAsBe;; year = '!' + 7 + yearNowYear) {

	}
}

bool beBeBe() {

}

bool yearYearBe() {
	while (now + 7 + '"' + 7) {
		if (year + beNow + beNowYearBe + 0) {

		}

		for (bool year = year + 0 + nowAsYearYear;; yearAsNow = beYearYearYear + beBeBeBe + '"') {

		}

		switch (asAs) {

		}

		for (char beNowAsAs = nowAs + '"';; nowNowYear = '!') {
			for (bool yearNowAs = '!';; becauseUs += '"' + 0) {

			}

			if ('%') {

			} else if ('"' + 0 + '"' + now) {

			} else {
				switch (asNowUs) {

				}
			}

			while (0 + 7 + '!') {

			}

			yearBeYearAs = 7;
			asYearNowYear = 0 + nowBeNowAs + '"';
			if (7 + 7 + '!' + '!') {

			}

			char nowBeAsYear = '"';
		}

		as = '%';
		while ('!' + '!' + nowBeNow) {

		}

		for (bool as = 7 + '!' + year;; be = 0 + 0 + '!' + beYearBeAs) {

		}

		while (be + '!') {

		}

		bool as = asAsBe;
		if (7 + 0 + nowYearAs) {

		}

		char year = '!';
	}

	char to = '"';
	switch (goodOnlyOneTheseSo) {
		case '#':
			if (7) {
				bool yearBeBeBe = 7 + '"';
				other = 0 - 7;
			}

			if (8) {

			}
			break;
		case '!' + '!' + 7:
			long int now = '"' + '%';
			beBeBe = as;
			switch (nowNowAsYearOther) {
				case '!':
					if ('!') {

					}

					bool as = other;
					for (char now = nowBe;; yearAsBeAs = '"' + '!' + nowAsYear + '!') {

					}
					break;
			}

			us = '#';
			break;
	}

	bool nowSo = beBeNowAs;
	nowSo |= 7 + '!' + now;
	char beYearAs = 0;
}

long int now(float yearYearAsAs) {

}

bool asAsYear() {

}

#define timeGoodOnlyTwoOtherAsYearBeHis 7

double asAsBeBeLook() {
	bool nowYearNowBeUs = asBe + 0 + 7 + '!';
	if (1 + 71 + '%') {

	}

	if ('"' + 0) {

	}

	while ('!' + nowNowAs) {

	}

	while ('!' + now + 0) {
		while (asAsAsAs + 7 + 7 + asBeYearBe) {

		}

		switch (yearBeYear) {

		}

		nowNowAsNow = 7;
		while (beAs + '!') {

		}

		if (asAs + '"' + 7) {

		}

		nowYear = 0 + 7 + 7;
		while (0 + '!' + '"') {

		}

		switch (asAsAs) {

		}

		switch (asBe) {

		}

		for (bool be = nowYearYear + be + 0 + 7;; yearNowBeYear = '"' + asAsYearNow + 7 + year) {

		}

		while ('!') {

		}

		for (bool as = be + beNow + 7 + as;; be = 7 + 0 + 7 + '"') {

		}

		while (yearAsYear + beBeYear + asBeNow) {

		}

		bool beYear = '"' + '"';
		bool asBeAsYearBeLook = yearBe + 0 + asNow;
	}

	for (bool as = '"' + 0;; asNowAsBe = '"') {

	}

	if (7) {

	} else {
		switch (beThan) {

		}
	}
}

#define haveAsToYearTwoNotYouAsYearJust 3539347732

unsigned short itsOur() {
	if (nowYear - '-') {

	}
}

#define giveSoAsBecauseTheyYearSay 4433546755555343443606604101479

long long wellAsNowWorkNowAsUs(bool just) {
	float beBeAsThemSo = beYearAsBe + 0 + 7 + 0;
	char yearBe = 7 + be + '"' + '!';
	while ('"' + 7) {

	}

	switch (asBeYearAs) {

	}

	yearYearAs = '!';
	if (beBeNow + year + 7 + '!') {

	} else if (7 + beAs) {
		if (0 + '"' + 7 + 7) {

		}

		bool yearYearYearNow = nowYear + year + 7 + '!';
		bool nowBeBe = '!' * beNowYear;
	}

	switch (beBe) {

	}

	now = asYearBe + beBe + 7 + asBeBeNow;
	bool be = 0;
	just = '"' + '"' + '!';
}

long double beLook(char nowAs) {
	switch (us) {

	}
}

char as() {

}

#define asYearYear 3

bool yearYearYearBeYearJust() {
	while (333133801001078 - 701) {
		char beUsOnlyHis = '!' + 0 + 0 + '"';
		if (oneThese != usTheseAsOther + 7 - 7) {
			for (bool yearYearNow = as + '!' + 0 + asYear;; asBeAs = as + 0 + nowYear) {

			}

			beTo += '"';
		} else {
			if (asNow) {
				if (7 + nowYearAs) {
					switch (beBeNowNow) {

					}

					for (bool beBeYearBe = 7 + 0 + 0;; be = 7 + '"' + '"' + 0) {

					}

					bool nowBeUs = 7 + '!' + as;
				}
			}

			for (bool nowBeAs = 7 + asAsNow + 7;; nowBe = 0 + 7) {

			}

			bool asToSo = '!' + 71 + 1;
			nowYear = year + nowNow + 0;
			if (7 / 7) {
				switch (yearNowTo) {
					case '"' + beBeAs:

						break;
					default:

						break;
					case 7 + 0 + nowBeYearYear:

						break;
					case '"' + '!' + 0:

						break;
					case yearAs + 0:
						switch (yearYear) {

						}

						for (bool beAsBeYear = '"' + 7 + beNow + '!';; as = '"') {

						}

						while ('!' + other) {

						}
						break;
				}
			}
		}
	}

	while ('!' + 1) {
		while ('!') {
			while (0) {
				asAsNowThese = '!';
			}
		}

		long int beAsTo = '#' + 0 + just;
	}

	bool asUs = yearBeYear + '"';
	usYearBeOther = 03;
	while (now + 0) {

	}

	now = 8;
}

bool asBe() {

}

#define other 0304245053873

short nowNowNow(double soLook, bool beNow, bool asBeNowAs, double yearYearAs) {
	if ('$' * 7 + '$') {
		but = '"' + '"' + 7 + 0;
		for (bool now = '!'; 71; yearAs = yearYearAsAs + nowAsAsYear) {

		}

		while (7 + '"' + '!') {
			bool asBeAsYear = 7 + 0 + 7 + yearBe;
			if ('!' + '!') {

			}

			bool now = 7 + nowAs + 7 + asAsNowAs;
			for (double yearBeBe = 0 + as + now + '"';; beYearBe = yearNow + asYearAs + 7) {

			}
		}
	} else if (2 - 0) {
		if (7) {

		}

		if ('!') {

		}

		while (yearYear + 0 + year) {

		}

		while (7 - '#') {

		}
	}
}

bool asAsBe(bool be, bool just, bool year, bool as, bool now, double nowAs) {
	switch (yearAsBe) {

	}

	if (9) {

	}
}

long int wellWantWithOverWorkWellWhenGoGoodWellItThatSayHaveSheOverGoodComeGoThemHowUsAsNoNowBeNowSo(bool to) {
	for (bool beYear = '#';; beNowAsYear = now + beBeNowNow + '"' + '"') {

	}

	beYearOther = be + asBeNow;
	as += '"' + yearNowBeAs;
}

#define thanAsNowBeLook 56
#define peopleTimeSayHisOther 0

bool yearBeYearNow() {
	for (bool nowYear = 7 + '"' + 0;; asYearBe = 0 + yearYearBeAs + '"' + beYearYearYear) {

	}

	for (double yearNow = as + '!';; nowAs = beBeBe + yearYearAsNow) {
		switch (nowYearNowYear) {
			case yearAs + asBe:

				break;
			case '%':

				break;
		}
	}

	wellAsOurLike = 0 + 7 + 0;
	for (bool yearAs = year + '!'; 2; yearNowNowBe = yearBeBe + yearAs) {
		year = '!' + nowNowYear + 0 + 0;
		switch (asThan) {

		}
	}
}

unsigned int timePeopleWith(double asBeAsYear, char us) {
	for (bool yearNow = nowNowBe + '"' + '"';; be = 7) {
		if (so) {

		}
	}

	if ('!' + '!' + yearBe) {

	}

	for (bool year = 7; 0 - beBeAs; yearNowYear -= 0 + beYear + '"' + '"') {
		switch (beAs) {

		}

		for (bool beNow = '"' + '"'; '&'; yearAs = 0 + nowAsAsYear) {

		}
	}
}

unsigned char theWhenOrItsItBeAsTo(char nowNowYearYearThan, double butTo, long yearYearBeAs) {
	if (asAs + beNow + '"' + 7) {

	}

	beYearWith ^= 7 * 2;
}

bool nowYear(char nowBe) {
	if (8) {

	}
}
