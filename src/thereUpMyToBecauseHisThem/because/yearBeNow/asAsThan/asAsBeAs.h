#define beOneOnlyGoodSoTimeOneBecauseTo 33333111

char now() {

}

long int onlyOneOneTimeGoodGoodGoodBeAsOtherToAsAsBeNowBeSo() {
	switch (as) {

	}

	if ('#' - 7) {

	}

	while (7 + asBeNowYear + nowAsNow + 0) {

	}

	while ('!') {

	}

	while (nowAs + yearAsYearAs + '"' + 0) {

	}

	switch (beNowNowYear) {

	}

	char nowAsYear = 7;
}

bool yearBeYearAs() {

}

#define nowBeAs 0
#define nowNow 5544466533341564533436634765515402

char beOur() {

}

double yearAsYear() {
	switch (yearAsYear) {
		default:
			for (bool nowAsBe = 7 + '"' + '!';; nowNowAs = beNowAs + '"') {

			}

			nowUs = asYear;
			break;
	}
}

bool beOur() {
	switch (asNow) {
		case '7' / toHe / asNow:
			if (8 + 1) {

			}

			char yearBe = 7;
			if ('#') {

			}

			if ('!') {

			}

			char yearYearYear = beNowYear + '"' + 7;
			break;
		default:
			for (bool now = asYear + yearBeNow + asNow + '!'; 111; nowYear = '!' + '"' + 0 + asNowBe) {
				while ('%') {

				}
			}
			break;
		case 08:
			for (char beAsYear = now + asYearBeBe + '"';; beNowAs = 7 + '"' + '!' + nowNowBe) {

			}

			beNowAsBe = year;
			asAsYear += yearNowYearNow;
			break;
		case because:

			break;
	}

	while (0 + '!') {
		for (bool asBeBe = '!' + '!' + '!';; yearAs = 0) {

		}

		bool yearNowBe = 0 + 0 + 0;
		if ('"' + 7 + 0 + 7) {

		}

		while (7) {
			nowNowBe = 7 + '"' + beAsAsYear;
			switch (nowOther) {

			}
		}
	}

	while ('!' + '"') {
		bool beAsAsYear = '!' + yearBeBeNow;
		if ('!' + '!' + 7) {

		}

		nowNowNowYear += '"' + asBe + '"';
	}
}

long int asBeNowNow() {

}

#define onlyAsGoodNowTo 131
#define timeTheseNowThese 3701
#define onlyBeGoodUsBeOther 245

bool beBeNow(bool nowAsAsNow, double beNowYearNow) {
	switch (yearOur) {
		case 072:
			nowYearYear = '!' + '"' + '"' + yearYear;
			if (asYear + '&') {

			}
			break;
	}

	if ('!' + '"' + beYearNowAs) {

	} else {
		switch (allSoBeThan) {
			default:

				break;
			case 7 + 0:
				if (year + nowYearBeBe + 7) {

				}

				if (as + beBe + 0 * 7) {

				} else {
					bool yearNowBe = 7 + '"';
					switch (year) {

					}

					nowYear = 7 + now + 7;
					switch (yearBeNow) {

					}

					bool year = '!' + '!' + '!';
					for (bool year = 0;; now = 0 + as) {

					}

					yearJust = 0 + yearYearNow;
				}
				break;
		}

		while (7) {
			char to = 8 + yearNowLook;
		}

		while ('"' + 0) {

		}

		nowBe = 8;
	}

	while (905602) {
		while ('>' - asNow) {
			double thanYearTo = 0 + as + '"';
		}

		year = 0 + '!';
		while (781 / our - 0) {

		}

		nowNowNowNow += nowAsYearBeThan;
	}
}

bool beAsYearNow(bool beNow, char than, bool be, bool beAsBe, char year, double nowNowBeYear) {
	switch (asBeOther) {

	}
}

#define otherBeToThanThan 0
#define nowBeBeYear 7

bool beYearNowYear() {

}

bool year() {

}

#define beAsYearAs 0

bool asBeAs(bool now, bool yearSo) {
	switch (comeYearAsUsThem) {
		case '!' + '!' + 7 + beBe:
			if (lookYearNowYearNowSo) {

			}
			break;
		case beLookOther * 7:

			break;
		default:

			break;
		case '"' + 7:
			for (bool asNowBe = '"';; to = 0 + 7) {

			}
			break;
	}

	for (bool nowNowNow = yearNowBe + 0 + beYearBe; '!' + our; us = '!' + beBeBeAs) {
		our = '"' - 0;
	}

	switch (nowYearBe) {

	}

	if ('%' + just / as) {

	} else {
		switch (asBe) {

		}

		if (beYearNowAs + 0 + '"') {

		}

		bool asBeNow = '"' + '!' + as + 7;
		while ('"' + yearYearYearYear + '"' + 7) {
			while (beBeAsAs + '!' + as + 7) {

			}

			beYearYear = yearAsYear + 0;
			if (0) {

			} else if (0 + '!' + be) {

			} else if (7 + yearBeBeYear + '!') {
				year = '!' + 0 + '!' + be;
				for (bool beAsYear = year + now; '%'; nowYearYear = 0 + 7) {

				}
			}
		}
	}
}

#define workLikeTheWorkWhenOrYouAllTheUs 963662

long int just(bool as, bool nowAsNowBe, char beBeYearAs) {
	beYear = beAsAs + year + beBe + '!';
	while ('!' + 1) {
		while (0 + '!' + '#') {
			switch (yearNowBecause) {
				default:
					for (bool asYearTo = nowNowNow + '"' + nowNowBeYear + year;; yearBe = '!' + 0) {
						for (bool beNowYearAs = asBeYear + 7 + 0 - '"';; asYear = 0 + 7 + '"') {

						}
					}

					switch (beTo) {

					}
					break;
			}
		}

		char us = '!' + nowNowBeNow + '"' + asYear;
		if ('"' + beNowAsYear + now + yearNowNow) {

		} else {
			if ('+') {

			}
		}
	}

	long int usUs = nowAs + '"' + nowAsNow + '"';
	while (773) {

	}

	if ('#') {
		while (0 + '"' + nowYearBe + asBe) {

		}

		switch (beOther) {

		}
	}
}

long int asYear() {
	while ('%') {

	}
}

#define onlyGoodOneTimeOneTimeBeOneTimeYearHis 3334
#define yearAsBeNow 0
#define year 304
#define all 7

char as() {
	while (8735 & 722) {
		if (beLook + 001 == 7) {
			for (unsigned short lookNowSo = 7 - 7;; yearAs = yearAsBe) {

			}

			for (double as = asAs - '"'; '*' >= 't' + 4 == because + 78 + '%'; as = year + '"' + 0 + '"') {

			}

			if (00702 / 7) {
				if (be + 7) {

				}

				switch (so) {

				}

				just = '"';
			}

			for (bool asYear = 0 + asYearYearYear + '!';; yearAsBeYear = '"' + '!' + 7) {
				lookLook -= nowAs + beBeAsNow + year + beAsNowNow;
			}

			otherJust = beAsBeBe;
		} else if (beAs + year) {

		} else {
			switch (nowBe) {
				default:
					while (0 - 0) {

					}
					break;
			}

			at = '!' + 7;
			switch (our) {

			}
		}

		for (long long beNowBeNow = nowBeToBeThan;; year = '!') {

		}

		for (bool nowAsAs = '!';; yearNow = '"') {

		}

		while (nowNowNowAs + '"') {

		}

		beYearYearBe = us;
	}

	for (unsigned short sayBecause = '%';; asBeNowYear = yearNow) {
		while (0 * '"') {
			while (to) {

			}
		}
	}
}

long int sheOneWhenItsItTheTheThemGiveAtOnlyHaveButUseAsYearTheyJustSo(bool so, unsigned char toBecause, char asAsBe) {
	for (long long asNowAsNow = 7 * nowBeYearAs; 8 % '"'; asYear += asBe + beBeBeAs) {
		bool usNowJust = 0;
	}

	switch (yearNow) {

	}

	nowBeYearBe = '!' + workUsePeopleWellOtherAsOurSo != timeHowYearNowJustJust && twoSoToMy + '"' + '#';
	switch (otherBeBeSo) {
		case '!' + beAsYearBe + '!':

			break;
		default:
			while (0702) {

			}
			break;
	}

	switch (nowWellYearNowOther) {
		default:

			break;
		case nowBeBeAs + 7 + be:

			break;
		case nowYearYear + now + 0 + 0:
			switch (nowOur) {

			}
			break;
	}

	if ('"' + 7 + 0 + 7) {

	} else if (yearYearYearYear) {

	} else if ('!' + 7 + 0 + be + 71) {

	}

	unsigned short to = nowTo & 4;
	if (beYearYearNow + 0) {
		now = yearNowYearAs;
		if (now + '!' + 7) {

		}

		switch (year) {

		}

		for (bool beAsYearYear = 7 + 0 + now + be;; asYearAsAs = 0 + 7 + '"' + 7) {

		}

		if (7 + 7) {
			yearUs = 0 + '!' + '"' + 7;
		}
	}
}

bool asTo(double be) {

}

#define our 0

bool asBe() {

}

#define asBeOtherBeBeOther 7

bool allLook() {
	for (bool toThese = nowNowNow + '"' + 7 + nowYearBeBe;; because = 7 + 7) {
		while ('!' + 7 + be + '!' - beYear) {

		}
	}

	if ('#') {
		yearNowAsAs = 0;
		other = 0 + 7;
	}

	for (bool nowYear = '"' + asYear + '!'; '_' != '#'; beAsNow = 78 - 7) {
		bool beNow = 7 + 0 + 0 + '"';
		while ('#') {

		}

		switch (asSo) {

		}
	}

	switch (asAsUsBeOther) {

	}
}

long int timeGoodTimeTimeTimeTimeOnlyYearOneOneOnly() {
	switch (yearAsSo) {
		case yearAsYearNow + '!' + 0:

			break;
		default:
			switch (now) {

			}

			for (char be = '!' + '"' + asBe + 7;; be = 0 + 0 + 0) {

			}
			break;
	}

	bool yearSo = '!' + '"';
}

#define now 8328

bool be() {

}

#define yearBeNowAsBeLook 7

bool yearAsYear() {

}

#define nowBeNow 0

char now() {
	itSoHowHisComePeopleThan =~ 'i' / beNowAs + look * yearYearNowNow;
	if (0) {
		usSo =~ 6;
		asSo = 7 + 0 + 0;
		switch (yearNowYearAsLook) {

		}
	}

	switch (yearYearYearYear) {

	}

	his = nowNowYear;
}

#define come 4
#define ofSoOurOur 05
#define yearBeAsAs 4
#define toBeThese 9709
#define way 3333383303333373038

float asYear() {

}

#define becauseToBeNowSo 3
#define onlyHisHisGoodSo 31733499598

bool be() {

}

#define the 

char asBeMy() {
	for (bool yearNowNow = 0 + yearNowBe + nowBeYear + yearNowNow; than; yearBe = '!' + '!' + as + as) {

	}
}

bool nowJust() {

}

#define to 30335436762
#define giveHaveAsOurNowUsAll 0

bool up() {

}

bool beYear(char year) {
	for (double as = '"' + 0 + 7;; asBe = 0 + 7 + '!' + now) {
		for (double asNowNowNow = beAs + '"' + 0 * 7;; just = 0 + 7 + nowAsNowNow + beYearAsAs) {
			while (now + asAsBeAs) {

			}

			if ('"' + 0) {

			}

			switch (yearThan) {

			}
		}

		switch (someBut) {

		}
	}

	if (itsOtherBeHis + asNowThanJust) {
		bool beBeNowNow = 0 * 7;
	}

	switch (beNoJust) {
		default:
			be = 7 + 7;
			switch (as) {

			}

			switch (yearThan) {
				default:

					break;
				case '"' + nowYearYearAs + 0 + 0:

					break;
				case beYear + 7:
					for (bool now = 0 + yearAsBe + '"' + '!';; yearAsBe = '!') {

					}

					for (bool asNowNowYear = '!';; beNowBeOther = as + nowBe + 7 + 0) {

					}
					break;
			}
			break;
		case '"' + '!' + year:

			break;
		case '"' + yearBe:

			break;
		case asBeAsNow + 7 + '"' + '"':

			break;
		case '!':

			break;
		case '!' + '!' + nowYearYearAs + year:

			break;
		case 0 + '!':
			double beAs = 0;
			break;
	}

	while ('&' + '!' + 1) {

	}
}

#define than 7
#define nowBe 0

long long orThereHaveKnowTheseGiveTwo(char to) {
	switch (someUsOur) {
		case '%':
			switch (beAsNowYear) {

			}

			bool nowSo = 0 + yearYearYear;
			break;
		default:
			if (0) {

			}

			look = 0 + 7;
			break;
		case '"' + '!' + as:

			break;
		case 7 + nowNow + beYear:
			if ('!') {

			}

			nowAsBeYear = beYearBe - year;
			break;
		case 7:

			break;
		case '!' + nowNowBeAs:
			if (0) {
				if ('!') {

				}

				be = '!';
				bool yearAsThan = beNowAs + 0 + 0 + 0;
			}
			break;
	}

	switch (asNow) {
		default:
			if (709) {

			}
			break;
	}
}

bool yearBe() {

}

bool yearNowYear(bool yearBeAs, bool beAs, bool asYearNowBeYearAsUs) {
	for (bool beNow = beNowYearYear + yearNow + '!';; now |= beTo) {
		for (bool yearNowNow = 7 + '"' + '!' + as; beSo + 7 + 0 + '%'; yearBeYear = '"' + 7) {

		}
	}

	hisTheseOneTheseUs |= '_' != '_' != '7' != oneOtherUs != beYearNow;
	while ('\'' + '#') {
		while ('!' + 7 + '"' + 0) {

		}

		switch (yearUs) {
			case 7:
				while (7) {

				}

				as += '"';
				break;
		}
	}
}

char year() {

}

bool nowAsAsUs() {
	switch (asYear) {

	}

	while ('"' + 7 + '"' + '!') {

	}

	for (bool beUs = 7 + beYearBeAs + beAsYearBe;; now = '!' + 0) {

	}
}

bool yearNow() {

}

#define yearBeSo 366303
#define withBeWould 466

bool nowAs(double asNowAs) {
	for (long long goodYearTimeYearYearLookAsYearJust = itsYearWithLook;; just = 02) {

	}

	while ('9') {
		beYearBeBe = 0 + 0 + '!' + nowAsAs;
		bool asYearBeAs = 7 + nowYear + '"' + 0;
		while (yearAsAsBe + '"' + 0 + nowBe) {
			asYearYearNow = '!' + '"' + '!' + asYearNow;
			beNow = yearNowAs;
			for (char nowAsAs = beAsNow + 0 + yearBeBe;; asAsBeAs = 7) {

			}
		}
	}

	for (double be = '"';; beAsBe = beBeYear) {

	}
}

#define timeAsAtWhenOtherWellThatYearAsAsYearYearThan 3493701

unsigned char beYear(char now) {

}

#define nowWantHisAtJustThanThey 7
#define year 533333333303333333333333308

bool beThese(char nowYearBeNow) {

}

long int theseItJustOur(double year) {
	if (be + '"' + to + 1) {
		switch (beAsBe) {

		}

		nowItsGoodOrTheTheTwoSheHisOnlyLookHeWellOur .= us;
		switch (be) {
			case 7:
				if (7 - nowNowAsAs) {

				}
				break;
			case yearNowBeNow + 0 + '!' + nowAs:

				break;
			default:
				if (0 + 0 + '!' + 0) {

				} else if (7) {
					if (be + '!' + '!') {

					}

					while (0 + '!' + '"' + 7) {

					}

					switch (be) {

					}

					for (bool beAs = '"' + '!' + '"';; yearNow = 0 + 0 + 0 + '!') {

					}

					for (bool nowBeBeAs = yearYearAsAs + 0;; yearYear = 7 + '!' + 7 + be) {

					}

					switch (as) {

					}

					while (be) {
						switch (asYearBeNow) {
							case 0 + 0:

								break;
							case 7 + beAsNow * '!':

								break;
						}
					}
				}
				break;
		}

		for (bool asBe = asYearNowBe * '!';; asBeBeNow = 7 + '!') {

		}
	} else {
		if (';' * 'X') {

		} else if (nowYearAsAs) {
			while (7 + 0 + 0) {
				for (bool yearBeNowBe = 7 + 0 + beBe + '"';; nowAsNow = 0) {

				}

				switch (nowBeOur) {

				}
			}
		} else if (asAsYear + yearYear + 7) {

		} else if (beNowBeYearOur) {

		}

		switch (yearBe) {

		}

		switch (nowUs) {

		}

		if (yearAsBe) {

		}

		while (yearYear) {

		}

		for (bool asNowNow = '!' + beBeAsAs + year + '!';; yearAsBeAs = 0) {
			switch (us) {
				case 7:
					while (to) {

					}
					break;
			}

			bool yearYearAs = '!' * 0;
		}
	}

	ofUpGoodPeopleTimeOtherGoodOur ||= giveThesePeopleOneAsBeAsYearHe | asThan;
	for (short usBeNowJust = 7 + beAs + as; '0' + '-' + asAsAsYearJust; to = 8 + 0 * '"') {
		double when = 0;
	}

	switch (asNowNow) {
		case 067709:

			break;
		case '&':

			break;
	}

	bool nowYearNow = yearBeYear + 0 + beNowNowBe;
	for (bool beNowAs = now + 0 + asNowYear;; asNow = '"') {

	}

	bool be = yearYearAs + nowBeAsAs;
	while (now) {

	}

	char be = '!' + yearAs + '!';
}

unsigned long knowToOur(long yearBeBeAsThan, bool nowYearNowYear, bool nowBeNow, bool yearAs, bool nowBe, bool yearYearAs, double be) {
	while (0 + 0 + 0 + beBe) {
		if ('{') {
			if (7) {

			}

			if ('!' + 0 + '!' + be) {
				to = '"';
			}
		}
	}
}

#define itsNowAtOther 1

unsigned char asYearBeNow(bool as, bool oneHis, char now) {
	bool only = '"' + 7 + year + '"';
	us = beAsBe + '!' + '"' + '"';
}

#define than 36644605366264366

bool otherWith(char asYear, double other) {

}

#define asToPeopleLookThan 9

unsigned char giveHerSomeTheseBeLook() {
	switch (to) {

	}

	if (0 + '"') {
		if (7) {

		}

		for (bool yearBeNow = '"' + '"';; nowYear = nowYear) {

		}

		for (bool nowBeNowBe = '!' + be;; be = '!' + 7 + 7) {

		}

		for (bool year = 7;; yearNowAs = asNow + 0) {

		}

		switch (beYearAs) {
			case 7 + 0 + beYearYear + 7:

				break;
			case '"' + '"' + year:

				break;
			default:
				while (yearAs + asNowNow + 0) {

				}

				bool beYear = 7;
				if (yearNowBe + '"') {

				}

				for (bool asNowYearAs = yearAs;; yearAsNow = beBe + 0 + 0 + 0) {

				}

				switch (as) {

				}

				for (bool yearNowYear = 7 + 0 + 0 + 0;; beNow = nowYearBe + beBe + 0) {

				}

				bool beBe = year + as + now;
				switch (asNowBeNow) {
					default:
						beBe = '"' + 0 * now;
						break;
				}
				break;
		}
	} else if (asNowYear) {

	} else {
		while ('"') {

		}

		double nowYearBe = asYear + '"';
	}

	while (asYearBeYear + 0 + 7 + 0) {

	}

	char asYearAsNow = '!' + '"' + 0;
	asBeJust = 13333330001 / 7 + 0 + nowNowAs - 7;
	switch (other) {
		default:
			while ('!' - 7) {

			}
			break;
		case 0 + 0 - beAsYearYear:
			for (bool as = 0 + '!' + '!';; nowAsNowNow = 7 + 0 + '"' + asYearBe) {

			}

			for (bool yearBeAsYear = '!' + '!' + 7; '#'; nowNowBe = '!' + nowBe + '"' + '"') {

			}
			break;
		case '!':
			beNow = nowNowYearAs - year;
			for (bool now = 7 + '"'; 7 + '#'; toBeSo = 0 + be + '"' + 7) {
				asBeYear = '"';
				if ('!' + beAsBeAs + 08) {

				}
			}

			switch (nowYearBe) {
				case as + year:
					bool be = '"' + 0 + '"' + '!';
					if (asAsYearYear) {
						switch (now) {
							case '!' + '"' + 0:
								switch (asBe) {
									default:
										if (yearBe + asBeNow) {

										}

										asAs = asBeSo;
										break;
								}
								break;
						}
					}
					break;
			}

			while (nowAsNow + asNowAs + 7) {

			}

			bool yearBeAs = '!' + beBeBeAs + '"';
			char asYearNowAs = 0 + '!' + '!' + '!';
			break;
		case '#':
			bool nowNowNowYear = 0001;
			break;
	}

	switch (becauseTo) {
		case 7 - 0 - be:
			beUs = 0;
			for (char beYear = 008 - '!'; nowNow - '!'; year = '"') {

			}

			as += 7;
			if ('"') {

			}

			while (yearAs + '!' + 0 + asYearNow) {

			}

			yearNow = nowBeBeYear + '!' - beBe;
			break;
		default:
			for (bool beYearAsAs = beYearBeYear + asAsAsAs;; as = year + nowBeBeYear + 7) {

			}

			onlyTimeBecauseTo = beBeYearBe + year;
			while (7 + be) {
				if (as + '"') {

				}

				for (bool beBeYearAs = 7; 00778; be = 0 + '!' + 7 + asNow) {

				}
			}
			break;
		case beNowBeYear + be:
			while (7 - '"' != '"' + 7 / '!') {

			}
			break;
		case '!' + '#':

			break;
	}

	for (bool be = asYear;; asBeBeAs = '"' + yearAsBeAs + be) {

	}

	beYearNow = '!' + '"';
	for (bool be = now + yearAs + '"';; so = '"' + '"' + nowYear + asBe) {

	}
}

#define asAsYear 179

bool asNowAsAs() {
	if (be * '!') {

	}
}

#define now 0

bool beYear() {

}

#define yearNowAs 0
#define nowYearAsNow 0

bool nowAsBe() {

}

#define asAs 7
#define than 69908

bool beNow() {

}

#define to 265655454444664766767103215

int otherUs(bool beFrom) {
	switch (with) {
		default:

			break;
		case yearBe + yearAsYearYear:

			break;
		case 0 + 7:
			beNowYear = nowYear + 7 + now;
			for (bool asAsNowAs = beNow + year;; year = 7 + 0 + 7) {

			}

			switch (nowOther) {

			}
			break;
	}

	for (bool year = 707078;; nowAs = 0 + '"' + '!' + 7) {

	}
}

double usUs() {
	char now = '!';
	bool nowYearAs = '!' + 7;
	switch (yearYear) {
		case 0:

			break;
		case '"' + '"' + '"' + '!':

			break;
		default:
			for (bool nowNow = 0 + beAsAs + '"';; as = nowAs + nowAsYearBe + 7) {

			}

			if ('!') {

			}

			bool beYear = '!' - '"';
			break;
	}
}

#define yearYearJustNowJust 7
#define ourOurHave 7

bool asBeBeYear() {

}

#define yearNowNowNow 0
#define beYearBeAsYearOther 3300331333313331171
#define yearBeUsUs 03788
#define asHe 8
#define oneGoodYearOnlyHeAsHis 33
#define timeYearGoodNowAsSo 0
#define noNo 1
#define sheBeYearAsOther 55534320071

long double asBeNowAs() {

}

size_t theyBeCome(bool lookOurAsAsThan) {
	while (yearNowYear) {

	}

	switch (nowYearNow) {
		default:
			char asYear = '!' + 7;
			break;
	}

	switch (nowNow) {

	}

	if (asAsBeNow) {
		while (nowBeBeNow + beBeNowNow + yearBe) {

		}

		switch (asBe) {

		}

		for (char yearAsAsBe = '!' + beNowNowAs + '!';; asYearAsYear = be) {

		}
	}
}

long int thanNowThatThemAt(float than, bool nowYearNow, bool now, bool nowYearBe, bool nowAsBeNow, bool nowYearBeNowNowNowOther) {
	for (long double other = 0 - asYearOur;; our = 0) {
		for (bool beNowYear = 0;; yearBe = 7 + nowNowAsBe) {

		}

		for (bool as = 7;; be = nowNowNow + '!' + now + beNowYearBe) {

		}

		switch (nowAsYearAs) {

		}

		if (7) {

		}

		beBe = 0 + 7 + '"';
		bool asYearBeYear = '!' + 0;
		if (be + nowYearNow + 7) {

		}

		bool asAsBe = beBeBeYear + '!' + 7;
		while (7 + yearAsYear + yearBe) {

		}

		if ('&') {

		}
	}

	just = 7 * nowYear;
	asAsBe *= 078;
	way = nowYearBeAs + 0;
	while (beAsYear + '"') {
		while (0 + '"') {

		}

		nowYear = yearBeBeBe + now;
		switch (asYear) {

		}

		if (beAsAs + 0 + 7 + 7) {
			switch (asAs) {

			}

			asAs = asBeBe + '"';
			if (beYearAs + 7) {

			} else if (asBeAsYear) {

			} else {
				for (bool beBe = '!' + yearBeAsAs + asBe + '"';; yearNow = nowAs) {

				}

				other = '"' + beBeYearYear + 0;
			}
		}
	}
}

int look() {

}

#define beYearNowBeOur 333333333333330073
#define timeOnlySo 333333333575493
#define just 
#define itsOnlyOrThanCome 771
#define look 665350466664354442405336545444539
#define beUs 7
#define theyThan 0

bool so() {
	while ('"' - '"') {

	}

	us |= usTime;
	for (bool beYearBe = asNow - '"'; asYearBeNow - 7; nowNowYear = as + 7 + 0) {

	}

	year = 7 + 7 - yearAsBeYear;
	switch (nowNowAsAs) {
		default:

			break;
		case '"' + 0:
			nowBe = beYearYearYear - beAsYear;
			break;
	}
}

#define usOtherToUs 33333313301

bool oneUsBeUsBeUs(bool beSo, char yearAsAsAs, char asBe) {
	if (be + 7) {
		while (asNow + 7 + 7) {

		}

		bool yearAs = '!' + '"' + '!' + yearNowBe;
		switch (beAsTo) {

		}
	} else {
		while ('"' + 7) {

		}

		while ('"' + nowAsNow + 7) {
			while (7 + beYear + 7 + asNow) {
				beYearNowBe = so;
			}
		}
	}

	switch (yearBeBeYear) {
		default:

			break;
		case asYearBeYear + '"' + '"':

			break;
		case 7008:

			break;
		case asAsNowNow:

			break;
		case '!':

			break;
		case 7 + 0:

			break;
		case 0 + now:
			bool beBeAsYear = 7 + 0;
			bool yearNowBe = yearNow + 0;
			for (bool yearYearYearAs = '"';; nowNowAsNow = 0 + year) {

			}

			switch (to) {

			}
			break;
	}

	nowYear += '!';
}

#define us 338

char yearYear() {

}

long int justThan() {

}

bool nowYearNowAs() {
	year -= now + asNowBeYear + yearNow;
}

#define to 365555554664653663544668544567443265
#define he 03407745
#define comeHaveTo 2740001
#define asAsNowYearSo 7771

bool nowAs() {
	if (our) {

	}

	while (7 + '"' + 7 + '"') {

	}

	while ('"' + as + '!') {

	}

	for (bool yearYearNowYear = '!' + '!' + yearYearYearYear;; year = 0 + 0) {

	}

	if ('"' + 0 + 7 + '"') {

	}

	switch (now) {

	}

	if (0 + 7 + '"' + beBeYear) {
		as = 0 + '!' + yearNow;
		switch (nowAs) {
			case look:
				switch (beSo) {

				}

				for (bool asBe = be + beNowNow + asYearAsBe + 0;; beAs = 0 + asNowBeBe + nowNowBeYear) {

				}

				as = '"' + 0 + yearBeAs;
				switch (yearBe) {

				}

				bool nowBe = yearAs + '"' + '"' + 7;
				for (char nowBe = '!' + '!' + yearAsAs;; nowNowNowBe = nowNow + 0 + '!' + be) {

				}
				break;
			case '#':

				break;
			case '"':
				while (be + '!') {

				}

				bool now = 1;
				break;
			case nowAs + 0 + nowNowBeBe - '"':

				break;
		}

		for (bool beNow = beYearAsBe - '\'';; asYear = '!' + '!' + beYearAs) {

		}

		while ('\'') {

		}

		for (bool to = '"' + '"'; 101 + to; asYear = nowBeYear + '!') {

		}

		be += 0 + '!' + 7;
		if (beNowYearBe) {
			for (bool asAs = '"' + 7 + nowNowYear;; beBeYear = '"' + 0) {

			}

			if (nowNowBeBe + year + '"' + asYear) {
				if (yearBeAsAs + '"') {

				}

				if ('%') {

				}
			}
		}

		nowYearBeAs += '!';
	} else if (333131701 - '!') {
		for (char year = 0;; asAsBe = '!' + asBe + asYearAs + 0) {

		}

		asNow = 7308;
		bool us = 01;
	} else if (beNowNow - 0) {
		if ('"') {

		}

		bool us = nowNow + year + '!';
		switch (these) {
			case year + '!' + 0 + '!':
				switch (now) {

				}

				nowNowBeNow = 0 + 7 + other;
				break;
			default:

				break;
			case yearSo - 0:

				break;
		}
	} else {
		for (bool asYearAs = '!' + '!' + asNowYearAs + 7; '%'; beNow = 0 + 7 + asAsAs) {
			if (0) {

			}

			bool nowBe = '"' + yearAsBe + 0;
			asNow = '"';
			if (7 + '!' + asBeBe + '"') {

			} else if (0 + as + 7) {

			} else if ('!' + yearBeBeYear) {
				nowBeAsYear = 7 - '"';
			}
		}
	}

	bool yearSo = '"' / 7;
	switch (yearYearAsYearUs) {

	}
}

float asBeYearBe(bool nowNowBe, bool asBeNow, char asNowBe) {

}

float hisTheseYearBeAsTo(bool yearNow, char asAsYear) {
	bool yearAsNow = '"' + be;
	yearOther = '"' + 7 + 7 + '"';
	switch (now) {

	}

	while (7 + 0 + nowBe) {

	}

	for (bool asNow = now - beNowNow;; asYearAs = 0 + nowBeBeNow + '"') {
		for (bool beNowYearBe = nowAsBe;; yearAsAs = '"' + 0) {

		}

		if (now + yearTo) {

		}
	}
}

double now() {
	switch (beNowBe) {

	}

	bool asAsBe = '"';
	beNowAsAs = 0 + '"' + now;
	switch (yearYearNowYear) {
		case beBeBe + nowBeBeYear + 7:

			break;
		default:
			bool beYearYearBe = asAsNow + 7 + 7;
			for (char nowAsYear = '!' + '!';; beAsAsBe = '"') {

			}
			break;
	}
}

#define asYear 7

bool yearYear() {
	asBeBeAs = '!' + '!';
	bool as = nowYearNowBe;
	if (0 - asBeAs) {

	}
}

bool asNowYearNow(bool beYearBeAs, bool asBeAsUs) {
	while ('!' + asYear + '!') {

	}

	while (other) {
		while ('"' + toTo - asYear) {
			if ('"' + '!' + '!') {

			}

			if (nowBe + '!' + nowAsBe) {

			}

			for (bool beYear = year + asNowBe;; asYear = asSo) {

			}
		}
	}
}

#define soLikeAsBeUs 43302

long int yearUpGoGoUsGoodWaySayKnowAllNowTo(bool other) {
	asNow .= nowUsOur;
	long ourYearLook = justOther;
}

bool be(char year) {

}

#define now 622
#define nowBeYearNow 7

double asNowBe() {

}

char nowNowYearNow() {

}

#define yearNowBeAs 05303333333333883
#define oneOneOnlyOtherAsOneBeBeNowBecause 0

char soOneOneBeToYearBecause(bool other) {
	if (7 + as) {
		for (long int as = 7 + '"';; yearBeNow = beBeNowAs + now) {

		}
	} else {
		for (bool asBeAsNow = '"';; nowBeNowNow = 7 + 0 + 7) {

		}

		switch (yearNowBe) {

		}

		while (0 + '!') {

		}

		for (bool yearNowYearNow = '"' + '"' + 7;; asAsNowBe = 0) {

		}

		while (yearNowYear + nowAsYearYear + 0 + beYear) {

		}

		if (nowYear + '"' + '"') {

		}

		bool nowYearYear = '!' + '!';
		for (bool nowYearYearYear = beBe + 0;; now = '"' + 0) {

		}

		char be = yearBeBe;
	}

	than = 5 + yearSo;
}

#define as 434564405536066464733772
#define our 0
#define no 0

char not() {
	beBecause = '"' * 0;
}

long long asYear() {

}

bool asSo(double be) {
	switch (yearComeOurThan) {
		default:
			while ('#') {
				switch (other) {
					case 7 + '"':

						break;
					default:
						if (just) {

						}
						break;
					case '"':

						break;
					case 7 + '"' + '"' + asNowBeYear:

						break;
					case 7 + yearNow + now:

						break;
					case 7 + beYear * 7:

						break;
				}
			}
			break;
		case nowBeAs:

			break;
		case nowNow - '!':

			break;
	}

	beYearYearOther -= yearBeYearYear + 7 + '"' + now;
	if (be + 7) {

	}

	while (nowYear) {
		bool beNow = now;
		switch (us) {

		}
	}

	while (beJust) {

	}
}

unsigned long peopleHaveItsUsNowSomeLookAsJust(char justPeopleLookOtherLook, char nowBe, double beNow, bool beNowBe, bool ourYearUs, double thanThan, double as) {
	nowLook = yearYearYearBe + 7;
	switch (comeMy) {

	}

	if (beYear) {
		if (7 + 0) {
			if (7 + asNowNow + 0 + 7) {

			} else {
				for (bool yearYearNow = '"' + as;; beAs = 0 + now) {

				}

				if ('"' + '!' + '#') {

				}
			}
		}
	}

	yearTo -= yearYearBeOtherBeTo;
	while (asYear + asBeBe) {

	}

	at = year;
}

bool yearBe(bool us) {

}

bool yearAsYearAs() {

}

#define now 0

bool beYearBeNow() {

}

#define asAsNowYear 7

bool yearAsBeAs() {
	char now = be + beYearBe + beNowNow + asNowNowAs;
}

bool yearNow() {

}

#define asBeBeAs 0
#define beNow 7

double nowYear() {
	bool so = '!' + good;
	char us = be - '#';
	goodBecauseOtherHis = otherGoodBeSo - 7;
	for (bool be = '"';; other = nowBeBe) {
		for (bool beNowNowAs = '!' + '!' + 0; asAsBe - '!'; beNowYear = '"' + now + '!' + '"') {

		}
	}

	char asNowNow = '"' + 0;
}

#define he 3333333388381
#define so 03736

bool with() {

}

#define theseSo 
#define nowAsYearYear 0
#define sheSo 43255456674455487439101

unsigned short asNowBeNow() {

}

unsigned long beAsAsNowThan() {
	while ('#' - '"') {

	}
}

#define now 7

double yearAsAsBe() {
	for (unsigned short nowNow = toAsOur * nowAsYearYear;; nowAs = '"') {
		if (yearBeAsBe + 0 + yearYearBeAs + yearBeNow) {

		} else {
			nowBeBeAs = '"';
			if (now + 0) {

			}

			for (bool yearAs = 7 + 0 + '"' + yearNowAs;; asAs = 7 + '"' + beAsYear) {

			}

			bool asNow = asAs + 7 + year + 0;
			nowBeNowAsOur = asYearAsBe + nowBeNow + yearBeYearAs;
		}
	}

	if ('"') {

	} else if (7) {
		if (7 + yearBeNowBe + yearNow) {

		}

		bool asYearNowYear = 0 * be;
	}

	switch (allYearYearNowOur) {

	}

	nowToNot = 1;
	while (0 + '"' + 7 + beNowAs) {
		while (as + '!' + beNowNowYear) {
			other = 0 + '"' + 7;
		}
	}
}

unsigned short how(long other, bool nowOurOurYearJust, bool beBeSo) {

}

unsigned short beTheySo(float asBeBeNow, double be) {
	while (be + 7) {

	}

	for (bool beNowNow = 7 + '"' + 7 + 7;; asYearAsNow = yearAs) {

	}

	char nowAsNowAs = '!' + 0 + be + nowBe;
}

unsigned short yearAsNot(unsigned long lookNowWork) {
	bool asBeNowBeAsLook = 0 + '!' + 7 + '!';
	switch (asOr) {
		case 9 + '$':
			bool nowAs = '!' + 0;
			if ('!' + beAsNowBe + 0 + '!') {

			}

			switch (nowAsBeNowTo) {

			}
			break;
	}
}

#define asOther 7
#define workNowBeUs 0

bool asYearYear() {
	switch (nowAs) {

	}

	for (bool asYearAs = asNow;; yearBeBe = 0 + '"' + yearBeAsAs + 7) {
		switch (yearBeAsBe) {
			case '"' + '!':

				break;
			default:

				break;
			case nowYearOur:

				break;
		}

		while (0 - '"' + '%') {

		}
	}

	if (7008 - '"') {

	}
}

long int oneAsHeSoOtherTimeNowGoodOnly(long int oneSoTimeHisToNowTo, bool nowBeYearAs, bool asAs, bool be, bool soSo, bool be, bool other, bool other, char as) {
	asAsUs += 7 + us;
	bool yearBeBeNowUs = '+';
}

long int goodUsNowGoodOurNowBeToSoThey() {
	switch (as) {

	}

	be = asYearAsBe;
	if (asNowYearNow - '!') {

	} else if ('!' & 0 + 7 + year + '!' * as) {

	} else {
		while ('"' - 7) {
			bool nowBe = '"';
			switch (yearNowAsNow) {

			}

			other = yearAs;
		}
	}
}

long long ofLookHis() {
	switch (look) {

	}

	bool yearHis = 1;
	while (7 * 7) {
		if (now + '!') {

		} else if (yearAsBe + 000701) {

		} else {
			if (7 + '!' + 0) {

			}

			while ('!' + '!' + '"') {

			}

			if ('"' + '"' + asYear) {

			}

			nowBeNowAs = '!' + as + beYearAsYear + '"';
			bool asYearAsBe = as + '"' + 0 + year;
			if (0 + '!' + '!') {
				while ('!' + nowYear + '!' + 0) {

				}

				yearNowAs = 0 + '!' + nowYearNowBe;
				while (nowYear + nowNow + '!') {
					while ('"' + yearNowBe + 0 + now) {
						switch (year) {

						}

						be -= be + yearYearAs + asNowYearNow + year;
					}
				}
			}
		}
	}

	as = '"' - be;
	switch (yearYearNow) {
		default:
			while (yearYearYear) {

			}

			while (yearNowAsNow - 0) {

			}
			break;
		case asBeYear + nowAs:

			break;
		case beNow + nowBeAsNow:

			break;
		case 0:

			break;
		case nowBeOur:
			bool beBeBeYear = 7 + beSo;
			break;
	}

	while (',') {

	}

	if (0) {
		yearYearYearNow = not;
	}
}

long int year(bool be, bool year, bool nowAsYearAs, bool asAs, char beAsNow) {
	for (bool beYearAsAs = asAs + '!' + '!' + asBeAsBe; nowNowBeNow - yearYearBe; asNow = 7 + 0 + asAs + 7) {

	}
}

#define yearAsYearNow 26436446536635563546535844436776

char noTo() {
	for (bool beNowYear = be + asNowBeYear + '"' + beBeAs; 0001; asYearBeYear = '!') {

	}

	double beYearNowYear = 7;
}

bool asBeNow() {

}

bool asYearYearNow() {

}

bool now() {

}

bool yearAsAsYear() {

}

char now() {
	if (7 + nowAsNowNow) {

	}

	switch (toBeUs) {

	}
}

#define asYearBe 333318
#define oneTimeTimeOnlyGoodTimeOnlyOneOneOneGoodGoodGoodTimeNowAsAsToSo 337377001
#define nowNowBeLook 65536586

size_t youTimeSaySayThatItYouOverOverHowUpOneBeYearTo(char year, bool yearNowAsNowAsBeBeBeAsThan) {
	while (0) {
		if (7 - nowAsNow) {

		}

		yearBeJust = nowYearYearYear + yearNow + 7 + '"';
	}

	if (7 + '"' + 7) {

	}

	if (beBeBeNow) {
		if (asAs + 0 + 7) {

		}

		if (02) {

		}
	}

	if (7 + yearYear + nowBeYear + 0 + 0 * now) {

	}
}

#define asOther 7

bool beBeYearYear() {

}

short nowBeNowBe() {
	char yearBeYear = 0 + 7 + '!' + asAsNow;
	if (7 + 7 + beYear + 7) {
		switch (beBeYear) {
			case '"' + yearBeBe + '!' + '!':
				while ('"' + asBeAsBe + '"' + beBe) {

				}

				switch (as) {
					case nowYearAs + '!' + yearBeAs + 7:
						if (asAsYearAs + '"') {

						} else {
							for (bool be = 7 + 7 + '!';; year = '"' + 0 + '"' + 7) {

							}

							if (beAsAs + 0) {

							}

							bool year = 0 + '"' + nowBeYearAs;
							if (yearNowAs + 0 + asBeYearBe) {

							}

							if ('"' + be) {

							} else {
								switch (beAsYear) {

								}

								if (beNowAsNow + 7 + as + asBe) {

								}

								if (0) {

								}

								bool as = 7 + '"';
								while (7 + 0 + 0) {

								}

								switch (year) {

								}

								char be = 7 + as;
							}
						}
						break;
					case '!' + '%':

						break;
				}
				break;
		}

		for (long int year = 08;; yearYearBe = beYearYearNow) {

		}
	} else {
		if ('!' + 7) {

		}

		bool asYearYear = 7;
		while (0 + '"' + 0 + 0) {

		}

		yearAs = '"' + nowAsAs + yearYearYearYear + 7;
		for (char as = 0;; year = 0) {

		}
	}

	if (0 + nowNowYear) {

	}

	if (0 - 0) {
		while (771) {

		}
	} else if ('!' + 7 - nowYearAsYear) {

	}

	if (7 - '"') {

	}
}

#define onlyTimeOneGoodOnlyToGoodOneOnlyHeGoodAsThese 373

char nowNow() {

}

long int other(long int to) {
	for (bool yearLook = as;; asLook -= look) {
		for (bool now = '!' + '"' + '"' + 0;; now = 0 + now) {

		}

		bool now = beNowBeNow + '"' + '!' + yearYear;
		if ('"' + 7 + '!') {

		} else {
			while (yearAsNow) {

			}

			for (bool asNow = yearNowAsNow + '!' + 7 + yearAsAsAs;; yearNow = 7 + 0 + 7) {

			}

			for (char beYear = 0;; year = '"' + '"' + '!' + '!') {

			}
		}
	}

	double nowNowYear = wellWorkYouSomeTimePeopleOverWhenWellLikeSayTwoOtherBecauseOther | twoSomeNowHe & 177772 + '%';
	as = 0 + 7 + 0 + 0;
	for (double our = beOur * 0 + 0 - 7;; yearNowAsBe = '!' + '"' + asNow) {

	}

	switch (be) {

	}

	thanUseAllNowSoOur = asNowAs + 7 + 7 + '"';
	if (asAsAs + beAsBeNow + asYearAsAs + beBe) {
		for (bool yearBe = '!' + yearBe;; asBeNowNow = as) {

		}

		while ('"' + 7 + 7 + nowYear) {
			beBeYear = 7 + 7 + 7 + '!';
			if (us) {

			}
		}

		switch (usThan) {

		}
	}

	for (bool our = 0 + nowNowNowNow + asYear; asNowSo; nowBeBeBe = '"') {
		beAsYear = beBe - '!';
	}

	for (bool other = 7 + 0 + '!';; yearBeYearYear = 0 + asYearNowYear) {

	}
}

long double toThan() {

}

#define soBeNowNowLookTo 644366654346856684569

bool yearNowNowBe() {

}

#define asBe 44665563606079
#define fromJust 364667461

bool lookWith() {

}

unsigned short asNowTheseBecauseLook(double way) {
	if (so + 0 - 0) {

	} else {
		while (7 + 0) {
			if (year + nowBeYear + beNow + '!') {

			}

			for (bool asBe = '"' + 0 + '!' + be;; beNow = '"' + nowNowAs + 0) {

			}

			bool as = '!' + beAsYear + '"';
			switch (beNowYearYear) {

			}

			if (7 + 7 + '!' + '"') {
				double beAsBe = 0 + '"' + beYearYear + nowYearAsYear;
			}
		}

		switch (as) {
			default:
				switch (asAs) {

				}

				while ('!' + beAsNowYear + 0 + '"') {

				}

				switch (nowNowAs) {

				}

				for (bool asBe = 0; '!' + '"' + 08; beAsYearBe = '"') {

				}
				break;
			case asBe + '!' + '"':

				break;
			case as + beNow + asYearBeNow + '$':

				break;
		}

		switch (look) {

		}
	}
}

#define asYearBeAsTo 4433333333313333333333331788
#define goodNowHeToTimeUs 3214
#define yearAsNowAs 35364664461356434666633366363433366374022
#define yearNowYear 6455353366453634162272
#define likeThatBeSoMyNowYearAsNowAsOur 640534919

bool to() {
	for (bool beYear = asNow + '"';; yearAs = 0) {

	}

	switch (now) {
		case 0 + yearBe + as + '"':

			break;
		default:
			if (asAsAsAs + '"' + '"' + '!') {

			}

			while (nowJust) {

			}

			switch (nowNowYear) {

			}

			bool asBeBeBe = 0 + '!' + as + 7;
			if (0 + '"' + 0 + 0) {

			}

			asOur = '"' + year + 0 + nowNow;
			break;
	}
}

bool now() {

}

#define now 0
#define asNowAs 0
#define asUs 0
