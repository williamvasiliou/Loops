#define becauseTimeOneGoodOneTimeOnlyUsTimeOnlyTimeTheseHeHeHeThese 3337383803
#define asYear 7

bool nowYearAsAs() {

}

float nowOtherNowBeSo() {

}

#define yearAsNow 0

float beYear(bool as, bool beAs, double yearAsNow) {

}

#define our 7
#define year 2
#define now 0
#define yearBe 7
#define so 102
#define allOtherWaySo 5172

bool nowSo() {
	switch (yearAs) {

	}

	bool asUs = wellGoodPeopleWantUpPeopleItAsThanSo - 7;
	while (nowUs * 0) {
		if ('G' + 71) {
			while (yearBeAs + '"' + now + 7) {
				float nowAs = beNowYearYear + asNow + '!';
			}
		} else {
			switch (beYearNow) {

			}

			beBeNowBe = '"';
			char asNowAs = '!' + '"';
		}

		for (bool be = '"' + nowNow; 67008 + 0 * '!'; beBe &= 7 + as + '"' + 7) {

		}

		long beNowAsYear = '!';
		bool nowNow = year + asBe + '!' * 7;
	}

	while (7 + 7) {
		switch (up) {

		}

		for (bool now = 7 / yearAsYear + 709;; nowBe = beAsYear + nowBeNowYear + '!') {

		}

		while ('!' + 0) {

		}

		while (nowBeAsNow % 0798 + '"' * 7) {
			if (be + 7 * '"') {

			}

			if (0 + 7 + 7) {

			}

			be = asAsAs + yearYear + yearNowNowBe + be;
			while ('"') {

			}

			nowNowAs = '!' + 7 + now + '"';
			our = be;
		}
	}

	while ('"' + beNowAsNow + 0 + 0) {

	}

	but = nowOur;
	for (bool nowNow = nowYearBe + beYearYearNow;; be = 7 + asBe) {

	}

	bool as = 0 + year + '"';
	if ('!' + '!' + '!' + 0) {

	}

	nowTo = 0 + beBe + asAsAs + 0;
}

#define asAs 7
#define now 7
#define than 0
#define soThan 5333333333333330333378307107718
#define us 3333452548
#define asNow 7

double year() {
	for (double asYear = asAsNowAs + '"';; nowBeNowBe = '!') {
		switch (wantTheHowThereKnowTheHaveOneWantThatWellPeopleHowNowPeopleGoodItsThereOverOfAllAsGiveBeOverThereThatUseBeThanTimeGoodGoWantAsOrHow) {
			default:
				unsigned int theHowOurAllThese = '-' - '#';
				while ('e' + 'B') {

				}

				asNowAs |= 'e' >= '-' >= 53000701 + 'k' != '!' || 19 != 'q' > nowLook + 3;
				break;
			case yearYearAs == 7 - 'g' % 7 + as * beNowNow:
				for (bool yearAsBe = beYearNow + 0 + 7 + yearBeNowBe; '&'; nowBeAsNow = 0 + 0 + 7) {

				}
				break;
			case '"' + beAs + nowBe + '!':
				short with = 7 - nowOther;
				asBecause ^= 92 < '!';
				for (double thatUs = asNowBe + 7 + 7 + '"'; 8001; so = 7 + '"') {

				}
				break;
		}

		for (unsigned short sayGiveGoTwoHaveGiveSheHowNowItsHaveSheOfButAsTo = 43678 == useWantSomeAllTwoBePeopleYearNot - ourHaveAsYearWould - 18 - 0; 6364344443367465956454641 && 'y' && 456376635535530670823 != 'o' & useYearOther >= 65305525707799; haveGoodUpWellKnowThePeopleTheThereUpOfThatTheComeUpTheTwo -= 0 + yearBeBe) {
			for (double thanNowAsThan = 7 + '&'; be - '"'; nowAsBeTo = '!') {

			}

			asAsYear = nowAs;
			if ('!') {

			} else {
				for (bool asNow = be + '"' + nowAs + 7;; yearAsYearAs = 7) {

				}

				if (year + '!' + 0 + 0) {

				} else {
					if ('!' + asAsNow + nowAsNowAs) {

					}

					if (asYearAs + beAsNowBe + '!') {
						if (other) {

						}
					}
				}

				bool now = 7 + '!' + 0 + be;
				switch (asBeYear) {
					case our:

						break;
				}
			}
		}

		size_t useHowComeKnowSomeUseUseGoodItsUpWellHerThereFromWellItSheThatUseGoodWhenUsePeopleThereComeWorkOneTheKnowTheSheUseTimeWhenItJustUsWantSheSayComeYouNotFromWith = 6654535456544533368566666342791 && 554656443646456456445543445355564655544434365643633436536664535563563566664463444436436441954256963434334534079 || '2' != 'b' + 600141 > would / yearBeYearBe;
		for (long int asHis = 179 || 244256 && '"' != 7 - '!'; 1002; yearBeBeBecause = nowNow - 7 + 0 + than) {
			if ('{' - 7) {
				switch (haveYearBeSo) {

				}

				switch (beNowYearYearAsAsThan) {
					case 9:

						break;
				}

				bool just = now + 0 + asBe + year;
			} else {
				while ('"' + 0 + '!' + nowNow) {

				}

				while ('!' + beBe + '!') {

				}

				year = 0 + beAsBeBe + 7 + '!';
				bool beNow = 7 + asNow;
				while (now + asYearBe - '!') {

				}
			}

			while ('!') {

			}

			switch (asAs) {
				case '!' * 0:

					break;
			}

			switch (not) {

			}
		}

		beBeYearOther -= '*';
		for (long double nowNowBeYear = 7 + 7 + '!';; yearYear = asBeAs + 7) {

		}

		for (bool other = 0 + 71;; asAs = year + '"' + year + 0) {

		}

		while (just) {

		}

		switch (nowAs) {

		}

		bool be = 0 + 7 * yearYearBe;
		char now = '!' + beBeNow;
		butYearAsYearUs += '!';
	}

	switch (peopleHaveGiveGoodThereTheComeThatAsTheAsNot) {
		default:
			for (unsigned int ofSheOnlyWantComeOtherUsBeUsLookYearBeAsTo = ':' < '!'; nowNowAsAsJustWhen + 072 - 7; ourOur = '"' + beAs + '&') {
				whenGoodThese &&= usGoNoAtNowFrom % 103771;
				if ('!' + beBeAsYear + '"') {

				} else if (yearBeAsYear) {

				} else {
					switch (nowAsYear) {

					}

					switch (beNowNowNow) {
						default:
							while ('"' + 0 + as) {

							}

							for (bool beYearYearBe = 7 + 0 + '"' + 0;; beYearNow = 7 + '"') {
								switch (now) {

								}

								beBeAsThan = 0 + '!' + 7 + as;
							}
							break;
					}
				}

				while (yearAsYearAsBeOther) {
					bool nowBeBe = 0 + beAs;
					for (bool year = asBeNowBe + 0 + yearAsYearNow;; nowAs = year + 0 + yearBeNowNow) {

					}

					for (bool nowBe = 0;; beNowAsNow = year) {

					}

					for (char asYear = asJust;; nowAsAs = 0 + as + '"' + '"') {

					}

					if ('"' + asNowNow + 7 + '!') {
						switch (nowYearBe) {

						}

						for (bool now = now + nowYear + now + 0; '$'; as = asAsNowBe) {

						}
					}
				}
			}

			long long giveThemOfYouKnowOurNoComeUsThan = justGoodLikeOverWantWantToLikeTimeYearTo;
			size_t sheGoThereItGoGoPeopleThatItYearHerItsPeopleNowWayThereOurSayOurNowYearJust = 3504050075 - 0 > '"' * '.' + nowAsTo;
			beTo = 7 + yearBeBe + 0;
			long lookFromLook = 7 + 7 + now;
			for (bool asYear = be + '!';; our = '!' + yearYear + '"') {

			}

			for (bool asBeBeYear = 7 - beNowBeBe * now; '\''; beBeYearBe = as + '!' + '"') {

			}

			switch (beAsAsSo) {

			}

			while (nowYearBe + '!') {

			}

			nowBeYearYear = 7 - 0;
			break;
		case goOrHerOfNowHowTwoComeOnlyWellUpWellGiveItUpOverHowOrYouPeopleItsBeYouWantOfLikeWhenComeSaySayHaveComeSheOnlyPeopleGoodOfOneOfHaveWellTimeTimeOnlyWouldThatThatLikeHowOneToYearThese + nowNowYearMy:
			while (twoLookAsYearJust > 0 + yearYear * '!') {
				switch (beYearYearYear) {
					case year + '!':
						for (bool yearBeNowYear = 0; look; yearNowBeBe = nowAs + beAs + 0 + asYear) {

						}
						break;
				}

				if (be * asYear) {

				}
			}

			bool as = year + 0;
			for (bool as = 7 + 771;; now = '!' + '"' + 0) {

			}

			while (nowBeSo) {

			}

			for (bool beYearNow = asAs + 7;; asBeAs = '!' + '"' + '"') {

			}

			beLook = beAs + yearNow + '!';
			while ('"') {

			}

			while (0 + 7 + as) {

			}

			for (bool year = 7 + nowNowAsBe;; yearNow = nowAs) {

			}

			for (bool beYear = '"' + asNowBeNow; 1; nowYear = 0 + '!' + beYearYearBe + 7) {

			}
			break;
		case 'y' + useGoodNoOther || '+' + nowBeNow * nowAs:
			bool other = '!';
			for (bool other = '.'; howBeTwoUsSo - '"'; as = year + 0 + 7 + '!') {
				short asThan = 3443 * '%' + 0 * 0;
				while (soAsBeOther - nowYearAs) {

				}

				while (as + '&') {
					if ('"' + now) {

					} else {
						beOur = as + '!' + 7;
					}
				}

				bool beYear = '"';
				if (0 + 7 + 0 + 7) {

				}

				if (7 + year + yearYear) {

				}

				switch (nowAsNow) {

				}

				yearAsYearNow = beNowAs + nowAs + 7 + yearBeNowBe;
				other = 7 + asYear + '"' + '!';
			}

			switch (be) {

			}

			if (look) {
				switch (yearAs) {

				}

				for (bool beYearNow = '!';; beNowYearNow = yearNowNowNow + '!' + yearYear) {

				}

				for (bool yearYearYearBe = 0 + 7 + yearYearNow + '!';; as += '"' + asBeNowYear) {

				}
			} else if ('"' + '"' + '"') {

			} else if (asNowNow + '"' + 7 + be) {

			} else if ('!' + now + beBe + beNow) {
				asAsAsBe = '!' + yearAs + 7 + 0;
				for (bool nowBe = yearBeYear + nowBeYear;; now = asBe + nowNow + '"' + yearNow) {

				}

				switch (look) {

				}
			}

			than = '!' + 0 + 7;
			break;
		case just >= beLook - '"':
			bool beYearNow = 7 + '$';
			char nowAsNow = 0 + asBe;
			for (bool yearYear = '"';; our = '"' + as + beYearAsAs + 7) {

			}

			switch (asNowLook) {

			}
			break;
		case 679:
			for (unsigned int fromTimeGoItThan = 0057000770077777007002 + 7089; '#'; beYearBe = year + 7) {
				bool as = as;
				bool nowBe = 0;
				for (bool asYear = asYear;; year = 0 + '!' + '"' + '!') {

				}

				switch (year) {
					default:
						yearLook = '!';
						break;
				}

				for (bool yearNowAs = 7;; asAsYearYear = '!' + '"' + nowNowNowAs + 0) {

				}

				double now = '!';
			}

			for (double yearYearBe = beAsYear; 'a' + notAsSo || 7 && 0 * '"'; just /= 7 + nowYear + beNow + nowBeNow) {

			}

			bool yearThanTo = asOther;
			while (1) {
				if (yearNowAs + nowNowNow + '"') {

				}

				if (7 + '!' + '!' + 7) {

				}

				while ('!' + 7 + '!' + asAsAs) {

				}

				for (double beNowYear = '"' + asBeBeBe + 7;; yearBe = '!' + 0) {

				}
			}

			char lookNot = 0;
			break;
		case 0 + as + '!' + beAsBe:

			break;
		case nowYearBeNow + '"' + nowNow + asAsBeNow:

			break;
		case lookSo / 28 + '&':
			nowBeNowBe = yearYearNow + '"' + '"';
			for (bool yearNowAs = 0;; yearBeBeNow = 7) {

			}

			switch (us) {

			}
			break;
		case 08:
			while ('!' + 7) {
				for (bool asAsAs = '!' + 7; beBeAs + '.'; asBeNowBe = 0 + asAsAs + nowAs) {

				}
			}
			break;
	}

	while (79 == '(' || '"' <= 2) {
		while (ourJust + '.') {

		}

		switch (thatWellKnowOverHerThanThemNotBeThanYearAsAsTo) {
			case '!' + '"' + 0:

				break;
			case yearYearSoBeTo:

				break;
			default:
				yearAs &= '!' + asAs + 0 + beYear;
				break;
			case beNow + 0 + nowNowNow + '"':
				bool beToAsTo = yearYearYearNow;
				break;
		}

		for (bool beYearBe = beBeNowAs + 0 + '!' + nowNow; '%'; yearYearBe = nowAs + 7) {

		}
	}

	switch (wellJustTimeHowNowWithAsThan) {
		case '"' + '"' + yearBeAs + nowYearNowNow:

			break;
		case 7 * 0:

			break;
	}

	switch (yearAsAsThan) {
		default:
			switch (be) {
				default:
					bool beAsBeAs = '!' + yearBeBeYear + '!';
					switch (us) {

					}
					break;
			}
			break;
	}

	if ('$' + 7 + '<') {
		while ('&') {

		}
	} else if (nowNowAs * now) {

	} else if (nowAsYearBe) {
		switch (as) {

		}

		while (asBe + '"' + yearYearBe) {

		}

		while (1) {

		}
	} else if (asAsAsAs + 0 + beAsBeAs) {
		switch (asOther) {

		}
	}
}

#define theWantSomeHowGiveUseWhenWantWorkSheWorkItWantWithSayJust 66665466436637435934
#define workKnowHowKnowTwoHowAsNowGoNot 5505520721

long long asYearTo(bool yearNowNowNow, bool nowBeJust) {

}

#define usHowMyLikeOur 054736028
#define than 9

unsigned long sayTheWellNoYearNowNowYearYearYearUs(char beNow) {
	bool other = atBeWant * '"';
	while (yearUs * '"') {

	}

	now -= beYearYearAs + 7;
}

#define nowNowBut 3471
#define yearAsNow 0
#define us 70000702

double yearBeAs() {
	if (than) {

	}
}

bool nowYearNow() {

}

#define yearAsNowYear 7
#define asYearYear 7
#define beAs 565454554555455454645665565305458

char yearYearNowYear() {

}

double nowAsBe() {

}

#define year 933337333333337333701
#define goodTimeOneTimeSoSoSo 331313173
#define beYearBeJust 444516346323
#define nowBeAsYear 7
#define beBeAsYear 5

bool now() {

}

#define yearYearBeYear 0

bool beYearNowBe() {

}

bool beNowAsYear(bool nowAsYear, char year) {
	switch (usBeHe) {
		case 7379 || than - 7 + '!' <= '!':
			yearThat = asAsYear + '!';
			switch (thanNowTo) {

			}
			break;
		case asBeYearNow + '%':

			break;
	}

	while (atYearYearBeJust + '!' - year) {
		while (asAsBeYear + 7 - nowAsBe) {

		}

		for (bool nowBeNowNowLook = 0;; yearYearBeNow = '"') {

		}
	}

	for (double yearAsNowAs = yearBeBeNow + now + as; 009; nowYearNowYear = yearNow + 0 + be + 7) {

	}
}

unsigned char other() {
	while (7 + 0) {

	}

	for (bool asBeNow = 7 + be + '"' + '!'; '&'; beAsAsYear = '!') {

	}
}

#define youComeWorkHowHaveTwoThan 628

double yearThan() {

}

#define nowOtherAsAsOther 02
#define yearBeNowAs 3
#define nowBeBeYear 0
#define nowAsAs 7

bool nowUs() {

}

#define be 570330133

long int oneGoodSoOnlyOnlyHisOnlyGoodAsTime() {

}

#define he 33138310083

long int asThereWouldYearAsThan() {

}

double be() {

}

bool asBeYear() {

}

bool now(bool nowAsAsJust) {
	for (long long justLook = 0 != beAsNow + '#';; nowBeAs = '"' + '!' + '!' + asAsYear) {

	}

	if (73) {
		while ('&') {

		}

		if ('$') {

		} else if ('"' + yearNow) {

		} else if (7 + yearBe + '"') {

		} else {
			bool yearYear = yearBeNowBe + yearAsYear + '"';
			switch (asLook) {

			}
		}

		long asAsYearAs = 7 + '"';
	} else if (so) {
		other = '"' + 7;
	}

	switch (youTwoYouJust) {
		case 0 + 7 + '!' + 0:
			while ('!' + 7) {

			}

			while (0 - nowAsNow) {

			}
			break;
	}

	double so = nowOther;
}

#define thereBeNowToThan 348089
#define someNowItTimeGiveNotYearYearAsBeAsThemNowOur 4003
#define hisYearYearBeUsYearThan 2

short soUs() {

}

unsigned long howFromFrom(bool than, double nowBeAs, float asYear) {
	for (double year = '!' + '!' + '"' + 7;; beYearNow = '!' + '!' + beAsYearYear + '"') {

	}
}

long int yearNowYearAs() {

}

bool be() {

}

#define asBeThese 35423663640306663359406967001
#define comeYearHisBeLook 4

unsigned short peopleYearJustBeThanOther(int beAsAs) {
	bool nowBeYearAs = 0 + '"' + 0 + '"';
	bool now = '!';
	bool us = '!' + be + '"';
}

#define nowYearBeAs 0

char beYearBeAs(double yearNowNow) {
	long int oneSo = 7;
	if ('"' != '!' + '\'') {
		if (yearBeBe + '!') {
			if (to) {

			}
		} else {
			while (beNowNowBe + 7 + now + 7) {

			}

			asAs = '!';
			while ('"' + '!' + nowAs) {
				while (nowAsBeYear) {

				}

				while (0 + 7 + as + 7) {

				}

				while ('"' + asAsNow + '!') {

				}

				while (0 + '!') {

				}

				for (bool nowBeBe = '"' + 0 + 7;; yearAsNow = 7 + nowAsNow + '!') {

				}

				switch (nowBe) {
					default:
						while (yearYear + yearYear + '!' + 08) {

						}
						break;
				}
			}
		}

		while ('"' + be) {
			switch (asYearYearNow) {

			}

			if (yearBeBe + 0 + 0 + '!') {
				if (708) {

				}

				beTo = 0 + '"' + yearBeYear;
			}
		}

		while (asAs + '"' + 7) {
			bool now = '"' + '"' + asAsBeAs;
			while (beOther + yearYearYearUs) {

			}
		}

		bool year = '"';
		while (nowYearNow + beBeBeBe) {

		}

		switch (to) {

		}
	} else {
		if (0 + 0) {
			bool asAs = '"' + 0;
			if (0 + 0) {
				while ('!' + 7 + '"' + beBeYear) {
					switch (us) {

					}
				}
			}
		} else {
			for (bool nowAsNowBe = beAs + now + '"' + asAs;; beBeNow = '!') {

			}

			bool asNowNowYear = 0 + '!' + nowAsYear + year;
			if (yearAs + 0) {

			} else if (yearNowAsYear + nowYearYearYear) {

			} else if ('"' + 0 + '"') {

			} else {
				for (bool yearBeBeNow = 0 + now; '!' + '#'; beBe = yearBe + '!') {

				}
			}
		}

		be = 7;
		switch (asYearTo) {

		}
	}

	switch (asNowNowAs) {

	}

	for (bool asBeAs = be + 0 + '!';; be = 0 + 081) {
		as = now + year + '"' + 7;
		only = '!' + yearBe + beNowAs + 0;
	}
}

long int asGoodOnlyOnlyTimeSoYearBeBeYearAsUs(float be, bool yearTo, float otherTheseBeToJustNowYearOur) {
	bool otherBeBeJust = 0 + 7 + nowBe;
	long int nowJust = '"' + nowNowBe + beBe + asBeBe;
	asYear = '"' + 7 + nowAs;
	bool beAsBe = 7 + 0;
	for (bool nowYear = 0 + 0; '"' + 0 - 7; now = '!' + as + '!' + beBeBeYear) {

	}
}

bool yearMy() {
	for (unsigned int goLikeComeLikeTwoYouNowGoodComeWellOur = '{' && overWantOneHisThatOtherGoodKnowAsOurNowBeThan + beLook + now / 7; 55335274588479 * 7; asSoAsAsTo += 5708 - '-') {
		for (unsigned int asNowBeYear = our != '&' + '4' - '"' + 7 * 0; 4520090708 <= 9; nowSo -= 0 + 0 + '"') {
			while (78) {
				for (bool than = 0;; asNow = be + nowAs + asBeYearAs) {
					double yearYear = 0 + '"';
				}

				switch (asAsYearYear) {

				}

				for (bool nowNowNow = 0 + 0 + yearYearAs + 0;; asYearAs = '!' + '"' + '"' + 7) {

				}

				while ('!' + yearBeNow + beThan) {

				}

				yearOur = '"';
			}

			for (bool as = year + 7;; yearNowNowBe = nowAsNow) {
				if (yearAsYear / beAs) {
					asAsAsBe += 0 + '"';
				}
			}

			if ('!') {

			} else if (asBe + '"' + '!' + 0) {
				while (beBe + '!' + '"' + yearNowYear) {

				}

				beBeAs = nowBeAs;
				bool beYearNowYear = 0 + '!' + asAsNow;
				asNowYear = asAsNowTo;
			}

			while (asYear) {
				while ('!' + '"' + nowNowAsAs) {
					beYearAs += 7 + '!';
				}
			}

			switch (now) {
				case '&':

					break;
			}

			if (beNowYearAs) {

			}

			char beAsYearYear = 0 + '"' + beAsBeBe;
		}

		if (6661071) {

		} else if (be + nowYear) {

		} else {
			if (78) {

			}
		}

		while (nowNowSo) {
			while (yearAsBeNow - yearAsBe) {

			}

			switch (nowAsBeAs) {

			}

			nowAs = nowBeBeNow + '"' + nowBeNowYear;
			double nowBeNowAs = 0;
		}

		for (bool yearAs = 0; yearUs; as = 0) {

		}

		while (7) {
			if ('"' + '"' + asYear + nowAs) {
				for (bool year = '!' + 0;; beAs = '!' + nowYear + 7) {

				}

				bool nowAsBeNow = nowYearBeBe + year + yearYearYear + nowAs;
				yearThan = '"' + 1;
			}
		}
	}

	while ('`' > 0643356343545535345646354443356345544635353463333643553633343454554453643436654335535366633543364344544533445633444634456646644566563334365335555346345345366566434337563666456653436346334636493634653646443454566336643553563363453433760650770778 != 536365656665664334536553436055454456043546661 != 'g' & 5645365345364435353554356535655634634355535436363654335555645546553645344464556646636453636443563445466345536436665465353353645456565563343546354666524333336564653643436446365545565435335365665654635443546644536535343333553575444374343354476475343 > 001 >= haveSomeItOnlyGoodHeOurAsAsYearBecauseThey || 'w' >= 35 == '%' || '!' || 77779 & theyJust | '$' * 0) {
		while ('[' - 0 - 0) {
			for (short than = '"' + 7 * now;; nowBeNow = '!') {

			}

			so = '"' + 7 + 0;
		}

		switch (to) {
			default:

				break;
			case 02:

				break;
		}

		switch (beNow) {

		}

		yearAs = 7 + beYearBeAs;
		bool asHeJust = yearAsYear + '"' + nowYearYear + nowAsAs;
		asYearAs += 0;
	}

	giveJustComeSayWhenKnowTwoTwoWellWhenOneOneThanPeopleOtherHisGoAsYearBeNowAsJust ||= 't' >= nowHaveAsThanNowThan;
	beYearBeYear =~ asBeNowOur != 'n' && 84708 + '.';
	while (2100002 * 'c' > '!' - yearAs) {
		for (long beNow = 7 - as;; nowBeBeBe = nowYear + '!' + 7) {

		}

		for (long double yearAsNowAs = 7 + 0 + asNowNowAs + 0;; year = '!' + 7 + '!' + 0) {

		}

		for (bool now = yearAsYearNow; '(' + '!' - '!'; now = '"') {

		}

		if (2 * 7 + '\'') {

		} else if ('!' + asAsNow + 7 + 0) {
			for (bool nowNowBe = nowNow + 0; just; beAsAs = '"' + 7) {

			}
		}

		if (yearBe) {

		}

		yearNow = yearAs + yearNowNowAs + 0 + yearBe;
		beYearNow = '!' + asBe + beNowBe;
		char yearYearNowNow = '"';
	}

	switch (youBeTimeHowJustNowOverAsSoTo) {
		case ofNowHowOfUseThem:
			double sheUs = '!' * 001 - '"';
			unsigned long way = '!';
			switch (nowYearYearNow) {

			}

			for (bool yearYear = yearBeAs; nowAsYearYear * beYear; nowAsYearNow = '!' + '"' + 0) {

			}

			bool yearUs = yearAs + asYearBe + 0;
			break;
		default:

			break;
		case 7 + yearBeBe:
			switch (year) {
				case yearAs + 0 + nowAsYearNow + asNow:

					break;
				case '!' + 0 + 0:

					break;
				default:

					break;
				case 7 + beAsBe + 0:
					beAsBeYear = 7 + nowYearYearNow + be + 0;
					switch (nowBeNow) {

					}

					bool yearYearBe = 0 + 2;
					break;
			}

			for (bool as = beBeNowNow; '#'; asYear = '!' + '!') {

			}
			break;
		case '"' + be:
			if (7 + '"') {

			} else if (nowYearAsYear + 7) {

			} else if ('!' + nowBeYear) {
				switch (beBeBeNow) {

				}

				for (bool now = '"' + '!' + 7 + 0;; now = '!') {

				}

				for (bool now = '!' + 0 + asAs;; asBeYear = 7 + '"' + '"') {

				}

				for (bool asBe = as + 7 + '"';; beNowNow = yearNow) {

				}

				while (7 + 0 + beBe + '!') {

				}

				for (bool yearBeYearYear = '"' + year + beAsBeNow + 7;; now = nowAs + 0) {
					if (71) {

					}
				}
			}
			break;
	}

	double nowYearYearNow = '!' + nowUs;
	for (bool asAsAs = '"'; '7'; asLook += now + '!' + beAsYear + beYearNowBe) {
		double beNow = 7;
	}

	if (0) {

	}

	just = 0 + 7 + '!';
}

#define ourButSo 606731

unsigned short theseButWith(bool year, float year) {
	while (1) {

	}

	long int nowYear = '&';
	for (double now = 0 + beAsYear + '!';; asAsAsAs = 7 + 0) {

	}
}

#define likePeopleKnowThatOfUpLikeItsUseOnlyGoodItComeBeItWhenTimeLikeWorkWantGoItHaveKnowTwoUpHerSheTwoUseComeTwoTheseThatOfItsUpJustGoodKnowYouYearSheBecause 3356366545450544364663353443846506

long nowBeYear(char beAsOther) {

}

char beAsYearYear() {

}

bool beAsYear() {

}

#define asYearYear 0

bool yearYearAsYear(long nowBe) {
	while ('"') {
		if (0 + asNowAsYear) {

		} else {
			if ('"' + 0) {

			}

			if (asThan) {

			}
		}
	}

	while (wantJustAsFromUs < 353453477871 >= just && yearNowYearBe + 8) {
		if (701) {
			for (bool asAs = just; '&'; yearNow = yearYearBe) {

			}

			for (bool year = '"' + nowNow + 0 + asNowYearNow;; asNow = 0 + beYearYearUs) {

			}

			if (209) {

			}

			nowOur = nowAsAsAs + yearBeNowNow + '"' + 0;
		}

		double be = '&';
		for (bool as = '!'; '$'; as = '"') {

		}

		switch (just) {

		}

		while ('"' + 0 + '"' + '"') {
			while ('$') {

			}
		}

		switch (asBeBeAs) {

		}

		if (beYear + 0 + beYear) {

		} else if ('"' + 7 + '"' + asYear) {
			for (bool be = nowAs + nowAsNow;; be = 0 + '!' + 0) {

			}

			switch (to) {

			}
		}
	}

	while ('.') {
		bool beAsBe = nowNowYearNow;
		bool nowAsYearBe = '!' + 0;
		for (bool as = beNow - '"';; yearBeBe = yearBeNowBe) {

		}

		if ('#') {

		}
	}

	switch (now) {
		default:

			break;
		case 0 + '"' + '!':
			char asAsBe = '"' + '"' + year;
			break;
	}
}

bool as() {
	if (7 + 702) {

	}
}

#define whenHis 7200780002

float asYearYearAs(bool nowSo) {
	for (bool onlyOneOneHeOneOtherOtherOther = 0 - '"' + 1;; onlyHisNowUsUs |= his != '/' / 337088 / '!') {

	}

	while ('/' - '!') {
		if (yearAsBeAs + 0 + 0 + 0) {
			if ('%') {

			}
		}
	}

	if (01) {
		while (beBe) {
			bool asNowNow = '"' + '"' + '!' + 7;
			beBe = 7 + '!' + nowBe + nowBeBe;
			for (bool yearBeAsYear = 7 + beYearAs + year + 7; '!' - 0; yearBeAsBe = yearAs + year + 7 + asAsBe) {

			}
		}
	}

	beAs /= asYearBeYear + beNowBe + asNowBeBe;
	while ('"' + nowBe - 0) {

	}

	while (7) {

	}

	if (1) {

	}
}

#define sheThese 0

bool yearBeYear(double nowNow) {
	if (useOverSomeTimeOnlyHaveSomeAsOverComeTimeOfThatItsTheUseComeThereHowUpGoOverGiveBeWantNoNowLookOurOur - '"') {
		switch (knowIts) {
			case 002:
				switch (yearBeAsNow) {

				}

				switch (yearAsAs) {
					default:
						while (0 + beAsBe) {
							for (bool nowYearYearYear = asYear + 7 + 7;; nowBe = '!' + 7 + 7) {

							}

							if ('"' * '!') {

							}
						}

						nowYearYearAs += '!';
						break;
				}
				break;
		}

		if (')') {
			nowYear = nowAsAsBe + 0;
			for (bool be = '!' - '!';; be = yearBe + '"' + nowAs) {

			}

			for (bool yearBeAsAs = asNowYear + nowAsBe + 7 + '"';; yearAsAs = '"') {

			}

			just = now + 0 + 7 + 7;
		}

		if (7 + 51 >= 086008 & '#') {
			switch (fromThan) {

			}

			bool asYearYearYear = '!' + nowYearAs;
			bool thanNowOurYearLook = as + 7;
		} else {
			for (bool yearBeNow = asBe;; nowYearNow = 0 + asBeAs + 7 + 7) {

			}

			switch (year) {
				case '!':
					switch (now) {

					}

					for (char yearYearNowBe = 0 + yearAsAsBe + '"' + as;; asYear = '"') {

					}
					break;
			}
		}

		if (71) {

		}
	} else if (0 != they + yearAsAsBe - '"' * nowNow) {
		while ('"' + '"' + 7) {

		}

		if ('!') {

		}

		switch (year) {

		}

		while (nowAsNow) {
			while ('"') {
				bool beBe = 0;
				while ('!' + '"' + '"' + 7) {

				}

				while (beYearNow + '!') {

				}

				beAsBe = asBe + nowNow + nowNow + beNow;
				bool year = be + 0 + asAsAs;
				beYearYear = '$';
			}
		}
	} else {
		haveNowFromNowJust /= '-' + nowJust;
		for (bool asBeBeAs = 0 + 0 + beYear; '%'; be = nowYear + '!' + 0 + '"') {

		}

		for (bool beBe = 7 + beAsAs + yearBe + '"';; nowNowYear = '!') {

		}

		asAsBeAs -= '!';
	}

	for (long long sheTo = other || soOtherNowOur && 388 & yearBe - '"' - 7 * '"'; 1 + '&'; timeUseUsToNowWith |= '9' + '"' + '&') {
		for (bool nowBeNowYear = beNow;; now = 7 + 0) {

		}

		nowYearAs /= 7 + yearYear + beNowAs + asYearBe;
	}

	bool beBeNowYear = 0 + '!';
	orBecauseOtherGoBeNowUs = just % be;
	double now = 71;
}

#define youOneTwoHerGiveUseSheOnlyTimePeopleComeTheJustHe 36544666565466272
#define nowOur 0
#define toJust 7

long int haveSoYearLook(bool yearYear, bool be, bool asAsBe, char as) {

}

long double than(double nowYear) {

}

#define beNowAs 0

bool be() {

}

bool nowYearBeBe() {

}

bool asBeYearNow() {

}

#define as 0

bool as() {

}

long asAsNow() {
	if ('%') {
		switch (nowAsSo) {

		}

		while ('!' + '!' + 0 + 7) {
			if (0) {

			}

			bool asBe = 0 + 0 + yearYearYearNow + '"';
			while (be + 0) {
				while (0) {

				}

				if (be) {

				} else if (0 + yearNow + beAs) {

				} else {
					now = 7 + '!' + '"' + nowYear;
					for (bool nowYear = as + '"';; yearYearNowYear = '!') {

					}

					switch (year) {
						case beBeAsBe + 0:
							while (beAsLook) {

							}
							break;
					}
				}
			}

			nowBe -= beNowAsAs + '!' + 0 + be;
		}

		long asYearAsYear = '!' + '"';
		for (bool be = as;; beAs = year) {

		}

		switch (be) {

		}

		bool as = year + 7 + 7 + 0;
		year = '!';
		nowBeYearAs = '"';
		yearYear = 0 + asNowAs;
		if ('!') {

		}

		switch (be) {

		}

		if ('!') {

		}

		switch (beNowAsYear) {

		}

		double beBe = '"' + 7 + be;
	} else if (0 + 7 + '!' + nowBe) {

	} else {
		yearBeNowBe = 7 + 0 + '"';
		switch (asYearYearBe) {
			case asOur:

				break;
		}
	}

	if (as + 0 + '!') {
		switch (us) {

		}

		if ('"' + 7 + asYearYearAs) {

		}

		switch (beThan) {

		}
	}
}

unsigned char herLookMyAsBeJust(unsigned char becauseBeOtherYearJust) {

}

size_t at(bool yearAs, bool asNowTo) {
	if (year + asBe + 7) {

	}

	while (72) {

	}

	for (double yearYearYear = yearNowAsNow;; asNow = '!' + nowAsAs + yearNowYear) {

	}

	nowAs = '!' + '"';
	for (bool asThan = 0;; yearBe = beAsAs) {

	}

	switch (yearAsBeAs) {

	}

	for (bool as = '"' + 7;; now = 7 + nowYearAsBe) {

	}

	yearYear = '!' + '"';
	nowAsNowYear = '"' + '!' - 0;
}

bool year() {

}

#define with 88

size_t yearWantThatAll() {
	double nowAs = 7 + '!' + beYearNowNow + '"';
	while (0 <= yearNow) {
		while (02) {

		}
	}

	because = asYearBeYear + '!' + 0;
	bool as = '!' * '%';
	for (bool nowNowYearBe = 7 + year;; yearNowBeAs = 7 + 0 + '"' + '!') {

	}

	for (bool beNow = '!' + 0 + asAs + '!';; beYearYearNow = '"' + 0 + yearNowAsBe + now) {

	}

	nowAs = '!' - 0;
}

#define look 6333333377331381
#define goodOtherOneUs 731

long int goodHeOneGoodOnlySoSo(float yearBeNowAs, char yearBe) {
	char yearNowBeBe = 7;
	beUs = be + '"' + yearAs;
	yearHisOther = 0 + 0 + 7;
}

bool asNowHer() {
	if (0) {
		bool as = 2;
	} else if (0) {

	} else {
		bool beBeAsAs = '!' + '"' + 7;
		while ('!' + year + '"' + 0) {

		}

		switch (asAsNow) {
			case 7 + 7 + 0:

				break;
			case 7 * yearYear:

				break;
		}
	}

	switch (beYearAsAsUs) {

	}

	while ('!' + '"' + yearBeBe + nowAsAsNow) {

	}

	if (asNowAs + 0 + yearBeNowAs) {

	}

	if (yearNowBe + be + beBe) {
		switch (yearBeThan) {
			case nowFrom * beNowNowAs:

				break;
			case '#':
				bool beNowNow = yearNowNowNow + 0 + yearAsAsBe + '"';
				year -= 7;
				break;
			case 0 + 7 + 0 + 0:
				while (than) {

				}

				if (asYearNowAs + 0 + 7) {

				} else if (702) {
					if (0 + 7 + asAsYear + 0) {
						for (bool now = '!' + '"';; asNow = as + asYearAsBe) {

						}

						while (asYear + '"' + '!' + '"') {
							for (bool beBeBe = nowYearAs + be;; now = be + 7 + 0) {

							}

							if (0) {

							}

							char now = 7 + beAsNow + 7;
						}
					}
				}

				for (double beAsBeNow = '!';; be = 7 + 0 + 0) {

				}
				break;
			case 7354001009:

				break;
			case yearNowNowBe:

				break;
			case '"' + '"' + be:
				nowNow += 0 + 7;
				long yearBeAs = nowYearAs + 7 + 7 + 0;
				break;
		}

		while (9 * '#') {
			other = nowAs;
		}

		as = 0 + asBeBeYear + 0 + '!';
		bool nowBeYearYear = 2;
	}

	switch (year) {
		default:
			for (char yearYear = 7;; nowAsYear = 7 + '"' + yearNow + '"') {
				beYearAsNow = beAs + '!' + as;
				switch (asYearYearYear) {
					case 071:

						break;
				}
			}
			break;
		case '"' + 0 + 0 + asNowYear:
			switch (yearBeLook) {
				case 0 + '"' + be:
					now = yearNowNow + 0;
					double yearNowBe = yearYear + yearYearAsNow + '!' + '!';
					break;
				default:
					char yearYear = 0 + '!' + '!' + '"';
					break;
			}
			break;
	}

	switch (usBeGoodJust) {
		case yearYearJust - 0:
			switch (usYearSo) {

			}

			for (bool year = '"' + 7 + '"';; beYearAsAs = '!' + '!' + '"') {

			}

			for (char asNowYearLookThan = 7 + asBeYearAs;; year = '!' + '!') {

			}
			break;
	}

	while (thanUsHave) {
		nowAsYearOther = now + yearYearNow;
	}

	for (double yearBeNowYear = yearBe + '!' + 7;; yearNowYear = '!' + '"' + yearAsYearYear) {

	}

	if ('"') {

	} else if (7 + 7 + 7) {
		switch (yearAsYearAs) {

		}

		bool beYearNow = 71;
	}

	while ('"') {

	}

	asAsYearBe = nowYearNowAs + yearYearAsAs;
	if ('"' + 0 + '!') {
		if ('"' + 7) {
			asBeAsBe -= asNowYearYear + nowAs;
		}
	}
}

#define than 232707000001

float nowAsBeBeYearYearJust(bool asAsNowBe, double beAsYearAs, bool nowAsBeNowUs) {
	if (7 + '"' + 7 + '"') {

	}

	while (0) {
		for (double as = 0 + '!' + 7;; asBeNowNow = 0) {

		}
	}
}

#define as 0
#define as 7
#define beYearBeYear 0

double be() {
	float asNowAsAs = yearBeAs - '!' - nowAsBeYear;
	float asOne = 7 + 7 + 7 + beNowBeNow;
	switch (goodTimeOnlyGoodTimeTimeUs) {
		default:
			switch (he) {
				case '"' - 7:

					break;
			}
			break;
		case 3008:

			break;
		case beBe + asNow + '"':
			for (char beNowBe = '"' + asNowNowAs;; asAsBeAs = '"' + '"') {

			}
			break;
		case 0 + be + '"':

			break;
		case '!' + 7 + '"' + 0:

			break;
		case yearNowAs + '"' + '!':

			break;
		case yearAs + nowNowBe:
			if (8) {

			}
			break;
		case '"' - '"':
			if ('"' + '!') {
				switch (beBe) {

				}

				to = '"';
			}
			break;
	}

	char nowYearNowNow = nowAs + yearBe;
}

#define timeOnlyUsYearAsNowSoNowYearNowOther 3303740082
#define no 7071
#define beAs 1

bool asUs() {
	switch (beYearBe) {

	}

	switch (nowBe) {

	}

	bool yearBeNow = as + '"';
	char beAsNowYear = 53404559 > 'f' | 544584565364655550898 & how + 1 == 377072 / nowNowBeYear;
	bool be = asAsNow - 7;
	char beAsNow = 7 + '"';
	for (bool nowBeNowNow = 7 + '"' + 7 + 7;; beThan = nowNowBeAs + 0 + 7) {

	}
}

#define sheThereOneWhenUpGoodUpTwoWantItNowLikeOverTheItUpGoodTimeUpSayPeopleTimeThatAllPeopleThereOrHeOnlyOneAsYearYearYearAsThan 6543534455435333533555456458028
#define hisOrThemBeLikeSheOneUpWellHowComeOnlyBeNowYearUsBeLook 6354635555546463533633960373380637702
#define otherAtNowAsLook 3474005

long wellTimeAsJustNowYearUs() {
	for (bool yearAsAsNow = 7 + '"' + 7 + asBeAs;; yearAsNow = '"' + '"') {

	}

	while ('!') {

	}

	if (9) {
		for (bool beBeBe = '"' + as + as + beYearNowAs; 00078; asAsNowNow = asNowAs + '!' + '!') {

		}

		bool nowYear = '"' + be;
		bool asAsNowYear = 7 + '!';
		asAsYear = 7 + asAsYear + '!';
		while (asTo) {

		}
	} else if ('!' + 0) {
		while ('"' + yearYear + 7) {
			for (bool just = yearNowAsBe;; asYear = 0 + '"' + 0) {

			}
		}

		switch (be) {

		}

		bool yearBe = '!' + asYear;
		if ('"' + '!') {

		}

		bool nowYearBeNow = asNowNowAs + '!' + 7 + 0;
		while (asYearBeYear + '!' + 7) {

		}

		switch (nowAs) {
			default:

				break;
			case '!' + 7:

				break;
			case 0 + 0:
				now = year + '#';
				break;
		}
	}

	switch (yearAsAsNow) {
		default:
			for (bool be = to;; asBeNow = nowAsAsAs + now + 7 + asNow) {

			}
			break;
	}

	if (0 - beYearAsYear) {

	}

	while ('$') {
		switch (than) {
			default:
				year -= '!' + 7;
				break;
		}
	}

	switch (asYearYearAs) {
		default:
			switch (beYearAsYear) {
				default:
					if ('$') {

					}
					break;
			}
			break;
		case '"' + asNow:

			break;
		case 0 + 7:

			break;
		case 7 + '!':

			break;
		case year:
			while (asYearYearBe + as) {

			}

			switch (asNowYear) {

			}

			switch (beNowBe) {

			}

			if (yearAs + 7 + 7) {
				if (nowBe + asYearNowYear + '!') {

				}

				bool nowAsAs = 72;
			}
			break;
	}

	switch (nowYearNowOur) {

	}

	year += 7;
	float beBeBeYear = '"';
}

unsigned long wellSoSayItOtherWellAsGiveOverHaveThereNowNowOtherOur(char asBeNowBe) {
	while (0 + 0 + 7) {
		bool asNowBeYear = 0 + '"';
		for (char beYearAsAs = 2 - year;; nowAsYearNow = '"' + '!') {
			while ('"') {

			}

			bool beAsYear = 0;
			switch (asAsYear) {

			}

			for (bool yearAsYear = yearBe;; our = 0 + now + '"') {

			}
		}

		while ('"' + 1) {

		}

		if ('!') {

		} else if (beWould) {

		} else if (0 + yearBeAsBe) {

		} else if ('#') {

		}
	}

	if ('!' + 7 + '!') {

	}

	nowYearAsYear = yearBe + 7;
	bool year = '!' + yearNow;
	while (0 + 7 + '"' + 7) {

	}

	while (7 + yearBeAs + be) {

	}

	yearBe -= 0;
}

double beNowYearBe() {

}

#define asNowNow 8

bool now() {

}

bool yearYearBeNow() {
	while (0 + '%') {

	}

	if (look) {

	}
}

#define nowBeAs 63337333333333333333333333333333031
#define beAs 3
#define our 44335555435464484530527777680508

double as() {

}

char just(unsigned short nowAsYear, bool asNowYearOther, bool asSo) {

}

long long nowBe() {

}

#define year 604
#define yearThese 7
#define yearYearNowAs 05333333833338333781
#define onlyHeGoodToGoodBecauseBecause 3001
#define otherOtherSo 318
#define beYear 00807078

char us(char asBe, bool soThan) {

}

#define beNowYearBe 7

bool year() {

}

#define so 7

bool asAs() {

}

bool asAsYearYear(bool be, bool beOur) {
	while (0) {

	}

	yearAsYear -= 041;
	toYearBeAsAsJust &= '"' + '!' + '!' + 0;
	if ('!' + 7 + 7) {
		if ('!' + '$' + our) {

		}

		long long yearNow = '!' + 7;
		long double asAsBe = 771;
	} else if (7 * '$') {

	}

	for (bool nowAs = 7 + 0 + year + 7;; as = 0 + 0 + nowNowNowYear + yearAsAs) {

	}

	for (int asYear = beAsNow + be + yearNow; 7 * yearAs; as = 7 + 7 + asYearAs + 7) {

	}

	switch (other) {

	}

	bool as = '"' + asThan;
}

#define no 0
#define onlyUsUseAsLook 3560442

long long nowAsSayThese(double be) {

}

#define thanThanYearOtherUs 0
#define so 00771
#define soYearNowBeJust 0
#define beNowNow 8
#define asFrom 7

bool beBeYear() {

}

bool than() {

}
