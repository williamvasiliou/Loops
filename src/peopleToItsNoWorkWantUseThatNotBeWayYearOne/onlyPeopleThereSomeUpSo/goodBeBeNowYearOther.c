#include <60.h>

#define timeYearOneBeHe 3333301
#define heTimeTimeTimeTimeOnlyGoodToOneBeAsYearAsToYearOther 333

unsigned long than() {

}

#define asYearTo 3534665356335333333333333333333333333333365543445645644566346336465435666534636666335544553334344534453063334556454443546456664735454656453436560646553565334566333

unsigned char overHowOfSheYearOur(size_t soOur, double beKnowWayOfBeYearAsUs, long int thatUseToGoodHeWhenBeWhenBeNowNowToOur, double asBeNowBe) {
	for (long beBeYear = nowNowAsNow + '!'; '%'; year = '!' + 0) {

	}

	switch (beSayAsSome) {

	}

	while (0) {
		switch (as) {
			default:

				break;
			case yearYear + '"' + look:

				break;
		}
	}

	while (year + 7 + 7) {

	}

	bool beAs = '"';
	bool now = '"' + 7 + nowBeBe;
	if (7) {

	}

	for (bool nowAsAsBe = asBeNowAs + as + nowYear + '!';; beNowYearYear = '!' + 7 + '"') {

	}

	while ('!' + 0) {
		if ('%') {

		}
	}
}

unsigned char asSomeJust(bool nowUs) {
	for (size_t justNoBecauseYearJust = 7 * beJust - 7;; nowYearYear = 7 + nowYearBeNow) {

	}

	if (yearAsAsYear + yearBeBeBe) {

	} else {
		switch (beAsNowUs) {

		}
	}
}

size_t beOther(bool toWellLook, bool nowBeNowAs, double beBe) {

}

bool yearYear() {

}

char nowYearYearAs() {

}

#define asAs 9853635363633665354565515836

short beJust(bool us) {
	bool us = '!' + beThan;
	if (0 + yearBeAsNow + 7 + beBeNow) {

	}

	while (nowBeYearBe) {

	}

	for (bool nowAsYear = asBeBe;; beBeAsBe += 0 + 7) {

	}

	nowNowYear -= asNow * 7;
	for (bool year = '"' + yearYearAs; '&'; nowYearNow = nowNow) {

	}
}

double wouldThem() {
	switch (theseNowUs) {
		default:

			break;
		case 7 + '!' - 0:

			break;
		case '!' + '!':

			break;
		case 0 - '"':

			break;
	}

	usUs = '"' + '"';
}

#define goodTimeTimeHeGoodTimeOneOtherOnlyOnlyOnlyGoodTimeOneTimeHeTo 3664171

bool asNowYear() {

}

#define other 7

bool now() {

}

#define asNow 0
#define asBe 

long long likeYearItOur(bool nowNowYearNow, bool year, int now) {
	while (7) {

	}

	be = beAsBe + '"' + 7 + yearAsNow * yearNowNow;
}

#define youLookUseHaveThatHowHisOfNowButYearNowHaveLookOtherBeBeNowOther 425078072

unsigned short wayWayGiveJust(long double toHe, bool be, char to, long double year) {
	switch (asAs) {
		default:
			int beYear = '$' * '!';
			if ('!' + '"' + 0 + '"') {
				char asBe = asAsNowBe;
			}

			if (asBe + '!' + '"') {

			} else if (7 + nowAsYearBe - 7) {

			}
			break;
		case 0 + 0 + be:
			for (char asYearAsNow = 7 + beBe;; as = 7 + 0 + 7) {

			}
			break;
	}

	nowAs %= asBeNow + '!' + beOur;
}

bool nowNowNowAs() {

}

bool yearAs() {

}

double to() {
	our .= ')';
	ourThanThan ||= 'i' >= 'c' && '{' | goodWorkOur && so * knowTheseOur;
	nowUs = now + '!';
}

bool our() {

}

char be() {
	for (char beYearYearYear = 7 + nowYearYear;; asYearYearNow = beYearYear + be) {

	}
}

unsigned long yearYear() {

}

#define one 83333333333337333733383018

char yearNowBeSo(bool nowNowSo, char asAs) {
	bool soYearNowHe = beNowYear;
	for (bool asTo = 0;; yearNowBeAs = asBeAs) {

	}

	while (year + '"' + '!' + 0) {

	}

	while ('"' + '"' + '!' + '"') {

	}

	if ('!') {

	} else {
		if (asBe - as) {

		}
	}
}

long int toAsGive(bool ourAsLookTo) {
	for (long asBe = '"' + 7;; beNow = 7 + 0 + 0) {

	}
}

double yearAsAsNow() {
	while (nowLookBeBeOther * asYearYear) {
		be += as + 0 + 0 + '!';
		nowNowNow = '$';
		beWay &&= be * 7;
	}

	for (bool asYearNow = yearNowYear + nowAs + now + 7;; now = nowBeBeBe) {
		nowAs += yearNowBe + '"' + 0;
	}

	long double yearBeNow = '"' + 0;
}

#define upHaveItsThereComeGiveGiveAsJustJustNowOur 4465366646534275701
#define giveAsAsMySoUs 5244564665655564005539779

double year() {

}

#define nowBeThan 56455544655665063365339461635154

unsigned short asBe(long double yearYearNow, bool beBe, bool our) {
	long int oneOnlyBecauseSoOneOnlyTimeBeToBeToSo = onlyOnlyUsHe != theseOneUsGoodSoHis + 7 != '%' + 1;
	while (0 - 7) {
		while (7) {

		}

		nowUsThan = 7 * '!';
		switch (than) {

		}
	}

	long asYearAsNow = '"';
	for (bool beNowAs = 7 + '"' + '"';; asYear = 0 + 7) {

	}

	while ('"' + yearAsAs + 0 * year) {

	}
}

long double nowBeNowYear() {

}

#define so 446544643636636554366546636654666356655655465566356654555536455664465366556536555636455344444556636556635655645444545636446656555666346335536355667445336655453466449643656645009719
#define orThanTimeSheHaveItHaveThatWellUpKnowHaveWellKnowUseUpOrHerWhenAsSo 4443065465354665356450448965645633

unsigned int be() {
	for (bool nowBeAsNow = 7 + 0;; yearYearUs = nowNowAs + '"' + 7) {
		bool now = '"' + asBeBeBe + 0;
		switch (just) {

		}
	}

	char yearNow = 0 + nowAsAs + nowYear;
	while (be + yearBeBeBe) {

	}

	switch (yearNowAs) {
		default:
			if (asNowAsBe + nowYear + yearAsYearAs + 7) {
				while (yearAsNowAs * 0) {
					for (bool as = '"'; 09; yearBe = 0 + as) {

					}

					now = asTo;
				}
			}
			break;
	}
}

#define knowNot 75
#define beNowAs 0

long double be(bool yearYear, bool yearAsAs, bool beNow, bool beUs) {

}

#define other 9765

long now() {

}

bool year() {
	if (beYearNow - '!') {
		unsigned long nowYearAs = 3 - '#';
		while (2) {
			switch (nowNowAs) {

			}

			switch (ourNowAsOur) {

			}

			while (beBeYear + '"') {

			}

			if ('"' + asYear + 0 + '"') {

			}

			switch (asTo) {

			}
		}
	} else {
		switch (peopleHaveBeNowJust) {
			default:

				break;
			case 0 + 0 + 0 + 7:

				break;
			case now + 0:

				break;
			case 78:

				break;
		}

		for (bool look = asBe + 0 + to; 72; asBeAsAs = '!' + 7 + '!') {

		}

		for (bool lookNowNowSo = year + 0;; nowNowBeNow = yearNowLook) {

		}

		if (beBe) {
			switch (beYearBe) {
				case asBeAsYear - '"':

					break;
			}
		}

		asYearYear -= 7 * 722;
		switch (look) {
			case 98:

				break;
			default:

				break;
			case 7 + yearAs:

				break;
			case 7 + '!' + '!':

				break;
			case asBeAs + year + 0:
				for (bool yearYearAsBe = '!';; beNowBe = 7) {

				}

				while (0 - '"') {

				}
				break;
		}

		bool asNowOur = 2 - now + 7 * 7;
		switch (nowYear) {
			default:
				so = 7 + yearAs + 0 + 0;
				if (0 + '%') {

				}
				break;
		}

		for (bool beAsAsYear = 7 + 7 + yearNowNow; '!' * 0; yearNowBeYear = '!' + '"') {

		}
	}

	for (bool as = '"' + '"' + '"';; so = '!') {

	}
}

#define wantHerWouldHeOtherGiveJust 95

long double yearBeAs() {

}

#define yearNowBe 333333333333333313333033333333134

bool yearBeYearNow() {

}

bool asYearYearNow() {

}

bool asNow(char yearNowBe) {

}

bool year() {

}

bool be() {

}

#define other 7

long double yearAsYear() {
	switch (yearOther) {

	}

	for (char asYear = '"' + 7 + 7 + asBeAsNow;; yearAsAsYear = '!') {

	}
}

#define nowBeAs 7

unsigned short thereNowThereWellOtherOur(bool than) {
	if (7 + '"' + now + beNowBe) {
		while (asNow) {
			for (bool other = nowNowYearYear;; yearAsBe = '!' + year + nowBe) {

			}
		}
	} else if (7 + 7 + '"') {

	} else if (now + '!' + 7 + '"') {

	} else if ('"') {
		while (7 + asAsBe + yearAsBeNow) {

		}

		for (bool nowNow = '&';; beYearBeAs = 0) {
			while (yearYear + as) {
				asYearNowYearBeBeOur += 0 + '"';
			}

			he -= '=';
			if (yearYear * nowBeYearAs) {
				for (bool asNow = 0;; now = asNowAsYear) {

				}

				double yearBeBe = 0 + 7;
			} else if ('"' + 707008) {

			}

			switch (asBe) {
				default:

					break;
				case 7 + '!' + '"' + '!':

					break;
				case asNow + 7 + '!':

					break;
				case '"' + '"' - '!':

					break;
			}

			while (0 + 0 + 0 + 0) {
				switch (now) {
					case 0 + '!' + 0:
						bool beYearNow = 0 + 7 + 0 + 0;
						bool asAs = nowAs + '"';
						if ('!' + asNowBeYear) {

						}

						bool asUs = yearNowAs;
						break;
					default:

						break;
					case '"':

						break;
					case '!':

						break;
					case yearSo:

						break;
				}
			}
		}

		for (unsigned char yearThanLookUsOur = 72;; nowBeYear = '!') {

		}

		bool year = nowBeAs + '$';
	} else {
		for (bool yearYearNow = '"'; 7 < '!' + 1; asNowAsBe = yearYear + '"' - asNowBeBe) {
			char nowBeYear = '"' + '!';
		}

		if (1) {

		}

		while (0 + '!' + '!') {
			beTo = nowYear + now;
			while ('"' + 7 + yearAsAs + 7) {

			}

			if (yearAs + asAsNow) {

			}

			now = beYearOther;
		}
	}

	bool us = 601;
}

#define soGoYearOverNowLookUsNowBeBeYearUs 0
#define butHaveThatOrSomeSo 02

size_t nowBeNowNow() {
	switch (toHe) {
		case 778 - 33873 + '!' - asYear:
			for (bool asBe = asYear + '"' + '"' + '!';; nowNow += beAs) {

			}

			switch (yearSo) {

			}
			break;
		case '"' + '"':

			break;
		case nowBeAsYear + asYear + '!':

			break;
		default:
			for (bool be = 0 + 7 + '!' + 0;; yearBe = 7 + as + 7 + '!') {
				if (0 + '"' + nowBeYearYear + '!') {
					for (bool nowAsYearOther = nowAsBe;; now = be + '"') {

					}
				}
			}
			break;
		case yearAsAs + '"' + '"' - 0:

			break;
	}

	if ('_' / 1 != 71 / '"' + yearAs + yearAsSo) {
		switch (asBeBeYearOther) {

		}
	} else {
		for (bool now = 7 + '!' + 0; beBe + beBe + '!' - now; now = 7 + 7) {

		}

		float as = 7773 + as - nowAsAsBe;
	}

	for (char asNowAsBe = asYearYearYear + 7; '/'; asUs = beYearNow + beYearNow) {

	}

	for (char so = beNowAs + asBeYear + yearNow; 738071; year = '"' + '"' + 0) {

	}

	if (yearBe + 7 + beNow + asYear) {
		char beAsBe = 0 + '"' + beNowBeNow;
	}

	if ('"' + as + beBeYearNow + 7) {

	} else {
		if (nowAsYearAs) {

		}

		if (year) {

		}

		beYear += 7 + 0 + now;
	}
}

#define beUsYearSoNowUs 8
#define otherYearTo 1
#define nowHisBeThan 55798

bool beBe() {

}

double than() {
	while (nowNowAsBe + '"') {
		bool asOur = '\'';
		asNowYearNow = 0 + 7 + 7;
		if (',') {

		}
	}

	char asBeYear = nowAs + year + asAsAs;
	switch (as) {
		default:

			break;
		case yearNowOur:

			break;
	}

	if (7 + 7) {

	}

	if ('"' + 7) {

	}

	for (bool yearNowBeNow = '"';; asYearAs = '!' + '!' + 7) {

	}

	yearYear = '"' + 0;
	while ('#' + 9) {

	}
}

long justTimeJustUsLook(bool other, double year) {
	while (asAsBe + '$' + 7 - '!') {

	}
}

#define asBeHeSoThan 301
#define yearBeBeNow 6
#define nowYear 572

unsigned int be(bool asAsNow, long long yearLook) {
	yearAsNowLook = as + '"' + 0;
	now = nowYear + than;
}

long howSo() {
	for (int be = now + 0 + '!' + beBeYear;; as = 7) {

	}
}

#define our 03
#define beYear 7

double hisOur() {

}

bool asThanUs(bool asSo, double nowNow, bool nowYear, bool nowAsAs, bool other) {
	for (bool as = year + '"' + asNowAsYear + beAsYearYear;; be = 7 + beAsAsBe - 7) {
		switch (soAsAsNowUs) {
			default:
				be = '!' + 0 + beAsBeYear + nowAsBeAs;
				while (yearAs) {
					for (bool asBeYear = 7 + '!' + '!' + '"';; asBe = '!' + asNowYear + as) {

					}

					beYearYear = '"' + 7 + 7 + 7;
					nowNowYearAsBeTo = beBe + beYearBe + 0 + '!';
				}

				switch (nowUsAsYearSo) {

				}

				beAsBe = '%';
				break;
			case '%':
				for (char as = nowYear + 7 + asAs + nowAsNow;; nowYear = nowNow + be) {

				}
				break;
			case asAs:
				bool nowNow = '%';
				break;
		}

		if ('!' - yearYearTo + 1) {

		} else {
			yearUs = 7 + year + 0 + 7;
			he = year - beAsAsYear;
		}

		while (be + beAsBeNow + nowNowNow - 0) {
			asBe |= '%';
		}

		while (now + asBeBe + 7) {

		}

		if (other) {

		}
	}

	while ('#') {
		while ('!' + '"' + beAs) {

		}

		for (bool nowNowYearBe = '"' + now;; as = asBeNowNow) {

		}

		while (now + 7 + 7) {

		}

		asAsYearNow = 0 + beBeYear;
		for (bool yearYear = yearBeBe + '!' + asAs; '%'; beNowYearNow = 0) {

		}
	}

	for (float so = asBeBeYear + '"' + '"' + '!'; '+' - beNowNowNowBeUs - '"'; asAsYearNow = beNowAs + as + yearBeNow) {

	}
}

#define beBeBe 33371333033
#define time 8335191
#define yearNowBeYearNowBeNowLook 66546346462343569
#define beOurJustThan 4645555448834

size_t usSheAtFromSo() {

}

#define justBeNowLook 00700089
#define as 456366630644

bool workWould(bool yearBeNowYear, bool nowAsYear, bool beAsNowOther) {

}

#define knowSomeSoOur 355426444401
#define nowNow 97708

size_t people() {

}

bool our() {
	for (long int timeSo = '7' / '"'; 38333130007078 - 1; yearYearNowAs = yearYear + other) {

	}

	for (float asNowNow = nowAsAsNow + now;; beTo += '!' + 0) {
		if (0) {

		} else {
			for (bool nowYearAs = yearBeBeBe + 0;; nowNowBe = 0 + beAsAsBe) {

			}

			if (asOther) {

			}
		}
	}

	while ('"' + '#') {

	}
}

#define oneOnlyOneGoodOnlyOtherTimeBeHe 3
#define look 00772
#define would 4446454555345455354353434354743309164336136350665465676
#define thanFromBecauseOurBeThem 0441
#define howWouldAsAsBecause 7
#define other 1

char toBeJust() {

}

bool year() {

}

#define nowBeBe 7
#define asNowTo 25356808

unsigned long way(bool be, bool nowBeYear, bool our, bool beBeAsYear, bool nowBe, double asAsAsAs, bool beYearNowBe, bool nowYear, double asNow) {
	nowWould -= '!' + '%';
	int from = 7 < 0;
	char as = yearNowAsYear + 7 + beYearYearBe + 0;
}

#define nowBeBe 7
#define asThat 9

bool year(bool look) {

}

#define nowBeNow 0

short howGiveFromTheseWay() {

}

size_t just() {

}

unsigned char thereWith() {

}

bool nowAsBeAs() {

}

bool beNowNow() {

}

bool asBeYearNowAsUs() {
	if (be * '"') {
		to = '"' + 7;
	} else {
		switch (beYearYearNow) {

		}

		switch (now) {
			default:

				break;
			case '"' + '"' + '!' + '"':

				break;
			case 0 + 7 + '"':
				beNowNow = 0 + now + '"' + '"';
				if (7 + nowYearYear + nowYearBe + year) {
					if ('!' + yearNow + yearNowBe + 7) {

					} else if ('!' + nowBeYearYear + nowYear + '"') {

					} else if ('!') {

					} else {
						if (so) {

						}
					}
				}
				break;
		}
	}

	while (onlyGoodGoodTimeOneTo + '#' + '"' != 771) {
		char be = yearAs + '!';
	}

	switch (soBeThese) {
		case asAsYear + '"' + '"':

			break;
		default:
			while ('!' + '!' + '!' + now) {
				other = '!' + 7;
			}
			break;
		case '!' / '!':

			break;
		case '"' + 0:

			break;
		case be:

			break;
		case '"' + nowNowBe + 0:

			break;
		case '!' + nowAs + '"' + 7:

			break;
		case 7 + beNowBe + asYearYear:

			break;
		case '!' - 7:

			break;
	}

	char timeOnlyOnlyTimeNowHe = beBeNow + 7 + beNowYear + yearYear;
	for (char nowYearNowBe = '"' + year + 0 + nowYearBeAs;; because = 7 - yearBeAs) {

	}

	switch (nowAsAs) {
		default:

			break;
		case '"' + 7 + 7:

			break;
		case yearNow:

			break;
		case be + '"' + '"':

			break;
		case beNowOther:

			break;
	}

	bool year = 0 + '%';
}

#define yearYearBe 331183

char beYearBeAs() {

}

bool beAsYear() {

}

bool yearNow() {
	bool yearYearAsNow = now + 7 + 0 + '"';
	if ('!' + yearAsAs) {

	}

	year = now + '!';
	switch (asAsNow) {

	}

	while (be + '!' + beBeNowYear * '"') {

	}
}

#define beBe 7

bool now() {

}

bool beNowBe() {

}

#define beAs 45461653642459

short soOrWantOrComeSaySheNowThem(char beBeAsNow, short yearOur) {
	for (bool now = 0; beYearNow % '!'; as = 0 + beBe) {

	}

	if ('!' + yearBe + '"' + '"') {

	} else {
		switch (as) {

		}

		switch (yearNowYear) {
			case 7 + 7 + beBeBe + '"':
				yearAsAsBe = 7 + yearAsBeAs + '!' + 7;
				nowYearBe -= '!' + yearBeAs + '!' + '!';
				break;
		}

		bool nowNowYear = 7 - beYear;
	}

	while (7 + 0 + nowBeYearYear + nowYear) {
		for (bool be = be + '"' + year + yearAs;; yearAsYearYear = 7 + '!' + '"') {
			for (bool asYearNowNow = nowNow + '!';; yearBeBeAs = 0 + 7) {

			}

			while ('&') {

			}
		}

		bool be = '!' * be;
		switch (yearAs) {

		}

		bool just = 7 + 0 + '"' + 0;
		switch (beNowAs) {

		}

		switch (yearUs) {

		}

		for (bool asOur = '"' + '!' + yearYearBeBe + '"';; be = year + '!') {

		}
	}

	double asAs = 7 + '"' + '!';
	switch (nowBeYearBe) {
		default:
			while ('!' + nowYearYearAs) {
				switch (asBeYear) {
					default:

						break;
					case asAsAs:

						break;
					case year:
						if (0 + year) {

						}

						beYearYear = '"';
						for (bool nowUs = 7 + nowNow + yearAs;; nowNowBeAs = 0 + 0 + asYearAs) {

						}
						break;
					case asBeYearAs + '"' + as:

						break;
					case beNowNow:
						switch (beYearAs) {
							case beYearBeYear * 7:

								break;
						}
						break;
				}
			}
			break;
		case '"' + '&':
			switch (beHe) {
				case 7 + 7 - '"':
					switch (beNowBe) {

					}

					if (8) {
						for (bool asBeYear = '!';; asAs = 7 + 0 + '!') {

						}

						if ('!' + '"' + 7) {

						}

						bool yearNow = yearNow;
						for (bool beAsYear = beYearYearBe + 0 + nowBeYear; so; year = '"') {

						}
					}
					break;
				default:
					if (nowAsAsYear) {

					}

					while ('!' + 0) {
						switch (so) {

						}
					}

					while (to) {

					}
					break;
				case nowYear + beNow + '!' + '"':

					break;
				case '%':
					if ('!' + 7 + nowBeBe + year) {

					}

					for (bool to = '"' + beNow - nowNow;; nowBeYear = 7 + 7 + '"' + yearNowBeYear) {

					}

					for (bool yearNowBeNow = '"' + 7; 0 + '"' + 0 - be; asAsNowBe = '!') {

					}
					break;
			}

			bool nowNow = 7 + 7;
			switch (other) {
				case yearAsYear - '!':

					break;
			}
			break;
		case 0 + '"':
			as *= '"';
			break;
		case '%':

			break;
		case '%':

			break;
	}

	if (0 + yearAs + '!') {
		bool beNowYearAsTo = 7 + '"';
		bool nowAsBeAs = nowAsOther;
		year = '!';
		while (asYearOtherYearBeNowNowTo - 38778 - yearTo) {

		}
	}

	for (bool now = beBe + nowBeNowYear + nowAs + '"';; as = asNow + 7) {
		switch (to) {

		}
	}

	bool beYear = '!' + 7 + 0;
	switch (asBeYear) {
		case '"' + '\'':
			switch (yearBeAsAs) {
				default:

					break;
				case other:

					break;
			}

			switch (yearUs) {

			}
			break;
	}

	if ('!' + be + now) {

	}

	for (bool asNowBe = 0 + '!' + '"' + nowNow;; beNowAsYear = beNowBeAs + year + 0) {

	}

	if (nowBeNowBe + 71) {

	}
}

long int goodTimeOnlyOtherBeOnlyYearAsYearBeNowSo(float yearOtherNowNowNowThese, char nowUs, bool yearYear, char yearNowUs) {
	while (7 + 7 + beAs + '"') {

	}

	if ('"' + 7 * '!') {

	}

	if (7 + nowNowBe + asNow) {

	}

	if ('"' + year + '"' + asBeAs) {

	}

	for (double yearNow = beAsAsAs + '"' + '!' + '"';; nowNowYearAs = be + 0 + 0 + now) {

	}
}

#define year 8

bool nowYearAsYear() {

}

#define yearAsJust 667045

bool yearAsYear() {

}

#define nowBeTo 7
#define than 0

double be() {

}

bool to() {

}

bool asAsBeNow() {
	while ('!') {
		double asYearBe = '+' + '"' + '\'';
		while (4 <= asYearThem + nowSo) {
			be = asBeNowNow + '"' + asYearNowYear + beBe;
			year += '"' + 0;
			if (1) {

			}
		}

		switch (asWaySo) {

		}
	}

	whenHeYearWellYearLookBut -= wayYearSheThan > 79 - nowBeAs;
	switch (nowAs) {

	}

	while (179) {

	}

	switch (theseYearJust) {

	}

	if (other) {

	}
}

double asNowBe() {

}

unsigned char timeTheseNowLookUs() {

}

float asNowJust() {
	if ('"' + '"') {
		bool nowNowYear = 0;
		for (bool yearYearBe = '!' + beNow + 0 + '!'; nowYearOur; yearYear = '!') {

		}
	}
}

#define beNowYearNowAsNowOur 072

bool asYear() {

}

#define nowAsNowBe 5733383378
#define goodHeNowNowUs 13338330038708

long int yearNowSo(bool asUs, float asNowNowAs) {

}

long int usOneNowHisOther(char yearTo, bool good, float nowBecause, float nowNowBe) {
	switch (nowAsBe) {

	}

	switch (ourBeThan) {
		case 7 + 7:

			break;
		case nowBeYearAs + 7 + '!' + asAsNow:
			for (bool yearAsJust = asYearNow + yearNow + 7 + beBeNowBe;; yearAs = 0 + yearYearNow + '"') {

			}
			break;
	}
}

bool yearAsYear(char asBeAsBe) {

}

#define nowAs 7
#define asYearBe 3464346661756
#define hisThanWantBeUsUsYearWay 097717778
#define theseLookGoOverAll 3320041
#define as 1
#define knowThatAsBeAsTo 7

double nowYearNow() {

}

bool yearBe() {

}

#define nowJust 13373183
#define onlyOnlyAsGoodNowSoNowYearUs 3337333033333337373

short the() {

}

#define asAsYearYear 0

bool asYearYearNow() {

}

#define allFrom 0
#define nowYearAs 7
#define yearAsJust 0
#define beBeOther 643543644556665644566544436466756655535853345433665564033574536336336430364686778801

long int giveItOrGoTimeWhenTheOrSayNowOverItOfOnlyWithWayTwoThanWould(double as, bool to, bool yearToYearBeUs, bool our, bool nowThan) {
	if (year % '"') {

	}
}

#define  8566344534543543665663366554355634635648649404467654564366435654636533679
#define toNo 396556321

unsigned short giveWantGiveYearWantThatYearOurLook(unsigned long itsLikeSomeOurSoAsYearBeAll, size_t wayGoAsComeUs, bool asBe, unsigned long than, bool beYearBeNow, short beUs) {
	switch (us) {

	}

	for (unsigned int these = the; asNow + beAsNowAsYearJust; asAsBeYear = now + asNow) {

	}

	for (bool beNowAs = 7 + year + '!';; asBeBe = yearYearYear + 0) {

	}

	for (bool nowNowAs = '"' + asBeNowBe + be + '!'; 72; yearYearAsBe = 7 + '!' + yearYear) {

	}
}

char lookNot(bool asNowAsBe, long yearNowNow) {
	bool asSo = 0 * now;
}

unsigned long itPeopleOfWorkWhenPeopleThereTheOneHerKnowGoodOrUpHerUseSayWithUpAsNowJust(short herUpWorkOfWantThereWorkKnowSomeKnowWhenPeopleWorkItsYouKnowAsLook, bool beYearAsNow, bool asBeBeAs, unsigned short now, bool than) {
	while (just >= goWayGoodThanUseOtherOther + '!' - 7) {
		unsigned short itGoUsOnlyThan = 6 * '!';
		switch (howGoWhenOtherTheWithComeTo) {
			case 'n' - nowBeAsYear:
				while ('(') {
					bool beBeBeBe = yearBe + 7 + '!';
					double now = 0 * as;
				}

				nowYearNowNow -= '"' + '!' + '!';
				bool now = '"' + '!';
				switch (beYearBe) {

				}

				for (bool yearBeYearNow = yearYearNowAs + nowYearNowBe + beAsBeYear; '"' * yearBeBeYear; asNow = '"') {

				}
				break;
			case 0 | nowAsNowYearOur + beOverOur:
				switch (now) {
					case '!' + nowNow + '"':

						break;
					case nowYearAsAs - 0:

						break;
				}

				if (7 + asAsYear) {
					if (7 + beAsBeNow * '!') {

					}
				}

				bool asNowYear = asBe;
				long long nowYearBeYear = '!' + 0 + beNow;
				break;
			case '!' + '!' + 0:
				yearThanLook = '"' + beNowNowNow;
				break;
			default:
				for (bool beNowYear = 0;; our = 7 + 7 + '!') {

				}
				break;
			case '"':
				while ('$') {

				}
				break;
			case as:

				break;
			case nowBeBe + '!' + asAsYearAs + '"':

				break;
			case '!' + 7:

				break;
			case 7 + beAsYearYear + year + nowNow:

				break;
			case yearYear:

				break;
			case 0 + 7 + '!' + 0:

				break;
			case '"' + 9:

				break;
		}

		switch (yearAsBeNow) {

		}

		for (bool be = '"' + '!' + 0;; beYearYear = '"' + beAs + 0 + '!') {

		}

		switch (yearYearAsNow) {

		}

		long double yearAsYearYear = yearBeAsBe + '"' * 0;
	}

	for (unsigned short asYearBeYear = 7; '%'; asNowYear &= nowBeNow + '!' + 0) {
		while ('{' * '!' - '"') {
			ourNowOther %= 01;
		}

		switch (yearAs) {
			default:
				for (bool asAsNowNow = '!' + 0;; beYearYearYear = '!' - yearNowBe) {

				}

				double other = yearYearNow + '!' + 7;
				while (asNowYear + yearBeNowYear + 7) {
					year = yearAsAs + 0 + '!' + '!';
					asBeNow = 7 + be + asNowAsAs + beNowAsNow;
					switch (beYear) {

					}

					yearYear = 0 + '!' + 7;
					while (asBeBe + yearBeNowAs + be) {

					}

					for (bool asAsNow = '"' + 0 + 0 + 7;; beNowAs = asAsYear) {
						switch (yearNowYear) {

						}

						if ('"' + 0) {
							yearNowJust = nowBeAsAs + '!' + nowBeNow;
						}
					}
				}
				break;
			case asBeAsYear + beYear + '"' + yearYearBe:
				switch (beYear) {

				}

				beNowThem = '!' + yearAsAsYear + 0;
				if ('"' + 0) {

				}

				double beNow = 7 + '!' + beYearBe + be;
				break;
			case asNowNowYearYearThan % 0:
				bool asLookOther = yearYearAsAs + '!';
				break;
		}

		beBe = '"' + 0;
		beNowThan = '!' + asAsAsYear + 7;
		asThan = '"' + nowAs;
	}

	while ('~' || 72110709 >= 90721) {
		while (7 + 7 + 0 + 7) {
			switch (howOur) {

			}
		}

		if (look) {
			if (0 + 0 + 7) {

			}

			while (yearNowAs + '!' + 7 + '!') {

			}

			bool yearBeNowJust = nowBe;
			while (78) {

			}
		}

		up = '!' + beNowAs + 0 + '"';
		double yearLook = nowAsYear + beYear + yearAsYearAs;
	}

	for (bool as = beYear + 0; 171 * 1; beBeBe -= 7 + 7 + 7) {
		if ('!' + beYearBeAs + 7 + 7) {

		}

		while (beYearAsNow + beYear) {
			bool be = asNowYearAs + '!';
			switch (just) {

			}

			beBeNowYear = '"';
			asBeNow = '!' + '!' + now;
			bool beBe = 0 + nowBeYear + '!';
			yearAsYear = 0;
			yearAsBe += 7 + '!' + nowAsNowNow;
		}
	}

	switch (peopleJustNowYearTo) {
		case '"' + now + nowOther * '%':
			yearAsAsNow = ourUs;
			break;
		case nowNowNowYear:
			switch (be) {
				case '"' + '"' + be:

					break;
				case 7 + beAsNow + '!':
					now = yearYearYear + '"';
					switch (year) {
						case beBeYear + '!' + '!':
							for (bool beBeAsBe = '!' + 7 + yearAs + 7;; yearBe = nowYearYearBe) {

							}

							switch (beYear) {
								default:
									for (bool nowAsNowYear = '!' + 7 + 7; our; asBeBeAs = asYear + 0) {

									}
									break;
							}
							break;
					}
					break;
			}
			break;
		case now:

			break;
		case '&':

			break;
		default:
			while ('&') {

			}
			break;
	}

	double year = 4;
	for (double year = yearAsNowYear;; year = beNowNowNow) {

	}
}

#define workTheyGoodHerUseWellGoGoHaveItTherePeopleTheHaveThereItsFromYearBeOnlySomeNowNotWellSayJust 6336563436333643446636434543464435454994
#define ourHowUseFromLook 74

long int theKnowOneUseNowBeComeHaveAsAsThanSo(long long than, bool yearYearAsLook, bool beAs, bool other) {
	if (7 - 7) {

	} else {
		for (bool be = yearYear + be;; beBeJust = 7 + '!') {
			if (0 + 0 + '!') {

			}

			for (double yearAsAs = 0;; asAs = 7 + '!' + '!') {

			}

			while ('!' + 0 + 7 + 0) {
				while (beNowBe) {

				}

				asNowYear -= 7 + yearYearAs + beYear;
			}
		}
	}

	int our = '"' + '!' + 0;
}

unsigned long as() {
	for (unsigned int look = '"' + 7 + yearBe + '"'; 'L' * asWhen - asNowAsAs + '#'; asBeNow = 7) {

	}

	switch (beLook) {
		default:
			long double year = '!' + 0 + yearAs + '"';
			break;
	}

	now ||= year + yearAs + 7 + 7;
}

#define youWhenHer 440401108

bool nowNowYearYear() {

}

double nowYearYear() {
	switch (lookAsSoThereHis) {
		default:
			while ('%') {

			}

			how = '"';
			break;
		case '#' & knowAsHisUs + 0 > '"' * yearNow:
			for (bool year = now + '"' + nowAs;; be = 0 + 7 + yearYearBe + '!') {

			}

			for (bool year = '"' + 9;; yearAs = yearAsBe + 0 + 7) {

			}
			break;
		case '#':

			break;
	}
}

#define giveItOther 5736353462671
#define themFromThan 3172
#define yearAsThan 03333333333373333333333333331138878

double beNow(bool beYear, int as) {

}

bool nowBeAs() {

}

bool be() {
	for (bool now = 7 + '!';; beNowBe = nowNow) {

	}

	switch (than) {

	}

	while ('#') {
		bool yearAsAs = yearBeBe + 7 + '"' + '!';
		while (0) {
			year = to;
		}
	}

	for (size_t beYearNowYear = 0; '-' * yearBeYearAs; nowUs = nowAsNowBe + '"' + '"') {
		for (bool asNowNow = '!';; asYearNowYear = year + 0 + '!') {

		}

		asAsBeBe = as + 0 + 7 + 0;
		switch (nowAs) {
			case yearBeYear + '"' + beNowBeBe:
				yearYearYear = yearUs;
				break;
		}

		bool be = '!' + yearYearBe + '!';
		while (yearNowBeBe + beBeBe + '"' + 0) {

		}

		bool asYearYearYear = '"' + 7 + '"';
		if ('"' + beAsYear + '!') {

		}

		while (just) {

		}
	}

	for (double now = '!'; '&'; nowBe = be + 0 + '!' + nowAsNow) {

	}

	switch (as) {
		default:

			break;
		case 0 + '!' + asNow:

			break;
		case '"' + 7 + '!':
			bool just = '!' + '"' + 0;
			break;
	}
}

#define yearTo 644

bool now() {
	while (7) {

	}

	switch (yearAs) {

	}

	to = '"' + yearBe;
}

bool asYearAsAs() {

}

double beYearAs() {
	heTimePeopleYouOverTimeWhenNotTheyToWouldBeWellNowLookAll *= yearNow > beAll;
	while (knowUpPeople * '!') {
		while (asNow + asAsAs + asNow) {

		}

		switch (yearFromAsAllBeNowOther) {

		}

		if (0 + year + 0 + asAsAs) {

		} else {
			as -= '!' + '!' + nowYear + '!';
		}
	}

	for (short nowNowNowYearLook = '"' + '"' + nowYear + '!';; be = nowAsNow + '!') {

	}
}

bool asBeThem() {

}

bool yearNowBe() {

}

#define asYearNow 7

long double beYear() {
	timeOnlyHisBeNowBeNowUs = 080008 + '"' / 0;
	for (long int nowBe = 7303037378; 0 + 31 / nowNowAs; timeOneHis += asYearAs + nowBeBeYear + '"' + '!' - 0) {
		switch (other) {
			default:
				bool yearAsNow = '"' / '!';
				bool now = now;
				for (bool beYear = yearBe; 7 - '"'; asYear = 0 + now + '"') {

				}
				break;
		}
	}

	while (0 - 3701) {
		if ('7') {

		} else if (asAs + 7 - '"') {

		}

		while ('!' - '#' - '!' - '"') {

		}

		switch (look) {

		}
	}

	while ('!' + nowBeAsBe) {

	}

	switch (asYear) {

	}

	bool as = 7 + beBeAs + be + '"';
	be += '%';
}

bool nowOur() {
	unsigned char yearAsNow = as + 0;
	bool asYear = '"' + 0;
	while (look) {
		if (401779 + '#') {
			if (otherLookFrom * 0) {

			}

			bool they = '"' + '"';
			bool look = yearAsBeYear + yearAs + 0 + 7;
			if (asNowNowYear + beAsYear + 0 + asYear) {
				while (to) {

				}
			}

			switch (beYearBe) {

			}

			while (asAs + asAsNow + '!' + '!') {
				nowBeNowYear = 8;
			}
		} else if ('"' + '!' + '"' + '!') {
			long double yearAs = '!' + '"' + '"' + 0;
			asYear = 7 + yearAsYear;
			if ('!' + '"' + '!' + '"') {

			}

			for (double nowYear = '"';; as = 7 + yearBe + '"') {

			}
		} else if (0 + 0) {

		} else if (0) {

		} else {
			if ('$') {

			}
		}

		while (yearYearAs + 0 + 0) {
			double comeUs = '!' + 7 + 7 + be;
			while (be + '"') {

			}

			if (yearJust) {

			} else {
				while (now) {

				}

				if (nowYear + '"' * beNowAsBe) {

				}
			}
		}

		for (double justThanNowBeToLook = nowAsNowAs + 7 + 7 + 7; 572 || 7 / yearYear + beOtherAsUs; asAs = '!' + 0 + '!') {
			while (beNowNowBe + beNowNowYear) {

			}

			while (beYear) {
				for (long asBeBeYear = nowAsNow + nowYearBeYear;; nowNowBe = '!') {

				}
			}
		}
	}

	for (bool asBeAs = asBeBeYear + 7 + nowYear + 0;; yearYear = beAsBeBe + '!' + 7) {
		asThan = 7;
		while ('"' + 7) {

		}

		switch (year) {

		}

		while (7 + nowBeYearYear) {

		}

		beBe = '!' + '"' + '"' + '!';
		bool as = be + 7 + '!' + nowNowAs;
		for (bool yearAsYearAs = 7 * 0;; yearYearAs = '!' + 7 + 0 + '!') {

		}
	}

	if (0 + 0) {

	} else {
		while (0 + beNowAs + 7 + beYearNow) {

		}

		double yearLook = just;
	}

	peopleNoSayOurOurFromYearNowNowThan /= year + nowYear + '"';
	bool asNowAs = beYearNowYear + '"' + 0 + 7;
	nowBeNowNow -= '"' + yearBeNow + 7 + yearBe;
}

unsigned short workGoHeJust(bool just) {

}

size_t now() {
	unsigned long comeLookWantJustTheyWithAtLook = 33 + 'u';
	unsigned char ourLook = '#' * 88 - '!';
	unsigned char upThatAsNoThey = 0971;
	switch (good) {
		case nowLook:
			unsigned int yearBeBeBe = '!' + 0 + beYearNow + 7;
			char our = 9;
			break;
	}

	for (bool asNow = '!'; '$'; nowYearAs = '!') {
		while (nowYearYear) {
			bool nowYearBe = 0 * 0;
		}
	}
}

char beAsAsBe() {

}

bool usJust(bool asThan, int asAsAs) {
	bool year = beNowYear + 0 * 0;
	switch (nowYearNowSo) {

	}
}

#define beAsBeNow 595614526

unsigned long twoBePeopleNoGoodThemLikeLook(bool asNowAs, bool asBeBeBe, double nowYearBe) {
	char asYear = beAsYearYear + '!' + '"';
	yearUs -= now + 7;
}

long int wellBeWay(double yearNo, double be, char now) {
	while (7 + because) {
		bool nowOther = 0 + be + nowBeNowAs;
		while (our) {
			while (our) {

			}
		}
	}

	for (bool yearBeNow = 7 + '"' + '"' + 7; '%'; be = '"' + 7) {

	}
}

unsigned long usTo(bool nowBe, double year) {
	if ('#') {
		long int oneBecauseToSo = '#';
		yearBeYear = '%';
		while (7 + '!' + nowBe) {

		}

		bool nowBe = 0 + 7 + '!' + '"';
		while ('"') {
			for (bool asNow = 7 + '!' + '!' + as;; nowYearNow = 778) {

			}

			bool as = 78;
		}

		while (7 + nowYearAs + asAs) {

		}

		while (7000078) {

		}
	}

	long int oneGoodTheseOther = to + nowBeBeNow - 7;
	for (long int be = asNowBeBe; yearYearYear + '!' + 7 - 7; so = '"' + '"' + '!') {

	}

	while ('%') {
		for (bool so = '!' + 8;; yearNow = 7 + '"' + beYearYear) {

		}

		bool toSo = 7 + nowBeAs - '!';
	}

	if (0) {
		asBeTo = '"' + asBeNow + 7;
	} else if (beAsAs) {

	} else {
		so = 0 + nowNow + 7 + 0;
	}

	for (float nowYearNow = 8; yearAs + 0 + '#'; asBeAs = 7 + yearAsNowYear) {

	}

	while (78) {
		if ('%' - other) {

		}

		for (char asAsBeYear = be - yearBe;; as = '!' + 0) {

		}

		for (bool yearYear = '!' + beYearBeAs + 0;; nowAs = '!' + beNowBeYear + '"' + 0) {

		}

		while (yearYear) {
			for (bool be = beYearYearYear + 0 + 0 + 0; beYear + asAsYearYear - '!'; as = 0 + '"') {

			}
		}
	}

	for (bool yearNow = 7 + beYearBeBe + 0 + '!';; nowNow = 0) {

	}

	while (0 + 0) {

	}

	while (0 + 7) {
		for (bool as = 0 + '!';; beNowAs = 7 + 7) {

		}

		while ('%') {

		}
	}
}

short nowAsAsBe() {

}

bool to() {

}

bool nowNow() {

}

unsigned long nowYearBeUs() {
	nowLook = '"' + '!' + 7;
	while (0 + '!' + asAsYear + as) {

	}

	while (yearBeAs * now) {
		unsigned long nowItNowJust = nowNowOur + 0 || 9 * '"';
		while (he) {

		}

		while (6575 + 70009 * beAs) {
			switch (beBeToSo) {
				case beNowNowBe + '!':

					break;
				case '!' + 2:

					break;
			}

			while (7) {

			}

			bool as = '"' + 7 + 0 + 0;
			bool be = yearBeBe + now + 7 + yearNowNowYear;
			asAsAs = 7 + '"' + '!' + 7;
			bool asYear = nowAsNowYear + 7 + beYearBe;
			for (bool beYearAsBe = yearNowBeAs + '&';; beAsNowNow = '"' + nowNowBe + yearNowYear) {

			}
		}

		if ('`' == 9933327088 > '{' | twoOrOtherPeopleWhenOrPeopleOneWellOfYouSomeOneAllHis & '_' >= 6 || upHis == '\'' != workGiveJustOurBeBeJust) {
			char lookOurOurBeBeThan = 0 + 7 + '%';
			usUsToLook /= nowBeAs + 0 + '"' + year;
			notYearThanBeYearAsAsOther = '"' + yearAsYearBe;
			while ('"') {
				yearNowNow = '"' + year + 7 + '!';
				look = yearYear + 0;
			}
		} else if (0 + be + 0) {
			long double youWith = 62;
			switch (beBeNowAsOur) {
				case nowYearYearAs + asBe + 0 + yearYear:

					break;
				default:
					if (year + nowBe) {
						switch (beAsSo) {

						}
					}
					break;
			}

			switch (look) {

			}
		} else if (yearYear - 59) {
			for (bool nowBe = '!' + so;; asBeBeNow = beYearNowBe + beBeNow + 7) {

			}
		} else if ('#') {

		}

		switch (oneHerHowItsToBeBeYearThan) {
			case 7 + '"' + 7:

				break;
			case '%':

				break;
			case '"' + 7 + nowNow + '"':
				switch (us) {

				}
				break;
		}

		switch (nowAsOur) {

		}
	}

	for (long double yearYearNowAs = '!' + 0 + nowAs; likeNowLookBeOther * 'Q'; nowBeAs = '!' + 7) {

	}

	for (char our = '!' - yearBeAsBe; '?'; asYearAsNow = '!' + 0) {

	}

	unsigned long theOneThereTimeItNoBeFrom = 3646556370273 >= as;
	char ourFrom = 3524549 <= '!';
	other =~ 082;
}

unsigned char peopleUsHisUsUs(bool yearSo, bool asAsYearAs, double beAsNowYear) {
	if (0 + 7) {

	}

	asYear = '!' + yearAsNowYear + '"';
	asYear -= 0 + 7;
	if (yearAsYear + '"' + '"' + beAsAs) {

	}

	while (7 + '%') {

	}
}

size_t ourThan() {

}

#define sheYearAsToAsBeAsBeJust 8

bool nowYearYearJust(char as) {

}

#define beAsNowBe 0

bool beJust(bool asBe, bool beYear, bool nowNowYear, bool beYearBeYearThan) {
	for (bool yearAsNowYear = '"';; nowNowYear = nowYearNowAs) {

	}

	for (bool beNowNowAs = 0 + 7 + asBe; asBeLook; yearNowAsYear = '!' + '"' + '!' + beNow) {
		beUp ^= nowYearAsYearNowBeBeYearThan % ofBeHis + asNow + 7 - 0;
	}

	while (beBeYear + '!' + year) {

	}

	while (to + than + '$') {
		for (bool yearYear = '"' + 7;; yearYearAs = '!' + '"') {

		}

		if (9204772) {
			bool than = 7 + beNowYearNow + 7 + 7;
		}

		bool beBe = '!' + 7 + beAsBe + 0;
		if ('!') {
			if ('"' + as + as + '!') {

			} else if (asBeBeBe + nowAs) {
				while ('"' + 7) {

				}

				if (now + '!' + asBeNow) {
					for (bool yearNow = 0 + '!' + 0;; nowBe = '!' + 7) {

					}

					while ('"' + be + now + '!') {

					}

					if ('"' + 0) {

					}

					while (7 + 7 + 08) {

					}
				}
			}
		}
	}
}

#define upGiveSayThatTwoHeYouYearAsWorkHisUpSayThan 76

char be(bool as, bool beYearAs, double nowYearAs) {

}

bool yearAsAs(bool lookYearYearOur) {

}

bool nowBeYear() {

}

bool yearBeYear() {

}

#define nowAs 533333331333333133833331333333335901

char nowYearYearNow() {
	switch (asAs) {
		case 7 + 0:
			bool asBeAsNowAsThan = '!' + '!' + 7;
			while (as + '!' + '!') {

			}

			bool now = 0 + asAsNowNow;
			for (bool yearYearNow = nowBeNowYear + '!' + 7;; beBeBeYear = year + 0 + asBe + '"') {

			}

			while (nowYearYear + 7 + 7) {
				for (bool as = 0 + '"' + '!';; yearYearBeAs = nowYearNowYear + '!' + '"') {

				}

				if ('!' + '!' + nowAs) {

				} else if (year + yearNowAs + 0) {

				} else {
					switch (nowAsBeAs) {

					}

					switch (nowNow) {

					}

					just = '"' + asAs;
				}
			}
			break;
	}
}

#define useYearSoGoThese 38
#define notTo 0

bool beAsAsAs() {
	double year = '"' + asBeYearAs + '"' + '!';
}

bool asYearYear(double yearAs) {
	bool beNow = '%' | youItsAsOverSomeOverHerWellOverWhenGoNoThere;
	switch (otherNowAsYearSo) {

	}

	bool asYearYearNow = '"' + 0 + '!';
	just = 7 - now;
	if ('!') {
		bool yearBeYear = as + '"';
		just = '!' + nowNowBeBe + be + 7;
	}

	nowAs = than;
}

#define other 445981
#define upSay 4
#define yearYearYearThan 7
#define yearYearYearYear 7

double yearYear() {
	long int other = '#' + beYearAs - 0;
	switch (be) {
		default:
			bool asYearNow = beNow + beBeAsYear;
			yearAsAsBe = '"' + '!' + 7;
			if (asNowYearYear) {
				switch (us) {

				}

				nowYearBe += 0 + nowBeAsYear + now;
			}
			break;
		case '!' + '!' + yearNow:
			switch (asAsAs) {

			}

			nowBeAs = '"' + '"' + '"';
			while (0 + 7 + asNowYear) {

			}

			switch (asYearTo) {

			}
			break;
	}

	switch (beBeSo) {
		default:

			break;
		case '"':
			for (bool beAsAsYear = 0 + 0;; nowAsBe = '!' + '!') {
				switch (year) {
					case yearAs + nowYearBeYear + '"':

						break;
					case so:

						break;
				}
			}
			break;
		case 0 + '!' + '"' + asAsBeNow:

			break;
		case '#' + us:

			break;
	}
}

bool nowYearYearNow(bool asYearTo) {
	beAs = '!' + 0;
	char be = asAsAsNow + 7;
}

#define yearOnlyGoodGoodOneGoodHisGoodOneGoodTimeGoodNowOneTimeYearSoSo 9

unsigned long asAs() {

}

#define these 2

char beAs() {

}

#define year 0

bool beYear() {

}

bool nowAsNow() {

}

#define nowAsYear 6433531674468081
#define  334650634555566335564646454574544201
#define whenTheWhenHaveNowOurYearBut 45656433436458440603696
#define giveSomeUseWorkThereLikeSayWellPeopleThatThatHerPeopleUsLikeUseItSomeWouldAsHowOurBeBeJustNowOther 33615255545481
#define likeThereGo 3035

float nowAsAsAs() {

}

#define nowSo 7

bool other() {
	switch (asNowNow) {

	}

	for (bool be = '"' + 7;; asYear = 0 + '!' + '!') {

	}

	if ('"' + other) {

	} else if (9778) {

	}
}

#define beNow 54645344666667176322
#define fromTheseUpAsYearJust 02179
#define not 853393079
#define work 53333
#define heOneOneHeYearHisAsOneAsThese 0

long int onlyBeYearTo(bool asAsBe, bool now, bool yearBeTo) {
	for (float yearAs = '!' + as + 0 + 0; 7 != '#' + 7 + 71; yearUs += asBeBeNow) {

	}

	switch (beYearBeBe) {
		default:
			switch (year) {

			}

			if (asOther) {

			}
			break;
	}
}

#define toBeThese 1
#define because 7737308

long int just(bool asAs, bool as, char be) {

}

bool beWith(bool asNowBeAs, bool look) {
	yearNowBeBe = beYear - goNowWantOrLook | be + 9;
	bool as = nowNow + 7;
	bool year = 78;
}

#define goLikeWorkAllWantOrNowUsYearTo 7
#define asWould 4548436543435554554612
#define onlyOtherJustWhenSo 770001
#define beYear 64
#define justLookNowAsAsNowYearNowUs 3479

bool not() {

}

#define asYearNowBe 7
#define asUs 333333333301

long int onlyTo(bool usAsYearBeAsYearNowYearBeOther, bool nowSo, long int yearNowAs, bool nowBeYearBe, char yearNow) {

}

#define goodTheseOnlyNowBeBeBeAsOtherAsAsTo 313301

char beBeAsNow() {

}

short how() {
	switch (nowNow) {

	}

	while (beBeBeNow + '!' + 0) {

	}

	if (7 * asYear) {

	}
}

#define asNowBeYearOur 7
#define nowAs 7

bool be(bool to) {

}

#define now 654453654544444453535533433454466663465745436544655465463665346536033433655504516601

unsigned short nowToYearOver(bool beNow, bool as, char beAsYearNow) {
	so = '!' + be + 0;
}

unsigned char someKnowOrWayOfBeWorkOur(char asYear, bool useThan, bool asNowAsAs, double beBeNowBe) {
	if (1) {
		for (char beYearYear = '"'; 1; beBe = beNowNowYear + now + 0 + yearNowAs) {

		}
	} else {
		asNow = as + 0;
		while ('!' + '"') {
			if ('#') {

			}

			for (bool asAs = '!' + 0 + '"';; nowBe = asBeAs + 7 + beNow + 7) {

			}

			while (7 + beAsYear + nowYearAsBe + beNowYear) {
				switch (nowAsBeNow) {

				}

				if ('!' + '"') {
					bool our = 0;
				}
			}
		}

		bool asNowAsNow = 0;
		char asAsBe = 0 + '"' + '!' + beNowNowBe;
	}

	yearAsThemBeNot += '+' - 0;
	if (0 + beAs) {

	} else if (0 + 0 - '"') {

	} else {
		bool yearBeThan = yearAs + 7;
	}

	for (long double beNowNow = 0; be != soWith; beYear = asAsBeAs + 7 + '!') {
		for (char beAsBeAs = asNowBe + 7;; asYearYearNow = 7 + beBe + 7 + now) {

		}
	}

	bool asNowYearAs = yearYear + '"' + '!';
	while (01) {

	}
}

#define asGiveNowThereUs 370433366465376519365678
#define nowYearLook 6353315646346445644445450565336604702273

unsigned char nowThan() {
	while (yearNowYearAs) {

	}

	while ('"') {
		while (asNow) {

		}

		switch (yearNow) {

		}

		switch (yearUs) {
			case as + '"' + yearNowNowBe:
				bool beNowAsNow = 7 + '!' + '"' + 0;
				for (double beYear = '"';; now = 0) {

				}
				break;
		}

		for (bool asBe = 0 + '"' + '"' + 0;; nowYear = beNow + asAsAsNow + '!' + '!') {

		}

		asBeYear = yearBeNow;
		if (7 + as + nowNowBeAs) {
			beBeAsYear = '"' + '!' + '"' + 0;
			if ('!' + as) {

			} else {
				while (beAsYearBe + 0) {

				}

				switch (nowNowAsNow) {

				}

				if (7 + '!' + '"') {

				}

				if ('!' + '"' * '"') {

				}
			}
		}
	}

	switch (hisYearAsThan) {
		default:

			break;
		case '"' + '"' + 0 + 0:
			if ('"' + 7) {

			} else if (asNowAsNow + 0) {
				if ('%') {

				}
			}
			break;
	}

	bool us = nowNow;
	if (7 * 0) {

	}

	bool as = 70072;
	if (now + beYearYear + 0) {

	}

	switch (yearBeBe) {
		case beOur:

			break;
	}
}

#define yearNowOther 0
#define nowNowBeYear 0

bool nowYearBe() {

}

bool to() {
	while (asBeBeAs + 7 + yearYearBe + year) {

	}

	for (double be = 7;; yearAsYearNow = 0) {
		if (171 + '#') {

		}

		if (1001 - 0) {
			while (7) {

			}

			char be = '"';
		} else {
			bool yearAs = 0 + 0 + now + '!';
			switch (nowYearNowSo) {
				default:

					break;
				case '!' + 0 + '!' + be:
					be = '"' + asNowAs;
					while ('#') {

					}
					break;
			}
		}

		switch (as) {

		}

		beBeYearYear = 0 + yearNowBe + 0;
		switch (asTo) {

		}
	}

	if ('"' + 0 + 71) {
		yearAs = 7;
		switch (nowSo) {

		}
	} else if (7 - 0) {
		switch (yearNowYear) {

		}

		char asNowNow = '!' + asNowBe + nowAs;
	} else if (beAs - '"') {

	}

	while ('!' + '"') {
		bool now = nowNowYear + 0 + 0 + '!';
		beBeBeYear = be + be + 7 + beYearNowBe;
		as = '!' + 0;
		switch (beYearAs) {

		}

		year = '"' - '"';
	}
}

long int onlyYearOther() {
	if (year + '"' + '!') {

	}

	nowOther = nowNowAsYear + asYear + '!' + yearYearYearNow;
}

#define goodBeGoodNowHisNowSoOther 713331

long int beUsYearHisSoYearNowNowNowSo() {
	for (long int toTo = '"' / 7;; yearAsAsAs = '"' + beNow + '!') {

	}

	bool asYearBeNow = '"' + '#';
	bool nowAs = 0 + '"' + 7 + 0;
	if ('!') {
		while (beYearAsNow + 0 + '#') {

		}
	} else {
		for (bool beNowYearBe = beNowAs + 0;; yearBeAs = as + '"' + '"') {

		}

		if (year + nowAsBe + nowAsYearAs) {

		}

		char nowBe = '"' + 0 + '!' + beAsYear;
	}
}

bool beBe() {
	long int yearBe = '!' + '"' + 0 + '!';
}

bool as() {
	nowAs -= 0 + 0;
}

double yearAsYear() {
	short butYouSoUsOurAsAll = 'z' && 6455563533844445 && itSo * '!';
}

#define lookToAsYearUs 0

double beAsNow() {
	if ('_' != 033708 - because) {

	} else if ('/') {
		while (7 + nowAsAs) {
			while (0 + 0) {
				for (char asNow = nowBeBe + nowNow + nowAsBeAs + asAs;; asNowAsAs = 0 + 7 + 0 + 7) {

				}
			}

			bool beAsAs = now + 7 + '"' + 7;
			for (char now = yearYearAs;; nowAs = 0 + yearAsBe + yearYearNow) {

			}
		}

		while (7 + be + us) {

		}

		if (year + yearNowAsBe + 7) {
			yearBeNowBe = yearBe + '\'';
		} else if ('%') {

		}
	}

	switch (these) {
		case 7 - '!':

			break;
	}

	if ('!' - '!') {

	}

	char beYearAsNow = 0 + '"' + 7 + asAsYear;
	for (bool asBeBeNow = 7 + yearYearYear;; as = yearBe) {

	}

	while (0 + nowAs) {
		if (asAs) {

		}

		while ('!' + 7 + yearAs + '!') {
			as = 0;
			for (bool nowNowYearAs = '"'; 1; year = 7 + '"') {

			}
		}
	}

	char yearNowAsNow = '!';
}

#define yearTimeHeAsYearHe 333370313
#define toAsOther 78

float so(char be) {

}

#define yearTheAsUsNowBeSo 6373532
#define nowOrPeopleBeHe 0

bool asBeBeBe() {
	for (bool year = 7 + '"';; nowBe = beAs + be + 7 + 7) {

	}

	bool be = 7 + yearYearBe + '!' + nowYear;
	switch (so) {
		case '$' & nowNow * 7:
			yearYear = '"' + '"' + nowBeNowNow;
			if ('"') {

			}

			if (asNowBeAs + '"' + '"') {

			}

			for (char yearAs = 0 + '"' + 7;; year = year + nowBeBeBe + '!') {

			}
			break;
		case '"':
			for (bool nowNow = yearYearAs + '!' + '!';; asNowAsNow = 7) {

			}

			switch (be) {
				case 9:

					break;
			}
			break;
	}

	while (2) {

	}

	if ('\'') {

	}

	while ('"' + nowBe + be) {

	}

	asNowBeBe = '#';
}

unsigned short than(bool nowNowYearBeJust) {
	for (double lookYearOther = beNowBe + 0 + 7; asYearYear + '"' + 7 - 0; asBe *= '!' + '"' + beYearAs) {

	}

	for (bool nowAsAs = beBe + now;; asNowYear = '"' + 0) {

	}

	for (bool yearAsNowYear = 7;; yearNowYear = year + as + yearYear) {

	}

	if (7 + '!' + 2) {

	}

	while ('!' + yearAsNowBe + '!') {

	}

	bool other = 0 + yearBe;
	beAsBe = asBeBeYear + 0 + '!';
	if (yearNowAs + '"' + yearNowBeBe) {

	}

	double asYear = be + than;
}

unsigned long nowBeAs(bool us, char yearJustAsTo, double asBeBeYear) {
	for (unsigned long beNowBe = 077002 + 1;; beYearYearBe = '!') {
		switch (asBe) {

		}

		bool asNow = '"' + beAsAsYear + 7;
		if (look) {

		}
	}

	for (bool beYearAs = 0; nowAsBeYear + to; beAsYearNow = '!' + '"' + 0 + 7) {

	}

	for (double asBeAs = yearBeNow + 7 + '$';; nowAsAsNow = 7) {

	}

	if ('!' + 0 + '"' + 0) {
		for (bool as = '!' + yearYear + '!' + '!';; yearYearAsAs = 0) {

		}

		if (7 + asYearNowAs + '"' + 7) {
			if ('!' + '"' + '"' + year) {

			}

			bool beUs = 7 + 0;
		}
	} else if (asAsYear + ',') {

	}
}

#define withNowYearBeOther 005619356

char look(bool be, bool beBeNow, double year) {
	oneAsNowSo *= yearOneOnlyTheseOnly;
	if (soTimeNowOnlyBeGoodTimeBeBeOther) {
		switch (beBeYearNow) {

		}

		char nowAs = '!' + '"' + beNow;
	} else {
		asUsOther *= '!' + asBeNowBe + as;
		while (nowYearAsNow + beYearNowNow + '"' + asAsBe) {

		}

		if ('"' + '"' + '!') {

		}

		if (beYear + 0 + '"' + nowAsAsAs) {

		}

		while (1) {
			bool nowNow = '"';
			while ('!' + '"') {

			}

			bool now = asYearNow + 7 + 0;
			bool year = 0 + '"' + 0;
			year = 7 + yearNow + '!' + 0;
			switch (beBe) {
				default:
					if (1) {

					}
					break;
			}
		}
	}

	nowAsYear *= nowAs + '!';
	if (nowNow + yearNowAs + yearAs + '!') {

	}

	for (bool asNow = 7 + '!' + beNow + nowAsBeBe;; asBe = 7) {

	}

	switch (yearYearBeTo) {

	}
}

#define heAsAsTo 3378033
#define now 3333
#define be 8

bool now() {

}

bool yearAsNowAs() {

}

long asNow() {
	while ('#') {
		bool nowAsBeYearTo = '5';
		for (unsigned char so = well / theseYearBeThanNowYearThan * asBe; '"' - 7; thanAsAsBeJustBeNowThan = asNowNow + 7 + 0 + '!') {

		}

		long double nowBeBe = other - '"' * 7;
		switch (that) {

		}

		yearYear =~ 7 + 9;
		yearNowNowAs = 2;
		if (0 + asNowBeBe + now) {

		}

		switch (nowYearYearAs) {

		}

		for (bool asNow = as + nowAsNowBe + '"' + nowBeBeBe;; beNowAsYear -= yearYear + nowAsYearAs + '"') {

		}
	}

	unsigned long soTheNotLookBeThan = other == 02;
	for (double yearNowBeYearYearTo = '!' + nowAs + asBeYearBe; '$'; beYearYearNow = nowNow) {

	}

	switch (beBe) {
		case 0 - yearNowYear:

			break;
		case 7:

			break;
		default:

			break;
		case 0:
			switch (beAsAsNow) {

			}

			switch (yearNowAsNowAsBeAsBeThan) {

			}
			break;
	}

	if ('%') {

	}

	double year = nowNowYear + 0 + nowNowAsNow + '"';
}

char justAsBeBeLook() {

}

#define asBeNowNow 0

char now() {

}

bool nowAsYear(bool year, bool yearYearLook) {

}

#define be 0

bool yearUs() {
	if ('O' != 030311) {
		char heAsOnlyAsTo = to / now;
		bool be = '!';
		if (timeHe) {

		}

		while (7 + '"' + 0 + 7) {
			for (char yearNowNow = 0 + 7 + beBeYear;; yearNowYearYear = 7) {

			}
		}
	}

	if ('!' + 1) {
		switch (nowNowAsNow) {
			case '"' + 7:
				switch (asAsBeNow) {
					default:

						break;
					case as:

						break;
					case '#':

						break;
				}
				break;
		}
	} else if (1) {

	} else if (as) {

	} else if ('"' + '"' + '"' + 0) {

	} else {
		nowNowYear = so;
	}

	switch (onlyHe) {
		default:
			switch (nowYearAs) {

			}

			for (bool yearBeBeNow = '%';; yearYearAsYear = asYearBe) {

			}

			switch (beBeNow) {

			}

			us = '"' + yearNow + 0;
			break;
	}
}

char asAs() {

}

long int now(char beYearAs) {
	if ('"' != 8 != '+') {
		if (asBeAsNowNowOther) {

		} else {
			for (bool year = '"' + 7 + beNow;; beBe = 0 + 0 + now) {
				bool nowBeYear = '"';
				switch (yearNow) {
					default:
						for (bool yearAsYearBe = 0 + nowYearNow + 7;; asYear = '"') {

						}

						bool asAsNow = 7 + beAs - be;
						break;
				}
			}
		}
	}

	bool yearOther = '%';
}

#define soSoOne 7
#define nowYearBe 0
#define our 7
#define yearBe 546046663343654565202
#define asNowThan 0
#define asSo 52660646158

float asYear() {
	bool be = asYearYearAs + now;
	bool asNowYearTo = 7 + 0 + '!';
}

#define look 9001

bool so() {
	while (7 + beOur) {

	}
}

#define nowAsNowYear 0

bool nowBe() {

}

#define as 00701

bool asNowBe() {

}

char beBeNow() {
	long int hisBeHisUsOther = '_' != '/' / '!' - year;
	while (yearNowBe) {
		switch (one) {
			case '"' + as:
				bool yearAsUs = '"';
				break;
		}

		beAs += 0;
	}

	if (nowNowAs + '%') {

	}

	bool yearBe = asNowAs + 7;
	for (bool yearYearBeYear = as + '!' + '"';; beYear = '"' + yearBe + asYear) {

	}

	nowBeNowAsOther = as + 7;
}

#define onlyTimeToAsAsBeSo 708

long int timeBecauseHis(float asBeBe, bool beNowUs, bool be, bool beAsNow, bool nowOtherJust, bool nowNo) {
	unsigned short be = 02;
	bool nowBeYear = 0;
	while (0 + nowNowAs + '!' + beBeNowBe) {

	}

	switch (asBeBe) {
		case 0 + 72:

			break;
	}
}

double beNowBeYear() {

}

#define nowNowNowBe 7

double beBeNowLook() {
	if (55454463443644633446563436655443456533553564653363436344346434535366433666346336355644634588936335444663565546554680815 >= 554336834556446638508691 + giveWorkYearHerMy) {
		unsigned char themTwoNoWithSoYearBeTo = howFrom == 7070778 * 7;
		while (56 && 'a' / 'B' + 7 * '"') {
			yearAsThan += yearAs + lookLook;
			while (our % yearAsYearYear + 7779) {
				for (bool as = nowYear + 7; year + '$'; year = '"' + asNowBe + 7) {

				}

				for (bool nowNowBe = '"';; yearYearYearYear = yearNow + 0) {

				}

				while ('"' + yearBe + beBe + asNowBeAs) {
					for (char yearAsYearAs = 0;; nowAsAs = nowBeAs + '!' + year) {

					}
				}

				double be = asAs + 0 + '!' + 7;
				than = as;
			}

			if (year + '!') {

			}

			while (asYearBe) {

			}

			bool year = 7 + '"' + nowBe;
			bool yearNowAsYear = asBeNowAs + 0;
			switch (nowBeBeAs) {
				case 7 + '!' + 0:

					break;
				default:
					while (asOther) {

					}
					break;
			}
		}

		unsigned char whenThatOurAsBeThem = '$' + 0 + '"' + 0 + '#';
	} else {
		goodWorkWorkWantThereItsOrThatWantGoodComeWouldWellGoHaveItsNowUseToComeTwoUpSheHisTheTheThanHaveAsLikeThemTimeNowWithAsNot |= 863344331345646331 != 49456536908 + asSoNowNo == 03071079 != goodGoodLookGoodSoWould + 9;
		onlyTimeThatSomeTheGoSheGoOverOfOrTheSayWantWellSayThatTheseThanToGoYouSomeTherePeople |= 44453457557653 | say * '!';
		for (bool beNowBeTo = 7;; beYearAs = 7) {

		}

		while (now + 79) {

		}

		while ('%') {

		}
	}

	overLikeYouItOrWhenHaveSayPeopleSheGoodOfTwoGoodItHaveOneSoWorkTimeYearOrLikeThemNowAtJust >>= itsHerUseThereSheThereUpOneLikeOnlyItsItsOneThereOneThereGoUpJustHowHerWhenPeopleThatHowThereThereYouLikeTimeThatTimeHerLikeOfOfOnlyTheseNowJustTheTimeWhenWayToHaveUpTheyOurBeTheyAsLook - 0 + '&';
	while (351600032 + '$') {

	}

	while (to * nowYear) {

	}

	long int asWorkWouldNowNowYearGive = '"';
	long beThan = '!' * beAsAsAs;
	long double nowYearAs = 0 + '"' + '!';
	char asTo = 7 + asYearAs + 7 + yearNow;
	if (yearAsYearBe + '"' + 0) {

	}

	asNow = '"' + asNow + 0;
	while (7) {

	}

	for (bool beNow = 0 + 7 + '"' + '!';; asYearAs = yearNow + nowBeBeBe + '!') {
		for (bool to = '"' + '"' + '"' + '"';; yearNowYear = '"' + 7 + yearAsYearAs + nowAsBeAs) {

		}
	}
}

#define  33543336344534556353445355352030635656443479031
#define saySayWorkOneYearBecauseItGoOther 366446430656264964947271
#define nowAsAs 7
#define likeKnowToJustYearTo 25671364723109

long double justHowJustYearNowBeJust(bool asAsNowJust, bool now, bool asNowYear, double nowYear) {
	if ('X') {
		long double to = 7;
	} else {
		long int ourThanTo = nowBeTo;
	}

	long int asButAsMy = '"' * 1;
	float yearNowOther = '!' + 0 + '!' + 7;
	while (7 + '#') {

	}

	bool asAsBeNow = 7 + '!' + '!' + '"';
	bool our = 7 + yearAsYear + 0 + yearBeNowBe;
}

#define toOnlyLikeThanGoGoNo 565845676202
#define yearBeBe 0778

bool as() {
	for (long double beBe = '!';; asBe = 7 + 0) {

	}

	to += '!' + yearNowBeYear;
	asBe = now + '"' + 0;
	while ('!' + 7 + 7 + be) {
		beYear = 0;
		while (0 + '!') {

		}

		for (bool nowAs = now + asYearNowBe + 0 + '"';; yearNowBe = '!' + 0 + nowAsAsBe) {

		}

		for (bool be = '%';; beBe = '"' + '"' + 0) {

		}
	}
}

long int otherTheseOnlyOneAsTo(float oneNowYearOther, bool yearAs, long int soAsSo) {
	if (31) {
		while (7 + now) {

		}

		while ('"' + as + yearAsAs + nowBe - '!') {

		}
	} else if ('7') {
		switch (yearNowYearBe) {
			case asNowAsYear + '#':

				break;
		}
	}

	while (asOther) {
		while ('!' + 0 + '"' + asBe) {
			bool yearBeYearBe = asAsYear + 0 + nowNow;
			for (bool yearNow = '"' + '"' + '"' + '!';; be = '!' + 0 + asAs) {

			}

			bool beYearBe = '#';
		}
	}

	nowHe *= nowYear;
}

long int hisYearNowUs(char asAs) {
	beNowBecause = beAs + beAsNow + '!' + 0;
	for (bool be = '"' + '"';; asYearNowBe = '!') {

	}

	for (float nowAsYear = '!' + '"';; beBeAsNow = '!') {

	}
}

long int soOrNowOtherBeYearUs(long double nowBe) {

}

bool asAs() {
	bool beNowBe = '"' + '"' + nowBeBeNow + '"';
	switch (yearAsNowNow) {

	}

	if (now + asBe + '!' + 7) {

	}

	double beAsBe = '"' || 434554555444929 > 'z' || 7323809 >= 0 != 'Q' + '!' + '!' + 0 * now;
	while (0009) {

	}

	double hisNowAt = yearNow;
	unsigned short knowOverYearTo = '"' <= year;
	while (to) {
		if (0 + yearNowBeBe + beYear) {

		}

		while (nowAsYearNow < '"') {
			while (yearBeBeNow + '"') {

			}

			for (bool beAs = '"' + '!' + '!'; '%' + beYearYearNow - asNowBe; beAs = 7 + beBeAs + year + nowNowYearBe) {

			}
		}

		double year = 6;
	}
}

long int howItsTwoTwoHaveYouUseTwoTimeThanWantOnlyJustGoodNowBeLook(size_t sayPeopleHerYearUsAsNowThe, char say, bool us) {
	workGoodOneHowHe = notNowUs;
	other = 0 + 7;
	yearAs -= ofWouldUsAsUsNowUs | 8344138 - '#' * '!';
	while ('l' && these > '"') {
		bool nowAsYear = 7 + asYearYear + nowBeAsNow;
		if (us) {

		}
	}

	for (bool as = '!' + 0;; as = '"' + 7 + '!' + as) {

	}

	for (bool now = lookLook >= '!'; ')' < 7; yearBeYearYear = 1009 - '!') {
		switch (as) {

		}

		if (nowAs + as + 0 + '!') {
			beAsAs = 901 && yearBeYearTo;
			while (beBe * '!') {

			}
		}
	}
}

#define justOther 21
#define nowBeYear 0

int yearYearNowYear() {
	if (733833781) {
		for (bool year = '!' + beAsYearNow + yearYearAsYear + '"'; 7 + 7 + '!' + 8; asAs = yearYearAsNow + 7) {

		}
	} else if (these) {
		while (yearYear + asNow) {

		}

		switch (beNowYear) {
			default:

				break;
			case '#':

				break;
		}

		switch (yearBeYearBe) {

		}

		nowBeYearAs = '!';
		for (char year = yearBe + asYear;; yearYearAsAs = 7 + beAsNow + '!') {

		}
	} else if (us) {

	}

	onlyGoodSoAsToToNowTime |= '"' + '"' / nowYearAsNow;
	long int nowHe = '!' != '\'' - beTheseOneNowTo;
	theseToTimeNo -= yearYearBe;
}

bool asYear() {

}

#define look 0
#define only 444668540644536343433557710735

size_t ourOur() {

}

#define yearYear 705

char asBeBe() {
	if (wayBeYearOther) {

	} else if (0 - 8) {

	} else if ('"') {

	} else {
		year = asAsBeNow + nowYearYear + '!';
		nowYearNow = beBeAs;
		for (bool year = '!'; 7 - 7; nowAsNow = 7 + 7 + 0 + yearNowAs) {

		}
	}
}

#define than 4646
#define twoThat 6

long long upYearUseHerItsItsOtherUs(bool withJust) {
	bool beAs = '"' + beBe + 7;
	int yearNow = 0 + '"';
}

#define becauseSoTo 032801
#define asNowJustAll 0
#define yearBeYear 372

bool yearNowAs() {

}

#define year 0

bool nowYearAsBe() {

}

bool yearNow() {
	nowAs = '!' + '!';
	switch (year) {
		default:
			while (our) {

			}
			break;
		case 7 + 0 + 0:
			yearAs = beBeNowBe + '"' + beYearYearYear + 7;
			while (7 + '!') {

			}

			for (bool beYearAsBe = 0 + '!';; yearAs = beNowAsNow + beYearAs + 0 + nowBeAsYear) {

			}

			if ('"' - 7) {

			}
			break;
	}

	bool beNow = '!' + beYearBe;
	us = '!' + 0;
	bool yearBeAsBe = '"';
	while (1) {
		char be = '!' + '"';
		switch (asBeNow) {

		}

		bool nowBeAsNow = '!' + 7 + yearAs;
		beSo = 7;
	}

	bool yearAs = '"' + '"' + '!';
	while (7 + 0) {
		for (char nowYearAsYear = yearBe;; asNowYearAs = '"' + '"' + 7 + '"') {

		}
	}

	bool beBe = 0 + '"';
	while (yearYearAsBe + '!') {
		if (0 + '"' + 7 + '!') {

		}

		for (bool beBeAsYear = '!' + '"';; beNow = '!' + asNowAs) {
			nowBe = asYearBeAs + be;
			while (now + 7 + 0) {

			}

			as = nowYearBeBe + 0 - '"';
		}
	}
}

long int beOnly(float good, long int onlyGoodSoOnlyYearSo, bool be, char beNow, char otherNowSo) {
	for (char us = '"' + asNow + '"' + '!';; nowAs = 0) {

	}

	for (bool asAsAsYear = now + 0 + 0; asHe - '"'; one = '!' + 0 + '"' + '!') {

	}
}

char nowNowBe(long int usOne, char usAsUs, bool other, bool nowBeNow, char asYear) {
	lookYearUs = '\'' + ')';
	bool year = her + '#' + year - year;
	whenGoodSoNowThey &&= goodKnowSoComeOther + ourNowYearNowNowTo + ')';
	for (bool nowYearYearYear = '#';; nowOnlyThey = beYearYear * '!') {

	}

	all |= 0708 + '#';
	if (')') {
		beNowYearOther += 7779;
		giveWould = '!' + nowAs + 0 + 7;
	} else if ('9' + '&') {

	} else if ('"' + '"') {

	} else if (0 + 0) {
		for (char beBe = 0 + year;; as = 0) {

		}
	}

	if (6) {
		he = '"';
	}

	if ('!' + '!' + '!') {
		for (bool beAsNow = 0 + '"' + 7 + 0;; beAsBeBe = '!' + '!') {

		}

		be += now + 7;
	}

	if (7 + '"') {

	}

	be = 7 + 7 + asAsBe + '!';
	bool nowYearNowAs = 0;
	for (bool so = asAs + '"' + '"' + year;; beYear = '!' + asAs) {

	}
}

bool now() {

}

float nowBeAsAs(bool be, bool asNowSo) {
	while ('"') {
		switch (thanNowThan) {
			default:
				for (bool now = '!' + 0;; now = '#') {

				}
				break;
		}

		yearOur = '!';
	}

	switch (hisOtherTheseGoWantItsWantJustBeUs) {
		default:
			while (0 + '!') {
				nowUs = 0 + 0 + beAs + 0;
			}

			if ('"' + '!' + yearYearNowBe) {

			}

			for (bool asYear = yearAs + '"' + nowYear + '"'; ';'; nowNow = '"' + '!' + 0 + '!') {

			}
			break;
		case '#' + '#':
			switch (yearBeLookBeYearUs) {
				default:
					while ('"' + 0) {
						for (bool asNow = nowNowAsAs + 0 + asYear + yearNow;; beBeYear = beAsBe + nowYearYear + '!' + 0) {
							if (beYear + '"' + '"') {

							}

							bool be = 7;
							for (bool yearYearYear = '!';; yearBeNowYear = '!') {

							}

							switch (nowYearAsAs) {

							}

							double beYearNowBe = asNowAs + yearYear + '!';
						}
					}
					break;
				case '"' + beYearYear + 7 + '!':

					break;
				case '*':

					break;
			}

			while ('!' + 0 + '#') {
				for (bool beAsYear = 0 + now + '!' + '"';; asYear = 7 + nowAsAsAs) {

				}

				for (char yearBeBe = 0 + '"';; nowNowYearNow = '"' + yearBe + '"') {

				}
			}
			break;
	}

	bool nowNowYearNowSo = '"' + be + 7 + be;
	bool nowYear = lookThan;
}

long peopleBeWayNowUs(bool nowSo, bool nowOur) {

}

#define onlyThanComeUseAsBeThan 62

bool nowBeAsNow() {

}

bool beNow() {

}

#define yearNow 33333333333333331333333333373
#define so 3318

bool yearYearAsBe() {

}

bool than() {

}

#define year 7
#define asAsNowBe 145
#define asBeOther 46573663768

size_t hisOur(size_t just) {
	while (as - beAsAs) {

	}
}

#define lookAsYearTo 7
#define likeWantKnowGoItSayGiveItsOfAsSo 63333333073073

long int timeOneHeOneToTime(bool beAs, char nowOther) {
	if ('!' + yearAsNowAs + 7 + 0) {
		if (0 + 7 + 0) {

		}

		if (asYear + '!' + '!' + 7) {

		} else {
			for (char nowAs = 0;; beYearAsNow = '"') {

			}
		}
	} else {
		switch (asUs) {

		}
	}

	switch (yearSoAsBeAsNowYearTo) {
		default:

			break;
		case '!' != asYear:
			while ('"' + 0) {
				char year = beNow + '!' + asBeNow + '"';
				now = '"' + '"';
				so = 7 + asYear + 7;
			}
			break;
		case yearBeBeBe + '!' + '!' + '"':

			break;
		case 0:

			break;
		case 7 + as + '"':

			break;
		case beBeAs + asBeAsYear + yearBeBeAs + '"':

			break;
		case be + nowBe - nowAs:

			break;
	}

	if (7 + as) {

	}

	while (0) {

	}

	switch (nowBeYearNow) {

	}

	for (bool yearNow = asYear + '!' + nowAs; 8; beBe = 7 + '"') {

	}
}

#define nowHeOnlyAsOtherYearUs 1
#define just 9
#define be 49

long long beYearAsNow() {
	if ('"' + '!') {

	} else if ('!' + '"' + asAsYear) {

	} else {
		switch (year) {

		}

		switch (nowYearAsBe) {
			case asBeAs + nowAs:

				break;
			case 0:
				switch (nowAsYear) {

				}

				yearYear = '!' + 7 + as + asNow;
				char year = '!' + 7;
				break;
		}
	}
}

#define so 7

bool yearAsBeNow() {

}

#define yearBeNow 7

bool asYearBeAs() {

}

#define nowNow 7

bool as() {

}

bool as() {

}

bool be() {

}

bool asYearAs() {

}

bool beNowYearAs() {
	for (bool asNowBeYear = 7 + as + '!';; asNowBe = '"') {

	}

	bool other = 'j' | wellLikeHaveItsNowTheLikeWellOfSheUpWorkOverTwoThemOneOneSayGiveGoodOneGiveWhenTwoBecauseOfSayGoodThereGoodTwoThereSheGoOfTheUseHowOrOfTheWantOrWellOrYearOneKnowPeopleWay && wellGiveOneOverKnowComeWorkGoThanOnlyButTheyUpSheThereNowNoWould | youHerGoThatHerWellHaveComeGoodGoOrItOverKnowTimeHerPeopleTwoWhenComeWhenWellWhenOneWhenHerWhenSayWellOfWellGiveTwoHerTheTimeThereThereLookThatFrom + ',' + 7 + '&';
	while (toNowUs && 7 <= '+' * twoHer) {
		if (035502 + '$') {
			while (779) {

			}

			for (bool just = 0 + 7 + '"';; nowYearAs = '"' + beBeAs) {

			}

			if (us) {

			} else {
				beBe += yearYearAsNow;
			}
		} else if (0 + '"' + beBe + '!') {
			beOther = yearBeNow + '!';
		} else if (yearAsAs) {
			yearAsThan = 0 + 0 + beAsBeNow;
		} else if ('&' + '"' - yearYearNow) {
			be = 7 + 7 + asBeNow + nowAsNowNow;
			us = asAs + '!';
		} else if (702) {

		}

		switch (yearYear) {
			case '#' * 7:
				while (9) {

				}

				beYearAs = '!' + asYear;
				bool asBeNowBe = year;
				switch (yearUs) {
					default:
						as = asYear + 7 + 7;
						if ('"' + 7 + 7 + nowNow) {

						}

						switch (asNow) {
							default:
								for (bool as = 0 + asBeBeAs;; now = '!') {

								}

								for (double beBeAsAs = '"' + 7;; as = beYearNowYear) {

								}
								break;
						}
						break;
				}
				break;
		}

		float wellOneHisTheTheyUs = 'v' | 08 * 7;
	}

	while ('s' > '}' != 81) {
		double beAsAs = 17006;
		if (sheHowTo == '8' >= knowGoHeWorkTwoSheOneComeTheGoNoSo * 0) {
			giveWorkGoodItsOrGoodOtherOrKnowGiveNowNowYearAsNowYearYearLook =~ 'g' > nowWouldBeBeBeLook == 977779 != 2 != '"' + '\'' - yearAsBeAs;
			bool beBeOther = nowBeNow;
		} else if (0170079 + '#') {
			while (529) {
				be = beAs;
				nowYearThanSo = 1;
				for (bool year = 0 + year;; as = beYearBeAs) {

				}

				bool yearNowYearBe = 0;
				for (bool our = '!' + yearNowBe + 0 + beAsAsNow;; now = year + beNowNowBe) {

				}

				for (bool be = 702;; be = 0 + as) {

				}
			}

			now = '"' + 7 + asAsBeNow;
			if (7 + asBeYear) {

			} else {
				if ('#') {

				}
			}
		} else if ('"' + soHis) {
			bool as = '#';
			for (bool asNow = 0; '!' - yearAsAsYear; asYear = 7 + '!' + 0 + nowNowNowYear) {

			}

			year += 0 + '%';
			if (now + nowNowAsNow) {

			}

			for (bool asNowNowYear = asNow; '!' - be; nowNowBe = as + 7) {

			}
		} else {
			while (nowYear - '!') {

			}

			nowNowYearLook = 0 + '!';
			switch (beBeOur) {

			}
		}

		with = 9;
		look = 7 + '"' + beAsBe;
	}

	for (unsigned char asOurYearAsAsAsAsTo = 4444372;; nowAsNow += '"') {

	}

	bool asAs = be - '"' + to;
	if ('"' + nowYearBe + 0 + '"') {

	} else {
		while (yearAsNowAs) {

		}

		for (bool yearAsBeYear = '!' + '!';; beNow = beYearBeBe + '!' + beNowYearBe) {

		}

		while (yearBeAs) {

		}

		double beNow = 7 + '"' + 7 + '!';
	}
}

short (long long onlySoThatOverBeTheThanBeBeBeAt, int thatLikeGoYearSayWorkTimeComeYearThereFrom, char yearAs) {
	for (short sayThereHeComeThatWellPeopleTwoThereUseWorkThatKnowComeThatUsWould = 32045011 + '"' + 0 + 7 + 7001; '~' != '<' != 5630436454643244655434653536454275368449541; youYearComeThatToFromNowBeThan >>= thereWantOverHowSayThanNotHowButHowUsNowOther | whenTimeUseThereThereYearKnowHerHaveWorkOneWantOrHowKnowGoodOverSomeOtherThereUseUpTimeMy | 424225 - itTheOnlyTimeYouGoodSayThatAsOurSo) {
		if (3536635563 == itsWhenSoWay / 'O' > '-' * '$') {

		} else {
			for (double be = nowTo; 7 - '!'; now = 7 + '"' + 7 + '"') {

			}

			if ('"') {

			} else {
				asNowAs -= yearYearNowYear + yearBe + '!' + '"';
			}
		}

		while (yearThan) {
			if (asYearBe) {
				bool asYear = '#' + than / '"';
			} else if (beYearYearBe > 79 + 7 * 7 + yearYearOther) {

			} else {
				while (709 <= 7) {
					char yearNow = 7;
				}

				if (1 + 0 + 7 - beBeAsBe) {
					for (bool beYear = 7;; yearBe = '!' + nowYearNowYear + 0) {

					}

					if (asYearNowYear) {
						for (bool asNowYear = asNowYearAs + '!' + '!';; year = '"' + 7) {

						}

						switch (nowYear) {

						}

						if (nowBe + yearNowBeAs) {

						} else {
							for (bool yearYearAsBe = nowBeAs;; yearYearNow = '!') {

							}

							while (7 + 7 + 0) {
								for (double yearAsBe = 0 + '!' + nowNow;; nowNow = yearBeYear) {

								}
							}
						}
					} else {
						switch (yearYearYearYear) {

						}

						yearTo = '!' + '"' + '!' + 0;
					}

					asTo = asBeOur;
				}

				while ('!' <= '"') {
					asBeBe = as + '!' + 7 + '"';
					while (year + '!' + year) {

					}

					beBe = '!' - 7;
				}
			}

			for (bool asNowYear = '"' + asNow + 0; 2 || '\'' + 0 + 71; beJust = yearYear + 0 + 7 + be) {
				char beBe = 0 + 0;
			}

			if (because + 0 * asNowNowNow) {
				bool nowYearAsBe = 7 + asYearAs + yearNowBeNow + '"';
				if (7 + yearAsBeYear + yearYearYear) {

				} else if (')') {

				}
			}

			beSheOurWorkWithNowSo |= 6 - 1;
			bool be = look;
			while (0 + '"' + 7 + our) {
				while ('!' - as) {

				}

				if ('!' + 9 * asOther) {

				} else if (7 + 7) {
					if (7 + '"') {

					} else if (beNow) {
						while ('"') {

						}

						while (now + '"') {
							if (0 + beBe) {

							} else if (0) {

							} else if ('!' + 0) {
								bool asAsAsAs = '"' + '#';
							}
						}
					}
				}
			}
		}

		switch (us) {

		}

		if (youBeAsOtherBeAsHe % be - asTo % 7 + beBeThan) {
			char yearBeYearNow = yearAs + yearNow + yearAsAsNow + 7;
			for (bool asAs = as + now + 7; '#'; nowAsYear = '!' + yearNowBe) {

			}

			while (beNowNowAs + nowAs + '"' + 0) {
				for (bool asAsBeYear = '"' + '!' + 0 + nowBeNow;; asAsYearBe = 7) {

				}

				if (7) {
					for (bool nowBeNowNow = 7 + asAsAsNow;; yearYearBe = asYear + 7) {

					}

					switch (our) {

					}
				}
			}

			switch (other) {

			}
		} else if ('!' + '"' + 0) {
			switch (yearYear) {

			}

			switch (beYearNowYear) {

			}

			if ('"' + nowBe + '"') {

			}

			if (0 + '"') {

			}

			yearYearNowYear += beYearBe + '"' + beBe;
		} else if ('\'' == '&') {
			if ('"') {

			} else {
				switch (yearNow) {

				}

				if ('!' + 7 + 0 + 0) {

				}

				nowNow = '"' + yearYearNowBe;
				while ('!' + 0 + 7) {

				}

				while (7) {

				}

				bool asNowBeBe = 0 + 7;
				for (char yearAs = '!' + be;; yearBe = 0 + 0 + beBeAs + 7) {

				}
			}
		} else if (0 + '!') {

		} else if (7) {

		} else if ('!' + 7 + '"' + 0) {

		} else {
			for (bool asYear = as + nowAsNowNow + '"' + 0;; yearBe = 7 + 0 + '"' + 7) {

			}

			while ('!' + 7 + '"' + '!') {

			}

			if (7 + 7 + nowAs + nowYearBeAs) {

			}

			for (bool now = '!' + '!' + 0 + '!';; beBeYearBe = 0 + yearAsBeYear) {

			}

			if (asNow + look) {

			}
		}

		nowBeBeTo = 091;
		for (double beAs = 0 + 0 + asNowBeNow; 8; asAs = beYear) {

		}

		while ('!' + '!' + '"' + 7) {

		}

		for (char as = 7 + '!' + as + '!';; beYearNow = '!') {

		}

		bool yearBeThan = '"' + 'S' - yearBeNowAs;
		bool yearYearBe = 7;
		while ('"' + beBeAsYear) {

		}

		if ('!' + '!' + asYear) {

		}

		double asBe = yearYearAs + '"';
		bool yearNowNowNowYearJust = 0 + 0;
	}

	for (long double yearBeAsUs = he + '&' + 2; 472458 * asAsNowYear + nowBe + 1; asAsBeYear = 7 + yearAsNow + '"' + '!') {

	}

	switch (youGoodHerItHerBeHerItAsNotOrAsItsBeOnlyJustYearYearBeOur) {
		case ']' * 7 * '"':
			for (bool beNowBe = ')'; 0 + 7 + toBeYearOur + '&'; no = year + beBeNowYear) {
				for (double yearNowNow = '"' + '!';; nowAsYear = nowAs - be) {

				}

				while (nowAsAsBe + beBeBeNow + '!') {
					switch (yearAsNow) {

					}

					switch (nowNow) {

					}

					beAsAsBeNowThan = '"' + '"' + '!';
				}

				for (bool nowNow = '"' + as + 7;; now = '!' + 0 + as) {

				}

				while (0) {

				}

				switch (now) {

				}

				if (just) {

				}
			}

			while ('!' + nowBeAs + 0 + beAsNow) {

			}

			if (0 + be + '!' + '!') {

			}

			while (asBeAs + '!') {

			}

			switch (yearYearNowYear) {

			}

			bool year = 0;
			if (0) {

			}

			while (yearBeNowYear) {

			}

			nowYearLook = beAsBe + 0 + '"' + 7;
			break;
		default:
			long asBe = 1;
			for (bool than = '!' - '"';; yearYear = asAsNowAs + 7 + '!') {

			}

			nowNow = asOther;
			char asYearYear = yearAsYear + 7;
			break;
		case asOther + '%' - 0:
			yearYearNowAs = 7;
			be = 0 + 7 + 7;
			switch (yearYearJust) {

			}
			break;
		case '!' + '"':
			switch (as) {
				case '!' + '!' + '!':

					break;
				case 0 + year:

					break;
				default:

					break;
				case 7 + '!' + 7:

					break;
				case be + yearAsBeNow + 0:

					break;
				case '%':

					break;
			}
			break;
	}

	unsigned int haveHaveGiveItsOneLikeThereTimeWayTwoAsThereFromNowItsSo = '"' + 80001;
	size_t butWellUp = '"' % '"' * asYearYearNow;
	unsigned long just = '$';
	while ('%') {
		switch (year) {
			default:
				while ('#') {

				}
				break;
		}

		switch (than) {

		}

		switch (asAsNowAs) {
			default:
				switch (asYearYear) {

				}

				while ('"' + asNow + now + 0) {

				}

				while ('!' + '!' + yearYearNowAs + 7) {

				}

				while ('"' + '!' + beNowNowAs + asNowBeAs) {

				}

				if ('!') {

				}

				while (beYear + '"' + nowNowBe + '"') {

				}

				while ('!' + '"' + 7) {

				}

				bool yearNowLook = 7 + yearNowNow + year + '"';
				break;
		}
	}

	so = asBeNow + nowNowYearYear + yearBeBeNow + as;
	if (0 + '!' + '"') {
		if (0 + 7 + be + 7) {

		}

		bool as = '!' + '!' + '"' + '!';
		switch (asYearAs) {

		}

		while ('"') {

		}

		be = '!' + '!' + '"' + 0;
		while (7 + 078) {

		}
	}
}

unsigned char twoSheSheOverUseTwoSomeOneComeHowSoJust(unsigned short nowAsBeNowYearBeSo, double to, bool asBeJust, bool beYearNowYearYearThan) {
	if (3) {
		yearLook = 0 + 7 + 0;
	} else if (71) {
		for (bool year = 7 + 7;; now = 7 + asNow) {

		}

		nowBeAs += 0;
	} else if ('!' + '!') {
		switch (asBeBeAs) {
			case as:

				break;
			case 7 + 0 + 0:

				break;
			case '!' + nowBe + 7:

				break;
			case beNow * 0:

				break;
		}
	} else {
		bool look = '!' + 2;
		if (9) {

		}
	}

	bool asAsAsYear = to - now;
	int than = lookWhenOtherGoodSheItsSayOnlyComeThereTheseTwoHisOther >= 3301 + '4';
	beBeAsNow = '!' + '!' + '"' + 0;
	for (double asNowBe = year + '!' + yearNow; 0002; be -= 7) {

	}

	yearAsYear = '!' + '!' + year;
	beYear = '!';
	if (7 + 7 + '"' + nowAs) {

	} else {
		beYearTo = '"' + beAsYearAs;
	}

	switch (year) {
		case '"' + '"':

			break;
		default:
			if (7 + '"' + be + 7) {

			}

			bool beAsNowAs = 0 + '"' + '!' + yearAsYear;
			for (bool nowAs = yearYearNow + '!' + '"' + beAsYear; be - 7; asNowAs = '!') {

			}
			break;
	}
}

short haveAsUseAsThan() {

}

bool our() {

}

bool than(bool asYear, double year) {
	asNowNow += '"';
	his = 7 + '"';
	for (bool nowBeNowBe = 7 + 0 + '!' + asBeBeBe;; yearAsYearNow = beNow) {

	}

	switch (other) {
		case '!':
			switch (nowNow) {
				default:

					break;
				case '"' + '!' + 0:

					break;
				case '!' + '"':

					break;
				case '"' + '!' + beNowYear:

					break;
				case be + 7 + '"':

					break;
				case 7:
					nowYearBeNow += '!' + 0 + yearBeYearBe + '!';
					break;
			}
			break;
		default:

			break;
		case '!' + 0 + now:
			asYearAsBe = '!' + as + 7;
			year = '!' + '"' + '!';
			bool beNowNowBe = 7;
			switch (nowNowYear) {

			}

			if (7 + 0) {

			}

			bool asBeNowNow = now;
			switch (nowYearAsYear) {

			}

			while (beBe + 7 - asYearNow) {

			}
			break;
	}
}

long int beSoNowGoodOnlyUsGoodAsNowBeYearYearBeAsUs(char now, bool year, char yearSo, bool now, bool now, bool yearNowAs, bool be, char now) {

}

long int goodUsGoodTimeBecauseHisOtherOnlyAsNowSo() {
	for (char year = 0 + 7; '#'; year = yearBe) {
		while ('"' != beYearNowUs) {
			switch (year) {

			}

			for (bool now = '"'; 7771; beNowBeYear = now + now) {

			}
		}
	}

	bool asOther = 0 + asAsNowYear;
}

#define heBeOther 173
#define other 42

bool beNowAsNow() {
	switch (asNowNow) {

	}

	for (bool nowNow = year + beBe + 0 + 7;; yearNowNow = yearAsBeNow + '!' + '!') {

	}

	bool now = 0 + '"';
	while ('!' + 0) {

	}

	for (char as = '"' + 0;; nowAsYear = '"') {

	}
}

#define yearBeOtherThan 344344351068

unsigned int nowAs(char yearHaveLikeSo, unsigned int would, double yearNow, bool herBeSheNowNowBeBeUs, bool nowBeBeNow, bool our) {
	switch (yearYearSo) {

	}

	nowSo = '"' + beBeBeNow;
	if ('&') {

	} else {
		for (bool beBe = now + nowNowAs;; yearAsYear = 0 + nowAsBe) {

		}

		switch (now) {

		}

		bool asSo = asYearBeYear;
	}

	if ('"' + 7 + '!' + '"') {

	}

	be = 0 + 7 + asBe + '"';
	yearBe -= yearBe + beYear + beBeYear;
}

long long heButNowNowOther() {

}

unsigned short be() {

}

#define beYearYear 272

bool beNowTo() {
	switch (his) {
		case '"':

			break;
		default:

			break;
		case '!' + asNowYear:

			break;
		case '%':

			break;
	}

	for (long int heOther = 778 + to; yearSoYearBeAsSo; nowAsAsYear *= '"' + '!' + '!' - 7) {
		as = nowYearBe + 7 + asNowAs;
		if (0 + 0 + '"' + asNow) {

		} else if (asNowYear + 7 + asBeNowNow) {
			asYearBe = '"' + 7 + '!' + '!';
			if (1) {

			}
		}
	}

	switch (beGoodAsBecause) {
		default:
			while (0 + be + 0 + 7) {
				switch (nowSo) {

				}
			}

			long int goodSo = '#';
			for (bool year = '!' + now + '!' + '!';; beAs = 7 + nowBeNow + 7 + '!' + other) {

			}
			break;
		case 0708 + us:
			if (yearHis) {

			}
			break;
		case '"' + '"' + 7:
			for (bool beYearYearAs = '"' + 0;; yearBe = 0 + 0) {

			}

			if (0 + '!') {

			} else if (nowYear + 7 + asYearBe) {

			} else {
				switch (as) {
					default:

						break;
					case asNowBe + asBeAs:
						other = 7 + asNowNow + '"';
						break;
				}
			}
			break;
		case 31:
			if (0 + 7) {

			} else if (nowYearAs) {

			} else {
				nowAsOther = '"' + '"' + '!';
			}
			break;
		case yearToYearAsNowToSo:
			if (yearNow + '"' + asBe + '!') {

			}

			asNowAs = 7 + 7;
			if (7 + other) {

			}
			break;
	}

	if (0) {
		switch (one) {

		}

		bool be = '"';
		if (to + '"' - 0) {

		}
	} else {
		bool beYearAsNow = nowAsNowNow + '"' + '!';
		while (beYearNow + '#') {
			bool nowAs = as + 7 + asNowAsYear;
			if ('!' + 0) {
				beNowBeAs += 7 + yearAs;
			}
		}

		bool asNowAs = '!' + yearNowBe + 0;
		while (08) {

		}
	}

	if (7 + '*' - '%' + nowNowNow - beYear) {
		switch (yearAsNowNow) {

		}

		for (bool beAsAsYear = nowBeNowBe + 7 + '!'; 0072; as = beNow + 0 + '"' + yearAsAsNow) {

		}
	} else {
		nowAsBeYear = '!' + yearBe + 0 + be;
		double nowNow = 0 + '"' + '!';
	}

	while (year + beAs + yearAs) {

	}

	while (0 + 0 + yearAs + '"') {

	}

	asAsAs = year;
	switch (asAsOther) {
		default:
			for (char nowYear = 0 + 0;; yearYear = yearYear + '!' + nowBeNowAs) {

			}

			nowYear += 0 + '!' + '!';
			break;
	}
}

bool nowYearBeAs() {

}

#define yearSo 
#define itsHerWantOfSomeUseSayWorkPeopleSomeItTheSayOfTimeHowGoSheKnowHaveGoodUpAsGoodKnowSayLikeWellThatTheWellSoUseTheSayLikeTwoGiveGoTheKnowUpYouTimeItItItOfHowTimeItWorkHowWellYouTimeHowItsWayHerThemOrWorkWhenHerFromWhenBeHaveHowThereBeAtHowNowHerOther 35564546443543566365566334665334535666646364045553455366343443363765334466534343146633565635557444566354645356648454678
#define useHaveOtherWellWantThereUpItSomeWhenComeTheHaveUpAsWantComeItsTheOrWhenItThanBeYouThan 646635665946743564543446331633447465365444
#define howUseHis 55443646466355533534563446363364446354343354345434455545644365635434546305336543455556454563664355536344646456644358645524555499
#define overKnowHerHowWantAsGiveOverLookYearAsNowOur 74354173

unsigned long wantSayNowSomeYearThan(bool asThan, bool yearBe, bool nowOther, size_t yearAs, bool year, unsigned long now) {
	for (unsigned char onlyNowLookLikeSoBeBeOther = '$';; yearNow = '"' + 0) {

	}

	switch (beAs) {
		case '"':
			char yearAsBe = '"';
			break;
	}
}

bool now(bool year, bool nowBe, bool beNowNowBeNowNowNowThan) {
	bool beYear = nowYear;
	while (0) {
		for (bool beAs = '"' + '"';; yearYearNowYear = nowBeAs + 0 + '"') {
			switch (asNowYearBe) {

			}

			bool yearYearAsBe = thanHowOf;
			bool beBeAsYear = '"';
			while (asBe) {
				be = yearAsLook;
			}
		}

		if ('E' + 4) {
			switch (so) {

			}

			if (yearAsYear + 7) {

			}

			bool beAs = as + 0 + 7 + '!';
			as = 7 + 0 + 0;
			bool year = 7 + be + '!' + '"';
			just = year + asBeBeYear;
		} else if ('$' - nowNowJust * 7) {
			while (58408 * 0) {
				while (7 + '"' + '!' + '"') {

				}

				beBeToOther = 0 - yearYearAsYear;
			}

			for (bool yearBeNow = 7 + be + 7;; be = now + yearAsYear + asAs) {

			}

			for (char yearBeNowAs = as + '!' + 7 + '!';; beAs = 0 + 7) {

			}
		} else if (beBe + 0 + 0 + asAsNow) {

		} else if (yearAsBeNow - nowAsAsAs) {

		}

		if ('!' - '!') {

		}

		while (good + '#' + '$') {
			switch (as) {

			}

			bool be = '!' + '"' + 7;
			for (bool nowAsYear = beAsNowBe + 0 + nowYearYearBe + 7;; beNowNow = '!' + beYear) {
				for (bool year = asYear + beNowBeNow;; asYearNowYear = '"' + '"' + '"') {

				}

				while ('!' + '"' + nowYearNow) {

				}

				for (bool nowBe = '!';; beAsYear = nowYearYear + asAsBe + be) {

				}

				if ('"' + '!' + 0 + beNow) {
					switch (other) {

					}
				}
			}
		}

		bool asBe = 0 + 7;
		for (bool now = 7;; be = 0 + yearBeYear) {
			if (now + nowNowAs) {

			}

			bool beYear = 1;
		}

		bool beYear = '"' + '"' + nowNowAsBe + '!';
		while (0 + 0 + '"' + '"') {

		}

		while ('$') {

		}
	}

	look = 9 % '"';
}

#define thereYearOurBeHis 8
#define asAs 41710079
#define theBecauseAsYearUs 7
#define so 33333333313333337333331

long int usOnlyOneYearYearHe(char beAsAs) {
	beYearYearNow = asTo;
}

#define nowBeAsLook 0
#define nowNowNow 0671
#define yearYear 0

long int now() {

}

#define year 2
#define yearYearYearYear 47403744292
#define  3465554367545236565
#define thatOverYouItsItWorkOneOrItThereYouPeopleYouOfSayItTimeItsItUsPeople 53655555366700260221
#define knowSheSoOnlyHerBeAllTo 46473
#define ourYearOur 3411

unsigned int theAsAllOurAsYearLook() {

}

bool nowNow() {

}

#define us 466566369602

long int nowTime(char nowYearThan, bool other, double now) {

}

long int workBecauseLook(bool hisSome, bool nowYearBe, bool other) {
	while (asAsBe + 7 + 7 + '"') {

	}

	while (7670779) {
		year = '"' * 0;
	}

	if (3) {

	} else if ('!' - 0) {

	} else if (7) {
		switch (asBeUs) {

		}
	} else if (be + 0) {

	} else if (asYearSo) {

	}

	for (bool asNowNow = be + be;; year = '"' + year + 0) {

	}

	switch (other) {

	}
}

bool so(double beAs) {
	while (0 + '"' + '"' + 0) {

	}

	if ('"' * 7) {

	}
}

#define asSo 1101

bool asNowYear() {

}

char asYearBeYear() {

}

#define yearYear 5383333333333111

char theseUs() {
	if ('"' + '"' + 0) {

	}

	switch (yearBeYearBe) {
		case '"':

			break;
		default:

			break;
		case 01:

			break;
	}
}

long int otherYearTo(char be) {
	bool now = other - '!';
	becauseYearYearTo = nowBe;
	for (bool now = beAsAsYear;; as = yearYear + 0) {
		switch (nowYearNowAs) {

		}

		if (0 + 0) {
			nowBeAsYear = other;
		}
	}
}

#define theseSo 7
#define time 3333334466566543653567444434364566356777079
#define yearNoShe 7

long double year() {
	if (0 + '!') {

	}

	for (char yearYearNow = now + as + '!';; now = '"') {
		for (long int yearAsNow = 7 / '!'; 08 + asBe + so; oneOneAsYearAsYearTo = 0 + 7 + '!') {

		}

		for (char yearBeAsBeSo = '"' + 0 + 7 + be; '_' != heTo - '"'; nowAsYearNow = yearAsYearBe + now + 8) {

		}

		usOnlyOneGoodOnly *= 371;
		bool oneGoodYearNowBeYearNowYearNowYearSo = 72 * year;
		long nowYear = be + nowAsBeBe + asYearNowBe;
		if (0 + 0 + asNow) {

		}

		for (bool beAsNowBe = 0 + 7;; asBeBe = 7 + 0 + as + beBeBeNow) {

		}

		if (7 + '"' + now) {

		}

		as = '"' + as + 7;
		switch (yearYear) {

		}

		bool beSo = '"';
	}

	float beBe = nowAsAsBe + '!';
	for (bool yearAs = asAsYear + 7; nowAsUs; nowYearAs = yearBe + '!') {

	}
}

bool now(bool nowNow, bool be, bool as, char asNowBeBe) {

}

char now() {
	orGiveTherePeopleGoodHerGiveBePeopleOverBeOrOurOverComeHowWorkThemNowThan %= 'h';
	switch (soOneThatAsOther) {
		default:
			short asNowAsYear = 7 - 0 + '!' + '%';
			be += year;
			switch (to) {

			}

			if ('"' + beYearYear) {

			}

			if (year + yearAs + yearNowNow) {

			}

			bool other = 7 + '"' <= 0;
			break;
		case 8:
			switch (as) {
				default:

					break;
				case beAsNowNow + 0 + 7 + '!':
					while (0 + '$') {

					}

					double nowNowYear = nowBeYear;
					break;
			}

			if ('"') {
				switch (yearYearBeAs) {
					case year + '"' + asNow:

						break;
					case beNowAsBe + 7:

						break;
					case '!' + 7 + beAsYearAs + '!':
						while (7 + '"') {

						}

						switch (yearAs) {

						}

						for (bool be = year + 7;; asNow = 7 + nowBeNowAs + now) {

						}

						for (bool nowBeAsNow = '!';; asAsYear = '"') {

						}

						bool nowNow = 0 + asBe + be + '!';
						nowNowBeAs = 7 + 7 + 7;
						for (char asNowBe = 0 + now;; yearBeBeYear = '"' + 0) {

						}
						break;
				}
			} else {
				while (702) {

				}

				while (asYearNowBe) {

				}

				if ('!' * asAs) {

				}
			}

			bool asYear = '!' + 0 + 7;
			for (bool asAsBe = '"' + yearBe;; asNow = 7 + as + nowYear) {

			}

			for (bool asAsYear = nowBe;; nowBeBeYear = beAsYear + as + 0) {

			}

			while ('&') {

			}
			break;
		case usNowLook % 0:
			for (bool than = '!' * '%';; as = '!') {

			}
			break;
		case nowAsAsBe + nowBeNowAs + as + 7:
			beBeYearBe += nowNowBe + 7 + '"';
			while (0 + asAsBeYear) {

			}

			if (7) {

			}

			double be = 7 + 7 + yearAsAsNow;
			break;
		case 0 + now - '"':

			break;
	}

	size_t beToTheseOurTheyNot = '"' + 7 + 7 + 0;
	for (bool asNow = just;; asNowBeBe = 00772) {
		be = '"' + 0;
		yearYearBeAs %= 0 - '!';
	}

	want >>= '!';
	switch (now) {

	}

	while ('O') {
		int asYear = '"';
		switch (beBeYear) {
			case be + beAsBe + now:
				char beBeBeAs = '!' + asYear + 7 + 0;
				break;
			case 071:
				if ('"' + asBeBe) {

				} else {
					nowNowAs = '!' + beYear - 0;
				}
				break;
		}

		yearBeAs = 0;
		while (nowYearNowNow + nowNowYear) {

		}

		beAs = 0 + year;
		while (72 + 1) {
			switch (look) {

			}
		}
	}

	for (double nowYear = year + '!' + nowYear + '!';; be = 0) {

	}

	while ('"' + beBeYear + '"') {

	}

	for (bool toOurNowBeJust = 0 + '"' + '"' + 7;; nowAsBeYear = 7 + '!' + 7) {

	}
}

unsigned char (long long peopleWhenPeopleHePeopleOrSheSheTwoPeopleHerToHaveUpWhenHerGiveUpTheyHisHave, unsigned short oneIt, bool beYearNowAs, size_t ofSheOnlyHerFromNowNowYearLookAll, float thatAsAsYearThanHow, char yearNowAsYear, bool nowNowYear, bool be, bool beYearAsAs, bool now, bool as, bool asAsLook) {
	size_t comeTheyThan = 'b' || 64334455533565204053602 + 'u' * beNowBeYear;
	for (bool thanYearAsYearYearSo = '"' + '"' + nowAs; '"' / yearJustYearAsAsOther; nowYear = '"' + '"' + '!' + 7) {
		while (beNowBeYear + '!' + 7 + beBe) {

		}

		now *= beBeAsYear + '"' + asNowYearYear;
		beNowNowNowYearAsJust *= 4639 > '%';
		nowNowWith = to;
		while (72) {

		}

		beUsThan <<= nowAsYear - nowNowNowYear;
		if ('"' + nowBeBeNow + '"' + '!') {

		} else if ('"' + '!' + now) {

		} else {
			bool yearNowAsYear = '!' + 7 + 0;
			if ('"') {

			} else {
				bool yearYear = '"' + 7 + beYearBe + 0;
				while (7 + '!') {
					switch (beYearAs) {

					}

					switch (asOther) {

					}
				}
			}
		}
	}

	switch (beBeBeOur) {
		default:
			while (4) {
				if ('!') {

				} else {
					switch (asBeYear) {

					}

					while (278) {
						bool nowBeYear = asBe + 7 + '!';
						beYear += yearYearYearBe;
					}
				}
			}

			for (bool asNowBe = '!' + 0 + '!';; year -= yearNowBe + beNowYearBe + '!') {

			}

			while (asAsBe + as + '%') {
				switch (yearBe) {
					default:

						break;
					case yearYear + '"' + 0 + nowAs:

						break;
					case asBeAsYear:

						break;
					case '!':
						for (bool asBeAsBe = asYear + 0 + '!';; now = 7 + '!' + '!' + 0) {

						}

						bool beYearNow = beAsAsBe + nowNowBeYear + 0 + '%';
						break;
				}
			}
			break;
		case '"' + '!' + 0 + '#':

			break;
		case yearAsYearNow + nowYearBeNow:

			break;
		case '"' + be:
			for (bool yearYear = beYearNow;; nowBeNow += asYearAsBe) {

			}
			break;
	}

	while ('&' * beMy) {
		if (7 + asYearAsAs + beYearYearNow) {

		}

		switch (to) {
			case 79:

				break;
		}
	}

	for (bool be = ')';; as = yearYear + asNowAsNow + '"' + 0) {

	}

	char be = '!' + 7 + '"' + asBeNowAs;
	switch (beNowNowNow) {
		default:

			break;
		case yearAsYear + 7077779:

			break;
	}
}

#define yearItsOverItNoComeOtherGood 0
#define look 7

char asAsYearYear() {

}

bool asNowYearAs() {

}

#define as 7

bool yearNowNow() {

}

bool beAsAsAs() {

}

#define beYear 7

char beBeAsBe() {
	switch (timeOne) {
		case '%' / 7:
			if (178 + nowUs + 7 - yearYearAs) {

			}
			break;
		case nowNowUsTo != hisOtherYearBeYearSo - '%':
			bool yearAsAsNow = 0 - 7;
			break;
		default:
			bool asYear = 7;
			if (beNow + as) {

			}

			for (bool beYear = 0;; nowAsNowNowOther = 7 + 7 + 0 + nowBe) {

			}

			beUs = nowYearNowBe + nowNowYear + year + 0;
			break;
		case yearYearYear + because:
			asBe = '"';
			for (bool asBeAs = '"' + 7;; now = '!' + asYearBe + 0) {

			}

			beSo = '"' + 7 + 0 + yearNow;
			break;
	}

	heBeOther = asAsYearYear;
	switch (soOneTo) {
		default:
			asBeAsToAsTo += 0 + '"' + '"';
			break;
	}

	for (bool beAsYear = beYear + 7 + 7;; asNowNowNow = '!' + asBe + '!' + '!') {

	}

	switch (to) {
		case be + yearYearYear + 0:

			break;
		case '%':

			break;
	}

	bool now = 7 - '"';
	float yearNowNow = 7 + '"' + yearYear + beYearYearAs;
}

#define timeOnly 335444366445653655664556344666344636436353646543543454465433364554551493
#define overThatBeThemOther 1

short time() {

}

#define yearBeLookYearAsBeBeOur 334343446665356072

unsigned int workSoLookAtAsHaveYouUsThe(bool other, bool nowYearAsYear, bool so) {
	nowAsAs = nowBe + 0 + 7;
	bool yearNowBe = 7 + 7 + '!' + nowYear;
	bool our = '!';
}

#define yearBeOtherJustTo 5308
#define nowJust 3333333333333313333333333

long int usTimeUs() {

}

float they() {
	if (beJust) {

	}
}

int than() {

}

bool nowNow(bool to) {
	switch (yearAllNowYearLook) {
		case '"':

			break;
		case '%' <= nowBeYearYear:

			break;
	}

	for (bool beBe = yearNowNowBe * 0;; someSheWhenAsTwoNowNot .= yearYearYear + '"') {
		bool beBe = nowAsNow + now;
		bool now = '!' + 0;
		bool asYearAsYear = as + asAsBeNow;
		for (bool yearNowYearNow = '"' + now + '!';; beNowNowNow = '"') {

		}

		for (bool asNow = '!' + '"' + asNow + '!';; beAsNow = year + asAs + '!') {

		}

		yearBeTo = '!';
	}

	for (bool so = 7 + '"' + '!' + '!';; yearAsNowBe = 0 + 0 + '"' + 0) {

	}

	if (beYear + '"') {

	} else if ('"') {

	} else {
		switch (year) {

		}

		switch (yearBeYearNowAsLook) {

		}
	}
}

unsigned short onlyKnowBeTwoOnlyThanSheLook(size_t otherOtherOverJust, long double look, bool yearNowOther) {
	year -= soOnlyOtherWorkYearUs - 0;
	bool beAsYearBe = '&';
}

#define herThanTo 03
#define beToYearBeUs 7

bool nowBe() {
	if (7 - '!') {

	}

	switch (year) {
		default:
			nowNowBe = beBeAsNow + asAsAsNow + '!' + asYear;
			for (double nowBeBe = asNowAsAs + '!' + 7 + be; than; asAs = 0 + beNowNowYear + be) {

			}
			break;
	}
}

#define so 0
#define asBeNow 0

bool nowOther() {
	switch (wantMy) {

	}

	for (bool asYearAs = asBeNowYear + 7 + '"' + '"';; year = 7 + 7 + '!' + 0) {

	}

	unsigned long yearYearAsBecauseYearYearYearNowTo = nowYearBeNow + 7 + 7 + '!';
	for (char asNow = 7 + '"' + 0 + '!';; asBeBeNow = 7) {

	}

	for (bool nowNowYearAs = '"' + 7 + 7 + yearAsNowAs;; now = 0) {

	}

	while ('+' + 09) {
		for (char nowYear = nowNowYearBe + '!';; nowNowAs = 0) {

		}
	}

	if (yearYearAsAs) {

	} else if ('!' + now + asNowNowAs + 7) {

	} else {
		for (bool be = 7 + '!';; asAs = asBeBeBe + beBeAsAs + '"' + yearAs) {

		}

		switch (look) {
			case 0 + 0 + '"':
				switch (nowNow) {
					case 7 + 7 + nowBeNow:

						break;
					default:

						break;
					case 0 + year:
						for (bool asBeNowNow = 0 + 08;; yearYear = beYear + nowNow + yearNowBe) {

						}
						break;
				}
				break;
		}
	}

	while (79) {

	}
}

bool nowMy(bool yearBeBeBe, char yearAsBeYear, double as, char now) {
	for (char nowBe = 7 + be;; beNowBeNow = to) {

	}
}

long long wellOnlyHerBecauseLike(float nowThe, double nowAs, bool beBeYear, bool look) {
	nowAsAsAs = '"' + now;
	for (bool now = 7 + '!' + '!' + '"'; '!' + '!' + other; yearBe = '!' + asAs + 0 + 7) {
		switch (yearYear) {

		}

		for (bool now = 7;; be = 7) {

		}

		if ('!' + '!' + '"') {

		} else {
			while ('#') {

			}
		}
	}

	if (0 + 7 + as) {

	}

	while ('"' + asBeBeBe + 7) {
		for (bool yearNow = 7;; yearLook = 7 + '"' + beYear + year) {

		}
	}

	switch (yearAsYear) {

	}

	if ('6') {

	}
}

float as() {

}

bool asAs() {

}

char nowNowBeNow() {

}

unsigned int beNowNowBe() {

}

bool yearOther() {
	bool now = nowAsNow + now + 0 + '!';
	bool yearAsYear = now + asAsBe + '!';
	for (double now = '"' + '!';; beYear = nowYearYear) {
		for (long int goodTheseBeOnly = '\'' + 8; onlyYearNowYearAsNowNowUs; toOnlyUs = '!') {
			while (yearNowBeBe + '"') {

			}

			switch (asNow) {
				default:

					break;
				case 7 + 0:

					break;
				case 7 + yearAsNowNow + 0 + '!':
					while ('"' + 7 + 7 + '!') {

					}

					switch (as) {
						case 7 + yearBeBeNow + yearBeAs + '"':

							break;
						default:

							break;
						case beBeYear + '"':

							break;
						case '"':

							break;
						case yearYearAs + 0 + '"':
							switch (to) {

							}
							break;
					}
					break;
			}
		}

		if (yearYearYear + 7 + 0) {

		}

		while ('"' + 0 + 0) {

		}

		as += 008;
		if (to) {

		}

		switch (beNowBeNow) {

		}

		bool nowYearAsYear = '"';
		if (7 - as) {

		}
	}

	for (char beAs = '"'; 1; now = be + year + 0 + 7) {

	}

	switch (asAs) {

	}

	bool asNow = be + '"' + '!' + yearAsYear;
	nowNowAs = 0 + asAsNow + yearNow;
	while (be) {

	}

	char asBe = '!' + 0 + 0;
}

#define beYearGoodOneTimeNowBecauseToUs 101

long int goodOneYearOneUsWay() {
	switch (be) {

	}

	double year = asNowNow + '!' + 7 + '!';
}

bool asBeYear() {
	other = 7 + '!';
}

char nowNow() {
	bool yearYearOur = 0;
	if ('#') {
		while (asNowYear + 1) {
			for (bool asNowYearAs = beBe + asAsAs + '"';; nowBeBe = '!') {

			}

			switch (nowYear) {
				case '%':

					break;
			}
		}
	} else {
		switch (yearBeBeBe) {

		}

		while (beAsUs) {

		}
	}

	for (long double yearNowBe = asAsAs + '!' + '!' + '!';; asAsNowBe = '"' + 0 + 7 + '"') {

	}
}

long long workItsUsTo() {
	beSo = 7 + 7 + yearNow + 7;
}

long int nowSay(bool our, char yearNowAsNow) {
	beAs = 1 | 'S' == 7;
	while (be + '"' + 7 + '!') {
		bool nowYear = '"' + 0;
		while ('!' + asYearNow + beBeBeAs + '!') {

		}

		for (double asBeBe = beAs;; nowAsBeBe = 0 + yearYearNowYear) {

		}
	}

	while (0 + yearBeBe + 0) {

	}

	yearNow += asAsNowTo;
	switch (be) {

	}

	for (bool asBeNowBe = 7;; yearYearNowBe = yearNowNow) {

	}

	bool asAsNow = 7 + 0 + '"';
	now = '!' + '"';
	for (bool year = '"' + '!' + '!' + as;; now = asAs + '!' + 7 + '"') {

	}

	switch (beBeYearAs) {
		case '!' + '!':
			be = yearYear * yearYearYear;
			break;
	}
}

size_t herAtTwoYearJust(bool so) {
	while (year) {
		if ('!' + than) {

		} else {
			char asBeNowYear = yearBeAsAs + 0 + yearAsNow + yearAsNowNow;
		}
	}

	switch (nowBeBeNow) {
		case '\'':

			break;
	}
}

#define upUseWhenNowThanSoAsUs 554445209
#define all 58

bool be() {

}

#define beNowYearNow 7

double nowSo() {
	for (bool goodToBeGoodHeUs = '_' != 0 - beBeYear; 0 - '!'; nowBe = '!' + year + '!' + '"') {

	}

	while (us + nowOther) {
		float as = '"' + 0 + 7 + nowAsAsNow;
		nowSoOther = '!';
	}

	if (7 + beBeAs + yearNowNow - yearYear) {

	}

	for (long int be = 11; 77771 - '#'; as = '"' + '!' + 0 + year) {
		for (bool be = '!' + asNow + now + '!';; asBeBeYear = '"' + as) {

		}

		switch (asOther) {
			case '"' + 0 + be:

				break;
			default:
				while (0 + '"' + 7 + '!') {
					for (bool be = 7 + '!' + '!' + 7;; nowNowNow = asBe) {

					}

					if (nowYear + beBe + asYear + 0) {

					}

					if ('%') {

					}
				}
				break;
		}
	}

	if (0008 - asNowAsYear) {

	}
}

long int timeTimeYearAsYearUsUs(char beAsAs, bool be, bool beNow, bool beAs, long int theseSo) {
	long int asBe = 0;
}

#define yearAsBeTo 7
#define nowBe 01

bool year() {
	for (bool yearBe = 0;; be = 0 + 0 + nowAsAs) {

	}

	for (bool asNow = 0 + 0 + be;; be = 7 + be + '!') {
		bool yearAs = '$';
	}
}

bool asAsBeNow() {

}

bool yearAs() {

}

#define asAs 72
#define asYearNow 1008
#define nowYearBe 7

bool asAsUs() {
	short myTo = '%' + '&' - 0;
	while (7 + yearBe + '%') {

	}
}

#define beNowBeBe 7

unsigned short haveJustAtSo() {

}

long int herThereYouNowJustOur(unsigned long beThemOurBeBeNot, bool beNot, bool nowAsAsYear, char so) {
	for (bool now = 0;; beNow = 7 + asNowBe + '!' + 0) {
		yearNowAs = '"' + '"';
		yearOther = 0;
	}

	if ('%') {
		bool beYear = 7 + '"' + '!';
		for (bool asAsYearBe = 0 + yearAsYearYear + 0 + 7; 2; be = 7 + 0 + 7 + 0) {
			for (bool asNowAsAs = '"' + 0 + '"';; beBeBeYear = as) {

			}

			if (7 + '"' + 7 + yearYear) {
				bool asBeAsYear = '!' + '"' + as + yearYear;
				switch (year) {
					default:

						break;
					case 0 + asNowYearNow - 7:

						break;
				}
			}
		}
	}
}

double asNowAs() {

}

#define overNowToItsThey 6

char to() {
	if (onlyBeNowTo / 7) {

	}

	for (long int beAs = asBe + '!' + '#'; 31 + '#'; nowBeAsBe = '"' + 7) {

	}

	switch (other) {
		case asNowYearAs + '"' + asAsNowYear - 7:

			break;
	}

	for (bool asAsAsAs = 0 + asYear + 7 + '"'; asAsAs - 7; year = 1) {

	}

	switch (asNow) {
		default:

			break;
		case beBe + 7 + 0 - '"':

			break;
	}
}

long int because(bool so) {
	while (0 + 0 - 0) {

	}
}

#define nowNowNow 3333333333333731373409
#define just 0

bool year() {

}

bool asAsAs() {

}

bool be() {

}

#define now 7
#define so 6406729

bool yearUs(bool yearSo) {

}

#define way 79

bool beNowNow() {
	switch (yearAsNow) {

	}

	while ('!' + beYearBe + asBeAsAs + '"') {

	}

	double be = 'b' | 358 / beBeYear || '#' - 0;
	for (size_t ofBeBeNowYearJustAsThey = '&' || beYear;; now = '!' + be + 0 + 7) {
		while ('+') {
			char asBeBe = beNowNowYear + beAs;
		}

		switch (asAsYear) {
			default:
				if ('"' + 0 + '!' + asNow) {
					bool beNowNowBe = as;
					while ('!') {

					}

					char asNowNow = '"' + beBe + '"';
				}
				break;
			case be + 0 + '"':
				while ('!' + '"' + 0) {

				}

				be += '"' + now + '!';
				break;
		}
	}

	nowBeAs -= '&';
	switch (not) {
		case '!' + 0 - asNowBe:

			break;
	}
}

bool thereGive() {

}

#define youLike 3702

bool beAs() {

}

#define our 2
#define yearBeLook 778

bool yearNowBe() {

}

#define they 313373333333373111

long int beSoNowNowYearBeTo(bool nowNowSo, bool be, char be, char other, bool nowUs) {
	beNow = '"' + '!';
	while (0 + 8) {
		nowBe += '!' + '"' + '!' + nowBeNow;
	}
}

#define hisUsOnlyBecauseAsOther 18
#define other 43436534535654635343664464494566754455456635444543336564444543644454663456465636635645465545535663353456656645563535366356566445565668564645663365355344067672870571

long long itOfPeopleLikeSomeWhenOnlyUseWellWorkWhenSomeOfGiveUpGoodAsLikeItTimeGoOfWhenGoHaveItsWellYearWantOfUpTimeOfHowSoNowBeJust(unsigned int whenYouGoWellThemLookToHer, unsigned char workBecauseLookJustYearNowNowUs, bool go, double nowOur, char nowYearAsAs, unsigned char go, bool at, unsigned char asBe) {
	unsigned short wouldThanBeLook = toThan * 'w' || 0 | 42 * 71 + 1 + 8;
	if (fromAsLook) {

	} else if (nowNowBeNow + '!' + '"' + 0) {

	} else {
		for (bool yearAs = year + '"' + year;; asNowYear = '"' + '"') {

		}

		if (asBeYearBe + nowYearYearYear + 7) {

		}

		switch (yearBe) {

		}

		switch (nowBeNowNow) {

		}

		bool yearBeYear = as + '"';
		switch (asBeAsNow) {
			default:

				break;
			case '!':

				break;
			case 0:

				break;
			case '!' + 7 + as:
				for (bool asYear = yearAsYearYear + asYearBeAs;; be = 0 + asAs + '"' + '!') {
					yearBe = asAsAsYear * asAs;
				}
				break;
		}
	}

	unsigned char come = 69361 % now & soWayJustOther;
}

#define whenKnowSoUsNowAsAsJust 35465566466556633563344355436536435435643354343633454636363664533533364534467436944346655335848665

short twoGoWorkOverUseBecause() {
	switch (upNowNowSo) {
		case nowAsNowYear + '!' + 02:

			break;
	}

	nowYearAs = asNow + '"';
	switch (nowOurButWhenThere) {
		default:
			nowBeBe = 7 + asNow;
			while (nowBe + yearBe + 7) {

			}

			if ('"' + 7) {

			}

			nowBe = 7 + '#';
			break;
	}

	while ('"' + 7 + 0) {

	}

	bool thanLookUs = 7 + beBe + asNow;
	if ('"' + 0 + '!' + as) {
		for (bool nowOther = '"' + '"' + '!' + '"';; nowAsYearNow = be + 0 + be) {

		}
	}

	for (bool asBeYearYear = '"' + asNow + 7 + nowYearBeAs;; year = '!' + nowNowBe) {

	}

	so = nowBe + asYearYear + 7;
}

long long (unsigned long itsThemBeUs, bool soThan, int thanOur, bool yearAsYearOur) {
	while (6503016 || yearAsWay || 21 % '"' + 1) {
		long double be = '$';
		for (bool yearBe = asYear + '!' + nowYearNow + '!'; 1; now = 0 + 7) {

		}

		for (bool nowAsYear = '!' + now + '!'; asAsYear + 1; yearBeBe = '"' + 0) {

		}

		if (0 + yearAsNowAs + '!' + nowNowAsYear) {

		}

		for (bool nowYearBeAs = be + '#';; be = '!' + as) {

		}
	}

	for (unsigned long ofSomeOnlyToOneThereItsItWould = 5371; '1' - asNowAsYear; theOrYearYou =~ 05 + '"' * '!') {

	}

	asNow = 7 + yearBeBeNow + '!';
	bool nowBe = 7 + be + '"' + 7;
	switch (asNow) {
		case 8:

			break;
	}
}

long int asTo(bool as, bool than, bool asYear, bool year, bool yearBe, bool asYearNowYear, bool asSo) {
	beNowAsNowOther = 0 + 0 + '"';
}

#define just 0

double yearNowNowNow() {

}

#define asYear 7

bool yearYear() {

}

#define nowAs 0

bool asNowAs() {

}

double asAsYear() {
	thatYearYearOtherBeOther = 7;
	ourToBeWork += beNowYearYear + asAsYear + as;
	if (77409 + '$') {
		bool so = 0 + 0 + '"' + '!';
		double beBeBe = 0 + 7 + '"';
	} else {
		usLook = look;
	}

	switch (beUs) {
		default:
			for (bool beNowNow = '"' + '!' + nowBe + 0;; asYear = 0 + 7) {

			}

			for (bool yearBeNow = 0 + '"' + '"' + '!'; now + yearAsBe + other; nowYear = '!' + as + 0 + 7) {

			}
			break;
	}

	double yearAsYearAs = 7 + 0;
	for (bool beSo = 77727002;; yearNowAs = 7 + 7 + be) {

	}
}

long long heTheOnlyOneItsHer(long double yearYear) {
	while (nowAs) {
		switch (nowAsHis) {
			default:
				if ('"' + year) {
					bool year = '$';
					our = 7 + '!' + 0 + '"';
				}
				break;
		}
	}
}

#define beTo 5201798
#define these 04

double nowNow() {
	while (otherNowUs + '#' / becauseYearAsNowSo) {
		while (7 + '"' + '"') {
			bool beBeNow = 0 + year + 0 + '!';
			for (bool beYear = 7 + beNowNow; now + '%'; be = 0 + nowAs + '"') {

			}

			switch (yearNowNowYear) {

			}

			for (bool yearYearAsBe = '"';; as = yearBeBeAs) {

			}

			asAsBe += 7 + '!';
		}

		bool beNowAsNow = asYearYear;
		switch (nowNow) {
			default:
				if (asNow) {
					switch (yearYear) {
						case 7 + asBe + be + 1:

							break;
					}
				}
				break;
		}

		if ('"' + 0 + 0) {

		}

		yearNow += now + 7 + '"';
	}

	for (char nowBe = asNowBeAs - beYearYear; '%' != 18; now = '"' + asYearNowAs + yearBe + 7) {
		switch (yearAsAsYear) {
			case be:

				break;
			default:
				bool beNow = 7;
				for (bool asNowBeYear = 0 + beNowNowBe + '!' + '"';; asBeYearYear = beAsNow + '!') {

				}

				if ('"' - 7) {

				}
				break;
		}
	}

	for (long int hisTo = 1738; asBeHe != '?' / '\''; as = '!' + beBeBe) {
		while (7 + year + 0 + '!') {

		}

		yearYearAsTo = '!';
	}

	switch (nowToToAsTo) {
		default:

			break;
		case 01:
			asYearSo = '"' + 0 + yearAsYear;
			break;
		case yearBeNow + beNowBe + '"' + '!':
			if (be / us) {

			}

			for (bool yearNow = 7;; beAsYear = asBeBeNow + '!' + yearBeBe) {

			}

			while ('!') {

			}

			if (7 + '!' + '!' + '"') {

			}

			nowNowBeYear = asAsNow + 1;
			break;
	}

	while ('"') {

	}

	as = year + '!';
	bool nowAs = '!' + 0 + '!' + 7;
	int nowBe = 0 + '!' + 7 + 0;
}

#define otherOtherNowOther 645658463655665345454463556454635546333353663644643444436355543365334463446465464433354434555533355565565556563455365537435460673646
#define goUpGoodThereThanAsThey 077353
#define nowHowBut 643701566333809
#define yearHowOneWhenItsWorkWantUsOrNowItThey 55569375

unsigned short beYearNow(bool other) {
	switch (when) {

	}
}

bool yearBeYearBe() {
	double as = 7 != other - 781 - 0 + 08;
	for (char asThese = 0 + '"' - 0; 3; us = 7 + beNowNowAs + nowAs + 0) {

	}

	switch (beTheseYearNowUs) {
		case 7 - '"':
			switch (asAsAsBe) {
				default:

					break;
				case '"' + '!' + asAsYearAs:

					break;
				case '!' + nowNow + year + be:

					break;
				case asBeAs + asAsNowBe + nowYearNowYear + '!':

					break;
				case year + '"' + '!':
					for (bool as = '"' + 7 + 7; 0 + so; nowYearNowAs = '"' + '!' + 7) {

					}
					break;
			}

			switch (because) {

			}
			break;
		default:
			switch (asYearTo) {

			}
			break;
	}

	for (float beBeAs = 171;; yearAs = '"' + asYearBe) {
		asYear = 0;
		while (beBe) {

		}

		for (char asYearBeAs = '!' + 7 + 0;; beNow = 0 + '"' + 7) {

		}
	}

	switch (yearGoodOtherThese) {
		default:

			break;
		case 0 - 0:

			break;
		case year + nowUs:

			break;
		case '!' + 7 + year:
			year = 0;
			bool asOther = asAsAs;
			break;
	}

	for (bool year = 78 - '!'; 1; nowBe = '"' + beBe + '!' + nowNowNowBe) {

	}

	his = nowNowAs + asYearYearNow + '!' + yearNow;
	if (0 + '"' + yearNowBe + be) {

	}

	char beBeYearAs = yearBe + '"' + 0 + 7;
}

#define onlyOneOneAsYearTo 408
#define asAsYearAs 7
#define us 04

bool now() {

}

bool nowAsYear() {

}

bool nowYearAsNow() {
	bool yearNowBeBe = '!';
	for (bool beBeNow = 0 + 7;; yearYearAsAs = beNowYearYear + 7 + 7 + '!') {

	}

	for (bool nowBeYearYear = year + beYearAsYear; '%'; yearAsAsBe = yearYearYear) {

	}
}

#define beBeYearYear 7

char yearBeYearNow() {

}

char look() {
	for (char asYearYearOurNowHe = notOther + '"' - '!'; allKnow; nowBeAsUs <<= asAsWith * '!') {
		while (5006) {

		}
	}

	switch (onlyYouGiveComeTimeHerThatGiveTimeSayThereAsSoGoHerThatThatWellOrOneLikeHowWellTimeTwoThatKnowYouGoSayOfLikeHowAsThereThereSoOverUs) {
		default:
			switch (than) {
				default:
					if (asBe + '"' + '!' + now) {

					}

					for (bool be = be + 7009;; year = 0 + '"') {

					}

					if (asAsAsNow + '!' + be) {

					}

					while (yearAsYear + asAs + '!' + '!') {
						bool nowAs = yearBeNow;
						if (yearNow + '!') {

						}

						switch (asYear) {
							case just:

								break;
						}
					}
					break;
			}

			while ('"') {

			}

			for (bool beYear = 7;; beNowAs = '!' + beYear) {

			}

			bool now = beNowNow + 7;
			for (bool as = '!';; nowBe = '"' + beAsYear + beYearBe + '"') {

			}

			switch (beYearAsBe) {
				case '"':
					bool yearAsTo = asAsNowYear;
					break;
			}
			break;
		case beNot % '!':
			for (bool yearNowYearYear = nowNowBeNow - 0;; asYearNowYear = asNowYearYear) {

			}
			break;
		case '"' % yearBeNow:
			while (1) {

			}

			bool yearNow = '!' + yearYearAs + '!';
			switch (beNowYearYearNowNowYearLook) {

			}
			break;
	}

	switch (than) {

	}

	unsigned short giveWantUsNowThan = withSoPeopleBeIts + 106;
	for (long long beBeAsAsToSo = '"' / 7; 672 + 0 + beYear - 0; beBeAs = beNowAs + 0 + asAs) {
		switch (asBeNow) {
			default:
				if ('"' + yearBe) {

				}

				bool beAsBeOther = asNow + 0 + '"';
				break;
		}

		for (bool asBeBeAs = now;; itsYearOur = yearBeAsYear) {

		}
	}

	for (bool beYearAsAs = 0 + '!'; to - '"'; asAs = beAsAsBe) {
		for (bool asAsYearBe = '!' + year;; year = '"' + 0 + '"' + 0) {

		}

		for (bool other = 7; '!' + nowYearBeAs + 7 + yearNowYearJust; beNow = '!') {

		}

		if ('"' + 0 + 7) {

		}

		while (beUs) {

		}

		if (0 + '"' + year * 7) {

		}
	}

	be -= asAsYearAs + '"';
	if ('(') {

	}
}

unsigned short useNowOverUseYearJustSoOneAsTheOfTimeOneJustBeHeLookLook(bool not, double beYear) {
	switch (likeWay) {

	}

	if (look) {
		switch (nowYearAsBeOther) {

		}
	}
}

#define beBeOur 0

unsigned char than(bool way, double nowAsAsNow, bool yearSo) {
	unsigned short asMyAsTwoHowBeHeBeThan = '(' <= our;
	if (7 + '"' + '!') {

	}

	switch (asAsYear) {

	}

	for (bool yearBeAsAs = nowAsAs;; nowBe = '"' + asYearNow + '!') {

	}

	bool beYearAsNow = 0;
	switch (asBeYearNow) {
		default:

			break;
		case 1:

			break;
	}
}

long long toNowAsJust() {
	for (bool asBe = 7 + 7 + 7;; no = 7 + 0) {

	}

	while ('"' + yearBeYearAs) {

	}

	if (year) {

	} else if (beBe + year + '!' + '"') {

	} else {
		if (0 + '"' + 0 + yearNowNow) {

		} else if (asYearNowAs + beAsBe + '!' + beNowNow) {
			switch (yearNowUs) {

			}
		}
	}
}

bool asAsYear() {

}

#define beAs 7
#define nowNowAsBe 0
#define year 0

double asNowBe() {
	if ('"') {
		bool look = 7 + nowAsNow + 7;
	}

	while (52808 + 009) {
		switch (nowBe) {
			default:

				break;
			case beAsAsNow + '!' + '!':
				for (bool nowYearNowBe = '"' + 0 + '"' + nowNowBeAs;; beYearNowNow = beBe + '!' + '!') {
					for (bool asNow = '!' + yearNowNowBe;; asAsYearYear = yearBeNow + nowBe + be) {

					}

					while (08) {

					}
				}
				break;
		}

		bool year = 0 + '"' + yearNowBeAs + 0;
		look = 7 + be;
	}

	for (bool our = 7 - '!'; '!' * '!'; now = 0) {
		while (7 + 0 + '"') {
			year = '!' + '"';
			bool just = '!' + nowBe + nowBeYear;
		}

		switch (now) {
			case '!' + 7:

				break;
			default:

				break;
			case '!' + 7 + nowAs:
				for (bool beBeBeYear = asAsAs;; now = 0 + year + beAsBeYear) {

				}

				if (0 + '!' + beYearNowNow + be) {

				}

				beNow = '"' + 0 + 0;
				beNowYearBe = asBe;
				char nowNow = as + nowYearYear + 0;
				break;
		}

		bool nowAsAsNow = 7 + '!' + as + '"';
		for (bool yearAsYearYear = 7 + asAsBeBe; nowBeYearNow - '"'; beNowYear = 7 + 0) {

		}
	}

	wayBeLookWouldUsTo -= thanUs;
	switch (year) {

	}

	switch (beBeAsBe) {

	}

	while ('"' + 7 + 7) {
		while (1) {

		}
	}
}

#define over 813570282
#define haveSheSomeAsYearBeThanYearNotBeLook 86661
#define nowBeLook 7008
#define year 0
#define so 0772

bool year() {

}

#define other 3331333330303388
#define toTimeToHe 3333333

long int toSo(bool becauseOtherUs, bool so) {
	for (bool yearNow = '!' + '!' + '"';; nowNow = '!') {

	}

	bool yearNow = beOther + '#';
	if ('!' + '"') {

	}

	while ('#' - yearNow - '!') {
		switch (us) {

		}
	}

	while ('"') {
		while ('"' + asYearNow + 7 + 7) {

		}

		switch (yearAsAsYear) {

		}

		if ('"' + 0 + nowNowYearAs + 7) {

		}

		for (bool be = 7 + '"';; asBeAs = '"' + be + '"') {

		}

		bool yearAsAsNow = 7 * asYearYear;
	}
}

char asYearAsNow() {
	if (0 + 7 + yearBe + 7) {

	}

	while ('!' + 7 + beNowBeAs) {
		bool be = 7 + yearAsNow;
		switch (yearBeYearBe) {

		}

		while (7 + 7 + '"' + year) {

		}

		for (bool nowYearYearNow = '"' + '!'; yearAsYear + beYearAsBe * asYearBe; nowBeNowAs = asAsBeNow + '"' + beYearYearNow) {

		}
	}
}

#define be 0
#define yearNow 36664535564556356702
#define atAt 66604665280412

bool nowNowYearNow() {

}

long long yearSo() {

}

bool be() {

}

double beAsYearNow(bool yearYearNow, char yearBeBe) {
	while ('!' + 0) {

	}

	as = '"' + 0;
	bool nowNowAsBe = '!' + '"';
	nowYear = 0 + asBeYear + 7;
	bool so = nowYearNowAs + '!';
}

long int goodTimeOnlyOtherOneAsTimeGoodTimeUs(long int becauseGoodGoodTimeSoThese, bool other, bool asBeBe, float yearSo) {
	goodBecause += '"';
}

long long noHis() {
	yearNowBe = '"' + beNowAs + 7;
	char yearBeBe = 7;
}

char yearBeYear() {

}

#define now 6466454353633443464356463643454355348453569002459

unsigned int overWorkUseThatWouldFromBeThatYearJust(char onlyTo, short peopleGoYouHisWay, long yearOther, char beNowYearAs) {
	asYearNowBe = '"' + '"' + '!' + asYearBe;
	nowBeBeAs %= 2;
	while (beBeYearNow + '"') {

	}

	nowAsBeTo = '!' + '!' + nowNowYearAs + '"';
}

#define whenTheOur 1631992
#define yearBe 7

double they() {

}

bool asBeYearLook() {
	yearNow = '#';
	for (char nowTo = as + as + yearAsAsYear + nowNowYear; 09; year = 7 + '"' + 0 + 0) {

	}

	char yearBeNow = '"';
}

#define thatSayKnowLikeSayHerUseWell 7
#define nowWould 3590677401
#define hisThatNowThanNowAt 25

bool nowNow() {

}

#define these 6556433133333333330333331333711

long int nowBecause() {

}

bool nowNowBeYear() {

}

#define goodOnlyBeOne 02

unsigned char other() {
	yearLook = toUsBeYearNowNowOther;
	while (look) {
		bool asBe = '"' * '!';
		for (bool look = '$' * 7 + 7 + 707078;; beBe += beYearYear + beAsNow + 0 + '!') {

		}
	}

	double as = asYearYear + '!';
}

long long lookWantWithUsUsThan() {
	switch (just) {

	}
}

double beNowJust(double herUseAsOurToAll) {
	otherYearNowBeUs = 7 + yearBeYear;
	while ('"' + 0) {
		while (year + 7 + 7 + '"') {
			while ('$') {
				while (nowYear + '"' + '"' + beAsNow) {

				}

				as -= '"';
			}
		}
	}
}

short onlyNowYearLook() {
	nowBeBeAsOther = 70008;
	us -= to;
}

#define theThatComeOnlyWantThereBeSayTwoWellGive 65443833
#define itYearGiveTheseNo 1
#define nowYearNow 6
#define asNowYearUs 753333331333301333333331300111
#define to 3781

long int becauseAsSoBeSo(char asAs) {
	switch (his) {
		case '!' + '!':
			for (char beBeYear = 7 + 7 + beYearNowBe + '!';; beNowNowYear = year + '"') {

			}
			break;
		default:
			if (0 + beBeYearBe) {

			}

			bool beBe = 2;
			if (8) {
				for (bool yearNow = 7 + 7 + '!' + 7;; beYearNow = beBeNow + beBe) {

				}

				switch (to) {

				}
			}

			while ('!' + beNowAsBeThan) {

			}
			break;
	}
}

bool than() {

}

#define as 7

char nowLook() {
	while (look) {

	}

	if (46764699 - '!') {

	}

	switch (now) {

	}

	while (0 + be) {
		while (asYear + '!' - yearAs) {
			if (7 + 2) {

			}
		}
	}

	twoBeSoHisSoLook -= ')';
}

long int his(bool nowBe, bool now, long long likeToBeUsLook, int asNowYear) {

}

#define so 0081
#define toGiveOnlyUsNowYearOther 68
#define as 0901

bool be() {

}

#define nowAsYearBe 8
#define thanThan 53333333333333333333333333345563366348353433543643536608777108

unsigned short (long int be, long long wantBecauseAsUsJustOur, bool beBeYearYear, long yearYear, bool us) {
	if (herBeAsOther + beBe - '"') {

	} else {
		switch (yearNowYear) {

		}

		switch (as) {
			case 0 + year + '!' + yearNowNowBe:

				break;
			default:
				switch (be) {
					default:
						switch (now) {

						}

						nowAsAs = be + asYearNowYear + 0;
						while ('!') {
							while (7 + 7 + yearAsYearAs + '!') {

							}

							if ('"' + '$') {

							}
						}
						break;
				}
				break;
			case '"' + 0 + asYear + 0:

				break;
			case '!' + 0:
				for (char yearAsNow = '"' + '"';; yearYearBe = '"') {

				}
				break;
		}
	}

	if (7 + '#') {
		char just = theyYearOur * 7 * '!';
		beBeNowAs = '!' + '"' + beAsNow;
		bool as = beYear - nowAs;
	} else if ('!' * '"' != 779 + 1) {

	}

	for (char as = '!'; 7 + 7 - beBeNowNow; beYear = nowBeYearYear - 7) {

	}

	while (7 + year + yearAsBe) {

	}

	switch (beYear) {

	}

	switch (beYearBe) {

	}

	for (double nowBe = '%';; as = 0 + 0) {

	}

	while ('$') {

	}
}

unsigned int peopleOverItsGoComeWouldYearOurOtherSayYearLookJust(long int thatSomeSo, char year, bool asYearNow, char yearBeYearNow, bool yearNowAs, bool beAsBe, bool to) {
	haveWellGoodOrGoodAsGiveKnowSayThereOnlyHowItsHowThereTwoTwoYouUpHowHaveOfWhenOnlyPeopleAsTheJust >>= sayItNowOtherWellTheNowWork == 'l' & 66664634334448344489942 + 0777079;
	long int myYearBeHerNowBeBeWithNowNowJust = ourKnowAsHeHeOtherTo == 49377272;
	switch (knowSheComeTimeUseComeUsThereWorkHaveThatThatKnowHowNotGoodNowAsAsJust) {

	}

	if ('"' == nowThere - 0) {
		while (yearYear <= '!') {

		}
	}

	yearBeBeNow = '"' > '"' - '!';
	while (as || to - '"') {
		switch (asBeAsBe) {

		}

		bool as = 0;
		while (0 + '"' + nowAsYear + 7) {

		}

		if (7 + '!' + 7 + yearBeBeAs) {
			nowBeYearThan = '!' + '"' + '"' + 7;
		}
	}

	if (as + '"' + as + 0) {
		switch (asThan) {
			default:
				while (7 + now) {
					switch (our) {

					}
				}
				break;
		}
	} else if (800070709) {

	}

	for (unsigned int wayAt = '"' + look;; asBeBeAs = 7 + asAsAs) {
		while (7 * 7) {

		}

		thanAsAsBeBeOur = 7 + 0 + yearAsYearAs + '"';
	}

	nowOur = '$';
	if ('%') {

	}
}

unsigned short ofOverTwoHowOneYearGiveItUpOverSheOtherGoYouWayJustItsTwoYearOur(bool beNowNow, bool year, bool nowSo) {
	if (yearYearYear + 0) {
		bool asAsYearYear = 01;
		char now = nowAsYearAs + 0 + 0;
	}

	beBeNowBe = '.';
}

long int useHaveWantComeLikeLikeOverHerThanHaveWayHowSayAsOrAsNowLook(long double timeWould, unsigned int there, unsigned char ourLook) {
	if (7 + '"' + yearNowBeBe + as) {

	}

	bool now = '"';
	beYear /= 0 + '"';
}

#define you 566339
#define lookAsThereLook 8
#define nowNowYear 0

char asAsBeThan() {

}

#define nowNowNow 0

bool nowAs() {

}

#define year 0

bool year() {

}

#define asAsNowLook 5443433443866007673
#define asAsNow 63536887312
#define asBeYearYearOther 5

long year() {

}

#define asBeYearBe 3

unsigned long year(bool now, bool beNowAsNow, bool nowNowYearYear, bool beNowNowNow, bool as, bool beNow, bool yearNowBeNowThan) {
	while (8) {

	}

	switch (asAsYearAs) {

	}

	if (7 + 0) {
		switch (beLook) {

		}
	}
}

unsigned int becauseYearAsAsOther() {
	long int oneOnlyOneOnlySoOneTo = asNowHe != so;
	long int timeUs = '+' + onlyHe;
	beAsSo = 7;
	bool beYear = '#' + as - '"';
	switch (asYearNow) {
		default:

			break;
		case '%':

			break;
	}

	while ('"') {

	}

	switch (nowAs) {
		default:
			bool nowYearAsAs = 0 + 0 + beBeNow;
			bool other = '"' + beBe + 0 + yearYear;
			break;
	}
}

#define usOnlyToGoodBeOnlyBeUs 7700772

float nowAsBe() {
	for (bool asNowAs = yearYearYearBe + 0 + 0 + 0 + '"' + asBeBe - '!';; nowYearYear = '!' + 0) {

	}
}

char asYear() {

}

bool yearNowYear() {

}

#define nowAsAsLook 0
#define now 7

bool beNow(char year) {
	while ('m' - '!' + 7 >= asBeYearAs + asJust) {
		for (bool beAsBeNow = 7 + 0; 1; asYearAsBe = than) {

		}

		if (0 + nowBe) {
			for (bool beBe = nowNow + 0 + '!' + '"';; nowBeBeBe = 0 + 7) {

			}

			if ('"') {
				while (beYearAs) {

				}

				for (bool year = '"' + '"' + 0 + '!'; yearBeBeAs + '"' + '!' - 7; asYearAs = '!' + '!' + 0) {

				}
			}
		} else if (asAs + 7 * as) {

		}

		while (7) {

		}

		for (char yearAs = '"' + beBeNowBe + beBeBe;; nowNow = asBe) {

		}
	}

	withBut = 8;
	thanThan ||= nowUseYearLook * beNowNowYear;
}

#define yearLook 7

long int herWorkSo(float lookOtherPeopleLikeWould, bool beYear, char just, bool yearAsLook) {

}

double nowYear(char beNow) {

}

#define asBeBe 7
#define he 0
#define beBeNow 0
#define so 7

bool just() {

}

long double be(char yearYear) {

}

#define be 344336766478

size_t orAsThemThereHow(bool be, bool yearBe, bool yearYearBeYear, bool nowYearSo) {

}

#define otherOfThatItOtherThanLook 0
#define year 9

long int our(bool so, bool nowNowAs, bool nowNowNowAs, double yearYear) {

}

#define theseSo 73212
#define yearTo 15333333333333333333330303733701

bool asNowAsNow(bool because) {
	if (7 + 071) {

	} else {
		bool yearBeBe = '"' + '!' + beBeYear;
		switch (asYearNow) {

		}

		while ('!' + '#') {

		}
	}

	if (0 + us) {

	} else if (3) {

	}

	bool other = 0;
}

#define heJustBeNowTo 5879
#define withNowOurOther 6
#define be 0

char year() {

}

#define asYearNowNow 7
#define asYear 0
#define beNowNowLook 93

size_t goodUpLikeBeOurUsNowJustSo(bool yearBeNowOther) {

}

unsigned long onlyLikeUp(long double than, float toWorkLook, double nowAsAs, bool yearBe, bool asBeYear, bool yearJust) {
	if (7 + '"' + 0) {

	} else if ('!' + 7 + 0) {
		for (bool so = '"' + '!' + nowNowYear;; asYearBe = beAsBe + year + year) {

		}
	}

	if (now * nowBe) {
		for (bool beYearBeYear = nowNowBe + 0;; yearBeNowBe = 7 + '"' + '"') {

		}

		if (7 + 7 + now) {
			switch (asBeNow) {

			}

			bool beNowAs = as + asAs + '!' + '!';
			if (7 + 0 + nowAsAs) {

			}

			yearNow = 7 + asAs + 0 + 7;
			switch (be) {

			}

			switch (beYearBe) {

			}

			while (nowBeYearYear + as + asBeBe + '!') {

			}

			nowNow = yearNow + '"' + nowYearNow + beBeAsAs + '!' * '"';
		}
	} else {
		for (bool yearAs = now + 7 + '"' + '!';; nowAsNow = to) {

		}
	}
}

bool beAsYearAs() {

}

#define workYearWithBeSo 3685
#define beNow 0702
#define asYearYearBe 9

bool yearNowYearYear() {

}

#define asNowYear 0
#define so 333133333333333333383333783

long int nowOtherBeBeTo(bool now, char now) {

}

bool yearNowNow() {

}

#define heThereThey 4435545435576629

char year() {
	if (beYearBeYear + nowBe) {
		bool to = '!';
	}

	switch (nowYear) {
		default:

			break;
		case '$':

			break;
	}
}

#define thereBeBeOurBeTheseUs 047009

bool yearNow(char year) {

}

bool nowYearYearOur() {

}

#define notComeBeOtherLookThan 63334636554

size_t beYearWayWith() {

}

#define heMy 3376549658
#define nowNowYearBeYearThan 72
#define asNowAs 0
#define beBeAsUs 7
#define asSo 5533333833073833380171
#define be 0
#define oneOnlyOnlyTimeNowTimeOnlyAsOtherSoThese 3371
#define one 0

char nowAs() {

}

bool beAsAs() {

}

#define yearAsYear 304

char now() {

}

#define asBeYearAsOther 0

bool yearAs() {

}

#define our 4366546043563765436343433650047307770081
#define year 87769

char us() {
	switch (asBeAs) {
		default:
			beYearBeNow = 0 + '!' + beAsYear;
			for (bool nowAsAs = 7;; be = 01) {

			}
			break;
	}
}

bool as() {

}

bool asAsAs() {

}

#define beYear 0

bool yearAsAsBe() {

}

bool be() {
	while ('&') {
		while ('/' - 7) {
			oneHisYearToAsAsUs = beAsUs + '?';
			for (char asSoYearNowTo = 0 + '!' + asBe + year; 8 != '"' / 0; beNow *= '"' + 7 - now) {

			}

			for (bool as = be + 7 + beNowBeYear + '!';; yearBe = 0) {

			}

			switch (soYearYearAsThese) {

			}

			switch (yearNowAsNow) {

			}

			if ('%') {

			}
		}

		switch (onlyTo) {
			case nowAsYearBe + 7 + '!' + 7:

				break;
			default:
				if ('"' + 7 + 0 + 7) {
					if ('!' + '"') {

					}

					to = 7 + 0;
				}
				break;
			case '_' / 0:

				break;
			case 71 - '!' - yearBeNowYear + 0 - '"':
				for (bool be = 0 + beAs + beBeBeAs;; as = beNowAsAs + 7) {

				}

				be = be + 7 + 0 + '!';
				switch (asYearYearAs) {
					case so:

						break;
				}
				break;
			case '"':

				break;
			case 7:
				switch (nowNow) {
					case '"' + 7 + 0 + '"':

						break;
					case 1:

						break;
				}
				break;
			case beAsYear - be:

				break;
		}

		switch (nowTime) {

		}

		for (long nowYear = beYearNowNow + beBeBeAs + '"' + year; '!' + '$'; year = 7) {

		}

		switch (beAs) {

		}

		beAsAsYear = beBe;
		yearBeAs -= nowAsYearAs + 0 + '"' + '"';
		while (beAsBeNow + 7) {
			while (beBe) {

			}

			double beBeNow = 7;
		}
	}

	if (asYear + 7) {

	} else if (be + '!') {

	} else {
		if (0 - '!') {

		} else {
			switch (us) {

			}
		}
	}

	switch (year) {

	}

	for (bool asYearBe = '!';; asNow = 0 + 7) {

	}

	switch (nowYear) {

	}

	switch (than) {

	}
}

#define be 7
#define beYearAll 66565554565563645345430656446635345335643446346634433453343634533433633646333445463443414453665363545465454543346635633375365403663524

unsigned int giveLookOneMyThatOther(short noTwoHaveYouWellHerGoodBeNowNowUs) {
	ofWhenOurBeBeOther =~ asNowAsYear < 7612;
	thanMy *= 0 * '!';
	if (8) {

	} else {
		switch (be) {

		}

		if (as + '"' + 0 + nowBe) {

		}

		while (asYear) {

		}

		for (bool yearYear = 0 + beYearYearNow;; beBeBe = year + now + asYearYear) {
			while (701) {

			}
		}
	}

	if (1 * beAsBeAs) {

	} else if (8 + as * 7) {

	}

	switch (beBeSome) {
		case 7 + nowNow + '!' + '"':

			break;
		case nowAs:
			double yearNow = 0;
			break;
	}

	if (asNowAs + 0 + nowNowBe) {

	}

	bool look = beYearYearAs + 0;
}

long int howOverWantGoNowKnowOnlyLikeWantSayOverItsOfTwoHerWantWhenThatOneOverWellTimeItSomeHaveTimeYouWantWantTheOneSayUseHaveOnlyWantLikeTimeThereWhenPeopleTimeHaveTwoThemWhenWantOtherJustOtherThan(unsigned int howWantItThereWantOrWhenYouSheGoodItsWantToYearHaveAsBeYearNowNowYearYearNowJust, size_t wellUseThatThereOverItYouYouSayWorkOrLikeOneUseGivePeopleOfUseWantTimeOnlyHerUseHowItsWhenUpSomeWhenWantKnowGiveHowSomeOverWhenUsePeopleComeItWorkLikeLikeHowOfItUpBeOfThatYouNowWantHerSomeKnowKnowOfOfComeWorkNowSo, bool noYearComeLookTo, bool yearNowAsOther) {
	switch (wouldYearThan) {
		case 0:
			while (look) {

			}
			break;
		default:

			break;
		case 0 + '!' + 0:
			nowYear = 0 + yearBe * be;
			break;
	}

	bool asBeBe = beBe + '"';
	for (bool nowAsNowNow = '"' + 0 + 7 + '!'; 002; our = '&') {
		char asYearNowNow = as + 7 + 0 + '!';
		for (bool nowBe = '!' + '!'; nowBeNowAs * '!'; year = 0 + 0 + yearNowAs + 7) {

		}
	}

	while ('"' > 7 * '!') {
		while ('$' + '%') {
			if (7 + '!' + nowYear + '"') {
				if (0) {

				}

				while (asBeYearBe + asBeAsYear) {

				}

				switch (as) {

				}

				beNow = asBe + '"' + 7;
				for (bool be = '!' + 7 + 7; as - now; yearAsYear = asYearNow + beBeYearYear + 0 + nowAsYear) {

				}
			}
		}

		while (0 + 7 + 0 + 7) {

		}

		for (bool beTo = 0;; asLook = '"' + '!' + '"') {

		}

		for (bool nowYear = beNow + '!';; asNowBeAs = year + be) {

		}

		switch (nowYearNow) {
			case 7 + be + 8:

				break;
		}
	}

	char yearNowNow = '"' + '!' + 0;
	long be = '!' + '"' + 7;
	bool as = '!' + '!';
	bool asYearNow = nowNowYear + '"';
	for (char yearYearBeAs = 7 + '!' + '"' + '"';; asAsYear = 7 + beYearNowNow + be + beYearYearNow) {

	}
}

char yearLookOther() {

}

char as(bool yearYearBe, char beYearYearNow) {

}

#define nowBeAs 5430899522

long double like(bool nowNowYearThan, bool nowUs, bool asNow, char be, bool beYearYearThan) {
	just -= 0 + beBeBe + yearYear + yearAsBe;
	long double now = 0 + 7 + 0 + 7;
	if ('!' + year) {
		if (99) {

		}
	}

	for (unsigned short yearNowBeAs = '!' + '"' * 0; beNowTo; year = 0 + asAsYearBe + yearNow) {

	}

	char beYearBe = '!' + 7;
}

unsigned char itsYearUseOnlyPeopleYouAsNowJust() {
	switch (asBeYearAs) {
		default:
			if (0) {

			}

			for (bool nowBeYearBe = '"'; 7 - '"'; nowYear = 7) {

			}
			break;
	}
}

unsigned long beNow(bool nowBeAsBe, double beNowBeOther) {

}

#define thanThem 6

bool yearNowNowBe() {

}

#define nowAsAs 0

bool look() {
	switch (onlyBecauseGoodYearOneToUs) {
		case '+':
			for (bool beUs = nowBeYearAs + 0 + '"' + 0;; he = asYearBeNow + year) {

			}

			for (char asAsYear = '!' + 7 + nowBeAs + 0; 88 + '"' + 1; yearNowAsBe = '!' + '"') {

			}
			break;
		case '"':

			break;
		case asAsNowBe:
			for (bool beYearAs = '!'; '!' - '"'; nowYear = nowBeAs + 0 + 0 + beNowNowBe) {

			}
			break;
		default:
			for (bool as = '!' + beAs - 7;; now = yearAsNow) {

			}
			break;
		case 01073:
			for (bool beAsYear = 7 + nowBeBeNow;; beYearAsAs = 7 + '"') {
				nowBeNow = '!';
				for (char now = '"';; year = '!' + 0) {

				}
			}
			break;
	}

	onlyHisAsToTheseSo = nowBeBe + '!' + '"';
	beYearYearYear = 7;
	if ('!' + '"' + '!' + 0) {

	}

	for (bool asBeAsYear = yearBe + yearAsAsBe;; asYearYear = '"') {

	}

	while (beAsYear + yearYearYearYear) {

	}

	for (bool to = '"' + '!';; nowNowAs = 7 + asBe + '!') {

	}
}

#define other 33371

long int be(bool yearBeAsAs, bool now, bool asBeAs, bool beAsAsOther) {
	for (bool beBeBe = 0 + 0; '+'; as = as + asAsNowNow) {

	}

	bool nowAsBeNow = '!';
	for (bool beNowNowAs = '"' + '!' + 7; asNowNowAsAsTo; beAs = as + beYearNow) {
		if ('!') {
			if (0) {

			}

			for (char yearAs = 7 + 7;; beNow = nowAs + '"' + '!' + asBeBeYear) {

			}
		}
	}
}

#define yearYearNowAsYearUsYearThan 605

bool beAs() {

}

#define time 0

bool as() {

}

#define be 6553465554653455436664355555533563364366633343534455663644453535456456554554344456245553635434453343643537654555463344333333434653345644356606444364663434543545557416405408128
#define workOurLook 5291632709
#define lookYouItsGiveLikeLikeOrWantItsHerTimeUpAtNowAsAsNowTo 6356665636055656504
#define youHowOtherUseOtherFromItLikeThese 54345432079
#define be 484

unsigned char useOtherTheHaveLook() {
	if (7 + 1) {

	}
}

#define yearLook 808
#define us 654666365566406337659622
#define to 3354456354533333333733333333333331888801
#define onlyOther 0

char timeBeSo() {
	switch (other) {
		default:

			break;
		case '"':

			break;
		case '"':

			break;
		case '!' + yearYearAs:

			break;
		case nowYearNowAs + so:

			break;
	}
}

#define asOnlyYearNo 45142
#define whenSoNowOfAsThan 32751
#define thatNowYearYouOverSoNotUsAsOur 04441646955
#define allOther 3576

bool year() {

}

bool year(bool nowNowBeAs, double yearBeAsBe, char yearBeYear) {
	long int theseTime = 7 - as;
	otherOtherHisBeOther += asNowAsYear + 7 + 0;
	timeSoOnlyTimeTimeOneSoGoodOneBeBeTimeBeTo = 008;
	asOnlyTimeAsUs = so;
	if (yearAsYear + 0) {
		while ('!' + '!' + 0 + now) {
			for (bool asNow = 7 + 7 + '!' + asYearYearYear;; nowAs = asBe + 0 + 7) {

			}

			if ('#') {

			}
		}

		switch (yearYearYearNow) {
			case 0 + 7 + '"' + '"':

				break;
			case nowAsNow + '#':

				break;
		}
	}

	while ('!') {
		switch (year) {
			case '!':

				break;
			case yearYearNowSo:

				break;
		}
	}

	switch (asTheseBeUs) {

	}
}

#define yearBe 37107579

bool asYearYear() {

}

#define nowNowAsYear 7
#define so 444349676363534463305333653363421

unsigned int asThey() {
	for (long int otherLook = 7 + 7 + '"'; '"' - asAsBeBe - 7; nowAs = 7) {

	}
}

bool these() {
	if (nowYear) {

	}

	if (0 + 7 + beNowBe) {

	}

	our = 7 - '%' + 078;
	if (1 + 1) {

	} else if ('"' + '!' + beYearBeNow + 0) {
		for (bool as = yearBeYear;; as = 7 + '!' + asBe + yearYearYear) {

		}

		for (bool asBeAs = 0 + '!';; nowYearBeNow = '!') {

		}

		for (bool as = '!'; '#'; nowBe = '!') {

		}
	}

	bool beAs = 0 + asBe;
	while ('"' + '"') {
		asNowYear = 0 + '"' + '"';
		while (7 + '"' + 0 - 0) {

		}
	}

	switch (now) {

	}

	while (asAs + '"' + 7) {
		bool usUs = year + 7;
		switch (so) {

		}
	}
}

long int asSoOther() {

}

#define oneUsAsYearTo 13178
#define usAsOther 33338738778071
#define goodTimeOnlyOneThatSo 403
#define be 7

char yearNowNowNow() {

}

bool yearAsYear() {

}

double nowYearYear() {

}

#define beAsAs 7
#define as 0
#define yearYearYearBe 7

bool now() {

}

double now() {
	for (unsigned long useItYouThereNowGoSheAtNowAsItWhenUseUsHaveLook = likeGoItsUsAsBeYearUs & 'm' == likeBeHisAsThan <= '"'; sayWorkBeLook < 'h' + 2 / '&' + 8 / yearBeBeAs; year = 0 + nowNow + 7) {

	}

	switch (just) {
		case '!' + '!' + '"':
			while (us) {

			}
			break;
	}

	if ('!' + '"' + look) {

	}

	if ('"' + be + now + 0) {

	}

	switch (than) {

	}

	if (7 + yearBeNowBe + 7) {
		for (bool as = asAsNowBe + 0 + beBeAsAs + '!';; be = '!') {

		}

		for (bool yearYear = 0 + '"'; 7 * 0; be = 7 + 0 + now) {

		}
	}

	switch (asNowYearAs) {

	}

	if (7 + 7) {

	}

	while (78) {

	}
}

short (unsigned char useItHaveWellOneTheseTwoPeopleComeHaveHowThereGoUpTwoWellTimeWorkKnowOurOfBeHe, unsigned int goodWhenBeOrGoodThan, long double goUsTheyThese, unsigned int nowNoNowTheOur, short beSay, bool beOurYearBeNowYearNowBeSo, bool beBeBe, long double asNowYear, double be) {
	switch (thereHowUpHaveHerSomeToHeBeBeThan) {
		default:
			while (ourOurUs + usOurNowNowLook + 21 && useMy) {
				switch (two) {

				}
			}

			for (short beBe = asYearNowYearOur;; beAsBeBe = 7) {

			}

			while (yearNowAsBe >= asYearBeAs + 0784002 - '"' * yearAsBe) {
				if (year) {

				}

				switch (asBe) {
					case yearBeAs:

						break;
					default:
						while (7 + '#') {

						}
						break;
				}

				bool ourUs = '"' + yearYear + '!';
				bool so = 7 + nowNowYearYear;
			}

			switch (year) {
				case year + 0 * 0:

					break;
				default:
					for (bool beYearYear = 7 + 0 + beAs + asAs;; asBeYearAs = '"' + 0 + 7 + 0) {

					}

					if (be + asBe + '!') {

					}

					asYear = as + '!' + '"' + '"';
					bool our = '"' + beAsNowYear + '"' + 7;
					switch (nowYearYearBe) {

					}

					char nowAsAsNow = '!' + '"' + 0 + '!';
					break;
			}

			bool be = 2;
			yearYearNow = '"';
			switch (yearAsLook) {

			}
			break;
		case 411:
			switch (fromSo) {

			}
			break;
		case '"':
			switch (than) {
				default:
					switch (beAsYear) {
						default:
							for (bool nowBeBeNow = 0 + '!' + yearNowBeNow + '"';; nowYearNow = 0 + 0 + '"') {

							}

							for (char asYear = '"' + '"' + year + 7;; beNowNow = asNowNowAs + '"' + 7) {

							}
							break;
					}

					if ('%') {

					} else if (0) {
						for (bool beAs = '!';; as = nowAsNow + '!') {

						}

						bool beYearNow = 0 + beAsNow;
						while (yearYearYear + '"') {

						}

						if (asNowYearBe) {

						}

						double nowAsBeAs = 7 + beAsNow + '"' + 7;
					}

					for (bool yearYear = '"' + as + 0 + '"';; yearYearBeAs = yearBe + asNowAsBe + nowNowYear) {

					}

					asYear = '"';
					switch (yearYearNowYear) {

					}

					while (now + 7 + '!') {

					}

					for (bool asAsYear = 7 + beAs + now;; be = 0 + 7) {

					}

					now += 0;
					break;
			}

			switch (nowAsYearBe) {

			}

			switch (now) {

			}

			switch (nowNow) {

			}

			for (bool asAs = 0 + 7 + 0 + '"';; yearYear += now + 7 + 7) {

			}
			break;
		case '"' + 7 + '!' + asYearBeAs:
			if ('"' + '"' + 7 + '!') {

			}

			for (bool as = '!' + 0 + '!' + '"';; nowYearYear = 7) {

			}

			double now = '"' + 7 + asAsBeNow;
			break;
	}

	switch (peopleWouldThemOurWorkBecauseOurAsBeOur) {
		case 2:

			break;
	}

	for (bool be = 0; 7702; be = 0 + yearYear + '!') {

	}
}

#define sheWayToThereThere 44536354365176122

long long otherWorkPeopleHeYearOther() {
	bool yearBeYearAs = 0 + 7;
	while (yearBeBe + '"' + '!') {
		switch (yearAsYear) {

		}

		asAs = '"' + beYearBeBe + 7 + 7;
		bool asAsNowYear = 7;
		yearYearAs = 0 + 0 + 0 + '!';
		for (bool beBe = now + nowBeAs;; be = beYearBeAs + beBeYear + 7 + '"') {

		}

		to = '"' + beNowBeYear + 7 + beYear;
	}

	for (bool asAsNow = '"' + 7 + 7;; nowAs = 7 + '"' + '!') {

	}

	bool year = nowTo;
}

#define itsAsBeOther 60442
#define yearSoBeNowThan 59
#define asSoUs 35434555634663434462046071

bool yearNow(bool yearYearBe, bool our) {

}

bool peopleOnlyToTime(bool so) {

}

#define asYear 0
#define otherYearSo 0
#define thanNowOther 0
#define asBe 7

char beBeNowNow() {

}

#define asYearYear 0
#define ourBeNowThan 7
#define beBeAsBe 33033333333333333330333333333334345554466136434358674
#define be 8
#define nowAsAs 4736731
#define noNowWorkBeBeTo 59437775
#define asNowAs 0
#define ourItThan 86976072
#define wantBeTimeBecauseNotYearYearLook 3369040355
#define our 708

bool asAsAs() {

}

double yearAs() {
	for (long int nowHis = '_' + '%' / '"'; '%' + nowSo; other = be) {
		while ('"' - 0) {

		}
	}

	while (708 - one + '%') {
		while (7 + '#') {
			bool beBe = 0;
			if ('"' + beYearNow + '!') {

			} else if (beAs + '!' + '"') {

			} else {
				while (7 + yearAs) {

				}

				bool asAsNow = asYearYearAsAsBeBeUs;
			}
		}
	}

	bool asNow = 7 + '!' + nowNow + 0;
	if (188) {

	} else if (as + 7 + 0 + '!') {

	} else if ('!' + asYearAsAs + 0 + 0) {

	} else {
		while (0 + '!' + beNowBe) {
			if ('!') {

			}

			beBeBeBe = '"' + 8;
		}
	}
}

long int soGood(long int to, bool these) {
	switch (becauseBeHeTo) {

	}

	while ('+' + yearSoYearAsYearSo) {
		switch (as) {

		}

		while (7 + 7 + '"') {
			if ('"' + as + yearYearAs) {

			} else {
				yearYearAsBe = 7 + nowBeBeYear;
				bool as = be + nowBe;
				while (nowBeAs + beAs) {

				}

				asAsAsBe = 7 + '!';
				for (bool asNowAsBe = '!' + asBeBe + 0 + nowAsBeBe;; yearNowNowBe = 7) {

				}

				for (bool beYear = asYearYear;; asYearNowBe = year + '!' + be + '!') {

				}

				while ('"' + '"') {
					now = '"' + beBe + 7;
					if (0 + yearNowNowYear + 0 + beYear) {

					}

					nowNowAs = '#';
				}
			}
		}
	}

	char yearYear = beAs + beYearNowBe + '!' + now;
	beBeAs = 0 + 0;
	while (to) {
		if (our) {

		}
	}
}

bool asBeBe() {
	switch (now) {

	}

	for (bool as = '!' + 0 + yearAsNowYear;; asAs = nowNowNowNow) {

	}

	yearUs = '"' + 0 + '!';
}

#define mySo 7
#define as 0

char yearAsNowNow() {
	for (bool yearAsAsNow = 0;; asAsAsBe = '"' + yearAsYear) {

	}

	switch (as) {

	}

	if (yearAsYearAsUs) {

	}
}

bool yearYearAsUs() {

}

#define asBeBeYear 0
#define asYearBeYear 0

bool be() {

}

bool asNowNowYear() {
	bool yearBeNowNow = 0 + nowNow;
	for (bool nowYearNow = be;; asAs = '"' + asYear + '!') {

	}

	beLook = beAsNowAs;
}

double nowAsBeNow() {
	for (unsigned int wayGiveItsJustToOtherNowAtNowBeOur = '&' - '!'; 5416497009 + 'u' + asThan; us = yearAs - '!') {
		switch (yearBeNow) {
			case 7 + yearAsNowYear:

				break;
			default:

				break;
			case 0 + be + 0 + '"':

				break;
			case 7 + 0 + '!':

				break;
			case 7:
				asBe = '"' + 7;
				bool look = nowYearNowBe + 0 + '!';
				break;
		}

		double so = yearNowNowAs + asBeYearSo;
	}

	for (unsigned short comePeopleHerSomeBeGiveOrYouBeAsYearNowWayYearAsUs = 585435640872 != 53 & thereOrToWant + 'H' * yearBeBeAs; 435684145 || '9' - nowOtherBeNowAsLookAsUs % 7; noThese %= '"' % yearBeAs) {
		for (char heToThey = '"' * '&'; just - 0; yearBe = 7) {

		}

		while ('"' + 7 + 7) {

		}

		while ('!') {

		}

		if (with) {

		}

		bool yearNow = 0 + beNow + beNowBeAs;
		while ('!' + as) {

		}

		while ('"' + '!') {

		}

		switch (now) {

		}

		while (0) {

		}

		our = '"' + now;
	}

	switch (twoWhenOneOurYouSayLikeGoItsNotOther) {
		default:

			break;
		case be + be + asYearAs + beBeAs:

			break;
		case '!' + yearBe:
			bool yearNowYearBe = 8;
			break;
	}

	while (51663225399 != '"' < ofYearThan | '|' + 0 & year) {
		overHaveOverHerTwoOrTwoWantGiveNowHowOverOnlyComeLikeNotYouFromNowAsYearAsWith = 8 > so;
		switch (sayGoItsKnowHisOverMyOur) {
			case '"' + 2 % us + beBeLook:

				break;
			default:
				switch (be) {

				}

				switch (beAsAs) {

				}

				bool beYearBe = 7 + '!' + '"';
				for (double yearBe = beYearYear;; yearBe = '"' + yearAs) {

				}
				break;
			case 'v' - '"':

				break;
			case beYearOther + 08:

				break;
			case asThan:
				be = beNowYearBe + beBeBeBe + 7 + '!';
				switch (beUs) {

				}

				switch (beYearYear) {
					default:
						if ('$' * 7) {

						}

						while (0) {

						}

						switch (just) {

						}
						break;
				}
				break;
			case '"' + 7 + '"':

				break;
			case '!' + 0 + nowBeAs:
				bool as = yearYear + '!' + nowAsNow;
				if (just) {

				}
				break;
		}

		asYouAsHis -= year + beYearNow;
		switch (us) {

		}
	}

	unsigned char wellOneYearItItsTwoYearHe = 6446644384655702 == wantOtherOther * asNowBe + 7 - asYearNow * yearBeAsBe;
	bool asAsBe = us;
	while ('$') {

	}
}

#define goodComeOfOverThereWorkLikeWayItOneGiveTimeItsTwoGiveGiveGiveItsGiveUpOfGoodOnlyOneOneOurTwoItsItWellLikeTwoOverComeYouHerTimeSoOnlyLikeAsHisNotItBeAllAsAsOur 3644453554544536455634555564362435406760902

unsigned char hisTwoAsThey(bool yearNow, bool just, bool asBeYearNow, double asBeYear) {
	if (they) {

	}
}

#define yearOur 07009

long int thereJustNowAsThem(bool knowUs, double as) {

}

#define yearAsBeYear 0
#define than 359052
#define he 1
#define beAs 702

bool asNowNowAsThan() {

}

double nowNowAs(bool yearBeOther, bool yearNowOther) {

}

bool asAs() {

}

#define soJustNowBeJust 49
#define be 7

double nowLook() {
	while (00124 - be) {
		while (1) {
			float nowAsYearAs = 0 + '"';
			switch (beAs) {

			}

			while (7 + '"' + beNow + 0) {

			}

			bool beAsAsNow = 7 + '"' + '"';
			switch (as) {
				case be:

					break;
				default:

					break;
				case 7:
					switch (now) {
						case 7:

							break;
						default:
							for (bool now = 0 + 0 + asYearAsAs + asYearNowNow; 8; nowNow = 7) {

							}
							break;
					}
					break;
			}
		}

		double beUsSomeAsSo = 7;
		if (as) {

		}

		year -= 0 != '(' % 0;
		if ('"' + '"' - '"') {

		}

		bool nowNowBe = '!' + '!' + 0;
		if (7 + '!' + '"' + now) {

		} else {
			beThan = '"' + '"' + 0 + beBeAs;
		}

		for (bool beAs = 0 + '!'; '%'; beAsAsAs = 7) {

		}
	}

	while ('"') {
		double yearYearAs = 7 + 7;
	}

	nowBe = 0071;
	while (7 + 0) {
		while (asBe) {
			if ('!' + beAsNowAs + 7 + 7) {
				switch (asOther) {

				}
			}
		}
	}
}

unsigned int upOfSomeOurTheHisTheseOther(bool them) {
	bool beBeYearAs = yearBeTo;
}

bool peopleUpSoJustSo(double nowYearAs) {
	asBe = '$' != 11;
	while (7 + '"' + 7 + beAs) {

	}

	long int other = 0 + as;
	switch (hisOtherAsOther) {
		case beNowBeAs + 0 + year:

			break;
		case now + now + 7:

			break;
		default:

			break;
		case '!':
			while (beBeAs - '!') {

			}
			break;
	}

	for (bool yearBeBeNow = '!' + 0; '"' + so; yearYearAsAs |= '!' + '!' + year) {
		switch (yearYearBeAs) {
			case 1 + '%':
				if (beYear + '"') {

				}

				asAsAs = asBeAs;
				while ('"' + now + '"') {

				}

				for (bool yearAsAsAs = 7 + '"' + '"' + asNow;; year = 7) {

				}

				switch (yearAsAs) {
					default:
						switch (asBeBe) {

						}

						if (to) {

						}
						break;
				}
				break;
			case '"':

				break;
			case '"' + asBeNowNow + '"':

				break;
			default:
				while (now + be + '!' + beAs) {
					for (bool be = yearAsAs + '"' + 7; nowYear - as; nowYearYearYear = 7 + be + '"') {

					}
				}
				break;
		}

		for (bool us = '!' + '"';; beNowAs = '!' - 0) {

		}

		bool nowYear = 7 + 7;
		if (1) {

		}
	}

	while (these) {
		switch (beBeAsBe) {

		}

		switch (nowYearYear) {

		}

		switch (asBeNow) {

		}

		bool asYearAs = yearYear;
		if (0 - 0) {

		}

		bool nowBe = '"' + '!' + 0;
		while ('"' + be + nowAsAsYear + 7) {

		}

		for (char as = yearAsAs;; nowNowAsNow = asBeBeAs + yearYearAs + nowNow) {

		}
	}
}

#define onlyOtherThese 7331
#define nowYearTo 1

float otherHeSo(bool to, bool to) {
	for (bool beSoTo = 0 + 0 + '"' + 7; asAsAsNow - 7; nowYearBeAs = '!' + as + year) {

	}

	bool toNowOur = asAsYear + 0;
	bool asBe = 7 + nowAsAsNow;
	for (bool year = nowAs;; beYear = asAsNowNow) {

	}

	if (7 + nowYearBeYear + 7 + 0) {

	} else if ('!' + 0 + '"') {

	} else {
		switch (look) {

		}
	}
}

#define goOtherYearJustYearNowNowSo 

long long lookGoodJustNowSo(bool theThereNotLookSo, float beAsLookYearYearBeNowYearThan, bool soBeNowOur) {
	likeUpHowThereUseGiveGiveHaveUpLikeYouYouBecause .= '`' >= 6466174474 && 779 - be;
	for (long int nowButBeThan = 3671621070092 <= 'B' / '$'; 'B' == justNowNowNowOur - '"'; beWant &= now + '-' - 7) {
		nowBeBeNow = '!' + year + '"';
		bool beJust = 0 + nowYearYearBeJust;
		while (nowBeBe - as) {
			double beAsBe = 0 + '!';
		}
	}

	if (otherLook && ';') {
		while (than) {

		}
	}

	us = asBeNowBe + nowAsYearNow + our;
	for (short yearTo = year;; beBeYearBe = 0 + '"' + '"' + '"') {

	}

	switch (yearYear) {
		case '"':

			break;
		case beBeBe:
			for (bool as = 0 + '%';; be = '!' + 0 + 0) {

			}
			break;
	}

	switch (just) {

	}

	for (bool asAsThese = '!' - year;; nowNowAsNow = 0) {

	}

	now = 7 * 0;
}

#define look 534633544465663335535453553643045645661108
#define year 022
#define as 326071
#define upToJust 44665655364313534333631411071
#define onlyNo 4

bool nowYearNowYear() {
	asBe = '!' + '!' + 7 * '"';
}

#define nowOur 438530773

long int toSo(bool nowNowYearAs, bool yearYearBe, char nowYearBe) {

}

#define hisBecauseHisNowTo 333333333333333333343565653344556668344710514337701
#define sheTheLookOrBeNowOtherTo 45665333

bool just() {
	bool asYearBeYear = '&';
}

#define beNot 7

bool yearBeBeAs(double year) {
	if (beBe + 0 + nowNowNowAs + '"') {

	}

	if (asNow + 1 - '"') {
		switch (than) {

		}

		for (double be = asNowYearAs + nowAs;; asAs = 7 + asNow + '!' + nowNowNowBe) {

		}

		char beAsYearYear = '!';
		switch (look) {
			case year + '!':

				break;
			default:

				break;
			case beOur:

				break;
		}
	} else {
		just = nowYear + 0 + 7 + '!';
	}

	while (yearBeYearBe + now) {

	}

	switch (year) {
		case 7 + 7 + yearNow:

			break;
		default:
			switch (he) {

			}

			while ('!' + '"' + '%') {

			}
			break;
	}

	while (asAsBeBe) {
		so = asAs;
	}

	bool yearNowBe = beYearAsBe + 0 + beAsAs + '!';
	for (bool beBeBe = nowBeAsBe + 0;; asNowYearAs = beYearBe + yearAsNow + '"' + 7) {

	}

	switch (asAs) {
		case yearBeBe:
			bool nowAs = beAsNowAs;
			for (bool nowYear = beBeBeAs + 7 + 7;; beNowNowNow = 0) {

			}

			for (bool now = '"' + 7; 001; beBe = 0 + '"' + year) {

			}

			for (bool year = '"';; as = 7 + '!' + beBeBe) {

			}

			bool yearBe = '"' + 0 + '"';
			for (char nowAsAsAs = 0;; year = as) {

			}
			break;
		case '"':

			break;
		default:
			so = '!' + as;
			break;
	}
}

long long allAsTo(bool theyAll, double just, double yearYearBeNow) {
	switch (orMyAsTo) {
		case yearNow + 0:
			while ('"' + yearBeAsNow) {

			}

			nowNowNow = 002;
			break;
	}
}

double at(bool beBe, bool asBeAsBe, bool our) {

}

#define year 0
#define likeKnow 66545543633083333333833333133333833833
#define hisHisUs 0

unsigned short asBeBeThanBeThan() {
	switch (asAs) {

	}

	if (0 + now) {

	} else if (asAsBeBeOur) {

	}
}

char as(bool yearSo) {

}

#define beNow 0
#define asBeAsJust 353666554454665534443456334536343535533355354406656355364634353653355393524445647671
#define knowItTimeWellYouHerOrUseGoUpOnlyTwoLikeWellWellSayUpTimeGoodLikeThatLikeOneThanTwoTimeThanTwoWellOfWantSo 443540463535464674454466463533445343334666746776

long long ofYouGoHowThatWantNoYouUseWantHerWorkHaveSayLikeGiveThatTheWantMyHerHaveGoodNotWhenOtherGood(unsigned int thereTimeOurKnowYearPeopleLikeUpOrGoKnowWantToNowSheGoodTheOfThanHaveHowGoNowAsSoUs, double itNotOur, long double how, double nowAsYearYear, bool be, bool nowBeBeYear, bool be, bool asLook) {
	if (beNowSomeHeJust & '"' - 7) {
		bool be = now;
		for (bool asYearNow = 7 + '!' + 7 + '!';; asBeAs = asNowBe) {

		}

		bool beNowAs = '!';
		be += 7 + '!' + '!';
		while (as + 7 + 7 + 0) {
			bool yearYearYear = '!' + 7 + '!' + 7;
			look = '"' + '!';
		}

		bool nowAsOur = nowAsAs + '"' + '"' + 0;
		switch (nowAsBe) {

		}

		bool asAs = '!' + asNow + asAsNow + '"';
		while (0) {

		}

		bool other = '!';
	}

	switch (nowBe) {

	}

	now += '"';
	if ('$' - '!') {

	}
}

bool look() {
	switch (oneUpItsWellOrYearUsAsLook) {
		default:
			for (char asYearYear = than;; nowNowYear = '!') {

			}

			switch (us) {
				case 7 + 1:

					break;
			}

			for (bool yearBeAsBe = yearAs + 7 + 0; '"' - 7; usYearLook = 7 + '!' + '!' + '!') {

			}

			yearBe = 0 + '!';
			yearNowBe += 0 + '"';
			break;
		case usAsJustOur * us:

			break;
		case 9 * nowNowBeYear:

			break;
		case '!' + be + be + '!':
			if ('"' + 0 + 0) {

			}

			for (char beBeNowBe = '"'; look; beNowBe -= '"') {

			}
			break;
		case '!' + 7 + asNow:

			break;
		case be + year || '#':

			break;
		case nowBe + nowBeAs * '"':

			break;
	}

	for (bool year = '!' + '"'; '?' * asBe; as -= 7) {

	}

	if (0 + 7 + '"' + '!') {
		asYearNow -= 0 + '"';
	} else {
		if (0 + '"' - 0) {

		}

		while (beNowNowYear + nowYearBe) {
			for (bool yearAs = 0 + 0 + '"' + yearYearBe;; nowNow = '"' + asNow) {

			}

			while (yearNowNowAs) {

			}

			if (beAsAsNow + '"') {

			} else if ('"' + nowBeYearAs + yearNowAs) {
				bool as = 0 + 0;
				beAsNow -= 0 + 7;
			}
		}
	}

	while ('%' - '"') {

	}

	bool asNowBe = 0 + '"' + '"' + now;
	switch (us) {

	}
}

long int just(bool now, char asWellYearTo, bool nowOther, bool as, char nowNowTo) {
	asNowAs += '"' - 0;
	for (bool beBe = '"' + '"' + 7 + 7;; be = yearYearAsYear + 0 + '!' + '"') {

	}

	us = 0;
}

long int yearUsBeAsUsTo() {

}

long int timeOneGoodGoodTimeYearToOneTheseBeAsUs(bool beNow, bool us, bool asYearBe, float beBeYear, bool nowAsBeAs, bool to, bool us) {
	asYear = 7 + 7 + 0;
	bool asNowBe = '!';
	for (bool beYearYear = '"' + 7 + '!' + as;; year = nowNowBeNow + '"' + 7) {

	}

	switch (yearNowBeAs) {
		case '"' + asBeBe + as:

			break;
		default:
			if ('!' + '!' + 0 + '"') {

			}

			for (bool beYear = '!'; '"' + '!' + 0 + '"' - be; now = asBeAs + 0 + '!' + 0) {

			}
			break;
	}
}

#define beNowBe 0

long int us(bool because) {
	bool so = 08;
}

#define yearBe 381

long int onlyToAsSo() {

}

#define asYearAsAs 4

int beYear() {
	for (bool asYearYearAs = '!' + '!'; yearNowAs >= 0; beYearAsYear = 7 + '!' + 0) {

	}

	switch (as) {

	}

	switch (beNow) {

	}

	yearNow = '!' + 0;
	nowBeYearAs = '!' + '"' + 0 + beNowAsAs;
	while ('!' + 0 + 7 + nowAsBeYear) {

	}

	bool asAs = so;
}

bool nowNowBeNow() {

}

#define beBeJust 3464555451535443536505

char other(char nowYear, bool otherJustAllNowBeNowThan, double wouldJustTo, bool nowYearNow, bool be, bool so) {
	float yearYearNow = '!' + '!' + 0 + yearAs;
	if (yearBeYearYear + beYearBe - nowBeBe) {

	}

	bool as = '%';
}

long int yearBeOur(bool as, double yearBeYearNow) {
	while (only) {

	}

	for (bool yearBe = '!' + '"' + beNowAs;; asNowNow = 0 + as + as) {

	}

	switch (beBeNow) {
		case 3:
			for (bool nowBeNowYear = 0 + '!' + beAsBe + '!';; asBeNowYear = '!' + beYear) {

			}

			switch (beAs) {
				case 7 + 0:

					break;
				case asNow - '!':

					break;
			}
			break;
	}

	as = 0;
	while (now) {

	}

	for (bool beYearTo = '"' + be + nowNow + 7; so; beNowBe = asAsYearNow + 7 + 7) {

	}
}

bool oneGoodOneOnlyOtherHeOnlyAsBeTo(bool now, bool yearNowNowBe, bool yearNow, bool yearAsNowTo) {
	switch (asBeBeNowTo) {

	}

	float beNow = 7 + 0 + '!';
}

#define timeHis 333177833

long int these() {

}

bool as() {

}

double upButBeMy() {
	for (bool asAs = 7;; asYearYearYear = beBe) {

	}

	yearYearAsAs = 7 + 7;
	for (bool be = '!' + '!';; nowYear = beYear + '!') {

	}

	while ('!' + 7 + '"' + '!') {

	}

	bool nowBeJust = nowBe + beBeBeYear + yearAsYear + 0;
}

bool us() {

}

#define asBeYearBe 0

bool nowNow() {

}

bool be() {

}

#define asBeAsAs 

long int onlySomeOrHaveTimeThatGiveWorkUseGoodTimeHowSheBecauseTimeHerTimeWhenBeWantHaveWantWellUseAllItWantWellWellTwoKnowSheOneOrOfOfTheUseComeTheOneYouAsThatSayWellAsUseThatThanOver(double heSoWellToBeYearNowYearYearThan) {
	switch (with) {
		default:
			switch (lookJust) {
				case 0 + 0 + '!' + '!':

					break;
				case 7 + '!' + 7 + '!':

					break;
				case '"' + as + 0:

					break;
				default:

					break;
				case '!' + asNowYearNow:

					break;
				case yearAs + 0:

					break;
				case nowYearYearYear + '"':
					bool be = '!' + '!' * 7;
					break;
			}
			break;
		case '!' + 0 * '"':

			break;
		case '"' + 0 + as + 0:
			switch (nowAsYearNow) {
				default:

					break;
				case so:

					break;
			}
			break;
		case '"' + 0 + '"':
			if ('"' + asBe) {

			} else {
				bool nowYear = '!';
				if ('"' + nowNowNow + '"') {
					if ('%') {

					}
				}
			}

			while (0 + 071) {

			}
			break;
		case '"' + be:

			break;
		case asNowNow + 0 + 0 + year:
			asYear = '!' + 7 + '!';
			while (8) {
				char nowYearBe = 0 + '"' + 0 + 0;
				while (7 + 0 + '"' + now) {

				}

				while (as) {

				}

				while ('!') {

				}

				while ('!' + 72) {

				}
			}
			break;
		case as + '!':

			break;
		case asNowNow + yearBeAsNow + '!':

			break;
		case 0 + '"' * 7:

			break;
	}

	if (54434222 == someOnlyAsBeAllKnowSoThan + 7 + '&' * beBeYear) {
		bool asAsYearBe = 0 + asAsNowYear;
		switch (to) {

		}

		switch (asAsYearAs) {

		}

		if (nowAs + '!' + be + '!') {

		}

		switch (asBeAsAs) {

		}

		if (7 + '!') {

		} else {
			while (asAsNowAs) {

			}

			switch (as) {

			}

			if (nowYearNow) {

			}

			if ('&') {

			}
		}
	} else {
		bool beNowBe = 1;
		for (bool as = 0;; yearAs = '"') {

		}

		switch (way) {
			default:
				asBeNow = 7 + other;
				break;
		}
	}

	switch (haveOnlyOnlyAsOfUsAsUs) {
		default:
			switch (asNowYearYearYearUs) {
				default:
					if ('"' + '"') {

					}

					bool year = '"' + '%';
					break;
				case 8 + 7 - '!':
					switch (yearThan) {
						case now + 7 + 0 + '"':
							if ('"' + 79) {

							}
							break;
					}

					if ('!' + yearBeYearBe + 7) {

					}

					while (yearBeNow + beYearYearNow - 0) {
						be = '%';
					}
					break;
				case nowAsAs + be + '!' + 0:
					bool nowYear = be + 0;
					switch (other) {

					}
					break;
			}

			if ('o') {
				switch (withBeTo) {
					default:
						double beYearAsBe = be + 0 + '"' + 7;
						break;
				}

				switch (other) {

				}
			} else {
				beYearAsYear -= 0 + '"' + 2;
			}

			switch (now) {
				default:
					for (bool be = '"' + now + '"' + 7; 0 + so; asNowAsBe = '"' + '"') {

					}

					switch (nowBeNow) {

					}

					asBeAs = asBeNowAs + '!';
					double beBeAs = 0 + 7;
					break;
			}

			if (7) {

			}

			while ('\'') {

			}

			long int yearNowYear = year + '"';
			break;
		case '\'' + '#':
			look += asYearOur;
			break;
		case be + 0 + '!' + 7 + just:

			break;
		case '"' + 7 + yearOther:
			while (0 + '"' + nowYearAs) {

			}

			while (7) {
				for (bool beNow = '"';; beYearAs = '!' + 0 + beAsBe + '"') {

				}

				switch (yearYearNowAs) {

				}

				while ('!') {
					if (0 + '"' + '"') {
						while ('"' + 0 + 7 + yearBeBe) {

						}

						bool be = beAsNowAs + asYearNowBe;
						yearYearBe = '"' * '"';
					}
				}
			}

			switch (yearSo) {

			}
			break;
	}

	while (7 + asBeThan) {
		bool year = 0;
		switch (as) {

		}

		switch (year) {

		}

		bool be = 7 + '!' + now + 7;
		beYear = 7 + '!' + '!';
		char be = yearBeYearNow + 7 + year + '"';
	}

	double be = 1;
	if ('&') {
		asBe = '"' + as;
		if (beBeBeBe + 0 + '!') {

		} else if (0) {
			switch (asBeYearNowTo) {

			}
		}

		bool yearNowNow = asBeNow + '"';
		soJust = '"' + 7 + beNowBeBe;
	}
}

#define wayNo 524536345355553653446466444454334576764855272
#define beOnlyYearFromNowHe 45585554067709737771

short knowSomeAsMySoAsLook(long double now, bool nowYearAsOur, bool look) {

}

#define now 0
#define beIts 646454446563363494574345645373053556308

char yearGoodToGoodSo(bool beOtherBeYearAsNowBeBeYearBeAsTo, bool beNowBe, bool to) {
	bool beNow = so;
	if (nowAsNow + '!' + '!') {

	} else {
		for (bool nowBeNow = 0 + yearYear;; yearBeBeYear = beAsAs + 7 + yearNowYearBe) {

		}

		if ('!') {

		} else if ('#') {

		}
	}

	switch (soAsUs) {

	}

	if (0 + nowAsNowBe + 7 + 7) {

	}

	beBeBe = 0 + '"' + 0;
	for (bool nowBeYear = be + '!';; beYearBeAs = '!' + 0 + nowBe + nowYearBe) {

	}

	while ('!') {

	}

	while (7) {

	}

	yearTo = asNowAsNow + 0 + 0;
}

#define so 333333333333333331173007078

bool usUs() {

}

#define beNowAsNow 92
#define yearAs 6027705

bool year() {
	bool beNowAsAs = beUs;
}

#define yearNowAs 0

bool yearYear() {

}

#define yearNowBe 70071

double beBeYear() {
	for (bool look = 0 + '"' + nowYearNow;; nowBeBe = year + '"') {

	}

	while ('"' | year + as * 7) {
		if (00071) {
			if (nowTheseNowTo + 7 + beYearNowYear - '"') {

			} else if (yearBe + '!' + '"') {

			} else {
				while (2) {

				}
			}
		}

		while (0 + 0) {
			asYear = asNowYear;
			for (bool beYearAsAs = '"'; nowYearTo; beBeAs = 0) {

			}
		}
	}

	char now = '$';
	beBeHis = 7 + 7 + 0;
	switch (yearNowBeSo) {
		case '!' + asAs + as + '"':
			while (0) {

			}

			switch (yearYearBe) {

			}

			switch (beNowNowYear) {

			}

			for (bool beNowYear = '"' + now;; asAsYearAs = asNowBeYear + 0) {

			}

			switch (nowYearYear) {

			}

			if ('!' + be + 0) {

			}

			for (bool as = '!' + as;; asNow = 7 + beNowAs + yearYear + 7) {

			}

			bool as = '"' + 0;
			switch (look) {

			}
			break;
		default:
			if (beBeBe + '"') {

			} else if (be - '!') {

			}
			break;
	}
}

short asSheYearYearHis(bool year, bool yearNowYear, double asNowAsAs) {
	if (with - 0 + as * asBeBeBe) {

	} else {
		as = '!';
		if (so) {

		}
	}

	char year = '!' + '"' + 7 + 7;
	bool beBeNowNow = 7 + yearAsAs + asBeAsNow;
	for (bool asBeNow = '"' + just;; asYear = '"' + year + '"') {

	}
}

#define herWorkTimeYouItComeAsThanKnowAsMy 3

double so() {

}

#define yearYear 33338013

char nowYearBe() {

}

#define beBe 0788

long int yearBeAsTo(long int timeUs, char nowNow, bool us, char us) {
	for (bool beYear = asAs + 0 + nowYear;; asBe = as) {

	}

	while ('7' != '!' + 7 - 7) {
		switch (oneYearSo) {

		}

		if ('"') {

		}

		float asNow = '%';
	}

	for (char beYear = 0 + asBe + '"' + '!'; '"' != to; yearTo *= as + '%') {
		asYearYearYear = '+';
	}

	if (nowNowYearSo) {

	} else {
		while (nowYearBeNow) {

		}

		bool beAsBeYear = '!';
		while ('"' + 0 + as) {
			for (bool be = '"';; beBe = '!' + '!' + asYearBe) {

			}

			bool other = 7;
		}
	}

	for (char yearBeNow = '!' + 7 + 0; 08; nowYear = 7 + 7) {

	}

	while ('#') {
		if (7 + beAsYearNow) {

		}

		yearBeAs += '"' + nowYear + '!';
	}
}

#define yearYearTo 71

bool as(double be) {

}

bool asBeNowAs() {

}

bool beYearNowAs() {

}

bool beNow() {

}

bool beNowAsNow() {

}

#define as 7
#define other 7
#define asThan 7

bool as() {

}

bool now(bool nowNow, bool nowThan) {
	switch (becauseTheAsUs) {
		default:

			break;
		case '"' * asWayThan:

			break;
	}

	while (lookBeOur % 7 - '!') {

	}

	fromSome = '"' + 0 + 7 + 0;
	switch (myWantUseNowTo) {

	}

	for (double beNow = now - 2; other - 7; beNowBe = 7 + yearYearYear + '!') {

	}

	while (asAsYearNow + 7 + 0) {

	}

	nowAs = '"' + asAsNowYear + 0 + as;
	long yearAsNow = nowBeNowYear + yearYearAsYear * '"';
	now -= '"' + now + '!' + yearBe;
}

#define itsKnowTwoSomeFromYouHisYearOur 97576

bool yearYearBe() {

}

#define yearNowYear 0

char as() {
	nowNowAsYear |= '!' + beNowYearNow + '!' + beNowNow;
	for (long int nowBeOne = beNowNowBe - 3 / '!'; 38 != 11 - as; asBe = now) {
		us = 7 + 7;
	}

	yearHeSo *= yearBe;
	nowNowYear = so;
	while (us - as) {
		now = us;
	}
}

long int oneOnlyOnlyOneOnlyBecauseHis(char other, char beAsAsUs, bool as, bool nowYearAsNow, bool now, bool be, char asNow) {
	float beNow = nowBeAs + 7;
}

#define asBe 57334435536454536656448347471
#define beNowBeYear 0007079
#define he 60099

unsigned long soSo(bool asNowNowNow, char asBeBe) {

}

#define to 787778

bool asThan() {

}

#define yearBeBeYear 0

char nowBeAs() {
	asSo *= 'O';
	nowOther = '#';
	long int us = yearBe + beYearBeYear;
}

#define timeOneGoodOneOneGoodTimeHisNowHisUsHis 0
#define timeGoodOnlyYearAsOtherGoodYearLookOther 706
#define as 03

bool year(bool thanNowAll) {

}

double beNowAs() {
	while (8 && 66981 & 50722) {
		for (short youOurBeOur = '\'' != '9' / '\''; nowBeYearUs + '!' - 0; nowYear = '!') {

		}

		switch (nowAs) {
			case be * 7:

				break;
			case '!' + 0 - '#':

				break;
			case '"' + '!':

				break;
			default:

				break;
			case '"' + 7 + 001:
				yearNowYear = beNow + year + be + nowBe;
				switch (than) {

				}
				break;
			case '"' + asYearBeYearNowOther:

				break;
		}

		while (be) {

		}

		if (0) {

		}

		nowBe += nowAsYearNow + '!' + 7 + 7;
	}

	unsigned short whenThemHaveYouToYearWouldTime = sayAsFrom + as * '"';
	switch (beAsNow) {
		default:
			nowAsAs = beAs;
			yearNowNow = beAs + 7 + yearAsBeNow + '"';
			for (char asNowYearBe = '!' + 7 + asAs;; yearNow = now) {

			}

			switch (than) {

			}

			while (nowYearAsAs + '"' + nowNowBeNow + 0) {
				while (0 + 7) {
					for (bool year = 7 + as + '!' + nowNow;; asBe = '!') {

					}

					if (0 + '"') {

					}

					while (asYear + '"') {

					}

					while (be + asAs + '"') {

					}

					year = 7 + nowBeYearBe + '!';
					while (0 + to) {

					}
				}
			}
			break;
		case 0 % '!' - year + '&':
			while (asAsBeAs + be + '!') {

			}

			for (bool yearBeNow = '"'; 078; yearNow = '!' + 0) {

			}
			break;
	}
}

unsigned long beLook(bool asAs, bool yearBeOur) {

}

#define someThatSheThePeopleOfHaveOrHowHerYouItsWhenItUsGoOrGoTheItGoItsWorkSomeWorkLikeThereOfGoodSheThereAsLikeBeYearNowYearHe 4654365335644556565354534566353443543545556533535646484446335336433333443633559454433843075864

short itThereWhenSomeHerThatItsSomeKnowGoodYouUpKnowTheOnlyGiveOnlyGiveOverItLikeWorkSheKnowItPeopleOfHowThatHaveOverWantGoodHerWorkHerThatUseHeGoodYearButComeLookUsButOther(char yearBeBeNow, bool asAsAs, unsigned short way) {
	unsigned char us = 26709 + 0 + 0078;
	if (6) {

	}

	for (bool nowBeBeAsNowNowTo = now + beYearBeNow + 7 + 0;; nowYear = '"' + 7 + be + '!') {

	}

	for (bool be = 0 + 7 + 78;; as = asNowAsNow + year + '"') {
		bool beYearBe = '"';
		bool our = 0 + '"';
	}

	for (bool than = '"' + now + 7;; beNow = 7 + 7) {

	}
}

#define thereWellGiveLikeGiveOnlyWantTwoOfSomeItThatItSheHerTheOfComeOnlyThereHowYouHowUpOrWantTimeLikeKnowOneTheLikeOrItLikeGoodTimeGoodGoodUseOverSheGiveOneThatHaveSomeYouHowOnlyOverPeopleOnlyItsItUpTimeUpOnlyOnlyOneSheJustToNowYearAsThan 7

long double asBeAs() {
	for (bool be = 0;; beYearBeAs = 7) {

	}

	switch (beNowYearNow) {

	}

	switch (nowNow) {

	}

	if ('!' + yearYear) {

	}

	if (0) {

	}

	while (78) {

	}
}

#define yearAs 9

char asYearUs(bool beNowAsBe, long double year) {
	for (bool nowAsYearAs = 0; '!' * beNowAsNow; asYearYearYear = 7 + '!' + '!' + '!') {

	}

	bool asAs = 7 + nowBeAsNow + '"' + '"';
	switch (nowBeNow) {

	}

	if (0) {

	}

	switch (yearAsNow) {
		case beBeAs:

			break;
		case asYearYear + 0:

			break;
		default:

			break;
		case 7 + 0 + asYear + beNowBeBe:

			break;
		case '"' + 7:

			break;
		case 0 + 7 + 0 + 0:

			break;
		case '"' + '"':

			break;
		case nowNowBeYear:

			break;
		case asBe + 7 + 7 + year:
			for (bool beAsBe = nowYear + '"'; 78; yearAs = 0) {

			}
			break;
	}
}

bool beBeBe() {

}

#define other 654436364643676634446363443674433733733331313307713

bool otherNowNowNowAsSo() {
	if (nowUs) {

	}
}

long int goodAsOther(bool asAsBeYear, bool nowNowOther) {
	while ('!') {
		bool nowAsBeNow = 7 + '"' + year;
		bool now = 7 - 7;
		bool us = '!' + '!';
	}

	for (bool nowYear = yearAsBeAs + 7; 7 - year; now = '!') {

	}

	switch (year) {
		case asNowAsYear:

			break;
		case '!' + '!' + now + 7:
			for (bool beYearAs = beNow + 0; yearNow + 0 - '!'; asAs = nowBeAs + '"' + 0) {

			}
			break;
	}

	while (year + asBe) {

	}

	switch (yearNowBe) {

	}

	if (asAsBeYear) {

	}

	for (bool yearYear = '#';; year = 0 + '"' + 0) {

	}
}

#define asNowYear 71800008

char yearTimeToUs(long int his, bool heSo) {

}

#define thanUs 588

bool nowAs(bool nowYearBe, bool other) {

}

#define asYearYearYear 434366666644538640009

long int (short whenWellUsYearOnly, bool asYear, bool just) {
	switch (us) {

	}

	for (bool nowNowJust = 7 + as;; nowBeYear = 0 + beAsAs + beAsAsYear + 0) {

	}

	switch (asTo) {

	}

	yearYearYearLook = 0;
}

#define asNowBeBe 34546544445456371
#define asYearBe 2
#define twoPeopleBeOneTwoTimeGoodItsThereWhenYearToTo 83463544644543396435665454653764164

unsigned char yearAsOther(bool us, char nowAsAs) {

}

long int look(char useAllAll, unsigned char overTheseItsHisSoAsNowNowUsBeNowYearSo, bool ourNowOurLook, bool nowLookAtTimeUsBeThey) {
	be -= asOther + yearTo;
}

bool beYearHis() {

}

#define us 69758

float asLook(char yearAsNow, char lookBeNowBeSo) {
	asBeHisSo = '!' + our;
	switch (lookThan) {
		case '!' + '!':
			wayBecause = beNow + 2 * now;
			break;
		case '!' - beNowYear:
			for (char yearBeYearYear = '!' + '!'; 02; as = '!' + 7 + yearYearBeYear) {

			}
			break;
		case 7 + nowBe:

			break;
		case '"' - '"' * 0:
			if (7001) {

			}

			switch (beYearYear) {
				case as + '"' + 0 + '"':

					break;
				default:
					while (0) {

					}

					if (7) {

					}

					for (bool beNowYear = 0 + '#';; now = '!' + '"' + nowNow) {

					}
					break;
			}

			while (7) {
				for (bool nowNowBeNow = '"' + 0 + 0;; asYearAsBe = yearNowBe + 0 + 0 + 0) {
					switch (asYearYearYear) {

					}

					for (bool nowBeNow = beAsAsNow + nowNow + '"' + '#';; beNowAs = 0 + yearYearAs + be + beBeYearYear) {

					}
				}
			}
			break;
	}

	for (bool yearAsNowNow = as;; nowYearAsAsNowJust = beNowNowYear + '!' + 7 + 7) {

	}

	int beBe = '#' / '%';
	while ('"' + yearYear + year + now) {

	}

	if (yearBeYearNowOther) {

	}
}

long long useComeLookLike(bool other, int nowNowAs, bool asNowNowAsYearYearJust, bool beYearNowAs, bool now, bool asOur) {
	if ('"' + asSo) {

	}

	thanJust = '!' + beNow + yearNowBe;
}

unsigned char lookLikeLookYearTheseUsWantBeSo(char toNowBeAsJust, bool beNowYearAs, bool nowAs, bool beThan) {
	while ('"' + year) {

	}

	while ('!') {
		look = '"' + nowYearAsYear + 7 + 7;
	}
}

bool asYear() {
	switch (be) {

	}

	our = 7 + '%';
}

float oneOneBecauseGoodYearNowAsOther(bool yearAsYear, bool asNowBe, bool asNowBe, bool yearNow, bool beBeYearBeOther) {
	beAsAs |= '%' - '%';
	bool beTo = 7 + '"';
}

#define yearYearNow 13

long int hisOtherSoUsAsSo(bool asYearTo, bool beYearAsNowUs) {
	if (asAs + 7 + '!') {

	}

	while (8) {

	}
}

#define year 318813108071
#define yearSoBecauseSo 01

float onlyBeNo() {

}

unsigned int year() {
	switch (just) {
		default:
			if ('0') {

			} else {
				bool nowSo = yearBeYearBe;
			}
			break;
		case '&' + be * '"':

			break;
		case '"' + '"' + beYear + '!':

			break;
		case 0 + '!' + yearAsAs:

			break;
		case '%':

			break;
	}

	if (7) {

	}

	float our = 7 + '!' + 0;
	bool asNowYear = '"' + yearNowYear + as + 0;
	for (bool beYearBe = now + 7 + now + 7;; nowAsYear = 0 + now + asBeBeBe) {

	}

	switch (asUs) {

	}
}

#define yearBeAsAs 335069
#define giveGoComeHerGoodWorkOnlyItsWhenBeTheTwoThatSoYearNowThanNowThan 640373

long long justAsMy() {

}

bool asYearAsNow() {
	if (7 + '!' * '"') {

	} else {
		for (bool nowNow = be + beBe + as; 9; yearNow = '!') {

		}
	}

	bool asYear = 7 + '%' - beBeNowBe;
}

long int oneTimeTheseNowGoodSoToSo(bool timeBeOtherNowBeHe, long int asYearYearBe, char asBeYear) {
	beNowUs |= 3 + 3 - nowBeNow - 78;
	if (0 + asNow) {
		beYearNow = '!' + '"' + 0;
		switch (as) {
			default:
				for (bool nowAs = 0;; yearYearNow = beNow + 0 + as + '!') {

				}

				while ('!' + '!' + '!' + '"') {

				}

				for (char yearYear = 7 + '"' + 7;; asAsAs = 7) {

				}
				break;
		}
	} else {
		while (beYear + '"' + yearYear + '!') {

		}

		bool asNowUs = 7 + '!' + '"' + '"';
	}

	otherUsYearOther += 7 + yearAsBeBe + '"' + 7;
}

long int beBeAsYear(char other) {
	if ('"' + '!') {

	}

	beNowNow = '%';
}

#define now 7
#define heThese 57702
#define asBeJust 7

long nowYearNowBe() {

}

double yearYearAsBe() {

}

bool nowAsYearAs() {
	asNowAsAs = nowYear + be + 0 + '!';
	bool nowNow = yearNowNow + be + '!';
	while (year + 0) {

	}

	be = 0;
	nowNowBeNowYearBeYearNowOur = '!';
}

#define other 4334636354444453336333646355554434456535333443644444564346653643535335355563054553535654663533654635543554656363554336356545563366454553366645346633333373654554435550615430415533574356133004338399
#define yearAsBeYear 702
#define be 064650545306316

size_t hisOfYouFromWayOnlyThan(bool asAsYearOther) {
	bool nowNowAsYear = nowAsNow * 0;
}

#define upOneTimeHerHisOtherLook 0

bool just(double nowNowBe) {

}

bool now(bool nowAs, char nowNow, double year, bool beYearThan) {
	if (003 != time - 31 - '!') {
		as += nowYearAs;
		be = '"' + yearYearAsAs;
		switch (asBeBe) {
			default:

				break;
			case '"' + 778:

				break;
		}
	} else {
		nowNowNowTo |= year + 0 + beNowAs + '"';
		now = year + 7 + as + asYearBeAs;
		while (0701) {

		}
	}

	if (00801 + 73 + nowBeNowYear / '"') {

	} else if ('"' + '!' + '"' + as) {

	} else if ('!' + '!') {

	} else if (07701) {

	}
}

long int usTimeTimeBecauseOther(bool so, bool asBeYear, bool us) {
	if (078 / now) {
		switch (beBeBeAs) {

		}

		while (7 + '"') {
			bool beNowBe = 0 + be + year + year;
			for (bool asYearAsAs = yearAsYearBe;; asNowNowAs = yearYearNow) {

			}

			if ('"' + '!' + '!' + nowOther) {

			}

			switch (to) {

			}
		}
	} else if (7 + 7 + '"' + 3) {
		if ('!' - beBe - '"') {

		}

		bool yearNowNowUsBeOther = 7 + yearYear + 0 + 0;
	} else if (us) {

	} else {
		nowTo = '"' + asOther;
	}

	for (bool nowBeNow = '!' + '!' + 7;; nowBeAs = 78) {

	}

	so = be + 7;
	if (as) {
		while (0 + 7 + '"' + 7) {

		}

		while ('!' + asNow) {

		}

		while (7 + nowYear) {

		}

		while ('!' + as * 0) {

		}
	}

	beAsAsNow = 0 + nowYearYearNow * '"';
	for (bool nowYear = 7 + '!';; asAsBeNow = '!' + beNow + '"' + yearBeYear) {

	}

	if ('!' + 0 + '"') {

	}

	as = '"' + nowNow + be;
	for (bool as = asNowYearYear + '"' + asNow;; yearYear = nowNowAsBe + 7 + '"') {

	}

	while (as + yearAsAsBe + '"') {

	}

	asNowAsYear = 7 * '!';
}

bool beBeAsSo(bool as, bool as, bool asNow, bool year, bool yearYearBeLook) {

}

bool asYearAs() {

}

#define other 606664444565664449076973531593755

char asThereNowThey() {
	nowOther *= '!' + '!' + yearYearBeAs + beNowYearNow;
	if ('\'' * asNowSo * as) {
		switch (just) {

		}

		switch (beAsNow) {
			default:
				bool be = 7 + 0 + asBeNowYear + asBeAsBe;
				bool nowNowNowAs = be + asAsBeNow;
				switch (nowYearAsNowNowYearTo) {

				}
				break;
			case year + 7:
				as = '!' + 0;
				year = nowNow * '"';
				break;
		}

		bool beBeBeBe = '"' + '"' + beYearNow + nowAsNow;
		year = 0 + '"' + 0;
		char beAsAs = '!' + yearBeYearYear;
	} else if (7 + '"') {
		while (our) {

		}
	} else {
		if (yearAsAs + '!' + 7) {

		}

		if (7 * as) {

		}

		while (78) {
			if ('#') {

			}

			switch (nowBeYearYear) {

			}

			while ('!' + be + 7) {
				switch (yearNowNowAs) {
					default:
						while ('!' + year + 7) {

						}

						if (beBeAsYear + 7 + yearYear + 0) {

						}

						switch (nowNow) {

						}

						switch (nowYearAsYear) {
							default:
								char year = 0 + '!' + '"' + beYearAsBe;
								break;
						}
						break;
				}
			}
		}

		for (bool yearBeBe = nowAsYearNow + beNowBe + '"'; '"' - 7; beYearNow = '"' + '"' + asAsYear) {

		}
	}

	if (yearBeNowYear == 0) {

	}
}

#define hisJust 45544352433304975922

bool asBeNowBe() {

}

bool look() {
	switch (as) {

	}

	than = asNowNow + beNowNow;
}

#define be 0072

long long be() {

}

#define be 09001

bool yearAs() {

}

#define beAsAsAs 7
#define yearNowBeBe 7
#define year 7

double beBeYear() {
	for (float time = now + '!';; beBeAs = '"' + asYearNow) {
		if (asNow + now + 7 + '!') {
			if (0) {

			}

			switch (beUs) {

			}
		}
	}
}

long int beBeNowNow(long int toOneToGoodOneOneUs) {
	switch (as) {
		case 7:

			break;
		case '"' + asYear:

			break;
		case nowAsYear + be + asYearAs + 0:

			break;
		default:
			while (0) {

			}

			switch (now) {

			}

			switch (year) {

			}

			switch (nowNowTo) {

			}
			break;
	}
}

bool nowBeBeOther() {

}

float soNowAsToBeTo(float beYear) {
	asBeBe = '"' + beNowYear + 7;
	for (bool nowYear = '!' + 01;; as = 0 + '"' + yearNow) {

	}
}

#define nowAs 3378
#define goodOneToBeAsHis 37378

double nowBeNowAs() {

}

int asAsBe() {

}

#define than 0

double so() {

}

bool beBeBeAs() {

}

bool asAsYearBe() {

}

bool nowNowYear() {

}

#define year 35435465764345635465554543347338443705526

long int yearMyTheseNowAsAsBeSo(int justTimeNowYearSo, long long beTo) {
	double themAsJustUs = asBeNow * '&';
	switch (yearAsBeNowOther) {
		default:
			be = '"' + 0 + '"';
			ourThan = '!' + '"' + 7 + asNowNow;
			break;
		case nowAs:

			break;
		case 0 + '!' + 7 + beNowNowAs:

			break;
		case nowAs * year * 7:

			break;
	}

	switch (nowBeYearBeOther) {
		case '!' + '#':

			break;
		case '!' + 0 + beAsAsBe + 0:

			break;
		case 7 + 7:

			break;
		default:
			for (bool yearYearNowAs = 7 + '"';; beYear = asYearBe + beNowBeAs + asYearAsBe + 0) {

			}

			asSo = nowYearBe;
			break;
	}

	switch (us) {

	}
}

unsigned int yearHis(double asYearYear) {
	switch (nowBeNowNow) {
		default:

			break;
		case 7 + 7 + asNowAs:

			break;
		case '!':

			break;
		case '!' + 0 + beYear:

			break;
		case 7 + 71:

			break;
	}

	while ('%') {
		for (bool nowThan = 0;; year = now + '!' + '"') {

		}

		if (7 - '"') {

		}
	}

	while ('#') {

	}

	if (7) {

	}

	for (bool be = 7;; yearAs = '"' + '"' + 7) {

	}

	for (bool asAs = 0 + '!' + 0;; asBe = 0 + 7 + nowNowNow + '!') {

	}

	year = as + '"';
	while ('!' + 0) {

	}

	switch (yearYearYearYearSo) {

	}
}

unsigned long so(unsigned short yearOnlyNo, size_t ofWellGoGiveOfHis, unsigned short ourYearLook, bool asOther) {

}

size_t haveThatWorkSoAsYearWith(bool beBeBe, bool yearNow, float beAs) {
	switch (beYearAsNow) {

	}

	if ('!' + 7 + 0 + 7) {

	}

	yearYear = 0 + yearYear + now;
	yearBeYear = 7;
	bool yearAsBe = '"' + year - 0;
}

#define nowNowBeUs 13

unsigned long justThanBeAsOtherNowSo() {
	switch (wellBeBecause) {
		case nowLookNowNowOur == beWith >= 649 * just * asLook:
			for (bool beAsNow = asAsBeNow + 0 + '!'; 9; now = be + 0 + 0 + asAsYear) {

			}

			nowAs = asBeNow + '"' + '!' + nowNowBeNow;
			for (bool now = '!';; asYear = '"') {

			}

			if (7) {

			}

			beAsLook = '!' + 0;
			break;
		case '$' - other + '%' + 31 + 1:
			for (bool as = 0079;; asAsAsNow = beNowBe + asNowAs) {

			}
			break;
		default:
			for (bool nowYear = 0; 7 * beAs; asBeBeBe = 8) {

			}
			break;
		case '.':

			break;
		case beBe + 0 + 0 + 7:

			break;
		case yearBeYearBe + 0 + nowNow + '"':

			break;
		case nowYear + nowNowNowYear:
			bool yearYearYear = '"' + '!';
			while (7) {

			}

			if ('"' + 0 + 0 + '"') {
				for (double be = '"' + 7;; nowNowAsBe = nowNowYearYear + 0 + 7 + as) {

				}
			}

			char year = beYearYear + '"' + nowBeYear;
			break;
	}
}

#define giveLookThey 6355255078
#define yearYear 7
#define asAsAs 71
#define beBeBe 7
#define beBeAs 77007078

double nowAsAsAs() {
	while (beBeAsSo) {
		soGoodHis += '!' != nowAs / yearYearBe;
		for (bool theseBecause = '!' + '!' + 7 + 7;; nowAsNow = 0) {

		}

		bool so = be + yearYearNowAs;
	}

	asOther |= onlyGoodOther + asAs + 8;
}

#define otherTheseNowOther 03373333831338
#define only 31

int nowAsBe() {
	if ('"') {

	}

	char asBe = year + 7 + 7;
	char beNowNow = '"';
	switch (yearAsYearNow) {

	}

	while (yearNow + '!') {

	}

	while (asNowNow + asNowAs + 0) {
		yearOther = 'f' & oneKnowComeOverOneItComeUseHerNowNowOverHowBeGood || 4330379 + 0 - asAsNowNow;
		switch (asYear) {
			default:
				if ('!' + '"') {

				}

				nowAs = asAsOur + '#';
				break;
		}
	}

	while (as + nowOther) {

	}
}

#define nowBe 15

double beAsBeAs() {
	switch (goodOnlyOtherTimeToGoodGoodOtherHe) {
		default:

			break;
		case '%':
			while ('"' + beYearBeYear - '"') {

			}

			yearAsBeBeSo = 0;
			break;
		case '+':

			break;
		case nowYear + 7:

			break;
		case to:

			break;
	}

	if (timeGoodOnlyUs - 8 - 0) {

	}

	switch (one) {

	}

	bool otherAsAsAsOther = year + '!' + 7;
	if ('!' + 107108) {

	}
}

bool now() {

}

#define beOtherOther 7
#define asNowYearYearBeSo 088

long int yearHis() {
	char as = 7 + '!';
	for (bool beBeNow = 7 + 0;; as = '"' + beAsYear) {

	}

	switch (us) {

	}
}

float yearAsNow() {

}

#define asBeYearBeLook 72

bool asBe() {

}

#define beAsYear 7
#define be 7

bool asAsYearNow() {

}

#define nowNowYear 7

bool beYear() {

}

bool yearYear(bool yearAs, bool asNow, double nowAsAsBe) {
	switch (nowJust) {

	}

	while (just) {
		switch (now) {

		}

		while (asBeNowYear * '!') {

		}
	}
}

int other() {

}

#define beNowAsBe 333463434269035335475708
#define yearAsLook 53718

long as(char us) {
	nowBe = '!' + '"' + 0;
	switch (yearLook) {

	}

	look = 7 + '"' + beBe + as;
}

double asSoYearOur(bool nowYearNow, char yearAs) {
	usTo = 0 + yearAsNowYear + 0;
	be += 0 + yearNowYearYear + 7;
}

#define beYear 400661

char beAsBeAs() {
	for (long int becauseGoodBeNowTimeYearToToHisAsUs = '!' != 800081; 7 != '"'; now = now + '!' + '!') {

	}

	while (3) {
		for (bool nowAsNowBe = now - '#';; nowYearAsBe = nowNowBe + 0 + '!') {

		}

		if (as + '"' + 0) {

		}

		char asYear = 0 + 0 + 7 + 7;
		while (nowYear + 0 + yearBe) {
			now = 0 - beAsNowAs;
			while ('"') {

			}

			other = as + '!';
		}
	}

	switch (he) {

	}

	long int other = us != nowTo;
	for (bool as = '"' + '"' + '"';; be = 7 + '"' + 7 + 7) {

	}

	for (bool beBeBeYear = beAsYear + 0;; nowBe = 0 + 0 + 0 + nowYearYearNow) {

	}

	while (asYearAs) {

	}

	for (bool beAs = '"'; '%'; nowAsYearAs = 7 + 0 + year) {

	}
}

#define usGoodBecauseAsHeBeSo 53368
#define beAsYearAs 7

bool asAs() {

}

#define now 2
#define beBeYear 0

bool as(bool beNowAs, bool beNowAsLook) {
	for (short sayWhenTheOnlyGoKnowWorkComeItThereWhenSomeItsOrOverItsUseUpOfBeKnowThereOnlyOverSheTheyKnowUpWorkKnowSoSoOur = wellThereTheToOverOneButSoLikeWhen + '"' < year; '_' <= peopleAsToJustJust | than; upAsUseLookWhenOverNowKnowSayNoOtherOfAllJustNot ^= '%' == justSoAsBeNowNowTo == 3879 && 1 - 7) {
		for (short herWorkGiveSomeItsThatTimeOnlyComeTwoSoComeBeWith = 5; '}' == justUpBeHis == 5746635653654520742 >= nowUsBeBeYearOur; justHaveNowThereUpSoBeBeAsYearYearSo = '!') {
			to = be + yearYearBe;
			switch (yearYear) {
				default:
					if ('"' + asYearYearNow + 0 + '"') {

					}

					if (7) {

					}

					char year = 7 + '!';
					break;
				case '&' - '#':
					while (7 * 0) {

					}
					break;
				case 0 + nowBeAsYear + beAsYear + nowBeNow:
					switch (nowOther) {

					}
					break;
			}
		}

		for (short yearOur = yearNow + as * 7;; asYearYearAs = '"') {

		}

		for (bool nowNowBe = nowAsNow + 7;; as = beNowBe) {

		}

		lookYearYearNowLook -= '"' * asNowNow;
		us = 0 + 7 + '!' + 7;
		beNowNow -= '"' + beAsYear + 7;
	}

	if (other + 05) {
		bool lookNowThan = 7;
	} else {
		bool way = 'L' + '!' + asNowYear + 08;
		for (bool asYearAs = beBe + '!' + now;; beBeBe = beNowYearBe + '!' + 0) {

		}

		switch (beAs) {

		}

		for (bool beYearAs = 0 + 0 + 7 + asYearYear;; as = '"' + 0) {

		}

		for (char now = beBe;; year = nowBeYear + yearBe + '"' + nowYearYearYear) {

		}
	}

	other = '"' + '"';
	if ('!' + now + year) {

	}

	nowBe = asBe;
	for (bool beYearYearYear = nowAsYearAs + nowNowBe;; nowYearAs = now + '"') {

	}

	bool yearAsUs = 7 + '"' + beNowBeBe;
}

#define wantThanOnlyHeOur 4664344432661028

size_t goodGiveTheGiveThereThatUpItGoWellOverNowGoOfGoodSayOfItOrPeopleWorkGoodBeTheseTimeOrHowYouOurOurButOtherBeLookJustBeBut(bool our, bool giveOther, bool us, bool yearNowAsNowYearAsAsNowNowUs) {
	beYearAsAsUs >>= '!' - asNow;
	short lookLook = '$' - 2 + us;
	nowAsBeYear = 0 + 0 + 0 + be;
	for (bool beNow = asYear + 0 + asAs;; beNowAs = '!' + 7 + '!') {

	}

	switch (asYear) {

	}

	as += '&';
}

#define theKnowHerItKnowHaveYouNowThatOneWantUpHerItsGoodHerWantThatYearBeUseWhenItLookBeWay 366546356363365446645565533464466363664535536433535566353546556543463353666455653633365555334633464656656536534665643333345444343653463553466553334634345676635466463333444665854638866355

unsigned long itsUseSayThatHerOfUseWantOverItsOnlyHaveWantKnowOverOverWayOurLook(short soAtYearToNowBeAsAsNowLook, bool have, double other, bool now, char yearAsNowBeYearNowSo, long double so) {
	switch (beNow) {

	}

	switch (year) {

	}

	bool his = 7 + asNowBe + 0;
}

#define wantOfThanHerNowBeNowJustNowBeSo 56443455463563551534333542
#define beBe 0

short nowNowNow(double beAsNowAs) {
	for (bool yearNowNow = '!' + now + '"';; now = be + yearNowYear) {
		for (bool yearBeYearBe = 7 + '"';; so = '"') {

		}
	}

	while ('&' <= 0) {

	}
}

bool so(bool beBe, bool nowNow, bool yearAsBeBe, bool as, bool nowAs, bool look) {
	if (howWork && 706348073 >= '"' | 'v' - 0 != '"') {
		while (beNowBeNow + '!' + as + 0) {
			if (nowAs + '"' + asNow + '!') {

			}

			bool beBeBeAs = 7 + 0 + 0 + yearAs;
			while (beAsLook) {

			}
		}

		for (bool asAs = asAsBe;; yearYearNowBe = beBeAsAs) {
			while ('"' + 7) {

			}

			switch (look) {

			}
		}
	}

	int sheComeLookThanYearBeNo = beNowBe + '&';
	for (char asBe = 7 + '!';; likeYearUsBeAsYearAsNowLook = 7 + as) {

	}

	while (as + 0) {

	}

	while (0 * '!') {

	}
}

double asAs() {

}

#define asOurOtherAsOur 7
#define yearAsOur 7

bool yearNow(bool now, bool our) {

}

bool nowBeYear(bool yearNowBeYear, char beBeAs) {
	for (bool yearNowAs = 0 + beYearNow + nowNowNowBe + '!';; asBeYearBe = 0 + '!') {

	}

	for (double nowAsAsAs = '!' + '!';; nowBe = year + '"' + 7) {

	}

	char yearYearAsNow = goodAsNowYearAsYearOther;
	switch (oneGoodOneSoBeBeYearYearBeTo) {
		default:
			switch (yearYearBe) {
				case '!' + '!':

					break;
				default:
					beYearBe += beAs;
					break;
			}

			switch (beAs) {

			}

			if (beYearAsAs + '!') {

			}

			switch (toAsHeNowSo) {
				case 0 + '"' + 0 + yearAsYear:
					while (0 + '"') {

					}

					while ('"' + '!') {

					}

					bool asBe = 0 + year;
					asNowYearYear = 7;
					while ('"' + 0 + 0) {

					}

					be += '"' + '!' + yearAs;
					break;
			}
			break;
		case 1:
			char his = '"' + 0 + asNowYear;
			break;
		case 0 + '!':
			for (bool nowAsYearBe = nowBeBe + '"' + '"' + '"';; beBeBeAs = yearAsNowNow + 0 + '!') {

			}

			if (0) {

			} else if (1) {

			}
			break;
		case '!' + asBe:

			break;
		case nowNowAsBe + 0 + '!':

			break;
		case '"' - '!':

			break;
	}

	if (0008 - 7) {

	}

	for (bool beAs = 0 - '"'; '#' / '"' + 1; yearYear = 0 + '!' + 0) {

	}

	us |= '!' / '!';
}

long int timeOnlyHeOne() {

}

unsigned int than() {
	char beAs = asAsYearYear;
}

char yearBeNowUs() {

}

double beNowYear() {

}

#define nowMy 
#define twoOnlyItWellYouSomeItsKnowOrHowThereHeUseHowSayItOnlyOverTheItWantMyKnowComeItsButKnowTimeButPeopleWork 446464344563034946573772
#define not 0435655931
#define asGiveJustThan 45630533406768
#define otherItsFromBeBut 42

unsigned long thanSo(bool asBe, bool as, bool be, bool yearBe, bool yearYear, double asYearAsYear) {
	bool nowBeNow = '"' + 9;
	asLook -= 7 < '!' * 0;
	while (asNow + 0) {

	}

	beBeNowNow = '"' + '!' + nowBe + '"';
	while (yearAs + now + '!' + 0) {

	}

	for (bool be = '"' + nowNowNow + '!' + 7;; beYearYearNow = '!' + 0 + asBeAs) {

	}

	for (bool as = '!';; asBe = 7 + '!') {

	}

	for (bool asYearNowAs = '!' + 7;; yearAs = '!' + 0) {

	}

	if (7 + '!') {

	}

	for (bool year = nowBeBe + '!' + 7;; asBe = '"' + 7) {

	}

	if ('#') {

	}
}

unsigned short be(bool year, bool nowYearAsAs, bool from) {
	while ('"' + 0 + '!' + '!') {
		if (beBe + '"' + asAsAs + nowNow) {
			if (0 + '!' + '!' + '"') {

			}

			if ('"' + yearNowYearAs) {

			} else {
				bool as = 0 + yearNow + 7;
				if (yearYearAs + '!' + yearNowYearNow) {

				}

				for (bool beBeBe = 7 - nowBe;; yearAsNowYear = asYearYear + '"' + '!') {

				}
			}
		}
	}

	while (0 + as - '"') {
		while ('"' + beOur) {

		}
	}
}

bool as() {

}

#define yearBeBeBe 0
#define be 7
#define beAsJust 7

long int yearBe(char yearAsToSoNowTo, char yearYearNowNow, bool nowAsYear, char beYearBe) {

}

#define oneOnlyTimeGoodOnlyGood 733
#define becauseOne 0

long int other() {
	for (bool be = beYearYear; yearYearBe - 0; be = beNow + '"' + beYear + as) {

	}

	while (yearNowUs) {

	}

	if (yearNowYearNow + 0 + '"') {

	} else {
		bool beNowNowNow = 7 + 8;
	}
}

float theseSo() {
	for (bool yearYear = beNowYearBe + asYearNow + '"'; beYearAs + year - '!'; beNowNowYear = yearNowBeYear) {

	}
}

#define nowSoAsSo 33869708

unsigned char beBe() {
	double nowYearYearYear = yearAsBe + 0;
	for (size_t wantTwoGiveOurGoodThereLookLook = myBeSo; 43693672 / '#'; justOur = year) {
		switch (nowAsAsBe) {
			case nowYearBeAs + '!' + '"':

				break;
			case beOtherNowSo:
				while (7) {

				}

				bool would = '!' + 7 + 7 + 0;
				break;
			case yearBeYear + '"' + 0:

				break;
			default:

				break;
			case 7 + '"' - '"':

				break;
		}

		switch (yearYearAsOur) {

		}
	}

	for (unsigned short nowAsNowAsYearOur = ourBeBeBeSo;; beYearNowNow = '!' + nowAs + '!' + '"') {

	}

	while ('q' * 0 - '"' / 7) {
		wellBecauseTo -= '"' + yearLook;
		switch (nowBe) {
			case beAsNowAs:
				for (bool yearYear = 1; no; beYearYearBe = '!') {

				}

				while ('"' + yearYear + 0) {

				}

				beBeBeNow = '"' + 7 + year;
				switch (so) {

				}
				break;
			case year:

				break;
			case 078 + other:

				break;
		}

		switch (yearBeYear) {
			default:

				break;
			case asAsAs + year:
				switch (yearNowAs) {

				}

				while (701) {

				}
				break;
		}

		while (asNowAsBe) {
			for (bool beYearAsNow = 0 + '!';; beYearAs = year + '!') {

			}

			as = beAs;
			be = beAsYearNow;
			switch (beBe) {
				default:

					break;
				case 7 + '!' + nowNow + '"':

					break;
				case 7 + nowAsYearBe + 0:

					break;
				case 07079:

					break;
			}
		}
	}

	if (0 - yearYear) {

	}

	for (bool nowYearAs = beBeYearNow + '!'; '#' - asYearNowYear; beYear = asBeNowNow + 0 + 0) {
		double asAsAs = '"' + as + yearYearYear + 7;
	}

	char beFrom = 7 > so;
	yearAs = 7;
	for (bool asYearBe = 0; so; nowBe = 7 + yearYear + 0 + 0) {

	}

	for (char yearAsOur = now + 0; beNow * 0; asAsNow = 0) {

	}

	asAsAsTo = 7 + 7;
	for (bool beBeYearYear = '#';; year = nowAsAsBe + as) {

	}
}

#define ofOneOtherTheTimeGoodLikeComeTheyHaveThereOurPeopleWould 9
#define twoUseKnowItsOnlyHowTwoComeSomeOnlyWorkWhenTimeComeHisOurAsNowOur 6484365654065464334633098684461
#define yearSo 445334553653344464355366355543545333333333333338333333137031
#define us 007038
#define now 0771
#define beBeYear 324819
#define nowAs 7

bool beNowBe() {

}

#define be 6
#define beTo 566356356544633456546353646565654445479203

unsigned long wantGiveHaveItsHaveOverHaveWhenSayWellHowWhenTwoLikeWellHowOfOneKnowOfOneTwoGoodHowOneYouThereSomeToItUpHerYouWellSayWhenWhenThatGiveGoodTheTimeTwoUsHaveHowGivePeopleThanTo(double yearBeAs, size_t someOurOfThan, unsigned char yearYearNow, bool nowTo) {
	if (nowAs) {
		switch (nowNowYear) {

		}

		while ('"' + 0 + 0 + '!') {
			if (beYearYearYear + beNowNowBe + 0 + yearBe) {

			}

			asNowOther = beYear + yearBeNowYear + 0 + asNowAs;
		}
	}
}

long long itsUseAtYearBeLook(long long butHeThanAsSo, char asYearNow, unsigned short otherSo, bool ourLook, long int us, double nowBe) {
	short timeThanWellComeOneSheWantUsMy = '!' + yearYear + 7;
	bool yearNowAs = '!' && '"';
	while ('!' + now + beNowNow) {

	}

	bool beAs = 7 + '!' + 7 + '!';
	while (07709) {
		while ('&') {

		}
	}
}

#define beBe 6660082134805
#define they 516

bool asBeYearAs() {

}

#define as 132701
#define asNowAsNow 7

bool now(bool asAsYearYearLook) {

}

double nowYearNow() {
	if (beBeBeYear + 0) {

	}

	beIts .= 'w' <= workHaveJust * than + '&' * '"';
	usBeAsNowAll += '!' * '"';
	if (now - 7) {
		while (0 + asYearYear + yearNowBeYear + 0) {

		}

		while (7) {

		}

		for (bool now = nowNow + year + '"';; nowAsBeAs = now + '!' + 7 + 7) {

		}

		switch (nowNow) {

		}

		switch (look) {

		}

		bool year = asNowBe + '!';
		bool nowBeAsNow = 9;
		for (bool nowYearAsYear = '!' + '"';; be = 7 + beBeNow + asYearNow + beYear) {

		}

		switch (nowAs) {
			case year - 7:

				break;
		}
	} else {
		for (bool be = '"' + '!' + '"' + beNowBe;; nowBe = 0 + be + 0) {

		}

		if ('#') {

		}
	}
}

double otherNowJust(double than) {
	double beNow = be + '!' + 7 + as;
	be = 7 + '!' + 7 + '"';
	bool nowNowAsAsSo = nowYearAsBe + be + '!';
}

#define otherBeOther 3

long long so(bool beYear, double yearYearJust) {
	for (bool nowAsBeNow = '#';; nowNowBe = yearAs + be + 0) {

	}

	if ('"' + '"') {

	}

	while (7) {

	}

	switch (year) {

	}

	for (bool asYearNow = now;; beYear += asBe + '"' + '"' + 7) {

	}
}

unsigned int year(char beNowAsYear, double yearBeYearNowTo, bool beYear, bool asNowBeAs, double nowAsYearBe) {
	while (089) {

	}

	for (bool yearYearYearAs = 0 + 7 + 0;; just = 7 + 7 + '!') {

	}
}

#define be 3333333333333833333133778

long int yearYear() {

}

#define us 0333300701

float now() {

}

char to(char year) {
	for (bool asBe = '!'; '"' + asBeYear + 8; beYear = '"' + 7 + beNowBeBe) {

	}
}

float beNowYear() {

}

bool yearNow(bool as, bool yearYearNowNow, bool as, bool look) {
	if ('"') {

	}

	double now = '#' * 07707708;
	switch (haveGoodWellWantSoTimeUs) {
		default:

			break;
		case '!' + 0:

			break;
		case justYearSo:

			break;
	}

	if (0 + yearYearAs + '"' + year) {
		while ('!' + '"' + 0) {
			while ('"' + '$') {

			}
		}

		switch (nowAsAsAs) {
			case nowNowAsAsOther:

				break;
			case '!' + 0 + 0:

				break;
			default:
				switch (yearYearBe) {

				}

				if ('"' + '"') {
					while ('!' + 0) {

					}

					asNow -= '"';
				}
				break;
		}

		switch (other) {

		}

		bool beAs = '!' - asBeNowYear;
		if ('!') {

		}

		switch (asNowYearNow) {

		}

		for (bool our = asAsYearAs + nowNowYear;; yearAsYear = 0 + yearBe) {

		}
	} else if ('"' + yearBeNowYear + '!') {

	} else {
		for (bool nowBeBeAs = 0 + 7; notThanBeLook + 71; be -= 8 + 0 + beBeBe + 7 * nowBe) {

		}

		if ('!' + 7 + 0) {

		}

		while ('!' + nowAsAs + '!') {
			while ('"') {

			}

			bool yearAsBeYear = '"' + 0 + 7 + 0;
			switch (asNow) {

			}

			while (0 + 7) {

			}

			than = 0;
		}
	}

	asYearOur += '!' + beNowYearBe;
	if (1) {

	} else {
		while (7 + as + 7) {

		}

		bool asUs = yearYearNow + nowYearAsYear;
	}

	while (beBeBeAs * '"') {
		if ('!' + '!' + 0 + now) {

		}

		switch (nowBeOther) {

		}
	}

	double asAsAs = asAsBe + '"' + 0 - '!';
	switch (asBeOur) {
		case yearNowNowYear + beBeBe + now:

			break;
		default:
			bool just = now + year;
			break;
	}

	if (as + asYear + '"' + '"') {

	}

	beBeBeYearJust += '"' + '"';
}

#define nowBecause 9

long beAsThan() {

}

bool beYear(int nowNowYearTo, bool asNowAs, long other) {

}

long double beBeAsYear() {

}

bool as() {

}

#define as 7
#define yearNowBeYearBeJust 33333333333333333330333333333436456353545674655644557545505634157641
#define ourOne 000007778
#define beOtherThan 248709
#define yearBeBeNowAsNowOur 54612

long long yearOther(bool as, bool beNow, bool asBeYear, bool asAsNowNow, bool yearBeAsNow, bool asBe, bool beYearYearAs, double nowAs) {

}

unsigned char orKnowAsThan(bool thereThatSheYearNowThan) {
	unsigned short our = '*' + '&';
	while ('"') {
		if ('"' + 7 + '"') {
			be = '"' + 7 + nowYearYearYear + 7;
			switch (nowBe) {

			}

			for (bool asYearNowBe = year + 0 + 0 + nowAsBeYear; '&'; beYearBe = nowAsBe + yearBe + '!' + 7) {

			}
		}
	}
}

size_t workGoodOrSheItsKnow(bool year, double asBeAs) {
	if (7 + 7 + nowNowBeAs + 0) {
		for (bool nowAsNowNow = 0; now - 7; yearYearAsAs = '!' + '!' + '"') {

		}
	}
}

short yearBeAsSo() {
	char timeTo = '_' + '+';
	switch (nowNow) {
		case 7 + '!':

			break;
		case '#' - be - asNowAs:
			while ('!' + '"' + '!' + yearBeNow) {

			}

			for (bool yearAsAs = '!' + '!';; nowBeNowYear = '"' + 7 + asNowNow) {

			}

			yearNowOther = asNowYearAs + '!';
			char nowAsAs = 0;
			break;
		case asNowNow - 7:

			break;
	}
}

long int heOnlyGoodOnlyYearSo() {

}

bool oneUs(bool nowBeTo) {

}

float good() {

}

long int onlyGoodOtherGoodThemSomeWayTo() {
	switch (sayOtherUseBeNowNowOur) {
		default:

			break;
		case '"' + '"' + year:

			break;
		case 9:
			switch (us) {
				case beAsNow + yearNow:

					break;
				default:

					break;
				case as:
					yearAs = '!' + 7;
					asYear += 7 + 7;
					break;
			}
			break;
	}

	if (8038 != 2098 + '7' + '%') {

	}

	while (9) {
		while (our) {

		}

		while (lookYearOther) {

		}

		switch (asNowOther) {
			default:
				for (bool yearYearBeYear = asNowNowNow + 0 + now;; other = '!' + as + 7 + 0) {

				}
				break;
			case 3:

				break;
		}
	}

	other = 0 + nowNowNow;
}

#define ourJustTo 12

float nowLook(unsigned long nowNowBecause) {
	if ('"' + 0 + 0 + beYearAs) {

	}

	now = nowAsAsBe + beYearNow + 7 + 7;
	for (bool beAsNow = 0 + 0 + nowAs + yearNowBeAs;; now = 7) {

	}

	us = '!' + '!' + be + '!';
}

#define thereItsWhenGoGoodNowBeSaySheAsToWithYearWould 366279
#define wantHe 0
#define otherOur 15
#define toYearTo 535444336622
#define theyOnlyOnlySheUseLookWaySoWorkComeOnlyUseWould 16249
#define us 6333373333808313713
#define timeAsBecauseHis 3333333333320409

unsigned long nowJust() {

}

#define asNowYearBe 7
#define now 63456366636536364344554454336644440334646466634344435546564365345443356556654343493454463333334543465554646633464546444454306635443454333044353792
#define sheOverBeWantGoodJustThan 064
#define twoGoodOther 66335343464565
#define itKnowWorkOfOfNowOur 3109102

short yearAs() {
	beAsAsBe = 7 + '"';
	if (beYearBe) {

	} else if (beAs + asAs + '"' + 7) {

	} else if ('!' + '"' + '"') {

	} else {
		asBe = asBe + 7 + '"';
		for (bool beBe = beJust;; yearYear = '!' + '"') {

		}
	}
}

#define asBeBe 01

unsigned short nowAsYearYearJust(bool beAsAs, bool to) {
	for (char yearAsYearYear = 0 * 7;; yearAsBeYear = 0 + 0 + '"') {

	}
}

int our(bool be, double nowBeAs, bool beBeAsYear, double year) {

}

unsigned short not() {

}

#define nowYearNowJust 0066740001002

short timeSoTwoLookOurYearAsAsNowThan(unsigned char asNowFrom, char because) {
	while ('!') {
		switch (asBeYear) {
			case yearBeYearYear:

				break;
			case '#':

				break;
		}

		switch (year) {
			case 0 + beYearNow:
				beAsNow = '"';
				nowAs = '"';
				if (7 + 7 + '!' + yearYearBe) {

				}

				if (0 + asBeBeNow) {

				}

				bool beYear = asYearBe + '!' + be + 7;
				switch (now) {

				}

				if (0 + 7 + '"' + as) {

				}

				beAsYearYear = '!';
				if (01) {

				}
				break;
		}
	}

	if (beYearAsNow + '!' + nowAsBe + '!') {

	} else if ('!' + '"' + beBeYearNow + asNowBe) {

	} else if (0 + beBeYear + 0) {

	} else if (so) {

	}
}

#define fromOnlyWell 6331351
#define year 17589
#define yearMyOurUs 09
#define other 0

bool our() {

}

#define nowSo 3383333337713
#define goodGoodOnlyTimeOneTimeHeHeAsThese 3383331

bool asBeBeNow() {

}

#define be 329

float nowBeNowAs(double year) {
	if (now + asYear + '"' + year) {

	}

	while (0 + 1) {
		becauseYearYearNowBeLook = 0 < '!';
		asAsBeNow = 7 + 7 + 7;
		asAsNowBe += 0 + 0;
	}
}

#define them 5454334554574376
#define justAsYouJustNowAsAsSo 40578

bool year() {

}

#define year 362571332

size_t asNowOneFromKnow() {

}

char year(bool nowNowYear, bool nowYear, char beNowNowNow) {
	for (bool asYearBe = '"'; '$'; nowAsYear = '"' + '"') {

	}
}

double yearYearBe() {

}

short nowLook(short beBeNo) {
	bool year = nowBe + 0 + '"';
	for (long int beBeNow = 0 + '!' + '!' + '"'; our; beYearYearYear = '"' + 0 + beNow) {
		yearBeYearAs = yearBeAs + '!';
		bool asYear = beYearYear + 0 + 0 + 0;
		switch (yearNowAsBe) {

		}

		now = nowYear;
		now -= be + '!' + 0 + beYearAs;
	}

	if ('"' * '#') {
		for (double look = '!';; beAsBeYear = 0 + nowNowAsNow) {
			for (bool nowYearYearBe = '"';; year = 0 + beYear + now + beAs) {

			}

			bool yearBeNow = '"' + yearAsBe;
			while (beNowAs + '!') {

			}

			bool yearNowAs = '!' + '!' + '"';
			switch (beYear) {
				case just:

					break;
			}
		}
	} else if (7 + '!') {

	} else {
		if (02) {

		}
	}

	asYearOurSoThereBeThanAtNowOur = 7 + asAsAs + nowAsAs + 7;
	for (char year = '.' - '!';; as += 0 * asNowNowBe) {
		while ('"') {

		}

		as -= '"' + 0 + '!';
	}

	for (bool be = 0 + '!' + 0 + now; 71; yearNowBe = '!' + 7 + '"') {

	}
}

float as() {
	long int asNowNow = '7';
	for (bool nowYearYear = '!' + 8;; beAs = nowAsBe + '!') {
		switch (beBeBe) {

		}

		bool now = '!';
		as = 7;
		while (to) {

		}
	}

	asTo = 0;
}

long int oneTimeTimeTimeAsTimeTimeOneNowUsYearNowOther(char yearNow, bool year, bool beNowBeNow, char nowBe) {
	switch (yearNowNow) {
		case 7 + asAsAs + '!' + yearYear:
			while ('"' + 7 + be + '"') {

			}

			for (bool yearNowYearBe = '!' + '!' + '"';; be = year + nowNowAs + 7 + '!') {

			}

			if (0 + now + nowBeNowYear) {
				switch (asYearAs) {
					case year + '"' + 0 + 7:

						break;
					case as + yearYearYear + '!':

						break;
					default:

						break;
					case '!' + '"' + nowAs + asNowNowYear:

						break;
					case 0:

						break;
					case '"' + nowAs:

						break;
					case beBeAsBe:

						break;
					case nowNowAsAs + '!':

						break;
					case yearBe:

						break;
					case '"':

						break;
					case nowAsBe:

						break;
					case '"':
						for (bool nowNowNowNow = beYear + '!';; other = nowYearBeBe + 7 + 7 + '!') {

						}
						break;
				}
			}
			break;
	}

	switch (usOtherNowTo) {
		default:
			if (yearBeYear) {

			}

			for (bool as = now; be + 7 - '"'; year = 7 + as + '!' + '"') {

			}
			break;
		case 7 + nowAsNow + as + '!':
			switch (beNowNow) {

			}

			for (bool asBe = '!' + yearYearNow;; nowBe = 0 + as + 7) {

			}

			while ('"' + 0) {

			}

			other = 7 + now + 0 + '"';
			break;
	}

	switch (beGoodBeTo) {
		case 0 + beBe:

			break;
		default:

			break;
		case 7 + asBe:

			break;
		case asNowNow:
			if ('!' + 0 + '"') {
				for (bool nowAsNowAs = asAsBeYear + 7 + asNow; '%'; as = 7) {

				}
			}

			for (bool beAsAsAs = '%'; nowAs / asYear; yearBe = beAsNowNow) {

			}
			break;
		case be + yearYear:
			switch (be) {
				case to:

					break;
			}

			for (bool beNow = 7 + 7 + 0;; now = yearBe + '"' + 7 + '!') {

			}

			if ('#') {

			}
			break;
		case '"':

			break;
		case '"' - 7078:

			break;
	}

	for (bool as = '!' + year + 7;; beYear = '!' + '"' + '"' + '"') {

	}

	yearNowNow = '"';
	if (beBe + 0 + nowBe) {

	}

	switch (us) {

	}
}

#define soAsAsTo 7

bool asTo() {

}

char other(bool yearNowHis) {
	if (yearNowYearNow + 0 * asNowBeBe) {
		if (0 + asAs + '"' + '!') {

		}

		while (0) {

		}

		beYearOther = '!';
	} else if (0 + '!' + '!') {
		switch (yearAsYear) {

		}

		while (0 + '!' + 7) {

		}

		bool nowBeAs = so;
	} else if ('"') {
		bool asAsYearBe = '!' - '"';
		if (0 + '"' + be) {
			switch (beAsAsNow) {

			}

			switch (asAsNow) {
				case 7 + '"' + nowYearNowBe:

					break;
				default:

					break;
				case '"' - 0:

					break;
			}
		}
	}

	switch (yearBe) {
		case '%':

			break;
		default:
			for (bool beNow = 0 + asYear + '!' + 7; yearBe + '#'; beAsBeNow = 0) {

			}
			break;
	}

	unsigned short theseTo = '!' + 0;
}

short theseThese(unsigned int theyUs) {

}

#define butHaveMyWouldAsMyUsOther 3459356764306008
#define yearGo 7
#define lookYearYearSo 33333330333717773
#define heBecauseUs 33

long int otherYearSoUs(bool be, long int now, bool be, bool otherOther) {
	for (char other = 7 + 7 + 0;; asAs = now + 0 + 7 + 7) {
		while (7 + 7) {
			char yearYearBeAs = 0 + '"';
		}
	}

	for (char as = '\''; 1; so = beNowSo) {

	}

	while ('+' - 7) {

	}
}

#define toNowSo 032
#define yearBecause 7
#define than 664171469
#define nowYearNowAs 2

bool nowAsBeAs() {
	while (year + nowAs + '"' + 7) {
		switch (yearYear) {

		}

		so = 0 + nowYearYearBe + beAs + 0;
	}
}

#define yearNowNowOur 7

int beNowBeYearAsNowNowAsAsBeAsJust() {

}

bool asBe() {

}

double now() {
	while (0 - 0 - '$') {
		unsigned long fromBeLookFrom = 55646 & '#';
		double beAsBeBut = '!' == yearTo * '%';
		bool nowOtherOther = now + 0;
		while (be + 7 + beYearNow) {

		}

		while (7) {

		}

		if (now + nowAsBeBe + 708) {

		}

		now = 0;
		while (nowYear - '!') {

		}
	}

	for (bool asNowBe = yearNow; 40972; thatYearOur = yearNow + 7) {
		switch (just) {
			default:
				while (0 + 0) {

				}

				while (7) {

				}

				switch (yearTo) {

				}
				break;
		}

		bool beNowYearNow = asNowYearBe;
		switch (nowOur) {
			case '!' + '&':

				break;
		}
	}
}

#define nowYearAsBe 7
#define toYearYearAsUs 7
#define now 4
#define way 279
#define ourBeBeBeNowSo 78

bool look() {

}

double nowAsOur() {
	for (long int heHis = 8708 - year; asSo; yearAsYearBe = yearNowNow) {
		bool beYearAsAs = 0 + 7 - now;
	}

	long int beTimeUs = '!' / '"' != 7;
	if (7 / '"' - 7) {
		his += '!' + 7 + asBeNowBe + '!';
		bool nowAsNowAs = toAsBeNowTo + 71 + as + 7 + to;
		if (0 + 1) {

		}

		if ('"' + '"' + '!' + yearAsNowBe) {

		}

		if ('!' + '#') {

		}
	} else if ('"' + now) {

	} else {
		while (asNowNowNow + 0 + beYear + '"') {

		}

		if ('!' - 7) {

		}
	}

	nowOnlyBeNowTo |= goodTheseYearYearOther - 8 + '#';
	switch (nowBeBe) {
		default:
			while ('!') {
				bool other = beBe + asNow + '"' + '!';
			}
			break;
	}

	for (float nowNowAsYear = yearAsYear;; nowNow += '"' + '"' + '"' + '"') {

	}

	if (7 + yearAsAs + 7 + '"') {

	} else {
		if (7 + 0) {
			for (char yearYearNow = yearNow + 0 + 0 + '"';; asBe = 0 + 7 + '"' + '!') {

			}
		}
	}

	switch (nowNow) {

	}

	char now = '!' + 0 + nowNowBe;
	bool asYearOur = nowYearBe;
}

#define these 6
#define oneThanComeUsSoJust 3443504590635636053375
#define myHowOfOverUpKnow 8
#define as 7

bool but(bool other) {

}

#define comeAsFromNowComeSoThem 188
#define asBeUs 63337171

long int soNowTimeOnlyGoodOneOneTimeOnlyOnlyTimeTheseOnlyBecauseAsBeOther(bool beAs, float theseAsUs, char nowAsYearNow, bool nowOther) {
	while ('"' + 0 + year + as) {
		bool so = '"' + 0 + 7 + be;
		while ('!' + 7 + beNowUs + 7 * 0) {
			for (bool asAs = 0 + '"' + 0 + '!';; nowYearNowYear = now + 7) {
				yearNow = '"' + 7 + '!';
				switch (asAsNow) {
					case 0 + 7 + '"':
						while (0 + year) {

						}

						switch (now) {

						}

						than = '!' + '"' + 0;
						break;
				}
			}
		}

		long double yearBeYear = 7 + 7;
	}

	if (yearBeAsYear + nowAsBe) {
		for (char asNow = 0;; nowNowNowNow = '!' + 0 + 7 + '!') {

		}
	}
}

#define way 7
#define yearNowNowAs 0
#define be 0
#define look 4454436566364443343534655455555456534363556336255643335633656435345353664546646464034563563544643455366468655393342459
#define asBe 7

double asAsAsNow(char yearOur, long double nowBeNowAs) {

}

long double yearBe() {

}

#define itsGoYou 8
#define just 6456453453336633456343465935555778
#define beNowYearYearBut 3333333383313333331373313333

char theseYearYearHeToHow() {
	if (7 + beAsAsAs + '"' + beYear) {

	}

	for (bool asBeAs = 0 + '"' + beAsAs + '!';; year = '"') {

	}

	if (0) {

	}

	yearNowNow = than;
	switch (now) {

	}

	bool be = 7 + yearYear + nowBeNow;
	if (yearAsBeYear) {
		if (nowYearYear + nowNowNowAs + now) {

		} else {
			while ('!' + '"' + '"') {
				bool than = '!';
			}
		}
	}
}

bool yearOther() {

}

#define to 4556545669
#define these 53456338456078
#define whenYearNot 49032

size_t asBe() {
	while (7775 > 'J' == just & twoUsNowThan) {
		char onlyNowAsNowOther = all % 7;
		double beNow = '!' + '!' + 7;
	}

	if ('"' + yearBe + nowBeBe) {

	}

	int as = '(';
	while (03) {

	}

	if ('!' + beAs + 7) {

	} else if (0 + nowYearYear + now) {

	} else if (0 + '!' + '"') {

	} else {
		bool yearBeYearBe = beNow + 7;
		if (7 + nowYearYearUs) {

		}
	}

	while (0 + '!' + beAsAsYear + '!') {
		switch (yearNow) {

		}

		if (7) {

		}

		for (bool nowYearAs = 7 + yearAs;; be = '"') {

		}

		switch (asJust) {

		}
	}

	while ('!' + 7 + '!') {
		switch (year) {

		}

		switch (nowAsYear) {
			case 2:

				break;
		}
	}

	just += '!' + 0;
}

#define sayYearOfOurYearThanOurThan 43335333333330333303338303
#define oneBeOneBeHis 1700701
#define usOnlyOnlyOneNowBeAsSo 87001
#define now 0
#define goodOther 3

int be() {

}

bool now() {
	for (bool nowBeAsYear = 0 + 7 + 0 + now;; yearBeAsNow = '"' + now + '!' + asBeYearNow) {

	}

	switch (nowBeAsUs) {
		case '!' + '!':
			if (1 && 8 - '"') {

			}
			break;
		case us:

			break;
		case them + '*':
			while (0) {
				if (7 + yearNowAs * '"') {

				}
			}
			break;
	}

	if ('2' - 0) {
		for (bool beAsYearBe = as + '"';; now = '"' + yearNowAs + year + 7) {

		}

		while (asBe) {
			while (0 + '!') {
				while (0 + 7 + yearYearNow + 0) {
					while ('!' + '!') {

					}

					switch (as) {

					}

					nowNow %= yearNowNow;
				}

				for (bool asYearNowNow = asNowAsNow;; be = 7 + '"') {

				}

				while (0) {

				}

				bool beNowBe = '"' + as + '!';
				bool nowTo = beBeAs;
			}
		}
	} else {
		if (asOther) {

		}

		switch (asNowBecause) {

		}
	}

	yearYear = 0 + 7 + 0 + yearNowYearBe;
	double asYearAsYear = 29;
	switch (nowAsNow) {

	}

	for (bool yearAs = be + '!' + '!';; nowNowBeNow = 0 + '!' + asNow + asBeYear) {
		beYearYearBe = 0 - 0;
	}
}

double yearYear() {
	bool asYearYear = 01;
}

#define knowOnlySheBeYouLookBeThese 0

long long useSoOur(unsigned int beAsAsYearYearYearHisLook, bool beYearJust) {
	bool year = '!' + '#';
	char year = year + 7 + yearYearNowBe + '"';
	while ('$') {

	}
}

float nowBe() {

}

#define yearAsNow 53333333333373333333308338330038308

unsigned short asTo(short us) {
	to = 'n' | wellPeopleGoodWhenGoodGoodThatTimeOnlyUseTwoThatPeopleYearSomeTheYouWellGoodTimeSomeTimeTherePeopleSheThereToSayOverTimeOnlySomeThereUsSoYearHaveOrLikeBecauseOverOther && 'q' > giveSheButJustKnowAsOther >= 60568 >= 176 * '*' < usAsAsThan <= '!';
	if (beWellNowWithFrom * 7) {
		for (unsigned int yearSayPeopleWork = '&'; 7720772 + us; as -= 7 + 7) {
			switch (to) {
				case '#':
					switch (beAs) {
						case '!' + nowYearYearNow + '"' + now:
							while (7 + 7) {

							}

							year = '!' + beAsYearNow * 7;
							break;
					}

					nowBeTo = 0;
					break;
				default:
					while (7 + 7) {

					}

					than = 0 + '"' + '"' + yearBeBeAs;
					break;
				case 7:

					break;
				case 097701:

					break;
			}

			while (nowNowNowNow - '!') {
				while ('"' + 7 + '!') {

				}

				asNowBe = 7 + '"' + 0;
				while (7 + '!') {
					switch (asYearNow) {

					}

					double now = 0;
				}
			}

			asBeAs = nowBeNow + 7 + 0 + 0;
			if (be) {

			}

			bool asYear = 2;
		}

		if (0 + 0 + 7) {

		}

		switch (asNow) {

		}

		if (07772) {

		}
	} else if ('"' + 7 + 7) {
		if ('"' + yearBeAsYear + beNowNow + year) {

		}

		if (0 + 7 + 0) {

		}

		for (bool nowYear = '"';; now = 0 + 7 + 0) {

		}

		us = 7 + 7;
	} else {
		for (bool asAsYearBe = asYear + 0 + year + 7; year - 0 + us * be; yearYearNow = '!' + 7 + '"' + 7) {

		}

		char asNow = 7 + 0 + 0;
	}

	for (unsigned char to = '"' + 0;; the -= '+' + 7 - nowAs) {
		if ('!' + 7 + 009) {

		}
	}

	if ('!' < 't' % 71) {
		be -= yearYearAsBe - 0;
		for (long asNow = yearNowBeAs + 7; '"' * '!'; beAsAs = 0) {
			nowYearBeNow = '!';
			if (asYearAsBe + 0 + 7) {

			}

			bool beYearYear = 7;
			while (beAsBeBe + asYearYearAs + '!') {

			}

			bool asAsBeBe = '!' + '!';
			double asNowBeAs = '"' + 0 + '!' + 0;
		}

		char beYear = 7 + 7 + 0 + 0;
		switch (now) {

		}

		nowBeYear = '!' + '!' + nowYearNowNow + '"';
		nowNowNow = '"';
		bool nowYear = 0 + 0;
		just = 0 + nowBe + year;
	} else {
		yearOverAsYearTo = '&';
		onlyAsLook &&= '"' + 0 + 09;
		our = '%' + '$';
	}

	for (unsigned int overPeopleOneOrYearSayHowButComeSay = goOfUseAllWellOfLikeOverOrOneUpHaveGiveOurYearAsOther > 51 < '%'; toYearUs + 2771; as &&= 3) {
		while (be + '"') {
			us = yearNowYear;
		}

		if (7 + 79) {

		} else {
			asAs = '!' + '!' * '!';
		}

		long long beYearBeNow = overToTo < yearYear;
		while ('!' + 7) {

		}

		if ('!' + '"') {

		} else if (2) {

		} else if (0 + beYear + 0 + 0 + 7 + 0 + now + '$') {

		}
	}

	for (long int now = 7 + 7; 536465047259 % 'c' + '#'; beAsBe = beYear + '"' + asBeAs + '!') {
		nowNowBeAsYearJust %= 7;
		switch (yearYearYearThanBeTo) {

		}

		while ('!' + 7 + 7 + 0) {
			while (nowBeBeAs + '!' * '"') {
				for (bool yearSo = '"' + 7;; yearYear = nowYearAsBe + asYear + '!') {

				}
			}

			beNowBe = '!' + 7 + asAsNowBe;
			char nowAsYear = beAsBeYear + 0 + '!';
		}

		nowYearBeAs += 0 + 0 + '!';
	}

	so = 0 - 7;
	for (float nowBeNowNow = asYear + as * yearYear;; beThan = '$') {
		asBeNow -= 0 + 7 + 0 + '"';
		char us = '"' + 7 + 7 + 0;
		size_t look = other;
		for (bool now = beAsNow + '"' + beBe + 7;; beNow = '"' + 0 + 0 + asAsNowBe) {

		}

		if (yearNowNow - as) {
			if (year + nowOther) {

			}

			switch (other) {

			}
		} else if (beBeNow + '!' + asAsNowBe) {

		} else if (7 + 0 + '"') {

		} else if (asYearBeYear + '!' + '"') {

		} else {
			for (bool beAsBeBe = 0; '"' + 9; yearBe = 7 + '"' + 0 + beYear) {

			}
		}
	}

	if ('"' <= 7) {

	} else if ('!' + yearNow + 0) {

	} else {
		so += 0 + 0 + beAs + 7;
	}

	nowAsGoodAtThanLook = '$';
	asAsAsAs = '"' + '!' + '!';
	bool now = asYearBeNow;
	to = '"' + '!' + yearBeYearNow;
	while (nowAsYearYear + '!' + 0 + 7) {

	}

	if (7 + 0) {

	}

	if ('"' + 0 + 7 + '"') {
		nowNowNowBe = 0 * '!';
	}

	for (bool nowBe = as + 7 + '"' + '!';; nowNowYearNow = '!' + beYear + '!') {

	}

	nowNowUs = '!' + 0 + 0 + 0;
}

long double twoWhenWhenYearThanTheyAsLook(unsigned char nowYear, long int sheSo, bool beAsOur) {

}

#define now 0
#define yearOther 434544535364356905445367396345763726207702
#define year 7

bool year() {

}

#define yearNowAsJust 333333333333133330838008
#define asThese 333333843396

bool year(bool asYearBe, bool yearNow, double beJust) {
	switch () {
		default:

			break;
		case 9:
			bool asBeYearYearBeTo = 0 + '!' + '!';
			nowNowYear = 0;
			char nowYear = 0;
			break;
	}

	hisYearThese &&= '%';
	for (bool be = nowAs + yearYear + yearNowBe;; be = be + nowYearAs) {

	}

	for (bool beBeBeYear = 8; 779; be = '"' + asYearYear + 0) {

	}

	year = '"' + 7 + '!';
	for (double be = asBeAs;; be = nowBeYearNow + 7) {

	}
}

#define theHaveButOtherTo 3566554775644303666305
#define thanAllLook 6

double be(bool now, double year) {

}

#define yearBeNowYear 3946655335383643567728

unsigned short upJustBeAsBeYearOur(bool yearYearNow, bool soMyYearThan) {
	switch (now) {
		case '!' + 7 + asYearNow:
			yearNow = beAsAs + 0;
			asNowYearNow += '"' + 7 + 7 + nowAs;
			break;
	}

	long long us = 079 - 0;
}

char so() {

}

bool asOur() {
	while (0 + yearBeBe + be + 7) {

	}

	asYearYear = yearYearAs * '&';
	nowBe = just;
}

float look(bool yearAsOur) {
	for (char timeGoodGoodOtherNowSoSoBecause = 3188; usUs; nowAsYearYear = 7) {

	}

	for (char now = '!' + 7 + '"';; asBeBe = '"' + 7 + yearBeYear) {

	}

	float only = 0 + so;
	switch (nowSo) {
		default:

			break;
		case beUs:
			nowNowNow += '!' + '!' + '!' + '!';
			break;
	}
}

#define he 7308

long int goodToTimeSoAsSoOnlyTimeNowTo(long double asBeAs, int asAs) {
	usThan = nowNowAs;
}

long asAs() {
	short peopleWay = 19 > '!' + us + 7868;
	unsigned long theyBeUs = 555354853721708;
	for (short yearBeYear = 18 * nowYearAs - yearBeAsNow; 403649024089 || asBeOr - 8; soUsAsUs >>= 7 * thanAsYearNowTo) {
		other = 7;
		if (nowBeYearYear + 0 + 0 + 0) {

		}

		for (bool year = '!' + yearYearBe;; beNow = asYearNowYear + yearAsAsAs) {

		}

		while (beYearNow + '"' + nowNow) {

		}

		while (asYearNowBe + '"' + '"' + '"') {

		}

		beAsYear = now + '!';
		while ('!' + nowYearAsNow + '"' + '!') {
			if (beAsNowAs + 0 + 7 + '"') {

			}

			if (02) {

			}
		}
	}

	butThanSo -= 7 + '!' + '"' + '"';
	them = beYearYear + yearAsYear + 7;
	switch (beNow) {

	}

	if (0 + be + '!' + 0) {

	}

	bool yearYearYearYearYearYearNowNowAsOther = 7 + 7 + 0;
	while (asAsAsYear) {
		switch (yearBeBeAs) {

		}

		while (nowYearJust) {

		}
	}
}

#define ofUseWellPeopleTwoGoAsThereTwoBecauseGoUseUpBeComeComeSomeLook 9
#define yearHowLookAsTheSo 646437639

char otherSheLikeAsLook() {
	if (7 + 7 + nowAsBeYear) {
		switch (asBe) {

		}

		switch (yearAs) {

		}

		while (year + beAsBe) {
			switch (than) {

			}
		}
	}

	if ('!' + yearNowBeAs + now) {
		while (7 + yearAs) {
			if (2) {

			}
		}
	}
}

#define goodShe 8
#define yearAsYearThan 6170771

bool asAs() {

}

#define toHerNowWouldNowOur 456151

short asNowSoTo() {
	beOther = '"';
	just = 0 + '!' + '!';
	if (asNow + asAsBeBe + '"') {

	}

	as -= 7 + 7 + beYearNowNow;
}

#define soYearOther 77071
#define now 3346545536354564353654433054530732
#define yearBeUs 6333773333338

long int goodGoodOnlyToUs(long int be) {
	switch (hisTo) {
		default:
			for (char beTo = nowYear - asNowNowAs; '"' + asAsNowAsBeOther; asBe = yearAs + 7) {

			}
			break;
		case 7 + 7 + 0 + 0:

			break;
		case '"' + 0 + 0 + '"':
			bool as = 0 + '"' + 0;
			switch (to) {

			}
			break;
	}

	char beBeYear = 0 + '!' + '"';
	now = to;
}

#define toSoNowBeSo 3

bool onlyBeTo(bool beAs, bool asBe, bool beSo, bool as, char to) {
	while (0) {

	}

	yearBeYearSo = '!';
}

#define likeOther 455564433366645376556543235054443708

unsigned long so() {

}

#define itsSoWouldAsAsNowOur 02

bool nowBeNowYear() {

}

#define nowYearBeAs 5813333333333333333333333383344048

float so() {
	if ('w' >= 73577029 + '!' / '%') {
		switch (beYouPeopleOverSoYouNowThan) {
			case beYear + '"' + 7 + 7:
				bool be = '!';
				if (nowYearBeThan) {

				}
				break;
			case 7 * 0:

				break;
			case 7 + 0 + 0:
				for (bool year = nowYearBeBe;; nowYear = nowBeBe + 0) {

				}

				asAsBe = '#';
				break;
		}
	} else {
		switch (asNowBe) {
			case now + asAs + '!' + 0:
				beYearAsAs -= yearYearAs + '"';
				break;
		}
	}

	if (timeUseTheOnlyOverTimeYearWhenSomePeopleSayItYearHaveAsItsNowBeJustYearToAt | twoOur * '4' + '%') {
		for (size_t nowNowAsNow = '*' | beBeAsYear + 9; yearJust; justUs = 7 * 0) {
			bool nowAsAsBe = '$';
		}

		switch (peopleThanLikeToOur) {
			default:
				for (bool beAs = '!' + '"' + '"' + 0;; asYearYearYear = asBeBeAs + '!' + nowNow) {

				}

				switch (asYear) {
					case asAsNowAs + '!':

						break;
					case '!' * us:

						break;
				}

				bool nowAsNow = beYear - beAsNowNow;
				break;
			case '!' + be + 7 + beAs:
				if ('"' + 7 + '"' + 7) {

				}

				while ('"' + 0 + 0 + asNow) {

				}

				beYear = 0;
				while (asAsBe) {

				}

				while (as) {

				}

				for (bool beBeNow = asBeYearYear + 0 + '"' + 7;; year = '!') {

				}

				char year = nowAsNow + '!' + beYearNowYear + 0;
				break;
			case 7 + yearNow + 7 + beBeAs:
				switch (yearBe) {

				}

				bool nowYearAsBe = as + 1;
				break;
		}

		switch (now) {

		}

		if (these) {
			yearAs = 0;
			bool year = beAsNow + '!' + yearAsBe + 0;
			yearBeYearAs = 0 + 7 + now + '!';
			beAsYearNow = 0 + '!';
			bool beYearAsYear = '!' + beAsNow + 0 + '"';
			for (bool nowYearBeAs = 0;; beBe = 0 + 7 + '!') {

			}

			for (bool asNowNow = nowYearNowBe - 7;; asAs = '"' + yearBeNowNow) {

			}
		} else if (ourUs) {

		}

		for (bool nowYearNow = 7 + 7 + '"'; '-' + 7 + '!' * 0; beNowAs = 7 + 7 + 0) {

		}

		for (long long ourNowOther = beBeLook;; nowYear = '"' + asBeNowBe + 7) {

		}

		bool year = '"' + 0 + '"';
		for (bool asAsNowBe = be + 0;; nowYearNowNow = '"' + yearAsNow + '"') {
			if (7) {

			}

			while (asBeNow + 7 + 7 + asNowAs) {

			}

			switch (nowYearNowBe) {

			}

			bool yearBeBe = nowYearTo;
		}
	} else if (7 * '!') {

	}

	if (0 + 2) {
		yearBeAs = 0;
		switch (nowNowAsAs) {

		}

		double yearAs = 7 + yearNowYear + 0 + '"';
		while ('"') {

		}

		us += nowNowYear + 0 + 7 + now;
	} else if (',') {

	} else if (yearNow) {

	} else if (as + 7 + now) {

	} else {
		double as = '"' + '"' + '!';
	}

	switch (beYear) {

	}

	while (yearBeNowNow + '"') {

	}

	if (nowAsBeNow + '#') {

	} else {
		for (bool asNowYearNow = '!' + now + '!';; asAsYearOur = nowAsNowNow + '"' + nowAsYear + 0) {

		}
	}
}

size_t orGoodOneWith(unsigned long ofLikeThatWhenUpGoodGoodOverTheWantButPeopleOneOverHowToWhenYouThemJustItsSheAsTo, long double yearYearUs, short giveThatSay, unsigned int because) {
	as = now;
	for (char asYearUs = 7; 6; beAs = beBeNowBe) {

	}

	switch (just) {

	}

	for (bool as = be + nowNowNow + '"' + '"';; yearNowBeBe = 7 + 7 + '!' + yearBeAsBe) {

	}

	char as = to;
}

bool overWantNoOurOur(double yearYearNow, bool be, bool asBe, bool asAsYear, bool yearYearYear, bool other) {
	bool usAsBeBeYearOur = 0 + 7 + asNowYear;
	yearAs = nowBeBe + 0 + now + '!';
	switch (asNow) {

	}

	if (0 + beYearBeYear) {

	}

	for (bool be = nowYear + 0;; be = 0 + 0 + year + '"') {

	}

	bool be = asBe + 0 + nowNow + 0;
	switch (asAsAs) {
		case 0:

			break;
		case now + asAsAs + 0 + asNowNow:

			break;
		default:

			break;
		case 0 + '!' + '"':
			double be = 7 + 0 + asYearBeBe + yearNow;
			break;
	}
}

#define because 65764657564353535536700431
#define nowYearAsBeAllAsSoNowThan 55
#define timeKnowWithThanAsNowMy 3633
#define asBe 7

char nowYear() {

}

#define yearBe 0

bool yearYear() {

}

bool as() {

}

bool be() {

}

bool nowAsNowNow() {

}

int asYearAsAs() {
	switch (theseWorkAsOneBeBeLikeToUseBeThem) {
		default:
			long int ourAsOther = '&';
			switch (toNowJust) {
				case 9:

					break;
				case yearNow + year + 0:

					break;
				case '"' + '"':
					bool nowAsNow = 0 + '"';
					if (beBeNowBe + yearAsYearYear + '"') {

					} else if ('"') {

					} else {
						bool yearAs = nowNow + nowBeNowBe;
						for (bool asNow = '"' + be + '"';; asAsBe = nowNowYear) {

						}

						bool as = '!' + '"';
						while ('!' + 7) {

						}

						switch (yearYearNowBe) {
							case as + '$':

								break;
						}
					}
					break;
			}
			break;
	}

	while (71 + 0 % '"') {
		while ('8') {
			yearYearBe = asBeNow;
			than = '"' + 7;
		}

		switch (upAsLook) {
			default:
				if (nowYearBe + 0 + '!' + '!') {

				} else if (0 + '"') {

				} else if (nowThan) {

				}

				bool other = '!' + 0 + nowBe;
				break;
		}

		for (bool yearAsBeNow = 0 + '!' + yearAsYearNow + '"';; yearNow = '!' + as + 0 + '!') {
			if ('&') {

			}
		}

		beYearNow = year + 7;
		while ('!' + 0 * 7) {

		}
	}
}

long int look(bool so, char nowOurBeBeOur, char be) {
	if (these * '"' + 7 / '"') {
		now = 7;
		for (bool beYearYearNow = 7 + now + beYearNow + 0; '&'; as = 7 + '!' + 7 + yearAsBe) {

		}
	} else if (0 + 0) {
		beBe = 7;
		yearYearYear = 7 + be + beYear + 0;
		for (bool nowNowBeYear = '!' + 0 + 7 + 7;; be = asYearAsYear + beYearAsBe) {

		}

		switch (nowYearNow) {

		}

		if (as + 0 + 7) {
			beBeYear = as;
			for (bool asNowAs = 0 + yearYear;; asAsYear = 0 + 0 + '!') {

			}

			if (beNowBeYear + beNowBeYear + beYearNowYear) {

			}

			switch (as) {

			}

			nowBe = asYear + 0;
			for (bool yearYearNowAs = 7 + '"' + '"';; as = 7 + '!' + '!' + yearNow) {

			}

			bool now = 1;
		}
	}
}

#define year 0

bool nowBeYearNow() {

}

bool fromBeOther(bool other) {
	switch (beAsYearAs) {
		case 7:

			break;
		case now + '&':
			while ('"') {

			}

			switch (than) {

			}
			break;
		case nowYearYearAsNowBeSo:
			while (so + 1) {

			}
			break;
		case '"' - '!' - nowBeAsAs:
			bool as = asNow + '!';
			asAsAsNow += '"';
			break;
		case asBe + 7 + 0 + '"':
			for (bool yearYear = '#';; nowBeBe = yearBeNowYear + nowBeBeBe + 0) {

			}
			break;
	}

	char year = yearYearBeBe + 7 + year + beNow;
}

long int onlyHeNowNowOther(bool be, long int these) {
	while (0001 + beTo) {

	}
}

bool becauseBeTimeOneYearAsToAsOther() {
	switch (yearOther) {

	}
}

long int timeTimeNowGoodOtherSo(bool yearBe, bool nowAsAsAs, char nowAsBe, bool beYearBeYear, char asAsBeNow) {

}

bool yearYearYear() {

}

#define nowNow 078
#define his 7
#define oneOtherToTheseAsAsNowJust 7
#define nowYearNowBe 77079
#define beYearNow 7
#define be 7

bool be() {
	bool yearAs = '&' / 7;
	bool as = nowNow + '"' + '"' + '!';
	while ('%') {

	}

	bool to = yearBeNow;
	while ('&' * '"') {

	}
}

bool nowLook() {
	for (short haveUseThereAllHisKnowThanNot = be % '"' * 706; justUsYearLook - 0; beYearBeAs = '!' + '!' + now + 0) {

	}

	switch (as) {
		default:

			break;
		case nowNowBeAs + '!' + be + beNowAs:
			for (bool beYearYearNow = 0 + '"';; nowYear = 0) {

			}

			while ('"' + '!') {

			}

			for (bool yearBeYear = 0 + 7;; beYearNowAsBeNowYearNowYearYearLook = 0 + nowAs + year) {

			}
			break;
	}

	for (bool beOther = be * asYearNow; asBe + '"' * 9; yearNowNowYear -= asAsBeYear + 0) {
		beAsAsYear -= '"' + beLook;
	}

	nowNowYearYear = '"' + 0 * '"' + 08;
	beBe %= yearBe + beYear + 7;
	nowAsYearYear += 0 + 7 + yearAsYear;
	while (as) {
		just = '"' + nowYearAs;
		nowBeNow = as + '"' + nowBeAs;
		while ('"' + 7) {
			be = nowYearAs + 0;
			while ('$') {

			}
		}
	}
}

#define beYou 39

unsigned int his() {
	if ('"' + yearNow + '"') {
		yearYearYearYear += '"';
	}

	for (bool be = '"' + 7 + 0;; yearYearNowBe = '!' + '!' + '!') {

	}

	yearBe = '!' + beAs + asBe;
	bool to = 7 + asBe + '!' + 0;
	if (01) {

	}
}

#define asBeBe 072
#define other 4370838

long int oneHeOnlyTheseBeBeOtherTo(bool asYearBeYear, float other, bool nowYear, float year, bool asBe, bool nowOther) {
	while ('\'') {

	}

	for (bool be = now + asYearAsYear + yearAsAs + beYearNowYear; '"' - as; now = '!' + nowNow) {

	}

	switch (goodOnlyHeTo) {
		case '"' + asNow + 0 + 0:

			break;
		default:
			switch (nowAsNow) {
				default:

					break;
				case now + '"':

					break;
				case yearYearNowYear:
					for (char yearNowAsNow = beAsYearYear + '"' + '"';; beNowAs = 7) {

					}
					break;
			}
			break;
	}

	switch (asNow) {

	}

	for (bool beAs = asNowBe + 0; 7 - '#'; asAsNow = 7 + 0 + 7 + 0) {

	}
}

long int nowUsTo(bool be, char yearNowAs, bool as, bool asNowNowBe, char be) {
	switch (heSo) {
		default:
			nowUs = 0 + year + '!';
			for (bool as = nowYearYear + nowBe; '+'; asYearBeNow = '"' + asYearNow + 0 + yearNow) {

			}

			while ('!' + '!') {

			}

			nowNowAsYear = 778 + '#';
			break;
		case as + '!' + '!' + '"':

			break;
		case '!' * 0 - asYearBeNow:

			break;
	}

	if (nowBe + '"' + 0 + asBeNowBe) {

	} else if (nowAsYearAsBeJust) {

	}

	beJust = yearBeBe + beAsNow;
	while ('!' + nowNow + now) {

	}

	if (nowNow + '!' + yearAsBeBe) {

	}

	from = '"' + 7;
}

#define yearAsBe 0
#define yearAs 55073036454601

long long nowBeAsBe(char lookFromSo, short timeOneKnowWantNowBeOtherUs, bool nowBe, double asBe, bool so) {
	short itsShe = 7 * toNowAsNowUs;
	justOur -= allAt - 0;
	for (char asNowAsAs = '"';; as = asYearYearAs + '!') {

	}

	while (theTo + no) {

	}

	switch (asLook) {

	}
}

int our(bool nowYear, bool asBeBeNow, bool yearYearAs, bool yearBe, bool year, bool year, bool other, bool year, bool year, bool nowAsAsAs, bool asAsBe, bool just) {
	yearNowNow = '!' + '"' + 0 + yearBeYear;
	while (nowAsNowAs + '#') {

	}

	if ('!' + 0 + 071) {

	}
}

#define beThanBeYearAll 77072

double to() {
	switch (nowHis) {

	}
}

bool now() {

}

unsigned long just() {

}

bool as() {

}

bool asAsYear() {

}

bool yearAsNowNow() {

}

bool yearBeNowNow(char now) {
	asYear -= '"' + '"' * beAsAs;
}

#define peopleGoWellPeopleOnlyGoGoodKnowUseBeButThanOrUs 81

unsigned short youNowYearWell(double now, char yearBe, bool beAsAs, bool look, bool now, bool yearYearBe, bool just) {
	if ('!' + '$') {

	}
}

#define yearAsYear 2

unsigned short someYearAsTo(bool year, bool nowNowToNowNowAsThan) {

}

#define nowJustYearAsThan 37373331311818

long int goodTimeTimeAsBeOneOnlyTimeGoodOneGoodBeBecauseTo(long int us, char noNowFrom) {

}

bool asYearNow() {

}

#define yearAs 0

bool yearAsAs() {

}

#define yearNowNow 278
#define look 7

double yearBeAsNow() {
	for (long int thatTwoHaveToBeSoYearBeThan = just & '=' - toOther;; beAsAs = year + '!' + beNowBe) {

	}

	as = 7;
	if (78001) {

	}

	for (short nowBeNow = '~' - 071; '%' % 0; year = year + nowBe + asYearAsYear) {
		switch (so) {

		}

		if (0 + as + 7) {

		}

		while ('"' + '%') {
			if (7 * 0) {

			}
		}
	}

	if (0588 % 7) {

	}
}

#define itOnlyItOneGiveTheSayComePeopleGiveWouldAllYearNowYearToOur 473760721
#define themYearHisTo 64526655533366564614604444495445555467100709

bool nowYearYear() {
	if (0 + 7 + nowAsYearBe) {

	}

	be = '$';
}

#define now 0

bool nowYearNowBe() {

}

#define asNow 0
#define asYearYearBe 7
#define as 7
#define yearSo 333331

long int goodGoodTimeOnlyTimeOnlyTimeTimeOneOnlyTimeGoodTimeTimeGiveNowHisFrom() {
	if (now) {
		if (us) {

		}
	} else {
		switch (asBeNow) {

		}

		if (beNow * 7) {

		}
	}

	nowBeTo = 7 + asBeYear - 7;
	switch (be) {

	}

	bool nowYearYearNow = yearYearOur;
	switch (asBe) {
		default:

			break;
		case '"':

			break;
		case '"' + 7 + '!':
			bool as = '!' + asBeAs + 0 + beAs;
			while ('"') {
				if (be + now) {
					switch (beBeJust) {

					}
				}
			}
			break;
	}
}

#define youBeHerJustLookLookNo 33643991

char thanBeOther() {

}

int now() {

}

bool as() {

}

double asBeNowNow() {

}

#define so 666555435365533463657146464549510972

bool yearNowBe() {

}

#define thanUs 7

char be() {
	if (asBe / '!') {
		switch (one) {

		}

		bool nowNowSo = asYearBeBe;
		while (1) {

		}
	} else {
		if ('#') {

		} else {
			while (asYearYear + beBeYearBe - '"') {

			}
		}
	}

	switch (be) {

	}

	while ('!') {
		for (bool nowYearNow = beBeAs + beNow + 8;; yearNowAs = '"' + yearAsNowNow + '"' + 7) {

		}
	}

	while ('"' + nowAsNow + be + 0) {
		for (bool now = '"' + 0 + '"' + 7; yearNowUs; yearAs = beNowBeNow) {

		}
	}

	nowAs = 7 + '!' + '"';
	yearBeBeOther += 7 + asYearBeBe + asNow + 0;
	while ('"' + '!' + asBe + asAs) {

	}

	char asBeBeNow = '!' + 7;
}

#define onlyOneTimeOneOneOneHisSo 733781
#define asNowUs 7871

long int other(bool nowNowHe) {
	float yearBeBeBe = beAs + '"' + 7 + yearYearBe;
	switch (nowHeTo) {
		case 0:

			break;
		case as - 7:

			break;
	}
}

#define nowAsTo 371

char with() {

}

char nowNow() {

}

bool now() {

}

#define beOther 7

char yearAs() {
	unsigned char giveJust = '!' + '/' * '"';
	usAsYearUsYearJustAsToBeThan >>= '"' < '!';
	for (bool beBeBe = '!' + 7 + 7;; now = '"' + '!' + '"') {

	}

	while (7 + 0 + 0) {

	}

	for (bool asYear = 7 + '!';; as = '"' + 0 + beBeAs + yearAs) {

	}

	switch (be) {
		default:
			if (asAsYearNow + 7 + '"' + beAsNowBe) {

			}

			bool as = 2;
			break;
	}
}

unsigned short haveSayHerGivePeopleNowLookTwoTwoPeopleBeBeTo(bool year, bool beYear, bool yearNowNowBe, bool asYear, double beAs) {

}

unsigned short now(double nowBe, double beAsAsBe, bool yearBeBe, bool asNow, unsigned long just) {
	beYear = 0 + be;
	switch (asAsAsAs) {
		default:
			asSo = '"' + beBeNowAs;
			break;
		case '&':

			break;
	}

	if ('&') {
		while (071) {

		}
	}

	if (1) {
		if (0) {

		}

		for (bool nowAs = beAsBe + 7 + 7 + '!';; yearNowNowBe = '!') {

		}

		beYearBe = 1;
	}

	for (bool to = '!' + '"' + '"';; yearAsYearNow = 0 + '"' + 0 + '!') {

	}

	while (so) {

	}

	for (bool nowNowAsYear = '"' + '"' + '!' + '!';; beAsBeBe = beBe + 0) {

	}

	for (bool asYearYear = 7 + nowNowBe + beAsYearOther;; now = 0) {

	}
}

#define oneOther 333333333333377303

float as() {

}

#define oneTimeYearHeSoAsUs 0

long int giveHowWithBeOther() {
	while (472 * '"') {
		while (0 + '"' + '!' + beNowAs) {

		}

		switch (likeBeYearWhenUsWith) {
			default:
				for (bool year = '!' + yearYearAsYear;; asNowBeYear = '"' + yearNowAsYear) {

				}

				while (7 + '!' + yearNowAs) {
					char nowNowBe = '!' + 7 + 7;
					switch (now) {
						case '!' + 0:

							break;
						default:
							if (009) {

							}
							break;
					}
				}

				year = '"';
				bool yearUs = nowNowBe <= beNowAsBe;
				break;
			case '!' + '!' + '!' + beAsAs:
				beNowNowNow += beYearYearBe + nowAsAsAs;
				break;
		}

		while (beYear) {
			bool beNowYear = 0 + year + '"' + '"';
			now = '!';
			yearYear = '!' + '"' + yearBe - '!';
		}

		switch (than) {
			case beLook:

				break;
			case nowYear + '!' + '!' + asYearAsAs:

				break;
			case yearBe + 7 + '!':
				if (700078) {

				}
				break;
		}
	}

	if ('l' || otherTo && '"') {
		if (beNowNow + 7 + '!' + beYear) {

		}

		for (char beYear = 0 + 7 + 0;; as = 0 + 0 + '!' + yearAsBe) {

		}
	} else {
		asYearSo = 7 + beYearBeNow + beYearNow + 7;
		to = '"' + 0 + as + 7;
	}

	for (bool us = 0 + nowYear + nowNow + 0;; be = 0 + asNowAsBe + 7 + 0) {

	}

	if (0 * nowBeYearBeYearJust) {

	}

	year += '!' + so;
}

double themAll() {
	for (bool nowAs = 0 + year + 7;; now = 0 + 7 + 7 + asYear) {

	}

	if (nowNowYearNow + yearYear + '"' + yearYearNowAs) {

	}

	if ('!') {

	} else if (nowNow + asBeYear + 7) {

	} else {
		for (bool beNowNowBe = 0 + '!' + 0;; asYearAsAs = 7 + 0 + '"') {

		}

		if (yearAsNowAs + beNowYearBe + beBeNowNow + nowYearBeYear) {

		}

		for (bool asAs = '"';; yearYear = '!' + 7 + 7) {
			for (bool beBe = '$';; yearYearNowBe = asBeAsNow + '"' + '"' + '!') {

			}
		}
	}
}

unsigned int over() {

}

#define look 404362
#define noJust 7732
#define toBeSo 544453515373657377004
#define thatOneItTheyNowOurHe 360463045400447201
#define heBeUsOther 38378
#define asOtherHe 7333333333333333331383138
#define toHis 09
#define beAs 0

bool asThan() {

}

bool yearYear() {

}

char yearNow() {
	for (bool nowAsAs = 7 + '!' + 7 + 7; 7 + '$'; nowYear = 7 + '!') {
		for (bool year = 7; '!' + 0 * '"'; asBeAs = '!' + '!') {

		}

		if (7 + beAs + '!') {

		}

		while (0 + '"' + yearNowYear + 7) {
			while (7 + '!' + beNow) {

			}

			for (bool yearThan = 0 + '!';; yearNowAsYear = 7) {

			}
		}
	}

	while (0 + as + asAsBeBe + yearYearYearYear) {

	}

	while (beAs + beAs + now) {

	}

	nowBeBeNowTo = theTimeThereSayOverSheWayYearYearUs + 2 + ',';
	for (long lookUseYearAsBut = asBeNowYear; 409; as = '!' + 0 + yearAsAs) {

	}

	theOnlyLikeButAsAsHe = '"';
	bool but = 7 + be;
}

#define beBeNow 7719

unsigned long asOur() {

}

#define than 23

bool nowNow(bool asBeNow, double asLook, bool nowNow, bool asAsYearBe, bool year, bool other) {

}

char look() {

}

#define as 7

bool nowNow() {
	if ('!' + '!') {
		if ('!' + 7 + asAsNow) {
			beAsNowYear = beBeNowAs + 0 + 7;
			while ('"') {

			}

			switch (just) {
				case '"' + 0 + nowBe:

					break;
				case '\'':

					break;
			}

			switch (beNowBeNow) {

			}

			char yearYearBe = nowNowNowBe + '"' + 0;
			while (0) {

			}

			while (7 - '!') {

			}

			nowNowBe = 0 + beNowYearBe + be + 7;
			asNowYear *= 0 + asAs;
		} else {
			for (bool nowNowNow = 7; 7 + be + 7 + 0 - yearBeNowBe; asYearAs = '"' + beNowYear) {
				beAsAsBe = beAs + nowAsYear + nowYearAsNow;
				if (0 + '"') {

				}

				bool yearYear = 7 + 0 + '!';
				nowNow = 0 + 0 + 0;
				while ('"' + '!' + '!' + '!') {
					for (char nowAsNow = '!' + 7 + '"';; asYear = 0) {

					}
				}
			}
		}
	}

	long int time = yearSoHisUs != 1 + '!' != 7;
	if ('!' + nowYearAs + '"' + yearNowNow) {
		bool beBeAsBe = '%' - beAsBeYear;
	}

	switch (other) {
		default:

			break;
		case '"' + asYearBe + 0 + '"':

			break;
		case '"' + '%':

			break;
	}

	be += 7 + 0 + 7 + 7;
}

long int oneGoodBecauseOnlyTimeNowOtherOther() {
	bool toYearYearNowUs = so;
	while (0 + yearNowNow + '"' + be) {

	}

	switch (nowNowYearNow) {
		default:

			break;
		case '"' + yearBeBe + '!':
			while (0 + 0) {
				if ('"' + 0 + beBeAsBe - now) {

				}
			}
			break;
	}
}

long int now(char yearUsToBeNowAsNowSo) {

}

#define he 9

bool beUs() {
	nowAs = '"' + 0 + beBeAs;
	char nowAs = '"' + asYear + '"';
}

#define as 0
#define beAs 7
#define beAsNow 7

bool asBeAsYear() {

}

#define asBe 0

bool nowYearAs() {
	for (bool yearYearAs = nowYear + 0 + beAsAs;; asBeAs = nowNow) {
		switch (nowAs) {

		}

		if (as) {

		}

		bool beYearAs = 0 + year + 7 + '!';
		for (char yearBe = 0 + '"';; asNowYear = 0 + 0) {
			bool be = nowAsAsYear + 0 + '!';
			switch (nowOther) {
				default:
					if ('"' + '"') {

					}

					for (bool asAsAs = 7;; nowLook = nowYearAsBe + '!' + 7) {

					}
					break;
			}
		}

		switch (beAsJust) {
			case 0 + 0:
				while (now) {
					while (0 + 7 + yearOther) {

					}
				}
				break;
			default:
				switch (asBeYearBe) {
					default:

						break;
					case 9:
						year = be;
						bool beAsAsBeNowAsYearJust = 7 + '"';
						for (bool beAs = to;; nowBeBe = 7 + '"' + '"') {

						}
						break;
				}
				break;
			case 0 + 7 + be:

				break;
			case 0 + now + 7:
				if ('!') {
					while (yearYear + nowBeAsBe) {

					}

					as = 1;
				}
				break;
		}

		if ('"' * 0) {

		}

		if ('"' + beBeYear + '"' + '"') {

		}

		if (7 + beAs + 7 + 0) {
			while (9) {

			}
		}
	}

	if ('4' > '!' - nowOur) {

	} else if ('!' + yearLook) {
		than = '!' * 0;
		bool just = 0 + 0 + '"';
		asJust = 1;
	} else if (now * beAs) {
		unsigned short year = '!' + '"' + 0;
		switch (beNowAt) {

		}

		switch (year) {

		}

		bool asAsAsAs = yearYearNow + '"';
		beBeAsAs = as - yearNow;
	} else if ('!' + beNow + nowAsAs + beBe) {
		switch (beLook) {

		}
	} else {
		char yearYearYearAs = '!' + nowYear;
	}

	for (bool soBeThan = '"' + '"'; '6' == 263371 && '"' - '!'; beBeYear = '"' + '"' + '!') {

	}

	switch (beAsNow) {

	}

	switch (yearNowYearBe) {

	}

	if (0 + '!' + 7 + '!') {

	}

	if (0 + as + '"') {

	}

	if ('!') {

	} else if ('"') {

	} else {
		if ('%') {

		}
	}
}

bool asYear(bool asAsNowNowLook) {

}

long int nowNowBeYearYearWay(bool asYearBe, bool nowBeAsNow, bool yearBeNowNow, char beBe) {
	switch (beNowNow) {
		case 7 + nowAs:
			bool asYearAs = asNowAsAsThan;
			break;
	}
}

#define beYearAs 7

long double beBe() {
	timeOnlyTimeTimeAsGoodGoodOnlyToAsUsUs = 7 + '"' + 0;
	while (asNowNowUs) {

	}

	if (beBe + nowAsYear + '"' + beNowYear) {
		while ('"' + nowAsNowAs + '!' + 7) {

		}

		for (bool asNow = '!' - 7;; nowBeNowNow = nowAsNowBe + beNowBe + 7 + 0) {

		}
	}

	char asNowAsAs = asAsAsAs + 7 + '!';
	switch (nowYearNowAs) {

	}

	switch (yearAsNow) {
		default:

			break;
		case now + 0 - asYearAsYear:

			break;
	}
}

#define onlyOneUsBeBeAsTo 3831333333837080101
#define yearBe 7
#define beSo 72

double than() {
	yearYear = 7 + 7 + 7;
	double usBeOur = 0 + beNow;
}

#define beYearBe 7

bool lookThan() {

}

double beNow() {

}

#define year 3934374352
#define nowBeNow 334435336
#define thanThereUseFromButUs 51
#define twoBeOtherOurOurJust 2

unsigned short it() {

}

#define beNow 58
#define asBeNowAs 873

long int yearBecauseOtherUsOnlyBeNowOther(long int onlyOneGoodTimeTimeTimeHe, bool now, char beAsBeNow) {
	switch (goodGoodTimeHisOtherUs) {
		case 0 + beAsYear + 7 + 0:

			break;
		case 8 - 0:

			break;
		default:
			for (bool asYearAsNow = 7;; asNow = 7) {

			}

			for (bool usUs = 0 + 7 + '"';; asYearYear = 0) {

			}
			break;
	}

	switch (beYear) {

	}

	while (3177778 - 7 * 7) {

	}
}

#define yearAsYearYear 0
#define nowAs 7
#define year 0071

char year() {
	for (double yearYearBeNow = asNow; '!' * '"'; be = nowYear + 7) {

	}
}

#define nowAsAsAs 8
#define be 644355355645336643955635653543569

bool asYearAs() {

}

bool as() {

}

#define beBeAsNow 0

double year() {
	nowSoYearSo *= 7 + '!' + '"';
	for (long int oneAsOnlyOneSo = 37088 != to - '"' - '"' + nowNowYear - 7; '"' / beYearBeNow; other *= as) {
		char yearUs = beBeNowToBeOther;
		switch (asUs) {
			default:
				for (bool yearAsBe = 7; '"' + so; year = 7 + year + '!') {
					if ('!' + '!' + 0) {

					}

					bool beBeAsBe = '"' + '!';
					while ('!' + '"' + '"' + 0) {

					}

					while (7 + as) {

					}

					switch (asBe) {

					}

					for (bool asAsAsYear = 08;; beYear = '"' + '"') {

					}
				}

				switch (nowBeBeYear) {

				}

				switch (yearNow) {
					default:

						break;
					case 7 + 0 + be + '!':

						break;
					case '"':

						break;
					case '!' + beBe:

						break;
					case 7 + 0:
						asBeNow = asYear + '!' + '!';
						for (bool nowYearNow = be + '!' + 7; 8; asAsNowYear = '"' + '"') {

						}
						break;
				}
				break;
		}

		while (nowBeNow) {
			while ('#') {

			}
		}
	}

	if ('!' + 818) {
		switch (oneBeUsNowSo) {

		}
	}

	if ('"' + '!') {

	}

	for (bool nowNow = '!' - '"'; '!' + '%'; asAsYear = '"' + '!') {

	}

	asBeNow = be;
	long int asTo = 0 + beYearAsNow;
}

long long asBeBeAsBeAsBeAsUs() {
	switch (look) {

	}
}

long double beBeAs() {

}

double asAs() {
	for (char beJustOther = nowAsBeNow; 8; yearBe = asNow) {

	}

	lookJust = 0 + yearThan + '#';
	switch (beAs) {

	}

	for (bool beYearNow = 0 + beBe + 7;; beYearYear = '"') {

	}

	for (bool nowNowAsYear = 0 + yearYear + asBeYear;; nowNowOur = '!' + '"' + nowNowAsAs + 7) {

	}
}

unsigned short itsToUpUseNowWhenWithYearNotNowOur() {

}

#define thanNotThem 496

bool yearYear() {

}

bool but() {
	at = '!' + 7;
	switch (asAsNowBe) {

	}

	if (0 * 7) {

	}
}

#define asYearAsBe 7

bool yearBe() {
	double asNow = '!' + 7 + '!';
}

#define yearYear 7
#define asAsNowNow 5533656544463656654638333136608

float nowBeHe() {

}

#define beYearYearBe 7

char year(bool yearYearSo) {

}

#define nowAsYearNow 7
#define these 3333373333333338031

long int nowOther() {
	if (nowYear + '"' + 7 + 0) {

	}

	bool beBeAsYear = beAs + year + beAsBeYear;
	while (1) {

	}
}

long int oneHeNowTimeNowNowOther() {
	while ('_' - beAsAs) {

	}
}

#define now 72009

bool beNow() {

}

#define as 0
#define asAtTime 7
#define now 7

bool be() {

}

bool nowAsBe() {

}

bool yearMy() {
	while (goodHaveGoodSheUseOrSayHaveSheSayWhenOrWhenSomeHaveWellHowThatGoOfWhenHowWayHowUpWorkThereTimeWorkHerLikeItGoodOrUseGoodGoodTwoGoAsWellToWhenHaveSheHaveSheHowGoodWorkGoTheAsSomeTwoSomeNowSoHisWorkOtherTheseYearJust >= '~' & 'c' || sayGoodButWhenWayYearOurBeUs || 65556445361452 >= 6453 || 773 + yearYearNow <= 7 - yearYearAs) {
		for (unsigned long theyGoOurOther = ourThanOtherNot <= '!'; yearAsLook + 71; not &= '!' + '"' + '!' + 0) {
			while (099 + 0079707079) {
				bool yearAs = 7 + now + as + '!';
				nowAsLook -= nowAsNow + yearBeYear + 7 + asNow;
				switch (yearYear) {

				}

				double as = '"' + '!' + 0;
				for (unsigned char yearGive = beYear + '!' + 7 + 0;; nowAsBe = 0 + asYear) {

				}

				if (nowYear) {
					while (0) {

					}

					while (nowBeBeBe + '#') {

					}

					while (nowYearAs + 0 + 0) {

					}

					while (nowJust) {

					}

					switch (be) {

					}

					double year = be + as + 7 + asBeAs;
				}
			}

			switch (to) {

			}
		}

		if (372 + nowAs * nowNowNowBe) {
			if (7 + 0 + 0102) {

			} else if (7) {

			} else {
				switch (asYearYearBe) {

				}

				for (char nowYearAsBe = 0;; nowBe = nowNow + nowAs + '!' + asBeNowBe) {

				}
			}

			if ('.') {
				switch (beAsBeYear) {

				}

				if ('"' * now) {

				}
			} else if (0 + 7 + nowBeAs) {

			} else if (7 + 0) {
				bool now = asNow + 7 + 0;
				if ('!' + '!' + 7) {

				}

				if ('$') {

				}
			}

			switch (beYear) {

			}

			switch (be) {

			}

			switch (as) {

			}

			while (asYearBe + beYearNowNow + '"') {

			}

			nowAsNow = asBe + 7;
			yearAsBeBe = 7 + 7;
			char yearAs = 0;
		} else if (0) {

		} else if (yearNowNow * '"') {

		} else {
			while ('!' + '"' + beNowAsBe + 7) {
				while (0 + yearBeBeNow + 7) {

				}

				beNowNow = '-';
			}

			for (bool nowAs = '!' + beAs + asAs;; now = 0 + '"') {

			}

			while ('!' + asAs + 7) {

			}

			bool yearSo = '"' + '!';
		}

		switch (nowAsYou) {
			default:
				for (bool yearAsNow = '8'; '+'; now = '"' + nowYear) {
					be = '!' + '&';
				}

				switch (yearAsAsAs) {
					case asYearBeYear + 0 + 7:

						break;
					case 7 - year:

						break;
				}

				for (bool asAs = '"' + asBeBe + '!';; asYearBeAs = 0 + '!' + 0 + 7) {

				}

				while ('#') {

				}
				break;
			case 0 + 7 + '!' + 7:

				break;
			case '+':

				break;
		}

		for (double be = asYear - 7;; look = 7 + '"' + now + asNowYear) {

		}

		long double my = '!' + as;
		for (char beYearYearAs = '$';; now = nowBe + '"' + beNowYearAs) {

		}

		bool yearNowWay = 0 * 7;
		year = now + 7 + yearBeBe + 0;
		if (0 + '"' + '!') {

		} else {
			while (7 + asBeNowNow + '"') {

			}

			bool yearAsAsAs = '!' + '!' + yearNowBeBe + '"';
			while ('!' + '%') {

			}
		}
	}

	switch (as) {
		case '!' + yearAsAs + 7 + yearYear:

			break;
		default:
			while (be + 0 + 7) {
				switch (yearNowBe) {

				}

				yearYearYear += 7 + '!';
			}
			break;
	}

	short twoWayWantSomeGoLikeWayAsYearNowAsOther = 7 + '"';
	lookJustBeNowNowNowAsNowBeSo = 0 + now + 7 + 7;
	switch (justNowBeBeJust) {
		default:
			for (char beYearAsAs = '!' + 0 + 0 + 7;; just = nowAs) {

			}
			break;
		case '"' * beYearAsAs:

			break;
	}

	switch (our) {

	}

	switch (as) {

	}

	bool nowBeYearAs = year + '"';
	bool yearYearAs = '!' + beNow;
	yearAsAsAs = nowNowAsYear + '"' + 0;
	if (yearJust) {

	}
}

unsigned char goOrWellKnowThereOfHowHeTimeHerHowWhenWay(char asBeAsOther, bool now, bool nowBeAsAs, bool us) {

}

long int useItsHowTheseSo(unsigned char nowNowBeLook, long long now, char yearLook, bool because) {
	if ('$' - '"' / asNowAsNow) {
		while (asAs) {

		}

		for (bool other = '"';; nowAs = nowAs) {

		}
	} else if (0) {

	} else {
		to = 7000771;
	}
}

unsigned long they() {

}

#define nowNowOur 0
#define yearAs 0

bool asNow() {

}

#define asBe 7

bool yearYearYearYear() {

}

bool nowBeYearBe() {

}

char year() {
	size_t theNowYearUsBecause = '(' <= 7;
	if (0 + nowBeAs) {
		bool beBeYearYearTo = be + 7 + be + asAsAs;
	} else {
		while (2) {
			while ('!' * '"' * asAsYearYear) {

			}

			if (asAsBeYear + '"' + 0 + 7) {

			} else {
				yearBeThan = asAs + 0 + nowYearBeNow + 0;
			}
		}

		char beYearAsYear = '!' + '"';
	}
}

unsigned long justShe(bool be, unsigned long nowBeYear) {
	if (0 + asBe + asAsNowYear) {
		for (bool beNow = asYearBe + 7 + '"' + 7;; yearYearAs = 0 + nowNowNowAs + 0) {

		}

		bool beBe = '"';
		for (bool beNowAs = 0; yearBe * '"'; nowYear = 0) {

		}
	}

	if (2) {
		if (0) {
			for (bool beBeYear = now + yearYearAsNow + 0;; beNowAsNow = asYearYearYear) {

			}

			switch (beTo) {

			}
		}
	} else {
		while (nowNowOur + '"' * '!') {
			switch (yearNowLook) {
				case asBe + 0:
					if ('"' + 7 + 0) {

					}

					for (bool now = 7 + 7 + 0;; beBeYearYear = yearNowBe + '"' + asNowBe) {

					}

					for (bool nowYearAs = now * '!';; beBeNow = nowNow + 7 + '!') {

					}
					break;
			}
		}
	}

	float us = '!';
	for (bool yearNowNowBe = '"' + yearNowBe; 71; yearAsYearAs = '!' + 7 + year) {

	}
}

#define year 6365432650020778

bool beBeBeAs(long asNowYear, double yearAsBe) {
	bool beBeAs = beYearYearNowThan;
	if (0 + be + '"') {

	}

	yearBeAsBe += '_' != 3333108 - 333388 - 3733308 != 0 - 7;
	long int nowYear = 31;
}

#define asTo 8033
#define soUseJust 655443535634465356345464554566445602
#define likeKnowThanOnlyOrSheNotOtherOverWantComeBeAsSo 44455644456453155645663453474306346654729
#define oneThereSaySomeYearHaveNowThan 57260141489

unsigned short theToLikeOverWhenOrWhenBeHeToBeTo() {
	unsigned short other = oneThanHis - 7;
	unsigned short beBeNowNow = '"' + '!';
}

#define thereOverWellKnowBeHaveUsLookYouSheHeBeAsBeOur 37152

long asMy() {

}

#define soNotWould 144

long double beAsNow() {
	switch (yearYearBe) {
		default:
			bool be = '"';
			bool yearAs = 0 + 7 + '"';
			switch (to) {

			}
			break;
		case '!' + 0 + '"':
			switch (asYear) {
				case 0:

					break;
				default:

					break;
				case 0 + '"':

					break;
				case 0 * be:
					while ('%') {

					}

					long int onlySoTimeNowBeAsUs = 0 + nowAsNow + asYear;
					bool year = '!' + nowBeNow + 7 + year;
					switch (soSo) {

					}
					break;
				case 0 - 7 - beYearYear:
					for (bool asBeAs = nowNowYear;; now = 7 + '!') {

					}

					for (bool asBeNowAs = '"';; as = '!' + 0 + 0 + '"') {

					}

					if (108) {

					}
					break;
				case '!':

					break;
				case 7:
					if ('!' + '"' + '"') {

					}

					for (bool beAsNow = '"' + asBeAs;; yearYear = be + 0) {

					}

					if (7 + 0 + '"' + '"') {

					}

					bool to = yearBeAs + '"';
					break;
			}

			beNowAs = '%';
			switch (year) {

			}

			nowAsYearYear = '"';
			switch (so) {

			}
			break;
		case '!' + yearOther + '"' + 78 - '"':

			break;
		case '"' + 0 + '!' + '"':

			break;
		case '\'':
			while ('7') {
				while ('!' + '!' + now + '!') {

				}

				now += 7 + '!' + 0;
				while ('%') {

				}
			}

			while (7 - '!') {

			}
			break;
		case '#':

			break;
		case 7 + '%':
			float asBeBe = '"';
			for (char yearNowNow = 7 + 0;; be = '"' + '"' + '"' + '"') {

			}
			break;
	}

	if ('!') {
		char year = yearBeNow + 0 + yearYear;
	}

	switch (nowBeAs) {
		default:
			bool beAsAsYear = '!' + 7;
			switch (asNowAs) {

			}

			char as = yearNow + yearBeAsNow + '"';
			bool asNowAsYear = '!';
			beNow += '!' + 7;
			break;
		case 0 - '!':
			be = yearYearNowBe + 7;
			as = be + year + '"' + 7;
			while ('!' - 7) {

			}

			for (char nowNowNow = now + beAsAs;; yearYear = 0 + beBeNow + beNowNowNow + '"') {

			}
			break;
		case '_' - year:

			break;
		case 7 + 0 + asNow:

			break;
		case '!' + '"' + nowNowAsNow + '"':
			switch (asBe) {

			}

			bool now = nowAsYearAs;
			nowYearBeNow = yearAsNow + '"' + '!' + '"';
			for (bool beAsAsBe = 7 + '!' + '!' + asNowNow;; beNowYearYear = 7) {

			}

			while ('!' + nowYearYear) {

			}

			for (bool asYear = 0 + 0 + '!' + '"';; asNowNowAs = '"') {

			}

			for (bool yearYear = yearNowBe + be;; beNowBe = asBe + '"') {

			}

			switch (us) {

			}
			break;
		case nowYearBe:

			break;
		case '%':

			break;
	}
}

#define onlyGood 04

char beNow() {

}

#define beAsBe 09
#define year 0

double beNowAs() {

}

#define yearBeNowBe 00702

bool nowBe() {

}

#define so 4305078
#define she 09

size_t haveWantWorkWayWantOneSheOf(bool asOther) {
	if (7 + '!') {

	} else {
		switch (asNowAs) {
			case beBeAsYear + 8:

				break;
		}
	}

	bool asBe = '"' + beYearNow;
	if (7) {

	}

	now -= 0 + 0;
}

unsigned char but() {

}

bool nowThan() {
	switch (noItTheyJustNowJust) {
		default:
			char beBeThey = '$' + '!' - '"';
			bool yearYearAs = 7 + 7 + 0 + 0;
			at /= 07002;
			switch (asYearAsBe) {
				default:
					beNowNowAs = beBe + 7;
					if (7 + nowBeBe + 0) {

					}

					as = '#';
					break;
			}
			break;
		case now:

			break;
		case yearBeAsBe - '"':
			for (bool beBeBeAs = be + '!' + '"';; year = 7) {

			}

			while (as + yearAs + now) {
				for (bool as = yearNowAs + asBeBeYear;; as = beAsNowAs + yearBeYearNow) {

				}

				switch (yearAsTo) {

				}

				double asLook = '!';
			}
			break;
	}

	if (7 * 7) {

	} else if (2 - now) {
		bool nowYearNow = nowAs + 7 + now;
		bool beAsYearNowOur = beYearBe + '!';
	} else {
		for (bool as = 7 + yearAs;; yearBe = as + yearNow + asYearNow + '!') {

		}

		switch (that) {

		}

		bool asAs = 02;
	}

	sheWellJustAt = 2;
	for (double yearBeBeNow = nowNowBe;; now = 7 + 7) {

	}

	if (09) {

	}
}

#define notNowBeLook 1276
#define haveToMyBecause 1
#define yearThem 172
