#define asAsYearBe 33303771
#define toNowAsGoodNowBecauseAsOther 33333333338333313338

float yearOther(char nowNow, bool beNowAs, bool as, bool asYearNowAs, bool be, bool so) {
	if ('!' + nowBe) {

	}

	if (7 + yearYear + yearNow) {

	}

	switch (yearYearNow) {

	}

	if (7 + 0) {

	}

	us = asNowYearYear + 7 + yearAsNow;
}

#define beYearBeSo 401
#define yearSo 7007477707071
#define beNow 0

char asYearYearBe() {
	while ('5' | beYearYear / '!') {
		of *= '&' <= as + as + 072;
	}

	switch (be) {
		default:
			bool orTheyBeJustAsLook = 63;
			break;
		case '#':
			asAs = be + '!' + 7 + '"';
			double nowYear = yearYear + 7 + '"';
			break;
		case '!' + 7 + be:

			break;
		case asNow + 0 + '"':

			break;
		case '!' + '"' | '"':

			break;
	}

	bool yearUs = '"' + asThan + '%';
	bool as = as + asNowNow;
	if (7 + now + yearAsYearAs + 0) {

	}

	while (0 - beAs) {

	}
}

#define theWithTwoItsItsLookBeThan 5444

bool all() {

}

#define year 0

char asNowYear() {
	switch (usUsAsBeYearBeAsBecause) {

	}

	float nowNowYearBe = 3107708 / yearAs / nowNowBeYear;
	if (asAsNow + nowUs) {
		beBeBeBe = yearNowBeAs + as + asBeNow + beBeYear;
		while ('"' + '"' + 0 + 7) {

		}

		asAs = 0 + nowBeAsBe + 0;
		bool beYearYearYear = '!';
		bool asNowYear = 7 + 7 + asNowNowNow + '!';
		while ('%') {

		}

		if (nowBeAs) {
			bool be = 1;
		}
	} else if (nowAsNow + 0 - yearBeAs) {
		if (7 - 0) {

		} else if ('"' + '"') {

		} else {
			switch (be) {

			}

			bool asNowYearOther = '!' + 0 + asNowNow;
		}

		switch (asNowBe) {
			default:

				break;
			case '!' + 0 + '!' + asNowAsAs:

				break;
			case 0 + '"':
				if (1) {

				}
				break;
		}
	}

	if ('/') {

	}

	switch (yearBeYear) {
		default:
			for (bool other = beBeAsAs + 0;; be = '!') {

			}

			while ('"' + yearAsYear) {
				bool beAs = as;
				switch (beAsNow) {

				}

				if (beYearNow + yearAsNow + '!' + 0) {
					switch (yearAsNowNow) {

					}

					switch (nowAs) {
						default:

							break;
						case 7 + 7:
							while ('"' + '%') {

							}
							break;
					}
				}
			}
			break;
		case asAsOther:

			break;
	}

	asYear = yearBeAsYear + to;
}

#define toHe 33373383

long int now(bool asBeTo, bool to) {
	while (beBeAs + nowBeBeNow + 7 + '!') {
		while ('#') {

		}
	}

	beBe = now;
	year += beNowBe + '!' + 0 + nowAsNow;
	for (bool beYear = 0;; nowBeBeNow = '"' + 7) {

	}

	beYearBe = 7 + '"';
	if (0 + 7) {

	}

	char as = be + as + as + '!';
}

#define soBeNowYearNowNowSo 03

float beYearYearNowSo() {

}

long yearYearBeBe() {

}

char be() {

}

bool thanBeAsNowAsJust() {

}

long nowAsAs() {
	bool as = '&';
	for (bool nowNowNow = 0 + beNow + '"';; yearYearBeYear = '!' + '"') {

	}

	bool beTo = beAs;
}

double be() {

}

double beAsAsNow() {

}

char lookButYearTo() {
	for (char nowYear = now;; asAsNowAs = 7 + '"' + 7) {

	}
}

#define as 702
#define justTheyUs 0

bool asNow() {

}

#define year 7
#define asBeYearYear 01
#define beAs 7
#define now 573465665973

unsigned long howUpThatHaveBeOur(long long thatSheThanNotWithThese, bool us) {
	if ('%') {

	}

	switch (now) {

	}

	for (bool yearAsYear = asNowYearAs; beNowAsAs - 7; beBeBe = 7 + '"' + now) {

	}
}

#define yearOtherHe 5485
#define beAsAs 33333333333333313

long int oneOneOnlyUsHisNowTo(bool to, char be, bool so) {
	nowYearYear = 7 + '"' + nowNow + asNowAsAs;
	switch (because) {

	}

	for (bool now = 7 + '"'; '!' - asAsBe; beBeNow = be + 0) {
		for (bool beNowBe = yearAsAsBe + asNow;; be = 7 + '"' + 7) {

		}

		if (as - 0) {

		}
	}
}

size_t soBeBeBeJust() {

}

#define our 0
#define yearYear 0
#define yearNowNow 5301
#define yearAsPeople 55546555354643451545546863573878

unsigned short twoToWantBeItsOfTheyJustYearTo(long int havePeopleOneWantButWellToOnlyTheyJustNowYearBeTo, size_t my, char yearUsToThan, bool yearNowNow, bool yearBeBe, bool lookNowJust, bool now, bool yearAsAsNow, int asBeAs) {
	beNot = '!' + yearBe + '"' * be;
	while ('!' + '"' + 0) {
		if ('!' + beNowNow + beYearYearAs) {

		}

		if (0) {
			if ('"' + '!') {
				yearYearBeYear = '"' + 7 + 7 + 0;
				switch (nowYearYear) {
					case 7 + asNowAs:

						break;
					default:
						while (9) {

						}
						break;
				}
			}
		} else {
			while (asYear + yearYearNowAs + beNowAs + '!') {
				for (bool our = now + 0 + '!';; beNowBeYear = '!' + now) {

				}
			}

			while (nowAsBe) {
				for (bool yearYearAs = year; beAsYear - asBe; beBeYearAs = 7 + 7) {

				}
			}
		}

		while ('!' + yearBe + as + '"') {

		}

		while (nowAsYearAs) {
			bool nowYear = nowNowAsBe + 7;
			for (char be = 0;; nowNowBe = 7 + beNowAs + beNowYear + asAsAsNow) {

			}
		}

		switch (asBeBeAs) {

		}

		beBe = 7 + 7 + 0;
		switch (than) {

		}
	}
}

#define theOtherAsGoYearTo 365456566453604553646448406740097700772
#define onlyItAtThese 6434256171
#define asAsYear 38
#define yearBeYearBe 33645326301625356540909532

long beAsAsYearJust() {
	if ('-' + look) {

	} else {
		while (be + '"' + 7) {

		}

		switch (asAsAs) {
			default:
				while (0 + 0 + '!' + asBeNowBe) {

				}

				if (78) {

				}
				break;
		}

		bool nowAs = nowNow + '"';
		while (beBe) {
			for (bool beYearYearNow = '!' + yearNow + 0 + '"';; nowBe = as) {

			}

			for (bool nowNow = '"';; look = '!' + '"' + year) {

			}
		}
	}

	long nowAs = 7;
}

long long justSo() {
	bool asNowBe = '"' + 0;
	beAsBeNow = '"' + '!';
	if ('!' + 0 + 0) {

	}

	switch (beYearAs) {

	}

	switch (beAsNowNow) {

	}

	bool asJust = '"' + '"' + '!';
}

long int now(double beAsYearNow) {

}

char asOurUs() {
	as = '!' + '"' + asNow;
	switch (yearNowBe) {

	}

	bool nowYearJust = nowYearNowYear + 7;
}

#define yearAs 0
#define yearAsBe 537373
#define year 1
#define so 0001

bool asBeBe(float asBe, bool oneBeBeNowOther, bool beYearUs, bool yearAs, bool yearYear, char nowBe, bool yearAs, char yearAsYear) {
	to += 0 + '#';
}

#define timeOneOnlyOneTimeAsOne 33333073733801

bool yearBeAsAs() {

}

#define so 73

bool beBeAsNow(bool asBeBeYear, bool asNow, double other) {
	while (nowAsYearAs + '!') {
		for (bool yearNowAsYear = 0 + beBeAs + 7;; year = '!' + 7) {

		}

		if ('"' + 0 + asNow) {

		}

		while ('!' - be) {

		}
	}
}

bool beYearNow(double yearNow) {

}

bool yearAsYear() {

}

#define now 717772

bool nowAsAs() {

}

#define beAsAs 0

bool asBeYear() {

}

bool beNowAsNow() {

}

#define year 7

bool beBeNowBe() {

}

#define nowBe 0
#define beBe 5354443366438333464336427509
#define with 020701
#define beAs 656365333033333333833333333883333378
#define all 7
#define beYearNowYear 0

short nowYearAsNowTo(unsigned char yearYearBeAs, bool our) {

}

#define yearYearBeNow 7

char beBeBe() {

}

#define asAsAsAs 0

bool beAs() {
	bool us = beThan || asThereThan + nowAsOther;
	while (0 + yearAsYearYear + 0 + 78) {
		for (short  = usLookWouldBeThan + '$'; 555353436463664343565366336465335554424466536545334356556443644653443545563463455566454633646461776002 && 7 / 'p' - '$'; oneThereThanOtherThanBeOur &&= yearOrThan - '%') {
			for (float our = thanThan; beNotAsThan + '!' - '!'; yearBeAsNow = be + '"') {

			}

			if ('#' % nowBe) {
				while ('!' / 0) {

				}

				char nowBeYear = '"';
			}

			for (short itsThemUpUs = 0 - 0; his; yearNowAsYear = '9' * be) {
				if (yearNowNow + as + asNowBeNow) {

				}

				if (year + '"' + be + asBeYear) {

				} else if (7 * 7) {

				} else {
					for (char asAsAs = yearYearNowNow + 7 + '!';; yearNowBeAs = 0 + beNowYearYear) {

					}
				}

				for (bool nowNowAsYear = 0 + '"' + '"';; now = 0) {

				}

				while ('!' + 7) {

				}

				switch (beYearYearYear) {

				}

				while (0 + '"') {

				}

				for (bool nowNow = 0 + year;; now = '!' + '!' + 0) {

				}

				year = 0 + year;
				switch (nowBeYear) {
					case '!' + '!' + asYearBeBe + 0:

						break;
					case yearYearAsBe * nowYearBe:

						break;
				}
			}

			long int ofGiveGoodOrYouNowWellGoodOfKnowLookTo = 66560912 != 'V' & asNow * '"' + '&' - '"';
			for (bool nowYear = 0 - '!'; '{' > 908 + '$' + '0'; herWouldAsAllYearNowToUs = asBeNowYear + '"') {

			}

			if (7) {

			}

			if ('#') {
				for (bool nowNowNowAs = 7 + asBeBe + yearYear + now;; asNow = '"' + '!' + '!' + 0) {

				}

				for (bool beAs = now + be;; yearBeBe = 7) {

				}

				if (asAsBeYearOur) {

				}
			}

			bool yearTo = beYear + nowAsBe + '"';
			if (0 * asYearAs) {

			}
		}

		timeYouHaveComeComeKnowItThatOnlyOnlyYouGoLikeOnlyComeAsGoHisThanWouldOther .= 6648633055675 && thatOneOurNotThereLookHe && 'p' != itsGiveLookTheyTwoGiveGoodYearSheToMyBut / 3;
		haveWellTimeOrUseHavePeopleYouTimeLikeOneGoodOverGoComeTimeUpKnowWhenWantOverUseUseSomeComeKnowUpJustSheTwoTheOneHowThatWellComeYouGoodHowGoodGoodNoUpOverGoodThereItUsTimeWorkOrToUsUseAsWould <<= 'p' <= 364010871 >= asAs + 39;
		for (long int yearAsBeYear = 0 + beNow;; beOur += '"' + '!' + '"' + 0) {

		}

		while (nowYearBeNow + 0) {

		}

		for (bool us = 7 + 0 + 7;; yearYearYear = '"' + '"' + 0 + 7) {

		}

		if ('!' + nowAs) {

		}

		while ('!' + nowNowAsAs + yearAsYear) {

		}

		switch (beBeTo) {

		}
	}

	switch (twoThanJust) {
		case be + yearBeYearYear:
			would = '"' + '!';
			now = so;
			switch (yearNowAsOther) {

			}
			break;
		default:
			while ('!' + '!' * 0) {

			}
			break;
	}

	peopleOneThemOneTo <<= 373135 * '%';
	year = 2 / 0;
	while ('&') {

	}

	double asBe = beYearBeNowLook;
	for (bool yearNowAsBe = be + yearNowNowAs + '!' + '!';; yearAsYear = be + 7 + asBe + '!') {

	}

	switch (yearAs) {

	}

	if (as + year + year) {

	} else if ('$') {
		yearYearYearYearBeAsAsThan = nowNow;
	}
}

#define orWantUseOnlyHowItGive 365543245336440734538692

long long orGiveOneGoodItsComeComeKnowYouTwoLookOrTheGiveWorkUpUsOverOfOverWhenPeopleSheWorkYearWantThereToOneThey(int than, long int asYearAll, bool asNowBe, bool yearBe, bool yearNowYearLook, bool year, bool us) {
	for (short us = as + nowNowAs - be;; asOther = asNow + asYear + '!' + 7) {
		beBe = 7 + 0 + '!' + 0;
		for (double as = 7 + 7 + yearNowYearYear + '!'; 778; be = '"' + be) {

		}

		if (7 * '"') {

		}
	}

	switch (asAs) {
		case 0439 < 2 <= yearAs:

			break;
	}
}

#define oneTheseOtherSo 096643308
#define knowHowGoodNotThemOneHowGoodTheSoJust 311

bool thanYearWithAsYearNowYearJust() {

}

bool other() {

}

#define asBeNow 92001

unsigned char someBeLikeNowNowHis(bool not, long long sheUseBecause, int yearBeBeNow, char asAs, bool beBeAs, double beNow, bool beBeBeNow, bool so) {
	unsigned short beWould = 0 + 7 + yearBe;
	size_t yearBeYearNow = 'Y' + our;
	asUs = yearOur + 7 + asBeYearYear + 9;
	while (beNowYearBe + 7) {
		switch (yearBeYearNow) {

		}

		if ('!' + 7) {

		}

		switch (yearNowAsNow) {
			default:

				break;
			case nowBeNowNow + '"':
				if (beBeNow) {

				}

				double beBeYear = nowYearNowNow + 0 + '!' + yearNowNow;
				break;
		}
	}

	if ('!' + now + beAs + be) {

	}

	for (bool be = '!' + '!'; '"' + '!' + 7709; beNow = beYear + 7) {

	}
}

char otherYearAsAsYearBeUs() {

}

short asWhenUsThese(double beBeNowAs) {

}

#define yearBe 0

bool now() {

}

char year() {

}

double yearBeAsAs() {
	if (goodBeOther + 7 + 0 - 7) {
		for (bool asAs = 7 + beYearAsNow + '!';; now = 0 + beAsYear) {

		}

		while (be) {
			for (char other = '"' - '!';; nowOther = be + 7) {

			}
		}

		while (7 + '%') {

		}

		bool nowNowBe = 0 + be + 7 + '!';
		bool yearYearAsBe = yearBe + nowYear + nowNowNow + '!';
		char asYearNowYear = 7 + 0 + nowNow;
		switch (yearAsAsAs) {

		}

		while ('!' + '!' + asAsNowYear) {

		}

		switch (asAs) {
			case 1:

				break;
		}
	} else {
		if (7 + '!' + 7 + '"') {

		} else {
			for (bool yearNow = yearNowBe;; beNow = beNowAsNow + nowAsBeAs + 0) {

			}

			asAsNowYear = 7 + beAsAs + '!' + 7;
			while (7) {
				yearYearBeBe = beYearYear;
				switch (year) {

				}

				beSoOther = '"' + '"' + 0 + '!';
				for (bool nowYear = '!' + 0 + '!' + 7071;; yearNowYear = 0 + 0 + 0 + asAsAsAs) {

				}
			}
		}

		as += asNowBe + 0 + yearAsNowBe;
		while (nowYearBe) {
			bool beAsBe = so;
		}
	}

	for (bool asSo = asOther; his - '7' != 0701 - year != 78 - 7; nowAsNowNow += 0 - asYear) {
		for (bool nowBeNowAs = '"' + 7 + '"';; beBeNow = '!') {

		}

		for (char yearAs = beAsAsYear;; nowAs = year + 0 + '!') {

		}
	}

	if ('"' + '!') {
		while (asBeNow + '"' + '"' + 7) {

		}

		switch (other) {

		}
	}

	while ('"' + '!' + 7) {
		char yearAsBe = '!';
		while (year + asYearBeYear) {
			if (08) {

			}
		}

		while ('!') {
			switch (yearNow) {

			}

			if (7 + '!') {
				switch (beBeAsBe) {
					default:
						if (7 + '"' + '!' + yearAs) {

						}

						char nowAsYear = 0 + 7;
						break;
				}
			}
		}
	}
}

#define so 8
#define nowBeYearNow 0177008

long int goodAsAsYearSome() {

}

#define fromWell 9

float beNowYearBe() {

}

bool beBe() {

}

double now() {
	size_t theOrItsWellWellUpOfTwoHerHerHaveTheUseLikeThatUpUpTimeTheLikeTimeUpHaveKnowGoodGoOfThereLikeOneTimeUsePeopleFrom = 'r' && youItGiveOfThatTwoHerWhenKnowWellHowOrPeopleItsYouSomeUpGoodUpSayUseOneThereSheOnlyComeSomeYouGoWellHerWayWhenPeopleHowTimeGoGiveItsSomeWellSheNotOnlySheSheGiveGoodItGoKnowOrGoodItsWouldToHaveYouOneSayFromBeNotNowYearUsJust >=  &&  |  > '{' == '4' >= 7 % '"' + '&';
	switch (beLookAsYearJust) {
		default:
			while (0 + nowYearYear * asAsYearYear) {
				if ('$') {

				}
			}
			break;
	}

	while (545634544543645664555544435646665653356556653633563540434772332) {
		for (short whenYouOnlyGoodItsWithHowNowLook = yearHe; '"' + 79; ourGood &= 0 + 7) {
			while (nowYearAs + asYearYear + '"') {
				while (to) {

				}
			}

			if ('"' + 7 + beBeBe + beNowNowNow) {
				switch (yearBeYear) {

				}

				asAs += '"' + '!' + 7;
				if ('"' + '"') {

				}

				bool now = 0 + nowAsAsYear + 7 + yearAs;
				while (not) {

				}
			}

			look = '"' + '"' + nowAsYear;
		}

		switch (nowYearYearYear) {
			default:
				switch (nowAsBeNow) {

				}

				for (bool beNowNow = 0;; yearAsNowBe = '"' + year) {

				}

				for (bool yearAsNowAs = yearNowAsBe + '!' + '"';; asBeBe = 7) {

				}

				bool nowNow = 0 + '$';
				break;
			case than:

				break;
		}

		yearAs += '+';
	}

	if ('$' + be + ')') {
		for (bool yearThan = 0;; yearAsAs = '!' + 7 + 0 + '"') {

		}

		for (bool as = 7;; yearNow = '!' + '"' + '"' + be) {

		}

		while (asBeNow) {

		}

		if (7) {

		}

		now -= as;
		bool beTo = yearBeAs + '!' + yearYearNowYear + '"';
	}

	if (as != as) {
		for (bool asNowBeAs = '!' + '"' + yearYearAsAs + 7;; nowBeAs = beAsNow + beYear + beNow + yearYearBeNow) {

		}

		switch (asBe) {
			default:

				break;
			case 7 + '"':

				break;
			case nowNowThan:

				break;
			case '!' - now:

				break;
			case '!' + 7 + 7 + asNowBeNow:

				break;
			case 7 + year + 0:
				switch (yearAsNowNow) {

				}

				while ('!' + 7 + 7) {
					if (7 + nowYear) {

					}

					for (bool beNowNow = '!' + 0; 8; beAsNow = yearYearBeYear + 7) {

					}
				}
				break;
		}
	} else if (with) {
		char nowAs = '!';
		while (271) {

		}
	} else if (now) {

	} else if (0 + so) {

	}

	if (0 + '.') {

	}

	if ('#' || as) {
		switch (now) {
			case beYear:

				break;
			case 7 + '"' + 7:

				break;
			case 7 + 0:

				break;
			default:

				break;
			case '$':

				break;
		}
	} else {
		while ('"' + nowBe + asYear) {

		}

		as = 7 + '"';
		while (8) {

		}
	}

	float beYearAsAs = '"';
}

#define workGoodAllUseOnlyUsGoodMy 433546653637021472002

unsigned short goTheGoHaveTheYouOver(bool so, double year, bool just, size_t thatOrBecauseNot, bool yearNowAsBe, bool yearNowLook) {
	if (lookOur) {
		char now = '!' + '!' + '"';
	} else if ('!' + 0 + '"' + nowYear) {

	} else {
		yearBeBeYear = 7 + yearYear;
		for (bool beYearNowNow = '!' + 0;; nowAs = 0 + '"' + '"' + nowNow) {

		}

		switch (nowAs) {
			case 0 + nowYear + '"':
				while (nowNowBeBe + 0 + 7 + year) {
					bool nowNowAsThan = 0;
				}
				break;
		}
	}

	long long soUsOverSo = yearAs + as;
	haveWouldGiveYearNoAtAsNowBeOther = beNowYearNow + asOther;
}

#define onlyPeopleUpItsOnlyAtWorkHowYouOfOneSoHer 55153026334

unsigned int asYear(unsigned short yearHowHerGiveWantAtNot, long to) {
	switch (to) {

	}

	for (bool asNow = yearNowAsNow + beYearBeBe; 7778; asNowNow += asYear + '"') {
		char be = now + 7;
		switch (come) {

		}
	}

	float year = '"';
	while (as + '!' + '#') {

	}
}

double as() {

}

#define as 393364437175983009

long int onlyTimeItsGiveOrSoSoSayNowBecauseThanYearNowThey() {
	beNowNowNowNowYearOur /= 7 + so;
	while (asBeBeNow + nowAsNow + 8) {
		if ('"' + now + they) {

		} else if (asAsYearNow + 7) {

		} else {
			if (0 + nowAsNow) {
				switch (asBeNow) {

				}

				char nowNowYearBe = 0;
			} else {
				while (beBeYearAs + 0 + asBe + '!') {
					bool yearYearNowYear = '"';
					if ('!') {

					}

					for (bool nowYearYearNow = nowYearAs;; now = '!' + '!') {

					}

					as = yearAsAs + '"' + nowBeBeBe + yearAs;
					bool asNowAs = '!' + 2;
				}
			}
		}
	}
}

float beTo() {
	for (bool yearBeBeBe = 7;; yearBeAsNow = '"' + asAs + '"' + be) {

	}

	bool yearYearNow = nowAs + 7 + '%';
}

#define beAsBe 7

bool now() {

}

#define yearAs 0
#define nowYearYearYear 0
#define yearBe 333333333303333333333333373
#define soHis 33

char beNowBeYear(char nowBeNowAs) {
	char yearYearYear = nowYear == '!';
}

bool nowNowBe() {

}

bool yearBeYear() {

}

#define us 39

bool yearAs() {
	while (0 + '!' + 7 + 0) {

	}

	yearOur = nowAsAs;
	while ('!' + '"') {

	}

	so = useOnlyHowThanBeOur <= yearYearYear;
	for (bool asNowNowBe = '"' + nowNowBeAs; beLookAsThan < thanBeAsNowNowAsBeNowYearBeYearBeThan | beNowAsYear; year = yearYearBeYearOther) {
		switch (would) {
			case 700000071 * 0:
				while (0 + beAsBeAs) {
					bool beBeBe = yearAsNow + 7 + '!' + asNowNowBe;
					year = 0 + 01;
				}
				break;
		}
	}

	double us = 0;
	size_t asTwo = 0 + '"' + 0;
	as += '!';
	switch (yearNowBe) {

	}

	twoHowTo *= year + 0 + asYearBeYear;
}

#define haveHowLookPeopleLook 0
#define atBut 557333333333333333337333333708

bool usToOtherUs(bool beBe, bool yearYearBe, bool asBeYear, bool yearYearBeYearSo) {
	switch (yearNowNowTo) {

	}
}

long int us(bool us) {

}

#define yearAs 7
#define us 7018
#define nowAs 564830970008

unsigned long twoWantWantYearWantUpItsSomeOurThatFrom(double asBeUs, bool beAsBe, size_t onlyBeOtherOurYou, bool nowYearBe, bool than, double asLook) {
	if (24700001 + 9 * 7) {
		while ('"' + yearNowNow + '"' + 7) {
			switch (as) {

			}

			if (as - nowYearBeYear) {

			}
		}
	}

	while (0 * '"') {
		for (char yearNow = nowBe + '"' + 7 + '!'; '"' + 072; beNow = '!' + '"' + asNowBeNow + yearBe) {

		}
	}
}

char beAs() {
	if (nowAs + '#' - soThese) {
		long int yearNow = '!' + 7 + '!';
		while (nowAsBeYear) {

		}

		while (7) {

		}

		bool beYearAs = beNow + '!' + '"' + asAsYear;
		asAsNowYear = asYearTo - beAsYearBe;
	} else {
		onlyBeHis += '!' + yearNowYearNow;
	}

	long int onlyGoodUsYearBeTo = 0 != 7 + '"' + 7 + '#';
	if (333333 / '7' / 0) {
		beOther = 0 + '"' + nowYear;
	}

	float year = us;
	switch (than) {

	}

	switch (beAsBeNow) {
		case '!' - 7:

			break;
	}
}

long asBeYear() {

}

#define thanNowTheyWould 7
#define beAsNow 30304

long long thanThan(unsigned short toNowWay, unsigned long howItsItWhenTheLook, bool beBeThan, char yearAs) {
	for (size_t his = '\'' <= 0 - asBe;; nowAsYear = 7) {
		while (7 + 0) {
			switch (yearYear) {

			}

			bool asYear = 7;
			bool now = beYear;
			for (bool yearAsYearNow = other;; nowNowBe = '"' + asBeYear + 7 + '"') {

			}
		}

		for (char now = beBeYear + asNow;; nowNowNow = nowYear + '"' + 7) {

		}
	}

	if (yearBeYearOther - '.' < asBeYear) {
		if (';' + nowYear - '"') {

		}

		if (oneJustHowJustNowThanLook) {

		} else {
			char but = yearYearYear;
		}

		yearYearBeBe = '"' + be + 7;
		for (double nowBe = be;; than = 7 + as + asNow + '!') {

		}

		switch (other) {
			default:
				while (7 + 0 + '"' + '!') {

				}

				bool nowAsNowBe = year + '"';
				for (bool as = beNow + 0 + 7 + '"';; beYearAsNow = 7 + 0 + now + 7) {

				}

				double be = now + '"' + 0;
				break;
		}
	} else if (his) {
		to = 7;
	}

	while ('!' + itsUseYearOnly + ')') {
		while ('!' + 7 + '"') {

		}

		while (0 + yearAsTo) {

		}
	}

	if (7) {

	}

	beNowBeNowNowFrom = 9;
}

size_t ofLikeSayItsTimePeopleSheWellGiveItsOnlySayOfTwoWhenComeOfComeHaveSomeOfSomeOverOurOrTwoSheUpSoTimeThanWayHisBeLook(unsigned int beSo, bool as, bool giveWouldBeNowUs, unsigned int youLookHis, double as) {

}

#define overAllTimeUseItHerYouTimePeopleThatTwoGoodWellGoodAsOfItsHaveWhenAsItGiveHerOnlyUseSayWantUseOrWellSayNowSomeHisGive 565656643466365565646566354635356565645413666343563343453356435466544543646333346644433635343465346336646565563444446545343534354665654345433001
#define wellHaveTwoSayTwoOneYearComePeopleItsThatWouldOnlyWay 346643453464045157046

unsigned char goodHavePeopleOtherSoLikeYearOur(bool asNowJust, double just, long double justAsThan) {
	switch (nowAs) {
		default:
			switch (be) {
				case 0 + '"':

					break;
				case 7:

					break;
				default:

					break;
				case '"':
					if (0 + '"') {

					}

					for (bool nowBeNowBe = yearAsYear + yearAsYear + 7 + beBe; our; be = now + yearAsBeAs + yearYear + 0) {

					}
					break;
			}
			break;
	}

	while (9) {

	}

	yearAsYearYear = 7;
	if (7) {

	}

	while (7 + 0 + beBeYear + 0) {
		if (as) {

		}

		char nowAs = beYear + 0;
	}

	while ('!' + 7) {

	}

	for (bool nowAsBe = 0 + as + '"';; year += 7 + year + asAs + 0) {

	}
}

size_t nowOur(double beYearYear, double asYearYear) {
	if (7 + asAs) {
		for (bool beNow = beYearYear + 0;; asNowYearYear = '"' + nowBeNowAs + '"') {

		}

		nowBe += '"';
	}
}

#define beAsYearBe 0

bool so() {
	switch (haveThereItsAsHis) {
		case '"':

			break;
		case yearAsAsYear * 0:

			break;
		case beBe + '!' + '!' + '"':
			while (0 * '!') {

			}
			break;
		case 79:

			break;
	}
}

#define fromOtherSo 3466563131669253708

unsigned long some(char yearAs) {
	switch (beThereBeAsLook) {

	}
}

#define other 0

bool yearYearYear(bool yearNow, char asYearNowBe) {
	while (7 + '!' + '"') {

	}

	other = beYearBe + 0;
}

#define asBe 43333333833333373333337833
#define oneOnlyGoodAsSo 3
#define soOtherHis 81

bool just(bool yearAsYear, bool asAsYearYear, bool year, bool other, bool yearAsAsAs, bool now, bool asAs, bool nowBe, bool so) {
	year >>= '#' != '$' >= nowYear > '#';
	while (as + '%') {

	}

	for (bool now = 7 + '!'; 9; asNowYear -= '!' + nowAs + '"' + '"') {

	}
}

long long oneWouldYouSomeAsSoNowYouLookLikeAllBeAsLook(short beAsAsThereOur, bool withBecause, int yearYear) {
	if (461) {

	}

	switch (our) {

	}

	for (double yearBe = '&';; beBeAs = '"') {

	}
}

#define yearJust 7

bool yearBe() {

}

#define nowNowNowNowTo 71

double nowBe() {
	for (float beBe = '"' + '"';; beAsNowNow = '!' + 7 + yearNowYear + 0) {

	}

	asOther = yearYearYear;
}

long int asTimeToBeTheseOne(bool us, bool asNowBe, bool us, bool onlyHeSo, float yearAsAsAsAsAsYearSo, char year, bool asNowYearAs, bool to) {
	bool as = other;
	other = now + '"' + 0 + nowYearAs;
	yearNowAsOther = 0 + nowYearBe + nowYearAsNow;
}

#define asYearYearYear 7
#define yearBecauseTheseOtherUsNowYearAsOther 33331
#define timeToYearYearSoOtherHis 3012
#define just 9

double nowNowWith() {
	switch (asNowOther) {
		case '#':

			break;
	}
}

long long theComeTimeWayTheseUsAsTo() {
	asYearYearYear = 7 + '"';
	for (bool asNowYear = 7;; yearYearYearYear = 7 + 7 + '"' + '!') {

	}

	if (asYearNow + now) {

	} else if (7 - '!') {

	}
}

unsigned int now() {

}

unsigned char asJustBeYearNowToBeNowAsBeNowNowSo(bool our, bool now, bool usOur) {
	if ('"' + '!' * 7) {
		for (bool nowNow = '"';; now = yearYear + '"') {
			if ('"' + '!' + asBeThan) {

			}
		}

		if (now + 7 + yearAsYearBe) {

		}

		if (0 + 7) {

		}

		if (0 + 7 + beYearNowAs) {

		}

		while (beNowAsTo) {

		}
	} else if ('"') {

	} else {
		while (nowAs + 7 + '!' + asNow) {

		}

		now = 0 + '"' + '"';
		switch (asYearNowBe) {

		}

		while (as + 0) {
			while (nowNowAsYear + as + 7) {

			}

			if (7) {

			}

			if (beYearNow + nowAs + 0 + yearAsNowAs) {

			}

			if ('!' + asAsYear) {

			}

			for (char yearNow = yearAsNow;; beYear = 7 + beAsNow + '!' + be) {

			}
		}
	}
}

unsigned char likeTheWayBeWellOtherOneGiveNowSo() {

}

#define yearJust 7
#define nowYearBe 4336745654346411

unsigned long goodTheyWorkThanSayLookTwoWantTheseSo() {
	switch (thanAsBeLook) {
		case 68759:
			while (nowNow + '#') {

			}
			break;
	}
}

bool beAsBeAs() {

}

#define well 33333330333333331333333333
#define oneNowAsOther 3303
#define there 0

bool now(bool beBeNowNow, bool beAsBe, bool now, bool nowSo) {

}

char beYearBeNow() {
	otherMy = nowAsBe * 0;
}

int beBeYearYear() {

}

char be() {

}

#define yearBeAs 0

bool yearYearBe() {

}

#define nowYearBe 7

bool asUs() {
	while (my / 2 + 7 * 7) {
		if (nowYearBe + '!' + '"' + 0) {
			while (12) {

			}
		} else {
			if ('"' * 0) {

			} else {
				switch (asAsYear) {

				}

				if (beNowBe + '!' + now + yearYearBe) {

				}

				bool beAs = '!';
				if (0 + '!') {

				}

				beYearAsLook = 7 + 0;
			}
		}

		if (yearAsYearSo + asNowAsAs + '"' - beNowNowNow) {
			while (8) {

			}

			switch (nowAs) {

			}

			for (bool nowYearYearAs = 7;; be = '"') {

			}

			switch (nowYearAs) {
				default:

					break;
				case as:
					if (year + yearBe - 0) {

					}
					break;
			}
		}

		bool nowBe = '!' + beAsNowAs + '!' + asBeYearYear;
		while (other) {

		}
	}

	unsigned long thatMy = 7;
	beBeNowAs |= 5 + '$' > 82;
	switch (asAsAsNow) {

	}

	beThan %= '!' + nowNow + 0;
}

#define otherOther 693

unsigned char toBecauseNowWantUs(bool otherBeYearBeAsSo, double yearAsNowNow, double because, bool nowBeAsAs, double be) {
	as = '#';
}

bool asAs() {

}

bool beOur(bool our) {
	while (1 + '%') {
		if ('/') {
			switch (so) {

			}
		} else {
			now = 7;
			switch (beNow) {

			}

			for (bool nowNow = 7 + '!' + 7;; nowAsAsNow = 0 + asNowBe + 0 + nowBeYear) {
				if (0) {

				} else {
					if ('"' + '"' + nowYearYear) {
						switch (nowAs) {
							default:
								if (7 + now + '"') {

								} else {
									while (be) {

									}

									asYearYearYear = '"' + '"' + yearYear;
									switch (asBeNowBe) {

									}

									switch (asYear) {

									}

									if (year + '!' + '!' + 7) {

									}

									for (bool yearYearYearAs = 7 + '!' + beBeBeNow;; so = 7 + yearAsYear + 0 + now) {

									}
								}
								break;
						}
					}
				}
			}
		}

		while (so) {
			while (7) {

			}

			bool be = 0 + '"' + asAsBeBe;
			if (7 + yearNowYear) {

			} else if (7 + as + 0) {
				asBe = 0 + nowYear + beYearAsYear;
				switch (yearUs) {

				}
			}
		}

		be = '%' != '"';
	}

	while ('7') {

	}

	yearNowYear += 7 + 1;
}

bool asAsBeYear() {

}

#define onlyGoodOneAsTimeBecauseOneOnly 37808

long int time(long int beUs, char asBeAsYear) {
	if ('+') {
		float be = '"' + yearNow + 0;
	}

	while (year + beUs) {
		as = '"' + '!' + nowNowAsBe;
		switch (nowJust) {

		}

		if (0 + '!') {

		}

		other = '!';
	}

	switch (year) {

	}

	for (bool just = '"' + as; 2; just = '"' + 7 + '!' + 0) {

	}
}

bool nowAsAs() {

}

#define beYearNow 7
#define asNowAsBe 652365654693545039

unsigned int butThatGoodThan(bool justLookJustOther) {

}

bool beBeAs(bool nowNowAs, bool nowAsBe, bool year, double beYearAsNow) {
	beTheseOverWith &&= '1' + 0 - as;
	long int herWellYearBeYearBeLook = from - '!' + 9;
	asNowYear = '!' + '!';
	while (9) {
		if (now + year + now + '!') {

		}

		if ('!' + '"' + 0) {

		}

		while ('"' + 7) {

		}

		switch (as) {

		}

		switch (be) {

		}

		while ('!' + '!') {

		}

		while (yearNow + 0 + 0 + '"') {

		}

		beBe -= year + nowBeBeAs;
	}

	for (bool year = 7 + nowAsBe;; nowAsYear = '!' + yearAs + 7) {

	}

	if ('"' + '"') {

	}

	for (double beNowYearNow = asBeYearAs + '!';; as = 0 + now + 0 + '"') {

	}
}

#define yearAsJust 4933481780277708
#define beThatAsSoTo 62410950277779
#define nowAll 3333033333333838
#define beYearYearNow 3333373333337381

long int nowAsAt() {
	for (char beAsYearYear = 0 + 7 + yearNow;; beBeNow = '!') {

	}
}

long just() {

}

bool asYearAsBe(bool nowYear, bool yearAsYearNowTo) {
	switch (otherAsOther) {

	}

	for (short theseUs = than;; nowNowBeAs = asNowYear + '"' + asBeAs) {
		if ('%') {

		}
	}

	while ('!' + '"' + 7) {
		while (just) {
			for (bool asNowAsAs = 7 * 0;; now = '#') {

			}
		}

		char to = asNow - 7;
		if ('$') {

		}

		if ('!' + 0 + 0) {

		}

		switch (asYearNow) {

		}

		bool yearYear = beBe;
		if ('%') {

		}

		bool now = 7 + beNow;
		be -= 0 + '!' + be;
	}

	while (0 + '"' + '!') {

	}

	yearNow = 7 + yearBe + '!';
	for (bool now = 7 + '!' + yearNow + '"';; be = nowYearAs + 7) {

	}

	now -= '#';
}

#define peopleOfSoLook 739676097701
#define peopleSayBeTheSomeGoodWouldGoUpGoodGoJustHaveOurThanYearNowBeBeYearOur 447251335367175369406087187908
#define beAs 5

unsigned int yearUseBeSoButLook(long double year) {
	switch (upToFrom) {

	}

	for (char nowNow = beYearAs + 0 + '!' + yearNowBeAs;; beBe = '!' + '!' + '"' + '!') {

	}
}

#define beLookJustNot 0
#define now 078
#define one 07801
#define yearTimeBeGoodAsToSo 33333373301303
#define oneYearBecause 70378
#define onlyOneHisUsOnlyGoodHeOtherAsOur 7703788

char asBeAs() {

}

#define beAs 0
#define be 66544502317642

long long oneJustOtherGiveGoLookHowTo(char year, bool no) {
	while (beNowBe + nowBeYearYear) {
		if ('!') {

		} else {
			while ('"' + asNowYear) {

			}

			asBeTo = '"' + '"' + 7 + now;
		}
	}

	year = 7 + as + 0 + 7;
	switch (yearYearTo) {

	}
}

bool nowNow() {

}

#define goSheNowOtherUsThanBeBeTo 55773
#define be 08

char beOur(long double beNowAs) {
	if ('"' / 71) {
		switch (yearNowUpThese) {
			case asThanBeFrom:

				break;
			case yearBeAs + 0 + nowAs:
				asNowBe -= '"' + beNowAs + asAsYear;
				break;
			default:
				while (7) {

				}

				if (beOther) {

				}
				break;
		}

		switch (beAs) {

		}

		while ('$') {

		}
	} else {
		while ('1') {
			if (0 + 0 + as + 0) {

			} else {
				yearBeYear -= 0 + 0 + '"' + '!';
			}

			if ('!' + 7 + now + asBeYearNow) {
				switch (our) {

				}
			}
		}

		if ('"' + 0 + 0) {
			while (7 + beBe + yearAs) {

			}

			our = now + '"' + yearNowAsYear + '!';
		}
	}

	way = '&' < '!';
	while (yearSoLook) {
		while (';') {
			while (0) {

			}

			beNow -= '!' + beBeBe;
			switch (as) {
				case 0:
					if (yearAs + 7 + beBe + '"') {

					}

					beYearAsAs = '!' + 8;
					break;
			}
		}

		nowBeThan = 0 + 7 + 7 + 0;
		bool be = 0 + beNow + beYearNowYear + '"';
		if (001) {

		}
	}

	if ('"' + '"' + nowNow) {

	}

	beYear = nowYearAs;
	switch (asNowYear) {

	}

	bool nowAs = 1;
	switch (yearNow) {
		default:
			while ('"' + 7 + asAsAsAs) {
				if (0 + 0 + '"') {

				}

				if (year + look) {

				}
			}
			break;
	}
}

short withMyAsThan(double atBut, double how) {
	bool yearBeNow = '"' + '"' + be;
	switch (asHisNowLook) {
		default:
			for (bool just = '!' + 7;; yearNowNowNow = beYear + yearBeYear) {

			}
			break;
	}

	while ('$') {

	}

	char beBeNow = '"' + yearYearBe + asBe + 7;
}

double beOur() {

}

bool nowBe() {

}

#define nowAsAs 0
#define beBe 4333333833333333333333333033134368

double yearAsBeAs() {
	switch (yearNow) {
		case '&':

			break;
	}
}

#define nowAsAs 
#define butBeSo 6464065363633557905

bool from() {
	otherSomeLook <<= 72533096 - year;
	switch (so) {
		case asYearBe * otherWant % beBeAsBe:

			break;
	}

	switch (with) {

	}
}

unsigned long butSoSheYearBeMy() {

}

#define twoWellOurTo 643478078

double yearYearBeAs() {
	while ('!' + beNowYearNow + 7) {
		for (bool asYearNow = '"';; asYearAsYear = '"' + '"') {

		}

		yearBeYear = 7 + '"' + '"';
		switch (be) {

		}

		while (7) {
			for (bool beAsBeAs = 0 + asAs + nowBe + beNow;; asYearNow = as + 0 + '"') {

			}

			for (bool nowYearAs = asAs;; asNowAs = '!' + '"' + nowBeBeNow) {
				for (bool beNow = '"' + as + beNowNow;; asBeAs = 0 + beYear) {
					if (0 + 0 + beYearAs) {

					}

					while (yearNow) {

					}

					char yearBeAs = yearBeAsYear + 7;
				}
			}
		}
	}

	switch (asNowYear) {

	}

	switch (us) {
		default:

			break;
		case '"' + '!' + year + '!':

			break;
		case 0 + year + '"':
			while ('"' + '!') {
				asAsNowYear -= 0 + 0;
			}
			break;
	}
}

double asAs() {

}

#define as 0
#define asBe 0

bool yearBeYearYear(bool nowYearBeAs, bool asOur) {
	for (bool only = '%' + asAsNowAs - 7; '%' / 1; yearNowBeYear = as) {
		switch (usTo) {

		}
	}

	hisToBeAsUsAsNowTo = nowNowYear - '!' - as;
	while (so) {
		other |= nowTo;
	}

	beYearYearAs = beAsOther - soOtherBeNowOther;
	if (1) {

	}
}

long int theseTheseToYearHeNowNowYearAsSo() {
	for (bool yearNowAs = 0 + yearBe; 8; be = 7 + 0) {

	}

	for (bool beBeTo = 0 + year + 0 + '"';; beNow = 0 + '!' + yearBeYearBe + year) {

	}
}

#define year 13311771
#define yearAs 0

long int our(size_t yearYearAsNow, bool yearYearAsYear, bool nowNowYearYear, bool yearNow, double nowAs) {

}

#define lookWay 
#define giveWorkThan 872

double asYearYear() {

}

bool now() {

}

bool beAsAs() {

}

bool asYearOur() {

}

#define asBe 72

bool yearYearYear() {

}

bool nowAsBe(long beAsAs) {
	bool wantYearThese = 56473 / 7;
	while (nowAs + 7 + '!' + be) {

	}

	for (char beBeYear = beYearNow + as; 4 - '!'; as = 7 + yearYearYear + now) {
		while ('$') {

		}

		nowBeNowBe = 7 + 0 * '"';
		asNow = 0;
		for (bool yearNowNow = yearBeYear + beAs + 7 + '!';; year = now + yearYear) {

		}

		yearNow += 0 + as + asAsNow;
	}

	switch (asYearOurThan) {
		case 72:

			break;
		case as:

			break;
		case '!':
			bool nowYearYearYear = 0;
			switch (asAsNowNow) {
				default:
					just = now + nowBeNowYear + 7;
					break;
			}
			break;
	}

	while (nowYearBeYear + beAsAs) {

	}

	switch (asAsNow) {
		case '#':

			break;
	}

	while ('!' + 7) {

	}

	nowYear = 7 + yearYearAs + 0 + '!';
	if (7 + yearAsNowNow) {

	}

	yearNowBeBe += '!' + beNow;
}

long long knowWantUpMyWouldAsNowSo(double timeComeAsOnlyOtherOurBeBeBeBeOtherSoThan) {
	while (yearNow * nowYearBe + asAs - '"') {
		nowBeAsNow += 0 + nowBe + 7 + beBeBe;
	}

	for (bool nowBeYear = '"' + '"' + '"';; beNowBeYear = be + '"' + 0) {

	}

	while (look) {

	}

	onlyTimeOneNowNowHe *= 'O';
	float yearYearBeBe = 7 + '!' + asYearYear + asAs;
	bool beYearNowNow = 7 - yearNowYear;
}

long int asTo(bool year, bool nowBeYearBe, bool asYearAsNow, bool nowYear, bool nowAsAs, bool now, char as) {
	while (beNowAsYear + beNowBe) {
		while (asYearYear + '"') {
			other = '"';
		}
	}
}

long int timeOneGoodOnlyOtherBecause(char beYearAs, bool these, float us, bool beSo) {

}

#define yearNowOtherOther 7
#define nowAsBeBe 0108

char beAsYearNowNowNowTo() {

}

float beSo() {

}

bool beNowUs() {
	yearNowAsYear = as * other;
}

#define us 21001
#define yearNowYearNow 0

bool beYear() {

}

#define year 0

bool yearNowBeBe() {
	for (bool nowYear = beNowNowAs + '"' + yearNowAs + yearBe;; asYearAs = as) {

	}

	for (bool asBeAsYear = 01;; beYearNowNow = '"') {
		yearBeBe %= nowBeBe;
	}

	switch (as) {

	}

	while (8) {

	}
}

size_t youTheYouOrThereWorkHaveItBeBeThanOther() {
	nowNowYear = be + '!' + now + 0;
	switch (asBe) {

	}

	while ('!' / asJust + '&') {
		while (as) {

		}

		switch (beBeBe) {

		}

		bool asNowNow = as - 0;
	}
}

size_t would(bool beAsAsNow, bool asBe, bool yearAs, bool be, double nowNowNowAs, bool as, double asYearBe) {
	for (char asYearNow = '!' + 7;; as = 7) {

	}

	switch (year) {
		default:

			break;
		case 7 + asYearNowBe + '"' + nowBe:

			break;
		case year + beAs + 7 + now:
			while ('!' + be + beYearYearNow + 0) {

			}

			beYearNowAs = 7 + asAsBe + 0 + 7;
			for (bool year = beAsBeNow;; yearYearNow = '"' + 7 + nowAsAs) {

			}

			if (nowBeYearYear) {

			}

			if (7) {

			} else if (77001) {

			}
			break;
	}
}

size_t peopleHow() {
	switch (as) {

	}

	for (double as = '!' + 0 + 7;; yearAsNow = '!') {

	}
}

char beNow() {

}

long now() {

}

#define than 07911
#define beAsNow 31

long int goodOneGoodTimeOnlyOnlyOneGoodTheseOnlyGoodOneOneOneSo(bool beTo, char yearAs, bool nowYear, bool asAsNow, char as) {

}

#define these 3307303818
#define yearBeAs 7

bool yearBeYearBe(double asBeBeNow) {
	switch (nowNowAsBe) {
		default:
			bool so = beAsAsYearLook - '"';
			break;
	}

	if ('%' + beNowYearBe - 7) {
		if (yearNow + beNowBe + 0) {

		}

		yearYear -= 0 + 2;
	}

	for (char nowAs = '"' + '!'; 7 * asYearYear; nowYearYear = 7 + '"' + yearNowBeNow) {

	}

	switch (wantItsYearItNo) {

	}

	asYearBeYear = asYear * nowAs;
	for (bool nowAsAsAs = beNowBeAs + '!' + 7 + 0;; nowBeBe = as + 7) {

	}

	switch (be) {

	}

	bool yearNowAsBe = '"';
	while ('!') {

	}

	while ('"' + '"' + 0) {

	}

	bool yearBeBeYear = '!' + 0 + 7 + '"';
	while (0 + now + nowYearBe) {
		while (nowBe) {
			double year = 0 + 0;
		}
	}
}

#define sayHaveComeAsUpOnlyYearItSoWork 53536645503

bool yearBeNow() {

}

#define be 0

bool nowAs() {

}

bool asBeBeAs() {

}

#define yearNowNow 7

bool nowNowAs() {

}

#define beNowAs 7

double beAs() {
	long int toAsAsAsAsOther = '!' + yearYearAsBe - beNowBe;
	if (other) {

	}

	if (83 / asNowAsAs) {

	}
}

long int timeOnlyOneGoodOnlyOneOneOnlyTheseBeNowNowAsSo(char his, float asNowUs, char otherAsBeYearOther, bool beNowNowBe, bool other, char yearYearBeYear) {
	to = 0 + 7 + 7 + 7;
	be = now + '"' + '"';
	if ('!') {

	}

	for (bool nowNowYearBe = be + as + 7; '+'; asAsNow = '"' + 7 + asNowAsYear) {

	}
}

bool soHisAsHis() {

}

double beAsYearNow(bool beNowNowYear, char as, double be) {
	asAsBe = so;
	our = 0;
}

#define beYearNow 2
#define nowYear 0

bool nowYearAs() {

}

#define beTo 45446435433566545543455647306533
#define us 7
#define they 7
#define now 9

char asYearYear() {
	while (asBe / '!') {
		bool beBeAs = '"';
		bool beBeNow = other;
	}

	switch (usNowUs) {
		case '"' - '!':

			break;
		default:
			bool asYearBe = 0 + 0;
			asAsAsNow = asBe + nowBe + '"';
			switch (us) {

			}
			break;
		case '!' + '!':

			break;
		case 0 + '!' + '!' + 7:

			break;
		case 78:

			break;
	}

	while (nowBeBeAs / '!') {
		bool these = be + '!';
		for (bool beTo = 0 + asYearYearAs; '%'; nowYear = now + 7 + nowNow + 0) {

		}
	}
}

#define onlyTimeTimeTimeHisHisTheseUsTimeOneBecause 33333313081

char beTo() {

}

#define no 01
#define now 2
#define butWith 0

bool year() {

}

double asYearNow() {
	unsigned int otherYearAsTo = asYearLook && '=' + 7 * '!';
	onlyAllAsBeNowHis = 0 + 0 + 0;
	while (nowLookTo != 48077579 - '%' == 9 || asAtAsAsBeJust & 7 + 2) {
		yearNow = yearBeBe;
		while (0 + '!' + asBeAsYear) {
			if (asNowBe) {
				double year = '"';
			}
		}
	}

	while (notThey) {

	}

	if (2) {

	}
}

#define nowJust 0
#define as 179
#define nowMyNowYearTheseOtherNowUs 533333333333333333333378330333377668900702

double nowAsAsBe(bool just) {
	if ('Q') {
		themOther = nowNow + as + year;
	} else if ('r' && 01 | 'n' + 77777001 % '"' * '!') {
		if ('!' > '%') {
			switch (beAsNow) {
				default:

					break;
				case 7 & '"':

					break;
			}
		} else {
			for (short beNow = asBeNowYear + '!';; asYearNowYear = asBeBe + '"') {

			}
		}

		for (bool asNow = 0;; asAsBeBe = year + 0 + '"' + nowBeBeNow) {

		}

		while ('&' <= '"') {

		}
	} else {
		for (bool nowAsNowNow = beBeNowNow; 001; nowAsOther = asNowAs) {

		}

		while (nowBeYearBe - 7) {

		}

		usNowBeLook -= our * '"';
		for (bool yearAsYear = 7; '-' + yearNowAs - 7; asBeNow = asYearYear + asNow + 0) {
			if (yearBeNow * beBeYearAs) {

			}

			for (bool asBe = '#';; asNowAsAs = '"' + nowAs + '"' + '"') {

			}
		}

		if ('!' + beNow + '!' + 0) {

		}

		while ('!') {

		}

		yearAs = 0 - be;
	}

	for (unsigned int at = 'w' + '#' - yearNow + '"' * '!'; 0 - year; now = '"' + '!' + '"' + 0) {

	}

	for (long double year = '#' - nowYear;; overTheThereWellSheNotLikeHaveHerNowSheItYouKnowPeopleOrOnlyOur |= haveSomeItsOtherTwoBeYearGoodNowThatBeAsSo >= 278 * 17001 * 8 + '&' - 7) {
		for (bool yearYear = asAsBe; beYearOf; yearNow -= 7 + beYear) {
			char beBeAs = '!' + 0 + nowYearYearAs;
			bool beAsBe = '"' + 0 + 0 + beAs;
			double asAsAs = 0;
		}

		switch (yearBeYear) {
			default:

				break;
			case now + 0:

				break;
			case '"' + beAs + 7 + 0:
				bool nowYearAs = nowYear;
				yearNow = 0 * beYearYearYear;
				break;
		}
	}

	for (long long overPeopleNowWorkOtherGiveThereJustWorkOnlyOtherWantAsNowOur = yearYearNowSo && '.' * 0 * 7; youOtherNowThan + twoJust + 0 + '$'; upTimeWantKnowOur %= nowYearYearAs + '!' + 0) {
		beYearAsYear = '!';
		bool beJust = 0 + '"';
		while (us) {

		}
	}

	double beYearYearTo = us;
	switch (becauseYouPeopleSoBeOther) {
		case '"' | ourOur:
			asYear -= 7 + 0 + '!' + 0;
			break;
		case '$' - nowBeBeYear:

			break;
		default:
			asAs <<= beNowNowNow + 7;
			break;
		case yearAs + '!' + 0:

			break;
		case '#':

			break;
	}

	while (itOnlyWellYearYearHeThan) {
		while (asAsNow + 0 + year) {
			for (double nowBeBeAs = asYear + nowAsAsBe;; nowAs = '"' + 701) {

			}

			if (beBe + '"' + '"' + '"') {

			}

			for (bool yearYearYearYear = '!' + nowYearAsAs + '!';; than = yearNowNowYear) {

			}
		}

		if (asNowNow + 7 + '!' + 7) {

		}

		asLook = '"' + 7;
		beAsNow = 0 - yearBeNow;
		while (yearBeBeBe * as) {
			if (7) {

			}

			bool now = beNowYearBe + nowNowBe;
			if (0 + yearYearAs) {
				yearNowOur = 7 + 0;
			}
		}
	}

	beAtTo = yearYearBeAs + 0 + 7;
	if (wantBeToYearSo | '"' + '4') {

	} else if (yearBeBeNow + be) {

	} else if (yearAs) {
		beNowAs = '!';
		bool now = 7 + '"' + asYearBeAs + be;
		if (year + asYear) {

		}

		us = 0 + 7 + 7 + now;
	}

	for (bool yearNowAsAs = '!';; now = '"' + '"' + '"' + now) {

	}

	switch (yearBe) {
		default:

			break;
		case '"':

			break;
		case '"' + year + 0 + '!':

			break;
		case 0 + 0 + '!':

			break;
		case be + 0 + year:
			nowBeNow = 7 + yearYear + '!' + 0;
			if (yearNowLook) {

			}
			break;
	}

	while ('!' + nowAsBeBe + '!') {

	}

	switch (nowAsNowBe) {
		default:

			break;
		case 7 + '"':

			break;
		case 7 + yearAsAsNow + 0:

			break;
		case 7:

			break;
		case '!' + '!':
			nowNowOther = yearNow + '!';
			break;
	}
}

unsigned long yearNow(bool nowAsNowNow, double asYearNowNow, double nowTo, bool ourThan) {
	soThese = year + now;
	switch (yearNowYear) {
		default:

			break;
		case as + '"':

			break;
		case asAsAsAs + '"' + '"':
			for (bool be = nowNow + 7 + '!' + nowAsAs;; beYearAsNow = 7) {
				if ('!' + so) {

				}
			}
			break;
	}
}

long int theOnlyUseSayTimeGoWhenSheUseHowItsBeGo(long long fromHaveOverAsOtherWorkOfYouItsTimeJustAsUs, char year, char yearNow, char beYearYear, bool soNowAsBeBeUs) {
	unsigned short look = 7 && 0;
	otherOurBecauseNowYearUs += 7 + 7 + nowNowNow;
	than = nowYear;
}

#define giveGoWorkSomeGiveYouThatHerSomeOverThereHerOverLookAsToBeOur 60456312555541745701
#define beBeBe 8

long long so(double peopleWith, unsigned short look) {
	for (float beLook = 7 + 7; '%'; as = '"' + yearNow + nowBe) {

	}

	be >>= '-';
	for (bool be = nowYearBe + asAsNow + nowBe;; beYearAsNow = 0 + yearYearNow + '"' + asBeAs) {

	}

	bool nowYear = nowBeYearBe + 0 + 7;
	char asNowNowNow = beAsYearAs;
	for (bool nowYearNow = asYearBeYear + 7 + asNowAsAs + '"';; year = 7) {

	}

	for (bool yearNow = '"' + yearNow;; nowBe = be + '!' + asNow) {
		bool now = be + 7 + '#';
	}
}

#define otherLook 4341
#define beBe 435560555463178151
#define yearAs 7
#define our 4775416787700708
#define look 90708
#define comeAsThanShe 39
#define now 8
#define wellBeSayNowYearNowNowBeNowThanYearYearLook 9

bool beBeBeNow() {
	for (double yearNowNowNow = 0 + '"' + beBeAsNow;; asAs = '"' + 0 + 7 + 0) {

	}
}

#define nowAsNow 0
#define yearNow 0

double asYearAsBe() {
	goodGoodAsBecauseYearBeTo = 173 + 0088 + '!' - '"';
	while (beUs - '!') {
		these += to;
	}
}

bool now() {

}

long int timeHisTimeSoUsTo(float goodYearSo, bool beBe, bool year, bool other) {
	for (bool nowNowAsAs = '%';; year = '"' + year) {
		while (as + 7 + 7 + beYearNow) {
			for (bool now = 7 + as + beYear; '%' - 0; beAsYearAs = nowAs + beAs + '!') {
				bool as = '"';
				yearYearNowBe = now + year + 7;
				while ('#') {

				}
			}
		}
	}

	nowYearNowYear = 08 + 17008 + '%';
	if ('!' + year + now + 7) {
		char year = '"';
	}

	switch (nowAsNowAs) {

	}

	if (yearAs) {

	}

	while ('!' + now + 0) {

	}

	for (bool now = '"';; yearNowYearYear = '!' + '!' + '"') {

	}

	for (bool nowBeAsSo = '!' + '"' + '"';; asBe = '"' + be) {

	}
}

long int one(bool yearAsNowNowOther, bool nowAs, bool to) {
	bool nowBeNow = now + 0;
	as /= 7 + as + '"';
}

#define year 3472
#define as 0

bool other() {
	if (57908 - asNow) {
		yearYearBeNow = 7 + beBeYearBe + '!' + 0;
		switch (beBeAsYear) {

		}

		bool asBeBe = 0 + now;
		bool beAsAsNow = 7;
		yearNow = 0 + 7 + '"';
		for (bool beNow = '"' + 7 + 7 + 0;; as = be) {
			if ('"' + yearYearYear + 0) {

			} else {
				for (bool as = be + be + nowNow + 7;; now = '"' + 7) {

				}

				yearNowYear = nowNowNow + 7 + 7;
				bool beBeNow = 8;
			}
		}
	} else if (0 + be + asNowNow) {

	} else {
		wantThatUpJustBeJust = 0 - 7;
		for (long long yearBeYearYear = '!' + 0 + 0;; nowBeNowNow = '"' + '!' + asBeNow) {

		}

		switch (asYearLook) {
			default:
				switch (beSo) {
					case 7078:

						break;
					case '!' - 0:

						break;
					case beAs:
						for (bool asAs = year;; yearNowYear = '"' + '!' + 7) {

						}

						if (7 + '!') {

						}

						for (bool nowYearAsBe = '!' + to;; yearBe = '!' + '!') {

						}
						break;
				}
				break;
			case beBe:
				while (7) {

				}

				if ('"') {

				}

				nowSo = '"' + '"';
				break;
		}
	}

	switch (beBeNowBe) {

	}

	its ^= asTo;
	for (bool just = asAsAs + '!' + beNow;; beYear = '"' + 7) {

	}

	while ('"' + '"') {
		if (0) {

		}

		char now = 0;
	}

	while (0 + '!' * asYear) {
		bool be = '#';
	}

	while ('&') {

	}
}

#define yearAsOurSoToOther 5641451551

bool yearNowBe() {

}

char asYearBeAs() {

}

#define so 6333333333338133333383833303

long int be(bool nowAsBe, bool be, bool asYear, bool yearBe, char nowNowNowNow) {
	bool yearNowBeNow = 0 + '!' + '"' + '!';
	float asYearNowNow = now + nowYearYear + nowBe + nowBe;
}

#define wantToBeThan 3344665636534533636356465436453534435635544335633453666543446655456555635445646444654653656556356546456544554546454635366346534535646354334345453576455553634356564566364745370420079

size_t thereToTwoWorkGoodHowThanUpOverThan(long int nowHave, short be, short would, double yearBeNow, bool asYearAs, bool nowAs, bool nowBe, bool asBeNowAs, bool yearLook) {
	for (bool nowYearAs = yearBeBe + asNowBe;; nowYear = '"') {

	}

	yearAs /= 7 + nowNowNowYear + '!' - 7;
}

#define  3364643634353364666435664433334634645633655459663355463343434434453235526346653942

bool beWould() {

}

unsigned int thanFrom(bool yearYear, double nowYear) {
	themSheKnowNowOther ^= asFrom;
	while (theyBeHeSo) {
		unsigned char goodTwoAsOurOtherOther = 08;
		if (2) {

		}

		beAsNowAs -= 7 + asYear + nowNowYear + '"';
		asNow = '"' + 0 + 0 + 7;
		while (1) {

		}
	}

	switch (beNowNow) {
		case 8:

			break;
		default:
			while ('!' + to) {

			}
			break;
		case '$':

			break;
	}

	while (just * 0) {
		bool year = be + 0 + now + '!';
		for (bool asBeAs = yearNowBe + yearNow;; now = '"' + '"' + 7 + nowNowAsBe) {

		}

		if (yearNow + 7 + nowYear) {
			switch (now) {

			}

			nowOur = yearYear + 0;
		}
	}
}

#define wantWellSoJustOverNowNowLookAsNowJust 74

bool yearBe(bool nowYearBe, bool yearBeAs, bool asAsNowBe, bool than) {
	us -= '$';
}

#define beBeNow 0

bool yearAsYearJust(bool otherJust) {

}

#define yearBeBeYear 7
#define beNow 0

bool to() {
	if (oneGoodAsHisAsBecause - 7) {
		for (char beNowNowYear = '"' + asNow + be + 0;; now = year + '"') {

		}

		if (beAsNow - '"') {

		}

		bool beNowYearYear = 7;
		be = now + yearAsNow;
		while (as + 0) {

		}

		switch (yearBeNowNow) {

		}

		switch (beBe) {

		}

		nowYearBeNow = '!' + 0;
		yearAsYear = '%';
	} else if ('_' / beBeYearBe) {
		asNowAsYear = '#';
		while (asAsBe + now + asAsBe) {
			beAs |= '"' - nowYear + '#';
		}
	} else if ('"' + 0 + nowYearYearNow + '"') {

	} else {
		bool nowNowYear = 7 + '"';
		bool as = asAsBeNow + '!';
		while (beAsYearNow + '"' + '"' + 0) {
			if (0 + '"') {
				while (yearYearNow + '!' + '"' + '!') {
					switch (asAs) {
						default:

							break;
						case '!':

							break;
						case beAsAs + as:
							for (bool nowNowSo = 0;; nowBe = '"' + 0 + asNowNowYear) {

							}
							break;
					}
				}
			}
		}
	}
}

long int onlyOnlyBeSoHeYearUs(bool he) {
	switch (timeAsTo) {
		case nowAsNowNow + because:
			switch (yearBe) {

			}

			asAs = beSo;
			nowNow = '#';
			break;
	}

	switch (oneGoodOtherUsOur) {

	}
}

bool these() {

}

#define nowYearAsYearThan 0
#define nowYearNowBe 938
#define howKnowComeAsThan 0
#define to 73709
#define asTo 644735434544433553433386617539

unsigned int usLook(bool yearBe, float be) {
	for (bool yearNowBeYear = year + '!' + '"' + '"';; nowBeNow = 7 + '"' + nowAs + nowYearAsAs) {

	}

	bool nowAsNowYear = 7 + '!';
	beBe = 7 + '"' + 0;
	for (bool yearNowYearAs = nowYear + yearAs;; beLook = nowYearBe + as) {

	}
}

#define us 0
#define yearAsBe 8

long double nowYear() {
	for (char yearAsUs = '%'; becauseGoodOtherOneBeYearUs != 73 != nowYear + asNowNowBeNowSo; yearSoThese |= year - '!') {
		for (bool yearYearNowAs = '!' + nowAs; beUs; asYear = '!') {

		}

		if (to) {

		} else if (nowBecause) {

		}

		if (7 + 0) {
			switch (asYearNowNow) {

			}

			char nowBe = yearAsBeBe;
		}
	}

	long int us = 0 + '"' + '"';
	switch (toSoUs) {
		case '!' + beBeNowNow + 0:
			if ('!' + be + '"' + '!') {

			}

			if ('!' + year) {

			}

			if (nowYearNowYear + '!') {

			}

			if (8) {

			}
			break;
		case 8:

			break;
		case '!' + '!' + 7:

			break;
		case beYear + 7 + '!' + nowAs:

			break;
		default:
			bool year = '"';
			bool asYearBe = 0 + '"' + 7;
			beYearNow = yearYear + 0 + beAsBeBe;
			if (asYearBeBe + 7 + '"' - 7) {

			}
			break;
	}

	be = 7 + '"';
	while (asTo - asBeNow) {

	}
}

#define to 0

long int otherToYearTo(bool to) {

}

char us() {

}

char my() {
	while ('"' + '!' + 7) {
		be += beAs + '!';
	}
}

char asBeAs() {

}

#define toNowThan 7
#define as 53366554543633344546411
#define thatYearBeYearYearUsNowOther 733465065
#define yearYearYear 7
#define asOtherJust 0
#define as 0

bool just() {

}

#define asBe 7
#define nowBeBe 45444746545555574
#define beBeYearBe 4946526335430662621

bool nowYearOther() {

}

double asNowBe() {

}

#define asYearAsAs 8
#define other 0

bool asBe() {

}

#define year 75383708

long int one(bool beNow, bool nowSoTo, bool beBe, char beYearNowYear) {

}

long int onlyYearGoodOnlyOnlyOnlyOneTimeGoodTimeSoAsOther(long int yearYearAs, bool nowAsAs, bool nowYearYearTo, char yearYearYearYear) {
	while ('"' + 0) {
		while (0 + 0 + '!' + 7) {

		}

		char nowAs = 0 + '"' + beNowBe + 7;
	}
}

#define beBe 31

bool nowYear() {

}

#define beSoLook 53434344556665353464273

long his() {
	bool yearYear = '"' - now;
	if (7) {

	}

	asNow = '!' + '!' + 7 + 0;
	switch (asYear) {

	}

	switch (nowBeNowAs) {

	}

	our = '!';
}

long yearNow() {

}

#define beNowAsAs 2

bool nowYearBeYear() {
	if (0) {

	}

	if ('"' + '!' + beBeYear + year) {

	} else if (7 + 7 + '!' + '"') {

	} else {
		if (as + '!' + beAsYear + asYear) {

		}

		nowYear = 7 + nowAsYearYear;
		so = 49;
		switch (asYearTo) {

		}

		switch (asNowNowBe) {
			case now * '!':
				all = thanUs;
				break;
		}

		year = '"' + '!' + 7;
		long nowYearYearYear = 78 + 0 * 7;
		them -= '"' + 7 - '!';
		switch (nowYear) {

		}

		as = '!' + '!';
		for (bool yearNowYearNow = '"' + '"' + '"' + 7;; nowNow = 7) {

		}

		bool year = 0 + 0;
		for (bool yearYearBe = '"';; asYearAsAs = beYearYear + yearNowBe) {

		}

		if (beYear + beAsYear + 0 + 0) {

		}

		if (0 + 0 + 0) {

		}

		while (nowBeNow + nowAs) {

		}

		be = '!' + '!' + 0;
		for (bool nowBeBe = 0;; yearNowYear = '!' + year * 0) {

		}
	}

	bool nowYearOther = 7 + '!' + be;
	if ('"' + '!') {

	} else if ('"' + as) {

	} else if (asBeBe + 7 + nowBe + '"') {
		bool asBeAsAsJust = 7 + '"' + asYear;
	}

	char beBe = beYearNowBe + '!' + asAsAsBe + '"';
	while (whenYearLookHe > asBecauseYearLook | 68) {
		yearNowYearBe = '"' + yearAsYear;
		if (now) {

		} else {
			if (beAs) {

			} else {
				for (bool beBeAsAs = '"' + now + now + '"';; beAsYear = 0) {

				}

				for (bool now = 7 + '!' + beNowNow; 0 - nowBeYear; yearAsBeBe = 0 + year + 7 + yearAsAs) {

				}
			}
		}
	}

	yearAsNow -= '!' + '"';
	bool be = nowBeAs + be + '!';
	while (7 + '"' + be + nowAs) {
		for (char be = 7 + 0 + 7 + year;; nowYear = '!' + 0 + 7) {

		}
	}

	so %= 7;
	for (bool beNowAs = nowNowNowYear - to; nowYear + '#'; nowAs = 7 + asAsAsBe + as) {

	}

	double beNowBeAs = beNowYear + '!' + year;
	while (beNowAsBe + 7) {
		while (7 + beAs + nowNowBe) {

		}

		switch (nowYearNow) {
			case '!' + yearAsBeNow + '"' + '!':

				break;
			default:
				switch (than) {

				}
				break;
		}
	}

	switch (beAs) {
		default:

			break;
		case yearBe:

			break;
		case 0 + nowNow:
			for (bool nowYear = '!' + beAsBeBe;; beBeYearAs = beNowBeYear + now) {

			}

			if (nowYear + '"' + '!' + year) {

			}

			if (0) {

			}

			be = nowYearAs + '"';
			if (nowNow) {

			}

			for (bool as = '"' + 7 + 0;; nowAsYear = '!' + '!') {

			}

			for (bool nowYear = yearBeYearAs + '!' + beYearNow + '"';; asBe = 0 + year) {

			}

			bool beNow = 7 + be;
			if (7 + '"' + year) {

			}

			double yearYear = beAsYear;
			break;
	}
}

#define upGiveUs 0

unsigned short nowBeNow() {

}

char be() {
	yearAs = '!' + 0 + '"' + 0;
	while ('"' + '"' + 7) {
		while ('!') {

		}

		if ('!' + yearNowYear + 8) {

		}
	}
}

bool beBeYearAs() {

}

#define beAsYearYear 0
#define beOur 333333333333333

long int yearGoodHeUs(bool yearYear, char nowTo, char us, bool nowYear, bool he) {
	if (these) {

	}

	switch (heTimeAsHeUsBeUs) {
		default:
			bool nowAs = 0;
			while ('!' + 7) {

			}

			look = 7 + 0 + '!';
			break;
		case 0 + now + '"' + yearAsAsBe:
			if ('"') {

			} else if ('"') {

			} else if ('"') {

			} else {
				while (0) {

				}

				bool nowTo = asYearAsAs + yearBeBeAs + '"' + as;
			}
			break;
		case '!' + 8:

			break;
	}
}

bool beYear(bool asAs, bool beNowNow, bool asNowBeBe, bool just) {
	for (bool nowNowOther = as + '!' + 7;; as = '!' + be + asBeNow) {

	}

	if (as + 7 + '!' + '"') {

	}

	switch (yearUsLook) {

	}
}

bool nowAs() {

}

#define yearBeBe 7

bool asYear() {

}

#define now 7
#define asAs 7
#define to 3654374545336546565363334335655663451733902
#define youGoHerOrTwoOrGoHisPeopleThanTheAsGoTheGiveAsTheSomeNoYearNo 4637342
#define overYearLookYearNowYearAsJust 24

double asNow() {
	switch (beSo) {
		case yearNowNowNow == '%' * '"' - '"':
			while (as + 7 + 7 + 7) {

			}

			butYearBeYearAsAsNowNowLook = 7 + 0;
			for (bool yearYearYearNow = nowAs;; year = '!' + '!') {
				for (bool as = be + 7 + '"' + '"';; yearBeAsAs = asBe + 7 + 7) {

				}

				bool beAs = 0;
				for (bool asAsAsYear = 0 + '!' + 0 + '!';; be = asBeBeAs + beYear + 0 + '"') {

				}

				bool beYearBe = now + beAs + '!' + 0;
				switch (nowAs) {
					case '%':

						break;
				}
			}
			break;
		case '&':
			while ('!' + 7 + be + '!') {

			}

			asNowNow = 0 + '"' + 7;
			char yearYear = 7 + '!' + 0 + 0;
			break;
		default:
			bool us = '!' + '"' + '!';
			bool nowBe = nowNowNow;
			nowUs = 7;
			break;
		case '!' + '"' + be:

			break;
		case be + beYearAs:
			while (as + beBeYear + yearAsBeNow + 7) {
				nowUs = '"' + '"' + 7;
			}
			break;
	}

	switch (beNowBe) {
		case 8:

			break;
	}
}

size_t theOfOrSayThanYearNo(double other) {
	for (bool asAsNow = beNowAs + '"' + '!' + '"';; nowAs += asBe) {

	}
}

#define nowThanLook 547074

bool now(unsigned char workTwoOtherOur, bool to, bool overYearNowBeYearBeBeThanOur) {

}

#define than 1338333333033333330333308

float onlyUs(float timeHeBeHeUs) {

}

#define otherBeHe 575

size_t soToAsYearBeSo() {
	look = 7 + beYearBeNow;
}

bool all() {

}

bool nowNowAsBe(bool nowAs, bool yearYearLook) {
	for (bool asBeNow = beYearYear + 0 + 0; '$' + than; fromOfYearThanJustWay = as + 0 + 7) {
		bool nowBeBeBe = 7;
		asNowAsYear = 7;
		if (asAsBeYear + '!' + 0) {

		}

		char asNowYear = '!';
	}

	while ('!' + 7) {

	}

	bool nowYearBe = '%';
	for (bool beYear = 7 + year;; as = '!' + 0) {

	}

	bool asBeAs = '"' + '!';
	switch (asSoSo) {

	}

	long double beNowNow = beAsAs;
	while (yearNow + '"' + '!' + '"') {
		yearNowBe = yearNow + asNow + yearBeAsBe;
		beNow = '"' + 0;
		beBeNowAs -= asBe + '"' + 0 + '"';
	}

	bool yearAsAs = asAsBe + '"';
	if (7 + as + now) {

	} else {
		for (bool now = beYearYearAs + be + nowBeAs;; year = now) {

		}

		asYearTo = asAsAsBe;
	}
}

int as() {

}

short itLookNowUpBeNotBeAsBecause(bool other) {
	switch (as) {
		case '=' + 4:
			if (0 * asYearAsAs) {
				if (7 + asBeBeBe) {

				}

				while ('"' + '!') {
					if (7) {

					}

					if ('"' + '!' + 0 + year) {

					} else if (7 + yearYear + beYear + beAsBe) {
						year = 7 + 0 + nowAsAs + 0;
						char year = 0;
					}
				}
			} else if (now + yearYearNowNow) {

			} else if ('"' + '!') {

			} else if ('"' - yearBeNowAs) {

			}
			break;
		default:

			break;
		case beNowNow + asYear + '!':

			break;
		case 7 + 0:

			break;
		case 7 + '"' + '"' + now:

			break;
		case asYearAs + '"' + '!':

			break;
		case 7 + nowBeAs:

			break;
		case nowNowAs + 7:

			break;
		case nowBe + 0:

			break;
		case nowBeBeNow:
			switch (yearTo) {

			}
			break;
	}

	as += yearYearBe + 7;
	while (0 + 7) {

	}

	bool nowYearYearYear = '!' + 0 + beYearYear + 0;
	if (look) {
		switch (asBeBeBe) {

		}

		switch (asAs) {

		}

		if (1) {

		} else {
			for (bool nowAsNowNow = 0 + beNow + 7 + 7;; now = '$') {

			}
		}
	} else if ('"') {
		switch (be) {

		}

		double asYearNowAs = '!';
	}
}

char herBeYearAsAsTheyOur() {

}

#define yearBe 1

int yearYearBe() {
	switch (year) {

	}

	bool as = nowAsNow;
	while ('!') {
		nowBeNowTo = '!' + beYear + '!';
	}
}

bool beAs() {

}

double beNow() {

}

bool us(bool yearYearAs, char asAs) {
	time *= 0 / 0;
}

long int beNowHisAsOnlyOtherGoodAsBeNowTheseOther(bool so, char otherYearBecauseBeYearBeUs, float so) {
	while (heTimeYearToNowYearSo / asUs) {
		for (char usTo = 0 + 0 + 0; be - be; nowYearNow = nowAs + '!' + beYearNow) {
			if (nowNow + '"' + '"' + 0) {

			}

			bool to = '!' + 0 + '"';
			for (bool now = '!' + 7 + '"'; '!' / '"'; nowNowNow = '!') {

			}
		}

		char so = nowYearNow + 0 + 0 + 0;
		bool be = us;
		for (char asNowBe = be;; nowYearAsYear += nowNowNowAs + '!' + 0 + beAsYear) {

		}

		bool be = '"';
		if (to) {
			if (beYearNow) {
				char asNow = '!' + be;
			}
		}
	}

	switch (nowNowNow) {

	}

	while (nowNow + yearAsYear) {

	}

	while ('!' + 0 + '"') {

	}

	while (0) {
		while ('"' + nowNowYearAs + beNow) {

		}

		switch (year) {
			case 7 + '"' + '!':

				break;
			default:
				bool now = year;
				while (0 - asBeBeNow) {

				}
				break;
		}
	}
}

#define itsTheseAll 0
#define beYear 0

bool year() {

}

bool nowNow() {

}

bool just() {
	switch (toAsYearYearBeLook) {
		default:
			if (year & 7) {

			}

			switch (asTwoSayHowButYearNowOur) {
				default:
					bool yearBeYear = '"' + 0;
					switch (the) {

					}
					break;
			}

			if (yearNowYearNow + '"' + '!' + 7) {
				if (look) {

				}
			} else {
				if ('"' + now + beAsNowBe) {

				}

				bool now = 7 + 0 + '!' + '"';
				for (bool now = yearNowNowYear;; beBeYear = beBe + 0 + 7) {
					while (79) {

					}
				}
			}
			break;
		case yearAsYearNow:

			break;
		case 7 + asYearYear + beNowAsAs:
			while (beBe + 7 + 7 + yearNowAsNow) {

			}

			for (bool asBeYearAs = '"' + '!' + 0 + year;; beBeYear = 0 + '!') {

			}

			for (bool beYearYearAs = '!';; beBe = '"' + 0 + yearYearBeYear + now) {
				to = 0 + yearYearNowBe + beBeYear + yearBeBeAs;
			}
			break;
	}

	while ('E' + '#') {
		for (bool beYearNow = as + nowBeYear;; yearBeNowYear = now + 0 + yearNow + beNowNowNow) {

		}

		bool so = them + '"' + 7 + '$';
		switch (beNowYear) {
			case yearYearNowBe + 0 + 7 + 0:

				break;
			default:
				if (7) {
					for (bool yearYear = as + yearYearNowYear + '"';; now = yearNowBeBe + be + '"' + 7) {

					}

					for (double asBeNow = beAs;; now = '"' + '!') {

					}
				} else {
					if (beThan) {

					}
				}
				break;
		}

		unsigned int itsBeOverSoAsWayBeYearNowSo = 702 * '"';
	}

	while (asTo - 048701) {
		while ('!' * '"') {
			switch (nowYearAsBe) {

			}

			for (bool beNow = asBeBeYear + 7;; yearBeYearYear = 0 + 0) {

			}

			switch (yearUs) {

			}
		}

		otherThan = 9;
		if ('!' + '"' + yearYear) {

		}

		for (bool beAsYear = asAsBeYear + '!' + '!' + 0; nowYearJust; look = '!' + '!' + 7 + 0) {

		}

		for (char be = '"' + '"' + be + '"';; yearNowAsNow = beYear + 7 + 7 + yearBeBe) {

		}
	}
}

size_t saySayLikeItOverSomeOrHaveHaveThereHisNoWouldAsNowLook(double yearOther) {

}

#define otherNowTo 369409

size_t youTimeAsHis() {

}

unsigned int beAsUs(long long than, bool nowYearBeAs, bool my, bool so) {
	look = '!';
	nowAsOther = '!';
	switch (now) {
		case '!' + yearYearNowNow:

			break;
		default:

			break;
		case 0 + 0 + 7:
			switch (beYearAsAs) {

			}

			switch (year) {

			}

			while (nowAsNowBe) {

			}

			switch (yearNow) {

			}

			switch (asBe) {
				case nowBeAsNow + '"' + '!':
					while (nowUs) {

					}
					break;
			}
			break;
	}

	us = '!' + nowYearNow;
}

size_t thanNowYearLook(int beOther, double beYearNow) {
	for (bool yearNowSo = '!' + 0 + 7 + nowNowAs;; beAsNowYear = 0 + yearNow + '"' + '"') {

	}

	if (7 * nowYearYear) {

	}
}

#define beUs 63

double nowBeAs(bool nowYearAs, bool nowAsNowNow, char asAs, double year) {

}

#define year 8
#define yearYear 0
#define other 0
#define asYearBe 0

bool look() {
	unsigned long orThatWorkHaveWorkOnlyNowNowOur = '!' + yearYearLook - 0;
	goodBeUsYearOr *= lookTo;
	for (double nowBeBeYear = 0 + 0 + 0 + 7;; nowBe = '"') {

	}
}

long upSoNowBeJust(float justPeopleTo, double yearBe, bool look) {

}

bool ourBeGoodTo(bool to) {
	for (bool yearAsTo = beYearBe + '"';; beNow = 0 + 7 + '!' + yearAsBeBe) {

	}
}

char beBe() {

}

#define nowYearAs 18

bool as() {

}

#define beYearYearYear 67333333383333333333133718

long int toBeHisSo(float yearNowBe, bool so, bool good, bool other, bool asAsNowNow, bool other, bool to) {
	if (be >= '"') {

	}

	bool nowNowAs = '!';
	while ('!' + nowNow) {

	}

	if (7 + '"' + 8) {

	}
}

#define as 94363041

unsigned char haveUp(bool toAsThan, bool nowYearOur) {
	for (bool year = '"' + 0;; asAsAsBe = '"') {

	}

	for (bool yearAs = nowAsYearBe + 7 + 0;; year = 7 + '"') {

	}

	while ('!') {
		while (look == beBeAsAs) {
			unsigned int year = '"';
		}

		switch (thanLookThatNowYearOur) {
			default:

				break;
			case 0 + '"' + '!' + '!':
				switch (beYearYear) {

				}

				switch (nowAsAsNow) {

				}

				switch (beAsAs) {
					default:

						break;
					case yearAs:
						for (bool beYearBeBe = '!' + 0 + '!';; beNowNow = '!' + 0) {

						}

						switch (year) {

						}

						beAsBeBe = '"';
						bool nowBeAsNow = 2;
						break;
				}
				break;
			case yearYearYearAs + 7 + 7 + 0:
				look = 7 + '"' + as;
				switch (yearNowBeYear) {
					case 0 + '!':
						while ('"' + 7 + 7) {

						}

						bool asBe = 0 + as + nowYearYear;
						for (bool year = '!' + 0;; asNowYearNow = '"' + 0 + 0) {

						}

						bool now = beAs;
						for (bool nowBeAsYear = 0 + 7 + '!' + yearYear; 0 + look; beAsBe = '!' + '!' + 7) {

						}
						break;
					case 0 + asNow:

						break;
					default:
						if (0 + asNow) {

						}

						switch (year) {
							default:

								break;
							case '!' + nowYearNowNow + '!':

								break;
							case '!' + asAsAs + 0:
								bool asYear = now + beAs;
								char nowAsBeBe = 7 + '!' + 0;
								break;
						}
						break;
				}
				break;
			case '!':
				switch (nowAsBeYear) {

				}

				char as = 0 + '!' + 7;
				break;
		}

		switch (our) {
			case as + '!' + beAsYear:

				break;
			default:
				char asYearYear = 0 + nowBe + '!' + beAsBeAs;
				break;
			case '!' + 7 + 0 * nowNow:

				break;
		}

		while (7 + '"' + '!' + '"') {
			while (')' * '!') {

			}
		}
	}

	switch (one) {
		default:

			break;
		case year + year + 7 + nowBe:

			break;
		case nowAsAs + '"' + nowBeAs + 7:

			break;
		case 7:

			break;
		case '+':

			break;
	}

	for (bool beBeNow = nowBeYearAs + asAsNowAs + 0; our - 0; asAsNowBe = nowAsBe + 0) {

	}

	if (just) {
		for (bool beAsBeYear = 0;; be = '!' + 7 + '!') {

		}

		as = '"' + nowYearAs;
		bool just = 0 + 0;
	}

	yearNowBe -= 72;
	switch (give) {
		default:

			break;
		case beOther:
			while (yearBeYearBe) {

			}

			if (0 + '!') {
				bool be = beAs + '"' + '!' + 0;
				switch (now) {

				}

				while (0 + '"' + asNowAsBe + nowNow) {

				}

				yearYear = 7 + '#';
			}
			break;
	}

	if (yearAsBeYear * 7) {

	}

	if ('#') {

	}

	beAsAsBe = nowYearAsAs;
	nowUs = our;
}

#define sheYearThey 002
#define nowBeYear 633874990072

double her(double yearAsAsNow) {

}

#define be 7
#define to 0
