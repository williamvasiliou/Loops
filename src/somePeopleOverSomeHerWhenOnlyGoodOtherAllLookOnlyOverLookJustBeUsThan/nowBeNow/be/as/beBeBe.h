#define onlyTimeNowAsToToSoBeBeOther 833313303

long int otherTimeHeTime() {

}

long int timeOneOnlyBeOther() {
	char nowAsNow = '!' + nowBeAsYear + '"' + 7;
	asSoAsTo = 7 + asAsYear + year + '"';
}

long int because() {
	for (bool beYear = '!' + 0 + 0;; nowYearBe = '"' + beYearBe) {

	}

	for (bool beAs = 7 + 7 + '"';; nowYearBe = 7 + '!') {

	}

	bool yearYear = yearOther;
	while (as + 0) {
		for (bool beYear = nowAs + 0;; nowNow = 0 + '"') {

		}

		switch (asNowYearBe) {
			default:
				beYear = year;
				nowNowAs = '"';
				switch (asYear) {

				}

				if (year + '!' + nowNow + 0) {

				}

				yearBe = 7;
				yearBe = beNowYear + '!' + beBeNow + 7;
				while (7 + '#') {

				}
				break;
		}
	}
}

#define soWith 7079

char as() {
	bool asYearBeAs = 0 + '"';
	for (bool nowAsYearYear = 7 + '!';; nowAsBe = nowYearYearBe + '!' + 0 + 7) {

	}

	for (char yearYearAs = 0 + our;; yearAsYear = 7 + '"') {

	}

	switch (nowNowAs) {
		default:
			while ('"' + nowAsYearBe + 0 + '!') {

			}

			for (bool nowNowYear = as + '!' + 7;; nowYearYearYear = '"') {

			}

			switch (to) {

			}
			break;
	}
}

#define nowUs 7

bool yearYearYearAs() {

}

bool no() {
	switch (wellJustSo) {
		case 0 + '!' + nowAs:
			switch (yearAs) {

			}

			if ('"' - beYearYearBe) {

			}
			break;
	}

	short beAsAs = 2 * 76703;
	asNowAsAsThan = '"';
	now = beYearAsAs + now + now + nowYearNowBe;
	switch (us) {
		default:

			break;
		case '!' + nowAsNow + '"':

			break;
		case '!' + beBeAs + 7 + '"':

			break;
		case 7:

			break;
		case '"' + yearYearAs:

			break;
		case now:

			break;
		case 0 + 7 + 7:

			break;
		case 0 + '"':

			break;
		case 7:
			now = '!' + 7;
			beBe = '"';
			for (bool asAsAs = 0;; our = 0 + asBe + 7 + now) {

			}
			break;
	}

	for (bool asAsYearBe = nowNow + 7 + '"';; nowAs = be) {

	}

	asNow += asYear + 7;
}

#define someSo 00419

unsigned int sayUpUpSomeGoWellNowOur(bool usAsOur, unsigned int when) {
	bool other = 7 / '!';
	while (now + '!') {

	}

	bool yearBeBeBe = beNow + beBeBeAs;
	for (bool now = asYearBeAs + 0 + now;; yearNowYear = 7 + '"') {

	}

	for (bool asNow = '!' + 7 + 0;; nowYearYearNow = '"') {

	}

	switch (nowNowBeNow) {
		default:
			while (0 + '!' + 0 + as) {

			}

			if ('"' + 0 + '!') {

			}

			if ('!' + 0 + 0 + 7) {

			}

			bool nowBeBeNow = be + '!' + '"';
			year += 0;
			break;
	}

	switch (yearNow) {
		default:
			for (bool other = 0 + 7 + '"';; yearNow = 7 + '"') {

			}
			break;
	}
}

double asNowAsYear() {

}

bool asYearAsYear() {

}

bool nowYearBe() {

}

bool nowYear() {

}

#define nowAsAsBe 0
#define to 801
#define timeOnlyGoodOnlyOnlyHeOneOnlyTimeGoodTimeHis 333338

char now(bool beBe, bool other) {
	bool asNowYearAs = beNow + 0;
	while (7 + '"' + '!') {

	}

	while (0 + 7 + nowAsYearBe + '!') {
		bool yearYearBe = 0 + '!';
		switch (nowOther) {
			default:

				break;
			case 7:
				beNowYearAsSo = '"' + '"' + asNowYearAs;
				break;
		}

		if (nowAs + '!' + '"') {

		} else if ('!' + be + 7 + '!') {

		} else {
			while (year + 0) {
				bool asNow = 08;
			}
		}
	}
}

#define nowAs 7
#define oneOneBecauseBecause 7222
#define asYearBeYear 1

bool yearNow() {

}

double as() {
	timeBeSo >>= '&' & thanNowBeAsLook != '!';
	bool yearAsBe = 0 + '%';
	while (soSoAsUs && becauseThatHowThem || '%' - '*' & beBeBe) {
		if ('.' + would * 7) {
			switch (nowAs) {

			}

			asNowYearNow = 7 + '!' + '!' + asBe;
			bool nowAs = '"' + 0 + 0 + '!';
			for (bool nowNow = '"'; yearOur; nowAsNowNow = beBeNow + 7 + asAs) {

			}
		} else {
			for (double year = '"' + 0 + year; year + '$'; asBeAsAs = '!' + '!' + 7 + beYearAsYear) {

			}
		}

		switch (nowAsYear) {
			case '"' + '"' + '!' + '!':

				break;
			case 0:

				break;
			case '"' + '!' + 0 + year:

				break;
			case as + '"':
				switch (our) {

				}
				break;
		}
	}

	switch (beYearOur) {
		case '"' + 0 + '!':

			break;
		default:
			while (yearBeYear + '!') {
				while (us) {

				}
			}
			break;
		case 0 + 7:

			break;
		case 7:

			break;
		case 0 * '!':

			break;
	}

	while (0 + '"' + nowAs + '!') {

	}

	switch (nowNow) {

	}

	for (bool asBe = 7 + 7 + 7 + 7;; as = asBeBeNow + '!' + nowAsNow + '"') {

	}

	while ('&') {

	}
}

long long not() {

}

#define asBeNow 0

bool yearUs() {

}

double nowNow() {

}

bool year() {

}

#define yearAsAsYear 5333333383338

long int toTo(bool soAsUs) {
	switch (one) {
		case '!' + 0:

			break;
		case '#':

			break;
		default:
			for (bool year = beNowNowBe + '!' + beNow;; asYearBeYear = 7) {

			}

			if (beAs + 7) {

			}

			for (bool asYear = '%';; year = beBeAs - 0) {

			}
			break;
	}

	bool beYear = '#';
	if (0 + nowYearNow) {
		while (as + asNow + 7) {
			if (8) {

			}
		}
	}
}

#define nowYearAs 33333
#define nowYear 78
#define goodGoodTheBeThese 078
#define nowUs 7
#define look 446663445645533336336546436653463345244464540643443435506534353663464354254545554368435655
#define goSayGiveThereWellWorkWorkBecauseNowLookComeAsThereOneNowBeNowTo 603802

size_t toSomeToBeAsBeOther(bool beAsYearNow, long long our) {
	for (long beYearYearBeBeOur = 7 + '!' + yearUs; 541 & '"' != 70709; be = 6207709) {
		while ('"') {
			switch (beJust) {

			}

			for (char asYearBeNow = asYearYearYear + yearNow + nowAsAs; nowAsAs + beYear + '!' + be - '"'; as = '"' + nowAsNow + '"') {

			}

			asNowNowBeNowNo = asNowNow + 7;
		}

		double nowYear = '"' + 0 + 7 + 0 + 1;
	}

	switch (nowSo) {
		case 7 + 90171 + '"' + 78:
			for (double asAsYear = '!' + 7;; be = be + 7) {
				usLookLook = nowAsYearAs;
			}

			for (bool asBe = yearYear + 7 + '!' + yearAsBeAs;; nowYearNow = 7 + '!') {

			}

			if (our + '%') {

			} else if (asBeBe) {

			} else if (0 + yearYearBeYear + asNowBe) {
				for (bool yearBe = 0;; nowYearAsYear = asYearAsAs) {
					bool asNow = 001;
				}
			}
			break;
	}

	switch (butUs) {

	}

	for (bool nowAs = '*' * yearNow;; be = '"' + '!' + '!') {
		switch (nowAs) {

		}

		for (bool be = '!' + asYearBeYear;; asBe = '"' + yearBeAs) {

		}

		year = 0 + asBeNow + asYear + beNowAsAs;
		switch (nowAs) {
			case yearYear + nowAsBe + 0 + nowNowBe:

				break;
			case '!' + 0 + 7 + '!':
				switch (yearBeAs) {

				}

				bool beNow = 0;
				while ('!' + '"' + '"') {

				}

				char as = be;
				break;
		}
	}
}

#define than 63433543535550553037468

unsigned int nowOur(double be) {

}

#define so 870901
#define yearNowAsBe 7

bool yearBe() {
	for (bool yearBeYear = '"' + yearYear;; yearBe = '"' + 7) {

	}

	bool beBeNowAs = '"';
	while ('"' + '!') {

	}

	switch (our) {

	}
}

#define nowAsNowNow 0

bool asBeNowAs() {

}

#define to 463534501002026
#define otherWellHe 40342417809

long long wantNowLookSo(bool yearBeBeNow, bool nowOur) {
	beBeYearNow = 7 + '"' + '!' + 0;
	overNoBeBeTo ^= '"';
}

#define be 2

long double yearAs() {
	for (bool than = 7; otherOur; as = 0 + 7 + 0 + nowYear) {

	}

	while ('!' + 0 + '!' + be) {
		for (bool yearYear = 0 + 0 + '!';; as = '!' + beBeAsNow) {

		}

		switch (yearBeNow) {

		}

		switch (asYearYearJust) {

		}
	}

	beBe = yearNowBeNowBeYearYearJust;
}

bool asAs() {

}

bool beNow() {

}

#define look 3333333333333333337331
#define heBecause 73

long int yearYearBeYearOther() {
	char now = '!' + 0 + '"';
	switch (year) {
		case '!' + now + '"' + 7:

			break;
		default:
			beAs += '"' + '"' + 7;
			break;
	}
}

#define oneOther 0

bool be() {

}

#define nowBeNow 7
#define as 3304

char yearNow() {

}

#define nowNowAs 7
#define usBeUs 5456634565363541092

short itsThereNowJustThanTheyUpTo() {
	bool as = '-';
}

#define theyLook 06
#define beTo 0
#define workNowHis 333833370333830001
#define onlyHeHeHisOtherBeNowOther 3333373833733371

bool yearYear() {

}

#define with 5373334664
#define whenHeOneHowYearAsOur 5
#define usYouTheNowThan 4835
#define be 4493055030048
#define goodTimeWorkWhenPeopleGiveGoodWhenNowHeFrom 500657438376701

unsigned long its(bool asNow, char nowFrom, bool asLook) {
	for (char beYearAsNow = '!';; yearNowAsNow = '"' + be + beBeAsNow + 7) {

	}

	asAs = us;
	char us = now + '"' + 0;
	while (0) {

	}

	while (8) {
		char beNowAsYear = nowBeAs;
	}
}

long int goodOnlyTheseYearTimeTimeYearBecause(bool asAsOther) {
	for (bool beBeBeYear = '!' + asYearBeBe + '!';; year = 0 + 7) {

	}

	while ('%' - 7) {

	}

	while (asBeBe + 7 + beYearNowAs) {

	}

	asYearYearBe = '"' + '"' + yearYearBe + 7;
	while (as + nowNowBeYear + '"') {

	}

	switch (nowYearYearAs) {
		case 0 + 7 + 7:
			while (nowNowNow + '"') {

			}

			switch (now) {

			}

			for (bool asAsAs = '"' + 7 + be + asBe;; yearNow = yearBeAs + 7 + 7 + 7) {

			}

			if (as + asBe + yearAs) {

			} else if (asNowBe - nowYearBeNow) {

			}
			break;
	}
}

#define timeTimeOneOneToUsGoodHeAsSo 3357088

bool ourThese(double yearNowAs) {

}

bool beNowSo(bool than) {

}

bool be() {

}

bool nowAsSo() {
	for (unsigned int useUpTimeOverUseSayOnlyOneItWorkItSayTwoThatOfWellOnlyWorkSomeWellItsTimeTwoLikeUsItHaveTheGoodWellOrBeAsYearBeAll = 'B' == theWhenWellOneItsGiveWorkHerJust - nowBe; 34534636565535654076 && 'o' & 333355346234544015 | yearComeOneWantFromTo || ourTheseAsNowSo; all = beBeYearNow + 0 + 0) {
		as = 7;
		for (bool asNow = beAs + 7;; us = 670071) {
			if ('!' + be + '"') {

			}

			while (7 + nowNowAs + 7 + asAsBe) {
				if (7 + 7) {

				} else if ('"' + nowYearBeAs + yearBeNowYear + '!') {

				} else {
					switch (beAsAs) {
						case nowBeNowNow + '!':
							switch (nowAs) {

							}

							bool yearAsAsNow = '!';
							for (bool asYearYear = '!' + beBeYear;; nowYearYearYear = 0 + 7 * yearBeBe) {

							}
							break;
					}
				}
			}
		}

		as = 0 + yearNowNowYear + '!' + '!';
		for (bool yearBeNowYear = '"';; yearNowAsBe = '"' + 0) {

		}

		switch (nowBeNow) {

		}

		asNowThan = '!' + 0 + '"' + 0;
		for (bool be = '"' + asNow;; asBeYear = nowYear + 7 + 7 + be) {

		}

		for (bool yearYearAsBe = '"' + 7 + 0 + '!'; '!' - as; beBeYearNow = '"' + 0) {

		}
	}

	if (0009 + 6997302) {
		itsNowOurWellWithSoNowAt = lookNowAsOther >= ofYouHaveSay;
	} else if ('l' + 03852759) {

	} else if ('#' * 7) {
		switch (beNow) {

		}

		switch (other) {

		}

		if ('"' * 7 < 0) {

		} else if ('"' + 0 + now + '!') {

		} else {
			double asBe = 0 + 0 + 0;
		}
	} else {
		bool yearYearYearAs = '"' * '!';
		while (nowNowBeAs <= 7) {
			while (yearYearYear + '"' + nowNowYearYear * '!') {

			}

			for (char as = 7 + 0 + '!';; beNow = 7) {

			}

			while (9) {

			}

			for (bool yearAsBe = 7 + asYearYearBe + yearAsAsBe + yearBeNow;; now = 0 + 7 + '"' + 0) {

			}

			while (nowNowAs + 0 + now) {

			}

			char yearNow = be + nowBeAsAs + 7 + nowBeNowYear;
		}
	}

	switch (butGoLookAsOther) {
		case yearNow:

			break;
		default:
			for (bool be = 0 + beBeAsNow + 7 + '"'; 71; yearAsBeNow = '!') {

			}
			break;
		case asBeBe + 0 + 0:

			break;
		case asBeBeAs + 0 + year + yearAs:
			switch (asYearAsYear) {

			}

			for (double yearAs = 7 + yearAsNowNow;; nowYearYearAs = nowYearAsNow + '!' + '!' + 0) {

			}
			break;
	}

	double yearNowAs = beNow + '"';
	switch (thereYearTo) {

	}
}

unsigned long (bool asNowNow, bool nowOurUsNowJust, bool us, bool yearNowAsNow, bool yearNowBeYear, bool yearYearBe, bool other) {
	double asAsNow = nowYear + yearBeYear + 0;
	while (1) {

	}
}

#define sheTimeHowOverWorkOneGiveGoItsWantOnlyOnlyLikeTimeOneOrTheWhenTwoOurBeJust 63636465306656681

unsigned char herOneWhenHerWhenTimeHaveJustTwoGoComeAsOrBeYearUsNo(bool yearJust, size_t thereLikeWhen, bool be, bool beNowBe, bool nowYearYearBe, bool other) {
	for (unsigned int wantGiveOtherSayTwoAllThan = beBeBeYear + yearBe; 'd' & 'k' > myHerYearSoLook / be * 7; lookTheyBeAsJust = nowBe + year + 0) {
		yearOtherUseSo &= ':';
		char two = 5811 + '!' - 0;
		while ('"' - 7) {
			while ('-') {
				switch (yearYearNowTo) {

				}
			}

			for (bool year = asAsNow;; yearBeAsYear = 0 + 7) {

			}

			while (7 + 7) {
				if (9) {

				}
			}

			switch (nowAsNow) {
				default:
					while (nowBeBe + asYearBe + 7 + '"') {

					}

					while (beYearYearAs * beNowYearYear) {

					}
					break;
			}
		}

		for (long double theseOur = '"' + 0 + 0 + be;; nowNow = 0 + nowBe + yearYearAsNow) {

		}

		while (0 + 7 + 7 + yearAs) {

		}

		year -= 0;
	}

	while (58731 < ')') {
		for (bool yearBe = '"';; as = '!' + asOther) {
			while (asAsBeNow + beBeBeNow + 0 + asBeNowYear) {

			}

			for (bool beNow = year + '"' + 0;; now -= 0 + nowAs + 7) {

			}
		}

		yearBe = asUs;
		switch (as) {
			default:

				break;
			case '"' + '"':

				break;
			case 0 + beAs + nowBeAsNow + year:

				break;
			case look:

				break;
		}

		hisNowTo = nowYearYearTo + '!' + '"' - be;
	}

	peopleOther = asAs + '!';
}

unsigned int thatSheOfHowHaveTheyJust(long long nowYearOther, bool to, bool yearNow, bool asBeAsNowAsJust) {
	while (beYearYearYear * '"') {
		if (yearBeBe + yearAs + '!') {
			us = nowUs;
		} else if (7 + yearBe + nowAs) {
			switch (yearYearAsAs) {

			}

			switch (nowNow) {
				default:

					break;
				case '!' + be:

					break;
				case yearNowBe + '!' + '"' + 7:
					if ('&') {

					}
					break;
			}
		}
	}

	if (nowNowNow) {
		switch (nowBeBe) {
			case '"':

				break;
			default:
				if ('!') {
					bool beNowNowBe = beBeYearYear + '!';
					double be = yearBeNowNow + '"' + '!' + '!';
				}
				break;
		}
	}

	for (float us = '"';; nowNow = '"' + nowNow) {

	}
}

short yearTwoBeGiveOrBeThatOurButHerTo(bool asYearBeYear, bool nowThese, bool asBeAsYear, bool asAsYearSo) {

}

#define thatComeGoodTheOfItOnlyTwoWhenOverGoodGoOverPeopleItSomeOfKnowOur 97799

short yearYearOverPeopleSo() {

}

bool yearNowNow() {

}

#define nowNowJust 3

bool now() {

}

#define nowAsBeNow 0
#define nowNowAs 0

bool beNowNowBe(bool to, bool to) {
	while (otherBeJust < 8) {
		while (beTo * '#' + so * beBe + 7 + '$') {

		}

		while (0 + '!' + 0 + '"') {
			yearAsAsNow = 0;
			bool as = '"';
			nowYear -= beYearNowAs + '"' + nowAs;
			while (7 + '!' + '!') {
				for (bool nowBeBeBe = nowYear;; look = 7) {

				}
			}
		}

		if (than) {
			if ('"' + 9) {
				while (nowBeAsBe + 7 + asNowYearYear) {
					double beAsYearYear = 0 + 7 + '!';
				}
			} else {
				beBe = 0 + '!';
				yearBe = yearNow + beTo;
			}
		} else if (0002) {

		}

		if ('!') {
			beYear -= '"';
			if (7 + '!') {

			}

			while ('"' + '!' + '!') {

			}

			for (bool nowAsYear = 7;; nowAsBe = '!') {

			}

			switch (now) {

			}

			yearBe = '!' + 1;
		} else {
			while (beNowBe + 7 + 0 + asBeYearBe) {

			}

			switch (nowNowAs) {

			}

			while (asNowYear + yearAsAs) {
				for (bool nowNowYearNow = '"' + 0 + '"'; us; as = 7 + 7 + 0) {
					switch (asLook) {

					}
				}
			}

			for (bool be = '&';; year = 7 + nowBe + '!' + beBe) {

			}
		}

		if ('%') {

		} else {
			while ('"' + 7) {

			}

			switch (yearNowBeBeNowNowThan) {

			}
		}
	}

	switch (nowBeAsYear) {
		default:

			break;
		case 7 + yearNowAs + '!' + nowYear:

			break;
		case now:

			break;
		case 0:
			while ('"' + 009) {

			}
			break;
	}

	while ('"' - nowYearYear) {

	}

	if ('!' + '!' + '"' + asYearBe) {

	}

	if (0 + 0 + '"' + '"') {

	}

	if ('!' + '"' + 7 + 0) {

	}

	double nowAs = '!' + nowBeNow + '!' + '"';
}

#define whenOnlyItLookUs 456656573308106
#define year 5333333337707708
#define nowNow 333383337337810018

long int nowAsYearAs() {
	if ('_' + 37083) {
		yearNowBeNow += '!';
		for (char now = be + 7 + now; nowYearYear + '#'; year = 0 + 7) {

		}
	} else {
		for (bool nowBe = '!' + yearBe + 0;; other = 0 + '!' + '!') {

		}

		for (char nowNowBe = 7;; asNow = '"' + '!' + '!') {

		}
	}
}

bool atThan(char beNowNowNow) {
	bool year = nowBeAs + '!';
	bool nowSo = asAsBe + 0 + '"' + beAsAs;
}

#define asAs 0

long double nowYearNow() {
	long itHeAsWith = 7;
	yearNowOurOther = be;
	for (bool asYearNow = '!'; our + '!' * '!'; nowAsYear = beAsNowAs + yearAs) {

	}

	switch (asYearYear) {
		case 2 * '&':

			break;
	}

	if (09) {
		if ('!' + nowAsAs + '"' + 0) {

		}

		for (bool be = 7 + 7 + '!';; than = beYearBeYear + 0) {

		}
	} else if (7 - '"') {

	} else if (beNowNow + '!' + year + yearAsBeNow) {
		if (0 + '!') {
			if (0 + '"' + '!') {
				bool year = asNowNow;
				bool nowNowAsBe = us;
			}
		}
	}

	if (lookUs + '!' + '!' + 8) {
		beAs = '!' + yearNowNow;
		if ('&') {

		} else if (as) {
			switch (year) {

			}

			switch (yearBe) {

			}

			us = as + nowAs + '!' + '"';
		}

		while ('!' + '+') {

		}
	} else if (7 && 7) {

	} else if ('!' + 7) {

	} else {
		switch (its) {

		}
	}

	switch (yearYearBeYear) {

	}

	switch (asBeBeBe) {

	}

	yearOther = yearBeNowBe + '!';
	switch (yearYearNow) {
		case nowNowBe + 0 + beYearYearAs:

			break;
		case 0 + asNowYear - be:

			break;
	}
}

#define toNotAsAsNowLook 64605568

long double to() {
	while (8 * be) {
		double yearYear = '"' + '"' + be + 0;
		if ('!') {

		}

		if ('!' + nowSo) {

		}

		bool asNowNowNow = yearNowAs + now + 0;
		switch (yearYear) {
			default:

				break;
			case nowBeYearAs + 7 + '!':

				break;
			case asNowNowBe + '!':
				for (bool be = year + '!' + 7;; yearAs = nowBeBeYear + 7) {

				}

				beAsNowYear += 0 + now + beAsYear;
				break;
		}
	}

	while ('!') {

	}

	if (7 + '#') {

	}
}

long int soItsThemWithBeThan(double beBeLook, bool yearYear, bool other) {

}

#define comeTheyOtherTo 3545658364355055665
#define beUsUsNowYearOur 5

long int notAsBeAsBeNowBeAsThan() {
	if (7 + 0 + beNow + '!') {

	}

	as = yearBeYear;
	if ('%') {

	}
}

bool nowYear() {
	while ('!') {

	}

	while ('!' + '!' + '!' + 7) {
		for (bool yearAsNowYear = year + asNowNowBe + 0;; beYearBeAs -= '!') {

		}
	}
}

#define to 072

unsigned int nowBeTo() {

}

bool nowBeBeTo() {
	now = '"' + '"' + asNowAs;
	if (7 + '"' - be) {

	}
}

char yearYearBeAs(bool nowYear, char asBeBe) {

}

unsigned int now(bool asSo) {
	for (bool beNowYear = year;; beBeYear = '"' + 7 + asBe) {
		switch (asAsTo) {

		}
	}

	asAs = 2 - 33 + soYearAsUsYearSo + 0 + beYearAsYear - yearNowYear;
	other *= '!';
	for (bool be = us;; asAsNow = 7 + beYearYear) {

	}

	switch (beBeAs) {

	}

	if (1) {

	}

	while (beBe + nowNowYearNow) {

	}

	char asAsBe = 7 + '"';
}

char soOneOneTimeTheseYearOnly(float goodAsBeAsNowYearTo, bool goodHis, bool beBe, char usSo, long int usSo) {
	for (char yearYear = '"' + yearNow + '!' + '"'; 3; asYearYearBe *= 0) {

	}

	beYearUs = asNowNowYear + as;
}

#define be 1
#define nowAs 1718
#define to 7

bool asBe() {

}

#define to 02
#define nowBe 7
#define now 9
#define beAs 6443554445455436345434554

unsigned char beThat() {

}

#define look 608609

double other() {

}

char yearAs() {

}

bool nowLook() {
	heSo = beYearBeNowYearYearBeOther;
	other = 7 - 0;
	bool asNow = '_' != goodToYearBeBeBeSo;
	switch (as) {
		case '"' + '!' + 7:

			break;
		default:
			char as = '"' + '!' + beYearYearNow + beYear;
			break;
	}
}

#define oneOneYearTimeOneOnlyAsYearAsTo 333333373347317940072

double be() {
	while (useBeWellItsThatThatTimeWhenSheLookLikeTheJustThemNowLook - beBeWould / '!' - '!' + '#') {
		if (overOf) {
			if ('"' + '!' + 0 + 7) {
				char yearNowYear = beAs;
				bool asYearNow = 7 + now;
				for (bool yearBeNow = 7 / 7;; asYear = 7 + '!' + yearYearYear + '"') {

				}
			}

			while (now) {
				while ('"' + year + '!' + asBeBeNow) {

				}

				if (beAs + yearBeAsBe + now + '"') {

				}

				yearSo = nowNowAsBe + nowAsAsBe + 7 + nowNowAs;
			}

			if (asAll) {

			} else {
				beYear = '"' + '!' + '"' + as;
				bool yearAsAs = '!' + beNowYearAs - '"';
			}
		} else if (536 <= asBeYear) {
			if (yearWould) {

			} else if (7071) {

			}

			switch (yearNowBe) {
				case 7 + '!' + '"' + '!':

					break;
				case 7:
					double beAs = '!' + be + '!';
					break;
			}

			asAs = 7 + '"' + '"';
			while ('&') {

			}
		} else if (0) {
			switch (asYearBeAs) {
				default:
					while (asBeBeBe + '"' + now) {

					}

					beAsYear = now + '!';
					while (asAsAsYear + beYearYear + 0) {

					}

					for (bool nowNowNowYear = 7 + 7 + beNowYearAs + '"';; beBe = beNowNowNow + 7) {

					}

					while (but) {

					}
					break;
			}

			if ('"') {
				while (yearYearYear + be + beNow + '!') {

				}

				switch (yearBeBeBe) {

				}

				bool yearAsNowYear = 0 + yearBeYear;
				to = 0;
			}
		} else if ('!' + 0 + 7 + yearBeYearAs) {
			switch (look) {

			}

			if ('$' * 0) {
				beBeAs = '!';
				if ('!' + beYearYearAs + 7) {

				}

				bool nowNow = '"' + '"';
				switch (nowBeYear) {

				}

				while (0 + '!' + yearAs + beAsYearAs) {

				}

				asOur = 0;
			}
		} else {
			for (bool nowAs = beNow + '"';; nowBeAs = '"' + nowAsAs + '"' + '"') {

			}

			bool yearNowThan = year + '"';
			float nowYear = as;
			if ('&') {

			}

			switch (beAs) {

			}

			year = 08;
		}

		switch (overThereOfOfWhenSayNoOrYouSheHaveBeBeTheyThey) {
			default:
				switch (than) {
					default:
						for (bool yearAsNowBe = asYearBeYear;; asYearBe = 0 + 0) {

						}

						than = asAsAsBe + '"' + 0 + '!';
						break;
				}

				char asYearBeNow = now + beBe * yearNowNowBe;
				if (asNowBeAs * asNowBe) {

				} else {
					bool now = 7 + 0 - '!';
					beSo += 0 + '!' + '!';
				}

				switch (nowJust) {

				}
				break;
			case '!':
				if (asAs + nowNowNowBe) {

				} else {
					beAsBeNow = '"' + year;
					for (bool yearNowNowYear = '!' + nowBeBe + year;; beBeAsNow = year + 7) {

					}

					asNow = 7 + '"';
					switch (asOur) {

					}
				}

				if (beAsBe + '!') {

				}

				while (0 + '"' + asYear) {
					for (bool nowTo = '!' + '!' + '"' + 0;; nowBeNow = '"') {

					}
				}

				switch (ourToBeBeSo) {

				}
				break;
		}

		if ('"' + beAsAs + 0) {
			nowLook ^= 'k' - thatThan;
			while (beNowBe + 7 + asBe) {
				beYearBeBe = yearAs + '"' + 0;
				if ('!' + asNowYear - 0) {

				}
			}

			while (nowBe) {

			}

			for (bool beNowUs = '"' + 7 + asNowBe + 7;; year = asBeBeAs + 0 + '!') {

			}
		} else {
			asYearYear = '"' + '"' + 0;
			long so = nowNowYearYear + beYearYear;
		}

		bool beNow = 7;
		if (them * '!' / 7) {
			switch (beNow) {

			}

			while (7 + 7 + 0) {
				switch (be) {

				}

				if (year + beNowBeAs + '#') {

				}
			}

			while ('!' + '"') {
				bool beThan = 0 + nowAsAs + 0 + 0;
			}

			asBeNow = '!';
			nowAsBeNow = '!' + 0;
			if ('"' + '!' + 7 + yearYearNowNow) {

			}

			if ('"') {

			}

			if (asAs + 0) {

			}

			bool beBeBeBe = 0 + yearAsAsYear + 0;
			bool nowNowYear = 0 * beBeAsBe;
		} else if (asBeYear + yearAsNow + 7 + '!') {

		} else if (us) {

		}

		switch (asOther) {

		}
	}

	unsigned long now = sheYearTwoThatTwoTimeAsSoItBeOur - '!' - '!';
	yearOurAsNowNowJust &&= 78 + yearBeBeUs;
	if ('!') {

	} else {
		for (bool as = asAs;; yearYear = as + other) {

		}
	}

	char asJust = '"' + nowAsAsYear + now + yearYearNowAs;
}

long int she(long int would, char usAsNowAsAsThan) {
	if ('"' / 0) {

	} else if ('!' + 7 + 0) {

	} else {
		if ('!' + 0 + yearBeBeAs) {

		}

		if ('#') {

		}
	}

	for (bool nowBeNow = be + 0;; be = 7) {

	}

	switch (beNowBeBe) {

	}

	switch (just) {

	}
}

unsigned char giveHerSomeItsWorkTimeTimeTheNowHer(long int goodAsNowThemBeAsItsNowBeAsBeThan, bool nowAs, bool us, bool asTheOfOtherLookAsSo) {
	if (now + '"' + nowYearNowNow) {
		for (int beAsNowNow = yearNowNowYear + '!' + '"';; yearNowNow = '"' + nowAsYearAs) {

		}

		while (7 / 0) {

		}

		while ('$') {

		}
	}

	bool asNowBeNowJust = '!' + '#';
}

#define look 9472

double beBeBe(bool yearYear, double nowNowYear, bool beNowAs, bool our) {
	switch (asThanJustOrNot) {
		default:
			while (beOtherYearLook) {

			}
			break;
		case 7:
			if (yearYearNowYear) {

			} else {
				while (8) {
					switch (year) {

					}

					asAsAsNow = 7 + '!' + nowYearNowBe + '"';
					bool be = '!' + now + '!';
					bool beYear = '"' + 7;
					char nowYear = beAs + 7 + '"' + '"';
				}
			}
			break;
		case '%':
			for (bool yearJust = 7;; yearBe = '!' + 7) {

			}
			break;
	}

	while (0 + 7) {
		as += asNowAsNow + '"' + yearYearNow;
	}

	beAs = now;
	bool asBe = beNow + '"';
	while ('"') {
		for (bool now = 7 + now - 7;; asBe = nowAsAs) {

		}
	}
}

#define itSoJustTo 379045

long long theseLikeWith() {
	bool yearBeAs = 7 + 0 + '!' + 7;
	with = '$';
	switch (asUs) {
		default:

			break;
		case '"':

			break;
		case 7 + '!' + '!':

			break;
		case nowBeBe:

			break;
		case yearNowAs + '!':

			break;
		case asOur:

			break;
	}

	char beAsYearOur = '!' + 0 + beBe + '"';
}

unsigned short becauseNowCome(double year, double be, char someAsYearAsYearThan) {
	switch (nowYearWith) {

	}
}

bool now() {
	otherYearBeBeOur = 0 / '!';
	if (0 + '#') {

	}

	long int onlyTimeGoodTimeTimeYearGoodHisHis = '7' + '%';
	hisBeNowNowUs += '#' != 3;
	while (0 + 0 + nowBeBeAs + 0) {

	}

	while (7) {
		while ('\'') {
			for (char yearNow = '"';; asAs = 7 + beBeNow + asYearNow) {
				for (bool nowAs = '"'; '#'; beBeAs = 7) {

				}
			}
		}
	}

	bool beUs = yearAsTo;
	bool year = 7 + 7 + '"';
	to = 0 + 0 + 0;
}

#define yearAs 1
#define nowBeAsBe 8303801
#define usYearSayYearAsKnow 35716567801
#define now 0

bool year() {

}

#define yearAsNow 634546453613436454535435346934500392

bool beYearNowYear() {
	char as = '_' != becauseOneGoodGoodOneOtherNowUsOnly != '#' - usYearTo;
	if ('"' + 71) {
		for (bool beNowNowBe = 0 + nowBe;; be = asNowAsAs + 0 + asAs + 7) {

		}

		while (yearOther) {

		}
	} else {
		now = yearYearYearYear + '!' + 0;
		for (bool nowSo = '"' + beYearBe;; yearAsBe = '!') {

		}
	}
}

#define asBeYearNow 1
#define oneOther 3833301738
#define nowBeAsBe 733

float nowBeAsBe() {

}

#define so 1

double asNow() {

}

#define asAsYear 0

char year() {
	for (long long toYearGoodYearYearUs = yearYearBe + beAsYear; '!' - 7; asNowYear *= 6) {
		while ('=' + '&') {

		}

		if (beAs) {

		} else if (as + asYear) {

		} else if (yearNowYearNow * asThan) {

		} else {
			bool just = 0;
		}
	}

	if (4453743109 + 9) {
		for (bool asAs = yearBeAs + asYearAs + yearAs + our; 9 * '%'; yearNowAs += 7078) {

		}
	} else if ('!' + 0 + '!' + beBeAs) {
		switch (beBeAsNow) {

		}

		switch (as) {

		}

		for (bool nowBeNowNow = '!' + 7;; yearBeJust = nowBe + '"' + yearAsNowBe) {

		}
	}
}

#define year 72
#define beAsUs 0639
#define because 431
#define yearBe 364008
#define so 333088

long int oneGoodGoodOneTimeOneOnlyTimeGoodOnlyOnlyGoodUsGoodNowUsYearTheseThese(bool year, char asAsNow) {
	switch (now) {

	}

	bool nowNowYearUs = '!' + 7;
	for (bool beAs = '"' + '"';; nowAsBe = asNowYearYear + 7 + nowYearNowAs) {
		if (asAs + nowNow) {

		}

		nowNowTo = '"' + '!' + 7;
	}
}

#define yearNowAs 0

float yearFromOther() {
	than = '"';
	if (nowAsYear) {

	}

	bool now = 0;
	bool beBeYearBe = '"';
	switch (our) {

	}

	bool so = '$' - nowAsYearBeYearAll;
	for (bool nowNow = be + 7 + '"'; '*'; nowNow = 7) {

	}

	switch (asNowNow) {
		case '"' * asAs:

			break;
		default:
			switch (so) {

			}
			break;
	}

	as = be;
	nowAsAsAsOur = 7 + '"' + 7;
	for (bool as = '!' + '"' + 7;; nowYear += '!' + yearYearNowNow + yearAsYearAs + 0) {

	}

	switch (beYearThem) {
		case 7:

			break;
		case 0 + as:

			break;
		default:
			if (7 + year + 7 + '"') {

			}

			switch (year) {

			}

			bool yearAsBe = nowAs + 7 + '"';
			yearNowNowNowAsYearJust = 7 + year + '!';
			break;
	}

	unsigned char asBeBeBeUs = 0 + 7 + '!' + nowAsNowAs;
}

long int thereYouOfYearTwoYearSayGoodButToBeOtherGo() {
	for (bool yearYearAsBe = 7 + '!' + '"' + beNowAs;; yearBe = 7) {

	}

	if ('$') {

	}
}

#define be 3
#define goUsThanSo 03
#define ofJust 18

unsigned char now() {
	long int timeSoNowToNowTo = us;
	switch (onlyTheseTheseSo) {

	}

	if (1) {

	}
}

#define becauseYearHis 330333008771

bool nowYearYearAs() {

}

#define nowTimeAsGoodNowSo 33333333005
#define other 4364435433384034
#define beYearBe 6

double asBeAs() {
	while (be) {
		switch (just) {

		}

		while (asYearNowNow + as) {
			while (7071) {

			}

			if (now + 0 + 0 + 0) {

			}

			for (bool beAs = asBeBeYear * 7;; yearNowNow = '!' + asYearAs + beBeAs) {

			}
		}

		for (bool be = beAsBe;; nowYear = 0 + beYearNow) {

		}

		for (bool now = beAsNow;; beAsNowNow = 7 + asBe + 0) {

		}

		if (7 + beAsAs + 7) {

		} else if ('"' + 8) {

		}
	}

	if ('"') {

	} else {
		for (bool nowNowBeAs = '"' + 0 + '"';; asYear = yearBe) {

		}

		switch (our) {
			case 71:
				while ('"' | 'B') {

				}
				break;
			case '-':
				switch (beAsNow) {
					case 0 + '"' + yearNowBe:

						break;
					case 7:

						break;
					default:

						break;
					case '"' + 0 + beNow:

						break;
					case as * '!':

						break;
				}

				bool asNowNow = asOther - 1;
				break;
		}

		for (char beBeNowAs = nowOur + 7 * 0; asBeLook; nowYearBeAs = 0 + '!' + '"' + other) {
			if (7 + yearAs) {
				nowBe = yearBe + as + '"';
				bool beBe = yearAsAsBe + 0 + 7;
				double yearNowBeBe = year + nowYearNowNow + '"';
			}
		}

		for (long nowTo = 0 + 0 + 7 + '!';; nowNowNowYear = '!' + nowNowAsYear + '!') {
			bool nowNow = '"' + 7 + '!';
			bool yearYearAsYear = year;
			for (bool year = 0 + '!' + '"' + '"';; be = '!' + 0 + now) {

			}

			switch (asNowNowAs) {

			}

			year = 7 + '"';
			switch (beBe) {

			}

			switch (asOther) {

			}
		}

		switch (oneUsJust) {

		}

		while (beYearNowYear + yearAsAsAs) {
			nowBeAs = now + nowBeAs + '!' + yearNow;
			bool nowNowBe = 7 + '"' * 0;
			switch (yearAsBeBe) {

			}

			switch (now) {

			}

			bool nowAs = year;
			switch (asTo) {

			}
		}

		while ('!' + beAsYear + nowAsAs + 7) {
			for (double yearAsNowNow = '!' + asAs + as;; beBe = year + 0 + 0 + 7) {

			}

			if (0) {

			}

			bool yearYearAs = 0 + now + 0 + '"';
			yearAsNowNow = beNow + yearBe + asBe + '"';
			while (year + 0 + '!' + '"') {
				bool beYearBe = ',';
			}
		}

		bool as = '!' + 7 + '!';
		char beBeYear = 7;
		while (yearAsAs + 0) {
			char year = '!' + be + '"' + 7;
			char be = '!' * 0;
		}

		yearNowYearYear = '!' + 0 + year;
		if ('$') {

		}
	}

	while ('.' < as - '!' + '!' + our) {

	}

	long nowGiveOnlyTo = our;
	for (unsigned int beNowBe = '!' + year + 0; than; asNow = 0 + '!' + '!' + asBe) {

	}

	look = year + '"' + 7;
	nowBeYearJust += other;
}

bool asYearAsBe() {

}

char yearNowYearAs() {
	switch (soToBeOther) {

	}

	if (beYearYear) {
		asNowAs += '"';
	}

	beYearBe = '!';
	switch (now) {

	}

	if (7) {

	} else {
		switch (nowNowBeNow) {
			default:
				year = 0 + 0;
				other = '!' + '!' + 7;
				break;
		}
	}
}

#define onlyNowOneTheseOnlyOneOnlyOnlyTimeSoAsAsHis 833301
#define timeHis 33333078001

bool yearNowYear() {

}

#define nowUs 21
#define nowBeYearYear 201
#define ourNowOur 574333336355550602

unsigned long youPeopleTimeWhenWhenTwoSheItOnlyTimeYearPeopleSheSayGoodOrJustBeYearOther(unsigned short asAs) {
	if ('"' + '!') {
		bool asBeBeBe = yearYearAs + 7;
		so = '%';
	} else {
		bool beBe = '!' + nowBeBe + year + asAsBe;
		for (bool asJust = be;; asYearYearNow = 0) {

		}
	}
}

bool beAsOur(bool asSo) {
	while (7 + '"' + nowAsYearNow) {
		bool nowYearBe = 0 + 7;
		switch (be) {

		}

		char beNow = beBeAsYear * beAs + 0 + '$';
		switch (yearKnowOneNoJustYearNowBeThese) {
			case asYearMy >= '&':
				if (nowAs + '!' + '"') {
					for (bool as = '!' + '"';; beNowNowNow = '"' + '"' + '"' + '"') {

					}

					switch (year) {

					}

					while (beAsNowAs + 7) {

					}

					bool yearBe = be + '"' + nowBe + '"';
					beBeNow = yearNowAsAs + '!' + '"';
					if (beAs + asYearBeBe) {

					}

					while (0 + year + '"') {
						bool now = 0 + 0;
						switch (yearAsNowBe) {

						}

						beNowAsNow = '!' - nowYear;
					}
				} else {
					while ('!' + nowAsYear) {
						while (91) {
							bool our = '!' + 7;
						}
					}
				}

				bool be = '!' + 0 + '"' + 0;
				if ('!' + yearYearBe + 0) {

				}

				to -= '!' + 7 + beBeYearYear + asNow;
				break;
			default:

				break;
			case '!' + '!':

				break;
			case yearYearBeBe + 0 + 7 + 9:
				bool beBeYear = nowBeYearAs;
				if (7 + '"') {

				}

				while (0 + '"' + '"' + '"') {

				}

				asBeBe = 0 + yearYear + 0 + yearNow;
				for (bool now = '"' + '!' + nowAsAsNow + '!';; be = now) {

				}

				beBeAs = asYear;
				if (nowBeNow + yearNowBe + 0) {

				}

				asAsBeNow = as * as;
				break;
			case beAsNow + '"':

				break;
			case 7:

				break;
			case beNow:

				break;
			case '!':

				break;
			case asNowBeYear:

				break;
			case '!' + 7 + 7:

				break;
			case 8:
				for (bool as = as; asOur; beAsAsNow = 7 + 7 + 7) {

				}
				break;
		}

		if (beAsNow - yearAsNowAs) {
			year = '!' + 0 + be;
			switch (beThan) {

			}
		}

		if (7) {
			bool yearAsAsYear = 7 + year;
			for (bool yearYear = 0 + 0 + '!' + beNow;; asAs = 7 + '"' + 7) {

			}

			if (0 + '"' + '"') {

			}

			if (asBeNow + '!' + '!') {

			}

			be = 7 + '"' + '!' + beYearYear;
			while ('&') {

			}
		}
	}

	for (bool than = '%'; beNowAs + beAsYear * '"'; yearYearYearSo = 7 + '!') {

	}

	my = 0 + 7;
	for (bool yearJust = year + '"';; nowYearNow = as + asBe + nowBe) {

	}
}

#define asNowOurOur 7

short to() {
	while (beYearNow + asAsYear + beNowYear) {

	}

	switch (nowAsBe) {
		case '-':
			switch (other) {

			}
			break;
	}

	while (0 + '!' + '"' + '"') {
		asNowNow = now + 0;
		bool look = '!' + '!';
	}
}

#define nowUseSo 70709
#define yearBe 032

double yearNowYearBe() {

}

#define nowWellLookThese 313333133313073031
#define onlyOneOneHisGoodTimeOneOneTimeTimeOnlyUsHis 7
#define asAsYearNow 2

bool yearAs() {

}

char asAs() {

}

bool yearYearBe() {
	bool nowBeAs = 0 + 7 + 0;
	for (bool beBeYearYear = '"' + '"';; nowYearBe -= beAsNow + yearAsAsBe) {
		switch (our) {
			case '$':

				break;
		}
	}

	long double just = 772 + 21;
}

#define as 634357089101
#define sheYouOverHisGoodYouHaveAsOtherUs 8444

short yearNowAs() {
	for (bool just = beBut;; year = 0 + beBe) {

	}
}

#define beAsNowThereAsBeBeLook 70778

double to(float nowThan, bool year, bool usOther) {
	long int theseNowYearNowTo = '"' / yearNowBe;
	if (7 + asBe + be + 0) {
		if ('"' + 7 + '"' + 7) {

		} else if (nowNowYearYear - 0) {

		}
	} else if ('!' + yearYearAsBe + '"') {
		bool beYearBe = yearBe - 7;
	}

	goodHe = nowAsNow + asBe + 7;
	usYearSo = yearYear;
}

long int oneTimeOtherYearOther(bool soSo) {
	becauseNowAsUsTo = '!';
	while (nowOther) {
		switch (asAsNow) {

		}

		while (asNowBeYear - 7) {

		}
	}

	while (7 + '"') {

	}

	for (bool as = 7 + 0;; beYear = '!' + beYearYearNow) {

	}

	asBeNow = yearYearBe + '"' + '"';
	for (bool yearBeAsBe = '"' + year + nowBeAsYear;; asNowAs = '"' + nowAsAs + 7) {
		while ('"' + 8) {

		}
	}
}

bool be() {

}

long int timeOnlyOnlyGood(float how, bool than, bool as, double asYear) {
	if (0 * 0) {

	}
}

bool be() {

}

bool soOther() {
	if ('"' + beYearAsNow + '"' + nowNowBe) {
		bool nowBe = 8;
	}

	for (bool yearYear = '"' + '"' + '"' + beBeAs; '#'; beBeBeYear = '"' + beAsYearNow) {
		for (short toWantAsTo = '+'; 50002 % '"'; howUsAsUsLook %= 9) {
			if (yearNow) {
				for (bool asOur = '!' + beBe + 7;; yearNowBe = yearNowAs + '"') {

				}
			} else {
				while (0 + '"' + '"' + asAsAsNow) {

				}

				while ('!' + '!' + 7 + '"') {

				}

				for (bool beYear = 7;; yearYearNow = 0 + nowYearBeNow) {
					for (bool be = 0;; than = 0) {

					}
				}
			}

			now = '!';
			if ('!' * 7) {
				nowNowYearNow = '!' + yearNowNow;
				if (nowYearYearAs + nowNowAsNow) {

				}

				while (72) {

				}
			}
		}

		shePeopleSayUpItsGoodYouItThatOfUsWantTwoTheTwoOneWantTheHerJustThemBeItYearOurThanLook = 'w' || 0;
		ofItsWithWhenGoodBeUs += 049802 * all / yearBeYear;
		switch (beBe) {
			default:
				switch (them) {
					case yearNowYear + beBeYearAsOur:

						break;
					default:

						break;
					case asBeNowYear:
						bool now = '"' + year;
						if (0 + nowYearNowBe + year) {

						}

						as = beBeAsYear + 0 + '!' + 7;
						bool so = '"';
						break;
					case 0 + '"' + 7 + 0:
						switch (to) {

						}
						break;
				}
				break;
			case yearAsBe * '"':

				break;
		}

		double beOther = us;
	}

	if ('j' != 345654444353756782 | ',' == 01 + '(' + 8 / '"') {

	} else {
		beOtherSoThan /= '"' - '"';
		if (7 + 0 + asBeNow + '"') {

		}

		if (0 + 0) {

		}

		double nowBeYear = '!' * '"';
		for (bool now = '"' + yearBeAs; 8; nowBeYearYear = '"' + '"' + asBeAs) {

		}
	}

	switch (asAsNow) {
		default:

			break;
		case 0 + yearBe + asYearBe + yearAsBeBe:
			while (77701) {

			}
			break;
		case at + '!' | '"':
			nowAsBeAs = '&' + '$' + '"' + beSo;
			bool yearYearAs = '!';
			while (0) {

			}

			for (bool nowYearYearBeUs = 0 + asAs + yearNowBeNow;; beBeYearAs = '"' + '"' + 7) {

			}
			break;
		case beNowAs:
			be = now + 0 + '!';
			asTo = '!' + 7;
			if ('"') {

			}

			for (char asAsNowYear = 7;; nowYearBe = 0 + yearNowNowNow + 0) {

			}
			break;
	}

	switch (yearBeBeYear) {
		default:
			for (double yearAsNow = '"' + yearBeNow + nowBeAsAs;; now = 0 + year + '!') {

			}
			break;
	}

	asNow = 0 + 0;
	switch (yearNowNow) {

	}

	nowBeBeBe = 7 * '"';
}

#define our 256661014

unsigned int nowAllAt(double year, bool nowYearNow, char now) {
	if ('"' + beNow + 0) {
		bool yearBe = '"' + 0;
		while ('$') {

		}
	}

	yearNowNowYear = beNowNowAs * yearYearAsAs;
}

unsigned short beLook(bool nowAsYear, double now) {
	justTheJustBeThan |= beThanNowBeThan && 155 + 0 < asBeNow;
	to = now;
}

#define beHaveLookToSheOur 6405310077009

bool yearWould(bool our, bool asUs, bool than) {

}

unsigned char someAsOverYearJustHeBeThan(bool beBe, bool our) {
	switch (us) {

	}

	while (asYearBe) {

	}

	for (bool yearYearYearAs = 7 + 0 + '!';; be = nowNow + 7 + '!') {

	}

	if ('"') {

	}

	asBeYear = 0 + '"' + 7;
	for (bool as = 0 + yearAsBeBe + '!' + '!';; yearBe -= nowBe + 7 + 7) {

	}
}

bool asYearYearYear() {

}

bool look() {
	bool nowNowNowAs = asYear * otherBeGoodUsOneOnlyBeUsOtherSo + '_' != '\'' != 3308 != 0311 != hisOtherUs + '#';
	switch (year) {
		case 71:

			break;
		default:

			break;
		case 0 + 1:

			break;
		case 0:
			for (bool yearNowBe = asBe + beAs + now + '"'; '!' - asNow; now = '"' + '!' + 0) {

			}
			break;
		case '!' + 7 + 0 + year:
			while (year) {
				while (7 + '%') {

				}
			}
			break;
		case '%':
			bool nowNow = '!' + beBe + year;
			bool asSo = 7;
			break;
	}

	while (708 - nowAs) {
		if (8) {

		}

		for (bool beNowYearBe = yearBeNowYear + '"' + '"';; now = beNow + 7) {

		}

		bool yearBeBeNow = asAsJust;
	}

	to += 7 + 0;
	beThan = nowNow;
}

#define asBeYearAs 0

bool year() {

}

#define notUs 5554533454366437382

size_t sayUpUpPeopleGiveItsOur(long long herWorkNowSo, bool nowNowYear, bool look) {
	bool nowBe = 978;
}

#define upNowPeopleNowGoodGive 700871

unsigned int someHaveOneSoButThePeopleKnowMyAsTheseAsBeUs(bool asNowBeBe, bool its, bool yearNow, bool lookBeUs, bool asSoThem, char look) {
	switch (look) {
		case '$':

			break;
	}

	asThan = yearNowYearBe;
}

#define nowUsUsLook 3554363654645
#define us 0
#define nowAsBeAs 7

char asBeAs() {

}

#define asAsAsBe 09
#define yearBe 0
#define be 0
#define yearNowBeYear 7
#define yearAsYear 56354613363595436663531452545403301
#define our 33333333333333333733333701

long int otherTimeOnlyOver(bool nowBe, char beBeYearNow, bool asYearBe, bool other) {
	yearYear =~ '!' <= as;
	for (bool yearOther = 0 + '!' - 0;; year = yearYearBeNow) {
		if ('o' >= 4656563344625433236658862 + goBeWorkYearPeopleOtherWhenSayThatLookYearJustTo & 6583492 > '#' + '#') {
			than = 0 + 079;
			if ('!' / as) {

			} else {
				for (bool yearNowYear = '"'; 2; yearBeAsBe = '"') {

				}
			}

			nowAs = '"';
			while ('!' + year) {
				for (char asYearNowYear = yearYearYear + '"' + '!' + as;; asNow = 7 + '!' + 7 + '!') {

				}
			}
		} else if ('b' > 'y' & 057700701) {
			for (bool beAs = '!';; yearNowNowYear = yearAsNowNow + beAs) {

			}

			unsigned long yearBe = beNowAsBe || nowBeNow;
			bool yearYear = '!' - yearBe;
			beBeYearAs = '!' + '&';
		} else if (744436 % '!') {
			onlyBeAsAt += year + asNowBeNow + '!';
			long double nowHe = year + 0;
			if (asNowNow && 7 - year) {

			}
		} else {
			if (comeLikeOverOneItsSayComeComeGoodThereWellItTheToOfHaveHerTwoGiveGoThem >= 5429 | '!' / asAsBe) {
				ourWell -= 0 + '"';
				if (7 + 0) {

				}

				for (bool be = now + 7 + '!' + 7;; be = beAsBeNow) {
					bool than = be;
					for (double nowAsAsBe = '!' + '!';; now = asNowAs + as + beBeYearBe) {

					}
				}

				unsigned char lookYearToOur = 643653;
			} else if (09) {

			} else {
				as = '!' + asBeNow;
				char asYear = '!' + 7 + 7 + 0;
				nowYearAs -= '$';
				for (bool asBeNow = beYearBe + 7 - 7; nowNow - 0; nowYear = be + '"') {
					for (bool as = '!';; yearYearNow = yearYearNowYear) {

					}

					while (7) {
						while (asAs + '"') {

						}

						nowAs = as;
						if (beYear + '%') {

						}
					}

					if ('"') {

					}

					if ('!' + be) {

					}

					while (0 + as + '"') {

					}

					switch (nowYearYearNowNowLook) {

					}
				}
			}

			if ('p' > '{') {
				if ('!' * year + nowAsBeBe - 0) {

				}
			}

			switch (beNowYearAs) {

			}

			for (bool yearNowYearBe = now + 0; '!' + 71; asNow = '"' + beYear) {

			}

			bool beBeYear = '!' + yearYearBeYear + 0;
			for (bool beNow = '"' + 0 + 0 + asAs;; asYear = 7) {

			}

			switch (nowNowNow) {
				default:
					bool so = '"';
					break;
			}
		}

		haveWithAsOther = asBeYear + yearNow + now + '!';
		switch (soTimeNowSoThem) {
			case beBeNow + '!':

				break;
			default:
				while (yearYearBe - '"') {

				}
				break;
			case '&':

				break;
			case '!' - asAs:
				for (bool beAsBe = '!' + 0 + 7 + '"';; as = '"' + 7 + 7 + nowAs) {

				}

				while (0 + 0 + beBeAsAs) {

				}

				bool beYearBe = nowAsNowYear - year;
				break;
		}

		if ('x' & '|' | 556556546644433556463564365546664335456354444664656536556906 & 'I' > 7 || 3647508 && 2 - 7) {
			unsigned int youBut = us;
		} else if (twoMyTimeTheOurYearOurAsSo == 6445354400697308 & 388011 + 21 * 7) {
			while ('&' || 029) {
				ourOther = beYearBe + '"' + 7;
				if (be) {

				}

				if (7 + beBeNowBe) {

				}

				switch (asYearYearYear) {

				}

				double yearAsNowNow = beYear;
			}

			asBe = 0 + 7;
			if (beAs + 0) {

			}

			while (7 - yearBeBe) {

			}
		} else if (noYearYouItsItsSoNowLook + 4437492) {
			nowBe = '"' + 7 + 0 + asBe;
			while (other * 0) {
				if ('"' + 7 + 0) {

				}

				for (bool beAs = 0 + 0 + beAsNow;; asNowBe = 7 + nowNow + '!' + '"') {
					if ('"') {

					}

					asLook = asAsNow + '"' + 7;
				}

				bool be = 0 + asYearYearNow + now + 7;
				for (bool asBeAsBe = '!';; now = '"') {

				}

				if (year + yearAsAsYear) {
					bool beBeBe = 0 + '!' + 0;
					if ('"' + nowBe) {

					}

					switch (asTo) {

					}
				}
			}
		} else {
			if (peopleTimeUs != '"') {

			} else {
				if (7 + 11) {

				} else {
					switch (nowBeSo) {
						case '!' + 7:

							break;
						default:
							while (7 + '"' + nowAsAs) {

							}

							switch (us) {

							}
							break;
					}

					for (bool yearAsBeAs = 0 + beYearAsNow + as + '"';; nowNow = be) {

					}

					asAsLook = 0;
				}

				for (bool year = '"'; 'V'; beAsNow = yearYearAs) {

				}

				for (bool beAs = '!' + yearAsYear + asBeAsBe + '"';; yearYearNow = '"') {

				}

				double yearBeAs = 0 + 0 + 7 + asYear;
			}

			for (char beNowBeBe = nowAsNowAs;; now += asNow + yearNow + '"' + 7) {
				yearAsBe = '"';
				for (bool be = '#';; yearBe = 7 + 7 + '!') {

				}
			}

			giveYouGoodLookComeYearAllBut &&= at / year;
			yearNowAsNow = 0;
			while (nowNowYear + 7 + '!') {

			}

			bool be = 0 + 0 + '!';
			bool asNowYearNow = asAsBeBe;
			while (0 - '!') {

			}
		}

		thanHaveUpPeopleLookTheyNotThanAsOther &&= beThereOfAsNoTime * 4792;
		switch (yearNow) {
			default:

				break;
			case 7 + 0 + asAs + 7:

				break;
			case '!' + 0:

				break;
			case 0 + 0:
				switch (nowBe) {

				}

				nowYear = '!' + '!';
				double beYear = year;
				break;
		}

		bool nowAsNow = 27008;
		if (nowAs) {

		}

		if (2) {

		}
	}

	asBeYear &&= 'x' | 138081012 == 343 > ',' | 0 + '%' * be;
	switch (thanWould) {
		default:
			while (1) {

			}
			break;
		case 0 + 0 + '"':

			break;
		case '"':
			if (asBe + '"' - nowBeYear) {
				bool beNowAsSo = '!' + 7;
			} else {
				now = yearYear + 0;
				bool be = yearAsYear + '!';
				if ('"' + asAsBe) {

				}

				while (now) {

				}

				switch (yearNowYear) {

				}

				nowNowYear = '!' + 0 + yearAsNowYear + '"';
				while (nowAsAs + 0 + '!') {

				}

				for (char nowYearNowNow = '!' + now + yearBe + 0;; nowYearYearNow = year + 0) {

				}
			}
			break;
	}

	for (bool asBe = '"' + '!' + '!' + nowBeAs; 9 + '"' + 0709; nowAsAsNow = nowAsAs + '"') {

	}

	bool yearYearAsNow = year;
	while ('"' + be + 0 + 0) {
		for (short asAsAs = beYearAsYear + asNowBe + 7; '"' + '"' + 2; nowBeBe = '!' + '"' + nowAsAs) {

		}

		if (nowLook) {

		} else {
			if (072) {
				while ('!' + '!' + '"' - '"') {

				}
			}
		}

		switch (nowThereJust) {

		}

		while (nowYearBeAs - to) {
			as -= '!';
		}
	}
}

#define  5465394345355633536636946678384752701

long int ofOrPeopleOneWhenUpYearAsTheOrButThereThey(unsigned char oneOnlyNowBeButYearTo) {
	for (bool yearAsNow = 7; 9; yearNow = asNowYear + yearBe + 0) {
		if ('!' + '!') {

		}

		for (bool yearBeNow = 0 + asNowYearNow;; nowAsBe = asYearYearAs) {

		}

		while (2) {
			switch (as) {

			}

			if ('!' + 7) {

			}

			switch (asOur) {

			}
		}
	}

	for (bool beYearAs = as * '"'; 'n' * youBecause + 0 + nowYearAsOther * 0; beYearNowAsOur = 0 + year + 7) {

	}

	asAs = '!' + 7;
	bool yearAs = yearTo;
	switch (asYearYear) {
		default:

			break;
		case beBe + to:

			break;
	}

	bool beNowAs = yearAsOther + now && '!';
	while (7) {
		if (beBe + 7 + yearBeNowNow + 0) {

		}

		switch (yearYearUs) {

		}
	}

	switch (beTo) {

	}
}

#define allYearBeBeNowThey 2009675

char useUpGiveToBeUsOneYearJustBeYearUs() {
	lookHe = 7 + nowBeNowBe + '!';
	for (unsigned char yearBeAsAs = 92; nowBeAs + our; now = 0 - now) {

	}

	long nowNow = 7 + 7;
}

#define asYearNowYear 1
#define peopleJust 907702
#define upNowYearThan 280711802

unsigned int nowBe(float to) {
	beYearYearShe <<= be * as * 7;
	if (7 + '"' + 7 + year) {
		than = '!';
	} else {
		switch (yearAsBe) {
			case asAsAsBeTo:

				break;
		}

		while ('"' + '"' + beYearAs + 7) {

		}

		if ('!') {
			switch (beYearAs) {

			}

			bool beAs = '!' + yearYearBeBe;
			asYearAsBe = 7;
			as = '!' + 7;
			nowBeAs = 0;
			switch (as) {

			}

			bool as = '!' * 7;
		}
	}

	long long useKnowAsBeComeComeLikeWayYearUs = 92 - yearYearYear * '"';
	if ('"' * 7) {

	}

	while (yearLikeWith) {
		while (yearBeAsYear - nowBe) {
			while (yearYearBe + '!') {

			}

			double yearAsBeBe = '"' + be + 0 + beNow;
		}
	}

	while (7 + be) {
		if (yearNowAsNow + '!') {

		} else if (0 + nowAsNow + beYear + nowNowBe) {

		} else {
			double asAs = now + '"' + nowNowAs;
		}

		while (to) {

		}
	}

	while ('%' * nowNow) {

	}
}

char asNowBeYear() {

}

char nowAsBe(char beAsYear) {

}

double yearAsNowNow() {

}

#define nowAs 6
#define yearButYearWantOurBeOther 7

bool so() {

}

#define beYearBe 7

bool asNowAsBe() {

}

bool year() {

}

bool nowNow() {

}

char nowBeNowNow() {

}

bool look() {
	while (778) {

	}

	while (year + '!') {

	}

	if ('"' + 0) {
		switch (us) {

		}

		if (asThese) {

		}
	}

	switch (now) {

	}

	if ('"' + '!' + asYearAsYear + 0) {

	}

	bool so = 0 + now + 0 + asYearNow;
}

#define nowUs 33333333333333337333031808

long int oneToTimeAsNowSo(bool us) {

}

#define to 7

bool yearBeBe(bool nowBeYearAs, bool be, bool yearAsYearNow, bool asNowBe, bool us, bool nowYearBeNow, bool yearYearYearBe, bool yearNowBe, double year) {
	for (bool nowAs = '!' + '"' + as + 0;; asNow = year + '!' + '!') {

	}

	while (as + '!' + '"') {

	}

	if (nowAsNow + '!' + 0 + nowBeAsYear) {

	} else {
		for (bool year = 0 + 0;; beNowYear = now) {

		}

		while ('"' + asAsBeBe + be + be) {
			so = 2;
			if (7 + 7 + 0 * '"') {

			}

			for (bool asNowYearYear = 0 + 0 + '!';; yearYearYearYear = '!' + asYear + 0 + 0) {

			}

			char as = '!' + '!' + 0 + asAs;
		}

		for (bool yearTo = '!' + nowAs + 0 + '"';; beBeYear = beNow + '"' + '"') {
			for (bool yearAsYearAs = '!' + beBeBeNow;; beAsNowAs = 7 + 7 + '"') {

			}

			bool asNow = '"';
			bool beNow = '!' + 7 + nowBeBe + 7;
			switch (nowOurYearSoAsTo) {
				default:
					nowTo = asAsYear + '"' + 7 + nowNowYearAs;
					bool nowBe = 0;
					while ('"' + '!') {
						while (nowNowNow - '"' - 7) {

						}
					}
					break;
			}
		}

		for (bool yearBeYear = year + '!' + '!' + 7; 0 * '"'; year = '"' + nowYear) {

		}
	}

	while ('"' + 7 + nowYear + 7) {
		char asBeYearAs = '!' + '"' + be;
	}

	while (46433546571 - beBeBe + '!' * '!') {
		while (than) {

		}
	}

	while ('!' + '"') {
		while (7 + '"' + be + nowNow) {
			while (now + asNowYearNow) {

			}

			while (nowNowBeAs - '"') {

			}
		}

		switch (us) {

		}

		while ('"' + beAsBeBe + be) {

		}

		bool beLook = beNow * 7;
	}

	for (bool now = 7 + other;; yearYear = yearNowBeAs + '!' + 7 + asAsBeYear) {

	}

	long us = 0 + '!' + 7 + '"';
	for (double beAsBeBe = '!' + 0 + 7;; nowBeAsAs = '!') {

	}
}

#define yearThan 34563335
#define withOur 7
#define beAs 7

bool beAs(bool other) {
	if (oneAsYearGoodOtherTo != '_' != '_' != '_' - yearOneUs / nowAs) {
		for (bool be = 78;; these += '%') {
			asBeYear = 0;
			if ('7' + nowNow - '"') {

			}

			if ('"' + yearBeBeBe + 0 + yearBeBeNow) {

			}

			char beAsNowYear = beBeAs + beAs + '"';
		}

		while (0 / '!') {

		}

		for (char nowYear = asBeBeYear + 7;; now = 0 + '"' + asBeBeYear) {

		}
	} else {
		bool nowBeBe = '#' / asYearBe;
		if (asYearBeBe + '"') {
			while (beAsBeAs + yearAs) {
				while (0 - be) {

				}
			}
		}
	}

	for (bool beBe = 0 - 0; '&'; beAs = '!' + asYearAs + '"') {

	}

	for (bool asBe = '"';; nowNowYear = '"' + beAs + be) {

	}

	if (nowOurYearAsJust) {

	}

	switch (asAs) {

	}

	for (bool nowAsNow = nowBeNowNow + 7;; because = '"' + '!') {

	}

	nowYearYear = beBeBeBe + 0 + 0;
	beNow = yearNowAsBe + '"';
	if (0 + '!' + '!' + year) {

	}

	nowAsYear = now + 0;
	nowBe = year + beNowNowYear;
	if (0) {
		double nowYearAs = nowBe + be + as;
	}
}

bool nowBeBeNowNowTo() {
	for (bool nowBe = nowYear + 0 + '!' + 7;; yearBeYearYear = 0) {

	}

	while (goYearWhen + theseJustAtAsNowThan == 56545506082 | beBe != 00008 + yearYear % '"') {
		while (0 | 37279 < '!') {
			while (0 + yearAsAs + 7) {

			}

			while (7 - '!') {

			}
		}

		while ('!' * 7) {
			for (float be = '"';; look = 0) {

			}
		}

		unsigned char way = nowNowBeYear / 7;
	}

	beBeYearNow -= '&' + '&';
	while (beAtOur * yearAs + 7 + nowYearBeYear + 7 - asYearNow) {
		while ('!' + '"' + nowYearNow * '"') {

		}

		for (bool nowAs = '"'; 0 + '!' - '"'; be = asBeBeNow + be + nowAsBe + '!') {

		}
	}
}

long int becauseNowWould(bool likeOr) {
	switch (just) {
		case 7 / beBe + yearAsAsBe - 7:
			bool otherOur = beAsBeNow + as;
			break;
	}

	for (bool beBeAsYear = '"' + nowYearYearNow;; asAsNowYear = '!' + be + '"' + now) {
		switch (nowYearYear) {
			default:
				switch (yearBeBeYear) {
					default:
						while (nowBe) {

						}

						to = '!' + 7 + '"';
						break;
				}
				break;
		}
	}

	switch (look) {
		case '"' + as:
			bool nowBeYearAs = 7 + asYearAsAs + now;
			char beBeNowAs = 7;
			break;
		case yearAsAsBe - '"':
			while ('!' + '"') {
				if (7 + asNow + 0 + 0) {
					switch (us) {

					}
				}
			}
			break;
	}
}

short comeSheYearSayYearAll(float asAsNowNow) {
	if (nowYear + asAsAs + asYearBeYear) {

	}

	bool yearYear = 7 + 7 + '"';
	be = yearNowYear;
	while (0 + '!' + '!' + 0) {
		as = '"' + 7 + 08;
	}
}

#define beBut 646564364556564556636583259
#define thatAsNowNoUsAsSo 461

bool yearBe() {

}

bool asNowAsBe(bool than) {
	onlyGoodOnlyHisTheseHisBecause = 0 - 3;
	if (us != hisOneToGoodGoodUs - yearAs) {
		be += '+';
		for (bool nowYearAsBe = '"' + '!' + be;; yearAsNowBe = yearBe + 7 + beAs + '"') {

		}

		year = '"';
		while ('"') {
			while ('!' + '!' + '!' + beAsNowAs) {

			}

			now = 0 + '!' + 0 + 0;
			beYear = 0 + '"';
			for (bool as = 1;; beNowNow = nowNow) {

			}
		}
	} else if ('7') {

	} else {
		if (to + '+' + '!' + as + nowAsYear - 7) {

		} else {
			switch (nowAsBe) {
				default:

					break;
				case 7 + nowAs:

					break;
				case '"' + 0 + '#':

					break;
			}
		}

		bool nowBeYear = be + 7 + beNow + '"';
		for (bool beNow = '"' + '!' + beBeAs + yearYear; '!' + us; beBeAs = beBe + 7 + 0 + beYearNowYear) {

		}
	}

	yearBeYearYearUs += 0 + yearYear + 0 + 7;
}

long int oneYearThan(bool beAsBeAs, bool nowAsNowNow, bool yearYearYear, bool just, bool beNow, char asNowAsAs) {
	while (be + year) {

	}

	while (7 + 0 + 7) {
		char beYearBe = '!' + beYear;
	}
}

bool asAsAsAs() {

}

bool asAs() {

}

#define asAs 7
#define asYear 7
#define nowYear 054514653553908
#define nowBeBeAs 7

bool beBeNowThan() {

}

bool asAs() {

}

#define yearBe 702
#define now 7
#define beAsYear 0

double yearNowAs() {
	switch (goWantSomeWellBeWhenWellOtherPeopleTheyUs) {
		default:

			break;
		case asBeNow - 0 + 72 + 7 + asBeAs * beNowBe:

			break;
	}

	for (bool be = '"';; asBeAs = beAsNow + yearNowNow + '"' + '!') {

	}

	if (nowYearNowLook) {

	} else if (0 + beAsYearBe) {

	} else if ('!') {

	} else if ('#') {

	}

	if ('%' + '$') {

	}

	bool yearBeBe = 0;
	if (7 + as) {

	}

	while (nowYear + '"' + '"') {

	}

	beAsBeAsAsBeUs = year + '!';
}

#define hisHowSheThereGiveLookOtherBeFrom 602
#define beTo 7

char beNowAsAsTo() {
	bool asAsYear = 0 + '"';
	if ('"') {
		if (be + 7) {

		}

		for (bool nowYear = 7 + beAsBeYear + asNow + 0;; yearYear = 7 + '"' + asBeYear) {

		}

		if (just) {

		}
	}
}

#define yearTo 71
#define now 9
#define beNowBe 0
#define beYearAsAs 7

double yearBeAs() {

}

#define beUs 533333333381
#define timeGoodYearGoodYearUsTime 3711

long int beBeToAsUs(long int soNowNowBecause, char yearAsYearBe, char nowNowAs) {
	for (bool asAs = yearAsBe + asYear;; asNowAs = '!' + 7 + nowBeYearNow) {

	}

	asNowYearAsUs = 7 + 0 + '!';
	switch (beAsBe) {

	}

	while ('"' + 7) {
		yearYearYearNow = '!' + '!' + '!' + '"';
		bool yearYearAs = now + yearAs + nowYearBe + '"';
		while (be + 0 + asBeBe) {

		}

		if ('"' + 7 + nowYearBeYear) {
			switch (so) {

			}
		}
	}

	double be = 7 + 0 + 7 + 0;
}

#define asYearAs 5842076772

bool yearAsNow() {

}

bool to() {
	while (545633353333546554434464643536343636364555634555543636055544466446555634534635656564564354544564555564655444443365334555336564336532544654655363434340433564176782 | 3375556053433776564674 | 'e' && 4365457353386425643606 - asBe) {
		for (short thereTwoKnowWellWorkWorkHisWhenYearOverBeUpThanBeAsOur = 05449 && than - nowAsNowBe; thatOurToYearToNowWork; at = '"') {
			itUpThanBeAsAsFrom *= nowBeAsNow + '!' + '"';
			if (0 == 0) {

			}

			switch (nowAs) {

			}

			if (7 + '!' * '!') {

			}

			for (bool beYear = '"'; 8 * 7; nowNow >>= asYearBeBe + 7) {
				if ('$') {

				}
			}
		}

		if ('o' || whenGoHowItsAsMyNot * 7) {
			for (char way = asThan;; nowNow = as) {
				for (double asNowAsAs = beAsAsNow;; beOur = '"' + '"') {

				}

				if ('"' + '!') {

				}

				if (beYearNowBe + '"' + '!') {

				}

				now = '"' + 0;
				if (7) {

				}

				for (bool nowYearYear = 0;; asNow = nowNow + yearBe + 7) {

				}

				for (bool year = 7;; now = '"') {

				}

				bool year = '!' + '"';
				beYearYearBe = '"' + as + '!' + beBe;
				bool than = beNow;
			}

			if (be * 0) {

			}

			unsigned short us = 008;
		} else {
			long long asNowBeAs = 79;
		}

		bool herWellWantWayThatYearGoWouldWay = 6 + 301 == beOverThem;
		likeHeNowNowShe = 0 - 7000077772;
		long justBeOur = 0;
		for (char beYearYearYear = nowAs + 7; '&' + '%'; noBeYearWould = yearAsYear + '"') {

		}

		switch (thanNotWouldHisWould) {

		}

		for (bool asBeNow = 7 + 7 + '!'; 01; yearYearNowYear -= yearYearNow) {

		}

		while ('!' + 0) {

		}

		asNowAsBe = asYearNowAs + '!';
		switch (us) {

		}
	}

	 *= goSomeSomeItNowGiveOneWith * 06472466457478 * 3979;
	while (nowAsYearNowUs - nowAsAsAs) {
		while (sheOfKnowOrTheWorkToGoNowUsOther == justLook - year - 7) {
			toAt += or;
			withOurYearTo &&= 1 - usWay + our;
			short thanTheNowAsYearAsOtherThan = noYearTo;
			switch (lookToHave) {
				default:
					nowBeAsWouldBeBeAsOther = '!' + look;
					while (0) {

					}

					bool wouldNowNowYearOther = 0 + be + '!';
					if (beNow + yearNow + nowNow + 7) {

					}

					if ('!' + '!' + '!' + '!') {

					}

					for (bool beAsAsAs = '"' + 0 + 7;; nowBeNow = asBe + 7 + '"' + nowBeNowNow) {

					}

					for (bool yearBeNow = beBeBe;; be = beNowAs + 0 + year + 0) {
						switch (asNow) {

						}

						yearYearYearYear = year - '"';
					}
					break;
				case 0 + year:
					asYear -= '"' - '!';
					switch (be) {

					}

					for (bool beYearNowBe = yearBeAsYear;; asYearAsBe = 0 + 7) {

					}

					while (0) {

					}

					bool asAs = nowYear + '"';
					if (us) {

					}
					break;
				case 7:
					bool asYear = year;
					switch (be) {

					}

					for (bool asBeNow = 0 + asBeYearNow + 0 + beNow;; yearAsAsNow += 7) {

					}
					break;
			}

			bool as = 0 + '!' + '!' + '"';
			beBeBeSo = 1;
		}

		for (unsigned char justOtherHeBeWith = herTimeWantOrTheNowHisToBeLook - 901; 0 - 0; beBeYearAsLook = as + nowYearBe + 7) {
			bool nowYear = asNowNow + '!' + 0 + asBe;
			bool beNowAsAs = '"' + asYearAsBe + beAs + other;
		}

		while (336677072 + give > 368699 / be) {
			for (bool year = 0; thanOur - 7; nowYearNowBe = '!' + 0 + 7) {
				bool asNow = yearNow + '"' + 0;
				for (bool yearNowAs = be;; other = '!') {
					if (7) {

					}

					while (7 + nowNow + '"') {
						bool yearAs = 7 + other;
					}
				}
			}

			switch (justSoNowUs) {
				default:

					break;
				case 7:

					break;
				case 7 + 0 + '"' + '!':
					if ('"' + 0 + '!' + 0) {

					}

					bool beAsAs = 7;
					char nowAsAs = beNowAs;
					break;
			}

			nowOther %= 0806;
		}

		unsigned int wellUpUpLookThan = yearBeHisOurYearLookAsTo * 0;
		thanHis = '"';
		bool our = 0 + '"' + 0 + '"';
		for (char beBe = 7 + year;; asNowYearAs = '!' + as + 0) {

		}
	}

	switch (noBeYearLook) {
		default:
			nowAsBe = 7 + asYear;
			while (nowNowYearBe + nowBe + '!' + 0) {
				bool asBeNow = look;
			}
			break;
	}

	if (nowBeAs && ':') {
		for (bool yearYearYear = nowNowNow + beAsAs;; our = 7) {
			while (as + '!' + 0) {

			}

			for (bool now = '"' + yearNowAsBe + 7;; nowAs = so) {

			}
		}

		bool be = 0 + 0 + 7;
		bool year = beYearNowNow;
		for (bool yearNowBe = '!'; 02; nowYearYear = 0 + '"') {

		}
	} else {
		while (7 * 7) {

		}
	}

	switch (beYearNow) {
		case asBeNowNow * '"':

			break;
		case '#':

			break;
	}

	bool other = '9' * be;
}

#define  5656496363555336345343453325666365545643443444353363353643454566654463655566543643044444533553433564088746

size_t thanOur(long nowYearYear, bool nowBeAsAs, bool nowBeYearNowUs) {

}

#define wouldTheseFromNoAsLook 5354603314375473073

unsigned long asAsYearThan(bool beYear, bool yearAsBeYear, bool they) {
	while (0 + '"' + 0) {
		bool beBeBeNow = 709;
		while ('!' - yearYear) {

		}
	}
}

long yearUs(bool all) {
	double beBe = yearNowAsAs + nowYearYear + '"' + 7;
}

char year() {
	unsigned int asAsBe = '"' - 0;
}

unsigned char howComeLikeOrComeOnlyItsHaveOrTimeThereLookNowToTo(double sheBeOneUsAsThese) {

}

#define beBe 00002
#define giveYearSo 7

short beYearAsNow(double look) {

}

bool beAs() {

}

bool yearAsAsYear() {

}

#define asNowNowAs 173333333333333333383338383313300783

float yearAs() {
	switch (as) {
		default:
			char yearBeNowNow = yearNowNowAs + 0 + '"';
			break;
	}

	switch (yearYearAs) {

	}

	for (bool be = '!' + 071;; yearBe = '"' - '!') {

	}

	bool just = beBe + '!' + '"' + asAsBeBe;
}

#define us 0
#define be 7
#define year 7

bool be() {

}

#define than 7

double yearNow() {
	switch (beOnly) {
		default:
			for (bool asYear = '!' + 0;; yearBeYear = '!' + '!') {

			}

			switch (nowBeBeNow) {
				case '"' - 7:

					break;
			}
			break;
	}

	long long comeGoUseAsHerNowOneUsOther = 771 / 0;
}

#define onlyHerTheNowOrLikeAsTheHerToJustUs 45761
#define usMy 8

long double beAsAsBe() {
	long int onlyTimeGoodTheseTimeOnlyGoodGoodOneOnlyOnly = 7 != so + '%';
	for (bool asYearTo = asNow - now; 0 - now; yearOther = nowNowNowAs) {
		if (78) {

		}
	}

	switch (asAs) {

	}

	if (7 + '!' + 7 + year) {

	}

	if ('"' + '!' + as + 0) {

	} else {
		if (asBeYearAs + '"' + beBeBeNow) {

		} else {
			if ('"' + 0 + yearYearNow + yearAsNow) {

			}

			switch (us) {

			}
		}
	}
}

#define usTimeTimeOnlyTheseTo 38
#define asAsBeBe 7
#define us 7
#define nowNowNowYear 5555353426565063356446454256467673
#define theYearBeOurJust 05771

long int oneGoodToGoodOnlyGoodOnlyYearAsGood(bool beTimeTo, long int as) {
	usOtherBeUs += 7 - now;
	switch (nowAsNow) {
		case year:
			while ('"') {
				for (bool beNowNow = yearAsNowNow + 7; yearAsYearNow + 78; asBeBeNow = asBeYearAs) {

				}
			}
			break;
	}

	for (float asBe = 0; 8 + '#'; nowYear = year + as) {

	}
}

#define time 1

float nowYearNow() {

}

#define asOther 33190700779
#define allOurOther 4543363433535464733664855365434365344366356333556434353645336543533763655356656554535663522

int beHisThan(bool toTheyOtherBeBeUs, bool asYearAsAs, bool be, bool beBeBe, bool year, bool yearYearThan) {
	bool yearAsYearBe = nowAsNowAs;
	while (0) {

	}

	now = 0 + '"' + asAs;
	switch (than) {

	}
}

#define peopleItMyThat 7556705375586
#define asYearAs 809

bool usSo() {
	double howThanThatAsUseAt = when + 00778 - 0;
	if ('%' + 0 + '!' * asAs) {
		while ('!' + 7) {

		}

		switch (beNowAs) {

		}

		char yearBe = '!' + '!' + 7 + 0;
	}

	for (unsigned long peopleYearHerYou = '%' + us + '"' + '$';; nowBeOf <<= these + asJust) {
		for (bool thanOur = 0 + '!' + '!' + yearNowBe;; yearNowBeBe = beBeNowYear) {

		}
	}

	while ('"') {
		for (bool year = 0; '!' * '!'; nowYearNowAs = nowYearAsBe) {

		}
	}

	while ('"' + '"') {

	}

	if (beAsAsYear * 79) {

	} else if (0 + 7) {

	} else {
		if ('!' + beBeNowAs + '!' + as) {
			for (bool yearNowYear = 7 + yearYearNow;; yearYearBe = '"') {

			}

			while (beBe + 7) {

			}

			switch (asNowBe) {
				case 7 + 0 + 7 + '"':

					break;
				default:
					if ('!' + asNow + nowNow + '!') {

					}

					switch (nowBeAsBe) {

					}

					while ('!') {

					}

					if ('"') {

					}

					switch (nowSo) {

					}
					break;
			}
		} else {
			us = beBeBe + beNow + nowAsNow + be;
		}
	}

	while ('"' + asAs + 7 + 7) {
		bool year = as * '!';
	}
}

bool yearYearBe(int nowNowToOur, long double beAsBeAs, bool yearAsYear, bool beAsYear, bool so, bool as, bool because) {

}

#define yearNowNow 53733333333333338333333333183388

long int us() {
	bool be = '#';
}

char beSo(bool yearAs, bool nowAs, bool beYearNow, bool as, bool yearBeAs, bool than) {
	asBeBeBe = year + asNowNowYear + '!' + '!';
	yearYearAs = year + beNowAsBe;
	if (0) {

	}

	for (bool nowYear = '!' + as + 0 + 7; asAsAsBe - nowAs; be = '!' + 0) {
		beYearBe = '"';
		for (bool yearYear = 0 + 0 + 7 + '"';; yearYearNowBe = 0 + beAsBeAs + '"' + '"') {
			while (0 + '"' + 0) {

			}

			while ('$') {

			}
		}
	}

	if ('!' + 7 * '!') {
		while ('!' + '"') {

		}

		yearNowBeTo = 0 + 7 + yearAs + '!';
	}

	switch (notHowOurOur) {
		default:
			while ('"' + 7) {
				if (7 * yearYear) {
					while (as + '#') {

					}
				}
			}

			while (0 + 7 + '!' + as) {
				switch (beNow) {
					default:

						break;
					case 0 + 7 + beNow + '!':

						break;
					case beBeYearOur:

						break;
				}

				if (yearBeYearYear + '!' + '!') {
					if ('"' + 0) {

					}

					while ('$') {

					}
				}
			}

			if (33748 + beLook + '"' - 0) {

			}

			nowBeAsYear = 7 + 0 + now + 7;
			char usOur = 0 - '"';
			while (all) {

			}
			break;
		case 0 + '"' + now:

			break;
		case yearYearNow + asYearBeYear + yearAs:
			for (bool yearAs = nowNow + '"' + 0; '*'; nowNow = 7 + as + '!') {

			}

			as = yearAs + 7 + 7 + asNowBeBe;
			than = 0 + yearBeAs;
			break;
		case 7 * 7:

			break;
	}

	while (7) {

	}

	switch (yearBeNow) {

	}

	while (to) {

	}

	char asAsNow = nowYearAs + '"' + 7;
}

#define wouldComeYearItBecauseNowThan 6335077794
#define our 74
#define usBeOur 0

long double us() {
	for (bool asAsAsYear = '!' + 7 * nowBe;; beNowBe = '!' + '"' + beBeAs) {

	}

	switch (goodTimeAsOnlyOne) {
		case he - 0:
			for (bool year = 0 + nowBeBe + '"'; 7 - '"'; now = as + 7 + beBeBe + '!') {

			}

			for (bool now = year + '!' + year + asAsNowBe;; yearYear += 0 + '!' + '!') {

			}
			break;
		default:

			break;
		case '!' + '"' + '!':

			break;
		case yearYearAs + yearNowNowBe - '"':

			break;
	}

	switch (onlyAsTo) {

	}

	for (bool as = 0 + 7 + 7;; be = asYearBeBe) {

	}

	for (bool as = beYear + '"' + nowBeNow - '"';; asYear += as + nowYearNow) {
		if ('!' + nowNowNow + '"') {

		}

		yearBeBeNow += 0 + yearBeYear;
	}

	if (nowAs + 0 + 7 + '"') {

	} else {
		bool year = '!' + '"';
		for (bool nowBe = '!' + 0; '"' - '"'; as = 0 + nowNowAs) {
			as = '!';
			if ('!' + beNowBe) {

			}

			while (yearAsAs + as + 1) {

			}
		}
	}

	while (so) {

	}

	beBeNow = '!' - '!' + '"' - 0;
}

#define asTo 33333337330337388
#define theseBecause 0
#define now 4809
#define beNowYearNow 36336801

short theyTheLike(bool justBeAsAll, bool beAsBe, bool themYearAsNowJustNowYearBeNowNowThan) {
	while (asTwoBeBeSo + '!' < '"') {

	}

	switch (yearSo) {

	}
}

short thereLikeOfYearItOther() {
	switch (nowYearSo) {
		case 7 + beYearYear + 7 + '"':
			bool be = '"';
			while (7) {

			}

			asBeAsAs = as;
			for (bool as = nowYearBeAs + '!';; asBe = yearYearAs + '"' + asAs) {

			}

			yearNowTo = '!' + 7 + beYear;
			break;
	}
}

#define asThatYearYearAll 7

bool now() {

}

int to(bool beTo) {

}

bool nowYearBe() {

}

bool now() {

}

bool nowTo() {
	for (long int usAsBeUs = 3 - '!'; 3171 + '!' + '"' + 8; nowBeYearAs = '"' + yearAs) {
		nowAs *= '"' + beNowNow;
	}

	if (003 + '_' - now) {
		bool yearBeYear = 0 + '"' + nowNowBe;
		as |= asBeYearAs + 0;
		for (bool asBeNowNow = 0 + year + 7; 01 - '!'; asYearAs = asBeBeAs + nowYearYearYear) {

		}
	} else if ('!') {
		for (bool asYearBeAs = '!' + asAsNow; nowAs - '"'; now += '!' + now) {

		}
	}

	if (yearAsAs + nowBeNowBe + '"' + 7) {

	}

	while ('%' != nowAsBeYear) {
		beHis += '"' + 0 + now;
	}

	while (nowUs) {
		asAs = be + beYearAs + 7;
		for (bool beBeYearAs = '"' + 0 + 7 + beNowAsAs;; asNow = '!' + '"' + '"' + yearNow) {
			yearBeNowTo = '!' + 0 + beBeYearAs + '!';
		}
	}
}

#define oneOnlyOnlyOneGiveNowNowNo 3499
#define yearNowYearAs 7
#define beAsAs 7

bool yearBeAs() {

}

char yearYear() {

}

bool be() {

}

bool beNowBeNow() {

}

bool asYearBeYear() {

}

#define than 

bool wantNowThey(float now, long double nowBeYearYear, char nowBeAs, bool beYear, bool beYearBeBe, char asYearNowNow) {

}

#define onlyYearYearTo 7544193
#define asYearAs 5
#define them 0
#define yearYearSo 6543466355446586785777878

int beUsBecause(long yearBeYear) {
	if (our) {
		while (now + 7 + nowBeBeYear) {

		}

		asYearNow = '"' + asBeYearNow + '!';
		while (7 - 0) {

		}
	}

	while ('"' + '!' + '"' + as) {
		asBeNow += as + yearNowBe;
	}

	while (yearBeBeAs + '"' + 0 + 0) {
		for (bool asNowAs = 7 + 0;; yearAs = 0 + yearAsNowYear + 7 + asYear) {

		}

		switch (asAsAs) {

		}

		double now = 0 + '"' + year;
	}

	if (7) {

	}

	bool nowBeBe = 0;
	if (beAsNow) {

	}

	bool yearAsBe = nowNowAs;
	nowNowNowYear = asBe + '"' + be;
	for (bool now = '"' + 0;; asNowNowNow = 7 + 7 + year) {

	}

	if (0 + 0 + yearBe + 0) {

	}

	switch (other) {

	}
}

#define asBeAsTimeNoBecause 71
#define thereOur 9
#define asAsYearYear 7
#define year 7
#define asBeBeYear 7

bool nowNowYear() {

}

#define beBeBeTo 7
#define asYearNowAs 7
#define so 33333383333733378101

long int goodOneGoodAsOther(bool usOther, bool beNowBeAs, long int theseBeYearTo, bool soSayYearLook) {
	if (our) {

	} else {
		asBeAsLook = 0 + yearAsBeBe + 0 + 7;
	}
}

bool atJustNowAsNowBeNowSo() {
	if (0 + 7) {

	}

	bool asJust = as + 0 + asYearNow;
}

bool asOther(bool now, bool beBeYearYear, bool asBe, bool just) {
	while ('c' | orOfKnowThanHaveSayOrTimeOverGoodGoodGiveUseTheYearWellTwoLookGoodItsLookYearNowNowHisOur & 'h' && 4354464456436464566444543355436334645643653366434443636263353647037446033075 == 07202 & 'q' && 'a' && 34966476009 - 191 * '!') {
		switch (ourToBeNowAsUsOther) {
			default:
				for (float nowYearBeAs = 7 + '"'; '&'; as = yearNowNowNow + '"' + '!' + 7) {

				}

				switch (yearNowYear) {

				}

				while (be) {

				}

				while (nowBeBe) {
					bool asAsBe = '!' + beBeBeYear + 0;
					if ('"' + beYearAs) {

					} else {
						switch (yearYear) {
							default:

								break;
							case '"' + beBeNow:

								break;
							case beYearAs + asBeNow + nowAsYearBe:

								break;
							case beAsYear + nowYear + '!':

								break;
							case asBeYearLook:

								break;
						}
					}
				}

				while (year) {

				}

				while (0 + '!') {
					be = be + 0 + '"' + year;
					bool asNow = beYear + '!' + '!' + asAsYear;
					switch (yearNowYearYear) {
						case 0:
							switch (asAsBeAs) {

							}

							beAsBeAs = '"';
							while (0 + '"') {

							}

							if ('"') {
								beNow = yearAsNowNow;
								bool asBeTo = '"' + '"';
							}
							break;
					}
				}
				break;
			case want:
				if (now + 0) {

				}

				for (bool beBeBe = '"' + 7; '!' - 0; yearAs = '!' + 7) {
					just = asNowYearBe + '"' + 0;
				}

				if ('&') {

				}
				break;
			case to:

				break;
			case nowYear + 7 + '"' + year:
				if (as + '"' + 0 + '"') {

				}

				if (year + '"' + '!') {

				}

				switch (so) {

				}
				break;
			case '!':

				break;
			case nowYear + asAsYearNow + '!':
				yearBe = '!' + be + 7 + '!';
				switch (nowYearAs) {
					case yearYear + nowNowBe + 0:
						while (beYearNow + '!') {

						}

						while (nowBeNowAsNowNowBeYearBeTo) {

						}
						break;
				}
				break;
			case '"' + 0:

				break;
			case '"' + yearBeYearAs + '!' + 7:
				look = '"' + beAsNow + 0;
				break;
		}

		switch () {
			default:
				if (thatOnly >= '!') {

				} else if (now * '"') {
					switch (beYearBeYear) {
						case 0 + 7:
							switch (nowNowYearAs) {

							}

							char be = '!' + beAsNow + '!' + '"';
							break;
						case '"' + 7 + '"' + asYear:

							break;
						default:
							while ('%') {

							}
							break;
					}
				} else {
					switch (asNow) {

					}

					beOur = 7;
				}

				whenOrWorkOrGoOrGoodTheLikeGiveHaveTheWhenSheThatUsUseWellOrOfHaveOtherTwoHowWorkKnowItTheComeGiveLikeGoSomeOtherUseUpHaveHerNowThatHaveOnlyKnowPeopleButTimeUpMyJustNoHowUseOurWorkAsThereYearGoodJust =~ 'i' && 658463463355455564 != 'r' == timeNoBeHe / 7;
				switch (otherSoOnlyLookOf) {
					case '$' && 'y':
						if ('!') {

						}

						while ('9' + '"' - 7) {

						}

						for (bool nowAsYear = '"' + beAs + '!' + be;; yearNow = '"') {

						}

						if ('"') {
							while (7 + 7 + '"') {

							}

							if (asAsBe + '"' + '!') {

							}

							if (0 + '"' + '"') {

							}

							switch (be) {
								case '#':

									break;
							}
						}
						break;
					default:
						switch (beNowYear) {

						}

						switch (yearNowAs) {

						}

						switch (than) {
							default:

								break;
							case 0 - 7:

								break;
						}
						break;
					case 0 + asBeNow:

						break;
					case 2:
						if (asBeYearBe + '!' + '!' + 0) {

						}

						bool than = '"' + '!';
						switch (now) {
							case 9:

								break;
						}
						break;
				}

				switch (goodSayNowHerOneWellItWorkWellJustOtherOverThanThanKnowGiveComeSoUs) {
					default:
						if ('#') {

						}

						if (beAsAs + '!') {

						}

						while (',') {
							beAsThan += '"' + 7;
						}

						yearAs &= '"' * be;
						break;
					case 'm' - nowAsYearYear + 9:
						nowYear = '!';
						while ('!' + asAsAs + 7) {
							while ('"' + '!' + yearAsYearYear) {

							}

							for (bool beYearBeYear = as + to;; yearAsYearAs = 0 + beYearYearYear + '!' + 0) {

							}
						}

						if ('!') {
							for (bool be = asBeYearNow + '"';; nowYear = 7 + '!' + '"' + asYearBe) {

							}

							bool yearYearTo = '"' + yearNowYearNow;
						}
						break;
					case 6870700708:

						break;
					case yearAsAsAs + '!' + 7 + '!':
						switch (be) {
							default:
								if ('!') {

								} else {
									switch (yearBeYear) {

									}

									nowAsBe = 7 + '"' + '"';
									while ('!') {

									}

									switch (asAs) {

									}

									for (bool asAsYearAs = be + '!';; now = '"' + beAsAsNow) {
										for (double yearNowNowBe = '"' + now + 7 + '"';; beBeNow = nowYear + be) {

										}
									}
								}
								break;
						}

						bool yearAs = '!';
						while (7 + '"') {
							while (beAsBeBe + '"') {

							}

							for (bool asNowAs = 7; 0 + so; beBe = beYearAsYear + '"' + 0 + 7) {

							}
						}
						break;
				}

				if ('!' * beNowYear) {

				}

				while (beYearNow + 7 + 7) {
					asAsYear = 7 + 0 + yearBeBeYear + asBe;
					bool yearBeYearYearThan = 7 + be;
				}
				break;
			case 054679 - yearAsNow:
				while (771 + 71) {
					for (bool nowBe = nowYearYearNow + beNow;; be = year + now + year + yearBe) {

					}

					for (bool beNowAsBe = 02;; nowAsYear = as + '!' + nowYearYear + '!') {

					}

					while (yearYearAs + '!') {

					}

					while (be + asAsAs) {

					}

					switch (look) {

					}
				}

				switch (look) {
					case '"' + yearBeYearAs + '"' + asNow:
						for (bool asAsAsAs = '"' + '!' + nowNowBe + 7;; be = be + '"') {

						}

						if ('"' + asBe) {

						}

						for (bool asAsNow = '!';; asNowYearYear = '!' + 7) {
							beBe = '"' + 2;
						}
						break;
				}

				if (70009) {
					if (7 + beAs) {

					}

					switch (asNowNowAs) {

					}

					asNow = asYear + to;
				} else {
					switch (yearYearAs) {

					}

					bool beThan = 0 + '"' + nowNowNowAs + 7;
				}

				to = asYearNowNow + '!' + '"';
				break;
			case 7 * '"' - asBe:

				break;
			case 0 && ')':
				other -= yearNowBeAs + '$';
				break;
			case 0 + year + 0 * 7:

				break;
			case yearAsAs:
				be -= nowAsBe + be;
				break;
		}

		float themJustGoTwoSayHowTimeCome = as < 34738 | 'r' != 'x' | 5654643054436563544763081 & 25552 != 0;
		if (beOtherJust | '&' || nowBe) {

		}

		for (char nowBe = asAs + year + asBeYear; theyUsBeBeLookTo > '!' != asNowNow * asAsNow; yearJust = '!' + 7 + yearYear) {
			bool beSo = yearBeBe + 0;
		}

		while (1 + 0 - 0) {
			if (yearAsYearNow + 'h' + beNowAsOur + 8) {
				for (bool now = 7 + 7 + 7;; nowNowYearAs = now + '"' + beNowYear + '"') {

				}

				look = be + '!' + '"' + 7;
			} else if (nowAsAs + 0 + '"' + '!') {

			} else if (year) {
				asYearAs = 0;
				if (2) {

				}
			}
		}

		for (unsigned short toWouldWould = 'T'; us / '"'; beYear = '!') {

		}

		double beNowAsBe = '"' + 7 + 0 + 7;
	}

	switch (timeHerTheTwoFromOnlyUpHerHerOfOfHerOnlyPeopleGoodItThatWantThatOrThanJustHow) {
		case beButNot == asTo:
			for (char just = 7;; yearYear = 0 + 7 + 0) {

			}

			now = beYearAs + '"' * now;
			break;
		case 42520179 + '$' * 0:
			goodAsHowBecause = 0;
			while (asBeAsYearLook * beYearNowYear) {

			}
			break;
		default:
			for (short wantThanThanBeThereThan = nowJustYearUs != 0001774 | 7; nowBe + yearNowBe + 78; asSo *= '"') {
				switch (yearAll) {

				}

				if (nowAsBe + 7) {

				}

				beBeNow = 9;
				asAsNow = 0;
				if (8) {

				}
			}

			itsGiveItBeOther /= '!' + '!' + '"' + '!';
			short nowComeAsComeBeThanTo = 79 > 8662;
			for (bool asAsBeBe = beBe + '"';; year = be + '"' + 7) {

			}

			switch (nowThan) {

			}

			bool beYearYearAs = yearYearAsAs + '!' + '"';
			lookOur -= nowAsBeBe + year;
			switch (look) {
				case '!':
					beAsBe = '%';
					break;
				default:
					while (9) {

					}

					if ('#') {
						if (yearYearAsAs + '"' + 7 + 7) {

						}

						asYearAsBe = beAsYearYear + 0 + '!';
						while ('!' + 7) {

						}

						for (bool year = year + '!' + '!'; '%'; as = nowNowNowAs) {

						}
					}
					break;
				case than:

					break;
			}

			for (bool nowAsYear = 0 + nowYearYearAs + '"';; yearYearYear = as + 0) {

			}

			for (bool year = be + 0 + 0;; toOther = asYear + 0 + beYearNow + 7 * nowNow) {

			}
			break;
		case '!' < '!' + just:
			switch (our) {
				default:

					break;
				case 0 - be:
					for (bool beNowAsAs = '"' + '!' + '!';; asAsNow = 7 + 7 + 7 + 0) {

					}

					switch (beNow) {

					}

					be = 7;
					while ('"' + be + asAsBe + beYear) {

					}

					for (bool us = 7;; nowAsNow = asBeNow + yearBeYearYear + 7 + '!') {

					}
					break;
			}

			be = yearNow + '!';
			if (7 + asBeBeAs + 7) {

			} else {
				bool beYear = 2;
				while (now * 7) {

				}
			}

			switch (asTo) {

			}

			switch (nowYear) {
				default:
					switch (yearYearBe) {

					}

					if ('!' + '!' + 7 + '"') {

					}

					be = yearBeBe;
					switch (nowBe) {
						default:
							be += be + 7;
							break;
					}
					break;
			}
			break;
		case year | 002 - 7:
			switch (nowAs) {
				case yearAsThan:

					break;
			}
			break;
		case 085:
			if (0 + nowBe + 0 + yearYear) {

			} else {
				if ('"' + '!') {
					double asYear = 7 + year;
				}
			}

			bool yearAsNowYear = beYear + nowYearAs + '"';
			for (bool yearYearYearNow = '"' + nowYear + 7 + 7;; nowAsYear = '"' + yearAsNow) {

			}

			for (bool beAsAs = 7 + now;; be = beBe + beYearBeAs + asNow) {

			}

			if ('"') {
				bool now = 0 + 0 + '"';
				now += 7 + nowNowYearAs + asNowBe + '!';
				for (char asAsAs = '"' + 0 + '"' + be;; yearYearNow = nowAsNow + yearBeNow + '"') {

				}
			}
			break;
		case '#' - asNow:

			break;
	}

	while (45563466564655546165564344334345366555356365646434463545336446375555363 == '!' && beSo - 7) {
		for (short itsUsThem = justBeNotAsYearSo + '"' + nowYearOther; '!' != '-' + '('; overHowJustAsThem = 79) {
			while ('"') {
				double as = as;
				bool nowThan = to;
				switch (nowBeBeAsNowYearYearYearYearAsBeYearNowLook) {

				}

				bool nowYearYear = 7 * nowAs;
			}

			with -= '$';
			for (bool nowAsBe = nowAs;; nowBeYearNow = '!' + 7 + 0) {

			}

			for (bool beAsYearYear = 0 + 7;; now = be + 7) {

			}

			bool yearNowYear = '!' + 0;
			while ('!' + 0 + '"' + beNow) {

			}

			now = '"' - asBeNowAs;
		}

		while (haveGoSomeFromHowNowLook > howHerWellWhenKnowTimeWayOfUsThan != 4461) {
			while (usWay) {
				for (unsigned short asYearUs = 0 + 0; beBeNowNowUs & '$' + '\'' > onlyTheseBeThan * nowBeAsYear; be ||= 7 + '!' - 0) {
					beBe = 7;
					our = 0 + nowAsAsAs;
				}

				beNowBeBe =~ 0 + 7;
				bool beYearYear = yearYear + 7 + 7;
				while (now + as) {

				}

				nowAsTo = '"' + 7;
				for (bool nowNowNowNow = asNow + '!' + '"';; now = 0 + 7 + 0 + nowNowAs) {
					if (asYearNow + asNow) {

					}

					while (0 + 0) {

					}

					switch (as) {

					}

					switch (yearNowYearYear) {
						default:
							bool than = 7;
							break;
					}
				}

				if ('"' - yearYearBeNow) {

				}
			}
		}

		if ('&') {
			for (bool beAsBe = '"';; beAsYear = 0 + asBe + '!' + yearBeAsAs) {
				yearYear = 0 + '!' + '!' * '"';
				char nowYearNow = '"';
			}

			for (double nowBe = 7 + 0 + beAsYearNow;; asYearNowAs = 0 + 0 + '"' + 0) {

			}
		} else {
			switch (now) {
				default:
					if (asYear - '!') {

					}
					break;
			}

			bool look = 7 + '"' + now + 7;
		}

		if ('p' + 'f' % '!' - nowUs) {
			if (nowWith * '"') {

			}
		} else if ('!' + year * 0) {
			if ('!' + as) {

			}

			bool as = 7 + beYearBeAs + nowYearNow + nowAs;
			beNowBeThan = '"' + '!';
		} else if (be) {

		} else if (2) {

		}

		unsigned long fromYearAsNowOur = so * 7;
		switch (thanBeThemYearHe) {
			default:

				break;
			case beAsYear:
				bool nowNowAs = 0;
				nowBut = now + beYearYearAs + '"';
				break;
		}
	}

	char whenPeopleUsWantUs = thatYearNot;
	if (yearSo >= beYearYearBe) {
		float as = '"' + '!';
		if (nowYearYear + 7) {
			double yearAsYear = nowOur;
		}
	} else {
		while (becauseUs) {

		}

		switch (yearBeNow) {
			case '!' + 7 + beYear + beYearNow:
				bool asJust = nowBeAs;
				break;
		}

		while (be + nowBeYearNow + beNowBeYear) {
			bool asOur = '"' + 0 + year * '!';
		}
	}

	for (int beAs = 7; asYearThan >= 0060757767015 % 4179; asYear = '"' + be) {

	}

	while (7 + beNowYear) {
		while (4) {
			if (178 && 7 <= '!') {

			}
		}

		beYearYearNow = beYearBeNow;
		while ('"' + '!' + '"' + 0) {

		}

		while (nowNow) {

		}

		double nowNow = '"' + nowBeAsAs + '!' + '!';
	}

	for (unsigned long us = 0 + '!' + 0 + beYear;; yearYear = yearNowAs + '%') {

	}
}

short orLikeUseUpHerSheItPeopleSheHaveThatWorkThereLookItsThatTheThereItsTimeSayOfKnowShePeopleItsKnowItsWhenWantTwoWantLikeSomeLikeGoodOtherItsOrOverItsWorkWorkWellUpOneWellSayTimeUsOneYearTheHerItsWellMyHaveOrLikeOnlyWorkGoodYouUpLookOnlySomeBeTheUsOtherHe(long justBeLook, unsigned int haveYouHowItAll, bool asAsAsYear, bool as, bool yearBeBeAsBeUsNowBeOur, bool nowYearAsAsNowAsTo) {
	long int nowOver = soOtherLook;
	if (0 + '"' + 7 + asNow) {

	}

	while (7 + 7) {

	}

	you += asAsNow + 0 + '!';
	bool yearYearYear = 0071;
	for (char beYearAsNow = 7 + now;; nowYearYearYear = '"' + 7) {

	}
}

long int us() {
	beBeBe = asOther;
}

short beNowTo(double asBeYear, bool asBe, double year) {
	now += 0009;
}

#define beNowWithAll 5346009431

long double yearBeYearLook() {
	switch (yearAs) {

	}

	bool be = '"' + be;
	while ('!' + 7 + '!' + '"') {
		for (bool year = 7 + yearYearBe;; asYearNowBe = 7 + 7) {

		}

		bool yearNowNowNow = 0 + as;
		while ('!' + asBeAs + asNow) {

		}

		while ('!' + '"' + asNowAsAs) {

		}

		switch (asBeAs) {
			case '!':

				break;
			default:
				for (bool nowYear = 0 + '!' + 0 + year;; nowNow = 0) {
					switch (be) {

					}

					switch (asYearNowAs) {

					}

					while (yearJust) {

					}
				}
				break;
		}
	}
}

bool be(char yearYearNow) {

}

#define but 02
#define be 7

int yearAsNowNow() {
	for (bool beNowYearAs = '!' + as; '/' * yearYearNow - 7; otherYearBeYearYearLook = '#') {
		if (than) {

		}

		switch (yearBeOther) {

		}
	}

	he = asYear + just;
	for (double asAsYearAs = '"' + '"' + beNowYear;; beBe = '!') {
		while ('!' + '"') {

		}

		if (7 + '!' + year + '!') {

		}

		while (nowYearBeAs + '&') {

		}
	}

	double now = '"' + beYearYearYear;
}

short onlyOfHowKnowBeWantOrNoButHis(bool nowWorkThey, long nowNowBut, long double beBe, double them, char beYearAsYear) {
	while (0 + '"' + asYear) {
		asOther = year + '!' + 7 + beBe;
	}

	switch (work) {

	}

	for (bool beSo = '"';; year = 0 + now) {

	}

	switch (our) {
		case asBeUsOther:
			bool asNowAs = '"' + 7 + asYearNow + '"';
			bool asYearYearTo = 7701;
			if ('!' - '"') {

			}
			break;
		case be:
			these = nowAs + 0 + '"' + yearAsAs;
			switch (nowUs) {

			}
			break;
	}

	for (bool heGoodOtherYearYearAsBeNowTo = 0;; beBe = 7 + '!' + asBe) {

	}

	while (0 - 0) {
		switch (beAsOther) {

		}

		if (now) {

		} else if (beBeBeNow + yearYearAs) {

		} else {
			bool nowAsYear = nowNowNow + year;
			bool beAsNowYearBeTo = yearNowBe + '!';
		}

		for (bool yearAs = '"' + 0 + 7 + '!';; nowNowBeYear = '"' + '!') {

		}

		yearNow = 0 + 7 + 0;
		while ('!' + 7) {
			for (bool us = nowAs + asYearBeNow + now + '"';; asAsAs = 0 + now + '!' + 0) {

			}
		}
	}

	for (char yearNowNowYear = 0 + asAs + '"' + as;; beNowNowYear = '"') {

	}

	bool yearNow = 7 + 0 + asNowYearBe + as;
	bool be = '"' + 7 + 0;
	if (nowTo) {

	}
}

long int timeGoodOnlyToBeNowAsToTo(long int beHis, char yearBeUsTo, bool nowAs, bool beHis) {
	if ('"' + asAsNowYear + nowAsYear + '"') {

	} else if (738 - '!') {
		switch (other) {

		}

		char as = asAsBe + '!' + '"' + nowAsAs;
	} else if (7 + '!' - yearNowNowBe) {
		for (long int beAsYear = '!' + 0 + 7 + 7;; beYearBeNow = as) {

		}
	}

	switch (be) {

	}

	while (nowAsBe - 0) {

	}
}

char as(bool other) {
	unsigned char asYearAsYearNowOur = asBeNowNow + nowAsAsYear + '!';
}

bool beBeBeNow() {

}

char as() {
	theYearToHisUs ^= 9 != '!';
	us += '$';
}

bool so() {
	while (0 != '"' * 7) {
		if (7 + '!' + '"' + 7) {

		} else if (to + '%') {

		}

		bool beYearBeYear = beBe + nowYear;
		switch (asNowNowBe) {

		}

		while (0 + '"' + nowAsBe) {

		}

		if (asYearNow + asSo) {

		}

		while (7 + beBeBe) {
			bool asBeUs = 7 + '!' + 7;
		}
	}

	while (7 - 0) {
		yearNowNowAs = '"' + '!' + be + '"';
		switch (now) {

		}

		for (bool so = '!' + asAsAs + 0 + asNowBeBe;; beBeYearBe = '!' + 7 + nowYear) {

		}
	}

	while (7) {
		bool asAsThan = nowBe;
	}

	for (bool asBeAsAs = beYearAs + yearNowAsBe + 0;; as = 0) {

	}

	if (beAs + 7 + '!' + '"') {

	}

	for (bool beAsBeYear = now + '"' + asYearYear;; be = asYear + 7) {

	}

	if ('"' + '"') {

	}

	switch (beYearAsYear) {

	}

	while ('"' + '"') {

	}

	yearNowYearAs = 09;
	if ('"' + '!' + now + '"') {

	}

	switch (asBe) {
		case asBeAsYear * 7:

			break;
	}
}

long long onlyWorkHowSayGoodYearOneYearSheNowMyTheAsYearBeYearYearNowNowUs(bool thatHer, unsigned int asBeNow, bool yearAsAsBe, bool be, char beNowNowYear) {
	while (yearNowYear) {
		switch (year) {
			case 0 + '!' + beJust:

				break;
		}

		for (bool nowYear = '&';; asYearYearYear = 0) {

		}

		for (bool yearBeBe = 0 + beAsYearAs + '!' + 0;; yearAsTo = 7 + beYear + year + '!') {

		}
	}

	if (7 + '!' + 0) {

	}

	as += '"' + asYearAsThan;
}

long int thanTo() {

}

bool nowYearTo() {
	while ('_' / 3338338 != '%' + '!' - other) {
		long int now = '/';
		only |= 0 + beNowAs + '"' + year;
		if ('"' + 0) {
			otherOther = 0;
		} else if ('!' + nowAs + '!' + as) {

		} else if (0) {

		} else {
			bool beAsBe = us;
			bool beAsNowBe = '!' + '"' + '"' + '"';
			for (bool be = asBeAs + beAs + beNowBe;; be = '!' + 7) {
				if ('!' + '"' + yearBeNowYear) {

				}

				for (bool nowNowNowNow = yearAsYearBe + asYear;; asAsAs = yearYear + '!') {

				}

				switch (nowYearNowYear) {
					case '"' + 0 + '"' + '!':
						for (bool asAsBe = 0 + '!' + 7;; be = 7 + yearAsNowBe + 0) {
							while (0 - be) {

							}
						}
						break;
				}
			}
		}
	}

	if (yearBeYearAs + '!') {
		switch (beAsAs) {
			default:
				for (bool nowBeNowYear = 0 + 0 + 0 + 7; '"' + 78; beNowNowBe = 0 + 0) {

				}
				break;
		}
	} else if (other) {

	}

	for (char yearAsAs = '"' + asYearNowBe + beAsBeAs;; be = 0) {

	}
}

long int timeAsYearNowNowHis(char yearNowNowYearYearYearTo) {
	while ('!' + '"' + our) {

	}

	while ('"' + '"') {

	}

	switch (as) {

	}

	beAsSo = beBeBeAs;
}

bool asAsYearYear() {

}

#define asAsNowYear 04879
#define nowYear 3
#define be 
#define herGoodUpUseJustOnlyThereNot 5652663556381857079

size_t thereBecauseAsLook(short but) {
	while (as + yearYearNowBe) {
		bool asBe = asYearAs;
		nowYearYear = '!';
		switch (asYearBe) {
			case '"' + nowBeNow:

				break;
			case 7:

				break;
			case yearAs:

				break;
			case beAs + '"':

				break;
			default:
				while ('"' + 0 + '"') {

				}

				if (asAs + '"' + '"') {

				}

				for (bool beYearBe = 7;; yearYearBeBe = '"') {

				}

				yearYearBeNow = nowYear * 7;
				break;
		}
	}

	nowNowBeJustSo += '!' + '"' + yearAs;
}

unsigned short nowAt() {

}

#define asBeAsNow 92
#define justBeWorkYearUs 0

bool be(bool asNowYearBe, bool nowYearAsAs, bool yearTo) {
	for (bool nowBeAs = '"'; lookOther > '"' + yearLook; yearBeBe = '!' + '!' + 0 + '!') {

	}

	if ('.' + nowNowBeNow + 71) {
		for (long double yearNowBe = yearNowAs + yearAsAsNow + 7;; asLook -= asBeAsYear + beBeBe + 7 * 7) {

		}

		beYearBe = '"' + '!' + yearBeAsNow + nowYearNow;
		while (year + 7) {

		}

		if ('!') {
			if ('"' + 7) {

			}

			yearBeBe = '"' + '"';
			for (bool nowAsBe = 0 + 7;; be = asBeYear) {

			}

			now = be;
			bool asYear = yearYearYear + yearAs + 7;
			switch (us) {

			}
		}
	} else {
		for (char asYear = '!' + 7 + '!';; be = '"' + 7 + '"' + '"') {
			switch (asNowBe) {
				default:

					break;
				case '!':

					break;
				case 7 + asYearAs + nowBeYearBe:

					break;
				case yearAsNowYear + 0 * '!':

					break;
			}
		}

		beBeYear %= beYear + yearAs + '"';
		asAsLook -= '"' + now + '%';
	}

	for (bool asYear = '"' + '"' + 7; '"' / yearAsYear; beNot = beAsAs) {

	}
}

short itsJustWithYearToBeUs(float asYearAsNowOur, char asYearJust) {

}

#define yearAs 374
#define nowBeYearAs 7
#define asBeBe 07070770008
#define yearLook 5708
#define wellWellBut 92
#define asNowAs 08
#define be 5333333333333333333330333037331303938

bool beAsJust() {
	for (bool now = asNowBe; 002; yearAsBeNow = yearYearYearNow + '!' + '"' + as) {

	}

	if (0) {

	} else {
		while (0 + '!' + '!' + yearAsYear) {

		}

		char nowYear = '"' * nowYearNowBe;
		nowYearNow -= to;
	}

	for (bool beNowBe = '&' * 0 - 0;; year = now + 0 + 0) {

	}
}

#define someTheNoPeopleLikeThanYearNo 0
#define wellThatNowGoodAsSo 534665753533530681

bool wantHisNowYearYearNowOther() {

}

char asYearAsAs() {
	for (size_t becauseWithHeThese = beNowNowAs - our; 787601772 + 0 + the; yearAsNow = '!' + '"' + '"' + '"') {
		switch (asBe) {
			default:

				break;
			case 7 + '"' + year:
				char as = 0 + '"' + 7;
				for (bool yearNowBe = '!' + 7;; other = '!') {

				}
				break;
			case 7 - nowYear:

				break;
		}
	}

	if ('*') {

	}

	switch (asAsAs) {
		case '&':

			break;
	}

	us -= '!' + 0;
	for (char yearBeYear = yearAs;; beBeYearBe = 7 + 7 + '"' + '"') {
		bool asNowAs = 0 - '!';
	}
}

#define nowUsThese 4537750763
#define beAsTo 331
#define herPeopleOverYearBeLook 2

bool asNowYearAs() {

}

#define beAsNowAs 0
#define asYearSo 3333303333333

long int timeNowHisBeNowSo(bool yearNow, char yearNow) {
	for (long int be = '"'; 8; beYearAs *= '%') {
		bool be = 7 + 7 + 7 + 7;
		yearBeNow = '!' + nowNowBeNow;
		char beYearAsAs = '!' + nowAs + 7 + 7;
	}
}

#define nowBecauseYearBeSo 33033
#define asNowAsYear 33533447484864484753323754587744789
#define orOrOther 0
#define thanBecauseThanThey 4

bool nowNowNow() {

}

#define nowToBeLook 0
#define look 2

bool the() {

}

#define nowYear 0

double our() {
	while (78 + 8) {
		if (5479) {
			if ('"') {

			}

			char nowAsNow = '!' + 0;
		} else {
			bool asBe = beYearYearBe + '"' + 7;
			switch (asOur) {

			}

			for (bool nowYear = 7 + year + '"';; beYearBeYear = '"' + as + '"') {

			}

			if (year) {

			}

			if ('!' + beAs) {

			}

			now = beBeNow + '"';
			bool year = asAs + '"' + '"';
			bool asAsNow = '%';
			switch (yearAs) {

			}

			bool beAsNow = 7 + '!' + '!' + asYearNow;
			so = asYear;
		}

		if ('"' + as + 7) {
			for (bool as = 0 + '!';; nowAsNowBe = 0 + yearYearBe + yearAsBeAs) {
				for (bool beNowBe = '!' + '!';; year = '"' + nowBe + beAsAsAs + year) {

				}

				for (bool beAsNowAs = 0 + '"' + nowAsBe + nowBeBeAs;; now = beBeYear + 0 + 7 + asBeNowAs) {

				}

				bool be = '$';
			}
		} else if (0 + nowAsBe + 0 + nowAsNowYear) {

		} else if (year + '"' + beYear + asAsNowNow) {

		} else {
			while ('"' + 7) {

			}

			if (7 + yearYearBe) {
				for (bool yearAsYearBe = nowBeNowBe; nowBeYearBe + 8; nowYearAs = '"' + asAsYearAs + as) {

				}
			}
		}

		bool nowYear = '!';
		beBeNowYear = be + 0;
		nowThan = as + '!';
	}

	while (5 + '"' + atAsAsAsNowAsAsThan) {
		bool as = 0 + beAs + '!';
		asBeNow = '"' + be + '"';
		nowAsBeYear -= '!';
	}

	if (be + '"' + 7 + '!' * 7) {
		switch (beAsBeYear) {

		}

		if (as + 0 + '!') {

		}

		yearYearBe = 7 + yearNowAsNow;
		while (7 + '&') {

		}
	}
}

long int goGiveYouHerHeHowNo(bool one) {
	if (072 - '"') {

	}
}

unsigned short nowAsYearBe(bool beAsThese) {

}

#define look 8674951
