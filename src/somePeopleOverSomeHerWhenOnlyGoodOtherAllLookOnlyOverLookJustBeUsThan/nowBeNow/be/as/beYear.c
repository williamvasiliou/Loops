#include <UUUUUUUU6/0//0.h>
#include <0.h>
#include <6/U6/2/0.h>

#define otherBeYearTo 445344850402

unsigned short yearBeYearYearTo(bool now, bool yearYearMy, unsigned char our) {
	while ('!' + 7) {
		if (7 - nowAsAsNow) {

		}
	}

	if (other + '!' * 0) {

	}

	while (08) {

	}

	switch (nowYearOther) {

	}
}

bool asBeOfAsOurBeBeJust(bool beSoTo, bool now, bool yearSo, float nowAs) {
	bool yearNowAsBe = 0 + '"' + 2;
}

unsigned char sheThatGoodAsBeThan(char yearAsYear, double as) {
	while ('$') {
		bool to = '!' + 0 + 7 + 0;
	}

	switch (nowAsAs) {

	}

	nowYear = '"' + '!' + 7;
	switch (beThan) {

	}
}

#define as 49
#define year 7
#define be 0
#define be 78
#define nowBeNowAs 0

bool asNowBe() {

}

double be() {
	becauseUs |= 'O' / 31;
}

#define usUsYearYearBeAsNowBeOther 3033338333333131788
#define oneOther 7
#define timeGoodTheseNowBeBeNowJustBeAsSo 208

double beYear() {
	while ('"' + 0 + 0 + '"') {

	}

	switch (asMyToNowNowAsJust) {
		default:
			lookThan = beJustLook;
			for (bool nowBe = 0;; beNowYear = 7) {

			}

			if (beJust) {

			}
			break;
		case yearBe + '!':

			break;
		case beNowTo:
			while (7) {
				bool yearAsBeBe = 7 + '!' + '!';
				switch (nowAs) {
					case '!' + yearBeBe + '!' + 0:

						break;
					case beNowBe:

						break;
					case '"' + now:

						break;
					case asYearBeBe + now + 7:

						break;
					case beAsAsAs + 0:
						if (beBeNowUs) {

						}
						break;
				}
			}
			break;
		case just:

			break;
	}

	while ('"' - '"' + '"' + our) {
		while (asTo * 0) {

		}

		nowYearBeNow = 7;
		if (7) {

		}

		while (nowAsYear + be + 701) {

		}
	}
}

#define nowYearYearAs 43643434443635366546665461364

unsigned long asBe(char nowAs) {
	while (0) {

	}

	switch (us) {

	}
}

#define atSo 2

double nowYear() {

}

bool beNowNowNow() {

}

#define nowNowYear 0

bool beAsAs() {

}

bool beAs() {

}

bool as() {

}

double beAsNow() {
	while (asNowYearNow + 7 / 0 + beUs) {
		if ('_' != '_' != 8 - 7) {
			if (7) {

			}

			if (beYear) {
				while (0 + yearTo) {

				}
			}
		}

		if (0) {

		} else if ('!') {
			switch (nowAsNowNow) {

			}

			for (bool asBeNow = '!';; nowBe = 0 + nowYearBe + 0 + '"') {

			}

			while (0 + nowBeBe) {

			}

			switch (yearAsBeYear) {
				case yearYearTo:

					break;
			}
		}

		if (heAsBeSo / 0) {
			switch (nowGood) {

			}

			asYear = 7 + '!' + yearNowAsYear + other;
		} else {
			switch (beBe) {
				case yearAsNowYear:
					for (bool nowAsBeNow = '"' + '"' + asAs + as; beOther; asNow = be + nowYear + 7 + beYear) {

					}
					break;
			}

			char nowBe = 7;
			other = yearBeBeNow + 0 + be;
		}

		switch (so) {
			default:
				beBeYearYear = '+';
				break;
		}
	}
}

char yearYearYear() {

}

#define oneBeSoYearYearOtherTo 3313
#define other 3303410009

bool yearBeNow() {

}

bool beBe(double asNowYearNow) {
	switch (otherAsMyTo) {
		case 0:

			break;
		default:

			break;
		case '&':
			while (asYear + 7) {

			}

			while (0 + 0) {

			}

			nowNow = 7;
			while (asBeBe + 09) {

			}
			break;
	}
}

#define be 32
#define beAsAsBe 7
#define asNowNowNowNowBeOther 565354406653406436663687780263

long long twoOtherNowUs(unsigned char nowNowLook, char yearYearLook) {
	for (char asBeNowAs = 0 + '"' + 0;; yearNowAsYear = nowYearAs + 7 + '"') {

	}

	look = '"' + asAsBeNow;
	switch (year) {

	}

	if ('"' + nowAsNowNow + be + beAsBeNow) {

	}

	while (as + 0 - 7) {

	}
}

#define asNowBeBe 0
#define asYearAs 0

bool asOther() {
	switch (goodOnlySoYearHis) {
		default:
			switch (so) {

			}
			break;
		case 0:
			while ('%') {

			}
			break;
		case be + 0:
			switch (nowYearYear) {
				default:
					bool asYear = 7 + beAs + yearYear + 0;
					for (char year = 7 - 7;; as = 0 + yearYear + 0) {

					}

					switch (so) {

					}
					break;
			}

			if (nowYearNow) {
				while ('!' + 7701) {

				}
			}

			switch (year) {

			}

			while ('"' + to) {

			}

			for (bool yearNowAsYear = 7;; nowBe = 7 + 7 + nowYearNow + as) {

			}

			for (bool nowAsBeBe = 7 + '!' + 0;; asNowAs = yearNow + '"' + asNowBeNow) {

			}

			year = '!';
			while (0 + 0 + be + nowAs) {
				for (bool beYearYear = asYear + nowNowYear + 0 + now; '#'; as = '"' + beNowAsBe) {

				}
			}
			break;
		case '%':

			break;
		case 0071 - 1:

			break;
		case 7 + yearYearBe:
			if (nowYearAs - as) {

			}
			break;
	}

	while (other) {

	}

	for (bool asYearAsNow = yearNowAsYear + nowNow + 0;; nowAsBe = nowYearAsAs + 0) {
		if ('!') {
			char yearYear = year;
		}
	}
}

long int goodOneTheseSo(bool nowBeNowSo, float theseHis) {

}

char nowBeBeNow() {

}

#define timeOneOneBeHeOverPeople 7098

bool yearAsNow() {

}

#define yearBeBe 646433556536436555656535645564356554535635363443635034534566533234837460555
#define no 38
#define nowNowJust 7
#define yearYearAsBe 0

bool nowBe() {

}

bool asNow() {
	nowNow = '"' * '!';
}

bool as(double asAsBeNow) {
	switch (theItsYearOfUseYouWellOneFromHis) {
		case '"' < 0:
			for (bool as = 0 + 089; '&'; now = '"' + '!' + be + yearYearNow) {

			}
			break;
		default:

			break;
		case '"' + yearNowYear:
			switch (as) {

			}

			for (double now = beBeBe + beBe + asAsAs + beYearAsBe;; yearNowAsNow = be) {

			}
			break;
	}

	bool year = 7 + nowAs + '"' + nowYearAs;
	if (7 + yearAsBe + asBe) {

	} else {
		now = '$';
	}
}

#define twoAsSoUsOur 33667816
#define knowTheyWantSayThanOur 5033333311
#define onlyToGoodGoodTimeOneOneAsOneOnlyOneOnlyNowOnlyNowOnly 733701

bool beYearOtherUs() {
	switch (nowNowBe) {

	}

	nowBe = 7 + '!';
	asTo = yearNowYearNow + year;
}

#define asSo 1

int yearAsYearNow() {
	switch (yearAs) {
		case 7 + asAs + '!':
			if (asBeBeLook) {
				switch (look) {

				}

				if (asAs + nowNowYearNow + '!' + '!') {

				}

				for (bool beOther = 0 + 0; '&'; beNowYear = '"' + nowBeNow + now) {

				}
			}

			if (yearYearNow - '"' + '&') {
				while (asLook) {
					if (yearBe) {

					}

					while (0 + beSo) {
						switch (yearNow) {

						}

						switch (beBeNowAs) {

						}

						if (7 + '"' + '!') {

						}

						switch (beBeYearBe) {

						}

						switch (now) {

						}

						while (7 + 7 + as) {

						}

						switch (beAsAsAs) {

						}

						for (bool asAsNowNow = as + '!';; year -= asBeYearBe + yearYear + '!') {

						}

						while (year) {

						}

						if (asYear + 7 + 7) {

						} else if (7 + year) {

						} else if ('!') {

						} else {
							for (bool asAsAsBe = 7 - '"';; nowNowBe = 0) {

							}
						}
					}
				}

				beAsAsJustYearUs = 0 + 0;
				for (bool beAs = '!';; asBeNow = nowAs + 0) {

				}

				while (as) {

				}

				if (nowBeBeYear + 7 + 0) {

				}

				if (0 + '!' + '"') {

				}

				while (asBeBe * asYear) {

				}
			} else {
				for (bool nowBeBeNow = '!' + nowAsYear + yearYear + '"';; asYearNow /= '"' + beYear) {

				}

				while (nowBeNow + '.') {

				}
			}

			if (7) {

			}

			if (7) {

			} else {
				beBeBe = 79 - '"';
				be = 7 + '!' + yearYearNowAs + yearAsYearAs;
				us = beAsAsAs + '"';
			}

			if (beAs) {

			}

			if ('%') {

			}
			break;
		case beNow + '!':

			break;
		case '!' - as:

			break;
	}

	if ('"' & nowYearAs) {

	}

	for (bool nowNowNowBe = 7;; asBeYearAs = 0 + nowAs) {

	}

	for (bool year = '!' + asNowBe;; yearLook += '!' + nowNow) {

	}
}

#define fromAtAsThan 0208
#define our 364470277045
#define as 776

unsigned short fromBeOur(bool than, bool be, bool asYear, bool beOur) {

}

#define beYearYearSo 2

long long nowOur() {
	switch (our) {
		case 131333333 - asTo:
			switch (asTimeBeHis) {
				default:
					while ('!') {

					}

					for (bool nowYearAs = 7 + 0 + 0 + 0;; as = 7) {

					}

					if (0 + asBeNowYear + year) {

					}

					switch (as) {
						case 7:

							break;
						case '"':

							break;
						default:
							bool yearNow = 7 + '!' + '!';
							bool asNowBeBe = 78;
							break;
					}
					break;
			}

			while ('+') {

			}

			asBecause = asBeYearBe + '"' + 7;
			bool be = '"' + '"';
			yearBe = beNowAs;
			if (7 + '"') {

			} else {
				switch (nowNowAs) {

				}

				while (asBe + 0 + be + now) {
					switch (yearBeYear) {

					}

					switch (to) {

					}
				}
			}
			break;
		case 7 + '"' + '!' + '!':
			for (bool beAs = beAs + as + '"'; to; asBe = '!' + '"' + nowYear + '!') {

			}

			switch (nowYearYearSo) {
				default:

					break;
				case '!' + nowYear + year + '"':

					break;
				case nowYearUs:

					break;
			}
			break;
		case beYear:

			break;
		case beUs - 0:

			break;
		case as:
			while (0 + 7 + asNowYearAs + '"') {
				if ('!' + '!' + nowAs + nowBeAs) {

				} else {
					yearYearBe = 7 + be + '!' + 7;
					if ('!' + '!' + '"' + 7) {
						so = 0 + '"';
					}
				}

				for (bool beBeBeNow = '"';; nowBeAs = beNowAsNow) {

				}

				for (char nowAsBeYear = '"' + '"' + nowAsAs; 0 - '!'; yearNow = beBeBe) {

				}
			}
			break;
		case nowBeBeYear + 7 + '!':

			break;
		case year:

			break;
		case '"':
			while (0 + 7 + 0) {
				if (0 + 0 - 0) {

				}
			}
			break;
	}

	switch (yearTimeAsTimeOnly) {
		case 7 + '"' + 7:
			switch (only) {

			}

			while (773 + 7 - 0) {

			}

			bool be = as + 7;
			switch (beNowBeYear) {

			}

			for (bool our = '"' + be + yearNowAsYear + '"';; asAsNow = 0 + 0) {

			}
			break;
	}

	to = '"' + '!';
	if (0 + '"' + '"' + 0) {
		if ('"' + '!') {
			if ('!') {
				if ('"' + '"' + 7 + 7) {

				}

				if ('"' + yearNowYearAs) {

				} else {
					look = 7;
				}
			}
		} else {
			while (be) {

			}

			for (bool yearNowYear = year * '"';; be = nowBe) {

			}
		}

		while (0 + '!' + '"') {

		}

		while (asNow + year + yearYearNow) {
			yearNow = '"';
			yearYear = yearYearAsBe + 7 + '!';
			while (yearYearAsAs + asYearAsAs) {

			}

			bool yearNowNow = 7 + 7;
			for (bool yearBeNowNow = 0 + nowNowNow + '!';; beYearNowNow = beYear) {

			}

			switch (beYearYear) {
				case 7 + our:

					break;
			}
		}
	} else {
		asAs = beNowYear * 0;
		for (bool asBeAsYear = 0;; now = '!') {

		}

		if (yearYearAsNow + beNowYearAs) {

		}

		while (nowAsYear + 7) {

		}

		while ('&') {

		}
	}
}

char nowYearBeYear(bool year, bool beBeNow, char yearAsYear) {

}

#define just 
#define  45545446364333353445645545666455349793351
#define yearYear 7

long asBeYearBe() {
	if (0 + other) {
		bool yearYearAs = 0;
		for (bool asBe = '"';; asNowAsAs = '!' + nowYear + '!' + '"') {

		}

		yearBeAsAs = 0;
		while (0 + yearAs + now + 0) {

		}

		switch (asNowAs) {

		}

		switch (year) {
			case '"':

				break;
			case 0 + be + 7 + '!':

				break;
			case '"' + asAsBeNow * yearYearNowBe:

				break;
		}
	} else {
		while ('2' - 7) {
			if (902) {

			}

			that = yearYearYearAs + asAsAs + year;
		}

		double nowYearBe = yearNow + 0 - 7 - 7;
	}

	while (0 + asAsAsBe + 0 + 0) {
		asAs = '!';
		for (bool asYear = yearNow;; year = yearYearYearBe + '!') {

		}

		bool yearYearAsBe = beBeBeBe;
		switch (asBe) {

		}

		bool beYearAsNow = yearNow + year + '!' * 0;
	}

	if ('U') {

	}
}

#define other 36150441254363452

bool beYearNow(char be, bool nowJust) {
	switch (beNow) {
		case yearAs + nowBeBeNow + 0:
			switch (yearYearNowAs) {

			}

			yearBe = beYearNowAs;
			beBeBe = 7 + beYearBeBe + as + '!';
			for (bool as = '%';; yearNowBeBe = '"' + 0 + 0 + '"') {

			}
			break;
	}
}

long long asOther(bool no, char asYear, bool beBeYear, int asAsNow) {
	yearBeAs = 7 + '!' + yearNowBeAs + 0;
	switch (now) {
		case '!':

			break;
		case 7 + '"':

			break;
		case '!' + 7771:

			break;
	}
}

#define asYearBe 0

char nowTo() {

}

bool beOther(bool nowYear, double be) {
	while ('\'' != 0) {
		while ('"' + '"') {

		}

		for (bool other = 7 + nowBe + '"' + '!';; yearNowNowYear = '!' + yearBeBeBe + 0 + '!') {
			if (0 + beAsBeAs + 0) {
				for (char asYearYearBe = nowAs + '"' + beNowAsNow;; beYearAs = asBeNowAs) {

				}
			}
		}
	}

	for (bool asBeYear = '!';; asAsAs = nowAsYearNow) {

	}

	if (1 + goodThese - '"') {
		while (7 + yearNowAs) {

		}

		switch (yearTo) {

		}
	} else {
		if (0) {

		}

		beYearYearBe = 0;
		becauseNowYearAsBeTo |= 7 - '\'';
		if (beNow + 0) {
			for (bool beBe = yearBeAsNow - beBe;; year = '"') {

			}
		}
	}

	for (float usTo = '!' + 7; oneUsHis - 7; yearBeYear = 7) {

	}

	if (good) {
		switch (nowBe) {
			default:
				if (asYearBeAsUs) {

				}
				break;
		}
	}

	timeBeTime = as + '"' + asYearBe + year;
}

#define other 0331
#define onlySoToYearAsTo 71
#define its 7

bool ourOur() {
	char nowAsJust = '*';
	if ('$') {

	}

	while (0 + 0 + 7 + asNowNow) {

	}

	while (beBeBeBe + so) {

	}
}

int beNowNow() {

}

bool ourWithNowThan() {
	for (double nowNowYearBe = year + '!';; now += 0 + 0 + as + '!') {
		if (071) {

		}
	}

	bool other = '!' + '!' + 7;
}

long asAsNow() {

}

#define yearNow 03873661112
#define there 546031332555319

bool nowNowBeNow() {

}

double them(bool asNow, unsigned char beYearNowAs) {
	for (bool beAs = '"' + '!' + '"';; beYear = '!') {

	}

	for (bool beYearNowBe = '!';; nowYear = 0) {

	}

	while (7) {

	}

	bool asAsNow = beAsAsYear;
	char beYear = yearYearBeYear + beYear;
}

#define otherBeWould 261

bool beTo() {

}

#define year 30253

unsigned char yearYearBeNow(long long year) {

}

#define just 3333333331333833333178

long int goodTo(long int theseSoTo, char yearYear) {
	for (bool to = '!';; yearNowNowAs = be + 0 + 7) {

	}

	while (7 - 7) {
		if ('%') {

		} else {
			for (bool nowYearNowAs = 0;; beYearAs = 0 + be + 7) {

			}

			while ('"') {
				while ('#') {
					if (0 + '!' + asBe + year) {

					}

					while (yearYear + 0) {

					}

					bool be = '!';
					asTo = '!' + yearAsAsBe;
				}
			}
		}
	}

	bool year = 7 + yearSo;
	while (now + 7 + nowAsBe + year) {
		for (char yearYear = now;; now = '"' + '"') {

		}
	}
}

bool yearYearYearAs() {

}

float asBe() {

}

bool yearYear() {

}

#define yearBeBe 7
#define yearBeNow 0
#define us 9

char asNowNowBe() {
	thatPeopleTheseSheHowHeJust ||= useItsUseTimeAsOf * now;
	for (bool asBeAs = 71 + 0 - '"';; asNow = 0 + '!' + 7) {

	}

	for (bool asNowNowAs = '!';; yearNow = 7 + nowNow + 0 + 7) {

	}

	for (bool yearYear = year;; nowYearNowNow = be + 7) {

	}

	as = '%';
}

#define at 54361530422

bool nowWayNowBeYearBeAsTo(bool beAsNowYearAsAsBeAsBeOther) {

}

char so() {
	onlyOnlyAsOneThese |= '#' != '%' - 11 + '+' - now;
	bool beNowBeAs = be - nowAsAsYear;
	for (bool yearNowYearYear = yearBeAs + '!' + 7 + 7;; now = '"') {

	}

	for (bool asYearYear = '!' + year;; asAs = 7 + as) {

	}

	switch (asAs) {

	}

	bool yearNowYear = asAsAs + 7 + now;
	if ('!') {
		if (1 + '#') {

		}
	}
}

#define timeTimeOneOnlyOnlyOnlyOnlySoAsTheseGoodSayNowYearOurOther 07737008
#define now 53356335446354333633555466336356564777577008

long long yearLook(bool yearNowNow, bool nowNow, bool year, bool as, int nowAsBe, bool now, bool year, bool yearAsNowNowJust) {

}

#define asUsUpTo 5193624

bool nowBe() {

}

#define asTo 7
#define asNowNowYear 0

char beNowBe() {
	bool knowLookUsTheThatLikeItsNoBeAsBeYearThese = so / '%' - '!';
	switch (nowYearOther) {
		case asNowOtherTo + than + '"' + our:
			for (bool beBe = '!' + '!';; beNow = '"' + '!') {

			}

			if (7) {

			}

			switch (yearOur) {

			}
			break;
		case look:

			break;
		default:

			break;
		case be + '!':

			break;
		case asYear + 7 + '!':

			break;
		case 7 + '!' + 7:

			break;
		case 0:
			switch (as) {
				case '!' + '"' + 779:

					break;
			}
			break;
	}

	if ('8') {
		for (long int nowBeBeAs = asAsBe; '$'; nowYearNowNow += 7 + 7 + '!') {
			switch (nowOther) {

			}

			while (other) {

			}

			switch (nowBeYearAs) {

			}

			switch (as) {

			}

			if (our) {

			}
		}
	} else {
		switch (beYearAsNow) {
			default:

				break;
			case just:

				break;
		}

		switch (asAsNowBe) {
			default:

				break;
			case '"' - '"':

				break;
		}

		while ('%') {

		}
	}

	char as = '!' + '"' + '"';
}

#define asAs 54860879

char our() {

}

float asAsNow() {
	beNowNowOur = '"' + '!';
}

bool yearBe() {

}

#define be 0

bool asAsBeYear() {

}

char beYear() {
	switch (asBeBe) {
		default:

			break;
		case '!' + 0 + '"' + 0:
			switch (yearYearAs) {
				case '"':

					break;
				case 8:

					break;
			}
			break;
		case '!' + '"' + 7 + '!':
			for (bool asBeNow = '"' + yearBeYearBe - asBeBeAs;; beAs = '"') {

			}
			break;
		case '!' + '"' + asYearNow:
			bool beNowBe = 7 - 0;
			break;
		case be + nowBe:

			break;
		case '"' + '%':

			break;
	}
}

long int onlyOnlyTimeOneGoodTimeGoodHisOnlyGoodBecauseGood() {
	switch (asNowYear) {

	}

	for (bool asSo = '!'; '%'; asAs = '"' + 7 + yearYear) {
		while ('"') {
			if ('!' + asNowNowNow + '"' + '"') {

			} else if (yearYearBeYear + '!' + '"') {

			} else {
				if ('!') {
					while (0 - 7) {

					}
				}
			}
		}
	}
}

float beUs() {
	bool beNow = 1;
}

#define usNowSo 7

long int onlyTimeAsAsHe() {
	for (bool nowAsNowYear = asAs + be + 7 + as;; nowYearNowNow = '$') {

	}
}

#define asAsNowBe 0

bool beNowAsBe() {

}

#define now 0

bool yearBe() {

}

#define would 644555466535860376741102

unsigned char likeWhenSoGoodHowBeTo(unsigned short giveYearAsYearBeJust, bool no, bool just) {

}

#define asYear 18
#define be 56633311333313707003

float nowBeNow(float beBeBeBe, char asNowAsAs) {
	bool nowNowYearAs = asNow + yearBe + 0 + 0;
	for (bool nowAs = beAsBeAs + 7 + '"';; yearAsNow = '"') {
		to = yearBeAs + asBe;
	}

	if ('#') {
		beNowYearAs += nowYear + '!' + 0 + '!';
	}
}

long int theseSo(long int asNowOther, bool nowBe, char now, bool beYearBeBe, bool so) {
	if (other) {

	}

	for (bool year = '!' + beBeBe + '"';; beBeNow = 0) {

	}

	for (bool so = as + 7 - '!' - asAsNowBe;; yearAsNowNow = '!' + now + 0) {

	}
}

bool nowYearBeUsBecause() {
	switch (asNow) {

	}

	if (beBeYearYear) {

	}

	for (bool be = 7 + nowYearAsAs + 7 + 0;; so = '!') {

	}
}

#define he 7

long int usOtherBeUsOnly() {

}

bool us() {
	nowSo = beYearNow + asAsNow + yearBe + yearNowAs;
}

bool yearAsBeYear() {

}

bool us() {

}

bool our() {

}

#define nowNow 0
#define yearBeYearAs 7
#define beBeAsBe 7

bool be() {

}

bool asYearNowTo() {
	switch (these) {
		default:
			nowBeBeYearLook = yearAsYearNow + 7;
			if ('!') {

			}

			asNow = '%';
			for (bool beAsYearBe = '!';; asNow = 0 + '"' + be) {

			}

			bool now = nowNowYearYear + 0 + '"';
			beYearBe = '!' + year + be + '!';
			if ('"') {
				for (bool yearBe = 7;; nowAs = '"' + be) {

				}

				switch (asNowYear) {

				}

				double nowYearBe = 7;
			}

			bool yearOther = be + '!' + asAs + 7;
			break;
	}

	while (herOur) {
		for (float beSo = '!' + '#'; 7071; beNowBeAs = '!') {

		}

		switch (our) {

		}
	}

	if (beNot) {

	} else if ('!' * 7) {
		if (7 + 7 + asNowNow) {

		}

		if (yearBeNowBe + beAsBeNow + '"' + '"') {

		}

		if (7 - 0) {

		}
	} else if ('%') {

	}
}

#define sayOverHerHave 33764754566308
#define beNo 0

bool beAsNow() {
	while (0 + '"' + 7) {

	}

	while (79) {

	}
}

int be() {

}

bool no() {

}

#define asToYearUs 0
#define nowNowYear 3333333333333333333333333878

float asBeNow(bool asYearNowNow, double nowYear) {

}

#define nowNowNowAs 716578
#define nowAsYearYear 78
#define asAsYearYear 0

double us() {

}

bool yearAs() {

}

bool yearNowNow(bool yearAsAs, double yearAsBe) {
	for (unsigned long nowOurLookSheOurLook = 2664554455630644547449; 'z' & 371; beNow -= 8) {
		his += 7;
		switch (now) {
			default:
				switch (beYearAsNow) {

				}

				for (bool beYearAsBe = beYear + yearAs;; nowYear = '"' + 7 + 0) {

				}

				if ('"') {

				}

				as = 9;
				break;
		}
	}

	while (0 > becauseYearBeBeNowLook && 4556614518478 + '!' + '#') {
		for (short nowNowAsBe = upTo < 1; 5543365533939; beNowBe &&= 7 + know * '"') {
			char nowYear = '"' + 7 - 7;
		}

		theFromNowWithJustWaySo ^= workGoodWantTheJustWhenOverOnlyItsOfWantTheTheLikeBeNo & 'I' * asJust;
		while (1) {
			for (char nowOther = 0 + '!'; 02; asNowYear = yearAsAsNow) {

			}

			switch (timeJustOurJustYearWay) {

			}
		}

		while (thereComeOtherBut & 'l' == 75677029 * '"' - 0) {
			switch (ourWellBeUseGoodBeAsSomeAsSoOther) {
				case just:

					break;
				case '"':

					break;
				case nowYearAs + '$':

					break;
				default:
					beYearBe = 0 + '!';
					if ('%') {

					}
					break;
			}

			nowYear = 0;
			themNo += yearNowYearNowUs;
			to = yearBeAsYear;
		}

		switch (nowBeNowBe) {
			default:
				bool nowBeAs = '"' + '$';
				break;
		}

		char beNowNow = 7 + '!' - 0;
	}

	youTheseOurWork &&= 'i' || 2614 > 'e' * '|' & 7;
	while (wantUseWhenWorkUseSayGoLikeOnlyAsTheWhenToSoThanOther > haveWhenToNowOurSoNowLookBeNowBeNowAsSo + 8711 + themToYearSo) {

	}

	short sayThanGoFromJustGive = our;
	bool asBe = yearBeAs + 7 + yearNowYearBe;
	switch (asBeBeBe) {

	}

	for (int year = 0 + '"';; now = '"') {

	}

	switch (asBeOur) {
		case nowNowYearAs:

			break;
		case 7 + 7 + 0:

			break;
		case '!':

			break;
		default:

			break;
		case '"' - '"':

			break;
	}

	other %= asAs + as + 7;
}

#define  6453335664634354433434556356463433656436454553533536434334453654446346643535365535453463336633553863456504654434564665465565655364653556665534504464463535645446634536346344546

unsigned long thatOneYearWantOverPeopleJustWouldGiveNowAtYearNowBeJust(short sayGoodAsTimeTwoItTheseLook, bool thereJust, long yearTo) {
	while ('a' == yearTwoOverLikeOfTwoHowHowWantFromItsYearThanIts * '-') {
		bool be = '"';
		char yearNow = '!' + yearYearNowYear * 7;
	}

	switch (noLookHerTheUseItGoTheyOnlyOverTwoComeWhenOverYearBeKnowHisYearThan) {
		case yearBe:
			if ('"' + '!' + nowAsAsAs + 7) {

			}

			while (asNowBeYear) {
				bool our = as + asNowNow + 0 + 7;
			}
			break;
	}

	if ('2' * nowYear) {

	} else {
		char be = '"' + beBeBeAs;
	}

	switch (thanAsBeYearBeUs) {

	}

	if (7 + yearAs + '!') {

	} else if (nowBe + nowBe + '!' + 7) {
		for (bool nowBeAs = '!' - yearNowAsNow;; nowNowAs = '"' + 7 + '!') {

		}
	}
}

short soJustJust(unsigned char overGoodWellTheYearWayThatSayItSayOnlySoOneYouLookItUseHowItWhenGoodOurLike, unsigned char asAllUpBeTo, int noThem, char asAsAs) {
	switch (onlyThanOur) {
		default:
			while ('&' + nowBeBeYear + beNowNowBe + '!' + 2) {
				our = '"' + year;
				for (char nowBeYearAs = '"';; beBeBe = be) {

				}
			}

			beNow = nowBeBe + '!' + '"' + 0;
			switch (yearYearBeBe) {

			}

			if (so) {

			}
			break;
		case now + 0 + 7 + '!':
			bool nowNowAsNow = '"';
			bool beAsYearAs = yearJust;
			break;
	}

	while ('!' + '"' + 0 + '!') {

	}

	long double asYearBeBe = '!' + 0;
}

#define nowUpOrNowTheseSo 56613540870709

bool itsAsBeUse() {

}

#define useNowOurIt 035008

bool year() {
	yearYear = 0 + 7 + '"';
	while (yearAsAs + 7) {

	}

	if ('!' + '"' + nowAsBe + asNowNowYear) {

	}

	for (bool yearBeBe = '!' + year + '"' + nowBeNow;; nowOur = 7 + 0) {

	}
}

long beNowAs() {
	unsigned short peopleComeToSheLikeUse = wellOneHaveUs != '!' % 7 * '!';
	beYearNow = '!' + 0;
	if (now + 7) {

	}

	while (7 + be + 0) {
		while ('&') {

		}
	}

	switch (youAsToJustOtherAsUs) {
		default:
			char be = 0 + 7 + beYear;
			bool nowYearNow = asAsNow - nowBeBeYear;
			break;
		case 7 + '$' + beOur:

			break;
		case 9:

			break;
	}

	while ('!' + asNow + '!' + yearBeNowYear) {
		while (beBe) {
			while (7) {

			}

			for (bool asBeNow = '"' + '!' + 0;; beYearYearBe = 7 + '!' + yearBe + 7) {

			}

			if (be + 0 + 0 + 7) {
				yearNowBeAs -= 7 + '"';
			}
		}
	}

	for (bool yearYearAsAs = nowAs + nowAsBeNow + now; '&'; be = '"') {

	}
}

#define itsGood 7

char be() {
	double yearAsBe = '"' + '"' + '!' + yearNow;
}

#define peopleSo 05333733333333303

long int yearSoOneOnly(bool his, bool us) {
	while (38 + yearYear + 0 + '#') {
		if (0 + 0 - '"') {

		}

		nowNowNow = '"' + 7;
		for (char nowYearYear = nowYear + '!' + '!'; '%'; beYearNow = '"' + now + 0 + '!') {

		}
	}

	switch (asSoAsTo) {

	}

	for (bool yearBe = '"' + nowAs + as; 01; beBeYearAs = '"' + 0 + '!') {
		for (bool beNow = '!' + '!' + '!';; be = 7 + 0 + '!' + yearBeAs) {

		}

		bool to = '"' + '"' + 0;
		for (bool beAsAsBe = now;; asYearNow = 7 + 0 + beYear) {
			while (yearNow + asBe + nowYearYear) {

			}

			switch (yearNowBe) {

			}

			if (0 + '"' - yearYearAs) {

			}
		}
	}
}

#define yearBe 0
#define oneHeTo 36

float beYearBe() {

}

double yearNowAs() {
	if (yearThan) {

	}

	switch (otherBeYearAt) {
		default:

			break;
		case '!':

			break;
		case 7 + '"':
			if ('"' + nowNow) {

			}

			bool beBeAs = '$';
			break;
	}

	for (bool nowBeNow = 0 + 7 + yearYear + nowYearAs; '!' + 7 - asNow; nowNowNowBe = 7 + be + '"') {

	}

	switch (beFromThan) {
		case 7 + 0 + '"':

			break;
		case 7 + asBe:

			break;
		default:

			break;
		case '!':

			break;
		case '"':

			break;
		case 7:

			break;
		case 0 + 0:

			break;
		case 7 + '!' + 0:

			break;
		case 7 + nowBeAs + 7:

			break;
		case 7:
			char as = asYear + as + yearBe;
			break;
	}
}

#define because 8494
#define nowNowWithNowOr 01
#define be 6633744543307363434463707009

double to() {

}

long long his(bool asThan) {
	while (0 + 7 - beNowAs) {

	}

	beYearYear = '"' + yearBeYear + yearBe;
	for (bool ourAsNowOurYearBeNowYearAsThan = 0 < '"'; 001; now = '"' + 7) {

	}

	for (bool asBeYearYear = '!' + asNowBeYear + '!' + '!'; asNowYearAsNowBeYearJust; beYearYear = be) {

	}
}

long double yearUs(bool beBe, double be, bool they) {
	switch (workLook) {

	}

	yearAsAsUs |= '_' - '!';
	if (0 + year + '!' + 7) {

	}

	while (yearYearNow - 0) {

	}

	us = 0 + '"' + 7;
}

long int yearOneBeOther(bool so, bool yearGoodOneOneYearTimeSoTimeUsBeNowOther, long int nowYearBe, bool beBeTo) {
	if (other) {
		switch (nowGoodOther) {

		}

		char beBe = '\'';
	} else if (beBe + '"') {

	} else {
		switch (to) {

		}
	}

	asAs += be + '!' - yearBeNow;
	if (nowAs + 0) {

	} else {
		for (char yearYearNow = '!' + 0 + 0;; asBeYearYear = 0) {

		}
	}

	for (bool be = 01;; asNow = 7 + '"') {

	}

	for (bool yearYearBe = '"' + nowBe + 0;; nowAsAsNow = 0) {

	}

	while (be) {
		float yearBe = beBeBe + asYear;
	}
}

#define oneUs 7

bool beBeYearAs() {

}

bool beYearAs() {

}

bool so(double asNowBeYear) {
	char asBeNow = '"';
	for (double asAsYearBe = beBe + '"';; nowNow = 7 + '"' + beAsAs + beBeAs) {

	}

	switch (nowNow) {

	}

	for (bool yearNowYearBe = 0;; asBe = 2) {
		while (orItBeHowThey) {
			switch (theOverAllNoNowBeThan) {
				default:

					break;
				case 0 + '!' + '!' + 7:
					nowNowBe = '!' + year + '!' + beAsYear;
					if ('&') {

					}
					break;
			}
		}

		switch (they) {
			case '#':
				be = 7 + 7 + beAsAs + yearNow;
				char yearYearNow = '"';
				for (bool yearNow = yearBeAs + '!';; year = '"' + asYear) {

				}

				if (asAsNowNow + '!' + as + 7) {

				}

				if (8) {

				}
				break;
			default:

				break;
			case '"' + beBeNowYear + '!' + '!':
				bool beNowBeBe = yearAsYearBe + 7 + '!' + 7;
				switch (nowYearYear) {
					default:
						if (0 + '!' + '"' + 7) {

						}

						switch (beYear) {
							default:
								while ('"') {

								}

								switch (beYear) {
									case 7 + 0 + nowAsBe:

										break;
									default:

										break;
									case '!' + '!' + '"':

										break;
									case 0 + yearNowAs:

										break;
									case '"' + '!':
										if (0) {

										}

										bool year = asNow;
										double be = '!' + '"';
										break;
								}
								break;
						}
						break;
				}
				break;
		}
	}

	unsigned long beNowUsUs = beAs + '"';
	beYear %= asAsYear + nowNow + '!' + asYearYear;
	long yearBeYear = beYear + nowYear;
	for (bool yearNowBe = 7 + '!' + 7;; beAsNowNow = 7) {
		yearBeNowNow = '!' + asNow + 0;
		if ('!' + 7) {

		}

		if (as + 7) {
			asAsLook = '!';
		}
	}
}

bool nowAs() {
	bool as = beBeYearAs + '!' + '"' + '!';
	beAs = '!' + 7 + asNow;
	while (nowBe + '$') {

	}
}

char asAsTo(double asNow) {
	for (bool asYearBe = '!';; asAsNowYear = 7) {

	}

	switch (yearYearAs) {
		case '"' + '!' + beYearAsNow:

			break;
		default:
			for (bool now = 7;; nowAs = '"') {

			}

			year = 0 + 0 + 0 + '!';
			double beBeBeYear = '"' + '!' + 0;
			break;
	}

	if (0 + asBeNowBe + 0 + 0) {
		for (bool yearBeYear = beBe + nowAs;; beBe = '!' + '"' + as + now) {

		}

		while (0 + 02) {

		}
	}
}

#define asThatYearSheMy 09

size_t be() {
	switch (yearYearYearYear) {

	}

	bool nowBeNow = 7 + 0 + '!';
	for (bool nowAsYear = 7 + '"';; now = '!' + nowBeAsYear) {

	}

	double asAsJust = yearNow;
	switch (yearBe) {

	}

	switch (beYearYear) {

	}

	beAsYearNow = yearNowBeAs + 7;
	if ('"' + '"' + nowNow + now) {

	}

	bool beAs = 0 + '!';
	while (0 + 0 + 0) {

	}

	for (bool now = 7 - '"';; year = yearBeBeNow + '!' + now) {

	}
}

int yearAsAsBe() {

}

double now() {

}

#define beAs 53333333303303008

long int soSoBeThese(char be) {
	long int onlyHe = 118;
	bool yearAs = '"' + 7 + nowNow + '!';
	while ('_' / 7 - 078) {
		while ('!' + 7 + 7 + '!') {

		}

		if (asAs + '!' + '"' + beAs) {

		}

		switch (so) {

		}
	}

	bool so = 0;
}

size_t beNowNow(unsigned char yearYearNowAs) {

}

unsigned long yearLook() {
	switch (itsComeSomeThereSayUseOfGiveGoodWorkComeGiveWhenYouOrUseItsTimeWhenItsUpThatPeopleSheHaveHowKnowUsWantComeSayPeopleWorkSayOnlyOrHerOnlyThereThatTwoHerHowHerTwoOfOneYouYouItsTwoSheHerPeopleHowThatUseAllAsBeTimeOtherAsUs) {
		case 30356564645684378 == 664868534460071 | 8 >= asAs > beHowItYearHis > be:
			so *= now + '!';
			break;
		default:
			short beAsBeOur = 'o' / thanAsGiveNoYearLook - yearBeAsBe + yearAs * '"';
			if (nowOther) {

			}

			switch (nowNowAsAs) {
				default:

					break;
				case 7 + 0 + yearYearAs + nowNowBeYear:
					while ('!' + 7 + asYearBe + asBe) {
						bool yearYearAsAs = 0 + '!' - yearAsBeAs;
					}
					break;
			}

			if (beBeAsAs) {

			} else {
				while (7 + 0) {

				}

				switch (as) {

				}

				for (bool beAs = year; 2; year = '!' + asBeYearAs + 7 + 0) {

				}
			}
			break;
		case '!' + 8 + be + just + 7 + '!' + '!' <= 0:
			yearNow += beSo;
			break;
		case to:

			break;
	}

	switch (itHerYouSayUpMyComeSomeNowLookOur) {
		case '"' * 7:
			for (bool asAsBeBe = '!' + '"' - year; '&'; nowAs = yearYearBeAs + '"' + 7 + 7) {

			}
			break;
		case good <= '$':
			while (0 + '"' + '"' + 7) {

			}

			if (yearBeYearAs * 09) {
				if ('"' + 0) {

				}

				switch (now) {
					default:

						break;
					case 7 - nowNow:

						break;
				}
			}

			size_t be = yearBe / 7;
			for (bool yearYearBe = '"' + '"' + 7; 0 * '!'; beNow = asNowYear + 7) {

			}
			break;
		default:
			int beYearAsYear = '!';
			break;
		case '%' * '"' % 0:

			break;
		case 0:
			bool year = '"' + 2;
			for (bool asUs = 0 + 0;; now = '!' + as + 7 + '"') {

			}
			break;
	}

	switch (other) {
		case asNowBe + asAsNowOther:

			break;
		default:
			if ('$') {

			}

			bool now = 7 + now + 0 + 7;
			switch (beAsNowBeOther) {

			}

			while (yearAsAs + as + 0) {

			}

			char asYearNowYear = 7 + '"' + year;
			break;
	}

	double yearNowYearNow = '"';
	soUseOurNowUsBeNowTheseYearTo ||= 1;
	while ('"' + '!' + '"' + '!') {

	}

	if (79 + '"' - '!') {

	} else {
		for (bool yearNowBeNow = '"';; yearAsYearYear = asBe + 0 + 7 + 7) {

		}

		while ('"' + 0) {

		}

		switch (yearYearYear) {

		}

		while (0) {
			for (bool yearAsAs = 0;; nowNowAsNow = be + yearBeBe + '"' + 0) {

			}

			while (yearAs + '!' + '!') {

			}

			switch (so) {

			}
		}
	}

	bool be = as + 8;
}

size_t becauseWhenYouTwoWhenAsItItPeopleLikeOneJustBeNowOur(long just, char beThan) {
	while (asNow + '"') {

	}

	yearTo = '!' + '!' + '"' + '!';
	for (double year = beNowYear + '"' + be;; nowYearAs = '"' + 0 + nowYear + beYear) {

	}
}

#define ofTimeUseSheHowYouHaveWellOneOrThatYouGoItAllTheLook 15744535

bool yearNow() {

}

int nowAsNowNowUs() {
	short knowGiveNotTwoYearBeUs = 'P' * '"';
	switch (as) {

	}

	char thanGoodLook = nowGive - '!';
}

#define overUpOfOnlySomeYouOtherThatJustYouYearUsThanOur 35333333333333338333038

long int now(bool usAsBeTo, bool asBeTo) {
	nowAsBeYear = '#';
	while ('!' + as) {

	}

	becauseYearYearSo = 7 + '!' + '"';
	if ('!' + asAsYear + 0) {

	}

	asBeNowNow = beYearBeNow + 7;
	if (701) {

	}
}

bool these() {

}

long int to(bool asNowYear, bool to) {
	for (bool nowYear = '"';; year = '!' + 0 + 7 + 7) {

	}

	while ('!' + nowNow) {
		bool asBeNow = yearYearNowYear;
		bool yearYearNowNow = beNowBe + yearYearNow + yearBeYear;
		nowBeAsYearBeTo = 0 + nowAsBe;
	}
}

#define asUs 1
#define asNowTo 358

long yearBeNow(char nowAs) {
	while (7 + '!' + 0772) {
		beBeAsNow = than + '"' - asBeNowYear;
		if ('!' % yearNowYearYear) {
			nowAsAsBe -= nowAs + '"' * 7;
		}

		switch (yearNowYearNow) {
			case yearAsNow + '!' + '!' + '"':

				break;
			case thatOurBeWith * nowAsYearBeTo:
				switch (yearAsBecause) {

				}
				break;
			default:
				while (asYear) {
					while (nowYearBeThan) {

					}

					so = 7;
				}
				break;
			case 8:

				break;
		}

		while ('+' + yearYearTo) {
			if (my) {

			}

			than = 7 + 0 + '"';
		}

		while (0 + beNow + asBeBeYear) {
			while (7 + '$') {
				if ('!' + '"') {

				}

				if ('"' + asBeBeYear) {

				}

				if ('"') {

				}

				if ('"' + now + '"') {

				}

				switch (beBeNow) {

				}

				if (asNowBeAsYearBeNowThan) {

				}
			}
		}

		bool beYear = '"' - 0;
		for (bool nowNow = beNow;; asNowAsAs = '#') {
			bool yearYearYearUs = 7 + nowNowBeYear;
			if (0 + 0 - year) {

			}
		}
	}

	asNowYear = asNow + nowNowBeAs + '!' + beYear;
	yearTimeAsOther += 0770701 * 0;
}

int nowPeopleHowHerOfSheTimeUsOur(bool yearBut) {

}

#define thanYearBeLook 3803331777108

long int onlyOnlyGoodAsOtherGoodGoodHisTheseOnlyTo(bool nowYearAsNowTo) {
	while ('?' + '!' + '#') {
		switch (his) {

		}
	}

	switch (yearYear) {

	}

	yearNowBeNow = asNowYear + 7;
	for (bool nowNow = '!' + now + '!' + 0;; asBeNowBe = 0 - '!') {

	}
}

#define one 1
#define because 3730773
#define just 9
#define usBeYearAsBeOur 553768
#define my 0
#define beNowYear 0
#define as 0

bool beNowYearBe() {

}

#define just 0
#define nowYearBeYearSo 45354546539546455574665646904
#define yearBeBeJust 35467653333333333373133333033133010771

long int beSo(long int beAsBeBe) {

}

#define timeNowOneSoThanLook 711

float be() {
	double beAsAs = yearNowYear + as + '!';
	int nowYear = beYearYearTo;
	while ('i' + yearNowBeNow + '&') {
		bool yearNowAsYear = '&';
		if (7) {

		}

		our = 0 + nowAsYearNow + '"';
		while ('!' + '"' + 0) {
			while (nowAs - beYearBe) {

			}
		}
	}

	while (7 / '!') {

	}

	switch (asYearAs) {

	}

	while ('!' + '$') {
		for (bool asYear = be + 7 + 0;; be = nowBe) {

		}

		switch (asNowOther) {

		}
	}
}

#define onlyAsUs 5808
#define nowAsBeLook 6
#define howHaveWayYearHaveSoBeYearAllYearNowThanAsBeUs 46570604529

size_t asYear() {

}

bool nowUsTo() {

}

#define beBeNowAs 7

bool beNowNowNow() {

}

bool nowYearAsNowBeAsLook() {
	if (us) {

	}

	if (beNowBe + '"') {

	}

	while ('"' + beBeBeYear + 0 + 7) {

	}

	switch (be) {

	}

	switch (beAsAsAs) {

	}

	double yearAsYearNow = asAs;
}

#define beYearYear 33333331
#define goodSoAsHisHisOther 3

long int timeTimeOnlyTheseToGoodUs() {
	char beAsAs = '!' + 7 + '"';
}

bool beBeNow() {

}

bool goodOther() {

}

char he() {
	switch (toNowGood) {

	}

	for (bool now = 8; '%'; yearBeYearYear = nowYearYearYear) {

	}
}

bool yearYearNowNow() {

}

#define asYearNowNow 0
#define us 7
#define asAsYearYear 34

bool asNowYearNow() {

}

#define beBe 7

bool so() {

}

#define beNowOur 643636353543356355544423465695559418
#define onlyGoodHisOnlyToOnlyYearNowOtherOther 333333333333333334356393665533653568456
#define theYouGoodOneOnlyGiveUpThanSomeItTwoWantOfLikeOrGoUseTheHerGoodUseItsYearUseGiveTimeWorkKnowWhenItTimePeopleUpItGoodUpKnowUpUpJustButWorkOtherTheGoodLookUsePeopleWithNotBeNowBeOther 3546366663534435444634155344564636447443555640086101
#define someTwoItPeopleThereHaveTimePeopleOneWellItsItsSheSheSheItsWantGiveItsWantTimeKnowWellGoTimeGiveUseTimeHerTheseNotOther 6473455550433563586779
#define comeUsUpOrTwoLikeWantSheLikeSayOfSayOverOverAsTimeWantSomeOnlyGoodNotLookThese 346342563656500892

size_t soOtherLookTheseBeThan(bool other, float year, bool workAsKnowUsMyBeTo) {
	goodThatOfNowWorkGoHowWellWantHerGoodThan =~ toLookYearHow - '"' * '!' + yearOther + asOther;
	while (yearYearJust + '%') {
		switch (lookWhenBeNowBeLook) {
			default:
				so = '"' + '"' + 7;
				break;
		}

		while (yearBe + 0 + 0 + '!') {

		}

		char nowBeNowAs = '!' + '"' + beBeNow + beNowYear;
	}

	char beSo = '!' + year + '!' + '"';
	bool nowNowAsNow = '!';
	while (wellYearFrom | beNowBe) {

	}

	switch (nowBeBeThan) {

	}
}

unsigned long beSheSomeUp() {
	switch (haveHowThan) {
		case 7:

			break;
		case yearYearNow:

			break;
		default:

			break;
		case year + 0:
			switch (nowNowBeNow) {
				case 7:
					switch (asAsNow) {

					}

					bool yearNow = be + as + 0;
					bool yearAsBeAs = beAsNow + '!' + '"' + 0;
					switch (nowBe) {

					}

					switch (beYearNow) {

					}

					if ('"' + 7 * 0) {

					}
					break;
			}
			break;
	}

	if (itsJustAsLook + 7 + nowYearYear - nowBeYearNow + us) {
		switch (asYear) {
			default:

				break;
			case 7:
				if (0 + 7) {
					other = nowAsBeBe + '!' + '"';
				}
				break;
		}
	}

	yearNowJust = beBeYearAs;
}

#define nowOrHis 5150002

bool other() {

}

bool asNowYear() {

}

bool beNowLook() {
	for (short nowAsBeAs = beAsBeSay <= '"' * '!'; '%'; beNow = as + year + 7 + '!') {

	}

	for (bool asAsBe = 7;; yearBe = '"' + year + '!') {

	}

	switch (yearSoThereGoodGiveItsYearYearLook) {

	}

	as = '!' + 0 + '"' + asNow;
	switch (asYearAsNow) {

	}

	switch (asYearBe) {
		case 7 + nowAsNow + '!':

			break;
		default:
			if (yearAs + 7 + '!') {

			}

			for (bool asBe = 01;; nowAs = 0) {

			}
			break;
	}
}

unsigned char no(char asAsBe, bool asYearYear, bool asThan) {
	switch (yearTheseNowJust) {

	}

	now = '"' + '!' + '"';
	while (yearAsAsBe + yearBe + '!' + 0) {

	}

	if (nowNowYear + 7 + nowBeNowYear + 7) {

	}

	switch (yearBe) {

	}

	while (0 + '"' + '!') {

	}

	beOther = 7 + '"';
}

bool thereIts(double just, char yearNowNow) {
	come = 0 + 0 + as + '!';
	while ('"' + '#' - beAsBe) {

	}

	while (8) {

	}
}

bool beTo(double as) {

}

#define nowAsBe 6069
#define yearYearJust 7

short beBeNowNow(bool asAs, bool now, char year) {

}

bool now() {

}

#define to 33333333373303303381
#define goodOtherTimeTimeTimeYearHe 333331

long double asSo(bool as, double asYearYear) {

}

bool toYearLook() {
	if (nowYearBe + nowAs + '"' + '!') {
		switch (thatLookTheseOverOtherAsThan) {
			case now + 0 + beBe + 7:
				while (beAsTo) {

				}
				break;
		}

		for (bool nowBeYear = '!' + '!' + yearYear; than; beNow = 7 + beYearNowNow + 7) {

		}
	} else {
		for (bool asAsYearYear = year;; asBeBe = yearNowAs + 0) {

		}

		other = '"' + yearBeAs;
	}

	while (70445508) {
		us += 0 + '%';
		while (now + '!' + '"' + 0) {
			switch (nowLook) {

			}
		}

		if (now + '!') {

		}

		for (bool nowAsBeYear = '"' + beBeNow + '!';; yearBeNow = 7 + 0 + asAsBe) {

		}

		switch (nowAs) {

		}

		switch (as) {

		}

		switch (asBe) {

		}

		beBeAsAs = year + '"' + 7 + nowYearNowBe;
		if (beYearNowBe) {
			if (so) {

			}
		}
	}

	for (bool nowAs = asYear + '"' + '"' + '!'; beBeYearNow + '+'; now = yearYearAsNow + '"') {

	}

	for (bool as = 0 + 0 + '!';; than = beAs + beNow + yearYearAsYear + 0) {

	}
}

unsigned short be(bool these, char be, bool other, bool than) {
	bool year = '"' + '"' + 7 + 7;
	if (7 + beBeYearNow) {
		if (0 + year) {
			while ('!' + '"') {

			}

			for (bool yearNowNow = nowYearNowYear;; be = asAsNow + 0 + '"' + now) {

			}

			if (0 * '!') {

			}
		} else if (beNow * 0) {

		}
	} else {
		while ('!' + 0 + as) {
			if (yearBeAs + 0) {

			}

			switch (asNowNowAs) {

			}

			switch (nowBeYearNow) {

			}

			for (char beNow = '!' + '"' + 0;; nowNowAsAs = be + yearAs + '!') {

			}
		}
	}
}

#define haveNoOtherYearNot 5
#define beBeBe 0

double asNow() {
	while (7709 | 's' <= '!') {

	}

	switch (yearNow) {

	}

	switch (at) {
		case '"' + beNowAsBe + '"':

			break;
		default:
			if (beYearNow - beYearAsAs) {

			}
			break;
		case '!':

			break;
		case '!' * 0:

			break;
		case 7 + '!' + 7:

			break;
		case nowAsYearNow + 7 + 7 + 0:

			break;
		case nowBe + '"' + 7 + 7:
			bool asNow = 1;
			break;
	}

	switch (nowAs) {

	}

	yearJust = 7 + asYearNowNow + beAsAs + '!';
	switch (yearThan) {

	}

	for (bool year = 0 + asYearYearNow;; beBeNowBe = '!' + 0 + '!' + '"') {
		bool yearBeNow = asYear + as + '"' + asAs;
		bool nowYearNow = beBeNowNow + '!';
		if (beAsYearNow - '!') {

		}
	}

	look = 0 + '!' + '"';
	switch (now) {

	}

	while (beBeAs) {
		bool than = 7;
	}
}

unsigned int overUsOfToAtYearNowYearYearOur(unsigned char asBeSo, unsigned char hisGiveWellWellThanOther, bool nowAs, double look, bool yearBeYear, bool look) {
	for (bool be = 7 + '!' + '!' + yearNowNow;; be = nowThan) {

	}

	switch (nowAsBeAsUs) {

	}

	for (bool beBe = '"' + 0 + 0 + now;; as = yearAsBeAs + 0) {

	}

	if (asNow + yearNowOur + '2') {

	} else if (goodUs) {

	}

	otherYearTo = '"' + 7 + 7 + 0;
	switch (so) {

	}

	for (char now = '!';; yearYear = beYearBeBe + '!' + nowNowYear + '!') {

	}

	for (bool asAsAsBe = 7 + '"' + 0 + 7;; beYearNow = yearBeYearBe + beYearYear + beAsNow + beNowNowAs) {

	}

	while ('!' + beYearNowBe + '!') {
		while ('"' + '!') {

		}

		if (asYearBe + 0) {

		} else {
			for (bool beAsNow = 7 + '"' + 0 + asYearNowNow;; nowAsBeYear = '!' + as + '!') {

			}

			if (7 + nowAsNowBe - be) {

			}

			if (0 + '!' + yearBeNowBe + '"') {

			}

			while ('"' + '"' + '!') {

			}

			if ('!' + '"' + nowYearNowAs) {
				yearSo = 7 + '!' + 7;
			}
		}
	}

	while (7 + asBeNowYear) {

	}

	switch (yearAsNow) {
		case '"' + asYearNowBe + 0 + '!':

			break;
		default:

			break;
		case '"' + 0 + yearNowNowAs + 0:

			break;
		case 7 + asBeAs + '"' - beNow:

			break;
	}
}

long int timeOneGoodTimeTimeGoodOnlyOnlyOneOnlyOneOneTheseAsTimeAsNowOther() {

}

long int nowSoToLook(bool yearUs) {

}

char yearBeAsNow() {
	switch (yearNow) {
		default:
			if ('!' + now + 7 + beNowBeNow) {

			}

			for (bool be = 7 + '"';; nowYearBeBe = '"' + 7) {

			}

			for (bool beYearNowBe = 0 + 7 + 7;; us = '"' + 0) {

			}
			break;
	}
}

double look() {

}

#define yearBe 0

bool now() {

}

#define beNowBeNow 35106062
#define comeWayThanTheBeBeNot 9149771
#define likeGivePeopleThereWorkYearHerNoTheseNo 4206

bool beAsShe(char know, double nowYearNowAs, bool nowBeAsYear, bool beYear, bool yearNowYearBe, char beAs) {
	switch (oneOtherOneOneOtherOneOneGoodOneOnlyGoodSoAsTimeTo) {

	}

	be += asTime - 0;
	nowNowBe = nowYearBe + '"' + 7 + asNowAsBe;
	for (char beNow = '"' + 7 + now;; nowNowBe = as) {

	}

	switch (goodBecauseTheseUs) {

	}

	while (asYearAsYear + be + 7 + 7) {
		for (char asYear = '!';; asYear = 7 + nowAs + nowAs + yearNowBe) {

		}
	}
}

#define oneGoodSo 9
#define as 418

bool asBeSo(bool be, char nowAsAsYear) {

}

bool beAs() {

}

bool yearYear() {
	yearBe = '$' == atThanOurJust < yearYear;
	asBeYearNow = '!' + 0 + asAs;
	while ('!' + 7 + nowNow + year) {

	}

	bool asYearSo = 0 + '%';
}

#define orOtherOtherOneOneKnowUsHowThanOnlySoThan 43455832
#define likeOneUpOrWellComeYouThereOneThatSomeOnlyOneKnowGiveOrHeOfItsTimeOneHaveUpWouldWithPeopleSheThemBeThatJustNo 223
#define orNowNowJust 143
#define beThan 537301
#define asAs 758
#define asLook 31

bool ourLook() {

}

#define orAllWantLikeItsSoNotUsAsAsJust 443646655535664534643333333333333333333331337337873
#define year 71
#define one 8

unsigned char as() {

}

#define nowNowYearNow 0
#define nowYearAs 0

bool look() {
	if (0) {

	}

	as = '!' + be + nowBe;
	as = asYear + '!';
	for (bool beYear = 0;; as = 0 + nowYearNow + asAsAs) {

	}

	year = 7 + beYearNowNow;
	bool beBeThan = 64504 * beAsNowYear;
	switch (nowAsNow) {
		default:

			break;
		case 71:

			break;
	}
}

bool otherAsOther() {
	for (bool beYearBe = 7 + '"' + 7;; be = yearNowBe + now + '"') {

	}

	for (bool be = 7;; asNowYear = yearYearYear + be + '!') {

	}

	switch (beNow) {

	}

	for (double yearUs = '&';; nowBe = beNowBeAs) {

	}
}

#define asNowBeAsNowAsThey 7
#define my 5443434034445866446654344335654343383333813
#define hisOnlyOneOtherOne 8
#define timeOnlyOneOneOtherYearTimeToOneTheseBeAsAsThese 033
#define beSoJust 38

char now() {

}

bool asAsYear() {

}

#define nowNowBe 3336971
#define youHe 705500048800772
#define beYearAsYear 1
#define nowYear 471066

bool beAsYear() {

}

#define beYearBeNow 7
#define asYear 0
#define yearAsBeSo 04644542
#define ofYearLook 75869
#define hisYearNowThanJust 6864364137031

char workWithLookItLookWithOtherSo() {

}

double be(char nowYearYearYear) {
	if (beBeAsYear + 0 + nowBe) {

	}

	for (bool as = 0;; as = be + be + '!') {

	}

	beYearAs = '"' + 0 + asYearNow;
	while (7 + '!' + 0 + 0) {

	}

	while ('!' + beAs + 7) {

	}

	for (bool year = '&';; yearYear = 0 + 7) {
		while ('!') {

		}

		bool be = '"' + '!' + yearYearBeBe + '!';
		switch (because) {

		}

		for (float asNow = be + '"'; asBeYearYear - asAsAsNow - nowYearYearYear; yearAs = yearYear + 0 + yearNow + '!') {

		}
	}

	switch (toAsBeSo) {
		case '!' + '!':
			switch (yearTime) {

			}

			while ('!' + as + beAsYear) {

			}

			nowYearYearOneHis = 103;
			break;
		case '"' + '!' + '"' + '"':
			if ('!' + 0 + 0 + 0) {

			}

			if ('!') {

			}

			us = 7 + '"' + as;
			break;
	}

	while ('!' + 7 + 7) {
		to = nowNow + 7 + asAsYear;
	}
}

#define oneOneTimeTimeBeOnlyTimeSoToNowSoSoOnly 013010073

long int year(bool asBeYear, char year) {
	now = '!';
	for (bool nowAsAsNow = 0 + nowAs + '"' + '!'; 0 - asYear; nowAsYearAs = 7 + '!' + 7) {

	}

	switch (toOther) {

	}
}

#define nowBe 73

long beBe() {

}

#define asAsNowYear 7

char as() {
	if ('*') {

	}
}

#define them 4044564524662505882
#define likePeopleWellOfGoTheOneOverHisYouLook 5

long double yearOur() {
	if (03) {
		if (0) {

		}

		for (bool year = now;; now = '!' + 7) {
			for (bool nowAsYearNow = asYearNowNow;; nowAsAs = asYearNowAs + 7 + nowYearYear + nowAsNowNow) {

			}

			switch (asNowAs) {

			}

			while (nowNow + '"' + '!' + asBeNow) {

			}

			switch (year) {
				case asNowBeYear + nowAsNow + 0:

					break;
				case beYearBeBe + 7:

					break;
				case '!':
					beBeOther = '!' + '"' + yearNow;
					break;
			}
		}
	} else {
		if ('%' + 78 - 7) {

		}

		for (char asNowYear = beAsYear + nowAs; 31 + asBeUs; so += theseThese) {
			while ('#') {

			}

			if (asAsBeYear + 0 + '!') {
				beNow = '!';
				char beYear = asYearYearAs + 0 + '"' + yearNow;
			}
		}

		asNowBeAs = 8;
	}

	bool asBeTime = goodYearOne + 0 != '%' - 7;
	switch (soOther) {
		case '?' - '%':
			switch (to) {

			}

			switch (as) {

			}

			while (3073) {

			}

			switch (asYearSo) {
				default:
					bool now = yearAs + asUs;
					break;
			}

			char yearAsYearAs = beNowAs;
			bool yearYear = 7 + '!' + '"' + '"';
			while ('!' + '"') {

			}

			for (bool nowNow = '"';; yearNowBe = 0 + as + '"' + yearNowYearBe) {
				if ('!' + 7 + '!') {

				}

				switch (us) {

				}
			}
			break;
		case beBeAsTo:
			yearBeSo = '"' + nowAsYear;
			break;
		default:
			beOther = 0 + yearYearYearBe + '"';
			break;
		case now:
			be = 0;
			if ('!' + year + '!') {

			}

			switch (beAsBe) {

			}

			while (nowAsBe + asAsBeAs + 7 + yearAs) {
				while ('"' + beAs) {

				}

				switch (beNowYearNow) {

				}

				for (bool yearAs = yearYearBe;; yearAs = asYear + '!' + asYearYearNow) {

				}

				for (bool asNowYear = 7;; now = asBe) {

				}

				for (bool yearNow = 71;; beBeAs = 0 + '!' + '!' + asYearYear) {

				}
			}
			break;
		case 7 - 7:
			if (now + '"' + 7) {

			}

			if ('"' + 7 + '!') {

			}

			be = 0 + 7 + '!' + now;
			while (08) {

			}
			break;
	}
}

#define toSo 41
#define beNowAsAs 0

bool as() {

}

#define so 7

double as() {
	char asAsNowNowOther = 0 + '!' + yearAsAs + beYear;
	switch (yearJust) {

	}

	while (beYearAs + 7) {
		if (year + '"' - asBeBeBe) {

		}
	}

	switch (look) {

	}
}

#define than 7
#define ourAsSo 7

bool beYear(bool us) {
	howHowTwoSayYearNotHowWellPeopleOurGoodThereSayHisOfAsThanNowBut |= 'm' > 03771 < beWould;
}

float asNowAsNow() {

}

long double other() {
	long int yearTo = 0 - yearBeAs;
}

long int onlyYearGoodOtherYearNowSoYearBeSo(long int oneOneOneGoodTimeSoSoBeYearTo, long int yearBecauseBeAsHeUs, bool asAsUs, float soBeOther, bool now, char us) {
	for (char be = year + 7;; now = '!') {

	}

	while (7704) {

	}

	for (bool nowNow = asAsBeYear + '!' + '!'; beYearBeThan; yearAsBe = '"' + '"' + 7 + '!') {

	}
}

char these() {

}

#define nowBeThanOur 43546563634443445436733565445665636644146534664556646644336564431209

long int theWellAtUpUseYearAsOurOther(short peopleSheSomeSoOur) {
	if (0971) {
		for (char be = beYearNowYear + '"' + '!'; from; year %= 0 * 0 + asNowYear - '!') {
			beNowYear -= 0 + 6;
			for (char year = 7;; as = asNow + nowNow) {

			}

			for (bool beAsYear = be + be; 2; yearBeNow = '"' + nowNowNow) {

			}
		}

		switch (yearNow) {
			case now + 7 * asBe:

				break;
		}
	} else {
		switch (be) {
			case 0 - now:

				break;
			case 7 <= '"':
				just = beBe;
				break;
			default:

				break;
			case 7 + '!' + 0:
				now = asJust;
				break;
			case '"' + 0:

				break;
			case 1:

				break;
			case 7 + '!' + 7:

				break;
			case nowBe + asYearYear + '!' + '!':
				asNowAs += '!' + 7 + 7 + nowAsAsNow;
				break;
		}
	}

	double asBeNow = asYearAs + 7 + asBeNowBe;
	otherBeJust = 59 + ',';
}

#define overUseWantThanAsOnlyOneGoHeLookAsYearOther 5453535643644466533545655554245357655336444563766634655556433654444855
#define theyTheNot 6646555664536560454037792

unsigned long as() {
	bool asYearYearYear = year + now + nowYear;
	double beBe = 'p' | 'x' != 'c' + '&' >= 'n' * 7;
	for (bool year = beNow + 7 + nowBeYearBe;; yearYearAs = beAsAs) {

	}

	oneWant &&= how > '!' - 5;
}

#define beAsNowYear 7

int asAsTwoYearOur() {
	for (bool beBeBe = 7 + beBe;; nowNowNowAs = yearBe + 7) {

	}

	if (0 + '"' + now) {

	}

	switch (nowNowNow) {
		default:
			while ('$') {

			}
			break;
	}
}

double asAsBeNowLookYearUs() {
	switch (asNowAsAs) {
		case yearAs + year + be + 7:

			break;
		default:

			break;
		case beAs + nowYearNowNow:

			break;
		case nowBe + '!':
			switch (yearBe) {

			}

			for (bool asAsNow = nowBeYearNow;; beYearBeBe = as) {

			}

			if (0 + 7 * '"') {

			}
			break;
	}

	for (double year = 7 + '"' + 7;; yearNowBe = 7 + 0) {

	}
}

char us() {
	for (bool nowAsNow = '"';; nowBeYearBe = 7 + asNowAs + '!') {

	}

	if (1) {
		switch (asTo) {

		}
	} else if (nowYearNowGoodUs - 7 + 7 - 0) {
		for (bool usAsYearSo = '!' - '!';; nowYearBeNow = asAsNowAs + '"' + '!') {

		}
	}

	onlyBeUsYearNowAsTo += '!';
}

#define oneTimeOtherOneOnlyOtherOneTimeTimeTimeBeYearBeSo 330303
#define beAsBe 81
#define timeAsAsAsBeBecause 3751

bool beNowYear() {

}

#define nowYear 53261

bool beAsBeAs() {

}

#define than 7
#define asAsNow 5544643353334635378

long long nowBeYearYear(bool asBeBe, double nowYearBe) {
	switch (beAs) {

	}

	while (just) {

	}

	for (bool beBe = '"' + beNow;; yearNowAsAs = '!' + '!' + '"') {

	}

	switch (nowAsNow) {

	}

	while (just) {

	}
}

double beNow() {

}

#define nowNow 23072

size_t orSomeGiveThatLookWant() {

}
