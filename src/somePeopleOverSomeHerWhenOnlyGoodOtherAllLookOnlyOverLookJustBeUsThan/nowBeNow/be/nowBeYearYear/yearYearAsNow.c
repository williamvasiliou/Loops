long int hisUs() {
	while (beBecauseBeUs - 7) {
		nowNow = 7 + 0;
		while ('"' + as + asNowYear) {

		}

		switch (beBeNow) {

		}

		year = nowUs;
	}
}

long int beBe(float these, char nowYearAsAs, bool be, bool so) {

}

#define usOtherOtherHis 31

long int asOther(bool yearBeAs, bool beBeAsYear, bool year, bool nowYear, float yearNowYear, bool nowBe, bool as, char nowAs) {

}

long int yearNow(long int hisOtherSoNowSoTime, long int giveOnlyOur) {
	asYearBeNow = beAsBe + '!' + yearAsAs + asBeBe;
	while (asAs + be + 7) {

	}

	bool our = year + '!' + 0 + asBeYearBe;
	nowNowBe = 0 + asAs + beNowNow + yearAsBeNow;
	switch (to) {

	}
}

char yearYearBe() {

}

bool asYear() {

}

#define asAsYearBe 6646636654655664666666665436636466666655546547746665778
#define onlyPeopleTheWhenWantOfYouOfHaveOnlyThatItsThatWellThatLikeLikeWorkWorkComeSomeOfUseOnlyComeTheItHerPeopleGoSheUpTimeWantHerItSayTheTheyAsAsYearYearTo 665465646364466534653464854655066346375494

unsigned char twoOfNotUpYearMyOurAsThanYearAsYearSo(unsigned int someOnlyThanYearOrAtJustBecauseUs, unsigned long asToYouOurOur, double howWhenFromSo, bool beWork) {
	unsigned char otherWouldKnowUpAll = 'h' * yearAs;
	peopleItsOtherWork = '&';
	char beBeNow = yearYearYearNow;
}

long long goHerThereWellThatBecauseSayTwoGoItsWhenGoTwoComeOfKnowUseWorkLikeWhenOnlySheOneSomeWellOverNowWellToThe(bool toYearUs, bool beYearAs, bool asBeBeNow, bool asBe, char now) {
	short nowBe = yearBeAs;
	yearAsBe -= 0 * asAsBeBe;
	if (nowNowYearYear) {

	}

	beNowAs = 1;
	if ('"' + '"') {

	}

	thanLook = '!';
}

size_t twoNoJustSo(bool useNoThan, char ourItsYearThanBeTo, bool nowYearAsYear, long long nowAsYear, bool yearOur, int nowBe, bool look) {
	if (nowJust + '#') {
		switch (just) {

		}

		if ('!' + now + '"' + '!') {

		}

		while ('!' + 0) {

		}

		long int beNow = 8;
		bool asAsAs = nowBeNow + year + year + asAs;
		beBeBeYear = 0 * '"';
	} else {
		nowNow = '!' + 0 + 7;
		if (0 + beAsNowNow + beYear + 7) {

		}

		yearNow = '"';
		bool beAsAsBeAsLook = '"' + 0 + nowYearAsYear + 0;
	}

	howOrWellOfOfSheBeUsGoodMy += beYearYear <= 7;
	switch (beUs) {

	}

	if (0 + '"') {

	}

	if ('!' + '"') {

	}

	switch (nowOther) {

	}
}

bool wayBeAsNowBeOur() {

}

#define herLook 260603501

size_t sheWhenBeNotJustBeYearSo(bool just, char as) {
	while (7 + yearAs) {

	}

	while (to) {

	}

	look = '"' + beAs + be + 0;
	yearNowBeAsTo = year;
}

#define nowWellOur 444166540743

double but(bool my, bool so, bool year, char nowYear, double beYearBe) {
	if (now < year) {

	}
}

#define lookWorkHe 7
#define beYearYearBe 0

bool as() {

}

double now() {
	while (08 + to) {
		for (bool as = asBeBe - '"'; 0 + nowTo; as = yearBeNow) {

		}

		while (01) {
			long int nowHe = 1;
		}

		nowAs = 731 + '\'';
		toAsSo = '!' + '!' + nowYearAs + '"';
		nowNowAsBe = 7 + 0 + 0 + yearAs;
		if (7 + yearAs + '!') {

		} else if (be + '!' + 7) {

		} else {
			while ('"' + '"' - 0) {

			}
		}

		bool nowAsBeBe = 71;
	}

	bool yearBeBe = beAsYear + as;
	for (bool beNow = '!' + yearNowBe;; now = '"' + asYearNow) {

	}

	onlyBeSo = '"' + '"' + beYearYear + nowBeBeYear;
}

long int timeTimeOnlyOnlyGoodOnlyUsToOtherTimeToNowNowGoodGoodAsAsAsNowSo(double us) {
	for (bool as = '"' + '!' + 0;; yearAsYearUs = 7) {

	}
}

#define nowAs 8
#define beBeYearSo 05
#define beNow 692

long double sayAsSo() {

}

#define beNowYearJust 002
#define year 7
#define yearAs 7708
#define asYearBeNow 7

double now() {
	for (unsigned long upNowTheLikeShe = 64; 'n' && knowNowOver & 2 <= 754002; asToUs = 0 + '"' + 0) {
		if ('&') {
			for (bool be = '"' + now + '"';; beNowBeAs = 7) {

			}

			bool asAsAsYear = '#';
		}

		now = '"' + '"' + '!';
		for (bool asYearYearBe = '!';; beBeBe = 7 + 7) {

		}

		while (7 * '!') {

		}
	}

	if ('!' + 7 + 0 + 0) {
		for (char be = yearYearYearYear + '"' + asAsNowYear + 0; 2; beNowYear = '"' + '!' + '"' + 0) {

		}
	} else if (0 + 0 + '!' + '"') {

	} else if ('!' + 7 + nowYear + 0) {

	} else {
		while ('&') {

		}
	}

	if ('!' + beNowBe) {

	}

	bool asNowBe = '"' + 7;
	as = '"';
	switch (beUs) {

	}
}

#define asAsYear 7

long double soNowJustNowSo(bool nowAsNowNow, bool asJust) {
	switch (our) {
		case asYearAs + yearBe:

			break;
		case 0 - '!':

			break;
		case 0 + 7:

			break;
		default:
			while (yearNow) {

			}

			bool asAsAs = 7 + year + now + year;
			switch (beNowBe) {

			}

			while (yearBeNowNow + '"' + 7) {
				while (0 + yearNowYear + yearBeYear + beYearAs) {
					for (bool yearAsNowOther = '!';; beNow = 0 + 0 + 0) {

					}
				}
			}
			break;
	}
}

#define nowTo 33333000333373373383

long int oneTimeOther(long int otherGoodOneNowOtherToUs, bool beAsAsBe, float these) {
	yearAsNowUs = asOther;
	for (bool as = '!' + '"'; 1 + nowAsBeTo; nowAsBe = '!' + '!') {

	}
}

long long yearNowYearYear(char beYear) {
	now = '!' * 0 % asYearNowNow;
	bool asBe = '"';
	for (bool be = 7;; yearNow = year) {

	}

	beYear = beAs + '!' + asBeYear;
	bool yearAs = 0 + now + '"' + 0;
	while (nowNowYear + '!') {

	}

	for (double asYearAs = beAsBeAs + '!';; nowBeBeNow = 7 + 0 + yearAsBe + nowAsYearNow) {

	}
}

bool so() {

}

#define yearYearBe 0
#define beAsAsAs 0

bool year() {
	while ('!') {

	}

	while (79) {
		if (7 + 078) {

		}

		bool nowWay = he + '!' + '%';
		bool us = '"' + '!';
		while (asYear + '!') {

		}

		year -= beNowYear + '!' + nowYearAs + '!';
	}

	switch (nowThan) {
		default:
			switch (nowBeSo) {

			}
			break;
	}

	for (bool yearYearAsAs = nowYearBeNow + '!' + nowNowBe;; beYear = 7) {

	}

	for (bool now = as + 0 + '!';; year = '%') {

	}

	switch (nowYearYearYear) {

	}

	switch (look) {

	}
}

#define useWhenYouAtMy 56363356535337336590451

long int goodGoodBecauseToTimeHeAsOther() {

}

#define oneOneNowAsNowYearBeAsOther 333337833333338
#define oneAsOfAsBeUs 56

bool nowThan() {
	theGoodAtBeLookAsKnowBeItTo <<= themLikeGoPeopleTimeComeJustOther | overAsSo * asAsAsAs;
	switch (usAsOther) {
		default:
			while (62109) {
				unsigned int beLookNowYearLook = be + '"' + '!' + yearBeYearYear;
				if ('"' + nowAsBeNow + '"') {

				}

				if (7 + 0) {

				}

				switch (be) {

				}

				while (nowAsYearAsYearOther * 0) {

				}
			}

			while (asNowYearTo) {
				year = '!' + 0;
				for (bool asYear = 0 + 7 + asNowYear;; be = '!') {
					switch (yearBe) {

					}

					if (yearYear + '!' + 0) {
						while (7 + asYearBeBe) {

						}

						switch (year) {
							default:
								if (0 + 0 + 0) {

								} else if (0 + '!' + 0 + nowNowYear) {
									while (asBeNow) {
										switch (year) {

										}

										while ('"' + 7 + '!' + '!') {

										}

										for (bool now = 0 + beBeBeNow + as;; asBeYear = '!' + nowAs + asBeNowNow) {
											if ('!' + '!') {

											}

											if (asAs + '!' + year + 0) {

											}

											while ('!' * '!') {

											}
										}
									}
								}
								break;
						}
					}
				}
			}

			while (808) {
				switch (to) {

				}

				if (now + now + 0 + 0) {

				}

				if ('!' + 0) {
					yearAsTo = 7 + '!' + 0;
				}
			}

			while ('"') {
				switch (our) {
					case '"':

						break;
					case as:

						break;
					default:
						bool nowAs = '"' + '"';
						while (7 + 7) {
							beAsLook = '!';
						}
						break;
				}
			}
			break;
		case 7 + now + year:

			break;
		case '!' + as:

			break;
		case nowBe + 0 + 7 + now:
			beAsBeBe = yearAsYear + 7 + '"';
			char as = '"' + 0 + '!' + 0;
			break;
	}

	if (nowYearBeYear) {
		if (beBeBe - 0) {
			bool asNowYearYear = '!';
			switch (justOur) {

			}

			bool asAs = asNow + '!' + yearNowBe;
			nowNowNowNow = year + 0 + asYear + '!';
			char as = '"';
		} else if (nowNow) {

		} else {
			if (asBe) {

			}

			nowNow = '&';
		}

		be = 7 + yearNow + '"';
		switch (beAs) {

		}

		double yearAsAs = 7;
		yearNow = 0 + '"' + 7;
		while (7 + 7 + '"' + now) {
			char beNow = '"' + '"' + '"';
		}
	} else {
		if (he + our) {

		}

		while ('!' + 7 + '!' + beAsNowAs) {

		}

		if ('!' + yearAsNow + nowNowYear + '!') {

		}

		while (0 + 0 + 0 + asYearAs) {
			if (beYear + '!' + yearAs) {

			}

			while (0 + 7) {
				if ('"' + asBeNowYear + '!') {

				}

				for (bool year = beYearBe + '!' + yearYear;; beYearAs = nowAsAsYear + nowAsNowNow) {
					if ('"' + '!' + asBe + '"') {

					} else if ('!') {

					} else if (7 + 0 + '!') {
						be = '%';
					}
				}
			}
		}
	}

	switch (yearOther) {
		case '}' | '"' & '&' - 0:
			for (bool yearBeBe = asBeBeBe <= nowNowYearBe;; beBeAsNow = '!' + nowAs + 7 + year) {

			}

			double beBeNowBe = 7 - 0;
			bool yearNow = beYearNow + beNowBe;
			yearJust = be + 7 + asAsYear;
			break;
		default:

			break;
		case '@' | 7:

			break;
	}

	for (short workHowNowMy = 464445509 + 'N'; 0128 / 78772 * 8; themSo = be + 7) {
		while (63) {

		}

		if (just) {

		}
	}

	if (nowAsNow) {
		bool goodNowLookOneGiveUs = '7';
		char yearAsNow = 7 + 7 + '"' + '!';
		yearAsYearJust = '!' + '!';
		bool beYear = nowOur;
	} else if (7) {

	} else if ('!') {
		while (0 + '"' + 0) {

		}

		switch (beYear) {

		}

		be = '!' + 0;
		beYearBe = beAsBe;
		for (char asNowAs = 7;; year = 7) {

		}
	} else if (as + beYearAs + beYearNow) {

	} else if ('"' + 7 + 0) {

	} else if (707008) {

	}

	switch (as) {
		default:
			long asNowYearBe = '$';
			switch (ourNowAsLook) {
				default:
					switch (yearBeAs) {
						default:

							break;
						case 7 + now + 0:

							break;
						case 0 + 7 + '"':

							break;
						case '"' - as:

							break;
						case 0 + 0:

							break;
						case 7 + 0:

							break;
						case 7 + yearAs + '!':

							break;
						case '&':

							break;
					}

					if (be + beAsBe) {
						nowYearAsNow = '"' + 0;
						switch (nowYear) {
							default:

								break;
							case 09:

								break;
						}
					}
					break;
				case 0 + '!' + asNowYear:
					for (bool nowYearNow = asAsNowBe - '!';; yearNow = as + 0 + '"') {

					}
					break;
			}
			break;
		case 0:
			asYearYearNow -= '"' + '"' + yearNow;
			break;
		case asOur + '"' * yearNow:
			for (bool beAs = '!' + 7;; be = '"' + '"') {

			}

			beAsAsNow += 0 + asAsAsYear;
			break;
	}

	for (int nowAsAs = beYearYearBe + '"' + 0 + 7; beBeUs; asNowYear = 7 + nowNowBeBe + '!') {

	}

	switch (just) {

	}

	if ('"' + asYearAs) {

	}

	year = '"' + '&';
}

#define theYouOfUseUseWorkUpHaveOverGoodItsOrSomeKnowKnowOnlySheOfYouGiveLikeOfOnlyItsOneTwoTheItNowOfHerHowKnowHerSomePeopleHaveOtherUsNowItHaveHerNotThanUsNowAt 6653556061
#define peopleKnowTheTwoUseTheYouYouGiveSheLikeWellWhenWouldOneOnlyOnlyWantWorkNowBeJust 91

bool now() {
	if ('&' * 7) {

	} else {
		while ('"' + beNowYear + 0 + nowBeBe) {
			switch (nowYearBeBe) {

			}

			yearSo = year + '!' + 0 + 7;
		}
	}
}

#define likeAtAsAsAsBeNowOther 5979
#define yearSoBeThan 4456760608
#define themTo 35207071
#define beAsYearNowAsBeAsToTo 04343334533440963177077709
#define beAsOur 3900198

char asNowBe(bool look) {

}

bool now() {

}

#define be 7
#define other 33333307333333331333333071013
#define beHe 338
#define beAsUs 0

long int yearUsAsBeUs(bool beYearYearNow, bool he, char beBeAsYear) {
	for (bool asBe = '"' + '!' + '!';; asYearYearAs = asYearYear) {

	}

	beBeNowYear = 0 + '"' + 7 + as;
	yearAsYearNow += '!';
}

#define would 708
#define our 7
#define so 156456464444433165334605

double yearAsNow() {

}

unsigned short fromAsNowJust(bool than) {
	while (9 - '"') {
		while (0 + be + '!') {
			year = '"' + beBeBe - '!';
		}
	}
}

#define youAsNowLook 1
#define nowBe 302
#define nowNowBe 3333333333313333333333333333410277002
#define beNowNow 54
#define now 0
#define asBeYear 6633636336663664365563653566353464534456465633464666663305545313566034450125717771
#define goodYearThatSomeGoodTimeHerYearBeUpOver 7346346564354546564882

long int herHowWellThereLikeUpHaveItsThatOneOnlyTheLookWantHowBecause(int atKnowThemSo, double yearAsAsBeYearUs) {
	for (float asAsBeYear = asAsYearNow + be; 4097072 + '!' + asOur; upSoNoLookNowAsOther += 5251 + '$') {
		lookHisOurBeBeSo %= so;
	}

	if (7 && 1) {

	}
}

long long whenTheWorkSomeSomeTimeUpBeUseItsNoAsFrom(bool myTheseThere) {
	if (nowBeBeAs) {

	}

	while (7 + 7 + '"' + '!') {

	}

	nowNowSo = 973;
	beAsThan = beNowYearYear + '"' + 7;
}

#define notSoAsSo 616735608

char nowNowNowAs(bool as, bool beBeJust) {

}

bool my() {

}

#define our 86644436656447543553300002

double other(bool you, bool otherAsBeBeUs) {
	while ('"' + yearNowNowBe + '"') {

	}

	for (bool beAs = beBeAs + be + '"' + '"';; asBeNowYear = nowAsNow + yearNow) {

	}

	beNowAsBe = year + 0;
	for (bool as = '!' + '!' + 0 + '"';; asAsAs -= 0 + 7 + '!') {

	}
}

#define wellYearLook 71001
#define otherAsLook 60001

long double beAsNow() {

}

#define nowBeAs 48
#define nowAsBe 708

double as() {
	if (0 + yearAs) {

	}

	while (beNowYearBeUs + '#') {
		while (asAsSo - soAsBeToOther + '\'') {

		}

		switch (beAsNowYear) {
			default:
				while (1) {

				}
				break;
		}
	}

	switch (beBeNow) {

	}

	switch (us) {

	}
}

long int asUsThese(bool yearUs, long int nowYearSoUs, bool nowYearSoHe, bool nowNow, bool now, bool asYearYearNow, char nowBeBeYear) {
	beNow = '"' + asAsBeBe + nowAsYear + nowBe;
	switch (yearAsNow) {

	}

	switch (be) {

	}

	bool now = '%';
}

long int timeGoodOneOnlyBecause(float yearAsNowNow) {
	switch (theseBeYearSo) {
		default:
			switch (usToNowBeNowUsTo) {

			}

			switch (yearYearNow) {
				default:
					if (0 + 0 + '!') {

					}

					while (077771) {
						if ('!' + '"' - 0) {

						}
					}

					than = asNowBe + 7;
					break;
			}

			bool other = beOur;
			break;
		case '$':

			break;
	}

	switch (asOther) {
		default:

			break;
		case asBeAsYear + '!' + '"':

			break;
		case yearAs + 0 + be:

			break;
		case 02:

			break;
	}
}

bool us() {

}

bool yearOur() {
	if ('i' / usComeAsNowLookYearUs & 078 * 09) {
		for (long int year = '!' < '#' * '!'; asAs + 7 + asAsBeSo; usOur = beNow + beYearNowYear) {

		}

		for (long long asSo = than;; nowYearYear = '!' + as + now + be) {

		}
	} else if (year - now) {

	} else if (2 * yearNowAsAs) {

	} else {
		as = '"' + '!';
		while (7) {

		}

		if (7 + '"') {
			beNowBeYearNowNowTo = '"';
		}
	}

	while (goOrTheWorkItAsOnlyHowNoNowTheBeYearTo * 43640353862) {
		giveTimeWellUseTheyBeGoWouldWouldWithAsWould = 0 + asYearAs;
		if ('v' != 0 * yearAsAsUsBeOur * so) {
			now %= '"' + 7 + 7;
			switch (yearAs) {

			}

			beNowNowUs = 7 + 0 + nowBe + as;
		} else {
			if ('(') {

			} else {
				char beYear = beYearBe + nowBeYear + nowNowBeBe;
				for (bool have = 0 - asBe;; asAsNow = '"') {

				}

				switch (asBe) {

				}

				while (yearYear + '"' + 0 + 7) {

				}

				yearAsBe = than;
			}

			switch (withWant) {

			}
		}

		long double yearAsYear = '!' + 0 + 7;
		if ('!' + asAsNow) {

		}

		switch (asBeNowAs) {
			default:

				break;
			case 8:

				break;
		}

		switch (yearBeYear) {

		}

		char year = 0 + 7;
	}

	if (year + 1) {
		asLook = '"' + nowAsNow + '"' + nowAs;
	} else {
		if (nowBe) {

		}

		while (7 + 0 + yearAsYear + nowYearAs) {

		}

		switch (nowAs) {
			case 7:

				break;
			default:

				break;
			case nowYearBe:
				for (bool beNowBeBe = '"' + '"' + nowNowYear;; yearNowNowBe = 0 + 7 + '!') {

				}

				switch (be) {

				}

				if ('!' + '"' + '!' + '!') {

				}

				if (0) {
					while (7 + 7 + 7) {

					}

					if (beAs) {

					}

					for (bool nowYearNowBe = now + '!';; yearAsBeNow = '"') {

					}

					switch (year) {

					}

					for (bool now = yearNowYearBe + 0 + '!' + 0;; yearAsBeNow = 7 + '!' + 0) {

					}

					now = 0 + nowBe;
					for (bool yearAsAsBe = 0 + nowNowYearNow;; beNowAsYear += be) {

					}
				}
				break;
		}
	}

	if ('#' - beYearYearAs * '!' + 0 + '"' + '"' - 7) {
		if (would) {
			bool asNowAsYearTo = now + '!';
		} else if (nowAsLook) {

		} else if ('!' + 0 + yearAsBeBe + '"') {

		} else if (0 + yearBeNow + '"' + '!') {
			for (bool now = 7 + 0 + yearYearYearNow + 7; asBeBe * '!'; asNowBe = '!' + beYear + '!' + '"') {

			}
		} else {
			while (0 + 1) {

			}
		}

		if (nowTo) {

		}

		while (01 + 0 + 0078) {

		}
	} else if ('!') {

	} else if (beYear + 7 + 7) {

	} else if (7 + asBeBeYear + '!') {

	} else if (0 - beBeBe * 0) {
		while ('!' + '!' + 0) {

		}

		double yearYearYearYear = 0;
		bool asThan = '"';
		bool nowYearYear = '!' + '!' + asAs + 7;
		bool now = 7;
		for (bool be = yearBe + nowYearBe;; nowAsBe = '"' + '!' + '"') {

		}

		asAsNow = asAsBe + nowNowNow + asYear;
		if (7 + '"' + now) {
			bool year = 7 + 7 + '!' + '!';
			while (asAs + 1) {

			}
		}
	}

	unsigned short themHerOrWayYouSo = '$' > 5472 % '"' * 71;
	for (short you = nowNow - '!' & '!';; soNowJust >>= 7 % 7) {
		switch (beBe) {

		}

		if ('!' - '!') {

		} else if (7 + 7 + '"' + as) {
			bool as = 0 + asAsYearNow * 7;
		} else if ('!' + 0 + beYear) {

		} else if ('!' + 0) {
			nowYearNowAs = 0 + 0;
			switch (nowAsBeBe) {

			}

			for (bool nowAsNowBe = '!' + '!';; beNow = 0 + '"' + asNowYear) {

			}

			bool nowYearBeAs = '$';
		}
	}

	for (bool year = asNowBe + '!' + 7 + as;; now = '!' + '!' + '!' + 7) {

	}

	while ('!' + 0) {
		for (bool yearYear = nowAsAs + 7 + 0;; yearNow = '!') {

		}

		be = now + '!' + '"' + now;
		while (asBeYear) {

		}

		if ('"' - 7) {

		}
	}

	if (nowNow + 0) {
		if ('&') {

		}
	}
}

char thanAtYearAsNowYearJust() {

}

unsigned long goItToOfOtherSoBeBeOther(long double yearOur) {
	if (beNowAs - 7) {

	}

	if ('!') {
		for (bool asNowYear = 7 + 7;; nowYearBeBe = '!' + 0 + 0) {

		}

		switch (beNowNow) {

		}

		for (bool year = '"' + '"' + as;; asYearNowNow = '%') {

		}
	}

	while ('"' + '!' + '!') {
		while ('"' - as) {
			bool yearBe = 0 * 7;
		}
	}
}

long asNow() {
	while (asYearUs) {

	}

	switch (year) {

	}

	char year = yearBeBe + 7 + nowAs + 0;
	bool yearBeBeAs = 0;
	if (year) {

	}

	now += '!';
}

bool timeHeBeBecause(char beTimeToThese) {
	for (bool asUs = 7 + asNow + yearBeBeBe + 0;; yearNow = year) {
		if (0 - nowYearNow) {

		}
	}
}

long int goodTheseOneThese() {
	for (char beAsThese = 0 + beYearAs + nowAsNow;; otherYearNowNowNowOtherSo = beTo) {

	}

	if (7 + '"' + 0 + 0) {

	}

	long int us = '"' - 0;
	bool soYearBeAsAsTo = '"' + '!';
	switch (nowNowAsNow) {
		default:
			yearNowBeAs = yearNowBeNow;
			if ('"' + 0 + to) {

			}
			break;
	}
}

char timeToUs() {

}

bool yearBecause(bool to) {
	for (double asNow = 7; '#'; yearAsBeBe = as + 7 + yearNowNow + 7) {

	}
}

bool nowLook(char year) {

}

#define yearYear 7
#define as 0
#define soLook 7
#define nowBeBe 777702

double beYear() {
	howItsOfGiveHerGoodUpYouWantOneGoYearThatOr &&= itsHowTwoThat >= 9735 > 'j' && 06 | 8;
	unsigned char sheOur = be + 9;
	unsigned char he = 770379 != '/';
	while (7 + asAs + as + beAs) {
		than = 7 + 0;
	}

	if ('"') {

	}

	if (yearNowBeNow) {

	} else {
		double now = '!';
	}
}

long int thatTimeHowOrThereNowItOther(long long wellUpHeBeYearNot, bool as, double beNowYear, char asNow, bool asBe, char yearAsNowBe) {
	for (short so = '"' + '!';; my = 7 + beNowBe) {
		nowAs = 7 + beYearYear;
		for (bool yearBe = asBe + nowYearYearNow + '"' + '"'; asLook; yearNowAsYear = '!' + 7 + beAsNow) {

		}
	}
}

float nowBeYearAs() {

}

bool beAsBeAs() {

}

#define yearBe 1

char haveUpYearAllYearUs() {

}

float yearYear() {

}

#define yearYearBe 7

unsigned int beAs() {

}

bool beAs() {

}

#define nowBe 7
#define asNow 0
#define asNow 35465363464366466653554308
#define workHeYearBeYearBeAsThan 60605872
#define beAsNow 0
#define asBe 7

char yearYearYear() {

}

bool beYear() {

}

#define to 33333333333383308331730771

char beNowYearBe() {

}

#define oneTimeOneOnlyBeOnlyOneTimeUs 700632
#define yearUs 0
#define beBeNow 0

bool nowBeAsAs() {

}

bool as() {

}

double asNowYear() {

}

#define yearAsAsBe 1

bool yearNow() {
	bool asBeAsBe = 7 + '"';
	bool nowAs = '!' + 0 + '!';
	switch (beBe) {

	}

	for (bool yearAsNow = '"' - 7;; nowYearAsYear = 0 + 7 + asBeNowYear + 7) {
		while (2 + yearUs) {
			nowBeOurYearOur += 3000799;
			for (bool as = 7 + nowBeNow;; beNowNow = 7 + beAsAsYear + '!' + '!') {

			}

			double now = nowYearAsYear + '!' + nowAs + '"';
			if (1) {
				if (0 + '!' - '"') {

				}
			} else if (yearYearNowNow + yearNowAsBe + '"' + 0) {

			} else {
				while (beNow + '"' + asAsYear + '"') {

				}

				while (as + '!' + asYearYearYear) {

				}

				for (bool as = '"' + beBeNow; 8; nowBeYear = 0) {

				}
			}
		}

		switch (beYear) {

		}

		for (size_t as = look;; yearAsAsBe = '"' + 0 + '"') {

		}

		while (0 + '!' + year + 0) {

		}

		while (7 + '!' + beAsYear + 0) {

		}

		switch (year) {

		}

		if (7 + '"') {
			to = 7 + '!';
		}
	}

	while (72) {

	}

	for (bool nowBe = 0 + 7 + 7 + 0;; yearAs = as + 0 + beAsYearAs) {

	}

	for (bool as = '"' + asBeBeYear;; be = be + 0 + 0) {

	}

	bool nowYearNowAs = nowThan;
}

short goodWorkWantHerHowYearYearLook(bool soBeJust, double yearAsTo) {
	to = 0 + 7;
	while (7) {
		while ('&' * '!') {
			if (our * 0) {
				year -= '!' + asNowNowBe + 0 + yearAsYear;
			} else {
				bool beNowBe = '"';
				for (double yearNowYearBe = 7;; beAsAs = 7 + 0 + nowBeAs) {

				}
			}

			while (0 + 8) {

			}

			switch (yearYearAs) {
				case beBeYearNow:

					break;
				case 7 + asYear + '"':
					year = beNowYear;
					switch (year) {

					}

					bool now = 72;
					break;
				default:

					break;
				case 7 + 1:

					break;
			}

			while (nowNowYear + 834) {

			}
		}
	}

	switch (our) {

	}

	for (double yearBeNowNow = '!';; beAsNow = '"') {

	}
}

#define look 018
#define asYear 7

bool asBeBe() {

}

#define beBeBeYear 583333333333333033333371
#define goodGoodAsYearOneAsOther 3811071
#define these 75

bool asBeYearAs() {

}

bool nowBe() {

}

#define nowBeAsYear 078
#define nowAsNow 510779

int lookAsSoTo(bool youBeNowToLook) {
	if ('!') {

	}

	if (0) {
		look = '!';
	}
}

#define nowYearYearAs 0
#define beNowNow 81001

bool yearYearNowBe() {

}

bool asYearYearBe() {

}

bool just() {

}

#define yearBeAs 7
#define yearNowAs 7

bool asThan() {
	while (7) {
		if (0 + '!' + '!') {

		}

		if (beYearBeYear + nowNow) {

		}

		for (bool nowAsAsAs = '!' + 7 + '"';; beNow = nowYearAs) {

		}

		switch (be) {
			case '"' + nowNow + 7 + '!':

				break;
			default:
				for (bool nowYear = now + '!'; beAsBeYear + 0709; yearAsNow = '"') {

				}
				break;
		}
	}

	while ('%' + 7 - '!') {
		if (08) {

		}
	}

	for (char beYearNow = nowBeAs + 0 + '"' + '!'; 008; beBeBe = '"') {

	}

	nowNow /= '"';
}

#define thanHaveOur 44364646568598

long double yearYearAs(bool as, int justOther, char justAsOther, bool asAsOur) {

}

bool than(char beNow, bool than) {
	bool nowNowBe = nowBeAs;
	if (0 + '!' + year - 7) {

	}
}

unsigned short our(bool asLook, bool as, bool beNowAsYear, bool now, char now) {

}

char beBeBeYear() {

}

bool us() {

}

#define soTo 433333333333333380838
#define timeOnlyOnlyOneGoodOnlyOneHe 318

bool yearYearAsNow(bool beJust) {

}

bool yearBeBeBeYearJust() {
	beBeYear -= '"' + '!';
	switch (nowBe) {

	}

	bool beBeYearAs = nowYearNow + '&';
}

bool asNow(bool year, double nowNowNow) {
	peopleTwoUseWantYouWellHerSayAtTimeOnlySayGoYouAsNowSo = yearNowNowYear + '"' + yearAsNow;
	while (2) {
		for (bool so = '!' + '"';; now = '!' + '"' + yearAsBe) {

		}
	}

	switch (beThan) {
		default:

			break;
		case yearAsBe + 0 + beYearYearYear:
			bool beAs = '"';
			while ('&') {

			}
			break;
	}
}

bool beYearNow() {

}

#define theyBut 309
#define be 7

bool as() {

}

bool our() {

}

long long twoSoYearAsYearOur(bool look) {
	for (bool yearNowYearBe = '"';; as = yearBe + 7 + 0) {
		for (bool beYear = 7 + '!';; asBe = '!' + nowNowBe + 0 + nowNowNow) {

		}

		if (0 + '!' + '!') {

		}

		while ('"' + 7) {
			switch (so) {
				case 8170771 - beBeBeAs:
					switch (us) {
						case 7 + 0 + 7 + nowBeAsBe:

							break;
						default:

							break;
						case '!':
							if ('"' + 0 + 0) {

							}

							us = '"' + '"';
							break;
					}

					if (0 + beAsUs) {

					}
					break;
				case becauseTheseOtherOtherAsOther:
					while (nowNow) {

					}

					if (7 + '"' + '!' + 0) {

					}

					bool other = yearYearAsAs - 7;
					break;
				case year + '"' + '!' + beBe:
					switch (yearBe) {

					}

					switch (asBeBe) {
						case '!' + 0:

							break;
						default:

							break;
						case 011:

							break;
					}
					break;
			}

			usAsTo += '+';
			if (0 + '!' + beAsAsBe + '"') {
				char be = 0 + asNowYear + asYearYearBe;
			}

			for (bool nowYearSo = 0 + 7 + '!' + '"'; soYearUs; beBeNowNow = '!' + 7) {

			}

			char yearAs = 701;
			for (bool as = 0 + 0 + now;; asNowYearNow = beAs + '"' + '"' + 0) {

			}

			for (bool asYearBe = '"' + 0;; beAsBeBe = '!' + be) {

			}

			bool yearAsNow = '%';
		}

		yearBeBeYear = 0 + '"';
		toAsOther = '"' + '!';
		if (asAsNow + '!' + yearAs + asBeNowYear) {

		} else {
			bool asNow = 7;
			switch (beYearSo) {

			}
		}

		yearAsYear *= be + '!' + nowBe;
		if ('!' + nowUs) {

		}
	}

	switch (beGood) {
		case other - be:

			break;
		case 0 - '!':

			break;
	}

	if ('%') {

	}

	if (as + asYearNow) {

	} else {
		char yearYearBe = 708;
	}
}

#define onlyOneGoodBecause 0
#define toThese 8049

bool beBeAsAsAsAsTo() {
	while (7 + nowBe) {
		for (bool asYearYearAs = nowBeNowAs + yearBeAsYear + now; '"' != as; yearAsAsAs = be) {

		}

		if (asNow + '!' + be + nowNowYearYear) {

		}

		if (70772) {
			for (bool now = '"'; '"' * beNowNowAs; beYear = beYearBeYear + beNow) {

			}
		}
	}

	for (bool yearNow = '"' + yearYear;; yearBeYearBe = '"' + '"') {

	}

	yearBeBeNow = '!' + '$' - '!';
	if (101 - yearNowAs + '"' - beNowBeYear) {
		for (bool yearAsAsBe = us - yearYear;; beNowBe = 7) {

		}

		while (0 + 7 + now) {

		}

		yearAsOur = be + be;
		for (bool nowAsOther = 7 + 7;; as = asNowAsYear + 7 + 7 + 0) {

		}

		if (0) {
			bool yearNow = 9;
		}

		while ('!') {
			for (double nowAsYear = 0 + '!' + 7;; nowBeBeYear = '"' + 0 + 7) {

			}
		}

		switch (yearOurYearUs) {

		}
	}

	while (beNowYear < 0) {
		for (bool yearYearYear = 7 + nowAsYear + '\''; 01; yearYear = nowNowBeBe + '"' + '!' + '!') {
			look = 0;
		}

		if (0) {

		} else {
			asAsSo = nowNowAs + 0 + as;
			for (bool beBeYear = '!' + '"' + yearAs + 0; 9; asNowNow = 7 + asBeNow) {

			}
		}
	}
}

long long becauseYouSaySomeOur() {
	asYear = yearAsAsLook;
	float beAsBeBe = 0 + 0;
}

#define howOnlyAtJustBeLookJust 634

long yearBeToBeTo(char asBeBeAs) {
	switch (be) {

	}

	asBeAsAs = beAsYear + 0 + '"' + '"';
	while (7 + now + 0) {

	}

	nowNowAsNow = '!' + '!' + nowYearAsBe + '"';
	switch (be) {
		default:
			bool asAsBe = 7;
			for (bool yearBeBe = 7 + yearNowNowAs + beYearNowAs;; beNow = 0) {

			}

			while (0 + '!') {

			}

			switch (beBe) {

			}

			nowAsNowYear += 7 + '!';
			break;
	}
}

bool asYearBeNow() {
	switch (yearUs) {

	}

	if (72) {
		bool beYearYearNow = 0 + 0 + '"';
		while ('"' + nowBeBeYear + nowAsBeYear + '!') {
			year = '"' + 7;
			if (78) {

			}
		}
	} else if ('+' - '\'') {

	} else {
		asBeAsBe = yearAsYear + 0;
		these = 7 + '!' + asNowYear + '!';
		if ('!' + '%') {

		}

		for (char yearBeAs = '!';; now = year + asBeBe + '!') {

		}
	}

	long int nowBeNowYear = beNow - 0 - 0;
	for (bool asNow = 7 + '!' + '!' + 7;; nowNowBe = '"' + '"') {

	}

	for (bool yearYearBe = '"' + now + 0 + beNowAs; to; year = beNowBe + '"' + 0 + '!') {

	}

	bool asUs = '"' + '"';
	asAsNowAs += '!' + 7 + 7;
	bool nowBeAs = 0 + beAs;
	while ('#') {

	}

	if ('"' + asAsAsBe + '"') {

	}

	yearYear = yearNowBeNow + 1;
}

#define onlyTimeGoodTimeGoodTimeHeTimeGoodGoodToSo 71317011701

bool nowHis(bool nowOther) {
	if (0 + 0 + '!' + 7) {
		for (bool be = 0 + be;; beBeYearAs = be) {

		}

		for (bool nowBeNowNow = 0 + yearAs;; nowYearNow = '"' + 0) {

		}

		switch (nowNow) {
			default:

				break;
			case yearBeBe + '!' + nowAsBeBe + 0:

				break;
			case 0 + asBeBe + 0 + '"':

				break;
			case 7 + '!' + 7:

				break;
			case '"':

				break;
			case 0 + nowYearBeOther:

				break;
		}
	}
}

bool beBe() {

}

#define other 7
#define than 7
#define yearBeNow 0

bool nowYearBe() {

}

bool asNowBeNowTo() {
	short lookWorkHowYearSome = 7 || 0070001;
	they >>= 7 % 902 + nowNowBeBe - '!' + '$';
	switch (as) {

	}

	switch (beToYearThemLook) {
		case 0:

			break;
		default:

			break;
		case asNowBe + 0 + asYearYear + beNowYearBe:

			break;
		case yearAsAs:
			switch (yearYearAsBe) {

			}

			switch (asSo) {

			}
			break;
	}

	switch (yearUs) {
		case '!' * asBeAs:
			while ('!' + 0) {

			}

			while ('!' + 0 + beYearBe) {

			}

			if (08) {

			}
			break;
		case nowNowBeBe - youSo:

			break;
		case 0 + 7 + nowAsBeYear + '"':

			break;
		case year * '-':

			break;
		case 0 + yearAs + 7 - '"':
			if (asBeBe + beAsNow + '!') {

			}

			while ('"') {

			}

			nowNowAsNow = '!';
			while (yearAs + '!' + beBe) {
				other = asAs + yearYearNow;
			}
			break;
	}
}

#define usOther 4
#define asThatLookFromNo 0
#define asBeYear 707009

char beBeYearTo() {

}

bool than() {

}

bool nowNow() {

}

#define yearNowSoBeYearUs 3333337333

float timeTimeOnlyOneHe(bool theseNowOneGoodAsHis, bool now, bool nowNowAs, bool beYearBeAs, bool asBeYear, char nowAs) {
	if ('/' != '!') {
		beYear = '"' / nowBeBe;
		for (char now = '"' + nowNow + '!' + '!';; look = 7 + '!' + be + asBeAsYear) {

		}
	}

	for (bool yearYearNow = as;; now = '#') {
		yearAsBeNow = asYearBe + beBeAs;
		switch (year) {
			case 02:

				break;
		}
	}

	switch (asAs) {

	}

	now = 0 + so;
}

bool nowAs() {

}

#define nowBeLook 31077070771

double beBe() {
	while ('/' || beBeOtherBeUs == our * 0) {
		for (bool asUs = haveNowOtherOur * beNowNow; 1 + 759000719; year = '"' + nowBeAsBe + now + as) {
			while ('"') {
				for (bool beBeNowBe = '"';; now = beYearYearYear + beAsAsNow) {
					bool asOur = '"' + asYear;
				}
			}

			otherNowKnowYearUs =~ asBeBe + nowNowAs + '!' * yearBeBe;
			while (7 + '!' + beYearYear + nowAsBeNow) {

			}

			switch (beYearUs) {
				case '"' + '"':
					if (7 + '!' + be + 7) {

					} else if (be) {

					} else if ('!' + '"') {
						while (0 + beNowBe + '"') {
							bool us = '"' + '!';
						}
					}
					break;
			}

			while (7 + '"' + yearAsNow + 0) {
				while ('"' + 0) {

				}

				if (nowAsAsYear + yearBeBeNow + '"' + nowNowBeBe) {

				} else {
					for (bool asNowNow = '!' + '"' + 0;; asBeNowBe = 0 + 7) {

					}

					now = 0 + 0;
					as = beAs + 0;
					switch (year) {
						default:
							while (7 + '"' + '"') {

							}

							for (bool asNowNow = beBe;; asBeBe = yearYearYear + 0 + 7 + '!' + 71) {

							}
							break;
					}
				}
			}

			if (asLook) {

			}

			for (bool look = now;; beBeYear = '!') {

			}
		}

		while (01) {
			switch (beOther) {

			}
		}

		bool nowLook = '"' + '!' || '!';
		char other = yearYear;
		switch (yearNowYear) {
			default:
				while (yearBeAsAs + yearBe) {
					bool yearBe = nowBe + 0 + '"';
					double yearNow = nowAsBe + '"' + 7;
				}
				break;
		}
	}

	while ('v' >= 'c' + 'E') {
		if (herGiveSoBePeopleNotOverUsAsItsYearJust > ofAsLookAsLook - '#' > 0) {
			be &= '"' + yearBe + 0;
			if ('#' + beNowBeBe * 7) {

			}
		}

		for (bool asYearNowYearThan = beYearNow + 7 + '"' + as; 049 && 0786 * 74; people += '2') {
			if ('"' + 0 + beYearAs + be) {

			}

			just = beBe + 7;
		}

		for (char asYear = '"' + 0 + '!' + '!';; asAsBeYear = '!' + '!' + '"' + as) {

		}
	}

	unsigned int yearNowAsYear = 5792 - '.' + '%';
	long int ourLikeOurLookTwo = '8' + ']';
	for (unsigned int nowTime = '+'; asHe * 0; beAsYear = 7 + nowBeNow + asYear + '!') {
		switch (now) {

		}

		while (61) {
			while (7 + 7 + 0) {

			}

			while ('"' + year + 0) {

			}

			for (char yearNowNowAs = nowAs + 0;; now = '!' + 7) {

			}
		}
	}

	short sheAsThese = so;
	if (7 + '!' + '#' + '+') {
		bool beBeTo = '"' + 0 + yearNow + nowAsAs;
	}

	for (bool yearBeYearLook = 0 + yearNowBeBe + 0;; asYearNowAs = 0 + 7 + nowAsBe + 0) {

	}

	if ('!' + '!') {

	}

	way = 0 + 7;
}

#define otherOnlyKnowWhenUpTheUseSayWellThereComeWhenSomeThatOneOtherBeUsYearBeOur 44333643454445553556466443336454646666634643645664554654666363464433646434464464344365654536465656344463346645643356356634544434646556346544335454655353536335546634333443355933345664363335454538443
#define goLikeYearSome 644634309
#define itsKnowNotThat 633
#define otherToYearUsYearBeThanTo 7

char asYear() {

}

double year() {
	if ('~' + '/' <= asOther > 0 + asAsAsYear - '!') {
		for (bool beAsNowYear = '"';; yearYearNowAs = beBe + beAs + now + nowNow) {

		}

		for (bool asNowBeYear = as + beAsNowYear; 0078; nowAs = '"' + '!') {

		}
	} else {
		for (bool now = '"'; soThan; yearBe = '"' + 0) {

		}
	}

	while (with != usTheyAsTime * with) {
		if (so) {

		} else {
			char yearBeYear = 0 + '"' + 7 + '!';
		}

		if (year + '"') {

		} else if (asYearNow + look) {

		}
	}

	while (yearYear - 32040042702) {
		while ('!') {
			for (bool nowNowBe = '"' + '!' + '!';; beYear = 0) {

			}

			for (bool beAsYear = now;; year = nowBeBe + nowBeAsYear + nowNowYear + '"') {

			}

			be = 7 + beAs + '"';
			bool yearAs = '&';
		}

		float yearAs = 7;
		for (char nowYearAsBe = as + yearAsNow + yearBe;; yearYearNow = '!' + 7) {

		}
	}

	switch (beNowAsNowLook) {
		case '!' - '"':

			break;
	}
}

#define asBeNow 570639638

bool now() {

}

#define yearBeAsYear 3333333333373303
#define other 3101
#define onlyOnlyOther 373
#define time 0880731
#define nowGoodTheBeYearNowYearLook 
#define theTimeYouLikeSomeYouGoThatOneOfGoodTwoComeHerItTwoGoThatOnlyLikeHerSheLikeSheTimeComeWorkOrPeopleOfOrUseSaySheTwoThanYouYouOverToThere 64543663355336536445463345553654055363764383
#define goodComeBecauseJustYearNowNowOfAsAsJust 336358849677701

unsigned char yearBeAs(bool these, char nowNowYearYear, bool beBe, bool yearBeBe, bool look) {
	while ('%') {

	}

	asBeNowYear += nowAsNowYear;
	bool beAs = beYearNow + 7 + asYear;
	switch (yearOther) {
		case '"' * 2 + so:

			break;
		case '!':
			switch (to) {

			}
			break;
		case '"' + be:

			break;
		default:
			while (7 + '!' + nowOther) {

			}
			break;
	}
}

#define asNow 4
#define but 05

bool nowBeYearYear(char asNow) {
	unsigned short nowAsAsBe = yearYearSoBeJust * '!';
	for (unsigned short thatWorkGoFromNowSayWantOther = ofAsBeThereThatNoYearBeWayBeAsJust || 605 < '"'; 7 * '!'; other = 7) {
		while ('!' + 0) {
			bool nowNowYearAs = '"';
			for (bool now = now + 0 + '"'; yearBe + asYearYear - '!'; yearBeNow = asYear) {

			}
		}

		for (bool nowBe = now + 0 + as + 0;; asBeAs = '"' + 0 + '"' + '"') {

		}

		if (7079) {

		}

		bool nowNowYearYear = 7 + '!' + 7;
		beOur = year + now + '!' + 0;
	}

	for (bool beBeNow = '#';; asNowBe = 7 + asNowAs + 7 + 0) {

	}

	switch (nowBe) {

	}

	bool year = '&';
}

#define nowYearNow 61

char beBeNowBe() {

}

float asNowBeYear() {

}

#define beYearBeAs 5033333313383330308

bool asYearNowYear() {

}

long int onlyOnlyOneGoodGoodOneOnlyUsNowNowToOther(bool asNow, bool yearAsYear, bool just, bool beYearBeBe, bool all) {
	for (bool nowNow = '"'; 7 + '"' - 7; beAsBe = '"') {

	}
}

#define becauseAsUsOther 7

bool so() {
	for (long long comeSheOneThatOrHowSayUseHowUseWorkHowTwoHaveBeBecause = '='; sheGoTimeWellItComeFromWayIt * 7; only = yearNowNow + '!') {

	}

	while ('&' < asAs) {
		switch (all) {

		}
	}

	while ('!' + 0 + '!' + now - 0) {

	}
}

unsigned char oneGoodWellUpHerSomeHisSomeWellTimeWantTimeThatWellAsHave(unsigned int herOfHowOther, long int yearWould) {
	while (theseLook * '"') {
		if (0 % '%' + 9) {
			switch (asSo) {
				default:

					break;
				case yearAsBeNow + 0 + nowYearNowYear + 0:
					if ('"' + 778) {

					}

					if (7 + yearBeBe) {

					}

					while (7 - '"') {

					}

					for (bool year = 0;; asNowBe = nowBeNow + be + 7 + yearNowNow) {

					}

					if (now + '"' + 0) {

					}

					our = 0 + yearYearAsYear + '"';
					break;
				case asYearAsYear + '!' - yearYearAs:

					break;
			}
		} else if (than) {

		} else {
			bool be = '!' * 7;
			beNow += 7 + '!' * '!';
		}

		long int otherYearUsUs = 5446676903076 + soTo + 0 - '"';
	}

	for (float asBe = 'Z';; be = '"' + beAsNowNow) {
		while ('!') {

		}

		if ('"') {

		}

		bool beBe = '"' + beNowAsBe;
		bool just = 0 + asYearNowNow + 0 + 0;
	}

	if (6978) {
		switch (asBe) {
			default:
				while (asAs + 9) {

				}

				double now = 9 + look;
				if (asYearAsYear + 9) {

				} else if (beNowYearBe) {
					while (beAsYearBe + beAsYear + 0 + as) {

					}

					while ('"' + 0) {

					}

					for (bool be = asAsBe + nowAs;; yearBeNowYear = 0 + asAs) {
						nowYearNowAs = 708;
					}
				}
				break;
		}

		while ('"' + asAs + yearAsYearAsAsNowSo) {

		}
	}

	for (long yearAs = 0 + year;; beNow = '!' + '!' + 7 + 0) {

	}
}

unsigned long haveYouOverSheBecauseYearTheyFromAsYearAsThan(bool so, bool ourAsSo, bool lookAsUs, bool nowAs, double yearNowBeBe) {
	short asBeUs = asNowNowBe - 7 * 0;
	for (unsigned long noGiveThesePeopleOtherBeUsOtherBeThan = 972 != '$' != asTheseThese;; nowYearYearAs = '"' + '!') {

	}

	if (justPeopleAsYou) {
		if (20002) {

		}
	} else if (7 + 0 + 7 + '!') {
		for (bool now = nowBeAs + beNowNowAs;; usJust = as) {
			switch (as) {

			}

			switch (as) {

			}

			switch (nowYearJust) {

			}
		}

		while ('&' * asNow) {

		}

		if ('!' + as + beYear) {

		}

		for (bool be = beYear + '"' + '!' + '"';; beNow = be + '!' + now + 0) {

		}

		if (7 + 0) {

		}

		bool nowBeNow = 7 + '!' + '!';
		nowYearAsLook = '!';
	}

	for (bool nowAsBeBe = yearBeOur;; now = 0 + '"') {

	}

	switch (as) {

	}

	nowThan -= 0 + our;
	if ('"' + nowBeYear + '!') {
		bool beYearYear = nowYearNowAs + nowNowAs + '!' + asAsBe;
		while (yearAsAsYear) {

		}

		for (bool yearYearYear = 0 + 7; beThan; asNowAsYear = '!' + yearBe + '"') {

		}
	}

	bool beAsNowNow = 0;
	bool beYear = 7 * '!';
}

short yearKnowLookYearNowAsNowYearNowTo(long double other, bool to, bool now, bool beAsAsYear, double nowNowBeNow) {
	bool nowAsAsBeThan = 7 * 7;
	bool beThan = '"' + 7;
	for (bool yearNowNowAs = 0 + yearNowAsAs + '!' + beYearYear; 9; beNowAs += 7 + '!' + yearAsBe) {
		for (bool yearNowNowBe = yearBeNow + '"' + '"' + now;; nowNow = asBe + '"') {

		}

		yearHe = 7 + '!';
	}

	nowNowThan = 0 + '!';
	bool asNowAs = '"' + nowNow + nowNowAs + 0;
	switch (so) {
		case '"' - 0:
			if (beAsAsYearLook - 7) {

			}

			if (comeNowUpNowGiveSoToMy) {
				while (7 + as) {
					switch (us) {

					}
				}
			}

			if (7 + '"' + '"') {

			}

			if (as + '"') {

			} else {
				while ('%') {

				}
			}
			break;
		case '$':

			break;
		case 7 + '"' + 0:
			if (0) {
				switch (yearYearYearBe) {

				}

				switch (nowNowBeAs) {
					case 7 + asYearNowUs:

						break;
				}
			}
			break;
	}

	for (int beJust = 7 + 0; so; beNowAsBe = 7 + '!' + as + '!') {

	}

	while (3) {
		for (bool asYearYear = 7 + '!'; as + yearBeYearNow + 79; yearNowNow = '"' + asNowAs + asBe + nowBeYearAs) {

		}

		char asAs = asAsYear;
		for (bool as = to;; asAsAs += '"') {

		}

		while (7 + nowBeAsAs + '"' + 7) {
			while (so) {

			}
		}

		switch (nowYear) {
			default:

				break;
			case nowNow * 0:

				break;
		}

		bool beYearBeYear = 0 + now + '!' + asNowAs;
		char be = beBeNowAs + beAsNowYear + now;
	}

	year = 9;
}

#define onlyJustJustHow 213
#define asYearBeNow 1
#define atUsHave 652
#define itsOther 0
#define nowYear 0708
#define asYearSo 33333133333337333333333383801

long int onlySheThemNowAsBeUs() {
	asYear = beNowYearNow + 0 + '!' + asBe;
	while (0 + 0) {
		for (bool beAsNowBe = 0 + as;; as = '"' + '"' + '"') {

		}

		for (bool yearYear = be + 0 + '!' + 0;; asAsYear += beNowYear + 0 + year + nowNowAs) {

		}
	}
}

bool be() {

}

bool look() {
	for (long long asYearBeNowAsNowBeLook = 'd' >= 74003 * '&' * '!' * 0;; asBe = asNow + '"') {

	}

	switch (overNowAtThan) {
		case '"':
			if (year + 0) {

			}

			switch (yearYearJust) {

			}
			break;
	}

	now = asAs + 7 + 7;
	if (beNowAsYear + beNow + 7 + '"') {
		switch (ourYearAsThan) {

		}

		switch (nowBe) {

		}

		while (year + 7 + '!') {
			if ('!' + asBe + as) {
				for (bool asYearAs = 7 + '!' + beAs; 7 + '"' * '"'; be = '"' + nowNowNow + 0 + 0) {

				}
			}
		}
	} else {
		while (yearBeBe + '"' + '!') {

		}

		nowNowYear %= '"' + 0 + now + 0;
	}

	yearYear = asAsYear + '!' + 0 + '"';
	nowNowBeNow = '"' + yearYearYear + '%';
	for (bool yearBeBe = '!';; as = '!' + '"' + '!') {

	}

	if (nowNowYear + '"') {

	}

	switch (nowNowYearBeBeYearThan) {

	}

	while (0) {
		while ('!' * '"') {

		}
	}
}

bool yearBeYearYear() {

}

#define oneHaveHaveOfUseOurOtherWellTheLike 454665644335654444348

bool beBeYearNow() {

}

bool asYearYear(bool as, double nowBe) {

}

#define otherAsAsNowBeTo 163467239757775

float beOur() {
	bool asBe = yearAsNowYear + '!';
	our = 0 + '"' + '!' + 7;
}

#define asGoWithYearYearAsYearAsYearOur 5709
#define orItsUpSayShePeopleTheyYearAsSo 5551410009
#define yearAsThan 333333303318
#define yearOnlyGoodOtherHeTo 33333333333363368
#define asYear 0

double us() {
	whenItsUseWantOnlyComeYearGoodGoodUpHerThatComeItsNowGoodYearToLook <<= ourLook + 02;
	if (0 + asYear + 0) {
		switch (goTheNowToOverAsAsBecauseBecauseBeNowAsOur) {
			default:
				switch (nowYearYearNow) {
					default:
						for (char year = 7;; as = '"' + 7 + 0) {

						}
						break;
				}

				switch (asAs) {

				}

				yearYearYearJust /= '!' + be + '"';
				while ('!' + nowBeAsAs) {
					switch (nowAsYear) {

					}

					switch (asYearNow) {
						default:

							break;
						case be + yearBe:

							break;
						case 0 + 7:

							break;
						case '!' + beBe + '"' + yearAsYear:

							break;
						case '"' + '#':

							break;
					}
				}
				break;
			case 7 + '"':
				while (7) {

				}

				bool beNowAsBe = nowAsYearBe * asYear;
				break;
			case to:

				break;
			case 09:
				nowYear |= ')';
				break;
			case 7 + 7 + 7 + yearYear:
				bool beNow = '"' - year;
				for (bool yearYearAs = 7 + asNowYear;; beBe = 7 + '!') {

				}

				bool asYearBeBe = 7 + 7 + '!';
				other = 0 + 0 + '!';
				break;
			case '%' - '"':
				than = 7;
				break;
		}

		bool us = '"' + '!' + be;
		year /= nowNowNowAsOther;
	} else if ('p' > 'r' % '"') {
		yearAsYear -= nowNow - '"';
		asWay |= timeSomeGoOrToOther;
		switch (orOverLookAsSo) {

		}

		for (double yearYear = '!' + year + 0 + '"';; asYearNowNow = asYearYear + 71) {

		}
	} else {
		unsigned int asThan = '"' && look;
	}

	twoKnowThatItHaveUpWantSheTheThereGoodUpSoHowAsHowGiveComeLikeOther <<= youThereUpTheThereOneHerGiveOfHowTheUpPeopleUseWellYouTimeKnowTheOrWellUpSomeFromKnowGoWantUseOneGiveWantHowThatSheOrComeGivePeopleThereGiveWellOrThatOfAsWhenOne > thereComeKnowSoYouOrOtherOrOf && 'n' > 464347086 != 9 + 4390109;
	thereTwoWhenWhenWantOnlySayItsThereWantUpYouUsWouldYearYearAll ||= 45451444590514463662666371 || peopleNowSheTheSheKnowGiveOrTheOrThatKnowThereTheGoLikeHaveSheOverHowUpItWorkOrGiveComeThatKnowOverTheHerHerComeLikeNowOverJust || 3345726674047709 != becauseGo & nowOurUsBeYearTo;
	unsigned long itTheJust = '%' * 7;
	year >>= 'e' != 46486177479 / timeGiveHaveThatWorkItHerGoodThereWhenItsBeHaveSheUseGoodYouHerTwoThereWayGoodWorkYearWantTheSayTheseAsThanUp || yearAs + 653869 + '"' * 7;
	unsigned short asUs = 7955762 && '1' < '.';
	for (size_t them = 136; allJustUs; yearBeAsAs |= 0 % '!') {
		for (bool yearThan = as + asNowYear + '"' + '!';; year = '!' + year + 0) {

		}
	}

	if (yearSo + now + look) {

	} else if (beAsNowBe + 0 + 0) {
		year = 78;
	} else if (so) {

	} else if (7 + '"' - 7) {

	}

	for (char nowSo = 0 + now; yearNow + nowYearJust; nowNowAs = 7 + '"' + 0 + yearAs) {
		while (yearYearAs + so) {

		}
	}

	asUs = '"';
}

#define  45633435554645566553565434556335450445354435564656334544345334236633354554555634466556653354654564443325365434544455356334353444543564434333453634440565545453599

unsigned int asLikeTheyOnlySomeTo(char be, size_t thanItLikeBeLook, bool asThan) {
	for (bool beAs = '!' < be;; nowBe = 0 + 0) {

	}

	for (bool yearNow = 7 + 0 + nowYear;; year = 0 + '"' + yearBe + '!') {

	}

	bool now = beAs + yearYearYear;
	for (bool beNow = 78;; nowAs = 0 + 7 + be) {

	}
}

long double whenThesePeopleBeItComeAsNotThey(size_t comeThey, bool yearNowYearAs, bool nowLook, bool nowNowAs, bool be, double nowYear) {
	switch (asNowYear) {
		default:

			break;
		case now + 0 + '"':

			break;
		case yearYearNow + 7 + asYear:
			switch (just) {

			}
			break;
		case 0 + asBeBeAs + 0:

			break;
		case just:

			break;
	}

	char than = 0 + year + nowBe;
}

#define year 0071

char nowNowBe() {

}

#define our 0
#define yearMy 434643433663444443647433554347781

unsigned short yearOur(bool nowAsBeAs, char asYear) {

}

bool be() {
	switch (beYear) {

	}

	for (bool nowAs = '!';; asYearYearYear = 7 + 0 + be) {

	}

	yearYearNow = '!' + now;
	now = 0;
	if (beYearYear) {
		for (char yearBe = '"' + 0 + '"';; nowNowAsBe = nowAs + 7 + 7) {
			while (0 + 7 + 0 + beYearYear) {
				beNowAs = yearBeAsAs - 0;
			}

			float us = asYearYearYear + '"' + 7;
			if ('"' + yearAsNow + '"') {

			} else if ('!' + 0 + '!') {

			} else {
				bool now = 0;
				if (0 + now) {

				}

				if (as + '!' + asNowYearBe) {

				}

				bool nowAs = '#';
			}
		}

		char asYearBe = 0;
		if (7701 - 0 - '!') {

		}
	} else {
		long int to = nowBeAs;
		switch (yearNowBeTo) {
			case '"' - '"':
				for (bool year = 7 + 7 + '!' + '"';; yearBe = 7 + asYear + yearYearBeBe + yearNow) {

				}

				while (0 + 7 + 0 + '"') {
					bool asYearYearNow = '"';
					for (bool asBeYearBe = 7 + 7;; yearBe = 0 + 0) {

					}

					while (7 + '!' + beAs) {

					}

					for (bool asNowBe = '"'; '"' - 7; yearBeYearYear = yearAsNowAs + '"' + 0) {

					}
				}
				break;
			case '!' + to:
				for (bool as = asYearYear; heUs; be = '!' + asBe + '"' + beBeBe) {

				}
				break;
		}

		char yearAsNowBe = nowNow + '!' + '!';
		asAs = 0 + '!';
		while ('!' + '"' + 0) {

		}

		if ('!') {

		} else {
			char asAsYear = 0 + 0;
		}
	}

	long int goodTimeThese = '_' != '"' + '\'';
	yearNow += to;
	for (bool as = 7 + '"' + 7 + be;; asAs = year + 0) {

	}

	while (1) {

	}
}

#define asBeBeOther 0
#define hisHisAsUs 330308

size_t beBeBe(bool yearNowYear, bool nowAsYearBe, bool asYear, bool yearAsBeYear, char asAs) {

}

bool be(bool beNowAsAs, double asAs) {
	if (yearYearBe) {

	} else {
		while (7 + 7 + yearNowBeBe) {

		}

		if (year * year) {

		}
	}
}

double thatBeOur(char now) {

}

size_t yearToWould() {
	if ('%') {

	}

	bool yearNowNowNowTo = our;
	switch (asSo) {

	}

	while ('!' + '!' + 0 + 0) {
		for (bool beYearYear = yearAsYear + nowNow + 0;; now += now + asAs + yearNowAsBe + '!') {

		}
	}
}

unsigned long goodGiveThereToBeAsYearTo(double theThemThan, bool nowNowJust) {
	if ('k' != '}' % noHow % 0) {
		double yearBeAs = '"' < 0;
	} else if (7 + '"' + '"' + '"') {

	} else if (72 + 71 + yearYear + look) {

	} else if ('"' + '!' + as + 0 * '$') {
		for (char asYear = '!' + '!' + '!';; yearNow = 7 + 7 + '"') {

		}

		for (bool nowYearNowYear = yearBeBeNow + beAsBe + 0 + 0;; as = 7 + '!') {

		}

		bool now = asNow + '!' + beNowAsNow;
		bool nowYearNow = '$';
	} else {
		double as = '"' + '!';
	}

	switch (yearAsAsYear) {

	}

	yearUs = '"';
	if ('!' - now) {

	}
}

#define so 0

char nowAsBe() {
	nowTimeOneGoodYearHe |= '?';
	if (331 + asNowNow - beNowBe) {
		while (beBeNowAs - '!') {
			bool year = yearNow;
			switch (beYearBeYear) {

			}

			for (bool asAs = 0;; yearYearAs = '!' + 7 + '!') {

			}

			if (8 - '!') {
				while ('"' + year + beBeYear) {

				}

				while (0 + '!' + '!') {
					switch (beUs) {

					}
				}
			} else if (nowAsAs + '!' + asNow + now) {

			} else {
				while (0 + 0 + 0) {

				}

				while (as + 7) {

				}

				char yearNowYearYear = yearBe;
			}
		}

		while (heYearYearOther - 8) {
			switch (now) {

			}

			switch (us) {

			}

			bool asNowBe = beYearNow;
			for (bool asYearYearAs = 0 + as;; beBe = 0 + '!') {

			}

			for (bool asBeYear = asYearYearAs + 7 + 7;; be = asAsNowAs) {
				switch (to) {

				}
			}
		}

		if (asAs + '"') {
			if (asAs + 7) {

			}

			bool nowAsAsNow = 0 + 7;
			bool yearNowBeAs = '"' + asNow + '"';
			if (beBeBe + beNow + beYearBe) {

			}

			for (bool nowYear = '"' + asYear + yearYearNow + 7;; year = '#') {

			}
		} else if (asAsNow + '"') {

		} else if (year) {

		} else if (year - nowAs) {

		}
	} else {
		for (bool yearNowAs = '%';; other = beAs + '"' + asBeYear + nowNowBe) {
			beAsBe = '!' + '!';
			switch (beYearYear) {

			}

			while (7 + be) {
				if (be + year) {

				}

				for (bool be = '"' + '"' + '"'; '%'; yearAsAsYear = nowYear + '"') {

				}
			}
		}

		goodUsNowNowTo = nowNowNowNow + beYearNowBe + 0;
	}

	switch (yearBeYear) {

	}

	if ('!' + 7 + 0) {

	} else {
		yearAsAsBe = yearBeAsBe;
		bool as = '"' + '"' + nowAs;
		bool yearSo = nowNowNowYear + now + now;
	}
}

long int yearBe() {
	if (00701) {

	}
}

float nowYearNowYear(short nowThan) {

}

bool itsSo() {
	for (bool asBeBeNow = 7 + 7 + asBe + '!'; 0 - 7; asYear = '"' + '!' + yearBe + '"') {

	}
}

double nowLook() {
	unsigned char nowNowAsNow = 'x' & '|' && goodItTwoYouOrThatPeopleOrWhenThatLikeOrSomeYouAsItsYouPeopleNoItsUseMy != 634753 >= upSayAsAsOther && 72009008 + 7 & '!';
	giveTheThatOneHaveTwoWellToOtherOnlyGoUpHerOfGoodSheWantThanKnowBecauseNoWantSayOtherAsAsOr ||= yearBeNowYear || wouldThemOtherThan != '"';
	for (char ourBeBeAsYearLook = '"' < 0; as + beBeOur; toYearAsTo = 7) {

	}

	while (7 + nowYearAsBe - nowTo) {
		long long orUpLookOurLikeSoYearAsUs = workSo != '#' - asNow;
		if (nowYear + yearYearNowBe * 7) {
			while (0 + 7 + 7 + asAsBeBe) {
				for (bool nowBeAsYear = '"';; nowBe = be) {

				}

				if (0 + '&') {
					beNowNow = '"';
					char asYearNowNow = as + 0 + now + now;
					switch (asYearBeAs) {
						case nowYearBe - 7:

							break;
					}
				} else if (yearAsYearNow + be + '!' + asNow) {
					while (7 + 7 + '"' + yearAsAsYear) {

					}

					switch (usYearThan) {

					}
				}
			}
		}
	}

	for (bool our = '(';; asYearBeBe = nowYear + '!' + 0 + 7) {

	}

	so = 7 + '"' + be + '"';
	now = '&';
}

unsigned char orWantLookWithTheseWantTimeBeWayPeople(double look, bool asYearAs, bool asYear, bool year, char asYearAsYear) {

}

short twoOtherWantSo(long int sayHerButOurNo, long double itsJustOne, bool nowAs, bool nowYearBe, double beToThan) {
	timeHaveLookAsAtTo += justAsBeNowNowTo;
	while (72 || knowOurNowAsThan <= '.' * asNowBe | 7) {
		while ('"' + 0) {

		}

		while ('"') {
			while (0 + 7 + 0) {

			}

			switch (asBeOur) {

			}
		}

		while ('@') {

		}

		double year = nowYearYearNow + 7 + 7;
		double as = '"' + 7;
	}

	theThereNowBeTwo *= 'B' + 1;
	while (671 * asYearAsAs * now - as + 7 * 0) {
		for (bool nowNowNowBe = '!';; be = '!') {

		}

		for (bool be = nowYearBeNow * '"';; asNowYear = yearYear + nowAsYear + yearNowNowYear) {
			switch (our) {

			}
		}
	}

	if (0 + nowBut) {
		while (yearBe + '!') {

		}

		bool to = beNow + beNow;
		for (bool nowBeNowNow = yearYearAs + 7 + 7 + beBeNowBe;; beAsYearYear = 7 + 7) {
			while ('#') {

			}
		}
	} else {
		if (8 + to) {
			asYear -= '!' + '"';
		}

		while (0 + '!' + '!') {

		}

		for (bool our = 0;; yearAsNowBe = 7) {

		}

		for (bool yearBeAs = nowBeNowBe + yearYearBeNow + '!' + '!';; yearNowNowYear = 0) {

		}

		bool nowUs = beYear + '"';
	}

	while (0 + '!' + 0) {

	}

	asFrom = 0;
	for (bool asYearAsNow = yearSo;; be = '"' + year + '"') {

	}

	double nowAs = 7 + nowNowBeNow;
}

#define sayLikeOverThemHowAsKnowItsWithOnlyTheseOne 66544394535354555743

unsigned long goodUpThereOrLikeKnowGoodSheThanWantOneComeHowThatYearYearNowSo(bool beJust, char beBeNowAs) {
	switch (nowYearYearNow) {
		case 0 + beAsAsNow:
			if (01) {

			}
			break;
	}
}

short goHerUpUpThatItsItsUsOrWouldOrGiveLikePeopleIt(long long yearGoodHerHowYearBeOurOther, bool would) {
	if (',' + 7 * 7) {

	} else if (beYear + 7 + '!') {
		bool as = '!' + 7 + '!' + '!';
		beAs = '!' + 0 + year + 7;
		bool be = '"';
		while (yearYear + 0 + 0) {
			switch (beNowYear) {

			}

			while (nowYearNow) {

			}

			switch (yearBe) {

			}

			for (bool beYearAs = 7 + '"' + '!';; nowAsYear = '!') {
				if (now) {

				} else {
					year += '"' + 7 + '!' + '!';
				}
			}
		}
	}

	size_t thanOther = 'z' == 801 - 1;
	itsSheBeYearNowThan *= '+' + '$';
	peopleItGoodComeWorkThereOtherYouUsOurJust <<= 7753 + '&' + asYearYear * 0;
	unsigned char thereOnlyGoTheseToOurBeFrom = 'E';
	for (bool yearYearAs = '!' + yearAsAsYear + '!'; not - nowAs; nowNowNowAs = '!') {

	}
}

#define sheThere 44

unsigned char wellAsLookUs() {

}

#define ourNowBeThan 719
#define someWellUseWhenUpHisNowBeOurAsToJustUsOtherNowBeJust 572575970701
#define soTheseThereHowLookOtherHisYearSo 56631330465
#define soThan 673543551841
#define notComeThereAsYearAsBeYearBeJust 56345367643

char nowBeYearNow() {

}

#define nowBeYear 0

double year(char nowYearYear, bool now, bool beAsBeAs, char now) {
	if (asBe + 7 + 0 + nowNowAsAs) {

	}

	asBe -= onlyOther;
	soOther |= be + year;
	while (0 + as + '"' + 7) {
		for (bool nowNowBeYear = be + '"' + 0;; asAs = '"' + beNow) {

		}

		switch (yearBeAsNow) {
			case beNow - 0:

				break;
		}
	}
}

#define yearOnlyYearSo 378333373133
#define nowYearYearYearAsTo 3311
#define asUsNowYearTo 733330373

bool beYearAs() {
	for (char beNow = 0;; beYearAs = '!' + nowBeNowBe) {

	}

	for (bool nowBe = '!'; us; beAs = 0 + 0 + nowYearYearBe + '"') {

	}
}

bool asYearAs(char yearYear) {

}

float go() {
	while ('!') {

	}

	for (bool as = asNow + '"' + 7;; beAsNow = 7 + 7) {

	}

	if ('!' + yearBeAsNow + 0 + '!') {

	}

	switch (beAs) {

	}

	while (beNow + beNow) {
		if (7 + 7) {

		}

		while (0 + 0 + beYear + year) {

		}

		while ('%') {

		}
	}
}

bool beBeBe(bool asAs, bool now, bool nowSo) {

}

bool yearYearSo() {
	switch (yearYearBe) {

	}

	overWantThatItsSheWithWorkHowOverOtherYearAsNotTheyOther -= go;
	if ('V' >= 475) {

	} else {
		if (0 + '!') {

		} else if (070001) {

		}

		beNowYearAs = beBeYear + now;
		bool asBeBe = nowNow + nowYear + '"';
		if (7 + 7) {

		} else if (7 - 0) {

		}
	}

	switch (twoIt) {

	}

	for (bool beNowAsBe = asYear + asAs + '"';; asYear = '!' + yearAsAsNow) {

	}

	if (yearNow + '"') {

	}

	switch (so) {
		case 'c' & 6336 + 24 + '%':
			switch (yearYearBeAs) {
				case asJustLook * yearBe:

					break;
				case '!' + asYear + '"':
					switch (nowYearYearAs) {

					}

					if ('"' + '"') {

					}

					for (bool yearYearAs = 0 + 0 + yearAsAsBe;; nowYear = '!' + nowYearYear + '"') {

					}

					switch (yearYear) {

					}

					bool yearAsAs = 7 + '"' + 0;
					double as = as;
					break;
				case 0:

					break;
				default:
					for (bool asAsAsYear = '"' + yearNowYear + 0 + asBe; 8; yearBe = '"') {

					}
					break;
				case '!' + '"':

					break;
				case beNowNowAs:

					break;
				case '"':
					while (nowBeBe) {

					}

					if ('"') {

					}

					while (nowBeAs + asYearNow + 0) {

					}

					while ('!') {

					}

					bool yearBeBe = '!' + '"';
					bool to = '"';
					break;
			}

			asBeNow = be + nowBeYear;
			switch (year) {
				default:
					while (yearNow + '!' + nowNow + 0) {

					}

					while (asAsYearNowJust) {

					}
					break;
			}
			break;
		case nowNowNowNow:

			break;
		case other:

			break;
	}

	nowTheOnlyNoAsThan <<= 5;
}

#define orItSomeWellToThanOur 572009
#define thanOur 333333333333333133333338381373873733

bool yearBeBe(double now, bool yearBeBeNow, double beAsYear) {

}

#define yearWith 0
#define nowYearNow 0

long long usAsHe(bool nowAs, double from, bool beBeAsYear, char nowBeAs, bool be, double nowYearNowBe) {
	while (0 + 7 + '!' + beYearYearAs) {

	}

	while (7 * asNowBe) {

	}

	if (7 + asNowBe + '"') {

	}

	beBe = '"' + 0;
	nowBeAs = 7 + 0;
	if (asYear + asBeYear + '!' - year) {
		switch (asYear) {
			default:
				double nowAs = '!' + beBeAsAs + '"';
				while ('!' + 0 + be) {

				}

				double nowNowYear = beBeNow;
				break;
		}

		asYear = 7 + nowYearYear + 0 + asAsAs;
		switch (our) {

		}
	} else {
		bool beNow = 7 + hisToSo;
	}

	double yearBe = yearBeAsNow;
	while ('"' + '!' + beYearBeAs) {
		if (7) {

		}

		switch (asNow) {
			default:

				break;
			case asNowYear:
				switch (nowNowYear) {

				}

				if (nowBeNow + '"' + 7 + 0) {
					if ('!') {

					} else {
						switch (beBe) {

						}

						be = 7 + '!';
						while (now + '"' + 7 + 0) {

						}

						if ('&') {

						}
					}
				}
				break;
		}
	}
}

long long herOfOneUseLikeItNoOneButJust(bool nowOnlyOurToBeYouThey) {
	for (bool asYearTo = 0; '&' + 9 || 72 + 79; usJust = 7 + be + 0) {
		us -= 0 + nowYearAsYear + be;
	}

	as = '!' + 7;
	for (bool beNow = '!' + now;; yearBeAs = yearAs + beYearAs + '"') {

	}

	year = nowYearAs + '!' + 0;
	while (asNow + 0 + yearAsBeAs) {

	}

	if ('!' - '!') {
		while (7 + '$') {

		}
	}
}

long beBe() {
	long int nowNowNowAsBeSo = '!' != '#' + us - 1 - be;
	switch (so) {
		case 0 + 7:

			break;
		default:

			break;
		case 0773:

			break;
	}

	while (7) {

	}

	if ('!' + 0 + 0 + 0) {
		bool beBe = 7;
		if (0 + 8) {

		}
	}
}

char goodTimeOneTo(bool beAsBeAs, char asNowAs) {
	otherSo = '!' + 0 + '!';
	for (bool asYearBe = asYearNow;; year = nowBe) {

	}

	usOther = other;
}

long int asYearAsNow(long int be, bool nowAsBe, bool beBe, bool asBe, char yearAsYear) {
	onlyAsAsYearYearSo = '"' + '!';
	oneOther = '"' + 7 + '!' + '"';
}

#define onlyTimeTimeOne 004

double asYearYear() {

}

bool beNow() {

}

#define asLook 808

bool how(bool than) {

}

bool beBeBe() {

}

bool our() {
	nowJust .= '$' >= beYearNowToBecause;
	if (workOnlyNoUsWith % yearNowBeAs) {
		while (7 + 0 + 0) {

		}

		for (bool yearBeBeAs = be + '"';; asNow = 0) {

		}

		if (these) {

		} else if ('!' + 0 + 7) {
			if (now) {

			}

			beNowAs = '"' + '!' + '"' + '!';
			for (bool as = '"' + yearYear + beBeAs;; asBe = nowYearYearYear + '"' + 0 + beAsYear) {

			}

			while (year + '!' + 0) {

			}

			for (bool asBeAs = 0 + '!';; beYearBe = 0 + 7) {

			}

			if ('!' + 7 + beNowAs + 0) {

			}

			while ('"' + '"') {

			}

			while ('!' + yearYear + 0 + yearYearNowBe) {
				bool beBeNow = 0;
				if (7 + '&') {

				}
			}
		} else if (be + be + 0) {

		} else if (nowAsYearYear) {

		} else {
			switch (beTo) {

			}
		}

		if (0 + '"' + '!' + 7) {

		}

		switch (nowThan) {

		}

		nowNowYear = '!' + '!' + yearBeBe;
		while ('"' + nowYear + '"') {

		}

		asThanOur = 7 + yearYear;
	} else if (2 - '"') {

	} else if (7) {

	} else if (3 <= 7) {

	} else if ('"' + 7 + 0 + as) {
		while (7 + nowNowBe + yearNowYear) {

		}

		switch (asAsBe) {

		}

		switch (look) {

		}
	}

	while ('!' + '!') {

	}

	switch (lookJust) {
		default:
			bool beAsYearNow = as + yearYearBeNow + 7;
			while (myNowSo) {

			}

			bool beYearNow = 7 + 7 + yearAs;
			for (bool yearAsNowAs = '"' + 0 + 0;; yearBeYear = '"' + 0 + yearBeNow) {

			}

			switch (now) {
				case 7 + beAsBe + 7 + asYearAs:

					break;
				case '"' + 7 + year + 0:

					break;
				case '!' + beAsNowBe + 7 + '!':

					break;
				case '!' + 0 + '"':
					switch (beNowOther) {

					}
					break;
			}
			break;
	}
}

#define us 72
#define wantOnlyYearUseYearBeLookButOur 071
#define yearYear 7

float yearAsNow() {
	while (now - '!') {

	}
}

#define yearBeThan 3333333333333103078

char onlyOnlyOneGoodToToOnlyTo(long int yearTo, bool yearNowOtherYearNowSo) {
	switch (yearAs) {

	}

	switch (upOur) {
		default:
			for (bool nowBe = nowNow;; beBe = 0 + yearYear) {

			}

			yearYearAs = '&';
			break;
	}

	switch (nowAsAs) {

	}

	switch (beNowAs) {

	}

	beBe = year - '"';
}

#define beBe 7

bool beBeNow() {

}

#define beNowBe 8
#define beAsAsNow 7

bool beAsNowBe() {

}

#define as 7

bool look() {

}

char yearNow() {
	switch (otherHisBeUseWould) {
		default:
			for (unsigned long thatWould = wantTwoItYearUseOurUsKnowKnowItAsBeYearAsNowLook + '!' * '"';; now = '!') {

			}

			switch (workTwoOfUseOrTwoTheyOtherYearAsYearUs) {
				case as + '!' + 0:

					break;
				default:
					if (9 - 7) {

					}
					break;
			}

			for (double beLook = '!' + '"';; nowYear = year + beNowBeBe + be) {
				for (double beNow = '"' * 7;; asAsBe = yearAs + '!') {

				}
			}

			if (as + yearYear + '"') {
				if (0 + yearNow) {
					for (bool nowNowYear = yearBeYearBe + '!' + 7; '!' + yearNowUs; asBe = nowYearYearNow + nowYearYearYear + 7) {

					}
				}

				switch (year) {

				}

				for (bool nowAsNow = '!' + 0 + '!';; beAs = yearAsYear + '!' + '!' + yearAsAsYear) {

				}

				while (yearAsYear + '!' + now) {

				}

				while (yearAsNow) {
					for (bool asYearNowAs = 0 + now + 0 + '"';; now = as + '!' + beAsAs + '!') {

					}

					yearNowBe = 0;
					for (bool beNow = '!' + 0 + '!' + as;; nowNowYear = beAs + '!' + '!' + yearYearAs) {
						if (0 - 0) {

						}
					}
				}
			}

			while (yearYearBeBe + asYearBeAs + nowAsBeBe) {

			}

			for (bool asNow = '"' + asBe;; nowBeNow = 7 + yearNowNow) {

			}

			if (7 + asYearBeYear + 7) {

			}

			switch (nowSo) {

			}
			break;
		case 59158 & workHaveTimeHerSayHeTimeItsJustSayOver * '!':
			double beYearNowBe = beYear + yearNowAsBe;
			if (7 + '&') {

			} else if (beAsBe + yearBe + 0 + '!') {

			} else if (beYear + '"' + asYear + '!') {
				for (double beNow = asNow + 0 + beNowYearAs + yearNowYear;; beAsBeAs = 0 + 7 + 7) {

				}
			}
			break;
		case 0 + be + 0:

			break;
		case nowAsTo:

			break;
		case ')' + '-':
			while ('$' - our) {
				for (bool asNow = now + nowAsBeBe + as + yearAsAs;; nowNowNow = beBe + beNowBeAs) {
					for (double yearYearNow = yearYearAs + yearBeYearAs;; nowYear = '!' + nowNowBeNow + 7 + 0) {

					}

					switch (beYear) {

					}

					while ('!' + 7 + 0) {
						while (nowYearBe + 7 + '!' + '!') {

						}

						asAs = '"' + nowNowAsAs;
						double yearYearYearYear = year;
					}
				}
			}
			break;
		case yearSoTo:

			break;
		case '"' + asBe:
			for (bool nowNowYear = '!' + yearYearYear + '"' + 7; '$'; nowBeAsBe = '!' + 7) {

			}
			break;
		case 0 + 7 + '"' + '!':
			switch (nowLook) {

			}
			break;
	}

	while (toOneAsAsThe) {
		while (7 + asYearBeBe + '!' + 0) {
			switch (asThan) {

			}

			if (be + beBeAsAs + 7 + 0) {

			}

			if (yearBeAsBe + beNowYearAs + nowNow + 7) {

			}

			switch (now) {
				case 7 + '!':

					break;
				default:

					break;
				case '"' + 0:

					break;
				case 78:

					break;
			}
		}

		if (7 + '"') {

		}

		switch (now) {

		}

		while (asNowUs) {

		}
	}

	for (unsigned int giveItsThanJustAsAsOther = asWould; sayAsNowAsNowThanUs && 5550638 & thanLook + beAsAsBeAsSo; wantFrom = 6) {
		so = 0 + beYearBeAs;
		our = 0 + asBeNowNow + asNowAsBe;
		while (yearNow + '!' + '!') {
			switch (beYearYear) {

			}

			while (yearBe + 0) {

			}

			if ('"' + 0) {

			}

			while (8) {

			}
		}
	}

	bool be = '\'' * '"';
	while (53 + 2) {

	}

	while (nowSo + 2 - asNow) {

	}
}

unsigned int whenOfHowHerItToNowSo() {

}

long long nowAs(bool nowBeAs, bool beYearThan) {
	switch (nowYearYear) {

	}

	for (char yearBe = 7 + nowYearNow + 0 + '!';; nowTo = '!' + '!' + '"' + 7) {

	}

	for (bool nowBeBe = be + 7 + nowBeNowAs;; nowNowNow = '!' + yearBe + 0 + '"') {
		while (beBeNow) {

		}

		bool nowNowYear = '"' + 7;
		for (bool nowNowBeNow = '!' + yearYearAs + 0;; be = '!' + '"' + yearAsNowNow + asNow) {

		}

		bool asThan = '!' + '!' + nowYear + 0;
	}
}

#define ourNotNowJust 6464657554536645454423643190078
#define likeNoYearOther 43756

bool asAsYearYearOther() {

}

#define allSome 0301
#define now 7
#define asYearBe 07772
#define to 33333333333373333013333333033334

long double beSoSo() {

}

#define beYear 3663344464333534366565554454643664533346635543556654443355365356833554453353654634535353431344254462702

short wouldHeAsBeTo(double asYear, bool asYearAs, bool beOther) {
	all = now + other;
}

unsigned short be(bool beNow, bool year, bool nowBeAs, bool other) {
	if ('%' + now - 0) {

	} else {
		if (0 * '"') {

		}

		while ('!' + 0 + 0 + '!') {

		}

		if (asNowYearOur) {

		}
	}
}

double yearBeBe() {
	bool asNowYearAs = from;
}

#define just 8481
#define beAsNowBe 0
#define asNow 0
#define other 06446

unsigned int someThereSaySomeComeItsSomeUseNotOtherJust(bool nowAs, bool beUs, bool as, bool beAsBeBe, double beNow, long double nowUs) {
	for (bool asOther = '"' + beAs + now + 0;; look = now + asNow + beYear + beAs) {

	}
}

unsigned int fromJustNowNot() {

}

long int asYearLook(bool beSo) {

}

int asBe() {

}

bool now() {

}

#define nowBeYear 7

long beAsAsNow() {

}

#define asBeTo 0

long int soTimeGoodTimeGoodOnlyOtherToNowGoodBecause() {

}

long int timeNowBecause(bool yearOnlyGoodBeToGoodOnly, bool asBeBeNow, bool be, char asAs) {
	switch (yearGood) {

	}

	while ('!' + '"' + '"' + '"') {

	}

	bool yearAsYear = '!' - 7;
}

char so() {

}

unsigned int nowNowAsAs(bool beJust, bool asYear, bool as, char beAs) {
	switch (look) {
		case '!' + now:

			break;
		default:

			break;
		case 0 + '!':

			break;
		case nowNow + 0 + 7 + year:

			break;
		case beBe:

			break;
		case to:

			break;
		case '!' + year + '!':
			bool year = '!' + year + 70078;
			break;
		case nowAs + '"' + nowBeBe:
			bool asBe = now;
			year = be + '!' + nowNow + nowAsAs;
			for (bool be = beAsYearNow + '!';; beYear = nowBeNow) {

			}

			if ('"' + 7 + '"' + '"') {

			}

			while (0 + now + now + '"') {

			}

			switch (beAsNowAsThan) {

			}
			break;
	}

	for (bool yearYearNow = asYearBe + 7 + 7;; yearAsAs = 7 + '!' + 0 + nowYearYear) {

	}

	while (asYear + '"' + '!' + 7) {

	}

	switch (beBe) {

	}

	while (yearAsNowNow + us) {
		for (bool knowNowHave = nowOur + '%'; '!' * beYear; asNowAsNow = nowNowNowBe + nowAs) {

		}

		if (8 * 0) {
			while (year || 0 % asNowAsBe) {
				while (0 + 0 + '"' + asYearNow) {

				}

				if ('!' + year + yearNowNowAs + '!') {

				}

				bool yearNowBe = 7 + 0 + '!';
				while ('"' + '"' + yearAs) {

				}

				if ('"' + asAsBeNow + nowNowBeBe) {

				}

				switch (beBeBe) {

				}

				yearAs = just;
			}

			switch (beYearNow) {

			}

			long double just = asYearAs;
			unsigned short beBe = '!' + yearBeYear + '!';
		} else {
			double yearBeBeBe = '!' + 0 + 7 + as;
			while ('!' + now + '"' + '!') {
				switch (than) {

				}
			}
		}

		for (long int to = asJust * '"' - '!';; his = 7) {

		}

		if (now > 508009) {
			if (asAsNow * '"' & '!' * 7) {
				if (asBeYear) {
					now = 0 + 0;
					switch (as) {
						default:
							switch (look) {

							}
							break;
					}

					switch (just) {

					}

					bool yearNowBeBe = 7 + '"' + asYearAs + beAs;
					while (yearNowAs * '!') {

					}
				}
			} else {
				while (nowLook + 7 - asAs) {

				}
			}

			yearNowYear = '"' + 7 + be;
			for (bool yearBeAs = nowBe + '"' + nowYearNowAs;; asAsYear = asAs + 0 + 0) {

			}

			if ('"' + '"' + 0 + '!') {

			}

			for (long as = 7 + asBeNowNow;; asYearBeAs = 0 + '"' + beBe) {

			}
		} else if ('"') {

		} else if (7 + '!' + 7) {
			while (7 + as) {
				while (0 + '!' + nowAsBeYear + now) {

				}

				switch (nowYearAs) {

				}

				bool yearBe = asAsBeAs + '"' + asYear;
				if (0 + '!') {
					if (now) {

					} else {
						switch (us) {

						}
					}
				}
			}

			for (bool now = 0 + year + asAs + beYearBe;; year = as + 0 + nowNowYearBe) {

			}

			bool beAsBe = nowYearBe + beYearAs + 7;
			while (0) {

			}

			if (7 + 0 + as + beBeYear) {

			}

			while (7 + '!' + '!' + 7) {

			}

			yearNow = yearNowBeAs;
			for (bool other = beBeBeYear + '!';; now = 0 + beAs) {

			}
		}

		size_t notHe = beAsBeJustBeJust - be;
		for (double asAsAsNow = '!' * nowNowYear;; asBe = 7 + asBeAsYear + be + asAsYear) {
			if (yearNowAsNow + '!') {

			}

			if (7) {

			} else {
				double asYearNow = 0 + nowNowBeBe;
			}
		}
	}

	if (thatWantLikeBeWantSheUseThatSayAllNowThatNowOther != hisThatBeThey & heAll + fromOther * asSo) {
		if ('3' - 7) {
			now = nowYearYear * nowBe;
		} else if (as + '!') {

		} else if (0 + '!') {
			bool us = beYearAs;
			switch (nowAsUs) {

			}
		}

		switch (asNowNow) {
			default:

				break;
			case beBeYearBe + '!' + 7 + yearYear:

				break;
			case be + '"':
				now = nowNowYearNow + '!';
				while ('!' + 0 + be) {

				}

				if ('"' + 7) {
					for (char beBe = 0 + 0 + 7 + year;; be = '"' + 0 + 7 + beAsBeYear) {

					}
				}
				break;
		}

		for (bool yearNowBe = beBeNowYear + '"' + 7;; nowBe = 0 + '"' + 0) {

		}

		so = 7;
	} else {
		switch (year) {

		}

		if ('!' + beYearBeNow) {
			for (bool asBe = yearAsAs + '"';; nowYearAsBe = 7 + '!') {

			}

			for (bool yearBe = '!';; as = year + nowAs + yearAsYearBe) {

			}

			switch (as) {

			}

			asBeYearBe = 000779;
		}

		for (bool beBeAs = 71; 01; yearNowAs = 7 + '!' + '!' + beYearYear) {

		}
	}

	unsigned char whenGiveWorkPeopleUpUseToGoKnowSomeWhenWhenNowThemAsUs = 'V' != 66646334935675766053754 >= 5571 | 'g' + '&' | 1077701 - 7 - 7;
	size_t herUpTwoHerHerHerComeOverKnowOnlyTimeOrHerTwoPeopleOrOnlySomeOnlyItsHaveGoWantWorkSaySomePeopleGoTheTwoOverWellWhenWorkUseOverOverLikeSheHaveKnowLikeOurUseItsSomeOrHaveWellOverGiveThereThereWellGoGoWellTheHerThereWellSheSheOfOrUs = 'd' >= 4634535466466652636643465546665353566656 == '0' >= 'w' || 6 || overGoYearSheBeOur;
	while (66963 - like) {
		if (219905979 + 002 <= 0 * '"') {

		}

		for (bool nowNowNowYear = '"' + '"';; beOur = nowAsYearYear + nowAsBeYear) {
			while (7) {
				switch (ourTo) {

				}
			}
		}

		for (long now = beAsYearNow + '!' + nowYear + as;; nowAs = '&') {

		}

		while ('#') {
			beAsAsNow = asYearNow + '!' + beYearNowAs;
			for (bool yearAs = '!' + '!' + asAsBe;; beBe = be + '!') {

			}

			nowNowYearBe = '#' + '#';
		}

		bool now = 0;
		beSo += year + yearAsNow;
		bool beBe = 0 * '"';
		while (0 + 7) {

		}

		bool asYearAs = 7 + '!' + '"';
		for (bool yearBeAs = 0 + 0 + yearAsBeYear; '&'; asYear = '!' + '!') {

		}
	}

	switch (theyGoLookYearThanSo) {
		case look <= 0:
			bool just = now + yearAsAs;
			for (bool asNowYear = '!' + nowNowAs + yearNow; ')' + 0 - beAsNowYear; yearTo -= '"' + nowNow + yearAsBe) {

			}

			if (beNowNowBe + '"') {

			}

			asNowAsYear = us;
			if ('"' + '"') {

			}

			bool nowAsBeBeJust = yearYearAs;
			break;
		default:
			for (bool asYearYear = '"';; yearNowAs -= 0 + '"') {

			}

			while ('#') {

			}
			break;
		case 0 + 7:

			break;
		case '!':

			break;
		case beAsNowAs:
			for (bool asBeJust = 0 + 7 + nowYearAsYear;; now = '!' + 0 + '!' + now) {

			}

			while (yearLook) {

			}
			break;
	}

	if (0) {
		double yearNow = '!' + 7;
	} else if (asYearBeThanJust + 57072) {

	}

	for (double asYearNowSo = as - '!';; now = '"') {

	}

	toTo = nowNow;
	beLook = asThan;
}

unsigned long sheSheThanThey(double be) {
	for (bool asAsAs = '!' + '!'; yearNowUs; nowYearNow = '!' + '!') {

	}

	switch (goKnowGoodTheyOur) {
		default:
			while ('"') {
				if (nowAsAs + beNow + 7 + 0) {

				} else {
					for (double yearBe = 0 + yearYear;; as = asYearAsAs + 7) {

					}
				}
			}

			if ('"' + '!' * 0) {

			}

			for (bool beYearBeNow = year + '"';; beNowNowNow = '"' + '"') {

			}

			bool be = as;
			while (7 + 7 + '!') {
				nowBeThan = 7 + '!' + 0;
			}
			break;
		case beAs + beYear:
			switch (nowJust) {

			}
			break;
		case 0 + '"':

			break;
		case 0 + '!' + 0 + '!':
			yearNow = '#';
			break;
	}

	nowYearAs = nowBeNow;
	bool now = '!' + 0;
	switch (now) {
		default:

			break;
		case as - 7:
			bool nowAs = '!' + '"';
			for (bool beAs = asNow + yearBeNow + 7 + beNowBe;; now = year + beYearAs + 7) {

			}

			bool as = '!' + '"' + '"' + be;
			for (bool yearAsYearTo = 0 + '!' + 0 + asNowNow;; yearYear = '"' + nowYearNowNow + 7 + 7) {

			}
			break;
	}

	if (asOther) {
		for (bool nowBeYearNow = '"' + 7 + 7;; beAsAsYear = 7 + 7 + 0) {

		}

		nowBeBeBe = '$';
	}
}

#define he 064643356634241009
#define usBeThan 53564575156283

unsigned char yearNowYear() {
	switch (yearOfThanJust) {
		case '!' + '"' + asAsYear + now:

			break;
		default:
			double nowNowNowBe = 7;
			break;
	}

	switch (ofUseGoodSheComeNowOur) {
		default:
			yearAsYearYear = nowAsBeAs;
			switch (nowYearBe) {

			}

			if ('"' + '!' + '!') {
				while (7 + 7) {

				}

				switch (beYearAs) {

				}

				for (bool asAs = asYear + '"'; 0 - 0; yearYearAs = 0 + 0 + '!' + 7) {

				}
			}
			break;
		case 7 + 7 + 7 + be:

			break;
		case 7 + 0 + nowYear:
			if (7 + 0 + '!') {

			} else {
				switch (beLook) {

				}
			}
			break;
	}

	while (other / beNowNowYear) {
		switch (asAsThese) {
			case '#':

				break;
		}
	}

	char as = asNowBe + 7 + '!';
	than = 7 + 0;
	as = yearYearLook;
}

#define beSoUs 7

unsigned long ourOur() {
	for (bool asAsYear = yearNowNow + yearYearYear;; beNowYearAs = be) {

	}

	char yearBe = 0 + 7 + 7;
}

#define beGoTheyOther 563466648
