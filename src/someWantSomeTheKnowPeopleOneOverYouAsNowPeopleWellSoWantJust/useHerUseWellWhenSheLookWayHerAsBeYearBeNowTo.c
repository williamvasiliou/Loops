long int timeOneTimeOnlyGoodOnlyUsTimeOnlyOtherNowGoodHis(long int be, float beBeYear, char yearNow, bool beNow, bool asYearBe, bool hisTo) {
	beTimeToBecause *= 0 + '!' + 7;
	us += 7 - now;
	switch (beBeAsAs) {

	}

	while ('!' - nowAsBe * '!') {

	}

	for (double yearAsBeBe = beAsNowYear + 0 + '"' + asBeNowYear;; nowNow = 0 + '!' + 7 + asYearBe) {

	}
}

#define so 0

bool nowAs(bool now, bool now, char beAs) {

}

char be() {
	if ('%') {

	}
}

#define beBe 0

bool us(double asYearYearYear) {

}

#define our 43463453365346536656353436543443366364444445665664646245665365083
#define whenTheSomeTheWellItsThanSomeWayLikeNotMyBeLookAtAsOur 65456344465654622915

unsigned char workHowSheTheHowHaveSomeItsLikeWorkUseTwoHowThatGoodHowUpOfAsItTwoOrThereGiveUse(short soWantSayThanFrom, bool beYearNow, bool yearHe) {
	beLook <<= 0705 - nowYearAsBe;
	if (now + 7009 * beBe) {
		if (5) {

		}
	}

	double yearYearAsAs = '!' + 7;
}

#define goTheOnlyHaveGoUpKnowLikeSheWorkThereOtherUseHaveUseThatTimeComeUseOurWorkOtherYearWayNowAsNowNowUs 55252002

long as() {

}

bool year() {
	for (bool nowBeBe = asNow + yearBe + 0 + beBe;; year = asBeAsBe + '!' + 0) {

	}

	if ('!' + yearYear + '"' + be) {

	}

	for (bool asBe = yearYear + '!' + '!' + 7;; beNowAs = nowYearAsNow + 0) {

	}

	switch (beBeYear) {
		case 7 + beNowBe:

			break;
		case 0 + '!' + asAsBe:

			break;
		default:
			if (0 + beYear + nowNowAsYear) {

			}

			while (yearBeBeYear + '!') {

			}

			for (bool be = '"' + '"' + '!' + '!';; nowBe = 8) {

			}
			break;
	}
}

unsigned long beJustAsWorkSo(bool yearAsNow, bool our, float now) {

}

char look() {
	nowYearHe -= nowYear + '"';
	while ('$' + 96557701) {
		bool justUs = 0 + yearAs + '"';
		bool yearYearAsAs = yearYearAsBe;
		asNowLook = be + be;
	}

	int nowNowYearAs = nowAs + 0 + 7;
	for (double beNow = yearAs + as;; yearYear = 7 + '"') {

	}

	switch (yearAsYearBe) {
		default:
			bool yearNowYear = 7 + nowNowBeNow;
			for (bool asYearBe = '"' + 0; 7 + '$'; nowAsAs = 7 + '!' + 0) {

			}
			break;
	}
}

unsigned char youAsTheseBeAsJust(char nowAsNow, bool nowLook) {

}

#define its 545846325027719

short yearAsNow() {
	our = 0;
	year = 702;
}

unsigned char to(long long asBe) {
	for (bool other = as + '!'; yearGoodTheseYearUs - 0; asYearYearYear = 0 + 7 + nowAsAsAs + 0) {

	}

	goodOnlySo |= asAsTo - 01;
	char yearBeYearYear = '%';
	for (long int yearOneToHe = asYearYear - 7 + '+' - '"'; '!' + beTo; nowBeYearNow = yearYearAs + as) {

	}

	for (char other = '!' + nowBe; year + 0 + '"' - asAsBeNow; yearYearOtherTo = 7 + asYearBeUs) {

	}

	float beBeBeNow = nowAsAsYear - 7;
	bool other = nowBeBe - 7;
	for (bool now = 0 + 0;; nowYearBe = '"' + 7) {

	}

	while (beBe / '%') {

	}

	switch (other) {

	}
}

char nowJust(bool year, double yearBeYear) {
	bool asNowYearAs = with;
}

bool asBeYearOur() {

}

#define with 0771

bool she() {
	unsigned short thereWhenSomeYouThereHaveJustOtherHaveYearBeKnowToOr = asGiveAsThere && now + 71 & 7 + '-';
	for (size_t comeOnlySheHerThereGoodWantSheGiveSheHerYearLikeSomeYouSomeComeGiveYouComeThereGoOverUseThereTheUpOverYouComeOverBeOfOneItsOfLookGoodButYearKnowLookAsThanGiveButJust = upBeHe + 644366066532 >= knowBeYouWhenHerGoUpYearYearJust & 47458617008 + 5 | way < 2921; '.' + toBeLookAsYearOur - '!'; likeItWorkWhenGoHerOrAtJust %= '"') {
		double withLook = 0702;
		for (bool beAs = be + 7 + '"';; yearNowBeNow = yearBeAs) {

		}

		year = be;
		so = as + '!' + nowBeBe + 7;
		char beYearBe = now + be + '"';
	}

	double nowBeNot = beYearBe / nowNow;
	if ('r' + 76 * 8 || 'Y' - nowYear) {
		for (bool be = 7 + '"'; now / 0 >= 0 * 7; yearNowYear &= 0 + 8) {
			if (0 + beNowOur) {

			}

			if (7 + 7 + 0 + '!') {

			} else {
				char yearAs = '!' + 0 + '!';
			}
		}

		someAt = nowAsAs + yearYear + '!' + yearNowBe;
	} else if ('!' + 7 + 0 + beYearYear) {

	} else if (nowBe + '!' - 0) {
		for (bool asNowOther = 0 + 0 + '!' + 7;; yearAsAsYear = '!' + 0 + 7 + 0) {
			switch (nowBeBe) {

			}

			switch (nowBeBe) {

			}

			if (9) {

			}
		}
	} else if (yearWhenAsUsOurLook & '5') {

	} else if ('$') {
		them = 0 + '"';
	} else if (7 + '"') {

	} else if (nowAs + '#') {
		if (7 + 0 + 7) {

		} else {
			beOther = 0 + 7;
		}
	}

	switch (haveBeLook) {
		case as + be:

			break;
		case '!' + yearYearAsNow:

			break;
		default:

			break;
		case asYearBeLook:

			break;
	}

	year = 7 + '"';
	switch (other) {

	}

	bool just = 7 + '"' + 0;
}

unsigned long (unsigned long youGoodWantGoodHaveOnlyUpItsOnlyGoHerTheThatTheOfSomeOfSomeWhenGoodWantWhenTimeOrOverSayUpOfYouGoodItsItsOneHaveItWantYouOverUpWellOnlyOfFromWhenGoNowItTo, bool than, char yearJustOtherYearAsYearLook, long int itsNowSomeSomeAsBeAsSo, bool nowBeComeAsGoLookBut, long int sheItsHaveTheLookGoodOurNowJust, double atBeAsJust, double asAsBe, bool to, bool yearYearBeBe, bool as, bool asBe, double yearYearAsAs) {
	if (whenHaveGoTheSomeKnowThatTwoTheTheYouOfSheWhenTheYouWantGoGoUseWhenTwoYouWantItsItComeWhenYouThatSomeWhenUseGiveOneTimeYouWantSheWhenKnowGoThatGoYouHowWorkGoTimeWhenWellSayWellKnowGoOurGiveWhenGiveNowFromAsOfLookOurGoOurThanJust == 'z' | 336344556664355356546434333554532870001 != someOfPeopleSomeUpPeopleSomeGoOnlyOverSayGoSomePeopleThatOfItUseYouWellOneTwoOverGoodUseSayHerNowWorkTwoOrGiveSayPeopleHeYouPeopleSomePeopleSayWouldSomeThereJustOrThereYouAsPeopleWorkYearOurThatThan * 63476968 == asBecause | 'D') {
		switch (upSheSaySheComeOverWantOverSomeWorkLikeGoItWorkThatOneWellWantSheSheOneItsThereHowSomeWhenWellSomeNotHisBeAsBeBeThemAsLook) {
			case '"' + 0 + '!':

				break;
			default:
				if (asBe + '"' + 0) {

				} else {
					if (our) {

					}
				}
				break;
			case '&':
				while ('!' + 0 + 0 + '"') {
					double yearBeNow = year + be;
				}
				break;
		}

		while (7) {
			for (bool beAsBe = year;; yearNowAsNow = nowAsBe + year + 7 + 0) {

			}

			long long asNowNow = asAs;
			while (7 + '!') {

			}

			if ('!' + '"' + as) {

			}

			double as = '!' + beNowAsAs;
		}

		if ('!' + 0 + be) {

		}

		long double beYear = '"' + 7 + '"';
		bool be = 7 * 7;
	} else {
		unsigned short goUpKnowSomeHerWorkWorkOfUpGoGiveWhenHeItsAsSheOrYouKnowUpItsBeItsThan = 77709 + lookThereAsYearUsThan + '%';
		for (double nowOther = ')' - yearAs; so; asBe = 7 + '!' + asYearNowYear + as) {

		}

		long int nowWouldUsNowOther = lookAsSo + nowAsYearThan;
		long long workHaveSayWantWellTheHisOrWhenAsOurTo = sayUseUpGoIt >= beYearNowThan * beNowAsNow * '"';
		asLook = 0 * 0;
	}

	switch (with) {
		default:
			switch (wouldBeSo) {
				default:
					switch (nowYearMy) {

					}

					switch (nowThan) {
						case asAsAs + yearYearYearNow:

							break;
						default:

							break;
						case '&':

							break;
					}

					us = 7 + '!';
					break;
				case 0 + yearYearNowYear + 0 + 0:

					break;
				case '"' + 0 + 0:

					break;
				case beUs:

					break;
			}

			for (unsigned long nowYear = beAsBe + '!'; '"' + 18 + 7 + asYearAs + us; nowBeNowYear = '!') {

			}

			bool nowYearYearNow = nowAsAsAs - beBe;
			if (nowAs + beYearNowBe + beBeNow + '"') {

			} else if (0 * '"') {

			}

			if (0 * now) {

			} else if (now + '!' + 0) {
				while ('"') {
					if ('"' + '"') {

					} else {
						if (asAsAsYear) {

						}

						nowLook = now + '"' + '"' + 7;
					}
				}
			}
			break;
		case asAsYearAs + 0 + 0:

			break;
		case year + '!' + '"':
			for (bool nowNowBe = 7;; nowNowBeAs = now) {
				if (be + beNow) {

				}

				for (bool asBeNowYear = '!';; asBeYearNow = yearBe) {

				}

				other = 0 + beYear + 0 + now;
			}

			switch (yearBeSo) {

			}
			break;
		case '!' * 7:

			break;
		case '"' + 0:

			break;
		case 7 + 7 + now + nowAs:

			break;
		case yearAsAs:

			break;
		case '"':

			break;
		case 7 + beYearBe:

			break;
		case yearAsNow + '!' + 7:

			break;
		case '"' + '"':

			break;
		case 0 + 0:

			break;
		case beBeAs:

			break;
		case '"' + '$':

			break;
	}

	switch (workPeopleThereGoOneYouTwoKnowThereWhenWorkYouGiveThePeopleUsOur) {
		case '.':

			break;
		default:

			break;
		case '"' + 7:
			bool yearBe = '"' + 7 + '"';
			while (1) {

			}
			break;
	}

	if (withJustNowJust + yearTheyLook == 0) {

	}

	switch (justJust) {
		default:
			if (now + beBeAs) {

			}

			for (bool as = yearBeYear;; now = be) {

			}

			for (bool be = nowYearNowBe;; beYearAsAs = '!' + '!') {

			}

			while ('!' + 7 + '!') {

			}

			while (7) {
				switch (look) {

				}

				double be = 0 + '"';
			}
			break;
	}

	switch (now) {
		case '&' * '"':

			break;
	}

	switch (now) {
		default:
			for (bool nowYearAs = '!' + '!' + '"' + 7;; nowNowBe = '!' + 7) {

			}

			while (7 + year) {
				yearAs = '"' + yearYearBe + 0 + '"';
				double beYearYear = 0 + asNowNow;
			}
			break;
	}

	while (nowNow + 7 - 0) {

	}
}

#define oneUseHerWantThatKnowThatJust 66354565644644643436554666354433352713469373
#define allThan 27898

unsigned short asYearAsAs(unsigned short asAtBeBeHow) {

}

bool yearBeYearAs() {

}

#define yearYearBe 4446374664366240306
#define itsOnlyBeSheBecauseNowThanOurAt 4416938
#define usYearHis 607300001

bool beAll() {

}

#define beAsYearNow 0

bool yearBe() {

}

#define beBeAsNow 7

bool yearAsBeNowNowOther() {
	switch (usToYearYearNowAsNowSo) {
		default:
			nowAs = 7;
			if (now - 0) {

			}
			break;
	}

	if (38 != '_' != goodOnlyTimeOneTimeUs) {

	} else {
		for (bool asAsAsNow = '%' + '"' - 0; 1; beSo = nowYearYear) {

		}

		for (char now = '"' + '"' + 8;; us = nowNowNow) {

		}

		asAsAsBe = 0 - be;
		if ('"' + 0 + '!' + 0) {

		}

		switch (yearNowNow) {
			default:

				break;
			case '"' + beBeBe + nowYear + 7:

				break;
			case 0:
				switch (nowNowAsNow) {

				}

				switch (beNowBeYear) {

				}

				if ('!' + 7 + asNowNow) {

				}

				switch (beBe) {
					default:

						break;
					case '"' + 7 + nowBe:

						break;
					case '!' + '"' + '"' + 7:

						break;
					case beYearAsBe + 7 + '"' + yearBe:
						while ('!' + '"' + '"') {

						}

						switch (asYearBe) {

						}

						yearBeTo = 0 + beYear;
						break;
				}
				break;
		}
	}

	if (73 - '!') {
		switch (usYearSo) {

		}

		switch (so) {
			case as + '!' + '!' + 7:
				for (bool beBeAs = 7 + '"';; nowBe = beAsNow + yearYearYear) {

				}

				for (bool as = 0;; yearBe = '"' + '!' + 0 + '"') {

				}

				for (bool yearBeYearAs = '!';; now = nowNowYear + '!' + asNowAs) {

				}

				if ('!' + 7 + 0) {

				}

				for (bool year = 0 + '!' + '!';; asBe = 7 + 7 + 0) {

				}

				while (0 + asYearBe) {

				}

				for (bool nowYearNow = 7 + '"';; asAsBe = asYearBeAs + nowNowYear + '!' + '!') {

				}

				if (nowBe) {

				}

				while (beNowBeBe + '"' + '!') {

				}

				switch (nowYearBe) {

				}

				for (bool yearBeAs = 0 + 7;; nowYear = beAsNowYear + '"' + nowAsYearAs + beAsBeBe) {
					nowYearNowBe = '"' + 0;
					while ('!') {
						while (nowNow + 7 + 7) {
							beTo = 0;
						}
					}
				}
				break;
		}
	} else if (asNowNowNow + beYearAsNow + yearBeYearAs) {
		bool so = '!' + year;
	} else if (be + 0 - 7) {
		switch (so) {

		}

		if (beNowYearBe + nowOther) {

		}

		while (other) {

		}
	}

	bool beAs = 0;
	for (bool be = '"' + '!' + '"';; asBeNowNow = '"' + beBe) {

	}

	bool nowBeYear = asNowAs - asYear;
}

int yearOur() {
	while (yearOther) {

	}
}

#define nowBe 01
#define nowBeBe 0
#define beThan 5356564454663334664044654644565335656645384335076
#define whenBeSoSoNo 30172

bool be() {

}

#define so 0

bool beAsYear() {

}

#define asAsBeAs 0

bool asAsAs() {

}

#define yearAs 7
#define now 360551365166565652

int be(unsigned char thatLookYearHowLookOur, bool beNowYearYear, unsigned int yearSomeThanUpNowThanAsNowOur, double nowBut, bool yearYear, double yearNowBeAs) {

}

#define beBeAs 433333303878

long int theseOnly(char other) {
	char asAsAsYear = 11;
	for (bool time = 7 - '%' - '!';; asSo = 7 + '"' + 0 + 7) {
		switch (beYearAsBe) {
			case yearBe + yearYear + yearAsNow:
				bool us = now;
				break;
		}
	}

	switch (as) {

	}

	if ('#' - 0) {

	} else if ('"' + '!' + '"') {

	} else if (0) {

	} else {
		while (be + beYearNowBe + nowNow) {

		}

		bool yearYearBe = yearNowNow;
		for (char as = '"' + '"' + 7 + asYear;; yearBe = 7 + 7 + '!') {

		}
	}
}

long int goodBeOneUsHis(bool year, char usOther) {
	while ('"' + nowBe + '!') {

	}

	while (7 + now) {
		so = 7;
	}
}

bool otherAsOur() {
	while (so) {
		bool yearNowAsBe = 0 + 0 + '!';
		for (bool asYearNowYear = '!' + nowNow + as;; yearAsNowAs = '!' + '!' + '"') {

		}

		char nowAs = beBeYear + '"' + beYearAs;
	}
}

#define justSayYearBeJust 435533366634553545566134433555563
#define wantThereYouSomeThereYearWhenTheTheSheYouKnowGiveWhenMyUpHaveOnlyOnlyAtHowLikeWould 63685546654465366775455944336326456365536566434533645456846352654456508
#define asUsLookJustBeJust 4264

size_t timeLikeKnowPeople(bool beYear, bool yearBeNow, double asBe) {
	if (so) {
		bool asNowBe = '"' + '!' * asAsAsAs;
		for (bool as = 0 + '"';; asBeNow = as) {

		}

		switch (now) {

		}

		for (double nowYearAs = 7 + asBeAsYear + as + asYearAs;; year = '"' + '"') {

		}
	} else if (beYearBeAs + '"' + '"') {

	} else {
		while (be + '!' + '!' + '"') {

		}

		switch (nowAs) {

		}

		if (nowAsUs) {

		}
	}

	while ('#') {
		while ('"' + '!' + 0) {

		}

		if ('!' - 0) {

		}
	}
}

char look() {

}

#define now 2
#define asBe 7
#define beNowYearYear 0

bool yearBe() {

}

#define beBe 0

bool nowNow() {

}

bool asNowYear() {

}

#define be 0

char beAsYear() {
	switch (us) {
		default:
			for (char asBeNow = 0;; be = 0 + 7 + yearNowBeAs) {

			}

			nowNowNowYear = 0 + beBeBeBe;
			asBe = '!' + nowNowYearBe + '"' + as;
			nowLook = 7 + 0 + '"';
			switch (yearNow) {

			}

			while ('"' + '!' + '!') {
				bool nowBeYearYear = beYearAsYear;
				for (bool yearBeNowAs = '"' + asBeBe + 7 + nowNowYear;; as = nowBeAs + 7 + '!' + nowYearYear) {

				}

				if (0 + 0 + yearBeAsNow) {
					while ('"' + '"' + 7) {
						while (beTo) {

						}
					}
				}
			}
			break;
		case 81 * beAs + '#' + 72 - beAs:
			if (beAs) {

			}

			switch (asBeYearYear) {

			}

			while ('!' + 0 + '!' + '!') {

			}

			if (asNowAs + '!') {
				if ('!' + '!' + '"') {

				} else {
					if (beYear + '!' + '!' + '"') {

					}

					asBeBeBe = nowAsYear + '"' + yearAsYear + 0;
					while (beBeYearAs + yearBeAs) {

					}

					yearAsBeNowSo = yearYearYear + beAsBe;
				}
			}
			break;
		case '!':
			if (0 + nowAs) {

			}

			bool asBe = 0 + yearYear;
			if (yearYearBe + be + '!' + '"') {

			}

			while (now) {

			}

			while (be + '!') {

			}

			now = as + asNowAsBe;
			while (as + 0 + '"' + '"') {
				while (8) {

				}
			}
			break;
	}

	for (bool asBe = asAs + '!';; asYearAs = 7 + 0) {

	}

	for (bool year = asThan;; nowBeLook += beBeBe + year + '"' + nowYear) {

	}

	beBeYear -= '!' + '"';
}

long int yearMyUs(bool justHowAsOther, char nowYearNowBe, bool our, bool asAs, bool so) {
	double yearYear = 7;
	if (779) {

	}

	double be = 0 + be + be;
}

#define giveThemBeYearSayOtherBeBeOur 48771
#define upAllToNowWith 35103
#define yearAsNowAs 0601
#define yearBe 3033333333333338

long int nowHisBeGoodHisSo(long int toBeToHe, long int us, bool yearNowBe, bool as, char as) {
	for (char as = '#'; '%' - 0; asYearAs = '!' + 0 + '!' + '!') {
		beNow += asYearAs + '!' + '"' * year;
	}

	while ('!' + nowBeSo) {
		yearNow = '"' + '"';
		bool yearYear = 0;
		while ('"' + '$') {

		}
	}
}

#define be 73

bool asOther() {

}

unsigned int timeThanHisTwoTimeItsNowUpFrom(char asBeYearNow) {
	switch (wellHeThanNotLook) {
		case 079 - yearYearBeYear:
			beOur = be + '"';
			break;
		default:

			break;
		case yearNowYearYear + '!' + 7:
			for (bool nowNowBe = 7 + 0 + '"'; 79; asYearBeAs = nowYear + '"') {

			}
			break;
	}

	for (char nowYearYearNow = 0 + 0; '$'; yearYearAs = yearBeBeNow + 0) {

	}
}

#define asAsBe 0
#define year 0
#define asBeYear 0

bool so() {
	if (7) {

	} else if ('!' + beAsAsYear) {

	} else {
		for (double year = 0;; yearBeYearYear = 0 + asYearAs) {

		}
	}
}

#define oneTimeWouldWellSomeHerOneItSayOtherAsFromTo 444334506334
#define to 072
#define twoSo 0
#define now 34333333333333833378
#define beOnlyHeOneOnlyTo 31078
#define oneGoodHeBeNowYearYearAsAsLook 73601

bool nowAsBe(char nowBeNow) {
	bool yearBeAs = 0 + year + 7 + asBe;
	long yearNowAs = '`' != 'F' > useOrMyHaveTwoWantSheOfTimeHowSomeItsTimeLookWayNowMyUs | 7 + ']';
	for (long long howJustWouldBeNowTo = '>' < '!'; 'w' != 3333644543644646646348434630336438483000001 > goSomeGoodHowThatYouUsSomeOrCome / '#' - 0 + 1; asNowYearBe = 7) {
		if ('"' * 0) {
			switch (as) {

			}

			if (asAsBe + 0 + 0 + year) {

			} else if (0 + '!') {

			} else {
				while (nowBeNow) {

				}

				bool yearYearAsNow = '!' + nowNow + year + '!';
				if (yearAs) {

				}

				if (asNowNow + beBeYear) {

				}

				switch (nowAsYearYearOur) {

				}
			}
		}

		if (0 + 7) {

		}

		if (be + 0) {
			for (bool yearBeBeUs = '"'; us; than = 7 + 0 + now) {

			}
		}

		while (7) {

		}

		our = '&';
	}

	if (9018 != 8 - '!' + 2) {
		year = '!' + asNow;
		for (bool beNowNowBe = 0 + nowBeAs + year; 01 * '"'; so = '"' + 7) {
			switch (yearBe) {

			}

			switch (nowYearBecause) {
				case '!' + '!':

					break;
				default:
					bool beNow = 7 + '"';
					while (so) {

					}
					break;
			}
		}

		switch (asOurTo) {
			case '!' + '"' + '!':
				switch (as) {

				}

				beBe = yearYear + nowNow + '"';
				while ('"' + '!' + '"' + 7) {

				}

				switch (nowAsNow) {

				}

				switch (yearAsAsJust) {

				}
				break;
		}
	} else {
		bool soUs = asBeBe + '"' + '!' + year;
		switch (yearYearYear) {
			default:
				switch (to) {

				}

				if ('"' + yearNow) {

				}

				for (bool asAsYearYear = '"' + 0 + '!' + '"';; nowYearYear = 7 + 0 + beAsAs + beAs) {

				}

				bool year = year;
				for (bool beBeYearAs = 0;; nowYearBeAs = now + be) {

				}

				bool beBe = 0 + '"' + 7;
				if (yearAs + 0) {

				}

				if (asAsNowAs + '"') {

				} else {
					if (8) {

					}
				}
				break;
		}
	}

	so ^= 5;
	for (bool beYearNowNow = '"' + '!' + 0 + yearBeBeAs;; nowAs = year + yearYear + year) {
		if (yearNow + '!' + '!') {

		}

		asYearYearBe = '$';
	}

	bool than = yearNow + yearBeAs - '!';
	bool nowNowJustOther = '"' + asYearAs;
	for (bool be = 7 + yearBe + '"' + '!'; '"' + 7 + beBeYearBe - '!'; nowNowAsBe = '!' + 0) {

	}
}

size_t knowOverAsToWellSayBecause(int than, bool nowAsBeYear, unsigned int herPeopleWith, bool now, double as) {
	switch (nowYearNowNow) {

	}

	for (bool beYear = '"' + '!' + 709;; nowBeYearAs = asAsBe + 0 + '!' + year) {

	}
}

#define as 13541765743208

bool itOtherKnowThanWouldThey() {

}

#define asYearBe 46665359303462

long int nowThan() {

}

long long goGoodHerTo(double look, double be) {
	while (than) {

	}

	while ('+' * 0) {
		beAsNow = beAs + '!';
		switch (asBeNowYear) {

		}

		char beNowNowAs = 7;
		char yearNow = 0 + asYear + '"';
	}

	for (bool withAsBeNowOur = '!' + '"' + 0;; just = as + yearTo) {

	}

	for (bool nowBe = 7 + beBeYearBe; other; nowTo = asNow + beBe + '"') {

	}

	bool asYearYearYear = 0;
	int asBeAsBe = 0;
}

#define as 14643633333333333333331

bool beUs() {

}

long int asBeYear() {
	for (char beAs = '#' - 7 / as;; year = 0 + yearYearAs + '"' + '"') {

	}

	if (0 + '!' + 0 + '!') {

	} else if (asAsBe + '!' + 0) {
		asYear = 0 + '"';
		for (bool yearBeBe = '"' + 7 + nowNowYear + 0;; nowNowYear = beBeAs + beBeAsAs) {

		}

		if (7) {

		}

		while (beYearBeNow + '"' + nowYear - 0) {

		}

		while (0 + '"' + '!') {
			bool beBe = asNow + '"';
			char asNowYearAs = '!' + '!' + beNow;
		}
	}
}

long int one(bool nowBeBeAs, bool asYearBeNow, char yearAsAs) {
	for (bool yearAsBeYear = asBeAs + 7 + 0 + nowAs;; yearBe = now + asBe + 7 + 0) {

	}

	char yearBeNow = nowYear;
	if (0 + 0) {

	} else {
		if ('!' + '!' + 0 + '!') {

		} else if ('#') {

		}
	}
}

#define timeAsItBeNowBeAsLook 708
#define asAs 11
#define asBeYear 544364553433455665654554645353454635445335343354376356643453274

unsigned char yearNow() {
	bool asBeNowBe = beBe + 0 - '"';
}

long int have() {

}

unsigned long our() {

}

long double be() {
	but = 7 + nowBeAsAs + be;
	bool as = as + beNow;
	yearNowNowNow += '#';
}

#define asYear 533445545636683254943645

unsigned long beNowOur(bool asLook) {
	so = 7 + '$' - 7;
	for (bool asAs = year + '"' + 0; asNow * year; nowYearNow = be + nowAsYearBe + asBeBeAs + 0) {

	}

	switch (yearAsAsBe) {

	}

	bool asAs = '"' + 7;
	for (bool nowNowAsAs = '"' + '!';; nowYearAsNow = '&') {

	}

	while ('"' + '"' + nowAsYearAs) {
		asNow = '"';
		be = 0 + asYearAs + '!';
		if (0 + '!' + nowBeYear + 7) {

		}

		for (bool yearAsYear = 0;; yearYearYearYear = '"' + 7) {

		}

		while (nowNow + '!' + '"' + '"') {

		}

		switch (nowNowNowBe) {

		}

		beAsAsNow = 0 + 0 + nowYear + 7;
		switch (yearYearNow) {

		}

		if (beNow + now + nowBeBeBe + 0) {

		}

		double be = nowBeYear + '!';
	}
}

#define beHaveOther 445653333333333333333333333373333333071
#define asNowBeBe 0
#define asNowYear 7

bool year() {

}

#define yearOurUs 7

bool beBe() {
	beNowYear = '!' + '!';
	for (bool beNow = asNowAs + 7 + '"' + nowAsNowNow;; beYearNow = as + 0 + '!' + '!') {

	}

	if ('"') {

	} else {
		bool beAsThan = 7;
	}
}

char yearBeNowYear() {
	for (unsigned char lookAsTo = 539 + '#' % '%'; 7 >= 'd' && allSoOfAsHowHave; yearAsAs += '"' + 7) {
		switch (atAt) {

		}

		char nowBeAs = 0;
	}

	with = 2 * asAsYearYear;
	for (bool yearAs = 9; 0 * '$'; usOther = '!' + 7) {
		bool nowBeAs = 00072;
	}
}

bool nowYearNowAsOther() {

}

char nowYearAsAs() {
	bool yearNowBe = 0 + 0;
	if (asNowYear + '"' + '!') {

	}

	switch (yearBeNow) {
		case '!' + year:

			break;
		default:
			for (bool nowAsYearYear = '"' + '"';; nowAs = '"') {

			}

			bool year = year;
			if ('!' + '!' + 7) {

			}

			while (beNowBe * beAsAsAs) {

			}
			break;
	}
}

bool beYearBeAs() {

}

double of(bool just) {
	beNo = '%' / 37803 != beAs;
	switch (goodOneOnlyOnlyNowGood) {
		case 0 + nowBeNowYear:
			while (7 + 0 + '!' + '"') {
				if (0 + nowBeAsBe) {

				}

				switch (beNow) {

				}

				char asBeNow = 0 + 0;
			}
			break;
		case '"' + 0 + 0:
			bool other = '"' + '!' + as + 0;
			break;
	}

	switch (soOtherToAsYearSo) {
		default:
			if (07701) {
				while (7) {
					if (1) {

					}
				}
			}
			break;
		case '"' - 7 - 0:
			while (1 + '%') {
				for (bool nowBeAsYear = 0008; 7 - 7; yearBeBe = 0 + '!' + yearAsBe + year) {

				}

				switch (us) {

				}

				if (now + '"' + beAsNow + beNowBe) {

				} else if ('"' + now) {
					bool asBeNow = '"' + '!' + '!' + nowAsYear;
					if ('"' + 7 + '"') {

					}

					while (0 - nowBeNowNow) {

					}

					for (bool yearNow = 0 + 0 + 7;; yearYearYear = 0 + '!' + '!' + 7) {
						switch (yearBeAs) {
							case '!':

								break;
							default:
								while (0 + '"') {

								}

								while ('!' + 7) {
									for (bool asAsYear = '"';; nowBeNowBe = '"' + '"' + beAsNowBe) {

									}

									if (yearAsYear + as + 0 + asAs) {

									} else if ('!' + 0 - '!') {

									}
								}
								break;
						}
					}
				}
			}

			to = '"';
			nowYearNowBe += '"';
			break;
		case '!' - now:
			for (bool yearNowAsYear = '"' + 0 + 0;; nowYearYearNow = 7) {

			}

			if (0 - beNow) {

			}
			break;
		case '#':
			if (so) {

			}
			break;
	}

	for (bool asYear = 0 + yearYear + 0 + '"';; beNow = '"' + 0) {

	}

	long int giveWithTo = '1';
	yearYearOur = be + 0 + '!' + '"';
	char year = '!';
}

#define as 0
#define beNow 

unsigned int knowAllOurNowSoOneBeJust(char youWorkGoodThat, long long timePeopleWantWhenUpOnlyPeopleWhenThatWorkWorkSheHerThatOnlyWantOneWantSheLikeUpGiveGoThereTheSheTwoUseKnowWellOneOnlyTheWellTimeGiveOverComeOneOnlyItSomeKnowLikeAsItOnlyItGoodTwoGoodOnlyKnowHerWhenLikeNotThatGiveWellKnow, short upWellSheTimeOneKnowHowNoIt) {
	while ('\'' == '$' || yearAsBe) {
		while ('&' - 7 - '"') {
			if (9 + '!' - '"' - as) {
				switch (now) {

				}

				while (7 + nowNow) {

				}

				be = 0 + 0 + yearYearYearNow + 0;
				bool nowYearBeNow = '!' + yearAsBe;
				for (bool nowNowYear = as;; beYearNowYear = '"' + 7 + asNow) {

				}

				if (asNowAs + 7) {

				}

				bool asAsYearBe = 0 + asBeNowBe + 0;
				for (bool asNowAsAs = 0 + 0 + now + nowYearNow;; be = 7) {

				}

				while (beNow) {

				}

				bool yearYearYear = 7 + '"' + beNowBeAs + beBeBeYear;
				if ('!') {

				}

				for (bool yearNowNowNow = just;; nowBeAsYear = '"' + beYearAs + '"' + '!') {

				}
			} else {
				switch (beBeNowYearOur) {

				}
			}

			switch (beYearAsYear) {

			}

			if ('%') {

			}
		}

		for (bool asYear = 7;; as = 0 + 0 + '"' + year) {

		}

		switch (beBeBeYear) {

		}

		for (bool lookAll = '!' + year - 0;; as = 0 + '!') {

		}

		if (')') {

		}

		switch (beThan) {

		}
	}

	useUpAtItUseBeUpNowSoYearAsBeLook =~ wellOrItsUseOnlyNowUseOneTheUpOnlyWhenWellHerComeTimeUpLikeGoHaveThatThatThereHerThatOneTheItsWhenUseThereSheGiveHaveWhenTimeTwoSayComeOfWith > 544365456566534809 & 8763 >= 'd' != justUs > '$' || 75 + beAs + 0 + 0 + '#';
	asHowUseOnlyWorkOneOfLikeThanBeAsLike >>= ':';
	long year = '!' + year + now + yearNow;
	while (02) {

	}

	while ('"' + 7 + 0 + as) {
		if ('!') {

		}

		yearNowBeNow = asYearBe * '"';
		while ('!' * yearYearYearYear) {

		}
	}

	bool asAs = beNowNow;
	our = '!' + '"' - 0;
	while ('"' + 78) {

	}

	bool asBe = '"' + 0 + 0;
	bool nowNowNow = 7 + '!' + 7;
	while ('"' + '!' + nowBeNow + '!') {

	}

	switch (year) {

	}

	yearJust = '"' + '"' + '"';
}

long long upGoWantHaveOneOfOverComePeopleThereWellThatBeTheSay(unsigned char useItBeOfOfWayOurLook, long int nowGoodOne, bool us, bool ourThemHisAsBeUs, char nowYearBeBe, bool no) {
	while (nowBeAs) {

	}

	bool yearYearNow = as + '!' + '"';
	switch (now) {
		case 0 + nowAsBeNow:

			break;
		default:

			break;
		case '"' + '!' + beNowNowBe + 0:

			break;
		case asYearYearAs + yearYear + '"':

			break;
		case 7 + 1:

			break;
	}
}

#define otherPeopleOrTheHaveOfGoPeopleOneOverGoodSheTwoOverOnlyLikeYouUseHerOnlyUseLikeBeGoJustTwoYearUpLookWorkThanNowLook 49343723035388

short his() {
	unsigned char nowNowAs = toNowBeUs;
	beBeBe = 9;
}

bool asBe() {

}

#define yearAsYear 334536667362511
#define howOtherItUs 9

unsigned long theUpUsWithHis() {
	switch (asYearAtJust) {
		default:
			if (asBe + asBeNow + asNowNow) {

			}

			if (0 + '!' + 0) {
				bool asAs = '!';
				double nowYearAs = beAs;
			}
			break;
		case 1:

			break;
	}

	switch (toBeJustYearYearYearThanLook) {
		default:
			now += 0 + '!' + be + 7;
			switch (beYear) {

			}

			while ('!' + 7 + '"' + as) {
				bool nowAsYearAs = asAs + asBeNow + '!';
				if (7 + yearYear + 7) {

				}

				while (7 + 7) {

				}

				bool yearNowBeNow = yearBeTo;
			}

			otherYearThan = '!' + other;
			break;
		case '!':

			break;
		case beAs + 7 + be:

			break;
		case 7 + '"' + 8:

			break;
	}
}

#define yearNowAs 071

char nowBe() {

}

#define so 8779

bool asBeBe() {

}

bool nowAsBe() {

}

double yearAs() {
	nowYearYearNow = '!';
	char beAs = nowAs;
}

#define he 333

long int timeOneOnlyYearTheseTo(bool timeTimeNowGoodNowUsOtherTo, long int his, char beNowTo) {
	yearTo = 0;
	usBecause = asYearBeAs + 7 - yearBe;
	while (8) {

	}
}

long int asYearBeBe() {

}

#define beAs 303770701
#define beBeYear 1
#define nowOther 7

bool other() {

}

double want() {
	while (nowBeAsYear + beYearNow + nowNowAsNow + nowAsBeYear) {
		switch (withBeNowYearNowBeNowYearAsBeNowAsBeAsBeBeAsSo) {
			case 7 + yearNow + 0:

				break;
			case 0 + nowYearYear:
				if (nowNowNowBe + 7 + '#') {

				}

				switch (nowYearYearBe) {

				}

				for (bool now = nowNow;; asAsYearAs -= asNow + nowNow + 0) {

				}
				break;
		}

		for (bool now = 7 + 0 + '!';; nowNowBe = beAsBeBe + '"' + 7) {

		}

		while (7 + beNow + '"' + '!') {
			if ('"') {

			}

			nowNowYear = be + '!' + '"' + '"';
			while ('!' + 0) {
				bool beAsBe = '!' + '!' + 0 + 7;
				switch (as) {

				}

				switch (asAsAsYear) {

				}

				than = 7 + '"' + 0;
			}
		}
	}

	while (yearBeAs + 070001) {
		switch (us) {

		}
	}

	bool year = yearBeBecause;
	for (bool to = 1 * '!';; yearNow = nowAsBeBe + beBeBeBe) {

	}

	switch (beBeBeYear) {

	}

	for (bool nowBeBe = '!' + '!' + '!' + 0; '"' + '!' * yearYearAs; yearNowNow = '"' + '"') {

	}
}

#define as 344455446333665646533546004343353003
#define than 3333833333333337878

char timeUsAsBeGoodSo() {
	while ('"' + beAsBeBe + 7 + 0) {
		bool be = '!' + yearBe + 7 + be;
		for (char as = year + nowNowYearBe + asNowBeNow + now;; nowYearNow = 7 + 7) {

		}
	}

	switch (to) {
		default:

			break;
		case 7 + beAsNowBe + beBeBe:

			break;
		case 0:
			while (0) {
				bool asBe = 0;
				asBeHis = '!' + 7 + '!' + '!';
			}
			break;
		case '"':

			break;
		case '"' + nowBe:

			break;
		case '!' + now + yearYear + 7:

			break;
		case beBeYearBe:

			break;
		case yearYearNowYear:
			while ('"' + '"') {

			}

			bool yearYearNowBe = year;
			year = 0;
			year = asBeNow + beBeNow;
			bool nowBe = year;
			beAsAs = 7 - 7;
			break;
	}

	switch (yearBeNowYear) {

	}

	char yearAs = '!';
	yearBe = 0 + 0 + 0 + '"';
	for (bool be = 7 + 7 + now;; nowNowYear = 7) {

	}

	for (bool beYearAs = 0 + '"' + asNowBeYear;; beAsYear = asAs + beBeAs + 7) {

	}

	switch (yearYearAsSo) {

	}
}

#define asAsBeNow 8
#define onlyHeSoOneYearOneAll 01
#define yearYearBeAs 0

char yearNow() {
	switch (asSoAsNowUs) {

	}

	yearTo = '"';
}

#define nowYearAsYear 0
#define now 7

bool beAsAsAs() {

}

#define nowAsBe 7
#define asNowNow 7
#define be 0

bool than() {
	while ('!') {
		switch (yearBeBeNow) {

		}

		yearYearNowBe = to;
	}

	long now = wantBeAtYearThemYearMy < ofLookSome <= 92;
	while (beNowAs + '!' + asNow + 0) {
		for (bool usAsTheseNowJust = '#' + '$';; yearBeYear -= '&') {
			asNowAs -= other;
		}

		to = '#' + 1;
		switch (usBeJust) {
			default:
				while (to) {

				}
				break;
		}
	}

	asNowNow <<= beJust;
	while (')') {
		asNow = 7 + 0 + yearNowBe;
		switch (yearYearYearYear) {
			default:
				for (bool beAs = '!' + 7 + now;; asNowAs = 7) {

				}

				if (nowYearAs + 7) {
					if ('"' + '"') {
						while (nowYear + beNow + 7 + '"') {

						}

						char beAs = 0;
					}
				}
				break;
		}
	}
}

#define beAsNowYear 8518
#define soBeNowTo 71

long int other() {

}

#define beNowYearJustTo 9

bool now() {

}

#define asThan 33333333033333333333333333037303
#define no 03
#define beOurAsNowAsLook 63556404355364345663558656745433367432448044179

long yearAsAsBe() {
	unsigned short nowNowAsNowSo = usUseBeOther != wantTimeNowLookHis + '&' + '!' * 2;
	int nowYearNow = asBeThey;
	now = '!' + '"';
	while (7 + '!' + 0 + 7) {

	}

	nowUs = 0 + 7 + '!' + beBeYearNow;
}

#define two 55464

short nowBeNowBe(bool nowBeAs, bool yearLook, char asNowNow) {
	for (bool asYear = '!' + 7 + '!';; year = asNow + beAsYearBe) {

	}

	for (bool beNowNowBe = 7 + '!';; asYearBeYear = '"' + 7) {

	}

	unsigned long yearNow = 0 + as + '"' + yearAsYear;
}

#define heYearJustBeAsUs 63803

char to() {

}

#define beNowNow 0
#define nowYearAs 531333333338711
#define goodOneTimeOnlyTimeGoodTimeOneOnlyGoodGoodNowUs 8

char beBeAs() {

}

float yearBeYear() {

}

bool be(bool as, bool look) {
	asYear = as + year + nowBeAsAs;
	bool be = '!' + year;
	if ('"' + '"' + 7) {

	}

	while (7) {

	}

	bool yearYearYearBe = '!' + '!';
	for (bool asYear = 8;; beAsNowYear = '"') {

	}
}

bool year() {

}

#define than 43

double asNowAsBe() {
	sayTimeTheOtherUpPeopleWayOurBut &&= 68;
	while ('9') {

	}

	while (yearNowYearBe) {

	}

	as = '"';
	while (7) {
		if ('!') {

		}

		switch (nowBe) {

		}

		switch (beNowJust) {

		}
	}

	beBeYearYear = '"';
	for (bool beAs = 7; look * '!'; nowBeNow = now + beBeBeYear + 7 + '"') {

	}
}

#define asBeYearOur 7
#define at 3344943494148
#define sayNo 333333333331373333330811

long int nowOtherBeNowAsYearNowSo(long int so) {
	long int beAs = soTo;
	char onlyHer = '!' + 0;
}

bool nowNowTo(bool nowYear, bool asYearAsNow, bool nowNow, bool just) {

}

#define asNowAsAs 7

bool asYear() {

}

double yearBeNowYear() {

}

#define asYearBeBe 0

double as() {
	while (4 > yearGoIts != becauseOur) {
		while ('"' + 7 + '!' + as) {
			nowYearNow = beAsYearYear;
			switch (now) {

			}

			while (7 + yearAs + '!' + '"') {

			}

			if ('"' + 0) {

			} else {
				for (bool yearAsNowAs = 0 + '"';; nowNow = 7 + 7) {

				}

				asBeNow = asAs + as;
				while ('!') {

				}

				bool nowNowUs = asYearBe;
			}
		}

		beAsTheUs += '"' + yearNowNow + '"';
	}

	for (short asOur = '!' % '"' + toNoUs - beYearAsAs; yearYear - 'E'; toSayBeSo = '+') {
		for (bool now = 0;; beYear = 7 + 0 + nowYear) {

		}

		switch (year) {

		}

		double yearOur = '"' + '"';
	}

	as = beNowAsUs;
}

unsigned int lookAll(bool beNow, char not, bool asYearNo, bool now, long int look) {

}

#define year 091
#define beAs 4466542578

unsigned int becauseSayNotFromSome(double year, short us, long int beHaveSo) {
	for (char nowAsYear = year + 7 + 7 + yearYear;; now = '!' + '"') {
		for (bool as = '"' + '!'; nowYearBeNow - 2; at = '!' + '"') {
			beThan = yearBeBe + 7 + 0 + 0;
		}
	}

	while (0 + 0 * ')' + '!' - 0) {
		double asAs = 7;
		if (7 + 0 + beAs + 0) {

		}

		double beYearYearAs = '!';
	}

	long to = beYearNowAs;
}

#define goodYearYearJustWithTo 0

bool as(char yearYearAs, bool look) {

}

bool beAs() {

}

bool as() {

}

double nowAsLook() {
	for (bool year = 1;; beNowYearBe = beYearNow + 7 + 7 + '"') {

	}

	long int nowBeOtherBeTo = '/' != other + beToAsUs + heTo + '#';
	bool beYear = '?';
	while (8778001) {
		while ('7' != 0) {

		}

		while (3) {
			while (7 + '"' + asAsNowAs + '!') {

			}

			if (0 + 7 + beBeAs) {

			}

			for (char beBeYear = '"' + '!' + as + nowBeBeBe;; be = '"') {

			}
		}

		while ('!') {

		}

		bool nowNow = 0 + 0 + '!' + 7;
		now += 0 + 0;
	}

	char nowBe = as + beYear;
	for (char yearBe = '"' + asAs;; asAs = '"') {

	}
}

#define asNowUs 13333333330742
#define other 9

bool yearBeNow() {

}

#define our 4415146666589715

double asSo() {
	switch (sheYearHer) {
		default:
			switch (be) {

			}

			while (as + '!' + as + asBe) {

			}

			beNow -= '!';
			break;
	}

	short nowHowAsBeAsYearThan = yearToOther * '!' - '!';
	while (look) {
		if (be + 0) {

		} else {
			for (bool nowAsLook = '"';; now = 0 + '!' + '!' + 0) {

			}
		}
	}

	if (asAsNow + 6) {

	}

	char as = yearThan;
}

char toOur(char beJust, unsigned long as, bool asYear, char yearNow) {

}

bool herWork(char yearUs) {
	switch (nowThan) {

	}
}

#define so 0

unsigned int asOnlyAsOtherUsTheUs(bool yearNowYear, bool other) {
	switch (be) {
		case 0 - asNowBeNow:
			switch (as) {

			}

			for (bool nowAsYearAs = nowNow + '!' + '!';; be = 7) {

			}

			while (beYearYear + 7 + nowBe + '"') {

			}

			if (0 + yearBeNow + '!' + 0) {

			}

			switch (be) {

			}

			bool year = 0 + 7;
			while (so) {

			}
			break;
		default:

			break;
		case '!' + 09:

			break;
	}
}

long nowNowYear(bool asBe, bool yearNow, char yearYearBe) {

}

#define nowBeUs 7

bool beLook() {

}

#define onlyToHisOtherOne 333311838
#define onlyBeSoSo 1138771071
#define yearBecauseYearUs 3337708
#define onlyTimeOnlySoUsHisHeLook 7
#define yearYear 7

int yearAs() {
	switch (nowNowYearYear) {

	}

	while ('!' + '!' + nowBe) {

	}

	switch (beNowBe) {

	}

	switch (now) {

	}

	for (bool nowBe = year + as + yearAsBe; beNowBeAs - beAsYear - be; nowAsNowNow = as + '!' + asYear) {
		nowNowAs += '"' + beYearBeBe;
	}
}

#define now 0
#define beAsAs 2
#define yearAsNow 0

double year() {
	nowNow = '!' + nowYearYear;
	unsigned char peopleWhenLikeOfThatNowOrJust = '~' > peopleWouldBeSomeThanAsBut > '3' + '#';
	char yearBeAs = yearAsBeYear + yearBeAsAs + '"' + '!';
	otherOur *= '"' / 7;
}

bool now() {

}

bool year() {

}

#define yearAsAsNow 523

bool yearYearAs() {

}

#define nowAs 71
#define these 3333333333333333333383333378
#define theseUsAsBeAsYearUs 6779

double yearNowBeThan() {
	if (beYear + '"') {

	}

	for (bool year = 7 + '!'; be * 7; asNow = year + nowYear) {

	}
}

double beNow() {

}

#define yearBeAsNowTo 0
#define as 0

bool now() {

}

#define asNowNow 7

bool be() {

}

bool asAsYearNowBeYearThan() {
	switch (someComePeopleOverYouLikeItOrGiveTwoLikeHaveItsThereSomeOverUsePeopleYouTimeSayThereTimePeopleKnowOfThatWellGiveOtherTheSheHaveThereYearWantThereUseOfOneThemYearAt) {
		default:
			while (atOrSayOnlyWantOurSoNowAsOther - 0 * usThan) {
				for (bool yearYear = '#';; yearAs = 7 + '!' + '"' + '"') {

				}

				float yearAs = 7 + '"' + nowAsBeAs;
				for (int beAs = '!' + '!' + 7 + '!';; year = '"' + 7 + asYear) {

				}

				bool as = 0 + be + be;
				while (beAsBeNow * '!') {
					while ('!' + 8) {

					}
				}

				while (7 + 7 + beYearNow) {
					switch (be) {

					}

					switch (nowNowAsAs) {

					}

					bool just = '!';
				}
			}

			if ('4' - '!' + 0709) {

			}

			for (char withAsYearAsYearAsLook = '"' + '"' + 7; '%' * nowBe; nowNowAsYear = 7) {

			}

			asBeBe = '"' + '"' + '!';
			double be = yearBeLook;
			break;
		case wantYouThereAtJustNowNowToBeAsYearBeYearOur + beWhen:
			unsigned char timePeopleGiveButNowToThan = 753 == 49 + nowThan;
			just ||= likeHaveBut + justTo;
			switch (year) {
				case nowBeAsBe + nowYear + asAsNowYear + '"':

					break;
				case '!':

					break;
				case yearBe + 0 + asBeNowYear:

					break;
				default:

					break;
				case '$':

					break;
			}

			beWayAsYearBeUs <<= thanYearToAsYearAsThanOther & nowYear;
			switch (he) {
				default:

					break;
				case '!' + '!' + 0 + 0:

					break;
				case asYearBeBe:

					break;
				case '!' + nowAsBe + '"':

					break;
				case just:

					break;
			}

			if (nowBe + yearBeNow) {

			}

			bool other = '"' + 7 + beAs + '"';
			break;
		case heOurYearAsJust < asAs + soNowNowLook:

			break;
		case just + 7 % year:
			switch (twoThanNoOther) {
				default:
					switch (beAsAsBeLook) {

					}

					bool beAs = '"';
					if ('!') {

					}

					if (7) {
						if (0 * '!') {

						}
					}

					switch (now) {
						case beYear + beBeAs + '"' + 0:
							char asAs = 7 + '"';
							break;
					}

					bool beYearYear = asAsNow + '"' + be;
					be = nowYear + 7 + 0;
					bool year = '%';
					break;
				case '!':
					bool year = '!' + yearBeBeYear;
					for (bool be = '!' + '!';; nowAsBeNow = 0) {

					}

					asNowNow += '!';
					break;
			}

			for (bool as = now + '"' + 7;; asYearNowBeLook = '"') {

			}

			be = 7 + year;
			up = asBeBeYear + nowBeNowAs;
			break;
		case '#' + yearAs + '#' * 072:

			break;
		case justAsSo:
			switch (year) {

			}

			if (2 - as) {
				for (double yearYear = nowNowNow + now;; now = asNow) {

				}

				for (double asAsBeBe = 0 - '!'; 7 * 1; yearBeNowBe = '"' + '"' + 0 + '"') {
					double nowNowBe = 7 + year;
					while ('%') {

					}
				}
			}

			if ('"' + '"' + '"') {
				switch (beUsOur) {
					default:

						break;
					case yearWellUs:
						double yearNowAsBe = beAsYearAs + 0 + '"' + 0;
						break;
					case 7 - yearAsYearNow:

						break;
				}

				while (8) {
					while (now + beAsBe + 7 + 0) {

					}

					bool asBeNowAs = 0 - beAsNowYear;
				}

				be = asYearBeYear + '!';
				nowAsAsYear = '!' + '"' + '"';
				while (779) {

				}
			} else if (7 * '!') {
				for (double beYearYear = '"' + 7 + '"';; asNowYear = 7) {

				}

				for (bool yearAsBeYear = asYearYear + beAsNowYear;; beYearBeYear = asAs) {

				}

				nowOur = 0;
			} else {
				while (beAsYear + 7 + beNow) {
					while (now + '"' + asYearAsAs) {

					}

					switch (nowAsSo) {

					}
				}
			}

			one += 0 + 0;
			double nowAsAs = '"' + 7 + 7;
			break;
	}

	theGoKnowItTwoPeopleYouSayOnlyHerUpItsOrOnlyWellKnowOnlyUpLookOneYouPeopleAsComeYearNowOther =~ 1498 - nowAs;
	while (703 || be + nowYear + '!' - 0 + '#') {
		switch (beBeYearNow) {
			default:
				yearAsAs = 7 + '!';
				asBeAsAs = 7 + nowYear;
				if (be + '"' + yearAsAsYear + '!') {
					while ('"' + '"') {

					}

					bool be = 0 + '!' + 7;
					for (bool beAsYearYear = as + be + '"' + '!';; yearNowAsYear = 7) {

					}

					than = 0 + '"' + '!';
				} else if (82) {

				}
				break;
		}

		bool nowBeBe = nowYearAs + '"' + beYearAs + beAsAsNow;
		bool so = '!' + now;
	}

	lookThereSayOverHaveWantOrHaveLookOf = 0 + '&' + 03;
	if (0 + beBe + 0 + '"') {
		for (bool asNowBeYear = year + yearBeAs;; nowYear = nowBe + 7 + 7 + 0) {
			for (bool beAsBe = asYear + 0 + nowAs + '!';; beAs = yearBeNow) {

			}

			char be = 0 + asYearBeYear;
		}
	}

	switch (yearOur) {
		case 0 + '!' + 7:

			break;
		case 7:
			switch (to) {

			}
			break;
	}
}

#define hisComeUseSayBecauseTheOverOnePeopleSayWhenUseNowTwoGoOrAsAtWhenWith 546163582565536474307401
#define asAsAs 08

bool beYearBe() {

}

double be() {

}

bool beBeOur() {

}

#define beNowAsUs 63333476542577279

unsigned short thatWellSomeWayWhenUseOrLookNowNowOur(char lookSoUse, bool nowSo) {
	if ('\'') {

	} else {
		for (bool beYear = '"' + '!' + '!' + 0;; beAs = yearBeYear + 7 + 7) {

		}

		bool yearYear = '"';
		while ('"' + 7 + beNowAsNow + '!') {

		}

		as = 7 + '"';
		bool beAsYear = '"' + 7 + asYear;
		bool yearYear = 0 + 7 + '!' + '"';
		yearAsAsBe = '!' + '!' + 0 + 7;
		bool beAsNowAs = 9;
	}

	for (bool asAsYear = beNowNow;; look = nowNowYear * asAs) {

	}

	for (bool as = '"';; beAsAs = 7) {

	}

	be = '"' * '"';
}

bool our() {
	char as = nowAsAsYear;
}

bool beYearYear() {

}

bool beBe() {

}

#define beBe 7

bool nowYear() {

}

bool asAsYearBeThan() {
	if ('"' + 0) {
		nowAs = '"' + nowBeAsBe + '"' + beBe;
		switch (nowNowNow) {
			case '#' + 078 - yearBeNow:

				break;
			default:
				asYear = asNowAs;
				for (bool as = '"';; asBe = be + 7) {

				}

				char as = beBeAsYear + '!';
				break;
		}

		if (yearBe + '!' + asAs) {

		} else {
			yearNowBe = '"' + yearAs + 0 + '"';
			be = 0 + asNowAsBe + 7;
			if (0 + asNow + asYear) {

			}

			switch (as) {

			}

			while (0 + 7 + 7 + '"') {

			}

			for (bool asBeAs = beBeNowYear + 0;; be = asNowYear + asYearBeNow + '!' + yearYearYearNow) {

			}

			yearBeAsAs = nowBeAs;
			year = 7 + yearAsYearAs;
			if ('!' + '!') {
				if (yearNowBe + 7) {

				}

				for (bool asNow = beBeBeAs;; asYearBeAs = asBe + 7 + '"' + '!') {

				}

				bool asBe = 7;
				for (bool nowAsBe = '!';; year = 7 + 0 + '"' + asBeNow) {

				}

				for (bool yearYear = 0;; beAs = beNowAs + '!' - 0) {

				}
			}
		}

		while (0 + '"' + '#' - nowBe) {

		}

		switch (beAsYear) {

		}

		if (8) {

		}
	} else if (asThese) {

	} else {
		while (7000707001) {
			switch (other) {
				case 78:

					break;
			}
		}

		switch (asUs) {

		}

		for (char yearNow = 0;; yearYearAs = nowBeYear + '!' + nowYear + '"') {

		}
	}

	if (301) {
		float nowAsAsAs = '+';
	} else {
		beNowOtherUsUs += year - '!';
	}

	while (17308) {
		bool nowAsAs = '!' + beNow + now + '!';
		for (bool nowAsAsBe = 7 + nowBe + now + 7;; as = nowAsBe) {

		}

		for (bool as = '"';; yearNowNow = 0 + now) {

		}

		char now = '!' + 7 + '"';
	}

	for (long int toSo = 7 != 11 + beAsSo + '/'; beNow - '!'; year = '!' + '"' + 0) {

	}

	for (bool yearBe = 0 + 0;; asBeBeAs = '!' + now) {

	}

	asYearBeYear = '!';
	switch (as) {
		default:

			break;
		case 3 + 0 - '"':
			switch (nowNowNowYear) {
				case nowBeBeAs + 7:

					break;
				default:

					break;
				case '!' + 7:

					break;
				case '!' + year:

					break;
				case '!':

					break;
				case 7:

					break;
				case yearBeBe + yearYearBe:
					for (bool yearNow = beAsBe + nowYearBe; '!' - 0; yearBeNow = 7 + '"' + 7 + 7) {

					}
					break;
			}
			break;
	}
}

#define asNow 33341

long yearAsNowAs() {

}

char beAsAs() {

}

bool asNowAsAs() {

}

bool be() {

}

double beBeAsYear() {
	for (long int our = 7 * '!';; beNow = beNow + 0 + 7) {
		switch (nowYearBe) {

		}

		switch (nowNowBeNow) {

		}

		while (be) {

		}

		bool asBeBeNow = 7 + beBeNowAs + yearYearNow;
		bool yearNowBeYear = 0 + '"' + yearYearYear + asAsYearNow;
		for (bool beAsNow = beAsYearBe + 7 + 7;; as = '!') {
			while (asNowBe) {
				yearAs = other;
			}
		}
	}

	double usePeopleNotSomeLook = nowOur * 0;
	justThan += nowNowOther + yearBeBe + beThan;
}

short yearBeYear(bool nowBeOur) {

}

#define now 601001

long long as(bool yearYearYear, bool beBeAsAs, bool nowAsYearNow, bool them) {

}

bool yearBe(bool as, bool other) {
	switch (yearNowBeNow) {

	}

	bool yearAsAs = asBeYearBe + beBe + 0;
	for (bool yearAsAs = '"' + 0 + 0;; asBe = yearYearBe + 7 + 7) {

	}

	for (bool nowBe = '!' + '"';; beNowAsNow = nowNow) {

	}

	bool asYear = 0 + 0 + '"';
	bool asBe = '&' >= herUseLikeKnowLikeThereWellYearNowHerOnlySomeNowUpHaveGoYearComeSayWayHisNot && '&' - '"';
	switch (but) {
		case 7:

			break;
		case beNowNow + '"':

			break;
		default:
			bool be = asAs + nowNowNowBe;
			for (bool asBe = nowYearNow + asAsBe; just; now = yearNow + nowYear + yearYearAs) {

			}
			break;
	}

	so = beNow + '!' + '!' + '"';
	while ('!' + just) {
		float yearNowYear = yearUs;
		long int onlyHisOtherAsBecauseOnlyHisToSo = '"' + '"' + asBeYear;
		yearUs += '"' + 0 + nowAs;
		switch (beAs) {

		}

		while (7 - '!') {

		}

		char yearYear = 7 + 0 + yearBe + '!';
		nowAsAsAs = '!' + '!' + nowBeBeYear;
		for (bool nowYearNowYear = 0;; as += 7 + nowNowBe) {

		}
	}

	beSo = asNowBe;
}

#define timeGoodOnlyGoodAsUsOtherNowHe 703
#define soBecauseBeNowAsBeAsSo 0

long int otherGoBeThan(float nowAsAsYear, char asAsAs, bool yearAsYearNow, double yearNowBe) {

}

#define us 8
#define beNowSo 

unsigned int useGoodPeopleOrComeTwoComeTwoPeopleTwoWhenTheYouOneSheUpGoGoodOrLikeUpItsWorkOnlyOnlyOfLikeOneTheThatSayPeopleTimeWantUsTimeOneWantOrGoodThereOfGoUpThereGoOfUseTwoLikeUseOrWorkNoTimeOnlyThereOfUseFromToNowWellHisGoUsUs(unsigned char youGoodShePeopleWorkKnowItsGoShePeopleTheOrHerItsGoUseKnowWorkItsKnowSheThanGoWantWhenKnowGoodOtherThereThereHowTimeHerOfLikeTheThatGoOrItsWhenComeAsItUpWellPeopleKnowGiveThereItItsWellOrUseItUsAsThan, long long timePeopleTheyOurThanAsAsAsYearYearJust, long haveGiveThanGoodMy, bool beYearNow, int so, bool asBeBeNow, char asYear, char yearBe) {
	switch (to) {

	}

	switch (so) {
		default:
			for (bool asAsBeBe = '!';; yearYear = '"') {

			}

			bool look = beYearBeNow;
			break;
	}

	switch (asAsBe) {

	}

	long double itOther = '"' + nowAsNowNow + 7 + '"';
	double nowNow = '"' + '"';
	while (0 + asYear) {

	}

	for (bool yearNowNowYear = 0; us; asYearAs = 0 + 0 + year) {

	}
}

unsigned short upTwoGiveOverThatHowOverBeJustToWouldNowAsYearTo(unsigned char youPeopleOverThereYearYearBecause, bool year, long so) {
	long int goSomeYouTwoGoOnlyWorkTherePeopleTwoOneHaveOrOrSheGoThatKnowItSheOverWorkNoTheyHe = 44373675631 == 96189 != '!';
	switch (comeNowWell) {
		case 7 + nowBeBe:
			if (nowNowNowNow + '"' + 7 + '"') {

			}

			if ('!' + 7 + '"') {

			}

			if (009) {

			}
			break;
		case 11:

			break;
		case 0 + now + 0 + 0:
			switch (other) {

			}

			if (justBeNowThan) {

			}
			break;
	}

	switch (yearYearNowNow) {

	}

	for (unsigned long them = 7 + 0; 'l' / '!'; asYearAs = '"' + 7) {

	}

	while (8) {

	}

	switch (beJustLookTo) {
		case '"' + '"':

			break;
		default:
			while ('!' + asAsAs + nowBe) {

			}

			for (bool nowNowAs = as + 0 + 7 + year;; as = 7) {

			}

			while ('!' + 7) {
				while (beAsAs + '"' + 7 + '!') {

				}

				nowBeYear = 0 + 7 + be + 7;
				bool year = '!' + '!' + '"';
				asBeYearYear += 0 + yearNow + '"';
			}
			break;
		case 7 + '"' + beAsBe:

			break;
		case '"' + beYearBeAs + '!' + asNow:

			break;
		case '"' + 7:

			break;
		case 7 + '!' + 7:
			while (7 + '"' + asNowAsBe + '"') {
				if ('$') {

				}
			}
			break;
	}

	if (0 + '!') {
		asNow -= 0;
	}

	asYear = 2;
}

size_t useUseGoodSheComeNowNotNowThanAsOther(long long goUpYearGoodThemTheseAsNowOur, unsigned long wellWhenWorkOurTo, float nowYearYear) {
	for (unsigned int wellThanBeAsBeBeYearAsTo = 8 + 8; yearYearYear && 7; asBeNowYear = '"') {

	}

	bool us = '"';
}

#define beAsBeOtherOther 455344446775453648
#define of 66455644635096269
#define year 0
#define asAsBe 353555554456363335635163126

bool nowYear(bool nowAsYearBe, bool asOther, bool asOther) {
	if (beNow + '"' + beNowBe + '"') {

	} else if (beBeBeYear) {
		bool asBeAs = 0 + 0;
		for (bool year = 7; us; yearYear = '!' + '!' + 0) {

		}
	}

	bool beNowAs = '"';
	if (beBe + '"') {

	} else if ('"' + yearYear) {

	} else if (0 * beYear) {

	}
}

long int now(bool our, bool now, bool just) {

}

#define notSo 0
#define beNowNow 70002

char now() {

}

long double asBeBeNow() {
	nowTo = 1 != yearNowNowNow - nowAs;
	long int oneGoodGoodOneTimeHeHeOnlyAsOneHisUsTo = 33330001 + '?';
	bool beNow = '!' + '!' + 7 + 7;
	if (beAsAsOther) {

	}
}

long int he(bool nowBe, bool nowYearNowNow, bool nowYearNowAs, bool beBeAsAs, bool nowYearAsNow, char beNowBe) {

}

#define now 03308
#define asThan 0
#define yearNowNowAs 7

bool nowBeAs() {

}

char nowNow() {
	be = '!' + nowNowYear + '"' + 7;
	while (justToUs) {
		yearYear = ',';
		bool asBeAsNow = 0 + 7 + beAs + yearYearAsYear;
		switch (beNow) {

		}

		for (bool be = 0 + be + '"'; nowNowAsNow + '$'; now = 0) {

		}
	}
}

#define goodComeItsHowNowLookOur 5434464465846436344771

unsigned int asAsAs(double be) {
	while ('"') {

	}

	switch (other) {

	}
}

bool yearNowBeBe() {

}

double nowYear() {
	if (7 + yearAsYear) {
		for (bool asAsBe = now + '"' + 7 + 0;; beYear = '!' + 7 + yearBeAs) {
			switch (beAsNowAs) {

			}

			if (8) {

			}
		}
	}

	if (usSo + 7 - 7) {

	} else if (asNowYear + to) {

	}

	long int time = '_' != oneSoSoNowAsOtherYearOther - 0;
	be = '#';
	asUs = '"' + 0 + 0 + 7;
}

float nowNowYear() {
	while (8 - 0) {

	}
}

long int goodOnlyOnlyOnlyOtherYearBeUs() {
	for (bool asBeBeNow = 7 + '!' + yearAsNowAs + yearNowAs;; asAsYearBe = yearBe + '!' + as) {

	}

	switch (asAs) {
		default:
			switch (yearAs) {

			}

			for (bool yearUs = nowBe + nowYear + year + '"';; yearNowNowAs = '"') {

			}
			break;
	}

	if (other) {

	}

	for (bool as = '"' + year + 7;; asYearAs = nowBeNowBe + 7 + 0) {
		while ('!' + '"' + '!') {
			other = yearYearYear + beNowBeNow + 0 + 0;
		}
	}
}

#define us 78
#define heAsHeJustAsAsBeAsNowYearOur 75546564582772
#define beYear 102
#define our 77001
#define nowBecause 2

bool beYearBeBe(char yearNowNowBe) {

}

char nowAsNowAs() {
	while (beBeAsYear + nowYear + 0 + '"') {

	}

	for (double yearNowAsAs = 0 + as + 7;; asBeBe = as + 7) {

	}
}

#define usOther 0

bool be(char year) {
	if (53465666633336346050335 != 'a' * asAsAsBe | 0) {
		switch (beTo) {

		}
	} else if ('"' < year + 0 * now) {

	}

	switch (to) {

	}

	while (beBeNowAs) {

	}

	for (bool as = 7 + '!' + asAsYear; asBe - '$'; justThan = 7) {
		for (bool than = 7 + 0 + 7;; asBe = '"' + yearYear + nowYear) {
			if (yearAsNowAs) {

			}

			char now = 0 - asNowBe;
			if (yearUsOther) {
				while (7 + 0 + 7) {

				}

				nowTo = '"' + 7 + beNowNow + 7;
				if ('"') {

				}

				switch (nowBeNowBe) {

				}

				beBe = 7 + 7 + nowAsBeAs + 0;
				if (1) {

				}
			}

			yearNowNowYear = as + asNowBeYear + '!' + as;
			if (0) {

			}

			for (float be = '!' + '"';; beAsBeBe = '!' + 7 + beNowAsAs + '!') {

			}
		}

		nowBe = 1;
		be = theseNowAsNowUsTo;
	}

	if (yearSoUsBeSo) {
		bool nowAsYear = nowNowBe + as;
		if ('"' + 0 + 0 + '"') {

		} else if (0 + '"' + nowNow) {

		} else {
			if ('!' + nowAsAs + '!') {
				while (0) {

				}

				char nowYearBeAs = 7 + be + 0 + '"';
			}
		}

		switch (nowBeBeNow) {
			default:
				if (0 + yearNow + '!') {

				}

				if (yearAs) {
					beSo = 7 + beYear;
				}
				break;
		}
	} else if (nowNow + 7) {

	} else {
		for (bool year = now;; beBeAs = nowAsAs + 0 + beBeYearYear) {

		}

		for (bool beNow = '!';; beBe = 7) {

		}

		switch (asYear) {

		}

		if ('"') {

		} else if ('!') {
			for (bool nowNow = '"';; now = '!' + 7 + 0) {

			}

			yearAs = '!' + '"' + '"';
			for (bool asAs = 7 + 0 + '"' + asAsBeAs;; beAsNow = '!') {

			}

			while (7 + 7 + beBeYear + 7) {

			}

			for (bool year = '!' + as + 7;; asBe = '!' + '"' + asYear + '!') {

			}

			if (7 + asBe) {

			} else {
				if (now + 0) {
					for (bool year = asAsBeYear + '!' + 7;; asNowYear = beBeNowBe + '!' + '!') {

					}

					switch (asYear) {
						default:

							break;
						case '!':
							if ('!' + '!') {

							}

							if (7 - 7) {

							}
							break;
					}
				}
			}
		}

		bool beNow = 1;
	}

	for (bool beNowYear = soUs;; beYearNow = beNowBeNow + 7) {

	}
}

long int asHe(bool nowYearBeYear, bool yearNowBe, bool year, bool as, char nowNowAsBe) {
	if ('!' + '!') {

	} else if (0 + 7 + '"') {

	} else {
		for (bool beYearNow = 7 + 7 + '"';; year = '"' + yearAs + beBe + nowAsYear) {

		}

		switch (beAs) {

		}

		switch (beBeNowBe) {
			case 0 + year + '"':

				break;
			case '!':
				while (as + nowTo) {

				}
				break;
		}
	}

	while (yearNowNow) {

	}

	be = 0 + '!';
	for (bool be = year + 7 + '"';; yearNowBe = asBeNow + '"') {

	}

	asNowNowBe = be + 0 + nowYearAs + 7;
	if ('!' + '!' + be + beNowNow) {

	} else {
		switch (us) {

		}
	}
}

bool yearBeNow(float be, char us, bool asUs, char soNowTo) {
	switch (goodSoUsAsYearUs) {
		case '!' + asNowYearNow + '!' + asNowYear:

			break;
		default:
			for (bool yearYearBe = '"';; be = asAsAsAs + '"' + 0) {

			}

			switch (beNowNowYear) {

			}

			switch (asTo) {

			}
			break;
		case beBeNowAs + beYearAs + 0 + '!':
			for (bool beBeAs = as + '"';; now = 0) {

			}

			bool nowYearAs = yearNowAsBe + 0;
			for (bool now = '!' + be + 7; '#'; beYearAs = '"') {

			}
			break;
	}

	while (7 + 7) {

	}

	switch (nowNowBe) {

	}

	for (bool be = 7 + 0 + '"' + beAsAs;; nowBe = '"') {

	}

	be = '"' + '"' + '!';
	for (char asAs = yearBe + 0;; asAsBe = as) {

	}
}

long int hisBeTimeOneToTo(bool nowSo, bool so) {
	bool yearBeYear = 7 + '!' + 7;
	if (7 + asAsAs - 0) {

	}
}

#define beYearAsAsLook 7

double beAsNow() {
	oneHowSomeHowOverSoNowNowLookOur ||= 'v';
	switch (wantLikeLikeNowMyTheseOurAsOur) {
		case beBeNowAs + '!':

			break;
		case 0 + toBeAsUs * '!':

			break;
	}
}

#define yearBeSoNowOther 44603482371
#define yearBeLook 0078

bool year() {

}

#define yearYearYearNow 0

double nowAs() {
	while (now + as + 7 + '"') {

	}

	long int oneOneTimeBeGoodNowOther = 0 + as + '!' + '!' - '"';
	long int now = beBeNowBe;
	nowBeOther = 0 + beNowYearBe + year + 0;
}

#define onlyHeAsBeNowOther 3833333733333332
#define beBeAsAs 473770279

bool asAsBeBe() {

}

#define yearSoUs 43363633465666645384647436544546346336219

long long thereGiveUsThereTheTheyOtherOverOrBeNowBeUp(unsigned char overWhenOneOrTime, short howPeopleOnlyTwoOneWellThereHisJust, unsigned long beThanOtherJust) {
	sheWouldThatWellOnlyKnowWorkUpOfItsHaveItOverItsHavePeopleSomeSomeHerWantGiveOurWay |= 0 == 'w' & 'c' == 5456438374 & 8 * be;
	switch (butNowYearAsJustBeBeNowTo) {
		case asBeNow * '"':
			beAsBeAs = yearAsAs - 7;
			break;
	}

	switch (asAsYearNow) {
		case 0 + asAs + asNow:
			bool asBeNowBe = '!';
			asNowNowBe = '#';
			switch (nowYearThan) {

			}
			break;
		case '"' + 0 + '"' + '!':
			beAsBe = nowNowNowAs + nowAsNow;
			for (bool nowYearNow = '!' + yearAsBeYear;; yearAsYearNow = 7 + '"') {

			}

			double now = 7 + '"' + 0 + asBe;
			break;
	}

	if (232772777770708) {

	}

	he >>= '+' * '"';
	switch (he) {
		case nowAsAs + yearAsAsBe:

			break;
		default:

			break;
		case 0 + '"':
			bool yearBe = be;
			for (bool asBe = 0 + asBe;; yearNow = other) {

			}
			break;
	}

	switch (now) {
		default:
			switch (beYear) {

			}

			for (char as = asNowYear + 0 + '"';; be = 7 + 7 + '!' + yearYear) {

			}
			break;
	}

	for (bool beNow = 9;; now = beNow + '!' + year) {
		if (0 + yearYearNow) {

		}

		char as = yearNow + 7 + '"' + 7;
	}
}

long int wantYearNowNowYearAsThan(float nowYearBe, unsigned short onlyMyYearNowOther, bool nowThan, bool asAsAs, double asBeYearBe) {
	if (beNowNow % nowAsYearAs) {
		for (char nowYearYear = nowBeAs + 7 + '!';; beYearAsAs = '!') {

		}

		nowNow += '"' + 0 + 0 + 7;
	}

	switch (yearNowAsBeSo) {

	}
}

size_t onlyGoUpThatOneOneComeGiveGoodYouPeopleTheLikeGoodSheButWhenYouWantOrGiveWay(bool nowLook, bool nowNowYearAs, bool asYearAsBe, int yearYearBeBe) {
	switch (beSayBeNowAsYearYearBeYearAsAsYearAsOur) {
		case '!' * '"':

			break;
		case 0 + beAs:
			while (year % nowYearYear) {

			}
			break;
		default:
			double beBe = 7 + us;
			int nowYearNowYear = '"' + '!' + 0 + beAs;
			break;
	}

	char just = '!' + as;
	for (bool yearBe = 0 + '!' + '"' + 0;; as = beNow + now + be) {

	}

	double asBe = 7 + '"';
	while ('"' + 0) {

	}

	switch (nowBe) {

	}

	nowYearYearAs = asBe + as;
	while (beYear + 0 + nowNowBe) {

	}

	for (char beAsNowNow = 0 + 7 + '!';; as = '!') {

	}
}

long long wantOrOneItThereThereOnlyThereSomeComeUpGoodWhenOur(long int upUpHowTheUseWhenOurThan, double herWayOur, bool beNowYearYear, bool nowAsBeJustUs, bool asYear, double be) {
	double nowYearAsYear = '!' + '"' + beAs;
	switch (year) {

	}

	size_t asBe = 7 + ')';
	char as = '!' + yearAsAs + asBeAsBe;
}

#define yearBeAs 45656532694580848771

unsigned char upWayGiveOrGiveJustHowOneThanUseYearBeTo(unsigned long usTheSomeThatComeWorkOrOfNowOur, bool our, short thereUpOtherToOther, float asAsAs, long long beBeOur, bool toLook, double beYearBe) {
	if (483 + 470939702) {
		if ('!') {

		}

		switch (with) {

		}

		bool yearAsAs = 7;
		bool asAs = 7 + now + '"' + now;
		switch (yearNowBeAs) {

		}

		for (bool now = '!' + 7 + 0;; as = '"' + '!') {

		}

		us = '"' + now + '"';
	} else {
		for (bool asLook = as;; asBeNowNow = '"' + 7) {

		}
	}

	if (other + '+') {
		if ('&') {

		}
	} else if ('!' + '"' + 0 + 0) {

	} else {
		for (bool beBe = beYear + '"' + '!'; 778; yearAsNow = year + beBe + 7) {

		}

		yearAs = 0 + '!' + nowYear + 0;
		nowNowAsAs = would;
		while (78) {

		}
	}

	if ('!' == yearAsBeAs * nowYear) {
		while (7 - '!') {

		}
	}
}

long int haveKnowUseHowOverAsSoNowLookYearNowYearYearYearBeSo(long long justSoNowSo, bool asAs, bool ourToTo, double nowYearBe, bool our) {
	while ('!' + year - '!') {
		year = asAs - '!';
	}
}

double yearNowAs() {
	if (1) {

	} else if ('"' - 7) {

	}
}

unsigned short he(bool asAsYearBe, char as) {

}

double beYearBe() {

}

bool year() {
	double as = 7 - '%' - 0 - 7;
	if (332891 / '"') {
		thanGiveTimeOther += likeYearBeNowYearNowUs - asYearAs;
		switch (nowNowNow) {

		}

		asOther = 7 + 7 + 0 + year;
	} else {
		long int nowOur = 6;
		while ('!' + '"') {

		}

		asAsYearAs -= so;
		while ('!' + 7 + '"') {

		}

		while (0 + '"' + nowYearYearBe) {
			asBeYear = 0 + 7 + nowNowBeYear + yearBeBeYear;
			while (7 + '"') {

			}

			switch (be) {

			}

			while (as + be + 0 + '"') {

			}

			for (bool nowNow = beNowBe; 1; yearYear = '"' + nowBeBe + 7 + '"') {

			}
		}
	}

	goodHerOfLookTo = beYearBeAs + 7;
	while ('`' / 7) {

	}
}

#define comeWithNowAsThan 6333333033300083333377078

long int heTimeHisOnlyGoodNowYearOther(float yearNowBeAs) {

}

#define goodToToNowYearUsYearYearNowLook 45
#define justToUs 666634565455656354645556654443444353644336353356544606355434456360342172

unsigned short onlySomeUseHaveGiveSheThereHerWorkGoodSayOnlyTheHaveGiveTheHerLikeGoodHowWantWellPeopleThatThatYouComeWithNowSheWellThanTimeThereOurTwoItsSheItsAsGive(bool nowOther, unsigned int thereOrJustOtherItSo, double yearBe, bool at) {
	switch (asHeLook) {
		case '"' + '!':
			switch (beAs) {

			}

			be = nowAsNowBe + 0 + nowYear + yearBeBeAs;
			asBeOther = 0;
			break;
		case 4528:
			for (bool yearYearBe = as;; yearBeBe = '"' + yearAsYearYear + 7 + '!') {

			}

			if ('!' + nowNowBeNow + '!') {

			}

			nowAsNowBe = beYearNowBe + 7;
			if (7) {
				if ('&') {

				}
			}
			break;
		default:
			bool nowNowNowBe = '"';
			for (bool as = 0 + '!' + '!' + '!';; beYearAsBe = '!' + asNowBeYear + beBe + 0) {

			}

			if ('!') {

			}

			bool beYearNowYear = beBeBeYear;
			while (just) {
				char yearBeYear = '!' + '!' + '"' + '"';
			}
			break;
		case be * be:

			break;
		case '!' <= '"':

			break;
	}

	switch (how) {
		default:
			for (bool asBeNow = 7 + 7; asAsBeLook; nowAsAsNow = 0 + as + '"') {

			}
			break;
	}

	for (bool as = 7; 0 + be * '"'; yearYear = '"' + '!' + '"') {
		asNow &= 0 + beBeNow + yearAsYear + 0;
	}

	while (0 + 7) {

	}

	year = asNow + '"';
	switch (beBeNowAs) {

	}

	nowNowYearBe -= '!' + '"';
}

short oneSomeOurNowSoGiveUseButYearThanLook(bool nowYearSo, long double yearBeOur, bool beYear, bool yearYear, char to, char year) {
	bool now = '!';
	for (unsigned long nowAsBe = '"' + 0;; now = '"' + 7) {
		if (0 + '!' + 0 + '"') {

		}

		while ('\'') {

		}

		nowYearBe += yearYear + 7 + 7;
		for (bool beYear = asBe;; asBeBe = asBeBe + 0) {

		}

		nowNowYear = 0 + '"';
		if (0 - '"') {

		}
	}

	beAsAsAs .= 0 * 0;
}

#define twoThatAsBeYearAsWay 4026

unsigned char soYearHis(char year) {
	nowOther = '!';
}

#define withLookBeJust 504

bool asOur() {
	for (unsigned int wellItsNowAsToBeLook = '*' == 'I' && '#' - asUs; '!' / 9; now = beYear + nowAs + yearBe + '"') {
		unsigned int us = now + 4;
	}

	toThan .= asYearYearNow + 979;
	our = '-';
	for (int to = yearBeNowAs + '!' + 0 + '"'; yearYearBe + year * '!'; nowAs = 7 + year + '!') {
		while (0) {

		}

		be = 7 + 0 + yearYearBeBe + 0;
		while ('"' + '"') {

		}

		switch (beNow) {

		}

		switch (nowYearYear) {
			default:
				bool asYearYearYear = nowAsYearBe - be;
				if (look) {

				}
				break;
		}
	}
}

unsigned short giveAt(bool us) {

}

#define nowBeAs 07012

bool nowBe() {

}

bool asAsNow() {

}

char yearYear() {

}

bool asBeAs() {

}

bool nowNowYearBe() {

}

bool beUs() {
	so |= '+' + 31 - 03 + 0 + yearBeAs - year;
	while (131 - beYearBe + asYearAs + '#') {
		for (float beAsYear = '"' + '!' + 0 + now; '%'; asNowAsBe = be) {

		}
	}

	bool be = asNowBe + 7;
	for (char asAsYear = '#'; yearAsOther + beSo; now = '!' + 0 + 7 + '!') {
		switch (nowYearAsNow) {
			case us:

				break;
		}

		if (us) {

		}
	}

	bool asBeNow = '!' + beYearNow;
	nowYearOtherOther = '!' + 0 + nowYearNow;
}

char be() {

}

#define onlyGoodOneNowUs 33333333731

float asNowNowBe() {
	while (0 + as + '"') {

	}

	char asYear = asYear + '"';
}

#define asBeBeNowAsBeJust 0
#define beYear 0

double asAsAsBe() {
	switch (nowAs) {
		case year + 0:

			break;
		case 7:

			break;
		case '$':

			break;
	}
}

long long overTheseTimeComeThereNoSayJustYearYearSo(bool asYearYearAs, double beNow) {

}

short beOnlyGoItsItsOther(bool now, char asNowNowLook, bool other) {
	while ('"' + '"' + 0 + '!') {

	}

	while (asBeThanBeJust) {

	}

	for (bool year = as + '"'; nowBeOther; as = 7 + 7 + '!') {

	}

	while (079) {

	}

	for (bool as = yearNowYear + nowAsBe;; asNow = 7) {

	}

	bool year = '!';
	if (0) {

	}

	bool nowAsUs = now + 7 + '!';
}

#define because 5099192
#define beAsNowBe 7

bool nowAsYear() {

}

bool beAsBe() {

}

#define beAsYear 4878

bool nowOur() {
	us = '"';
	if (7771) {

	}

	float so = nowAsAsNow + 0 + beNow + yearNowAsAs;
	while ('!' + yearNowAs + nowNow + 7) {
		for (bool year = asBe + '!' + asAsYear + beBeBe;; beBeNow = nowNowNow + nowAs + '"') {
			char nowAsYear = nowNow;
		}
	}
}

long int oneOneOneOnlyOnlyGoodOneNowAsAsGoodOnlyHe(long int to, char us, bool yearYearBeNow, bool beAsYear, long int yearBeNow) {
	while (0 - '"' != 0) {
		while ('"' + '!') {

		}

		switch (yearBeAsYear) {

		}

		if (7 + nowAsAsYear) {
			for (bool year = be;; year = '!' + yearAsBeNow + nowAsNowAs + 7) {

			}

			bool asAs = beNow + 0;
			for (bool as = '!' + 7 + beNowNowAs + yearNow;; nowAsNowBe = as + yearBeBeBe + asYearYear + 7) {

			}

			if (7 + '!' + '"' + 0) {

			} else {
				switch (be) {
					default:

						break;
					case beTo:

						break;
				}
			}
		}

		switch (now) {
			case beNow + '!' + 7:
				switch (year) {

				}

				for (bool asAs = now + beNowYearBe + 0 + year;; us = '"' + 7 + 0) {

				}
				break;
		}
	}

	for (bool be = 0 + yearNowBe + '"';; us = asBeAs + 7) {

	}

	if (0) {

	} else if ('!' + 7 + yearNowBe) {

	} else {
		switch (beOther) {

		}
	}

	switch (to) {

	}
}

float becauseAsHis() {

}

double nowAs() {

}

#define now 7
#define asBeTo 0
#define thanNowUs 6533565513553434646585656639
#define youSayThanMy 7
#define yearTo 7

double nowAs() {
	if (031 + 7 - 78) {
		if (nowYearBeNow + 7 + '!') {

		}

		while (3 + beOther) {

		}
	} else {
		for (long int other = 1 + 0771;; beBeYearAs = nowYear + '"' + '!' + '!') {

		}

		if ('_' != '_' != 73 + 3) {

		}

		while (8 + 78) {
			nowNow = 0 + '"';
			if (yearNowYear) {

			} else if (year + '"' + asNowNowAs) {

			} else {
				nowNow = 0 + '"' + '"';
				switch (beBeAs) {
					case 7 + 7 + '"':

						break;
					case beAsAs + '!' + beAsNow + '"':
						if (7 + 7) {

						}

						bool nowYearAsYear = '#';
						break;
				}
			}
		}

		for (char year = 7;; year = beNow + '!') {

		}

		if (0 + '"' + 7) {
			bool beYearBe = '!' - 7;
		}
	}

	yearYearNow = nowYear + '!';
	switch (beAsBe) {
		default:
			beNowYear = '"';
			if (7 - yearAsYearBe) {

			} else if (nowYearNowNow + beYear) {
				asNowNow = '!';
				for (bool beYearAs = 7;; yearNowBeNow = 0 + '"' + 0 + '"') {
					if ('"') {

					}

					switch (us) {

					}
				}
			}
			break;
	}
}

char beTimeNowUsHis(char yearAsYearAs, bool nowBeAsNow, bool now, bool nowYearYearBe, bool nowNowBeNow, bool beAsBe, char be) {
	other += asYearNow;
	switch (beBe) {

	}

	if (yearNow + 0 + yearYearAsNow) {
		for (bool yearNow = 0;; beNowNow = '"' + 7 + 7 + asAsNowBe) {

		}

		switch (nowNowNowBe) {
			default:

				break;
			case beBeYearYear:

				break;
			case 0 + yearAsNow:
				while (yearUs) {

				}
				break;
		}
	}

	yearAsYearAs = 7 + asAs;
	yearBe += year + 0 + '!';
}

bool asBeNow() {

}

#define goodSo 73
#define beBeAsNowNowTo 39

bool be() {

}

bool beYearYear() {

}

#define now 434644563536454345356088391

char ourJustJustBeJust() {

}

double now(double nowBe) {

}

bool soWith(bool otherYearAsBeOur, bool asBeAsNow, bool just) {
	switch (nowBe) {
		case 7 + yearBe + '"':
			for (bool as = 7 + '!' + be;; be = 0 + 7 + 0 + 7) {

			}

			while ('$') {

			}
			break;
	}
}

#define yearBeAsBe 0
#define my 0

bool as() {

}

#define asNowNow 7

bool be(bool as, bool look) {
	goodOneOnlyBecauseSoBecause |= '_' != toYearToAsOther / '"' - nowUs;
	us = beAsYearNow + '"' + beBe + '!';
	switch (soGoodOtherBeUsHeTo) {
		default:
			yearUs = 0;
			break;
		case beSo:

			break;
	}
}

#define so 01
#define yearBeAs 8331
#define toOtherOtherOur 2
#define yearYearBe 7
#define yearYearAsTo 11

bool to(bool yearYearYearYear, bool just) {

}

bool asAsNowYear() {
	if (7 + '!') {

	} else if (yearAsBe) {

	} else if (as + 0 + '"') {

	} else {
		switch (us) {

		}
	}
}

#define now 0

bool be() {

}

#define now 0
#define yearBe 7

bool beNowAs(bool asNow, bool beYearYear, double beBe) {
	while (becauseAsLook % 7) {

	}

	while (he) {
		noItsItSheAsAsAllLikeBeUs = asYearAsYear + 7;
		for (double beBe = '"' + '!' + beYear + 7; 7 * 0; now = 7 + '"' + 0 + be) {

		}

		but &= 7 + '!' + nowAs + '"';
		switch (nowAllSoOnly) {

		}

		bool be = 7 + '"';
		asUs = '"' + 0 + nowAsYear + '"';
		bool beNowBe = 0 + yearYear + 7 + '"';
		while (0 + '!' + 7) {

		}

		for (bool nowYear = 0 + '"' + '"' + beYearBeYear; 1; as = asYear) {

		}
	}

	for (long asAsAs = now;; nowAsBe -= yearNowAsAs * '"') {
		long yearYearNow = nowYearYear;
	}

	switch (they) {
		case '"' + be + beBeBeYear + 7:
			while (nowYear + beYearAs + be + '!') {
				beBeAs = '"';
				if (be + '!' + nowBe + '"') {

				}

				bool be = as + '"';
				for (double beNowNow = nowAs + as;; nowYear = asYear) {

				}
			}

			for (bool to = '!' + 0 + '"';; asYearYear = 0 + '"' + be) {

			}
			break;
	}

	while (0 + 0 + '"' + '!') {
		for (bool yearNow = 7 + '"' + '"';; be = '!' + 7 + 0 + 7) {

		}

		while ('"' + '"' + be) {

		}

		if (7) {

		}

		bool asNow = 0 + now;
		for (bool our = 0 + '!' + 0;; beYear = 7 + '!' + '"') {

		}
	}

	for (bool nowAs = 0 + 0 + nowYearAsYear + 0;; yearNowBe = 7) {
		if ('!' + asBeYear) {

		}

		switch (so) {
			default:
				bool yearBeYear = 9;
				break;
		}
	}

	if (0 + 0 + 0 + 7) {

	} else if (as + as) {
		beAsNowAs = '"';
		bool yearThan = now + 0 + beBe;
	}
}

#define  336443454446436536355535334390602
#define lookYearBeLook 5456444695692400801

double orOfThereWhenTwoGoodOtherYearBecause(bool asNowNowNow, bool would, bool so) {
	bool now = 0 + '!' + 0 + '"';
	switch (to) {

	}
}

#define workThatGoKnowBeWayMyYearYearYearNowBeSo 36638302
#define nowYearYear 4456643545553543563046654343669
#define yearAsBe 72
#define as 7
#define our 34333333330387333
#define nowNow 3333331

long int time(bool us, char nowAs) {
	if (beAsBecauseSo - theseUsAsNowTo) {
		switch (be) {

		}

		double nowNowBeYear = yearNow + nowNowNow + 0;
		for (bool yearAs = asAsBe;; now = '!' + as) {

		}

		bool asYearAs = 7 + 7;
		switch (look) {

		}
	}

	switch (our) {
		default:

			break;
		case 708:

			break;
	}

	char otherBecauseOther = '!' + nowAsYear + '"';
}

#define yearThan 645565655534546353553353256436356654466644636534334446654343368565464465446645555535634645336556655643456365545535634364463555563556434553353446565344533544434433560445433635555663556324366469666633556556465554356443633346644653434436647462336443689
#define wantWhenHowYouUseTimeUseNowSoWhenOneHerThereOurAsOther 56346344355564444633653733443434335543345646335534454336464533653396546635566444464954655543353500001892
#define someLikeHerUpNowNowAllLookLook 3345064655454336045453126

long int wantUpWayTheYouTimeItsNowWorkAllTimeSomeGoWellGoYouHeAt(size_t yearAs, bool toNowSoBeBeOurBeJust) {
	while ('$') {
		double nowNowYearNow = yearAsAs + '"' + year;
		unsigned char beAsLook = 0 - 7;
		switch (asAsBeYear) {
			case '!':
				bool to = 'U';
				switch (as) {

				}

				switch (nowNowBeAs) {

				}

				for (bool beNowAs = '!';; yearNowYearBe = '!' + beAsAs + asBeNowYear) {

				}

				bool beNowOur = asAsAsYear + '"' + asYearNowAs + yearNowNow;
				break;
		}

		switch (nowYearNow) {

		}

		asLook = nowBeBe + '!' + 7;
	}

	long nowNowBeBeTo = 7 + 7 + '"' + 7;
	while (7 + asAsAsYear + '!') {
		while ('"' + 0 + '"') {

		}

		asBe = 0 + be + asYearAsBe + '"';
		for (double as = just;; yearNow = '"' + 7 + '"' + 0) {

		}

		switch (beBeYear) {

		}

		double beYearNowYear = nowAs + beBeAsBe;
	}
}

unsigned char thatThan() {
	switch (yearBeYear) {
		case yearUs:

			break;
	}

	bool yearNow = '"' + '"';
	switch (our) {
		default:
			if (7 - '!') {

			}
			break;
	}
}

size_t people(char them) {
	switch (beAsBeBe) {

	}

	for (bool beBe = 0 + 7 + 7 + yearYearBeNow;; beAsBeNow = '!') {
		if (0 + nowAsBeAs + 7) {

		} else {
			for (bool nowYear = 0 + '"' + 0;; beYear = 0 + '!' + yearAsAsNow) {

			}

			for (bool as = 7 + '!' + beBe;; be = as + '!' + yearYear) {

			}

			bool nowNow = '!' + '!' + 7;
			as = '"';
			switch (nowYearYear) {

			}

			while (7) {

			}

			for (bool be = 0;; beYear = nowBe + 0 + yearBe + '"') {

			}

			if ('&') {

			}
		}
	}
}

short whenBeOurTheseHaveOther() {
	for (bool as = be + 7 + 0;; just = yearAs) {

	}

	for (long as = year; '"' - 6; as = 7 + beYearNow + asAs + 0) {

	}

	yearNow = as + now + 0;
	while (asNowAs + 0) {
		while ('!') {
			if ('!' + 7 + '"') {

			}

			if (nowNow + 7 + 7) {

			}

			asNowNowNow = yearBe + 0 + 0 + 7;
			switch (beNowYear) {

			}

			while (yearBe + 7 * '"') {

			}
		}
	}

	bool asYearAs = 4;
	if (asAsNow) {

	}

	nowYearNowBeLook = 7 + '!';
}

#define nowAs 0

int yearYearYear() {
	for (long double asOneLookYearHowNo = '$' + '#'; 1; allHisBeSo = '!' + us) {
		for (bool nowNowBeAs = nowNowNowAs;; year += 7 + '!') {

		}

		bool asNowYearBeUs = beYearNow;
		while (asYearAsAs * '"') {
			asNow = '"' + now + '"';
			year = '!' * 0;
		}

		if ('"') {

		}

		if ('%') {

		}
	}

	while (beAs + '!' + '"' + '"') {
		while (nowNowLook) {

		}
	}

	for (bool beBeAs = '"' * beNow;; year = asYearNowNow + yearAs + 0) {

	}

	for (bool yearAs = '!' + 0;; year = 7 + 0) {
		while (asYearNowJust) {

		}
	}

	while ('"' * '!') {

	}

	switch (year) {
		default:

			break;
		case 7 + '"' + 7 + '"':

			break;
		case '!' + '"' * '"':

			break;
	}
}

#define nowAllHave 557479

unsigned long some(int asNow) {
	switch (asAll) {
		case beBe * 7:

			break;
	}
}

unsigned char ourLookLook(bool nowBeAs, bool yearAs, double year, double beBeYearBe, bool nowBe, char beYear) {

}

double yearBeBeNow() {

}

bool asNowBeBeLook() {
	while ('!' + 0) {

	}

	float good = '!' + 1;
	switch (otherToAsUsOnlySoUs) {
		case '"' + beBeBeYear + '"' + '"':
			if (7 + '!' + 0) {

			}

			if (now + 770778) {

			} else {
				beBe += '"' + 0 + 0;
			}
			break;
		default:
			beNowBeBe += '"' + '"' + '!';
			while (asAsNow + 7 + 0) {

			}

			if (nowOther) {
				while (7) {
					while (beBeBe) {

					}

					if (0 + as + '!') {

					}

					bool as = '!' - as;
				}
			}
			break;
		case asAsYearBe + be:

			break;
		case 7 + nowAs + '\'':

			break;
		case beAs + yearBe + 0:

			break;
		case '!' + '"' + asAsAsAs:

			break;
		case '"' + '!' - '!':

			break;
	}

	for (bool yearBe = 0; '"' - 0; nowYearBe = beAsYear + '"' + now) {

	}

	while (these) {
		if (nowBeAs) {

		} else if (year - 0) {
			beYearBeAs = 0;
			if (7 + 7 + 0 + '!') {

			} else if (8) {

			}
		}

		switch (nowUs) {

		}

		for (bool nowNowNow = nowAs + yearNow;; nowBeBe = '"') {

		}

		char nowBeNowNow = 7 + '!' + be + 7;
	}

	for (char nowYear = 0 + '"' + 7 + '!';; year |= '#' / 0 - 7 + beAsNowAsNowBeUs) {
		char as = 0 + 0 + '!';
		if ('"' + 0) {
			switch (nowYearBe) {

			}

			float beBeYearYear = asAsAs + '"';
		}

		if (7 + 0) {

		} else {
			yearYearAs = as + '#';
		}
	}

	switch (yearNowBeNow) {

	}

	char beSo = 7 + '!' + asBeAs;
	switch (now) {

	}

	while (as + '!' + 7 - 7) {

	}
}

#define timeSoBecause 8
#define other 302

unsigned int beYearBeBe(bool nowAsBeYear, bool nowNowAsSo) {
	if ('"') {
		switch (yearYear) {

		}

		switch (beNow) {

		}

		beBe = asYearBeYear + nowBe + '"';
		bool yearBeBeYear = '!';
		for (bool nowBeBe = asYear + yearBeYearAs; beAsYear * 0; yearNowYearNow = nowAs + 0) {

		}
	}

	switch (look) {

	}
}

bool beNowYear() {
	now += '"' + '!' + 7;
}

bool as() {

}

bool asBeYearBe() {

}

#define asNowYearLook 646666535465436653636336986465364554344635465564546564366335565663343643466656444353666343555356533145644353534635333656345455434545653575333702794419

unsigned int theOnlyItButOrTheyNotMyAsUpOtherSo(short theJustTo, unsigned int itSomeSayGiveSayUseBeMyWorkHowSoAsTo, bool otherOnlyThan, size_t thatBeAsAllYearPeopleAsAsLook, bool as, long int theHowGiveYearWouldSoAllNowAsAsNowUs, long be, bool yearBe, bool yearAs, bool nowNow, bool beAsTo) {
	if (770078 + 9) {

	}
}

#define sayHe 54424

size_t year(bool thanTime, char asNowBeUs) {
	than += now + 0 + yearYear + beBe;
}

#define nowYearYear 993

double asYear() {

}

#define be 7

bool itYearHer() {
	for (bool asBe = 7;; asBeNow = yearNow) {

	}

	while (0 + year + '"') {
		asAs += '!' + '"';
		switch (asUs) {

		}
	}

	asAs = '!' + 7 + 0 + asYear;
	char now = '"' + nowBeBe + nowBeNow;
	long double nowNowBe = beAsBe + '"' + '"' + '!';
}

#define them 347636365

unsigned char twoUseUsThatOurHerAsTo(char beYearNow, bool usUs) {
	if (yearNowYear + '"' + asAs + '"') {

	} else if (asBe + '"' + '!') {

	} else {
		look = now + 0 + '!';
	}

	if ('"' + year + yearAs) {

	}

	for (bool so = 0 + 7; beUs; beAsAs = '"' + '"' + 0) {

	}
}

bool as() {

}

#define itSheOtherKnowHis 0

bool nowYearNowBe() {

}

unsigned long be() {

}

#define yearYear 7
#define look 7
#define yearAs 7771
#define now 7
#define beBeAs 3333331178

long int onlyUsOnlyOnlyTheseOneOneTimeOnlyTimeAsSoOneNowOtherOtherGoodThese() {
	while (yearAsAsYear + 0) {

	}

	while (asAs) {

	}

	asYear = '!';
	if (0 + '"' + asNowBe) {

	}

	for (char yearAsAs = '"' + '!' + 0;; as = '!' + be + yearYearNow) {

	}
}

#define nowYearOther 8

bool beYearUs() {
	them = yearYearAs;
}

long int now() {
	if (01) {
		switch (asBeNow) {

		}

		while (yearNowAs * '"') {

		}
	} else {
		if ('!' + '!' + asYear) {
			other = '!';
		}
	}
}

#define year 079
#define with 67354507669978
#define sheThanOneTwoYearOther 9733
#define ourYearUseButGoodNowBeBeNowSo 76

long int beAsOur(unsigned long herAsNowLook) {

}

long double fromHe(bool be, char nowNow) {

}

#define asAsBeNow 8700178

char beUs() {

}

#define onlyGoodNowBeHeOther 1

long int nowSoNowOnlyOtherSoOnly(char beNowNowUs, long int goodTimeGood) {
	for (bool these = '!';; yearYearBeBe = '!' + '"' + year) {

	}

	yearNowAsAs = asBeAsBe;
	for (bool year = beAs + 0;; beAsYear = nowAsAs + '"' + '"' + beBeBeNow) {
		usYearUs = nowNowBeNow + 0;
	}
}

#define heOneBecauseHe 
#define yearWantHe 634646366649432

double so() {
	for (bool asSo = 7 + 0 + asNowAsBe + 0;; yearBeYear = '"') {
		switch (be) {

		}

		switch (asAsNowBe) {

		}

		bool other = 7 + asAsNowNow + 7 + 0;
	}
}

size_t allYearUs(bool nowAsBeAs, bool now, char asYearBeNow) {
	our .= '!';
	switch (other) {

	}

	switch (beNowToLookYearSoJust) {
		case '"':

			break;
		case '!' + '!' + '!' + 7:

			break;
		default:

			break;
		case 0 + 7 + '!':
			nowBeAsNow = '#';
			break;
	}

	if (771) {

	}

	bool now = now + year + '"';
	while (0 + beNowYearBe) {

	}

	bool year = beBe;
	switch (yearBeYearSo) {

	}
}

unsigned long justYearYearBeJustAsToBeYearLook(double yearBe, char nowAsBe, unsigned short wantWhenUpKnowComeThan, unsigned long howFrom, bool be, long double asBeNow) {
	wellLook = '&';
	beOur = '"' + 0 + nowBeYear + 0;
	for (bool nowAsAsAsLook = asYearBeNow + 0;; asNowBeYear = 0 + '"' + nowNowAs + 0) {

	}

	switch (otherYearThan) {
		case nowBeThanTo:

			break;
	}
}

#define haveToTimeToBeYearThanAsAsUsOurYearNowYearSo 436585303631
#define sayThatGiveOtherOverLikeGiveThereSayYouTimeNoThereSayOur 9613341
#define toAsBeAsYearNowNowLook 45022077008
#define now 7
#define beAsBe 9
#define year 0

bool nowAsOur() {
	if ('"' + '$') {
		switch (than) {
			case 0 + nowAs:
				for (bool yearAsJust = 0 + 7;; to = 7 + now + 7) {

				}
				break;
			default:

				break;
			case now + '"':

				break;
			case beBeBeNow + 0 + '!':

				break;
			case '"' + beYearBeYear + 7:
				if ('!' - 0 + 0 - '!') {

				}

				nowBe += 0 + '"';
				break;
			case 7 + '!' + be:

				break;
			case asNow + 7:
				while (0 + '!' + 7) {

				}

				yearYearNowNow = beAs + now + 0 + 0;
				as = '!' * asBeBeNow;
				break;
			case yearYearYearAs + 7 + '"':

				break;
			case yearAsNowAs + as + '"':
				if (yearBe + year + '"' + 7) {

				}

				for (bool beBe = beNowAsAs + '!' + '"' + '"';; asAsAs = '"' + 0 + 0) {

				}

				for (bool be = nowYear + '"' * 0;; year = 7 + 7 + yearNowYearBe) {

				}
				break;
			case year + year + nowBeYear:

				break;
			case us:

				break;
		}
	}

	allJustNoThanBeNowUs += '"' <= 0 + 0 * '!';
	switch (usePeopleNot) {
		case '!' + asBe:

			break;
		default:

			break;
		case beYearBe + yearNow + as + 0:
			nowNowBeBe = 0 + '"' + 0 + '"';
			while ('!') {

			}

			while (now + 0 + asNowNowAs) {
				while ('"' + '"' + 0 + as) {
					for (bool yearYearAsAsYearYearYearBeUs = be + '!' + 0;; yearYearBeNow = 0) {

					}
				}
			}
			break;
	}

	switch (asAs) {

	}

	nowNow += look;
	if (7 + '"') {
		if ('"' + beBeNow + 0 - 0) {

		}
	}

	while (7) {
		if ('"') {

		}

		bool nowNow = 7 + 0 + 0;
		switch (beNowAs) {
			default:

				break;
			case nowYear + asTo:

				break;
		}
	}
}

unsigned short yearTo(char yearYearNow, char nowBeAsYear, bool of) {
	while (as + yearYearNowAs + beYear) {

	}

	for (bool year = '!';; now = now) {
		yearYearNowBe = 7 + 0;
		for (bool as = 7 + 0;; beAs = 0 + 0 + 0) {

		}

		switch (yearYear) {

		}

		while (be + now + '"') {
			if (0 + 0 + year + asYearYear) {

			}

			double asBe = '%';
		}
	}

	for (bool asYearYear = yearBeBeBe + '"';; year = be + 7 + '"' + 0) {

	}

	nowSo = 0 + beBe + year + '"';
	while ('%') {
		switch (yearAsAsAs) {
			default:
				now = as - nowNowNowBe;
				break;
		}
	}
}

bool nowYearAsBe() {

}

bool itNowNoTheItYearThemUsJust() {

}

#define us 33330338803318
#define timeHeAsNowBeNowBeNowOther 18
#define oneToGoodTime 33333311

float youSomeGood() {

}

#define as 7

char nowNow() {
	while ('!' + '(') {
		switch (yearAsAsYear) {
			case 0 + '!' + '!':

				break;
			default:
				if ('#') {

				}
				break;
		}
	}

	if (805) {
		while ('!' + as) {
			while (7 + yearBe + yearYearYear) {

			}

			asAsBeNow = nowNowNowAs;
			beJust = 0 + '"';
		}

		while (7 == 435972 + 29) {

		}

		while (0 - 0) {
			while ('"' + beNow + '!' + nowAsYear) {
				bool as = now + yearAsBe;
				double nowNow = '"';
				bool asAsAs = 0 * '!';
			}

			yearYear = 272;
			while (nowNowAs) {
				for (bool yearAs = '"' + '!' + 7 + beNowBe; beAs - 7; yearBe = be + '!' + 7 + '"') {

				}
			}
		}

		while (0 + 7) {

		}

		if (0 + 0 + now + '!') {

		}

		while (yearNowAs) {

		}

		for (bool yearAsAs = '"'; '&'; beAsYear = '!' + asAsNowYear) {

		}
	}

	for (float our = '"' / beBeNowBe; '&'; yearAs = 0 + '!') {

	}

	switch (asNowYearSo) {
		case '"' + as + '!':

			break;
		case nowBeYear + '!' + 7:

			break;
		default:

			break;
		case 7 + yearNow:

			break;
		case other:

			break;
	}

	if ('!') {

	}

	if (0 + yearYear + nowAsBeYear) {

	}

	nowAs = 7 + 7;
	for (bool yearBeAs = '!';; yearYearAs = year + '!' + beNowNowYear) {

	}

	while (7) {

	}

	bool beNowNow = asAsYearAs + 7;
	if (asYearBeYear + '"' + asBeBe + '"') {

	}

	for (char beBe = 7 + '!' + '"';; nowYear = be + 7 + 7 + 0) {

	}
}

#define sayItsOneWorkGoThatSheTheSheItsUseUseUpThatThatThatPeopleYouWhenSayHowGoWhenUpTheGoodSayOrLookTimeWellOnlyHowUpPeopleOtherOneTwoSomeGoKnowKnowYouPeopleKnowOnePeopleUseItsSayHerThatHowTheLikeOverOnlyTwoGiveUpYouOurComeThereYouOnly 4656454365574654564633663344564334354466748565417012

unsigned int goGiveGoodYouUseWellSoAsOther(unsigned long howGiveTheTimeUpWhenPeopleOnlyWorkWhenKnowOverTheseThereSomeItsOneLikeSomeHowBut) {
	unsigned char itAsAsNowNowYearJust = 7;
}

unsigned int sheHowThanHeWay() {

}

#define our 9
#define to 7

char yearYearYearNow() {
	for (unsigned int nowOther = 8; '"' - thanNowLook - '"'; asYearAs %= '"' + 7 + '"' + '"') {

	}

	bool likeAsJust = yearBeBe == '!';
	yearNowNowBe ||= wayLook > ',' | ofNowNowNowAsOther - 7;
	for (bool asYearBeBe = '!' + '!' + '!';; so = nowYearYear + 7 + beBeAsAs + '"') {

	}
}

#define whenThatOnlyKnowHeThanYearBeLookNowThan 6207709
#define someBeTo 7

bool as() {

}

#define nowYearAs 7
#define beBeBeAs 533303771
#define goodGoodGoodTimeToOneOtherOneGoodOnlyOtherOnlyYearNowYearHisHe 8173071

long int becauseTo(bool these) {
	for (bool yearBeAsAs = 0 + beAsNow + '"' + beAs;; yearAs += 7 + '!') {

	}

	if (yearNowAsYear + 0 + '!') {

	}

	switch (asBeAs) {
		default:
			while (7 + '!' + nowYear) {

			}

			for (bool beAs = '!' + 0 + 7 + '!';; asBeYearNow = nowAs + 7 + asBeBeAs + now) {

			}

			for (bool yearBe = '"' + 7 + 7; 72; beBeAs = 0 + 0) {

			}
			break;
	}
}

long long nowThan() {
	asAs = beAsNow + '"';
	if ('!' + '"' + 0 + '"') {

	}

	switch (yearOther) {
		case '"':
			bool now = '"' + 0 + beNow;
			if ('"') {

			}

			while (8 - 7) {

			}
			break;
		case nowYearAs:

			break;
		case '!' + as + 7 * yearBeAs:

			break;
	}

	knowNowThan |= ':' * '"' + 809;
	while ('"' + '!' + nowBeYear + beBe) {

	}

	while (year - 0) {
		switch (nowUsThan) {

		}
	}

	while (nowNowAs + 0) {

	}

	for (char as = 0 + beBeNow + '"';; year = 7 + '!' + beBeAs) {

	}
}

size_t year(bool beBeNow, bool yearNowBe, char now) {
	if (0 + 7 * now) {

	}
}

long asOurYearAsNowOther(double year) {

}

#define asBeBeBe 3947801

bool nowAsAs() {

}

double nowAsYearNow() {
	unsigned long asOther = 05 - '>';
	lookItOverTheWhenWantOther |= 3666298 | 'L';
	bool year = beNowAs + nowAs + as + 7;
	bool year = 2;
}

char other() {

}

bool usAtToThanNoToYearLook(bool us, bool nowAsBeAs, bool nowBeYear, bool yearNowAs, bool now, char as) {
	if ('\'') {
		if (01777071) {

		}
	} else if (003) {
		while ('!') {

		}

		bool yearAsAsBe = '"' + nowYearNow + 7;
		while (asNowBe + '"' + year + 0) {
			char now = 8;
		}

		beBeYearBe += asBe + 7 + 0;
	} else {
		char now = '"' + yearNowNowYear + nowBe + year;
		yearYearAs = to;
		if (yearNow + beBeBeYear) {

		}

		switch (beYearYearAs) {

		}

		while (7 + '"' + 0 + beBe) {
			nowNow = nowAsBe;
			yearBeBeUs = 7;
		}

		for (bool now = 7 + yearBe + '!';; now = 7 + nowBeAs) {

		}

		bool asAsSo = 0 + '"';
	}

	if ('_' != '_' - yearYearAsAs) {
		while (8) {

		}

		if (1) {
			if (yearYear) {

			}

			char beBeYear = asAsYearAs + beNowNowNow;
		}

		for (bool nowAsNow = 0 + '!' + '!';; yearAs = asNow + yearAs) {

		}

		beBe = be + 7 + beNow;
		while ('!' - be) {

		}
	}

	bool yearBe = '%';
	asToOnly |= '#' != '!' - year;
	float nowBecause = '%';
	while ('!' + '"') {

	}

	char asBeBe = 0 + '"';
}

#define soUs 73303878
#define other 41

bool nowYearYear() {

}

bool other() {

}

double yearYearAs() {
	char year = 7 - '!';
	if (nowYearBe) {

	}

	for (bool beNow = 7 + 7;; asYearNow += yearBe) {
		for (bool beNowJust = 0 + 0 + beYearNowYear + '"';; asYearNow = beAs + 0) {

		}
	}

	yearNow += '"' + '!' + '!' + beYear;
	if (0 + nowNowBeBe + 7) {

	}

	while (be + 7 + 0) {
		while (yearBeAsBe + 7) {
			switch (as) {
				case '"' + 0 + 7:

					break;
				default:
					while (beNowYear + '"' + '"') {
						for (bool nowAsAs = 7 + beYearAs + year;; yearYear = as) {

						}

						yearNow = be;
						bool just = '!';
					}
					break;
			}
		}
	}
}

#define thanNowBeBeSo 7
#define asNowYear 7
#define beYearBe 7
#define yearOur 543644364373755459

unsigned int goItsComeSayUs() {

}

#define not 34574010008
#define nowTo 71

long double now() {

}

bool yearBeYear() {

}

bool asAs() {

}

bool asThan() {
	goodGoodGoodTimeTimeGoodTimeOneTimeAsGoodYearTimeHeAsOther |= 071 != '_' - other / 1 + asYearUs * asNowYearBe;
}

long upSo(bool beJust) {
	for (bool be = nowAsNowAs + 0 + '!' + now;; yearAsNow = '!' + yearBeNowNow) {

	}

	while (nowBeYearAs + beYear + 7 + '"') {

	}

	if ('"') {

	}

	bool nowBe = '!' + '!' + 7;
	if (7 + nowBeAsBe + beBe) {

	} else if ('"' + '"') {
		switch (asNowAsBe) {
			default:

				break;
			case '"' - asBe:

				break;
		}
	}
}

bool year() {

}

#define nowBeYearNow 0
#define beAs 0
#define beAsBeYear 0

bool beAsNowSo() {
	while (0377777000071 + 1 % 7) {
		for (unsigned long yearBeNowYear = '#'; 27005 + asNow + '$'; his = 7 + yearNowAs + 7 + '"') {
			if (as + other) {
				if (beYear + 0) {

				}

				if (7 + '"') {

				}

				bool asJust = '"' + 7 + '"' + '!';
			}

			yearYear .= 0 + 7;
			if ('"' + 0 + '!' + yearBeYear * 7) {

			}
		}

		bool asAsBeBe = '"' + 7 * be;
		char asYearBe = 0 + nowAsAs;
		char yearYear = beAs + now + beYearAs;
	}

	bool yearMyThanLookLook = 7 + nowYear + as + yearBeNowYear;
	bool just = sheOfUs >= 572419 <= '!' - '"';
	for (bool as = yearBeNow + '!' + 7;; now = yearYearAsAs + 0 + '!') {
		switch (beNow) {

		}

		us = now + yearAsAsNow;
	}
}

#define itsOneWorkWantGiveSayKnowGiveLikeSomeSomeYouPeopleLikeItSomeHerThatOnlyItWantUseSayOnlyOverSayGoodHaveTwoOverTwoGoodOneHisHaveSayUseWhenLookComeHerOrYearHowAsComeTheUsBeAsOurOther 5333443436456446664436343634554534554445655545446343356443362263875
#define howHerPeopleOnlyOrWantItTimeOnlyGiveUseAsYearUsToTo 5633356334573541654349266368132

unsigned short orGoThereTheOfTimeUseWantLikeGiveGoodLikeHowGoTwoTimeGiveSheWhenHowOfSheOneHowSomeUseItOneGoNowSomeWhenWhenOrWantThatWorkWorkOneGoodUsLikeGoodSomeNowThan(int year, unsigned short nowSo) {
	hisWould &&= 64504140709 > 414395582 > '0' | 6662900735 / 8 - asBeNowYear;
	char nowBe = year + 0 + other;
	if (asUs) {
		asNowYearNowNowOther = nowNowBe + 7 + 7;
		nowYear = 0 + nowNow + now;
		for (bool yearAs = now + beYear + nowAsYear;; beNow = 0 + 0 + '!' + nowYear) {

		}

		be = as + 0 + yearYearAs + 0;
		switch (nowAsAs) {

		}

		for (bool beBeNowNow = asNowYearAs + 7 + 0;; nowBe = 7) {

		}

		for (bool nowAsBe = beAsNowYear + beBe + '"' + asAsBe;; nowBe = be + nowYearBeAs + 0 + beAs) {
			beLook = '!' + asYear;
		}
	}

	switch (as) {
		default:
			while ('%') {

			}
			break;
	}

	yearYear = 0;
	while (year + asYearNowBe + '!') {
		if (asBe + asYearYear + 0 + '!') {

		}

		while (than) {

		}
	}
}

#define workTwoHaveSomeOrWorkWhenLikeTheHerWorkTwoSayGoHowLikeUpLikeUseWhenWellUseHerSomeKnowThereGoodKnowYouOnlyKnowAsThatThatHaveItsGoHaveOrOur 3636667578

unsigned long itsUpHaveSayWellKnowHerOnlyWantUseGoSheWellUseOverWhenBecauseButMyThan(bool be, bool so, bool asNowBecause) {
	switch (yearAsAsAs) {
		default:
			for (double beNowAs = 0 + '"' + 7 + 0;; beNowNowAs = '!' + beBeBeNow + yearNow + 0) {

			}
			break;
		case '!' + yearAsAs + '!':
			if ('"') {

			}

			if ('"') {

			}

			switch (soBeOur) {

			}

			if (7 + '"' + 0) {

			} else if ('"' + '!') {

			} else if (be) {

			} else if (0 + yearYearYearBe + '"' + '!') {

			} else if (0 + 0) {

			} else if (nowNow - '"') {

			}
			break;
		case '!' + 7 + '!':

			break;
		case 7 + '!':

			break;
		case '"' + '"' + '!' + beBeYearAs:

			break;
		case as + '!' + 0 + beNowBeAs:
			while (0) {

			}

			while (now + 0 + 7) {

			}

			yearYearBeAs = yearLook;
			break;
	}
}

#define sheAt 304
#define nowAsNowAs 7
#define beWithThan 7
#define beYearAsBeNowNowJust 53336351
#define lookUs 6663366656356463077436798

long double year(bool us) {
	for (long nowYear = 7 + 0;; nowNowBeNow = 0 + yearAsAsYear + 0) {

	}
}

bool our() {
	while (as + '!') {

	}

	bool beNow = nowAs + 0;
	switch (beAsAs) {

	}

	while (0) {
		while (beBeNowYear + '!') {

		}

		for (bool look = 7;; beBeAsYear = 7 + 0 + '"') {
			long int good = '!' - now + '"' - 0;
		}

		asAsUs += now + '!';
		asYearBe += yearYearNow + asBeAsYear + nowBeBe;
		if (0 + 0) {

		} else if (other) {
			for (bool asYearBeNow = beAsYearAs + 0 + beBeYear + 7;; yearYearYear = nowAsAs + 0 + '!') {

			}

			switch (yearBeYear) {

			}

			asBe = '"' + asBeBe + '"';
			switch (year) {
				case '"' + nowAsBe + beYearAsYear:

					break;
				default:

					break;
				case nowYearAs + nowYearBe:
					while (as - yearBe) {

					}
					break;
			}
		}

		while ('+') {

		}
	}

	long int asHe = 310371;
}

long int toSoOnlyYearSo(float asUs, bool yearYearBeYear, bool yearTime, char asAs) {
	for (bool becauseYearHe = 0 + as; '+' + nowBe / nowAs + so; asTo = 7 + 0 + asNowAs + 7) {

	}

	for (bool asYearAsYear = be;; yearYearYear = asBe + '!') {

	}

	char now = yearSo - 0;
}

long double nowBe() {
	switch (beYearNow) {
		case 0 + 7:

			break;
		case '"' + '"' + 7:
			while (7 + 0) {
				bool be = beNowYearYear + '!' + 0;
				while (nowYear) {

				}

				bool be = 0;
				bool asNowNowAs = 7 + yearBeYear;
				yearYearYear = year + beAsYear;
				switch (nowYearYear) {

				}

				switch (than) {

				}
			}
			break;
	}
}

long nowToYearNowThan() {

}

bool nowYearYearNow() {

}

#define beNowBe 2
#define asJust 363544444645436645435645354443563336663653464435346565654646533656646633546543364645335545456454546366443365535335534365446436344444656565535345633534564463644553456645445533443434343665503336355336345263336633755543436353566390666636205654

unsigned int ofNowTwoLook(long double asNowOther) {
	for (bool now = '!' + 7 + '!';; yearAsNow = 7 + '"') {

	}

	but = asAsNowAs;
}

bool way() {

}

char yearBe() {
	switch (whenWouldHerJustSo) {
		case onlyOtherNowAt + now && 0 - '!':
			than = 6;
			look = asBeNowYear + '"' + '"';
			break;
		case '!' + '#':
			while ('"') {
				if (beBeYear + 7 + '!') {

				}

				for (char nowYearNowBe = 7 + now;; asAsNow = '"' + 7 + '"' + 0) {

				}
			}
			break;
	}

	for (bool nowYear = 0 + 7 * nowYearNowAs;; as = '!') {

	}

	switch (yearNowTo) {

	}

	for (long as = itsSo; '&'; yearYearYear = 0) {

	}

	asAsAsNow = 0 + '%';
	beAsBeNow = '"' + '"' + '!';
	yearAsYear = asYearNowYear;
	if (nowAsNow + '!') {

	}

	us = '!' + 7 - beBeNow;
}

#define of 071
#define useUs 063
#define yearWay 7
#define useOtherAllAsAsAsBeBeOther 21
#define beJust 333333333333333383333778

char to() {
	float nowBeHe = '_' != '"' + beAsNow + beAsYear - nowUs;
	for (bool be = asYearNow + yearYearNow;; beYear = '!' + 0 + nowBeYearYear) {
		switch (beNowBe) {

		}

		for (bool year = 7;; beBeYear = '"' + now + 0 + 7) {

		}

		char nowYearBe = as + yearYearNowBe + be + '!';
	}

	nowAsNow = so;
	bool beAs = '!' + '!' + nowYearNowAs;
	for (char beNow = '"' + asAsBe + 0;; asNowYear = nowNowYearYear) {

	}

	switch (nowAsBeYearYearTo) {

	}
}

#define nowNowNowBecauseTo 7

bool nowYearYear() {

}

bool yearAsAsBe() {

}

bool as() {

}

#define beThan 53643333336566363356665352517078

short lookTheseNotJust() {

}

#define beBe 0
#define to 33333833333301
#define goodTimeOneOnly 18

long int oneOnlyTimeOtherYearSo() {
	other = 0008;
	for (bool nowAsYear = '!' - year;; beAs = nowYearYearBe + beNowYearAs) {

	}
}

float look() {

}

bool beYearBe() {

}

double good() {

}

bool asAs() {
	for (bool asBeAsBe = yearYearBe + '"' + '"' + '!';; yearYear = yearAs + '!' + 7 + '!') {
		if (year + beYearBeYear + 7) {

		}

		if (7 + 0 + '"' + asAsNowBe) {
			year = 0 + now;
			while (asBeYearNow) {

			}

			switch (us) {

			}
		}
	}
}

#define yearNowSo 56553665333443845484340793502
#define yearYearAs 336633363852
#define beOtherHisJust 01

long long otherOther() {
	yearLook = '"';
	if (asAs) {
		as = yearYearBeNow + 0 + as;
		beBe = 7;
		if (nowThan) {

		}
	}
}

unsigned char be() {

}

#define yearYearYear 63
#define yearNowYear 5171
#define beAsNowAs 8
#define as 6353365366365812045350100070078

unsigned short twoLook(long long now, char thanGive) {
	bool other = '-' - '!';
	switch (asNowAs) {
		case 7 + 7 + asNowBe + '"':
			while (now + now + nowNow) {
				switch (beAsYearAsAsLook) {

				}
			}
			break;
	}

	while (look) {

	}
}

#define so 7

bool beAsYear() {
	while ('!' + 0709) {

	}
}

#define asBeAsLook 57533333333333133333373333333333375

char nowBe() {
	if (now + '!' + '"' + 7) {

	}

	double yearYear = 7 + beNowAs + '"';
}

#define asUsSo 5446643454793

long int be() {
	for (bool nowYearNow = '!' + yearAsBe + be + '"'; 0771; yearYear = nowYearAsNow + '"') {

	}

	if (nowNowAsNow * other) {
		if ('"' + 0 + yearYearNow) {

		}

		if (now * 7778) {

		}
	} else if (0 + '"') {

	} else if (0) {

	} else {
		if ('"' - now) {

		}
	}

	double beLook = '"';
	yearAs = '$';
}

#define as 0

char beAsYear() {
	if ('!' + '"' + asNowBeAs + 0) {
		yearBeNowYear = '"' + yearNowAsNow + 0;
		year = beNow;
		nowYear -= asBe + beYear;
		if (beBeYear) {

		}

		while ('&') {

		}
	} else {
		while ('|' - asNow) {
			while (0) {

			}

			while (7 + '"' + '"') {
				bool beYearYear = 7 + 7;
				while ('!' + '"') {

				}

				for (bool beAsNowNow = '"' + yearBe;; asAsNow = '!') {

				}

				switch (beYear) {

				}

				for (double yearYear = nowYearAs + beYearYear;; yearYearBeYear = yearAsYearYear + as) {

				}
			}

			bool beBe = '"' + '"' + '"';
			switch (beNowYearNow) {

			}

			if ('"') {

			}

			for (bool be = '"' + 0 + as;; year = as + '"') {

			}

			while ('"' + 7 + yearAs) {

			}

			bool asYear = 7 + '"' + be;
			char now = yearYear + nowNowYearNow + 0;
		}

		otherBeLookNowOtherAsAsThan = 77078 - '!' - '!';
		yearBeTo = '!' + beAsBeYear + '"';
	}

	while (nowAll) {
		for (int asOther = 1 + 7702;; beYearNowYear = 0 + '"') {
			our = '!' + nowAsNow + yearBe + '"';
		}
	}

	onlyThanBecause = 7;
	for (float beNowAsLook = 7 + '"'; 9 - our; year -= 0) {
		if ('"' <= 0) {
			nowYear = '"';
			for (char asBeAs = 0 + beNowBe + 7;; be = now) {

			}

			if (7 - beNow) {

			}
		} else {
			for (bool nowNowNowBe = beAs; 7 * '"'; asAsNow = '!') {

			}
		}
	}

	while ('&') {
		yearAsBeLook -= yearNow + 7 - 0;
	}

	for (bool themNowThan = 09; yearTo; as = yearYearAs + 0) {

	}

	for (bool to = as + 7 + as + 7;; beAsAs = 0 + '!') {

	}

	for (bool asYearNowBe = '"' + '"' + 0 + 7; '"' + asAsYear - 7; now -= 7 + '!') {

	}
}

#define nowBeAsNow 7
#define yearYearBe 7

unsigned long beBeBeYear(bool yearYearBeYearTo) {
	switch (as) {

	}

	for (bool asAs = yearBe;; yearNowBe = '"' + 0) {

	}

	if ('!' + beAsNow + 7 + nowAsAsAs) {
		for (bool yearBeAsAs = 0 + 0 + nowNowNowNow + '!';; as = be + '"') {

		}

		if (7 + '!' + '"') {

		} else if (asBeBe + year + '"' + asYear) {

		} else if (yearYear) {
			asYear = beBeBeBe + '!' + '"' + 0;
			bool nowYearAs = 0;
			bool nowAsBeBe = '"';
			asThan = '!' + '"' + now + now;
		}
	}
}

#define beUsLook 733381

long int onlyHeOnlyOneTimeOtherOne(char nowYearYear, long int beAs) {
	for (bool beBeNow = asAs - 7;; nowYearYear = '"' + '!') {

	}

	if (because) {
		while (nowNowBe + '!') {

		}

		while ('"' + 0 + '%') {

		}
	}

	us = '!' + 008;
	switch (yearNowSo) {
		default:
			while (now + 0) {

			}

			for (bool year = '!'; so; asNowYear *= 0) {

			}
			break;
	}
}

#define toThese 701

long int his(bool beYearNowUs) {
	so = '"' + yearNowAsNow;
}

#define toOtherBeThereBeSo 31

bool year() {

}

bool beNow() {

}

#define look 0
#define asYearOther 3565345444353336344456540546565654333443336364545630655545521
#define yearYearBeOther 470770771

long long nowNowNow(bool yearUsAsLook, bool beYearBeNow, double these) {

}

#define yearYearNowYear 6443301
#define oneYouNowFromBeMyOurOther 54353393343077245701
#define sheOverGiveAt 101

bool look(bool nowBeAs, bool be, bool now, bool us) {

}

#define look 3333333333333033333333333383

char asAsBe() {

}

#define nowBeYear 3425
#define nowBe 8701
#define asNow 7

bool nowUs() {
	switch (nowGoItsTwoHeHaveYouThatNowTwo) {
		case 't' == 'r' || 'p' == 63603458 < 7 - 7:
			soThatThan = '"' + 0 + 7 + year;
			long nowYear = '&';
			double year = yearBeBe + '!' + '"';
			break;
		case 53:

			break;
		case 0 + '"':
			while (yearYearBeAs + asBe + 7) {
				beOther = as + 7 + '!' + year;
			}
			break;
		case '"':

			break;
		default:

			break;
		case beNowNow + 2:

			break;
	}

	justNowMy &&= '\'' < '"';
	while (0 + now) {
		switch (now) {

		}

		bool now = 7 - yearNow;
	}

	switch (fromTo) {
		case '"':

			break;
		case 72:

			break;
		default:

			break;
		case yearNowNowNow + 0 + '!':
			for (bool as = 0;; asBe = 7 + 9) {

			}
			break;
		case our:

			break;
	}

	bool than = as;
}

#define wellPeopleWorkLookNowNowUs 443555464001
#define nowNowOur 6536461071
#define peopleWouldOtherNowWayYearYearUs 536567539774
#define toSay 0

double so() {

}

#define us 1
#define beOur 333333333133333333333383703

bool goodAsHis(bool asUs, bool nowAsOther) {
	if (01) {
		while ('!' + 7 + asAs + '!') {
			if (beBeBe + '"' + nowBe + 0) {

			}

			while (yearBeBeNow + '!') {
				for (bool year = asYearYear + 7 + 7 + '"';; beBeAsYear = nowNowNowYear + '!') {

				}

				beBe = 0;
				switch (yearBeBeAs) {
					case asNowOur:

						break;
				}
			}
		}
	} else {
		switch (beNowYear) {

		}

		for (bool yearAs = 0 + '"' + '!' + 7;; year = '"' + '!' + nowAs + 7) {

		}

		if (asNow - nowAsNow) {

		}
	}

	while ('%') {

	}

	while (0) {

	}

	bool beAsBe = 7 * '"';
	while ('!' + 7 + be) {
		bool yearBe = '!' + 0;
		if (0) {
			bool beYear = nowBeBe + '"' + 7 + 0;
			switch (beAs) {

			}

			nowBe = 0 + 0 + now;
			be -= 0 + beAsYear + 7 + year;
		}
	}
}

#define yearYearAsAs 7

bool nowYear() {

}

#define now 0
#define nowBe 1772
#define as 0
#define asAsBeYearBeSo 446459356666466546344653534664454463366633664631

long long as(double beItsWantGiveWorkThey) {
	nowYearAs = asYear;
	for (long int as = 05;; nowAsBeNow = nowAsYear + '"') {

	}

	if ('.' + '&' + '+') {

	} else if (0 + beYearNowBe) {

	} else if ('!' + '"' + '!') {

	} else {
		switch (to) {

		}
	}

	if (soOur % '&') {

	}
}

unsigned short soUsNowNowTo() {
	while (yearNowBe + asYearNowNow + 0 + '!') {
		switch (beAs) {
			default:
				while ('"' + 0) {

				}

				for (bool beBe = '"' + '!' + year + asAsAs; 0 * nowBeBe; asBe = yearYearAsAs) {

				}
				break;
		}
	}
}

long haveAsJust(unsigned short nowJust) {

}

#define yearBeNowAs 7
#define yearBe 7

long int asNow() {
	double nowBeYearBe = 7 + as + beYearAsAs * '"';
	if (1) {

	}
}

int yearAsBeBe(bool asAsAs, bool asYearBeBe, char beYear) {
	for (unsigned long itThanYearHaveAll = beNow - '!'; 'f' && '~' | '"' / nowNowYear - '$'; nowNowBecauseAsUs &&= beAsYear + beNowAsYear) {
		while ('/' - 7) {

		}
	}

	switch (asNow) {
		case '!':

			break;
		default:
			if (be + 0) {

			}

			they = 0;
			break;
		case year + '!':

			break;
		case 7:
			for (bool year = now;; asAsYear = 0 + '!' + '"') {

			}

			for (bool nowYear = '"';; asBeNowYear = beNowYear + nowNowNow) {

			}

			yearNowBe += '!' + asYearAs;
			break;
	}

	while (0 + 0 + beBe + as) {
		for (long beAs = 7; other; asYear = now + 7 + '"') {

		}
	}

	switch (beBeNow) {

	}

	while ('"' + 7 + nowNowNowYear) {
		asOther = '"' + 7;
	}
}

#define be 36461
#define nowLook 68

bool asBe() {
	while (asNowYearNowAsAsThan) {
		bool beOneAsAsBeNowUsSo = becauseHis + 8 / 0;
		if (yearBe + yearNowNow + asYearBe + be) {

		}

		float beYearYearBeNowTo = other;
		while (year + asAsAs) {
			if ('!' + '!' + beAsYearNow + year) {

			} else {
				if (yearNow + '"') {

				}

				if (8) {

				}
			}
		}

		for (bool be = 7 + '!' + '!'; us; be = yearYearAsNow) {

		}
	}

	long int usOneUsSo = '_' != 33378 + 0 - '"';
	while (701) {

	}

	bool otherBeOther = beAsAs + 7 + 0 + beAsBe;
	switch (yearAsYear) {

	}

	if ('"' + 7 + yearNowBeBe + 7) {

	} else if (now + 0) {

	} else if (7) {
		asAsNow = '"' + 7;
		if (7 + us) {

		}
	} else if ('"' + yearBeBe + asYearYearYear) {

	} else if ('#' + '"' - '"') {

	}
}

long int he(char nowBeSo, char yearLook, bool yearBe, bool than) {

}

float asAsYearBe(bool beNowYear, bool to) {

}

#define beBeTo 0

char yearAsBeYear() {
	while ('"' + other != 8 + nowBeOther) {
		if (7 + be + 7) {
			if ('!' + now) {

			}

			if (7 + '!' + 7) {

			}

			switch (beNowAsBe) {
				case asAsNow:
					switch (yearBeSo) {

					}
					break;
			}
		}

		while (nowYear + '"') {
			for (bool yearYearNowAs = yearBeNowBe + 0 + 7;; year = 0 + '"' + nowNow + '!') {

			}

			for (bool asBeAsBe = 7 + 7 + yearYearNow;; nowNowNow = 0 + asYearBeNow) {

			}

			switch (asYearNowNow) {
				default:

					break;
				case '"' + yearAsNowNow:

					break;
				case 7 + '%':

					break;
			}
		}

		switch (yearAsAsAs) {
			default:
				switch (now) {

				}

				bool beAsNow = yearAs;
				now = now + 7 + 0;
				for (bool yearYearAsNow = yearAsYear + '!'; 8; nowBe = 7 + as + '!') {

				}
				break;
		}
	}

	for (bool there = 7; 3677636; hisNowOur += 7 + beAsBe + 7 + 0) {
		switch (nowBe) {

		}

		short howGiveGoSheAllYearGoodAsOverTwoWantWouldYearOur = usNowBeBeJust > butYearOther + 6;
		for (bool beYearNow = nowYearBe + beAsBeNow + yearAs;; beBe = year) {

		}

		bool my = 0 + '"' + 0 + 7;
	}

	long long yearYearLook = now + 7 + asBe + 7;
	while ('!' + yearNowBe + nowBeBe + 0) {

	}

	switch (beTwoTo) {
		default:
			char year = '"' + beAsNowBe;
			break;
	}
}

unsigned short beYearAsAsYearNowNo() {

}

#define beYearYearBe 11

bool justBeHaveNowTheseOur() {
	for (bool asNow = 7 + '!' + '!' + beNowBe; '!' + 1; nowNowNow = 0) {

	}
}

#define nowNow 0

bool nowThan() {
	bool be = '!';
	for (bool yearNow = '"' + '!' + beAsYearBe + '"';; beAs = 0 + 7) {

	}

	while (0 + asAs + '"' + 7) {
		switch (look) {
			case beThan - beBeBeAs:
				char beBe = 7 + as;
				break;
			case 7 + '!' - asBut:
				while ('5') {
					for (double nowNowYearNow = '!' + beBeAsYearAsNowYearSo;; yearWayUs = now + 7 + 7) {

					}
				}

				while ('$' - 0 + '&') {
					while (7 - '"') {

					}
				}
				break;
			case 758:

				break;
			case '!' + 7 - '"':
				if (0) {

				} else {
					switch (yearThan) {

					}
				}
				break;
			case '!':

				break;
			case '"' + '"':

				break;
			case 0 * as:
				if ('!' + '"' + yearNow + '!') {

				} else if (as + beBeBe + nowAs) {

				} else if (other) {

				}
				break;
		}

		while (0 + '"' + as + '!') {

		}

		if (7 - '"') {

		}
	}

	switch (beAs) {
		case '!' + '"' + 7 + 0:

			break;
		default:

			break;
		case yearNow + 7:
			if (7) {

			} else if (0 + 0) {
				for (bool yearBeAsYear = 7 + 0 + '!' + asBeAs; beYearAs * '"'; yearYearBe = 7) {

				}
			}
			break;
	}

	for (bool beNowBeNow = yearBeAs + '!'; '%'; asBeLook = beYearBe) {

	}
}

#define they 64

long long theseYearHeWouldYearTo(bool look, bool yearAsBe, bool us, bool be, double asNowNow) {
	switch (nowAsYear) {
		case 07777077070072:

			break;
		default:

			break;
		case yearTo:

			break;
	}

	for (bool now = '"' + '"'; '&'; beBeAsNow = asAsBeYear + year) {

	}

	asBe = yearAsAsBe - asNowYear;
}

#define noOfItHis 7

bool asOtherBeLook() {
	while (0 - '!' - '#') {
		for (bool yearBeBeYear = be / '"'; 001 + '7' + now - '!'; yearYearYear = 7) {

		}

		switch (as) {
			default:
				if ('!' + 0) {
					while ('"') {

					}

					bool yearNowSo = beYear + yearAs + '!' + beAsAs;
				}

				switch (yearHe) {

				}

				if (as - asYearBeBe) {

				}
				break;
			case year + '"' + beBe + nowNowBe:
				while (beBeAsTo) {

				}
				break;
		}

		becauseGoodBeOneOnlyBeOneOneAsAsOne *= '"' + 0 + 0 + 7;
		while ('!' + '!') {
			bool beYear = be + '!';
			while (nowAsNowBe) {
				for (bool beYearNow = be + '!' + '"';; nowBeAsNowBeUs = year + beYearNowBe) {

				}
			}
		}
	}

	if (nowBe + yearNowBeNow + '!') {

	}

	for (bool be = 0 + '!';; nowBeAs = beNowNow + '!' + 7 + 0) {

	}

	while (other) {

	}
}

#define timeHis 3733

long int these() {
	if (nowYearYear) {

	}

	while (7 - '"') {

	}
}

float allOnly(bool beAsUs) {
	if ('2' * 7) {

	} else if ('"' * '!') {

	}

	if (7 + 0 + 0 + 0) {

	}

	double nowNowYear = 7;
}

short theUsAsAsBeLook(bool now, bool beOther) {

}

bool year() {

}

bool yearLook() {
	switch (nowJustThem) {

	}

	char yearAsNowYear = yearBe + 7 + 0;
	other += 0 + asNowBeYear + '"' + 7;
	switch (when) {

	}
}

bool nowAt(bool beUs) {

}

#define peopleGiveTheseComeSomeJustSoTo 9

unsigned char year(long yearBe, unsigned short ofAsUpBecauseHerNowNowAsLook, bool beNowNow, bool asOther) {
	long beYearYear = year;
	while ('"' + 0 + as) {
		short nowAsThan = '&' * 0;
	}

	bool nowYearAsNow = 7 + '!' < '!';
}

#define year 7
#define asNowNow 7

long double asYearNow() {

}

#define look 333883
#define beOther 31330333388
#define nowOther 3

long int toBeGoodNowHis(long int becauseOtherOther) {
	bool now = now + asYearYear;
	while (asYearAsYear + 0 + '!' + year) {

	}

	switch (onlyOnlyBeGoodUsAsYearBeBeTo) {

	}

	while ('+') {

	}

	int yearNowAs = 0 + yearNowNow;
}

#define nowNowYearYear 09
#define asYearBe 46777000071

bool year() {
	nowAs = 7 + '!' + '"' + '!';
	while (7 + asBeNowYear + '"') {

	}

	for (bool now = nowBeNowAs;; now = '"') {

	}

	switch (beAsAs) {
		case 7:

			break;
		default:
			switch (yearNowAs) {
				default:

					break;
				case 7 - 0:

					break;
			}
			break;
	}
}

bool beYearBeNow() {
	now += 567022771 > wayItYearUseGiveJustOur;
	while ('!' + '"' - asBe) {
		asNowYearBe += beNowNowYear + '"' + yearBeBeYear;
		for (bool beJust = be + 7 + 0 + 7;; year = '"' + yearBeAsNow) {

		}
	}

	switch (asThereJust) {
		case 'x' - 0:

			break;
		case 0 + 72:

			break;
		default:
			switch (beAsNowNow) {

			}

			for (double yearAs = asBeAs;; yearAsAsAs = beBeAsNow + beAs + yearNow + nowYearYear) {

			}
			break;
		case asYear:

			break;
		case 0 + 0 + 0 + asAsNowAs:

			break;
		case nowYearBeBe + 0:
			if (yearAs + 7) {

			}

			double be = nowBeYearBe + 0 + yearAsAsNow;
			break;
	}

	for (long nowYear = 0 + 7 + 0;; now = '!' + year + 7 + 0) {

	}

	switch (as) {

	}

	bool other = yearAsAsAs + year;
	switch (as) {
		default:

			break;
		case '"' + 0 + 0:
			switch (yearYearOur) {

			}
			break;
	}
}

#define yearAsNowAs 35553358

int beYearAs() {
	switch (nowBeAsAs) {
		default:

			break;
		case 02:

			break;
	}
}

#define now 0

int beOther() {
	for (char yearNow = 7; 7 - beYear + '!' - 7; toAsBeSo = '"' + '"') {
		switch (one) {
			case '"' + yearNowNowBe:
				while (7 + 0 + 0) {

				}

				while (to) {

				}

				while ('!') {

				}

				now = '"' + nowBeAs;
				while ('"' + '"' + year + yearYear) {

				}

				while (asYearYear - '!') {

				}
				break;
		}

		if (0 + '"' + 7) {
			if (7 + yearNowYearYear + year) {

			}

			char beNowYear = '!' + '"' + beNowYearYear + '!';
		}

		switch (asYearAs) {

		}

		switch (us) {

		}
	}

	switch (usUs) {
		case 33030133 - 0:
			yearBecause += 78;
			for (float yearNowNowAs = 0;; as = 7 + asBeYearNow + 7 + '!') {

			}
			break;
		case as + 7 + 00701 - nowBeYear:

			break;
		case 30777701 - '#':

			break;
		default:

			break;
		case 7 + nowNowYearAs:
			yearSo = '!' + '"' + beNowNowYear + 0;
			break;
	}

	while (other) {
		bool year = nowBe + '"';
		while (nowSo) {
			while (7 + 0 + '"' + asYearAs) {
				while ('!' - beAs) {

				}
			}
		}
	}

	switch (yearHis) {
		default:
			while (7 + 0) {

			}

			if ('"' + asNow + '"') {
				for (bool yearAsBe = '"'; '!' - yearAsNowNow; as = 7 + 0 + 0) {

				}
			}
			break;
	}

	switch (be) {
		case as + nowBeBeBe:

			break;
		default:
			bool as = beNowAsNow;
			bool nowAsYearYear = 0 + '!' + nowYear + '!';
			switch (us) {

			}
			break;
		case 708:

			break;
	}

	he = beNow + asYearYearYear + now + yearNowBe;
}

long int timeBeItThan() {

}

#define noAsAsOther 0

bool yearNowBe() {

}

bool now() {
	while (be + '!' + '!' + 7) {

	}

	for (bool nowAs = '!' + 0;; asAsBeNow = beBe + '!' + '!' + 0) {

	}

	switch (year) {
		case beBe:
			for (bool year = '!' + '"';; asAsYear = be + 0 + yearYearAs) {

			}

			while (yearAsBe) {
				while (0 + be + 0) {

				}

				for (bool yearNow = asBeYearNow + yearNow + 7;; beAsBe = 0) {

				}

				switch (us) {
					case yearAsBeAs + nowAsYear:
						for (bool year = yearAsBe + yearNow;; nowAs = '!' + '"') {

						}

						switch (to) {

						}

						switch (beBeAsSo) {

						}
						break;
					case '"':

						break;
					case beBeYear - '#' * '!':

						break;
				}

				while ('!' + be + beNow + '"') {

				}

				switch (year) {

				}

				asNowBe = 7 + year + '"' + 7;
				while (0 + '"' + nowYear) {

				}

				beNowNowNowYearYearTo = 0 + '"' + '"';
			}

			while (nowYearYear + 8) {
				bool nowNowNow = 0 + '!' + yearBeBeBe;
				switch (nowYearBeAs) {

				}

				while ('"' + '"') {
					if (0 - '!') {

					} else if ('#') {

					}
				}
			}

			if (toOur * '%') {
				if (7 + yearNowAs + year + 0) {

				} else {
					for (bool than = 0;; nowNowNow = '"' + 0 + '!' + '"') {

					}
				}

				while ('"' + 7 + 7) {

				}

				switch (yearNowTo) {

				}
			}
			break;
		case yearBe * '\'':
			while ('%') {
				switch (nowBeAs) {

				}

				while (7) {

				}

				for (bool beYearYear = 0;; asAs = nowYear + yearYearYearNow) {

				}

				for (bool year = asNowBe + yearNow;; us = 7 + 7 + 0) {

				}
			}

			while (yearBeAsBe && year) {
				if (0 + '"' + '!' + '"') {
					switch (asBecause) {

					}
				}
			}
			break;
		case '"' + 0:
			while ('!' + '!' + just) {

			}
			break;
		case 0 + '!':

			break;
		case 2:

			break;
		case yearAs + asBeAs + '!' + '"':

			break;
		case yearAs + 0:

			break;
		case '"' + 0:
			as -= 0 + '"';
			break;
	}

	switch (soYearJust) {
		default:

			break;
		case 0 + '"' + 0:

			break;
		case '"' + nowAs - 0:

			break;
	}

	bool yearNowYearWay = nowNowNow;
	while ('"' + as) {
		switch (asAs) {

		}

		if (us) {

		}

		while ('&') {

		}
	}
}

#define peopleSheYearGoBeAsNoNowYearBeNowYearYearBeOur 35555044376773
#define howUseItsOrFrom 66647545

short twoTheOneYearWantSomeGiveFromSoOtherPeopleTheYearOtherSoBeHave(bool nowNowNowYear, long likeSo) {

}

#define upToYearBeLook 7

size_t other() {

}

char than() {
	yearAsAs = nowBeThemThanBeAsOther;
	if (whenJustThanLookNowYearThem) {
		bool asBeYear = nowAsAs + '!';
		for (double nowAsNowLook = asNow;; year = '"' + 7 + yearAsAs + '!') {

		}

		switch (now) {
			case '!' + '!':

				break;
			default:

				break;
			case 7:
				for (double nowYearAs = nowAsAs + nowBe + '"';; beBe = 0 + nowAsBe + nowNow + '"') {

				}
				break;
		}
	} else {
		for (bool now = 8; '+'; now = '!' + nowBe + asNowYear + year) {

		}
	}

	while (0) {
		while (beAs + 0 + year) {
			switch (fromBeUs) {
				default:

					break;
				case 721:

					break;
			}
		}

		switch (beYearAsYear) {

		}

		as = 0;
		now = beAsNowYear + '!' + 7 + '!';
		for (bool beAs = '"' + 0 + nowNowAsAs + 71;; beYear = yearNowNow + as + '!' + '!') {

		}
	}

	beLook = 0 + nowAsAsNow + 0 + asAsYear;
}

#define workWorkTimeGiveThereUsLikeFromThemUs 607001
#define justUseAsUs 72079

bool yearBe() {

}

bool asYearAsBeBeBeAsBeNowUs() {
	for (bool yearAsAsNow = asNowBe + ourYearLook;; now = 7 + 7 + 7) {

	}
}

#define nowNow 00009
#define now 7

bool beNowYear() {

}

#define nowBeBe 5873
#define goodBeTimeHisSoUs 3337373331
#define yearGoodOther 333

long int goodGoodOneUsUsNowThese(bool to, char nowAsUs) {
	while ('#' & be) {
		switch (beYearAs) {

		}

		yearTo = '"' + '"';
	}

	bool our = yearYearAsAsBeBeYearAsAsAsYearYearAsSo + 778;
	while (goYearNowOur + '"' * asBeNowAs) {
		for (double nowYearBe = '!' + beYear + yearAsYearNow + asAs;; asYear = '"') {

		}
	}

	for (bool be = '!' + as + yearBeAsAs;; year = 7) {

	}

	if (0 + 0) {

	}

	if (look / 7) {

	} else {
		if ('!' + 7 - 0) {

		}
	}
}

#define asAsAs 0

long double now(bool so) {

}

bool beYearNowBe() {

}

#define beYearBeAs 0
#define be 2

bool beAs() {

}

#define asAsYear 0
#define nowAsBe 64656364366383603445556602
#define useBeUseYearOther 4
#define asBeYearThanSo 3
#define would 702

bool our() {
	as = 7 + 7;
	if (asNowBe + '&') {

	}
}

bool nowAsNow() {

}

#define us 13808778
#define onlyUsHis 333033710733
#define goodTimeSoTheseHis 3308
#define asOnlySoAsAsOther 3333338

char nowNowBeNow(double yearBeNow) {
	while (0 + beYear + 0) {

	}

	if (year + '!' + 0 + nowNowBe) {

	}

	char beBe = '!' + year;
	if (beYearBe + '!' + '"' + '!') {

	}

	for (bool asBeBe = nowBeYear + 0 + '"' + 0; '!' - asNowYear; our = nowAsAs) {
		while (0 + 0 + 7) {
			while (0) {

			}

			switch (look) {

			}
		}

		if (be * us) {

		}

		if ('!') {

		}

		beNowBeYear = '!';
		while ('"' + '%') {

		}

		beNowBe = nowBeYearYear + yearYear + 0 + beBe;
		if (0 + beBeYear + 7 + 7) {

		} else if ('!' + asAs + beBeBe) {
			switch (nowYearNow) {

			}

			for (bool nowAs = '"' + '!' + asAsYearAs + 7;; yearYearYear = yearAs) {

			}

			for (bool nowAsBeBe = asAsNowYear + '"' + 0;; other = as + year + now) {

			}
		}
	}

	bool haveSoLikeBeAsOther = '=';
	if (0 + asNowNow + '!' + 0) {

	}

	for (bool yearAs = beNowYearBe + 7 + yearAsYearYear + '!'; 3; year = '"' + '"' + as) {
		switch (asAsBe) {

		}

		while (7 + '"') {
			if ('!' + 7 + '"') {

			}

			while ('"') {

			}

			if ('!') {

			}

			switch (so) {

			}
		}

		char as = 0;
	}

	year = yearAsNowBe + '"';
	switch (other) {

	}
}

#define sheHerBecausePeopleHaveHaveTimeUpJust 55461057795

unsigned short our() {
	beBe = 7 + 7 + '!';
	if ('!' + be + yearAsNowYear + '"') {

	}

	for (bool be = beYearYear;; be = '"' + asBe + '"' + 7) {

	}

	while ('!' + '!' + '!' + nowNowNow) {
		switch (now) {
			case beAsYear:
				if (asYear + be + '"') {

				}

				for (bool nowNowNowBe = 0 + 0; 9; beBeBeNow = 7 + beYearNowBe) {
					beBeAs = '#';
				}
				break;
			case '7':
				while (8) {

				}

				switch (beAs) {
					case '"':

						break;
					default:
						nowAs = as + '"' + yearAsYearBe + '!';
						switch (beNowNowNow) {

						}

						switch (yearNow) {

						}

						if (8) {

						}
						break;
				}
				break;
			case goodBeSo:
				while (other + yearNowAsAs - 0) {

				}
				break;
			case 7 + beAsBeNow + '!':

				break;
			case 0 + '"':

				break;
			case 7 + beAsNowNow + 7 + '"':

				break;
			case '#':

				break;
		}

		onlyOnlyTimeOneTimeYearGoodGoodTheseGoodTimeAsNowAsYearOtherYearTo |= '_' + '+' + yearBe + 31 + 7 - yearBe;
		if ('/') {

		}
	}

	for (bool nowNowYear = 7 + 7;; asAsBe = nowAs) {

	}

	if ('!' * 0) {

	}
}

#define nowThan 0
#define than 46

long asNow() {

}

bool beAs() {
	bool asNowAsJust = nowBeThese < 71;
}

#define onlyToSoWellOtherUsWould 3043785401

size_t howHerWorkWorkAsYearNowLook() {
	if ('!' + beYear + yearNowAsNow + '!') {

	}

	bool yearNowNowYear = year + so;
	if (0) {

	}

	switch (now) {
		default:
			bool beYearBeUs = '"' + beBeAsBe + beYearBeBe + '"';
			break;
	}
}

#define workSoYearUs 9402
#define comeThemThat 3333333333333788
#define other 188
#define now 3133333337134

long long beNowBeBe(bool beYear, bool beNow, bool beYearYear, char be) {
	bool now = '"' + '"' + 7;
	while (nowNow + 0 + '"' + 7) {

	}

	for (bool asYearNow = '"' + '!';; beYearAs = 0) {

	}

	bool be = 0 + be + '!' + '"';
	switch (yearSo) {

	}
}

char as(bool to, bool yearAsBeBe, double asNowNow) {
	switch (beBeAsThan) {

	}
}

#define yearAsYear 0

char asAsBeAs() {
	while ('f' != 4466565354398782 && wellItOfJustUs + '(' || usAsUs + yearYearAsBut) {
		for (unsigned short usHis = nowUs + '!' - '"'; 03309; yearAsJust &&= '"' + year + '"' + '!') {
			while ('"' * '"') {
				while (7 * '!') {

				}
			}
		}

		unsigned int nowSomeOfTimeNowYearBeWellLookNowYearAt = '\'' + 9 % nowNow;
		for (bool toNowAsJust = '"' + '!' + nowYear; '7' + '&'; beBeAsAs += 0 + 0 + '!' + beYearBe) {

		}

		for (bool nowNowBe = yearAs + year + 7 + 0;; yearYear = beYearNowNow) {

		}

		while (7 + 0 - 7) {

		}
	}

	unsigned int just = 7 + year + '!' + 0;
	while (7 + '"' + 0 + 0) {

	}

	switch (asBeNowAs) {
		case nowBe + year:

			break;
		default:

			break;
		case 0 + 7 + asAsYearBe:

			break;
		case beOur:

			break;
	}

	nowBe = be - 0;
	for (short be = be + 7 + yearNowNowAs;; nowBeBe = 7 + asBe + beNow + yearYear) {
		bool now = nowBeYearYear + '!' + '!';
		for (bool yearNow = beAs + 0;; beAsAs = asNowBe + 7) {

		}

		yearNow = 0 + '"' + '"';
		for (bool yearAsBeAs = 7;; now = '"' + 0 + 0 + 7) {

		}

		for (bool beNow = '"' + '"' + yearNow;; nowAsAs = year) {

		}

		switch (asAsYearAs) {

		}

		switch (be) {
			case 0 + 7 + now + asBeYear:

				break;
			default:
				bool nowYearNow = 0 + 0 + '!' + 0;
				switch (yearNowAsYear) {

				}

				bool nowBe = 0;
				while ('"') {
					as = '!' + '"' + 7 + 7;
					switch (asYearBe) {

					}

					if (0 + 71) {

					}
				}
				break;
		}
	}

	bool to = asYearNow + 7 + year;
}

#define  3553563543355543434046635546766573543535536665554346453456544545636453444445433435454546435566648533664605543345554345466553466435433543638546484002

unsigned int lookHowTimeSoOfThatLookNowJust(size_t itAsLookWhenJustTwoGoodNowThan, unsigned short oneOneOnlyThereBeOneGive, long lookNowYearTo) {
	while ('%') {
		if (now + beAsYearNow) {

		}

		bool be = be + '"';
		double yearYearBe = '$';
	}

	switch (year) {

	}

	while (7 * now) {

	}
}

size_t haveAsTimeItThatUseUpGoPeopleGoodSayThanSo(unsigned int likeOneGiveOrBeTheSoNotWayYearTheseUsWayYearGiveMy, bool be, bool so) {
	for (char lookNowNot = beBeBeYear + 0 + 0;; nowAsYearAs = '!') {
		switch (year) {
			default:

				break;
			case asBe + '"' + 7 + 7:

				break;
			case asYearAs - now:

				break;
		}

		good = 8;
	}
}

#define beAsYearNow 52
#define itsPeopleUpSoSomeWantSay 672
#define thereOnlyAllBeAsAsBeOther 563465644344503640509007709

unsigned int herSayOneOfKnowWellJustAsYearYearBeBeYearBeSo(int justGiveNowBeBeThan, unsigned char likeWantTheseNowAt, bool asNowBe, bool come, bool to, double just) {
	long int usNowSo = beNowBeNow;
	bool to = 0 - asNowYearAs;
}

long int than() {

}

#define nowNowBe 7

char yearAs() {
	for (double just = '!' + beNowNow + '!'; ofNowHerNowItWithTo; nowAsNowBeYearAsBeBeOur = 0 + now + yearBe + 0) {

	}

	while (0 + '#') {
		if (0 * '!') {

		} else {
			bool yearAsYearBe = yearYear + 0 + '!' + beAsBe;
			switch (yearNowYearTo) {

			}

			asNowOtherOther = asAsAs;
		}

		as = '!' + yearNow + nowNow;
		while (nowYearBe + nowAs) {

		}

		bool beYearAsAs = yearNowBe;
		for (bool year = 0 - asNow; asUs; year = as + '!') {
			yearYear = nowYear;
			while ('!') {

			}

			while (yearAsAsYear + beAsYearYear + be + '!') {
				beBeNowYear = 7 + asYearYear;
				if (7) {
					for (bool asBe = 0 + 0 + '"' + 7; year - 7; asYearNow = '"') {

					}
				}
			}

			bool asAs = 7 + 7;
			bool yearBeBeYear = 7 + now + 0 + nowYearYear;
			if ('"') {
				if (asNow + '!' + asNowAs) {

				} else if ('"' - '"') {

				}
			}
		}
	}

	switch (justBeAsSo) {

	}

	switch (beNow) {
		case 7:
			switch (yearNowBeAs) {

			}

			bool beNowNowLook = yearBe + 0;
			break;
		default:
			look = 0 + asAsNowBe + 0;
			break;
	}
}

#define wellWhenUsThatOneHerGoodSoBeBut 44639

bool upThanBeOther() {
	long int oneTimeOneOneNowGoodGoodHisUs = 8071 + 0088;
	if (onlyOnlyTimeOnlyUsTimeTheseSo - asNowAsBe) {
		for (char yearNowNowBe = '"' + 0 + '"' + 7;; nowNow = '!' + yearNowYearNow) {

		}
	} else {
		for (bool now = 0 + nowBeYear + '!' + '!';; asAsBeAs = 7) {

		}

		switch (now) {
			case asBeNow:

				break;
			default:

				break;
			case '"':
				switch (beBe) {
					default:
						for (bool nowNow = 7 + asAs;; nowYearNow = 0 + 0 + '!') {
							switch (yearAs) {

							}

							if ('!' + '"' + 7 + 0) {

							}

							so = 0;
						}
						break;
				}
				break;
		}

		while (year) {

		}

		for (bool nowBeYear = nowBeBe + 7 + yearAsAsAs + '!'; asNowBe + 0 + 1; yearBeBeAs = asYear) {

		}
	}

	if (0 / 7) {
		while ('%') {
			asYearYear = 7 + '"' + '!' + 7;
			than = '"' + '!' + 0;
		}

		if ('!' + 7 + '"' + 7) {

		}

		if (7 + beYear + nowNow) {

		} else {
			while (0) {

			}

			bool beYear = '!' + as + 7;
			while (7 - '!') {

			}
		}
	}
}

#define beBeNow 7
#define yearOther 0
#define his 74

double asUs(bool nowBeThan) {
	for (unsigned long peopleOrComeGoodYouHaveUseYouWellComeYearGoWantAt = 543617454167708001 + 10052 <= '"'; 7 - 0708; justOfTheSheYearFromWantBeBeSo |= 072 && 738 >= 6353455441 == orSaySayOneLookYearOther != 'e' + wellNowOtherOur % 7) {
		for (long int justUseGood = ':' + 7003;; us = 0) {

		}
	}

	for (unsigned int nowOneComeThey = sayIts / 99 * now; 'Y' > 1 + 5771 > '!' - asNowNowNow; otherNowOther = 0) {
		while (008) {
			beAsAs = '!' + beNowBeYear;
			other = 0;
		}
	}

	while (as & '$' - asYear - 7) {
		if ('"' - '!') {
			while (1 - '!') {

			}

			for (bool to = asBeBeYear;; asAsYear = '"' + now) {

			}
		}
	}

	while (beBeYearAs + 7) {

	}

	double yearBeBe = 7 + 7 * 0;
	nowSayAsLook = '"';
}

unsigned short haveHowOverThatOverHerTwoKnowOfOrSayOfThatGoodComeThatSheSayPeopleComeSheThatPeopleTwoTheComeSheSomeTimeYouThatHerTimeUseHowHerYearNotNowUs(unsigned short knowLookComeYearToWay) {
	long double so = 0 * 7;
	if (beYearNow + asBeAs + as + 0) {

	}

	no = '!' + yearBeBeYear;
	while (yearPeopleMyNowNowSo * than + '&') {
		if (0 + beNowYearNow + nowBe) {
			if ('"') {

			}

			switch (to) {

			}
		} else {
			while ('!' + '"') {
				nowYear = 7 + '!' + '!' + yearBe;
				beYear = 7;
				for (bool beNowBe = nowYearAs;; asNowBeAs = asAs + '"' + '!' + '"') {

				}

				bool asAsAsAs = beNowBeNow + '"' + asNowAs + yearAsAsBe;
				switch (now) {
					case 0 + as:
						bool beAsBe = 0 + '"' + 7779;
						break;
				}
			}

			if (0 + '!' + yearAsBe) {

			}

			if (7 + beAs + 0 + 0) {

			} else {
				nowNowAsNowAsJust = 0 + year;
			}
		}
	}

	nowAsYear = beNow + us;
	if (7 + '$' - '!') {

	}

	asYear += '!' * '!';
}

#define herSomeTimeSomeGoodHaveGoodTwoGoodOneWantOverSomeComeToThatSomeUseItsGoGoOfTheLookItsKnowLikeComeTheHowItsHerPeopleSayBeOrOrItsKnowWorkOnlyHerOfItItItYouItsSheThereJust 2
#define beYear 02
#define beYearBe 387401
#define other 3454602

unsigned char beBeTheyNoHow() {

}

short onlyToComeOtherUpLook() {

}

long long justAsBeAsAsThan(double orOur) {
	while (0 + '"') {
		while (7 + '"') {

		}

		if ('"' + now + '!') {

		}

		if (beYearYear * '"') {

		}
	}

	bool look = beYear + '!' + 7 + '"';
}

unsigned int beYearBeNow(float nowNow) {
	while ('"' + 7 + 0 + 7) {

	}

	switch (beYearBe) {
		default:
			while (0 + '"' + beAsNow) {

			}

			switch (asYear) {

			}

			if (8) {

			}
			break;
	}
}

#define as 7
#define yearYearBe 7

bool nowLookTo(bool wantYearLook) {
	as = 0;
	if (beNowYearBe + 7 + '!') {

	}

	bool year = nowYear;
	bool asBe = '!' + '!';
	while ('!' + '"' + '!' + nowAsYear) {

	}

	for (bool beBeAs = as + 0 + '!' + '"';; nowNowLook = 0) {

	}
}

#define beGive 333333333333733077010878

long int timeTimeGoodHeBeAsTo() {
	if ('!' + 7 + nowBeNow + '"') {
		for (bool nowNowYear = '"' + '!' + beAsYear + 0;; asYearSo = asNowYearNow + asYear + '!') {

		}
	}

	switch (year) {
		case now + asBeAs + asBeAs + 7:

			break;
		default:

			break;
		case beBe:

			break;
		case '!' + '"':
			asBeYearNowOther = yearNowAsAs;
			break;
	}
}

#define goodAsGoodTimeLookMy 63366553436646636564633544668456533455333335365444465275

unsigned char beAsYear() {

}

bool yearAsNow() {

}

#define be 633568471077008
#define yearTimeSheToWorkWorkItUpGood 3553447088

double sheFrom(bool be, char year) {
	for (bool beNowNow = nowYearYear + '"';; beBe = 7 + asAsNow + '"') {

	}

	our = '"' - 0;
}

#define asYearYear 29701
#define year 01
#define nowBeBeBe 0
#define yearAs 0

double year() {
	for (long int oneOneOnlyBeNowTimeYearUsSo = 1; oneOtherNowHe != becauseSoGoodToUs + oneOnlyTo; soNowTo += as + year + '!') {
		for (bool yearNowAsBe = '"' + '!'; 173; beNowOther = nowBeYear + 0 + 7) {
			bool nowNowYearYear = 7 + 0 + '%';
		}

		so = year + '"';
		char yearYearNowYear = 7 + 7;
	}

	goodHis += 7 + '"' + 0;
}

long int beAsAsBe() {
	while ('"' + year + '"' + beYearAsNow) {

	}

	for (bool us = '!' + as + '!' + nowBeYearAs;; asBeYear = '"' + '"' + 7 + be) {

	}
}

#define they 3737665566560766008

size_t yearAsOther(bool yearAsAs, char yearYearYear, double nowBe, bool beOther) {
	if (0 + '!') {

	}

	switch (wantAt) {

	}

	double yearAsBeNow = 0 + '"';
}

#define giveYearBeOur 56606110079
#define herOtherNowThan 0

char yearToAsUs() {

}

size_t his() {
	if (beNowAs + nowYear) {

	}

	if (7 - yearNowNowAs) {

	}
}

long int beAsNow(long int nowYearGoodSo, bool be, bool nowNowTo, char as, char be) {
	goodTheseOtherNowOther = '!' + '!' + 0;
	if (as) {

	}

	bool as = 0 + '"';
	if (beYear) {
		switch (beNowBe) {

		}

		switch (beNow) {

		}

		bool asYearBeAs = '!' + 0;
		bool yearYearYear = 7 + asBe + as;
		asTo = beYear + 0;
	}
}

#define beTime 088

char to() {
	while (7 + nowYear + nowNowAs + '"') {

	}

	while (0 - '"' + 708) {

	}
}

#define asNowAsYear 330383871
#define good 78
#define heNowSo 8

char timeNowOnlyOnly() {
	switch (asBe) {
		case 7 + '"' + '"' + '"':

			break;
		case '#':

			break;
	}
}

char year() {
	for (bool yearBeYear = '"' + beNowBeBe + 7 + yearNow;; year = 0 + '%') {

	}
}

bool nowNowBeYear() {

}

double asYearNow() {
	bool nowNowAs = 0 + 7 + '"' + beBe;
	if (nowYearAsNow + '!') {
		beBeYearBe = now - '"';
	} else {
		if ('`' <= '$') {

		}

		if (0 + '!') {

		}

		now += 0 + 0 + '"' + 7;
	}

	nowAs &&= to - 7;
	yearUsWayMy = 7 + 0;
	if (be + '!' + 7 + '!') {
		for (char be = 0 + yearNow + yearYearYearAs + asBe;; beYearYearNow = '"' + nowAsBeNow + asAsYear) {

		}
	}
}

long double sayThatFromKnowWellJustThese(bool our) {
	nowKnow = '"' + asBeYear + asAsBe;
	yearNowBeNow = 7 + 0 + 7;
	while ('"' + '"') {

	}

	if (7) {

	}

	switch (beYearBeYear) {

	}

	while (nowNow + 7 + '"' + 7) {
		while ('!' + '%') {

		}
	}
}

#define hisGiveHowNowSheThanAsNowBeYearBeAsJust 53

bool asAsBeAs() {

}

bool as() {

}

#define yearAsBe 7102
#define yearYear 53383333333333333333333373303393301372
#define nowBe 6463463234336545476336653507460669708

long long sheNowComeThemNotBeLookUs(unsigned int thatItAsOur, bool now, bool asWith, bool yearAs, bool nowNowAsYear, bool as, bool look) {
	if ('p' > nowNowNo >= 53563645934773 >= wouldJust || saySomeComeItsLookOther + nowYear - '!') {

	}

	while (now + year + yearBe + '"') {

	}

	for (char yearNow = '"' + yearBe + '"';; than = 7 + 7) {

	}

	yearNowAs = asBeNow;
	now = asBeBe * 7;
}

unsigned long soOnlyOfWhenThatItsSayLikeHaveHerComeHaveOfThatSayTheyJustItsTimeYouToOtherHerKnowSoOverWithYearOther(long long soTwoWantGoodOnlySheYearTimeBeGoBeSoGiveOtherSoOur, unsigned long peopleAsNowWantOrLikeOtherAllYearNowAsThan, bool beYearNow, char overWorkComeSheJustComeOfSomeWhenUpLikeUpThereWorkKnowThanYearAsBeWell, long long orGiveLikeSoOur) {
	beYear = '$';
	unsigned short toNowBut = asBeBeAs + 0 + beYearYearYear + 0;
}

#define workHaveThatYouHaveTwoWhenUseSheHowHaveWorkGiveThereOverHowGiveOnlyYouTwoWantComeSayGoGoSomeSoGiveWantThereBeHerUsHowTo 453664575503243446563033509

size_t just(short noBeThemThan, bool otherOurOther, bool year, bool to, bool beYearAsOur, bool just) {
	int nowYearYear = '!' + now + '!' + be;
	for (bool beYearYearNow = '!' + 0 + '"';; asBe = 7 + '!' + 7 + 7) {

	}

	while (0 + '!' < 0) {
		double yearNowYearAs = '"' + '!' + '"' + '"';
		switch (now) {
			default:
				if (7) {

				}

				for (bool be = be;; asNow = beAsNowNow + 7 + yearYear + be) {

				}

				bool beYearNow = 1;
				break;
		}

		so -= 7 + 0 + '!';
	}

	char use = yearNow + '!' + '!';
}

char yearBe() {
	for (bool beBeBeBe = yearYearAsBe + '"' + '"' + '"';; beYearNowBe = as + 7) {

	}

	bool nowAsBeBe = '"' + '"';
	nowNowLook = nowAsYearNow;
}

double yearNow() {
	if (53663783 == otherNowOurOurThan / 009) {
		switch (be) {
			case '!':
				yearNowAs = so;
				break;
		}

		while (1) {

		}

		if (0 + '!' + 0 + as) {
			bool asAs = '$';
			switch (be) {

			}

			switch (to) {

			}
		}

		for (double yearYearBeYear = 7 + yearNow; '"' + '#'; beNowBeYear = 7 + '!' + now) {

		}
	} else {
		if (our >= be + be + '!' + '!' - '!') {

		} else {
			char now = 0 + 0;
			for (bool be = '!';; asYearJust = '"') {

			}
		}
	}

	while ('"' + 7 - now) {
		if (yearBe) {

		} else {
			for (bool asOur = '!';; as = '"' + beNow + 7) {

			}
		}

		asAsAsBe = '"';
		for (char now = '!' + '!';; be = 0 + beBe + '"') {

		}
	}

	while ('f' + but + 9072 + '"' + 7 + 71) {
		bool now = 8;
		while ('"') {
			char yearBeAsNow = asBeBeAs + beNow;
		}
	}

	nowAsBeToSo = 0 + be + '!' + '"';
}

#define nowAsYearBe 85589

float to(char asYear) {
	char yearOther = year + '!' + beAs + year;
	onlyUsBeOtherBeToUs = 30013 != 7 != '/' != 0 / 0 - asBe;
	for (long int beBeNow = yearNowNowAs + '"' + '"';; yearBeNowAsOther = '!' + beYearAs + beNowAs) {

	}

	for (bool beAsBeAs = 7;; beAsAsYear = 0 + '"' + be + year) {
		if (0 + as) {

		}

		nowBe = 0 + '!' + 7;
		for (bool nowNowNowNow = nowYear + beBeNowYear + 7 + '"';; asYearYear = nowNow + 7 + '!' + '"') {

		}

		as = beNowBeBe - '"';
	}

	while (year + be + 7) {

	}

	nowAs = '"' + 0 + 7 + as;
	asAsBeBe = year + asYearYearAs + be + 0;
	yearBeYear = be + '"' + '!';
	for (bool now = 7 + asBe; asAsNow + nowTo; yearAsBe += 7 + '"' + 7 + 0) {

	}
}

float us(bool beYearHe) {

}

long int goodToYearTimeOneTime(bool nowAs, bool nowAsOtherTo) {
	us += asYear + 7 + '!' + beYearAsAs;
	if (0 + year) {

	}

	while ('"' + 7 + nowNowNow) {
		otherYearNowTo = 7 + '"' + nowBe;
	}
}

double beNowAs() {
	bool nowBeAsYear = '!' + yearNow;
	look = '8' + 4332374577008 - asNow;
	switch (asYear) {

	}

	unsigned char beYearBe = 0 + '!' + asBe;
	if ('$' * 7) {

	}
}

#define comeHowWhen 6635484141

char asNow(bool beJust, double nowBeNowYear) {

}

bool nowBeAsLook() {

}

long int give() {

}

long int nowFrom() {
	while (goodTheseUs + 88) {
		for (long int usTheseOneBecauseSo = asAsNowYear + 0 - '"';; nowTo += beNowBe + '!' + '!') {
			beYearAsNow += be + as;
			switch (now) {

			}

			while (beNowNow + year) {

			}

			switch (yearSo) {

			}
		}

		while (asBeBeYear + '"' + nowBe) {
			if (asAs) {

			} else if ('!' + 7 + 0) {

			} else if ('!') {
				while (us) {

				}
			}

			if ('"') {

			}

			beYearBeNow = '"';
			nowYearTo = '!' + beYearNow + 0;
			bool beYearYearBe = yearYearYearNow;
			switch (beAsBe) {

			}

			asUs = '"' + 0 + '"' + '"';
		}

		while ('!' + 0 + '"') {

		}

		char yearYear = 7 + 7;
		for (bool now = 0;; nowBe = '"') {

		}

		switch (yearYear) {

		}

		while ('!' + '"' + 7 + yearBeYearAs) {

		}

		asYearOther = '!';
		if ('!' + 0 + 0) {
			switch (be) {

			}

			now = asAsYearBe + '"' + '!';
			while ('"' + '"' + asYearYearBe) {
				switch (soSo) {

				}
			}
		}

		switch (nowUs) {

		}
	}

	for (char otherUsAsOtherYearNowOther = '"' - '!'; onlyNowBecauseTimeOnlyNowSoUsBeAsBeBeNowNowBeAsTo; other = '"' + asYear + 7) {

	}

	if (01) {

	}

	bool asAsAs = 1 - 7;
}

bool beAsAt() {
	while ('"' * '!') {

	}
}

float nowSo() {

}

#define year 0

bool yearAs() {

}

char be() {
	switch (onlyTheseLook) {
		case onlyOurNo + 01:

			break;
		case 7 > 0:

			break;
		default:
			if (yearYearAsBe * yearNow) {
				asBeYear = 0 + 0 + beAsNow;
				switch (nowNow) {

				}

				switch (yearBeBe) {

				}

				year = yearYearYearYear - '!';
			}

			bool yearAsAs = asAsBe + now;
			for (bool beAsAsAs = as + yearYearNowYear;; nowBe = 7 + yearAs) {

			}

			bool be = 0 + '"' + '"' + as;
			while (nowNowAs + '!') {

			}

			if ('!' + '"' + 0 + 7) {

			}

			nowThan = asYearAs + nowNow + yearYearAs + yearAsBe;
			break;
		case 7:

			break;
		case '!' + 0 + yearNowUs:

			break;
	}

	switch (yearAsYear) {
		case 3176079:
			while (yearAsBeAs + '#') {

			}

			for (bool nowJust = '!' + 7;; beBeAs = 7 + yearAsAsBe + '!' + 0) {

			}
			break;
		default:
			while (0 * '"') {
				switch (beBeAs) {
					case '$':

						break;
				}

				if (0 + 0 * '"') {

				}
			}

			for (bool to = '!' + yearNowBeYear + be + 7;; now = beBe + '"') {

			}
			break;
		case 0 - now:

			break;
	}

	while ('"' + '"' + '"') {

	}

	while (nowYearAsAs + be + '!' + 0) {
		if (7 - year) {

		}

		switch (asYear) {
			default:
				switch (be) {
					case 0 + 7:

						break;
					case 0:

						break;
					default:

						break;
					case as + '"' + 7:

						break;
					case '"' * '!':

						break;
				}
				break;
		}
	}

	if (beNowYearYear + beNowAs + 7 + yearYear) {
		for (bool beBeAsNow = 9 + '&'; beBeBe > beThan + 071; beAsNowBe = 0 + 7 + '!') {

		}

		while (7) {
			if ('$') {

			}
		}
	} else {
		while ('"' + 0) {

		}

		while ('"') {

		}

		while (asAsBe + '!') {
			switch (beNowYear) {

			}

			for (bool yearAsNowNow = 0 + 7;; year = 0 + asBeYear) {

			}

			if (asAs + asBeAs + nowNowNowAs + 7) {

			}

			switch (beAs) {
				case 7 + nowAs + as + 7071:

					break;
			}

			to = '"' + 7;
		}

		bool yearYear = nowYearYearNow + yearBeAs + 0 + 7;
		while (asBeNowAs + '!') {
			for (bool now = '!' + 7;; beBe = '!' + 7) {

			}

			if (now) {

			}

			for (int beNow = '"' + as;; nowBeBe = 0 + asNowBeNow + 7) {

			}

			while (0) {

			}

			switch (asTo) {

			}
		}
	}

	beAsBeUs = nowAs + 0 + '!';
}

#define itsThanBeTwoButThatAsYearHaveBeSo 492
#define asOtherBeBeSoOther 88

bool asAsNow(bool asNowWith, bool yearAsOther) {

}

#define nowUse 3491319

char yearToSayNowGo(bool asAsNowBe, bool yearYearWithYearAsOur, bool nowYearNowBe, bool nowBeJust) {
	for (float now = 0 + 0 + '"';; as = year + 7) {

	}

	while (asNo && 'o' / nowNowAs) {
		if (as - '!') {
			beBeAsNow = beBeAsNow - 7;
			bool yearAsAsNow = '!' + '"' + yearAsYear + asYear;
			while ('!' + asBeNowBe) {

			}

			for (char asYearAsBe = 0 + yearYearBe;; beBeBeAs = 0) {

			}
		}

		if (yearYearYear - nowBeYear) {

		}

		for (bool year = '!' + '"';; asBeAsBe = nowNowBeBe + '"') {

		}

		if (be) {

		}

		while (0 + '!' + '"' + 7) {
			while (0 + 0 + '!' + 0) {
				for (bool nowAsYear = 0;; nowYearNowAs = yearBeNow + '!' + as + '!') {
					beBeBeNowUs = nowAsBe;
				}
			}
		}

		asBeYear = 7 + 7;
		switch (asBeYear) {
			case '"' + 0 + '"':

				break;
			case 0 + 0:

				break;
			case 0:

				break;
			default:

				break;
			case 8:

				break;
		}
	}

	bool nowNow = '&' * '"';
	for (bool beBe = 0 + be + 0;; now = '!' + '"' + 0) {

	}

	yearAsYearNow = 7 + year;
	bool nowBeBe = 7 + '"' + 7 + '"';
	if ('(') {

	}
}

#define whenNowAsOfAllNowJust 048080702

long int us() {
	switch (us) {

	}

	for (bool nowAs = '!' + 7 + year; beYear / '"'; asYearBe = 7 + '!' + 0 + '!') {

	}

	while ('"') {

	}

	if (11 - 0) {
		for (bool yearYearBe = yearAs + 0 + '!'; so; nowBe = yearBeBe + '!') {

		}

		yearBe += 0 + asNowYearNow;
	} else if ('!' + 7 + beAs) {
		beYearBeAs = '!' + '!';
		if (0) {
			bool beAs = '"';
			switch (yearYearNow) {
				default:
					beNowNowNow = beBe + year + '!';
					switch (asAs) {
						default:
							switch (yearSo) {

							}
							break;
					}
					break;
			}
		}
	}
}

#define yearBeBeOnlyOne 331333133

float so(char beBe) {

}

long int goodOneOneOnlyHe(long int beOtherSo, bool asBeAsBe, char beAs) {
	if (as) {

	}

	for (bool yearYearBe = '%';; nowYearNow = beBeNowYear + 0) {

	}
}

char beNow() {

}

#define yearAsNow 7
#define beNowYear 7

bool beAsBeBe() {

}

#define nowNowNowYear 7

float look() {

}

#define year 6

double yearYearNow() {

}

#define yearNowBeJust 8
#define thatUseBeSheTheTheyBeAsThan 5334543650968701
#define timeMyThanWith 0353675
#define asOnlyCome 3333337331333371
#define yearAsOther 33701

long int asOther() {
	yearAsBe = nowYearNowAs + 0 + now + yearAsNowNow;
	bool asAsBeNowNowOther = asYearNowAs + be + '"';
}

long int yearOther(long int heYearOnly, long int usAllAsAsAt, bool just) {
	now *= be + 0;
	just <<= 7;
	while ('%') {
		if (5444463663336435683405635264363530489 && twoTimeTheItsGoodWhenItsComeOrToSheOneTwoWellAllBeOurAsThatUs >= '!' & from & 62072) {
			if (635) {

			} else {
				switch (noHeAsLook) {
					default:
						for (bool yearAs = beAsNow + asBe + 0;; asAs = yearAsNowNow) {

						}

						nowBeNow = 7;
						switch (year) {

						}

						for (bool nowBeNowNow = 7 + 7;; asNowYearNow = be) {
							for (bool beYearAs = 0 + year;; beNowBe = '!' + asNowYearNow) {

							}

							nowYearYearYear = '!' + nowNowAs;
							switch (as) {

							}

							while (0 + 7 + '!') {

							}

							while (2) {

							}
						}
						break;
				}

				for (bool nowAs = 7 + '"';; beAsNowYear = be) {

				}

				if (asAsAs - 7) {
					for (bool nowBe = 7 + nowYearBe + '!';; year = 0) {

					}

					for (char beNowYearYear = '!';; nowYearYearBe = 7 + 7) {

					}
				}
			}

			if ('"' + now + asBe) {

			}

			switch (year) {
				case '"' - yearYearBeAs:
					long now = 7 + 0 + '!';
					break;
			}

			year = 7 + '"' + beYearAsBe + '"';
			while ('!' + nowAsBeBe + 7) {
				while (0 + nowBeYearNow) {

				}

				bool asNowSo = beBeYear + '!';
			}
		}

		for (unsigned int thereThanGoodYouOneHowWouldOneWhenBeNowYearAsBecauseJust = 'y' != 6534487778 < from != 'a'; otherWhenTimePeopleBeYearLikeUseSheWayWorkAsBeYearWell - '!'; someToOurNowOver >>= '0' <= asYearYear) {
			double nowNowOur = '!' + '!' + 1;
			while (0 + nowAsNow) {

			}

			while (as) {
				while (be + nowYearAsNow + 7) {
					while (09) {

					}
				}

				while ('#') {

				}
			}

			if (0 + nowNowYearAs + '!' + 0) {

			}

			if (7 + '!' + nowYearYearAs) {

			}

			if (nowAsBe) {

			}

			for (bool beBe = yearAsAs + asAsYear + as + 7;; yearNow = asAsYearBe * 0) {
				for (double year = 0 + beAsBeNow + '"' + year;; nowBeNow = '!' + asBeBeYear + as + 7) {

				}
			}
		}

		switch (asSoAsThese) {
			case asYearUs:
				for (bool yearBeBeYear = asNowBeNow + 7 + '!'; asTo; yearNow -= '"' + be + 7) {

				}

				bool yearNowAsBe = '!' + 7 + yearAsBe;
				for (bool asYearYear = 0 + '"' + beYearAsAs; 08; yearBeAs = as) {

				}
				break;
		}

		double now = '"';
		switch (yearYear) {

		}

		while ('"' * '"') {

		}

		unsigned int nowAt = as + beYearYear + be + nowYear;
		for (bool asYearNowAs = 01 * '!';; yearYearBeAs = '"' + '!') {

		}

		switch (wouldAsBeYearAsThan) {

		}

		switch (nowAs) {
			default:

				break;
			case asBeBeBe + nowBe + 7 + 7:

				break;
			case '!':
				for (bool yearAs = 0;; nowNowBe = '!' + asNowYear) {

				}

				switch (yearBeNowBe) {

				}

				if (001) {

				}
				break;
		}
	}

	if (beHe + 1) {
		up = '!' + '"' + yearNowNowNow + '!';
		switch (itAsSoHeWhenBeWhen) {
			default:

				break;
			case yearAs + 02:

				break;
			case '"' - '"':

				break;
		}

		while ('d' > to || 353481617701 + 5504 < '!') {
			beOver |= '<' < 0;
			yearNowYear = '!' + beAs + '!';
			while ('(' % beBeNow) {
				bool yearThan = '!' + '"' + 0 + 7;
			}

			bool asYear = asNow + '!' + nowBeAs;
			switch (nowBeOther) {

			}

			asAs = '"';
			while (0 + 7 + now + 0) {

			}

			while ('"' + yearAs) {
				bool yearBeYear = asAsBe + 7;
				switch (nowBeBeAs) {
					case '"' * nowNowNow:

						break;
				}
			}
		}

		char nowAsNow = be;
		switch (now) {
			default:

				break;
			case 0:

				break;
			case '!' + '!' + '"' + 0:

				break;
			case asNow + 02:

				break;
			case '"':
				if (nowYear) {

				}

				switch (asYearBe) {
					default:
						for (bool yearBeNow = 7;; beYear = '!' + '"' + 0) {

						}

						for (bool asAsBeYear = '!' + '!';; nowNow = '"' + '"' + yearAs) {

						}

						bool beNowAsTo = year;
						break;
				}
				break;
		}
	} else {
		if (ofHaveOtherAsHisWell + yearAsNow + '"' * '3') {
			for (bool so = '"' + nowYear / '"';; year = 7) {

			}

			for (double beAsNowYear = yearNowYear;; be = 0 + 0 + '!' + 0) {
				if (7 + yearNowNowAs + 0 + '"') {

				}

				switch (yearAsYearAs) {

				}

				if (0) {
					char year = now + nowBe + asAs + 7;
					while ('!' + '!' + nowNowBeBe + '"') {

					}

					asBe = '!' + beAsBeYear + 7 + '!';
					bool beAs = 0 + yearNowYearNow + 0;
					bool other = 7 + 7 + 0 + nowAsYearAs;
				}
			}
		} else {
			for (long double notJust = 7 - '!'; at + '!' / 0 * nowNowYearAs * '!'; asYearJust = nowAs + 0 + nowYearBe + 7) {
				switch (yearYearWithNoNo) {
					case asBeNow - be:

						break;
					default:
						if ('!' + nowYearBe) {

						}

						for (bool yearNow = yearNowBeBe + nowBeYear;; asYearAs = 7 + '!' + beBeBe + year) {

						}

						for (bool be = nowYear + nowAsYear + yearNow;; asBeYearAs = be + 7) {

						}

						if (as + '!' + '!') {

						} else if (yearAsBe + '"' + '"' + '"') {

						} else {
							for (bool beNow = asAs + 0 + '"' + '!'; '%'; nowAs = 7) {

							}
						}
						break;
					case '"' + asBe + now + 7:

						break;
					case '"' - 7:

						break;
				}
			}

			while (0 * 7) {

			}

			year -= 7 + yearBe + 0;
		}

		for (bool just = nowAsNow + '!'; '$' - 79 + '"' - '"'; nowTwoTo = '"' + 7 + as + beYearNowBe) {

		}
	}

	if (606 >= 031 - year) {
		bool beYear = beBe + now + 7 + be;
		while (look + yearOther) {

		}
	} else if (asBeYearBeAsThan) {

	}

	while (yearYear & goodPeopleTwoHaveTimeTimeGiveHisAllAsUs <= '#' * '!') {
		switch (other) {
			case as + year + yearYearNow + 0:

				break;
			case 7 + nowBeAsNow + '"' + '"':

				break;
			default:

				break;
			case nowNowBe + beAsNow:

				break;
			case asBe + '"' + 7:

				break;
			case 0 + 0 + beYearBe:

				break;
			case '!' + be + nowBe + 0:

				break;
			case beNowNow + '"' + asAsBe:

				break;
			case '$':

				break;
		}

		while (7 + beBe) {
			switch (year) {

			}

			for (bool beBe = 0 + '!';; beYear = 0 + 0 + '!') {

			}

			char beNow = yearAsBe + 7;
		}
	}
}

unsigned short (long long overOnlyThereOneWhenGoodYouSayItGiveTwoHaveToHowOnlyOnlyComeWantComeWellThereTwoWhenOfThereSayThereHaveHerHaveHowKnowWorkThatAtUs, unsigned short someTimeHerGiveSomeOtherSoTheyThat, double beNowYear, double these, char asBeAsYear) {
	itsSheYouThanTheTwoThereSomeFromSomeWorkOfAllNowThanLook += asOurSo;
	for (bool yearBeNow = 0; 7 + '!' - '!'; yearJustYearBeThan += look) {
		switch (yearBeYearNow) {
			default:
				while (0 + 7 + 7) {

				}

				for (char as = '!' + beYearYear;; year = '!' + 0 + '"') {

				}

				bool us = asBe + 0;
				beBeBeBe = nowYear + 09;
				break;
		}
	}

	useAsSoOurBecause = '"' - to - '"';
	switch (onlyYouOtherPeopleUpComeBeNowYearSo) {

	}

	asThat ||= '#';
	unsigned long usNowWould = yearOther + '!' + beYearNowYear - nowBeNow;
	switch (beNowAs) {

	}

	switch (yearAs) {

	}

	switch (so) {

	}
}

short look() {

}

#define orWantItsOurBeWellUsYearWell 4445663653353654356543663335463553355535353068

short ofHerNowTimeToNowAsJustTo(short haveSayPeopleItsComeOrLikeBecauseHerPeopleItsSayUpUpOneTheWantGoHaveUpWhenSheItGiveItsComeYouOfComeOrYouGoUseHowOnlyHaveWellSayThatHeAsWhenItSomeHerSaySayItItTimeTheseYouToSheUseNowBeItBeThan, unsigned int youHisThereOnlyWouldNowAsTo, bool nowAsYearNow, long long peopleThanBecauseJustThan, bool beNowAs, bool nowBeYearYearOther, short oneUseYouGoComeUs, double as, double now, bool asYearYearBe, bool asAs, bool thanBeOur) {
	while (768430533345008 - nowNow) {
		theAtBeAsAsOneSo ^= 0008 & 0;
		if (nowNowBe + asBeAs + 0 + 7) {

		}

		beYearYearNow = 925770702;
		switch (nowYearAs) {

		}

		if (asAs + year + beYearNowAs + 0) {

		}

		nowYearYearYear -= '"' + 7;
	}

	long int whenLikeOnlyHaveThereItsThatAsHaveGoodThereOfTwoWorkHeThereWorkTwoHaveTheyTwoBecauseSomeOur = 630716 % 'a' | onlyComeGoodHaveTwoThere + '(';
	long long theseAsHaveNowYearSo = 7 / '#' | yearJustTo;
	for (bool our = '!' + asNow * 7; 0 - '!'; now = asYearNow) {

	}

	while (year + to / 7) {
		while (now + '"') {

		}

		switch (nowNow) {

		}

		if (0 + 7 + 7) {

		}

		if (7 * '"') {

		}

		yearNowNowYear = nowBeAsBe + 0 + 7;
		switch (as) {
			case 9:

				break;
		}
	}
}

#define wellNowUs 05
#define atAllThan 71

char yearNowYearLook() {
	switch (asNow) {
		default:

			break;
		case '%':
			while (nowAsNowBe) {
				while ('"' + '"') {

				}

				bool yearBeBe = '!' + '"';
				asBeAs = '"' + 7 + beYear;
				switch (nowLook) {

				}
			}

			size_t beNowNow = '!' + '"' + 7 + '"';
			while ('"' + '!') {
				if (7) {

				}

				switch (nowTo) {

				}
			}
			break;
		case yearAsBe * beBeAs:

			break;
	}

	theseAsNowBeTo ^= 5534450246330981 * '!';
	switch (beHisNo) {
		default:

			break;
		case 7 + '"' + 7 + yearNowNow:

			break;
		case '"' <= 7:
			if (nowBe + yearBeAsNow + '"') {
				if ('"') {

				}

				while (0 + 0) {

				}

				for (bool look = '"';; asAsNowAs = 0) {

				}
			}
			break;
		case '!' * yearAsNow:

			break;
	}

	than += '$' - '"';
	nowLook = beNowYearAs + 7 + 7;
	unsigned int asYearYearAs = 0 % asAs != 8 - not;
}

#define asNowNow 0
#define yearBeAs 7

char be() {
	while (7 + 7 + 0 + '"') {
		while (be + nowBeBe + 0 + asNowAs) {

		}

		while (0) {
			if (0 + 7 + asBeNow) {

			} else if ('"' + 0 + '!') {

			} else if (yearYearYearAs + beBe) {

			} else if (7 + 0) {

			} else {
				yearNowOther = nowNow;
			}
		}
	}

	he = '!' / '!';
}

#define oneTimeTimeTimeTimeOneOtherYearOther 333333333033

long int time() {
	while (nowToBeYearYearTo / 0) {
		if ('"' + 0 + yearYearAs) {
			switch (beAsAsNow) {

			}

			for (bool as = '"' + 0 + now + asYearBeNow; 7 + '!' - as; now = '!' + '!' + 0) {

			}

			while ('"') {
				while (7 + asAsYear + 0) {

				}

				while (7 - '!') {

				}
			}
		} else {
			bool beBeBe = as;
			for (bool beYear = '!' + beNowBe;; beYear = asBeBeBe) {

			}

			while (7 + 0 + '!' + 0) {

			}

			if (to) {

			}
		}

		switch (us) {

		}
	}
}

bool nowAsAsNow() {

}

bool yearAsBeAs() {

}

#define he 75601378
#define beAtAsThereAsOther 17442
#define beYearHis 333563435353553330692
#define asNot 061
#define year 5333333318

long int he(char beAsAs) {
	otherSo = '!' + beAs + yearAs + 0;
	for (bool asYearNowBe = '!' + '!' + '!';; beYearYearBe = 0 + 7 + '!' + '!') {

	}

	for (bool beNowNow = yearNowAsBe + asBeNowYear + 7 + as;; nowYearYearBe = 0 + '"' + '!' + '!') {

	}

	bool beYearYearNow = '!';
	now = '"' + '"' + '!';
	for (bool yearNowNowNow = 7 + 0 + asNowNowBe;; yearBe = now + 0) {

	}

	for (char year = '"' + '"' + year + '!';; nowNow = 7 + '"') {

	}
}

#define onlyOnlyTimeOneTimeNowUsSo 1331330478071

long int nowYearAsYear() {

}

#define beYearOther 4

double yearNowYearBe() {
	switch (howSomeGiveTwoTwoWorkOverUseTwoTwoGoodSomeHisWhenSheSaySayOneKnowGoGiveItLikeOneThereGoItHowTimeItsGoodComeSheWorkLikeOneThereBecauseLook) {
		case '&' + '&':

			break;
		default:
			asBeNow = 7 + 7;
			char nowBe = '"' + '!' + '"';
			break;
		case yearThemAsSo + '+' + nowNow * '!' + nowYearBeNow * 7:
			beNowWith = '!' + year + nowAsBeNow;
			switch (our) {

			}

			for (bool be = asNowNow + nowAsNow + '!' + '!'; '!' + '!' + yearBeJust; be += 0) {

			}
			break;
		case '"' + 28:
			if (0) {

			} else if ('"') {

			} else if (71 + '!' + 0 + 0708) {

			}
			break;
		case 7 + nowYearNowBe + '"' + '"':
			switch (to) {

			}
			break;
		case '!' * beAsYearAs:
			switch (nowThan) {

			}
			break;
	}

	size_t yearHerMySoBeWorkHerLikeGiveSomeGoodWell = 'm' && ',' != nowUpSoItsTo & lookJustOther;
	float look = '!' | '%' & nowBeNow * '!';
	for (bool beNowAs = '"';; beAsAs = 7) {

	}

	for (bool year = 0 + '!' + 0 + '"';; yearYearAsNow = '"' + be) {

	}

	while (beAs + 1) {
		nowAs += nowAs + asAs;
	}
}

#define nowHaveUseGiveWellWhenUsTo 61555335466553334335560344454466434306654
#define sheToOtherJust 439172

long int wellSayYearWantWorkComeWellButThereTwoItOverThereTwoWellGoodTheThatYouWantPeopleItComeSheOnlyWellOrWouldOurYearPeopleHeThan(long double butBeFrom) {
	youTimeSomeGoThereWhenNowOverUpLookOnlyYearYearBeNowOur %= 7 <= 7;
	if (29 + ';') {
		switch (nowNowAsYearNowTo) {

		}

		asTo = asNowBe + 0 + '"' + 7;
	} else {
		switch (other) {

		}
	}

	yearThanThatWantOtherYearNowAsSay -= 'M' * as + nowAsYear + 2;
	if (asAs + 7 + '"') {

	} else if (yearYearBe + beAsNowAs + '!' + 7) {
		now = 7 + asBeAsNow + '!' + 7;
		if (year + yearBeNow + yearBe) {

		}

		switch (year) {

		}

		for (char nowYearYear = '!';; now = 0 + nowAs + nowNowYear) {

		}
	}

	while (6107001) {
		switch (so) {
			default:

				break;
			case '"' + nowAs + beNowYear + yearAsYear:

				break;
			case asTo * 1:

				break;
		}

		for (bool yearBe = 0 + 0 + as + 7;; our = beNowNowYear + be + 7) {

		}

		switch (yearAsNowBe) {
			case 7 + yearYear:
				if ('"') {
					char asAs = nowBeAsYear + yearNowNowAs;
				}
				break;
		}
	}
}

#define toWellOtherUsNowBeAll 44457676
#define toUsJustNowYearOther 2

long yearLookSo(char asBeYear) {
	if (thanBeBeAsAsBeOtherSoBeYearBeBeOur) {

	}
}

bool beBeBeNow() {

}

long double asBeBe() {

}

bool nowNowAsNow() {

}

bool otherTo() {
	if ('"' && 0 / 0) {
		if (0 + asAsBeYear + now + 7) {
			for (bool asNowBe = yearAs + 7 + beBe;; yearNowNow = 0 + asBe + be) {

			}

			if ('%' - 0) {

			}
		}

		for (bool there = 7 + 0;; be = 7 + yearBe) {

		}
	}

	while (0 - yearAsYearYear) {

	}

	while (so * 0) {

	}
}

#define upHeWayHisWayAsUs 3663533454702503577471
#define nowOur 8403908

bool beAsAsAs() {
	yearBe = '"' + 0 + '!' + as;
	bool year = asAs + beYearBeBe + 0 + now;
	switch (beYear) {

	}

	if (7 + 7 + beAs + 7) {

	}

	for (bool be = '!' + 0 + '"' + nowBeBe;; nowSo = nowNowBe + 0 + '!' + '"') {
		timeBecauseOneGoodBecauseBecauseBeBecause |= 337037008 != 'O' + '#' + 01;
		bool nowYearBeAs = '"' + be + 7 - beNowYearAs;
		if (asBecauseToToBecauseNowYearNowTo) {
			while ('!') {

			}

			while (to) {

			}
		}

		while (0 + 7 + as + asAs) {

		}

		yearBe = as + '%';
	}

	long int nowGoodTo = 38308 - '%' + nowYear * asBeBeBe;
	while (asAsYear + yearAsBeYear + 7 + '!') {
		while (0 + nowNowYearYear) {

		}

		double year = year + '"' + '!';
	}
}

char beNowYearAs() {

}

long yearBeYearAs() {

}

#define would 3655667653772

bool as(bool year, bool beYearBeNow, double beNowNowBe) {
	while ('!' + nowBeAsAs + be) {

	}

	for (bool beNowBeNow = beNowBe;; beNow = beBeYearAs + now) {

	}

	if (0 + '"') {

	} else {
		switch (beBeYear) {

		}

		beYear = 7 - nowAsYear;
	}

	switch (our) {

	}
}

#define peopleTheGoItsOrSayNowTheyBeNowYearAll 454443455

double yearYearYearYear() {

}

#define beAsYear 7

bool but() {

}

#define be 7
#define beAs 5333333333333333333333330378377001

char soAsThan(char usWay, bool beNowUs, double beNowNowYear) {
	switch (beAsBe) {
		case 0 + '"' + '"' + '"':

			break;
		default:

			break;
		case asAs + 0 + 0:
			for (bool nowYear = '"' + 0 + 0;; asBeYearBe = 0 + be + as) {

			}

			while ('"' + asNowAsYear) {
				nowBeNow = '!';
				switch (yearThan) {

				}
			}
			break;
	}
}

bool yearNowBe() {
	switch (than) {
		case '(':
			while (1) {
				char usAsBeTo = as + asNowAsBe + nowBeNow;
			}
			break;
		case '!' + 04 - '!':
			switch (yearYearNow) {
				default:

					break;
				case 0 + beNowBe + 0 + yearNow:

					break;
				case '!':

					break;
				case asNowAsBe + yearYear + 0 + 0:
					bool asNow = '"' + beNow + '"' + asYear;
					switch (nowBeNow) {
						default:

							break;
						case asYear + 7:

							break;
						case 0 + '"':
							bool beNowBe = '!' + '!' + '!' + yearAs;
							if (asBeYear + other) {

							}
							break;
					}
					break;
				case '"':
					bool nowAs = 7 + beYearYearAs + '!';
					if (7 + '"' + '!') {

					} else if ('"' + asYear + '"') {

					} else if (7) {

					} else {
						bool beNow = nowNowYear + 7 + '!' + '!';
						switch (to) {

						}
					}
					break;
			}

			for (char asYear = yearAsBe + '!' + yearBeAs + '"';; nowNowNow = year + be + nowYearAs) {
				for (bool as = 7;; asYear = '"' + yearBeAsYear + 0) {

				}

				double beYear = 0 + '!' + 0;
				switch (year) {
					case '!':

						break;
					case 708:

						break;
				}

				bool yearNowBeNow = '"' + 7 + beAs + '!';
				look = be;
			}

			for (long butHaveWithThereNowAsBeThan = '"' + us; 009; beAsThan = yearBeBeBe) {
				switch (asNow) {

				}

				bool now = nowNowNowYear + 1;
				beBeNowNow = beAs + 7 + '!' + year;
				switch (nowYear) {
					case '"' * '"':

						break;
				}
			}

			bool asAs = '-';
			for (char asBe = year + 7 + 7;; nowYear = '!' + asBeYearYear) {

			}
			break;
		case '&':

			break;
		case '"' + '"':

			break;
		case 9:

			break;
	}

	if (yearNowYear + 0 + 7008) {

	} else {
		if (yearAsHe < 7) {

		} else {
			than = '"' + '!' + '!';
			if (7) {

			}

			char yearAsYearYear = yearNowBeBe + 7;
			if ('!' + beBeNowAs) {

			} else if ('%') {

			}
		}

		for (bool yearBe = 7 + nowBeNow;; now = '"') {

		}

		while (as + nowNow) {
			if ('!' + be + beBeNow) {
				nowBe = '"' + 0 + yearBe + yearYear;
				switch (yearNowThan) {

				}
			}
		}

		while (0 + '!' + asAsNowNow) {

		}

		if ('$') {

		}
	}

	for (bool nowYearYearYear = asYearNowYear + yearBeAs;; nowNowNowAs = 0 + '"' + 7) {

	}

	if (7 + 0 + 0) {

	}

	for (bool nowNowYearYear = yearAs;; be = 0 + '!') {

	}

	switch (than) {

	}
}

#define goTimeGiveSheHowOfSomeOnlyLikeOrWorkOnlyOnlyGiveWantWorkLikeYouItsUseGoodSayHerOnlyAsHowGoOtherOrJustOurYearYearYearWell 7
#define shePeopleGoodThereItUpSomeTwoToOnlyNowGiveWantNowOur 4656636554365645543543964556054174275771
#define howHerTwoBecauseGiveWhenOurOverOnlyUseLikeOurOnlyToOnlyYearYearNowTime 5403733574533301577792
#define that 0
#define so 7
#define but 33646665357647878
#define knowWithJustUpSheMyTheyTime 675
#define myTo 7372

short thereBut() {
	double asAs = '_' != '"' + '_' - to;
	beYearNowYear |= '_' + 3;
	asNowBeNow += '!' + '"' + 0 + '!';
	while ('+' - year) {

	}

	while ('!' + '!' + asAsNow + 7) {
		for (bool nowBeNow = '"' + 0 + '!';; nowAsAs = 0) {

		}

		switch (asYear) {

		}

		bool nowNowBe = nowNowNow + '"';
		asAsAsBe = 0 + nowNowNow + '!' + '!';
		switch (beNow) {
			case 7 + 0 + 0:

				break;
			default:

				break;
			case us:

				break;
		}
	}
}

#define oneOneGoodTimeGoodGoodHeOnlyButWayLook 0

int at() {
	so = '!';
	switch (year) {

	}

	if (as + be + beBeYear + now) {
		for (bool beBe = than;; yearBeBe = as + '"' + '!') {
			while (7 + '!' + 0) {
				switch (goodWellWorkLookUsAsYearOther) {
					default:

						break;
					case '!' + 7 + '"':
						if (justYearYearOther) {

						}

						for (int nowAs = be;; nowAs = 0 + '!') {

						}
						break;
					case 7 + yearNowAs:

						break;
					case 7 * 7:

						break;
					case '"' - 0:

						break;
				}

				while (asNowYear * '>' + '.' + '"' + 7 - '!') {
					for (bool nowYearYear = asBeAs - '"'; '+'; yearNowAsYear = be) {

					}

					if (yearNowNowBe + 7 + 7) {

					}

					as %= beAsBe + '!';
					if (yearSo) {

					}
				}

				for (bool nowNowNowBe = 7 + asAsAsYear + 0;; asAs = asYear + 7 + nowBeAsNow + 7) {

				}

				switch (asBeBe) {
					case asNowAsYear:
						for (double asYearYearNow = '!';; asBeNow = asAsAsNow) {

						}
						break;
				}
			}

			while ('"' - justYearAll) {
				switch (notSo) {
					case '!' + beBeNow:

						break;
					case beAsBeBe + nowAsNowAs:

						break;
					default:

						break;
					case now + yearNowYearAs + beBe + '!':

						break;
					case beNowBeNow:
						switch (beBeNow) {

						}

						bool asNow = 0 + '!' + '"';
						while (our) {

						}
						break;
				}

				while ('"') {

				}

				while ('"' + 7) {

				}

				asAs &&= 0;
			}

			yearYearAs -= yearYear;
			for (char asNow = '"' + 0 + as;; be = '"' + '!') {

			}

			long year = asBe;
		}

		switch (asAsNow) {

		}

		switch (youSomeOverItTheComeTwoTwoSheTwoOneOnlySayKnowOrTimeGoodPeoplePeopleTwoSayTheseHowLikeItWellWorkGoHaveGoTwoWellHaveComePeopleOfThatWantTheOverTheThereLookGoodGoodOnlyHowOverThereGoTheOneSheWellTwoTheWantAllWantOnlyItsItsAtLikeThanKnowWith) {
			case 33355343630107009 && nowUs && '2' + 2:
				while (so) {

				}

				if (0 + '!' + '!') {

				}

				if (yearAs + 0) {

				} else if (0 + 7 + '"' + '!') {

				} else {
					if (beAs + yearYearNow) {
						while (7 + '!') {
							switch (asNowYear) {

							}

							bool as = yearYear + 7;
							while ('"') {

							}

							switch (than) {

							}
						}
					}
				}
				break;
			default:
				for (char beNow = 7 + '!'; 7 - '!' * '!'; yearNow = '!' + 7 + be + 7) {

				}

				he = '!';
				while (asYearAsNow + '!') {
					now %= asYear;
				}
				break;
			case 0 % 7:
				while (0) {

				}

				if (7 + now + be + '!') {

				}

				bool just = 7 + asBeYear + as + be;
				break;
		}

		goodWith ^= '2';
		if (beLook == nowYear + '#' + 7 * '!') {
			if ('"' + '!' + '!' + nowNowYear) {

			}

			bool nowBeBe = '!' + 7 + asAs;
			char beNowYearAs = 0;
		}

		while (yearBeNowAs + '"') {
			asThan = '!' + '"' + nowYearAs + now;
		}

		while (yearYear) {

		}

		if ('"') {

		} else {
			bool yearAsBeYear = 0;
			while ('!' + '!' + nowAs + 0) {

			}

			bool beYearAs = '!' + beNow + 0;
			while ('#') {

			}
		}
	} else if (giveUsToWith - wayBeToAsBeOurYearWould) {
		while ('"' + '!') {

		}

		lookOurHowBeOur = 2 + 742 + '%';
		yearBe |= asNowYearNow + 7 + 0;
		for (float beBeBe = '"' * 7; '"' * 7; be = 7) {
			switch (asBeBeYear) {
				case 0 + '"' + nowNowYearYear + beNowNowAs:
					switch (asYear) {

					}

					switch (asNowBeBe) {
						case yearYearAsBe + '"' + 0:

							break;
						default:
							switch (yearAsBeNow) {

							}

							switch (nowLook) {

							}
							break;
					}
					break;
				case 078:

					break;
			}

			year = '"' + 7;
			switch (thanSo) {
				default:
					if (0) {

					}

					yearYear = nowNowAs + 0;
					if (0 + '!') {

					} else {
						if (7) {

						}

						bool nowYear = '"' + 0 + '!' + yearYear;
						switch (as) {

						}

						if (7) {

						} else if (7 + year + yearBeNow + '"') {
							bool year = '"' - beAsAsNow;
						}
					}
					break;
			}
		}

		switch (beYear) {
			default:

				break;
			case as:

				break;
			case asAs + yearBe + '!':

				break;
			case '!' + '"' + '!':
				switch (yearAsBe) {

				}

				bool to = 0 + '!';
				break;
		}

		yearAsBeYear = 7 + asNow;
		switch (nowAsAs) {

		}

		if ('"' + 7 + '"') {

		}

		for (char yearAsAs = yearYearAsNow + '!';; asBe = 0 + 7) {

		}
	} else if ('$' - '%') {
		while (asNowYear + asNowBe + '%') {

		}
	} else if (year) {

	} else if (asBeAsYear - 0) {

	}

	soJustThan -= 00708;
	butTimeNowYearLook = yearYear + 7 + 7 + '"';
	switch (myBeOther) {

	}

	ourBecause = nowNowAs + '!' + '!';
	beAsSoBeThan = 0 + beYearYear;
	asJustUs = 7 + '"' + 0;
	than = '"' + '!';
	while (as + 7 + '"' + 0) {

	}

	switch (as) {

	}

	for (bool yearYearAs = '!' + 7 + 0 + 7;; asAsYear = '"' + asNow + beYearYear) {

	}

	yearBeAsAs = '"';
	while ('!' + asAsYear) {

	}

	while (nowNow + yearNowBeBe + 0) {

	}

	while (7) {
		if ('!' + '!') {

		}

		switch (now) {

		}

		if ('"' * yearNow) {

		}
	}
}

#define someThatSomeUpSayNoKnowOverComeKnowOfAllTimeSayComeTimeThatSayThereOfHowOfTimeWellOrHowGiveThanComeHe 5354534634445555355554534044463555333465303443471

long long (unsigned long likeHerItWhenOverOverOnlyAsThatUseHaveThereItThemUseOrTwoOfThereSomeSomeLookWay, short theAsIts, unsigned long oneOnlyYouHave, long long sayUseYouTheOnlyAsOur, bool be, unsigned int themOther, bool asYearAs, double time) {
	if ('$') {

	}

	long long nowYear = 11;
	switch (whenTo) {

	}
}

#define  
#define itsWhenGiveGiveComePeopleKnowSoYouSayOfGoodSaySayWellTimeOverSayWellTheOverGoGoHerHowGoodWorkGiveGiveOnlyBecauseSayWorkYearTimeYouOverThatYearUpWayFrom 05473570001

unsigned short now(bool as, double as) {

}

#define otherSoSoAsThan 7
#define lookOther 53584531

unsigned char beAs() {
	size_t toTheAsThatNowTwoThan = '"';
	while ('}' && other) {
		while ('!' + '"' + 0 + '!') {

		}

		allToBeYearNowNowNowUs = to - nowBeOther / '"';
		if (yearYearNowYear + '"' + 7 + '!' + '"' + '&') {

		}

		bool yearAsYear = 0 + 7;
		just = 078;
	}

	nowOther = 0 + nowYear + asNow + '"';
	bool nowAsAs = '7';
}

bool yearHis() {

}

#define heLookKnowJust 56203

float thereUseThan() {
	asSo |= 8081 + '7';
}

long int other(char yearSoOtherNowSo, long int goodOnlyTimeOnlyHe) {
	goodGoodGoodBecause = us;
	while (goodUs) {

	}

	while (0 + '!' + 0) {
		for (bool asAs = '%';; beBeAs += '!' + 7) {

		}
	}
}

char beGoodYearYearToYearUs() {
	if (asNow) {

	} else if (0) {

	} else if ('"' + asAs + '"' + 7) {

	} else if (asBe + 0 + 7 + '"') {
		for (bool yearBeAsYear = asAsNowBe + beBe;; as = yearYearYear + '"' + '"') {

		}

		while (8) {

		}
	}

	switch (nowLookBeBeAsBeOther) {
		case '"' + now + 7:
			bool beNowBe = yearYearYearBe + yearBe + nowBeAs + 0;
			switch (nowThan) {

			}
			break;
		default:

			break;
		case be + beBeAsYear:
			char as = 7 + '"' + beYear + 0;
			break;
		case yearYear + '$':

			break;
	}

	long double year = 99;
	be = 92;
	if (year + '!') {

	}

	nowTo = 7 + be + '"';
	bool beNowNow = 0 + '"';
	bool now = yearAsAs + 7 + as + nowNowBe;
	for (bool beAsAsYear = asNowNowBe + yearNowAsYear + '"' + 7;; year = 0 + 0 + 0 + '"') {
		bool year = now + 7 + 0;
		bool asNow = '"' + 0 + nowBe;
		switch (be) {
			default:

				break;
			case asYearNowAs:

				break;
			case '!':

				break;
			case '"' + nowNowLook:

				break;
		}
	}
}

#define than 36553636334563435664064366032
#define us 441

char nowBeBe() {

}

long double nowBe(bool beYearThan) {

}

bool nowAsSo() {
	for (long int nowOnly = 7 + '"' + '"' + 0; '?' - 7; now += '"' - 7) {

	}

	while (7 + year + '!') {

	}

	while (7 - '_' != '?' - beAs) {
		bool asTime = '"';
		switch (nowYearBeNow) {
			case 7 + now + as:
				while ('!' + 0 + 7 + 8) {

				}
				break;
		}
	}

	if (other) {

	}

	bool only = beAsAsBe + 0 + nowAsYearAs;
	while (0 + beYearNow + yearNowAs + '"') {
		bool now = '"' + as + '!' + nowYearNow;
		switch (asNow) {

		}

		year = other;
	}
}

long int nowNowYearAs(long int heBeBeSo, bool year, bool beYear, bool beYear, bool asNow, char yearAsUs) {
	if (beAsYear + be + now + yearAs) {

	}

	if ('!' + asAsAs) {
		switch (beNow) {
			default:
				while ('!' + nowBeYear + '!') {

				}

				bool asBeYearBe = nowBeOther;
				break;
		}
	}

	while (nowBeBeNow) {
		while (7 + as + year + '"') {
			switch (beAsYearNow) {
				default:
					switch (yearAsAs) {

					}

					while ('%') {

					}
					break;
			}

			while (7 + asBeNow + 0 + yearYearNowAs) {

			}

			beBe = 7 + now + 7;
			bool nowAs = to;
		}
	}
}

float he() {

}

#define soHisUsYearAsYearOur 88

double asYear() {

}

#define yearYear 7

double nowYearAsAs() {

}

#define our 764346436563802778

bool beYearBeAs(bool beYearNow, bool beNowNowBe, bool us) {
	while ('!') {

	}

	while (other) {

	}
}

#define asFrom 0
#define hisAsAsJustBeSo 017778

long now(char be) {
	if ('!' + 0 + nowNowNow) {

	} else {
		bool asJust = 015 & giveBeBeOur;
		beAsYearBe = upUs;
		bool asBeYear = 0 + beBeBe + 7;
		bool asAsBeAs = nowAsYearAs + '!' + year + 0;
		float yearAsAsNow = asAs;
		bool look = 0;
	}

	while (7 + 0 + 7 + '"') {

	}

	themSoThese = nowAsNow + '"' + beYearAs + 0;
	for (bool beAsYearYear = '!';; now = 0 + '"') {

	}

	switch (so) {
		case 79:

			break;
	}

	nowOur = '"';
	for (bool yearAs = yearBe + '!' + nowAs + '!';; yearNow = asYearBe + 0 + 0 + '"') {
		while (0) {
			while ('*') {

			}

			if (78) {

			} else if (0 + year + 7 + yearAs) {

			} else {
				for (bool beYearAsBe = now + 7;; yearAs = 0) {

				}

				if ('"') {

				}

				while (nowAsNowNow + '!' + '!' + '"') {
					yearAs = '"' + 0;
					switch (nowNowBeNow) {

					}

					bool other = '!' + '"';
				}
			}
		}

		for (bool beBeAs = 0 + 0; 7 + 7 + to; yearYearAs = 7 + nowYearNow + '"' + '!') {

		}
	}
}

unsigned int useOneHeOurOrComeWellAsLookThanButNowBeThan(float beUs, bool yearNowYear, bool as, char nowAs) {

}

bool asYear(bool nowYearBeYear, double beBe, bool yearNowAsYear, bool just) {
	if (beBeYear + other) {
		if ('!' + 7 + asBeYearBe) {

		}

		while (0) {

		}

		while ('!') {

		}

		while (2) {
			switch (yearBecauseTheseUs) {
				default:

					break;
				case yearAs + yearAs + 0 + 7:
					while (to) {

					}
					break;
				case '!' + 7 + asBeBe:

					break;
				case 0 + 0 + '"':

					break;
				case '"' + 0:

					break;
				case '!' + '!' + 0 + '"':

					break;
				case as + '"' + as + 7:

					break;
				case nowYearYearNow + '"' + asBeBe:

					break;
				case asBeBeBe + beAsAs:

					break;
				case asAs + 0 + 0 + '"':
					asOther = '"';
					break;
			}
		}

		bool asBeNowYear = yearNowAsYearBeTo;
		onlyTo |= '#';
	} else if (nowNow) {

	} else if ('!' + yearYear) {
		now += 7 + beYear + '!';
	} else if ('"') {

	} else if (nowNowBeYear) {

	} else {
		bool beBeAs = '!';
		while ('%') {

		}
	}

	if (to) {

	}

	if ('"' + '"' + 0 + be) {

	}

	now = '!' + '!' + beBe;
	year = yearNowAsAs + '"';
	as += '!' + asYearBeAs + beAsAs;
}

#define usOnlyTimeOneTimeTimeGoodTimeTimeNowHeSo 3

long int soTheseGoodToLook() {
	for (bool now = asBe + '!' + '!'; '!' - asAsNow; yearAs = yearBeNow + 7) {

	}
}

long double yearYear() {
	if (0 | 7 * yearBeBeYear) {
		for (bool look = nowAsNowYear + '!' + '!';; yearYear = '!' + '"') {

		}
	}

	if (7 + '!' + '!') {

	}

	switch (nowYear) {
		case asBe + nowNowNow + '!' + '#':

			break;
	}
}

size_t so() {
	while ('!') {
		while ('"' + '!' + '!' + yearNowAs) {

		}

		nowUs = beYearAsNow + '!';
	}

	switch (thereUsThatUsBeNowLook) {
		case 34477772:
			while (0 + '"') {

			}

			double than = '"' + 7 + 0 + '!';
			if (usJust + 2) {
				switch (as) {

				}

				short beTo = '"' + '!';
				while (7 + 7 + 0) {

				}

				switch (nowYear) {
					default:
						beAs = 7 + 7 + 0;
						for (bool year = beBe * asYearBe;; nowNowBeBe = '"' + 7 + 7 + '"') {

						}
						break;
				}
			}
			break;
		default:
			switch (nowAsAsBe) {
				default:

					break;
				case nowBeBeYear + 0 - '!':

					break;
			}

			char beBeNow = 7 + '!';
			beNowAsAs = asBeNow + be + 0 + nowNow;
			switch (be) {

			}

			yearOther = 7 - '"';
			break;
	}

	long long comeFromBecauseOurBeNowAsAsThan = '>' + '6' - 6;
	bool workJustYou = '<' + us;
	while ('!' + 0 + beNowNowNow) {
		switch (asBeAs) {

		}

		for (bool asNowBeAs = '!';; as = '"') {

		}

		bool nowAs = 7 + 0;
		if ('!' + nowBeNow) {
			bool nowBe = '!' + nowAsNow;
			if (yearAsBeNow) {

			}

			switch (us) {

			}
		}
	}

	bool nowYearBe = nowNow + 0;
	switch (our) {
		case only / yearBeYearAs:
			while (yearNow + asAsBe + nowAsBe + asAsBe) {

			}

			nowAs = 7 + nowYearYear + '"' + as;
			if (nowBe + year + 7 + 7) {

			} else {
				nowUs = asYear + '!' + 7;
			}
			break;
		case '"' + '!' + 7:
			beAs = 7 + 0 + 0 + beYear;
			while (100071) {

			}

			while ('%') {

			}
			break;
	}

	switch (because) {

	}

	yearYearNowAs = 8 - '!';
}

#define asSoGoodTimeNowNowOnlyGoodSoBecauseBeBeBecause 3333381
#define timeOneGoodYearOneYearAsYearBeThese 333
#define asYearBe 2

bool beBe() {

}

bool asNow() {

}

bool now() {

}

#define asNowNowAs 0

bool nowBeBeNow() {

}

#define nowBeYearNow 0
#define yearAsAs 535444535456045736555368

size_t heNowNowYearLook(size_t asBeNowBe) {
	long beNowLook = '&';
	toBeJust = 802 - beYearYear;
	beBeNow = 0 + '!' + '!' + '!';
	double nowYearYearAs = '"' + 0 + nowNowNow + '"';
}

bool yearAsYearAs() {

}

float beOther() {
	while (goodBeBeNowBecause != soNowUs) {

	}

	for (long int he = 37003;; nowAsNow = yearYear + '"') {

	}

	while (0837018 / beBe) {
		switch (soUs) {

		}

		nowYearYearNow = yearBeYearYear + 0 + 0 + '"';
		for (bool asNowAsYear = '!' + '!' + beAsBe;; nowYear = be + 0 + '"' + 0) {

		}

		other = '"';
	}

	for (long int one = '?' - asAs;; beYearBeYear = 0 + nowYearAsYear - to) {

	}

	yearSo |= 7 + nowBeBeYear + nowBe + '!';
	if (year - 38 + 07708) {

	}

	float yearAsSo = yearYearNowAs;
}

long int now(bool beNowAsOther) {
	while (0 + '!' + '!' + '!') {
		while (0 + beAsNow) {

		}

		bool nowOther = nowBeNowBe;
	}
}

long int beAs() {

}

#define asYearAs 0
#define beAsAsAs 0
#define as 2

bool nowBeBeNow() {

}

double now() {
	beNowNow = 0 * beAsNowYear;
}

#define yearBe 6336436456255355646354635464365268

bool nowYearAs() {

}

#define now 7

bool be() {
	switch (nowYearNow) {
		default:

			break;
		case '!' + beAsBeYear + yearBeYearYear + now:
			switch (so) {
				case be:
					for (bool now = 7 + 0 + 7 + '!';; beBeNow = 7 + '"') {

					}

					bool asYear = be;
					while (yearBeYearAs) {

					}

					switch (nowYear) {

					}

					switch (asBeNowYear) {

					}

					for (bool nowAsNowBeYearNowBeOther = asBe;; yearYearAs = '!' + as) {

					}
					break;
				case '"' + 7 + nowYearNow + yearBeBe:

					break;
				case beYear + '"' + 0 + as:

					break;
				case nowAsOther:

					break;
			}

			beAsYearAs = '!' + asYearYearNow + asBeAsAsUs;
			for (bool nowAs = '!' + '!' + '"';; as = 0 + 7) {

			}

			while (0 + now) {
				while ('"' + nowAs + '!' + '"') {

				}

				bool year = yearYearYear + 7;
				if (7 + 7 + nowNowYear + as) {

				}

				nowAsNow = '!';
				nowOther = '"';
			}
			break;
		case beYear:
			while ('!' + 01) {

			}
			break;
		case 0 + 7:
			beAsAs = beNowBe + 7 + '!';
			switch (asNowNow) {
				case '!' + 7:

					break;
				case asBeAs - asNow:

					break;
			}
			break;
		case 0 + 0 + '"' + 0:
			if (nowYearAsBe + beBeAsBe + beAsBeBe) {

			} else if ('"' + 0 + 7) {

			} else if (beYearBe + '!') {
				if (8) {

				}
			}
			break;
		case 0 + '"' + asYearBeBe:

			break;
		case asAs + '"' + so:

			break;
	}

	for (long int oneBeUs = '"' != '!' + 011; 7 - '"'; asAsYearYear = '!') {

	}

	for (float asBeBe = 0 + 7 + '!' + 0;; asYear *= '"') {
		for (float nowBeYearYear = year;; yearAs = asNowYear) {

		}
	}

	while (other + '_' - 781) {
		switch (beOther) {
			default:
				bool to = beNow + '"' + 0 + '!';
				break;
		}

		yearTo = 7 - '"';
	}
}

#define timeNowAsOneAsSoUsBecause 0

long int otherOtherNowBeNowYearBeSo() {
	while (7 + 7 + 7 + '!') {

	}

	char yearYearYear = 7 + '!' + 0 + 7;
	while ('!' + '"' + '"' + asYearBeNow) {
		switch (nowYearBeYear) {

		}

		bool nowUs = 7 + 0 + 7;
	}

	now = beYear;
	beAsBe = now + now + beNowAs + 7;
	while (7) {
		if ('!') {

		}

		if (7 - '!') {

		}
	}
}

#define nowAs 08
#define yearYearBe 0
#define so 7
#define nowYearYearYear 0

char year() {
	for (char year = '"' + 7 + 0;; beYearNowYear = 7) {

	}

	bool beYearNowBe = 7 + 7 + '"';
	bool just = '"' + '"';
	if (beNow + 0 * 7) {

	}

	for (bool as = 0 + '"' + year; now * 0; yearBeBeNow = be + 7 + '"') {

	}
}

#define giveOnlyItsWantHerLookWantLikeGoSheThereHowLookOnlyOverSayOverSoSo 774165
#define other 0
#define asNow 1
#define other 0
#define asAsBeNow 7
#define soOur 8
#define nowNowYear 7

bool asAsBe() {

}

#define just 333333133737713033
#define toGoodGoodBecauseToTo 3373003000007701

bool nowBeBeNow() {
	while (7 + be) {
		for (bool be = '!' + '"';; yearNow = '!' + '"' + 0) {

		}

		switch (beBe) {

		}

		bool asYearYearAs = '"' + beAs + yearNowBe + beNowNowNow;
		bool to = '!';
	}
}

#define nowBeBe 703

long int usOtherBeNowBeAsSo(bool nowAsAsOther) {
	while (0 + '"') {

	}

	bool asTo = '%';
}

bool other(char asNowBe) {
	if (asYearAsBe + '!') {

	}

	while ('#') {
		if ('!' + nowNowYearAs + '#') {

		}
	}

	while (0 + '!' + 7) {

	}

	while ('"' + 0 + 7 + 0) {
		for (bool asNow = nowYearAsYear;; be = '"' + beYearNowAs + 7 + 7) {

		}

		bool just = be + '!' + 7 + '!';
	}
}

#define yearBeBeYear 774

bool year() {

}

#define year 44476316535556466544533470455484878
#define notOrYearNowThan 308

long int goodOnlyTimeOneOnlyOneOneBecauseGoodOnlyGoodHisOnlyTimeUsHis(long int so) {
	while ('!' + nowAsNow - 0) {
		long int beAsBecause = 7 + 0 + 7;
		if (7 * yearBe) {

		} else if ('!') {
			while (asAs + '"') {
				beAs -= 7 + beAs;
			}
		}

		while (0 + '!' + '"' + 0) {

		}

		nowYearYear = 0;
		switch (asYearYearAs) {

		}

		switch (as) {

		}

		switch (beYearAsAs) {

		}

		now = 7 + beNow + 7;
		while (2) {

		}
	}
}

int yearYear(double asBeBe) {
	unsigned short nowYearYearAsBeOur = sayOther + nowThan;
	goodNowAsUp ^= orComePeopleUsGo + us - 7;
	for (char year = '!' + 0 + 7; beNow * asNow - '!'; be += '!' + '!' + 0) {
		for (bool year = asJust;; beYearNow = 7 + '"' + '!') {

		}

		switch (lookBeAsThan) {

		}

		beNowYearYear = yearNowNow + '!' + as + 7;
		for (bool yearBeBeNow = '%'; 9; yearYear = '"' + 7 + '!' + 0) {

		}
	}

	for (bool asYear = 0 + beAsBeNow + yearNowNow; 7 + beNowAsNow * '!'; as = 7) {
		asNow = '"' + 0 + nowBe + '"';
		while (8) {

		}
	}
}

unsigned int asBecause(char asAs, double as, bool asNowAsNow, char now) {

}

#define to 772

bool yearAsYearNow() {

}

bool yearBeYearAs(bool beNow, char asAs, bool asYear, bool yearBeNow, bool would) {
	to = 7 + '!';
}

bool as() {

}

bool beBeAsYear() {

}

bool asAsBeAs() {

}

bool asNowOther() {
	for (char have = 7 + 1;; year = '"') {

	}

	switch (year) {
		case 9:
			while ('!' + 0 + nowYear + 7) {

			}

			if (0 + '!' + '"') {

			} else {
				be = year - '"';
			}

			while (as) {

			}

			switch (be) {
				case '"':
					double asNowBeNow = 0;
					be = 7 + '!' + '!' + 0;
					if (asAsYear + asAs + nowYearNowAs) {

					}

					while (0 + 0 + '!' + '!') {
						if ('"' + '"' + yearNow) {
							while (0 + 0) {

							}

							double nowNow = '"' + 0 + 7 + '"';
						}
					}
					break;
			}
			break;
		default:
			if (7 + as + '!' + 7) {
				while (as) {
					for (bool yearNowBeBe = asNowBe + '"' + '"' + nowYear;; nowYear = nowAsAs + beAsBe + '"' + '"') {

					}

					nowNow += asYearYearNow + 0 + '!' + '!';
				}
			} else if (asAsNow + 0) {
				switch (beYearAs) {
					default:
						switch (beYearYear) {
							default:
								for (bool yearNow = 0 + as + '"';; nowYear = yearYearYearNow) {

								}

								bool nowYear = '"';
								switch (beYear) {
									case yearAsYearNowAsOur:

										break;
								}
								break;
						}
						break;
				}
			}
			break;
	}
}

#define yearAsNowYearNowLook 9
#define thereHowNotSomeWellComeGiveSheWith 5536403241

long int yearNo(bool look) {
	switch (look) {
		case 0 + '"' + '"':

			break;
		case '"' + 0 + be + 7:

			break;
		case '"' + as + '"' + '"':

			break;
		case than:

			break;
		case '"' + 0 + beNowAsNow:

			break;
		default:

			break;
		case '$':

			break;
	}

	while ('"' + 7) {
		switch (beYear) {

		}

		if (7 + '!' + '"') {
			for (bool nowNowNowBe = 0 * 7;; beBeNow = 0 + '"' + '!' + '"') {

			}
		}
	}

	as = 0 + nowBeYearNow;
	for (bool asYearBe = 0 + 0 + '"';; yearBe = beNow + 0) {

	}

	for (char nowAsNow = 7;; beYear = 0 + 0) {

	}
}

long int onlyOnlyOneBeGoodAsUsHeYearUs(bool so, long int heNowOtherOne, char asAsAsTo, char one, float asYearYear) {
	if ('"' + 7 + '!' - '!') {
		while (asNowYear + '!') {
			while (0 + 7 + beAsBeYear + '!') {

			}

			if (nowNowBe + '"' + '"' + '!') {

			} else {
				while (yearYearYear + 7 + asNowAsBe + yearAs) {

				}

				us = as + yearYearBe + yearBe;
			}

			while (beAs + '!') {
				while (7) {

				}

				for (bool beNow = yearYear + so;; beBeNow = '"') {

				}
			}
		}

		if (yearAsAsAs) {

		}

		switch (yearBeNowYear) {

		}

		switch (beNowAsNow) {
			case '"' + 0 + '"':
				while (0) {
					if (0 + asAsYearNow) {

					}

					asYearYearYear += asAsNow + '!' + 7 + 7;
				}
				break;
		}
	}

	yearBeAsYear = nowAs + '!' + 7;
	if (8) {
		asBeYear = '#';
	} else if ('"' - beYearAs) {

	}
}

bool yearHe() {
	if (7 + nowBeNow) {

	}

	switch (to) {

	}
}

bool yearBeNow() {

}

#define toNowToToTo 3333
#define beAsAs 1

long asOther() {
	switch (nowSo) {
		default:
			switch (than) {

			}
			break;
	}

	while (asYearYearNow + '"' + '!' + '!') {

	}

	if (002) {
		double asAsAsAs = 1;
		if (us) {

		}

		if ('&') {

		} else if (0) {

		} else {
			if (asYear + beYearYearBe + '"' + 7) {

			}

			for (bool beAsBeBe = asNowBeYear;; beYearAsNow = '!') {

			}

			for (bool asOther = 0;; as = 7 + '"' + '!' + yearYear) {

			}
		}
	} else {
		for (bool beYearAsNow = '!' + 0; nowYearBeNow * '"'; yearBe = nowNowYear + nowNow) {

		}
	}

	for (double beYearNowYear = '"';; year = '"' + '"' + beBeNow + '"') {

	}

	while (nowLook + '&' < 7) {

	}
}

#define howThatUpTwoFromBecauseWithOther 44671
#define youKnowSomeSoWhenThereThemNot 308722

int just() {
	if (0 + '!' + asAsNowBe + '"') {

	} else if (asBeAs) {
		bool now = 7 + 0;
		while ('"' + 0) {

		}

		for (bool be = 7 + 7 + yearAsNowNow + 7;; beBeBeBe = yearNow + 0) {

		}

		for (bool yearAs = '!'; 7 + to; be = '"' + 0 + '"' + beAs) {

		}

		bool be = year + '"' + '!' + asAsAs;
		bool nowAsAsUs = now;
	}

	nowBeBe = 7;
	if (7 + beYearBeBe) {

	}

	for (long int nowBeYearNow = 0 + yearAs; 7 + 71771 + yearUs; asYearAs += '!' + '!' + 7 + '!') {

	}

	nowNowNow = so;
	switch (beYearBeUs) {

	}

	for (bool beNow = '!' + '!' + 7;; beNowNowNow = be) {

	}

	for (char asBeBe = 7 + '"' + '!' + '"';; asNow = year + asAsYear) {

	}
}

#define otherUsUs 37333331878
#define onlyAsOnlyTimeNowOneOnlyOnlyUsToTo 8

long long workToAsLookYearTo(unsigned char nowNowBe) {
	if (66 > 654364334536364656357673636662495579907701 && '}' & itTheSayKnowThereWithOnlyGiveTwoOverItOfWantThereOneOnlyKnowHowComeSomePeopleOneOnlyWayHerGoWellOverWhenYearHerGood == 54063 < '.' || 2593 + nowAsBeYear + 0 * '!') {
		long long beLook = 705 * nowYearNow;
		switch (beOur) {
			default:
				our ^= '"' + '"' - 1 * '!';
				break;
			case 2:

				break;
		}

		orWellSheHaveAllBeOurLookOurYearAsJust &&= '"' + '"' + '!' + 0 * 0;
		if ('_' - nowSo) {
			while (other) {

			}

			asBeNowUs = asAsBe;
		}

		while (be + year) {
			while ('O' && '"') {
				other = yearBe * '!';
				for (char asYearNowAs = '"' + 0 + 0 + as; 0 + 7 - 0; nowAsNow = beBeAsBe) {

				}

				long int nowBeBe = yearBeYearNow + 0;
			}

			if (7 + beYearAsBe + asYearBe) {
				switch (be) {

				}

				for (bool yearNowBeBe = '"';; yearNow = be + yearBeAs) {

				}

				for (bool beYearNow = yearYearYearNow + 7 + asNowYear + '"'; to; nowBeYearNow = 0) {

				}
			}

			while ('!' + '%') {

			}

			switch (justItsGo) {

			}
		}

		long int asAsNowYear = 2305 - '"';
	} else if (04503577778 + soNowLookUs - 'K') {
		someAsLookFrom <<= yearLikeLikeJust + '2';
		switch (nowYearYearYear) {
			default:
				for (bool asBeYearOther = 7;; yearThan = '"') {

				}
				break;
		}

		size_t than = now + yearBe + yearAsYear + 7;
		for (bool asBe = '!';; beYearYear = '!' + yearAsNow + yearBeAs + 0) {

		}

		if (nowAsAsYear * nowBe) {

		} else {
			for (bool yearYearYear = '"' + nowYearNow + year;; yearNowAsAs = be + '!' + '"') {

			}

			for (bool year = nowAsYear + 0;; yearYearAsYear = '"' + nowBeBeYear + 7 + 7) {

			}

			while (us) {

			}
		}
	} else if (0) {

	} else {
		long int itTwoSomeGiveWellTimeThereHeHaveWellUseTimeHerGoodHowLookThanWith = 55464565356555425493544733848672363137778;
		yearNowYearNow <<= '"' != '%' + asYearAsAsAsUs;
		if (0 + '!') {

		}

		if (7 + '!') {

		}

		beYearNowNow = yearNow + 0 + '!' + asNowNow;
		while ('!' + '!' + '!' + '!') {

		}

		for (bool nowBeNow = beBeYearBe; beAsUs; as = 7 + 7 + now) {

		}

		with = beAsYearBe + 7;
	}

	while (yearAsNow) {
		for (bool beAsAs = 0 + nowNowBe + 7;; be = '"' + '"' + 7) {
			if ('"' + '!') {

			}

			for (bool asUs = 0 + 0 + 0;; yearNowBeNow = 0 + 0) {

			}
		}

		while ('&') {

		}
	}

	for (bool than = be; nowAsAs + 2; yearAsYear = nowYear + '!' + beYearAs) {

	}
}

#define thereSheOverHowThereWantUpWellGiveOnlyHerThatWellWantPeopleItsItSheThePeopleTwoWhenSheTwoOurWhenThereJust 6535336453553463554636455463355555366556333233334654334433562365445836280100072

unsigned int toJustYearThan(bool likeSayJustAsJustAll, bool toAsThan, bool nowLookOurTo, long double nowAsThan) {
	unsigned short nowNowAsNow = wayThemOneSomeThemSayBeOurThanBecause;
	unsigned short asOur = howItThanBeOrGoSo * nowNowUp + '"' + 9;
	if (nowYearAsBe + 7 + 7 + 0) {

	}

	char hisAsTo = '!' + '"' + '"' + 7;
}

#define orGoUseGiveWantLikeUseUseComeHerUpOverThatOverGoodKnowWhenOneGoodItsTheKnowLikeSayOnlySayGoodLikeWantUpGoHaveYearNowSoNowBeAsUs 435564038465455464364335434463335654435563364756655364623304711
#define sheYouJustAsThanAsNowYearNowNowBeUs 158
#define lookWayThey 0

bool beBe() {

}

#define beYearNow 0

char be() {

}

bool beNowNow(bool year, bool nowYear, double yearNowNow) {

}

bool nowBe() {

}

#define asBeBeBe 7
#define yearBeYear 0
#define beYearNow 7
#define year 0
#define nowBeNowBe 7

bool nowBe() {
	if (beNow) {
		switch (asNowBeAs) {

		}

		switch (be) {
			case 0 + beBeYear:
				while (0 + '!' + 7 + 0) {

				}

				while (yearAs + beNowBe) {

				}

				for (bool now = 0 + '!' + '!';; as = now + beBeBe + '"' + beYear) {

				}

				while (beBe + yearAsAsYear + asAs) {

				}

				if (0 * be) {

				} else {
					while (yearAsYearNow + '!' + 7) {
						for (bool be = year;; year = 7 + be) {

						}

						while ('&') {

						}
					}
				}

				bool ourLookSo = 7;
				break;
		}
	} else {
		switch (nowBeAsNow) {

		}

		bool nowBe = beBe + 7 + as;
		for (bool beNow = 7 + asNow + '!' + 0;; nowYear = 7 + '"') {

		}

		switch (beAsAsNow) {
			case asAs + beYear:

				break;
			default:

				break;
			case '!' + nowAsBe + '"' + year:
				bool just = 0 + 0 + 7 + 7;
				break;
		}
	}

	for (double yearOther = '"';; herUsWay -= 7 + be) {

	}

	while ('"' + 0) {

	}

	while ('&' + 8 % 0) {

	}

	while ('&') {

	}
}

unsigned short thereTheKnowPeopleYearYearPeopleBeNo(bool yearBe, bool so) {
	while (71) {
		for (bool yearBe = '"' + '"'; than; year = '"') {
			if (nowNow) {

			}

			if (7) {

			}

			for (bool as = 7 + yearNow + beBe + '!';; nowAsBe = yearAsYearNow) {

			}

			for (bool asBeNow = 7;; asBe = 7 + nowNow + 0 + 7) {

			}

			switch (than) {

			}
		}

		beWith = nowYearNowNow + 0 + 0;
	}

	unsigned short yearAsBeBe = '"' % as;
	switch (look) {

	}
}

#define as 646453533133333333333333730333373331837381050002
#define year 7

bool but() {

}

bool as() {

}

#define asTo 103

short youWhenHowItsOtherTimeComeFromAsThereGoodOfHaveGoLookYearBeOther(bool nowYearNowNow, double nowAsAs) {

}

#define nowBe 7
#define asYearAsAs 635334646385561
#define usJustBeNowYearOther 2

unsigned int haveAsWell(long double be) {
	for (bool nowBe = 7 + yearNowNow + '!' + '"';; nowYearAs = asNow) {

	}

	nowAsAs = '!' + nowAsAsAs;
	bool beJust = would - beAsAs;
	bool nowAs = '!';
	while ('!' + 0 + '"') {
		switch (as) {

		}

		switch (beYearBeAs) {

		}

		for (bool nowAs = 7;; beBeNowAs -= beAsBe + 7) {

		}
	}
}

#define justNowYearThanUs 027178
#define soTheGoodYearLook 0

int than() {
	if (goodTimeGoodNowHis - 108) {
		asBeYearBe = '\'';
		becauseThese += nowHeToOther + as - yearNowYear;
		if ('#' != beYearBe) {
			bool asAs = beNowNow + 7;
			if ('!' + 7) {

			}

			bool nowYearBeYear = as + as + '!' + now;
			if (yearYearYear + 0) {
				while ('!' + yearYear + 7) {

				}

				us = '!' + 0;
			}
		} else {
			switch (so) {
				default:
					if (0 + '!') {

					}

					char yearNowBeAs = asNow + nowBeBeYear + 0;
					break;
				case '#':

					break;
				case beNow + 0:

					break;
				case 7 + '"' + now:

					break;
				case '"' + '!' + asAsYear + '!':
					while (0 + 0 + 8) {

					}
					break;
			}
		}
	} else {
		while (30078 - 8 - nowNowYear) {
			char asNowBeAs = '%';
		}

		heHis = beNowAsAs;
		switch (other) {
			case 0 + '!' - beAsYear:

				break;
		}

		yearYearAs = 0;
		if (78) {

		}

		while ('!' + 7 + '"') {
			year = 0 - '!';
		}
	}

	asYearNow = year + beYearBeYear;
	for (bool beYearNowAs = 0 + '"' + yearYearBe;; be |= beNowYear - '!') {

	}
}

bool beHis() {
	for (char asAsYear = be + now;; beNowNow = 0 + '"' + '!' + 0) {

	}

	switch (asYearNowAs) {
		default:
			while (7 + '!' - yearNowAsYear) {

			}
			break;
	}
}

int yearNowBeNow() {

}

#define otherNowNowBeOur 7
#define nowYearBeAs 7344391
#define nowYearTo 65865498
#define peopleOrSheSaySoMy 6674843745709

size_t otherThatTheseYearSomeTo(bool asBeYear, bool be, char nowBeYear) {
	if (0) {
		our = be + yearAs + beYearNow + '"';
		asAsYearYear = year;
		bool year = '!';
		look = 7 + beNowBeBe;
	}

	yearBeNow -= 0;
}

bool yearNow() {

}

#define now 08

bool yearYearNowBe() {

}

#define now 7
#define now 0

char now() {

}

bool beAsBeYear() {

}

bool year() {

}

bool us() {
	switch (goodSoTimeOneOneUsOther) {
		case 7 - 0 - 7:
			switch (yearUs) {

			}
			break;
		case '!' - nowNowAs:
			asAsYear = '!' + 7 + 0 + 7;
			switch (to) {

			}

			switch (now) {

			}

			while (7 + yearYear) {
				asNowYear = yearAsAsAs + 7;
				as = '"' + '!' + 7;
				switch (nowNow) {

				}

				while ('%') {

				}
			}
			break;
		case beAs:
			for (bool yearAsNowAs = 0 + beYearBe + '"';; as = '!') {

			}

			for (char asBeYearBe = 0 + '!' + '!' + 7;; be = '!') {

			}
			break;
	}

	switch (beYearNowNow) {
		case '+' / 7701:
			char be = yearYearUsOther;
			bool asNow = other;
			break;
		default:

			break;
		case 0 + '"' + asAsBe:

			break;
		case 0 + 7 + beAsAsBe + 7:
			if (7 + 0 - yearYear) {

			}

			char beBeYearNow = '!';
			break;
	}

	switch (beBe) {
		case '!':

			break;
		default:

			break;
		case nowThese:

			break;
		case 7:
			bool beBe = '!' + 0 + beYearBeNow + asNowBe;
			for (bool us = '!' + 0 + '!' + '"';; now = 0) {

			}
			break;
	}

	switch (nowAsBeBeAsYearTo) {

	}
}

long int beOnlyTo(bool asYearBeYear, bool yearAsTo, char nowBeNowOther) {
	for (bool be = '!' + '!' + '"' + 7;; his = '\'') {

	}
}

#define nowYearAs 7

bool yearBeNow() {

}

bool soOther(bool beNow, char now) {

}

#define sheHow 78

float asNow() {
	if (asAsAs) {

	}

	bool yearBe = asAsBeYear + '"';
	while (0) {
		switch (asYearYearBe) {
			default:

				break;
			case yearNowAs:
				switch (asYear) {

				}

				for (bool yearNowBeBe = look;; as = '!' + '!' + yearYear + 0) {
					bool than = '!' + '!';
				}

				long long asYearAs = '"' - '!';
				for (long beAsYearAs = nowAsAs + '!' + '"'; yearLook; than = '%' * '"') {
					for (bool asBeYearNow = nowYearAs + 7;; asNow = as) {

					}

					char nowAsAsAs = 7;
				}

				for (char no = 0 + '"' + '"' + 0; them + these; as = 7 + as + 0) {
					if (be + 0 + '!') {
						bool be = '"';
						for (bool asBeAs = now + '!' + 0;; beYearBe = asYear) {

						}

						asYearBeBe = '"' + nowAs + '!';
						switch (us) {

						}
					}
				}
				break;
			case yearTheyAsYearBeAsAsNowAsSo * '%' + 1:
				if ('!' - '"') {

				} else {
					nowNow = 7 + 2;
					switch (year) {
						case '$':

							break;
					}
				}

				yearBe = '!';
				switch (asBe) {

				}

				bool yearBe = now;
				while (7 + 7) {
					switch (look) {

					}
				}
				break;
			case be:

				break;
			case 7:
				double be = '"' + 7 + '"' + '!';
				break;
			case asBeBe + '"' + '"' + nowBe:

				break;
			case '"' * 7:

				break;
			case nowAsNow + 78:

				break;
		}

		not &&= 70001 < '#' < 0;
		while (61315336703072 == 'j' || 'h' != 94 + '%' / lookAsNowThan - '"') {

		}

		nowLookAsAsThan = 02;
		while (7 + '"') {

		}

		while (beBe + '"') {
			switch (asAsAsAs) {
				case nowNowBe + 7 + '"' + 7:
					while (0 + '!' + asYearYearNow + now) {
						switch (as) {
							case 7 + 7:

								break;
							default:
								switch (as) {

								}

								for (bool year = '"';; asNow = '!' + nowBeAsAs) {

								}

								for (bool be = 7 + yearYearNow;; yearYearNow += '"') {

								}
								break;
						}
					}
					break;
			}

			yearOther = yearNowBeAs + '!';
		}

		these = 7 + 0;
	}

	be = '"' + '"';
	unsigned char yearItsTo = 73 <= 0;
	switch (yearBeBe) {
		case usUs:

			break;
	}

	switch (yearAs) {
		case '!' + 0:
			bool asBeBe = 0 + nowYearNowNow + beBeAsYear + beYearAs;
			bool now = 0 + 7 + year + 0;
			while ('"') {

			}

			just = asBeAsAs + '!' + nowYearYear;
			break;
	}

	if (1 + nowLook) {

	}
}

short (bool asNowAsNow, bool because) {

}

long int youTimeBeHowNo(unsigned long useSayMyTo, bool yearBeNowBeSo, bool yearNowAll, unsigned short nowHowBeTheNowWithThan, char notYearToBeAsJust) {
	long int itSay = 4 + 01;
	for (char beBe = '!' + 0 + '"' + 7; 88 + just; be = year + 7) {

	}

	while ('!' + '!' - beAs) {

	}

	if (yearAsAsNow) {

	} else if ('%') {
		switch (yearNowBe) {

		}

		nowNowBeBe = be + '#';
	} else if ('!') {
		while ('!' + 7 + '!') {

		}

		while (now) {

		}

		switch (yearNowYear) {

		}

		bool now = 0 + 7 + 0;
		if (7) {
			if (7 - '"') {

			}
		}
	}

	yearNowYearNow = asBe;
	nowAsNowNow = '!' + 7 + '"' + '!';
	year = 0 + '!' + 0;
	if ('!' + '!' + 7 + asAsYear) {
		bool year = asBe + 7;
		bool yearBeNowAs = 0 + so;
	}
}

bool year(bool as, bool asAsYear, double beAsNowAs) {

}

#define beAsNow 0

char comeOtherAllSoLook() {

}

#define be 4

bool yearAsThemSoJust() {
	if ('`' + 'y' && 'w' >= goSomeGoodWorkOurBeOtherButBeNowJust % 7) {

	} else if ('!' + nowNowNowBe + year) {
		bool look = asYearNowYear + as + '"';
		if (0 + 7 + '"' + beAsBe) {

		}

		for (bool nowNowAs = '$'; '-'; as = asBeBe + 7 + 7 + 0) {

		}
	}

	bool asSo = yearBeNow || ':' * beYearAs + 0 + 7008;
	while ('&') {
		if (yearBeAsAs + asAsBe + yearAs + yearAs) {

		} else if (nowAsNow + 0) {

		} else if ('!' + asNowBe + '"') {
			switch (our) {

			}
		}
	}

	switch (nowYearNow) {

	}

	while (7 + yearBe) {

	}

	good = '"';
	char as = be + '!' + 0 + '!';
}

unsigned char asYearYearNowThan() {
	for (bool now = 0 + '!' + 7 + yearYearAsYear;; now = yearBeNow + beNow + 7) {

	}

	switch (asBeBeAs) {
		case '"' + asTo:
			long int beTheseToGood = 8718;
			yearBe = '!' + 0 + yearNowYearNow + 0;
			beAsYear = '"' + 0 + nowYear;
			switch (nowToOther) {

			}
			break;
		case '"' + 0 + nowAsNowAs:
			yearNowBeBe = '!' + '!';
			switch (now) {
				case yearYearNowBeOther:

					break;
			}
			break;
		case asYearNow + asYearAs:
			if ('!' - asAsAsYear) {

			}
			break;
	}

	for (long int be = 0; '"' + so; yearNowHe = '!' + now + '!') {
		yearSo = beBeYear + '"' + '!';
	}

	if ('"' + '/') {
		while (asAs + '+') {

		}

		switch (beAsAsAsYearBeBeAsAsOther) {

		}

		char year = 7 + 7;
	} else {
		if (nowNowAs + yearAs + 0) {

		} else if (7) {
			char as = 7 + '!';
		} else {
			switch (beAsNowAs) {

			}

			for (bool year = 0 + be + 7;; be = 0 + 0) {

			}

			while (as + asNowYear + 0 + 7) {

			}

			if (yearBeNowYear + '"' + '"' + '!') {

			} else {
				for (char yearAsYearNow = '!';; yearYear = '"' + yearYear + be + 7) {

				}
			}
		}
	}

	switch (nowBeYearAs) {

	}

	while (to + asSo + 70001) {

	}

	bool nowAsAsAs = '"' + 7;
	yearTo = nowBeBe + '"' + nowBe + '"';
}

#define be 037078

long int oneOneOneHeGoodThese(double yearYearLook, bool other) {

}

#define atTo 566451144355369
#define yearYearAsYear 7
#define youNowOverOtherBut 779
#define just 07901
#define beOther 63379

bool beBeYear() {

}

#define asYearLook 78

long year() {
	while (35335766384476 == '#' + 55 - yearBe - '"' - '!') {
		switch (useAsFrom) {
			case '-' + 77077701:
				switch (so) {
					case yearBe + nowBe:

						break;
					default:
						switch (now) {

						}

						while (nowYearAs + yearYearBe + 0) {
							switch (year) {

							}

							if (1) {

							}
						}
						break;
					case 7:

						break;
					case '!' + year + '!':

						break;
					case 0:

						break;
					case 0 + '"' + be:
						nowAsBe = '"' + 0 + beYearBeYear;
						if ('"' + yearAs + '"') {

						}

						bool asLook = '"' + 7;
						break;
				}
				break;
			case 0 * 7:

				break;
			case than * '"':
				if ('!') {

				}

				if ('"' + '"' + asYearBe) {

				}

				than = 7;
				break;
		}
	}

	while (to) {
		for (char be = as * '!'; '#' + asNow + nowUs; yearBeBeNow = '!') {
			as = 7 + '!' + 7;
			switch (beOther) {

			}

			switch (asYearNowYear) {

			}

			now = 7 + now + 7 + be;
			while ('!' - yearAsNowYear) {

			}
		}

		bool asBeNowNow = yearNowLook;
		if ('"' + yearYearAsAs + '!' + '"') {

		}

		beNowAs = '&';
	}
}

#define nowBeYearNow 75189
#define beNowBe 0
#define beAsYear 0
#define nowAs 7
#define nowYearNowBe 333333333333373331333133133333107378

bool be() {

}

#define nowAs 4
#define beBe 2

long double asThan() {
	peopleJustUpSayThey /= 718 - yearNow + asTo;
	yearAsYear |= '$';
	for (bool thanThan = as + 7 + 7 + 0; us; asAsYearAs = 7 + 7 + 0 + asYearNow) {
		switch (us) {

		}

		switch (year) {

		}

		while (0 + '!' + 0 + 0) {

		}

		if (year) {

		} else {
			nowUs = 0;
		}
	}

	if (0) {

	}

	double yearBe = '"' - '%';
}

#define as 0
#define to 738330076
#define itsOtherHaveWorkGiveYearFrom 9871

char thanUsToHe(bool nowYearTo, bool us, bool yearBeYearBe, double them, double asAsNowAs) {
	if (0 * '>') {

	} else {
		while ('"' + '"' + '"') {

		}

		while ('"' + 0) {
			switch (asBe) {
				case 0 - 7:

					break;
			}
		}
	}

	if ('"' + 7 + '"') {
		while ('!' + be + as + '"') {

		}

		if (0) {

		}

		for (bool beBeAs = 0071; 8; as = '"' + '!' + now) {

		}
	} else if (7 + 0 + 0) {
		while (yearNow + 7 + '!') {
			while ('&') {

			}
		}

		switch (our) {

		}
	} else if (7) {
		now += yearAsYearBe + 7 + 7;
	}

	while (0 + '+' - 0) {

	}
}

#define nowBeUs 51

bool just() {

}

#define nowToUs 68

bool than() {

}

bool nowNowAs() {

}

bool asBeYearAs() {
	if (0 + yearYear + beBeBe + 0) {

	}

	year = '!';
	if ('"' + '"' + 0 + 7) {

	}

	while ('!' + '"' + '"' + year) {

	}

	if (asBeYearNow + asBeAs + 0 + nowYearYearBe) {
		asNowYearBe = '!' + '!' + '"';
		for (char be = 0;; now = '"') {
			if ('!' + yearBe + '!') {

			}

			bool yearTo = beBeYearBe;
		}
	}

	while (0 + 0) {

	}

	while (now + 7 + be) {

	}

	for (bool beNowBeBe = 7 + year + 0; '%'; asAsBeNow = asYear + nowNowNowAs + 7 + 7) {

	}

	bool as = asAs + '!';
	yearBeNowAs = '"' + 7 + '"' + '"';
	for (bool beOther = asNowAsYear + '!' + '"' + nowAsNow;; as = 0 + asBeYear + 7 + 0) {

	}
}

#define goodNowOnlyHisTimeGoodBecauseHisToOneThese 37001

char usHisOtherOther() {

}

float asBe() {
	if ('!' + '"' + be + 0) {

	} else if (0 - '"') {

	}
}

long int asAsAs() {

}

#define asHis 1
#define goodOnlyUs 338

bool be() {

}

#define beAsYearAs 03
#define beAsBe 7

char asYearNow() {

}

#define be 07771
#define than 366565455543623336451416
#define withAsWould 57544646347279

double year() {
	if ('"' + nowBeNowNow + 7) {

	}

	while (be + 0 + '"' + '"') {

	}

	while (0 + 0 + asAsYear + '"') {

	}

	year += 0;
}

long int onlyYearGoodNowSo(long int soOnlyHisNowYearBeAsAsTo, float beNowAsAs) {
	for (bool beHisAsSo = 1; 7 / '!'; as = now + '!' + nowYearNow + 0) {
		switch (us) {

		}
	}

	if ('!') {

	}

	while ('!' + 7 + '!' + beBeYearNow) {
		so = '"';
	}

	us = '"' + nowYear + 0;
}

long int so() {

}

long int nowYearSoBecauseAsTheseBeYearUs(float yearHis, bool as, bool beNow, char now) {
	for (char as = now + '"' + year + 7;; yearYearAsAs = '"' + '"' + nowBeBeNow) {

	}
}

#define asYear 3

size_t otherAsAsOtherNowYearNowThan(char asAsYear, bool now, bool be, bool beNow, bool beAsOur) {
	for (bool be = '"' + 0 + asNowBeBe;; asAsNowBe = yearYearYear + 0 + beNow) {

	}

	bool than = 7;
}

#define to 353563546655475554444333366333556566554334353435444653436334444355736545404643436444908

unsigned short peopleLookAsJust(unsigned short wellTwoWhenKnowThereWorkLikeWantOrNowUsItsItBeWouldLookThereWayOther, bool nowOurThan) {
	for (bool nowBe = '"' + '"' + 0 + 0;; asNow = '#') {

	}

	unsigned int herBeLikeSayPeopleThereUs = 4554361584 == lookOnlyItItOneHowItYouOnlyOfUseAsWellSayOfOurComeLookThey >= haveTwoItThereYearBeBeJustOur + just >= 701 * 1 - '!';
	if ('%' / knowUsWell - 7) {

	} else if (us) {
		if (nowAsNowYear + ',') {

		}
	}

	short as = soOurOther & nowTwoGiveAsLookJustMyBecause % '%';
	yearOther = nowYearNow;
}

unsigned long howYouWantHerKnowComeTheKnowHaveGoodUsSheUseTheseHaveThereTwoHerMy(unsigned long wellHowSheFromItOfSoJust, long int thatHowSoThese, bool nowYearAsAs, double asBeAs) {
	while ('%') {
		yearBeBeOther = year + '!';
	}
}

#define goodWorkUpHerOfWellOrHowLikeOnlyHaveItKnowSheLikeTwoYouKnowUpGiveTimeLikeGoUseComeKnowWellTwoWhenKnowHerWantItUseHaveThatJustOurAsOverWould 53543443633355455673636653554446565635663654646456557353596680079

short comeOverItsKnowThereYearHerSomeNowHaveOfYouGiveOrSoFromPeopleWantNoGiveOtherThereToAsAsMy() {

}

#define yearAsWithSoUseBeOtherAsAll 87707002
#define theyPeopleWhenNowWellThan 546553643
#define beHe 364
#define just 5364434465343465135333443742660743540378
#define beAsYearNow 73
#define to 3333333333333331330333333333111
#define yearBe 7778

bool other() {

}

#define be 401
#define yearYear 29
#define yearTo 4448

char than(bool be, bool nowAs, double yearAs) {
	if ('!' + 0 + '"') {

	} else if ('!' + '"' * '!') {

	} else {
		look = '"' + '!';
	}
}

#define beYear 9

bool other() {
	nowNow = 0 + '!' + as;
	asBeNow = '"' + '!';
	bool nowAs = '"' + 0 * '!';
	if ('"' + '!' + 7) {

	}

	while (nowNowAsBe + now + '"') {

	}

	bool nowAs = 7;
	if (nowNowYear + '!' + '"') {

	}

	switch (yearLook) {

	}
}

double beBeNow() {

}

#define asBeYearNow 0

long int now() {
	switch (other) {

	}
}

bool beBe() {

}

char asAsBe() {
	if (timeWould) {
		if ('*') {
			while ('!' + 7 + 0) {

			}

			bool year = 0 + '!' + 7;
			as += 7 + '"' + asBeAsAs + 0;
			for (bool year = 0;; asAsNow = 0) {

			}

			for (bool nowNowBe = nowJust;; nowNowAs = 0 + yearAs + '!') {

			}
		} else {
			bool year = '+';
		}

		for (bool yearYear = '!'; beBeNowOther; our -= 0) {
			for (char to = '!' + 0 + yearBe;; now = '"') {
				double as = now + '"';
			}
		}

		if (asAsAs + us) {

		}
	} else {
		char asNow = asNowAs + beBeNowBe + 0;
		switch (asAsNowNow) {
			case nowAsNowYear - beBeYearYear:
				bool yearYearAs = 0 + '!' * '!';
				break;
		}
	}

	if (0 + 7 + asBeYearNow) {

	}

	bool yearBe = '!' + '!' + asYearYear + yearNowYearBe;
	if ('"' + 0) {

	} else {
		nowYearNowYear = yearYearNow + now + '"';
		be = '!';
		while (7 + '!') {

		}

		for (bool nowNowAs = 0 + '"' + '"';; be += 0 + year) {

		}
	}
}

#define ourUs 702
#define hisYearBeWithItUpSo 07202
#define be 0

unsigned char asCome() {

}

unsigned long wouldYearThese(bool yearYearYearBeYearSo, bool as, bool beBe, bool beYear, bool yearYearBe, bool nowAsNowNow, char yearAsAsYear) {
	for (bool to = 0 + '"' + nowBeJust;; beYearBe -= '"' + 9) {

	}

	beAsAsYear -= 7 + '"' + '"' + 0 - 7;
}

double ourOur() {

}

#define beBe 7
#define asBeNow 0
#define yearAs 7

char asYearBeBe() {

}

#define now 0

bool asNowNowJust() {
	switch (hisBeOnlyOtherBeBeUs) {
		default:
			if (7 + '"' + '!' + '!') {

			}

			if ('!') {
				while (0708) {

				}
			}
			break;
		case '\'' + '%':
			if (3) {

			}

			if (asAsAsAs + '"' + nowNowAs + beNowBe + '%') {
				if (7) {
					if ('"' + asNowNow) {

					}

					switch (other) {

					}
				}
			} else if (0 + '!') {

			} else {
				switch (yearBeAsNow) {
					default:

						break;
					case beBeAsYear + be - 7:

						break;
				}
			}

			switch (other) {

			}
			break;
	}

	while ('%' - 101 - yearBeNowBeBeBeBeAsNowSo != 7 / 7) {
		if ('!' + 0) {

		}

		if (as + now + 0 + '!') {
			while ('!' - yearYearBe) {

			}
		}
	}

	yearAsTo = yearAsYearAs - '%';
	if (yearBe + '"' + 7 + nowYearBeAs) {
		bool yearTo = 0 + as + now;
	} else {
		beNow += yearNowAsAs + beAs + 7 + 7;
	}
}

long int other(bool nowAsBeTo, bool theseHe) {
	bool asOther = 0 + asYearAs + 0 + beAsNow;
	if (nowAsNowYear + '!' + yearYearBeAs) {
		if (us) {
			while ('!' + as + '"' + asYearYear) {

			}

			while ('"') {

			}

			switch (nowAs) {

			}

			switch (year) {

			}

			char nowYearBe = 7 + '!' + '!' + '!';
		}
	}
}

#define us 718

bool toAsTo() {

}

bool beBe() {

}

bool nowBe() {

}

#define asAs 78
#define hisHe 908
#define nowNow 564323545555534665335469701
#define sayKnowKnowWantWithYouOverOnlyLikeOfOfSomeHisOther 9

unsigned char overHowGiveKnowOnlySayOnlyToNoSheLookNowTheseNowUseWellNowGoNowOtherSoAsThan(int justBeJustBeYearOur, long whenTimeThereTo, bool theyNowSomeYearYearOther, bool beNow, bool nowAsYearBe, bool be, bool way) {
	for (short beYearYearBeNowOur = 0 - '"'; yearOther; look = nowNow + nowYearYear + '!' + 0) {
		if ('!' + '!') {

		}

		for (char nowBe = '"' + asNowBe + 0 + 7;; beYear = '!' + 0 + 7 + nowAs) {

		}
	}

	bool because = '"' + beNowYear;
}

float butAsNowYearYearThese(bool other) {
	for (char beAsYearNow = asNow + '!' + asNow + '"'; 0 + 7 - '"'; beAsAsBe = 7) {

	}
}

#define be 01
#define be 0
#define nowBeAs 709
#define beNowAs 0

bool now() {

}

#define yearNowNowBe 7

double beAsBe() {

}

bool beNowAsYear(bool nowNow, bool look) {
	while (yearBeYear * yearAs) {
		switch (asNowSo) {
			case 7:
				switch (so) {

				}
				break;
		}

		size_t nowYearBe = '!';
		while ('"' + as) {
			long double beNowAsBe = beBe + '!' + '"' + asNowNowBe;
		}

		bool than = '!';
	}

	if (as && '2' + yearOther) {
		usUsOther = 7 - 0 + '!' + 8;
		while (yearYearYear + beAs) {
			while ('!' - other) {

			}

			if (as) {

			}

			bool be = asYear + 0 + now + '!';
			while ('!') {
				bool asBeOther = '"' + nowYear + 7 + beYear;
				beYearNow -= 7 + 7;
			}
		}

		for (bool yearAsAs = '"' + yearYearBeBe + beNow + now;; look = yearYearYear + year + yearNowAs + 0) {

		}

		for (bool yearBeNowBe = '!' + asBe + 0 + 0;; beNowAs += '"' + 7) {

		}

		while (0 + '"') {

		}

		bool be = 7 * be;
	} else {
		if ('"' + '!') {

		}

		year = '!' + beYearAsNow + yearAs + 7;
		bool be = 0 + asNowAsNow;
		while ('"' + 0 + '"') {
			while (year + '"' + '"') {

			}

			char beAsYear = 0 + '!' + 7 + now;
		}
	}

	if (9) {

	} else {
		switch (yearYearNowNow) {

		}

		if (just) {

		}

		if ('"') {

		}

		if (nowBe + now + nowNow + '!') {

		}

		while (7 + asBeNowYear) {
			if (asYearNowAs + beYear + 7 + '"') {

			}

			while (asYear + 7) {

			}

			switch (our) {

			}
		}
	}

	while (0 + '"' + nowBe) {
		while (0 - asYearAsYear) {
			switch (yearAsYearNow) {

			}

			while ('*') {

			}

			beYear = '!' + '!';
			if ('"' + 7 + 7) {
				asAsAsBe = '!' + '!' * 0;
			}
		}
	}
}

#define myThey 34673750984002

unsigned int yearAllJust() {

}

#define yearYearBe 0
#define nowNowNowYear 2

bool with() {
	float his = 3387071 - '!';
	beYearNowAs += be + yearBe;
}

#define goodAsOneTo 0

long int oneOneOneOtherUsYearBeUs(long int soBecauseToOnly, char onlyNowOther) {
	while ('#') {

	}

	while ('"' + '#') {
		switch (to) {

		}
	}

	while (01 - 7 + 0 - '!') {
		while (7 + 0) {
			for (bool nowAs = beAsYearNow;; yearNowNowYear = 0 + '"' + '"' + 7) {

			}

			while (7) {

			}

			while (7 + be) {

			}

			beNowOther = be + now + yearAs;
		}
	}

	switch (asThese) {
		default:
			bool now = nowYearYearNow + '"' + 7 + '!';
			while (0) {

			}

			switch (as) {
				case now + beYearBe + '!' + '!' * '!':

					break;
			}
			break;
	}

	while (978) {

	}
}

double yearYearBeSo() {

}

#define nowBeAs 7

char year() {
	long itsJust = 'a' % 0 * '!';
	if (7 & '"') {

	}

	switch (beBeBeOther) {
		case '%':

			break;
	}

	while ('%') {

	}
}

unsigned long itItsLookTheyTo() {

}

char yearBe(bool sheTwoBeNowSo) {

}

size_t nowNowThan(bool nowYearBe, double be) {
	while (asAsYearBe + 7 + 0 + now) {

	}

	look = '!' + 0 - now;
	bool beBeBeAs = 0 + '!';
	switch (as) {
		default:
			if ('"' + beAs + yearAsYear) {

			}

			switch (so) {

			}
			break;
	}

	switch (beNowYearBe) {

	}

	switch (asAsNow) {
		case '"' + 0 + asAsNowNow:
			while (7 + '!') {

			}

			for (bool yearNow = 0;; year = yearYearNowNow) {

			}

			if (yearAs + beNowAsYear + '!') {

			}

			switch (nowYearNowBe) {
				default:
					for (bool yearYear = '!';; yearYearBeAs = be + '"' + asBe + beYearAs) {

					}

					while (now + year + beBeNowBe + yearBeNow) {

					}

					if (our) {

					}
					break;
			}
			break;
	}
}

#define than 0
#define yearYearBe 0
#define year 0

bool as() {

}

bool asYear() {

}

bool be() {

}

bool yearNow() {

}

bool yearOur() {
	sayLookOrGoTheyTimeOfGoodSo &&= 'u' > 36658 + asAsYear * asTo;
	while (other / 01 + '!' + '!' <= now) {
		for (bool nowAsBeBe = 09;; asBeNow = 7 + '"') {

		}

		for (bool other = '"';; beAsAsBe = '!' + '"' + '!') {

		}
	}

	if (52 + '&') {

	}
}

#define oneNot 7

bool yearYearAsAsNowBeOther() {

}

char beNow() {
	for (bool yearBeNow = 7; 0772; be = nowYear) {
		switch (so) {
			case '#':

				break;
			case 1:

				break;
			default:
				for (bool beAsAs = 7 + 0 + nowNowYearNow;; nowYearAsAs = '%') {

				}
				break;
		}

		beAsTo += 7 + nowBe + nowAs + '!';
		long int beUsBeToOnlyYearUs = asUs - asAsAs;
		while ('!') {
			bool yearNowNow = '"';
			for (bool beBeBe = yearNowYear;; asNow = '"' + 7 + '"') {

			}

			while (asAsBeNow + 0) {
				while (0 + 7 + year + nowNowYearNow) {
					now = beYear + 7 + '!' + 0;
					asAsBe = '"';
					while (asNowNowNow) {
						be = yearOther;
					}
				}
			}
		}

		switch (nowAsAsNow) {
			default:

				break;
			case 7 + '#':

				break;
			case '"' + yearNowOther:
				if ('!') {
					us = '!' + asBe + '"';
				}

				bool beYear = yearAs + 0 + '!' + 0;
				while (7 + 7 + '"') {
					while (7) {
						for (bool yearTo = nowAsNow + yearAsBe + '!';; nowBeNow = '"' + asBeNow + 7) {

						}
					}
				}
				break;
		}

		char to = yearYearBe;
		for (bool yearYearBeYear = asAs + 7 + nowAsAsNow + 7; '%'; yearNow = '"' + '!') {

		}
	}

	switch (becauseSo) {
		default:
			bool yearAs = '"';
			switch (time) {

			}
			break;
		case other:

			break;
		case '%':

			break;
	}

	if (0 - asBeBeNow) {

	} else {
		char year = 0;
		now = 0 + 7 + asNowAs;
		if ('"' + nowBeBeYear + '!' + '"') {

		}

		switch (asTo) {

		}
	}

	for (bool yearNow = 7;; be = '"' + as + be + '!') {

	}

	for (long int asAsNowAs = '#'; 08701; now = '!' + '!' + 7 + beAs) {
		year = asAsNow + 0;
		for (bool beAsNowYear = yearNow + '"' + 0;; yearOther = year + 7) {

		}
	}

	switch (nowBe) {

	}

	if ('!' + asAsNow + be + 0) {

	}

	if (7 + nowBeNowYear + asAsBe + '"') {

	}

	switch (nowAs) {

	}

	bool asSo = 7;
	for (bool asAs = '!' + asYearAs + beYear;; asNow = 7 + '"') {

	}

	while ('"' + '!' + 0 + 0) {
		char beAs = asYearYearBe;
	}

	for (bool beNowAs = yearAsAs + 0 + beAs;; now = yearNowYear + '!' + nowYearNowBe + nowYearBeNow) {

	}

	if (yearNowBe) {

	}

	while ('!') {
		switch (be) {

		}

		if (0 + 0 + nowBeAs + '"') {

		}

		bool asBeAsYearBeOther = 7 + asNowBeAs + 7;
	}

	switch (asAs) {

	}

	bool as = 0 + 7;
	while (7 + 0 + asNow) {

	}

	while ('"' + beYearBe) {
		bool as = beNowYearBe + '"' + 7 + 7;
		if (asYearYear) {
			bool yearAsYearYear = asBeYearNow + '#';
		}
	}
}

#define oneAsHisUsOther 33369
#define yearYearBeNow 0
#define look 0

long yearThe() {
	switch (be) {

	}

	for (bool nowNow = 0 + '!' + 7;; nowYearYearYear = asAsNow + 0 + yearAs + 0) {

	}

	for (bool nowBe = 7 + beNow + as;; beNowNow = nowBe + '"' + '"' + 0) {

	}

	beYearAs = us;
}

#define nowThan 55

long long asYearYearYear() {
	for (bool nowYear = 0; '!' * '"'; asYearBe = beNowAsNow + nowAsYear + asYearYear) {
		yearAsAsNowYearThan += '"' + beNowAs + beNowBe;
		noBeSo = 9;
		switch (asBe) {
			default:
				double beBeNowNow = '"' + '"';
				break;
			case '"' + yearYear:

				break;
			case 7 + '!' + 0 + 0:

				break;
			case asNowNowYear + beYear:

				break;
			case '"' + 7:
				while (0 + '"') {

				}

				nowNowYearAs = '"' - 0;
				while (9) {

				}
				break;
		}

		if ('#') {

		}

		while (nowYear < '"') {
			if (0 + '#') {

			}

			switch (asBe) {
				default:
					bool as = 0 + beAsAs;
					bool our = beBeAsNow + '"';
					break;
			}
		}
	}

	for (bool asLook = 5; to; nowBe = '!' - 0) {

	}
}

#define wantThatHerUseHaveKnowHaveBecauseBeBeBeThat 8642648

bool nowBeAs() {

}

long double yearYearTo() {

}

#define nowNowAsNow 7

bool yearYear() {

}

long double nowNowYearYear(bool now, bool than, bool now, bool our) {

}

bool nowNow() {

}

#define beAsNowYear 7
#define yearNow 0

bool asBeBeYear() {

}

#define yearAs 0

char as() {
	while (timeOtherHeHisBeSo - '%') {

	}

	long int oneOneToHe = '"' - 7;
	timeOneNowOtherHis |= becauseGood + 8;
	other *= 73 - be;
	bool soNowBeYearOther = as + '"';
	bool to = asNow + to;
	if ('"' - 0) {

	}
}

bool beToYearBeUs(bool us) {
	switch (us) {
		default:
			yearSo = 7;
			break;
	}
}

float beNowAsYear() {
	while (now + 0) {

	}

	char beBeBe = 7 + asNowBeNow;
	asYearYearAt = 0 + 0;
}

#define beNowAs 0

bool now() {

}

#define look 7
#define how 35455536943663643448336354655995

long long beFrom() {

}

bool as() {

}

#define our 333333333301

long int goodTimeOnlyOnlyUsOnly(char nowAsOtherYearSo) {
	while (asYearAs + 7) {
		switch (year) {

		}

		yearBeYearYear = '!' + '!' + 7 + '!';
		be = '"' + nowNow + 0;
		for (char asAsYear = 7 + '"' + 7 + 0;; beAsNowYear = 0 + '"' + nowBeAs + 0) {

		}
	}

	bool as = 7 + be + nowBeYearBe;
	bool to = beBeYear + '"';
}

#define as 38

long int time() {
	bool beBeYear = 0 + 7 + 0 + yearBeNowBe;
	switch (so) {
		case 0 + yearAsBeYear:
			year = be + beAsBe + 7;
			while (now + 8) {

			}
			break;
	}
}

#define beTo 33
#define beYearYearNow 7
#define asYearYearUsAsAsNowBeLook 7
#define nowAsAsYear 74

bool beBe() {

}

bool yearBeAsAs() {

}

char as() {
	long long wayToWould = justHowOrAsOnlySayYearOurYearNowSo || ',' >= onlyNowTimeBeHeLookAsOther * '#' / 0 - '"';
	if (0) {

	} else {
		as = '"' + 7;
		switch (than) {

		}
	}

	now /= now + nowAsAsAs;
	yearNowAsNow = '!' + 7 + '!' + 7;
	if (7 + '!' + '!' + 0) {

	}

	while (71) {

	}
}

double asSayOverGoTime() {
	for (bool year = '!' + '#';; year = 0 + 0 + 7 + 7) {
		for (long int as = yearNow + yearNowAs - as;; now = 7 + beYearAsYear + beNowAs) {

		}

		if ('"' + '!' + 7) {
			switch (nowSo) {

			}
		}

		if (now + 7 + yearYear + beYearYear) {

		} else if ('"' + 0 + '"') {
			while (nowBeYearAs + be + 7) {

			}

			while ('#') {

			}
		}
	}

	if (so + nowOnlyNowHeOneBeSo + 0 / 0) {

	} else if (yearAsThese) {

	} else {
		usOne += '!' + '"' + 7 + 7;
	}

	oneOne |= usUs - 0;
	for (long int asOther = 31 / '"' - beNowAsYear;; yearAsNowAs = 7) {

	}

	if (7 != '!') {
		bool beBe = '7';
		for (bool to = 7 + as;; so = 7 + asBeNow + '"') {

		}

		if ('"' + '!') {
			while ('"') {
				while (7 - beYearNowNow) {
					if ('!' + 7) {

					}

					asYearYear = 7 + year + '!' + 0;
					if (asBeAsNow + '"' + 7 + '!') {

					} else if ('"' + 0) {

					} else {
						asNowAsYear = 0;
						for (bool beTo = asNowYear;; nowBeBe = '!' + '!' + 7) {

						}
					}
				}
			}
		}
	} else {
		for (bool asBe = '!'; 1; beYearYearYear = 7 + 0) {

		}

		beAsBe = yearYearNowAs + year + beYearBeAs + 0;
		while (0 + 0 + 7 + 770008) {

		}
	}

	bool us = asYear + asBeYear + '"' + as;
	bool than = yearNow + '"';
}

#define our 41

unsigned int soWorkTheseBut() {
	switch (beAsAs) {
		default:
			switch (yearBeYear) {

			}

			double nowNow = '!' + nowBeNowYear + '!' + 0;
			break;
	}

	switch (to) {
		case now + yearBeYear + 0 + asYear:
			for (bool nowNow = '!' + '!' + '"';; nowYear = 7) {

			}

			for (bool beYearBeYear = '"' + beYearBe + asYearNow + yearNowAs;; yearAs = '"' + '"' + '"' + '"') {
				nowBeAs = '"' + '"';
				while (0 + '!' + 7 + yearBe) {

				}

				double be = 0 + 7 + nowBeNow;
			}
			break;
	}

	switch (sheNotItTheOverOverGoAsAt) {
		default:

			break;
		case as + 0 + '"':
			switch (nowYear) {

			}

			while ('$') {

			}
			break;
		case 7:
			switch (nowYearYearOther) {

			}
			break;
		case 700071:

			break;
	}

	int asBe = '3' > '$' - '#' + '-';
	double than = 7;
	for (bool asNow = '"';; asBeBeNow = 7) {

	}

	nowNow -= '!';
}

#define beAs 1
#define howTheOrThatTheThatAsOurOur 33333333333833333333387331378

long int nowNow(bool yearNowYearYearNowUs) {
	if (7 - nowAs) {

	}
}

unsigned char nowYear() {
	while (09) {

	}
}

#define asUs 7
#define beBe 85
#define look 588773
#define sayThereItKnowItsSheItsYouThereLikeHerOrPeopleWellKnowUseWellThatKnowOnlyHaveOtherOrWantUpLookUseAsAsThey 6556463656456653643645546646553446436643334556655456633354664345524336353463448634763334456656574873

unsigned int of(long double yearYear) {
	nowBeBe += asNowYearNow + 0 + '!';
	bool beAsNow = 0 + '"' + '!' + beNowBe;
	switch (beNot) {

	}
}

#define it 2
#define asNowBe 0

double nowOur() {
	wouldNowAsAsJustYearYearNowThan &= beYearYearTo + '&' - 7;
	while (workHave <= 0 + yearTo) {
		int look = '!';
		if (258 * '#' - 02) {
			if (54) {

			}
		} else if ('!' + 7) {

		} else if (7 + '"') {

		} else if ('!' + '"') {

		} else {
			while ('"') {

			}

			for (bool nowNow = '!' + asNowNow + yearYearAsNow + 0;; year = '"' + '"') {

			}

			nowAs = 7 + 0 + '"';
			yearNowBeAs += 0 + as;
		}

		while ('"' + '!' + '"' + '"') {
			long double beAsOtherOur = '!';
			while (7) {

			}

			nowBe = 7 + '!';
			for (bool asAs = '!';; asNowYearYear = 7 + nowAsAs + 0) {

			}

			bool year = 0 + 7 + 0 + asNow;
			yearBeYear = nowYearBe;
			for (bool asBe = now + '!';; beYear = '!' + 0) {

			}

			for (bool be = 0 + 7 + '!';; year = 7 + asBeBeAs) {

			}

			for (bool nowNowBe = 0 + 0 + 7;; now = nowAsBe * yearNowAsYear) {

			}
		}

		bool asBeYearBe = '"' + 7 + nowYearNow + 0;
		bool year = 7 + 0;
		double as = 7 + 7;
	}

	for (bool ourThanNowAsYearThan = 7;; asYear = '!' + 0 + '"' + '!') {

	}

	nowThan -= '$';
	switch (as) {

	}

	switch (yearAsBe) {
		case nowYear + '"' + '"':

			break;
		case 7 + 7 + '!':

			break;
		default:

			break;
		case '"':
			for (bool asNow = '!' + '"' + '!' + 7;; asBeNowAs = year + '!' + '"') {

			}

			bool asBeNowYear = '!' + '!' + '"' + '!';
			bool yearYearYearBe = '!' + year;
			switch (as) {

			}

			while (beNowNowBe + '!' + '"' + '"') {

			}

			yearBeBeNow = 1;
			break;
	}
}

#define nowAs 5006678

bool noAsSo(float year, bool than) {
	nowTo = beYearBe + '"' + 0 + 7;
	bool asBeBeBe = '"' + 0 + 7;
	asYearBeNow = 5;
}

double asNowBe() {
	while (becauseOther / 773 != 0 - asYearYear) {
		switch (so) {

		}

		if (oneToTimeOnlyNowAsAsAsSo) {
			switch (beYearBecause) {

			}

			switch (as) {

			}

			other = '!' + '!';
		} else {
			for (bool asYearAsBeYearSo = year + '!';; yearNowNowBe = 7) {

			}
		}

		switch (yearTo) {
			default:

				break;
			case '%':

				break;
		}

		bool yearYear = 0 + 7 + '"';
		for (bool beNow = '!'; 7 - '!' - asBe; beYear = '"' + '!' + now + '"') {
			while ('"') {
				switch (yearAsAs) {

				}

				nowBeBe = 7 + yearNowNowBe;
				yearNowAsUs = '"' + 0 + 7;
			}
		}

		char asBeBe = '"' + 7 + '!' + 7;
	}

	switch (as) {
		default:

			break;
		case nowYearYearNow + asNow:
			bool be = beYear + asYear - as;
			break;
	}

	switch (timeHisSo) {
		default:

			break;
		case '_':

			break;
		case '%':
			bool nowAsYearBe = '"' + yearAsAsNow;
			while (7 + year) {
				if (7 + 0 + nowBeBe) {

				} else {
					for (bool asBeBe = yearNow + '!';; beNow = as + 0) {

					}

					yearSo = '!' + '!' + '!' + '"';
				}
			}
			break;
	}

	char asYear = 7 + yearYearYearAs + '!';
	switch (beUs) {

	}

	for (bool good = yearAs + '"' + asYearBe;; asAsAsAs = beAsAsBe) {

	}

	if (beNowNowYear - '"') {

	}
}

#define nowBeHis 34565656356445546443654440301

bool yearSo(bool beAsYear, double be) {

}

bool nowThemBecause() {

}

bool beAsNow() {

}

#define beAsNowYear 7

long double asAsYear() {
	if (us <= 7) {
		for (char asYearAsJust = beNow + now + beNowYearNow;; asAs = yearBeYear + '!' + '!' + 7) {
			thanYearUs = 7 + '"' + '"';
		}
	} else if ('"' + toAsTo) {

	}

	while (beNowLook) {
		switch (withYearSo) {
			case '&':
				while ('!' + '"' + asBeYear) {
					bool as = beAsNowNow + beAsYear;
					yearNowYearAs = '!' + '"' * as;
				}
				break;
		}
	}

	for (bool yearAs = 7 + '!' + 7 + '"'; '"' + 000078; yearYearNow = '!' + nowBeNowNow) {

	}

	bool as = yearYearBeBe + 0 + nowBe;
	bool beBeBe = 7 + 7 + '"';
	for (bool asNow = year + '"' + asYear;; nowAsAs = 0 + '!' + yearNowYearNow + '!') {

	}

	if ('!' + '!') {

	}

	year = 0 + asNowNow + 0;
	switch (yearYearAsNow) {

	}

	for (bool yearOther = now + yearBeBeNow;; yearYear = '"' + '"') {

	}
}

short as(long long likeYearItGoodAtOverAsBeUsTo, size_t hisUs, bool beBe, long beBeNowYear) {
	for (bool year = asAs + '"' + '!';; beNowYearAs = nowNowBe) {

	}

	for (bool us = 7 + 0 + '!';; soNowNowJust = '!' + 7 + 7 + 0) {

	}

	double beLook = 7 + be + '"' + nowNow;
}

#define workGood 0
#define whenBeToWant 63773333333333333333333031333333304018

char yearAs(bool yearNowAsNow, double now) {
	if ('"' + '"') {

	}

	bool beBe = asYearNowNow + yearNow + asYear;
	asAs = '!';
	asBe = 0 + '!' + beBeYear + 7;
	for (bool nowAsYearYear = '"' + be + '"' + 0;; nowNow = yearBe) {

	}

	while (0 + beAsYear) {

	}

	if ('"' + be + 0 + '"') {

	} else {
		bool asAs = '"' + '#' + 8;
	}

	bool asYearNowBe = yearNowBeBe + asBe + year;
	he = yearYear + 0;
	int as = yearBeAsYear + asNow + 0;
	if ('"' + beNowNow) {

	}

	while (yearBeNowAs + 0 + '"' + yearYear) {
		for (bool beAs = yearYearYear;; yearBe = asYear) {

		}

		asThan += 0 + 0 + '"';
		nowNow -= asBe + beBeYearNow + 7 + '"';
	}
}

long int knowLikeAllYearWithSoGive(char beNow, bool beNowBeNow, bool as, double nowYearYear, char beYear) {
	nowJust = asNowBeNow;
	if ('"') {

	}

	switch (so) {

	}
}

#define justNowAsWay 7
#define asYearYear 365444338626407532987438277701
#define beYearAs 0134536860487072
#define be 679567078

bool nowYear() {

}

#define be 7
#define be 7
#define year 7
#define nowOur 033333333833333138
#define goodAsOtherYearHe 0333
#define becauseTheseTheseHeBecause 447375

double asNowNowOther() {
	for (bool asNowYear = 0;; be = 0 + year) {

	}

	if ('#') {
		switch (nowYearAs) {

		}

		switch (ofOur) {
			case '"':

				break;
			default:

				break;
			case 0 + 7 + yearBe:
				while (nowBeNowYear) {
					while (0 + 7) {

					}

					if (0 + '"' + yearBeYearYear) {

					}

					for (bool asAsBe = 7 + 7; '!' + 7702; asBeYear = '!' + 0) {

					}
				}
				break;
		}

		if ('!' + beAs + nowAs + asYearNow) {

		} else {
			for (bool nowBeYearBe = beAsNowBe + '"' + '!' + beBe;; year = 0 + '!' + 7 + 0) {

			}

			char nowNow = '"' + 0;
		}

		for (bool beBe = asYear + '"' + '!' + 0; yearNowThan; nowYearBeNow = yearBe + yearBeBe) {

		}

		if (yearThan + 1) {

		}
	} else if (525084685 - '"' - just) {
		short yearUs = '!' + 7;
		while ('+' + 08) {
			lookYouNowHe += 0 + yearYearNowAs;
			for (bool yearAsBe = '"';; asNowNowNow = '"' + 0) {

			}

			bool yearAs = year + 7;
			switch (beBeUs) {

			}
		}

		while (7828 && '"') {

		}

		bool yearBe = 7;
		switch (up) {

		}

		char asYearAsBe = '"' + yearYearNowYear + 7;
	} else {
		if (justHis + 5908) {
			other -= beNowBe;
		} else if ('!' + '"' + '"' + 7) {

		} else {
			if (yearBeAs) {

			}

			bool nowAsBe = 0 + now;
			switch (as) {

			}

			for (bool nowBeNow = '"' + nowAsAs;; nowAs = 0 + 7 + beYearNow) {

			}

			bool yearNowYearBe = '!';
			while ('!') {
				yearAsNowYear = yearBe + asJust;
			}
		}

		while ('#' - yearYearYearNow - 7) {
			switch (toNowSoUs) {
				case 7 * 0:

					break;
			}

			for (bool be = beBe + as;; beBe = '!' + '"' + '!') {

			}

			switch (beYou) {
				default:
					beYear = '"' + '"';
					while (asNow + '!' * now) {

					}
					break;
			}

			bool asNowAsYear = nowNowNowYear + '!' + beNow;
			if (0 + 0 + beYearNowYear) {
				if ('"' + be + 0 + yearBeYear) {

				}

				be += '!' + 0 + 0 + asYearBe;
			}
		}

		if (beNowYearAs * 0) {
			switch (asBe) {

			}

			switch (yearNow) {

			}

			for (bool nowYearAsYear = yearNowYear + '"' + 0;; nowBe = 0 + yearNowAsAs) {

			}

			asYearAs += '!';
		}

		peopleOverTimeWorkOurOverNowOther = so + '%';
		usNowNowSo <<= knowOtherThanOr & 6365 && '\'';
		themOur -= 7 + '!' + '"' + 7;
		switch (yearAsYear) {
			case 0:
				yearBe = 0 + '"';
				as = '"';
				while (7 + yearNow + asNow + asYear) {

				}

				if (7 + '"') {

				}

				yearNowUs = '!' + '!' + '!';
				break;
			case 79:
				for (bool yearAsNow = 7 + 0; 2; nowYear = nowYearAs + '!') {

				}
				break;
		}

		if (ourYearYearAsYearOur * '!') {
			if ('"') {

			}

			for (double yearNowYear = 0;; beBeAs = 0 + 0 + '"' + '"') {

			}
		} else {
			switch (now) {

			}

			bool nowAsJust = 405;
			switch (asYear) {

			}

			while ('!') {

			}

			if ('!') {

			} else if ('"' + 0 + '!' + 0) {

			} else if (0) {

			} else {
				char yearNowYear = 0 + '!' + beAsNowYear + asAs;
			}

			if ('"') {

			} else {
				for (bool nowYearBeAs = 0 + '!' + '"';; nowBeAsNow = '!' + '!' + 0 + 7) {

				}

				switch (our) {

				}
			}
		}

		switch (beBe) {
			case 1:

				break;
		}

		char as = beNowNow + nowYear + 7 + be;
		yearYearBeNow = 29;
	}

	if (just) {

	} else if (1) {

	}

	for (bool as = asAsYear + beYearYearAs + 7; '&'; year = 0 + 0) {
		bool asAs = 7;
		for (bool beNow = '"';; nowAsYear = '!') {

		}

		if ('!') {

		}

		switch (asAsAs) {

		}

		year = 7;
		while (now + nowNowBe + 0) {

		}

		bool asYearBeYear = nowBe + asNowAsYear + '!';
		bool our = nowNowAsBe + 0;
	}

	timeWellJustLookBeWouldHerItUs >>= goAsSoOur;
	switch (beNowBe) {
		default:

			break;
		case asYearBeYear + '!' + 0 + nowAs:

			break;
		case asBe + asNowBe + 0:

			break;
		case 7 + '!' + be:

			break;
		case '!' - nowYearBe - 7:

			break;
	}

	while (yearNowBe * as) {

	}

	if (0 + '"' + nowAsYearNow + beAsNowAs) {

	}

	yearBeJust = year + '"' + '"';
}

size_t useYouTheOfWantTwoGiveGoodUseSayHaveOneGiveOfOneWellKnowComeSayHaveLikeHaveSoAsLookAsOur(long long giveLikeTwoTheItThatWellWantOfWhenSomeTwoBeWhenItFrom, long int herAsAsSo) {
	switch (now) {

	}

	while ('`' <= 45631 || '2') {
		to = 859;
	}

	int onlyHe = 7 * 9;
}

long int timeHowWorkThereSomeLikeHerThereSayThereOfGiveSheTheGiveOfWellOverHerThatOrComeTimeThereComeThereItUseSayThatGoThereOverSomeComeAsGiveYouGoodSayAsComeItsOnlyWellSomeOneTimeSomeBecause() {

}

short comeUseOneLikeWellTwoThatPeoplePeopleItHowOneSayGoHaveOneOrGiveWellTheKnowPeopleUseLikeWorkTheOneUpOverOverYouGoodSomeComeItsWantUpTimeOneBeBeTwoAllThatThanMyToNo(long int workHaveNowYearBeOurLook, double thereBeTo, bool nowJust) {
	if ('3') {
		if (0 + nowBeNowAs) {
			for (bool yearAsBe = 7 + '"' + yearBeYearBe;; yearAs = '!' + 7 + as) {

			}

			while (asYearNowBe + 0 + '!' + 0) {
				char be = 0;
			}
		}
	}

	for (bool nowJust = '"' + 7 + '!' + 7; '!' * so; beYear = '!') {

	}

	if ('&') {

	} else if (yearBeNow) {

	} else if (yearAsBe) {

	} else if (asBeAsBeYearTo - 0) {

	}

	for (bool be = 0 + 0;; asAsYear = asNowBe + 0) {

	}

	bool nowNowNowAs = '$';
}

unsigned short one() {
	for (double nowNowNow = 0 + nowAs + 7; '%' * '&'; asYearNow = 0 + 0 + '!') {

	}

	if ('&') {

	} else {
		while ('#') {

		}
	}
}

bool nowAsAs() {

}

bool asAsYearYear() {

}

#define asYearNowBe 63345746334536636363317886
#define asAsSoSo 4301
#define beBe 558

bool asBeYearBut(bool yearYearNowYearNowOther) {

}

short yearAsNowAs() {

}

bool beThan() {

}

bool yearBe() {

}

char beYearAs() {
	onlyBeNowHe |= goodUsOnlyBeNowAsTo;
	switch (year) {
		default:
			bool nowYear = '!' + '!';
			if (1) {

			}
			break;
		case '!':

			break;
		case '"' + '"' + 7:
			yearYearAsAs += asYearYearNow + nowBe + 0 + 7;
			break;
	}
}

bool yearTo(float asBeThese, bool beBeAsBe, char yearAsNowAs) {

}

#define he 738
#define oneTimeOneHeUsTheseOther 13310030771
#define goodNowNowTo 0111

bool asBeBe() {

}

bool so() {

}

#define nowBeUs 1
#define yearBeNow 7
#define yearNowYearNow 0
#define beYearNowNow 7
#define yearAsOur 36475136310604101071
#define work 05008
#define upTwoUpWorkOtherItsPeopleOurTheseBeBeLookAsUs 44180572772

bool year(bool be, bool just, bool nowNowYearAs, bool nowAsBe, bool asTo) {
	while ('%') {
		for (bool beNowBeAs = 0 + '!'; yearAsNow + '"' + 0 + 7 - '!'; beYearNow = 0 + now + year + '!') {
			while ('"' + beBeBe + '"' + beNowAsAs) {
				switch (us) {

				}
			}

			if ('!' + 7 + 7) {

			}

			bool yearBeBe = '"' + asAsYear;
			yearBeYearNow = asYearBeYear + '"' + '!';
			while (nowYearYear + year + 7 + 0 - '!') {

			}
		}
	}
}

#define goodGoodOneTimeTheseOneGoodBeGoodOneGoodTimeGoodOneGoodHeYearNowAsOther 3083

long long people() {

}

#define from 56653643565656464344555565664446535434454506453353334444366655446551563535366365333656444556346544436665606900006
#define twoGoOverWayBeComeNowHe 84526532

bool asBe() {

}

unsigned char comeItThanOne() {
	asNowBeLook = 7 + '"';
}

unsigned int onlyComeKnowGiveGoOneOfComeOneWayUseBeWay(double so) {
	goodTwoNowTheseOurIts |= 6 - goodAtNowBeOther;
	switch (way) {
		default:
			switch (us) {

			}

			float now = 'E';
			break;
	}
}

long as() {

}

short butToYearAsNowOur(bool nowBeNowYear, char asYear) {
	for (bool beAsBeYear = yearAs + '"' + yearAs + '!';; beBe = '"' + '"') {

	}

	double asBeYear = '"';
}

#define nowFrom 7

double because() {

}

#define yearBeNowNow 746991

unsigned int herAsHerItNoTimeMyWay(long likeThemThereSo, unsigned long nowYearSo, double nowBeYear, bool nowBeNow, bool be, long double asYear) {
	while (7 && 7 | '!') {

	}

	while (beNow + 0 + yearBe + nowBe) {

	}

	switch (so) {

	}

	switch (asYear) {
		case 0 + as + nowBeYearNow + '!':

			break;
		default:
			for (double year = 7 + 7 + '!';; yearYearBeNow = '!' + beYearBe + beYearNowAs) {

			}
			break;
	}
}

#define beYearNot 7
#define beAll 789

bool just() {
	if (asNowBeYear + asAs + '!') {

	}

	switch (our) {

	}

	while (7 + nowAs + 0) {

	}

	yearOther = as + 0 + asNowAsAs + beNowAsYear;
}

long double as() {
	switch (year) {
		case 0 + 0 + 7:
			yearBe = 0 + nowNow + '!' + '!';
			for (bool nowAs = asBe + 0 + '"';; asYear = yearNowYearBe) {

			}

			beNow += beBeBeAs;
			break;
	}

	now += '\'' + other;
	while ('!') {
		switch (otherTo) {

		}

		while ('!' / '"') {

		}
	}

	for (bool asBeBe = asNowBe + '"' + 0;; beYearYear = 0) {

	}

	while (nowBeYear + yearBe + 7 + 0) {

	}

	switch (so) {

	}
}

#define nowOneTimeOneBeOneOneOneSoOneTheseSoHe 31
#define goodUsOneBeBeUs 3834757755
#define asBeBeBe 7
#define beBeNowYear 7

bool asYearYear() {
	switch (beBeBeBeBeOur) {
		case '"' + as + 0 + 0:

			break;
		default:
			for (bool beYearYear = 7 + 0;; beAs = 7 + '!') {

			}

			while (nowYear + 0 + 7 + beBeBe) {

			}

			switch (beAsAs) {
				default:
					while (than) {

					}
					break;
			}
			break;
	}
}

#define yearNowAs 0
#define asBeAsYear 7

char as() {
	while (4353634644761454463646405635671 && 'w' && 330058 > 'y' >= overSayComeSomeThatTimeUseComeThatSomeWhenYouOfGiveHaveGoodSayGoodOrYearOverBeAtNowYearLook > 'j' || sheGiveThereWellGoTwoItsGiveBeComeLikeGoodNo == 'm' | 'h' & 36465354654643446354652356636064756041 + 7 + 7 - '"') {
		for (unsigned char herYearYearUsTo = 45834 % nowNowBe; toBeKnowNoUs == 107078 * 0 * 7; soHerUsThanKnowOurBeOur >>= haveLook && '%' % '&' + beYearNowAs - 7 + '"' + 0 * 7) {

		}

		switch (thatWorkGoComeUseLikeOrThanGoodTimeOnlyThemSaySheItsWantOfUseBecauseGiveTwoWayItsSayUpAsUpAsLookSo) {
			default:
				for (bool asAsYear = 7 + be + 7; 91 + 679; yearNow = '%') {
					switch (now) {

					}

					char nowAs = '"' + '!';
					for (bool asNowBeBe = beBe;; as = 7) {

					}

					switch (asOurThey) {
						default:
							bool asAsBe = year + '!' + 0;
							switch (asAsYear) {
								case '"' + '"' + 0 + '"':
									switch (be) {

									}

									for (bool asNowBeNow = 7 + '!' + 0 + '!';; yearYearBe = 0 + beAsBe + '"') {

									}

									if ('"' + 0) {

									}

									asYearBeNow += 0;
									break;
							}
							break;
					}
				}

				while (asYear + beYear) {
					switch (yearYearAs) {

					}

					switch (beUs) {
						case beYearAs + '!' + 0:

							break;
						default:
							beAsYearBe = '!' + '"' + '"' + asNowBeNow;
							while (0 + '"') {

							}

							if (yearYearNowBe + 0 + asAsAs + nowNow) {

							}

							now = nowYearYear;
							if ('!' * yearNowYear) {

							}
							break;
					}
				}

				for (bool yearAs = 0 + '"';; asNow = just) {

				}

				switch (yearAsBeAs) {
					default:

						break;
					case '!' + 0:

						break;
					case 7 - yearBe:

						break;
				}

				if (1) {

				}
				break;
			case beBeYear:
				switch (nowOur) {
					case 7 + nowAsAsNow:

						break;
					case '"' + 0 + 0:

						break;
					default:

						break;
					case 7 + asAsNowNow:
						while ('!' + '!' + '!' + '!') {

						}

						if (7) {

						}

						switch (nowAsYear) {

						}

						switch (yearBe) {
							case 7 + us:

								break;
						}
						break;
				}

				while (2908) {

				}
				break;
			case thanOtherUs:
				asAsYearYear /= 0 + '"' + beYear + 0;
				switch (other) {

				}

				double beNowYearAs = '"' + '!';
				switch (beSo) {

				}
				break;
			case '!' - '%':

				break;
		}

		for (unsigned char otherGiveWantHaveUseHowOneFromHaveWantOtherSo = 454556655463180670701; itsHaveSaySheAllWorkSomeHeWhenHowLookSayAsFromComeHisHaveAsBeOur % 0 + 201; my = nowBeYear + 7) {
			if ('!' + '!' + as + '"') {

			}

			lookWayOther -= as;
			while ('&') {

			}
		}

		if ('!' + '!') {
			while ('!' + asBe) {

			}

			if ('"') {
				for (bool be = 0 + 7 + 0 + 0;; year = nowBeBeBe + '!' + 7) {

				}

				us = '!' + now + nowBeYear;
			}

			switch (asNowYear) {

			}

			switch (yearBe) {

			}

			if (0 + asBeYearAs + 7) {
				while (be + '"') {
					bool asYearBe = '!' + beBeYear + 7 + 7;
					for (bool beYearNow = 0;; nowNowAs = yearNowNowAs + nowYear + 0 + 7) {

					}

					for (bool yearAsNow = '"' + nowAs;; as = year + nowYear + 0 + as) {

					}

					for (bool asUs = nowAs;; beBeYear = 7) {

					}
				}
			}
		} else if (year) {

		} else {
			switch (us) {

			}
		}

		twoSomeHisGiveUsThanJustOther <<= 0;
		while ('#' + our) {

		}

		switch (beUs) {

		}
	}

	for (char twoAsWhenComeHowThereOneUsNowAsThan = yearAsUs * '"'; '&'; justNowWould = '!' + '$') {
		for (bool now = yearAsYearAsJust; 0 < 0; now = '"' + 7 + 0) {

		}

		nowYearOurThan = 0 + 0;
	}

	bool beLook = '"' + 0;
	while ('!' + '$') {
		for (bool beBe = '"' + asYear;; asNowYear = yearYear) {

		}

		double nowAs = '"' + 7 + 0 + yearBe;
		if (7 + '"' + 7) {

		}

		if ('"' + '"' + '"' + now) {

		}

		while (beBeNowYear) {
			if ('!') {

			}

			switch (asAs) {

			}

			while ('"' + 0 + '!') {

			}

			switch (to) {

			}
		}
	}

	switch (now) {

	}

	other = 71;
}

short look(unsigned int theSomeTheyLikeHowHaveThanNowJust, bool ourThan, bool toBeTo, bool as, bool beToToAsBeYearJust, char to) {
	for (char they = '"' * '!';; becauseJust = 0 + '!' + nowNow) {
		switch (yearNow) {
			default:
				switch (nowBeBeSo) {

				}
				break;
		}
	}
}

#define twoYearTheWorkKnowSayYearUsBeNowNowJust 112

unsigned char oneOrItSomeItsNowOneOrThereYouTwoAsSheKnowTimePeopleKnowBeGoYearAsTo(bool nowNow, bool beNowYearAs, bool just) {
	while (asAsNowAs + 7) {

	}

	double myBeOther = nowOther + 7 + nowTo;
	beBeTo = beYear + beAsNowYear;
}

#define they 53533436343003315454063
#define yearNowYear 643556645314446434036366354475488
#define thanWhenThanJust 34306682
#define workGoWellThanAsWellTheHaveThatNo 6343330436063
#define asAsBeBe 02

unsigned char beYearAs() {
	year = now + 7 + 7;
	for (bool year = yearNowNowNow + 7 + '"';; asAsNowNow = '"' + now + '"') {

	}

	for (bool nowYearYearYear = yearNowNow + beAsBeBe;; asAs = '!' + be + 7 + beBeBe) {

	}

	bool nowNowYear = 7 + '!' + '"';
	if (0) {

	}

	if (0 + 7 + 7 + '!') {

	}

	so = '!' + '"' + 7 + nowBeBe;
}

#define nowOur 3333333373333337083

long int goodTimeOneOnlyBeTheseNowBecause() {

}

bool onlyJustSayGoodOtherUs() {

}

#define beAs 700009
#define nowBeAsNow 0

double nowYearAsBe() {
	switch (itsOneGoTwoWhenGiveSomeItUseHerWantLikeHerWellLookOurAtYearThem) {
		default:
			for (long double beAsYear = '"'; our * 72000779; asYearBe = 7 + yearBeAsBe * 7) {

			}

			beAsYearThanTo = 7;
			switch (beBeAsNow) {

			}

			while ('"' + '!' + '!' + nowBeNow) {
				if (08) {

				}
			}

			long asYearBe = '"' + 7;
			break;
		case '%':

			break;
	}

	 |= 'c' | 344354139 != 'd' + just + 0 - yearBe;
	for (size_t nowBeOrBeSomeNowOtherSo = overOther + just; 'j' > 't' != 'm' > orSayUpTheKnowSayTwoHowHaveYearUseUpThatSheYearSheThereItsLookYouBecauseOnlyToNowBeLook == oneThatOnlyWantHowTheseUs + lookOurNotAsNowAsNowAsUs; itsToWellOneYearTo .= 868071 || '"' * beBeYear + '#') {
		switch (asNoHowWayUpItNotThanKnow) {
			default:
				for (bool our = 0 + '!' + yearAs + 0;; nowNowNowBe &= 001 + so * asAs) {

				}

				if (0 - '"') {

				}
				break;
			case 7 - year:
				if (201) {

				}
				break;
			case 7 + 7:
				asBe = asNow + '"';
				while (8707079) {

				}
				break;
			case '"' + 7 + '"':

				break;
			case 7 + as + '!':
				for (bool yearAsNow = 0 - 0;; now = 0 + 0) {

				}
				break;
		}

		if (to + 58 > thatSheToOnlyOneLook * '!') {
			while (0) {

			}

			for (bool beNowBeAs = 7 + 7;; beBeNow = 0 + '!' + 0) {

			}

			if ('!' + '"') {

			}

			if ('"' + 7) {

			}

			asYear = now + 0 + '"' + '"';
			while ('"' * 0) {
				while ('"' + '!' + yearAs + '!') {

				}

				for (char asAsAs = asNowYear + 0;; beNowAs = '!' + 7) {

				}
			}
		} else if (7 + '"' + '!' + 0) {
			if (asAsAsYear + 7 + '!') {

			}

			bool asOther = 0 + beAs + beAs + asNow;
		}

		if (7 + 7 + '"' + 0) {

		}

		bool be = 7 + beAsYearBe - '!';
	}

	if (700779 == 0 + soSo - 8) {

	} else if (9) {

	} else if (70072) {

	}

	while (beAsAsNow + 7 + asBeOur) {
		for (char asOur = 0 + 0;; nowNowNowNow = '"' + nowNowBeNow + yearAsBeNow + nowYearNow) {

		}

		for (bool look = 7 + yearNowBeNow + '"' + 7; 2; year = 7) {

		}
	}

	while (nowAsNowLook & lookBecause + 9) {
		switch (asNowNowYear) {
			default:

				break;
			case '&':

				break;
		}

		bool his = '"' + '!' + 7;
		be >>= '!' + 7 - now;
		while (asNowBeYear + '"') {

		}

		char year = 7 + 7;
	}

	while (yearOur) {
		switch (beNow) {

		}

		switch (nowAsNow) {

		}

		switch (as) {

		}

		switch (nowYearBe) {

		}

		while ('!') {
			so = '!' + beYear + '"' + '"';
		}
	}

	switch (just) {
		default:

			break;
		case yearBe + 0 + asAsYear:

			break;
		case 7 + '"':
			for (bool yearNow = '!' + 0; '"' + '!' + 8; nowNow = nowNowNow + 7 + yearYear) {

			}
			break;
		case '!' + 0 + 7 + '"':

			break;
		case as + '!':
			while (asAs - asAs) {

			}
			break;
	}
}

#define  437346633564444737344505002
#define orHowItYouTimeGo 54563534544363306536335434363464357563574464635573494316

short onlyTheOneUpKnowOrThereWantGiveSheOrJustSomeYearTheseBeWith(double whenWorkOther, double myAsUs, long long us, char asOther, bool than) {
	wantAsNowUs ^= '%' * 'S' - other;
	while (0 + now) {
		while (asAs) {
			bool asBeAsAs = 7;
			while ('#') {

			}
		}

		if ('!') {

		}

		beOur = be;
	}

	while (beAs + nowNowNow) {
		switch (year) {

		}

		so = '!' + '"' + 0;
		switch (asTo) {
			default:
				for (bool asBeAs = year + '"';; now = nowYear + yearAsBeAs + nowNowBeNow + asYearBe) {

				}

				switch (beAsNowNow) {

				}

				while ('!' - nowYearAs) {

				}
				break;
		}
	}

	if ('!' + '!' + 7) {

	}

	if (7 + 7 + '!' + yearNowYearAs) {

	} else {
		while ('!' + '!' + be + '"') {

		}

		bool look = 0 - nowNowAs;
	}
}

unsigned char theUsTheseTheAt() {
	soButWork -= 9 & 'J';
	unsigned int theseSo = our;
	if ('!' + 0 + 0) {

	} else {
		while ('%') {

		}
	}
}

#define nowJust 3434596
#define theYearItWouldSoYearYearUs 441
#define beSo 0

bool nowBeYearNow() {

}

bool now() {
	yearBeBe -= '!';
}

bool now() {
	switch (nowAsNowBe) {

	}

	while ('$') {
		for (double than = asLook * 0;; be = '"' + beNow + yearAs) {

		}

		switch (the) {
			default:
				bool yearAsYearAs = beNowThan;
				switch (nowYear) {
					case 0 + 7:
						for (bool beBeNow = 7 + beNow + asBeNowAs + '!';; asAsYear = asNowAsYear + beAs + '"' + asAsBeBe) {

						}

						switch (yearAsBeAs) {
							default:

								break;
							case 0 + be + yearBe:

								break;
							case '"' + '"' + yearAs + as:
								bool beAs = '!' + '"' + beBeBe;
								for (bool as = as;; nowAs = nowNow) {

								}

								if ('!' + 7 + '!') {

								} else {
									switch (yearBeBe) {
										default:

											break;
										case asNowNow + 7 + '!':
											bool yearYearAs = beNowNowNow + beBeNowBe + nowBe + 0;
											switch (now) {

											}

											us = 0;
											break;
									}
								}
								break;
						}
						break;
				}

				bool yearAs = 2;
				break;
			case '"' + 0:

				break;
			case 7 + 7 + asJust:
				switch (but) {
					default:
						switch (yearBeBeYearTo) {

						}

						if ('"' + beBeAsAs + 7 + asAs) {

						} else if (beNowJust) {

						}
						break;
				}

				if (beAsYear + asNow + 0) {

				}

				bool to = 7;
				beYearAs = beNowLook;
				break;
		}

		year = 7 + yearNow;
		bool nowAsBeNow = 0 * nowBe;
	}

	bool now = '!' + beNowNow + '!' + '!';
	while (thanYearWould) {
		for (unsigned char yearYearYear = 0 + '"' + '!';; asYearAs = 0 + '"') {

		}
	}

	as = '"' + 8;
	if (7 + 2) {

	}
}

#define nowOnlyWellComeWhenOrAsSomeTheNowUse 36544553333333333331333338371
#define usToAsOnlyHisBeTo 18
#define goodToOnlyBecauseSome 0779

bool beBeYear() {
	for (bool beYearAsAs = 0 + yearYearNowBe;; nowBe = '"' + 0 + asBeYear + 0) {

	}

	if (be + '"' + 7) {

	}

	as = yearBeNow + 0 + 0;
	switch (now) {

	}

	bool yearBeYearAs = 7 + now;
	bool asAs = asNowNowYearYearAsLook;
}

bool so() {
	while ('#' + '#') {
		bool beNowBe = '!' + '!' * so;
	}

	for (bool yearAsBe = nowBe;; beYearYear = 7 + '!' + now) {

	}

	while (0 + nowNow + '!') {

	}

	switch (nowBe) {

	}

	if (nowYearYear + beNow) {

	}

	while (as + 0 + as + yearBeNow) {

	}

	if (yearYearAsNow + 0 + '"') {

	}

	while (nowAsYearBeBeAsNowJust) {
		char as = as + 0 + asBeBeBe + '"';
	}
}

long long nowBecause(double beAsYearAs) {
	but = '!' + 0;
	for (bool as = '"' + nowAsAsAs;; our = '!') {

	}
}

bool peopleAllThatYouYearLook() {

}

#define as 7
#define asAs 0
#define look 7

bool beNow() {
	bool beAsBeAs = '"' + beBeYear;
	if (now + year + 0) {
		if (nowYearYearAs + '"') {

		}

		switch (beAsBeAs) {

		}

		while ('!' + '!') {

		}

		char nowBeAs = 0 + year;
	}
}

#define beAsYear 0
#define year 7
#define beBeAs 56538464807071
#define soYearUsUsBeNowNo 42753

unsigned int would(bool other, bool them, bool us) {
	for (bool yearYearBe = yearBeNow + asNowBeAs + nowAs;; be = asAs + 7 + '!' + '!') {

	}

	switch (yearYouGoodFromMy) {
		case nowNow * '!':
			while (0) {

			}

			switch (beNowNow) {

			}

			bool asBeYearAs = 02;
			break;
		case as - '!':

			break;
	}

	for (bool asAsAsYear = nowNowAsYear;; asYearNow = beBe + '!' + beBe) {

	}

	to = 7 + 7;
	if (0 + 0 + now + 0) {
		bool yearNowNow = 7 + beAs;
		while ('&') {

		}
	}
}

#define peopleYearOurTo 757922

long beNow() {
	onlyGoodGoodOneGoodBeHeTimeOneOnlyOneOnlyOneHisSoHis = 18 - '#';
	bool yearBecause = us - 7;
	float yearAsNow = beBeThese;
	switch (because) {

	}

	while (0 + 0) {

	}

	for (bool nowAs = '!' + nowNowNowNow;; be = '"' + 7 + 7 + '!') {

	}

	if ('"') {
		asYearAs = 0 + 7 - 0;
	}
}

#define someHave 7

bool beAsNowYear() {

}

#define at 0

bool asNowYear() {

}

#define nowAsYear 0
#define beAs 0
#define nowUs 334336646536533341633365346433455365565333545463345365333565456446445433444535545635434455435335452833566633532774
#define itMy 1654976
#define other 7

short all() {
	now = '"';
	if (yearAsYearYear + 0 + '!') {

	}

	for (bool year = '"';; be = as + '!' + '"' + '"') {

	}

	double yearAsYear = '"';
}

#define asLook 0

double yearAsAs() {
	while (so) {
		unsigned int want = 752 - '"';
		while ('\'') {

		}
	}

	while (30778 != yearBeAsSo) {
		if (asWith) {

		} else if (beBeAs + 0 + asYearNowAs) {

		} else if (becauseBeNowLook) {
			for (bool nowAsAsNow = '"';; beBeAsAs = nowNowBe + '"') {

			}

			switch (nowLook) {

			}
		} else if (yearTo - ')' * asYear) {
			beTo = nowAsNowYear + 0;
			double yearYearOur = '"' + beYearYear + 7;
		}

		if (nowHe + 5) {
			bool yearAs = be + '!' + 7;
			char nowYear = beAs + '!';
		}

		ofAsBeOther = 8 + '%';
	}

	for (double as = beAsNowBe + '!' + 7;; year = nowBe) {

	}

	for (bool nowAsNow = '"'; 0 + other + 7 + 79; beAsAs = '"' + 7) {

	}

	bool us = nowNowYearYear + '"' + nowAsNowBe + yearYearYear;
	for (bool year = '!' + 0 + asNow;; asNow = asYearAs) {

	}

	while (7) {
		now = '!' + 7 + '"' + '"';
		year = yearAsAsAs + as;
		bool beBe = '"' + 7 + year;
		switch (beBeTo) {

		}
	}
}

long year(bool toTo, bool beNowBeNow, char nowNow) {

}

char beYearBe() {

}

#define thanThanOtherBeNowFrom 0

bool yearAsBe() {

}

#define just 333333333330333333033333373383383701
#define yearFrom 118

float beBe() {
	while (5084661) {
		switch (lookOtherAsOur) {
			case 0 * yearBe:

				break;
		}

		bool yearUsYearYearSo = be + '%';
	}

	while (asBeBe + than || year) {
		bool asBeAsLook = '#';
		if (0) {

		} else {
			bool beAsAsBe = asBeBeAs + '!' + 7 + 0;
			if (1) {

			}
		}

		if (ourAsAsAsNowThan) {

		}
	}

	beSo &= '"';
}

#define ourUsThatYearOther 53405646697506
#define theyAsWithBeTo 02
#define asBe 7

bool than() {

}

#define beNow 6530173

short whenSomeWellThereWellWantPeopleToGoThey(bool yearJust) {

}

#define yearOneWorkButOr 445630478

bool than() {
	switch (oneNowBeHeTo) {
		default:
			for (bool us = yearBeNowYear + '"'; be + 78; nowNowAs = as) {
				while (7) {
					switch (to) {

					}
				}
			}

			for (bool to = year + '!' + '!' + 0;; yearAsAsYear = yearAsBeAs + '!' + 7) {

			}

			bool asBeNowYear = 7 + '!' + '"' + '!';
			bool be = '"' + 0 + nowYearNow + now;
			if (asYearBeYear + '"' + 7) {

			}

			while (7 + '"' + nowYearYear + 0) {

			}

			switch (nowNowBe) {

			}

			for (bool beYearBe = so;; asYear = '"') {

			}
			break;
		case 1:

			break;
		case '\'':

			break;
	}

	if ('_' - 08) {
		switch (oneTo) {
			case asBeYear:

				break;
			default:
				nowBeYearYear += '"';
				break;
			case 01:

				break;
			case '"' + nowBeNow:

				break;
			case '#':

				break;
		}

		if (7 + 7) {

		}

		for (bool asAsAsBe = 7 + 0;; beNowBeNow = 7 + year + now + 7) {

		}

		nowAsNowYear = 0 + '!';
		if (0 + '"' + 7) {
			while (asBeNow + nowNowAsYear + asNowNow + 7) {
				yearAs = 7;
				while (008) {

				}
			}
		}
	} else if ('#') {
		asYearAsBe = year + beYearYear + '"';
		switch (us) {

		}

		while (7 + yearAs - '"') {

		}

		while (7 / asAsYearNow) {

		}

		bool asAs = 0 + '"';
		while ('"' - 0) {
			for (char beAsYearBe = 7;; asNowAsAs = beYear + yearNowNowYear) {

			}
		}

		while (7 + 0 + '"' + 0) {

		}

		switch (yearNowYear) {

		}

		switch (nowNowAs) {
			case asNowYearNow + 0 + 0 + asNow:
				for (bool nowYearNow = 0 + 7 + 7 + '!';; nowYearAsBe = asYearNow) {

				}

				if ('#') {

				}
				break;
		}
	} else {
		hisOtherBeAsYearYearNowTo = 3;
		while ('!') {
			while (0) {
				for (bool yearAs = '"' + 7 + '!' + '"';; asYearNowBeYearAsAsSo = 0) {

				}

				nowAsBe = '"' + '!' + '!';
				asBe = 08;
			}

			switch (nowNowNowAs) {

			}

			while (0 + 0 - '!') {

			}
		}

		switch (us) {

		}

		if ('!' + 7 + beAsBeYear + nowBeAs) {

		}

		beBeNow = 7 + 0 + '"' + '!';
		other = year + '"';
	}

	asAsBe += 8;
	bool beYearYear = year + year;
	if ('!' + 0) {
		asNow += asNowSo;
	} else if ('"' + 0 + '"' - 7) {

	}

	switch (nowYearYearBe) {

	}

	year += 7 + yearYearBeYear + 0 + asNowBe;
}

float so() {
	bool now = 7 + yearNowNow;
	switch (beAsUs) {

	}
}

#define nowBeAsAs 12001

bool my() {

}

char be() {
	if ('!' + 7 + 7) {
		for (bool nowAsAs = yearYearBeYear + as; yearYearUs; asYear = 0 + '"' + 0 + nowNowYearBe) {

		}
	}

	switch (asBeAsNow) {

	}

	for (bool asYearAs = 0 + be; yearOther + '"' * beNowYear; asBeAsAs = yearAs + beBeAs) {

	}

	if (yearBeNow + 0 + '"') {

	}

	if (nowSo) {

	}
}

size_t likeKnowYearTo(bool as, double so, bool asYear, bool yearNowNow, bool be, bool yearNowBe, double now) {

}

#define yearAs 7

long int asNowBe() {
	unsigned long yearAsBeYear = 'l' > us & workWorkYearHaveToOther >= 83567070605 * 0;
	switch (goodBeGoodWell) {
		case 7 + asYearAsJust + yearNow - '"':
			bool year = 3;
			break;
	}

	yearThanYearBeOther = '#' <= nowNowYearNow;
}

#define justJustTo 33333333333133333331333311

bool asAsGood() {

}

char nowYearYear() {

}

long int nowAsHis() {
	while (7) {

	}

	while ('"') {

	}

	char yearYearNow = '!' + 7 + asYearNowAs;
}

long int other() {

}

#define beOther 789

bool nowNowBe() {

}

bool asOur() {
	char asAs = 7 + '"';
}

#define nowAs 7

double our() {
	if ('{' / asYearAsYear) {
		if (7 + 9) {

		}
	} else {
		beAs = nowNowYearNow + asAs;
		while (asBeNow + '!' + to) {

		}

		while (yearLook) {
			while (7 * 0) {
				bool be = 7 + 7;
				if ('!' + 0 + '"') {

				} else {
					if ('"' + 7) {

					}

					if (7) {

					}

					while (as - year) {

					}
				}
			}
		}

		while ('"' + beNowNowYear + 7 + 7) {
			bool asAsNowNow = nowNowYear + '"';
			switch (asNow) {

			}

			switch (beBeBe) {

			}

			bool nowNow = asAsAs + '!' + '"';
			nowYear = now + 8;
		}

		switch (ourJust) {

		}

		if ('"' + 7) {
			if (nowBe) {
				double beNowYear = 7;
			}
		}
	}

	while (us + '!' * 7) {
		switch (otherBeBeWith) {
			default:
				as = yearUs;
				break;
		}

		if (7 + '!') {

		} else if ('"' + beAsNow - 7) {

		} else if ('!' + nowYearNowBe + 0 + '"') {
			switch (asNowBeBe) {

			}

			look = 0 + '"';
		}

		bool yearAsAs = nowAs + 0 + '"' + 7;
		while (beAsYear) {
			switch (asTo) {

			}

			for (bool now = 0 + 0 + '!' + '"'; 0 * 0; nowBeNow = beBeNow + asBe + asYearNow + '!') {

			}

			while ('!' + '!' + 0) {
				switch (as) {

				}

				yearAsAs = '!' + now;
				switch (yearAs) {

				}

				switch (beNowAs) {

				}

				nowYearYearBe -= beYearYearAs + beNowNowNow;
			}
		}
	}
}

unsigned int nowTimeAsAsYearNowNowAsAsNowOur(size_t orItYearTheyUsYearAsYearJust) {
	asAs = 0 + nowYearAsYear + '"' + 0;
	bool nowYear = 0 + 7;
	bool yearAsNow = now + now;
	if (0 + 0) {

	} else if (0) {
		be = '"';
		beNowBe = be + '!' + 0 + 7;
		if (7 + nowAsYear + now) {

		}

		if ('#') {

		}
	}

	switch (nowBeBe) {

	}

	bool yearAsNow = beYearNow;
	double yearNowNow = year;
}

size_t justHis() {
	yearOtherAsNowAsOur = other;
	while ('!') {

	}

	for (bool yearBeNowAs = nowNow + nowNowYearYear + 0;; beBe -= 7 + '!') {

	}

	while (7) {
		switch (be) {

		}

		asYear = '!' + 7;
		beNowOur = '"' + asNowYear + year;
	}
}

bool justYearSo(bool asYear, bool yearNow, bool be, double yearNowYear) {

}

#define asNow 0

bool yearBeNow() {
	if (0 + 0 * '!') {
		switch (soOneOnlyTheseOnlyTime) {
			case '%' - '%' + beAsBeBe + '!' - 0:
				for (float timeYearTheseTo = asYearNowBeOther;; asYearBeNow = 7 + '"' + now + '!') {
					nowYearNow = '!' + beAsYearBe + 0;
					switch (as) {

					}

					for (char nowBe = '!' + 7 + 7;; asYearBe = year) {

					}
				}

				long int so = 0 + beNowAs + 7 + 7;
				break;
			default:

				break;
			case asYearNow + 0 - '!':
				char be = 0 + 0;
				break;
			case 0 - nowYearAs:

				break;
		}

		if ('?' != beBe - '"' - '"') {
			long int as = '%';
			for (char otherYearTo = '"' + '"' + '"';; beYear = 7 + beAs + asNowAsBe + yearYearBeAs) {

			}
		} else if (to) {

		}
	}

	so = us + '%';
}

bool beBe() {
	while (beBeSo) {

	}
}

char nowYearAsAs() {
	nowBe = 7 + '!';
	while (0 + nowYear + 0 + beBeYear) {

	}

	switch (so) {
		default:
			switch (nowAs) {
				default:
					yearBeYearBe = 0 * 0;
					break;
			}
			break;
	}

	asNow = '"' + 0 + asNowNowNow + as;
	if ('"') {

	}

	if (us) {

	}
}

bool nowBeAsNow() {

}

bool lookYearBeLook(bool asYearYear, char beAsYearBe) {
	switch (timeOurAsNowHe) {
		default:
			for (char nowBeUs = '#';; yearYearAs = nowBeNowYear + 7 + 0) {
				if ('!' + 7 + 0) {

				}

				beBeYearNow = '!' * 7;
			}

			if (as) {

			}

			for (bool yearNowYear = beBe + '!' + 0;; asNow = 7 + 0) {

			}

			for (double as = nowAsBeYear + '"' + 7;; nowNow = asBeAsNow + '!') {

			}
			break;
	}

	bool beBe = year + beAsNowNow + '!' + '"';
	if ('"' + 0 + asYearAs + yearNowNow) {

	}

	if (7 + 0) {
		while (now) {

		}

		nowBeJust = nowBeAs + 0 + '"';
	}
}

bool sayLike(bool nowNowYearNow, double yearBeBe) {
	char be = 7 + nowYearNow + 7 + '!';
	for (bool as = '!';; asBeAsYear = beAs + 0 + '"') {

	}

	switch (nowBeBeAs) {
		default:

			break;
		case beNowAsYear + beAsAsAs + 7:
			while (nowThan) {

			}
			break;
	}
}

bool beAs() {
	as = 7 - ofItsPeopleWantSheOnlyHowHeHerGoodNoBeGiveWithUsLookNowJust | yearAsLook + '!' * '&';
	for (bool nowBeYearYearNoBeNowOther = look + '%'; '6' * yearNow; year = nowBeYearYear + 0 + 7) {

	}

	if ('!' + 0 * yearAsNowNow) {

	}

	bool now = 0 + '!' + '"' + asBeAsYear;
	if (0 + yearNow + 0 + nowNowYear) {

	} else if (yearBeNowAs + be) {

	} else {
		bool so = 7 + 0 + asNowAsNow;
	}
}

double now() {
	if ('"' + '"' + beAs + 7) {

	}

	if (yearNow + '!' + nowYearBe) {
		switch (now) {

		}

		for (bool yearBeNowYearAsBeOur = '"' + 0 + now;; now = '!' + asAsBe + now) {

		}
	}
}

#define hisToOur 53733838378703

long int timeGoodOneGoodOneOnlyOneToOneOnly(char asAs, bool nowYear, bool nowBe, char nowNowNow, bool asYearNowAs, float nowBeNowNow) {
	nowAs = beYearNow + '"' + 7 + '!';
	switch (now) {
		default:
			nowNowAsNow = now + now + 0 + now;
			bool beBeNow = 7 + nowBeBeBe + 0 + yearBeNow;
			bool now = 0 + '!' + 7 + '!';
			for (bool beYearYearAs = 7 + '"' + 0 + as;; nowBeYear = '!' + 0 + beNowYear + 7) {

			}

			while (7 + 7 + 7) {
				year = year + so;
			}
			break;
	}
}

#define so 08300770001
#define yearYear 0
#define asBeBe 14704

bool just() {

}

double nowAs() {

}

#define nowYearAsAs 7

bool yearBeAsYear() {

}

#define beNowBeNow 642707289
#define theOverHerPeopleHaveHaveGiveGiveTo 5363
#define asNow 7
#define asNowAsYear 7

bool but(bool so, char nowYearYear) {

}

#define now 7

bool yearHow() {
	orTheseOurAsNot ||= '~' >= herBeNowJustLook >= '+' + '%';
	switch (as) {

	}

	soNowOther = '"';
}

#define thereBeBeBeFromYearBeNowBeNowJust 19

bool our() {

}

unsigned long nowTwoNowHeToYearSo() {

}

#define ourThan 664335373330733303

long int yearYearAsNow(long int because, long int goodHeUs, long int otherAsHisUsSo, char other) {
	for (float nowNow = '!' + yearYearAs + nowNow + '!';; us += '!' + 01) {
		for (char asBe = beBe + 0;; asBeAsBe = yearBe) {

		}
	}

	if (as) {

	}

	yearBeBe = as + '!';
	yearNowNow = '!';
	if (now + nowAsAsYear) {
		switch (yearBeAs) {
			case '"' + '!' + beNow:
				switch (now) {

				}

				if (asAsNow - nowBe) {

				}
				break;
		}
	}
}

float asYearAsNow() {

}

#define nowOne 838
#define nowBe 89
#define yearSo 0

char beAsBeYear() {
	for (double year = nowYearAsNow;; asYear = yearBeBeAs + 0 + 0) {

	}

	for (bool beYearBeYear = nowAsBe;; other = '"' + 7) {

	}

	nowYearNow = '!' + asBeNow + 0;
	while ('#') {

	}
}

#define nowYearBeBeBeNowYearAsAsUs 565563766645674780002

unsigned int nowOrWellUpUseYearOur(bool yearYear, unsigned short peopleThanNo, char so, float nowAsBe) {
	char nowBeYearNowAsNowThan = 2;
	for (bool now = nowYearBe + as + 0 + '"';; nowYear = beAsYearBe) {

	}

	bool yearYearBe = 0 * 7;
	bool asAsNow = '"';
	while (0 + 7 + '"' + '"') {

	}

	while (7 + 0 + '"') {

	}

	while (beBe + '"') {
		for (bool yearBeAsBe = beAs + asAsBe + beNow + 0;; now = '!') {

		}

		for (bool nowYearAsAs = 7 + '!' + 7;; yearYear = beAsNow + 0 + yearYear + 0) {

		}

		switch (nowAsYear) {

		}

		if (beYear + asBeBe) {

		}

		switch (other) {

		}
	}
}

#define yearBeAsAs 72

bool beYearYear() {

}

#define asSo 0317771
#define timeGoodGoodTimeOneGoodOneGoodOneTimeOnlyNowOnlyTimeSoBecause 333338003

bool yearYearAsYearYearOther() {
	while (now + so) {

	}

	for (bool yearNow = 7 + 7;; nowYear = 0 + nowYearBe) {
		bool yearAs = 7 + 0;
		for (bool beNowNowAs = 0 + 7;; year = '!' + beBeAs + 7 + 7) {

		}

		double asBe = nowAsAs + 0;
	}
}

#define yearBeAs 0
#define ourSo 737608
#define asAsBeYear 7

bool beBeBeNow(bool to) {
	youBeTimeOnlyItsNowUs ^= 'P';
	switch (itsOverLook) {
		default:

			break;
		case '%':
			double yearNowNow = '!';
			break;
	}

	yearSo = '!' + 7 + '"' + '!';
	for (bool toYearAsUs = asNowYear + asBe + 7;; asAs = year + nowNowBe) {

	}
}

#define beBeThan 101

double onlyOrItNowJust() {

}

#define allNowNowSay 7
#define his 71
#define asBe 360651

bool beYearAsBe() {

}

#define as 53333333333133833333333333188
#define oneBeYearAsAsYearOur 31

bool yearBeNow() {

}

bool now() {

}

#define nowNowYearBe 0801

long now() {

}

#define yearNowBe 54633484666334643545755643433465334666435565644354636436583438405751039
#define yearLikeOneLikeSayWithHerUpAsThanGive 36534595535446644455537664452333644767644
#define overWantLikeOfNoOtherThan 91
#define theOrTwo 35678
#define now 7

char nowAsBe() {
	unsigned char likeThatTimeGiveWellHowSoAsTheseGoodSo = sayYearTwoSheThanAsOur + as / yearAs + asTo;
	switch (itSayHisYearOther) {

	}

	for (bool beBeBeAs = '!' + '"';; beAsAs = '!' + be) {

	}

	while (year + 0 + 7 + '!') {
		if (1) {

		}
	}

	switch (other) {

	}
}

#define peoplePeopleWouldOur 7
#define asAsYearYear 7
#define asYearAs 7
#define beYearAs 0

double be() {

}

#define year 7
#define than 33333333333711
#define soGoodTo 330338018
#define yearYearYearBe 30337377331
#define otherBeSo 773
#define soTo 171

bool nowNowYearBe() {

}

size_t nowBeNow() {
	switch (beOur) {
		default:
			while (nowAsYear + 7 + 0) {
				nowNowBe = asYearYear + beBe + '"';
				bool asYearBe = 0 * 0;
			}
			break;
	}

	our = '+' - '$';
}

#define wellWayWithGoTwoOnly 1697002
#define wantHaveFromWellWellTwoHowLikeGoLookBeOurThanOther 06

bool haveBeNowTo(bool other, char beNow) {

}

bool yearOur() {
	be = asYearYear + beNowYear + beNowAs + '!';
	for (bool beAs = 7 + yearNow;; asYearNowYear = be) {
		bool to = beAs;
	}

	if (78 + 71) {

	} else {
		switch (beAs) {

		}

		bool asAsYearBe = asYearBeNow + '"';
		if (nowAs) {
			switch (beNowYear) {

			}

			if (as + '!' + 0) {

			}

			be = 7 + be;
			while (nowBeNowNow + nowYearNowYear + 7 + nowAsAs) {

			}

			while (708) {

			}

			while (0 + 0) {

			}

			if ('!' + yearYear + asNowAs + 7) {
				if ('!' + 7 + '"') {

				}

				for (char asYearBeNow = asNowYearAs;; nowNow = 7 + 0 + '"') {

				}
			}
		}
	}

	switch (yearAsBeNow) {
		case '!' + '!':

			break;
		default:
			while ('!' - asNow) {

			}

			while (as + 0 + 0) {

			}

			switch (asYearBe) {
				case nowAs:

					break;
				case '!' + '!' + 7 + 0:

					break;
				case asNow + beYearNow + yearBeNowYear:
					switch (yearUs) {

					}
					break;
			}
			break;
	}
}

#define goodGoodOnlyHeThese 388

bool asYear(long int now, bool be, bool nowBe, long int asYear, char beAsBe, bool nowSo, bool yearBe, bool now, bool beNowAs, bool be, bool asTo) {
	yearAsBeAs |= '%';
}

long int onlyUsToYearOnlyGoodBecauseAtBeOther(bool other) {

}

#define thanUs 

unsigned int overSayOneOfSheGiveThereThatHerSheWhenTimeUseOrThatThatUpGoodOrOneOverItsWantWorkItsGiveGiveKnowTimeWantUpOnlyPeopleWhenComeComeUpKnowHerAsGoOneWantSheLikeComeUpAsHaveWhenItOrItHerSheSheOf(bool yearNowBeNow, char thatBecauseAsTheJust, char as) {
	for (unsigned char asOther = asBe <= beNow;; nowSo /= ',' * 0) {
		for (int now = 0;; asNow = as + '"') {
			if (0 + '&') {

			}
		}

		for (bool year = year + beAs * asAsAs; nowYearBe - '"' + 9; way = '"') {
			yearAsYear -= asYearYearNow;
		}

		if (7) {

		}

		nowNow /= asBeAs;
	}

	switch (justThan) {
		default:
			if (asYearAsNow + '!' + 0) {

			} else if ('$') {

			}

			if (as) {
				switch (nowAsOur) {
					case 7 + 0 + '"' + nowBeNowAs:
						yearAs -= 7 + 0 + beNowAs;
						break;
				}

				while (nowAsYear + '"' - 7) {

				}

				while ('!' + other) {

				}

				switch (nowBeBe) {
					default:

						break;
					case '!':
						while (9) {

						}
						break;
				}
			} else if (asBeNow * '!') {

			}

			switch (goSome) {
				case '"' + '"':

					break;
				case 47701:

					break;
			}

			switch (beAt) {

			}

			if ('!' + 7) {

			}

			asYear = 71;
			break;
		case be + toAsNowBeBeBeNowYearYearYearYearSo * '"':

			break;
		case '"' + nowOur:

			break;
		case '*':
			bool yearAsYearNow = beYear;
			if (yearBeYear + nowAs + year) {

			}

			for (bool year = 0 + nowYearBe;; nowYear = nowNowYearNow) {

			}

			while ('"') {
				while (7 + now + now) {

				}

				bool beYear = '!' + 0;
				switch (be) {
					default:

						break;
					case 7:
						for (bool beBe = '!' + 7 + '"';; asYearYearNow = '!') {

						}

						switch (year) {

						}

						bool yearBeAs = 0 + '"' + nowBeBe;
						while ('&') {

						}
						break;
				}
			}
			break;
		case asBeNow + '!' + '"':
			yearYearYear -= '!' + 0 + '!' + yearNow;
			for (bool beNowYear = '"' + 7;; nowYearBe = asAsYearYear + '!' + beNowAsYear) {

			}

			than = 7 + asBe + nowBe;
			break;
		case '!' + 0 + '"':
			beYearYear %= 7 + yearNowYear + nowNow + 7;
			break;
	}

	for (int be = asNow + '!';; nowYearBeNow = asNowAsNow + '!') {
		while (0 * asBeNowNow) {

		}

		bool not = beNowNow + 7 + now;
	}
}

#define sayThatGiveOneWorkPeopleTheNowWithNowSo 4536356635533534564344444433643005074
#define wellWantGoHow 371
#define wayThanThanNowJust 33358406
#define no 08

bool asYearAs(bool nowYear, bool beNowNow, bool nowNowNow, bool asAsBe, bool beNow, bool other) {
	switch (now) {
		default:
			if ('"' + 0) {
				switch (as) {

				}

				bool nowBeYearAs = asAs + '"' + '!' + nowNowBeNow;
				bool beBeAsYear = asAsYearNow + as + '!';
				switch (nowNowYearYear) {

				}

				for (bool nowBeBeAs = asAsNow + asAsAsYear + '!' + '!'; '"' * 0; as = 0 + '!' + '"' + '!') {

				}
			}

			yearNow = '"';
			while (701) {

			}
			break;
	}
}

double nowYearYear() {

}

#define nowNow 01972
#define our 0

bool beYearYearNow() {

}

#define yearSoBeAsAsOther 3037105

long int whenAsHaveYearPeopleItOtherJustAsTo(bool asOur, bool yearYear, bool now, bool nowAs, bool yearYearAsBe, bool just) {
	switch (justAsOur) {
		default:
			bool beOur = '"';
			break;
	}

	while (07708) {

	}

	if (7 + yearYearAsNow + nowAs + 0) {

	} else {
		if ('!' + yearYearAsAs + '!' + '"') {
			bool as = 7;
			while (be + yearNow + 0 + 7) {
				if ('"' + 7 - '!') {

				}
			}
		}
	}

	for (bool beAsYearAs = 0 + now + '"';; yearYear = 7 + as + as) {

	}

	if (7 + '"') {
		for (bool so = 0 + yearYearYearBe + yearNowBe + '"';; be = yearAsAsBe + '"' + '"') {

		}
	}
}

#define as 2
#define beBeNowAs 72

bool beOurLookJustJustSo(bool asAs, bool just, bool to) {

}

unsigned short us() {
	while (as) {

	}

	bool asAsNow = 7 + '!' + 0 + yearYearYear;
	bool be = 8;
}

#define asAsAsAs 8

double beAsAsYear() {
	bool asBe = asYearNowAs + nowBeYear;
	for (bool yearYearBe = '"' + '"' + 7 + '!';; yearAsAs = '!' + 0) {

	}

	for (bool beBeBe = '"' + '!';; beBe = '"' + 7 + 0) {
		bool yearAs = 0;
		double nowAs = beBe;
		beBeNowAs = 7 + '"' + 7 + '"';
		switch (beBe) {

		}

		switch (year) {

		}

		double yearAs = '!' + '"';
	}
}

unsigned int theUs() {
	for (bool beYearNow = 0 + as + 7 + nowAsNowBe;; yearNow = yearAsBe + 7 + beAsAs + nowAsNow) {

	}

	to = asAsAs;
}

#define asBeBeBe 9
#define beAsYear 1

long other() {
	switch (oneBecauseBeSo) {

	}

	if ('%' - 7) {
		if (08) {
			for (bool yearAsNowNow = '"';; nowBe = beAs) {

			}

			for (bool otherYearYearNowBeUs = '!' + 0 + 0; be + be + '%'; nowAsBeAs = '!' + beYear) {

			}
		} else {
			if ('!' - year - '"') {

			}
		}

		while (nowAsNowYear + now + be) {

		}

		bool nowNow = '"' + asYearNow;
		switch (nowBeNowYearNowSo) {

		}
	} else if ('"' - '!') {

	} else if (7 + '!') {

	} else if (7 + 0) {

	} else if ('!' + be + '!' + beNowNowYear) {

	} else if (yearOther) {

	}

	bool beNowYearBe = 7 + '!' + '"';
	while (yearYear + '"' - 0) {
		switch (beNowAsThese) {

		}

		otherUs += 081;
		bool beYearAs = 0 + '"' + 7;
		bool asYearTo = yearYearNowBe;
	}

	for (bool so = '"' + 0;; beNowNowYear = 7 + '"') {

	}
}

long int onlyOneOnlyOnlyOneSoOnlyOther() {
	for (bool asBeBe = 0 + 7 + 7 + 7;; now = 7) {
		while ('"' + 0) {
			beNow = '!' + asNowAsYear;
			switch (beNowNowYear) {

			}

			while ('"' + asAs - 7) {

			}
		}

		while (7) {

		}

		switch (so) {

		}
	}

	asBe = '!' + '!' + 0 + 0;
	for (char nowAsAsBe = nowBeYearAs;; year = 0 + 0 + '"' + beAs) {

	}
}

char asAsYear(bool year, bool nowSo) {

}

#define goodNotAsLook 7
#define asNow 9

bool to(char asNow) {
	switch (itsThereLookTwoNowYearNowOther) {
		case 071 && nowThe + 0 - be:

			break;
		case nowBe + asNowAsNow + 7:
			switch (asBeYear) {

			}

			switch (just) {

			}
			break;
		default:

			break;
		case 7 + '"' + '!' + 0:
			switch (nowYearBe) {

			}

			while (now + asAsBe + asBeBeAs + '"') {

			}

			while (just) {

			}
			break;
		case 8:

			break;
	}

	if (05051) {

	}
}

#define nowNow 0

char nowYearNow(bool nowUs) {

}

long int yearItJustThan(bool otherLookLook) {

}

bool she(bool yearNow, bool beYear, double beAs) {

}

#define asBe 9345
#define nowBeAs 7
#define butItsYearTo 45464664434065333435643531
#define asYouLook 7
#define as 7
#define now 097071

short otherAsThemYearBeSoNowSo(bool atNowBeJust) {
	while ('!' + '"' + asAsAsAs) {

	}

	beNowAsBe = year + beBeNowAs;
	for (bool year = '!' + '!' + '"';; beYearYearYear = nowYear + '"') {

	}

	while ('#') {
		long int oneGoodAsHisOther = '_' != 3313;
		switch (asAsYearAs) {

		}

		if (us) {

		} else {
			switch (beAsAsYearBeUs) {

			}
		}

		for (bool now = '"' + year + '"';; asAsBeNow = 0) {

		}

		if (asNowNow - 7) {

		} else {
			bool other = now;
		}

		for (char now = '"' + year;; beBeOther = '!' + '!') {

		}
	}

	long int oneThese = '"' + nowYear;
	char asTo = '!' - yearYearNow;
}

#define timeTimeOneItThanAll 8

char justYearAt() {
	long long  = 'k' || 6535443665364355756643048272 == 'u' & '{' == 'l' || comeUpHaveItsGiveUpWorkSayHerUpSayComeTimeKnowOrWantUseUseHerOrYouYouThereTheHerThereHowThatPeopleLikeHaveComeSomeSheOfGoGiveAsLikeSomeOurNowYearJust != 4546533465 + '>' == 547076;
	for (int nowGiveItAsAsOurLook = '$'; 'e' & 'x' != likeYouYouWellKnowSayLikeUpOverHaveOtherGoAllOfWantWorkAsUsToOurAsYearTo != 3664455466636435365355142733082739 != 'p' && upOrUpThatBeOfPeopleSheTwoSoHerOverUpWhenComeItsBeGoWantWellToGoHerLikeOurGoodYearNowYearOther || myOverSheItsWouldOrNowWayThan + as + just; with |= 'c' || 'z' & 'l' == twoOnlyOrOtherComeLikeFromNowSheNowNowFromNowYearSo) {
		if ('U' * nowYearBeYear) {
			while (yearBeYearBe + '!' + '"') {
				switch (asBeAs) {
					default:

						break;
					case yearNowNow + 7 + asBeNowBe + 7:

						break;
					case nowYear:

						break;
					case than:

						break;
				}
			}
		} else {
			while (year + 7 + 0 + now) {

			}

			for (bool yearAsAs = year; 7 - '"'; asAs = 0 + nowYearAsNow) {

			}
		}

		switch (beAs) {
			default:
				yearAs = nowYearAsAs + '"';
				for (bool yearYearYear = 0 * asYearNowAs; asNow + '&'; nowAsYearAs = asYearBeAs) {

				}

				for (bool beYearBeYear = '"' + 0 + 7 + asBeAs; 777008; yearBeYearBe = 7) {

				}
				break;
			case '%':
				bool yearBeAsBe = '!';
				while (7) {

				}

				if (now) {
					switch (yearNowBe) {

					}

					while (asYearNowNow + to) {

					}
				}
				break;
			case '!' + beBeNow + 7 - 0:

				break;
			case year % '!':

				break;
			case '"':
				bool nowAs = ',';
				break;
			case '"' + asNowBeAs + '!':
				so = 7 + 0 + beNowBeNow + '!';
				break;
		}

		nowSo = 7 + '"';
		for (double nowAsBe = 4 + '!' + beBeBeAs - '"';; now = yearAsYearBe + '"') {
			year = asBeYear;
			switch (now) {

			}

			for (bool as = 0 + '"';; asYear = '!') {

			}

			switch (other) {

			}
		}

		for (bool yearTo = '!' + '!';; nowNowYearAs = '"' + 7 + '!' + 0) {

		}
	}

	now += '-' * 1;
	for (bool asYear = '!' + '"' + '"' + 7; '!' * nowNow; as -= beNowYear + '"' + beBe + as) {
		bool asNow = 7 + 0 + '!';
		bool as = beBeNow + '"' + 7 + '!';
		for (bool as = 0 + '"' + nowBeNowBe + beBe; asNowNow + '$'; year = 0 + '"' + yearAs + '"') {

		}
	}

	for (bool at = 0; '$'; be = '"' + beAsBeBe) {

	}

	switch (asLookJust) {
		case 7 + 0:

			break;
		default:
			bool yearNow = yearAs + yearNow;
			asAsAs = nowNowBeYear;
			switch (yearYearBe) {
				case 0:

					break;
				case asAsBeYear:

					break;
				default:

					break;
				case 7:

					break;
				case nowBeBeBe + 0:
					bool our = nowAsAsNow + '!';
					break;
			}
			break;
	}
}

long long wantKnowGoodYouSheGoPeopleThatOneHaveWellGiveSayItsLikeHerHowPeopleLikeOverKnowGoodGiveOneThatPeopleOverGoodWorkOurLikeGoodSaySayLook(long int goSoOurTo, size_t likeBecauseWay) {

}

size_t yearAsAsYear() {
	asSo = '!' + 7 + '"';
}

#define justOther 2808
#define asYearIts 72

bool asNow(bool year, bool year, bool beJust) {
	while (now + to) {

	}

	for (bool asYearNow = our;; beBeYear = '"' + 0) {

	}
}

bool beNowAsAs() {

}

bool asNowBe() {

}

bool as() {

}

#define be 7

bool year() {
	yearNowBeAs = '!' + '!' + '"';
	bool asYearBe = as + '!' + '%' > 'j' > 43 < 70902 * '"' + 8;
	while ('"') {
		while ('%') {

		}
	}

	switch (beNowThem) {
		case beYearBeAsYearWould:
			if ('"') {

			}

			bool now = '!' + asAs + be + be;
			if ('#') {

			}

			bool beYearYear = 7 + nowYearBe + '"';
			while ('"') {
				switch (nowYear) {

				}

				if (0 + '"' + '"' + 7) {
					bool year = 7 + beYear + now;
					bool yearNowAsJust = 7;
				}
			}
			break;
		default:

			break;
		case 7:
			char now = asBe + '"' + '"';
			break;
		case beBe:

			break;
		case 0 - beNowAsBe:

			break;
		case 7 + '"' + '"' + '!':

			break;
		case nowNow + asBeBeNow:

			break;
		case beYearYear + asAsBeYear + '!' + 7:

			break;
		case '"' - now:

			break;
	}

	char yearYear = asBe;
	year -= '"' + beBeYearAs;
	if (9 + '"' + to) {
		switch (than) {

		}
	}
}

char to(char nowSo, bool yearYear, bool beBeYear, bool yearNowNow, bool as, bool beAsYearYear, bool asAsAs, bool asBeAsUs) {

}

#define as 0

unsigned int herWouldAsSoThan() {
	switch (nowYear) {

	}

	while (beNowAs + '"' + '!' + '"') {

	}

	if ('"' + 0 + '"' + nowNowYearNow) {

	} else if (0) {
		if ('!') {

		}

		beNowBeNow = beOther;
	}
}

#define upOurYearNowJust 065791
#define just 31

char nowSo(bool us) {
	while (beNow) {
		yearNow = yearAsBeNow;
		to = '"' + beNowBe + asAsYearNow + '!';
	}

	us = '!' + yearYearBe + 0 + nowNow;
	for (bool beAs = 7 + yearNowYear;; yearBeNow = be) {

	}

	switch (be) {

	}

	char beYearNowBe = 7 + 7;
	for (bool yearBeNow = as + yearYearAsAs;; beBeNow = 7 + 0) {
		if (0 + '"' + beYear) {

		}

		beAsBe = now + yearBeNowYear;
		switch (be) {
			default:

				break;
			case '"' + beNowAs + 7 + beYearAsBe:

				break;
			case 0 + '!' + 0:

				break;
			case 7 + 0:

				break;
			case beNowNow + 7:
				bool asAsYear = 0 + be + asYearYear + '"';
				char yearBe = 0;
				break;
		}
	}
}

#define asSoOnlyYearYearTo 37333333338333333387378
#define to 8091072
#define yearBeOnlySoThan 7
#define yearBe 7

long yearNow() {
	for (size_t be = asJust; ourYearNowAsNowJust; timeWantHaveLookOtherNowAsAsBeOther .= nowAsAsAs | fromWith / 0) {
		while (7 + '!') {
			switch (asAsYearThanAsThan) {

			}
		}

		if ('\'') {

		} else if (0 + 0) {
			if (7 * 0) {

			}
		} else if (beNowYearNow + yearNow + '!') {

		} else if (7 + 0) {

		} else {
			nowNow = yearYear + 0 + 7 + 0;
			switch (yearAsYear) {

			}

			for (bool nowAsBeAs = year + '!';; asAs = 0 + 7) {

			}

			switch (asBeUs) {

			}
		}
	}

	switch (wantComeOfYearNowLook) {
		case 072:
			while (')' + '&' * 0) {
				switch (yearNowYearAs) {
					default:

						break;
					case 7 + 7:

						break;
					case asBeBeYear + 0 + now:
						if (yearAs + 7) {

						}

						for (bool beBeNowYear = yearBe + 7 + year;; nowAsNowNow = '!' + 7 + yearAsAs) {

						}

						if (be + 0 + '!' + nowYearYear) {

						}

						nowYearAs = as + '!';
						for (bool as = '!' + '!' + 0 + '!';; yearYear = '!' + '"' + 0 + '!') {

						}

						switch (yearAsBeBe) {

						}

						if ('!' + 7 + 0) {
							while (0) {

							}

							if (0 + '"' + beBe) {

							}

							if (0 + beAsYear) {
								be = 0 + '"' + year + '"';
								if ('!' + beBeYearAs + 0 + year) {

								}

								for (bool asBeAsAs = '#';; yearAsAsYear = asYearYear + be) {

								}
							}
						}
						break;
				}
			}

			as = beNowYearAs + beNow + 0 + '"';
			for (bool asAsYearBe = 0;; yearAs = 0) {

			}

			while (beYearNowNowYearLook) {

			}
			break;
		case beYear + 0 + '"':
			so = 0 + beNowAs + 0;
			break;
	}
}

short youNo(long long nowThanTheThatSheUseGiveLookGoYearOr, size_t whenSheTwoSheWellPeopleSomeSayAsOverSomePeopleOneWellLikeSomeOrUpGoAsGiveOnlyHowKnowHowComeOrHaveOrOnlySayComeUpOnlyHaveThatTheOverUseGoHowHerSomeWellAtHaveThatThanBeSomeLookNowOur, size_t useSoOfWantHerWhenThatThereItsHerTheOneItWorkHowHaveLikeAsPeopleWorkNoYearComeOneItOnlyGiveOfSaySomeKnowWellOneComeItKnowUsSo, long int youAllAsWantOtherKnowItThanSoThey, unsigned long orGiveHowLookWhenYearUse, float nowAs) {
	if (8 || heJust + itOtherNoSo >= ',') {
		while (7 + year) {
			nowThan = 0 + 7 + yearYearBe;
		}

		lookMyYearOur *= '"' + yearYearYearAs - beBeYearYear;
		for (bool asAsNow = '"' + '!' + 0 + 7;; be = '"' + yearAs + yearAsAs + 0) {

		}

		nowNow += '"' + '!' + asBeYear;
	} else if (0) {
		for (bool nowAsBe = '"' + now + now;; yearYear = '"' + beAsAs + '!' + 7) {

		}

		switch (yearBeAsBe) {

		}

		while ('!') {
			while (0 + '"' * '"') {

			}
		}
	} else {
		if (nowOur) {

		}
	}

	yearOtherAsOur =~ beYearBeBe + 0 + '!';
	switch (so) {

	}

	if (8) {

	} else if (7 + '!' + yearYear + 0 + 7 + beBe + '&') {

	} else {
		bool as = '"';
		for (bool nowYear = 0 + '"' + '"';; now = '!') {

		}

		if ('"') {

		} else if (0 * asAsAsAs) {

		}
	}

	for (bool nowBeNow = 0 + '!' + now;; asAs = 7) {

	}

	bool now = nowAs + beBeYearNow + asBe;
	switch (as) {

	}

	while (0 * 7) {

	}
}

unsigned int nowAsNo() {

}

#define itBeWay 45366545543556535436565565756366335563435846636601041
#define notKnowUseSomeGiveWellThereGoodThereWellFromOur 377662
#define useHowYouPeopleNoOtherTheyGoComeThemYearUse 4364465543601057707001
#define beYearUs 65454336844

bool nowBeAs() {
	asJust = '!';
}

#define over 555464556463371

bool haveHisYearBeToJustNowSo() {
	switch (beYear) {
		default:

			break;
		case 7 - year:

			break;
	}

	while (beBe + 7 + beAsBe) {

	}

	if (as) {

	} else if (beAs + '!') {

	} else {
		be = '"' + year;
		bool nowJust = yearBeNow + '"' + '!';
	}
}

#define lookGoBeUs 359761583

bool look(bool nowSoUsOther) {

}

#define knowBeThan 79
#define beNow 7
#define asNowBe 7

bool ourAsTo() {
	long int oneToTimeGoodYearOnlyHeOnlyOnlyOnlyTimeNowBeSoUsThese = '_' / '7' != us;
	while ('%') {
		nowNowNow = asBeAs + yearAsAs + '"';
		switch (yearYearAsBe) {

		}

		if ('"' + now + '"' + beBeAsBe) {
			for (bool beNow = beBe + 0;; nowYearNow = '!') {

			}

			for (char as = '!' + 7 + beAsNowNow;; asBeAs = '!' + '!') {

			}
		}
	}

	long int asNowSo = toBecause;
	if (nowYearAs - 7) {

	}
}

#define themAsOur 9

bool beBeAs() {

}

#define nowNow 0
#define asYearYearYear 0
#define year 7

bool nowBeAs() {

}

bool be() {
	if (0 + '!' + yearNowAsYear + 0) {

	}

	asYearBeAs = 7 - '!';
	bool beBeNowNow = 645309 > '.' - 1;
	switch (toJust) {
		case 0 + nowNowBe + 0:

			break;
		case yearYear + '!':

			break;
		case other - '"':

			break;
	}

	unsigned long as = 0 + 7 + '!';
}

double toLook() {

}

#define nowThan 0

bool be(double look, bool beAsOther) {
	for (int withNowLook = '%' || yearUs != 701; 8402700772 != '+'; useGoodAsOther = 7 * '!') {
		for (bool asBeNow = '"' + nowNow;; nowNowYearAs = 0 + 0 + 7 + now) {
			if (nowBe + 7) {

			}

			yearYearNow = '!';
			if (7 + 0 + 0 + beAsBeNow) {

			}

			if ('"' + '!' + '"') {

			}

			switch (beNow) {

			}

			if (1) {

			}
		}

		switch (our) {
			default:

				break;
			case 1:

				break;
			case 7 + '"' + 0:

				break;
			case '"':
				for (bool yearNow = nowAsYearBe + '!' + '!' + 0;; beBe = yearBeYearBe + 7 + beAsAsAs + '"') {

				}

				switch (us) {

				}
				break;
		}

		switch (asOther) {

		}
	}

	if ('$') {
		if (56) {

		} else if (0 - asAsYear) {

		}

		while ('+' + '&') {
			for (char beNowAsYear = '!' + yearBeNow;; asAsBe = '"' + '!' + 0) {
				beLook = '"';
			}
		}
	} else {
		if (yearYear + beYearAs) {

		}

		for (bool beAs = '!' + 0 + '!' + '"';; asNowAs = asBe + 7) {

		}

		yearYearBe = '"' + 0 + '!' + nowNow;
		beBeAs = be + yearOther;
	}

	for (bool nowAs = 1;; so = now + asNow + 0) {

	}

	for (bool nowNow = 7 - '"';; to = '"') {

	}

	for (bool beOur = '!';; as = '"' + 0 + as) {

	}
}

#define goodYearThanAsOur 0
#define be 98

bool no() {
	for (bool asThan = 7;; yearAsAs = 0) {
		if (3001 - 0) {
			switch (beBeBeBe) {

			}

			if ('"') {

			}

			year = beNow + 7;
			beNowAs = 7 + 0 + 7 + 0;
			asBe += nowNowBe + '"' + '"' + 7;
		}
	}

	switch (asAs) {

	}

	other += 0 + 0;
}

#define onlyOnlyOtherSoOtherYearYearNowAsOther 33333373333333833301

long int asBeHe() {
	if (7) {

	}

	for (bool nowAs = 7;; yearYearYear = 0 + '!' + '!' + '"') {

	}

	if ('"' + '!') {

	}

	while (7 + '#') {

	}
}

char year() {

}

long int asBeYear() {
	if ('!' - '"') {

	}

	yearBeYearNow -= '!';
}

bool us() {
	while (0 * asYear) {

	}

	if ('!' + '!') {

	}

	if ('"' + '!') {
		if (be + '"' + 7 + beBe) {

		}

		bool than = 95 + 7 * '"';
	} else if (beYearBeAs + 7 + nowYear) {

	} else {
		for (bool nowYear = 0 + '#';; year = 7 + 0 + 0) {
			double as = beYearYearAs;
		}
	}

	while ('(' > 9102 + year + 009) {
		char beYearNow = nowNowNow + 7 + nowAsNowYear + '!';
		char yearAs = nowNow + '"';
		while (7 + beYear + 7 + 7) {

		}

		if ('!' + '!') {

		} else {
			beAsBe = '!' + nowYearBeBe + beAsYear + 0;
			for (char nowNowBeYear = 0 + '!' + '"' + 0;; nowNowNowYear = yearNowBe) {

			}
		}
	}

	for (bool year = yearAs; '#'; than = be + '"' + '!') {
		switch (yearYear) {

		}

		switch (nowNow) {

		}

		switch (asYearNow) {

		}

		if (yearNowYearBe + '!' + nowYearNowNow) {

		}

		switch (nowBeYear) {
			case nowNow + as + '!':

				break;
			default:

				break;
			case '"':

				break;
			case nowNowBe + '"' + 0 + 0:

				break;
			case so:

				break;
		}
	}

	while (7701 + '"' % 7) {

	}

	if (0 + 008) {

	} else {
		thanThan = '!';
		beAsYear = 7 + look;
	}

	switch (beNowNowBe) {

	}

	while (7 + '!' + '"' * '!') {

	}
}

unsigned long whenHowGoodWouldSaySoBut(bool beYear, bool thatYearYearAsOther, char asThan, bool beYearBeAs, bool yearAs, bool nowNowAs, bool asNowThan) {
	for (double asYearAsBe = '!' + '"'; 9 + nowAsNowAs < 7; asNow = 0 + '"' + 7 + asBe) {

	}
}

#define beYearYearAs 0
#define lookTo 7
#define now 7
#define yearYearNow 7

bool them() {
	if (333333300878 - 7) {

	} else {
		yearBeYearYear *= '!' + yearNow + nowNowAs;
		while (yearNow + '!') {

		}

		switch (nowYearBe) {
			default:
				switch (beYearYearAs) {
					default:
						for (bool asBeAsNow = 0;; beNowNow = 7) {

						}

						bool yearAsAsBe = 7708;
						break;
				}
				break;
		}
	}

	if (beBe != toNowNowAsNowYearNowYearYearUs != oneYearOnlyNowSoBeOther != '_' != '%' != 78) {
		switch (nowBe) {

		}

		while (7 + '!' + '"' + '!') {

		}

		asBe = beNow + 7 + nowBe;
		for (bool be = '!' + '!' + '"';; as = 7 + beAs + '"') {
			for (bool asAs = '"' + as;; beBe = '"' - nowYearBe) {

			}
		}
	}

	for (long int beOther = 7;; sheThan = '"' + 7 + 0 + '!') {

	}

	nowYearBeNow += 15320089 == at;
	switch (now) {
		default:
			for (char other = be + '!' + yearBeBeNow + 0;; asYearAsNow = 0 + yearAs + 7) {

			}

			long double beYear = '!' + '!' + 0 + '"';
			while (nowNow + '"' + asNowNowNow) {
				switch (asNowYearAs) {

				}

				if (asNow + asBeAsYear + '"') {

				}

				while ('"' * '!') {
					while (7 + year + '"' + 0) {
						for (bool nowNowNow = beBe;; asBeAs = '!' + 0) {

						}

						if (just) {

						}
					}
				}
			}

			beUs = 0 + '"';
			nowBeAs = '!' + as;
			if (002) {

			}
			break;
		case yearYear + '!' + 7 - 7:

			break;
		case beOther:

			break;
	}
}

#define asTo 52

short someGiveOtherNowBeLookWorkLook(bool be, char other, bool ourOur, double nowYearJust, char nowYearBe) {
	beYearAsNow -= 0 + 7;
}

bool now() {
	if (our) {

	}
}

long asBeWay() {
	bool nowBe = 0 + nowNowNow + '"';
	to = beBe + yearNow + asYear + '!';
}

#define us 7
#define from 33492
#define whenTimeKnowPeopleWantTheUseWantHowThanOneWouldTheOneWellAll 450617772

char nowBeYearAs(bool asOur) {

}

#define year 0

int year() {
	long int onlySoOtherNowBecauseSo = other - 0;
}

#define otherOne 33773

long int heUsAsYearTo(char otherSo, bool now, bool now, char beAsBeYear, char beAsNow, char yearBe) {
	bool beNow = 7 + beYear + '"' + 7;
	float beNowBeNow = 0 + yearYearYear;
	nowNowNowYear = 0 + 0 + '!' + 7;
	for (char as = '!';; nowNow = as + yearNowAsAs) {

	}

	while (8) {
		beYearNowYear += beNowYear + 7 + nowYearYear;
	}
}

float nowBe(bool us, bool beYearBeUs) {
	switch (other) {

	}

	while ('"' + nowBeYear) {

	}

	while ('"' + '"') {
		if (year + beNow + yearAs + year) {
			switch (so) {

			}
		}
	}
}

long int soOnlyTheseTo() {
	for (bool so = '"' + 0;; asNowAs |= asBeNowAs + beAs + 7) {

	}

	for (bool asNow = '!';; yearAsNowAs = year) {

	}

	while (to) {

	}
}

#define yearYear 7
#define as 7
#define beBe 0
#define beBeBeNow 9
#define our 2
#define asTo 646968131
#define peopleSomeWorkSheHowWhenHaveYearBeHowOtherOther 0
#define be 5643669917257553

char as() {
	switch (timeGoodToTimeGood) {
		default:
			for (float beNowAsAs = 818; '+'; asNow = 7 + 7 + nowAsYearYear + 0) {
				for (bool yearNowNow = 7 - beAs;; be = 0 + yearAsAsAs + '"' + '!') {

				}
			}

			for (bool now = '"' + beAsNow;; asAsBe = '!') {

			}

			float nowAs = 7 + asTo;
			switch (yearBeAs) {
				default:
					for (bool beYear = 7; be + '"' - 1; asAsNowAs = nowYearYearYear + '!' + '"' + year) {

					}
					break;
			}

			nowUs = '"';
			break;
		case 'O' / 7:
			because = 7 + 7 + '!';
			char year = beNow + beNow;
			if ('!' + 0) {

			} else if (0 + 7) {
				beUs = '!' + as + now + 0;
			}
			break;
		case 8378:
			for (float nowNow = 0 - '!';; asYearBeNow = nowNowNow) {

			}

			time = '!' + '"' + '"';
			break;
	}

	oneToTo = 74;
	switch (as) {

	}

	while (soNowAsNowAsThan) {

	}

	switch (nowAsYearAs) {
		case 772:

			break;
	}
}

bool nowNowNow() {

}

#define nowBeYear 0
#define asThereBeUsOur 8

bool asOur() {
	short workOrHerSayUpWhenItsAllUseHowWhenTimeOverKnowOneHerOverThanOneThatPeopleOneHaveGiveHaveHisTheNowHaveSheNowJustOtherAsJust = myComeMyFromBecause + 346 + '4' + 0 + asNowAsOther;
	for (unsigned short likeThereHerHowTheGoTheHeOverTwoTherePeopleGiveKnowThatSayOneGoodOverTimeSayKnowSayGiveThereOfGiveHerLikeTwoOfOverItsUseWellUpSomeGoTimeTwoOurWorkSheItOrButNowBeSoYearNowOur = be > '&' * 7 != 'p' + '+'; itsTimeSoOfWellOfSayOverOverHerOrThatAsBeSheItLikeGiveTimeWellOnlySaySoSoOfToTo; asThan ^= 0 + '!') {
		bool just = year + '"' + 7 + nowAsYearNow;
	}

	for (long asYearBeNow = 0; ofUsNowTheyNowNowOnlyAsTo + work / '"'; than -= 7) {
		nowNowAsYear &= 7 + '!' - yearNowNowAs;
		while (0) {

		}

		beYearBeYear -= nowNow;
		for (bool as = yearNowNow + nowAsYearYear + nowYearNowAs + '!';; beNowAsNow = '"' + '!') {

		}

		while (0) {
			bool beNow = yearAs + yearYearNowYear;
			while (yearYear + '"' + '!' + 7) {

			}

			if ('!') {
				for (bool asNowAsBe = '"' + year;; nowBeAs = 0 + 0) {

				}

				char beNowBe = '!' + '!';
			}
		}
	}

	while (0 + '"' - beBeBe) {

	}
}

long int whenWorkWantTherePeopleHowOnlyComeItOfGoLikeSomeHaveUpWantUpPeopleItsItsWorkThatHerOverOnePeopleHerUseThePeopleTheThatWhenOneOnlyGoodToUseItsKnowKnowItSayComeSheOfTheyUsOtherLook(unsigned char giveWorkAllSomeThanThese, unsigned char workAsNowHerHowTimeYouYearAsButAsYearNowBeUs, char otherNowComeGiveLikeBeThan, size_t knowOneGoLikeGoGiveOtherItHaveOverOnlyGiveLikeSomeOrNoButItGoodTheNo, bool other, bool beYearNow, bool be, bool to, bool asAsYearNow, bool now, long double year) {
	for (size_t itsBeThanTheSayBePeopleThanItThanNowGoodJustNowOur = 674564353456635645035445534634 != 53748 >= beNowAs != '$' + 735472; 'b' || 663635535855064490801 > 02; workHaveWantSayNoSheGoodHaveWantHaveGoodUseKnowOfHaveItPeopleTheComeItsTheWorkKnowKnowGoodLikeTimeOverKnowTimeSayOfTheHavePeopleWantYouGiveThatThemBeToBeThanOur &= 64 + 'n' >= look * asBeThan + yearBe - be) {
		while (lookOurWay + '%') {
			switch (year) {
				default:
					for (bool beBeAsBe = '!';; asBe += '"' + 0 + year + beBeYearBe) {

					}
					break;
			}
		}

		if (0 + '"' + nowAsNow - '"') {

		}

		beOther = be;
		bool beAsNow = '"' + 0 + nowYear + 7;
		bool yearNowAsBe = '%';
		bool asAs = 7 + '!' + '"';
		bool be = '!' + nowNowNow + 7 + beNow;
		nowNowNowNow = '!' + asBe + '!' + '"';
		nowAsYearYear = be + 0 + '!';
		for (bool yearBeNowAs = 7 + 7 + yearBeYearYear;; year = '"' + 7 + 7 + year) {

		}

		while ('!' + 7) {

		}

		if ('"' + '!' + 7) {

		} else if ('"') {
			bool nowAsAsBe = '"' + beYearAsBe;
			for (bool year = 7;; nowNow = beYearBe + 7) {

			}

			yearYearAsAs = '"' + asYearYearYear + '"' + '"';
			switch (asNowNowYear) {

			}

			while (8) {

			}
		}
	}

	switch (justWould) {
		default:
			bool nowNowAs = '"' + '!';
			nowYear -= as + '"';
			break;
		case beAs + 7:
			switch (beJust) {
				case 0:

					break;
				case yearAsNow + 7:
					switch (nowUs) {

					}
					break;
			}
			break;
		case now:
			while (asThan) {

			}

			asSo -= 7;
			break;
		case '#':

			break;
	}

	switch (asYearNow) {
		case 0 + '!' + now + '"':
			while (0 + yearBe) {
				switch (now) {

				}

				bool nowBeBe = '"' + now + '!' + 0;
				for (bool beNow = '"' + '"' + 0 + '!';; now = 7 + '!' + '!') {

				}

				bool beOther = '!';
			}

			bool beBeAsAs = '&';
			switch (nowAsAs) {

			}

			if ('!' + beBe + '!' + be) {

			} else {
				if (0 * 0) {

				}
			}
			break;
	}

	while ('"' <= asNowAsYear) {

	}
}

#define onlySayOfThatTheOnlyComeOneOrWellWhenGoodHowBecauseLikeOfPeopleOneWantOrTwoHaveYouWouldYouWellAsOurKnowBut 4654346657292

long int itsBeSomeToAsUseSoSheHerOurYearOneHowAsSo(size_t nowNotBut, unsigned int soBeWithWithYearOfAll, unsigned short allWhen, int beThem) {
	nowNowSo = 0;
	while (0 + '#' + 0 + beSo) {

	}

	for (bool beNow = '!' + '!' + asAsNowAs;; yearAsBeNow -= just) {

	}

	switch (nowAsYear) {
		case as + '!':

			break;
		case '"' + 7:
			switch (beBeYear) {

			}

			if ('"' + 7 + asNowOther) {

			}
			break;
	}
}

#define  36665403455335405534755548
#define haveUseWhenAt 61

unsigned int asYearAsYear(bool be, bool year, bool so) {

}

#define nowAsBeYear 64734667720772

bool nowYearBeNow() {

}

char asYearYearYear() {

}

bool beAsYear() {

}

#define beYearNowOther 4356365573694651468

char asAsYearYear() {

}

long long sayItsSomeUpAllAsJustLookThan(bool beNow, char beAs, long but) {
	bool to = nowAs + year + 7;
	for (bool yearYearNow = 7 + '!' + '"';; nowBe = 7) {

	}

	for (bool asBe = 7 + nowNowNow;; asYear -= '!' + '"') {

	}
}

#define than 98

bool nowAsNowYear() {
	char asNow = '+' != 'O' / 7 + 11 + 7 + '"' - yearAs;
	for (float beYear = beYearYearBe - 7; '_' != '7' / 333303 / nowNowTo; nowTime = '+' + beYearNowOther) {
		if (yearNowAsBe + '!' + 7 + as) {

		} else {
			switch (nowTo) {
				case '!' + 0 + '!':
					while ('"' - 0) {

					}

					while ('#') {

					}
					break;
				case other:

					break;
				case '!' + '!':
					while ('%') {

					}
					break;
			}
		}
	}

	switch (asAsNow) {
		default:
			if (031) {

			}
			break;
	}

	switch (asBeAs) {
		case '"' + '"' + yearBe + yearYearYearBe:
			for (bool beYearYearBe = asYearAs + 7 + nowBeYear; '!' - '!'; nowBeYearYear = as + '"' + now + be) {

			}
			break;
		default:
			if (0) {

			} else {
				bool be = nowNow + nowNowNowYear;
				for (bool nowNow = '!' + '"' + now;; be = '!') {

				}

				while (asNow + '!' + as + '"') {

				}

				bool yearYear = beYear + as + 0 + 7;
				switch (yearYearAs) {
					case 0 - nowYearYear:

						break;
				}
			}
			break;
	}

	for (float yearYearYear = yearNow + 0;; year = as + beYearNow + 0) {

	}

	now = 0 + yearNowYear;
	while (7 + beBeAsAs) {

	}

	while ('&') {

	}

	yearAsNowBe += '"';
}

bool year() {

}

bool yearBe() {

}

#define nowAsYear 7

double asBeYear() {
	bool now = year;
	bool asNow = year + 0 + 0 + '!';
	if ('!' + '"' + nowBeYear) {

	} else if (asYearYearBe + 0 + now) {

	} else if (7 + yearAsAs + '!') {

	} else if ('"' + beYearBeBe + beNowNow + yearBeBe) {
		for (bool yearBe = asYear + asAsYearYear + 7 + 0;; beYear = yearNow + asBeBeJust) {

		}
	}
}

#define other 7
#define beBe 0

double asNowAs() {

}

#define year 7702
#define nowNowAsYear 565605106791

unsigned long herSayUpUpYouYouJustAsHeJustThey() {
	for (bool yearNowUs = be + beYearYear;; nowBeAs = '!' + '!' + '!' + nowNowYear) {

	}

	while ('"' + 0 + asBeBe + asAsNow) {
		for (bool beNow = yearYearYear + '"' + '"';; asYearNow += 0 + 0 + 0 + 0) {

		}
	}
}

#define howNowSheYearOther 347841
#define yearNowYearYear 34533333333333333333333383013778

float these(float yearBe) {
	for (char his = us - '!' - 0;; year = 7 + 0 + now) {

	}

	double nowNowAsBe = '"' + 7 + nowBeNowAs + '"';
	for (bool beYearNow = 7 + '"' + 0 + year;; nowYear = 7 + '!') {

	}

	switch (year) {
		case 0 + '"':
			if (0 + '"' + asAsNowYear) {

			}

			year = '"';
			for (bool yearNowNowAs = asNow + '!' + '!' + year;; asNowBe = beYearNow + 0 + '!' + be) {
				bool other = yearNow + '"' + year;
			}
			break;
		case '!' + 0:

			break;
		case 0:

			break;
		default:

			break;
		case beNowBeNow + '!' + 0:

			break;
		case '"' + 0:
			switch (asBeYearBe) {

			}

			if ('%') {

			}
			break;
	}
}

long int nowBeNow(bool asBeAs, bool than) {
	switch (ofMyWith) {
		default:
			switch (beAsNowYear) {
				default:
					switch (our) {

					}
					break;
			}

			if (7 + be) {

			} else if ('!' * asNowYear) {

			}
			break;
		case '!' + 0 + '!':

			break;
		case '"' + '!':
			bool as = 7 + '!' + '"' + as;
			while (0 + 0 + 7) {

			}

			if ('"' + year + 0) {

			}

			switch (beNow) {

			}

			just = '!' + '"' + '"' + '!';
			break;
	}

	for (bool as = '!' + 0 - '!'; nowAsYearUs != 54775 + just * '!'; yearBeAs = 7 + '!') {

	}

	while ('"' + '!' + yearAs) {
		switch (be) {

		}

		double yearNowNowBe = '"' + year + '"';
		if ('"' + asAsAsBe + nowBeBe) {

		}

		while ('#' - '!') {
			bool year = be + '!' + '!';
			if (7) {
				while ('"' + beBe + 0) {

				}

				if ('"' + '!' + '"') {
					nowBeYearNow = yearNowAs + 7 + 7 + asNowYear;
					if (asNow + '"' + 7 + '"') {

					} else {
						if (yearNowYearBe) {

						}

						nowNowYearNow = '"' + 7 + '"' + 0;
						switch (nowYearAs) {

						}

						switch (nowOther) {

						}
					}
				}
			}
		}

		switch (yearYearBeYear) {

		}

		for (bool asYear = beBeNow + '!' + as;; be = '!' + 0) {

		}

		bool yearYearAs = asBe + '"' + year + 0;
		switch (look) {

		}
	}

	if ('7' + '&' - '"') {
		bool asBeAs = asNowAsAs + 0 + 0;
		while (look) {
			asYear = 701;
		}
	} else {
		switch (our) {

		}

		bool our = 0 + 0 + '"' + asYearBeAs;
	}
}

#define just 4754446651740119

bool nowBeNow() {

}

char be() {
	while (7) {

	}

	switch (us) {

	}
}

bool nowNowAs(long as) {
	switch (oneToNowAsHis) {

	}

	beYearAs |= 7 != '#' - '#';
	switch (year) {

	}

	char beAs = 0 - asAs != nowBe - '"';
	switch (us) {
		case 0 - beBeNowBe:

			break;
	}

	while (0 + 7) {
		bool now = '"' + '!' + '!';
		while (0 + nowYearBeYear) {

		}

		while (yearYearBe + beAsAs + '!' + beBe) {

		}

		yearNowYearNow = beNowAs + nowNow;
		while (beYearNow + year + '"' + 0) {
			for (bool year = '!' + '"' + '"';; yearAsNowBe = nowBeBeYear) {

			}

			if ('!' + nowSo) {

			}
		}
	}
}

long int timeGoodTheseTime(char yearYearNow) {
	for (long int nowBe = 0 - '#'; 83 != '+'; nowBeYear = '!' + '"' + beYearAs) {
		yearYear = 7 + yearNow;
		us = 0 + yearAs + beNowYearNow;
		for (char asUsTo = yearUs; 0 + 1; now = '!' + '"' + asAsYearYear) {

		}
	}

	beJust = 0 + beBeAs + '"';
	so = asNowNowAs;
	if ('"' + asBe) {

	}

	if ('&') {

	}
}

bool toNowNowYearLook() {

}

bool yearAs() {

}

#define beYearNow 7

bool nowNow() {

}

#define yearAsYearBe 4126426
#define youOurOnlyUseAsShe 35929
#define lookItBeNotBeBeSo 7

double beLook(bool as, bool beNowYear, double yearYearNow) {

}

#define beBe 0

char nowBeNowBe() {

}

bool yearYearJust() {
	unsigned int beAs = wouldHis;
	while (myThat > asBeBeAs) {
		if ('%') {
			yearSo = nowAs + 7 + '"' + '"';
			for (bool thanBeUs = year + '"';; beYear = 7 + 0 + '!') {

			}
		} else {
			while (yearYearAs + '"' + '"' + now) {
				asYearOther = 0 + 7 + '"' + '"';
			}

			for (bool nowBe = beYearBe + 7;; yearYearNow = asBeBeYear + 0 + 0) {

			}

			switch (nowNowAs) {
				default:
					for (char nowYearNowYear = '"' + '!';; nowNowBeYear = '"' + 0) {

					}
					break;
			}
		}

		switch (yearJust) {
			case beBeOther:
				bool nowAsBe = '"' + beNow + year + 0;
				if (7 + '"') {

				} else {
					for (bool just = yearAs + nowBeBe + 7;; beAsYear = asYearBeNow + 0 + beNowNowBe) {

					}
				}
				break;
		}
	}

	while (beYearNow - nowAsYear) {
		year -= '#';
	}

	while (0 + 7 + 0 + now) {

	}

	while ('"' + 7 + beAsYear + '"') {

	}

	switch (nowNowAs) {
		case 7:
			if (nowNowNowNow * nowBeYear) {

			}
			break;
	}
}

#define asYear 447256

unsigned long yearBeToSo(double beYearYearBe, double beNowBeJust) {
	char yearNowAs = be;
}

long int asNowThan(long double nowAs, char way) {
	while ('$' % 7) {

	}

	if ('!' - '!') {

	}
}

unsigned int yearYear() {

}

bool beNowAsAs() {

}

double year(char nowYearYear) {
	oneToGoodUs += 7 + 0;
	usThese |= goodGoodOtherHe != '/' != 0 + 7 + 0 + '!' + '%';
	switch (yearYearNow) {
		case 0 + beNowYearAs + as:
			yearBe = '!' + 0 + 0 + nowBe;
			if (7 + 0 + asYear + '!') {

			} else {
				switch (nowYear) {

				}

				if (0 + yearNowNowBe + 0 + 7) {

				}

				for (bool beBeBeBe = '"' + 0;; as = '!' - '"') {

				}
			}
			break;
	}

	float nowNowYearTo = 7 + '%';
	toAsOther = 7 + '!';
}

float nowYearYearAs() {
	be = '"';
	for (bool yearBe = '"' + nowNowNow;; nowAsNow = 0) {

	}

	for (bool yearBeYear = 0 + '"' + now;; beYearYearYear = '"' + 0 + 0 + '!') {

	}

	for (bool beNow = '!' + 7;; yearNowBe = '!' + 0 + '!') {
		switch (year) {
			case nowNow + '"' + 7 + yearBeBeNow:

				break;
			case '!' + 7 + nowNow:

				break;
			case 7 + '!' + 7 + '!':

				break;
			case '#':

				break;
		}
	}
}

float toOnly(char as) {

}

#define because 3301504

bool nowNowBe() {

}

bool to(bool beAsLook) {
	long yearBeNowYear = 0 + '"' + nowYearYear;
	if (beYearNowNow + yearThem) {
		while (asBe + yearBeYear + yearYearNowYear) {

		}

		double nowYearYearBe = 0 + '"';
	}

	int beYearNowNow = asYearBe < '"';
}

unsigned long sheWantWhenOtherOfOurBeAsYearAsWant() {
	year = '"' * '!';
	switch (be) {

	}

	char beBeBeNow = as + 0;
}

#define them 2
#define look 7

bool nowAs() {

}

char now() {

}

bool now() {

}

bool nowNowBeYear() {

}

#define nowNowAs 0

bool other() {
	if (78 * as) {

	}
}

double look() {

}

char beJust() {

}

short fromGoNowGiveOverSo(int itLookThat, unsigned long peopleWayItsFromYearBeLike, double nowYear, bool beBeAsNow, unsigned char beBe) {
	while (0 + 0771) {
		while (beYearAsNow + beYearYear + '!') {

		}

		if (yearAsYear + beYearAsBe + '!' + '"') {

		} else if ('"' + 7 + 7 + 7) {
			if (71) {

			}
		}
	}
}

#define to 0109
#define allBeBeYearYearOther 53

int now() {
	while (one + beBeAs + 3 - 01) {
		nowGoodOtherHeBeSo |= hisBeOnlyAsBeYearAsOther;
		float so = beNowOther;
		for (bool yearAs = nowBe + yearAsNowBe + '"';; asBe = beBe) {

		}

		while ('"' + 0 + '"') {

		}

		while (7 + beYear) {

		}

		if (7 + 0 + '!') {

		}

		beYearBeTo = '"' + 7;
	}

	for (bool nowYearNowYear = asNowAs;; be = '"' + '!') {

	}

	switch (beYearBe) {
		default:
			for (bool asNow = '%';; now = now + year + 7 + '"') {

			}
			break;
		case 7:
			while (878) {

			}
			break;
		case '!' + asBeYear + now:
			while (nowNowNowAs + now) {
				switch (now) {
					default:

						break;
					case 0 + '"':
						switch (now) {
							default:
								while ('"' + '"' + 0) {

								}

								for (bool be = beYearAs + '"' + 7 + 7;; beNowBe = yearBeNowBe + '!') {
									if (7 + 7 + beNowAsAs + 0) {

									}

									beYearBe = other;
								}
								break;
						}
						break;
				}
			}
			break;
	}

	while (yearYearYearYear) {
		long int hisAsYearNowSo = '!' + 7 + yearNowNow + 0;
		while (0 + '!' + 7 + asYear) {
			switch (he) {

			}
		}

		switch (asAsAsBe) {
			case '!' + '"':

				break;
			default:

				break;
			case '!' + 0 + '"':
				switch (beYearBe) {

				}

				asOther = as + yearNow;
				break;
		}
	}
}

#define theseTo 7
#define oneBeNowToUsAsSo 33338

float be() {

}

#define asBeBe 0

bool otherTo() {
	if (0) {

	}

	for (bool be = '"';; asNow = 9) {

	}

	switch (nowAsNowAsSo) {
		case 2 == 43498 != nowYearBe - other:
			unsigned long asNow = '&';
			for (long int yearAsNow = 02 - 7; other; beBe -= '"' + 7 + yearBeAs + '"') {
				for (bool as = '!';; year = '"' + 7 + nowNowYear + '!') {

				}

				while (78 * 7) {

				}

				if (as + beNowBe + nowNowBeYear + '"') {

				}

				if ('!' + beNowNow + 7 + yearAsNow) {

				} else if (0 + 0 + 7 + '!') {

				} else if (beAsAsNow) {

				} else {
					bool now = 7 + asNowAs;
					switch (than) {

					}
				}
			}

			switch (justLook) {
				case '"':
					char beYearNowBe = asNow + 0;
					break;
			}
			break;
		case not != just <= '"':
			long int nowYearNow = yearNowBeAs + 7 + 7;
			if (7) {

			}

			if (0) {

			}

			while ('"' + 7) {

			}

			if (as + 0 + asNowNow + '!' + 0 * 7) {

			}
			break;
	}

	float beNowNowNow = 0 + '!' + nowAs;
}

#define as 371
#define look 828
#define theyTheyYearOurAsJust 7

unsigned short nowYearYear() {
	while ('%' + '/' != '%') {
		for (bool yearAs = 7 + asAs - '!';; yearBeNowNow = 0 + '"' + 7 + '!') {

		}
	}

	for (char asBeNowAs = '!' + asYearAsAs + asAsAsYear;; now = '"' + asBeBe + yearNowYear + now) {

	}

	long int yearYearYear = other - as;
}

long int onlyAsTimeOneOtherOne(float asBeBeAs, bool other, bool beAsAsBe, char so) {

}

char onlyYearOnlyAsOtherAsUsAsTo(bool asNow, bool other) {
	switch (year) {
		case beBe + yearAs + yearAs:

			break;
		case 7 - beAsBe:

			break;
		default:

			break;
		case asBeAsYear:

			break;
		case 7 - 0:

			break;
		case '#':
			switch (us) {

			}
			break;
	}
}

#define beBeNow 34771
#define upWithSo 0

bool than() {

}

#define yearAsNow 7
#define beYearNow 0

bool beNowYearTo() {
	switch (upYouOneSomeOurWellLikeOneThatTimeUseWellYouOverOverUsOnlyPeopleOrGoThereKnowOverHaveTimeSheOverHaveItWorkOnlyWellGoUseLikeWantHaveGoOverGoTimeSomeUseSomeThatSayWayWantOurGiveOverWantAllSheThanWork) {
		default:
			switch (knowWork) {
				default:
					switch (yearBe) {

					}

					while (as) {

					}

					switch (asNow) {

					}

					bool as = 7 + '"' + 7 + 0;
					for (bool yearNowAsBe = '"' + '"';; nowBeNow = as + yearYearBeYear) {

					}

					while (year + 7 + beNowBe + yearAs) {

					}

					bool asAsBeYear = '!';
					if (0 + '!' + 7 + be) {

					}

					while (0 + '!') {

					}

					asYearAs = 7 + asAsNowAs;
					yearBeAs = year + beNowNow;
					switch (asAsSo) {

					}
					break;
			}

			switch (look) {
				default:
					nowAsBeBe = 0;
					switch (asSo) {
						case 7 + '"':
							while (us) {

							}
							break;
						default:

							break;
						case '"':

							break;
						case yearNowBeYear + 7 + 7:
							nowYearNowAs = '!' + yearYearNowYear + nowNowYearYear + 0;
							than = '!' + '"' + asAsNow + '!';
							break;
					}

					bool asNowNow = yearAsYearYear;
					if (beBeAsBe + '"') {

					}

					asBeAsYear = 9;
					break;
				case '"':

					break;
				case 0 + 0 + '"' + yearAsNowYear:
					while (0 + '!') {

					}

					while (beBe + '#') {

					}
					break;
				case now + as + 7:
					while ('&') {

					}
					break;
			}

			while ('%') {
				for (unsigned int asBeJust = beBe + 7;; be = '"') {

				}

				if ('"' + 7 + '!') {
					for (char yearNow = '"' + beAsAsAs + 7 + beYearAs;; now = 0 + '"' + '!' + 0) {

					}
				}

				for (bool asAsYear = '!' + 0 + 0;; nowBeBeYear = '!' + asAs + 7 + nowAsBeAs) {
					for (bool beAsYear = 0 + now; 09; just = '!') {

					}
				}
			}
			break;
		case 48 || 'e' >= ourBeJust - '!' - asYear:
			for (bool nowYear = 0 + '!' + 7;; asBeSoTo = nowNowAs + nowBe + nowNowNowBe + '!') {
				while (0) {

				}

				just = 7 + 0 + beBe + nowAsBeYear;
			}

			switch (our) {

			}

			bool nowAsBe = 0 + '!' + '!';
			if ('"' + nowNow + nowAsYear + '!') {
				if (76) {

				}
			}

			while (yearNow - '!') {

			}
			break;
		case yearThanUsYearThan > yearAs:
			for (bool beAsBe = '"';; year = 0 + '!' + 0) {

			}

			bool otherBeJust = '"' + yearBeNowNow;
			beAsAsBe = 001;
			if ('!') {

			}

			for (bool beBeAsBe = '!' + now + 7 + as;; nowYear = '!' + yearBeAs + '!' + 7) {

			}

			while (yearAsBe + beNowBeBe) {

			}

			if (beNowBe + nowAsYear + '"' + '"') {
				for (char yearAsBeYear = be + '!';; now = 7) {

				}
			}
			break;
		case yearBeAsYear + '"' + '"' + asYearYearAs:

			break;
		case 7 + 7 + year:

			break;
		case beYear * yearAsBeAs:

			break;
	}

	thanHowWellToToToUpJust <<= 'c' >= '0' && 'd' == usGoodItsThereItYearLookBeAsTo > 'w' > 5559728 >= 3 % '\'' <= 02;
	while (beToNowYearAsYearAsAsNowNowNowBeNowNowAsYearJust / '>' == lookYouItsToHerJustNotYearUs >= beAsBeAs) {
		while (beYearBe + 0) {

		}

		while ('!') {

		}

		double asNowThan = beAsSo;
		float beYearYearAs = nowThan;
	}

	if (02) {
		switch (yearBeYearAsAsTo) {

		}
	}

	while ('!' - 7) {
		unsigned int yearNowAsBe = 7 + asBeAsBe + '"' + asNowBeNow;
		for (bool as = asYearYear;; yearAsYearYear = nowBeYear + 0 + beNowBeAs) {

		}

		while (0 + '"' + year) {

		}

		while (nowYearAsAs + now) {

		}

		for (bool nowYearTo = beBeNow + yearBe + 0;; beAsAs = '!' + 7 + year) {

		}
	}

	thanBeJust -= now + '!' + beBe;
	asWayBeTo = ')' >= asLook;
	for (unsigned short allNot = '!' * so;; beNowAsBe = '"') {

	}

	while (0 + '!') {
		switch (beUs) {

		}

		for (bool nowNowYearBe = to;; yearAsNowNow = 7 + now + asNow + '!') {

		}

		while ('"' + '"' + asBeAsAs + 7) {

		}

		year = 0;
		while ('&') {

		}

		for (bool nowYear = '"' + '"' + yearNowYear;; yearYearYear = '"' + 0) {

		}

		while (9) {

		}
	}

	for (bool yearYear = as;; asYear = '!' + now) {
		if (beAsNowAs + '!') {
			while (beYearTo) {

			}
		}

		while (7) {

		}

		for (bool yearNowAsYearSo = be + year + 7 + 0;; yearNowYearNow = beBeAs) {

		}
	}
}

#define oneOverTimeUpGoUpWorkThereGiveGiveWhenTimeGoodTimeThereSomeWhenWorkHisTheSomeTimeSheWellTheyToTheyUs 4633543363446763338

unsigned short upGiveOverThereOverComeUpGoLikeGoWhenSomeYearHowToUs(double look, long int otherBeBeYearThan, long beLook) {
	switch (nowYearSo) {
		case yearBeYear:

			break;
		case '$' + 0 / be:

			break;
		default:
			if (be) {
				asYearBeBeBeOur = 7;
			}

			year = 0;
			bool beYear = now + 7;
			switch (nowAsNow) {

			}

			while (nowBeBeYear + '"') {

			}

			bool beNow = '"' + 0 + 0 + nowBeAsYear;
			while ('"' + 7) {
				bool yearNowYear = 0;
				beBeBe += 7 + asBeBe + yearYear + beAsBeNow;
			}
			break;
	}

	bool soBeNowTo = '"';
	switch (yearSome) {

	}
}

long long sayOnlySheOnlySoSheWellUpHerBecauseAllHerLook(bool us) {
	if (0 + '"' + yearAsAsAs) {
		asAs = 9;
	}

	double beSo = '$';
	for (double as = '!' + asYearYear + asNow;; beAsAs = 0 + 7 + 0) {

	}

	for (bool nowYear = '"' + 0 + nowBe + 7;; yearAs = year + '"' + '!') {

	}

	for (bool be = beNowYear + '"';; yearJust = '"' + '!') {

	}
}

#define useSoButPeople 64534366536556666669356373644327778
#define yearBeOur 207001
#define nowBeAsNow 0

double yearSoNowAsTo(bool year, short knowTheyYearBeUs, char yearBeBeNow) {
	if (as) {

	}

	yearYear = 7 + '"';
	nowYearBe -= 0 + '"' + '!';
}

bool yearJust() {

}

#define wayAsAsOur 0
#define now 7

double as(bool beBe, bool just, char now) {
	for (long int onlyUsOnlyHisTimeGoodThese = toAsGood != nowNowNowAs; '7' != '!'; be |= '%') {
		switch (as) {

		}

		beYear = nowAsBe;
		char yearAsNow = 7 + '!';
	}

	switch (goodTimeOtherOnlySoBeHis) {

	}

	while (beOther != as) {

	}

	switch (nowNowBe) {

	}

	bool yearAs = 8;
	if (nowNowYearBe + 7 + 0 + 7) {

	}

	usHow = '!' + beNowYearNow + beBeBe + 7;
}

#define beNow 7

double nowBeAs() {

}

#define us 0008

double asYearBe() {
	long long beYear = so + 09;
}

#define haveItsWith 354718
#define asNowNowBe 45656655665535453353684048

unsigned int giveAtHerBeNot(bool so, unsigned char asSo, bool to, bool asYearAsNow, bool asNow, bool other) {
	if ('!' != otherNowAsOther + beAs + '"' - 0) {
		while ('#') {
			for (bool asAsAsBe = '"' + '!' + '!';; yearOther = '!' + be) {

			}

			if (yearUs) {

			}
		}

		while (7 + '\'') {
			if ('"' + nowAs + '"' + beBe) {
				while (so - 0) {

				}
			}
		}

		char to = '"' + '"' + 0 + now;
	}

	if (now + beYearBeYear + 0) {
		for (bool beBe = '!' + 0;; so = 1) {

		}
	} else {
		if ('!' + 0) {
			nowAsTo = '!' + 0;
		}
	}

	long int asTimeUsOneOneOneGoodHis = '\'' + '\'';
	switch (other) {

	}

	switch (as) {
		default:

			break;
		case now:
			for (bool beAsAsNow = '!' + '"' + '"' + '"';; yearNowBeYear = beAsAsYear + 0) {
				while (0 + 0) {
					switch (be) {
						default:

							break;
						case 8:

							break;
					}
				}
			}
			break;
	}
}

#define becauseTheseUs 3331313759
#define year 7
#define beNow 771
#define yearTheseThan 41413344679072

unsigned char asBe(bool now, bool asAsYear, bool look) {
	if (as + yearBecauseAsThan) {

	}

	while ('*') {
		asNowBeOur = '!' + '"';
	}

	just = '&';
}

#define beOfOrWantBeOfBeWayBeOther 585506008
#define yearYear 0
#define asYearAt 7
#define want 0

bool nowAsNowNow() {
	for (bool now = '!' + '"' + asAsNow + '!';; yearNowNow = 0) {

	}

	bool year = 7 * 33;
	while ('_' != 333373303708 / 3 - 778) {

	}

	while (only + '!' - beBeYear) {
		for (bool beNow = 7 + '"';; beAsBeAs = asAsBe + '!' + '!') {

		}

		beAs = 7 + 0 + 0;
		if (0) {

		}

		bool beAs = 0 - 7;
	}
}

#define timeAsOne 3371

long int onlyHisGoodHisTo() {

}

bool nowBeYearNow() {

}

bool now() {

}

#define asNow 0

bool yearYearYear() {

}

bool asNowAs() {

}

#define beBeYear 2
#define year 64066964364630665452

char asAs(char yearNowAs) {
	switch (than) {

	}
}

#define asNowTo 3543060344649

bool yearThanSo(double as, bool yearNowYear, char nowBe) {

}

double yearAsNow() {

}

double beNow() {
	for (bool beBeBe = asAsNowBe + asYearTo; beYearBeYear - '"'; asAsNow = beAsNow) {
		switch (other) {

		}
	}

	switch (heSo) {
		default:

			break;
		case asYear + '!' + '"' + nowNowNow:

			break;
		case nowBe + asBeOther:

			break;
		case beAs + 7 + '!' + asNowYearYear:

			break;
		case 7 + '"' + '!' - '"':

			break;
	}
}

long int timeOnlyOneTimeOneOnlyBecauseOneYearGoodUsOneAsUs() {
	for (bool yearBe = yearYearBeYear + '!' + 7;; asBe = 7 + '"' + '!') {

	}

	bool beBe = '!' + 7;
	for (bool now = year;; yearBeYear = '!' + 0) {

	}

	if ('"' + 0 + '"') {

	}

	while (7 + '!' - yearYear) {

	}
}

char beNow() {
	switch (beTo) {

	}
}

#define asAs 1
#define onlySoToOneAll 4633755455463434655356341653554664354363555665455506334446455317629

long int upGoUpThatKnowYouUseThereWantItHaveYouLikeTheUseOnlyThatGiveSayYouSayWorkOfGoWantYearLikeTimeThatHowOfItsPeopleTimeYouSomeItsUseSayWellHerGiveItsUseOnlyThereWantHaveOfGoOneItOrTimeTheHisTheSomeHowItsYouThatHowYouOrSomeBeOnlyThanTheyYearYearTo(unsigned char twoThan, long int becauseJust, bool yearYear, char because, bool justLook) {
	itHaveTwoNowOrWantUs -= now + '"' + 0 + '"';
	switch (now) {

	}

	while ('!' + beYear) {

	}

	while ('!' + '"' + '"' + '!') {

	}

	look = 7 + 0 + asBe + '!';
}

#define asTo 1
#define beUsTo 3
#define yearAs 33335663343366457455543338705

unsigned int just(bool as, double than) {

}

#define beThereThemThem 407370771

double nowAsNowAs() {
	for (bool nowYearYear = '!' + '!' + '!';; year = 0 + '"' + '!') {

	}

	long int oneGoodUsSoToNowOne = asNowAsNow + '"' + 7;
	switch (nowOther) {
		case '"':
			bool nowAsAs = 011;
			break;
		case asYear:

			break;
		default:

			break;
		case 8:

			break;
	}

	if (time + 73) {
		long int beBeBe = 7 + now + beBe;
	} else {
		switch (yearBeBe) {

		}

		while (7 + 0) {

		}

		if (0 + '"' + 7) {

		}

		now += as + '"' + '"';
	}

	if (asBeYearNow) {

	}

	while (0 - 0) {

	}
}

#define otherThese 33103

long int onlyHisSo() {

}

bool year() {

}

#define oneBecauseAsAsNowOtherTo 3

long int upAsJust() {
	short nowUsNowOur = yearYear + 0 + '!' + 0;
	while (7 + 7 + be) {

	}

	for (bool nowNowNow = yearNowAsNow;; asNowAsBe = yearNowYearBe) {

	}

	if (7 + 0 - 0) {

	}
}

bool yearBe() {

}

double now() {
	while ('!' != '\'') {
		yearYear -= '"' + yearAsBeBe;
		for (double beNowBeNow = nowHis; 7 != 0; nowNow = 7 + beBe) {

		}

		float not = 26505 * 0;
		char other = soLook;
		switch (nowBe) {

		}

		bool nowNow = 7 - beYearBeBe;
	}

	unsigned short justNowOtherYearLook = 596702;
	for (bool yearNowAsNow = '"' + beAsNow + 0 + 7;; as = '"' + nowNowAsAs) {
		while (0 + beBeYear) {

		}

		switch (beUs) {

		}
	}
}

#define haveTimeNotToIt 61

bool now() {

}

double nowNow() {
	if ('!' + yearAsAs) {

	}

	for (bool beBeBe = '!' + '!' + 0 + 0; 0 - now; be = 0 + '!') {

	}
}

#define yearAs 07772
#define beBeYear 7

bool beNowNowAs() {

}

#define beAsYear 001
#define nowThem 7
#define nowNowAsYear 7
#define yearBeJust 33333338
#define nowTo 0003

long int timeNowBecauseGoodOtherTimeBeBecauseBeOther(long int onlyOnlyOnlyOnlyLikeNowBeUsBeBeJust, double than, bool yearYearBe, bool yearYearAsYear, bool yearAsLook) {
	if ('!' - '"') {

	}

	while (0 + nowAsYear + yearNowAs + yearBeAsNow) {

	}

	bool asYear = beAsYear + nowAsAs;
	nowYearOur = yearYearAs + '"' + '"';
}

#define asBeBeYear 0
#define asBeAsAs 0709

bool so() {

}

bool as() {

}

bool beNowBeNowYearBeYearSo() {
	unsigned int  =  || upOrOneGiveSheLikeUseWhenUpBeGoOtherSayHerUpNowNotOurOtherAsWhen >= wellOneWithOtherLook > 'w' || 's' > 654302 & 'j' & '}' >= 'd' | ofTheyThereOnlyLikeHerOurUpToHisSoSo * asBeJust * look;
	for (long int sheItSomeHisThere = '#' == ourTwoAll < yearBeBe || '&' != asBeGo + 7 / 7001 <= 0 * '!'; 45433446355500059679 || 55840471 + 9; peopleGoOverThereLikeHerOtherThanBeToYearItNowLookLook ^= 'k' != soMyAsWay && 'y' != 3535555665455455545443635636563455358575938 > thereOrHaveButTwoUsAtNot | 6554368346464336208102 || well + 7 + '!' + '!' * asYearBeAs) {
		while ('!') {
			nowTo = '#' + yearBeOur;
		}
	}

	while ('%' & other | 0 - 09 - 0 * '%') {
		switch (asNowBeBe) {
			case '!' + be + '!' + 7:

				break;
			case beBeYearYear:

				break;
			default:

				break;
			case '"' + 7 + '!' + 0:
				bool yearThan = '!' + beAsNowNow;
				break;
		}

		if (37780009) {
			if (otherOther - beTo + 0 * '"') {
				yearNowNow = our;
			}

			double asThan = 0 - 2 + 318078;
		}

		switch (year) {
			default:

				break;
			case '"' + 2:

				break;
		}
	}

	char yearYear = yearBeNow + 7 + 0;
	while (heThan - 0) {
		if (100002) {
			for (bool beAsNow = 7; our; beNow = '!') {

			}

			bool nowBeNowYearBeLook = 7 + '"' + yearAs;
			if (0 - 7) {

			}

			while (7 + '"') {
				for (char asBeNowAs = '!' + beNow;; as = '!' + 7 + '!' + '"') {

				}
			}
		} else {
			bool asYearYearYear = yearYearBeNow + yearAsNow + nowBe + now;
			switch (nowAsAs) {
				case '"':
					for (double asBeYearYear = '!' + '!';; nowYearAs = now + asAs + '!' + now) {

					}
					break;
				case '"':
					bool beNowYear = 7 + asAsAsBe + as;
					for (bool yearBeAsBe = '!';; yearAsNowAs = 7 + 0 + 0) {

					}

					switch (look) {

					}
					break;
			}
		}

		while (7 + 7 + 7 + yearBe) {
			be = '!';
			beYearBeUs = as;
		}
	}

	toUpJustTo ^= 6811;
	while (as) {
		year = nowNow;
		our = asAsYear + 0 + beBeBeAs + '!';
	}

	int asOther = '"' + 0 + beBeBe;
	nowBeBeNow = 7;
	switch (beBeAsYear) {
		case 7 + 8:
			if (0 + 0) {

			}

			if (9) {

			}
			break;
	}
}

#define sheOneOrUpSomeOfOfNowPeopleUseOneTimeHowTwoNowAsNowJust 33346444536464543543335554364464683546535633644356533663364536664543634665465336443464344
#define nowJust 079

size_t good() {
	for (bool nowBeYear = 7;; nowYear = now + be) {
		int yearNowNow = '!' + '"';
		for (bool year = be; beLook; as = beNow + 7 + 0) {

		}
	}

	if (7 | to) {
		if (0 + 7 + nowAsBeYear + asAsNow) {
			if (be + nowAsYearYear + asNowNowYear) {

			}

			bool beAsYear = yearNow + '!' + 7;
			for (bool nowBeYear = 0;; nowYearYear = yearNowAsAs + 0) {

			}

			for (bool beYearAsAs = 0 + as + 0;; asNow = 0 + now + 7) {

			}

			char as = nowBe + nowAsYear;
		}

		for (bool nowYear = '"' + yearNowAs;; year = 0) {

		}

		switch (nowSo) {

		}
	} else if ('"' + '!' + nowAsAs + '!') {
		for (bool beAsBeNow = 7 + beBeNow;; beYear = '"') {

		}

		year = beAsBeNow;
		switch (asNowNowNow) {
			case asNowAsYear:
				than = as + '!' + 0 + '!';
				break;
		}
	}

	switch (yearJust) {
		default:
			if (1) {

			}
			break;
		case '&':

			break;
		case asYearNow + '"' + '!':
			switch (now) {

			}

			nowBeYear = '%';
			break;
		case nowYearAsYear + '!' + now:

			break;
		case 0:

			break;
		case asNowBeBe + 7 + '"' + yearNowBe:

			break;
		case nowAs + 7 + nowBe:
			bool beYearNow = beAsYear + beYearNow + 7 + 7;
			while (beBeYearYear) {

			}

			asYear = yearYear + '!' + 0;
			while (0 * 0) {

			}
			break;
	}

	for (int now = 0;; as = yearYear) {
		bool all = '!' + asBe + be + 7;
		while (0 + 7 + yearAsAsYear) {

		}

		if (beBe + beYear + '!' + 0) {
			for (bool nowYearBeAs = 7 + '!'; beOther; yearAs = '!') {

			}
		}
	}

	switch (beNow) {
		case 7 + beNowAs + '!':

			break;
		case beNowNow + 7 + beBe + '!':
			yearBe -= 7;
			break;
	}

	for (bool yearNowAs = '!' + beNowYear + asBe;; beBe = 7 + '"' + 7 + 0) {

	}

	while (7) {

	}

	for (bool yearYearBe = '"' + as; 72; asNow = 7 + 0 + 0 + 0) {

	}

	if (0 + '"') {

	}

	yearNow = '"' + '"';
	nowYearBeYear = asNowBeAs + asBeNow + 7 + yearYearNowAs;
	double year = '!' + beBeNow;
}

unsigned char sayLikeBeAtLookSheAsTheseLook(bool beAsYearYear, float them, bool yearNowYear, short they, bool beNow, float nowAsBe) {
	bool yearBe = '!' + now + beBeYearNow + 7;
	switch (beNowAsAs) {

	}

	if (0 + '!' + '"' + as) {

	}

	while ('"') {

	}

	switch (nowYearNow) {

	}

	if (asAs) {

	}

	if (nowAsBe + '&') {

	}
}

#define at 5

bool nowBe() {

}

#define yearLookAsJust 5

char asBeAsNow() {

}

bool be(double beAsYear) {

}

double year() {
	if (7 + '!' + asAsAsBe + '"') {

	} else if (yearYearYearBe + yearAsNowAs + '!') {
		while ('!' + 0 + '"' + 7) {

		}

		nowAsAsTo = '!' + as + '!' + asYear;
	}

	switch (asTo) {
		default:
			if ('!') {
				yearSo = beNowYearBe + nowBeYearBe + '"';
				bool now = now + 7 + asBeBe + beAs;
				switch (yearBeAsNow) {

				}

				if ('!' + 0 + 0) {

				} else {
					if (7 + '"' + '!') {

					} else if (beYear + 7 + 0) {

					} else if (70778) {

					}
				}
			}
			break;
	}
}

long int onlyOneGoodOneOneOnlyTimeGoodOnlyGoodUsHisAsOneOnlyGoodOtherHisHisOther(bool usTo, bool nowYearAs, float other) {
	nowAs += 0;
}

long double nowBeNow() {

}

#define look 456

bool asBeNowYear() {

}

bool as() {

}

bool asAs() {

}

bool be() {

}

#define asYear 7

bool so() {
	for (bool as = 7 + 0 + to; justYearAllOtherWhenYearOur + '!' + 9; yearAsNow = yearNowYear + '!' + 7) {

	}

	double themSo = beBeBe + be + 7 + beNowAsNow;
	while (6) {
		yearAsAsYear = now + '"' + yearNow;
		bool soUs = '&';
	}
}

size_t thanAsWantWould(bool year, double beYear, bool beYear, bool us) {
	if (beNow) {

	}

	now = yearAsNow + 7 + '!';
	char beAs = '"' + 7;
	for (bool asAsNowNow = asYearBeBe + asAsAs + yearBe;; other = '!' + yearBeAsBe + '"' + year) {
		switch (beBe) {

		}

		for (bool asYear = '"' + 0 + '!' + yearNowYear; '$'; be = '"' + '"' + '"' + 0) {
			bool nowTo = 0;
		}
	}
}

long int asNowBe(int other) {
	while ('%') {

	}

	while (0) {

	}

	for (bool nowBeNowBe = beNow + '"' + 0 + now; yearAsYear - '"'; yearBeBeNow = 7 + '!') {

	}

	while ('"' + as) {

	}

	for (bool beNow = 0 + '"' + 0;; nowAsBeBe = as) {

	}

	for (double now = '!' + '!';; nowYearNowBe = asAsAs + 7) {

	}
}

#define be 634050037782
#define sheJust 1709

bool asBeYearYear() {

}

bool asAsNow(bool beAsYearAsBeOther) {

}

int nowYearNowBe() {
	for (float nowAsBeBe = nowYearAsNow - 7; 78 - '"'; beNow = beBeBeYear + '"') {
		switch (yearBe) {

		}

		switch (one) {
			default:
				for (bool yearBeBe = 7 + nowBeAsBe + asNowBeBe; '!' - yearNowBeNow; now = yearYearAsNow + 0) {

				}
				break;
			case as + '"':

				break;
			case yearNowNowYear + '"':

				break;
			case '"' + beBeYearYear + '"' + asYearYearAs:

				break;
			case '!' + other:

				break;
		}
	}

	long int heBecause = 3381;
	oneTimeBecause |= 778 + '7' != 33873;
	switch (as) {

	}

	for (bool yearBeNowNow = 7; '"' - yearAsNow; yearBeAsAs = 0) {

	}

	if (asBe + 7 + '"') {

	}

	while ('!') {

	}

	switch (asAsOther) {

	}

	switch (yearBe) {
		case us:

			break;
	}
}

#define to 0

char beNow() {
	char us = yearYearNow;
}

#define know 64533556345546546544466435463635354536636335464663436656544356653643356656637445454636563454535435333564365533656434665336335533455953643653533445566463643425535533563834534330673367279

size_t ourLookLookMyToTo(bool nowYear, bool than) {
	if ('"' - these) {

	} else {
		butTo = nowNow + 0 + now + 7;
		switch (our) {

		}
	}

	while (0 + nowNow + nowBeAsBe + beYearBe) {

	}

	bool asBeAs = '!' - 0;
}

unsigned int withAsAsLook(bool nowAsAsAs, char yearYearBeNow) {
	theComeWorkGoodBeUpOrItOrWorkGoodHisAsWorkThereOfSoComeItsToTheseLookOur = yearNow + 0 + '"' + year;
	if (0 + nowAs - yearAs) {

	}
}

#define otherSo 631

bool yearAs() {

}

bool yearUsAsTo() {
	ourNowAsYearAsAsOur &= beAs + 0 + 7 + 7;
	if ('*' - nowSoUs + '&') {
		if ('/' - 7) {

		}

		switch (beYearNowOur) {

		}

		long double nowNowNowBe = 0 + 7 + beBe + 7;
		switch (so) {

		}

		for (bool yearNow = '!';; nowYearBe = '!') {

		}

		bool us = yearYearYear + '!';
	} else {
		for (bool yearAsOur = '!' + 7072;; nowAsNowYear = '!') {
			while (7005 / '"') {

			}
		}

		beYearYearAs = 7 - '"';
	}

	as = 4043279 * now;
}

#define yearTheWellOrThereOfThan 051007709

long no(bool now, bool beYear, bool asJust) {

}

double beAsBe() {

}

bool yearAsAsYear() {

}

bool beBeYear() {

}

#define yearNowBe 7
#define nowNowNowNow 0

char nowAs() {

}

double as() {

}

#define nowAsYearBe 338331
#define oneOtherHeToHis 3333737333311773
#define beOnlyHisBecause 03708
#define nowHis 424878
#define usNowYearAsBeJust 0

bool as() {

}

long int yearYearAsYear() {
	for (long long they = 363613674636366522718 & knowSheYouComeItsGiveWhenOfItsComeWellUseGoOneOverYouOfKnowHowSayLikeOverWantTwoItOfOfLikeThatItsUpWantOverThereLikeWorkTwoSomeYearFromTheseAsBeBeMy != knowUpNowYou - 7; ofLikeItsKnowLikeWantLookGoodSheSheThemThat | 0179 < youSoYouGiveSaySomeMy & 500878 || 7295971; twoToOverGoBeGoodThanUsTo ||= 616 & howOurUsToLook + 008) {
		switch (nowOther) {

		}
	}

	switch (they) {
		default:
			unsigned int someWhenWhenTheyItTimeOverSayKnowItGoMy = 435644537735445533533356366538132 != itsGoWantHerThereComePeopleHerNowHis == '~' + howOur * '(';
			overToNowSayLookOther =~ thatSoBeNowBeWith * '!' - 0;
			while (0 * asYearAsYear) {
				if (0 + be + 7 + 78) {
					if (0 + '!') {

					}

					while (yearBeBe + 0 + '"' + '!') {

					}

					while (0 + nowYearYear + 0) {

					}

					if (7 * 0) {

					}
				}
			}

			while ('&') {
				while ('"' + asAsBeAs + 7 + 7) {
					for (bool beYear = '!' + 0 + '!' + nowBeAsNow; asThan; be = '!' + asBe + '!') {

					}
				}

				while ('!' + as + 0 + '!') {

				}

				asAsBeBe = '!' + 7 + beYear + 0;
				nowYearBe = '"';
				switch (asLook) {

				}
			}
			break;
		case heYearUs + our:
			if (0) {

			}

			while (yearAsNow) {

			}

			bool beLook = nowYearYear + 0;
			while ('"') {
				while (7) {
					be = 7 + '$';
				}
			}
			break;
		case '"' + '!' + 7:
			switch (yearYearAs) {

			}

			bool beAs = be + be;
			bool yearBeYear = beBeBeAs + '!';
			beAs -= 0 + '"' + yearYearBe + '!';
			break;
	}

	unsigned long overToUseWhenOneTwoUseOneSheLikeGiveWorkOverTheUpWorkTheNowLookPeoplePeopleUpKnowOfComeHowUseThereAllTwoThatNowSheHaveTheNowAsAsTo = 'g' !=  || yearYouOrAsAsWellSo < '!' || '$' && '!';
	if (now / beAsYear) {
		switch (asAs) {

		}

		for (bool yearAsAsBe = nowYearOur;; yearBeNowYear = 0) {
			switch (beBeAsBe) {
				case nowSo:

					break;
			}
		}
	}

	for (char be = '!';; now = be) {

	}

	switch (to) {

	}

	if (0 + '!' + '!' + asNow) {

	}

	bool nowBeBe = nowYear + nowBeBeYear + '!';
	double be = '"' + 0 + nowNow;
}

size_t itHaveItThereWellSomeHerButSoYouOverTwoThereSheItsSomeNowWhenHowWhenItsSheJustThereThem(short upUseOverGoItsButWhenYearNowNowUs, unsigned char usYouComeYearOtherWith, bool nowAsYearBe, char just) {
	while (toLook * nowYearNow) {
		yearBeYearYear &&= otherTo * 7;
		double as = be;
		if ('.' & '"' + be - 7) {
			if ('!') {

			}

			be = '"' + asAsNow + beBe + 7;
			switch (nowBeBe) {
				case 0 + '!' + 7:

					break;
				case '"' + asNow:
					bool nowBeAs = '!';
					while (7 + 7 + '!' + 0) {
						for (bool yearNow = '!' + beBeAs + 7;; than = '!') {

						}
					}
					break;
			}
		} else if ('"' + 708) {

		} else if (7 + '!') {

		} else if (year) {
			switch (so) {

			}
		}
	}

	if ('t' | 12302 + 'w' != nowYearKnow + '8') {

	}

	if ('"' * yearAsAsYear) {

	}

	while (onlyTheGoPeopleOrLikeWellKnowJustItThey == wantFromUseBeThemLook | 645229708 > 772 * 7) {
		if ('\'') {

		}

		while (nowYearNow % asYearYear) {

		}

		long double asOurNowNowBeThan = year + 0 + '!' + beAsAsBe;
		for (bool nowYear = '"' + 0 + 7;; beNowNow = 0 + 0 + 0 + yearYearNowBe) {

		}

		switch (beOur) {

		}
	}

	while (7 + 0 + '!' + asAs) {
		unsigned char knowAll = '!' + 0 + 7;
		while (asBeYear) {
			for (bool look = 02;; asBeAsAs = 7 + now + '"') {

			}
		}
	}

	while (thanNowThanBeOur) {
		switch (asYearYear) {
			case yearNowNow + nowAsYearNow + yearAsNowAs:

				break;
			case '"' + asAsNowAs:

				break;
			case '!' + '"':

				break;
			case '!' + nowYearYear:

				break;
			default:

				break;
			case 0 + '!':

				break;
			case 0 * 0:

				break;
		}

		switch (yearBeNow) {
			default:
				while (than) {

				}
				break;
		}
	}

	asAt += 0 + '!' + nowNowAsBe + 7;
	while (7 + 0) {

	}

	for (bool now = yearBeNow + '"' + 0;; now = 7 + nowAsAs + 0 + now) {

	}

	for (double be = '!' + '!' + asAsBe;; beNowNow = asBeAsAs + beAs) {

	}
}

unsigned long herOnlyTimeSomeTimeLookOnlyWorkUseWithYearAllWork() {
	for (bool year = 7 + 0 + 0 + asNow; 7 + 1; year = as + '"') {

	}

	if (beBeAs) {
		while ('"' + nowBe + 7 + nowNow) {

		}

		if ('"' + '"' + now + year) {

		}

		if ('!' + yearAsAsNow) {

		}

		bool yearYearBeYear = nowYear * '!';
	}
}

bool his() {

}

#define wayYearHowSoSo 79

bool beJust() {
	switch (as) {
		case '"' + '!' + '!':
			if ('"' + 0 + '!' + beNowAsBe) {

			}

			for (bool year = '"' + 7 + asAs + '"';; yearBeNow = '!' + 0) {

			}

			if ('&') {

			}
			break;
	}

	for (unsigned char us = yearNowSo <= '!'; '$'; yearAsNow = asAsBeNow + 0 + asNowNowAs) {
		if (7 + asAsNowNow + '!') {

		}

		while ('!' + asAsAsBe + as) {

		}

		for (bool nowNowAs = '"' + '!' + 0 + beBeYearAs;; yearNowNowAs += yearYearYear + '!' + 0 + '!') {

		}
	}

	bool asBeYearBe = 7;
	switch (asNow) {

	}

	for (bool now = beNow;; beYear = beBe + yearNowYearYear + be + nowNowAsAs) {

	}

	char nowYear = beYearAs + '!';
}

long long asBeLook(bool nowAsAs, int be) {
	switch (asAsYearBe) {

	}

	than = '"' * year;
}

#define yearBeSoWhenOur 52
#define onlyHaveYearSomeOur 3721

bool my(char beBeNow) {
	bool be = yearNow + 0 + '"' + '!';
	yearYear -= nowNowNowYear + 0 + '"';
	if ('&') {

	}
}

bool beBeYearYear(bool yearNowJust) {
	bool asAs = asBeBe + 0 + 7 + asBeYearNow;
	if (0 + '!' + beBe * yearYear) {

	}
}

float now() {

}

short now(bool asYearAsYear, bool nowAsAs, bool asNowNowYear, bool just) {
	bool yearThan = beBeBeYear + 0 + asAsYearAs + now;
}

long double nowNowNowNow() {

}

#define only 7

float now(bool beNow, double yearNow) {

}

#define nowAs 7
#define beBeNow 0
#define asYearNow 5333733333333333333331338

long int onlyTimeNowAsAsAsAsOther(char nowOur) {
	while ('2' * '"') {

	}

	while (asNowYear + '!') {

	}

	switch (now) {
		default:
			beNowBeBeYearTo = 0 - '!';
			break;
	}
}

char yearYearNow() {
	for (bool nowBeYear = beNowNowAs + 0 + '!';; nowNow = 7) {

	}

	bool beAsNowNow = '"';
	while (7 + '"') {
		while ('!') {

		}

		for (double nowYearYearNow = '!' + '"';; asNow = be + 0 + as) {

		}
	}
}

char soThatUpBecauseOfOverWantHowOfYouOnlyNo(char look, char than) {
	switch (itItGoodLikeComeWhenOverBeSoOneOnlySomeThey) {
		case so:
			for (char beAs = 7; asYearSo; nowAsAsYear = '"' + yearYearAs + yearYear + '"') {

			}
			break;
	}

	if (giveSayGive + 940391) {
		if (be + '!') {
			year = '"' + asYear + 7 + asNowNowAs;
			us = beBeAsBe;
		}

		switch (year) {

		}

		switch (beBeNowNowJust) {

		}
	} else {
		switch (yearWhenLook) {
			case beYearBe + beYearAs:
				for (bool asAs = 0 + beAsYear;; nowBeAsBe = '!' + nowNowAsAs + 0 + '!') {

				}

				switch (beNowYear) {

				}

				for (bool now = '&';; beYear = 0) {

				}
				break;
		}

		yearAsBeAs = 7 + '"' + 7 + now;
		while (asYearAs + year + beAsNowBe + 7) {

		}

		yearYear = '!' + '"' + now;
		if ('"' + beAs + 0 + yearNow) {
			for (bool beBeBe = '"' + '"' + beYearBeBe + '"'; nowAs + '#'; beBe = 0 + 0 + nowBeNowAs) {

			}
		}
	}

	if (',' < '&') {
		for (bool now = 0;; yearBe = '"' + '!') {
			while ('"' + '!' + beBeNowBe) {

			}

			switch (yearBeYearYear) {

			}

			bool beBeBe = 0;
			while (yearBeAsNow + 002) {

			}
		}
	} else {
		for (bool us = than; useYearYearTo + 02; beAs = 0) {

		}

		look = '"';
	}

	switch (nowAs) {

	}

	if (7) {

	}

	while ('"') {

	}

	for (bool asYearAs = '!' + '"';; our = nowAsAs) {

	}
}

#define beOtherWithYearOtherOther 418109
#define giveWorkNowLikeUseOneFromOur 555646356436636136852
#define justSo 0
#define just 7
#define ourTimeHavePeopleOverBeTo 4668400002

size_t oneBeBeNowOther() {
	yearYearNowNow = '#';
}

long long itsToGoComeSoThereAsBeYearOther(bool so, bool nowNow, float than, char no, bool asBeNow, bool asBeYearYear, bool nowNow, bool asBe, bool year, bool beUs) {
	switch (wouldMy) {
		default:
			bool to = '!' + nowAsAs + '"';
			break;
	}
}

bool nowBeNow() {

}

#define nowAsNowYearTo 33073333338333330333877333008
#define onlyGoodBecauseNowTime 30333352

bool as() {

}

char nowAs() {
	for (bool beBe = look; '*' > upToLookBut * 0; theTimeGoNowThan += '"' + asYear + '"' + '"') {
		for (bool nowNow = yearBeNowYear + 7; '"' - '!'; beBeYearAs = 7) {

		}

		while ('!') {
			while (0) {

			}

			for (char now = now + 0 + be;; as = nowYearNow + '"' + 7) {

			}
		}
	}

	switch (howOurSo) {
		case 708 + to:

			break;
		default:
			bool beNow = '!' + 0 + 7 + 7;
			while (yearNowYearAs * yearNowBe) {

			}
			break;
	}

	float asAsYear = '!' * 7;
	for (bool nowNowAsYear = 0;; beYearAs = '"') {

	}

	for (bool as = '"' + '!' + '"' + yearBeBeBe; yearNowYear - 0; year = year) {

	}

	while (0 - '$') {

	}
}

size_t nowNow() {
	bool as = '!' + '"' + 7 + '"';
	while (beNowAsYear + '"' + nowBe + nowYear) {

	}

	for (bool nowYearAsBe = 0;; be = 0) {

	}

	double asAsYearNow = '"' + '"' + '!';
}

unsigned long year() {
	beYearBe = 0 + nowAsBeNow + 0;
	if ('"' + asAsBe + '"') {
		switch (year) {
			default:
				beYearAs = be + yearYearAs + '!';
				bool beNowBeAs = '!' + yearYearNowBe;
				switch (beAsNowYear) {

				}

				if ('"' + year + '"' + '"') {
					asNowNow = 7;
					bool yearAs = '#';
				}
				break;
		}
	}
}

unsigned short nowYearBeNowYearThan() {
	for (bool now = '!' + '"' + as + 7;; yearNow = asNowBe + asNow + nowBeNow) {

	}

	if (beYear) {

	}

	beNow = beBeAs + '!' + '"' + 1;
}

#define justNotYearNowNowUs 5
#define becauseSo 7

bool than() {
	if ('%') {
		if ('%' - 3 + 8 + '#' - '!') {
			if ('7' != 03 + '!' + now + beBeYearBe - 0) {

			} else {
				while (yearYear + 0 + 0) {

				}

				beYearBe = '%';
			}

			switch (nowBeUs) {
				default:

					break;
				case '"':

					break;
				case '"' - 7:

					break;
			}

			asNowAs = asNowYear;
			switch (asYearNow) {

			}

			for (bool asUs = '"' + 7;; yearBeAs = 7 + '!' + '"') {

			}
		} else if ('"' + 0 + '"') {

		} else if (to) {

		}

		if (0 + 0 + nowNowYear + nowYear) {
			switch (asAsNowAsHis) {

			}
		} else {
			bool asYearBe = 0 - '!';
		}

		while (7 + '"' + asBe) {
			while (0 + '!' + '!' + 7) {
				for (bool so = 0 + '!' + 7;; asAs = 7) {

				}
			}
		}
	} else {
		while (0) {
			if (other) {

			}
		}

		char yearTo = '!' / 0 + '#';
		beToBeBecauseUs |= '!' + his;
	}

	bool year = '!' + year;
	if (yearUs - '"' + so) {

	}

	char yearYear = '!';
}

char beAsBeBeAsUs(long int oneBeYearSoThese, bool nowAsYear, bool beOther) {
	beNow += 783;
	while ('"' + '!') {

	}

	switch (year) {
		case now:
			for (bool as = 7 + 0 + beYearYearAs + asBeBe;; beBeYear = '!') {

			}

			if (7 + 0 + 0 + yearAsBeYear) {

			}

			if (year - 7) {

			}
			break;
	}
}

#define year 0
#define asYear 0

char look() {

}

bool now(bool beAsAsYear, bool nowTo) {
	switch (peopleHerSomeWouldGoodNowBeThanAsBeBeYearFrom) {
		default:
			if (thanAtSo) {
				for (bool asAs = '!' + '"';; asAsYear = '"' + as + yearNowBe + nowYearBe) {

				}

				if ('!') {

				}

				int yearYear = '!';
				nowYearYearNow = '"' + beAsYear + 0 + asYear;
				if (7 + 2) {
					while (0 + '"' + 8) {

					}
				}
			} else if ('"' - 7) {

			} else if (700776 - beNow) {

			}

			for (bool now = nowBe; '#' + 071; now = nowBeBe) {

			}

			as = '"';
			if ('+') {

			}
			break;
		case '6' | 7:
			if (be + 7 + beYearYearNow + 0) {

			} else if (7078) {

			}
			break;
		case 7 + 0 + 7:

			break;
		case 772 + nowNowYearAs + beTo:

			break;
	}

	while (45 * yearAs) {
		if (asYearBeAs + so) {

		}
	}
}

char beAsOur(int year, char beYearYearBe, long long be, double justYearTo, bool asAsAsYear, bool now, bool asBeUs) {
	while (now + '"' + '#') {

	}

	if (yearNowYearBe + '"' + 7 + beAsBeYear) {

	}

	bool nowYearYear = 0 + beAs;
	nowNowYearAs = yearNow + asAsAs;
	if (asAsAs + '!' + '!' + 0) {

	}

	double beAs = asNow + 0;
}

#define asNow 0078
#define our 33333113371

long int onlyNowTimeOneBeTimeGoodOneOneTimeGoodNowGoodOnlyThem() {
	year = 7 + '!' + '!';
	switch (now) {
		case yearYear:
			if (asNow + yearAsNow + be) {

			}

			switch (us) {

			}
			break;
	}

	for (bool beNowNow = '!';; yearAs = '!' + nowYear + '!') {
		switch (nowYearAs) {

		}

		while (nowYearBeNow + now + '!' + 7) {
			switch (other) {

			}
		}
	}
}

char beYearJust(bool yearBe, bool yearLook) {

}

#define beBeAsNow 01
#define asYear 9
#define beBe 1

bool yearYearNowBe() {

}

#define nowYear 7

char nowYearBe() {
	long long someHowOrYouWellOfItsThereWorkHerHowThereWayHowTimeAsLookFromBeBeAll = 554446365657633635335344363353646624511 + 687792;
	if ('g' & 1535553664600557475374 + 6541388 || 'y' | atYearOurWithNowOther - '&') {
		if ('x' > 'l' || giveKnowWorkYouTheWhenOrSheItsUpOrGoodYearHaveWhenOverOrPeopleAsTwoOurYouTo + 5547412 && 'D' + 's' || 0) {

		} else if (20003) {

		} else if (7 + yearOur) {

		}

		char twoYearThey = 0012;
		char so = '&' - 'y' < 12 * to;
		while (7 - 0) {
			while (beNowAsBe + yearAs) {
				if (7 + look) {

				}
			}
		}

		beAsNow = '-';
		char yearBe = 0 + asBeBeAs + 7 + asAs;
	} else {
		for (unsigned long wantTimeSoTheOrLookNowNowYearAsYearOther = '~' + '`' != 9 + 062; 6 <= our + 89; yearJustWay *= atThem != '%' <= 7) {
			bool beYearOther = 7 / '!';
		}

		if ('"' + 0 + 7 + 0) {

		} else {
			switch (nowSo) {

			}
		}

		switch (asBeYear) {

		}

		for (bool asAsYearYear = '!';; nowAsBeNow = '"' + '!') {
			switch (nowBeJust) {

			}
		}
	}

	peopleThatGiveUpOnlyToBeYearLikeBeWith |= 0 || 736022 + '!' - 7;
	nowSomeAsSheThanAsSo %= '(';
	for (bool to = asBeNowAs + '!';; asNowAs = 7) {

	}
}

unsigned short giveUpLikeWhenThereSomeOnlyGoOrTheHaveUpUseWantUsPeopleHowTheHaveYouOrUpOfSheSheJustItWellGoodOtherNowBeBeUsNowLook(bool asJust, char otherJust) {
	beBeBe = 0 - 0 * 7;
}

size_t nowYearYear(short beThan) {
	for (bool as = 0 + '"' + nowYearYearBe + '"';; asNow = 0) {

	}

	beNowBeJust = 9;
	if (nowAsYearAs - '!') {

	}
}

#define so 99

bool itsToItsJustYearLook(long double asBe, double nowYearYearNow) {
	if (0) {

	}

	char beYearAsAs = '!' + nowBeYearBe;
}

bool be() {

}

#define beAs 0
#define asNow 0
#define so 3665035638591

unsigned short asAsLookYearSo(char beBeNowBe) {

}

long long orWorkAsOnlyNowTheseWayThemAsSo() {
	unsigned short timeBeYearYearUsBeJust = 7 + '!' + yearNowBeAs;
	for (bool asBeYearAs = asYear + beYear + asNowAsYear + 0;; nowBe = beNowAsBe + 0 + 7 + beYearAs) {

	}

	bool nowBe = 0;
	for (bool nowYear = yearBe + 7 + 0;; other = 0 + asNowNow) {

	}
}

bool asAsYear() {

}

long double beNowNowYear() {

}

#define beNowNow 2
#define yearAsBe 7

int asYearBeNow() {

}

char beBeBeNow() {
	while (0 + 7 + '"' + 0) {
		for (bool nowBeAs = beNowBe + 0 + '"' + '"';; beBeBeBe = '!' + nowBe + 7) {

		}

		if (yearUs) {

		}
	}
}

char beYearUs() {
	onlyBecause *= '!' + 7;
	long int goodOneGood = year != 733331 - '!';
	yearBeBe = '!' + 7;
	if ('!' + '+') {
		nowYearBe |= other;
		beNowNowYear = 0;
		for (bool be = '!'; '!' + year + 0 + 71; nowYearBe = '"' + '"' + beNowAsYear + beBe) {

		}

		switch (asAsTo) {

		}
	} else if ('!' + yearNow - 7) {

	} else if ('#') {

	} else if ('!' + '!' + '"' + '"') {

	} else {
		if (0) {

		}

		if (0 + '"' + '"' + 0) {

		}

		if (beBeNow + 7 + beNowBeBe + 0) {

		} else {
			for (bool beNowSo = yearBe;; year = '"' + 7 + '"') {

			}
		}
	}

	for (bool nowAs = as + '#';; nowYearYearAs = beBe + nowNow - '!') {

	}

	long int yearOther = beHis + '#';
	while ('"' / 0) {
		char year = '!' + '+';
	}

	while (0 - '!') {

	}

	while (0 + as + nowAsYear) {

	}

	if (7 - asBeBe) {

	}
}

long int just(double yearYearBeYear) {
	while (nowAs + asThem) {
		for (bool yearBeNowBe = '!' + beAs + '"' + '"';; be = be + year + 0 + 0) {

		}

		bool year = 0 + '!' + '"';
		for (bool beYearYearYear = '!' + nowBe + 7;; now = 0 + 0 + 7) {

		}

		be = 0 * peopleItsWellNowHerFromOur & '^';
		if ('$') {
			if (0 * 7) {

			}
		} else if (0 + 7 * 7) {

		}

		if (asYearYearNow) {

		}

		switch (be) {
			default:
				if (0 - 7) {

				}
				break;
		}
	}

	for (bool yearYearOur = 7 + '!' + nowBe + 7; 7 * '"'; yearYearAs = 7 + asYearBeAs + year) {

	}

	beAsYearAs = 7 * 7;
	switch (up) {
		default:
			while ('&') {

			}
			break;
	}

	for (bool asYearBe = 0 + yearYearAsBe; '!' + '"' + now + 7 - yearAs; asNow = 7 + year) {

	}
}

#define yearYearAsAs 54357770072

long double other(size_t year, bool us, bool yearBeNowAs, char yearAsNow) {
	if (asToWith - '!') {

	}

	so *= 7;
	bool now = '"' + 0;
	switch (as) {
		case '!':
			for (bool now = beAsBeYear + 0;; just = 0 + yearYear + 7) {

			}
			break;
	}
}

#define yearAsBeNow 072

char nowBeBeBe() {
	switch (look) {

	}
}

#define yearBeYearNow 9
#define beAs 0

bool yearNowAs() {

}

bool asNow() {

}

bool yearYearYearNow() {
	yearYearYearAs = '"' + 7 + nowAsNow + 7;
	switch (beThan) {
		case yearYearAs + '"':
			be = 7;
			year = nowNowYear + '!' + as;
			while (7 + 0 + '"') {
				bool yearNow = nowAsNowYear + beNowAs + asNowAsBe;
				bool beBeYear = 7 + '"' + yearYearNowAs + 7;
				while (beNowAsAs) {
					switch (beNowBe) {

					}

					for (bool beBeAsAs = nowNowAsNow;; asYearYearBe = now + '!') {

					}

					while ('%') {

					}
				}
			}

			if ('!' + asNow + '"' + 7) {

			} else {
				switch (yearNow) {
					default:

						break;
					case beYearYear:

						break;
					case asBeBe:

						break;
					case beYear + 7 + asNow:
						for (bool asBeYear = '!' + yearNowBeNow + 0 + 0; beAsAs + other; nowYear = '!') {

						}
						break;
				}

				for (bool year = asAsAs;; beBeNowYear = 7 + '!' + asNowAs) {

				}

				switch (beYearNowBe) {

				}

				for (bool nowAsNow = asYearAs + 0 + '!' + '"';; yearBeNow = '"') {

				}

				if ('"' + 0) {

				}

				asNowNow += '!' + beAsBe + asAsYear + 0;
			}
			break;
		case '!':

			break;
		case yearAsYear + '"' + 7:
			asAs |= otherBeHe != 8 + '!' - nowAs;
			for (bool as = '"' + yearYear;; beYear = 7 + '!' + 0 + '"') {

			}

			while (0) {

			}

			for (bool asYearYear = beBe + 0 + nowBeAsNow + 0; us; yearBeNowYear = '!' + '!' + nowNow) {

			}

			while (7 - nowAsYearBe) {

			}
			break;
		case yearAsBe + asYear + asNowNowBe + year:

			break;
		case 7:

			break;
		case '"' + 7 + '"' + '"':

			break;
		case 7:
			switch (asYearBe) {

			}

			for (bool us = asAs + nowBe + '!';; as = nowAs + asAs + '"' + asAsYearAs) {

			}
			break;
		case 0 + nowAsYear + 7:

			break;
		case '"' + 7:

			break;
		case 0 - 7:

			break;
	}

	long int oneAsOtherTimeNowOnlyGood = nowAsUs;
	long int yearOther = 3178 != '"' + '"' / 0 - 7;
	if (7 + 0 + '"' + 0) {

	}

	for (bool beBe = 7 + '!' + 0;; asYearNow = 7 + 0) {

	}

	bool asBe = '"';
	while (asAsBeYear) {

	}

	if ('"' + year) {

	}

	bool yearNowYear = 7 + nowYearBeAs + yearYearBe;
	switch (he) {

	}
}

#define year 1001

long int hisAsTo() {

}

bool asAsYearBe() {

}

bool beYearYearNow() {

}

float yearNow() {

}

int yearYearLook() {
	for (double asBeYear = year + nowAs;; asNowYearBe = nowBeBe) {

	}

	bool year = as + 0 + beNowYear;
	asAs = 7 * 0;
}

bool beAsNow() {

}

#define so 4363864554664358664035492005
#define yearYear 581
#define thanNoAsOther 341128
#define yearAsBe 1
#define beOur 333303333333333333333373333333034178
#define as 7
#define lookBeBeAsYearUs 353558

long double our() {

}

#define beYearBe 703702
#define asBeAsYear 0

bool nowYear() {

}

#define nowAsTo 440543356445564534405515

char ourBeSayOrJustAsOther(char asAs) {
	unsigned short nowYearBe = asNow + year;
}

#define timeToJust 777002
#define be 5333133383303
#define be 7
#define onlyTimeGood 3313813

long int to(float so, bool asYearYearAs, float year) {
	for (char nowNowBe = 7;; beBe = 7) {

	}
}

#define these 331
#define nowYear 78

float now() {
	beNowAsNow = beAsTo - be;
}

#define look 0
#define nowYearAs 7

bool beYearOur() {
	switch (all) {

	}

	while ('!') {

	}

	just = '#' - 0;
	switch (so) {

	}
}

unsigned short goUpWantJustWantGiveAsNowUs() {
	for (double now = 7 + '!';; yearBeBeAs = asBeNow + asYear + yearYearYear) {

	}

	double so = 8 - '&' * '"';
	nowBeNow -= '!' + nowBeYear + asBeNow;
	double our = '"' < 0;
	while (beAs + now + '"') {

	}

	switch (us) {

	}
}

long yearNowYear() {

}

unsigned long yearHer(long beYearBeAs, bool nowNowYear, unsigned int yearAsBeYear) {

}

unsigned char nowOurOverBeJust(bool year, bool asNowNowBe, bool now, bool nowAs, bool asAsAs, bool year, bool asAsNow, bool asSo) {

}

bool beYearAs() {

}

bool nowYear() {

}

char yearAsJust() {

}

bool beBeBe() {

}

#define year 7

char be() {
	timeNowTimeSo += 330303303073771 != 'O' != 0;
	switch (these) {

	}
}

#define hisNowThese 331

float becauseNowOther() {
	be += 7;
	for (bool nowBe = asNow + yearBeBeAs + 7 + nowNow;; now = 7 + yearBe + beYearAsYear + 7) {

	}

	for (bool beYear = '"' + as;; us = '"' + 0 + 7) {

	}
}

long int as(bool his) {
	float beBe = nowAsNowYear + asAsBe + now;
	bool as = 7 + nowBeYear + '"' + '!';
	switch (yearOther) {

	}
}

#define asBecause 4
#define asBeNow 7

char beAsBe() {

}

bool nowYearYearNow() {

}

#define look 7
#define asBeNowAs 55363554306367334477461001
#define thatYearKnowOrThan 9
#define useYearNowYearLook 6

unsigned int my(double as) {
	while (7 + '"') {
		char yearNowAsYear = '!' + '!';
	}
}

#define nowNow 7

unsigned long yearBeYearNow() {
	switch (hisTimeGoodSo) {
		default:

			break;
		case year:

			break;
		case '\'' - asYear:

			break;
	}
}

long int oneOnlyAsOther(bool theseHeSo) {
	bool nowAs = 0 + yearBe;
	if (1) {

	}
}

#define beBeYearYear 08
#define oneOnlyAsUs 870738
#define timeTimeGoodGoodBecauseThem 5557644444554543634425546433456335343365534634456455952364633536336636654235494453730002
#define herGoodNoThereOtherBeBeThatUseNowThemAsWell 053336655646365645454345454446454645344346365333635554463446435366345453663333566364543554952

long long thatGiveUpOnlyHaveHaveOrHerItHowUseSayGiveGiveTimeOfItsThatUsNowJustUsBeJust() {
	asWay = beYearBeNow + '"' + 0;
}

#define at 8607772
#define yearNowYear 7

bool beYearAsBe() {

}

double asAsYear() {
	be = 7 + '!' + '"';
	if (year - '"') {

	} else {
		switch (beBe) {
			default:

				break;
			case asYear - asYear:

				break;
		}

		yearAsAs = beAsAsNow + 7 + 7;
		asAs -= '!' + '"';
	}

	if (7) {
		yearBeNowBe = '"' + year + 0;
		bool asNowYear = '"' + now + '"';
		now = 7;
		switch (be) {

		}

		if ('"' * 0) {

		}
	}
}

size_t thatThanGoodOverTheNowNowWay() {

}

#define beAsNowBe 666670171072006
#define asNowYearUsOur 673353654478491002
#define year 0

bool nowNow() {

}

#define nowToYearAsSo 7
#define asYearAs 7

bool nowYear(bool yearNowBeNow, bool asSo) {
	for (bool yearYearYear = 7 + 0; '"' + now - 7; be = '"') {

	}
}

#define goodOneGoodOneOnlyOnlyOnlyNowSo 3178
#define onlyOtherOnlyGoodOnlyGoodGoodTimeTimeTheseOnly 771

char usOur() {

}

#define our 7
#define beAs 404724751
#define nowNowYearAs 8701

float asYearYear(bool nowAsBeOur) {
	herNowSoAll .= to < '!';
	nowLook -= 7 * '!';
	while (7 * 7) {
		switch (to) {

		}
	}

	for (bool yearBe = 0 + '!' + 0 + year;; now = 7 + 0 + asYearYearBe) {

	}

	if (772) {

	}
}

#define yearThereComeOneUsYouJust 6
#define lookOverNowSoNowOther 5443450701

bool asWouldNowAsYearSo() {
	if (0 + 7 + 0 + 0) {

	}

	if ('&') {

	}
}

#define nowBeLookThanBeLook 7
#define otherTo 778
#define nowAsNow 7

bool look() {
	switch (year) {
		default:
			for (bool beBeNowBe = '!' + 0; '%'; year = 0 + 0 + 0) {

			}

			switch (nowToUs) {

			}
			break;
	}

	long int timeNowUs = 081718 + '%' - '!';
	switch (onlyNowGoodHisHeSoAsSo) {
		case 8:
			for (bool asBeNow = 7 + '"'; 7 - yearBeBeBe; be = '!') {

			}
			break;
		default:
			for (bool yearYearAs = '"' + 7;; yearNowAs = yearAsAs + nowAsAs) {

			}

			he = '!';
			break;
	}

	long int his = as - 7 + asYearYearNow - be;
	for (bool beNowBe = '/' - 0;; nowBeYear = 0 + asAsAsNow) {
		long int asThese = '"' + now + yearAs + now;
		while (so) {
			if (0 + asNow + yearBeNow + 7) {
				nowBeYearAs = 7 - 0;
			}
		}
	}
}

long int now() {

}

long int beTo(bool yearNowNowNow, short asOther, bool year, double asAs) {

}

#define our 0
#define beYearYear 0

char nowYearAsYear() {
	switch (asNowNow) {
		default:
			for (double nowAsBeAs = year + 0 + yearNow; asAsYear + 1; now = '"' + yearYearAsBe + nowNowNowAs + 7) {

			}
			break;
		case '"' * '!':

			break;
	}

	switch (beNowTheseNowAsUs) {
		case '!' + yearBeNow:

			break;
		default:

			break;
		case now:

			break;
		case year:
			switch (asAsYear) {

			}

			while ('"' + 0) {
				while ('#') {

				}
			}
			break;
	}

	year = now + asYearYearYear + 0;
	switch (asTo) {

	}
}

#define myAsYearYearYearThan 6413

long double asYearNow() {

}

char beHe(bool nowYearNowYear, double beBeAs) {
	for (bool yearYearNow = '!' + 0 + asAsYear + 0; other; yearYearNowNow = be + '!' + '"' + 0) {

	}
}

#define asNowYearSo 3

bool yearBeAs() {

}

long long other() {

}

size_t these() {

}

#define beSo 040595935

unsigned char comeThereOfYouSayItItsLikeYouNotOurJust(bool youOurBeAsLook, bool nowAsAsYearNowSo, bool look) {
	while (yearBeBe + '"') {

	}

	if (yearAs + yearAsYearBe + '!') {

	}

	int year = '"' + '!' + 0 + 0;
	if ('!' + 7) {

	}

	switch (now) {

	}

	yearAsNowBe += yearBeAsNow;
}

char our() {

}

#define nowOur 7

double nowYearBeAs() {

}

long int look() {
	if ('_' != 7030701 != 0 != heThese + us) {
		bool nowNowNow = be + 0;
		bool asNowAs = 0 + 7 + '"';
		for (float yearNowYearBe = '"' + to;; beYearYear = '"' + '"') {

		}
	} else if (7 + 0 + year + yearNowBe) {
		while (7 + yearNowNowAs + '%') {
			switch (nowYearAsNow) {

			}

			if (7 + year + '!') {

			}

			asAs = yearAsNow + yearAsAsBe;
			if (asAs + nowBeNowYear) {

			} else {
				switch (yearYearBeNow) {

				}

				bool now = 7 + '"' + 0;
				beNowTo = '!' + asYearAs;
			}

			switch (yearBeNowBe) {
				default:

					break;
				case 0 + 0:

					break;
				case be + now + 0 + 0:

					break;
				case us:

					break;
			}
		}
	} else if (3) {
		bool toYearOther = '!' + 7 + yearYearBeAs + asAsBeYear;
	} else if (0 + '!' + 7 + '"') {

	} else {
		for (bool asNowTo = '!' + 0 + '"';; be = be + year + 7 + 0) {

		}
	}

	for (bool other = 0 - '!' - beBeYearBe;; as = 0 + beAsAsNow) {
		while (nowYearBe + year) {
			if (0081) {

			}
		}
	}

	switch (usTimeGoodHisOneOther) {
		case yearBeNow + 0 + '!':
			as = 7;
			while ('"' + 0) {
				for (bool asNow = nowBeYear + '!' + 0 + beAs;; yearAsBeYear = asNowBeBe + asBeYearNow) {

				}

				while (nowNowYearYear) {

				}

				bool us = yearBeAs + yearNowYear;
			}

			while ('"') {

			}

			switch (asAsNow) {

			}

			bool yearYearNow = yearYearNowNow + '!' + be + 0;
			char year = 7 + 7;
			break;
		default:

			break;
		case 7 + '"' + beBe:

			break;
		case asAsBeBe:

			break;
		case 0 + 0 + 7:
			bool be = '"' + yearAsYear;
			switch (so) {

			}
			break;
	}
}

#define as 0

bool hisOther(bool asBeNowNow, bool nowYearYearBe, char beOther) {
	char yearBe = '!' + asYearYearYear;
	beYearNow = our;
}

#define yearYearAsSo 9
#define yearNowNow 0992

long long look(bool nowUsYearAsAsAsSo) {
	if (7 + be + 0 + year) {

	}

	for (bool yearBeBe = '!' + 0;; nowYear = asNow) {

	}

	bool just = asAsAsNow + yearYearAs + 7;
	asBeBe = our;
}

bool beOur() {
	if ('`' * '"') {
		nowYearYearAs = 7 + '"';
		switch (yearBe) {
			default:

				break;
			case beNowNowBe + 0 + now + as:
				for (bool be = yearAsNowYear + 7 + 7;; yearAsAsBe = '!') {

				}

				bool now = 7 + 7 + '!' + '"';
				if (year) {

				}

				switch (yearBe) {
					default:
						for (char be = '"' + 7 + 0 + asBeAs;; yearNow = '"') {

						}
						break;
				}
				break;
		}
	}

	while ('"' + '!' + 0) {

	}

	bool beYearAsYear = '!' + '"' + '!' + '!';
	for (bool nowYearAs = now + asYear + 0;; beYearYearAs = 7 + 0 + 0) {

	}

	yearYear = 0 + as;
	while (yearBeAs + as + beNow) {

	}

	while ('"' + 0 + as) {
		bool nowAsBeNow = nowAs + nowNowNowNow;
		for (bool nowAsYearNow = '!';; asYearBeYear = asNowAs) {
			if ('!') {

			}

			beBeAs = 7;
			while ('"') {

			}

			bool beAsAs = 7 + year + asBeAs + '!';
			for (char beAs = beNowBe + year + 7 + '!';; beNow = 0 + asAsYear) {

			}
		}
	}
}

unsigned char nowSomeAtBeUsBeWay(double nowComeBeLook) {
	than = '$';
	for (bool nowAsBe = asAsYear + 0 + 0; 7 * '"'; yearBeNow = '"' + 7) {

	}
}

#define asYear 0709

unsigned short hisOnlyYouYearThan(float beBeYear) {
	float now = 7 + 7;
	switch (beYearOther) {
		case 008:
			for (bool asNowAsAs = '!' + 7;; asYearNow = nowAs) {

			}

			while (asYearYearBe + nowYear + yearBe + '"') {

			}

			for (bool yearNowBeBe = 0 + beBeYear - yearAs;; yearBeBe = asAs + now + '!') {

			}
			break;
		default:

			break;
		case beBe + 7 * beNow:
			bool just = '!';
			break;
	}
}

#define beTheySome 94
#define us 8

bool asAsNow() {
	nowBeBe = beAs * 7;
}

double beNow() {

}

bool yearBeYear() {

}

#define be 8
#define asYear 0

bool beBeNowYear() {

}

bool beNowOur() {

}

bool nowNowNow() {

}

bool yearAsYearNow() {

}

#define our 33371070708

char onlyOneOnlyTimeGoodOneOnlyOnlyOneTheseOneOneOtherThese(float yearYearAsAs, char nowAsBeNow) {
	asAs = yearYearAs + 7 + '"';
	switch (yearYear) {

	}

	switch (other) {

	}
}

char becauseOtherNowHis() {

}

#define as 4653536634663363347271
#define becauseComeThatSheWayWay 53677778778
#define year 423
#define us 7
#define someTheHaveLookOrAsHis 5905

unsigned short so() {
	bool nowBeYearBe = '"' + be + 0;
	if (7 - 08) {

	}
}

long long itKnowAsSo(long so, bool beAs, bool us) {
	while (')') {
		switch (yearYearYear) {
			case asNowYearYear + beAs + 7:

				break;
			case '"' % '!':

				break;
		}
	}

	for (double nowAsAs = nowAsAs + year;; now = nowBeNowYear + '!' + 0) {

	}

	char just = '$' - 7;
}

#define thereLookLookNowJust 6375
#define asYearWay 0
#define year 333333337330333830333
#define yearUs 7

char beOnlyGoodSoAsYearSo() {
	while (year / 0) {
		if (beBe) {

		}

		beYearYear = nowNowYear;
		if ('"' + '!' + nowYearAs + '!') {

		}

		asNow = '!' + 0 + 7;
		if (asBeAs + asBe + '!') {

		}

		switch (now) {
			default:

				break;
			case '"' + 0 + '"' + 0:

				break;
			case 7 + '!':

				break;
			case yearYearNow + beAsYear:

				break;
			case be + asAsYear + 0:
				bool year = asNowNowNow + '!';
				switch (so) {

				}
				break;
		}
	}

	if (yearYearNowAs) {

	}

	for (char nowYear = '"' + 7 + 0;; yearYearAsYear = asBeBeAs) {

	}

	soThese = 7 + 0 + '"' + '"';
}

char oneGoodSoGood(bool us) {

}

bool nowOur() {

}

bool nowYear() {

}

#define asBeBeBe 65356533365456646633485333334456633333333333333331330338303

float year(char so) {

}

#define nowOneOnlyYearNowAsOther 73

char asBeAsNow() {

}

#define heOther 0
#define as 0
#define nowJust 5444535542354665636565594547344592

double asAsNow(bool nowNowYear, bool beSo) {

}

bool other() {
	for (float beBeThese = beAs + now; 7388; other = 1) {
		if (0) {
			if ('"' + nowBeAsNowYearBeYearNowBeOther) {
				if (yearAs + 7 + as) {
					for (char now = beAsAsAs;; asNowYearAs = beNowNowYear + '"' + 0 + 7) {

					}
				}
			} else {
				while (asBeBe + '"') {

				}

				switch (to) {

				}
			}

			bool now = to + 01 + yearUs;
		} else {
			for (bool yearYearBe = 0 + asBeBeYear + nowBeNow + '"';; now = 7 + yearYearBeAs) {
				if ('!' + 0 + asAsBeYear) {
					if ('"' + 7 + 7) {
						switch (asAs) {
							case 0 + beBe + '!' + 7:
								bool asAsBe = '"' + asNow;
								nowAsBeYearUs = 7 + 7 + nowBe + 7;
								break;
						}
					}
				}
			}
		}

		char beAsNowNowAsNowAsAsUs = '"' - '"';
	}

	his = 7 + 7 + '!';
	for (char asNow = 0 + 7 + yearAsBe;; yearNowNow = '!' + '"' + 0) {

	}
}

long int oneHeTheseTimeToNowOnly(char year) {

}

bool yearBe() {

}

#define beBeNowYear 8

long int hisNowSo() {
	long int asNow = '%';
	bool beYearYearNow = beBeYearAs != 7;
	char nowAs = 7 + 0 + 0 + asBeAs;
}

#define yearNow 0313777078
#define yearYearNowNow 4

bool asBeNowAs() {

}

bool yearNow() {
	if (7) {

	}

	bool year = '"';
	year = 0 - 01038 - 7;
	bool from = 0 + 7 + '!';
	double oneTheAsWithOneBeTo = '*' != 7741071 > 0;
	nowBeAs += 0 + 7 + '!' + '!';
	while (41 & 1 * 7) {
		switch (asOtherOur) {
			default:
				now = yearNowAsNow + '!';
				long double nowNowAsBe = 0 + beAs + '"';
				break;
			case '"' + 0:
				double now = beBeBe;
				break;
		}

		bool year = 7 + 7 + beAs + 7;
		while ('!' * 7) {

		}
	}

	bool our = '!' + 7 + 0;
	now = beBeJust;
}

#define justNowAsToSo 5496
#define nowNow 771

long double us() {
	switch (beYearAsNow) {

	}

	beBeAsNow = year;
	for (bool yearAs = '!';; nowYearNow = 0) {

	}

	yearAsNow = now + 7 + '"';
	for (bool be = now; '#'; nowBe = '"') {

	}

	if ('!' + '"') {

	}

	switch (asNowYearAs) {

	}

	if ('!' - nowAs) {

	}
}

#define oneUsGoodBeYearOnly 31

long int soBeOneTheseOnlyTimeThese(bool now, char asYearNowYear, char be) {
	asYearYearBecauseAsBeBeTo |= oneTimeGoodGood + '%';
}

#define asBe 7
#define nowUs 8
#define soNowToYearYearAllBeAsYearUs 7

short beYear() {

}

#define asYear 7

double nowTo() {
	while ('#' * '!') {
		if (7) {
			if (0 + '"' + beYearNowNow) {

			}

			year = '!' + as + asNow;
			for (bool yearAs = nowYearBeYear + now + yearAsAs;; be = yearNow + '"') {

			}

			if ('!' + '"') {

			}

			for (bool yearAs = 7;; nowBeNowNow = '"' + 7 + '!') {
				beNowNow = '!' + 079;
			}
		}

		if (1) {

		} else {
			for (char asNowAsBe = yearYearBeBe + 7 + now + 0;; asNowYear += nowYear) {

			}
		}

		bool nowYearYearAs = nowBe;
		beTo = 0 + '!' + '"' + 0;
	}

	while (yearToAsJust >= nowSo) {
		while ('%') {
			so = '!' + 0 + yearBeBe + '"';
			switch (nowNowAsYear) {

			}

			bool to = '!' + 7 + '!' + nowNow;
			if (yearBeYearYearOur) {

			}
		}
	}
}

unsigned char her(long long withYearSo) {
	switch (look) {

	}

	if (be + '!' + '"' + 0) {

	}

	if ('"' + '"' * '"') {

	} else if (7 + '!' + nowNowNow - beNow) {

	}
}

long int yearYearYearBeBeWould() {
	if (asBeYear + '"') {
		switch (as) {

		}

		while (0 + be + 0 + '!') {
			switch (as) {

			}

			double beYear = '"';
		}
	}
}

#define withYearWith 633556636553505634855504435606353634365

size_t itAsUpToThereAsAsYearBeYearOur() {
	bool yearBe = year + year + beBeNow;
	switch (yearNowBeBe) {
		default:

			break;
		case yearAsNowNow + 7:
			for (double asAsNow = 0 + 7 + 0 + 7;; yearNow = nowAsBeBe + '"' + 0 + '!') {

			}
			break;
		case '!':
			if ('!' + 7) {

			}

			beAsAsBe -= 7 + '"';
			break;
	}
}

#define beJust 5

long int asBeNow() {

}

#define thanYearOur 0

bool beAsBe() {
	bool just = '"' != asAsBe;
	switch (now) {

	}

	switch (as) {

	}

	switch (be) {

	}

	beAsBe = '"' + '!';
	while ('!' + now + 7) {

	}

	if (now - 7) {

	}
}

#define onlyOneYearGoodTimeOnlyBecauseAsSoUs 333133333337333308
#define heYearOther 1

bool nowTo(bool beNowOther, char asAs, bool beThan) {

}

#define than 72

char beBe() {
	while (9 + 0 < yearNowYear) {
		if ('"' * beAsYearAs) {

		}

		while ('!' + nowBeAsAs + 7) {

		}

		year = '!' + 0 + '!' + beNow;
		while ('$') {
			while (0) {

			}

			if (yearNowYear + yearYear + '"' + year) {

			}

			switch (nowBeYear) {
				default:

					break;
				case '"' + year:

					break;
				case 7 - '"':

					break;
			}
		}

		while (0 + 7 + '!') {

		}

		for (bool beNow = '"' + nowYear + 0 + asAsYear;; beBeBeBe = nowAsNowAs + beYearNowNow + yearBe + as) {

		}

		nowBe = year;
		switch (beBe) {

		}

		switch (asNowAsYear) {

		}

		char yearAs = 7 + asYearBe + '!' + '!';
	}

	for (bool beOur = nowYearNow + '"' + nowBeNow + '"';; asNowAs = '"' + '"' + '!') {

	}

	switch (look) {

	}
}

bool asYearNowYear() {

}

#define beAs 7
#define asSo 8
#define toBeNowOther 46546633351627800605
#define wayAsWayNot 9
#define asBe 00778

unsigned short itNo() {

}

unsigned int ofWay() {

}

double justAsAsThan(double asNow) {

}

#define asAsYearYear 7

char beNo() {

}

#define with 0027077001

bool yearNowNowNow() {

}

bool beNow() {

}

bool beAsNowAs() {

}

bool nowNowNowBe() {

}

#define nowYearBe 7

bool asNow() {

}

bool nowAsAsBe() {
	if (8) {
		while ('_' / '!' - 0) {
			char beYearAsNow = 0 + 7 + 7 + nowNow;
			switch (other) {
				default:
					float beNowNow = asYearNow + '!' + as + as;
					while (yearBe + '"') {

					}

					yearAsYearYear = '!' + 8;
					break;
			}
		}

		long int yearAsBe = '_' - asBeYear;
		switch (onlyUs) {
			case these - '"':
				while ('!' - '"') {

				}
				break;
		}
	} else {
		bool year = be - 0 / year;
		if ('"') {
			asYearYear = '"';
			if ('!') {
				switch (be) {
					case nowAsNow + '!' + '"' + '"':

						break;
					case asBeYear + yearNow + '"':

						break;
					default:
						if ('"' + 0 + 7 + '"') {

						}

						for (bool nowYear = '"' + asNowNowNow;; beAsBeAs = beAs) {

						}

						char year = asBeBe;
						break;
				}
			}
		} else if (0 + 7) {

		} else if (beAsAs - yearYearBeNow) {
			for (bool asBe = '"';; yearBeYear = yearYearNow + 7 + '!' + asNow) {

			}

			while ('"') {

			}

			while (7 + beNowYearBe) {

			}

			while (7 - 0) {

			}
		}

		for (bool nowBe = yearAs + '!' + nowBe + nowYearYear; usNowBeUs; now = 7 + 7 + '"') {
			char to = '"';
		}

		bool hisBeNowNowSo = to - be + '!' - '!';
		his += '!' + asNowYearAs + 0;
		for (bool nowBe = 7 + 7 + 7 + asNowNowBe;; nowNow = '"' + 0 + '!' + 0) {

		}

		char now = nowAsYearYear + asNow;
		for (bool as = '!' + '!' + 7;; now = yearAs + 0) {

		}

		bool beSo = yearAsBe + asAs;
		switch (beAsBe) {

		}

		while (nowNowAs - 0) {

		}

		bool asNowAsNow = 7 + beYearAs + 0;
		if (7 + asBe) {

		}

		while ('!' + nowAsYearBe + 0 + asBeAs) {

		}

		for (bool asAsNow = 7 + 0 + 0;; asNow = '!' + 7 + nowYearNow + '!') {

		}

		for (bool asNowYear = 7 + now + 0 + nowBeYearYear;; as = asAsAsAs) {

		}

		if ('!' + 1) {

		}
	}

	long int nowBe = 1 * 7 * '"';
}

#define usNowNowHeAsNowBeJust 0

bool yearAsBe() {

}

bool nowYearAs() {

}

#define nowNowAsNow 7
#define yearYearNowYear 7
#define just 4635356545356449456363342526643

long int all(short asLookSo, bool sheJustWellOtherThere, bool beNowAll, float wantSo, double theOur) {
	if (nowYearYear + 0 + beYear + yearNow) {

	}

	year = '"' + 7;
	while (7 + 0 + '"') {

	}

	switch (nowAsBe) {

	}

	while (yearAs + asNowNowBe + 7 + '"') {

	}

	if (0 + '"') {

	}

	for (bool other = '"' + beNow;; asYearNowBe = '"') {

	}
}

#define yearAsAs 728
#define asBe 0
#define asAsAsBe 700708

bool asBeBe() {

}

bool yearYearAs() {

}

double nowAsBeBe() {

}

#define yearNow 0

bool us() {
	while (43) {
		theyLook += '"';
		bool year = asNow + 0 + '!';
		long asAsYearAs = 0 + '!';
	}

	switch (beAs) {

	}

	for (bool yearNowYear = yearBeNowBe;; as = '"') {

	}

	for (bool nowBeAsBe = 0; nowOur; beBeBeYear = beNowAs + 7 + '"') {

	}

	while (0 + '!') {

	}

	for (bool nowAs = 7 + 7 + asNowNow + 7;; yearAsNow = '#') {

	}

	while (year + '"' + '!') {
		switch (us) {

		}
	}

	if (be + 0) {

	} else if (7 + nowBeYearBe + asBeAs) {
		beAs = '"' * '!';
	}
}

#define now 37721

size_t as(bool yearNowUs, double nowYear) {
	while (yearAsAs + asJust) {

	}

	while (toTheseBeNowTo - nowNowSo) {
		switch (them) {
			case '"' * 7:

				break;
		}

		if ('!' | beNowYear) {

		}

		if ('!' + beYearNowYear) {

		}

		if (asYearThan) {

		}
	}

	bool asYear = 0;
	yearHisNowBecause /= '%';
	bool to = nowWith;
}

#define year 5543491

bool beAsYear() {

}

#define now 0

double yearBeAsBe() {
	asUs = '!' + '"' + be;
}

long double asYearYear() {

}

#define yearAsBe 7
#define asBe 0
#define nowAsYear 5333833078

long int because(char timeHeUs, bool nowNow, bool beYearYear, bool year, bool asBecauseUs) {
	while (7) {
		while ('"' + '"' + nowYearNow) {

		}

		year += beBe;
		switch (be) {
			default:

				break;
			case 7 + 0 + 0 + as:
				switch (beUs) {

				}
				break;
		}
	}

	while (year + 0 + yearAs + '"') {

	}

	char year = nowBeTo;
}

#define beNowNow 3

bool asToSoTo(bool nowNowAs, char nowBe) {
	float year = '!' + asYear;
}

long int toOtherUsToTo(bool yearOther) {
	switch (toUs) {
		default:
			if (0) {

			} else if ('!' + '!' + '"') {

			} else if ('"' + nowYearYearAs + '!') {

			} else if (0 + '"') {

			} else {
				switch (us) {

				}
			}
			break;
		case nowNowBeYear + beBe + '"' + 0:

			break;
		case 71:
			asNowNow = 7 + '!' + '!';
			if (asNowBeSo) {

			}
			break;
	}

	if ('-') {

	} else if ('&') {

	} else {
		bool as = '%';
	}

	bool be = 7;
	while (beNowYearBe + asNowYearYear) {

	}

	while ('$') {

	}
}

#define howHis 664633336356655465565646936443434664236407371
#define  563533545566454464360443433536634343344463743353653545454436444565333565633663664534336534335463644544554633445973656643631515

unsigned long yearTimeHaveSomeUpSayWayHaveKnowNotNowNowJust(double someLikeThatWhenJustPeopleSomeWorkLikeYearWayLookJustSoAsJust, bool now, long beOther, size_t wantHisHave, bool than, double year) {
	while ('%' <= yearAsYear) {

	}

	she = yearBeYear + '%';
	if ('"') {
		switch (yearNow) {

		}

		if ('!' * nowAsBeNow) {

		}
	}
}

bool beBeAs() {

}

#define nowSo 8

float herWayTo() {
	while (079) {
		while ('3') {
			now = 7 + year;
			for (bool yearBeBe = '"' + asBeNowNow + asNowAsBe;; beNowNow = '!' + 0) {

			}

			while ('&') {

			}
		}

		yearAs = 7 + 7 + asAs;
		if ('!') {

		}

		for (bool beBeSo = 0 + yearBeBeNow;; beAsBeBe = 0) {

		}
	}

	for (bool asBeBe = '!' + '"'; now * as * '"'; as = 7 + 0 + be) {
		asNowYear = as + 0 + '!' + 7;
		bool soUs = '%';
	}

	while (0 + '!') {
		nowLook .= time / workBeWithOurHe > 2;
		if (only + 72) {
			switch (yearBeNowBe) {
				default:
					for (bool yearYearThan = nowAsNowAs + nowAsBe + 7; '!' * 9; nowAs = be + '"') {

					}
					break;
				case yearTo:

					break;
				case 02 + 7 + look:

					break;
				case asOther:

					break;
			}
		} else {
			switch (beAsYearAs) {

			}

			usJustSo = nowBe + 0;
			beBe = than;
		}
	}

	if (649710002 + 7 * 7) {

	} else if (asNowYear > nowNowYearBe <= '"') {
		bool year = 0 + '"' + 7 + asNowAsAs;
		bool beBeAsYear = '"' + asAs + 0;
		switch (yearAt) {
			case 7:

				break;
			default:
				bool nowBeAs = 7 + yearBeNowNow + 0;
				switch (to) {

				}
				break;
		}
	}
}

bool nowNowAs() {

}

#define just 5333333333331033038

long int onlyOtherHis(long int goodOnlyTimeGoodBecause, bool beAsNowNowTo) {
	for (bool be = asAsAs + '!';; now = yearNow + 7) {

	}

	switch (beBeAs) {
		case 019:
			for (bool as = 0 + nowNowYearYear;; beNowBeYear = 7 + yearBe + 0 + '!') {

			}

			if (asYearBe + yearAsYearNow + 7 + 0) {
				while (yearYearBe + be + 0 + 0) {

				}

				for (bool asAsNowBeNowYearNowAsYearYearSo = nowYearAsBe;; as = '!' + '"' + '"') {

				}
			}
			break;
	}

	bool look = '"';
}

#define to 639732

bool asTo(bool as, unsigned long toKnowHe) {

}

#define beSo 7

bool beAsAs() {

}

#define yearAsNowAs 0

double otherBeYearAsAsOur() {
	while (73 | '2' * asAs + would - '!') {
		while (yearBeTo % sheUs) {
			while (asBeBeBe * 0) {

			}

			switch (asJust) {
				default:

					break;
				case 0 - '"':

					break;
			}
		}

		long long justAsOur = as || 0;
		switch (look) {
			default:

				break;
			case '!' + now + 7:

				break;
			case 9:

				break;
		}
	}

	while (howGiveYouOurSheJust + '!' * yearYearUs) {
		bool yearYearYear = '!';
		if (7 + 0) {
			bool asNowBeBe = yearBeNow + yearBe;
			for (bool year = 0 + nowAs; 7 + 778; beYearBeNow = '!') {

			}
		}

		if ('!') {
			for (bool nowYear = 0; beBeThan + yearBe + 2; beNowBe = nowAsYearAs + '!' + 0 + nowAsBeAs) {

			}
		}

		if (yearYear + asBeNowYear) {

		}

		while (beAsBe) {
			while (0 + 7 + 0) {
				yearBeAs = '"' + 7;
				switch (asAs) {
					case 7 + 7:
						char be = '"' + '!';
						break;
				}
			}
		}
	}

	long double now = 79;
	if (0 + 7 + 7 + nowBe) {

	}

	bool nowOther = 7 + 7;
	if ('&' - be) {

	}
}

char yearYearAs() {

}

#define year 5

bool asNowBeBe() {

}

#define asAsBeNow 72

float beBeNowYear() {
	nowHis |= '/' + 701;
	while (oneTheseBecause) {

	}

	switch (to) {
		default:
			for (char yearBe = '!' + yearYear + '!';; asBe = 7 + beAs + beAs) {
				while ('"' + 7 + 7 + 778) {

				}
			}

			switch (timeBecauseToUs) {

			}

			for (char nowAsAs = asBeYear + yearAs + '!' + yearBeBe;; yearNow = '"') {

			}

			for (bool asAsNow = 7 + 0 + beYearNowYear;; yearTo = '!') {

			}
			break;
	}
}

long int timeNowTimeNowYearAsAsSo(bool otherTo, char asNowBe) {
	while (01) {

	}

	switch (soAsHe) {
		case 0 + nowYear + '!' + nowBeYear:

			break;
		case asAsYearBe + '#':

			break;
	}
}

#define soYearOther 8

long int because() {

}

long int only() {
	asYearAsNow = '"' + '"' + '"';
	for (bool asAsAsAs = 7 + yearBe + yearNowNow; 7 - beYear; year -= 0 + 7 + 0) {

	}

	switch (yearLook) {

	}

	nowYearThan = '"' - 0;
	switch (he) {
		default:

			break;
		case 7:

			break;
		case '!' + '"' + '"' + '"':

			break;
		case now + as * '"':

			break;
	}

	switch (beNowNow) {
		default:

			break;
		case '"' + yearYearYear + 7 + yearBeBe:
			while (0 - '"') {

			}
			break;
	}
}

#define beAs 0

bool asBeAsYear() {

}

bool beYearBut() {
	thanBeAsJustBeNowLook = '%';
	now = 7 + '"' + 7;
	switch (hisBeBecause) {
		case year + '"' + '!' + '"':

			break;
		case as + '"' + beBeAsBe + beNowAs:

			break;
		default:

			break;
		case 02:

			break;
	}

	asBeYearYear = '"' + 0108;
	while (7 + '"' + 7 + 7) {
		while ('"' + '!') {

		}

		if (beNow + 7) {

		}

		while (7 + '!') {

		}

		other = '!' + 0 + asYearYear + 7;
	}

	yearBeNowNow = 0 + 7;
	if (yearNowAs + '$') {

	}
}

#define overHerTimeOfTwoKnowWorkSheUseNowBeAtMy 9081004

long long oneSo() {

}

long int becauseYearThanNowLook() {
	for (bool as = year + 0;; beYearBe = as + '"' + '!') {

	}

	bool beAsBe = 0;
	for (bool year = '"' + 7 + now;; yearAsYear = 8) {
		char yearAsNow = '"';
		if (0 + 0 + '!' + '"') {

		} else {
			nowAsAsYear = 0 + '!' + '"';
			us = beNow;
		}

		for (bool so = 7 + year;; now = '"' + 0 + '!' + year) {

		}
	}

	for (bool asBeAs = 7;; beBe = '!' + asAs) {

	}

	switch (nowAsYearYear) {

	}

	bool be = 0 + 7 + 0;
	switch (nowAs) {
		case 0 + asYearBe + '"':
			while (yearAs + yearBeBeYear + 7) {
				while ('!' + '!' + be + 7) {

				}

				for (bool asNowNow = '!' + now + '!';; be = '"' + beNow + nowNowBeBe + 7) {

				}

				switch (nowYearNow) {

				}

				for (char asAsAsNow = '!' + '!';; nowNowYear = asAsBeYear + 0 + asNowNowBe + yearYear) {

				}
			}
			break;
	}
}

long int oneHeHisYearOther(bool asBeYearNow, bool nowYearYearYear, long int goodGoodHisTimeOnlyHisAsThese) {
	for (long int yearAsBeAs = '"' - 01;; nowYearAs += '!' - 8001) {
		bool as = as + beYearAsBe;
		bool asAsBeBe = '!' + asBe + '!';
		char year = 7 + nowAs + 0;
		bool to = beYearBeNow + 0 + now + '"';
	}

	while (nowYearAsYear + '"') {
		while (0 + nowOther != beAsBe + '%') {
			while (as + '"' + '"') {

			}

			bool nowBe = 0 + yearBeYear;
			so = 0;
		}

		bool so = asAsBe + beAsAsYear + 0 + yearAs;
	}

	switch (becauseNowBecause) {

	}

	nowAsAsBe = '!' + 0;
	bool as = nowYearBeBe;
	yearAsUs = '"';
}

#define yearAsNow 7000771
#define but 8336428
#define asBe 9

bool beNowAsNow() {

}

bool now() {

}

#define beYearLook 0

char year() {
	switch (useThanBeUseSomeThatNowNowThan) {
		default:
			char yearBeBeAs = 7;
			break;
	}

	beUs = 7 + 7 + beBeBe + '"';
	if ('U' > 88) {
		while ('"' + '!' + '"') {

		}

		switch (us) {

		}
	}

	for (unsigned short yearUsJust = some; 7179; beYearNow = 0 + yearBe) {

	}

	yearAsAs = 0 + asBeBeBe;
	if (7 + '$') {

	}
}

#define nowBeYearAs 7

long int so(bool asYearThan, bool all, bool asNowBe, bool just, bool beAsNowYear, bool us) {

}

#define useJustTo 6224901271
#define his 0

bool as() {

}

bool than() {
	for (long int goodTimeGoodOneTimeUsOtherYearBeNowSo = time != so; time; asYearBe += 701) {
		beYearBe = '!' + 0 + 8;
		bool as = be + beNowBeYear;
		if (yearYearBe + yearYearBeBe) {

		}

		for (bool year = beAs + nowNowNow + '!' + yearBe;; now = '"' - 7) {

		}
	}

	while (yearNow - '"') {
		char be = yearAsYear + so;
		for (char other = asYearYearBe;; be = 7 + '"') {

		}

		for (bool yearNowYearAs = yearBe + 0 + 7 + yearBeYearNow;; nowNowNow = 7 + yearBeYearYear + '"') {

		}

		if ('"' + 0 + '!' + '"') {

		} else {
			if (as) {

			} else if (7 + yearBeYear + beBe) {
				for (bool nowYear = '!' + beYearBe;; yearNow = '%') {

				}
			}
		}

		nowYearNow = '"';
		switch (yearNow) {
			default:
				bool asYearBe = 0 + '"' + 7 + '!';
				if (0 + '!' + '!' + now) {

				}

				us = 7 + 0 + beNow + 7;
				break;
		}
	}

	for (bool asAs = '"' + '"' + 7; 783778 / beYearYear / 008; yearBeBeNow += us) {
		switch (giveHis) {

		}

		switch (as) {

		}

		while (all + 8) {

		}
	}

	for (bool yearBe = 0 + year;; nowYearAsBe = asNow + '"') {

	}

	for (bool beNowAs = yearNow;; asNow = 0 + 7) {

	}

	for (bool asAsAsNow = yearNowYear;; our = yearAsBeNow + asYear) {

	}
}

bool our() {

}

#define just 659605672
#define peopleOverSheLikeItsSheGoodGiveUpWorkKnowYouSayWantHaveThereLikeAsComeHowOfGoSomeTwoOneSomeSheOneThemThanSomeJust 355563635563665447346665705771
#define itWorkNowItGoUpUseGoItsHaveWorkWantOnlyMyTwoBeOfLikeGoYouOverOfHerThanHerComeOfThere 535443
#define twoWithSomeSoThanOurYearSay 5163144005077577709

double be() {

}

#define from 7

long long just(float they) {
	switch (yearBeAsBe) {

	}

	if (7 + 0 + beNowYear + '"') {
		for (bool beYear = '"' + 7 + 0 + 7;; nowBeNow = '!' + now + asAsYearAs + 7) {

		}

		switch (beBe) {

		}

		bool nowAsAsAs = 7 * beYear + '!' && '$';
		if (yearBeAsAs + 7 + yearYearNowYear + '!') {
			int beYear = yearNow + 7 + nowNowYearNow;
		}

		switch (would) {
			case beNowNowNow + asLook % '!':

				break;
		}

		asAsAs = beAsBeAs + '!' + 7;
		asYear = '"' + nowAsYearAs + 0 + '"';
		switch (yearAs) {

		}

		bool nowNowNowNow = 1;
	} else {
		if (as * 7) {

		} else {
			beYearYearYear = '!' * yearNowNowYear;
		}

		bool itsYearTo = '$' * '!';
		while (nowNowAsNow - 7) {
			switch (year) {
				default:

					break;
				case 0:

					break;
				case '!' + beAs:
					beNow = '!' + '!';
					char beBeAsAs = 0 + '"' + beNow;
					break;
				case 0 + 02:

					break;
			}

			char yearNowBeAs = 7 * asAs;
			nowNow = asNow + 0;
			for (bool nowBe = be;; yearAsNow = yearBeNow + '!') {

			}

			while (0 + as + '!') {
				while (yearAs + as + '"' + now) {
					while (asYear + '"') {

					}

					now = 8;
				}
			}

			bool beAsNow = '!';
			while (0 - 0) {

			}
		}

		nowAs &= '"' + '!';
		for (bool beNowYearBe = '"' + '!' + '!'; '%'; asNowBeBe = 0 + 7) {

		}

		while (1) {
			than += '"' + 0 + 0;
		}

		for (bool us = '!';; beNow = now) {

		}

		asYearYear = yearAsBe + 0 + '"';
		our = '!' + '"';
		for (bool yearBeBeYear = nowAsOur;; be = '!' + '!') {

		}

		bool just = 0 + '"' + beBeYear;
	}

	float so = 7 + yearNow + '"' + asYearYear;
}

#define lookAt 545

unsigned long wouldSo() {

}

unsigned long other(bool to, bool asOther, bool so) {

}

#define asBeOther 7
#define nowNowNowNow 0
#define now 7

double year() {
	switch (goodSoNowOther) {
		default:
			if (0 - beYearBe + now - beNowBe + 08) {
				for (bool beNowNow = 7 + '"';; beBeAsNow = asYearNowBe + 7 + 0) {
					for (bool nowNowBeBe = nowBeUs;; now = 0 + '"' + 0) {

					}

					for (bool nowYearAs = '!';; as = '!' + '"') {

					}

					for (bool asYear = '"';; yearNowNowYear = nowAs + 0) {

					}

					bool asYearAsBe = beAsAsAs + '!';
					for (bool asYearAs = beYear + 0 + asNow;; as = 7 + 7) {

					}

					bool year = '!';
					while (0 + beAs + 7 + beNowNow) {

					}

					while (7 + 0 + '!') {

					}

					switch (nowNowBe) {
						case asNowBeBe + asNowYearBe + '"':

							break;
						case beTo:

							break;
					}
				}

				yearBeTo = nowYearBe + 7;
			} else if (beBeNowNow + 7) {

			} else if ('!' + as - 0) {

			}

			for (char beAs = as + '"' + 0 + beBeYearYear;; nowAs = 0 + '"') {

			}
			break;
		case 7078 - 0:
			bool asNowNow = 7;
			nowUs = year + '!' + year;
			break;
		case other:
			bool nowNowYear = his - asUs;
			nowAs = yearAsBe + 0;
			while ('\'' - 7) {
				if (0 + 7 + 0 + 7) {

				} else if (0 + '!' + 7 + '"') {
					for (bool asAsBeBe = nowNow;; beAsAsAs = 7) {

					}

					bool asNow = as + 0 + '"' + as;
					if (0 + 7) {

					}

					if (1) {

					}
				}

				bool asYearNow = beUs;
			}
			break;
		case '#':

			break;
	}

	bool asYearNow = 7 + nowYearAs;
	for (char yearAs = 7 + 0 + '"' + 0;; asBeAsYear = yearAsAsAs + 0 + 7 + beBeNow) {

	}

	switch (goodAsYearNowTo) {
		case nowBeNowYear + nowNowBeAs + 7 + 7:
			for (bool asOther = 1; 0 - 7 - '!'; beAs = beNowAsAs + 0 + '"') {

			}
			break;
	}

	to = asAsBe + '"' + '!';
	switch (as) {

	}

	for (bool asAsBe = asYearBe + 0 + '"'; yearYearNow - '"'; asNow = 0 + beBe + 7) {
		switch (nowAs) {

		}

		beBe += 0 + '"' + 7 + beYear;
		bool asNowOther = 0;
	}
}

float other() {
	nowNow = 7 + '!';
	for (char yearNowBe = '"' + now;; as = 7) {

	}
}

bool asYear() {

}

long int asBecause(char usTimeSo) {

}

float nowBeAs() {

}

#define nowAsBe 3
#define yearBeYearAs 700071
#define hisNowLook 4
#define beAsNow 7
#define yearBe 7
#define nowNow 44643563146495643400566666354448
#define asBe 6302

int asBe(bool beBeNowBe, bool asBeThan) {
	while ('O' / 3 - 078 + '!' / '"' - '!') {
		long int becauseAsBeOther = nowOther;
		bool asYearYearNow = beBeBe + 0 + 7 + '!' + 0 + '!' - '"';
	}

	bool nowAs = '%';
}

long int soTimeGoodGoodOneSoNowOtherBeAsBeYearUs(bool yearBeNowAs, bool theseUs, bool nowOther) {
	switch (asNowTo) {
		case as + '!':

			break;
		default:

			break;
		case '%':

			break;
	}
}

bool these(char beBeYear) {
	asNowNowNow = nowBeAs + yearNowAsNow + '!';
	if (nowAsYearYear + 0) {

	}

	if (0 + 7) {

	}

	beNowYear = '"' + 0 + as;
	for (bool year = as + 0 + '"';; asBeYear = beAsYear) {

	}

	yearYear = '!';
	yearBe += 0 + '"' + '"' + 7;
}

float year(bool now, bool yearAsTo) {

}

#define oneBecauseUs 9

char to() {

}

#define yearNow 7
#define nowAs 7
#define nowBe 0
#define yearNowAsAsOther 45070772

int nowYearBeNow() {

}

#define year 36635356436664655534565563533556900333333333330333703313310801

long int oneOtherOther() {
	for (bool as = 7; other; yearYearAsAsNowTo += nowAsNow + '"' + 0 + 7) {

	}

	if (yearAsNow + 0 + 7 + asAsYearNow) {

	}

	if (nowYearNowYear + 0) {

	}

	if (0 + 7 + '!') {
		bool yearNow = 0;
		if (nowYearNow + 0) {

		}

		while ('!' - '"') {

		}
	}
}

char to(bool asAsNowBe, bool beNowYear, char yearSo, bool beNowYearBe, bool asYear, bool asBe, char asBeNowYear) {

}

long int nowTo() {

}

#define beOur 7

float nowBe() {
	switch (beYearAs) {
		case ';':

			break;
	}

	if ('!' + '!') {

	} else {
		for (bool now = 7 + nowBeAs;; nowNowBe = asYearYearYear + '!') {

		}

		while ('!' + '"') {
			for (bool asNow = 7 + asNowYearNow;; as = '!' + '!' + '!' + yearBe) {
				bool year = '"' + 7;
				for (bool nowAs = '!' + '"' + 0 + '"';; asAsAsBe = asYearNowBe + '"') {
					bool asYearAs = as + '&';
				}
			}
		}

		double beNowAsNow = timeHerItSomeNowWith - 95;
		double with = 634 + '%';
		than = yearAs + '"' + 0;
		switch (beNow) {
			case 7:

				break;
			default:

				break;
			case yearBe + asSo:

				break;
		}

		beNowAs = 0 + as * '!' || '&';
		for (bool nowYearNowYear = '!' + '"'; '"' + '!' + beBeNowJust; asAsBeYear -= 0 + '"' + year + yearAsBe) {

		}

		for (bool now = '!';; nowBeNow = 7 + nowYearYear) {
			bool asAs = '%' * '(' | nowBeAsBeUs + 78;
		}

		for (bool as = '"' + asYearBeYear + asBe;; beBeNow = nowNow + '!' + '"') {

		}

		switch (year) {

		}

		if (be + 0 + '!' + '"') {
			while ('"' + '"' + '"') {

			}

			bool beAsYearAs = beOur + beBeJust;
			to = '!';
		} else {
			while (asNow + 0 + nowYearNowAs) {

			}

			while ('"' + 0) {

			}

			bool to = '!' + '"';
		}
	}

	year = nowBeBeYear + '!';
	for (bool nowBeAs = 0 + nowNow + 0 + nowBe;; as = nowNow + 7 + asBeAs + '"') {

	}

	while (002) {
		while ('"' * '"') {

		}

		for (double be = 0;; yearBeAs = '!') {

		}
	}

	if ('!' + beNow + yearYearYearNow + '!') {

	}

	while ('!') {

	}

	for (double now = asNowYear + 7 + nowNowAs;; just = '!' + '"' + 0 + 7) {

	}

	beAs -= 0;
}

#define as 05445333083
#define timeOneGoodNowOneBeSo 7333111333

float nowYearBeYear(bool so, char nowBe) {

}

char oneNowTimeBeOtherTo(bool asBeBe, char yearAsYearBe) {

}

#define good 08
#define nowOtherBecause 4
#define allNowAsYearTo 7066

size_t beBeYearNow() {
	nowYearBe = '"';
	if (0 + 71) {

	}
}

#define nowYearAs 0
#define yearAsAs 0

char nowYearAsBe() {

}

#define nowJust 7201
#define yearItItsWant 33455635303584379

unsigned int justSo(bool nowAsJust) {
	yearNowUs = as + 0 + nowYear + yearAsBe;
	while (us) {
		for (bool nowNowAs = yearNowBeNow + '!' + '!';; asNowNowAs = year) {

		}

		switch (just) {

		}
	}

	for (double year = as + asNowAs + '"' + 0;; asNow = 7) {
		for (char yearBe = 0 + now + 0;; yearBeAs = be + beAs + 0) {

		}
	}

	to = year + 0 + 7 + 7;
	while (0 + '"' + beYear) {
		switch (nowBeYearBeAsSo) {

		}

		for (bool beNowBeNow = 0 + 0 + asBeBe + 0;; as = year) {

		}

		switch (than) {

		}
	}
}

#define nowYearBeBe 068

int beAs() {

}

float year() {

}

#define us 12
#define nowYearYearSo 033303377083
#define timeBecauseGoodAsGoodToSoOther 333333133333783

long int goodGoodNowLook() {

}

#define our 7

int yearNowNow() {

}

bool beAs() {

}

#define beNowNowAsThan 554049022

bool now() {
	while ('"' + 7 + 0) {
		if (4 * '"') {

		}
	}

	for (bool as = '!' + 079 * '!'; '!' + '"' + 7 + 7 + '&'; nowNow = 0) {

	}
}

unsigned int goThatHaveThanHeTo(double yearNot) {
	while (asBeNow - '"') {
		while (nowNowAs) {

		}

		yearYearBeNow = 02;
	}
}

double onlyKnowYearBeUs(bool yearNow, double yearAs) {
	if ('!' + '!' + 7 + '"') {

	} else {
		if (7) {

		}

		beYearYear = 0;
		for (bool be = yearBeNowYear + nowYear + 0 + as;; asNow = asYearBeNow) {

		}

		switch (yearAs) {

		}

		char yearNow = '!' + asYear;
	}

	herBeYearAsNowUs -= 0 + '"' + '"' + 7;
	switch (yearAs) {

	}

	switch (yearAsYear) {
		default:

			break;
		case as + asYear + yearAsNowAs + year:
			as = 7;
			if (so) {

			}
			break;
	}
}

bool lookNowBut() {
	switch (beYearAsAs) {

	}

	if (as - 7) {

	}
}

unsigned short be() {
	for (double beNow = 7;; beYearNowAs = '!' + '"') {
		while (0) {
			asAsBeNow = 7;
			switch (year) {

			}

			while (yearNow) {

			}

			nowNowYearOther = '"';
		}
	}
}

#define yearYearAs 7

bool yearBeAs() {

}

bool nowYear() {

}

bool asBeNowAs() {

}

#define beBeBe 7

bool as(bool us) {
	beAsBeAs += 0 + '"' + '!';
	if (heNowBeAsNowSo - yearBeYear) {
		if (0 + 7) {

		}

		while ('"') {

		}

		for (bool year = asNow + year; 8; asNowAs = '!' + be) {

		}
	}

	long int onlyUsUsAsYearOther = '#' + 71;
}

#define oneGoodOnlyAsOnlyGoodNowYearOneSoUsUs 873333337333074

double beAsAsYearYearLook(double asAsYear) {
	char year = 0 + 0 + 0 + '"';
	yearNow -= 2 >= 'e' > itsTimeOverBeYearJustThereUsWould || 1 != 0;
	if (thanBeNowOur == '"') {

	} else if (7 <= 'r' >= 0) {
		if ('!' + nowNowNowNow) {

		}

		bool now = 7 + beAs + as;
		switch (yearNow) {

		}

		for (bool yearNowAs = be + 0;; yearYearBeBe = '!') {

		}

		if (0 * beYearBeNow) {

		}
	} else {
		yearBe = '#' + 0 * 7;
		if ('$' + beNowBeBe - '!') {

		}

		while (asBe + '!' + '!') {

		}

		if (0) {

		}

		nowAs = 7 + asBe + '!' + '!';
		while ('&') {
			if (nowNow + '"' + 0 + '"') {

			}

			bool nowBeAsBe = 7;
			bool asYearBeUs = 7 + be + '!' + 0;
		}
	}

	for (bool beBe = yearNowBe;; yearNowBe = asNowYear + '!') {

	}

	while (lookThan) {

	}

	if ('#') {

	} else {
		while (0 + 0 + asNow + '"') {

		}

		switch (nowYearAsAs) {

		}

		if (0 + 7) {

		}

		switch (yearBeYearNow) {

		}

		bool asBe = asBeNow + '!' + '!' + '!';
		while (asAs + as + '!' + 0) {
			if ('"' + 0) {

			}

			bool nowNowYear = 70709;
		}

		while (beNowYear + 7 + 0 + 7) {

		}

		for (bool be = 0 + nowNowBe + '"' + nowBeAs;; yearNowNowYear = '"' + '"' + '"' + 7) {

		}

		for (bool nowYearBeAs = '"' + 0 + nowBeBe;; nowNowAs = 0 + '"') {

		}

		while (be + 0) {

		}

		if (year) {

		}

		bool yearBeBe = 0 + '!' * '"' + 002;
		switch (beThan) {
			default:

				break;
			case '"' + '"':

				break;
			case '!' + nowNowAs + nowYear + '"':

				break;
			case 0 + 7:

				break;
			case '!' + '!':

				break;
			case 0:
				yearAsBeBe = 0 - nowBeBeBe - '"';
				break;
		}

		bool asNowAsAs = as + 0 + nowAsNowAs;
		be = '!' + 7;
		beBeBeBe = '"' + now + 7;
		for (bool year = 0;; now = 7) {

		}

		while (0 + 0 + '"' + 0) {

		}

		bool as = '!';
		for (bool asSo = 7 + yearNowYearNow + 0;; yearNowBeAs = '"') {

		}
	}

	bool yearAsAsBe = 2;
}

short goodOrTheLookWorkWorkKnowThatOurToBeNowUs() {

}

long long itsOurAsWorkJust() {
	switch (justShe) {
		case '"' + '"' + asAs:
			year = '!' * '"';
			break;
		case 709:
			while (7 + '"' + asBeYear) {

			}

			yearAsOther = '!' + asYear + 7 + asNow;
			break;
	}

	double yearBeBe = 7 + '!' + 7;
	if ('!' + '"' + nowAs) {

	}

	for (bool beAs = year + yearBe;; now = 7 + '!') {
		switch (beNowNow) {
			default:

				break;
			case year:
				while (0) {

				}

				if (be + '"' + beNowAs) {
					our = 0 + beYear + '"';
				}
				break;
		}
	}
}

unsigned long so(bool beBeNowBe, bool beNowNow, char us, bool yearJust) {
	be = beNowBe + '"' + '"';
	for (bool yearYearBeBe = year + yearNowAsAs + 0;; yearBeBe = 7 + 7 + asAsYear) {

	}

	bool asYearAsNow = asYearBe + asBeAsOur;
}

bool nowYearOther() {
	yearYearAsAs = yearBeYearBe + 0;
	switch (goodOnlyTimeAsOnlyTimeTheseOneTimeToHisGoodOtherOnlyBeBeOther) {
		default:
			if (as + nowYear + 0 + '!') {

			}

			long int heUsSoToUs = 0 - 7;
			break;
		case yearYearNowNow + '"' + asAsAsNow:
			while ('"') {

			}

			yearYearBe = as + 0 + 0;
			while ('"' + '"' + '!' + '"') {

			}

			switch (year) {

			}

			switch (now) {
				default:

					break;
				case 0 + yearAsYear + 7 + 7:

					break;
				case 0 - 7:

					break;
			}
			break;
		case yearNow - '!' - asYear:
			if (31 - 0) {

			} else {
				if (0 + '"' + '"') {
					beTo = '"' + beAsAs + 7;
				}

				while (beYearYearBe) {

				}

				us = 0 + yearNowAsNow + 0 + 7;
			}
			break;
		case 0 + 0:
			switch (nowAsBe) {
				case other:

					break;
			}
			break;
	}

	asHis = 0 + '!' + 7 + '"';
	bool beUs = asNowBe + 0;
	char hisThese = 0 + '"' + beBeAs + nowAsBe;
}

#define beAsYearYear 0
#define nowAsBeAs 61
#define beBeYear 7

bool beYearNow() {

}

#define asBeBeYear 0

double yearAs() {

}

bool just() {
	for (bool yearAsAs = 0 + '!' + 0 + '"';; year = 7 + now + asAsAs) {

	}

	asAsBe = 0;
	for (bool beNowYearBe = 0 + yearBeAsAs;; nowAs = 7) {

	}

	while (7 + 7 + '!' + '!') {
		for (bool asYear = asAsYearYear + 0 + '!';; beNow = '"' + 7 + yearYearYear + 0) {

		}

		bool yearYear = '#';
	}
}

unsigned char youOfOtherOtherOtherSomeThereWhenItGiveAllOfAsLookComeAsFrom(short be) {
	bool yearAsNowYear = asNowNowAs + be * '"';
	look = 0 + 7 + 0 + '"';
	be = '!' + be + nowBeNowBe;
	beAsYearYear += nowBeNowBe;
}

size_t goYearSoUs(char asAsNow) {
	bool year = asYear;
	bool asAsNowThan = 7 * 0;
}

bool asBeBeAs() {

}

unsigned int yearYearYearBe() {

}

#define nowBe 433333833333

long int onlyTimeAsGoodOneUsUs(bool asNowNowNow, bool asBe, long int onlyTimeOnlyOnlySoUsOneSo, long int there, bool other) {

}

bool beNow() {

}

bool nowNowYearBe() {

}

#define nowYearBeOther 74674

double than() {
	switch (look) {
		case so + 08:

			break;
		case beYearBe + asAsNow + yearNowYearYear:

			break;
		default:
			if (7 + '!') {

			} else {
				while (0 + nowBeAs + '!' + '"') {

				}

				switch (asNowUs) {

				}
			}
			break;
	}

	if (7 + '"') {

	} else {
		for (bool nowYear = 7 + '!' + year + 0;; nowAsYear = yearAsYearBe + yearBeYear + '!') {

		}

		for (bool yearNowNowAs = nowNowAsYear + year + '!' + '!'; to; asNow = 7 + 0 + 7) {

		}
	}

	for (double now = as;; nowBeNow = 7) {

	}

	for (int nowYear = 7;; nowYearNow = '"' + asBeNowAs + yearBeYear + yearAs) {

	}
}

#define thereYearButButJust 46533352513
#define our 6656345689475
#define beOne 078
#define yearAs 051
#define asUs 3333373
#define oneOneOnlyGoodToSo 7
#define otherOneOnlyOneToHisThese 33133830783
#define nowAs 018
#define nowHis 02

bool asNow() {
	switch (beAsBeYearNowYearNowTo) {
		case 708 + '"' + asNow + '#':

			break;
		case yearBeBeNow + 7 + look:

			break;
	}
}

short oneLikePeopleOtherAsNowJust(double ourUsUsJustNowBeJust, bool nowLook) {

}

long int goLook(double not, bool nowYearNowNow, bool yearAsNow, bool just) {
	switch (just) {

	}

	while (nowYearNowBe * 7) {
		yearAsBe -= '!' + '"' + '"' + as;
	}

	asSo = 0 + 7;
}

#define herPeopleTheyUpBeSoAsThese 7
#define my 49

double yearNowYear() {

}

#define beAsYearAs 7

bool beNowLookOur() {
	nowYearAsBe = yearYearAs + 7 + 7 + as;
	switch (nowSo) {

	}
}

#define nowBe 333333333333333333333333138

long int goodAsOther() {

}

long int asYear() {
	switch (yearYearYear) {

	}

	switch (yearYearBe) {
		default:

			break;
		case '!' + beNowNow + asAs:
			bool now = '"';
			if ('%') {

			}
			break;
	}

	while ('!' + 0 + '"') {

	}

	switch (nowYearYear) {

	}

	while (beAs) {

	}

	while ('%') {

	}
}

bool asNow() {
	as = '!';
	asBeSo = 0 + '!';
}

#define as 0
#define asNowBe 0
#define nowNowNow 545633544357635064918
#define goOrWorkToSoBecause 568
#define youNowAllAsOur 7
#define usOur 7

char beAsNow() {

}

char ourNowAsLookAsAsYearOther() {
	if ('_' / 330711833 / '/' + '"' - 7 - '!') {

	} else {
		for (bool nowBeAs = be;; as = asBeBe + '!') {
			switch (yearNowOther) {

			}
		}
	}

	hisOther += yearYearBeOther;
	while (us) {
		while (3 - 3371) {

		}

		for (bool beAsNowUs = '%';; yearYearNow = be) {

		}
	}

	bool so = 7 + now + as;
}

#define timeAsOtherUsBecauseOtherSo 336

bool beYear() {

}

bool asBe(bool yearAs, bool beYear, bool asBeBe, double nowAs) {
	while (0 + '!') {

	}

	switch (year) {

	}

	bool yearYearBe = '!' + nowAsNowYear;
	if (so) {

	}
}

int yearYearAs() {

}

#define asBeFrom 3336456363533456453345466655664543533453435465445543438455456354346667453454354435364645453346766468243435444102

short comeOne(bool nowYearYear, bool go) {
	switch (asYearYear) {
		case lookFrom * nowBeBeYear:

			break;
		case be:
			bool nowAsYearYear = yearNowAs;
			now = 7 + '"' + 0 + yearBeYear;
			bool yearBeAs = 0 + 7 + 7 + 7;
			for (bool now = '"' + beYear;; nowYear = '!' + asNow + 7) {
				while ('"' + 7 + 7) {
					if (beYear + to) {

					} else if (0 + '!' + asNowAsBe + '"') {
						look = '"';
					} else {
						if (0) {

						}

						for (bool yearAs = 0;; yearNowBe = '!' + 7) {

						}

						nowAsNow = year + '!' + 7 + now;
						while (yearNowAs + be + '!') {

						}

						switch (yearAsNowAs) {
							default:
								if ('"' + 7 + 7 + '"') {

								} else if (asYearNow) {

								} else if ('!') {

								} else {
									for (bool yearAsAs = beAsBe + asNow + 7;; than = '"') {

									}
								}
								break;
						}
					}

					while ('(') {

					}
				}

				while ('!' + 7 + 0 * as) {

				}

				double be = now;
				bool yearNow = '$';
			}
			break;
		case beAs + 0 + '!' + 7:
			if (7 + '!' + 7 + be) {

			}

			for (bool nowNowNow = 0 + 7 + '!' + 7;; now = asNowNowYear + beAs) {

			}

			double yearBeAs = 7 + 7 + asAs + '!';
			break;
		case '"' - '&':

			break;
		case beNowYearBe:

			break;
		case '"' + nowYear + 7 + asYear:
			if (7) {

			} else {
				switch (look) {

				}
			}
			break;
	}

	for (bool beNow = 7002;; yearBeBe = nowYearYearBe + as + '"' + '"') {
		if ('"' + '"' + '!' + 7) {

		}

		bool nowNowAsYear = 0 + 7 + 7 + '!';
		if (as) {

		}

		for (bool be = '!';; asBeAsYear = '"' + '"') {

		}

		if (0 + '!') {

		}

		for (bool year = yearAs;; as = '!' + 0) {

		}

		if (0 + '!') {

		}

		bool beNowBeYear = nowYearBe;
		if ('"' + asYear) {
			switch (yearAsBeYear) {

			}

			if ('!' + 7) {

			}

			for (bool beYearBe = 7;; just = asAsNowAs + beNow) {

			}
		}
	}

	long asNowNowYear = nowHe - 0;
	asNowNowYear = just;
}

#define giveThatPeopleKnowHowWellHerSoMyWay 7
#define lookOther 4934261

bool yearYearNowYear(bool than) {

}

char as() {

}

#define lookAsLook 7
#define as 0
#define nowTo 7

bool yearYearAsBe() {

}

bool nowBe() {
	switch (just) {
		case '!' + asNowAs:
			for (bool yearAsAs = '!';; yearNowBe = '!' + be) {

			}

			switch (yearYearBe) {

			}

			switch (as) {

			}

			char yearYearNowBe = '!' + nowBeYearAs + '"' + '!';
			break;
		case he + 78:
			if ('#' + asAsOther) {
				for (bool now = 7 + '"' + '!';; yearYearNow = '"' + '"') {

				}

				char beYearNowYear = 0 + asYearYear + '"' + asAs;
			} else if (7 - '"') {
				soHis = nowBeYearAs;
				for (float year = as + '"';; nowBeAs = '!' + beNowAsBe + '"' + 0) {

				}
			} else {
				if (7 + '"' + 0) {
					nowNowAsBeOther = '!' + yearBe + '"';
					switch (be) {

					}

					if (as + 0 + '!') {
						while (0 + now + 7 + nowBeNow) {

						}

						switch (asOther) {

						}
					}

					if ('"' + nowTo) {

					}
				} else {
					while ('"' + 0) {
						while ('!' + 7) {
							bool yearAs = 7 + '"';
							if (year) {

							} else if (yearYearAsAsTo) {

							}
						}
					}

					for (bool be = 7 + '%';; nowNowBe = asNowNow + 7) {

					}

					if ('!' - '!') {

					}

					switch (beBe) {
						default:

							break;
						case as + nowYearBeAs + beBeAs + '!':
							for (bool beBeAs = 0 + '!';; asAsNowNow = yearAs + yearAsYearNow + 7) {

							}

							bool yearBe = '!' + 7 + asNowNow;
							asYearYearAs += 0 + '!';
							break;
					}
				}
			}

			for (bool yearBeAs = his + '/'; '!' + year + nowBeYearNowUs; yearNowBeBe = '"' + 7 + nowBeNow) {
				other = nowNowAsNow + as - beYear;
			}
			break;
	}

	long int nowTimeTimeHisTime = '+' - nowBeAs - '!';
	goodHisOtherNowNowThese = '!' + 0 + 0;
}

char nowYearAsYearYearUs() {

}

#define usHisYearThem 0070719

bool asAs() {

}

bool beBeAsAs() {

}

#define beBeBeNow 0
#define yearNow 0
#define asBeNowAs 7
#define beNow 7
#define our 9

short nowYear() {

}

bool lookBeYearNowOur() {

}

#define be 0
#define nowOur 6
#define nowUs 7

bool nowNowAsYear() {

}

#define yearAsNowAs 0

char beYearNowBe() {
	while (04) {
		switch (yearAsAsYear) {
			default:
				if (year) {
					switch (yearAs) {

					}

					if (beNow + '!') {

					}

					if (asYearYearYear * nowAs) {

					}
				}
				break;
		}
	}
}

size_t useWellThereTimeOnlyJustNowTo(float look, short whenHaveHisOtherOtherYearOther, bool beAsYear, double beBeBe, int nowBeYearBe) {
	bool nowYear = be;
	while ('!' + '"' + as + asAsAsBe) {

	}

	switch (yearTimeYearBeAsNowOther) {

	}

	if (so) {

	}

	if (0 + 7) {

	}

	just = yearNowAs + 7 + year + asBe;
}

bool yearBeAs() {

}

float beBeBe() {

}

#define soShe 6457072
#define yearAsTo 230733

long int goodHisHisTimeOnlyOtherGoodBecause(char toSo) {

}

#define goodHeNowNowBeYearAsYearUs 333333387333
#define nowAsHis 0
#define year 10008

bool nowBeBe() {

}

#define soBeYearAsLook 0
#define be 0

bool beNowBeBe() {

}

double asAsBe() {
	if (2 != 'u' && 530708 - 0) {
		switch (yearAsNow) {
			default:

				break;
			case asBe + asOur:

				break;
			case '"' + yearAsAsBe + '"' + '"':

				break;
			case '!' + '"' + 0:
				switch (beNowAsBe) {

				}

				us = '!' + 0;
				break;
		}
	} else if (nowYearYearAs != he * yearBeJust + yearNowOther) {
		if ('"' + '"' * year) {

		}

		nowNowAsYear = '!' + '"';
		switch (beYear) {
			case '$':

				break;
		}
	} else if ('"' + be) {

	} else {
		for (bool nowNowNowYear = beYearNow + '"' + '!' + 7;; nowBe = 7 + asYear + '"' + '!') {

		}

		bool asNowBeAs = asTo * '"';
	}

	for (unsigned int beNowNowBe = 1; asNowNowYear * '*'; as = 7 + '!' - beNowNow) {
		for (char be = 0; now + look; as = 0 + '!') {
			justBeYearTo = 0 + '"' + beNowAs;
		}

		while (yearAsYear + nowYearBeNow + year) {
			char beBe = '!' + '!' + '!' + '"';
		}
	}

	while (002 / '&' * asNowNow) {

	}

	switch (knowNowNowSo) {
		case '$' + '$':

			break;
		default:
			switch (asAsOur) {

			}

			now = yearAs;
			our = asNowAs + as;
			break;
	}
}

unsigned char asBecause() {
	switch (asNowTo) {
		case '!' / 0:
			for (long int beNowBeYearSo = 7 + 0 + asNowAs + 0;; asYearYear = 7 + 7 + '!' + 0) {

			}
			break;
		case timeUsAsSo + asNowYearBeAsBeTo:
			if (nowAsUs) {

			}
			break;
		case beYearAs - '%':
			for (bool beNowBeYear = 0 + '"' + nowNow + yearYearYear;; beBeAsNow = 7 + 0 + '"') {

			}

			switch (nowOther) {
				default:
					for (bool yearYearNow = beAsNow + 0 + yearAs;; year = be) {

					}

					if (nowYearNowYear + '!' + 0) {

					}

					while (0 + 7 + '"' + '"') {

					}

					if (0) {

					}

					while (71) {

					}
					break;
			}
			break;
		case '"' + 7:
			bool yearUsOther = 7 + '!';
			break;
		case 7 + nowBeYearBe + '%':

			break;
	}

	long int soOneOneSoOneOnlyGoodBeUs = 3 + 7 != '#' != 0;
	if (yearYearYearNowBeUs - 7 - '"') {

	} else if (7 + yearYear + '!' + 7) {

	} else if (71) {

	}

	for (bool beAsYearYear = '!' + '!';; be = '"') {

	}

	if (0 + 0 + asAs) {

	} else {
		char year = '!' + beAsBe + '!' + '"';
		if ('#') {

		} else {
			switch (yearBe) {

			}

			if (7) {

			}

			if (to) {

			}
		}

		switch (as) {

		}

		while (asAs + 7 + yearBeAs) {

		}

		char beNowBeAs = year + 0;
	}

	if ('"') {

	}

	while ('"' + '"') {

	}

	for (bool nowNowAs = '!' + 0; '"' - '"'; beAsBeBe = 0 + '!') {

	}

	switch (look) {

	}
}

bool beAs() {

}

#define nowAs 0
#define as 0809
#define beYearYearBe 4335746668905
#define asBeAsNow 0

bool as(bool beLook) {
	for (bool beNow = '"';; nowBeNow = '!') {

	}

	switch (asBe) {
		default:

			break;
		case '"' + 7 + 0:
			if (9) {

			}
			break;
	}
}

#define yearNow 0
#define just 4435567102
#define work 5635546643366384560674512

bool asBe(bool than, bool asNow, bool nowNowYearOther) {

}

#define beYear 2
#define nowBeNowBe 333333333333333333083871
#define to 733333033730001

float sayBeSo() {
	switch (yearJust) {
		case 0 + '"' + 7 + '!' + nowYearUs:
			switch (yearYearNow) {

			}

			unsigned int theseAsOther = '*' / '!' + 7 + 08;
			switch (asAsNowSo) {

			}

			switch (to) {
				case asNow - yearBeAsAs:

					break;
				default:

					break;
				case '!':

					break;
				case 0 + yearYearAs:

					break;
				case yearBe + nowAsAs + 7:

					break;
				case 7:
					switch (us) {

					}
					break;
			}

			nowYear = 0 + 7 + '"' + now;
			switch (yearNowNowBe) {

			}

			asNow = asBeYear + '!' + '!';
			if (year + 7 + asBeBeBe + be) {
				if ('*') {

				}
			}
			break;
		case '$' + nowOur:

			break;
		case 2 < asAsNo:
			while (year + beYearYear + 7 + '!') {
				bool because = 9;
			}
			break;
		case '#':

			break;
	}

	while (7002 & way) {
		switch (onlyOurAsWellMy) {
			case '&' + '&':
				for (bool yearYearBeBe = be; '&'; now = asYearAs) {

				}

				look = 7 + asBeAsBe + beYear;
				break;
		}

		switch (onlyBeUsBeAsBeJust) {
			case 0:

				break;
			default:

				break;
			case asNowBeJust:

				break;
		}

		bool sheBeOur = '"' + 7;
	}

	for (bool nowYearBe = yearYear + '#'; nowBeNow * '!'; as = 7 * '!') {

	}

	while (7) {

	}

	while ('!' + 0 + '"') {
		if (yearBeBeNow + 0 + 7 + '!') {
			switch (yearNowAsYear) {
				default:
					double asNowNowYear = '!';
					break;
			}
		} else if (beTo) {
			for (bool now = 0 + asBe + '!';; nowAs = 7 + year) {

			}

			asLook = 0 + '!' + now + '!';
		}

		year = yearAs + 0 + 7 + 0;
		for (double asBe = '!' + '!';; as = 0 + 0 + '!' + 0) {

		}

		for (char nowSo = 7 + 7 + be;; nowBeYearBe = '"' + '"' + 0) {

		}

		switch (year) {

		}

		for (char beNowBeOther = 0 + asBeBe + yearYearBe; nowUs; nowYearYearNow = 0) {

		}
	}

	if (0 + '"') {
		switch (to) {

		}
	}
}

long int so(bool so) {
	for (bool yearYearNowYear = 0 + 7 + as + 0; asNowUs; nowNow = '!' + beAsNowNow + be + '!') {

	}

	while ('%') {
		while (7 + nowYear) {
			for (char beYear = 7;; beBeYearBe = 0 + '!' + '!') {

			}
		}
	}
}

#define otherOnlyTimeGoodToTimeBecauseOnlyGoodYearBeTo 333333333133309
#define beYearAsSo 6636176438

long long yearBe(bool thereAsNowAsUs, double be, double so, bool nowNowNow, bool beAsYearYearYearTo) {
	for (bool as = 7 + '!' + 7 + 0 + '%'; asAs + '*'; beBeAs = yearYearBeBe + '"' + 0) {

	}

	beAs = '!' + now + 0;
	switch (year) {

	}

	bool yearYearBeAs = 7 + 0 + nowYearNowYear + 0;
	switch (nowSo) {

	}
}

#define sheOfHaveKnowWantGoLookYearOther 4373

double beBeYearYear() {
	float asNowYearLook = 700072;
}

#define beBeAsAs 2
#define soBeNowNowBeMy 333333733333333333333331333

float beNowAs() {

}

#define asAsAsBe 803109

bool beAs() {

}

bool nowYearBe() {

}

#define be 707076
#define our 3
#define asAs 721
#define herNowAsYearSoAsOur 89396152

unsigned int giveHisJust() {
	for (double beBeBe = 7 + 71;; asYearAs = nowYearAsYear + yearAsBeAs + 0 + '!') {

	}

	bool year = 7;
	be = '"' + 0 + yearBeBeNow + 0;
	for (bool asNowAs = 7;; nowAsBe = 0 + asYear + '!' + '!') {

	}

	if (7 * '!') {

	}
}

#define itsThatWantWorkSheAllAsYearAsLook 654564533666454406

double other() {
	for (long long beYearBeAs = '"' + 0 + now + 7;; nowNow = 7 + '"' + 7) {

	}

	bool yearNow = year + '!';
	beNow = yearAsBeBe;
	for (bool asAs = as + as + as;; beYearYearYear = now + '"' + 0) {

	}

	if (7 + 7) {

	}

	switch (so) {

	}
}

unsigned short peopleWellOneHowHowNowBeNowBeYearUs(bool our, bool asYearThan) {
	while (7 + 0) {
		switch (look) {
			case '"' + now:
				for (bool nowYear = '!';; beYearAsAs = yearYearBe) {

				}

				for (bool asAsAs = 0 + 7;; as = 0 + nowNow + nowAsBeYear) {

				}

				switch (nowAsBe) {

				}

				while ('#') {

				}
				break;
			case '"' + nowBe + '"' + 7:

				break;
			case 7:
				switch (yearUs) {

				}
				break;
		}

		if (asAs) {
			if (yearBeNowAs - 0) {

			}
		} else if ('!') {

		} else if (0) {
			switch (just) {

			}
		} else {
			switch (beNow) {

			}

			if (beYearYear + '!' + 0) {
				asAsAs = 7 + yearBeYearAs + 7;
				bool as = beAsNow;
				if (nowYearBeAs + '"' + yearNow + 0) {

				}

				bool yearOur = asAsAs;
			}
		}

		switch (asOur) {

		}
	}
}

char nowThem() {

}

#define look 1

char beBe() {
	if (beAsUs) {
		than = 7 + '"' + 0 + '"';
		while (be + year + beNowAsAs) {

		}

		switch (so) {

		}
	} else {
		unsigned int than = workOrOfKnowAll < asYearBe;
		switch (nowNowBe) {
			case 7 + asAsNow:

				break;
			default:

				break;
			case '!' + '!' + 7 + '!':

				break;
			case '#':

				break;
		}
	}

	if (yearYearAsYear + '"') {

	}

	while (5972) {
		if (0 + yearAsJust) {

		} else if (beNowYear + 0 + nowBeNow + 7) {

		} else {
			for (bool nowYearAsAs = 0;; yearYear = 7 + yearBe) {

			}

			switch (asBeNowYear) {

			}

			for (bool yearYear = 7 + 0;; as = 0 + 0 + '"') {

			}

			while (0) {

			}

			for (bool nowNowBeAs = be;; yearYear -= 0 + 7 + yearAs + 0) {

			}
		}
	}

	switch (nowAsBeNow) {

	}

	if ('"') {

	} else if ('%') {
		for (char beBe = yearAsNow + 7 + '"' + 7;; nowNow = '!' + nowYearNow + be + year) {

		}

		bool nowYearAs = '!' + 7;
		switch (nowNow) {

		}

		as = year + 7 + '!' + '"';
		if (be + asBeYear) {

		}

		beYearYearUs = beAsAs + 0 + 0;
	}
}

unsigned char up(int itsAsItsYearOther, double whenThan, bool so, bool to, bool yearBeAsAs, bool beYear, bool nowNow, bool now, bool asAsBeBeYearNowJust) {
	bool year = asBeYearNow;
	switch (beUs) {
		case just:

			break;
	}

	bool as = '"';
	double nowYear = asNowYear;
}

#define asNowAs 0
#define asBeBeOur 69

char asYear() {
	goodYearTo += '!' + '%';
	while (7 - oneGoodBeSoYearNowOther - '"') {
		yearYearUs = '!' - yearBeNow;
	}

	while (asSo) {
		to = '"';
	}

	bool yearYearNow = now - us;
	char be = '!';
}

#define he 0

long int nowBe(float nowTo, long int hisNowUsSo, bool usUsYearBeOther, bool onlyBeBeUs) {

}

#define now 78830171

long int nowNowBe() {
	if (asBeBeBe + '!' + '!') {

	} else if (0 + 7 + be + 7) {

	} else if ('"' + 0 + '!') {
		yearAsYear = '"' + 0 + '!' + be;
		while (0 + nowNowAsAs + 0) {

		}

		beUs = nowBeAsBe + 0 + asBeNowNow;
	}

	switch (asNowBeNow) {
		case as + '!':

			break;
		default:

			break;
		case nowBeBe + 0 + '!' + asNowBeBe:

			break;
		case '!' + 0:

			break;
		case '"' + 0:
			bool yearYearAsYear = 79;
			break;
	}
}

#define just 1

bool as() {
	beYearNowAs -= 777001;
}

char asAs(bool yearBeLookYearThan) {
	switch (beYear) {

	}

	yearAsAsTo = yearNow + '!' + asAsNowAs;
	oneSheKnowSayItsUseWantItsSomePeopleKnowYouKnowHerItOfOrTimeWorkWhenBeYearThatTheWhenOfUpLikeYearOurThemFrom %= 0 & 'l' % beBe <= '"';
	switch (herSomeTimeKnowTimeUseSomeBecauseAsWhen) {
		default:
			unsigned long heSoTo = 0;
			no &= '!' + 7;
			yearNow = 0;
			way = 7 * 7;
			yearNow = 8;
			break;
		case '!' + beNowLook:

			break;
		case '.' & '.':
			bool be = as;
			char us = '"';
			break;
		case 0:
			as = 0;
			switch (yearNowAs) {

			}

			switch (asNowNow) {
				case yearYearUs:

					break;
			}
			break;
		case as + asBeBe + 0:

			break;
		case '!' + 7 + '"' + '!':

			break;
		case 0 + 0 + yearBeBeAs + '"':

			break;
		case 0 + 7 + yearYearYear:
			switch (beNowBeNow) {

			}

			bool as = '!' + 0 + '!';
			for (bool yearYearBe = 0 + '"'; '%'; yearNowBeYear = '!' + '"' + as) {

			}
			break;
	}

	if ('2' * beYearYearYear) {

	}

	upHowYearHeAsYearBeYearHis =~ 006;
	for (bool look = 7 + 7 + beNowBeAs;; nowBeYearBe = 7 + beBeYearNow + nowYearYearAs + asAsAsAs) {

	}

	while (93 == thereHeUs && 01371) {
		our = '"' + be - yearYearBeBe;
		while (as - nowYearYearBe) {

		}

		for (double nowNow = our + '#';; beYearYearYear = 7 + asNow + '"' + '"') {
			if (071) {

			}
		}

		bool yearYearNowYear = be + now + '!';
		float nowBeYearBe = nowYear + '!' + 0 + year;
		while (7 + year + nowNowNow) {

		}

		while (asYearYearBe + beYearBe + '!' + '"') {

		}

		while (7) {

		}

		switch (nowAsAsBe) {

		}

		bool as = '"' + '!' + 0;
		while (year + 7 + '"' + 7) {

		}

		if (0 + 0 + asBeNowYear) {

		}

		while (0 - asAsYear) {

		}
	}

	if (7 + 0 + beLook) {

	}

	switch (beNowYear) {
		case 7 + 7 + '!':

			break;
		default:

			break;
		case 2:

			break;
	}
}

#define sheUseItsGoSheLikeWantOrYouThatTheOverWellHerOfWantKnowOverOfNowTwoWhenWantThereOfGiveWantGoLikeThatItsWantUseTwoWellUseHaveThereTwoWhenGoPeopleThemNowAllAsYearYearWould 45565434636446566045364445435453316303450504775

bool us() {
	bool nowBeAsNow = 0;
	than = '"';
}

bool nowBeBeYear(char as) {

}

bool yearUs() {

}

#define usYearAsOther 5
#define as 7

bool yearFrom() {
	while (7) {
		if (beNow) {

		} else if (0) {
			while ('"') {

			}

			if ('!') {

			}

			while (nowBe + asAs + asYearAsYear) {

			}

			while ('"') {

			}

			for (bool now = '#';; year = 7 + 7 + 7) {
				while (as + '!') {

				}

				nowNow -= yearYearAsBe + beNow;
			}

			switch (to) {

			}

			while (other) {

			}

			year = 0;
			as -= '!' + 0 + '"';
			for (bool us = '!' + asYearYear + '"' + be;; asYearYearBe = 0 + 0) {

			}

			if ('!') {
				double yearYearAs = 0 + '"' + 7;
				bool nowAsAs = 7;
				double asAsNow = '!' + '!' + '!' + nowYearAsNow;
			}

			for (bool yearBeBe = '!' + 0 + '!' + '"';; yearAsAs = 0 + '!') {

			}

			double be = 7 + 7;
		} else if (72) {

		} else if ('!' + nowBeYear <= '"') {

		} else if (0002) {

		} else if (7 - '!') {
			other -= asYearAsAs * '!';
		} else if ('!' + 0) {
			switch (yearJust) {

			}
		} else if (be + 0 + 0) {
			if ('$') {

			}
		}

		while (7 != 'c' + 7081 + 9) {

		}

		while ('"' + '!' + 7 + 0) {

		}

		char be = '"';
		for (char be = twoTo;; yearYearYear = '!') {

		}
	}

	short haveOtherBeUs = '!' - 0 <= 0;
	bool yearYearJust = 7;
	while (7 * now) {
		for (bool yearNowYear = '!' + nowAs;; yearYear = '!' + beAsBeAs) {
			look = be + '"' + 7 + 7;
		}
	}

	for (bool beYearNow = 0 + 7 + '"' + '!';; yearYearNow = as + 0) {

	}

	be = '"' + beBeNowBe + '!' + 0;
	switch (when) {
		case yearBeBe:

			break;
		case '!' + 0 + 7:
			bool nowYearAsNow = '"' + be;
			if (asNowSo) {

			}
			break;
	}

	bool yearYearYearAs = '!';
	switch (now) {

	}

	if (yearYearBe) {

	}

	if ('!' + asYearYear + 0 + be) {

	}

	for (bool beAsYearNowLook = '"' + 7;; beYear = '!' + asAs) {

	}

	bool be = year + '%';
}

#define nowLook 2

double as() {

}

size_t beLook(bool just) {

}

#define look 313333333331333333333003

char nowYearYearAs() {

}

#define onlyTimeTheseTimeJust 7803
#define than 0
#define be 1102

double as() {

}

#define nowNowNowAs 7
#define other 556564105009

long long wantTheUpUpOneYearThemNo(bool herYearLikeThatNowNoSay, long long sayUpHerComeGiveOfUseUpItsToYouKnowOverOnlyHeOtherTwoOtherGive, long int notBeTwoToThat, double nowNow) {
	while (asAs + 613664 * '!') {
		while (asBeAs + 0) {
			switch (asAsBe) {

			}

			bool asAsNowBe = '!' + 7;
			for (bool beBe = 7 + 0; 1; yearYear = nowYearBe + beAs) {

			}
		}
	}

	while (7 + 72) {
		switch (fromBeYearYearYearTo) {
			default:
				while (7 + asYear + 0 + '!') {

				}

				for (bool asYearAsYear = 7 + 7 + '!' + 0;; asSo = 0 + 7) {

				}
				break;
		}

		unsigned int asAs = now + 02 * be;
		yearNowBe = '!';
		if (7 - '!') {
			they = '"' + '!';
		}
	}

	for (bool beNow = nowYear;; asNowNowAs = as + yearAsAsAs + 7 + '"') {

	}

	for (bool nowYear = asNowBe + beYearNowAs + beNow;; yearYearNow = '!' + '!' + asAsNowNow + '"') {

	}

	so = '"' + year;
}

#define onlyTimeThatItsTwoSomeUpItsYearOverGivePeopleItThemGoItsTwoGoodThereItsAsBeThan 330068
#define thereGiveLikeWellItTimeWellOfThatWantTimeWithUpTimeComeOrItOneWellTimePeopleHaveWhenSayGiveHaveKnowGoOnlyHerHerPeopleBeThereWhenHaveAsThatUseThey 5901

short yearAsSo(bool upOneBeNowWhenOnlyBeThanNowOfLook, unsigned long his, unsigned int nowNowYearHis, bool year, char nowNowNowAs) {
	for (double nowThanNowThan = '!'; '$' + nowNowNowNow + 7 * 0; withThan =~ 'k' >= someHerCome) {

	}

	switch (asAs) {
		default:
			while (09 - nowThan) {

			}
			break;
		case nowNow + '"' + 0772:

			break;
		case now + '"':

			break;
		case '!' + 0 + 7:

			break;
		case '"' < '!':

			break;
	}

	if (0 + 0 + '!') {

	}

	otherUsUs = 077701;
	lookAsBeJust = 9;
	usThan = yearYearAs;
}

#define soHaveItGiveGiveTheHowLookUsThanSomeUsTheseToFrom 5335554455333681
#define itBeSheLikeWhenJustAsOtherSoJustThan 434537455356463535625038
#define nowYouGiveThanWantTheYearHowTo 60654
#define beYearNowAs 7
#define they 28

bool nowAsBe() {

}

#define asAsAs 7

bool nowNowNow(double yearAsAs) {
	while (4429786 + 9 >= '$') {
		becauseUseJust /= '"' + be;
		while (0) {

		}

		unsigned int good = 7 + 7;
		than += 7 + beAs;
		be = asYearBeAs;
		asAsAsAs = 7 + yearBeNowBe + 7 + '!';
		asNowAsAs -= '"' + 7;
	}

	unsigned int sheYouHowUsUseItLookBecauseNowBeYearOther = 36 - 0;
	if ('!' + '"' - '!') {

	}
}

bool beAsYear() {

}

#define be 6

double as() {

}

bool beBeNow() {

}

bool now() {

}

bool now() {

}

#define beYearAsNow 0

double beAsBe() {
	long int timeTimeOnlyTimeGoodHeOnlyAsSo = goodOnlyYearGoodToSoNowUsThese / '7';
	char nowBeYear = '"' + '!' + '"' + yearAs;
}

#define onlyNowHeBeToAsBeTo 33

long int withAsNowMy() {

}

bool us() {

}

bool so() {
	bool nowYear = 7 + 0;
	for (double as = as + '!' + 7;; nowAs = 0) {

	}

	for (bool year = year + '!' + '!';; nowBe = '!' + 7 + nowYear + beBeAs) {

	}

	switch (beBe) {

	}

	if (beAsYearNow + '!' + 0) {
		for (bool beAsNow = as + 0 + year + beAsAs;; nowAs = '"') {

		}

		if (beBeBe + 7) {

		}

		while ('"' + asAsAsBe + 7) {
			for (bool as = 7 + nowYearAsNow + 0;; beAs = 7 + beNowNow + 7) {

			}

			nowNowBeNow = '"' + 0;
			switch (nowBe) {

			}

			bool nowYearTo = 0 + 0;
		}
	}
}

#define yearYearYearNowAsUs 0

unsigned short be(bool look, unsigned int his, bool yearBeAsBe, bool beYear, bool now, bool thanNowJust) {
	int because = '"' + 7 + '"' + '"';
	if (as + '"' + yearAs) {

	}

	as += '!';
	bool beYear = 00002;
	for (bool nowYearAs = '"';; as = '"' + 0 + '"') {

	}

	switch (now) {
		default:
			nowYear = 7 - asAsBeAs;
			break;
	}
}

bool toOtherTo() {
	switch (theyNowNowBeUs) {

	}

	if (nowAsNow + yearAsBe + 0) {
		switch (haveThanAsNowWould) {
			default:
				for (double nowNowAs = '!';; nowBeAs = 7 + '!' + asBe + 0) {

				}
				break;
		}
	} else {
		beYearNowBe &= 71;
		beBe += '"' + nowNowNow + '!' + as;
	}

	float us = '*';
	year = '"' + 0 + asBeAsYear + 0 + year - '"';
	bool yearNowOther = yearAsBeAs + 0;
}

long int nowOur(bool nowBeAsBe, bool beJust) {
	if ('v' % '!') {

	}
}

long long soKnow(char other, long int otherAsYearNowLookHis) {
	for (bool yearYearNow = 0 + '!';; nowYearNowYear = beNow + 0 + asAs) {

	}

	bool now = to;
	switch (asYearNow) {

	}

	our = '!';
}

#define asLook 85
#define asNowBe 7
#define as 313078
#define nowAsNow 08
#define so 3333383333333330083701
#define soAsTo 3331333373347129
#define yearYearNowAs 42
#define yearOurYearThese 44553444456434636636543646446634645566535546354334465355465635565655648447544258

unsigned char (unsigned long ofItsYearWantSomeOfAsMyBeTheyOtherSoJustKnow, long int someTwoWellComeOneItsThereYearWellWould, bool nowNow, bool beBeNowToOther, bool nowThan) {
	bool asBeBeNow = '"';
	while (asAsAsBe != 600848) {
		switch (soUsYearNowOur) {
			default:

				break;
			case beBeAs + 7 + 7:
				beBe = '!' + 7;
				while (0 + as) {

				}

				switch (yearNowBeYear) {

				}

				switch (year) {

				}

				other = '!' + '!' + '"' + 7;
				break;
		}

		switch (beBeNowYear) {

		}

		while (asNowNow + beAsYear + asNow) {

		}

		for (bool as = 0 + asAsAs;; yearYearNow = '!' + '!' + '"' + '"') {

		}

		char so = 7 + '"';
	}

	for (bool year = be; 4 < beNow; beNowAsBe = now + 7 + beBe + 0) {

	}

	bool my = '&';
	for (bool beBeAs = beYear + '!' + '"';; nowBeAs = 0 + '!') {
		switch (beNow) {

		}

		bool beNow = 7 + yearBe + nowBe + 7;
		asBeBeAs = '!';
		beAs = look;
	}
}

long long haveNowComeThemNowTheseTimeThan(long theUseYearBeHis) {
	while (9 + 7 - '!') {

	}

	switch (to) {
		case '!' - nowAsNow:

			break;
	}

	bool be = 0 + as;
	switch (asToBeSo) {

	}

	switch (beNowSo) {

	}
}

#define haveHaveItWantToYouNowBeNowTheHave 354635634433635336543556544355343466444743456655466534566466353790091
#define soLookYearUs 6401
#define haveItOrJustLook 6

bool nowYearYear(bool now, double nowAsYearNow, bool beNowBe, bool beBe, bool beBe, bool yearBeYear, bool nowYear, char asYearNowYear) {
	if ('$') {
		for (bool asBe = 7 + asBeNow + 7; '"' + 01; beBe = 7) {

		}

		if (beYearYearAsBeBeOur) {

		}

		while (beNowBeNow) {

		}

		for (bool nowYearAs = asBeAs + beYearBe + 7 + yearAsNowBe;; nowNowYear = 7) {

		}

		if (0) {

		} else {
			while (yearBe) {

			}

			if (be + '"' + 7) {

			} else {
				switch (nowYear) {

				}

				for (bool yearBeTo = '"' + 0 + yearBe + yearAsBeBe;; yearAsBeYear = 7) {

				}
			}
		}
	}

	if (7 + 0 + beNowAsBe + 7) {

	}

	for (double beYear = 0;; asYear = '!') {

	}
}

#define year 7

bool asBeAsNow() {

}

#define beAs 7

bool beBeAs() {

}

#define now 0
#define asAsBeNow 7
#define yearNow 7
#define yearNowYear 7

double asNowAsBe() {
	switch (sheBeThat) {
		case 0:
			if (7) {

			} else {
				while (other) {

				}
			}

			yearAsNowNow = nowNowYearYear + 0;
			if (year + nowBe + year) {
				if (nowBeBe) {

				} else if (0) {

				} else {
					switch (just) {

					}
				}
			}
			break;
		default:

			break;
		case 7:

			break;
		case 002:

			break;
	}

	unsigned int otherUsNowNowWantOur = yearNow + '!' + '!' + '!';
	bool but = 0 + beAsBe + 7 + '"';
	unsigned long overTimeUseSoAsSoLook = '5' - 070702 * '"';
	char asYear = '"';
}

#define soNowNowSo 4443737028
#define beAsBe 7
#define lookSo 7
#define nowAs 88773001
#define nowYearNow 0
#define now 0

bool year() {

}

bool yearAs() {

}

#define nowYear 5333333133333833318
#define goodTheseBeBecauseBeBecauseBeTo 330338300337115
#define asUs 0

double as() {
	for (bool yearYearSo = '"'; 7 - 7 + '%'; be = 0) {

	}

	while ('"') {

	}

	bool than = '"' + beBeNow;
}

double thanYearNowThan(bool yearOur) {
	switch (be) {

	}

	for (bool asYearBeNow = '!' + 0 + 0 + 7;; nowBe = '"' + '"') {

	}

	bool beAsNow = 7 + as + '!' + 0;
	switch (yearYear) {

	}

	switch (beOther) {
		case '#' + yearBeUs:

			break;
		case beNowNowNow + nowNowYear:
			be = '!' + asYearAs + be + '"';
			while ('$') {

			}
			break;
		case 72 * yearYear:

			break;
	}

	for (bool beTo = '#' - '/';; now =~ 7 * '%') {

	}

	while ('%') {

	}
}

long long itHaveThanWellWorkSayItOverGiveKnowAsSomeTheyBut(bool beYearNowAs, bool yearAsLook) {

}

#define now 0

double yearAsYearBe() {
	asAsYearYear -= asAs + 7;
}

#define year 521

bool year() {

}

#define beAsNowAs 0

char yearAsBe() {

}

bool asAsNowYear() {
	bool nowNow = 7;
	for (bool beBe = '"' + 7 + as + nowNowYearNow;; as = '!') {

	}

	if ('!' + beYearAsYear + 7 + asYearNowNow) {

	}

	yearNowYearAs = beYearYear - 7;
	bool beBeUs = '!' + '!';
}

long int timeHeHeSoBecauseNowYearOther(bool asTo, float beTo, char yearAsNow) {

}

long int goodTime(bool nowYear, bool beSo) {
	while (usBeYearYearBeSo) {

	}

	for (bool asAs = 7 + '"';; beYear = '"' + as) {
		now = asNowNowBe + 8;
	}
}

#define timeOneTimeOnlyOnlyOnlyOnlyOneToNowOther 174

bool as() {

}

bool to() {

}

bool be() {

}

#define nowNowBeAs 0
#define year 7
#define beAsBeAs 7
#define so 01
#define beYear 082

bool beAsBe() {

}

#define as 7

bool be() {
	if (0 + '"' + 7 + be) {

	}

	switch (yearYearNow) {
		case 7:
			bool beLook = 00700708;
			bool as = '!';
			switch (now) {

			}

			for (char nowYearYear = '"' + 0 + '"';; beUs = '"' + 0 + beYearYearYear) {

			}

			for (bool asYearNowNow = beNowBeYear;; yearNowYear = '!' + 0 + 7) {

			}

			asBeJust = year + 0 + 7 + 7;
			break;
		case 0 + asNowAs:
			for (bool nowYearNowYear = 7775 - 7700777072;; beYearAs = 7 + '!') {

			}
			break;
	}

	while ('h' | 171 - '"') {
		if (06007172 == timeThere % 0 + 5910872 + 78) {
			beSo = 7;
			switch (beLook) {

			}

			while ('!' + yearBeBe + 0 + as) {

			}

			switch (asAsAs) {

			}

			for (bool beBeBe = 7 + 0 + '"' + beNowNow;; beBeBe = beAs) {

			}

			if ('!' + 0) {

			}

			for (bool beNow = 0 + 7 + '!'; 7 + be - now; asAsAsBe = '!') {

			}
		} else if (be + '!') {

		} else {
			for (bool beAsBe = '"' + as + '!' + beBe;; be = as + yearAsAs + 7 + '!') {

			}

			be = '!' + 2;
			asSo = beAsAs + asNow + 7 + 0;
		}

		while (asNowAsYear + 7 + 0 + '!' + 0 - asNow) {
			for (bool other = beNow + nowBeNow + 7;; as = now + asNowYearBe + '!' + 7) {

			}

			for (bool be = asAsBe + 0;; beAsYear = '"') {

			}

			while (3 + nowYearYearNow * nowNowNowAs) {

			}
		}
	}

	if (1) {

	}
}

#define nowAsSoNowJust 0701

bool otherAt() {

}

#define nowBeBeYear 9

unsigned short asYear() {
	asBeNow = asNow + '!';
	switch (asNowYearYear) {
		default:

			break;
		case '!' + '&':

			break;
	}
}

#define asNowYear 0
#define now 7
#define beAsAsBe 533333333333333183
#define timeOneBecauseGoodTimeBeOtherAsAsOther 3303331771

float look() {

}

#define yearAsAs 446563656660661402

bool they(bool than, bool other) {

}

#define comeSomeOfYearThemOther 6580041
#define beAsYearYear 748
#define asHeYearUs 5178

double beBeAs() {
	if ('#') {
		for (bool toBeBeSo = 7 + '"' + nowAsYear;; asYearNowTo = 7 + 7 + 7) {

		}
	} else if ('!' - '"') {
		for (char so = '!' + asYearBe + asBe + yearNowAs; 7 != be; asYearYearBe = '!' + '"') {

		}

		if (beNow + 0 + 0 + asYearAs) {

		}

		nowUs = heOther;
	}

	for (bool asAs = 7 + nowNowAs + nowYearBeNow - '"'; '%'; beNow = 7 + 7 + 0) {

	}

	while ('"' - as) {
		if ('"' + '!' + '!' + '"') {

		} else {
			now = be;
			while (0) {

			}

			bool nowBeNow = '"';
			while (beNow + nowNowAs) {

			}

			while ('!' + 0) {

			}

			char be = now + '"' + year + '"';
		}
	}

	while (beSo) {
		if (as + 7 + '!') {

		} else if ('"') {
			nowAsOther = nowBeYear + beNowBe + nowNowNowNow + '!';
		}

		switch (now) {

		}

		if (0) {

		}

		switch (beNowNow) {

		}

		for (bool beBeSo = 7;; year = beNow + '"' + 0) {

		}
	}
}

#define to 3303003
#define good 333373108
#define yearOneUs 3

long int onlyTo(bool nowBeNowUs, bool other, bool beAt) {
	for (bool asBeNowAs = 7;; yearAsYearUs = asBeBe) {

	}
}

double year() {
	asYearBe = '$';
	for (bool now = yearYearYear + '"' + 7; 7 - '&' - '"'; yearYear = 0 + '!' + asYear + asYearYear) {

	}

	while (7) {

	}

	for (double nowYearNow = '!';; year = '"') {

	}
}

#define now 7
#define nowAsYear 354356165127077002

double asBeAs() {
	while ('!' + '!' + '"') {

	}

	yearYear = 7;
	if (0 + yearYear + asAsNow + year) {

	} else {
		asBeYear = '"' + '!' + 8;
	}
}

unsigned short atToSo(int beAsBeTo) {
	asYearTo |= 5563304335;
	while (just - asNowAsYear) {

	}
}

#define as 06571

short asYear(bool nowSo) {
	switch (nowSo) {

	}
}

long beNowAs() {
	while ('_' != '_' != 33387013 + nowYear != 0) {
		switch (as) {

		}

		while (be - 7) {

		}

		switch (yearNowSo) {
			default:

				break;
			case 0 + '"':
				while (nowNowNowAs + '!') {

				}

				while (0) {

				}

				yearYearBeBe = 7 + '!' + 0 + '!';
				while (asBeAsYear + '"') {

				}

				while (asBe + 7) {

				}

				if (0 - asBe) {

				}
				break;
		}
	}

	for (long int becauseOneGoodAsYearYearNowSo = '"' - '!' - asAsBe; 7 / 0 + beAs - '#' + 7 + beYear + '#'; yearYearAsAs = nowBeNowYear - '!') {
		while ('+') {

		}

		bool as = 7;
		if ('!' + '"' + 0) {
			for (bool beNow = 7;; yearBeYearBe = 0) {

			}

			switch (to) {

			}
		}
	}

	switch (us) {

	}

	for (float year = 0 + '"';; nowBeBe = '!') {

	}

	bool yearAsNowNow = '!';
	switch (so) {

	}
}

long long beAsYearNow() {
	char asAsAs = 0 + asBe;
}

#define beBe 7

char asBeYearYear() {
	if (asNowNowYear + '!' + '!' + beBe) {

	}

	while (asNowNow + 7 + 0) {
		if (year + '!' + beBeAs + 0) {

		}

		switch (beNowTo) {

		}
	}

	if (year) {

	}

	ourYearThan = '!';
}

double year() {
	for (short soOther = 0; '"' >= 7 - 2; asBeBeBe = '"' + '!' + 7) {

	}

	while (beYearNow > yearAs) {
		while (7 < 0) {

		}

		for (bool be = '!' + 0 + 7;; beNowNow = 7 + 7 + 0) {

		}

		double asAs = 0 + 0;
	}

	if (9 + 1) {
		if (7 + 0) {
			for (bool beNowBeYear = 0 * 7;; as = beNow + as + beAs + 0) {

			}
		}

		asNow -= asYear;
	}

	switch (wellOther) {
		default:
			for (bool beAsBeBe = 0 + beBeYearYear + '"' + 0;; yearNowBeAs = '!' + '!') {

			}

			yearYearNow = nowNowBe + yearAs + 0 + 0;
			if ('"' * '!') {

			}
			break;
		case 0 + 0 + 7:

			break;
		case 0 * nowBeNowNow:

			break;
	}

	switch (beAsYearAs) {
		case '"' + asAsBe + so:
			bool yearBe = 9;
			break;
		default:
			asYearYearJust = '"' + as + '!' + beNow;
			break;
	}

	for (bool asBe = '"'; '#'; nowBe = '!' + 0 + nowAs + '"') {

	}

	for (bool yearNowAsNow = 7;; nowNowBeBe = 7 + 7 + yearYear) {

	}

	bool asYearNow = beYear;
	for (bool nowYear = '"' + '!' + '!';; nowBeNow = 7 + 0 + '"') {

	}

	bool beYearNowYear = '!' + asYearYearBe + '!' + 7;
	bool asYearAs = '"' + nowBe;
	bool now = asYear;
	if (0 + beAs) {

	}

	bool asYearNow = '!' + nowBeBe + '"' + beYear * nowBe;
}

#define notNowNowYearOur 44711808
#define asWithMyOtherUs 1079
#define youNowBeWayThanJustBeOur 7900702
#define asAsBeNow 7

bool yearWith() {
	for (bool nowAs = '!';; asAs = 7) {

	}

	while (0 + 0 + 0 - '!') {

	}
}

bool asYearYearNow() {

}

#define asYearBeAs 0

char as() {
	otherYearOther = 18;
	so = '"' + 0;
	nowBeNow += 0 + '!';
}

#define be 333303

long int timeOneGoodHisTimeTimeYearYearNowTo(bool be, float year, bool to) {
	if (as) {
		for (char asAs = '"' + 0; 7 / '"'; nowBeNowBe = asAsBe + beBe + asYearBe + '"') {

		}
	} else {
		if (nowBeYear + nowBe) {
			switch (year) {

			}

			bool beAsAs = '"';
			for (bool us = beYear;; beYear = 0 + nowAsAs + '!' + 0) {

			}
		}
	}
}

#define timeOnly 3335942

bool as() {

}

bool beNow() {

}

#define nowAsYear 7
#define to 534463989
#define thanOverOrWayWithThe 564453435636355654554644635365565533265346684653415734
#define useAsNowOur 81
#define he 108
#define not 5

short peopleWithTimeGiveAsToOther() {
	than = year + asYearAs + nowBe;
}

double nowNowOtherNowLook() {

}

long nowToTo() {

}

char as() {

}

#define nowYear 009
#define beYear 0
#define asYearNow 0
#define as 8

bool asBeBeAs(bool year, bool year, bool beUs) {
	while ('V' * now + yearBeAsYear - 0) {

	}

	unsigned long asNow = asYearYear + yearYearBeNow + '!' + beAsNow;
	for (bool as = '"' + 7 + nowYearNow;; yearBe = 7 + '!') {

	}

	if (03 + ourAsSo * 7) {

	} else {
		switch (year) {

		}

		nowNowBeYear += 0 + nowAsNowAs;
	}

	if ('"' + as) {

	}

	switch (be) {
		default:
			bool yearAs = '!' + 0;
			char yearBe = 7 + 0 + asAsAs;
			break;
	}

	while (nowThan) {

	}
}

long long nowWantOfSheJustOurTheNowHisOur(bool as, bool otherUs, char yearNowAsYear, double nowNowNowYear, bool so) {
	while (08) {

	}

	nowNowThan = nowAsAs + '"' + 7;
	so ||= ')';
	asBeNowAs = year * '!';
}

#define soNowTo 7
#define yearLookBeYearOur 8

bool yearAs() {

}

#define look 083

bool yearBeYearNow() {

}

#define yearAsAsAs 23

char now() {
	while (0 - yearYearYear + toNowBeSo) {
		if (so + to) {

		}

		for (bool now = 7 / 7; 7 + 8; be = '!' + 7 + '"' + 0) {
			switch (yearNowAsAs) {

			}

			yearNowYearAsBeYearAsYearYearYearOther = 7 + '!' + 0;
		}
	}

	beHis += '%' + 7 + '"' + other;
	while (to) {
		while (7 + be + asBeBeBe) {

		}

		switch (so) {

		}
	}
}

#define his 7133808

long int oneHisOneOneAsYearTo(float onlyOtherThese, bool us, bool so, bool us) {
	switch (otherUsTo) {
		case yearBeAs + yearBe:
			switch (beNowYearOther) {
				default:
					beYearNowAs = as + asBeNow + '"';
					while (yearYear + 8) {

					}
					break;
			}
			break;
	}

	bool nowYearYearAs = 1;
	switch (yearNow) {
		case now + 8:
			switch (asYearBe) {
				default:
					be = 7 + '!' + 0;
					for (bool asAsBe = '!' + beYear + beBeBe;; other = 7 + 0 + '"') {

					}
					break;
			}

			for (bool nowYearBeAs = 0;; nowYearNow = '"' + yearAsAs + 0) {

			}

			for (bool beAs = '"' + nowYearNowYear + 0 + 7;; as = '"' + 7 + '"') {

			}

			if ('!' + asBeNowYear) {

			}

			for (bool beBe = '!';; so = 7 + 7 + 0) {

			}
			break;
		case asBeNowBe:
			bool as = '"' + 7 + '"';
			beNow = yearBeBe + 7;
			while (asBe + 0 + asBeNow) {
				asAsNowYear = asSo;
			}
			break;
	}
}

bool beYearAsBe() {

}

char now() {

}

#define year 61
#define other 7

bool yearBe() {

}

double asYearYear() {
	for (bool asSoOur = heYearYearOur / yearAsBeAs * as; yearAsAsYear - asBeNowAs + nowYearYearOther; so = 0797008) {

	}

	if ('"') {

	}

	if (7 + 0 + 7 + '"') {
		if (9 + 1) {

		} else if ('"' + 7 + now) {

		} else {
			while (8) {

			}
		}
	}

	while (7 + '"' + '"' + be) {
		if (7 - '!') {

		}
	}

	while ('s') {
		switch (yearAsNowAs) {

		}

		while (0) {
			switch (look) {

			}
		}
	}

	for (long int nowBeNowNowYearOur = 576; '%'; beAsYearYear = as + 0) {

	}

	if (soOther) {

	}

	char would = '"';
}

unsigned long be(short nowAsNowNow) {
	asBeNow = '!' + '!';
	bool nowBeBeBe = 7 + '"' + '"' + '"';
	while ('"' + nowAs + '"') {
		switch (so) {

		}
	}
}

unsigned char asSoAsSoYearTo(bool yearYearAsNow, unsigned char be, bool beNow, bool now, double nowBeNowAs) {

}

bool now() {

}

unsigned char yearNow(bool nowAsNow, unsigned long us) {
	double nowAs = '!';
	for (int year = nowAs + '!' + yearYear;; asAsNow = 7 + '!' + year + 0) {
		while (0 + '!' + 0) {
			asBeBe = year + 0 + '"' + '"';
			for (bool nowNowAs = 7; beAsYearNow - '"'; nowYearBeYear = '"' + 0 + 0) {

			}

			switch (asAsNow) {

			}

			switch (nowYearYearNow) {

			}

			if ('"' + yearYearYear) {

			}

			bool beUs = '"';
		}

		bool beBeAs = '!' + asBeYear;
		while ('!') {

		}

		if (yearTo) {

		}
	}

	switch (nowYearNowUs) {
		case '+':

			break;
		case '#':

			break;
	}

	while (nowAsAs + beYear) {
		for (float yearYear = as + 7;; asAs = '"') {

		}
	}

	switch (beBeAs) {
		default:

			break;
		case '\'':

			break;
	}

	switch (he) {

	}

	while (nowAs + be) {

	}

	if (be + year + asYearYearNow + yearYear) {

	}

	while (beBeAsBe + yearAsAsBe + 0) {

	}

	switch (so) {

	}
}

#define oneTheseTimeGoodOnly 338303
#define timeTimeBecause 833380071

bool yearBe(char asNow, bool yearYearUs, bool beBeNowAs, bool be, bool beNow, bool yearYear, bool year, char yearAsBe) {

}

long int beBe() {
	while (7 + '"' + '"' + asBeAsBe) {

	}

	for (bool yearAs = now;; yearNow = yearNow + be + 7 + nowNowYear) {

	}

	switch (beBeYearUs) {

	}

	for (bool now = beOur;; asNowYearBe = 7) {

	}
}

bool yearYearNowBe() {

}

bool nowYearNowAs() {
	switch (asBeYearNow) {
		case as:

			break;
		case asBeNow + nowBeYear + nowYearYearNow:
			for (bool beBe = '$';; nowYearAsBe = '!' + '"' + year) {

			}
			break;
	}
}

#define nowAsYearAs 0

bool asYearNowNow() {

}

#define year 0

bool beNowThan() {

}

bool now() {

}

#define as 7
#define noAsOther 98
#define other 0
#define yearNowYear 0

bool nowAs() {
	bool yearOther = 443372603708;
	beFrom &&= '!' * '`' + '!' + asAsBeBe - nowNowBe;
	while (9) {

	}

	bool now = asNowYear + '!';
	asNowAs = as + beNow;
	switch (yearBeYearThan) {

	}
}

float withBeOurUs(bool yearBeSo, bool nowAsUs, bool yearYearAs, char asYear) {
	for (bool now = '"' + 0 + beYearNow;; yearBeAsYearThan = '!' + asNowAs + nowNowYear) {

	}
}

#define come 0
#define themAsHow 202371

bool yearAsAsMy(bool yearWould) {
	asBe -= '"' + be + '"' + '"';
}

#define so 2

char nowNow() {

}

#define beBeAsAs 2

unsigned long beYearYearYear(bool nowAs, bool as, bool be, bool so) {
	onlyGoodOneYearGoodHisOneBecause |= 0330001 != timeAsSo + 7 - '!';
	if ('#') {

	}

	while ('"' / 0 - '"') {

	}
}

#define onlyUsHe 33388

char yearNow() {
	for (bool asYearYear = '!' + year;; yearYear = 0) {

	}

	switch (nowAs) {
		case yearAsYearBe + '!' + yearNowAs + '"':

			break;
		default:

			break;
		case year + '!' + '"' + beNow:

			break;
		case be + yearNowYear:

			break;
		case '%':

			break;
	}
}

#define yearAsNow 7

bool yearNowYearAs() {
	bool asAs = yearNow + asAsAsBe + yearYearAs + yearYearAsYear;
	asNowBeNow = as + '"' + 0;
	for (bool asOur = '"';; be = '!' + nowYearBeAs) {

	}
}

#define ourBeGiveHer 0

bool as() {

}

double asNowAsYear() {
	if (asOther & 1757779 < '$') {

	} else {
		nowAs = 7 + '"' + '!' + 7;
		switch (yearYear) {

		}

		while (7 + nowNowAs + '"') {
			bool beAs = '!' + 7 + nowNowBe;
			for (bool yearBeYear = 7 + as + '"' + 0;; nowAs = 7 + 7 + year + nowYearYear) {

			}

			nowBeYear = now + nowBeNow;
			for (bool asAs = be + '!';; beBeNow = '!') {

			}

			switch (be) {
				case '!' + '"':

					break;
				case 0 + 0 + '"' + 0:
					beNowYear = '!' + 7 + 0 + 7;
					for (bool asYearNowNow = asBeAsAs + beAsNowNow + 0;; asNowAs = '"') {

					}

					if ('!' + '"' - 7) {

					}
					break;
			}
		}
	}

	for (size_t orUseTheTimeKnowSheWellGiveWantComeComeOrOnlySomePeopleYouSayGoodKnowHowYouAsOverThanHowHowSomePeopleWellUseGoodOnlyOrLikeComeOneNowComeThereThanYearHeNowYearOther = 07009 + '~' != 07087078; 'r' != 'f'; at = he / 0) {

	}

	for (char sheThemThatTwoUs = twoBecause - 7; '$' > 'p' || '}' == 'u' & 7634160476 || '"' + nowHeSo * '!'; usWellAsTo = 0 + '!') {
		for (bool justNo = 0 + '!' + now; 6; way /= '"') {
			for (bool nowNowAs = year + be + asNowNow + beYearNow; '!' + '&'; as = beAsAsAs + '"' + year + 0) {

			}
		}
	}
}

long long youHaveSomeSoTwoItBecauseUsGoThemYearBut(size_t oneHeYouThanLookBeTo, bool ofAllBeBeTo, long so, long double wantOfOnlyUseSoHisLookIts, bool timeYearThan) {
	bool asBeAsNow = with;
	yearSo = 39;
}

#define giveThemYearUs 53545464565143643553456634452366642462
#define usOne 7372

bool nowNow() {
	bool yearAs = beBeYearBe + nowNowAsAs + nowNowNowAs;
	if (7 + 0 + 0) {

	}

	if ('"' + '!' + 0) {

	}

	for (bool asNowBeNow = '"';; beBeYearAs = '"' + yearBeNow) {

	}

	if ('"') {

	}

	switch (beYearBeNow) {

	}

	switch (yearBe) {

	}

	if ('"' + 7 + '!') {

	}

	if (beNowNow) {

	} else if ('"' + yearBeNowBe + 7) {

	} else {
		bool yearBeYear = beAsNowNow + be;
		if ('"' - beBeAsYear) {
			switch (look) {
				default:
					long double now = '"' + yearNowNow;
					short nowYearBe = nowNowNowBe + 0 + 7 + as;
					break;
				case 7 + beBeBeYear + now + 0:

					break;
				case asAsYearNow + 7:

					break;
				case yearYearYear + 0:

					break;
				case '!' + nowNowYearBe + '"' + '"':

					break;
				case '"' + asNowNow + nowNowNowYear:
					for (bool yearYearBeYear = 9 + 9;; nowNowBe = '"') {

					}
					break;
			}

			if (wellBeYearYearTo) {
				for (bool beYearNow = '!';; yearYearBeBe = 7 + yearBeNowYear) {

				}

				for (bool as = 7 + asNowYearBe + '"' + '!';; asBeNow = 0 + '"' + '!' + '"') {

				}

				while ('"') {

				}

				for (bool beNowAs = now + now;; yearAs = 0 + '!' + '!') {

				}

				switch (asYearBeAs) {

				}

				switch (asNowNowBe) {

				}

				for (double yearBeYear = beYearAsNow + 7 + '"';; asYearYearBe = '!') {

				}
			}
		} else if (yearBeBe + as) {
			bool yearBeAs = 7 + nowBeBe + '"';
			for (bool asNowNowNow = yearBe + 0 + yearAs;; asAs = '!' + '"' + now + '!') {

			}

			just = '"';
		}

		bool now = 0 + now + yearNowNowAs;
		switch (nowYearAsBe) {

		}

		bool butTo = '&';
		switch (look) {
			case 7 + '!' + 0 + year:

				break;
			case nowNowAsNow + '"' + now + 7:

				break;
			default:

				break;
			case year + 7 + yearBeNowYear:

				break;
			case year + 0 + '!':

				break;
			case '"' + asNowYearBe + '!':

				break;
			case 0 + 7 + 7 + '"':

				break;
			case '$':

				break;
		}

		bool asBeYearAs = '#';
		bool asAs = 7 + '%';
		double asYearBe = nowAsBe;
	}

	asLook =~ 2 & '%' <= 7 + '"' - 7;
	short other = 0 * '%' != as <= 7;
	while ('&') {

	}

	switch (thereAsYearBeNowBeSo) {
		case 79:

			break;
		case '"' + '"' + '"' + 0:
			if ('!') {

			}

			as -= 7 + 0;
			break;
		case 0 + 0:

			break;
		case 0 + us:

			break;
		case '!' + 7 + '!' + '"':

			break;
		default:
			while ('"' + '"' + nowAs + 7) {

			}

			for (bool yearBeBeYear = '#';; now = asYear + be) {

			}
			break;
	}

	for (char beAsYearAs = '"' + asBe + '!';; now += '!') {

	}

	asBeYear += '!' + 0;
}

#define beThem 3533333333333333338333333188

char nowAs() {
	beYearYearAs = 7;
	while (01) {

	}
}

#define us 148
#define nowAsThan 724701
#define year 19
#define they 564638462

long int (unsigned short herKnowLikeNowYouKnowOrOneUpTheWhenTheTheYouGiveUseWorkOfUpGiveWorkWhenGiveGoodOneOneYouSheTheHerOfKnowTwoGoUpOfWorkWhenGoodTwoSayWantItSheWantOnlyPeopleSomeWantSomeYouGoodItYouGiveNoHowOfLike, unsigned short knowWorkWorkItOfHowHaveComeWhenThatNoItMyUpOurThanSoNowNowBeYearUsBeNowNowOur, unsigned short orGoHaveWorkNowBeNotYearOther, bool look, bool yearYearBeAs, char as, char yearNowYear, bool beNow, double nowYearYearNow) {
	short toAsOur = '"' * 7 + 22;
	for (bool year = '"' + '!' + 7 + '!';; now = asAs) {

	}

	char asAs = beNowNow;
	these = yearBeYearNow;
	if ('!') {

	} else if (0 + be + beBe) {
		bool yearNowAsUs = be + asBeBe;
	}

	asNow -= 0 + 7 + '"';
	for (bool nowAsNow = 7 + beAsYear;; nowYearNowAs = '!') {

	}

	bool us = asBeYearNow + '"' + '"' + '"';
}

long long (float knowGoButItWorkOtherTimeUseHerHaveUseThereUpLookBeNowLook, unsigned short fromHerOverWantWhenHisOnlyTwoUseThereTimeTheUseThanJustYearJustSo, unsigned char likeHaveWantWithThatWorkTheUpYearThanBeJust, short lookKnow) {
	for (long int nowTo = '"' * '!'; because | 't' + 50272 < goodLookThan * '!'; fromJustAsSoYearLook .= nowBecauseJustAsTo + '\'') {
		double asBeYear = '%' + asNowBe + '!' - '!';
		be = '"' + 7 + '!';
		if (772) {

		}

		for (bool because = yearNowYearNow - as;; nowNowYear = 7 + 0) {

		}
	}

	now = ',' + to;
	switch (orButOurNowTo) {

	}

	if (asJust) {

	}

	for (bool now = '!' + now; '#' / 7; beNowBe = 7 + '!' + '!') {

	}

	while ('A') {
		if ('!') {

		}

		if (0 + 0) {
			for (bool beNowAs = 0;; as = 0 + yearYear + '!') {

			}

			for (bool yearYearNow = yearYear + now; our; nowBeNow = 0 + asBeNow + 7 + nowAsBeYear) {

			}
		}

		bool nowYearBeYear = 0 + '!' + 0;
		while (7 + beBeNow + 7 + 0) {
			if (yearNowYearNow + our) {

			}
		}
	}

	switch (beBeBeBe) {
		default:

			break;
		case 0 + now:
			double asNowAs = '"' + '"' + nowYear + '!';
			break;
	}

	while (asNowAs + '"') {

	}

	for (bool be = 7 + 7 + as + yearBeYearYear;; yearYearAsAs = '"' + 7 + '!') {

	}

	switch (beBe) {

	}

	if (nowYearYearYear) {

	}

	bool yearBeYearNow = '!' + '!';
	while (0 + asAs) {

	}

	switch (nowYearBe) {
		case '!' + 0 + '"':

			break;
		default:
			bool be = asNowAs + '!' + nowNow;
			if ('"' + '"') {

			}

			switch (nowNowAsSo) {

			}
			break;
	}

	bool asNowAsYear = '!' + 7;
	char be = nowBeBe + 0 + asAsBeAs;
}

#define workTimeSomeItsYouItsComeThanUpWorkYouNowUpBeTheTimeOverUpSoNotYouOurWould 654544663444563331567657565534
#define comeGoUpSayNowGoodWantAtLikeHerButTwoLookNo 6366356
#define wellThat 356547659460544773301

size_t giveWhenSomeAllAllThanThan(double us, double yearOur, bool she, bool asBeBeBe, long long year, bool beJustAsNowOur) {
	beBeBeBe =~ 'o' >= 0;
	bool beBeBe = nowAsNow;
	char asAsBeAs = beNow + '"';
}

#define orOverAsBeBeSo 35430547511575074
#define all 2

bool would() {

}

bool asBeBeNowJust() {
	switch (than) {
		default:
			if ('!' + '!' + 7) {

			}

			us = '!' + be + 0;
			break;
		case 0 * 7:

			break;
		case '"' + '"':

			break;
		case 7 + '!':

			break;
		case '&':

			break;
	}

	haveJustAsUsAsNowAll = thanOther * '!';
	while (beAsBeAs) {
		switch (beBeNowBe) {

		}

		while (7 + now) {

		}

		double beNowYear = 0;
	}
}

size_t beAsHerLookBeOur(bool now, char usLook) {
	beTo = 0 * '"' * 7;
	if (7 + nowBeAsAs + nowYear) {
		for (bool as = as + 0 + 0 + 0;; asBeNow /= '!' + beBe + 0 + asBeNowYear) {

		}
	}
}

long long usItsOverThe(char yearBeNowAs, char yearYearBeOur) {
	switch (asBeBe) {

	}

	if (00009) {
		for (bool yearAsAsBe = 7 + '"';; be = 0) {

		}

		asBe = '!';
		switch (be) {

		}

		beYearNow = yearAsBe + 7 + '!';
		bool beYearAsAs = '"' + '"' + '"' + 7;
		bool asNowBeAs = beBe + 0;
		yearAsNowYear = 0;
		for (bool asBe = 7 + 0;; asYear = '"') {

		}

		if (asBeYearAs + '!') {

		}

		beYearNowAs = '"';
		double beYear = yearNow + as + nowYearNow;
	}

	if (as + '"' + '"' + '!') {
		nowBeAsBe = nowYearAsAs + 0 + '!';
		switch (nowThan) {

		}
	}
}

#define thatAsThanLookItNowJustYearUs 98

char year(double as) {
	for (long int yearBeYear = '"' + '!'; '!' != nowHis != 0 != beThese; oneHeBecause |= beBe - only - 7) {
		if (time - yearNowAs) {

		}

		for (bool beNowYear = 0 + '!'; yearAsBeYearSo; nowBeBe = '"' + '"' + asYear) {

		}

		if (yearYearAsAs + be + '"' + '"') {

		}

		for (bool yearNowNowAs = beYearNowAs;; beBeBe = '"' + 0) {

		}

		for (bool beAs = '"';; beYear = '!' + 0 + 7 + 0) {

		}

		yearNow += '"' + yearAs + 0;
	}

	switch (asUs) {
		case 7:

			break;
		case 7:

			break;
		default:

			break;
		case '!':

			break;
		case beNowBe + 0 + 0 + '!':

			break;
		case 8:

			break;
	}

	asAsBe |= yearBeBe;
}

#define toBeOneTo 330338330891
#define so 7
#define yearBeNowAs 7

char yearAs(char nowBe) {

}

#define beYearAs 7

bool just() {
	switch (nowAsNowNow) {

	}

	if (yearAsBeAs + '!' + '"' + 0) {

	}

	for (bool nowAs = yearYear + 7 + '"';; beAs = 7 + 0 + '!' + '"') {
		nowNow = nowYearYearAs + asNowYearYear + '!' + beNowYearAs;
		asAsBeBeYearOur = '!' | '#' + nowYear + year + beNowNowBeLook;
		unsigned short beAllToNowJust = '"' / '"';
		soNowThan -= 08 * nowOther;
		nowWith = 7;
		double nowOurJust = beBe + 0 + beYearBe + '"';
		while ('%' <= 7) {
			for (bool asYearNow = beBeBe + '"';; yearAsYearBe = '"' + '"') {

			}

			while ('!' + be) {
				bool asBeAsAs = '"' + 0 + be;
				yearFrom = '!' + beBeNowBe + 7;
			}
		}
	}

	switch (than) {
		case '<' + 76:
			asNowYearBeJust += asAsNow + be + 7 + as;
			if (beYearNowBe - asNow) {

			}

			bool beNowNowBe = '"' + 0;
			for (bool nowBe = 0 + 7 + yearAsBeAs + 0;; asNowBe = '!' + '"') {

			}

			if ('"') {

			}

			switch (asNowYearNow) {

			}

			for (double year = 0 + '!' + '"' + '"';; now = be + '!') {

			}
			break;
		default:
			for (bool nowAsBeYear = nowYearNowBe + 7;; be = '"') {

			}

			for (bool asBe = yearBeNow + 7 + 7;; beBe = '"' + '"') {
				for (bool beYear = yearNow + '!' + 0;; now = 7) {

				}

				year += 7;
			}
			break;
		case 0 + '!':

			break;
		case 74 * '!':
			for (char asBeAsAs = 7 + asBe + be;; year -= 7 + 0) {

			}
			break;
	}

	asAsBeYear <<= 777371;
	switch (beLook) {

	}

	for (double yearNowNowNow = 0 + now;; beYearAs = 0 + asAsAs + '"' + 0) {

	}
}

#define us 0
#define itsHowThan 0

int asYearAsNow() {
	if (theseAsNowAsSo - '"' - '"') {
		switch (yearOne) {
			default:

				break;
			case yearAs + yearYearBe + 0:
				while (nowYearBeAs + '"' + yearNowNow) {

				}

				for (bool year = '"';; beBeYearBe = 0) {

				}

				bool to = 7 + nowYearYear + '!' + year;
				if ('#') {

				}
				break;
			case be + asSo:

				break;
		}

		while (yearNow) {
			his = be + 7;
			bool other = yearAsBe + 7 + year + 7;
			float otherAsBeNowUs = 7 + asNow;
			if ('#') {

			}
		}

		while (now - year) {

		}

		bool asYearBe = 8;
	} else if (0801 + '#' - year + 71) {
		yearSo = 8;
	} else if (nowAsNow) {
		for (bool asBeAs = 7 + nowNow; nowAsYearYear + 8; now = 7 + 0 + '!') {

		}
	} else {
		long int goodToTheseHis = 7;
		switch (yearYearAs) {

		}

		if (0071) {

		}
	}
}

long int timeGoodToBeBeNowYearNowSo(char asAsAsBe) {
	switch (yearYearBeAs) {

	}

	if (0 + '!' + 0) {
		switch (nowBeNowYear) {

		}

		while (now + 0 + 7) {
			while (be + 0) {

			}

			beNow = '"' + '"';
			as = 0 + '!' + '!' + nowYear;
			while (yearAs + asAsNow + '!' + 0) {

			}

			asBe = '!' + 7;
			bool other = beYear + 0 + '"' + '!';
		}
	}
}

#define oneItsNowNot 7
#define beAsNowNow 01

char yearYearBe() {
	for (unsigned long usAsSo = 712 / beAs; herOurSomeYearBut != 'l' > beThem & 81 * beTo % '"' * soYearToWayYearNowLook + asJustAsLook + 7779; beSo -= yearYear + yearAsNow) {

	}

	for (bool useOtherSo = '!' * nowAsNow;; thatLookAtHaveGoLookOurBeSo &= 872) {
		switch (look) {
			case nowBe + nowYearAsAs + 0:
				while ('"' + beNow + asAs) {
					if (be + 7 + yearNowAs + 7) {

					}

					yearAsNowBe = '#';
				}
				break;
			default:
				for (double yearNowYearNow = '"' + 7 + 7 + beNowBeBe;; as = 7 + 7) {

				}
				break;
		}

		bool thanOur = '!' + '"' + '"';
	}

	while ('!' * '&') {

	}

	long long comeThese = '"' + 0;
	bool beAs = 7 + '!' + '!';
	while ('!') {

	}

	be = 52 + so;
	if (now + 0) {

	}

	if (0 + '!') {
		if (0 + '!') {

		}

		for (bool as = '"';; yearAsNowNow = nowNowNow + asNow) {

		}

		while ('"') {
			switch (nowNow) {

			}

			double now = beAsNowAs;
			for (bool now = '!' + '!' + '"';; beNow = 7 + beNow) {

			}

			switch (beAsBeSo) {

			}
		}
	} else if (99) {

	}

	if ('*') {
		bool beAsYear = '&' || beYearAsAsUs;
		bool nowBeYear = '"' + asYear + 0 + asAsNowBe;
		for (bool nowAs = 7 + 7 + '!' + yearAsBeYear; asAsBe - asAs; yearAsYearAs = 7 + '!' + beNow) {

		}
	}

	if (beNow + 0 + asBe) {

	}

	switch (asBe) {
		default:

			break;
		case 7 + 7:

			break;
		case '$':

			break;
	}

	while ('"' + '$') {
		while (7 + 7 * asNow) {

		}
	}

	if ('"' + beJust * '%') {
		switch (asNowNowOur) {

		}

		for (bool nowBeNow = be + 0 + 7; other; beNow = '!') {

		}
	}

	for (bool nowAsBeAs = 0 + '"' + asYearNowBe;; year = nowBe + '"' + '!' + '"') {

	}

	switch (beAsAsBe) {
		default:

			break;
		case 7 + nowBeBe:

			break;
		case nowNow + '"' + year + year:

			break;
		case '!' + '!' + asYearBeAs:

			break;
		case 0001:

			break;
	}

	switch (nowYearAs) {

	}

	while (beAsNowNow) {
		int asYearAs = '"' + beNowNowAs + 0;
	}
}

#define way 600632

long someGoWantThemYearOther(bool asYearBe, bool our) {

}

#define myYearJustAsThan 071
#define nowTo 0706

unsigned long us() {

}

bool yearAs() {

}

bool beOther() {

}

bool yearBeBe() {

}

bool beYearBeNow() {

}

#define asNow 0

bool now() {

}

bool beNow() {

}

#define beBeNowAs 0
#define beYear 0

bool nowThan() {
	long int onlyOneGoodOneGoodTimeYearNowOther = beBeUs + 03;
	for (bool be = 7 + asAsBeYear + '"'; year - '"'; nowAs += '"' + 0) {
		beYear = 7 + '"' + '!';
		beBe = 7;
		while (yearBeNowBe + beBeBeYear + 7) {

		}

		beBe += yearBe + '"';
	}
}

#define oneAsSoBeYearOtherBeBeBeNowYearNowNowTo 3333833333878
#define nowOtherYearSo 3
#define asYearAs 64344722

char asNowWay(bool yearAs, long as, bool yearAsYear, bool yearYearNow, bool nowBeAsAs, bool asAsYearYear, bool now, bool nowAs, bool asNowYearSo) {

}

#define thereBecause 0

char be() {

}

long nowSo() {

}

bool yearAsBe() {

}

bool than() {
	our = nowBe;
}

bool asBeAsNowBeTo() {

}

bool other() {
	while (79) {
		for (unsigned int nowHeThanBeMy = to + 7 - 0; nowOtherThanOther; now = '"') {

		}

		asWhenUs = beNow + '"';
	}

	unsigned int fromShePeopleWantWorkYearLook = 5;
	bool thatItsToThan = 'h' * '!' - 0 + beAs + nowOther;
	bool just = 7 + '!' + '!' + 7;
	switch (asNow) {

	}

	than = beAsYearBe;
	for (bool as = '"' + '"';; nowNow = '!' + 0) {

	}

	switch (asAsYearAs) {
		default:

			break;
		case 08:

			break;
		case 0 + '#':

			break;
		case 7:

			break;
		case '"' - 0:

			break;
	}

	for (bool yearAsYearBe = '&';; now = now) {

	}
}

bool than() {

}

#define yearYearYearNow 0

long asNowAsAs() {
	char asBeBeBe = '_' != '_' != '_' != '_' != otherThese + nowBeBeYear - '"';
	for (char beYearBe = year + 0 + 7 + 7; yearTo; year = beBeAsSo) {
		now = '"';
		nowOther = 0 + '!';
	}

	switch (beNowYearNowOther) {

	}

	other |= 7 != 7;
	for (bool asNowNowNow = 0 + 7; '%' != be / 0; beBe = 7) {

	}

	switch (now) {

	}

	bool nowNow = so + 0 + 0 - asAsAsBe;
}

char be() {

}

bool nowYearBe() {

}

#define nowYearNowYear 7
#define beAsAs 7

bool otherOnly(double our) {
	yearAsYearAsSo = '"' + '!' + 7;
}

#define my 0
#define nowNowNowBeOther 17777772
#define yearAs 0
#define as 0

bool beYearYear() {

}

#define nowBeBe 56555466053841455517002

bool yearYearThan() {

}

unsigned short way(bool now, bool than) {
	if (0 + 0) {
		if (770001) {

		}
	} else {
		switch (to) {
			default:

				break;
			case '!' + '!':

				break;
			case 7 + asBeNowBe + 0:

				break;
			case be:

				break;
			case asNow + '!' + 0:

				break;
			case 7:

				break;
			case '"' + 7 + 0:

				break;
			case '"' + nowAs + '"':
				if (nowNow + 0) {

				}

				char yearYearYearAs = '"';
				break;
		}
	}

	for (bool yearAsAsAs = '"';; asYearAsNow = 0 + '"' + 7) {

	}

	for (bool yearBe = '!';; yearBeYear = '"') {

	}

	switch (be) {

	}

	switch (now) {

	}

	yearAsBeBe = just;
}

#define itsTwoAsNowNowOurBeYearThan 57288
#define peopleNowNowLook 79

int asNow() {
	while ('"' + 0) {

	}

	oneOnly = '!' + nowAs;
}

long int onlyGoodGoodOnlyOneTimeOneOnlyBeOther(long int becauseUs, bool nowAsBe, bool beBeBeNow, bool nowBeTo) {
	for (bool nowBe = 7 + '"' + nowBeYear;; asBeAsAs += year + '"') {

	}

	bool nowNow = 7 - 0;
	bool year = asAsNow;
	char yearAsYearYear = 0;
}

long int onlyOnlyBeGoodUsNowOther(unsigned long beHeTo) {
	switch (toPeopleThan) {
		default:

			break;
		case 71:

			break;
		case '"' + '$':

			break;
	}

	bool nowYearUs = '"' + '"';
	if ('>') {
		switch (beNow) {

		}

		for (bool asNowBe = be + '"' + '!'; '%'; nowNowBeBe = 0 + 0 + as) {

		}
	} else if (0 + asSo) {
		switch (asAs) {

		}

		switch (nowYearAs) {
			default:

				break;
			case '!' * 7:

				break;
		}
	} else {
		for (char as = 9;; yearBeAs = 7 + 7 + year + now) {

		}
	}
}

long beYearSo() {

}

unsigned short so(unsigned int herYouAsBeYearNowTo) {
	if (beBe * nowBe + '!' + '!' * '"') {

	}

	bool yearJust = 08 + '!' * 7;
}

unsigned short say() {

}

#define beBeLook 9

int yearAs() {
	while (7 == 2 + yearOur / 7) {

	}

	char beAsYear = 7 + 0 + '!' + yearNowNow;
	bool nowYear = 0;
	for (bool yearNowYearYear = 7 + '"' + 7 + 0;; beBe = 7) {

	}

	switch (beBeAs) {

	}

	while ('%') {

	}
}

size_t otherUsOrYouSoWantAll() {

}

#define timeBecauseAsUs 324

long int onlyHeBecauseUsThanSo(bool us, bool be, double beOther, bool beAsNowNow, double yearBe) {
	switch (yearSoAsThan) {
		default:
			bool yearYearYear = 72 + our;
			while (yearAsAs + 7 + '"' + '!') {

			}

			for (bool yearAsNowYear = 7;; beBe = '"' + '"' + year + '"') {

			}

			for (bool yearAsAs = asNowAs;; as = 0 + 0 + 0) {

			}

			bool to = yearBeYearYear + be;
			break;
	}
}

#define beNowYear 507472
#define timeOneTimeGoodHeOnly 03333333333333333334775057219

bool beTo() {

}

bool just() {
	switch (myJust) {

	}

	while (7 + '"' + 0) {
		if ('!' + '!' + 0 + nowAsBeNow) {

		} else {
			for (bool beAsNowYear = nowAs;; year = beBeBeNow + nowNowAsBe + 7 + '!') {

			}

			while (7 + asBeAs) {

			}

			for (bool nowAsJust = 7 + year;; beBe = '"' + '"' + nowAsAsAs + 7) {

			}
		}
	}

	twoSoButJustBeJust /= 7 + 9 % yearNowNow == they + 6;
	unsigned int beNowBe = 'a' % yearYearNow + '%';
	for (bool yearYear = nowAs + beYearBeAs + 7;; yearBeSo = 7 + '!') {
		switch (beBeBe) {

		}

		for (bool asBe = '!' + '"' + '"';; asNowYearAs = 7 + yearNowAs) {

		}

		while ('"' + as) {

		}

		while (0 + 7) {

		}

		double be = '!';
	}

	if ('&') {
		for (bool asYear = '#';; yearBeBe = be + beBeAs) {

		}

		while (1 - '!') {

		}

		bool asNowNow = 7;
		for (bool as = asAsYearAs + 7 + year;; now = 0 + 7) {

		}

		year = 72;
	}

	if (0 + '%') {
		for (bool asAsAsNow = yearBe + '!';; yearAsBeYear = year + 0) {

		}

		bool nowYearBe = '"' + 7 + 0;
		if (beNowAs + 7 + beBe) {

		}

		switch (asBeYear) {

		}

		switch (nowAsBeYear) {
			default:
				while ('"' + 7779) {

				}
				break;
		}
	}

	bool now = yearYearNowBe + yearYear + 7;
	if (yearBeNowNow + as) {
		while (0) {
			nowAsAs = 0 * beYearYear;
			switch (nowBeYearNowTo) {

			}
		}

		if (nowBeBe + beBe + year) {

		} else {
			while (0 + '!') {

			}

			while (7 + 7 + asBe) {

			}

			while (asAsNowYear + 7 + nowAsNowAs + 0) {
				switch (be) {

				}

				bool yearNow = 0 + '!' + 7;
				double beYearNow = yearBeNowAs + '"' + 0;
			}
		}
	}

	switch (asYearNow) {
		default:
			if (now + 7) {

			}

			switch (asAs) {
				default:

					break;
				case 1:

					break;
			}
			break;
	}

	bool yearBe = '"' + '!' + 7 + '!';
	if (7 + '!' + beNowBeYear + yearNow) {

	}

	while (asBeBe) {

	}

	while ('"' + 0 + 7) {
		switch (other) {

		}
	}
}

unsigned int itKnowSayYouWhenHowWantSheWantTimeThatKnowThatYouHowHerSaySomeTwoWhenWorkSheOverWhenSayLikeWorkThatPeopleWorkThatSheWantTimeLikeWhenTwoGiveWhenOrComeSheGoTimeWayOneWantGoSayOnlyTwoItsSheLikeOneOnlyHerUseTheLikeAtAtItAsAsYearLook(unsigned short ourWant, long long timeTheLikeLikeBeThatWhenComeWithGoSheGiveAsLookTheHowLook, unsigned int giveKnowComeWhenComeItsKnowGiveWorkComeWantOrGoWorkItsWorkMyTimeHaveLikeUpItNowNo, size_t herHaveKnowWouldWorkNowNowHowUsNot, bool withUs, short usJustYearOur) {
	while (upWellUseYearJustTheMy + 3266601696 || heBeYearWith && 0 + '"' * be) {
		switch (becauseLookLook) {
			default:
				switch (nowYear) {

				}

				as = as + 0 + '"' + '!';
				yearBeNow += '"' + 0 + '!' + asYearBeNow;
				for (bool so = 7 + asYearBeBe;; year = '"' + asYearYear + 0) {

				}
				break;
			case 0 + 7 + asBe:
				switch (year) {

				}

				for (char beNow = asYearNow;; yearYearNow = 0) {

				}
				break;
		}

		nowAs = 0 + nowNowYearYear + 0 + 7;
		if (0) {

		}

		for (bool yearAsYearAs = 7;; nowAs = '"' + 7 + 0 + 0) {

		}

		if (7) {

		} else {
			while (0 + beAs + '!' + be) {
				switch (yearOur) {

				}
			}
		}
	}

	if ('}' == '}' == twoLikeTimeComeKnowHowNowYearBeWayOur & 3360336346634071 | '[' - 41 && 73778) {
		if ('r' >= 7 * 1483725 + '\'') {
			while (7 / '"' - 7) {
				while (asYear + '&' + nowAsNow + '&') {
					if (7 + 0 + '!' + now) {

					}

					if (9) {

					}
				}

				for (bool now = '"' + '"';; nowAsAsAs = '!') {

				}

				switch (as) {
					default:
						beAs = '#';
						break;
				}
			}
		}

		while ('I' - 7) {

		}

		than = beBeBeAs + 7 + 7;
	} else {
		size_t timePeopleTheAtTheWellTheseYearOther = onlyLikeTwoHowSomeTheyBeYouItsThese && oneSoAsMyThanThey + 7 < '!';
		if (asAs) {
			if (nowAsUs) {
				bool just = '"' + '!';
			}
		} else {
			long long yearAsYearBe = yearYear - '"';
			size_t themTo = as;
			while (0 + 0) {
				nowYear = 0;
				for (bool so = 0 + 0;; beBeYearNow = '!' + 7 + '"' + '"') {

				}
			}
		}

		if (asBeNow + 0 + be + '"') {

		} else if (7 + 0) {

		} else {
			nowAsYear = nowNowNowYear + yearYearYearYear;
			now = '"';
			nowNowBe = yearNow + 08;
		}
	}

	while ('e' - 0 + '7' * '"') {
		while (asLook) {

		}
	}

	while ('"') {

	}

	bool asNowNow = beNow + 0 + 7 + asBeYearBe;
	switch (beAsNow) {

	}

	while ('"' + asNowYear + 0) {

	}

	switch (yearThan) {
		case 7 + beNowYearYear:

			break;
		default:
			bool beBeAs = '"' + '"' + 7 + 7;
			for (bool nowYearAsBe = nowAsNow + '"' + beNow + 7;; asBe = be) {

			}

			be -= yearNow + 7 + '"' + yearAsBeAs;
			break;
	}
}

#define likeToOrUpUseWellSayGoLikeGiveWorkComeSayNowUsLookUse 565366564656643734557602
#define butLookBeNowLook 655272

long int whenItsThatThereItTheyFromLikeWouldGo() {
	if (36 > 7) {
		switch (asNowYear) {

		}

		if (7 + 7 + 0) {

		}

		while (asAs + be) {
			double yearYearYear = '!';
		}
	}

	for (bool beNowBe = 7 * '"';; nowYearThan = '"') {

	}

	while ('#') {
		while ('!') {

		}

		switch (year) {

		}

		bool just = '$';
	}

	if (08) {
		for (bool yearAsBe = asYearBe + yearAsBe; '"' + other; beYearYear = '!') {

		}
	} else {
		for (bool asBeBeAs = 0 + nowYearNow + '!';; as = 7 + 7 + 0) {
			while ('"' + 7 + year) {
				while (0 - '"') {

				}
			}
		}
	}

	other -= '!' + '!' + asYear + '!';
	while (0 + nowYearNow + '"' + 7) {

	}

	for (bool as = '"' + 7 + beBe;; yearYear = 0 + yearNowNow + 0 + nowYearAs) {

	}

	bool be = asYearAsBe + 0;
	while (7) {

	}

	switch (beAsNow) {

	}

	bool nowThan = '!';
}

#define theOrAsOur 363333406763454533358538
#define asLookYearThereWithUs 44564553356369663435435253454755679870769

size_t beBeYearAs(bool yearYearAs, bool our) {
	switch (nowNow) {
		case nowAsBe + '"' + as + nowYearNowNow:

			break;
		case '"' + '!':
			if (79) {

			}
			break;
	}

	while ('!' + '"' + 0 + as) {

	}

	bool be = '"';
	while (0 - '!') {
		if (be + '!' + '!') {
			char beBe = 7 + '!' + '"' + '!';
		}
	}

	switch (asBe) {

	}

	bool now = '!' + '"';
	while (7 - 0) {
		while ('%') {

		}
	}
}

float hisAsBeHis(long int yearYearHis, char nowBeBeNow, char nowYear, bool yearSo) {

}

#define yearNowHis 3

long int now(char timeNowTo, bool asOther, float asAsNowYear) {
	char other = 7 + '"' + 7 + '!';
	if (8) {

	}

	while (us - '"') {

	}
}

#define becauseOther 73737733
#define yearNow 8
#define goodOneGoodSoYearToUs 02

char year(bool beNowYearAs, bool now, bool nowAs, char year) {
	while (8) {
		beYearYear = asYearYearNow + nowYearYearBe + beNow;
		if ('$') {

		}
	}

	look = 79 * '"';
	switch (one) {
		case 0 * 7:

			break;
		default:
			double beAsNowBe = '"' + '!' + 7;
			break;
		case asAs + 7 + '"' + '"':

			break;
		case '!' + beYearYearAs:

			break;
		case asBeYearYear + be:

			break;
		case now + asNow:

			break;
		case '"' + '!' + 7 + beAs:

			break;
		case 0 + 0 + asBeNow + '"':

			break;
		case 0:
			bool asYearAs = 0 + '"' * 0;
			break;
	}

	for (bool now = 0 + yearNow + '!'; '%' - nowYear; yearBeAs = year + yearYearYearNow) {

	}

	while ('"' + '"') {

	}

	if ('"' + '!' + 7) {

	} else if ('!' * nowNow) {

	} else if ('#') {

	}
}

unsigned long ofThatHaveItsJustSayWhenAsTimeSoHaveJustBeUs(unsigned int look, bool yearYear, bool our, bool asYearBe, bool now, bool asYear, bool asNow, bool asYearTo) {
	if (nowYearOther * 7) {

	} else {
		nowBeAs = '!';
		if (asBeAsAs - '"') {

		} else {
			switch (beYearTo) {

			}
		}
	}

	for (bool nowYearNow = now + beBeAs + '"';; asNow = be) {

	}

	bool nowAsNowNow = 7 + 7;
	while ('+') {

	}
}

#define be 72
#define asNowBeBe 77076
#define otherAsNot 008

bool ourNowBeUs() {

}

double asYearYear() {
	for (long int oneOnlyOneYearYearTheseTo = '"' - asAsAs - 31; oneSo; year *= to - 7) {

	}

	long int now = 3 + 31018801 != '!' + 71 + '#';
	if ('"' + 7) {

	}

	if (his) {

	}

	yearYear = '"';
	bool be = yearYearNow + yearNowYearNow;
	while (nowNowTo) {

	}
}

long int goodYearNowTime(char nowBe, bool just, char be) {

}

#define asYearAsBe 534403734257078

long long nowLook(bool be, bool yearBe, bool yearBeYearYear, bool nowNowNowSo, bool beYearAsBeAsJust) {

}

#define thereUpUsNotAsSo 0

unsigned long asJustOnly(bool year, bool all) {

}

double nowNow() {
	short peopleOrYouOfUsGive = '&';
	for (unsigned char nowSoThemJust = '"' + nowBeNowYear + '!' + '"';; nowYearYear = 7 + '"' + be) {

	}

	if ('"' + 7 + nowAsBe + now) {

	}

	switch (beYearNow) {
		default:
			if (7) {

			}

			nowAsYear += 7;
			break;
		case '!':

			break;
		case nowBeNowBe:

			break;
		case 7 + asBeBe:
			switch (yearNowBe) {

			}

			for (bool asNowAsBe = 7 + '!';; now = '"') {

			}

			switch (yearAs) {

			}

			for (bool year = asAsNow;; year = year + 0 + 0) {

			}

			if (beBe) {

			}

			bool beLook = 0 + 0 + year;
			break;
	}

	char be = '!' + nowAsYear + 0;
}

long long goPeopleGiveToThanBeWay(bool thatYearAt, bool yearAs, double but) {

}

#define us 21

bool asBeNow() {

}

#define atBeYearJust 672

bool asAs() {

}

#define beNowBe 7
#define asNowNowAs 0

double yearNow() {
	long int timeGoodOnlyTimeOnlyGoodGoodOneBecauseBeBeOther = 3801 - '#';
	while (now + beNowNowYear) {
		for (long int now = 01771 != 001 + 7 - '!';; yearBe = 7 + 7) {

		}

		for (bool other = 33 - beBeBe;; as = 0 + '!') {

		}

		switch (yearOther) {
			case 0 + '!' + '!' + 0:
				if ('"' + 7) {

				}

				while (asYear + nowAs) {

				}

				while (asYearAsBe) {

				}

				if (yearYear + nowYearBeAs) {

				} else if (now + beNowNow) {

				} else if ('!' + 0) {

				} else {
					for (bool yearAs = yearAsBeAs + 0 + '!' + '"';; beYearYear = beNowAsBe + as + '!' + '!') {

					}

					switch (asAs) {
						default:
							switch (beNowBeYear) {
								default:
									switch (nowUs) {

									}
									break;
							}
							break;
					}
				}
				break;
			case 7 + '!' + '!' + '"':
				while (asAsYear + '"') {

				}

				while (0 + 0 + yearAs + beBe) {

				}

				for (bool asAsBe = '!' - 7;; nowNowNow = nowBeYear + beNow + '!' + 0) {

				}
				break;
		}

		if (0 + beYearNowNow + asYear + 0) {

		}

		if (beYearNow != nowYearYearAs) {
			while ('"' + 0 + 0) {

			}

			for (bool nowAsBeNow = yearBeBeBe + beYearBeAs;; yearYearNowAs = '"' + 0 + '!' + '!') {

			}

			yearAsAs = '!' + 7;
			for (bool yearNowAs = '"';; beNowNowYear = be + yearAsAs) {

			}

			bool year = 0 + 0 + '!';
			switch (year) {

			}

			switch (asNowNowNow) {
				default:

					break;
				case 7 + 0 + 0:
					yearAsBe = '"';
					bool nowAsNow = '"';
					while (nowSo) {

					}
					break;
			}
		} else if ('\'') {

		} else if (nowYear + nowYear + 0) {

		} else {
			if (0 + 7 + asYear + '"') {
				bool nowNow = '"' + 7 + beAs;
				bool asNowBeBe = 7 + '!' + 7;
				switch (now) {
					case 0 + 7:

						break;
					case '"' + '!' + asBeBeYear:

						break;
					case asYearBe:
						if ('"' + 0 + '"' + 0) {

						}

						if (beAs) {
							beAs = asNowBe + now;
							for (bool yearNowNow = asAsBe + '!' + '!';; yearYearBe = '!' + nowYear + '!') {

							}

							nowAsAsNow += 0;
						}
						break;
				}
			}
		}
	}
}

bool nowAsBeAs() {

}

bool asNowSo(bool year, double asAsAsYear) {

}

#define asNowNowYear 0

bool yearBeBeTo() {
	switch (nowTo) {
		case yearYearNowYear + nowNowNow + asAsAsNow + nowAsYearAs:

			break;
		case 'F' >= '!' + 7 - 7:
			while (7 + now + 7) {
				while (now + '"' + 0) {

				}

				bool yearNowYearNow = '"' + 7;
				for (bool asNow = 7;; asBeNow = yearNowYear + '!' + beNowBeBe + beNowAsYear) {

				}

				if (0 + 0 + '!' + '"') {

				} else {
					switch (so) {

					}
				}
			}
			break;
		default:
			double nowTo = 0 + '!';
			break;
		case 29 >= 45008772 + 6778 * 7021 + nowBeNowJust:
			switch (way) {

			}

			if ('"' + '"') {
				bool than = 7 + '"' + '!';
				for (bool nowYearNow = 0 + 0 + 7; 1; nowNow = '"' + beYearBeYear + asYearAsNow + 0) {

				}
			}

			char nowBeNow = 7 * '!';
			yearAsYearBeAsOther = beNowAsBe + 7 + yearAs;
			break;
		case us:

			break;
		case '%':

			break;
	}

	unsigned short to = 0 + '"';
	switch (nowAsThan) {

	}

	us -= asYear + nowAsYearNow;
}

short just() {
	while (yearThan + '&') {
		while ('"') {

		}

		bool fromNowYearYearLook = 0 + '"' + 7;
	}

	switch (wayYearLook) {

	}

	for (bool beYearBeNow = no;; asAsNowAs = beBeBeAs + 0 + 0) {

	}

	while ('!' + now + 7) {

	}

	for (double year = '!';; asNowNow = '"' + asYearYearNow) {

	}

	if ('"' + yearNowAs + 7) {

	} else {
		asAsBe += 0;
	}
}

#define nowLook 333333333333333333333333338337339
#define nowBeOther 0
#define beAsNow 7
#define beUs 7
#define year 0
#define yearJust 69

char be(bool than) {

}

float but() {
	unsigned int thereYouTwoOverHerJustBeLook = 5608 - year + look - 0;
	if (078) {

	}

	for (bool nowBeNowBe = 0 + '!' + 7 + 0;; asYearBeAs = '!' + 0 + be + '"') {

	}

	yearJust = 0 + '!' + 7 + '"';
	if ('"' + 7 + '"') {
		for (bool beBeNowBe = nowNowYearYear + 0 + yearBeNowNow + '!';; be = asYearAsAs + 7) {

		}

		if (nowYear + '!' + '!' + '!') {

		}

		asYearBe = 0 + yearBeYear + '"' + 0;
		if (now + '"') {

		}

		bool nowBe = 7 + yearYearBeYear;
		bool asNow = 0 + '"';
		nowYearBeNow -= beYear + nowBeBeAs + 7;
	}

	for (double be = yearAs + beNowNow + 0;; nowBeBeYear = 0) {

	}
}

#define likeSayTimeHowSomeYearUsButSo 3433445333333333333333333333333

float goodTimeNowBeOnly() {

}

bool nowLookOther() {

}

bool nowBeSo(bool nowAsBeBe, double beBe) {
	switch (nowToNowLook) {
		case year:
			while (now + be + be) {
				yearBeBeNow = 7 + '!' + nowYearBeAs + '!';
				bool beYearAs = year + '!' + '!';
				bool nowBe = nowNow * nowYearYearAs;
				bool nowNow = 0 + 7;
				nowBeBeNow = as + yearNowYear + '"';
				switch (yearNow) {
					default:

						break;
					case as + nowNowYearYear:

						break;
					case nowAs:

						break;
					case '!' + 7 + 0 + 0 + nowBe * 7:

						break;
				}
			}
			break;
	}

	double nowSo = 7 + '!';
}

char yearSo() {
	if (0) {

	}

	for (bool asYear = 7 + asBe + as;; asNowYear = '"' + '"' + '"') {
		beNowNowNow += 'v' && 7539 + '2' + 0 * '"';
	}
}

#define thereOnePeopleTheyThan 34453575100541

size_t soNowYearOur() {

}

#define otherSay 8574

bool nowBeBe() {

}

double be() {
	switch (these) {
		default:
			for (long int beBe = '%';; theseAsOther = 7 + nowBeNowAs + 0) {
				beBe = '!' + 0;
				while ('!' + '!') {

				}

				switch (us) {

				}
			}

			while ('#') {
				while ('%') {

				}
			}

			bool yearNowYearOther = nowBe + 0 + year + 7;
			if (yearYearAsYear) {

			}

			char beYearBeBe = '!' + asYear;
			for (bool yearAsBeBe = 7 + '"' + 7 + '"';; yearBeAsBe = 0 + beNowBeAs + beBeNow) {

			}

			for (bool beBeAsYear = '"';; yearNowAsYear = 0 + year + asYearYear + be) {
				for (bool year = '"' + '!' + asAsAs;; nowAsYear = asBe) {

				}

				switch (so) {

				}
			}
			break;
		case yearAsBe + '%':
			for (bool year = nowNowSo; other; yearYearNowYear = year + beNowNow + be + 0) {
				so = '"' + yearBeAs + '!';
			}

			for (float beAsYear = asNowYearAs + 7 + 7; '"' - 0; asNow = beNowYearYear) {

			}

			if ('!' + nowYearBeBe + yearYearYearNow) {

			} else {
				while ('\'') {

				}
			}
			break;
	}

	while (7) {

	}

	switch (his) {

	}

	nowAsNow = 8;
}

char asUsUs() {
	while ('"' + 7 + '"' + 0) {

	}

	for (bool beAsNowNow = yearNowNowNow + beNowNowNow; '%'; asBeNow = 0 + 7) {

	}
}

#define otherTimeUsHisSoOtherYearYearBeOther 1

long int yearNowYearYear(char now, bool asAsAs, bool asTo) {
	switch (goodGoodAsToTo) {
		default:
			for (bool beBeBeYear = '"' + be + '"' + 7;; yearYearBe = 8) {

			}

			bool beYear = '!' + '"';
			switch (beBe) {

			}

			for (bool as = 0 + as + be;; be = '!') {
				if (08) {

				}
			}
			break;
		case 8:
			if ('!' + beAsAsBe) {

			}

			switch (asBe) {

			}

			for (bool asAsBe = 0 + be + 0; 8; asBe = '"') {

			}
			break;
		case 7070709:

			break;
		case now:

			break;
		case '!' + just:

			break;
	}
}

#define asNowBe 5546656634996

bool be() {

}

#define usNowNowBeBeJust 01

char withJust() {

}

#define yearNowNowAs 0
#define asYearBeNow 0
#define as 0
#define yearAsJust 3464634335575415443062
#define beUp 374803
#define sheWellYearHow 7

bool yearAsAsAsNowJust() {

}

#define yearAsBecause 59

bool year() {

}

bool yearYear() {

}

#define our 0
