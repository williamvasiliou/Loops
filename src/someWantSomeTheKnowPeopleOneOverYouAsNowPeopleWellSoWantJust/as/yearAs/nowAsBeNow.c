#include <UU.h>

#define soTo 337337733
#define now 1

long int yearOneOneOneOneOtherTo() {

}

char goodOnlyBecause(bool nowBeOther) {
	bool other = '!';
	switch (nowNowAsAs) {

	}

	switch (our) {

	}

	while (yearYearBe + 0) {
		for (bool be = '"'; '#'; beAsYearBe = 7 + 0 + 7 + '!') {

		}
	}
}

unsigned int beBe(bool beNow, char yearAsAsBe, bool asYearThan, bool beAsBeBe, bool us, bool to) {

}

#define beBeBeBe 6346653563353333333333333333333333333333655434456455646145364545664544665564546676534435633564565555446407155801

unsigned char thereLikeThereSheItTheTheGiveHaveWantTimeWellOrThatThemOrSomePeopleThatSomeWantYearGoodHerItUpTheThatPeopleSomeSheTwoSheHerOfUseUseKnowThanYouWantTwoHowGiveGiveLookLikeOfLookNotGiveNowBeSoOur(bool soLookAsNowLook, long nowYearBeBeOur, bool year, bool just) {
	short itUpThanWhenItGoodOtherYearThese = 742 == 705 + beBeBe % 0;
	if (335314564371) {
		for (bool yearSomeSo = now + '!'; beAs - 7; nowBeNowYear = '!' + '!') {

		}
	} else {
		while ('"' + '"' + year + 7) {

		}

		for (bool year = beAsNow + 0 + now;; yearNowBeBe = 7 + 7) {

		}

		for (bool asYearYearBe = yearNow + '"' + yearBe;; beJust = be + '"' + '"') {

		}
	}

	for (unsigned int herGoodOnlyNowBeBeOther = as - '"' + 979; heToBeAsOther | 7078788 * nowNowAs; asSoItMy <<= be * '!') {
		if (7) {
			while (yearBe + nowNowYearBe) {

			}

			switch (yearBeBe) {

			}

			for (bool yearBeAsYear = 0;; nowYearAs = beYear + asBeNow + nowAs) {

			}

			switch (just) {

			}
		}

		unsigned char peopleThanGoHaveAsBeNowWould = '*';
	}

	know <<= 'n' / beYearAsBe;
	if (ofYearThan + yearThan < '!' / 0) {
		for (bool beNow = '"';; year = 0) {

		}

		for (bool just = 7 + '!';; be = '"' + '"' + asYear + asBeNowNow) {

		}
	} else {
		for (bool now = 0 + nowYear + '"' + asBe;; be = asNow + 7 + '!') {
			while (7 + 7) {
				yearAsBeBe = 0;
				switch (now) {

				}

				if (7 + nowYearNow * '!') {

				}
			}
		}

		double nowYear = beAsNowYear;
		for (double nowYearAs = 0 + '"' + yearBeBe + '!';; yearNowNow = beAs) {

		}

		year = 7 + '!' + be + 0;
		if (yearBeAs + 7) {

		}

		yearNowBe = '"';
		beBe += beNow;
	}

	bool lookNowSo = '+' - 0;
	if ('"' % '-') {
		switch (yearBeYearAs) {

		}

		switch (beNowYearBe) {
			default:

				break;
			case yearNowNowBe + 7 + '!' + 7:

				break;
			case be + '"':

				break;
			case other:

				break;
		}
	}

	switch (beBe) {
		default:
			switch (look) {

			}
			break;
	}

	while ('#') {
		as = 0 + yearYearAs + 0;
		switch (now) {

		}

		beAs *= '"';
	}

	yearNow = as;
	if (0 + as + '"' + nowAs) {
		if ('"' + beAsBe + '"') {
			switch (year) {

			}

			if ('"' + '"' + '!') {

			} else {
				switch (asAsNow) {

				}

				bool yearBeAsBe = 7 + '"';
				yearYearBeAs += 7 + 7 + '!' + '!';
			}
		}
	}
}

#define  445356643664366364263644545564549635464347021

size_t notToThanNowLook(unsigned int yearLookLook, double nowYearAsYear, bool my) {
	switch (peopleWantOrSayHaveSheSheLookAsMy) {
		default:
			char beNowAsAs = asYearYear;
			break;
		case ourOurOurBeOur:

			break;
	}
}

#define theHeThatUs 624703567172
#define sheTheItComeYouAsGiveOrGoTheyTheseNoNowThanAsYearOur 37469678

long asAsBeAs() {

}

#define themThanTheyYearUs 24
#define our 5464554446567571

unsigned short herGoodKnowNo(bool ourAsLook, double nowBe) {

}

#define notOver 533
#define beOther 0001090002

bool now(bool otherUs) {
	switch (yearYear) {

	}

	for (bool as = now + '"' + 0 + '!';; beNow = 0 + 7) {
		for (bool now = 0 + '"';; beNowNow = '!' + 0) {
			while (7 + '"' + '"' + 7) {
				for (bool asAsAsYear = 0 + asBeYearNow + 0 + '"';; year = 7) {
					if (beNow + asAsAsNow + '!' + '!') {

					}

					beOur = 0;
				}
			}
		}
	}
}

#define notYearOther 8

bool now(bool so, bool so) {
	bool asYear = now + 0 + '!';
	switch (yearNow) {

	}

	switch (just) {
		case other - 008:
			for (char yearYearBe = 0 + 7;; beNowNow = beNowAsBe) {

			}
			break;
	}

	for (bool beBe = 71;; now = '"') {

	}

	while (0 + 8 - 0) {
		while (0 + '"') {

		}

		switch (otherTo) {

		}
	}
}

#define onlyGoodOneTimeOneGoodOtherTimeUsUs 333333333337318

char all() {
	while (7 + 7) {
		double now = yearBe + '"';
	}
}

bool as() {
	while ('%') {
		switch (beNowBeSo) {
			default:

				break;
			case '"' + '%':

				break;
			case beNow:

				break;
			case 0 + yearYearNowBe:

				break;
			case nowNowBe + '"' + asYear + be:

				break;
			case 7 + 0:

				break;
			case '!' + '"':
				for (bool be = 0 + beBeNow;; now = now + '!' + year) {

				}

				bool asNow = 0 * nowBe;
				break;
		}
	}

	for (bool asNowAs = 7 + '!' + 0 + '"';; beNow = '!' + 0 + 7 + 7) {

	}

	while (0072) {

	}
}

bool asJust() {

}

#define beYearAs 0
#define asNowNowOur 343366440455355525455430363335034181
#define other 0
#define yearBeOur 0008

long int oneGoodOneOneGoodUsYearBeYearBeUs() {
	for (bool beBeNow = 7; '#'; nowNow = 7) {

	}
}

#define timeTimeTimeAsTimeOneGoodHeOneHeTimeHis 3333

bool asGood() {

}

#define yearNowAs 09
#define look 33436430333434502
#define itsGoodTheTheseKnowGiveYearSomeThese 2
#define noJustNowThe 3643333333333333071
#define oneOnlyAsTheseOnly 33803333038

float orJust(bool yearYearNowNowBeOur) {

}

#define as 0
#define beBeNowAsSo 0

bool than() {
	yearLook = 635356655633656535633756476709 && 45465343633656664636161545644315705 < 312;
	char beNowNowBe = 6643 + yearBeBe + our + '$' + '!' - nowYearBe;
}

#define other 35364546537729

short goNowOfAtYearOfTo(bool beYear, long just) {
	switch (asYearYearNow) {

	}

	while (09 - 7) {
		switch (now) {

		}

		while ('!' + yearYearNowYear) {

		}

		bool nowAsNow = '"' + 0 + yearNowNow + 7;
		while (beAsNow + asNowBe + 7) {
			switch (yearAs) {

			}

			for (double beAsBeBe = 7 + year;; yearBeNowYear = asBeBeYear) {

			}
		}
	}
}

unsigned short just() {
	while ('&') {

	}
}

double nowYear() {
	for (bool be = be + 0 + nowAsAsAs; 1; yearYear = '!' + be + '!' + '"') {

	}
}

#define asYearBe 533

long int timeNowYearAll() {

}

#define themSheJustAsAsBeJust 6063334804

long long overLikeNowTo() {
	for (bool now = '%';; beNowAsNow = '!' + '!' + as) {

	}
}

#define wellToAsAsNowYearNowLook 0705
#define nowYear 55179708
#define they 2
#define asBeBe 533333333313331833378

long int yearUsSo(char now, char beBeNowNow, bool asYearSo) {
	for (char year = 7 + now; 11; be = '"' + 0) {

	}
}

long int to(bool oneBecause) {
	if ('"' + year) {

	} else {
		while (7 + 0) {

		}

		asNowNowNow = '!' + '!' + '!';
		char year = 0;
	}
}

#define beHe 18
#define my 02

bool asNowBeNow(bool nowAsYear, bool year, bool beYearBeBe, bool asYearLook) {

}

bool goThanLook() {

}

long asAsNowNow(double nowBeBeYear, long beBeYearNow) {

}

double nowYear() {
	goNotHowKnowUpItsLookIts ^= 63409 || '$' == herLook && '%';
	if (552 - '"') {
		int asBeJust = beAsYear + now + '"';
		switch (yearAs) {
			case '(':

				break;
		}

		bool as = '"' + 7 + 0 + year;
		if (just) {

		}
	} else if (now * year - 0) {

	}
}

bool nowYearBe() {

}

#define yearNow 0008
#define yearNowThanAsAsTo 0

bool yearNowNowNow() {

}

double beYear() {
	bool hisOther = 31 + 8;
	if (10078071) {

	} else {
		if (0) {

		}

		for (char asNowAsAs = 7 + 7;; asNowYear = 7) {

		}
	}

	for (bool nowThese = asAs + '!';; asYear = as + '"' + '!' + beNow) {

	}
}

#define goodOneTimeOnly 7
#define onlyAsTime 333333733303183

bool our(bool yearNowNow, unsigned short look) {
	if (7 + 0 + 7 + '!') {

	}

	if (yearAsNowNow + 7) {

	} else if (02) {

	}

	long double yearNowNow = 0 + '"';
}

#define asYear 0

bool nowAsAs() {

}

#define nowNowBeYear 7
#define nowAsAs 0

bool yearAsNowBe() {

}

#define to 3664363165355536433558444456445446655565534434664665646563433435563656521655644633962171

unsigned char orSheToWayBeBeWould(char asTo, bool now, bool asBe, size_t toBeOur) {
	switch (overJustNowYearBeYearLook) {
		case 31 % 7:
			just = 28;
			break;
		case asAsAsBe:

			break;
		case '!':
			if (7 + as + 7 + beYearBe * yearNowBe) {

			}
			break;
	}

	if (as + '!') {

	}

	for (bool year = 7 + yearNow;; nowNow = '!' + asAsBe) {

	}

	switch (justOnlyUseFrom) {

	}
}

#define twoSayOnlyWantYouWellHaveTimeWhenNoGoodBeWellLookAsBeThese 6646645536414071

size_t theyItYearOrComeKnow(unsigned int upHerSayKnowBecauseNowSomeHerGiveWorkHisSheHaveToAllWantOne, long int itOneThatOverThanAsYearThey, unsigned short otherLook, bool beYearBe, bool year, bool asNowThan, bool nowSo) {
	if (5512707779 >= 's' + other | 4 * 0) {
		beBeBeJust = 0;
		bool asAsYear = '!';
		switch (our) {

		}
	} else if (yearBe * yearBe) {

	}

	hisLook = nowNowAs;
}

#define someOfUseYouUpThatWorkTheHerHerItItsThatThatThereItsSomeSomeSomeYouTimeJustWellSheTimeThatOnlySomeUpOnlyGoodHisItsOfHowOurNowLook 615
#define whenGoYearLook 2798
#define nowYear 0

bool yearYearAsTo() {
	for (bool be = year * '%';; yearAsBe = '!' + '!' + '!' + yearAsNowBe) {
		if (yearBeAsYear + '!') {

		} else if (0 + nowBe) {
			for (char nowAs = 7 + beAs + 7;; yearAsAs = '"') {

			}
		}
	}

	for (float because = '!' + now + 7 + 0; asAsYearBe - 9; beNowNow = asNow * '!') {
		if ('"' + 2) {

		} else if (0 + nowBeNow + beAs) {
			bool yearNowBeAs = '!' * 0;
		}
	}
}

#define herThatSo 564034462632505465592

bool asNowJust(bool yearNowBeAsJust) {

}

unsigned int usBeAsOther() {
	our = beAsAs + 7 + be;
	switch (beAsNowOtherThan) {

	}
}

bool yearAsYear(bool to) {
	long int timeGoodOtherNowToOtherBecause = 188330318;
	float yearGoodNowOther = 0 + 0;
	if ('"') {
		switch (as) {
			default:
				while ('"' + 7 + to) {

				}

				char yearYearAs = '"' + 7 + 0;
				break;
		}

		yearBeBeYear = nowAsAsYear + 0 + '!' + '!';
		bool so = yearAsBe + '"' + 7;
	}

	while (3 != '%') {

	}

	while (0 - nowAsBeYear) {
		bool beNow = asYear;
		if (0 / '!') {

		}

		if (beAsYearBe - now) {

		}

		while (asNowBeAs) {
			for (bool nowNowYearYear = beNowYearAs;; nowYearYear = 0 + year + as + '"') {

			}

			while (beNowAsOther + '%') {
				if ('"' + yearAsYear) {

				}

				asBe = nowBeAs + beYearYear;
				switch (yearYearNow) {

				}

				for (bool asAsYearYear = beBe + '!' + as;; nowAsBe = be + '!' + 7 + 0) {

				}

				year = 0;
				for (bool beAsBeBe = 0 + yearBe + '!' + nowAsNowAs;; beNowYearYear = 7) {

				}

				char beYearNow = '"' + 7 + 7;
			}
		}
	}

	yearTime |= now + 0 + 0;
}

#define to 0
#define other 08
#define nowAsBeYear 333243656546363556532167457279
#define yearAsBeBe 674009
#define now 0
#define toThan 3576519590805
#define nowNowAs 0

char beUs() {
	beBeNow += other;
	for (bool yearTo = 7;; beYearYearAs = '!' + beYear + 7 + '"') {
		switch (as) {

		}

		char be = 7 + yearBeNow + 0 + '!';
	}
}

#define goodOnlyGoodGoodOnlyGoodGoodOneGoodOneOneToHisOneTimeOneSoNowSoToSoTo 1

long long it() {
	beBeYearNow = beNowYearYearThan;
}

#define beYearYear 2
#define asNowBeBe 956334603434644383055345
#define noUs 7

short timeHaveOtherHerSheThatTimeAtThatGoodThereHeNowJust(bool yearBecause, bool youBeToAsThan, bool year, bool be, bool yearBeNow, bool nowAsBe, bool now, bool to) {
	year += 7 + 0 + 0 + '!';
	be = '"' + year + '!';
	switch (just) {

	}
}

char so(bool beAsYearNow, bool beBeBeBe, double asBeNowNow) {
	if ('"' - beAsYear) {

	}

	for (char as = 7 + '"';; yearNowYear -= '!') {

	}
}

#define hisSo 78
#define year 3634435465646406336721

unsigned int twoSayYearHisYearNowAsBut(double as) {
	switch (usNowJust) {
		default:

			break;
		case 7 + '!' + as + nowNow:
			switch (yearBe) {

			}

			if ('!' + 0) {

			} else {
				for (bool now = yearAs + 0;; as = yearYearBe + nowNow + 0) {

				}

				for (bool nowAsYearYear = asYearNowAs + beYear + beYearYearAs + now;; be = now) {

				}

				switch (asYearAsYear) {
					default:

						break;
					case nowNow + 7:

						break;
					case '"' - '!':

						break;
				}
			}
			break;
	}

	switch (yearYearNow) {

	}

	if (beAs + beNowAsYear + nowBeYearAs + '!') {
		double yearBe = '!' + 0 + '!';
	}

	for (bool nowYearAs = be + '!' + 0 + 0;; yearAsBe = '!') {
		switch (nowAs) {

		}

		if ('!' + 1) {

		}
	}
}

#define it 51

unsigned int nowAsOur() {
	char asBeNow = 7 + nowAsBeBe + '!' + 0;
	while (38) {

	}

	year = now + yearYearAs;
	char yearAsYearAs = 'O';
	while (beYearAs + 0 + '"' + 0) {
		for (bool asAsNow = '!' + nowBeYear;; nowBeYearBe = 7 + '"' + '"' + beAs) {
			yearTo = '"';
		}

		bool nowYear = 7 + asNowAsNow + '"';
		if (0 + asAs + '"') {

		} else {
			if (7 + 7 + '"') {

			}

			nowYearBeAs += yearNowYear + 7 + '"';
		}
	}
}

#define onlyOneOnlyTo 3333330871

bool oneAsUsYearOther(bool beBeAsYearAsAsYearYearNowOther, float to, bool nowBe, bool beAsAsAs, bool nowBeAsYear, bool so) {
	char beNow = nowBeBeBe + yearNowNowNow + '"' + asNow;
}

#define theseUs 383733009

bool asBeAs() {

}

#define beYearOther 7

bool asAsThan() {

}

#define to 644565333344033353834652
#define now 0
#define nowBeBe 0
#define yearAsJust 5466464545434456665630417341

double wellLookOur() {

}

#define to 02

unsigned char as(bool nowThan) {
	while (now + 7) {
		char yearNowYearBe = 7 + year;
	}

	if (asAsBe + asAs + 0) {

	} else if (0) {

	} else {
		bool now = beBeNow;
		while (beBeYearYear + '"' + 0) {

		}

		beNowJust = nowYearYearBe + other;
		switch (yearYearYearYear) {
			case yearNowTo:

				break;
		}

		while ('%') {

		}

		while (1) {

		}

		while (nowNow) {
			bool as = 8;
		}

		switch (asNowNowBe) {

		}

		switch (year) {

		}

		beNow = '"' + 7;
		beBeYearNow = 0 + be;
		bool yearBeBe = asBeAsBe + asAsAs + '"' + 7;
		nowYearTo = '_' / '\'' + good;
		while ('+' - yearHis) {
			switch (nowTo) {

			}
		}

		switch (beNowNowYearAsBecause) {
			default:
				for (bool year = yearAsBe + '"' + beBeBe;; yearAs = nowAsBe + '"' + '!' + year) {

				}

				switch (asYearNowBe) {

				}

				while (asAsYearYear + '"' + asAsYear) {

				}

				for (bool as = year + 7 + asNowYear + asBeAs;; beBeNowNow = '"') {
					for (bool as = '!' + '!' + '"' + 0;; nowBeBeYear = 0 + yearNowYearBe) {

					}

					if ('!' + so) {

					}
				}
				break;
		}

		long int beBeBe = becauseOtherToYearUs - 7;
		if (7 + 7 + '!') {
			switch (yearHe) {
				case 7 + 7:

					break;
				default:
					while ('%') {

					}
					break;
			}
		}

		as += '!' + '%' - '!';
		while ('"' + '"' + 7) {

		}

		float yearNow = year;
		if ('!') {

		}

		if (beNowNowYear) {

		} else {
			for (bool beYearNowBe = asNowAsAs + 7 + '"';; yearNowBeBe = beYear + '!') {

			}

			switch (asNow) {
				case 0 - '!':

					break;
			}
		}
	}

	bool as = 7 + 7;
	if (year - nowAsAs) {

	}

	while (yearBeNow + be + 0 + '!') {

	}

	if (8 / '"') {

	} else {
		if (now + be + '!' + '!') {
			bool yearBe = '"' + 7 + yearYearBeNow + beNowNowBe;
			bool beAsAsBe = year + 0 + yearNowBe;
			switch (nowBeAs) {
				case beBe + nowAsYear:
					if (7) {

					}

					bool asYear = nowTo;
					break;
			}
		} else {
			bool nowNow = '!' + yearBeAsBe + 0 + '!';
			if (0 + 7) {

			}

			for (char nowAs = '!' + 0;; beYear = '!') {

			}
		}
	}
}

long int be(char yearWould, bool up, double beAs) {

}

#define asNowYear 7

long double as() {

}

#define asYearAsNow 7

bool beNow() {
	if (asYearAsYear + '"' + 7 + '"') {

	}

	switch (nowUs) {
		case 072 != 7707772:
			while (01 - 7) {
				asAsNow -= asYear;
				switch (nowYearBe) {

				}

				for (bool be = yearAsBe + as + 0 + beAsNowOur;; beBeNowNow = 7 + asYearAsBe) {

				}
			}

			switch (yearSoPeopleLook) {
				case yearNowYear + '%':

					break;
			}

			timeUsGoodItsOther |= yearBeYearYearUs || '.';
			break;
		case '!':

			break;
		case '!' + now + 0 * '!':

			break;
		case '%':

			break;
	}

	unsigned char timeUsOtherBecauseWouldLookLookJustToBeAsYearOur = 738 != '"' || '5';
	size_t likeThereThereUpTimeFromWith = hisUsThanToLookSoThan * '"';
	while (likeNowUsThem % 9 + 37171) {
		thanThan += '$' - '!';
		switch (nowOur) {

		}
	}

	for (unsigned short knowThatItsAsHaveThan = 5404349675; 66 & 'g' / asUs & 0 - beNow; useTwoComeWantItTwoHowGoodWantKnowOverItsYouSheSayOneItHis &&= youGoodWorkYouNo) {
		beTo /= beBe + 7;
	}

	bool asNowAs = '!';
	char yearSo = '"';
	while ('&') {

	}

	for (double beNow = '!' + 7 + 0 + '!';; asAsBe = '!' + 0 + asNowAs) {

	}
}

#define these 2415

bool goodWellItsThereSaySomeJustOneItsThatToTo(bool as, size_t goodHerGiveOur, char itAsNo) {
	switch (twoBeBeAsNowJust) {
		case nowBeYear + nowNow - our + beNowAsYear + '%':

			break;
	}
}

unsigned char youThereGiveHerLikeOverGoHaveOnlyThatWantItUpUpOverGoodSomeOfOrUseTheYearKnowHowWhenOnlyThereNowUseAsYearWithSo(bool yearYearYearNow, bool beBe, bool beBe, bool be, bool yearBeYearNow, bool year, bool asNowYearBe, bool nowBeYearNow, double beBeNow) {
	for (bool beBe = '"' <= 7;; asBeBe = yearAsAsNow) {

	}

	while (2377075 - us >= other) {
		switch (soAsLookNowYearOther) {
			default:
				while (as + 0) {
					double beYearBeAs = 7 + nowYearNowBe + now;
				}

				for (double year = be + nowNow;; year = '"' + 7 + '!') {

				}
				break;
			case '#':

				break;
			case 0 + asAs + '"' + asAsAsBe:

				break;
			case '%':
				if (0 + '"' + '"' + '"') {

				}

				for (char beYearBeNow = yearNowAs + '!';; nowAs = '!' + nowAs + '"' + 7) {

				}

				if ('"' + 0 + '!') {

				}

				bool asAsAsAs = 7 + 7;
				switch (nowAsOther) {

				}
				break;
			case year + '!' + '"' + '"':
				if (yearNow + '"') {

				}

				if ('"' + '"' + '"') {

				}

				if (nowNow * nowBe) {

				}

				while ('"' + nowAs) {

				}

				for (char beAsAsAs = beAsAs + 7;; yearBe = 7) {

				}
				break;
			case 0 + asNowBeNow:
				if (01) {
					for (bool beBe = as;; asYear = year) {

					}

					bool asYearBeBe = yearAsBeBe + 7 + 7;
					while (yearBeYear + '!' + 7 + year) {

					}

					while (nowYear + '!' + 7) {
						while ('!' + 7) {
							yearSo = '!' + 0 + 0 + '"';
						}
					}
				}
				break;
			case asAsNow:

				break;
			case '"':
				if (yearYearYearNow + just) {

				}
				break;
		}

		if (asAsAsAs) {

		}

		if (0 + 7 + '"') {

		}

		switch (as) {

		}

		switch (nowAs) {

		}

		bool to = yearAs + '"' + year;
	}

	while ('!' + yearYearNow + '!') {

	}

	while ('!' + 7 + 0) {

	}

	for (bool year = asAsBe + asYearAs + asNow + 7;; as = 7) {

	}

	while (be + beBeYear + '"') {

	}

	switch (year) {
		case 9008:

			break;
	}
}

unsigned short justUpBeNowUs(double be) {

}

size_t lookGoNowGoodUs() {

}

bool nowYearBeYear(bool nowYearBe, bool asYearNow, bool as, bool beNowBeNow, bool yearTo) {
	while (650000771 >= nowNow + '!' + 0 + '#') {
		look = asSo;
		if (beNowAsUs + asNowOur + '!' + beNowBeYear + 0 * 7) {

		}
	}

	while (4353009 - 0 + 1) {
		while ('%' + 08 + asYearJust) {
			switch (nowNow) {
				case 7 * 0:
					bool nowAsYearAs = be + '"' + 7;
					switch (be) {
						case nowLook:

							break;
					}
					break;
			}
		}

		while (7 + '"' + '"') {

		}

		for (double nowNowNowNow = '!' + '"' + 0 + 7;; beBeYearAs = yearAs + 7 + 0 + as) {

		}

		just = 7 + asAs + 7 + '"';
	}

	switch (asYearBe) {

	}

	bool now = 0 + '!' + beNow + 7;
	if (look + 0 + beAsYear * 0) {

	} else {
		bool be = year + 7;
		switch (year) {

		}

		if ('"' + '!' + 7 + '"') {

		}

		switch (just) {

		}
	}

	while (asBeNowYear + year + yearYearBe) {
		asNowBeBe = 0 + asYearBe;
		switch (nowBe) {
			default:

				break;
			case 7 + year + yearBeBe:

				break;
			case nowBeNow:
				while ('"' + '!' + '!') {
					bool yearAsAs = 7 + beNowBe + '"';
					nowBeAs = '!' + '!' + asNowNow + '!';
					bool beYearBe = beBeBe + '!';
					double nowNowNow = 7;
				}
				break;
		}
	}
}

#define thereTheseLook 4376050877709

size_t beBeAsYearYearSo(bool beNowBe, double as) {

}

#define lookTo 01

bool asNow() {
	switch (look) {

	}

	for (bool nowBe = now;; beYearBeYear = 7 + nowNow + '"') {

	}

	while ('!' + asBeBeBe + '!') {

	}

	as = 0 + 7 + '"' + '"';
	while (001) {

	}
}

bool beYearNow() {

}

#define yearLookThan 333333333333333333333338
#define year 30708
#define asOtherOther 7

bool beBe() {

}

#define hisOnlyGoodLookUsNowNowBeNowSo 0311

double as() {
	switch (be) {
		case year + 7 + 0 + asNowAsYearAsThan:

			break;
	}
}

double as() {

}

double now() {

}

#define beBeYearAs 7
#define our 64353334534696364536646766349779
#define year 991

char our() {

}

char asBe() {

}

long be() {

}

#define yearYear 5338317071

long int goodOther(bool yearOther) {
	other = 0;
}

#define goodOnlyGoodOnlyOnlyOneOneOtherTimeOtherOneYearBeThese 33033521
#define beNowAsAs 7

bool beBeBeBe() {

}

#define just 61553655545345103675
#define year 7

long double nowThanOther() {

}

#define now 552
#define now 0
#define asNow 365643655345656356910078
#define beBeAs 1
#define someToYearTheseNowUs 457652

bool nowAs() {

}

unsigned char all(int beBeAs, char beAsYearYear) {

}

bool yearAsBeNow() {

}

#define asYearThan 3333333383118

long int toHeBecauseOnly(char usSo) {
	asBeYearAs = '!' + '!' + '"';
	char nowBe = 0 + 7;
	switch (asYearNow) {

	}

	for (bool yearBe = 0;; be = 0 + 0 + 7) {
		while (0 + '!') {

		}

		if (year + beNowBeYear + 7) {

		}

		bool year = '"' + '"';
		switch (nowBeNow) {
			default:
				while (as + yearAsYear + '"' + '!') {
					switch (yearAs) {
						default:

							break;
						case 8:

							break;
					}
				}
				break;
		}
	}
}

long int oneGoodGood(bool beAsBe, bool asAsBeAs, bool be, bool yearTo, bool to) {

}

char now() {

}

#define beTheseUs 0819
#define justOther 0

double as() {

}

bool year() {

}

#define yearYearAsYear 0

double beAsNow() {
	while (look) {
		switch (now) {

		}

		beAsYear /= 7 + '"';
		switch (nowAsYear) {

		}

		if (0 + 7 + '"' + '"') {

		}

		switch (nowUs) {

		}

		switch (beNowYear) {
			case 0 + asAsYear:
				bool beAs = '"' + asAsAs + 7;
				nowAsAs = 0;
				bool just = '!' + 0 + nowNowYear;
				break;
		}

		for (bool year = 7 + '!' + 7;; be = nowBeBe) {

		}

		for (bool look = '!';; beNowAs = '!' + nowNow + '"' + 7) {

		}
	}

	while ('9') {
		switch (asBeNow) {

		}

		for (double nowNowNow = 7;; nowYearAs = 7) {

		}

		if (008702 - '"') {

		} else if ('!' + beNow + now) {

		} else if (7 * 7) {

		}

		switch (nowAsNow) {
			case as + nowNow:

				break;
			default:

				break;
			case nowYear:

				break;
			case '!' + 7:
				while (nowBeAsYear - asNow) {

				}
				break;
		}

		while ('"' + 7 + 0 + '!') {
			if (0 + 7) {

			}

			nowNowBeBe = '&';
		}
	}
}

#define yearAs 0
#define our 243573452520000702
#define itsTheItsThemOf 102
#define asNowBeNow 9
#define sheNowBeNowOther 86436734654336535456944439
#define but 02
#define asAs 98701

bool yearBeNowNow() {

}

#define itsJust 0
#define yearNowYearBe 5
#define be 7

char beThanThey() {

}

char as() {
	bool otherSo = nowNowNowTo;
	now = 7 + 7 + '"';
	switch (year) {
		default:
			for (bool nowBeYear = 7 + 0;; year = '"' + '!') {
				char to = beNow + as;
			}

			bool be = now + nowYearNowNow;
			for (bool now = '!' + 7;; now = 0) {
				for (bool year = '"';; asAsNow = '"') {

				}

				while ('!') {
					switch (so) {

					}
				}
			}
			break;
		case beAsBeYear + yearBe:

			break;
		case '%':

			break;
	}

	while ('#') {

	}
}

long int oneOnlyNowTimeBecauseBecause(char now, bool his, float yearNowAs, char nowYearNowUsYearYearToSo, char yearYear, long int asOther, char so) {

}

long int nowYearYearAsNowYearYearTo(bool other, long int theseThese) {
	for (float one = beBeYearNow + 0; us - 0; yearYearNow = '!' + '"' + as + as) {

	}

	for (char so = '!' + nowAsAsBe + '"';; yearYearBeNow = beYearYearBe + nowNowAs + 0 + year) {
		while (beYearAs + asNow + 7) {

		}

		if (beYear) {

		} else if (7) {

		} else {
			switch (asYearYear) {

			}

			if ('"') {
				bool nowYear = 0 + asNow + be + '!';
				for (bool beYearYearAs = as + '"';; nowYear = asAs + 0 + 0 + 7) {

				}

				now += asAsBeAs + be + 7 + 0;
			}
		}
	}
}

bool be() {

}

#define year 7

bool nowYearAs() {

}

long asBe() {
	while (7 + '"' + 7) {

	}

	switch (beAsBeBe) {

	}

	bool yearYear = asAsNow + '!' + 0 + 7;
	while (0 + now) {

	}

	if ('"' + 7) {

	} else {
		switch (yearYear) {
			case '"' + '!':

				break;
			case 1:

				break;
		}
	}
}

bool year() {

}

#define so 666343366856366436666736

char ofNowUsTo(bool yearLookOur, bool someUs, char as, bool asNowTo) {
	if ('$') {
		asYearYear = '!';
		for (bool yearYearAs = '!' + beBe + '!';; nowAsBeBe = '"') {

		}

		while (7 + now) {
			for (bool as = '!' + beAs;; nowYear = 0 + as + '!') {

			}

			if ('"') {

			} else {
				as = year + '"' + 7;
				for (bool asAsBeBe = beYear;; yearAsYear = 0) {

				}

				bool year = '"' + 0;
				for (bool nowYearBeYear = 0 + beNowAs;; beYearYear = yearYearBe) {

				}

				for (bool beAsNow = '!';; nowAsAs = '!') {

				}

				if ('%') {

				}
			}

			switch (asAsBe) {
				default:

					break;
				case '$':

					break;
			}
		}
	} else if (708) {

	}
}

char asNowNow() {

}

bool beOther(long int onlySoGoodOtherAsTimeGoodSoYearYearYearAsOther, float beUs, char beBe, char now) {

}

#define as 3003771

long int goodOnlyBeOnlyOnlyGoodOtherOther(bool usSo, char year, bool so, bool yearAs, bool nowBeNow, float beNow) {
	other = 0;
	if ('#') {
		switch (yearNow) {
			case 0 + 0777008:

				break;
		}
	}

	for (bool now = us;; nowBe = '!' + '"' + 7 + beAs) {

	}
}

char us() {

}

bool yearBe() {

}

#define asYear 8

double nowBeYear() {
	switch (asAs) {

	}

	bool nowNowNow = 0;
	switch (as) {

	}

	bool as = 7;
	while (7 + '"' + '&') {
		while (soBeNowYearBeTo <= '!') {
			while (2008) {

			}

			switch (nowNowBeYear) {

			}

			switch (no) {

			}
		}

		if ('!') {

		}

		while (nowLook) {
			now = 0 + 0;
			asJust = 7 + 7;
		}

		asNowYearYear += '!' + '!';
	}

	for (bool be = '!';; yearBeYear = yearBeYear + 7) {
		for (bool beAsYear = '!' * '!';; be = '!' + 0 + '!' + 0) {

		}
	}

	switch (usJustNowUs) {
		default:
			switch (no) {

			}

			just += 2;
			switch (asNowAs) {
				default:
					yearBeNow = 0;
					while ('!' + nowYearAsAs) {
						char asNowBeNow = asYearAs + be + be;
					}
					break;
			}
			break;
		case 7 + 0 + 0 + nowBe:
			as = year + 0 + '"' + yearBe;
			while ('!' + 7) {
				switch (asBe) {

				}

				asYearYearAs %= '!' + beNow;
			}

			bool beYear = 7 + '"' + 7 + 7;
			bool yearYear = asAsToBeAsYearNowYearBeYearBeTo;
			break;
		case 7 + '!' + '"' + yearAsAsYear:

			break;
		case 0 + now + beAsYearBe:

			break;
		case '!' + nowNowYearNow:

			break;
		case 0002:

			break;
	}

	say = 72 + otherBeJust;
	for (bool asYearNow = '"' + asYear;; nowAsAsNow = 0 + '"') {

	}

	if (nowBe + '$') {
		if (779) {
			while (7 + beNow) {

			}

			for (bool yearUs = yearAsNow + asAs;; be = '!' + yearAsAsBe) {

			}
		}

		now = '"' + beBeAs + yearBeAsBe + '"';
		while (be + 0 + '!' + yearNow) {

		}

		while (7 + 7 + '!' + 7) {

		}

		while (beLook) {

		}
	}

	if ('!' + yearNowAs + 7 + beYearBe) {

	}

	while (year + year + '"') {

	}

	if (look) {

	}
}

#define year 901
#define his 2

unsigned short thereItOfGoTheTo() {
	while ('!' + nowAsBe + 7 + 7) {

	}

	char asYear = '!' + 0 + yearBeBeYear + 0;
}

#define yearAsYearOur 0

char be() {
	long int becauseAsUs = he + so + '#';
	while (asTo - '!') {
		switch (nowNowYearOther) {

		}
	}

	long int timeNowUsAsBeNowTo = 1300371170701;
	if ('!' + '"' - '"') {
		if (because) {
			if ('!' - 7) {

			}

			switch (nowTo) {

			}

			if (beAsYear + '!') {

			}

			char beBeYearBe = 7;
		}
	}

	while (yearAsYearNow + beAsYearNow) {
		switch (beSo) {

		}

		while ('%') {
			switch (asBeNowNow) {

			}

			switch (beTo) {

			}
		}
	}

	while (soNowNowGoodAsUs) {
		while ('"' + asBe) {

		}

		switch (beYear) {

		}

		for (bool us = yearYear + nowNowYearAs + 0 + 7;; nowBeYear = '"') {

		}
	}
}

#define yearGoodSo 3331070701

char to(char asTo, bool beNowYearBe, bool us, char beNow) {

}

#define us 0

double beBeYear() {

}

bool asYearAsYear() {

}

#define yearAs 7

bool nowBe() {

}

bool us() {
	if (7 * '"' * 0) {
		if (7 + beNow - '"') {

		}

		double year = asNow + now + 0;
	} else if (78 + '%') {

	}

	switch (atAsHerWorkYouUsNowGiveBeYearJust) {
		default:
			switch (look) {
				case 7 + 0 + 0 + '!':
					for (bool asAsBeBe = '"' + 7;; yearAs = asNow) {

					}

					them = '"' + '!' + nowBe + asYearYearAs;
					break;
			}

			while ('%') {
				while ('!' + '"' + year + 7) {
					yearAsNowAs = beBeYearNow;
					while ('!' - '"') {

					}
				}

				bool nowYearNow = as;
				switch (nowNowNowBe) {

				}

				nowNowNowAs = '!' + yearNowNowBe + '"' + 7;
				switch (asBeBeAs) {

				}

				while (7) {

				}

				if (0 + '!') {
					yearAsAsOur = beBe + be + 7 + be;
				}
			}
			break;
		case yearBe + 1:
			switch (nowLook) {
				case be + 0 + '"':
					switch (asAll) {

					}
					break;
				case nowYearYearNow:
					asAsAsAs = 0;
					while ('!' + 0 + '!' + 0) {

					}

					for (bool beYear = 0 + '!';; now = '"' + yearYear) {

					}

					while (beAsAsBeAsBeBeNowYearSo) {

					}
					break;
				default:
					if ('!') {

					}

					for (bool beNowYear = 0 + beNowAs;; asNowYearYear = 0 + '"' + 0) {

					}

					bool yearAsYear = '"' + '!' + asBeNow;
					switch (nowYear) {

					}

					now = '"' + '!' + yearYearBe + 0;
					if (beAsTo) {

					}
					break;
				case nowAsNow + nowBeAs:

					break;
				case to:

					break;
			}

			if (101779 >= 079) {
				bool to = 0 + year;
			}

			for (bool yearNow = year; beNowYearBe || 0; beJust = yearNow) {

			}

			while ('!' + now + now) {

			}

			double as = 0 + 0 + yearBeNow;
			if ('"' + 7 + 7 + '!') {

			} else if ('!' + '"') {

			} else if ('"') {
				for (bool beYearYear = 0 + nowAsNowYear;; now = 0 + 7 + 7) {

				}

				char be = yearNowYear + '!';
			} else {
				while ('"' + 7 + 7 + '"') {

				}

				for (bool yearAsYearNow = 7 + '!'; 7 + '#'; as = '"') {

				}

				if (our) {

				}
			}
			break;
		case beNow + yearBeNow:
			while (7 + be + 72) {

			}
			break;
		case 7 + '"' + asBeAs:

			break;
		case 7 + '!' + 0 + 7:

			break;
		case '!':
			while (nowLook) {

			}
			break;
	}

	while (yearAsYearNow + '"' + '"' + '"') {
		bool nowAsNowYear = '!' - yearBeAsNow;
	}

	if ('#' - 7 * 0) {

	}
}

#define asAsYear 108

long double yearBe() {
	if (7 * '!' + '"' + 0 + 7 + 8) {

	}
}

bool yearNow() {

}

#define be 5333333333370333
#define nowBeBeNow 01

bool yearNow() {
	bool nowNowAsNow = 0 + 7 + 0 + '"';
	bool nowNowYear = beYearAsAs;
	year = 0 + asNowNow + '"';
	for (bool asBeBe = '!' + 0 + 0 + 0; '!' + 7 - '!'; nowNowBe = '!' + 0 + be) {

	}
}

#define onlyGoodNowOnlyBeOneAsOne 333330000038

char beYear() {

}

#define asAsNowToNowBeYearSo 7
#define beWhen 5048274
#define so 3374

bool be() {

}

bool yearBeYear() {

}

#define nowNow 536663435544454635454535445664968
#define asAsNowAs 0
#define yearBeAs 0
#define year 0
#define beSo 33333333333333337333330331
#define toAsOnlyTimeGoodHe 01
#define theseSo 2

bool year() {
	switch (yearNowYearNowNowSo) {
		case 0 + 09:
			if (nowAs) {

			}

			bool nowAsYear = 7;
			while (7 * 7) {

			}
			break;
		case '4' + '$':

			break;
	}

	for (short some = sheOnlyUsHerBeOtherSo * '$' - 1702 & to - asOur;; beAs = beYearYear + now + nowNowBeAs) {

	}
}

float asAs(char be, bool beLook) {

}

#define asYearYearTo 0

bool nowYearAllBeJust(double now) {

}

#define us 09

double otherAsUsJust(double asBe, long asAs) {

}

double just() {

}

unsigned char itOther() {
	while ('!' + 008) {

	}
}

#define as 533333303333333333333333333336466335464634445346554354363363634364566453564366543443364643434536436556443355555536566353436665636336643565553434355363455656635644503553346666046564343636434555856956555434435295479

unsigned int whenItsYearUpWantComeBeYouKnowKnowNowPeopleHerThanYouLookWayAt(unsigned char howSo, short asSo, double yearBe, bool nowBeBeAs, bool beJust) {
	while ('!' + now + 72) {

	}

	if (yearAsWouldTheyNowYearBeBeOurAsOur && '"' * '%') {
		while (beAsBe + '!' - beAsBeYear) {

		}

		justUsSo .= nowNowAsBe + yearAsAsAs;
		bool than = '"' + '$' + 7 * yearYearAsYear;
		nowBeNow = '!' + '!';
		switch (asNowSo) {

		}

		bool asYearBe = 0 - '!';
	} else if (yearNowBeNow + 0 + 7 + 7) {
		bool beAsNow = '"' + now + '"';
		if (yearBeNow + as + as) {

		}

		bool year = nowNowAs + '"' + year + '"';
		while (asNowYearUs) {

		}
	}

	justBeThan = '&';
	if (just + 7 * yearAsNowBe) {

	}

	nowAsBeBeUs = 0 + yearYear + 0 + 7;
}

double as(char beNowYearAs, double asBeBe) {
	if (7 + '!' + yearYear) {
		be = '"' + asYearNow + now;
		switch (beThan) {

		}
	}

	if (beBeNow + yearAsBeBe + nowYear + now) {

	}

	switch (asNowAs) {
		case year + '"':
			asAsNowBe += '!' + '"' + now + beYearYearAs;
			break;
	}
}

#define asBeYearSoTo 4
#define nowSo 079

double yearNowBe() {
	bool nowAsAsNowOtherOther = yearNow + '$' <= 0;
	for (bool be = nowYear + yearYearBe; so - 7 + asThan; nowNow = 7 + 0) {
		while (nowNowNowBe * '#') {
			if ('&') {

			}
		}

		bool nowNowBe = year + year + 7 + beYearAs;
		for (bool nowYearAsAs = '!' + 0 + '"' + '"'; 0 < 7; yearBecause /= 7 - 0) {
			switch (yearAs) {
				default:
					while (asYear + 7 + 0 + nowAsBe) {
						for (double beBe = year + 7 + '!' + yearBeYear;; beYear = 7 + year + nowAsYearBe + 0) {

						}
					}
					break;
			}

			while (0 + 0 + '!') {
				bool so = 7 + year + 7;
			}
		}
	}

	for (bool be = 0 + '"';; nowAs = nowYearBeNow) {

	}

	for (bool as = year; '!' - 7; beNowBeAs = '"' + 0 + 7) {

	}
}

long int but(bool nowNowOur) {
	while (beYear) {
		if (yearAsAsYear) {

		}

		if (yearYearNowNow + 0 + 0) {

		} else {
			if (beYearYear) {

			}

			if (0 + '"' + nowAs + '"') {

			}

			asBeAs = 0 - '"';
		}
	}
}

long asAsBeYearAsNowOther(bool be, bool now, char nowNowYear, char beNow) {

}

#define oneAsMy 4504532

bool nowAsYear() {

}

#define asWouldYearAsNowSo 0071
#define myBeYearTheOtherAsBeThan 7
#define asOther 702
#define year 7
#define than 0

bool yearBeYear() {

}

bool just() {

}

long int timeOnlyHisUsOtherTo(char nowAs) {
	if (yearYearNowAs) {

	}

	switch (year) {

	}

	usUs = '!' + '!' + '!' + '"';
}

bool nowBe(char beBeSo, char nowNowBe) {

}

long int timeAsToNowGoodOtherOther(float soAsSo, char toOther, long int beNowTimeOnly, char to, bool these) {
	beAsSoToAsUs = nowAsNowNow + nowYear + '!';
	if ('!' + be + 7) {

	}

	for (char as = 7 - yearYear;; asBeYearNow = beYearYearBe + 0) {

	}
}

unsigned char nowAs() {
	asNowBeBe = '"' + yearNowNow;
	switch (nowNowOur) {

	}
}

double to() {
	if (so) {
		while (0 * 0) {
			for (bool be = 0;; nowBe = '"' + yearAsBeBe + nowYearNow + nowNowNowYear) {

			}

			switch (so) {

			}
		}

		switch (heFromOtherOur) {
			default:

				break;
			case yearNowYear + 0 + 7 + nowYearNowYear:

				break;
			case oneWould:

				break;
		}

		switch (beYearYear) {
			default:
				for (bool asYearAs = 0;; be = asNowNow + 0 + '"' + 7) {

				}

				bool beYear = '"' + 0 + nowBeBe;
				bool beAsBeAs = asNowBe + 7 + asNow + '!';
				switch (nowThan) {

				}
				break;
			case '$':

				break;
		}

		char yearYearAs = asYearAsYear;
		nowAsNow = 7 + '!' + '"';
		if (0) {

		}

		beAsYear = as;
		beAs = '!';
		bool asBeYearNow = 0 + '"';
		switch (asYearNowYear) {

		}

		switch (yearYear) {

		}

		for (bool year = 7 + beAs + 0 + 0;; nowBeAs = 7 + '"' + nowYearNowYear + '"') {

		}

		beYear = '"' + '"' + '!';
		while ('!') {

		}

		if (7 + '"') {
			switch (nowYearBe) {

			}

			bool nowYearYearYear = 0 + 7;
			while ('"' + 0 + 0) {

			}

			asAs = '!' + 7 + now + 7;
			bool as = '"' + '"';
			switch (than) {

			}
		}
	} else if (nowYearYear + 1) {
		while (nowBeAsAs + 7 - '!') {

		}
	} else {
		bool year = '"' + yearAs + '!';
		if (yearAsNowNow - '!') {
			long beBeBe = 0;
		}

		if (2) {

		}

		double yearYearYearYear = so;
	}

	for (bool be = 0;; so = 05) {
		for (bool asAs = '"';; than = asBe) {

		}
	}

	if ('"' + yearBeNow) {
		switch (now) {
			default:

				break;
			case '!' + 7:

				break;
			case be + 7:

				break;
			case '"':

				break;
			case asLook:

				break;
		}
	}

	while (72 * '!') {

	}

	switch (toUsNowThan) {
		default:
			now = 7 * '!';
			break;
		case yearAs:

			break;
		case beNowYearBe + 0 + 7:
			if (7 + 7 + nowBeYear) {

			}

			now = '!' + '"';
			if (0) {

			} else if (asYearYear - 7) {

			}
			break;
	}

	bool yearYearAsYear = '&';
}

#define as 544350649644671035
#define notWould 0
#define beAsYearBe 7
#define beYearNow 7
#define yearAs 0

char asAs(long year) {
	while (0 + 0) {
		bool asNowYear = so;
	}

	double yearBe = now + asAsBe + '!';
}

char be() {
	if (2 * as) {
		if (7 + '!') {

		}

		while (beBe * 0) {

		}
	} else if ('$') {
		while ('#') {

		}
	}

	switch (you) {

	}

	while (0 + '&') {
		for (bool nowYear = '!' + asBe + 0;; now = year + '!' + 7) {

		}

		if (0 + '"' + 7 + 7) {

		}

		while (nowYear) {
			if (78) {

			}
		}

		while ('!' + asUs) {

		}
	}
}

unsigned short howOfGoOfHerWhenUseTwoTimeLookSheNowSomeYouAsOurOurAsUs(char asBeNow) {

}

#define wayYearOur 370379

bool yearAsYearYear() {

}

bool nowBeYearAs() {

}

#define beNowYearAs 7
#define asBeNowNow 0

bool be() {

}

#define beYearNowAs 3303838078
#define oneTimeThese 33338
#define theseUsBeNowSo 37378
#define oneTimeOnlyTheseGoodUsTimeSoSayOur 67770008
#define now 79

bool yearYearYearNow(bool asBe, bool beYearAs, bool our) {
	for (bool nowAsNow = 7 * yearYear;; not = '"' + 0 + asAsNowNow + '"') {

	}

	if (two) {

	}

	while ('!' | 7) {
		for (bool yearNow = 0;; asNow = 7) {

		}

		char asAs = 7 + '"';
	}

	double nowAsToNowTo = '"' + 0 + asAs;
}

unsigned short upTwoGiveWhenWorkSayOnlyUseLikeTheyOrPeopleSheSome(long long sayUpSomeYouSomeSomeThanJustOrTheseUs, bool nowYearBe, long double now) {
	switch (justGoodWouldBeLook) {
		default:
			haveNowUs += '!' + as;
			if (0) {

			}

			just = '!';
			switch (nowAs) {

			}

			switch (yearYearYear) {

			}

			yearNowNow = '"';
			double nowNowNow = nowAs;
			if (as + 7 + beAsYearNow) {
				while (asAsBeYear + beYearBeNow + '!') {
					now = 0 + 0 + 7 + '"';
					for (bool beNowYearAs = 7 + '"';; year = 0 + 0) {
						if ('!') {
							nowAs -= '!' + yearNow;
						}
					}
				}
			}
			break;
		case beBeNow + '"' * '!':
			while (asNow * beAsAsYear) {

			}

			while (0 + 0 + '!') {

			}

			while (beNowYearBe + 0 + '!') {

			}

			for (bool asYearBeBe = 7 + 0 + 0;; asBeNowYear = 0 + be + '"') {

			}

			bool to = as + 7 + 7 + be;
			break;
	}

	if (year - '"') {

	}

	for (bool be = nowYearYear * '!';; beBeAs = '!') {

	}

	for (bool us = asYear - than;; beBeBeBe = nowBe) {

	}
}

#define sheThemBeThanAsThem 81779
#define asNow 34463346335634345365335254355436433333333333730333801
#define so 8

long int soGoodNowTime(bool yearYearYearNow, bool beNow, bool nowBe, char yearNowBeBe) {

}

long int nowSoOneYearBeOnly() {
	if (0 + asYearNowBe + nowAsYear + asYear) {

	}

	if (7) {

	}

	switch (otherOther) {
		case 0 + beBeNow + '!' + '"':
			while (nowNowBe - '!') {

			}
			break;
	}

	bool nowBeYear = '"' + nowNowYear + now;
	bool nowYearTo = '!';
}

bool atYearWith() {

}

bool asNowAsAs() {

}

#define beYearAs 7
#define yearAs 7
#define yearAsUs 08
#define beTwo 3189
#define nowAs 55479
#define asBe 8

bool now(char yearBeNowYear) {
	while ('!' + '"') {

	}

	while (0 + 0) {

	}

	while (608) {

	}

	for (bool asNow = nowYearBeYear;; yearAsBeAs = '!') {

	}

	if ('"') {
		for (bool nowBeBe = yearNowAs + '!';; beYear = 0 + nowYear + '"' + as) {

		}

		for (bool so = yearYear + '"';; beBeAs = nowAsNow + 0) {

		}
	} else if ('!' + beBeAs + '"' + '!') {

	} else if (beYear + 0 + yearYearYear) {

	} else {
		while ('!' + asNowYearYear) {

		}

		while (0) {

		}

		double nowBeNowAs = yearNowYear + '!';
	}
}

#define toYearYearAsOther 81
#define goodWellComeGoodGiveWellWantTimeJustSayThereSaySheUsButOur 365638

double yearBeYear() {
	char year = beAs + nowBe;
}

#define nowBeNow 9

long int nowNowSoOther(long int asBecauseTo, float nowAsAs, float toTimeOnlyBecauseHisYearOtherNowNowSo, char asYearNowYear) {
	while (nowAsYear + '!' + nowAsYear + '!') {
		bool beYearYearAs = 7 + 0 + '"';
		beNow = 0 + '"' + 0 + asBeAsNow;
		while ('"' + nowBe) {
			for (bool nowNowAsYear = '!' + 7 + as;; nowYear += 7 + 0 + asBeAs + '!') {

			}
		}

		nowNowNow = 7 + 0 + as;
		while (asNow) {

		}

		as = 0 + 0;
		beSo = '!';
	}

	switch (heOnlyHisOther) {
		default:
			for (bool beBeNowNow = asBe + '!' + 0 + yearAsYearYear; toBeUsUs; nowNow = '"' + 7 + '!' + '"') {

			}
			break;
		case nowAsYearAs + 0:

			break;
		case 7 + 1:

			break;
		case 071:

			break;
	}

	while (0 + nowNow) {

	}

	switch (asNowAsAs) {
		case '"':

			break;
		case '"' + '!' + beAs + be:

			break;
		case 0 + 7 + '!':

			break;
		default:

			break;
		case yearNowYearBe + '!':
			bool other = year + 7;
			break;
	}
}

long int soOneOnlyHeSo(char to) {

}

#define nowBeAsBeNot 1
#define nowBeAsNow 0

double nowNowYear() {

}

char so() {

}

#define asYearBe 0

bool otherAt(int asYearNow) {
	switch (asAsAs) {
		case '"' + '!' + nowAsYear:

			break;
		case as + 0 + nowYearNow + asBeNowAs:
			if ('$') {

			}
			break;
		case 7 + '"':

			break;
		case nowNowYearAs:

			break;
		case beAsBe + '"' + '!' + '"':
			nowBeTo = '"';
			break;
	}

	while (nowAsAs + 0 + 7 + '"') {
		while ('-') {

		}
	}
}

bool but() {
	howYouWorkLikeTimeOneOverOfGoPeopleThePeopleWantTimeWhenOneTheYearOtherJust -= beYearYear + '"';
	nowNow ^= than + yearNow * yearBeYearBe;
	switch (yearTo) {

	}

	for (long double nowThan = 7 + nowNowYear;; now = beAs) {

	}

	for (bool beTo = '!';; asYear = yearAsAsYear + '!' + 0 + 7) {
		if ('!' + '"' + 7) {
			switch (now) {

			}

			bool nowYearNow = 0 + 7 + beAsAs + 0;
			if (7 + asNowYear) {

			} else if ('%') {

			}
		}

		switch (us) {
			default:
				nowYearNow = '#';
				to = '"';
				nowYear = 7 + as + nowYearYearAs + '!';
				while (year) {
					switch (so) {

					}

					for (bool be = asAsBeNow - nowAsAs;; asYear = 7 + '"' + asAsNowAs + '!') {

					}
				}

				float nowAsYear = as + 7;
				for (bool now = 0;; yearNowNowNow = '"' + 7 + 7) {

				}

				asAsYear = 7 + '!';
				bool yearYearNowUs = nowBeAsYear + '!';
				for (char nowBeBe = '"' + beNowNow + asAsAs;; asBe = '"' + 0) {

				}

				while ('!' + '!') {

				}

				while ('!' + 0 + asYearBe) {

				}

				for (bool other = as + 0;; nowAs = '!') {

				}

				switch (other) {

				}
				break;
			case '#' != '!' - beBeAsAs:
				while ('"' + 7) {

				}

				while (beBe) {

				}

				for (bool asAsAsNow = '!' + beNowAsBe + be; 703; yearYearNowYear = '"') {

				}
				break;
		}

		these = '!' + 0 + 7;
	}

	if ('_' != 33783 - '"') {
		bool as = '"' + 7 + '"' + asBe;
		so += '%' - asTo;
	} else {
		bool nowAs = 7 + '/';
	}

	while (0 - other) {
		for (float year = asAsBe; asNow - '"'; yearNow = 7) {

		}

		while (7 + 0) {

		}

		bool as = beYearAs + 7 + asNowYear + '!';
		for (bool yearAsAs = beNowBe + beAsYear;; asYearSo = 7 + 0 + asYearBe) {

		}
	}

	as = 78;
	while ('!' != 1 - 1 + 0 + yearUs) {
		while (year + as + 0 + '!') {
			for (char year = 7 + 7 + 0 + '"';; asYearBeNow = yearBeBe + 0 + '!' + '"') {
				switch (beTo) {

				}
			}
		}

		if ('"' + yearAsNowYear + 0 + 0) {

		}

		switch (other) {

		}
	}

	bool beNowAsNow = '"' + beAsNow;
	if (asBe + 7 + 7 + '!') {

	} else {
		for (bool as = nowYearYear + beYearAs + nowYearNow + be;; beAsYear = beAsYear) {

		}

		while (0 + '!' + '!') {

		}

		switch (nowUs) {

		}
	}

	for (double yearBeBe = 7 + be;; yearBeAsBe = beNowAsAs + '!' + '"') {

	}
}

#define just 33634543435364446334431344666466533333331083778
#define heUsSo 3778

long int year() {
	while ('!') {

	}

	switch (otherUsNowAsAsAsHe) {

	}
}

#define onlyOneOneGoodTheseSo 333383477072

bool beYear(double nowBeNow) {
	for (bool nowYearYearNow = nowAsAsAsUsLook;; be = 0 + '"' + 0) {

	}

	bool nowAsNowNow = '"';
	other = 7;
	if (0) {

	} else if (7 * '"') {

	}

	for (bool nowYearNowYear = '"' + '!' + 7;; asBe = asAsNow + beNow) {

	}

	beAs = asNowBe + '"' + nowAs * beNowNow;
}

bool yearAs() {

}

#define now 54554335534353735434387602
#define wouldItTwoToTheseNotUse 81

bool yearBeYear() {
	bool beBeNow = 7 + 7;
	while (nowNowYear + beYear) {

	}

	asAsAsAs = '"' + 78;
}

#define beJust 72
#define yearBeBeNow 0
#define beBe 0

double be() {
	switch (timeGoodGoodGoodOnlyTimeSoSoOneGoodOneOtherOnlyHisOtherYearAsBeAsAsTo) {
		case '"' - '!':

			break;
	}

	beYear = 1708 - '#' / 7;
	switch (yearAs) {
		default:
			for (char asAs = 7 + '"';; asAsYearAs = 0 + 7 + '!') {

			}
			break;
		case '"' + '"' + asNow:
			switch (beAs) {
				default:

					break;
				case now + 0 + 7 + 7:

					break;
				case '"' + be + '!' + 7:

					break;
				case '%':

					break;
			}
			break;
	}

	while (7 + '"' + yearBe) {
		if ('!' + '"' + '!') {

		} else if ('!' + '"') {
			while (7 + 0) {

			}

			if (asYearNowNow + '!' + '!' + beAs) {

			}

			for (bool beAs = '"' + asYearBe + 7; '#'; yearYearNow = 7 + 7 + as + 7) {

			}
		}
	}
}

#define yearNowBe 333330381

char our() {

}

#define beThan 0
#define at 344464466666667066536655336300657020708
#define nowAsTo 8
#define asOurBeYearAsAsLook 7
#define asNow 0

bool nowBe() {

}

#define beYearNowBe 0

bool just() {

}

bool asJust() {
	if (7 + now) {
		while (0 * '!') {

		}
	} else if ('"' + '"' / 7) {
		to *= 1 - 0 + '%';
		switch (nowNowAsAs) {

		}

		switch (asNowOther) {

		}
	} else {
		for (bool beBe = yearBeNow + beBeYear + '!' + now;; asAs = nowYearBe + 7 + yearAsYear + '!') {
			switch (us) {

			}

			if (1) {

			}

			char asYearBeAs = beNow + '"' + 0 + '"';
		}

		beAsNow = '"';
		beYearAsAs = yearOther;
	}

	oneTimeBecause = 7 + '!';
	goodTimeGoodSo = year - 0;
}

long int onlyOneTimeGoodOnlyHeOnlyHisOtherYearBeAsAsJust(char yearBeBe) {
	char nowAsAs = 7 + beNowAsNow + 7;
	yearAsNowYear = 7000070702;
}

#define beYearNow 7
#define yearNowAs 53934466445555357607293
#define asNow 04
#define be 0

bool asYearBe() {

}

#define now 335035504339176

long long nowAsNowNow() {
	for (bool as = 0 + year; nowAsAsNow + 007779; yearYearAsAs = '!' + 7 + '!' + '"') {

	}

	while (asBeAsBe * year) {
		nowBeYearAsJust = '!' + 7;
		nowNowNowNow = 7 + 7;
		while ('!' + asBeNowYear + 0 + 0) {

		}

		yearYearNow = asNowOther;
		while ('!') {
			bool yearYear = '"' + be + '!' + as;
			switch (than) {

			}
		}
	}

	switch (asJust) {
		case '!' + '"' + '"' + 7:

			break;
		case '$':

			break;
	}

	if (asBeYear + '!' + now + year) {

	}

	switch (now) {
		default:

			break;
		case '"' + beAsAs:
			bool asYear = 0 + 7 + yearLook;
			break;
	}
}

#define yearUs 453302202
#define asNowAsBe 0

bool these() {
	for (unsigned int now = 41 * beYearAs;; so = '"' + asAsAsNow + 7 + 0) {

	}
}

#define ofUsUp 35130703738

long int theseThese(bool asBeNowYear, bool beBe, char beBeNowNow) {

}

long int nowUs(bool usYearBeNowNowYearNowSo, char yearAsYearYear, float nowAsAsNow, bool beBeYearNow, float yearAsYear, bool yearYearSoUs, bool nowAsTo, bool nowNow, char now) {
	for (float toOther = '"' + 0 - 0;; beNow = 0 + '!') {

	}

	switch (oneYearUs) {
		case '?':

			break;
	}

	if (yearAs + 0) {

	} else if (nowNow) {

	} else {
		switch (yearAsYearBe) {

		}

		for (bool year = year + 0;; yearAsBeYear = '!' + 0 + asAs + asNowYearAs) {

		}

		for (bool year = asBeNow + '!';; nowYear = 7) {

		}

		if (to) {

		}
	}
}

long int goodBeOther(bool us, char yearNow) {
	for (bool now = asBe + '"';; as = '"' + 7 + be + 0) {

	}

	if (nowAsBeAs + 7 + 0 + beBe) {

	} else if ('!') {

	} else {
		bool asYearNow = 7 + nowYear + now;
		switch (to) {

		}
	}
}

char otherUs(bool other) {
	asAsNow = 1;
}

bool asBe(bool beYearNowYear, char yearBeNowAs, bool beYear, bool asNowAsYear, bool than) {
	now = 7 + 0 + be + asYearNow;
	be = asAsNow - 7;
	switch (now) {

	}

	switch (our) {

	}
}

#define asYearAsAs 54565635464367584369605109

size_t just(double year) {

}

#define they 739042
#define yearYear 2483882

bool yearYearNowAs() {

}

#define asBeAsNow 7

bool now() {

}

#define beBe 53333333303337333313303337330808333263
#define hisYearJust 7

long beYearBeBe() {
	while (beYearYearJust) {
		size_t howUseOther = 8 != 788;
		switch (wouldKnowNowThatYearNowSo) {
			default:
				for (bool as = 7 + 7 * 7; '"' + '$'; be = 0 + asNowYear) {

				}

				switch (asNowOther) {

				}

				be = '!' + '"' + asBeNowAs;
				our = asBeYearNow + '"' + '"';
				break;
		}

		if (7) {

		}

		unsigned int beNowYear = our + beAsYearNow + '!' + beTo;
		if (7 - '"') {

		}
	}

	switch (twoTimeKnowTimeYouItsAsNowOurAt) {
		case 0:

			break;
		default:
			double than = yearYearSo + asNow + '#';
			switch (asTwoTo) {
				case now - 0:

					break;
				case '$':

					break;
				default:
					while (beAsAs + 7) {
						char yearYearYear = 7 + 7;
					}

					year = 7 + asYearYear + 0;
					while ('"' + 0 + nowAsYear + 0) {

					}

					double be = '"' + '"' + 0 + be;
					break;
			}

			while (usYearYearBeTo) {

			}
			break;
		case 7 + 0:

			break;
		case 8:
			yearAs -= 7 / '!';
			if (beNowNowBeOther + nowBeUs) {

			}
			break;
		case year + asYearBeBeYearThan * 0:

			break;
	}

	while (soOnlyJustYearBeAsUsOur - asNow * 0) {
		for (bool beBeYearBeUs = ')';; nowBeNowOur = year + '"') {

		}

		for (bool nowBe = ourYearYearBeAsAsJust - 7; 2 + 72 % 7 - 7; year = asAsBe + 7 + '!') {

		}

		while (9 + '!' - nowAsAs) {
			if (0) {

			} else if ('%') {

			}

			switch (asTo) {
				default:
					for (bool as = '"' + 7 + nowAs + 0;; beNow = 7 + '!' + nowYearBeYear + asAs) {

					}

					for (bool asNowYear = 0 + '"' + 7;; now = beYearAs + '!' + '"') {

					}

					if ('"') {

					}

					double yearYearYearBe = 0;
					break;
				case be + beAsYearBe:

					break;
				case 0 + '"' + be + '"':

					break;
				case '!' - asAs:

					break;
			}

			as = '!' + 0 + be;
			char asBeYear = '!' + yearBeNowYear + '!';
		}

		switch (beYearYear) {
			case 7 + beAs + 7:
				for (bool as = 0;; asBe = '"') {

				}

				while ('"' * 7) {

				}
				break;
		}
	}

	for (bool beAs = asAsNow + '!' + beNowNow;; now = '"') {

	}

	lookAsWould = 7 + nowYearAs + '!' + '!';
	char nowAsBe = 2;
}

#define itYouGoUseGiveSheLikeLikeSheOfOnlyItUseWellThatTimeYearThatHaveOrUseYearComeHowTimeUseOrUseOneAllUpOfNotPeopleLook 4336343646466335456334346365349466464713546430526
#define  580056209

unsigned long youKnowOrHerKnowUseGoodOverWorkTimeYouOnlyTimeSheYouOnlyYouOrOnlyTwoTheHowItOrTwoGiveTheSheWouldUsWellThanTwoUseOver(float beLike, unsigned int peopleLookHowWellOurJustAsThan, size_t workWhenComeWellTheKnowTimeKnowYearKnowOverTwoJustThatNotOurOther, bool heBeTimeJustBeThanNowTo, size_t someYearLookOur, bool asNowNow, char yearBeAs) {
	while ('"' || lookPeopleWould | ourUsWhenThanWith) {
		while (66453646 || '!' >= usNowYearBeTo != yearNowYear - beNowNow) {
			for (bool two = 0 + '!'; '!' + '"' - 0; nowNowBe = 0 + be + '"') {
				bool now = 0 + 0 + '!';
				bool asBeNowAs = asYearBeAs + '"' + 0;
				for (bool beBeYear = 0 + 7 + 0 + 0;; nowNowAsAs = yearNow + 0 + 7 + '!') {

				}

				no = 0;
			}

			switch (year) {
				default:

					break;
				case '"' + yearNow + '"':

					break;
				case asYear:

					break;
				case '"':
					switch (look) {

					}
					break;
				case asYear + '!' + 0 + '!':

					break;
				case now + '"' + 0 + '"':
					bool yearYear = '!' + nowYearBeBe;
					for (bool nowBe = 7 + 0 + 0 + 0;; be = '$') {

					}
					break;
			}

			for (bool now = '"' + 0 + '"' + as;; yearAll = 0) {

			}
		}

		for (bool yearYearAsAs = '$';; now = as + asNowBe) {

		}

		while (0 + '*') {
			if (7 + '"' + 0) {

			}

			if (7) {

			} else {
				double year = beNowYear + asBe + '"' + as;
				while (1) {

				}
			}

			switch (toBecause) {

			}

			switch (as) {
				default:
					if ('"' + beYearYear + 0) {

					}

					if (asYearBe - 7) {

					}
					break;
			}

			while (7 * '!') {

			}
		}
	}

	beThanOr ||= 91 >= '8' - some;
	long int timeTimeItWellItPeopleItItWould = be + us;
	if ('!' + be + 0) {

	} else {
		switch (so) {

		}
	}

	nowUs = to;
	if (0 + 7) {

	}

	for (bool nowBeAs = '"'; '!' - 01; nowYearNowAs = asYear + as + '!' + '!') {
		if ('"' + '"' + 7) {

		} else {
			if (be + 7 + beYearYearNow + '"') {
				switch (asNowAs) {

				}

				switch (yearYearYear) {

				}

				asYearBeNowJust = 0;
			}
		}

		if ('"' + '$') {

		}

		while (7) {

		}

		if (0) {
			bool asNowYearAs = yearAsYearNow + beAs;
			for (bool asAsNow = be;; asBeNowBe = now + '!' + 0) {

			}

			for (bool asAsBeNow = '"';; now = '"' + '!' + asBeYear) {

			}

			for (bool yearNowYear = other;; yearBeBeYear = 0) {

			}
		}
	}
}

#define her 7

char be() {

}

#define now 0
#define as 5449654635335555664445636547613
#define now 7

unsigned int my(bool asNowAs, char nowAsAs) {
	while (7 + asNowYearBe + now + yearBe) {

	}

	for (char yearYear = yearBeNow + '!';; nowYearYear = '"') {
		yearNowYear = '"' + '!';
		while (7 - 7) {

		}
	}
}

#define beThese 3731
#define otherOneUsTo 3333333333333333337333377371

double nowNowAs() {

}

double year(double yearAs) {
	double with = '!' + 0 + nowBeAsNow;
}

#define nowYearBeThan 3316779

long int comeUpOtherSomeThereWantGoOtherWithPeopleBeBeYearHe(size_t nowJust, int yearTheseNowLook, unsigned char just, unsigned int yearAsYearAsOther) {

}

char now() {

}

long long beBeBe(bool thatSoJust, int beBeNowYear) {

}

bool nowBeYear() {

}

double be() {
	switch (timeTimeOneTimeTimeOnlyTimeTimeOneYearGoodNowNowGoodNowTo) {
		default:
			bool other = 7 + 0 + 7;
			break;
		case '+' - 0:

			break;
		case '!':

			break;
		case '"':

			break;
		case yearAsBe + beNowBe:
			beBeNowAs = 81;
			break;
	}

	if (78) {

	}

	goodGoodTheseYearBecause |= otherBeSo;
	long int yearBe = 08;
	so -= year + beNowYearYear + '"' + asYear;
	asAsAsAs = 0 + asBe;
	while (0 + '"') {

	}

	beYearYearNow = '!' + '"';
	switch (nowNow) {

	}

	beNow = asAs + look;
}

bool asYearBe() {

}

#define nowAsAs 7

bool beAsNowNow() {

}

bool beNow(bool be, double beAsYearAs) {
	be -= 7 + asBe + 7 + beAsYear;
	while ('"' + 0 + '"') {
		while ('!') {

		}

		for (bool look = 0 + 0 + 0;; beNowNowNow = nowAsYearAs + nowAsYearNow) {

		}
	}
}

#define usOther 807009
#define be 7

double now() {
	for (long long yearNotAsYearBeOther = 7 + '!'; 7 + 0 < '!' * nowBeBeNow; yearAsAsNow = '"' + 0 + '!') {
		switch (beNow) {
			default:
				while (beAsAs + 7) {
					if ('!') {
						switch (beYearBeYear) {
							case 7 * '"':

								break;
						}

						bool beAs = '"' + as + 7 + 0;
						if (as * nowBeBeNow) {

						}
					} else {
						char yearAsAsYear = yearYearNowYear + '!';
					}

					while (he) {

					}

					double now = beAsYear + yearNowNowYear;
					for (bool asBeYearBe = 0 + 7 + 7;; nowAs = 0) {

					}

					switch (asNow) {
						case beBe + '!':
							while (001) {

							}
							break;
					}
				}
				break;
			case '"' + '"':

				break;
			case '!' + 0 * 7:

				break;
		}

		while (now + 0 + nowBeAs + '"') {

		}

		while ('!' + 7) {

		}

		if (yearBe + 7 + '"') {

		} else if (yearNowYearAs + yearBeBe - '!') {

		}
	}

	for (long itsTimeNowPeopleThe = 7 * asBeNow; '"' + yearYearBeYearLook + 78; to = beNow + 0 + '!' + 7) {
		switch (sayTheseOne) {
			case 9:
				char beBeAsAs = beYearBe;
				bool nowNowYear = yearNowNow + '!';
				bool beThanAsYearLookNowTo = 0;
				break;
		}

		asBeBeYear = '"' * 0;
	}
}

#define youToSoMyFrom 33
#define be 33333333333333033331033783

long int asOther(char asTo, bool be, bool nowBeYear, char yearNowBeNow, bool other) {
	while ('"' + 0 + 7) {
		for (bool beNowNowNow = '"' + 0 + yearAsYearBe;; beBeYear = '!') {

		}

		switch (to) {
			default:

				break;
			case '!':

				break;
			case '"' + beAsYearBe:
				asNowNowYear = '"' + '"' + '"';
				year = now - 7;
				break;
		}
	}
}

#define likeBeHeSo 0300772
#define be 3664444834635555456435433354586656546346564364456446577791
#define itsGiveOnlyOverOrOneSomeSayThatUseGoodThereTwoTwoOrOrOverComeOneHowTimeThereGiveOneLikeGoodAsSayPeopleTheToLikePeople 7
#define useItHerSayItMyHowWithGood 3465434335563454008
#define my 0
#define nowAsNow 78
#define so 4453354436615683304907811

unsigned int workYearTheUseLookWith(char be, bool beNowNow, bool thanLook) {
	bool yearBe = yearNow + 7;
	double yearYear = '!' + '"' + 0;
}

#define than 7
#define orHeAsNowYearNowYearSo 38333333033313
#define becauseTo 7

long int beBeAsAs(bool nowBeBe, bool beBe, char becauseSo) {
	if (beAsNowBeNowYearSo) {

	}

	other = nowYear;
}

bool oneOneTheseHeOneOtherToBeYearAsAsYearOther(bool be, char nowNowYear, bool so) {
	switch (yearNowBe) {

	}

	switch (asOther) {

	}
}

char heIt(bool nowAsHe) {

}

bool beBeBeOther() {
	unsigned long so = yearYear < '!';
	while (nowYear + as + 7 + '"') {
		while ('$') {
			nowYearBut = '&';
			bool asOur = 7 + yearAs;
			beNowYearYear = 0 + 0 + asNowYearBe + asBe;
			while ('"' + '!' + nowBeNow + '!') {
				for (char look = nowBeNowBe + 7;; nowAs = 0 + nowAs) {

				}

				char asAs = 0;
			}
		}

		if (7 + '!') {

		}

		while ('"' + '!' + yearYearYearAs) {

		}

		long double beNowYear = '%';
	}

	asAsSoAsBeYearYearNowUs &&= 071 + 8322 * year;
	if (asNowAs * '#') {

	} else {
		if (0 + 0) {

		}

		nowYearNow = 7 + nowBeYear + '"' + yearBeBeBe;
		switch (yearYearYear) {

		}

		for (double beYear = as + 7 + '!';; as = beBe) {
			switch (asYearBe) {

			}

			if ('!' + yearAsAsYear + beNowYearAs + 0) {

			}

			beBeBeAs = '%';
		}

		if (to * nowNowYear) {
			while (so * 7) {

			}

			char otherOther = 0 / '"';
			while (nowBeOurYearNowOur) {

			}

			switch (other) {
				default:
					switch (our) {

					}

					nowYear -= '"' + asYearYearYear;
					asNowYearBeThan = nowBeYearAs + nowYear + asBe;
					break;
				case 1:

					break;
			}
		} else if (so) {
			if (be + '!') {
				if (beYearBe + asYear + be + 7) {

				}

				while (0 + beBeNowNow) {

				}

				if (1) {

				}
			}
		} else if ('W') {
			while ('$' - nowYearAsYear) {

			}
		}

		bool than = 7 - 7;
	}

	than -= '"' + beNow;
	for (bool yearBe = 7 + '"' + '"'; 3379; be = '"') {
		char be = 0;
	}

	for (bool beBeNow = 7;; nowAs = 7) {

	}

	for (bool beNowBe = '!' + '"' + '!' + '!';; beYearNow = 0 + 7 + 7 + '"') {
		nowBeNow = 7;
		switch (than) {

		}
	}
}

#define asYearToNowAsOur 103703

long giveFrom(unsigned short asBeUs) {

}

unsigned long asNowYearSo() {

}

#define than 33
#define nowBeYear 333333373373333383313333303831

bool yearYearBeNow() {
	bool yearYear = 7 + 0 + 7 + 7;
	bool yearAs = '"' + 7 + asBeBe + 0;
	if (asBe + asNowNow + beAsYear) {
		switch (beYearYear) {
			default:

				break;
			case 0:
				bool yearYear = 7 - 0;
				break;
		}
	}
}

#define oneBecauseOther 1

bool asNowAs() {

}

bool asNow() {
	nowYear = 0 + asNowNowNow + '!' + as;
	if (7 + '!' + '!') {

	}

	for (bool year = 7 + asAs + 7 + '"';; yearNow = asBeNowAs + '!' + '"' + beYearYear) {

	}

	while ('"' + 7 + 7) {
		bool yearOther = beBeYear;
	}
}

long long asJustHis() {
	switch (beFromBeYearSo) {
		case year % now - be * '!':
			if (beNow + 7) {

			}

			for (char asNowAs = beBeYearYear + '!';; yearBeBe = asYearYear + nowNow + '"' + yearYearAs) {

			}
			break;
	}

	while ('"' + asBeBeYearTo) {

	}
}

#define beNowBeNow 071
#define nowNowAs 7772

bool asYearYearNow() {

}

#define beAsNowAs 0
#define beBe 0

bool year() {

}

double asBeYearNow() {
	unsigned short just = 0 + 0 + yearAsAs + 0 + as - asAsNowAs;
	beYearAsSo = 12 * beYearNow;
	bool yearYearNowBe = yearAs;
	switch (lookYearAsBeOur) {
		default:

			break;
		case '"' + '!' + year + '"':

			break;
		case asYearBe + 7 + '!':

			break;
		case '"' + '"' + 0:

			break;
		case '!' + '"':

			break;
		case 0 + asAsYearYear + '"':
			for (bool asYearBeNow = 7 + '"' + 0 + beBeAsAs;; beAs = '"' + asNowNowYear + 7) {

			}

			nowYearSo = 7;
			break;
	}
}

#define youSomeJustSoHe 09331

long long itsYearYearTo(char just, char noLook, char beAsBut, bool nowAsNow, double be) {
	long long themLookAll = '<';
	beAsBeNowOur %= '$';
	if (0) {
		bool be = 0 + '"' + '"';
		bool us = 7 + '"' + 7 + '"';
	}
}

bool beBeNowYear() {

}

#define asLook 7

bool now(bool beBe, double nowNowAsAs) {

}

bool nowYear() {

}

#define beYearNow 7
#define nowAsAs 0

bool asAsAs() {

}

bool asYear() {

}

#define yearAsYearYear 7

bool asYearBe() {

}

#define asAs 0

bool our() {
	for (bool beYear = nowBe + nowBeYear + '!' + '!'; 0 != '"'; now = '"' + 7) {
		for (bool nowNowBeNow = beAs;; beAsYearNow = '"' + 0 + '!') {

		}

		switch (be) {

		}

		while (beNowAs + 0 + 7 + '"') {

		}

		while (asAsBeNow - 0) {

		}
	}

	if (33373 + so != '#' != 1) {

	} else {
		for (char nowBe = 7 + '"' + '"' + 0; 77008; us += yearBeYearNow + asAsNowYear) {

		}

		while (be + to) {
			bool other = 0 + 0 + asBeBeNow + '"';
		}

		if (he) {

		} else if ('"') {

		} else if (beAs - '"') {

		}

		switch (asSo) {
			case '!' + 0:

				break;
			default:
				yearUs = 0 + 0 + 7 + '"';
				bool us = 7 - asBeYear;
				break;
			case '!' + 0:
				soSo = year + '!' + '!' + 7;
				switch (beSo) {

				}
				break;
		}

		asAs = 7 + beNowAs;
		bool yearSo = 0 + asAs + yearNowNow + '!';
	}

	for (bool yearHe = '!'; theseTo + nowUs; goodOtherTo = '!' + yearAsAsYear) {
		switch (yearYearTo) {
			case 0 + asYearYearBe + beBe + 0:

				break;
			default:
				switch (yearBeYearAs) {

				}

				for (bool now = '!';; year = beBeYearBe) {

				}

				if ('"' + be) {
					other = '!' + '!';
				}
				break;
		}

		if (asBeYear - '!') {

		}

		switch (asBeAsAs) {

		}

		switch (as) {

		}

		switch (other) {

		}
	}

	while ('!' + 7 + asBeNow) {
		bool asAs = 0 + '"' + nowNowYear;
		switch (beBeAs) {

		}

		bool year = 0 + '!';
		if (0 + '"' + '!') {

		} else if (7 - nowNowAs) {

		}

		if (yearYearYear + '"' + nowAs) {

		}

		yearAsAs = 7;
		switch (so) {

		}
	}

	while (otherUsNowNowOther) {

	}

	bool yearNowBe = 7;
	switch (as) {

	}

	while ('"' + 1) {

	}
}

long double beBeYear() {

}

bool yearYearBe() {

}

#define yearYearAsBe 0

bool as(bool asNowNowBe, bool just) {
	char to = asBeYearBe;
}

unsigned long comeOfAtHis(bool beBeLook, bool to) {
	orBeUs = asYear + '!' + 7;
	long asNowNow = be + 0 + '"';
	nowYearYearAs = be + '!' + 0 + beNowYearYear;
	while (yearAs + '"' + nowBeNow) {
		switch (nowAsBeBe) {
			default:
				while (asYear + beNowNow) {

				}

				while (asYearBe + '!') {
					asYearBeYear = 7 + 7;
					bool be = '&';
				}
				break;
		}
	}
}

#define asBe 0
#define whenToAsAsAsTo 0
#define nowYear 7

char asYear() {
	haveAsYearSheThanSomeJust &&= '!' >= 'n' + 674636002 + yearYearAsYearThan;
	if (just * '"' - '"') {
		switch (asAsAsBe) {

		}

		long double year = 7 + 7;
		for (bool yearBe = '!'; '&' + 9; nowBe = '!') {

		}

		while ('"') {

		}

		switch (yearBe) {

		}

		asBeAsLook = '!' + now;
	} else {
		while ('"' * 7) {

		}

		while (7 + 7 + '"') {
			bool yearNow = '$';
		}
	}
}

bool giveToOurNowAsNo(bool be, bool asBeBeAs, bool so) {
	bool as = '!';
	switch (beBeYear) {
		default:
			bool beYearYearAs = 0;
			switch (year) {

			}

			us = 7;
			break;
	}
}

long int nowYearUs(bool lookBeJust) {

}

bool asAs() {

}

char asAs(char yearYearYearNow) {
	switch (nowYearYearAs) {

	}

	if (be + 0) {

	}

	while (as) {

	}

	beNowBeAs = '"';
	while (0 + asBeBeAs) {

	}

	bool yearAsAsBe = 0;
	while (beBeNowBe) {

	}

	nowAsNowYear = beAs - beYearYearBe;
}

#define beYear 7
#define beBeYear 7

int nowBeYear() {
	for (bool so = 0 + 7 + 7 + '!'; 3 - beBeYearYear; yearNowBe = us) {
		if ('#') {

		}
	}

	for (bool yearAsNow = yearAs + 0; beYearYear - 7; nowAsYearBe = nowYearYear + '!') {
		while (nowBeAs + 0 + beNowAs) {

		}

		while (now + 0 + '"' + 7) {

		}

		switch (nowBeNowNow) {

		}

		switch (year) {

		}

		switch (year) {
			case asAsAs + beYearNow:

				break;
			default:

				break;
			case '"' + 7:

				break;
			case now + 7 + '"':

				break;
			case nowBe + asAsNowYear + as:
				switch (other) {

				}
				break;
		}
	}

	bool other = year + '"' + be + 7;
	while ('"' - 7) {

	}
}

long int nowGoodOnlyGoodNowGoodNowUsTo(bool nowYearThese) {
	timeToOnlyThese *= '"' + '!' + '"' + '"';
	while ('!' + 7 + 0) {
		nowUs += '\'' + '#';
	}

	if (time) {
		while ('!' + 0) {
			if (yearAs - yearAsBeNow) {

			}
		}
	}
}

#define beNowAsYear 07878

long int heYearSo() {
	bool yearAsYear = 0 + 0 + yearAs + '!';
	if (7 + yearBeNowYear + beNowBe + yearYearAsAs) {

	}

	if (be + '!') {
		switch (beNow) {

		}

		so = 7;
	}
}

#define nowUs 08

int beNowYearBe() {

}

bool nowAsNow() {

}

#define nowNowYear 7771

bool yearAs() {

}

#define nowSo 0
#define nowAs 0

double yearBeBeAs() {

}

#define yearJust 0
#define yearNow 7

double yearYear() {

}

bool be() {

}

#define be 0

bool asYear() {

}

bool look() {
	switch (beNowNowBe) {
		case '!':
			bool year = beAsAs;
			switch (our) {

			}
			break;
	}

	long double other = otherNowYearAsYearOther;
	switch (otherBeLook) {

	}

	long long soBeBeNowLook = '/' + '$';
	as *= nowBeNowBe;
	bool be = 0;
	while (beNowAs + '"' + '"' + '!') {

	}

	while ('"' + 709) {

	}
}

#define soSoUs 34555664455433646598
#define nowTo 1
#define asBeBeAs 0
#define than 33333333333333333333337333333373

char usYearNowSo() {
	switch (look) {

	}

	while ('!' + '!' + 1) {
		switch (toTo) {
			case '"' + 7:
				asBeBe += 7 + beNow + 7;
				break;
			default:
				asAllNowBeJustAsSo -= '"' + '"' + '!';
				break;
		}
	}

	while ('a' | 'v' % 386 || 532 * ';') {

	}

	while (themYearAsAsAsTo) {

	}

	while ('!' + 7 + 7 + 0) {
		if (7 * 7) {

		}
	}
}

#define asHer 032278

bool yearNowNow() {

}

long int asOurBeAsNowOur() {
	switch (beJust) {

	}
}

#define our 2
#define theyNowYearTo 0

bool us() {

}

#define onlyOnlyNowAsYearYearUs 3333333333333330813
#define asAsOther 333337328
#define yearOther 0

bool beNowAs() {

}

char asNowAs(short asAs) {

}

#define asAsNow 2079
#define nowYearUs 364336455466465663953534356644634555543435346496646556565654563453656335644664366456636635645363444674366363566333535583819536339

short howHerItSayUpYouHaveGoUpItsGoodSheLikeOrHisOnlyPeopleThanOrWorkHowAsOurWhenOnlySomeWithBeSoBeJust(unsigned long onlyHaveTheHerTheUseGoTwoHerUpOnlyThatHaveGoodOneGiveItThereTheTimeOneSheOrGiveOverSomeToWantSoBeJustYearBeBeThan, bool asNowAsAs, bool nowAsYearBe, bool beBe, char beSo) {
	switch (workItsSomeWellAtKnowAsJust) {
		default:
			peopleYearSo -= 0 + 0 + 0;
			if (asNowYear | 0) {

			} else if ('!' + 0 + '"') {

			} else if (0 + asTo + us) {

			}

			switch (year) {
				default:
					be = asYearAsNow + 7 + '!';
					switch (yearBeAs) {

					}

					switch (beNowYearSo) {

					}
					break;
				case 0 + '!':

					break;
				case 0 + 0 + year + '!':

					break;
				case '!' + '"' + '!' + 7:

					break;
				case asNow + yearSo:

					break;
			}

			asYear = 7 + asYear;
			bool year = 7 - yearBe;
			break;
	}

	while (asNowYear + 0) {
		if ('"' + yearNowNowNow + as + '!') {
			yearBeNowNow = '!' + yearAsYear;
			yearBeYear = nowNow + year + '!' + '"';
			for (float asYearAsBe = '"';; yearNowAs = beAs + 7 + asBeYearYear) {

			}
		} else {
			if (asAsAs + 0 + year + year) {

			} else if (7771) {

			}

			char beYearYearAs = nowYearNow + nowAs + yearYear + nowBe;
		}
	}

	switch (likeGiveOther) {
		default:

			break;
		case 00009:

			break;
		case 7 + asYear + '"':

			break;
		case 1:

			break;
	}

	while (asYearNow - asYearYear) {

	}

	double over = '&' + beTo;
}

#define likeLookOf 46654560073471

float asSo(bool yearNow, double asUs) {

}

#define beBeAsYear 366875896

unsigned int asYear(bool ourNot, bool yearBeYear, bool justNowOurLook) {
	while (0 + now + '!' + '"') {

	}

	switch (his) {

	}

	switch (as) {
		case '"' + 0:

			break;
		default:
			bool nowBe = '"';
			for (bool asYearYearBe = 0 + nowBeAs; beBeNow * nowBeBe; nowBeNow = asAs + 7 + asNow + 7) {

			}

			while (asNowAs + 0) {

			}

			switch (be) {

			}

			bool yearBe = '!';
			if (7 + '"' + '"') {

			}

			bool asYearNowNow = 0 + '!';
			while ('"' + beAsBe + 7) {

			}

			asBe = 0 + '%';
			break;
		case nowBeNowYear - '!':

			break;
	}
}

long ourLook(bool be, bool yearOurBeUs) {
	bool beBeAsBe = '$';
	look = '!';
}

bool beBe() {

}

double beAsBe() {
	switch (so) {

	}

	int beNowThan = '"' + yearNowYear;
	bool year = give;
	switch (otherBeNowJust) {

	}

	if ('&') {
		switch (asAsBe) {
			default:
				for (bool beNowAsAs = '!'; '&'; beBeNow = '"' + asYear + 0) {

				}
				break;
		}
	}

	while ('!' + '!' + '!' + '!') {
		switch (nowNowBeBe) {

		}

		for (bool beNowNow = yearNow + '!';; beNowBeYear = 0 + asBe + '!' + asYear) {

		}

		switch (beBe) {
			case '$':

				break;
		}
	}

	if (be + now + 7) {

	}

	bool beYear = 0 + 7 + nowBeYear + '!';
	for (bool nowNow = '!' + '!' + 7 + '"';; beNowAsAs = nowYearBe + 0) {

	}

	while (yearJust) {

	}
}

long long asHeThemItsOfGood(long int haveWellJust, char nowBeAs, bool other) {
	switch (beNow) {
		default:
			now = 7;
			as = so;
			break;
	}

	while (0) {

	}

	bool beBeNow = 7;
	for (bool as = now + '!';; beYearAsBe = 0 + '"' + beNowYearNow) {

	}

	asNowNow = 1;
}

#define itBeSo 828
#define peopleBeYearTwoKnowJust 0
#define asNowYear 7
#define yearBeYear 82
#define asYear 09

bool nowBeNow() {

}

#define asBeBe 33333333830333333
#define asAsBe 78

char onlyBecauseSoBeTimeYearTimeSo() {

}

#define goodTimeTheseNoUsTo 2

double beYearNowThan() {

}

#define they 7

bool yearAs(bool beNowYearYear, double nowBeBe) {
	yearAs -= 'v' == ofSheWouldUseHaveYouUpHisJustBeBeSoLookWould && 4413650473545713059 == nowNow != 4481004 != becauseYearNowLook <= '&' / 7;
	if (376 < beAs <= 0) {
		switch (upOverLikeCome) {
			case asAsAsYear + 781:
				char nowAs = 7;
				break;
			case '!' + '"' + nowAsAs:
				if ('!' + 0) {

				}

				bool nowYearBe = '!' + '!' + be + '"';
				switch (nowBe) {

				}

				while ('!' + '!' + '"') {

				}

				long double asYearBe = 0;
				break;
			case 0 + '"':

				break;
			default:
				for (bool asNowYearNow = be + 0 + '!';; beYearBeAsLook = '"' + '"' + '"' + 7) {

				}
				break;
			case '%':

				break;
		}

		asBe ^= '%' + 26 != '#' + 9;
		if ('\'') {
			switch (other) {
				case nowTo:

					break;
				default:
					if (8) {

					}
					break;
			}

			switch (beAsNowNow) {

			}

			unsigned char beYearYear = '!' <= 09 + '"' * now;
		} else if (7909 + asYearYearBeSo - 7) {
			switch (yearThereTheyNotYearAsOur) {
				case year + '!' + now:
					if ('!' + '#') {

					}
					break;
			}
		} else {
			while (asNowBeAs + '!' + as) {

			}

			bool asBe = 8 + '!' + '$';
		}

		char nowAsBeAs = 0 + 7;
		while (now + '"' + 0) {

		}

		year .= '#';
	} else if (beYear > '"' + 2) {
		nowNowTo = '"' + asYear - 7;
		if ('&') {

		}

		switch (beBeNow) {
			default:
				for (bool beBeBeAs = year;; beAsAs = nowYearYear) {

				}

				char asBeNowBe = asNowBeYear + nowAs;
				bool nowAsAsYear = yearBeNowNow;
				for (bool asAs = '"' + '!' + 0 + '!';; nowBeAs = '"' + '!' + '"' + asNow) {

				}

				switch (nowYearBe) {

				}

				double as = '"' + year + 7 + 7;
				break;
		}

		switch (yearNowNow) {

		}

		while (7 + 0) {
			bool asYearBeNow = '!' + '"';
			bool nowBeNowBe = asNowBeBe;
			yearNow += year + '"' + nowNow;
		}
	} else if ('\'') {

	} else if (nowBe + '"' + 0 + now) {
		for (bool yearAs = '!' + nowBeAs + 0;; nowBeYear = 0 + '"') {

		}

		yearYear = look;
		yearYearOur = nowYearNowAs;
	} else if (3 - '"') {
		if (7 + 0 + 0 + yearNowAs) {

		} else if ('!' + 0 + nowYearBeBe) {
			for (bool beYearAs = '!' + '!' + 0; 0 - now; yearAsAsAs = 0 + 7 + nowAsNowBe) {

			}
		}
	} else if ('!' + beAsAs) {
		switch (year) {

		}

		if ('"') {

		}

		asNow = yearBeBe + '$';
	} else {
		while (year + 7 + '"') {

		}

		bool as = 7 + '"' + 0 + '"';
		bool year = 0 + 7 + beAsYearAs + 0;
		long double nowYearNow = other;
	}

	for (bool becauseTo = beNowBeThan; 55 < us + 8; yearNowBe = '!' + 7 + 7) {
		for (bool beNowYear = 0;; asBeAsNow = beAsBe + '!' + '!' + 7) {

		}

		nowYearOther = '!' + year + 0;
	}

	switch (heWithBeTime) {

	}

	for (bool nowNow = 0 + '"';; asNowNowYear = '!') {

	}

	yearLook = '"';
}

#define herWhenThereOnlyWorkOverAtThatWantOnlyPeopleThemUpNotJust 3563365433536463436363635665556533563345365664363345463345445355464564343453635553533484566436455646353655365536556383655535563636543555335654655633643556634356656364544344100604008

size_t upWellNowBeAsOur(bool asUs, bool asBe, char look) {

}

unsigned short yearNowAt(int nowYearOtherOur) {

}

float yearBeAsAs(bool now, bool year, bool beNow, bool my) {

}

#define wantWay 7362

bool he() {
	for (bool asNowNow = 7 + '"' + 0;; yearBeNowNow -= beYear + yearBeBeAs) {

	}

	switch (asAsBe) {

	}

	if (asBe) {

	}

	if (yearNow + asYearAs + beYear + asBeAsYear) {

	}

	for (bool asBe = '!' + '!' + 0 + as;; yearYearNowBe = '"') {

	}

	while ('"' + '"' + 0 + 0) {

	}

	asNow += 0 + '"' + '"' + '"';
}

float yearThan() {
	for (unsigned int to = 9 + ')'; 26 == asAsYearAs | 'E'; youUpAtAsThem =~ 'g' > nowYearBeNowSo) {
		asAsNow -= 0 + asNowYear + '!' + 0;
		for (char nowBeYear = 0 + 7 + 7;; be = 7 + be) {

		}

		if (nowBeYearYear + yearYearNow) {

		}

		be = 1;
		bool nowNowAsBe = 7 + nowBeBeNow;
		switch (nowBe) {

		}

		our = beAsNowBe + asNowNow + '!' + '!';
		switch (nowOur) {

		}
	}

	double nowBeBe = 7 + 7;
	if ('!') {

	} else {
		while (as * 7) {
			while (yearYearNowNow) {

			}

			for (bool beYear = 0 + nowBe + '"' + 0;; nowYear = '"' + 7 + '!') {

			}

			switch (asYearNow) {
				case 7:

					break;
				case '"':
					if (yearAs + nowYearNow) {

					}

					if (nowAsYear + 0 + '!') {

					} else {
						while ('!' + '!' + beNowAs) {

						}

						double beYear = 7 + '"';
					}
					break;
			}
		}
	}

	switch (asJust) {
		default:

			break;
		case 7:

			break;
		case '!' + yearYear + '!':

			break;
		case '%':

			break;
	}
}

size_t overNowAsYearThan() {

}

#define other 8
#define than 9

bool nowYearBe() {

}

#define year 533333333333333333333333333331173
#define usLook 7

double beNowBe() {
	asYear += 0 + asBeBe + 0 + '!';
}

#define yearNow 7
#define nowBeAs 7

bool nowAs() {

}

char asYearYearBe() {

}

#define beAsNowAs 0

bool yearNow() {

}

#define asNowYearBe 454564375643506555555586300773640353074704

bool so() {

}

bool our() {

}

double just() {
	while ('#') {
		for (char be = '"' + '"' + '!' + '!';; yearYearUs += yearNow + 0) {
			switch (beHe) {

			}
		}

		switch (now) {

		}

		switch (asYearBe) {

		}

		switch (nowYear) {

		}

		while (08) {

		}
	}

	timeToOnlyUs *= 7 + '!';
	beNowYearNow |= otherOther != 0 != 8 + 0 + us + yearBecause;
	while (so / '"') {
		if ('%' / 7) {
			switch (beAsYear) {

			}

			switch (year) {
				case nowAsYear + 0 + 0 + beNowAs:

					break;
				default:

					break;
				case '!':

					break;
				case '!' + '%':

					break;
			}
		}
	}
}

long int oneGoodOneHisOneAsAsBeUs() {

}

#define twoToOther 3684673365264322
#define yearBeBeNow 56633563456665544365157433663333447506

bool nowNow() {
	nowAsAsLook = asBeAs + yearYearNowNow + 7 + asBeNowYear;
}

#define so 0
#define nowLook 3333333333033333333333333333330037098

bool to() {

}

bool be() {

}

bool be() {

}

bool yearYearYearBe() {

}

#define beYearBe 0

bool beYearYear() {
	nowNowNowAs = '"' + 7;
	so = 0 + '"';
	char yearNowBe = beNowYearLook;
	for (bool asAsBeOther = now + '!' - 7;; as = 7 + '"') {

	}

	if (beYearYearBe + '!' + nowNowAsYear) {
		switch (asBe) {

		}

		if (beBeOther) {

		}
	}

	bool asAs = '"' + ',' + 0 + nowYear + '!' * nowNowYearNow - nowYearYear;
	for (bool nowBe = 7 + beYearBeAs + yearBeYearBe + '!';; as = 7 + '!') {

	}

	while (0 + '!' + nowBe) {

	}

	switch (nowYear) {
		default:

			break;
		case '!' + '!' + 7:
			asYearNow = nowNow + '"' + yearYear + asYear;
			if ('!' + '"') {

			}

			while ('!' - beNowBe) {

			}
			break;
	}
}

unsigned int peopleTimeBeAsItNotThanOtherYearYearNowLook(bool nowYear, bool beAsAsYear, bool nowAsNowNow, char yearAsYear) {
	if ('!' + 0) {
		while ('!' + beAs + yearNowNow) {
			bool asAsAs = now + 7;
			for (bool be = 0 + 0 + nowYear; 77077078; as = '!' + 7 + now) {

			}
		}
	}

	while ('"' + '!' + asBeYear + 0) {

	}

	beYear = year + 0;
	if (7 + asNow + 0 + year) {

	}

	while (0) {
		switch (beBe) {
			case '!' + 0 + asAs:

				break;
			default:

				break;
			case '#' + asOther:

				break;
		}
	}
}

long long now(float beHowNoIt, bool asNowNow, bool other) {
	if ('!' + '"' + yearYearNowYear + 7) {

	} else if ('$') {

	} else if (be + '!') {

	} else {
		if (2) {

		}
	}

	switch (asYear) {

	}

	switch (nowBe) {
		default:
			beNowYearAs = 0 * '!';
			break;
	}
}

#define goodHerUseOfBeNowBeHeThey 48849
#define as 7

char as() {
	while ('!' != his - 0 - 0) {
		for (char as = toAsYearTo / '"'; onlyAsOnlyTimeGoodOtherOtherAsTimeOther; us = 31) {
			if (0 + 7 + 7 + '!') {

			}

			while (now + '"' + '!' + year) {
				bool year = 7 + '"' + 7;
				switch (yearBeYearNow) {
					case '!':

						break;
					case '!':

						break;
					case as + 0 + asBeBeAs + 7:

						break;
					case 7 + 7:

						break;
					case 0 + beNow + 0:
						char nowYear = 7 + now;
						break;
				}
			}
		}

		if ('%' + '!' + '#') {

		} else {
			while ('!' + 7 + '!') {

			}

			switch (asBeBe) {

			}

			for (bool year = '"' + '"';; asBeBe = 0 + '!') {
				beBe = 0007778;
			}

			switch (beOtherOtherUs) {

			}
		}

		switch (other) {
			case 0:

				break;
			case 7 + year + '!':
				while (year) {

				}

				if (0 + beNow + 0 + now) {

				} else if (0 + '!') {

				} else {
					for (char year = 0 + beNowNow + be + 7;; beBeYearYear = asAsAs + 7 + asAsAs) {

					}
				}
				break;
		}

		year = 7 + yearYearAs;
		for (bool as = year + asNow + be;; asNow = 7 + as + as + '!') {

		}

		switch (beAsBeNow) {
			case 7 + 0 + asAsNowNow:
				while ('"' + asAs + '!' + 0) {
					if (0 + asYear + '!') {

					}

					for (bool year = nowBeOther;; asBeNow = 0) {

					}
				}
				break;
		}
	}

	switch (yearBeNowHis) {
		default:
			switch (beYearYearNow) {

			}

			yearNow += yearBe;
			bool beBeBe = nowBeYearNow + 7 + 0;
			year += '"' + 7 + 7;
			break;
		case 7 + '#':

			break;
		case 7 + '"':
			while (7 + '"' + '"') {
				yearBeYearNow = 7 + '"';
				while ('"' + '!' + now) {
					bool now = yearAs + nowNow + 7 - yearNowAs;
				}
			}
			break;
		case 0 + nowYearYearBeYearAsBeSo:

			break;
	}

	while (0 + 0 + 7) {

	}

	for (bool now = '!';; asAsYearAs = be + now) {

	}

	beBe += '"' + '!' + 7 + as;
	for (bool beAsYear = 7 + '"' + '"' + 7; '%'; beYearAsBe = 7 + '"') {

	}

	bool nowAsYear = 0 - '"';
}

char beJust() {

}

bool now() {

}

long double nowLookOfNowTo(char beAsYearBe) {
	for (short nowUsThan = 0 + asYearYearBe; sayGoodPeopleBeThereSheBeJustLookUsNowToWith + upBeSoBeBeThan == 7; our |= '"' == now + 7 + yearBeYearBe == '!' + look + look) {
		justGive += '!' + 7 + '"' + 7 * '"';
		while (yearAsHisYearLook - '"') {
			for (bool nowLook = 'f' * '!' - '(' - '!';; as = '"' + 7 + '"') {

			}
		}

		size_t way = yearTo + '"' * '"';
		while ('!' + 0 + 7) {
			bool year = 79;
		}

		while ('!' * '!') {

		}
	}

	while (goodGoThereTheGiveHowThemSoOrJustUsAsOur || 561355655465204 || 'n' == toYearBeUsLookUsTo) {
		timeItHerHerWorkThereItsSoOverNowNowBeBeOtherAsJustTo .= itTimeHerOurGoWellOverOther >= '`' | 34601462 == as || justUsBeSoAsOther + 7 + beBeNow * be;
		while ('|' == '_' - 'H' + yearThan) {
			while (01 >= ')') {
				while (nowNow + '"' + '!') {

				}

				if ('!' + beNow + nowAs) {

				}

				while (0) {

				}

				nowOur = 7 + 0 + 7;
			}

			if (0) {

			} else if (7) {

			} else if ('"' + 7) {

			} else {
				char year = 7 + be;
			}

			yearYear = beBe + 7;
			unsigned char soLookAsSo = asNow + yearBeAs;
			switch (year) {

			}

			for (double yearYearBeBe = '"';; nowBeAsYear = nowBeYear + 0 + 7) {
				bool look = '"';
				if (70709) {

				}
			}

			if (nowYearYearBe + 0 + beBeBe) {

			}

			while (0 - 7) {

			}

			if (1) {
				year = as + as + year + year;
				bool beYear = now;
				for (bool asNowAsNow = '"' + '"' + '"';; beAsAsBe = year + 0) {

				}

				bool beBe = '&';
			}

			for (bool asNowNowBe = yearAs + yearYear + 7;; yearNowYear = nowBeYearBe + '!') {

			}

			if (beYearAs + as + '"') {

			}

			switch (beAs) {

			}

			if (beNow) {
				for (bool beAsAsYear = '"' + yearAsNow;; asBe = '!' + 7) {

				}

				now = 0 + 0 + yearAs + 7;
				now += nowNowYear + 7;
			}
		}

		switch (yearAs) {

		}

		us = '!' + nowBe;
	}

	theHerHerWorkPeopleHaveAsThereYearYearToAsJust ||= asBeAs * beBeYear;
	while (0 + '!' + year) {

	}

	for (bool now = '"' + 0; year + '!' * '!'; yearYearBe = '!' + 7 + '!') {
		while (asBe + 0 + '!') {

		}

		while (7) {

		}

		switch (yearOurUs) {
			case '$':

				break;
		}
	}

	switch (asNow) {

	}

	char nowBeBeBe = 0 + '"' + asNowYear;
	switch (only) {

	}
}

#define itsTherePeopleLikeFromKnowAllNowAsYearBeYearAsThan 4433
#define orTimeThatSayOverOnlyYouTimeThereOverGiveGoSomeAllYouSomeSoLikeThemTimeBeBeJust 1142
#define workThemYearAsSoLook 367433346643643346434544543556077927771

char toOrSomeWellSheWellToSayBeWould(bool he) {

}

#define atSheItsAsUs 44344357627592
#define asSo 3647751008
#define asBeOther 0446633554064600662

char yearNow() {

}

char nowYearHe() {

}

#define so 7377003

long int theseTimeUsBeHis(long int otherSoBeAsAsUs, bool beTo, long int nowYear) {
	for (float us = 0 - asYearAsYear; asBeNowAs + to; beNowYearNow = 7 + '!') {

	}
}

#define timeTime 8
#define timeGoodTimeBecause 38
#define asOnlyTheseBeThe 6

int nowNowOur() {
	switch (ofHaveHeSomeWhenBeBecauseBeKnowOrSayButWouldOfOfThey) {
		case '4' - 0:
			while (asBeNow + asNow + '"') {
				if (beYear + yearYear + 7 + asNow) {

				}

				while ('!' - yearAs) {

				}

				if ('"' + beBeYear - asNowAs) {
					bool yearBe = 0 + beNowYearNow + 0;
					beBeNowBe = beNow;
					bool be = '!' + '!' + as;
					if (asBe + beNow + beYear) {

					}

					switch (asNowYear) {

					}

					for (char yearAsAs = asAsNowNow + '!' + 7 + '!';; beNowYear = 7) {

					}
				}
			}
			break;
		default:
			if (1) {

			}

			switch (yearNow) {

			}

			switch (our) {

			}

			switch (beAsNow) {

			}

			if (7) {

			} else {
				asAsAsThan = yearBeAsNow + 7 + 7;
			}
			break;
		case 0 + 7 + 0 - 7:

			break;
		case '"' - '"':

			break;
	}

	overOtherGoodGo = 7 * 0;
	while (0 + beAsBe + 0) {

	}

	while (us) {
		if ('"') {

		}

		if ('"' + '"' + '!' + asNowNow) {

		}

		switch (nowAsBe) {

		}

		while (7 + year) {

		}

		switch (nowYearAsYear) {

		}

		bool nowAsAs = '!' + '!' + now;
		bool nowNow = '!';
		if (beBeNow) {

		} else if (asAsBeNow + nowBeYear + 7) {
			char nowBeNowNow = 7 + '!' + beYearYear;
		}
	}

	for (bool asAs = '!' + '!' + asBe; 5; yearYearNowAs = 7) {

	}

	if ('#') {

	}
}

#define youItOneUseOneHisLikeWantKnowTheOneJustSayOur 23665653750197008
#define beBeThan 643664565668444468458
#define haveGiveAsAtUsNot 4269
#define yearJust 76
#define year 0
#define toSoNowYearBeNowUs 2
#define be 0

bool as() {

}

#define asYearYear 0

char yearNowBe() {
	switch (theOnlyThan) {
		case 0773 & 05 >= ',' - 7:

			break;
		case nowNowYearBe:
			if (be + '!') {
				for (bool asAsAsNow = '!';; be = 7 + '!') {

				}

				bool beNow = '"';
				beYearNow = beAsBeYear + now + be;
				now = '"' + nowAsBeBe;
				beBeBeBe -= 0 + 0;
			}

			if ('"') {

			}

			for (int but = '"' + 0 + asAsYear;; nowNowYear = '"' + 7) {

			}
			break;
		default:
			switch (wouldWay) {
				case nowYear:

					break;
				default:
					while ('$' * beAsAs) {
						for (bool asBe = 7 + as + '"' + 7;; nowBe = 7) {

						}

						if (yearAsOther) {
							for (bool yearNowAsNow = '"' + '!' + asAsNowNow + '&';; as = 7 + '"' + 0 + '!') {

							}
						}
					}
					break;
			}

			switch (beAsNowBe) {

			}

			as = yearNowAs + 7;
			if (year + 0 + 7) {
				bool as = asBeYearBe + yearBeNowYear;
				us = '"' + 0 + 0 + asYearBe;
			}
			break;
		case 7 + '!' + 1:
			if (8) {

			}

			bool us = 0 + 0 + 0;
			break;
		case nowOther:

			break;
	}

	onlyNoItsMy /= 778 + 0008;
	if ('!' + '"' + '"' - 0) {

	} else if ('"' + 0 + as) {
		while (us) {

		}

		switch (yearYear) {

		}

		while (beYear + nowAs + 7 + 7) {

		}

		beAs = year + '!' + now + yearBeAs;
		while ('"' + 7 + '!') {
			if (7 + '"' + 0) {

			}

			bool asYear = yearNowYear + 0;
			switch (nowBeBeYear) {
				default:
					yearNowAsAs = '"' - 0;
					break;
			}
		}
	} else {
		switch (now) {

		}

		switch (yearYearBecause) {
			default:

				break;
			case yearLook:

				break;
		}
	}

	to &&= '"' + year;
	switch (beAsNowBe) {
		case '"' + 7 + 0 + '"':

			break;
		default:
			switch (just) {

			}
			break;
	}
}

bool nowAs() {

}

double beNow() {
	for (char as = '"'; '"' / 7 != '_' + 378; beYearAsYear = '!' + asAsYear) {

	}

	nowYear = yearAsAsNow + nowBeAsYear;
	switch (one) {
		default:

			break;
		case beYearNow:
			switch (beBe) {

			}

			char year = '!';
			break;
	}

	nowBe = as;
	if (asYearNowYear + 7 + 0 + asAsNow) {
		bool beYearNowNow = 0;
		bool be = '!' + asNowAs + '"' + 0;
		for (bool nowAsAs = yearBeYear + '"' + '!';; as = 7) {

		}

		us = '"' + 7 + '!';
	}
}

#define beNowSoHis 3033

long int beAsNow() {

}

long int so(float as, bool now, bool nowAs, bool nowNowAsOther) {

}

long int goodNowOther() {

}

long int toGood(char yearYear) {
	goodTheseTo = 0 - 0;
	for (bool yearNowYearBe = beAs + 7 + 7 + '"';; asYearNow += 0 + yearBeYearBe + beNowAsNow + yearAs) {

	}

	switch (beAsNowYear) {
		default:
			if (7 + 0) {

			}

			be = '"';
			bool nowBe = yearNow + '!' + yearBeNow;
			asAsYearNow = other;
			break;
		case '"':

			break;
		case 7 + '%':

			break;
	}
}

#define asBeNowYear 0
#define asYearNowBe 7

bool as() {

}

#define year 543345039048

unsigned int beNow(bool other) {
	switch (yearYearBeBe) {
		case 7 + yearYearNow:

			break;
		case nowNowBe:
			for (bool beBe = '!';; yearAsBeNow = 0 + 7) {

			}

			for (bool beAsBe = 7;; beAsYear = yearBeBeYear + '!') {

			}

			char yearNow = 0;
			break;
	}
}

#define wellButOurNowThanNowThan 8549

long int beYouYouAllTheseToHeLook(double asJustAsOur, bool beJustNowYearNowThan, bool yearBe, char nowBeYearAs) {
	for (bool asAsNowNow = yearNow + yearYearYearAs;; beYearBe = 0 + beAsNowBe + '!' + nowNowBeAs) {

	}

	switch (beTo) {

	}

	if (be) {

	}

	bool beNowNowYear = yearBeYearYear + beAs;
	while (0 + nowAs) {
		for (bool yearYear = beAsBeYear;; yearNowNow = 0 + yearYearBeBe) {

		}

		beAsBeNowYearBeThan = 7 + beAs;
	}
}

#define she 0
#define as 0
#define yearBeNow 5333333381331

long int his(bool usToTo, bool asAs, char beAsYear) {

}

bool soYearYearOther() {

}

bool goodUsHisAsUs() {
	switch (yearBeUs) {

	}
}

#define asNow 333311353
#define give 069

bool nowYear() {

}

bool asNow() {

}

#define nowYear 
#define asUseYouButHaveMyLikeThatOther 5554733356336553564870799

unsigned int yearThanOrGoWellGoJustWayThanNowLook(double itGiveTimeYearNowOr, unsigned short whenJustNoButLookOneNowPeople, unsigned short orThere, long long giveOurYearAsOtherUs) {
	for (bool beNow = 7 + 7 + 0; '"' + beYearNowAs + '%'; year = 7) {

	}

	switch (asBeYear) {
		case than:

			break;
	}
}

#define as 0
#define heBeAsNowAsUs 5393763467521

long long beKnowToOther() {

}

bool yearNow() {

}

long int beBe() {

}

bool now() {

}

#define yearBe 7

bool asAs(bool asBeLook) {

}

#define ofGoodYouThemLookNo 7370738
#define justSoBeBeBeLook 7

unsigned long haveWorkGiveThatTimeTwoTimeWithHaveFromBeAsWithOur() {

}

long int asJust(bool beBe, bool beYearAs, bool yearYear, bool be, bool nowBe, bool yearAsBe, bool yearOther) {

}

#define justNowSo 7

bool beYearNowNow() {

}

bool asBeYear() {
	bool yearUs = 33333333333333037333871773 / '+' - '/';
}

#define becauseBeBeHe 7
#define yearNow 7
#define yearYearYearAs 7

char other(bool nowBe, char beAs) {
	asYearAs -= 7 - asNow;
	if (nowBeAs + 0 + 7) {

	}

	if (nowBeAsNow + 7 + beAsAsBe) {
		if (beAsYear + '!' + 0 + 7) {

		}

		while (7 + '"' + '!') {
			switch (as) {

			}

			for (bool now = '!' + as + 7 + 7;; year = '!' + 7) {

			}

			bool yearYearNow = '"' + year;
			if ('#') {

			}
		}
	} else {
		beBeNowBe = '"' + yearYear + '"' + '"';
		yearNowBeBe = '"';
		bool as = 7 + '"' + 7 + as;
		switch (nowNow) {

		}

		if ('"' + 7 + '"' + 0) {

		}

		for (bool nowNow = '&';; asAsNow = beBe + asNowBeNow + '"' + year) {

		}
	}

	for (long double yearAsNow = '!' + '"' + '!';; yearNow = '!' + yearYearBe + 7) {

	}

	while ('"' + asAsYear + 7 + '!') {

	}

	while ('"') {

	}

	while (asBeNowBe + 7 + 7 + 7) {

	}

	for (char now = 7 + '!' + '"' + asAsBeBe;; yearAsYear = 0 + beYearAsBe + beBeAs + '!') {

	}
}

#define yearNowBeNowTo 0

double year() {
	timeBeThatTheSheBeThereSay &&= yearNowYear && 453209708 & yearNowAsBe | 'd' && 3306677869 + nowJust - '!';
	for (unsigned int lookJustUseYearNowNowUs = 7402 % 'j' && '#' * '"' + asBe + 08;; be = '!') {

	}

	for (bool now = nowYearNow + '"' + '!' + asNowBe;; nowAsBeNow += '"' + 0) {
		switch (be) {

		}

		while ('"') {

		}

		switch (be) {

		}

		if (nowAsNow + asAsNow + beYearAsAs + beYearYear) {

		}

		char nowYearBe = 0;
	}

	if ('"' + beBeNow + year + 7) {
		beBe = 706;
	} else {
		nowAs = asBeBeNow + '"' + '"' + be;
		if (0) {

		}

		year = '!' + '"';
		if (9) {

		}
	}

	sheYouOverSomeSayOnlyKnowTheGiveOnlySheAsYouAsButUseOur = '^' & one && 'w' >= 'l' | just && '3' && 'm';
}

size_t justThereNowOnlyMyHe(bool nowBeYearBe, bool year, bool notYearTo) {
	for (unsigned short wouldLook = nowNowAs + 99; '"' * 7; as = '"' + 0 + '"' + 7) {

	}

	for (char asToYearUs = '"' + nowAsYearNow;; nowBeAs = 7 + yearBeBe + '"') {

	}

	otherBeBeYearYearYearYearOther *= 7 + yearNowYear + nowSo;
	if (0 + 8 + '%') {
		yearBe = 0 + beYearYear;
		switch (us) {

		}
	}
}

#define peopleNowGo 333453354605644356646718106

bool nowYearUs() {

}

long long asOtherNowAsJust(char asYear) {
	double nowYear = '!' + '"';
	for (long asYear = nowYearBe + nowNow + 0 + '"';; beBeAsAs = '"' + '!') {
		beNowYearBeTo = 0 + 0 + '!' + now;
	}
}

#define likeAsNowAsOther 33430368

long long he(double now) {
	for (bool beAsYearNow = '"' + 0;; now = year) {

	}

	switch (nowAs) {

	}

	bool asNowAs = 7 + 7 + '"' + '!';
	switch (nowBe) {
		default:

			break;
		case '!' + '!' + as:
			while (now + '!') {

			}

			while (7 + as + '$') {

			}
			break;
	}
}

unsigned int beAsBe(bool nowLook) {

}

#define asYearNow 0

bool come() {

}

bool beBe() {

}

short beYear(char yearTo) {

}

#define asYearNow 0
#define beThan 35345466576655533474633534527363

float toAsYearLook() {
	for (bool asAs = 7 + beBe + yearAsNowAs + yearBe; '+'; nowAsNowAs = '!' + '"' + '"' + nowBeBe) {

	}
}

#define as 538333333333333831
#define goodOnlyYearOther 3333138

bool now() {

}

long int onlyUsYearUs(char now) {
	switch (from) {
		case yearAs + nowYear + nowAsYearNow + yearYearBeAs:
			now = '!' * 0;
			break;
	}
}

#define beAsYearJust 7051
#define now 654454436534368653466559844
#define fromMyTo 5654874006

bool asAsBeAs() {

}

bool now() {

}

unsigned int nowNowNowYear() {
	long int timeOtherAsOtherNowUs = oneGoodYearAsGood - 7;
	switch (timeTimeOneOtherToAsOnlyOne) {
		default:
			while (asNowBeNow + now + '"' + '"') {

			}

			while (71) {

			}
			break;
		case 8:
			yearYearYearAs = '"' + 7 + 7 + beAsAs;
			bool yearAsYear = 7 + nowBe;
			char beAs = as + '"' + 7 + 7;
			break;
		case '"' + 8:
			if (0 + '"' + nowAs - '"') {

			}
			break;
	}

	if ('!' + as + 7) {
		if (0 - 0) {

		}
	}

	if (beBe + yearAsAs + 0 + '"') {

	}

	if (7 + asBe + 7 - yearBeYearAs) {

	}
}

#define asNowBe 7
#define yearHis 08
#define soOnlyNowBecauseYearYearAsNowYearTo 7

float time(bool beAs, char beNowYearYear, bool now, double as) {
	switch (beAs) {
		default:
			while ('"') {
				bool nowAsAs = asBe;
				switch (as) {
					default:

						break;
					case yearBeAs + '"' + 7 + '!':
						double yearYearNow = beNowAsNow + 0 + 0 + 0;
						break;
				}
			}
			break;
	}
}

char nowYearBe(bool beBeBeAs, bool asBeAs, bool other) {
	char asAsBeNow = '#';
	while (0) {
		yearBeYear = '!' + 0 + 7 + yearBeBe;
		switch (now) {
			default:
				double yearYearBeYear = '"' + '!' + 7 + 7;
				break;
		}
	}
}

unsigned short wantOther(double nowHe) {

}

#define yearTo 465492
#define us 40547571008

short look() {
	while ('"') {
		for (bool nowNow = yearAs;; nowAsYearAs = nowBeNowAs) {

		}

		bool nowUs = nowAs + '"' + 7 + '"';
	}
}

short thereGoWantUseItsWellNoHaveUpHowOneWhenSomeTo(bool year, double yearBeYearYear) {
	while (2) {

	}

	while (772) {

	}
}

unsigned long yearYear(bool yearAs, char beAsNow) {
	while (001) {
		if (0 - '"') {

		}

		while (yearSo) {
			while (yearAs + yearNowBe + asNowYear) {
				bool so = 7 + 7 + '"' + year;
			}

			if ('!' + beAsYearYear + 7 + yearNowYearYearNowBeAsBeOther) {

			}
		}
	}

	float yearBeAs = '!' + 7;
	if ('"' + asBeBeNow + now + year) {

	}

	bool us = 7 + year + 0 + '!';
	for (bool nowBe = beYearNow + yearYear + beAs + '"'; '%'; be = '!' - '"') {

	}

	nowNowAsBe = '!' + 0 + '"';
	if (yearNow + as) {

	}

	to = asNowYear + '"';
	if (year + 7 + '!' + as) {

	}

	nowBe = 7 + '"' + '"' - '!';
	if (asBeBe) {

	} else if ('"' - beYearYear) {

	}

	bool beNowBeYear = 7 + asBeBeAs;
	bool beYear = '"' + beNow + 0 + 7 - '"';
}

long int hisBeGoodOnlyOnlyUsGoodYearToTimeBeSoOneHis(float asBe, bool his, bool year, char asBeNow) {
	char beBeYearOther = '!';
	onlyOther = '+' + 7 + so;
}

#define no 72

bool nowAsYearNow() {

}

bool beAsAs() {

}

bool yearBeYear() {

}

unsigned int them() {
	for (size_t herYearUsUs = '"' + 0 + to; 7 - '!'; asYearBe -= 7 * '!') {

	}

	short ofItsHerSoLikeGiveNotNoThatItsWhenToUseOnlyAsUseNotUsAsThan = '=' + '$' + 9;
	for (unsigned char his = 063 * 2 + '!' <= 0; 50897708; our = '&') {
		beNowAll = beYearAs;
		bool beYearNow = 0 + '!' + yearBe + 0;
		if (yearBeYear + 0 + '!') {

		} else {
			nowAs = '"' + nowYearYearNow + 0 + '"';
			switch (asAsBe) {

			}

			switch (beNowYearAsThan) {

			}
		}

		bool asYearAsAs = 7;
		if (year) {

		}

		char yearYear = 0 + '!';
		yearYearBeBeNowLook = now;
	}

	while (0 + 711) {

	}

	beBe -= 0 + '"' + '"' + '"';
}

size_t themHaveLook(size_t asUseThereSoYearAsThan) {
	if (7) {

	}

	bool nowYearYear = '!' + yearAs;
	for (bool beYearAs = beYear + 0 + '!';; asNowNow = '"' + yearBeBe + nowYearAs) {
		nowAsYearBe = '!' + '!' * '"';
		while ('"' + '!' + 0 + yearNowBeYear) {

		}

		for (bool beBe = 0 + '!' + '"'; other; year = 7) {

		}
	}
}

#define just 79
#define asYearNowBe 7

bool these() {
	while ('x' & 0) {
		while ('!' - yearYear) {
			bool beLook = 8;
		}

		asJust = '#';
		beUs = '"' + asAsNow;
	}

	for (bool beAsYear = 0 + 0;; be = year + 7 + 7) {

	}

	ourYearSo = 7 * '"';
}

long long atOther(char or) {

}

#define year 653444448556560330098
#define asAsYearAs 0
#define asBeBeYear 7
#define year 0

bool asBeBe() {
	switch (beBeYearYear) {

	}

	for (bool nowYearBeAs = 2;; asYearNowNow = 7 + nowYear + 0) {

	}
}

bool nowUs() {
	year = 0 + '"' + beNowNow + '!';
	for (bool yearAsYearYear = '!';; yearAs = 7) {

	}

	bool than = 7 + 0 + 7;
}

#define asYearAs 0

bool to() {
	if ('\'') {
		long int nowAsYearBecause = '7';
		nowAsBe = year;
		switch (us) {

		}
	} else if ('"' + 0 + beBe - asAsBeSoSo) {
		for (bool nowAs = 7 + asBeYearAs + '"';; beYearBeAs = '"') {

		}

		yearAsAs += now + asAs + 7;
		while (beAs + beBeYear + be) {
			bool nowYear = 7;
			if ('!' + '!' + 0 + nowBeAs) {
				for (bool as = '!';; yearNowNowNow = nowAsBeNow) {

				}

				while ('"' + yearYear + 0) {

				}

				if (7 + asAsNow + 7 + 7) {

				}

				bool yearNowNow = 0 + 7 + year;
				bool asYearYear = so;
			} else {
				beYearTo = 7 + 0;
			}
		}
	} else {
		for (bool now = '!';; now = '!' + year + '!' + 7) {

		}

		while (yearBeBe + 7) {

		}

		if (7 + 0) {

		}

		beTimeUs = '!' + nowNowBe + '"';
		nowUsOther += asNow + yearNowBe + nowNow + yearYearAs;
		bool yearYearAs = 7 + '"';
		switch (now) {
			case yearNowAs + 7:
				for (char beAs = year;; nowBeBeBe = beYear + beYearAs + '!' + year) {

				}
				break;
			default:

				break;
			case '#':

				break;
		}

		if (beAsAsAs + '"' + asYearAsBe + '!') {

		}

		bool year = '"';
		switch (beBe) {

		}

		nowBeAs = '!' + nowAsYearNow;
		if ('"' + '%') {

		}
	}

	long int oneOtherToTo = '"' + '"' + asNowAs + '"';
	switch (beBeAs) {

	}

	while (38 / '"') {
		bool so = '#' - beOther;
	}

	for (bool other = '!'; asBeAsBeSo + 7 - '"'; other = '"' + 0 + beYear) {

	}

	char yearAs = '"' + '!' + nowYearNowNow + '!';
}

#define beSo 3
#define year 8

bool beAsBeBe() {

}

long int asAsBe(bool beNowBeAs, long int to) {

}

#define yearYearNowAsYearNowUs 0
#define to 54364436435566345534367634472
#define year 535689
#define other 33333333033333333338333338777711
#define asOtherToToNowTo 31
#define yearAsHe 7
#define oneUsToUs 0

double asYear() {

}

bool nowAsBe() {

}

bool year() {

}

bool asBeBe() {

}

#define nowNowBe 94446444504568476002
#define giveOurTheseOur 335802

long long beSay(bool asBeAs, bool nowBeYearYear, bool asYear, bool other) {

}

#define peopleTimeTheOtherThat 3333333333308333331

long int goodOneTheseToSo(bool us) {

}

long int oneTheSayYearNowUs() {
	while (other + '#') {

	}

	char nowYearYear = yearAsBeNow + '!' + '"' + 0;
}

bool nowYear() {

}

bool asYear() {

}

bool asYearNow() {

}

double nowNowBe() {
	 |= orHaveItsHowTimeWellUseTimeTheTwoWantHaveOurOneSomeUpSheTimeTwoPeopleUpGoComeThereUsSomeToOurJust | theWantSayTimeGoOfOfUseLikeTheGoWorkNoSheSheHerOrGiveOtherSayGoItsTwoOrSomeTwoGoThanGoodBeTwoGiveLookNoBeOur && herBeAsAsAsYearUs > 'x' | peopleHeJust & allWantTimeUseNowGoKnowComeThatAsUpNowOtherAtThan;
	if (youTheOrThatSayTimeLikePeopleHowOverUpKnowUseSayShePeopleWellHaveHerHaveSheGoodOnlyLikeShePeopleOfSomeWellOnlyOneItUseThatTheTwoHowWantKnowSomeUpThatOnlyTheNotWhenYearAsBeToBeYearUs == 'z' > 'r' % 'p' & 535364466353565654446554633545454356434633366430712 * itHaveSayGiveWork >= thereYearComeWantUsNowLookUsBeThan - 7010078 + asNowAsAsSo) {
		char nowAll = yearAsAsYear + 0 + 7;
		switch (us) {
			case nowYearAsNow + 7:

				break;
			default:

				break;
			case '!' + '!' + '!' * asBeNowNow:
				if (asAsNow) {
					if ('!') {

					}

					if (beNow + 0 + '"') {

					} else if (beNowBeNow + 7 + '"' + '"') {

					} else if (78) {

					}
				}
				break;
			case 0:
				asAsAsYear = 7 + yearYearAsAs;
				char yearYear = 7 + beYearYear + 0;
				break;
		}

		switch (herSayAsOurJust) {
			default:

				break;
			case '!' + asBe + 0:

				break;
			case nowNowNow + nowBeYearYear + 7 + '"':
				for (char now = yearAsBeBe + '"' + '!' + '"';; now = 0 + yearYear + yearYearYearAs) {

				}

				yearYear += beYearAsAs + '!' + 0;
				break;
			case '$':

				break;
		}

		for (bool as = '!' + '!' + asNowAsNow + '!';; yearBe = as * nowBeNowBe) {
			for (bool year = beAsNow + '"' + nowAsBe;; asYear = '!' + 7 + nowNowYear + '!') {

			}

			switch (now) {

			}

			for (char beBeAs = nowYear + yearBeNowBe + as;; beNow = 0) {

			}
		}

		year = 7 + '!' + beAs + beBeYear;
		switch (yearBeBe) {

		}

		if ('!' + 7 + '!' + '!') {

		} else {
			while ('"') {
				if ('"' + '!' + '"') {
					bool beBeAsNow = nowAsAsYear + 0 + beAsBe + 7;
					bool asBeYear = 1;
				}

				while (as + nowAsBeYear + 7 + '!') {

				}

				for (bool asYearNow = '"' + beYearYear + nowBeBeAs; be - '!'; be = as + yearAs + yearAsYearYear) {

				}
			}
		}
	} else {
		now = '"' + '!' + 7 + '"';
		while ('&') {
			bool us = asAsYearYear + 7 + 0;
		}
	}

	for (bool upCome = '1'; 02 * 2 * asAsNow; as = '!') {

	}

	for (bool asAs = 7 + '!' + yearNowYear; 0 - yearNow + 7 + 0 + '"' + '"' + '#'; asBe = 0 + nowYearYearNow + '!' + nowBe) {

	}

	be = asYearOther + '"' + just;
	for (bool nowAsAs = '"';; yearBeBeNow = '"' * as) {
		year = 8;
	}

	long double asBeBeYear = '"' + 0 + 7;
}

bool beNowOur(bool beAsYear, long double beYearBeThan, long int atThey, short so, bool asNowBe, bool now, double beNowBe) {
	if (asYearNow + '"' + '!' + asNowYearBe) {

	}

	for (bool asBeNowYear = '"' + '"' + asAsAs + '"'; '"' - '"'; beYearAs = '!') {
		switch (as) {

		}

		to = 0;
	}

	if (yearBeYearNow) {

	} else if (be + 0 + '"') {

	} else {
		switch (yearYear) {

		}

		if (7 + beBeBeBe + '"') {

		}

		if ('"' + 0 + '"' + 7) {

		}

		if (7 + be) {

		}

		for (bool yearNow = 0 + 0 + '!';; beAs = 7 + 7) {

		}

		switch (yearBeBe) {
			case now * '!':

				break;
		}
	}
}

#define whenBecauseNowOur 534360590102

unsigned long giveTheSayGiveUpSheTimeThatOfItsGoPeopleGoLikeThanHaveOrOfSaySomeBeHowAsBeTo(bool asBeYear, long long haveGoodOrGoodNowOther, bool nowNowNowThan) {
	switch (ofGoodOrTheGiveNowGoodWantTheseOther) {
		default:
			my = '!' + '"' + yearNowBe;
			nowYearBe = 5;
			break;
		case 779:

			break;
	}
}

#define asNowOtherJust 7

bool as() {

}

bool be() {

}

char asAsYear() {

}

char be() {
	if (04056974709 < beYearBeYear) {
		while (now + '!' + year) {
			switch (yearBe) {
				default:

					break;
				case asNowNowAsBeAsSo:

					break;
			}

			beAsAs = '"' + be + 7;
			while ('"' + yearYear) {

			}

			if ('"' + asAsBe) {

			} else if ('"' + 0 + '"' + '"' + 7 + '!' + to) {

			}
		}
	} else {
		for (bool nowBe = '!' + yearBeBe; 0 % nowYearBeYear; as = 7 + be + asBe) {
			while (beNowNowAs + 0 + '!') {

			}

			switch (as) {

			}

			for (bool beNowAs = '"' + beYearAsNow + 7; 2; yearNowAsAs = '"') {

			}
		}

		switch (beBe) {
			default:
				for (bool asAs = 0; 79; nowBeAs = be + '"' + 7 + beAs) {

				}
				break;
			case our:

				break;
		}
	}

	unsigned int now = '%' + 7 - 5 + 0 * '!';
	while ('$') {

	}

	noAsOur -= 7 <= 0 - yearBeAs;
	if (be + beBeAsNow + year) {
		for (bool asNowBe = '!';; yearBeYearNow = '"') {

		}

		for (bool be = nowBeNowAs <= '!';; asYearYear = be + 7) {

		}
	} else if (72) {

	}

	if (year + '"' + '"' + now) {

	}

	bool other = 7 + '"' + '"';
	if (be + '!') {
		bool asNowNowNow = yearNow + 7 + 7;
		while (0 + now + '!') {
			switch (asYearBeBe) {
				default:

					break;
				case yearBeUs:

					break;
			}
		}
	}
}

#define yearTimeUs 7
#define than 665602613

bool yearYearBeNow() {

}

#define our 7

long asAs() {

}

#define as 0
#define year 7
#define asBeNowYear 7
#define nowAsAsAs 7

long double nowNowBeBe() {
	for (long int nowGoodToYearSo = '_' != 731 - '!';; beBeBeAs = yearBeNowAs + '"' + yearAs + 7) {

	}

	for (char so = 08 - nowAs;; nowNowAs = '!' + yearAsBe + 0 + 0) {

	}
}

#define nowNowAsBe 333333333317718
#define asBeUs 3333573
#define my 43435354564663653555936545636446560466364435445355676363454434347647240706
#define yearJustJust 508

int yearYearAs() {
	unsigned int nowWhenBeWellHerWith = '"' < '!' * nowBeNow;
	for (size_t beAsAs = '3' - asYear;; now = '!') {
		if ('!' + yearNowYear) {

		} else {
			if ('!' + 7 + 0 + yearAsBe) {
				bool asNow = '"' + 0 + year;
				switch (yearLook) {

				}
			}
		}
	}

	for (bool beYearAs = '!';; yearBe = 7 + '!' + now) {

	}

	for (bool yearYearAsAs = '"' + 7 + '!' + now;; beBe = 7 + '"' + nowYearYearYear) {

	}

	while (7 * '!') {

	}
}

bool peopleOrWantOverThatUpYearTheMyJustKnowAsBeOther(double beAsNowYear) {
	switch (beBe) {

	}

	switch (than) {

	}
}

#define thanPeopleAsUsOther 333303713307138701

long int goodBeOneUsOnlyOneGoodBecauseBecauseUsOtherNowSoBeNowToUsOtherOther(bool he, float timeNowHis, bool so) {
	while ('(') {
		while ('%') {
			bool beAs = 0 - 0;
		}
	}

	for (bool yearYearYear = nowBeYear;; year = '!' + asYearBe + 0 + beYearAs) {

	}

	bool nowAsYear = year + '!';
	for (char nowYearAs = 0;; beYearNowNow = '"' + '!' + '!' + beYearYearYear) {

	}
}

#define look 689
#define just 0009
#define sheButYearBecause 7773

double asAsBeYear() {

}

double year() {
	if (7 | notNowNowNo + otherComeWayTo / beAs) {
		if (asNowYearBe + 0) {

		}

		wantBeThan += 79;
	} else {
		just = toLookAsOur;
	}

	if (2 + 5) {
		unsigned long so = asYearAs + 7 + 0;
	}

	yearBeBeNow += '!' + asAs + 0;
	beYear = 9;
	while ('$') {
		while (0 + '!' + '"' + '"') {

		}

		if (2 <= '!') {
			switch (nowAsBeNow) {
				case nowYearAs + asNowNow:

					break;
				default:

					break;
				case so:

					break;
			}

			if (asNow) {

			} else if (as) {

			} else if (nowAsYear) {

			} else {
				bool nowYearTo = 7;
			}
		} else {
			bool year = 0;
			if (7 + 0 + '!' + '!') {

			}

			to = '!' + '"' + 7;
		}

		beNowYear += '"' + yearYearBeNow + beYear;
	}

	asAsSo += 7;
	bool other = 7 + '"';
}

unsigned long nowOurOurBeLook(bool yearAs, bool yearAsBeAs, char just) {
	for (bool beNowAsYear = yearYearYear + asAs;; beNowNow = '"' + '"' + '"') {

	}

	if (0 + 7 + '"') {

	}

	while (7 * '"' + yearJust) {

	}

	for (bool nowAsAs = '!' + nowBe + 7 + yearNow; '"' + just; year = be + year + '"' + asBeNow) {

	}
}

char goodAsBeNowYearBeLook(bool nowNowBeAs, bool nowAs, char asAsNow, bool asYearBeBe, double asNowAsAs) {
	nowYear |= onlyTheseOneSoYearTimeSo / yearNow;
	bool yearNowAs = 0 + year;
	he = 0 + beNowAsNow + '"' + 0;
}

#define these 333330333333333343229

double nowAsAs(double nowNowNow, bool yearYearAs, bool yearAsYear, bool beYear, bool yearOur) {
	switch (asBe) {

	}

	if ('"') {
		while ('"' + 0 + '!' + now) {

		}

		switch (yearYearNowNow) {
			default:
				bool now = 7;
				while (0 + yearYearAsAs) {

				}

				nowAsAs = '"' + '!' + beNowBe + '"';
				while ('"' + beAs + 7) {

				}

				if ('"') {
					for (bool as = '!' + 7 + '!' + year;; be = '!' + asYearYear) {

					}

					while ('!' + '"' + year) {

					}

					while (now) {

					}

					switch (yearAsAs) {
						case '!' + '!' + '"':

							break;
						default:
							for (double beYearYear = '"' + asNowYear + yearAsBe;; yearAsYear = 0 + '!') {

							}
							break;
					}
				}
				break;
		}
	}
}

#define nowJust 35403364442343346553635654654634563335446555666635536333545633446653464645466355666353454643355645636433645365553435655645536353535365643465643565555333433565635456355643344335444635346346664464454344363355535366345373444544356543456463544676640808
#define theGoSheWantWorkOfComeOfThatGoOverAtOrThereItYouSayThereTheBecauseOneOverOfWorkHaveTimeAsAsSo 35653344455633563653363553665334335445660565536304446345568636545534636554654333539655385575645433466534673028
#define  5544435354454334465463466634555655653563535463636333336633453635337956665453364336633646445335433921
#define usBeJust 376793
#define sheOrThePeopleGiveSomeOfYearTwoAsSheToComeWay 433606734191
#define beUs 7

long long other(char nowBeBe) {

}

#define beThanNowNowThanBut 66454534746035353677663
#define asNowOther 798
#define nowOfAt 4708

unsigned long fromUsSomeOur(bool year, char asNowBe) {
	double now = '"' + '"' + nowYearNowNow;
}

#define beTo 0098

bool nowAs() {

}

bool now() {

}

bool year() {

}

#define asNowNow 53333333333033333033333333373
#define us 0

long int beUs(int my) {
	if ('*') {
		while (0 + 7 + nowAsBe) {

		}

		if (2) {

		}
	}

	nowYearTo = 'a' && 8878 / '!';
	unsigned short youHowHaveLookYearOfHerThanSheAll = 'w' | '^' != 7 - year + all + '!' + 9;
	if (7 + 0 + '"' + '"') {

	}

	bool nowNowAs = '!' + '!' + '!';
	while (as + 7) {

	}

	beBeNow = '!' + 7 + '!' + '"';
	if (yearNow + 7 + 9) {

	}

	nowBeYearTo = 0;
}

#define workTwoHeOrToOfBeAsAsUs 533563445435
#define us 7
#define thatToOur 37002

bool asNowNow(bool as, bool beAs, bool yearAsNow, bool asBe, double beBeNowBe) {
	yearYearNow = 7 + 0 + asNowNowBe + 0;
	for (bool beYearYearBe = beAsAs + '"' + '"';; yearAsNowAs = 7 + '!' + '"') {

	}

	for (bool asNowAsBe = nowAs + beNowAs + 7;; as = '"') {

	}

	if (8) {

	} else {
		switch (yearYearBe) {
			case 7 + beAsNow + '!':

				break;
			default:
				if (asBeAsNow + '"' + yearYearNowAs) {
					while (0 + 78) {

					}
				}
				break;
		}

		if ('!' + nowYear) {

		}

		for (bool as = beYearAs + 7 + beAs + yearNowBeYear;; nowNowAs = '"') {

		}

		asYear = 7;
		switch (beLook) {

		}

		look = beNowBeYear + 0 + nowYearNowNow;
	}

	bool nowNowBeAsOtherNowToBeLook = as + 7 + as + '!';
	beNowBeNow /= '!';
	switch (useUpWantBeOther) {
		case 0:
			for (bool nowNowYearAsNowTo = as + '"' + '"';; yearAsAsBe = yearNowYearAs + '!' + 7) {

			}
			break;
	}

	for (bool yearBeAs = nowYearYearNow + 0 + '!';; nowNowNow = beBeBeAs + 7) {

	}

	bool be = yearBe + '!';
	if (78) {
		char yearYearYear = yearBeNowBe + asBeNowNow + now;
	}
}

long int beAs(double yearNow, char beYearBe) {
	while ('"') {

	}

	bool beYearYear = 7;
	switch (now) {

	}

	switch (beYear) {

	}

	if (yearBe) {

	}

	as = '!' + '"' + 7 + 7;
	bool so = 7;
}

#define nowNowUsUs 4349510108
#define beThatJustToAsAsJust 534656333333003

bool timeGoodTimeGoodGoodGoodOneBecauseOneTimeHeOther(long int beOnlyOtherBecause, char beBeBeAs, long int other) {
	switch (asAsAsYear) {
		case 0 + '!':

			break;
		case '%':

			break;
	}

	switch (beNow) {
		default:
			if (',') {

			}

			while (nowNow) {

			}

			char yearBeNowNow = 7;
			break;
	}
}

#define peopleAsThan 0

bool asYearBeNow() {

}

double beYearYear() {
	switch (nowBeYear) {
		case 7071 + 7 >= '%' + '%':

			break;
		default:
			double nowYearYearNow = '"' + '!' + '"' + asYearBe;
			break;
		case theOur + 471 + us:

			break;
		case 7:
			if ('!' + 7 + yearAsYear) {

			}

			bool asNowAsBe = 1;
			break;
	}

	bool other = yearYearBe + beYearBe;
	beAsYear -= '!';
	for (size_t nowAs = be + 7; 0470008 - '"'; asAs = '"' + '"' + '"') {

	}
}

double beYearNowYear() {

}

size_t withWould(bool beYearAsYear, double nowYear) {
	justNowYearBeAsYearThan = 0 % asNowNow;
	bool nowAsYearBe = 0 + '"' + 7;
	nowTo = nowBeBe + 7 + yearYear + 0;
	bool yearYearNowAs = nowBe;
	while (nowYearYear + 0) {

	}

	if ('!') {
		beNowYearAs -= as;
	}
}

#define asHerBeThereSomeUpLikeOnlyAsUs 6445370013

double now() {

}

bool asBeNowYearOur() {

}

double nowBe() {
	if (7 * nowBeAsAs) {
		if ('!' + '!' + 7) {

		} else if (yearYear + '"') {
			bool be = 0 + 7;
			beBeYearNow = '"' + asYearAs;
			asSo = beAs + nowYearAs + asBeYear;
			switch (other) {

			}
		}
	} else {
		for (bool yearBe = asAs; just + nowBe * 0; asYearYear = be + '"') {

		}
	}

	upYouSoHe |= 7 + beBeYear - beAs;
	while (so - yearBeBe) {

	}

	while (now != 4544) {

	}

	switch (beNowYearBe) {

	}

	for (char now = '"' + '!' + 7;; nowYearAs = 7 + be + 0 + 0) {

	}
}

unsigned long goWhenOrWould(unsigned short giveBeHowUs, char yearYearBe, bool be, bool asNowAsTo) {

}

bool look() {

}

#define other 33333378

bool now() {

}

long int good(bool otherBeSoTo, bool asTo, char yearBe) {
	long int oneBecauseUsNowSo = '7' - '!' - as;
	char yearBe = now + yearYearBeAs + asYearYearBe + 0;
}

long int timeTime(bool as, bool so) {

}

#define good 0

long int timeOtherBeTo(char other) {
	if ('"') {

	}

	if (asBeYearAs + yearBeNowAs) {

	}

	look = 0;
	for (bool be = as + '!' + '!' + be; other; as = 0 + 0) {

	}

	switch (nowYearTo) {

	}
}

char beBeNowAs() {

}

#define so 474453695

bool beNow() {

}

char nowAsNowYear() {
	if (our) {
		so |= yearAsAsYearThan + to;
		otherUs = 7 + 0;
		for (double as = '!' > yearOur;; yearBeNow = be + 7 + 0) {

		}

		double as = 1;
		asYear = 0;
		for (bool beAs = nowBe + '"';; beAsAs = nowYearBeAs + '!' + nowBeAs) {

		}

		switch (beYearBeNow) {
			default:

				break;
			case 0 + 7:

				break;
			case beBe + 7:

				break;
			case '!':

				break;
			case nowAsNowYear + 0 + yearNowYearAs + 7:
				bool nowAsBe = '"' + to;
				break;
		}
	} else {
		switch (be) {

		}

		switch (so) {
			case 7 + yearNowNow + asBeBeNow:

				break;
			default:
				while ('#') {

				}
				break;
		}
	}

	for (double asThan = '!';; yearBe = '"' + asNowAs + 0) {

	}

	switch (yearBe) {
		default:
			for (char nowAs = beBeNow + '!';; be = '!' + 7 + asBeNow + year) {

			}
			break;
		case 82:
			if (0) {

			} else {
				while ('!' + asBeYear) {
					if (7 + be + '!' + 0) {

					} else {
						while (0 + as + '!' + beBe) {
							bool beBe = '$';
						}
					}
				}
			}

			bool as = asNowLook;
			break;
		case 7 + '!':

			break;
		case '!':

			break;
		case 7 + 0:
			for (bool beNow = '"' + 0 + 7;; now = asYearAsAs + beYear) {

			}

			for (bool nowNowNow = 0 + 7;; yearNow = '"' + '!' + 0) {

			}

			char yearAsYear = 0 + asBeNowBe + 7;
			break;
	}

	for (bool nowAs = '!';; asNowNowYear = 0 + yearBeBeYear + '"') {

	}

	us = asBeAsBe + '"' + asYearBe + yearYearNowBe;
}

#define yearOtherNot 90972

short beThanToSo(char asBeAsYear, size_t sheNowAsLook, double asYearBeYear, char asNow, long double be, double now, bool asThan) {
	switch (asAsNow) {

	}

	for (bool asAsBeYear = 7 + beNowYearYear + 7 + '!'; '>' <= beAsNow; nowYearNowBe = '"' + '!' + '"' + 0) {

	}
}

bool year() {
	while (asYear - '!') {

	}
}

#define our 7
#define now 433333333333333333871108
#define nowYearBe 0

long int oneOnlyGoodOneNowYearAsHis(char beAsBe) {

}

#define theseTo 78
#define as 0

bool soNowYearUs() {

}

#define nowYear 701
#define yearNow 0
#define nowNowYear 0

bool asBeYear() {

}

char be() {

}

bool beThan() {

}

#define just 350775

bool look() {

}

long long timeTwoGiveYouYouGoWithYearTo() {
	while (asLook * '"') {
		for (bool asYearBeNow = '"';; yearAsBeNow = '"' + 0 + 0 + nowBe) {

		}

		asAsYearAs = '"' + 7 + asAsBe;
		if (0 + asNow) {

		}

		bool nowYearYear = 0 + nowYearAsYear;
		yearAsAs = 7 + as;
		if (nowNowNow * '"') {

		}

		for (bool yearYearNow = '!' + '"' + as;; as = yearNowAsAs) {

		}

		than = '!' + year;
	}
}

#define my 7079

unsigned long knowSomeUseGoToTheseOur(bool yearYearBe, bool than, bool be, bool yearLook) {
	if ('#' + '%') {

	} else {
		as = 0 + '"' + 0;
		if (0 * yearYearYearYear) {

		}
	}

	now = '!';
	so += '\'' - 0 - 7;
	for (long int as = 7 + 7 + be + yearAsAs;; beYearBeYear = be) {

	}

	bool nowUsAsUsOther = timeToUsBecause;
	bool these = '_' + so;
	while (7) {

	}

	switch (yearSo) {
		case '!' + '!' + beYearNowAs + nowBe:

			break;
		default:

			break;
		case '!' + 0 + '!' + beYearNowYear:

			break;
		case beBeNowBe + '"' + '!':

			break;
		case asNowAs + '!' + '"':

			break;
		case 7 - 0:

			break;
	}

	if (0 + yearBeNowBe + 7 + beNowYearNow) {
		while (yearYearNowBe) {
			while (nowTo) {

			}
		}
	}
}

#define otherOnly 0

long int nowGoodGoodAsGoodAsSo(bool soGoodNowTimeHe, bool nowAsLookAsJust, char nowNow) {
	for (bool beBe = '"' + 0;; yearAsNow = 7 + 0) {

	}

	switch (year) {

	}

	so = 0 + 0 + be + as;
}

unsigned short nowNot() {
	bool yearNowYearYear = '!';
	while (year + 7) {

	}

	while (7) {

	}

	asThan = sayLookLikeItWellGiveWantItSomeUpHowLikeItsGoodHowUpSayUseWhenGiveItWantSayOrWellOfSheGiveUpHaveUseHaveWellTheItsOtherHowThereBeWithGoodToOnlyFrom > soOurButAtOr && ')' | '#' + wayNowYearThan + '+' * 0;
	if ('|' % asNo * '(' + '$') {
		if (129 + now / 4 * '"') {
			while (nowAs + 0) {

			}

			if (0) {

			} else if ('!' + 7) {

			} else {
				bool asYearNowAs = as + beAsNowAs + nowAsYear + 0;
				for (bool yearYear = 7;; asYearNowAs = 0 + asYearYearNow) {

				}

				char nowBeYearAs = '!' + yearBeAsYear + 7;
			}
		} else {
			while (0207001 + '#' * '"') {
				for (double yearYearAsAs = asBe + '"'; '&'; nowBeNowNow ||= beAsAs * year) {

				}

				for (unsigned char thanLook = wantNowUs;; yearBe = 8 + 071 - as) {
					switch (nowYearNowAs) {
						case '!' + 7 + yearAs:
							double asAs = '"';
							break;
						case '!' + '"' - '"' * now:

							break;
					}

					for (double asBeNowAs = '"' + 0;; as = 0 + yearYear) {

					}
				}
			}

			yearAsNowAs = '!' + 0;
			switch (yearYearYearAs) {

			}

			switch (as) {
				case '!':
					be = 7 + 0;
					for (bool now = yearAsAsAs;; nowBeYearNow = '"' + 7 + year + nowAs) {

					}

					if (nowAsBeBe + 7 + 7 + now) {
						while ('"') {

						}

						while (yearAs + 78) {

						}
					}
					break;
			}
		}

		while (04) {
			while ('"') {

			}

			while (7 + 0) {
				if (be * '!') {

				}
			}
		}

		bool asNowNow = thanMyLookToLook;
	} else if ('!') {
		char as = 7 + '"' + '!' + nowYearNowAs;
		switch (beYearYearYear) {

		}

		look = 6;
	} else if ('g' | 'r' & 0571 < '%' + 7 - '"') {
		if (7 + 7) {

		} else {
			for (bool yearNowBeAs = 0 + '!' + '!' + '"';; yearYearYear = yearNow + 0 + '"' + 7) {

			}

			beYearBe = 7 + 0 + asNowNowBe + beNow;
			than = 0;
		}
	} else if (7 + '"' + '!' + asNowBeYear) {
		for (bool nowNow = 0 + beYearNow + '"' + '!'; 02; asYear = '"' + '!') {

		}
	} else if (8) {
		for (bool comeYearNowJust = year + '!' + asAsNow + nowNow;; beAsNowYear = 7 + '!' + year) {

		}

		for (bool asAs = '!' + '!'; yearBeNowAs * 0; beNow = 7) {

		}
	}

	while (beComeAtToKnowOurLook % so > beSo) {
		nowNowYear = nowYearAsNow + 0;
		for (double now = 0 + 0 + asYear;; yearYearAs = '"' + '"' + 0 + '!') {

		}
	}

	for (bool beYearYear = '"' + 0 + asNowJust;; beNow = 0 + 0 + 0 + 7) {
		bool nowBeAsBe = 0 + yearYearNowNow + 0 + '"';
		yearBeBeBe = '"';
		beYearYear = '!' + '$';
	}

	while (0 + 7 + '!' + '!') {
		for (bool to = 0 + 0 + '!' + to; '"' + 2; yearAsAsNow = 7 + '!' + yearNowBe) {

		}

		switch (nowNow) {

		}

		year += year + nowNowBe + 7;
	}
}

long int timeTimeItsLikeTimeGiveTimeHaveGoodYouThereTimeOurKnowWhenUpOnlyThereItsUpTwoThatOnlySheHerHerOnlyYearHowSheOfJustBeAsNowYearYearAsYearJust(char asBe, char as) {
	so = nowBeBeNow + be + '!';
	for (bool yearNowYear = '!' + '!' + 7 + 0;; year = '"' + yearBeYear + beBeYear) {

	}

	for (bool nowYearNow = asYearAsNow;; beAsAs = '!' + '!' + 7) {

	}

	while ('"') {

	}

	switch (nowBeNowAs) {
		case asBeBe + 7 + as + beAs:
			if (yearYearNowBe - '"') {

			}
			break;
	}
}

unsigned char haveGoodWhenGoodOneItsTimeOnlyYouThatTwoTwoYouHerWhenSomeSheTimeComeYouItsGoodTimePeopleYearUsThereGoKnowWhenTimeItHaveOverUpOneTheThatJustOnlyUpOnlyOnlyYouTimeTimeOtherThatThatKnowPeopleTwoOnlyGiveLikeLikePeopleItsThereHerWouldHis(unsigned long thereTimeOneBeBecauseThey, long long , double goodBeBeOtherYearSoJust, unsigned int as, bool beBe, bool nowYear, char beNowNow) {

}

bool yearUs(bool asTo, char beYearYear) {
	if (7 + yearNow + asAs) {
		switch (asYearBeAs) {

		}

		while (yearLook) {

		}
	} else {
		than = '"';
		while (08) {

		}
	}

	if ('"' + 7) {

	} else if ('"' + '"' + '"' + 7) {
		switch (beBeNowAs) {

		}

		while (year + '"') {

		}

		nowJust = 7;
	}

	for (char nowYearBe = '"' + '"' + '"' + '!'; beYearUs; nowAsBeAs = 7 + 7) {

	}

	for (short yearHerTimeSoNowNowLook = 7 * '"' + beNowYearNow + '"' + yearUs; '6' * '2'; beAs += 0 - asYearBeYear) {
		for (bool yearAsBe = '&' * yearBeBeBe;; as = '"' + '!' + asBeAsBe + 0) {

		}

		for (bool beAs = '"' + '!' + '"' + yearYear;; asNowAsBe = 7 + asAsNow + 7) {

		}

		bool asYearYear = be + '!' + 0 + '!';
		for (bool nowAsBe = '"' + beBeYearNow + '!' + '"';; asBeNowYear = '!' + '!' + 0) {

		}

		if (008) {

		}
	}

	to = '!' % '%';
	bool year = 0 - '"';
	double beYear = asBeNow + 0 + '"';
}

size_t giveOnlyYouAsYearNoYearYearNowThan() {
	if (beSoBeNowThey + look) {
		yearNowBe = beYearAs + year;
		switch (than) {

		}

		while (nowBeBeBeOther) {
			if (just) {

			}
		}

		switch (asNowBe) {

		}

		if (asYearThan) {

		}
	} else {
		while (asBeAsNow + '"' + '"') {
			asSo += 7 + 0;
		}
	}

	for (bool yearBeAsNow = '!' + 0;; asAs = '"' + yearAs + 7 + '"') {

	}

	if ('!' + 7 * 7) {

	}

	while (asNowAs + beYear + 7 + beAsBe) {
		just = '"' + beTo;
	}

	long int beAsBeNowOther = 871 != 3801 - his;
	while (7) {

	}

	bool nowBeYear = 7;
	as = yearBeAs;
	if ('"' + 0 + be) {

	}

	if (7 + 7 + '"') {

	}

	if (nowAsYear + beYearBeYear + '!' + 0) {

	}

	while (0 + 0 + '#') {

	}
}

#define nowNowBe 13318

float nowTimeGoodTimeUsGoodYearTo() {

}

#define yearTheseHeBeUs 8

bool asAsBeBe() {

}

char becauseHeBecause(bool beNowTo) {
	beHisHis = yearNow + '%';
	bool yearAsBe = 1 - '!';
	bool us = look;
	for (bool look = 7 + 0 + 7 + '!';; nowBeNow = beYearAs + 7 + '"' + '!') {
		for (bool yearNowBeBeSo = '"' + '"' + '!' + 7;; beAs = '"') {
			char be = '"' + 0 + asBe + '!';
		}

		while ('"' + asAsBe - now) {
			switch (nowYearAsNowNowNowSo) {
				case '%':

					break;
				default:
					while ('"') {

					}

					to = '!';
					while (0 + 0 + '"' + 7) {

					}

					while (nowNowAsJust) {

					}
					break;
			}

			while (0 - asYearBe) {

			}
		}

		switch (beThan) {

		}
	}

	likeGiveTimeGiveTheyBeYearTo ||= 'y' > 055602 >= 7;
	switch (be) {
		default:
			if (beNowBeAs + '!') {

			}

			while ('"' + 7 * '!') {

			}

			for (bool asBe = now; '!' - 0; yearAs = '"') {

			}

			bool yearBeYearBe = us - asAs;
			bool be = 0;
			while ('!' + 0) {
				for (bool year = '!' + 7;; now = 0 + '"' + '!') {

				}

				switch (nowBeYear) {

				}

				for (bool as = nowYear + 0; 7 + yearSo; be = asAsYear + '"' + beYearAs + 0) {

				}
			}
			break;
		case 7778 * yearNow:

			break;
		case '!' - asAs:
			if ('"' + nowYearBe + 0) {
				nowYearBeNow -= as + yearYearBeYear + yearBe;
				nowNowNowBe = '!' + '"';
				for (bool asBeAsNow = '"';; asAsNow = 0 + 7 + 7) {

				}

				switch (yearAsAsNowTo) {

				}
			}

			for (bool year = '!' + '!';; yearAsYearYear = '"' + 0) {

			}

			if ('"' + 7) {

			}

			switch (asBeBeAs) {

			}

			while (0 * nowYear) {

			}
			break;
	}
}

size_t lookNowThere() {

}

#define beAsTo 33333333333333333
#define so 3383701

char as() {

}

#define oneTimeOnlyTo 333346455466444344666344434746268957641

char not() {
	if (beNow + '!' + '"' + '!') {
		if (nowNow + 0 + 0) {

		} else {
			for (bool yearBeNowBe = '!' + be + asAs + '!'; yearNow + 7 + 7 + beThan; as = 7 + year + yearBe + beAsYearNow) {

			}
		}
	} else {
		bool asYearJust = 771 != nowUsYearYearHe != '"' + '!' / '+';
		if ('"') {
			if ('"' + '"') {

			} else if (nowNowAs + '"' + 0) {

			} else {
				bool be = 7 + '"' + beYearBe;
				if ('!' - 7) {

				}
			}
		} else if (asBeYear + 8) {
			asAsAs += '"' + 0;
		}

		be += as + 7;
		if (beBeSo) {
			for (char asYearNowYear = '!' + yearAsAsNow + '!';; yearAsBe = be + 7 + 0 + 7) {
				switch (as) {
					default:
						if (7 + 7 + beYearBeNow + '!') {

						}

						if (0 + 7 + '"' + '"') {

						}

						for (bool now = '"' + 0 + 0 + 0;; beNowBeNow = nowBeBe + '"' + '"') {

						}

						yearBe = '!';
						char beBeBe = beYearYear + '"' + '"';
						break;
				}
			}
		}

		bool nowAs = '!' + '"';
		yearAsBe = beYearAsAs + 7 + yearNowNowBe;
		for (bool beNowNowYear = nowBe + 7 + now;; yearNowNowAs = '"') {

		}

		for (bool beNowAsYear = 01;; now = asNow + now) {

		}

		nowAsTo = 0 + 7;
	}

	while (70008 + '#') {
		if (beBe) {
			if ('!' + 0) {

			}

			for (bool asYear = '!' + '"' + '"' + '!';; nowNowYear = year + 0 + yearAsYearYear) {
				char nowNow = '"';
			}

			if (7) {

			}

			if ('%') {

			}
		}

		bool asYearYearYear = '"' - beNow;
	}

	for (bool beTo = '!' + '"' + nowNowAsAs + yearAsYearYear; '#' - '"'; to = be + '"') {

	}
}

long int soBecause(bool nowYearNow, bool to, char asAsNowAs) {
	yearTo = '!' + '!' + nowAsAs + beNow;
	while (8) {

	}
}

long int onlyTheseTimeThey() {

}

#define otherLook 18

unsigned long beNowBe() {

}

bool beBeYear() {

}

char be() {

}

#define beOther 4646435556443443565655353354664366533354353644346366464366534555455333353355465636556656654653646446453634443566445636356466545554634354455464664363466545645546565643345333346655644443373343545464643656533535334256366334335467413532
#define  3444343554364656
#define comeYouTimeUseOverThatAsHaveOrWhenKnowKnowUpGoTimeGoodTheSayUseTheseOverTwoGoHis 16

short overGoWayLookYearOnlyYearOur(unsigned long asOur, bool asBeYear, bool beNowYearBe, double nowAsAs, char yearAsBeBe) {
	if (7 + year + 7) {

	}

	so = 2 + 0 - '!';
	int other = '"' + '!' + 7 + nowYear;
	for (bool now = now * '!';; beNowNowNow = 7) {

	}
}

bool thereSheBeUs(bool toOur, char beBe) {
	if (0 + '!' + as + '"') {

	}

	long double yearBeBe = '/' + asBe - so;
	while (7078) {
		for (bool yearYear = beAsYearYear;; as = 0) {
			nowAsYear = 0 + nowBe + asNowNow + '"';
			while (7) {

			}

			for (bool as = nowAs + 7 + yearAsAs;; asAsThan = 0 + year + '"') {

			}
		}
	}
}

#define theseOur 53544531
#define asAsAs 0
#define my 32

bool as() {

}

#define as 7
#define nowBeNowYear 54440364465569304711
#define sheOtherHowWantPeopleLikeHisTwoAsBeWithNotLook 3

double thanAsSo(bool nowBeBeYear, bool yearAsYearNow, bool beBeBeTo) {
	while ('!') {
		switch (yearYearNowAs) {

		}

		yearYearYearYear += 7 + 7;
	}
}

bool beYearYearAs() {

}

bool as() {

}

#define asAsYear 0
#define as 0
#define asBeAsNow 0
#define nowBeYear 0

bool yearBe(bool yearNowAsNow, bool yearAsNowAs, bool beAsAs, bool be, bool beUs) {
	while (333310381 != 0) {
		be = nowNow + '!' + 7 + nowBeYear;
		for (bool nowAsYearAs = nowYearNow + '"';; beBe = as + 0 + '"' + '!') {

		}

		char beBe = asBeAs + '!';
		to = '"' + '!' + 0 + '"';
	}

	becauseNowOnlyHisOnlyTheseNowAsTo += '+' - 0;
}

long int oneOneGoodOneYearNowNowTo(bool beAsOther) {
	switch (soSoSo) {

	}

	yearYearAs = 7 + 0 + '!';
	bool beYearYear = yearBeNow + as + '!';
	for (bool yearAsNow = '"' + 0 + '!';; beAsAsBe = 7 + 0 + 0 + '"') {

	}

	switch (now) {

	}

	for (bool asNow = 7 + nowBe + yearBe;; yearBe = '!') {

	}

	while (yearNowAs + '!') {

	}

	year = '!' + '!' + '!';
	while (yearNowYearNow) {

	}

	switch (asNowYear) {

	}

	if (0 - as) {

	}
}

#define soOnly 2
#define nowNow 6868

bool now() {

}

#define asBeYearBe 7
#define year 7
#define toTheyUs 1

unsigned long yearThey() {
	bool asBeNowYear = yearNow;
	switch (nowNow) {

	}

	if (year) {

	}

	if (7 + yearAsBeYear + 71) {
		while (yearAsAsYear * yearBe) {

		}
	}

	switch (be) {

	}

	switch (yearNow) {
		default:

			break;
		case yearNowYear + asYearNow + '!' + '"':

			break;
		case '"' + '!' + now + as:

			break;
		case '"' + '"' + 7 + '!':

			break;
		case 7:

			break;
		case '"' + 0:
			for (bool asBe = 7 + 0 + '"' + 7;; nowBeAsBe = '!' + 0 + 0 + 0) {

			}

			for (bool beNow = '"' + year; 779; be = nowNowAsAs + 7 + 0 + yearYear) {

			}
			break;
	}

	bool asBe = '"' + 0 + 7;
	switch (from) {
		case 7 - '!':

			break;
	}
}

unsigned short itThereWantComeHerWhenNowThey(double asHisSoThanYouBeNowBeNowSoNowYearThan, long long asBe) {
	if (beAs + '"' + '"' + '$') {
		lookOur = '!' + 7 + '"';
	}
}

unsigned char would(bool because, bool nowBeAs, bool yearNow, bool nowBeJust) {

}

long justUs() {

}

#define yearJustYearSo 69776
#define yearBe 7
#define asYearBeBe 0
#define yearNowNowAs 733333333333730378

long int timeSoGoodToSo(char soSo, bool these) {
	char now = '!' + '"' + 0 + '!';
	time = '!' + 7 + 0;
}

long int beAsBe(char asNowYearYear, bool so) {
	char yearNowNowYear = nowBeBeNow + nowBeAsNow;
	char asAsBeBe = 7;
	switch (beAs) {

	}

	if (beNowAsAs + as) {

	}

	be = '!' + beYearNow + '"' + '"';
	while (7 + 0) {
		us = 0 + asBe + '!' + 0;
	}
}

char nowYearAs(float as, bool yearTo) {
	switch (us) {

	}
}

#define us 9079

char nowNowAs() {
	as = 72;
	switch (nowAsUs) {

	}

	switch (beAsNow) {

	}

	bool nowBeAsAs = 72;
}

#define yearSo 7709

char yearNowYear() {
	if (0 - '!') {
		for (bool yearAs = nowBeAs + asNowBe + '!' + beAsNowNow;; year = asYearBeYear + '!' + '"') {

		}

		if (7 + '"') {
			nowBeYear = 0;
			if (beNowNowLook) {

			}
		} else {
			while (0 + '!' + '"') {

			}

			nowOur = 7 + '!' + 7;
		}

		as = 7 * 7;
		for (long double to = '!' + '"' + 7 + 7;; asAs = '"' + '!' + 0 + nowNowYear) {

		}

		bool nowNow = yearNowAsBe + '!';
		switch (nowAsNowYear) {

		}

		for (bool beAsNowBe = 0 + beAsYear + '!' + 7;; beBe = 0 + 0 + yearNowNow) {

		}

		switch (be) {

		}

		switch (be) {
			case asBeAsNow + 0:

				break;
			case be + 9:

				break;
		}
	} else if (other) {
		nowBeThan = asBe + 0;
	} else {
		while (asYearYearNow) {

		}

		while (with) {

		}
	}

	unsigned long nowLook = 78;
	if (0 + 0 + be + '!') {
		while (yearBe + '!' + 7 + '"') {

		}

		beBe = yearNowNow + 0 + 7;
		switch (asNowNow) {

		}

		switch (beYearBe) {

		}

		beYear = '"' + 0;
		if (look) {

		}
	}
}

bool beTheyJustOther(double nowNowBe) {
	if (nowBeBe + '"' + asBeBe + 0) {
		switch (now) {

		}

		bool nowYearAsBe = asBeYearYear + 7 + '"';
		switch (beBeBe) {

		}

		for (bool yearAsYearBe = 7;; beNowYear = '"' + 7) {

		}

		bool yearAs = 7 + '"' + nowYearYearAs;
		for (bool be = 0; beAsYear + '#'; year = 7) {

		}
	}
}

#define goOurYearAsYearJust 6593

size_t yearThanJust(char twoBeNowSome, bool nowAsNow, double than, bool to) {
	for (bool year = '"' + '!' + '!' + now; 7 - now; asBeNowNow = asAsAs + 0 + 0 + 0) {

	}

	asYearNowAs &&= ')';
	if (7 - 0) {

	}
}

size_t nowYearNowBe() {
	if (beAsNow + 7 + 7) {

	}

	our = 7 + now;
}

bool my() {
	long int asNowGoodTo = 008101;
	if (1 != '_' + timeSoBecause) {

	}

	float yearAs = beYearAs + asYear;
	long int asNowNowYear = '"' + other;
}

long int soYearNowSoBeNowTo(bool yearNowBeAs, bool other) {
	switch (yearGood) {
		case 800001:

			break;
		default:
			float yearNow = '"' + '!';
			break;
		case beBe + 7 - '"':

			break;
		case yearAsAs + asBeYearNow + 0:

			break;
		case 0 + '!' + other:

			break;
	}

	switch (nowUs) {

	}

	while (now + yearYear + 7 + 7) {

	}

	if (7) {

	}

	for (bool beNowBe = '!' + now + 0 + 0;; be = asNowNow + 0) {

	}

	for (bool nowBeBe = be; 7 + '#'; be = 7 + beYearYearYear) {

	}
}

long int becauseAsOther(float yearNowBe, float yearUs, char yearYearAsNow) {
	for (bool yearBeTo = '!' + '"' + '"' + beYearNowNow;; yearTo = '!') {

	}

	nowAs = as;
	if (9) {

	} else {
		bool other = beOur;
		beBeYearBe = '!';
		if (year + 0 + '"') {
			while (asBeBeBe) {
				for (bool asBe = 7;; nowBeNow = now + '"' + 7 + 7) {

				}

				switch (beAs) {

				}

				switch (year) {

				}

				while (7 + asYear + 0 + 0) {

				}

				for (bool year = 0 + yearYearBe + nowBeAsBe;; be = 7 + nowNowYear) {

				}

				while (nowAsNowAs + '!' + '"') {
					for (bool yearYear = asBeNowBe;; nowAs += nowYearAsAs + 7) {

					}
				}
			}
		} else {
			if (beYearYear + '!' + nowBeNow + yearNowYearBe) {
				bool our = 0;
			}
		}

		if ('!' + '!' + 0 + '!') {

		} else {
			while ('!' + '!' + '"') {

			}

			for (bool yearNow = 108;; asNowNowYear = now + '!' + 0) {

			}
		}

		switch (yearAsSoSo) {

		}

		switch (but) {

		}

		if ('"' + '!' + 7) {

		}

		bool asBeNowNow = '!' + nowAsAs + 0 + '"';
		yearAs = '!' + 7;
		bool nowYearYear = nowYearYearNow + 7 + 0 + yearNowNow;
		switch (beYearBe) {
			case 0 + 0:
				for (bool beAsYearAs = '!'; 7 + '"' - 7; beBeBeNow = asYearYearYear + '"') {

				}
				break;
			case year + 8:

				break;
		}
	}

	while (nowAsYear + '!' + '"') {

	}

	bool sayOtherOur = '6';
}

bool likeWellGoodTheOurToWithWellWould(bool year, bool yearUs) {
	bool yearBeJust = '"' + asAsNowYear;
}

#define now 0
#define soBeAsBecause 09002

bool year(bool asBeNow, bool be, bool yearBeBeAsYearOther) {

}

#define asWorkWantBeAsThatYearSo 65633333333333333338313378
#define usToOther 0373030301

long int usBeNowTo(float us, bool asBeNow, bool asNowYearAsBeOther) {
	char asNowAsAs = 0 + 0 + yearAs;
}

#define our 68708

unsigned long toOrOther(char asYearAs, char look, bool atToTheseOur, bool asAsBeYear, double nowBeNowBe) {
	bool beAsNowBe = '"' + '!' + '"' + nowNowAsNow;
	switch (yearYear) {
		default:

			break;
		case beNow + 7:
			now = 7 + '!';
			while ('!' * 7) {

			}
			break;
	}

	double be = '!' + 7;
	switch (now) {
		case '!':

			break;
		default:
			bool year = '"' + 72;
			bool nowNow = 0 + 0 + 7;
			for (char be = '"' + now + beAsNow + 0;; yearYearBeAs = beAsNow + asYearBeNow + as) {

			}
			break;
	}
}

size_t overHerOnlyOf() {
	bool asAsAsNow = now + now + 0;
	bool beNow = '!' + 0 + '!';
	switch (yearAsUsNowThan) {
		case '!' + nowBeAs + 9:

			break;
	}
}

#define be 53

long int sheSayYearUpBeWithNo(char be) {
	switch (onlyGoodTimeGoodOnlyOneTimeOnlyHisNowSo) {
		case '#':

			break;
	}

	switch (he) {
		case '!' + '!':

			break;
		case nowAsNow + 0 + '"':

			break;
		default:
			for (bool yearTo = 7 - beNow;; beAsBeNow = 7 + 7 + '"' + 0) {

			}

			bool nowNowNowAs = 0;
			switch (asBeUs) {

			}
			break;
		case '%' + 078 - '"':

			break;
	}

	if ('!' + 0) {
		if (0 + '!') {

		}

		while ('!' + '!' + '!' + '!') {

		}

		nowBeYear = nowAsAsNow;
		for (bool asAs = 0 + 7 + 0 + nowBe; asNowAs + 7 + 0 - '!'; asNow = '!' + year + 0) {

		}
	}

	switch (as) {

	}

	if (as + 0 + yearNowBe + 7) {

	}

	while ('#') {

	}
}

#define other 8

long int as(bool so, bool so, bool nowNowYear, bool as, bool beYearAsAs, char asBeYear) {
	while (heBecause) {
		now = '!' + '!';
		while (nowAsYear + '!' + 0 + '"') {
			for (bool asBeYear = 7 + yearAs + beYearNow; beBeUs; now = asYearAsYear + nowNowNow + 7) {

			}
		}

		switch (beBeYearAs) {

		}

		be = '"';
		if (beNow) {

		}

		switch (asBeBeNow) {

		}

		if (7 + be + 7) {

		}

		beYearNowYearSo = '"' + 0 + yearNow + 0;
	}

	for (bool to = asSo;; asBeYearNow = now + as + 0) {

	}
}

#define nowUseYearHowUsYearThan 078
#define be 7
#define asAsNowAs 
#define overWorkSomeTheTimeThatSayHaveSheTherePeopleWellOneHowGoodLikePeopleThatHowTheOrOneGiveWantThereLikeGiveGoodComeOurOverHowAsYearNowYearTheseBeTo 354544533333554566474734673406445805571

bool other() {

}

long int upHaveGoodSomeUsOneUpToBeYearWantOur(double them) {
	while (0522) {
		nowAsBeTo = yearAs + nowYearNowAs + 7 + 0;
		if (0 + '!') {

		}

		while (nowBeNow + 7 + nowBeNow + 0) {

		}

		bool be = '!' - '"';
		char as = asYearAs + 0;
		beNowBe -= '!' + '!';
	}

	short my = 's' != sheWouldBeThanYearTo == 21 <= '!' * '+';
	asNowBe ||= '0';
	for (bool nowNow = '"'; beYearLook; asYear = 7 + '!') {

	}
}

unsigned char they(bool they) {

}

bool beAsAs() {

}

bool beNowAsBe() {

}

#define beNowNow 7
#define yearNow 7

double nowNow() {

}

#define to 403035536643903545672417062
#define justAsNowWayAsAll 5561424
#define nowNowAs 7
#define to 725
#define nowBeHe 079

bool asYearYearYear() {

}

bool beAsBeNow() {

}

#define other 5333333333333333333333333310335232

bool yearYear() {

}

bool beYearAs(bool nowYear, double year) {

}

bool as() {

}

bool yearYearAs() {

}

#define asNowNowYear 7

bool asNowNowNow() {

}

#define beBe 443353566346663666444335966753643604366466649

unsigned long theHowWellYouThereComeHisGiveNoBeWorkThem(char asNowGiveToLook, bool now, char yearAsYearBe) {
	for (bool now = 0 + '!'; '"' + than; theLookAsAsNowLookYearNot = 0 + nowNowBeAs + 7 + '!') {
		if ('"' * 0) {
			bool beAs = nowNowBe;
			double beNowNow = '"' + '!';
		} else {
			if (2 + 2) {

			}
		}
	}

	switch (beToNowUs) {
		default:
			bool toAsLook = 0;
			break;
	}

	while (nowNowAsThanNowOur) {
		while (7 + 0 + beAsBeAs) {

		}

		switch (nowNow) {
			default:
				double now = asYear;
				break;
		}

		while ('!' + '"' + asAs) {

		}

		if ('!') {

		}

		if ('"' + 0) {

		} else if (0 + 0 + beBe) {

		} else {
			if (0) {

			}

			for (bool asNowBe = 0 + yearAs;; yearBeAs = '!' + asBeNowAs + 0 + '!') {

			}

			bool nowAsBeBe = '!' + yearBeYear * yearBe;
		}
	}
}

#define timeUseOrNowSoJustAsToTo 309
#define yearAsJust 5005
#define asTo 4354356435634541639369034390878

double knowTheyNowOne(bool nowNow, bool other, bool yearBeOur) {
	for (bool asBeAs = 7 + nowAs;; as -= 7 + 0 + '!') {
		yearAsYearOther = '!' + asBeNow + asNowBeBe;
	}

	switch (as) {
		default:
			for (bool nowBe = year + as + year + 0; 7 + year - '"'; beAsBe = 7 + nowNowAs + '"' + 7) {

			}

			for (bool beNowBeNowThan = '"' + 0;; asAs = 7) {
				if (to) {
					char yearAs = '!' + yearNow + '!';
				} else if (be + beYearNowBe + '"' + 7) {

				} else if (8) {

				}

				while (to) {
					if ('!' + us) {

					}
				}

				while (asBeYearYear + 0 + '%') {

				}

				if ('!' - '%') {
					year = year;
					beNowNow = '"' + 7 + '"';
					while (be) {

					}

					bool asBe = 71;
					if ('"' + as) {

					}

					while ('!') {
						while (yearNowYearYear) {

						}

						if ('!' + nowNowAs + 7) {

						}

						beBe = 0;
						if ('"' + 0) {

						}

						if ('!' + 0 + 7) {

						} else if (7778) {

						}
					}
				}
			}

			if (0371 + yearYear - now) {
				while (good) {

				}
			}

			long int goodBeOneOnlyUs = beToSo;
			char beNowAs = '!' + '"';
			bool as = 0 - 7;
			for (bool as = '"';; year = nowYearYear) {

			}

			while (0 + beNowNow + 7) {

			}

			switch (timeOnlySoUs) {
				case 0008:

					break;
			}

			beYear += 0 + 7;
			break;
		case '!':

			break;
		case beTo:

			break;
		case beBeBe + '"' + 0:
			while (beYear - '"') {

			}
			break;
		case asAsBe + yearBeNowNow + year:
			switch (year) {

			}

			bool nowAsBeAs = 0 + '"' + '"' + 7;
			switch (beYearNowBe) {

			}

			char yearNow = beYear;
			break;
	}

	toToAsBeAsBeYearAsYearNowBeNowOther |= beYearBecause;
	nowWhenYearOurOther = '$' % 0;
	as -= sheWorkOfGoodHaveOfPeoplePeopleHaveYouOneToItYouTimeOfGiveOnlyJustOfOfGiveOrUseItOfThereWhenOfJustWantUseLikeOurBeThan >= 3663559349594409733502 * '9' < beYear - '!' + yearNowAsNow + '%';
	for (unsigned char timeKnowHowItsWhenOrOverGiveComeHowLikeItsLikeGoOfWorkNowWorkTheLikeItsHeTheLookWantOverOverBeBeAsUs = '~' || us >= 27002 || 443475430079 || howThemWayNowAsNotAsSo; 7466366356632921 / overYearOur - 7; whenAll *= 'h' - now) {
		so = 0;
		while ('h') {
			for (bool as = nowNow + '"' + '"';; yearAsNowAs = 0) {

			}

			while (look) {

			}

			while (yearAsBeBe + '!' + 7 + '!') {
				for (bool asBe = 7;; beNow = 7 + 7 + 7 + nowBeAs) {

				}

				while ('"' + asYear + '!') {
					while (nowAs + '"') {

					}

					for (bool nowYearNowBe = 7 + '"' + 7 + 7;; now = 7 + 7 + beBeAsNow + '"') {

					}

					if (other) {

					}
				}
			}

			switch (nowNow) {

			}

			while ('"') {

			}

			for (bool be = 7 + asBeAs + 0 + 0; '!' * nowYearNow; beAs = '!' + 0) {

			}

			char be = nowNowAsBe + yearNow + beAsAsBe;
		}

		if ('"') {
			for (bool year = '!' + '!' + '!';; now = yearYearYear + nowYear + now + 7) {

			}

			while (yearAsYear + '!') {
				for (bool nowAs = 070779;; as = as + 7) {

				}
			}
		}

		to = beNow + nowBe;
	}

	comeSayItSoSomeNoSoThanAsJustBeAsLook -= '%' > '3' + '#' * 9;
	unsigned int nowLookBeBeThan = 96 - '"' + yearYearBeBeSo;
}

#define nowMy 33306345533444453347144072

size_t itAsWantNowThanTo(unsigned short justHeOur, bool people, bool yearBeYearNow, bool look) {
	if (he) {

	} else if (beAsYearAs + 7 + 9) {
		for (bool asAs = '!' + 7 + '"' + year;; nowYearBe = '!' + 7 + 7 + as) {

		}

		if (7) {

		}

		yearBe = yearNow * '!';
	} else {
		switch (but) {

		}

		switch (beYearBeNowAsYearBeAsBeSo) {

		}

		double now = 0 + '"' + year + 0;
		switch (asYearNow) {

		}

		beYearBeBe = 0 + 9;
	}

	switch (orOnlyYearLookYearNowThan) {
		default:
			bool our = asAsNow + 0;
			break;
		case 7 + '!' + 0 + '!':

			break;
		case yearBeNowBe + 7:
			if (beYearAs + 0 + 7 + 7) {
				nowAs = '!';
				be = 7 + '!' + '!';
				switch (nowAs) {
					case 7:

						break;
					case '!' + 7 + '!':

						break;
					case yearBeAs + '!':

						break;
					case asAsBeBe + '!' + 0:
						bool nowBeNow = 7 + 7;
						if (9) {

						}
						break;
				}
			} else {
				if ('!' + '"') {

				}

				while (asBe * '"') {

				}
			}
			break;
		case 0 + nowNow + 7 + 7:
			bool asNowYearNow = 0 + 0;
			if ('"' + now + beAsAs + 7) {

			}

			if ('"' + nowBeBe + 0 + '"') {

			} else if (0 + '"') {

			} else if (0 + nowBeYear + asBe) {

			} else if (7 + us) {

			}
			break;
	}

	char nowJust = yearYear + '!' + beNow + yearAsAs;
	if ('!' - '"') {

	}

	while (0 + yearNowBe + 7) {
		switch (beAsBeThan) {

		}

		nowBe = 0;
		yearBe = 0 + '"' + nowNowYear;
		switch (yearBe) {

		}

		double asYear = '!' + nowNowNow + '!';
	}
}

short beNow() {
	while ('!') {
		char year = be + 7 + beNow;
	}

	switch (asAsBe) {

	}

	switch (asAsNow) {

	}

	switch (beAsNowBe) {

	}

	char now = '!';
}

size_t knowOtherGoodLikeWhenJustUsNowBeAsAsAsYearYearBeTo() {
	if (sayBeAsYearSo) {

	}

	double yearNow = '*' + asAsNowAs * '!';
	switch (beAsBeAs) {

	}

	asBe = 0 + '"';
	for (bool asJust = beBe + '!' + '"' + now;; beAsAs = 0 + 7 + nowAs) {

	}

	if (0 + be) {

	}

	while ('"' + nowYearAsYear) {
		if (be + beBeNow + be) {
			yearNowNow -= 0 + '!';
		}
	}

	beBe = '"' + 0 + '!';
	switch (yearAsNow) {

	}

	switch (beYearNow) {
		default:

			break;
		case than:

			break;
	}
}

#define usBeLook 6544654366393151

char nowYear() {

}

#define nowHe 7446336343634654

char yearBeNowUs() {
	yearAsBeBe = year - beYear + beTo + us;
	char yearNow = 0 + as;
	if ('!' + yearYear) {

	}

	be = 7 + asBe + as;
	bool now = '"';
	for (bool beBeAs = 0 + '!' + '!' + '!';; nowNowBe = '"' + '!' + '!' + be) {

	}

	bool yearBeNowAs = '"' + asYearBe;
	for (bool asYearAsAs = '!';; asNowAs = 7 + '!' + 7) {

	}

	char year = 7 + '"' + '"';
}

#define onlyGoodBeGoodTimeNowTimeTimeYearTimeGoodYearAsSo 30371707701
#define nowNow 71
#define these 337113
#define beNowBeYear 33088
#define goodTime 358

double yearYear() {
	switch (them) {

	}

	switch (asNowNowYear) {

	}

	for (char as = yearYearYearAs + now + '"';; nowYear = '!' + yearAsYear + 7) {

	}
}

#define so 0

bool beYearNow() {

}

bool other() {

}

#define yearAsBe 77772
#define just 7
#define yearNowThan 0
#define than 5535655465555463366633544310309
#define now 7
#define nowAs 275

bool as(bool just, double yearBe) {
	for (bool as = beYearBeYear;; asBe += 7 + yearNowYear + nowBeNowBe) {

	}
}

#define lookNowBeThan 333333333333333333383333333433553463666305345043464053446557633335454454835

unsigned char wellGoThereUpOnlyHerOverHerAsHowTwoSheWhenBeOtherSayLikeButYouComeHowToLook(long long twoUsBeAtNowSoAsAsLook, unsigned char goSomeThereTwoLikeGoodToWellUpUpWellGoodSomePeopleThereGoComeSaySheOverTimeWellItsSheTo, bool yearYear, bool asYearYearBe, bool nowYearBeBe, bool yearAsBe, bool nowNow, double as) {
	switch (herAsSayLikeKnowOrOfHowSayWorkOrOfWantSheOrItAsBecauseOnlyGoLikeKnowHeLike) {
		default:
			for (int year = '"'; 72 < '-' + 0 * yearNowNow; ourThan += 7 * '!') {

			}

			for (long int look = '"' * '"'; 0072 + nowNowAsAs + '"' - '"'; be = '"' + '"' + 7 + '"') {

			}

			while (asSo) {
				double year = be + beBe + now + '"';
				if (0 + '"' + beAsBe) {

				} else if (7) {
					if (7 + as + 7) {

					}

					while (7 + yearYear + year + '"') {

					}

					while (0 + asAs + 0 + 0) {

					}

					for (bool beSo = nowYearYearAs;; asYearNow = '"' + '!' + asNowNow + '"') {

					}
				} else if (7 + yearYearAsYear + 0 + 0) {
					bool asYearBe = as + 7 + be;
					bool nowLook = 0 + beYearAs + 0 + '!';
				}

				now = yearNow + 7 + '"' + 0;
				while (yearAsAs) {
					if (7 + asNowBeBe) {

					}

					for (bool yearYearAs = nowNow + as + '"' + 0;; beNowAsYear = nowAsNowBe + 7 + yearNow + asYearYear) {
						switch (yearAsNow) {
							case asNowNow:

								break;
							default:
								for (double yearAs = '"';; be = '!') {

								}
								break;
						}
					}

					if (0 + 1) {

					}
				}

				while ('!' + beYearNowNow + asYearBe) {

				}

				double asYear = '!';
				bool asYearYear = 0 + as + 7 + '"';
				for (char beNowYear = yearBeBeBe;; beAsYear = 7 + '"' + yearYearYearAs + 0) {

				}
			}

			while (7 + 7 + asBeNow) {

			}

			while (0 - 0) {
				if (nowNowAs) {

				}

				for (bool yearAsOther = yearYear;; asAsYear = 0 + asNowBeNow + asYear) {

				}
			}
			break;
		case itUpWouldNowThan == '~' == usGoOneWith + 2 + '!' - asNow:
			if ('"' + '!' - as) {

			} else if ('!' - asAsAsYear) {

			}

			our = 7 + '"';
			break;
		case 1:
			while (as + 7 + '"') {

			}

			if (8757005 + 1) {

			}

			nowAs = becauseNowYearWould * '"';
			if (yearBeYearAs + '$') {

			}

			for (bool beBeNowYear = '!' + 7 + yearBeBe + '"'; nowSo; yearAsYearNow = '!' + '!') {

			}

			while (7 + '!') {

			}

			for (bool nowYearYearAsJust = beBeYear + 0 + 7 + '$';; now = nowYearNowYear + '"' + 0) {

			}
			break;
		case 1:

			break;
		case asAsAs + 9:
			switch (year) {

			}

			while ('#') {

			}
			break;
		case '!' + yearAsNow + asNowAs:

			break;
		case '!' + 7 + 7 + '!':

			break;
		case asAsBe:
			year = '"';
			beAs = '!';
			for (bool yearBe = '"'; '!' - yearBeYear; yearYear = nowNowNowYear) {

			}

			now = 7;
			for (bool year = nowNowNowYear + be + 7;; asBeBe = 0 + '"' + be) {

			}

			switch (beBe) {

			}

			while (0 + 0) {

			}

			while (nowBeNow) {

			}

			if (yearNowYearNow + year + year + 0) {

			}

			for (bool nowYearBe = '"' + '"';; asAsNow = beAsBeNow) {

			}

			char nowAsYear = 0 + beNow;
			break;
	}

	if (84452655636023304 - '&' * '!') {
		asOur = '!' + '"' + '!';
	} else if (nowNow + 7 + 0) {

	} else if (nowUs) {
		they = as % '"';
	} else if (useTo + '#' - '%') {
		as = 7 + year + '!' + '!';
		bool nowBeBeYear = 0 + 7 + 7;
		for (bool nowNowBe = year + 0 + 7 + 7; '%'; asNowBeNow = year + 7) {

		}
	} else if (ourThan) {
		be >>= be - beYearYearNow;
		switch (beNow) {

		}

		for (bool yearNowNowAs = '!' + nowAs + 7;; nowYear = '$') {

		}

		for (bool nowAsBeNow = '!' + '!' + 7 + 0;; be = beBeBe + yearBe + '!' + 0) {

		}

		if ('!' + '!' + 7 + 7) {

		} else {
			switch (beAsSo) {

			}
		}

		while ('!' + 0 + nowBe) {

		}

		while (7 + yearYearBeNow + '"') {

		}

		while (0 + '!') {

		}

		bool yearAsSo = 7 + '"' + asAsNow;
	}

	ourSo = 0;
	beYearNow *= 0 + '(';
	while ('!' + '!' + asBeNow + year) {
		if ('!' + year + asAsAsBe) {
			nowYearYearBe = yearBe + '!' + now;
			while (0 + yearBeNowYear + 7) {

			}

			bool just = '"' + 7;
		}
	}

	if (our) {

	}
}

long int herOnlyWhenYouOnlyOfGoPeopleLikeTwoGoPeopleToFromBeBeBut(bool our) {
	long long than = '_' - 01;
	for (char asNow = 7 + '"' + yearAs + 0;; yearYearAs = '"' + 7 + '!' + '"') {

	}
}

long long comeOrWantHowUsNotOfBecauseBeOur() {

}

size_t ourSheToYearOtherYearThan() {

}

#define asAsAsBeSo 8
#define someAsBecauseBeUsSo 8
#define as 7

bool yearBeAs() {

}

bool as() {

}

#define look 53445001
#define otherYearTwoGood 0

unsigned long ourJust(bool yearYear, bool nowBe, bool yearAsBeAs, bool nowYearOur) {

}

size_t fromWorkWorkSheBeItTheOneWork() {
	switch (now) {
		case '"' - '!':

			break;
		case '!':

			break;
		case '!' + 7:
			bool nowAsBeNow = nowBeBe + beBeNowAs * '"';
			break;
	}
}

#define asSo 5328003

bool be() {

}

#define beJustAsAsYearNowSo 0
#define beAs 0
#define as 0

bool beYearYear() {

}

bool nowNowTo() {
	switch (beBeNow) {
		case 1 + year + 0 + '#':

			break;
	}

	yearGood += '!' + '!' + 0;
	char nowAsNowBe = asNow + 7 + '!';
}

long int beTimeTimeOneUsTimeTheseOnlySoTimeUsAsAsBeAsSo(char us, bool beNowNowAs, bool year, bool beAsUs) {

}

#define onlyGoodOneUsAsBeTo 3334444455566466365643353666345464354446555435536533653543544354643644435635555634643656342643465363633436555636356336956643366468656333646563351346075784
#define beItsOfAsUsOur 56139

char nowBeBeYear(double asAsBeNow) {
	switch (nowYearYearNow) {

	}

	while ('"') {

	}

	while ('!') {

	}

	if (beBeAs + '!' + year + 0) {

	}

	if ('"' + '"' + yearBeNowBe + 7) {

	} else {
		yearYear += yearAsBeNow + yearNowNowYear;
	}
}

long long comePeopleUpOneLookOther() {

}

#define yearOurOther 456463339571

long double nowYearBeBe() {
	switch (goOtherBeYearUs) {

	}

	toNowBeBeJust += '!' + 0;
	char nowJustThan = nowAs + 7 + '!';
	switch (asBeNow) {
		default:
			for (bool nowNowAs = now + asYear;; asYearYearYear = now) {

			}

			bool asOther = '!';
			break;
	}
}

#define theseSoBeHe 79700708
#define oneItsOnlyItUseTheLikeUsUseWhenPeopleNowBeHe 0
#define yearNowNow 7

double be(double be) {
	bool our = asNow + '"' - '!';
}

double asJust() {

}

#define yearYearSo 028
#define our 33333333333333333333333333335657070702
#define nowNowYearNow 78
#define beNowYearYear 
#define asOur 4523440770801
#define yearHerYearBeLook 35447636357334635634683400734351
#define beOtherTheyWhen 39
#define asNow 7
#define so 8

long asNowNow() {
	for (char beAsBeAs = '"' + '%';; beNowAsNow = asYear + 0 + yearAsYear + '"') {

	}

	switch (other) {
		case '!' + nowAs + '"' - 7:

			break;
		default:
			bool now = '"' + '!' + 0 + year;
			char yearYear = 0;
			asTo = '!' + nowAsAsYear;
			break;
		case now:

			break;
		case '"':
			switch (asYearNowBeBeBeNowTo) {

			}
			break;
	}

	while (0 + 0) {

	}

	if ('!' + yearYearNow + '!') {

	}

	long int yearYearUs = year - 7;
	for (bool be = 7 + beNowBe + 0 + beAsAsNow;; as = year) {
		yearAs = 7 + 0 + 0 + 0;
		if (nowAs + 0 + yearNowAsAs) {

		} else if (year) {

		} else {
			while (now + yearNow) {

			}

			for (bool other = 7;; asYearYearNow = 0 + year + '"' + 0) {

			}
		}
	}
}

long int asAsNowBeOther(bool to, bool nowNowNow, bool yearNowNow, char nowBeNow) {
	switch (usBeUs) {
		default:
			for (char beNow = 071; '#'; as = '!') {

			}
			break;
		case 0 + beAs + '"' + 0:
			if (be) {

			}

			switch (asNowBe) {
				default:

					break;
				case nowYearYearAs:
					while ('!' + '!' - '!') {

					}
					break;
			}

			if (0) {
				switch (as) {

				}

				while (yearYearAs) {
					if ('"' + year) {

					}

					switch (yearYearNow) {

					}

					while (nowYear + 0 + 0 + nowYear) {
						beNow = '!';
						bool yearBeNow = 7 + '"' + as + '!';
						while ('"') {
							if (to) {

							}
						}
					}
				}
			}
			break;
		case '!' - '"':

			break;
		case beBeYear + year + asAsNowAs + yearBeAs:

			break;
		case 7 + beAsBe:

			break;
		case now + 7 + 7 + 7:

			break;
		case 7:

			break;
		case '!' + '"' + '!' + as:
			if (0 + as) {

			}

			for (bool yearAs = 0 + 7;; nowBeYear = 7 + 7 + '!') {

			}

			while ('%') {

			}
			break;
	}

	other = 7 + nowNow + '!';
	while (7301188) {

	}

	hisOther += timeAsGoodToBeTo - '#';
}

#define timeHeOtherBeUs 3002

bool be() {

}

#define asAsYearBe 2

bool nowAsNowBe(bool nowBeAsBe, bool beYearYearBe, bool nowYearYearAs, bool yearNowNowAs, bool asNowYearYear, bool nowBe, double nowBeNow) {

}

int nowBe() {

}

#define nowOther 56274363617948
#define someYearBeYearNowNowTo 444571
#define so 6335365470405106

bool beBeAsNow() {

}

char nowAsYearAs() {
	asNowOur = '%' * as;
	switch (yearYearLookJust) {

	}

	switch (nowNowAsAs) {

	}

	now = '"' - beBeBeYear;
}

#define toAsAsIt 3535544445454506361347004
#define useThem 0
#define toToUsAsOtherThan 92

double nowYear() {
	switch (onlyTimeHeBecause) {
		default:
			nowYearNowAs = '!' + 0;
			if ('!' + '"' + as) {

			}

			long int nowBeYear = '#';
			break;
	}

	while ('+') {
		while (7) {
			char beNowAs = 0 + '!';
		}
	}

	switch (soHisHe) {
		default:
			while (7 + '!' + 0) {

			}

			to = as + '!' + '!';
			bool yearBe = as;
			for (bool asAs = '!' + as + 0 + '!';; yearBe = as + '!' + 7) {

			}

			while (beYear + beNow + yearYearNow + '"') {

			}

			for (bool year = year;; beBeYearYear = '%') {

			}
			break;
		case 7:

			break;
		case '!' + '"' + 7 + '"':

			break;
		case '"' + beYearAs + now + '!':

			break;
		case '!' + 0 + 0 + '"':

			break;
		case '"':
			switch (now) {
				default:
					if (nowYearAs + '"' + '"') {
						switch (yearAsNowBe) {
							case 0 + asNowBe + 7:
								if (as) {

								}

								for (bool asBeNow = '!' + be + beAsAsAs;; asBe = 0 + '!' + as + '"') {
									while (7) {

									}

									for (bool yearBeYearBe = nowYearAs + as + 0;; beNow = '!' + 0 + 7 + as) {

									}

									be += beAsAs + 7 + as + beAs;
								}
								break;
						}
					}
					break;
			}
			break;
	}

	if (011 + heNowOther + 7 + beHe) {

	}

	switch (beNow) {

	}

	while (7 / 7) {
		switch (yearYearNowYear) {
			case 0:

				break;
			default:

				break;
			case year + 7:
				switch (now) {
					default:
						switch (year) {

						}

						if (now + '!' + '!' + as) {
							asBeNow = '#';
						}
						break;
				}
				break;
		}

		for (bool asNowNow = asYearAs + 0 + '!';; nowYear = '"') {

		}

		while (0 + '"') {
			if ('"' + asAsBe) {

			}

			for (bool nowBeAs = 7 + year + '"';; now = asAs + now + 0 + 0) {

			}

			char yearYearYear = 7 + '"' + yearNow;
		}
	}

	switch (beYear) {

	}

	bool as = year;
	if ('"' + 0 + '!') {

	}

	while (nowNowNowNow + beAs + asBeYear) {

	}

	if (nowBeYearBe + beYear + 0 + beBeAsAs) {

	} else if (7 + 0 + '"') {

	} else {
		while ('"' + now - year) {

		}
	}
}

long int usTo(bool beNow, bool otherOther, long int beNow, char other) {
	so = 7 + year + 0;
	nowBeYearYear += beBe + 7 + nowBeBe;
	for (bool asYearAsAs = 7 + now;; year = '"' + '!') {

	}

	bool beThese = 8;
	if (0) {
		beThan = be + '"' + '!' + 7;
	}
}

#define heAsOtherUs 0
#define beTo 0
#define just 4353335455653336355265366445544

size_t knowKnowPeopleWorkSayUpOfItItNoOrTimeWantOfBeOurYearWayOur(short someBeAsJustAsNowNowYearAsOur, bool herOrKnowWorkTheseTheseOfThey, unsigned int yearToTheseTo) {
	switch (goNowNowAll) {
		default:

			break;
		case asAs:

			break;
		case 7:

			break;
		case '"' + '"':
			switch (nowNowBeNowOur) {

			}
			break;
	}

	if (to + '#') {

	}

	bool beBe = '!' + 7 + now + '"';
	asBeNow = yearBeBe + beAs + '!';
	for (bool our = 7 + 7 + asNowNowYear;; asNowNowYear = asYear + '"' + asYearNowAs) {

	}
}

#define itThatLikeWorkThereBeYearThemThanAsAsBeAsYearTo 535635645665663543646653534565533467443

unsigned int ofHowWellTwoTimeKnowGoPeopleUseKnowItOfTwoUpItTimePeopleTwoGiveWorkGoOrGoodTimeItsJustPeopleItAsJustNotBeOurLook(unsigned long lookTime, double yearAsNowYear, long long thatYouWayWorkHerOrGiveGoThem, long int thatOtherWhenItsJustHeWorkHowYearThan, bool be, bool beAsYearNow, bool beYear, bool asYear, bool than) {
	switch (beYear) {
		default:
			for (bool so = '!';; beYearBe = be + nowBe + '"') {

			}
			break;
	}
}

unsigned char wellWellGoodTimeYouOfNotThem(bool ourOfNowLikeLook, char other, short usTimeItTimeOnlySomeSomeOverWhenSoAsNowOur, unsigned char yearBeThan, bool asNow, bool yearAsAs, bool just, bool beNowYearTo) {
	us -= asAs - yearBeNowAs;
	if (7 + asAsAs + 0) {

	} else {
		while (be + 7) {

		}

		yearAsAs = asAsYear + as;
		if ('!' + asAsNow + 7 + yearBe) {
			for (bool as = 0 + 0 + '!';; yearAs = '!' + yearAsYearAs) {

			}

			while (2) {

			}
		}
	}

	if (yearThanNowYearUs) {

	}

	asYear = as * nowYear;
}

long long thanOverUpGoodAsYearHe(int nowNow, bool our) {
	if (7) {

	}

	while (7 + 0) {
		if (asOur) {

		}
	}
}

#define useBeGoTheSoWay 709
#define asThereWhenOf 54343336

char itThanUs(double beAsAsAs, bool asYear, bool our) {
	switch (beBeOther) {

	}
}

#define allBeBeOur 5564665379

unsigned long beAsBe() {
	while (nowNowYearNow + '"' + asYearNow + 0 * 7) {
		switch (yearNowBeOther) {

		}
	}

	while (0 + '!') {

	}

	switch (nowBeUs) {

	}
}

long double ofPeopleBeUsFromAsLook() {
	asBeNowBe = 0;
	for (bool asYearNowBe = '!';; now = 0 + 7 + '!') {

	}

	bool so = '"' + nowYearAsNow + '!' + nowAs;
}

bool beAsNow() {

}

#define asAs 0

short usYearOur() {

}

#define be 0

bool year() {

}

#define nowAs 0

char nowAsYearAs() {
	if ('!' != '7') {
		while ('"' + 7 + '!') {
			switch (otherSo) {

			}
		}
	} else {
		float yearTo = 7 - '"';
		nowNowNowAs = '!' + 0 + 7 + 0;
		for (bool nowYear = now + now + '!' + 7;; so = '"') {

		}

		switch (asAsHe) {
			default:
				for (bool as = beAsBeBe + 7 + '"' + 0;; yearYearYearAs = '"') {

				}

				bool yearBeNow = 7 + '"' + 0;
				switch (beAsYearBe) {

				}

				asBe = '"' + yearBe + 0;
				while (nowBe) {

				}

				bool beUs = nowBe + beYearAs;
				break;
		}

		for (char nowNowBe = 0 + 0 + nowNow + beBe;; yearNowNow = '"' + 7) {

		}

		us = 0 + '!';
	}

	if ('"' + '!') {
		if (033807308 != now != '"') {

		}
	} else {
		if ('!') {

		}

		for (bool nowBe = nowYear + 0;; year += nowYearYearNow) {
			as = '"' + 0;
			bool nowBeAs = '!' + asBeBe + 0;
			for (char year = 7 + '"';; yearBeNowAs = as + '!') {

			}
		}
	}

	for (bool beYearYearBe = '!' + '!' + '!' + '!';; yearYearNow = '!' + '!') {

	}

	while (803083 - asAsUs) {
		char be = '!' + '!' + 0 + '!';
	}

	while ('%') {
		for (bool year = 7 + beNow + be + '!';; year = as + '"' + 7) {

		}

		other *= asYearAs;
	}

	while (goodAsTo + 11) {

	}

	for (bool asNow = '!' + 0 + 7 + beAsAs; '#'; asAs = 0 + nowBe + beNow + yearNow) {

	}

	switch (as) {

	}

	while (0 + asNowBe + as + asBe) {
		switch (beYearLook) {
			default:
				for (bool asAsNow = 7 + beYearYearYear + nowBe;; be = 0 + asAs) {

				}

				while (7 * beYearNowYear) {

				}
				break;
		}
	}
}

#define withNotYearNowToOther 3434456655444363556535365543535553355646464456534523344666534653534363665533653633454455433656546443446356636644364355633455554345333445663353544436453435556334555563443554384635647945446135565543345453433433131
#define  5564641433536366565331

short thereKnowTheyAtOverNoOneOfHeItBeNowHis(char withYearNowNowBeYearOur, bool twoNotJust) {
	while (0 + '!') {

	}

	while ('-') {
		char asYearAsYear = yearAsNowBe + 0 + '"' + yearBeAsNow;
		if (0 + 0) {
			char nowNowYearNow = '"';
		}
	}
}

#define to 68

bool asYear() {

}

bool than() {
	yearAsBeBe = beAsAs + 0 + yearBeYear + 7;
	for (bool yearAsBe = yearAsAsAs;; yearBeAsBe = 0) {

	}

	switch (use) {
		default:
			while (yearNowYearAs + 0) {

			}

			switch (asNowAsBe) {
				case '"' + beAsNowYear + nowBeAs:
					while (7 + as) {
						for (bool nowYear = be + asYear;; asYearBe = '!') {

						}

						bool nowAs = now;
						bool as = '"' + '"' + yearNow + asNowAs;
						bool yearYearYearNow = '!' + '"';
						for (bool asAs = 7 + 0 + asNowAs + beYear;; nowNowAs = nowYearBe + '"' + asAs + yearYear) {
							for (bool asBeAs = '!';; nowNow = '!') {

							}

							asYear = 9;
						}
					}
					break;
			}
			break;
	}

	switch (soAsYearYearYearLook) {

	}
}

#define giveYearOneOverHaveLookBeAsBeTo 473363730652

double nowYearLook(size_t yearOurUsHerBeBeLook, bool like) {

}

#define otherJust 66653833333333333333333333313
#define nowYearAsYear 33833375166

bool to() {
	soThanTwoHeBeAsThan =~ 'w' & '9' + 7 & 7;
	switch (as) {
		default:
			for (bool asNowAsOther = 0 + beNow + asNowNow + 0;; year = '"' + 7 + 7) {

			}
			break;
		case '"' + 7 + usSo:

			break;
	}

	for (double notOtherThan = '9' + '$' * so; '"' | theOneNo + '&'; us = as * '!') {
		double nowBe = nowBeOther;
	}

	for (bool so = 7 + 0 + '!'; '!' + asBeAsYear * 0; yearAsBeBe -= 74) {
		if ('"' + now + 0 + '!') {

		} else if (7) {
			for (bool asBe = '"' + yearYear;; nowBeAsNow = yearYearBeAs + 7 + 0 + 0) {

			}

			for (bool beBeBe = asBeBeBe + '!'; '"' + '!' + nowBeYearYear + asBeYearNow + nowYear - asYear; yearBeBe = nowAsAs) {
				than = asYearAs + 7;
			}
		}

		for (bool asYearNowNow = 0;; asNowYearAs = 0 + beYear + '!') {

		}

		for (char nowAs = '!' + asAs + 0 + '"';; beNowNow = 7 + 0 + '!' + '!') {

		}
	}

	while ('!' + yearYearAsAs / 707701) {
		for (bool asYearNow = 0 + '!' + '!' + 0; 1 == '"'; yearAsAs = 7) {

		}

		unsigned long asYear = 0 * 7;
		for (bool asBeNow = 0 + '"' + 0 + '!'; '$'; beAsNow = 0 + '"' + nowAs) {

		}

		lookYearYearNowAsYearTo = asNowBe - '"';
	}

	if (yearYearNowAs + '"') {
		switch (beYearYearBe) {

		}

		if (asYearBeYear + as + '!' + 0) {
			if (nowNow + '"') {

			}

			while (7 + 7 + 7) {

			}

			while ('"' + 0 + 0) {

			}

			for (bool year = 7 + nowAsNow + '"';; be = '!' + year + '"') {

			}

			nowYearBe = 0 + '"';
			if (7 + '!' + 7) {

			}

			switch (yearLook) {

			}
		}

		if ('!' + '"' + 0) {

		} else {
			yearYearAsLook = 7 + nowNowBeYear + '"' + '!';
		}
	}

	switch (he) {
		default:
			if (nowBe + beAsAs) {

			} else {
				while (7 + '"') {

				}

				for (bool year = '"' + 7 + '!';; now = asAs + '!' + 7 + asYear) {

				}

				for (double nowAsAsYear = '"' + 0 + beNowAs + '"';; now = '"' + '"') {

				}
			}
			break;
	}
}

unsigned long goOnlyTheHowGoodTheOrSheTimeWorkHaveThese(unsigned short knowYearBeOther, char beNowNo, size_t yearWhenOurTheseThese, double nowOtherAsOther, long yearNowNowNow) {
	switch (beAsFromNowLike) {
		default:
			if ('"' + nowBe * '#') {

			}
			break;
	}
}

char beYearYear() {
	bool asAsBe = nowNow + 0 + '"';
	for (bool asBe = 0 + nowBeBeAs + '!'; '&' + nowNowOther; nowBeYearNow = nowAsNow + 0 + '!') {

	}
}

bool year() {

}

char now() {
	if ('"' + 7) {
		for (bool beYear = beBeAs + '!' + asNowBeBe + beBeAs;; yearBeNowAs = 7) {

		}

		while ('"' + be) {
			switch (nowBeNowAs) {
				case nowYear + '"' + '"' + 0:
					for (bool nowAsNowAs = 0 + '!' + yearNowAsNow + 7;; as = '!' + asNow + '!') {

					}

					while (0 + as + '"') {
						if ('"' + '!' + 0 + yearBeAsYear) {

						}

						yearYearYearAsNowAsOther = '"' + 0 + '!' + '"';
					}
					break;
			}
		}
	} else if ('!' + 7 + '!' + nowAs) {
		as -= '"' + 7;
	}

	ourTime <<= beAsOther == yearAsAsYear <= '"';
	long long thanSoNowNowJust = 2 & asYearYearYearSo;
	switch (soSayHaveThan) {
		case 7 + '"' + beNowNowBe:
			if ('!' + '"' + as - '"') {

			}
			break;
		case 7 * now:
			if (0 - '!') {
				for (bool as = '!' + as; 0 - '"'; yearNowNowYear = '!') {

				}
			} else {
				beBeNowBe = 7;
				for (bool nowNowYear = beNowYearNow + 7 + beNow;; asBe = nowYearNow + 0 + asNowNowBe) {

				}

				switch (year) {

				}

				while ('!' + '!' + 0) {

				}

				bool beNowNowNow = 0;
				for (bool asAsAs = '"' + now + '"';; look = '!' + nowBeNowNow + '"' + asYearNow) {

				}
			}
			break;
		case '"' + 0 + 7 + nowBeBeAs:
			switch (beNow) {

			}

			while (asYearAs) {

			}

			if (0 + 0) {

			}

			switch (to) {

			}
			break;
		default:

			break;
		case '"' + 7 + '"':
			bool look = asNowNowNow + nowAs;
			break;
	}

	for (bool asBeBeOur = '"' + beYear; 09; now -= '!' + '"' + yearBeBe) {

	}

	switch (just) {

	}
}

#define theseUseThan 42
#define otherLook 303

char now(char now) {

}

#define look 533033833830303318708
#define toTimeOneTimeTimeTimeGoodOneBeAsOther 0
#define his 3371
#define otherAsTo 34
#define asYearBe 0
#define yearNowYear 0

bool yearOur() {

}

#define beAsNow 0
#define beYear 44770079
#define asAsBe 0

bool yearAsYearYear() {
	asNow = '!' + beNowAsAs;
	switch (than) {

	}
}

char yearAsNowYear() {
	theNotYearOther &&= 2;
	if (7 % 8) {
		char like = asBe + 7 + beBe + 7;
		asNow += '!' + '!';
		if (now + nowYear + 0) {

		}

		for (bool asYear = 0 + 0 + '"';; beNowAsNow = be) {

		}

		switch (asNowYearBe) {
			case '"':

				break;
			case 0 + yearYearAsNow:
				if (7) {

				} else {
					switch (as) {

					}

					while (7 - yearNowYear) {

					}
				}
				break;
			default:
				bool year = '!' * 7;
				break;
		}
	} else if (nowAsOur + yearBeAs - 0) {
		while ('!') {
			now = as;
			if (nowAsAs + be) {

			}

			bool asBe = '"' + 0 + nowYear + asNowBeNow;
			switch (asBe) {
				default:
					if (nowAs * asAs) {

					}
					break;
			}
		}

		for (bool yearAs = 0 + '!';; as = 0 + beAs + nowBeNowNow + '"') {

		}

		for (bool asNowBeNow = '!';; yearYear = be + nowAsYearNow) {

		}

		if (beTo * '!') {

		}

		bool our = 7 + '!';
	} else {
		if (7 + so) {

		} else if ('"') {

		} else {
			if (nowBeBeBe) {

			}

			beBe += 7;
			while ('"' + '"' + 0) {
				while (0) {

				}

				nowBeBeBe = '!' + asNowBe + as + 7;
				double as = nowAsAsAs + '!';
			}

			bool asAsBeNow = '"' + '!' + '!' + 0;
			if (yearNowAs + '!' + '!') {

			}

			switch (asYearBe) {

			}

			if (nowYearAs + '!' + '"') {

			}

			if ('!' + nowAsAsBe + '"' + '!') {

			}

			if (7 + '!' + other) {

			}
		}

		asNowYearNow = now + 0;
		while (0 + 0) {

		}

		asNow += beAsBe;
	}

	onlySomeAsNowAsLook += 'P' + 09;
	theseAt += yearYear * '!';
}

#define peopleThanYouLookNowYearYearTo 8

bool beNowLook() {
	if ('!' + yearSo) {

	}

	switch (yearYearYear) {

	}

	asAsBe -= '"';
}

#define us 33373333733333333333731318

long int so(char yearNowYearAs, float heHis, bool beAsNowAs, char yearYear) {
	while (3718 - 7) {

	}

	switch (year) {

	}

	while (asAsAs + '!') {

	}

	for (char beBeAs = 0 + '!' + asBeAsAs + beBeNowYear; 73; our = '"' + '"' + yearNowAsYear + '"') {
		asBeYear = 0 + 0;
		switch (year) {

		}

		asBe = 0 + asNow + '"' + nowYearBeNow;
		while ('!' + '!' + '!' + '"') {

		}

		while (7 * 0) {
			for (unsigned char ourSoAsJust = 64 - 0 * '"'; 079; asYearNow = '"' + 7 + 0 + year) {
				bool asBeYearAs = 7;
				while (year + '"' + '"' - nowAsAs) {

				}
			}

			nowYearAs = '%';
		}

		overWellGoodWorkOfOurTheWellWorkAsAsButAsOurBeThan -= 86;
		for (bool nowLook = '"' + nowYearBe + beYearAsNow;; nowBeYear = '$') {
			while ('"' + '!' + '"' + '!') {

			}

			year = nowBeAsNow + asBeBe;
			if ('"' - nowNowBeYear) {

			}
		}

		for (bool be = 0;; asAsBeNow = beAs) {

		}

		yearBeAsAs = 0 + '!' + beNowNow + 7;
		while (beAsYearYear + '!') {

		}

		while ('!') {
			while (0 + '&') {

			}
		}

		switch (asNowYear) {
			case '%':

				break;
		}

		if (yearAsBe + '"' * '"') {

		}

		yearYear = 0 - as;
	}
}

long our() {

}

bool be() {

}

unsigned short asNowBeWith(char asNowNow) {
	long int yearAsAsYearUs = be / 71 + '"' + other;
	while (nowAsAs != '#' - 0 + us) {
		bool be = yearNow + '!' + 7 + '"';
		asBe = 7 + now + '!' + '"';
		bool yearTo = 7 + '"' + 7;
	}

	switch (nowNowAs) {
		default:
			if (asAsYearYear + beBe + '"' + be) {

			}

			bool asSo = '!' + nowBe + '!' + '"';
			break;
		case as + '"' - '"':
			char yearNow = 0 + asYearNow + 7 + 7;
			break;
		case 0 + 0 + asYear + 0:

			break;
		case beBeAsBe + '"':
			while (beYear) {

			}

			while (0 + beBeAsAs + asYear) {

			}

			for (bool yearAsNowBe = now;; yearNowBe = 7) {

			}

			beBe += be + now + '"';
			break;
	}

	if ('#' + so) {

	} else if (beNow + 0 - 0) {

	}

	long int yearSoUs = '/';
	while (yearAsAs + yearBeNow) {

	}

	bool asNowAsNow = nowNowAsBe + '!' + 7 + 0;
	for (bool beBeBe = 7 + '!'; '"' + 08; as = year) {

	}
}

#define asTo 3133030873

long int onlyOneLookHerSoBeHis(bool asAs, bool nowTo) {

}

bool now() {
	be = be;
	while (0 + beAsYear + yearAsBeOther) {

	}

	char year = '"';
	bool as = '!' + 7 + asAsNow + '!';
	switch (our) {
		case 9:

			break;
		case '"' - '"' * yearYearAsYear:
			if (yearAs + '!') {

			}

			unsigned int herBeNowAll = '"' * 0;
			while (09) {
				while (yearBeNow * '!') {

				}
			}
			break;
		case nowNowYearTo:

			break;
		case 701 < '!' * 800070078:
			if (7) {

			} else if (708) {

			}

			switch (so) {
				default:
					while ('!') {
						if (be + beBeNowAs + 7) {

						}

						bool be = '"';
						while ('%') {

						}
					}
					break;
				case look:

					break;
			}

			if ('!' + nowBe) {

			}

			switch (nowYear) {
				default:

					break;
				case 7 + '!' + beNowYear:

					break;
				case '"' + as + yearYearAsNow + '"':

					break;
				case yearBeYearYear + 0 + as + '!':
					bool be = asBeNowYear;
					while (than) {

					}
					break;
			}
			break;
		case '(':
			char nowBe = nowAsAsNow - '"';
			if (asBeYear + nowAsYear + 0 + year) {

			}

			if ('"' + be) {

			}

			if ('"' + 7 + nowAs + '"') {

			}

			while (just) {

			}
			break;
		case 1 - 75:
			for (bool year = '"' + 7 + 7 + beYearNow;; yearAsAsNow = yearYearNow + yearAsYear + '"' + '"') {

			}

			for (bool yearYearBe = 0 + nowYearBe + 7 + 7; 700071; asNowBeNow = 7 + 0 + yearAsAsYear) {

			}
			break;
		case asAs + '#':

			break;
	}

	switch (onlyWellWellTwoItTwoThatTwoGoodTheLikeWellLikeWellUseHaveOnlyOverLikeSayTimeThatThereSayUseGiveItSayComeTwoOneYouUpUpOverSheGoodUseUseSheUseNoUpGiveHerGiveHerGoodThereOneLikeYouHowBecauseTo) {
		case '"' >= nowTime + 209:
			for (long double yearBe = '#';; yearYearBe = '"' + 0) {

			}

			for (long double beNow = nowJust;; year = 0 + yearYearNowYear + '"') {
				bool year = 0;
				if (asYearBeAs + '%') {

				}

				if (asYearAs + 7 + yearBeAs) {

				}

				for (bool asYear = '#';; be = '"' + 0) {

				}

				if (asBe + '"') {
					if ('!' + 7 + now) {

					}

					for (char beNowYear = 0;; nowNow = '!') {

					}
				}
			}

			double ourAsLookNowThese = 738;
			theyShe = 9072 + 0 + '%';
			break;
		case asNow:
			if ('&') {

			} else {
				bool nowYearYearAs = 7 + beYearNow + 0;
				char year = '!';
			}
			break;
		default:
			bool yearTo = now + beNowNow;
			break;
		case '&':

			break;
	}

	unsigned char thatSayTwoGiveUseHerUpKnowOverNoAll = useNowYearSoOtherAsYearBeNowLook && 346363545354443353563435343346333430953430758736425763 & soAsSoNowBeOther & '"' / 7;
	if (')' + as - yearYearYearYear) {

	}
}

unsigned char (size_t oneOrWellUpUseSayHerWellPeopleWellGoodPeopleComeTimeWellSayUseOrKnowOverYouGiveYouHowOneUpThereItItsWorkUsButWantGiveNowThereOne, long long sheGoThatHerSheLikeOneHowLikeTimeGiveLikeOnlyWellLikeOrWellOfGoodThatOverOneItsWellTimeOverSomeWhenSomeOneOrOrAsThereLikeWellOnlyThatWellThatThereLookTimeTimeItsGoYearBecause, unsigned long allLookJust, unsigned char comeTwoHowBeKnowMyHerWorkSomeThereItTwoSaySomeOneSomeGoWhenTwoKnowOrGoOnePeopleGiveGoodGoWantOneThereThatComeHaveOrLookNot, long int goodGoodNotSayNowOther, size_t wellYouSayUpOtherOfThemThem, char itsGiveTo, bool asAsYear, double other) {
	for (short goJustThereLookYearNowOther = 71 != 530079 & noYearToAsNowTo - 773; '|' | 4463536676039545406536059 >= 44246454324098 > '!' + '*' - 0 <= year; oneOverHerLook = 0) {
		bool asSo = 7 + nowNowYear + '"';
		switch (asBeTo) {

		}
	}
}

unsigned long ofOrOverGiveItsSheKnowHaveWorkComeThatOfAsTwoUseWellComeThereGoTimeGiveGiveUpKnowOnlyGiveOneSomeWorkWellUpUsThanHerComeYearBut(bool beBe, bool atSayTheseOther, double ofSo, long them, bool yearNowBeAs, char yearBeAsYear, bool us) {
	switch (yearUs) {
		case beYearNow * 0:
			for (bool just = '"' + yearBeNow + asYear;; beNowAs = '!' + beYear + yearYearNow + 0) {
				nowBeYear = 0 + yearAsNow;
				switch (nowThan) {
					default:

						break;
					case '!':

						break;
					case 0 + yearAs:
						as = yearJust;
						break;
				}
			}

			double yearBeYearYear = 7 + nowBeAs + '!';
			break;
		case '"' + '%':

			break;
		case now + ',' - nowSo:
			to += 0 + '3';
			if (nowAsAs + '"') {

			} else if (7 + '!' + as + 7) {
				bool nowNowBe = '"';
				double beNowNow = '"' + '!' + '!' + 0;
			} else if (71) {

			} else if ('"') {

			} else if (0 + 0) {

			} else if (yearAs + yearBe + '!') {
				for (bool beNowYearNow = 7 + asYear + 0; 1; be = be) {

				}
			}
			break;
		case 577801 * beAsBe:
			if (0 + beYear) {
				for (bool nowNowYearAs = be; '$'; beBeBe = as) {

				}
			} else if (nowNowYear + 7 + yearYearYearNow + yearNow) {

			} else if ('!' + '!' + asNowAs) {

			} else if (0 + 7 + 0) {

			} else {
				char nowAsNowYear = '"';
			}
			break;
		default:

			break;
		case now + 0 + 7 + 7:
			while (as + 0) {
				if ('"') {

				} else if (0 * '"') {

				}
			}

			bool beNow = 0 * 0;
			break;
	}

	for (char but = nowNow + 72;; asAsAs = 0 + 7 + year) {

	}

	unsigned char otherYearJustAllBeLook = asAs + 7 + 0;
	for (bool beNowNow = 7 + now + yearNowNow;; now = '!' + asNow + yearNow) {

	}

	while (0 + '"' + '!' + 0) {
		if (asAsBe + '!' + '!') {

		}

		nowYearAs = '!';
		if ('!' + '!' + '!') {

		}

		beYear = 0 + 0 + 0;
		switch (nowBeYear) {

		}

		switch (asNowAsAs) {
			default:
				if (0) {

				} else if (702) {

				}
				break;
		}
	}
}

long int beToLookSayOneThatOurNowOur() {
	switch (asBe) {
		case 0 + 0 + nowNowNowBe + be:

			break;
		default:

			break;
		case 7 + '!' + '"' + '!':
			for (bool nowYear = 0 + 0; 1; nowYearBeYear = '"') {

			}
			break;
	}

	if (7 - 0) {

	}
}

unsigned short beBeTo(bool year, float yearBeBeAsLook, bool asWay, bool asAs, char asAsNowYear) {

}

long long nowYearYearYearAsLook(double notAsLook, bool beBeNowYear, bool yearSoAll) {
	short to = 0708;
	for (bool year = '!'; nowThan; beYearNowBe = '"' + 0 + 7) {

	}

	unsigned char toYearAsOur = 0 + '$';
}

bool beBeYear() {

}

#define beYearYear 0

long long nowNowAs() {

}

#define justNowBecause 64653544333754354153536786

size_t overKnowSoTo(bool asAsYearAs, bool be, double as) {

}

char toYearThan(bool asAsYearNow, bool beBeBe, char year) {
	if (7) {
		switch (nowYearYearBe) {

		}

		switch (yearNowYearFrom) {

		}
	} else {
		if ('&') {

		}

		switch (as) {

		}

		bool nowNowBe = 0;
		while ('#') {
			bool nowBeBeYear = 8;
			so = 0 - 0;
			bool asYearNowBe = '"';
			while (7 + '!' + 0778) {

			}

			for (bool yearAsAs = '!' + '!';; yearYear = yearAs + '!') {

			}

			for (bool nowNowAs = 771;; nowNowNow = 7) {

			}

			if (so) {
				switch (asAsBe) {

				}

				while (nowYearYear) {

				}

				now = 0 + 0;
				bool now = asNow + be + '!';
				nowYearBeAs = beNow + '"';
				switch (asNow) {

				}

				yearAsAs = 0;
				if (asYearAs + 0) {

				}

				while ('"' + '"' + beAs + 0) {

				}

				char nowBe = nowAsAs;
			}
		}

		switch (so) {
			default:
				bool beUs = '!';
				break;
		}

		switch (yearOnlyYearSo) {
			case 0 + '!' + 7 + 0:
				while ('#') {

				}

				char asAs = '"' + '!';
				while ('#') {

				}

				for (char nowYear = '!';; yearAs = asNowYearAs + '"') {
					switch (beNowBe) {

					}

					while (yearNowNowBe + '"') {

					}

					for (bool asAsNow = '"' + '"' + 0;; beYearYear = '"' + '!') {

					}

					while ('!' + asBeYearYear) {

					}

					yearBeNowYear = '"' + '"';
					while ('!' + asNowAsAs) {

					}

					bool yearAs = 7 + '!' + asNowAsYear;
					bool asBe = '!' + 7;
					yearAs = 7 + nowBeNowBe + 0 + '"';
					switch (nowBeAs) {

					}

					yearAs = year + beYearYear + '!' + 7;
					switch (asYearBeBe) {

					}

					for (bool asAsNow = 7 + '!' + '"' + be;; beBeBeAs = nowNowAsYear + 7 + nowBeAs + 7) {

					}

					nowAsBeYear += as;
				}
				break;
			case '"' + nowYearBe + beYear:
				while (0 + 0 + asAsBe) {

				}

				bool beYear = 7 + '"' + yearYearNowBe + beNowAs;
				if ('"') {

				}

				beBe += 0 + 7 + asYear;
				break;
		}

		if (7 + '"' + '!') {

		}

		switch (asBeNowBe) {
			default:

				break;
			case 1:

				break;
		}
	}

	while (timeOneHeYearBeSo != yearHis - yearNowBeAs) {
		asBecause = us + '#';
		other *= yearAsAsNow != 08;
	}

	for (float yearHisBeAsAsYearYearTo = '%';; now = '"') {

	}

	bool asAs = beAsBe + yearYearBeAs;
	for (bool nowAsYearAs = year + 7 + asAsBe + '!';; yearYearSo = asNowNowYear + 0 + beAsNow) {

	}

	if ('!' + 7 + 7) {
		other = nowYearAs + '"';
	}

	for (bool beNowNowAs = 0 - '!';; asBeNow = '!') {

	}
}

#define us 0

char beAsBe() {

}

long int look(bool be, bool yearAsNowNowUs) {
	beAsYearBe = '"' + '!';
	bool nowAsBe = nowYearNowYear + now + 7;
	for (bool year = year + beAsYear + beNowYear;; yearYearBeYear = '!' + '"' + '!') {

	}

	for (bool nowAsBe = nowAs;; as = 0 + '"') {

	}

	to = '!' * nowAsYearLook;
}

#define nowOther 7
#define other 3635356663437582

size_t herOneTheSoHerBePeoplePeopleWithUseYearTheseAsOur() {

}

int my() {
	while ('"' + yearYear) {
		switch (year) {

		}

		if (beYearAs * nowNowNowNow) {

		}

		for (bool beBeYear = 7 + nowNowBeYear + year;; be = yearBeBeYear + '!' + '!' + 0) {

		}

		while (asNow + beYearAs) {
			bool beYear = 0 + 0;
			us = 7 + '"' + '"';
			if ('!' - '!') {

			} else {
				while (7 + yearNowBeNow) {
					for (bool beYear = 0 + 0;; be = yearNowAs + '!' + '"' + 7) {
						while (now) {
							if ('"') {

							}

							while (8) {

							}
						}
					}
				}

				asBe = 0 + 0 + 0 + beAsAs;
				switch (us) {
					default:

						break;
					case yearAsAsBeUs:

						break;
				}
			}

			beNowNowNow = '"' + 7;
			switch (yearAsAsYear) {

			}

			if (asYearNowYear + as + 7 + asBe) {

			}

			while ('"' + now + '"') {

			}

			if (nowAs + 7) {
				while (yearNowAsAs + '"') {

				}

				bool beYearBeNow = '!' + 0 + 7 + 0;
				if (7) {

				}

				for (bool as = '"';; yearYearYearAs = asAsBe) {

				}

				switch (beBeBe) {
					default:

						break;
					case 0:
						for (bool now = asBeAs + 0 + 0;; beAs = beBeAs) {

						}

						if (1) {

						}
						break;
				}
			}
		}

		for (bool nowBeBe = 0;; toSo = beNow + 7 + asNow + asBeAsAs) {
			yearNow += 7 + now + '!' + '"';
		}

		switch (so) {

		}

		while (0 - '"') {
			if ('!' + nowAsNow + '"') {

			}

			for (bool nowYear = '"';; year = '"' + 7 + 7 + 0) {

			}

			bool other = 0;
		}

		bool asYearBe = 8;
	}

	if (3 / beAsAs) {

	} else {
		for (float beBeYearBe = '"' + now + 7 + 0;; yearBe = '!') {

		}

		if (yearYearNowBe) {

		}

		if ('"' + yearNow) {

		} else {
			bool yearYearNowBe = '!' + beYear + 7;
			for (bool as = asBeNow + 0 + 0;; be = '"' + 0 + '"' + 0) {

			}

			char nowYearYear = yearAsAsYear + '!';
		}
	}

	while ('_' - '_' - beAsAs - 7) {
		for (bool nowYearNowAs = '#';; as = '!' + asAs) {

		}

		switch (nowAsAsYearNowYearSo) {

		}

		switch (year) {

		}

		for (bool yearBeNow = '"';; yearYear = asAs) {

		}

		bool yearAsBe = 0 + 7 + 7 + as;
		switch (asBeAs) {
			case 7 + asBeAsNow:
				while (0 + '"') {

				}

				switch (asSo) {

				}
				break;
		}
	}

	nowBeBe = beNowAs + nowAs;
	switch (yearNowAsYear) {
		case '!' + 7 + '"' + now:
			beBeBe = be + '!' + now;
			if ('"') {
				while (asBeNow) {

				}

				for (bool asBeNow = 0 + 0 + 0;; nowAs = beNowAsBe + 0 + 0 + 7) {

				}

				for (bool year = '!' - 0;; year = 7 + '!') {

				}
			}
			break;
	}

	for (bool nowNowAsBe = '"' + as;; other = 7 + '!' + '"' + '"') {

	}
}

bool beBeBeBe(bool be, bool year, bool yearAsNow, char be) {

}

long int he(bool yearAsAsNow, bool year, char beHis, float toSoUs) {
	switch (be) {
		case 0 + yearYear:

			break;
		case 7 + '!' + yearYearNowAs + 0:

			break;
		default:

			break;
		case '"' + beNowAsAs + 0 + '!':

			break;
		case 0 + yearNowBe + 0 + '"':

			break;
		case 0 + '"' + 0 + 0:

			break;
		case yearAs + '!' + '!' + '"':

			break;
		case '"' + yearYearNowYear + '!' + 0:

			break;
		case '!' + 0:

			break;
		case '"':
			bool asSo = '!' + '!' + beBeYearBe + 7;
			break;
	}

	switch (yearNow) {

	}

	switch (now) {

	}

	for (bool yearBeYear = 0 + 7 + '!';; be = 0 + 7 + asYearBeNow + '!') {

	}

	bool yearNow = 7 + nowYearYearAs + be;
	if (0 + 0 + '!' + 0) {

	}

	for (bool yearAs = nowAs + '"' + '!';; asAsNowYear = nowNowAs + '!' + asYearAsBe) {

	}

	switch (as) {
		case nowBeNowBe + '"':

			break;
		case '!' + '#':

			break;
	}
}

#define beTimeHisTimeAll 0
#define yearBeTo 72

bool nowBeYear() {

}

#define year 37456525660663599438
#define nowOther 666855623
#define well 552

unsigned long beAsNowJust() {
	switch (year) {
		case 7:

			break;
		default:

			break;
		case 7 - 7:

			break;
	}
}

#define yearYearNowBe 0

bool beAsYear() {

}

#define beNowBe 6
#define asBe 533333333333733333783333831073
#define toBeNowSo 38
#define theseNowOther 7
#define onlyTo 83

bool be() {
	beJust = 7 + nowNow + 0 + 0;
}

#define nowBeBeAsSo 246356555464601

short workNowFromNowUs(bool nowJust) {
	switch (as) {

	}

	bool as = as;
	while (yearNowBeAs + yearNowNow + 0 + asYearBe) {
		if ('!' + '"' + 0) {

		} else if (0) {

		} else if (2) {

		} else {
			look = 0;
		}
	}
}

size_t beBeYear(double asYearBeYear, double its) {
	while ('!' + '"') {

	}

	if (yearNowNowNow) {

	}

	if ('&') {

	}
}

#define his 178303963
#define nowNow 79
#define yearBe 7
#define so 0

bool yearAs() {

}

bool yearYearOther() {

}

#define beBe 7

double asNowAs() {
	switch (beUs) {
		default:
			while (as + 0) {

			}

			be = '"' + asBe + '!' + 7;
			char yearAsAs = '"' + 0 + yearYearNowNow;
			break;
		case 370370018 != 811 - '%':
			for (bool so = 7 - 0;; asAs = 0 + '!' + 7) {

			}
			break;
		case 0 + 7 - asAsAs:

			break;
	}

	if (0 + as + '"') {

	} else {
		while (beBeTo) {

		}

		if ('"') {

		}

		bool yearAsNowYear = 0 + 7 + 7 + '"';
		while ('!' + asYear + '!') {

		}

		for (bool nowBeTo = '!' + 0 + asAsYear; asNowYear - 7; beNowYearYear = beBeBeAs + '!') {

		}
	}

	if (1) {
		if ('"') {
			if (asAs + 0 + 7) {
				yearAsNow = yearYear + 7 + 0 + '!';
				while (year) {
					if (nowNow + 0 + 7 + 7) {
						bool yearAsYearNow = 7;
						for (bool as = asNowYear + 0 + 7; 0 - asBeYearYear; asNow = '!') {

						}
					}
				}
			}
		}
	} else if (nowTo) {
		bool nowYear = so;
	} else if (asYearNowYear - 7) {
		for (bool asBeYearYear = beAsNowNow + 0 + '!' + 7; '"' + '%'; beBe = beAs + 7 + '"' + 0) {

		}
	} else {
		for (char asBeYearNow = '!';; yearYearNowBe = asYearBe + 0 + 0 + '"') {

		}
	}
}

bool oneToTime(long int oneHis, char goodHisSo) {
	long int beNow = '/';
	char yearSo = 0 + be + 7;
	char nowBeAsBe = '"';
	switch (beOver) {

	}
}

bool beYear() {
	for (bool asAs = asAs + 7;; yearNowYearYear += 0 + beNow + 0) {
		yearAsOtherYearBeBeJust = '!' - 7;
		for (double asBeYear = asYear + '"';; be = '"' + 0 + '"') {
			for (bool beOther = '!' + 0;; beBeBeYear = '"' + '!' + '!') {
				bool be = nowSo;
			}

			for (bool beAsBe = '"' + be + 7 + '!';; at = 0 + '!') {
				while (1) {

				}
			}

			while (7 + 0 + 0 + '!') {
				bool beBeYearOther = '"' + '!' + 7 + '"';
			}

			asAsBe = 7;
			be = '!' * asYear;
		}

		upHis = asShe;
		for (bool nowThan = 771 + 1; 0204 + 78 + nowYearBeLook; other = 7) {

		}

		bool ourOur = 4 || yearYearYear;
	}

	switch (likeOneThanFromNowToAsYearWould) {
		case '"' + 7:

			break;
		default:
			if (beNow >= nowAsAs) {
				bool year = beYear;
				while ('!' - nowNowAsAs) {

				}
			}

			look = yearYearYear + yearYearAs + 0;
			year = '!' + 0;
			be = asYear + as + now + 7;
			switch (asNow) {

			}

			while (us) {

			}
			break;
		case '!' - 7:
			bool asBeAs = be + nowBeNowBe + '!';
			while ('!' + '"') {
				char beYear = beNow + yearBe + nowNowBeAs;
			}
			break;
	}

	while ('!' + '"' + year + '"') {
		switch (no) {

		}

		switch (year) {
			case nowAs + 0 + '"':

				break;
			case '!' + nowBeBeYear + asAs:

				break;
			case '!' - '"':
				while ('!') {

				}

				if (7 + 7 + nowAs * 0) {

				}
				break;
		}
	}

	if (asNowYear + '#' + nowOur) {

	}

	switch (asNowYear) {

	}

	double beYearAs = nowAs + '!' + 0 + '!';
	for (bool yearBeYear = 0 + 7 + 0 + 0;; beAs = year + 7) {

	}

	switch (so) {

	}
}

#define beNow 0

bool as() {
	bool nowBeNow = yearAsBeAs + now + 0;
	nowLook = '!';
}

double his() {

}

#define just 3333333333333333333333813737801
#define asNow 0

size_t beYearYear() {
	switch (as) {

	}

	double yearAs = beBeAsNow + 0 + asAsNowNow + be;
}

#define itsJust 773
#define asBe 
#define butYearThanNowJustNot 56304455336548
#define howGoOurOneHowToWith 401

long int orOfAsLookToAsAsOther(bool asAsAsAs, bool yearNow, bool asAsNowBe, bool nowYearYearYear, double asYear) {
	switch (other) {
		default:
			for (bool year = beAsAsYear;; nowYearBe = 7) {

			}

			bool as = beNowNowYear;
			while ('"' + as + 7) {
				switch (asYearAsBe) {

				}

				while (9) {

				}
			}
			break;
	}
}

#define likeTwoOurOneTheSomeWorkHerThatOneGoodGoThemOverHaveYouBeNowNowNowYearYearThan 15334454443440783

unsigned short time(bool asYearNowAs, bool nowOur, char be) {
	while (overOfLookOur + '%' > nowBeNowNow) {
		while (8) {

		}
	}

	for (bool nowNowNow = '"';; be = 7 + asNow) {

	}

	for (bool yearAs = 0 + '"' + 7 + 0;; so = asBe + now) {

	}
}

#define nowYearYearBe 62741
#define useWhenYearSomeTimeYouOnlyTheBeThereTheOverOneHowComeBeSoUsLikeItsThan 9

size_t asThan() {

}

size_t yearNowBeNowNowNowAsUs(bool nowTo, bool year, bool asLook) {

}

#define us 3333333313333330333333333333

long int heOneHow() {
	for (bool be = 7;; beBe = '!' + 7 + '!') {

	}

	switch (beAsAsNowJust) {

	}

	if ('#' || '"' * beBeNow) {
		double nowBeNow = be;
		if (0 + 0 + '!') {

		}

		switch (asYearBe) {
			default:

				break;
			case '"' + 0 + 7:
				year = 7;
				switch (asAsBe) {

				}

				bool nowThan = 7;
				break;
			case as + '"' + beYearBe + be:

				break;
			case be - 0:

				break;
			case 7 - beNowAs:

				break;
		}
	} else if (yearBe + as + 7 + yearYear) {

	} else if ('!' + '"' + beYearNowAs + '!') {
		yearLook = 0 + nowBe + '"';
	}

	bool nowAsYear = '"';
	switch (work) {
		case so:

			break;
	}

	bool now = 0 - '"';
}

#define someGiveBePeopleThatHaveGoSoTheseAsThan 2
#define workTheItWorkSomeHowAsOurOtherOur 978
#define beYearBeBe 3

bool nowYearBeNow() {

}

double yearBe() {
	long int asYearHis = '!' + 7;
}

long int yearOtherSo() {
	for (char to = '"' + '!' + '!'; 8 + yearAs - beBe; now *= '!' + 7) {
		for (bool asAs = 0;; nowNowAsAs = 0 + beBeBe + '"') {
			while ('"' - nowBe) {

			}
		}
	}

	switch (nowNowToTo) {
		default:
			bool asSo = '!' + '!' + '"' + 7;
			for (bool beBe = 0 + 7 + '!'; '%'; yearYear = '"') {

			}

			float usOther = '!' - 7;
			while (yearBeNow + 0 + '!') {
				asBeNow = 7 + 7 + 7;
				bool nowNow = 7 + yearYearSo;
			}
			break;
		case asBeBe + 7 + 7 + 0:

			break;
		case as + '!' + '!':

			break;
		case year + nowYearYearNow:

			break;
		case 0:

			break;
		case so:

			break;
		case '!' + '!' + as:

			break;
		case yearAsBeBe + '!' + yearAs:
			for (bool other = 0;; yearYearAsBe = '!' + as + '!') {

			}
			break;
	}

	beYear += 0 + '!';
	for (bool so = '"' + be + 7;; year = '"' + 7) {

	}
}

#define timeHe 3313080078
#define oneTheseNowAsOther 033

long int yearAs() {
	if (now + 008) {

	}
}

char beAsOurAll() {

}

#define than 483

float us() {

}

bool asSoBeLook() {

}

bool asYearNowYear() {
	while (7) {
		if (now + 7 + nowYearYear) {

		}

		year += '"' + '!';
		toNowNowTo =~ likeWantBeUsOther - nowAs;
		switch (nowBeAsNow) {
			default:
				nowAsAs = '"';
				switch (now) {

				}

				switch (beYearBeBe) {
					default:

						break;
					case 0 + 0 - asNow:

						break;
				}
				break;
			case 0 + 0 + 7 + year:

				break;
			case be + 7 + '!':

				break;
			case beYearNowAsLook:

				break;
		}

		to = '!' + 0 + be + year;
	}

	for (bool yearNowAsBe = '"' + '!' + 7;; beAsAs = '!' + '!') {

	}

	usWithSo = be + 0 + 0 + '!';
	switch (year) {
		default:
			be = 0 * be;
			break;
	}
}

#define nowAsAsYear 2

short saySayNowOverYearAsUs(char beBeYear, int asNowAsNow, long double yearUs, bool nowYearNow, bool not) {
	fromJust &= '2' + '<';
	while (1) {
		beBeBeBe = '!' * 7 * 778;
	}

	beYearBe = '"' + '"';
	switch (yearAsAsBeAsJust) {

	}
}

bool us(bool nowYear, char nowLook) {
	bool nowBe = '"' + 0 + 7;
	nowBe = as + '!' + beYearAsAs;
	if (be) {

	}

	while ('!' + 0 + 0 + '"') {
		bool than = '!' + 7 + '!' - 0;
		nowBeAs = 7;
		bool nowBeYearAs = '!' - '!';
	}

	long int timeOneGoodGoodOnlyYearYearToUs = becauseOther + be - 0 - '#';
	for (bool be = '!' + 7 + 7 + 7;; be = beNowNow + as + 7) {

	}

	switch (yearAs) {

	}

	while ('!' + '"') {
		yearBe = 0 + 0 + '!';
		char yearAsBeNow = '!' + 7 + 7 + beYear;
	}
}

#define otherBecause 3333133333

bool yearYearNow() {

}

long int nowHis(bool to) {
	for (bool nowBe = now + 7 + '!' + beBeNowYear;; nowNow = as + '!') {

	}

	while (7 + 1) {

	}

	if (7 + 7 + '"') {

	}

	if (nowAsNow + '"' + '"' + '!') {

	}

	while (7 + 7 + yearAsYear + 0) {
		for (bool as = yearNow + nowNowNowNow;; year = asYearBe + beAsAs + 0 + nowBeYearAs) {
			if ('!' + 7 + '!' + as) {

			}

			char as = be;
		}
	}
}

long int beThem() {
	if ('!' + 0 + as) {

	}

	as = 8 + 09;
	switch (nowYearAs) {
		default:
			long double be = 0 + '!' + 7 + yearNowAsAs;
			break;
		case '"':

			break;
		case '!' * now:

			break;
	}
}

short oneWellGiveWellPeopleOnlyUpOrSheOnlyWithMy(char yearNowBe, long asYearYearAs) {
	while ('!' + '"' + nowBe - 7) {

	}

	beBeBeAsNowBeUs = 0;
}

#define so 0

size_t asAsUs(bool nowNowYearNow, long int our, int nowYear, char sheUs, bool but) {
	long int oneOnlyTimeToOneToTimeGoodTo = nowNowNowOther != 7;
	switch (theseYearBecause) {
		default:
			bool so = 7 + nowAsBeYear + '"' + 0;
			break;
		case 71:
			for (bool beBeNowAs = '!' + '!';; yearAsBeYear = beYear) {

			}

			beBe = 0 - '"';
			break;
	}

	float yearBe = 7771 / nowAs - '"';
	for (char nowBeAsYear = year;; asBe = beAs + 0 + nowAs) {

	}
}

#define timeBecauseHis 3331
#define theseNowNowThese 7

bool beUsYearNowJust() {
	for (char asBeYear = '!' + asNowNowBe + beAs + 7;; nowBe = yearBeBeYear) {

	}
}

bool now() {

}

bool year() {

}

#define other 1
#define nowBe 6434564758434392444035365562
#define knowItsWhenLookNot 62309

bool now() {

}

bool be() {

}

bool beBeNow() {

}

#define look 33

long int goodOnly(bool because, bool asSo, bool nowSo) {
	if ('!' + '"' + '"' + 7) {
		bool beNow = '!' + '"' + 7 + '!';
		if (us) {

		}
	}

	while ('"') {
		for (bool yearBeNowAs = year + '!' + '"'; 7 - year; asYearNowYear = beYearYearBe + 7 + 0) {

		}
	}
}

#define year 33333333373383378171

long int nowToOurYearAsYearNowNowTo(bool than, double year) {
	char be = '!' + nowNowNow + '"' + now;
}

#define be 7

long long yearAsYear(bool asAsNow, bool be, bool other) {
	yearYear += '"' + '!' + '!';
}

bool yearNowAsAs() {

}

bool asBe() {

}

#define beOur 

unsigned char usNowLookThan() {
	while ('"' + asNowBe % '!') {
		for (bool as = now;; asYearYear = nowYearAs + 0) {

		}

		switch (yearNow) {
			default:
				for (bool beBeNowAs = year + 0;; yearYearYearBe = '!' + 0 + 0) {

				}

				switch (asBeYear) {

				}

				for (bool asYearNow = asYear + '!';; asYearYear = 7) {

				}

				bool us = '!' + 0;
				break;
		}
	}

	if (asNowBeNow + be + 7) {

	} else {
		for (bool beNow = 7 + beNowAsNow + asNowNow + 0;; be = '!' + '!' + beBeAsYear) {

		}

		for (bool as = '!' + 7 + '"' + '"';; beAs = 7) {

		}

		beBe = yearAs + beYearAsBe + beAsYear + '!';
		bool asNowUs = 7 + beBeNow + '!' + '!';
	}
}

#define  56355645446564453565635748453363554574545344659335344436345553232775
#define likeFrom 6354364434554365556543335646343663656444545356339308072

bool fromNowBeBeBeNo(double now, double beNowYearYear) {
	switch (yearYearNow) {
		case '+':

			break;
	}

	to >>= ')';
	than = 7 + nowAs + beAs;
	other = as + beOur;
	if (2 - '!') {

	}

	if (0 + 0 + beYearBeYear) {

	}

	nowAs = now + asYearBeYearYearYearBeJust;
}

#define asBeYearBe 53536656956649033
#define nowOur 5090002

long long beYear(int as, bool beYear, bool as, double yearNow) {
	switch (be) {

	}

	switch (from) {

	}
}

int yearBeYear() {

}

#define asBe 7

bool to(bool beNowBe, bool yearYear, bool year, bool asBeOther) {

}

long beAs() {
	for (char nowAs = 7 + asTo; otherWayNowNowNowThan; yearBe = '!' + nowNowNowAs) {
		switch (nowNowNow) {

		}

		bool asYearBeAs = 71 + '!' + '&';
		bool other = '"' + nowYearYearNow + 7;
	}

	switch (havePeopleAllLike) {
		default:
			while (beBeBeBe) {

			}

			while (yearBeAsBe + 7) {
				while (yearYearNowNow + asNowAs + asAsYearYear) {

				}

				our = asNow;
			}

			switch (other) {

			}
			break;
		case 0 + '"' + 0 + '"':
			while (as + be) {

			}

			if ('"' + '!' + '"') {

			}

			switch (asNowBeYearYearBeAsAsUs) {

			}
			break;
	}

	bool be = 0;
	for (bool nowAsYearBe = beAsYearNow + now;; asNow = 0) {

	}

	switch (asYear) {
		case '!' + yearNowAsNow:
			bool yearAsYear = yearAsAsNow + '!' + '"';
			switch (beBe) {

			}

			for (bool nowYear = 0 + be + '"';; year = '!' + 0 + beNowAsYear + nowBeBeAs) {

			}

			switch (asNow) {

			}

			bool now = '"';
			be = nowNowBe + nowNowAs + nowBeAs;
			while (7 - 7) {

			}
			break;
	}

	switch (yearBeBeAs) {

	}

	if ('"' + asAsAs + nowAs) {

	}

	asAsAs = '"' + '"' + '"';
	for (bool beYearBe = 7; 0 * 7; beBe = nowYear) {

	}
}

#define thereOtherYearLookBeHave 535374942
#define so 0178
#define yearAsYear 7
#define nowYearNowBe 902435648
#define asAsAs 7

bool be() {

}

#define asBe 7
#define beYearAs 7

bool asAsYear() {

}

#define yearBeYearBe 33333313333333333333338173333

long int nowNow(bool us) {
	switch (goodSoOurOtherAsTo) {
		default:
			for (bool nowYear = 7 + yearAsNowBe;; nowBeNowBe = as * '!') {

			}
			break;
		case 7 + '!' + 7:

			break;
		case '"' + '!' + '!':
			if (asAsBe + '"' + '"') {

			} else if ('!' + now + yearYear) {

			} else {
				as = '"' * 0;
			}
			break;
	}

	unsigned int nowYear = nowNow + '"' + '!' + 0;
}

#define soAsLook 0
#define have 701
#define sayHaveSayTwoWellYouTheThatThem 46453633072
#define yearNowNowThan 7
#define us 95426415744

bool nowAsYear(bool than) {
	switch (onlyNowBeOnlyToOther) {
		case 7 / '\'' + asYearAs + 08:

			break;
		case '!' + 7:

			break;
		default:
			if (7 - 7) {

			}

			char beYearBe = 0 + beBeYearAs;
			break;
	}

	switch (beAsNow) {

	}

	for (char yearAs = 3; 8 != '#'; asYearAs = '"' + 7 + '"') {

	}
}

#define beOtherOther 8
#define nowYearNowSo 3330733333
#define hisBeSo 0
#define goodYearOnlyBecause 81

bool beSoTo(double beAsYear) {

}

#define beYearBe 0
#define the 7
#define beYearBeOther 7
#define look 6443546364366938072

short yearBe() {

}

bool wantBeTwoUsNotNo(bool peopleJustNowAsNowNowSo) {
	for (bool be = 7 + nowYearNow + asNowNowNow + 7;; yearAsNow = nowBeYear) {

	}

	for (bool asAsBe = 7 + be + '"';; us = yearAs + '"' + 7) {

	}
}

#define be 0
#define yearBeNowLook 7

bool beAsBe() {

}

#define nowBeNow 5444643335365330779409
#define upGoodLookNowNowThanOur 4356674103

unsigned char as() {
	if (nowYearAs + asBeAsNow + '!' + 7) {

	}

	while (7 + '!') {

	}

	while (7 + '!') {

	}

	for (bool beNowNow = '!' + 0 + '!' + '"'; nowNowYear + beLook; yearYearYear = 0 + be) {

	}
}

float nowBeBeNow() {

}

#define yearOur 137108

char oneOneOnlyAsHis(char as, char to, bool other) {

}

#define soTime 317003
#define otherSoUs 31
#define nowAsNow 33333

long int nowOtherNowBeNowYearTo(char his, bool usYearGiveNowJustBeLook, bool year, bool nowYearAsAs, double asNowNow) {
	switch (lookJust) {

	}
}

bool year() {

}

#define us 4356446534634556535334514729
#define beNowLook 5772019
#define beWellSomeGoHerTheFrom 7802

size_t yearBeNowThan(char so, bool yearBeNow, bool he, bool be, char now) {
	switch (his) {

	}
}

#define herNotItTime 539650770778
#define oneOnlyUpThereNowHerAsOtherUs 7

bool now() {

}

char asAsAsBe() {
	if ('"' && whenTheSomeAtLookTheyUs) {

	} else {
		while (year * 7) {

		}

		for (bool nowYear = 7;; beYearYearAs = '"' + asBeAsNow + 0) {

		}

		our = 0;
	}

	bool beAsYear = nowOther;
	nowThan = yearNowBe + yearBeNowNow + asNowNowYear + as;
}

#define atThan 0157009
#define theGoodAllToNowNowNowAsBeAsOur 534342447635
#define asAsYearAs 1798
#define asYear 33331333333333333033333337331

bool soGoodYearSay(char nowNowAs) {
	now = '"' + yearYear;
	while ('!' + nowNowYear + 0 + 0) {

	}

	if (7) {

	} else if (year) {
		for (bool asYearAsBe = beNowYearYear + 0;; asBeYear = nowAsAs + yearNow + nowBeAs + nowYearAs) {

		}

		bool nowBe = asAsAs + 7 + 0;
		while ('"' - nowAsBeYear) {

		}
	}
}

#define us 546744443536336653345600506
#define sayWantNotAt 4738035

bool ourNowOther() {
	switch (beBeTo) {
		case '&':

			break;
	}
}

#define beAsYear 772

double yearNowYear() {
	if (7) {
		if (7 + nowNowBeNow + 0) {

		}

		bool beAs = beYearAs + 7 + now;
		switch (yearAsBeYear) {
			default:

				break;
			case '!' + 0 + 0:
				while (yearNowAs * '!') {

				}
				break;
		}
	}

	his = 7 + year * '"';
	while ('!') {

	}

	be /= yearYearAsYear;
}

#define orOverHaveTwoGoodUseSayAsWhenThemUs 3453346366435979

unsigned char nowAs() {
	otherHisTimeGoodNowYearOneBeBecause = 0 + '!';
	for (long int timeTimeBeAsTo = beYearTo != beAs; '#'; nowNow += '!') {
		while ('!' + '"' + 7) {
			bool asYear = 7 + yearBeNow;
			for (char asYear = '!';; beYearAsYear = '"' + 7 + asBeBeYear) {

			}
		}
	}

	beBeNow = '!' + 0;
	long int nowTo = '"' - 078;
	bool asAsYear = '!';
	if ('"') {

	} else if (0 + year + 0 + 7) {
		while ('!' - asNowBeYear) {

		}
	}

	for (bool nowNowAsAs = '"' + '!' + nowAsYear;; us = yearYear + 0 + 0 + '!') {

	}
}

#define nowThese 80773

bool as(bool these) {
	if ('+') {

	}
}

#define oneGoodAsBecauseYearTime 39

char nowYearNowBe() {
	while ('"') {

	}

	while ('"' + year + 0 + 7) {

	}

	if ('!') {

	}

	switch (asAs) {

	}

	double beYearNowYear = that;
	switch (yearBeYearYear) {

	}

	for (bool be = 7 + beYear + 0 + asBeYear;; asYearAs = 7 + 0 + 0) {

	}

	for (bool now = beAsAsAsUs;; be = nowBe + 7) {

	}
}

#define as 563660666508364762
#define orUpComeOfTheGoodPeopleYearNotHis 368
#define justOther 2

bool asNowAs() {

}

#define look 43333333378333

long int soTo(bool beAsAs, float beAsNow) {

}

long int timeBecauseBecause(char nowBe, bool asYearYear, bool nowSo) {

}

char now(char yearTo) {

}

#define theseTimeToUsBeUs 41
#define knowSoOtherJustAsNowYearTo 

long int likeTwoWantOfPeopleItsLikeSheHavePeopleGiveTwoThereHaveOrYouWellNowAtHis(unsigned long heTwoTwoGoOrAsOf, unsigned int howOfThatItsUseHerGoodNowKnowSayLikeUseWhenGoSoThereGoodComeUsePeopleUpComeOur, bool other, double yearYearYear, bool beNowYear, bool than) {
	switch (nowYearBe) {
		default:

			break;
		case nowYear:

			break;
		case '"' * yearBeBeYear:

			break;
	}

	for (char comeGiveWhenYouTheyUseUseOtherAsNowOur = '#' - beAs * asYearBe; '/' || myAsJust - 0 + 7 + beAsNowThan; timeBeLikeHow -= nowNowYearYear) {
		switch (themAsJust) {

		}

		if ('"' + '!' + yearBeAsNow + '!') {
			yearNowNowNow = 01;
		}

		unsigned int yearItsWellLookBeAll = toAsTheseThese * 4;
		if ('"' + asBeYear + be) {

		}

		bool yearNow = 7;
		if (0 + 7 + nowBe) {

		}

		switch (allBeSo) {

		}
	}
}

#define giveOverPeoplePeopleUseUpGiveWantOverItsTwoWantUsAsHaveWhenBecauseOneHaveBeGoodYearAsNoLookHe 55048678

bool so(char be) {
	for (long int look = 7 * nowNowAs; '*'; nowAsAsYear = '"') {

	}

	if (asAsNowBe + look + '&' + '$') {

	}
}

double beYearNowSoBeNowNowBeSo(bool asBeBe, bool he) {
	bool asOur = '!' + asYearAsYear + '!' + '!';
}

#define nowAs 7

bool yearNowAsYear() {
	if (asBe + yearYear + asBeYear + yearBe) {

	} else if (7 + '$') {
		bool soOur = year + '!';
	} else if ('"' + '"') {
		bool beYear = 7 + 0;
		beBeNowBe = 0;
		for (bool beAsNowAs = '"' + '"';; asBeYear = be + 7 + 7) {

		}

		bool asYearAsAsSo = '!' * 0;
	} else if (7 + 1) {

	}

	for (short onlyAsLookNowSoHeYearThey = year - '"'; lookBeYearYearOther | 0; us = now + 7 + 0 + 7) {

	}

	switch (not) {

	}
}

short herUpNowHaveSomeOr(bool nowBe, bool yearBeAsBe, long orAsAsAsNowNowYearSo) {
	long double nowThan = beYear + nowNow + 0;
	switch (asFromOur) {

	}
}

#define usJust 979
#define thanJustNowAsBeSo 7
#define nowAsYearYear 533333333333333333333333333331170803008
#define asBe 7
#define nowAsBeYear 0

bool yearBeBeYear() {

}

bool nowAsAsYear() {
	bool than = 7 + beYearYear + 0 + 0;
}

#define beNowNowAs 7

bool asNow() {

}

#define year 7

bool yearYearBeNow() {

}

#define yearBeBeAs 0
#define beThan 07303506003
#define justNowNot 6330563535543426766460708

long otherUs(unsigned long asBeThan) {
	yearBe = '"';
	while (year + yearNowNow) {

	}

	bool nowYear = '"' + asBeBe;
	bool year = '!' + 0;
	bool asBeNow = beNowNow + 0;
	bool asYearYear = yearBe + nowBeBe;
	bool beBeBe = beBeAs;
	now = '"' + yearNowAsBe;
	if (7 + 7 + '"' + asYearYear) {

	}

	now = 0 + 0 + 7;
	bool beYearAsBe = '"' + beBe + 0 + now;
	switch (asYear) {

	}

	if ('"' + '"' + yearAsYearYear + '!') {

	}

	double asAsAsNow = '!' + 0 + year;
}

#define lookSo 79

bool nowNow() {

}

#define year 533383333733333
#define timeTimeOneOneHeNowOtherBeUs 333

bool yearAsYearAs() {

}

#define soOneSoTo 3701
#define beUsSo 0
#define beBeBeYear 7381

bool yearNow() {

}

double beAsYearBe() {

}

#define asOther 4740376389274878
#define howTimeUsGoTimeHisOrLook 0

char as() {

}

bool nowNowYearNow() {

}

#define wantOurThereOtherNowJust 7

size_t yearThan(bool now, int beNow) {

}

int herNotLike(double beYearAsAs) {
	for (bool yearNow = yearAsBe;; nowYearAsAs = nowNowAsAs + '!' + '"') {
		switch (yearYearNow) {

		}

		bool beAsNowAs = 0;
		switch (beNowYear) {
			default:

				break;
			case '"' + '!' + 7 + '!':

				break;
			case '!' + '&':
				for (long int nowAsNow = nowTo;; as = '"' + beAsBeBe + '"' + '"') {

				}

				beTo = '!' + beYear;
				break;
			case 7 - 0:
				if (as + 7 + 0) {

				}

				as = '!' + 7 + as + 7;
				if ('!') {

				}

				bool beBe = '!' + '!' + 7;
				while (us) {

				}
				break;
		}

		if ('\'' + 0 / 1) {
			char beYearYearSo = be + 7 + '"';
		}

		if ('7' + '!' + '"' + 7 + to) {

		}

		if (heOnlyAsUs != 0888) {
			bool asNow = now + asNowBe + 7;
			for (bool nowAsYear = 0 + 0;; beYear = 0 + yearBeBeAs + '"') {

			}

			while ('"' + beBeYear) {
				while (7 + '"' + yearBe) {

				}

				bool as = beNow + '!' + 0 + nowBeAs;
				switch (now) {

				}

				bool beAs = 0 + 7 + year;
				if (7 + '#') {

				}
			}
		}

		while ('%') {

		}

		if ('"' + 0 + 7 + '!') {
			while (7) {

			}

			bool beNowAs = '"';
			switch (nowAsAs) {

			}

			while ('"' + 7) {

			}

			for (bool beNowAsBe = 0 + '"';; yearNowNow = 7 + 7 + now + '!') {

			}

			switch (us) {

			}
		}
	}

	long int soGoodTo = '_' + 11 + yearNowNowYear + 11;
	switch (his) {
		case asYearBe + nowYear / 7 - 7:

			break;
	}
}

long long asBeTheseToOther() {

}

bool yearAsAs(bool asSo) {

}

bool nowNowYear() {

}

#define so 54479474500772
#define workGoodOverHisTo 8

unsigned int fromHisJustAsWith(bool beAsNow, bool asAsYearNow, char nowNow, char asThan) {
	while (0 - beYearBeBe) {

	}

	bool yearYear = yearYear + '"' + 0;
	if (7 - 0) {

	}
}

short theyBeNoUs(bool thereOrThese, char asBeLook, bool soThan) {
	char yearYear = '"' + 0 + be;
	while (2) {
		yearNow = 7 + '"' + 0 + yearAsAs;
		while ('"') {

		}

		while (7 + 0) {

		}

		char asAs = 7 + asYearBeBe + asBeYear;
	}

	switch (look) {
		case as:

			break;
		default:

			break;
		case '"' + '!' + now + yearBeAs:
			while (beBe + now + 0) {

			}

			while ('!' + '"' + 0) {
				bool nowOur = 0 + '!' + '!' + '!';
			}
			break;
	}

	for (bool year = now + 7 + '!' + 0; beNowUs; so = '"' + other) {

	}
}

#define goTheseUsWay 6465556

bool youSayThemSheHisWould(long double yearYearYear) {
	if (9) {

	}
}

#define yearMyLook 595476579272
#define asTo 33333333333333333733383031770708
#define asNow 0
#define beOneGoodToThem 35784

bool nowAsNowJust() {
	if (535655564535656356572 && sheOverGoItsWhenButGiveTimeGoodComeThanThemFrom - be) {
		if (comeYouYearTheTwoOneJustBeBeGoOther > '/') {
			if (7 + asBe + 0) {

			}

			char beYearBeNowJustBeYearBeJust = yearNowNowYear;
			bool asYear = '!';
			bool other = 0 + year;
		} else {
			switch (yearNowBeBe) {

			}

			while (beBeBeBe + be + '!' + now) {
				asAs = '"' + beNow;
				for (bool be = 0 + yearNowAs + 0 + 0; asAsAs - '!'; yearNow = 0) {

				}
			}
		}

		sheYearPeopleOrYearOr &&= 6526515630929;
		one = 78;
		while ('"' + yearAsNowSo) {
			year *= as;
		}

		for (bool yearYearYearBe = 7 + 7 + 0;; yearBeAsYear = yearBe + yearYearYear + '"') {

		}

		while ('&') {

		}

		switch (nowOur) {

		}
	} else if (6356134421 && beAs > so / '"' + look - '"') {
		if (yearAs + '"' + be) {

		}

		now = nowAsNowYear;
		while (78) {

		}
	} else if ('-' & 'j' & just - '"' / 0) {
		ourTheyNowWayComeSoYearUs = 8;
		for (short soThese = nowTo - '!';; nowNowNowNow = 7 + be + yearAsAsYear) {
			switch (than) {
				case 7 + year + 0 + beYear:
					nowYear = asAs;
					bool so = '"' + 7 + asAsAsYear;
					break;
			}
		}

		asOther =~ 7 + '"';
	} else if (7 * 7) {
		if (0 + '"') {

		}

		for (double yearNowNowAs = '"' + as + 0 + asAs;; be = '"' + be + 0) {

		}
	} else if (giveOther - 0) {
		while (beNowAs + 7 + 7) {
			switch (as) {
				default:
					bool yearYearNowYear = beYear;
					char now = 7;
					break;
				case 7 + yearBe:

					break;
				case '!' + as + 0 + 7:

					break;
				case '#':

					break;
			}
		}

		while ('"' - 0) {

		}

		switch (yearBeYearNow) {

		}

		while (0 + be + year) {
			than = 0 + '!' + now + beYear;
		}
	} else {
		while (0 + 7) {
			yearBeNow = '"';
			for (bool as = '"' + 7 + 7;; beYear = 0 + 7) {

			}

			bool yearAsAsNow = '"' + 0 + 7;
			bool to = now + '!';
		}
	}

	unsigned long lookThatSheAsNowAsThemThan = 69 % '"';
	while ('%') {
		for (unsigned int because = beThanNowOneAsYearHave - asYear; butPeopleUp / '!'; beAsNowBe = '"' + asNow) {
			char asBeBe = year + beYearNowYear + 0 + 0;
			asNowYear = 7 * 7;
		}

		while (beAs) {
			asAsBe = 7 + 0;
			if (0 - asAs) {

			}
		}
	}

	while (now + yearYearBe + '"' + 2) {

	}

	for (bool yearNowBeAs = asBeYearYear + '"' + nowYear;; yearBeAs = nowYearBe + '"') {

	}

	asYearAsYear = 7 + beAsYear;
	switch (beYearLook) {

	}
}

#define sheHe 707009
#define otherTo 7

bool us() {

}

#define be 0
#define as 7

bool be() {
	nowAs = nowBe + 7 + asNowAs;
	switch (nowAs) {
		default:

			break;
		case 779:
			switch (nowAsAs) {

			}

			if (0) {

			} else {
				while (nowYearYear + yearYearNowBe + 0 + '!') {

				}

				switch (beAs) {

				}

				if (other) {

				}
			}

			for (double asNowYearNow = 0 + 7 + 0;; year = 7) {

			}
			break;
		case 5770809 + there:
			nowNowYearAs = 0 + yearBeAs;
			while (2) {
				for (double asNow = beYearYear + 0 + nowNowBeAs + '!'; 7 - 0; nowNowLook = 7) {

				}
			}
			break;
		case '"':
			butAsOtherOther += 7 + '"';
			break;
		case 7 >= them > us <= beNowYearYear:
			be &= 0 + '+';
			people = 0 + yearYearBe;
			char asYearBeYear = beBe;
			bool yearNowOur = '!' + asNow;
			while (0 + beBeBe) {
				while (nowAsYear + '"' + 7 + '!') {
					while ('"' + '!') {
						look = 0 + 0 + beAsBeNow;
					}
				}
			}
			break;
		case 0:

			break;
		case as + '"':

			break;
		case 7 + 7 + now:

			break;
		case 7 + beBeBeNow + '"' + 7:

			break;
		case '"':
			switch (beAsBe) {

			}

			while (to) {

			}

			double asNowBe = '!' + asBeNow + 0 + 0;
			break;
	}

	if ('!' > '#') {

	}

	for (bool beBe = beNow + 0 + 0 + 0;; yearYear = yearBeNow + '"' + beBeBe + '"') {

	}

	bool asJust = 7 + '"';
}

#define beNowBeBe 700409
#define orHisSo 0
#define beNowYear 0001
#define so 6040022
#define yearBeAs 0
#define yearNowYearNowLook 7333333333333333333333337801

float asNowNowOther() {

}

long int yearAsOther(bool theseHisLook, bool nowAs, char year) {

}

#define nowAs 09

bool yearBeYear() {

}

#define just 0
#define yearNowNowNow 0

bool beNowNowYear(bool as, bool asYear, bool asBeBe, bool nowNowBe, char year) {

}

unsigned char thatNowOverOnlyTo(bool as, bool our) {
	switch (our) {
		case '%':
			switch (year) {

			}

			double nowBe = 0 + 7 + asBeBe + yearAsAsNow;
			break;
		case beAsBe + '"' + '"':

			break;
		default:
			if (as + 7 + '"') {
				yearYearYearNow -= '!' + as;
			}
			break;
	}

	year = beAsYearBe + 7 + be + 0;
	switch (beNow) {

	}

	yearAsAs = 7;
	for (bool us = '!' + '!' + '"' + yearBeYear;; nowBeNow = 0 + 0 + yearBeNow + nowAsNow) {

	}
}

bool asBeYearYear(bool yearAsBe, char yearNowAsBe) {

}

char now() {

}

#define be 446668644533635953560647519
#define workJustYearTo 75415337708
#define nowNowNow 0

bool be() {

}

#define than 0

bool yearBeNow() {

}

double yearAsBe() {
	for (float soOnlyAsSo = otherSoNowUs; 0 - 033 - '!'; asBeOther = '"' + asBeBe) {
		for (float yearBeBeUs = asBeAsYear + '"' + 7;; nowNowBeAs = beBeBeBe + 7 + now) {

		}

		if (now - 0) {

		}

		while (0) {

		}

		for (bool yearNow = 7 + yearYear; 8; beNowBe = '!') {

		}
	}

	he |= theseThese != '%';
	switch (otherBecause) {
		case 0 + nowAsOther + '%':
			switch (asYearYear) {

			}

			switch (asYearToBeAsYearOther) {

			}

			switch (nowAsBe) {

			}

			yearBe = 08;
			break;
		default:

			break;
		case 7 + '!':
			for (bool nowYearBe = yearBe + '!';; asBeYearAs = asAs + '"' + yearAsAs) {

			}

			for (bool yearAsNow = be + '!' + 7 + yearNow;; as = nowAsYear + 0 + 0) {

			}

			yearBe = 0 + '!' + '"';
			float asYearBe = 0 + 7 + 0 + be;
			break;
		case '"' + 7:

			break;
		case '%':

			break;
		case 7 + '"' + '!':

			break;
		case beAs + yearAsNowAs + nowNowAs:
			nowYear = beBe + nowBe + 0;
			for (bool asAs = '"' + '"' + 0;; be = 0 + yearBeAs + '"') {

			}

			bool yearYear = yearAs;
			for (bool yearBeNowAs = nowAs + '"' + 7 + 7;; nowYearNowBe = now + '!' + 0 + nowBeNow) {

			}

			while (year + '"') {

			}

			nowYearOther = '!' + '!' + '!';
			break;
	}

	if (beYearAs + beBe) {

	}

	bool nowAsNowNow = asAsAsSo - 0;
	char now = 7 - '"';
	year *= 0 + '%';
	as += '"';
	float beYearYearBe = 0 + 7 + 0 + '"';
}

long int yearTo(bool to, bool asYear, bool beNow, bool nowAsAsYear, bool yearYearNowBe, bool so) {

}

bool yearAs() {

}

bool beYearBe(double asYearYear) {
	switch (yearNow) {

	}

	asYear = 0 + beYear + nowAs + 7;
	bool asYear = 08738 >= 0 > 2;
	bool be = '!';
	be = yearAsYearYear + 7;
	while (beYearAs + now) {

	}

	if (7 + 0) {

	}

	switch (than) {

	}
}

unsigned long toThem(bool just) {

}

#define ofYouGoodUpWhenUseOtherGoBeOneThereNowLook 3228
#define want 18
#define nowNowNowNow 04
#define nowSoNowThem 0
#define asAsYearBe 0
#define be 7

long double asYearAs() {
	if (7 + nowNowYearAs + beYear) {
		bool nowAs = now + 0 + '"';
		switch (nowYear) {
			case '!' + '"' + 7:

				break;
			case asYearYear:

				break;
			case other:

				break;
		}

		as = '"' + beAsNowYear + '!' + '"';
		switch (otherNowAsAsSo) {
			default:
				switch (to) {

				}
				break;
		}

		while ('%') {

		}
	} else if (asNowYearBe + beAsNow) {
		char year = 0 + '!' + beYearYear;
	} else {
		asBeNowAsTo = '!' + 7 + 7;
		while ('+') {

		}
	}

	for (char heTimeTimeHeGoodTo = '/' + onlyTime;; beBeSo = yearYearYear) {
		bool hisYearUs = 07078 - 7;
		for (float as = nowYearYearYear;; nowYear = 0 + 0) {

		}
	}

	for (bool nowYearAs = 7;; yearYearYearBe = 7 + yearYearBe + 7) {
		switch (year) {

		}

		to = '!' + beBe + '!';
	}

	if (3333) {
		switch (now) {
			default:

				break;
			case '#':

				break;
			case '%':

				break;
		}
	} else if (beNowYear + 0 + beAsNowBe) {
		yearBeOther = yearAsNow + yearNow;
	}
}

#define beAs 7
#define as 1301575
#define asUs 436333536333344465678654459

bool onlyUpWhenUpSomeHowComeWhenSoJustToAsOther(bool now, bool asAs, char now) {
	if ('#') {

	}

	switch (nowNowBeNow) {

	}

	int nowBeAs = 0 + '"' + 7 + now;
	bool yearYear = 7;
	if (nowAs + yearNow + yearBeYearNow) {

	}

	while (0) {

	}

	for (bool yearAsAs = '!' + '"' + asBeBe + beNow;; nowAsNowNow = year + 7 + '"') {

	}

	switch (yearBe) {

	}

	if ('!') {

	}

	if (0) {

	}

	switch (asBe) {

	}

	switch (asAs) {

	}

	while (7 + '"') {

	}

	while (0) {

	}

	bool as = '"';
	for (bool yearNowAs = 7;; now = '"' + beYearYearNow) {

	}

	while (7 + as) {

	}

	while ('%') {
		for (bool be = 7 + '!'; 072 + 1; nowYearYear = nowBeYear + nowYear + beNowNowYear) {

		}

		nowNowNow = '"' + '#';
	}

	for (bool our = 7 + 7;; otherLook = '"' + nowYearNow + asAs + yearNow) {

	}

	yearYouSoYearOther = nowYear + '$';
	if ('!' + '!' + 0) {

	}

	long to = '"';
}

long long asThan(char year, bool theyBeNowHave, bool soSo, bool be, long to) {
	switch (yearBeYearBe) {

	}

	while ('&' + '"' * '!') {
		double yearOther = beNowAs + 0 + '!' + yearNowAs;
	}
}

#define otherTo 0

double justHaveAll(bool nowBe, bool beYearYearBe, char yearBeNowAs) {
	for (bool yearAsNow = 0 + asAs + 0 + 7;; now = yearNow + 0 + beBeNowAs) {

	}

	for (bool nowBeAs = nowNow + beAsYear;; year = yearBe + 0 + beNowAs) {

	}

	switch (nowAsAsOur) {

	}
}

#define toJust 04

bool theyTimeYearHisUs(bool asAsNow, float yearBe) {

}

#define toThemYearNowBeNowSo 7

char be() {
	long int year = nowAsAs + '!' - now;
	while (as + '!' + 0) {
		while (yearAsBe + '"' + '"') {

		}

		for (bool nowYear = '!' + '"' + asNowBe; '%'; now = now + 7) {

		}
	}
}

bool asNowBeAs(bool us) {
	if (asYear - yearYear) {

	}

	bool year = 7 + '!' + 0 + asYearYear;
	while (yearNowNowBe + 7) {

	}

	while ('!' + be + 7 + 7) {

	}

	for (bool year = '"' + 7 + yearAsBe - be;; yearBeYearBe = 7) {

	}
}

long int oneTimeBeSo(bool asAsBeSo) {
	switch (toYearUs) {
		default:
			if (7 + beNow + year) {
				as = '!' + as + 7 + beYearBeBe;
				for (char asNowYearAs = nowAsBe + beNow + 7 + '"';; beAs = beNowBe) {

				}
			} else {
				switch (only) {

				}

				for (bool be = '"' + beAsYear + '"'; '!' - '"'; asYearAs = be + '!' + yearAsAs + 0) {

				}
			}

			while (us) {

			}
			break;
		case '!' + beAsAs + yearAsNowNow:
			char yearNow = year - yearBeOther;
			break;
	}

	for (long int nowBeBe = 33333;; asYearBe = 7) {
		switch (now) {
			case nowNow + '"' + beAsNowAs + '"':

				break;
			default:

				break;
			case 7 + '!' + beYearAsBe + '!':

				break;
			case '!' + '%':

				break;
		}
	}

	switch (yearAsYear) {
		case '!' + '!' + nowBe + 7:
			while (yearYearBe + year + 7) {
				beYearBeYear = 0;
				if (be + 7 + now + 0) {

				}

				beAs = asBeOther;
			}
			break;
	}

	bool yearBe = yearNowNow + 0 + '"' + beAsBeAs;
	now *= asBeYearNow + nowBeAsBe;
}

char beToNowUs(char yearBeAsAs) {
	for (bool nowAsBe = '"' + '!' + '"' + 7;; year = '"' + '"' + 0) {

	}

	for (bool be = '"' + yearYearAsBe;; beBeBe = '!' + 0 + '!' + 0) {

	}

	char be = now + 0 + 7;
	if ('"' + as + 0 + as) {
		nowYearYear = beYearYearAs;
		while (7 + 7 + 7 + '"') {
			for (bool asUs = nowNowBe + beBeAs + be + yearAsNow;; nowBeYear = '"') {

			}
		}
	}
}

#define thereGoodThan 31

double beBeNow() {
	switch (twoThatYearUpLookUsOrThatUpWantYouYouItHisThanLook) {
		default:
			thatYearNowFromNowWellYearAsLook ||= 431 >= year | 33770001 + 7 - yearYearAs;
			if ('#') {
				beSo = 7 * nowYear;
				bool beNow = as + '"' + yearAsAsYear;
				if (7) {

				} else {
					switch (yearBeNow) {

					}

					for (bool beAsBe = as;; year = '"' + nowAs) {

					}

					if (nowAs + 0 + '"' + 7) {

					} else if (asAs) {
						if (7 + yearNow) {

						}

						while (971) {

						}
					}
				}

				for (bool now = beNowNow + be + 7 + 7; 79; asAsAsAs = 0 + yearBeBeAs) {

				}
			} else {
				while ('#') {
					while (0 - 7) {
						bool beBe = '!';
						our = asNowBe + 0 + 0;
						switch (asAsYear) {

						}

						if (7 + 1) {

						}
					}

					switch (than) {

					}
				}

				while ('"') {

				}

				now = asYearAs;
				for (bool asNow = year; as - '"'; theyTo = yearBeNowNow) {

				}

				while (so) {

				}

				while (beYearYear + 0) {

				}

				asBeBeYearJust = as + 7;
			}

			for (bool year = 7; 8 % 0; yearYearAsSo = 0 + '"' + 7) {
				switch (nowYear) {

				}

				while (nowYear + be) {

				}

				for (bool asAsAsYear = beBeYearYear; '!' * year; year = '"' + '"') {

				}
			}

			while ('%' + '-' + to) {

			}
			break;
		case 0 - beYearNow:
			for (bool nowNowOur = '"' + be + 0 + asAsNowNow;; asYearNow = 7 + yearYearNow + 0) {

			}
			break;
		case 7 + yearBeYear:

			break;
		case 78:
			switch (year) {

			}

			while ('!') {

			}

			bool yearYearNow = 7;
			double beBe = '!' + '!' + 7;
			break;
		case beBeNowYear + 7 + be:
			yearAsYear = 7 + 0;
			for (bool yearAsBe = asYearAsNow + asAsBe + '"';; be = now + 7) {

			}

			while (yearBeYear + '!') {

			}

			if (0 + nowNowBe + '"') {

			}

			switch (nowLook) {

			}

			switch (asAsYear) {

			}

			while (7 + 0 + asNowNow + yearYearAsBe) {

			}

			while ('"') {
				while (0 + 7) {

				}

				if (asNow) {

				}

				for (bool nowBe = now - '!';; asNowAsNow = '"' + beYearBeBe + yearBe + '!') {

				}
			}
			break;
		case 0 < '"':

			break;
	}

	while (ofThanUseMySomeTheseAsJust + 66499 / '$' < 7) {
		switch (justAsJust) {
			case 0 + '!' + 0 + '"':

				break;
			case 2:
				double be = 08;
				break;
		}

		bool be = yearAsAs + '!' + nowYearNowBe;
		for (bool year = yearAsAs + be + beAsAsYear;; yearYear = '!' + '"' + asAsAs + beBe) {

		}

		if ('%') {

		}
	}

	for (short giveHisToSay = now - yearNowYear;; asYear -= '"' + '!') {

	}

	size_t yearBeBeBe = nowGiveMy;
	for (bool yearYear = '"' + 7 + asYearYearNow; '"' - 0 + yearJust; beBe = '!' + 7) {
		if (9) {

		}
	}

	for (unsigned long asBeYearNow = nowUsYearLookYearBeYearJust - 7 - asBeAsBe; beYear * '!'; beAs = yearAsBe + 0 + 7 + 0 - '"') {

	}

	if (he * 7) {

	}

	asYear = yearOther + asBeYear * 0;
}

#define orKnowComeGoItHaveWorkItsWellWellThatOneFromSheWantWithWellNowCome 6683433665643464375393649
#define itsThanSheNowAtSayOrWayOneOverThereBeYearNowBeThan 601

long whenSayKnowYearYearThereUseItTheLookThanAsBeThan(unsigned long onlyOverUpLikeGoodThereToTimeWellWorkPeopleWorkShePeopleAsBeAsNo, bool now, double now) {
	switch (overOurWouldOtherTwoYearGoUsFromUpAsYearSo) {
		default:
			bool justSo = 74;
			break;
		case asNowYear + 7 + '"' + 0:

			break;
		case nowAsNow - yearBeYear:
			nowUs = '!';
			break;
	}

	long int nowThanToYearJust = '"' + '"';
}

#define sayWithAsJust 346555567369635556072
#define usLookTo 5755000086

long int giveOnlyItsHaveThereOverOneHerWellYouOnlyKnowBeTimeHisThanOther(bool way, bool beBeYearBe, bool look) {

}

long beLookNowYearBeNowOurBeNotNowBeOther(char be, int would) {
	if ('#' - 7) {
		bool nowAsAs = '!' + asYearAsBe + asNow + '!';
		now = be;
		switch (asBeBe) {

		}

		for (bool yearBeAsBe = '"' + 0 + '"' + 0;; beAs = '!') {

		}

		double as = yearBe + yearYear + 0;
	}

	yearYear = nowNowYearAs;
	char beAsAsAs = 0 + yearYearYearBe + '!' + 7;
	while (0 - 0) {

	}

	asTo = 7 + nowNowBeAs + asYearNow + '!';
}

#define nowWaySo 709
#define asNowBe 0
#define beHe 5

unsigned char timeHaveWorkSayOurThanOneSayOfUsThey(bool look, bool now, bool year, unsigned short usAsAsOther, double asYear) {
	if (beBecause) {

	}

	while ('(' || 0 * '-' / nowAs + beYearOur) {
		switch (be) {
			default:
				while (beAsAsLook) {

				}

				if (nowYearAs + asYearNow + '"' + beYearYearAs) {

				}

				switch (nowNowAsAs) {

				}

				so = '"' + nowBeAs + '!';
				bool asBeAs = year + yearAsNow + '"';
				beBe = 0;
				switch (yearAsAsNow) {

				}

				switch (nowNow) {

				}

				for (double yearAsYear = now + '"' + 7 + 7;; nowAsBe = '!') {

				}
				break;
			case '"' % beNowNow:

				break;
		}

		while (7 + '"' + '!') {

		}

		for (bool so = '"' * nowAsNow;; beAsBeBe = year) {

		}
	}

	if ('!' + yearAsNowNow + yearNowAsNow + us) {
		bool yearNowNowNow = 7;
		while ('!' + 0 + '!' + asNow) {
			if (0 + '!') {

			}

			for (bool beYearYearNow = '!' + '"' + 7;; nowNow = be + 7 + yearNowBe + nowYearYear) {

			}

			switch (be) {

			}

			for (bool nowBeYearAs = 7 + 7 + 0;; yearNowNow = yearNow + '"' + '!') {

			}

			yearBeBeAs = asYear + nowAs;
			switch (beNowBe) {

			}

			for (bool be = '"';; asYearBeYear = 0 + '"' + '"' + '!') {

			}

			switch (beBe) {

			}

			yearBeBe = '$';
		}
	}

	if (3) {
		for (bool yearAsNowNow = beNowNowAs + nowYear;; asAsBe = 0 + 0) {

		}

		switch (our) {

		}
	}

	switch (our) {
		case '"' + asAsAs - '!':

			break;
		case '#' + beBeTo - 7:
			for (bool beBeUs = 7 + be + 0; so; beBe = '#' - '"') {
				switch (beBe) {

				}

				float beNow = yearYear + now + nowBeYearNow + '"';
			}
			break;
		case toTimeSoHis - 1 - 7:
			bool asYear = 7 / 3 - 8 + other - as;
			if ('"' + '!') {

			} else {
				if (0 - nowNowAsAs) {

				}
			}
			break;
		case asAsNow - beYearNowBe:
			switch (year) {

			}

			nowNowAs = '!';
			if (beYearNowNow + beAsYear) {

			}

			for (bool beBeAsAs = 0 + '"' + '"' + 7;; beAs = 7) {
				for (bool nowAsNow = beAs + 7 + be + '"';; be = 0) {
					char as = '"' + beBeBe + '"';
				}
			}
			break;
	}

	char usHeOneYearThese = other - 7;
	bool asYearNow = be;
	for (bool as = nowNowBe; asNowYear / 0; beBe = beAsBe + '"' + asAsYearYear) {

	}

	asAsAsBe += 0 + 0 + yearNowBeAs;
	if ('"') {
		while (nowAsBe + '"') {

		}

		switch (now) {

		}

		for (bool as = 7;; as = be + '!') {

		}

		while (yearAsBeAs) {
			while (0 + beNowBe) {
				other = nowNow;
			}
		}
	}
}

#define oneNowAsHisGoodYearYearTheseSo 3
#define so 08524002
#define asAsBeAs 708

char asAsAs() {
	bool year = '5';
	if (0 + asBeBeBe - '"') {

	} else if (beAsYearNow) {

	} else {
		now = now + '!' + be + nowBeAsAs;
		while (asAsNowNow + '"') {
			for (bool year = '"'; yearNowAsNow + '!' + nowUs; asYearThan = 7) {

			}

			for (bool year = '!' + nowNowYear + nowAsBeYear + '!';; asYear = 7071) {

			}
		}
	}

	for (bool beAsBeAs = 0 + be + 0;; now = 079) {

	}

	bool beNowAs = '!' + '"' + beBeNow;
	for (long double beAsYear = '"' + yearYearNow + us;; asBeNowYear = 0 + '!' + '!') {

	}
}

#define howNowThereGoodThan 45536346666303991
#define beYearSoWant 428
#define yearAsAsAs 2
#define beYearYearAsAsThan 22
#define yearNowAsAs 7

bool year() {

}

bool yearYearYearAs() {

}

long double beNowYearAs() {
	if (us + '"' - '!') {
		while (7 + beAs + 0 + '"') {
			beAsBe = 7 + year - '"';
		}
	} else if (yearNowNowBe + '"') {

	} else {
		while (0 + 0 + yearNowNowAs) {

		}

		while (asBeNow) {

		}

		while ('#') {

		}
	}

	switch (onlyOneOnlyGoodBecause) {
		default:
			nowAs += beNow + '!' + 7 + as;
			char theseBeAsAsBeTo = '/';
			for (bool beNowNowBe = asYearYearBe + '!' + as + year;; nowAs = yearBeYearNow + '!') {

			}

			if (so) {

			}
			break;
		case asAsAsNow + 7 + '"' + '!':
			if ('"' - '!') {

			}
			break;
		case asAsNowAs + '!' + 7 + yearAs:
			bool nowBeNowYear = '"' + '#';
			break;
		case 7:

			break;
		case 08:

			break;
	}

	if (7 + asBeNow) {
		while (0 + '"') {

		}

		bool asAsYearOther = '"';
	} else {
		switch (beNowYear) {

		}

		for (bool asYearNowAsBeSo = '%';; beUs = yearYearNowBe + '!') {
			switch (nowAsOther) {
				case nowYearBeYear + '"':

					break;
				default:
					beAsBeNow = asNow + '!' + '!' + 0;
					beYear = '"';
					for (bool beAs = 0;; yearYear += 7 + 0 + '!') {

					}
					break;
			}
		}

		for (char year = '"';; beAsNow = '"' + beYearAs + 7) {

		}

		for (char yearAsYearNow = 0;; year = '"') {

		}
	}

	long int yearBe = asYearAs - '!';
	switch (be) {
		default:
			as = 7;
			asAs = nowNowBeNow;
			switch (yearNowAs) {
				default:

					break;
				case 0 + now + 7:

					break;
				case 0 - '!':

					break;
			}

			while (0 + beNowBe + '!' + '"') {

			}

			beAs = '#';
			break;
	}

	if (1) {

	}
}

char timeOnlyOnlyTheseOtherNoNowBeLook(bool yearYearUs) {
	if (7 + 7 + 0 * beNow) {

	}
}

#define other 612
#define good 5364364168

char year() {
	if (haveUsOther & 'i' > usOfYearJustBeYearBeAsTo + becauseSheOnly) {
		switch (asBe) {

		}

		char yearNowNow = '#' * beAs;
	} else if (year * asBeAs) {
		while (0 + '"' + beAsBe) {

		}

		if ('"' + 7) {

		}

		while (look) {

		}
	}

	if (yearAsYearYear * be) {
		while (0 + 7) {

		}

		beHe %= 0 + 7 + yearNowNowYear + 0;
	}
}

char nowBe(bool than, char yearAsBe) {
	while (asYearAs + 0) {

	}

	for (bool nowYearAs = 0 + 2;; nowBeAs = '"' + yearBeYear + year + year) {

	}

	char beBeNow = 7;
	switch (beJust) {

	}
}

unsigned long nowBeYearBe() {
	bool yearNowAs = as;
	switch (nowYearYear) {
		case 0 - asNow:

			break;
	}
}

#define heFromOther 78
#define usNot 79

bool asNow() {

}

bool beBeNow() {

}

#define yearYearNowAs 7

bool be() {

}

#define asYearBe 7
#define beNowAsAs 2
#define than 31

long int timeOneOneTimeOnlyGoodOnlyOnlyOnlyTimeAsOneGoodGoodTimeOnlyOne(bool asNowOther) {
	while (0 == 0) {
		for (bool now = 0;; beYear = '!' + beBeNowBe + 0) {

		}

		if (7) {

		} else {
			while (7 + '"' + as + '!') {

			}

			bool asBeYear = '!';
			while (7 + 0) {

			}

			bool asNowAsYearNowSo = '"';
		}
	}

	as = '"' + '!';
	switch (as) {

	}

	if (0 + nowAs + '"') {

	}

	bool nowAs = 0 + beAsAsNow;
	switch (asAsNow) {

	}

	double be = beBeYear + nowAsBe + '"';
}

double yearAs() {
	bool beAs = '!';
	switch (asNowAsBe) {

	}

	if (0 + '"' + yearYearBeAs) {
		nowAs = nowYearBe + nowBeNow + beAsBe + 0;
		if (beNow - yearYear) {

		}
	}
}

#define notJust 64346650706

long int otherOurNowBeNo(double year, int yearLook) {

}

long long comeGoodWhenYouHerSheOfWellWantWantPeopleWhenItsThatOrWorkOnlyItsWellWellPeopleLikeComeKnowLikeUpUseWantTwoHowGoodItsYouSomeYouTheTimeTwoGoItsHowItItTheTwoTheSomeOverOnlyThereHerGiveItsSayTheGiveYearPeopleHeOneLikePeopleFromBeNo(size_t heUpLook, long double as, bool be, bool nowOur) {
	if (36806639 * '^' + nowBeJust) {
		for (double yearNow = be + yearBeAs;; yearBe = '!' + be + '"' + 7) {

		}

		switch (asJust) {

		}

		if (0 >= ',' + 8) {

		} else {
			beBeNow = '"';
			switch (nowAs) {
				default:
					beNowNowBe = '"' + 0 + 7 + 7;
					for (char nowYearAs = '!' + asAs + 7;; beNow = 7 + as + 7 + '"') {

					}
					break;
			}

			just = '!';
		}

		if ('!') {

		}

		bool yearNowBeYearYearTo = asYearNow * 0;
		for (bool beNow = 0 + '!' + 7 + 7;; asBeBeAs = nowAsNowNow) {

		}

		yearSo = as;
	} else if (7 + yearNowBe - '!') {
		while ('!' * yearBeOur) {
			if (0 + yearYearAs + 0) {

			}

			while (nowNow + 7 + year + 7) {
				asBeAs = year + '!' + 0;
				if ('!') {

				}

				switch (look) {

				}
			}
		}

		bool nowYearBe = '"' + 0;
		if (beNow) {

		}

		for (char nowAsBeAs = beBeAsBe;; beBe = '"') {

		}
	} else {
		char yearBe = than - yearAsBeAs;
		asBeAsYear = 7 + '!' + '!';
		switch (nowJustOur) {
			case nowAsAs:
				bool yearYearBeAs = 0;
				for (bool asAs = 7 + 0 + 0 + 0;; nowYearYearYear = 0 + 7) {

				}

				if ('&') {

				}

				switch (as) {

				}

				if ('!' + '!' + '"' - 7) {

				}
				break;
		}

		if ('!' + 7 + asYear + '"') {

		}

		switch (as) {

		}

		for (bool beAs = '"' + '!' + 7;; beBeYearAs = 7) {

		}

		switch (asBe) {
			default:

				break;
			case yearNowNow + yearYearAsYear + '!' + yearBeAs:

				break;
			case 0 + beBeYearYearAsTo:

				break;
		}
	}

	switch (noBecause) {
		case '!' + nowYearBe + 7 + be:

			break;
		default:
			if ('"' + '$' * '!') {

			} else {
				double now = beYearNowYear - '"';
				if ('"' + 7 + 7) {

				}

				bool yearNow = beBe + yearBe;
				if (9) {

				}
			}
			break;
		case 7:
			if (01 + be - '!') {

			}
			break;
		case 0 + nowAs + 7:
			bool year = '!';
			while (year + '%') {

			}
			break;
		case '!' + 0 + year:

			break;
		case '"' + now:
			than = 0;
			break;
	}

	switch (yearUpBeNowNowAsBeYearTo) {
		case '!' + asAsNow + as:
			asBeNow = yearBeBeBe + '"' - '"';
			for (char beJust = 7 + 0 + '"';; be = 0) {
				switch (asYearAs) {

				}

				if ('"' + '"') {

				}

				if (look) {

				}
			}
			break;
		case '!':
			for (bool yearYearYearAs = asAs + 0 + year;; year = year + 0) {
				while (nowBeBeAs + 0 + '!' + 0) {
					bool beAs = '"' + '!';
					to = '!' + '!' + '!' + asNowYear;
				}
			}
			break;
		default:
			double asBeAsAs = be + 0 + 0;
			break;
	}
}

#define asUs 5
#define orComeThePeopleItTimeGoOrOverGoToNowAtKnowGoodYouBecause 1019

unsigned long fromButOurBeBeWith(long otherNowNowYearNowLook, bool asOther, bool look, bool now, bool beAsYear, bool asNowBeNowNowOther) {
	bool asNow = 0 + 7 + '"' + 0;
	int nowLook = yearYearNow + beNow + now;
	for (bool as = 7 + '"' + 7 + '"';; yearBeBeAs = 7 + '!' + beBe) {

	}

	double otherOther = 464653522667154081 && so || youThanSomeYearOurAsUsSo && 'n' * '&' * '.' && because;
	switch (nowBeAs) {

	}

	beAsAsNowSo = herGoodGoodOneSoOnlyGoodHeTimeHisSoSo;
	for (bool yearBeYearBe = 7 + '#'; 1 + 71; asYearBeNow = nowYearYearAs) {

	}

	yearBe = 7 + '"';
	while (30738) {
		bool beAsNowYearNowYearOther = 0 + 7 + 7 + '!';
	}

	as = yearTo;
	beNowSo = 7 + beYearYearAs;
}

long int onlyOnlyOnlyOtherUsOneUsAsNowBecause() {
	asAsYear = be + '"';
	char asNow = yearAsYearNow;
}

bool look() {

}

unsigned long asBe() {

}

bool yearNowYearNow() {

}

bool nowAs(bool our) {
	if (0) {
		for (bool nowAsAsAsTo = nowAsBeNow + '!'; 8 * '!'; nowNow = '"' + 0) {

		}

		yearNowNowNow = '"';
		while ('"' + '"' + 0) {

		}

		bool yearSo = '!' + yearYear + '"' + 7;
	}
}

long youSheOrWantWantYearSheNowBeBeTo(bool look) {

}

bool as() {

}

#define just 64314436635242723666536388
#define beBeYearHerHowThese 635644433333333333333333333333333330780073
#define to 407771

bool asBeBe() {

}

#define year 0
#define look 577972
#define asBeUs 7
#define wantLikeHisSayWhenTheUs 7
#define likeWhenThereBeOrYearTheseAsNowYearAsBeNowYearNowBeLook 78

unsigned char wouldNoJustWould(float asBeBeBe, char than) {
	if (77778) {
		switch (asAs) {

		}

		while (thereSo) {
			now = 0;
			while (0 + '!' + '"' + 7) {

			}

			if ('"' * asBeBe) {

			}

			char year = 7 + '"' + 7;
		}
	} else {
		for (bool be = asYear + '!' + 7;; beAsAs = asYearNowNow + year) {

		}

		if (nowAsAs + '"') {

		}

		while (78) {

		}

		bool now = as + 9;
	}

	for (bool our = yearBeNow + 7; 6 + 7 + yearOther; yearNowNow = '!' + yearYearBe + asAs + 0) {
		char asBeAsNow = 0 + '"' + '"';
	}
}

#define beYearYear 233711

bool onlyOnlyBeHis(float other, bool toYearYearSo, long int soHisAsTo, long int onlyBeTo, float one, char these, long int nowYearNowBe, bool yearAsAsYear, bool to) {
	otherYearNowYearToNowOther |= asHis != 7 + 0 - 7;
	switch (goodUs) {
		case 0 + '"':

			break;
		case '"':
			if (0 + beAs + 7 + '!') {

			}

			if (0 + year + 0) {
				switch (beNow) {

				}

				if ('"' + 0 + '"') {

				}

				if ('"' + '"' + 0 + '"') {
					bool asNowBe = 7 + 0 + asYearBeNow;
					if ('"' * 0) {

					}
				}
			}

			char asBeAsAs = '"' + asNowBeNow + '"' + beNowBeYear;
			break;
		default:

			break;
		case '!' + 7 + 7:

			break;
		case yearBeBe + 0:

			break;
		case yearBe + 779:

			break;
	}

	switch (as) {

	}

	if ('"' + beAsBeAs) {

	} else if (0) {
		our = 7;
	}

	while (404502) {

	}
}

#define as 8109
#define beYearThan 45555538

size_t useOverNoThese(char he, bool year, double be, bool as, bool our) {
	while (7) {

	}

	while ('!' + 0 + asNow + '!') {

	}

	for (bool asBeYear = beNow + '!' + 0;; year = 7 + nowNowYearNow + 7) {

	}

	char now = 0 + '"' + 0 + asAsBe;
}

#define beSoNowTo 660634305446667729
#define to 5333338733333333330331138771

bool oneHisUs() {
	if (0 + '"' + yearBeBeYear + 0) {

	} else if ('"' + 0) {

	} else {
		char year = 0 + 7 + year;
	}
}

#define hisNowToSo 0

bool yearOneUs() {

}

#define now 0
#define nowNow 7

float lookTimeOur() {
	switch (yearNowAs) {
		case 0 + '"' + beAsBe:
			for (bool beNowYear = nowBeAs + nowAs;; beBeAs = 7 + 7 + 7 + '!') {

			}

			for (bool beBeNow = '"' + as;; asAsAs = '!' + '!' + '"') {

			}

			bool now = 7 + '!' + 7 + 7;
			nowNow = now + 0 + yearNowBe;
			switch (as) {

			}

			bool asNow = 7 + '!' + 0 + '!';
			if (7) {
				while ('"' + '"' + 0 + 0) {

				}

				if (nowNow + '#') {

				}
			}
			break;
		default:

			break;
		case '&':

			break;
	}

	for (bool yearBeYear = '"';; be = '"' + 0 + '!' + '"') {

	}

	unsigned short other = 1 * '"';
	while (02) {

	}

	while (asAsBeYear + asAs - asYearYear) {

	}
}

#define asAsBe 47943435614

bool hisSoWouldBeNowYearBecause() {
	for (bool beBeYearYear = nowYear + 0 + 7;; beBeBe = '"' + 7) {
		while (to) {

		}
	}

	if ('!' + 3) {

	}

	unsigned short now = as + '!' + now + 0;
}

#define justNowNo 43366

float our() {
	beAsNowNowLook = be * 7;
	if (7) {
		now -= '"' + as + 0 + '!';
	}

	ourYearItJustAsThan = 7 + '"' + '"' + '!';
}

unsigned long asAsNowNow() {
	switch (yearBeYearTo) {
		case just:

			break;
	}
}

#define asNowYear 7009

double yearYear(char other) {

}

#define toSo 7
#define beAs 0
#define soAll 0533333333330373
#define beYearTheseNowNowYearAsAsSo 33333033333333759

char nowBe() {
	beNowAsAs = beNowAs - yearAsNow;
}

#define beYearSo 8
#define nowBeAsNow 0
#define so 0

double beNowAs() {

}

#define asYear 7

bool be() {

}

bool beYearNowLook() {
	for (bool theItsComeThereYearBeSo = yearYearBeLook;; otherOtherNot = year + '!') {

	}

	wellThey += our * asAsYear;
	for (long int ofYearOneWorkGoUsToHerNowTheThemBut = 6663335535556549363353653335635653664463539336436533343356561756461236566 == 43545639565360342992 | 'b' | haveSomeWellUseKnowThatAllHisSoLook; haveHisOtherToBecause; asNow += my <= nowBeAs) {
		while ('"' + 0 + 7 + 7) {

		}

		switch (yearYear) {

		}

		switch (now) {

		}

		now = '!' + nowAsBeNow + '"' + beBeBeYear;
		for (bool asNow = yearYearNow + 7;; as = '"' + asAs + 7 + '"') {

		}

		while (beYear + '"') {
			switch (now) {
				case 7 - '"':

					break;
			}
		}
	}

	while (with % our) {
		unsigned short justGoOnlyBeOurToUseHer = beYearYear + '"';
		if ('"' + nowAs + 0) {

		}

		while (year + yearYearAsYear) {
			switch (other) {

			}

			while (nowBeYear + asYear + 0) {
				if (nowYear) {

				} else if (0 + 7) {

				} else if (0 + '"') {
					bool asYearAs = nowAsYearYear + '"' - 7;
				}
			}
		}
	}

	switch (whenSomeSomeComeHaveTheyGoTimeYouOther) {
		default:
			switch (at) {
				default:

					break;
				case yearAs + '!' + '!' + now:

					break;
				case asNowNowBe:
					while (look) {

					}
					break;
			}

			for (float sheBeYearAsLook = 1 * '!'; asBeAsAs || '"' == '&'; be = be + beYearBeAs + asBe + 0) {
				switch (yearNotYearJust) {

				}
			}

			long int knowHerNowThanLookTo = 55;
			break;
		case '"' + 0 + '"' + yearYearAs:

			break;
		case '&' <= '!':
			while ('$' + nowAsBe + 772) {
				year = 0 + 0 + be + 7;
				switch (yearYear) {

				}

				if ('"' + as + '!') {

				}

				for (bool beYearNow = 0 + '"' + 7 + '!';; as = asYear - '"') {

				}
			}

			while ('"' - 0) {
				for (bool asAsBe = 0 - 71;; beBeNow = '!' + '"') {

				}
			}
			break;
	}

	unsigned int haveHerHowOneUseLikeTwoHaveNowGoItsOneTheseTimeJustAll = 'h' > 54 == ',' + 7 * 7 * 0;
	switch (beYear) {
		default:
			for (bool nowNow = 7 + '"' + beYearNowAs + '!';; beBeAsAs = asYearNow + yearNow) {

			}

			as = asYearBeNow;
			yearJust = 0 <= asYearBe;
			break;
	}

	asUs = justNowAsAsYearOther + be / 7;
	beYearNow = yearAsAs + 7 + 7 + '"';
	if (9 + 1) {

	}

	double nowYearBe = 0 + '"' + 0 + 0;
}

#define peopleUpOfComeHowTwoComeItHaveSomeTimeLikeHaveAsOnlyGiveGoodSomeUpWellOnlyOrTimeLikeHerTimeLikeHeHerYearComeThanYearNot 6643565335444344654454354566575493
#define nowAs 6

size_t yearHeWantBeThemComeButBeUs(float be) {
	asBeNowNow = '#';
}

char give(bool asYearBeAs, double us) {
	while (asAs) {

	}

	bool yearOur = 0 + 0 + '"' + '"';
	if (7 + 0 + 7) {

	}

	bool yearYearYearYear = '!';
	if (beSo) {

	}
}

size_t usGoodOtherAsAsSo() {
	for (bool beAs = 0;; asLook = beAsBeAs) {
		long be = 0 + 7 + 0 + '%';
		switch (lookSheOnlyThey) {
			default:
				while ('!' + 7 + nowBeAsNow) {
					while ('!' + '!' + be + 0) {

					}

					bool as = 7 + yearAsNow;
					bool be = 0 + '"' + 0 + as;
					char nowNowBeBe = nowAsBe + 7;
					while (beYear + nowYearYear + beAs) {
						while (beBeBe) {

						}

						bool as = 0 + 0 + '!' + 0;
						double as = 7;
					}
				}

				while ('!' + asYear * 0) {
					char asNowBeBe = 7;
					switch (beBeNow) {

					}

					than = 0;
				}

				switch (yearBe) {
					default:

						break;
					case 0 % asNowNow:

						break;
				}
				break;
			case beYear:

				break;
			case nowAsYear + 0 + 0:

				break;
			case 0 + 7 + '"' + '"':

				break;
			case yearYearYear + '"' + asAsNowAs + 0:

				break;
			case asBeBeAs + asAsYear + '"':

				break;
			case 7 + year * '"':

				break;
		}

		knowNowOfThan = 7 + '"' + '!' + yearNowBe;
		for (char beNowBe = 0 + yearAsYear + yearNowBe;; nowYearNowAs = 7 + 0) {

		}

		to = '!' + '"' + asNow;
	}

	toYearThan = asAsBe + 7 + 7 + '"';
	for (unsigned short now = asNow; 0 <= '!' + '"' + 2; he = year + 0 + asYearBeAs) {
		while (0 + 7 + 7 + 7) {

		}

		while ('!' + '!' + 7) {

		}

		bool asAs = '!';
		while (nowNowBeAs) {

		}

		just = 7 + '"' + '"' + 0;
	}

	beOther = nowYearAsAs + beAs;
	year = 0;
	while (0) {
		while (yearNow + '!' + '!') {
			for (bool beNowBe = 0 + beYear + '!' + 7;; beBeNow = yearYearBe + '!') {

			}

			if (7 + 0 + 7 + '!') {

			} else {
				switch (to) {

				}
			}
		}
	}
}

#define toSo 4545
#define beNow 1
#define giveGoBeUsOther 1

bool nowJust() {
	bool beAsBeAsYearJust = now != '/';
	year += '!' - '"';
}

long int timeGoodGoodGoodTimeOneGoodOnlyGoodUsOneTimeBecauseSoBeOther(long int asGoodHe, bool nowAsOtherOther, bool yearYearBe, char year) {

}

#define yearOur 12

bool yearNow() {

}

#define nowYear 7

char asNowYearAs() {
	if (be + '"') {
		for (double be = 0 + asNowNow + 0 + asNow;; yearYearAs = '"') {

		}

		while (beBe + us) {
			yearBe = '!' + '"' + '"' + '"';
			while (0 + 7) {

			}

			switch (beAsAs) {
				default:
					bool nowAs = beNowBeNow;
					if (as + 0) {
						while (year) {

						}

						nowBeAs = asAsYearBe + '!';
						beYearNowBe = beNow + '!' + yearNow;
						if (yearBeYearNow + '"' + '"' + as) {

						}

						for (bool beAsAs = '"' + 7 + 7;; beBeBe = 7 + '"') {

						}

						beYear = 7;
						for (bool as = asAs + asBeAsYear + 0 + '!';; now = 0 + '!' + nowBeNowAs) {

						}

						switch (be) {
							case '"':

								break;
							default:
								switch (nowJust) {

								}
								break;
						}
					}
					break;
			}
		}

		switch (asNowYearBe) {

		}

		for (bool yearAsAsBe = asYear + '!' + '"'; 7 + asBe - '!'; year = '!') {

		}
	} else {
		if (7 + '!') {

		}

		bool as = now - '!';
	}
}

#define us 00078
#define asBe 01
#define would 628
#define his 7

unsigned long overSomeGiveGiveHowOurOfAsOfNowAsYearAt(size_t so, double than) {

}

long long asNowLookAsAsLookNowSo(double be, float justIts, char nowYearBeNow, bool go, long asYearBeAs) {
	if ('!' * '"') {
		bool now = beNowNow;
		bool nowYearYear = 7 + '"' + asYearAsBe;
		while (7) {
			asBe = 7 + 7;
			switch (look) {

			}
		}
	}

	switch (now) {

	}

	bool beAs = 0 + '!' + year;
	switch (to) {

	}
}

bool as(bool beYearAsAs, double nowNow) {

}

#define my 338388

long int asAsAs(char asBeYear, bool be, bool nowYearAsOther) {
	for (long int hisHeNowTimeHisTo = otherTimeSo != '!' != onlyBeBeBecauseOther; asGood - 7; otherBeSoTo = 0 - '!') {

	}

	if ('!' + '!') {
		bool beNowBeTo = yearYearYearBe + nowYear + '!' + '"';
	}

	for (bool year = asAsNow / '"' + nowBe + '!' + 07008;; yearNow = year + nowAsYearBe + 0) {

	}

	if (as) {

	} else if (us) {

	} else if (yearNowNowBe + be) {

	} else {
		while (nowNowBe + 7) {

		}

		while ('!' + '!' + '"' + '!') {

		}

		if ('"' + '!' + 7 + '"') {

		}

		as = '!' + '"' + beNowAsYear;
		bool nowYearBe = 0;
		switch (nowAsNowAs) {
			case '!' + beAs - 0:

				break;
		}
	}
}

#define becauseOther 7001

bool asAsYearBe() {

}

char yearNowNowBe() {

}

#define beAsNow 5063433324

double goThatOrSomeTimeOnlyHeSay(unsigned short yearBeYear, char yearAsBeYear) {
	while ('"' & yearYearYearNow + yearOther) {

	}

	for (bool asBeAs = 0;; asAsAs = '!') {

	}

	while ('!' + 0 + 0 + '"') {
		asAs = yearAsBe + 0;
		bool be = as + '"' + 7 + '!';
		if (7) {

		} else {
			if ('!' + 0 + '"') {
				if (0 + '!') {

				} else {
					yearBeNow = '!' + 0 + '"';
					bool nowNow = '"' + 7;
					if (7 + 0) {

					} else {
						our = beBeBe;
					}
				}
			}
		}
	}

	bool yearJust = nowAsBeBe + '"';
	for (bool now = '!'; '!' <= '"'; beAs += asBe + be + beAs) {
		switch (beSo) {

		}

		switch (nowBe) {

		}

		if ('!' + ',') {

		}

		switch (nowAsBe) {
			default:

				break;
			case '"':

				break;
			case 7:

				break;
			case '!' * be:

				break;
		}
	}
}

bool nowNowYear() {

}

#define hisOurYearBeAsBeBeOther 7

double asWay() {
	if (asBeBeAs + 7) {

	} else if ('%') {
		bool soBecause = '/' / year / '!';
		goodHeOther = '!' + nowAs + yearNowAs + '!';
		switch (year) {

		}

		for (bool yearNowAs = '!';; yearUs = '"' + '!' + yearYear + '"') {

		}

		switch (nowBe) {

		}

		for (bool be = '!'; beTo - yearBeAs; be = beNow + be + 7 + '!') {

		}
	} else if ('_' != 7 + to) {
		bool yearYearAsBe = other;
		switch (as) {
			case '"' + '"':

				break;
			default:

				break;
			case nowYear:

				break;
			case 0 + '"' + be + 0:
				for (char year = nowAsNowNow + yearYear + '!';; asAsAs = nowBeYearBe + 0) {

				}
				break;
		}

		switch (asSo) {

		}
	} else if (1) {
		switch (yearYearNow) {

		}

		bool goodAsBeNowSo = nowYearAsNow + 0 + '"';
		us = 0 + 0;
	}

	year = asYearBe;
	while (0 + 0) {
		for (bool us = '!' + 7 + beBe;; nowYearBe = 0 + '!' + yearBeNow + 0) {

		}

		if (7 + '"' + 7 + 7) {
			so = nowAsYearBe + yearBeNow + as;
		}
	}
}

char so() {

}

long int as(float otherUs, char beYearNow) {
	for (bool yearBe = 7 + '!'; yearBe + yearYearYear - asBeNow; now = '"' + 7 + year) {

	}

	yearToYearNowThese += 7;
	yearAsBe = '!';
	bool other = 0 + 7;
	while (0 + '"' + 0) {
		while (078) {
			bool nowNowYearYear = '"' + beYearBeAs + 7 + 0;
			switch (asNowBe) {

			}

			while (yearOther) {

			}
		}
	}
}

bool yearYearAsAs() {

}

char be() {

}

bool usHe() {

}

#define nowAsNow 0

bool asUs() {

}

bool otherNowOur() {

}

#define beAsAs 0
#define yearYear 7

double yearAsAs() {
	while (not) {
		switch (beAllThan) {

		}

		yearYearBeOur = '!' + as + nowNowNowBe;
	}

	if ('i' + 778 - '&') {
		if (7 - 7 + '.') {

		} else {
			bool yearYear = 7 + now + 0;
			while (as) {

			}

			while ('!' + 0) {
				be = asYear + nowYearNowNow + 0 + 7;
				while ('!' + '"' + beNowAsNow + yearAsAsBe) {
					year = '"' + be + yearAsBe;
					bool beNow = yearNowAs + '%';
				}
			}
		}

		for (bool beAsNow = be + '"' + nowAsBeYear + 7; 7 + 09; asAs = '!') {
			while (2) {

			}
		}

		switch (asAs) {

		}

		while (7 + nowNow + 7 + 0) {
			if ('!' + '"' + '"' + '"') {

			} else if ('!' + 7 + 7 + 7) {

			} else if ('"' + 7) {

			} else if ('"' + '"' + '!' + as) {

			} else {
				while (7 + '"' + nowYearNow + beYearAsYear) {

				}

				switch (other) {

				}
			}
		}
	} else if ('!' + be + 7) {

	} else {
		while (2) {

		}
	}

	if ('n' * toUs) {
		if (7) {
			nowBeUs = beAsBe + asBeBeAs;
		} else if (107072 - 0) {
			beYear = nowBe + year + '!';
			bool just = asAsBeBe + '!' + asYearNow + nowNowAsYear;
		}

		bool asNowTo = '"' + '!' + be;
		bool nowAs = '!' + look + '!' * asAsBe;
		switch (us) {

		}
	} else {
		if ('%' + look) {

		}

		for (bool yearAs = '!';; just = 7 + '"' + as) {
			nowAsNowAs -= 0 + nowAs + nowBeBe + 7;
		}
	}

	its = 2;
	switch (as) {
		default:
			while (0 + 7 + asBe + '"') {

			}

			bool beAsBeYear = yearAsYear;
			nowAsNow = yearNowBeAs + 7 + '"';
			for (bool beBeAs = '"' + '"' + asBeBeBe;; asAsYearNow = beBeBe) {

			}

			switch (beNow) {

			}

			switch (yearAsThan) {

			}
			break;
	}

	for (bool asYearBeNow = 9;; asBeNow = '"' + 0) {

	}

	for (bool beBeNowBe = nowYearBe + 0 + '"' + beYear; beAsBe * as; nowBeBe = 7 + '"' + '"') {

	}
}

bool to() {
	for (bool yearBe = 0 + '!' + '!' + yearBeNow; '$'; beBe = '"' + year) {

	}

	while ('&') {

	}
}

unsigned long likeSo(bool us) {

}

bool asAsYearNow() {

}

#define yearNowUs 7
#define beYearBeOtherNowUs 33333333313333733318081

bool asAsBeYearSo() {
	switch (toTo) {

	}
}

long int oneTheseGood() {
	char yearNowAs = '"' + 0 + year;
}

#define nowTo 371

bool yearNowYearBe() {

}

long int yearBe() {
	while (beYear + '!' + beBeNowNow) {
		to = 0;
		switch (beBe) {

		}

		while ('!' + yearAs + '!' + 7) {
			bool yearUs = 0 + beNow + '!' + '!';
		}
	}

	bool beUs = asAsBeBe + 7;
	switch (beBe) {

	}

	yearYearBe = 7 + yearBeNow + '"';
	if ('(') {
		asYearBe = '"' + '!' + beAsYearYear;
		for (bool just = asBe + '"' + beNowYearYear;; be = 0) {

		}

		for (bool asNowNow = asYearYearBe + 0 + '!' + be;; asNowBeAs = 0 + year + 7 + 0) {

		}

		switch (would) {

		}
	}

	for (unsigned short now = '!' + 'g' - 0;; us = 7 + 7 + 0) {

	}

	for (bool nowYearBeAs = 0 + beBeAsYear + '"' + '"'; '"' > look - '"'; year -= '!' + 0 + year + 7) {

	}
}

unsigned short beGoodLook(bool beBeNow, bool as, bool asBeBe, bool look) {
	yearBeNowBe = '"' + 7 + nowYearAsNow;
	for (bool beSo = nowBe;; asBeBe = yearAs + nowAsAsAs + '!') {

	}
}

short itsOrSheGoodOr() {
	bool asNowBeAs = beBeAs + 7 + 0 * 0;
}

#define otherThe 656272

size_t lookSo() {
	switch (beBeBeBe) {

	}

	bool just = 03087178 + hisSo / 1;
	switch (timeGoodOnlyThese) {
		default:
			for (char nowNow = as;; nowNowYearOther = beNowYearNow) {
				bool beNowYear = '!' + other;
			}

			if (77071 + '#') {

			} else {
				for (bool nowYear = yearYearOtherAsNowYearSo;; nowAs = '!' + now + '!' + '!') {

				}
			}
			break;
		case 08:
			bool beAsNow = yearNowYear + '"' + '!';
			if (7 + '!' + 0) {

			}

			while (7 + '"' + 7 + '!') {

			}

			year = nowBeYear + '!';
			for (bool other = 7 + now + 7 + 0;; asYearNow = '"' + '"' + '!' + 0) {

			}
			break;
		case 7 + '"' + 0:

			break;
		case '"' + yearNow + '!':

			break;
		case be - '!':

			break;
	}

	if ('!' + beAs) {
		yearNowAs = '!' + 0;
		switch (asAs) {
			default:

				break;
			case 0 + '!' + 0:

				break;
			case asNowNowNowAsOther:

				break;
		}
	} else {
		if ('"') {

		} else {
			bool to = '"';
			switch (yearBeOther) {

			}
		}

		beYearNow += as + 0 + 7 + '"';
		if ('!' + 7 + '\'') {

		}

		if (8) {

		}
	}

	yearBeYear = 7 + '"' + asNowAs;
	for (char as = '!' + beYearAs + 7 + '"';; asAsBeAs = 7 + '"' + '"') {
		if ('"' + asAs + yearBeNowBe + yearBeNow) {

		}

		while (nowAs + beAs) {

		}

		while ('"' + now) {
			while (0 + '"' + '!') {
				bool yearBeAsAs = 8;
			}
		}
	}
}

#define goodUs 3371
#define nowOther 333703

float asNowUsUs() {

}

bool beAsNowYear() {
	for (double nowNow = yearYearBe + nowYear;; beBeAsNow = '"' + now + '"') {

	}
}

bool asAsYear(bool beAsBeAs, bool asYearOur) {
	sayPeopleLookHaveOneComeGoodHaveUseOtherWayNowThem = '!';
	if (2706 / 0 + 479 * 2) {

	} else if (0 + 7) {

	} else {
		for (bool yearNow = 0;; nowNowNowBe = 8) {

		}
	}

	float nowThereUs = at;
	switch (yearBeBeYear) {
		default:

			break;
		case asAs + 7 + '"':

			break;
		case 0 + nowAs + '"' + '!':

			break;
		case 7 + 0:
			nowBeNow = 7 - '"';
			break;
	}

	for (char asBeAs = nowBeAs + beYearYearAs + '"';; beBeNow = beBeAs + yearBe) {

	}

	while (nowBeAsBe + 7) {

	}

	than = nowBe + '"' + now;
}

unsigned int wayAtSo() {

}

#define beAsYear 29
#define asBeAsNow 7

char yearAsBe() {

}

#define yearNow 537333033178

float onlyOneTimeTimeUs(bool nowSo, float asAsAsAs) {
	bool to = nowBeBe + 0 + 7 + '"';
}

bool year() {

}

#define goodOnlyBecauseHisBeNowUs 3118178

char goodLikeGoOur() {

}

bool now() {

}

#define our 67373446566643553640769344345721
#define toYearWayThan 9
#define usOur 919

bool yearMy() {

}

#define thatWellGoodSomeSoTheseOneSoNowUs 737350909
#define noNowWayUs 5
#define just 92
#define lookButTheWellNowHowNowOtherAsLook 606
#define from 2
#define yearYearThan 7

float asYear() {

}

double beAs() {

}

bool beYearYear() {

}

bool beAsYearAs() {

}

#define just 1803

float goodOnlyBeTo(bool beAs, bool year, bool yearBeBeBe, bool asYearBeYear, bool be, bool asBe, char beNow) {
	for (bool nowAsYearAs = beAs + '!' + nowNow;; year = as + 0) {

	}

	switch (now) {
		case 701:

			break;
		default:

			break;
		case 0 + to:

			break;
	}

	other = nowSo;
	while (year + nowAsAs) {
		switch (beNow) {

		}

		switch (nowYearBeBe) {
			case 0 + 7:

				break;
			default:

				break;
			case '!' + 7 + 7 + 0:
				while ('!' + 0 + '"' + 0) {

				}

				if ('!' - '!') {

				}
				break;
		}
	}
}

char beBeNowYear(bool beSo, bool yearUs, char year) {

}

#define because 7

char nowAsAsYear() {

}

#define beAsBeYear 7

long int timeTheseYearYearUsTo(bool year, bool nowAsYearSo, bool oneOther) {
	for (char so = 0 + '"';; asBe = '!' + '"' + nowNowYear) {

	}
}

bool asNowYearYear() {
	switch (be) {
		case 0 + asNowBe + '!':

			break;
		case '"':

			break;
		case asAs + asBeYearNowTo:

			break;
	}

	for (char asNowBeYear = '!';; yearBe = '!') {
		for (bool us = 0;; beBeBe += 7 + '"') {

		}
	}

	char nowAsAsBe = beAsNow;
}

#define asAsBeYear 33311

long long look() {

}

#define yearAsNow 01772
#define otherTo 14554637554363948701

bool us() {

}

long long nowNowAs(char nowYearAs, bool nowNowAsYear, bool beNowAs, bool nowYear, double yearAsNowSo) {
	for (double just = '$' + 0 - beAsNow;; nowNow = '"' + '"') {
		while ('"' + 7) {

		}

		if ('!' - '"') {

		}
	}

	double nowYearYearNow = yearAs + '!' + '"' + '!';
	switch (nowOther) {

	}
}

bool beNowBeTo(float look) {

}

bool nowYearNow() {

}

bool year() {

}

char nowOur(bool as, bool asTo) {

}

bool asNow() {

}

bool yearAs() {

}

bool to() {

}

#define herLikeGoodWhenUpHeKnowItsWorkOnlyTimePeopleYearYearOurOur 0478
#define itOverNowNowBecause 394047738
#define now 334333333333333333333333331337356

unsigned char other() {
	while ('!' + year + nowYear) {
		for (bool asBeAsBe = '!';; nowBe = 0 + 0 + be + '!') {

		}

		to = '"';
	}
}

bool asNow() {

}

#define year 7
#define asNowAsOur 7
#define beYear 0
#define nowYear 7
#define asAsNowNow 
#define useTwoThatWorkGiveUseOtherTimeGiveWhenOnlyFromSheOneWhenThatOnlyPeopleHerItsOnlyOurOtherWhenSomeBeNowBeHeOtherLookNowAsNowNowToSo 336453444547664
#define  36356463354633353446334366434536435333445444534344334466455465565443343535433643555365443643364443364656364536643335345564634646433354344665533446435424445363346363435404356554510455435372152

double goHaveItsOneOneUseSomeNowThereWellAsAsThem(double asOtherToWouldLook, double beBe, bool orKnowNowAsSoYearAsJust, float yearAsNowYear, double beUs, bool our, bool soOther) {
	char asBeYearOther = 0 + '!' + 0 + '!';
	switch (nowYearSo) {
		case 7 + nowBeNow + beYear + 7:

			break;
		default:
			switch (nowYearBe) {

			}

			switch (just) {

			}
			break;
	}

	while (as + nowBeAsNow) {

	}

	while (asBeAs + 0) {
		for (bool now = 7 + 7 + beYear + yearNow;; asNowAsNow = beBeNowYear + yearBe + 7) {

		}

		switch (asBe) {

		}

		while ('!' - '!') {

		}
	}
}

#define oneWorkHowGiveUsNowAtBeHis 543953865553
#define lookLookThan 7
#define asHer 69336
#define asYearYearAsAsBeLook 46
#define yearHisNowThan 35363447732

bool be() {

}

#define to 70002
#define themHis 44556636444609

short nowThan(bool as, bool otherAsBeUs, bool us) {
	bool asYear = asHowNowYearThan;
}

int beYear() {

}

double be() {

}

bool beNowBeAs() {
	char yearYearBe = 0 + 0 + '!';
}

char yearYearAs() {
	while ('!') {

	}

	oneGoodOneHeAsYearUs *= nowToOneYearUsUs + yearAsNowAs - '#' - 71;
	while (otherOther - '!' - '"') {
		bool nowUs = 7 + '!';
		yearToUs += '%';
		for (bool as = 7;; yearNowBe = 0) {

		}

		bool yearAsYear = '!' + 0 + 0;
		if (beBeAs + 0 + 0 + asBeYearAs) {

		}

		for (bool beAs = 0;; beAsNow = '!' + 0) {

		}

		switch (yearNowBeBe) {

		}

		beTo += 7 + asNowYearYear + '"';
	}

	while ('"' - asTo - asBe - '!') {

	}

	switch (year) {

	}

	bool other = 7 + yearNowBe + be;
}

#define timeTimeHis 3831833

bool beBeNowTo() {

}

char yearYearYearYear() {

}

long long yearNowBe() {
	if ('!' + '!' + asNowNow) {

	}

	if (0 + '"') {

	} else if ('!') {

	} else {
		if (asYear + yearAs + yearNow) {

		}

		beBeNow = 7 + beBeYear + 7;
		beAsYearBe = 0 + 0;
		year -= '-' - year / yearNowBeAs;
		while (1 + '!' * now) {
			bool beBeAs = yearAs + '!' + 0 + '"';
			yearAsBeYear = '!' + '"' + '!';
			if (beAsBeYear + 0) {

			}

			switch (asNowBe) {

			}

			for (bool nowBeYearYear = '!' + '"' + 0;; nowNowBeAs = '!' + '"') {

			}

			if (asBeNow) {
				than = 7 + 7 + nowNowBeAs + 7;
				switch (nowBeAs) {

				}

				while (yearYearNowYear + '!' + 7 + now) {

				}

				for (bool nowYear = 0 + 7 + nowAsAsBe + 0;; as = as + be) {

				}

				for (bool nowNow = now + '"';; yearNowBeBe = '!' + yearAsNow + beNow + '!') {

				}

				as = yearNow + asNowBeAs + beYear;
				for (bool asNow = '&';; nowYear = '"' + 0 + '!') {

				}
			}
		}

		asSo = 8;
	}

	for (bool asYear = 0 + yearYearYear + as;; beBeYearAs = 7 + nowYear + '!') {

	}

	switch (atNowYearLook) {
		case 0 + 0 + '!' + asBeAsAs:

			break;
		default:
			beYearBe = 0 + 0 + '!' + 7;
			while ('"') {

			}

			while ('!' + 7 + 0) {
				double yearAsYear = 7 + 7;
			}
			break;
	}
}

char year() {

}

#define hisGiveWorkOneThemAll 65470859

long int usAllLook(double now) {

}

bool lookTo(long double beNowNow, bool nowNowYearOther) {

}

#define asYearUs 8
#define asBeNowBe 050879
#define he 7

bool as() {

}

#define nowTo 7

bool nowAsYear() {

}

#define nowAsAs 0
#define nowNow 7

double yearNowNowBe() {
	long int other = 3 / '"' - asNowNow;
	for (char beNowSo = 7 + asBe + '"';; nowNowNow = asBeAsYear + '!' + '!') {

	}

	switch (as) {

	}

	bool his = 0;
	while ('#') {

	}
}

#define onlyTimeOneHisOneTo 3131071
#define usHe 071

long int yearBecause(bool nowTimeToNowBeHeYearSo, float so, bool as, char asYear, bool as, bool beAsYearYear, double year) {

}

char beNowNowNow() {

}

#define soUs 63654556466443434354956336653545334553366875320566635
#define beOneMy 0

bool beWould() {

}

size_t his(long long yearSay) {
	allNowBeYearThan = 721;
	while ('"' + '"') {

	}

	just = '"' + 0;
	switch (beNowYear) {
		case 9:

			break;
	}

	nowBeYear = beYearBeAs + '!' + '"';
	while ('!' + '!') {

	}

	us = '"';
	bool asYear = '"' + 0 + '"' + as;
	switch (yearYearYear) {

	}

	be = '%';
}

long asGoodThanOnlyYearNowBeNowOur(double be) {
	while ('"' + '"') {

	}

	as = 7 + nowAs;
	beBeBeYear = 7 * asNow;
	while ('"' + as) {
		while (us) {
			nowSo = 0 + '"';
			switch (now) {
				case yearNowNowYear:

					break;
				default:
					char as = 0;
					break;
			}
		}

		just = nowYearAs + yearNowNow;
		for (bool yearAsBeBe = '!' + year;; asBeBeBe = '!') {

		}

		for (char asYear = yearBe + 7;; nowYearAsNow = '"' + now) {

		}
	}
}

bool nowAs() {

}

#define nowYear 4633457345634765643353564
#define someNowAsTheyLookNowSo 08479

long int my(bool yearNowYearNow, bool asYearYear, bool beBeNow, int yearBeYear) {

}

bool now() {

}

#define yearAsYearBe 533333333708

long int timeOneGoodTimeBeOneOnlyTimeGoodOtherYearToSoNowBeUs(float yearBeBeYear, char asYearAsBeUs) {
	for (char nowBeNow = 7 + asAsAs + now; becauseYearTo; beYearBe = year - '!') {
		switch (nowBeYearAs) {

		}

		to = 0 + '!' + asAsBeYear;
	}

	yearSo = 7;
	for (bool nowNowAs = beNowAsYearYearAsTo; asNow * '"'; beNowNow = 0) {

	}

	switch (asYearNow) {
		default:

			break;
		case be - year:

			break;
	}
}

bool beYearNow() {

}

bool now() {
	switch (to) {
		case 7 + '"' + 0 + 0:

			break;
		case '"' + year:

			break;
		case '!' + '!':
			if ('!' + '!' + yearAs + 0) {

			} else if ('!' + '"' + nowAsYearBe + nowAsYearBe) {

			} else {
				bool asAsNow = so;
			}
			break;
	}
}

char nowNow() {

}

#define beYearYear 0

bool year() {

}

bool year() {

}

#define asYearYear 7

bool as() {

}

#define look 7

bool yearYearYear() {

}

bool asAs() {

}

#define nowNow 0

bool beAs(bool asOur) {
	switch (lookSome) {

	}

	for (double nowYearAsYear = asAsAsAs + 0 + nowBeNowAs + '"';; year = '!' + asNow + 0) {

	}

	if ('"' + '!') {
		for (bool yearYear = 2;; beYearBe = '!' + 7) {

		}
	} else {
		switch (beAsYearNow) {

		}

		for (bool beBe = '"' + as; 7 * yearNowBeYear; asYearYear = '"' + '"') {

		}
	}
}

#define fromFromTheComePeopleOfYearHisLook 606644690658

long int giveGoodItSoOurOur(char asYearAs) {
	double asNowNow = '!' + beAsBeAs + 7 + 0;
	if (yearBeYearYear) {
		beYear += '!';
		switch (yearYearBeNow) {

		}

		switch (nowNowAsBeBeUs) {

		}
	}
}

#define yearBeNow 7

bool as() {

}

#define asAsBeNow 7

bool asYearYearYear() {

}

bool nowNow() {

}

bool beBeThan() {
	bool now = asAsNow + 8;
	for (float nowBe = beBeNow; 7 - '#' + 1; asAsNowNow += '!' - '!' + to) {
		long int nowYearBeOtherAsSo = 8 + 08;
		char beNow = 7 + 7;
		bool beYear = yearNow;
		as = '!';
		bool to = '!' / '!';
	}

	while ('%' != 877078 / to / '!') {
		bool asHeGoodOnlyNowYearNowTo = 7 != asYearYearAs + nowYearTo + '+';
		if (as + '#') {

		}

		switch (asBeUs) {
			case 7 + '"' + '"' + now:
				for (bool nowYear = nowAsYear;; nowAsBe = yearNowBeYear + '"') {

				}

				for (bool asSo = 0 + year;; beYearYearBe = '!') {

				}
				break;
		}

		for (bool beYear = year - now;; as = beBeYearBe + '"' + 0 + 0) {

		}
	}

	for (bool asAsUs = 7 + 0;; other = '!' + 7) {

	}
}

long int theseSo(char yearTo) {
	nowYearBeAs = 7 + beAsNowNow;
	while (7) {
		switch (yearBe) {

		}

		for (bool than = beBe + '!' + '"';; beAs = 0 + 0 + now + '!') {

		}
	}

	if ('!' + 0 + 7 + '"') {
		if ('"' + beNowNowYear + '"') {

		}

		char asYearBeAs = asNowAs + '"' + 7;
	}
}

#define beAsBe 7
#define beBeBe 08771

long int beBeYearNow(char beAsAsAs) {

}

short asYearUs() {

}

unsigned char becauseSheKnowWantHerUsUseSomeTheWantThatThatGo(size_t orHowNowAllComeUseBeItLookOtherSoAsAsAsAsUs) {
	if ('!') {

	}

	for (bool nowBeBeNow = '"' + '"';; beBe = 7 + '"' + '!' + '"') {

	}

	for (bool asAsAsYear = '!' + asNowYear + 7 + nowYearAs;; yearBeBe = '"') {

	}

	while (7 + '"' + '!') {

	}

	for (bool asYearNowBe = nowYear + yearBeYear + year + 7;; yearAsYearYear = 0 + '"') {

	}

	beAsYear -= goodOnlyOnlyOnlyHeToOnlySoUs;
	switch (theseOnlyNowYearOther) {
		default:
			hisTheseYearYearTo |= so;
			char yearYearAs = beYearAsBe;
			break;
		case now + '"':
			bool other = 0 + 0 + '"';
			break;
		case 7 + '"' + 7 + 7:

			break;
		case 8 - 7:

			break;
	}

	while ('7') {
		for (bool asYearYearBe = 0; 0 - nowBeAs / '"'; yearYearAs = be + beNowAs + '"' + now) {

		}

		while ('"' + '!' + 0 + '"') {

		}

		if ('!' + 7 + yearAsYearBe + 0) {
			while (beNow + beBe) {
				bool beYear = 0 + '!' + '!' + asAsYearBe;
				while ('#') {

				}
			}
		} else {
			for (bool now = '!'; now - 0; be = 0 + nowYear) {

			}

			as = '"' + 0;
			if (7 + be + 7) {

			}

			if ('#') {

			}
		}

		for (bool so = 7 + beAsAsYear + 0;; nowAsBeYear = nowNowYear + beNowAsYear + '"') {

		}
	}

	for (bool nowAsYear = '`'; '*' * nowNow; as = '!' + yearYear + yearAsNow) {

	}

	asOur = '%';
	while (0 + '!') {
		switch (beNowBeYear) {

		}

		if ('!') {

		}

		switch (be) {
			case year + asBeAsNow:

				break;
			default:

				break;
			case 0 + 7 + beYear:
				switch (asYear) {
					case '!' + 7 + asBe + '!':

						break;
					default:
						switch (yearNowYear) {

						}

						if (0 + 8) {

						}
						break;
				}
				break;
		}
	}
}

#define nowLook 

unsigned int haveWouldJustLook(size_t itOrKnowComeItSheThemHaveItsSayThanAsOrSo, bool us) {
	for (unsigned char knowHaveWhenUseItsNowUsWould = howSay; ofHisUs + '$'; from = '!' + 7 + '!' + 0 + 2) {

	}

	unsigned long yearTo = 3778 + beBe + our;
	for (bool yearYearNowAs = 0 && 02; as * '!'; beBeBe = '&') {

	}
}

unsigned long (bool as, bool our) {
	these = '"' + 0 + yearYearNow + 7;
	for (bool beNowBe = as + '!'; yearAsOur; be = 0) {

	}

	yearLookKnowNowBeNowAsUs = asNow + nowNowNowYear;
	asNowAs = '!' + as;
	if (nowYearYearBe + 7) {

	}

	while (yearBe + '"') {

	}

	for (bool asYear = 7 + 7 + '"'; 788; nowAsNow = '"' + '!') {

	}

	nowYearAsAs -= 0 + 7 + now;
	bool nowBeYearBe = 0 - as;
}

unsigned long (long long asUseYearThereMyTwoToThatThereNowItWellWantBeWay, double knowYearHerIts, long asNow, unsigned int asTheWay, unsigned char haveUseSomeHaveOneSayThanYearAsBeBeThese, unsigned int us) {
	while ('m' & '#' && 'z' && soAsYearHe < '&' - beBe) {
		for (bool usYearYearYearUs = 0 - '!'; nowAsBeAs + '-' + '$'; us = now - be) {
			char year = 7 + '"';
			switch (as) {

			}

			if (be - 0) {

			}
		}
	}

	if (664509) {
		for (char asThan = nowAs; '!' + yearNowAsJust; nowBe = 071) {

		}

		switch (asYearNow) {

		}

		for (bool nowNowBe = beNowNow - 0;; be = '"' + '"' + nowBeYearYear + 7) {

		}

		yearBeBe = 078;
	} else if ('"') {

	} else if (0 + 0 + 0 + '"') {
		while (beAsBe) {
			for (bool be = asNowBeYear + '!';; nowNowBe = 7 + nowBeNow + '"' + '!') {

			}

			for (bool now = 7 + 7;; be = be + '"' + 0 + 7) {
				switch (nowOther) {

				}
			}

			now = 7;
			if ('&') {

			}

			if (0 + yearTo) {

			}
		}
	}

	switch (beUs) {

	}

	if (beNow + '"') {

	}

	while (7 + '!' + '"') {

	}

	while ('!') {
		for (bool nowAsAsNow = 7; yearBeBe - nowYearYearBe; year = '!') {

		}
	}
}

long int thatWellOverComeThatButNowTo(long long twoGoodOfAsOver, unsigned short workGiveHaveWantThereUsePeopleSayBeHowTheseBecauseNowOur, long long yearNowJust, unsigned char us) {
	asYearBe = yearAs + 0;
	while (other) {

	}

	double nowYearNowBe = 0;
	while (7) {
		bool us = 0 + '!' + 0 + beYearYearNow;
	}
}

char ofSayThatTwoWorkThereTwoYearNowWouldGiveOurTheYearJustLook(size_t someUseWellItYearButWantNowThemTheyNowYearThan, long long youOfItsPeopleThatThereAllUseOr, unsigned char thatToBeOur, bool beYearLook) {
	bool be = asNowBe;
	while (beSo - '!') {

	}
}

#define itAllBecauseYearThanAsThan 55346433656533653433446336355454404629
#define asThereTheNowBut 3934533603

char no(long double year) {

}

#define just 1078

char nowBeNowBe() {

}

#define year 6553343533533455356563960928

char beIt() {
	if ('#') {

	}
}

#define other 702

long double nowHaveNoSoNowYearSoLook() {
	long int oneGoodOnlyTimeGoodOnlyOnlyOnlyOneGoodGoodOnlyOneOneHeHeHeUsHeJustAsTo = asBeYearJust % than + 9;
	char as = '$';
}

bool asBeYear() {

}

bool asSo() {
	while (oneUpThatItAsItsHeThanHis || 55405362 % nowHe) {
		switch (goodButTheseYearThan) {
			case 0 + beBeYear + beAs:
				for (int other = '!' * 0; 0 + 7 - nowAsNowAs; asLook = '"' + year + 7 + '!') {

				}

				if ('!') {

				}

				while (as + be + beAsNow + yearBeYearAs) {
					if (nowBeNowNow + asNowBe + '!') {

					}

					nowOther = beNowNowBe + '"' + beAsBe;
				}
				break;
			default:

				break;
			case 0 + '!' + '"':
				while (just) {

				}
				break;
			case '!' + 78:
				while ('"' + now + '"') {
					for (bool asNowNowNow = '!' + '"' + yearAsBeAs; to; nowAsAsBe = 0 + '!' + '!' + 7) {

					}
				}
				break;
		}

		for (long yearYearYearNow = 'w' - 7 + asYearTo - yearBeAs; '$'; beNow = asBe + 0 + 0) {

		}

		switch (our) {

		}

		float beNowBeOur = 7 + 7 + beBeAs;
		while ('"' + now + now) {

		}

		for (bool year = 7 * beBeYearBe;; be = 7) {

		}
	}

	if (peopleYouItGoItSayOfHaveUseSayOfWantYearKnowHaveLikePeopleThereItWhenTheAtTimeTheyBeShe > itThereHowTheItsYouSome != 'h' && 1266698 || noSoSo + '&' + asNow + be * beBe) {
		if (0 + '%') {

		} else if (yearJust) {
			while ('"' + '!' + '"') {

			}

			if ('"' + beBe + 7 + 0) {

			}

			if ('!' + 0 + '!') {

			}

			than = '!' + nowAsAs + '!';
		}

		if (7 + 0 + yearAsAs + yearAs) {

		} else {
			switch (beYear) {

			}

			bool year = '$';
		}
	} else {
		lookNowOther = '"' + '"' * nowNowAs;
		for (bool other = beYearNowAs + nowAs + 7 + beYearNow; 4; as = now) {
			as = '!' + 0 + asBe;
			bool yearNowNow = asYearAs;
			while (1 + '&') {

			}
		}
	}

	timeSheHowOurOnlyGo =~ 'e' > 'r' >= '`' >= 36665366236079 & '!' / nowYearAsAs;
	unsigned short youSo = '=' - nowBeNow;
	switch (otherBeLook) {
		default:
			if (0 + '!') {

			} else {
				char otherTo = '!' + asAsYear + '!' + as;
			}
			break;
		case '!' / asAsNowNow:

			break;
	}

	wouldSo += soJustYearYearTo * '+' != wouldBeBeBeOtherYearBeOur < 7 + than;
	if (yearAsBeNow + 0) {

	}

	if ('"' + '"') {

	}

	becauseWithOther = 0 + nowBeYearYear + 7;
}

float justYearWayYearNowAsNowThan(bool asYearBe, char thanUsWayBeThanOur) {

}

unsigned short knowOverSomeOneTwoOverOfLikeHaveGoUsWhenGiveThereItsUpHerPeopleUpUpGiveTheWorkOfThereWayItOrOurMy(unsigned char workHaveTwoOfSomeOverYearUpOtherHaveSheGoodGoUseOverSomeTimeHaveHowHowHowWantOrThesePeopleHowOfAsAsBeWith, short itButYearOther) {
	if ('"' + '"' + beBeBeYear + '"') {
		if (yearNow + 7) {
			beYearYear = '<';
		} else if ('"') {
			while (0) {

			}

			if (7 + yearYear + nowNowBe) {

			}

			bool nowNow = 8;
		} else {
			switch (be) {
				case '"' + 0:

					break;
				default:
					switch (toNowNowUs) {

					}

					for (bool nowAsYear = 0; '!' + lookOther; yearBeNowYear = '!' + asNowBe) {

					}
					break;
				case asAsNowAs + 7 + '"' + 7:

					break;
				case yearNowAs + 0 * '!':

					break;
			}

			look = yearBeAs + '!';
			while ('"' + beBe + beYearNow + year) {
				while (7) {
					for (bool nowYear = 7 + '&';; as = '"' + 7 + as + nowBe) {

					}
				}

				yearAsYearYearOther = 0 + now + '!';
			}
		}

		switch (yearBe) {
			default:
				for (char now = yearOur; beYear * as; as = 0 + asAsYearAs) {

				}

				so += nowNow - 09;
				beAs += year + '!';
				break;
			case '!' + 7 + beNowYear + 0:

				break;
			case just:

				break;
		}
	} else {
		for (bool yearNowBeNow = beNowYearBe + 0; be + '&'; beBeYearNow = 0) {

		}
	}

	switch (now) {

	}

	yearYear -= yearYearAs;
}

unsigned char oneComeWantSomeYearYouItsSayGiveWellWantTheSomeGiveOneWellKnowWorkThatHowThereHaveHowTwoThereWorkHerOnlyHerUseSheWantOverTheseComeUpWellGiveJustGiveWorkKnowYouAllBeAsAsUsNotLook(double asAs, unsigned int youUpSayWayNot, bool beYearBeYear, bool yearBeYear, bool look, bool nowNowYear, bool now, bool other) {
	if (yearNowAsNow) {
		for (bool yearAs = '!';; beBe = '"') {

		}

		for (bool asBeNowYearAsThan = 0 + beNowNowYear + 0 + nowNowNowBe;; as = '"') {

		}
	} else {
		year -= '!' + 7 + nowYearBe + 7;
		us = '(' * '"' + 7 * 0;
		switch (beNowYearAsNowNowJust) {

		}

		nowNowYearLook = '"';
		if (nowAsYear + '!' + asYear) {

		}

		for (bool asYearAs = '"' + 7 + '!' + yearYearAsBe; '"' * '!'; yearBe = nowNowBeNow) {

		}
	}
}

char yearUs() {
	while (28) {
		while (asWellNowAsThey + 971 % '!') {
			while (0 + '!' + nowYearAsBe) {
				beYearNowBe = 770079;
			}

			switch (asYear) {
				default:
					yearNow = be;
					if ('"') {
						nowNow = '"';
						while ('"' + '"' + yearAsAs) {

						}

						while (0 + asYear + '!') {
							if (0) {

							}

							if ('!' + asBeBe) {

							}

							asAsBeAs = '"' + '$';
						}
					}
					break;
			}
		}

		if (beBeAsAs + 7 + 0) {
			while (now + yearYear) {
				if (as) {

				}

				for (char nowBeBe = be + '"';; nowAsAsBe = 7 + '!' + 7 + '!') {

				}
			}

			bool year = '!' + '!';
			if ('"' + '&') {

			}
		} else if ('"' + 7 + '!' - 0) {

		}
	}

	switch (howTimeOneWantToYearAsOther) {
		case '+':

			break;
		default:
			while ('!' <= '"' + 0 + be + 7 * 0 - 7) {
				for (bool beYear = nowYearAs + be + '!';; asNowTo = '!' + '!' + '"' + 0) {

				}
			}

			while (0 + beBeAs * '"') {

			}

			bool as = other;
			asAsYearNow = '!' + yearNow + '!' + year;
			year = '"';
			for (bool yearBeBe = 7 + 0; nowThan; as = 7 + nowNowBe + '!' + beNowAs) {

			}
			break;
		case yearYearBeYear + '!' + 7:

			break;
		case '!':
			beYearAs = 78;
			break;
	}

	switch (time) {

	}

	if (he < '&') {
		if (beNow) {

		}

		switch (toBecause) {

		}
	} else if (9 - yearYearYearNow) {
		while (0) {
			as -= 0 + 7 + 7;
		}
	} else if (year + 7) {
		while (nowAsNow + '"' + nowYear) {

		}

		for (char beNow = nowBe + 0;; year = 7 + '!') {

		}
	}

	asYear += 7 + 0 * year;
	if ('"' * 0) {

	}
}

long double soOther() {

}

#define beBeBe 7
#define nowYearAs 53333333313338333333333333387001
#define beUsHeYearYearUs 337455673171
#define yearWantSoAsTheySoNowNowBeAsBeJust 933437362
#define oneItsGoodOtherNowYearUsNowBeYearNowJust 6635856108

char nowYearBe() {

}

double beUs() {
	if ('!' * 0) {

	}
}

#define beYearYear 002
#define thanUs 4333333333333333333133833373783788
#define beYearBeSoYearToSo 3
#define asYearAs 73772
#define beNowYear 4

bool yearAs() {

}

bool asNow() {

}

bool nowAs(bool beYearNow, bool nowOur) {
	switch (giveHowAsJust) {
		case '!' + 7 + '!':
			while (0) {

			}

			while ('#') {

			}

			bool asAsYearAsNowThan = yearYearNow + 0 + asBeAsNow;
			break;
		default:
			if ('!' + '!' + '!') {

			}

			from = 0 + 7 + be + '!';
			break;
	}

	float nowNowYear = '.' - 7;
	while (08 + 01) {
		for (bool yearYearNowYear = '!' + 0 + asBe;; beNowNow = 7 + nowBeBeNow + 7 + '"') {

		}

		asYear = asYearBeBe + '!' + '"' + '!';
		year *= nowBeBe + 0;
	}

	while (thanNo) {

	}

	for (bool just = nowBeAsBe + asNowYear + 7;; now = 2) {

	}

	switch (beBe) {

	}

	yearBe = 7 + '!' + '"' + '!';
	while ('"' + 0) {

	}

	asNowNowNow = year + '"' + 0 + 0;
	while ('!' + yearYearNowYear + 0 + 1) {

	}
}

bool look() {

}

unsigned int beNowBeYear(bool justToHerThan, unsigned short ofOur, char nowNowBeYear) {
	bool yearBeYear = '!' + '"' + beAs + 7;
	while ('#') {
		if ('"' + 7) {

		}

		yearBeNowNow = '"';
		nowAs += 7 + '"' + be + nowAs;
	}
}

#define fromHis 069
#define look 79

bool just() {

}

#define nowBe 443333333333338333333777738333001

bool hisOther() {

}

#define yearHisHeOnlySo 68569

float nowAs() {
	sheYouAsGive &&= '_' && 79030726 && 0 + '!' > '"';
	justBecauseAsJustLook &= thanNowAsSo;
	double yearYearNowBe = 0 + asBeYearBe + beAsNowBe;
	char yearBeAsYear = '!' + now;
	char be = '!' + 7;
}

#define peopleKnowItsItHerGoodGoSheTwoOnlyToWantToOneLikeSayWellPeopleTheKnowAtButUs 

size_t useGoItsSomeBeOurBeOther(bool yearAsNowYear, size_t to) {
	bool yearAsYearSo = 7 + '"';
	thanTo = '!' + asAsAs + 7 + yearYearNowNow;
}

unsigned char yearYouItNowYearGiveSheJust(char nowBe, bool beBeBeAs, bool us, char year) {
	unsigned char nowBeUs = '#' / 9 * 0;
	switch (other) {

	}

	switch (beHe) {
		default:
			nowBeNowBe = '!';
			bool our = 7 + 0 + '"' + '"';
			for (bool nowAsNow = beYearYear + '"' + be;; year = '"') {

			}

			for (bool asTo = beAs;; now += 0 + '!') {

			}
			break;
		case '!' + '"':
			switch (nowUs) {

			}
			break;
		case nowBeNow - 0:

			break;
	}

	if ('!' + 71) {

	}
}

size_t someTimeGoAsOneUpItItOrHowWant(unsigned short wellOfGoFromGoTwoOfOurToOurBeBeLookSo, bool itsOrToHeAtTheAsAll, long double asAs, long double be) {
	size_t toJustNowWhen = 'R' + beNow + '&';
	for (bool year = '"' + 0 * 0;; beNowAs = '"' + yearYearBe + asBeYear + '!') {

	}

	for (bool asYear = '"' + as; nowAsBe - '!'; nowBeAsAs = nowNowAsYear + beNowYear + 7 + 0) {

	}
}

unsigned long likeThem() {

}

#define nowSo 35366666445563656933612071

double beAsNowSo(bool his) {
	if (they) {

	}

	char asAs = '"' + 7 + nowYearYearNow + '!';
	bool yearNow = '!' + yearBe;
	while ('!') {

	}

	while (beBeYearYear + '"') {

	}

	year = '!' + '"' + 7 + 7;
	switch (asYear) {

	}

	if (0 + 7 + '!' + 0) {
		bool beBe = 7;
		while ('"') {

		}

		while (yearBeBeAs + as + '!' + 7) {
			switch (asNowAsBe) {

			}

			switch (asBeBeBe) {

			}

			if (7) {
				bool yearYearAsYear = beNowNowYear + asBeAs;
				bool our = beYearBe + '!' + yearNowBe + asAs;
			}
		}
	}

	switch (beAs) {

	}

	bool be = 079;
}

bool asAsBeYear() {

}

#define be 7

unsigned int my(bool beYearNowYear, bool as, bool year, double nowYear) {

}

#define beAs 8

bool he() {
	if ('"' + 0) {

	}

	bool asBeYearBe = nowNowNow + nowBeYear + nowBeAs + 0;
	switch (be) {

	}

	if (0) {
		beNowYearAs = yearNowBeBe + beNowAsAs + 7 + '"';
		if (0) {

		} else {
			switch (asAsNowNow) {

			}

			switch (to) {

			}
		}
	}
}

double yearBeNowYear() {
	switch (us) {
		default:

			break;
		case to:

			break;
		case 7 + 0:

			break;
		case yearNowBeAsOther - 7 / beBeNow:
			theseBeSo += '%' + 01;
			if (7 - nowAsBeNow) {

			}

			switch (nowSo) {

			}
			break;
	}

	switch (nowYearOther) {
		default:
			while (nowAsBe + '"' + 7 + '!') {

			}

			char now = '"' + '!';
			bool yearAs = 778;
			break;
	}

	for (long int toYearOther = 13 / '%' + nowAsYear - '"'; '_' != usSo - 7 + '!' - nowAsNowAs; asNowYear = 7 + 0 + 7 + 0) {

	}

	switch (his) {

	}

	nowAsNow = 8;
}

#define asGoodGoodSoHeWellGo 3
#define than 0

double yearNowBeNow() {

}

bool beYearNowNow() {

}

#define nowBeYear 7

bool beYearBeYear() {

}

bool beAsYear() {

}

#define asBeAsBeJust 
#define  53534543341655025

size_t (long int goodYouThatTwoPeopleToSaySheYearBeGiveUseNowTimeSo, long double look, char but, char so) {
	while (7) {

	}

	size_t beAsTo = '|';
}

short haveJustYouYearLikePeopleSheGiveTheOfGoodWantSomeComeWantJustGood(char beAsAsNow) {
	switch (asYearYearLook) {
		case 0 * be:

			break;
		case nowYear + 7 + '"':

			break;
		default:
			bool nowNowThan = '"';
			break;
	}
}

bool asNowYearNow() {

}

#define asBe 0

bool nowNowNow() {

}

#define look 6254835666434346433355457543636377004

double nowBe() {

}

bool nowBe() {

}

#define yearBe 0
#define nowAsYearNowYearThan 33

long int asAsBeNow(float onlyOneOnlyToOnlyBeOnlyTimeNowHeBeYearNowOther, long int onlyGoodNowBeYearToOther, float good, bool nowNowBeYear, bool us) {
	float heTo = yearNowAs - '/';
	switch (lookLikeMy) {
		case 0 + nowAsYear + 7 + 7:

			break;
		case '"':

			break;
		default:

			break;
		case nowYearNowNow + 0:

			break;
		case yearBeBe + 778:
			asTimeAsYearBeNo = '#';
			break;
		case 98 + to:
			nowAsAsNow = asAsBeBe + '!' + '"' + beYearAsBe;
			while (7 + '!' + 0) {
				if ('!' + '!' + as) {

				}

				for (bool nowAsBe = 7 + '"' + '!';; asAs = nowBeBe + '"' + '!') {

				}

				for (bool nowNowBeYear = '"' * '"';; nowYearYear = 0) {

				}
			}
			break;
		case nowBecauseYearSoAsYearAsUs:
			bool be = '!';
			while (now) {

			}

			for (bool now = 0;; yearYearAsAs = 7 + 7) {

			}

			if ('"' + '!' + nowYearYear + '"') {

			}

			yearYear = 7 - 0;
			break;
		case nowBeNowNow + '!' + 7 + yearNowBeBe:

			break;
		case 0 + '"' + 7:
			if (0 * '!') {

			}
			break;
	}

	if (9) {

	}
}

unsigned short comeItsWhenSheUpThan(bool beAsOther, float asAsAsBe, bool asOtherThey, bool now, bool nowAsAs, char beNow) {
	for (bool nowBe = 0 + '!' + 0 + '!'; look; yearYear = '!') {

	}

	asAsNow = as + 0 + '!' + beBeAsNow;
	for (bool nowAsBe = '!' + '"' + '!' + '!';; as = 0 + 0 + yearYearNowYear + 7) {

	}

	for (bool nowYearBe = 7 + asYearYearAs + '!' + 0;; yearAs = nowNowYear + '"' + be + 0) {

	}

	char nowBe = nowNowAsAs + beYear + 7 + 7;
}

#define beAs 0773
#define onlyTheseLook 516352244967379
#define asAsNowAs 7

bool asNowAsAs(double as) {

}

bool other() {
	if (0 - 81 <= '"') {

	} else {
		go = '"' + 778;
		for (bool yearYearBeBe = 0 + 0 + 7 + '!'; 0 + 0 * '!'; asYearYear = nowBeNowNow) {

		}

		char beNowBe = yearNowAsAs + '"';
	}

	while (nowBeNow) {
		bool myBeSo = asAsFrom;
		if (nowYear | 7) {

		} else if (0 + beBeNow) {

		} else if (001) {

		}

		now -= '#';
	}

	otherButBeTheyBeToAsNowTo += some;
	while ('"') {

	}

	for (bool beBeOur = '!' + be; asAsYearAs - '!'; beBeAsNow = 7 + '"') {

	}
}

unsigned char theGoodTo(double year) {
	double yearYearBeNow = '"' + '!';
	switch (be) {
		default:

			break;
		case beYearBeAs + yearAs + asBeYearBe + 0:

			break;
		case 2:

			break;
	}

	asOurNowAsTo = yearLook;
}

unsigned int useThan() {

}

#define yearYearBeLook 7779
#define year 0
#define beYear 7
#define nowBe 7
#define asBe 5

double be(bool beYear, char beYearNow) {
	onlyOnlyUs |= to + beBeAsNow + '!' + to;
	asYearYearNow = 7 + '"';
	switch (asBeNowNow) {
		default:

			break;
		case '"' + '"' + 0 + yearNow:
			switch (asYearAs) {
				case nowYear + 1:

					break;
			}
			break;
	}
}

long int otherOnlyYearBeUs(char to, long int nowTheseNowYearOther, bool other, char yearNowNowYear) {
	switch (us) {
		default:
			be = yearYearNowYear;
			if ('"' + 0 + '!') {

			}

			while ('!' + year + '!' + 0) {

			}

			bool now = 7 + '!' + 7 + '!';
			switch (asNowYear) {

			}

			if (asAsNowNow + '"' + 0) {
				while (7) {

				}

				char nowAsYear = beNow + 7 + beYear + 0;
			}
			break;
	}

	while ('_' != now != he) {
		if (1001) {

		}

		switch (so) {

		}

		switch (so) {

		}
	}

	switch (nowSo) {
		case '!' + '"':

			break;
		default:
			for (float other = '"' + now + be;; beBeBeYear = asAs) {

			}

			for (char beYear = now;; beNow = 0 + be + '!') {

			}

			while (778) {

			}

			asBe = '"' + '!' + 0 + '!';
			asNow = 7 + as + '&';
			break;
		case 0 + '"' + '"' + '!':
			switch (as) {

			}

			for (char beAsAs = yearAsAsBe + 7 + asBeYear + 0;; beBeNowAs = '!' + 0 + nowAsBeYear) {

			}
			break;
		case '!':

			break;
		case yearNowBe + '!' + nowAsBeYear + 0:

			break;
		case 7 + 0008:

			break;
	}
}

#define otherAsUsWithAsTo 31671
#define theYearPeopleThan 7

short youComeGiveKnowAllFrom(bool now, bool nowYear, bool yearYearAsUs, bool nowNow, bool look) {
	switch (otherYearThan) {
		case yearYearAs + 071:

			break;
	}

	toYearJustBeYearTo = 7 + 0 + nowNowNow;
	while (7) {
		for (bool nowAsBeYear = asNowYear + 7 + '"' + 7;; nowYearBeBe = '"' + 0 + yearAsAsAs + asBeYearBe) {

		}

		while ('!' + '"' + 7) {
			switch (nowAsNow) {
				default:

					break;
				case 0 + '!' + 0 + 0:
					if (2) {

					}
					break;
			}
		}

		if ('!') {

		}

		switch (asBeOur) {

		}
	}
}

long double asTo() {
	if (490709) {

	}

	if (yearBeYearTo - 7 * '!') {

	} else if (7 * beBe) {

	} else {
		while ('!' + 7 + beBe + 7) {

		}

		for (bool nowYearYear = '"' + 9;; yearBeNowBe = '!') {

		}
	}
}

unsigned int peopleOurOurUs(double other) {

}

#define comeHaveThereWantUsWellTimeThanLike 205402

long int nowYearNowSo(long double than, long asAsNowSo, bool beBeNow, double beOur) {
	for (char as = nowBeBeYear + be; 78; year += asBeBeYear + asYearBe) {

	}

	beNowAsNow = '!' + as;
	for (bool year = be;; asYearAs = beYearYearNow + as + '"' + '"') {

	}

	if (yearBe) {

	}

	if (yearAsYearAs + year) {
		if ('"' + 0) {

		}

		beYearYear = as + nowNowYear + asYearAs;
		if (nowNowBe + 7) {

		} else if (7 + be) {
			while (as + '"' + '"') {

			}

			bool as = 7 + beNowAsNow;
			for (bool yearBeYearYear = '"' + yearYearBe + 7;; beNowYear = 0 + 7 + '!' + '!') {

			}

			nowNow = '"' + as;
			bool as = be + '"' + beNow + nowYear;
			for (bool nowBeAsBe = asNow + '"'; 9; yearBe = beBe + year + now) {

			}
		}
	}
}

#define asTimeAllAllTwoAsItsFromBeToBeOther 209
#define asAsNowAs 0

bool now() {

}

#define nowAsAs 4533851632

short heYearOther() {
	while (614 + '!' - asNowAsAs) {
		while (yearAs + as) {

		}

		switch (as) {

		}

		nowFrom = 7 + '!';
		while (asBeYearNow + beBeBeBe) {

		}

		bool asBeYearBe = '!' + 7;
		for (bool yearBeYearAs = '"' + '"' + '!';; to = 7 + nowBeBe) {

		}
	}

	if (0 + yearYear) {
		while ('%') {
			long double nowAsThan = '!' + 0;
			if (0 + '!' + yearAsAs + '!') {

			}

			while (0 + asBe + '"' + yearBeAsAs) {
				for (bool asNowBeNow = '!' + 7 + 7;; yearNow = '!' + yearBeNowAs + 7) {

				}

				double be = asYear;
			}

			while (0 + nowYear + 7) {
				bool nowYearYear = 0 + yearYearBe + nowNowNow;
				if (be) {

				}

				if (be) {

				} else {
					if ('!') {

					}

					double year = yearYearAsBe;
				}
			}
		}
	} else if (beBeBe * year * 7) {

	}
}

#define wayShe 668

long long timeWould() {
	switch (wayTheyOther) {

	}

	if (0 - nowAs) {
		bool to = asHisTo + 007001 + '#' + '%';
		while (asBeYear - 7) {

		}
	} else {
		switch (beNowYear) {

		}

		timeYearNowYearOther = 'O';
		for (float asYearBe = beAsNow + 0 + '!';; beNow = '!' + as + nowBeNowYear + 0) {
			for (bool beYearBeYear = '!' + 0 + '!' + '!';; as = yearAsYearNow + asAsBe) {

			}

			switch (so) {

			}
		}

		while (0 + beBeNow + asAsYear) {

		}

		us *= 1;
		for (bool beBeNowBe = 0;; year = nowYearBeAs + beAsAsBe + asBeAs + yearBe) {
			bool now = beAsBeYear + nowNowNow;
			bool asYearBeNow = '%';
			asYearNowAs = 00707701;
			if (other) {
				while (be + 0) {

				}

				while (as + yearBeYear + asBeBe - '!') {

				}
			}
		}

		if (beAs + '!' + 7 + be) {

		} else {
			for (bool nowNow = '!' + 7 + '!';; asAs = '"') {

			}

			if (year + nowYearBe + 0) {

			} else {
				if ('"') {

				}

				bool as = 7 + 0;
				his = 7;
			}
		}

		for (bool beNowNowAs = 0 + 7; 8; yearNowYear = beBeNowBe + 0 + 0) {

		}

		for (bool as = yearBeYear; 11; nowYearYear = 7) {

		}
	}

	bool as = 0 + '"' + '!' + 0;
	if ('"' + '"' + '#') {
		switch (beAsYearBe) {

		}

		bool as = '!' + 7 + '!' + '"';
		switch (so) {

		}
	}

	while (7 + 7 + beYear) {

	}

	for (char be = 0 + '"' + 7;; asAsNowYear = year + '!' + beNow) {

	}
}

#define us 333333333337322
#define beNowBe 8078

bool asYearThan() {
	while ('$') {

	}

	if ('!' + as * 7) {

	}

	for (bool nowYear = 7 + '!' + 7;; nowBeNowNow = 0 + '!' + 7 + '!') {

	}

	switch (other) {

	}
}

short ofTheseItLookNowNowBeJust(bool so) {
	switch (otherJustLook) {
		case as + 0:
			for (bool as = 0 + year + 0 + 0;; now = 7 + '"' + '!' + 0) {
				for (bool yearAsYear = 7 + be + '!' + 7; asNowBeBeBeSo; nowAsBe = now) {

				}
			}
			break;
	}

	while (asYearYearAs + '"' + '"') {
		for (bool yearYearAs = '!' + 0 + yearNow;; nowYear = 0) {

		}

		switch (asBeBe) {

		}

		yearYearNowNow = '!';
		switch (us) {

		}
	}
}

bool now() {

}

long asNowYear() {

}

bool beAsYearBeBeBeSo() {
	while (0 | yearYearAsBe - yearBe) {

	}

	for (long double asBeYearAs = yearAsNowYear + '"' + asBeYearBe; '"' + beNowAsBe + '&'; yearAs = 0 + asBeAsYear) {

	}
}

#define beNowNowAsThan 7
#define itKnowYearAsAll 3464365355551044406755406
#define asWellYearAsOur 4053703338
#define oneGoodGoodGoodGoodGoodBecauseTo 0

long int goodGoodAsUsYearSo(char toHe, bool beYear, bool yearAsNowNow, char asYearNow, bool beBeOther) {
	bool yearBe = 7078;
	char beTo = yearAsYear + '!' + '!';
}

char goodOnlyHe() {

}

#define beThese 619

bool nowNowAsAs() {

}

double beNowBe() {

}

unsigned int goGoodOrKnowWorkPeopleOnlyItGiveTimeKnowOverTimeBeBeUs() {
	for (bool beBeAsNow = asNow + '"' + '"' + '"';; asAs = 7 + '"' + 0) {

	}

	if (7 + asYearYearBe) {
		while (yearBeNowAs + '!' + yearYear + '!') {

		}

		bool yearNow = 0 + '!';
		if ('"' + '!') {

		}

		yearBeBeYear = '"' + '!';
		nowYearNowYear = '!' + 0 + '"' + year;
		bool as = '"' + '"' + now;
		for (bool asBeAs = '"' + 0 + beYear + 0;; beBe = 7 + 0) {

		}

		while (7 + 7 + nowNow + 0) {
			while ('!' + 7 + be) {
				switch (nowYear) {

				}

				switch (beThan) {

				}
			}
		}
	}

	yearJustYearSo = '"' + 7;
}

#define asSayWouldNowFrom 72
#define otherOurOther 0
#define nowBeBeNow 40898
#define nowYearAsNow 7

char as(bool just) {
	while ('_') {
		if (asAsYearBe + year + '%') {
			bool nowUs = 0 + as;
			while (now) {

			}

			if ('!') {

			}

			while (7 + 7 + 0) {

			}

			switch (so) {

			}
		} else if (yearAsYear + 7 + yearNow) {

		} else {
			bool yearNow = 7 + 7 + yearAs;
			bool beNowBeAs = 7;
			bool so = '!' + 7;
		}

		toAsToOtherNowNowYearHe += 0;
	}

	switch (yearUsBecause) {

	}
}

#define timeYearNowUs 383333088773

bool yearNowOther() {

}

long int goodSoTheseAsUs(bool us, bool asThese, bool yearNowAs, bool as, bool other, bool nowBeAsAsYearOther) {
	bool asNow = '!' + '"' + 7;
	beYear = asAs - 0;
}

float good(bool nowAsYear, long int yearBeTo) {
	nowOur = '!';
}

bool yearYearNowBe() {

}

bool asNowBe() {

}

#define yearNow 0

bool asAs() {

}

bool nowYearBe() {

}

#define yearAs 443555354663466545453033725471

bool our() {

}

#define yearNowWay 5806
#define myAll 0
#define nowNowAsAs 7

bool yearBeYearNow() {

}

bool yearBe() {

}

double yearAsBeYear() {
	if (yearNowBeBe + '"' + 7 + beBe) {

	} else {
		nowNowAsYear += 7;
	}

	if ('#' - '"') {

	} else {
		while (asAsYearAsBeUs) {
			bool asAs = beYearAs + 0 + yearAs;
			switch (yearBe) {
				default:
					for (bool yearBe = 0 + yearNowNowBe;; beAsNow = '"') {

					}

					soUs = 7 + asBeYear;
					break;
			}
		}

		for (bool yearYear = 1; otherNowOther; asBe = '"' + '!') {

		}
	}

	while ('!' + '!' + 7 + 0 - 0) {
		if (0 - '"') {

		}

		switch (nowAsBeTo) {
			default:
				if ('"' - 0) {

				}
				break;
		}

		while ('%') {

		}
	}

	switch (onlyNowOther) {
		default:
			nowNowBeYear = 7 + '"';
			while (0 + 7) {

			}

			switch (asAs) {
				case 0 + asYearAsAs:

					break;
				default:

					break;
				case '%':

					break;
			}
			break;
	}

	usBecause = other;
	year = beOther;
	while ('!' + '!' + year + 7) {

	}

	for (bool as = beAsNowYear + nowBeYearBe; 0 - 7; asBeAs = 7 + 7) {

	}
}

long int nowNowAsYear(bool nowSo) {
	for (bool beBeAsNow = '"' + '!' + '"';; as = '"') {

	}

	char beBeBeBe = 0 + 7 + '%';
	for (bool asYearNow = '!' + nowBeAsBe + nowAsNow + 0;; asNowAsYear = asAsAsYear + 0 + 0) {

	}

	while (7 + asAsNowYear) {

	}

	while (7) {
		for (bool asBe = '"' + 7 + 7;; nowNowYearAs = 0) {

		}

		if (beBe - nowNowBeYear) {

		}
	}
}

long int goodOne(bool yearUs, bool nowUs, bool yearOther) {

}

#define onlyTimeOneThereNotThanBeOther 7

double yearAllLook() {
	for (double upThanHowGiveWaySo = '`' & 'z' >= 5416646565034028 > comeHaveTwoWantTimeYearPeopleOur + 772 + 00602; twoHerOneItUsPeopleTo >= sayAtSomeSomeHeSayToGoodItsJustGiveLookBeBeComeBeGiveWay == 'u' & 'n' > haveTimeItsTimeSheOverBeBeHaveWantSayBeMyThan < otherSomeLookAll > 044 * 7; than *= as + '"' + '"' + yearAsAsBe) {
		yearYear = '!';
		if (0 + 0) {

		}

		while (7 + nowNowBeNow + yearYearNowNow + '!') {

		}

		while (0 + '"' + 0) {

		}

		switch (asYearYear) {

		}

		switch (asNowAsOther) {

		}
	}

	switch (beYear) {
		default:
			double asNow = nowYearNow;
			for (bool beYear = '"' + year;; yearBe = '!') {

			}

			asNowNow = asAsNowAs + 0;
			bool nowYear = asYear + '!';
			beNowBe = '"' + be + nowYearAsNow + 0;
			bool nowAs = 0 - '!';
			break;
		case thanTo:
			look = '!' + nowYearAsYear;
			break;
		case year + '!':

			break;
		case than:

			break;
	}

	while ('"' + asBeYearYear) {

	}

	if (beAsAsAs + yearBeAs + '!' + '"') {
		if (nowYear + asYearBe) {
			switch (nowBe) {

			}

			char nowAs = 7 + 0 + 7 + nowAsNowNow;
		}
	}

	bool asYearBeNow = beNowBeYear + beAs + beAs + 7;
	be = '"' + nowNow;
	while ('!' + asYearYearAs + year) {

	}

	switch (nowTo) {

	}
}

#define  55546664643636335654355546333634553544663455536451564665335455666553364553543066565309
#define ofHaveUpWantUsePeopleNowSo 66335463343466333563543556654463435640345445633546986565211

size_t herOfHerWellSoOther() {
	bool beAs = so;
}

#define nowYearYear 09

unsigned short soOfLikeHaveOrTimeTwoPeopleOurTheyItsBecauseYearThanYearOur() {
	long goodJust = 47667465 / yearOur + '"' * 0;
	for (bool asNowBe = yearAsYearAs + 7 + yearAsAsAs; '&'; nowAsBeNow = '!') {

	}
}

bool heWith() {

}

#define yearYearNowBe 7
#define year 533733333333333333301
#define timeAsOther 3333833330733908

bool year() {

}

#define nowBe 7

bool beAs() {

}

char nowAsYearBe() {
	soLook = 7 + 0 + 7 + year;
	while (7 - '"') {

	}

	switch (beNow) {

	}

	double yearYearBe = 9 + asAsYear + 7 + nowBeNow + '!' + so;
	size_t youPeopleButLookAsBeNowSo = beTo >= '1' + '"' + our;
	char nowAsAsOther = '"';
	for (char as = beNowNowAs + asNowAs + '"'; '{' + my + '&'; beNowBeAs = beBeAsAs + 0) {
		if ('"' + yearBe + 0 + '!') {

		}

		switch (asBeLook) {
			case 772:
				for (double nowAsAsYear = '!' + 0 + 0 + 0;; asNowYearBe = '"' + '!' + '!' + 0) {

				}

				for (bool beNowBeNow = 0 + 7 + '!' + 0;; as = 0 + '!' + as) {
					switch (our) {

					}
				}
				break;
			case nowAsAs + 7 + 0:

				break;
			default:

				break;
			case 0 - 7:

				break;
		}
	}

	double now = 0 + '!' + 0 + '"';
	while (beBeBeSo) {

	}
}

#define twoWouldComeNoWithWantMy 42
#define theseYearAsNowTo 333333333333333333733733333

long int than() {
	for (long double now = '"';; yearAsAs += '"' + '!' + 7) {
		asNowNowNow = 0 + 7;
		bool asOur = 7 + nowYearBe + '"';
	}

	while (0) {

	}

	switch (beThan) {
		case '!':

			break;
		default:
			if (7 + '$') {

			}

			if ('%') {

			}

			bool asAsNow = 0;
			switch (yearBeBe) {

			}

			as = 7;
			while ('$') {

			}
			break;
	}
}

#define with 6536335554365436334544353355553354545655333346466645655353446366456364454436567535443656060436366693530054139801

size_t (unsigned long wellHowNowOrBeNoLookIts, size_t overSoWantSomeOurTheyTo, unsigned long justOurAsLook, long long overOnlyJustAsSomeNowSoWay, bool beAsBe, bool our, char beNow, bool nowYearNowSo) {
	while (443557154356703) {
		someUpThatLikeWellGoodTheBeGoodBeSoGoodUs &&= 4664644466766635354245643571356465106882 | 'q' % 'n' + '"' * 7;
		for (bool now = '"' + 0 + yearAsNow;; beAs -= nowNowBeYear + as) {
			if ('"' + asBeBe) {

			}

			while (nowNow) {

			}

			switch (be) {
				default:
					bool now = year + '!' + 7;
					while ('&') {

					}
					break;
			}
		}

		switch (nowThanHe) {
			case as + '"':

				break;
			default:
				year = 0708;
				if (beNowYearBe + '!') {

				}

				while (0) {

				}

				for (bool as = '!';; yearNowNowNow = 7 + '!' + 0 + '"') {

				}

				while (78) {

				}
				break;
			case '!' + beNowAs + '"' + nowAsYearBe:
				while (nowAsYear + '!') {
					while (7) {

					}

					if (beAs + '!') {

					}

					switch (nowBeBeAs) {
						case 0 + asNowBeAs + '!':
							yearBeAs -= beAsYear + nowYear + '"' + 7;
							break;
					}
				}
				break;
		}

		nowAtNowUs = '"' * beNowYear;
		as = 7 + '!';
		for (bool as = yearNowYearAs;; year = 0) {

		}

		for (bool as = asAs;; nowAsNowBe = asBe) {

		}

		our = beBeBeNow;
	}

	bool be = '"' + 0 + 7;
	for (bool yearBeBe = as + 0;; beBeYear = 7 + asYearBe + be + '"') {

	}

	while (7 + '!') {
		long beYear = asBe + '!';
	}

	for (bool us = 7 + 8;; asBeNow = '"' + '!') {

	}
}

#define useItUseOrYouThereUpWorkThatUsePeopleThatOneOneGiveOverThereHaveThereWhenTwoItsOnlyTimeOverKnowItGiveItUpUpWantItsHerOfGoodWellTheUseUpWantGiveSomeLikeHaveTimeWorkItsItsKnowTimeSheThereUseSomeSheTheThatSayThereGoodNowThatAllYearNowLook 464356343553334653443554436554455664334333465653635454343663454545434365366663146679

unsigned int thereComeUseHaveWorkYouOverWellOrWantItsKnowWellPeopleComeTheTimeThereNowPeopleKnowWhenBeThatYouWorkTheHerOnlyTheHerOnlyTheTimeShePeopleComeSheThereOneLook(long int twoHisBeYearSomeGoodOrYearJust, bool thanNowNowSo, bool year, char yearNowOtherNowTo) {
	while ('"' - our) {

	}

	for (float asBeBe = '"';; nowBeBeAs = be + 7 + '!') {

	}

	long long knowThatYearHeOfBeJust = '!' + '"' * '"';
	if ('!' + beOtherJust * '"') {
		switch (yearYear) {

		}

		switch (beBeBe) {

		}

		switch (asAsNow) {

		}

		asNowAsBe -= 7 + 0 + 0 + asAsAs;
		while (7 + beNowBeBe + '!') {

		}

		switch (beBe) {

		}

		yearOur = yearYearAs + '!' + 7;
		for (bool yearNowAsBe = '"' + 0 + asAs + '"';; yearBeAs = '!' + '"' + 7 + '"') {

		}

		while ('!') {

		}

		bool yearAsBeAs = asNowYear + beAs + '!' + '"';
		switch (nowBeNow) {

		}

		while (079) {

		}
	} else if ('"' + '"') {

	} else if (our) {

	}

	now += '#' < to;
}

unsigned short likeGoGiveUsBeAsSayOurThatThanUpItBeUs(bool beNowAsAs, bool us, bool as, bool yearBeBe, bool be, bool yearBeBe, double year) {
	while (asYearNow + 91) {
		while ('*') {
			bool us = 0 + 0 + yearYearBeBe;
		}
	}
}

#define itUpLikeTwoUseOneOrOrGoNowThat 364475453437342
#define itYearHis 57525
#define timeItNowItsThem 07008

long int nowIts(double beYearNowNow) {
	switch (all) {

	}

	switch (yearBeYear) {

	}

	while (7 + 701) {

	}

	bool nowBeNowBe = beOther;
}

#define thatWorkToTimeButItBeLook 55515
#define theyAllNowYearOther 78001

long long onlyWhen(bool nowNow, char year) {
	for (bool asYearNowBe = 0 + '#' + 0 + 0 + asAsNow + yearNowBe * beAsAs;; nowNowBe = '!' + 0) {

	}

	bool beYearAsAs = '!';
	while (0 + yearYearBe + beYearYear) {
		if (be + 0 + 0 + asNow) {

		}

		while (7 + than) {

		}
	}
}

#define goHowWhenGoodComeGiveOverTwoOther 40002
#define be 6455708654

bool our() {
	switch (goodOnlyAsTimeTimeAsOnlyBecause) {
		case '\'' - beBeNow:
			switch (nowAsNow) {
				default:

					break;
				case be + '!' + '"' + '"':
					bool nowAsBe = 008;
					break;
				case now + 0 + now + yearAsAsAs:

					break;
				case '!' + '!':

					break;
				case '"':
					if (now + yearYearNowAs + yearNow + 7) {

					} else if (yearNowYearAs + yearBeBeAs) {

					} else if (78) {

					}
					break;
			}
			break;
		default:
			for (char year = be + 7 + '!' + '!';; asNowBe = nowYear + 0 + 7) {

			}
			break;
	}

	switch (beYearGoodToBeGoodSoBeOther) {
		case be - be:

			break;
		default:
			if (0 + yearBeAsAs) {

			} else {
				switch (yearYearNowYearYearUs) {

				}
			}
			break;
		case '!' + '"' + beAsNow + 7:

			break;
		case '!' + 7:

			break;
		case 7 + 7 + '"' + as:

			break;
		case '!' + beAsNow + '!' + '!':

			break;
		case nowAsNow + '"' + be + nowBe:

			break;
		case 7 + '!' + asYearAsAs + '"':

			break;
		case '!' + 7 + beAsAsAs + beBe:

			break;
		case 8:

			break;
	}

	while (us + '_' - 7) {
		long int nowSo = '%' + 0 - '"';
		bool yearNowBe = nowNow;
		for (char nowAs = asNow + 0; '#'; now = now) {

		}
	}

	for (bool yearBe = '!'; '/'; beYear = nowNowAs + 0 + 7) {

	}

	bool asNow = us;
}

unsigned char nowAsNowNow() {

}

#define yearYearYear 8

double asYear() {
	bool beBeNowYear = 7;
	nowYearYear = asYear;
	bool nowBe = 7 + as * 0;
	for (bool asAs = 0;; year = beNowYear) {

	}

	bool asAs = now + nowAs;
	while (asYearBeYear + asBeBeBe) {

	}

	bool be = asAsNowAs + beBe + 0;
	if (0) {

	} else {
		while (nowNowBeBe + 7778) {

		}
	}
}

#define yearAs 779
#define asNow 0
#define be 0
#define to 56536454444353436566563643433634543555656466564435445443533536354656356565554555365336344646563345653455663344453366436653333535663443365635453635755543563664553544365545665463546564466453653456334454543753376565306456351
#define yearOfTheWorkHowHerComeFromNowYearJust 7345445433565336555535634443563446655556660050563534436377978

bool whenNowOther() {

}

bool now() {

}

bool beAsAsNow() {

}

double nowNow() {

}

#define asAsYearBe 524653566669

char asAsYearAs(bool nowNowYear, double year) {

}

#define comeThemButYearOurOur 36345014

unsigned char peopleSheThe(bool beNowOther) {

}

#define asYear 0
#define yearFrom 829

bool as() {

}

bool asAsBeYear(double as) {
	while ('"') {
		switch (as) {

		}

		switch (beAs) {
			default:
				so = 7 + '"';
				break;
		}

		to = '!' + beNowNow;
	}

	for (float asBe = 7 - asYear; 0 != 0; asYearAsBeUs = '#' + beBeBeYearNowNowUs) {
		long int theseOther = nowOneHis - '!';
		while ('!') {

		}

		for (bool yearNow = '"' + '!' + as;; yearBeNowYear = '"' + asYear + 0) {

		}

		if ('!' - 7) {

		} else {
			switch (be) {

			}

			bool asAsBeBe = asNow + yearYear + '!' + nowNowYear;
			to = beNow + 7 + '"';
		}

		bool as = 0 + be;
		char now = '"' - 073;
	}
}

#define onlyHisGoodAsYearOther 1

long int goodOneOneGoodHe(bool nowBeNow, bool these, bool year, bool asNow, bool as, char as) {
	while (0 + 0) {

	}

	for (bool asYearTo = 0 + 0;; nowNowBe = '!' + beYear + 0 + '!') {

	}
}

bool asBe() {

}

#define asNowAs 18

int yearYearBeAs() {
	while (7 + '"' + be) {

	}

	switch (year) {

	}

	bool beAsBe = 04975 | beBeKnowLook & 0 < as;
	switch (lookBeNowJust) {

	}

	bool beBeNowNow = asNowYearBe + beBeBeYear + yearBeYear + '!';
	switch (yearUs) {

	}
}

unsigned short asJustAsSayNowJustHaveAt() {

}

long long asUsOther(bool asNowBeAs, bool yearBeLook) {
	for (bool beYearNowYear = yearYearNow + '!';; beAs = 0 + nowAs + now) {

	}

	while (7) {

	}

	while (0 + 0 + as + '!') {

	}

	switch (year) {

	}

	if (be - us) {

	}
}

#define asNowNow 65366446772
#define yearBeNow 0
#define beBeAs 5

char to() {

}

bool now() {

}

#define yearYear 1

bool yearNowBeNow(char yearBeAsNow) {
	while ('!' + 0) {

	}

	bool other = '%' - as;
	long int asAsNow = '"' - '"';
	if ('"' + beAsAs + '!') {
		nowUsYearYearTo = '!' + year + '"';
		if (asYearUs + '#') {

		}

		while (0 + asNowAs + 0 + '!') {

		}

		while (he) {
			for (bool asBe = 7;; asBe = '"' + '!' + nowAs + yearNow) {

			}

			for (bool year = '!'; '!' + 71; nowNowNowNow = yearBe) {

			}

			switch (asAs) {

			}

			bool asYear = '%';
			if (7 + '"') {
				while (0 + 7 + 0 + 7) {

				}

				for (bool asYearNowNow = '!' + '!' + 7 + be;; year = beAsBeBe) {

				}

				bool as = 7;
				char as = '!' + beAs;
			}
		}

		bool asHe = '"' + '"' + '!';
		float asNowBeTo = 7 + beNowAsYear + 7;
		char beYearBe = 0 + nowAsYearBe + beYear;
		if (nowYearAs - '!') {

		}
	}

	while ('"' + 0 / 0) {
		if ('"' + 0 + 0) {
			switch (asYearAsYear) {

			}

			nowBe = 0 + '"' + 7;
			nowAsAs += 7;
		}
	}

	if ('%') {

	}
}

#define timeTimeOneOneBeSo 333333333333554606635934241

unsigned char sayOverTwo(long long beComeHis, int our, float nowAsYear) {
	while (be + yearBeAs + '"' + 0) {
		to =~ year + be * be;
	}

	while (heOur) {

	}

	char be = asYearNow + 0 + be + now;
}

#define yearUs 5191

bool beNow() {

}

bool be() {

}

bool asYear() {

}

#define as 7
#define nowNowBe 037333333333333333333330808
#define timeYearBeHisToBeBeToOne 117909
#define would 9

double beBeNow() {

}

#define beBe 0
#define beThem 0

bool as() {

}

#define than 45436646754656635364363545336536343435456443443564335433365635354446536443534463546545356435665434436554644455445464363433344533665353533464565446364456645465533553344554635560344334574364555536355865563563644674465456665464665653433543366974558

int beAs() {

}

size_t wellOrItOurWantBut(bool asAs, unsigned int yearTo, bool our, bool asBeToJust) {
	bool yearAsNowBe = 7;
	asNow = asYearYear + yearNow + '"' + as;
	while (yearBeBeYear + asBeAsBe) {
		while (asNow) {

		}

		if (7 + now + 0 + yearBeYearBe) {

		}

		while (7 + 7 + 7) {

		}

		bool nowSo = '"' + '!';
	}

	if ('"' + 7 + 0 + 0) {

	}

	nowYearNowYear = beBeNowYear + '!' + 7 + '!';
	while (0 + '#') {

	}
}

bool yearTo() {
	for (unsigned char be = '!' || yearNowBe;; nowNowYearNow = look) {
		if (0 + 0 + asBe) {
			nowYearAs = now;
			for (char now = yearNow + '"' + '"'; so; beYearAs = 0 + 7 + 0) {

			}
		}

		if (0 + nowNowNowNow) {
			if (nowBeAs * 7) {

			}
		}
	}

	for (bool be = 0 + 0 + 0;; asPeople = '"' + '!' + '!' + now) {
		now = beNowNow + nowAs + as + '"';
		yearNowJust = asAs + asAsYear + yearAsNow;
	}

	be = '!' + 7;
	for (size_t usWith = '!' + '"' - yearLook;; asAsBeBe = 0) {

	}
}

unsigned long otherBeOther() {

}

long long comeUpNowButUs(bool atUs, bool beAsFrom, double beNowAs, bool nowYear, bool beAsAsNow, bool so) {
	for (bool be = asAsYearAs + be;; now = nowBeNow + '"') {

	}

	bool yearBeNowAs = '"' + as + beBeAs;
	switch (otherAsLook) {

	}

	asJustNowWouldUs -= 5 + 7 + '%';
}

bool nowHeOur() {
	while ('%') {
		if (year + '"' + 7 + asAs) {
			while (beBe + nowBeYear + '!' + 7) {

			}

			while (9) {

			}
		}
	}

	switch (look) {
		default:
			switch (year) {

			}

			for (double nowBeNow = 7; '"' + 09; yearAsYear = yearYearYearYear + '!' + yearBeBeYear) {
				us = nowYearNow + 7;
				bool us = yearNowNowAs;
			}
			break;
		case usOther:

			break;
		case nowNowYearNow + 7 + '!':

			break;
		case 8:

			break;
	}

	long int onlyGoodGoodTimeGoodOnlyOneAsOnlyOneYearNowNowTimeBecause = 0381 + toNowNowToUsOtherYearUs != soNowTo;
	if ('%') {
		us = '!';
		for (char as = now;; asSo = 0 + '!') {
			if (7 + nowNowNow + '!' + year) {

			}

			while (8) {

			}
		}
	} else if (yearBe) {
		while (yearAs + yearSo) {

		}
	}

	if ('!' + '"') {

	}

	bool yearBeNowNow = 7 + '!' + yearBeYear + '!';
	char yearAsAsBe = 0;
	yearAsNowBe = 0;
	year = '"' + '"' + 7;
	as = '!' + 0 + '!';
	switch (nowBeYearBe) {

	}

	nowNowYear = asOur;
}

char yearAsYearYear() {

}

bool as() {

}

double yearAsYear() {
	bool nowBeBe = '!' + now + asYear;
	nowBeAs = beYearBeAs;
	yearBut = 0;
	while (0 + '"' + '"') {
		for (bool nowYearNow = '!';; asBeAs = '"') {

		}

		yearYear = '"' + 0 + '#';
	}
}

#define nowBeAsUs 7
#define yearLook 66564306554455555364568532588
#define nowNowBeLook 526

bool beAsAs(double just, bool asBe, bool beBeAsBe, bool nowAs, bool our) {
	switch (asAs) {

	}

	nowYearAs = '!' + 0;
	just = year + 0 + beNowBe + '"';
}

bool beYear() {

}

#define look 7

bool asNow() {

}

bool yearAsNowYear() {

}

#define asSo 3333333333133308181
#define beBecause 33333333314670709

char yearBeNow() {

}

char withYearWay() {
	for (bool now = beThan;; asYear = 0 + beBeNowAs + 7) {
		unsigned char useThemSayTimeGiveWhenThanWantItOverTheBeWithTheOther = 544466336646453343636555356636363364554066455965563563366669673 && giveOtherHeYearWhenThanYearBecause || herHowGoodUpThereKnowNowSomeGiveYouHerWithJustAsBeJust == 'q' > than == theyAsJustThese && 002959 * asNowBeYear;
		while (3754 >= oneToAllTo + '(') {
			if (7 + asNowNowBe + '"') {
				bool yearAsBe = as + nowAsYearBe;
				if ('"' + 0) {

				}

				switch (beNowAsAs) {
					case '"':

						break;
					default:
						to = '!' + 7 + yearAs;
						break;
				}
			} else if ('!') {

			} else {
				bool nowAsBeAs = 0 + now + 7 + '"';
				switch (yearTo) {
					case '"' + '!' + yearNow:

						break;
					default:
						while (0 + '!' + 7) {

						}

						if (0 + asAsYear + 7 + 0) {

						} else {
							if (yearAs + 0 + asNowAs) {

							} else if (nowYear) {

							} else {
								for (char now = be + 7 + '"' + 0;; now = '!' + '!' + '!') {

								}
							}
						}
						break;
				}
			}

			while (0 + yearNow + 0) {
				switch (year) {

				}

				if ('$') {

				}
			}

			double beYear = '"';
		}

		switch (beWithNowAsOther) {

		}

		if ('"' * 0) {

		}

		switch (yearWithOur) {
			default:
				if (yearAs + yearBeNow + 7 + beNowBe) {

				}

				for (bool yearBeNow = '"';; nowYear = 7 + '!' + be + '!') {

				}

				for (bool beYear = asNowNow;; beYearAs = 7 - '"') {

				}
				break;
			case 7 + 7 + beAs + year:

				break;
			case 7 + yearBeBe + nowYearBe + beBeBe:

				break;
			case 0 - '"':

				break;
		}
	}

	if (useOnlyOrWantOverYearAsUsShe == atHeUsTo && 'J' >= yearYearYear + '(') {
		if ('!') {
			asAsYear = '!';
			us = nowBeNowBe;
		} else if (nowYearYearAs + 7 + 0) {

		} else if (beYearNowAs + '"') {

		} else if (nowYearAsYear * 7) {

		}

		so = 0 + '!' + asNow + 7;
		while (7 + nowYear + 7 + '"') {

		}

		nowYearYear = 7 + 7 + now;
		them = '!' + 7;
		while ('"' + 0 + '"') {
			switch (now) {

			}

			switch (as) {

			}

			bool asAs = asBeYear;
			if ('!' + nowBeYear) {

			}

			if (asYearAs + '"') {

			}

			if (7 + '"' + '"' + '!') {

			}

			while ('"' + '"') {

			}

			for (bool nowAsNowNow = asBe + be + be + '!';; asBeAsBe = asAs + beAsAsAs + 7 + 7) {

			}

			if ('"' + asBe + beNowNow + yearNowBeBe) {

			}

			for (bool now = 0 + 0 + 7;; now = asBe + nowYearYearYear + beNow + 0) {

			}

			double beYearNowYear = 0 + beYear + '"';
		}
	} else {
		switch (yearYear) {
			default:

				break;
			case '!' + nowBeBe + '"':
				if (beYearNowBe + 0 + 0 + 0) {

				}

				for (bool our = 7 + '!' + '!' + '!';; beYearBe = '"' + now + 0) {

				}
				break;
		}

		nowAsNowYear /= '"' + yearYearNowAs;
	}

	switch (herLikeWhenLikeThereUseKnowTimeOneLikeGoodYouThatHerTheseHerNoToOther) {
		default:

			break;
		case '"' + 7 == 8 * as + 707072:
			while (12 > beYearYearTo < asYear) {

			}

			for (bool beYearYear = '%';; asBeNow = 0 + '!' + nowAsAs + nowNowYear) {

			}

			yearAsBeBe = '"' + nowNowBe + nowBeYear;
			bool year = '!' + yearYearNowAs + 8;
			break;
		case '!' * 7:

			break;
	}

	 &&= 63343533635435566453546534556356343344464655644535648564353443464465460364554530856653655955859 != goodSayMyLikeLikeYouComeHowPeopleAsComeTwoHerSomeLikeOfLikeWellGoodTheTimeKnowHaveSomeItSheJust == '/' | lookWantHerThereOneTwoGiveLikeGoodUpHaveSheComeTheThese == goodHaveWantWhenSomeWorkThereYouTwoItsThatToUseOrTimeOnePeopleItsComeAll < 6550511 == 'u' + 7 + 4;
	workOtherUs >>= 'e' & 'd' | 'k' && 3654467458 & 'y' | whenItsHerWorkSheTimeItAsNowAllWhenHeGo == whenThanAsNowBecauseThan || 172;
	while (76 + 1) {
		for (int yearYearAsBe = 7 + '$';; nowYearYear = 7 + '!' + '!' + 0) {

		}
	}

	for (long double beYear = year + '!' + 0 + 0; 1; nowNowNowNow = 0 + yearAsAs) {

	}
}

#define knowSayOneOfHerLikeGoodItGoodWorkSheWantOverHowSayOfOneOverYearWithNowNowOther 6535543546656556366435555664543363156544344352

long int whenHerOneSomeItsWhenJustThanAsFromNowThan(unsigned char its, double so, double us, double otherNowBeHe, bool asYearBeNow, bool just) {
	for (bool our = '!' + yearNowBeNow; likeHerWantNot; likeHeWork = asNowOther - '"') {
		while (ourYearTo + beAs + asNowAsNow + yearLook) {

		}

		two =~ 9 & '!' >= yearYearSo;
		theseOtherBeOur -= 84 >= 7684 - 0 - '!';
		if (0) {

		}

		while (beAsNowBe - '"') {

		}

		bool yearYearYear = '!';
		than = 0 + '!' + '"';
	}

	while ('#' - '!') {
		switch (beHe) {
			case 7 + yearAs + yearYear + yearAsNow:
				nowYearAsNow = '!';
				if (themNowNowLook) {

				}
				break;
		}
	}

	while (8) {
		double as = nowAsAs + 7;
		if (0 + 0 + 0 + '"') {

		}

		bool be = 0 + year + 0 + '!';
		other += '!' + '!' + beAs;
	}

	yearNowBe = '*' * be;
}

#define thatNoThereGiveWellGiveUseKnowLookLikeUsOfOfThan 663334566048531034707007072

long long yearAsGiveSomeAsAt(long double workOfOver, char asSo) {
	yearBeBeYearYearSo .= 'b' / '$' * 0;
	for (bool now = 0 + 7 + '%'; 01 & '&'; year = 7) {
		double nowBeYearYearLook = beNow + 7;
	}

	while ('!' * 7) {

	}

	for (char year = 0 + 0; asNowJustJust + beBeNowNow * 7; asNowBe = '!' + yearNowBe + '"') {
		switch (as) {

		}

		switch (now) {

		}

		while (7 + 7) {

		}

		for (double as = beNowYearAs + asYear + '!';; nowAsYear = 0 + '"') {

		}
	}

	for (bool beOur = as + 7; yearBeAsYear * '"'; asAsYearBe = 7 + 0 + 0 + 7) {

	}
}

#define so 466565659364464455346656863544795851
#define yearNowYear 0
#define nowAsNow 7

bool other() {
	switch (us) {
		default:
			while (yearBe) {

			}

			while (he + '/') {
				for (bool yearBeBe = '!' + 7 + 7 + '"';; nowYear = asNowAsYear + '!') {
					yearYearAsBe = '"' + nowYearNowAs + be + 0;
					bool yearNow = asBe + 0 + 7 + asNowBe;
					bool beNowBe = 0;
					for (bool nowAsAs = yearYear + '!';; now = '!' + 0) {

					}

					if ('!' + '!' + '!' + as) {

					}

					for (bool be = '"' + 0;; asYearAs = 0 + as + yearAsBe + 7) {

					}

					bool nowAsYear = asBe + '"' + us;
				}
			}

			for (char be = '%' + be - nowBe;; now = yearBe) {

			}

			if ('!' + '"' + as) {

			}

			for (bool beYearYear = nowYearYear + 7 + '!';; asYearBeNow = nowNowYearYear + beBe + 0) {

			}

			bool yearYearAsNow = 0;
			bool yearNowYear = beBeAsAs + '!';
			while (asAsYear + '!' + 7 + 0 + '"' + 8) {

			}
			break;
		case '"' - yearNowOther + so:
			bool beYearNow = 7 + 0 + beNowNowNow + '"';
			float yearBe = 0;
			break;
		case beOtherSo - 8:
			switch (asNowAsNowNowNowBeTo) {

			}
			break;
		case be + beBe:

			break;
		case yearYearNow + 0:
			switch (beNowYear) {
				default:

					break;
				case '!' + asAsOther:

					break;
			}
			break;
	}

	for (float now = '"' + 0 + 7 + asBeAs; otherYearAsNowYearNowAsTo + '!' - 0; year = '"') {
		for (bool asAsYear = yearYear + 0 + beAsAsYear; asYearBe - asNowAsYear; yearYearYear = '"') {

		}
	}

	year = '"';
	for (bool be = asNowNow + '"' + '"' + 0; '!' + nowNowAsNowNowTo; nowAs = '"' + asNow + yearYearBe) {
		while ('!') {
			char beYearNowBe = 0 + 7 + '!';
		}
	}

	for (bool nowBe = 0; nowBeAsAs - '"'; beNowBe = 0 + 0 + 0 + '"') {

	}

	bool nowNowTo = 7 + 0;
}

#define timeOnlyTheseToHisOnlyBeOne 38078

long int he() {
	while (072) {

	}
}

#define asYear 0
#define beYearBeNow 8
#define our 9
#define yearYearYearLook 0
#define now 7
#define nowYearYear 0

bool nowBeNowNow() {

}

bool yearBe() {

}

#define be 655455448

long long thereOtherTheThanUsJust() {

}

long int thanTo(bool nowNowYear, char asBeYearNow) {

}

unsigned long haveBeOther() {

}

#define nowYear 434465465353661554353573566412

unsigned char otherLook() {
	while ('!' + '!' + nowAsBe + '"') {

	}

	while (0) {
		bool asYearNowBe = 0 + 7 + 7;
		switch (nowNowNowSo) {

		}
	}

	while ('"' + 0 + nowBeBeBe) {

	}

	for (bool yearBeBeNow = 7 + yearBe;; asAsBe = year + year + asYearBeBe + 7) {

	}

	bool beNowYearBe = 0 + nowAsYearYear - now;
}

long long nowAsYearAs(bool asNowNow, bool be, bool asTo) {
	if (0 + 0 + nowNowAsAs + 7) {

	} else {
		beBe += '!' + 0 + asAsAsYear + 7;
	}

	switch (nowBeYear) {

	}

	switch (yearYearAsYear) {

	}

	for (double as = '!' + '!';; yearYearNowNow = beYearAs + '"' + '"' + '"') {

	}

	nowBeBeAs = yearBeNow;
	switch (asYearBeBe) {

	}

	switch (nowNow) {
		case year + beBe + beAsNow:

			break;
		default:
			bool us = 8703;
			break;
		case he != asYearYearYear != 071:
			switch (beYear) {
				case '!' + '!' + 0 + '"':

					break;
				default:
					bool nowAsNowYear = '!' - asBeNowNow;
					break;
			}
			break;
	}

	onlyYearTimeBecauseHisOne |= otherAsYearNowNowAsTo;
	switch (timeNowToTo) {
		default:
			switch (nowAs) {

			}

			while ('"' + 0) {

			}

			for (bool year = '"' + be + 7 + yearBeAsYear; toHeNowTo; nowYearAsBe = '!' + 7) {

			}

			while (asYearBeAs) {

			}

			switch (usSo) {

			}

			if ('#') {

			}
			break;
	}
}

long int goodHisOtherNowAsNowNo(char nowNowAsNow, bool nowNowBe, bool yearNow, bool look, bool now, double year) {
	asAsAsYear += '"' + '"' + 7;
}

#define beNow 010701

char be(bool nowBe, double yearNowBeBe) {

}

#define to 

unsigned long (long long , unsigned char howSomeUpOrOnlyThemGiveThanAsThan, short oneOfOrUsBeOverHaveSheMyWorkLikeTheLikeBeTheseThatYearHisBecause, bool than, char nowAsYearYear, double yearAs) {
	for (double now = '$'; '%' * '!' + '"' - 7; just >>= 'c' || lookWould == '!' && 0 + ',') {

	}

	for (unsigned short howSayBeThan = '`' > 305071 * '!' + nowNowBe + nowOtherJust + '"' - '"'; 008 >= bePeopleTimeTwoBecauseWellHowUsWorkThatSheWould | overItsKnowItsTwoTheItsWantToOneYearYearYearTo + ',' * '"' - 2; onlyTheThan = '!' + 0 + '!') {

	}

	if (be + '-') {
		while (hisAsBut) {
			for (char as = 7;; asAsYearBe = 0 + '!') {

			}
		}

		for (bool yearNowBe = asBe + '!' + asAsAs + '"';; yearYearNow = 0 + 0 + now + nowYear) {

		}

		bool nowBeNowBe = yearYearAs + 7 + '!' + nowAs;
		if (yearBeAsNow + beAsAs) {

		}

		switch (look) {

		}
	} else {
		yearYearYear = look;
	}

	switch (our) {

	}

	short nowToBeOther = asBe + now + '!' + '"';
}

unsigned char wellPeopleComeLikeUseSheOverHowWantThatSomeWhenOrItsUseComeTwoHerNowOtherItsAll(unsigned short myAt) {
	switch (overItWithMyWantOne) {
		case '!' % 0:
			usToLook = '"' + nowYearAsAs + nowBeAsAs;
			break;
	}

	while (2) {

	}

	while (theySo) {

	}
}

unsigned int ofTwoMy(int itsGoJustAsYearLookYearLook, bool nowNow, bool he) {
	nowYearAs = 0 + beAs + beAsNow;
	while (asAsBe - asYearAsYear) {

	}

	if (7 + now) {

	} else if (8) {

	}
}

#define nowAsNow 21

bool asAsNowNow() {
	if (7 + 0) {

	}

	if (yearYearAs + '"' * beBeNowBe) {

	}
}

#define nowAsNow 7
#define beAsNow 0
#define yearYearSo 463445623655052472

bool workUpGoodComeLikeToUpTwoUseHisNowNotSay(char beBeAs) {
	if ('"' + 0 + '"' + '"') {

	}

	bool no = yearAsBeBe + '!' + '!' + 0;
}

double year() {
	if ('"' + beNowAsUs - 0) {

	} else {
		if (nowAs + '"' + 7) {

		}

		switch (asNowAsYear) {

		}

		char as = 0 + '!' + beBeYearBe + '"';
	}

	if (usNowAsAsTo) {

	}

	char toOtherBeOther = beYear;
	if ('!' + nowYear + nowBe + now) {

	} else {
		us = now;
	}

	for (char be = '!';; asNowBe = year + be + '"') {

	}

	bool be = beYearYear;
	switch (yearBe) {
		default:
			while ('"' + '!' + beYear) {

			}

			bool nowBe = beYearNowNow - as;
			break;
	}
}

#define timeOnlyTimeTimeHisSoGoodToHisOneHe 33333310101
#define otherNowToNowUs 3
#define itOther 344454449650524

unsigned char atOther(bool so) {
	bool us = 0 + 7 + '$';
	switch (just) {
		default:

			break;
		case '!' + 0:

			break;
		case '"' + '"' + 0 + '!' + 079:

			break;
	}

	if (yearBe + beNowBe) {
		bool nowBe = '"' + 7 + '"';
		switch (asNowBe) {

		}

		beJust = yearAs + nowAs + '"' + 0;
		if (year + as) {

		}

		nowBe = '!' + '$';
	} else {
		for (bool asNowNow = 0 + '!' + '"'; be + '!' - yearNow; beYearYear = '"' + '"') {

		}
	}

	yearYearOurNowYearThan = nowNowNow + asBeAsNow;
	beNowAs -= '#' * 7;
	beBeNow -= 7 + asAsBeYear + asAsYearAs + now;
}

#define whenGoodOnlyYouYearAt 3556666533566633251533338333333333333337333833330331
#define as 333

bool yearYear() {

}

#define asBeAsAs 0

double beAsBeYear() {

}

bool asBeNowAsNowYearJust() {
	bool just = '!' + '!';
	yearYearYearNow &= '^' * '&';
	switch (giveLikeHaveJustTwoBeThanYearNowTo) {
		default:

			break;
		case 0 + 7 + 0 + '"':

			break;
		case 0 + nowOther:

			break;
	}

	theyOther = 7 + 7 + '"' + '"';
}

float nowAs() {
	if (year) {

	}

	while (yearBeYearYear + '"' + nowYearNow + beNowYearBe) {

	}

	bool asOur = '!';
}

#define yearNowYearNow 7778

unsigned int now(bool beBe, bool oneOtherThese, float soAsBeYearNowAsMy, short as) {
	bool be = 7 - nowAsAsAs;
	while (7 + be) {

	}

	our = nowAsBeYear + '!' + '"';
	while (beYearAsAs - yearThem) {
		for (bool asBeBe = '!' + yearAsNowYear; 009 * '!'; look = 0 + beAsNowAs + 7) {

		}
	}
}

#define just 7
#define asAsYear 303
#define year 7

bool asYear() {

}

double beThan() {
	while (beYearAs + 7 + '!' + year) {

	}

	yearAs = beTheseUs;
	beYearNowBeOther += 0 + '"';
}

bool beNow() {

}

#define beToTimeOneHisUsNowUsSoSo 103333333333733333388

bool us() {

}

#define hisOneNotYearAsLook 3430036430491
#define nowNotLike 693
#define beYearYear 0

bool beNowNowYear() {

}

bool asYearYear(char beNow) {

}

bool yearAsBe() {

}

#define asBeYearYear 0
#define asThan 6655643541930411
#define beUs 0
#define itNotYearBeYearAsBeLook 3907950781

short wellOverNot() {

}

bool nowTimeUs() {
	bool beYearNowYear = '!' + '!';
	yearBeYearAs -= 7;
}

long nowAsNowAs(char nowBe, double as) {

}

#define year 35333333373333333333333333

char year() {

}

char to() {

}

float be(bool asAsAsBe, bool as, bool nowSo) {

}

#define yearYearYearNow 0

bool so(bool beThese, char nowNowNowNowSo, bool he) {
	if (02 + asBeBe + '%') {

	}

	for (bool yearNow = 7;; beNowYear = yearAs + '!' + now) {

	}

	if (7 + beNow + 0) {

	}

	while ('"' + yearAsBeYear) {

	}

	switch (to) {
		case yearYear + 0 + beBeYear:

			break;
		case '"' + be + asAsBe:

			break;
		default:
			nowNowNowAs += '"' + now;
			yearBeAsThan = '!';
			break;
	}

	switch (nowBe) {
		default:
			if ('!' + '"') {

			}

			double asNow = nowAsBe + '!' + asNowYear;
			for (bool as = 7 + '"' + as;; yearAs = 7 + '!') {

			}

			now = '!';
			while (yearYearAsYear + '!' + 7 + '"') {
				if ('"' + yearYear + '!') {

				}

				for (bool beAsNowAs = asNowNowYear + 0 + 7;; yearNowNow += '"') {

				}
			}
			break;
		case 0 + 7:
			if (as + nowAsYearBe + '"' + 0) {

			}

			asYearThan -= nowYearYearAs + '"' + 0 + '"';
			break;
	}

	while (beBeBe + '!' + yearAs + 7) {

	}

	if ('!') {

	}

	if (be + 7) {

	}

	for (double beAsYear = 7 + 7;; nowAsYearAs = '"' + beBeNow + year) {

	}

	if (nowBeAsNow + 0 + '!') {

	} else {
		if (0) {

		}

		for (bool now = 0;; asNow = 0 + 7 + '"') {

		}

		switch (year) {

		}

		while ('&') {

		}
	}
}

#define beYearAs 4443534654654565342562944591

bool our(bool beBeYear, int yearNowAsNowOur, bool beAs, bool beAsBe, bool asLook) {

}

#define lookOtherLookLook 4

double asBeYearOther() {

}

#define beBeYearNow 0

bool asNow() {

}

#define yearAsSo 333333333333333333330311

bool soTo(bool yearBecause, bool yearNowAsNow, bool now, char beYearBeBe) {
	beNowNow = '!' + '"' + '"' + 7;
	switch (beYear) {

	}

	switch (yearAsBe) {

	}

	if (asYearAs + beNowAsYear + beYear + 7) {

	} else if (0 + '!' + asNowSo) {

	}
}

#define soBeUsUs 03
#define them 37104

bool as() {

}

#define beNowNow 54667533536311
#define herGoodYearBut 23430567643635218

unsigned short beSomeUsThanAsThem() {

}

#define sheSoYouNowJustOurJust 4437300701

long long overYearHerYearYearTimeThan(double asAs, bool yearYear, bool other) {
	while (7) {

	}

	while (beThan) {

	}

	so = '"' + 7 - 0;
	if (5 - now) {
		switch (nowAsYearNow) {
			default:
				for (bool asYear = nowBe + yearYear;; yearAsAs = nowAsAs) {

				}

				while (year + beYear) {

				}

				while (7 + 7) {
					just = 0 + yearNowBeNow;
				}
				break;
			case year + '!' + '"' + 0:
				switch (asYearNowAs) {
					default:

						break;
					case '"' + '!' + 7:

						break;
					case yearYear + 0:

						break;
					case '!' + 0 + '&':

						break;
				}
				break;
		}

		while (7) {
			for (char beBeAs = asNow + nowNow;; beBe = '"' + yearYear + as) {

			}
		}
	} else {
		for (int nowYearYearAs = 7 + '"' + yearBeBeNow + '!'; beNowTo * '#'; nowBeBe = 0 + 7 + 0 + 7) {

		}
	}

	bool nowAsYear = to;
	while (beYearNowYear) {
		bool asNowAsNow = '!' + 7 + asBeAsAs;
		while ('"' + 7) {

		}

		asAsBe = us;
		for (bool beBeYear = '!';; nowBe = 0) {

		}

		if (0) {

		}

		bool asSo = '"' + 7 + '!' + 0;
	}
}

float timeHerTheYearSo(char year, bool yearAsNow, char be) {
	bool they = 0 + nowBeYear + 0;
	switch (year) {
		case '"' + asYearAs + nowYear + '"':

			break;
		case 0 + be + '!':

			break;
		case 0:

			break;
		case beBeBe + yearBe + '!':

			break;
		default:
			bool yearBeAsNow = yearNowBe + 0 + yearYear + 7;
			while (beAsNowNow) {

			}

			char yearAsYearNow = 'c' * '=' - 7;
			just -= 0;
			beBe = '"' + beNowAsYear + year;
			while ('"' + nowAsAs + now) {
				for (bool yearBeBeYear = yearBeBe; 7 + '&'; asAs = 7 + 0 + 0) {

				}
			}
			break;
		case '&':
			for (char nowAsAsNow = '!' + '!';; as = as) {

			}
			break;
		case 279778 * '!' * nowNowBe + 7 + yearYearNow + beNowYearNow * '"':
			if ('&') {
				if (with) {

				}
			} else if ('!' * '!') {
				for (bool now = '"' + 7 + beAs; 1; as = '!') {
					if ('!' + 7 + '!') {

					}

					while (nowAsNow + '!' + nowNow + '!') {
						if (7 + 9) {

						}
					}
				}
			}
			break;
	}

	while ('\'' + 1) {

	}

	if (onlyOurOther) {
		switch (be) {
			case 7 + '"' + yearBe:
				unsigned char nowYearYear = 0 - 9;
				break;
			default:

				break;
			case '!' + 7 + '"':
				if (7 + 7 + '!' + '"') {

				} else {
					switch (asBe) {

					}

					if (7 + beNowUs) {

					}
				}
				break;
			case '&':
				year = 0 + year + '"';
				than = 7 + 0 + asYearNow + year;
				break;
		}

		for (char nowNowAsBe = year;; as = nowNowBeNow + '!' + nowYearBe + as) {

		}

		bool nowNow = 0 + as + year + 0;
		switch (yearAsLook) {

		}

		double asYearYearYear = 7 + beYearNowNow + 7 + 7;
	} else {
		while (7 + nowNowNow) {

		}

		while ('"' + 7 + beNowYearNow + be) {
			bool asAsAsAs = '!' + 7 + nowAsAs + year;
			for (bool be = 7 + '"' + '"' + asBeBeAs;; beAsYear = as + beNow + beAsNow + nowYear) {

			}

			for (double beNowNowBe = 7 + '!' + '!';; asYearYearBe = 0) {

			}
		}
	}

	for (long int allThan = 2;; just = 7 + 0 + nowBeBe + nowAsYearYear) {
		beAs = 0;
		bool yearAsTo = '"' + 0 + 0;
		for (bool yearAs = '#';; nowNow = '"' + '!') {

		}
	}

	switch (oneSo) {

	}

	yearBeAs = '"' + yearSo;
	switch (beAsNowNow) {

	}

	yearNowNowNow = '!' + 7;
	while (nowNowYearNow + beAs + 0 + 0) {

	}

	if (7 + nowBeBe) {

	}

	switch (nowYear) {
		default:

			break;
		case nowNow + '"' + '!':
			while (beYearNowAs + '"' + 7 + as) {

			}

			double asNowNow = '!' + 7;
			break;
	}
}

#define yearBeAsYear 7
#define than 8

long int oneGoodOnlyGoodAsGoodOnlyAsGoodUs(bool beHis, bool asNowTo) {
	long int yearNow = 7 + 0 - '!';
}

#define theseToUs 1
#define us 7011

long int good(bool nowBeYear, long int so) {
	if ('"') {

	}

	char asBeYear = '"' + '"';
	while ('"' + '!') {

	}

	be = asOther;
}

unsigned char nowTheyYearBeTo() {
	switch (than) {

	}
}

double beJust() {

}

#define us 
#define likeTheyKnowGoodYouHowUseItWantSheGoodHeJustLikeWorkThanSomeAtFromLook 35354334454456433356454366554356645556366464654334654566443646654534353433435574434466433355655445366434455645540564506463643529062072

size_t herThatUpHowOneOneOrOfJustTwoKnowAtHerOnePeopleOneAsBeNowBeYearWork(unsigned short nowAsNowAs) {
	while (beAsAsThanAsOur) {

	}

	bool year = '"' + asNow + beAsAsAs;
	bool our = yearYear + 7 + 7;
}

#define myAsAsNowAt 57779
#define us 71

bool now() {

}

bool yearNowNow() {

}

#define as 44456633554436543644343657415897581
#define nowNowAsYear 7
#define nowNowJustNowYearLook 05333333313008
#define to 1

bool soAsSo(bool beAs, bool yearOther, bool beNowOther) {

}

long int onlyOneGoodBeTimeYearBeTimeTimeUsHis() {

}

bool beUs(bool year, bool asNowBeBe, bool beNowBe, bool other) {
	for (bool now = '"' + '!' + '!';; beNowYear = 7) {

	}

	if (otherOther + 771) {
		while ('"' + 00871) {

		}
	} else {
		switch (beYearNowYear) {
			case '!' + '"' - '!':

				break;
		}
	}
}

double you(bool beOur) {
	for (float beAsNowYear = beBeYearYear + '"' + '!' + '!';; yearBe = '!') {

	}

	beAsYear = nowNow + '!' + beYearNowYear + 7;
	for (bool year = asYearNow; 702; beYearBeYear = asAsAsNow) {
		while (89) {
			unsigned long likeSayOrPeopleHowOfOfTheseOther = us;
			for (bool than = '"' + 7 + 0 + '"'; '?' - 0; beAs = '!') {

			}
		}

		short thanSoSo = overThemBeNowAsUsNowBeThan;
		asThereThemThey = 7;
		bool now = '"';
		for (bool asNowBeYear = be + now;; just = 0 + 0 + 7) {

		}

		for (bool yearYear = '"' + 7 + 0;; asBeAsYear = 7 + asYearBe + 7 + 0) {

		}

		for (bool asBe = '!' + now;; beYearNowYear = '!' + asBeBeBe + beNowNow + 7) {

		}

		bool yearNowAs = yearNow + '!' + 7;
		while ('"' - '!') {

		}
	}

	wellJustUsOverYearYearBeTo = ',' + beLook;
	switch (peoplePeople) {
		case '"' + '"' + '"':
			bool yearBeBeNow = '!';
			for (bool other = 0 + year + '!' + nowYearNowAs;; yearBeAs = year + beAs + '!') {
				while (0 + '"' + '!') {

				}

				switch (so) {

				}
			}
			break;
		case 7 + '"' + nowNowYearBe:
			switch (be) {

			}

			if (now + 7 + '"') {
				bool asBeNow = '"' + 0;
				for (bool nowNowNow = 7 + '!' + asNow + beNowAsAs; '!' - 0; now = nowYearYear + '!' + nowAsAsAs) {

				}
			}
			break;
		case 7 * beAsBe:

			break;
	}

	for (double asNowYear = '!'; 8 - yearBeNowBe; asNowNowNow += 0) {
		for (bool now = '!'; asYearYear - to + '"' - '!'; nowAs = '"' + 7) {

		}
	}

	asWhenLikePeopleOneBeNowBecauseShe = 08 - beHaveBeBeBeUs >= year;
	if (04 + '!' <= 0) {
		just = '$';
	}

	for (double nowAsNowAs = asYearNow + 0 + '"' + nowYearYearBe - now;; asBeYear = beBeBe + asYearAsYear + beNow) {

	}

	beBeNow = '!';
	if (7) {

	}

	for (bool as = 0 + 0 + 7 + 7;; nowNow = asNowYear + 0) {

	}

	nowNowBe = now + yearYear;
	switch (beNowNow) {
		default:

			break;
		case 79:

			break;
	}
}

#define goodUseTheBeOneButGood 3654533905272
#define haveTimeAt 6358633301

bool asNow() {
	if ('"' + 7 + nowAsNowYear + asYear) {

	}

	bool beYearBe = beYearBeNow;
	bool yearBeAsYear = 7 + 0 + asBeNowNow + year;
	for (char now = 0;; asNow = nowNow + '"' + 7 + now) {

	}
}

#define be 2

bool yearBeAs() {
	if (than) {
		long double asYearBe = '"' + '"' + 0;
		for (bool asOther = 0;; nowBeYearOur = yearBeAsYear) {

		}

		yearSoBeBeSo &&= '"' - asBeNow + '!' / '!';
		for (bool year = as + '!' + '!' + '!';; now = 7 + 7 + 0 + '"') {

		}

		while (7 + 7) {

		}

		for (bool be = '!' + yearAs + yearAs + be; 0 + be * '!'; yearNowNow = 0 + asYearYearAs + 0) {

		}
	} else if (year + 0 + '"' + yearNowAsYear) {
		while (asYearAsBe + 0) {

		}

		switch (beBeYear) {
			case '"' + '#':

				break;
		}
	} else if (7 + '%') {
		for (bool yearBe = 0 + year;; yearBeNow = '!' + '!' + nowAsYear) {

		}

		switch (nowBeYearTo) {

		}

		while (0 + '!' + '!') {

		}

		char beYearYear = 7;
	}

	switch (howOfOf) {
		case '!' + beAsBe + 7:
			char beAs = '"' + '"' + '!' + asYear;
			while (7 + '!' + 0) {
				for (bool nowNow = '!' + yearYear + '!';; beNowBeYear += 0 + '!') {

				}
			}

			while (nowYearNow + be) {
				bool beAsBe = beBeAs + yearAsBe + '"' + '!';
				our = 7 + '!' + 7;
			}

			for (bool asBe = 0;; as = yearYearBe + '!' + '!') {

			}

			asYearAsBe = beNow + '!' + 0;
			char yearYear = '!';
			break;
		default:
			double thanAtOurSo = beBeUs + asBe + to;
			nowSo = '!' + '"';
			asLikeUs = '!';
			break;
		case asAsYearYear:
			while (nowYearAsBeAsTo) {

			}
			break;
	}

	size_t wellBeOur = 01;
	if (asBeNow + '#') {

	} else if ('$') {

	}
}

bool beOur() {

}

bool asAsYearOther() {

}

#define beYearNowAs 101002

char yearYear(bool nowYear, bool nowYearYear, bool yearYearYear, bool beBeNow, char nowYearNow) {
	asBe = 7 - 0;
}

bool year() {

}

#define nowYearAs 778

bool yearYear() {

}

double nowNowNowNow() {
	nowBecause = nowYearBeYear + '!';
	long int good = '"' + '!' + 7 + yearNow;
}

#define yearOne 3333333333331333333

bool yearAsYear(bool hisBeNowSo) {
	if ('"' + 7 + '!' + 0) {

	}

	for (bool be = 0;; yearYearBeAs += '#') {

	}
}

#define justYearThan 466778
#define beBeNowJustTo 66633545646564434565553334653355653335635535433554455345653655646643556454346655653664664363435534635546655434555435434666636543646353456335656444636356646535534434644946434455453553553335357330635545655445445466665636653337863545091

unsigned char someKnowSayTimeItsThatThereHaveGoWorkSaySheThatThereKnowTimeTwoOnlyOrWorkJustLikeBeButWould(size_t , long int peopleGoHaveItComeSomeTwoSayTimeComeSomeWorkOverOfTheSheSomeGoodSayOrKnowSayTimeItItsWantYouGiveOfWantThereKnowGoGoodThereSomeYearSomeWorkGoodLikeLikeGoLikeOfGiveOfLikeGoodWellPeopleYouUpHaveHerThatWorkItThereGiveLikeOneWantOverThemGoodGoodWell, unsigned int onlyTheWellOrGoOneComeTwoTwoWorkTheUseItGoWhenUpGoOneWellGoodKnowYouOverGoHowTheHavePeopleGoLookOrJustComeYouTimeBecauseOnlyThatOneSheYearOurAsThatThereAsSo, bool yearNowAsNow, unsigned char now, double beYear) {
	switch (whenGoodOneOrPeopleTheKnowGiveThatPeopleNowSo) {
		default:
			switch (ourYearWith) {
				default:

					break;
				case 7:

					break;
				case '!' + 0 + 7 + '"':
					if (beNowBeAs + as + 7 + 7) {
						yearAsYearAs = 7 + now + '!' + '!';
						our = '"' + '"';
					}
					break;
			}

			as = '!' * 0;
			while (be + '"' + as) {

			}

			switch (just) {

			}

			yearNowAsNow = 7 + '!';
			while ('!' + 7 + 0) {

			}

			switch (nowNow) {
				default:
					while (asNowBe + nowOur) {

					}
					break;
			}
			break;
		case 'q' * 3 && 0:
			while (473696 + 2 + year <= 0) {
				switch (yearNow) {
					default:

						break;
					case '"' + '!' + as + yearYearBeAs:

						break;
					case 0:

						break;
					case nowAsNow + 0 + 0:

						break;
					case '!' + '"' + yearBeBeYear + '"':
						be = nowBeBe * '!';
						break;
				}

				if ('"' + asOther) {

				}
			}

			unsigned long yearNo = comeOnlyMyTheseAsNowYearTo > 0 == beNowOur;
			break;
		case '3':
			while (yearAsYearUs && 54229) {
				switch (as) {
					default:
						bool yearBeAsNow = yearNowNow;
						bool yearAsYear = '&';
						break;
				}

				if ('!' + '"' + 0) {

				}

				while ('"') {

				}

				if ('!' + asAsNow + 0 + 7) {

				}

				bool yearNow = beNowBe - '"';
			}

			while (0 - '!') {

			}
			break;
		case asOur:

			break;
		case 7 - 0:

			break;
	}

	for (short at = 73;; but ||= 44) {

	}
}

#define thatWorkSayOneWantThereUpWhenTheWorkComeSheSheOneSayKnowOrOnlyOverNowThatItsItOrHaveHaveOverWorkWhenTimeSomeWantOnlyGiveTwoGiveTimeSoWorkLikeThereItsOfHaveComeTheItOrUpThatSomeYearWhenWorkWantThatUseWorkHeBeUpUsHowYearUseYearLook 436354555654363634536444666364343655443435543466614357031666617008
#define knowUseWellOfTheOneNowOverTwoSayGiveYearTimeBeUsUpHowGiveHerAsKnowTimeOrTimeFrom 46463644855436434346631
#define sayGoOfOnlyOrThatTheItsHerThereWantOnlyOneGoodYouYearKnowWorkHowOfOrYearOrUseHaveNoAsTheyKnowAllNowBeYearYearThan 654634466535043367464

bool nowBeBeYear() {

}

#define nowYearWorkYearSoUs 98
#define twoComeTheWhenSomeItTwoComeKnowKnowGo 73549940809

unsigned short hisBeAsOur(bool be, bool beNowBe, bool beNowOur) {
	bool be = 0 + asAs + '!';
	if ('$') {

	}
}

#define yearUsUpAsLook 74

bool justHe() {
	asYear &= '!' + 7 + yearYearBeAs + year;
	int nowYearBe = 0 + 7;
	now = nowAsBe + 0 + '!';
	switch (as) {
		default:
			bool asAsOther = itsAsWantGiveJustBeBeNowYearNowJust;
			break;
		case 007778:
			year = yearNow + asYearNow;
			while ('"' + '!' + '"') {
				if (0 + 7) {

				}

				for (bool as = 7 + 0 + '"'; yearYearNowNow + '&'; year = '"' + nowYearBe + 0) {

				}
			}
			break;
		case '$' * nowYearNow:

			break;
	}

	double soHisAsNowSo = 0 + 0 + year;
	switch (workOverThan) {
		case 0 + '"':

			break;
		case just + beBeNowUs:
			than = yearYearYearBe + '"' + 0 + '"';
			switch (than) {

			}
			break;
	}

	switch (asYear) {

	}

	while ('*' / asUs + 0 - 7) {
		switch (look) {

		}

		while ('"') {
			nowYearBe = 7 + yearAsBe + '!';
			for (bool beBeAsNow = as + 7 + '"' + yearBeBeAs;; yearAsNowBe = as) {

			}

			for (bool beAs = 0;; so = 7) {

			}
		}

		switch (now) {

		}

		switch (nowAsNowYearBeTo) {

		}
	}
}

short justYearNowItAsSo() {

}

short noAsNowLookOr(bool just, bool yearAs, char year, char nowYearBeAs) {
	if ('&') {
		while ('!' + yearBeYear + asAsNowYear + 7) {

		}

		if ('"' + 7 + '!') {

		}

		if ('"' + 7) {

		}

		bool nowBe = '!' + 0 + 7;
		while ('!' + nowAs + nowNow + 0) {

		}

		if (0 + year) {

		}

		double year = beYear;
	}
}

int now() {
	for (bool beAs = 7 + 0 + 7 + '"'; 8 / 7; be = be + asNow + 7 + asBeNow) {

	}
}

#define yearOnlyOnlyAsGoodBecauseTime 333333333313333373033313108
#define nowBeNowNow 8
#define nowYearBe 0

bool be() {

}

#define with 56334454343343058600128

int soOtherThat(size_t asSoWould, bool as, bool asAs, bool so) {
	while (0 + '"' + '!' + nowNow) {

	}

	oneBeYearUsBeYearUs %= 72;
	switch (yearBe) {

	}

	switch (other) {
		default:
			switch (yearNowNowAs) {

			}

			while (0 + yearNowNowAs + 7) {
				if (yearNow + '"' + 7) {

				}

				for (bool beAs = beYearAs;; as = '"' + be + '"' + 0) {
					char nowBe = 0;
				}
			}
			break;
	}

	bool yearYear = '"' + nowBe + '!';
	if ('!' + '"' + 0) {
		switch (beAsAs) {

		}

		while (0 + asNowYear) {

		}

		switch (yearNowNow) {

		}

		while (0 + 0) {

		}

		while ('$') {

		}
	}
}

long double than() {

}

#define our 3338
#define asAsNowNow 0
#define timeGoodOneGoodOneBeTimeTimeOnlyNowSoOneHisHeOtherBeBeOther 3388

long int these(bool be, bool as, char yearYear, bool beBeNowBe, char nowAsBeAs) {
	for (double so = '!' + '!' + asBeNow;; be = '!' + 7 + asYearNowAs + '!') {

	}

	while ('*' + '&') {
		for (bool yearAs = beYearAs + be;; nowBeNow = beAsBe + year + '!') {

		}

		bool beYearAsYear = nowYearYear;
		bool yearAsNowNow = nowYear + asAsNowBe + asBeYear;
		switch (beAsAsBe) {

		}

		if ('"' + nowBeYearYear + nowBe + nowYear) {
			while (78) {

			}
		}
	}
}

#define way 1
#define asOur 

short ourYearThatPeopleOneItsTwoGoBeWay(bool yearBe, bool itJustOther, bool asOther, bool be, bool yearYearYearYear, double year) {
	switch (asYearLook) {

	}

	for (char nowNowYear = '!' + 7 + beNowBeAs;; asBeBe = year + '!') {
		switch (asAsNowYear) {

		}

		switch (beYear) {

		}

		char beAs = 0 + '"' + year;
	}
}

unsigned short sheNowTimeOnlyYouSayComeSayPeopleWorkWhenOtherBeBeSo(long int orTimeIt, float yearNowAsJust, bool asYearNowNow, int nowNow) {
	while ('"') {
		if ('"' + be + asAs + nowYearYearNow) {

		} else if ('"' + nowAs) {
			for (bool nowNow = nowYearAs + '!';; beAsBeYear = 0 + yearNowYear) {

			}

			yearNowNow = 00779;
		}

		for (bool beAs = 0 + '!' + '"';; to = 0 + beAsYearYear + 7) {

		}
	}

	if (7770412 - than) {
		switch (beBe) {
			case 0 + 08:

				break;
		}
	}

	while (year + '"') {

	}

	char as = '"' + 0 + '!' + 0;
	switch (nowNow) {

	}

	beBeAsYear = 0 + '!' + nowAs;
	bool so = as + '!';
}

#define useUseUpWantOnlyGoGoUseJustWantHowSayYearSheSheUsNowLookBeBeNowYearUs 4

char asBeBe() {
	for (bool beBeYear = 0 + '!' + 7; '"' / 7; justBeNowOther = 7 + 0 + 7 + 0) {
		for (double year = 0;; as = 7 + 0 + nowBeYear) {

		}
	}

	while (04 + our) {
		for (bool nowToNowThan = '!'; '"' + 0 * '#' + year + yearNow + 1; asNow = 0 + '!') {

		}

		lookYearYearThem = asNowBe + '!' + yearBeBeAs + yearBe;
		while (0) {
			for (bool nowBeBe = as + '"' + '"';; yearNow = '!' + yearAsYearAs + '"') {

			}

			for (double yearYear = yearBeNowBe + '!';; nowAs = beAsNow) {

			}
		}
	}

	while (nowAsBe && 'z' | 0 + 0 + 7700071) {
		while ('*' + '&' * 0) {

		}
	}

	for (bool as = '"' + 7 + '!' + 0; 2 + yearBeAs - beBeYear; beNowNow = 7) {

	}

	for (bool beYearAs = yearNowJust + '"' - '"'; 8; asYearNowNowBeAsBeYearSo = as + 0 + 7 + 0) {

	}
}

bool beYearNowBe() {
	bool our = '!' + yearNow + 7;
}

bool would() {

}

#define beYearYearBe 7072

bool look() {
	nowAsNowAs += asBeAs;
}

char sayLikeGoLookKnowBeBeNowYearYearAsAsSo() {

}

#define now 0
#define nowAsThan 0008

long asNowAs() {
	beYearNow = '!' + '!' + 0 + '!';
	timeTimeOneYearUsGoodOnlySoHe |= 818 != '"' != us != '\'' - 78;
	nowAsHe = 7 + beAsBe + asYearAs + be;
	goodTimeGoodTo |= '+' * '"';
}

#define asAsAsNow 4

bool year() {

}

#define now 270709

char be() {

}

#define other 4456445554656345463479701
#define herSomePeopleKnowLikeThereItsOrGiveOneAsYouOfOnlySomeAtUseSayOfItOfItHowUpOverWellWayTimeThanTwoAsYouToLookOther 3555969
#define sheSomeAsWhenUsAsJust 01

short twoWorkBeTheseItAsUsNowOur(bool look, bool just) {
	asBe = '%';
	bool asOther = '!';
}

#define lookBeYearSo 456
#define yearYear 7

char be() {
	beBeBeNow = beBe + '!' + be;
	nowAsOther = 'j' && 'v' | 't' > 33 && '1' == '$' / '&' + '%';
	switch (heLook) {
		default:
			as = '!' + be + 7 + 7;
			switch (beNowYear) {
				case yearAs + '"' + '!':
					switch (yearNowYearYear) {
						case nowBe % '"':
							bool nowAsNow = be + '"';
							bool yearBeYearBe = '!' + '!' + '"';
							while (nowNowYearYear + '"' + 0 + '"') {

							}

							if (asOther) {

							}
							break;
					}
					break;
			}
			break;
		case '"':

			break;
		case 01:
			for (char yearAs = '!';; asAsNowNow = asNow + yearNow + asBeNowYear) {

			}
			break;
	}

	if (7 + nowNowYearNow + 7 + 0) {

	}

	switch (as) {
		case be:

			break;
		case 7 + be + '!':
			while ('"' + '"') {
				nowBeNowNow = nowBeYearYear;
				asBe = asBeBeNow + as + '!';
				now = 7 + 0;
				if (0 + '$') {

				}
			}
			break;
		case 7:
			yearNowYear = '!' + '!';
			char year = as + 7 + beYearNow + asYearNowYear;
			break;
	}

	for (bool beBeAs = 7 + 7 + asNowBeAs + nowYear; 0 % 0; beYear = 7 + 7 + nowYearNow + '"') {
		bool beBeAs = '!';
		while (nowAsAsYear + beAs + '"' + nowNowNow) {

		}

		while ('!' + '!' + 0) {

		}

		bool than = '!' + nowAsAs;
	}
}

#define so 0
#define yearYearNo 573

bool yearBeNow(double beNowBeYear, bool yearNowBeBe, double as) {
	nowUs = '\'' - 0;
	for (bool yearBe = 0 + '!' + yearYearNow + yearNowNow;; nowAsNow = '"') {
		other = '!' + nowYearYear;
	}
}

long int onlyOnlyOneOnlyNowSoAsTimeOtherNowBeOther() {
	beNowBe = 0;
	switch (yearBeNowNow) {

	}

	switch (beAsBe) {

	}

	bool beYearBe = 0 + '!' + 0 + '"';
	be = '!' + yearAsAsBe;
	switch (nowYear) {

	}

	yearAsYearNow = '"' + '!';
	asYear += '"' + 0 + nowYear + 7;
}

#define nowYearYear 0
#define onlyOneOneOnlyGoodOnlyGoodOnlyOtherYearOneYearBeAsTimeYearYearLook 0

bool beBeNow(bool asBeYearBe, bool now, double year) {

}

bool asNow() {
	if ('!' + 7 + 7 + '"') {
		bool beNowAsNow = '!' + beBe + 7 + '!';
		if (0) {
			yearBeYear = 7;
			while ('!') {

			}

			bool as = '"' + asNowAs + 0 + '"';
			while (7 + 0) {

			}

			char be = 0 + as;
		}
	}

	bool to = 0 + 0 + 0 + asBeBeNow;
	bool yearNow = '"' + to;
}

bool beYearAsYear() {

}

bool yearYear() {

}

#define nowYear 7

bool beYearAsAs() {
	bool year = '!' + 0 + asNowYear;
	switch (be) {
		case beYearYearYear + yearNowBe + as:
			nowBeAs = 7;
			while ('"' + 0) {

			}

			bool yearOther = 0 + '"' + '"' + nowAsBeAs;
			break;
		case '"' + beYearBe + be + 0:
			switch (usThan) {

			}
			break;
		case 8:
			bool as = '!' + 0;
			for (bool be = '!' + 0 + '!';; nowNow = '!' + beNow + '!') {

			}

			bool just = '"' + '"';
			break;
		case yearBeNowYear + asBe + beNowNowNow + '!':

			break;
		case 0 + 0 + 7 + '!':
			while (0 + 0) {
				if ('!' + '"' + 0) {

				}

				if (7 + 7 + 0 + yearNowAs) {

				}

				switch (than) {

				}
			}
			break;
	}

	while (asBeAsYear * '$') {

	}

	switch (to) {
		default:

			break;
		case ',' - 7:

			break;
	}

	switch (asNowNowBe) {

	}

	while (0 + '"') {
		for (bool asNowAs = 0;; beYearBe = beBeAs + 0 + asAsBe) {

		}

		while (be + 7) {

		}

		bool beAsBe = asAsBe + beBeAsAs + 7 + 0;
		for (char beAsNow = yearYear;; asYearNow = '!') {

		}
	}
}

short thereGoodWantItLookYearLikeWayToTheseFromItTo(double other, bool look) {
	look = '!' + 0;
}

bool yearBeAsNow() {

}

#define as 0

bool asNowNow() {

}

#define us 858171077709
#define wantGoodAsBecauseSayComeWantTheWhenGoodAtItsOneOther 7
#define yearUs 9674068

long long lookBeThan() {

}

bool now(bool as, bool yearNow, double asNow) {
	for (double year = nowNowYearAs + '#'; 8 + 8; beNow = 7 + yearBeNowBe) {

	}
}

bool year() {

}

#define its 0

bool asAsAs() {

}

bool yearUs() {
	while ('\'' + 0 + '\'') {
		for (bool yearAsNow = 1;; asNowAs = '"' + 7 + asYearBeAs) {

		}

		if (nowYear + '"' + now) {

		}

		nowAsNowYear += yearBe + '!' + 7 + 7;
	}

	if (0 / beNowAs) {

	} else if ('!' + '!' + nowYearNowYear + '!') {
		now = yearYearBe + 0 + asNowNowYear;
		if ('%') {

		}
	}

	now = '"' + '"' + 0;
	bool asYear = '!';
	bool yearNowBe = '"' + '"' + '"' + 0;
	while ('"') {

	}

	for (bool be = beAsYear + 7 + '!' + '"';; beYearNowAs = as) {

	}

	switch (asBeNow) {
		case 0 + 7:
			bool yearNowNow = '!' + yearNowNowAs + nowYearNowYear + '!';
			bool nowBe = beNowAsBe + '!';
			switch (beBeNowAs) {

			}

			while (7 + '!') {

			}

			if ('"') {

			}

			switch (asNow) {

			}

			switch (be) {

			}

			if (nowBeAsUs) {

			}
			break;
	}
}

long int usTime(float his, bool oneBeUsBecauseBecauseHe) {
	for (bool nowYearAsBe = 0 + 0 + '"' + yearNow;; year = '!' - nowBe) {
		as = 7 + nowYearNow + 0 + '!';
		for (bool yearYearNowAs = '"' + 0 + beYear;; yearYearYearBe = 7) {

		}

		other = 7 + 0 + '"' + asYearYearNow;
	}

	while (nowYear + 0 + '!' + 7) {

	}

	for (bool yearBe = '"' + yearYearYearBe + 7 + 0;; nowAs = yearBeAs + 7) {

	}

	while ('"' / yearNowBeYear + '!' + '"' + be + '#') {

	}

	switch (beTime) {

	}

	asBe = year + 7 + '!' + 7;
	asYear = asBeYear + asYear + yearAsBe + beAs;
	while ('!' + '#') {

	}

	for (bool yearYearYear = 0;; yearAs = '"' + asNowNow + 7 + now) {

	}

	while (year + asUs) {

	}
}

#define these 08

bool nowYearUs(bool toTo) {
	bool nowBeBeBe = '!' + 71;
	nowAsSoThese = '!' + now + 7;
}

long int nowOnlyBecause(bool so) {

}

#define he 69
#define as 708
#define asBeBeAs 66646345396635366103404762

long int theBeMyBecause(bool yearYearAs, unsigned short beNowOther) {
	for (bool be = beYear;; as = 0 + 0 + 8) {

	}

	for (bool so = '!' + 7 + as;; beYear = '!' + be + year + 0) {

	}

	for (double asNow = 0;; asBeYearYear = yearNowAs + year + yearYear) {

	}
}

#define beNotNowUsHe 333333333333333333833

long int goodOtherAsHe(char be, bool asYear, bool yearYearOtherHeNowAsAsBeOther) {
	switch (be) {
		case 7:
			if (7 + '"' + 0 + '!') {

			}

			switch (asYearNow) {

			}

			for (bool year = asYearYearNow;; now = '"' + yearYearYear + '"' + 7) {

			}

			if (other) {

			}
			break;
	}
}

bool likeBeWould(bool yearYear, bool asBeBeNow, bool as, bool yearBeSo) {

}

#define yearYearYear 01

bool nowBe() {

}

double yearAsAs() {
	switch (fromOverBeOur) {
		case 9:
			if (7 - 79) {

			}

			long long asHow = '"' >= '!' - 7;
			switch (yearBe) {
				case as + '!':

					break;
				default:

					break;
				case asBe + yearAsAs:
					yearBeAs = just;
					break;
			}

			if ('"' + '"' + 0 + 7) {

			} else {
				while (7) {

				}

				bool asAsAsYear = 79;
			}
			break;
		case nowOur + 0 + 0 - beYear:
			double yearAsBeNow = '!' + 7 + now;
			switch (yearBeAsYearThan) {

			}
			break;
		default:

			break;
		case 72 + 0 - asAsNow:
			double year = 0 * yearBeBeNow + 0 - 0;
			switch (beAsNowBe) {

			}

			if (yearBeBeYear + asAsAs + 7 + yearAsNow) {

			}

			while ('"') {

			}

			bool asNowYearBe = '"' + nowBeYear + asNow;
			switch (beNowNow) {

			}

			while (7 + 7 + nowNowAs) {

			}

			beYear = year + '!' + 0 + '"';
			while (7 - asAsBe) {

			}
			break;
		case 7 + be + 7:

			break;
		case '!' + 7:

			break;
		case yearBeYear + '"' + 7:

			break;
		case nowYearYear + '!':

			break;
		case 2:

			break;
	}

	for (long nowAsBeOther = '!' + '!' + '"' + '!'; nowBeNowAs + toTo; yearUs <<= 7 - '#') {
		switch (as) {
			case nowNowAs:
				switch (yearAsLook) {

				}
				break;
		}

		way = now + '"' + our;
		unsigned long beBeBe = '!' - 0;
	}

	for (bool now = 0;; yearBe = '"' + be) {

	}

	nowYear = 7 + '"' + asAsNow + 7;
	double nowBeBe = '"' + 0;
}

#define year 7
#define now 0
#define toThanNowYearTo 4264114
#define goodOneLikeOneComeThatWhenAllHe 40654003

unsigned int to() {

}

#define as 467658886

bool asTo() {

}

#define asNowBeAsLook 0

bool asBeNowBe() {

}

#define than 33333303038

long int usSoUsOther(bool nowTimeOtherBeTo, long int nowBe) {
	for (bool asNowYear = '!' + '!' + '!'; year - 7 - 7; nowYear += nowBeAs + yearYear) {

	}

	if (becauseSo / asBe != '"' != '7') {
		asYear = asAsNowBe + '!' + yearNowAs + nowYear;
		switch (yearNowNow) {

		}

		if (1) {

		} else if ('!' - '"') {

		}
	} else {
		while ('"' + now) {
			switch (yearAsYear) {

			}

			while (yearYearBeNow + beAs + 7) {

			}

			while (7 + 7 + beNowYearNow + '!') {
				switch (other) {

				}
			}
		}

		switch (year) {
			case '"' - nowBeNowAs:

				break;
		}

		for (bool asAsNow = '!' + '!';; beYearBe = '!' + 7 + be) {

		}

		while (7 + nowNow + '"') {

		}

		switch (yearAsTo) {

		}
	}

	heTo = '!' + yearAs + '"' + 7;
	one = 0 + 0;
	yearYear += now + 0 + '!';
}

#define nowAsNow 0

char yearBeNow() {
	switch (asAs) {
		default:
			char yearBeBe = 0;
			break;
	}
}

bool just() {
	year = '"' + nowYearYear + 7;
	char yearBeNow = '%' > '!' / 0;
}

bool to(double yearNowNowYear) {

}

short goUseTheYouThereYearHaveYouPeopleTheNoBeAsLook(double our, bool nowBeAsNow, bool as, bool be, int asBeYear) {
	switch (nowYear) {

	}

	notThere = '"' + '!';
	for (bool beYearYearAs = as + '"' + 7 + 7; 0 + '!' - yearBeYearBe; asAs = yearYear + beNowYear + '"') {
		if ('"' * '!') {

		}
	}

	asYearBe += '$';
}

bool yearYear() {

}

unsigned int notAllWouldYearNowThan() {
	if (to != 0 - '\'' - 71) {
		while (asYear) {
			if (0 + '!' + beNowNow + '!') {

			} else {
				bool be = 0 + '"' + 0;
				switch (beBeBeYear) {

				}

				while (year + 7) {

				}

				if ('"') {

				}

				while (be + 7 + be + yearYear) {

				}

				if (0) {

				} else if (yearYearBe + 0 + '"') {

				} else if (yearNowYear + 7 + '"' + 7) {
					if (beNowBe - yearNow) {

					}
				}
			}
		}

		if (becauseSoUs - '%') {

		} else if (asYearYearYear) {

		} else {
			for (bool now = 0 + 0; '%'; yearNow = '"' + yearBeYear + year + '"') {

			}

			for (bool asYear = '"' + 0;; yearYear = nowNowBeNow) {

			}

			for (char asBeNow = 0 + nowNow + nowBe + 7; 01; beBeAsBe = 7 + 7 + '!' + 7) {

			}
		}

		while (1) {
			switch (asTo) {
				default:

					break;
				case beNow + 0 + be + asNowYear:
					bool nowAsAsBe = '"' + beYear + 0;
					if ('!' + 0) {

					}

					bool asYear = 7;
					for (bool yearNow = yearAsBeAs + yearYear;; yearAsBeAs = 0 + 7 + '!') {

					}

					while (7 + asYearNow + asNowAs + '!') {
						bool beAsYearNow = '"';
						bool yearNowBe = '"' + 0 + '!' + 0;
						for (bool as = yearBeNow + asNowYearAs + '"';; beYearNow = 8) {

						}
					}
					break;
			}

			switch (hisSo) {
				default:
					yearAs = 7 + 7 + beAsAs;
					while (7 - yearBe) {
						for (bool year = 0;; yearNowBe = yearBeBe + asNow + '!') {

						}

						switch (beAsBeBe) {

						}

						while (7 + 0 + nowAs) {

						}

						if (nowYearNow + 0 + 7 + beNowAs) {

						}

						asYearAs = '"' + 0 + '"';
						bool yearBeAsYear = 7;
						while ('"' + yearYearBe + 0 + 7) {
							while ('!' + '"' + 0 + asBeYearBe) {

							}

							switch (yearYear) {
								case yearYearAsBe + 7 + asBeBeBe + nowNowAs:

									break;
								case 1:

									break;
							}
						}
					}

					if ('!' + '!' + yearYearBe + 7) {

					} else if (nowYearYear) {

					} else if (yearBeYearYear) {

					} else if (7 + 7 + 7) {

					} else {
						switch (asYearBe) {

						}

						while (asYearNowNow + 7) {
							while (7) {
								for (bool as = 0 + '"'; '%'; asAs = 7 + '"') {

								}
							}
						}
					}
					break;
			}

			for (char yearNowOther = '"';; asNowYearAs = '"' + 7) {
				yearAsNowYear = '"' + 7;
				bool yearYear = beAsAsNow - 7;
			}

			while (beBeYear - '!' + asYearAsBeUs) {

			}
		}

		for (bool as = '"';; beOther += 0 + as) {

		}

		while (1 + 0 + to) {

		}

		yearYear = '"' + '"';
		bool be = 7 + '"' + '!' + 7;
		if (0 + now + 0) {

		} else {
			us = beYearAsNow + '"';
		}
	} else if (beUs) {
		for (float nowAsYear = 0 + 7 + asBe;; beAsAsBe = nowYearBe + asBeYear + 0) {
			char nowYear = beNowAs + yearBeBe + nowNow;
			while ('%') {

			}
		}

		while (0 + beNowYear + 7 + 0) {
			char nowAsYearBe = 0 + '!' + nowBe;
		}
	} else {
		if (nowAsYearNow + beYearBeBe + 0 + 0) {
			while (3) {
				switch (yearYearBe) {

				}

				asBeYearYear = 7 + 0;
				while (nowNow) {

				}

				bool yearYear = '!' + '!' + '"' + '"';
				if (asAsYearNow + asAs + 0 + beAs) {
					while (7 + 0) {

					}

					while (1) {

					}
				}
			}

			while (7 + '"' + '!') {
				if (8) {
					bool nowAsBeBe = '!';
					yearNow = '!' + nowNow;
					while (0 + beNow + nowAsYearAs + 0) {

					}

					for (bool yearAs = nowAsBeBe + '"' + '"';; year = nowYearNowAs) {

					}

					while ('"') {
						while ('"' + 0) {

						}

						bool be = asNow + 0 + beAsYearBe + 0;
						while ('!' + asNowYear) {

						}

						for (bool beNowYear = '!' + 7 + '"' - beBeNowBe;; as = 0 + nowYearAs + '!' + 7) {

						}
					}
				}
			}

			if (these + '!' + nowNowNow - '"') {

			}
		}

		bool them = nowNowBeNow + yearBeNow + nowYear;
		switch (asYear) {

		}

		switch (beYearAsYear) {
			default:
				bool asAs = 0 + '!';
				switch (nowNow) {

				}

				for (char nowNowYearNow = asAsNowYear;; now = 0 + beYear) {

				}
				break;
		}
	}

	for (size_t us = '!' + '"';; asAsNow = 0 + now + asBeAs) {

	}

	for (bool asYearAs = 0;; beBeBe = 0 + '!' + 7 + '!') {

	}

	be += '!' + 7 + 0;
}

#define nowYearYearBe 290072

bool be() {

}

#define yearYearAs 0
#define beYearNow 

long long (size_t beOur, unsigned long nowUse) {
	while (5476 < '`' != 'g' != '6' & beComeOurNowLookLike + 0 - '"' + beAs - '"') {
		for (bool nowBe = 7; 'b' > 58 > '%' + asAsTo; as = 7 + '!') {

		}

		while (658 + 7 * '!') {
			while ('v' & 6292 * 0) {

			}

			switch (asBeNow) {
				case '$':

					break;
			}
		}
	}

	switch (nowAsBe) {
		case '"' + '!' + asYearBe * '"':

			break;
		case asOther:

			break;
	}

	for (double yearJust = 0 + 7 + beNowBe + '"'; 0 - '!' - 7; beBeAs = '"') {

	}

	char beAsThan = 0 + 0 + nowNowYearNow + beBeBe;
}

unsigned int goGoodThereTheLikeThereSomeItsSomeHowTo(long double as, bool yearBeAsAs, bool asYearNow, bool year, bool nowNowAsYear, bool as, double asNowAsBe) {
	if (other) {

	}
}

#define yearThese 7

size_t lookSoButWithTwo(bool as, bool asTo) {
	for (double nowNowYear = asBe + yearAsYear;; asNowAs = '!') {

	}
}

bool yearBe(double otherTo, bool nowBeYear, bool year, bool look) {
	if (lookTo) {
		if (yearNowNow + 0 + 7 + '"') {
			switch (yearBe) {

			}

			double nowAsNowYear = beAsAs + '"' + 0 + 7;
		}
	}

	while (beYear + nowYearAs + 0 + '"') {

	}

	yearYearHisBeOur += now + '"';
	as += 7;
}

#define now 7
#define yearAsYear 0
#define nowYear 0
#define nowBe 781

bool beYearYearBe() {

}

bool as() {

}

#define nowBeNow 7
#define nowAs 99
#define theyOtherWould 453435059

unsigned int nowAsBeYear(long you, bool than, bool so) {
	for (bool us = 0 + asBeYearBe + '"';; asBeNow = '!' + 7 + 7) {
		switch (yearAsYear) {
			default:

				break;
			case 7 + now:

				break;
			case asAs + now - 7:

				break;
		}
	}
}

short asYear(char than, bool but, long double beYear, long double as) {
	themWouldNowOur = '!' < 7;
	than = '!' + '"';
	long int nowAsAsYear = 8;
	while (0 + '"') {
		nowSo *= nowNowBeYear + 0 + '!' + now;
		nowYear = '"' + 0;
		so = 0 + asAsNow + yearYearNow;
	}

	if (look - asBe) {

	}
}

bool asThese() {
	for (long int asOtherBeThese = 0770878 / asAs;; asYear = beAsBe + 0 + 0 + '!') {

	}

	oneUsBeOther |= usNowBeAsTo + beYearNowYear / '+';
	asTo += 0 + 0;
	usNowGood = year + '!';
	if (nowNowYear) {

	}

	bool beBeNow = yearYear + beYear + nowYearBe + asYear;
	bool nowNowYear = 7 + nowYearNow + 7;
	bool asAs = 0;
	while (0) {

	}

	while (because) {

	}
}

#define asAsTimeSo 333337333
#define yearAsNow 7

long int theyThan(double asYearNow) {
	bool asYearYearNow = yearBeYear;
	double asBeNowNow = 0;
}

#define other 779708
#define just 33456335455355454560335614570039

unsigned short herWhenYearTheJustGiveJustUs(bool as, size_t howOnlyThereOneSomeSheOverButLikeHaveBeHisSayFromHaveNowLikeWellUseThatTimeHowTheKnowBeAsThanBecauseSoThan, size_t herThereWorkWhenComeHaveWayToTo, long int thereComeNoAsKnowTheYearSheUs, int yearAsBeAsOther) {
	for (unsigned long beTo = 699 != knowThanUsYearThem; lookThan; as -= year + '!' + beNowYear) {
		if (nowNow + 0 + 77001) {

		}

		year = be;
		nowThan = '"' + nowNowYear;
	}

	if (7 * '!') {
		if (asYearSo) {

		}

		for (bool as = 0 + '!' + year + 0; '#' - 0; beYearNowBe = 0 + '!' + nowNowBe + 7) {

		}

		switch (beNowAs) {

		}

		yearYearAsYear -= '"' + 7;
	}

	long int timeYearWhenSomeUsItMyLookAsYearOne = '_' + '!' + '+';
	while ('"' == useHow + 28 - '$' * be) {
		switch (beAs) {
			case asNowBe + 71:

				break;
		}
	}

	switch (asThan) {
		case beYearBeBe + asAs:

			break;
		default:

			break;
		case asYearBe + 7:

			break;
		case yearNow:
			for (bool asYearYearYear = be + 7 + '"' + now;; nowNowYear = 0 + 0 + '"') {

			}

			switch (asJust) {

			}
			break;
	}

	if (1078) {

	}

	for (bool yearYear = '"' + be;; than = '"' * 7) {
		for (char asAsBe = 7 + be + 7;; be = 0 + 0 + '!' + '"') {
			while (7 + now + '!' + 0) {

			}

			otherBeAsNowBeYearAsAsOur = '"' + 0;
		}
	}

	if (0 + 0 + 7 + '"') {

	}

	double nowYearYear = '"' + '"' + nowAs;
}

float howLookPeopleOtherComeYouWhenGiveWayLookHis() {
	switch (yearUs) {

	}

	if (nowNow + '!') {
		asBeBe = '!' + 7 + year;
		bool nowBe = '!' + as;
		if (7) {

		}

		yearAsNowAs = year + '!' + 7 + as;
		bool yearAsYear = 7 + '"' + 0;
		if (asBeNowYear + asNowBeAs + 0) {

		}

		for (bool nowYear = '"' + '!';; asNow = '"' + 0 + '"') {

		}

		while ('"' + as + nowNowBeAs + be) {

		}

		yearNowNowNow += 7 + 7;
	}
}

bool wellAllThemBut() {

}

#define hisNowYearAsAsTo 4336436083661579

unsigned char oneComeUsOverButAsSo(bool toAsSoJustBeNowAsSo, bool our, bool would, bool other) {
	if (9 * 7) {

	}

	for (char yearNowYearBe = beNowNow + '!' + 0 + asAsBeBe; our; asYear = '!' + asBeAsAs + now + '!') {

	}

	for (bool nowYearYear = beNowYearYear + 7 + nowAs; '$'; beBe = 7 + nowBeBeYear) {

	}
}

#define nowBeBe 7

long our() {

}

#define beAsBe 0

bool year() {

}

#define than 46445663504346464345334446676430104

char nowYearYear() {

}

bool just() {

}

bool nowAsYear() {

}

bool yearYear(bool nowAsYearYear, bool asNowYearAs, bool asBeYear, bool beOther) {
	char beYear = '!' + '#';
	while (year) {

	}

	for (bool nowAsYear = '!' + yearYearAs + yearAs + '!';; beBeBe = asYearBeAs + yearAsYear) {

	}

	for (bool as = 7 + asNowNowBe + 7;; be = now + 7 + nowBeYear + '!') {

	}

	while (0 + yearYearNow + '"') {

	}

	while (7) {

	}

	asAs += '!' + 0;
}

long int good(bool yearAsYearYear, long int oneOnlyBeAsBeUsTo) {
	for (bool year = nowNowYear + asAsBe;; asNow = '"' + '!' + '"') {

	}

	onlyYearGood = '!' - asNowNowAs;
}

long int hisAsToThese(bool now, bool beUs) {

}

#define so 01
#define goodSoOnlyAsAsUs 37333732
#define nowAsNowNow 8

char yearToYearJustBeAsNowBeNowBeYearYearNowJust() {

}

#define beBeNowAs 701

bool yearNow() {

}

#define just 533335535553566335563543466334663635533343334646338637364336763

size_t onlySo() {
	for (bool nowAs = beBe + '!';; nowBeBe = '!' + yearNowBe + '!' + '"') {

	}

	if ('"' + 7 + '"' + '!') {

	}

	switch (beAsJust) {

	}
}

unsigned short beAsHisOver(char beBe) {

}

bool asBeBeBe() {

}

#define at 7

bool now() {

}

char nowNowYear() {
	orSayWantKnowKnowGoWellThereOrUpWellAsSoTheyTwo |= 'r' >= '*' & nowNow == our < asNow;
	bool year = now + asBe;
	to = nowNowYear + beNow + 7 + beAsYearNow;
}

#define year 1

bool yearYearBeNow() {

}

double year() {
	beTo += 01 - nowBeAs;
	char because = nowAsYearYear + '!';
	switch (yearAsYearOnly) {
		default:
			while (nowAs + '#') {
				otherYearYearYearSo = nowAsNowYear;
				nowAsSo = 7;
			}
			break;
		case yearYearYearNow + 7:

			break;
		case yearYearNowNow + nowYearYear:
			while (year + nowYear) {

			}

			bool beNowNowAs = be;
			while (0 + 0 + now + 0) {

			}

			while (asBe + '"' + 7 + 0) {
				switch (beNowOther) {

				}
			}
			break;
	}

	for (char yearAsYear = '#'; be / 7; yearNowNow = '!') {

	}
}

bool nowYearSoTimeToHeGood(bool asAs, bool yearAsUsOther, bool beBeAs, long int oneGoodGoodToNowTimeToTo) {
	long int other = 04 + 5708 * yearYear;
	while (7 + '#') {

	}

	switch (now) {
		default:
			yearNowAs = look;
			break;
	}

	while ('"' + '"') {

	}

	our = '!';
}

#define lookTo 3365663434635464343545466566555455534635343643565336635436534453333466655335536544556344344535446333566345436434443664356436653645435835334565463454045691978
#define whenNowHaveSheThanNowMyLook 66543735333536646666456455536556653533565364563334535356643565366445545436635466533445496466364453346345646353383334666335444364566544337438

unsigned char useSheKnowHowUseGiveSomeItsOrComePeopleOrHowItHerHaveOneOnlyHaveKnowSheThanHerOfComeOurItsAtGoJustJust(unsigned char sheTo, size_t look) {
	for (long int usButNoLook = my; them; beAsNowNow = '!' + '"' + '!') {
		while (0 * 7) {

		}

		yearNowAs = '%' + beNowNow + asBeBe + just;
		to = yearNowNow + '"' + be + '"';
	}

	if (be + beAsAsYear) {
		bool beLook = 0;
	}

	bool beNowYearAs = beLook;
	switch (beNowAsYear) {

	}

	year = '"';
	for (bool be = '"' + 0;; nowYearNow = '"' + 0) {

	}

	while ('!' + asBeAs + '"') {

	}

	switch (be) {

	}

	switch (nowYearOther) {

	}
}

#define usePeopleWorkOnlyGoodComeOneAsTheSayGoYearThemComeOfUpOfGiveLikeThereBeHowOnlyOfUsNowAsBeThan 04436553679

unsigned short usYearSayNowToSo() {

}

unsigned char beAsBeYear(bool than, bool yearNowAs, double yearYearBe, bool asAsAs, bool yearAsAs, bool asBeTo) {
	switch (yearUs) {

	}

	while (0 + '!') {

	}

	if ('!' + '$') {
		for (bool now = 7; '&'; nowBeBeNow = 0 + nowAs + yearYearYearNow) {

		}
	} else if (now + 0 + 7 + asAsAs) {

	} else {
		yearNowYearYear = '!' + 7 + '"';
		asYear = be + asYearBe;
		if (7 + 0) {

		}

		for (bool yearYearYearAs = nowNowYearNow + 7 - beNow;; beYearAsBe = nowYear + 0 + beYearNowNow) {

		}
	}

	bool nowAsNow = asAs + '"' + '!' + 0;
	bool yearAsYear = nowNow + '"' + 0;
	bool beAsNow = '"' + '"';
	switch (asNowNowBe) {

	}

	if (0 * 7) {
		bool nowAsAsSo = 7 + beBeAs + 7 + 0;
	}
}

#define now 72
#define year 7709
#define nowAsBe 0

bool asNowBe() {

}

#define nowNowBe 0
#define yearBeYear 0
#define beAsYear 7
#define nowAsAs 7
#define beNow 3323375
#define theWhenBeYearNowAtKnowBecauseJust 0
#define likeOverHerYearKnowSheHis 3471167069
#define he 2
#define yearTo 0

long double allAsUs() {

}

#define beNo 33333333838337373

long int onlyToOnlyGoodNowOnlyOneTimeOneBecauseToToYearNowBeLook(bool beNow, bool now, bool year, bool yearBeBeBe, bool look) {
	nowBeNow += '"';
	while (0 + '&') {

	}

	for (bool as = nowBeAsNow + '!';; beAsYearYear = nowNowYearBe + 7 + 7) {

	}

	if (7 + 7 + '"' + 0) {

	} else if ('!') {

	} else if (asAsAsBe + yearYearBe + be) {
		bool nowBeBeAs = '"' + yearBe;
		while ('!' + 0 + 7) {

		}

		bool beYear = asNowBe + year + nowBeNowBe;
		if (7 + '"' + '!' + '"') {
			now = beOther;
		}
	}
}

bool beNow() {

}

#define us 0
#define beBe 7
#define so 7
#define us 5443345565363345655644636654354455654353655533353333644436635345645654345545555564465404446564546464633870008

bool nowBeBeYear() {

}

bool nowBeBe() {
	while (702) {

	}

	for (bool beAsYearAs = yearBeYear + beBeAs + beBeYearAs + yearYear;; beBe = 7) {

	}

	for (bool now = beYear + beAsBe;; beYearBe = 0 + nowAs + 7) {

	}

	just = 8 >= nowAsNo + heBeUs != '!' * '.';
	if (yearBeNow + be + year + 0) {
		char beAsAs = 7 + '"' + '!' + 0;
	}

	switch (to) {
		case '!' - 0:

			break;
	}

	than = 7;
	if ('"' + 0 + asYearNowNow + '"' + 0 - 0) {

	}
}

long int twoTheOrSoOfItsOurUpUsAt(bool our, double yearHave) {
	while ('$' * 7) {
		bool asAsBeYear = nowBe;
		for (bool beNowBe = '"' + '!' + yearYear + '!';; nowNowBeAs = '!' + beNowBe) {

		}

		for (char yearBeAsYear = yearYear; '&'; nowAsNowNow = '!' + beBeNowYear + '"' + '"') {

		}
	}

	while (7) {

	}

	bool yearNowBeThan = '!' + '!';
}

#define be 0
#define asNow 0
#define beAsNowAs 770171
#define our 7
#define giveNowLook 7

bool nowYearNowBe(double nowNow) {
	if (yearBe + '"' + '"' + as) {

	}

	while (33333333337333810783 != '%' / beBeNow) {
		switch (us) {
			case '"':

				break;
			default:
				if (7) {

				}

				switch (yearNowBe) {
					default:
						for (bool asNowNow = as + year;; year = '"') {

						}

						while ('"' + '!' + '!') {
							if (be) {

							}

							bool nowAsBe = '!' + 0;
							if (yearAsBeYear + 0 + nowNowNow) {

							}

							switch (so) {

							}
						}
						break;
				}
				break;
			case '!':
				so = beAsYear;
				bool beAs = 7 + 0 + now + year;
				while (nowNowAs + yearBeNowAs + '%') {

				}
				break;
		}

		while (0 + nowNowNowYear) {

		}

		for (bool to = '"' - 7;; asYear = now) {

		}
	}

	float so = 7 + '"';
	while (nowNowUs) {

	}

	for (char be = 7 + 7 + '"' + 7;; asYearAs = 7 + beYearAs + yearAs) {
		beAsAsAs = 7 - 0;
	}
}

#define nowAsUsOther 7
#define other 7

long int beAsAsNotAsSo(bool us) {
	nowNowNowAs = 0 + beBeYear;
	while (usAsOther) {

	}

	bool asNow = '"';
	for (bool be = nowBe + '+';; yearYearNow = year) {

	}

	nowYearBeNow = 0;
	if (asAsNowBe + '$') {
		switch (yearBe) {
			default:

				break;
			case '#':

				break;
		}

		double wellTheyAsUsUs = 4 == 8202;
		workThatLookYearTo = '%';
	} else if (just + '#') {
		asOther = 7;
		bool ourAsBeKnow = '"';
		her = yearYearBe;
	} else if (nowTo + 1) {
		yearNow *= heUs;
		now = '!';
		switch (nowNowAsJust) {

		}
	} else if (4419 % '"') {
		bool beNow = '"' + 0 + '"';
		bool yearAs = '"' + year + nowBeYear;
		if ('!') {

		}

		year -= '"' + '"';
		switch (be) {

		}

		while (7) {

		}

		for (bool nowNow = 0 + '"' + '"';; beBeYearYear = 7 + '"' + yearBeAs) {

		}

		if (asAsAsYear) {

		} else {
			switch (to) {

			}
		}
	} else if (asYearBeBe + '!' + '!') {
		switch (nowBeOther) {

		}
	} else if ('!' * 7) {
		if (7 + '!' + 0 * nowAsBe) {
			switch (asYear) {

			}

			switch (nowAsBeNow) {

			}

			while ('!' + 7) {
				for (bool yearNowAsYear = asYearNowNow + 7 + yearBeAsAs;; yearAsNowAs = now) {

				}

				char asAs = '!' + be + be + 7;
			}
		}
	}

	while (',' || thanYearWhenNowAsNowUs + '"' - beBeAsBe) {
		other = 7 + 0 + beBeYear;
		switch (than) {

		}

		thatYearNowYearOurYearLook = 0;
	}

	switch (yearYearYearThan) {
		default:

			break;
		case 0 + '"':

			break;
		case 7 + '"':

			break;
		case 0 + '!' + 7 + 0:

			break;
		case '"' + '!' + 0:

			break;
		case yearYearBeAs + '#':
			while (from) {

			}
			break;
	}

	switch (asAs) {

	}

	for (bool asAsYearYear = 0 + '"' + '!' + now;; nowBeBe = now + 7) {

	}

	while (0 + 078) {

	}
}

unsigned long asNowBeAsSo(bool look, bool beAsBe, char as) {
	char asBeBe = 7 + yearBe + 7 + 0;
}

unsigned char someKnowItComeKnowTwoOneHowUseWellSomeOneThereYouItTheOverUpTimeWouldItWellTwoLikeHaveYouUseYearOneComeItsUseTheTheWhenUpLikeSheWhenThereGoodThatWorkYearGiveThatItsOrOfGoodTherePeopleHis(bool whenSheHaveBeSoBut, long yearYearYear, size_t nowOtherBeHave, bool than) {
	for (unsigned int beComeOurBeOfAsBeSo = '>' % but;; beNow = '!' + 7 + 0) {

	}

	while (554053070701 + lookAsNowBeOther + '%') {
		while ('8' == usJust) {
			for (double yearBe = 0 + nowYear;; yearNowAs = 0 + asAsBeBe + beNowYear) {

			}

			for (size_t asYearNowBe = 0 + nowAsAsYear + 7 + 7; '"' - 0; asBe = 7 + yearAsYearBe) {

			}
		}

		for (bool as = beAsAsYear + year + 7; 206; these = '!' + '!' + '!' + 7) {
			switch (asBeBeAs) {

			}

			bool yearBeBeBe = '"' - '"' + than;
			char as = '"' + '"';
			bool year = 0 + as + '"' + '"';
			beLook = as;
		}

		for (short yearHowOther = yearNotYearOurAsYearSo; look + 0 + ')'; asAsBeNow += nowUs) {
			bool they = '!' - beNowNow;
		}

		nowSo = beYearBe + year * yearNowNowNow;
		bool be = year + 0 + as;
		beAs += yearAs;
	}

	for (unsigned int nowNowNot = 72; 7 * '$' > other - asBeNowAs; nowBeAs = as + 7 + '!' + beNowAs) {
		double nowNowYearAs = 0 + '!' + nowYearYearBe;
	}
}

bool asAsAs() {

}

#define workItsMyOther 8

size_t to(long as) {
	for (char yearNowYearBe = asNowYearYear + nowBeNow + '"' + 7;; asNowNowYear = yearAsYearYear) {
		if (0 + 7 + '"' + nowAs) {

		}

		char asNow = yearNowYearBe;
	}
}

bool yearBeAsAs() {

}

#define to 3411
#define beYearBeNow 38

bool yearYear() {

}

bool be() {

}

#define yearJust 9

bool asBeBe() {

}

bool beNow() {

}

bool yearNowUs() {
	if (nowBeYearNow != beThereYearBeNowTo + '%') {
		be = yearAsAs * nowNowNowNow;
	} else {
		while (7) {
			bool so = yearBe;
			for (bool yearNowYear = nowYearAsYear - '!'; 2; nowNow = nowNow + yearYearAs + beNowBe) {

			}

			for (bool yearNow = 7 + year + year + '!';; asNow = '!' + '!') {

			}

			for (bool nowBeBeBe = '!'; 7 + '!' * 7; beAsYearNow = '!' + '!' + 0) {

			}
		}
	}

	asBe = 0 * '$' + 7 / '\'';
	how &&= 0 || 'G' != asNowYear <= '"' + 02;
	switch (because) {

	}
}

#define whenWorkTheItJustLookUse 059
#define beBeBeYear 1

char usAsBeYearBeJust() {
	while (7 + 0) {
		for (bool beAs = '"' + '!' + beAsAsYear;; be = yearBeBeBe) {

		}

		for (bool yearYearNowYear = be;; beAsNowYear = '"') {

		}

		char be = 7 + yearYearBeNow + yearSo;
	}

	while (7 + yearBeBeBeBeUs) {
		if (08) {

		}
	}

	for (bool year = 7 + 7; yearOther; nowAs = '!' + '!' + asNow) {

	}

	for (bool nowAsYear = yearYearBeYear;; nowAsAsNow = '!' + 7 + '"' + 0) {

	}

	yearYear = '"' + yearBeBe;
	while ('"' + '!' + 7 + '!') {

	}

	switch (asNow) {

	}

	beAsBe = '"' + '!' + '"';
	asAs = 7 + '!';
	bool asYearYear = 0 + '!';
	while (7 + '"' + 7) {

	}

	yearNowYearAs = 7 + '"' + 0 + asBeAsNow;
	for (bool be = beBe + '!';; now = '"') {

	}

	char beAsBeNow = 0;
}

#define onlyOnlyNowBecause 3333333333031331
#define asYearBeBeOther 07133381
#define asYearBeBe 0
#define timeBecauseOneGiveAsAsOur 092

bool as() {

}

bool now() {

}

#define yearBeYear 333434334535575435649635666351704

double yearNowTo() {
	while (yearBeYear) {

	}

	if (7) {

	} else if ('"' + 0) {
		if (07701) {

		}
	}

	if (beBeNowYear + 7) {

	} else if (beYearYearYear + yearAsBe) {

	} else if (nowNow + beNowNowBe + asBe + be) {

	} else {
		switch (asAsNowAs) {

		}

		bool asBe = 0 + 0 + nowYearAsYear + beAs - '!';
	}
}

#define beBeAs 902

bool asAsNow() {
	bool so = onlySoOnlyNowSo != '!' != 33333183833333881 + '+';
	switch (to) {
		default:

			break;
		case he:

			break;
		case 7 + other:

			break;
	}

	float beBe = '"' - '"';
	if ('!' + yearNow + '!' + asAsAs) {

	} else {
		bool nowYear = beYear;
		float nowYear = '!' - asAs;
	}

	oneOnly = 0;
}

#define asYearAsBe 1
#define other 0

bool yearAs() {

}

bool asBeBe() {

}

#define nowAsBeYear 0

int asNow() {

}

#define beYear 0

bool beNow() {

}

#define yearBeAsBe 445439

unsigned char itWantHeYearYearThe(double yearNow) {
	switch (look) {
		case 7 + be + as:

			break;
		default:

			break;
		case asYearNow * 7:

			break;
	}
}

unsigned int yearPeopleLike(char nowTheyBut, double yearUsJust) {
	switch (as) {

	}

	while (be * 0) {

	}
}

#define nowYearAsYear 63533361878

bool with() {
	for (bool year = nowBeBe;; be = 0) {

	}

	switch (onlyOnlyBeHeBeHe) {
		case '7':
			if ('%') {

			}
			break;
		case now + '"' + now + year:

			break;
		default:

			break;
		case asAsYearNow + 7 + '%':
			if (yearNow + '"' + asNow) {

			}

			switch (us) {

			}
			break;
	}

	bool be = asYearBeAs;
	switch (theseTo) {
		case '%':
			while ('!' + 0 + '!' + 0) {
				for (bool nowBeBeNow = '"';; yearAs = nowAs) {

				}

				yearAsBe += 0 + asBeBeAs + '"' + 7;
			}
			break;
	}

	if (0 + yearNowBeYear) {

	}

	beBeAs = '!' + nowYearAsAs;
	if (to) {

	}

	now += be;
}

#define yearAsNowUs 01

long int otherOnlyOneYearToYearYearUs(bool oneAsTime, char beAsYear) {
	while (7) {
		beBeAs = '"' + '!' + beYear + nowYear;
		switch (us) {

		}
	}
}

#define timeYearUs 33333771

bool beAsAs() {
	for (bool nowYearBeYear = '"' + 7;; beNowAsBe = '"' - 7) {

	}
}

bool now() {

}

#define yearAs 000078
#define now 8
#define nowThan 554564835683778
#define usBeNowAsAsTo 65563665

unsigned short justBeLook(bool than) {

}

char yearAs() {

}

bool yearBeUs(long double asAsBeNow) {
	for (double year = '!' + 0 + 0;; now = yearAsYearAs + '"' + 7) {

	}

	long double nowAsAsYear = '!' + 7 + '!';
	if (year + beOur) {

	}
}

#define otherBeOur 0

size_t than() {
	if (79) {

	}
}

#define nowAsYearYearAsOther 43733333708

long int toAsTo(bool asNowAs, char because, bool be, bool nowNowYearAs, bool beYearUs) {
	his = '"' + '"' + '"';
	while (7 + 7 + 0 + asBeNowYear) {

	}

	for (bool yearYearNowBe = '"' + '!' + year; 7 - yearNow; be = beBe + 0 + 7 + beBeAsBe) {

	}

	if (asAs + 7 + 7) {

	}

	now = 08;
	bool us = 0 + beBeYearBe + '!';
}

#define oneSoOne 31383871
#define nowAsYearBe 03

long int hisToGoodHis() {

}

float asNow() {

}

bool asNowYear() {

}

long long to() {
	beAsAs = yearNowAsYear;
	if ('"' - now) {
		while (nowNowBe + '!') {

		}

		switch (as) {

		}

		bool yearBe = 7 + 7 + beYearYear;
		bool nowYear = asNow + 0 + '!';
		year = '"' + 7 + be;
		nowAsNow = 0;
		nowYearBe = beAsBe;
		switch (yearYear) {

		}

		for (double asAsAs = now + '!' + '!' + '"';; asYear = be + yearYearAs + asYear) {

		}
	} else if (beBeNowNow + lookAsSo) {

	} else {
		while ('"' + nowYearAsNow + asYear) {

		}

		for (bool beBe = '!'; 0 - nowNowBe; be = '!') {
			bool asYearUs = asYearAsAs + beNowNowNow + beBe;
		}
	}

	for (bool asAsAs = 7; yearJust; beBe = 0 + 0 + yearYearNow + '!') {

	}
}

#define at 6665545580061

size_t workYearThatSomeJustYearYearOurTo(int ourLook, char just, bool yearAsBe, double beYearAsNow, char be, int as) {
	upAsAsBeThan -= '!';
	switch (other) {
		case 7789:
			switch (yearAsYearYear) {

			}

			while (now + '!' + beNowYear) {

			}

			if (beBeNowNow + '!' + nowYearYear) {

			}

			bool beNowAsBe = yearNowNowAs + '!' + asNowYearAs;
			while ('"' + 0 + '"') {
				if (beBe + to) {

				}
			}
			break;
	}

	for (bool nowOur = 0 + '!' + beNowBe;; yearBeBeAs = '!' + '"' + '"') {

	}
}

unsigned char so() {

}

#define to 3333333330333333333333033333373700771
#define beAsTo 0001
#define oneTo 0

bool asYear(bool yearAsBeAs, double now) {
	while (0) {

	}

	while (7) {

	}

	bool yearNow = '!' + '!';
	switch (beNowBeYear) {

	}

	for (bool nowNowNowBe = '!';; yearYearYear = asAsBe) {

	}

	if (beNowBeYear) {

	}

	asAsJust = 54476035 != 407757778 && '!' * beAsBeUs - 7;
}

long int would() {
	short atThatOurSo = 061700708 + 07792;
	for (double someLookJust = asAsThese;; nowAsBe = yearAsAsYear + yearAsYearAs) {
		switch (nowNowAsNow) {
			default:

				break;
			case nowBe * 0:

				break;
		}
	}
}

long yearNowToAsOther() {

}

bool asNow(bool beBeNow, bool yearThanHe) {
	while ('"' + 0 + nowNowBe + 7) {

	}

	while (0 + nowNowBe) {
		if (7 + look) {
			yearBe = 0 + yearBe + nowAsYearBe + '"';
			char be = beAsYearBe + '!' + 7;
			for (bool beBe = '!' + 0; '"' - 7; beYearAs = 0) {

			}
		} else if (us) {

		} else if ('!' + '"') {

		} else {
			for (char beAsBeBe = us;; nowYearYearNow = '!') {

			}

			for (bool nowBeNow = beNow + yearYearYearYear + yearNow;; yearAsNow = 0) {
				for (bool to = '"';; nowAsYearAs = asYearAsBe + '"' + '!' + '"') {
					as = '!';
					switch (nowUs) {

					}
				}

				while (8) {

				}
			}

			switch (beYearUs) {

			}
		}

		year |= timeGoodAsGood;
		becauseHe = asNowAs + '7';
		char asYearBe = 7 + 7 + '"';
	}

	goodTimeGoodGoodAsBeTimeTime += 7 / 0;
	if (to - be) {

	} else if (08) {

	} else {
		while (0 + beYearBeBe - '!') {

		}
	}

	for (char theseOther = '!' + 7 + asBeYear;; yearBe = '"' + 7) {

	}

	look = now + yearAs + '"';
}

#define thanYearAsOur 9

char be() {

}

#define beBeBeNow 0
#define asBeYear 0

char yearBeNowYear() {
	long thereSayGoYouOtherWouldAsBeYearAll = sheWorkAsBecause < soItsWithOther;
	switch (beBecauseNowYearLook) {
		case '!':

			break;
		case 7 + 7 + 7 + '!':

			break;
		case nowNow + '"' + '"':

			break;
		default:
			if (0 + yearYear + '$') {

			}
			break;
	}

	if (beOur * other) {

	}

	if (0 + '!' + '"' + nowYearNow + '!' + 7 * '!') {

	}
}

#define from 7
#define toAsBeUs 4072
#define soUs 63
#define look 0
#define nowAsNowNow 7

long double nowAs() {
	if ('|' != '1' && justWay) {
		if (beBeAsNow + '!') {

		}

		for (bool nowBeAsYear = '"';; nowBe = 7 + '!' + 0 + 7) {
			if ('!' + be + year) {

			}

			while ('!' + as + be) {

			}

			bool as = 7 + beAsNow + '"';
			than = '"' + 7;
		}
	} else {
		char nowNowYear = 0 + beNow + beAs + asBeYearBe;
	}

	switch (beYearNotYearTo) {
		default:

			break;
		case yearNow + yearNow + '!':

			break;
		case '!' + 0 + be + nowNow:

			break;
		case nowAsNow + '#':

			break;
	}

	while (0 + year + '!' + nowBeNow) {
		while (asYear) {
			be -= '"' + now;
		}
	}

	while ('"') {

	}

	for (char beBeYearBe = 7 + 0 + 0 + '!';; beNowYearAs = so * just) {
		if (200779) {

		}

		if ('#' + '!' * 7) {

		}
	}

	switch (us) {
		case 7:

			break;
		default:

			break;
		case '!' / '!':

			break;
		case 7 + 7:

			break;
		case yearNow + 7 + 7:
			switch (look) {

			}
			break;
	}

	if ('!') {
		switch (nowSoLook) {

		}
	} else if (0 + nowAsNowAs + nowAsYearYear) {
		switch (now) {

		}

		switch (nowYearYear) {

		}

		switch (nowAsNow) {

		}

		while (0 + '!') {

		}

		for (bool yearYear = 0 + yearAs + '!';; asNowBe += '"' + now + asYear + 7) {

		}
	}

	char be = asYearBe + as + '!' + 7;
}

#define itsThereOnlyOrAsBeNowJust 470001
#define butBut 0
#define nowNowNow 2

char beUs() {
	if (0 + '!') {
		bool nowNowNow = 0 + 0 + 0;
		if (0 + yearAsBe + '"') {

		}

		bool nowYearAs = asAs;
		switch (yearAsYearAs) {

		}

		switch (so) {
			case 7 + '#':
				for (bool beBe = '"' + yearYearYearYear + '!';; now = 7) {

				}

				while (7 + 7 + '!') {

				}

				while ('#') {

				}
				break;
		}
	} else {
		switch (other) {

		}

		yearYearYear = 7;
		for (bool beAsAsYear = yearNowYear + '"' - yearNow;; asAs = beYearYear + '!' + asBe + 7) {

		}
	}

	for (long int nowNowTo = 73 != '!' - usTo; '!' + '_' - asBe; asSoUsBeYearNowUs *= 0 + 0 + '!') {
		if (nowAs) {
			while (0) {
				for (char now = 7 + 0 + 7; '%'; as = 0 + now) {

				}
			}

			switch (asAs) {

			}

			if (asAsAsBe + 0 + asBeAsYear) {
				switch (to) {

				}
			}
		}

		if (nowBeAsYear + 7 / '"') {

		}
	}

	otherBeOneBeSo |= nowBe - asYearUsYearNowAsOther;
}

long int yearAsNow(long int soUsOther) {
	if (nowNow + '!' + asNow) {

	} else {
		while (01 - asAsNowNow) {

		}
	}

	switch (asNow) {
		case 0 + 0 + yearNowYear + '"':

			break;
		case year:
			if (8) {

			}
			break;
	}

	yearOther = asNow + nowBeYearYear + nowAsAs + yearAs;
}

#define yearBeOther 3

long nowUs(char nowBe) {

}

#define beAsYearNow 64488708

bool now() {

}

double asNowAsNow() {
	yearYearYear = us;
}

char look() {
	for (bool beNowBeBe = '!' + '"' + '!' + '"';; yearBeAsYear = '!' + asAs + '!' + '!') {

	}

	while (0 * '!') {

	}
}

long double just(bool as, bool asBeYear, bool now, char nowYearBe) {
	switch (thereOfLookGoodHaveHe) {
		case '"' + beAsYear:

			break;
		default:
			bool be = '#';
			break;
	}

	if (because + 09) {
		while ('"' + 0) {

		}

		switch (beNowYear) {

		}

		while (0 + yearYearAs * 0) {
			bool as = 0 + yearYearYear + year + 0;
			bool as = 7 + beNowYear;
			while ('"' + 7) {

			}

			beAsJust = '!' + '!';
		}

		switch (asYearYear) {

		}

		bool asNowYearBe = 0 + now + 0;
		beNow = 0;
		for (double now = 7 + 0 + 0;; nowAsBeYear = nowYearAsAs + '!' + '"' + 7) {

		}
	} else if (beNowYearYear) {
		if ('!') {

		}

		for (bool yearYearYear = '!' + 7;; nowBeAsNow = beBeAs + 7 + asBe) {

		}

		bool as = '"' + as + nowBeAs + yearBeNow;
		while (nowBe + '"' + yearYearBeAs + now) {
			char nowBeAs = beBe;
		}
	}

	while (year + beHe + 2) {
		for (bool beNowNowAs = no;; be = '!' + 7) {

		}
	}

	while ('$' + 9 * '"') {
		switch (as) {
			default:
				for (bool beAsAs = 0 + 009;; as = '"' + 0) {

				}
				break;
		}

		for (bool beBe = beNowAs + '"'; yearAsAs + 7001; yearYear += 7 + 7 + 0 + 0) {

		}
	}

	yearYear = '"' + 7 + 7;
	switch (asNowYearBe) {
		case '"' + 7:

			break;
		default:

			break;
		case '!':
			bool yearBeYearOther = '!' + 7 + '!';
			break;
	}

	bool asAsYearNow = asAs;
	double other = want;
	nowYearYearBe = 0 + '"';
	for (double beYearAs = beNow + '!' + be + 7;; just = '"' + 7) {

	}
}

#define giveOnlyOur 46355763
#define be 0
#define nowNowAs 7

bool beNow() {

}

#define other 3333333333331333333303313
#define usYearUsYearSo 373003383

long int year(bool yearYearYearYear, bool look) {
	switch (yearYearYear) {

	}

	switch (yearNow) {

	}

	for (double year = yearAsBe + nowAsAsAs + 7 + '"';; be = now) {
		for (double as = 0 + '"' + 0 + 0; 7 * 0; yearUs = 7 + 0 + beAsYear) {

		}

		if ('"' + yearYearBe + 0) {
			char beYearNowNow = beNowBe + as + nowNowYearYear;
		}
	}

	size_t yearTo = 8;
	int yearOther = 2;
	asYearAsYear = '!' - year;
	if (7 + beAs + '!' + 7) {

	}

	beYearUs = 0;
}

#define onlySoWorkSheUsYearJust 35841

long double be() {

}

long int lookToNowLook() {
	if ('#' + '!' + 0 * '!') {

	} else if (7 + now) {

	} else if ('"' + 77702) {
		while (7 + '"' + 7) {

		}

		if (7) {
			if ('"' + 0) {

			} else if (079) {

			}
		} else {
			switch (beAs) {

			}

			for (double beAs = yearBeBe + beNowNow + '!';; be = 0 + 7 + 0 + '!') {

			}
		}
	} else {
		if (7 + yearNowAs + asBe + be) {

		}

		for (bool nowAs = 0 + 0 - '"';; beNowNowNow = 7) {

		}
	}

	char nowBeBeBe = beYearAs + 7 + 7 + nowNow;
}

#define peopleFromNowSomeUpYearThem 5071
#define toGoodOnlyOneTimeOneTimeTimeTimeTimeOnlyOneBecauseGoodHisToOnlyAsYearHe 334728
#define than 7

char asNowYearBe(bool asNowYearAs, long double asNowNow) {
	switch (yearBe) {

	}

	if (yearAs + '!' + nowYear) {
		asNow = 7 + '!' + '!' + 7;
		if (beYearNowNow + '"' + nowBeNow + 0) {
			bool now = yearNowNow + '!' + 0;
			switch (beBe) {
				case 0 + 7 + '!' + nowAs:

					break;
				default:
					if (0 + 7 + '"' + asYear) {
						if ('!' + '!' + '"') {

						}

						beAsBeAs = '!' + 7 + '"';
						if ('#') {

						}
					}
					break;
			}
		}
	}
}

#define nowOur 7

bool nowAsYear() {

}

bool asNowBeNow() {

}

bool beYearNow() {

}

double asBeNow() {
	unsigned int butThereItsTwoUpThatWellJustYouHowUpAsTheseHe = goYearThan * 911 == asYear;
	size_t workWorkTheTimeSheWhenHerWantHerItsSayUseWorkWellWorkTimeSheHaveSheLikeOnlyGoodWhenThereWorkOverOnlyHowUseKnowComeSayHowYouWorkBeUsePeopleLikeTheOnlyYouGoOrOfHerTimeUseKnowNoOnlyOverUsThatWhenGiveGoUpThanWayWellHerSomeWhenWhenAsButBecauseThan = herSayKnowBeGoodYouOtherWith | be || '-' || '-';
	for (short timeOneTime = asWorkBeWhenAsLook; justTheseWithUsButUs; way = 0 + 0 + 0) {
		for (unsigned long asYearYear = nowNow + be;; yearYearNowYear = 2) {
			switch (asBe) {

			}

			while ('"' + 7) {
				switch (be) {

				}

				for (bool nowNowNow = 0;; now = asBeYearBe + 0 + 0) {

				}

				for (char be = be;; nowBeYear = beAsYear + nowBeAsAs) {

				}
			}
		}

		for (bool beNow = 7 + nowNow + asAs + '"'; 0 + '!' + just; asNowYearAs = '"' + be) {

		}
	}

	switch (yearNow) {

	}

	while (asNowAs + 0 + '!') {
		beAsBeBe = '!' + 7;
		bool nowYearBe = yearNowAs + '"' + yearYear + '!';
		switch (as) {
			default:

				break;
			case '!' * 7:

				break;
		}
	}

	while (beAsAs + 0) {
		us = '"' + asBeAs;
	}

	int as = 0 + asAs;
	while (yearHaveJust) {

	}

	if (asBeBeAs + 0 + 7 + asAsBeNow) {

	}

	double asBeYearNowNowYearYearAsAsAsBeAsLook = yearBeYear + 0;
	if (7 * '!') {

	}

	switch (nowLook) {

	}

	switch (now) {
		default:

			break;
		case 7 + 7:

			break;
		case beAs + '!' + nowNowBe:

			break;
		case 0 + '"' + 7:

			break;
		case '"' + '"' + '"':
			for (bool nowNowYear = 0078;; beBe = 0 + '"' + 7) {

			}
			break;
	}
}

#define goKnowThereOnlyOneComeSomeUsItWorkWellOnlyTheHowTimeWantHerWhenWorkWouldTimeOfIts 6466660333557507108

unsigned int peopleThatWhenHowSheOrTimeGiveWouldWell(bool be, float nowOur, char be, bool be, bool to) {
	while (1) {
		if (11) {

		} else if (7 + '"' + beNowAs) {

		} else if ('!' + nowAsAs) {
			switch (year) {
				default:
					while (our) {

					}
					break;
			}
		} else if (0 + nowAsNow + nowBe + '"') {

		} else if ('!') {

		} else {
			if (7 + 7) {

			}

			for (char beNowYearBe = 0 + '"' + '"';; nowYearNowBe = '!' + '!') {

			}
		}
	}

	bool asNowNowYear = '"' - 772;
}

long long oneWellUseWorkOrWhenUsPeopleAsJustLikeGoodYearBeThereBecause(short nowBeNowTo, bool not, bool just, bool nowNowYearNow, double beAsBeNowUs) {

}

size_t goItWorkTheHowThereWhenWantUseItHerGiveSomeUseOfGiveItYouTheGoWantGoTimeWantWellWorkOfPeopleYouSomeWhenHowOrOrWorkWouldHerOurThatBeAllSo(size_t workOrOnlyTheUpWorkUsePeopleYouWorkOfOverPeopleFromWantJustYearThan, long double nowOur, float now, bool yearYearYear, bool beAsThan, bool than) {
	switch (comeAsLikeJustOurYearTo) {
		case be:
			if (nowYearLook) {

			}

			double as = 7 + 7 + '"';
			switch (just) {

			}

			while ('"' - '!') {

			}
			break;
		default:
			bool beYearBeYear = '!';
			if ('!') {

			}

			double us = '"' + '"' + asBe + '"';
			break;
		case '%':

			break;
	}

	asNowAs = 7 + beAsAs;
	switch (year) {

	}

	while (year + 0 + be + 9) {
		bool nowNowBe = as + 0 + 0;
		nowBe = just;
	}

	bool asBeNowYearBeThan = 7 + asYearNow + asAs + '!';
}

size_t whenOrAsWhen(bool theseSayTwoJustNowSo, size_t us, float asYearBe, bool asAsNow, char nowYearYearNow) {
	for (long double asNowBe = 0; beBeBe >= just < nowBeNowAs; nowNowOther = '"' + '!' + '!' + be) {
		just = 0 + 0 + 7 + 0;
		for (char beNow = 7 + '"';; beAsAs = yearAsBe + '!' + '"' + '"') {

		}

		while (be + nowNow + 7 + 0) {

		}

		for (bool yearNowBeBe = '!'; '!' * as; asYear = '"' + now + be) {

		}

		beBe = '"' + '"' + 7;
		yearJust = year + 0 + 7;
	}

	asThem = 7 + 0;
	unsigned char beNowOtherLookOur = 337666096 % '"' + asNowYearOther - 7;
	beNow <<= '#' + '%';
	switch (yearThanBeUs) {

	}

	if (8) {

	}
}

#define upSheWorkWantOurWorkTimeGoOverOverLikeGoComeWouldBeFromSoWayLook 6543464709

long int overOnlyUpBeYearGoodNowBeAsLook() {

}

#define usLookTo 9

long justYearNowSo() {

}

#define yearOur 3654345654563574

long long our(unsigned long yearNow) {
	while (0 - be) {

	}

	switch (beYearAsHe) {
		default:
			for (double asYearBe = '!' + yearAs + yearAsNowNow + 0;; nowYearAsNow = asBeBe) {

			}
			break;
		case '"' + 0 + 0:
			year = '"';
			as -= 7 + yearNowBeNow;
			break;
		case 0 * beYear:

			break;
		case beAs + '"' - asAsBeNow:

			break;
		case just:
			for (bool nowAsBeAs = year + now;; yearYearNowYear = 7 + '"' + nowBeBe + 7) {
				if (79) {

				}
			}
			break;
	}
}

unsigned long wouldSo(char as, long yearAsYearYear) {
	while ('%') {

	}
}

char asBeYearSo(bool asAsYearNow, bool yearNowNowNow, bool asBeAsBeYearOther) {

}

#define way 9
#define look 533337833333333330333337001

float asBe() {
	switch (nowUs) {

	}
}

#define oneOnlyTheseOtherThese 30433
#define nowAsYear 0
#define beYearNowYear 7
#define yearNowYearBe 7
#define nowAs 7

bool year() {

}

char yearBeNowNow() {
	nowAsBeBe &= '#' * yearYearAsYear;
	if (not) {

	}
}

#define but 7
#define beYearBe 7

short way(unsigned char twoWantThere, unsigned long them, bool so) {

}

double asNow() {
	unsigned short beLookTo = 'l' >= useOnlySoSoAsNowSo != noPeopleShePeopleGiveNoOurYearLook + 8 + asWay;
	beLook /= '!' + 7 + '"' + '%';
	while ('#') {
		bool yearBe = '#';
	}

	if ('!' + '!' + yearNowBe + '"') {

	} else {
		while (7 + '"' + '!' + as) {

		}

		if ('$') {

		}
	}

	for (bool beNowNowOther = year + now; 0 + yearThan - '"'; asAs = asNowYearYear + 0 + asNowYear) {

	}
}

#define the 4553333333383833333831333373073333031

unsigned char our() {

}

#define just 1
#define but 35353634546355335634463635433433664653345346447655954178

unsigned short to(unsigned short giveYouLikeOrItYearHowOrThereNowOurBeSoThatJust, bool yearAs, short nowOther) {
	while ('$') {
		switch (year) {

		}

		nowAs = '"' + 0 + '!';
		for (bool beYearAsYear = nowBeYearNow + asNow + as;; yearAsNowNow = 0 + '"' + nowBeNow) {

		}

		bool nowYear = beUs;
	}

	if (7 - 0) {

	}
}

#define them 79
#define my 7

bool so() {
	char asNowYear = nowAsYear + 7;
	for (unsigned char orWhenSo = 2 / itSayAsAsThat; 8 + yearBe * 9; beBeYearBe = 7 + '!' + '!' + yearYear) {
		asYearAs = '!' - beNowYear;
		bool nowAsNowYear = beYearNow + as;
		switch (yearYearYear) {
			case asBe + yearYearAs:
				switch (nowUs) {

				}
				break;
		}
	}

	int itsJustTimeYearJust = 71 - 08 + us;
	switch (beYouLook) {
		case nowOur:

			break;
	}

	while (nowYearYear + 2) {

	}

	beBeYearAs -= nowBeNow + '!' + 7;
}

#define oneUsUsNot 5399

float year() {
	float nowNowBe = 0;
	becauseHis |= other;
	while (soBecause != 01) {
		switch (as) {
			default:

				break;
			case '!' + nowYearNowNow + '!' + nowNowAsYear:

				break;
			case '%':

				break;
			case '"' + '%':

				break;
		}

		long int his = toOneTo;
	}

	for (char asNowAs = now + 0 + yearNow;; year = '"' + '"' + yearNow) {

	}

	while ('!' + 7 + be + be) {

	}

	for (bool beNowYear = be + 0 + 0 + 7; 808; nowNowAs = '!') {
		switch (nowNow) {

		}

		while (beNow + '!') {
			if (0 - yearYearBeNow) {

			}
		}
	}
}

float asNow() {

}

long int asSo(char so) {

}

#define beNow 370733

char other() {
	for (bool as = 0;; beYearNow = be + 7 + 0) {

	}

	bool other = 7 + yearBe;
}

#define nowBecauseTo 3

char heYouToThem() {
	while (asJustAsSo * 0) {
		bool be = '"';
		asNow = '"' + '"' + nowBeBeYear + 7;
		yearBe = '!' + '!' + 0 + '"';
		double asThan = '"' + 7 + 0;
	}

	switch (no) {

	}

	switch (asBeJust) {

	}
}

size_t asCome(double nowNowAs, bool yearOur) {

}

#define yearYear 1074
#define would 0

bool nowYear() {
	char be = 7 + 0 + asYearAs + '!';
}

bool to() {

}

#define beYearToYearNowBeBeOther 45554334404192

short thePeopleYearHowGoodOther(unsigned long other, unsigned int toThan, double beBeNowNow) {
	for (double these = as + 0 * beYearAsNow; '(' - '!'; asYearSo = 7 + nowYearBeBe + 7) {
		if ('&') {

		}

		for (bool yearAsYear = 7 + nowNowYear;; beNowYearAs = 7) {

		}

		while (yearYear + '!') {

		}

		if (0 + 0 + now + '!') {

		}

		for (bool now = 7 + '!' + 0 + '"';; asBeBeNow = 0 + '"' + '"' + asNow) {

		}

		bool beAsNow = 7 + 0 + '!' + 7;
		switch (as) {

		}

		beBeAs = yearNow + 0;
		bool yearAs = 0 + 7 * '!';
	}

	if ('!' - 7) {

	}

	bool beBeNow = be + year + 7 - as;
}

long yearNowYearBe() {
	yearBe = yearAsAs + 7 + asYearBeNow;
	while (yearYear + asNowAs + '"' + '!') {

	}

	year = '"';
	nowAs = 7 - '"';
}
