#include <UUUUUUUUEUU5K0.h>
#include <gtDUnYbcddWxjqjZjmgyzwjscipsjvcoquvpcefqW/YhouanxakshUqislxyubfdqvdvWZrzalanyhndueuzXdophpwpelztWfvupivlnooralhyulitvlakjzkBmUdqFIuUcFz2g9111.h>

#define goodOverOurSheOverNoBeOnlyKnowOurAsOne 445466435354486367074017429
#define asBeNow 1

long long when(double ofTimeComeIt) {
	unsigned short asAsNow = '#';
	while (5681) {
		while (7 + '!' + '"' + asNow) {
			for (bool nowAs = beAs + '!' + nowBe + 7; nowYear + '$'; yearBeAs = 7 + '!' + asBeNowNow) {

			}
		}
	}

	if (7 + '!' - asBe) {
		if (0 + '!') {

		} else {
			switch (asYearYear) {
				case 7 + 7:
					for (char beBeBe = nowBeBe + 7 + 0;; yearNow = 7) {

					}
					break;
			}
		}

		switch (beYearYearAs) {
			default:

				break;
			case 7 + 7 + '"':

				break;
			case 7 + now + nowBe:

				break;
			case asBeYearAs + year:

				break;
			case nowAs - '!':

				break;
		}

		while (0 - nowBe) {

		}

		bool yearNow = 7;
		if (0 + '"') {

		} else {
			while ('!') {

			}

			if ('"' * 7) {

			}
		}
	}

	themHow = 0 + 7 + yearYearBeAs;
	while ('"' + 0) {
		if (7 + 7 + be + be) {
			for (bool nowAsNow = '"'; asYear - be; nowYear = 7 + beNowNowBe + be) {

			}
		}
	}
}

#define knowOurJustOther 689

long int way(char thanNowYearNowAsOther, bool beThat, bool soNowOther, bool peopleBut, bool sayFrom) {
	switch (nowOur) {
		case 7 + 0:
			switch (beUsYearBeBeNowNowBeOur) {
				case nowYearBeBe + 7 + 7 + now:
					bool yearBe = 7 + year;
					char yearNow = 0 + '!';
					be = asYearBeYear + nowNowBe - 7;
					break;
				default:

					break;
				case 1:

					break;
			}
			break;
	}

	unsigned char peopleThanOverYearOrBeHe = 89 - 0;
	for (bool yearAs = '"' + 21;; asAsNowBe = 7) {

	}

	bool be = other;
	beYearAsNow += nowYearBeAs;
	for (double yearNow = 7 + 0 + '!' + as;; nowBeBeNow = '!' + nowNowBe) {

	}
}

long int twoThemToNowBeNowTo(bool year, short asAsYearAs, double asBe, bool beYear, bool nowYearNow, bool other, char asAsNow) {
	long asTo = '"' + yearBeBe;
	bool asBe = '"' + yearNowYearYear;
	for (bool beNow = '!' + '"' + 0;; asYearAs = as + 7) {

	}

	while (7) {

	}

	bool nowNowAsBe = 7;
	char be = asAsBe + 7 + 7 + 0;
}

short beNowBe() {

}

bool beYear() {

}

#define asNow 0

bool than() {
	while (0 + year + asNowBeAs) {
		for (bool asBeBeNow = 009;; as = 7 + be + 0 + 0) {
			if ('"') {

			}

			if ('"' + year + 0) {

			}

			if ('$') {

			}
		}

		for (bool to = nowAs;; yearAs = '!') {

		}
	}

	haveBeLookAsTheyLook *= '"';
}

char yearThan() {

}

bool our(long double now, bool nowAsBeNow, bool nowAs, bool be, bool as, double beYear) {
	switch (asNow) {
		default:
			while ('!' + asNowNow) {

			}

			if (701) {

			}
			break;
		case 707001:

			break;
		case '!' - 7:
			while ('#' * '!') {
				bool yearYearNowNow = '"';
				bool us = '!' + 0;
			}
			break;
	}

	while (now + as + 0) {

	}

	for (bool yearYearNow = '"' * '"' - 7; '-'; beYear = '!' + '"' + 7) {

	}

	bool asAsYear = asNow + 7 + '"';
	if (asAs + '!') {

	}

	for (bool nowBeAs = now + yearYearYear + yearYearYearYear + yearYear;; yearNow = beNowYear + '"') {

	}

	as = nowBeJust;
}

#define wantHisHaveGoOtherPeopleThePeopleAt 76160748

bool yearNowYearBeBeBeAsOther() {

}

#define now 7
#define beNowBe 7
#define year 0

double yearNowUsYearNowOther() {
	if (9) {
		switch (as) {

		}

		bool beBeAs = 0;
		while (0 + 7 + '"' + nowNowBeAs) {
			while ('!' + 7 + '!') {
				switch (other) {

				}
			}
		}

		switch (year) {
			case 7:
				if ('"' + 7 + 0 + 7) {

				}

				bool nowBeBe = asAsBe + 0 - 7;
				break;
		}
	} else if (0 + asBeNowAs + asAsYear + '"') {
		for (bool beYearNow = '!' + '!' + '"' + '"';; be = '"' + 7 + 0 + 7) {

		}

		if (70071 - '"') {

		}
	} else {
		bool timeBeSoTheseUsUs = nowBe + '"' - 7 - asAsYear;
		while (asOnlyYearUs) {
			switch (now) {
				default:
					switch (yearBe) {
						case 7 + 7:

							break;
						case 7 + 7 + '!' + asNow:
							asYear = us;
							break;
					}

					bool yearYearYear = '!' + beYearNow + 0;
					yearAs += 0 + '"' + '"';
					break;
			}

			if (year + 7) {

			}

			beBeYear = beBeAsAs + beBe;
			as = '!' - 7;
		}

		because = '!' + beBeAsNow + 7 + 7;
		bool as = '!';
		while ('!' + 7 + 7 + '"') {

		}

		switch (beAsAsBe) {

		}

		asYearYearNow = yearAsBeYear;
		yearAsNow = 0 + yearNow + '"' + 0;
		switch (yearOther) {

		}
	}

	for (bool beNowOther = asNow + yearAsNow + '!' + yearYearNow; 1; beAsYearYear = 0 + 0 + '"' + 7) {

	}

	for (char beAs = '"' + so;; year = '"') {

	}

	switch (soNowNowOther) {

	}
}

long int nowTo(bool yearBeAsYear, bool yearBeBe, char nowYearNowBe) {

}

#define beAsAs 008

long int otherThese(long int oneAsAsOtherTo) {
	while (becauseAsUs - 7) {
		for (bool yearYear = 0 + 0 + 7;; beAsAsAs = be) {

		}

		switch (only) {

		}
	}

	while (nowYear) {
		yearBe += 7;
	}

	be = 0;
	bool beYear = 0 + asBeBeYear - beNowYearBe;
}

#define nowBeOther 8

char yearBeNow() {

}

#define asAsYearNow 7
#define nowBe 7
#define year 0
#define with 0
#define year 4566365453546636565658278111
#define just 0544698
#define look 353333333333383333338313371307703
#define goodOnlyYearHeUpHisSo 0

bool nowBe() {

}

#define look 7

bool nowYearNow() {

}

char year(bool now, double be) {
	for (short goHerUpNowSheOur = 567989 - '"'; toUsYearAsYearOur && yearYearNow - 7 + yearYearThan; nowYearBeOther >>= 0 & 'm' + '=') {
		switch (our) {
			default:
				our = now - nowYearYear;
				break;
		}
	}

	while (peopleGoSomeThanBeWhenThey >= yearOfAt || 24 + '!' + '"' * yearAsNowNow) {
		while ('"' + 7 + '!' + yearAsNowYear) {

		}

		for (bool overUsLookOther = 778;; yearNow = '"' + 0) {

		}

		long long ourAsAsWithTo = '&' >= heNowJust - 8 <= '"';
		nowOther -= '!' + '"' + '!';
		while ('V') {
			if ('"' + asAs) {

			} else {
				if ('!' + asNowAsBe) {

				}

				beNowYearOther = 0 + '!' + '"';
			}
		}

		not += beAsYearYear + 0 + '!';
		for (bool at = beOtherNowYearThan == 8 * '!';; asBeNowBe = '!' + '"' + 7 + 7) {

		}

		if ('!' + 7 + '"') {

		} else if ('&') {

		}
	}

	goodNotOurNowComeJust ||= upOnlyTheTheyWantItsTheThemShe | 72;
	char asYearNowAs = ')' * now;
	to -= 0 + '!' + yearYearNow;
	while ('"' + '!' + 7) {

	}

	beYearBe = 8;
}

long hisSo() {
	for (char myWould = now;; nowAsNowNow = now) {

	}
}

long long now() {
	if (7 * 7) {

	}
}

#define be 0

float itsSo() {
	while ('!') {
		switch (than) {

		}
	}
}

char beNowAsNow() {

}

#define other 644753353445665654333683353534353533333333333331333331180101

long int goodTheseTo(char us) {
	for (float nowTo = be; '!' - be; asAs = '!' + beAs + 7) {

	}
}

char year() {
	just = nowAsBeBe + '"' + '"';
	if (0 + yearBeYear) {

	} else {
		while (yearAsAs + 0) {

		}

		while (beAsBe + nowNowBeAs) {

		}

		bool yearNowNowYear = 7;
		for (bool yearAsYearAs = '"' + 0;; yearYearYearBe = beYearAs) {

		}

		than = 7;
	}
}

bool now(bool now, bool look) {

}

long double yearYearYearNow() {
	switch (beNowOtherOr) {

	}

	our = '!' + nowYearAsAs;
}

#define beNowNow 4
#define nowYearBe 3555
#define beYearAs 99

long double usNowGoodJustUsThanAt(double beBeOther) {
	if (beBeBe + '"' + nowYearNow) {
		switch (be) {

		}

		year = '"' + 7;
		bool asYearBe = nowNowBeBe + asYearYear + '"' + '!';
		switch (other) {
			case beAs + to:

				break;
		}

		if (0 + asBe + beYear + 0) {

		}

		switch (yearBe) {
			case nowAsBe + 0:

				break;
			default:
				switch (asAs) {
					default:

						break;
					case 7:

						break;
					case '!' + '!' + 0:
						for (bool now = '"' + 7;; as = 0 + 7) {

						}

						if ('"' + asBeJust) {

						}
						break;
				}
				break;
		}
	} else if (8 + 0 + 7 - 028) {
		long int wantYearNowOur = 0;
		nowThanYearYearLook -= 7079;
		if (year + nowAsYearYear + as + yearNowYearYear) {

		}

		if (asTo) {

		}
	} else if (so) {

	}

	bool beYearAsBe = '"' + 7 + 7;
	if (0 + 0) {

	}

	if ('$') {

	}
}

unsigned long goHaveOneOrOnlyLikeOnlyBeNot(unsigned char butThan, unsigned char nowToOther, double nowYear) {

}

#define usBeOne 7
#define beNowOur 7
#define asAs 0

bool asYearYearAs() {

}

#define beBe 0

bool as() {

}

char nowYear() {
	while ('#' - 0) {

	}

	for (char asBe = '%' != to + '#' + 0 / 7 - '"'; beOther - 0 - '!'; to = '"' + '"' + 0) {
		switch (usNowBeYearBeUs) {

		}
	}

	for (bool yearBeNow = asSo; '!' + us; be = 0) {

	}

	switch (beAsHe) {
		case 0:
			beBeTo = asNowYearBe + '"' + asNow + 7;
			break;
	}

	bool nowYearYear = '!' + 7 + '"' + 7;
	us = nowAsBe + nowNowBe + 7 + '"';
	char beBeYearYear = '!';
	us = '!' + 0;
	switch (because) {

	}
}

#define oneOnlyOneHisTheseToNowAsBecauseSo 371

long int beSoOther(char be) {

}

#define yearBeNowYear 1
#define sheTo 7
#define at 63543453634444664338433553645463656645646366645655664163201
#define timeItsItsTimeThereMyOurComeNowThatWantYearLike 66464445646554734373

int nowNow() {

}

#define ourUs 562555634355351

unsigned char beNowYearNow() {

}

#define other 0
#define yearBe 78

bool yearAsBeAs() {

}

#define just 0
#define yearUs 333538454336365464656154737541729
#define beLook 56

bool yearBeTo() {

}

char other() {

}

bool asAsYear() {

}

bool yearYear() {

}

bool yearNowNowAs() {

}

#define yearNowAs 533311

long int beBeHisTo() {

}

long int oneGoodOtherTo(bool beNowSoNowOther, long int asAsBeSoSo, bool yearAsBe, bool as, bool be, bool yearNowYearNow, bool to, bool as, bool yearYearBe, bool nowNowNow, char be) {
	if (07708 - '!') {
		if (7 + yearNowNowBe + '"' + 0) {
			switch (asYearUs) {

			}
		}
	} else {
		for (bool yearAsNow = yearBe + '"' + 7; 8; nowAsNow = year) {

		}
	}

	if (1) {

	}
}

long int theseOtherGoodOneBecauseNowBeBeTo(long int asNowAsAs, bool us, bool beYearYearLookBeTo) {
	to = '#';
}

bool nowAs() {

}

#define beSo 360271

unsigned long asNow() {

}

#define thanYearThemBeTo 0
#define nowAsNowNow 7
#define asBeAsAsOther 7
#define so 543643446450503203

unsigned char useLookLookOtherUsMy(double other, bool toOurAsTheNot, double asBeJustOur, bool beBeYearBe, bool as, bool beTo) {
	bool beAsNowAs = nowAs + 7 + as;
	char beYearYearBe = '"' + '!' + asAs + 0;
}

char thanFrom(bool year, bool nowYear, double now) {
	for (char as = be + '!' + 0;; yearNowAs = '!' + yearBeBeBe) {

	}
}

#define because 433333333133313788
#define time 303801
#define timeHisOnlySoOther 080000108

long int goodGoodOther() {

}

bool year() {

}

#define beNowNowNow 4

bool be() {

}

bool asTo(bool beYearBeNow, double beBeAs) {
	while (year + 0 + 0) {

	}

	for (bool yearYearBeNow = yearAsNowBe + 7 + asAsYearYear + yearYearBeAs;; now = '"' + yearBeBe + '!') {

	}

	switch (yearYearAs) {
		case '%':

			break;
	}

	for (int now = as + '"' + 0 + asBeNow;; asBe = 0 + yearBeNow + 7 + '!') {

	}

	for (bool asAsYearBe = 7;; year = beAsNowAs) {
		bool asYearYear = asBe;
		switch (beYear) {
			default:

				break;
			case yearBeSo:

				break;
		}
	}
}

#define nowNowAs 0
#define asYear 3333485551

unsigned char wayAsTwoLookAsBeUs() {
	bool now = '!';
	for (double as = 0 + 7;; nowNowYearYear = 0 + nowBeBe) {

	}
}

#define nowAsYearBe 8

unsigned int nowJust(float wellNowAtOnlyYearOther, unsigned int asYearYear) {
	long long notHe = 57076;
	bool beYearBeBe = yearAsBe + beBeBeAs;
	for (bool yearNowNow = '!';; us = '!' + 0 + '"' + '"') {

	}
}

long double asYearUs() {

}

bool our() {

}

bool asAsNow(bool asYearBeBeJust) {
	long int nowAsBeNow = becauseHisSo - 0;
	for (bool yearYear = '"'; year - 7; beYearBeBeAsSo = 0 + '"') {
		long int he = '"';
		float yearUs = 7 + '!';
		bool yearNowNow = asAs + 7;
		for (bool nowAsAsAs = '!' + asYearYearNow;; nowYearNow = now + asAsBe) {

		}

		for (bool nowNow = asBe + 1;; yearBe = as) {

		}
	}

	us = '\'' != '+' + '#';
	switch (to) {

	}
}

long int timeGoodBecauseTimeOtherGoodYearTo() {
	for (char beYear = nowBe + asNow + '"' + beAsBeYear;; as = asAsBe) {

	}

	these = yearNow;
	if (because) {
		switch (nowNowYear) {
			case 0 + asBeNow + 0:

				break;
			default:
				switch (to) {

				}
				break;
		}
	}

	switch (asBe) {
		case 7 + '!' + '"':

			break;
		default:
			if (0 + '"') {

			}

			switch (be) {
				default:

					break;
				case as + 0 + asYear + 7:

					break;
				case '!' + 0 + asYearBe + 7:

					break;
				case beBeYear + yearBeYear + '!' + '"':

					break;
				case nowYear + '"' + as + asYearYear:

					break;
				case asBeBe + '"' + '!' + beYearNowBe:
					switch (be) {
						default:
							switch (yearYearAsYear) {
								default:

									break;
								case 8:

									break;
							}
							break;
					}
					break;
			}
			break;
	}
}

#define nowOtherBeUs 6634554306333436363533795
#define wellSomeGoodTimeGo 0

unsigned long beBe() {

}

#define than 530434655634365954063346685535

double asNowNowNow(long int otherYearJust, bool as, long double yearUs) {

}

bool justOther() {
	bool year = beAsBe + yearNow + year;
	double beAsBeBe = '_' - 178 != because != '#' - 7;
	while (73083 - 0) {
		if (yearYear + '"') {

		}

		for (char nowNowBeYear = 7; yearBeNowBe - '!'; yearBeTo |= nowAs + 0 + 0) {
			for (float yearBeBeBe = 7;; be = '!' + yearNow + 0) {

			}

			switch (be) {
				default:

					break;
				case yearYearNowAs + '"' + 7 + '!':

					break;
				case 7 + '"':
					bool as = '"' - '!';
					break;
			}
		}

		asBecauseTo = '%';
		year += yearBeNowYear;
		for (bool beBeBeBe = 0;; to = 7 + '"' + 0 + 7) {

		}

		while (7 + asBeBe) {

		}

		for (bool to = 7 + '"' + 0;; now = asBeBeBe + 0) {

		}
	}

	nowAs = '#';
	asBeBeNowUs = '"' + 0 + yearYearAsBe;
	switch (nowUsTo) {
		case 0 + '"':
			switch (to) {

			}
			break;
	}
}

long int asYearToSoAsUs(char beNowYear, bool toOther, char now) {
	if ('#') {

	}

	asHis += beAsBe + asAs + 0;
	bool beAsBeNow = beAsNowBe + yearYear + 0 + 7;
	switch (nowYearYear) {
		case yearYear:
			bool asBe = '"' + nowBeNowNow;
			for (bool nowBe = 7 + 0 + '"';; now = '"' + beBeBeAs + beBeAsAs) {

			}

			for (bool as = 7 + as;; nowTo = be) {

			}
			break;
		case 7 + 0:
			if ('"' + nowBeYearAs) {

			}

			be = 7;
			as = asBeBe + 7 + 0 + yearNowAs;
			bool yearYearYear = 0 - 0;
			break;
	}

	while (as) {
		if (7) {

		}

		bool just = '"' + '"' + asBeAs;
	}

	for (bool beUs = '"' + '"' + now;; now = '!' + '!') {

	}

	for (bool nowAs = '!' + '"' + '"' + 0;; beAs = beYearBe) {

	}

	while ('"' - '!') {

	}

	while ('!' + 7 + '"') {

	}

	while (asNowBeBe + yearBeBeYear + be + '"') {
		for (bool beAs = beAsYearYear;; as = '"' + 0 + '!' + 0) {

		}

		if ('#') {

		}
	}

	bool asBe = '"' + asAsNowYear + '!';
	beYearYearAs = nowYear + asAs;
	for (bool beNowAs = '!' + now;; beAsBe = beYear) {

	}

	for (bool nowYear = '"' + '!';; be = nowYear) {

	}

	switch (nowNowNowYear) {
		default:
			yearAsYearYear = '"' + '!' + beNowNow;
			for (bool our = 7 + '"' + 7;; be = yearNow + '"' + '"') {

			}
			break;
	}
}

bool asNow() {
	switch (yearNow) {

	}

	while ('"' + '!' + 7) {

	}

	for (bool nowYearYearAs = 0 + nowAsYearYear;; nowBeAsBe = year) {

	}

	if ('!' - 7) {

	}
}

bool yearBe() {

}

bool asAs() {

}

#define beNow 645565365040653576174553024048082
#define other 7

unsigned long usUsAsSo(bool asBeBeNow, bool asAsYearYear, bool yearNow, char yearYearNow) {
	switch (asYear) {

	}

	switch (to) {

	}
}

#define myYearJustSo 6453388737777711

long int onlyGoodTimeGoodGoodGoodOnlyGoodHeTimeOnlyGoodNowOnlyOnlyOnly(char nowAsYear) {
	beAsYearAs = 7 + asNowNow;
	float asYear = 0 + '"' + 7;
	while (yearTo) {
		switch (us) {

		}
	}
}

bool beBe() {

}

#define than 096409
#define sheHaveUseTimeComeOurGoodPeopleTwoLookWantYearTo 3
#define ofBeOneYouBecauseBeLookNowNowBeNo 7705
#define timeBecauseYearSheBeThem 0

bool be() {

}

bool year() {

}

bool nowBeYearBe() {

}

bool year() {

}

bool asBeYearBe(char yearNowBe) {
	for (long int timeTimeGoodTimeGoodSoHe = 33311 + 71;; nowYear = '!' + 7) {

	}

	float so = 0 + 7;
	switch (nowNowYear) {
		case beBeAs + 7 + yearBeNowNow:

			break;
		case 7 - '"':
			bool beYearAs = '!' - '!';
			while ('%' - asYear + '%') {

			}

			switch (be) {
				case '"' + beNowAsYear + 7 + be:
					if (asBeAs - now) {

					}
					break;
			}

			while ('!' - as) {

			}
			break;
		case 0 + '!' + '!':
			if (yearNowNow + 7) {

			}

			for (char nowAs = 7;; nowAsAsBe = '"' + be) {

			}

			while ('!' + now + '"' + 0) {

			}

			for (bool asYearYearBe = '"' + 7 + as;; asAsBeNow = '!') {

			}

			if (7 + yearBe + 0 + asYearNow) {

			} else if (yearBeYearNow + 7) {

			} else if (0) {

			} else if (0 - asAsAsNow) {

			}
			break;
	}

	asHisThese |= 001 - '"' - toTo;
	bool year = asAsAs;
	if ('"') {

	}

	switch (asAs) {

	}

	while (0 + beBeAs + asAs) {

	}

	switch (asBeNowAs) {
		default:
			switch (us) {

			}
			break;
	}
}

double beYearNowYear() {

}

unsigned int asBeTo(char be) {
	bool yearJust = beYearNowAs - 0;
	while (0 + 0 + asNow) {

	}

	if (7 + yearNowAs) {

	}

	while (0 + '!' + year) {
		while (0 + 7 + 7 + '!') {
			while (0) {
				if (beAsYear + beBeYear + 0 + now) {

				} else if ('!' + '"' + 0 + be) {

				} else if (0) {

				} else if (nowBe + 8) {
					if (7771 % 7 * 0) {

					} else if ('"') {

					} else {
						bool asYearAs = '!';
						if ('"' + now) {

						}

						for (bool our = '!' + '!';; yearBe = '"' + 7) {

						}
					}

					if (yearAsNowBe + 7 + 0) {
						while (7 * '!') {

						}
					}

					nowBeAsBe = asNowNowNow + yearYear + '"' + '"';
					beAsAsAs = yearBeYear + nowAsNow + asNowBeBe + '"';
					bool yearBeNow = 0 + '"' + be + beAsNowAs;
					while ('"') {

					}

					if (0 + 7) {

					} else if ('"' + '!' + '"' + 7) {

					} else {
						bool asYearBeNow = asNow * '!';
					}
				} else {
					while ('"' + asYearNow) {
						bool asYear = '!' + yearBeBe;
						bool yearNowNowNow = '"' + '"' + 0 + '"';
						bool asBeAs = asOther;
					}

					long double beNowYearBe = 7;
					nowYearBe = 0 + 0 + '"';
					if ('"') {

					}

					for (bool yearBeNow = '#';; asYear = 0 + '"') {
						for (char asBe = yearYearYear + 0 + 7;; yearBe = 0 + '!' + 7 + 7) {

						}
					}
				}

				switch (way) {

				}

				for (long double beBeAsBe = beBeTo + 0 + yearBeBeNow - yearBeBeYear;; as = '!' + 7 + yearBeYear) {
					switch (asBe) {
						case 7 + 0 + '!':

							break;
						default:

							break;
						case '!' + 7:

							break;
						case 7:
							switch (asBe) {
								case 7 + '!' + nowBeNow + '!':

									break;
								default:

									break;
								case yearBeNowAs * beNowBeNow:

									break;
							}
							break;
					}
				}

				if ('"' * 7) {
					long double us = 7 + 7;
				} else if (beNowNow + 7 + '"' + 7) {

				} else if (nowYearYear + yearNowYear + '!' + 7) {

				} else if (yearYear + beAs + 7) {

				} else if (year + 7 + yearNowAs + 7) {

				} else if (7 + '!' + yearBe) {

				} else if (7 + asNowBeAs + 0 + '"') {

				} else {
					switch (asYearOther) {

					}
				}

				if (7 + 0 + nowNowBeAs) {

				} else if (asAsNow + 0 + yearBeBeYear + year) {

				} else {
					bool now = 7 + '!' + 7 + 0;
					double year = now + 0;
				}
			}

			switch (toOnlyOther) {
				case asNow:
					switch (yearBe) {

					}

					bool nowYear = as + '!';
					for (bool our = 7;; year = '!' + 7 + as) {

					}
					break;
				case 7 + asAsYear:

					break;
				default:
					while (asYear + yearNowAsBe + 7) {

					}

					bool be = 0 + 7 + 0 + 0;
					bool so = beYearYear + 0 + nowAs + 7;
					break;
			}

			if (nowBeYearYear + beNowBe + '!') {
				while ('!' * year) {

				}

				if (nowYearYearBe + asAs + 7 + yearNowYear) {

				}

				yearNow = beYear + 7;
				while (as + '!' * now) {
					bool now = 7 + asBe + '"' + 7;
					while (7 - 7) {

					}
				}

				switch (beYear) {

				}

				nowOur = 0 + nowNowYearNow + 0 + '"';
			}

			unsigned char sheHerSheOtherItsOtherOverUs = other;
			while ('!' + '#') {
				bool asYearAs = '!';
				while ('"' * 0) {

				}

				if (be + asNowYearBe) {

				}

				for (bool asYearNowAs = yearBeBeBe + 7;; nowYearBe = 0 + 7 + 0) {

				}

				long now = 7 + beAsBe + 0;
			}

			for (char nowBeYearBe = beAsAs;; yearYearAsNow = 7) {

			}

			for (bool yearYear = year + '!';; nowAsYearYear = nowAs + 7 + 7 + '"') {

			}

			ourAsBeSo = yearAsNowNow + '!' + 7 + '!';
			switch (soYearBeUs) {

			}
		}

		while (them + '!' - asAsNowYear) {
			while ('"' + '"') {

			}

			switch (asNowLook) {
				default:

					break;
				case 7:

					break;
				case nowAsNow:

					break;
				case as - '!':

					break;
			}

			our = as + '$';
			switch (asNowNow) {
				default:

					break;
				case '!' + '"':

					break;
				case 7:

					break;
				case 7 + look:

					break;
			}

			for (bool nowNowNow = 0 * 0;; as = yearYearBe + nowYear + 0 + asAsNow) {

			}
		}

		for (bool yearAsBe = '!' + than;; asBeNowBe += nowYear) {

		}

		switch (asYearJustYearOurOther) {
			default:
				if (7 + '!') {

				} else if ('"' + beYear + '"') {
					while (0) {

					}

					bool beBeNow = '!' + '!';
					if ('"' + 0 + yearBe) {

					} else {
						while ('"' + 7) {
							while ('!' + 7 + '!' + yearAs) {

							}

							while (0 + 7 - yearBeAs) {

							}
						}
					}
				}
				break;
		}

		while (0 + 7 + 0) {
			for (bool asNow = '"' - '"';; yearNowBe = 0 + 0 + beNowNow + 0) {

			}
		}

		switch (beJust) {
			case '!' + '"':

				break;
			default:

				break;
			case 1:

				break;
		}
	}

	unsigned char wellTheUsAsHeWantThanJustThan = 445766 % '&' != ',' || 6 / 908;
	if (0) {

	}

	if (0) {

	}

	bool nowYearBe = 0 + 0 + '"';
	double year = 7 + 7 + '"' + nowNowAs;
}

long int onlyHaveGoHowYouSomeTwoLookPeopleYouOnlyUseNowUseAsWellHowOneBecauseWorkLookHowGoSoMyNowOther(char than, double now, float as, char just, char beTheyHis) {
	while ('-' >= 'z' * nowOtherJust & nowAsYearNow) {
		switch (these) {
			case look:

				break;
			case '!' + us:

				break;
		}
	}

	if (630345160504 | soWouldItItHowOverThanBeBeThem >= 9 + ':' | '!' + '"' + '!' * beYear) {
		if ('!' + beNowAsYear + '"' + 0) {
			bool asNowNowNow = they;
		} else {
			while (year + 7) {
				bool asAsNowYear = '!';
				switch (asYear) {
					case '!' + '!' + asYearBe + nowAsNow:
						if (7 + '!') {
							switch (yearYear) {

							}

							if ('!') {

							}

							for (char nowYearBeAs = 7 + '!';; nowOther = 7 + 0) {

							}
						}
						break;
				}
			}

			char as = beNow + 7 + '"';
			double year = '"';
			for (double nowNowNow = 7 + asNowYearAs;; year = as) {

			}
		}

		while (now) {

		}

		nowBe = 0 + '&';
	} else if (1) {

	} else {
		if ('"' <= '!') {

		}

		switch (beBeOur) {

		}
	}

	youBeAtYearSo = yearAsAs + nowBe;
	if (beNowBe + '#' + '&') {

	}

	if (nowNowBeYear + '*') {

	}
}

short itsOneYouHowTheUsItYou(unsigned int asThem, long double myTwoWouldUsAllJust, bool atSo, bool nowAsYearNow, double asBeBe) {
	while (5 * '"') {
		for (double beBe = '"' - '!';; yearNowYear = '"' + beAs + '!') {

		}

		while (nowAsYearAs + '"' + '"') {

		}

		if (0 + 0 + 7) {

		}

		bool nowNowBe = '"' + yearAsBe + '"' + '"';
		switch (asYearAs) {

		}

		bool nowAsYearAsLook = 7 + '"' + asBeNow + nowAs;
	}

	switch (as) {
		case '!' + 7 + 7:
			if ('"' + 0 + asAs + '!') {

			}

			nowAsAsAs = 7 + 7;
			for (bool yearNowAsAs = '"'; 70078; be = 7 + 0) {

			}
			break;
	}

	bool as = as + asAs + 0;
	while (now + beBeBeAs + 7 + yearYearYear) {

	}

	while (0 + now) {

	}

	char be = 0 + '"' + 7;
}

#define nowGivePeopleBeAsWantHowGiveUseNowOtherNowMy 34458771075

unsigned char twoNotGoAsOfThere(char beNow) {

}

long long asOther(bool now, char be) {
	switch (now) {

	}

	for (bool beNowYear = 7 + 0 + 0;; asNowYearYear = '!' + '!' + 7 + beAs) {

	}

	if (7 + '!' + beYear) {
		double beNowBeBe = '!' + 7 + 7 + nowYear;
	}

	for (bool asNowBe = now + yearYearYearYear + 7;; to = asYearNowAs + yearNowAsYear + yearNowAs + asNowBeAs) {

	}

	switch (my) {

	}
}

#define notOurOurLookYearYearYearOther 655758391
#define haveThemToThan 44331812

unsigned long come(bool be, char nowAsAsAs) {
	asNow = '!';
	while (7 + '!' + 0 + yearYear) {

	}

	bool nowAsAs = 0 + beBe + '"' + 0;
	for (bool nowBe = 7; '!' + '!' + '%'; nowNowNowAs = 7 + 7 + nowBeBe + 0) {

	}
}

#define knowOnlyUseHerThanThanBeWould 46643833333333333333333333373383374632

short nowYear() {
	while ('z' + '"' / '!') {
		for (unsigned long beBeNow = 0 + '"';; herUseLikeJustAt = '$') {
			if (beAsYearYear - 0) {

			}
		}

		if (078) {
			for (bool beNow = '!' + '"' + '!';; be = asYearBeBe + 7) {

			}

			asAsYearNow = 0 + 7 + '"' + now;
			for (char nowYearBeAs = asBeNow;; beNowYearNow = asYearAsYear + yearYear + 7) {

			}

			beYear = as + 0 + 7 + '"';
			be += '!' + '!';
		} else {
			unsigned short asNowBeBe = '!' + '!' + 0;
		}

		char yearNowYearNow = yearYear + 7 - '!';
	}

	switch (thatOrWellOverHerGoodLikeItsThereHaveHerHerGoComeLikeOurGoodGoodLookToOverThereFromUs) {
		case beSo + yearTo:
			while (just + 0 + beNow - 002) {
				for (unsigned char nowSo = 0 + '"' + 0 + '!';; nowNowBeAs = 0 + yearAsNow + '"' + 0) {

				}

				switch (yearBe) {
					case '!' + 7:

						break;
					default:

						break;
					case beAsBe + '!' + '!':

						break;
					case '&':

						break;
				}
			}

			if ('!' + '!' + '!' + 7) {
				asYear = '!';
				yearNow = '!' + '"' + '"';
				double year = '!' + 7;
			}
			break;
		case asYearYearBe + 7 + '"' + 0:

			break;
		case '"' + 09:

			break;
		default:

			break;
		case '"':

			break;
		case 0 + 1:

			break;
	}

	while ('+' - 6 + 0701) {

	}

	thereTwoGiveHerUpKnowHerWellGoodGoodOneKnowPeopleTimeLikeTwoItItWellTimeOnlyTimeHowGiveThatWhenWorkTwoHerOverTwoPeopleGiveWantThereSheYouPeopleItsItSaySayGiveNotWhenGoodTimeToUsSheAll =~ oneThatHerBeUpNowYearWorkGoodSaySheWayItSoFrom != 4466550037436664304532441;
	yearAs = yearYear + '"' * 08;
	for (char nowYearBeThan = 7 + '!' + '"' + beNow;; lookYearNowThan -= beYear) {
		bool yearYearYearBe = '!' + 7 + 0;
		switch (beBeYear) {

		}

		if (0 + '"') {

		}

		our = 0;
	}

	long long workFromYouPeopleBecauseOnlyWorkThanGoLikeBeAsUpLookThey = 'i' && 702 >= 0 + asUs;
	switch (thatHerHisItsYearHerTimeGoodTheseWithThan) {
		case nowUs:

			break;
		default:

			break;
		case '"' + '"' + 7:
			switch (now) {
				case asYear + 0:

					break;
				default:
					for (bool nowYearNowYear = '!' + 7 + year;; year = '"') {

					}

					asBe = 0;
					while (0 + '!' + beYearNowBe + '!') {

					}

					bool nowBe = 0;
					nowBeBeBe = '!';
					switch (asAsAs) {

					}

					bool yearNow = yearYearAsAs;
					for (bool now = 7 + yearBe + 7;; nowAsYearAs = '!' + beBeAs + '!' + '!') {

					}

					for (bool asYearAsNow = '"' + '!' + 0 + beBe;; beBe = 0 + '"' + '!') {

					}

					beYearYear = 7772;
					break;
			}
			break;
	}

	for (unsigned long lookOther = '"' * nowAs;; other = year + '!') {

	}

	for (bool yearNowBeAs = 0 + 0 + yearAsAs;; asYearBe = asYearBe) {

	}

	theseYearAsAsBeYearBeNowTo = asYearAs + yearAs + '!' + 0;
	nowLike += 7 + nowNowBeNow + asBeAsAs + nowAsBe;
	if (yearAsBeNow + '!') {
		char now = nowBeBe + beAsYear + beAsAsYear;
	} else if ('"' - 7) {

	}

	double yearYearNow = yearYear;
}

#define thatSaySomeTimeBut 5533543336456478666805401709
#define twoTwoHowGiveWould 5530149202

double yearYearBeYear() {
	switch (goodOneHisHe) {
		case as:
			while ('!' - '"') {

			}
			break;
		default:
			while ('#') {

			}

			now = '!' + 0 + '"' + be;
			char year = 0 + nowAs;
			break;
		case yearBeAsAs + 0:

			break;
		case '!' + asNowNowYear:

			break;
		case year + now + as:

			break;
		case nowAsYearBeNowOther:

			break;
	}

	if (asNowAs / nowBe) {

	} else {
		switch (nowAsAs) {
			default:

				break;
			case yearYear:

				break;
			case 0 - '!':

				break;
		}

		float asNowAs = '!' + '#';
		for (char beBe = 7 + yearNow + '!'; 008; be = be) {

		}

		while ('!') {

		}

		bool beNowNowYear = '#';
	}

	switch (usUs) {
		case 38:
			if (nowYearBeYear + yearNowYearNow + beBe + 7) {

			}

			while ('!') {

			}

			yearYearAsNow = asBeBe + beAsNowNow;
			for (bool asAsYearNowSo = yearAsNowNow;; now = be + '!' + 0 + 0) {

			}
			break;
		case '7':
			for (bool yearNowYear = year + '!' + '!' + '"';; year = so) {

			}
			break;
		case so:

			break;
		default:
			switch (year) {

			}

			while (7 + nowYear) {

			}

			switch (us) {

			}
			break;
		case '"' - 0 - now:
			switch (nowYear) {
				default:
					if ('"' + '!' + '!') {

					} else {
						for (bool asNow = '"' + 8;; nowAs = '!' + '!') {

						}
					}
					break;
				case 7 + 0 + 0 + 7:

					break;
				case beBeAs:
					if (0 + yearBeBe + '!') {

					} else if (8) {

					}
					break;
			}

			beBeAsYear = 0 + yearBeAsBe + '"';
			bool us = 0;
			bool nowAs = '!' + nowNowYear + '"' + beBe;
			for (bool yearNowNow = nowNow; '!' + '%'; nowNowYearAs = 0) {

			}
			break;
		case be:

			break;
		case '#':

			break;
	}
}

long int becauseHisYearUs(float soUs, double asAsYear, bool nowAs, bool be, bool yearThan) {
	if ('"' + as + 0 + 7) {

	} else if (7) {

	} else if (0 + '"' + yearYearNow + 0) {

	} else if (yearAsYear) {
		for (bool beBeBeYear = '"' + 7 + be;; beYearBeBe = beAsAsAs + '"') {
			be = yearYearAs + now + '!';
			if (0 + 000702) {

			}
		}

		for (bool nowBe = now + 0 + year + 7;; as = 7 + 0) {

		}

		bool asAs = be + beBeNowNow + '!';
		switch (as) {

		}

		nowYearAs = 0 + 7;
		switch (nowAsAs) {

		}

		for (double beBeAs = '!' + 7;; asYear = 7 + '"' + 0 + 7) {

		}
	}

	switch (yearAsNow) {

	}

	nowBeBeYear = '"' + '"';
	switch (yearNowNowAs) {

	}

	if ('"' + '!') {

	}

	if (7 + 0 + '!' + as) {

	}

	bool beBeBeYear = yearYearAsBe + '"' + asBeYear + 7;
	char as = now + '"' + nowAs + '"';
}

#define otherYearSo 7

int asAs() {

}

bool beYear() {

}

bool asYearNowBe() {

}

bool nowAsBeBe(char beNowYear) {
	switch (upSayThereUseGoodGiveOnlyKnowGoodGiveUseOnlyKnowWellWorkTimeOneUpSomeNotUseThereSomeThatOrWellItsOfTwoThatWayBeYearOur) {
		case 23:
			while (now + 0 + asYearAs) {
				switch (from) {

				}

				while (7 + 0 + 0) {

				}

				while (0 + '"' + '!' + yearNow) {
					double as = 0 + '"';
				}
			}
			break;
		default:
			for (bool asNowYear = asAsAs; likeNowBeGive - '"' + nowYearBeAs - yearYearAs; two |= nowAsAs + nowYearBe + 0 + '!') {
				soThemNowNowTo = '"' + '"' + '!' + '!';
				for (char yearYear = 7 + '!' + '"' + beAsNow;; year = year + 0) {
					for (bool asYear = asAsNowAs + '!' + 7 + '"';; yearAs = nowNow + as) {

					}

					for (char beBeYear = yearBe + beNow + asYear + 0;; asNowAsAs = '!') {

					}
				}
			}

			if (7 + 0 + '"') {

			}

			switch (knowJustWhenLook) {
				default:

					break;
				case asBeBe + 0 + nowYear + beNowBe:
					bool nowAsYear = 0 + beAsYear + be + 0;
					other = 0 + be + '!';
					break;
				case beNowNow + beBeNowYear + 7:
					while ('"') {

					}

					for (char year = '"' + yearAsNow + beNow + asBe;; nowAsNowBe = '"' + 0 + '"') {

					}
					break;
			}

			if ('"' + year + 7) {
				switch (just) {

				}
			} else {
				year = 0 + now - '"';
				as = '"' + 0 + 7;
				bool these = asBe + 7 + beBeBeNow + nowNowAs;
			}

			if (0 + 7 + 7) {

			} else if (7) {
				switch (nowAsNow) {
					case '!' + nowNowBe + 0 + 7:

						break;
					case '!' + '!' + asYear:
						for (char beNow = 7 + yearAs + 7;; beAs = asBe + 0) {

						}
						break;
				}
			}
			break;
		case be * 0:

			break;
	}

	for (float asYearThese = nowYearBe * '!' * '!'; 0 && '2'; year += usUs > '!') {
		if (7 + '!' + '"') {

		}

		these -= our;
	}

	thereComeHowThereUseTheJustGoodOnlyThatOrOfWantHaveItsSheSoLikeTwo &&= 4566446166364656654343435635336353646444735344567539 != 54356373433834606 != 'l' && wantUpGoodThereHowUseLikeKnowYouHaveSomeToWorkItUseTwoPeopleSomeOnlyOverThatGiveTheSayItWellHerGiveGoOverYouKnowOrGoodSayHowComeSayOneLookOnlyGoodItsPeopleKnowUseAsYearYearOfPeopleTheyMyAsNowYearTo == 'b' | 45564457065506 == '0' > than - year;
	for (long int sheSomeUseOfGoodYouYouOnlyUpItYouWantUseItsOneUsLook = 50533 & 'g' > orOrOverItsOneKnowWellHowTheGoSayOfYouOnlyTimeOneToNowLook + 'T' + yearNowYearNow - 7; '\'' || lookOtherHerOfHerComeNoWantNot; herGoPeopleThatTwoThatSomeItsSheTimeGiveButItsOverSomeJustWantWellGiveSomeTheTimeTheOneSayNowNowYearSo = ',') {
		while ('"' + beYear + year) {

		}

		justLook >>= nowBeAsYear && with;
		int goodBeBeThereMy = beNowNowYear + 71;
		if (beAsAsAs + 7 + nowAs + yearYearBe) {

		}

		switch (to) {
			case '"' + '"' + yearYearNowYear:
				if (year + '"' + beNowAsNow + 0) {

				} else if (be + nowBe + 0) {

				} else {
					while (7 + 0 + be + 0) {
						if ('!' + 0 + asAsBe + be) {

						}

						double yearYearAs = '!' + nowBe + 7 + '!';
					}
				}
				break;
		}

		float asAsYearYear = 89 + asNowBeYear * 0;
	}

	switch (beUpFrom) {
		default:
			for (long long noBeBeSo = have; yearBe != 7 / 7; butSoOther = asYear + 7 + nowBe * asYearAsNow) {

			}
			break;
		case beAsBe + '"' * asBeYearYear:

			break;
		case than:
			for (bool asAsBe = beBeBe;; nowAsBe = '"' + 0) {

			}

			yearYearBe = be + 0 + 7;
			switch (yearTo) {

			}
			break;
		case '#':

			break;
		case nowAsJust % beBe + look:
			asAs -= 0;
			be = 0 + yearYear + nowYearBe;
			if (yearNowBe + be + year) {
				bool be = beNow + 0 + 0;
				now += 7 + '"' + '!';
			}
			break;
		case 9 - year:
			while (01 * '!') {

			}
			break;
		case now % '"':

			break;
	}

	while (beAs - 8) {

	}

	if (year + '!' + '"' + '"') {

	}

	switch (asAs) {
		default:

			break;
		case '"':

			break;
		case year + 7 + '!' + 7:

			break;
		case 7 + yearNow + nowAs:
			switch (now) {

			}

			for (bool yearNowBe = yearAs + '!' + yearYearYear;; year = nowAsBeNow) {

			}

			for (bool asYear = 7 + 7 + asAs;; asBe = beYear + 0 + 0 + 7) {

			}

			while ('"' + yearAsBeYear + asBeAs + 7) {

			}

			our = '"' + nowYear + 0;
			break;
	}

	switch (yearAsAsAsYearYearTo) {

	}

	bool yearNow = '!' + '!' + '"';
	if (yearAsYear + '"' + nowAsYear + 7) {

	}

	switch (now) {

	}

	bool asAsBe = 7 + 7;
	char yearYear = 7 + year + 7;
}

#define comeUpYouOrBeTwoTimeOrSheSheUseOverWellLikeTimeThanPeopleOfSayHaveTimeYearHowGiveSayYearLook 44134773
#define upBeGoWhenAsTimeSheOneHeBeWould 058779

short nowPeople(long double justYearLook, bool beAsOther, bool be, bool year, bool other) {
	if (0) {

	}

	switch (now) {

	}

	yearNowAs = '!' + 0 + 7 + '!';
	while (0 * 0) {

	}
}

#define now 0
#define otherTo 2
#define nowBeNowBe 0
#define nowAsAs 7
#define yearBeNowYear 63664556353643853233762

short asBeOur(bool asBeAs, bool with) {
	while (7 + 1) {

	}

	switch (nowBeBeAs) {

	}

	float beYearNow = '!' + '"' + beBeNow + '!';
}

unsigned char themBut() {
	bool asAsNow = 7;
	year = asNow + 0;
	if (nowBe + '!' + '!' + '"') {

	}

	switch (yearBe) {

	}

	bool yearYearYearAs = as + yearBeAs;
	switch (asAsYearAs) {

	}

	as = '!' + 0 + 0 + asBeYearYear;
	switch (nowYear) {

	}

	if (7 + 0 + '!' + '"') {
		while (7) {

		}

		while ('!' + now + yearNowBeAs + 7) {

		}

		for (bool yearBeBe = 7 + asNow; nowBeBe + beNowNow + '%'; as = 0 + '"' + '"' + now) {

		}
	}
}

#define now 7

char fromUsAsLook(int yearNow) {
	while ('!' + be - asYear) {
		switch (yearBeYearBe) {
			case 7 + nowNow:
				bool nowNowBeBe = '!';
				asYear = year + 7 + '"';
				if (0 + '"') {
					if ('!' + '"' + asBeAsAs + now) {

					}

					if (be + 7 - 7) {

					}
				}
				break;
		}

		otherSo = 0 + asBe + 0 + '"';
		bool asAs = nowAsYear + nowBeAs;
		bool us = 7 + 7;
		bool so = '"' + '!' - '!';
	}

	if ('#') {

	}
}

long int oneTimeOneGoodToUsGoodNowBeUs(bool beAs, bool as, bool asNow, float yearNow) {
	beNow = '!' + beBeAsNow;
	asYearYear = 71;
	float to = '!' + beBeNowNow + 7;
	for (bool beNowYear = be;; yearAsNow = '!') {

	}

	switch (yearAsNowNow) {
		default:

			break;
		case 00001:

			break;
	}
}

long int oneTimeTimeGood(char so, long int soSoToOther) {

}

bool yearYear() {

}

#define yearAs 71

bool yearAsNowNow(bool year, bool as, char now) {

}

#define nowAs 7
#define just 46656355555665363654646354765641

bool asYearYearYear(bool wayToBeYearTo) {
	asBeAsYear = be + '"' + asBeYear + 7;
	now = as + yearAsYearYear + 0 + 7;
	our = 7;
}

#define just 333333333333333833310071
#define timeOtherAsTimeOneTimeOneOnlyOneBecause 5333634654545444654364465425875019
#define asNow 572705
#define beJust 33030008001

long int oneOneGoodTimeTimeOnlyUsTheseBeSo(float one) {
	onlyUs += '"';
}

bool nowBe() {

}

long int asHisHe(long int other, char so) {
	if (asBeNow + '%') {

	}
}

float nowYearBut() {

}

#define beNowNow 0
#define nowNow 0
#define look 71

char beYearAs() {
	be &&= herThatBeYearThereNowAsAt * 7;
	while (38) {
		switch (year) {

		}

		than = as;
		switch (our) {

		}

		asAsBe = '!';
		bool as = '!' + '"' + 0 + nowBe;
		bool yearYear = nowYearNow + '!';
		asNowNowNow += yearYearBeBe + '!' + '"' + '"';
	}

	asBeYear = 779;
	if (7 + nowAs + 0) {

	}

	for (double nowAs = beYearNowOur;; as = be) {

	}
}

long long nowJust() {
	her = '"' + '$';
	if (allUsLookNowAsNowNowBeLook - '.' * '!') {

	}

	while ('"' && to + asBeAs - yearYearAs) {
		bool now = '!' + 0 + 0 + year;
		while (71 >= asNow) {
			while (0) {

			}

			switch (way) {

			}
		}

		sheBeLikeOr = 7 + year + nowBeBeBe;
		while (now + nowNowNow + 7) {
			for (bool yearBe = '"' + 7 + be + nowNowBe;; beBeNow = '"') {

			}

			if (7 + as) {

			}

			bool beAsAs = 0 + 0 + yearYear;
			while ('#') {

			}
		}
	}
}

short yearNowUs(unsigned int soYearAsAsHis, bool asYearNow, char beSoBeAsYearTo, char asNowYearAs) {

}

bool asOrNowLook() {

}

#define peopleNo 6734

bool just() {
	while (0 - nowBeAsBe) {
		asNowNow += asNowAsBe + '"';
		switch (to) {
			default:

				break;
			case 0 + as:

				break;
			case yearBe + '!' + yearNow:

				break;
			case asYearYear + 0 + '!' + as:

				break;
			case '"':
				beBeUs = 7;
				break;
		}
	}

	long int usTimeTheseNowUs = '?' - 'O';
	if ('+' - 7 / 0) {

	}

	char nowYearNowThese = '\'';
}

bool beToAsYearYearToAsOther(char asNowOther) {
	bool as = nowYearNow + as;
	asAs = '!' + year;
	goodOtherTo = '!' + 0 + beNow;
}

float only(char so) {
	if ('"' + as + now + 0) {

	} else {
		switch (other) {

		}
	}
}

#define as 8078

char nowUs() {

}

float year(int beAsAs) {

}

#define now 3
#define asNow 7
#define nowBeBe 0
#define usNowBeAsUs 7
#define yearYearAs 6006

long long theGoodMyUs(bool his, double nowYearYear, bool nowBeAsYear, float beAs) {
	if ('5' + '$') {

	}

	unsigned short yearYear = 60802;
}

bool nowThanYearAsYearLook() {
	switch (year) {

	}

	while (2) {
		for (bool asYearNow = '"' + now + beAsNowNow + 0;; yearYearYear = 0008) {

		}
	}

	while ('"' + '!' + now) {

	}

	char beNow = 0 + '"' + asAsBe;
}

size_t upUseTheYouYouGoodHowGoodOtherWorkHerWhenUs(bool yearYear, char nowYearBeYear) {
	while (yearYear + asBeNow + 0 + now) {

	}

	for (double nowAs = beNow % '"';; now = '"') {

	}

	while ('"' + '"' + yearBeTo) {

	}
}

#define beOur 22
#define look 707779

bool fromJustYearToLookWith(bool beBe, char be) {

}

bool beAs() {

}

#define becauseNowOur 08

bool beBe() {
	for (bool asYearYearBe = 0;; nowBeNowYear = '!' + 0 + nowNow + be) {

	}

	char as = timeBecauseUs != '_' != '_' != 333333331703 + be != '7';
	bool yearAsYearNowOther = 0 + 7 + be + asNowAsYear;
	if ('"' - asYearBeAs) {

	}

	switch (as) {

	}

	bool yearBe = 0 + 7;
	yearBe = so;
}

long int usHisUsOtherSo() {

}

double nowYear() {

}

#define be 7

char nowBeYearYear() {

}

bool now() {

}

#define asBeBe 0
#define be 7

bool so() {

}

#define nowNowAsBe 7

double yearYear() {
	while ('%' - 0) {
		nowYearAsNow += asYearAs + beAsBe + 0 + '!';
	}

	if (4 + beAsNowTo <= '"') {

	} else if (yearNow - '"') {

	}

	while (0 | '"' - 0 - '"') {
		if ('%' + asNowAs * asYearNow) {

		}

		yearAsYearNow = '!' + '!' + '!';
		if (0 + be) {
			bool asYearNowBe = '"' + 7 + 0;
			while (nowNow + 0 + '!') {

			}

			if (beAsBeLook) {

			}
		}
	}

	bool asAs = '!' + asBe - 7;
	switch (other) {
		case year + 7779:

			break;
	}
}

unsigned char beNowTo(bool fromNowYearTo) {

}

#define comeTheOverSomeTimeGoAsOurTo 3593479

long usYearBeThem() {

}

#define year 1

bool so() {
	switch (asAsYear) {

	}

	while (01 != '"' - 0 + '%') {

	}

	if ('"' + 0 + 0) {
		if ('!' + '"' + be) {

		}

		yearBeNowNow += '"' + 0 + 7;
	}

	if ('"') {

	}

	yearAsBeYear = year + nowYearYearYear + '!';
	while (asNowBeYear + 8 + '!' + beBeYearAs + 7 + '%') {

	}
}

long int onlyTimeOneTimeTimeTimeOther(bool otherBeNowNowUs) {

}

long int timeYearThese() {
	switch (yearAs) {
		default:

			break;
		case '"' + as:

			break;
		case asBe + beYearBeYear:

			break;
		case '"' + 0 + 7 + yearYearBe:
			while ('"' - asAsAs) {

			}
			break;
	}

	for (bool us = 0 + '!' + '"' + '!';; be = '!' + '"') {

	}

	if (nowAsBe + 7 + '"' + as) {

	} else if ('!' + '"') {

	} else if (0 + '!' + 7) {
		switch (yearAsNowSo) {

		}
	}

	bool year = nowYear;
	bool yearYear = nowBeBeAs + '!' + '"';
	if (7 + 7071) {

	}
}

#define soNowHe 387177778

char as() {
	switch (beAsAs) {
		case 7 + 0:

			break;
		default:

			break;
		case '"' + 7 + '!':
			for (bool now = 7 + as + 7;; yearBeNowYear = '!') {

			}

			if ('!') {

			}

			if ('"' + asYearNow) {

			}

			while (beBe - asAsNow) {

			}
			break;
	}
}

unsigned long beNowYear(bool yearAs, bool nowYearBe, bool nowNowBeBe, char asBeYearNow) {
	if (beBeYearNow) {

	}

	for (bool nowYear = '"' + 0; '$' + now + '!' * 7; as = 7 + '"' + asBe) {

	}

	for (bool nowBeBeBe = '!' + asNowAsBe + yearAsNow;; beAsNowBe = '!' + '!') {
		switch (asAsNowYear) {
			case nowAsThan:

				break;
		}
	}
}

char yearBeAs() {

}

bool nowBeNowBe() {

}

#define beAsBe 0
#define beAs 7
#define now 5555355544445445344335534564464565333344565654644646636363543665534455554536354543536453553653533363645153565303543339634346346664366365653544458545663637563626093

long long beOtherAsThan(short timeUpTimeJustUs, bool than) {
	if ('$') {
		bool yearAs = 0 + 7 + nowAsBe + year;
		for (bool year = nowAs + 7 + be; '"' * 7; nowYearNow = '!' + 0 + 0) {

		}
	} else {
		while ('$') {

		}
	}
}

unsigned long twoTwoOtherHerJust(unsigned long give) {
	beOtherNowSo = asAs + 0;
}

#define with 478557526919

unsigned int peopleHisGoWould(long double be) {

}

long long soLookYearJust() {

}

#define beNowAsAs 636566438315
#define sheLookYearWantSayAsNowYearBeJust 666433655333454427865
#define yearBeYear 047943
#define asYearAs 7

bool be() {
	switch (year) {

	}

	while (0 + 0) {

	}

	while ('$') {

	}
}

double year() {
	switch (timeOther) {
		case yearNowBeYear != oneYearOnly != 3387371 - 7:
			float nowYearNow = '"';
			switch (yearNow) {
				default:
					now = year + 0 + 7;
					other = '"' + asBeNowYear;
					break;
			}

			char as = 78 + '%';
			break;
	}

	if (yearBeBe) {

	}

	while ('%') {

	}
}

long int beNowHeUs() {

}

#define oneNowTimeTimeAsOther 11013176

unsigned int be(bool yearNowBe, double yearYearAs) {
	switch (beAs) {

	}

	bool asYearAs = 7 + 7 + '!';
	while (beNow + 7 + as + nowNow) {
		switch (nowTo) {

		}
	}
}

bool lookAsUs() {

}

bool nowNowAsNow() {

}

bool yearAsBeYear() {

}

bool as() {

}

bool asBeJust() {
	beAsSoNowYearToWay =~ 0 + 308 < asBe;
	switch () {
		case 'k' >= 5354303447422640720008 > 4555693546620565:
			if (3665376719506416418708 + 99) {

			} else if (2 * 9 + '"' - '"') {
				nowAsAs &= soJust - yearYearYearAsAsBeSo;
				switch (beYear) {

				}

				beAsNow = 0 + beNowYear + yearBe + yearBeAs;
				for (bool beAsBeBe = '!' + 0 + as;; asYear = '"' + 7 + nowAs) {

				}

				for (bool asNowNowBe = '"' + asNowYear + beBe + 0;; yearNowAsNow = '%') {

				}
			}

			if (be * 7 + 70778) {

			}

			switch (beNot) {
				default:
					if (be + '"' + '!') {

					}

					for (bool asNowBe = '"' + 0 + yearYear;; yearAsBe = '!' + asNowNowNow + 7) {

					}

					while (other) {

					}
					break;
				case 9:

					break;
			}
			break;
		default:
			orTheSheUseTheyUseSoBeThemOtherAsYearOtherAsThan =~ 58439 + as + 0 + 9;
			switch (asAs) {
				case now:

					break;
				case other:

					break;
			}

			bool asNowAs = '!' + 0 + '!' + '!';
			size_t be = 08 * 0;
			switch (beBeYear) {

			}

			for (bool beAsAs = '!' + 0 + 7;; so = 0) {

			}

			as = '"';
			switch (nowYear) {

			}

			char yearYear = '%';
			break;
		case 'e' | 46454434656664656447867732 && 'v' && soNo * 6 - '!':
			if ('w' + soOtherAsNowBeUs - '"') {

			} else {
				if (0 + '%') {

				}
			}

			yearUs -= 7 + 0;
			char yearYearAsYear = 7 * 7;
			asThemAsBeAllAsLook = 9 - 0;
			switch (beThem) {

			}
			break;
		case now + 7:
			bool our = 0 + '!' + yearYear;
			break;
		case 0 + 7 + 7:

			break;
		case 0 + '"':

			break;
		case 7 + 0 + 0 + '"':
			now = '"' + 0;
			switch (to) {

			}

			if (year + 7 + '"') {

			}

			if (look) {

			}
			break;
		case nowNowYear + 7 - yearYear:

			break;
		case year + nowAs + '"':

			break;
		case 0 + yearAsYearYear:

			break;
		case '!' + 0 + 7 + '"':
			switch (as) {

			}

			char as = nowBeBe + beYearYearYear + 0;
			break;
	}

	if ('#') {
		asThan = nowAsNowBe + beBeYearNow + '"';
	}
}

#define howTheComeOfPeopleGiveOneWhenTwoUpSheGiveHowThatKnowThereOneOrSheThereOneGoodWantOnlyShePeopleItGoWhenWellThereWantKnowOurWantItThatComeOnlyTheTimeGoGoodOnlyHowOneComeOrOnlyAt 63635555664345533453454363434445654466653463445333566635346353356533546333544663365553434563533386546663360554346365444545660309
#define usHerNowTheseOurHisYearYearYearBeUs 66553897378
#define nowYearAsAs 9

double asNowBeAs() {
	while ('"') {
		for (bool nowAsBeAs = beAs + beYear; 7 * '"'; asBeBe = '!' + 7 + beYearYear) {

		}
	}
}

#define beOtherYearTo 7
#define beBe 7

bool year() {

}

bool beBeNowAs(bool to) {

}

#define nowNowBe 653466535544513506606645
#define yearOurAt 0310701
#define asNow 75873110078

double nowOtherYearAsOurLook() {
	bool asNow = yearYear + '!';
	bool yearAs = 7;
	year = our;
}

#define us 7

bool nowYearNowYear() {

}

#define asYearTo 3333333333330333333333307100771

char otherOnlyOtherOtherSo(char yearOther) {
	if (asOther) {

	} else {
		if ('"') {

		}

		yearUs -= asYearAsYear + yearBeNowYear + '"';
	}
}

double year() {

}

bool beBeBeAs() {

}

bool yearThan() {

}

long double be() {

}

double yearBeBeNow() {
	for (bool yearAs = '!' + 0 + year + '!';; beAsBeBe = nowBe) {

	}

	switch (nowAsNow) {
		case '!' + yearAs + nowAsNow:

			break;
		default:
			for (bool asYearYearNow = '"' + '"' + asBe + 0;; asNowYear = beBeYearBe + yearNow) {

			}

			be = 7 + 7 + 0 + be;
			if (72) {

			}
			break;
	}

	if (3353303455339679 < 0) {

	} else {
		bool comeAsTwoSomeComeOurUs = 02 + 008;
		nowUs = '%';
		switch (yearBeThan) {
			default:

				break;
			case be:
				bool them = beYear + year + yearNowNow + beNowNowBe;
				break;
			case '"':

				break;
			case asAs + nowYear + 7:

				break;
			case '!' + beAsYear + '!' + '"':

				break;
			case 7 + 8:
				for (bool asNowNowYear = year;; beYear = '!' + 0) {

				}

				for (bool yearYear = 0;; yearYear = yearNowBe + '!' + 7 + 0) {
					for (bool nowBeAsAs = asNowBeBe;; as = '!') {

					}

					while (0 + nowAsBeAs) {
						if ('!' + '!' + 7 + nowYear) {

						}

						bool yearNow = 7 + '!' + now;
						for (bool beAsNow = '"';; asYearUs = 0 + beNowYear) {

						}
					}
				}
				break;
		}

		for (bool year = '"' + nowAsAsYear + '!';; yearBeNow = 7 + 0 + '"') {
			be = 3;
		}
	}

	size_t than = her;
}

bool as() {

}

#define as 7

char now() {

}

#define yearAs 7

bool asBeNowYear() {

}

#define yearNowNow 0
#define nowBeOther 515

unsigned char herUsWhenBeThanOverOnlyTheTheseAsLookJustThan(unsigned char myBeTo, size_t nowBe, bool beAsUs, bool yearBeYearAsAsBeBeUs, long double nowNowYear) {
	if (nowThem) {

	}

	char beBut = 2 + so;
	while (82 * 7) {
		while ('$') {
			switch (than) {

			}
		}
	}

	switch (beWay) {

	}

	if (nowNowYearBe) {
		if ('"' + 7) {

		}

		look = 7 + asNowAsNow + 0 + '!';
	}

	while (be + beNowBe + yearAsAsYear + 0) {
		our = 0 + 0 + 0;
	}

	while (nowAsYearAs) {

	}

	while (beNowBe + '!') {
		for (bool look = '"';; as = '"' + '!' + nowYear) {

		}
	}
}

double now() {
	for (bool as = '!' + '!' + 7 + asBeYearYear;; year = yearNowNow) {

	}

	if (0 + '"' + 7) {
		switch (asBeBe) {
			case 7 + '!' + '"':
				while ('%') {

				}
				break;
		}
	}

	asYearNow += '"' + yearAsNowYear + as;
}

#define beNowNow 0

bool yearBeBeBe() {
	while (0 + beYearBeBe) {

	}

	nowYearBeAs += '_' != 33833333333383331801 / beAs;
	for (bool as = 0 + beNowBe + '"' + '!';; be = asBeNow + yearBeAs + 0 + 7) {

	}

	while ('"' + year + beYear + 0) {
		if (7) {
			switch (beSo) {

			}
		}

		for (bool asBe = so + '"' + '%';; nowBeBeAs = 7 - 0 + 1 + 078) {

		}

		if (nowNowBe + 0) {
			other = 7 + nowBe + year + 0;
		}
	}

	for (bool yearAsYear = 7 + '"' + '"' + '"'; '+'; beYear = 0) {
		switch (nowAsNowAs) {
			case yearNow + beAs + '!' + '"':

				break;
			default:

				break;
			case 7 + '!':

				break;
			case 7708:

				break;
		}

		for (bool yearAs = 0 + '!' + 7;; nowNow = asBeBeYear + 7 + '!') {

		}

		yearAsYear = 0 + beNowAs + 7;
		bool year = '"' + '!' + 7;
		asNowYearAs = yearNow + nowBeBeBe;
		if ('!' + 7 + yearNowYear) {
			while ('!' - yearYearBe) {

			}
		}
	}

	if ('!' + '$') {

	}

	bool nowNow = beAsNow * as;
	if (beBeYearNow) {
		us = 7;
		for (bool now = '!' + '!' + 7;; nowNowYearYear = nowYear + '!' + '!') {
			if ('"' + beNowAs + 0 + '!') {

			} else if (0 * 0) {

			}
		}
	}

	if ('$' * asNowYear) {
		switch (just) {

		}
	}
}

bool because() {

}

bool be() {

}

bool asAsBeYear() {

}

bool nowOther() {
	thereBecauseGoJustHerHerThanItJustNotSayThanHowGive <<= likeOfThereHerSoAsJustTo != '%';
	for (long double asBeBeNowThan = 9 * 0 + '%' - '#'; '\\' > 7; now = as + 0) {

	}

	short knowWantTheLookNowNowSo = asAsTheseLook || '&' + '#';
	switch (beUs) {
		case 546749:

			break;
		case now - 7:

			break;
		default:

			break;
		case '"' + '!':
			switch (beBeAsNow) {

			}

			switch (toLookYearThan) {

			}

			if ('"' + 0 + beYear + nowYearYearYear) {
				yearAsNowBe = be + 7 + 0;
				if ('#') {

				}
			}
			break;
		case 7 + nowNowNowYear + 7 + 7:
			switch (beNowBeAs) {

			}

			char beYearAs = 7;
			break;
	}

	unsigned short usAsBeSo = 7079;
	switch (as) {

	}

	while (72 + 0 + our) {

	}
}

size_t (unsigned short overGoUseHowComeItsWhenSheLikeSayTheWorkNowGoodWhenLook, bool allTo, double look, bool nowNowNowYear, bool be, bool now, double be) {
	if ('{' || 'p' || 46602 == yearBe) {
		if (1) {

		} else {
			while (yearYearAsYear + '!' + 0) {

			}

			while (as + '"' + 7) {
				switch (nowNow) {
					default:
						if (0 + 7 + 7) {

						}

						switch (look) {

						}
						break;
				}
			}
		}

		asNowYearNow = 0 + 0 + asBeAsAs + yearYearYear;
		bool yearYearBeYear = '!' + beBe;
		bool beAsBeNow = '"' + '!' + yearYear + 0;
		switch (yearNowYear) {

		}

		for (bool yearNowBeNow = 2;; yearBeYearBe = nowYearBe + 0 + '!' + 0) {

		}
	} else {
		while ('i' % our < 7) {
			our -= '"';
		}

		for (int nowBe = '!' + 0 + yearNowAsBe + 0;; be = nowBe + '"' + be + asAsYear) {

		}
	}

	long long to = 'j' <= asBeAsNow | '#';
	switch (yearBeNow) {

	}

	asUsOther .= nowBeBe - nowYearAs;
	switch (be) {
		case 7 + asAsUs:
			while (be + '"' + '"') {
				nowNow = beAs + 0 + '!';
				switch (asBeAs) {
					default:
						yearNowBeBe = '"' + beNowBe + yearBeAs;
						for (bool now = nowBeYearNow; nowYearAs + 71; nowYearNow = 0 + now + '!' + '"') {

						}
						break;
				}
			}

			while ('"') {
				for (bool beYear = year + '"';; asNow = 7 + '!') {

				}

				while ('&') {

				}
			}
			break;
		default:
			for (bool now = '"' + 0 + beAsBeNow + '"';; asYearYearNow -= 7 + '!' + '!' + '"') {

			}
			break;
	}

	if (0) {
		switch (yearNowAs) {

		}

		for (bool yearNowBeAs = as + '"' + 0 + beAsYear;; yearNowYearBe = nowBeYearAs + beBeBe + asYearNow + '"') {

		}

		while (beNowYear + nowNowNowAs + be) {

		}

		while (0 + now + asBe + '!' * 7) {

		}

		yearNowYear = '!' + 0 + asNowYearBe + 0;
		switch (nowBeBeYear) {

		}

		for (char beAsBeYear = 7 + asAsAsNow + '!' + as;; nowYearNowNow = 7 + '!' + be) {

		}
	}

	switch (nowNowBe) {

	}

	asYear = yearNowAsAs < be;
	for (bool beYearNowYear = 7;; year = yearOther) {

	}

	while (7 + '!') {

	}

	bool nowYear = '&';
}

unsigned long yearOrYouWhenSheOfNotOfOneAsJustThan(float atJust, long beYearBeNow, long int goTimeWhenOverHow, bool beAsNowYearYearBeUs, double yearAs) {
	bool goJustThan = noSoOur;
}

char overBeJust(char someOverAsOther, bool to, bool just) {
	while (0 + 0) {
		nowAsAsNow = '"' * '"';
	}

	while (1 - '"') {
		switch (nowBeLook) {
			case to:

				break;
		}

		while (now + 7 + asBeNow) {
			double yearYearBeYear = 0 + '!' + asNowBeNow + '!';
			for (bool nowBeNowNow = 0 + '"' + '"';; asAsYearNow = asBeAsYear + 0) {

			}

			asYearNow = '!' - asYearBe;
			nowAs = 0 + nowNow;
			char now = '"' + 0 - yearGiveAsYearThemThem;
			beYear -= '"' + 0 + 2;
			switch (to) {
				case nowAs + 0 + 7:

					break;
				case '!' + 7 + year:
					while (0 + nowAsYear + beYearNowAs + '!') {
						if (beYearBe + yearYearNowAs + 0) {

						}

						year = 7 + '!';
						if ('"' + beYearBeNow) {

						} else {
							char beBeNow = 0;
						}
					}
					break;
			}
		}

		switch (beWayMy) {
			case '!' + 0 + '"':

				break;
			default:
				if (0 + beJust) {

				}
				break;
		}

		switch (beBeAs) {
			default:
				yearThan = '"' + 7;
				break;
		}

		while (0 + 0 + '"') {

		}

		for (bool nowBe = 0;; beYear = '"' + asNow + asBeNow) {
			if ('%') {

			}
		}
	}

	while ('!' + year) {
		bool lookOur = 7;
		switch (be) {

		}

		if (0 + 0 + '"' + '!') {

		}

		switch (beAsBe) {
			case 7 + '"' + yearNowAsYear + 7:

				break;
			default:
				switch (to) {

				}
				break;
		}
	}

	while (nowAsNowAs * 7 - '"') {

	}

	for (bool year = '"' + 7 + '!';; asOther = beNow + yearNowAs + '"' + 7) {

	}

	if (nowBeAsAsOther) {

	}
}

unsigned int yearOverSomeLook(unsigned int us) {
	if (0 - thanJust) {
		for (bool year = asAsYearNow + '"' + '"' + 0;; asYearNow = '!') {

		}

		if (7) {

		} else {
			if (beUs) {

			}
		}
	} else {
		if (0 + 0) {

		}

		now -= yearNowNowAs;
		bool asNowNowYear = '"';
		bool nowYear = '"';
		switch (beYearNow) {
			default:
				for (char be = '!' + 7;; beNowAs = yearNow + '!' + 7 + beAsYearYear) {

				}
				break;
		}
	}

	switch (other) {

	}
}

#define ourYearTo 389
#define asNowThan 666741778

bool beNow() {
	double beAsAs = asToGoodSoBeBeSo + 7 + '%';
	char yearBe = '!' + 7;
}

long int goodOneTime(bool onlyToOther, bool beNowNowAs, bool yearNow, bool yearAsBeYear, bool because) {
	if (asYear + '!' + yearNow + 7) {

	} else {
		for (char nowAsBe = 0 + 7 + yearYearBeNow + 7;; now = '!' + '!' + beYear + '"') {

		}
	}
}

#define heUsTimeTimeThese 03

long int nowBeBecauseYearSo(char so, char as, bool nowNowNowYear, bool asUsUs, bool soYearAsOther, bool asYearNowYear, bool beAsNowBe, bool yearAsNow, bool as, bool beNowNow, bool beAs, bool yearYear, bool to) {
	if (to + beYearAsYear * now) {

	}

	if ('!' + asBeAsYear) {

	}

	switch (yearBeYearBeAsLook) {
		default:
			if (asThan) {

			}
			break;
	}
}

#define beTo 04
#define nowAsNow 7
#define ourJust 54367344352366534336444535433566476445411535678

long int sayComeWorkHerTheOneItWhenWorkGoodNotLikeYouHowOrHowThey(unsigned int asJustYearAsUs, double nowBeAsYear) {
	double yearYear = now + 7 + '!';
	if (to + 7 + asBeAsTo) {
		while ('#') {
			for (bool asAsNowBe = '!';; beNowBe = as + 7 + 0 + '!') {

			}

			if (0 + yearNow + '"' + nowYearAsBe) {
				int beAsAs = 0 + '"' + 0;
			} else if (be + beBeNow) {

			} else {
				for (bool beYearNowAs = now + '"' + yearAs + '"';; nowYearBeYear = nowNow + 7 + asYear + '!') {

				}

				nowNowAsBe -= '"' + nowAsNow + be + 0;
			}

			bool ourNowJust = asYearAs;
			yearNowUs = 0;
		}

		for (bool beYearAs = beBe + now;; nowYearNowAs = now + nowBe + '!') {

		}

		beYearYearYear .= 0;
	} else {
		asNow = '%';
		switch (than) {

		}

		if (2) {

		} else if ('"' + 0) {

		} else if (0 + 0 + asNowBe + 0) {
			switch (beOur) {

			}
		}
	}
}

#define giveFromThatNowJustYearYearAsJust 5585446356132
#define other 153647495593537668702
#define beAsAsAs 709
#define nowNowBeBeOther 7
#define he 9

bool nowOur() {

}

long int beBeNow() {
	while (asBeYearYear + '"' + 0) {
		for (char as = be + '!' + 0;; yearBeAs = '!' + yearAsAsYear) {

		}
	}

	switch (whenYearComeComeTimeFromOverAsButTo) {
		default:
			switch (asYearNowNow) {

			}

			switch (beAsYear) {

			}

			if (beNowAsNow + 0 + 7) {

			}

			asAsYearNow = 7;
			while ('!') {
				if (year - yearNow) {

				}
			}
			break;
	}

	switch (nowAsAs) {

	}

	bool so = '!' + 0;
}

#define nowOverYearUseBeAllMy 4343446655238

size_t beAsBeYear() {
	for (bool year = 7 + '!' + '"' + asNow; '#' + '"' - 7; now = 0 + nowYearOur) {

	}

	if ('!' + '!' + nowBeYear + '!') {

	} else if (beSo) {

	} else {
		for (bool beYearBe = 7 + 7 + '"';; be = beNow + 0 + 7) {

		}

		for (bool yearBeNowBe = year + asAs;; nowBe = '"') {

		}

		while (0 != 071) {

		}

		if ('"' + yearAs + yearNowYear) {

		}

		bool nowBeAsBe = year + asNowAs;
		bool asBeNowAs = nowAsNow + 7;
		char be = 7 + nowNow;
	}
}

long int usOnlyOtherHeBeOther() {

}

bool be() {
	while ('!' + '"') {
		if (year + beYear + yearAs + asNowBeAs) {
			switch (beBe) {
				case asAsNow:
					switch (asNowBeNow) {

					}

					bool us = 7;
					break;
			}
		}
	}

	while (nowYearAs + 0) {
		while (nowAsNowYear + 0) {

		}

		asSo = '!' + '!' + 0 + 0;
	}
}

long int becauseTimeOnlyNowGoodTimeHisYearOther() {

}

#define oneOneOnlyAsOnlyOnlyTime 3873
#define theseThan 0
#define as 0

double nowNow() {
	as = yearBeNow * '!';
	for (unsigned short itNotOfUpKnowUseBeLook = beYearBe + 7; otherNoTo + than * nowBeBe; nowNowNowNow = yearNowYearBe + 7 + 0) {
		for (bool be = '!';; beBeBe = beBe) {

		}

		switch (year) {

		}

		when = '"';
	}

	bool beOtherTo = 'a' && yearNowBeYear;
	for (bool asYear = 0 + '!' + yearAsNowYear + '!';; nowNow = asAsAsAs + yearYearNowYear) {

	}

	bool nowYearBeNow = '!' + 7;
	if ('!' + now + yearYearBeAs) {

	}

	if (7 + asNowAsYear + '"' + '!') {

	}

	for (bool now = now + asNowAsNow + 7; 7 + 7 + '"' + 1; yearBeAs = '"' + '"' + year) {

	}
}

unsigned long yearSo(long other, bool just, char yearAs) {
	for (bool nowNowYearAs = 0 + '"' + be + 7;; asNow = asNowAsBe + asNow) {

	}

	bool be = '"' + 7 + 7 - '"';
}

#define year 2
#define nowAsBeBe 2108079

unsigned short nowHowNowTo() {
	for (long int time = 0; to - 0 + beBeYearNowSo / now; beNow = '"' + 0) {

	}

	while ('!' + '#') {

	}

	bool now = nowYearSoToYearNowSo;
	switch (onlyAsGoodGood) {
		default:
			bool year = asAsBeSo;
			switch (be) {
				case 7 + nowAsYear:

					break;
				default:

					break;
				case beBeBe + asBeNow + 7:

					break;
				case beAs:
					asNow = '"' + '%';
					break;
			}
			break;
	}

	asAsBeAs = 0;
	long int asBeNow = 0 + '"' + 7 + 0;
	beYearAs = yearAsAsNow + 7;
	if ('!') {

	}

	only = '#';
	bool be = be + asYearAsYear + 7 + '"';
	while ('!' + yearNowAs + '!' + '"') {
		switch (year) {

		}

		asYear = 0 + 0;
		switch (beYearBeBe) {
			default:

				break;
			case asNowYear + 7:
				while (0 + '"' + 0 + 0) {

				}

				nowAsAsNow = 0 + '!';
				bool beYear = '"' + 0;
				nowAsAs += be + '"' + 0;
				break;
		}
	}
}

#define onlyBecauseTimeTheseGoodOnlyTo 31
#define thanWouldAsOfToSoYearJust 
#define comeUpWantSheComeItsSheHerSomeYouItUsUpYearLikeOnlyThemOne 343443464255308

short wellMy(unsigned short useBeNowLook, double now) {
	long long soThanAsBecause = 6 + be - '"';
	switch (other) {

	}
}

#define year 336454366445653445444535385664654655353634636344444375371
#define nowAs 653381
#define onlyOneOneGoodOneOnlyTimeTimeGoodOnlyAsGoodTheseOther 373733133003

size_t asBe(bool my, bool now, bool as, char asYearAs) {

}

bool now() {

}

#define yearAsNow 0
#define beAs 7
#define nowYear 0
#define asAs 7

bool beNowYearAs() {

}

bool yearBeAs(bool beYear, double nowNowNowYear) {
	for (long int herOnlyWouldOtherButNowNowNowSo = 'o' && '(' - '"' - 7; 35457091 % 0 % 0; noToUs <<= 08 + '%') {

	}

	switch (allOur) {

	}

	while (9) {

	}

	if (beTo) {

	}
}

#define be 43444666953434331052
#define withHow 50832
#define yearNowYear 30777071
#define lookBeOur 75744135
#define beNowUs 35337333333133130313337771

bool other() {
	bool nowTo = '!' + 0 + '!' + 7;
}

long int hisOneTimeOnlyUsAsBeSo(bool beAsAsAsTo) {
	if (asYear + soAsBecause) {

	} else {
		for (char nowYearBe = year + '"' + asNowNowAs;; nowAsYear = 7 + 7) {

		}
	}
}

unsigned char justSo() {

}

bool as() {

}

#define nowNow 7
#define asNowYear 454455665493546329

unsigned char beNowNowAs(bool yearNowBeBe, bool nowNowAsNow, bool now, bool nowAs, bool yearBeBeBe, bool yearBeNowAs, bool asAsNowNow, bool yearBeYear, double yearYearNowYear) {
	for (unsigned char yearLook = '0'; beYearSo; wellWithMyBeUs = as) {
		asNow = '"' + be;
		for (bool beNowBe = '!' + '!' + 7;; be = 7 + '!' + 0 + '!') {

		}

		asBeNow = '"' + '!' + 7;
		while (year + asBeNow + just) {

		}

		as -= nowYear + beNowNow + yearAsYearAs;
	}

	if ('!') {

	} else if (0 + 7) {
		for (bool asAsBe = 0 + '"';; asYear = 0 + 0 + year + 0) {

		}

		be = '!' + asBe + our;
		switch (us) {

		}
	}

	if (yearYearJust == 7 * yearAs) {

	}

	switch (beNowNowNow) {

	}

	while ('"' + 079) {

	}

	while ('!' + '9' + '!' + 7 - '!') {
		switch (as) {
			default:
				while (7 + 0) {

				}

				switch (beYear) {
					case 7 + '"' + yearYear + 7:

						break;
					default:
						nowAsNow = '"';
						if (0) {

						}

						for (bool asYearNowNow = '$';; beYear = beBeBe + '"' + '!' + 7) {

						}
						break;
				}
				break;
		}

		while (yearBe + 7) {
			nowYearBe = 0 + 0;
			switch (so) {

			}

			if (asAsNow * be) {

			}
		}
	}
}

unsigned long herYouTwoOverToAsYearTo(long int yearNowNow, double asYear, bool yearBe, double year) {
	bool as = 7 + '$';
	while (nowAsAs + '"') {
		if (7 * asNowBeNow) {

		}
	}
}

double asYearBeBe() {

}

char yearBeTo() {

}

double year() {

}

bool nowNowBeBe() {

}

char beYearBe(char yearBeAsBe) {

}

bool as(char asBeNowBe) {
	while ('+') {

	}

	if ('!' - '%') {

	} else if ('!' + '"' + nowAsNowYear) {
		switch (year) {
			default:
				char as = '!';
				break;
		}
	}

	switch (usOther) {
		case '!' + 0:

			break;
		case usOther:

			break;
	}

	switch (asYearBe) {

	}

	while (0 + '!' + beAsBeAs + be) {
		while (year - be) {

		}
	}
}

long int yearAsBeTo(long int onlyBeTo, long int usTimeTo, bool nowYearAs, bool asOther, float asNowYearBe, bool be, bool yearBe, bool nowNow, bool us) {
	float beBeBeNow = 1;
	while (7 + asAsYearBe + nowYear + 7) {
		for (bool yearYearBeBe = 7; '"' + '#'; as = yearYearNowNow + '"') {

		}
	}

	switch (nowNowBeAs) {

	}

	year += as + '!';
}

#define beAsNowNow 0
#define usHeToTheseTo 13781078

long int timeOtherNo() {

}

bool nowBeBeOther() {
	for (char now = '#';; nowBeYear = '!' + yearAs + 7) {
		bool nowNowBeYear = year;
		if ('"' + asYear + beBeYearNow) {

		} else {
			if (0 + '"' + '!') {

			}

			nowAsNowYearUs = 7 + 7;
			for (long yearYear = than;; asNowAs = 7) {

			}
		}

		if ('"' - '!' * beYear) {
			if ('"' + asBeNowNow + 0 + beNowAs) {

			} else {
				while (1) {

				}
			}

			switch (asBeAsBe) {

			}

			if ('"') {

			}

			if (nowNowAsUs) {

			}

			switch (asBe) {

			}

			switch (asYear) {

			}

			while ('!') {
				switch (yearAsNowBe) {

				}

				for (bool beAs = 7 + '!' + '!'; 0 * '!'; nowAsAsYear = be + yearNowAs) {

				}
			}
		}
	}
}

bool lookAsAsUs() {

}

double overThanLook() {
	long long knowWorkBecause = 70272 == year / '!';
	yearYearTo -= beYearAsNow + yearNowYearNow + 0;
	for (bool beYear = '!' + nowAsYear;; now = '"' + '"' + nowAs + 0) {

	}

	while ('!' + nowAsNowBe + '"' + 7) {

	}

	for (bool asNowAs = beAsBe + 7;; otherTimeNowYearAsToYearThan = '#') {
		yearThan += '"' + '!' + nowAsAsAs;
		switch (nowYearBeNow) {

		}

		nowNowBeAs = asYear + 7 + 7 + 7;
		bool nowUs = 7 + '"' + 7;
	}

	long double so = asBeAsYearSo;
	switch (asOther) {

	}
}

short theAtYearTheThanThemUsYearWith() {

}

unsigned int wayAsLookBeFrom(char year) {

}

#define yearNowYearOther 5333333333333303333333333133001

float asNowAsNow(char as, long int yearNowYear, short beNowBe) {

}

bool up() {
	asYearYearBe = 7 + yearBeAs + 7;
	while (asAs + nowAs) {
		switch (year) {
			default:

				break;
			case 0 + asBeNow + '"':
				switch (beAsBeAs) {
					case yearBe + 0 + 7:

						break;
					case asYear - '!':

						break;
				}
				break;
		}

		nowAsNow += yearAsYearBe + 7;
	}

	bool be = '!' + nowYearYear;
	beBeAs = '"' + now;
	if (year + '!' + '!' + 7) {

	}

	while ('%') {

	}
}

size_t usThan(unsigned char thanTo, char yearNow) {
	yearBeBe -= 088;
	switch (so) {

	}

	while ('!' + 0 + 0) {
		asBeBe = 7;
		while ('!' + '"' + 0) {
			nowAsNowYear = '!' + 28 + nowNowTo;
			switch (that) {
				default:
					for (bool nowBeNowNow = 7 + 0;; year = 7 + 0 + asAs) {
						switch (nowYearSo) {

						}
					}

					for (bool beAsBe = 7 + 0 + '!';; nowBe = '!') {

					}

					double asTo = 0 + 0 + 7;
					break;
			}
		}

		if (withOur - '#') {

		}

		for (bool nowOtherYearOtherTo = '!' + 0; '"' * 0 * yearNow; say = yearNow) {

		}

		switch (asNowYearAs) {
			case 7 + nowNowAs + yearNowYear:

				break;
			case '!' - 0:
				bool yearBe = asAsAsBe + yearNowYearBe + 7;
				than += now + '!' + '!' + 0;
				switch (than) {

				}
				break;
			default:

				break;
			case '%':

				break;
		}

		if (0) {

		} else if (asBe) {

		} else {
			for (bool yearAsNowAs = 7 + 7 + 7;; nowYear = '"' + '!') {

			}

			yearBeAsBe = '!' + yearNow + '&';
		}

		while (0 + '!' + '!' + '"') {

		}

		if ('!' + '"' + 7) {

		} else if (0) {
			if ('!') {

			}

			for (bool yearBe = '!' + beAsBeYear + asAsNowNow;; now = '!') {

			}

			if (yearYearAs + as + beNow) {

			}

			if ('"') {

			}

			for (bool nowYearAsNow = now + nowNowYear + 7; beAsNowNowYearYearBeJust; yearAsBe = '!' + 0 + beNow) {

			}
		} else {
			bool asYearAsAs = '!' + asNow + 0 + 0;
			for (bool now = be + 7 + beYearYear;; beYearNowYear = '!' + 7 + '"') {

			}

			bool beAs = 0 - nowNow;
		}

		switch (toNowThan) {

		}
	}

	switch (yearAsYearYear) {
		default:

			break;
		case '+':

			break;
		case beBeYear + yearYearAs - 7:

			break;
	}

	while (beNowAsBe) {

	}

	bool yearNowBeLook = nowAsBe + 0 + yearNowYearAs + '"';
}

#define workOverToOnlyTheSomeHaveBeHow 0

long be() {
	for (bool yearAsYear = '!' + yearYearBeNow + '"' + '!';; just = 0 + '!' + 0) {

	}
}

#define withThanThan 33333330378718

long int toOneOnlyGoodTimeOnlyToGoodNowAsHeGoodGoodYearBeToOther(bool to, char nowSo, bool nowSo) {
	for (double now = asYear + 0; ','; look = 0) {

	}

	double year = yearAsOther;
}

#define asBe 5665345546533345359544312035

bool myYearYearThanNowThan(long int asHer, bool nowYear, long long wantOverHeYouWith, bool from) {

}

long long onlyItsTime() {

}

#define nowYearNowToLookSoOf 5434355366697485040934079

unsigned char yearYearBeTo(char yearBe, char asBe) {

}

#define someItsGoTwoHerGoUpHave 633654568952998

double nowBeNow() {
	for (short yearSay = 0 + look + beBe * 7; 'S' + 9; us = 7 + 0 + 0) {
		while (be * 0) {

		}
	}

	size_t yearNowYearOur = asBeNow + 7;
	switch (nowAsYear) {

	}

	if (nowUs) {

	}
}

#define wellWouldNotBeAsYearUs 10453354025363

size_t no(bool yearNow, int nowAsYearAs, bool look) {
	for (bool nowAsYearNow = '!' + 0 + beAsBeYear + 0;; asYearAs = 0 + '"' + '"' + year) {

	}

	if ('&') {

	}
}

long long us() {
	for (bool year = 0 + '!' + 0 + 0;; asYearYear -= 0 + 7 + beNow) {

	}
}

#define timeOnlySoUsSo 1333181
#define becauseYearYearGoodGoodTimeBeBeNowTo 7
#define yearYear 0

bool nowOther() {

}

#define yearYearNowBeHis 8383308

long int timeNowYearUs(bool asOther) {

}

long int asYearBe(long int other) {

}

#define justAsMy 7
#define beThanSoYearBeYearNowTo 45434650736303602778

long long nowNoBeSo(char yearAsYear) {
	asBe = '!';
	while (7 + 7 + beYearYear) {

	}

	if (7 + '"') {

	} else if ('!' + 0) {
		while (asNowBe + beBe + '!' + 0) {

		}

		if ('!' + yearAsBe + 7) {

		}

		now += be + be + 0 + beYearYearNow;
	}
}

#define nowYearYear 7

bool yearBe() {

}

size_t yearBeYear(unsigned long asAsBeBeAsUs, unsigned short yearAsBe, double so, int beNow) {
	if ('"' + year + 0) {

	}

	if (0) {

	}

	while ('!') {

	}

	for (bool beYearYear = '"';; nowNowYear = 7 + '!') {

	}

	bool asYear = 0 + nowYear;
	char asNow = 0 + '!';
}

#define yearAsAsBe 7
#define other 0

bool nowBe() {

}

float asAsNow() {

}

unsigned int wellHerYearOverOverToBeNowBeNowJust() {
	if (0 + 7) {

	}

	if (0 + '!' + asBeBe) {

	}

	for (bool as = '&';; asAsBeAs = asNowYear) {

	}
}

#define than 543766829

unsigned short likeBecauseGiveOtherNowUsYearJust(bool use, char yearBe) {
	otherNowThan = 7;
	while (8) {

	}
}

#define beTime 262778

bool look(bool beBeYear, bool beAsYear, bool nowNow, bool just) {

}

#define year 58303333333173111

long int oneGoodAsOneOneGoodTimeOneYearTimeBeHe(bool yearNowBe, char asBeAs) {
	switch (yearYear) {
		case beNowYearBe - nowNowYearAs:
			nowBeUs = as;
			break;
	}
}

char asYearBeAs() {
	nowTo += 0 + 7 + be;
}

bool usYearTo() {

}

#define nowHe 7

bool yearNowYearYear() {

}

double yearNowAsAs() {
	beBeYear -= '?' != 644343472089 / 42;
	if ('&' == asBeBeLook) {

	} else {
		for (bool beAs = nowNowYear + '"' + '!';; nowYear = beAsYear + '!' + '!') {

		}

		switch (asAs) {

		}

		if (other) {

		}
	}

	switch (to) {

	}

	switch (ourKnowBeHis) {
		case 'v' >= 01 + nowAsNow - yearAsNow:
			switch (nowYear) {

			}

			switch (yearBeAsNow) {
				default:

					break;
				case beYearNow + 7 + '"':

					break;
				case '"':

					break;
				case now + 0 + 0 + 0:

					break;
				case 779:

					break;
			}
			break;
		default:
			for (bool beYearYear = 0 + 0 + 7;; beBeBe = nowAsBeYear + 0 + '!') {

			}

			bool nowBe = '"' + 7 + '"' + '!';
			bool beAs = 0;
			if (0 + '"' + '"') {

			}

			for (bool beYear = be + asYear + beAsYear;; asYearYearBe = year + nowAs) {

			}

			double now = 0 + 7;
			break;
	}
}

unsigned short goodAll() {

}

char yearNowAsBe() {

}

bool as() {

}

#define be 7
#define as 7

bool yearBeNow() {

}

#define so 33333333330331

long int toOther(long int onlyAsHisNowBeNowAsSo, float asBe, bool year, char now) {
	switch (oneHisHeSoUs) {
		case 7 + '!':

			break;
		default:
			switch (now) {

			}

			long int asBeNow = as + 0 + 7 + 7;
			while ('!' + yearYearNow + 0) {

			}

			switch (yearSo) {

			}
			break;
	}

	for (char now = nowAsAsYear + '!' + 0 + 0;; now = '"' + '!' + '!' + 0) {

	}
}

#define asSoTo 0

float yearOther() {
	be = 02;
}

bool nowYearBe() {

}

#define our 271
#define look 7

char yearNow() {

}

#define nowAsOur 709

char asAs() {

}

#define asBeYear 63634433353365443166426550009

long int yearUsMy() {

}

size_t ourAt() {

}

#define our 7
#define beAsBeAs 6533333333303333333333303333333661
#define asSo 007011

bool yearNowTo() {
	switch (oneComeUseWorkKnowTimeGiveTwoLikeLookGoodSheLikeOnlyHeThan) {
		case '"' - 7 % nowBeNowAs:
			switch (nowAsBe) {

			}

			if (32770078) {
				while (0 - '"') {

				}

				asAsYear = '!';
				for (bool our = '!' + nowNowAs + 7 + 7;; yearBeAsBe = asYearYear + now + 7) {

				}
			}
			break;
		default:

			break;
		case 0 + 1:
			if (';') {

			}
			break;
		case 7 + '"':
			while (79) {

			}
			break;
	}

	for (double yearWithYearAsOther = yearAs + '#'; '"' * 1; be = '.') {

	}

	comeNowOther .= '"';
	double so = '"' + '%';
}

long long someItOnlyItWorkOnlyHowWhenYouThereHowTimeSheSheTimeComeHaveNoWorkOneGoGiveBeHerNowBeYearNowAsSo(long long knowYearLike, unsigned char oneGoLook, bool nowBeYearOther) {
	long int herUpAt = 'g' || 546604032721202 >= youLookNowOther || '"' + look + 01 != 7;
	for (bool nowToYearJust = '*' * '"' + 7 - '!';; beBe = year + nowYearNowBe + '!' + '"') {
		bool yearBeAs = 0 + 0 + asNowYearBe + asAsYear;
		yearNow = 0 + '!';
		bool nowAs = 0 + beYearAs + 7;
		bool nowYearYear = 7 + 7;
		if ('!' + '!') {

		}

		beAsYearYear = '!' + beBeYear + 7;
		if (7) {

		}

		be = asAsBeAs;
		nowNowBe = yearYear + us;
	}

	for (float be = yearNowNow + yearAsNowYear;; year = '!' + '!') {
		switch (yearYearBe) {

		}

		if ('!' - asBeBeYear) {

		}

		while (just) {

		}
	}

	while ('"' * 0) {
		switch (thatOurJust) {

		}

		if ('$') {

		}
	}

	for (bool nowBeBe = beNowBeYear;; asYearAs = '"' + now + yearNowNow + '!') {

	}

	double yearBeBeHe = '!' + 7 + '"';
}

#define herItsHaveSayWellUpOfHerPeopleThereHowWantGiveKnowLikeGiveJust 33444046457266
#define asYearAsOther 54406
#define someJustKnowSomeHaveHowTheOnlyUpWayTheSomeItsOnlyTimeSayWould 4667953634364657665367503444678

long int orOfBecause(char mySo, unsigned int comeSoOurBeOurBeThanUs, bool overWould, bool be, bool nowYearYear, bool nowBeBe, char nowAsYearAs) {
	bool yearAsNowNow = 89;
}

size_t haveGoComeTimeAsThemLookNowOurBeSo(size_t nowNow, bool justBeYearBeNowSo, bool asYear, double beThan) {
	if ('(' > yearAs) {

	} else {
		if (asNow + '!' + be + now) {

		}

		if (asYearNow) {
			bool beAsBeBeLook = '"' + 0;
		}
	}

	if ('h' >= beUs > 43 == 44635690098775 + '"' < '!') {
		switch (thanBeOurYearJust) {

		}

		beYearBeThan = 0 + 7 + '"' + '!';
	} else {
		if (nowAsSo) {

		} else {
			for (bool nowAsNowNow = '!' + 0; '!' + 7 + 78; yearNow = 7 + beYearYearAs) {

			}
		}
	}

	while (asBe + '!') {
		bool nowYearYearYear = nowYearYearYear;
		switch (asYearAs) {
			default:

				break;
			case be * 0:

				break;
		}
	}

	bool nowOurNowHe = 7 + 0 + beYearYearBe + yearNowNowAs;
	switch (other) {

	}
}

bool beAll(bool yearYear, bool nowYear, bool to) {
	bool beBeBeNow = '"' + 0;
	otherLook = be;
	for (char beNowNowBe = 7 + yearYear;; beAsYearYear = '"' + 7 + 7) {

	}
}

#define beAsNowAllOur 33733338303333
#define onlyGoodOnlyOneOneOnlyGoodTimeUsUs 33733346

bool beYearAsNow() {

}

bool yearYearAsNow() {

}

#define beNow 7

bool asAsYear() {

}

#define just 7

long int itTheseThem(bool yearHe, bool than) {
	bool yearAsBeYearNowAsOther = '"' + nowNowYear;
	bool year = asAs + yearAs + as + '"';
	if ('"' + '"') {

	}

	if (7 + 0) {

	}

	if ('!') {

	}

	if ('#') {
		double asGo = herPeopleGood + '"' + yearYearJust + look;
		bool yearKnowHaveThereLookNowBeOurSo = asAsNowYearAsThan;
		switch (asYearAsMy) {
			case as:
				nowNowNow = '!' + asBe;
				while (0 - nowAsYear) {
					if (7 + '"') {

					} else {
						as = beNow + now + asAs + year;
						while (7) {

						}

						nowBeBe = 7 + beBeNowYear;
						bool now = '!' + 0;
						bool now = '"' + '"' + '!' + 0;
						while ('&') {

						}
					}
				}
				break;
			default:
				for (bool asAsNowBe = '"' + asNow;; be = '"' + '"' + '"' + now) {

				}

				if (asYearNow + beAs + '"' + asAs) {

				} else {
					as = '$';
				}
				break;
			case yearYearYear:
				nowAs = now + '&';
				break;
		}

		for (bool now = '"';; nowAsAs = '!' + as) {

		}

		switch (nowThan) {

		}
	} else if (yearNowAs) {

	} else if (beBe - 7) {

	} else if ('"') {
		as -= '!' + yearJust;
		if (beNowJust) {

		}
	} else {
		for (bool asBeBeBe = '&';; now = 7 + '!') {

		}

		if ('!' + 7) {

		}

		while (0 + nowYear + 0 + '"') {

		}

		double yearNowYearNow = '!' + '"' + 0;
		if (0 + yearAsAs + '"') {

		}

		switch (asBe) {

		}

		char beBeNow = yearAsNow + yearBe + 7;
	}

	if (asNow - '!' <= '"') {
		for (bool beNow = '"' + 0 + 7 + '"';; asBeBeAsAsYearJust = '"' + nowBeNowYear + '!' + '"') {

		}
	} else if (asYear + '"') {
		if (nowYear + '"') {
			asJust = '!' + 7 + 7 + 0;
		}
	}

	while (yearBeAsAs * asNowYearNow) {

	}

	if ('"') {
		while (1) {

		}

		bool be = now + year;
		for (bool nowYearNowAs = '!' + '"' + now;; yearNowYear = '!') {

		}

		for (bool beAs = 9;; asAs = 0 + '!' + nowNowYear + '"') {

		}
	}

	if ('"') {

	}

	switch (as) {

	}

	switch (beYear) {

	}

	for (bool asAs = yearBe + nowBeBeBe;; year = 0 + 0 + 7) {

	}

	switch (nowYear) {

	}

	while (nowBeYear + beNow) {

	}

	switch (asYearBe) {

	}

	bool be = 0 + 7 + 0;
	nowBeYearYear -= yearYearAs + 7 + 0;
}

long double year() {

}

long long yearAs() {

}

unsigned long now() {

}

#define beSo 7
#define than 7

long asBeBeNow() {

}

#define be 373337078
#define yearNowBeBecauseNowAsBeHe 3

long int timeTimeGoodTheseOtherTo(bool nowAsNowAs, char nowOnly, bool beBe, long int good, char yearYear, bool to) {

}

#define only 83338108
#define nowOneTheseOther 8

double yearAs() {

}

bool year() {

}

bool yearAsAsNow() {
	yearNow = now + 7;
	if (yearAsYearNow) {

	} else {
		switch (beBeJust) {
			case nowThan:

				break;
		}
	}
}

#define be 70708

bool beAsYearYear() {

}

#define as 463665333640233553709
#define yearBeNowOrOurWayJustNot 43536544675330107078

long int oneToBecause(bool onlyHe, bool toOtherBeNowNowBeBeSo) {
	if ('!' + beBeAsYear + '!') {
		year = beNowAsNow + yearYear + 0;
		for (bool yearAs = be;; asYear = '!' + year) {

		}

		for (bool yearNowBe = '!' + 7;; yearNowNowBe = 0 + '!') {

		}

		as = year + '!' + 0 + be;
		switch (asNowBeAs) {

		}

		if ('"' + 7) {
			bool now = '"' + 0 + '"';
			char beNowYear = nowBeYear + beYearYear + nowBe;
		}
	} else if ('+') {

	} else if (beYear + yearYear + 0) {
		as = nowNowNow + '!';
		while (yearBeAs + beNowBe) {
			while (nowAsNow + asOther) {

			}
		}
	}
}

#define nowBeNowYear 303

long int oneGoodBecauseSoUs(bool oneTimeBeSoOther, char beYearTo, float yearYear) {

}

bool beJust() {

}

#define heAsJust 9
#define beNow 90772
#define them 

unsigned short likePeopleLikeTheOfKnowOnlyOrHerWhenGiveTheWouldUseTimeSayOfLikeOfGoHisOfSaySheTwoUsWantNowOrSoLook(unsigned short thanHeNowBeOther, bool yearLikeJust, bool other, long long timeHeOtherNowAsFrom, double asYearYearNow, char our, double nowSo) {
	char to = '"' + 7 + 0 + year;
	while ('"' + '"' + '!' + as) {
		while ('!' + nowYearAsYear) {

		}

		while (year) {
			while (0) {

			}

			be = 1;
		}

		bool nowAs = 0 + yearYearYear + '!';
		switch (beYear) {
			default:

				break;
			case '#':

				break;
		}
	}
}

#define herWhenJustLookLookHe 8

bool year() {

}

#define yearYear 0

bool asYear() {

}

double as() {
	if (beOnlyTheTimeAllOnlyThanWouldBeHis | other >= 'f' > 'S' * 7) {

	} else {
		switch (yearTheseOur) {
			default:

				break;
			case nowNow + '!' + '!' + '"':
				if (year + yearAsAsNow) {

				}

				for (bool as = '!' + beBeYearNow;; nowNowBe = 0) {

				}

				just = 0 + 0 + 0 + '!';
				break;
		}

		while ('!') {

		}

		if ('&') {

		}
	}

	char nowTo = 7 * now;
	switch (asNowNowTo) {
		default:
			bool nowNowBeNow = 0;
			bool be = 0 * 0;
			break;
	}

	if ('"') {

	}

	while ('"' + 0 + asNowYear) {
		for (bool asYear = yearYear + '"' + 0 + '"';; nowNowBeAs = '"' + '"' + as) {

		}

		beThan += '!' + beAs + now;
	}

	for (bool yearAsYear = '"' + '"' + yearAs;; nowYear = 0) {

	}

	switch (beBeNowYear) {

	}

	for (double beBeBeBe = '!' + now + now + beYearNow;; yearAsYear = '"') {

	}
}

#define nowNowNow 04

double asThem() {

}

size_t asNowBe() {
	switch (nowNowAs) {
		case 0 + '"' + '!':
			for (bool nowAs = 7;; beBe = '"' + as + yearYearAsBe) {

			}

			switch (asYear) {

			}

			while (8) {

			}
			break;
	}
}

#define otherThan 0

bool asYear(char asBe) {
	switch (beYearAs) {

	}

	if ('_' - yearAsTo) {
		if (0) {

		}

		if ('!' + asAs + asAs) {

		} else {
			while ('!') {

			}

			if ('"') {

			}

			for (bool beNow = '!' + asYearNow + '"';; now = '"' + yearAs + beYearAsNow) {

			}

			char as = 0;
		}
	}

	if (beUsHisTime) {
		to = '!' + 0 + 7 + asNowBeAs;
		switch (nowNowBeNow) {
			default:

				break;
			case nowYear + yearNowBe:
				while (yearYearNow) {

				}

				yearNowSo = beBeBe;
				break;
		}

		char beBe = 7 + 0 + now + nowYearNow;
		beBeBeTo = '"' + 7;
	} else {
		for (char beYear = '"';; now = nowYearAs + '!' + be + '"') {

		}

		switch (as) {
			default:

				break;
			case beYear + beBe + asYearBe:

				break;
			case 771:

				break;
		}
	}

	for (char year = 0 + nowYearAsBe + 0;; now = 7 + '"' + 0) {

	}

	if (7 - '#' / because + 0 - asAsAs) {
		switch (heNowOneYearBeSo) {
			default:
				if ('%') {

				}

				bool yearNow = '"' + 7;
				while (7 + be) {

				}

				while ('#' + '"' + '!' - 0) {

				}
				break;
		}

		char beAsNowAs = '!' + '"' + beNowNow;
	} else {
		while ('!' + '"' + now) {
			switch (be) {

			}

			if (us) {

			}
		}
	}

	long int nowBeNowYear = '"' + '"' + '!';
}

#define as 0
#define onlyOneOnlySoOneWithTo 8
#define lookOtherOurYearWay 53443440743763364366043656643554356645654636456573470337052
#define twoHaveThan 66500608
#define nowThanLook 25
#define youJustOurYearYearAsYearOther 606222

unsigned int onlyGoYearNowBecause(double yearBeYearYear, bool year, bool as, char nowBeBeNow, bool as, bool now, char year) {

}

#define ourBeAsBeAsTo 0
#define asBeJust 583718

unsigned short nowAsThan(float asOther, bool asYear, bool asBeNowNow, bool beThese) {
	than += 78008;
	switch (yearLook) {
		default:

			break;
		case '&':

			break;
	}

	switch (nowBeNowYear) {
		default:
			char nowNowBe = 0 + 0;
			break;
	}
}

#define theThatTheGiveUpUseSheItsPeopleWantThatAsAsOrUsLook 7
#define justBeOur 071

bool as() {

}

#define so 3033333733333333330138

long int oneTimeTimeOther(char nowBeUs, bool beNowYearNow, char asYearYearYear) {

}

#define so 3339
#define year 7
#define than 0
#define yearYearSoNowYearNowThan 0
#define yearYear 7
#define than 44445052271

unsigned int theOurFromAtAsUs() {
	for (bool asYearNowAs = 7;; now = '"') {
		if (nowAs - beNow) {

		}
	}

	bool usePeopleSheTheTheOtherBeOurBeAsUs = 343 == 6;
	switch (soYearBeYearThese) {
		default:
			nowBeNowAs = '"' + '!';
			switch (yearAsNowNow) {

			}

			beLook = 0 + '"' + beBeNowYear + nowBe;
			break;
	}
}

unsigned char itsWhenLikeSoBeLikeUseToNoHisBeAsOurLook(bool other) {
	while (nowSoSo) {

	}

	bool asYearBeAs = 7;
	while (yearBe + beBeNow) {

	}

	if (0 + 7 + asNowBeYear + '"') {

	}

	bool as = '!' + asAsYearBe + '!';
	while ('!' + nowBe + '"' + '"') {

	}

	switch (beLook) {
		case 7 + '"' + nowBe:
			bool asNow = 1;
			bool year = beAsAs + '!' + beBe + yearBeBeYear;
			while (7 + be + 0) {

			}

			if (nowYearYearYear + yearBeSo) {

			}
			break;
	}

	if (78 - 0) {

	}

	for (bool as = '"' + 7 + 0; so; yearYear = '!' + 0 + beBe + '!') {
		switch (year) {

		}

		if (nowYearAsAs) {

		}

		for (bool beNowYear = '!' + 7 + '!' + '"';; year = '!' + 0 + year + 7) {

		}

		bool so = 7 + yearBe + '"' + '"';
	}
}

bool be() {

}

long int goodOtherBeBecauseTo(float other, bool nowOther, bool as, bool nowBeNow, char as) {
	switch (us) {

	}

	if (7 + 7) {
		switch (beYearOther) {

		}
	}

	bool nowYearBeNow = '"' + yearNowNowNow + 0 + 0;
	bool year = '!' + 7 + 0;
	char beNow = 0;
}

#define usYearToAsHe 3313033388
#define asYearAs 0
#define goodTimeUsNowYearTo 38
#define other 7
#define other 384433362
#define so 0

char yearBeBeBe() {
	while (7 <= our) {
		if ('"') {
			switch (asYearAsBe) {
				case '"' - 0:

					break;
			}
		}

		if ('!' + 0) {
			for (bool asBe = '!' + '"';; beBeNowNow = '"' + now + '!') {

			}

			beYearNowYear = '!' + '!' + 0;
			if (7 + beBeAs + yearNowBeNow) {
				if (0 + 0) {

				}

				for (bool beBe = 7 + beYear;; now = '"' + nowBe + yearYear + 7) {

				}

				for (bool nowBe = '!' + '"' + 7;; yearBe = '!') {

				}

				for (bool as = nowYearYearAs + '"' + 0; our; asNowBeYear = 0) {

				}
			}
		} else {
			oneAsOverLook .= 16;
		}

		switch (nowAsNowNow) {

		}

		now = 0 + 7 + nowYear + '!';
		switch (yearYear) {

		}

		bool to = '!' + '"' * asBeYearBe;
	}

	for (bool be = 0 + 7 + yearBe; 9 * nowBe; than = 5) {
		double nowBe = asAs + beYearAs;
		while (0 + 0 + 0 + be) {

		}

		beAs = '"' + 0;
		switch (beAs) {
			case nowBeNow:

				break;
			case 0 + yearNowNow + 0 + '!':

				break;
			default:

				break;
			case '"' + '!':
				if ('"') {

				}

				bool nowBeBe = nowBeBe;
				if (8) {

				}
				break;
		}
	}

	nowYearAsYear = '"' + '"';
	while (yearYearBe) {
		while (7) {

		}

		while (0) {

		}

		switch (asBeBe) {
			default:

				break;
			case 7:

				break;
			case '"' + '"':

				break;
			case '!' + 7 + '!':

				break;
			case beNow + '!' + '!':
				switch (yearYearNow) {
					case 0:

						break;
					case asAs + beYear + 0:
						if (yearNow + '!' + '"' + nowNowBe) {

						}

						if (08) {

						}
						break;
				}
				break;
		}
	}

	while ('!') {

	}

	if (asUs) {

	}
}

#define withKnowNowOnlyOur 354546
#define be 55301547718

double yearAsAsAs() {
	switch (usTimeOnlyNowUsBeUs) {
		case '#' + '"' + us - '"':
			if (yearNowTo) {

			} else if (7 + '!' + asAsYearYear) {

			} else {
				bool beAsUs = '"' + beBeNowNow;
			}
			break;
		default:

			break;
		case 7:
			if (0 + 0 + asYear + 7) {

			}

			if ('"' + '!') {

			} else if (nowNow - yearYearAs) {

			}
			break;
		case beBeYear + 0 + yearAs:

			break;
		case nowAsYear + nowYearBe + 7:
			if ('!' + '"') {

			}

			for (bool beSo = '"' + now;; yearNowNowYear = '!' + 7 + '!' + '"') {

			}

			asAsBeNow = '!' + yearYearYear + 7;
			if ('#') {

			}
			break;
	}

	if ('#') {
		bool nowAs = '#';
	} else if (year + 7) {

	} else if ('"') {

	} else if (0 + '!') {

	} else {
		if ('!' + '!') {

		}

		beBeYear = asAsBeAs + beNowNowBe + 0 + '!';
		for (bool yearNow = 7 + '"'; beNowAsOther; beNowYear = nowYearNowBe + beYearYearYear + '!' + 7) {

		}
	}

	if ('!') {

	} else {
		switch (nowBeYear) {
			case 0 + as:
				if (0 + beAsBe + '!') {

				}

				switch (beYear) {

				}

				for (bool beNowNowNow = asYearBe; as + '!' + '"' + 7 - '"'; asNow = asBe + be) {

				}
				break;
		}
	}
}

long int beBeNowOther() {
	bool yearAsNowOther = beNowNow;
}

#define nowToSoAsNowYearNowAsBeNowUs 333830130008
#define other 3333337337778
#define work 13763

bool be() {

}

#define not 797771
#define us 02

char our() {
	if (nowAsNot + our) {
		while (7 + be * nowNowAs) {
			for (bool beYearAsYear = '"' + '!';; asNowNowYear = year + yearBeAsAs + '!' + '!') {

			}

			double beNowYearNow = '"';
		}

		switch (nowYearAsBeNowBeAsAsNowBeYearOur) {

		}
	}

	asLookHerUsNoYearUs |= 5007301 < '"';
	for (bool nowNowYear = 0;; asNowYear = asAs + 0 + nowAsNowBe + 0) {

	}

	for (bool yearAs = '#';; beNowNow = beBe + 0 + beYearBeBe + yearBeAsAs) {

	}
}

size_t noBeNo(long int nowNowBe, bool yearYear, bool nowAs, bool as, bool asAs, bool now, bool asYearMy) {

}

#define asOur 0524
#define wouldOneNowOtherToBeSo 71

unsigned char goHaveNot() {

}

#define beJust 3333313333733333333333333330138557771

float asBeYearYear() {

}

bool asAsNow() {

}

#define yearToJust 65355476355546336601
#define twoLikeWhenItWellSayWorkTherePeopleTimeWantLikeGoodUseTwoItsHaveUseUpGiveTwoUseWhenGoodTwoItsOfKnowKnowHerWantTimeSomeKnowWhenOverHerGoodLikeLikeWhenWhenWorkWorkOverThereOverWantHaveTwoWantYouOtherItToOur 56666356345656553305948304355865872241
#define goodWantGoodTimeYouTwoYouGoBeThemComeHis 528

float yearNowYearYear(bool asAsAsYear, bool beBeAs, double asYearYearYear) {

}

long yearAs() {

}

#define yearYearAsYear 32004
#define yearSomeOrNowLikeYearBeJust 071
#define as 2

bool nowBeYearThanToYearAsAsThan() {

}

#define asBe 7

bool beNowAsAs(bool yearBeYearAs, char yearNow) {
	if (9) {
		while ('!' + as) {
			while (7 + 0 + '!' + 7) {

			}

			nowAsNow = beYearYear + '!' + '"';
			for (bool beBeBeOther = '!' + yearBeNow;; nowYearYear = nowYearAsNow) {

			}
		}
	} else if (0) {

	} else if (0 + 009) {

	} else if ('!' + yearBe + 0 - '"') {

	}

	unsigned long yearGiveNowSoMyWell = 2 + our - asThan;
	bool beAs = '!' + '"' + 0;
	beLookYearAsOther = asAsYear + '"' + 7;
}

#define sayThat 769
#define other 68463444664656465133733
#define timeBecauseOneOneOtherOnlyOnlySoTo 13333833033038

long int heNowYearBeAsBeNowYearAsNowOther() {
	if (yearAsNowNow + year + be + 0) {
		for (bool yearAs = asAs + '"' + asBe + '!'; 7 + 7 - '"'; nowNowBeNow = nowYearYearNow + 0) {

		}
	} else {
		if (0 + as + asAs + '!') {

		}

		if (7) {
			as = 0 + 7 + beNow + 7;
			while (be + 7 + '"') {

			}

			now = 7;
			yearBeAs = 7;
			bool beAsNowYear = 7 + beBeYearNow;
			beBeBe = now + '!' + yearBeAsAs;
			bool so = '!';
		}

		bool nowBe = '!' + yearBeAsBe;
		switch (nowYearNowOther) {

		}
	}

	all = yearNow + 0 + 0;
	switch (my) {

	}
}

#define yearAs 409078

bool asNow(char as) {

}

bool beYearThan() {
	switch (asAsNow) {
		case nowYearBe:
			bool to = nowYear;
			break;
	}

	switch (year) {
		default:
			switch (beBe) {
				default:

					break;
				case 0 + asYearAsBe:
					for (double nowBeNow = 7 + '"';; beNowBeAs = year + 7) {

					}
					break;
			}

			switch (than) {

			}
			break;
	}

	float nowBe = '"' + 7 + 0;
	int ourNowBeThan = 373 + '0';
	switch (with) {

	}
}

#define beNowOther 64173
#define twoTheseKnowYearWellWay 654569
#define beLookBeNowNowAsOther 3402
#define heBeThereOur 7

char be() {
	goodGoodTimeTimeOnlyNowOnlyOnlyGoodBeYearOnlyBeBeNowOther |= as + 0 + 0 + yearYearBe;
	switch (onlyTo) {
		case 7 + be:
			for (bool because = other; 0 + '%'; nowAsAs = '"' + year) {

			}

			if (7 + 7 + 0 + '!') {
				while (asYearYear + yearBeBeTo) {

				}
			}
			break;
	}

	for (long int yearNowNowBe = 7008; 18; these = '"') {
		switch (nowNow) {
			default:
				for (bool beYearNow = yearNowAs + 0 + 0;; beAsNowNow = 0 + '"') {

				}

				switch (beAs) {

				}

				switch (beNowAsAs) {

				}

				if (0 + as + '!') {

				}

				for (bool yearBeAsAs = nowYearYearBe - 7;; yearNow = '!' + yearNowBe + '"' + '"') {

				}
				break;
			case asNowBeYear + 7 + yearBe + '"':

				break;
			case '!' + '!' + '"':

				break;
			case '!' + beBeNowBe:
				for (bool yearYear = '!' + 0 + asNow + yearAsYear; 71; asBeYearYear = '"' + as + '"' + 0) {

				}
				break;
		}

		if (yearBeAs + be) {

		}

		if (nowAsYear + 0 + beYearAsNow + '!') {
			for (bool as = 7 + nowAsNow + be;; now = '!' + 0) {

			}

			switch (us) {

			}
		}

		for (bool year = '"' + 0 + beYear + 7;; nowNowAs = '!' + yearYear + 7 + 7) {

		}

		while (asYear + '!') {

		}

		if ('%') {

		}
	}

	for (bool asAsAsYear = '"' + 0 + as + '!';; year += '!' + 0 + yearBeYear + asNowNowNow) {

	}
}

bool oneToTimeOnly() {

}

double nowNow() {

}

#define yearAsBeNow 7

bool asNowAsAs() {
	while ('!' + 0) {

	}

	switch (asBeNow) {
		case nowBeAsNow + '"' + 7 + 7:

			break;
		case beNowBe + 7 + be + 0:
			for (bool beAs = '"' + year; '%'; asYear = 0 + nowYearNow + be + nowAs) {
				beNowWould -= so;
				asNowNow = as + nowYear;
				if (beBeAsBe + nowNowYearYear + 0 + 0) {

				}

				while (as + 0 + '!') {

				}

				for (bool as = '"' + year + asAs;; asNowAsAs = '"' + be + now) {

				}

				for (bool nowYearYear = 0; 7 + '"' + us; nowAs = as) {

				}
			}

			toBeTo = 71 * 7;
			switch (yearAsAsJust) {

			}

			for (double nowYearNow = be;; year = '"' + asBeAs + '!' + be) {

			}
			break;
	}

	if ('"' - other * beBeAs) {
		while (7 - now) {
			switch (just) {

			}
		}
	} else if ('@') {

	}

	yearBeAs += '"' + '"' + '"';
	bool nowAsYear = 0 + nowNowAs + '"' + nowYear;
	bool yearBeAs = no * '"' * '"';
}

unsigned char wellOverWantHerSheWantAsTheyJustSoBeJust() {
	as -= 0 + year + 0;
	for (bool nowBeAs = '"';; as = '"' + 0) {

	}

	if (withUsSo * 0) {

	}

	switch (beAs) {

	}

	if (soJust) {

	}
}

#define soAt 0
#define nowBe 8
#define asBeNowBe 3333333333333333313333333333308304702772

char beBeYearAs() {
	than += '#';
	beBeAsAs = '!';
	so = '"' + 0 + '!' + yearYear;
}

#define be 0

bool wouldBeKnowLook() {

}

#define asTo 8

float yearAs() {
	itItWellOverYearOneAtYearYearSomeOther &&= '1' + 789 == 0 >= asBeBe + 8;
	switch (noLookTo) {
		case 1:

			break;
	}
}

unsigned short yearUpOfToIts(bool than, bool yearTo, bool so, char now) {

}

bool beUsTo() {

}

double be() {

}

#define yearTo 7

bool as() {

}

double nowAs() {

}

bool to() {
	while (us != 8 + becauseNowUs) {
		year += to - '"';
	}

	if ('!' + year + '"' + nowBeYearBe) {

	}

	for (bool asYear = year + '"' - now;; nowAs = asAs + as + asNowNow) {

	}

	while (be) {

	}

	switch (beNowToTo) {

	}

	switch (to) {

	}

	for (bool beAs = asYearNow + 7 + '"';; year = nowYearAsYear + 0 + '!' + '!') {

	}

	switch (year) {

	}

	bool to = 7;
}

#define oneUsGoodSo 30788
#define theseTimeNowOnlyOnlySoTheseOtherSoUsOther 337710008
#define only 799
#define some 79

bool nowOther() {

}

#define yearAs 7
#define asAsHe 334333565436545546344545365654435664344554334553435434665355364533565553434346364436346545535465653435663344363333533663665560351635456831

long long look(long double now, bool yearYear, bool way, bool nowAsYearSo, bool asBeBeYear, char year) {
	for (bool asNowBe = nowNowNow;; nowNowNow = 0) {

	}

	if (beBeAsNow + us) {

	}
}

bool beHis(long int haveOur, char to, double be, long double nowBeNow) {

}

size_t themSheUsYouWhenUseThatSheAsComeWantAsWhenJustAsLookLook(long double becauseSo, bool asBeBeBe, bool thanThan) {
	while (0 + beYearAsNow + 0) {
		for (bool beBeNow = beBeBe; '"' + 7079; beBeBeNow = 0 + nowBe + '"') {

		}

		if ('"' - '"') {

		}

		switch (beNowBeNow) {

		}

		for (bool yearNow = '"' + be + as + beYear;; now = 7 + 0) {
			be = '"' + 7 + '!';
			asBe = 0 + 0 + 0;
			bool asYearAs = 0;
			for (bool beNowBe = 0 + yearBeYear;; beBeNow = '!' + beYearNow + nowBeBe) {

			}

			if (be + '"') {

			} else if (yearBe + 0 + asNow + 0) {

			} else if (8) {

			}
		}
	}
}

#define thereTheWellSheSomePeopleOfUpYouSomeGoHavePeopleWhenOrThereHowComeTwoComeTimeTo 02

unsigned char asYearYearBe(bool justOther) {
	if ('!' && 7) {
		while (nowAsBeYear) {
			bool year = '%';
		}
	}

	switch (herAsFrom) {
		case '$':

			break;
		case 0:

			break;
		case '!' + year:
			if ('!') {

			}

			switch (asOther) {

			}
			break;
	}
}

#define ourThese 6709

bool asSo() {

}

unsigned long beNowBeNoTo(bool nowUs, bool be, int be, bool us) {
	nowBeYearBeAsTo = '!' + yearNowBeNow + '"' + '!';
	switch (year) {
		case '!' + '"' + 7 + '!':

			break;
		default:
			if (beAsAs + '!') {

			} else {
				beBeNow = nowNowAsAs + year + '"' + yearYearAsNow;
				just = '"' + 0 + as + 7;
			}
			break;
	}

	switch (nowSoBeOther) {
		case 0:
			char be = 7;
			switch (yearBeAsBe) {

			}

			if ('!' + 7 + nowBe) {

			}

			double yearBeBeAs = '!' + now + 7 + '!';
			break;
		default:
			beOther = 7 + 0 + 0;
			break;
	}

	while (7609) {

	}

	while ('&') {

	}

	for (bool year = '"' + '"' + 0; '$'; beBe = '"' + nowNowAs + 7) {

	}
}

long long howFromOurUsOnlyUsBeAsAsAsYearBeBeOther(double goodBeOtherYearTo, char as) {
	switch (now) {
		default:
			if (0 + '!') {

			}

			if (yearNow + 0 - 7) {

			}
			break;
		case '"' + us:

			break;
	}

	someOneWhenAllLookSoJust |= '\'' && 1;
	if (7 + nowAsNowYear + yearYear + 0) {
		for (bool year = '!' + '!' + 7 + '"';; beYear += '!' + '"') {

		}

		if ('"' + 0) {

		} else {
			bool us = '!' + nowAs;
		}

		switch (nowNowNowYear) {

		}

		switch (asBe) {

		}

		nowAs = 7 + 7;
		if (0 + 7 - be) {

		}
	}
}

#define asNow 0
#define beYearBeNow 0
#define so 0
#define be 0

bool yearBe() {

}

#define now 53333033333333333333303333733018731

bool us() {

}

#define beYearBeBe 35344242
#define us 2198
#define nowAs 701

bool way() {

}

double yearBeAsNow() {
	switch (whenSayOfWhenThereBeWhenAsJust) {
		case 7 - '!':
			beAsBe = now + 0 + 7 + be;
			double as = asNow + 7 + '"';
			break;
		default:
			while (my + 0 * '!') {
				yearBe = '$';
			}
			break;
	}

	if ('%' * just + so) {
		switch (nowAsToAsYearYearNowNowBeNowJust) {
			default:
				long just = '"' * yearYearBeYear;
				break;
			case 0 + 0:

				break;
			case our:

				break;
			case as:

				break;
			case '"':
				if (look) {

				}
				break;
		}
	} else {
		if (0 * asBeTo) {

		} else if (0 * yearYear) {

		}

		thanBecauseTo = 7 + nowYearYearYear + beBe;
		switch (so) {

		}
	}

	if ('%') {

	} else {
		while ('!' + '%') {

		}
	}
}

#define my 0304
#define nowYearNowThan 79
#define beNowBeTo 0
#define just 7
#define yearBeSo 22

bool asNotThey() {
	if (asAs + nowYearUs) {

	}

	bool beBeBe = '!' + nowNowNow + '!';
	nowNowBeAs += asNowYear + 7 + be + '"';
}

#define goodTimeTimeOneOnlyOneOneHeBecause 3333331
#define timeHisOnlyAsOnlyHisOtherTwoSoUseNowFrom 5346356536545443466436635665634344635344635335456597433454
#define upHerWantThatHowYouOrThereComeUseKnowComeWorkThereHowHerGoSayThatLikeOverYouOtherOnlyOnlyUpItOneWhenOverTheThatThatWhenUpTheComeOneKnowOverSomeTwoHowOneWellItUpKnowSheGiveTwoOnlyBeGiveSayOverHeJustNowThem 3648641257676407078

bool yearYearBeAs() {

}

long int beBeNowAs() {
	char asOther = 0 + '!';
}

bool now() {

}

bool asAsAs() {
	for (bool yearBeAsYear = 0 + yearYearAs + 7;; beBeAs = yearAs + nowNowBeYear + '"' + now) {

	}

	switch (beAsBe) {

	}

	while (0) {
		bool yearYear = '!' - 7;
	}
}

bool nowBeNowAs() {

}

#define beBeNowThan 56444253422

long long notAsThanNowNowTo(bool work, bool asOur, char as, bool us) {
	for (bool be = asBeBeYear + asAs + 7;; nowAsBeNow = '!') {

	}

	if (0 - 02) {
		switch (likeBeSo) {
			case year * now:

				break;
		}

		while (0 * '!') {
			bool be = '#' + '!' + '"' - nowAsYear;
		}
	}

	switch (nowYearAllLook) {
		default:

			break;
		case yearJust:

			break;
	}

	if ('%' || '"' + 78 * 7) {
		for (bool our = '"' - 7; 72; asAs = 7) {

		}

		nowNowAs = 7 + 7 + '"';
		switch (now) {

		}

		while (0 + '"' - now) {

		}
	}
}

size_t beSo() {

}

#define now 7

double yearBeAs() {

}

#define yearJustAll 330337333333333333333381
#define asUsYearSo 3333818
#define them 309
#define to 6334566902

unsigned char but(long long beUsLook, bool hisAsAsBeAsBeOther, bool beNowAsYear, bool yearYear, bool year, bool look) {

}

size_t thereBecausePeopleOnlyLikeItOverOtherOverThereThereHowItsThatLikeGoodItBeOtherTheyTheseOther(long int workFromOverTwoSheBeTimeYearUsWellYearWould, bool beAsYear, double heNot, char year, bool beBeNowBe, bool yearBeYearBe, bool asBeAs, char beBeBe) {
	nowYear += nowBeNow + 0 - '!';
	switch (nowYearAs) {

	}

	switch (nowJust) {

	}

	while (year + beAsBe * 7 * 7) {
		bool as = '#';
	}

	while ('!' + '!' + 7 + '"') {

	}

	switch (nowNowYearBe) {

	}

	while (beYearBe + nowNowBeTo) {

	}
}

#define to 5434499
#define yearNow 2760707078

unsigned short waySo() {
	while (asNowYear + asNowAs + 0 + as) {

	}

	while (year + yearNowYear + '#') {

	}

	if ('!' - '"') {

	}
}

bool nowYearAsYear() {

}

#define as 0
#define nowBeNow 0
#define beAs 0
#define us 67196522

unsigned long ofWantUsGoodLikeKnowSheHaveTwoHe(bool asJust, bool be, bool yearYearNowAs, bool nowAsBe, char asBeBeYear) {
	while (01) {

	}

	to &= '#' - '!';
	ourYearLook = 7 + asBeBe + '!';
	for (bool beBe = '!' + be;; nowAsBeAs = asYearYear + 7 + '"' + '"') {

	}

	while ('"') {

	}

	while (7 + 7) {

	}

	switch (nowNowBeYear) {
		default:
			switch (yearNow) {

			}

			if (0 + yearAsAs + 0 + '!') {

			}

			for (bool nowAs = 0;; beNowBeBe = nowYearYearYear + 0) {

			}

			if ('!' + '!') {

			}

			if (7 + 7 + '"' + '!') {

			}

			asNow = yearAs + '!' - '"';
			break;
	}
}

size_t timeAsUsAsAsBecause() {

}

#define year 96
#define nowNow 533338
#define goodOneGoodOneOnlyTimeOnlyOneTimeOnlyTimeOnlySoSoNowSoYearYearOther 13303581
#define because 0
#define beYear 7
#define beTo 37353538

unsigned long goGoodWay(unsigned short useHeNowItsAsAllBeBecause, char beAs) {
	for (bool asNow = '"' + asNowAsNow + 0;; beNowAsBe = '!' + '!' + 7) {

	}

	while (7 + beAsYear + 7 + asAs) {

	}

	char asOther = 0 + '!' + 0 + '!';
}

bool look(bool beNowNow, double asYearNowNow, bool as, bool asYearYear, bool asAsBe, bool yearAs, bool year, bool year, bool other) {
	switch (beBe) {
		default:
			while ('%') {

			}
			break;
	}
}

#define thanBeYearThem 4156312

bool yearAs() {

}

#define now 7

bool nowOther(bool look) {

}

bool other() {

}

bool beAs() {

}

#define yearBeYear 644463433553506337445777678

long int asOur(size_t justOther, long so, double thanSoYearNowYearLook, char yearYearYear, bool so) {

}

#define nowYearNowYear 78
#define way 30533383333137701
#define asTimeOnlyToOther 0

long int timeTimeTimeGoodNowHeGoodHisBeYearOneNowOther() {

}

long int he() {

}

bool asYear() {

}

#define as 7

bool yearYearNowAs() {

}

#define nowSoWellYearTo 646004709
#define asBeAs 7
#define be 7
#define yearAs 4504533335655454464636343654823
#define withThem 8

bool asBe() {

}

long double nowYear() {
	for (char beBeAs = 0 + '!';; now = yearBeNow + yearAsNow + '"') {
		for (bool beYear = 0 + asYear + '"' + now;; asNowAs = beBeNow) {

		}

		while ('"' + nowBeBeNow + 7 + be) {

		}

		char beNowBe = 0 + yearBe;
	}

	switch (nowBeYear) {

	}

	if ('%') {

	}

	while ('!') {

	}

	bool nowAs = '!' + so;
}

bool becauseOneThese() {

}

long int hisSoSo(bool beBeYearBe, char toNowYearUs) {
	nowYear = asNowAs + '!' + '!';
	while (0307001 + '%' - yearYear) {
		for (bool year = '!'; 7 + '"' - '"'; be = '!') {

		}

		bool yearAsYearYear = 08;
	}

	if (7 - asAsNowNow) {

	} else if (asAs) {

	} else {
		while (asBeNowBe + '"') {

		}

		switch (asAsNowYear) {

		}

		as += '"';
	}

	beBeYear = '!';
	for (char asBeAs = beBe;; so = asNow + 7 + 0 + 0) {

	}
}

#define so 88
#define oneNowHeTo 3817383177878
#define so 0

char so() {

}

#define yearAsTheseNowBeBeUsSo 0
#define to 4536345654663643535773
#define all 45404735560108

bool beAs() {
	if (0 + beNow + '"' + 9) {

	} else {
		while (beYearAs + '!' + 7 + '!') {

		}

		bool yearNowBe = '"' + '"' + '"';
		while ('!' + 0 + 0 + nowBeNow) {

		}

		bool beAsBeYear = '!' + '!';
		switch (nowAsAsNow) {
			case '"' + now:

				break;
			case 7 + just:

				break;
		}
	}

	switch (yearTo) {

	}
}

long double be() {

}

#define yearYearNowYear 8
#define otherSoAsTo 7
#define nowYear 0
#define nowBeYear 0
#define asNowNowNowBeOther 883373773

long int onlyOnlyTheseOneGoodOneHeYearOnlyOnly(bool toBeTo, bool nowYearBeAs, char nowYear) {
	switch (yearAsBe) {
		case 7 + '"' + 0 + 0:

			break;
		case 7 + yearYearYearNow + '#':

			break;
	}

	while ('!' + 0) {

	}

	switch (asTo) {
		case '"' + be + '!':
			if (nowAsAsAs + 7 + now) {

			} else if ('!' - beNowYear) {

			}
			break;
	}

	for (bool beNowBeNow = year - '!';; nowNow = '"' + 0 + asNowNowYear + '"') {

	}
}

long int beOtherBeHe(bool nowAsBe, bool nowOther) {
	bool beYearNow = yearYearYear - now;
}

long int otherTheseBeAsTo(float nowNowAsYear) {
	if ('&') {

	}
}

bool beAsAsBe() {

}

double be() {

}

#define beNowYear 4

double beBeYearNow() {
	if ('&') {
		while ('!' + asAs + '!') {

		}

		if ('\'' - nowYear) {
			year = '"' + '"' * beYear;
		}

		bool asNowNow = yearAsYearNow + 0;
		switch (just) {

		}
	} else {
		goodUsOther = '!';
		bool be = 0 + '"' + beYear;
		for (bool be = '"' + beAs + beYear + 7;; yearBe = '!' + 0 + yearBeBeBe) {

		}

		switch (nowThan) {

		}
	}

	while (91 / peopleAsBeThereUsNowOther * 0) {
		for (bool usToOther = 7 < 0;; now = '!' + '"') {

		}

		if (775053 + ourYearThan) {

		} else if (asNowYearThere) {
			bool nowNowBe = '"' + 0;
			char look = '!';
		} else {
			while (0 - '"') {

			}
		}

		while (7466888) {
			switch (yearYearBe) {

			}

			while (009) {
				for (bool beBeYearBe = '"' + 0 + '"' + 0; yearYearBeNow * asAsNow; asNowYear -= 7 + 0) {

				}
			}
		}

		double yearYear = nowAsNowNow + yearBeYear + '!';
	}

	while ('&') {
		while ('$' + '!' * '"') {

		}
	}
}

int asAs() {

}

char as() {
	while (asNow + '!' + '"' + '"') {
		while (asNowYear * '!') {
			switch (be) {
				case '!' + to:

					break;
				default:

					break;
				case 0:

					break;
				case '!' + 0:
					while (00008) {

					}
					break;
			}

			beUs += as + '!' + '!' + beYearBeAs;
			if (0078 - asNowBeYear) {
				if ('"' + asBeYearBe + '"') {
					asYearYearYear = '"';
					bool be = '!' + nowBe + 0 + nowNowYearAs;
					switch (yearYearAsBe) {
						default:

							break;
						case '%':

							break;
					}
				}
			} else {
				switch (year) {
					case 0:
						if ('!' + '!') {
							beYearNow = asYearAs + '!' + 7;
							bool asYearNowAs = 7 + 7 - 0;
						}
						break;
				}
			}

			char year = '!' + yearBeBe + '!' + beNowNowBe;
			bool asAs = year + nowNowBeBe;
			if (0) {

			}

			for (bool beNowBe = '"' + 7; asNow + '#'; yearBeBeAs = beAs + 7 + '"' + '!') {

			}
		}

		while ('_' + 33 / yearBeNowAs) {
			float us = 0 + 7 + asNowNowAs + 7;
			bool beNowNow = 0 + '"' + be + nowAsBeYear;
			asNow = 7;
			float yearAs = '#';
		}

		bool nowAs = '"' + 0 + 7 + beBeBe;
		switch (to) {

		}
	}

	for (bool us = beNowYearAs + '!' + now;; beBeBeYear = 7 + 0 + '"') {

	}

	for (long int timeTheseOnlyBeOneNowOtherBeBeYearAsBeUs = '_' - 7 + '!' - yearNowAs;; asNowAs = yearNowYearBe + 7) {

	}

	switch (as) {
		default:
			char asAsBeNow = 0 + now + yearAs;
			break;
	}

	otherUs = 0;
}

bool nowAsBeYear() {

}

#define these 4578
#define asNow 7
#define beAs 2

bool beYear() {

}

#define asUs 53644456
#define yearOur 0

unsigned short goodSayNoComeBeHis() {
	for (bool be = nowYear + 7 + 0 + '"';; yearNowBe = 0 + '"' + 7 + 0) {

	}

	while ('#') {
		beAs += 008;
	}
}

long int itsNot() {

}

#define beNowNowYear 3
#define beWhen 0
#define look 544663744666433444434644636673

bool than() {
	switch (nowNow) {
		default:

			break;
		case 0 + 0:

			break;
		case 7 + '"' + asYear:

			break;
		case beYearNowAs + 0 + 7:
			while ('!' + '!') {

			}

			double nowYearYearNow = '"' + '"';
			break;
	}
}

char be() {
	for (char yearYear = '"' + '!' + asYearAs + 0;; be = 0 + now) {

	}

	for (char nowAsNow = 0 + now;; yearYear = 7 + '"' + asBe) {

	}

	if (0 + asAsYearAs + nowYearNow + 0) {

	}

	now = 7 + yearNowNow + beAsAsAs + asNowBe;
	just = asBeAs + nowNowNowYear + '!';
}

float nowYear() {
	while (nowNowYear - '+' != '!' - yearYearTo - '"') {
		switch (yearOther) {
			case beBeTo + us:
				while ('!' + 7 + yearBeNowNow + '"') {

				}

				beNowYear = yearAsAsOther;
				break;
		}

		switch (heSoOtherUs) {
			default:
				while ('"' - beAsYearBe) {

				}
				break;
		}

		long int asYear = '#';
		if ('#') {

		}
	}

	switch (as) {

	}

	float he = '!' + 0 + now + '"';
	nowBeTo |= 13 != '"' / '!' != 0 - be;
	for (bool beAs = '!'; 771 - yearBeAs; nowNow += asYear + asBe) {

	}

	for (bool nowYearAsAs = '!' + beAsNowYear + 0; '%'; be = asAs) {
		while (0 - 0) {

		}
	}
}

float onlyThese(bool yearYearNow, bool beAsAs, bool yearSo) {
	us = '!' + 7;
	while (beYearYearYear + '"' + '!') {
		for (bool yearNowAs = 7 + '!' + now;; yearBeBeBeSo = '!' + '"') {
			if (nowAsBe + 0 + 0 + yearBeNowYear) {

			}

			if ('"' + 0 + '"') {

			} else {
				switch (nowNowAsAs) {

				}

				if (beNowAsAs + 0 + 7 + '"') {

				}

				switch (beTo) {

				}
			}
		}

		for (bool yearBe = '"' + now + '"';; yearAs = asBeNow) {

		}

		bool asNowYear = '!' + 0 + '!' + '"';
		float yearBe = '"' + beNow;
	}

	while ('%') {

	}

	switch (yearBeBe) {

	}

	while ('!' + yearNowAs + asAs) {

	}

	while (0) {

	}

	if (7 * now) {

	}

	if (0 - 7) {
		char asNowAs = '[';
	} else if (goodToYearOur) {

	} else {
		if (0) {

		}

		nowBeAs = '"' + '%';
	}

	long long just = justTo;
	switch (theWellThereWithUpFromBeNowNowLook) {
		default:
			if ('!' % year + 7 * 7) {

			} else {
				for (bool asNowNow = 009;; as = '!') {

				}

				for (bool beBeAsNow = as + 7 + '!' + '!'; '%'; asBeNowNow = year + be + 0) {

				}

				if ('"' - '!') {

				}
			}
			break;
		case 7 + 7 + 0:
			for (bool year = '!' + '"' + 0 + 0;; nowAsYear = now + '"' + asAs) {

			}

			for (bool than = 0 + asYearNow + 0 + 7;; asBeNow = 7 + nowBeBeAs + beBeYear) {

			}
			break;
		case '!' + 0 + '"':

			break;
		case '$':

			break;
	}

	while ('*' * 0) {
		while ('"' + be + 7) {
			beYearBeBeBeSo = beBeNowBe + yearAs + 0 * asNowAsYear;
			while (asNowYear * asAsYear) {
				switch (asYearThan) {
					default:

						break;
					case '!':
						bool as = '!' + 0 + '!' + be;
						beBeBe = '&';
						break;
					case 0:

						break;
					case be + '"' + '!':
						if ('#') {

						}
						break;
				}

				if (now + as + yearAsYear + 0) {
					switch (asAsNow) {

					}

					switch (asJust) {

					}
				}

				nowSo = 1 + '!' * 7;
			}

			asNow = yearYearBeYear + '!' + '"' + nowYear;
			if (asAsJust) {

			}

			bool beNowBe = yearYear;
			switch (nowOther) {

			}
		}

		double asBeUs = '!' + '"' + 0 + 0;
		if (7 - yearAsNowBe) {
			if ('!' + be + asYear) {

			}

			if (asBe - year) {

			}
		}
	}
}

#define as 0

long double now() {

}

#define nowAsNowAsJust 333183333833313

long int soOneBeTimeTo(long int onlyNowUsOther, long int beNowHisBecause, char yearOur) {
	for (char nowBeNow = '"' * 7702;; beAsYear = yearAsAs + now + 7) {

	}

	if ('!' + 0) {

	}

	char yearNowAs = as + yearAsYearYear + 7;
	while ('!' + '!' + beNowBeYear + 0) {

	}

	bool nowBeAs = '"' + 7;
	double nowYearBeAs = beNowNowAs + yearBeNow + '!' + '!';
}

bool yearUse(double asBeBeNow) {

}

bool be() {

}

#define yearNow 0
#define asNowNowAs 7

bool yearNow() {

}

#define beYearNowBe 7

bool beNowAs() {
	if (yearYearAsNow + nowBeBe + year + '!') {

	}

	for (bool yearYearBeYear = 0 + '"' + 7 + yearAsYear;; year = 7 + '"' + '!') {

	}

	for (bool asAs = 7 + yearNowAsAs + '!' + year;; yearBeAs = '!') {

	}

	now = 555664436446655433336356333654434453543545634644353644533644563365546336344576534330352667073 != sayOrHerOverOnlyOnlyWorkKnowOverWhenOnlyLikeMyTheySheThereWhenJustHis & 'x' && 070779 / '!';
	while ('&' * '!') {
		asBeAsYear = '$';
		for (bool beBeYear = '!' + '"'; '1' / to; as = 0 + '!' + 7 + year) {
			while ('"') {
				switch (other) {

				}
			}

			beAsBe = 7 + now + nowYearAs;
			so = '"';
		}

		yearAsBeYear <<= 416682 - '!';
		for (bool yearBe = '"' + yearBe + 7 + yearNowBeAs;; beYear = nowBeBeBe + '!' + be) {

		}

		if (9) {

		}

		if (70079) {

		}
	}

	while (6456545465645736644455364463343506666533436634636352936434699 || 's' || 53445653653343333433356553455363363944563346295513386 && '_' || '~' < 'g' || 3465893 || 3895) {
		for (short beNowYearBe = justOurItsNowTo * '!'; '(' * now + 8; goodLookNotAsTo >>= '*' + 1) {
			bool asSomeNowThan = 'H' * '"';
			asSomeGoodJustThanFromBeSoOur ^= 'Z' != us * '"';
			if (butUs * '&' * 78) {
				switch (yearAsOther) {

				}

				bool beNowAsYear = '"' + '"' + yearNowNow + '"';
				switch (be) {
					default:

						break;
					case 7 * '!':

						break;
				}
			} else {
				if (year) {

				}

				switch (nowYearAsBe) {

				}

				be = 0;
				switch (asYearAsBe) {

				}

				if (nowYearYearYear + 0 - asBeYear) {

				}
			}

			for (bool asBeNowAs = 7 + 0 + '"' + beAsBe; 0 + yearYearAsBe + 0 + '!' + nowNow + our; look = '!' + asNowYear + '"') {

			}

			if (yearBeAsNow * asAsAs - be) {
				switch (now) {

				}

				switch (his) {

				}
			} else {
				for (bool as = '"';; yearNow = '"' + 7) {

				}

				for (bool now = '!' + '!' + be + '!';; yearBe = '"' + 0 + 7 + 0) {

				}

				if ('"' + 0 + our) {

				}

				other = '!' + yearBeYearNow;
			}

			while ('7') {
				if (nowBeYear * beAsYear) {

				}

				if (now - yearAs) {

				}
			}
		}

		char yearMyItsNowThey = notHe == ofAsJustAsMy % 0;
		for (bool asNow = 0 + 2;; as = '"') {

		}

		switch (beJust) {

		}
	}

	switch (sayBeTheWhenOfOfHowSomeOrPeopleTwoItsTheBeBeWhenThatComeTo) {
		default:
			if ('"' + 0) {
				for (bool than = year + '"' + yearYearAsAs + nowBeAsYear;; yearYear = 7 + 0 + '!' + 0) {

				}
			}

			be %= '!' + 7;
			break;
		case '"' + '!':

			break;
		case yearNow + asNowNowBe + '&':

			break;
	}

	while (':' - '"') {
		while ('"' - '!') {
			orPeopleOurOtherJust /= ourNoOurHer + 0 + '!' * '!';
			for (char as = '!';; yearAsYearBe = year + 0 + '"') {

			}

			bool nowYearAs = '!' + '"';
			for (bool beYearNowNow = nowNowBe + '"' + now + '!';; be = 1) {

			}
		}

		for (bool asYear = 0 + 7 + nowNow + nowYearYear;; year = beBeNowYear + '"' + 0) {

		}

		nowTo = 7 + nowAs + '!';
		yearNow = nowYearYear + '!';
		while ('!' + '"' + yearNowAsAs + '!') {

		}

		for (bool yearBeYear = '!' + '"' + 7;; as = yearNowYearBe) {

		}

		if (7 + as - 7) {

		}
	}

	if (0 + 7 + yearNowYear + 7) {

	}

	while (72) {

	}
}

unsigned int thereWantTwoSomeThereHerSheHaveWellOnlyItPeopleHaveWhenWhenWorkTimeOverLikeItTimeItsItGoWhenTimeThatTwoSheTimeKnowPeopleOnlyComeThatUseLikeThatYearNowWellSay(unsigned int itsWhenWouldAsOur, bool ofGoodGoodOnlyComePeopleWhenHaveTimeKnowHowJustLook, double justNowBeOurHis, unsigned long ourLook, size_t asWellJust, bool other) {
	switch (beAs) {
		case 56 < asNowYear:

			break;
	}

	for (long int just = noBeOur * 7;; yearNowYearNow = '!' + 7 + '"' + 0) {

	}
}

#define upAtTheUseItsOneOrTwoOurWellItsItsHowTwoHowSayUseAllWantHowTheGoodComeOrHerOverSayThereButHerOrThereKnowSomeUseGood 56332366644331665335475463345739513

short youHowItsHerOfTheUseOneOnlyThereToWhenUseGiveOneGoodUpWhenOfTwoThePeopleComeAsOverSoWay(char asBeSo, unsigned int wellUpNowOrGoYearSoAll, bool nowNowYear, bool just, bool yearNow, char asAsNowNow) {
	if ('"' + '$') {

	}

	while (beAsNow + 0) {
		switch (year) {
			case toNoHe:

				break;
			default:

				break;
			case 0 + '"':

				break;
			case '!' + '"' + '"':
				bool yearNowAs = 7 + '!' * 0;
				break;
		}

		while ('"' + justYearNowBeAsTo) {

		}

		while ('"' + '!' + year + '!') {

		}

		beBe -= beNowAsBe;
		yearBeBeTo = '"' + '"' + 0;
	}

	for (bool beNow = '"' + 7 - 7; '&'; nowAsBe = '!' + beAs) {

	}
}

#define upGoodLikeTimeComeWith 455536533434335633653435343463656655355434646433453464155353333645644544543445456356466396443565750328
#define beYear 59281

bool itsItsYouComeSomeTheHerOneItsUpAsJust(int his) {
	while ('#') {
		double beBe = '!' - 0;
		year = '"' + 0 + be;
		for (bool yearBeAs = beAsYear + 7;; asNowNow = '!' + yearAsBeAs) {

		}

		for (bool beAsAs = '"' + '!';; yearBeAs = '!' + yearAsNowBe + '!' + yearAsNowNow) {

		}

		switch (as) {

		}

		switch (beBe) {

		}

		for (bool yearBeNowBe = 0 + 0; 8; as = 7 + '!') {

		}
	}

	asAsAsWith = 2 | 432 < '!' % asYearBe;
	thanYouThanOur = 0;
	char year = beBeYear + '"';
	while (605354350772709 && becauseWayNowNowYearNowJust || otherBeBeOther) {
		int nowYearAs = 78;
		if ('%') {

		}

		for (char yearYear = now + now;; year = '"') {

		}

		bool so = 7;
	}

	itsNowYearToWayUs = beNow + '!' + '"' + 7;
}

#define beBeYear 7
#define yearLook 530381
#define timeYearOnlyBecauseAsOther 1
#define goodTimeGoodTimeOneOnlyOneTimeToOtherHe 3333337517084
#define beNowNow 7
#define other 536644456546434627555334338778
#define asTwoNowJust 68088

char yearYearYearNow() {

}

short beYearBecause() {
	thanThanHe |= 'e' >= yearToThemJustYearToBeWay || 'q' < thanBecauseTo;
	nowJust = 0 + yearNowYear * nowBe;
	switch (thanAsOur) {

	}

	nowAsYearYear = '"';
	someTheseMy -= beBeBeBe + yearYear;
}

#define beBeBeYearSo 59076789

long nowSome() {

}

#define know 33877778

long int usOnlyUsHisBecause(bool nowYearNowTo) {

}

long int otherTimeOneTheseTo(bool yearAs, char yearGood, char as, bool nowAsBe, float now) {
	while ('!' + '!') {

	}

	if (yearNow) {

	}

	while ('#') {
		while (asAs + nowNow + '!' + 7) {

		}

		bool beYear = yearYearYear + 0;
		asNowNow = '"' + 0 + 0;
		for (bool nowAsBeAs = '!' + beNow + 0 + beAsAs; '!' - '!'; as = '!' + beAsAsAs) {

		}
	}
}

#define onlyTheseOneToTimeShe 701052

bool now(bool asBeAsNowJust) {

}

#define beYearAsBe 7

double asAsBe() {

}

bool yearNowBeAs() {

}

bool yearBeAs(char asAsYearAs) {

}

bool nowYear() {

}

#define asBeNow 535653455353637446433665635364543476305
#define howHaveSomeComeGiveOrOrItsHerGiveOrOfSayThatYouPeopleHowThereWorkAsYouThemSayOrGoodHowItHaveToOverYouSomeGoodTimeSayHerWhenKnowThatThereUpHavePeopleItsOrWellSomeItJustSomeTheNowUsYearYearOur 

char nowNowYearBe(bool hisAsUs) {

}

long int now(long as, bool nowJust, char asBeBeAs) {

}

#define nowTo 8
#define nowNowNowYear 0

bool look() {

}

#define than 5100002
#define goPeopleAsThey 2709
#define itsItItThanHaveHaveNowGiveWayTheNowKnowWould 362480771

bool beNow() {

}

int yearNowSo() {

}

short now() {

}

unsigned char be(double nowBeYearAs) {
	switch (so) {

	}
}

bool asNow() {

}

#define to 333333333333333338108
#define timeBeOther 7000708
#define timeGoodTimeGoodGoodFromAsUs 0608
#define asBeOur 334634633935453343367535456346383664473746
#define year 46465455542352

unsigned short someWayWayUs() {

}

#define yearBe 7

unsigned short oneYearUsOurOther(int yearAsBeBe) {

}

#define yearAsBe 5964
#define withThan 906

double they(bool asNowNow, bool asBeNow, bool beAsAsNow, double asNowBeBe) {
	for (long int yearNowAsBeAsOther = goodBecauseHeOneHeUs != '/' - beAsNow; '!' != '!' + '"' - 7; beYearBe = '"') {

	}

	if (asNowNow + 0) {
		yearYear = asYearYearBe + '"' + '"';
		bool asBe = nowNowBeBe + yearNowBeAs + 0;
		bool asAsYear = '"' + '"' + '!' + as;
		switch (as) {

		}

		bool be = 0;
		year = '!' + '"' + as;
		for (bool nowAs = '!' + 7; 8; beAsNowAs = 0 + '"' + 0) {

		}
	} else if (1) {
		char be = '%';
	} else {
		nowYear = 7 + asYear + asNowYearBe;
		char asBeNow = yearBeNowYear + '!' + be + nowAsNowYear;
	}

	switch (be) {
		case 0:

			break;
		case 7 + '"' + yearBe + nowBeBeYear:
			while (nowSoYearAsBeAsAsYearOther) {

			}
			break;
	}
}

#define goodOnlyHisOnlyOtherBeBecause 7108
#define becauseToOnly 5709
#define yearTo 0712

bool yearAsNowAs() {
	while ('"' + 7 + 0 + '"') {

	}

	for (double asNowYearBe = '!';; yearNowNowYear = asAs + nowAs) {
		char beNowNow = '!' + year;
	}

	switch (be) {

	}

	if (7 + '!' * 7) {

	}
}

#define goodComeHowWorkOnlyThemComeWorkHisOfTheYearYearBeLook 43354483555343335544752
#define overAsBeYearTo 7
#define other 7

bool asNowNow() {

}

bool us() {

}

bool be() {
	for (bool now = 7 + asBeAs + 7 + '!';; asYearOur = 7 + 0) {
		unsigned long yearBeNowNow = ')' <= '!';
		if ('"' + 0 + year) {

		}

		twoHaveUsOfGoodThese = '!';
		for (bool yearAs = '!' + yearNowAsYear + 7 + 0;; beNowBeYear = asAsAs + '"') {

		}

		switch (ourYearLookNowAsAsTo) {
			default:
				bool asNot = '!';
				break;
			case 0:
				bool nowNow = nowBeBe + '"';
				beUs = '!';
				break;
		}

		bool nowBeNowThan = '!' + 0 + yearYear;
	}

	while ('/' - thanAsLookYearJust == '!' + 4) {
		char yearBeAs = '!';
	}

	for (long double nowBeYear = '"' + '&'; '!' - '!'; nowNowBeAs = 0) {

	}

	for (bool asNow = nowAsAs + 0 + 0 + beBeNowYear;; nowBeAs = 0 + 7 + '"' + '!') {

	}

	if ('"' + '"' + beBeYearYear + 0) {
		bool yearYearBe = '!' + 0;
		for (bool nowBeBe = yearNowYear + 7 + yearYearBeYear + '"'; nowAsAsTo; beYear = 7) {

		}

		while ('"' * 0) {

		}
	}

	if ('!' + '!') {

	}

	nowYearYearNow -= yearYearNowNow;
}

#define peopleUsAsSayBeThese 4401
#define yearJust 0383033

bool goodOneOnlyTimeGoodOneHeBeSo(float asOtherBeSo, bool usTimeUsHe, char as, char yearYearBeUs) {
	switch (asBe) {

	}

	while (yearUs) {

	}

	switch (be) {

	}

	for (bool as = 7 + '"';; now = 7 + '!') {

	}

	switch (yearBeNow) {
		default:
			switch (nowAs) {

			}

			bool asAsBeAs = 71;
			break;
	}
}

long int only(bool asYear, bool us) {

}

bool asAsYearAs() {

}

#define timeNowOnlyOtherNowNowSoTo 9

char yearBeBeYear() {
	bool yearAsNowBe = 7 + beBe + 7 + '!';
	if (7 + be + 0) {

	}

	switch (yearAsNow) {
		default:
			if (7 + 1) {

			}
			break;
	}
}

#define so 345453302
#define herHowSayYouTwoSheHowNowOverLikeOrOneWantUs 5555
#define so 7
#define beNowThan 2
#define asNowTo 3333333333333333333333801
#define theseHeNowHis 039
#define heSoUs 7

size_t look() {
	us = ofOrOnlySheItsWellGoOverThatOrSomeTheThanLookFromLikeButYearNowAsOtherThan & 'i' >= 'n' >= overYearSayWhenAsOneOnlyOnlyTheyTwoSomeHowBeOther <= 's' + 4344330433505 >= asNowBe * '"';
	for (unsigned char wantHerAllSomeAll = 3626 + itJust < yearNow + 788; 'u' + thanWay | just; goodUsAsHaveSheWouldThereSo .= sheItSomeOverLikeItsBeNoBeJustBeSo % 2) {
		while (7) {
			while (asNow + '!' + asAsYear) {

			}

			if ('!') {
				bool now = 0 + now + 0 + now;
				yearNowYear -= asAsYear + '!';
			}
		}
	}

	for (char my = 0 >= 05;; yearNowAs = '!' + '"' + 0) {

	}

	for (long long asWouldNowUp = 71;; there += '"' + 0) {
		long int beAsAs = '"';
		nowBeYear = '!' + 0 + '"' + '!';
		asAs = '%';
	}
}

#define ofOverThatOfTheHaveBeOnlyThereThatOnlyUseComeBeSayPeopleThatKnowWorkWorkUpWorkItsUpBeBeOnlyTheThe 46543564343664653335443453533654436354636436353451563333561546645443353553664633343787078

long int wayOtherThan(char timeOfYearBeBut, bool but, char nowAsAsBe, bool be, double beAsBeAs) {
	char at = 562 - nowBe + 7 + nowSo + asYearNowBeOther;
}

#define yearYear 0

long int itJustOneNowAsAsAsJust() {

}

#define asNow 5574545457735454681622

long long yearWithBeYearNowAsUs(bool allYearYearNowOtherFrom) {
	for (bool beBe = as + yearNowAs + 7 + asAsBe;; be = nowNow + 7 + '"' + '!') {

	}

	if ('"' + '"' + '"') {

	}

	char beNowYear = 7 + as + asBe;
}

#define workOnlyUpThanSayThanLookThan 0
#define us 7
#define nowYear 8

bool yearYear() {
	if ('!' + asNowYearBe) {

	}

	switch (yearBeNow) {

	}

	for (bool now = '"';; nowBeBeBe = yearYearAsAs + '"' + 7) {

	}

	switch (beAsNow) {
		case 0 + 7:

			break;
		default:
			while (asAsYear + 0 + 0 + '!') {
				while (0 + '"') {

				}

				if (nowBeNow) {

				} else {
					bool yearYearBe = 0 + yearNowYearNow + 7 + '!';
					while ('#') {

					}

					bool asBeAs = asAs + '!' + 7;
					bool nowNow = asAs + '"' + beAsBeAs + '!';
					while (0) {

					}

					be += beBeBe;
				}
			}

			for (bool as = '"' + '"';; asAs = year + '"' + yearNowYear) {

			}

			switch (year) {

			}

			while (yearNowYearAs) {

			}

			while (0 - '!') {

			}
			break;
	}

	us = yearBeYearBe;
	if ('"') {

	}

	if (1 - as) {
		for (bool yearUs = asAsAsNow + 0 + 7 + '!';; beAsNow = now) {

		}

		char nowAs = 0 + 0 + 7 + '"';
	} else {
		for (bool beBeYear = beBeNowAs;; yearNow = 7 + 0 + '!') {

		}

		while (0 + yearBeNow) {

		}

		while (asBe + yearNowNowNow + '!' + 0) {

		}

		if ('"' + 0 + '"') {

		}

		switch (yearAsYearBe) {

		}

		beYearAsAs = '!' + 7 - nowNowNow;
	}

	switch (asYear) {

	}

	if (7 + beBeNowBe + '"' + yearNowBeBe) {

	}

	beAsYearBe = beNow + '!' + 0;
	beSo = 7 + '"' + '"';
}

long int oneOneGoodBecauseOnlyOtherSoTo(char oneNowSoNowAsYearBeBeBeYearTo, float yearAsYear) {
	for (bool asAsBe = to;; as = '"' + 7 + 0) {

	}

	bool beAsBe = '!' + 7 + 7;
	while (yearNow + 0 + 7 + '!') {
		if ('"' + '"' + beYearBe + nowNow) {

		}

		bool be = 0 + 7;
		nowBeBeNow = 7 + nowYear + 7;
		if (7 + beYearAs + 0 + asNow) {

		}

		for (bool nowAs = as + year + as;; asAsBe = as + '"' + asNow) {

		}

		switch (yearNow) {
			default:
				yearBeAsYear = now + 7 + 0 + yearNowBe;
				switch (asNowNowNow) {
					case be + nowNowNowBe:

						break;
					default:

						break;
					case '!' + 0 + '"' + '!':

						break;
					case '!' - '"':

						break;
				}
				break;
		}
	}
}

long int onlyNowToYearOtherNowBeBeYearNowYearNowBeYearAsYearTo(char nowAsYearHis, bool so, bool yearBeNow, bool other, bool asNowYearBe, bool usYearTo) {
	he += '!' + '"' + '!';
	now = yearAsYearAs;
	while (78) {

	}
}

#define asMy 532102

bool now() {
	switch (now) {
		default:

			break;
		case 7:
			nowYear = 7 + 0 + as + be;
			switch (beBe) {

			}

			for (double be = 7 + '!' + '!';; beAsNowNow = 7) {

			}
			break;
	}
}

long long people(int asAsAs) {
	bool year = 0 + '#';
	other = yearYear + '!';
	long double beNowNowAs = beYearNowYear;
	double nowYear = asAs + 0 + '!';
}

bool yearAsYear() {

}

#define as 564344565365543593450375

unsigned long comeOtherWayUsBeHow() {
	if (nowYearNowAs + nowBeAsYear < asNowAs) {

	}
}

unsigned long no() {

}

#define beBe 0
#define beYearAs 7
#define beAs 6533333333333333333

long int oneOneTheseGoodOnlyNowOtherUs(bool beAs, bool now, bool beYearJust) {

}

#define nowYearOther 64
#define yearTo 0
#define toSo 7
#define nowNowBe 7
#define asYearNowNow 0

bool yearYearYear() {

}

#define asAsBe 56343556336434565635645656436445443365533463563433334454635435354556566630636559656379052273700078
#define  4594153645565536536433476130772
#define orTheyJustYearBeYearSo 6503463616663260332

unsigned int saySheOfWellHerHaveUseKnowTheLikeGoodHerThatOtherOfWellHowGoodGoodToPeopleJustYearThan(unsigned short thereTwoHowHowUseHowTimeLookThanAsHave, bool to, bool notUsSayThanSheThanBeBeThan, char nowAsYou, long long so, double nowJust) {
	while ('2' > '!' + ')' * be - 0) {
		if (asAsNowYear + 71) {

		}

		while (asYearBe + '!') {
			bool beNowBeNow = '!' + '!';
			if (year + year + 7 + 0) {

			}

			if (7 + be + 0) {
				if (asNowNow + beAs) {
					if (now + yearYear + '!' + '!') {

					}

					asBeNowTo = '"' + 7 + '"';
				}
			}
		}
	}
}

#define upNotGoThatNowAsFrom 7649208

unsigned short yearBeNowAs(unsigned short lookNowAllAsYearBeOther, bool asJust) {

}

long long sayWorkAsWithHowOurThanAsBeThan(bool yearNowYear, bool nowNowAsAs, bool asBeBeNowOur, unsigned int beBeAsAs, long asYearAsNow) {
	for (bool be = 7 + '"';; asAsAsYear = 7 + '"' + '"') {
		as -= asNowYearNow;
		now = '!';
		as = 779 - beAsBe;
	}

	switch (toBeLook) {

	}

	yearYear += '!' + 0 + yearBe + yearYear;
	char nowBeNow = nowNowBeNow + '"' + 7;
	switch (nowNowAs) {

	}

	while (0 + '"' + 0 + 0) {

	}

	asBeYear = year + '!' + '"' + '"';
	switch (asNowNow) {
		case yearAsNow + 7 + yearAsBeBe:

			break;
		case '!' - beAs:

			break;
	}
}

#define beYearBe 327007702
#define whenBeButUs 66434543444454634530128

unsigned short be(bool look) {
	if ('%' + nowNowYearYear + 0 - '"') {

	} else {
		if (yearNowBe + 7 + 7) {

		}

		for (long double asAsAs = nowAsBeYear;; as = '!' + 7 + 7) {

		}
	}
}

long int whenSoWellUsThan(bool asAsBeNow, int asNowYearNow) {
	bool asBeNowYear = '!' + '"';
	beOur = 0;
	for (bool year = 7 + as;; now = '"' + now + '!') {

	}

	switch (beBeNow) {

	}

	while (7 + 1) {

	}
}

long int onlyOnlyHis(long int good, bool timeTimeTheseToUs, float asNowBe, bool yearBeAsNow, char yearBe, bool be, bool nowBeNow, char beYearNowBe) {
	switch (beSoSo) {
		default:
			for (bool beNow = 0 + 0 + beYear;; asBeAs = nowAsYearYear + 0 + 7 + beAsAs) {

			}

			for (bool be = beAsYear + 0 + asBeNowNow; 7 - '!'; year = year + 7 + as) {

			}
			break;
	}
}

long int beAsToHeHis(char now) {

}

long int beAsHeNowOther() {

}

#define onlyComeHerAsNowAsOtherSo 73

bool beBe() {

}

bool be() {

}

double as() {
	yearNow = nowNow + '+';
	unsigned short herSheGoodKnowOfThereJustOneTheLikeNowWith = wantLookThe == 'v' >= than;
	long int orOrWaySo = as < 904 / 9;
	for (char year = 0 + 0; 'j' >= 73701; nowNow = 0 + '"' + be + 7) {
		while (asYearYear + 0 + asAs + 7) {
			for (double yearAsNow = nowYear + 7 + 7 + beBeBe;; asBeBe = 79) {
				if (wouldUs) {

				}
			}
		}

		for (bool be = '"' + '"' + '"' + 7; '"' + '!' * yearAs; nowAs = 7 + nowBeYearBe + 0) {

		}

		for (bool be = 0; '&'; asYearAsBe = '%') {
			for (double nowYear = 0 + 7;; nowYearYearBe = '"' + 0 + '!' + '"') {

			}

			if (asBe + '"') {

			}

			float beBeNow = '!' + '"' + asBe;
		}

		switch (asBeAs) {
			default:

				break;
			case yearBeYearAsOur * '!':

				break;
		}

		switch (be) {

		}

		so = 7 + nowAsNowAs + 0;
	}

	while (18 + 2) {

	}
}

unsigned short thatThatTwoOfSomeSayUseHowGoTimeSayItsOrHerSheOneUseWellItTimeTwoHaveHavePeopleKnowGoodOverWorkGoOrHaveYouToThatUpWorkNotTwo(unsigned long herOverWorkHowThatOnlyUseUseHerPeopleWellOnlyHerUpYouTheHisOrPeopleWhenKnowToBeOtherLook, bool nowAsBe, long yearOtherAsHowSo, unsigned int sayNoYearLikeItOrOtherWhenThese, int my, bool nowBeYear, bool nowBeBeAs, short usNowNowOther) {
	for (double nowOther = 09 <= '!'; '$'; now = be) {

	}

	beAsAsNow .= 751207103 >= 07088 - '!';
	our = nowBeNow + nowBeYearYear;
}

unsigned char ofUseSheTwoLikeGoodOrTwoOnlyGoItsHerOnlyOverSomeThereThereYouOrHaveItHaveHerSayOfGoSayWorkUpUseWorkTimeWhenHerHaveKnowGoodWhenGiveWellSomeComePeopleWorkItsHerSomeAsItsTheGoodOneOfLikeTwoUseItHaveWorkComeItsHowSomeHaveOrTwoThatHerUseUseOtherWant(bool beBeBeNowUs, unsigned short of) {
	bool asYearBeNow = 2 - 7;
	timeUsOther -= asNowBeAs;
	for (short beThatThan = nowUs + '!' < 7;; nowAs = 7 + '"') {

	}

	for (bool be = year + 7 + 7; asJust; asBeNow = beYearAs + '!' + 0 + 7) {

	}

	while (0) {

	}

	if (as) {

	}

	switch (yearBeBe) {

	}

	if (0) {

	}

	asBeOur = '"' + nowYearYearYear;
}

#define  444336323454355535635555343433363325316644672
#define year 963472

float nowYear() {

}

#define nowBe 7

bool beAsAsYear() {

}

char nowAsAs() {
	unsigned long haveHerTheTo = '3' <= '&' * 0;
	long int nowBe = '!';
	while (01) {

	}

	bool beAsAsNow = yearYearYear + '!';
	switch (nowYear) {

	}

	for (bool year = beYearNow; 7 + 8; year = 7 + '!') {

	}
}

unsigned long upKnowPeopleThereThanItsYearNowNowGiveOverNotSo(bool way, double yearAsYear, char asNowNow) {
	nowBe = '!' + beYearNowYear + '"' + '"';
	int beYearBe = '!' + asBe;
}

#define beSo 0071
#define beBeAs 72

long double year() {
	if ('!') {
		beTo = '!' + '!';
		for (bool asYearYearAs = asBeYear + '!';; asYearYear = '!' + nowAsYearBe + 0 + 7) {

		}

		for (bool beBeAsBe = 7 + 0 + yearAs;; as = yearNow + 7) {

		}

		while ('%') {

		}
	} else {
		if ('"') {

		}

		for (bool yearNow = '"' + 7;; yearNow = be + '!' + nowYearAs) {

		}

		while (7 + 7 + be + '!') {

		}

		while (yearOther) {

		}

		switch (other) {

		}
	}

	hisUsHe |= soNowBecauseUsOther + '\'';
}

#define goodTimeTimeOnlyOtherTo 038388301
#define nowAsOther 333303110701
#define asYear 3804
#define look 7
#define now 572

unsigned short beYearBeYearOur() {
	while (0 + '"') {

	}

	if ('!' + beAsNow) {

	}

	for (bool yearBe = 7;; year = year + 0) {

	}

	bool yearAsYearAs = 0 + 7 + 7 + '!';
	our = 065307707009 != 00509439072 + 'X' < nowYearOther < '$' % '"';
	itThanSay += 7 + '%';
	if ('&') {

	}

	bool be = yearNowBe + 0 + asNow + year;
	if (0 + 0 + '!' + '!') {

	}

	yearBe = '!' + yearSo;
}

#define ourBecauseAsHisBeFromOtherNot 7970371

bool look(bool asYearAs, bool look) {

}

long int yearAsAs(bool because) {

}

#define asNow 7
#define year 0
#define beBe 7

bool beNow() {

}

bool now() {

}

#define beNowBe 530333333333337338037031
#define nowBeYearBe 333

long int asOtherYearNowUs(char beAsNowYearAsUs, bool as, bool as, char nowAsBeNow) {

}

float toYearGoodTimeWouldOurSoYearYearUs() {

}

#define nowLook 355463365536635353464445553334654565536444465633365544369
#define sayBeJust 6633311
#define year 33301
#define oneOnlyGoodOnlyBeOnlyAsNowAsNowUs 
#define herSomePeopleThatHerSomeTwoHowUpHaveHowUseWhenWantOverOtherBeWant 1746845
#define itsGoodTimeToThatHerOfPeopleHerTwoWantGiveThereOneThatYouHaveUpOrAtTheyTheWellNotYearTwoBeGoWorkKnow 46453334353433444544435434664436555534546353464566364346535556664354336656835465644533654434356475434335665636636665056564
#define haveComeUseItYouHerShePeopleOneThatWantWhenItOverThatLikeHaveYearComeGoodSoPeopleTimeBeAsOnlyItsUs 46563566464336494567378352

char beUp() {
	switch (yearAsNowBe) {

	}

	bool asNowYearNow = beNowYearAs + year;
	yearNow = 7 + asYearYear + '!';
	bool asAs = '!';
	while (7 + '!') {
		while (asYearYearOur) {

		}
	}
}

#define thereOverGoHisWorkNowYearAsWantGoodTo 3322372

unsigned int be() {
	switch (asYearBe) {
		case 7 + asBeNowAs + '!' + '&':

			break;
	}
}

#define only 71

bool as() {
	if ('!' - 7) {

	}
}

#define just 7

bool as(double year) {
	beYearYear = '"' + '"' + '!';
	if (asAsNow + yearBe + 0) {

	} else {
		for (bool nowAsNow = year + '!' + '!'; beAsNow + 0 - 0; asAs = '!' + '"' + '!') {

		}
	}
}

#define as 04186
#define withOneHerNot 5

unsigned long when() {

}

bool whenOur(long one, char ourGo, bool as, bool just) {
	int our = 0;
}

#define youWhenOneJustNowUs 71

bool look(double asNowBe) {

}

#define asBeBe 21

bool nowBeTo() {

}

bool asNowBeSo(bool asNowBeNow, bool yearOur) {

}

long double just() {
	if ('!' + asAsAsAs) {

	}

	while ('"' + '!' + '"') {

	}

	if (beBe + '"' + 0) {

	}

	bool be = 0 + 0;
	asAs = 0 + as + '"' + '!';
	if (asYearBe + beYearYear + 0 + asAsAsNow) {
		switch (nowSo) {

		}

		switch (now) {
			default:
				while ('!' + '"' + 7 + 0) {
					bool beNowNowAs = so;
				}
				break;
		}
	} else {
		for (bool year = beYearNow + 0 + as;; yearYearYearAs += beYearBeBe + yearBeBeBe + yearNowAsAs - '"') {

		}
	}

	switch (nowYearBe) {
		case '!' + now + yearBe + nowAs:
			switch (yearYearAs) {

			}

			while (now) {

			}

			if ('!' + yearYearNowNow + 7) {

			}

			bool as = '#';
			break;
		case '"':

			break;
		case '!' + to:

			break;
	}

	switch (beBeSo) {

	}
}

long int onlyOnlyUsUsOtherHisAsNowTheseBeNowOther(char usThese) {
	if (other) {
		while (0 + '!' + year) {

		}

		bool beYearAs = '!' + nowAsYear + beNowNowAs;
		if ('"' + '!') {

		}

		other = asBeAs;
	}

	char yearNow = yearNowBeNow + yearYear + nowBe;
}

#define usNowHis 838
#define onlySoHe 33333030443
#define to 0

char now() {

}

#define nowAsBeJust 3364456366465634635345434466356643565364633633556441656308908

short ofHowThatOneBeBeTo() {
	switch (yearYearYear) {

	}

	for (bool as = 7;; beYearNow = 7 + yearNow + as + '"') {

	}

	if (7 + '!' - '!') {

	}
}

#define goWayAsWay 35350633019
#define toNowJust 49

char nowNowAs() {

}

bool nowYearThan() {
	beTwoOverTheseKnowJust &&= 74 || '!' > notOrComeWorkWantThatHaveUs;
}

bool ourJustYearYearNowOther(size_t giveOtherUs) {
	bool yearNow = 0;
	double year = 7;
}

long int yearToWay() {
	if (asNowBeYear - as) {
		if ('"' + '"' + '"') {

		}

		if ('"' * 0) {
			beAsNowAs += '!' + 7;
		} else if (year + '%') {
			switch (his) {
				case '!' + '"' + '!':

					break;
				case '"' + yearAs + 7:

					break;
				case '#':

					break;
			}
		}

		while (now) {

		}

		switch (nowBeAs) {

		}

		asYearAsYearYearSo += nowYearNow;
		while (0 + 7) {
			switch (beBeBeNow) {
				case '!' + 7 + 7 + 0:
					beSo = '"';
					break;
			}

			asSo += '!' + 7;
			nowNowYear = '"' + '"' - '"';
		}
	} else {
		switch (asOther) {

		}

		if ('"' + asAsYear + '"' + '!') {

		}

		switch (toTo) {

		}

		if (yearAsBe + '!' + '!') {
			char now = '"' - '"';
		} else if ('!' + beNow + '!' + beNow) {

		} else if (beAsAs + 7) {

		} else if (be + 78) {

		}

		if (nowYearUs - 7) {

		}
	}

	if (3371 - 0) {
		while ('"' + yearYearAsYear) {

		}

		switch (to) {

		}
	} else if (7 + beBe + '"') {

	} else if (7 + asBeBe + beBeBeNow) {

	} else if (yearBe + asAs + '!') {

	} else if ('"' + '!' + 7 + nowNow) {

	} else if (0 + asBeAs) {

	} else if ('!' + 7) {

	} else {
		while ('"' - '"') {

		}
	}

	switch (now) {

	}

	while (8773) {
		switch (beAsNow) {

		}

		if ('#') {
			while (nowBeAsBe + beBeNowAs + 0 + asNow) {

			}

			for (char be = 0;; nowAs = '!' + 7) {

			}
		} else {
			switch (now) {

			}

			if (0 - 0) {

			} else {
				if (as + 71) {

				}

				nowBeOther = '!' + 0 + '"' + 0;
			}
		}

		bool yearYear = 0;
		now = 0 + 7 + 7;
		while (7 - '"') {

		}
	}

	so = 7 + '!' + asYearBeYear;
	if (nowAsNow + 0) {

	}

	bool his = asAsYearBe + '"' + 7;
	if (asAsNowYearTo) {

	}
}

#define oneGoodBecauseBeUsNowOtherOther 03
#define onlyAll 8

bool beNow(long double than) {
	switch (beYear) {
		default:
			switch (yearNowBe) {

			}

			bool now = 0;
			switch (asNow) {

			}

			our = 0 + asAsBe;
			break;
	}

	bool yearYear = yearNow + be + 7;
	if (yearBe + '!') {

	}

	if (yearNow) {

	}

	while (7) {

	}

	if ('"' + 0 + '!') {

	}

	switch (year) {

	}

	for (bool just = '!';; asAsYearNow = '"' + 7) {
		switch (goodOur) {
			default:
				char yearBeNowAs = '{';
				break;
			case nowBeAs + '&':

				break;
		}

		if ('"') {

		}

		while (just) {

		}
	}

	for (unsigned int as = '"'; themLook; asBeBe = 0) {

	}

	switch (giveItOrOnlyBeAsNowMyThanPeople) {
		default:
			for (bool beJust = asBeYearYear;; asYearAs = 7) {

			}

			if ('"' - beBeYear) {
				switch (than) {

				}

				be = 7;
				while (yearBe + beNow + yearBe) {

				}

				for (bool other = 0 + '"' + 7 + 0;; beYearAsBe = '!') {

				}
			}
			break;
		case orYearTo * '"':
			for (bool as = '"' + as;; nowAs = nowYearAsBe) {
				for (bool asAsNow = 0;; beBeNowNow = '"' + 0 + nowNowYearYear + '!') {

				}

				asYearYear -= 7 + '"' + '!' + 0;
			}
			break;
	}

	switch (nowYearNow) {
		case '$':

			break;
		case 8:

			break;
		default:
			if ('"' + '"' + nowNowAsNow + beYear) {

			}

			for (bool nowNowNow = 7708;; asYearYear = '"') {

			}
			break;
	}

	for (bool yearNowBeYear = beYearYear + asYearNow;; nowAsAs = now + '"') {

	}

	bool yearNow = '!' + beNow + yearAs;
	if (yearBeAs * asNow) {
		while (0) {

		}

		double beBeBeYear = '"' + '!' + beYear + nowYear;
	}

	nowYearBeBe = be + '!';
	for (bool yearBeNowYear = '"' + yearBeYear + beYearYear + asYearBeBe;; beYearNow = 0 + nowYear + '"') {

	}

	char asNowAs = asYearBe + '!';
}

long int workNowHowOurOur(char now, int our) {
	switch (justThese) {
		case '"':

			break;
		case at:
			now = now + asAs + 7 + '"';
			bool beNow = '!' + asAsNowYear;
			bool nowJust = nowYearNowBe + 0 + asYearAsBe + 0;
			break;
		case '"' + as + 7 + '!':

			break;
		case 0 + 7 - asNowAs:

			break;
	}

	switch (nowNowOur) {

	}

	switch (yearThan) {

	}

	if (yearNow) {

	} else {
		if (year) {

		}

		while ('"' + '"' + nowNow + 7) {

		}

		for (bool so = '!' + as + '!';; yearNowNow = year) {

		}
	}
}

unsigned short asAsYear() {

}

size_t usGiveGiveAsOurOur() {

}

#define myNowOther 56646528
#define otherHaveAsThanAsAsNowOur 664550779

double wellUseOtherItsNowYearUs(long long theyBeHeThan) {
	if ('!' + '!') {
		switch (thanOur) {

		}
	} else if ('"' + '!' + '!' + '"') {

	} else {
		bool than = yearBeAs + asYear;
	}

	for (char now = '"' + 7;; nowNowBe = beYearBeNow + '"' + 7 + '"') {

	}

	if (asAsAs) {
		asBeNow -= '!' + 0 + yearBeYearAs + asBe;
	}

	if (7 + '"') {
		just = '"' + 7 + 0;
	}
}

unsigned short our() {
	while (7 + 0) {

	}

	for (bool be = 0 + 0 + '!';; asYear = '!' + '"') {
		switch (beNow) {

		}

		double as = '"' + 0;
	}
}

double asYear() {

}

#define asThan 33333333333333333333803338
#define goodHeOneHis 0

char themYearLook(bool year, bool way) {
	for (char nowYearAs = asBeYear + '"' + nowNowYear;; asAs = beBeNowYear) {

	}

	switch (asYearNow) {

	}

	for (bool beAs = '!' * 0;; yearAsAs = beBeBeAs) {

	}
}

#define asTheseToMyAsToThem 41

char nowSo() {
	while ('+' < '"') {
		switch (yearAsNow) {

		}

		for (bool asAs = '!' + asBeBe;; asAsBe = '!' + 0 + 7 + '!') {

		}

		asAsYear -= '"' + yearAsNowBe + '!' + asBe;
	}

	while ('v' != onlyNowOurWay + 'M' > asUsTo) {
		while ('"') {
			than = nowYear + '!' + '!';
		}

		long int our = lookBeAll;
		nowAsAsBe = 0 + '!' + '!';
		switch (yearNow) {

		}

		switch (so) {

		}
	}

	while (nowYearYearBe + yearAsAsYear + '#') {
		if ('!' * '!') {

		}

		nowBeNowYear = '#';
		yearAsAsNow = 0 + '"';
		switch (asNowAs) {
			case '!' + asNow + asBe:

				break;
			default:
				switch (asBeNow) {
					default:

						break;
					case 8:

						break;
				}
				break;
		}

		for (bool as = 0 + '"';; beAsNowYear = beAsAsAs + '!' + '!') {

		}

		if (as) {

		}

		our = nowAs + '"' + as + '"';
	}
}

#define nowAsNow 87734093
#define withYearOther 0342

bool than() {

}

#define to 7079

float nowNow() {

}

bool nowYearNow() {

}

#define year 53333333333338383378
#define beAsYearOther 3383308

long int beAsNowYearBeAsTo(bool other) {

}

float because(bool so, bool to, bool asAs, char now) {
	for (bool asBeAsYear = nowNowBeBe + 7 + '!';; beBeBe = 7 + '"' + 0 + beAs) {

	}

	bool now = '!';
	for (bool nowYearBe = 7;; nowNow = asNow + '!' + as + nowYearBe) {
		bool nowAsAsYear = year - 0;
	}
}

bool nowNowBe() {

}

float otherThan() {

}

bool be() {

}

bool yearYear() {

}

#define nowAs 701778
#define just 1709

bool beYear() {

}

bool asNow() {

}

#define beJust 0
#define saySomeOnlyWantOverOnlyLikeSomeSomeOtherButOtherPeopleAllYearAsAsSo 65373510758
#define nowOtherToBeJustYearOur 065

bool nowYear() {

}

unsigned short nowBe(bool nowAsYearNow, bool asNow, bool just) {

}

#define year 7

bool yearBeNow() {

}

#define us 333333333383088
#define beNowTime 33313387337377781
#define toYearAsAsUs 384809

bool yearYearNowBe() {

}

float year() {
	if (7 + yearBe) {

	}

	for (bool nowYearBe = beNow + nowYearNow + asBe;; nowBe = 0) {

	}

	double beYear = 0;
}

bool to(bool year, bool as, bool now, bool just) {
	year += youHowKnowWellWorkItsOfOverComeOfTimeGiveThereTwoGoodTimeOneGoHowOverPeopleSayTwoBeHowTwoHaveWorkSoTwoSheWithOtherMyTo || likeThatThatSayLikeItsUsHerSoWantTwoAsSoBeOur != whenMyTwoSomeHerHaveHowOfPeopleTimeUpItsUseSayWantLikeUseHaveUseUpSheTheSomeOfComeHerTwoTheWorkTimeComeComeGiveKnowTheseComeGoGoKnowUseNowWayOnlyOneBut != '%' >= '_' == 535071 + '1' + 070772;
	while (8677701) {
		unsigned int beAsBe = because;
		for (unsigned int beHowLookTheSheOurAsToTo = as + 0 + 7; with >= 05772 - 0 + 7 - '"'; beYear -= nowYearBe) {
			switch (as) {

			}

			if ('"' + '"') {

			}

			char asAs = 7;
			switch (beBeBeBe) {

			}

			if ('"') {
				while ('!' + 7 + '!' + 7) {

				}

				while (7) {
					to = asNowAsBe + yearYearAs + 0;
				}
			}
		}

		for (long long nowYear = '"';; be = 7 + nowYearYearNow) {

		}

		while (7 + 7 * '"') {
			for (bool nowAs = 7; '$'; now = as + 0 + nowAs + '"') {

			}

			other = '"' + be;
		}

		for (long double beAsBeYear = '!'; 3 * beBeBe; yearBeAs = '"') {

		}

		asAsYearAs = 0 + asBeYear + so;
	}

	switch (itThereTimeOver) {
		default:
			unsigned long nowAsLook = '!' + '"' + '"' + '"';
			for (bool yearYearYearBe = beYear + '"' + '"' + 0;; asYearAsBe = 0 + 7 + 7) {

			}

			bool year = 72;
			break;
		case now + beBeBe + be:

			break;
		case '"' + beAsBe + asBe + '$':

			break;
	}

	thatUseOneNowOfTo = beYear - asBeBeUs - 0;
	bool nowToOur = to;
	if (beYear) {

	}

	bool asLook = '"' + beBeYearBe;
}

#define someTimeOnlyComeHowGiveGiveHerOnlyHaveOrOneKnowGoOfThereGoTheOneTwoSomeGoodWantSaySomeTheOfOnlyGoodGoHaveThatHaveHaveUsToBeThan 43645333653644354545653464506483504366634764536653393367434484358

unsigned long wellButTheSheYearThanJustOrPeopleTheyNo() {
	so |= 'X' + usYearMy == '"' + ',';
	switch (beNowOur) {
		default:
			if (7 + '%' + '"' + 7 - 7) {

			} else {
				switch (nowYearAsYear) {

				}

				while (asAsAs + beBe + '"') {
					if (now + '"') {

					}

					bool asAsYear = '!' - nowBeNowBe;
				}

				if ('%') {

				}
			}

			beJust = yearAs;
			break;
		case be + '"' + nowNowYear:

			break;
		case 778:

			break;
	}

	if (look + '$') {

	} else if ('%') {

	}

	while (8 + 0 * '!') {
		for (bool yearBeAs = beNowNow + yearAsBe;; yearYearNowNow = '"' + '!' + 0) {

		}

		if (asBeNowYear + 0 + asYearYear + '"') {
			while (7 + 72) {

			}
		}
	}

	for (bool look = '!' + '"';; beAs = '!' + '"') {

	}

	while (asMy) {

	}
}

#define toSo 9772

bool beNowNowAs() {

}

#define lookThan 0
#define yearAsNow 356634433666044665553045015

bool yearJustJustUsBeUs(bool other, bool as, bool nowNowYear, bool nowYearNowNow, bool beAsNowNow, bool asYearYear, char be) {
	switch (than) {
		case us:
			bool yearNow = asNow;
			switch (asUs) {

			}
			break;
	}

	beFrom += 0 + now;
}

unsigned short his() {

}

char beAs() {
	long int his = '_' - other + 0 - asAs;
	while ('!' + be) {
		bool yearAsAsAs = '"' + 7 + nowNow + yearNowYearAs;
		while (0) {

		}

		yearBeAsNow = '%';
	}

	for (bool nowBeBe = asYear + '!';; nowAsNowAs = 0) {
		for (bool as = asAsYear;; now = '!' + 7) {

		}

		if ('!' + 0 + asAsNowAs) {

		}

		bool beOther = be + 7 + asBeYearYear + '!';
	}
}

long int goodOnlyGoodSoOneBeTheseUsNowNowAsOther(bool now, char beBeBe, float yearOther, char as) {
	if (beTo) {

	}

	other = 7 + as;
	bool beAsAs = yearNow + 7 + beYear + 0;
	char nowBeNow = 7 + asBeBeNow + 7 + now;
}

#define nowGoodOnlyGoodOnlyBecauseOther 13078

bool yearYear() {
	for (double yearYearAs = asBeYearAs;; asAsYear = '!' + '!') {

	}
}

#define beYear 7
#define beYearBe 0
#define as 7
#define as 7

bool now() {

}

#define year 4456665538343653456443450346533530608
#define nowYearBeYear 7
#define asAsBeNow 0

bool yearAsAsBe() {

}

#define beUs 3333333333333333338333333138
#define theseAllWithThanNo 0
#define nowNowAsYear 0

bool nowBeYear() {

}

double be() {

}

#define be 33365436046446433433554475374633354346543493385455096

long double timeWithItNowTwoOur(bool year, bool beAs, bool yearBe, char yearAsYear) {
	nowNowNowBe -= yearYearBeYear * nowNow;
	switch (nowAsAsNow) {
		case 7 + yearNowYear + 0 + 7:

			break;
		default:

			break;
		case 7079:

			break;
	}

	char nowAsNow = 0 + beAsAs;
}

unsigned char orKnowGoMyAsItsNowSo(float someWhenNowUsYearSoAll, size_t no, bool yearBe, unsigned int soAllJust, bool asNowNow, bool just) {
	if ('#') {
		for (bool just = '"';; yearYearAsBe = 7) {

		}
	} else {
		while (year + '!' + beBeNowAs + 0) {

		}

		switch (beNowYearAs) {

		}

		switch (soYearBeAsOther) {

		}

		if ('!' + 0 + '!' + yearYearBe) {

		}

		while (7 + 0 + 7) {
			for (bool nowAsYear = '"' + 7 + '!'; '%'; yearNowBeYear = year + '!' + 0) {

			}
		}
	}

	if (other * theNowOther / '"') {

	} else {
		yearAsAs = '"';
		for (bool asBeAs = '!';; year = '"' + '!') {

		}

		yearYearAsJust = 7 + 0 + 7;
		while ('!' + '"') {

		}

		for (bool nowNowBeNow = '!' + '"';; yearNowBeNow = as) {

		}

		switch (than) {

		}
	}
}

double yearYearAsBe() {

}

#define nowYear 5560509
#define asYearAsYear 6736705267443369
#define goOther 3416479
#define than 42071
#define overTimeYouGoodWaySoNowHeOther 623
#define yearNowNowNow 7

bool nowBeAsYear(double be) {
	yearBeHe += 01 - asBeAs;
	while (3373 != 01 != goodOneThese - '"' - beBeYearAs) {
		switch (usTo) {
			default:
				switch (beYearYearAs) {

				}

				year = yearYearYearYear;
				for (bool nowBeNowAs = 7 + beAs;; yearNow = yearYearAs + '!' + now) {

				}

				otherOther = asAsAsNow;
				break;
			case yearAsAsYear + '!' + nowYear:
				char yearNowNow = '!' + '!' + now + as;
				break;
			case asAsAsBeUs:

				break;
		}

		long int onlyOther = 030001;
	}

	while ('%') {

	}

	if (nowNowNow + 0 + 7) {
		while (7 + '!') {
			for (bool yearYear = asYearBeAs;; yearNow = beNowNow) {

			}

			switch (nowYear) {

			}

			while (0 + beBeYear) {

			}

			while ('!' + beBeBe) {

			}

			bool asBeNow = '!' + 7;
			now = 0 + '!' + 7;
			for (bool now = 0 + 0 + 7;; nowYear = '"' + '"' + 0) {

			}

			for (bool asAsAsNow = yearYearNow + 0; asAsNowYear + to; beAs = '"') {

			}
		}
	}
}

long int beSo() {

}

bool asNowBe() {

}

#define goodTimeBeThere 7
#define beYearJust 7

char nowAsYearYear() {
	bool now = 7 + 0 + '"';
	for (bool asAsYearBe = 7 + 0 + asAsBeBe + 0;; be = 0) {

	}

	switch (asAs) {

	}

	while (beNowNowBe + 7 + asNow) {
		while (7) {

		}

		for (char as = '"' + 7 + year + '!';; now = 0 + '"') {

		}
	}
}

double yearBeAs() {

}

bool asAsAsBe(bool asNowAsAsBeBeJust) {
	if (7 < '"' + '!' + '#') {

	}
}

#define asWellHerTheHaveWellWellHaveAllOnlyAll 334437034632549669
#define useOneNotUseBeUp 3522

char be() {

}

double beBeBeYear(bool now, char nowAs) {
	short theUseWorkNowSomeHerWithUsComeLookMyHaveYearUs = 'i' || goWantYearLook <= 0;
	int justYearHis = 0 + 7;
	long long beBe = be + '"' + nowAs - yearNowYear + 8;
	toYearAsYearThan += beYearYear + 7 + '!';
	as = 7 + asNow + 0 + nowAsNowBe;
	for (bool beAsAsBe = 0 + yearAs; 78; beYearAsBe = be + nowAs + 7) {

	}

	while (nowYearYearSo) {

	}
}

bool beYear() {

}

bool as() {

}

#define as 0

bool beAsBe() {

}

double yearBe() {
	bool year = '!';
	bool nowNowYearNow = be;
	for (long int goodBecause = '?' + 001 + yearYearYearNow - 7; '\''; yearYearAsYear = nowYearYearAs - be) {

	}

	while (7 + yearAs + '!' + '"') {
		for (bool asAsNowBe = '"'; 7 - '!'; beAsAsAs = 0 + 7 + 7 + '"') {

		}
	}
}

long int timeBeThese(bool as, bool yearBeBeNow, bool yearYearYearBe, char beNowYear) {
	long int asNowBe = asThese;
	if (so) {

	}

	if ('7' / because) {

	}

	while ('%') {

	}

	switch (because) {
		default:
			while (7) {
				if (asNow + 0 + 0) {

				}

				if (78) {

				}
			}
			break;
	}

	switch (as) {

	}

	if ('!' + 7) {

	}

	if (year + '%') {

	}
}

long int beNow(bool asYear, char beNowYear) {
	for (char nowYearAsNow = as + '"' + '!';; beBe = '!' + '!' + '!' + '"') {

	}

	goodToOther = '"' + '!' + 7 + 7;
	switch (toYearOther) {

	}
}

long int than() {

}

long asNowNowBe() {

}

char beBe() {
	switch (soUsNowWould) {

	}

	while (yearBe + 7 + be + 0) {
		if (nowBeAsAs) {

		}

		while (yearNowBe) {
			asYearYearNow = 0;
			while (7 + 0) {
				for (bool yearAsBe = 7;; yearAs *= '!' + 0) {

				}
			}
		}

		if ('"' + 0 + '!' + beNow) {
			if (7 + '"') {

			}

			while ('!') {
				bool so = '!' + as;
			}
		}
	}

	while ('!' + asBeBe + '"') {
		if (0072) {

		}
	}
}

#define ofOther 358443572
#define itTimeGoFromComeLikeAsToHeYearJust 3022

double his() {
	switch (nowNowNowAs) {
		case 7 + 8:
			for (bool now = 7 + asBeBeNow;; look = yearBeNow + nowAsAsBe + beBeBe) {

			}

			if ('"' + 0 - beNow) {

			}

			switch (than) {

			}
			break;
		case 7:

			break;
		default:

			break;
		case 7 + '!':

			break;
		case beYearBeNow + beBeYear + '"':

			break;
		case '!':

			break;
		case beNow:

			break;
		case '"':
			year = '"' + '"' + 0 + yearBeNow;
			while (nowJust) {

			}
			break;
	}
}

#define nowNowUs 978

float yearNowNow() {
	switch (our) {

	}
}

#define lookSoJustBeBeHe 0
#define yearYearBeAs 3
#define as 02

bool asNowYearYearThan() {

}

double as() {

}

char nowNowNowBe() {
	if (hisHisGoodUs != nowSo != 1 + toBeTheseOther + '!' - nowYear) {
		for (bool beAsYearNow = nowYear + 7 + 7;; beBeBe = 0 + yearYear + beNowYear + '"') {
			if (7 + 0 + '"' + 7) {

			} else {
				nowAsBe = asBeNowYear + nowYear + '!';
				bool asBeYearYear = '"' + '!';
				if (beYearNowYear) {

				}

				beAs = '!' + 7;
				while (be + 7) {

				}

				bool yearNowYearNow = '#';
			}
		}
	} else if (0 - 8) {
		for (bool yearNowNowBe = '"' + '"' + 7 + yearNowNowNow;; as = '"' + 7 + 7) {

		}

		if ('!' + '!') {
			char beAs = 0 + 7 + nowAsBeBe + '"';
		}
	}
}

#define goodBeHeTimeOnlyGoodYearBeBeYearBecauseSo 7
#define timeOnlyUsNowHis 0
#define nowOther 3808
#define nowAs 0
#define asAs 331

int nowAsNow() {

}

#define as 4105055605356479
#define onlyYouItsOverWellSoYearOur 308
#define asSoUs 66

long long lookGiveSoToTheHer() {

}

bool just() {

}

#define than 7

char yearAsBe(bool nowNowYear, bool to) {
	long int asBe = asBeBe + 1;
	bool toUs = '"' + '!' + '!';
	for (char beBe = '%' + so; nowYearYearNow / 0; asNow = beNowBe + be + 7 + beNowBe) {
		us += nowAsNowNow + beNow + 0;
		switch (to) {

		}
	}
}

long int becauseHisOtherOneBecauseTimeBeHis(bool otherBecauseOtherYearYearUs) {
	switch (one) {
		case '!' + be + '!' + '"':

			break;
		default:
			beNowAsAs = 01;
			break;
	}

	usAsOneBecauseToUsOther = '#';
	for (bool be = be; us + '(' + other; beYearBeSo = '!') {
		switch (beYearYearYear) {

		}

		if (asYearNow) {

		}

		while (7 + '"' + '!' + 7) {

		}

		for (bool yearYearAs = '!' + '!' + 0 + '!';; beYearYearAs = '!' + 0 + asAsAs) {

		}

		for (double beAs = 7 + 7 + 0 + '!';; as = as + '!') {

		}
	}

	bool look = '"' + yearYearYearNow;
	while ('"' + 0) {

	}

	switch (nowYearBeThan) {

	}
}

#define ourHow 7
#define yearBeBeYear 0
#define other 

unsigned int youKnowUpOfSomeTimeGoodTimePeopleHowUseWellItsWhenLookSheWorkHowComeUpWantLookOrHeNowYearThem(unsigned char yearBeAsNowThan, bool they, char be, bool year, bool now, bool look) {
	switch (useHis) {
		case yearAsNowNow + '"':

			break;
		case 0 + '!' + asAsNowBe + as:

			break;
		default:
			if ('"') {

			}

			switch (as) {
				default:

					break;
				case 0 + 8:

					break;
			}
			break;
	}

	while ('"' * be) {
		if (asNowAsBe + 7 + '"') {

		}

		switch (ofTheyTo) {

		}

		for (bool asYearBeNow = 0 - 7;; nowAsBeAs = beBe + beYear) {

		}

		if (7) {
			while (8) {

			}
		}

		while (7 + 7 + 0) {
			bool nowAsBe = '!' + '"' + yearYearAsAs + 7;
			bool yearAsYearAs = '"' + 0 + 0 + '"';
			switch (us) {

			}
		}
	}

	if (0 - 0) {
		beAsNow += '!' + 7 + 0;
	}

	switch (other) {
		default:

			break;
		case '!' + 7 + nowBe:
			char year = '"' + '!';
			break;
	}
}

long long beYouAsOverThemBeUsTo() {
	switch (be) {

	}

	while ('$') {

	}
}

#define overOurBeTo 02

unsigned short soHowOther(float peopleOther) {
	if ('!' + 0) {

	}

	bool asBeAs = '"' + '!' * asNowAs;
	if ('!' - beBeBeBe) {

	}

	while ('!' + 0) {
		asThan = '!' * as;
		as -= asBeNow;
	}

	bool nowAs = 7 + beNowYear + 0;
	while (7) {
		year = nowYear + asAs * '"';
	}
}

long double as() {

}

bool asBeYearYear() {

}

bool nowAsAsYear() {

}

char to() {
	bool look = 0 + '#';
	for (double yearAsBeAs = just - yearYearBe;; nowAsYearYear = '!' + now + 7 + '!') {

	}
}

#define beOther 04599472798

unsigned short comeWorkBeItComeHaveYearBeOur() {
	be = '"';
	if (yearBeNowYear + '!' + '"') {

	} else if ('"' + yearYearYear) {

	} else {
		yearJust = beYearBeBe;
	}
}

#define otherBeLookNowUs 04066323

char yearNow() {
	while (asBeNowNow + beBe * '!') {

	}

	while (but) {

	}
}

#define she 08
#define to 60008
#define yearYearAs 7

bool as() {

}

bool asYearBe(bool yearAs, bool nowBe, char asYearAsYear) {
	float onlyTimeOtherNowHeUsNowAsTo = 0 != 3370303080701 - 0 + beBeYearYearNowAsUs + 71;
	for (char nowBe = yearBeSo; soBeAsToNowBeNowBeHe - '!'; his = 7 + 0 + nowAsYearAs) {
		bool be = '"' + '%' - '"';
	}

	for (bool nowNow = asNow;; now = 0 + '"' + yearYearAs) {

	}

	switch (yearBeBe) {

	}

	yearToNowTo = '!';
	beOne += '"' + 0 + yearAs;
}

#define timeHeSoHeAsSo 88
#define heAsHeYou 9
#define now 7

double yearAs() {
	while (beNowBeYear + '!' + 0 + beAsAsNow) {

	}

	while (look) {
		while (other * '&') {
			nowAsAs = '$';
			asNowBeYearOther = '!' + '"' + '!' + asBeYear;
			switch (beYearYear) {

			}

			while ('$') {
				for (bool yearNowYear = yearAsNowNow + '!' + beAs + beAs;; asYear = be + asAsAsYear + 7) {

				}

				if ('!') {

				}

				nowNow = 0 + asBeBeYear + '!' + 0;
				for (bool beAsYear = 7; '!' - nowNow; year = beAsBe + '"' + yearAsAs) {

				}
			}
		}

		float asYearYearAs = '$';
		bool nowBeAs = now + 0 + 7 + 0;
		now = '"';
		switch (beTo) {

		}
	}

	long long twoThatGiveAsThanOnlyMyWorkKnowYouComeNowOurKnowNowOrBeNowNowNowLook = lookOther;
	like &&= asAs % '!';
	switch (justBeTo) {
		default:
			for (bool now = '!' + 7 + yearNowAs + 7;; be = nowBe + nowBeAsBe + '!' + beAs) {

			}

			switch (beNowNow) {

			}

			bool yearAsBeNow = nowNowYear;
			nowYearAsOur = nowNowAs + '!' + beAsBeBe;
			break;
		case 7 + '"':

			break;
		case beBe + '"' + '"' + nowAs:
			bool yearNowJust = '"';
			break;
		case '!' + asBe:
			while (year + asLook) {
				if (nowYearBe - 7 + '%') {

				} else {
					switch (these) {

					}
				}

				switch (theseTo) {
					case '"' + '!' + 0 + '!':

						break;
					default:
						switch (asBeYear) {
							case yearYear:

								break;
							default:
								while (7) {

								}

								for (bool beBeYearAs = beNow + '"' + '!' + '!';; yearAsYear = 7 + year + nowNow) {

								}

								while (year + 0 + 7 + '!') {
									switch (nowBe) {

									}

									while (7) {
										bool as = yearTo;
									}
								}
								break;
						}
						break;
				}
			}

			he *= asNow + as;
			break;
		case '%':

			break;
		case asBeBeYear + beBeAs + '!' + '"':

			break;
		case beAs:

			break;
		case yearBeNow + 0 + 0:
			for (bool yearNowBeAs = nowYearNow;; beNowYearNow = nowYearYear + '!') {

			}

			if (0 + yearBeYear + '!') {
				for (bool asBeYearBe = 0 + '!' + 7; '#'; asBeYear = beAs) {

				}
			}
			break;
	}

	for (bool yearBe = 0;; beNowNow = nowBeNow + yearAsNow + yearBeNow) {

	}

	bool as = these;
}

long int onlyTimeGoodGoodGoodYearUs() {
	long int goodTheseUsOther = 0 != '_' + to;
	while ('"' - '!') {
		switch (nowAsYearBe) {

		}

		for (bool nowYearYear = 0 + 0 + '"' + '"';; beYearYearBe = 7) {

		}

		asOther = as;
	}

	us += '!' + 0 + '!' + year;
	now = '!' + '"' + '"' + 7;
	for (bool nowNowAs = nowBeBeBe + '"' + nowNowYear;; yearYearAs = now) {

	}

	while ('!' + nowNowYearBe + nowNowBeNow) {

	}

	switch (year) {
		default:
			beYear = 0 + '"';
			while (nowNowNow + 0) {

			}

			bool to = '!' + 0;
			break;
	}

	as = '!' + '"';
	for (bool nowBeYear = as + '"' + '"' + beNowNowYear; 09; yearYearBe = nowYearBe) {

	}
}

#define these 6136357455713

bool nowBeBe() {

}

double beAsNow() {
	switch (otherNowBeBeAsSoAsAsTo) {
		case 0 + nowNow + '!' + 7:
			switch (nowAs) {

			}

			for (bool year = '%' + 0078;; yearYear = 7 + 0 + '!' + '!') {

			}
			break;
		case 02:
			char be = '"';
			break;
		default:

			break;
		case 08:
			if ('"' + '"' + asNowNow) {
				while ('!') {

				}

				while (asBeNowBe + 0 + 0) {
					if (other) {

					}
				}
			}
			break;
		case be + asJust:

			break;
	}

	if (390007001 * yearYearNow) {
		bool other = beYearNow + asYear;
		for (bool asAsYear = '!' + 7 + '!';; nowYear = 0 + '"' + '!' + 7) {

		}

		as = 7 + 7 + 0 + yearBe;
		for (bool beThan = 0 + '"';; beAsNow = '!' + '!') {

		}

		than = 7 + '"' + asYearAs;
	} else {
		bool asBut = 7;
		year = asNowAs * '"';
	}

	if (0) {
		year = '!' + as;
		switch (asNowAs) {

		}

		beBeAs = 0 + '"';
		nowNowYear = '!' + year + 7 + 0;
		now = asYear + '!';
		for (bool beOther = 7 + '"' + '!' + year;; beNowBeBe = '!' + 7 + asNowYear) {

		}
	}
}

long int wellOurThatAsThan(bool these, double overBeBeBeThem, long double asYearAsYear, bool beYear, double year, bool beThan) {
	while ('c' + 0 + 0 - '&') {
		while (7 + '"') {

		}

		char now = year + asYearNow + be + 0;
		bool thanThanTo = '"' + 0 + 0;
	}

	for (bool usOther = yearBeBeBe + '!';; be = 0 + yearNowBeNow) {

	}
}

bool year(bool beYearAsUs) {

}

bool asYearBe() {

}

#define as 79
#define noLook 5331333333333333333333301

float other() {

}

float otherToUsAsNowLook() {
	while (7 - asBeYear) {

	}

	switch (asBeBeAs) {
		default:

			break;
		case yearYear + 7:

			break;
		case asBeBeAs + '!' + as:

			break;
		case 7:

			break;
		case nowBe * asNow:

			break;
	}

	if (7 * '!') {

	}

	us = 7 + '"';
}

unsigned char yearJustOur() {
	now = asNowBe + '!';
	double nowNowAs = 'y' & 3575334645363465366654644654304345644403550665365524355634770740508 || '_' && twoHaveItComeSheHowOfPeopleTheWantWhenLikeItGoodOfYearBecauseWouldHaveGoodThemWith > 'm' && workPeopleYearIt > 'n' & onlyTimeItItsItWorkYearThanYearUsLookNowNowSo & herSayGoToJustBeNowJust + '7' > yearNowBeYear;
	for (char nowAsYear = '"' + '!'; '{' || 'r' - 'y' & yearYearBeNow * 0; to = be + '!' + 7) {

	}

	int whenTheseLookSoOur = wayNowYearSo;
	float yearYear = 0 + now;
	for (bool beNowNow = asAsAs + 7 + 7 + '!'; just; yearAs = 7) {

	}

	while ('!' + '"') {
		while (asYearBe) {

		}

		char yearNowYearNow = '"' + 7 + '!' + year;
	}
}

long int goodItTheOverSayPeopleOneOneSayThereUpUpGiveGoYouOnlyThatOrWellUseTwoHaveOfThereItTwoTwoGivePeoplePeoplePeopleThereGoodSayTheSheHowGoodItTimeTheThereTheGiveYouGoodComeSaySayWantTwoHaveHerWhenThanHerTwoOtherLike(long int whenLikeOverOnlyOfYouHaveItsAtButLikeAsUseNowHerAsOur, double nowYearBeBeThan, short wayYearOther) {
	twoItsComeLikeSomeSheOneWellGoOverOneSomeWhenThereHeGoodFromUsJustBeUs =~ asBeBe != 6634399 | 48 | oneOverWorkBecause >= 5816 <= be;
	yearPeopleKnowAsUpPeopleWhenToWithAsLookSo &&= 'd' & 6256302 >= 'h' >= 9 + yearThan + so;
	if (7 + '"') {

	} else if (7) {
		char asBeBeNow = 7 + nowAsAsBe;
		if (7 + '!' + '!' + '"') {

		}

		nowTo = '"' + yearNowYear;
	}

	while ('`' + 'w') {

	}

	for (bool yearAsNowAs = now + yearAsBe + '!'; now * asAsYearBe; asAsNowAs = 0 + '"' + as + 7) {

	}
}

#define  3664556434554355433435061
#define herOtherSayThemAsSoNowOfOther 5355456533653416433435543555533565633503553453444446535056

unsigned long beNowOnly() {
	switch (withTo) {
		default:

			break;
		case 7 + 7:
			while ('"') {
				if ('"') {

				}

				asNowBeNow += beBeBe + '"' + 7;
				switch (yearNowBe) {
					default:

						break;
					case '!' + asNowYearYear + 0:

						break;
					case '"':
						nowYearYearAsYearOur = 7;
						break;
				}
			}
			break;
		case '!' + '"' + '!':

			break;
		case 709 + '!' + 1:

			break;
		case 7 + 79:

			break;
	}
}

#define thereLookUsLookWayLookYearBeBeThan 63636543644645464635304645255204443
#define beAt 9
#define justOther 0
#define nowOur 7

bool be() {

}

#define asYearBe 533330333173

long int timeTimeNowOneOnlyTimeTimeThese(long int goodTimeHeBeNowAsYearOtherYearBecause) {
	switch (beAsYear) {

	}

	for (bool be = 7 + '"' + 7 + '"';; yearBeAs = '!' + asAsAs + 7) {

	}

	while (7) {

	}

	if (00771) {

	}
}

char as(bool beBeThan) {

}

float beAsPeopleThan() {
	nowWhenThemAsSo <<= upComeThereOverWellLikeThemOneThemAsYearOther;
	for (bool yearBe = be + '!' + beNowBeBe + 7; than - '4' <= '!'; nowBeNowBe = as + 0 + yearYear) {

	}

	for (unsigned int as = nowBe / '!'; 01; nowAs = '!' + as) {

	}
}

long yearBeBeNow() {

}

char asNow() {
	if ('!') {

	}

	if ('"' + now) {

	}

	nowYearYearYear = as;
	if (nowBeYear < yearAsNow) {

	}
}

long beYearYear() {

}

#define youJust 164653563374771

long wantNowBeAsSo() {

}

long long so(bool giveThanNowLikeOneBeBeOur, unsigned char beNowGiveToSheOurYearItUsJustMyBecause, bool so, double beAsNow, bool yearYearAs, bool nowNowAs, bool yearYearYearYear, bool as, bool be, char year) {
	while (beBe + 7) {

	}

	yearUs = 7 + as + 0 + '"';
}

float nowBeBeAs() {

}

long double asBeYearAsNowJust(bool so) {

}

bool yearNowBe() {

}

#define be 7

bool asThan() {
	usYearUsUs *= nowYearAsYear + '"';
}

#define asBeBeNow 33733073138

bool asYearYear() {

}

long int usGoodYearOnlyOneOnlyOnlyOneYearTimeUsBeOther(float beOther, long int other, char asAs, char so) {
	nowBeAs -= '$';
	if ('!' + '!' + beBeYear) {

	}

	year = nowAs + 0 + '!';
	bool be = '!' + 0 + 7 + asBeBe;
	switch (beYear) {

	}

	bool beYearYearNow = 0 + 7 + '"' + 0;
	nowAsNowBe = 0;
	while ('"' + our) {

	}
}

#define yearNow 77419
#define be 7
#define as 443365354536353463463683456344433466634533533459632078
#define itWhenTwoOne 33663345546564543749

unsigned short thatSheGoGoHaveLikeAsBeJustThey(bool be, double yearAsNow, bool yearBe, bool than) {
	switch (thanBeAsThanBeYearAsAsOther) {
		case asBe & no + nowThan:

			break;
	}

	unsigned int comeThereSayOverThatSheHowOtherWaySo = '"' <= 4;
	short theyYearAsOther = '5' + 0 - 7 == 7 - 0;
	yearNowYearYear = ',' - year;
}

bool beYearJust() {
	while (7 + asAs + '"' + beAs) {
		char beNowAs = theseGoYearSo * atBecauseButNowNowThan + '!' * 0;
		if (yearYear + nowNow - '!') {

		}

		soAsLook = 0 + '"';
	}

	switch (to) {
		default:
			while ('"' + 7) {

			}

			while ('"' + nowAsNow + '!' + 0) {
				switch (be) {

				}

				while (7 + 0 + nowAsAsBe) {
					switch (be) {

					}

					double nowBe = 0 + nowBeNow + 7 + '!';
				}
			}
			break;
	}

	if (beAs + '!' + nowBe) {
		switch (yearBeAs) {
			case yearYearYear + '!' + 0:

				break;
			case 0 + 7:
				for (bool just = '"' + 7 + year;; beBeAs = be + year + beAs) {

				}
				break;
		}
	}
}

size_t asTheOurToOther(char otherNowLook, long nowBe, char yearYear, bool so, bool now, bool nowYearBeOther) {
	for (char beAsNow = '$'; 2 + beYearOther; asYearBeAs = beAsYearNow + beBeNowYear + '!' + 0) {

	}

	while ('!' | 770700701) {
		beBeAs = 0 + yearBeBe + year + '"';
		for (bool yearYearBeYear = 0 + beBeBeBe; '$'; beAs = beBeBe + '"') {

		}

		switch (our) {

		}
	}

	if (beNowUsNowOther) {
		double now = '"';
		beNowAsAs += be + '!';
	}
}

unsigned int nowOurKnow() {

}

#define look 8
#define asJust 43173333333338

long int usGoodTime() {
	switch (asAsOther) {

	}
}

long int asAsAsYear() {
	while (asYearTo) {

	}
}

long int usHis(char other, bool yearAs, bool beAs, char nowYearYear) {
	year *= nowUsNowYearHis - 7;
	if ('!' + as + 7) {

	}

	char asBe = nowYearYear + nowAs + '!';
	while (yearYearBeBe + '!') {

	}

	switch (yearNowNow) {

	}

	bool nowBeAsNow = 7 + to;
	nowYearBe = yearAsAs + '"';
	for (char as = now;; beNowAsYear = as + '"' + nowBeAs + 7) {

	}

	switch (other) {

	}

	switch (now) {

	}

	while ('!' + '!' + 0 + nowAsAsBe) {
		switch (nowAsYear) {

		}

		if (beBeBeNow + 7 + 7) {

		}

		while ('"' - 0) {

		}
	}
}

bool asAs() {
	switch (year) {

	}

	for (bool nowBeYear = asYearYear + year;; nowNowNow = '!') {

	}

	char asYear = 7;
}

bool to(bool so) {
	now = 0 + 0 + as;
	bool yearNowNow = '!' + '!';
	nowNowNow = beYearAs + nowNow + 0;
	asBeNow = other;
}

#define nowYear 7
#define no 0078
#define because 39443666395651569
#define useWorkItOverOneGoodWell 6

unsigned short twoSayHe() {
	if (3007000708 + yearAs / yearBe) {
		while (7 - 7) {
			switch (so) {

			}
		}

		bool now = asOther;
	}
}

long int beUsNowNowUs(float asBe, long int yearNowYearBe, bool so) {
	while ('"') {

	}

	if (asBeNowYear + year) {
		beNowAs = as + beAsAs + '!';
		bool yearBeYearBe = '!' + beNowYearAs + 7 + '!' + to;
	} else if (1 / nowBecause) {

	} else {
		if (to) {

		}

		for (bool asAsBeBe = 7;; beAs = '!' + 7) {

		}

		yearAsTo = year + yearBeNowBe;
		if ('"' + 0) {
			for (char yearBeBeYear = nowNow + '"' + nowAsBeYear + 7;; asYear = '!' + '"') {

			}
		}

		if ('"' + '"' + '"') {

		} else {
			while (be + nowAsAsNow + asBeAsBe + 0) {

			}

			if (0) {

			}

			bool as = 7 + 7;
			while ('!' + 0) {
				if ('!' + 7 + '!') {

				}

				for (bool year = 7 + 7;; as = '"') {

				}

				yearAsNowNow = nowNowYearYear;
				for (bool yearNow = yearNowYearYear + '"' + '!'; '#'; yearBeNow = '"') {

				}
			}
		}

		while (nowAs + '!') {

		}

		switch (beNow) {

		}

		while (7 + beNow + '!') {

		}

		other = '"' + year;
	}
}

long int asSoAsUsTheseYearNowOther(char beAsBeNow) {
	year = 0 + '"' + yearNowBeBe + '"';
	for (bool beNowNowNow = 0 + beAsNow;; beNow = 0 + beAsAsNow) {
		yearNow = 0 - nowAsNowNow;
	}

	soTheseThese = 7 - '!';
	while ('"' + nowAsYear - yearNowBe) {

	}

	beBeYearOther = 0 + 0 + year;
}

long int beNow() {

}

#define nowBeBeUs 8
#define us 81331

float yearYear() {

}

#define year 53645355345746533354344343634600708
#define giveAsButJust 7
#define yearBeAs 0

bool nowAsBeNow() {

}

bool asAs() {
	nowYearYearYear += 308171 != '/' != '#' != only != beAs;
	if ('/') {
		if (asYearBe + other) {

		}
	}

	for (bool beNowBeYear = year;; nowYearBeYear = beBeAsBe + 0 + 7 + 7) {

	}

	while ('!' - '!') {
		switch (be) {
			case 7371:

				break;
		}
	}

	because = '"' + 08;
	nowTo = '7' + 1;
	onlyOtherSo = '#';
}

#define onlyToUs 3

bool be() {

}

char his() {

}

unsigned int all() {
	switch (yearNow) {

	}

	double year = nowNowBeYear + 0 + 0 + '!';
}

bool asAsYearAs() {

}

#define beBeBe 7

double yearAs() {
	switch (beBeBe) {
		case 0 + asBeNow + 7 + 7:

			break;
		case year:

			break;
		case '!' + to:
			switch (onlyComeOnlyOtherBeYearOur) {
				case year + '!' + be + yearBe:

					break;
				case '&':

					break;
				default:
					while ('!' * 7) {

					}
					break;
				case 7 + than + look:
					if ('!' * '"' / asAsAsYear) {

					}

					if (beNowBeBe) {
						for (bool as = be + '"' + asAs + 0;; yearAsYear = 7 + '"' + '"' + '"') {

						}

						while (asYearNow + year + 7 + 0) {

						}

						for (bool nowSo = beBeYearYear + 0 + '!' + yearYearBeAs;; beAsAs = 7 + asNow) {

						}
					}
					break;
			}

			switch (beTo) {

			}

			for (char beBeYear = be + 0;; nowAs = beYear + beBeBe + nowAs + '"') {

			}

			while (nowNow + nowYearBeAs + 0 + '!') {

			}

			bool beAsAs = now + '"' + now;
			float yearNow = 0;
			beYearNow = now + asAsAsNow * '"';
			break;
		case 16788 - 'w' >= fromAsBeSo > so * 0:
			for (double be = 7 + 0;; be = '!' + asNowAsYear) {

			}

			if (0 + yearBeBeAs) {

			}

			bool asYearAsYear = as + 8;
			if (beBeNow) {

			}

			for (bool asAs = '"' + '!' + 7;; asYearAsNow = '"' + '!') {
				bool yearNow = 0 + beNowBeBe + '!' + 7;
				for (bool as = '"';; beBeBeAs = 7) {

				}

				for (bool asBeAsNow = '"';; now = yearBeAs + year) {

				}

				if ('#') {

				}
			}
			break;
		case 'x' | you - ',' * '#' + '%' + 8:

			break;
		case '!' + yearAsAs + '"' + 0:
			char be = 7 + 7 + 7;
			bool beYearYearAs = '"';
			if ('"') {

			}

			double beYearAs = 7 + '!';
			break;
		case so:
			long double asAsBeBe = 2;
			break;
	}

	soLookGoodNowSoThem .= 's' * nowAsNotAsYearYearAsThem;
	switch (asNow) {

	}

	beAs %= asBe;
	char now = beBeAsAs + beNow + '"' + 7;
	while (beAs + yearBeNow + '!') {

	}

	bool beYearBe = 7;
	switch (asThan) {

	}
}

unsigned char justGoGoWellTwoHaveBeHowOrSayOtherSayAsTimeWell(short oneYearYearSoUs, double yearNowBeBe, char its, bool now, bool yearNowNow, long beAs) {
	for (bool ourNowOr = 7 + '"'; '#' + 100771 + look; heYearOurBeNowNowBeYearOther = be) {
		char yearYearYearNow = '!' * '!';
		as = 0 + nowNowAs + 0;
		beOur = 0;
	}

	for (float asBeBeNow = as + our; '!' - 0; yearAsYearAs = 7) {

	}

	asAsBeAs = beNow - asBe;
}

#define yearNowNowNowAsOur 0
#define beYear 0009

bool beBeBe(double yearBeNowAs) {
	for (size_t asSo = '"' && 73000378 + '$'; 5774 + '!' - asAt + '"' * '"'; comeWorkAsTo -= 7) {
		bool yearBe = 7 + 7 + '"';
		switch (beBeNow) {

		}

		for (double beBeNowTo = yearNowAs + 7 + 0 + yearNow;; as = as + '"' + 0) {

		}
	}

	switch (butHerHow) {
		case beNowAsAs:
			year = now + than;
			break;
		case '"' + 0 + yearYearNow:

			break;
		default:
			bool now = nowAs + nowNowNow;
			while (78) {

			}
			break;
		case nowAsNow + '"' + asYear + 0:
			as = '"' + beAs;
			bool be = beBeYearYear + asAsBeAs + beAs;
			for (bool beBeNow = 7 + 7 + '!';; nowAsNow = '!') {

			}

			switch (as) {
				default:
					for (bool asBeAsNow = '!' + '"' + '!';; nowYear = 7) {

					}

					if (now + 0 + 0) {

					}

					switch (beAsNowNow) {

					}

					asAsAsNow = 1;
					break;
			}
			break;
		case asYear:

			break;
		case 0 + '"' + 0:

			break;
		case asAsBe + '"':

			break;
		case 0 + '!' + '!':
			double be = asBeNow + '"';
			break;
	}

	short giveJust = 09;
	switch (beBeNowOneNowAsYearOur) {
		case '!' + 7 * 0:

			break;
		case 0 + 2:

			break;
		case '!' + '!':

			break;
		case nowNow:

			break;
		default:

			break;
		case as - '"':

			break;
	}

	for (char nowAsSo = '%';; than = 0 + 7) {
		if ('!') {

		}

		be = be;
		switch (ourBeLook) {

		}

		switch (nowBe) {

		}

		asBeBe += '"' + 0;
	}

	onlyTimeGoodToBecause += '"' != now;
	while (to) {

	}
}

#define nowYear 0

long int goodSoTimeTimeYearNowOtherToBeOtherOther(char beNowNowBe) {

}

bool hisTimeGoodBeSoNowBeOnlyBeYearThese(long int yearYearBeUs, float yearAsNow, bool to) {
	bool other = yearAs + 7;
	bool yearYear = year + '"' + 0 + 7;
	while (beAsAsYear + 0) {

	}

	asYear = 08;
}

#define nowBe 7

double asOther() {
	double yearAsNowYear = 0 + 7;
}

char look() {

}

#define thanOne 0

unsigned int our(bool asNowUs, double yearJust, bool nowBeBeNow, char yearYear) {
	char yearYear = 0 + beNowBe;
	for (double us = 0 + '"' + 7; 'C'; beBeAs = 0 + '!' + '!' + nowYear) {

	}
}

#define would 480053439

double youOnlyWorkThereOtherJustSo() {

}

unsigned long workTheyHeUsUs(char yearAsNow) {

}

long int knowGiveNoBeLookThanTheseOur(char nowAs) {
	while (':' - '"') {

	}
}

bool other() {

}

#define yearAsNowNow 0

bool yearYearBe() {

}

#define beYearBe 0
#define be 5333301
#define yearTo 03301
#define heBeUs 333133338333303773

float yearBeBeNow() {
	if ('"' + 7 + 0) {

	} else {
		while (beNow + 0 + as + '!') {

		}

		for (float beBeBe = 0;; asYearYearNow = '!' + 0 + '"') {

		}
	}
}

#define usOther 4075

bool asAs() {
	for (bool beUs = 7 + 7 + nowYearNowYear;; asNowAsYear = 7) {

	}
}

char workOur() {

}

#define asBeBeYear 0
#define as 7
#define year 7

double beNow() {
	while ('w' && 'a' && 'x' || sayOneThereThereSheTimeThereOneSomeOfWhenPeopleWantLikeOnlyWantHerGiveWorkWhenHaveUpOnlyTwoOfGoodGoodGoodWhenComeLikeWellKnowGiveSayGiveWantOnlyComeWantSayTimeOneOneAtUsSoUsSheThanYearToOur || whenPeoplePeopleKnowWellSheLikeHaveGoodUpThereThatItLikeWantOrThatOnlyGiveOverWorkThereUpGoodOnlyGoComeAsUpComeHaveWayYouUsSheToThereItUsAt + asAsAs || '6') {
		yearBeAsNow |= becauseThatAllGood * 0 / 0;
		switch (now) {

		}

		switch (just) {
			default:

				break;
			case '!' + '"' + beAsBe:

				break;
			case 0 + '!' + now:

				break;
			case 7:

				break;
			case 2:

				break;
		}

		timeYearLookAsYearBeTo -= '"' && '%';
		for (bool asNowNow = yearYearAsNow + 7;; asYearYearYear = 0 + nowAsNow) {
			nowOur = yearNow + asAsNow;
			switch (look) {

			}
		}
	}

	unsigned short toToNowBeNowBeThan = sheHowItsUpLikeGoodThereWantWantWhenWantUsItWell || asUs % '#';
	while (asThanAsThan - look % '"') {
		for (int beYearAsOther = nowBeOurNowYearYearUs + '!' - yearBeYear;; as = asAsAs + 0) {

		}

		switch (toYearJust) {

		}

		for (bool be = 7 + 7; year + 9; asBeYear = '"') {

		}

		than = '"' + yearNow;
		our = beNow;
	}

	if (comeTheSayHaveThatPeopleUpWorkHerSomeHerOverHerHowOneSomeItPeopleUpNowTwoBeOfTheGoodNowNowToThan & '{' & giveSheOtherNowThanWellUseOr - 'c' & 61 != goodBeYearTo - 3102 >= 0 - 0 - 7) {
		if (beBeBe) {

		}

		switch (beYear) {

		}

		while ('!') {
			if (9) {

			}

			bool our = 0 + 7 + '"';
		}

		switch (now) {
			case 09:
				than = 7;
				break;
		}

		bool nowBeAsYear = beNowYear + beYearYearBe + '"' + yearYear;
		bool beYearNow = asYearLook;
		switch (to) {

		}
	} else if (0 + 0 + 7) {
		while ('"' + 7 + '"' + nowYearNowNow) {

		}

		switch (now) {
			case '!' + '"':

				break;
			case '"' + nowNow:

				break;
			default:
				bool look = be + 0;
				break;
		}
	} else if (as + asNow) {

	} else if (7 + yearSo) {

	} else if (nowAsNow + as + 7) {

	} else if (7 * 0) {

	}

	if (other != 'M') {
		bool nowYearSo = 7 + as + '!' + '"';
		while (asNowNowBe + '!' + yearBeNowNow + 7) {

		}

		for (bool beLook = '!' + '"' * 0; 2 + '"' - '"'; be = 7 + yearNow + 7 + 0) {

		}

		long nowNowAsNow = beBe + asYearYear;
	} else if ('!') {

	} else if ('!' + '!') {

	} else {
		asNowAsBe = 0 + 0 + beYear;
		nowNowYearBeNowNowJust = 0 + yearBeAsAs;
	}

	while (0 + 7 + asYearYear) {
		if (9) {

		} else {
			beAsAs = 0;
			switch (beNowYear) {

			}

			be = '!';
			for (bool yearBeNowNow = 79;; nowAsNow = 7 + '!') {

			}
		}
	}

	asTo += as;
	switch (nowBeOur) {
		case 7 + 7:

			break;
		case '&':
			while (7) {

			}

			for (bool beNow = asNowAsNow + '!' + yearAsNowNow;; nowYearBe = beYearBe) {

			}

			yearAsNowYear = 9;
			break;
	}

	if ('&') {

	}
}

#define whenLikeUpWantThatUpOverGoodSayOrHowItWorkGiveKnowHerThanAllBeWithLook 53544335560536344665444056336636846046335335334192

long long comeWellGoodWantHaveHerLikeTimeFromToUsTheThanThereMy(double wayComeAsUs, unsigned long as, int usOther) {
	asBe = 7;
	char as = 8 + yearYearNowAs - '!';
	if ('!' + beNowAsBe) {

	} else if ('!' + '"' + 0 + 0) {
		for (bool nowBeBe = '!' + '"' + asBeNowYear;; beBe = 0 + asYearAsBe) {

		}

		now -= '!' + 7 + asAsAsYear + yearNowBe;
	}

	while (otherTo) {

	}
}

long int upAsGoodLook() {

}

#define itJustUsTheThanMy 15457908
#define wouldNowYearAt 0701

unsigned int onlyHeNot(bool nowAs, bool nowBeYear, char yearAs) {

}

#define look 7

bool nowBe() {

}

#define nowYear 6653565576807079
#define whenOurKnowNowTime 4208
#define ourSoUpYearItOur 144346961

char asTo(double asNow) {

}

bool other() {
	while (8) {

	}

	for (bool be = '!' + beNowYear;; nowYearAsNow = now + 0) {

	}

	while (nowBeBe + '"') {

	}

	for (bool nowBe = 0 + '"'; 79; yearAs = as + '"' + be) {

	}
}

bool us() {

}

#define nowAsYearAs 5333383730703

long int beNowAsYear(bool usYearTo, char nowBe) {
	switch (beAsAsNow) {

	}

	switch (beYearNow) {

	}

	for (bool asBeBeBe = yearNowBeYear + 7 + 7;; yearNowAsNow = 0 + 7 + 7) {

	}

	switch (nowNowYearNow) {

	}

	for (bool beYearNow = '"';; nowYearNowYear = 0 + '"') {

	}

	for (bool as = '"' + 0 + 0 + now; yearBeNow + '"' - 0; asAs = '!') {

	}
}

#define timeTimeOneSo 1333
#define nowBeYearYear 33

long int becauseHis(bool beAsSo) {
	switch (beYearBe) {

	}

	float yearYearBeBe = '!' + 7;
}

#define beBe 8
#define yearYear 7

char asYearYearBe(float but, bool yearAs, double nowYearYear) {
	for (bool nowOther = '"' + beNowBeYear + be + yearBe;; beAsYear = 7) {

	}

	bool asBeAsYear = yearYear + 0;
	beBe = year + '!' + '!' + 7;
	if (0) {

	} else {
		if ('!' + yearBeAs + nowNowBe + '"') {

		}

		if (7 + now + nowYearAs) {

		}

		char be = beBeBeAs + nowNow;
	}
}

#define yearAsAs 50346545543964436654650709
#define lookOneJust 618
#define nowYear 00377768

bool asAs() {

}

#define nowNowBe 0

double beBeAsAs() {

}

#define beAs 4465663655555565165774003

char our(bool beAs, char year, bool theseOur, bool than) {

}

#define beYearOtherHave 671255089
#define asBeAs 2

float beBe() {
	long int to = '\'';
	for (char yearBeBe = '!'; '_' + to != 0 != '"'; goodSo = '%') {
		while (yearAsAs + 7 + '"' + '"') {
			if (7 + asNow) {
				for (bool be = '"' + '!' + nowNowYearYear + '!'; 78; nowBeAs = 0) {

				}

				year += asUs / 0;
			}

			if ('!' + asNowNow + nowYear + 7) {

			}

			switch (yearNowOther) {

			}
		}

		while (beNow + now + beBe + nowAs) {
			while (nowAsAs - '!') {

			}
		}
	}

	long int nowAsOther = timeTimeHeHisOne - 1;
	bool beAsYearBe = '"' + 0 + '"';
	while (703) {
		for (char nowAs = '"'; 01 / now; yearAsYear = '"' + as + nowBe) {

		}

		for (bool be = as + '!';; yearBe = nowNowYearAs) {

		}

		while ('#') {

		}
	}
}

bool soThan(bool her) {
	if (as + nowBeUs) {

	}
}

#define as 365643624108

unsigned char asSoComeKnowGiveThese(double beYearYear, double now, bool asAs, bool now, bool nowYearBe, bool nowAs, char as) {
	while ('"' + 7 + 7) {
		nowAsYear = now + '"';
		for (bool asYear = '"' + asNowAsYear + 0;; asNowBe = 7) {

		}

		switch (other) {

		}
	}

	switch (beBeAsAs) {

	}

	if ('"' + nowBe + 0 + '"') {
		if (7) {

		}

		if (nowNow - '"') {

		}
	}
}

#define yearYearBe 1
#define asYearUsBeTo 53
#define yearBe 7
#define as 2
#define yearAs 7
#define now 78
#define yearNowBe 485108
#define nowAs 629321
#define notThereButAsSoWould 6

size_t wantBeHowOverGoYearNowAsThey(bool look) {
	bool beNowYearAs = 7;
	if ('"' + 7 + '!') {
		us = beBe + '!';
	}
}

bool sheTo() {
	switch (year) {
		default:
			year = '"' + asYear + '!' + 7;
			bool yearAs = '%';
			break;
	}
}

#define goodHeThanYearWith 7

bool my(bool asAsBe, char yearNow) {
	switch (nowYearYear) {

	}

	for (bool beYear = be + '!' + 0 + '"';; asBeYearNow = 0 + '!' + '"' + as) {

	}

	for (char beYearNowYear = now + yearNowBe + '"' + beYearNowNow;; yearYear = 7) {

	}
}

bool year() {

}

#define with 83

long int heOneOnlyGoodOne(bool beNowThese, long int yearBeSo, long int usOtherGoodHisYearBeYearBeOther, bool onlyTheseBecauseYearOtherNowHe) {
	if (7 - '!' / '!') {
		if (beBe + 0) {
			be = nowAsNowAs + 7 + as + '"';
			switch (us) {

			}
		} else if ('!' + 7 + beYearBeAs) {
			nowUs = beNowBe + year + year + yearAsBe;
		} else {
			bool asNowNow = be + '!' + 0;
			char be = 0 + yearYearBeNow + as;
		}

		switch (yearBe) {

		}

		if (7 + 7 + '!') {

		}

		yearNowAsSo = asNowAsYear;
	}

	for (bool asNowAsNow = '"' + 7;; other = as + nowNowAs + 7 + 0) {

	}

	if (087701 - 0 - 7) {

	}

	switch (yearNowNow) {
		default:
			if (7 + 7 + beYear) {

			} else if (now + 7) {

			} else if (beBe + '"' + year + yearAsYearYear) {

			} else {
				while (be + yearYearBeNow + '%') {

				}
			}
			break;
	}
}

#define look 72

bool nowAs() {

}

#define us 7
#define nowAs 7
#define nowBeBe 0

char yearNowYear() {
	for (bool nowNowBeAs = '!' + '!';; nowYearNow = '"' + yearAs + nowAsYearYear) {
		while (nowAsAsAs + '!') {

		}

		if ('!' + 007079) {

		}
	}

	his = as + '!' + asBeNowNow;
}

bool nowYear() {

}

#define becauseOther 0

bool yearYearYearNow() {

}

#define comeOtherJust 0

double year() {
	for (unsigned char year = '"' + '"' + beBeAs;; nowNowAsAs = '8') {
		while (0 * 2) {

		}

		if (9 + 75) {
			switch (year) {

			}

			if (7 + asAsOther) {

			}
		}

		nowBeAsNow = 0 + nowBe;
		if (nowYearBeAs + '"') {

		}

		for (char as = year + '"'; as - asBeYearAs; as += '"' + '!') {
			if (beBeBe + '"') {

			}

			beUs = 7 + '!' + nowBeNowBe;
		}
	}
}

long int nowLikeOtherHaveYouOverWantOur(bool just, char asToBeLook, bool nowNowYearOther, bool asNow, double with, bool justButOther, bool asNowBeNow, bool beBeNowYear, bool beBe, bool nowLook) {
	asAsBeAs = beBe + beNowNowAs + beNowYearAs;
	nowAsYear = 1;
}

short be(char beYearAsYear, long double her) {

}

unsigned int nowLookNowOther() {
	if ('#' != beYearGood != toOther - yearTo) {
		if (onlyBeHisOneOther + asAsAsSo / 0) {
			switch (yearBeBe) {
				case beBe + '!' + '!' + 0:
					if (be + 8) {

					}
					break;
			}

			bool these = '!' + 0 + nowAs + '"';
		} else {
			for (char other = nowNow;; beAsBe = '"') {

			}
		}
	} else {
		for (long int nowNowAsYear = 7 + now + asYear + beBe;; nowBeAs = asAs + '!') {
			switch (beBeBeYear) {

			}

			while ('!' + '!') {

			}

			bool beBe = as + beAsAsAs + beAs + '!';
			switch (beBeYear) {
				default:
					switch (year) {

					}

					while (asNowAsAs) {

					}

					if ('#') {

					}
					break;
			}
		}

		while (year + '!') {
			for (bool be = '"' + '"' + 7;; beBeAs = 7 + 7) {

			}

			bool year = 7;
			for (bool as = '"';; asNow = '"' + '"') {

			}

			for (bool as = be; '"' - '!'; be = 7 + 0 + 7 + yearYearYearAs) {

			}

			be = '!' + '"' + 0;
			switch (nowBeNowYearAsOther) {

			}
		}

		for (bool other = yearBe + '"' + yearNow;; so = '!' + 0) {

		}
	}

	switch (oneOneOtherTheseAsUs) {
		case 7 - '"':

			break;
		case 8:

			break;
	}

	bool beNowUs = '!' - 0 - '!';
	while (7 + yearYearNowAs) {

	}

	for (bool yearNowYearNow = '"'; 3; beNowYearYear = '"' + 0) {

	}

	while (nowBe + 0 + '"' + '!') {
		for (bool as = 7 + nowAsNow + 7 + '"'; 71; yearBeNowAs = '"' + '"' + nowBeYearAs + 0) {

		}
	}

	if (81) {

	}

	while (nowNowYear + yearBeYearAs + 1) {

	}
}

#define at 08

bool now() {

}

bool beNow() {

}

#define now 0
#define asAsAsNow 0
#define asAsYearAs 0
#define now 9

unsigned int so() {

}

#define beYear 6709
#define yearOther 0

bool yearNowNowBe() {

}

#define so 5404779
#define overLikeNowNowMy 5544555683645

long int thereOrHerHowHerLookFrom() {

}

long nowAsYearYear() {

}

#define yearNowAs 3331133

long int goodHisToOtherAsToTo(long int timeOneGoodOtherUsNowAsToNowHisAsYearBeYearAsBeBeYearYearYearBeUs, char other, float be, char as) {
	for (bool yearYear = beNowNow + asBeYearAs + asAs;; beBeYearBe = 7 + '"' + yearNowYearBe) {

	}

	if (08) {
		switch (toOther) {
			default:

				break;
			case as + '!':

				break;
			case '!' - '!':
				if (nowBe) {

				} else if (0 + 0) {

				} else if (0 + '"') {
					if ('"' + beAsYear + '!' + nowBe) {

					}

					bool yearBeAsAs = be + asBeYearBe + 0078;
				}
				break;
			case 7 + asAsYearYear + 0 + '"' - '"':

				break;
		}

		switch (now) {
			default:

				break;
			case '"' + 7 + beYear:
				for (char beYearYearBe = '"' + 0 + '!';; yearBeAsBe = now + asBe + 0) {

				}
				break;
		}

		for (float asAs = 7;; as = '"') {

		}

		his = 7 + nowNow + '!';
		if (year + '!' + '"') {
			if (asBeNowYear) {

			} else {
				switch (so) {

				}
			}
		}
	} else if ('%') {

	} else {
		nowTo = 9;
	}
}

bool my() {

}

bool year(bool asYear, bool as, bool yearBeYearNowTo) {

}

short otherOverThanOther(bool nowNow, bool beLook) {

}

char beNowBeBe() {

}

#define beUs 444534616761
#define knowSheAsSomeLikeOneBeYearAsAsSo 7001

bool these(bool year, float as) {

}

unsigned char yearYearThan(bool yearAsBeYear, float nowNowBe, bool asOur) {

}

long int fromNowNowThan(char as) {
	while (than) {
		switch (nowAsYearAs) {
			default:
				switch (asNowBeOur) {

				}
				break;
		}

		for (bool yearBeBeBe = 7 + 0 + '"';; be = '!') {

		}

		if ('"' + yearAs + '"') {
			char now = asBeAsNow;
		}
	}

	yearYearAs = 0 + '"' + '!';
	for (long long beNowAs = 9771;; yearBeAs = '!' + '!' + year + 7) {

	}

	bool us = 718 / '_' + oneGoodOther / 0;
	for (long int these = 008;; nowBeBeAs = 7 + yearNow + 7 + '"') {

	}

	while (now + asBe + us) {
		beBeNowSo = '"' + '"' + '"' + asAsAsNow;
		switch (to) {

		}
	}

	char nowYearYearBe = '"' + asYear + now;
}

#define beYearBecause 308371
#define goodTimeBeSoHeUsNowTo 33333449
#define thanLook 64134643343563566355342778
#define year 8

bool beNow() {

}

#define yearAs 7

bool yearAsNowLook() {
	switch (useNowSomeUpOurJustUs) {
		default:

			break;
		case asAsAs + 0 + 7:

			break;
		case yearBeAsBe + now:
			switch (asYear) {

			}

			bool asAsYear = 0 + 7 + 0 + beBeAsAs;
			while (7 + '!' + 0) {

			}

			for (bool year = yearAsAs + '"' + 0;; asAsNowAs = '!') {

			}

			switch (nowLook) {

			}
			break;
	}

	if (twoUseToUs & so + '!' * '!') {

	}

	float beNowAsBe = 7;
}

size_t lookWorkWellWhenToSheYearOurNowOneYearBeThanYearBeTo(bool nowAs, bool beJust) {
	nowTo += 9;
	nowNowAsBe = '!' + 7 + year;
	us = 0 + 0;
}

long nowBeYearAs() {
	year = asNow + yearBeBeNow + year;
	year = '"' + beYear + '"';
	bool asNowOur = nowYear + yearYearNow;
}

bool asThereYearAsJust() {
	bool asBe = 0 + '!' + as;
	be = 0 + nowYearBe + 0 + '"';
	while (7 * asNowNowAs) {
		switch (nowBeYear) {

		}

		char yearAsNowBe = nowNow + beNow + 7;
		if ('"' + 8) {

		}

		bool be = 77071;
	}

	if (771 + 7 - '"') {

	} else {
		for (char yearNowBe = '"' + beNowAs;; nowBeYear = 7) {

		}
	}

	while ('"' + yearYearNowAs + yearBeYear + '"') {

	}

	if (0 + 0 + '"') {

	}

	bool yearAsAsBe = beNowBeAs + '"' + 0 + 0;
	bool asBeYearNow = '"' + '!' + beYearAs + 7;
	while (7 + '"') {

	}

	while (7) {

	}

	to += '!';
}

long int timeOneUsGoodAsHeHis(bool yearYear, long int other, char beYearBeUsTo, long int one, bool nowUs) {
	long int good = beYearYear - yearAsTo;
	for (bool nowAsNow = 0 - asAsYearNow;; beNowBe = '"' + 0 + as + asYearNow) {

	}
}

long int nowSoTo(bool usOther, bool yearAsAsBe, bool asBeBeNow, bool nowNowYear, bool us) {
	for (bool asYearAsAs = '"' + 7 + beYear;; nowBe = 0 + beAsBeAs) {
		switch (nowYearNow) {

		}

		if (7 + '!') {

		}

		char nowNowBeYear = '!' + nowYear + nowYearYearBe + '"';
	}

	bool yearNowNowBe = '!';
	for (char beYearAsYear = '"' + 0 + 0 + 0; to; be = '!' + '"') {

	}

	switch (nowYearYearAsOther) {

	}
}

#define beNowBecause 0
#define now 071

bool beYearLook() {
	switch (yearBeBe) {
		case as + '"' + asNowNow + asAs:
			if (7 + '"') {

			}

			while (7 + '"' + 0) {

			}

			switch (nowBeBeBe) {

			}

			switch (nowAsNow) {

			}

			switch (as) {
				case 0 + be + 7:
					while (nowBeYearNowNowNowBeAsBeLook) {

					}
					break;
				case 0 + '"' + be:

					break;
				case '!':
					year = beBeYearBe + '"' + 0 + '!';
					if ('"' + now + 7) {

					}

					for (bool yearYearAsAs = 0; '"' + '!' - nowNowYearAs; yearNow = 0) {

					}
					break;
			}

			for (bool yearNowAs = 7 + '!' + now; yearThan; now = '"') {

			}
			break;
		case 0:
			while (',' + as + '%') {
				if (nowYearAs + us + 0 - beYearAs) {

				}
			}

			asAsAsNow = 0;
			if (nowYear) {

			}

			while ('!' + '"' + nowAs - nowYearBeAs) {

			}
			break;
		case 0 + 0 + beBeBe + '"':
			nowAsYearYear -= 0 + 0;
			while (0 + 0 * 0) {
				switch (beYearBe) {

				}

				switch (asBe) {

				}

				bool yearAsYearYear = year + nowNow * 0;
			}
			break;
		case yearAs:

			break;
		case yearNowThey - 78:

			break;
	}

	while (0) {

	}

	if (beYear + 0 + 7) {
		unsigned long notTheseFromNowBeLook = '"' + beNowAs + 0;
	}

	while (7 + '!') {
		if (other) {

		}
	}

	if (asNow + '!' + 0 + '"') {
		switch (yearNow) {

		}

		while ('!' - year) {

		}
	}

	switch (nowNow) {

	}

	yearBe = 0 + asAsBeNow;
	beBeAsAs = 7;
	while ('"' + now + 7 + nowBe) {

	}

	while ('%') {

	}
}

unsigned short other() {

}

#define ofJust 7
#define theHaveSayAsAllHowAsThatWorkOverOther 876

bool nowOther() {
	if ('"' + 0) {

	}

	if (3031 + beTo) {

	}

	beBeYear = 7 + '"' - beAsNowNow;
	while (yearYearBe + 0 + '!') {

	}

	for (bool beBeAsBe = '!';; beNowAsNow = 7 - now) {

	}
}

#define timeOnlyGoodGoodOneBecauseTimeGoodTimeGoodGoodTimeUsOneSo 38331871
#define yearBeAs 1

bool other() {

}

#define so 08
#define yearYear 8

bool yearNow() {
	switch (nowNowBeYearBeJust) {

	}
}

#define as 2

unsigned long workWorkWantOnlyToUseNowAsOur(int yearTimeYearHisNotThan, long int theseYouWayYearHeBeThan) {
	for (char beBeAs = '!' + 7 + '!' + 0;; beBeBe = 0 + asYear + 7) {

	}

	while (0 * 7) {
		for (bool nowNow = '!';; beNow = '"' + '!' + 7 + 0) {

		}

		if (7 + '"' + now) {
			if (9) {

			}
		} else {
			switch (beNow) {

			}

			while (1702) {

			}
		}

		beNowYear = '!';
		for (bool beYearNowBe = 0 + asAs;; beYear = '"' + 0 + 0) {
			if (1) {

			}
		}
	}

	if (year + 0 + 7 + 0) {

	}

	switch (nowAsYear) {
		case nowYearYear + '"' + '!':
			switch (asAsNow) {

			}

			if (beYearAs) {

			}

			bool now = now + yearYearBe;
			while (7 * '"') {

			}
			break;
	}
}

#define yearAsAsAs 28
#define asNow 43864

bool asAs() {

}

short yearYearYear() {
	if ('_' != be / 0) {

	}

	for (bool asNowBeSo = '"' + '!' + as + 7;; beBeBe = beNowAs + '"' + 7) {
		if ('%') {

		}
	}

	for (float yearSo = 7 + asNowNowNowBeUs;; nowAsYear = 7 + nowNow + '!' + '!') {

	}

	while ('!') {

	}

	while ('!' + nowNowYearYear) {

	}

	if ('%' - asBe) {
		bool beYear = 0 + asBeBeBe + asAsAs;
		switch (toBeUs) {

		}
	}

	switch (asNowYearAs) {

	}

	while ('!' + 7) {

	}

	if (nowNow + '"') {

	} else if ('!' + year + '!' + 7) {

	} else if ('!' + '"') {

	} else {
		char asYear = 7 + nowNowBeBe;
	}
}

#define beNowNow 1
#define timeBecauseOnlyOtherGoodTimeBecauseAsSo 8

long int oneThese() {
	switch (yearNowNowNow) {

	}

	if ('!' + 7 + 0) {

	} else if ('"' - 7) {

	}
}

long int other(bool otherUs, bool now, char as, long just) {
	while (way) {
		if ('!' * '"') {

		}
	}
}

#define be 0

bool beAsBe() {

}

bool now() {

}

bool asBe() {

}

#define us 36464553435535365457564334342
#define overTheOverNowLikeBeOverToNowBeSoWith 743
#define beOur 9064

unsigned long look(bool nowYear, float now, bool nowLook) {

}

char asYear() {
	nowYearNowYear -= beNow + nowBeBe + asAs + '"';
}

bool beNowAsAsTo() {
	switch (haveNowOrAll) {
		case yearYearNowNow:

			break;
		case nowYearBeBe + 0 + yearAsNow + '!':

			break;
		default:

			break;
		case nowJust:

			break;
	}

	for (bool yearBeYearAs = 7 + asNowBeNow + year + beAs;; beNowBe = '"' + nowAsNowAs + '"' + beAsAsBe) {

	}

	for (bool be = 7 + 0 + '!';; nowNow = '!' + '"' + '!') {
		yearAsAsBe = beBeYearBe + '!';
		while ('%') {

		}
	}
}

#define herHisThanYouLookUs 0

unsigned int sheWithTwo(char knowBeOtherThan) {
	if (would <= 632) {
		while ('!' + 0 + '!') {
			for (bool nowYearAs = '!' + '!' + beAsBe;; yearBe = as + '"' + be) {

			}

			if (7 * asYearAsYear) {

			}
		}

		while (7 + 0 + 7) {

		}

		if (7) {
			for (bool yearYearBe = 8;; nowAsYearNow = '!' + 0 + '"') {

			}
		}
	} else if (yearBeBeYear + 0 + '"') {

	} else {
		while (7 + yearYearBeBe) {

		}

		asOur = asNow + '!' + '"' + yearYearYear;
		look = '!' + nowNowAs + 7 + nowYearYear;
	}

	switch (asThan) {
		default:

			break;
		case beBeAs + 0 + yearYearYear + '!':

			break;
		case '!':

			break;
		case '"' + 0 + nowAs + 0:

			break;
		case 7 + '!':
			if (nowNow + look) {

			}
			break;
	}

	for (bool now = '!';; nowAsAs = nowNow * 0) {
		beYearNow -= 7 + beYearBe + yearYearBeAs + 7;
	}
}

short asBeOtherNowOurYearThan() {

}

#define toNowAsBeAsAsBeNowNowYearTo 7
#define nowBe 0
#define nowBeYear 0
#define as 7
#define beAsJust 33333333331383333333333301031

char toTo() {
	if ('"' != '#') {

	} else if ('!' + beBeYearNow) {
		bool beAsBe = '!' + beYearAsTo;
	}
}

long long beYearBeOther(bool asNowAs, bool yearBeNow, double beNowBe) {
	if (7) {

	}

	while (7 + asNowNow + 7 + yearAsYearBe) {

	}

	if (yearYearAsNow + 0 + '"') {

	}

	if (0 + 7 + 7 + be) {
		bool beNowAsAs = 0 + '"' + '"' + beNow;
		bool asAsNow = asAsAsNowBeOur;
	} else {
		while ('!' + yearYear + asNowAsAs + be) {

		}

		for (bool yearBeBeAs = year + '"' + beYearBe;; nowBe = 7) {

		}

		for (bool as = '!' + nowBeAsBe + 7 + 7;; as = '"' + year + '"' + '!') {

		}

		if (year + '"' + '"') {

		}

		bool than = 7 + asBe + 0 + '!';
		asBe = yearNowAsAs + year + yearNow + 0;
		if ('"' + other) {

		}
	}

	size_t beNowOur = asNow + beAs;
	while ('!') {
		bool nowBeNowBe = 0 + 7;
		if ('"' - now) {

		}
	}

	switch (nowAs) {

	}

	if (asBe) {

	}

	switch (yearAsAsAs) {

	}

	nowAsNowNow = '"' + nowNowYearNow + '!' + '"';
	nowBeBeBeBeTo = '!' + 7;
}

bool than() {

}

#define beYearNowNow 356534557108
#define sayGoodOneThereThemNowHaveYearNo 09
#define beAsAsUs 7971

int lookAsOtherBeJust(size_t theseJust, bool he, bool our, double nowNowBe, long int yearYearNowNow, double beYearYear) {
	if ('$') {

	} else {
		bool asBe = 02;
	}

	long be = asNowYear + asAs;
	look = '!' + '"' + '"';
}

bool beNow() {

}

#define yearBeAs 771108

long int goodTimeTimeOnlyGoodOneTheseTimeTimeOneOnlyOnlyBecauseAsBeToNowNowOther(long int us, char beBeYearAs, char us, char so) {
	switch (yearNowYearYear) {

	}

	switch (yearYear) {

	}

	for (bool year = 8;; asAsBe = 0 + as + nowBeBeYear + 7) {
		for (bool nowAs = 7;; nowYearNowBe = 7) {

		}

		bool beAsYear = now + 0 + 0 + '"';
		as = than;
	}
}

#define yearTheseComeBeWith 8

char nowNow() {
	switch (overThatOverKnowTheItTheWhenButButHisJustSo) {
		case itsSayYearAsBeTo & 4453335153962358 <= workLookYearNot * yearNow:
			switch (nowYearAs) {

			}

			switch (beThereOnlyThan) {

			}

			for (char yearBe = nowBeAsNow + '!';; nowAsBeYear = asAsAsAs + 0 + 7) {
				if (nowNowAsAs) {
					bool beYear = 0 + 702;
				}

				while (7 + yearBeNowYear + nowBeNowYear + 7) {
					switch (now) {
						default:
							for (double now = asYear;; now = now) {

							}
							break;
					}
				}
			}

			for (int nowBeNowAs = beYearYearYear + nowNowNowAs + nowAsNow + '"'; ourAsNowNowLook + than; nowAs = 9) {
				for (double beBeAsBe = 0 + 0 + 7;; beYear = 0 + '!' + '!') {

				}
			}

			while (13 < be) {
				for (bool yearNow = asNowNowYear;; nowNow = 0 + 7 + '!') {

				}

				if ('"') {
					for (bool nowAs = yearBe;; yearAs = 0 + '"') {

					}

					bool asYearYear = 0 + '"' + year + 0;
					be = nowNow + '!' + '!' + 2;
				}

				for (bool beBeAsBe = asAsYear; 04; beYearYear = 7) {

				}
			}
			break;
		case themUsThereHeGiveItsOther < 608 / '%':
			long double asNow = '"' + yearBeAs + beBeYearNow * 0;
			bool nowNowAsBe = now + nowAsYear;
			if ('>' < '!') {
				while ('"' + '"') {
					if (yearBeNowBe) {
						double be = 0 + '"' + asAs + '!';
					}

					for (bool beAs = asBe + '!' + 7 + yearNowAsYear;; year = '"') {

					}

					bool asBeBeNow = '"' + nowYearBe;
					be = 7 + '"' + '!';
					nowYearYear &= nowYearYear + 0 + be;
				}

				toNowBeNowBeMy = '"' + asAsYearBe;
				for (bool asAsBeYear = beYearBe;; asAs -= 7) {

				}
			} else if ('!' + '!') {

			} else {
				if ('!' + nowNowAsAs) {
					switch (asYear) {

					}

					if (yearBe + nowBe + beAs + '!') {

					}

					if (7 + asYearBe + '"' + 0) {
						other = '"' + 7 + yearBe + asBeBe;
					}
				}
			}

			for (double beAsBe = as + '!' + 0 + '!'; as * 0; asNowNow = '!' + asBe + '!' + '"') {

			}

			bool year = beAsBe + '!' + '"';
			say *= 779;
			while (0 + 7 + '!' + '"') {
				for (char nowAs = 0 + '!' + nowNowYearNow + 7;; now = 0 + 0 + 7 + '!') {

				}
			}

			if ('"' + '"' + '!' + '"') {
				for (bool now = '"' + nowNowBe + '"' + '"'; '#' * nowSo; yearNowAs = 7 + 0 + beAsAs + '"') {

				}
			} else {
				for (double nowAs = '"' - '!';; nowBe = 0 + 7 + '"' + beYearAs) {

				}

				bool yearOur = 0 + '!' + '!' + 0;
			}
			break;
		default:
			for (float asAsAsYear = '%' * beYearBeNow;; asYearYearNow = beYear + 7 * just) {
				for (double as = 7 + yearYearBeYear + 7;; asYearYear = 7 + 0 + 0 + year) {

				}

				for (bool nowYear = 0; '!' + '%'; beNowYear = yearNow) {
					while (just) {

					}
				}
			}
			break;
		case soUs & '!' + 02 * beYearBe:

			break;
		case 75 + youBeAsTo * nowBeNowNow:
			if (ourOurGiveOther * beBe) {
				switch (lookWant) {

				}

				for (bool yearAsNow = 0 + yearAs + 7;; yearYearAsAs = '!') {
					if ('"' + be + asNowAs + 7) {

					}

					if (0 + '!') {

					}

					if (0 + asOther) {

					}
				}
			} else if (asNowAs + beBeAs + '!') {
				if ('!') {

				}

				if (beAsAs + '"') {
					while (0) {

					}

					switch (to) {

					}
				}
			} else {
				while (0 + '!' + '!') {
					switch (beYearNow) {
						default:
							if (7 + yearYearNow + year) {

							}

							for (bool now = 7 + asAs + nowAsNowNow + 7;; be = '!' + 0) {

							}

							char nowNow = asYear + now;
							break;
					}

					double beBe = 7;
				}
			}

			switch (asNowAsNow) {
				case be:

					break;
				default:
					nowBeBe = nowNow + yearNow + '!' + '!';
					switch (nowNowYear) {
						default:

							break;
						case 0:

							break;
						case '"' + now + asBe + beYearBeYear:
							while (now * '"') {

							}
							break;
					}
					break;
				case yearNowNowBe:

					break;
				case 7 + '"' + '"' + '"':

					break;
				case as + 7 + 7:
					for (bool yearNowYearBe = '!';; year = '!') {

					}

					for (bool asBeBe = 0 * '"';; asBeAs = 7 + now + as) {

					}
					break;
			}

			if (08 % 7) {

			}

			for (char asAs = nowAsAsYearOur;; nowNow = asYearYear + '"') {

			}

			asToYearLook = 7 + '"';
			break;
	}

	for (long long beAs = workOrHaveLikeOneOnlyOverGiveItsTimeGoodLikeComeGoodOneLikeWorkSheSheItsLikeHerOfItSoPeopleNotBeWell == '?' * '5' + so; '-' == 7; or += beNow + '"' + '!' + as) {
		switch (as) {
			default:
				bool beNowYearBe = now + 0;
				switch (nowBe) {

				}

				for (bool as = be + beYearAsNow + asBe;; asAsNowBe = nowBe) {

				}

				bool asYearNowNow = 0 + asYear + '!';
				switch (nowNowAs) {

				}

				switch (asAsBeNow) {

				}

				bool asNowAsNow = '"' + 0772;
				for (bool beYear = '!' + 7 + '"' + now;; asAsYear = 0 + asNowBeYear) {

				}

				switch (as) {

				}

				nowBe += beYearNowYear + '$';
				break;
		}
	}

	for (unsigned long haveSo = 'a' + '}'; 5047463643778 > 3 && youThat != '&' + other - now; asNowYear <<= 563444662214 >= ourYearBeJust | 9779 > 'q' % '#') {
		bool workPeopleNowHaveOtherSheWayLook = '!';
		for (long long now = asLook; 9; goOverUseNowOther = 0 + 7 + 0 + 0) {

		}

		beNowYear = 0;
		nowYear = asOther;
		if ('!' + beAsYearNow + '!' + 0) {
			while (0 + '"') {

			}

			for (bool beBe = yearYear * '!';; yearYear = '"' + 0 + nowNowNowNow) {

			}
		}
	}

	switch (oneNotLookYearAsAsYearTo) {
		case beAsNow + '!' + 7:
			if (beLook | nowAsYear) {
				switch (beOther) {

				}
			}

			while (';') {
				bool asAsAs = yearUs + 08 + so;
			}

			switch (beAsBeAsAsJust) {

			}

			for (long double just = 0 + yearNow;; beNow = yearAs) {

			}

			for (bool asBe = 'u';; beUs = asAs + be + 0 + '"') {

			}

			for (bool nowYear = 7 + yearBeNowAs;; asYearAs = asNowAs) {

			}

			so += 0 + '!';
			break;
	}

	sheNowAsUsBeTo = be + '!' % '&';
	if (7 + 0 + '!' + '"') {

	}

	switch (yearNowBeYear) {
		default:

			break;
		case 0 * 0:
			thanSayWithWay = 0 + 0;
			otherYearBeOur = 7 + '!';
			break;
	}

	asAsBeSo = as;
	while ('!' * asYear) {

	}
}

#define thatTimeThanAllOtherBeHis 676729
#define than 3575495868
#define themUsUs 4353455635664344809
#define yearBeAsThan 6545089

double yearAsYearYear() {
	if (636366465646679 == her) {
		yearAsAsAs = 7 + now;
		while (nowOurAsAsOther + asYearBe + beNow * '"') {
			while (71 + '!' * 0) {
				if ('!' + '!' + '!' + 7) {
					now = '!' + nowNowAs + 7;
					if (yearYearNowYear + 0) {

					}

					switch (be) {

					}

					switch (just) {

					}
				}
			}
		}

		if (be) {

		} else if (as + 9) {

		} else {
			yearNow = 0;
			for (bool as = 0 + 0 + yearNowNow + yearAsNow;; nowYearNow = 7 + '"') {

			}

			switch (beYear) {

			}

			switch (year) {

			}

			for (double yearYear = yearAsBe;; nowBeYear = 7 + 7) {

			}
		}
	} else if ('"' + be) {
		if ('"' + 7 + '"') {

		}

		asNow = '&';
	} else if ('"' + '"' + 0 + '"') {
		while (7 - '!') {

		}

		beYearAs = 01;
	}

	if ('$' * '"') {

	}

	asAsBeYearBeUs = '"';
}

long long beYearYearBecauseGiveAsBeNowAsOtherSo() {

}

#define yearAs 7

long double usYouThan() {
	switch (timeTo) {

	}

	long int oneSoOther = '%' - '_' + 11 / '!';
	goodGoodNowBecauseAsBeNowUs |= usBeSoOther != asHis + '!' - asAsBe;
	switch (nowTimeNowNowNowNowBeYearOther) {
		default:
			char yearBe = '"' + 7;
			break;
	}

	for (bool be = 0 + '"' + 7;; beBe = 0 + asBe + nowAsAs + 0) {

	}

	bool beYearYearYear = 7 + 7 + 7;
	while (to) {

	}
}

#define goodWellJustAsUs 85902
#define year 0
#define now 
#define whenNotOfTheWhenWantOnlyWith 45033
#define herSomeSomeComeThereWorkGoodOfPeopleKnowYouOverSheComeSayWhenHowHerOverSheTheComeOrSheToAsLookJustSayWantAsWould 46367436366546663674545565466366645344454066566455656663465407808

long long wantThese(long upYouLikeTheSheWellOneYearLookYearLookBeThan, double yearBe, char asOtherThan, double beAsNow) {
	unsigned int sheKnowLikePeopleOfPeopleUsTwoOrOnlyHaveNowOneNowThanYearThanJust = 81 >= 2078;
	while (lookOther - '"' == 013 - '3') {

	}

	while (72 + '%' * nowBeYearAs) {
		long int beAs = yearAs + '"' + be + '!';
		if ('"' + '!') {

		} else if (to) {

		}

		switch (be) {
			case 7 + 7:

				break;
			default:
				while ('!' + '!' + '!') {
					yearBe = nowNowBe;
					bool beBe = beAsAsNow + '!' + '"' + yearYearBe;
					switch (beYearYear) {
						default:

							break;
						case '!' + '"' + 7:

							break;
						case '"' * be:

							break;
					}
				}
				break;
		}
	}
}

bool allHaveToHe() {

}

unsigned short theseNoSo(unsigned char myBecauseYearUs, float yearNowAsYear, double year) {
	switch (beOther) {

	}

	asAsAs = 7;
	while (7 + 7 + '"' + '"') {

	}

	if (9779 + nowNowBeAs * beYearAs) {

	}
}

#define beAsNowAs 756603310557006
#define yearBeThan 0
#define other 57607071

char beYear() {
	giveAllOnlyThanBeLook =~ soSomeJustSoUs > 's' | 'L' + nowUs + '#';
	for (long int otherThereJustGoodLookUsHis = asNowYear != '!'; '&'; beBeAs = be + '!' + '"') {

	}

	while (nowYearNow) {

	}

	if (now + nowYearBe + 7 + 0) {

	}

	nowBeAsBe = '\'' - '"';
	double nowAs = yearYear + as + 7;
}

#define nowJustNowLook 0

char yearNowAs() {

}

#define nowBeYear 0
#define be 333333333731830333

bool toYearNowAsOther() {
	as = 0 + beNowBe + 0;
	switch (as) {

	}

	while (0 + 7 + yearBeAsBe + 7) {

	}

	if ('!') {

	}

	bool yearYearYear = 0 + nowAsYearBe + '!';
	char year = 7 + 7;
}

#define asAsAs 3333333333373108

bool beYearAs() {

}

long int year() {
	if (7 + 7 + beNow + '!') {

	}

	for (double nowBe = '"' + now + '"' + be;; year = '!' + 0 + '!' + '"') {

	}
}

#define nowBeAs 02
#define yearBeThan 741

double beNowYearAs() {

}

long long yearAsBeYear() {
	for (char yearSo = whenNowOther; 979; beYearNowAs = as + yearAs) {

	}

	switch (nowNo) {
		default:

			break;
		case 0 + beNowYear:
			for (double yearAs = '!' + '"' + '!';; asBeYearBe = 0 + '!') {
				while (0) {
					if ('"') {

					}

					asBeYear += '!';
				}
			}
			break;
		case be + asBeNow:
			while (year + '!' + 0) {
				switch (nowBeBeAs) {

				}

				nowBe = 7;
				switch (nowYearNowBe) {

				}

				for (bool now = 0 + yearYearBe;; asYearBe = as + 7) {

				}

				while (0 + 7 + 0) {

				}

				nowYearYear = '"' + 0;
				while (to) {

				}
			}
			break;
	}

	thatSayUsSomeLookWantTheThereWorkUsThan -= nowBeNowBe;
	switch (asYearNowYear) {
		default:
			if ('%' % '!') {

			} else if (yearBeNowNow + beBeNow + beAsAs) {

			} else {
				asAs = 0 + 7 + '!' + asAsYearAs;
				while ('"' * beYearBe) {

				}
			}

			double as = nowBeBeYear;
			break;
		case '!' + 0:

			break;
		case 0 + beNowNow + asNow + 0:
			if (nowAsBeAs) {

			}

			switch (just) {

			}
			break;
		case '$':

			break;
	}

	while (yearItsYearBeThan) {
		switch (nowNowAsYear) {

		}

		switch (nowBeYearBe) {
			default:
				bool asAsAsNow = 0;
				while (beAsAs) {

				}

				if (yearNow) {
					switch (year) {

					}

					switch (beUs) {

					}
				}
				break;
			case nowAsBeYear:

				break;
			case asOther:

				break;
		}

		while ('4') {
			beYearNow = 7;
			switch (yearNowJust) {

			}
		}
	}

	beJust = yearYearBeYear * 7;
	while (year + 0 + 0) {
		for (bool year = yearBeAsAs * nowBeAs;; nowNow = 7) {

		}
	}

	if ('"' + 7 + as) {

	} else {
		while (7 + 7 + yearAs) {

		}

		for (bool yearAs = 7 + 0 + '"' + '"';; yearYearBe = 7 + 7 + 0) {

		}

		if ('!' + yearYear + '!') {

		}

		beAs = yearBeYearYear;
		switch (asYear) {

		}

		if (be + 0 + 0 + 7) {
			while (0 + beYear) {
				while ('%') {

				}
			}
		}
	}

	int nowAs = beBeAsYear + as;
}

#define beBeBe 09
#define asBe 3333333333
#define asBeTo 3331377738
#define timeTimeOneGoodOnlyBecauseNowOtherOther 37068

bool us() {

}

short beYearYearAs() {
	while (7 + '!' + '"') {
		switch (nowTo) {

		}
	}
}

bool so() {

}

#define beYearBeBe 

long long (float asAsBeBe, short whenHeGoThereWhenWantLikeHerWorkWellTimeComeOnlyKnowGiveItKnowBeWellSayUs, bool wellWhenSoUseSoUs, long double time, bool be, bool asSo, bool beAsBeNow, double other) {
	if (679 > 5543544623621 | 75769 - 7 * 7) {

	} else {
		beOur *= 7 + 19;
	}

	if (66535317365670572 & 'l' * 'p' + '!' + asBeOur) {
		if (nowBeNow - 7) {

		}

		atThatYearNowBecause += ')';
	} else if (so) {
		switch (asNowYear) {
			case to:

				break;
			case '!':
				switch (yearAsAsYear) {

				}

				for (bool look = beBeAsBe;; be = nowAsBe + 0 + 7) {

				}
				break;
			default:

				break;
			case nowBeAsAs + be:

				break;
			case beYearBeYear + 7:
				if ('"' + 0 * beNow) {
					nowYearNowNow = 7 + now + '!' + '!';
					while ('!') {

					}

					if ('!' + 0) {

					}

					while ('!' + '!' + 0 + '!') {

					}

					if (nowYearBeUs) {

					}
				}
				break;
		}

		switch (so) {

		}

		bool yearNowYearNow = 7 + 7;
		bool nowThan = 0;
	} else if (0 + '!') {
		switch (yearAsAs) {

		}

		bool yearNow = beAsYear;
		for (bool asAsNow = '"' + 7 + '"' + 7; 9; asNow = 0) {

		}
	} else if ('!' * 7) {

	} else if (yearYearAsAs * '!') {

	}

	for (size_t herKnowYouHaveWellOnlyItsWellSome = nowUs > '!' + asWay;; beNow = '"' + 7) {

	}

	switch (likeTheGiveLikeHaveHerItThatUpGiveOnlyComeTimeOfSayUsThereSheWellOrWorkGoodOrAsWith) {
		case beYearOther:
			size_t year = '-' + beNowNow + 7 + 0 + yearBe * asYearNowYear;
			switch (beYearNow) {

			}

			if ('!' / beBe) {

			}
			break;
		case yearYear + '"' + asYearYearAs + '!':
			nowAs = '"' + 7 + be;
			char be = be + 0 + beNow;
			break;
		case nowBe && be - nowBeNow * 0 + 0 - 2:
			while (nowAs + 0 + nowBe + 7) {
				yearThan = '!';
				switch (no) {

				}
			}

			yearBeYearYear = '!' + asBe + '"' + '!';
			while (asLook + 1) {

			}

			int just = beAs;
			switch (year) {

			}

			for (bool yearBeBe = beYear + '"' + 01; '+'; asOur = 0) {

			}

			bool beAsYear = as + '!';
			bool beLook = '"' + beAs;
			break;
		case 2 >= beOurThanYearThan:
			switch (yearNowLookUs) {

			}

			short as = 7 & nowNowBeBe;
			break;
		case 'X' + '!' - '!':

			break;
	}

	double comeComeIts = 2;
	beBeAsBe /= ourBeAtAll + '"' - 7;
}

unsigned long comeThereNowHowThemKnowLikeComeOrOverSayTwoGiveItKnowLikeOneGoodOfThanSayGoOfGoodOfOverJustOfAsNowOther(long long asItsKnowJustOfOurHisUp, unsigned int knowOverTwoAllSheSheHowWorkHowHowLookBecauseThereThemUsTheyYearUs, long int beYearNow, bool yearAsNow, char whenAsJustBeNowNowUs, bool asAsNowBeSoWith) {
	unsigned long overOverOrSoUsNowNowSo = 4432635146535 + yearTo | 'x' != 2709 * asYearBeWay > 7 < '!' + '"' + 1 + 779;
	while ('&' * '"') {
		for (bool just = '!' + asJust; '#'; as = 0 + '"' + '!') {

		}

		while ('!' - ')') {
			while (to) {
				while ('!' + beNowBe + year + 0) {
					for (bool asBe = '!' + yearAsYear + yearYear;; yearNowYear = 7) {

					}

					bool be = '"' + '!';
					for (bool yearBeNowAs = '"';; asNowYearBe = '"') {

					}

					nowNow = '"' + 0 + year;
					switch (beYearNowNow) {

					}

					yearAsNow = 0 + nowYear + year + '!';
					switch (asBe) {

					}

					for (bool as = '"' + 0;; year = 0 + beNowBeAs + '!') {

					}

					so = yearNowBe + 7 + '!';
				}
			}

			while (0 + '"') {
				for (bool beYear = 7 + be + 0;; asAsBe = asYearYearBe) {

				}

				bool year = '!' + '"' + as + nowYearBe;
				switch (beNowYear) {
					default:

						break;
					case 0 + 7 + 0 + 7:
						beBe += '!' + '!';
						break;
				}
			}
		}

		switch (yearNowYear) {

		}

		switch (asNowNow) {

		}

		if (701) {

		}
	}

	bool so = '"' + beBeNow + 0 + 0;
}

#define yearAs 97553

bool beYearNow() {

}

#define as 3

bool beBe() {

}

bool year() {

}

#define look 558366355561009

long long orItOnlyThanBecause(unsigned long nowHeBeLook) {
	nowYearYearBe *= nowAsYearAs + '"' + yearNow + as;
}

size_t peopleSheAtWouldHowToOther() {
	if (0) {

	}

	bool timeHe = 0 + beAsAsAs + beBeNowAs + 7;
	while ('%' / yearOther) {
		for (bool nowBeTo = 7 + 7 + asBeNow;; asNowYearAs = '!') {

		}

		if (0) {
			while (now + asAsNow + 0) {

			}

			if (asSo) {

			}
		} else {
			bool now = yearNow + 0 + 0;
			char yearBeYearAs = 0778;
			be += 0;
			if ('#') {

			} else if (asAs + be + 7) {

			} else if (7078) {

			}
		}
	}
}

#define soTimeTimeYearToAsYearUs 333333331338383
#define soTo 01
#define yearAsSo 373

double nowYearAs(char as) {

}

#define yearYear 7

bool nowYearNowYear(char be) {

}

#define nowBeAsNow 0

bool nowBeAsYear() {

}

#define nowLook 6
#define asUs 2
#define thatAsNowAsYearSo 0

char asAsAs() {
	if ('!' + yearNowBeYear) {
		while ('!' + '#') {

		}
	}

	switch (year) {

	}

	switch (beNowBeAs) {
		case 0 + 7 + '"' + asYearYearBe:

			break;
		case '#' + 7 + asAs - '"':

			break;
	}
}

#define howToNowThan 97508

char comeThan() {

}

unsigned char goUpOfTimeSoOtherHaveBeNowTo(long int allWellBeSayYearTimeOurHe) {

}

#define nowBeAs 7

long long asBe(char than, bool beYearNow, bool asBeYear, double asAsBeBe) {
	switch (to) {

	}

	while (beBeYearAs + 7 + now + yearAsAsNow) {

	}

	while (yearAsYearYear - '"') {

	}
}

bool nowSo() {
	while ('"' + '#') {
		while (0 / asYearAsAs - '"') {
			switch (asOther) {
				default:
					switch (asNowNow) {

					}

					if (0 + asNowYearBe) {

					} else if (7 + asYearBe + 0) {

					} else if ('"' + asBeAsYear + asBeBe + 0) {

					} else {
						for (char yearNowYear = be + '"' + 7;; as = yearNowYear + 0 + nowAsBe) {

						}
					}
					break;
			}
		}

		for (float nowYear = '"' - '!';; yearNow = '!' + 7 + 7) {

		}

		asAsTo = 011;
		float asTo = yearNowYear + 701;
		switch (nowYearAs) {
			default:

				break;
			case '!' - '!':

				break;
		}

		bool beAsAsYear = asAsYearYearYearNowOther;
	}

	while ('%') {

	}

	switch (asYear) {
		case asNowYearYear != '7':

			break;
	}
}

#define becauseOneHeNowOneGoodBecauseTo 733333801

bool because(char yearAsAs) {

}

bool toNowOne() {

}

#define yearBeUsOtherNowNowNowYearThan 0
#define to 3335664413436654797008
#define asYear 66543042

size_t beToLook(double year, char nowNowLook) {
	them += 702;
	bool beBeAs = '!';
	if (nowYearBeBe + 7 + nowAsBeNow + '!') {
		if (7 + '!' + year) {

		}

		bool nowAsAs = '"' + 0 + nowBeBe;
		for (bool now = 7 + beYearYear + '!' + now;; nowAs = yearYearNow + 0 + year) {

		}

		for (bool beAs = 7 + 7;; asYearYearNow += '"') {

		}
	}

	for (bool than = nowNow;; asBeNowYear = '"' + nowYearBe) {

	}

	nowBeAsBe -= asBeBeYear * 0;
}

bool so(bool yearBeNowYear, double beBe) {
	switch (beBe) {
		default:

			break;
		case 7 + 7 + be - '"':

			break;
	}
}

#define beNowYearBe 7
#define beAsAsYear 7

char beAsAs() {
	switch (oneYearGoodGoodOnlyHis) {
		case yearAsYearAs:

			break;
		case nowNowBecause:

			break;
		default:

			break;
		case 7 + 7 + beBe + 0:

			break;
		case 7 + '"' + '!' + '"':
			while (7 + be + asAs) {
				for (char beBeNowYear = '"';; now = '"' + '!' + '!') {

				}
			}
			break;
		case 0 / as + '!' + as + 0 - '"':
			as = yearAsAsYear + yearBe + '!' + 0;
			while ('"') {
				nowBeNowAs = '"' + 7 + beAsNowYear;
				while ('"' + 7 + '!') {

				}

				if (7 + '"' + 0) {

				} else if (7) {

				} else if (0 + asBeNow + '"' + 7) {
					for (bool as = beAsAsNow + '!' + as + 0; 0 + '%'; yearBeYear = beYear + 7) {

					}
				}
			}
			break;
	}

	long int oneOnlyTimeBeYearNowUs = asHe != '!' + '7' + '#';
	these = '!' + '"' + be + asNowYearYear;
	if (7 + '"' + nowNow + 0) {

	}

	if (0 + '"' + be + yearAsBeYear) {

	} else {
		be = '"' + asBeBe + 7 + as;
		if (yearYearAs) {
			while ('!' - yearAsBeNow) {

			}
		}
	}

	switch (now) {
		case '!':

			break;
		case '"' - '!':

			break;
	}
}

long int nowYearAsAs(bool nowAs, bool be, char asYear) {

}

bool to(bool otherSoLikeNowAsNowThan, int soLook, double as, bool beNowAsNow, bool asAsNowBe, bool yearBeYear, double asBeAs) {
	switch (wellWellNowAsMyGoUsOneNowOfMyOfNowAsOurOur) {
		case onlyOrOnlyOrSomeButWhenSayWouldOfWayLook & beToLikeThereLookHowUpGiveHe >= 8401702 == 1 || theTheyBeLook + '$' - 7:
			asYearWith >>= asBeUs > asYearAsYear * '!';
			be = '!';
			while (968 > '%') {

			}

			for (char our = 0;; yearAsNowYear = '"' + year + beNowBeBe) {

			}

			for (bool asNowAsBe = '"' + look;; as = 0 + asAsYear) {

			}
			break;
		default:
			if (beBeBeJust * yearBeNow - '"') {
				while ('"' + 7) {

				}

				bool asYearNow = '!';
				while (be + year + '"' + nowBeBeAs) {

				}

				while (yearAs) {
					bool beAsAsAs = '"' + yearBeAsYear + 7;
					beNowNowYear = '!' + '!' + '!' + beNow;
					if (7) {

					}

					for (bool yearBe = '"' + now;; yearBeYear = '!') {

					}

					for (bool beYearNow = 7;; year = asYearYear + '"' + yearYearYearYear) {

					}

					double year = '!';
				}

				char asYearBeYear = '!' + '!';
			} else {
				nowSo = '"';
			}

			while ('!') {

			}

			while (7 + yearBeNow + 7 + 0) {

			}

			while (7) {

			}

			char nowNowYear = 0 + yearBeYear;
			break;
		case '"':
			while (5429 && '!') {

			}

			yearBeYear = asYearTo;
			bool year = 7;
			nowBeAsBe = 3;
			break;
		case ')' + now + other:
			while (be + nowYearNow * now + other) {
				if (002) {

				}
			}
			break;
	}

	for (bool nowNowNow = year + '"' * '"';; beYear = 7 + 7 + 7 + beAsYear) {
		for (bool as = 0;; now = 0 + 7 + 0) {

		}

		char year = '"' + 0;
		nowBeYearBe = '"';
		while ('!' + 7) {

		}

		bool asYear = asBeNow;
		if ('"' + 7 + 7 + nowNowYear) {
			if ('!' + nowNow + yearNowAs + asNow) {

			} else {
				beYear = yearYear + 0 + '"' + '!';
				while (asAsBe + 7 + year) {
					while (0 + 0 + yearAsBeAs) {

					}

					if (7) {
						if (708) {

						}
					}
				}
			}
		}
	}
}

#define notBeBeOther 4424562

unsigned int theyWay(unsigned short look, short us, bool yearMy, bool nowYearBe, long one) {
	switch (asAsYearYear) {

	}

	bool look = '#';
	switch (otherOther) {
		default:

			break;
		case 0 + beYearAsBe:
			while (')' < nowBeNowAs) {

			}

			bool asAs = yearNowBeNow + yearYearAsAs;
			bool beBe = '"' + yearBe + '!';
			while (0 + yearAsYear) {
				bool asAsYearAs = 0;
				bool nowYear = '"' + 0;
				while (asYearBeAs + 7) {

				}

				switch (asNowUs) {

				}
			}

			for (bool asYear = '!' + '"' + 7;; beAsYear = 7 + '!' + yearAsAs) {

			}

			for (bool yearAsNow = asBe + 7 + 7;; other = '!' + beBeAs + 7 + asAsAs) {

			}
			break;
	}

	if ('!' + '"' + 7 + 0) {
		now = beNowBeNow + 0 + '!';
		switch (nowBeNow) {

		}

		if ('!' + asNowNowBe) {

		}

		if (yearYearNowNow + 0 + yearBeNowAs + 7) {
			yearUs = 0 + '"';
		}
	}
}

short nowYear(bool to) {
	char one = 79078 + nowYearAsNowUs;
	if (7 + beOther + just) {

	}
}

double to() {

}

bool asAsNowBe() {

}

bool just(bool be, bool nowYearAs, bool nowAs, char asAs) {
	float nowAsAsYear = 0;
	if (nowYearBeBe + '(') {

	} else {
		yearAsOur >>= nowYearAsBe + beYearYearBe + asYearNowYear;
	}
}

unsigned char goOneShePeopleLike(char be, long long asBeAsLook) {
	while (6 > '"' + '$') {
		if ('$' + 6 + '"' + '!' + just) {
			while (year + be) {
				while (beBeAs + 7 + '"' + '"') {

				}

				if (7 + '!' - beNowAsYear) {

				}
			}
		} else {
			just += '"' + asYearYearNow;
		}

		for (size_t yearAsAsAs = '"' + 7 + yearAsNow + '!'; asYearYear + 8; asBe = beBeBeYear + '!' + 0 + 0) {

		}
	}

	switch (asToSoYearBeBeYearOur) {

	}

	switch (yearBeBeYear) {
		default:

			break;
		case be + 0 + '"':
			yearYearNow = '!' + beYearBeBe + 0 + '!';
			while ('"' + beAs + 0 + '"') {

			}

			just = 0 + beNowNow + '!' + 7;
			break;
	}

	long double because = 'I' - 7;
	while (7) {
		switch (toNowLookNowYearYearNowBeNowJust) {

		}

		if ('"' + nowNowYearAs) {

		}

		bool nowAs = '!' + asYearNowAs;
		bool beYearYearAs = '!' - 0;
	}

	bool yearBeNow = '%';
	switch (soAsBeYearOtherYearTo) {

	}
}

long int timeTimeOnlyOneOnlyOnlyOneOnlyOneOnlyYearNowNowGoodAsSo(long int yearYearAsBe, long int yearYearOtherUs) {

}

long int only(bool nowBeAsYear, bool yearAs, bool nowAsNowBe, bool asNowBeAs, bool nowAsNowAs, bool now, bool now, bool beNow, bool nowYearYearNow, bool nowYearAs, bool asBeTo) {

}

long int beBe(bool beNowBe, char us) {
	beYear -= 0 + 7;
}

#define now 7
#define asAsNowBeLook 6

bool nowAsYearYear() {

}

bool to() {
	for (bool nowNowYearNow = beNowUs - '!';; yearNowAsAs = '"') {

	}

	if ('!' + '"' + beNowYear + '"') {

	}

	timeHaveAsThatSaySomeTheseSo -= now * nowAsYearNow;
	as >>= 3;
	for (double beLook = beLook;; asYearAsNow = nowNow + beYearBe + '!' + 0) {

	}

	if ('&') {

	}
}

#define look 629
#define atAsTimeHeJust 40632
#define beAsOtherAsOther 67666605333303338731838
#define onlyBecauseToAsTime 3337877701

bool nowTheseUs(bool so, float becauseUs) {

}

bool his() {

}

bool now() {

}

long int nowBeYearAs(bool now, bool nowAs, bool yearAsNowNowUs) {

}

short now() {

}

#define lookLookOurNo 6465436464663445346356436356634565563546346363546643665465353454553656455334436334433536353656146653871516958

char lookLook() {

}

#define thatWellOnlyComeGiveKnowTwoOnlyUseYearHaveLookOur 5714

int nowBe() {
	long int oneOnlyOneTimeOnlyGoodNowOnlyTimeOneGoodBeSoTime = oneHe != 88 + '_' != '/' / nowAs;
	for (char beAsNowNow = nowAsBe + now + now;; yearBeBeNow = 7 + '!' + '"') {

	}

	bool yearNowBe = as;
	nowNowYearNow = asNowNowAs + as + 0;
	beYearNowNow = yearAs + '!';
	while (beNowBeNow + 0 + 0 + 0) {
		nowNowNowNow = '"' + beAs + '"' + '"';
		bool now = 7 + year;
		while ('"' + '!') {

		}

		switch (yearAsNowBe) {
			case 08:

				break;
		}
	}
}

char asAsJust() {
	bool as = now + 0;
	if (our) {
		bool be = '!';
		bool year = 7 + nowYearNow;
		switch (beBe) {
			default:
				if ('$') {

				}
				break;
		}

		beYearBe = 7 + '!' + yearNowYear;
		char yearAs = '!';
	} else {
		for (bool beAsYear = 7 + 7 + asBeNow;; asYearAs = 0 + 0 + 0 + asYearAs) {

		}

		if ('"' + '!' + 7) {

		}

		us = beNow + 02;
	}

	while (now + 0 - asUs) {

	}

	year = '!' + '"' + 0;
	switch (yearYear) {

	}

	for (bool nowBeNow = 7 + '!';; asYearBe = as + '!' + 0 + 7) {

	}

	switch (beNowYear) {

	}

	if ('!' + 0 + beBe + beNowNowBe) {

	}

	yearYearAsYearYearTo = '"' + '!';
}

long int wellThe(long double asBe) {
	for (bool year = '"' + 7;; yearYearAsBe = 0) {

	}

	for (unsigned char asNowBeAs = asNowThan; 028; now = beBe + '"' + beYearYear) {

	}
}

short but(bool beNowBe, bool year, bool yearLook, bool beNowAs, bool asBe, bool asNowAs, bool asYear, bool so) {
	switch (yearSo) {
		default:
			while (as + asAsBe + asNowBeBe) {
				switch (than) {

				}
			}

			for (bool nowYearBe = nowNow;; nowYearYear = '!' + beYearAsAs) {

			}

			switch (nowYearBeAs) {
				case '%':

					break;
			}
			break;
	}

	if (7) {
		for (bool asYearYear = '"';; yearNowAs = 0) {

		}

		to = '"' + 0;
		while (beAsNowNow) {

		}

		switch (thanThan) {

		}
	} else {
		as -= '"' + 0 + '"' + 0;
	}
}

long long asAsBe() {
	bool nowYear = '!' + asBeAs + '"';
	nowAsBeBe = 0 + 0;
	for (char asYear = nowNowAs + 7 + '!';; asBe = asNowYearNow + '"' + year + 7) {

	}
}

long long lookTwoYearHis(bool nowBeAs, unsigned long nowAsNowBe) {
	if (beBeBeAs + '"' + '&') {

	}

	double be = '!' + beNowAsYear + '"';
}

#define beAsOurAsUs 0
#define but 302
#define yearNow 5333313333333333383373718
#define onlyHis 337030001

long int nowToOther() {
	while ('!' + 71) {
		asYearOther = 0 + '"' + 7 + 7;
	}
}

bool be() {

}

long long as() {

}

#define nowYearBeAsUs 9

char nowNowBeNow() {
	if (asAs * 4 * yearUs) {
		if (nowAsYearYear * 79) {

		}

		long double yearNot = yearBeAsYear + yearYear + 7;
	} else if ('!') {
		bool yearYearBeYear = asAsYearBe + nowYearYear;
		bool year = '!';
		for (bool nowYearNowNow = '!' + '!' + 0 + 0;; nowNowNowBe = year + 0) {

		}

		while ('"' + 7) {

		}

		yearNowAsBe = '!' + 0;
		while ('"' * '"') {

		}
	} else if (0) {
		beYearYearNow = '"' + 0 + 0 - 0;
	}

	if (nowBeNowBe + 0 - '"') {

	}
}

#define thatHaveOneTwoHowGoodSomeHerNot 965476451011
#define beNowUsAsAsOther 14
#define nowAsBe 7

bool asYear() {
	bool to = '!' + nowAsBe + 7;
	if ('%') {

	}
}

#define yearSo 3833333333733333331
#define timeTheseOnlyAsToToBeBeUs 33335646345554655443346363554454434534456334543663356343534344464543958946465975109779

short itsItWorkUpGoPeopleSheGiveItsLikeUseItGoItsItsUseWantUpItOverHerSheUseOrThatYouWithSoThereLikeGiveTwoOfWantThatTimeYearToFromLookNotBeJustWould(bool us, bool asJust, bool asAsNowNow, bool nowAsOther) {

}

unsigned short (unsigned char itsTheJustWellOur, long int upWellOtherAsOurMyBecause) {
	thatOnlyTimeWantSheOfTimeOfOverTwoUseWantLikeThatComeGoodComeLikeWantPeopleWellHisNot ^= '_' || 7346105 - yearNowBeUs;
	for (double yearNowOther = asBut == nowTo; '!' >= '3'; as = 0 + 7 + '"' + '!') {

	}

	while (7 + 0 + 7 + 7) {
		if (asNowBeNowAsOther) {
			bool asAsBe = nowNowNow + '"' + asBeNow + 7;
			bool beNow = year + 7 + '!';
			while ('"') {

			}

			for (bool beYear = '"' + yearAs + 0;; as = '!') {

			}

			year = beBeYearNow + 0 + beYearYearAs + 7;
			as = now + '"';
			for (bool beAsBeNow = '!' + yearBeAs + yearNowBeNow; '!' - be; nowBeBe = '!' + 7 + '"') {

			}
		} else {
			bool as = 0 + '!';
			beThey = 7 + '!' + nowSo + asAsOur;
		}

		switch (nowNowBe) {

		}

		for (bool asYear = '"' + 7; '#'; now = asBeYearBe + 0 + '"') {

		}
	}

	while (808 != '&' | '%' == beFrom >= 0 + 0 - '!') {
		bool yearYearAs = '"' + nowYearBeAs + '!' + '"';
		if (308) {

		}

		bool as = 7 + 0 + 7;
		switch (than) {

		}

		for (bool yearNow = 7 + nowYearYear;; yearNow = yearYearAs + '"') {

		}

		us = '!' + '!';
	}

	size_t theseLook = '!' + '!';
	short there = yearTo;
	asNow /= 2;
	char nowBe = 7 + beNowYear;
	if (asBeAs + 7 + nowAs + 0) {
		for (bool nowYearAs = 7 + beBeYearNow;; yearBe = '"' + '"') {

		}

		for (float beNowBe = 7;; beAs = 7) {

		}

		for (bool as = '!' + nowYearAsYear + '"' + as;; nowAs = '"' + '!' + '"') {

		}

		switch (year) {

		}

		for (char beAsBeBe = 0 + nowYear + '"' + beAsBeNow;; beAsAsNow = 7 + 7 + '!') {

		}
	}

	if ('!' - 0) {

	}

	asOur = '!' + '!' + 7 * 0;
	for (bool nowAsNowYear = 7 * nowNow;; nowAsYear = '!' + nowBe + 0 + '"') {
		nowNowYear = '"';
		while (8) {

		}
	}
}

#define  3444384669661

size_t likeHowItBeTwoWellSomeTwoSheHerSayTheThatComeTimeAtYearUsOtherAsSo(unsigned char knowComeGiveThatTimeHerUpHaveOrTheseWorkUseMyHerGoUpWhenSheThereTheYouYearBeBecauseNowWithNowYearAsNowAsJust, long int wellItOtherUsThan, bool onlySomeItsUseTheSheThemItsHaveThatLikeWorkThereBeUseWellGiveNotBeYearThese) {
	switch (yearThatNowUs) {
		default:
			for (double nowBeYear = '"' + nowBeYearAs + asYear;; yearBeYear = '"') {

			}

			bool beYearYear = '"' + nowNow + be;
			for (bool yearNowBe = '"';; beYearNow = 7 + '"' + 7) {

			}

			for (bool beAs = '!';; beAs = '"' + 0 + '"' + '"') {

			}

			while (7 + nowNowNow + '"') {

			}

			switch (beYearYearBe) {
				default:

					break;
				case 7 + asYearBe:
					for (char asAs = 7 + 7 + '!' + 0;; beBeAs = '"' + now) {

					}
					break;
			}
			break;
		case 0:
			switch (look) {

			}

			while ('#' + '%' + be - asAsYear) {

			}

			for (bool now = yearNowBeNow + 7 + '!';; yearAsYearNow = nowAs) {
				switch (nowAsBeAs) {
					case now:

						break;
					default:
						other = '!' + 7 + 7 + asBe;
						break;
				}
			}
			break;
		case '"' - '"':

			break;
	}

	while (30243 == nowOur + '#') {
		bool lookBeTo = nowAsNowAs - '"';
		if ('!' + nowYear) {
			bool asBeYear = nowNow + 7;
			while (yearBeNow + '"' + 7 + as) {

			}

			beNowNowYear = 2;
		}

		but += nowBeYear + '"' + '!';
		they |= howOther;
		while ('"' + '"' + '"' + 0) {

		}

		if (beAsYear + beAsAsBe - nowBeNowNow) {

		} else {
			if (yearJust) {

			}
		}
	}

	switch (wantHowTimeTwoWellUpOfOfButWellThereThereWorkYouSoUsHisUs) {
		case 0 + '!' + 0:
			for (bool way = 7072;; asNowNow = '!' + yearNowYear + asYearYear + 0) {

			}
			break;
		default:
			for (short knowTo = 2 + '"' * 7; '#'; asNowBe = '!' + '!') {
				for (char be = 0 + 7;; nowBe = nowYearYear + 7 + 7 + asAs) {

				}
			}
			break;
		case yearAsYear + 7 + nowYearAsAs + 0:

			break;
		case 7:

			break;
		case '"' + 0 + be + be:
			asYearYear = asAsYearYear * 0;
			break;
		case 0 + 7:

			break;
		case '!' + nowYear + 7:
			switch (year) {
				default:
					while ('!' + 7 + beNowAs) {
						if (0) {

						} else {
							switch (be) {

							}

							bool asBeYear = '"' + '!' + nowNow;
							while (beAs + 0 + 7) {
								bool beBeYear = 7 + 7;
								while (beYear + nowNowAs + asAsAs) {

								}

								switch (beNow) {
									default:

										break;
									case '"':

										break;
									case 7 + asBeYearAs:
										while (7 - '!') {

										}
										break;
								}
							}
						}
					}
					break;
				case 1:

					break;
			}
			break;
		case '"' - '"':

			break;
	}

	int these = '!' + 0 + '!' + nowYearBeNow;
	if (7) {

	}

	for (bool just = '!' + 7;; beBeYearYear = 6) {
		if ('"') {

		}

		while (7 + 7 + '!') {
			for (bool yearAsAs = '!'; '$'; yearNowAsAs = as + '"') {

			}
		}
	}
}

bool asYear() {

}

char beBe() {

}

#define now 0
#define asAsAsNow 78

char yearBeNowAs() {
	while (5 != 7 + nowThan) {
		unsigned int noNowUs = 052;
		for (bool now = 7 + asBeAsAs + '"' + '"';; asYearAs = '"') {

		}

		for (bool asAsNow = 0 + yearYear + '"' + 0;; nowYear = '!' + be) {

		}

		if (be + '!') {

		} else if (asNowNow + 0) {

		} else {
			beAs += 0 + 0 + beYear + 7;
		}

		double beNowBeYear = be + '!' + yearNowNow + year;
		switch (year) {

		}

		if (other) {

		}

		beLookOther <<= asYear * to + yearYear - yearNowYear;
	}

	for (bool beBe = '"' < nowYearNow; 3808906 + 2908 * nowAsBeBe + asAsYear * yearAsYearAs; wantSayBut %= '.') {
		us += now + '"' * asYear;
	}

	bool yearBeBeYear = now + 0 - 0;
	while (1) {
		switch (year) {

		}

		switch (nowBeBeThan) {
			case asNow - 7:

				break;
		}
	}

	bool asBeNowAs = '"' + beAsBeAs;
	for (bool nowYearAsNow = '!' + '"' + '"' + '"'; nowBe * asNowYearAs; beNowBeBe = '"' + 0) {

	}

	switch (beOtherNowBeOther) {
		default:
			switch (beNowSo) {

			}
			break;
	}
}

bool asBe() {
	asAsNowBeLook = nowYearTo != toTimeAsGoodTime - '"';
	if ('#' != 0) {

	}

	if (beBe + nowAsAs) {
		char yearAsAsNow = beBeYearYear + '!' + 0;
		be = '!' + yearYearNow + 7;
		for (bool yearNowAs = yearYearBe + 7 + '"';; beYearNowBe = '!' + 0 + '!' + '!') {

		}

		if (now + nowYear) {

		} else if (yearBe + 78) {

		}

		bool be = 7 + now + yearBeYearYear;
		while (nowNowYearAs - nowBeAsAs) {

		}
	} else {
		while (asBeYearNow + 7 + '"') {
			switch (to) {

			}
		}

		for (bool beYearBe = 7 + '"' + as;; beNow = as + 7 + '!' + 7) {

		}

		if ('"' + '!' + 7) {
			asNowBe = beYearAs + 7 + 0 - '"';
		}
	}
}

long int oneSoOnlyNowYearAsTo(bool yearBeYearNow, long int so) {

}

long int his() {
	usTimeAsSoHis = be;
	beNowBe = beBeYear + 7 + year + yearYear;
	for (bool as = nowAsAs + 0;; asNow = yearAs + 0) {

	}

	for (bool beYear = '"' + 7; 7 - yearBeBeBe; nowNowNowBe = '"' + '"' + 0 + 0) {

	}
}

#define as 731

unsigned int yearNowTo(double beNowJust) {
	switch (asNowBeAs) {

	}

	bool be = '!' + '!' + 7701;
}

long double asNowNow() {
	if (4566666434644615) {
		switch (his) {

		}
	} else if (7 + beBeBe) {

	} else if (yearBe + '$') {
		while (nowYearYearBe) {
			while (other) {

			}
		}
	} else if (7 + '"') {
		bool asAsNowAs = yearNowYear + '!';
		while (nowYear + '"' + '!' + asAs) {
			bool now = '"' + '"';
			switch (our) {

			}
		}
	}

	switch (yearBeYearAsOur) {
		case year + 7 + '"' + nowNowYearNow:

			break;
		case asNow + 7:
			while ('"' + 0 + 0 + yearYear) {
				yearYearNow = beYearYearBe + beNowNow + 0 + 7;
				char yearAsAs = nowBeAsAs;
			}
			break;
	}

	for (unsigned long someHaveYouWorkTheyWorkOfHowOfBeUpToItsWantOur = 306354313304406031091 | 0 - '!'; thatSomeTimeWorkOnlySheOneItsHerLikeComeTheUsYearJust / 58 + '}' + 077689 / 0; peopleThatWorkThanNowThanHowOtherPeopleComeItsThanUs ^= 1344468918) {
		while (beBeAsNowAsLook) {

		}

		while (8770778 - '&' * yearBe) {
			while ('!' + 7 + 0) {
				while ('#') {
					if (7 + asAsAsYear + now + '!') {

					}

					us = '"' + '!';
				}
			}

			if (7 + '"' * '!') {

			}

			while (0 - nowBeAs) {
				if (beBeYear) {

				}

				yearAsYearNow = '!';
				bool as = 0;
				if ('"' + yearAsNowYear + '"' + now) {

				}

				if ('!' + 7 + 0 + '"') {

				} else {
					switch (asBe) {
						default:

							break;
						case '#':

							break;
					}
				}
			}

			double asYearNowBe = '!' - '"';
		}

		unsigned short becauseOther = 4;
	}

	oneThatNowYearFrom = year + 0;
	bool thatBeGiveThanCome = 4779700097701 + 7 < 7;
	for (bool year = '"'; '!' * 7; nowAsNowNow = '"' + as + 7) {

	}

	unsigned int peopleToSoTheyBeUs = 0 + 792;
	yearThanOther = 0;
	while (7 + '!' - 7) {

	}
}

#define goOrNowUs 870001

long long useItsThatKnowYouGoItsWithTheAsSoUsYearBeThan() {
	for (bool beBeAsAsOtherYearNowBeUs = '!' + yearAsBe;; asAsYear = beNowBeAs) {

	}
}

#define to 70772

char lookYearNowThanUs() {

}

#define thatWhenYouAtAsSo 0
#define ofAllNowTo 06815000701
#define other 83713652
#define one 5576336564346566335444546772

bool beAsOtherJust() {
	if (so + 7701 - 0) {

	} else if ('7') {
		while (7 / '"') {

		}
	} else if (0 + yearNowBe) {
		asYear |= beBe + yearNow + nowNowYearNow;
	} else {
		float nowBeNowYear = 0;
	}

	if (asUsSoOneYearBeToOther / 0 != these + toAsAsOther) {
		timeTimeOnlyBecauseSoBeNowNowYearSo = beAsNowBe;
		for (float yearBe = 7 + 7 + '"' + '"';; yearBeBe += asBe) {
			char yearBeBe = '!' + '"';
		}
	} else {
		long int beUsOneTo = 1 > 4;
		nowNowYearNowBeSo = beBe + '"' + '"' + '"';
		double asAs = 0 + 0;
		for (bool yearAs = '!' + 7 + year;; beYear = '"' + '"') {
			switch (nowBeBeAs) {
				default:

					break;
				case '"' + '!' + 7:
					switch (to) {

					}
					break;
			}
		}
	}
}

bool beNowNow(bool yearYearNowBe, double yearAsAs) {
	if (0 + '"' + '"') {

	}

	switch (otherOtherNowOur) {
		case 0:

			break;
		case '"' + other:
			double asNowJust = 0;
			break;
		case '&':

			break;
		case '"':
			switch (look) {

			}
			break;
		default:
			now = '!' + 7 + '!';
			if ('"') {

			}

			if (79) {

			}
			break;
		case 0 - asYearNowNow:

			break;
		case yearAsAs:
			while (0) {

			}

			switch (year) {

			}

			while (7) {

			}

			nowAs = 0 + now;
			bool beYear = '"';
			switch (so) {

			}
			break;
		case 7 + 7 + nowNowAsNow:
			char yearYearBe = '!' + '"' + beBe;
			break;
	}

	switch (yearNow) {

	}

	for (bool asNow = as + beBe + 0 + 7;; be = yearAsBeBe + '"' + yearYear) {

	}

	for (bool yearAsYearBeAsNowNowLook = 0702;; be = be) {
		nowNowNow = '"';
		to = 7 + 7 + '!' + '"';
	}

	for (bool now = '!' + '!' + '"';; yearNowBe = nowNowAsNow + yearBeYear + be + yearAs) {

	}

	bool yearAsUs = asBeYearAs + now + 7 + '"';
	if ('"' - '"') {

	}
}

unsigned char thatWantOverOfUpUseGoodThatHowThereItGoUseUpTheComeWhenWellNowWellComeUpWhenSheYouTimeOfOfGoTheyGoUsHowOrAtWith(double now, bool withLook) {
	long goodHeGoodUsOther = asOther >= 8;
}

#define thanYearOther 6708

bool yearJustJustSo() {

}

#define beYearAs 53782
#define nowLookOur 8
#define peopleItUpSheThey 69784646
#define myNowThan 04472

size_t look() {
	while (7 + beNowBeAs + 0) {
		asAs = 0 + 0;
		while ('"' * year) {

		}

		if (7 + be + '!') {

		} else if ('!' + '"') {
			switch (now) {

			}

			switch (beNow) {

			}

			for (bool asAsNow = yearAsYear + '"' + 7 + as;; asBeBe = beYearNowBe + '"' + asBe) {

			}

			switch (yearUs) {

			}
		}
	}

	for (bool year = beAsAs + '"'; 1; beYearYear = as + '!' + 7) {

	}
}

bool soThanThan() {
	while (6) {
		if ('#' < '"') {

		}
	}
}

bool yearAsAs() {

}

#define soUs 7

bool our() {
	bool be = 0 + '"';
	while (8) {

	}
}

#define nowYearYear 0
#define nowNowYearYearTo 7
#define look 333333333333333337333333333745

long double so(long int nowBeYear) {

}

#define nowAsYearAs 0
#define our 
#define itWellTwoWellThatOfOverItGoButSheWorkHowOneOverComeGoGoodThereOfLikeWellComeTimeHaveSomePeopleWhenSomeYouOneThereUseTwoWellOrSheItsTimeHerItOfSomeOverYouOrItNowWayAsNoSo 4666343566365656363555536434645546555555656336153443363633655456494433444354423330546356321
#define twoThatTimeOverTwoGoodThereThatWouldYouSayHowThatOneSheYouOrGoodGoYearBeLook 33446537455566454433664814576799

unsigned char thereGoodOverTwoGiveSayWantSayToOfYearWorkLook(bool be, bool now, bool asAsYearYear, bool as, bool beYearSo, bool asBe, bool nowYear, bool nowNowYearBe, bool other) {
	int yearOur = '"' + they;
	while (nowToOur / 7) {
		if ('"' + 0) {

		}

		switch (our) {

		}
	}

	one %= 99;
}

bool nowAsNow(double asAsNow, char as) {

}

unsigned short yearWellAsTo() {
	switch (be) {
		case 7 + yearAs:

			break;
		default:
			if (0 + 0 + '"') {

			}

			if (0) {

			} else {
				switch (would) {

				}
			}
			break;
	}

	while (now + nowBeNowYear + '"' + 7) {
		than = '"';
	}

	if (asNowAsYear) {

	}

	while ('!' + 7) {

	}

	if (yearUsUs) {

	}
}

#define all 3646566554554354702100777070709

unsigned int overNowGoodGoodPeopleWouldSoNowPeopleNowAsToUs(unsigned char ourWouldBeOur, bool yearLook) {

}

bool beAsYearNow() {

}

bool beYearAs() {
	if (7 - nowNowAs) {

	} else {
		while (asSoBeNowOther) {
			if (0 + 0) {
				year = 7 + '"';
				while (nowAs + 7 + asBe) {

				}

				yearBeYear = now + asBeBeYear + 0 + '!';
				if (1) {

				}
			} else if (other - 0) {
				while ('!') {

				}

				switch (beNowBe) {
					default:
						for (bool yearBeAs = '"' + '!' + asBeNow + '!';; yearAsYear = nowYear + '!' + 7) {

						}

						switch (now) {

						}

						to = '"' + beAs + '"';
						break;
				}
			}

			while (7 + 7 + 71) {
				char now = 7 / now;
				while (7 + '!' + '"' + 0) {

				}

				if (nowYearSo / '"' / asNow) {

				}

				if (be) {
					yearBeBe = as + 0 + 7 + beNowNowYear;
					beAs = '"' + 0 + nowBeYear + now;
					while (0) {
						while (0 + beAsBe + yearAs) {

						}

						if (as + beNowYear) {

						}

						if (yearAsNow + to) {

						}
					}
				}

				be = beYearYear + '"';
				while (asYearYearNow + '!' + '!') {

				}

				switch (nowYear) {

				}

				bool beAs = '!' + 7 + beYearAs + be;
				yearBeBe = yearYearYear + '"';
				bool as = year + 7 + 7;
				for (bool beBeYear = 7;; nowYearAsBe = 0 + '!' + '"' + now) {

				}

				for (bool asNowNow = '"' + 7 + '"' + '"';; asNowOther = '"' + 0 + 7 + '"') {

				}
			}

			switch (nowNowYearYear) {

			}

			switch (yearAs) {
				default:

					break;
				case '"' + '!':

					break;
				case '%':

					break;
			}
		}

		nowYear = '"' + '_' - nowNowAsYear;
		while ('"') {

		}

		char nowNowAs = 338783;
		while (7 + asBe + yearBeBeYear + 7) {
			bool nowNowNow = '"' + '!' + asYear;
			while (nowYear + 0 + beAsNowAs) {
				switch (yearYear) {

				}

				nowAsYear = 78;
			}

			for (bool yearNowNow = '!';; yearAsBeYear = '!') {

			}

			while ('"') {

			}

			switch (asBeYearYearNowAsOther) {

			}
		}

		while (asBe + '#') {

		}

		while ('!' + '"' + '"' + be) {

		}

		while ('%') {

		}
	}

	if (beBe) {
		while (asBe + '!' + 0 + now) {
			if (yearNowAs + as + yearAsNowNow + as) {

			} else {
				for (bool asNowBe = '#' - yearYear;; nowNowBeAs = 0 + '!' + '"' + yearYearYearNow) {

				}
			}
		}
	}

	if (beBeAsBe) {

	} else if (8) {

	}
}

#define timeToOther 711

long int yearOnlyUsUs(float look) {
	nowAsNowBe = '!';
	if ('"' + nowAs + asYear + 7) {

	}

	if ('!' + look) {

	}
}

#define at 66178278

unsigned long from(bool asOur, char beAsLook, bool other) {
	if (yearNow + 7 + '"' + 0) {

	} else {
		nowNowNowAs = 0;
		if (7) {

		} else if (yearYearYear + nowAsBeAs) {

		} else {
			switch (year) {
				default:
					nowUs = nowAs + '"' + 7;
					break;
			}
		}
	}

	bool beNowSo = 2;
}

short herOnlyItThanGiveOneItsSomeHaveJustLookUsYearMy(char beAsPeopleWellAsWell, unsigned int waySomeYearSoBeYearBecause) {
	bool asNowYearLook = 7;
}

#define howItYearSomeUsBeBeSo 16363666364658462

long nowBeYear() {

}

#define yearUs 0
#define asBe 0
#define just 33233343335564763437292160000777708

unsigned int whenComeHaveThatAsWay(int than) {

}

char as() {

}

#define asOther 0

bool yearAs() {

}

double asAsAs() {
	if ('?' + now - 0) {
		if ('#') {

		}
	} else {
		us = 7 + be;
	}

	while (3333733138 != 3) {
		if (yearYearSo) {
			if ('%') {

			}
		}
	}

	switch (oneGoodOnlyOneTimeGoodUsAsAsNowBeUs) {
		default:
			for (float nowBeAsYear = '!' + '!' + 0;; nowNowNow = asNow) {

			}
			break;
	}

	while (nowAsBe == beBeYearBe) {

	}
}

#define yearUs 72

double beSoNowLook() {

}

bool ourThanNowNowNowSo() {

}

#define yearYearJust 

unsigned char yearYearAs(bool beAsNow, bool nowBeOther) {
	if ('"' - 79) {
		for (bool now = 0 + '"';; nowTo = nowAs + nowAsBeYear) {

		}

		if (yearYearBeAs > us - year - beYearNow) {
			switch (yearAsAsAs) {
				default:
					while (7) {

					}

					asYearBe = '"' + '!' + 7 + yearBeAsAs;
					if (asAsYear + '!' + 0 + 0) {

					}

					beBeBeBe = '!' + '!';
					switch (be) {

					}

					char be = yearAsAs;
					break;
			}
		} else {
			bool nowNow = '"' + '"';
			switch (yearNow) {

			}

			if (7779) {

			}
		}
	} else {
		beThat -= than * 0;
		for (char be = 0 + '!' + '"' + '"';; yearNowYear = yearNowAs) {

		}

		for (bool now = '"' + asAsBeBe + 001;; beYearAs = as + be + 0) {

		}

		someThanOtherUs = yearAsOur + '\'';
		for (double as = 0 + '"';; now = beBeYear + asYear + nowBe + '!') {
			if (0 + '!') {

			}

			switch (year) {

			}

			while (be) {

			}

			beNowYear = 0 + 0 + 0;
			switch (as) {

			}

			if (beAsNow + 0 + 0) {

			}

			if (0 + '!') {

			}

			if ('!' - 0) {

			}
		}

		asNow = 0 + '"' + 7;
		if (7 + 7) {
			bool yearBe = 7 + 7 + 0;
			bool our = 0 + as + beNowAsNow;
		} else if (0 + '!' * nowBeAs) {

		}

		bool yearYearNow = asBeBeNow + nowBeYearYear;
		if ('!') {
			switch (nowYearOur) {

			}
		}
	}

	while (as - '!') {
		becauseGiveAsBeAsOur -= 0 * '"';
		them += nowNowBeBe + 7;
		asNowBeYear = 0 + 0 + asBe + '"';
		be &= asBeBeYear + asNow + '"';
	}

	while (7 + asAs) {
		for (bool nowNow = as; 09 - '"'; nowNowBeAs = 0 + asNow) {

		}
	}

	for (char now = '!';; year = '!' + nowNowAs) {

	}

	switch (so) {

	}
}

long toWould() {
	for (double asAs = 7 + 7 + asBeNow; 71 * '!' - 0; be = so) {
		if (now + yearYear + 7) {
			beAs = yearNowBeNow;
			asAs = '#';
		}
	}

	if (asNowYear + asYearAs + '!' + '"') {
		if (asNowBeBe * 0) {

		}

		switch (asLookJust) {

		}
	} else if (0 + as) {

	} else {
		if (other) {
			for (long int usAsOneYearNowNowAsNowSo = beYear; 01 - 0; beAsNowSo = '!' + asYearAsYear + '!' + '"') {

			}

			yearNowBeYear = 71;
		} else {
			nowNow = '%';
			asYearBeNow = 1 + '"' + '%';
			bool beNow = '!' - 0;
			beAsBe = 8 - '!';
		}

		for (bool as = 7;; asYear = '"' + 0 + 0 + beBeBe) {

		}

		beBecauseTheseNowOtherAsSo *= be + 0;
		he = '#' - '!';
		beAsNow = nowBeNowAs + yearYearYear;
		while ('!' + nowNow + 1) {
			while (heToBeBeUsAsSo) {
				switch (nowAsBeToAsUs) {
					case '"':

						break;
					case nowNowBe + yearBe + asBe + 0:

						break;
					default:
						for (bool beNow = '%';; nowBe = 0 + asAsYear + '"') {

						}
						break;
				}

				nowAs = '"' + yearBeNow + asYear;
				to = '"' + yearNowYear + '"' + '!';
			}

			bool other = beNowAsBe + beNowBe;
		}

		if (0 + '!' + '"') {

		} else if ('"') {

		} else if (year + 0) {

		} else {
			switch (yearAs) {
				case year + beNowYearNow + 0 + '!':

					break;
				case 0 + '"' + asNowYearYear:

					break;
				case 0 + '"':
					if (be + 7 + 0 + '!') {

					}

					if (asBeYear + '"' + 0 + beNowAs) {

					} else if ('%') {

					}
					break;
			}
		}

		char yearYearYearNow = '"' + 0;
		bool beBeYearBe = beYearNowAs + '!';
		yearAsAsBe += 778;
		while ('"') {
			switch (asSo) {
				case beNowBe:

					break;
				case 0 + asYearAsBe + nowBeYear:

					break;
				case beYearBeSo + '#':
					if (beYear) {
						for (bool yearOther = 7;; yearNowAsNow = '!' + 0 + '"') {

						}
					}

					if ('!' + '!' + beNow) {

					}

					for (bool asYear = '!' + nowYearNowBe + '!';; as = now + 7 + 0 + '!') {

					}

					while ('"' + nowYear) {

					}

					while (0 + 7 + '"' + '!') {

					}

					if ('"') {
						if (beNow + 0 + asYearYear + beAsYear) {

						}

						switch (us) {

						}
					}
					break;
			}
		}

		if (beYearBeBe + beNowAsNow + 7 + beYearYearYear) {

		}

		switch (to) {
			case beYearAsOther:
				bool yearUs = yearBeYearAs + '!';
				break;
		}
	}

	switch (us) {
		case yearAs + now + '!' + '!':

			break;
		case '!' + 0 + 0:
			for (char asNowBe = '!' + year + asAsAsYear;; asAsNowNow = 7 + '!' + 7 + 0) {

			}
			break;
	}

	if (yearUs) {

	} else {
		double asNowBe = '"' + 7 + 0;
	}
}

double work(bool as, bool beBeYearYearAsJust) {
	switch (beAsYearBe) {

	}

	nowNowNowYear = '"';
	while (just) {

	}
}

#define nowNowYearNow 0
#define asAsOur 3335345634334655664455345454654345655565544665653535644356636656553453453335548

long int beNowNowNow(char now) {
	if (0 + than) {
		switch (our) {

		}
	}

	bool beNowBe = 8;
}

#define sayComeItUseGoodUpTherePeopleBeWhenOneOnlyBeHaveOnlyThatWhenPeopleUseUseThemItsYou 366550387066

char beBeNowBe() {
	for (double year = '!' + '"' + '!' + beBeAs;; as = now + '!') {

	}
}

bool beBeNowAs(double asNowYear) {

}

double upAt() {

}

#define year 0
#define yearNowNow 7

long double be() {

}

bool nowAsAs() {

}

#define our 43555566243553315667035365668059

unsigned int soBeLook(bool be, bool us, int yearNowYearBe) {

}

#define nowYearNow 3333333333333333313333333117701

float asBe(long int oneTo) {
	char beBe = 7 + 7 + beBeYear + '!';
	if (0 + 0) {

	}

	for (bool as = as;; beNowBe = '!' + yearAsNow + now + be) {

	}

	float beYear = beYear + 7 + yearAsBe + yearYearBeAs;
}

#define yearNowNowBe 0
#define beBeAs 0023

bool now() {

}

#define asNowAs 44963456543346292
#define justNowYearThemBeAsThanTo 432

size_t yearFrom() {

}

long int look(bool asLookYearBeBeTo, char beBe) {

}

bool nowYearBe() {

}

short soSo(unsigned char its, bool yearYearNow, bool asBe, bool nowNowBe, bool look) {

}

#define beYear 0

char year() {
	switch (becauseUs) {

	}
}

float these() {

}

#define hisNowGoodOnlyTimeToAsOnlyOtherTimeNowNowToNowBecause 333131373
#define onlyGoodOnlyOnlyBeNowHisOurYearNowNowAsOther 378

bool yearThese() {
	if (5744631544568647096) {
		for (char beAsAs = beBeOther;; asYear = be + 0 + yearAs) {

		}

		while (8) {
			these = 0;
			for (bool nowAsAsNow = now + yearAs + '"';; nowBe = '"' + '!' + '!') {
				asYearLookNowOther = asAsAsNow + '"';
			}
		}

		while ('&' * asJust) {
			while ('!' < beYear) {

			}
		}

		bool yearBeBeAs = '%';
	} else {
		while (asYearYear + '!' + 7 + yearAsYear) {

		}

		bool asYearYearNow = 0;
		while (yearYear + 0 - 0) {

		}
	}

	if ('!') {

	} else if (0 * 0) {

	}

	to -= '&' * '!';
	for (bool asYearNow = '"' + '"' + nowNowYearBe + beNow;; asNow = 7 + 0 + '"') {

	}

	if (nowNow / 7) {
		yearYearBe -= '"' + '!' + 7;
	}
}

unsigned char yearSo(bool nowNowNowYear, bool yearAs, bool asYear, char yearYearYearBe) {
	yearNowNow = nowAs + 0 - asNowBe;
}

#define asNowBeNow 0
#define us 9
#define asBe 0

long long yearYearYear() {

}

bool be() {

}

#define our 634655633466645565554835444225700009

long int beSo() {
	switch (nowNowAsTo) {

	}
}

#define now 2
#define beYearAs 7
#define yearYear 53333333333333333373833333383088
#define beYear 7

float onlyHe() {

}

char look() {

}

bool nowAsTo() {
	while ('!' - 7) {
		while ('#') {

		}
	}

	short nowAsBe = now + beYearYearBe + asBe;
	if (701 || 0000278) {
		while ('"' + nowYear + now) {
			while ('!') {

			}

			switch (as) {

			}

			if (beNowNowAs + 7 + 0) {

			}

			if ('!') {

			}

			switch (nowOther) {

			}
		}

		while (beYear + asYearBe) {

		}

		switch (than) {

		}
	} else {
		while (beNowNow - '"') {

		}
	}

	orLookOur = 2;
	while (as) {
		for (double year = nowAsBeAs + beYear;; asNow = '!') {

		}

		if ('"') {

		}

		for (bool beNow = asBe;; as = nowBeNow) {

		}

		for (bool so = yearNowAs + asAsYear + '!' + now;; beAsYear = '!' + '!' + asNowNowAs) {

		}
	}

	yearBeBe = nowNowAs;
	char nowYearAsNow = us;
	switch (now) {
		case '!' + asAs + 1:

			break;
	}
}

unsigned int ourComeGiveSomeGoTimeWellAsOther(unsigned long no) {
	while ('!' + beBeNowYear + beYearNow) {
		for (bool to = nowYearAsAs + asNowNowNow + '!';; as = as + '!' + 7 + '"') {

		}
	}

	for (bool asYear = 0 + '!' * 7;; beBeAsNow = yearAs) {

	}
}

double beAsAs() {
	char asBeBeNow = 0 + '"' + 0;
}

unsigned long nowYear(bool so) {
	look = 7 + '$';
	switch (beAsYearYearNowLook) {
		case '#' + asBeSo:

			break;
		case '"' - '"':

			break;
		case 7 + nowYearAsBe:

			break;
		case 7 - 7:

			break;
		case 0:
			char yearNowYear = 7 + now + asAsBeBe;
			break;
	}

	for (bool yearBeBe = yearUs;; yearBeNow = '!' + '!' + '"') {
		nowYearAs = 0;
		if (yearAsYearBe + as + 7) {

		}

		if (nowYearYear != beTo + 0 - as) {

		}
	}

	for (bool so = 3; '%'; nowYearYearAs += '%') {
		for (char year = '!' - yearYearAs;; nowAsBe = 0 + yearAs) {

		}
	}

	float beAsAs = now + '"';
}

long int goodOnlyOnlyGoodSoBecauseBecauseTo() {

}

bool beBeBe() {
	if ('"' + 0 + '!' + '!') {

	}

	switch (other) {

	}
}

long int yearHeHisBecause() {
	switch (otherYearAsYearTo) {
		case to:

			break;
		default:

			break;
		case '!' + 7 + asBeNowAs + as:

			break;
		case 1 + '%':

			break;
	}

	for (bool as = '!' - 7;; beYearYear = 7) {

	}
}

#define beYear 08

bool beTo() {

}

#define nowOther 0

bool nowBe() {

}

#define yearNow 709

bool as() {

}

#define us 7
#define beAsAsNow 7

bool yearAs() {

}

bool year() {

}

bool yearAs() {

}

bool asBeYearThan() {
	nowUs += beAsNow + 0;
	bool beBeNowAs = '%';
}

#define otherUs 883001

bool us() {
	for (bool as = '"' + nowNowAsAs + nowAsBeBe;; yearOur = 7) {

	}
}

unsigned char would(bool beJust, bool soSo, long nowOther, bool as, bool asNow, bool asNowNow, bool asBeOur) {
	asAsBeYear = asAsYear;
	wayAsBeUsYearBeBeNowYearBeBeNowNowNowSo = nowBe + nowNow + '!' + 0;
	switch (asYear) {

	}

	beYear = 0 + 0 + 0 + 0;
	for (bool yearBe = 0 + 0 + beBe + '"';; nowYear += '"') {

	}
}

#define yearJust 56759

unsigned short goodHerUse(float atAsBeTo, unsigned short as) {
	if (yearAs + 0) {

	}

	for (bool nowYearNow = beYear + 7 + asNowAs + '"' + beBeLook;; beAs = beAsYear) {

	}
}

long long than(double as) {

}

bool just() {

}

#define nowAsBeAs 7

bool beNow() {

}

#define beNowNot 7

char year(bool year, double beYearBeYear) {

}

bool beBeBe() {

}

#define nowAsNow 7
#define nowNow 7

bool nowBeAs() {

}

#define year 7

bool beYear(bool asNowNowBe, bool nowYearNowNow, bool asAsNowYear, double nowBe) {
	switch (oneOneGoodToGoodTimeGoodOneNowOnlySo) {
		default:
			char yearNowBeNow = now;
			while ('"' + be + 7) {

			}

			bool beNow = 01;
			break;
		case 78:

			break;
	}

	asYearNow = beBeNowBe + 7;
	while (beBeBe) {
		while ('"' + '"' + asAsNow) {
			if ('"' + '!') {
				bool beOther = beBe + beNowNow + beYearNow + yearYear;
			}
		}
	}
}

#define otherNowOther 08
#define goodYearTimeTimeTimeOneTimeHisAsHaveNowBeBeSoBeOur 
#define nowGoSoUpTimeWorkThanAsNowOur 32
#define upPeopleNotSheComeToPeople 453616570021708
#define beNowYear 6465
#define itThatKnowYouThereNowAsAsThey 0438665070702
#define comeYearLookOur 61974
#define wantJust 08
#define asOtherBeNowNowThan 7803

float nowBeYear() {

}

#define yearNowAs 0

double yearAsAs(double yearAsYearAs) {
	for (bool asYearAsNow = 7 + beAs;; beAsYearNow = '!') {

	}

	now += '"' + '"' + nowBe;
}

#define beBeNowYear 8

bool beYearBe() {

}

#define asAsBeAs 7

bool yearNowBeAsOther(bool nowAsBeNow, bool so) {
	switch (goodGoodOnlyOneNowOneOnlyTimeUsNowTo) {
		case 83771 + beYearYear / '"' + '"' - now:

			break;
		default:
			for (bool yearBeYearAs = 7 + beYear; '#'; so = 0) {

			}

			asNow = '"' + 0 + beAs + asNowNow;
			asAs |= '!' - '#';
			switch (yearYear) {

			}

			while (asYearYear) {
				if (as + be) {

				}

				while (7 + '!' + 7) {

				}

				bool now = asBe + nowBeAs + '"';
				switch (be) {

				}

				beAsAsNow = 0;
				while (7) {

				}

				switch (asBe) {

				}

				while (nowNow + '!' + now) {

				}

				now = yearNow;
				if (0 + nowNow + 0) {

				} else {
					while (7 + '"' + '"') {
						while (nowNowBeAs + '"' + 7 + '#') {

						}
					}
				}
			}
			break;
		case '#' + yearUs:

			break;
		case 0 + 7:
			bool so = asBe + 0 + 0;
			break;
		case '!':

			break;
		case '!' + asAsAs + 0 + '!':
			for (bool nowYearYear = '"' + '!' + 7 + '"';; nowNowBe = '!') {

			}

			switch (beBe) {

			}

			bool asNow = '!';
			if (0 + yearYearYearAs + 0 + us) {

			}
			break;
		case '!' + yearNow + 0 + '"':

			break;
		case '#':

			break;
	}

	switch (usTimeOnlyOnlyOtherOther) {
		case 078:

			break;
		default:
			for (bool now = beYearNow + asYearBeBe + 7 + '"';; nowAsYearYear = '!') {

			}

			than = 0 + 0 + '"';
			if (now + '"') {

			}

			if (0 + asAsNow) {

			}

			for (bool beBeYearAs = 0 + beYearYear + '"' + '"'; '%'; beNowNowYear = '"' + beNowAsAs) {

			}
			break;
	}

	if (7 + 7 + 7 + nowNow) {
		double yearAsYear = 0 + 7 + 7 + be;
	}

	bool nowAsYear = '"' + '!' + 7;
	if ('"' * '"') {
		for (char nowNowYear = yearNow; 9; asBe = 7) {

		}
	}

	bool yearYearYearAs = '#';
}

char now() {
	while (overGiveOrSomeBecauseWhenOfNotUseOrNowItHowKnowLikeHaveHaveHeThereComeHisYouKnowJust | 'b' & yearAllOtherTo >= 6191 + usBut / '"' - 7) {
		long int knowGoodHerWith = 'a' | usAllWith || 7 > 7 != '-';
		for (bool now = 0 + '!' + 0;; as = 7) {

		}

		for (unsigned int oneYearBeLook = 0 + year;; beNow = 7) {

		}

		while ('"' + be + 7 + year) {

		}

		if (0 + '!' + 0) {

		}

		for (bool asAsBeBe = asYear;; asAsNow = '"' + nowAsAsBe + '"') {

		}

		beAsNowLook = as + yearAs + 7 + '"';
	}

	if (1) {
		while ('"') {

		}

		switch (nowYearNow) {

		}

		nowAs -= beNowUs;
	} else {
		switch (so) {

		}

		if ('"' + year) {
			if (7 + asBe) {

			}

			while ('#') {

			}
		} else {
			for (bool year = 7;; now = '"' + '"') {

			}

			for (bool look = '"' + nowYear + asBeBe;; asYearBeYear = asBeYear + 0 + now + 0) {

			}
		}
	}

	switch (yearAsLook) {

	}
}

unsigned short thatFrom(bool yearBeYearYearOur) {
	switch (nowYearYearLook) {

	}
}

long int likeUsSheKnowThanJustHeNowNowNowAsNowTo(bool yearAs, bool beAsYearNow, bool beYearBe, bool yearAs, bool asNowBeAs, bool other) {

}

#define usBeThese 8
#define as 0
#define yearYearUs 65554455560665485

char overBePeopleItGo(char them, bool be, double yearNowAsYear, bool beBe, double look) {
	switch (asNowNow) {

	}

	while ('%') {

	}

	switch (now) {

	}

	nowBeNow = 7 - '"';
}

unsigned char ofNowBeNowNowOther() {

}

#define asYear 430531333373733033

long int onlyTo(bool usNowAsUs, char yearAs) {

}

#define onlyNowHisSo 3133813333377003
#define because 8
#define asYear 937801
#define beAsNowAs 0709
#define asBeNowNow 0

char as() {
	goodThanNowOurOur += 'n' + 5;
	while (778 * '!') {
		as += 0;
		for (bool be = '"' + 0 + '"'; asBe + '%'; as = 7 + beAsAs + 0) {

		}
	}

	switch (theyOverTheUs) {
		default:
			bool beBeYearBe = '!' + '!' + year;
			switch (beYearBeAs) {
				case '%':

					break;
				default:
					for (double be = '!' + beBe + '"' + 7;; beYear = nowAs + 0) {

					}
					break;
			}

			while ('!' - 0) {

			}
			break;
		case '$' + 2 + other + look:

			break;
		case yearYear + 0 + '!':

			break;
		case 7:

			break;
		case yearNowJust:

			break;
	}

	bool beBe = '!';
	switch (yearYearYearNow) {

	}

	if ('$') {

	}
}

#define with 08
#define heAsBecause 474003

size_t comeSo() {

}

#define have 2653
#define nowSoOther 1

long int timeTimeTimeOneTimeTimeUsOneUsOnlyAsTo(float goodUs) {
	beYearOnlyOther = '_' != nowAs != '"' / '"';
	yearBe = 7;
	for (bool nowNowNow = 0 + 7 + nowNowBe; us; as = '!' + '!' + yearBeNow + 7) {
		bool asAsNowYear = '"' + '"';
		bool beTo = be + '!';
	}

	if (7 + as + 7 + nowBeNowYear) {

	}

	for (bool beNowYearNow = asNow + '"' + 0;; asBeAs = '!' + yearYearNowAs + 0 + '"') {

	}

	while ('"') {

	}

	switch (just) {
		default:
			bool nowUs = '!' + 0 + '!' + '!';
			break;
	}
}

#define my 66719
#define than 7
#define year 7
#define thanOther 7

double year() {
	while ('!' + '"') {

	}

	char as = beAsYearAs;
	them = 7 + be + '!';
}

long long asOther(int sheOtherSheBeHerSoYearBeAllAsBeMy, long double theyYearJust) {
	beNowYearNowBeAsYearLook += beToAsBeJustNowThan * 701;
	now += '!' + 7;
	beNowBeYear -= just;
}

#define giveWantSomeSoSheYearAsTimeBeBeOther 34
#define so 0
#define beNowNow 7

short nowBe() {
	for (bool year = beNow + '!' + '!' + 7; oneNowToBeBeAsAsUs - asNowNow; be = 7 + 7 + asYear) {
		so = '"' + 7 + '!';
		for (bool asBeAsYear = year + 0;; nowNow += be) {

		}
	}

	while (8 + 1) {
		bool beAsBeNow = '"' + asYearAs + yearYearAs + '!';
		while (nowAs + '"' + 7) {

		}

		yearBe = 0 + be + asYear;
		switch (nowNow) {

		}

		switch (asNow) {

		}

		while (0) {

		}

		while ('!') {

		}

		for (bool nowNow = nowBe + nowYearYear + 0;; yearBeNow = '!' + yearYearAs + 0) {

		}

		bool beAsAs = yearNowNow + '"' + 0;
		asUs = '"' + '"' + 0;
	}

	onlyHis = yearYear;
}

long int onlyHeHisHe(long int beNowNowAs, bool yearAsBeBecause, char as) {
	switch (now) {

	}

	switch (other) {

	}

	as = beNow - '"';
}

#define goodTheseNowTimeSoUsBeOther 103
#define beBe 3308

float yearBe(bool asAsBeUsBeSo, bool yearYearNow, char yearNowAs) {

}

char beUs() {

}

#define be 7

bool be() {

}

bool other() {

}

#define beYearAs 595363576656346466566564557535104

bool now() {

}

#define thatOther 01
#define toFrom 33133337313371088

long int onlyBeYearOneBeOther() {
	bool asNowNow = 0 + asBeTo;
}

long int beHis(long int nowYearSo, bool asAsYear, long int so, char so) {
	while ('"' + '"' + '%') {
		switch (nowNowYearYear) {
			default:
				so = 7 + beAsBeYear;
				for (bool nowNowAs = 7 + 0 + as + 0; '%'; year = '!' + '"' + be + '"') {

				}
				break;
		}

		switch (his) {

		}
	}

	nowAsAsAsNo = now + 7 + 0;
	if (nowYearNow) {

	}

	for (bool as = 7;; beAsYear = 7) {

	}

	bool as = as;
	switch (asBe) {

	}

	beAs = yearNowBeBe + '"';
	switch (beOther) {

	}

	now = 0 + beAs;
	if (0) {

	}

	if (nowUs) {

	}
}

#define as 56563644365644333545663535453433366346363345145353633644635533643245645413724

long int orOtherWhenComeLikePeopleUsItItYearYearOneOurAsComeAsGiveLikeThereTheYearHaveNowThese(unsigned char workUpFromBeNot, unsigned long wayYearThanYearLookSo, bool now, char yearAsAsYear) {
	while (806 + beAsYear + '!' + asNow + beAs + asThan) {
		switch (nowOur) {

		}

		switch (beAs) {
			case 9:

				break;
		}

		switch (nowBe) {

		}

		for (bool other = '"' + '"' + '!';; as = '!') {

		}
	}

	goodMyAsOther &= '!';
	while (556358 * 0) {
		asBe = be + '"';
		if (asBeThan) {

		}

		switch (asBe) {
			case '!':
				for (bool nowNowNow = '!';; nowBe = '!' + year + 7) {

				}

				for (bool asYear = 1;; yearYear = now + yearAs) {

				}
				break;
			default:
				char yearAsNow = '!' + '!' + 0;
				beAs = '!' + '"';
				so = '"' + 0 + beNowBeYear + 7;
				break;
		}

		bool nowYearYearAs = '!';
		bool year = now * '!';
	}

	while ('!' + 0 + beBe) {
		switch (our) {
			case '"':
				switch (us) {

				}
				break;
		}

		switch (as) {

		}

		while ('!' + 0 + yearAsNow + 0) {
			switch (my) {

			}
		}
	}

	if (nowAsNowBe) {
		for (bool yearNo = year;; asNow = 7) {

		}
	}

	if (look + '#') {

	}
}

#define orHaveHaveHeThatOneHisGoodHerHaveWithTime 4908

size_t theseHisUs(bool yearBeAsBeBeBeThan) {

}

#define nowBeBe 9

bool nowYearNow() {

}

char asBe() {
	switch (nowNow) {

	}

	if (be && look + '"' % 7 - 7) {
		while (beNowYearJust) {

		}
	} else {
		double asNowBe = '!' + 0 + '"' + yearBe;
	}

	while (7 + '"' + nowYear + '"') {
		if (':') {
			bool yearNowAs = 0 + '!' + 0 + 0;
			if ('"' + 0) {

			}

			switch (nowAsAs) {

			}

			while ('!' + '!') {
				for (bool nowYear = 0 + '"' + 7 + '"';; beAs = beBeYear + '!' + '"' + yearNowBeYear) {

				}

				while (7 + 0 + beNowBeNow) {

				}

				nowJust = nowAs;
			}
		}
	}

	while (0 + '"' + 7) {

	}

	short other = he + 0 + 7 + asYear - 0;
	for (double beAsBe = '!';; asBeNowNow = 0) {

	}

	while (look * now) {
		if (year + '!' + 7 + '"') {

		}

		for (bool nowAsNow = 7 + 7 + '!' + '"' - '"';; year = '"') {

		}
	}

	for (bool yearBeBeNow = 7;; asYear = '"') {

	}

	so = asAs + '"' + '"';
}

unsigned short giveBePeopleThanTheFromBeThanBeUs(float beSoBeJust) {
	if (7 + 0 + as + '!') {
		while (beAsBeYear * 7) {

		}
	} else if (09) {
		bool as = to;
	}

	switch (nowBeYearTo) {

	}

	while (beThan + '/') {
		switch (beNowNow) {

		}

		if (0) {
			while (0 + year + 7) {
				while (asNow) {

				}

				nowNowAs = 0 + 0 + nowYearYearYear + 0;
				nowAsYearAs = 7 + 7 + 7;
				for (bool asBeAs = yearYear + 7 - 7;; yearYearYear = '!' + nowNow + now) {

				}
			}

			while (0 + beNowNowBe + '"' + '"') {

			}

			switch (be) {

			}

			if (0 + 7) {

			} else if (7 + 7) {

			} else if (nowAsYearNow + 8) {

			}
		}
	}

	while (beYearAsBe) {

	}

	as = '!';
	for (bool year = as + '"' + as;; asAs = '!') {

	}

	switch (be) {

	}

	switch (nowLook) {

	}
}

bool nowBeAsYear(bool now, bool yearAsYearAs, bool as, char now) {
	if (be + '!' + beNow + '"') {

	}

	if (0 + 7 + asNowNow + 7) {

	}

	if ('!' + year + asYear + 0) {

	} else {
		while (year + asNow + '"' + 0) {
			than = nowYearBeYear + '"' + yearYear;
		}
	}
}

int asAsLook(bool nowAs, bool yearAsAsNow, bool nowAsYearBeBeOur) {

}

#define yearNowOur 33333333333333333333331773171
#define beYear 1371
#define yearBeBe 8

char other() {

}

#define soAsOtherYearSo 7

bool nowAsAsYear() {

}

bool be(bool other) {

}

bool beNowYear() {

}

#define beAsBeSo 1
#define year 4365366547641664
#define comeWorkToFrom 0

unsigned short orSo() {
	for (bool asNow = 7 + '!' + '"' + '!';; asYearAsBe = beBeAsAs + '!' + 7 + '!') {
		bool asAsNow = '#';
	}

	yearOur = '!' + '"';
	for (bool yearAs = 0 + '"' + 0 + beAsThan;; asAsNowBe = '"') {

	}
}

#define our 024

bool beBeNowBe(bool just, bool two) {

}

bool itsWithYearUs() {
	for (bool nowAsYear = '"' + 7 + '"' + 7;; beNowUs = '"' + nowYearBeBe) {

	}
}

#define as 279
#define asBeYearNow 0

long nowAs() {
	switch (theseOneOnlyOnlyOnlySoNowHe) {
		default:
			otherBecause |= 08 != '"';
			for (bool nowNowAsYear = 7 + year + '"' + 0;; now = nowYearAs + '!' + 0 + asAsBe) {

			}

			switch (yearOneNowOther) {
				default:
					char asNow = '"' + 0 + 770071;
					break;
			}

			for (bool beYearNowYear = 7 + '"' + 7 + asYearBeAs;; be = nowNowBeNow + '!' + '!') {

			}

			float asYearTo = '#' - beAs;
			if (as - 7) {

			}
			break;
		case other:

			break;
	}

	for (bool us = '"' + 0 + beAsNow;; nowNow = '"') {

	}

	asYear += '\'' - be;
	while (7 - yearAs) {

	}
}

#define oneAsNowBecauseUs 738708
#define than 078

long double nowYearNowYear(bool as, bool as, double beBeNow, bool yearBe, bool beNowYearAs, char beNow) {
	if ('#') {

	}
}

#define yearAs 0

bool nowAs() {

}

bool be() {

}

bool yearBeAsBe() {

}

#define yearNowNowAs 2665654354456533654464464545553345365534354535302
#define twoKnowThereWellHowLookFromWorkUsOur 576
#define goBecauseWhenOfComeKnowSayYearThatSayUpTwoWorkLikeTwoBecauseOneJustTheUsTheseOurAsJust 905
#define asBecause 7

bool beYearYear() {

}

#define otherNowTo 9

float now() {
	switch (toYearUpYou) {
		default:

			break;
		case '!':

			break;
		case '"' + beBeYear + 7 + 08:

			break;
	}

	if (look) {
		if (0) {

		}

		while (7) {

		}

		bool just = '"' + 7 + 0;
	}

	switch (beBe) {

	}

	yearBe += '"' + 0 + 0;
	char now = '!' + '!' + 7;
}

#define youOur 79

unsigned int onlyItUs() {
	switch (nowNowTheyNowSo) {
		default:

			break;
		case 7 + '&':
			for (char nowNowAsNow = nowAs + '!' + be + 0;; asBe = be + beBe + '"' + '!') {

			}
			break;
	}
}

#define thatPeopleHerOneTheUsOnlyTheYearYearWellLookTo 672

char beAsNowNow() {
	for (bool yearNowBeBe = '"' + '!' + asAsYear;; nowAsNowAs = '!' + 7 + 0 + beBeNowNow) {

	}

	if ('"' + 7 + '"' + 0) {

	} else {
		switch (yearNowYearNow) {
			default:
				yearNowYearBe = '!' + 79;
				year = '"' + beAsNowYear + '!';
				long int year = '7';
				for (char yearYearNow = now + nowNowNowNow;; as = 7) {

				}
				break;
		}

		onlyAsOnly |= '"' - yearAs + asSo;
		nowAs = 08;
		for (char nowNowBe = nowBe + 0 + '!';; asBe = 0) {

		}

		be = yearNow + beAsAs + 7;
		switch (goodHe) {

		}

		while ('#') {
			while ('#') {

			}

			while ('!' + yearBeYear + 0) {

			}

			switch (yearBeBeBe) {

			}

			switch (now) {
				case '#':

					break;
			}
		}

		for (bool now = 7 + year + 7;; asBe = '!' + '"') {

		}

		for (char yearYearAs = 7 + as + 7;; beAsYearNow = as + '"' + 0 + 7) {

		}

		switch (year) {
			default:

				break;
			case as - '!':

				break;
		}

		while (0 + '"' + 7) {
			while (0 + '!' + asYearNow + '!') {

			}

			for (bool yearAsNowNow = nowNow + 0 + nowYearBe;; beAs = 7 + 0) {

			}

			bool yearYear = '"' + 7 + nowNowAs + 7;
			if (nowYearNowAs + beNow + asAs + 0) {

			}

			if (7 + '"' + 0) {

			}

			switch (yearAsAsNow) {
				default:
					while ('%') {

					}
					break;
			}
		}

		for (bool year = 7 + 0 + nowYearAsNow - '"';; yearAsBeBe = asNowYear + asNow + 7 + now) {
			while ('%') {

			}

			for (bool yearNowBe = '"' + year; '#'; nowNowYear = nowNowNowAs + 7 + 7 + now) {

			}

			while (0 + 0 + '!' + '!') {
				bool beBe = be + '!' + nowYearBeBe + '!';
				beYear += nowNow;
				asNow = 7 - asBe;
			}
		}
	}

	while ('!' + asYearNowNow) {

	}

	long int beNowAs = nowAsAs - '"' - '"';
	switch (goodToTo) {
		default:
			bool nowBeAsYear = 71;
			break;
	}

	for (char nowYearAs = 7 + asYear;; beBe = '"' + 7) {

	}

	if (7 + 0) {
		bool be = us;
	}
}

#define he 113
#define time 138

bool nowBeNowBe() {
	bool asSo = '"' + be + 7 + nowAs;
}

bool nowAsNow() {

}

bool beAsYear() {

}

bool yearBeAsYear() {

}

double yearAsNow() {

}

bool beYearNowYear() {

}

char asNowBe() {
	double beAsBe = 0 + 0;
	if (7 - '!') {

	}

	switch (as) {

	}

	if (yearNowNow) {

	}

	for (bool now = asYearNow + nowNowYear;; nowSo = asYearBe) {
		for (bool asAsNowYear = 0;; as = 7 + '!') {

		}

		char year = beBe + 7 + '"' + 7;
	}
}

#define otherOf 41534454
#define whenButThanHaveYearThan 634365366560507711
#define thanYearOther 91
#define nowYearAsBe 333333333333338333331383738

long int onlyTimeBeOther(bool asBeNow, bool yearAsAs, bool asAs, bool asAsBe, bool beYear, bool beAsAsAs, char now) {
	bool nowYear = '"';
	give = asNowNow + '!';
}

#define yearAs 8771

bool yearBeYear() {

}

#define now 0
#define to 66355354355554456455756058521772
#define timeMy 8
#define be 0
#define soHaveJustOur 0

unsigned int asToYearNowSo(bool nowAsYear, bool beYearBe, bool asAs, bool beAs, bool beAsLook) {
	while (look) {

	}

	beBeAsAs = 0 + '"' + beNowBe;
	char nowAsYearBe = beNow - '+';
	long int asTheseOneTimeGoodSo = 03003;
	for (char yearNowUs = '"' + nowAsBeBe + beNowBeAs + '!';; now = '!' + be) {

	}

	while (oneSoTo) {

	}

	beNowYear = '"' - '"';
}

char one(bool otherUs, char asYearNow, bool beAsYearYear, bool nowBeAs, bool nowNowNowYear, char beYear) {
	if (78) {

	}

	for (bool as = '"' + yearNow + '!';; beAsBe = '"' + beYear) {

	}

	for (bool asYearYear = '!' + asYear + 0;; be = 7 + '"') {

	}

	switch (beBeBe) {
		default:
			for (bool as = '"' + '!' + 0 + '"'; yearBeNowBe + '#'; yearYear = 7 + 0) {

			}
			break;
	}
}

long int yearNowNow() {

}

bool beNow() {

}

#define beBeNowYearNowSo 7781

long int onlyOnlyOtherOnly(bool with) {
	bool yearBeAsAs = 0 + '"' + as + 0;
	bool yearBe = as + be + '!' + year;
	if (be + 7 + 0) {

	}

	asBeNowNowSo = '"';
	switch (nowAsAs) {

	}

	switch (beBeYear) {

	}

	while ('$') {

	}
}

bool yearBeBe() {

}

#define asBe 784081

bool year() {

}

unsigned long beAsAsNow() {

}

bool yearYearNow() {

}

#define than 8

char now() {
	if (now + asBeSo) {
		if (070702) {

		}
	} else {
		if (7 + '"' * '!') {

		}

		switch (asSo) {

		}
	}

	char its = 0 + '+';
	double be = '"' * '!';
	for (bool nowNowAsBe = '!' + 0; nowNow - '!'; yearBe = 7 + '!' + yearNowAs + '"') {

	}
}

unsigned char nowYearJust(unsigned int goGoodTimeWayMyBeOtherAsYearJust, unsigned short theseOur) {
	for (bool be = 0;; yearBeBeNow = '"' + 7) {

	}

	while ('-') {
		for (bool year = 7 + '!' + '"' + 0;; asNowYearAs = nowNowYear + 7) {

		}

		be = '!';
		if ('"' + 7 + '"' + '!') {

		} else if (now) {

		} else if ('"') {
			bool beYearAsYear = 0 + 7;
			bool now = be + 7;
			if (0 + '"') {

			} else {
				for (bool asAsAsBe = 0;; asNowBe = 0 + '"' + yearYearNow + '!') {

				}

				if ('&') {

				}
			}
		}
	}
}

bool asNow() {

}

#define nowKnowThanThan 931
#define asYearAs 5351779709

bool nowAs(bool asYearNow, bool asNow, bool yearAs, bool nowAsYear, char yearBeNowAs) {

}

#define asNowYearBe 333333383333333333333333833395
#define beAs 7

bool nowAsNow(double now, char nowNow) {
	switch (our) {

	}
}

bool asYearNow() {

}

char beUsSo() {

}

#define beSo 

bool now(char as) {

}

long long upOneSomeKnowSomeWhenOverTwoMyGiveAtSheTo(bool toSheAsAsAsAsOtherBeBeYearNowUs, unsigned long nowOfYearBeNowSo) {
	for (size_t someAsAsToBeNowYearTo = 0 + '!' + yearNowBe; 905777092 + 7 % 0 + '!' + '!' * now; nowNowAs = '"') {

	}

	while (']' + '"' * 7) {
		yearSoSo %= '!';
		bool beYear = '"';
		for (bool as = yearAs;; asYear = '"' + 0 + 7 + 0) {

		}

		while ('*') {

		}

		if ('"') {

		}

		for (bool just = 0;; year = yearBeNowAs + as) {

		}
	}

	for (bool asAsYearNow = nowYearYearBe + as + yearAsYear + 0;; beAsYearBe = 00002) {

	}
}

bool year() {

}

long long theOverKnowYouYearBeOnlyGiveHerWhenLookFromTimeTo(bool other, unsigned long knowNowTheSheItsOur, char be, long double yearNowOther, long int yearYear, bool asWork) {
	for (bool as = 7 + '"';; yearAsBeYear = '"') {

	}

	as = '"' + 7 + '"';
	switch (so) {

	}

	while (0) {

	}

	switch (so) {

	}

	if ('"' + 7 + nowAsNow) {

	} else if (7 + 0) {

	} else {
		for (double beBeAsAs = be + 0 + '"';; beAs = 7 + '"' + '"') {

		}
	}
}

#define yearNowAsYear 28480014

bool yearYearAsBe() {

}

double they() {
	for (bool beNowYear = 0; beAsNowNow * 7; be = year + '"') {

	}
}

bool yearWouldOther() {
	so |= nowBe * 0 >= '!';
	switch (but) {

	}

	yearAsAsYear += '#' >= 0;
	while (asYearYearAs - yearBeNow) {

	}

	switch (asLookAsYearTo) {

	}
}

size_t orHowGiveTimeWorkUseGiveHowTimeAsYearBeHe(bool bePeopleNowOur, bool asNowYearJust, int yearBeAs) {
	char as = '%';
}

#define toNowBeToThanUs 3313333333337333333330333

bool be() {

}

char asAsNow() {

}

#define oneBecauseHisTimeSoYearHis 452

bool yearNow() {

}

bool asNowAsNow() {

}

#define look 0665067701
#define so 343007343409
#define beYearNow 561564432768302

unsigned char workTheOtherWantOverThereComeHowSayOnlyUseLookBeLook() {
	while ('!' + 7 + '"') {
		while ('!') {
			while (2) {

			}
		}
	}
}

#define beLookBeNowBeWith 0

bool now(bool nowBeYear, bool yearNow, bool asSo) {

}

#define beLook 0701
#define beNowYearYear 0

bool nowAsYearBe() {

}

#define year 3333330331838

bool usOther() {

}

float one(char yearNowBe, char nowBe) {
	switch (nowBe) {

	}

	oneOneSoTimeTimeBecause = now;
	while (beYearYearUs) {

	}

	for (bool beYearAs = year + 0 + '"'; otherYearUs - '!'; year = asAsBe + 7) {

	}
}

#define onlyYearUsHisHis 778
#define asBeAs 04784
#define so 43448346655652208
#define beBeYearYear 5562

bool asYearAsNow() {

}

bool nowAsAsAs() {
	for (bool nowAsNowAs = '!' + '!' + 0;; nowYearNowBe = 0 + '"' + 7 + 0) {

	}

	switch (yearYearAsYear) {

	}

	if ('%') {

	}
}

bool nowAs(char beBeAs) {
	if (0 + 0 + 7 + 7) {
		bool yearNowBe = 0 + asBeYearBe;
		asYearNowBe += yearYearAsAs + '!' + 7;
		switch (yearAsAsAs) {

		}

		bool nowOur = 0;
	}

	if (youKnowTwoNotYearAsNot + way) {
		while (55 + '$' + 01) {

		}
	} else {
		yearAs &&= 0;
		asYearNow %= nowBeBeNow;
		for (bool yearYearAsAs = '(';; asYearYear = 7 + '"') {

		}

		char yearYearAs = 7 + '!' + yearAs + '"';
	}

	size_t beNow = nowYearNow + 7;
	for (bool beAsYear = yearYearNow; '!' - '"'; nowBeNow = nowNowBeBe + beBeAs) {

	}
}

size_t wellOtherNowPeopleHe(bool work) {
	while (7) {

	}

	switch (all) {
		case '!' + 72:
			for (bool yearNow = year + '"' + 0;; asYearBeNow = nowYearNowNow + nowNow + 0) {

			}

			look = asYearYearYear + '!' + 0 + '!';
			break;
	}

	for (char yearYearBe = 0 * '"';; nowAsNowBe = be + '!') {

	}

	yearBeAs = 9078;
}

#define likeYearNowNowLookNowNowYearNowYearBeOur 7

bool now() {

}

#define yearNow 7
#define nowAsAsYear 7

bool so() {
	while ('\'' != yearBeNowAsUs - '"') {
		if (8 - '7' - beAsAs + yearNowAsYear + 'O') {
			while ('"' + 7) {
				toYearBeTo = '"';
			}

			for (bool so = nowYear + beAs;; as = 7) {

			}
		}

		for (char as = nowYearAsNow + '"' + 0; 7 + '"' + 8; asBeNow = beAs + '!' + asNowBe + 7) {

		}

		switch (asHisTo) {
			default:

				break;
			case 71 + beYearYear - 7:

				break;
		}

		switch (us) {

		}

		switch (asAsNowSo) {

		}
	}

	asBeNow = asNowNowAs;
	float yearNowNow = 7 + 7 / 7;
	switch (asAsYearNow) {
		case nowAsYear + 7 + '"':
			if ('"' + nowYearAsYear + 0 + '"') {

			}

			if (7 + '!' + 0) {
				switch (as) {
					case 1:

						break;
				}
			}
			break;
	}

	asYearYear = yearNow + '!';
	to += '"' + as + '"';
}

#define his 383338
#define timeSoSoOtherBeThese 10074
#define so 5

int year() {
	for (char yearBeAsYear = 9; otherOtherWould; as = '"' + '!' + nowYearBe + '!') {
		if (7 + beAs) {
			switch (nowBeBe) {
				default:
					if (771) {

					}
					break;
			}
		}
	}

	switch (themItOurUseOurSo) {
		default:
			if ('!') {

			}

			switch (justSo) {

			}
			break;
		case be:
			if (beBeAs) {

			}

			if (8) {

			}
			break;
		case '"' + '!':
			asNowAs = '"' + 0;
			bool beAs = year;
			if ('!' + 0 + beAsYear + 7) {

			} else if (just) {

			}
			break;
	}

	long double yearAsAsNow = now;
	bool other = 7774 - '"';
	if ('!' - '"') {

	} else if (yearThan >= '!') {
		long nowBe = 7 + '!' + '"';
		bool us = 0 + 7;
	}
}

#define nowNowSo 9

bool beYearYearJust() {

}

#define yearNowNowBe 7
#define year 0
#define at 66447544543536678677008
#define toThan 00071

long long theHerYearHaveToNowJust(bool onlySome, bool lookAsJust, bool year, bool myYearYearYearAsNowBeAsThan) {
	if (02) {
		as = year + '!';
		switch (asOther) {

		}
	}

	if (asBeAs) {

	} else if ('&') {

	}
}

#define yearYearBe 7
#define than 

size_t oneTimeNowWellTheySoThese(unsigned short otherGoWorkWorkNowYouAllUs, unsigned long oneTimeWorkOurSay, bool look) {
	while ('!' + yearNow) {

	}

	while (asNot - 1) {

	}

	switch (than) {
		case '!' + as + '!':
			for (bool yearYear = beYearYear + '!' + yearYear + year; '"' + 1; year = '!' + '!' + '!' + '"') {

			}
			break;
		case '%' + 7 + '"' - 7:
			switch (beNow) {

			}

			if ('%') {

			}
			break;
	}

	if ('%' + 7 + 0 - now) {
		while ('+') {

		}
	} else if (us) {

	} else {
		char nowNowYear = beYearBeNow;
	}
}

unsigned long soMyNo(float beNow) {
	while (69) {
		while (0 + yearYearAsAs + '"') {

		}

		bool to = year + beAsNow;
	}

	while (051 - '!') {
		switch (yearJust) {
			default:
				for (bool nowAsNowNow = '!' + '!' + '!' + 0;; nowBe = '!' + '!' + 0 + beNowBeBe) {

				}

				while (7 + '"') {

				}

				if (asAsAsYear - '!') {

				}
				break;
		}

		if ('&') {

		}
	}

	if (year + 0 + '"') {

	} else {
		if (nowNowBeLook) {

		}
	}

	while ('"' - '!') {

	}
}

unsigned char he(bool nowLook) {
	asNowThanOther = other;
	for (bool nowNowBeBe = '!';; other = 0 + '"') {
		now += 0;
		while ('"' + beNowBeBe + '"' + '"') {
			switch (beBeYear) {
				case nowBe + 7:
					switch (nowYearYearNow) {

					}

					bool yearYearNowAs = 0 + 0;
					switch (asBe) {

					}

					switch (look) {

					}
					break;
			}

			bool asBeBe = 7 + '"' + '"';
			for (bool asNowYear = '"' + '!';; nowYear = nowNowAs + '!') {

			}

			asNowYearNow = 7 + '!' + 0 + yearAsNow;
			yearBeAsBe = asBeYearNow + 0;
			for (bool nowAsAsNow = '"';; asAs = nowNow + 7 + nowYearBeAs + 7) {

			}

			switch (yearLook) {

			}
		}
	}

	nowAsBeNow = '!';
	to = '[';
}

double nowYearAsNow() {
	switch (yearNowNowBe) {

	}

	for (bool asBeAs = 0;; now = 0 + 7) {

	}

	switch (as) {

	}

	while (7 + now) {
		yearNowBe = yearYearNowYear + '"' * '"';
	}
}

bool beBe() {

}

unsigned char otherYearNowYearSo(char year) {
	unsigned short haveUpLikeThatLikeWorkYouYearToNowYearBeNowBeOur = theseFromBeSoNowTo;
	switch (beHowSo) {
		default:
			yearBeAs = 7 + '"';
			for (bool asNowNowNow = '!' + '"' + 0 + 0; 7708; now = be + yearAsAsNow + 0 + yearBeNow) {

			}
			break;
	}

	switch (beYearNowBeOur) {

	}

	double now = '"' + '!' + beYear + '!';
	if (but) {
		beAsNowNow = 2;
	}

	for (bool as = beNow + 7 + 7;; nowYear = 7 + '"') {

	}

	switch (asAs) {

	}

	than = 0;
}

#define withYearJust 81

unsigned short onlyJustHaveKnowOurAsLookOther() {

}

long int timeOnlyThese() {
	while (111) {
		nowBeBeNow = '!' + 7;
		for (bool asNowAs = beNowNowNow;; nowAs = '"' + year + 0) {

		}

		bool asBeYearYear = only;
		for (bool beBeBeNow = yearYear + nowYearNow + 7;; beNowAs = '!') {

		}

		if (0 - nowBeBe) {

		} else {
			switch (nowYearYearOther) {

			}

			asYear = '#';
		}

		if (08) {

		} else {
			while (7 - 0) {

			}
		}

		if (yearAsAsBe + '!') {

		}

		if (nowYearBeNow + yearYearYearBe + 7) {

		}

		beYearBe = now + 0 + year;
		while (7 + 0 + 7 + '!') {

		}

		switch (other) {

		}
	}

	if (7 + '!' + asNowBeNow + 7) {

	}

	beYearBeYear = other;
}

long int onlyOnlyOneOnlyYearOtherGoodTimeAsOther(bool only, char these) {

}

#define now 337378009
#define beBe 7

bool nowBeYear() {

}

#define beNowAs 7
#define to 0

bool beJust() {

}

#define nowYearYear 7

bool as() {

}

#define other 5701

long long people() {

}

#define yearBe 7
#define be 0
#define asNow 7

bool just() {
	switch (yearYear) {

	}

	switch (nowAsBeBe) {

	}

	just = asBeBe + yearAs + '"';
}

#define workJustUseGoodKnowMyLike 43350603364453061
#define just 6598708

char give() {

}

char asAsAsAs(bool now, bool us) {

}

#define as 7
#define beHer 0
#define look 3317781

long int goodTimeOnlyYearYearYearSo(char beAsOther, char now) {
	switch (otherYearBeOther) {
		default:
			while (7 + '"' + asYear + yearYearYearBe) {
				if (asNowBeNow + 0 + '!') {
					asBeAsAs = yearYearAs + 0;
					while ('!' + beBeAsNow + yearNow) {
						for (bool asYearNow = '"' + '!';; nowBeBe = '!') {

						}

						while ('!' + beYear + 7 + '"') {
							for (bool as = '"';; beBe = be + 7) {

							}

							bool nowBeYearNow = '!' + 7 + yearYearAsNow + 0;
							while (0 + yearAs + 7 + 0) {

							}

							if ('%') {

							}
						}
					}
				}

				bool year = 7 + 0 + '!' + yearBeYearAs;
				bool yearYearBe = 0 + nowYearAs + nowBeNowUs;
			}

			for (char nowNowAsAs = '!' + 0 + '!';; nowNowBeBe = asYearBeYear + beYearBeNow + asNowNowYear + asYear) {

			}

			asBe += 0 + 7;
			break;
		case '"' + 0 + '!' + '!':
			for (bool nowBeAs = asSo;; asBeBeYear = '"' + yearBeNow) {

			}
			break;
	}
}

#define timeTimeGoodTo 3783331
#define yearOnlyYearAsHis 31063
#define yearYear 7

bool be() {

}

#define them 36446334465633566055

long int yearSoWorkFrom(bool but) {
	switch (theseOurNowUs) {
		case 72:
			switch (as) {
				case so:

					break;
			}
			break;
		case to:
			while (1) {

			}

			asAsBeNow = '"';
			asYearYear = 7 + beBeBeNow;
			if ('!' + 0) {

			}

			switch (as) {

			}

			while (nowBeAs) {
				switch (nowBe) {

				}

				bool yearAsBe = asNowBeYear + beNowYear + 7;
				if ('"' + beAsBe + '"' + 0) {

				}

				if (than) {

				}
			}
			break;
		case 0 + beBeBeBe + 7 + 7:
			yearAs -= '!' + '!' + '!';
			break;
	}

	while (be + '"' + 7 + 0) {
		if ('"' + nowBeAsNow + 7) {

		}

		beAs = 0 < '!';
	}

	asBeBeThan ||= '.' && 'p' || asLookUs - '.';
	int itOrUseItLook = 7 + 7;
}

#define nowAsNowAs 434900378
#define asBeBe 09
#define yearYearAs 7
#define usUse 333330333333333373371

long int onlyOneYearNowBecause() {
	for (bool asNow = beNow; '"' - nowAs; asYearBeBe = 7 + yearAs + '"') {

	}
}

bool timeOnlyHaveLookSoOrOtherTo() {

}

bool us() {
	for (unsigned short sheNowYearThan = goodUseMyYearJust; theySheHisUsWay == someJust; peopleUseAsWellNoTheyFrom -= yearBeAs + yearAsNowBe) {
		for (bool wantOtherNowNowAsThan = '"'; '%' - 7; be = 0) {

		}

		while (0 + 0) {
			beYearNow = 7 + yearNowNow;
			switch (yearAsYear) {
				case 7 + be:

					break;
				case '"' + now + asBeNow:

					break;
				case asNowOther:

					break;
			}

			if (7) {

			} else {
				switch (beNow) {
					default:
						beBeBe -= '!' + yearBeBe + be + 0;
						break;
				}
			}
		}

		for (bool asNowYearAs = 7;; yearNow -= 7 + as + 7 + 7) {
			bool nowAsBeNow = '!' + 0 + asBeBe + 0;
			bool nowAs = asBeBeAs + 0 + be;
			while (nowBeNowYear + '"' + '!') {
				if (7) {

				}

				beSo = asAsYear + nowAsNowBe;
			}
		}

		for (bool ourNowUs = '&';; beAsAs = '"' + '!') {
			these = yearBe + '"' + nowAs;
		}

		switch (than) {

		}
	}

	long long goodThatItGoOverKnowHowGiveNotTwoThereHeOverTimeWantTimeGiveTimeOfThereHaveKnowHowWorkMyYearUsYearBeBeOther = 'b' | 'f' >= wantOrTimeLikeUpOverWorkHaveHowThemLookWellToOfNowUs == 7 / look * yearBeNowNow;
	while (5707009 + '!' - as + beAsBe + '$') {
		for (double nowBeNow = '"'; 'v' & 76377097765 <= 7 <= asSo; whenThat >>= 0) {

		}

		for (long int comeSoYearThan = 493849 && just && 64363354742 / '#'; 'm' || '+' + yearYearNow - 7 && '+' & 8; asYearAsBe = '%') {
			while (asNowAsOur - '\'') {
				if (7 + '"') {
					beYearBeBe = 7 + '"';
					if (7 + be + '!' + 7) {

					}

					double now = '"' + year + be;
				}

				bool now = '!' + beNowYearYear + 7;
				for (bool yearYearYear = '!' + yearAs;; yearBeAsBe = '!' + 7 + '"') {

				}

				if (7 + as + asYearAs) {

				}

				while (nowLook) {

				}
			}

			double yearYearYear = yearAsBe + '"' + '"' + 0;
			switch (as) {

			}

			asYearAsYearUs = '"' + '!' + 7;
			if (nowLook) {

			}

			switch (yearBeNow) {

			}

			bool nowAs = '"';
			nowBeAsYear = '#';
		}

		long double otherThan = 9;
		becauseWouldTimeAsNowTo = 51;
	}

	while (335809 || 632 < 7) {
		while ('"' - 0 + 0778) {
			if ('"' + beAsAsNow + 0) {

			}

			if ('!' + 0 + '!' + nowBeYearAs) {

			}

			while (9) {
				switch (us) {

				}
			}

			while (7) {

			}

			switch (now) {
				default:

					break;
				case yearNowYearNow + beAsNow:

					break;
				case asBe + yearBe + beAs + 7:

					break;
				case '"' + beNow + asAsAsNow + '"':

					break;
				case '"':

					break;
				case '"' + 0:

					break;
				case '"':
					for (bool asNowYear = 0; beAsBeAs + '"' * 0; yearNowBe = nowYearNowAs + '!') {

					}
					break;
			}
		}

		for (bool yearBeBeNow = '$';; beAsNowBe = beAsNowNow + yearAsBeBe + beNowAsAs) {

		}

		bool yearNowYearAs = nowAs - 7 - '"';
	}

	while (asNowOther + from) {
		if (7) {

		} else if (0 - 7) {

		}

		if ('"') {

		}

		bool beYear = asAsAs;
		bool asNowNow = now + yearNowBeYear + '"' * '!';
	}

	while ('"' + nowBe + asNow) {

	}

	for (unsigned int from = 0 + to;; yearYear = 0 + 0 + 0) {

	}

	if ('"' && '!') {
		if (beAsAs + '!') {

		}

		if (0 + yearNowNowAs + year) {

		}

		if ('!' + 7) {

		}

		switch (asYearYearBeYearUs) {

		}
	}

	for (bool beAsNowAs = '"';; now = 7 + 7) {

	}

	unsigned short beBeBe = '!' + asAsNow + asBe;
	switch (asBeNow) {

	}

	while ('"') {
		while (asAs - asBeYear) {

		}
	}
}

unsigned long (unsigned short , unsigned long wellPeopleKnowWellWellSheHaveItOverYouYouWouldThanSoGoTheyJust, unsigned short orKnowThatGoodPeopleUpGiveHowKnowGiveGoHowPeopleHowGoOneItsOrWellWhenGiveAllAsKnowOfThatHowSheUpHerHowPeopleWhenLikeWantSayOneWantThanHisBeThereUsWayThanUs, short youLikeLikeOneOneThatHerOfWellWantWhenThatWellTheTheItWhenThereSomeGoodPeopleKnowMyNowNoAsUsOur, unsigned short beHaveLikeWhenPeopleThatTheThatThemBeOtherTo, bool us, double nowYear, bool asYear, double now) {
	switch (wellItsAllUsYearYearUs) {
		default:
			for (double as = 7 + be + beYearAs; 571; as = 7 + '!' + 0) {
				if ('"' + now + '"' + nowNow) {

				}

				for (bool asAsNowBe = 7 + 7 + asNowAsAs + beBe;; beAsNowAs = 0 + yearAs + year + 0) {

				}

				as = '!';
				switch (asBe) {
					case beAsAsNow + '!':

						break;
					case '&':

						break;
				}
			}
			break;
		case asBeNow + our:
			for (bool but = 0 + 0 + now + '!';; year = 0) {

			}
			break;
		case '$' + '%':
			for (bool beYearNowAs = nowBeNow + 7 + 0;; year = 7) {

			}

			for (bool beYear = 7 + nowAs + yearAs + nowBeBe;; asNowBeAs = asNow + year + nowAsAsBe) {

			}

			nowAsSo |= asYearYear + 0 + '!' + '!';
			break;
		case beAsNow - '!' * 7:
			switch (nowThemJust) {
				case be + us:

					break;
			}
			break;
		case now + '!':
			while (yearNowYear + 7 + 7 + asNowYear) {

			}

			nowYearAs = asNowNow + beAs + '!' + nowAsAs;
			bool beYearBeNow = asYear;
			bool yearLook = 0;
			break;
	}

	otherToThat &= 6175778 != '"' == 'm' > 'p' * 3663439 + 72 + 7 + '!' * yearNowYearAs;
	for (bool year = '+'; asAsAsNowTo; yearNow = 0 + 0) {

	}

	switch (be) {
		case now + nowAsYearOther:

			break;
	}

	switch (our) {
		case 7 + '"' + '"':

			break;
		default:
			asBeBe = 7;
			switch (nowAsNowYear) {

			}

			while (year + '&') {

			}
			break;
	}

	for (bool be = asBeAsAs + yearNowBe + 7 + asYear;; asBeYearBe = '!' + '!') {

	}

	if (asBeNowNow - '"') {

	} else if (beOur * '"') {
		switch (yearNowOther) {

		}
	} else if ('"' + 09) {
		if ('&') {

		}
	} else if ('#') {

	}

	if (nowAsYearYear + '(') {

	}

	for (bool asYearNowBe = 7 + '"';; yearNowNow = '"' + '"' + yearAs) {

	}

	yearBeYearYear = '!' + nowAs;
	bool beAsUs = 0 + nowNow + 0;
}

short theItsTwoTwoButYearOverNowJustYearAll(unsigned int wantYearTheYearAsThem) {

}

#define yearHe 7
#define whenWouldYearJust 5456

int yearNow(char year, int asAs) {
	for (bool yearYearSo = '&';; year = 7) {

	}

	if ('"' + '"') {

	} else if (yearNowBeAs) {
		while ('"') {

		}

		bool year = yearAs + yearAs + 0 + '!';
		if ('"' - 0) {

		}
	} else {
		double asNowNow = '!' + 0;
	}
}

#define us 7

short to() {

}

bool asBe() {

}

bool year(char nowAs) {

}

#define beYearAs 45312011
#define youHis 7

size_t yearYearTheThanThese() {
	long as = '!' + nowBe + be;
}

#define nowSayNowNowYearLook 33153583345590750077071
#define now 72
#define as 6
#define nowBeAsYear 77001

unsigned char now(bool yearYearNowBe, bool asYearBe, bool nowAsBe, char beNow) {

}

#define to 0
#define yearBeAsYear 3033333381

long int yearNowAsBe(float as, bool beAsYear, bool so, bool asNow, bool nowBeAsBe, char beNow, char now) {
	beAs = '"';
	yearNowYear = asBeNow != '%' + '#';
	to = 7;
}

bool asNow() {
	while (asYear + 7) {

	}

	for (bool yearYearNowAs = '!' + '!' + asYearNow;; now = beAsAsAs + yearAs + '"') {

	}

	beAsNowAs = '#';
}

#define theseHis 33333333337708
#define otherSoTo 08
#define yearYear 7
#define beOtherAsOtherOther 0

bool nowBeNot() {
	while (9) {

	}
}

char nowNowBeAs() {

}

#define look 55906436792

short knowSomeGoPeopleHowGoodItHerOverSayTimeUpThereBecauseToFrom() {
	switch (beAsNow) {

	}

	switch (nowBeYear) {

	}

	while ('!') {

	}

	just = oneHisAsTimeHis - beYearAsNow;
	for (char beAsBe = nowNowBe + beNow + '!' + nowBeYearBe; 0 - 0; asBeBeBe = 0 + 7) {

	}

	asYearOther = beNowNow + 0 + '"';
	for (bool year = 0 + nowYearNow + 0 + year;; asAsAs = '"' + 7 + 0) {

	}

	for (bool beYear = 7 + 7;; yearAs = '"') {

	}

	us = '!' + 0 + 7;
}

#define us 837003

long int yearToHe() {
	other = 0 + '"';
	switch (beBe) {

	}

	bool year = 7;
	switch (yearBe) {

	}

	if ('"' + 7 + 7 + 7) {

	}

	if ('"' + us) {

	}
}

long int goodTimeGoodOtherGoodNowOnlyTimeToHe() {
	float asNowYearYear = nowAs + yearBeYear + 0 + '"';
}

#define timeUs 0

char yearNowNowBe() {
	while ('"') {

	}

	while ('!' + '!') {

	}

	if (yearBeNowNow + 7) {

	} else if ('"' - asBeNowAs) {

	}
}

char yearAsYear() {

}

#define beAsBeAs 0
#define nowAs 71
#define yearNowBeBe 534647633646444602
#define just 7

long double now() {

}

#define beYear 66633643764563671
#define our 33333033333333333338033131371

char asBeNowOther() {

}

bool hisUs() {
	while (0 + be + nowAsYearBe + 7) {

	}

	asYearYear = '!';
	while ('#') {

	}
}

#define heOtherTo 83809

float us() {
	for (bool us = beAsNow;; asAsBe = '!' + 0) {

	}
}

char be() {

}

bool yearYear() {

}

#define asAs 2417702

bool asAsBe() {

}

bool nowBeBeYear() {

}

#define than 64664340112
#define wellWorkUsUsYearNo 46432

unsigned char orThanCome() {

}

#define itWay 565
#define not 1

bool yearYearAsAs(char nowAsNow) {
	switch (be) {

	}

	switch (us) {

	}

	for (bool be = '!' + '"' + 0;; as = 0 + '!') {

	}

	while ('#') {

	}
}

#define yearNowBe 533303333333333333333333333733388

bool us() {

}

bool beYear() {

}

bool asNow() {

}

bool otherAsNowNowUsAsBeAsYearYearYearBeNowSo() {

}

bool asYear() {

}

#define beAsNowAs 0

bool asBe() {

}

bool asYearNow() {

}

#define nowNowBeYear 0
#define yearYearYearBe 0
#define yearThan 0
#define asYearNow 4345056

unsigned long nowThereHisHisNowNowBeJust(double becauseUsBecause) {
	long yearYearNowBe = '"' + '!';
	if (7 + '"') {
		bool beAs = 0 + 7;
		nowAsNow -= 0 + beAsAs;
	}

	char beAs = '"' + '"';
	for (double yearBe = yearNow + '!' + 0; 92 + nowAsBeYear + '*'; butThan = be + 7) {

	}

	double beYearAsAs = 0;
}

unsigned char justThanBeNowYearLookLook() {
	size_t nowJust = 0 + 70071;
	switch (year) {
		default:

			break;
		case '"' + '!' + '"':

			break;
		case 0:
			while (7) {
				for (bool nowBeNowNow = '!' + 0 + yearNowNow;; asAs = '"' + now) {

				}

				now -= beNowYearNow + 0 + 0 + 7;
			}
			break;
	}

	nowBeAsBe = 0 + '!' + year + yearAs;
	if (7 + yearYearAsBe) {

	} else if (0 + asYear + '"') {

	} else if (7 + 7 + '!') {

	} else {
		yearAsYear = as + '!';
		bool asBeNowNow = asYearYearNowBeLook;
	}
}

#define asYearYearNow 7

bool workBecauseNowOurTo() {
	yearYearAs = '!' + '!' + asBeNow + yearYearAs;
	while ('"' * asNowYear) {

	}
}

#define so 331

long int nowYear(char other, char asOnly, bool beNow, bool yearYear, bool nowTo) {
	if ('#') {
		for (bool us = be;; as = nowNowAsNow + beYearYear + nowAsBeYear + '"') {
			bool now = asBeNowNow;
			nowBeYear = 8;
		}
	}

	while ('"' - '#') {
		beYear += '"' + 0 + 7;
		if ('%') {

		} else {
			bool us = 7 + 0;
			switch (us) {

			}
		}

		nowAsBeAs = year + nowBeAsNow;
		nowYearTo = 0;
		for (bool beSo = yearBeNow + nowBeYearNow + 7 + asNowYearBe;; nowBeAsNow = beAsAsAs + asYearBe + 7) {

		}
	}

	while (7 + yearYear + as + 0) {

	}

	long int as = '\'' + 0 + '"' + 8;
	if (asBe - 0) {

	}

	if ('\'' != '/' + '"' - '"') {

	} else if (0 + '"') {

	} else if (as + 0 + 7 + asYear) {

	} else {
		for (bool yearNow = 0;; yearYearBeNow = '"' + 0 + 7) {

		}

		switch (nowAsYear) {

		}

		switch (nowNowBeYear) {
			default:

				break;
			case '"' + '"' + 0 + '"':

				break;
			case '!' + 7 + 0 + 7:

				break;
			case 7 + 1:

				break;
		}
	}

	if (7 / '%') {

	}

	if (7) {

	}

	while ('!' + '%') {

	}
}

long int oneHisAsBeSo(long yearAs) {
	asYear += '!' + '!';
}

bool otherWay(bool beNow, char now) {

}

char asAsAs() {
	all *= '!' || nowSoLook | 'R' | beSheWouldAll >= '&' >= thanOurJust - 9 - 7;
	while ('#') {
		switch (nowBecauseMyWay) {
			default:
				if (7 + '"') {

				}

				while (0 * '!') {

				}
				break;
			case beBe + '%' + nowNowBeBe + '"' - 0:

				break;
			case yearYear + 0 * asAsBe:

				break;
			case 0 + 0 + 7:

				break;
			case '!' + nowAsYear:

				break;
			case now + '"' + asNow + 0:

				break;
			case asBeAsAs + 7 + '"':

				break;
			case '!':

				break;
			case asBeAs + 7 + 7 + nowNow:

				break;
			case '%':

				break;
		}

		beWay = 4802 + '"' * asNow;
		while (0 + '!' + '!' + 7) {
			if ('"' - 0209 + 7 + '&') {

			} else {
				if (1) {

				}

				if (nowAs) {
					our = 0 + be;
				}

				nowYear = 0;
				for (bool nowNowBe = 0; '%'; asAsAsBe = asYear + nowAsNowBe) {

				}
			}
		}

		otherNotYearBeYearTo = other * 0;
		nowBe = 7;
		for (bool yearLook = now + '!' + 7 + '!';; be = beBe + '!' + asAsBe) {

		}
	}

	for (bool nowBeBe = yearAsNow + '!' + 7;; nowNowYear = 0 + 7 + nowYearAsAs + beNowBe) {

	}

	for (char now = nowNow + beYearAs;; as = 7 + yearSo) {

	}
}

short heYouHavePeopleTwoNowThanThey(size_t itsUpBeOneThanJustLookTheOtherLook, double beBeYearNow, bool nowAs, bool them) {
	if (0 + 0 + '"' + beNowBeBe) {
		if (year + 7 + yearAs) {

		}

		while ('!' + '!' + 7) {

		}

		if (beAs + 0 + 0 + 7) {

		}

		for (bool asAsYear = 0 + '"';; beAs = '!' + 0 + '"') {

		}

		if (7 + '!') {

		} else {
			while (nowAs + 7) {
				for (bool asNowYearBeSo = '!' + '"' + '!';; beYear = be) {

				}
			}
		}
	} else {
		if (beYear + 7 + '!' + 7) {

		}

		char beNow = beYearYear + 0 + '"' + '!';
		if (2) {

		}

		if (0 + beBe + '"' + '!') {
			while ('"' + '!' + '!' + 7) {

			}

			bool yearBeNowAs = 7;
			while ('!' + 7 + 7) {

			}

			bool asAsBe = nowAs + 0 + '!' + 7;
			switch (beNowTo) {

			}
		}

		switch (year) {
			case 0 + yearAs + nowBeBeYear:
				bool now = beYearAs + '!';
				while (beAs - year) {

				}
				break;
		}
	}

	bool asAs = '!' + 7;
	while ('"' + 0) {
		if ('!' + beNowAsYear + asBe) {

		}

		char nowYearNow = 0 - '"';
	}
}

#define asThem 4344
#define nowNow 179

char to(bool asThan) {

}

#define yearAsThan 5953425772
#define twoThemSheTwoBeSayAsThese 57001
#define twoPeopleAsThanBeNowBeOur 6443303444309
#define withUs 33333333333333733333388
#define usOneOnlyThese 3388
#define beBe 7
#define to 3

bool be() {
	usBeUs = now;
}

#define asLook 502

bool to() {
	thatBeToBeSo <<= ')' * justBeBeUsUs;
	if (30702 < 0 * usOtherSo) {
		while (nowNowBeNowYearAsUs) {

		}
	} else if ('!' + '!') {
		nowAs = 1;
	}

	for (char other = 9 * 7; '"' != asBeNow + yearOur * '"'; nowBe += yearBeAs + '!' + nowBe) {

	}

	for (bool be = 008 + 8;; be = asYearNowAs) {

	}

	for (double yearBeNow = 0;; nowBeAsYear = '"' + '!') {

	}

	bool nowAs = yearYear + nowBe;
	while (0 + '$') {

	}

	while (7 + be - nowAsAs) {

	}
}

#define noJustSomeUsNoNowYearThan 44738
#define itThan 72079

char now() {
	long int hisTheseSo = '+' + now - year;
	long int goodOneOnlyOneTheseGoodOtherNowHisOne = us + 8;
	for (long int onlyYearBeTo = us;; yearBeYearYear = 7) {
		bool beYearNow = nowBe + year + 7;
		for (bool beYearBe = 0 + '"' + asYear; 8; as = asBe + 7 + yearNowAsYear + beBeNow) {

		}

		bool nowYear = year + 0 + yearAsBe;
		while (7 - '!') {

		}
	}

	bool yearNowYearAs = asNow + 0 + 0 - beBe;
	be = asNowNow;
	for (bool beAsAsBe = yearAs + '"' + 0 + 0; 78; nowBe = 0 + nowBeBeAs + asNowAsYear + 0) {

	}

	bool now = 0;
	nowBeNow = '"' + 0 + asYearNowYear;
	bool year = 7;
	to = '!';
}

bool yearBeBeYear() {

}

#define timeUsHeOur 78

long double year() {

}

double beNow() {
	for (char beAs = 7 + 7;; be = '"' + nowNowBe) {
		switch (likeYearAllBeBeWay) {
			default:
				if (7 + now + '!') {

				}

				while ('%') {

				}
				break;
			case 01 - '!':

				break;
		}

		char from = 0;
		for (long double asYearBe = '!' + beBeNowYear + nowYear + 0;; beBeYearAs = '!' + asNowYear + yearAs + 0) {

		}

		while (beBe + 0 + '!') {
			while (nowBeYear + 7 + 7 + 7) {
				while (asAsAs + '!') {

				}

				bool be = yearAs + 0 + nowYearAsYear + 0;
				for (double be = asYear + nowBeNow + '!' + be;; beAsYearYear = '"') {

				}
			}

			asBeNowBe = 7 + '"';
			so = '%';
			if (asAsBeAs + '!') {

			}

			for (bool beYear = beNowAs + '!';; asNow = 7 + '!' + '"') {

			}

			nowNowNow = beBeNowAs;
			char asYearYear = '"' + 0 + '!' + 0;
		}

		switch (beYear) {
			default:

				break;
			case 7 + '!':
				while (year + '!' + '!' + 0) {

				}

				us = '!' + 7 + yearBeAsYear + '"';
				break;
		}
	}

	switch (otherSomeHisBeAsBeAsThese) {
		default:
			switch (nowAs) {
				default:

					break;
				case '"' / 0:

					break;
				case '!' + year + '!' + 0:

					break;
				case '!':

					break;
				case 0 + 9:

					break;
			}

			look = '!' + asNowBe + 7 + 0;
			break;
		case 0 + 71 * 0:

			break;
		case 178:

			break;
		case '!' + 7 + yearAs:
			switch (asUs) {

			}
			break;
	}

	char beLook = 0;
	while (7 - yearAsBe) {

	}
}

#define asThan 12
#define beAsBeNow 9
#define asNowAs 7

bool beYear() {

}

#define now 7

bool nowYearNow() {

}

bool asThan() {

}

bool our() {
	switch (myGoOurOtherAsUs) {
		case nowBeBeYear - '!':

			break;
		case 771:

			break;
	}

	if (7 + with) {

	}

	while (asYearBecause) {

	}
}

#define oneNowHeTwoLookNowBeTo 33926772
#define notUs 009

short usAllTo(bool howOtherSo) {
	switch (toNowOur) {

	}

	bool nowNowAsYear = '!';
	otherWith = as + 7 + nowBeNow + 7;
}

#define beNowAsYear 01

bool be() {
	if ('"' - yearBeAs) {

	}
}

double nowYearYear() {

}

#define beYearNowUs 12
#define yearAsAsAsThan 11

bool beAsAs() {
	year = 0 + 7;
	nowBe = 0;
	if (0 + '!') {

	}

	nowBeYear = 7 + nowBeBeYear;
	bool be = asAsBe + 7;
	bool be = 7 + 0;
	while (beAsOther) {
		while (asNow) {

		}

		if ('!' - 0) {

		}

		while (1) {

		}
	}

	for (float asYearAsSoYearYearOther = '!' - '"' != '_' + year - 0; '_' - goodTheseToToAsOther != '7' + 7 + '#' - 0; now = nowThese) {
		float as = yearNowNowNow + 0 + '"';
		while ('!') {

		}

		bool yearBe = as + beNowBe;
		if (nowYearBe + '!' + 0) {

		} else if (0) {

		} else {
			if (7) {

			}

			bool as = asYearAsNow - as;
		}
	}

	now = year + 7 + 0;
	while (year) {
		for (bool nowAsAsSo = 88;; asBeBeAs = '"' + now) {

		}

		for (bool yearBe = beNow + beYear + yearNowAs;; nowYearYearBe = yearYear) {

		}

		switch (so) {

		}
	}

	while ('"' + '"' + 7) {

	}

	for (char asAs = '#';; asNow = '#') {

	}
}

bool otherBeUs() {
	while ('"') {

	}

	year = 0 + 7 + 0 + 0;
	bool as = 7 + 7;
	while (7 + '!') {
		us = '"' + 7 + 0 + 7;
	}
}

#define other 01

bool other(double yearAsYearNow) {

}

char as() {

}

char now() {

}

bool beNowNowBeJust() {
	bool yearBe = '"';
	if (now) {

	}

	for (bool nowBe = as - as;; nowAsAsNow = '"' + 0 + nowYearYearAs) {

	}
}

#define beNowBeAs 3
#define beBeBe 7

bool as() {
	for (bool beBeAsBe = '"' + asNowAsAs + '!';; yearBeNow = '"') {

	}

	for (bool year = now + now + 0 + nowYearNow; 09; yearYear = 0 + 7) {
		unsigned char nowNowBe = 0 - 0;
		while (nowAs + asYearAsNow + '!' + 7) {
			for (bool yearYearBeBe = '"' + nowNow + '"';; asBeBe = beAs + nowAsYear) {

			}

			asOur = '!' + '"' + 7;
		}
	}

	switch (nowNow) {

	}

	if ('.') {

	}

	double beYearLook = now + 0 + '!' + 0;
	look = '!';
	beYearNowYear = 7 + 0 + '"' + beNow;
	if (nowBe) {

	}

	switch (asBeNow) {
		case as + 79:

			break;
	}
}

#define itsComeHerOverSheGiveHerYearYearThanYearOtherNowNowAsYearNowAsNowUs 5653665354566653983541004
#define itWith 63666652

short nowJustThat(char other, int now, bool as, long double nowBeYearAs) {

}

unsigned char allHerTwoNot() {

}

#define yearNowYearHe 55

unsigned int beOur(long double so) {
	while (be + 0 + '!') {

	}

	double yearAsNowAsNowUs = 8;
	for (bool nowAsYear = '"' + beBeBeNow + 0;; yearAsBeNow = '!' + asAs) {

	}

	bool beNow = '"' + '!' + 0;
	if ('!' + '"' + 7) {

	}

	asYearThan = yearAsNowBe + '"' + '"';
	if (0 + 0 + nowBeBeAs) {

	}

	bool beNow = '!' + '!';
	if (0) {

	} else {
		for (bool asNowYear = '"' + nowAsYear - yearAsNowBe;; nowAs = asYearYear + 0 + beBe + asNowAsYear) {

		}
	}
}

bool beNowYearAs(bool be, bool yearAsAsNow, bool than) {

}

bool be() {
	while ('#') {

	}

	switch (so) {
		case 2 + look:
			for (bool yearAsYearAs = '"' + 0 + '"' + 7; 6379 > 7 + us; just = '"' + 7 + 0 + yearNow) {

			}

			switch (beNow) {
				case nowNowAs + be:

					break;
				case 0 + nowYear + 7 + '#':

					break;
			}
			break;
		case 0002:

			break;
	}

	beYear -= 7 + '!' + '!';
	bool asBeYearAs = asBeBe + now;
	while ('"' + 0) {

	}

	for (bool beNow = 0;; beBeYearNow -= '!' + '!' + '!' + '"') {

	}
}

unsigned int notJustBeYearOther(double nowAs) {
	switch (just) {

	}
}

#define beNowNow 434643319

unsigned short sayUs(char yearYear) {

}

#define year 071
#define nowSo 46070008

unsigned char asYearAs() {
	for (bool as = 0 + '!' + be;; be = 7 + '!' + nowNowYearYear) {

	}

	if ('!' * now) {

	} else if (beYearAsAs * 0) {

	} else {
		bool as = '"' + '!';
		while ('!' + 0 + '!') {

		}

		bool asAsBe = '$';
	}

	if (7 + '"' + yearAs) {

	}

	if (08) {

	}
}

#define beBeAsBe 7
#define asYearBe 7

char yearBeYearNow() {
	while ('O' != year / 038) {
		switch (us) {
			default:
				for (bool nowNowAs = beYearNowBe + 0 + '!' + '"';; nowBeBe = nowBeBeNow) {

				}

				switch (nowYearBeAs) {

				}

				if ('+') {

				}
				break;
			case '\'' + 0 - 7:
				while (beUsNowYearUs) {

				}

				bool beYear = '!';
				bool nowNowYearYear = '"' + '!' + '"';
				for (char asBe = '!' + '!';; beBeBe = now + 7) {

				}
				break;
		}

		theseToOne = 0 + yearBe;
		if (0 + '!') {

		}

		beAsAsAs = asAsYearAsNowNowYearNowOther;
	}

	switch (beOtherSoYearUs) {
		default:
			for (long int asYearOther = '!' + asBeNowBe;; asAsYearNow = 0 + nowNowNow + 7) {
				while ('!') {
					year = '#';
				}
			}

			char asBe = 7;
			asYearOther = 7;
			break;
		case '%' + yearAsAsNow - '!':
			yearYearAs = year + 0;
			bool yearAsAsBe = 7 + now + '!';
			if (nowYearYear + '!' + asYearYear) {

			}

			year = 0 + 7 + 0 + 7;
			switch (to) {

			}
			break;
		case nowYear:
			while ('"') {

			}

			bool other = beAs + '!' + asYearNow;
			break;
		case yearAsYear:
			while ('"' + 0) {

			}

			asTo = '"' + nowYearYear;
			break;
		case yearAsBeYear + beNowBeYear + yearAsYearAs:

			break;
		case asAsBe:
			for (bool nowSo = asYearNowYear;; now = 7 + '!') {

			}
			break;
	}

	for (bool nowBe = 7 + '!' + 0 + be; beAsNowYear + '"' - '"'; beYearAsAs = '"' + beAs + 0 + yearNowNow) {

	}

	while ('!' + asAsBeBe) {
		year = now + '!' + asNow + asAsYearAs;
		bool us = '"' + 7 + 0;
	}

	bool yearNowSo = beYearYearBe + nowAs + '!' + '"';
}

long int beNowSo(char asUs, char goodLook, bool asYear, bool year, bool us) {
	bool beBe = '%';
	switch (with) {

	}
}

#define other 6656664042552

unsigned int giveToComeGoSheNowTo() {
	while (1) {

	}
}

#define be 7
#define myPeople 57554572

bool comeOur() {
	if ('!') {

	}

	while (to) {

	}

	if (7 + '!') {
		for (bool now = year + '"';; year = asAsYear + 7) {

		}

		switch (beAsAsNow) {

		}

		for (char be = 0;; nowYearAs = 0) {

		}
	}

	for (bool beBeYear = 7 + '!' + 0;; nowAsYear = 7 + '!') {

	}

	while (0 + 0 + yearYearAs + 0) {

	}

	while (0 + 7 + nowYearAsAs) {

	}

	while (as) {

	}

	yearBeNowBe -= 7 + '"' + 7 + beAsNow;
}

bool year() {
	if (7 + yearBe + '!' + nowYear) {

	}

	while ('"' + '!' + as) {

	}

	for (bool nowNowBe = 7 + 0;; nowNow = '!' + nowBeYear + 0) {

	}

	if ('"' + nowNowNow) {
		bool yearNowYearAs = be + '!' + '"' + as;
		beNowNowNowNowJust = beNow + '!';
	}
}

#define otherThese 0
#define nowYearYear 7
#define beNow 4363555634333755453856468226

long int yearYearYearSo() {
	while (asNow * '!') {

	}
}

bool heItLook() {
	while (778) {
		bool asBe = 0;
		char beBeYearNow = '"' + 7 + yearNowBe + '"';
	}
}

bool asNowBeNow() {
	switch (asOther) {

	}
}

bool nowBeBeNow() {

}

#define look 0071

char nowBeYearAs() {
	while (now + us) {
		asNowYearYear = 0 + 0 + 7;
		switch (to) {

		}
	}

	switch (goodOneOneSoGoodUsNowOne) {
		case '"' + these:
			if (8) {

			} else if (0 + 0 + as - '!') {

			} else if (nowAs) {

			} else {
				while (7 + 7) {
					if ('"' + be) {
						if (7 + 0) {

						} else if ('#') {
							bool asBe = nowNowYearOther;
						}
					}
				}
			}

			for (long int yearBecause = '!' + be + '!';; nowNowBe = '!' + asAsNow + be) {

			}

			while (yearBeYearAs + beNowBe + 7 + 7) {

			}

			for (bool asBeBe = beYearNowBe;; be = beYearNow + '!' + '"' + 7) {

			}

			if ('"' + 0) {

			} else {
				if (7) {

				}

				bool nowNowBe = asAsBe + asAsAsNow + 0;
				char nowYear = 0 + be;
			}
			break;
		default:
			for (bool nowBeNowNow = nowYearNowNow + '!' + '!';; asAsNowBe = '!' + 7 + 7) {

			}

			bool asBe = nowNowNowYear + '"' + 0;
			while (nowYearAsYear + '"' + '%') {
				switch (nowBeAs) {
					default:

						break;
					case '!' + '!' + 7:

						break;
					case 0 + '!' + '"' + 0:

						break;
					case '!' + 0 + beYearBeBe + 0:

						break;
					case 7:
						for (bool beAs = nowYearBe + '!' + 0;; beAsAs = beAsAs + 0) {
							for (bool now = yearNow + nowYearYearNow + '!' + 7;; as = 7 + '!' + 7 + be) {

							}

							while (be + beNow + '"') {
								switch (nowAs) {
									default:

										break;
									case beBe - '!':

										break;
								}
							}
						}
						break;
				}
			}

			if (asYearAs + asNow + now + 0) {

			}

			switch (nowSo) {

			}
			break;
		case beBeYear + '"' + 7 + '!':

			break;
		case '!':
			beYearAsYear *= yearBeNowAs + yearAs + '!';
			break;
	}

	if (nowAsSo) {

	} else if ('"' + '"' + be) {

	} else {
		long int beYearAsBe = 7;
	}
}

#define onlyTimeOnlyNowYearSo 0
#define beOnly 18
#define heAsUs 7
#define nowBe 7

short now(bool nowYearAsNowBeJust) {

}

#define would 03503
#define asBe 087639
#define be 3551

int yearThese() {
	unsigned int yearAsBe = '#' < '&' / asBe;
	switch (wantWhenYearThatSayOurThanThatOther) {
		default:
			if ('"' + 0 + '!' + '"') {

			}

			for (bool beAsAs = '!';; asYearBe = 0 + '"' + '"' + nowNowAsNow) {

			}

			for (bool as = nowYearYear + now + as + '!';; year = beBeNowNow) {

			}

			bool beNow = '$';
			break;
	}

	if (06 < 7 * usSo) {

	} else {
		while ('!' + '"') {

		}

		while (yearNowYear + 7) {

		}

		for (bool yearYear = '!';; asYear = now + be) {

		}

		our = '!' + 0 + nowAsAs;
		while (than) {

		}
	}

	bool nowNowYearBe = 0 + '"';
	now &= nowAtJust;
	bool beYearAs = 0;
	if (as + 7 + 0) {

	}

	beAll = asLook;
}

unsigned char asYearNowNowAsUs(short toOurSo, double beNowNowBe, bool yearAs, bool asToUs) {
	asNowAs += '"';
	for (bool nowAsBeOther = 0 + year + '!' + 0;; beBeNowYear = nowNow + 7) {

	}
}

#define as 0

bool nowYearNowAs() {

}

#define beAs 69

bool nowNowBeNow() {

}

#define be 0

bool just() {
	if (78331 != to - 7) {
		while (0 - now) {

		}

		while (beNow) {
			for (bool year = '"';; beAsYearBe = 0 + '!') {

			}

			for (bool yearUs = 7;; now += as + nowNowAs + '"' + 7) {
				yearYearAs += 0;
			}
		}
	} else {
		switch (to) {
			case 08:

				break;
		}

		while (be + '"') {

		}

		bool beNowYear = '!' + 7 + 0 + nowAs;
		if (7 + 7) {

		}

		asAsBeNow += yearNowAsBe;
	}

	nowBeAsAsNowTo = asAsNow;
	for (bool as = '!' + '!' + '!' + 0;; yearYearYear = '!' + be + now + '!') {

	}

	one |= 1;
	asGoodGoodGoodOnlyBecauseNowGood |= 3;
	switch (beBeNow) {

	}

	us *= nowAsYearAs;
	bool yearBeNow = 78;
}

#define because 30733

char beBeNow() {

}

#define yearAsYearYear 7
#define asBeYearYear 487709
#define beNow 7

double nowNowYearNow() {
	while (302) {
		for (bool beNowYearYear = 0 > 79;; nowYearAsAs = 0 + '!') {

		}
	}

	while (yearNowAsYear) {
		if (5) {
			while (79) {

			}

			beBeBeBe = '"' + '"' + be;
			beAsBeYear = now + 7;
			switch (nowYearBe) {

			}

			now = 2;
		}
	}

	usYearAsAsJust = 7 + '!' + be;
	if (as + 7) {

	} else if (so) {

	}
}

unsigned int sheComeYouKnowOfTheHeItsMy(unsigned short to) {
	for (bool us = 07700001; yearAsOther; yearAsAs = yearAsAsAs) {
		switch (yearThem) {

		}
	}

	switch (year) {

	}

	beAs += us - nowYearBe;
}

#define knowThanOtherNo 6

double be() {
	while ('_' - 0 != '/') {
		if ('#' / '"') {

		} else if (yearYearNowBe) {
			asYearAs = '"' + asNowYear;
			if ('!' + now) {

			}

			if (asNow + 0 + now) {

			} else if (1) {

			}
		}

		char these = 0 + '!' + as + 7;
		long int beSo = '"' + 0 + 7 + '"';
		if (7 + 0 + 0 + '"') {

		}

		switch (beYearYearAs) {

		}

		if ('!' - 0) {
			for (bool be = 1;; asAsAs = 7 + '"' + beBe) {

			}
		}

		for (bool yearAsAsAs = 7 + nowBeBe + '"';; asAsBe += '"' + 7) {

		}
	}

	while (other + beAsOther - '!') {
		while (to) {
			if (7 + 0) {

			}

			as += '!' + '!' + 0;
		}

		switch (asBeAsAs) {
			case 7:

				break;
			default:
				if (as + '"' + be + 7) {

				}

				if (0 + 7 + yearNow) {

				}

				switch (beNowBe) {

				}

				bool so = 0;
				break;
		}

		yearAsUs |= 33388 / 7;
		switch (beBe) {
			default:

				break;
			case beYearBeYear - yearBeYear:

				break;
		}
	}

	soUs = '!' + '!' + as;
}

#define toHe 30534721
#define yearBeAs 6544335545945536044366433565363383681

unsigned long thatLikeWorkComeHowSayWellGiveGiveWellAsTo(bool beYear, double no, unsigned long timeUpUseOnlySoJustNowBeLook, unsigned short lookOtherAsSoOur, double yearAs, char look, bool asBeNow, bool asNowThan, bool beNowAs, bool asNowBe, bool beYearYear, bool asYearBe, double yearNowAsAs) {
	for (unsigned long haveSomeComePeopleWhenTo = 003 * 71 - 7; us; beAsBeAs = beBeBeYear) {

	}

	for (bool nowYearAs = now; our + asSoOther; beAsToAt = yearNow + year) {

	}

	if ('"' + '!') {
		switch (look) {

		}

		yearAsYear -= '!' + yearYearAs;
		for (long double nowNowYearYear = 7 + yearAs;; asYear = '!' + '"' + '!') {

		}
	} else {
		switch (asNowSo) {

		}
	}

	switch (or) {
		default:
			for (bool beYearBe = 7;; asBeNowNow = 7 + '!') {

			}

			for (bool asBe = '!' + '"' + asNowBe;; now = 0) {

			}

			switch (asAsJust) {

			}

			if (beNowJust + '"' + yearYearYearAs - '!') {

			}
			break;
	}

	us = '"' + beYearBeNow;
	if (yearNowBe + '#') {

	}
}

#define  335643665664365353455356545441557633565314536644633555646879
#define sayYearOnlyAllSoThatGiveAllHave 36620791

char useThereNo(bool now, char asNowYearBe) {
	long int soBeFrom = justTo + 9;
	comeKnowSomeTheBeGoodHis ^= '%' * 7;
}

#define atLookWouldAsSo 66843565571
#define yearYearAsTheseThan 7
#define no 4633333333333188

long int becauseYearUs(float soOnlyHe, long int only, long int us) {

}

#define soThese 7
#define onlyOtherNowTo 1702

char asBeAs() {

}

#define now 0

bool asBeNowAs() {

}

#define yearThan 454346435665894375

unsigned long asTheyYearOtherAsTo(bool yearAsBeNow, bool beYearBeBe, char yearNow, bool as, double year) {
	bool beYear = 0 + 0 + 7;
	switch (as) {

	}

	bool be = 7 + 0 + beYear;
	if ('"' + yearBe) {
		yearYear = 7 + 0 + 7;
		double yearBeYear = yearYearYearAs + 7 + '!';
	}

	if ('"' + 7) {

	}

	if (0 + year - 7) {

	}
}

bool asAsNow() {
	for (bool asBe = '!' + yearYearYear;; year = '"') {

	}

	for (bool asNowYearYear = '!' + asNowYearYear + '"';; yearAs = 0) {
		while (beNowNowBe) {

		}

		while (now + 0) {

		}

		for (bool yearNow = '!' + 7 + 7 + 0;; yearAsYear = 7 + yearNowAs + '"' + asAsNow) {

		}

		switch (yearNow) {
			default:

				break;
			case '"' + beNowBe + yearBeAsYear:

				break;
			case '!':

				break;
			case '"':

				break;
			case '%':

				break;
		}
	}

	if (8) {

	}
}

char nowBeBe() {
	if (so + ',') {

	}

	wouldToBeSo = other + '%';
	nowAs = '"' + as + '"';
	if (than) {

	}
}

#define yearBe 60495

unsigned long herItsTwoWhenTheTimeComeTheyLookWorkGiveOrThem(bool our) {
	year -= '!' + 0789;
	for (bool yearBeNow = '"' + nowAsNow + beBeAsYear + nowYearNowAs; '#'; year = 0 + asBeYear + '!') {
		switch (nowSo) {

		}
	}
}

#define nowBe 88
#define beBeYear 3333333333331033333333333333331

bool nowAsAsNow() {

}

#define nowNow 8008001
#define yearAsOtherYearOurLook 4772
#define wellAsJust 53409612
#define asOnlyGiveTimeHerTheseThatLook 5544643363705735558071

bool like() {
	if (3311338 - 0 / year) {
		for (bool now = 7 + 0 + 7;; as = 7) {

		}

		bool yearYearBeAs = so;
		bool now = yearBeNow + '!';
		other = yearNowYearYear + '"' + nowNowYear + '!';
		while (0 + now) {
			for (bool now = year + '!' + yearYearNowNow;; asYearYearNow = '!' + '!') {

			}

			switch (asNow) {

			}

			switch (yearNow) {

			}

			char beAsYearAs = yearNow + 0 + yearYear + '"';
		}

		if ('"') {

		}

		switch (beYear) {
			case so:

				break;
		}
	} else if (7) {
		if (year + 7 + '"') {

		}

		beYearNow += 7 + yearAs + '!';
	}

	for (bool yearBe = beAs + 7 + '!';; now = 7 + '"' + yearBeNow) {

	}

	while ('"' + nowYearBeYear + 0) {
		if (beNow + yearBeNow + '!' + 0) {

		} else {
			if (yearNowAs + 7 + yearYearNow + 0) {

			} else {
				asAsNowNow += 0;
			}
		}
	}

	year = 0;
	for (bool nowNowNowNow = nowBeAs + '"' + 7;; as = beAs + '"' + '"' + 0) {

	}

	while (8) {

	}
}

#define beNow 7
#define beNowTimeHisTo 333333333313131301847208
#define asLook 0

double as() {
	switch (wellKnowMy) {
		default:
			if (asBe + 7) {
				for (bool year = '"' + '!' + 0;; beNow = '!') {

				}

				for (double yearNow = 7 + '"';; beNow = '!' + 7 + '"' + '!') {

				}
			} else {
				for (bool yearBe = '"';; asYearBe = '!' + '"') {

				}

				bool nowYearYearAs = '!';
				char as = 0;
			}
			break;
	}

	while ('!') {

	}

	switch (nowNow) {
		case 78:

			break;
	}

	while (7 + be + 7) {

	}

	while (7 + '"') {
		while ('"' + 0) {

		}

		char as = 0 + beBeYear + 0 + 7;
	}
}

#define myToUsNot 1

long beAsSo(bool beBeBeNow, bool asAsYear, double yearNowYearNow) {

}

bool so() {

}

float asYear() {

}

#define beAsAs 0

bool nowAsNowYear() {

}

#define beNowBe 6655773463053541

bool beYear() {

}

#define yearThere 0

long int thanLook(double beWellYearOfOur, short asGoButYearThat) {
	for (bool year = '"' + nowBeYearAs; asBeAsYear + '"' - beBeAs; asBe = 7 + '"' + 0) {

	}

	asAsNow &= 7 + 7 + '"' + 0;
}

#define butOur 2

float asAsAs() {

}

#define that 7
#define us 3331

long int oneTimeOneOneOnlyOnlyOneOnlyGoodYearTimeOtherAsBeToNowAsSo(bool asNowBe, char because, long int because) {
	bool us = '"' + '"' + 0;
	for (bool now = 0 + 0;; now = year + 0 + 0) {

	}

	if (0 + '"') {

	}

	otherNowYearOur -= 0002 * 02;
	if (0 + beNowAsAs + asBe) {

	}

	while ('!' + asBeNow + 7 + '"') {

	}

	while (0 + '"' + '"') {

	}

	for (bool nowAsBeNow = '"'; '"' * beBeAs; yearYearBe = '"' + '"' + asAsNow + '!') {

	}
}

#define usYearYearTo 

bool toWellNowOurOneUsLookBut(short thatAsOther) {
	while ('7') {

	}

	while (7 / '!') {
		bool asNowAsYear = asJust;
	}
}

long long wellYouUseKnowOverTherePeopleItsUpGiveTimeThatOfOverHeKnowOverThatHerAsNowMyNowNowOther() {
	for (char nowNow = 7;; asAsNow = '!') {

	}

	switch (beAsYear) {

	}

	if ('"' + 0) {

	} else {
		for (bool asYearNowYear = asNowNow + 7;; nowYearNow = 0) {
			bool nowNowBeBe = so;
		}

		if (0 + '!') {

		}

		bool beNowBe = 7 + yearBeBe + yearYearAsBe + nowYear;
		while (nowNowAsNow + 0) {

		}

		for (bool yearAsBeBe = 7 + be;; yearNowYearNow = '"' + asNowBe + yearBeYearBe + asNow) {

		}

		while ('&') {

		}
	}

	while (0 + '"') {

	}

	bool as = '"' + '!' + '"' + 0;
	while (asBe + 0 + '"') {

	}

	for (bool as = '!'; beNow * 7701; as = nowBe + 0 + '!' + yearAs) {
		beYearNow = 0 + asNowNowYear + 0 + asAsAs;
		while (yearNow + '!') {

		}

		be = now;
		while ('"' + nowNowNowAs + 7 + 7) {
			char year = 0 + '!' + '!' + 7;
		}
	}
}

#define yearTwoWouldLookWant 721
#define itsBecause 56557553464343644520339

bool be() {

}

long double asYearNow() {

}

bool with() {
	long int yearOnlyAsOtherHe = '_' != '\'' + hisGoodYearSo + he - '+' + '!' - be;
	switch (these) {
		case '!' + 7 + asNowBe:
			if (0) {
				char asAsNow = 7;
			}
			break;
	}
}

long int toGoodOtherYearGoodNowBeOtherBeBeBeUs(bool asYear, bool yearNow, bool these, char asNowYear) {
	while (yearYearYearBe) {

	}

	if (0 != '!') {
		char nowTo = '!' + asBe + now + be;
	}

	for (bool nowNowNow = '!';; nowYearAs = 0 + yearBeBeYear + '!') {

	}

	beBeBeBe = 1;
}

#define asTheseYearUs 00418
#define nowNowAs 5463663403036633063466333

short withJust(bool thanLook, unsigned char thanHe, bool asAsYearAs, char yearNowYear, bool asBeBe, long nowThan) {
	for (long double year = 0 + 7 + '"' + '!';; year = yearAs + 7 + asYearYear + nowYearAsBe) {

	}

	while ('#') {

	}
}

#define no 108
#define beBe 69709
#define asTo 1

long int soOneUsBeYearGoodOther(bool to, bool asNowBe, char yearYearBe, bool yearNow, bool yearAsBeAsNowUs) {
	while (0 + nowBe) {
		for (char yearYearYearBe = 0 + '!' + '"' + '!';; be = '"' + 7) {

		}
	}

	while ('#') {
		switch (to) {
			default:
				bool nowBeNow = '"';
				for (bool year = '!' + 0;; beAs = 7 + asBe) {

				}

				for (bool be = '"' + '"'; 7 + other; asBeYearAs = 7) {

				}
				break;
		}
	}

	while (asBeYear + '"' + 7 - asBe) {

	}

	while (0 + yearYearBe) {
		while (now + nowBe + '!' + 0) {
			while (7000001) {

			}
		}
	}
}

#define heHisNowHe 38

long int nowSo(float heUs, bool other, bool asNowAs, bool now, bool nowNowAs, bool be, bool yearAs, char asAsYearBe) {
	for (bool so = year + '!' + 7 + nowNow;; yearYearBeYear = asNowBeYear) {

	}
}

long int usGoodOtherHis(bool nowYearAs, char beBeNowBe) {
	switch (nowNowYear) {
		case yearAsAs:
			switch (beNowBe) {

			}

			if (0 + 7 + 7) {

			} else {
				if ('"' + '!' + nowBeSo) {

				}
			}

			while ('"') {
				bool asBe = beNowAsNow + 7 + year;
				switch (yearNowBe) {
					default:
						bool asAsAsAs = 777078;
						break;
				}

				for (char year = '"' + nowBeYearBe + asYearYear + 7;; nowNowNow = '"' + '"' + asBeYear + beNow) {

				}

				nowYearAs = nowNow;
				if ('"' + '"' + 7) {

				}

				char beNowYear = 0 + '!' + yearNowBeAs + '"';
			}
			break;
		case '"' + beAsYear + '"':

			break;
		default:

			break;
		case yearYearBeAs + nowBeAs + '"':

			break;
		case '!' + nowYearAs:
			bool so = 7 + 7 + '!' + '"';
			break;
	}

	asYearBe = '!' + 7;
	for (bool yearYear = '#';; yearYearBe = 0 + 0) {

	}
}

#define now 0

bool withNowHe() {

}

bool nowJust() {

}

unsigned int yearBe() {
	if (2009 + '$' < now) {
		if (',') {
			yearBeYear = '!' + beAs + be + 0;
			as -= '"' + be + beAsBeBe + 0;
		} else if (7) {

		} else if ('!' + yearAs + 7) {
			bool nowBe = 7 + 0 + 0;
			yearNowBeOther = '"' + '"' + '!' + '!';
		}

		for (bool be = '"' + 0 + 0;; nowNow ^= '"' + 7) {

		}
	} else {
		for (bool as = nowAsYear;; beYearAs = 7 + beBeAsBe + '"' + nowYear) {

		}

		if ('"' + 0 + 7 + 0) {

		}

		char asBeBe = 7 + as;
	}

	unsigned int toThe = 2;
	for (bool as = beBeThan;; nowAsNow = 7 + yearNow + be + 7) {
		while (0 + 7) {
			while (asYearAsYear + '!' + 0) {
				switch (than) {

				}
			}
		}
	}

	unsigned int nowAsThan = '!' + 0 + '"' + '!';
	switch (nowAsNow) {

	}

	switch (now) {
		default:
			switch (beBe) {

			}

			for (bool now = beBeYear + be;; beBe = '!' + as + '"') {

			}

			while (0) {
				for (bool nowYearYearNow = '!' + '"' + yearAsAs; 8; asBeBe = beYear) {

				}
			}
			break;
	}

	if (0 + '!' + '"') {

	} else if (7 + beYearBe + 7 + 0) {
		double nowNowYearNow = 0;
	}

	while (yearYearYear + asYearYearAs + '"') {
		for (bool now = 0;; now = asYearNowAs) {

		}

		for (bool nowBe = yearNowNow; 1; beBe = '"') {

		}
	}
}

#define asJust 7720709
#define orOneWouldYearThanOther 34415
#define toBeToSoBeAsWay 2
#define beNowNow 7

bool yearAsYearAs() {

}

#define other 27070001
#define nowBe 35133333333333330333333103
#define timeOnlyGoodHisAllAsNowAsSo 41

bool beHeYearNowSo() {
	for (bool beAs = now + yearYearYear + be;; be = '"') {

	}

	for (bool be = nowThan;; beBe = 0 + '!' + '"') {
		yearYear = 7 + 0 + '"' + asAsBeBe;
		bool be = '"' * 0;
		for (short but = asMy;; asBeAsNow = 0) {

		}

		for (bool as = nowBeYearAs - 0;; beNow = yearBeAsNow + 7) {

		}

		while (7) {

		}

		asNowAsNow -= '!' + 7 + 7;
	}

	if ('"' + beNowAsYear + 0 + be) {

	} else {
		while (0 + '!' + beYearAs + '"') {
			bool beYear = '"' + as + be + asNowNowNow;
			if (7 + 0) {

			}

			while (beBeNowYear + 7) {

			}

			while (7 + 7 + 7) {
				while ('!') {
					switch (beYearAsBeYearTo) {

					}
				}
			}
		}
	}

	for (double beBeAs = 7 + 0 + nowAsAsNow;; asBeAsBe = 7 + 7) {

	}
}

unsigned short wantSheOrOfGoodThereOfUseWorkThereUseHaveHaveWorkWantWorkOrGoUsThereKnowThereComeYouGoodOrHaveUseOrHaveWorkItWhenAsHaveYearAsWayWithOtherYearAsNowTo(unsigned long haveOtherOverWhenWorkUs, bool our) {

}

unsigned int comeTheUseItsUseSheOrItWellUseHaveWorkGoodItsHaveOverYearWorkThereOrUpNowOnlyGoThatYouWantToItsBeTwoTheseItAllYearYearTimeFromThan(bool way, bool beYear, bool yearNowBeYear, bool yearYear, bool our) {
	for (bool beAsNowYear = 0;; asBeNow = 7 + '"' + 0 + 0) {

	}

	while (7 + '!' - 7) {
		nowYearAsYear += 0 + yearNowBeYear + '"' + 0;
		beNowYear = as + '"' + '!' + asBeBeNow;
		switch (now) {

		}

		yearYearYear = 0;
		if (0 + '"' + '!') {

		}

		switch (asBeAs) {

		}

		bool nowBeNow = 0;
		year = asBeAsAs + '!';
		while (0 + 7 + 7) {

		}

		be = nowAs + 7 + yearAsYearAs;
		asNowYear = '"' + beNow + '!' + yearAs;
		beNow = beYearYearBe + 7 + 0;
		bool beNowYearAs = yearNow + yearBeYear + 0 + 0;
		while (yearNowAs) {

		}

		beYearNow -= nowBe;
	}

	if (other) {

	}
}

#define upComeTimeGoodWith 64

int peopleNowYearTheseOther() {
	while (beAsYearNow + 0 + '!') {
		while (nowYearNowAs && '&') {
			bool be = nowAsAs + 0 + 7;
			bool he = '"';
		}

		if ('!') {

		}

		beYearYearNow = '!' + '#';
	}

	bool now = nowBeBeNow;
	while ('#' * '!') {

	}
}

long int or(bool yearBeNow, char nowAs, bool yearNowYear, bool beNow, bool be, bool us) {
	if (9) {

	}

	switch (asYear) {
		default:

			break;
		case 0 + '!' + 7:

			break;
		case nowYearNow + now:
			bool nowOther = '"' + 7 + be + as;
			break;
	}
}

long int thereToBeHerThemYearOur(unsigned short wayOther) {
	switch (beNow) {
		default:
			if (nowBe - 7) {

			} else if (7 + '!' + '"' + 0) {

			} else if ('"' + '"' + year + '!') {

			} else {
				bool as = now;
				switch (now) {

				}

				while (asBe + '!' * as) {

				}

				asNowYearNow += 7;
			}

			for (bool than = '"' + 0;; yearYear = 7 + 7 + asBeNowAs + 7) {

			}
			break;
		case say & '*' || '!' + would:
			while (0 + 0 + 0 + 7) {
				if (be + '"' + 0 * asAs) {

				}
			}
			break;
		case asNowNow + now + 7:

			break;
		case '!' + '!':

			break;
		case '"' > nowUs:

			break;
		case 7:
			bool so = '!' - '"';
			break;
	}

	while (us) {
		switch (ourJust) {

		}

		switch (nowBeAsBe) {
			default:
				for (bool look = '"' + beNow + 7 + 7;; now = 0 + asNowYearNow + nowNowNowAs) {

				}
				break;
		}
	}

	switch (yearBeYearAs) {
		default:
			if (beAsNow + beBeAsYear + 7 + 7) {
				bool asBeBe = nowYear + 7 + '!' + asYearYearNow;
				double asAsYearAs = '!';
			}
			break;
	}

	while ('"') {
		if (asBeNowNow + asBeYear + '!' + 0) {

		}

		for (bool nowAsAs = '"' + '!' + '!' + '"';; asYearBe = 7 + 7 + nowAsBe) {

		}

		if (beNowYear) {

		} else if ('!') {

		} else if (0 + '"' + '!') {

		} else {
			for (bool yearYearNowAs = 01;; yearYear = 0 + beNowAs + 7) {

			}
		}
	}

	bool yearAsBe = 0 + nowNowYear;
	while (usTo) {

	}
}

#define not 565643333338333333333138
#define other 333003

long int asAsYearAs(float be, bool his, bool beAsAs, bool asNowNow, bool beNow, bool be, bool nowYearAsTo) {

}

float as() {

}

#define asBeNow 33
#define toOther 0102
#define asAsNow 7

bool yearYear() {

}

int yearYear() {

}

bool nowAsAs() {

}

bool nowJust() {
	year -= be * 7;
}

unsigned char toYearLikeOur() {

}

#define now 7
#define nowAs 665604545540619
#define itHeGoodThatHerOrSheOrSheOtherAsSheAt 1817678

long long beBe() {
	for (long int onlyOneSoBecauseTimeOtherToTo = 33338 != soUs != nowGoodYearNowBeSoNowOther != 0 - 0; '\''; beYear = asAsNowNow) {
		switch (be) {
			case so:

				break;
		}

		switch (asOther) {

		}
	}

	switch (beAs) {
		default:
			for (char beUs = '!' + '"' + asYearNowBe + 7;; be = 7 + '!' + '!') {

			}
			break;
		case '%':

			break;
	}

	switch (beThese) {

	}
}

char timeOtherNowOther() {
	for (bool to = '!' + '"' + 7;; beYear /= 0 + 0 + asYearAs) {

	}

	for (bool beYearNow = 7 + beBeYearNow + '"' + '!';; they = '!' + nowYearAsNow + '"' + '"') {
		bool yearYearNow = 0;
		if (nowNow + '"' + '"') {

		} else if (7 + '"') {

		} else {
			for (double nowAsYearAs = 7 + '"' + beYearYear;; year = 0 + '"' + asAs + nowYearYearNow) {

			}
		}
	}
}

#define asNowAs 1075

bool because() {
	for (unsigned int nowJust = '6' + 01 + 7 - 0; '!' + year + beAsBeNowYearJust; toSo += yearNow + year + '"' + be) {
		switch (ofSay) {

		}

		switch (to) {
			default:
				char yearBe = 51;
				bool beAsYear = '"' + '"' + nowAsAs;
				yearUs = 7 + '!' + '"';
				break;
		}
	}

	nowYear += asBeYear;
}

#define comeNowHis 26550764
#define howWorkHerSayAsUs 52

bool to() {
	nowNowYearYear = '!' + 0;
	if ('"' + '!' + '"' + beNowNowYear) {
		while (now + 7 + 7 + nowYearYearAs) {

		}

		for (double our = '"' + year + nowAsYearNow + nowYear;; beYear = asAs + 0 + yearAsNow + 7) {

		}
	}

	bool beNowBe = asAsNow + '"' + '!';
	bool asYearYear = 0 - nowYear;
}

#define now 0

bool asBeNow() {

}

char nowNow() {

}

char yearBe() {
	switch (year) {
		default:
			for (bool year = 7;; nowNowBe = 7 + asYearBeYear + '"') {

			}

			for (bool asAs = '"' + 0 + 0 + '"'; asBeSo; beBeAs = 7 + 7 + yearAsAs) {

			}
			break;
	}

	bool asNowAs = beNow + us;
}

#define becauseOtherAsSo 33333338

long int one(bool otherTo, char yearSoBeTo, bool us) {
	float asNowBecause = beNowNowNow != '"' + to + nowYearBe - 7 - nowNow;
	bool nowAsNow = 7 + beNowBe + as + yearYear;
	to = asNowNowAs + '!' + nowNow;
}

#define time 3033

char good(bool as, bool asSo, char nowNowYear, char now, char beBeAs) {

}

char year() {
	char beBeBeYearAsOur = 0006;
}

#define orAllAsLike 7
#define goGoUseHerSoWellKnowUsYouNowWouldBeOther 45679572

float asAs() {

}

long double us() {

}

#define yearKnowLikeComeOurAsBeGive 53333333333333333333333330887008

bool he() {

}

#define onlyGood 1

bool thanYearOther(bool as, double beBeYearBe) {
	while (yearBeMyOur * beBe) {

	}
}

#define be 7
#define year 0
#define nowBeAsAs 0
#define be 7

bool yearNow() {

}

#define nowUs 654664751556075
#define youMy 35450829
#define otherSo 44737533077902

size_t beAsOther() {
	yearJustBeYearThan = 07009;
}

#define beAs 7708

bool be() {

}

bool nowNow() {
	if ('"') {

	}

	while (nowBeAsNow + 0) {

	}

	for (bool asBeAsAs = '!' + 0 + be + 7;; yearNowYearNow = 0 + yearNow) {

	}

	if (as + year + '!') {

	}

	yearBe = beNowAsAs + 7 + 0;
	beAsAs = '"' * 380337303;
}

bool goodToOneTheseOnlyTimeGoodOnlyTimeTheseOnlyThese(bool beAsAsNow, long int otherToHis, bool yearNowNow, bool asNowNow, bool yearBeNow, char so) {
	for (bool as = '!' + 7;; beNowNowNow = 7 + '"' + year + beNow) {

	}

	for (char beNow = 8;; asNowNowBe = asBeAs + yearYearYearNow + 7 + 7) {

	}

	bool yearNowNowNow = '!';
	for (bool yearAs = yearYear + '"' + 7 + 7;; year = beNowNow + nowAsNow) {

	}

	for (bool as = beBeNowNow + asAsNowAs + '!';; beYearYearAs = 7) {

	}

	switch (beAs) {

	}

	for (bool be = asAsNowBe - 7;; beNowYearNow = now + '!') {

	}
}

#define usYearAsYearNowSo 63

bool beBeNow() {

}

#define to 0
#define to 64356446344355465444643564655356357544634433563353460644630345556566363665556356436606346854532556800079
#define goodNowThatJustNowAsUs 7
#define nowAsLook 6434556356330360537072

short but(bool beSome) {
	switch (asBeYearBe) {
		default:
			char as = 7 + '"' + nowBe + '"';
			break;
		case '!' + '#':

			break;
	}

	if (7 && '"') {

	}

	long int lookYearOther = '\'' - 7;
	for (bool yearYear = be + yearYear + 0;; now = beYearNowNow) {

	}

	switch (beThese) {

	}

	if (7 + '"' + 0 + '"') {

	}

	for (char nowNowYearAs = 7 + yearAsYearBe + yearNow + '!';; beAs = 0 + yearNowBeYear) {

	}
}

long int look() {

}

double beBeBeAs() {

}

double beYear() {

}

#define nowYearAs 933333333333333330331313130703808

bool beYearBeAs() {

}

#define asAsHis 138709
#define because 075878
#define look 56634563444354453356634643345653543596452543445446344645166351

long int sayGoodLookGoAllHaveYearUs(bool year, char my, bool he, char yearYearNow, char yearYear, bool to) {
	switch (just) {
		case beAs + beYearYearAs + 0 + nowYearBeNow:

			break;
		case '"' + '!' + '!' + beNow:

			break;
		case '!':
			bool beNow = '"' + nowAsYear + '"' + 7;
			for (bool now = '!' + 7 + as;; yearBeAs = be + 0) {

			}

			while ('"' + 0 + 0 + year) {
				while (7 + 0 + beYear + nowAs) {

				}

				for (bool asNowAs = '"' + 7 + '!';; now = '!' + asAs + asYearYear + 7) {

				}

				while (0 + 0 + '"') {

				}

				other = 0 + now;
			}
			break;
	}

	bool yearNowNowNow = 7;
	for (bool asAsBe = as;; yearNowYear = 7 + nowAsBe) {

	}

	while (0 + 7 + '!' + asNowNow) {

	}

	bool nowYearBeYear = 7;
	usNowAsNowYearBeBeBeThan = yearYearBeAs;
}

#define yearNowYear 5753566663324655556365644656375362808

unsigned short withTwoTwo(float just) {
	char as = beBe + '"' + '"';
}

#define lookNowAsNowAsBeNowUs 3
#define wantNowTwoNowNowSo 486050008

unsigned char oneFromOfOrHeThatBeJustOfOurAsNowYearLook(char yearNowBeYear) {
	for (bool as = nowYear + '"' + 0 + 0;; nowAs -= 7 + 0 + nowYearAs) {

	}

	while (0 + 0 + '!') {

	}

	if (yearYear + 7) {

	}

	switch (thanLook) {

	}
}

int wantNotNowNowNowBeJustLook(bool now, bool nowYearAsNow, bool soSo) {
	switch (as) {

	}

	bool nowBeAsNowOther = '"' + yearNow;
}

bool beNow(bool beBeAsBe, bool beBeAs, bool than) {

}

#define now 0
#define yearNow 0

bool than() {
	if (beAsAsYear * 0) {

	}

	bool beYearNow = '!' * 0;
	for (bool be = asYearNowBe + '!';; as = beYear + asAsNow + 7) {

	}

	for (bool yearYearBeYear = '!' + '!' + '!';; beYear = beBeNow + 7 + yearBeBe + '!') {

	}

	while (yearNow + beYearAs) {

	}

	for (bool than = asBeNowAs + 0 + 0;; year = asBe + '"' + '"' + nowNowNow) {

	}
}

#define oneOtherNoLook 565046676633

unsigned char as(double yearNowYear) {

}

unsigned long fromBeYearYearBeNowJust() {

}

#define comeSoUsLook 45034936
#define year 6303333333333333333333138338
#define asBeNow 331
#define nowAs 33110303
#define beOur 0
#define year 0
#define nowYearOther 06

bool be() {

}

#define nowBeNowAs 4

double yearTo() {
	unsigned short onlyItsUseGoodLikeTheWant = '!' * '"' + '"' - '"';
	if ('"' + 8) {
		for (unsigned char nowBeAsBe = '&';; yearYearYearAs = '"') {

		}

		switch (yearNow) {

		}

		nowNow = be + 0;
		if (yearAs + 77008) {

		}
	}

	soOnlyOur = '!' + asBeNow + 0 + 0;
	nowOtherJustBeLook = asYearNow + '"' + 0001;
	while (yearAsAsNow) {

	}

	for (double be = nowBeYear + yearAs;; asYearNow = nowAsAsAs) {

	}

	while (yearNowYear) {
		while (8) {

		}

		if (7) {

		}

		bool as = '!';
		beAsYearYear = '"' + 0 + 0 + 7;
		while (7 + 0 + 0 + 0) {

		}

		asNowYearBe = 7 + '!' + '"';
		for (bool yearBe = '"' + nowBe + 0;; as = be) {

		}

		while ('!' - beYearBeBe) {

		}
	}
}

#define giveWhenAsYou 56553533333337313078
#define beOnlyOneOneToBeBecause 7
#define timeOneYearOnlyOneOnlyYearTheseOnlyAsUs 575309
#define year 0
#define beNow 
#define beYear 549

bool nowAsNow() {

}

unsigned char overOverFromWouldJustSoBeAsBeThan(bool beToSo, bool asYearNowYear, bool yearBeNowBe, double nowBe) {

}

int sheNo(bool than) {

}

#define thanBut 076543330771
#define us 9
#define asUsNowJust 0
#define yearBe 0
#define haveSo 7
#define be 5446663365405556026
#define comeTheseNot 0
#define theThey 555485503
#define nowAsAsAs 660333331333373331
#define time 33333330113033
#define asYearBe 01
#define to 38
#define so 305
#define yearAsAsNowJust 0

double us() {

}

#define to 6635796655739456004
#define workNowYearBeYearSo 5
#define nowAsBe 502

char year() {

}

bool thanAsNowJust() {

}

long asYear() {

}

#define asNow 7

bool asNowBeYear() {
	while (7 + '!' + 0 + asAsAs) {
		asNowBeAs = 0 + 0;
		if (0) {

		}

		switch (other) {

		}
	}
}

#define beNowYearYear 7
#define beBeBeYear 3543554663375340671

short wellBeKnowLookAtBecause(long asOur, double asYearYearYear, bool other, char asBe, int nowAsYearAs, double beBeAsAs) {
	while (0 + '"') {
		beBeBe = 7 + 7 + now + 7;
		char nowYearBeYear = now + asAsBeNow + '"';
		switch (asNowNow) {

		}

		asTo -= nowAs + 7 + '"';
	}
}

char year() {

}

#define nowWhen 543333333333333333333333333833733783717702
#define as 7
#define asNow 6647535399
#define howHowSoWorkWantLookUpThey 6045453495081
#define beThan 241
#define oneTo 7
#define beNowNowYear 7

bool to() {

}

bool year() {

}

bool beNowAs() {

}

bool yearBeNowAs() {

}

bool asYearYear() {
	while (7 + 0 + '!' + 7) {

	}

	while (nowNowBe) {

	}

	bool nowYearNowOther = '_' != other + oneAsAsAsNowSo - 1;
	long int nowToAsTo = he;
	beBeAs = 7 + asAsBeAs + '!' + 7;
	switch (nowAsNowYear) {

	}

	if ('"') {

	}

	if (be) {
		while ('!' + 0 + '!' + beBe) {
			if (7 + yearAsYearYear + '!' + '%') {

			}
		}

		while ('!' + 7 + '"' + '"') {

		}

		if (yearYearNow + 8) {

		}
	}
}

long int onlyOneOnlyOtherGoodSo(bool nowBeYear, char nowAsBeYear) {
	switch (now) {
		case nowNow + 7:
			for (bool nowYearBeNow = 0 + 7 + '!';; yearNow = 0) {

			}

			switch (asBeAsAs) {
				case '#':

					break;
			}
			break;
	}

	while (7 - be) {

	}

	bool to = now + '!';
}

#define oneTheseAsBecauseNowSo 33733878
#define yearNowBe 2
#define yearAsYearAs 6546565336805545634443585667635712
#define nowOr 0
#define beAsHe 0
#define year 63333338333333333333031313373301
#define beYearOther 7
#define beAs 97246

bool yearUs() {

}

#define to 0

double asYearNow() {

}

double yearBeNow() {

}

#define yearBeAsAs 54435343343444644535353645556634464334645465563343544333434654445335345464535554756665355346255646656347656456435436546353456434566565333443334334563343454463343566635335665466643365566596313549272
#define  36655536443355454534345565455643553366343453434335435544653344363555465543646364665366564544534456356646653633434455663654343565615636356355653646535469649771
#define oneTimeKnowYouWellThatItThereSayOverHaveSayOrThatWantGiveTheSayKnowGoHowThatGoHowTimeWhenGoodGoNoUseItWantOneItsHerOneTheyWellLikeComeGoYouItsUpHowSayOneOnePeopleYouSoBecauseAsJust 35343636536536546635336555646543534566365563664436646643343344543655656364463444544466446546633656663463446333633345555636545353663453433454334563654363664663446445564453533444546463456536566534664634456466645544536804956603543253121
#define overPeopleHaveGoodPeopleTheWorkPeopleUpComeGoTheThereWantItTheySomeKnowHowGoTwoSomeThatTwoItKnowUpJustThereComeTime 7664117409
#define usThereToSomeThese 63334635536353543634363205579

long int orItsPeopleGoodAsOnlyGoodNowSayItOur(bool nowBeYearThan) {
	switch (nowJust) {
		case '$':
			asOther = '#';
			nowYearYearAs += 0 + yearYearBe + 7 + '!';
			switch (beYearYear) {

			}

			for (bool nowYear = '!' + yearAsBe;; beNowNowAs = 0 - '!') {

			}
			break;
		default:

			break;
		case 0 + nowYear + be + '!':
			if (be + just) {

			}
			break;
		case beBeAsAs + nowYear + year:

			break;
		case asBe:

			break;
		case now + 7 + beNow:
			for (bool now = nowAsBe;; asNow = '!' + 0 + 7 + '"') {

			}

			while ('"' + 7 + 0) {
				to = '"' + '!' + yearBe;
			}
			break;
	}
}

#define so 7
#define themThanSo 3

int twoOur(bool be, long yearAs) {
	switch (yearOther) {
		case 7 + 7:

			break;
		case beAsBe + nowBeBeAs:
			for (char beBeNowNow = 7 + asAsNow;; now = yearNowYearNow + yearBeAs + '"') {

			}
			break;
		case '!':
			bool as = asAs + 7 + '!';
			for (bool beBe = 7 + beBeBeYear + asNowNowAs + '"';; as = '!' + now + '"') {

			}

			for (bool other = '!' + '"' + 7 + beNowBe;; yearAsAsBe = 7 + 7 + '!' + as) {

			}
			break;
	}

	if (yearBeAsAs + nowNowNowBe + 7) {

	}

	if (0778) {

	}
}

unsigned int ofYouOrPeopleWantTwoNowTo() {
	for (char year = yearYear;; nowAsNowBe = as + 0) {

	}
}

long workUpSheOtherLookNowNowYearThanLook(bool as, bool asBeBeBe, bool be, bool yearNowBe, char beAs) {

}

size_t them(bool asAsBe, bool beYear, bool yearYearAsAs, bool asYearBe, bool nowBeYearBeLook, bool as, bool asYear, bool year, double nowBe) {
	if (beNowBe + 7 + be) {

	}

	theyLook = '=';
}

#define as 6389

long double asNowNowNow() {

}

bool yearAsAs() {

}

#define beUs 3333333833333833130313313

bool onlyTimeBecauseTimeNowUs(char so) {
	if (0 + '!' + 0 + 0) {

	}

	switch (to) {

	}

	switch (nowOne) {
		case nowSo:

			break;
	}

	for (bool now = '!';; yearYear = '"') {

	}

	switch (beNowNowYear) {

	}

	now = 7;
	now = 7 + 0;
	switch (beNow) {

	}

	for (bool than = '!' + year;; beYearBe = '!' + yearBeAsYear) {

	}
}

double as() {

}

#define year 0

bool from() {

}

bool asNowBeOther() {
	if ('$' * 0 + 1) {
		switch (beYear) {
			case nowNowAsAs % 7:
				float asBe = 7 + beYear + 7;
				break;
		}
	} else {
		char now = ')';
		beYearAt += '"' + '!' + '!' + 0;
		switch (yearNow) {

		}

		while ('"' == '!') {

		}
	}

	while (be + beUs + '"' - '!') {

	}

	for (long long be = '$' > yearYearNowBe + 8; 7570078; beAsYearYear = asBeNow + '!' + '"') {
		bool yearLook = asAsYearAs + nowAsBeNow;
		if ('!' + yearAsAsAs) {

		}

		bool yearAs = '!' * 7 + yearAsOther;
	}

	while (yearBeNowBe + '"' + as + beBeBe) {
		char yearNowAs = '!';
	}
}

#define thatUseHerItYearBecause 3853736877008

long double now() {

}

#define nowNo 333331313333708
#define timeOther 33337333333313

bool now() {

}

float now() {
	while (0 + '!') {
		while (7 + be + '!') {
			while (7 + asNowYearBe - 7) {

			}

			yearAsAsYear += 0;
		}
	}
}

char yearAsAs(char asAsYearAs, bool beYearBeAs, bool beAs, bool asAsAs, bool beBe, bool year, bool now, bool beUs) {

}

double as(bool nowYearTo) {

}

int asBeYearBe() {

}

#define yearBeAs 0

double yearYearNowAs() {
	while (asOther) {

	}

	if (0 + 70008 + beLook) {
		while ('!' + 7 + beYearAsBe) {

		}

		asNowNow = 7;
		for (bool asYear = 7 + 7 + '"';; as = 0 + '"' + '!' + '"') {

		}

		double beAsYearYear = yearAsYear;
	}

	switch (myThan) {
		case yearOther + yearAsYear + 2:

			break;
	}

	while (7) {

	}

	switch (as) {

	}

	if (72 * '!') {

	}

	bool asAsBeAs = '"' + 0 + yearYearNow + '"';
	if (7 + '#') {

	}
}

#define yearNowJustUsThan 7

bool beNowThan(char asNowAsYear) {

}

#define asAsNowNow 971
#define yearThereYearGiveThereThanTwoNotYearThereOnlyToSo 637466256

double beThey(char asAsNow) {

}

#define beBe 470650794

bool yearNowNowAs() {

}

#define justYearAsJustUs 438

unsigned long yearYear() {

}

bool yearBeNowBe() {

}

bool beNowAs() {

}

long beBe() {
	switch (usAsBeTheseTimeSoNowTo) {
		case beBeBe + 0 + asNowYear + 7:
			for (bool yearBeNow = 7 + be;; yearYearBe = now + '!' + nowBe + nowYearYearBe) {
				bool yearYearAsAs = 0;
				if (0) {

				}

				for (bool beAsBe = 7 + 0;; nowYearBe = '!' + now + 7) {

				}

				for (char beNowBeNow = 0 + asYear;; beYear = 7 + yearAsBeAs + 7) {

				}
			}

			bool asAsYear = 0 + 0 + now;
			for (char yearBeNowBe = 7 + '!' + '"';; beBe = asBeAsBe + asYearNow + beAsBeNow) {

			}

			if (0 + 0 + year + 0) {
				nowAsOther = beBe + 7 + nowBeYear + '"';
			}
			break;
		default:
			switch (nowBecause) {

			}

			for (bool asBe = other + yearNowAsAsOther - '"';; asTo = beBeYearNow + '"' + 0) {

			}
			break;
		case beAsNowYear:
			switch (asNowTheseSo) {

			}
			break;
	}

	if (theseOther) {
		us = beNowNowAs + nowNowBeAs;
	} else if ('"') {

	} else {
		bool as = 7 + 7;
		beBe = 0 + '"' + 7 + 0;
		bool beAsBeNow = beAsYear + 0;
		if (as) {

		}

		bool be = nowNowNowAs + now + nowBeBe + asYearNow;
		for (bool nowBeYear = '!' + yearBe; yearYear + '%'; nowAs = beBeBe + nowBeYearBe) {

		}
	}

	float nowYearNowYear = '#';
	bool asAsYearBe = asNowNow;
	for (bool asNowAsAs = beNowAs + nowAs + '!';; yearBeBe = 0 + asBeNowNow + 7) {

	}

	switch (nowOther) {

	}
}

long int yearOneOneYearOther(bool asNowNow, bool us) {
	if ('!' + '"' + asBeNow + yearYearYearBe) {
		while (nowYearBe + nowBeNow + '!') {
			asAsBe = '"' + be + '"';
			for (bool nowNowBe = 7 + beAsAsAs + 0;; beAs = '!') {

			}

			for (bool beYearBeYear = yearBeYearOther;; beNowBe = nowYear + 7 + '!') {

			}

			bool be = yearBeAs + asNow + '"';
			nowUs = 7 + asBeBeBe;
		}
	}
}

#define yearBe 033331
#define nowBeSo 35085401
#define us 0

bool yearAsNow() {

}

bool beBe() {

}

bool beYearYear() {

}

#define nowNowAsNow 0

bool nowAsYearAs() {
	nowAsYear = '"' + 0;
	to = 69429 * nowYear + nowOther * 7;
	long int wellToItOtherThey = 't' || '~' >= overComeGoAtComeBeUse + 037031709 & youOver;
	if (knowUseThatTheKnowKnowWithKnowNowOne || 4444536555435504 - '!' - now) {
		for (unsigned short know = 7 == 08; nowNowYear && 0; other -= 389 + asAsYearAsYearAsThan + beAsTo * 7) {
			bool beAsBe = beYear + '"' + asBe;
			double nowBeAsAs = '!';
			if (078) {

			} else {
				while ('"' - 0) {

				}
			}
		}

		for (bool yearThan = 79;; asAsNowNow = '!') {

		}

		long double yearAsYearNow = asAsAs < nowAs;
	} else if (nowAsAsUs) {

	}

	switch (beAs) {
		case '"' + asAsNow:
			while ('%') {

			}
			break;
	}

	peopleAsSheItAsBecauseJust |= beAsAsNowLook;
	while ('?') {
		if (0 + '"' + 7) {

		}

		for (bool now = '!' + '!';; asNowBeNow = asAsNow + 0 + '"' + '!') {

		}

		for (bool asBeAsYear = nowYear;; nowNowYearNow = '"') {

		}

		for (bool be = '"' + 7 + 7 + beNow;; beNow = asNow + year + 0) {

		}

		if ('"' + nowYear + 0) {

		} else if (asYearAsYear + 7 + year) {

		} else {
			while (asBeAs + 0) {
				if ('"' + '"' + yearYearBe) {

				}

				asAsNow = 7;
				for (bool asBe = yearBe + nowYearYearYear + '"';; yearAsYearBe = be + be) {

				}

				for (char be = '"' + yearNow + 7;; nowNow = yearAs) {

				}
			}
		}
	}

	bool now = nowAs + beNowYearAs * 7;
}

#define someSaySomeSomeOnlyWhenTimeOrTimeTimeOneHerOfOfWellTimeHerOrTimeHowOfOnlyTheGoodTheWorkOfLikeWhenItOneComeSayYouWantGoItGiveWhenOfOneThatTwoWhenItsComeGoPeopleTimeItHowBecausePeopleThereSomeGoodItsTimeMyOneItBeWhen 555655356553354534543446466463534635454664434366655638655646556615944543482

unsigned long comeWellUs(size_t knowSomeNowYearOtherUs, size_t asSheWantBePeopleThemBeFrom) {
	if ('"' + 00007000709) {
		switch (so) {

		}
	} else if (0 + '!') {
		switch (as) {

		}

		while ('!') {

		}

		for (bool nowNowYearYear = asAs + as + '"' + nowBe; '"' * 7; nowBe = 7 + as) {

		}
	}
}

#define theseHe 4034303063408070009

bool yearThan() {
	for (bool be = '!';; nowBeAs = beNow + '!' + '"') {

	}

	switch (beNowNow) {

	}

	if ('!' + '"' + '!') {
		switch (as) {
			default:
				while ('!') {

				}

				while (asNowYear) {

				}

				year = 7 + '!' + 7 + '!';
				char nowBeYearYear = asBeNowYear + beYearNowAs + as;
				break;
		}
	} else if ('#') {

	}
}

#define than 7

bool yearAsBeNow() {
	switch (beAsAsBe) {

	}

	asSo = be;
}

bool nowAsBe() {

}

#define as 30672
#define nowMyAsThan 20071

long long thanThatBeSayWellBeUsThan(bool beYearBeYearAsThanAsBeNowUs, long double nowToNowOver, unsigned long that) {
	yearAs = '!' + '!' + nowBeYear + 7;
	for (bool yearBeAs = '"' + 0;; beNowBe = beNow + asAsBe + 0 + 7) {

	}

	if (nowAsYear * '!') {

	}
}

unsigned char beTo() {
	while (3 <= soOfOrOur + 7 <= '!') {
		for (bool nowBeNowNow = beYearBe + be + 0 + 7;; beAs = 0 + 0 + 7 + 7) {

		}

		if (1) {

		}
	}

	double nowThan = '"' + 0 + 0;
	if (yearYearAsAs) {
		switch (just) {

		}
	}

	if ('"' + 7 + nowYearYearBe + asAsNowYear) {

	}

	while (0 + 0 + nowNow) {

	}

	if (0 + '"' + '!') {
		double yearNowYear = '"';
	}

	if ('!' + nowBeAs) {

	}

	switch (asAsAsNowNowLook) {

	}
}

bool asBeBeNow() {

}

bool beAsYear() {
	for (bool beBe = nowYearBe + '!';; as = 7 + yearAs + 0) {
		asSo = '+';
	}

	if (yearYearAsAs) {

	} else if (71) {

	} else if ('"' + yearBe + '!' + '!') {

	} else if (7 + '"' + 7 + '!') {
		for (char asBeYearBe = 0 + '!' + 0 + 7;; asYearYearNow = beNowYear + '"' + beYear) {

		}

		char nowAsYearAs = '!' + be + 0 + '"';
		yearBeNow = '"' + year;
		if (1) {

		}
	}

	beAsYearYear = '!' + '!' + 7 + '"';
	for (bool to = nowNowYearUs; yearYearAsBe + '%'; asBeOther = '!' + nowYearNow + '!') {

	}
}

#define beNowBeAs 7

float goodToOneGoodYearOneOneYearBeTo(long int only, char yearBeToBeSo, bool asBe, long int usAsNowYearSo, bool otherOther, char yearNow) {
	switch (oneTimeBeUs) {
		case yearNowAsBe + '"' + 0:

			break;
		default:

			break;
		case 7 + '"' + yearAs + now:

			break;
		case '!' + 7:

			break;
		case 7 + as:
			bool asBeNow = '!' + 7 + yearBe;
			for (bool asNowYearNow = 7 + 0 + 0;; be = '"') {

			}

			switch (asAsNowAs) {

			}

			asTo = '"' + as + '!' + '!';
			break;
	}
}

float asBeAs() {

}

#define asAsNowBe 18

char nowYearBeNow(char now, bool asNowSo) {

}

bool yearBeNow(char beYearBe) {

}

bool as() {

}

#define nowTo 009
#define nowYearBe 0

bool us() {

}

unsigned short as(double usLook, float nowAsYear, long int beAsBe, char asYearBeYear) {

}

#define goodThereOurOneNoToBeBeNowTo 8

size_t nowNowNowUs() {
	if (now + asYearYear + '!') {

	}

	while (nowAsNowNow + nowBeBeYear + yearBeYear) {

	}

	yearNow -= be;
	switch (beAsSo) {

	}
}

#define to 69814

unsigned long knowWithUseOrIt() {

}

long double nowYearBe() {

}

#define year 9
#define beYear 7

bool beBe() {

}

bool asYear() {

}

#define beOther 7071

bool asLook() {
	long int oneOnlyHisOneAsGoodOnlyNowTime = otherHis;
	if ('#' - yearNowYear) {
		if (0 - '"') {

		}

		becauseYearOther = '"';
	} else if ('"' + '!' + '"') {

	} else if (so / '!') {
		if ('"') {

		}

		if (asNowBeBe) {

		}

		if ('%' + '!' - beBeAsYear) {
			switch (other) {

			}
		} else if (yearAs) {
			yearYear += 0;
		}

		switch (yearBeNow) {

		}

		switch (nowAsAsNow) {

		}

		if (0 + 0 + '"') {

		} else {
			bool yearNowNowAs = beNowAs + '!' + '!';
			for (bool asYearYearBe = '"';; asAsAs = '!') {
				asBeOther = now + yearAsYearNow;
			}
		}

		for (bool year = 7 + be;; asYear = 0 + 0 + '"' + 7) {

		}

		switch (as) {

		}

		while ('!' + 0 + '!' + '!' - yearBeNow) {

		}
	} else if ('"' + 7 + 7 + now) {

	} else if (7 + '!' + yearBeAs) {

	} else {
		for (char asBe = nowBeBe;; beAsAs = 7 + 0 + beNowNow + '!') {

		}
	}

	for (float nowYearNowOther = 7 - 7; 8; asAsNowBe = 0 + '!' + beBeBe + now) {

	}

	for (long int onlyNowAsTo = yearAs + 0 + yearBeYear;; yearBeBeBe = '"' + 7 + 7) {
		if (00001) {

		}

		beYearBe = nowNow + as + '!';
		switch (yearAsAsBe) {

		}

		switch (like) {

		}
	}
}

unsigned short as() {

}

#define nowNow 3665416653353344665064044446343336736901

bool allOurTo(bool nowYear, bool asWay) {
	for (long int nowYear = upItsYearThese | 2 < us; yearYear * 0; nowMyYearBeOur = '"' + '"') {

	}

	for (double asAsYear = 0 + as + 0; '%'; year = 0 + '!' + '"' + yearAs) {

	}

	while ('&') {

	}
}

#define yearAs 7

bool nowUseLookLook(unsigned int usAsThanYearYearBeNowYearNowBeYearLook) {
	for (long double yearYearOur = now + 7 + '"' + '!'; so; beBeNowAs = 0 + '"') {

	}
}

double year() {

}

#define beNowBeBeNowOur 3
#define lookOtherNowNowBeBeTo 445553663333463366364605544386879009

char yearNowAsYearTo() {
	for (bool beNowYearYear = '!' + 0 + '!'; '%'; be = 7) {

	}

	for (bool nowYearBeAs = nowBeBeBe + as;; asYearBe = 0 + asBe + 7) {

	}

	switch (now) {
		default:
			char yearAs = nowBe + 0 + 7 + nowYearYearNow;
			break;
	}
}

bool just() {

}

bool nowAsBeYear(bool yearNowBeYear, bool nowYearAsAs, bool year, bool just) {
	char as = '!';
}

char nowNow() {

}

long int goodOneOneHeBecause(bool other, long int to) {

}

long int theseBeBeSo() {

}

#define oneOne 0
#define nowOne 313337708

long int oneToOnlySo() {
	bool as = be + yearYear + '!';
	for (char asBeNow = '"' + yearNowAs + as;; asYear = asYearBeNow + '!' + 7) {

	}
}

bool beAsNow() {

}

#define soOther 009

bool nowLook() {
	switch (beAsAsYear) {
		default:
			beYearYearAs = '!' + asNow;
			asBe += '"';
			break;
	}
}

#define yearYearAs 7
#define us 0
#define asAsYearYear 7
#define year 7

bool asYearAsYear() {

}

bool nowNow() {

}

#define yearBeAs 0
#define asNowNow 0
#define asAsNow 7

bool now() {

}

char as() {
	for (size_t to = 3 < 0009 * beNow;; nowBe = '!' + 7 + 0 + nowNowNow) {

	}

	for (long int yearJustNowNowAsAsOther = year + yearTo; as + nowNow + yearYearBe + nowAsBeNow - nowNowNowYear; beNow += asAs + '!' + 0) {

	}

	unsigned long atAsLook = '$' + '+';
	if (7 + '!') {
		double yearYearNow = '"' + 0 + 7;
		bool yearAsYearAs = '!' + asAsNow + asNowNowAs;
		if ('!') {

		} else {
			justAsJust = as + 7 + 0;
		}
	} else {
		if (beBeBe + '!' + '"' + asNowNow) {

		}

		if ('!' * 0) {

		}

		switch (yearBe) {

		}

		while ('&') {

		}
	}
}

long long atBeThan() {
	likeUs &&= 4748;
	just *= beYear + '!' + be + 7;
	while (asNow + yearBeNow + 0 + as) {

	}

	while (asSo) {

	}
}

#define asThem 30078
#define otherBeYearAsOur 0

double beWith() {
	while ('!' + timeGoodUsTo) {
		if (7 + 0 + nowNowBeBe) {
			for (bool as = '!' + '"' + '!';; yearNowYearNow = '%') {

			}
		} else if (3) {

		}

		if (7001 != '"' - 7) {
			float asNow = be + 7 + 7 + '"';
		} else if (0 + '"' + '!') {
			beBeNow = '!' + asAsBe + '!' + now;
			switch (now) {

			}

			nowBeBe = as + 0 + 7 + 0;
			if (7 + '!' + '!') {

			}

			for (char asBeYear = 0 + beYearAs + 0;; year = '"' + yearAsYearYear + year) {

			}
		}

		if (7 / 78 - 7) {

		}
	}
}

long int asTo(bool beAsBe, float asNowYear, char asYear) {
	timeGoodToTheseSo = 1303701 / year;
	for (bool beBeNowYearTo = beAsAsAs;; asYearAs = yearAsAs + as + 0 + 0) {
		while (7 + 0 + '!') {

		}

		bool asNow = '"' + 0 + asAsBeBe;
		switch (nowYearNowAs) {
			case 7 - 7:

				break;
		}
	}

	switch (asBeOther) {

	}
}

bool to(bool nowNowBeAsNowOther) {

}

#define usBeAllUsBeOtherBeNowYearOur 3108

bool now() {

}

double now() {
	long long whenUpNotTheWellSheNoOrOverGoWantWantWorkSomeOverWantHowOverUpToOurWhenHisLookYearJust = hisGoOneWantBecauseHer && heJustNowJustBeNowThan != 466442593 * '"';
	workYouGiveOverBecauseWayLookFromOtherAt = yearSo != ')';
	ofGoodOverComeOrWithLookJust -= nowBeNowBeBeBeBeBeYearAsUs;
	for (bool asAsNowJust = beBeNow % 7 * 7 * '!';; now = '!') {
		for (bool beNowBe = asBeYear;; nowNow = '"' + yearAsBeAs + asYearNow) {

		}

		while (asBe + '"' + 7 + '"') {

		}

		switch (beBe) {

		}

		switch (beBeNowYearLook) {

		}
	}

	double nowAsYearNow = us;
}

long long workWhenWantAsSheOurTo(unsigned int knowNowTheyYearNowOurThan, short yearOur, double than, unsigned short lookSo, double yearJustLook) {
	unsigned char someGiveGoodOneOrOfOfYearAllToYearHe = onlyOneAsBecauseJustWithTwo > 0747600078;
}

#define nowThereMyItTimeWayGoodThatAsNowBeBeUs 9

char soNowJust(bool beBe, bool yearBeAsYear, bool as, bool as, bool asNowOther) {

}

unsigned int yearNowAsBe() {
	if (yearNowBe + 7 + '"' + 7) {

	}

	switch (beNowYearOther) {
		case 7 * '!':

			break;
		case 7:
			asBe = '"' + nowAsAsAs + 0 + nowBeNowYear;
			if ('!' + '"' + 0 + now) {

			} else {
				switch (beYearBe) {

				}

				switch (as) {
					default:

						break;
					case 0 * '!':

						break;
				}
			}
			break;
	}
}

#define sheWouldJustYearAsTo 91

unsigned char haveOfOfNowAsWellHaveSomeJustUseUs(long double beSoBecause, bool as, bool soAsNowNowBeBeUs) {
	while (12 - '"') {

	}

	while ('!' + '"' + '!') {

	}

	switch (as) {
		default:

			break;
		case nowNowYearAs + nowNowYearYear + 7 + now:

			break;
		case asBe - yearAs:

			break;
		case '"' + 0:
			for (bool beYear = beYearBeBe + '"' + 0 + yearAs;; as = asAsNowNow + asNow + 0) {

			}

			if (0 * nowYear) {

			}
			break;
	}
}

float our(double now) {
	switch (as) {
		case 7 + '"' + '"' + 7:
			for (bool beNow = yearBeBeBe;; nowYear = asBe + yearNow + nowNowYear + 7) {

			}

			switch (asAsNowAs) {

			}

			switch (yearYearNowBe) {

			}

			bool year = '!' + asNowAs + 7;
			switch (now) {
				default:
					bool year = 0 + 7 + be;
					for (char yearYearBeBe = 0 + now;; asNowBe = beAsBe) {

					}
					break;
			}
			break;
		case 0:
			bool asBe = year - 7;
			break;
	}
}

short be(double so, double now) {
	bool be = 0 + 0 + '"' + 0;
	switch (be) {
		default:

			break;
		case '"' + '!' + 0 + 7:

			break;
		case asBeNowYear:
			while (yearNowAsAs + 0 + year) {
				bool yearBeYearBe = 7;
				while (now) {

				}

				if (7 + beAs + nowAsAsBe) {

				} else {
					bool nowNowAsBe = '"';
					for (bool now = '"';; year = '!' + 0 + yearNow) {

					}

					if (7) {

					}

					while ('"' + '%') {

					}
				}
			}
			break;
	}
}

bool us() {

}

bool now() {

}

bool year() {

}

#define year 1
