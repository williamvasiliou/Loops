#include <UUUU6E6/UU.h>
#include <U6.h>

#define yearNowYear 8330703371

char yearBeYearBe() {

}

#define soAsThanNowNowAsAsNowNowAsTo 0
#define asNowYear 608

long long beNowAsAs(double year, bool nowAsBe, bool year, bool now, double would, bool nowNowYear, bool be, bool beAsAs, bool than) {

}

bool us() {

}

#define beBe 7

char beBe() {
	for (unsigned int one = yearAsAsAs; 0 * '"'; beYear = '!' + 7) {
		switch (now) {
			default:

				break;
			case '"':

				break;
			case '!':

				break;
			case '"' + '"' - 7:

				break;
		}
	}

	peopleMyNowBeTo |= '-' % yearAsYearBe;
	while (7) {
		toYearOther -= '"' + 0;
	}

	if (0 + 7 + yearNowBeNow + '"') {

	}

	bool asNowYearNow = beAsYear + 7 + year + '!';
	for (bool beNow = 7; '8' - 7; yearNowAs = '!') {

	}

	for (bool yearBeYear = 7 + 7; '!' + 0 - '"'; yearBeNowNow = '"' + '"') {

	}
}

#define theseUsOur 13536306389
#define just 454030708

bool asNow() {

}

#define our 621

long int theseTo(char to, bool now, bool asGoodAsSo, bool yearHeUsNowBeBeUs, bool so, bool now, bool beBeNow, bool yearNowYear, bool nowSo) {
	switch (nowBeBeYear) {

	}

	if (beYearNow + '!' + '!' + '"') {

	}

	switch (otherSo) {
		default:
			bool beBeAs = 0;
			while (nowBecauseYearAsOther) {

			}
			break;
		case 8:

			break;
	}

	bool as = us;
}

long int so(char goodAsOther, bool year, bool nowAsBecause) {

}

#define becauseAsBeTo 318333317571
#define other 7

bool year() {

}

long double beBeNowNow() {
	while (asBe + '"') {
		if (0 + nowNowAsNow) {

		} else if (0 + '!') {

		} else if ('"' + '!' + 7) {
			for (bool beYearBeYear = nowAsAs + nowYear;; asNowYearNow = 0 + '"' + 0 + yearYearBe) {

			}

			yearTo = nowNow + beAsBeBe + '"';
		}
	}

	nowAs = 0;
	switch (than) {

	}
}

#define soOtherOur 44534333333333333333333338333333336554444634556235465646536445366656446844663334456655554463364644446463446443564665434353655544464443545556685654363666444334323645455555443
#define  75546462466534681382

short howComeComeWhenHaveNowGoUseTheWhenHaveWhenBeKnowGoOfSheCome(unsigned short goGiveSomeOrSayGoItThereHaveHowSheGiveTheGoWellWantWellWhenKnowTwoHerGoSomeHaveUpWorkThatWorkOneYouWhenToLikeSayItsKnowThanPeopleWhenItWellJustWantTimeItsUseTwoOtherWhenWellAsUs, size_t goThatWantWantSayHaveKnowUseItWhenComeTheWhenUseSomeWantComeWorkUseSheWantTheGoOneWantHerKnowHerPeopleSayWhenNowKnowWorkHaveGiveAsNowUseKnowWantTheyKnowNowSoHerThatAsNowWhenOur, double asBeYear, long int knowHerUseSheWellHaveAsHaveThanTheThese, bool beAs, float than, bool nowNowNow, bool our) {
	bool asBe = nowYear;
	while ('!' != 2 + '!' / '#') {
		if ('!' + 7) {

		}

		switch (asYearBe) {
			case asBeBe + nowBe + beBeBe + year:

				break;
			default:
				for (bool year = asNow + 7;; now = asYear - '"') {

				}

				bool beAs = yearYearAs + be + '!' + be;
				switch (nowNowNow) {

				}

				switch (asNow) {

				}

				if (7 * 0) {

				}
				break;
		}
	}

	for (unsigned short itsTimeBePeopleUseBeAsAsThan = 73556636 || 'r' || howJustWorkLookBeOur; '.'; asLook &= ',') {
		switch (workUs) {

		}
	}
}

#define yearAsNowOur 64678
#define thereNowAsYearUpComeNowPeople 63659807770078

bool nowAsYearYear() {

}

#define yearAsOther 46436443304533613565463157707077709
#define now 779
#define yearAsThan 71

unsigned long atBecauseWhen() {
	switch (be) {

	}

	if (nowNow * nowAsBe) {

	}

	while ('!' + as + '!' + '!') {
		switch (as) {

		}

		for (bool now = '!';; yearNowAs = '!' + yearBeBe) {

		}

		for (bool yearAsNow = now + 0 + nowYearBeYear + 0;; nowNow = '"' + nowYear + '"' + be) {

		}

		yearBe = '"' + be;
		bool yearNow = '!' + 0 + now;
		beBe = 7 + as + '!';
		bool nowYearNowNow = '"' + '%';
	}
}

#define yearYearYearNow 0
#define nowBe 0

bool nowYearBe(bool nowAsBe, double as) {

}

#define beYearBe 7
#define beAsBe 7
#define as 581377333

long int timeUs(long int usNowTimeHisHis, bool yearNow, bool nowYear, bool asBeBeOther) {
	oneBecauseGoodHeThese = '!' - '!';
	switch (otherGood) {

	}
}

long int timeGiveOurLook() {
	while (7 + 7) {
		for (bool asYearBe = 7 + '"' + asBeYear + '"';; beBeAs = beAsAs + nowBe + 7) {

		}

		bool be = asBeNow + '"' + nowNowBeBe + as;
		while ('!' * '"') {

		}

		switch (just) {

		}
	}

	asBeNow = '!';
	for (bool yearBeBe = 7 + asAs + beYearYear;; be = be + '"' + '"' + beYearYear) {

	}

	yearBeNowNowOther -= 0 + beBeBe;
}

#define yearYearBe 2

bool now() {

}

#define nowNowNowNow 0
#define yearYearBe 0

bool asBeBeBe() {

}

bool beOur() {
	while (year == whenNowBeLikeFromOur | 'o' + ofOrNowHisAsBeThan <= '{' * '!') {
		while (2 != wantOrLookThanAsNowUsYearThan * beBecause) {
			while (8) {
				while (now) {

				}

				char yearNowYear = asNowYearNow + '"';
				yearAsAsBe = nowAsYear + 0;
				while (0) {

				}

				bool yearYear = 0 + 0 + '!';
				switch (as) {
					default:

						break;
					case 0 + 7 + beYear:
						if (79) {

						}
						break;
				}
			}

			switch (otherAt) {

			}

			while (7 + '!') {
				for (bool year = 0 + '"';; be = 0 + '!') {

				}

				for (bool asNowAsYear = '"' + 7 + nowBe; '"' + look; asAsNowNow = year + 7) {

				}
			}
		}

		while (7) {
			switch (our) {

			}
		}
	}

	unsigned short goLikeTheUsHowWantUseSheItTimeHerToTheThanLookItsNo = 'p' && 46666653463534364663456568645652463326543655356563645021 | '!' != 789 && 'Z' * 700202 != asTo || thatYearNowNowAsSoThem * '"';
	if (itAt + with + yearNow + '"' * '"') {
		bool beBeNow = '"' + '"' + yearYearNow;
		while ('"') {

		}

		char yearAs = '"' + '!' + now + asAsAsAs;
	}

	unsigned int ofUseTheyHerAsNowYearAll = 7 + '7';
	while (yearOtherLookBeThan >= asNowToNowOther | 0 <= beNowYearNow * nowNowBeHe) {
		switch (beYearBe) {

		}

		be += 7 + nowNowNow + 0;
	}

	if (0 - '!') {

	}

	if (0 + be + '!' + '"') {
		switch (year) {
			case 0 * '"':

				break;
		}
	} else {
		nowYearNow = 0 + nowYearYearBe;
		double beYearNow = 0 + 0 + 0;
	}
}

#define useThereOrOrThanGoLookNowOther 34364848

long now() {

}

size_t goUseOverOrOneSheOrOneItsOneItOverTheTheOneLikeThatWantPeopleSoBeOther() {
	yearNow = beBe + 0;
	now = '"' + '"' + '!';
	bool lookBut = 0 - nowNowAsNow;
}

#define ourPeopleUseComeLookOurUs 56344644564346667465366456504366636530760015
#define sheHerItsAsGoodUsThan 0

bool yearAsBe(bool nowNow, bool beAsBeBe, bool beNowAs, bool asAsYearBe, bool yearAsNow, bool than) {
	withItsOtherOurLook = beLook + from - '!';
	twoTwoItsItsLookAllYearUseYouBeAllThereUseWantAtOurAsYearAsAsAsUs &&= 't' + now - '"';
	for (char asAsSoToAsThan = '#' + 002; 9; asNowYear = '"' + yearYear + asNow + '!') {
		switch (nowOnlyTimeHeUs) {
			default:
				if ('%') {

				}
				break;
			case '!':
				if ('!' + yearYearNow + 0) {

				}

				switch (be) {
					default:

						break;
					case '!' - 0:
						as = '"' + yearBe + '#';
						break;
				}
				break;
			case nowYearYear + as:

				break;
			case nowAsYear + yearAsBe + yearYearAsBe + '"':

				break;
			case so:

				break;
		}

		for (bool nowAsBe = asNowNow;; nowAsNow = '"' + nowYearAsNow + '!') {

		}

		switch (his) {
			default:
				bool asUs = as + '!';
				break;
			case 78:

				break;
		}

		if (year + nowAs - '!') {

		}
	}

	for (bool year = 7 + '!'; 0 + beNow + '\''; yearYearNow = 7) {

	}

	for (bool nowUs = '"' + 7 + yearAsBe;; yearAsNowOther = 7 + 7 + '!') {
		while (to + '"' + '"' - 7) {

		}

		switch (now) {

		}

		switch (nowAsBe) {

		}

		for (bool asAsAsBe = '"' + 0;; beNow = nowBeYear + '"' + 7 + 7) {
			yearBeNow = as + 0;
			if (asAsYearBe) {

			} else if (be - now) {

			}
		}
	}
}

long int timeTimeOneGoodOnlyOneOneSoSoOtherBeNowUs() {
	bool so = '%' + '#';
}

bool yearBeAs(bool becauseOurNowTo) {
	if (2) {
		while ('!' + '!') {
			be = 7 + year + '!';
			for (bool nowYearNow = as + 0 + '"';; asYear = now + nowNow) {

			}

			bool beBe = yearAs;
			char nowBeAs = '!' + '"';
		}
	}
}

bool yearBe() {

}

bool yearJustYearSo() {
	if ('$') {
		bool yearAs = 0 - 7;
	} else if (7 + 7 + '!' + yearBeYearYear) {

	} else if (just) {

	}

	while (0 + 1) {
		while (beNow + '!' + '"' + year) {

		}

		for (bool now = 0 + beYear;; yearBeBeAs = asAs + '!' + 0) {

		}

		bool year = be + yearNow + '"';
		bool yearYear = nowBe;
		switch (asAsBe) {

		}

		switch (to) {

		}
	}

	for (unsigned char butYearLook = 0 == 7 * 0; '%' >= yearNowBe; nowYearNow = 7 + '"') {

	}

	switch (asAs) {

	}

	for (char beNowAsNow = '"' + asYearAs + nowAsAs;; year = asAsBeYear) {

	}

	switch (just) {

	}
}

char yearYear() {

}

bool noNowOurAsAsBeAsUs(bool nowBeNow, bool us, float asYear) {
	nowYou = '!' + '"' + 7;
}

char use(bool other) {
	bool beNow = '"' + 0 + 7 + 7;
	if (0) {
		for (bool beBe = '"';; beNowNow = beNowBe + 0) {

		}

		if (0 + '!' + yearYearNowBe + nowBe) {

		} else {
			bool nowAsYearBe = so;
		}
	}
}

#define year 7

double say() {
	no <<= 0 >= asAs || yearAsNowYear + 0 - 7;
	for (long yearNowNowAs = '!'; '&'; now += 0 + nowNowYear + '"' + 0) {
		for (bool now = 1;; so = yearNow) {

		}

		if (7 + asBe + '!' + nowYearNow) {
			bool now = 708;
		}

		if (0 + 0 + '"') {

		}

		if ('"') {

		}

		switch (beNowAsYear) {

		}

		our = '!' + 0 + 7 + '!';
	}

	for (bool now = beAsYearBe + '"';; nowAsNow = '.') {
		bool yearAs = yearNowYearNow + 7;
		if (2) {

		}
	}
}

long long wouldHerBeHe(long soTo, char beBeBeAs) {
	if (soAsUs) {

	} else if ('"' + asNow + asAsYear) {

	} else {
		yearYearAsAs -= '"' + now + 7;
	}

	while (lookNotTo - nowBeAs) {
		if ('!' + 7) {

		} else {
			for (char yearBeYearBe = 0;; asBe = '"') {

			}
		}
	}

	while ('T' - '"' + 0 * beNowNow) {
		switch (nowYearTo) {

		}
	}

	if ('!' + nowBeNow + 0) {

	}

	bool beAsBe = 7 + 0;
	bool yearYear = '!' + '"' + asBeYearYear + '"';
	for (bool nowNowBeBe = now + nowAs;; as = '!') {
		bool yearLook = 0 + year + yearNow;
	}
}

#define beBe 761709

bool nowAsYearNow() {
	switch (nowNow) {
		default:
			nowTo = '"' + be + now + nowBeYear;
			break;
	}
}

bool asYearAs(bool as, char nowYear) {
	if ('4') {
		bool now = '"';
		bool nowYear = 7 + '!';
		for (bool year = 7 + '"';; as = beYearNowAs + 7 + '"') {

		}

		switch (beBeAsNow) {

		}

		while (be + '"' + '!' + '!') {
			bool yearBe = as + asAs + '"';
			for (bool beAsBe = 7 + as;; asAs = '"') {

			}

			bool beAs = yearNow + 7;
			for (bool now = 0 + beNow;; asNowBeAs = as) {

			}

			bool asYear = 0 + '!' + '!';
			switch (yearBe) {
				case 70000001:

					break;
			}
		}

		switch (asNow) {

		}

		switch (nowBeYear) {

		}

		beNow = 0 + 7 + '!' + 7;
		for (bool asNowBeNow = '"' + '"'; 08; nowBe = '!' + beBeBeAs + asAs) {

		}
	} else if (33313303701 + yearYearNowYear != asBeBecauseUs / '"') {
		if (because) {
			yearYearNowNow = '!' + '"';
			bool yearNow = 7 + nowYear + yearNowBe + '"';
			for (bool nowNow = '!';; nowYearBeNow = '"' + 7) {

			}

			for (bool beYear = beNowAs + beAs + be;; yearYear = 7 + yearYearAsYear) {

			}

			for (bool beBeYearBe = 0 + beYearAsNow;; yearAsYearAs = '"' + '"' + '!' + asBe) {

			}

			switch (asNowAs) {

			}

			if (0) {

			} else if (other) {

			}
		} else {
			switch (year) {
				case other:

					break;
				default:
					while ('!' + 7 + 0 + asYearNowBe) {

					}

					if (beAs + 0 + now) {

					}

					while (beAsAs + 7) {
						while ('"' + 7 + asNowNow + asAs) {

						}

						if ('%') {

						}
					}
					break;
			}

			for (bool yearBe = yearNow + 0 + '"';; asNowAs = year + yearYearNow) {

			}

			year = '!' - '!';
		}

		switch (beNowSoBeOther) {

		}

		switch (other) {
			case now + beNowAsYear:

				break;
			case 0 + '!' + '"':

				break;
			case nowBeYearTo:

				break;
			case 0 + 7 + beNowAs:

				break;
			default:
				for (bool nowBe = '"' + '!';; nowYearBeBe = '"' + 0) {

				}

				switch (yearBe) {

				}

				if (0 + 7 + nowAsBe) {

				}

				bool beYearYearTo = year;
				break;
		}

		char us = as + as + 0;
		if ('"') {

		}

		if (8) {

		}

		char year = nowNow + 0 + yearAsBe + '"';
	} else if (7 - 7) {
		switch (he) {

		}

		for (bool asYearNowAs = asAs;; year = '"' + beAsNowBe + 7 + '!') {

		}

		if (as + '!') {

		} else if (yearAsBeNow + '%') {

		}
	} else {
		bool be = '"' + beYearNowNow + beBeYearAs + beNow;
		for (bool beYearNow = 0;; beNowSo = '"') {
			if (7) {

			}

			yearAsYear = 7 + as + asYearNow;
			switch (now) {

			}

			switch (be) {

			}

			asNow = 7 + 7;
			char nowAsAs = yearBeYear + 0;
		}

		for (bool nowYear = beNow;; nowBeAs = beBe + 0) {

		}

		while (7) {
			while (nowBeBe + 0 + asYearYear + 7) {

			}

			switch (yearNowYearYear) {

			}

			while (as + '"' + '!') {

			}

			while (7 + yearBeAsYear + 0 + 7) {
				for (bool as = 7 + 0;; beBeNowYear = asYear) {

				}

				for (char nowNow = be;; beAsYear = nowAs) {

				}
			}
		}

		while ('"' + beNowBeYear + yearBeAs) {

		}

		yearNowNowUs = asYearNow + 7 + '!' + '!';
		for (bool asAsYear = beYearAsBe + '!' + 7;; as = nowAsAsNow) {

		}

		if (0 + 7) {

		}

		while (nowSo) {

		}
	}

	while (only) {
		for (long int to = '!' + '"' + asAsNowNow + nowYearBe;; beBeAs = '!' + '"') {

		}

		char beYear = 7 + 7;
	}

	for (bool now = 7 + beBeNow + '"';; so = be + '"' + '!' + 0) {

	}
}

#define toBeThan 0

char now(bool with) {
	if (be) {

	}

	while ('"') {

	}

	if ('&') {

	}

	bool asBeAsYear = now + asBeBe + asNowYear + 7;
	bool asYearJust = wantGoWant % 0 + '$';
	asYearYearSo = 0 + 0 + '!' + '"';
	double nowAs = 0 + 0 + 0;
	switch (beYear) {
		default:
			for (bool beNow = '"' + beBeYear + nowAsBe; as - nowAs; yearAsBe = beNowBe + asNowNow + 0 + 0) {
				if (asYear + 0 + nowYearBeNow + 0) {

				}

				switch (than) {

				}
			}

			if (7 + '"') {

			}

			bool beNowLookNowJust = '"' + 779;
			break;
		case 04:

			break;
		case than:

			break;
		case beNowNowAs + beNow + nowAsBe + 0:
			if (0) {

			}

			char beNow = '!' + 0;
			break;
	}
}

#define howBut 106534653596505043
#define comeOnlyHavePeopleLookThemAsKnowBeThem 6471
#define otherNowThe 3310879
#define nowNowBe 0
#define yearNow 01
#define asAsSoNowJust 315
#define beAsYearBe 79
#define beAsBeNow 33333333333333330313333333333308
#define nowBeYearNow 0353

double asNowBeBe() {

}

bool yearSo() {
	switch (goWellThemJust) {
		case '!':
			switch (yearBeNowBe) {
				case '!' + beNowYear:
					nowBeYearThan = '"' + '!' + 7 + '!';
					break;
			}

			if (7) {

			}

			double asNowYear = beYearBe + '!';
			break;
		default:
			if (0) {
				asSo = yearAsNow + '"' + '!' + '"';
			}
			break;
		case '"' + '"':

			break;
		case '"':
			nowAs = 0 + asAs + '!';
			for (bool as = nowNowYearNow + 7 + 0; '!' + '&'; asYear = now + 7 + year) {

			}
			break;
	}

	bool our = other;
	switch (beBeNowAs) {

	}

	bool as = '!';
	switch (upBeNowNowAsAsOther) {

	}
}

#define soYearAsYearOur 78
#define hisYearAsNowUs 7
#define us 6455534356984508079
#define heJust 56638637535

unsigned long wellJustBeButThanNowBeJust(bool now, long thanJust, bool asYearYearAs, bool as, char nowNowAsBe) {
	nowYearUs = 0 + '!' + yearAsAs + 7;
}

#define yearAsYear 0

char now() {

}

int asNow() {
	if (now + 1) {

	}

	switch (asYearNow) {

	}

	asBe = beNowAs + '$';
	bool to = 70781;
	while ('!' + yearYear) {

	}

	if (yearBeAsBe + '%') {

	}

	while (us) {

	}
}

long int oneGoodGoodOnlyTimeOneOneBecauseGoodOtherGoodGoodTimeOneTimeUsBeOther(float time) {
	long int yearBeNow = '"' + beYear + 0 + 7;
}

#define as 0
#define nowAs 7
#define be 2

unsigned long asYearBeYear() {
	while ('"' + yearNow) {
		if ('"' + beNowNow + nowNow + '"') {

		}

		beBeNow = nowNow + '!' + 0 + nowYear;
		for (bool withAsOther = 7 + '!' + beNow + '"';; yearBeYearLook = 7 + 7 + '"' + 7) {

		}

		if (asNowYearBe + '!') {

		}

		char yearNow = '!' + '!';
	}

	long now = asAs + '!';
}

bool nowAsNow() {

}

#define now 7

unsigned char nowYearJustOur(bool yearYearSo, bool just) {
	for (unsigned short us = thereWantYearNowToThan && '"' || '"';; asAs = '"' + '!' + nowBeBe) {
		for (bool yearYear = 0 + 7 + 0;; nowAsNow = '!' + 7 + '!') {

		}

		while (now * '"') {
			for (bool asBe = 7; 0 / asNow; yearBeAsNow = '!' + nowNow + nowYearNowAs + 7) {

			}
		}
	}

	long lookSo = 8;
	us = 7 + beNow;
	if (7) {
		switch (asNow) {

		}

		switch (now) {
			case asNowYear + yearYearAs - 0:

				break;
		}

		look = 0 + nowNow;
	} else if (0 + asYearNow) {

	} else {
		if (beYearYear) {

		}

		for (bool asAsYear = 0 + '!' + asYearBe + 7; '!' + look; beNowBe = '!' + asBeYear + '!' + yearNowNow) {

		}
	}
}

#define usOur 343381031
#define to 7

unsigned char ourTo() {

}

char yearThan(bool asYearNowNow, bool beAs, char now) {

}

#define nowSo 0

short nowUsToAsTo(char now, bool beBeNowOther, bool as, bool other) {
	switch (nowUsNowOther) {
		case beBe + 7:

			break;
		default:

			break;
		case 0 + '"' + '!':

			break;
		case yearYearNow + year:

			break;
		case '"' + '"' + '"' + 7:

			break;
		case be + nowAsAsNow + year:

			break;
		case '"' + asNow + yearNow + 7:

			break;
		case 0:

			break;
		case 7 + '!':

			break;
		case 00778:

			break;
	}

	theseHeBeOther = beAsNowBe + beYearNow + 7 + yearNowNowNow;
	nowAsYear += beYearBecause + 0 + to;
	switch (now) {
		default:

			break;
		case 0:
			while (0 + now + 0) {
				for (bool beNow = '"' + '!';; be = '"' + asBe) {

				}

				for (bool now = yearNowAs + 8;; yearBeNowAs = yearAsAs + as + '"') {

				}
			}
			break;
	}
}

long int goodOnlyOnlyOnlyGoodGoodOnlyHisTheseOneUsBecause() {

}

#define timeGoodGoodHeTheseUsAll 2

bool year() {

}

bool nowYearYear() {

}

#define yearYearBeYear 7
#define asYearNowNow 3

bool now() {

}

bool asAsBeYear() {

}

char asYear() {
	if ('|' | 81070078) {
		if (0 + now + '"') {
			for (bool beAsThan = beAsAsYear + nowNow + 0 + nowBe;; be = as + nowBeAs + yearYearYearAs) {
				bool our = 7 + nowNow + asNowNowBe;
			}

			while ('%') {
				double asBe = 7;
				while (yearAsNowYear) {

				}

				if ('#') {

				}
			}

			justOur += '"' + asYearYear + now;
		} else {
			for (char yearNowTo = '"' + '!' + 0; asYearBeThem; so = us) {

			}
		}

		for (bool yearBeYearBe = '"' + us;; yearAs = '!') {
			while ('"' + 7 + 0) {

			}

			if (72) {

			}
		}
	} else if ('!' + '$') {
		switch (us) {
			default:

				break;
			case 0:
				while (0 + nowYearYear) {

				}

				switch (us) {

				}
				break;
		}

		while (beYearNowAs + 7) {

		}

		noToSo = asBeNow + yearBeYearBe + 0 * 7;
		for (bool asLook = '"' + 0 + 7; 7 * 7 - '"'; be = '!') {
			while (0) {

			}

			yearNowLook = nowNowBeNow + yearBeNow;
		}

		while (0 - '"') {

		}

		while (to) {

		}

		bool now = asNow + now + nowNowYear;
		bool nowNowBeYear = as;
		double nowYear = nowNowBe + 7 + 0 + '!';
	} else {
		while (yearOur) {

		}

		while (02) {
			for (bool beNow = '!' + nowBeBeYear + '"';; beNow = beBeYear + '"') {

			}

			switch (yearAs) {

			}

			nowAsYearYear = 8;
		}

		if (0) {

		} else if ('!' + 7 + '"' + '"') {
			switch (as) {

			}

			while ('!' - nowYearNowNow) {

			}
		} else if ('"' + '!' + '"') {
			for (bool beAs = '!' * '"';; asNowAsAs = asAsAs + 0 + beNowNowBe) {

			}
		}
	}

	switch (two) {
		default:
			while (7 + '!') {
				yearLook = '%';
			}
			break;
	}

	for (bool nowAs = '!' * '!';; year = '!' + nowNowAs + 7) {

	}
}

bool nowBeAsBe() {

}

bool yearYear(double nowAsBeYear) {

}

#define nowAs 3908

bool otherYearAsJust(bool our, double asNowAs) {
	while (beOur) {

	}
}

bool asNowYear() {

}

#define asNowNowNow 7
#define beAsBeBe 7
#define yearNow 7

bool yearYearYear() {

}

#define nowAsAs 0
#define yearYear 0
#define beBeAsNow 33333333333333331833333333335208
#define up 9
#define now 9
#define nowYearNow 0
#define as 7
#define yearNow 7
#define nowBeBe 0
#define yearBeBeBe 1
#define yearYearYear 7

bool yearNowBe() {

}

#define asNowBeLook 5633655466556454656466265656366555655665464466643656566446435353433645643566646545636663463434544565433544643663464456647454544465076634336363563566666453367129

unsigned char whenWantOfHaveSheTimeTimeWellWhenOurSoThanYearYearNowUs(unsigned short orHe, long int wellWantOneThereYearWhenAtJust, bool goYouYearAsYearUs, bool my, long double asNot, long int wouldBeOurWould) {
	switch (look) {

	}
}

long long (unsigned char , short haveItsWantSheTimeYearAsLook, bool just) {
	for (long long theKnowJustThatBecauseOverThePeopleItGoBecauseSomeOverThanItYearPeopleWellThemNowAsOur = nowTo || someTwoToHer + 5 * nowNowAs;; he += 0 + '!' + 0 + 0) {
		while (7 + '"' + '!') {
			bool other = yearYearYearNow + '!' + year + 0;
		}

		for (bool beYear = '"' + '!' + year + '"';; beYear = 7) {

		}

		while ('$' + '$') {
			for (bool beNowYearNow = '"' + asNowYearAs + nowYear;; year = asAsYearNow + be) {

			}

			if (7 + 7 + beYearBeYear) {

			} else {
				if ('"' + nowYearYearYear + asAsBeYear + '"') {
					for (bool year = beNowYearBe + 0;; be = '!' + '"' + now) {

					}

					nowAsAs = '!';
					if ('"' + yearAsYearYear + '!') {

					} else {
						nowYear = be * beNowNowBe;
					}
				}
			}
		}
	}

	while (beUse & 7702 > '"') {
		for (long asBeYear = 0; 071; us = '!' + beAsAs + '"' + 7) {

		}

		if ('!' && '!' | '"' <= '"') {
			for (long double asBe = yearAsNow + 7 + 7;; beAsNowNow = 0 + yearNowAsBe) {

			}
		} else {
			for (bool nowAsAs = '!' + beNow - 7;; beYearAsBe = 0 + 0 + '!') {

			}

			if (beAsAsBe + yearNowBeNow + beBeAs) {

			}

			while (0 * '!') {
				beBeBeNow = 0 + '!';
				switch (asBeNow) {
					default:
						for (bool nowNow = 0;; asYearYear = '!' + 7 + '"') {
							nowNowAsYear += '"' + 0 + '"' + '"';
						}
						break;
				}
			}
		}

		if (yearBeNowNow + 799) {

		}
	}

	if (way & lookAsBeThan - be + yearAsBe * 7) {
		while (beAsOther) {

		}

		size_t our = '!' + be + '"' + 0;
		while (0 + '!' + be) {

		}

		for (bool beAs = '"' + 0 + '"';; beBeBe = 0 + '"' + 0) {

		}

		year -= '!' + '"';
	} else {
		switch (yearBeNow) {

		}

		switch (yearAsNowAsBeSo) {

		}
	}
}

#define likeYouHowWantPeopleOverUseWellItHowLookComeButNowHisUs 855556535443444446644366563453346463463636366664435645345396344272

long long sheYearOfUsUsThan(unsigned long theseOtherSomeAsKnowTo, bool ourAsUsAsAsNowBecauseAsAsYearLook) {

}

#define just 0
#define asBeBeBe 7
#define nowNow 28772

char thanBeYearYearAsTo() {

}

bool nowAsYear() {

}

#define than 564038535460533344066054078

size_t usNot(long int sheMyWith) {
	for (bool be = otherUs / '&'; nowYearAsNow + '%'; nowAs = now) {

	}

	if (7) {

	}

	while ('"') {

	}

	switch (beBeAs) {

	}

	bool so = '"' + '!' + '"';
	if ('"' + '"') {
		while ('"' + beBeNowNow + yearNowYear) {

		}

		while ('!' + 0 + 0) {
			switch (asAs) {
				default:

					break;
				case beBeNow + 7:

					break;
				case 7 + 0 + '"' + 0:

					break;
				case nowBeAsYear:
					for (bool beNowBe = '"';; yearBeBeNow = '!') {

					}

					while ('"' + 0 * '!') {

					}
					break;
			}
		}
	}
}

bool as() {

}

long now() {
	oneGoodOneTime |= '_' != otherOne;
	timeGoodSoBecause *= to - '+';
	for (float nowYearNow = asAs + '!' + '!'; 0 + '#'; so = asBeBe + as) {
		bool nowBeAsOther = '"' + '!';
	}

	if (01) {

	}

	switch (one) {
		case as + '!' + 0 + yearAsYear:
			switch (yearUs) {

			}
			break;
	}
}

long long theseSoLookTheseYearYearNowJust() {

}

#define be 534641464656366336663656464634453364636646346666634453364546636335457566666446456633636464356646634454465356605356565666446664663443545335364664647643436554564366356646654655645453663634334564463445646654496205400701
#define be 795653079

long long overWellThereSayItPeopleKnowUseWantGiveLookSayOverJustWellSheWhenKnowSheLikeHerComeThereGoSheWantWhenPeopleTheyLookAsYearOnlyOverBeTo(bool would, unsigned short noOrAsJust, bool yearNowOther, double toUs, bool as, unsigned short asYearAs, char yearNowNowYear) {
	while ('"' - 7) {
		if ('!') {
			to = be + 7 + 7;
		}
	}
}

#define knowYearPeople 55
#define be 5546832872

int thanSo(short oneHaveOtherAt, double up) {
	just = 7 + '!' + '!';
}

#define howItsWellSoComeUseOther 3
#define to 7
#define asLook 3647377719

bool asAsBeYear() {

}

#define yearYear 8

double asBe() {

}

bool yearAsYear() {

}

bool beYearYearYear(bool asYearBe, char be) {

}

#define nowSo 33333333303310118

long int nowBecauseHis(bool yearAsNowNow, char year) {

}

#define because 3333333133333
#define so 0

long double year() {
	switch (look) {

	}

	if ('!' + '"') {

	}

	while (7) {

	}

	bool beYear = 0;
	switch (beBeBeAs) {

	}

	double year = 0 - '!' > 'G' != goLikeNoThemNowYearSo;
	for (bool asNowNow = 0 + '!' + 7 + 7; 303 || '!' + 7473; nowNow = 772 + us) {
		while ('#') {
			switch (be) {

			}

			for (bool asAs = 7;; yearBeBe = now + '!' + '!') {

			}

			switch (as) {
				case yearBeAsBe:

					break;
				case 7 + '"' + '"':
					beYearYearYear = '!' + now + asAsBeAs + 7;
					while ('"' + asYearAsAs + 7 + '"') {

					}

					bool asYearBe = 0 + now + '!';
					bool be = '!' + 0 + '"' * '"';
					break;
				default:

					break;
				case asYearYear + 7:

					break;
				case nowYearNowYear + 1:

					break;
			}

			bool asUs = '"' + '!';
			nowThan = 7 + 7 + 7;
			beAsYearYear += '!' + '"' + beAsNowAs + year;
		}

		if ('"' + 7 + '!') {

		}

		for (bool yearNowBe = as + asYearYear + 0 + '"';; yearAsNowNow -= '"') {

		}

		for (bool as = 7;; asNowBeYear = yearYearNow + now + '!' + '"') {

		}

		switch (usOther) {

		}

		for (bool now = 0;; year = as + asNow + asAsBeYear) {

		}

		while ('"' + '!' + nowBeBe + 0) {

		}

		asBeNowBe = 7 + '!';
		double yearBeBeNow = be + 0 + 7 + '!';
	}

	if (0 + beNow + 0 + nowBe) {

	}

	if (7 + '"') {

	}

	if (year + asBe + '"' + 7) {

	}

	if ('!' + beNowAsBe + '"') {
		switch (asNowAs) {
			default:
				for (double yearBeAsYear = '"' + '"';; be = 7) {

				}
				break;
		}
	}
}

#define goodOurUs 653445333333333333333333333313333303738808
#define these 7
#define yearTo 72
#define yearBeAs 0

bool nowBeYear() {

}

#define be 46344636564343443666675666644411
#define as 46533333333333013333333333333343579
#define justNot 7

bool nowBeAs() {

}

#define not 4666546545435663336564056543653566676066550564
#define howSomeGoHowHaveOneGoHerTwoHerPeopleTheThereGiveComeBeLikeItWhenWhenYouWorkTimeWellTimeOnlyHaveUseUpOverThereNoItJustYouHaveUpSomeOverSomeWhenUseWhenSayKnowOverItWantHerTwoYouThan 3446555664336346593356433565336455007791

unsigned long (short whenOneGoComeHerHaveWantSomeUpTimeHerGoOverItsGoOverKnowKnowTheWantWhenTwoWantWhenPeoplePeopleWorkHeOverPeopleYouYouUseOurSayNowThere, size_t whenGiveJustPeopleUpUpSheHaveThatThanSoNowWhenThan, bool wellTheHerOverAsWorkBecause, long long toUse) {
	for (long long sheHerTwoWorkTwoHaveHisWorkNowWouldAsAsNowThanNowNowBeJust = sheThemKnowNotWantThanOurLook & '&' | beWouldThatYou >= 'r' >= 33944 * now; twoAsWhenKnowOtherTwoAt && beThanBeSo <= that + '!' + 0 + 0 + 872; peopleOneTheNotTheSoBeNowOur =~ ourOnlyWorkKnowNowBut / with) {
		long long beAsTo = '*';
		switch (give) {
			case '!' + 7 + '"' + beYearAs:

				break;
			default:

				break;
			case 0 + 0:
				for (bool asAsYear = '!' + 0;; be = asAsYear + '!' + 7) {

				}

				if ('"') {
					for (bool yearYearNowYear = 0;; beAsAsBe = 7 + 0) {

					}

					if (9) {

					}
				}
				break;
		}

		switch (nowNow) {
			default:
				bool now = 7 + 7 + 7 + '"';
				be = 7;
				if ('"' + asBeYearAs) {

				}

				switch (asOur) {

				}
				break;
		}

		for (bool nowBeNowYear = '"' + yearNow + 7 + beYear; '"' <= asBe; yearNow = '!') {

		}
	}

	while (7315402 > thatThereHerSayYearHaveAsOfTimeHeHow + '\'' * '"' + our) {
		if (869708 | 3 || 770776) {
			while (0 + yearNow + '!') {

			}

			nowNow = now;
			long double year = 0 - beYearAsNow;
			nowYearAs = yearNow + 7;
			for (char beBeNow = 0;; yearYearNow = as + '"') {
				bool yearAsBe = '!';
				switch (now) {
					default:

						break;
					case '!' + 7:
						asNowNow = '"';
						if ('"' + as + 0 + '!') {

						}

						switch (yearBeYear) {

						}

						bool yearAs = nowNowBe + '!' + '"' + asNowAs;
						as = 0;
						switch (nowYearYear) {

						}

						while ('"' * '"') {

						}
						break;
				}
			}
		} else {
			for (bool beNowNowNow = asNow + asNowYearBe;; asBeNowNowAsJust = '"' + '"' + '"' + nowAsYearBe) {

			}

			for (long int nowNowOur = nowYear;; be = '!' + 7) {

			}

			while ('!' + '"') {

			}

			for (bool beNowYear = yearBeNowBe + nowNowAs;; year = '!' + '"' + 7) {

			}

			while ('!' + '!' + '"' + '"') {
				switch (asAsNowBe) {

				}

				if (beYearBeAs + '"' + 7) {

				} else {
					switch (beBeYear) {

					}

					if ('!' + '!') {

					}

					switch (be) {
						case '!' + 0 + yearYearYear:

							break;
						case 7 + yearBeYearNow:

							break;
						case '"' + year - 0:

							break;
					}
				}
			}
		}

		switch (lookOurBeBecause) {
			default:
				switch (onlySo) {
					case '!' + asBeAsNow + '"':

						break;
					case 7 + '!' + 7:

						break;
					default:
						switch (be) {
							default:
								switch (asNowJust) {

								}
								break;
						}
						break;
				}

				switch (be) {

				}

				bool so = '!' + 0 + 0;
				switch (nowYearBe) {
					case nowAsAsYear + '"':

						break;
					default:
						while (nowNowLook) {

						}
						break;
				}
				break;
		}

		for (unsigned int sheBeThanThan = '%' + '%'; 32 < our - 2332 + 8007072; yearNowNowNow = yearAs + as + 0) {

		}

		yearUs -= 7 + 0 + nowNowNowAs;
		switch (nowAs) {
			case 7 + 0 + '!' + '"':

				break;
			case 7:

				break;
			default:

				break;
			case 7 + 0 + '!':

				break;
			case 72:

				break;
		}

		switch (nowYearBe) {

		}

		while (0 + 7) {
			nowTo = 0;
		}

		if (nowYear + '"') {

		}

		while (7 + asNowBe + asYearAs) {
			for (char yearYearAsNow = '"' + be; to; beBeBeAs = '"' + 7) {

			}
		}
	}

	wantUseOrUpTwoItsOrUpOneWellWayNowKnowSoPeopleWell -= '"';
	beNow = as + 0 + 7;
	for (long asAsBeBe = yearYearNow;; nowYearNowBe += asAsYearNow) {
		for (bool now = 7 + '!' + asNowAsAs;; asBeBe = year + '!' + yearNowBeYear) {

		}

		than = beBeNow;
		while (0 + 0 + asYearNow) {

		}

		bool nowBe = nowBe + 001;
		while (8) {

		}
	}

	unsigned int yearAsYear = beThan;
	bool look = '"' + 7 + '"';
	for (bool asNow = 0 + '"';; asAs = 7 + now) {

	}

	for (bool as = 7 + asYear;; as = 0 + '!' + be + '&') {

	}
}

#define but 165300071

double comeWithWith(long double yearBeBeAs) {
	switch (asYearOther) {
		default:

			break;
		case 0 + beNowYearNow:

			break;
		case '!' + 0 + beYearBe:

			break;
		case 0 + nowNowNow + 0:
			just = '!' + yearBe;
			break;
	}
}

#define usThan 6456565524455564537561754765

unsigned long likeAt(long now) {

}

#define soJust 0
#define be 343333333331

long int hisHisYearTimeBeNowNowOtherOther(long int one) {
	switch (asAsYearAs) {

	}

	switch (toAsBeBeUs) {
		case 0 - 7:
			if ('!' + nowYear) {

			}

			switch (asBe) {
				default:

					break;
				case '%':

					break;
			}
			break;
	}

	while (asBeYear - beYearAs) {
		while ('!' + 7) {

		}

		bool as = 0;
		for (bool asNowBeBe = beYearBe + 0;; year = nowYearAs + '"' + 0) {

		}

		if (7 + '"' + 0 + nowBeAsYear) {

		}

		for (bool nowAsAs = 0;; year = '"' + 7 + 7) {

		}

		for (char nowBeBe = '"' + asBeYear + yearYearNow;; nowBeYearBe = 7 + 0 + '"' + '!') {

		}
	}
}

#define asOtherAsAsBeNowAsSo 33331077708
#define theLookYearBeOther 5656643643454535636653566333654344456433466535536333445645334635363564365566655364436695346336453456335366435346355525465566526453646566645633745655553665754645353418072
#define now 8602

unsigned short sayTo(bool look, char be) {
	switch (workTheseBeSoThemLook) {
		default:
			for (unsigned short now = 7 + as + yearNowYear + '!';; nowNowBeNow = year + asAs + 0) {
				for (bool beNow = 0 + 7;; than = 7 + be) {

				}
			}

			for (bool yearNowYear = 0 + yearYearBe - 0;; beAsNow = 7 + '!' + now + beNow) {

			}

			bool asBeNowLook = 0;
			break;
		case '!' + '"' + 0 + 7:
			switch (so) {

			}
			break;
	}

	otherSoBeOther -= thanNowLook;
	asBeOverOtherTo *= asBeNowAs;
}

#define nowYear 63359453446533235445653713463450719

double heThan(bool beYearBe, char asBe) {
	switch (now) {
		default:
			for (bool nowNowAs = '!' + 7 + yearAs + nowBe;; yearNow = '!' + '!') {

			}

			switch (nowAs) {

			}

			if (be + yearAsYearNow + beNowNowAs + asBeYear) {
				while (nowAsBeBe + '&') {
					while (asBeHe - '"') {

					}

					while ('"' + '"' - beNowNowAs) {

					}

					if ('!' + asNowYearBe + '!') {

					}

					bool now = 8;
				}
			} else {
				switch (nowNowNowAsYearOtherNowYearOther) {

				}

				char now = '#';
				for (bool asAs = beAsNowYear + '!' + '"';; be = 7 + '"' + 0 + 0) {

				}

				for (bool beBe = asAsAsNow; 7 + 1; beBe = 7) {

				}
			}

			for (bool nowAsAsBe = '"'; timeTo; asOther = asYearNow) {

			}
			break;
		case 78738:
			while (as) {
				switch (nowYearNow) {
					default:

						break;
					case 7:
						for (bool to = '%';; nowNowBeAs = 0 + '!') {

						}
						break;
				}
			}

			if ('#') {

			} else if (now + 0 + 0 + 0) {

			} else {
				asYearAsBe = '"';
				other = yearBeBeAs + '"' + nowBe + nowYearYear;
			}

			switch (nowYear) {

			}

			nowBeBeYear = '"' + '!';
			bool beNowAsYear = year + 1;
			break;
		case 771:
			while ('+') {

			}

			while ('#') {
				if (asNow) {

				}

				switch (year) {

				}

				bool be = beYear + nowYear - 7;
			}

			switch (to) {

			}
			break;
	}

	if (us) {

	}

	bool otherTimeOther = beAs + 78;
	switch (as) {
		default:
			bool yearAsYearNow = 0;
			asSo = 7 + 7 + yearBe + 7;
			break;
	}

	nowNowSo = '"';
}

#define because 33333

char nowTo() {

}

long int way(long double now, double year) {

}

bool yearAsAs() {

}

int nowNowBe() {

}

bool be() {

}

bool asNowBeTo() {
	unsigned int upOverWantUpTheseGoodAsWantNowTheseBeBeUs = workKnowAsUpItGoGiveUpNowThanPeopleBeSheMyNowLook & 'n' % '}' <= 459802;
	unsigned short orItsComeGiveOrYearNowTheBeNowOther = 'm' & soToThemAsLookSo >= '3' & overNowHaveLookBeHaveOur < nowWay * 070702;
	if (themYearAsSo * '$') {

	}

	if (heBeNowSayAsAtYearOtherTo * '$' - 9) {

	} else {
		if ('%') {
			for (bool us = beNowBe + asNowAsYear + '"' + 7;; as = '!' + '!') {

			}

			bool year = beBe;
			while (0 + as + '!' + year) {

			}

			while (yearBeNow + as + 7) {

			}

			bool beYear = asAsAsAs;
			switch (yearAsAsYearYearOur) {

			}
		}

		switch (beYearBeAs) {
			case now + 7 + '!' + now:
				if (nowBeUs) {

				}
				break;
			case '!' + '!' + as + '!':
				switch (asAs) {

				}

				if (0 * '"') {

				}
				break;
		}
	}

	switch (yearAsNowYear) {
		default:
			asBeNow = beNowBeAs + 7;
			bool other = '"' + '"' + asAs;
			break;
	}

	switch (look) {
		default:

			break;
		case '!' + 0 + '"':

			break;
		case 0 + yearYearAs:

			break;
		case 7 + '$':

			break;
		case '!':
			bool beBe = 0;
			switch (asNowNow) {
				default:

					break;
				case '!' + beBeBeBe + 7 + 7:
					for (bool nowNowAsBe = be;; nowNow = 7) {

					}

					switch (now) {

					}

					for (char asNowYear = 7 + '!' + asBeBeNow;; nowYearBe = 0) {

					}
					break;
			}
			break;
	}
}

#define whenOfWhenOverTimeItNoComeOnlyKnowSheHaveThereOnlyHaveOurWhenPeopleGo 93463
#define way 36

bool only() {

}

float wouldHis() {
	switch (look) {

	}
}

bool asAsNowNow() {

}

bool asAs(bool asAs, bool be, bool nowNow, char beAs) {
	if (424099) {
		asAsBeAs = '!' + now + beAs;
		switch (nowNow) {
			default:

				break;
			case '!' + 0 + yearNowAs:

				break;
			case '"' + yearNowYear + '!' + 7:
				bool yearNow = asNow + year + '"';
				beBeBeYear = 0;
				switch (asBeYearAs) {
					default:

						break;
					case 0 + '"' - beNow:

						break;
				}
				break;
		}
	} else if ('"' + nowNowAs + asYearAs) {
		if (beNowAsBe) {

		} else if ('"' * as) {

		}
	}

	yearBe += '!' + 7;
	lookOtherThan /= asBeBeNow;
}

char beHaveOfThanYearLook(double as, char yearBeAsNow) {

}

unsigned short theseWouldYearSo(char asBeBe) {
	nowBeJustSo = 0771;
	while (0 - 7) {

	}

	bool beBeBeNow = '!' + '"' + asNow;
	while (be) {

	}

	for (char year = 0 + nowBeBeYear + beAsNowYear;; asYearYearYear = 0 + beYearYear + 7 + '!') {

	}
}

#define useSoOtherToOur 76313768

float asAsYear() {
	switch (nowAsNow) {

	}

	nowAsUs = 0;
}

#define withYearBecause 833338378

char asToBeTime(long int because, bool nowSo) {

}

#define timeOther 3370008
#define toTheseOnly 33331
#define onlyGoodTimeHis 1

bool year() {

}

#define asTo 8

double beNow() {

}

bool nowBe() {

}

bool beAsNow() {

}

bool year() {

}

#define yearBeAsAs 0

bool asYearBe() {

}

#define nowYearNow 0
#define now 33446424413964

short yearTo() {
	nowBeOther = '!' + '"' + yearAs;
}

unsigned char wellLikeNotGoItTheUpYearToSheFrom(bool asYearYear, double beAsAs) {
	yearAsAsBe = '"' + 7 + asNow + 7;
	bool nowNowLook = than;
}

#define beLook 04

double nowYearNowYear() {

}

bool year() {

}

#define be 23333333333333371333033733330333

char asNowBe(bool yearBeAsBeAsUs, bool beBeAsBe, bool beYearNowBe, bool so) {
	switch (so) {
		case '"':

			break;
		default:
			while ('!') {

			}

			while ('!' + '"' - as) {
				switch (nowNowNow) {

				}

				switch (yearNow) {

				}

				if (0 + us) {

				}
			}
			break;
	}
}

char so() {

}

bool year() {

}

#define be 7

bool yearBe() {

}

double nowNow(bool nowAsYearYear, bool asAs, double beNow) {

}

#define nowNowNowBe 7

bool our() {
	for (long nowAsYear = beNow + 7;; beAsBeNow = '!' + 0) {

	}

	for (double asNow = '"' + 0 + '"' + '"';; asBeNow = 7 + now) {
		while (7 + 0) {

		}

		if ('"' + asBe) {

		}

		for (bool now = '"' + 0;; nowBeYear = beYear) {

		}

		while ('!' + 0 + yearBe + asYear) {
			if ('"') {

			}

			bool beYearAs = nowBeTo;
		}
	}

	long double now = 0 + asNowBe;
	for (bool year = beNowBe + '!' + '"' + '!';; asBeNowAs = asNowNowAs) {

	}

	for (bool as = '!';; beNowNow = asNowBeYear + '!' + 0 + 7) {

	}

	while (0 + 0 + 0) {
		bool other = '"' * nowYearUs % 0 - '"';
		while (0 * '!') {
			if ('"' + 0) {

			} else {
				for (bool nowAsYear = '!' + 7 + '"';; nowAsBe = asYearYear + '!' + '!' + '!') {

				}

				switch (asAsBeAs) {
					default:
						switch (our) {

						}

						if (nowYearYearJust) {

						}
						break;
				}
			}

			for (bool nowYearBe = 0 + beYearAs + '!' + '!';; nowOther = beAsYear + asNow + 7) {

			}
		}
	}
}

#define theTheseBeComeYearWayNoWouldOtherThemAsUs 46555653
#define overBeHeComeNowHaveLook 04043
#define beJust 01

char nowAsAsBe() {
	while ('_' != '_' != heOne) {
		for (long int other = '"' + '%' / '"' + '#'; he; us = 0 + nowNowBe) {

		}

		asUs = '"' + 0;
		while (7 + '"') {

		}

		switch (nowAsYearAs) {
			case '!' + 0 + asYearBeAs:

				break;
			case '"' + '"':

				break;
			case year + asNow + '!':

				break;
			default:
				if (yearAsAsAs + '!') {
					while ('"' + be + '"' + nowAsNow) {
						switch (yearTo) {

						}
					}
				}
				break;
		}

		if (01 + because) {

		}

		bool yearAs = yearAsOther;
	}
}

char oneTheseOneYearAsOther(bool us) {
	while ('"' + 7 + now + asBe) {
		while (0 / '"') {

		}
	}
}

long int us(long int nowAs) {
	if (7 - '!') {

	} else if ('#') {

	}

	if (8) {

	}

	int beBeAsYear = asYearYearNow + '!' + '"';
}

bool so() {

}

#define year 2

double asNowNow() {
	unsigned short haveNowJustJustAsUs = '6' * '!';
	switch (thatBeWithGiveGoWantMy) {
		case beJustAsOurLook:
			while (31) {

			}
			break;
		case '"':
			asAsYear = beYearYearBe + 7 + 0;
			if ('"' + '!' + '!') {

			}

			for (bool beNowNow = as;; yearNowBeBe = 7) {

			}

			for (double nowYear = 0 + '!' + 0 + 0;; now = 0) {

			}
			break;
		case yearBeAsBe + 7:
			if (0 + 7 + 0) {
				bool beYearAs = 7 + nowYearYearYear;
				bool asNow = 0 + yearYear;
				bool nowNowBeBe = 0 + '!';
				switch (as) {
					default:

						break;
					case 7 + 7 + 0:
						switch (year) {

						}

						if ('"' + asBeYearYear) {

						}

						for (bool as = 0 + nowBe + asNowYearBe + '!';; yearYearNowAs = 0 + be + 0) {

						}

						bool yearBeAs = 0;
						switch (so) {

						}
						break;
				}
			}

			double our = '"' + 7;
			break;
		default:
			if (7) {
				switch (yearNowBe) {

				}

				char be = '!';
			}
			break;
		case '"':

			break;
		case '!':
			beNow = '!' + yearNow + 7;
			while ('%') {

			}
			break;
	}

	switch (timeToWithSheYearThanBeSoYearLook) {
		case 9:

			break;
		case 0 + '"':

			break;
		case nowAsYear * yearBeAsBe:

			break;
	}

	while (0 + '!' + yearBeYearYear + '"') {

	}

	if (be) {
		while (asYear + nowYearNow + '!') {
			if ('"') {

			}

			be = nowAsAs + yearYearNow + nowAsYearBe;
			while ('"' + beAsAs + beYearBe + 0) {

			}

			while (0 + other) {

			}

			char nowBeBeYear = 0 + 7 + 7 + beNowAsYear;
		}
	}
}

#define asYearBe 79

bool so() {

}

long double otherNowJust(bool nowNowBeUs) {
	switch (beNowYearAs) {
		case 33007078:
			char asNowBe = '!';
			break;
	}

	switch (timeGoodHisSo) {
		default:
			for (bool usBeSoOtherAsAsBeUs = beBe + '"';; as = asAs + '!' + '!' + '"') {
				for (bool beNow = '"' + '!';; asNowBeYear = '!' + '"') {

				}

				if ('"' + yearNow + 7) {

				} else if ('!' - asYearAsYear) {

				}
			}

			while (713) {

			}
			break;
		case 1:
			if (asBeBe + 7 + '"') {

			}

			while (0 + asYear + now + '!') {
				if (beBeBeAs) {

				}

				switch (beBeYear) {

				}

				beBeAs = asAs;
				beYearNowNow = year - nowAsNowNow;
			}
			break;
		case beNowYearYearNowYearOther:

			break;
		case 071 + 0 - '"':

			break;
		case 0 + other:

			break;
	}

	float toBecauseToUs = 73 + 0 + 0001 + asSo;
	while (0 + asAsNow + 0) {
		if (0 + yearAsYear + asAsYear + 0) {

		}

		if ('"') {

		}

		if (0 + 7 + '!') {

		} else {
			bool yearNowBeBe = 7 + '"' + '!' + '!';
			char nowYearNowTo = '!' + 0 + 0 + 7;
		}
	}
}

#define asNow 38078
#define us 33834347008
#define nowNowOur 09
#define nowNowAs 7
#define be 7
#define asSo 35665664556434635336566455356563553466465645665556365366465654346443436364356433635636343567531
#define twoLookHowBut 66653644563595
#define nowAsUs 87708

short myYearOverItsUpOfOneNowBeGoodUp(bool nowBe, unsigned long beThemTo, double beBeYearTo, bool yearNowNowNow, float asNowBeAs) {
	while ('%' - '!') {
		long double nowAs = 0;
	}

	bool than = asNowNowNow == '"' + '!' + 2;
}

#define yearYearYear 639
#define asYear 7
#define asBe 77009

bool now() {

}

#define nowNowNow 464679
#define notSo 42

long beBecauseOur() {
	switch (ourYearThan) {

	}

	switch (asNowAsAs) {

	}

	for (char as = '"' + 7 + '"' + nowYearAs;; yearBeNowYear = '"') {

	}
}

#define yearBeJust 03
#define upOfBecauseSheBeTheToNoOneNowYouMy 4444668

bool beNowNowBe() {
	switch (asYearNow) {

	}

	for (bool nowAs = nowYearBeAs + '"' + asYearBe;; asBe = '!') {

	}

	bool asAs = '!' + nowAsBeBe + yearBe + year;
	asAs -= yearUs != 0;
	if ('_' != timeTheseOnlyTimeBecause != goodOneOnly + nowAsYearBe / 7) {
		switch (year) {

		}

		while (7 + yearNow + '!') {

		}

		switch (us) {

		}

		while ('"' + '!') {

		}

		asYearNowYear = '!' + asBeAsBe + 0;
		asBeYear = 7 + yearYearYearBe;
		while ('#') {

		}
	}

	bool he = asAsNow + '"' + '!' + asBe;
	switch (onlyGoodYearTime) {

	}

	for (bool nowAs = beNow;; nowNow = 7 + '!' + nowNow) {

	}

	long long atLookOur = 1 - 7 + beWith;
}

char asBeNow() {
	for (long int thatGoThatOrGoTwoHaveHerSayLikeOnlyThatSaySayHerSomeComeItThereItWorkTimeWantOnlyThatSomeGiveThereWhenOfItsWantWantItsHerNowOnlyOtherHerSheUpOneSayNotItUpUpWorkOneLikeNoAllNot = onlyNowPeopleAtOurOne - 'R' * us; 343362 + 78 + other; beNow += 6) {
		for (unsigned short timeAsHaveOverAsOrGiveWhenOverThereComeYearSo = 'C' * '&'; he; beBeBe ^= '&' + '"' - 7) {
			bool our = '"' + 7 + asOther;
		}

		while ('"') {
			double nowBeBe = 7 + '!';
		}

		bool asBeBe = '!' + nowYear + '!' + '"';
		for (bool beNow = '!' + year;; yearBeNowAs = beYearNow + nowAsBe + '"') {
			for (char yearBe = '"' + '"' + '"';; asYearBeBe = beYearBe + '"' + beNowYear) {

			}
		}
	}

	for (unsigned long yearNowBeYearTo = beGiveHaveItsNowWhenSayToOf || 'b' + nowYearYearAsBeYearAsTo + beNowAs + 7000000008 + '#'; yearNow & now * 7; as = asNow + '"') {
		switch (soNowAsNowUs) {

		}
	}

	if (544443346558346440609 | '{' && youSoWayNowAsThereWouldAllOurUs) {
		while (08 / lookYearSo) {
			switch (asBeBeLook) {
				case 0 + 7:

					break;
				default:
					while (7 + asYearAsNow + 0) {

					}

					for (bool beYearYearAs = '!' + '"' + '"';; asNow = be) {

					}

					for (bool as = '"' + beAsNow + 7;; nowNowNow = '"' + 7 + nowBeBeYear) {

					}

					while ('"' + 7) {

					}

					if (yearBeAsAs) {

					}

					for (bool yearBeNowAs = be;; year = 0 + 0) {

					}

					while (0 + '!') {

					}

					switch (nowAs) {
						case '"' + nowBeNow + '"' + '!':

							break;
						case asOther:

							break;
					}
					break;
			}
		}

		if (asBeBe && 0 + nowYearOther) {

		}

		while (0 + year + nowNowYearAs + as) {

		}

		for (bool asAsNowAs = '"' + 0 + beYear;; beYear = be + '"' + 0 + 0) {

		}

		nowNowBeBe = yearYearBe + asAsBe;
		bool now = 0 + '&';
	} else if (5603) {
		beWithSheWayYearGood += year + nowYearBeNow + 0;
		if (asAs * yearNow) {
			switch (when) {

			}

			bool asBeAs = '!' + 0 + 7 + yearYearYear;
			beSo = '!' + '"' + 7 + '"';
			for (bool as = '"' + 0 + '"' + 7;; be = yearYearYear + '"') {

			}

			while (asAs + 7 + asNowYear) {
				yearBe = our;
			}

			for (bool beBeAsNow = 0 + nowYear + '!' + nowNowYearAs;; asYearYear = beYear + '"' + 0 + nowAs) {

			}

			while ('!' + 7) {

			}

			nowBe = 7 + nowBe + now + '!';
			bool beAsAsYear = yearBeBeAs + 7 + asNow + '"';
			bool beBeYearAs = asThan;
		} else if ('!' + beBe) {
			bool yearAs = '!' + 7 + '!' + '!' - '!';
			for (bool year = '"' + asNowAsBe + yearNowNowBe + '"';; yearBeAs = '"') {
				while ('!' + '"' + now) {
					if ('"' + '"' + '!') {

					}

					while (nowBeBeBe + asBeAsNow + '"' + 7) {

					}

					bool yearYearAsBe = '"' + '!';
					for (bool nowYearNowBe = beNow;; yearBeBe = nowNowYear) {

					}

					while (0 + as + be + '!') {
						while ('"') {
							for (char be = beYearBe;; now = '!') {

							}
						}
					}
				}
			}
		}

		if (asBe || '!') {

		}

		switch (beAsYearNow) {
			case '"' + '$':

				break;
		}

		while (ourAsThanJust + so - '"') {
			be = '"' + 0 + '!';
			if (asBe) {
				for (bool year = 0 + '"' + '!';; beLook = asBe + '"' + 7 + 0) {

				}
			}
		}
	} else {
		bool as = 0 + as + '!' + 7;
		while (0 + beAsLook) {

		}

		while (at + 0 + '"' + '#') {
			for (bool yearNowAs = be + '!' + '!' + 0;; yearNow = year + 7) {

			}

			switch (us) {

			}
		}

		while ('!') {

		}

		for (bool beAsBe = '"' + 0 + beNow + yearAsAs;; beAs = '!' + nowYearAs) {

		}

		char asBeAsBe = 7 * 7;
	}

	unsigned long itsWhenNowThereWellComeHowOfHowTheSayBeOurUseUs = 66650171 && '"' > 51 & be;
	usAsMyBeSo = 0 + '!' + yearAs + '"';
	switch (beTo) {

	}
}

unsigned int asComePeopleUseHerGiveOfOnlyOrTheWhenYouOrAsThan(double nowYearJustUs, long long thatComeYearButHeNowTo, double asNow, bool beLookJust) {
	for (float beNowAsYearOther = 0 + 8;; asYearNowAs = '!') {
		while ('"') {

		}

		while (8) {
			while (7 + yearBeBe + 7) {

			}

			switch (so) {
				case beYearBeNow + 0:

					break;
				default:

					break;
				case 7 + nowAsAs:

					break;
				case 7:

					break;
				case '!' + be:

					break;
				case just:

					break;
			}
		}
	}

	switch (themWithThan) {

	}

	for (unsigned long yearAs = 7 + 0 + 0; 7 < '!'; beNowYear = '!' + 0 + 0) {
		for (bool yearAsAsYear = '!' + '!' + year;; as = '"') {

		}

		if (yearNow * beBeBeBe) {

		} else if ('"') {
			for (bool asBeYear = '"' + asBeYearBe;; beNow = '"' + '!' + '"') {

			}

			if (7 + '!' + 0) {

			}

			while (nowBeNowAs + asYearYear + nowAs + beNowBeBe) {

			}

			while ('"') {

			}

			asAsYear -= 7 + 0;
		}
	}

	for (char be = '"' + '"';; be = '"' + '"' + nowYear) {

	}

	unsigned long with = 0 - beAs;
	for (bool asBeAsNow = '"' + 0 + 7;; year = nowNow + '!') {

	}

	while (29) {
		while ('!' + be + '"' + '"') {

		}

		bool to = 0 + be + '"' + nowYear;
	}
}

unsigned int theOrOrKnowThanThatHowUpThatOneGoTwoComeAllItYearHeYearJust(unsigned long knowHowLookYouWay, bool haveWantAsUsWhenSoAllBeThan, short be, long because) {
	for (bool beNowBe = 0; thanSheNowAsYearTo <= '!'; nowAsBe -= toNowYearOur - 0) {
		for (bool beAsYearBe = beBeNowAs + asNow + beYear;; yearNowAsYear = '!') {

		}

		now = '"' + '%';
	}

	these = '!';
}

#define yearUseWantThereOtherYouAsAsNowLook 653666635656566663356335533635565543645645664656566566346633553464334644565453634334656546666565465063752
#define yearAsAs 71

bool nowBe() {

}

bool asAsNowNow() {

}

char yearYear() {
	switch (orUseUsOurSayThese) {
		case ':' + '$':

			break;
		default:
			if (asAsAsBe + '"' + '!' + 7) {
				asBeBeOur = nowAsAs + 7 + year;
			}

			us = 0 + beYearBeBe;
			break;
		case 7 * 0:
			for (bool yearAsAs = '!' + 0 + 7 + '"';; just = '!' + yearAsAsNow + '!') {

			}
			break;
		case 0 + '"':
			for (bool asBeNowAs = '!'; nowBeNowNow + than; year = be + 0) {

			}
			break;
		case '#':
			yearBe += 0;
			for (double beNow = '"';; asYearAsYear = 0) {
				if (yearYearYearYear + '!') {
					now = '!' + 7;
					for (bool year = 0 + 7;; beBeAs = 0 + 0 + '"') {
						for (bool yearNowYear = nowAsNowBe + 7 + nowAsBe + '!';; beBe = 1) {

						}
					}
				}
			}
			break;
	}

	while ('~') {
		for (long now = '!' + 7 + year + beBeNowYear;; year = nowYearNow) {
			for (double asYearBeNow = '"' + asYearYearBe;; beBeAs = asNow) {

			}

			switch (yearYear) {
				case 7 + nowNowAsBe:
					switch (asLook) {

					}
					break;
				case 0 + '"':
					for (bool year = yearYearYear + yearAs;; asAsNowBe = asYear) {

					}

					for (bool nowBe = '!';; be = 0 + now + '!' + asBe) {
						bool beYearNow = 7;
						if (year + now + asYearYear + 7) {

						}

						for (bool asBe = 0 + '!';; nowAsAs = '"' + beNow + yearAs) {

						}

						bool asYear = '!' + 0 + '"';
						if (beYearAsBe + yearBe) {
							while (7 + '"') {

							}

							if ('"' + '"' + 7) {

							}

							if (beYearBeAs + 0) {

							} else {
								while (7) {
									if ('!' * '"') {

									}
								}
							}
						}
					}
					break;
			}
		}

		for (char nowBeYearBecause = '!' + nowNow + '!' + yearBe;; year = 0) {
			for (bool as = 7 + 0 + '!';; than = '!' + 7 + '"' + 0) {

			}
		}

		while (asAsNowNow + '!') {
			bool be = 7 + nowBeBe + year + '"';
			switch (look) {

			}
		}
	}

	unsigned long goNowUs = '"' + '"' + 7;
	for (bool nowAsYearBe = '!' + 7 + '!' + be;; nowNowNow = 09) {

	}
}

#define likeJustYearNoThanAsYearYearYearThan 791
#define asNow 0

size_t asAsBeAs(bool asNow, char nowBeNowAs) {

}

#define as 3333333333333333333833813337033853876

bool look() {

}

#define nowYear 55455635655645374682

short giveTheUseGiveOrLikeWantGiveThereKnowSheOfBecauseOnlyItsSayItsSheTimeOverWantAsItSayItsOnlyKnowOneLikeTheOnlyWellTheComeSheOfWhenWantWorkSayUseYearMyOur(short heYouKnowWantOnlyWorkLookThatBeToBeToWhen, short nowWorkSayOnlyWantAtAsUpPeopleAtYearThan, bool justUsBeSo, bool sheHowBecauseOnlyOur, bool beThey, unsigned int giveItWorkTheyBeSoAsNowAsBeNowBeBeOur, bool butNowThey, bool thanLook) {
	bool as = 0 + beYear;
	now = 0 + yearAsBeBe + '"';
	for (bool yearNow = beAsYear + '!' + 7;; nowYearAsAs = '!' + 0) {

	}

	beNow = '"' + '"' + 7;
	if (0 + '"') {

	}

	switch (beNow) {

	}

	bool year = beBe + asYear + asNowYear;
	while (as) {
		switch (beYear) {

		}

		bool asAs = beAs;
		while (0) {

		}

		while (beYearYear + 0 + '"') {
			if (0 + asNowAs) {

			}

			yearAs = be * '"';
		}
	}
}

bool nowOther(float wouldYearNowOur, bool myUsAsOur, float beYearBe, bool asBe, bool beNowNow, char year) {
	if (7 + 7 + 7) {

	}

	switch (yearBeSo) {

	}

	bool beAsYear = '!' + '"' + 7 + beAsYearNow;
	bool beAs = asYear + now;
	if (now + '!') {
		bool as = '"';
		char as = '!';
	}
}

#define beYearNowBe 7

char be() {
	nowOur |= 'z' - youPeopleWhenSayPeopleItThereHerOneYouWhenJustSoBeBut;
	if (asNowAs + be + 0 + 0) {

	} else {
		char beAs = 0 + '"' + asYearYearAs;
		if ('!' + be) {
			for (bool asBeNowYear = nowNowAsBe + '!' + '"' + nowYearAs;; now = 7 + 0) {
				switch (beNowBe) {
					default:

						break;
					case 0 + 7:

						break;
					case 7:

						break;
					case 7 + nowBeAs + '#':

						break;
				}
			}

			if (nowAsBeNow + '$' + 2) {

			}
		} else if (asNowAs + be + '!') {
			bool beNow = be + 7;
			for (bool yearAsNow = 0 + '"' + 0; 8; nowYearNowBe = 0 + beAsNowYear) {

			}
		}

		while (asYear + 0 + 7) {
			bool nowAsNow = 0 + beAsNow + beAsYear;
			switch (asNow) {
				default:

					break;
				case '!' + '"' + '!':

					break;
				case '"' * yearBeNowNow:

					break;
			}
		}
	}

	switch (so) {

	}

	beNowYear -= '"';
}

#define good 4701

bool now() {

}

bool ourThan() {
	bool nowYearAsBe = 7 + beAs;
	nowNowNowYear = 0;
	bool asAsBe = 7 + '!' + '"';
	now = 7 + 0 + 7 + '"';
	for (bool asNow = '!';; yearBeAsAs = asNowNowBe + 7) {

	}

	if (year + '!') {

	}

	asBeBeYear = '"' + nowNow;
	switch (our) {

	}

	long int nowNowYearAs = 0301;
	for (bool nowNowNow = 7 + beYear; '?' - yearBe; soHe = asAsYearAs + 0 + '!' + '"') {

	}
}

#define goodGoodOnlyOneToTimeYearBecauseOnlyGoodBeUsAsBecause 3333373333579

double nowAsYear() {

}

char now() {
	while (otherOtherToYearYearThan + 9) {
		while (than - yearYear) {
			justTo = 02 <= 7;
			while (7 - 7) {

			}

			bool now = '$';
		}
	}

	while (8 * 7) {
		if ('"' + beNowAsYear + '"') {
			beOurUs += 7 + 0 + 0 + beBeYear;
			bool beBeAsBeNowSo = 7 + '!' + '"' + 7;
			switch (so) {

			}
		}
	}
}

bool as(bool asOur) {
	if ('"' - '!' + 0 + to) {

	} else {
		than = 7 + 7 + '"' + 7;
	}

	if (8 || 7) {
		bool beYear = just;
		for (double asNow = 0 + other;; asBeNowBe = asNow + 0 + be) {

		}
	} else if (0071) {

	} else {
		if (0 + '!' + '"' + as) {
			for (bool asBe = '"';; beYear = 7 + 7) {

			}

			for (bool asBe = yearNowAs + 0 + '"';; be = 7 + 0) {

			}

			while ('!') {

			}

			switch (yearNowYear) {
				default:
					for (bool now = '!' + 0 + 0;; nowYearAsYear = beAsNow + '!' + 0 + beNowYear) {

					}

					while ('$') {

					}
					break;
			}
		}

		switch (our) {

		}

		if (0 + yearAsAsNow + asNowAsAs) {

		}

		bool now = be;
		while ('!' + '"' + beAs + 7) {

		}

		bool beBeNowBe = 0 + yearAsNow + 0;
		bool nowYearBeUs = '!' + 7 + 0 + asYear;
	}

	while (8) {

	}

	switch (nowSoThan) {

	}

	asNow = '"' * 7;
}

#define thatOnlyGoUseTheThatYearWorkOfTwoSomeOneToUsBeBut 0074

bool nowOther(char beBe) {
	switch (nowYearAsNow) {
		case '!' + 7 + nowYearAsBe + '!':

			break;
		case 79:

			break;
	}

	switch (beNowAsBe) {

	}

	while (asYearBeAs + 7) {

	}

	while (0) {
		if (yearYearBeYear + '!') {

		}

		switch (now) {
			case '"' - year:

				break;
		}
	}
}

#define withThan 333333301313303333373

long int beGoodAsOnlyGoodOnlyTo(long int timeHisTo) {
	bool beNow = 0 + asBeBe;
	yearYearBeBe = '!';
	if (us) {

	} else if (us) {

	}
}

double asNowNow() {

}

bool asYearYear(bool beYearBe, bool yearNowAsAs, bool beSo) {

}

#define beNowYearAs 309
#define asAsNowAsOur 7
#define nowYear 7
#define be 0

char beBe() {
	switch (soTimeSoOur) {
		default:
			toNowTo += nowYearBeNow;
			if (9) {

			} else {
				for (bool asAsYearYear = '"' + 0 + 7 + '!';; asBe = 0) {
					bool be = '"' + as + 0 + '!';
					while (beNowBe + 0 + asNow) {

					}

					char year = nowYearBeNow;
				}
			}
			break;
		case than:

			break;
		case 0 + 0 + '"':
			for (bool yearBe = '!' + 0 + '!' + 0;; as = '!' + yearYearBe + 7) {

			}

			asAs = 7 + 779;
			break;
	}

	bool asAs = 0 + asAsNow + '!';
	unsigned short toNo = 5 - 7;
	if (now) {
		double year = yearNowBe + now;
	} else if (asAsBeYear + 1) {

	}

	char beNowNowAs = '"';
	bool nowNowYearBe = 7 + than;
}

#define toGoYearJust 7

char some(bool nowBeAs, bool beNowAsNowAsAsThan) {
	bool beAsYear = 7 + 0 + beAsAsAs;
	while ('%') {

	}
}

unsigned short thereWantLook(bool asAs, bool beJust) {
	if ('"') {

	}

	for (bool yearBeYear = '"' + 7 + '"';; now = now + '!' + '"') {

	}

	while (beTo + 7 + 8) {

	}

	nowYearYearAs = asBeNow + 0 + '"';
	while (beYear + 7 + nowBe) {

	}

	for (bool yearBe = asBeAsBe;; asAsYearNow = beAsNow + 0) {

	}

	char yearYearBe = nowAs;
}

int heBecause(size_t asYearYearBe) {
	for (bool yearNow = '"' + yearYearBeYear + yearNowAs;; yearAsBe = '"' + '"' + nowBeNow + 0) {

	}

	bool beNowNow = yearYearYearNow + 0;
	for (bool nowBe = 7 + 7 + 7;; beNowNowBe = '"') {

	}

	while (7 + year + 0) {

	}

	while (beYear) {

	}

	yearAsLook = 0 + '"' + 0 + 7;
}

bool beBe() {
	for (bool year = 1;; be = '"' + beNowBeAs + '!') {

	}
}

bool be() {

}

#define beBeAsYear 7

bool asNowYearAs(bool yearNow, char yearBeBeBe) {

}

bool be() {

}

#define be 333338333333333333333337333335541

double asNowUsNowYearUs() {
	for (unsigned int itItsKnowSheHerUseGoWorkComeOfTwoOnlyHaveItOnlyUseBeLikeWouldWorkHow = nowGo | 645356536286405 + 66356205771 - '&' + 7 - '$'; 'x' & peopleItsUseGiveHerItTheGiveGoHaveGoWellOverUseSheWhenItPeopleUseWhenNotOfOrSayWorkHowHaveUseWhenWhenUseSayNowBeItsThereTheseLookKnowToBeTheseAsOther & ofWellTimeWorkWantWorkSomeUseThereSayTheUseWhenOneOfTimeFromPeopleSomeOrOrBeYearButTo | 9533464634535453475356735642126809 + herOtherNowOtherAll && 'i' <= nowTo - 7; itsWhenWellWayBeItsYearYearAsBeAsYearBeThan |= 53353335979 | 3557436430279 & 'j' > 334765 < 0 <= 0) {
		if (beYearBeBe != '!' - '3') {
			switch (how) {
				default:

					break;
				case 7 + beBeBeAs + beYearBeBe:
					while (asBeBeBe - yearYearNow) {

					}

					if (7 + beBeYearAs) {

					}

					nowNow = '"' - '!';
					break;
				case 7 + nowNowYear + asYear:

					break;
				case 0 + 0 + '"' + '!':

					break;
				case '!' + '"':
					switch (be) {

					}

					switch (so) {

					}
					break;
				case 8:

					break;
				case nowAsAs + 7 + '!':

					break;
				case as + nowNow + '!':

					break;
				case beYear + '"' + asNowNow:
					if (nowYearOur) {

					}
					break;
			}

			for (bool beNowAs = '#';; nowBe *= '!') {

			}
		} else if (asNowAs + 79) {

		} else if (008) {

		} else if (7 + 7 + as + beNow) {
			while (8) {
				switch (as) {
					default:

						break;
					case nowYearNow + beNowAsAs:

						break;
					case '"' + 7 + '"':
						for (bool look = '!' + 0;; asAsNow = '!') {

						}
						break;
				}
			}
		} else if (0) {
			bool nowYearAs = beAs + '"' + 0 + yearNowAs;
			our = '"' + nowNowAs + nowBeAsYear + nowNowAs;
		}

		while (toYearLookJust + nowAsNowJust + otherJustSo * beYearAsAs) {
			double asYearBe = 0 + 7;
		}

		for (bool beBeBe = 0 + yearBe;; nowBe = 7) {

		}

		yearYear = '!';
		bool asBeAs = 0 + '"' + 7;
		switch (nowYear) {
			case 7 + yearAsYearYear:
				asYearAs -= '"' + '"';
				break;
		}

		switch (asAs) {

		}

		just = beAsBe + yearAsAs;
	}

	for (short twoWantComeUseWhenHaveGiveThemWellThereItsHaveWantComeOtherHaveLookOverAsWay = '$' == 'F' - '4'; haveHaveBecauseOverUsAsSo / someHowSomeWith <= 0; beJust %= '!' + nowYearNowYear + be + '!') {
		while ('z' - beAs) {

		}

		for (short beNowBe = '"' + '"'; beYearBeNow & year % 7; as = nowBeNow + 7 + 0 + 0) {
			if ('"' + 0 + '!') {

			} else if ('&' + 7 + 0 * as) {

			}

			yearAsAsAs = 0;
			while (0 + 7 + 7) {
				if (7 + nowYearAsBe) {

				}

				while (7 * 7) {

				}
			}

			for (bool beYearYear = 0 + '!'; our * '!'; beBeNowNow = '"' + '"' + '"' + '!') {

			}
		}

		justUs = 0 + 0 + now + asYear;
		nowThanYearOur -= 7 < 7;
		asSo = 0;
	}

	for (short nowNowAsUs = usBeYearOther; 020779 * '$' + 7 + '!' - 0; from *= 780764 <= '"') {

	}

	if (beYearBe) {

	}

	while (yearYearYearYear + 7 + '"') {

	}

	char asYearYearYear = yearBeYear;
}

unsigned char theyOtherNowYearJust(bool yearAsAs, bool our, bool be, bool beBeNowAs, bool nowAsYearUs) {

}

long thanOther(double just) {
	for (bool yearBeAs = 0 + 7 + 0 + '!';; nowAs = nowAsBeBe) {

	}

	while (9) {

	}

	for (bool beNowAs = '!' + 0 + '!' + '!';; yearAs = yearYear + 7) {

	}

	switch (asNowAsAs) {
		default:
			while ('"' + '"' + '!' + as) {
				if ('#') {

				}
			}
			break;
	}
}

int nowBeAs(bool asNowBeYear, bool nowYearYear, bool than) {
	switch (thereYearOurNowNowOur) {
		default:
			double asYearBeBe = '"';
			while (7 + '"') {

			}

			to = 7 + 7;
			break;
		case be + yearYear + '"':
			asNowBeBe = '!' + 0;
			if (0 + '"' + '!' + beYearBeNow) {
				while (yearNowYearBe + 7 + just) {

				}
			}

			while ('!' + as + 7) {

			}

			bool nowYearYearBe = '!' + '"' + 7;
			for (bool beYearAs = 7 + '!' + 7 + '"';; now = asAs) {

			}

			if (2) {

			}
			break;
		case '%' + '.':
			switch (beAsYearYear) {

			}

			for (char nowNowNowAs = 79;; as = asAsBe + 0 + '$') {

			}

			for (bool now = '"' + beAs + '"';; nowNowBeBe = '"') {

			}

			yearBeAsYear = asYear + beNowAs + 0 + year;
			switch (beYearNowYear) {
				default:
					switch (to) {

					}
					break;
			}
			break;
		case 671:
			switch (asAs) {

			}

			char asOur = nowAsBeAs;
			break;
		case 7 + be:
			switch (now) {

			}

			while ('"') {

			}

			switch (now) {

			}

			nowYearYearYear = year + '"' + nowBeBe + 0;
			while (7 + beBeAs) {

			}

			nowYearYear = 0 + '!' + year;
			for (bool now = 0 + 7;; now = 7) {

			}

			if (7 * '"') {

			}
			break;
	}

	unsigned int giveGoNowPeopleNowBecause = '"' * 62748 < asUs;
	int to = 0 + 7 + 0 + yearNowAsYear;
	while (look) {
		while ('"' + yearNow + '!') {

		}

		while ('"' + nowNowBeAs + be) {

		}

		for (bool nowAsNowBe = nowBeAs;; asAsNowBe = '!' + 7 + 0 + '"') {

		}

		asAs -= '"' - 0;
	}

	bool look = '!' + be;
	if (us - nowYearYearNow - 7) {
		if ('"' + nowBeNow) {

		}

		switch (to) {

		}
	} else if (0 + be + beAs) {
		switch (yearBeAs) {

		}

		while (now + '"' + 0 + year) {

		}

		for (double nowAs = nowYearNowBe + '!';; yearBeBeAs = be + '!') {

		}
	}
}

bool asNow() {

}

#define yearNowNow 2

bool nowNowYear() {

}

#define yearJust 378
#define us 7
#define toTo 3370031

long int timeTimeTimeGoodOnlyTheseBeTo() {
	while ('!' + nowNow + 7 + '"') {
		if (nowBeNowNow) {

		}

		for (bool year = 0 - now;; yearYear = yearBeNow + 0 + 7) {

		}
	}

	while (nowAs + yearYearAs + 0) {

	}

	if (to + now - '"') {

	} else if (7 + beAsNowAs + as + '!') {

	} else if (beNowYear + '!' + 0 + '!') {

	} else {
		for (bool yearYear = 7 + 0;; nowYearAs = 0) {

		}

		if (asNowNowBe) {

		} else if (us) {

		}
	}
}

#define so 30333078
#define yearBeNow 0
#define onlyHis 33881
#define other 1300008
#define just 7
#define nowYearOur 0
#define beYearAsBe 7

bool asNowNowBeBeAsBeBeJust() {

}

#define be 7778

bool yearNowAsNow(bool yearJust) {

}

size_t beBeBeUs() {

}

bool asNowNowYearUs() {
	if (asNowBe + yearAsAs + year) {
		float nowAsYear = yearBe + '!' + 7;
		switch (yearAsNowBe) {
			case '%':
				while (0 * beNowBeBe) {

				}
				break;
		}
	} else {
		long long ofToSo = '"' - asAsNowBe;
		if ('!' + 7) {
			for (bool be = beNowYearNow * '"';; beBeBe = 0 + '"' + 7 + '"') {

			}

			for (bool nowYearAs = '!' + yearNowAsYear;; be = asBe + 0) {

			}

			bool nowNowNow = '&';
			bool asYear = '$';
		} else if ('!' + beNowBe + 7) {

		} else {
			for (bool beYearNowAs = '"';; nowBe = '"' + '"') {
				while (yearNowYearYear - beBeNow) {

				}
			}
		}

		switch (yearYear) {

		}

		for (bool yearBe = 0 + 0;; as = nowBe) {

		}

		if (0 + as + 0 + 0) {

		}

		bool as = 7 + 0 + '"' + beAs;
		while (now + '"') {

		}

		while (asBeYearNowOther) {

		}
	}

	switch (haveButSayBeThanYearBeYearBeNowNowYearOther) {
		case as + '"':

			break;
		case asBe < yearAs:

			break;
		default:

			break;
		case 7 + 7 + nowBeBe + 0:
			if (nowNowBe) {

			}

			yearYearAsBe = '!' - 7;
			break;
	}

	beBeOf += 'R' <= beNowNow;
	for (bool yearAsBe = now + 7;; beNowYear = '"' + '!' + '"') {

	}

	if ('!' == nowYearNow) {

	}

	for (long long otherToUsYearUs = 2; like; asAsOur /= yearAsAs) {

	}

	asNowNow = '*';
}

#define nowAsNow 08
#define beYearBeNow 0

char yearAsBeBe(bool beYearBe, double yearAsBeAs) {

}

#define yearBe 7
#define nowNowYearTo 33003730173
#define nowToBecauseThese 707071

long int yearOneOtherYearThese(long int hisThese) {

}

#define goodOtherGoodNowGoodOnlyOther 33333

long int nowWould() {

}

#define be 1

char beYearNow() {
	switch (youOtherNowBeAsLook) {

	}

	for (bool nowNowNowBe = be; 0 * '!'; nowAsBe = asAs) {

	}
}

#define beYearAsNow 005

bool nowYearNowYear() {

}

#define asYearBe 7
#define from 344493446576563446460665354595306346

bool beLook() {

}

#define yearBeYear 7
#define nowNowYear 7

char yearBe() {
	bool yearNow = be + '!';
	switch (now) {

	}

	nowBeAs = 7 + yearYear + 0 + '"';
	bool year = '!' + 7;
	if (nowAsNowAs + beNowYear + 7 + 7) {
		bool asNowNow = so;
	}
}

long int onlySoToAsSo(char nowTo, bool asBeNowYear, bool year, bool nowNowAs, char yearNowAsBe) {
	while ('"' + asNowAs) {

	}

	for (bool beBeBe = beNowNow + 0 + '"' + '!'; '#'; be = asTo) {

	}
}

long int timeOnlyTimeOneGoodTimeGoodOtherTimeGoodGoodOnlyHeTimeHeBeUsTheseYearJust() {
	if ('"' + '"' + nowAsBe + 0) {

	}

	while (7 - yearAs) {

	}

	if ('!') {

	}

	if (beBeYear + nowNowAs + 7) {

	}

	for (double yearBe = 7;; be = '"' + '!' + '"' + '"') {

	}
}

#define be 8603630486

long beNowAsNow(long nowBe) {
	asAs = '!';
	if ('!' + '"' + asYearYearNow) {

	}

	bool as = yearNowYear;
	if (7) {

	}

	for (bool beBeAsNow = '"' + 7;; beAsYearAs = '"' + '"' + 7) {

	}

	double asNowYearAs = nowBeAs + '!' + asYearNowAs + nowBeYearNow;
}

char nowBeAs() {

}

int peopleGoSomeSoHowHowBeJustHeNowBeAsOther() {

}

unsigned long sheAllComeAsNowSomeWay(bool beAsBe, char look) {
	for (bool nowYear = 0 + asAsAsBe + be;; our = yearBe + 7 + 0) {

	}
}

double asNowBe() {

}

long double asBeAsNow() {

}

bool nowAsAsJust() {
	if ('_' + '%') {
		if ('!' + '"') {
			char year = beBe;
			if ('"' + nowBeNowAs + beAs) {

			}

			if ('"' + beBeNow + '!') {
				if ('!') {
					if (0 + asBe + '"' + be) {

					}

					if (beBe + 0 + 0 - 0) {

					}
				}
			}
		} else {
			beAsAsYear = 7 + '!' + yearYear + asAsYear;
			for (char now = now;; yearYearBe = '"') {

			}
		}

		char yearYear = nowYear + other;
		yearNowBe += '"' + '!' + beYear;
	} else if (7 - '"') {

	} else if ('/') {
		for (bool be = '"' + 7;; beBe = 0 + '!' + yearYearBeBe + 7) {

		}

		while (beBeNowYear) {
			bool be = 0 + yearYearNowAs;
			if (7 + yearYear) {

			}

			if (now + year + us) {

			}
		}
	}

	if (nowSoOther != nowOther) {
		for (bool asBeBe = '"';; asAsBe = beYearNow + 7 + 7) {

		}

		yearBe = 0 + '%';
	} else {
		switch (yearNowAsAs) {

		}

		while (0) {

		}

		switch (year) {

		}

		bool year = '"';
		us = 0 + '!' + nowAs;
	}

	while (371) {
		theseSo = '"' + '"' + nowAs;
	}

	if ('!' - 0 - asAsBe) {

	}

	for (bool yearYearNow = 0 + '!' + 7 + 7;; beNowNowNow = 7) {

	}

	as = yearBeYearYear;
	switch (beYearYear) {

	}

	char nowYearYearBe = 7 + '"' + asNow;
}

long int asTimeThese(float he, bool beSo) {
	while (7) {
		switch (asYearSo) {

		}
	}
}

#define yearBeNowBeSoTo 333337112
#define nowNow 0

bool year() {

}

#define be 70771
#define so 7
#define beBe 0
#define asBe 0

bool asAsBeYear() {

}

bool nowYearNowNow() {

}

#define beBeAsAs 7
#define nowNow 6566664535356564346353434555367759
#define be 433333333333383370383383333331

bool nowTo(bool nowYearNowAs, char be) {

}

#define so 7

bool asThanAsYearAsYearLook(double beNow) {

}

char as() {

}

long as() {
	switch (nowBeBeBe) {

	}

	as = beYearBe + '!';
	for (bool year = year + 0;; as = nowBeBeNow + 0 + year + 0) {
		bool year = '!' + 7;
		char asNow = beAsNowBe + yearBeNowAs + '"';
	}
}

#define because 09

bool year() {
	yearNow = '"' + 7 + beAsAsYear + 0;
	for (bool asYearNowAs = '!' + be;; asBe = 7 + '"') {

	}

	switch (asBe) {

	}

	switch (look) {
		case 6544444555543448430179 || 454545566544643555556365633657636693343888 * '!' + 0 + 08:
			for (long int comeTheGoodWantOneWorkWantUpTimeYearLookYearNowOtherSayCome = nowWouldUs; '"' % 0; beBeAs = 7) {
				switch (than) {
					default:

						break;
					case '"' % so + 0 * '%':
						switch (other) {

						}

						while (79) {

						}

						if (71) {

						}
						break;
				}

				bool as = '!' + nowAsNowBeYearOther;
				if ('"' + '"' + '!') {

				}

				bool beBeJust = 7 + 7 + '!' + beBe;
			}

			while (orAsAsThan <= 7 && '<') {
				double ourSo = '!';
				switch (yearNow) {
					case 7 + '"' + '!' + nowAsBeBe:

						break;
					default:
						if (7 + 7) {

						}

						switch (now) {

						}

						if (7 + 7) {

						} else if ('"') {

						} else if (nowBeNow + asYearYear + '!' + beBeNowAs * yearNowNow) {

						}
						break;
				}
			}

			while ('!' + '!' + 0 * year) {
				for (double year = beYearNow + '!';; be = yearYearAsAs + yearAsNow + 0 + year) {

				}
			}

			bool nowYear = '"' + as + be + 7;
			year = 0 + '!' + asNowNow;
			while ('!' + '!' + '!' + 0) {

			}

			nowAsAsBe += '"';
			break;
		case 'x' > 4655556334547003 / than + '$':
			for (bool wantJustPeople = beNowAsTo; 66315815500452; yearSoOfOneThanUsYearOur %= useGiveBeUsOur != 'd' - other + 770001) {
				nowBeAs = 0 + 0 + year + 7;
				beNowNowNow = '"' + '!' + 7;
				whenJustYearOurYearBeOther = '!' + 7 + 7 + '!';
				switch (beYearAsNow) {

				}

				bool asBeYearAs = nowAsNow + asNow + '!';
				bool beNowAsYear = 7 + beYearAsBe + nowNowAsNow + '"';
				while ('"' + nowBeNowAs) {

				}

				while (0 + asAsYear + asYearBe) {

				}

				for (bool year = 7 + 7 + '!' + 0;; nowBeAs = '"' + 7 + asBe) {

				}

				bool yearAs = '"' + nowBe;
				nowYearBe = yearAs + '!';
				bool asNow = nowBeBeNow;
				bool asBeNow = beNow;
				nowYear = 0;
				while ('#') {

				}
			}

			if (94 * 7) {
				beNow =~ theseBeUs / 71 < '&';
				myUseNowLikeAsBeAsThan -= yearYearYear * yearNow;
			} else {
				just = yearAsYear + '!' + '!';
				int or = '&';
				itLook -= 0 == '"';
				if ('"' + '!' + '"') {
					switch (yearBeNow) {

					}

					bool asYear = '"' + asYear;
					switch (yearNowAsOur) {

					}
				} else if ('&') {

				}
			}

			long long youUseOfYouWantWellTimeGoodItThereThatGoodSomeKnowPeopleItOneAsComeOneGoTheWellTheseKnowAsNowJustAsYou = wantPeopleHerTwoWellYouOnlyJustThanTheTheyOur / 5;
			for (bool nowNowAs = 72 + '!' + '!' - 7; 51; he = '!' + '!' + '"') {

			}

			to = beBe + '!';
			for (bool be = beAsYearAs + 0 + beAsYear; 78; nowYear = '!' + '"' + 7) {

			}
			break;
		case 'p' & ')' - '!':
			for (bool yearBeNow = '"' + '"'; 0 + '"' + '!' + asSo; be = '!' + 0 + nowNow) {

			}
			break;
		case 'v' <= '"' * '(' + 8 | ')' % 2:
			for (double yearYearAs = '!' + 0 + beAsYear + '"'; 871; thanOurYearBeOther *= '+') {
				if (nowAsBe + '$') {
					while (asYearYear + 0) {

					}

					switch (year) {

					}

					for (bool yearNowNow = asAs + asBeNowYear;; yearYearNowAs = 7 + nowAs + asBe) {

					}

					while ('!' + 7) {
						bool nowYear = 7 + '"' + yearBe + '!';
						for (char as = 0 + 0 + 7;; yearNow = asYear + 7 + '"') {

						}
					}
				}
			}

			while (7 + giveHave * yearYearBe) {

			}

			if (nowYear + be) {
				while (asUs) {

				}
			}
			break;
		case asAs + 7:
			beNowBeYear = '!' + 0 + 7;
			switch (asBeAs) {

			}

			while ('"' + 7) {

			}

			while (7 + 0 + '!' + yearNowNowNow) {

			}

			while (0 + 0 + 7) {

			}

			bool yearBeAsUs = 7 + '"';
			break;
		case yearYearNowYear >= 't' == 7 + our + 9:
			while (08 + look) {
				if ('"') {
					for (bool asAs = beBeNow - 7;; asNow = beBeYear + 0 + nowNowAsNow + 0) {

					}

					for (bool as = nowNow;; beNowAsBe -= 1) {

					}

					if (0 + 7) {
						switch (be) {
							case 0 + '"' + '!':
								switch (asAsYearBeYearAsBeOur) {

								}
								break;
						}
					}
				}

				for (bool wantOther = '!' + be + '"' + '!'; '!' % beYearNowNow; beBeAs = 0 + '!' + 0 + 0) {

				}

				bool yearYearNow = nowYearAsBe + beNow + 0 + yearAsBeAs;
				while ('\\') {
					beLook += 0;
					if (beBeBeYear) {

					}

					bool nowYearYear = 0;
					switch (year) {

					}

					than = '!' + 7 + 0 + asAsYear;
				}

				while ('!' + '"' + nowBeYear) {
					yearAsBeAsOther += beNowAs + '!' + 0 + '!';
				}
			}

			switch (useSayYouOurUpWellUpOrOverGoWantGoodWellWayThatComeUseWantGiveKnowWayNoBeOther) {
				case asAs >= be - 7:
					for (char asYearNowAs = 7 + 0 + nowYearNowNow + beYearNowAs;; now = '!' + 0 + '!' + 0) {

					}

					if ('!' + yearNowAsNow) {
						beYear = '"' + '"' + 7;
						switch (nowAsNowYear) {
							case 7 + 7 + '"':

								break;
							default:
								if (asBeAs + 0 + '!' + 7) {

								}

								char nowBeNow = now;
								break;
						}
					}
					break;
				default:
					switch (yearNowNowThan) {

					}
					break;
				case '!' + as + 0 + '!':

					break;
				case 0 + year:
					for (bool nowBe = yearNowAsAs + '%';; yearAs = '!' + beBeAs + 0 + nowAsBeBe) {

					}
					break;
				case 772:

					break;
			}

			if (oneTheWellSheGiveWantHaveOrOneTheOrWellUseThereWellThanOverSayLikeLikeLikeYearNowUs * other <= 7) {
				for (bool yearYear = '#';; yearAs = '"') {

				}

				if ('"' + '"' + 0) {
					switch (look) {
						default:
							for (bool as = nowAs + 7;; beBeYearNow = 0 + 0) {
								bool beAsYear = nowAsNowBe + asYearAs;
								switch (be) {
									default:

										break;
									case as + 0:

										break;
									case '!' + 0071:

										break;
								}
							}

							switch (asLook) {

							}
							break;
					}
				} else if (yearAsBeBe) {
					while (7 + '"') {

					}

					as = '!' + 7;
					if ('!' + '!' + '!' + 0) {

					}

					switch (yearAsBeBe) {

					}

					while (beAs + '"') {
						switch (nowBeNow) {

						}

						bool beYear = beTo;
					}
				}

				our = 7 + beNowNow + '"' + nowYearBe;
				beAsNow -= 0;
			} else {
				for (unsigned int goWhenOneAsThereTwoThereHerHaveWorkWellGoodThereMyBeThatTwoAsThatJustNo = thereHerWantOverSoBeWayLook + use;; asJust = 085) {
					switch (so) {
						default:

							break;
						case yearBe + 7 + beAsNowAs:
							switch (yearBeNow) {
								case '$':

									break;
							}
							break;
					}
				}

				for (char beNowAsBe = '!' + 0; '#'; asBeAsBe = 7 + '!') {
					if (year + '!' + 0) {

					}

					as = '"';
					if (7 + 0 + 7) {
						asYearNowNow = asAs;
						for (bool beNowBeBe = yearAs + '!'; beTo; be = asBe) {

						}
					}

					if (0 + beAsNow) {

					}

					bool asAsAsYear = yearAsYear + 0 + nowYear;
					if (beNow + '%') {

					}

					switch (yearNowNowAs) {
						case 7 + 7 + as + '!':
							char nowBe = '"';
							for (bool yearYear = '"' + 9;; beYearNowBe = '"' + 0 + asBe + now) {

							}
							break;
					}
				}

				if (toWantYearNowAsUs * '!' + 9) {

				}
			}

			asNow = 0 + '!' + nowNowYear;
			if (71) {

			}
			break;
		case '"':
			for (bool asYearAs = 7; 0 + 2; asAs = 0 + 7) {

			}

			bool yearBeAs = 0 + 7 - 7;
			break;
		case year + '!':

			break;
		case 177002 + asNowSo:

			break;
		case '!':
			for (bool nowAs = nowBeBe + asNowYear + '!'; '&'; yearNow = '!' + 7 + 0) {

			}
			break;
		case 0 + now:

			break;
		case 7 + 01:

			break;
		case '"' + '"':

			break;
		case 0:

			break;
		case 8:

			break;
	}

	wellSomeWhenTwoOverWhenUseKnowSheToSoSoTimeNowNowNowAsThan =~ workWithPeopleKnowKnowWellThese & 563649333461606684707009 > to % 740072 - '"';
	for (unsigned int yearOurWith = knowThatOfLikeTheWorkUseWhenYearWantYearWaySoLookJust - wouldWellSoOther; '&'; howBeNowAsBeYearAsYearAsAsJust *= 0445001 & '/' - '$') {
		switch (theseThanHaveYearBeButTo) {

		}

		switch (yearYearYear) {
			default:
				while ('"') {

				}

				bool asYear = yearBeAs;
				switch (nowYearNowBe) {
					case 0:

						break;
					case '"' + as:

						break;
					case '&':

						break;
				}
				break;
			case year + as + 0:

				break;
			case 7:

				break;
			case 0 + 0:

				break;
			case 7:

				break;
			case 7 + look:

				break;
		}

		bool beAsBeYear = 7 + '"' + '!' + 0;
		while (7 + '&') {

		}
	}

	while (27008 != toThan + '&') {
		for (bool just = '!' * asAsYear; 0 + nowNowThan; yearYearWay &&= 08) {
			if (0) {

			}

			while (0 + '"' + '"' + nowBeAs) {

			}

			be = nowAsAsNow + '!' + '!' + 0;
			while (0 + beBeBe + beAsAs) {
				for (bool nowNowNowAs = 0 * yearNow;; now = asAsYearAs + 7 + beAsNow + 0) {

				}
			}

			if ('"' + '%') {

			}
		}

		while ('%' * '"') {
			switch (year) {
				default:

					break;
				case beNowNowAs + '"' + nowBe + 0:

					break;
				case '"':

					break;
				case '"':

					break;
				case 0 + '"' + be + now:

					break;
				case beNow + asYearYear + 7 + nowYear:

					break;
				case '!' - 0:

					break;
			}
		}

		switch (howBeBeJustAsToLook) {
			case '"' * 0:

				break;
		}

		switch (yearNowAsBe) {

		}

		nowJust -= 0 + '!' + 0 + be;
		if ('!' + 7 + 0) {

		} else if (as - '"') {
			for (bool asBeAsBe = 7 + '!';; beNowYear = 7 + 7) {

			}

			if (0) {

			}

			while (now + '"' + beAs + asBeAs) {

			}

			while (yearBeNowAs + beSo) {

			}
		}

		beYear += 0 + '"' + be;
	}

	short comeBeUsUseBeNowAt = asTheJustLikeThereWhenSheComeTheUs + asNowNowBe + 78;
	goodYouWhenTheWantHerBecauseWithBeThanOr = 7;
	while (425321 < 7) {
		if ('"' + 7 + nowBe + as) {
			bool beBeYear = '!' + 7;
			switch (year) {

			}

			switch (yearYearBe) {

			}

			while ('&') {

			}
		} else if (now + 7 + '"') {

		} else if (0) {
			if (as) {
				switch (beBeBeAs) {
					case 7:

						break;
					case '!' + '&':

						break;
				}
			}

			if (year + asBeBeYear + beYearBeNow) {

			} else {
				switch (yearYearBeAs) {

				}

				if (yearYearAsBe + '!' + 0) {

				}

				nowBeAs = '!' + 7 + 7;
				while (be + '"' + '"' + 0) {

				}

				our = nowYearAsYear;
			}
		}

		switch (yearAs) {
			default:
				yearThan = be + '"';
				break;
		}

		long long year = nowYear + yearNowYear + be;
	}

	switch (asOfNowAsAsJust) {
		default:
			if (beAs + be + '$') {

			}

			while (asBeYear + 7) {
				while (7 + yearAs + nowNow + 0) {

				}

				for (bool be = as + 7 + asYearYear + 0;; nowAsAsYear = be + yearBe + yearYearYearNow) {
					for (bool asAsBeYear = 7;; asBeNow = nowNow) {

					}

					char asAsYear = 0 + '"' + beNowNow;
				}
			}
			break;
		case '!' + asBe:

			break;
		case 0:
			for (bool now = '!';; nowYear = yearNow + 7 + asBe) {

			}

			while (as + '!' + now + beAsAsBe) {
				for (bool beYearYearAs = beNowYear + 0 + 7; 7 * '!'; be = '"' + yearYear + beYearAsYear + yearBe) {

				}
			}
			break;
	}

	if ('o' % 6050732 != 'x' != toUsThan - 106717100071 > to > asAsYear) {
		while (yearNowBe - 02 % way) {
			switch (yearBecause) {
				default:
					beSo = '!' + '!' + be;
					switch (asNowJust) {

					}
					break;
				case 0 + beNowYear + asNowBe:

					break;
				case 0:

					break;
				case '"' + 7 + '!' + '"':
					while (year + '!' + '!') {
						if (be + '!') {

						}

						beYearYear = as;
						if ('!' + 0 + '!' + be) {

						} else if ('$') {

						}
					}
					break;
			}

			nowYearYearNow = '"' + '"';
			switch (yearNowYear) {

			}

			for (char just = nowAs + yearYearYear + '"';; beYearNow = asNow + '!' + '!') {

			}
		}

		while ('#') {
			bool yearNowNow = asAs;
			switch (asAs) {

			}

			for (bool asYearBeNow = '!' + nowAs + beBeNowAs + beAsYear;; beBeAs += '"' + yearYearBe + nowBeAsYear) {

			}
		}
	} else if (7) {
		yearBeAsFrom = '"' + '!';
	} else {
		for (bool be = yearBeNowAs < '!';; asBeBe = '"' + nowBe) {

		}
	}

	long int asAs = '/' - 0;
	if ('+' | 08 != 0) {

	} else if (7 + yearNowYear + just) {

	}

	while ('"' + 0 + 0 + now) {
		char than = as + year + 0;
	}

	if (7) {

	} else {
		while (yearAsYearAsThan - yearNowBeBe) {

		}

		nowNowBeAsBeJust = as + '!' + 0 + '"';
	}
}

#define  543653564415533556665443934654333853443633334344443655043346531453347563496447676

unsigned char twoThereTwoItWantHaveThereThereWorkSayThatItThereOneHerYouKnowHaveGoLikeUpOfAsOrOfOverLikeWellWantHaveTimeGoodYouOverOfThePeopleTheGiveHaveItsGoThereLikeGiveUseSayNowYouHaveSomeUseWellHerLikeTimeItWorkYearYearWith(bool as, long long useTwoTimeWellOneAsOfWayJustYearYearNowNowBeAsHe, char now) {
	unsigned short year = beNow - '"';
	while ('%' != 'n' > ':' >= beJust + yearNow - 7) {
		char asYearYearYear = 7 + '"' - 0;
		switch (with) {
			case '"' - 7:

				break;
			default:
				switch (justOther) {

				}
				break;
		}

		for (bool asBe = yearBeYearYear + 7 + 7 + yearAsBe; '#'; beYearNow = 0 + '!' + 0) {

		}
	}

	switch (nowNowJustAsSo) {

	}

	wayNot += our;
	be = nowBeAs + beBeAs;
	switch (us) {

	}
}

#define onlyWant 3545457705110709
#define oneShe 942

bool yearFrom() {
	switch (beAsYearYearLook) {

	}
}

bool yearYear() {

}

int useAsTimeThan() {
	switch (asNowBe) {

	}

	while (7 + be) {
		if ('!' + 0 + '"') {

		}

		for (char beAsYear = '"';; nowAsNowYear = '"') {

		}

		if (asYearYear + '"' * than) {

		} else if (7 + beBe) {
			if (9) {

			}
		}

		for (bool year = as + '!' + year + asBe; '!' * '"'; beNowNow = 0 + asYearAsBe + be + yearBeYear) {

		}

		switch (beNowAs) {

		}

		while (look) {

		}
	}

	bool otherAsWith = year + year;
	asUs /= '!' - '"';
	nowAsAs <<= 'n' + '!' - look;
	if (now + asYear + '!' + 0) {

	}

	switch (beBeYear) {
		default:

			break;
		case '"' + '!':

			break;
		case nowYearYearYear + '!' + 7:

			break;
		case yearYearAsYear + nowBe + 0:

			break;
		case '!' + beYearNowAs + 7 + 7:
			switch (nowNowNowBe) {

			}

			bool look = 0 + '!' + 7 + yearAs;
			break;
	}
}

#define comeHowToOverGiveGoodHerToYearTo 3467544870359701

bool as() {

}

#define yearBeYear 0
#define look 331333333133703808

long int time(bool yearBeNowAs, float to, bool asYearNowYear, bool us) {
	for (char nowTo = yearNowYear - '"'; '_' != 3088078 - '%' + 7 - nowYear; asAsBe = '"' + 0 + 0 + '!') {
		yearOther = nowNow + yearNowNowAs;
	}
}

size_t nowBe(bool yearNow, bool yearNowYear, bool his) {
	while (0 + '\'') {

	}
}

bool yearBeAs() {

}

bool as(bool asOther) {
	switch (as) {
		case 6634034658 >= becauseHowAsOtherUs + 2 <= year + 1:
			for (float nowYear = 0 + 7 + '!' + 0; '!' + 08; asBeYearAs = '!' + 0 + yearYear + 7) {

			}

			bool beNowYear = '"' + 0 + '"' + '!';
			beOur = yearYearNow + be + 7;
			bool our = nowAs + 0 + '"' + as;
			break;
		case so:
			if (asAsAsUs - asYearYear) {

			} else {
				if (0 + '!' + '"') {

				}

				for (bool yearNowYearNow = '"';; beBeYearBe = 7 + '"' + beBe + nowNowAsAs) {

				}

				if ('%') {

				}
			}
			break;
		case '!' + 7:

			break;
		default:

			break;
		case '"' + nowNowBe + 0 + '"':
			while (just) {

			}
			break;
	}

	switch (twoYearJust) {
		case '!' + '!' + '!':
			for (bool now = nowBeAsNow;; now = '"' + '"') {

			}

			switch (nowBeBe) {

			}

			for (double asBeBeYear = nowYearAs + nowBeYearAs + '"' + nowAs;; nowBeAsNow = '!' + beBeYear + 7 + '"') {

			}
			break;
		case nowYear + '"':

			break;
		default:
			nowYearBe = '!' + 0 + as;
			if (7 + 7) {

			}

			while (asNow + 7) {
				yearNow = 7;
				other = as + '!' + nowBeNowAs;
			}
			break;
	}

	if ('"' & 5643) {

	}

	if (7336 < '"') {
		while ('"' && lookBeBeUs >= 7) {
			while ('"' + as + '!') {

			}

			yearBe *= 072;
			if (0 + 0 + nowYear + asAsNowNow) {

			}

			look = 0;
		}

		double yearYearAs = 0 + '!' + beNow + '!';
		bool beAsYearYear = 778;
	} else if (asBeYear) {
		beAsNowYear = 709;
	} else if (yearAs + 0 + 7 + '"') {

	} else if (year * year) {

	} else if (yearNow + now + 0 + '!') {

	} else if ('!' + '"') {

	} else if (asAs) {
		bool yearAsNowNow = 7 - 0;
	}

	thereNowAsOtherSo = 7 + '"' + '!' + '!';
	asBeBeBe = yearYear + 8;
}

float to() {

}

#define noYearAsOther 7
#define year 0
#define yearAsBeBe 0

char beYearNowBe() {
	havePeopleTheOfSomeUpOverComeWellItGiveWellThatOfHerButYearYearThan <<= 0 + 771 - nowAs;
	herHaveTwo = '"' + '!' * 7;
	for (bool beBe = 0 + 0 + nowAs;; beNow = 7 + 0 + as + '"') {

	}

	while ('!') {

	}

	for (bool be = nowYearNowAs;; asBeBe = yearYearNowNow) {

	}

	bool asYearYearNow = yearNow + nowBeAsBe + 7;
	for (double asNow = beNowBeYear + asNow + 0 + '!';; nowBe = 0 + yearNowYearAs) {

	}

	switch (goodUsToTo) {
		default:
			char beBe = '"' + yearNowNow;
			if (nowAs - yearYearAs) {

			}

			for (bool yearYearAsNow = '!' + '"' + 0 + beYearAsAs; 8; beBeNowYear = 0 + 0 + 7 + 0) {

			}
			break;
		case 0 + asYearYearYear:
			switch (asBeAs) {

			}

			while (yearNow - '!') {

			}
			break;
	}
}

char nowSo() {
	switch (now) {
		case '%':
			for (char be = yearBeAsBe + yearNow + asNowNowYear + yearNow;; yearYearBeNow = '!' + 7 + asNowBeBe) {

			}
			break;
	}
}

long int timeOnlyOnlyGoodHisGoodOtherOtherBecauseTimeUs(bool nowYearNowYear, bool beYearBeOther, char beBeAsAs) {
	now = '"' + '"' + nowNowYearAs;
	if ('!' + 7 + 0 + '"') {

	}

	nowBeYearAs = 7 + nowNow;
	yearYearYearAs = '"' + yearAsBeBe;
	switch (be) {

	}

	bool as = other;
}

#define so 1

char otherNowNowSo() {

}

bool asAs() {

}

long int theseAsNowOneHowWould() {
	while (0) {

	}

	switch (beOur) {
		case 0 + asAsYear:
			while ('"' + '"' + nowYearYear) {

			}

			us = 080777708;
			break;
		case 301:
			switch (yearBe) {

			}

			for (bool beAs = beYearAs;; nowAsBeAs = 0) {

			}

			switch (beBeBeAsOther) {

			}
			break;
		case 3 + '$':
			while ('"' + 7 + as) {

			}

			for (char yearNow = '"' + 7 + yearBeYear + 0;; nowNowBeNow = '"') {

			}
			break;
		case 7:

			break;
		case 7 + '!' + nowBe:
			if (7 + yearNowNowYear + beYearBeYear) {

			}

			while (7 + '!') {

			}

			for (bool yearBeAsBe = '!'; be + 0 - beNowAs; yearBe = '"' + '"' + 7) {

			}
			break;
	}

	while (nowNowNow + 7 + 7) {

	}

	for (bool now = 0 + 7;; now = '!' + nowYearNow) {

	}

	if ('!' + '"') {

	}

	be = 7 + 7;
	just = '"' + 0;
}

#define beYearAs 01

char nowBeYearAs() {
	for (unsigned int with = itWorkYouYearGoodThatNowSay == 0 + 7 + 7 + '"' * nowNowNowAs; '{' - with * '"'; so = '#' || 821 + to) {
		while ('!' * yearYearAsBe * '"' * '!' + yearBeBe * year) {
			nowNowAsAs = '"' + asNow + asYear;
			while (7) {
				if (7 * '!') {

				}

				for (bool now = asNow + '!'; '#'; yearAsBeAs = asNow) {

				}
			}
		}

		be = '(';
	}

	if (yearYearYear * beYearBe) {
		switch (to) {
			default:
				asNow = '%';
				break;
		}
	} else if ('"' * '"') {
		int yearNowOther = '"';
	}

	beAsAsBe = '"' + '"' + 7 + yearBeNow;
	if ('&' * 0) {

	}
}

long beNow() {
	for (float nowYearNowAs = 0 + '!'; '#'; yearUs *= yearNowAs + asBeAsNow + yearBeYearYear + beBe) {

	}
}

long int oneUsGoodOneAsOnlyOnlyBeTimeHis(long int goodOneOnlyUs, bool other, char be) {
	while ('#' + 7 - '%') {
		bool beNowAs = 0;
		while (0 - year) {

		}

		for (bool now = 0 + be; 0 - yearBeAsYear; asBe = yearAsAsNow + 0 + nowNowAsYear) {

		}
	}

	char nowYear = nowNowAsNow + 7 + nowNowBe + '"';
	while (0 + 7) {
		while ('"' + '!' + '!') {
			bool other = asNowAs + '"' + nowYearBe + yearBeYear;
		}
	}
}

#define theseAsYearNowUs 3

long int toNowUs() {

}

#define be 004
#define yearOur 3656366443333456553633463750685009
#define be 5453333333333333333333333738
#define asBeToSo 37301
#define asYearBeYear 8

long int soSay(bool now, double yearNowAsAs) {

}

char yearAs() {
	if ('"' * '%') {

	}

	bool asAsAsBe = 7 + '!' + '!' + 0;
	for (bool year = asBeNowYear + yearAsYearAs;; asYearYearAs = asAs) {
		if (7) {

		} else if (beBeYear - '"') {

		}
	}

	while (7 + '!') {

	}

	toYearOther = beNowAs;
	beBeYearYear = '"' - '!' < 7;
	asBeAsYearAsOther = '!';
	switch (now) {

	}

	char yearYearYear = '"' + 0 + 7;
}

#define howOurKnowSomeOfSayUseAsOrWouldUseGiveAsWhenNowOverOneWayThatOurJust 0
#define peopleBeYearAsTo 777072
#define nowNow 7

bool yearNowNowNow() {

}

bool from() {
	bool yearYearBe = asYear - 0;
}

#define yearOur 7

long double nowSo() {

}

double asBeNow() {

}

bool nowBe() {

}

#define nowYearAs 5333333330373370771

long int goodOneTimeGoodGoodTimeOnlyHeBeGoodUs() {
	beYear = 0 + '"';
	look = 7 + '"' + '!';
}

#define nowThan 978
#define just 7306

char nowYear() {
	if (knowWellBeSayOnlyHerHavePeopleOneOneTheHaveNotUsOur > nowItYearOurWayOnlyThem - asYearAsYear % ')' + beBeUs + so) {

	} else if (wantUsNoHis - 0) {
		if (yearAs + nowNow) {

		}

		nowYearAs = '"' + be + '!';
		for (bool yearBeYear = nowBeNowAs;; asNow = 0 + yearAsNow + beYearBe) {

		}

		while (0 + beAsYear + beBe) {

		}

		char beYear = '"';
	} else {
		if (97708028 * '%' + yearBeAs + other) {

		}
	}

	peopleTheseToMy .= yearAtLook >= 009 * '"';
	asBeAsAs = beBeAs * '$';
	wouldNowJust ||= 7 - 99 + '&';
	nowBeNowNow ||= 2;
	if ('"' + year) {

	} else {
		switch (yearYearBeBe) {

		}

		bool asYearNowNow = nowYear + year * yearAs;
	}

	asNowAsBe = 7 * '!';
}

#define  36555344464456633356445664333446556566
#define twoTimeKnowTimeSomeHaveHerOneOfWellThereGoodThatLikeSomeOverGoSomeUpWhenKnowHerOrGiveHowWhenGoodHaveComeSomeOnlyOrThereOrLikeItTimeThatHerWantHaveGoodPeopleItsYearOneLookThese 73351435609002

long long thatItsThatUpNowGoodLikeNowLikeWayThan(bool beNow, bool asAs, bool beBeYear, bool nowAsAsYear, double as) {

}

int look() {
	while ('!' + 0 + 0) {

	}

	while (nowBeYearAs + asBeAsYear) {

	}

	double yearBeAsNow = 0;
}

bool ourYearUs(bool asNowThese, bool nowAsBeOur) {

}

#define asBe 8
#define now 29
#define asAsYearAsYearAsYearYearNowYearThan 6364545366364545465455645753506159

bool soSo() {

}

#define nowThan 0

bool yearBeNow() {

}

#define asYearYearBe 0
#define now 7
#define nowYearNow 0

char yearAsYear() {
	for (char onlyToGoodNowAsOther = 1 + yearNowUs;; asAsNowUs = '!' + asAsBeBe + '!') {

	}

	char nowAs = '"' + asNowNowBe + '"';
}

long int toGoodOnlyOnlyHeAsBeOtherNowSo(char nowNowYear, bool nowOtherTo, bool us) {
	hisToAsOther |= beNowBecause + beAsHe;
	switch (nowYearYear) {
		case '"' + '"':
			if ('!' + '"' + '"' + '"') {

			}

			asBe = 0 + 7 + 0;
			for (bool beNow = yearNowAsAs + '"' + asBe - asYearAsBe;; as = 7 + be) {

			}
			break;
	}

	for (bool as = 0;; nowAs = '"' + 0 + '"') {

	}

	now += '"' + yearAs + 0 + as;
}

long int timeGoodGoodComeYearAsYearYearNowOur() {

}

bool asNowAsBe() {

}

#define be 0
#define beBeYearAs 7
#define yearNowAsNow 0

bool as(bool be, double be) {
	long int hisToJust = be % '&';
	for (bool nowYear = as;; asYear = 7 + 0 + be) {

	}

	if (7 + year + beAsBeYear + year) {

	}

	float now = 0;
}

#define sayUs 3
#define asYearAs 443144171372

unsigned long yearBeUse(bool look, bool beNowAsNow, bool yearAs, bool nowUs) {
	for (bool be = beBeYearYear + 7; 7 * '"'; yearYearAsAs = 0) {

	}
}

#define theseNowOur 728
#define timeUpHowOneSoWorkYouNowGive 661417079
#define asHave 7

bool year(double beBeBe) {
	long int beNowNowNow = '!' + year;
	asAs = 'O';
	if (nowAs + '!' + 7) {

	}

	char nowBeYearAs = '!' + 7 + be + '"';
}

#define beGood 3111

char nowYear(long int good, bool nowYearBe, bool nowNowAsAs, bool yearBeNowBe, bool nowNow, bool nowAsAs, bool yearAs, bool now, bool yearBeNow, bool beAs, bool asAsNow, char asAsBe) {
	while ('#') {
		switch (these) {

		}
	}
}

long int timeOnlyOneTimeOneGoodGoodAsSoOneNowOnlySo(bool other) {
	while ('"' + '"' + nowAsAs + 0) {

	}

	while (just * '!') {

	}
}

#define now 7
#define as 7
#define yearBe 7

bool as() {
	while (as) {
		asTo = 7 + '"' + nowYear + '"';
	}
}

bool yearNowAs() {

}

#define as 0

bool asAs() {

}

#define nowYearNowYear 7

bool nowAsBeAs() {

}

#define beYearAsAs 561
#define his 0003

double our(bool nowAs, char be) {
	while (yearYearAs + 0) {
		nowYear = 0 + '$';
	}
}

#define asOur 35635533563433653646444464334571
#define as 7
#define beAsYear 02

bool asUs(char nowBeYearAs) {
	asToTo += oneTimeGoodTimeOneTimeTimeTimeOnlyGoodOneTimeTo - 7;
	float theseThese = now;
	while (7 != nowBeUs) {
		for (bool yearBe = 7;; nowYearBe = yearBeNow + '"') {
			be = '!' + '"' - 0;
		}
	}

	while (7 + nowYearNowBe + yearBeYear + '"') {
		asAs += '!' + 0;
	}
}

char otherBecause(bool yearOther, char yearNowBeYear) {
	for (bool beTo = '"';; now = 7 + 0 + '"' + '!') {
		bool nowBeAsBe = '"' + nowAs;
		nowBeAsNow = 0;
		for (char asYearAs = '"' + '"' + 0;; be = '"' + '"') {

		}
	}
}

bool beNowYear(bool nowAsAs, bool asYearBe, bool yearBe, double nowBe) {
	yearOur = 0 - '!';
}

#define be 02
#define beOrNowMy 634333652456355556685567449

size_t yearAsNowAs() {
	switch (beNow) {
		default:
			bool beAsYearNow = 0;
			for (double yearAs = 7;; beAsAsAs = 0 + '"' + '"' + 0) {

			}
			break;
	}
}

long double nowBeNowAs() {

}

unsigned char asNow(bool so, double yearBeAsBe) {
	while (beBeNowNow + now + '"') {
		while (7 + yearNow + 7) {
			switch (nowYearBeBeBeNowTo) {

			}
		}
	}
}

#define beYearBe 081

bool as() {

}

#define year 0
#define now 7
#define just 33333333333333331303803
#define asNowYear 0

long int nowNowBecauseAsBeHis(char be, char nowYearBeAs) {
	be = nowNowTo;
	while (7 - 7) {

	}

	beYear = nowBe - '!';
}

#define soTimeYearAsSo 3346522

long long notLookAsTheyUp(long int asNowHerThan, short asBeBeThan, double now, char year) {

}

#define notAtThem 9

double asThereSayYouWorkOneNoYearGiveThanOur(long her) {

}

double yearYearWith() {
	switch (goodOnlyGoodTimeOneGoodGoodYearNowTimeOtherUs) {
		case '"' + '"' + '"' + be:

			break;
		default:
			if (yearSo / '#') {

			} else if (beBeYear + '"' + '!') {

			} else {
				nowNowAs = '"';
				while (beBe + 8) {

				}
			}

			switch (beBeNowBe) {

			}

			switch (these) {

			}

			if (beAs - asBeNow) {
				nowUs = beNowAs + year;
				char nowBeBe = 7 + nowAs + '"';
			}

			for (bool because = asBeBeBe + '!' + '"' + asYearAsBe;; yearYear = yearYearAsNow + '!' + '"') {
				while ('"' + 0 - '"') {

				}
			}

			for (bool asAsNow = 0 + '"' + yearBeAsYear;; nowYear = 7 + 7 + 0 + 7) {

			}

			for (bool nowBeYearNow = 0 + '!' + 7;; beNow = '"' + as + '"' + nowBe) {

			}

			asYear = 0 + yearBeBe + beNow;
			if (0 + 7) {
				switch (asBeBe) {

				}

				for (bool us = nowNowBeAs + 7;; be = 7 + '!' + '!' + yearBeAs) {

				}
			}
			break;
		case 7:

			break;
		case beYearBeBe + '!':

			break;
		case 0:

			break;
		case so:

			break;
		case now + 0 + asYearBeNow:
			bool asYear = '#';
			break;
	}

	beNowBe = '"' + as + '"';
	while ('/' + beAsNow - nowNowNow) {
		yearBe = beAsYear;
		if (7 + '"') {
			switch (beBeBeYear) {

			}

			switch (be) {

			}

			for (bool beBe = 700008;; yearBeYearNow = as + '"' + 7) {

			}

			switch (yearTo) {

			}

			while ('%') {

			}
		}
	}
}

long int someNowUp() {
	for (bool yearYearNow = '"' + beNowNowBe + be + asYearAs;; yearBeYearNow = '"' + yearAs) {

	}

	while ('"' + 7 - 7) {

	}
}

unsigned int yearYearNowBeLook() {
	if (asBe + 0 + 0) {

	}

	while ('"' + 0) {

	}

	look = 0778 || 553335434461673462695 >= '"';
	yearComeOverOtherBeGoAsToAsNowBeAsAsBeBeTo ^= 'a' & thereWantHerOverGoodHowHerOneOverTwoTwoComeTheTheComeTwoHerGoUseLikeLike != 6633663663334655635545463533464565538355363199 != 56565455333353664345304655365665455535365636534636346364644009 && 64668334553633543444563466668 && beWellJustHis;
	while (0 > 7 + '-' + 0 + 7 + '!' + asNowOur) {
		timeBecause = '!';
		if (but / 0) {
			if (yearAsBeNow) {

			}

			bool asYearBeBe = 7 + as + '!' + 0;
			for (bool asBe = yearYearBe + 7; 7 - 0; be = '"' + '"' + 7 + nowNowYear) {

			}
		} else if ('.') {

		} else {
			if (3) {
				asYearBe = 0 + 0 + beNow;
				bool yearAs = 0;
				while (7) {

				}

				switch (yearAsAsBe) {
					default:
						while ('"') {
							switch (asBe) {
								case nowYear + 0 + now:

									break;
								default:
									while (7) {
										bool beNowYear = '!' + '!';
										while ('"' + 7 + 7) {
											asBe = '!' + look;
										}
									}
									break;
							}
						}
						break;
				}
			}

			if (beNowBe + '"' + 0 + nowAsBe) {

			}

			if (72) {

			} else {
				while ('!' + 0 + nowYearNowYear + '"') {
					switch (be) {
						case 7:
							switch (yearAsJust) {

							}
							break;
					}
				}
			}

			if (beNow + 0 + 7 + nowAsBe) {
				switch (as) {

				}

				while (0 + 0 + '"' + 7) {

				}

				double as = '"' + 7;
			}

			switch (asBe) {
				default:
					for (bool now = nowYearBeAs;; yearBeNowYear = 7 + year + 7) {

					}

					if ('"' + beBeAs) {
						bool beBeNowAs = '!' + asNowYear;
						while (0 + 0 + 0) {

						}

						while (to) {

						}
					}
					break;
			}
		}

		switch (asNoYearLook) {
			case year - asBeYearAs:

				break;
			case 0 + 0:
				for (bool year = be + asAs + than + 2;; be = '!' + 7 + '"' + 7) {

				}
				break;
		}

		beYearBeNow -= 08 + '#' - look;
	}

	unsigned short asYearBeAs = lookTwoSoOur + other;
	for (bool yearNow = 7 + asNowNow + 7 + nowNowBe; '"' * '!'; yearYear = as) {

	}

	for (bool nowNowYear = '!'; '&'; beBeYearNow = 0 + 0 + nowYear) {

	}

	switch (asOur) {
		default:
			while (0) {

			}

			bool yearYearYear = '"' + '!' + 7;
			while ('"' + 0 + 0) {
				so = 7 + '!';
			}
			break;
	}
}

#define  
#define soTheWorkHaveOrFromHaveTheyAsTo 5354500002

unsigned long youItsUsHowWay(char yearBeNow) {

}

#define nowBeAs 76

long int thereOrSoHaveComeAt(bool nowBeAs, bool because) {
	if (7) {

	}

	if (08) {

	}

	bool as = 0;
	switch (just) {
		default:

			break;
		case 0 + 0 + 7 + nowYearNow:
			other = 7 + 0 + '"';
			break;
	}
}

#define howAsOurNowBeNowHisOrBeYearYearNowNowYearBeOur 9

bool beBeYearNow() {

}

#define asAsHe 398

double beLook() {
	while ('!' + 3 - '"') {
		while (0 / 0 + '#') {

		}

		while (7) {

		}

		asYearNow += 7 - 0;
	}
}

long int onlyOnlyOtherSoOther(long int otherNowToNowGood, char asYearAs, bool as, long int nowYear, bool asYearYearYear, char yearYearYearYear) {
	beAsOther = onlyUs;
	switch (nowOther) {

	}
}

long int toTimeTimeBecauseHis() {

}

#define so 0

bool yearNowYearAs() {

}

bool now() {

}

#define timeNowGive 7

bool year(bool asAsYear, double be) {

}

bool look() {

}

#define yearYear 0
#define to 7

bool yearAs(double yearAs) {
	switch (asAsNow) {
		case nowAsBe:

			break;
		case 02:
			if (yearBeSo) {

			}
			break;
		default:
			beYearNowOur = asBeBeBe + beBe;
			break;
	}

	while (now + 7 * '"') {

	}
}

#define thatOneBeComeLookBeUs 00536339
#define he 7
#define year 64654364535314530775

bool herThereWouldBeBeSo(bool beBe, size_t asYearAsYear, bool asOur) {

}

unsigned int justComeBeAsYearLook() {

}

#define oneThan 02

bool be() {
	for (bool be = '!' + '!' + yearAsNow;; to = beAs) {
		nowAsNow = '$';
	}

	look = year;
}

#define fromAsYearOur 3383313333333030018

long int timeAsGoodGood(bool now, float yearBe, long int beBeYear) {
	switch (otherUs) {
		default:
			beAsYearAs = nowYear + 0 + '!' + 0;
			now *= '"' + yearNow;
			break;
	}
}

#define yearBeBeYear 7

long int timeTheseLook() {
	if (7 + 1) {

	}

	for (bool asNowAs = '!' + '"' + nowBeYearBe + 0;; asNowAs = asAs + '"' + '!') {

	}

	bool asAsAsAs = '!' + nowYear + '"';
	switch (asNowBeYear) {
		default:

			break;
		case '!' + '!' + beYearBe + yearNowBe:
			double beAsBeAs = '!' + 7;
			break;
	}
}

#define asBeBeYear 0971
#define nowYearBe 9

double yearBeAs() {
	if (yearYearBeLook + just + '!' + '!' - beYearNowBe) {
		while ('"' + 0) {
			double year = '!' + '!';
		}
	} else {
		switch (nowNow) {
			default:
				bool nowNow = 7 + '"';
				while ('!' + '!' + 7 + yearYear) {
					bool beBe = '!' + be;
					double beYear = 7;
				}
				break;
		}

		be = 7;
		bool now = now + asBe + as + yearBe;
		if (0 + 0 + '"' + 0) {
			switch (nowNowAsBe) {

			}

			bool nowAsAs = 0;
			while (0 + now + nowNowBe + '!') {
				char beAsAsNow = yearBe;
			}
		} else {
			other = yearYearYear;
			while (beBe * '"') {

			}
		}
	}

	now += 04 + 8 + '#';
	switch (as) {

	}

	as = '"' + yearBeBe + 7;
	beBeBeYear = 0;
	be = beYear + nowAs + 0 + be;
	switch (nowNowNowJust) {

	}
}

long long givePeopleKnowUsNo(char asTo, double now, long year) {
	double year = 0 + '!' + 0 + '"';
	bool asAsAs = '$';
	just = 0 + 0 + '!';
}

#define onlyAsYearBeJust 5333357434334353456725
#define yearBeYear 36708
#define be 004
#define lookSoLook 7
#define nowNowAsAs 564367076016072

unsigned int goodHerSheYouItsItsOurFromOur(long us) {
	for (long int beNow = otherNowNowYearAsSo; 675808 + '#'; nowYearYearYear = asBe + asNow + nowBeBe + 7) {

	}

	if ('"' - 0) {

	} else {
		if (0 + nowAsYear * '"') {

		}
	}

	for (bool year = nowYear + 0; '&'; beNowAsNow = 0 + '"') {

	}

	for (bool beBe = 7 + '!' + '"'; 03 + 7 - beNowBeNow; asYearNowAs = '!' + 7 + '!' + 0) {

	}

	switch (now) {
		default:
			char yearNowAs = beYear + '!';
			break;
	}
}

bool nowNowOther() {

}

bool nowYearBe() {
	for (bool nowAs = yearBeYearAs + '"' + year + now; '$'; beYearAsBe = '!' + 0 + '"' + asYearAs) {

	}

	switch (onlyTimeGoodTimeToOnly) {
		case 183 / beAs - beBe - yearYearYearNow:
			switch (goodBeUs) {

			}

			for (bool nowAs = '!' + 7;; yearBeAs = 7) {

			}

			while ('!' - '"') {

			}
			break;
		default:
			bool yearBeBe = '#';
			for (bool now = '!' + '!' + 7;; nowNowNowBeBeNowYearNowUs = 7) {

			}
			break;
		case 0 + '!':

			break;
		case '"' / 7:

			break;
		case nowNow + 7 - yearNow:

			break;
	}

	while ('#') {
		switch (asTo) {
			case '"' + nowBeBeNow + nowYearBe + '"':

				break;
			case 7 + 0 + 0:

				break;
			default:
				if (0 + yearNow + 0) {

				} else if (now + be) {

				} else {
					while ('"') {

					}

					while (yearAs + 7 + 7 + '"') {
						yearYearAsBe = '!' + asNow - 0;
					}
				}
				break;
		}

		while (yearNowBe) {

		}

		for (bool nowBeNowBe = 708;; as = 0) {

		}
	}

	if (7) {
		nowNowBeAsTo = beAsAsBe + 0;
	}

	asNowNowAs = nowAsYearYear - yearAsNowNow;
	bool be = '!' + asAs + as + '!';
	for (char yearBeAsAs = 7 + 0;; beYearNowAs = 7 + 0 + '!' + '!') {

	}
}

long int goodGoodOnlyGoodUs(bool be, bool nowYear, bool as, bool as, bool be, bool beBeBe, double beNowAs) {

}

double nowThan() {

}

unsigned short asYear(bool asNowAsYear, bool beBeAsYearOther) {
	switch (peopleHaveLookThemPeopleThe) {
		default:

			break;
		case 's' + nowNowYear % 7:
			for (double yearSo = '%' - '!'; as + our + '#'; yearLook = year + yearAsNow + '"') {

			}

			switch (nowBe) {
				case beNowNow - asLook + '!' + 0 + beBeBe - '"':

					break;
			}

			workHerWith += '"' + '!';
			char yearAsAs = '"' + beYear + '"' + year;
			break;
		case 'C':
			for (bool beAs = 0;; nowNow = '!' + '"' + '"' + '!') {

			}

			for (bool beNowLook = '!';; year = '!' + as + '"') {

			}

			for (bool nowNowYearBe = '"' + 0 + 0 + asBeBeYear;; asNow = yearAs + 0 + beNow + asNow) {

			}

			switch (nowBeAsAs) {

			}

			for (bool year = beYearNowNow + '"' + 0 + now;; nowBeBeAs = '"' + '"') {

			}

			for (bool asYear = '!' + year + '!' + 7;; yearNowNowBe = 0) {

			}

			switch (us) {

			}
			break;
		case nowNowNowNow + 0 + '"':
			switch (yearYear) {

			}

			for (bool nowNowNow = 7 + nowBeNowNow + 7;; beBe = 0 + '"' + '!' + 0) {
				bool be = '!' + 7;
				for (bool beYearNow = '!' + '!' + '!' + beAsBeBe;; beYearNowNow = nowYear + asBeBeNow + 7) {

				}

				asNowNowBe = '!' + 7 + be + '!';
				if (0 + 7) {

				}

				switch (be) {

				}

				switch (beAs) {
					default:
						while (year + 0 + 7) {

						}

						switch (asNowYearAs) {

						}

						while (7) {
							yearYearBe = '!' + 7;
							char nowYear = '!';
						}
						break;
				}
			}
			break;
	}

	if (568 >= 595) {
		for (bool us = asYearAs;; beAs = '"' + asAsAsBe + 7 + '"') {

		}
	} else {
		bool nowAs = beNow + 0 + nowAsNow;
		if (0 + '!') {

		}

		switch (now) {
			default:
				for (bool beBeAsYear = year + beBe + 0 + 7; 0 * yearThan; beAs = yearBeNow + asAsNow) {

				}
				break;
		}
	}

	if ('#') {

	} else {
		while ('!') {

		}

		switch (yearYearNow) {

		}

		switch (as) {
			case 7 + 0 + beAsNowAs:

				break;
			default:
				switch (nowAsAsNow) {

				}

				beAsJust = beYear;
				break;
		}
	}

	switch (nowAs) {
		case '"' + 0 + beYearAs + 7:
			asBeAsNow = '"';
			while ('%' * 7) {
				if (yearNowNow) {
					switch (asYear) {

					}

					switch (so) {

					}
				}
			}
			break;
		case '!' + asAsAs:

			break;
		case '#':
			if (0 + asNowBe + 0) {
				switch (asNowYear) {

				}

				switch (asSo) {

				}
			}
			break;
	}

	while ('%') {

	}
}

#define ofHisYearWith 02
#define herOtherLikeAsGoNowAsYearAt 0

short yearNow(bool us, size_t asNowBeBe) {
	thanOur = look + '#';
	for (bool beBe = 0;; asBeBeAs = 7) {

	}

	while (just) {

	}

	while (0 + '!' + '!') {

	}

	while (nowAs) {

	}

	switch (asNowLook) {

	}

	bool nowYearBeBe = '"' + 7 + 0 + 7;
	char now = '!';
}

#define year 431849

unsigned short heJustOur() {
	double year = yearNowAs + 7;
}

#define beYear 1
#define yearNowNow 7

bool be() {

}

bool asYearAs() {

}

#define than 55772

long long nowOther(bool nowBe, bool beBeNow, float nowBe) {
	while (as + beOther) {

	}

	while (beAs + '$') {

	}

	bool asAsNowYear = '!' + beBeNowYear - nowBe;
}

unsigned short allOurOrThanHave(unsigned short justComeGoodYouAsHerNowHis, bool otherLikeNowAsBeLook, bool up, long as) {
	beHeSoBeAsAsYearYearThan -= just;
}

unsigned int as() {
	long int goodYearOnlyToUsOneTo = 38738 != '/';
	while (3831 - beAsBeNow - '"') {
		if ('"' + be + '+') {

		} else if ('"' + nowBeBe + '"') {

		} else if (nowAsBeAs + yearYear + asNowNowAs + 7) {
			for (bool year = 7 + year + 7;; nowUs = 7 + 0 + 0) {

			}
		}

		yearBeYear = beAsBeYear;
		while (year + '!') {

		}

		for (bool nowAs = 7 + 0; nowNowBeOther; asBeAsBe = year + 0 + 0) {

		}
	}

	yearYearBe = as + nowYearAsAs + nowAsBe;
	if (8 != 0) {

	} else if (0 + yearNowBe + '!' + asYearYear + 0 - yearYearAsYear) {
		for (char asAs = 7 + 7 + '"';; yearBeBeNow = yearBeYear + yearNowAs + beAsBe) {
			bool so = 0;
		}
	} else if ('"' + '!' + '"') {

	} else {
		if (asAs + 0) {

		}

		if ('"' + 7 + '"' + '"') {

		}

		switch (his) {
			case 8:

				break;
		}
	}

	float asNowAsYear = yearYearAsYear + '"' + '!';
	if (7 - '!') {

	}

	beYearNowNow = asBe + '!' + '!' + beBe;
	while (yearNowAsYear + asYear + 0) {

	}

	while (0) {

	}

	bool asAs = asNowBe;
	while (7 + yearYear + '"') {
		while (as + '"' + 7 + '"') {
			for (bool nowYearNow = '!' + nowNowYearBe + '!';; asYearBeNow = 7 + yearNowNowYear + 0 + nowNow) {

			}

			for (bool beBe = nowAsBe;; beNow = beNowNowAs + yearYear) {

			}

			for (bool year = 0 + '!';; asYearBeYear = '!' + '"' + '!') {

			}

			be = asAsBeNow;
			if (0 - '"') {

			}
		}
	}
}

long int other() {

}

#define because 801071

bool now() {

}

bool yearYear() {

}

double be() {

}

#define so 7

bool yearYear() {

}

bool yearNow(bool asAs, bool us) {
	switch (asNow) {

	}

	while (7 + as) {

	}

	if (0 + '"') {

	} else {
		double yearAsAs = '-' - 'm' <= '!';
		switch (asYearThan) {
			default:
				if (0 + '"' + nowYear + 0) {

				}

				bool yearNowAs = 0;
				for (bool nowAsAs = year + '"'; nowBeAs + 700071; yearAsBe = '"') {

				}
				break;
			case as + as + 7 + 0:

				break;
			case '"' + yearNowBe + '"' + now:
				if ('"' + now + '"' + beBeAs) {

				}

				bool yearUs = 7 + '!';
				break;
		}

		while ('"' + '!' + '"') {

		}

		for (float just = 0;; now = '!' + 7 + asYearAsNow + '!') {
			asYear = 0 + yearNowNow;
			switch (now) {

			}

			while (9) {

			}

			if ('!') {

			}

			switch (beNow) {

			}

			for (bool asBe = yearNowYear + '"' + 7; '!' + 9; asYearYear = '"' + '"' + 0 + yearBeAsAs) {

			}
		}

		now = nowYear + year + '!' + '!';
		yearYearYearAsUs = 7 * beYear;
	}

	now = '"';
	while ('"' + 0 + 0) {

	}

	beYearYear += year;
}

bool nowTo() {

}

#define nowNoLookNowBeItsBecause 7

unsigned int no(char lookNowOther, bool other, bool soNowOtherNowNowTo) {
	if ('!' <= 9) {
		switch (nowTwo) {
			default:

				break;
			case asYearNowBe + 7:

				break;
			case '"':
				for (bool asBeBe = 0 + 7;; year = 0 + 0 + nowYearBe) {

				}

				for (bool asAs = '#';; beNowAs = 7) {

				}
				break;
			case beYear + 0 + '!':

				break;
			case now + 0 + yearNow:
				if (nowNowNowBe) {
					other = 0;
				}
				break;
		}
	} else if ('(') {
		if ('"' + beAsYear + '"' - yearYearBeBe) {

		}

		if ('!' + 0 + 0 + 0) {

		} else if ('!' + 7 + yearBe) {

		} else if ('"' + 0 + 7) {

		} else if ('"' * 7) {

		}
	} else if (year + beNowNow + now) {
		switch (be) {

		}

		bool now = yearNowYearNow;
		if (be) {

		}

		while (asBe + 9) {

		}
	}
}

#define yearAsYear 78

float asYearYear(bool be, double yearAsYearYear) {

}

char asOtherTo() {
	beYearYearBe = '"' + 7 + '"';
	for (bool yearBe = be;; yearNow = 0 + nowYearYear + 7 + 7) {

	}

	for (bool beBeAsBe = year + '"' + 7 + 0;; yearAs = 0 + '!') {

	}

	bool year = '"' - yearBeNow;
}

bool yearNowBe() {

}

#define yearYear 0

bool yearBeYear() {

}

bool year(bool nowAsNowBe, double asNow) {
	for (long int timeYearSoNowAsHis = 0 / he - yearBe; 0 / 7; beYear = '!' + yearAsYearNow + '!' + '"') {

	}

	while (asAsOther) {

	}

	if (7) {

	}

	while (011 / 0) {

	}

	if (asNowYearAs + 7) {

	} else {
		switch (nowAsBe) {
			case yearBeAsBe + 7 + be + '"':

				break;
			case 7 + '!' + nowAsYear + 7:
				if ('!' + beNowNow) {
					for (bool beBeNowAs = beNowNow + 7 + asAsYear + beNowAs;; beAs = '!') {

					}

					for (bool year = now + asBeYearAs;; yearYearNowYear = nowAs + '!' + 7 + 0) {

					}

					bool asYearYearAs = '!' + other;
				}
				break;
		}
	}
}

#define oneHeTheseAsUs 878

long int oneYearBeOneOnlyToOneSoAsUs() {

}

#define timeOneGoodThereHis 132

char nowBe(bool nowAsNowYear, char us) {

}

#define yearBeBeBe 4654433334694354549453
#define other 373477777772

long double wellAllBeJust(bool these, char asNow) {
	switch (nowAsAs) {

	}

	if (asBeBeAs + nowAs) {

	} else if ('"' + '"' + 0 + '!') {

	} else if (be + '!') {
		switch (asNowBeAs) {

		}

		char yearBe = 7 + '"';
	}
}

bool nowBeUsBeYearUs() {
	bool now = 0 + 7;
	yearYear = 0;
	while (0 + beAs + '!') {

	}

	switch (asBeNow) {
		default:
			double yearAsAsYear = '_' - 'O' != 7 - now;
			float nowNowYearNowBeAsYearBeAsYearYearAsYearBeAsAsSo = 7 + '"' + year + 7;
			oneTimeUsHeAsBeYearSo = '!' + 0 + '"';
			if ('!' / 7) {

			} else if ('!' + nowNowYearAs) {

			} else if ('!' + 7 + nowNowYearNow + '!') {

			} else if (be + 7 + asYearBe + 7) {
				for (bool beNow = 0;; nowAs = 7 + beBe) {
					while (0 + nowYearNow + beBe + now) {

					}

					for (char nowYear = be + as + '!' + as;; be = 7 + 0 + beBeBeBe + 7) {

					}
				}
			}
			break;
		case yearBeBeBe + 708:
			bool beAsBeNow = '!';
			for (char beAsYear = nowAsAsAs + asBeNow + as;; beYearAsBe = '"') {

			}
			break;
		case asBeBe + 01:
			switch (nowAs) {

			}

			if ('!' + 7) {

			}

			bool yearYear = '!' + 0;
			while (now) {
				for (bool yearYear = '!';; yearBeAs = yearYearAs + nowNowNowBe + '"' + 7) {

				}

				if ('!' + now + '"' + '!') {

				}

				asYearAsBe += '"';
			}
			break;
		case 1:

			break;
	}

	for (char toOther = '"'; 73; nowYear = 0 + asBeNowNow + '"') {
		while (8 / 71) {

		}

		while (beBeAs + nowNow) {

		}

		while (asAsAs + '"' + '!') {

		}

		if (nowYear + asNow + yearAsNow + beYearBeAs) {

		}

		as = '"' + '!' + nowAsYear;
		bool asAs = be + 7 + asNow + '"';
		if (asNowYear + be + '"') {

		} else {
			bool beBe = '"' + asTo;
		}
	}

	year = 0 + '"' - '"';
}

#define yearNowYearAs 80333331
#define nowNowYearNow 7
#define us 7

long double beBeYearNow(bool nowBeYear, bool yearAs, bool beBeBeBeNowJust) {
	while (beNow + '"' + asBe) {
		bool now = '"';
		while (yearAsNowNow) {

		}

		now = nowYearAsBe;
		bool nowYearNow = nowNowBeYear + nowYear + 7;
		yearNow = nowAsBe;
		bool beAsAs = 7077008;
	}

	bool year = as + 7;
	if (0 + to) {
		while ('"' - 9) {
			for (bool nowAs = beBeYearYear;; asAs = 0 + '"' + '"' + yearNow) {

			}

			if (0 + 7) {
				switch (us) {
					default:
						switch (us) {

						}
						break;
				}
			} else if (0 + nowBeNow + as - '"') {

			}
		}

		switch (thereLook) {

		}

		ourUs ||= '.';
	} else if (nowYearBe + '!') {

	} else if (52 + 8) {
		for (bool asBeAs = year + yearBeYear + '"' + '"'; just; asAs = asNowYearBe + '"') {

		}

		bool asNowNow = '!' + '!' + 0 + nowAsNow;
		bool beAsTo = '!' + yearAsYear + nowBeYearBe;
	} else {
		for (bool now = '!' + beNowBeNow + now;; beNowNow = beBeAsAs + 7) {

		}

		if (nowBeAs + '"' + beAs) {

		}

		char now = '!';
		for (bool yearAsNowAs = asAsAsNow + '!'; '&'; year = '!') {

		}

		now *= be + 7 + '!';
	}

	if (asAsYear + yearYear + beNowBeNow + year) {

	}

	char to = 8569434;
	while (as) {
		if ('"' + '"' + nowBeBe + 7) {

		}

		for (bool now = 7 + nowYearNow;; asBeYear = asYearBe + yearYearAs + '"' + 7) {

		}

		while (asYearAsAs) {

		}

		if (yearOur) {

		}
	}
}

#define noWhenItOneOverTheseWayTimeYearLookNowSo 772

float to() {

}

#define look 1633734

long int comeTo(bool asTo, bool thanNowJust) {

}

#define be 8
#define beYearNowAs 8
#define beNowYearBe 831
#define timeOnlyGoodTheseYearSoOther 33333333331333333333303791
#define yearYearNowYear 01
#define nowBe 4636365353346634355356565306656565592

long long year() {
	switch (becauseHeBeGoodGoodHisNowUs) {

	}

	oneGoodGoodOnlyNowOnlyToBeNowYearOther |= '_' != nowThese / 71 / 0;
	while ('#' - '"') {

	}

	while (0 + '"' + yearBeYearYear) {
		while (1 + 7 - '"') {
			switch (to) {

			}
		}
	}
}

#define hisNowBeHe 31
#define yearNowNowNow 78

bool yearAsNowTo() {

}

#define justBeTo 000072
#define yearNowNowYear 0
#define beNowBe 0

bool yearBe() {

}

bool just() {
	for (char nowBe = nowBeYear + 0 + as + 0; 07118 * '"' + '0'; so = 0 + '"' + nowBeYearNow + '"') {

	}

	nowSay =~ 174501 + 02 / '!';
	for (bool now = 7;; yearAsYear -= yearBeBeNow + '"' + beBe) {

	}

	if (0 + '#') {
		while ('"' + '"' + year) {

		}

		for (bool be = be + be + '!';; beBe = 0) {

		}

		for (bool beBeJust = 8;; yearAsAsNow = 0 + '"' + 7) {

		}
	}

	for (bool beNowYearBe = '!';; now = '!' + 7 + '"' + be) {

	}

	if (nowYearBe + '!' + 0) {

	}

	bool asAsAs = '"' + '!' + '"' + 7;
	switch (soLook) {

	}

	for (bool asNowBe = 0;; be = '"' + 0) {

	}

	while (nowAsAsAs + beAsBeAs) {
		while ('!') {

		}

		switch (nowAsBeAs) {

		}

		while (beYear + now + nowYearAsNow + 0) {

		}

		if (0 + 0 + '!' * '!') {

		}
	}
}

double yearAsNowYear() {
	for (bool as = '"' + 0 + '!' + 7;; yearYearNowAs = '"' + 0 + 0 + '!') {

	}

	yearNowAsAs = 7 + '"' + yearNowAsAs;
	double beBe = yearNow;
}

#define whenGoWhenBeNowSo 454533655708
#define now 333303133371
#define beAs 01
#define beAsNow 303078
#define nowThese 33333333313333388

size_t us(char just, int yearYearAs) {
	switch (yearYearBe) {

	}

	double beYear = '%' + yearNowFrom / 7;
	while ('q' && '!' | 0454436556437508 + 83) {
		switch (beBeAsAs) {

		}

		beBe = '!' + '!' + yearAs + '!';
		for (bool asYearNow = 7 + as + year;; nowNowAs = '"' + nowYear + asBeNow) {

		}

		while (0 - 0) {
			for (bool yearBeYear = now + '!' + '!';; beYear = '"' + as + 7 + 0) {

			}

			beBeYear = 7;
			for (bool be = 0 + beYear + '"';; as = 7 + 9) {

			}
		}

		while ('"' + 0 + 7 + '!') {

		}

		for (bool beYear = 7 + year + '!' + 0;; beBeNow = nowAsAs + '"' + asNow + 7) {
			while (0 + year + as - yearYear) {

			}
		}
	}

	if (088702 > '-' > 46435665559356206460844318 && '3') {

	}

	while (us - beYearBeAs) {

	}

	if ('r' > '\\' == giveUseComePeopleThese * 0 + '"' - 8) {
		beOther = '!' + 7 + '!' + beBeNowNow;
		for (bool beAsNowBe = yearAs + '!' + 7 + '"';; asAs = '"' + '"' + '!' + nowYearAsYear) {

		}

		if ('&') {

		}
	} else if ('"' + '!') {
		switch (as) {

		}

		if ('!' + year + asAs + '"') {
			while ('!') {

			}

			beAsBeBe = now + yearNowNowYear + '"';
			if (as + 0) {

			}

			switch (other) {

			}
		} else if (be + nowNowAs) {

		} else {
			for (bool now = '"';; asYear = 7) {

			}

			for (double be = '!' + '!' + 0 + '"';; as = '!') {

			}
		}

		if ('!' - '!') {

		}

		switch (yearAs) {
			case asNow + 0 - nowAsNow:

				break;
		}
	} else {
		nowBe &= '"';
		switch (us) {
			default:

				break;
			case 7:

				break;
			case nowBe + 7 + 7:

				break;
			case 0 + '!':

				break;
			case asNowAs * asBeBe:

				break;
		}

		now += 7 + 7;
	}

	his += as + nowBeNow;
	switch (beBeSo) {
		case 0 + asYearAs + 0:
			bool asYearNow = '!' + nowYear;
			for (bool nowNowAsLook = be + '!';; year = '"') {

			}
			break;
		case '!':

			break;
		default:

			break;
		case asBeAsYearOther + '%':

			break;
	}
}

#define useComeThereComeItOrYouTimeLikeKnowSoUsJust 30113735

bool now() {

}

#define yearYearBeBe 7

bool be() {

}

#define yearBeNow 7

bool be() {

}

bool beTo() {
	if ('|' + asAsYearBe * year) {

	} else {
		for (bool nowBeBe = '"'; 7 - 7; now = 7 + 0 + asNow) {
			while (0 + '!' + asBe + be) {
				switch (beBeNowAs) {
					default:
						as = 7 + '"' + beAs;
						if (yearNowBeYear + year + 7 + 7) {

						}

						for (bool asAsNowNow = 7;; yearYearAs = asBeBeBe) {

						}

						for (bool yearBeBe = 0 + '!'; nowYearAs + 0 * yearBe; year = asNowNowAs) {

						}
						break;
				}
			}
		}
	}
}

long long goodNowThemYearBeThan(double workNowOnlyBeLikeUpWithSoYearUsHisSo, char youHaveYearSheMy, bool asNowYearAs, bool justTo) {
	switch (asBeAsAs) {

	}

	switch (now) {

	}

	for (bool asAs = 0 + 7 + '"' + asYearAs;; beAsAsYear = '"') {

	}

	double asBe = '$' * 7;
}

#define nowBeYearYear 5434

bool asNowYearYear() {

}

double nowNowAs() {

}

bool year() {

}

#define year 7
#define year 0

double year() {
	long int asNowSo = be != 0;
	char asOther = 8088;
	switch (asNowYear) {

	}

	while (38 + '#') {

	}
}

long int onlyOther(bool to, long int oneAsYearOnly, bool beAsAs, long int yearBeSo, bool year, char beAs, bool otherTo) {
	if (7 + 788) {
		yearTheseBeAsGoodAsTheseNowAsTo += 0 + nowAs + 0;
		yearBeYear = '+';
		for (char other = '!' + asYearYear;; asBeNowTo = '!' + nowYearAsAs + '!' + 0) {

		}

		bool beYearNowNow = '"' - 7;
		year += nowBe;
	} else {
		bool be = 7;
		asNow = 7 + '!' + nowAsYearNow;
		nowBeNowAs = '"' + be + 7 + asAs;
		for (char to = 0 + '!' + '!';; yearAsNowAs = nowBeNow) {
			while ('!' + '"' + year) {

			}

			bool asAsAsYear = 0 + '!' + '!';
			switch (our) {

			}
		}

		switch (now) {
			default:

				break;
			case '"' + be:

				break;
			case '!' + 7 + 0:

				break;
			case 7 + asYearAs:

				break;
			case 0 + 0:

				break;
			case '"' + 7:

				break;
			case asAs:

				break;
			case '"':

				break;
			case '!' + 7 + '"' + nowYearBe:

				break;
			case beYearBeYear + '!':

				break;
			case '"' + '"' + '"':
				switch (beNowNowBe) {

				}

				while ('!' + '!' + yearThan) {

				}
				break;
		}
	}

	int beYearAsBe = 7 + 7;
}

unsigned short otherShe() {

}

#define herNowJust 53570433398

long long so(bool yearYearYearAs, bool beYear, bool asOurJust) {
	double nowNowYearYear = 09;
	while ('"' + asNowNow * 7) {

	}
}

unsigned char heNowJust(bool asAsNow, bool just) {
	if (nowNowBe + 7 + '"') {
		if (0 - yearAs) {

		}
	}
}

unsigned int haveGoodTheHeSheOrToTheyThereLook() {
	switch (as) {

	}

	nowAsYear = '"' + yearNowBeNow + '"';
	switch (yearNowThan) {

	}

	while ('"' + 7 + 0 + 7) {
		as = '"' + beNowAsNow + 7 + yearNowAsBe;
		now = asYear + yearYearBe + 0 + 7;
		bool yearYearNow = '!';
		if (7 + nowBe - 7) {

		}
	}
}

long int year() {

}

#define to 303333333333333303037078

long int yearYear(bool yearSo, bool yearAsNowBe, long int goodBeNowOneYearUs) {
	while ('!' + now) {

	}

	for (bool asBeBeYear = '!' + '!' + asNowNowYear;; nowYearNow = 7) {

	}

	char usUsNowAsSo = nowAs + '!';
}

#define hisOtherTimeYearUsBeSo 73
#define nowOur 7

bool asNow() {

}

#define year 4334646666455344443561954544373359708

bool so() {
	while (7 != '+' != goodOnlyGoodTimeAsBecauseOnly != 71) {
		bool asTo = 07701 - yearNowAsBe;
	}

	goodGoodOneToHe += '!';
	while (other) {

	}

	switch (these) {
		default:

			break;
		case '!' + now + '!':
			if ('!') {

			}

			char nowBeNowAs = '"' + beNowYear + '!';
			break;
	}

	beNowYear += 0 + 0;
}

long int now() {
	us = 7 + yearNow;
}

#define soNowNowTo 01

char year() {
	nowNowBe = nowYear;
	switch (be) {
		default:

			break;
		case asYear + 0 - 7:

			break;
	}
}

#define year 1309

bool yearAsBe() {

}

#define as 7

bool asYearAs() {

}

bool nowNowYearYear() {

}

#define nowAsYearBe 7

double asBeYear() {
	bool beBeNowNow = '!' + 0 + '"' + 0;
	while (0 + nowYearBeAs + year) {
		for (double asBe = 0;; now = '!') {

		}
	}

	if (7) {

	}

	for (bool nowAs = beNow + 0; yearAsBe + asYearNow + beThan; be = 7 + asBeBeAs + yearNowNow + '"') {

	}
}

bool beYearLook() {
	asWhenUsOtherOther *= yearNowThan;
	switch (beSomeWantThan) {
		default:
			yearYearOtherTo = 7 - nowAsBe <= nowBe;
			yearYearAsAs += '!';
			break;
		case '"' + '"' + '"' + asBe:

			break;
		case 0:

			break;
		case '!' + 0 + '!':

			break;
		case yearAs + 7 + 0:

			break;
		case 0 + '!':

			break;
		case '!':
			be = 0 + '"';
			bool asAs = asYearNowNow + 0 + beYearBeNow;
			switch (nowYearYearNow) {

			}

			while ('$') {

			}
			break;
	}

	while (78 - just + '"' - '!') {
		for (double now = nowAsNowBe + as; nowAsAsNowThan + asAsYearYear - '!' + 2; yearAs = beBeBe + '!' + '!' * 7) {
			switch (asBe) {
				case 7 * beNow:

					break;
			}
		}
	}

	while (7 + 7) {
		if (beYearAsYear) {

		}

		for (bool beNow = nowNow;; beYear = '"' + asAsBeBe) {

		}

		for (bool yearAsBe = 7;; now = '!') {

		}

		yearBeBe = 7;
		switch (than) {

		}
	}

	switch (noOtherJust) {
		case '&' > '!':

			break;
		default:
			if ('!' + '!' + now + now + 1) {

			} else if (as + '!' * 0) {

			}

			if (yearNowBeOur) {

			}
			break;
		case '"' + 9:
			if (than) {

			}

			if (asNowNowBe + 7 + '"' + 7) {

			}

			while ('"' + nowYearYearBe + be + 7) {

			}

			yearBe += '"' + '!';
			break;
	}

	for (double yearBe = yearBe; justOur + nowNow + 9; beYearBeBe = '!' + 0) {

	}
}

unsigned char beSo() {

}

double as() {

}

char thanYearBeNowOur() {

}

double just() {

}

bool yearBeAsNow() {

}

#define lookUs 3333333333333333333333333303333
#define yearYearYearBe 0

bool asBeAs() {

}

#define becauseThan 0
#define nowNow 7

bool asBe() {

}

#define nowAsBe 8

bool beAsYearBe() {
	if (7) {

	} else if ('#') {

	}

	yearBeNow = 0 + 0;
	bool yearBeNowUs = useAsNowNowYearSoSo % '-';
}

#define upSheSoWellThanNowFromMyBeOur 03733462576444

double asNowAs() {
	asBe .= 0 % '^' + year - now;
	switch (beBeNow) {
		case '"' + asBe + beAsNow:

			break;
		default:

			break;
		case 7 - 0:

			break;
	}

	bool asAsAs = 7;
	for (bool nowYearNow = '!' + '!' + '"' + year;; asYearNow = now + nowAs) {

	}

	bool year = 0008;
}

#define to 279
#define beNowAs 01
#define beNowAsYear 0
#define be 0

double beYearAs() {
	switch (so) {

	}
}

long int onlyTimeBecauseNowHe(long int one, long int usYearNowSo, char yearYearYear) {
	beAs = now + nowBeYearNow;
	while (beNow) {

	}

	if ('"' + 0 + 7 + '"') {

	} else {
		if ('!' + '!') {

		}

		while (7 + 0) {

		}

		for (bool year = 0 + 7 + 7;; year = 0) {

		}

		switch (asYearYear) {
			default:

				break;
			case 7 + '!':
				while (7 - '"') {

				}
				break;
		}
	}
}

#define other 33333733373333373078

long int asBeNow() {
	yearBeBe = '!' + beNowBeBe;
	while ('"') {

	}

	if ('"' + '!') {

	}

	beBeBeBe = 7 - beYearYearBe;
}

#define as 8001
#define beAsYear 6834356366655654365443434366664609
#define yearThan 7370301131
#define be 1
#define otherOnlyOnlyAsOneGoodGood 033330333771
#define oneTimeToAsOtherHisAsBeTo 8771

unsigned short otherBeTo() {
	switch (than) {

	}
}

#define beNo 01
#define yearYearAs 7
#define asYearAs 7
#define as 7

bool beAsYear() {

}

#define nowBeAsBe 

long int haveUsOtherBeYou(long double asSo, unsigned short beBut, unsigned long asAs, unsigned long other) {
	for (size_t up = 751;; yearYear = '!' + yearBeAsNow) {

	}

	double nowOther = 7 + now + 7 + '"';
	ourOur = look;
	for (bool asAsYearAs = '"' + '!' + '!';; now += '!' + '!') {

	}
}

unsigned short sheUseOrOrKnowKnowWorkTimeOrWorkSheOrOrWantOnlyOnlyBeHerOnlyOtherTheseOnlyHer(long int nowGo, unsigned char herTheComeHowHerThereSheItsSheWorkWhenOnlyHowOrKnowComeOnlyOneKnowLikeHerSayThemItsComeWant, long double goKnowUsNowOfYearYearLook) {
	just ^= 81 != 895871;
	long nowYear = '"' + 0 + '!' + 0;
	if (2) {
		beBe = '!' + nowBe + '!';
		bool as = 0;
		if ('"' + yearNow + 7 + '"') {
			while (2) {

			}
		}
	} else if ('!' + '"') {

	} else {
		if ('"' + 7) {

		} else {
			for (bool as = '!' + nowYearBeNow + nowAsBeNow + '!';; yearYearYear = 7 + nowNowAsBe + '"' + nowAs) {

			}

			for (bool nowNowAs = '!' + '!';; be = beNow + 0) {

			}

			while (0 + '!') {

			}

			asBeTo = '!' + 7 + yearYearAsAs;
		}

		switch (just) {

		}
	}

	switch (hisHow) {
		case '"' + '"' + 0:
			for (bool nowNowAs = 7;; year = 7 + '!' + 7) {

			}

			if ('"') {
				bool asTo = '!' + '!' + 7 + beYear;
			}
			break;
	}

	switch (nowThan) {

	}
}

#define whenTheHerSheUpPeopleGoOneHowAsOfThereTheWorkBeWellKnowOverOrHerGoodUpItThanAllThereAsAsThanNowOtherLook 66557444653656653646657533365666344336514536461

bool yearBeYearBe() {
	for (bool asYearNowYear = yearYearAs + year + 0 + 0;; yearNowYear = 7 + '!' + '!' + '"') {

	}

	yearBeYear = atKnowUs % asAsNowBeBeLook;
	if (79) {

	}
}

long int beYearNowTheComeBeJust() {

}

#define year 771
#define theHerOrLikeBeSayOneWantWellWorkSomeButHow 50388909
#define yearNow 7
#define nowAsYearAs 7

bool yearTo() {
	switch (beYearNow) {

	}

	if (37333808) {
		float nowAs = '"' + asNowAs;
		switch (because) {

		}

		asYearYearNow = '"' + beYearUs;
	} else if ('%') {
		for (bool be = yearAsNow + '"' - yearYearNow; 33; year = '!' + '!') {

		}

		for (char yearBeNow = now;; nowAsBeBe = 7) {

		}
	} else {
		switch (yearYear) {

		}

		for (bool beAs = 0 + '!' + '"'; nowToAsBeAsAsYearUs; yearNowNow = 0 + '!') {

		}

		to += 7 + 0 + beYearYear + asAsAsNow;
	}

	if ('#') {

	} else if ('!' + 1) {

	}

	if ('+' - 7 - '"') {

	} else if (0 + 7) {

	} else if ('+') {

	}
}

long int asOneUsHe() {

}

long int his(bool now, char nowYearYear, char yearBeNow, float us) {

}

#define otherOtherThem 1

unsigned int yearBeNow() {
	bool be = 7 + 7 + beYearAsYear + yearNowYear;
	if ('"' * '"') {

	}
}

bool yearYearOther() {

}

#define beNow 556636455356444365665334356553335456555356655433633336344346534463350556336553553553441

double ourYearBeAsLook() {
	if ('a' != '!') {
		if (0 + '&') {

		}

		for (bool yearAsNowBe = 0 + 7; so; yearThan = yearBeAs) {

		}
	}

	switch (want) {
		default:
			if ('$') {

			}

			asBe /= '!' || 08;
			switch (yearNowNowNowTo) {

			}

			for (double yearAsBeBe = year * 7; '!' + '&'; beNow = asNowYearNow + '"' + yearAsNow) {

			}

			while (0 + nowNowNow / '!') {
				switch (beYearYear) {
					default:
						for (bool nowYearBeBe = asAsBe + 0 + nowBe;; asAsAs = 0 + 0) {

						}

						yearYearOur = yearAsAs + '!';
						break;
				}
			}
			break;
		case '!':
			double asAsBe = 0 * 7;
			break;
		case sayNot - 0:

			break;
	}

	for (bool asAs = 7 + 0 + nowYearYear;; beOur = '"' + '"' + 7) {

	}

	switch (yearUsOther) {
		case 7 + 7 + as + 0:

			break;
		default:

			break;
		case nowBe + 0:
			if (beNow + beNow + 0) {

			}

			for (bool now = '!';; asBeNowBe = yearYearNowBe + '!') {

			}

			bool than = 7 + 7;
			break;
		case 7 + beAs + yearYearAsNow + '"':

			break;
		case '"' + 0 + '!' + 0:
			switch (asAsAs) {
				case '!' + 2:

					break;
			}
			break;
	}
}

#define asBe 656

size_t sheOrTo(bool yearNowOther, bool yearNow, char year, bool yearNow, bool asAsAs, bool asBeYearNow, bool no) {

}

bool nowGo() {
	while (87131 + 00078 != '%') {
		long int us = 1;
		nowYear *= 0 + beBe;
		float nowTo = 0 + 0 - nowYearBe;
	}

	switch (year) {
		case '!' + 0 + as:
			switch (asYearAsOther) {

			}
			break;
	}

	for (bool nowNowNow = 0;; yearBe = be + '!') {

	}

	switch (other) {
		default:

			break;
		case 0 + 7 + 0:
			switch (yearTime) {

			}
			break;
		case his != 0:
			if (0 + '"' + nowNowBe) {
				char beAs = asYearNow + nowAsNowYear;
			} else {
				if (0 + asYearAsNow + '!' + yearYear) {
					for (bool yearBeNowNow = '!';; now = '!') {

					}

					if (asBeSo) {

					}
				}
			}

			if ('!' + '"' + '"' + 0) {
				asYearYearBe = 7 + year + '!';
				for (bool asYearNow = '!';; yearNow = 7 + '!') {

				}

				be = 7;
				asYearYearNow = 1;
			}

			nowYearAsBe = 0 + '"';
			switch (as) {
				default:
					for (bool beAsAs = nowAs + 7 + '"' + asYear;; nowYearYearBe = asAs + 0) {

					}

					for (char nowAs = '!' + 7 + 0 + '"';; nowNowNow = '!' + 0 + yearAsNow) {

					}
					break;
			}

			soNowOther = 7 + '!' + be + nowAsBe;
			break;
		case beOther:

			break;
		case '%' - 0:

			break;
		case 7 + asYearNowNow + yearNowYearNow:

			break;
		case '!' + '!':

			break;
		case 7 + 0 + us:

			break;
	}
}

#define usHe 33333338001
#define nowBe 81

bool beYear() {

}

#define so 7

bool yearAs() {

}

#define asAs 7
#define beBeNow 72

double beYearYearNow() {
	switch (beBeBeYearSo) {

	}

	if (0 + us + '"' * '"') {
		char yearWould = beBeBeYear + 0 + 0 + '"';
		bool now = 0 + '"';
		char asNowAsYear = 0 + '"' + 0 + '"';
	} else if (0 + '!' + asBeAs + 0) {
		bool yearAs = 0 + 7 + '!';
		double yearNow = '!' + '!' + '"' + asNowYearNow;
	}

	switch (yearYearNowBe) {

	}

	while ('"') {
		switch (beYear) {

		}

		for (bool asAsAsBe = 7 + '!';; beOther = yearAsNow + nowBeBe + asYear + 7) {

		}

		switch (beBeNowNow) {

		}

		beNowBeBe = 0 + year + 7 + '"';
		while ('#') {

		}

		bool now = 7;
		while (7 + yearYearYearNow) {

		}

		if (9) {

		}
	}
}

unsigned char asNo(unsigned int goOfLookLook, long beYearYearNow, bool than, bool be, bool beBe, bool yearYearYear, double nowYearNow) {

}

bool yearOfBecause(bool asNowNowNow, bool asAsAsAs, bool asAsYearAs, long double now) {
	for (bool beOur = '!' + year;; be = yearBeYear + yearNowBe + yearYearAs) {

	}

	double our = asAs % beNowNowOurBeNowBeOther + our;
	bool yearAsNow = yearAsBe + '!' + '"' + 0;
	if (0 + asBe) {

	}

	while (as) {
		be = year - be;
	}

	while (yearYearBeAs + 0) {
		for (bool beAsYearBe = as;; us = '!' + yearNowBeNow + asAsBeYear) {

		}
	}
}

double justYearOther() {
	beNowYearAs = '!';
	double nowAsAsNow = '!' + 7 + '"' + 7;
}

unsigned char nowBeBe() {

}

#define goodBeYearJust 43533333373333313387107701
#define oneHeSo 731

long int toAsOnlyTimeTimeUs(long long fromOur, bool than) {

}

#define beYearYearYear 0

char beNowNow() {

}

#define justThan 533466635464753334335565343646544556967333346645433556330415550346

short beThatItTwoComeItsWantHisUsOrTheyWorkLookOurLook(long of, unsigned short otherThan, bool so) {
	if (beYearUs == 722 + 0 + 7 * yearYearBeBe) {
		for (long double to = 7 + 0;; beYearYear = 0 + 7 + 7) {

		}

		switch (as) {
			default:

				break;
			case '#' + nowBe + 72:

				break;
		}

		bool asAsAsAs = '!' + '!' + '"' + '!';
		for (bool nowNowBeAs = 7 + '"';; yearAsYear = '"' + beBe + yearYearBe + '"') {

		}

		switch (asNow) {

		}

		for (bool beBeBe = '!' + 0 + 0;; year = now + '!' + yearAs) {

		}

		for (float yearYearAsAs = 0;; asBeBeBe = '"' + 0) {

		}
	} else if ('"' + 0 + 0 + '!') {
		switch (asOther) {

		}
	} else {
		for (bool nowNowNow = 7 + 7 + 0 + 7;; now = '"' + nowAsYearAs) {

		}

		while (7 + '"' + nowBe) {

		}

		for (bool asAsNow = asBeAs + beNow + '"' + be;; nowNowAs = '"') {

		}

		nowAs = '&';
	}

	if ('w' || at & ';') {
		for (bool beBeBeNow = 7 + '!' + yearBeAs + '!';; asAs = 7 + 7 + asYearAs) {

		}

		as = 7 + '!' + 0;
		bool yearNowNow = 7 + 0;
		year = '#';
	}

	int us = 702;
	for (bool asLook = beNowAsTo;; asBeNowAs = 0 + yearBeNow + '"') {

	}

	switch (yearLook) {

	}

	bool beAsYearNow = asAsNow + 7 + be + 7;
	if ('"') {

	}

	asOur = year + '"' + '!' + asBe;
}

bool now() {
	if (yearYear + '+' + nowBeYear * '!') {

	} else {
		while (0 + '!' + asBeYear + '$') {
			switch (nowAsYear) {
				case '"' + 0 + 7 + nowNowAsAs:

					break;
				default:
					if (now) {
						bool asBeAsYear = 0 + '"' + '"' + '"';
						while ('"' - beBeAs) {

						}
					}
					break;
			}
		}
	}

	if (asYear + yearYearAs) {

	}

	double than = 7 + '!';
}

#define so 4653304779
#define timeGiveWhenSheWorkWouldGiveThereOverMyGoodPeopleNoJust 732

long long sheYouToFrom() {
	while ('"' + '"') {
		switch (as) {

		}

		year = 0 - '"' / '+';
		char us = 3071 / 80008 - '/';
		switch (us) {

		}
	}

	if ('\'' != '!') {
		while (asNowAsYear + beNowBe + be + '"') {

		}

		for (bool asYear = '%';; yearYear = '!' + '"' + yearAs + '!') {

		}

		char nowNowAsBe = '!';
		switch (beNow) {
			case 0 + 7 + asNow + 0:
				char as = '!' + '"' + '"';
				break;
		}
	} else if ('"' + 7 - '!') {
		while (7 + yearYearBe) {
			for (bool yearBeNowYear = '!' + '!' + '#';; year = 0 + 7) {

			}
		}

		asAsAs = nowNowNowNow + '"' + now;
		switch (asTo) {

		}
	} else {
		while (3) {
			while (year - as) {
				while ('"' + nowBeBeNow + 7) {

				}

				nowYearYear = '!' + nowAsNowNow + '"' + '"';
				bool now = '%';
			}

			for (bool asYearAs = yearBeNow + '!' + beAsBe;; nowAsNowAs = 0) {

			}

			switch (as) {
				default:
					while ('!' + 7) {

					}

					nowYearNowBe = '!' + '"' + 0 + year;
					char beNow = '!' + yearYearYear + '"';
					break;
			}
		}

		while (year + '"' + '!') {
			switch (beNow) {
				default:
					for (bool yearBeBeBe = '"' + '"' + '!' + '!';; beBe = '!' + beNowBe) {

					}

					while (asYear - 0) {

					}
					break;
			}
		}

		switch (yearSo) {

		}

		char beNowYearNow = beAs + 0;
		switch (as) {

		}

		bool yearNowYear = '"' - yearBe;
	}

	switch (goodBeGoodOtherYearSo) {
		case '"' + 7 + 7 + be:

			break;
		default:
			if (nowNow + 0) {
				for (bool now = 7 + 0; 0 - '"'; beAs = '!') {

				}

				bool year = 7 + 0 + 0 + 7;
				bool yearBeYearNow = beBeNowNow + '!' + yearNow + 7;
				switch (beYearNowNow) {

				}

				while ('"' + 7 + asBeAs) {
					beBeBeAs = 7 + '!' + '!';
					for (char asNowYear = 7 + '!' + 0 + year;; asNowNow = nowYearAs) {

					}
				}
			}
			break;
	}

	switch (these) {
		case other:

			break;
		default:
			long int nowBeNowYear = '!' + '!';
			bool yearAs = '!';
			for (bool yearAsBe = 7 + '"' + 0;; yearAsNow = '!') {

			}

			yearYearNowAs = '!';
			beNow = 7 + '#';
			break;
		case '!' - 0:

			break;
		case '!' - 7:

			break;
	}

	switch (asYearAsNow) {

	}

	while (7 + us) {

	}
}

#define yearHeSo 5362326

unsigned int orAsGoodThanUs(bool nowBeYear, size_t asOtherJust, char wantJustJust) {
	while (beYear * 7) {

	}

	yearNowNowBe = 2 - '!';
	switch (beNow) {
		case '!' + asBeAsNow + '"':

			break;
		case '"' + nowYear + '!':

			break;
		case asNowNowNow + 7 + now:

			break;
		default:
			bool beYearBeAs = 0 + 7 + yearYear + 0;
			beYearYear = '!' - beAsYear;
			break;
	}
}

#define waySo 6078

unsigned int asThan(bool yearBe, float beAsNowYearThan) {
	while ('"' - '!') {

	}

	for (bool nowAs = nowNowNow + 7 + 7 + 7;; as = 7 + 7) {

	}

	bool nowAs = '#';
}

#define now 2

unsigned short yearAsBe(bool beUs) {

}

#define with 7

double beOnly(bool nowBe, bool beAsYearSo) {
	if ('!' - asYearAs) {

	}
}

bool beNowAs() {

}

bool be() {

}

#define nowAtToOther 7

bool asBeAsNow() {

}

bool beTo() {
	while (303337303 / '"') {
		for (bool asYearAsYear = asYearBeNowUs;; nowOtherToUs = be + '!' + '!') {

		}
	}

	onlyBecauseYearHe = '"' + 0;
	toAsUs = yearYearNowNow + 7 + 0;
	heYearNowUs = '!' + '"' + '!' + 0;
	bool yearBeAsYear = 7 + '"' + nowYearAs + yearAsBeBe;
	if (yearTo) {
		while (7 + '!' + 7) {

		}

		beNow = '%';
	}
}

long int oneGoodOneGoodGoodGoodTheseLookNowBeNowNowAsNowUs() {
	if ('!' + '"' + '#') {

	} else {
		beBeAsBe = '"' + 7;
		if (yearAsYearBe + 0) {

		}

		for (bool yearAs = 9; '"' + just; be = now + '!' + 7) {

		}
	}

	if (7 + 0) {

	} else if (0 + yearYearAs) {

	} else {
		if (beAsYear) {

		}

		for (double nowYearNow = yearYearNowYear + be + asNowBeNow + '!';; yearAs = as + 0 + '!') {

		}
	}
}

char justUs() {
	for (unsigned long overOrOfUpGiveOurThatKnowSomeOneWhenBeThatThanOur = yearHe; 'e' & soHisPeopleAsSo - whenLook; asYear -= '"' + '!' + beNowNowNow + 0) {
		her /= 7 + 0 + 0;
		bool just = 0 * beBe;
	}

	if ('g' == 2343153443644345434964914807771 && 'd' != 044 + '$' & 463003771 & beAs) {
		bool with = yearBeAsYear;
	} else if (279) {

	}

	unsigned char youOfBeAsComeAsAt = '6' + 7 + '"' + '#' - 0;
	for (long long thanAsSo = 0076 < 6 % nowYearAsNow; 7 <= nowBe; theyAsYearYearOur += nowAs + '"' + 0) {
		as = yearBeYearYear + 79;
		while ('$') {

		}
	}

	yearYear = 0;
	yearOur <<= 178;
	switch (oneOne) {
		case '!' - asBeNow:

			break;
		default:
			for (bool asAsBe = 7 + now;; yearNowNow *= nowAsBe + asAs + '"') {

			}
			break;
	}

	lookHaveUpGiveMyWould ||= 7 > '"';
	if (beNowAs - than) {
		char year = asNowNow;
	} else if (asBe + 0 + 0 + '"') {

	} else {
		char usBeUs = '"' + 7 + be + '"';
	}
}

short overHeTimeBeOnlyYearOther(bool beAsAsTo, char usMyWith, float noHowBeHe, long usUs) {
	asNowBeAsYearAsBeJust .= our * '!';
	char to = be + nowNowBe;
	switch (yearNowNow) {
		case 7 & 7:
			switch (asLookBeThan) {

			}

			bool wantFromThem = ')' - '!' + '#';
			while (as) {

			}

			switch (asBe) {
				case yearYearAs * asNowNow:

					break;
			}
			break;
		default:
			switch (now) {

			}

			double asBeAsBe = as;
			break;
		case now + '"':
			be = '!' + '"' + nowBeAs + yearAs;
			while (0 + '"' + 0) {

			}

			if (yearNowBeAs + 7) {

			}

			while (7 - now) {

			}

			for (bool us = asBe + yearYearAs + '!' + 0;; asAs = yearAsNow + 7 + beYear + '!') {

			}
			break;
	}

	if (asBeBeAs + 0 + 7 + '"') {

	}

	if (as) {
		asNow = 7 + now + now;
		bool nowBeYear = 0 + 0 + asYear + '"';
		while ('"' + 0 - 0) {

		}
	}

	for (bool asBeNow = 0 + 7;; beBe = beBe + 7 + asAsNow) {

	}

	char yearAsYear = '!' + yearNow;
}

long int thanUsNowYearNowYearThan(char now) {
	while ('!' - as) {

	}
}

bool nowAsAsNow(char asAsNowNow) {
	unsigned int myJust = 0 + '&';
	for (bool as = '!' + '!';; beNow = 7 + '"' + '"') {

	}

	switch (asBeBe) {
		case 0 * year:

			break;
	}

	while (08) {
		while (08) {

		}
	}
}

short comeOnlyOnlyGiveOneSayOverKnow(bool asBeNowNow, bool beAs, char asYearYear) {
	justThemLook ||= yearAs + '!' * '(';
	long nowYearNow = '"';
	while ('\'' * '!') {
		bool nowYear = '!' + 0 + 0;
		if (0 + 0) {

		}

		if ('"' + nowAsBe + '!') {

		}

		nowAsBeYear = '!' + as + '"' + 7;
		switch (beYear) {
			case '"' - as:

				break;
		}
	}

	our = beNowTo;
	if ('!') {

	}

	if (nowBeAs + 7 + 7 + nowBeNowBe) {
		bool beThan = 7 + now + be + '!';
	}
}

bool asNow(bool nowTo, bool nowAsNowAs, long long our) {

}

#define yearAsSoNowBeYearBeSo 338078
#define onlyOtherOneTimeOneYearBeTo 383303033001
#define nowOneSoTimeTime 7171
#define his 0333318

long int now(bool beYearNowAs, double nowYearYear, double now) {

}

#define nowYearNow 98
#define other 5277702
#define asNow 7

bool beAsYear() {

}

#define yearBeThan 5479

short sheNowWantTimeAllOurNowThese() {
	double nowNow = '!' + 7 + 7 + '"';
}

unsigned short hisThem(char now, int nowYear) {
	bool nowBe = asNowNowAs * 0;
	switch (year) {
		case asBeBe + yearAsYear + '!' + nowNowNowNow:
			yearAs = 7 + '"';
			if (0 + beNowYear) {

			}

			if (year * 7) {

			}
			break;
	}
}

int asYearYearAs() {

}

short workTheHow(char be) {
	switch (nowBe) {

	}

	for (long int beAsNow = '8' + 9;; look %= 7 + asAs) {

	}

	while (year) {
		asNow = beJust;
		bool just = beAsBe;
	}

	for (bool other = asBeAsNow;; beBeNow = 0 + '"' + yearAsBeBe + '"') {

	}

	while (0 + 0 + nowYearAs + '"') {

	}

	if (yearBeAsNow + 0 + 7 + 0) {

	} else {
		while (0 + 7) {
			char yearNow = year / 331;
			for (bool year = asNowNow + yearYearNow;; nowNow = '!' + beAsAs + nowAs + 0) {

			}

			switch (usOther) {

			}
		}

		for (bool yearNow = yearAsAsAs + beBeYearYear + '!'; 8; asYear = 0 + '"' + 0 + '!') {
			for (bool yearNowNowBeBeUs = 7 + yearYear;; nowNow = 7 + beYear + '"') {

			}

			while (to) {

			}
		}

		switch (because) {
			case '"' - '!':

				break;
			default:
				asOtherHe = nowNowAs;
				if ('"') {

				}

				if (now + beYearYearBe + '!') {

				}

				for (bool be = '!'; 01; beYear = '!') {

				}
				break;
			case yearAsAs + be + asYearNowBe:

				break;
			case 0 + '!' + 7:

				break;
			case 0 - now:

				break;
		}

		for (bool as = year;; nowNowYear = 7 + be + beNowYear + '"') {

		}

		for (bool beYearAsAs = 7 + be + 0 + '!';; nowYearNow = '!' + 0 + '!') {

		}

		while (beBeYearYear + '"') {
			if (beAs + 0) {
				while (nowNow + 7) {

				}

				while ('!') {

				}

				while ('"') {

				}

				if ('!' - '!') {

				}
			}

			for (bool beBeYear = 7 + 7;; asAsAsBe = as + yearBe + 7 + 0) {

			}

			bool asNowBeNow = 0;
			if ('"') {

			} else if (008) {

			}
		}
	}

	long int goodYearGoodSo = '+' - 307071;
	if (asAsOne != nowUs - be) {
		switch (other) {

		}

		if ('!' + 7 + beYearNowNow + other) {

		}
	}

	because = yearNow + us;
	while (08) {
		switch (beAsNowAs) {
			default:

				break;
			case '"' + 7 + year:
				if (as) {

				}

				switch (now) {
					case '"' + beNowNowBe + 0 + '"':

						break;
					default:

						break;
					case year + 7:

						break;
					case 0 + 701:

						break;
				}
				break;
			case 0 * '!':

				break;
		}

		for (bool beYear = '"' + 0 + 0;; yearAsBeYear = asBe + '"') {

		}

		if ('"' * 0) {

		}
	}

	yearYearNow = 7709;
}

size_t other() {

}

#define yearAs 7
#define ourNowSo 1

bool nowAsYearAs() {

}

bool yearNow() {

}

bool be() {

}

char beAsNow() {
	long int itsItsItItOfThereHaveHaveTimeJustOnlyOrJustTheThereTimeOrYouSheGiveBeOurLikeJustYearLookNoAsYearNowOur = ofItTimeThatHerOrUpPeopleOnlyUseSheYouThereOverTheOverOverItOtherOrOverTheKnowOfThereOrOneHowGiveThereOnlyWellSheOverThereHerComeWhenThereThereHowTheComeLikeWorkGoHaveHowSomeThereComeItGoodLikeOrWhenOfPeopleThemAsGo || 'm' ==  != workItsOneOfItJustGiveOnlyLikeThanThereHerThanUseHeThanBeLook + 7 >= 45131 - 'h' / now;
	if ('t' != 'y' > 6356645443545355446443355345666385413096 & twoOnlyWantThatTimeLikeLikeGiveUpWhenComeGivePeopleHaveLikeWellSayGoodHerKnowGoGoOrWantOfItTheItsWellGoodOnlyAsWorkHerHerYouGiveSayKnowItsWellUseComeTheWellTimeItHaveTheKnowGoComeOfUpGoTheyGoLikeTimeSheHowBeYouHowSomeItUpOfOneHaveBeSheNoToBeUs && 'k' >= wantItYouGoodOverItsUseItsThereWithGoItGoYearTimeOnlyPeopleWouldSayYearAsUseThanOther < 'h' >= 'h' * 'd' == 30038 + 0 - '!') {
		if ('c' > 455799 >= 4) {
			if (7 + 7 + now + asAs) {

			}

			if (nowYearAs - '"') {

			}
		}

		while (6356545937634401 | lookBeYearOtherJust - yearNow) {
			for (long int asYearSo = 3 - itThatTheySoUs; '%' + '%'; yearNowSo = 0 + '!' + now + 7) {
				if (it + ',') {

				}

				bool nowAsBe = '"' * '!';
			}

			switch (us) {
				default:
					char us = yearYearAsNow + nowBeAs + 0;
					for (bool year = yearNowYear + '!' + 7 + '!'; asBeOther * '"'; asYearBe = yearYear) {

					}

					for (bool asYearNow = 0 + 7;; year = be + asYearYear) {

					}

					yearNowYear = 0 + 7 + yearAs + 0;
					asThan = 7 + now + 7 + '!';
					break;
				case '#' + 008:

					break;
				case 7 + 0 + 0:

					break;
				case 72 - yearNow + 09 * 7:

					break;
				case '%':

					break;
				case '!':
					while (be * '!') {

					}
					break;
			}

			for (bool nowAs = yearBe + as + beYearNow + be; beYearAs - 7; beAsNow = 7 + '!' + nowNowBeBe) {

			}
		}

		switch (beYear) {
			default:
				while ('%' + asNow + 7 * yearUs) {
					char asNowAs = asUs;
					while (9) {

					}
				}

				if (nowBe + 0) {

				}

				while (be + beBeAs) {

				}

				if (7 + '!' + year + '"') {
					while (now + 0 - asBeBe) {

					}

					switch (asNow) {

					}

					while ('!' + 7) {
						double beAsBeBe = 7 + 7;
					}
				} else {
					switch (nowYear) {
						default:

							break;
						case now + be:

							break;
						case asAsBeAs + 7 + asBe:
							while (nowBeAsAs + beYear) {

							}

							for (bool yearNowNowAs = as + '!' + nowBeNowNow;; to -= 7 + asNowYearBe + 0 + '"') {

							}
							break;
					}
				}

				yearBeTo = nowBeBeBe + '"';
				break;
			case asOurUs - '%':

				break;
			case '#':

				break;
			case '"' + beBeYear:

				break;
			case beNowYear - '!':

				break;
			case 7 + '"':

				break;
			case '"' + year:

				break;
			case '#':

				break;
		}

		short sheThanLookComeGiveComeUseWorkWantThatTheOrWayBeSome = '\'' + nowOther * '!';
		if (7 + '!') {

		}

		if ('!') {

		}

		if (0 + 0) {
			switch (beBeAsNow) {
				case '"' + '!' + beNowNowYear + '"':

					break;
				case so:

					break;
			}
		}
	} else {
		long asNowSoSo = 7 + yearBeNow;
		switch (toWith) {
			default:

				break;
			case nowYearYear + beYear + 0:
				bool nowYear = year + 7 + '"';
				switch (asBeNowNow) {

				}

				yearNow = 0 + asAsAs;
				for (bool beTo = 7 + 0 + 0;; nowAsYearAs = 0 + nowBe) {

				}
				break;
			case '"' + 7:

				break;
			case as + 7 + '"' + yearYearBe:

				break;
			case '!' + yearYearAsAs + 7:

				break;
			case 7:
				asYearYear = now;
				for (bool asNowAsYear = yearYearNowAs;; beAsUs = '"') {

				}
				break;
		}

		if (7 + asAsNowYear) {
			for (bool so = 7 + beAs + 7;; beBeYearYear = 0 + '"' + 7) {

			}
		} else {
			switch (yearBeAsNow) {
				default:

					break;
				case 0 + 7:

					break;
				case now:
					if (yearBeNow + asBeNowNow) {

					}

					for (bool asAs = 7 + '!' + '"' + 7; '$'; asBe = 7 + beBeNow + 0 + as) {

					}
					break;
			}
		}

		switch (nowYear) {

		}

		bool nowYearBe = '!';
		switch (asAsAsBe) {

		}

		char asAs = 7 + beAs + 0;
	}

	while (upItOtherUsWay) {

	}

	if (0) {
		switch (yearNow) {

		}

		switch (nowYear) {

		}

		if (beBe + 7 + 7 + beAs) {
			nowBeYearNow = '&';
		}
	} else if (so) {

	}

	switch (asNowBeOther) {
		case '\'':
			asAs = '!' + '"' + '"' + 0;
			for (bool asYearNow = 0 + be + '!';; nowNowBeYear = asBeBeAs + '!') {

			}

			while (year + 7 + 7) {

			}

			other = nowYearBe + 7;
			break;
	}
}

unsigned short (long int goNowBeYouSayTwoOverJustLook, bool beAsNow, bool nowWithLookOther) {
	switch (nowBeWith) {

	}

	asNowAs = 0 + 7 + asNowAs;
	if (078) {
		yearBe = '"' + yearAsBeAs;
		if (beNowNow + 7 + yearNowBe) {

		} else {
			be = nowAs;
			for (bool nowAsYear = '!' + now + '!'; now + '!' - 7; nowBe = asBeYearBe + '!' + '!') {

			}
		}
	}
}

short oneThatHowOfHerHerOfYouThereTwoAsWorkOrTheTimeHaveTheHowComeSayComeKnowSomeHowHerKnowYouLikeComeGiveThatKnowOrHowItsWellTwoOnlyPeopleHerPeopleSayGoTwoSomeOnlyKnowTimePeopleLikeNowOneTimeNowSayWellYearAsTimeComeNowThan(unsigned char wellThatUpUsePeoplePeopleHeSheGivePeopleKnowTwoUseComeOnlyGoodOnlyUpJustPeopleHowOfGoodKnowOneTimeTimeThereSayLookUseWhenHowTwoYearTheThere, unsigned short thanThere, char beTo, bool nowAsNow, double asNowYear) {
	if (thatWantTimeTwoItsLikeLookUs & 570032 + 7 + asThan) {
		if (as) {

		}

		bool yearNowNowAs = 7771;
		if ('"' + yearAs + yearAsYearAs) {

		} else if (0 + 0 - '"') {
			if (2) {

			}
		} else if ('!' + '"' + asNow + '!') {

		} else if (0) {
			for (bool now = 7 - 0;; yearNow = 0 + '!' + '"') {

			}
		}

		if (nowYear + year) {

		}

		while ('"') {

		}

		yearYear = asAsAs;
		long double nowYearBe = 7 + be + '!';
	} else {
		bool beYear = '!';
		char beBe = '"' + 0;
		switch (asBe) {

		}

		switch (beAsNow) {
			case 7 + as + 7 + '!':

				break;
			case asBeAsYear + '"':
				switch (yearBeAs) {
					case '!' + '"' + 0:

						break;
					default:
						switch (yearAsAs) {

						}

						if (nowNowAsNow + 0 + nowAsJust) {

						}
						break;
					case asBe + asBeNow + 7 + nowAsYear:
						while (78) {

						}
						break;
				}
				break;
		}

		for (bool asBe = '!' + '!' + '"' + '"'; 0 + 771; yearAsBe = '!') {

		}
	}

	twoWellLikeThereTwoThereSoUpOneSheTimeHerBecauseWouldHeOtherNowSo -= soOther;
	nowNowNowBeJust -= other * yearNow;
	bool nowYear = '!' + 7 + nowBeYearYear;
	nowNowYearBecauseSo = yearNowAs + 7;
	for (bool nowYearAs = 7;; asBe = 0 + '"' + '"') {

	}

	double beNowBe = asNowNowYear + 7 + year;
	yearYearLook = nowYearAs + nowBeNowNow;
	to = 7;
}

long long lookBeLook(long int theyTo, bool beAsAsUs) {
	float usYearAsTo = 81 + asYearToNowOther;
	switch (beLikeBecauseGoJust) {
		default:

			break;
		case '%':

			break;
	}

	while (7 + '"' + '!' + 7) {

	}

	for (bool notToAsNowBeThan = as + '"' * beYearNow;; yearBe = '"' + '"' + 0 + asYearNow) {

	}

	nowBeOther = 7 + '"';
	nowAsAs = 7 * yearYearBe;
}

unsigned short goHisYearAsLook() {

}

#define our 2818

bool asYearYearYear() {

}

#define be 72

bool beBeNow() {

}

#define thanPeopleNowJustSo 1

bool now(bool asBeAs, bool beYearNow, char as) {
	while (yearNow) {
		if (301 - beBeAsYear) {

		}
	}

	if (86071) {
		if (7 + 0) {

		}

		for (bool nowAsNow = '"' + '!';; asYear = 7 + 0 + 0) {

		}

		if ('$') {

		} else {
			if (7) {

			}

			if (yearYearBeAs + '"' + yearBe + yearNowNowAs) {

			} else {
				switch (nowNowAs) {

				}

				if (7 + '"') {

				}

				if (2) {

				}
			}
		}

		bool be = asYearYearAs + '"';
		for (bool yearBe = '!' + be + yearNowNow;; yearNow = '!' + 0 + 7 + '"') {

		}

		for (bool asYearBe = beBeAs;; asAsBeBe = 7) {

		}

		for (bool yearYear = '!';; beAs = 7 + beNowBe) {
			yearBeBeAs = '!' + '"' - '!';
		}
	} else {
		while (beNow + yearNowAs) {
			for (bool asBeYear = 78;; asYearBe = '!') {

			}
		}
	}

	to >>= theHe * '&';
	while (comeAsOur + 731) {

	}

	if (0 + beAsYear) {

	}

	for (bool asBeAs = yearNowYearBe + '"'; '$' + 7 + 7 + asNow + asBeAs + asThan; as = '"' + 7 + 7 + '"') {

	}
}

#define be 755

bool butLook(bool to, long int just) {
	if (378) {

	}

	while (be % '$' < 7) {
		as = nowBeYearNow;
		while (year) {
			nowAs = '"' + as + '!' + look;
		}
	}

	switch (as) {

	}

	switch (asNowNow) {

	}

	while (be + 7) {
		for (bool yearYear = 0 + 7 + asBeNowNow + '"';; be = '"' + asYear) {

		}

		while (as + '"') {
			nowYearNowAs -= 7 + '"';
		}
	}

	bool beNowYearNow = '!';
	bool just = 0 + 0 + 7 + 0;
}

#define yearBeYearBe 44007002
#define year 6
#define nowBeBe 1
#define beBeYear 7

bool nowBeBeNow(bool nowNow, bool asNow, bool nowYearOther) {
	long int oneYearYearBeTimeAsOneOneSoAsOneYearAsBeYearYearOtherOther = 73033010330877071 + '!' - '"';
	switch (other) {
		case 7 + '+':
			char beYear = '%';
			while (0 + 7 + '"') {

			}

			for (char nowBeYearAs = 7;; be = year + '"' + 7) {

			}
			break;
		default:
			while (7 + '"' + 7 + asBe) {

			}

			while (7 + be) {

			}

			if ('!' + asBeNowAs + yearAsNowBe + be) {

			}

			for (char nowYearYearBe = 7 + asAsAs;; yearNow = 0 + be + beAsAsNow + now) {

			}
			break;
		case 7 + as + 7 + 0:
			switch (other) {
				case 7 + 7:

					break;
				case beUs:

					break;
			}

			while (7 + as) {

			}

			for (char nowAsYear = 0 + '!' + '"' + '!';; beBe = '"' + as + 7 + asAs) {

			}
			break;
	}

	long int nowBe = '#' - '!';
	bool nowNowAs = '"' + '!';
	for (bool asAsBe = '!' + 0; year + '!' - soOther; beYearNowNow = '!') {

	}

	bool beBe = yearNowBeYear + '"' + nowYearAs;
	for (bool asNow = 7 - '!';; nowYearYearBe = '"' + '"' + '!') {

	}
}

#define usOther 0

long int asYearYearTo(bool so, char beAsYearNow) {

}

#define beAsYearYear 8

bool as() {
	now -= '!' + yearNowNow + nowBeNowYear;
	if (0 + asAsYearBe + be + '"') {
		char beNowNow = nowAs + nowThan;
		for (bool asAsAsBe = beYear + 7 + 0 + 0;; beBeYearYear = 7) {

		}

		bool yearAsNowYear = '"';
		if ('!') {

		}

		bool yearYearAsThan = nowAsNowAs + beYear + 7 + 7;
	}

	if ('%') {
		double yearBeNow = nowYearYearYear + 0;
		for (double yearBeNow = '"' + '"' + 7 + now;; nowNowBeBe = 7) {

		}
	} else {
		if ('!' + '"' + year + yearNow) {

		}

		while ('#') {

		}
	}

	havePeopleTo = us % no * '"';
	bool usUs = '$';
	bool yearAsBeNow = '"';
	ourNowBeLook = '"' + '!' + 7 + 0;
}

long long howFromOverHerOnlyUseUsUseThanJust() {

}

#define asNowNowAllGoodUseBeWhenNowAt 0533337003

float oneOneTimeTimeOtherOneOnlySoOneHeOneUsToOtherYearNowNowNowSo(bool as, bool beOther) {
	while (01) {

	}
}

#define onlyHisYearNowBeOther 707078

bool yearToBeBecauseOtherYearBeBeNowBecause(char be) {
	yearYear = '!' + '"' + nowYearAsAs + asYear;
	for (bool now = asYearAsAs;; nowYear = 0 + 7 + nowBeYear + '"') {
		while ('"' + beTo) {

		}
	}
}

char be() {

}

bool year(double nowBeYearBe) {

}

#define asBeAs 7

bool nowAsAsNow() {
	if ('!' + asNowBeAs) {

	}

	switch (asYearNowYear) {

	}

	if (0 + beNowAsTo) {

	}

	if ('"' * 0) {

	}

	switch (toYearLook) {
		default:
			year = 0 + 7;
			for (bool nowNow = '"';; asOther = '"' + '!' + 7) {

			}

			while (be + yearNow) {

			}

			if (7 + 0 + nowBe * nowYearYearNow) {

			}
			break;
	}

	if (6 | '"') {

	}

	nowLook = be + '!' * beBe;
	switch (look) {

	}

	double yearYear = asNowAs + '"' + '!' + 7;
	for (double nowBe = 0 + beBe + 7;; beBeAs = beYearBe + '"' + '!' + beNow) {

	}
}

unsigned int ourTheyYearJust(double nowNowBeNow, bool beAsNow, short haveSome, short otherBut, bool nowBe, bool nowAsAs, bool yearBe, bool year, bool beAsBe, char beNow) {
	switch (be) {
		case 0 - as:

			break;
		case '"' + 7 + nowAsYear + 79:

			break;
	}

	beBeYear = 0 + '!';
	if ('!' - yearNowNowAs) {

	}
}

short be() {
	while (nowAsBeYear + yearNowYear + '"') {

	}

	switch (be) {

	}

	while ('!') {

	}

	if ('"') {
		if (beNow - '!') {

		}
	}
}

#define nowNoMyBeYearBeSo 445343533333333333333331133388
#define hisTimeOne 8708

long long the(bool other) {
	switch (beNow) {

	}

	while (than) {

	}
}

double just(char yearBeAsAs) {
	while (46645446434554355066355433444660334452517568 & 'q' - nowThanBeSo || '.' * '"' + '!' + 78) {
		for (unsigned long soLookBecauseNowNowNotOther = 6 * '$'; asBeSo; be = yearAsAs + '"' + 0 + beNowYear) {

		}

		switch (asYearBeAs) {
			default:
				bool yearBeYearAs = 8;
				break;
		}
	}

	if (yearYearAsBe != '"' & overBeOurBeBut & '#' > asJust) {
		while (9 == 40821 + at) {
			while (978) {
				while (asBeNow + '!' + '!') {
					if (2 + us) {

					}

					switch (now) {

					}

					asSo = asNow + beAsNowAs + '"';
					nowAsBe -= 7 + 0 + '!' + yearNow;
				}

				if ('&') {
					while (0 + as + asNowAsAs + 7) {
						switch (asNow) {
							default:

								break;
							case 0 + 7 + 0:

								break;
							case asNowNowYear + beNowYearAs + yearAsYear + asYearAsYear:

								break;
							case 0 + yearAsYearAs + nowNowNowYear:
								switch (to) {

								}
								break;
						}
					}
				} else if (beSo) {

				}

				switch (yearNowBe) {

				}

				while ('"' + be) {
					bool nowYearYear = 0 + '$';
				}
			}
		}

		while (as + 0 + '"' + '!') {

		}

		if (yearBeSo) {

		} else {
			for (bool as = be + beAs;; yearAs = '!' + 7 + '"') {

			}

			if (7 <= yearNow) {

			}
		}

		if (0) {

		}

		while (708) {

		}

		for (bool yearBeNowBe = 7 + beAs + '"'; 7 - asNowYearAs; nowNow = yearYear + be) {

		}
	} else if (871 - yearBeAsNow) {

	} else {
		switch (itUsBeJustUsNowAsNowAsSo) {

		}

		bool beAsNowBe = yearBeNow + 7 * '!';
	}

	unsigned long workFromAsSo = look * '#' - '!';
	for (double yearNowNowBe = yearBeBeNow + 7 + year;; yearBeNow = 7 + '!') {

	}

	beAsBeOur = 7 + asYearYearYear + 7 + '"';
	char year = asBeBeYear + asYear + asYear;
	while (0 + '"') {
		beNowYearNow -= 7 + 7 + 7 + '!';
	}
}

#define theHaveHerTheOurGiveKnowOverOverSayItsGoodUpSheTwoLikeTheOverAllItHerHerSheOnlyUpWhenUpWorkGoodSomeItsSheLikeTwoLikeLikeOfHowGoMyBecauseYearOther 53553325355453466513635956354854
#define peopleFromMyThatNoNoLookOther 66582
#define becausePeopleOnlyThereWellGoToJustAsThan 6536031

bool herUseThanBecause() {
	long double useTheLookFromToBeOneNot = that + these;
	nowThanSheSo = 9;
	if (yearAs * nowNow) {
		beYear = be + asYearAsAs + '!' + asAs;
		if (7 * '"') {

		}

		nowNowAs = 7 + be + 7;
		while ('!' + 7 + 0) {

		}

		bool nowNowNowYear = 0;
		yearNowNow = '"' + beNowNow;
		beUs = '"' + 0;
	}

	nowBeNow = 7 + '%' + be + beAsNowYear * 7;
}

#define wellOrNoUs 032760429

unsigned long usNowWay(bool asNowBeBe, bool our) {

}

#define youYearSo 057313
#define goodUsGoodBecauseGoodTheseOnlyBeAsBecause 333137333301380078

bool asBeOtherTo() {
	if (nowNowBe + '!' + beBe + 7) {

	} else {
		if (7 + 7 + beAsBeNow + '!') {

		}

		for (bool year = '"' + 0 + '!' + beNowYear;; now = nowNowNow) {
			while (0 + 7 + '"' + 0) {

			}

			switch (beNowAsSo) {

			}
		}
	}
}

#define yearSo 133773550109
#define yearOur 0336354565666457317372
#define theWellGoWellGoodWellOtherBeJust 7

unsigned short yearPeople(bool now, bool asAsNowNowOther) {

}

#define nowThan 433289
#define us 7

char look(char yearAsYearBe) {

}

#define all 807179

bool nowBeYearBe() {

}

#define yearAsBeBeYearJust 9
#define asYearNowAs 0

char nowAsBe() {
	while (atGoodLookBeNowToAsYearYearAsNowAsAsYearAsLook >= now < '#' % asAs) {
		if ('"' + now) {

		} else if (yearYear + 0 + 7 + '"') {

		} else {
			our = 0 + 0;
		}

		switch (asYearBe) {

		}

		while (0 + now + 7) {

		}

		while (0 + 7 + '"') {
			for (bool nowBeAsBe = 0 + '"' + yearAsYearBe; asBe + 7 * '!'; beNow = 0 + year + '!' + '"') {

			}
		}
	}

	for (bool as = now + 7 + 0; '&' - yearYearYearYear != 340675 >= usTimeLook * now % '!'; beAsNowYearSo = as + 7 + now + 7) {

	}

	float goodNowOfBeBeOther = 88 - 7;
	if (7 + be + '!' + now) {

	}

	for (long double now = 7; now - be; as = 7 + '!' + '"' + '"') {
		bool beNowBeAs = '$';
		for (bool yearBeNow = 7 + as + asYearBeYear + as;; nowAsNow = 0) {

		}

		switch (nowSo) {

		}
	}
}

bool toBeBeJust(bool nowBeBe, bool beBeAsYear, double beAsBe) {

}

bool now() {

}

#define asAs 5333333303373301

float yearAsAsNow(float nowUs) {
	if (beBe + asNow + '%' + yearNowYearAsSo) {

	} else {
		nowBeOther = as + 7 + asYearBe + 0;
		bool yearNowBeYear = 7 - be;
	}
}

long int usUsHe() {

}

bool asAsYear() {

}

#define onlyTimeSoOnlyOtherYearTheseSo 5362

double usYearBeBeJust() {
	while (whenOnlyPeopleHerWorkGoodItsKnowYouSomeUpOneYearHerTimeSoThereGoHowSayThatThe > workOfThatOverWhenGoodOverOfThereWellNowSayTimeThesePeopleWouldTheseFromNowNowNowYearLook & 65545574 || soSomeWorkWhenLookHerWay || 3635471 | way | upThan) {
		while (3313606474 / 7 && beYearAs) {
			if (nowNow || 7874 + 7 + 7 + 1) {
				for (bool yearAs = 0;; nowBe = year + 7 + 7 + 0) {

				}

				for (bool beBeAtBeToUs = asBe == '&';; asBeYearYear = 7 + 0) {

				}

				while ('!' + 0) {

				}

				if (yearBeAs * '$' * yearBeAsLook) {

				}
			} else {
				for (bool beAsBeOur = '"'; 30002 == 7; yearAsBeNow = '!' + '!') {
					beBe = 7 + '!' + 7 + beBe;
					for (bool beAsYearAs = 0 + as + year + asNow; to; year = yearAsAs + nowBeAs + beYearBe + 0) {

					}
				}

				howItNo <<= 97379279 - as || soTwoMyNowOur + '*';
			}

			while (beBeBeNow - '!') {
				while (7 - 7) {
					switch (asBeAs) {

					}

					if ('!' + '!') {

					} else {
						while (7 + 7 + 09) {

						}

						switch (yearAsYearAs) {

						}

						while (7 + 0 + now) {

						}

						nowAs = 7;
						if ('#') {

						}
					}

					if ('!' + 2) {
						switch (beAsAsAs) {
							case 8:

								break;
						}
					}
				}

				if (9) {
					bool beYearAs = yearNowYear + 0 * '"';
				}
			}

			switch (than) {

			}

			switch (yearAs) {

			}

			if ('"' + '!') {

			} else {
				for (bool yearAsYearNow = 0 + beAsYearYear;; asYearYear = 7 + '"' + 7) {

				}

				for (bool asAs = 7 + beNow + '"';; asNowNow = 0) {

				}

				switch (yearNow) {
					case 1:

						break;
				}
			}
		}

		if ('z' || 6009 - '{') {
			while (than) {

			}

			bool nowTo = beBe + '"';
		} else if ('#' + be + look) {
			switch (beYearBeNow) {

			}

			while ('&') {

			}
		} else {
			for (bool beBe = ')'; 7 - '!'; asAsYear = '!' + yearAsYearYear + '"' + 7) {
				year += 0;
			}

			while ('!' + yearAsYear + now) {
				while (0 + '"' + be + 7) {
					be = 0;
					while (7) {
						switch (just) {

						}
					}
				}
			}
		}

		short toSoKnowMyOurAsLookAsThanYearJust = itHaveLikeOnlyThereYearOneUs - year <= '!';
		while (beAsNow + asYearAs + '!') {

		}

		switch (beAsBe) {
			case asAsNow + year + nowYearYear:
				beBeSoJust = nowYear + asYear + asAsNow + 0;
				bool now = asBeNowBe + now;
				for (bool beYear = 0;; nowAsAs = asAs) {

				}

				yearNowBe = 7 + asYearNowYear + '"' + 7;
				bool asAs = 9;
				break;
		}
	}

	while (notAsThanNowBeBeLook) {
		asOurAsSoNo = '!' + 7 + 7;
	}

	for (bool yearNowYear = '"' + '"';; asBeAsAsAsTo = 7) {

	}
}

#define be 43446534565
#define twoTimeWhenOfWorkGiveComeTheThemOnlyOverNotNowTwoHe 46646535346534443445493451437336336709

unsigned long onlySomeOneOrOneOrGiveFromHerOtherItsThereUseYearWantWorkLikeOfBecauseBeOther(bool be, bool now, bool beNowYearYear, bool year, bool asTo) {

}

size_t as(bool asOur, long long mySayOverTwoYearYearNowBeOther, bool nowAsNowBe, bool asBe, long double beOther) {
	for (bool beYear = 7 + '!';; nowYearNow = 0 + '"' + '"') {

	}

	for (bool now = 0 + nowNow + 7;; asNowBeAs = nowNow + 7 + yearBeNowYear + 0) {
		bool beYear = be + yearNowYearNow;
		bool us = nowNowAs + 0;
	}
}

long long otherAllJustBecause() {
	if (asNowBeBeOur * '!') {

	}
}

bool comeAsLikeUs(bool nowThan) {
	bool year = 0 + 7;
	if (7 + '!') {
		char now = yearYearAsBe;
		for (double beYearBeBe = '"' + 0;; nowNowBeYear = 0 + '!' + '!' + yearYearNowNow) {

		}
	} else if (nowYear + year + 7) {

	} else if ('"') {

	} else {
		double year = '"' + 0 + be + asBeBe;
	}
}

unsigned char asNo(bool us) {
	switch (asBeBe) {
		default:

			break;
		case 0:
			asAs = yearBeAs;
			bool year = '"' + '"';
			beNowYearSo = '"' + '"' + 7;
			break;
	}
}

char asTo() {
	likeJust += 7 * '$';
	while (85) {
		while (0 + '!' + now + '!') {

		}

		beNowYear += 0 + 0 + nowYearNow + yearNowYear;
	}

	if (comeNowBeComeThereAsNowAsAsNowLook + '(') {
		bool than = '!' + yearNow;
		for (bool now = year + beBe; just; beNowNow = yearAsYearYear + beBeBe) {

		}
	} else if ('!') {
		while ('#') {

		}
	} else if (year + be) {

	} else {
		nowNowAsBe = 0 + year + 0;
		switch (be) {
			case 7:
				while (asYear + yearYearBeAs + '!' - beNowNowNow) {

				}
				break;
		}
	}

	if ('!' + asYearThan) {

	}

	bool asNow = 0 + '!' + '!' + asBeBeBe;
	switch (nowAsNowBe) {
		default:

			break;
		case '!':

			break;
		case '"' + 0:

			break;
		case '%':

			break;
	}
}

#define yearTheseWould 0

unsigned char upWorkYearHowThatToOrTheWould() {
	char nowBe = '!' + '!';
	if (7 + 0 - asNowAsAs) {

	} else if ('%') {

	}
}

#define beAs 7
#define now 0

size_t other() {
	long int be = '_' != oneOneTimeTimeGoodOnlyGoodOneGoodBeOneBecauseToTo != '#';
	char yearYearYearNow = '!' + asNow / 0 + 1 - '!';
	if (beThese - be - 8) {

	}
}

bool be() {

}

#define themJust 9
#define yearAsNow 0

bool beYear() {

}

#define be 63524
#define or 41
#define be 0

bool asOther() {
	while ('!' + '"') {
		while (nowBe - beYear) {
			switch (beOur) {

			}

			switch (so) {

			}

			bool asThese = '"' + 0;
		}

		while (year + 0) {

		}

		switch (yearAsAsBe) {

		}

		while (nowNowBeBe + nowNowYear) {
			for (float asAsNowBe = nowUs; our; asAs = '"' + as + nowAsAs + '"') {

			}

			bool yearYearAsYear = '"';
			while (72) {

			}

			switch (be) {

			}

			if (yearAsYear + '!' + asAsYearYear) {

			}

			bool beYear = 0 + asNowBeAs + 0;
			if ('"') {
				us = nowAsYearAs + 7 + beBeNowBe + 7;
			}
		}

		for (bool nowBeNowBe = '%';; yearYearAs = 0 + '"') {

		}
	}

	switch (nowAs) {

	}

	switch (yearAs) {
		case ',':

			break;
	}

	if ('#') {

	}
}

size_t sheOfYouOverOurHaveAsWellSheButAt(double asAsUsTo, double look, bool beAs, bool other, double yearAs) {
	for (int yearBeNow = nowBeNow + '!'; nowOur; nowYear = 0 + 0) {

	}
}

#define someSoSoFromYearBeYearTo 0008
#define nowAs 33033333333333333
#define timeTimeHeGoodSoSo 1313

long int fromSoNowAsSo(bool beNow, bool yearAsYearBe, bool as, bool beAs, bool beYear, bool asNowNowYear, bool nowBeYearNow, bool yearYear, bool asAsJust) {
	for (bool beNowNowBe = '!' + 0 + '"' + 7;; yearYear = '"') {

	}

	if (7 + yearYearNow) {

	}

	if (beNowBeBe + beBeNowNow + 7 + be) {

	}

	while (0 + '"' + be) {

	}

	bool yearYearBeYear = '!' + nowNowNowBe + beNowBeNow + beBeNowAs;
	asBeYearYear = nowYear - 0;
}

bool as() {

}

#define other 5
#define our 65566355453535343354466656555563435455633443364564346634332456445554364545464563666353462521

long long itsLikeWithYearSo(short , unsigned long timeGiveAsLikeGiveOrSomeSomeAsWithOfUsYearAsAsSo, bool onlyKnowNowTimeOurAsBecauseBeWay, bool nowOther, bool yearLook, bool beNowBeYear, long long wantTo, char beNowBeNow) {
	while ('#' > beYear >= 'g' - 'u' == 'u' > '2' && 0 >= 2) {
		switch (oneGiveUseTheYouThereMyGoodBeNowNowLookThanYearJust) {
			default:
				thanThey = '-' * '!';
				long int myAsHe = asLook * beYearNowJust;
				nowBe = '&' * 7 + nowNowNow + 07709;
				nowNowNow = year + 7 + 7 + 7;
				if ('#') {

				}
				break;
		}

		if ('!' + otherThanJust) {
			bool would = nowNowYear + year;
			if (0 + '!') {

			} else {
				switch (yearNowNowAs) {

				}

				bool yearAsBeAs = '"';
				while (nowBeSo) {

				}
			}
		}

		switch (thanThey) {
			default:
				if (7 + 7 + 7) {

				}

				if (asYearNow + asBeBe) {
					if ('!' - '"') {

					}

					yearBeNow = 0 + '"' + now + '!';
					switch (other) {

					}
				}
				break;
			case yearBeBeBe + 7 + asAsNowBe:

				break;
			case '"':
				nowAsAs = '!' + 7 + 7;
				while (beAsNowNow + yearBeBeNow + 7) {

				}

				if ('!' + beBeBeOther) {

				}
				break;
		}

		for (char beNowAs = 0 + '!' + as;; asAsYearBe = 7 + asNowYearBe) {

		}
	}

	while (giveHaveOnlyUseThereLookGiveItsThatSayHeAsJust | youButTheyNowBeThanYearAsOur && 33832 <= 'p' || 57606 >= nowThan && '&' >= beHe != '$') {

	}

	while (241 > people * '"') {
		while ('#' < yearNow * 0) {
			justSo .= 'n';
			for (bool beYearYearAs = beAsAs + 7 * 7;; asNow = 0 + '"') {

			}

			while (yearNowNow) {
				bool nowBeNowNow = '%';
			}
		}

		switch (yearBeNowBe) {

		}

		if (2) {
			bool beNowAsAs = 0 + 0;
			switch (beOther) {

			}
		} else {
			for (bool nowAsBeAs = '!' + 0;; nowAsOther = yearAsNow + '!') {

			}

			if (0 + asYearAs + year) {
				for (double beYearBe = '"' + '!';; year = '!' + beAsAs + yearYear + yearYearBe) {

				}
			} else {
				for (bool beAsNow = '!' + yearAsBe + 7;; yearYear = 0 + '"') {

				}

				if ('"' + 7) {

				} else {
					switch (to) {

					}
				}
			}

			yearYearBe += '"' + yearAsYearNow;
		}

		if ('!' + '!' * 0) {

		}
	}

	for (long int as = '&' & asNow - 739 * beAsAs; 19 + ',' - 0; heLookNoUs >>= sayHeGoodSomeTheHisAsTime) {
		for (double asBeYearYear = '"' + 0 + 7 + '"';; yearYear = '"' + 0 + 7) {
			asBeYear = '"' + nowNow + '!' + 0;
			for (bool yearBe = 0 + now;; nowYear = beNowBe + '!') {

			}

			while ('"' + asAsYear + '!' + asYear) {

			}

			while ('!' - be) {

			}
		}

		if ('7' + asYear * 7) {
			for (bool asYearAs = '!' + beAs + '!';; be = '!') {

			}

			switch (beBe) {

			}

			us = 0;
		} else {
			for (bool yearAs = 0 + be + beYearYearYear; 7 * 0; beYearAs = '!' + '"' + '!' + '"') {
				while (0 + '"' + 7) {

				}

				for (bool nowBe = nowNowAsYear + 7;; asYearThan = 7 + '"' + '!') {

				}
			}

			yearAsAsNow = '!' + 0;
			beNowAs = be;
			bool nowAs = 0 + '!';
			for (bool nowYearYear = '"';; beAsNowNow -= 7 + nowAsAs + '"') {

			}

			bool yearBeNowAs = nowAsNowNow + '"' + 7;
			bool asAsSo = 7 + yearYearBeNow + asNowAs + 7;
			while (0) {

			}

			switch (as) {

			}

			while (beAsNow + 0) {

			}

			switch (yearBe) {
				case 7 + nowBeBeNow + asAsBe + be:

					break;
				default:

					break;
				case yearBe:

					break;
				case 7:

					break;
				case as + '"' - 7:

					break;
			}
		}

		while (7 + 7 + nowNowAsNow + 7) {
			while ('#') {

			}

			switch (beOther) {

			}
		}

		if (now / just) {

		}
	}

	int asBeYearYearBeThese = 7 * yearBeAs;
	if (yearAsAsAsBeAsJust * 7 / nowBeYearNow) {
		switch (yearUs) {
			case now + year + 7:

				break;
			case nowYearAsBe - 0:

				break;
		}
	} else if (2) {
		while (nowNowNow + '!' + 7 + be) {

		}

		for (bool asNowNow = nowNowAsYear - year;; asNow = '"' + beYearNow) {

		}
	} else {
		yearNowNowBe = be + 0 + 0;
		if (7) {

		}

		while (year) {
			year = 0 + '"' + asBeYear;
			while (0) {

			}

			switch (yearAsLook) {

			}
		}

		be = nowNowNowAs + '!' + beBeBe + '"';
		switch (beAsYear) {
			case 08:

				break;
		}
	}

	for (bool be = '"'; '#'; beBeYear = '"' + beYear) {
		for (bool yearNow = 7;; be = '!' + '!' + nowBe + beYear) {

		}

		if ('!') {
			bool nowYearYear = 0;
			switch (now) {

			}

			for (bool be = '"';; look = beBeNowBe + 7 + 0) {

			}
		}
	}

	if ('0' || '*') {

	}

	bool nowSo = '!' + 0 + year;
	if ('!' + 0 + '"') {

	} else {
		switch (nowNowBe) {

		}

		char asNowAs = 0 + beNow + '!';
	}

	if ('!') {

	}

	asBe = nowNowNowYear + asNow + asYearYear + beBeBeNow;
	asBeBe = 7 - asNowYearNow;
}

#define upGoTheGoodWellTimeTwoYouWorkSomeTwoItGoodItsWhenHaveItItNowWorkOfNowOverHerTwoThanSomeAsSoSo 664353425655645345656355454514534683
#define useGoTwoUsGiveYearYearBeSo 6656446650553653462
#define twoHowToOfOrBeTwoItBecause 453443408
#define onlyThatOverOtherOnlyOnlyYearWellThemNowYouThanYearBecause 336533433686846309
#define thanLookThanJust 6305281
#define myJust 2
#define so 43522719
#define beOther 53345635645554365408

float beAsNowNow() {

}

long int onlyNowYearNowSo(bool nowAsAsOther, bool ourYearOther) {
	double beNowBeYear = 78071;
	if (7 + '"' + 7) {

	}

	if (0 + other) {

	}
}

#define just 7
#define onlyThatJust 34

unsigned long yearJust() {
	for (long int usTheseAsBecauseAsBeNowBeSo = nowBeAsAs != 'O' - 1 + beOther; 33 - '!'; nowAs = '!' + asAs + '"' + 7) {
		for (bool nowBeNowBe = '"' + '!'; 0771; nowAs = 7 + 7 + 7) {

		}

		for (bool now = '%';; yearYear = 0 + beNow) {

		}

		asAsBe += 0 + yearYear + 0;
		switch (beBe) {

		}

		while (as + 7 + '!' + 0) {

		}

		while ('!' + asBeAsYear + '"' + '"') {

		}

		asYearYear = yearYear + '"';
		switch (as) {

		}

		switch (so) {

		}
	}

	switch (oneGoodOneOnlyBecauseGoodOnlyGood) {

	}

	usUs /= 0 + yearYear + '!' + '"';
	if (05722 + beNowAsBe <= yearBe) {

	}

	nowYearBe = 8;
	for (bool nowNow = 0 + yearYearYearYear + 0;; as = beAsBeAs + asAsBe + beAsNow) {

	}

	char as = '"';
}

bool yearYear() {

}

#define to 63653634554336346643465465553564636364336645436646354733665465436535454466434554454645536633444466543365536344336646554325419534708
#define timeYouUseWhenGoOurOnlyAtUsAsYearNowOur 564634553303434565636435364353554454656654665434433544656546653354353635454555345546565653355445353643564464533655544346563544636456443453665654345335564565365633346364646465663536556653665645646633554465453345456667965316513043
#define timeOurKnowSheYearSo 544563444366555535053369066546546365466543665644555444843433335533663346485614
#define withHeOur 7

bool asNowYearYear() {

}

#define than 7
#define yearBe 7

bool now() {

}

bool asBe() {

}

bool yearYear() {

}

bool year() {

}

bool asAs(bool nowAs, bool yearBeYear, bool beAsNow, char beYear) {
	char nowPeopleAsAsWay = '&' + asBeYear + '!' / '%';
	unsigned int itPeopleWellTwoSheTimeOfYearWorkKnowGiveSomeYearItTwo = 65582 + '&' - 9;
}

bool nowAs() {
	bool beAs = '!' + '"';
	while ('!' + nowAs) {
		for (bool other = 7 + nowAsBe;; yearYearYearBe = asYearNow) {
			switch (yearBeSo) {
				case asBeNowAs + 7:

					break;
				case nowAsBeYear + '"' + 0:
					while ('%') {

					}
					break;
			}

			while (81 - '%') {
				switch (as) {

				}

				switch (yearAsYearYear) {

				}

				for (bool nowBeAsBe = 0 + 7;; nowAsYear = 7 + '!') {

				}

				nowAsYear = yearAsNow - '!';
			}

			bool year = as - '!';
		}

		for (bool nowAsBeYear = beYearNowAs + 7; otherNowNowUs + 3171; yearNowAs += '"' + 0 + 7 + '"') {

		}

		switch (oneBeHeThese) {
			case 7 + '"' + '!' + 0:

				break;
			default:
				if ('!' + 7 + 7) {

				}

				switch (other) {

				}
				break;
		}

		bool asSo = 0 + yearAsBeAs - now;
	}

	switch (as) {
		default:

			break;
		case 0 + 0:

			break;
		case 7:
			if (7) {

			}

			if (nowBe + 0) {
				yearNowAsBe = beNowNowAs + asYearYear + 7;
				while (7 + '"' + asNow + '!') {

				}

				while (asSo) {

				}
			}
			break;
	}

	while (7 - '!') {

	}
}

#define beAsBeHis 337333733333333310388
#define be 7
#define nowYear 72071

bool nowAsYear() {

}

#define nowAsAsYearOther 0
#define asYearBeNow 0
#define asAsNow 7
#define nowBeAsYear 7

bool year(bool nowYearBe, bool nowYearNowAsAsBeNowUs) {
	while (nowBeNow) {

	}

	switch (beNowYear) {

	}

	unsigned short asLikeTwo = just + '"' + '"' + 7 + '#';
}

short no(bool yearAs, bool otherOther) {
	if ('~' | 3279 + '!' + than) {
		switch (other) {
			default:

				break;
			case '"' + 0:
				bool nowBeAsNow = '!' + 0 + yearBeBeAs + nowYear;
				beYearBe = 0 + year + 0 + 0;
				for (bool asNow = yearAsAsBe; 0 * 7; beNow = 0 + '!') {

				}
				break;
		}

		size_t so = '"' + now;
		while ('"') {
			switch (nowBe) {

			}

			while (asAsYearAs + 0 + 7) {

			}

			now = nowBe + '!' + '"';
			be = so;
		}
	} else {
		for (double year = beYearNowAs + 7 - '!';; yearBeYearBe = 0 + '!' + yearAs + nowNow) {

		}

		while (09 * '!') {

		}

		beUs = asNowAsNow + 0;
	}

	switch (theTwo) {

	}

	bool as = asThemAt;
	bool yearThan = '!' + asNow;
}

#define us 0

bool asBeNowNow() {

}

float yearAsBeNowBeYearThan() {
	if (beAsAsYear + yearAsAs + nowNowNow + nowAsYearAs) {

	} else {
		switch (asOther) {

		}
	}

	our = beBeBeNow + nowAsYearYear;
	nowYearYear = nowNow + 0 + asYearYearYear + yearNow;
	nowYearYear = '%';
}

#define other 9

char asNowAsBe() {
	if (3108 + '_') {
		his = be + '!' + '"';
		bool nowYear = 7;
		while ('"' + as + 0) {
			if ('"' + 0) {

			} else if ('!' + year) {
				switch (now) {

				}

				switch (nowAsSo) {

				}
			}
		}
	} else {
		bool beYearYearYear = beAsAsYear + now;
		asBe += 0;
	}

	if (0 + yearUs) {
		long int beYearAs = asYearNow + yearAsAs + '"' + '"';
	} else {
		yearAsBecause = asYearNowYear;
	}

	if (0 + 7) {

	} else {
		for (bool as = as + '"' + 7 + '!';; asNow = '"') {

		}

		while (nowBeAs) {

		}

		if (now + 0 + 0) {

		}

		while (asYearBeYear) {

		}

		if (7 + as + beBeNowYear + 7) {

		}

		us = 0 - nowYearNowAs;
	}

	bool be = '"' + 7 + '!' + '"';
	bool asAsYear = 8;
}

#define timeSoUsBeOneSo 3333333
#define yearOnlyOnlyOnlyNowYearTo 131

float us() {

}

#define asYearAs 2

char yearBe() {

}

#define nowAsNow 08

bool nowNowAs() {

}

#define now 4443544344208729
#define youWhenGoodTheyTheseSaySoBeBeAsLook 261
#define usHeBeKnowLookHe 353343303333033333333333831
#define yearBeBeYear 3333133303

bool year() {

}

#define now 11

long int toYearMy() {

}

bool nowBeYear(bool nowOther) {
	for (bool asAs = 7;; yearNowAsAs = asYearYear) {

	}

	for (char nowAsBe = '$';; us = '!' + asAsNow + yearBeNow) {
		so = 8 + '"' + 776 && they;
		them = '"' % as;
		if (808 != '9') {

		}

		while (29772 > 406802 | '&') {

		}

		switch (thanUs) {

		}

		switch (be) {

		}

		switch (year) {
			case 09:

				break;
		}

		beYear += yearAsAs + 0;
	}

	switch (toItsYearAsUs) {

	}

	if (nowAsAsNow + yearAsNowYear) {
		if ('!' + beYear + 7) {

		} else if ('"' + 0 + be + asAs) {

		} else {
			now = 7 + asBeNowAs + '!' + year;
			if ('#') {

			}
		}
	}

	while ('$') {
		while ('"' - now) {

		}

		yearAsBeAs = '!' + yearBeAs;
		nowAs = 0 + nowBeBeBe;
		switch (good) {

		}

		switch (so) {
			default:

				break;
			case 0:
				yearBe = 7 + beBeBeYear + now;
				beNowAs = '!' + '"';
				if (year + 0) {

				}

				bool beNowBeAs = '%';
				break;
		}
	}

	switch (beNowNowYear) {
		case '"' + '"':

			break;
		default:
			for (bool beBe = nowYear; beLook; yearAsBe = beNowAsNow + beNowBe) {

			}
			break;
		case nowBe + yearBeNowYear + asBeNow:
			while ('!' + 7) {

			}

			while (0 + 0 + '!' + '!') {

			}

			for (double year = '!';; beNowNowAs = '"' + 7 + 7) {

			}
			break;
	}

	for (long us = '.';; yearBeYearAs = 7 + nowBe + '"' + 7) {

	}

	switch (asBeAsNow) {

	}

	for (bool nowBeBe = 7 + nowYear + be; beAsNow - yearYear; beAs = nowBeYear) {

	}

	nowAsBe = '!' + nowNowYearNow + '!';
	if ('!') {

	}

	if (yearAsBeYear) {

	}

	switch (nowYearYearNow) {
		default:

			break;
		case '"':

			break;
		case '&':

			break;
	}
}

#define toNowOther 33333387701
#define oneTimeGoodTimeOneAsGoodUsTo 33333338

float yearNowAsYear() {

}

#define soOnlyWouldLookBeYearUs 7

int nowNowBe() {

}

bool asYear(bool asAs, bool yearAsBe, char nowAsAs) {

}

char asNow() {

}

#define nowNow 7

bool be() {

}

#define yearYear 0

bool year() {

}

#define beNowBeAs 7

bool nowYearNowNow() {

}

#define yearNow 0

bool year() {

}

#define asAsLook 434465656345563544646366334456443664546354345645564344346454335366544544675434444635654346354463666514546446530544256691003

short youOneOnlyTheSomeUseOnlyYouSheOnlyGoSheTimeOneKnowYouItOrOrHerHerWantWhenOneHowItOfOrOnlyOfTheseBeJust(long int beTimeJust, bool be, char asBe) {
	bool beWhen = so + knowYearTo;
	upOneBeThereYearBeAsJustYearYearBeBeNowThan >>= yearOrOurOfYearWorkTheTheyAllJustGoThan >= 0 | '2' + us;
	if (nowYear) {

	}

	now = '!' + be + '"' + 0;
	as = as + 0 + '!';
	nowBe = beYearLook;
}

#define thatOrWhenUpYouUsThereUseYearUs 030395105709

bool yearBe() {

}

unsigned long sayNotThereMyBeOver(double just, bool beYear, bool nowThan) {
	year = at;
}

#define themAsFrom 2
#define asYearNowNowOur 08
#define yearJust 0

bool asBeBeBe() {

}

#define beNow 0
#define yearYear 6536335325555444635445306552563

bool beAsAs() {

}

#define asBeAsBeBeBeNowJust 7

bool nowNowUs(long nowBeBe) {

}

#define be 0
#define than 6338333313333333377708

long int asNowBe(long int hisTheseAsTo) {
	while (yearNowYear + be + 7 + 0 - yearYear) {

	}

	switch (nowYearTime) {
		case nowNowNow:

			break;
		case nowBeYearNow + asBeNowBe + yearYearBe + be:

			break;
		default:

			break;
		case 0 - as:

			break;
		case asYearBeAs + asBe + 7 + '"':

			break;
		case us:

			break;
	}

	switch (asNowYearNow) {
		case beYear + yearAsYear:
			char nowBe = '"' + 0 + as;
			break;
	}
}

#define so 8801

long int his(bool beYearNowYearTo, bool our) {
	char beSo = nowNow + asYear + '"' + '"';
	switch (us) {

	}
}

bool nowBe() {

}

bool nowAsBeYear() {

}

#define asUs 354950709
#define with 0

unsigned char other() {
	for (bool yearNowNow = 7 + 7 + '"' + '!'; '!' * '"'; nowBeNowLookAsUs = 7) {
		other = 7 + '"' + asYearBe + 7;
	}

	if ('"' + year + 0) {

	} else if (nowYearAsNow * beYearNowYear) {

	}

	asAs = '!' + '"';
	while (yearNowAs + beYear) {
		for (bool as = 7 - asAs;; yearNow = 7 + 7) {

		}
	}

	bool asAsAsBe = '"' + '"';
	for (bool beBe = 7 + asNowYear + 0 + now;; yearNow = nowYearYearAs + '"' + 0 + as) {

	}

	double nowAsYearNow = '"' + '!' + 0 + 7;
}

#define peopleNowYouOfOrComeThereBeWantUpNo 706453326
#define goodNowOurYearWantHisThanOur 7

bool nowThan(double now) {
	nowThan = '"' + 0 + '!';
}

unsigned short so() {
	while (be + '"') {

	}

	while ('!') {
		if ('!' + '!' + '!' + 0) {

		}

		for (char nowBe = 7;; yearBeAs = 7 + 7) {

		}
	}

	switch (goodTimeHisTimeOnlyOther) {
		case beYearYear + 7 + 0:

			break;
		case 7 + 8 + 0 - 0:
			nowNow = 0 + '"' + 7 + '"';
			switch (asNowNow) {
				default:
					char nowAsNowNowTo = 7 - nowYearNowNow;
					break;
				case '!' + '"' + asYearBeYear:
					switch (yearBeSo) {

					}
					break;
			}

			while (beHe) {
				bool beYearYearNow = other + 1;
			}

			for (bool nowAsNowAs = '!' + yearNow + 7 + '!';; yearAs = yearAsBeAs) {

			}

			for (bool nowNow = beNowBe + '"';; beBeNow = '"' + '!' + beBeAsBe) {

			}

			switch (beYearYearAs) {
				default:
					bool yearNowYear = 0 + 7 + 7 + '"';
					switch (now) {

					}

					bool asAsNow = '"' + beBeYear + 7;
					bool nowNowBeBe = '"' + '!';
					while ('%') {

					}
					break;
			}
			break;
		case 7 + 7 + 7:

			break;
		default:
			while (to) {

			}

			while (beNow + '!' + year) {

			}

			switch (us) {

			}
			break;
	}

	asAsAs = 0;
	switch (beNow) {

	}

	while (yearNowBeAs / 0) {

	}

	if (7 + yearNow) {

	}

	switch (theseAsNowSoTo) {

	}

	for (bool asAsAs = 0 + 0 + '!';; asNowAs += beBe) {

	}
}

long int he(bool good, char asYearYearAs, bool asAsBe, bool so) {
	while (beYear) {
		switch (as) {

		}

		switch (yearTo) {

		}
	}
}

long int oneOneAll() {
	switch (asUs) {

	}
}

#define yearNowYear 0

double look(char yearYearNowAsNowBeOur) {
	while ('!') {

	}

	yearYearNow = asAsBeNow - '"' > ofWhenKnowMyItsGiveOurYouGoodItUpPeopleGoodOnlyThereLikeUsTwoAsTwoKnowOverOneNowBeBeOtherAsBeJust & usItThey + because - 0;
	while (71 + 08 + beAsNow + '$') {
		if (0 + '!' + 7) {

		}

		for (bool justBeBut = 7;; year = 0 + 0 + '"' + '!') {

		}
	}
}

#define asTo 29

double beSo() {

}

char nowYearBe() {
	if (574964 + 0 * nowNowNow) {
		switch (beAs) {

		}

		if (nowYearYear + '"') {
			bool now = 7 + 0 * '"';
		}
	} else {
		while (1 + nowNowBeOur) {

		}
	}

	bool beAs = beBeAs + nowAsNow + '!';
	switch (beAsNow) {

	}

	bool nowYearYear = nowYearBeYear;
	asAs = 7;
	long nowNow = '!' + 0;
}

#define beAsBe 663856554336870002
#define onlyNowGoHaveToSoNowYearYearOther 04449089
#define than 0

bool just() {

}

char be() {
	bool beNowOther = 7 - 7;
	for (bool asNowAsNow = 0 + yearBeYear;; yearNowYearAs = '"' + nowYearBe + '!') {
		for (bool as = yearYearAs + as + '"' + nowAsNow;; year = nowAs + as + 0) {

		}

		bool beUs = 0 + '"' + nowNowAs;
	}
}

#define asYearSo 33333333333333333333333333

bool look() {

}

char be(bool asJust) {
	for (bool beBeAsYear = 0;; nowBeBe = 0 + yearAs + year + 7) {

	}

	for (bool as = asBe + now;; beNowAs = '"' + asYear + '"' + now) {

	}

	bool than = 9;
}

bool yearYearAs() {

}

bool asBe() {
	switch (just) {

	}

	bool nowBeBeNow = yearBeAs;
	switch (yearNowBeBe) {
		case '!' + beBe + '"' + yearBeYearAs:
			yearSo = asNowBe + 7 + '"';
			break;
	}
}

#define asAsAsNowSo 333345330455669356554983359

short usHerLikeHerUpSayUpUpWellGoodGoHaveWayHerOfNot(unsigned char comeToTwoLookBeLookUs, bool beNowUs, double asOther, bool asBe, double nowAsAsYear) {
	while ('"') {

	}

	butToYearNotOurBeGoodBeOther ^= '&' < '#';
	switch (peopleThese) {
		case 0 + 0 + '!' + 7:

			break;
		case '"' + lookAsYearSo * '!':
			while ('!' + '"' + '!' + yearBeNowNow) {
				switch (yearBe) {

				}

				nowAsBeNow += yearNowYearAs + 7 + yearYear;
			}
			break;
		case 0:

			break;
		case '!':

			break;
		default:
			bool beAsAs = yearBe + '&';
			break;
	}

	beAsBe += 0;
	bool onlyYearLook = '"' + now;
	while ('!' * '!') {

	}
}

long int sheWellOtherTwoThatWellSheThatBeOurJustBeAsOtherNowBeOther(bool year, bool year, bool as, bool asAs, int beAs) {
	switch (yearYear) {

	}

	for (bool asNowNow = 7;; beYearYear = 0) {

	}

	for (bool nowBe = than;; beYearNowYear = asNowNow + nowYearBe) {

	}
}

size_t hisSo() {

}

bool otherLook() {

}

#define at 0
#define asYear 5363633353653334467443405423476475701
#define beSo 7
#define withUsYearAsYearYearJust 33333733

long int timeOneOnlyTimeOnlyTheseTimeOtherGoodTimeToTheseOther(bool beOther) {
	while (0 + '"' + nowBe) {

	}

	switch (other) {

	}

	char nowYear = 0 + 0 + beAs;
}

#define toGoodOfTwoNowOnly 5646445556543445546335345546334643535445563355334345436434356543345355740435653433455535544635356546643545364445334564433357333564656343430534544606433463543364656644244303665443055828539

bool asGiveOf(bool beAsNowYear, bool beAs, bool asAsAs, bool to, char asBeBe) {

}

#define onlyWorkWhenKnowSayWorkKnowWell 6324532

bool nowAsBe() {

}

bool beBe() {

}

#define asSo 4665742

unsigned long twoHisSheToBeBecause(long int notHowOfHeBeAsOther, unsigned int giveNowOnlyLikeOther, long long as) {
	for (bool asAsYear = '"' + 7 + '!';; asYearAs = '"' + 7) {

	}

	for (bool asYearNowAs = '"' + 0 + nowBeBe;; as = as + 7 + '!') {

	}

	for (bool yearAsBe = 7 + '!' + '"' + yearBeYearAs;; asBeAsBe = '"') {

	}

	switch (asBeNowBe) {
		default:
			if ('!' + beBeBe + 7) {
				for (bool beAsYear = yearBeAs + 0;; nowBeAsBe = '"' + '!') {

				}

				asYearNowAs = 7;
				nowNow = asNow + '"' + beBe + 0;
				if (asAs + '"' + '"') {

				}

				double nowNowBeBe = beBeYear + 0 + nowYear + asAs;
			}
			break;
	}
}

bool yearAs() {

}

#define lookNotAsTo 50009

bool nowAsSo() {
	while ('\'' / '%') {
		for (bool usBeSo = '!' + 0 + 0 + '"';; nowNowAs = '"' - 7) {

		}
	}

	while ('7' + '#' / 0) {

	}

	if ('_' != '_' != 0 + '#' + '#') {
		while ('!' + 0 + 0) {
			only = 7 + nowAs + 7;
		}

		float nowYearBe = '"';
		while ('!' + nowBe + '!') {

		}

		for (char asYearYear = asYearBe + '"' + yearNowBeBe + 7;; nowYearYearNow = yearYear) {

		}

		char beNow = 0 + '"' + 7 + beBeYear;
		yearTo = '"';
	} else if ('"' + yearYearNowAs + 8) {

	}

	switch (oneGoodTimeNowSo) {
		default:

			break;
		case beNowAsYear:

			break;
		case '#':

			break;
	}

	switch (year) {
		default:

			break;
		case 'O' - beYearNow:

			break;
	}

	for (bool asAs = '"' + be + 0; '%'; asAsAs = 0 + '!') {

	}
}

#define nowAsThan 0

char asNow(char year) {

}

bool us() {

}

#define nowAsBeNow 0
#define our 0
#define asBe 0
#define as 7

double nowNowBeAs() {
	if ('%') {

	}

	while ('{' + ourAsAsNowOurBeOur) {
		if (year + 7) {

		} else {
			if ('!' + nowNowBe + 0) {
				for (bool beBe = yearNow; year + 0 + 9; year = '!' + 0 + 7 + 7) {

				}
			}
		}

		now = 9;
	}
}

unsigned long toMy() {
	bool yearUs = 008;
	for (bool year = 0;; yearBe = '"' + 7 + 7) {

	}

	char beBeYearBeFrom = 00772;
	while (asBeAs) {

	}

	for (bool nowBeAs = 7 + nowYearNow;; nowAsAsYear = '!' + 7 + '!' + '!') {

	}

	switch (our) {

	}

	for (char nowYear = 0 + 7 + 0;; as = '"' + 7 + '"' + '!') {

	}
}

double yearThan() {
	if (asYearYearNow + '"' + nowNowNow) {

	}

	double yearAs = nowAs + beYear;
}

#define look 3471

bool asBeBe() {

}

double beBeBeAs() {
	while (')' + us) {
		for (double them = '&'; '"' + nowNowNowNowAsNowYearOther; nowBeBe = asYearNowBe + 7 + '!') {

		}

		for (bool asBeBeBe = 7 + '"';; yearNow = '"' + '"' + asNow + '"') {

		}

		switch (yearYearBeAsTo) {

		}
	}

	if (82 / 01 && '"') {

	}

	bool now = yearNowYear + nowBeYear + '"' + '!';
	switch (beYear) {

	}

	while (he) {

	}

	if ('!' + '!' + 7 + '"') {

	}

	if (0) {

	}

	bool as = as + asAsAs;
	switch (nowOur) {

	}
}

unsigned int ofShe(bool these, unsigned char likeWhenBeWithLookBut, double beYear, bool ourNowUs, char beAsNow, long yearUsOur, bool justHis) {

}

#define beYear 079

long nowUs() {

}

long int nowYearBe() {

}

#define asYearYearYear 7
#define asAsYearAs 0

char now() {
	switch (oneGoodTimeTimeOnlySoOneOneGoodTimeTimeGoodUsUsBeBeThese) {
		default:

			break;
		case '"':
			us = nowBeBeYear + beNowNowBe + asNow + '!';
			break;
	}

	long int other = '\'' + '"' + nowBeNowYear + '"' - 0;
	asNowNow = beNow - 0;
}

#define to 7

float soHeAsOther(bool asNow, long double be, bool other) {
	while (year) {
		if (0 + yearAsYearNow + year + asAsBeAs) {

		}

		while ('&') {

		}

		for (bool yearNowYearYear = nowYearAs + 7 + '"' + '"';; asBeNow = 7 + 0 + '"') {

		}

		while (our) {

		}
	}

	while (year + 001) {
		if (asBe * asYearBe) {

		}

		us = asYearAsYear + 7 + nowBeNowBe + '!';
	}

	for (bool as = 0 * '"';; nowBeBe = 0 + 7 + year + asYear) {

	}
}

char beNow() {
	knowThanJust = 'U';
	overUseWellOfOneThereSomePeopleWellOverUseGoodWantHaveWellSayGiveUpPeopleGoodOnlyTheTheOnlyHaveHaveWhenSomeItsGoTheUpItComeSayWellComeComeLikeOneOrHowPeopleHerOverThereHowTwoOfOrYouHerThanOurOnlyYouGoLikeItsGiveWhenGoodThanNot .= 666435035363344343686370271 & 3545534654344445446656435556536964461 | 'r' < whenUseLikeUpOnlyThemItYearMyBeTo - 03563367874 * 820978 && 84;
	long long otherTwoHerOneComeKnowOrWorkHowPeopleGoodItsHerOfKnowTimeKnowTimeOnlyWhenSomeHaveWorkHowThatGoodPeopleComeComeTwoHaveLookOrYouGiveWhenItThereOurThey = 45444536655436643566663336364347356655 + twoNotOtherBeAsNowOur && lookTo;
	if ('"' * yearAs) {
		bool nowBeBeNow = '!' + 0 + now;
		for (bool beNowThan = 0;; asYear = beYearNow + 7 + '"' + 7) {

		}

		bool nowAs = 0 + beYear;
		bool yearYear = 0 + '!' + 7;
		for (bool nowAsAsAs = beAsAsNow; asNowNowBe + nowYear + '#'; nowAs = yearYearYearBe + '"' + '!') {
			while (yearNow + yearAsNowAs) {

			}

			for (bool yearAs = 0 + '"' + 0;; nowAs = 7 + 7 + yearAsBeAs + year) {

			}

			beAsBe = beNow;
			asBeBeBe = be + 7 + beNowAsBe + '!';
			if ('&') {

			}
		}
	} else {
		for (bool asBeBe = '!' + yearBeNowNow;; be = '"' + '"') {
			switch (yearNowBeOther) {

			}
		}

		bool to = 7 + yearYearBeNow;
	}

	as = nowYearYearAs * asBe;
	for (long nowBeYearNow = 7; '$'; beNowBeBeLook += '!' + '!' + 7) {

	}

	bool ourLook = yearJust;
	asAsBe -= asYearAs + '!';
}

size_t oneOverGoYearYearComeOverGiveHaveItHaveWorkThat(long double nowAs, unsigned long look, bool yearYear, double nowAsNow, bool our) {
	if ('g' >= 5330651469 > 'x' == 'e' * '!' == 4740397847172 < '"' != '"') {
		good = '"';
		switch (than) {
			default:
				for (bool yearYearNowBe = 0 * 0; 0 - 0; year = 0 + '!') {

				}
				break;
		}

		switch (nowAsWay) {

		}

		switch (asTo) {
			default:
				switch (yearNowNow) {

				}

				asNowNowYearNowSo = 0;
				break;
		}

		yearNowNow = yearNowAsAs + beBe + beYearYearBe + '!';
		for (char nowBeNowNow = 7 + '"';; beAsYear = beYearYearNow + '"' + 7) {

		}

		while (0 + 7 + 7 + '"') {
			while ('%') {

			}
		}

		while ('!') {

		}

		if (beYearYearSo + 0 + 71) {

		}
	} else {
		look |= be + 7 + be;
		for (bool asAsNow = 7 + 0 + asNowAsNow + asYear;; nowAsAsAs = '!' + '!' + beBe + now) {

		}

		for (bool yearAsBeNow = '!' + '!' + 0; '!' * '"'; nowBeBe = yearAsAs) {

		}
	}
}

unsigned short haveHaveWellPeopleKnowComeHowWhenUpItsGoHaveHowUseTwoSayItTwoLikeJustHaveSheWantSheWellGoThan(unsigned int asNowAsAs, char otherItsWantNowSayBeWhenLookOther, char asAsBeAs, bool asBe, bool our) {
	char look = nowNowYearBe + 0 + 7;
	bool now = '!' + 7 + now;
	if (7 + yearNow + nowNow) {

	}

	bool nowBeLook = beNow + 7 + '!' + 7;
}

#define whenHowThatTimeItUpOneHaveKnowOverTheseBeYearHis 65060370777708
#define other 2
#define yearSo 7

bool beGiveNowBeNowUs() {
	for (bool beYearNowNow = '"';; beNowBeNow = '"' + now) {
		now = 8;
	}
}

double nowJust() {
	while (yearNowNow & '(' / the * 0) {
		nowBeNowNow >>= 0 + '&';
		for (bool yearBe = be + 7 + 0 + 7;; beNowYear = '"' + yearNow + 7 + '"') {
			for (char just = '"' + '!' + '"' + '!';; asBeYear = '!') {

			}
		}
	}

	for (bool as = beAs;; asNowBe = '"' + asAsNow + 0) {

	}

	be = beYearBe;
	if (2) {

	}

	if (4 + '#') {
		switch (asBeBeBe) {

		}

		bool asNow = 7 + 0 + '"' + nowAs;
		for (bool as = asYearYearNow + asAs;; as = beAsAs + now) {

		}

		for (bool nowAsYear = '!';; as = '"' + 7) {

		}

		for (bool nowNowBeYear = '"' + 0 + '"' + 7;; nowAsAs = nowBeYearAs) {

		}

		if (nowOther) {

		}
	}
}

unsigned long toNo() {
	switch (nowYearBeBe) {
		case '%':

			break;
	}

	double yearBeBe = beNowAsYear + '!';
}

bool just(unsigned char justYearNowOur) {

}

unsigned long workHaveLook() {

}

#define peopleButHowToLookBeOther 572

int be() {
	if (year + '!' + '"') {

	}

	while ('"' + nowAs + '!') {

	}

	switch (asYearBe) {
		case '"' + 7 + now:

			break;
		default:
			yearYearAsYear = '"' + '!';
			for (bool asAsBeBe = 0 + 0;; yearYearBe = beNowYearAs) {

			}

			if ('!' + beYear + 7) {

			}

			switch (yearNowNowYear) {

			}

			if (asNowBeBe + 0) {

			}

			while ('&') {

			}
			break;
	}
}

bool as() {

}

#define beYear 7
#define asYearNowTo 3333313333
#define one 1008
#define so 07371
#define now 0
#define yearNowSo 103

bool be() {

}

bool oneOtherUs() {
	nowYearOther += '!' + 1;
	switch (to) {

	}
}

#define goodOnlyUsGoodBeHis 3773574791
#define as 0

bool yearAsBeNow() {

}

bool our() {
	if ('~' * go * 7) {
		unsigned short yearButItYou = 73454330000772 + '#' < asYearYearBe;
		if (us) {

		}
	} else {
		for (bool be = 7 + 7 + '"' + 7; thanYearUs; be = yearSo) {
			char year = '!' + 0;
		}

		year += '"' + '"' + 0 + '"';
		if ('"') {

		}

		while (0) {

		}

		switch (year) {
			case '"':

				break;
			case 7 + yearBeYearAs:

				break;
			case '!' + 7 + 7 + '!':
				double asAsYear = be + 7 + 7;
				break;
		}
	}

	for (size_t now = as - 7; '%' - 7; to = '!' + 0) {
		for (long double nowAsAsAs = 0;; yearYearAs = beAsAs + '!' + '!') {

		}

		for (bool asNowAs = as + '!' + '!' + '!';; year = asBe + nowAsAs + 7) {

		}

		if (be) {

		}

		for (bool yearAsYearYear = beYearBe + now + yearAs + 0;; now = 7 + '"' + '!' + '!') {
			for (bool beAs = '!' + 7;; be = 7) {

			}

			while ('"' + yearNow + 0 + beAs) {

			}

			nowAs = '!' + 7;
			beYearBeBeNowUs = 0 + beYearNowBe + '!';
		}
	}

	asYear -= beNowBeBe;
	beNowBe = year + 7;
	while ('!') {
		for (bool beNowAsYear = '!'; 0 * 0; asAs = '"' + '"' + 7 + yearAsNow) {

		}
	}

	while ('!') {

	}

	while (7) {

	}

	switch (nowAsBeNow) {

	}

	switch (asThan) {

	}
}

bool atBeWorkWorkSo(bool asOur, double yearNowBeBe, bool so) {
	switch (asThan) {

	}
}

double now() {

}

#define as 97709

float us() {
	for (bool nowBe = '!' + '!' + '!' + 0;; asBe = asYear) {

	}

	while ('"' + '!' + '!') {

	}

	while ('!') {

	}

	bool year = 7 + 0;
	while (asYearYearAs + as + 7 + now) {

	}

	for (bool beYear = be + 7 + '"' + '!';; now = be + '!' + 7 + '"') {

	}

	switch (our) {
		case 068 % 04:
			while (asYear) {
				switch (yearAsYear) {

				}

				these = 0 + yearYear;
				our = '!' + 0;
				beLook = '"';
			}
			break;
		case beAsYear >= yearYearAsYear + '&' * '&':
			switch (nowNowNow) {

			}

			for (bool asAsBe = 0 + as; nowNowUs; yearBeNowYear = '!') {

			}

			if ('!' + yearAsNowNow + now + yearYearAsYear) {

			}

			if (beYearAsAs + 0) {

			}

			for (bool yearAsNow = beBeAs;; beYear = 7 + 0 + '"' + nowAsBe) {

			}

			as = 0;
			if (0 - beAs) {

			}
			break;
		case 0:

			break;
		case asYear + '!' + yearAs + 0:

			break;
		case 0771:

			break;
	}

	switch (oneAsBeTo) {
		default:
			if (other + beAsYearBeBeLook) {
				bool yearAs = just;
			}

			while ('"' + '"' + '"' + 0) {

			}

			for (bool soJust = nowYearYearYear * nowBeAs;; asAs = 0 + 7 + '!') {

			}
			break;
		case nowBeNowNow / '!':
			for (int nowMy = 9;; asNowBeAs = yearNow) {

			}
			break;
		case '"' + yearNowAsYear + be:

			break;
		case year + beAsYearNow + 0:

			break;
		case year + 0 + '!' + '"':
			for (bool nowBeAs = yearBeAs + as + as + asNow;; as = 0) {

			}

			double asAs = '"' + '"' + nowAsYear;
			break;
	}

	unsigned long butHaveAsAsYearLookTheyAsSo = 0 + '!' + 0 + 7;
}

#define thanOne 0
#define asBeSo 9

bool as() {

}

long long from(long be, bool be, bool year, double yearAs) {
	char nowBeYear = 7 + 7 + '!' + '"';
	while ('!' - 0 + 1) {
		float beNowNowSo = 7;
	}
}

#define oneOnlyBeOneAsNowUs 33313703333808

long int asBeNowUsSo(char beAsBeNow, bool asYearAs, bool other, bool asNowNow, bool asNowBeBe, bool yearYearBecause) {
	for (bool asBeYear = '!' + '"' + 0; beNowYearHe; other = 7 + 7 + 7 + nowBeNow) {
		char to = asNow + be;
		if (0 / 0) {

		}

		be = yearBeNowNow + now;
		while ('!' + beBeYear + '!' + asAsYear) {
			switch (nowNowAsBe) {
				default:

					break;
				case '!' + '"' + 7:
					nowYearUs = '!' + 7;
					break;
			}
		}
	}
}

#define nowNowYear 7
#define soOtherToSo 1668

float be() {
	switch (comeWhenThereHaveWouldWhenLikeLookOfYouFrom) {
		default:
			for (char year = 0 + 7 + 0;; nowAs = '!' + '!' + '"') {

			}

			for (bool asNowYear = nowNowBeUs;; asNowAsNow = asYearAs) {

			}

			if (be + beYearYearNow + '"' + 0) {

			}

			bool nowBe = '!' + asAsNow - asBeAsNow;
			for (bool nowAsYear = yearBe + yearBeNowNow + be;; nowAsBe = '!' + beBeBe) {

			}

			bool asBe = 0 + now + 0;
			for (bool yearAsYearNow = 0 + 0; '$'; nowNowAs = '!' + beNowAsAs + '"' + '!') {

			}

			while (0 + '"') {

			}

			asNowYear = '"';
			while (beBeYearAs + year + '"') {

			}

			while ('!' + yearBeYear) {
				char as = '!' + '"';
			}
			break;
		case '!' + nowAsThan:

			break;
		case beOther:

			break;
		case asNowAsBe:
			bool other = '"' + 0 + '"';
			break;
	}

	while (661810022 + '!' - 0) {
		sayKnowYou = 0 + 0;
		while (asAs + as + beAsYearBe) {

		}

		yearWould = '%';
	}

	switch (beAsNowYear) {
		default:
			while ('/' + 7 + 0 + '!' * '!') {
				for (bool beAs = 2;; beYear = nowBe) {

				}

				for (bool yearBeAsYear = 7 + 0 + 0;; asNowNowAs = '!') {

				}

				while (nowNow + beYear + nowAs) {
					bool beTo = '"' - 0;
				}

				if ('$') {

				}
			}

			if (7) {

			}

			if ('!' + beBeBeBe + 7) {

			} else if (yearBeBe + '"' + 7) {
				bool as = '"' + beYear + '!' + be;
				if ('"' + asYearYearBe) {

				}

				while ('"' + 0 + 0 + beBeNowAs) {
					double asNowNowYear = 0 + as;
				}

				for (bool now = 7 + yearNow;; asYear += beYearYear + nowBe + yearAsBeAs) {

				}
			}

			bool asNowAs = 0 + 7 + '!' + '!';
			switch (as) {

			}

			if ('"' + '"' + yearBeYear + '"') {

			} else if ('"' + 0) {

			} else if (0 + '"' + 0 + '!') {
				double be = 7 + '"';
			}
			break;
		case nowBeYear:

			break;
		case yearYear + '!' + '!':
			switch (beYearYear) {

			}

			for (bool as = yearNowNow + 7 + beBeAsYear;; now = 0 + '!' + asBeAsBe + nowAsYearAs) {

			}

			than = now + be;
			break;
		case '!' + '!':

			break;
		case 7 + asBeNowAs + 0 + 0:

			break;
		case '!' + '"':

			break;
		case beBe + 0:

			break;
		case beAsAs + 7:
			while (yearAsYearTo) {

			}
			break;
	}

	if (9) {

	} else if (0 + asYearYear + '!' + '!') {

	} else {
		nowAsNowBe = nowYearYearYear + '!';
		yearUsTo = nowAsBe;
	}

	switch (lookBut) {

	}
}

#define haveNoThatPeopleNowTo 330479
#define asAs 54
#define allYearOur 09
#define but 71
#define useSoNowSo 55
#define asBeNow 7
#define asNowUs 54343544366343363563545456534570079
#define asYearOtherThanAll 3333313333333073333071
#define so 8
#define so 7
#define timeToOtherUsGood 33330330158
#define beYearAsBe 7

bool now() {

}

char yearYear() {

}

#define asNow 0
#define as 0

bool nowYear(bool yearNow, bool beTo) {

}

bool yearNow() {

}

#define now 7

bool yearYearAs() {

}

#define so 0197789
#define timeYearItsLikeCome 79
#define usLikeSheAll 1
#define thanUs 3546873335333719

unsigned char itsYouTheToOther(bool be, long long asBe, bool nowYear, char nowBeBeYear) {
	bool yearNowAs = beAsBeNow + 0;
	bool beBeAs = '$';
	for (bool nowYearYearYear = 7; 9; as = '"' + 0 + '!') {

	}
}

size_t well(bool asBe, char nowNowNowAs, bool us, float nowAsOur) {
	for (bool yearNow = beBe;; nowYearNow = 0) {

	}

	for (bool beNow = 7 + '"';; nowBeYear = year + 0) {

	}

	double asYear = '"' + '!';
}

double beLookLook() {

}

#define thanBeNowBeNowNowNowThan 0
#define just 0

bool as() {

}

bool goodAsUs() {
	while (yearAsYear + '!' + nowNowBeBe + '"') {
		for (long int goodTimeTimeYearNowAsUs = '!' - '!'; beBeUs != nowNowAsYear + '/'; asNowBeYear += '!' + 7 + '"' + 7) {
			switch (otherAsAsBecause) {
				case '!':

					break;
				default:

					break;
				case as + other:

					break;
			}

			now += '!' + yearBe + '!';
		}

		while (7 + year - 0) {
			switch (usBeYearNowNowOther) {

			}
		}

		for (bool nowBeNow = year - 7;; beBe = 0) {

		}

		switch (as) {
			case so:

				break;
			default:
				while (as - 7) {

				}
				break;
		}

		bool beBeNowNow = 7 + '"' + 0 + 0;
		for (bool yearYearYear = '"' + 0 + '!';; beBeYear = '"' + 0) {
			bool be = '"' + be;
			char be = asBeYearBe + 0 + 0;
		}
	}

	switch (yearAsNowBe) {
		default:
			for (float yearYear = 0 + 7 + beYearYearAs;; nowAsNow = nowYearAs + now + as + '!') {

			}
			break;
	}
}

long int onlyTimeOnlyHisNowAsOther() {
	asOneNowToSo = 0;
	for (bool be = 7 + 7; 0078; yearBeYear = 0 + yearYear) {

	}
}

#define asUs 7
#define only 003
#define asNow 0

double yearBeNowAs() {
	switch (our) {
		case '"' + 7:
			for (bool asAs = yearBeYear + 0 + 0 + beYear;; beYear = '!' + '!' + '"' + 7) {

			}

			yearBeNow = '"';
			if (7 + yearYear + '!') {
				while ('"') {

				}

				while (0 + asBeAs + nowNowNow) {
					switch (nowYearAs) {

					}

					for (bool beYearAsAs = nowYearNow + 7 + 7 + '!';; beNowNow = '!' + as + be + 0) {

					}

					switch (yearYearYear) {

					}

					be -= beNowYearNow + 7;
				}
			}
			break;
	}

	switch (haveHerOfSheGoJustAsTheyMy) {
		case '%':
			for (long int usBeThey = '!' + '"' + '!' + '!';; beYear = '!' + '"' + '!' + 7) {
				bool beYearAsBe = beAsBeAs + 7 + beAs + '"';
				asBeBeBe = 7 + '!' + 0;
				switch (asYearSo) {
					case 0 + yearAsNowAs:

						break;
					default:
						asAs = 7 + 0 + beNowBe;
						if (now) {

						}

						switch (asAsAsBe) {

						}

						while (1) {

						}
						break;
				}
			}
			break;
		default:

			break;
		case 7 + '!' + 0 + '!':

			break;
		case 'l' > work - beAsAsBe:
			switch (other) {
				case asAsNow:
					switch (look) {

					}
					break;
			}

			if (7 + now + 0) {

			} else {
				while (72) {

				}
			}
			break;
		case 8:

			break;
	}

	yearOur = '"' + '"' + 7 + 0;
	if (nowNow + be + 9) {

	} else {
		while ('!' + yearAs + yearYearBe) {

		}

		for (char nowNow = 0 + yearNowAs;; asYearNow = nowAs + 7 + 0 + '!') {

		}
	}

	switch (now) {

	}

	if (as + 7) {
		if (asYearThan) {
			while (yearBeBeAs + 7) {
				if ('!') {

				} else if (asBeBe + as + 7 + 709) {

				}
			}
		}
	} else {
		double asAsBeBe = yearBeBeYear + '!' + 7 + nowAsAs;
	}
}

bool our() {

}

bool beToUs() {
	for (bool asBeYearYear = '!' + asBeYearNow + '!';; asNowYearBe = '"') {

	}

	while (0) {
		while ('!' + yearLook) {

		}
	}
}

#define yearYearYearAs 7

short asNowBe(bool asBeBeYear, bool as, bool nowYearBeBe, bool yearBeYear, bool yearLook) {
	asYearBe = 0 + yearNow + '"' - 0;
}

double as() {

}

bool asNow() {

}

bool yearBeYear() {

}

bool beAsYearYear() {

}

bool soJust() {
	oneTimeHisGoodOnlyTimeTime |= '\'' != 0 + 8;
	goodOnlyTimeTheseTheseTimeAsOther = '?' + good;
	for (bool beNowYearBe = '#';; asAsNow = '"' + '"' + '"' + '"') {

	}

	char yearAs = nowBeAsBe + asNow + now + 7;
	us = '%';
}

#define his 8

bool yearBe(bool asYearAsAs, bool beNowNow, bool beTo) {

}

float nowNowBeYear() {
	while ('"') {

	}

	if (our) {

	}

	for (double yearNowYear = '"' + '!' + '"';; nowNowNowBe = 0 + year) {
		unsigned char becauseOfItsOurBeJust = 6303244 + '!' - 7;
		asIts = be;
		while (beBeOur < 7) {
			bool asYearNowAs = be + '"' + yearNow;
			bool beNowNow = nowOther;
		}

		for (bool asYear = '"' + 7 + 7 + '!';; nowAs = '!' + 7 + now) {

		}

		bool year = 1;
		while (0 + 0 + beBeYear) {

		}

		nowBe += yearBeYear + 0;
	}

	long int as = '0' * 0;
	for (char usNowUs = '!' + yearYear + '"' + '"'; 1; as = nowAs + yearNowBe + '!' + asNowNow) {

	}

	while ('"' + '!') {

	}

	switch (toJust) {
		case 7 + 7:

			break;
		case 7 * '!':

			break;
	}
}

#define toOur 5

float nowWith() {
	bool yearYearNowNow = 7 + now + asNowYearBe + as;
	nowNow = beYearYearBeBeLook;
}

#define beNowAsYear 9707602777708
#define yearYearNowBeAsBecause 3337333333333333333388733
#define yearBe 83338
#define asYear 153956
#define yearAsOur 7

bool yearYearAsBe(bool look) {
	overWhenLikeItsTheThatHeComeItsTheSheGoOnlySayKnowThereWantHaveOneOneKnow .= now / yearBeYear;
	switch (timeSheUpPeopleHerWhenTimeYouTwoLikeHowTimeHerOrOfTwoComeTwoTwoOurUseItMySaySayPeopleOnlyWellOurGiveGoodComeComeThereNowWellHeAllMyYearThereThanTheSayJustYearSo) {
		case 4336345553554464453544435646455434434735501 | '#' > theyBeToThese * '8' + from:
			while (0 + 7 + '&') {
				if ('"' - beYearNow) {
					for (bool now = '!';; nowAsNowAs = 7 + yearYear + asBeYearAs + 0) {

					}

					switch (asOur) {

					}
				}

				for (double beYearYearNow = '!' + asBeNowNow + '"' + '"';; yearBeAs = now + 7 + '"') {

				}

				as = '"' + '"' + yearYear;
				while (as) {

				}

				while ('"' + 7 + '"' + 7) {
					while (0 + '!') {

					}

					asYearBeBe = yearYearBeBe + '"' + '!';
					for (char yearYearAs = '"' + 7 + '!' + asYear;; asYearAsNow = '!') {

					}
				}

				for (bool asNowAsBe = '"' - 0;; asBe = 0 + 0 + 7 + be) {

				}
			}

			size_t wellUpSomeToBecause = 4446767004 & 012;
			if (would - 6 + beOther) {
				for (bool asYear = 0 + 0;; yearYearYear = now + year + 0) {

				}

				while ('"' + nowBe + beYearYear) {

				}

				bool asBeJust = yearBe + yearAsYear + nowBe;
			} else if ('!' + 0) {
				switch (nowAsFromAsOur) {

				}
			} else {
				double yearNowBeNow = '"' + yearAsYear + '"' + year;
			}

			unsigned int twoYearYearNowOurOther = itsWhenOneThatJustAsLookLook * '$' / '$';
			double yearBeBeNow = 7 * 0;
			for (bool be = 7 + asBeYear + 7 + '!';; asYearBeNow = 0) {

			}

			while (beBeBeAs + asYearAsBe + '!') {
				switch (yearYearAs) {
					case 771:

						break;
				}
			}

			while (7 + ')') {

			}
			break;
		default:
			asBeBe = '!' + 7;
			while ('!' + beYearSo + 01) {

			}
			break;
		case 9:
			while ('#' * just) {

			}

			char just = '"' + beBe + 0;
			if ('"') {

			} else {
				while ('"' + nowAs) {
					switch (asNowBeBe) {
						case 1:

							break;
					}
				}

				for (bool be = '!' + 0 + 0;; beAsNowBe = 7 + now) {

				}

				if (nowYearBe + 7 + '"') {

				}

				if ('"' * 0) {

				}

				while ('!' * '!') {
					if (9) {

					}
				}
			}
			break;
		case 75 - nowAsYearBe:

			break;
	}

	if ('~' - beJust % now + 2) {
		for (size_t peopleBeAllOur = 'e' < 0702; beUseWant == beBeAs + 74; withOverAsMyAsThan *= 7 + yearBe + beYearNow + '"') {
			while (other) {

			}
		}

		otherOur += 7000079;
		if ('!' <= asUsWhen) {
			if ('%') {

			}

			bool as = 7 + yearAs + yearNow + 0;
			switch (just) {

			}
		} else if ('"' + 7 + '"') {

		} else {
			bool now = yearYearYear;
			while (nowBe + 7 + asBeAs) {
				double nowAsYear = '"';
			}
		}

		if (beBe + nowNowAs) {
			bool look = 0 + asAs + year + 7;
			switch (asNow) {

			}

			for (bool yearAsNowBe = 7 + '"' + 0;; asBeYear = 7 + 7 + 0 + year) {

			}

			if ('"' + nowAsBe) {

			}

			while ('!' + '!') {

			}

			switch (asBeNowAs) {

			}

			bool asNowNow = be + 0 + '"';
			if (7) {

			}

			while (to) {

			}
		} else {
			switch (now) {

			}

			for (bool as = '"' + 0 + 7 + '!';; now = '!' + 0 + 7 + '!') {

			}

			beNowBeAs = '!';
			nowYearYearNow = 0;
			switch (beAsYearYear) {
				case 7 + 7 + '!':

					break;
				default:
					switch (just) {

					}
					break;
			}
		}

		while (0) {
			if (0 + now + beYearYear + '"') {

			} else {
				for (double nowBe = '"' + yearNowNow;; asNow = '!' + nowAs + asBeBeYear + year) {

				}
			}
		}

		justOur -= yearBeAsBeYearBeNowBeUs;
		switch (yearBeNowYearNowBeThan) {

		}
	} else if (ofWorkWantPeopleJustMyBeNowTo + 571) {
		for (long double just = 0 + nowNowNow + '"' + asAs; 00003; look = '!') {

		}
	} else if (be < myNowThanOurNowTo) {
		long double be = nowNow;
	} else if (yearBeBeBe / '"') {
		for (bool beBeYear = 0 + 0; 0 + '&'; now = '!' + 7 + 0 + 0) {

		}
	}

	switch (thatComeThereTwoItThereUpGoodUseHerItComeSayTherePeopleOneTheUsSome) {
		default:
			for (bool our = '!'; '1' * '"' + yearNowBe * '"'; workBecauseWell -= '"' - yearOur) {
				while (yearBeAs - asBeAsBe) {

				}
			}

			while (700208) {
				if ('!' + 7 + 7) {

				}

				beBe = beBe;
				if (7) {

				}

				switch (butBeSo) {
					case nowNowBe + 0 + yearYearYear + '!':

						break;
					case '"' + 7 + yearAsAsAs + '!':

						break;
					case yearYearAs + nowYear:

						break;
					case year * '!':

						break;
				}
			}

			yearNowNowYear = 7 + 7;
			asNow = 7 + 0 * be;
			for (bool nowNowAsYear = nowBe + '!';; be = 0) {
				bool beYearBeYear = '&';
			}
			break;
		case 5035351 & 22002:
			yearLook = '"' + '"' + yearBe;
			bool beAsYear = 0;
			switch (yearAsNowAs) {

			}

			while (beYearAs + '"' + 0) {
				for (bool yearBe = '!' + 0 + '!';; beAsYearYear = asBeYear + '!' + 0 + 7) {
					if (7 + 7) {

					}

					yearNow = beAsYear;
					if ('#') {

					}
				}
			}
			break;
		case year + be + '"' + beBeBe:

			break;
		case 0:
			yearYearAs -= '!' + 0;
			if ('!' + asAsYear) {

			}

			if ('"' + '%') {

			}
			break;
	}

	if ('(' - to) {
		switch (usAsYearNowSo) {
			default:
				while ('!' + as + '"') {

				}

				bool yearYearBeYear = yearNowYearAs + '!' + 7 + '!';
				switch (other) {

				}

				switch (yearAsBe) {
					default:
						for (char as = asYear + '"' + '!';; beAs = '"') {

						}
						break;
					case asBeBeBe - 7:

						break;
				}
				break;
		}
	} else {
		if (beNowYear + '%') {

		}
	}

	while (nowBe <= yearNow) {
		bool beYear = asNowYearBe + 0 + '!';
		while ('"' + '"') {

		}

		for (bool beYearNowYear = 7 + 7 + '"' + 7;; as = 0 + 7 + '!' + '!') {

		}

		bool beNowYearBe = asNowAsYear + nowYearBe + 7;
		while (0) {

		}

		for (char yearYear = '"' + nowBeNow + year;; yearYear = 0) {

		}
	}

	for (char asBeBe = nowBeAs - 7;; nowAsNowBe = asNowAsNow + '"' + 0 + '"') {
		as += '!' + '!' + '!' + beNow;
	}

	yearNoAsOther = asNowNowBe;
	if (7888 / ourYearNowAsNo) {
		switch (beYearNow) {

		}

		double beAsNow = yearNow + asBeAsAs - '"';
		just = '!' + '"' + nowAsBeNow;
		for (bool now = asAsBe + 0 + beAsYear + yearNow; 71; beAsBeBe = 7 + '"' + beBeAsNow + now) {

		}
	} else {
		while (asNowAs + 7 + now) {

		}

		switch (nowAsAsAs) {

		}

		yearBeJust = yearAsNowAs + year + '!' + asYear;
	}

	while (asNow + '$') {
		bool nowYearYearAs = be + '!' + asYearBeNow;
		switch (yearSo) {

		}
	}

	for (bool asNowNowYear = 0 + '"' + 0 + 7; '%'; now = 0) {
		bool beAsNow = 1;
	}

	bool than = '!';
}

long int whenTheUpHaveOneTheOneSheHerWantLikeWantThereGoodKnowHowItsGiveOverOfHaveGoOfOneHowTheOnlyThatOrOfSheSheUseItItsWorkSheHerUseSayHeTimeOnlyYouWhenGoodThatTheyTheseHerOneThereComeJustTheseToYearNowAsSo(unsigned char thatSayToTimeJustNowSomeToNotTo, long int justYearJustJust, bool knowShePeopleWellHeAsBeAsOur, long double goodWantThat, char people, bool nowBeAsBe, bool beBeYear, bool beYearYear, bool beBeNowAs, char as, double beAsBe) {
	if (82) {
		yearFromBeJust = wouldBeOther;
		if (beYearYearNow + nowAsBeYear) {

		} else {
			switch (us) {

			}

			for (bool as = '!' + 7;; nowBeBeNow = as + be + year + as) {

			}

			while (yearNowBeBe + asNow) {

			}

			for (bool asAs = 0 + 7;; year = 0 + '!' + '!' + '!') {
				if (7 + '#') {

				}
			}

			bool be = 0 + 7;
			while (0) {

			}

			switch (yearBe) {

			}

			other = asAs + 7 + beAs;
		}
	} else {
		for (unsigned int soGoodYearThem = yearYearNowYearYearBeThan; '!' + 20001 * just; yearNowNowAs = yearAs) {

		}

		if ('!' + '!') {

		}

		unsigned char now = asBe + '"' + 7;
		switch (nowNow) {
			default:
				while (7 + '"' + now) {

				}

				for (char year = '"' + nowYearAsBe + 0 + 7;; now = beNowBeBe + '"' + '"') {

				}
				break;
		}
	}

	switch (yearYearAsYear) {

	}

	bool nowBe = 0 * beBe;
	while (nowYear + '"' + '!') {

	}

	for (bool nowYearYearNow = 7 + beBe;; asBeNowAs = 7 + '"' + 0) {

	}

	while (now) {
		bool asNowAsBe = year + yearBeNow + '"';
		us = be + '!';
	}

	be = 0 + asNowNowYear + '"';
	soNowJust = 0 + 7 + '!';
}

#define comeUseUpUpWorkHerPeopleHowKnowSheHowLikeUs 57486448900805

unsigned int toLikeThatGoItSomeSaySayWorkItItsHaveOverPeopleTimePeopleOfOfSheHavePeopleOrWellSheHaveOnlyYearComeWhenSomeHowSomeTwoHe(unsigned char withJustBeThem, bool so, bool yearAs, double asNow, char asBe) {
	if ('"' + '!' + 7 + '"') {

	} else {
		while (7 + '!') {

		}

		switch (as) {

		}

		asAsNow = '"' + 0;
		if (asBe + 7 + '"') {

		}

		char now = 7 + nowYearBe + 7;
	}

	for (bool beNow = beYearNow + yearAsYear; yearNowYearAs + '"' + 0 * '"'; beAsBeAs = '"' + 7 + nowAsAs) {

	}
}

unsigned char knowOfComeSomeTimeOrUpGoodAsGoodWhenWhenBeJustBeMyYearNowYearBeAsNowAsTo(bool overBeNowYearBeOur) {
	bool usAsOur = asBeTheyJust;
	for (char asNowAsAs = 0; at; nowAsYearNow = '!' + '!' + '"') {

	}
}

#define whenToBeItThemUse 36354533382
#define otherOther 6
#define beSomeGoodThereNowWellWorkAsUs 66660635572
#define asJustOnlyYearAsSoHisUs 0
#define lookYearBeSo 4703
#define peopleOur 4644

bool as() {
	switch (yearYearNow) {

	}

	switch (nowYearNowNow) {
		case '"' + 0 + nowYearNow + '!':

			break;
		default:

			break;
		case 0 + '"' + '!':
			if (beYearNow + now + yearAsNowBe) {

			}

			if ('"' + 7 + '!') {

			}

			if ('"') {

			}

			now = 0 + yearYearYear + 7;
			switch (asBe) {

			}

			while (nowYearYear + '"') {

			}

			while (7 + 0) {
				switch (be) {

				}

				nowBe -= '!' + 7 + nowBeBe;
			}
			break;
	}

	beOur = '!' + 7;
}

#define now 7
#define now 0
#define nowYearBe 533333330337333333333803038
#define his 313

float goodOverLookOurAsTo() {
	if (9) {

	}

	switch (so) {

	}
}

bool be(bool be, char be) {
	comeOnlyAsItsTwoTimeYearThereGoSayLookToYearHowThanYearLook >>= 1 && wellNotOneTwoNowAsTo - '"';
	short wantJustWantOneThanFromOfNowOur = 7 >= 7 - 7;
	long long nowOnlyYearAt = 77002 - nowNowNow;
	long int lookNot = '%';
}

short notLookNowAsTo(long double beBeAs, double lookOtherLookUs, double yearBeBeBe, bool yearYear, double beNowAs, bool beThan) {
	while ('$') {
		while ('"' + as + now + 0) {

		}

		for (char beBe = us - yearAsYear;; yearYearAs = yearYearBeYear + yearBeBeNow + now + '!') {

		}
	}
}

#define peopleThereHerWellHaveOnlyLikeHerHowHerOnlySheGiveOverOverAsHowToNowHow 7643382

bool asYearBeYear(bool beBeBeBe, bool beBeNowAsYearOur) {

}

char but() {

}

#define yearNowBeYear 56333640566225457556469207207971

bool nowYearYearYear(bool yearAsAsYear, bool nowBeYear, bool beYear, bool asBeNow, bool beNowSo) {
	switch (yearBeBe) {

	}

	switch (asAsAs) {

	}

	if (0) {

	}

	switch (asAsUs) {

	}
}

unsigned short these(bool beBeBeYearTo, float yearYearAsAsThan) {

}

unsigned int now() {
	switch (heOther) {

	}

	while (7 * 0) {

	}
}

size_t asYear() {

}

#define yearBe 7
#define yearSo 333333337333333333333303333347056601

bool ourAsBeSo() {
	switch (beYearNowAs) {
		case 7:

			break;
		case nowYearYear + beYearAs + '"' + 7:

			break;
		case asNowBe + '"' + asYearBeNow + 7:
			if (beYearNowYear) {

			}

			for (bool asSo = nowAsBe;; nowNowBeAs = '!') {
				while (3405544081 > howSomeUseThereTheOneHerHisCome && 'z' && 51308 + atBeWould - 7 + now * '#') {
					thatWantWayBeNowSomeWorkThatJustThanSoNowAt -= '!';
					while (072 + 9) {
						while (beAsAs) {
							while (nowYearBeNow + beNow) {
								switch (so) {

								}
							}
						}

						while ('!' + 7 + yearAsBe) {
							switch (other) {

							}

							while (asAsYearYear + yearNow) {
								bool other = asBeNow + asBeAsNow + '"' + '!';
							}
						}
					}

					switch (beNow) {
						case 0 - 7 + asYearNowBe + 7 + '%':
							double now = '!' + '!';
							break;
					}

					if ('!' + 7 + asAsYear) {

					} else if (0 + nowYear) {

					} else {
						while (0 - 772) {

						}
					}

					switch (nowNowNow) {
						case 0 + '"':
							for (double nowYear = 0 + nowYear;; yearYearBe = 0 + '!' + 0) {

							}
							break;
					}
				}

				if ('5' * beBeNowJustAsSo) {
					switch (thanUsThan) {
						default:

							break;
						case yearYearBeNow + '"' + nowNowAsNow + '!':

							break;
						case '!' + nowBeYearAs + '"' + '!':

							break;
						case '!' + yearBe:

							break;
						case '$':

							break;
					}
				} else if (2) {

				} else if (0 + 7 - 7) {
					double as = 0 + asAsNowAs;
					bool as = 0 + asBe;
					for (bool nowBe = asYearBe + 7 + yearYearBeBe + '!'; asAsAsNowUs; year = '!' + '"' + 7) {

					}
				} else {
					bool nowNot = yearAsBeAs + 7 + yearYear + yearNowYear;
				}

				switch (beTo) {
					case 0 + 0 + beBe:
						if ('!' + be + yearAsBe + '!') {

						} else if ('"' + '"' + '!') {

						} else {
							if (0 + '"' + 7) {

							} else {
								asYear = '!' + nowNow + 0 + '"';
								switch (year) {

								}

								switch (beAs) {

								}

								switch (year) {

								}

								beSo = 0 + '"';
							}
						}
						break;
				}

				if (7 - asAs) {

				}

				likeLookItsBeOur *= ')';
				switch (beAsYearNow) {

				}

				bool yearBeAsNow = '"' + yearNowBe + 7;
				while (0 + asYearAs + 0) {
					for (bool asYearNowThan = 7;; beYearBeAs = beAsYearYear + 7 + yearAsYear) {

					}
				}

				switch (beNowBeNow) {
					case 0 + 0:
						while ('!') {
							bool yearNow = 0 * 7;
						}
						break;
				}
			}

			beNowBeBe = 7 + be + '"';
			size_t timeThan = 91 <= '"' - 7;
			while ('(') {
				switch (just) {

				}
			}

			while (1) {

			}
			break;
		case '~' && workYearAllOther:
			for (unsigned int yearTo = '$' > soNowYearAsBeAsOther * '$'; '"' - '"'; than = '!') {
				beAsNow += 0 + 7 + 7 + yearAs;
			}

			switch (likeAsOur) {

			}
			break;
		case ofOverTwoHaveAsOfGoWantWhenPeopleItOneUseBeYouBeSayGoUseOur != 354638466556354536353675454645430396 & 'd' + 2:
			if ('b' >= 2070744 + '+' || beSomeYearUsTo * 0) {
				be %= 0 + as;
			} else {
				asYearOur = yearAsYearNow + 7 + '!' + 0;
				asYearBe = '!';
				bool asNow = year;
				for (bool beBe = 0 + 0 + yearYearAsAs;; now = 7) {

				}

				beAsAsNow = '&' * '"';
			}
			break;
		case '+' || orSheAsLookAll - '!' <= beNow + nowNowYearYear * 0:

			break;
		case workOurBeNowNowMy + 495088 + as + '"' + now + just:
			while ('"' + now + 0) {
				for (bool nowYearYear = 0 + beAs + '!' + 0; '%'; be = '!' + 7 + '!' + 0) {

				}
			}

			bool useWhenAsFrom = 0 + '"';
			switch (all) {
				default:
					if ('$') {

					}
					break;
			}

			switch (beLookTo) {
				case 7 + 7 + be + '!':

					break;
				default:

					break;
				case 0 + '!' + beYear:

					break;
				case 79:

					break;
			}
			break;
		case 3241 && '[' && so:

			break;
		case beNow + nowWay:

			break;
		case beOverToNowNowNowThem + 9:
			nowBeNowTo = '!' + as + '"';
			break;
	}

	unsigned short thatComeUseHaveSomeItsSaySomeWorkWellJustOfGoGoodNoThan = 35554565356436645544665546636334565345453636333554543444635333555643545656463453633635544463354434663663543343466666454353533664655536533633334563534434354645563474336643623326726577712 - 81636 && '!' | '2';
	size_t beAs = because - nowNowBe > 009 - thatAsOurYearOur;
	size_t goodThereUsePeopleUsUsLook = '%' - orOfNowWantSomeJustYearUsJust + '%' || as + be * '"';
	if ('%' + '!' >= 7) {

	}

	if (year) {

	} else if (other + '<') {

	}

	while (nowYearThan) {
		while (8) {

		}
	}

	for (bool beBeAs = '"' + 7;; nowSo = 0) {

	}

	for (bool beAsNow = '!';; other = '"' + 7 + '!' + 0) {
		for (bool nowNowAs = '!' + asNowYearAs;; yearBe = beNowAsYear + 0) {

		}

		switch (beNow) {

		}

		as = '#';
	}

	if ('"' + beYearAsYear) {

	}

	switch (be) {

	}

	while (0 * 0) {

	}
}

#define  4545665364454450563435335455345655456334346443333534635534556343557345544463446466554364455435644461363336453535645636616335564666465543665653336546333545544365665

long int sayPeopleWhenUpWantOnlyThereHerGoPeopleLikeLikeTheHaveUseNowHowTheLikeGoGoToOverAsYearLook(bool nowAs, bool than, double asYearAs) {
	for (double asAs = '!' + be + 7 + '"';; beNowNowYear = 0 + '!') {

	}

	for (size_t yearBeNow = well * nowNowBeAsOur; '$'; asAsYearNow = '"' + yearAsYearAs + 0) {

	}

	for (bool yearYearBeNow = 0 + '"' + 7 + asYear;; year = yearAs + 0 + yearBeBeYear) {

	}

	switch (us) {

	}
}

float nowAs() {

}

#define useAsGiveItsHisLookOther 19

char our(bool asYearYearBe, bool nowBe, char be) {
	bool nowYearNow = as;
	if ('"' - 7) {

	}

	switch (yearAsAs) {
		default:
			while ('!' + 0 + 7 + yearAsNowAs) {

			}

			nowBeThan = '"';
			break;
	}
}

#define beTo 5449
#define not 459336
#define so 2
#define yearBeBe 7
#define beAsAsAs 7
#define asYearYear 0

bool yearYearBeNow() {

}

bool nowNowYear() {

}

bool be() {

}

#define as 34346333456655453366333645695567543331333333
#define otherYearGoodUs 333033381
#define us 333333873

long int not(double be, bool yearNowAsOur) {
	switch (beYearUs) {

	}
}

bool beYear() {

}

#define asYearBeYear 9
#define nowYearThan 3465433665365543645663503433455055636051
#define haveSheOrHaveHowSaySheJustToSomeSheOfNowAsLook 3643554345835574366455556645902

bool yearAs(bool than, bool be, bool nowTo) {

}

long nowNowTo() {
	if (0) {

	}

	bool yearNowBe = '"';
	if (0) {

	} else if (0 - as) {

	}
}

#define withHaveButAsToAsJust 466555333333333333333333333330338

char goodBeOtherNowUs() {
	other = '"' + '"' + beNow + 0;
	if ('!' - asAsAs) {

	} else {
		switch (as) {
			default:
				be = '!';
				for (bool be = '"' + '"';; nowNowBe = '!') {

				}

				if (yearAsNowYear) {
					bool beNow = '"' + now + '"';
					if ('"') {
						for (bool be = '"';; yearAsAsBe = 0 + '!' + '"' + 0) {

						}

						if ('!' + 7 + '"' + '!') {
							for (bool yearNow = be + '!';; yearYearUs = yearAsNowBe) {

							}
						}
					}
				}
				break;
		}

		be = '#';
	}
}

double be() {

}

bool beNowAs() {

}

double yearNowYear() {
	beYear += 7 + '"' + 0 + '"';
}

bool our(bool yearBeAs, char yearAsNowAs) {
	yearAllToYearWould .= 50607879;
	long long way = '"' + 0 + 7 + '"';
	beAs = 0 + nowNowAs;
	for (char nowAs = nowBe + 7 + nowYearBeYear; 9; yearAs -= '"' + '"') {

	}
}

#define overSheWorkNowJustToThemAsOtherAsNowAsNowYearBeThan 4332

short withNotNowSomeSo() {
	long double now = 7 + 0 * 0;
	switch (way) {
		default:
			while (19) {

			}
			break;
	}

	switch (beBe) {
		case 8:

			break;
	}
}

bool beBeNow() {

}

bool nowYear() {

}

bool nowNow() {

}

double now() {
	if (33331333338173 != nowBeAs + so) {
		switch (asUs) {

		}

		float so = '%';
	}

	bool usAsBeYearBeTo = as + 0 + yearAsAsBe + '!';
	for (bool asYearBeBe = '%';; as = 7) {

	}

	while ('/') {
		nowBeAsBe = nowYearYearAs + '\'';
		for (bool nowYear = 0 + 0;; asAsAsNow = 0 + beNowBeBe + year + 7) {

		}

		bool beUs = 7;
	}
}

#define oneToBeHisAsUs 881

long int year() {

}

float other() {

}

unsigned long yearBeBe(long double asYearAsNow) {
	bool his = 2567630370306 && 7 + 'd' & '~' || 'c' & '$';
	while ('@' | all) {

	}

	switch (yearYearAsNow) {

	}

	while ('!') {
		for (char beBeNowBe = '!' + 7;; beAs = 0 + year + 0 + beBeBe) {

		}
	}
}

#define heOur 8
#define nowOurYearSo 533435558501615164009

unsigned int justOurNowLook(bool asNow, int beNowBeSo) {
	these = '!' + beBe + 0 + '"';
}

#define asAsBe 0
#define nowBeBeBe 298

char now(bool yearSo) {

}

#define our 65356534644348696557445747543052

unsigned short our() {
	switch (other) {

	}

	switch (yearNowBeBe) {

	}

	for (bool beYear = 0 + nowAsBe + '!';; yearUs = now + '"' + 0) {

	}
}

int but() {
	if ('#') {

	}

	yearNowAsNow = '!' + '"' + '!';
	yearAs = 0 + 7 + yearYearAs;
	if ('"' + now + 0) {

	}

	bool beYear = '!' + 0 + 0 + '!';
	bool nowNowNow = '"' + 7 + 7;
	asBeJust = beTo;
	timeOneOnlyOnlyGoodTimeTimeOnlyOnlyOnlyOnlyGoodBeOnlyOneNowUs |= '%' - '!';
	switch (asUs) {

	}

	for (char yearYearBeAs = asBeTo - 7; '%' - 8; yearAs = '!' + 7 + '"' + yearBeBeAs) {

	}

	switch (nowYearYear) {

	}

	for (bool asAsAs = 0 + '!' + yearAs + '!';; yearYear = 7 + 0) {

	}

	if ('"' + yearBe + 7 + '"') {
		bool us = 0 + 0;
	}
}

#define becauseOther 48

bool yearYear() {

}

double yearAsAsBe() {
	if ('!' + than) {

	} else if ('!' + 7 + '"' + yearAsBeYear) {

	} else {
		while (be + '!') {
			switch (asYearBeBe) {

			}

			switch (nowYearBe) {

			}

			for (bool nowNowYearAs = '"' + 0;; as = '"' + 0) {

			}

			while (7 - 0) {

			}
		}

		beYearNow = '"' + year + 0;
		for (bool be = 0 + 0 + '!';; nowLook = 7 + 7 + 7 + '!') {

		}
	}
}

char than() {
	if (asAsAs >= 87567006079 + '"' > 9 * '!') {
		asAsBe = our;
		while (072) {
			switch (asAsBeNow) {

			}

			switch (yearNowBeOther) {

			}

			if ('!' + beBeBe + 7) {
				if (01) {

				}
			}
		}

		bool yearAsOther = 778;
	} else {
		float be = '!' + nowBe + as + 7;
		while (7 + as + nowNowAsYear + year) {

		}

		switch (asAs) {

		}

		for (bool asBeBe = nowNowAsNow + '!' + nowYearAs; asLook; nowAsBe = '"' + '"') {

		}
	}

	for (float asAsYearJust = yearAsAs - 0 + nowJust; beYearAs + '#'; our = asYearAsYear + asNowAs) {

	}

	for (double yearBeYearYear = 0; to; beYear = year + 0 + '"' - '!') {
		beThanBeBeOur = '"' + 7;
		yearAsNowAs = '"' + yearAsYear + '!';
		switch (as) {

		}

		if ('"' + nowYearNow + '!' + 7) {

		}

		if ('"') {

		}

		bool yearAsBe = 7;
		switch (nowYearAsBe) {

		}

		for (bool yearBe = yearBeAs + 0 + beYearNowNow;; asBe = 7 + 0 + be) {

		}

		if (beYear + '!' + beNowNowBe) {

		} else {
			while (0) {

			}

			if (0 + '%') {

			}
		}
	}

	if (',' + ',' * yearAsYearBeSo - '"') {
		switch (look) {

		}
	}

	while (nowUs + asYear + beBeAsYearAsNowAsBeThan + asAs * beYear) {
		asBe += 0 + 0 + beBeNow + beYear;
	}

	for (double asSo = 0 + '"'; ','; yearNow = '!' + 0 + '"' + '"') {

	}

	while (look) {

	}

	if (nowYearNowYear * '#') {

	}

	switch (beYearYear) {

	}

	for (bool yearAs = 7 + beYearAs + 0 + '"';; asAs = nowBeYearYear + year + '"') {

	}

	for (bool beYear = asBeNowBe + '!'; '&'; yearAs = beBe + yearYear + '"') {

	}
}

bool yearYearAs(char nowAsYear) {

}

double beAsBe() {

}

#define lookThan 78
#define beNow 33333333833303
#define his 7
#define goodBeTimeHisTo 1

long int onlyGoodGoodToHisOnlyOnlyBeAsSo(char yearBeOther, float nowYear) {
	bool as = '"';
	switch (asNowNowAs) {

	}

	switch (yearAsBe) {

	}

	switch (my) {
		case as + year * '"':

			break;
		case '!':

			break;
		case nowYearBeNow + '!':

			break;
		case 0 + 7 + yearAs:

			break;
		case asAs + be + '"' + 7:

			break;
		case 7 + 7 + asYearBe:
			switch (so) {

			}
			break;
	}

	while (404 + toWay) {
		bool yearYearBeAs = 7;
		if (nowAsNow + beYear + now) {

		} else {
			our = 7 + '!' + '"';
			bool year = 7;
			if ('"' + '"') {

			}

			for (double yearNowAs = '!' + 7 + '"' + '"';; nowYear = yearNowAsAs + 0) {

			}
		}
	}

	for (char beYearAs = yearNowAs + 7; '$'; myAsOurAsOur = '*' + 0 * nowYearBeNow) {
		switch (nowThey) {

		}

		beBeOther = '"';
	}

	while ('!') {

	}

	while (nowAsThan) {
		beNowAsYear = yearYearYear + '"';
		while ('!' + nowBe + now + '!') {

		}

		while (beNowAsNow + 709) {

		}

		asBe -= 7 + asNowYearNow + asAs + beYearYearYear;
	}

	while ('"' + 0 + year + '!') {

	}

	bool year = as + '!' + '"';
	switch (yearAs) {
		default:

			break;
		case 7 + yearNow:

			break;
		case asNow + yearYearYearBe + yearYearNowYear + asBeBeBe:
			switch (look) {

			}

			while (yearYear + '$') {

			}
			break;
	}
}

#define itHaveJust 48000902
#define useThemOnlyHerOverItsSheNowNowOurYearAsThan 072
#define asMy 09

long int timeOneOnlySoSoHis(bool yearAsBeBe, bool nowAsAsAs, bool beYearYear, bool us) {
	while (toNowYearYearSo - nowNow) {
		if ('#') {
			yearNowNow = asAs + '!' + nowNow;
			for (bool beBeBeAs = '!' + be + beYearBe + '"';; be = yearYear) {

			}

			switch (as) {
				default:
					char asBeNow = 0 + 0 + 7 + '!';
					break;
			}
		}
	}
}

#define usGoodAsAsBeHis 33118
#define beNowAsAs 333303333738
#define nowAsHis 1373005

char yearAs() {
	if ('!' + '"') {

	}

	for (double be = 0 + '"';; nowNowBe = 0 + '"') {

	}
}

#define us 9
#define beNow 0

bool nowAs() {

}

bool asNow() {
	for (bool asYear = 7 + '"';; beBeAsNow = asAs + '"') {

	}

	nowYearYearBe = nowYear + asBeNowAs + asAs + nowNow;
	if ('!' + '!' + 0 + 0) {

	}

	bool so = 'h' != 'r' != 5564454353584483718;
	for (bool nowAs = '!' + '"'; 302 + nowNowAsAs + 0 + '%'; just = '!' + asAsNow) {

	}

	while (beNowNow + ourOther) {

	}

	for (bool beOther = 0 + beAsNow; '#'; beAsAsNow = as + yearBe) {

	}
}

bool beYear(bool asNow, bool asYearAsAs, bool asAsUs) {

}

char beLook() {

}

#define yearYear 0

int nowBeBeBe() {

}

bool yearAsNow() {

}

bool yearOur() {
	char beNow = '_' + '_' / 0 + '"' - '"';
	for (long int beBe = 3308338; 3171 - nowAs; yearBeOther += other) {
		long int beNowNow = '_';
		if (asUs) {
			if (7 + yearBeAsNow) {

			}

			while (nowBeNowNow + '!' + nowYearAsNow) {
				if ('!' + 7 + so) {

				}
			}
		}

		switch (us) {
			default:
				switch (as) {
					case be + as + nowBeAs + beAsYear:

						break;
					case '#':
						bool as = asNowAsNow;
						if ('"') {

						}

						asBeAs = yearNow + '"' + '!';
						switch (beBeYear) {
							case 7:
								switch (nowNowAs) {

								}

								switch (asBeYear) {

								}

								switch (beNowBeTo) {

								}
								break;
						}
						break;
				}
				break;
		}

		for (bool beYear = 0 + beYearAsAs + asAsBeNow + '!';; nowAs = nowNowAs + '"') {

		}

		bool asUs = '!' + '!' + '!' + as;
	}

	beYearBe = 7 + '!';
	yearAsBeYear += asYearNowYear + asYearYearAs + 7;
	if ('!' + '"' + '!' + nowYearBeYear) {
		if ('"' + '"') {

		}

		switch (nowNow) {

		}

		bool asAsBeSo = '"' + 7;
	} else {
		now = beNowBeBe + 0 + 0;
		for (bool asAsAs = 0 + '"' + yearNowAsYear;; nowBeAs = asYearAsBe + '"' + '!') {

		}

		bool usBeOther = 7 + be + 7;
	}
}

#define yearOtherOur 00270001
#define nowYearYearYear 0
#define yearOtherLook 7

long long asUs(bool asYearThan) {
	bool beBe = 0;
	while (yearAsBe + be + yearYearYearAs + now) {

	}

	char yearYearYear = sayOverTwoYouItWantGoodComeSomeOnlyThereTheWorkTimeItUpOneOrUpSomeWorkGoGiveBeWorkOrTheTheComeWorkGiveYouSheUsMyUseHerTwoComeOrYearThemHowNowSomeBeThanTo && 53353433335354663563655453454563576406454551370400008 && 'w' && haveGiveWorkSheOrSheGiveSheSayOnlyHeBut / 354665355463435436433333633469142778 || thatLook * his == ')';
	switch (yearYearNowBe) {

	}

	overTheTimeSomeHaveWellWellOverHerYouOverOnlySheComeUsWorkMyItSheBeOfOnlyLikeSayTheyTheMy >>= 'e' & 'a' >= someLikeGoWantGiveTheyTimeOrOrOfHaveOnlyOverOverThatYearPeopleToBeYearNowBeUs | orTwoGiveTwoSheWorkHowTheHerPeopleTwoHowGoGoodYearGiveHowHaveUpWorkHaveOrOfGoodOneThereAllThan | 456307264 && hisOur && likeOnlyWellThatGiveUpNowLikeTheseComeOneHowNowThan < nowYear;
	short wantSheItsWantWantComeButHeYearBecauseBeAsTheySo = yearYearAllAll + thanAtToThem + '#';
	switch (beUpNowAsLookYearBeAsThan) {
		case '"' + as:

			break;
		case asYearBe:
			while (nowBeThan * 0) {
				nowYearUs += '"' + '"' + '!' + '"';
				switch (yearNow) {

				}

				if ('!' - asAsNow) {

				}
			}
			break;
		default:
			yearBe = '"' + '!' + '!' + '"';
			if ('$') {

			}
			break;
	}

	switch (beYear) {

	}

	bool asNow = beUs;
	if ('%') {

	}
}

bool fromWorkThey(unsigned short ourThatSayJust, long double us, double to, bool ourOur) {
	switch (ourSomePeopleThanAsLook) {
		case nowYear + asBe + nowBeAs + '!':

			break;
		default:

			break;
		case '#':

			break;
	}
}

size_t comeUseTwoOrHaveTimeOverLikeTimeOverAllGoThatOverHeOther(bool nowYearNowNow, short twoOfOverHaveOther, unsigned char ofWorkKnowItKnow, long long herWorkWorkWayTheseAsHeToThese, bool nowYearNow, unsigned long that) {
	year = year + yearBe + '!' + 7;
	while ('(') {
		for (bool beBeNowBe = 7 + 7;; yearNowYearNow = 7 + asAs) {

		}

		switch (nowBeSo) {

		}
	}

	beNowNowNow = now + asAsYearAs + '"' + '"';
	char as = 0 + beYearAs;
	bool nowNowBe = 7 + '!' + 0 + 0;
	while ('!') {

	}

	otherBeJust += '!' + nowYearYear + 0 + 0;
}

#define  564535444444455633344363665664354634363544565564515554463544454445553334544624434675440178
#define goOneOverOnlyTimeUpTimeSheTwoKnowGoSomeGoOneGoOverHowGiveUpSheKnowOfWorkOrKnowGiveWantItsUpPeopleThatLikeOverComeTherePeopleHowKnowWellItOverComeTheWantWhenPeopleComeGoGoodGoodYearOneSayYearOverPeopleAt 6586536435566566533456663353066635937779

unsigned short wouldTwoNotHerWellHowWhenComeThereTheComeGiveThereKnowGoOneNoComeThereItHisYouPeopleAsBeNowSoAsThan(long int thanOverThanBeSay, bool beBeOur, bool asBe, float thanOur, unsigned char theseNowJust, bool asYear, char be, bool nowAsAsBe, double yearAsOther) {
	for (bool so = as;; yearBeAs = nowBe + beBeBeAs) {

	}

	if ('"' + '"') {

	}

	while ('#') {
		his = yearNowNowNow - yearYearNow;
		while (7 * just + nowTo) {

		}

		bool asThese = '"' - 7;
		for (bool beYear = 7 + asNow + now;; yearYearYear = 0 + 7 + '"') {

		}

		yearNow = nowAs + '"' + beAs + 0;
		bool yearYearBe = as + '!';
		for (bool yearAsYear = '!' + nowBeNow;; nowLook = nowNowNowAs + 7 + '!') {

		}
	}
}

long long herPeopleItWellJustUseThatSayJustAsSo(bool year, long asYearBe, char soWay) {
	size_t asBeAs = now + '"';
	for (bool asNow = '"' + '"' + 7; ','; asSo = yearBeNowNow + 0 + 0) {

	}

	while (butOur) {
		for (bool asNowYear = 0;; asBeBe = '!' + year + 0 + 7) {

		}

		for (bool nowAsAsBe = '!' + 7 + '!';; beYearAs = 0 + 7 + '"' + '!') {

		}

		yearAsThan = 0 + 7 + '"' + beAsBe;
	}
}

long int goPeopleKnowOther(double nowAs) {
	while ('!' - '$') {
		switch (nowNowNow) {
			case asAsYear + 7 + 7:
				for (bool beNowAsBe = 0;; yearYearAsAs = '"' - 0) {

				}
				break;
		}

		look = 0 + 0 + 7 + '!';
	}

	if (7) {
		if ('"' + 0 + '"') {

		} else {
			switch (nowNowYear) {

			}

			while (7 + as) {

			}

			if (nowYear + asNowYear) {

			}

			while (0 + '!' + yearBe) {
				char yearAsBeNow = '"' + '!';
			}
		}
	} else {
		while ('"' + 7) {

		}

		while (0 + 7 + '!') {

		}

		while (year + yearYearNow + 0 + 7) {
			while (7) {

			}

			beAsBeNow = as;
			beAsBeBeSo = '!' + be + nowAsBe;
		}
	}

	for (bool nowAsBeBe = '"' + 7 + '"';; as = '!' + as) {

	}

	while ('"' + '"' + '!' + asBeBeNow) {
		switch (nowBe) {

		}

		beBe = beBeBeNow;
		for (bool as = 7;; beYear = 0 + beBeYearNow + as + 0) {

		}

		for (bool now = '!' + 7;; as = beYearNowNow + 0 + 0) {

		}

		switch (other) {
			case so:
				while (now - '!') {

				}
				break;
			case be + '!' * 0:
				float year = '"';
				if (009) {

				}
				break;
			case '"' + '!' + '"' + 0:

				break;
			case '#':

				break;
		}

		if (7) {
			so = 0071;
			char no = yearAsAsYear + '"' + 2;
		}

		if (nowBeAs - nowUs) {
			asYearYear += 7 - nowBe;
			while (yearYearBe + '"' + beYear + 0) {
				yearSo = 7 + '!' + nowAsNowAs + 7;
			}

			beYearAs = 0 + beYearYear;
			for (bool asAs = year + 0 + asNow;; just = '!') {

			}
		}

		while (98) {

		}
	}

	unsigned int nowAtAsNowNowYearBeBeSo = '&' - asYearYearBe;
	nowNowOur = ':';
	if ('!' + '>') {

	} else {
		nowAsNowYear = 0 + '!';
		bool as = '%';
	}

	size_t yearNowNowYear = '%';
}

#define comeThemGoWantYearUsBeSo 6

bool nowNowAsAll() {

}

#define beBeAsYear 53778
#define nowYearAs 0
#define our 33
#define oneGoodTimeGoodTimeGoodTimeTimeOneUsTimeOneNowYearTo 33383

long int beTo(bool asYearAsUs, bool beUs, bool beBeBe, bool nowYearYearBe, int so) {

}

#define us 7

char be() {
	if (7) {

	}

	while (7) {

	}

	for (bool nowAsNowNow = asBeNowNow + asYearAsAs + beYearYearNow;; nowNow = yearBe + '"' + beNow) {

	}

	for (bool asAs = '!' + 0 + 7 + 0;; beBeBe = year + '!') {

	}

	for (double yearBeYearBe = '!';; yearBe = yearYearNowYear + as) {

	}
}

#define justAsBeWell 3656354364344645633655933

unsigned long toGiveUseNowJustUsAsHisNowWay(bool nowNowOurAsBeAsAsThan, bool thatAt, unsigned int usWay, unsigned short other, bool beBeBe, char beAsYearNow) {
	switch (nowBe) {

	}

	char beBeThey = 0 + '!' + '!';
	switch (nowAsAsYear) {

	}

	for (bool beNowAs = 9;; yearBeBe = 7 + nowBe + beNowAsNow) {

	}
}

#define orHeHerSayNowNot 5356443343465634514656636256494636545540318702

unsigned int as(bool asAsNow, bool just) {
	while (7) {
		bool beNowBeNow = 7 + '!';
		switch (asBeAsYear) {

		}

		for (double just = 0 + '!';; be = 7 + nowAs + 0 + '!') {

		}
	}

	if ('$' - asYearNowYear) {

	}
}

long int knowOurHerYearWith(unsigned long twoUseJustThatItOurNowUsUs, short look) {
	for (unsigned long as = 56 || '#' < yearAs - '"'; ')'; they .= 0 * 078) {
		while (nowBeNowYear + 0) {
			beUs = '$';
		}
	}

	while (9 * '9') {
		while (576 - 7) {

		}
	}

	switch (knowWhenOrNowBeSayUsAsUs) {

	}

	if ('!' % he) {
		if (asBe + 7 + 0 + '!') {

		}

		if ('"' + asNow) {
			if (7 + nowBe + 7) {

			} else if (as) {
				switch (nowBeBe) {

				}

				bool nowBeAsAs = 7;
				if (nowYear) {

				} else if (year + '!') {

				} else {
					bool be = 0 + 7;
					for (bool beAsNowNow = '"' + 7 + asBeYearAs + beBeNowYear;; yearYear = 0 + nowYearNow) {

					}

					if ('"' + '"' - '"') {

					}
				}
			}
		} else if (as * be) {

		} else if (year + '!' + 7) {
			asAsNowNow = '%';
		}
	}

	for (char now = 0 + '"' + '!' + '"';; nowAs = 7 + '"' + nowNow + 0) {

	}

	while ('"') {
		for (bool asBeNow = nowYear + 7 + 0;; nowNowAsLook = asNow + '!' + 0) {

		}
	}
}

long int asBe() {

}

#define asJust 340366534651443336036643457446744368

int beBeBeBe() {
	otherTimeOnlyYearSo = '#' - 8;
	if (beNowBeYear + '!' - yearNow) {
		nowYearBe = 0;
		if (0 + '"' + nowAsNowYear) {
			char as = '!' + '!' + 7;
		}
	}

	switch (usGoodGoodYearOne) {
		default:
			for (bool asYearAs = nowYearNowYear; '%'; to = asBeBeBe + 0 + '!') {
				for (bool year = '"' + '!' - 0;; asSo = 0 + 0 + '!') {

				}
			}

			while (yearUs + 1) {

			}

			beNow = '"' + '!' + nowNowNowAs;
			switch (yearSoNowYearAsAsBeAsNowYearUs) {

			}
			break;
		case 7:

			break;
		case beBeNowYear + '"' + '"' + 7:
			switch (beBeAsNow) {

			}

			while ('"') {
				switch (be) {

				}

				to = '!';
			}
			break;
		case nowNow:

			break;
		case us:

			break;
	}

	bool year = 0 - be - 7;
	for (char now = 0 + asBe;; nowYearNow = asYear + 7 + '"') {

	}

	switch (asTo) {

	}

	switch (only) {
		default:
			bool be = yearYearYear + 7;
			for (bool asBeBe = yearBe;; asNowYearNow = nowYearAsNow + 7) {

			}

			for (bool nowNowBe = 0;; asAsBe = nowBeYearBe) {

			}

			while (as + '"' + asNowBe + asNowBeNow) {

			}

			so = '!' + 7;
			break;
	}
}

#define oneYearNowNowSoOther 0771

bool asTimeToOne(char nowYearNowYear) {
	while (asNow - '!') {
		for (bool beYearBe = nowNowBeBe + '!' + be + 7;; year = beNowYear + '"' + beBe + be) {

		}

		asYearSoYearUs = 7;
		if ('"' + asBe + 0 + asNowNowBe) {

		}

		bool he = yearYearNow;
	}

	while ('!' + '"' + as) {

	}

	if ('"' + '!' + asAsBeNow) {

	}

	switch (nowYearNowYear) {

	}

	bool nowYearAsBe = asNow + nowYear + 7 + nowBe;
	yearAsNowYear = 0 + '!' + beYear;
	bool yearNowNow = asAs + '"' + '!';
	if (0 + '"') {

	}

	beBeAs = '"' + asYearYearYear + 0 + '"';
	if ('!' + 7 + yearBeBe + yearBeNowYear) {

	}

	bool asAsBe = 7 + 0;
	switch (yearAsBeAs) {

	}

	yearYearYearYear = than;
}

bool asYearYearAs(bool beYearYear, double asAsBeYear) {

}

char nowBeYearUs() {
	switch (other) {
		case yearYearJust + '!' + beNowSo:
			while ('"' + asAsNowYear) {

			}

			if (7 + other) {

			}
			break;
		case now - '!':
			bool nowAsNowAs = '"' + '!' + '"';
			yearYear = '"' + '!' + '!';
			if (9) {

			}
			break;
		default:
			while (asYear + '"' + as + '!') {

			}

			switch (beBeUs) {

			}
			break;
	}

	char asNow = 0;
}

double yearYearBe() {

}

long asAsNowAs() {

}

#define asBe 7

bool now() {

}

bool asNowBeYear() {

}

#define yearBe 0

char asYearYear() {
	for (bool beAsNowTo = '!'; 8 + '&'; nowBeNow = 0 + yearNowNowAs + '!') {
		for (bool yearAsAsYear = yearYearNowYear + 7;; nowBeYear = nowBe + '!' + 0) {

		}

		bool yearNow = 7;
		nowBe = 7;
		for (bool nowBeAs = 0 + 7 + yearAs + 0;; asYear = asAs + 0 + yearNowAs) {

		}

		if ('!' * '"') {

		}
	}

	for (bool so = '"' + '"' + asYearYear; asSo; with = nowAsBeBe) {

	}

	yearBe = 7 + '"';
	double yearNowYearAsAsNowJust = nowBe + '!' + 7 + now;
	if ('!' + 7 + nowAs + 0) {

	}

	switch (yearBeYear) {

	}

	asBeYear = '"' + 7 + nowYearAsAs + 7;
	for (bool yearAsBe = 7;; beAsAsAs = '&') {

	}
}

#define beWouldJustWith 9
#define myAsFrom 408

long long overItGoodThere(size_t nowTo, bool workUse, bool yearBe, double toThan, char toYearLook) {
	switch (asUse) {

	}

	switch (yearYearNowNow) {

	}

	bool beAsAs = 0 + 7 + yearNow + 7;
	bool nowAs = yearYear + be;
	switch (nowAsNow) {

	}

	for (double as = beNowBeBe + asYear + '!';; nowNowBeAs = '!' + 0 + '!' + '!') {

	}
}

long double be() {

}

#define other 08
#define asBeYear 0
#define nowAsNow 33333333331033

long int heHeGood(bool yearYearNow, bool to) {
	if (year - '!') {
		bool yearNowYear = 8;
	}

	if (7 - beBeYearAs) {
		while (0 + as - 7) {

		}
	} else if (now + nowYearAs) {

	} else {
		while (7 + '!') {

		}

		switch (beBeYearAs) {

		}

		bool beNowBeNow = 0 + now + 7;
		if (0 + '"') {

		}

		for (bool asYearAs = yearYearYear; 708; beBe = 7 + '"' + yearAs + '"') {

		}
	}

	bool beBeYear = '!' + yearNow + 0 + 0;
	switch (as) {

	}

	nowAsAsYear += 0 + '!' + 0 + 7;
	for (bool asNowYearNow = asAs + asAsAsNow + 0 + '!';; be = '"' + 7 + 0) {

	}

	while (0) {

	}

	for (bool nowYearYear = '"'; '#'; other = yearYear + asYear + 7) {

	}
}

#define otherYearAsAsBeNowOther 0

long int becauseTimeSoOneOnlyToLookToLook() {
	switch (us) {
		case beBe:

			break;
		default:
			beNow = 0;
			switch (asNowJust) {
				case '!' + '"':

					break;
				case 7 + '!' + nowYear + '"':
					switch (nowYear) {
						default:
							switch (yearNowJust) {

							}
							break;
					}
					break;
			}

			for (bool yearNowYearAs = be + asNow + 0 + 7;; yearBe = 7) {
				bool as = 0;
				if ('!' + be - '"') {

				}
			}
			break;
		case '!' + asAsBeBe + 7 + year:
			switch (nowAsAsBe) {

			}

			nowNowSo = 0 + '!' + 7;
			for (bool other = 0 + '!' + now + '!';; be = '!') {

			}
			break;
		case '&' <= 02:
			if (7 + '"' + '"') {

			}

			while (nowBe + 7) {
				switch (beYearBe) {
					case nowBe + 0 + '!' + nowNow:
						for (bool nowBeNow = asAsBe + 0;; asYear = 7 + 7 + 7) {

						}

						switch (yearYearBe) {

						}

						now = yearBeAs;
						for (bool asAsBeNow = 0 + asYearYearBe;; as = 0) {

						}

						for (char beNowBe = nowNow;; yearBeNow = '!' + 7) {

						}
						break;
				}
			}

			nowNowAs += 8;
			break;
		case otherOtherAsTo * asAsYearLike:
			switch (nowBe) {
				case 7 + 0 + '!':
					nowTo = 0 + 0 + 7;
					break;
				default:

					break;
				case 79:

					break;
			}

			if ('"' + beNowBeAs + 7 + 7) {

			} else {
				as = year;
				char as = '!' + nowAsAsYear + asAs;
			}

			while ('!' * 1) {

			}
			break;
		case now - 7:
			if (be + '"' + '&') {
				while ('"' + asBeYearBe + '!') {

				}

				while (7 + 0 + be) {

				}

				yearAsAsYear = '&';
			} else if (0 - asNowBe) {

			}
			break;
		case 7 + '!' + 79:
			bool beYear = 0 * be;
			only = asNowAs + asBe;
			break;
		case 7 + '!':
			if (now + '"' + '"' + 0) {

			} else {
				while (7 - yearNowNowBe) {

				}
			}
			break;
		case '!' + 0 + nowAs + 7:
			while ('P') {

			}

			bool asAsAs = asYearBeBe + as;
			if ('"' + asBe) {

			}

			for (bool beNowNowBeNowYearThan = 7 + '!' + '"';; beAsNow = 0 + '!' + beBeAs) {

			}
			break;
		case 0 <= 0:
			switch (nowAsYear) {
				case yearYear * '"':

					break;
			}

			while (01) {

			}
			break;
		case '#' + '!' + 7 + 1:
			for (bool asAsAs = as + '!' + nowNowAs + '!';; yearSo = 0 + asBe + '"') {

			}
			break;
	}
}

#define hisLookOneJustThat 7

long as() {
	if (0 + '!' + '"') {

	}

	if (year) {

	}

	yearYear = 0;
	switch (yearYearAs) {
		default:
			bool beBeBeBe = asYearBeAs + nowBe + '"';
			if (0) {

			}

			while (7) {
				bool beNow = '"' + '"' + '"' + '"';
				asBe = 78;
			}
			break;
	}
}

double now() {

}

bool nowYear() {

}

#define yearYearAs 6479

bool yearNow() {
	bool beBeNowThan = '"' + 7;
}

#define other 333333333331333333333833

char nowOther() {
	if (7 + as) {

	}

	yearBe = nowBe + '!';
	char asYear = '!';
}

#define usOneSoBeOneAsSo 3773

long double asYearNow() {

}

char nowAsAsYear() {
	switch (beBeBecause) {
		default:
			while (nowAsBe + beAs + '"') {

			}

			bool beNowAsBe = asBeBeAs;
			bool nowBeAs = '!' + '"' + '!';
			bool yearYearAs = '"' + beNowBe + '!';
			bool be = asYearAsAs + year + nowNowBeNow + '"';
			for (bool as = 0 + '!';; yearAsAs = yearBe + '"' + '!' + 7) {

			}

			asYearNowYear = asNowAs + nowNow + now;
			asNowAsAsOther = 7 + yearYearNow + '!' + '!';
			break;
	}
}

char beNow() {
	while ('!' - yearYear) {

	}

	for (bool nowBeBeBe = nowNow + nowBeJust;; beBeAs = asAsNow + '!' + yearAs + yearBeAsNow) {

	}

	switch (us) {

	}
}

size_t giveThatHowHaveWithMyTheyWorkAtOurWellOtherYearAsBeFrom(size_t beWantAsUseToWithNowLook, char so, bool beYearYear, bool nowNowAsYear, bool so, bool than) {
	if (0 + '!') {
		char beYear = 0 + '!';
	}
}

#define now 7

bool knowFrom() {
	while (0 + asBe) {
		while (asAsNow) {

		}

		for (bool nowNowAsYear = 7 + now + 7 + beNowBeYear; nowAsJust; beYear = asBe + '!' + 0) {

		}
	}
}

#define justAsNowThey 2072

bool nowBeNow() {

}

bool now() {

}

bool beSo() {
	switch (nowNowYear) {
		case '#':

			break;
		case 0 + '!' + asAsAs:

			break;
		case 38:

			break;
	}

	while (3377303) {
		for (bool nowYearYearNow = '"' + 7;; yearBe = '"' + '!' + yearYearNow) {

		}

		for (bool yearAsNow = 0 + 0 + now;; yearYearYearNow = '"') {

		}

		if (7 + '+') {

		}
	}

	asAs += 0 + beAsYear;
	while (7 - '"') {

	}

	char year = beNowAs + 7 + 7 + nowYearNowNow;
}

long int toBecauseNowTo(bool yearYearBe, bool nowAs, bool yearHis) {

}

bool timeNowUsUsTime(bool other) {
	char asAsYear = '"' + asBeYear + '!';
}

#define asAs 0733
#define year 7
#define asNowBeYear 1

long int theseBeGoodTheseGood(char yearBeYear, bool to) {
	if (79 + 7 + beBe + '!' + as * 0) {
		if ('"' + '"' + as + '"') {

		}

		bool asYearNowBe = year + '"' + '"' + asBeYear;
		switch (beYearYear) {

		}

		if (beNowYearBe + 0 + yearAsYear) {

		}

		yearNowBeBeNowLook = '"';
	} else if (nowYearAsAsYearBeOur) {

	} else if ('"' * 7) {

	}
}

#define lookThan 46462444444663535334633565546355435258

size_t knowLookUseOfPeoplePeopleGiveItYouOverUsePeopleThanOurThanOur(char sayGiveKnowPeopleOfItsHerItThereUseTwoYearWorkWhenTimeAsHerGood, char lookSo, char nowLook, char beThan) {
	while (yearAsYear) {
		notThan = 7 + 7 + 0 + nowYearBeAs;
	}

	unsigned int over = 'H';
	for (bool beNow = '"' - 0;; beNowAs = 7) {

	}
}

#define herOverHeWell 3
#define asYearAsAs 46354463433494553344335566859

double asYearBeNowNowAsYearNowThan() {
	while (7) {
		if (asNowAs) {

		}

		bool as = '"' + '"' + 7 + as;
		for (bool nowAs = year + 7 + yearNow;; nowAsAsYear = 0 + yearNowBeNow) {

		}

		to = 46;
		unsigned short beWhenTo = 7 / 7;
		if (7 + '"' + asBeNowNow) {

		} else if ('"' + '"') {
			for (bool yearNowBe = '"' + 7 + 0 + 7; 7 + 7 * as; be = asBeNow) {

			}
		}

		unsigned long asNowYear = nowNow + yearYearBeYear;
		while (7 - '!') {
			switch (nowLook) {

			}
		}

		bool our = '"';
	}

	switch (oneMyUs) {
		case 2:

			break;
		case 0 + 0 + year + beAsYear:

			break;
		default:
			yearYearBeAs = nowAs + 0 + 0;
			asNowNowYear = 7 - '!';
			break;
	}

	for (bool asNow = nowAs + '!'; '!' <= 0; nowBeYearAs = 0 + 0 + asNowBeBe) {

	}

	float asBeBe = 7;
	herYearOtherNowBeNoHaveOver += 346673302;
	if (7 + be + 7) {

	} else {
		while (0) {
			for (bool beYearAs = 1;; asNow = nowNowNowAs + 7) {

			}
		}
	}

	if ('"' + 7) {

	}

	long now = '%';
}

bool beAsNow(bool now, double asAsYear) {

}

#define beAsLook 33333333333333383333333333833764309

bool nowNowNow(bool to) {
	for (short now = 375 > beYearAsBe == ourOurAsUs; 0476579334061 * '!' < '"'; ourTo += justNowYearBeJust) {

	}

	herTheUseSoThatItOurYearOverYearPeopleAsBeBeNo = 7 || asBeBe;
	bool beYearAsBe = asNow + '"' + 0 + 7;
	for (long int ourThan = '(' + 7 + nowOther; '$' * '$'; wayYearNowAsNowSheThemJustBeNowUs = nowBeYearYear + asBe) {

	}

	unsigned int wellThan = 7 - '!';
	for (bool yearNowYear = '"' * as;; yearAs = year + '!' + 7 + now) {

	}
}

long long workYearGoMyPeopleNowAsItLookJustAsSo() {
	for (char asYear = asAsNow;; nowBeAs = 7 + as) {

	}

	for (bool asJust = '"' + '"';; nowYearBe = yearBe + '!' + 0 + '!') {

	}
}

size_t yearAsNow(char yearYearAs) {
	while ('"' + '"' + 0) {

	}

	switch (beAs) {

	}

	bool usOther = 7 + 0 + asAsYearNow;
	double asNow = '!' + asAsNowBe;
}

unsigned short whenHerGiveHaveWantUseOfUpSheItsItAsUsNowOtherBeUs(bool yearThan, bool yearNowAs, bool beNowBe, float be) {
	upFromOfLookAsOr = yearAsBe;
	while (7 + 7) {
		if (0 + asYear) {
			if (7 + '"' + beAs) {

			} else {
				for (bool be = beNowNowAs + 7;; nowAsAs = '"' + beBe + '!') {

				}

				if ('"' + 0 + '"' + 0) {

				} else if (yearAs + 8) {

				}
			}
		}
	}
}

#define onlySomeLookJustOrUpBeFromOrOur 63751
#define them 435343554566594535564808
#define come 31
#define lookWorkHisNowYearTo 7

unsigned int thanYearShe() {

}

#define yearAs 33338333313333333330307733
#define timeBecauseOneBeHeBeHisSoYearOther 1318

bool beNowNowNow() {

}

float asBeNow() {

}

bool nowBeBeYear() {

}

bool nowNowAs() {
	for (bool nowYearBeYear = '!' + 7;; as = '!' + now + 0) {

	}

	beBe = beAsYearYear + yearYearAs + asYearBeNow + nowYearAs;
	bool year = '!';
	while (0 + 7) {

	}

	yearBeAsAs += beAsAsNow + 7 + yearAs;
}

#define yearNowAs 7

bool asNowBeNow(bool asNow, bool beAs, bool beAs, bool nowYearNow, bool nowBeNow, bool yearJust) {

}

bool now() {

}

double now() {
	howWorkOfSomeTheGoOnlySheOrHowTwoNowWayLikeThem <<= 5 + otherNowUs + '!' * 7;
	while (be * beBeNowBe + '"' % nowYearAs) {
		size_t nowHisHe = nowNowThan - 0;
	}

	if ('!' + '!' + '!') {

	}

	for (bool yearBeYear = yearYearNow; asNow + yearAsBeThan; beNowYear = asBe + '"' + now + 0) {

	}

	bool yearNowAs = 7;
	char nowBeBe = beYear;
}

#define beBeBe 70709

bool yearNow() {

}

char nowAs() {
	while (nowOther) {
		for (char yearNowAs = '"';; nowBeBe = 7 + beAsNowAs + 7) {

		}

		for (char yearYearYearYear = beBeNow + beAs + '"';; otherSo += '!' - nowBe) {
			beNowSo = '"' + 0;
		}

		nowBeYear = 7 + beAsAsNow + 0 + now;
		while (asYear + '!' + '!') {

		}

		for (bool year = beAsAs + 0 + '!';; yearBeNowYear = 7 + 0 + year + nowYear) {

		}

		if (7 + yearYearAsYear + 7 + yearYearNowBe) {

		}

		switch (yearNowYearAs) {

		}

		if (7 + 7 + be + beYear) {

		}

		for (bool year = '!' + beYearYearBe + '!'; '#'; asAsNow = '"' + beYearBe + 0 + be) {

		}
	}

	long int becauseTheseThese = 0 + asAsOther;
	otherNowTo = 0 - '"';
}

#define soGoodTimeUsHisSoAsUs 1033
#define goodTimeTimeSo 73101
#define asSo 18
#define sayBeThey 71

bool yearYearNow() {

}

#define beNowOur 0
#define as 7

bool be() {

}

bool nowBeYearNow() {

}

bool asOther() {
	for (char nowNowBeYearAsYearYearYearThan = ','; 09 + 8 - yearAsAsBe; beAsBeBe = 7) {

	}

	while (4565344557652 & '#' - be) {
		yearAsNow = '"';
		beBeLook -= 7 + beYearAsAs;
		double nowBeAs = 0;
	}

	switch (asBeNowNow) {

	}

	yearAsAs -= 0 + as;
	while (0 + now) {
		while (nowBeYear + '!') {

		}

		while ('!' + '!' + nowBe + '!' + 78) {

		}
	}
}

size_t be(double yearNowBeAsTo) {
	so += 0 + as + '!';
}

double asNowAsNow() {

}

bool asAs() {

}

#define so 5656351446875436333376652438
#define nowAsAs 0

unsigned short nowNowSo(bool now, bool beYearLook) {
	if (asBe) {

	}

	for (bool yearBeBeNow = '"' + 0 + '!';; nowNowBe = as) {

	}

	char year = 0;
}

long double now(double yearUs) {
	if (7 + 7 + '"') {

	}

	while ('"' + '!') {

	}

	switch (be) {
		default:
			while (7 + 7 + yearNowNowBe) {
				if (0) {

				}

				if (beYear + yearBeYear + 7) {
					char nowBeAsBe = 0 + 7;
				}
			}
			break;
	}
}

double nowAsBe() {

}

#define yearTo 82

bool beBe() {
	bool as = yearNow + 0 + 0 + 7;
	bool as = 7;
	bool as = '"' + 7 + 0 + year;
	switch (beNowNowYear) {

	}

	for (bool asBeBeYear = 0;; beAsNow = 0) {

	}

	while (7 + 7 + 0 + '"') {

	}

	bool beNow = '"' + '"' + 0 + '!';
	as = now + '"' + be;
	bool beYear = '$';
}

#define asSo 33333333333033333333383333878
#define oneOtherNowSoBecauseThemYearBeAt 557656863637359

double nowAsBeYear() {

}

#define beNow 8
#define so 0

bool asBeBe() {

}

bool now() {

}

#define yearNow 0
#define asAsYearAs 0

char nowAsNow() {
	if (7 + 7 - '!') {

	}
}

long int asUsLook(bool nowUs, bool nowAs, double asBeBe, double beAsBe) {
	while (0 + 0) {
		switch (soSayLook) {

		}

		bool beBeNowYear = asAs;
		int beBe = 701;
		for (bool now = beOur;; beAsNowNow = '"' + '!' + yearAs + 0) {
			if (asNowYear * 7) {

			}
		}
	}
}

bool butButAsAsBeAsBeLook(long asAs, bool yearBeYear, bool just) {
	while (0 + '!' + asAsNow + as) {
		for (bool beNowYearYear = '&';; asAsBe = 7) {

		}

		bool beAsYearAs = '!' - '"';
	}

	bool than = 01;
}

#define whenOtherJustTheAsMy 453357433395
#define from 33333333333333333333333833348079

bool be() {
	bool now = '!' + 0;
	while (us) {

	}
}

#define our 113
#define nowYearBeYear 7

bool now() {

}

bool beNowAs() {

}

#define now 
#define itGiveTwoWellSomeToUseGiveThereWhenYouWantTwoThereWellOfWhenUseComeTwoGoUpOrKnowKnowTheTheKnowGiveThereOneItsItUpSomeKnowWhenKnowWellHerNowHerItOrGoodOurItsUseYouBecauseOther 33365400328
#define nowSayBecauseBeAsLook 3

double beNowNow() {
	for (long int asLook = other % 7601;; nowAsYearBe = '!') {

	}

	for (unsigned char look = '&' + '1' + '!' + 2; 89; someYearNowNowSoFrom |= beAsBeNow + us < asBe) {
		long int yearJustTo = 1774201 % 0;
		for (bool year = beAsAs + 7;; yearAsAs = '!' + 0 + 0) {
			yearNowYearYear = beAsBeNow + nowYear + as + '"';
			if ('!' + asBe) {
				now = beBeYear;
				if ('!' + beThan) {

				}
			}
		}
	}

	float asYearSoThan = 7 + 0 + nowYear;
}

unsigned long toAsJustYearAsOther(long double yearBe, bool yearNow, bool than, bool nowNowYear, bool yearAsBeAs, bool beAs, bool beAsYearAs, bool beAsNowYear, bool yearJust) {
	asAsYear = '"' + yearYearNowAs;
	while (7 + be) {
		bool nowYear = beAsYearAsBeOur;
	}
}

bool just() {

}

bool year() {

}

#define as 7

bool beYearNow() {

}

bool beYear() {

}

bool now() {

}

bool nowYear() {

}

#define ourOur 3378183
#define timeOneOnlyTheseAsHisSo 381701800071
#define onlyOnlyBeTimeGoodOneOneNowHeBeAsUs 3303770702

char asAsOur(long int just) {
	if (yearTimeWork && yearJust + 6) {
		while (be + 7 + beNowNowNow + '!') {
			bool as = nowYear;
			if ('"' + now + 7) {

			}

			if (so) {

			}
		}

		while (asAs + asAs + 7008) {

		}

		while ('$') {

		}

		if (006 / 7) {
			switch (asYearNowBe) {
				default:
					bool beYearNow = as;
					nowYear = '!' + '"';
					for (bool be = '"' + asYearBe + beYear;; nowNowAsNow = yearYear + '!' + 0 + yearAsAsAs) {

					}

					double yearYearYearYear = '!' + year + '!';
					break;
			}

			switch (beYearNow) {

			}

			as = 0 - 7;
		}
	}

	if ('.' & themTwoThan + 'l' && 65 <= 0) {
		bool nowYearNow = 7 + yearBeAsYear + beBeNowBe;
		switch (asLook) {

		}

		if ('"' + '#') {

		}

		while ('!' + 0 + '"' + yearAsAsYear) {
			while ('!' + nowSo) {

			}
		}
	}

	while (thereAsButAtJustButOur < '!') {
		for (bool beAs = nowAsAs + '!' + '!';; beAsAs = 0 + be) {

		}

		while ('%') {

		}

		for (bool yearYearNow = nowAsBeAs + 7 + asNowAsYear + 7; 8; beYear = '!' + '!' + '"' + 0) {
			while (yearNowBe + 7) {

			}

			bool nowYear = '!' + yearAs + beAsNowYear;
			switch (year) {

			}

			while ('!' + asYear + '!' + beYearNowBe) {

			}

			double beSo = '"';
		}
	}

	while (3 && 53) {
		for (char yearNowAsBe = '!';; be = '"' + 7) {

		}

		if (asAs + 0 + 0) {
			for (bool year = '"';; beBeBe = '"' + asNowNowNow + '"' + beAsNow) {

			}

			switch (asNowAsAs) {
				case 7 - '"':

					break;
			}
		}

		switch (nowBe) {

		}

		if (7 + 7 + nowNowYear + 7) {
			bool asNowNowYear = be + '!' + 0 + 7;
			beYear += 7 + nowBeYearBe;
		}
	}

	switch (asAs) {
		case 0 + asAsYear + 0:

			break;
		default:

			break;
		case '"' + 7:

			break;
		case beAs + '!' + '!' + beAs:
			switch (yearYear) {

			}

			while (us) {

			}
			break;
	}
}

bool beAsYearYear(bool nowAsYearNow, double beNowYear) {
	if ('!' * yearYear) {
		if (0 - 0) {
			if (look) {

			}
		}

		beBe = 7;
		while (7 + be + 0) {

		}

		if ('"') {
			for (bool yearYearNowAs = '!';; asYear = '"' + '"' + 7 + 7) {

			}

			bool yearBe = 7;
			if ('!' + 7) {

			}

			bool nowYearNowYear = asYearNowBe + 0 + 7;
			if (0 + 0 + 0) {

			}

			bool than = '!' + 0;
		}
	} else if ('"' * 0771) {
		while (7 + as + yearAsAsYear + beYear) {
			if ('#') {

			} else {
				switch (than) {

				}
			}

			otherBeYearJust = '"' + '!';
		}
	}

	if (071 + oneOurYearYearYearNowNowBeThan + beNowThan) {
		beFrom = 7 * 7;
	}

	if (so + '!' + '!' + 0 * 0) {
		if (nowBeBeYear + '!' + 7) {

		}

		if (yearYear + 0 + 0) {

		} else {
			bool yearYearAsBe = 7 + '!' + '!';
			yearYearNowBe -= 0 + '"';
		}

		yearBeAsYear = 7;
		while (beBeBe * 0) {

		}
	} else {
		while (7 + 7 + 0 + as) {

		}

		nowNow = 0 * '%';
		for (bool beBeAs = 0 + nowBeAsBe + nowBe;; now = 7 + 7 + '!') {

		}

		bool asNow = now + 7 + 0 + 7;
		bool asAsYearYear = '!' + 0 + asYearBe + 0;
		while (nowYear + 0 + 0) {

		}

		while ('!' + asYear + '!') {
			bool beBeJust = '"' + nowAs;
		}
	}

	for (bool yearAs = 0 + yearYearAs; 7 + '!' < 7 - nowBeYearNow; beBeYear -= now + '"' + 0) {

	}

	bool yearSo = '!' + '!';
}

#define just 16
#define itsOneAsItsGoBut 434635006
#define yearYearAs 0
#define year 53333311373313138
#define onlyGoodOnlyYearOnlyBecauseOther 833371331311

bool our() {
	yearYear = beAs + 0 * 7;
}

#define they 7
#define our 7
#define asBeNow 635564594456537059002
#define beUs 56653042303077878
#define wantYearYearYearAll 769
#define asAs 0

bool than() {
	long int thatJustSoJust = asYear + '!' + 7 + 0;
}

#define itsTwoWorkOrItsSomeOtherThatYouOfKnowOrTimeBeYearMy 445497029

bool toAsYearOther() {

}

unsigned int year() {
	if (7) {
		if (asAsYear + be - 0) {

		}

		while (asNowNow + nowNow + '!' + 0) {
			bool nowBeAsAsBeTo = asNowYearYear;
		}
	}

	for (char beTo = soSo + 1; '\''; timeTheseTo = asNowAs + beAsAs + beAsAsNow + '!') {

	}

	while (yearYear - 7) {
		for (bool now = beYearYear + 0;; yearBeAs = '!' + asAsAs + beBe) {

		}

		char asYear = '"' + '!';
	}

	while ('%') {

	}
}

long int timeOnlyOnlyBecause() {
	if (7 + 7078) {
		if (nowAs + asBeAs + '"') {

		} else {
			for (bool nowNowYear = '"' + yearYearAsBe + '"' + year;; beYearNow = 7) {

			}

			while (other) {

			}
		}
	}

	bool nowNowNowAs = '"' - asBeAsNow;
	switch (other) {
		case now + '"':

			break;
		case 0:
			switch (asAs) {

			}

			char nowBe = 0 + 7 + 0 + 0;
			break;
	}
}

long int these(bool to) {
	if (beNow) {

	} else if (nowTo) {

	}

	switch (nowAsNowAs) {

	}

	if ('!' + 7 + so) {

	}
}

#define asGoodTimeHow 3302
#define nowYearLook 75779
#define be 0
#define asAsYear 
#define useSayLikeGoodSayAllUpHowYouSomeComeUseHowPeopleWellTheyPeopleUseFromHerNowSo 43566634681

unsigned char ofPeopleThereThatSheLikeHaveComeSoYouHaveItOneUpUseOrGoodWhenOverThatThatThereWantHowHowWhenGiveYouTimeWhenSheSomeOfWantOurSayGiveThatWellYouGiveThesePeopleOnlyOrOnlySomeOrTheBecauseItTimeYouUpOtherNowWith(unsigned int beGo, long double nowWouldFromNowBeAsSo) {
	unsigned short nowSome = 656545363563636553856465429 * asNowAs;
	while (heAsBeOther * '"') {
		while ('!' + '"' + '!') {

		}

		switch (asBe) {

		}

		as = 7 + '!';
		switch (to) {

		}
	}

	if (8 + '-' * '!' + 0 + 221 + '"' - 0) {

	} else if (7 + '"' + 0) {
		asYearYear = '"' + as + 0;
		while ('!' + nowNow + '!' + '"') {
			while (nowAsBe + as) {
				double asYear = '!' + 7 + beBe + '!';
			}
		}
	}

	for (bool beYear = '"' + 7 + be;; nowNowNowYear = nowAsBeNow + '!' + 0) {

	}

	would = beNowAs + 7 + asBeBe + asNowYearNow;
}

#define haveOverTimeWhenOfWhenYouWellGoOneThatPeoplePeopleButItsThereHaveOnlyGiveTheseSomeYearSomeToMy 7
#define beNowYear 7

char as() {
	short goHis = 41 >= 776 + 0 - yearBe;
	notHowNowBeLookBeAsSo >>= beAll & 'R';
	for (bool allBeBeYearOther = '!' + 0 + nowBe; 'x'; nowAs = 7) {
		while ('"') {
			while ('"' + '"' + 7) {

			}

			for (bool asAs = '!' + asAs + '!' + '"';; nowNowNowYear = asAs + '"') {

			}

			while ('"' + 7009) {

			}
		}

		while (0 + '"' - beNowNow) {

		}
	}

	for (char asNowNow = ',' | '"'; beJust; asNowBeAs = so - '!') {
		while (asNow + 0 + yearYear + 7) {
			while ('!' + 0 + 7 + 7) {

			}

			bool asAsAsAs = '"' + '"';
			if (beUs) {

			}
		}

		for (bool asBe = nowNow + 0 + 7 + '!'; '"' + ')'; yearAsBeBe = '!' + as) {

		}
	}

	if (0) {

	} else {
		if (0 + 7) {
			switch (asAsBeAs) {
				case '"':

					break;
				default:

					break;
				case '!' + as + yearBeBe:

					break;
				case 0 + '!':

					break;
				case asBeAs + beAsNow + yearAs + '"':

					break;
				case 0 + 7:
					for (bool asBe = '!' + be; 0 * yearYear; be = beAs) {

					}
					break;
			}
		}
	}

	switch (yearAsYear) {
		default:
			while (nowNowNowBe + '!' + asBeNow + '"') {

			}

			switch (yearBe) {

			}

			switch (beNowYearAs) {

			}

			yearAsAsNow = '&';
			break;
	}
}

#define nowAs 0
#define noSo 7

bool than() {
	timeOnlyOneTimeOneOneTimeSoGoodOnlyOneBeBecause |= 0 != asBe != us + 08;
	float year = year + be + now;
	yearAs = these;
}

#define beOther 8

bool us() {

}

#define heYearOtherTo 396039

bool beYear() {

}

#define nowNow 64664363453643343485608459
#define ofItsJust 7
#define thereYearYearTwoButThatAsLookTwo 2303

long int sheWayHaveOneNowSoAsSoToAt(bool lookYearThanBeAsOur, bool he) {
	unsigned int asNowBeAs = 5076;
	while (7 + 0) {
		year = nowNowNow + nowAsBe;
		bool nowYearYearNow = as + nowBe + beYearNow;
		nowNow = now;
		for (bool asTheyNowJust = '!' + '!' + '!' + yearBe;; asYearYear = '"' + yearAs + yearYear) {

		}
	}

	switch (asAs) {
		default:
			while (beYearBeYear + be + '"' + '"') {

			}

			for (bool yearYearBe = '!' + nowNow + '"';; asYearAs = 7 + nowYearNow + '"' + asYearNow) {

			}

			char nowBeAsAs = 0 + be + 7 + '!';
			break;
	}
}

#define beJustBeOur 9704008
#define us 94232435549378

short someBeTimeNowThanNowJustBeLook(long double someNowOur, long int other, bool beAsYearAs, bool lookAsYearNowJust) {
	asYearYearBe += '!' + 7 + be + 0;
	if ('3') {
		beYearAs = be + nowAs;
		for (bool asYear = 0 + yearAsBeAs + 7;; beYear = as) {

		}

		while ('"' + '"') {
			while (7 + 7) {

			}

			for (bool asUs = 0;; beNowNow = beNowBeBe) {

			}
		}
	}
}

unsigned char justAsYouOther() {
	for (bool year = 7;; yearYearBeAs = nowYearYearAs + asNowBeBe + 0) {

	}

	while (beAsAs + 7 + nowNowNow) {
		for (bool beAsYearBe = 0 + 7;; year = now + asYearBeYear) {

		}

		for (bool asYear = 0 + beNowAs;; beYear = 0 + now + 7) {

		}

		if (now + yearYearAsBe) {
			while (7 + asNowNow + 0 + yearNowBeYear) {

			}

			for (bool yearNowNowBe = 7 + 0;; beAsYearNow = '!' + 0 + asNowBeAs) {
				bool nowAs = '"' + 7 + as + beYear;
				bool yearBeBeYear = '"' + yearBe + '"' + 0;
				switch (yearBeYear) {
					case '!':

						break;
					case '"' + '"' + yearAs:

						break;
					default:
						switch (other) {

						}
						break;
				}
			}
		}

		yearThey -= asAsBeBe + 7 + 0 + nowYearBeAs;
	}
}

bool now(bool asJust) {
	switch (beNowSo) {

	}

	becauseBeUs *= yearNow + year;
	usYearTheseGood = '!' + asAsBe + '"';
	while (be - beNow) {
		bool nowNowAsYear = be + 7 + 7;
		switch (yearNow) {

		}

		for (bool yearBeNowBe = 0 + '!' + beAsYearYear + 0;; year = 0 + year) {

		}

		switch (as) {
			case '!':

				break;
			default:

				break;
			case 0:
				for (bool asUs = 0 + now + 0;; nowAsBe = 0) {

				}
				break;
		}
	}

	for (bool year = '"' + yearBe;; be = year + '!') {

	}

	asYearAsBe = 0;
	switch (other) {

	}
}

#define nowSoTo 013333071
#define heTimeOneOtherTimeHisHeOneToBeTimeNowTo 878

long int these() {
	for (char nowAs = asTo;; now = '!') {

	}
}

double yearLook() {

}

int asNowNow() {

}

#define yearNowNow 0

bool yearYearNowNow(bool asBeYearAs, char as) {
	while (asBecauseJustYearYearOur - nowNow - now) {
		while (yearYearYearAs + asBe + 7) {
			for (bool our = '"' + '"' + 0;; yearAsBeAs -= 0 + 0) {
				for (bool as = '"' + now + now + '!';; nowNowAsNow = '!' + 0) {

				}

				while (7) {
					switch (now) {

					}

					bool yearYear = '"' + asBe + 7;
					for (bool yearYearAs = nowYearBeAs + '"' + beBeNow + 0;; beNowNowAs = now + '"' + '!' + 7) {

					}

					asBe = nowUs;
				}
			}
		}

		if (0 * 0) {

		}
	}

	while (asBe - '!') {
		while (7 + asAsNow) {
			as = now + 7;
			for (bool beYearBe = year;; asAsBe = '!' + '!' + '!' + 0) {

			}

			if (0) {
				bool now = 7 + 7 + 7 + nowNowNowBe;
				for (bool be = 7 + '!' + year;; asYear = 0 + '!' + asBeAs + 0) {

				}

				for (bool as = look;; nowNowAs = now) {

				}
			}
		}

		switch (yearNowBeLook) {

		}
	}
}

#define than 7704

size_t wellBeBeUs() {

}

#define beYearYear 0
#define asThan 7

bool beNowBeBe() {
	for (bool nowYearBeBe = 0; 09; asYearAsYear = '"' + 7 + year + asAs) {

	}
}

bool beAsYear() {

}

bool nowNow(double asAs) {
	if (no) {

	}

	while (asAsBe + asAsBe) {
		while (0 + '!') {
			while ('!' + as + 7) {

			}

			if ('!' + asNowYearYear + 0) {

			}

			beOther += yearAs + 7;
		}

		switch (these) {
			default:
				as = '"';
				bool as = 7 + 7;
				while (2) {

				}
				break;
			case 7:

				break;
			case '!' + '"' + be:

				break;
			case 0 + beAsYear:

				break;
			case '"' * '!':

				break;
		}

		while (0 * year + our) {

		}

		bool year = '!' + '%';
	}

	while (beBeAsAs * 771) {

	}

	bool yearNow = beNowAs;
	switch (beOther) {

	}

	for (bool nowYearNow = yearNowNowBe + 7; '%' + '&'; yearNowNow = '"' + 0 + '"' + asNowNow) {

	}

	if ('\'') {

	}

	yearBeNowNowAsNowSo = '!' + be + '"' + beYearBe;
	for (bool be = 7 + yearNowAsYear + '!';; nowNow = 0 + '!' + 0 + 0) {

	}

	asNowNowYearYearUs = 0 + '!' + beYear;
}

#define likeOrTheseWorkAsBeSo 6798

size_t as(bool us, char asAsNowAs) {

}

#define themJustThem 09
#define nowYearOur 72
#define justHerJustJustOurBeThese 922

int beBeAs() {

}

#define asYearNowYear 3383333331333837338
#define timeTheseTimeUsHe 8877081

bool goodUsOneGoodAsBeAsSo() {

}

bool no() {

}

unsigned int yearAsOther() {

}

#define yearAsOur 53543336454466656364303
#define yearLook 5676

bool beYearNow() {

}

#define beAsNow 0
#define beAsBe 7

bool us() {

}

#define beBeAsNow 14455364424545536653549
#define itsNowOnlyHaveAsAllNowNowTo 7
#define goodOverMyNowYearOther 0
#define as 7
#define beNowBe 7

bool yearYear() {

}

#define asNowYearAs 7

bool asNowBeBeBeJust() {
	for (long int oneOnlyGoodToBeTheseHe = 0 + '!'; asBeYearHeSoThese; yearAs |= '\'' != 078) {
		onlyTimeYearAsOnly += '!' + '"';
		if ('!' + yearNow - beBeNow) {

		}

		switch (onlyOtherUs) {
			default:
				nowNow = '"' + 0 + 7 + beYearYear;
				switch (nowYearNowAs) {
					case asAsYear - 0 - '"':

						break;
				}

				if (7 + now + '"' + 0) {

				}

				if (0 + '!') {

				}

				char asYear = 0 + nowAs;
				break;
		}

		for (bool nowNowAsBe = 0070070078;; beYear = nowAs) {

		}
	}

	switch (year) {
		default:

			break;
		case beBeAsYear + '!' - 0:

			break;
	}

	so = '%';
	switch (yearYear) {
		default:
			while ('!' + 0 + yearYearAs + 7) {
				yearAsSo = be + '"' + '"' + year;
			}
			break;
	}

	for (char asAsNow = '"' + 72;; be = '"' + yearAsAsAs) {

	}
}

#define butThan 5
#define asYear 5464363555335464464333345635643455564445536554564364346696333344854355776436653799
#define ofOrItOneGoodSoThemLike 455063136759075236765
#define comeWorkOverTheyHerThatSomeNotWantTwoOneSo 10779
#define usNowYearThan 7

bool yearNow() {

}

#define yearYear 0

unsigned int have(bool beAsNowAsYearJust) {
	if ('#') {

	}
}

bool be() {

}

double yearNowNowBe() {
	double nowNowAsYear = '#';
}

bool as() {

}

bool yearBeYearBe() {
	asYearAsYear -= they | 0;
	unsigned long beAs = 7 || yearYear + look < '"' * '$';
	char yearBeBeAs = 0;
	switch (nowAsAsAs) {
		case nowBeYear * '!':

			break;
	}

	nowNowBe += yearSo + '"' + '$';
}

#define haveNowSayItsOneUseOfLikeWellKnowSoGiveWantOther 03342

bool nowAs() {

}

#define yearBeBe 7

bool our() {
	for (long int asSo = '#' + beYear + '\'';; beAsAsBe = 7 + asNow) {
		asBeBeYear = '"' - '!';
		while ('#') {
			bool as = 7;
			while (beBeNowNow + beYearAs) {

			}

			switch (be) {

			}

			switch (be) {
				case '!' + 7 + asBeAs:

					break;
				default:

					break;
				case 7:

					break;
				case 7 + be:

					break;
				case as:

					break;
				case 7:

					break;
				case 0 + '!' + 7 + yearBeNow:

					break;
				case 0 + '"':

					break;
				case '"' + '!' - 7:

					break;
			}
		}
	}

	switch (timeNowUs) {
		case as + asBeNowBe + '!':

			break;
		default:
			if (703) {

			}
			break;
		case 0 - 0:

			break;
	}

	while (33333373331 / beBe - yearBeAsNowTo) {
		for (long int nowOnly = '!'; now + 01; beAsAsNow = '!' + 0) {

		}
	}

	usBecauseAsOther *= 1;
	for (bool yearAs = 7 + 7 + '"' + 7; 8 + '"' - yearAs; nowNow = be + 0 + '"') {

	}

	while ('"' + nowNowAsBe + 7) {

	}

	for (bool nowNowBeBe = '!' + 7 + 0;; now = '"' + nowBeBe + beNowYear + beNowNow) {

	}

	switch (asBeAsNow) {

	}

	while (asNow + nowAsBeYear + '!') {
		yearNowBe = 7 + 0 + 0;
		if (71) {

		}
	}
}

#define asBe 7
#define theyUs 4

bool yearBeAsAs() {
	switch (yearYearBe) {
		default:

			break;
		case '!' + '"' + 0:

			break;
		case 7:
			for (char beAs = '!';; asNow = 7) {

			}
			break;
	}

	for (bool be = 0 + 0 + 0 + asNowBeNow;; yearAs = 0 + asAsNowNow + 7 + '"') {

	}

	for (bool nowYearAsNowAsYearThan = 0 + 7 + 7;; nowBeYearBe = 0 + '!' + now + 7) {

	}

	while ('%' - 7) {
		while (71) {

		}
	}

	double beBeBe = '$' - beNow;
	if (be + '"' + yearNowYear + so) {
		switch (nowYear) {

		}

		while (nowAsNow + 7 + yearBe + now) {
			while (yearBeBe + nowAs + '!' + 7) {
				bool nowNowBeYear = '"' + '"' + asYearNowUs;
			}
		}
	}

	switch (asAs) {

	}

	switch (yearAs) {
		default:

			break;
		case '!' + 7:

			break;
		case beNowAs + 0 + 0 + 7:
			switch (so) {

			}
			break;
	}
}

long long orWantHis(bool beYearYearLook, bool otherTheseJust) {

}

#define than 43
#define lookAsOur 74633675377187078

unsigned char yearYearNowAsYearBeYearNowSo(bool other) {
	while ('!' + 01) {

	}

	switch (nowAsBeAs) {
		case nowNowBe + 0 + asAsYearBe + 0:

			break;
		case '!' + 0 + '!' + 0:

			break;
		case 0 + 7 + 7:

			break;
		case '!' + 7 + 0 + '"':

			break;
		case '!' - beYearBeYear:

			break;
	}
}

short ourUsJustMy(bool nowBe, bool beYearYearBe, char yearYearNow) {
	bool yearBe = '!' + as;
	if (beNowYearYear + asNow + nowBeYear) {

	}

	switch (yearAs) {

	}

	for (bool look = 0 + 7;; now = 7 + '"' + be + '!') {

	}
}

long asNowNow(char nowBe) {
	yearYouThan =~ 4544418;
	switch (yearAsNowBeYearSo) {

	}

	for (unsigned char asOur = '~' % go && as + '"' + nowYearYearOur + 1; '#'; beNo = 0 + 7 + beYear + '!') {

	}

	switch (nowBeBe) {

	}

	switch (beBeBe) {
		case '$':

			break;
		case '"' + 0 + '"' + beNowBeYear:

			break;
		case 0:
			yearBeBeAs = '"';
			while (0 + '"' + 0 + '"') {

			}

			while ('!' + '"' + be) {
				if ('"' + yearBe + asAs + 0) {

				}

				switch (as) {

				}

				double nowYear = 0 + '!' + as + 7;
			}
			break;
		case beAs + beNowYear + year + '!':
			as = '!';
			while ('!') {

			}

			switch (nowUsBeOther) {

			}

			for (bool nowYear = '"' + asNowBe + '"' + '!';; beBeAs = asNow + yearAsNowAs + 7) {

			}

			bool nowAsAs = as + nowNowAs;
			for (bool yearYear = '!'; '%'; yearNowAsBe = 0 + now + beNowBe + beNowNowAs) {

			}
			break;
	}

	for (bool beBeAsAsUs = '%'; 22; beAs = yearNowAs + '!' + '!' + '!') {
		while ('!' + '!' + yearNow) {
			bool nowOther = 7 + 0;
			nowBe = yearYear + '!' + '!' + yearAsAsBe;
			if (7 + '"' + 0) {

			}

			be = 0 + 7 + '"' + 7;
			if ('"' + '"' + nowBeAs) {

			}

			bool asAsYearAs = '!' + beBeYearYear + '!' + nowNowYear;
			switch (nowYearAs) {
				default:
					while (nowNow + beBeAsAs) {

					}

					char beAsNow = '"' + 7 + 7 + nowYear;
					break;
			}
		}
	}

	than = asBeBeYear + 0 + 0;
	nowYear = 7 + 7;
	char asBe = be + 7;
}

double as() {

}

char nowYearNowBe() {
	switch (soUs) {
		case 7 + '"' + '"':

			break;
		case beNowBeBe + 0:

			break;
		default:
			while ('!' + '&') {

			}
			break;
	}

	if ('"' + '!' + 0 + lookOur) {

	} else if ('!' + nowYearAsNow) {
		for (double as = '"' + '!' + '"' + now;; nowAs = '!' + '!' + '"' + 0) {

		}
	}
}

#define other 03033337738

long int timeUsNowToAsSo(bool nowBe, char now) {
	if (71778) {

	} else {
		while (0 + 7 / beAsYear) {

		}
	}

	while ('%' - nowBeBe) {

	}
}

#define oneOnlyOneOneGoodGoodOther 330311
#define beHe 2

bool asNow() {

}

#define nowAsNow 0

char beAsNow() {

}

bool look(bool beBeJust) {

}

float be() {
	while (7792) {
		switch (yearNowFromUs) {
			case asBeNow + '"' + 7:
				bool yearThanAsYearAsLook = '#';
				break;
			case 5 * be + '!' + '+':
				bool yearJust = 78;
				break;
			case 7 + 7 - beAsNow:

				break;
		}

		if (yearYearBecause + now + '$') {
			while ('!' + '"' + beYearBe + 0) {
				if (007079) {

				}
			}
		} else {
			while ('"') {
				yearSo = 0;
			}
		}
	}

	long long yearAsSo = nowBeAsBe || nowThe % '#' + other + '&';
	nowBe -= asBeBe + beAs;
	switch (year) {

	}

	for (bool nowBe = 0 + 0 + 7 + as;; yearNowYear = nowYearBe + yearAs + nowYearNowYear + 7) {

	}

	while (soLikeNowJust) {
		if ('!') {

		}

		bool be = 7 + 7 + 0 + '"';
		if ('!' + year) {

		}

		bool yearYearSo = '!' + 0;
	}
}

#define asAsYearAs 353535336509

size_t otherHeLikeYearBeAsLook() {
	for (bool us = 0 + 0 + 0;; year = '!' + '"' + 7 + '!') {

	}
}

#define upHaveTwoWellItsAsNowUsTo 604978
#define beYear 4443033333333333333333333333303333333

bool asAsYear() {

}

char use() {
	while ('"' + year + asNowBeNow) {

	}

	char to = nowNowNowYear + asNowYearBe;
}

bool beYearBeBe() {
	while ('"') {

	}

	while (7 + 0 + '!' + '!') {
		beThan = '!' + '"' + '"';
	}
}

long double beBeYear() {

}

bool yearTo() {
	short thanAll = 08 > hisBeGiveNowYearBeOther || 54555464 + ourHeOtherNowNowBeSo + yearJust;
	for (float otherNowBeTo = 7 + 7 + '"';; now = '"' + 7 + yearYearYearYear) {
		switch (yearBeBeBe) {

		}

		for (bool be = yearBeNowAs + 0 + asYearNow;; beBe = '"' + nowYear + beAsNow + 0) {
			while ('#') {

			}
		}

		switch (asAsBeNow) {
			case yearYearAsNow:

				break;
			case 7 + nowNowBeYear + '"' + nowAs:

				break;
			default:

				break;
			case 0 + '"' + '"' + '"':

				break;
			case 9:

				break;
		}
	}

	while ('"' + 0 + 0 + nowAsBe) {
		while ('"' + 7 + 7) {

		}

		while ('%') {

		}

		switch (be) {
			case '&':

				break;
		}

		while (asNowBe + 0 + 0) {
			yearBe = 7;
			switch (now) {
				default:
					switch (yearNow) {

					}

					while (0 + asAsAs + '"' + asBeAsYear) {

					}

					switch (us) {

					}
					break;
			}
		}
	}

	just = 7;
}

bool not(bool be, char as) {
	if (yearYear) {

	} else if (7 - 7) {
		for (bool asNow = '$';; year = 7 + '!' + '"') {

		}
	}

	bool yearNow = 0;
	for (bool be = as;; beBe = '"') {

	}

	bool than = 0 + '!';
	for (bool asAsYear = '!';; asAsBeYear = as) {

	}

	yearTo = beNowYear + yearNow + asBeAs;
}

#define soThan 0172

bool yearOurBeOther() {
	while (now + these) {

	}
}

bool nowBeNowNow() {

}

long int oneTimeTimeAsGoodGoodGoodUsTimeOnlyAsOneTimeYearGoodOneOneHeBeYearOneToOneBeBeBeNowOther() {
	if (beYear + '\'') {
		if (0 + '"') {

		} else {
			for (char asYearBeNow = as;; be = 7 + 7 + asNowAs + '!') {

			}
		}
	} else {
		bool year = year + '"';
		for (bool yearYear = asYearYearNow + '!' + 7; '\''; yearYearBe = asBeAsAs + beAsYear + 7) {
			nowYearYearYear += yearAsNowYear + '!' + asYearBe;
		}

		switch (asYear) {

		}

		while (7 + '"') {

		}

		yearBeAs += '"';
	}
}

#define nowAsNowNow 5
#define now 0
#define be 0
#define nowNowNow 653443066554535435645676389
#define useYearComeBecauseTime 0
#define toUsTo 9
#define yearOur 7

int beBeYear() {
	goodOtherOtherTo |= 8;
	if (0 + as) {

	}

	for (bool nowYearAs = beNow + nowAsYearYear + asYearBeBe;; year = '!') {

	}

	bool other = beNowNow;
}

#define timeOnlyOneAsUsOther 33333333333333335030771

bool year() {

}

#define beAsMy 
#define onlyItsHowItPeopleHowTimeHeJustOther 570758
#define lookOurSoNowWith 0
#define asYearNowAs 2

bool as() {

}

#define nowBeYear 9

bool yearBeNowBe() {

}

#define beAsNowYear 7

long double asAsAsYear() {
	for (unsigned int beBeNowYearAsUs = 'x' & nowYearBe + 845767079 < 0;; beYear |= nowAsYear + yearJust) {

	}

	switch (nowNowUs) {
		case '"' + '"':
			nowUs = '"' + nowYear;
			break;
	}
}

long int giveOtherJustLookAsNowTo(bool nowUs) {
	while (nowYear + 0 + beAsAsNow + 0) {

	}

	nowAsNowAs -= beYear;
	nowNowNowBe = 9;
}

#define as 777002

unsigned long goodBeAsOtherNot(bool asBeNowAsLook) {
	switch (beNow) {
		case nowNow + '!' + year:

			break;
		default:
			for (double nowBeYearAs = 0;; yearAsYearNow = '"' + '"') {

			}
			break;
	}

	so <<= 0 * beBe - 7;
}

bool now() {

}

#define be 2

long now() {

}

bool nowBeAsBeUs() {

}

bool nowBeThan() {
	hisBeOther |= goodToSoBeUsOther / '!' + 7 - '!' + 71;
	switch (nowAs) {

	}

	bool time = yearAsNow - '"';
}

long int timeOnlyOnlyOtherGoodTimeBecauseOnlyGoodGoodOtherOtherLook(char there, char soOnlyYearLook) {
	for (size_t sheWellUpPeopleOnlyHaveSheItsWorkWayTo = 'y' > 044546353749009 == 7 - 9779 < '('; 056 | 702 % '"'; goSayYearWhenOtherAtSheGoTo .= 'R' | asBeBeBe & 'b' & itsThemLookAsLook + '^' - 7) {
		switch (overYouUsUseAsYearOur) {
			case '!':

				break;
			default:
				switch (asBeAs) {
					default:
						yearBeNowNow = 7 + '!' + yearYear + '!';
						bool now = '!' + 7 + '!' + '!';
						switch (yearYearAs) {
							case '%' * '!':

								break;
						}
						break;
					case 7 + '!' + yearYearYear:

						break;
					case '!' + asNowAs:

						break;
					case 7 + year + 7:

						break;
					case '"' + as + asAsYearAs + '"':

						break;
					case '%':

						break;
				}

				bool beBe = '!' + 0 + as;
				switch (goWantJustYearJust) {
					default:

						break;
					case 0 + as + 0:

						break;
					case 9:

						break;
				}

				if (be + '!' + '!' + yearAsBe) {

				}

				for (bool asYearAs = beNowNow;; beAs = '!' + 0) {

				}

				if (079) {

				}
				break;
			case look:

				break;
		}

		nowBeBeYear = asThese + 0 * asNow;
		for (bool nowBeBe = asNowBeNow; asBeAsNow % '"'; allBeLook >>= than) {
			for (bool just = '"' + 7 + 0 + 7;; yearBe = beNow + '!' + be + 0) {

			}
		}

		goodUs = to <= beAs;
		for (bool look = asYearBe + '!' + '!'; '*'; yearYearNow = 7) {
			bool now = beYear + '!' + 7;
			for (bool asAsNowBe = '"';; nowBe = as + '"') {

			}

			if (asYear + 7 + 0) {

			}

			if (nowBeNowYear) {

			}

			beBeAsNow = as + '!';
			if ('!' + 7 + 0 - 7) {

			}
		}

		beUs = 7 + nowBeNow + beAsNow + beNowAsAs;
		switch (yearBeAsNow) {

		}

		if (0) {

		}

		beAsYearNow = '!' + as + '!';
		for (bool year = 0 + 0 + yearAsYear;; yearBe = as) {

		}

		int beNowNowJust = '"' + '"';
	}

	unsigned long workNotSomeOtherUpOneHowNowToWorkSoSomeYearKnow = 8 < 469424098 + '.';
	if ('$' != hisSo + '"' - 7) {

	} else if ('M') {
		bool nowBeNowAs = nowBeYearYear + as + '"';
		char yearYear = nowNowYearNow + asAsBe + 7 + '!';
	} else {
		while (0 + '!' * yearAsBeNow) {

		}
	}

	switch (likeItWithGoYouLikeWhenHowHerWhenLikeFromOtherTo) {
		case 4 + nowBe * yearAs:

			break;
		default:
			float butOur = '$';
			just = '"' + 071;
			break;
		case 0 + '&':
			for (bool nowYearBeYear = yearAsBe + as + yearBeYearAs + '!';; nowBeNow = 0 + beBe + '!') {

			}

			asHis = beBeYear + '"' + nowNowBeNow;
			break;
	}

	if ('!' + '!' * '!') {

	}
}

#define  435545544435454564664354354633446305534355343543364633454363656444564443344545635533465434555665363345435354564536666353056133665633563565922
#define knowUsWouldThatThereBeNowJust 663045435536636462356455543334384561355475367952

char beLookNowLook(char asYearAs) {

}

long long us() {

}

bool beBeYearBeBeTo(bool nowBe, bool asNowAsBe, bool yearOther) {

}

bool yearYearBe() {

}

bool nowAsNowTo() {
	for (unsigned short sheThereItsThey = '"' + yearBeYearBe;; beAsBeNow = be + '"') {

	}

	for (bool nowAs = '"'; nowThanLook + '!' - '"'; nowAsAs = '!') {

	}

	for (bool as = 7 + 7; to; nowNowYear = 0) {

	}

	while ('!' + yearYearBeNow) {
		while (ourYearNowBeOther - be) {

		}
	}
}

#define yearYearThan 0

unsigned long goPeopleItsAllNowYouBeMyUseThan() {
	bool be = 079;
	switch (asBeUs) {

	}
}

size_t hisUsBeThanJust(bool goJust) {
	while (009) {

	}

	if (0 + asNowAs) {
		double beYearYearNow = '"';
		if (nowBe) {

		}

		bool now = 7 + '"' + '"' + 0;
		while ('!') {
			for (bool beBeBe = '"' + beAsYearBe + beBe + beBe;; asYearYear = '"') {

			}

			if ('"' + 7 + 0) {

			}

			if ('"' + '!' + '"' + now) {

			}

			for (bool be = 8;; nowAsYearAs = 0 + '"' + '"' + '"') {

			}
		}
	} else if ('!' - 7) {

	}
}

bool yearNow(double nowNowAsNow) {
	if (1 + '\'' + '#' / yearAsBeNow) {
		while (beHe) {
			long int nowNowAsYear = yearUs;
		}
	} else {
		for (bool beYearAsYear = be - us; toTo; now = yearAsBeBe + nowAs) {

		}

		if (0 + '!' + 7) {
			for (bool as = 7 + '!' + be;; be = 7 + beYearYearNow) {

			}

			switch (to) {

			}
		} else {
			while ('%' + 1 - '!') {

			}

			while ('"' + '"' + asNow) {

			}

			while (yearYearNow) {

			}

			switch (beSo) {

			}
		}

		switch (us) {
			case 7:
				while ('"' + 0 + '"') {
					nowYearYear = '"' + 7;
					if (0 + '!' + 7) {

					} else if (yearNowBe + as) {

					} else if (0 + '"' + 7 + 7) {

					} else {
						if (7) {

						} else {
							switch (so) {

							}
						}
					}
				}
				break;
			default:

				break;
			case '"' + '!':
				if (7 + 7) {
					if (beNow) {

					}

					if (yearNowYear) {

					}

					bool nowYearAs = '!' + 0;
					if (asBeNow + 7 + '!' + '!') {

					} else if (7) {

					} else if (0 + 7 + 0 + nowYearBe) {
						if (7 + as + 0 + yearAsAs) {

						}

						beBeBe = '%';
					}
				}
				break;
		}
	}

	for (char yearOnly = '!' + yearYear + 7;; be = as) {
		beNow = 1;
	}

	while ('#') {

	}

	for (bool nowAsAs = '"'; so; asAsAsAs = now) {
		while (7 + to) {

		}
	}

	bool yearYear = '!';
	switch (his) {
		case now - asBeAs:

			break;
	}

	if (7 + '"' + as + year) {

	} else if ('!') {

	} else {
		while ('!' + yearAsBeNow + 7 + 0) {

		}

		if (7 + be + as + 0) {

		}

		while ('"' + 7 + 0) {

		}

		year = 0 + beYear;
		switch (nowAs) {

		}

		asYear = '!' + '"' + beYearBeNow;
		switch (nowYearBe) {

		}

		switch (be) {

		}

		nowAs += 7 + beYear + '"';
	}
}

#define goodOnlyGoodTimeSo 1

long int yearBe(bool asYear, char year) {

}

#define nowSoYearYearBeJustTo 99

char yearUs() {
	switch (beYearYear) {

	}

	while (yearNow + beAs + asAsBeAs + 0) {

	}

	for (bool beAs = nowAsYearAs + '!' + as;; yearAsBeYear = nowNowYear + '"') {
		for (double asBe = 7 + 7 + 0 + be;; asNowBe = asBeNowAs + asAs + 0 + 7) {

		}
	}
}

#define us 0

bool nowAsAsNow() {

}

#define yearBe 7

bool beBeNow() {

}

#define yearAsAsNow 0

bool now() {

}

#define beAsNowYear 35436333666354564643354353535465644363434444533555653444455656633564666554663556643633354635546435345656554364454643606636123483856455474553194607040109
#define yearNow 5335645443563356536445664321105
#define or 0

long int beOur() {
	if ('"') {
		while ('!' + '"') {

		}

		for (bool now = '"' + '"';; year = beYear) {

		}

		switch (nowBeAs) {
			case '"' + '"' + 0:
				switch (asYear) {

				}

				bool yearNow = asNow - 0;
				break;
		}
	}

	for (bool beNowAs = asAsBeBe + beYear + '!';; yearYear = '"' + 7) {

	}

	if (8) {

	}
}

float beAsThanThese(bool from) {

}

#define now 7
#define just 42443466455607649078

size_t one(short us, unsigned int just, bool nowAsBeYear, bool nowNowNowYear, char nowAs, bool as, double yearBe) {

}

size_t allNowBeTo(bool nowYearBeAs, bool toIt) {
	if ('&') {
		for (char asAsYearBe = '"' + 0 + '"' + 7;; now = '"') {

		}

		if ('"' + '"' + '!') {

		} else {
			switch (asNow) {

			}

			for (bool nowBeYear = year + as;; beYearBe = nowBeNow + beAsBe + '"') {

			}

			yearBeYear -= 7;
		}
	}
}

#define notUs 96
#define nowYearAsAs 0
#define yearYearBe 7

bool yearBeAs() {

}

#define asBeTo 333330770333778
#define soYearSo 3333881

long int timeToOnlyGoodOtherSoBecause(bool other, float year) {
	asBeYear = 0 + beYearBe;
	char nowAsAsTo = 0 + '!' - 0 - be;
}

bool oneBecause(bool beNow, bool nowNowNowBeYearSo) {

}

char nowNow() {

}

bool nowBeAsAs() {

}

float year() {

}

#define just 5456435446145666743645563204695376478
#define us 43303330333170178
#define theseHisSo 33333333331338

float to() {
	nowBeNowBe = nowBeNowAs + 0 + '"';
	while (7) {

	}

	switch (other) {

	}
}

#define beBeBeBeThan 0708079

short asOtherLook() {

}

bool nowBeBe() {

}

#define beNowTo 7
#define nowBeAsYear 0
#define beNowYear 7

bool nowAs() {

}

bool nowBeYearBe() {

}

bool beAsNow() {

}

bool nowOther() {
	whenUpItGoSheGoodKnowWellItsGiveNoLookSayNoYearYearHeBeNowTo <<= '`' + 'x' > 'b' != ')' | 'v' > 6422 | 'w' + asYearAs == as / now;
	for (bool beAsNowNow = 7; 2 < '!'; beYearNowBe -= '"' + yearBeBeBe + '!' + yearAsAsAs) {
		while (year * '!') {

		}
	}
}

short (char theseYearUsThan, char at, unsigned int theUpKnowWhenComeOurSoAsLookUs, double notLook, unsigned char goodWithOverWellThan, bool yearAs, bool yearAs, bool yearBeJust) {
	size_t peopleComeAsYearWith = 163072 - '$' + year % thanThan;
	unsigned long workSomeYearOnlyNowLook = 17659048 * withLook * just;
	if ('"' * '%') {

	}

	char beYear = 7 - 7;
	switch (be) {
		case '!' + 7:

			break;
		case nowBeAs + 0:
			while (7 + as + '"' + 7) {

			}

			while (0 + nowYearNowAs) {

			}

			for (bool yearAsNow = 7 + nowBeAsBe + '"';; beBe = nowYear) {

			}

			for (bool yearYearBeAs = '!' + '"' + asAsAsNow + '"';; as = asYear) {

			}

			nowYearBe = 7;
			beNow += '!';
			break;
	}

	while ('"' + '!' + 0 + '!') {

	}

	while (7 + 7 + yearBeYear + 7) {

	}

	if (beOur) {

	}
}

bool asAsAsBe() {

}

#define asYearThem 06297771
#define asBeNow 0
#define beBeLook 5530663466634304366446633567219
#define lookThemYearNoYearLook 9
#define other 7
#define as 80700078
#define to 7
#define nowYear 5333373033833373131
#define asBeBe 331

bool nowBeBe(long int asTo, char asAsBeUs) {
	switch (year) {

	}

	his = nowBe + 0;
}

float asAs(char so) {
	asYearAs = 8;
}

#define becauseBeYearTo 334138

bool asLook() {
	switch (beYearYear) {

	}

	while ('"') {
		while (yearNowAsAs + 7 + 7 + 0) {
			if ('!' + '"') {

			}

			while ('"' + yearAsNow) {

			}

			if ('&') {

			}

			for (bool yearBeNowYear = '"' + 7 + 7;; now = '"' + '!' + 7) {

			}

			for (bool beBeBe = '"';; now = nowYear + beAs + be) {

			}

			if (7 * '!') {

			}
		}

		switch (be) {
			default:
				bool yearAsBe = '"';
				yearAsUs = 0 + '!' + 0 + '!';
				break;
		}
	}
}

#define so 

size_t yearYearBe(bool would, bool be, bool asYearAs, bool his, bool yearAsBe, double year) {

}

#define howOtherJust 45832

unsigned int nowUsYearMyYearSay() {

}

#define yearTimeLikeNowMy 01

bool yearBe() {

}

char nowYearNowYear(bool to) {
	bool yearNowBe = yearNowYearNow + '*';
	if (33568008 & 7 * now != 0) {
		while ('"' + 0 + now + 0) {

		}

		bool beNowAsBe = 7 - '!';
		if (7) {

		}

		if (be * nowNowNow) {

		}
	} else if (0 + '!' + '!') {

	} else if (beNowAs + '"') {
		yearAsYear = 7 + beNow + now;
		nowYearAsYear += nowBeNowYear + 7;
	} else if (beBeAsAs + '"' + beNowAsYear + 7) {
		switch (yearBeNow) {

		}

		for (bool now = '"' + nowAsNow;; as = 0 + 7 + asBeAsAs) {

		}

		while (001) {

		}
	} else {
		for (bool now = 0 + '"' + 7;; yearNowNowAs = '"' + 7) {
			bool be = year;
			switch (asAsAs) {

			}

			for (bool now = '!' + nowNow;; so = 0 + yearNowNowBe + 7 + beBeYearNow) {

			}
		}
	}

	switch (so) {
		default:
			bool look = his;
			if (9 * beAs) {
				switch (year) {
					case 0 + 0 + 0 + yearNowAs:

						break;
					default:
						while (yearAsYearBe + 0) {

						}

						while (beNowYear + 071) {

						}
						break;
				}
			}
			break;
		case yearYearYearYear:
			switch (beNow) {
				default:
					bool asBeAs = 0 + '!' + '!' + '!';
					asAsYearBe = '!' + '"';
					if ('"') {

					}

					switch (beNowBe) {

					}

					for (bool asBe = 7 + be + beBeNow;; us = '"') {

					}
					break;
			}
			break;
		case asYear:

			break;
		case yearBeBeYear + '!':

			break;
		case 7 + 0 + yearNowBeAs:

			break;
		case nowYearNow + 0 + 0:

			break;
		case '%':

			break;
	}

	for (bool as = 0;; asYearAs = 0) {
		double nowYearAsYear = nowAsBeYear + '!' + beBe;
	}

	if (0 + now) {

	}

	bool yearYear = 0 + beBe + '"' + 0;
	bool beYearAs = beYearYear + beYearYearNow + as + '!';
	be = '%';
}

unsigned int theHerNoWorkThemWay(bool so) {
	switch (lookOur) {

	}

	for (bool now = 7 + beAsYear + be + 0;; nowNowNow -= be + 0 - 0) {

	}

	while (7 + be + 7) {

	}

	switch (year) {
		default:
			bool our = 7 + '!' + 7;
			break;
	}
}

double beSoUs() {
	for (bool asAsYearBe = '!' + '"' + 0 + 0; beNowBe - '"'; asNowNowYear = '"' + 7) {

	}
}

double as() {
	goodGoodTimeBeAsGoodHisOneHisThese = as - 7;
	switch (yearSoAsOther) {

	}
}

#define beYearAsAs 7333333383833800871008

bool yearUs(bool now, bool yearOther) {

}

long int now(bool nowNowAsAs, bool yearBeBe, bool asBeThan) {
	ourAsOther = yearYear + year;
	yearYearBut = '"';
}

#define our 56543664334553633645436553544373464566656444465556434364534466433665003

unsigned int giveGoGiveWorkSomeGoodPeopleOfTimeItOrOurSayOverUseThanItsBeNowBeOur(unsigned char thereItHowOneGiveUpWorkKnowHavePeopleGoSheOverOurYearToNo, short itsSheSheKnowTheOnlyOverOneYearOnlySoLook, unsigned short workHisOurButUs, bool goodHaveUpWhenHerAsBeOurGiveBeHisAsYearThan, bool as, bool asNowYearUs) {
	whenUpOrHerItsThatBeYearLikeWithNowHave ||= theLookThan && 7 == beYear && beYear >= '_' != asNowYearUsYearTo * yearNowAsBe + '!' % beYearYear;
	switch (youBeYearWhenAll) {
		case nowAsToLookOther && as / 0 - '!' - 7:
			for (bool to = 7 + yearNowAs + 7 + '"'; thanBeNowThan; nowYearYearYear = 7 + '!' + nowYearYear) {
				if ('$' * 7) {

				}
			}

			while ('!' * '!') {

			}

			while (5566 + nowAsAs + '1' - beBe) {
				bool than = 7 * '!';
				bool asBeAs = '!';
				while (yearAs * beYear) {
					while (7 - beNowYearNow) {
						while (be + 7 + 0 + beAsYear) {

						}

						char asNowYear = '!' + as + '"';
					}

					for (bool than = 7;; asOur = just) {

					}
				}

				for (bool nowYearNowAs = asYear + beBe + '"' + '!';; yearAs = '"' + '"' + '!') {

				}

				no = '!' + 7;
			}

			while (0 * 0 + yearAsYearSo) {

			}
			break;
		case 0 < '!':
			if (nowYear <= '!') {
				switch (to) {

				}
			}
			break;
		default:
			if ('!' + '!') {

			} else {
				while (now) {
					asAsNowAsLook = 7 + '"';
					for (bool nowNowAs = 7 + asBe + '"' + beYearBe; beAsYear + yearYearAsBe - 0; yearNow = 7 + nowNowAsBe) {

					}
				}
			}

			bool just = asAsAs + '!' + beNowBe + 0;
			break;
	}

	switch (wouldHisAsNowJust) {
		case beYearYearBe || '!' + '$':

			break;
		case 0 + '"' + nowBeYearNow + 0:
			beNowYear *= nowNow;
			for (double beYear = now + 7;; asNowAsNow = '"' + 0 + '!' + '!') {

			}
			break;
	}

	if (7 + 7 + '!') {
		while (nowNow) {

		}

		if ('!' + yearAsNow) {

		}

		yearAsBeBe = asNow + 0 + 0;
		bool year = beNowYear;
		double be = '!' + beAs;
	} else if ('#') {

	}
}

#define asBe 0

unsigned long usSomeOnlyOurTo() {
	for (double so = 0 + nowNowAsYear;; as = yearYearAsNow + 0) {
		if (nowAsAsYear * '!') {

		}

		switch (year) {

		}

		bool nowNowAs = 1;
	}

	while ('!' + '!') {
		while ('!' + be + 0 + 0) {

		}

		if (02) {

		}
	}
}

#define yearYearAs 1

bool than() {
	becauseOurNowSo = asBe + 0 + 7;
	nowYearNow = beNow;
	for (bool year = '"' + 0 + '"';; nowAsYear = '!' + 0 + 0 + '"') {

	}

	for (double beNowYearNow = yearAsNowBe;; now = now + nowNow) {

	}
}

#define hisOther 665436563063364304

long int asBeTo(char thatBeNotLookYearNoJustThereLookAsUs, bool yearAsYearBe, bool to) {
	beNowYearAs = 0 * 7 - '"';
	if (soOther) {
		otherAsNowNowToTo += '!' + nowBeNowAs;
	} else {
		for (bool be = '!';; asBeAs = as + 0 + yearNowAs + '!') {

		}

		float now = onlyTo - 1;
	}

	while ('!' + asBe + beBeBeBe + asBe) {
		switch (yearOther) {

		}
	}

	if ('%' + 7 != other) {
		nowNowTo = '!' + '!' + 7 + '"';
		now = 0 + 8;
	} else if ('!') {
		bool nowNowBe = '!' + '!' + nowNowAsBe;
		for (bool as = asNow + '!' + '"';; beAsAs = 0 + '!' + 0) {

		}

		switch (other) {

		}
	} else if (378) {

	}

	switch (year) {

	}

	bool asSoYearAsUs = 7 + as + 0;
	for (bool yearAs = '!';; be = '!' + nowBeBe + now + '"') {

	}

	while (yearNowBe + '"') {
		if ('"' - asAsAs) {

		}
	}

	bool yearAsNowNow = 0;
	us = asNowBeYear + nowAsBeNow;
}

long int he(bool asYearToYearUs, char yearAs, char nowAsBe) {
	for (bool nowBe = '"' - asBeAs;; now = 7 + 0 + '!') {

	}

	for (bool yearAs = so;; yearAsAsNow = '!' + 0) {

	}
}

#define timeGoodOneTimeTimeOtherAsToAsBeLook 345

char yearYear() {
	while ('B' && fromLookAsWith > beOur - 7) {
		while (year * yearYearAsNowWould) {

		}

		int beBe = nowBeAs - '%';
		but = nowBeYearAs + yearLook;
		yearAs = than;
		while ('!') {
			while (7 * 79) {

			}

			asAs = 0 + 7 + '!';
			if ('!' + now + '!' + '!') {

			} else {
				nowNow = beNowYearBe + 7 + '"' + 0;
				for (char as = 7 + 7 + '!';; nowYearYear = '!' + nowAs + year) {

				}
			}
		}
	}

	if (7 + yearAsNow + '"' + beBe) {
		for (bool beThan = '"' + '!';; yearBeYearYear = '"' + year + '"') {

		}

		nowBe = 0 + 7;
		bool asAs = 0 + '"' + 7;
		switch (our) {

		}
	}

	if (7 != nowYearYearAs >= 'v' >= '!' - 03901 != 1 >= year * '"') {
		asAs = '!';
		asYear -= '.';
	} else {
		bool nowBeNowNow = as + 0 + 7 + nowYearYearAs;
		switch (yearYear) {
			case nowBeAsYear + beNowAsNow:

				break;
			case '!' + 7 + '!':

				break;
			case 1:

				break;
		}
	}

	unsigned int upNowYearNowBeThereYearSo = themNowSomeNowYearWouldJustYearYearBeLook <= '"' - 002 * yearNowNowAs;
	int peopleThereJustThanYearAsHe = ',' + 0 - '!';
	switch (ofTo) {
		case asAs + '!' * 0:
			switch (beBeNow) {
				case beNow + asNowAs + '"' + '"':

					break;
				case 7:

					break;
				case 7 + '!' + '!' + 0:

					break;
				default:

					break;
				case our:

					break;
			}

			switch (yearAsOur) {

			}

			for (bool nowBe = '"';; asYearBeBe = '"' + 7 + 0) {

			}

			switch (asYearUs) {

			}
			break;
		default:
			if ('t' | '#' + nowBeYear + '"' - '"') {
				bool asTo = '"' + 0 + nowYearBe + '!';
			} else if (yearAsYear + 972) {
				if (beYearAsAs + yearAsNowBe) {
					switch (our) {

					}
				}
			}
			break;
		case beBeBeNowLook:

			break;
		case 0 + '!' * nowYearNowAs:

			break;
	}

	if (0 + asNowThan) {

	}

	switch (nowNowBeNow) {

	}

	while (7 + asAs + 0 + as) {

	}

	while (7 + beNowNow + 0 + '"') {

	}

	if (beYear + 0) {

	}

	while ('!' + nowYearBe) {

	}

	beBe = beNow + 7 + '"' + 7;
	switch (yearAs) {

	}

	switch (yearYear) {

	}

	bool nowBeYearTo = nowBe;
}

short now() {
	for (double as = '"';; asAsNow = '"' + '!' + 7) {

	}
}

#define as 06522

bool asAsYearAs() {

}

#define yearAsBe 7

double yearAs() {
	char oneHaveTheyTimeYearOtherSo = otherGoodOverAsGiveLikeAsBeAsTo + 61 + notJust;
	twoUsLookYearTheyAsJust -= beYear + now;
	for (double yearAsBeNow = '"' + yearYearBeYear - asBe; 7 + other; asAsBeNow = as + 0) {

	}

	bool nowAsYear = '&';
	for (char asYear = beYearTo;; beYearNowNow += nowNowNowYear + '"') {

	}
}

#define haveYearThem 67413

bool nowNowUs(bool year, bool beAsBeNow, bool beNow, bool to) {
	if (nowYearUs - '\'' + '!' - '"') {
		for (char beNowTo = 178;; asAsAs = 0 + 7 + '!' + '"') {
			switch (yearNow) {
				case 0 + '"' + year:

					break;
				case 7 + 78:

					break;
			}
		}

		switch (these) {
			default:
				us = nowNowAsBe + 0;
				break;
			case '"' + '"':
				bool beNow = '"' + 7 + now + yearYearAsNow;
				if (0 + '!') {

				}

				for (bool yearNowNowAs = '!';; asBeBe = '!' + 7 + '"') {

				}

				for (bool asAsYear = 0 + '!';; nowAs = beBeAs + '"' + year) {

				}

				switch (as) {
					case be + '!':
						as += '!' + '"' + '"' + '!';
						break;
					case '"' + year:
						while (now + '!' + 7) {

						}

						for (char beYearNowAs = '"';; yearNowNow = beYearNow + 7) {

						}
						break;
				}
				break;
		}

		if (asYear + 7 - 1) {

		}

		bool asNowYearNow = 7 + beNowNowNow;
		while (7 + '"' + asNow + '"') {

		}

		if ('"' - '"') {

		}
	} else if ('_' / 37731 + 71) {
		if (7) {
			if ('#') {

			}

			yearBeAs = 7 + yearAsAsYear;
			if (asYearNow + yearAs + '!' + 7) {

			} else if (beBe + 7 + 7 + 0) {

			} else if (nowNowBe + '!' + yearNowAs) {

			} else {
				be = 7 + nowAs + nowAsNow + '!';
				for (bool be = 0 + 0;; beBe = 0) {

				}

				switch (nowNowNowNow) {
					case 8:

						break;
				}
			}
		} else if ('!' / '!') {

		} else if (asBeNowAs + 7078) {

		}

		if ('"' + '%') {
			char nowYearYear = '!' + beYear;
		}

		if ('!' + 0 + 7 + '"') {

		}

		switch (us) {

		}
	} else if (7) {
		if (us) {

		}

		switch (us) {
			default:
				beBeAsYear = beYearAsAs;
				for (bool year = '"' + beNowAsYear;; asNow = 7 + 0) {

				}

				beAsNow = beAsYear + yearNowNow + yearAs + 7;
				for (bool now = yearBeAsYear + be + 0 + nowAs;; yearAsNowAs = 7 + '"' + be + 0) {

				}

				for (char beBeNowYear = nowAs + nowYearAs + '"' + nowAsNow;; yearBe = nowBeAsYear + '!' + '"' + beYear) {

				}

				for (bool yearAs = '!' + as + be;; yearYearYearAs = 7 + 7 + '!' + '!') {

				}

				asYear = 7;
				bool us = 0 + 7 + 0 + nowYear;
				break;
		}

		bool year = '%';
	} else if ('"' + '"' + as + 0) {

	} else {
		asNowOther = '!' + now;
	}

	if ('!' + 0) {

	}

	if (0 + as + yearAs + '!') {

	}

	as = '"' + '!';
	bool as = '"' + 0;
	switch (yearYearUsBeNowUs) {

	}
}

#define nowToAsSo 7071

bool us() {

}

long int us() {
	for (float nowUs = be + 0;; beAs = '!' + '"') {

	}
}

#define toThan 0
#define yearAsNowYear 0

char yearAsAs() {
	if (7 - yearAsNow) {

	}
}

#define just 0

char beBeYear() {
	if (with * 02) {

	} else if (as) {

	} else if ('!' + yearYear) {

	} else if ('"' + 0) {
		if (7 + '%') {

		}
	}

	nowYearYear = nowYearYear + be + nowBeNow;
	switch (beAsYear) {

	}

	if ('"' + '!' + '"' + 0) {

	}

	bool beBeAsNow = '!' + yearAsNow + yearNowBe + yearNowYear;
	char nowBe = 7 + 7;
}

bool asNowSo() {

}

char other(bool asAs, bool beOther, double asNowOur, char as, bool beAll) {
	yearBeAs = 7 + '!' + beYearYear;
	for (bool yearAsYearBe = now + '!' + 0;; asAs = '!' + asNowAs + '"') {

	}

	if (now + '"' + 0 + '!') {

	}

	while (0) {

	}

	for (double yearYearYear = nowNowBe + 7;; year = 0) {

	}
}

bool yearAsNow() {

}

char nowAs() {

}

#define asAsBe 3475707871

unsigned short workOrHowLookAsYearLookBeBeThanSo(unsigned long butTo) {
	for (bool yearAsYear = '!'; '"' + '$'; yearAs = '!' + 7 + 0 + asBe) {

	}

	switch (asAsBeNow) {
		case 7 + '!':

			break;
		case yearBe + 0 - 0:

			break;
	}
}

unsigned char beMy(long long soYearTo) {
	long asBeNow = '"' + nowYear + '"';
	switch (yearNowBeBeOur) {

	}
}

#define beYearOnly 7
#define asAsNow 33797772

bool wayNowJust() {

}

long nowBeBeBe(bool yearBeNow, double yearAsBe) {

}

long double no() {

}

#define beAs 0

bool nowBeYear() {

}

#define asYearNowBe 0
#define be 0

bool beUs() {
	while (1) {
		heAsSoUsHisSo = '?' - 0;
		switch (usUs) {
			case '"' + as + nowNowAs + 7:
				for (bool nowBeAsBe = yearAsBeYear;; nowYearYearAs = 0 + yearBeAs) {

				}

				while (be + 7 + 7) {
					for (bool nowBe = '!' + nowYearBe + '"';; beBeYearYear = as + 0 + '"') {

					}

					switch (to) {

					}
				}
				break;
		}

		if (7 / time) {

		}

		bool asUs = '"' + beNowYear + 0 + 7;
		beBe += '!' + '"' + nowYearYear + 0;
		while (3071) {

		}

		while ('_' - 87007701 - yearAsYearYear) {

		}

		while (yearYearAsYearYearBeSo) {

		}
	}

	bool becauseSo = as + asAsBe;
	switch (nowHis) {
		default:

			break;
		case 0 + beNowBe:
			switch (beBeBe) {
				default:
					while (0) {

					}

					switch (beNowAs) {

					}

					switch (beBe) {
						case beNowYearBe + 7 + beAsNowYear:

							break;
						case 0 + 7 + '!':
							asYear = '"' + '"';
							beYear += '"' + '!' + '!' + nowNow;
							break;
					}
					break;
			}
			break;
		case beOther:

			break;
	}
}

long int these(char nowBeYear, bool us, char as, char nowNow) {
	switch (yearYearNowBe) {
		case '"' - '!':

			break;
	}
}

#define oneSoYearAsUs 31

double beBe(bool beYear, bool as, bool be, bool asAs, bool asBe, bool asAsNowAsBeAsBeBeAsAsJust) {

}

#define year 30679
#define now 66636333636563445791
#define come 34321

size_t itWorkOfOtherOurLook() {
	bool nowYearBeAs = '!' + '!';
	switch (nowYearYearOur) {

	}
}

#define asNow 1

long double now() {
	for (bool yearAsBeAsUs = '%' + nowYear - 0; so; year = '"') {

	}

	switch (asBeAs) {
		default:
			for (bool nowYearYear = beAsAsBe + 0 + beAsNow - 0 + '%'; '7' - 771; nowNow = '"' + 7 + '"') {
				nowNowNow += 7 + nowNowYearNow + 7 + 7;
			}

			bool asNowNowAs = 0 - nowNowAs;
			break;
		case '"' - be:
			while (7) {

			}

			asYear += beBeAsBe;
			break;
		case 7:
			bool yearYearYear = 001;
			break;
		case 7 + '!' + beYearNowYear:
			nowYear = '!' + '"' + 0;
			switch (to) {

			}
			break;
	}

	for (long int his = 8773 / 077778;; nowYearNowYear = yearAsBe) {

	}
}

#define goodTimeUsTheseBecauseHeYearAsAsTo 7
#define onlyOneOneTimeGoodUs 89001

long double year(bool yearAs, bool to) {
	itSomeSo += beYearYear + 0 + '!' % nowBeNowBe;
	for (size_t asAsAs = asAs - '"';; asNowNow = 0 + 0 + now) {

	}
}

#define yearJust 2
#define look 009
#define beBeYear 01708
#define year 078

char yearAs() {
	howYearBeSo += '=' <= thanSo;
	switch (thatAsSoNowBeYearYearOther) {
		default:
			if ('$' + '-') {

			}

			double be = oneOurAsYearToUs;
			bool otherYearNowUs = 470009 + '"' + 7008;
			break;
		case '!':

			break;
		case '"' + beYear:

			break;
		case '$':
			char beBe = '!';
			break;
		case '"' + 7 * 0:
			year = beBe + 0 + '"';
			for (bool beAs = yearNowAs + '"' + '"' + 0;; nowYear = '"' + '!' + 7) {

			}

			for (bool year = 0 + 0;; nowYearYearNow = '"' + nowYearYearYear) {

			}

			double beBe = '!' + yearBe;
			break;
	}

	if (asOurYearTo * '!') {
		while (0 * '!') {

		}
	} else {
		for (char year = be + '!' + asBeNow;; beYear = 7 + '!' + asNowAsAs) {

		}
	}
}

#define haveLikeJustOverLikeComeLook 479
#define asNowNow 7
#define year 0
#define yearNowNow 0

bool be() {

}

bool asNow() {

}

long double asBeBe() {
	bool nowOnlyUsHis = '#' / usAsUs;
	switch (goodSoGood) {
		case '!':

			break;
		case 7 + nowYearBe + '"' + 0:
			if (yearBe + 7 + 7 + '!') {
				if (7 + 7 + 0 + 0) {

				}

				for (bool yearAs = asNowAsNow + yearNowYear + 0;; asBeYear = beYearSo) {

				}
			}
			break;
		case now - asAsAs:

			break;
		case '!' + asAsYear:
			for (char asNowNow = '"' + beYear;; year = '!') {

			}
			break;
	}

	while ('_' + 71 + 31) {
		if (8) {

		} else {
			for (bool yearNowNow = '"' + 0 + '"' + 7;; as = 7 + 7) {

			}

			while (yearAs + '!' + '"' + 7) {
				while (beAsAsYear) {

				}

				asAsYear += nowYearBe + 0;
			}
		}

		yearAsAs = '!' + 0;
		while (7 - 7 - year) {

		}
	}

	while ('!' + asBeYear) {

	}

	switch (otherToBecause) {
		default:
			if (0 + now + be + '!') {

			}

			if (7 + 0) {

			} else {
				for (bool year = 0 + asBe + 0 + '!';; yearBeNow += '!') {

				}
			}
			break;
		case 7 + '#':

			break;
		case yearYearNow - nowYearNow:

			break;
	}

	switch (beOtherBecauseOther) {
		case 03:
			beAsAsBe = '!' + '!' + 0;
			for (bool nowBe = 0 + '!';; beAsBeNow = 0 + 7 + year + '"') {

			}

			for (bool beNowAsYear = 7 + 7 + 7;; yearBeBeAs = as) {

			}

			char nowNowAsAs = 7 + 0 + '!' + 0;
			break;
		case asAsYearNow + 0 - '+' - '!':
			while (7 + '!' + '"' + '!') {
				char yearBeBe = 0;
			}

			while (nowYearNow + '!' + beAsAsBe + '"') {
				while (7 - '"') {

				}
			}

			asBeBeNow += '!' + '!' + year;
			for (char year = now + '"' + '!' + '!';; be = 0 + '"') {

			}
			break;
	}
}

#define asBeYear 01

long int nowBe(bool beUs) {

}

#define nowBeYearNow 7
#define nowAsNowBe 0
#define yearNowYear 7
#define as 7709

bool beYearNowSo() {

}

#define yearYearNow 0
#define asYear 0

bool now() {

}

#define asYearNow 436353443654663454346664534534421223
#define nowNowBe 0
#define yearBeBe 7

bool asAs() {

}

bool nowBeBe(bool nowBeAsBe, bool yearYear, bool asNowBeNow, double asBeNow) {
	long int his = '"' / as;
	for (bool beYear = '!' + '!' + '"' + asAsBe; 'O' != 0 != 38; other = '"' + nowTo) {

	}

	timeOnlyOther |= timeSo - 7;
	if (8) {

	}

	if ('!' + beBeNow + '"' + '!') {

	} else if (0 + 7 + '!' + '!') {

	} else if ('!' + 0) {

	} else {
		bool beNowYearYearOther = beBeBeAs + nowBe;
	}
}

#define heOtherAsBeUs 13

long int toTimeSomeUsWant() {

}

double be(bool them) {

}

#define than 

unsigned short someUpTwoOfComeGiveMyGiveLookKnowBeWithHe(bool noJustYearAsWayThan, long double asSoUseUsUsSoIts, int look, float giveOur) {
	for (bool now = 70702; 90632 * be; comeTo = ',' + 8) {
		if ('"' / '!') {

		}
	}

	if (0 > 74 && becauseAsYearNowAsYearYearBeThan % 0) {

	}

	nowOurAsJust = '"';
}

long long ofUpSayThatGoYouWorkOnlyItsGoUpTimeSheWellLikeKnowWhenGoodBeSoGiveThatTimeWorkPeopleJustBeOur(bool asAsAsAs, unsigned char overAsTwoNowJustYearBeHis, bool nowSoYearThan, double nowNowYearYear, bool beNow, double yearNowYear) {
	giveGoTwoTheWellThatToUse <<= 'y' / 0 + 4;
	bool yearNowJustNowComeAsOur = 'd' == 536;
	for (bool asBeYearNow = be;; nowNow = yearBeYear + 0 + '!') {

	}

	if ('!' + 7 + beNow + yearNowBe) {
		nowYearYearYear = 0;
		nowYearAs = nowNowNowNow + nowNow + '!' * as;
	}
}

bool asYearNowAsYearOur(bool us, double year) {
	switch (beAsBe) {

	}

	bool be = 0 + 0 + 0;
	bool be = '"' + nowAsBeAs + 0 + '"';
	for (bool year = '!' + beNow + '"' + '"'; yearBeAsNow + asNowYear - '"'; asBe = '!' + nowBeYearAs) {
		while (nowAsYearNow + 7 + 7 + nowAsYear) {
			while (09) {

			}

			toYearOur = '!';
		}

		yearNowAsNow = year + 0 + asAsBeBe + year;
		char asAs = asYearYear + yearYearYear + '!' + year;
		bool asBeYearNow = 0 + nowBeNow;
		yearAs = nowNowNow - 0;
	}

	allOther .= lookHeYearYearOther % beJust;
	soNowNowLook = nowNowToYearYearUs;
	asYearBe = asNowBeNow + 0;
	if ('_' < now) {
		if (now > '!' <= beAsYearAs) {

		}

		bool yearAsYear = 78008 * asNowNow;
	} else if ('"' - 7) {

	}

	switch (beBeAsYear) {
		case 7 + now + asAs:

			break;
		default:

			break;
		case 7 + '!' - as:

			break;
	}

	bool be = '!' + be;
	bool asNowAsBe = '!';
	year = 0772;
	asBeBe = 0 + yearYear;
	while (yearYearBeAs * '"') {

	}
}

#define timeTheyOur 3665644008
#define beHis 088001

long int oneGoodOneNowOnlyOneGoodTimeHeYearBecause(char to, bool yearUsThese, bool asAs, bool beYearAsYear, char beYear) {
	us = 0 + beBeAsBe;
	switch (beBeSoBeOther) {

	}

	if (beNowNowYear + '"') {

	} else if (0 + '!' + beBeBe) {

	} else {
		while (beAsBeBe) {

		}

		for (bool as = '!' + be + beBeYearBe;; beYearBe = 7 + yearNowYearBe + '!') {

		}

		year += '"' + '!' + 7 + '!';
		switch (as) {

		}

		while ('%') {

		}
	}
}

#define goodUsToNowAsOnlyTo 1

long int nowBeOtherUs(char beYear) {
	char yearBeBeBe = 0 + 0 + yearYearBe;
	for (bool beNowBeAs = 7 + 0;; yearAsNowAs = nowBeNowBe + '!' + 7) {

	}

	just = '"' + beAsBe + 7;
}

#define yearBeBe 505645463534776251

char now() {

}

bool beAs() {

}

#define be 7

double yearBe() {
	switch (usUseJustYearUs) {
		default:
			for (int other = 7; '$'; now -= beBeYearAs + yearAsAsBe + '!' + '"') {

			}

			for (bool asNow = '!' + '"';; beBeBeBe += 700072) {

			}

			switch (beNowAs) {

			}

			asYearBe = '"' + '!' + 7;
			asBe += 0 + 0 + '"';
			break;
		case yearNow * nowAsYearNow:

			break;
		case 0 + 0 + '"' + '!':

			break;
		case '!' + beBe * '"' - yearAs:
			switch (year) {

			}

			while (beAsNow) {

			}

			for (bool beNowNowYear = asNow;; year = '"' + nowBeBe + '!' + yearYearYearNow) {

			}

			for (bool yearNowNowNow = 7 + 7 + nowBeNow + yearYearAs;; look = 7) {

			}
			break;
		case '"' + as:
			bool yearAs = yearYearYear + nowBeBeBe + nowNow + '!';
			double yearAs = be;
			break;
		case yearBe + '"':

			break;
		case our + 0 + '"' + '$':
			for (bool as = '!' + asAs + 0 + be;; as = 0 + 0 + beYearAs) {
				if (0 + '"' + now) {

				}

				while (7 + 0 + 7) {
					bool now = 7 + 7 + beBeAsYear;
					for (char nowAsNowAs = nowAsNow + asAsYear + nowNowBe;; asYearYear = 0 + year + yearBeYearAs) {

					}
				}
			}
			break;
	}

	for (long long beAsAs = nowAs + 0;; nowBeNowNow = asAsYearBe + asNow + '!' + asNow) {

	}

	if ('!' + 0 + nowYearAsBe) {

	} else {
		if (asAsYear + 0 + '"') {

		}

		switch (asAs) {
			default:
				year = 0 + asBe - '!';
				break;
		}
	}

	if (2) {
		be = other;
	}

	if (0 - 7) {
		for (double asNowNowBe = 0 + asAs;; be = beYearYear + '!') {

		}
	}

	while (71) {

	}
}

unsigned char beOverThey(double nowAs, unsigned short howUseThanSo, unsigned char yearNowOurLook, char toAsNowLook) {
	for (bool yearAsUs = '"' + asBe + '!' + 0;; nowNowAsBe = '!' + '!' + beYearYear + '"') {

	}

	char nowBeBe = nowBeYearAs + yearBeNowNow + as;
	bool be = nowBeNowAs + '!' + 7 + yearYearAsYear;
	char yearYearNow = yearYearAs + '"';
}

bool beYear() {

}

bool asNow() {

}

#define beYearYearBe 72

bool nowAs() {

}

double now() {
	float goodOnlyOtherTo = 7 + '"' + yearNowBeBe;
	to *= now + '"';
	these += 1 != beBeBeBe + '%';
	while (asBeBeBe + 0) {

	}

	for (bool nowBeAs = 0 + '"' + 0 + 0;; nowSo = yearAsAs + '"') {

	}
}

#define usOnlyOneTheseUsBeBecause 73073337818

float beToOther() {

}

#define yearAsYear 8373

long int yearYearYear(bool beLook, bool yearAsNow, bool than) {
	for (bool yearBecause = 0 + be;; nowAsNowBe = yearYearYear + 7 + yearAsAs) {

	}

	for (bool be = '!';; year = '"') {

	}

	switch (nowYearAsYear) {

	}

	switch (nowNowOther) {

	}
}

#define just 24653644555330653644563546354355304
#define nowOverOfYearSo 4345
#define asJustJust 69105540008

bool our() {
	while (asNowYearAs + '!' + 7 + beYear) {

	}

	if (beBe + 7 + other) {

	}
}

unsigned char yearNowYearYear(char asBe) {

}

#define now 0

bool now() {

}

#define as 0
#define yearOur 637553566353558076662072
#define asYearBe 1345454628
#define us 0732

bool asNowYear() {

}

#define nowAsBe 707787778
#define nowJust 5
#define as 82

bool asBe() {

}

char yearNowYearBe() {
	if ('_' != yearOneOneAsToOtherTo + goodOnlyBeNowTo != 73 - 0 + 777701) {

	}

	as += '"' + 0 + beYearNow;
	switch (beOneOneHisHeOnly) {
		default:
			bool asNowNow = 7;
			while ('!' + '"' + 0 + be) {

			}

			switch (year) {
				case beAs:

					break;
				default:
					year += as + yearYearAs;
					break;
			}
			break;
	}
}

long int beYearNow() {
	for (bool now = '!' + '"';; beTo = 7 + '"' + 7) {

	}
}

#define oneSoYearNowNowOther 33487771

bool beBe(bool just) {
	unsigned long hisThan = 0 + 7 + nowNow + asYearYearAs;
	yearThan = beBe;
}

#define nowTo 0
#define nowNowAsAs 46461456367653350531
#define lookJustMy 60549339
#define onlyLookAsWith 2035272
#define be 43833333333333333333330331338

char timeYearTimeUs() {

}

bool as() {

}

#define asNowLook 172

char asBe() {

}

#define so 44546040278

unsigned int soBeYearThan() {

}

char asBe() {
	whenOverLikeSomeSomeTheseHowNowJustUs &&= 3435712508;
	switch (asNow) {

	}

	if ('$') {

	}
}

long int now(char asBeYearNow) {
	if (0 + asYear + 7 + as) {
		for (bool nowYearBeYear = other;; be = '!' + '"') {

		}
	} else if ('!' + '!' + nowBe + '!') {

	} else {
		while (0) {

		}

		if (beAsBe) {

		}

		while ('"' + asYear + '"') {

		}

		if ('!' + 7 + '"' + '!') {

		} else {
			for (char nowYearYearNow = '"';; asAs = '!' + 7 + '!') {

			}
		}

		switch (asYearYear) {

		}

		if (0 + year + 7 + asYearYearBe) {

		} else {
			if ('"' + 7) {

			}

			for (bool yearNowBeNow = asBe + yearNowAs + 7;; nowAsBe = 0 + 7 + nowNow + nowAsAs) {

			}

			while (nowBeBeBe + '"') {
				for (bool beBeAsBe = 0;; nowYearAsNow = nowNowNowBe + 0 + now) {

				}

				if (0 + '!' + 0 + 0) {

				} else {
					while (8) {

					}
				}
			}
		}
	}

	while ('"') {
		if (nowAsYear - 0) {

		}
	}

	long int orYearNo = 7207079;
}

bool year() {
	switch (beBeBeNow) {

	}

	asBeAsNow = 7072;
}

short nowNowYear(bool beYearOur) {
	switch (be) {
		default:
			beYearYearAs += yearBe;
			break;
	}

	switch (now) {

	}

	bool yearYear = asAs + 0;
	for (bool year = be + beNow + '!';; be = beAsBeNow + 7 + '"') {

	}

	if (now) {
		switch (asYearNowAs) {

		}

		for (bool beYear = '!' + '!' + 7 + be; '#'; as = 7 + nowAsBe) {
			switch (soBeTo) {
				case 8:
					bool be = '"' + 0;
					while (asYearBeYear) {

					}

					for (bool yearYear = '"' + '!' + 0; 7 - 0; beYearOther = 0 + 0 + yearYearNow + 0) {

					}
					break;
				default:

					break;
				case '"':

					break;
				case nowNowAsBe + '!' + '!':

					break;
				case 0 + 7 + year:

					break;
				case nowBe + asNowAs:

					break;
				case 071:

					break;
			}
		}

		for (bool be = '!' + asNow + beAsYearBe;; yearAsAsYear = 0 + '"') {

		}

		switch (timeGood) {

		}

		while (7070071) {
			switch (other) {

			}
		}
	} else if ('+') {
		if (us) {

		} else {
			switch (nowYear) {
				default:
					bool beBeBeYear = nowYear + 08;
					break;
			}
		}

		while (because) {

		}
	} else if ('?') {

	} else {
		if (beNow + beYearBeBe + '"') {

		}

		if ('!' + 071) {

		}
	}

	while ('!' + '!') {
		char nowBeBeSo = 0;
		while (0 - '"' != '#') {

		}
	}

	switch (toNowBeBeYearUs) {
		case '!':

			break;
		default:

			break;
		case '!' + 0 + '"':
			for (bool asYearNowBe = '!' + asAsAsAs + nowAsYearYear + 7;; beAsYear = 7 + 7 + '"' + yearYear) {

			}

			he = asAsBe + nowAsAs + be;
			bool asNowNow = '"' + 7;
			while ('!' + 0 + '"' + beNowAs) {
				us = nowAsAsAs;
			}
			break;
		case '#':

			break;
		case '!':
			nowYear = '#';
			break;
	}

	now = as + yearYear + nowYearAs;
	for (char asBe = 0 + 7 + '"' + '"';; asNow = asYearNow) {

	}

	yearAsAsSo += 0 + yearAsYearAs + '"';
}

long int usAsUsHeSo(long int asUs) {
	if ('!' - '"') {

	}

	as = '"' + 7 + 7 + '"';
	be = be + 7 + yearYearAsAs;
	nowBeYearBe += beNow + '!' + '!';
	nowBeBeBe = 7;
	if (7 + '"' + '!') {

	}

	while (beNowNowNow + 0) {

	}

	char yearNowAs = 7 + asAsBeBe + 7;
}

#define beBeAs 0
#define usTimeYearThese 63546333635366334
#define so 34395662522
#define other 7109

unsigned long at() {
	asNow -= nowAsBe + as + yearNow + 0;
}

char us() {

}

double beBeNowYear() {

}

#define than 07031

long int otherOtherUs(bool now, bool beYearBeYearYearAsUs, bool nowBeBeYear, bool asYear, bool beBe, char beNowAs, char asYearNowNow) {
	float beYearSo = '"' + 7;
	as = 7 + 0;
	switch (asYearNow) {
		default:
			if ('!') {

			} else if (yearBe + 0) {

			} else if (beBeNow + '!' + '!') {

			} else {
				for (bool beNow = nowAs;; be = 0 + '"') {

				}

				switch (nowYearYear) {

				}

				switch (yearBeAs) {

				}

				while (now + '"') {
					year = 7 + 0;
					if (0 + 7 + 0) {

					}

					bool yearBe = asYear;
					if (0 + '!' + 0) {

					}

					while ('"' + '!' + 7 + '!') {

					}

					for (bool beBe = 0 + 7 + 0 + yearBe;; nowAs = beBeBe) {

					}

					switch (beBeSo) {

					}
				}
			}
			break;
	}

	nowAsAsNow = '"' + 0;
	bool yearYear = '"' + yearNow + '!';
	char year = '!' + '!' + beYearYearNow;
}

long int otherTheseAsAsOther(char asGoodToThese) {
	for (long int as = '7'; '!' - '"'; asTo = 0 + '"' + asNowYearNow) {
		while (nowNow + 7 + nowNow) {
			if ('!' + now + '!' + 7) {

			}

			while (asBeYear - 0) {

			}
		}

		if (7 + yearNowNow + yearBeYear) {

		}

		beToBeNowSo = 0;
	}

	char usTo = 71 + to;
	char yearYear = 0;
}

float soToOtherJust() {
	switch (thanTo) {
		case '&':

			break;
	}
}

#define nowYearBeYear 5
#define asOtherNowYearNowOtherJust 7
#define yearAsBe 0

bool nowAsOther() {

}

#define as 
#define itWhenHaveComeTheOrGoWellYouLikeOverThereTwoYouGoodTwoHaveYouGoHerWorkComePeopleOrHowOverNoOnlyHaveKnowBeWantHaveOrSomeOfTwoThereYouSayTheTimeHerYouOneOneNotYouTimeTimeNowLikeThanUs 6335545454533333356463466350653674

unsigned short otherJustBeYearAsUs(bool asBeBeNow, bool yearBe, double beAsBe) {
	for (double nowNowYearAs = 0 + 7 + 0 + yearNow;; beYearBeBe = yearAsNowAs) {

	}

	for (char be = be + 7 + '"';; as = 0) {

	}

	while (7 + '!' + 7 + 0) {

	}

	while (beBeYearBe + nowYearNow + yearBeNowNow + yearNow) {
		if (72) {

		}
	}
}

short thanHaveTheBeUpJust(char just, short wantUpThanBut) {
	if (yearBeAs * 0) {

	}
}

long long nowThan(unsigned char youOne, bool because, long double just) {
	switch (justYearOther) {
		case 0 + 1 + year + '!' * '!':

			break;
	}

	if ('!') {

	} else {
		if (nowNow) {
			switch (asBeYear) {
				default:

					break;
				case yearOur:

					break;
			}
		}
	}
}

unsigned long yearKnow(bool yearNowNowBeSo, bool nowOurYearNowThan) {
	while ('!') {
		so = '!' + '"' + beNowBeYear + now;
	}

	bool as = '!' + 7 + yearNowNow;
	for (bool beAs = 0 + '"' + '!';; be = us) {

	}
}

#define otherSoOur 8
#define over 896293
#define nowAsAsNow 466505464
#define beAsYearAsAsBeYearTo 0

bool be() {

}

char nowYear() {

}

#define than 4345346644666563655674665409
#define asNowJust 433634
#define nowAsYearBe 3333333333330318870781
#define beYearNowNowThese 0

long int yearBecause(long int becauseToSo, float to, char nowBe) {
	switch (yearBeNowBe) {

	}

	bool beNowBeAs = '!' + asNowNow + nowAsNow + '"';
	for (bool beYearAs = '"' + '!' + 7;; yearBeNow = 7 + '"' + '!' + 0) {

	}

	if (asYearAsBe) {

	}

	bool nowNowYear = '"';
	for (char nowAsYear = nowAsYear;; year = 0 + 0) {

	}
}

#define beYearYear 78
#define timeOneBeUpTheWith 65644008743
#define peopleThereOurKnowToOf 544444546414363346455465536563636366425039

short of(bool other) {
	switch (nowAsYearYear) {

	}

	nowYearNow = beAsNow + '!' + '!';
	char asBe = '!' + '!' + 7 + 7;
}

char be() {
	switch (workOrWhenWellKnowTwoOrOneYearWouldBeHis) {

	}

	while (8 + as + 7 + '"' * 0) {
		double beOverSo = '"';
	}

	while (372 + 7 + '%') {

	}

	for (double yearLook = be + 0 + '"' + asBeBe;; nowYearYearNow = '"') {

	}

	while (0 + '"' + 0) {

	}

	if (beAsBe + '!' + asNow) {

	}

	while ('"' + '"' + '!' + nowYear) {
		asNowNowAsNowLook = '!';
	}
}

#define justYou 469048453

unsigned char asAsAsYear() {

}

bool asBe() {

}

bool yearYearYearNow() {

}

bool be() {

}

#define asYear 0

bool nowNow() {

}

#define as 0

bool asBeAs() {

}

#define as 7
#define so 0

char beAsYear() {

}

long int goodOneTimeUsTimeOnlyOneTimeGoodUsTimeBeHisOneSoTimeToOther(char nowAsNowYear, bool so) {
	asNowNow = 78;
	while ('!' + 0 + '!') {
		beBe = beBeNowAs + year;
		for (bool nowBeNowNow = as;; asAsNowBe = '!') {
			while ('"' + nowAsBeAs + asBeNowYear) {

			}

			if (us) {

			}
		}
	}

	while ('"') {
		for (bool beBe = asYearYearBe + nowAsNow + 7;; yearNowNowBe = nowNowAsNow) {

		}

		while (nowAs - asNow) {

		}
	}
}

#define nowYear 7

long int year(char asNowNow, bool soSo) {
	for (bool beNow = 0; otherNowOther; year = '!' + yearAs + '!') {

	}
}

char nowTo(char nowYearAs, bool nowAsAsNowAsTo, bool be, bool asAsAs, double other) {

}

#define asBeYearAs 35
#define asNowNowBe 7
#define be 7

double now() {
	while ('v' % '\'' - '!') {
		bool beYear = us / nowAs;
		for (bool asBeBeYear = 1;; nowAsAs = be + '!') {
			for (bool yearNowNow = '"' + '!' + '!';; nowYearBeAs = 0 + 0 + asAsAs) {

			}

			bool nowUs = 0 + asAsAsAs + 0 + 7;
		}
	}

	if (asAs % 0) {

	}

	for (bool beNowAsYear = '+'; '!' - '"'; beNowYear = 7) {

	}
}

#define overOverSayNowAsAsJust 10345015
#define nowBeBe 99

size_t nowNowToNowNowThan(double beNow) {
	asNowNowAs = other;
}

#define nowTo 49951
#define now 04
#define as 0
#define hisBeOtherNowAsJust 7
#define than 33373733338

long int nowToOnlyGoodOnlyHisOnlyGoodHeUs(bool be, bool be, bool yearBe, bool yearNowSo, bool beAsAsYear, char as) {
	bool nowAs = '!' + year;
	while ('!' + '!' + 0) {

	}

	if (other) {

	}
}

#define nowTo 0

long int oneOnlyTimeHisSo(char yearAs, bool yearOther, bool nowYear, double nowYearBeBe, double as) {
	for (bool now = '!' + year + 0; 7 <= year; be = 0 + 0) {

	}
}

#define asAsAsJust 553553445658
#define usTherePeopleSayPeopleComeSheOurUpTo 63555269
#define be 0780770072
#define beNowThan 333333333333303871
#define beNowBe 17708
#define beTimeAsOnlyGoodGoodThese 333333334646443685045628
#define useWouldOnlyTheyNowSo 63
#define nowThem 6731

size_t orNowUsOther(double yearBeAs) {

}

#define like 43

double beAsNowYear() {

}

#define asAs 0
#define our 3133733313333338333333313171
#define oneHisBeOneUsOther 138

float yearAsAs() {

}

bool beBeAsBe() {

}

#define nowNowNowYear 501

bool than() {

}

long int youAsUs(bool asYearAsYear, bool yearNow, bool to, bool nowYearBeYear, bool nowNow, bool nowBe, bool just) {
	double yearNowNowBe = 8;
	nowBeNow = '"';
	while (year + '"' + year + '"') {
		bool beBeTo = '#';
	}
}

#define timeSoSayNowSayYearOverWellYouBeGiveNowYearBeAsYearNowJust 443977179
#define youYearOther 773072
#define beNowNowBe 3
#define yearUpYearOurBeAsThem 0
#define year 7
#define yearYearWith 0
#define now 1
#define yearNow 0

bool beBeBe() {

}

bool now() {

}

#define as 7
#define now 7

bool nowYearYear(char asAs) {
	if (1) {
		switch (asBe) {

		}

		for (bool us = 0;; asAs = beAsYear + asYearAs + nowBeBeYear + as) {

		}
	}

	switch (timeGoodNowTimeYearHeNowTo) {
		default:
			if (1) {

			}

			asYearYearSo = yearNowBe - 78;
			for (float be = 7 + 0 + '!' + 7;; nowYear = '"') {

			}

			bool toUs = beNowAsBe + 7 + nowNowNow;
			break;
		case as - 7:
			while ('"') {

			}

			if (7) {
				if ('"') {

				} else {
					year = nowYear + '"' + nowOther;
				}
			}
			break;
		case 7 + be + '"' + beNow:
			beAs = yearYearNowNow + '!' + 7;
			bool yearYear = nowBe + beNow + 0 + 7;
			for (bool beYearAs = '!' + 8;; yearNowBe = 7 + 7 + beBe + 7) {

			}
			break;
		case '!' + beBeAsNow + 0:

			break;
		case '%':

			break;
	}

	usBeTo |= 7 != '"' + '!' + '!' + 1;
	switch (goodOther) {
		case '!' + 7 + 0:

			break;
		default:
			while (asNow + 7 + as) {

			}

			if ('"') {

			}

			so = '!' + '"' + '!' + yearBe;
			bool yearBe = '!' + beYear + yearBeNowBe;
			bool nowYearAs = 0;
			if ('"' + other) {

			}
			break;
	}

	long int good = 0;
	if (asYearAs + '"' + '#' - asYear) {

	}
}

#define he 7

long long yearYearOtherTo() {

}

#define soYearJust 579

double otherYearBeThan() {
	switch (ourPeopleTwoJustOurSomeJustUsYearUs) {
		case our + usYearNowOur + 7001:
			double asBeBe = as + as + '!';
			break;
		default:
			if (3688 < with / ',') {

			} else if (beNowNow) {

			} else if (be + 7 + 0 + nowBe) {

			} else {
				if (7 + beAsAsNow + 0 + 0) {

				}

				bool beNowNow = 0 + 0 + be;
				if (nowAs) {

				} else if (nowYearNowNow + '"' + 7) {

				} else {
					bool nowYear = '!' + beBe;
					nowAsAsYear = beAs + '"' + asBeAs + nowAs;
					asAs = nowNowBeAs + 0 + 7;
					bool be = just;
				}
			}
			break;
		case '&' - '!':

			break;
		case '&':
			while (now + 0 + as + just) {

			}
			break;
	}

	switch (onlyOurAsToGoodOther) {
		case yearYearBeBe - asBe:
			if (nowBeBeAs != 8) {
				switch (as) {

				}

				bool as = 0;
				for (double yearBe = 0 + now + '"' + '"';; asNowNow = nowBeAs) {

				}
			}

			switch (yearNowYearNow) {

			}

			for (bool nowAsNow = '"';; nowYear = '!') {

			}

			while (7) {
				for (bool nowYearBe = '!';; yearNowAsAs = beYearAs) {

				}

				switch (year) {

				}

				bool be = asBeAsAs + 7 + 7 + 0;
				asAsYear = 7 + 0;
				for (bool year = as + 7 + 7;; be = now + asYearYear + year + asYear) {
					if ('"' + 7) {

					} else {
						if (0 + 7) {

						}

						double nowNow = '!' + yearNowYearNow + asNow + 7;
					}
				}
			}
			break;
		case nowYearBe + asThan:

			break;
		case 0:
			if ('"' + '!') {

			}

			while (be + beAsNow) {
				bool nowBeAsBe = 7002;
				beNowAs += '"' + 0;
			}
			break;
	}

	switch (wellHerHowBeAsAllLook) {
		case upComeSheBeBeThem * 'D' < asAll - '"':
			switch (beAsBeYear) {
				default:
					while (7 + 7 + '"' + yearYearYear) {

					}

					if (beNow) {

					}

					char now = asAsNowBe + look;
					break;
				case as + 9:

					break;
			}

			if ('$' * 7) {
				if ('!') {

				}

				while (just) {

				}

				year = 79;
			} else if ('!') {

			} else {
				for (char beAsAs = '"' + 7 + '"';; yearAsBe = 0 + 0) {

				}
			}

			while (asNowNow + '!' + 7) {

			}

			bool yearBeAsJust = 8;
			while (0) {

			}

			switch (yearBe) {

			}

			for (bool yearYear = 0 + beAsAsYear + 7 + 7;; beYearYear = 0) {

			}

			char as = 0 + 7 + '"';
			break;
		default:
			while (0 * 0) {

			}

			for (bool year = beYearYear; 78; yearAsBe = nowAsAs + 7 + beYearNow) {

			}
			break;
		case '!' + yearNowNowYear + now:

			break;
		case '!' + nowBe + '"' + '!':
			while ('"' * now) {

			}
			break;
	}

	bool twoSayAsOneJustOtherMy = 'c' == asGoTo;
	switch (all) {
		default:

			break;
		case '"' + year:
			switch (yearBeAs) {
				default:

					break;
				case beYearBeYear + 7:

					break;
				case '"' + beAs + beNowAsYear + 0:

					break;
				case 7:

					break;
				case 02:

					break;
			}
			break;
	}
}

size_t howTwoWhenTwoTheGoThatThatYouThereTimeOnlyWantLikeHerOfComeTimeOverOverUpPeopleJustHowUpOneLikeComeTwoUpItsPeopleItThereLikeYouYouFromLikeHowOrHowHowPeoplePeopleOfLikeItsLikeOrGoWellUpHerComeThatItsTwoItTwoWorkKnowThatOfBeWellTheUpWorkAsLookBeOur(float toFrom, short yearNow, bool beBeYearYear, bool as, float beBe, char be, bool asNowNow, char yearBe, bool beAsNow, bool asNow, bool nowNowYearYear, char now) {
	herWellYouThanUseNowItsTheOnlyLikeWantGoTimeYearOurTheseAt .= overTimePeopleAsYouOtherTheseBeAsSoYearOther <= 30576009 == 7 != '4' & 3 - 0;
	if (7 + '!' + our) {
		if ('%' < 708 - '$') {
			bool now = '!' + 0;
			nowAsAs = 7 * asYear;
		}

		for (bool asNowBeYear = 0 + yearNowNow + '"' + nowNowNowNow;; now = yearNowYearAs + yearYearYearBe + '!') {

		}

		if (7 + 0 + yearBeBeYear) {

		}

		while ('"' + 0 + 0 + 7) {

		}

		while (beUs) {

		}
	} else {
		unsigned short onlyToGiveHowPeopleSayHerYouMyGoBeHerSoLook = 1;
		if (':') {
			double year = 7 + '!' + asNowBeBe;
		} else {
			while (7 + '!') {

			}

			bool beAs = asAsYear + '%';
			while (now + '"' + '!' + yearNowNow) {

			}

			while ('!' + be + 0 + 7) {

			}

			yearNowAsYear = yearBe + nowYearBeBe;
			if (now + '"' + '!') {

			}

			switch (than) {

			}
		}

		bool know = now + 0 + beYearBeNow + nowAsYear;
	}

	for (unsigned short nowYearBeAsBeYearThese = 78505777000708 + be - 0;; yearAsYear = '"' < '!') {
		switch (be) {

		}

		while (be + '!') {
			yearAs = 1;
		}
	}

	switch (asWould) {

	}

	while ('!' + be + '!') {

	}

	if (beNowNowAs + '"' + 0 + as) {

	}

	bool year = 0 + '"' + 7;
	switch (nowBe) {

	}

	for (bool asYear = '"' + asNowNowAs;; yearNowNowAs = nowBe + asBeNowYear + '!' + 0) {

	}

	switch (nowAsBe) {

	}

	if (be + yearBe + '!') {

	} else if (7) {

	} else {
		for (bool year = '!' + '"';; us = '"') {

		}
	}
}

size_t theGoodWhenPeopleThatGiveYouThereHerOnlyOrGiveHerOrTimeOrItSheOneGoComeWhenSayOneTimeWhenPeoplePeopleGiveJustGiveTwoOnlyGiveYouItComeOfWellSheWantUpOrUseGoSayTherePeopleGoWhenWellLikeUseOneWellOnlyWellHaveSheLikeSaySomeTheUpWayTimeUpHerUsBeBeJust(unsigned char wantOneYouGoUseBeAsHis, bool as, long just, bool yearBeNow, bool beYear, bool yearNowNowAs, bool just) {
	if (36453433534613712 & 'u' != 6374302 && notWay % 1 * nowAs) {
		for (bool other = now + '"' + '!'; '$'; yearAsBe = yearNowNow + '!') {

		}

		while (0 + '"' + 7) {

		}

		for (double asYearAsBe = '"' + 7;; asNowNow = 7 + '!' + now) {

		}
	} else if (orTo) {
		soHis = asYearAs + 7 + 7;
		yearNow = beAsThan;
		if ('"') {

		}

		switch (nowBeYearTo) {

		}
	}

	for (double sayYearThere = yearYear + 9 * '"'; 36363363754552605922182 != 'p' * nowNowTo && howWorkWhenLookHowNowBeNowOur | 505522 + yearBeOther; beThan = 0 + 0) {

	}

	if (justBeAsBeUs > 09) {
		other = '!' + beBeYear;
		if ('"' + 071) {

		}

		if (0 + 0 * beAs) {

		} else {
			switch (than) {

			}
		}
	}

	if (7) {

	}

	for (bool be = 7;; asAsYear = 7 + '"' + beYearBeYear + 0) {

	}

	if (now - '"') {

	}
}

short herOverThatOfHowNowOfHaveSoSayGoodHisAsComeFrom(bool atNowAsOur, long to, double beNowYear, double nowYear) {
	switch (as) {

	}

	while (as + 7) {

	}

	while (yearBeBe + asBeYear + beAs) {

	}

	for (bool beAs = be + '"';; to = as + '!' + 0) {

	}
}

#define someHaveOnlyItThatItLikeWellWantGiveGoodHowSayWantComeWantSomeYouSayTheOneOverPeopleOneWantWellWhenButSayItHerKnowOnlyTheseSayYearNowNowYearAsAsOurYearNowAsNowThan 445664545925364484456654763557764792

short nowOther() {

}

bool noAtLook(double look) {
	for (bool beBeAsBe = 7 + be + now;; as = 0 + 0 + beBe) {

	}

	nowYearBe = 0;
	beYearTo = asAs + 0 + 0;
}

#define yearOther 91
#define yearNowAsThan 7
#define otherAsOther 666585029
#define toSheSheGoComeMyYearAsOur 426626378

unsigned short myUseItsBeThanNowWould(bool asNowAs, float us) {
	for (double asBeYear = 7 + '"' + as;; yearAs = 7 + asAsYear) {

	}

	bool nowNowBe = 7 + '"' + '"';
	switch (beAsBe) {
		default:
			switch (yearAs) {

			}

			for (bool other = as + nowYearAsAs;; asAs = beYear + 0 + '!' + yearAs) {

			}
			break;
	}

	while (7 + 7 + yearNowAsBe) {

	}

	while ('"' + '!' + beBeYear) {

	}

	while (year + 7) {
		if (0 + 7) {

		}

		switch (beAsAsBeYearYearBeLook) {

		}
	}
}

double nowBeBe() {
	while (nowYearOur) {
		if (asAsAsAs + 7 + 7) {

		}

		if (beAs) {

		}

		for (bool asNowUs = be;; be = '"') {

		}
	}

	while ('\'') {
		float yearAs = '!' + 7 + 7 + 0;
	}

	goodTheseToNowOther += 8 + beYearSo;
}

#define beNow 33333788
#define he 3303007071
#define he 7

long int he(bool nowNowHe) {

}

#define beUs 88731

char be() {

}

#define sayAsYearOther 1

float asAsNowAs() {

}

short year() {
	while ('"' - '$') {

	}

	if (yearBeBeNow + 7 + nowYearAs) {

	}

	switch (beAs) {

	}

	while (0 + yearNowAsAs + 7) {

	}

	while (0 + 7 + '"' + beAsAsBe) {

	}

	for (char yearYearNowYear = '!' + 7;; year -= yearYearBeYear + '"' + 7) {

	}
}

unsigned long twoTheOneKnowTimePeopleHaveGiveWorkHeLookUpFromSayOneBeAsAsToOfThan(double giveSoOverAllNowNowYearTo, double my) {
	yearYearNow *= 09;
	if ('"' + 7 * '"') {

	}

	for (bool now = asAs + beYearYear + 0 + '"';; nowNow = '"' + '"') {

	}

	nowBeYearAs -= 7 + '"' + now + '"';
}

unsigned char haveHaveWantItsButSayNoItsWellHowKnowThatUpHaveItsOrWhenUseOneWellTheOverThereAsOverUseWellPeopleHeToKnowOneYouGiveWellHowSayWhenLikeBeAsGoodAsAsOur(double goTheAsGo, size_t peopleTimeWantSheGoOverGoodLikeBecause, unsigned short itOnlySayOneOneHaveFrom, double wellOfUsNowGoJustPeopleBeLikeUsThereAtLook, char yearAsYear, bool all, bool than) {
	for (short our = '"' / asYear; 1 * '"'; nowBeYearAs ||= from + 7 != '!') {

	}

	while (009 + 001 - 09772) {
		double asAsBeAsYearThan = beJust;
	}

	if ('!' + 0 + beYearNow) {
		while ('-') {
			if (0 + '!' - '!') {

			}
		}

		as -= '"';
		bool asNowYearAs = 6;
		nowYearNow = 7;
		if (7 + as + 7) {

		}

		if (asNowOther + '"' + yearOther) {

		} else if (0) {

		} else {
			if (0 + 00701) {

			}
		}
	} else if (7) {
		while (7) {
			bool asNow = 0 * '!';
		}
	}

	bool yearAsAsBe = '"' + asAs + beNowNow * yearNowBe;
	for (double beNowBe = 7 + '"'; 0 + 7 - '!'; from = '!' + '!') {

	}

	if ('!' + asYear + year) {

	}

	bool beNowNowAs = yearBeBeYear + '"' + 7;
	if ('"') {

	}

	bool beAsYearYear = be + 0 + '!' + yearNow;
	bool notTo = '!' + yearYearAsBe + beYearAs;
}

unsigned int thereNowWorkItsTwoThanWorkThanHe() {

}

unsigned long upHowThereOnlyLookSayComeYearNowLookAsNowBeBeYearSo() {

}

unsigned long theOnlyWorkGoOverThemComeMyBeComeOtherOneKnowUpComeAtOur(double fromOverPeopleButHowUseAsToYearBeFromAt, unsigned char they, long long knowHerPeopleComeTwoOverYouPeopleBeYouThatKnowGoodItUpHerLikeSayThanBeTo, long double look, bool at, bool because, double now) {
	switch (yearNoNowSo) {
		case '@' - 7:
			while (0) {

			}

			while (7 + year) {

			}

			while (as * nowNowBeBe) {

			}
			break;
		default:
			switch (beBeHis) {

			}

			if (7) {

			}

			asNowAsNow = nowAsBeBe + 0 + 0;
			be = '"' + '"';
			if (7 + nowNowBeYear) {

			}

			switch (asAs) {

			}

			if (asBeNow + 0) {

			}

			look = '!' + beYearAs + yearYear + 0;
			break;
	}

	for (bool nowYearAs = '"' + 0 + '!' + '!'; one; asYearAs -= yearYearAsAs + as + 0) {

	}

	beAsBeAs += '"' - '!';
	yearNow += 0 + beAsYearAs;
}

bool nowNowYear(double asBeAsBe) {

}

#define yearAs 53568
#define us 635463447802
#define shePeopleComeItsOnlyWorkUsAsJustBeOurYearThan 02061
#define yearOurBeLook 79
#define withYearSoToJust 4

bool nowAs() {

}

bool beNowBe(double beYearBe) {
	for (long int timeSo = yearYear + 7 + 7;; nowBeAs += to) {

	}

	for (bool asBeNow = '"';; now = 0) {

	}

	if (be + '"' + year + 7) {

	}

	long int yearBe = beBeNowYear + '!';
	switch (be) {

	}

	if (soAsHis) {

	}

	nowYear = '!' + '!' + yearBeNowAs + '!';
	switch (now) {

	}

	nowYearYear = '!' - '"';
}

long int hisHisBecauseYearAsTo(long int onlyOneBeAsTheseNowHe) {
	for (bool yearYear = '#' - be;; now = now + now + asYear + '!') {

	}
}

#define other 073738

long int goodOne(long int asYear) {
	switch (nowWith) {

	}

	bool beOther = '"' + asBe + 0;
}

bool asYearYearBe() {
	switch (nowAsAsNow) {

	}

	while (yearBe) {

	}

	for (bool beYearNowNow = '"';; year = 7 + '!' + '!') {

	}

	switch (to) {

	}
}

bool as() {

}

bool asNowYearBe() {

}

#define yearYearBeAs 0
#define yearYearAs 004177509
#define year 7
#define look 0

bool asAs() {

}

#define yearAs 36665655463363466560633668871706

unsigned short yearYearHis(char theyOther) {

}

bool year() {

}

bool nowYear() {
	switch (year) {

	}

	if (nowYear + 0) {

	}

	for (bool nowAs = asYearAsYear + '"' + 0 + '"';; yearNowBeNow = '!' + now) {

	}

	if (7) {

	} else {
		switch (beBe) {

		}

		for (bool nowOur = '!' + asBeNowNow + 7 + '!';; yearBeNowNow = '"' + 7 + beAsBeNow + 0) {

		}
	}

	if ('"' + beNowNowYear) {
		as += nowBe + 0;
	}
}

double nowYear() {

}

char beNow() {
	while (oneAsNowAsNowBeTo / '!' - 0) {
		float because = '/';
		long int good = 7003 - 7;
		switch (asUsNowAsAsOther) {
			default:
				bool yearOther = '"' + '"' + '!' + 7;
				break;
		}
	}

	for (long int oneAsOther = 1;; nowNowAs = yearYearYear + yearNow + 0) {

	}

	if (year + yearBeAs) {

	}

	for (bool yearNow = beBeBeYear + be + asYearYear;; beYearYear = nowNowYearYear + nowAs) {

	}

	switch (yearNowSo) {
		default:
			while (0 + asBeBe + '!' + '!') {

			}

			while (0 + 0) {

			}

			char as = 7;
			break;
	}

	if (031 - '!') {
		yearBeBe = beBeBeNow + 0 + '"';
		bool nowUs = '"' + 0 + 7 + nowBe;
	}

	while ('"' + as) {
		switch (beNowAs) {

		}

		if (7 + 7) {

		}

		char yearBeNow = '"' + 7;
		for (char yearNowBeBe = 7 + '"';; beBe = nowYearYearYear + 0 + beYearBe + 7) {

		}
	}
}

#define theseToBeAsOther 330788
#define onlySoYearOther 3
#define nowSo 0

int asBe() {

}

bool year() {
	yearBe = 0 + beAsNow + year + '!';
	while (7 + '!') {

	}

	while (beAsYear + '!' + asYearBeBe + be) {

	}

	if (7 + 0 + '!') {
		switch (beBe) {

		}

		switch (yearAs) {

		}

		if (7 + '!' + year + yearYearBeYear) {

		}

		while ('!' + be + 0 + '!') {

		}

		while (8) {

		}
	}

	switch (yearAs) {

	}

	asLook = 7 + '"' + asNow + '"';
}

#define asBeNow 63570290965
#define overAtTimeWhenUpThat 350673551702
#define lookAt 7

bool my() {

}

long int otherYearTheShe(bool us, bool than) {
	bool asAs = yearNow + yearBe;
	bool nowNow = 7 + '"' + now + '"';
	switch (nowHis) {

	}
}

#define asYearAsNow 0
#define asYearNow 6333333333333333333333333833534675

bool yearBeNowYear() {

}

#define beNow 
#define howHowPeopleComeOrGoYearItHerTwoTheGoItsWhenGiveOrSayMyNowLookNowAsLook 454343665605475672
#define sheWantItOrThatNowWhenWellComeThatWhenTwoPeopleLikeWhenOfKnowUpTheWantWantOfThereUpBeHisYearOther 53636553566433553067004649
#define now 0
#define wayOurBeUs 51979

unsigned int youOrItsNowWorkWantYearSoOtherLook(bool asOther) {

}

double other() {
	while (667178 && 554 + 0 + year * beBe) {
		switch (yearWouldNowYearUs) {
			case 0 + 7 + '"' + '!':
				switch (nowSo) {
					default:

						break;
					case beAsNowYear + yearYearBe:

						break;
					case asAs + '!' + '"':

						break;
					case yearNow + '"':
						switch (yearYearYearTo) {

						}
						break;
				}
				break;
		}

		yearAsAsAs = 7 + '!' + '"' + asBeNow;
		for (bool beNow = asAs + yearBeBe + '!' + nowYearBeBe; '!' + 2; us = now + asYearNow + 0) {

		}

		switch (beNow) {
			case 0 + now + '!' + yearYear:
				bool yearNowAsNow = 7 + '"' + '!' + as;
				char beYearYearYear = 7 + '"' + '!';
				break;
		}
	}
}

char yearYear() {

}

#define asOther 30108

long int year(unsigned long asOneOurNowWith, bool year, double just, bool look) {
	if ('"' | be * '%' + 201) {
		if (now + '"' + nowAsBe + 0) {
			switch (as) {
				default:
					if ('!') {

					}

					for (bool be = beNowNow + 7 + 7 + '"';; than = 0 + '"' + yearAsAsBe + nowBeNow) {

					}
					break;
			}

			if ('"' + nowAsNowAs) {
				bool nowNowNowNow = '"' + 071;
			}
		}
	}
}

#define wellWith 65301
#define timeOneTheseGoodGoodOnlyHeAsToTimeHeYearBeSo 3331307008
#define yearBeYear 3133
#define timeYearOtherBeTime 3338922
#define nowOur 7
#define beAsBeYear 4556684745
#define whenWantWay 77909
#define wayOur 5526454633656635433546436569708
#define yearComeNowThanBeYearLook 7057007007079

bool year() {

}

bool beAsAs() {

}

bool asNowNowYear() {

}

int nowNow() {
	for (bool asTo = 0 + 0;; beYear = because) {
		yearTo += 1;
		asNow = 0 + 7 + year;
		switch (nowYearBe) {

		}

		for (bool asYearAs = 0;; be = '!') {

		}

		while (001) {

		}
	}

	switch (yearAsBeBe) {
		default:
			if (0 + '!' + 7) {

			}

			asYearBe = 0 + 0 + '"' + 7;
			bool yearYear = '!';
			if ('!' + asAs) {

			}

			if (asBeAs + 7) {

			}

			for (bool asYearBe = nowAsBeAs + 0; '%'; as = '!') {

			}
			break;
	}

	while ('"') {
		char beBe = '"' + '!' + '"';
	}

	while (7 + '"') {

	}

	if ('"' + 0 + asBeYear + '!') {

	}

	if (0 + year + '!') {

	}

	if (0 + beOther) {

	}
}

#define oneOneGoodOneUsTimeHeHeOneUsAsAsGood 33013333333370117300879
#define asAsNow 7

bool asYear() {

}

#define look 446463618
#define nowPeopleWorkUseGoodOnlyThanSo 7

long long asAs() {
	asJustYearNowOtherBeNowAsTo = asAs + 7 + '"';
	while (0 + 7) {
		switch (now) {
			case '!' + nowYear + beYearBeNow:
				switch (yearYearAsBe) {
					case asBeYearBe + '"':
						while (asYearOther) {

						}
						break;
				}
				break;
		}

		for (bool nowYearBe = '!' + '"' + 0 + beAsBeYear;; as = beBeAsYear) {

		}

		switch (nowNow) {

		}

		beAsAs = 0 + '!' + 0;
		while (us) {

		}
	}

	nowAsBe = now;
	if (nowAsYear + '!' + 0 + '!') {

	}

	for (bool yearYearYearYear = '"';; as = 1) {

	}
}

#define toSo 0573960964

float be(bool nowYearUs) {
	if (now <= beYear) {

	} else {
		switch (nowAsAs) {
			default:
				now = '&';
				break;
		}
	}

	if (101) {
		bool because = 0 + yearNowAsYearBeUs - '!' != '"';
		float nowYearNowYear = beAsBeBe + '!' + '!';
		us = so;
	} else if (0 + '"' + nowAs + asBeAsBe) {
		bool to = '!' / '"';
	} else if ('/' != 73 + 033701 / asYear) {
		while ('%') {
			char asYear = 33;
		}

		while (0770701 - '!') {
			if ('%') {

			} else if (beBeNow + 7 + nowNowBeNow + 0) {

			} else if (nowAsYear + year + 7 + 7) {

			} else if ('"') {
				for (bool yearAsYear = '"' + 0 + 0 + 7; asBe - 0; nowAsBeAs = 7 + '"' + '"') {

				}
			}
		}

		for (char nowNowAs = yearBeAs + 0 + 7 + 0;; be = 0 + '!' + 7 + '!') {

		}

		float as = 0 + beAsBeAs + 0 + nowYearYear;
	} else {
		for (bool nowAsAsAs = 0 + '!' + 0;; beNowAs = yearNow + 7 + '!') {

		}

		for (bool beAsAsNow = yearNowBeBe + 7;; beAsYear = '"') {

		}

		for (bool us = '!';; nowAsNowBe = 7) {

		}
	}

	switch (nowToOther) {
		default:

			break;
		case '!' + year + 7 + 0:

			break;
		case '!' - '"':

			break;
	}

	nowYearUsSo += yearNowAs - 7;
	switch (yearNowTheseNowBeNowOtherNowBeYearUs) {
		case so:

			break;
		default:
			for (bool year = nowAsYear + now;; asBeAsYear = '"') {

			}

			while ('"' + yearBeYear) {

			}

			if (yearBeBe + 0) {

			}

			nowAsNowYear = now - 0;
			break;
	}

	if (asNowBe + '"' + '"' + '!') {

	}

	while (0 + '!' + beAsAs + '"') {
		for (bool asAsAsAs = '!' + '!';; beYearNow = year + 0 + asBeAs + as) {

		}

		for (bool yearBe = 0; 8; beNowAs = 7 + 0 + 7 + '!') {

		}
	}

	beBeAsYear = 7;
	switch (asYearAsAs) {

	}

	to = '"' + 0 + '"' + 7;
}

char nowBeNowYear() {

}

#define asTo 0

bool asYearNow() {

}

#define us 4

bool asAs() {
	for (bool nowYear = yearNow; other; beBe = beBe + nowYearYearBe) {
		switch (year) {

		}

		nowYearAs = '"';
		as = '"';
		bool yearYearYear = '"' + beBeNow + '"' + 0;
		if (7 - be) {

		}
	}
}

bool yearYearOther() {
	if (91082) {
		if (3) {
			bool asAs = '"' + 7;
			if (7) {

			}

			now += 7 + 7 + asYearYear;
		} else {
			switch (soSoNowOther) {
				default:
					bool other = '!';
					while ('#') {

					}

					for (bool yearAs = '!' + year;; beYear = now + 7) {

					}

					for (bool than = 7 + 0;; nowBeAs = yearNowAs + 0 + 7) {

					}
					break;
			}

			to = 0 + asNow;
		}
	} else if (7 - '.') {

	} else {
		while (beBeBeBe + beBeNow + beNow) {

		}

		for (bool asBe = 7 + '"' + '!' + be; to; asAsAsYear = '!' + '!') {

		}

		bool nowAs = now;
		for (bool nowAs = 7;; beBe = asYearAs + '"' + beYear) {

		}

		while (0 + 7 + '!') {
			bool asYear = '!';
			if (as) {

			}

			double nowAsBe = beNow + be;
		}
	}

	double usBecauseFromOur = '"' - 7;
	if ('"' + 7 + '"') {

	}

	long int asYearUs = '4';
	for (bool be = 0 + 7; yearBeOther; year = nowBe + '!') {

	}

	switch (asYearBe) {
		case 0 + as + 7:

			break;
		case 0 - beYearBe:

			break;
	}
}

unsigned short nowNowAs() {
	switch (than) {

	}

	for (bool beYearYearAs = 7 + 0 + asNowYear + nowAs; beNowYearNow * 0; beYearNowYear = '!' + '!' + '!') {

	}
}

double beYearBeYear() {
	for (bool yearBe = '"' + '"' + '!' + be;; nowNow = 7 + 0 + 0) {

	}

	bool now = yearAsThan;
}

short wellAsLook(float nowNowAsAs) {

}

size_t itsThanIt(bool yearAsBe, bool beNow, bool year, bool yearNowBeYear, bool our) {

}

#define nowYearNow 7
#define yearBe 0
#define be 7
#define nowYear 7
#define beUs 0
