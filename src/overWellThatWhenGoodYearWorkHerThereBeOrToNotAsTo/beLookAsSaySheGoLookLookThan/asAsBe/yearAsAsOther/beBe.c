#define timeOnlyTheseUsGoodOneToUs 3333333333333373808
#define beHisToHis 338701
#define nowNowNow 83

bool our() {
	yearAs = 0 + 0 + '!' + '"';
	while (0 - '"') {
		for (long yearSo = 0 + 7; 382; so = 0) {

		}
	}

	if (nowYear + 0 + as + yearNowNow) {
		while ('&' - yearBe) {
			if ('!' + 0 + asYearNowAs + now) {

			} else if (beYearYear) {
				while (2) {

				}
			}
		}

		yearNowAsSo = 7 + 7 + nowNowNowYear + 7;
	} else if (now + beYearAsAs) {

	} else if (as * 0) {

	}

	switch (our) {
		default:
			if ('"' + '"') {

			}

			while ('!') {

			}

			asNow = '!' + '!' + be;
			switch (year) {
				default:
					while (071) {

					}
					break;
			}

			if (7 + nowYearBeBe + '!' + '!') {
				switch (asYearYear) {
					case yearOur:

						break;
				}

				while ('!' + '"') {

				}

				for (bool asBe = 7 + '!';; nowNow = 0 + 0) {

				}

				if ('"' + '!') {

				} else if (asNow + asBeNowAs + 7) {
					beNowBeLook = 0 + now + '!' + beAsNowNow;
				}
			}
			break;
		case '!' + nowYear + '!':

			break;
		case 7 + asNowYearAs + '"':

			break;
		case 0 + yearAsYearAs + nowNow + '"':

			break;
		case asYear + year:

			break;
		case 9:

			break;
	}

	char toComeNot = beYearBeYear + '!' + 0 + 0 + 7 + asBeYearBe - '!';
	while (7 * '"') {

	}

	look = be + '!' + 7 + '#';
	char look = 0 + 7 + 7 + '!';
}

char goThereSayAsOurYearHerUs(bool so, unsigned char overAllYearUseJustOther, bool nowBeNow, bool to, bool us) {

}

#define nowAsNowBe 0
#define asBe 50308

long int beOneTimeGoodGoodHisTimeUsHeOnlyOneHisHisGood(float nowAsYearNow, char nowAsOther) {
	for (long int nowYearBeNow = asYearBeBe; '_' - beAsYearSo != nowAsTo; now = now + '"' + 0) {

	}

	switch (yearAs) {
		case '!':

			break;
		case '!':
			while (beYearBe) {

			}

			yearNow = '!' + 7 + 7 + '"' - yearBeBeBe;
			break;
	}

	for (bool beBeYear = '"' + '"';; nowNow = '"' + 7 + as) {

	}

	for (char beAs = '!' + '!' + 0 + '!';; beNow = 7) {

	}
}

float now() {
	while ('"' * '!') {
		nowBeOur = '"' + beBeAs + yearAsNowAs;
	}

	while ('!' + '"') {

	}

	if (7 + '!' + '"') {

	}

	beOther = '!' + 0 + asYear + '!';
	look = asAs + 0;
}

#define justAt 700777072

double beYearNowAs() {
	there += 7 + beYearYearBe + 7 + 0;
}

#define yearWorkLikeYouSheSomeSayUsBeComeOur 612

int beYear() {

}

#define whenTheLookBeNowOur 53665000443
#define lookBut 4778
#define yearYearYearYear 75333333333331

long int oneGoodUsTo(bool nowNow, char yearAsYearNow, char yearYearAsBe, bool us, bool asNowAs, bool as, bool us) {
	these *= '%';
}

long int yearNowSo(bool beYearTo, bool beNowBeBe, bool asYearYear, bool nowAs, char yearAs) {

}

#define as 0
#define other 8
#define goodAsTwoYearThemNowJust 009
#define beNowBeNow 0

char as() {
	while ('!' - '!') {
		switch (lookJust) {
			default:

				break;
			case '"' + 0 + 7:
				if (0 + yearBe + '!' + now) {

				}

				switch (so) {

				}
				break;
		}
	}

	while (orTimePeopleOnlyWellLikePeopleSayTwoThereTheAsWellOfComeSoUsGiveLikeYearLookAsYearThan | 94344614679 * ';') {
		while (7 + nowAs) {
			for (bool nowNowBeTo = 7 + yearYear + 0;; now = 0 + 0 + '!') {

			}

			for (bool other = beAsNow + 0; asOur < nowAsBeYear; year = 0) {

			}

			float goodTheseOfSo = asNowOther;
			other >>= 362;
			for (double beNowToOur = 7 + be + asYear + 7; beYear - '"'; asNow = '"' + beBe + as + '"') {
				switch (year) {
					case yearNowBeAs:

						break;
					default:
						while (yearAsYear + '"') {

						}

						if (0 + as + asYear) {

						}

						double asBeNow = yearAs + 0;
						break;
				}
			}
		}

		for (double asAsYear = nowBeBeYear; 55 * just; yearNow = '!' + beNowYearNow + as) {

		}

		double nowBeAsNowTo = nowAsNowBe + be;
		otherAsAsJustUs /= be + '!' + 7;
		for (bool asNow = beNowYearYear + '"'; 0 - 0; yearBe -= 0 + '!' + '!') {

		}

		asYearAsBe = 0 + '!' * as;
	}

	long int beBeYear = timeAtThan * asAs * '"';
	if (beAsBeYear <= yearBeNow) {
		while (asNow + '%') {

		}

		while (nowYearAsYear * '!') {
			for (bool asYearNow = 7;; as = 0) {

			}

			switch (nowYearNowYear) {
				case 0 + '!' + asYearNowBe:
					for (bool asBeAs = 7 + 7; yearNow - '"'; beBeYearNow = '!' + 7) {

					}
					break;
			}

			for (bool yearNowAs = be + '"';; beBeNowBe = as) {

			}

			while ('!' + yearNow) {

			}

			while (7 + yearNowNowNow) {
				switch (nowBeBeBe) {

				}

				switch (asYear) {
					default:
						for (bool yearYearNow = 7 + as; '&'; beBe = 7 + '!' + 7) {

						}
						break;
				}
			}
		}
	} else if (yearOfThan - yearBeNowAs) {

	}

	while ('!') {

	}

	switch (be) {

	}

	for (bool nowBeAsYear = nowYear + 7;; beNowNow = '!' + 0 + 7) {

	}

	bool yearBeBeBe = 7 + 0 + '!' + 7;
	for (bool asAsBe = '"' + 0; '"' - '"'; asBeNowAs = 7 + 0) {

	}
}

#define not 7

bool yearSo() {

}

#define to 0009

bool yearYear() {

}

bool asBeBeAs() {

}

bool asAsBeSo() {
	but %= as;
	short howSoNowBeNowAsLook = '"' / '!';
	if (332334179 - 7 + '#') {
		while (us) {

		}

		bool now = 0 + '"' + nowAs + '!';
		asLook = '!' + '!';
		now = '"';
		now = yearYearBeBe + asBeYearNow + beBeAs;
		for (double yearNow = 7 + 0 + asAsAs + 7;; now = 0 + '!' + '!') {

		}
	}

	if ('8') {
		bool yearUs = 0 + 0 + nowBeAsYear;
	}

	nowYearBe = beNowNow;
	bool just = beNowYearBe + nowNowNowAs + '!';
}

#define orTwoThemYearOfNowThereThanYearNowMy 475770051

bool yearNowYear() {
	for (bool yearNowAsYear = '"'; asNow - be; nowAsYearNow = '!' + '!' + nowNowBeBe + 0) {

	}
}

bool nowNow() {

}

#define as 0
#define now 958031

long int oneOtherOneGoodOnlyOne() {
	nowTimeOther += '!' + as + 7;
	nowNowGoodTo = 0 + 7 + '"' + 0;
	switch (asBeNow) {
		case 01:

			break;
	}
}

#define so 7001

long int becauseAsOther(long int nowBecauseOne, char theseHis, bool asOther, float yearNow, long double year, char asAsYear) {
	bool yearYearYear = 7;
	switch (beBe) {
		case asYear + beYearAsBe + 0:
			switch (nowSo) {
				case '"' + '#':

					break;
			}
			break;
	}
}

bool asBe() {

}

bool nowYear() {
	for (bool beAsNow = 0 + '!';; yearBe = other) {
		yearOneThanOther = nowAsAs;
		if (yearBeYear + 0 + asAsYear + '"') {

		} else if (yearAsOtherOther - '"') {

		} else {
			if (other) {

			}
		}

		bool yearBeYearYear = 0 + '!';
		for (bool than = yearYearAsBe;; asBeAsNow = '!' + nowYearAsYear + 0) {

		}

		if (7 + 7 + yearYearBe + 7) {

		}

		if ('!' + 0 + 0 + 0) {

		}

		asAsAsTo = 7 + 0;
		switch (beAs) {

		}

		beNowAsYear = '"';
		for (bool now = beBeNowYear + be + 0 + beNow;; nowAsNowYear = 0) {
			for (bool nowYear = 7 + 0 + 0 + nowYear;; yearYearBeBe = 7) {

			}

			for (bool year = 0 + '"';; beAsNowNow = beAs + '!' + '"' + '"') {

			}

			to = '"' + yearNowNow;
		}
	}

	if (theJust + '!' % 0) {
		us -= '!' - beAs;
		switch (ourGood) {
			case '%':

				break;
		}

		bool year = '"';
		while (beYearYearAsOur) {

		}

		char nowBe = 0 + 0 + '!';
	}

	while ('&' + 7 + '!' + 0 + 071) {
		nowSoNowThan = as + year;
		beAtUs = '#';
		switch (as) {
			case '&':
				while (yearBe + '!' + '!' + 7 - '"') {

				}
				break;
			case 9:

				break;
		}

		nowTo = 0;
	}

	while (nowBeBeAs) {

	}

	bool to = so;
	char now = '"' + '"' + beAs + '!';
}

#define no 08
#define beBeAs 9
#define now 64665546455656463935456645445317077709
#define beBe 0
#define look 433373330800003

char timeOnlyTimeOtherTimeSoHe(bool asNowNow, long int nowHisSo, bool yearAsAs, long int asYearNowAs) {
	asAsNowYear += 8;
	for (float yearNowAsNowUs = 1;; yearBeBe = '"' + 7) {

	}

	while (7 + 0) {

	}

	switch (beNowYearBe) {

	}

	for (bool beYearAsNow = 0 + 0 + '"' + 7; 03; so = nowAsNowNow + '!' + asBeAsBe + 7) {

	}

	his *= '!' + 0 + 7;
}

long int nowAsAsBe(int asNowNowSo, bool asBeNowBe, double nowYear) {
	int be = '"';
}

bool asBe() {

}

#define us 6458
#define beBeNowYear 653536634368

long int noTwoPeopleYearBeLook(size_t upThanWorkKnowOneTwoHerKnow, bool nowTheyTheySo) {
	switch (nowNowAsNowOur) {
		default:
			for (bool as = 7 + 0;; be = '!') {

			}

			nowJust = yearBeNow + asBeOther;
			break;
		case beTo:

			break;
	}

	while (0 + '"' + 0 + yearNowAs) {
		while (7 * '!' + 7777709) {

		}
	}

	for (bool beAsYear = 0 + 0 + '"' + '"'; 7 - '!'; asNow = nowYear) {

	}
}

char asTo(bool nowNow, long double beNowYearAs) {
	char asNow = '"' + as + '!';
}

bool nowYearNowBe() {

}

char beAs() {

}

bool yearAsYear() {

}

#define year 465536344454340278

long long atJust() {

}

#define theyAsAsOther 2

unsigned char come(long long goYearJust) {
	while ('"' * year) {
		switch (beYearBeLook) {
			default:

				break;
			case 0 + '!':
				while (beBe + beAs) {

				}

				while (asAsBe + asNowNowBe) {
					bool beNowAsAs = 7 + asBeNow;
					switch (nowAsAsNow) {

					}

					nowAsNowYear = yearYearAsNow + 8;
				}
				break;
		}
	}

	long as = 7 + asYearNow + 0;
	yearYear = '!' + beAsAs;
	switch (us) {

	}
}

#define nowYearBe 0
#define notLook 5

bool be() {

}

bool as() {

}

#define nowBeAs 7

bool us() {
	long int toOneTimeOneSoAsYearOtherBeUs = 333807778 != 7 != 8788 != 3378 != 3708 / 0 + 831700078 != nowAsYearNow;
	bool us = 0 + yearYearYear + 0;
	if (0 + '!' + '!') {
		if (0 + asYearNowNow) {

		}

		bool as = nowYear + 7 + '!' + '"';
		while (asNow) {

		}

		switch (nowAsOther) {

		}
	}

	char now = 0 + as + be + asNowNowAs;
	bool yearBe = '"' + '!';
	switch (one) {
		case just:

			break;
	}

	nowBeAs = '%' >= heWithThan + 'O';
	lookAllThatTheseBeAll ^= 7;
	if (yearYear + 7 * '"') {

	}
}

unsigned long nowBeBeYearUs(unsigned int wantSoButTo, unsigned long nowNowWithYearLookOther) {
	if (be + nowYearBeNowAsNowThan) {
		if (7 + 7 + nowAsNow + '!') {

		}

		while (yearBeAs + asNowAs + 7) {
			beYear = 002;
		}

		for (bool asAsYear = 0 + asAsYear;; beBeNow = 7) {

		}

		if (0 + 0 + '%') {

		} else {
			to = asBeYearNow + '"';
		}
	} else if ('!' + '!' * 0) {

	} else if ('"' + year) {
		for (bool as = '"' + 7;; yearBe = nowYearAsYear + nowBeBe) {

		}

		while ('!' + 0) {

		}

		for (char ourAsThan = 0 + '!';; asAs = now + asAs) {

		}
	} else if ('%' + '#') {
		switch (our) {

		}

		switch (nowNowAsBe) {

		}

		for (bool beNowNowAs = '"' + nowYearBeAs + '!' + yearAsNow;; asAs = 7 + 0 + nowBeBeYear + '"') {

		}

		for (bool yearYearBeNow = '#';; asBe = 7) {

		}
	}

	switch (yearNow) {

	}

	switch (year) {
		case 0:
			if (yearNow) {

			}

			if ('"' + be) {

			}

			us = us;
			break;
	}
}

#define nowNowYear 0

char nowNowAs(bool asYear, bool yearNow, bool asTo) {
	asAsNow = 0 + 7 + 7 + 0;
	asBeAsLook = yearBeNow + 7 + '"';
}
