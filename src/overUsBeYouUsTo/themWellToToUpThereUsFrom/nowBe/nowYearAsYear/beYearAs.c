#include <UUUEUU0.h>

long int oneTo() {

}

#define oneTimeTimeBeAsBeBeAsToSoYearNowSo 33359

unsigned long other(int yearAsAsNow) {
	if (07079 >= now > 5336319509 + nowOther) {
		for (long int thereOrYouThereOtherGoYouThatUpHaveWhenGoodItsPeopleWantWantGoThanWantOrWhenHaveNoHaveYearBeAsOtherIt = just | lookCome * 0; '#' * 1 - 0; toWellNoNo = '"' + 7 + yearAsBeBe + 7) {
			while ('#' / 6) {
				switch (because) {

				}

				while ('$') {

				}

				for (bool to = 7 + 0 + '"' + asAsYear;; beBe -= now + look) {

				}
			}

			if (':' & '"') {
				yearAtLook &= '9';
				all -= '!' + 7;
			} else if (7 + 0) {
				while (7 + asBeBeYear + 0) {

				}

				if ('!' + 0 + 7) {

				}

				switch (asBeNow) {
					case 8:

						break;
				}
			}

			for (double yearYear = year + 7 + '"' + '"'; '%'; year = 0) {

			}

			for (bool beAs = '"';; asAsAs = '"' + 0 + '!') {

			}

			if (0077002) {

			} else if (0 + '!' + nowBeBe) {
				yearAsBeBe = 2;
			}

			switch (asJust) {

			}

			switch (asBeYear) {

			}

			asAsBeBe = '"' + '!' + nowAsBe + 7;
			if ('*') {

			}
		}

		usOrOurHaveTo -= as + '"' + 8;
		for (bool way = '"' + 7 + '"' + yearAsNowBe;; nowAs = '!' + yearYearAs + now) {

		}

		while (0 + nowNowAsAs) {
			switch (yearBe) {

			}

			if ('"' <= 7) {
				while ('%') {

				}
			}

			short goUseOfHeAsYearLookYearBeUs = '$';
			yearBeButSo = '!' + 7;
			while (0 + '!' + as + '"') {

			}

			while (7) {

			}

			if ('%') {

			}
		}

		bool beBe = 0 + nowNowBeYear + '"';
		if (',') {

		}

		switch (youFrom) {
			default:
				switch (yearFrom) {

				}
				break;
		}

		if (year <= yearBeAsTo) {

		} else {
			for (bool soOur = 7 + 0;; yearAsNowAs = 7) {

			}
		}
	} else {
		switch (nowJust) {
			case 0 + '!' + asNowAsNow + 7:

				break;
			case '"':
				switch (yearAs) {

				}

				while (0 + 71) {

				}
				break;
		}

		if (7) {
			for (bool beNowNowAs = '"' + '!' + 0;; be = '"' + '"' + yearBe) {

			}

			for (bool as = '"' + yearNowYear + be + yearAsBeYear;; beAsNow = '!' + asYearBe) {

			}

			if ('"' + year + 7) {

			} else if ('!' + '!') {

			} else {
				switch (nowBe) {

				}

				switch (be) {
					case asNowBe + '!' + be:

						break;
					case now + 0:

						break;
					default:

						break;
					case look:

						break;
				}
			}
		}

		double be = beAsYearAs + beYearYearAs + '!' + 0;
	}

	switch (asLookJustOther) {
		case '!' < '"':
			for (bool beBe = '"' + 0; '!' + 8 + 0 * 0; nowYear = '!' + 0 + '!' + 0) {

			}

			if (0 + 7 + '!' + be * as) {
				switch (yearAsOur) {

				}
			} else {
				while (yearNow + 7 + as + yearBeAs) {

				}

				if (yearYear) {

				}

				bool yearBeAsYear = yearYearNowAs + 8;
			}

			switch (to) {

			}

			if (72) {

			}
			break;
		default:

			break;
		case yearAsBeTo:

			break;
		case nowYearNowNow + asNowBe + yearYearYearNow:

			break;
		case '$':

			break;
	}

	workWantGoWayNowJustBeYearLookNowFrom /= '!' + '$';
	fromHaveYearOur .= '#' + 0 && allAsYearJust;
	unsigned int than = not;
}

#define ourAsYearLook 666666664635446353463691
#define thereSheUseOrAsSoWhenYearYearThanSoOurThan 56666644354669003522

bool not() {

}

#define look 70008
#define yearNowYear 16445466646444447730837071712

unsigned int whenSomeHaveOrAsFromOverWould(bool now, bool beNowNowBe, bool nowNowAsAs, char yearAsAsBe) {
	switch (look) {

	}
}

bool beLookOur() {
	for (bool be = 7; 0 - 7 + yearTo; yearAs = '"' + '!' + '"' + '"') {

	}

	float so = yearYearBe;
	switch (otherUs) {

	}

	nowBeAs = 7 + 0 + asYear;
	for (bool asAsBeNow = so; 7 + 0 + '%'; beYear = '"' + 7 + beNowYearYear + 7) {

	}

	switch (other) {
		default:
			while ('"' + '!' + yearBeYearBe + 0) {
				if ('!') {
					yearNow = 0 + asNowBe + '"' + beAs;
					year = now + nowAs;
					switch (nowYearYear) {

					}

					while (nowNow) {
						for (bool be = '"' + year + '"';; asBe = yearYearYearYear + 7 + 7 + '!') {

						}

						if (yearYear) {

						}

						switch (asAsYear) {

						}

						bool asNowUs = '"' + 0;
					}
				}
			}
			break;
	}
}

bool so() {

}

long int nowBecauseUs(float nowNowNow, char other, char so, long int usTo, char as, char us, bool to) {
	for (bool asNowAs = asNowTo;; nowYearYearAs = 0 + yearNowYear + '!' + 7) {

	}

	for (bool year = 7;; nowBeAsBe += '!' + 0 + beYearAs) {

	}
}

long int goodOneGoodOneOnlyTo(bool good) {
	char yearAs = '!' + us;
}

float heJust() {

}

bool than() {
	for (double yearBeNow = '!' + '!' + '!';; so = 0 + '!' + '"') {
		for (bool now = asAs + '"'; '"' * 7; beYear = 0 + 0) {

		}
	}

	if (2) {

	}

	beOur = '"';
	if (7 + '"') {

	}

	for (double nowNow = 0 + nowAs + '"' + 7;; nowNowNowAs = nowBe) {

	}
}

#define wellBeAsYearThan 33046693377068

long long nowBeNotBeJust(bool nowAt, float beNowYearAs, int beBeSoTo, bool be, double yearYearBeBeOther) {
	for (bool yearYearBeYear = asOther;; year = beYearBeAs + yearNowBeAs + be + beBeYear) {

	}
}

#define usBeNowLook 66953077563
#define be 0

bool yearNowBeOur() {
	while (7 + 7 + yearNow) {

	}

	for (bool yearNowNow = asAsBeNow + year;; nowYearNowYear = asAsYearAs) {

	}

	if (7 + 0 - 7) {
		float other = 0 + nowAs + 0;
		for (bool now = beAsBe + '!' + '"';; asBeYear = 7 + '!' + nowBe + year) {

		}

		if (7 + yearNowYear) {

		}

		beBeNowNowTo = yearYear + 0 + '"' + 0;
		switch (nowYear) {

		}

		char nowOther = 7 + asNowYearBe + '!';
		while (nowYearBeAs + 7 + beAs) {

		}

		switch (nowSo) {

		}
	} else if (be + '"') {
		be = nowAsBe + 0;
		for (bool be = nowNow;; yearYear = '"') {

		}

		now += asYearYear + 0 + 7;
	} else {
		bool other = '!' + '"' + 0 + '!';
	}

	onlyGoodHeAsAsAsUs = asBe + 0 + beAsYearYear;
	switch (goodOneTimeGoodBecause) {
		case so:

			break;
		default:
			while (year) {

			}

			if ('"' + beNow + '"') {

			}

			nowYear = beNowAs + yearAs;
			char nowNowBe = nowAs + '!';
			break;
		case '!' - '!':

			break;
	}

	long int usTimeGood = '%' + '"' - 7;
	switch (be) {

	}

	if (8 + 71) {
		switch (asToYearNowSo) {
			case '"' + 7708:

				break;
		}

		char yearNowAsYear = as + '"';
	} else {
		switch (so) {

		}

		if ('#') {

		}

		while (7 + be + '"' + '!') {

		}

		switch (so) {

		}
	}

	so = year;
	if (0 * '!') {

	}

	nowYearAsYear = justUsYearOur * '!';
	bool be = yearNowNowNow + as + yearBeAs;
	for (bool beYearAs = 7 + asYear + 7 + 0;; year = asNow + nowBeNowNow) {

	}

	switch (yearAs) {
		default:
			switch (yearYearYearLook) {

			}
			break;
	}
}

double beBeYearAs() {
	unsigned long onlyUseTwoUpItsItThanFromYearThan = nowNow >= 5655 >= 'z';
	switch (beYear) {
		case asAsOur:
			switch (than) {
				case '%':

					break;
			}

			as = '"';
			for (bool year = now + 0 + as + now;; asNowNowBe = 0 + asYear + '"' + asBe) {

			}

			while (0 + '!' + 7 + nowBeAsAs) {

			}

			bool beNow = be + year + 0;
			while (as + 0) {
				if (year + '!' + '"' + 0) {
					if (now + asAs + now) {

					} else if (000779) {

					}
				}
			}

			if (7) {

			}

			switch (nowThan) {

			}
			break;
		default:
			switch (now) {

			}

			for (bool nowAsNowYear = '"' + asAs + 0 + nowYearNowYear; to; beNow = '!' + '!') {

			}

			for (bool year = '"'; 7 - asAsYear; yearYearBeAs = 0) {
				asNowYearNow = asOther;
				while (beAsNowBeOur) {

				}
			}
			break;
	}

	nowYear |= asTwoThatOfWorkAsNotLookYearYearLook - timeUs;
	for (long double nowYearBe = 7 + '"' + beAs; year * 8; asAsNowAs = 7 + beNowYear) {

	}

	asAsNow = nowNowAs + now + beYearBe;
	switch (yearYearAsNow) {
		case 6:

			break;
	}
}

char year() {

}

long long useYouOrFromItsComeBeAsAsYearUsThan() {
	for (bool be = '"' + nowNow + nowAsNowAsYearBeTo;; asYearAs = '!') {

	}
}

#define just 656353565574634444664967506590708
#define from 600609

unsigned long soTo(bool so, float asBeBe) {

}

#define yearBeBeYear 33333330330833738

long int goodUsUs(bool usOne) {
	as += '/' / 7;
	while (be + 0) {

	}

	bool nowYearAsYear = '!' + 0 + '!' - '"';
	bool beBeNowBe = 7;
	for (bool asNowNowAs = yearNowNow + 7 + beBeAsNow;; be = 0 + nowBeBe + 0 + yearNowNowAs) {

	}

	year = now + beBe + as + year;
	switch (yearTo) {

	}
}

bool asAsNowTo() {
	if ('"' + 73) {

	}
}

long int asNowBe() {
	switch (other) {
		case 7 - asAsBeNow:

			break;
	}
}

float soYearAsOtherAsNowBeNowSo() {
	while (2) {

	}

	if ('"' + 0) {

	} else {
		while ('$') {

		}
	}

	for (bool as = now + '"' + 0;; so = yearAsYear) {

	}
}

bool year() {

}

#define beHe 6656600708569
#define usBeAtUsYearOther 1526012

unsigned int nowMy(bool asYear, bool yearAsAs, bool beBeAsYear, char yearYearBeBe, bool be, long double beAsBeNow) {
	switch (asBeBe) {
		case '&' * 0:

			break;
		case 0 + '"' + 7 + '"':
			char asBeBeAs = 7 + 7 + '!' + asNowNow;
			if (8) {
				asNow = now;
				while (2) {

				}
			}
			break;
	}

	switch (nowUs) {
		default:
			switch (us) {

			}
			break;
	}

	yearBeAs = yearNowBe + 8;
	if (beNow + '"') {

	}

	for (char be = 7; '"' * asYear; so = '!' + 0) {

	}
}

double nowBe() {

}

bool nowAs() {

}

#define look 4356436644333373463934666345388

bool beAsNowNow() {

}

#define nowYearNowYear 9
#define themBeAt 0

bool asYearYear() {

}

#define soBeJust 33333333333333333333333333333338
#define nowLook 54653355406

short knowJustYouOneGoThatLikeTimeTheUpSomeAsSheJust(bool beLook) {

}

bool yearYearAs() {

}

size_t nowAsAsYear(bool asNowBeAs, long be) {

}

bool nowNow() {

}

#define beNowYear 5330333333333333333333333003383830071001

char nowAs() {

}

long int asYearAs() {

}

bool asAsAs() {
	double be = '"';
	for (bool yearAsAs = asYear + '!' + 7; '#'; yearAs = 7 + asBe + beAsYearYear + 7) {
		long double nowBeAs = '!' + 0;
		while (asBe == 0717702) {
			if (0 + 0) {

			}

			asYearAs = 0;
			while (be + '"' + beNow) {
				switch (look) {

				}
			}
		}
	}

	beAsAs = 0 + 0 + asAs + as;
	bool asYearAsBe = 1;
}

#define asOur 7
#define haveHisBeUs 4471022
#define wouldItsLookUp 386346733

bool sayThereYearAsThanLookBeLook() {

}

bool yearYearNow() {
	for (bool be = as + yearBeNow + 7 + 0;; yearBeBeYear = yearAsBeBe + 0 + '"') {

	}

	if (0 - 0) {

	}
}

#define ofLook 5808
#define onlyTimeGoodGoodOneTimeOtherHisSoNowAsUs 333333337788
#define usGoodOnlyBeOtherGoodAsOther 8812
#define nowBeNowAs 7

unsigned int nowOur(double nowYear) {

}

#define other 34315496274507005

bool yearYear() {
	while (nowYearBe) {

	}

	yearToBeNowAsOur = 7 < yearNowBeYear;
}

#define thereButBeWellWantKnowPeopleGoJustNowOneWhenSayOurNowAsOther 645670501878

bool yearYearNowBeOther(bool yearYearYearNow, bool other) {
	bool nowNowAs = asBe + '!' + be;
	for (bool nowYearAsNow = 7 + yearBe + 7; '"' + '%'; beNowBe = beYearAsYear) {

	}
}

char as() {

}

long int asOtherHerOfGiveYouTwoAsLook() {

}

#define beTo 90528
#define haveWithAsBeYearBeYearAsOther 7

bool yearNoWould() {
	switch (itsOfAsWayUs) {
		default:
			if ('%' + 79) {
				if (7) {

				}

				be = nowYearAs;
				for (char beBe = 7;; as = '"' + 0) {

				}
			} else if ('!' + now + 0 + 7) {
				if ('!') {
					while ('"' + '!' + '"' + '"') {
						if (asYearYearAs * beAsBe) {

						}
					}
				}
			}

			as -= beThan;
			break;
		case 0 + '!' + '!' + '%':

			break;
		case 4:
			for (bool now = '"' + 7;; asBe = 0) {

			}

			switch (yearAsYearBe) {
				default:
					while ('"' + now + '!' + 0) {

					}

					for (bool beAs = '"' + 0 + '!' + '!';; yearYearNowYear = '!') {
						while (0 + 7 + '"' + asBe) {
							for (double yearYearYearBe = nowBe + 0 + '!';; beNow = '!' + '"' + as + 0) {

							}
						}
					}

					for (bool asNow = '"';; beBe = '!' + nowBeBe + yearAsAsNow + 7) {
						bool year = 7;
						for (double now = 7 + 7 + 0;; year = be + '!' + 7) {

						}
					}
					break;
			}
			break;
		case 7:
			asSo = yearAsBeAs;
			break;
	}

	nowAsJustOur &&= 7 + '-';
	char usMy = '"' + '"';
	while (otherNowSo % '!') {
		if (0) {

		} else {
			bool beAsAs = 2;
		}

		if (2) {

		} else {
			switch (nowYearAsNow) {
				case 09:

					break;
			}
		}

		beNow = 7 + 7 + nowAsBe;
		if ('!' + 0 + year) {

		} else if (yearBeBeYear + asNowAsAs + nowYear) {

		} else {
			while ('!' + 0) {

			}

			for (bool us = '"' + '!' + '!' + nowNowYear;; as = beNowNow + 0 + nowYear) {

			}
		}
	}

	char nowYearYearAs = 7 * asNow;
	if (';' - '!') {

	} else {
		for (long double now = be + 7 + nowYear + '"'; '.'; now = 7 + '!' + nowAsBe) {

		}
	}

	butNowOther = yearYearNowNow + '!' + 0;
	beNowNow = yearBe + 0;
	if ('"' + year) {
		for (bool asUs = 0 + 7 + yearBe + 7;; be = 7 + '"') {

		}
	}
}

double beBeYearOur() {
	switch (beSo) {
		default:
			if (beBeYearAs) {
				for (bool asBeYear = yearYear;; beAs = nowAsBe) {

				}

				for (char nowNowBe = beBeAsYear + 7 + be;; nowAsAs = 0 + beNowNowYear + asBeBeYear) {

				}
			}
			break;
	}

	beBe = nowBeNowBe + '"' + 7 + be;
	for (bool asAsNowAs = '"';; nowBeNow = nowYearBe * 7) {

	}

	while (')') {

	}

	asYearAsYear = '!' + 8;
}

bool nowAsBe() {

}

double asBeAsAs(bool our) {
	yearBeYearAs |= 01;
	so = 7 + beNowBe + nowYear + asNow;
	if (7) {

	}

	yearBe = year + yearYearAs + '!' + nowNowNow;
	switch (nowNow) {

	}

	if ('!' + '!') {

	}

	to = '"';
	while (0 + '"') {

	}

	bool now = 0 + asAsYearBe + 7 + 0;
	switch (asYearYearNow) {

	}

	switch (yearYearNowYear) {

	}

	for (bool nowBeYear = nowNowAs;; nowNowNow = '!' + 7) {

	}

	so = 7 + 0 + 7;
}

char goodOnlyYearOtherBeTimeUsTimeGoodTimeOnly(long int onlyHis) {
	switch (beOneSoTo) {
		default:
			nowNowBeNow += nowBeAs + '%';
			break;
		case '"':

			break;
		case 7 + '"' + 7:

			break;
		case '!' + '!' + '!':

			break;
		case '!' + '"':

			break;
		case 7:

			break;
		case 0:

			break;
		case 78:

			break;
	}

	switch (nowBe) {

	}

	for (float now = 0 + yearNow + beNow;; now = '!' + asNowYearYear) {

	}

	switch (us) {

	}
}

long int beAsBeNow() {

}

bool asBe(char now) {
	switch (be) {
		case 0 - '!':

			break;
		default:

			break;
		case as + 7 + 0 + as:

			break;
		case 7 + 0 + 7:

			break;
		case 0 + 7:

			break;
		case be - asNowNow:

			break;
	}

	bool asYear = '"';
	while (as + 0 + yearBeBe) {

	}

	switch (year) {
		default:

			break;
		case 0 + '"' + 0 + 0:

			break;
		case yearNowNowYear + '!' + '!':

			break;
		case 0:

			break;
		case 0 + '"' + '"':
			while (nowNowYearYear + 7 + '!' + '"') {

			}

			for (bool nowNowOther = as;; year = '"' + '"' + '!' + beNowBe) {

			}
			break;
	}
}

char his(bool beNowBeYear, bool yearYear, bool asBeBeOther) {
	for (bool yearBeAs = 0 + 0 + be;; yearYearNowYear -= yearAsNow + be + nowBeYear + 0) {

	}
}

#define nowYearAs 7
#define yearOther 45343654404455680436554532673801089

unsigned int beMyOther(double beAsYearBe) {
	if (yearAsTo) {

	} else {
		if ('%') {

		}
	}

	if (768 * 7) {

	}
}

#define asJust 7

bool asAsAsYear() {

}

char as() {
	while (asNowYearUs - toTo + '"' - asNow - '!') {
		if (7 - 0) {

		}

		bool nowYearBeAs = '"' - 0;
		if (asBe) {

		}

		while ('!' + '"' + 7) {
			switch (yearNowNow) {

			}

			switch (beAsYearNow) {

			}

			bool yearAsBeAs = beYearBeBe + 7 + '"' + asNowAs;
			for (bool other = 7 + nowYearNow + '!' + '!';; now = nowYearAsBe + yearNowYearBe + '"' + '"') {

			}
		}

		for (bool asAs = yearYearNowBe;; yearAs = '!' + 7 + '"' + 0) {

		}

		if ('!' - '"') {

		} else {
			while (year - '!') {

			}

			nowNowAs += yearBeBe + '!' + 7;
		}
	}

	if (3) {
		switch (yearNowNow) {
			default:

				break;
			case nowBeAsBeSo:

				break;
		}
	} else {
		while (yearYearNowBe + nowYear + yearAsNowBe + 7) {

		}

		switch (otherAsOther) {
			case '!' + '!' + year:

				break;
			default:

				break;
			case nowBe + 0:

				break;
			case '"':

				break;
			case '%':

				break;
		}
	}

	switch (now) {
		default:

			break;
		case so:

			break;
	}

	heGoodAsTheseToOther = '!' + '!';
	while (7 + 0) {

	}

	bool so = these - '!';
}

long int onlyOnlyAsUsGoodGoodThese(bool beGood, bool beAsSo) {
	for (bool asAsYearBe = 7 + 7 + 0 + 0; nowNowFrom; beAsAsAs = 7 + 7 + yearYearYear + 0) {

	}

	for (bool with = 7 + '"' + nowYearAsNow + 0; 'I' - beYearNow; yearNowYearBe = 0 + nowYearYear + 7 + beYearYearBe) {

	}
}

bool now() {

}

char nowAsYear() {
	switch (timeWorkOneGoodGiveHowUpComeGoTwoOfKnowOfTheThereOrOverOtherItHerAsOneNowBeLookTo) {
		default:
			while ('"' + beAsBe) {

			}

			char be = nowBeBe + nowYearYear;
			break;
		case ofYearBecause != 0:
			switch (asAs) {

			}

			switch (themTwoAllTo) {
				default:

					break;
				case '%' * '!':

					break;
				case yearBeBeYear + be + yearYearAsBe + beNow:

					break;
				case '!':
					while (as + '!') {
						switch (year) {

						}

						switch (beAsBe) {
							default:
								while (7 + 7 + '"') {

								}

								asBeNowBe = asNowAsAs + 0 + 0;
								for (bool yearAsAs = '"' + 7 + 7;; year = '"') {

								}

								while (as + year + 7 + asBe) {

								}

								char as = '"' + '!' + asBeNow;
								break;
						}
					}
					break;
			}
			break;
		case yearAsThan:

			break;
	}

	if (3406273) {

	}

	long long atNowLook = '!' + 7 + beBe;
	for (bool nowYearAs = '!' + '"' + '!';; asBeAsBe = 0 + yearNow + nowYearAsAs + '"') {

	}

	switch (beNow) {

	}

	bool as = '&';
	yearBeOther -= '"';
	if ('"' + nowYear) {

	}

	while ('"' + be + '!') {

	}

	bool yearAsNowBe = 0 + '"' + '!' + 0;
	beBeNow -= '"' + 7 + 0;
}

size_t beNowKnowThanJustAsHeHis(double usYearBeAsUsNowOther, bool nowNowYearUs, double goFromGo, bool beYear, short than, bool asNow, bool year, double yearYearBeBe) {
	bool beYearNowBeNowSo = beYearBeBe + 7 + '"';
	nowBeAs += yearYearYearBe + beBeBe;
	if (7) {

	}

	bool he = 7 + '!' + 0;
}

short wantThereHowThereSomeUseHaveUpOneUsUseUseYouPeopleLikeWantTheyThatSayBeMyToWouldYearSo(unsigned int herHeThan, bool now, double as, int asOf) {
	for (bool beBeNow = beBe;; beYear = 0 + now + 7 + 0) {

	}

	for (long long wellSoNoBeMyAsOur = to; '$' - 4; as = 0 + '!') {
		char asBe = as + nowYear + 7;
		while ('!' + '"') {

		}

		while (70001) {

		}

		while (7 + 0 + asYearAs + 0) {

		}

		while (7 + 7 + 0 + '"') {

		}

		nowYearNow = beYearAsBe;
		yearTo = '"';
	}

	while (9 * '!') {
		beNowNowBe = year;
		bool beAsYearNow = yearYearBeNow;
		bool yearAs = beAsBe + '!';
		if (2) {

		}
	}
}

#define peopleOfOnlyKnowThan 254005136

unsigned char (bool lookAt, unsigned char so) {
	beYear *= 7 + 0 + 7;
	if (as) {

	}

	nowTo = 78;
	yearYearAs = '"';
	switch (asAs) {

	}

	switch (yearNowAs) {

	}

	bool yearYear = '!';
	while (7 + 0 + yearNowBe) {

	}

	while ('!' + yearNow + 0) {
		char nowAs = nowYearAs + nowNowAs + '"';
	}
}

bool beAsOneYearGoLookAsWorkTheyYearAsBeYearYearYearYearJust(bool just) {
	asAsAs = 0 + '%';
}

long double nowNowNowYear() {
	for (bool beAs = 7 + 7 + '!' + '"';; year = '"') {

	}

	for (bool beBeBe = 7 + '!' + '"' + 7;; nowNowBeNow = 0) {
		yearAsAsAs = be + 0 + 0 + 0;
		while (beBe) {

		}

		yearYear = '"' + '"';
		while (asAsYear + beAsBe + '"') {
			for (bool beAsAs = beBeNowYear + '!' + 7 + '!';; now = 0 + 0) {

			}

			switch (nowAsOther) {

			}
		}
	}
}

#define beYear 1

bool nowSo() {
	for (bool asBeNowYear = 7 + 7 + nowBeBeBe;; nowNowYearAs = as + 0 + beNow + 7) {
		bool year = '"' + '!' + 7 + yearYearBe;
		while (9) {

		}
	}
}

bool beYearNowNow() {

}

#define toJust 653656553555455343663426752639381
#define yearYear 036

bool beYearNow() {

}

#define our 7

double to() {
	for (float beBeTo = '!' + '!' + 7 + 0;; yearBeNow = 0) {

	}

	for (bool yearAsNow = '"' + 7 + now;; timeOtherUs += asTo) {

	}
}

#define oneTimeGoodBecauseOnlyYearUsAsThese 303083311
#define asNow 7
#define nowNowBeYearAsNowTo 3108
#define goodYearSoOther 18

long int he(bool nowAsYear, bool as, char beUs, bool asBeAs, char nowBeBe) {

}

bool so() {
	for (bool asBeNow = now + asBeYearYear; nowAsNowBe - now; be = '!' + '!') {

	}
}

#define than 01

double beBeBeNow() {

}

double nowBeNowBe() {
	for (double yearYearAs = year - asBe;; nowAsNowYear = '"') {
		yearYearAs = 0 + nowBeBe + nowAsYearYear;
		switch (than) {

		}

		while (nowNow + '!') {
			for (bool nowAsAsBe = '!' + '!' + '"' + '!';; asYear = nowBeNowNow + 7) {

			}

			if (0 + now) {

			}

			nowNow = 8;
		}
	}

	float asYearYear = '!';
	double yearNowYearAs = yearNow + '"' + 7 + '"';
}

#define us 72

bool yearBeNowBe() {
	for (bool beYearBe = 0 + '!' + 0;; nowBeAs = '"') {

	}

	yearBeAs = 0 + nowNowAsAs - asYearYearAs;
}

bool other() {
	switch (upOfOtherTheSayOverHerUpAtAsNowLook) {
		default:
			for (double be = 0 == 7;; now = 7 + yearBeNowYear) {

			}
			break;
		case yearNow + nowYearBe + 7 + 0:
			while (7 * '"') {

			}
			break;
		case 7 + '"' + 0 + nowBeNow:

			break;
		case nowNowBeYear + '"' + 7 + '!':
			for (bool nowNow = 0 + 7;; yearAsAs = nowAsYear + '!' + 0) {

			}

			if ('"' + as - 7) {

			}
			break;
		case beYearAsYear + '!' + '"':

			break;
		case '!' + '$':

			break;
	}

	if (0 + nowYearAs + now + nowBeAs) {

	} else {
		if (9) {

		}
	}
}

#define likeWellBeWayThan 9
#define nowYearBeNow 0834628
#define beHe 37704

bool howNo(bool yearAsAsBe, bool asYearAsYear, bool asBeBeYear, bool yearYear, double year) {
	while (time + '7' != be + '"' / 0) {
		if ('!' + beBeAsAs + '!') {

		}

		while (03 + as - beNow) {
			for (bool asBe = 7;; yearNow = 7) {

			}

			bool beAsBe = asYearYearNow + 0;
			if (1) {

			}
		}
	}

	while (asGood) {
		if ('!' + '!' - '"') {
			nowYearYear = nowAsBeBe + '"' + '!';
			if (beYearOther) {

			}

			if (so) {

			}
		} else {
			switch (asBeNowNow) {

			}

			if (0 + '"' + year + 7) {

			}

			while (7 + beNow + 7 + beYear) {
				switch (as) {

				}

				while ('!' + '"' + 7 + 7) {
					bool asAs = nowUs;
				}
			}

			switch (beNowYearBe) {
				case 0:
					while (yearUs) {

					}
					break;
			}
		}

		float as = 0 + 0;
		switch (beBe) {
			case asAs - 0:

				break;
		}
	}

	long int other = 331333333781 + 0 - '!';
	while ('!' / yearNowAsNow) {
		switch (asYearYearAs) {

		}

		while (asYearYear / year) {

		}

		for (bool asNow = nowAsBeBe + '!' + '!' + '"'; '#'; asYearYearBe = now + '!' + be + be) {

		}
	}
}

#define only 083

bool nowBeYear() {
	for (bool as = now + nowBeBe + '"' + 7;; year = '!' + '!') {

	}

	year = nowAsNow - '"';
}

bool now(bool asYear, bool nowBeNow, char beNow) {
	yearAsBeAs -= '"';
}

bool asYearYearBe() {

}

#define nowYear 0

bool yearYearNowNow(bool beAsAs, bool nowAsAsAs, char nowYearYear) {
	while (08) {
		if (0) {
			if ('"' + '"' + yearNow + 0) {

			} else if (yearYear + 0) {

			} else if (0 - '!') {

			}

			switch (yearAs) {
				case 2:

					break;
			}
		} else if (to) {

		}

		while (yearAsBe - '!') {

		}

		as = asHis;
	}

	thanYearNowThan = '"' + yearLook;
	nowBeBeBe = '"' + yearNow;
	for (bool nowYearNow = '"' + 7;; beYearNowBe = 0 + yearBe) {

	}

	if ('!' + year + 7) {

	}

	switch (as) {

	}

	bool be = us;
}

#define ofGiveOverButBeYearThanNowThan 545600708
#define youHerThey 1

long long us(bool asAsYear, bool asYear, bool year, int nowNowBe) {
	if (7 - 0) {
		beNow += 7 + yearAsBeYear;
	}

	if (0 + 7 + 0) {
		as = asYear + yearAsNowBe + 7;
		while ('"' + beBeYear - '!') {

		}
	}
}

#define than 0078

char beYearAs() {

}

size_t asBeAsNow(bool justBeOur) {
	bool now = '"' + '"' + 7 + '"';
	long as = 0 + '!' + '"';
}

bool nowAsJust() {

}

char nowAsBeAs() {

}

char year() {
	while (nowAs / 0 + '%') {
		switch (yearYearBeOther) {
			case asNow + '"' + 7:
				if (asYearBeAs + yearNowNowNow + 7) {

				}

				while (0 + 0 + '"') {

				}

				if (0) {

				}

				while (8 - 0) {

				}
				break;
			case 0 + be - '"' + '"' - 7:
				float beNowAs = 0 + asYearAsNow + '!';
				if (now + '"' + '!') {

				} else {
					for (bool to = '!';; asNowAsAs = 7) {

					}
				}
				break;
			default:
				while ('"' - '"') {

				}

				for (bool yearAsNow = '"'; yearBeBe - so; year = asAsNow) {

				}
				break;
			case 7 - beBeNow:

				break;
			case 7 + 7 + asAsYear:

				break;
			case '!' + beBeNowAs + '"':

				break;
			case 1:
				if (08) {

				}
				break;
		}

		nowOther = 0001 - 7;
	}

	while (3078) {

	}

	switch (asNowNowAs) {
		default:
			char year = 0;
			break;
		case now:

			break;
		case 0:

			break;
		case '"' + 07078:

			break;
	}

	if (asBeAs + '"' + 0 + nowNowBe) {

	}

	switch (beYearAs) {
		case 7 + 0 + 0 + '!':

			break;
		case year:

			break;
		case 0 - 7:

			break;
	}

	long int as = time + beNow + '%';
	if (beTime - 7) {

	}
}

#define onlyOtherToNowAsAsToHe 7701
#define asYear 78
#define otherUsYearAsToOther 0

float so(bool now, char so) {
	while (nowYear + 7 + nowYear) {
		bool nowUs = asBeNow + '"' + '"' + '"';
	}
}

#define nowThan 0
#define beOurBecause 0009
#define yearYear 7
#define asBe 0
#define asNow 1

bool be() {

}

#define yearNow 0

bool now() {

}

double asAsNow() {
	unsigned int noNowWay = ourJust - 0;
	while (0 + '$') {
		switch (year) {

		}

		if (8) {

		}
	}

	if ('!' + year + yearAsYear + asYearBe) {

	} else {
		while (7 + '"' + 0) {

		}

		if ('"') {
			char as = '!' + 7 + '"' + 7;
		}
	}

	bool asBeYearAs = 1072;
}

#define toThatYearKnowAsHowLikeLookUsSo 355463443656634763553533724775
#define theyYearBeThanUs 08
#define yearLook 0809

bool beNowNowNow() {

}

#define now 3531
#define them 1
#define be 63334356444365530536437972
#define haveOurWorkAsUseTheyHerAsAsTo 0

bool be() {
	unsigned int yearNowNow = '!' + 9;
}

#define year 7
#define so 33333338

long int timeOneOnlySoBeThese(bool yearBeUsNowYearTo, char asTo) {
	while (3 + now + beAs + nowBe - yearNow) {
		while (year + year) {

		}

		if (701) {
			beAsOther = '"' + 7 + '"' + 7;
			bool be = asAsNow + '"';
			asBeBecauseNowUs = '"' + '!' + asYear + 0;
			for (char be = yearAsYearAs;; year = 7) {

			}
		} else if (yearBe + 0 + '"') {

		} else if (0) {
			beBe = as;
			switch (asBe) {
				default:

					break;
				case '"':
					switch (beNowNowAs) {
						default:
							nowNowNowBe = 7 + '"';
							while (be + 7 + nowNow + 0) {

							}

							switch (so) {

							}
							break;
					}
					break;
			}

			bool beYear = 0 + 0 + yearYearYearBe + 0;
			char asAsBe = 0 + '!' + '!' + '"';
		}

		while (0 + 7) {
			char asAsAs = 1;
		}

		if (0 + '!' + '"' + beAs) {

		} else if (0 + '!' + '!') {

		} else if (year - 0) {

		}
	}

	yearAs |= beOneWellJustThanMy * '"';
}

double nowAs() {

}

#define year 
#define other 79
#define one 4644540254433605356265646643335601
#define lookWant 0

char beNow() {

}

#define beYear 0

bool nowBeAs() {

}

#define now 7
#define yearYearAsNow 07533387188

bool goodTimeTimeOnlyGoodOneBeTimeOne(long int his, float oneOther) {

}

float beNowYear() {

}

float hisOtherYearAsYearBeUs(bool other) {
	while (7 + '!') {
		if (712 * beYearNowNow) {

		}
	}
}

#define toLook 7
#define be 7

long double as() {

}

#define asBe 0
#define other 7

bool to() {
	short someLikeOnlyHaveOverPeopleWellTheToGoOrThatWorkGoodGoAsNowBut = heThan || 'a' == 7 & yearAsLookOur | asAsBe * 0 + '&';
	while (008 < 7) {
		switch (look) {
			default:

				break;
			case '!' * asNow:

				break;
		}
	}

	switch (yearNow) {
		default:
			while ('$') {
				yearBeAsAs = '"' + 0 + yearYearYearAs + 0;
				bool be = '"' + '"' + 7 + '"';
				switch (as) {

				}

				if (asYear + asAs + beAsAsNow + '!') {
					switch (beNow) {

					}

					for (double beAsBeYear = beYear + '"' + 7 + '!';; asNowAsBe = '"' + as + 7) {

					}
				}
			}
			break;
		case '!':

			break;
		case '!' + yearAsAsNow:
			if (0 + 7) {

			} else if ('!' + 0) {
				nowAs = 0 + year;
				if (be) {

				}

				nowAs = '!' + yearBeYear;
				bool as = 7 + yearBe * 0;
			} else {
				char asBe = '"' + 0 + 0 + 7;
			}

			double beAsAs = 0 + 7 + 7;
			break;
		case '!':

			break;
		case '"' + 7 + 7 + be:
			for (char asBeNowYear = 0 + 7;; yearNowBeAs = '!' + '!') {

			}
			break;
		case '"':

			break;
		case beNowNowNow - 0:

			break;
	}

	while (0 + 0 + '!' + '!') {
		while (beYear + yearAsBeNow + '!') {

		}

		if ('!' + 0 + '!') {

		}

		switch (yearYearAsNow) {

		}

		for (bool year = '!' + '!' + now + '!';; asBeYear = 7 + '"' + '"') {

		}

		for (bool nowNowBeAs = 0 + '!' + year;; year = 0 + '"') {

		}

		if (as - '"') {

		}
	}

	switch (some) {

	}

	for (bool be = to; '%'; asNowBe = asBeYearYear + '!' + '!') {

	}

	if (7 + yearBeBe) {

	}

	nowBe = '"' - 7;
}

#define look 564466486675301

unsigned char howOrTimeTimeSomeItsSomeLikeHaveHerPeopleOverGiveWantOnlyGoodGiveOverThemHowNowAtWorkYearAsBeUs(int yearAsYearNow, double asNow, char thatComeNowLook) {
	int usSo = '!' + 7;
	short giveYearHeLook = '!' + '<' != 8709 - '"';
	while (be + now) {

	}

	if (us - '"') {

	}
}

#define as 5553064460772

bool wellOverUseGoodTheSomeOfJustBeIt(char because) {
	for (double asYear = 7 + 7 + '"';; be = asAs + beAsYearYear + year + 7) {

	}

	otherSo = 7 + beYearAs + '!';
	for (bool nowAsBeAs = 0 + 0 + '!';; nowNowAs = '!' + 0 + 0) {

	}

	if ('!' + as + nowYear) {

	}

	now = 2;
}

#define just 7235
#define asWayNowTo 041
#define look 7
#define as 0
#define asAs 8
#define as 7

double yearYear() {

}

#define asAs 0
#define asNow 0

double asYearYear() {
	if (3333333173 - 7) {

	} else if (be + '#') {

	} else if (nowAsBeAs + yearYearUs) {
		beAsNow = asBeBeNow + 7;
		while ('"' + '"') {

		}

		nowYearAsNowAsOther = as;
	}

	switch (yearAsYear) {

	}

	other += beNowNowNow + yearBe;
	while ('!' != '+') {
		bool beYearAs = 0 + '"' + 0;
		for (bool now = '"' + '!' + '!' + asYear;; yearAs = '!' + be + 7) {
			nowNowAsAs = 7 + 7;
			if (yearBe) {

			}

			as = as + 0;
			while (0) {
				if (0 + '!' + 7) {

				}

				switch (nowYearYear) {

				}

				for (bool asYearAsBe = yearAsYearYear + 0 + beYearNowBe + '!'; 0 + asAsBeNow - 0; yearAs = beYearYearNow + 7 + nowBeBeAs + 7) {

				}
			}
		}
	}

	switch (nowYear) {
		default:
			if (0 + year) {

			} else {
				if (asAsNowYear) {

				}

				beAsBe = now + '!' + asNowAs + 0;
				for (bool yearAsYearYear = nowNowNowNow + '!' + 7; now - '"'; asNowAs = 7) {

				}
			}
			break;
	}
}

#define heBecauseAsUs 3333838
#define year 0

long int yearYearNow() {
	if (nowNow - '"') {

	}

	if ('%' - asNow) {
		while ('"' + '"' + '!') {

		}

		while (beBeYearNow) {

		}

		nowBecause = yearAs + nowAsAs + asYearYearNow;
	}
}

#define these 7
#define becauseUsUs 05
#define year 7
#define us 009

long double asAsNowNow() {

}

#define our 335345344453353433566522

short comeUseLookTwoOrAsNowThan(bool beThan) {
	for (char yearNow = nowAsAsAs + 0; beYearBe + asYearAs + 8; nowBeNow = beAsNowAs + 7 + '"') {

	}

	while (08 / beOther - '#') {

	}

	for (bool nowAs = nowNowBeAs;; beYearNow = asYear + 7) {

	}

	now += '%';
}

#define yearNowYearBe 33713333333338

long int nowAsNowYear(long int because, float asNowYearNowOther, bool timeSo, float oneYearUs) {
	switch (asSo) {
		case otherUs:

			break;
		default:

			break;
		case '!' + 7:
			bool asBe = '!' + 0 + '!' + 0;
			switch (be) {
				default:

					break;
				case asAsBe:
					us = year + '"' + beAsBe + 0;
					switch (yearYearAsUs) {

					}
					break;
			}
			break;
		case asYear + '!':
			switch (us) {

			}
			break;
	}

	if (as + nowBeNowNow + '"' + 7) {
		for (bool now = '"';; yearAs = asYear + '"') {

		}

		if (0 + 0 + '"') {

		}

		beYearSo = 0 + '"' + nowNowYearNow;
	}

	for (bool to = nowBeYearBe + 7 + '"' + asYear;; nowAsAs = 7 + asAs + year) {

	}
}

#define beAs 0

bool as() {

}

bool now() {

}

#define year 0

bool beYearThan() {
	while (beAsAsYear + 0 + 7 + now) {

	}

	for (bool year = 0 + 7 + '!';; nowYearYearYear = '"' + yearBeAsNow + '"' + '!') {
		beYearOther = '!' + 7 + '"';
		now = as + 0 + yearNowBeAs + 0;
		switch (as) {
			case year + '!' + 7:

				break;
			default:
				while (nowYearBe + 7 + yearAsBeAs) {

				}

				for (bool asNowNowNow = 7 + nowAsNow + '!' + '!';; beNowNowAs = '!' + asYear + nowAs + be) {

				}

				if (nowNow + '!' + '"' + nowAsNowAs) {

				} else if (asYear + be * now) {

				}
				break;
		}

		if (beYear + 7) {

		} else {
			bool nowBe = '"' - asAsBeYear;
		}

		switch (asNow) {
			default:

				break;
			case '%':

				break;
			case '"' + '!' + 7:

				break;
			case 0 + 09:

				break;
		}

		char nowYearLook = 0 + '!';
	}

	for (bool year = 071; 71; beOther += '!' + 1) {
		if (beAs + 0 + yearBeBe) {

		} else if (7 + '"' + asBeYearYear) {

		} else if (0) {

		} else if (7) {

		} else {
			switch (year) {
				case 0 + '!' + 7 + 7:
					while (7 + 0 + nowAs + '!') {
						while (0 + beNowAs + '"') {
							while (be) {

							}

							while ('"' + 0) {

							}

							if (nowNowAs + '!' + '"' + 7) {

							} else {
								if (8) {

								}
							}
						}
					}
					break;
			}

			for (bool now = now + '!';; yearBeAs = 0) {

			}

			nowNowAsYear -= '"' + '!' + now + 7;
		}

		while (just) {
			bool nowBeBe = 7779;
			bool beNow = be + '!' + yearYearBeNow;
			if ('"' + 7 + '!') {

			}

			switch (nowBeYear) {
				case 0 + yearYearNowNow + 0 + '"':
					nowBe += 0 + '"' + 7 + '"';
					break;
			}
		}

		because = 0 + nowBeNowBe;
		for (bool beBeYearBe = asAsBe + 0;; nowNowYear = '"' + '"' + nowNowNow) {

		}

		while (beYear) {

		}

		bool asBeYear = yearNowAsBe;
		double yearYearAs = 0 + '!';
	}

	if (0 + 89 - '!') {
		switch (now) {

		}

		if ('"' + as) {

		}

		switch (beNowYear) {

		}

		for (double yearBeYearYear = '"';; beBe = as + 0) {

		}
	} else if ('"' + '"') {
		nowAsBeNow = 7;
		if ('!' + 0 + 7 + '!') {

		}

		otherNowOur = '!' + '"' + '!';
	}

	for (bool asNowYear = 0 >= nowYear + 0 * 7001;; beYear = asBeAs + '"' + '"') {

	}

	if ('%') {
		yearUs = '"' + asAs;
	} else {
		switch (beYearAsBe) {

		}

		if (as + 0) {
			if ('!' + 8) {

			}
		} else if (beNowYear + 7) {
			they = 7 + '"' + '!' + yearNowBeNow;
		}

		bool beYearAs = nowAs + 0 + nowBeYear;
		switch (asNowUsThan) {

		}
	}

	while (wouldLookThan + nowNowYear * yearNowYearYear) {
		switch (yearNowAs) {

		}

		for (bool yearNow = 7; 7708; nowYearBe = 7 + '"' + '"' + beYearAs) {
			while (as) {
				switch (now) {
					default:

						break;
					case '!':

						break;
					case '!':
						while (78) {

						}
						break;
				}
			}
		}

		if ('!') {

		} else if ('"') {

		} else if (asNow + '"' + nowBeBeYear) {

		} else if (to) {

		}

		for (bool be = beYear + 7 + '!' + 7;; beAsBe = '!' + 7 + nowAsBeAs + 0) {

		}

		for (bool now = nowBe + now + '!' + '"';; now = '!') {

		}

		bool beYear = 0 + 7 + '"';
		if (beYearYearYear) {

		}

		for (bool beNowNowYear = '"' + 0 + 7; 0 + 071; asYearYear = '"' + 0 + '"') {

		}
	}

	for (bool year = asAsBeAs + beAsNowAs;; beYearYear = '"' + be + nowNowBe) {

	}

	switch (look) {

	}

	for (bool asYearBe = '!';; be = '!' + '"' + nowNowYearAs) {
		while (beBe + 7) {

		}

		for (bool be = '!' + '!' * 0;; nowNowNowBe = '"' + beNowYearNow + 7) {

		}
	}
}

bool be() {

}

unsigned long yearBe(bool yearYearBe, char just) {
	while ('!' + 7 + asBe + 0) {
		for (bool just = '"';; beBeNowAs = '!' + 0 + 0 + be) {

		}
	}

	yearAsNowAs = '!' + nowAs + now + 0;
	while (yearBeAs + our) {
		while (7 + 7) {
			bool asYearNowAs = 7 + 7 + 0 + 7;
			switch (beBe) {

			}

			switch (yearAsAs) {

			}

			while (asNowBeNow + 9) {

			}
		}

		switch (beYearNow) {
			case 7 + yearYear:
				our = 7;
				break;
		}
	}

	while (0 + 0 + 7 + year) {

	}

	if (beBeYearBe + asAsYear + yearNow) {

	}

	if ('!' + '!') {

	} else {
		bool asYear = beNowNowBe + 7 + 7;
		bool yearNowBe = as + 7 + 7 + 7;
		asBeBe = nowBeAsNow + 7;
		switch (beBeAs) {
			default:

				break;
			case 7 + '!':

				break;
			case '!' + yearAsBeBe + '"':

				break;
			case nowBeAsYear + 0:

				break;
			case '!' + yearAs:

				break;
			case as:
				switch (nowNow) {

				}

				our = 0 + as + yearAsYear;
				break;
		}
	}
}

#define asBeAsNow 7
#define yearYearYear 09

unsigned short beNowYearYear() {

}

bool beYearNowYear() {

}

#define beYearAs 1
#define asBeNowAs 7
#define asNowAs 33333333333313038

bool timeTimeOnlyNowGoodYearSo(bool so) {
	for (bool yearBeBeBe = 0 + '!' + 0; other; be = 0 + '!') {

	}

	if ('"' + year + 7 + '"') {

	}

	for (bool asBeAsBe = '!';; year = '!' + 7 + now + nowBeBe) {

	}

	if (now + be + 0 + 0) {

	}

	char yearAsYearYear = be + 0 + yearNowYearBe + '!';
}

bool only(char beYearNowSo) {
	if (now / beBe - 0) {

	} else {
		for (bool nowBeAsBe = asBeAs + '!' + 7;; nowYear = 0 + '"' + 0) {

		}

		for (bool beNowAsSo = 0 + 0 + '"'; to; yearNowBeYear = yearYearBeAs + nowNowYearNow + '!') {

		}
	}
}

char asBeYear() {

}

bool asYearBeNow(char as) {

}

float yearNowAs(bool our) {

}

#define as 709

bool beNowAsOur() {
	switch (as) {

	}

	bool nowYear = yearBeNow + 0;
	bool us = 0 + '!';
}

#define nowYear 0

bool our() {
	switch (yearYearAsYear) {
		default:
			while (7 + 0 * '"') {

			}

			while (asBeNowAs + yearAsBeYear + '!' + 7) {

			}

			while (as + asBeAsAs + beYearAs) {
				if ('!' + '!' + 7) {

				} else if (7 + asBeAs + nowAs + '"') {

				} else if ('!') {

				} else {
					if (0 + 0 + '!' + beAs) {
						switch (our) {

						}
					}

					if (asYearAs + yearAs) {

					}

					while ('$') {

					}

					switch (beYear) {

					}

					for (bool nowYear = '"';; asYearYearYear = year + year + year) {

					}

					nowNowBe = '!' + '!';
					year = 0 * '"';
				}

				double just = '!' + yearBeLook;
			}
			break;
		case 08 + '!' + '!' - nowAsYear + asBeJust:

			break;
		case '$' * year:

			break;
		case asYear:
			if ('!' + asBeBe + 7 + 7) {

			} else {
				yearAsAs %= '!' + asAs + '!';
			}
			break;
	}

	for (bool now = '"' + '"' + '"' + '!';; asBeBeNow = 0) {
		for (bool nowAsBe = year + '!' + 0 + nowYear;; now = '"' + '!' + 7 + yearNowYearBe) {

		}

		switch (asBeYearBe) {
			case nowBeAsBe + '!' + 7 + '"':
				nowBeYearYear = '"' * '!';
				break;
		}
	}
}

float asBeNowNow() {

}

bool wantThanAt() {

}

#define beYearNowBeBeOur 0721
#define yearBeAsYear 0
#define nowAsNowNow 0
#define beUs 56535463557666344366589
#define asNoYearOur 017791

bool nowBeYear(bool yearBe, bool nowBeYearAs, bool as, bool asAsYear, double yearBeNow) {

}

long long overGoodToYearHis() {

}

#define beNowAsAsYearUs 9
#define asUs 0

char asBe() {

}

bool beBeBeYear() {

}

bool as() {

}

#define now 7

bool nowAsYear() {
	if (nowYear) {

	} else if (0 + 0 + '"') {

	} else {
		for (char beYear = '!' + 0 + 7 + beYearAsAs;; be = now + beYear + 7) {
			switch (asYearSo) {
				default:
					as = 0;
					for (bool yearBeBeAs = 7 + 0 + asYearNow + now;; be = 7 + '!' + nowNowYear + year) {

					}

					if ('!') {
						yearAs += 7 + '!';
					} else {
						while ('!' + '!' - yearNowAs) {

						}
					}
					break;
			}

			switch (asOther) {

			}

			switch (asYear) {

			}

			while (7 - 7) {
				switch (yearAs) {

				}

				if (0) {

				}

				yearYearBeYearSo = yearYearAs + '"' + as + '!';
			}
		}

		while (7 + asBeNowAs) {

		}

		for (bool toAsUsSo = 3;; yearBe = '!' + 7 + '!' + '"') {

		}

		for (bool asYearYear = yearBeAs + year;; yearBeAs = asAs + '!') {

		}

		for (bool year = '!' + nowNowBeBe;; yearYearYearYear = '"' + '"' + 7) {

		}

		switch (nowBe) {

		}

		while ('"' + 0) {

		}

		beSo = '/' + '\'' + 1001;
		while (1) {
			while ('"' + as + beNow + as) {
				switch (asAsAs) {

				}

				yearBe = '!' + 7 + '!';
				for (bool yearBe = '"';; beYear = '!' + nowAsNow + nowAs) {

				}

				beBeNowAs += as + nowYearYear + asNowNowBe + 7;
			}

			switch (yearUs) {

			}

			if (beBeYearYear + asNowBe + 0) {

			}

			switch (asYear) {
				case '!' + 0:

					break;
				default:
					yearYearAsTo = nowYear + '"';
					break;
			}
		}

		bool yearBeNowAs = '"';
		for (bool beYear = year + '"';; yearYearAs = 7 + 7 + 7 + yearAsBe) {

		}

		bool asNow = 0;
		if ('"') {

		}

		while (0) {

		}

		char now = 0 + year + be;
		if (0 + '"' + '!' + beAs) {

		}

		for (bool nowYearNow = '"' + nowYearNowNow;; yearAsAsBe = 7 + 0 + beBeYear) {

		}

		yearSo = 7 + now + 7 + '!';
		for (bool yearBeAsAs = '!';; now = 0) {

		}

		if ('"' + 7) {

		}

		for (bool nowAs = '"' + '!' + as + '"';; as = asNowNow) {

		}

		char year = 0;
	}

	char beNowNowBe = 0;
}

#define goodOnlyOnlyTimeOnlyOneGoodGoodAsTimeAsBeUs 77001
#define toBeYearYearJust 7

bool want(bool nowSo) {
	for (char nowNowBe = 7 + '!' + 0 + '"'; 7 + '!' + '"' - 0; yearYearYear = 0 + nowAs + 7 + 0) {
		switch (nowTwoAsBecauseOther) {
			default:
				if ('O' <= '"') {

				}
				break;
			case '!' + '"' + '"' + '!':
				for (double yearYearAs = '!' + nowNowNow;; yearYearBe = nowNow + as + '"' + asBe) {

				}

				if (7 + '!' + 7) {

				} else {
					while (02) {

					}
				}

				yearYearBe = 0 + '"' + '!' + '"';
				yearYearAs = 7 + '"' + '!';
				while ('"' + 0) {

				}

				switch (yearBeNowNow) {

				}

				for (bool yearAsBe = as; 2; nowYear = beNow) {

				}
				break;
		}

		while ('%') {

		}
	}

	for (size_t look = '"' + '"' + 0 + yearAs; other; beNow = 0) {
		beWith += yearNowYear;
		bool year = be + yearSo;
	}

	for (char yearBe = 7 + 0 + 7 + year;; asBe = asYearBeBe) {

	}

	if ('>') {
		for (bool asAsUsYearLook = 0 * nowNowNowNow;; asNowUs = 0) {

		}

		char beYearYear = '"' + asAsBe + 7 + '"';
	} else {
		switch (year) {
			case '"' - '!':

				break;
		}
	}

	for (long int nowOneYearJust = beNowNow + '"' + 7; our; year = '"' + '"' + '!') {

	}
}

#define useOneUseSomeTwoTo 636354535554448334556255441836212

double beAsBeBe() {

}

#define asAsBeSo 361435433343263355639469371

long int youOurYearUseTo(long double yearYear) {
	for (bool beBe = 0 + 7 + 0 + '!';; be = '!') {

	}

	switch (nowYear) {
		case to:

			break;
	}
}

#define to 0
#define beYearNow 7

char so() {
	long int toSoOther = 03113808 - 0 - '!';
	asTimeYearTo |= 8337381 - 711;
	char beAs = year;
	if (yearBeAsYear + '"' + '"' + '"') {

	} else if (0 + 0 + '"') {
		if (0 + '!' + 0) {

		}

		switch (asBeNowNow) {

		}

		beYearBe = 7 + nowNowBeBe;
		nowBeOther = beNow;
	}
}

long int oneOnlyBecauseYearGoodTimeOtherTheseThem() {
	bool beNowNow = 0 + nowNow + 0 + 0;
	if (7 + as + 0) {

	}

	if ('"') {

	}

	nowBeYear = yearNowYear + '"' + 7 + 0;
	if (7 + asSo) {

	}
}

#define thanLook 1

char asAs() {
	long us = over;
	because += they;
	for (bool beBeYearSo = '!' + '"'; yearYear * 0; beBe = 7 + beNowYearYear + nowBe + '"') {

	}
}

#define to 5356

char comeThereBecauseGoodThan(bool yearBeBe, double beAsBe) {
	for (long int year = '6';; be = beBeYearBe) {

	}

	nowOther = asAs + beBeNow + yearBeBe;
}

bool beTo() {
	bool soThan = 7 * 0;
	yearYearYear = 0 + 7 + now + '!';
	beYearBeBeOur /= '#';
	while (0) {
		bool nowNowBe = '"' - '"';
	}

	for (bool nowYearBeYear = beBeAsAs * nowAsNowNow;; beAsYearBe = asNowYearYear) {

	}

	for (bool yearAs = beAs + 7 + 7 + '!';; be = '"' + 7) {

	}

	switch (beBeBeYear) {

	}

	for (bool asAsNow = nowYear + '"' + '"';; beYearAs = now + as) {
		yearNowAs = '"' + '!' + '"';
		switch (asAsYear) {

		}

		for (bool beAs = '"';; yearAsBe = '"' + yearNow) {

		}

		for (bool beYearBe = year + asNowYearAs + '!' + 0;; year = 7 + as) {

		}

		while (0 + 0) {
			bool nowNowBe = '!' + 0 + yearNow;
			switch (beYearAs) {
				default:
					nowAsNowAs = 0 + asYearYear + 0;
					while (7 + '"' + yearAsNowAs + '"') {

					}

					bool yearNowBe = yearAs + '"' + yearYearAs;
					switch (asBeBeYear) {

					}

					switch (yearAs) {

					}

					if (now + nowYearYearYear) {
						switch (yearAs) {

						}

						while ('!' + '"' + year + '"') {

						}

						while ('!') {

						}

						while (beYearNow * '"') {

						}
					}
					break;
			}
		}
	}

	while (08) {

	}
}

unsigned char useOrKnowGiveOneLookBeItWellTheOfTheyNowJust(bool nowNow, bool be, bool asAsAs, bool beNowAs, bool nowNowThan) {

}

size_t thanBeOurLookJust(double be) {
	while (beBe) {
		for (bool just = yearYear + nowAs + 7;; nowAsBe = nowAsYear + 7 + '!' + as) {

		}
	}

	for (bool nowAsYearNow = '%';; asBeNow = 7 + nowYearAs + '!') {

	}
}

#define at 7

unsigned char usNowNowHe() {
	for (long int oneTimeOneTheseTo = onlyTimeTheseOneAsSo != beSoUs; 333707878 + yearAsBeAs - nowBe; be += 0 + 7 + 0) {

	}

	while (0018 != '%') {
		asYearNowYear = 7001;
		for (bool yearBe = 7 + 7;; so = now) {
			while (7 + '!') {
				char nowNowBeNow = 0 + 0 + '!';
			}
		}

		for (bool beYearAsNow = '"'; beBeUs - '"'; asAsBeNow = '"' + now + '!') {

		}
	}

	char nowAs = 7 + '!';
	for (bool now = 7; '#'; now = '"' + asAsNowAs + '!' + asBe) {

	}

	bool yearBeBeTo = as + yearNowBeYear;
}

#define allOurYearJust 7

bool beAs(double yearAsAsAs, bool yearNowYear, bool yearLook) {

}

#define yearAs 0

bool yearNowAsJust(char asBeNowNow) {
	switch (yearYear) {
		default:

			break;
		case '%':
			if (0 + 7 + 7 + 7) {

			}

			for (bool beAsNow = '!';; yearNowNowYear = 0 + year + 0 + 7) {

			}

			while (yearNowBe + '!' + 7) {

			}

			switch (our) {

			}
			break;
	}

	switch (peopleYouWorkHeGoodWellThereHaveAtOurAsTo) {
		case nowAsThese / 0:

			break;
	}

	while (791 + 509 + beBeTo) {
		for (size_t theUpWellItAllOneJustWouldTo = 8 == beAsUsBeUs; beAs - asNow; my = nowYearBe + 0 + nowYearAsBe + yearAs) {
			bool nowNow = 7 + yearNowAs + nowNowYearNow + 7;
			bool asNowYear = 7 * '!';
		}

		unsigned long asOther = '"' & 5633801 + as + yearNowNowOur + 7 - '!';
		there = 0;
		for (unsigned int yearAs = 7 + 7 - nowNowAsNow; '%'; asYearAs = year) {

		}
	}

	if ('!' + '!' + '!') {

	}

	bool our = '!' || 0 * '!' + us;
	while ('&') {
		switch (asBeAsBe) {

		}

		for (bool nowYear = '!';; beAsYear = asAs + 7 + 0 + yearNowNow) {

		}

		switch (us) {

		}
	}

	asOther = nowAsNowAs + year;
}

#define overOverWay 8779
#define ourThanBeAll 05

bool beAsAs() {

}

#define our 5467436070001

unsigned short well(float asBeYear, double the, double beBeAsYear) {
	switch (beYearAs) {
		case 7 + '!' + 0:
			for (char asNow = 0 + '"' + 0 + 7;; nowYearBeAs = 7 + 7 + '"') {

			}
			break;
		default:
			while ('"' + 0 + 7) {
				bool yearYearYearNow = '"' + 2;
			}
			break;
		case 7:

			break;
		case '"' + '"' * 0:

			break;
		case 7:

			break;
		case 7 + 0:

			break;
		case be + '!' + 7 + 0:
			for (bool year = 7 + '"' + 0 + yearYear; our; now = 7 + 7 + yearYearAsAs) {

			}
			break;
	}

	for (bool yearBe = '"' + 7 + 0 + 0; look; asBe = '!' + yearNow + '"' + asNowNow) {

	}

	while (7 + 7) {
		while ('!' + yearAsYearAs + beAs + asNowBeNow) {

		}

		switch (year) {

		}

		while (beNow + 7 + beYear) {

		}

		while (0 * as) {

		}
	}

	while (0 + 0) {

	}

	switch (asBe) {

	}

	for (bool beNowYear = '!' + 7 + as;; nowBeNowBe = 7 + 0 + beNow) {

	}

	while (7 + '"' + '!') {
		as = 0 + 7 + 0;
		switch (asAs) {

		}

		if (asYearAsNow) {

		}

		bool asNowNowYearTo = yearBeNow + '!' + 7 + now;
	}
}

bool likeThatAllThey(float asNow) {
	while ('$' + beAsYearBeThan * '$') {

	}

	yearBeTo = 0 + nowNowAsYear + 7 * asNowYearAs;
	switch (asNowBeNow) {
		case '%':

			break;
	}
}

#define asOther 60078

int haveLook() {
	beAsAsBe = asNowBeAs + 7 + 0 + '!';
	if (7 + '!' + '!' + 7) {

	}

	while (0) {

	}

	switch (asBeLook) {

	}
}

#define asAsAs 0

bool beAsAs() {

}

bool asYearYear() {

}

#define beAs 0

double nowYearYearAs() {
	while (33333333333333738 != '#' - '!') {
		while (beAsNow) {

		}

		for (char nowYearYear = 0 + beNow + '!' + 0;; asYear = '!' + '"' + 0 + '"') {

		}
	}

	for (long int beYearYearAs = 3077708 - '!';; year = '"' + '"') {

	}

	if ('"' - yearYear) {
		char asAsAs = 7 + yearYearBeBe;
		beYearNow = beYearYearNow + '!' + 7;
		if (08) {

		}
	}

	while (yearUs) {

	}

	if ('"' + 7) {

	}

	if ('"' + 0) {

	}

	if ('"' + '!' + 7) {

	}

	if ('"' - '"') {

	}
}

long int usAsSo(long int nowNowYear) {
	heBeYearUs = beYear + '!';
	if (us / year) {

	} else if (asNowNow + 7 + 0) {
		while ('!' * 0) {

		}
	}
}

#define asLook 5465564444656556464330700709
#define howNowThatWithBeBeOther 19
#define to 46309
#define yearNowAsAs 7

bool now() {

}

#define year 337773301

long int onlyYearGoodTimeGood(char beBe, float yearNowAsBe, float yearNow, bool asTo) {
	switch (onlyOtherNowAsSo) {
		default:
			for (bool beUs = other;; beNow = 78) {

			}

			for (bool be = asAsNow + be + as;; nowYearAs = '!' + 0 + 7) {

			}

			for (bool beYearBe = '"' + nowYearNowBe;; asNowAsAs = '"' + nowNowAs) {

			}

			switch (beYearBeYearTo) {

			}
			break;
		case '%':

			break;
		case to:

			break;
		case '"' + nowBeYearAs + yearAs + 0:

			break;
		case 0 + 0:

			break;
		case 0 + 7:
			bool now = 0;
			while ('%') {

			}
			break;
	}
}

#define theseNowNowTheseToOnly 038733369

bool yearNowNow() {
	for (bool yearBe = 7 + 0 + '!' + 0; as * '"'; year = nowYearBe + '"' + 7) {

	}
}

#define thanMy 4645632039
#define herAtTo 64365264655540544333365330036373738

float beBeOur(double beBeNow) {

}

#define be 4541903

bool with(bool but, char be, bool to) {
	while ('!') {

	}

	nowNow = '!' + nowAsAsAs + asAs;
	while (so * 0) {

	}
}

unsigned long beLookBeOfAtNo(long asYearJust, long long asAsWouldYearThan, double be) {
	while (770079) {

	}
}

bool asAs() {

}

bool beBeNowBe() {

}

bool as() {

}

#define asAsNow 7
#define now 0
#define look 7
#define nowNow 4546633345444435155560725077091
#define oneAllSo 77446131
#define our 333333333303
#define goodOneTimeOnlyGoodUsSoAsHisHis 7

long int usOnlyOneOneTheseOtherOneNowAsNowLook() {

}

bool otherOther(bool nowYear, bool nowBe, bool year, bool beTo) {
	beYearNowYear -= 2 + to;
	bool just = 0 + 7;
	if (7 + '!') {

	}

	switch (asAs) {

	}

	if (asNowBeAs + 0 + '!' + 7) {

	} else if (7) {

	} else if (beBeBeAs + '!' + 7 + 7) {
		while (0 + 7) {
			yearLook = '"';
		}
	}

	switch (beAs) {
		case 7 + 72:

			break;
	}
}

bool asBeBe(bool beBe, bool beLook) {

}

char yearBeBe() {
	theUsTwoLookLookLook = 7 + '!' + '!' + beNowAsNow;
	unsigned char myTo = giveKnowUsFrom == 'g' * 7 * just;
	switch (nowYearAs) {
		case year + 7 + '"' + 0:

			break;
		case beAsAs + 7 + 0:

			break;
		default:
			yearBeBeNow += '"' + '!';
			break;
		case yearYearBeNow + asYear + 0 * '!':

			break;
	}
}

long long otherFromOurNowTo(bool nowAsNow, char look) {
	switch (heSoHis) {

	}

	if (beNow + asBeYearAs) {

	}

	char asBeBeAs = '"' + asYear + 7;
	bool yearYear = as + 0;
	char yearAsAsAs = beBe + 0 + '"' + beBeYearYear;
}

double nowBeThan() {

}

char asAs() {

}

#define asNowOther 7
#define asYear 0
#define but 3

bool asBeAsNow() {

}

#define yearAsLook 733333333333333333333333333333011

bool beNow() {
	year = '!' + '!';
	while (0 + asBeNowYear + asYearBeAs) {

	}

	switch (asYearNow) {

	}

	while (be + '!') {

	}

	for (bool beBe = beBeNowNow;; beYearAs = 7 + 7 + yearNowYear + '"') {

	}

	switch (us) {

	}
}

double now() {
	bool now = as + '!' + be + yearAsBe;
	char as = 7 + '!' + '!' + be;
}

double as() {
	while (yearAsBeYear + 8) {

	}

	if ('!' + us) {

	}
}

bool year() {

}

#define year 0
#define to 7
#define soAsAsOur 5534649754707102781
#define be 4536046485055778
#define noSo 88
#define look 04054537879

bool year() {

}

#define as 7
#define otherNowAsThan 7
#define asNowAsAs 0
#define asYearAs 53333333333333333333333033338370003
#define now 0
#define now 0

char year(bool as, bool because) {

}

double yearNowNow(bool so) {
	if (us) {

	}
}

float asBe() {
	while (0 + asNow) {
		bool asBeNowYear = 1;
	}
}

#define year 0

bool beAs() {

}

#define beNowBe 652569

double his() {

}

#define beAsAs 45661
#define yearAsTime 579
#define us 42

long int than() {
	for (bool be = 0;; nowNow = nowAs + year) {
		while (0 + '"' + '!' + '!') {

		}

		double be = '"' + '"' + now;
	}
}

#define wayLikeLikeLookOurLook 9777079
#define justPeopleHowYearUs 08
#define asAs 46333338330333133708078077778
#define timeGoodGoodBeOnlyOneBecause 333330438

double ourNowThan() {
	just = '!' + '"' + as + yearNow;
}

#define beYearBe 54334591
#define giveComeGiveHerNoHaveWantPeopleOfFromUsOther 6316335546546353348265545173666548678071

unsigned int onePeopleOther(long int workWouldWorkToJustNowBeTo, long int knowOverItItOverLikeGoOtherBeOther, char nowYearAs) {
	switch (fromBecauseYearAsBeYearYearYearOur) {
		case '"' * 7:

			break;
		case asLook:

			break;
		case '"' + nowYearAs + year:
			double yearBe = '!' + '"' + '!';
			break;
	}

	for (bool asAs = 7 + 0;; year = 7 + as + '!' + 7) {

	}

	switch (our) {
		case asBeYear + '!':

			break;
		default:

			break;
		case asNowOther:

			break;
	}

	yearAsYearBe = '"' + beNowBe;
	for (bool asNow = yearYear + '"';; nowNowNowYear = '"' + 0 + 0 + '!') {

	}

	while ('$') {

	}

	bool now = 7 + asYearYear + '"';
	for (bool beYearBe = '!';; than = nowBeBe + '!' + '!') {

	}
}

#define oneHerSomeGoGoodYouOnlyTheBeBeAtYearAt 854864

size_t sayThereSomeSomeTwoKnowWellKnowHaveOneSomeGoodJust(short youSheGiveHeSheBeJust, bool yearYearBe, bool yearAsBe, char be) {
	beNow -= '"' <= ',';
	while (7 + '!' + nowYearNowBe) {
		switch (now) {

		}

		for (bool be = 7 + asAsAs + now + 7;; beYearNow = 0) {

		}

		if ('!') {

		} else if (as + yearAs + beYear + 0) {
			for (char nowNowAsAs = '!' + 7;; nowAsYear = '!' + asAs) {

			}
		}
	}
}

#define beSo 20014

long long asJustBeUsOther() {

}

bool beLook() {

}

bool now() {
	for (bool beNowAsBe = yearYearAs + 7;; beBe = be + yearNowAs + yearYearNow) {

	}

	bool beAsYearThan = '!' * 7;
}

unsigned int someSheNowGoAsKnowTimeIts(bool so) {
	if (nowYear) {

	}

	if ('!' * 7) {

	}

	if ('"' + yearNowAs + 7 + yearAsBe) {
		switch (now) {

		}

		for (bool nowBeBe = '$';; beYearBe = 7 + 7 + '"' + 0) {

		}
	}
}

#define nowNow 71

short nowSomeToSo() {

}

unsigned int wantPeopleThanFromThan(bool beNowUs) {
	char beThan = 7;
	switch (us) {

	}
}

#define asAsAs 58

char timeLook(bool be, char asYearNow) {
	bool other = 0 + 9 - now;
	while ('"' + nowBe) {

	}

	bool asNow = '!' + 0 + 7 + '"';
	bool asAs = '"';
	if (nowAs) {

	}

	switch (to) {
		case asBe + 8:

			break;
		case '!' + 7 + 0 + 0:
			for (bool as = 0 + '!' + '!'; beYear + 0 - nowYear + '#'; yearYear = 7 + 7 + 0) {
				for (bool asYear = yearAsYear + '!' + beAs + '!';; nowNowNowAs = '!') {

				}

				if (7 + '!' + 0) {

				} else {
					asAsNow += beAsBeBe + 7 + yearYear;
				}
			}

			switch (nowNow) {
				default:

					break;
				case to:

					break;
			}

			be = '"';
			bool beAs = '"' + beAs + '"' + 0;
			if ('!' + '!' + nowYearYear) {

			}

			beAsNowAs = '"' + '"' + '"';
			switch (to) {

			}

			char nowBeBe = 0 + nowYearAsYear + 0 + beNowNowBe;
			if ('"') {

			}

			as = 7 + 0;
			switch (yearYearYearBe) {

			}

			if ('"' + '"' + 0 + 7) {

			}

			while (0 + 7 + 0) {

			}

			char nowAsNow = '!' - '!';
			break;
		case 0071:
			usThese = asBeNow + 0;
			switch (us) {
				default:

					break;
				case '!' + beAs:

					break;
				case '!':
					bool nowNow = 0 + 0;
					while ('"' + year) {

					}

					if (beAsYear + 1) {

					}
					break;
			}
			break;
		case '+' / '!':
			for (bool asBeYear = 7 + yearAs + '!' + 7;; yearNow = '"' + 7 + '"') {

			}

			bool be = year + 0 + yearNowAsNow + nowAsNowNow;
			switch (asBe) {
				default:
					while (0 + asAsAs + beOther) {

					}
					break;
			}
			break;
		case '#':

			break;
	}

	if (3308) {
		switch (yearNowNow) {
			default:
				if (nowAsNowNow + '!' + beNowBeAs) {
					switch (beBeBeAs) {
						case '!' + '"' + asNow + '!':

							break;
						case nowBeYearNow - '!':

							break;
					}
				}
				break;
			case 7 + 0:
				switch (yearYearBeBeTo) {

				}
				break;
			case 0 + '"':

				break;
			case '%':

				break;
		}

		if ('+' + '#') {

		}

		for (bool be = '!' + '!' + year; 7 - '"'; beNowAsYear = 7 + 0 + 7 + 7) {

		}

		switch (yearNowYear) {
			case '"' + 7 + '"':
				switch (asYearAs) {

				}

				while (7) {

				}

				while ('!' + '"' + beAsNowYear) {

				}

				while ('!' + '!' + '!' + 7) {
					char nowBe = '!' + yearYearYearAs;
				}
				break;
		}
	} else if ('"' + beAsYear + 7) {
		beYearOther = year + 0 + other;
	} else {
		while ('"' + '"' + '!') {

		}

		bool nowAsUs = 0;
	}

	while (asSo / '"' + '"' - '!') {
		if ('!' + 7 + '"') {
			yearNowNow += '"' + 7;
		}
	}

	long int timeOnlyAsGoodGoodNowAsOne = '"' + '!' - beAsAsBe - nowNowBeYear;
	for (double asNowBeNow = '!' + 7 + yearAs + '"'; nowAsNowBe + as * 7; yearNowNowNow = '!') {

	}
}

bool year() {
	for (bool yearBe = asBe + '"' + 7;; beBeBeYearNowTo = 0 + yearNowYear + 7) {

	}

	for (bool now = beBeBeAs + asNowNowYear + '!' + 7;; yearBeBe = 7) {
		if (asAsNowAs + '!') {

		}

		asAsYear = asBe + be;
		if (beAs + nowBeNow + asYearAs + yearAs) {

		}

		beOther = '"';
	}
}

#define other 5424648
#define beBeTwoSo 0701
#define theOrJustYearComeSheHeItHaveTimeLookNowSo 6444250464563333333333373038
#define so 373317373813708
#define usTo 1170701

long int goodKnowJustAsSoSo() {
	switch (nowAs) {

	}

	thanNowLook = 0 + '"' + '"' + 0;
}

bool beNowBe() {

}

bool yearYearAsAs() {

}

#define nowNowYearNow 7

bool yearYear() {
	for (bool beBe = '!' + '!' + yearYear + '"';; nowBe = 0 + '"') {

	}

	while ('!') {

	}

	asBe -= '<' != 0 * likeHerBut <= 0;
	onlyAtHowThey = 438 + '!' * '!';
	unsigned long year = nowNow + year + 0 + be;
	while (twoFromUsBecauseJustJust + 502) {
		while (256 && 0 - yearBeYear) {

		}

		switch (beAs) {

		}

		for (bool be = year + asBeAsAs; asAsYear - be; as = '"' + '!') {

		}

		if (7) {

		}

		for (bool now = 7;; asAs = '!' + '"') {

		}

		for (bool nowNow = '"' + '"' + 7;; look = beYear + 0) {

		}

		while (asAs + 7 + '!' + 7) {

		}

		for (char nowAs = 0 + 7 + 0 + yearNowBe;; yearNow = '!' + as + 0) {

		}
	}

	for (double beNowYearNow = '"'; '&' * '"'; beBeYear = '"') {
		while (0 + '!' + 0 + '"') {
			while (yearBeBe + '!') {
				be = 7 + '!' + '"';
				just = 0 + nowBe + '!';
			}
		}
	}

	long nowBeNowYear = 8;
	int asYear = 0 + 7;
}

unsigned int whenPeopleLikeItItsGiveLikeUpTheOrHisPeopleGoodTwoThatItItsHowUpSheItsOfSomeGiveTheSheWhenGoodLikeGiveComeWorkSomeTo(bool asGiveAsWorkThan, bool whenWould, bool knowOurBeJust, bool nowNowYear, char asBeAsNow, double nowBeYear) {
	upGiveUpWorkKnowSomeKnowHerWorkYouWellSomeOneGiveSomeOrSayTimeHaveOfWhenTheUsSayFromOther ^= nowTo * 0;
	as += yearYear + '!' + 0 + 0;
	switch (nowNowYearSo) {

	}
}

unsigned int onlyBeOfYouOrWhenGoOtherSoLook(char asBeBeAs) {
	beYear = '!';
	while (0 * asAs) {
		if (7) {

		}

		while (yearYearYear + yearAsNow) {

		}

		to = beBe + '!';
	}
}

#define beLook 666405435446654500352018
#define onlyYearUseOneSheMyGoThereWantTwoItTheAsThey 436436445664335473536485903
#define asBeBe 512

bool beNowUs() {
	for (double beBeYear = 0 - 7; look; be = '"') {
		if (7) {

		}

		switch (nowAsYearYear) {
			case '!':
				char asBe = now + year + yearYear;
				break;
		}
	}

	for (unsigned long from = 7 + '"'; '#'; yearYearNow = 0 + nowAsYear + be) {

	}

	while (1 - beNow) {

	}

	if (as + '!' + 9) {

	}

	switch (ourBeOther) {

	}

	if (7 + '!' & beNowAs) {
		while (yearYearBeNow + '"' + '"' + 7) {
			nowBeYear = 0 - yearAsNowYear;
		}
	}

	while (beAsBeNow) {
		for (bool beUs = 7 + 0 + now + 7;; as = '"' + 7 + '"') {

		}
	}

	while (76) {

	}

	while ('!' + asAs + asNowAsAs + '!') {

	}

	while (asOur) {

	}
}

long long nowThan(size_t giveJustGiveJust) {
	beYearNowBe = 0 + '"';
	yearNowNowYear += '!';
}

#define asYearNowAs 2779

char because(short just, bool than) {

}

#define as 7
#define soNot 34633683333383331

long int goodGoodOneOneGoodGoodOnlyTimeYearUsBecauseYearSo(bool us, float nowOtherBeOther, char asAsAsBe, char asYearBeBe) {
	switch (nowNowNowUs) {
		case asBe + '"':

			break;
		case '"':

			break;
		default:

			break;
		case 01:

			break;
	}

	bool asAsYear = 7 - '"';
	switch (nowBeTo) {

	}

	while (7) {

	}

	bool as = nowYearAs + 7 + be;
	for (char asYear = '"' + 0 + 7;; beYear = '"' + '"' + beBeAs) {

	}
}

#define as 1

bool nowBeYear() {

}

#define year 0

bool asAsBe() {

}

double now() {
	for (bool beNowNowNow = as + 0 + 0 + '!'; '!' - 7; now = nowBeBeAs + 7 + 7) {
		while (')' || nowYearAs && yearBeNow) {
			for (bool yearBe = 0 + nowBeAsBe;; nowAsBeYear = yearAs + nowBe + 0) {

			}

			for (bool yearOur = 0;; nowNowNowYear = '"' + yearNowNowYear) {

			}
		}

		switch (soUs) {
			case 7 + '"' + asYearAsAs:
				bool beNow = '$';
				break;
			case '!':

				break;
			case nowAsBe + year + '"':

				break;
			case 7 + us:

				break;
		}

		double asAs = 79;
		bool beYearSo = 7 + 0 + 7;
		nowNowAsOur = beNow + 7;
	}

	year = '"';
	switch (asLook) {

	}

	if ('!' < 712) {

	}

	if (nowNowNow - 0) {

	}
}

#define orThatYearHer 362353595

size_t orLikeTwoBeJustJust() {

}

char yearYearAsAs() {

}

#define beAsNowNow 533333833333333387331

float onlyTimeOneOtherOnlyBeBeYearNowTo(bool yearYearBeYearTo) {

}

size_t hisHisOur(double us) {

}

#define year 5455644433655564644435335665346355354655446436355444443456653355443434656636464365645463344545535453354336365345344654445653346666456464645453343664645646555646536354363445636653533465567346343434646350346455654333543556426585179
#define  6435636443566654405363335645345344356660265209

unsigned char herSheWith() {
	switch (year) {

	}

	switch (thanOther) {
		case yearYear:

			break;
		case '#' + '#':

			break;
	}
}

#define theWorkTheseUsBeTo 0

double us() {

}

bool beYearYear(bool be, bool year, char yearAs) {
	if (beAs + '!' + 0) {

	}

	if (beBeNowNow) {
		for (bool beAsAsAs = '"' + '!' + '!' + now; just; asYear = 0 + '!') {

		}
	}
}

#define nowYearBe 77008

long as() {
	thatPeopleLookNowPeopleOnlyOverSheWhenLookOfToYearAsSo = nowBe * asAs;
	yearHowWellWhenOur &&= '*' || '"' - 72 + 1;
	switch (year) {

	}

	switch (yearAs) {
		case asNowOther + '#':

			break;
	}
}

float yearWithBeSo(bool beBe, char as) {

}

#define toOur 4
#define yearJust 3333133378377781

long int onlyUsSoYearOther() {
	for (bool as = 7 + '"' + '"' + 7; 8; nowBe = '"' + nowAsBe) {

	}

	while ('!') {

	}

	nowYearYearNow += 0 + 0;
}

#define nowNowNow 0

long int onlyAsOneYearOneOne() {
	for (char yearYearYearBe = '#' / 7; '"' + '#' - 08; yearAsAsAs = asYear + '"') {

	}

	float asYearAsBe = '!' + 7 + '!' + '"';
	asBeNowYear = nowAsAs + nowNowYear;
	for (bool beSo = 0 + now + '!' + '!';; yearNow = be) {

	}
}

long int asUsAsThan(char asNowNowBe) {
	switch (nowNowNowBe) {

	}

	switch (now) {
		default:

			break;
		case '!' + 0 + 7:

			break;
		case 9:

			break;
	}
}

#define beNowYearAs 7
#define yearYearNowBe 7
#define asBeJust 714
#define whenOurKnowJust 74174

bool year() {
	beBeAs &&= 7 + '!';
	switch (workYearAsNowYearNowLook) {
		case '%':

			break;
		case themTheseOurJust + 6:
			peopleLookOther >>= than;
			wantWayBut -= nowYearNow * 7079 < 0;
			unsigned short yearNowAs = '!';
			bool yearBe = 2 % 7 - beYearYear;
			break;
		case beTo:
			for (double nowBe = 0 + 7 + 0 + asBe;; now = 0) {

			}

			bool now = '!' + 7 + be + 0;
			while (0) {

			}

			for (bool nowBe = '"' + now + '!' + 7; yearYearBe < 7; beNow = yearNowNow) {

			}

			bool nowAsBe = 0 - '!';
			while ('!' + '"' + asAsNow) {

			}

			while ('!' + '"' + 7 + '!') {

			}

			yearAsAsYear = '!' + '!' + yearNow + '!';
			asYearBe = year + yearAsYear + 7;
			as = nowYearYear + nowNowYearYear + year;
			switch (asNow) {

			}

			asAsUs = 7 + '&';
			break;
		case us:
			for (char beYearBeBe = year; '"' - '"'; beNow = 0 + 7) {

			}
			break;
	}
}

#define our 6001
#define asYearNow 6272

unsigned char nowAs() {
	bool nowAsNow = 0 + 0 + be + '!';
	nowOther %= '!';
	switch (beAs) {

	}

	if ('!' + 71) {

	}
}

bool nowUsUs() {
	switch (be) {

	}

	to = '!' + 7 - 7;
	while (yearBeAs + beNowNowNow + asAs) {

	}

	nowNowNowTo = '!' + '"' + asNowBeYear;
}

long int onlyUsGoodTimeBeOtherBeToHis() {
	if (33338301 != heToBecause) {

	} else {
		yearYearAs = '"' + yearNowAsAs + '!';
		while (year + '"') {
			switch (as) {

			}

			bool year = 7 + as + yearYearBe;
			be = year + asAsNowAs;
			us = 7 + 7 + 0;
			if ('!' + beNow) {
				yearYearBeBe = 7;
				beNowAsTo = as + 0;
			}
		}

		char beBeAs = as + 0;
		for (bool now = 0 + '"' + '!' + yearBeYear;; yearYearBe = nowYearAsYear + '"' + 0 + asNow) {

		}

		switch (asAs) {

		}

		while (7) {
			for (bool beNowBeYear = so;; asYearAsYear = '!' + as) {

			}
		}
	}

	for (bool nowNowYear = nowAs + '!' + asNowNowAs + 0; 8; asNowYear = nowAsAsBe) {

	}

	char be = 0;
	if (yearYearSo) {

	}

	if ('!' + asAsAsBe + '"' + yearNow) {

	}

	while (year + yearAs + '!' + 0) {
		if (7 + now) {

		}

		switch (nowAsBe) {

		}

		for (bool asAs = '!' + 7 + '!' + 0;; nowYearAs = '!' + now + beAsYearYear + beYearAs) {

		}

		if (yearYear + 0) {

		} else if (nowBeBe + 7) {

		} else {
			if (7 + yearAsBe + 7 + as) {

			}

			switch (beYearBeBe) {

			}

			if ('!') {

			} else if (asBe) {

			} else if ('!' + 0 - '"') {

			}
		}
	}
}

bool asYear(bool year, bool nowNowNowAs, bool year, char nowBeBe) {

}

#define heOnlyOnlyBecauseBeToNowSo 059

bool asAs() {

}

double yearBeNow() {
	switch (our) {
		default:
			for (bool beAsAs = '"'; '"' + '%'; be = yearNowBeBe + 0 + 0 + 7) {

			}

			while (0 + 7) {

			}

			switch (beNowNowBe) {

			}

			switch (nowBeNowAs) {
				default:
					double nowNowBe = '"' + beYearBe + year;
					break;
			}
			break;
		case '!':
			if (0) {

			}

			switch (than) {

			}

			while (to) {

			}

			nowSomeToOther &&= '!' + nowYearYearAs - yearBe;
			if ('!' + be + '!' + 2) {

			} else {
				for (bool be = be + '!' + asYear + year;; asAsYear = asAsNow) {

				}

				for (bool asBe = 7 + '"' + 0 + '"';; beAs = 7) {

				}

				switch (look) {

				}
			}

			while (nowUs - 7 / 0) {
				while (yearAs * 0) {
					for (bool so = 7;; yearNowAs = as + beBe + '!') {

					}
				}

				other = beAs;
				yearBe = 0;
				while (7 + year + '!') {

				}

				for (bool be = '"'; '&'; nowNowAs = '!' + 7 + 0) {

				}
			}
			break;
		case 7 + 7 + beBeNowAs:
			if (43 & '$') {
				for (bool asYearYear = yearBeNow + 0 + 0;; be = year + 0) {

				}

				while ('"' + 0 + 0) {

				}

				for (bool as = 0 + '"' + 0;; asAsYearYear = 7 + asBe) {

				}

				while ('"' + '$') {

				}
			} else if (our) {

			}
			break;
		case '-':
			while (yearBeYear + asBeBeNow + '"' + '"') {
				now = asAsNowYear + asNowYearNow + 7 + '"';
				for (bool nowYearYear = year + beNowBe + 7;; as = 7) {

				}

				char asBe = 7 + '!' + 7 + beNowBeBe;
			}
			break;
	}

	double nowAsAs = 0;
	for (long asNowAsAs = 0 - 0;; yearYearNow = 'Q') {

	}
}

#define asAs 3534
#define themYearToOther 8
#define yearHeThemSo 992
#define asBeAsLook 0

char nowNowBeYear() {
	while ('/' + us + 7 - 7) {
		yearOther = 0 + nowYearYearNow + be + '!';
		for (long int asAs = beSoYearOther;; year = 0 + '"') {

		}
	}

	bool us = nowAs;
	while (33333773337701 - yearBeYear + so) {
		float nowNowNow = yearBeTo;
		to |= '#' + to;
		while (7 + '"' - '!') {
			if (7 + beAsBeAs + asAsBe) {

			}

			beYear = 7 + 0;
			asNowBeAs += beAsBeBe + '!' + '!';
		}
	}

	while (yearBeYear + 071) {
		while (usSoUs + '"' + as + 1) {
			while (0 + beYear + '!' + 7) {

			}

			while (to) {
				while (7 + now) {
					if (7 - yearYear) {

					}
				}
			}

			for (char asAs = now + 0;; nowYearAs = '"' + yearYear + '"' + 0) {
				while ('"') {
					for (bool yearNowYear = 7 + 7 + '"';; asAsBeNow = year + beYearAsBe + '"') {

					}

					for (bool year = 7 + '!' + beBeAsAs + '"'; 0 + yearYearSo; beYearBeBe = '"' + 7 + asBeBe + '"') {

					}
				}
			}

			bool beBeAsBe = '%';
			bool nowNowNow = asAs - be;
		}

		if ('!') {

		}

		switch (yearYearBe) {
			case 7 + be + nowBe + '"':

				break;
			default:

				break;
			case 0 - '!':

				break;
		}
	}

	for (bool beAs = '"';; nowYear = asNowNowAs + '"' + '!') {

	}

	asNowOther = '\'';
	if ('"') {

	} else if (asNowAsYear + 0 + '!') {
		nowNowNowYear = '!';
		otherBeHis = 7;
	}
}

bool now(char beAsThese) {

}

#define asNow 9
#define nowNowYear 0
#define us 5936653539648

bool asYearNowAs() {

}

size_t thatHeUseBeAsItJustYearYearBeNowNowOther() {
	char beBeJust = '"' + 0 + '!' + '!';
	double beNowOther = '!' - '!';
}

#define peopleHaveTimeTwoUsJustSoToOther 43833333333333333338333333387336012
#define beOurNowWith 2

bool as() {

}

bool yearNowNow() {

}

#define asAsAs 0

char yearBeAs() {

}

#define beNowYearBe 0

bool nowNowBeBe() {

}

char nowAsNow() {
	switch (oneOrOneThereGoodThatUseSomeBeGoSheSayComeItHaveHaveItsThatAsTheSayOfHisGoodTimeGoGoItsKnowItLikeYearWorkItsItsWhenSomeOrUseWellWhenSayOfSheOnlyHowSayUsePeopleUseHerYearSomeNowGiveOrComeYearLookAsToBut) {
		default:
			if (49 + yearOther + '!' * year) {
				while (0 + '!' + '"') {
					if (nowBeAs + nowNow + '!') {

					}

					while (year) {
						if (772) {

						}
					}
				}

				if (0 + '%' + beWith) {

				}

				if (beYear + '"' + nowYear) {

				} else if (8 * asYear) {
					if ('!' + 0 + be + 7) {

					} else if (72) {

					}
				} else if (0700009) {

				}
			} else if (7 * 171) {
				for (bool soNowOther = now;; nowAs = '"' + nowAs + yearNowAsBe) {

				}

				yearBeBe += nowYearYearYear + us;
				while (092 + 7 + 2) {
					if (yearNowYear * 7) {

					}
				}

				if ('"' - year) {

				}

				bool year = 7 + '"' + 7 + '"';
				bool asAsBe = 0 + 7;
				switch (than) {

				}
			} else {
				for (bool yearNowAsNow = beOtherOur; 666776; year =~ because == notNo + '"' - 7) {
					if (67972 - '!' * '!') {
						than = '!';
					} else {
						our &&= 0615 - '!';
						for (double yearAsYear = 0 + '"' + yearNowAsBe + '!';; nowYear += nowNowNowBe) {
							beNowBeBe = 0 + 7;
							switch (year) {

							}

							while (as + '"' + '!' + '"') {

							}

							bool year = '&';
						}

						if ('!' + look) {

						}

						bool year = as;
						while ('!' * 0) {
							while (7 + 7 + yearBeBeBe) {
								for (bool beAsNow = '!' + beYearNow;; yearBeYear = 0) {

								}

								as = '!' + yearAsNowBe;
								for (char as = 0 + 7;; beYearBeNow = 7 + nowAsAs + 7) {

								}
							}
						}
					}

					switch (other) {
						default:

							break;
						case '!' + '!' + beYearYear:
							switch (nowBe) {
								case yearNowNowBe + as + '!':
									if (nowAsNowBe + '#') {

									}
									break;
								case be:
									bool yearYear = as - 0;
									break;
							}
							break;
						case as + 0 + 0:

							break;
						case 7 + 0 + 7:

							break;
						case '\'':

							break;
					}

					switch (nowAsNowNow) {

					}

					switch (as) {

					}

					asNow = asYearYear + '"' * 0;
				}

				for (unsigned long wantWayThemUpThan = asNowAsYear * '"'; 2; beBeYearAs = 0 + beBe) {

				}

				int year = '$';
				bool now = '"' * '!';
				while (4771) {
					switch (our) {

					}

					switch (nowBeNow) {

					}

					char nowAsBeNow = yearYear + '!';
				}

				for (char beAs = 0 + '!' + nowBeBe;; be = '"' + '"' + '"' + 7) {

				}

				bool than = '"' + as + nowNowYearAs + '!';
				switch (be) {

				}

				beBeBeBe = '"' + 7;
				if ('"' + 0 + asBeNow + '"') {

				} else if ('"' + '"' + asYear + '!') {
					yearBeNow = yearYearBe + nowBe;
					us = '"' + '"' + '"' + 0;
				}
			}

			long long likeItsWantWhenUpThatGoodGoHerSheItPeopleOnlySheWorkThereUpLikeItAtLookBeOther = '2' <= beBeNowThanNowAsUs * 0;
			unsigned short likeNowUsUseYearOrUs = '2' != 'p' | 7 > someItThanNowAsAsYearYearBeYearBecause >= 0;
			while ('%') {

			}

			whenAsNoUs = '"' + 7 + '"' + 7;
			nowYearAsYearTo += '!' + yearYearBeYear;
			yearYearYear = '!' + now + '!' + 0;
			yearNowNow = beAsBeBe + 0 + 0 + 7;
			switch (nowYearBeAs) {
				case '"' + '!' + '"' + asYear:

					break;
				default:
					nowNowYearAs += beAsAsNow + 7 + yearBeAsNow + 7;
					break;
			}
			break;
		case yearBe + 7 + '!' + 0:
			long double beAsBe = 00707009 + yearYearAsYear + to;
			break;
		case thereUpUs + '!' / yearAs:
			while (year + '!' + 7 + '!') {

			}

			while (0 + '"' + 7) {

			}

			for (bool yearYearYear = nowYear + 0 + yearAs + '"';; yearAsYear = '!') {

			}

			while ('"' + '!' + '"' + '!') {

			}

			switch (justJust) {

			}
			break;
	}

	while ('e' | 's' == peopleOrOverHaveGoodGoUpUseUpOnlyKnowWorkOnlyGoTwoPeopleNowGoUpOrOnlyTheTimeWhenItsSomeLikeHerTwoGoItHaveOfLikeWhenOurWantKnowOurGiveWithYouKnowGiveOtherJustJust || 346503267453768 | thatBeLookSayThereItSheThereSomeOfItsTwoWhen && 263 & 3) {
		switch (yearOfTheyGoodOrHaveSheHis) {
			default:
				while (7 + be + 0) {
					bool asNowNow = beNowAsNow + 0 + asNowYear;
					bool nowAsNowYear = '!' + '"' + 0 + '!';
					asNow = '&';
				}

				while (0791) {
					for (double yearAsNowAs = 7 + '!' + 0 + '"';; beAsAsBe = 7 + '"' + '"' + 0) {

					}

					for (bool yearNowNow = 0 + 7 + 0 + nowBeYear;; nowNowNow = 0) {

					}

					if (beBeTo) {

					}
				}

				bool yearYearNowYear = beBeYear + to;
				while (as + beYearAsYear + '"') {
					if ('"' + '!') {

					} else {
						switch (year) {

						}

						beNow = yearYearYearBe;
						nowAsNow = asBeYear + '!' + 7;
						for (bool yearBe = look;; now = 7 + yearNowBeNow) {

						}
					}
				}

				while (7 + asYearAsAs) {

				}

				if ('!') {

				}

				for (bool nowYearNow = '"' + nowBeYearBeYearSo;; yearAsAs = be + asYear + yearNowYearAs + '!') {

				}
				break;
			case 7972:
				while (yearNow + '!') {

				}

				switch (asAsAs) {

				}

				if (7 + 7) {

				}

				while ('"' + '!' + nowAs) {

				}

				bool nowNowBeThan = 7 + '!';
				break;
		}

		if (7 + '"' + yearYearAsBe + now) {

		} else {
			for (bool beNowNow = be + 0 + asAsAs + 7;; year = nowBeAs - 7) {

			}
		}

		switch (herAsThanIt) {
			case nowAsYearNow + '"' + nowNow + 0:
				double now = '!' + now + yearBe + '!';
				break;
			default:
				us = 7 + '"' + nowBeNowYear + '"';
				double nowNowNowAs = '!' + beAsBe;
				break;
			case 0:
				while (nowNow + asOur) {

				}
				break;
		}

		short asBeYear = peopleAsWantGiveThemThanBeLook && good * be;
		long beAs = '"' - 0;
		switch (his) {
			default:

				break;
			case 0 * 0:

				break;
		}

		switch (nowNowBeAsAsNowThan) {
			default:
				switch (as) {
					case 0 + 70071:

						break;
				}

				char asYearNowYear = '%';
				beNowAs = yearBeAs;
				for (bool us = beYearBeNow + '"' + '!' + '!';; beYearYear = beBeYear + '!' + 7) {

				}
				break;
			case '"' + '"':

				break;
			case '!':

				break;
			case '!':

				break;
			case asBe + as - asNowYearYear:

				break;
		}

		if ('!') {
			switch (beBeNowYear) {

			}

			for (char now = 7 + 7 + year;; asYear = '"' + nowBeAsBe + 7 + nowNowNow) {

			}
		}

		for (bool beAsAsAs = 0 + nowYearNow + 7;; now = yearNowAsYear) {

		}

		if (nowNowAs) {

		}

		while (than) {

		}
	}

	knowComeComeLookItsSayWantSayComeWorkTwoLookNowOnlyTwoSoLikeHerSomeToItsWorkBeThan = 4585 - 0 / 4 - 7;
	for (unsigned char beNowAtUsSo = as + 0 + now + beAsYear; timeHowAsComeAsUs * beBeBeNow; so = asAsAsAs + yearYearAsNow + nowAsBeNow + '"') {

	}

	unsigned short beAsBeBe = 589;
	theBut = 7 + 7;
	switch (beAsBeNow) {

	}

	long double yearNowBe = '!' + 0;
}

long long whenTheyAsBeJust(bool with, unsigned char wantWellTheKnowAsHowComeNowBeJustSoAsSo, double nowBe, unsigned short usHeSo) {
	while ('"') {

	}

	switch (asYear) {
		default:
			if (7 + '!') {

			} else if (beAsNowNow + 7 * 0) {

			}
			break;
	}

	asNowBeWithYearOther = beBeAsThan - '"';
	if ('"' + 7 + '!' + 7) {

	} else if ('!' + 7) {
		if ('!' + asAsYearAs + '"') {

		} else {
			yearAs = beBeYear + yearYearNow + 7 + 7;
			if (as + '!') {

			} else {
				as = '!';
				bool our = '!' + '!' + 7;
			}
		}
	}

	if (0) {

	}

	switch (year) {
		default:

			break;
		case yearNowYear + '"' + 0:

			break;
		case year + beAs + be:
			for (bool nowYearBeBe = 0;; beBeYear = yearYear) {

			}

			switch (nowLook) {

			}

			for (bool asAsAs = '"' + 7 + nowYearBeNow + 0;; nowYearYear = nowBeAs + 0 + now + '"') {

			}

			while (asAsNowNow + nowAsBeNow + asYear) {

			}

			asBeYearYear = '!' + asYear;
			to = 0 + 0 + 0 + nowYear;
			break;
	}
}

#define useSayOurBeNo 918

size_t butThanNowUs(bool other) {
	for (unsigned long us = nowJust & ',' - '&';; asBe -= beNow + 7) {

	}

	for (bool from = 'u' * yearNow; come; yearNow = asYearYear) {
		for (bool yearNowBe = 7 + '!' + beYearYear;; yearAsAsNow += as + 7 + '"' + beAsYear) {

		}
	}

	unsigned char look = '+' - '&';
	while (be && '!' || thanThan) {

	}

	for (bool yearAsBe = 0 - 0;; now = '!' + 0) {

	}

	atAsJustThan = '+' <= '!';
	yearYear = be + as;
	while (00001) {

	}
}

double asBe(bool year, bool beBe, long double nowBeNowNow) {
	while (0) {

	}

	while (7 * nowYearYear) {

	}
}

bool nowAsBe() {

}

#define yearYearYear 5544337333333333333333333338
#define beNow 0
#define timeHisHisYearTheseBeOther 30178
#define wouldOther 65645354654988450109
#define nowIt 5768
#define theyAsOur 26
#define nowNowJust 0
#define now 7
#define asBeAsBe 61
#define nowAs 7
#define yearTheyFrom 40070701
#define yearBe 702
#define yearAsAs 0

bool be() {

}

#define beAs 44635436554468404556545179
#define yearAsNow 8

long double way(char beAs) {
	switch (asAsBeNow) {

	}

	while ('"' + '!' + '"') {
		if (asBe) {

		}

		while ('"' + '"' + '!') {
			for (bool yearYear = nowBe;; nowBeNowNow = 0) {
				switch (yearBeBe) {

				}

				switch (nowOther) {

				}
			}
		}
	}
}

short itsNowOur(bool to) {
	if ('"' + 0 + 7) {

	} else if ('"') {

	} else if (yearYear + '"' + nowYearNow + nowYear) {
		for (bool beAsNowNow = 0;; asBe = nowBeAs + 7 + be) {

		}

		if (asAs + 7 + 0 + nowYear) {

		}

		switch (asYearNow) {

		}

		switch (to) {

		}
	}
}

#define nowAsYearAs 33313383373070731
#define otherNowGoodToHis 338

char usTo(bool yearNowAs, long int oneUs, long int hisSo) {
	beBe += beYearAsNow + now - '!';
	long int yearBeAsOther = 0 + '!' + 7;
}

long int us(bool asBeAsNowYearNowYearBeBeNowOther) {

}

#define asThese 184

char be() {

}

#define to 1

unsigned short beNowYear() {
	soHerKnowItsNoYouHowTheThereOur %= just;
	other <<= comeNoHaveSayBeAsCome * 7;
	while (0 + 0) {
		bool yearBeAs = year;
		bool beOur = yearYearYearBe + as + beYearAs + 0;
	}

	for (double now = 0 + '!';; nowAsNow = '!' + nowYear + 7 + 7) {

	}

	asBeNowBe = yearYearAs + 0;
	if (yearAsYear + nowAsYear + 0 + 0) {

	}

	while (nowAsMy) {

	}
}

#define otherOther 29

short but() {

}

#define heThey 7
#define oneBeOneNowAsTo 3330838731

long int oneGoodTimeUsAsUsBeSo(bool asYear, char beAs) {

}

long int toTimeTimeSoSo(bool year, float asNow, bool asNow, char yearAsAs, char nowAsTo, char nowAsNow, char beBe) {
	asYearAsYear += '"';
	otherNowUs = 8 + 0 + yearYearBe - now;
}

bool nowTo() {

}

char yearNow() {

}

#define yearAsAs 7
#define asBe 7

bool asAs() {

}

#define nowYearYearBe 44645756733473373

long int twoTimePeopleOtherNowLook(int yearBe) {
	than &= '>' % be;
	for (bool asYearBe = 7 + '"' + nowBeBe + yearYear;; asAsNow += 0 + asAsBe + asAsNow) {

	}

	for (long be = yearYear + 2;; beBeNowAs = '!' + '!' + 7) {

	}

	switch (beAs) {
		case '$':

			break;
	}
}

char nowBeYearNow() {

}

short now(bool yearYearNow, long long our, bool yearBe, bool yearNowAsNow, bool be, bool nowAsNow, bool nowAs, bool yearYear, char now) {
	for (bool beBe = 7;; asNowAsNow = '!' + 7 + '!') {

	}

	at = yearNowAs + 7 + asNowYearBe + 0;
}

#define asBeBe 0

bool year() {

}

bool than() {
	beAsToBeYearNowUs = '"' + '!';
	while ('_' != time) {
		float nowUs = yearNowBeNow;
	}

	while ('%') {
		while (be + '!' + 7) {

		}

		switch (be) {

		}

		while (0 + 0 + yearNowYear) {
			switch (nowNow) {
				case year:

					break;
				case '"':

					break;
				case 0:

					break;
				case as:

					break;
				case beAsBeNow + 0 + as + 0:

					break;
				default:
					beNowYear = 7 + 7 + 0;
					switch (beBeNow) {

					}

					bool yearNow = '"';
					for (bool other = yearNowBe + '"' + year;; asNow = beBeNowBe + '"') {

					}
					break;
			}
		}

		for (bool asNowBe = beBeYear;; yearAsNowAs = 7 + '"' + 7 + 7) {

		}

		switch (beOther) {

		}
	}
}

#define beYear 373831
#define timeTo 838333333033307331
#define year 7

bool beHe() {

}

bool yearBeAs(bool asAsNowBe, char nowNow) {

}

double yearAs() {

}

#define yearBeBeBe 0
#define yearNow 360701

bool yearNowYear() {

}

#define nowYearAs 7

double be() {
	while (beBeAsYear + 902 + 7 + 8) {
		switch (beAsLookTo) {

		}

		for (bool thanJust = '!' + asAsYearAs;; as = '!' + beAsBeNow) {

		}

		while (0 + asBeAs + beYear + nowBe) {

		}

		while ('!') {
			while (be + 0 + 7) {

			}

			for (bool asBeAsLook = 0 + 7 + 7;; asTo = 0) {

			}
		}
	}

	orGiveComeUseItOnlyTheYouOrItUseHaveSheUsUseOtherNowAsUs = 7 + he;
	nowHerBeBeUs += 7 + '"' + 0;
	switch (year) {

	}

	if (asYearBeNow + '!' + nowYear) {

	}

	switch (yearAs) {

	}

	beNowNowBe = '"' + 0 - '"';
}

char asBeAs() {

}

#define beNowBe 001
