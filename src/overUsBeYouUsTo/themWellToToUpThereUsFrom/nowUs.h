#include <EUEU00U22U0/0U26///6.h>

#define hisBeTimeBecause 8873031
#define asNowNowYear 563555336456333641

unsigned short onlyTheOtherBeAsHis() {

}

short sayLookSo(bool beBeNowNow, bool be, bool beHisOur, bool beAsBeAs, char be) {
	asNowNow -= '"' + 7;
	for (double asBe = '"' + 7;; yearNowAsYear = asBe + 0 + '"') {

	}
}

short nowNowYearYearUs(char asNowYearYear) {
	goodAsOtherSoToBecause = '"' + 7 + '"' + beBeYearAs;
	for (bool nowBe = 7 + '"';; now += yearAsYearNow + 7 + nowAs) {

	}
}

long int onlySoOnlyBecauseTimeBecauseSo(long int now) {
	for (bool as = '!' + 7 + yearAsAs + as;; nowYearBeYear += beYear + '"' + 7) {

	}

	while ('O') {
		switch (beAsBeBe) {

		}

		char usOther = nowYearNowYear + '!' + 7;
	}

	for (char asBeNowAs = us; yearNowNowBe - 7; nowYearUs = beAsNowAs + '!') {

	}

	long int these = 7 + '!';
	yearTo = yearYear + 0;
	if ('\'') {
		if ('!' + '"' + 0) {

		} else if ('"') {

		} else {
			for (bool as = '!' + '"' + 7 + '"';; asBeTo = nowYear + '!' + 0 + '!') {

			}
		}

		for (bool now = '!' + 0;; nowAs = '"' + beNowYear + as) {

		}

		if ('!' + 7) {

		}

		nowAs = 0 + nowBeYear;
		while ('%') {

		}
	} else if ('"' + yearSo) {

	}

	while (beBe + 7 + be + year) {

	}

	bool beBeAsNow = 7 + beYearYear + '!';
	if (yearNow + 0 + year + '!') {

	}

	for (float year = '!';; beNow = 7 + '"' + 0) {

	}
}

bool as(bool yearAsAsAs, float asNowBeNow) {

}

char asSo() {
	while ('!' + '!' + asBeYearAs) {

	}

	for (bool as = 7 + '"' + 7;; yearYearAsAs = 0 + beAsNowBe + '"' + 7) {

	}

	if (0 + '"' + '!' + now) {

	} else {
		while (0 + 7 + 0) {
			switch (asYearTo) {

			}
		}
	}
}

bool beAsBeYear() {
	if (year + nowNow - 0) {

	}

	bool as = asNowBeAs + 0;
	be = asAsYear;
	switch (asBeYear) {

	}

	bool beYearAsNow = '"' + beYearAsBe + '"';
	if ('"' + '!' + '"' + asYearYearNow) {

	}

	char beBe = 7 + asYear + 7;
}

bool all() {
	yearNowYearNow = 7 + 0 + yearYear;
	other = 540808;
	bool beAs = asAsAs;
	switch (beBe) {
		default:
			if ('$') {

			}
			break;
	}

	asYearAs = 9;
	char nowBe = 0 + yearYear;
}

#define nowYearBeOur 34664609

long long goWellWhenHerSayAtYearBeAsWithTwoNowWith(unsigned int goAsLook, bool our) {
	while (yearNowYearBe + 0 + '!' + 7) {
		if ('!' + 0 + '!' + 7) {

		}

		beNowYearYear = '"';
		for (bool be = '"';; nowNow = '!' + asAsAs + 0 + 7) {

		}

		for (bool nowNowAsBe = nowNow + '!';; now = as + 7) {

		}

		while (7) {

		}

		while (year) {
			while (nowAsNowAs - '"') {

			}
		}
	}

	while (7) {
		bool year = be + 7 + 7;
		bool us = 0 + '!' + yearYearNow;
	}

	switch (to) {

	}

	if (nowAsYearYear + nowNowBe) {

	}

	for (bool asAsYearBe = '"' + '"';; nowNow = be + 7) {
		beSo = asBe + asBeBeYear + '"' + nowBeYear;
	}
}

#define asTo 0

bool beBeYearAsJust() {
	beBe = 78 - '!';
	if (1) {

	}

	switch (beOther) {

	}
}

long int timeUsOnlyNowGoodOnlyTheseTimeUs(char beYearUs, bool yearBeNowNow, bool nowAs, bool becauseTo, bool otherSo, bool asNow, bool nowTo) {
	if (theseUsOneUs != 3333370078) {

	} else if ('"' + beYearYear + beNow + yearBeNow) {
		bool these = beYearBeAs - 0;
	}

	bool beAsNowAs = '\'' + '%';
}

char yearOur(bool asBe, char yearYearYearNow) {
	bool beYearYearOther = '"';
}

#define be 5666304446364742406447702

unsigned int nowYear(bool beAs, bool yearBe, bool nowNowOur) {
	switch (nowBe) {
		default:

			break;
		case '"':

			break;
		case 7 + '!' + now:
			while (as + nowAsYear) {

			}

			nowNow -= 7 + asBeYear + asNow;
			break;
	}

	double nowAsBeBe = '"' + '"' + '!' + '!';
}

unsigned int giveYearYearNowNowBeJust(long double be) {
	while ('&') {
		for (bool asAs = nowBeBe + '!' + '"';; just = be + nowBeAs + 0) {

		}
	}

	while (of * 7) {
		for (bool nowAsNowYear = '!' + '!' + nowBe;; yearAs = '!' + '!' + '!') {

		}

		while ('"' + 0 + '"') {
			for (bool beNow = 7;; yearBeAsAs = 7) {

			}

			bool beBe = '%';
		}
	}

	beYearNow = '"' + '!' + '"';
	for (bool nowBeOur = 0 + '"' + nowNow + 0;; asYear = 0 + year + '"') {
		asYearAs = '!';
		now = '"' + '"';
		bool nowBeAs = 7;
		bool beNowYear = yearBe + '"' + 0 + 7;
		if (0 + '$') {

		}
	}

	while ('!' + '"') {
		asNowOur = '"' + 0 + 7;
	}
}

bool asYear() {

}

bool look(bool asYear, char asBeBeYear) {
	while ('"' + '!') {
		bool beBe = asAsAs + 0 + '"';
		switch (than) {

		}
	}
}

#define now 333333313333333333333333333011838

long int asBe() {
	while (beNowAsBe + nowNowNow) {

	}

	char be = '"' + asBeYearBe + nowAsBe;
}

#define be 0
#define nowAsNowBe 0
#define be 7

bool nowBe() {

}

#define us 0
#define asNowAs 09

bool nowYearBe(bool look) {
	while (702 != 09544093039) {
		double beYear = 02;
		for (bool yearBe = 0 + yearNowNowYear + '"' + '!'; beAsAsNow + our; as = nowYear) {

		}

		if ('!') {
			for (bool nowNowYearAs = 7 + year;; year = nowAsAs + '!' + beAsNow + 0) {

			}

			while (0 + 0 + 7) {
				if ('!') {
					asNowAs = now;
					while (be * beBeYear) {

					}
				}
			}
		}
	}

	if (7) {
		for (bool beAs = '!' + beYear + '!' + 0; asYearOurNot; yearYearOther += asYear + nowAsAsNow + 7) {

		}
	} else if ('&' + 009) {
		if (7 + '!' + '!' + 7) {
			switch (us) {

			}
		}

		for (bool beNow = '(';; yearBeAs = 0) {

		}
	}
}

long long sheGoodYearBeBeThey(float butSomeUs) {
	for (bool beAs = '!';; beBe = '!') {

	}

	while (asBe + asYearYearYear + '!') {

	}

	yearNow -= '"' + '!';
	while ('!' + '$') {

	}
}

float just(long all) {

}

#define nowNowBe 050001

bool us() {

}

#define asNowNowNow 5333333333333333333330331333337787008
#define beOther 35655566336756

long long ourYouNotSheAsNotYearThan() {
	for (bool nowNowAs = 0 + 7 + '"' + '"'; asAsNow - asBeYearBe; be = '!' + '"' + '"') {

	}

	for (double asAsAs = 7 * as;; as = 7) {

	}

	yearBe = be * be;
	bool be = 7 + '!' + '"' + '"';
	char nowBeYearNow = '"';
}

#define wellWantGiveJustLikeSoThese 53675
#define yearNow 0

float beNowYear() {

}

bool beYearNowAs(char nowNowAsBe) {

}

bool yearAsNow() {

}

#define year 7
#define now 31333733830338

char yearNow() {
	switch (beBeBe) {

	}

	switch (nowBeAs) {

	}

	year = beAs;
	while ('!') {

	}

	switch (other) {

	}
}

long int beYearAs(float yearNow, char beAsYear) {
	if (0 + '"' + '!' + '"') {

	}

	char yearAsYear = '!' - asNowBe;
	bool asAsBeNow = 78;
}

#define beNow 717001

bool beThese(bool good, char asNowAs) {
	switch (year) {

	}

	if (nowAsAs) {

	}

	yearAsAs += asAsYear + 7 + 7;
}

#define nowBeAsHe 331171

long int asNowBeNow(char beAsBeAsBeNowYearSo, bool asUs) {
	soToYearBeYearUs = 0 + 0 + 7;
	bool to = nowBeBe + nowAsAs + now - '!';
	for (bool beNowBe = '$' + 001 + nowOther; '&'; nowNowAsNow = now) {
		nowBeYearNow = 7708;
	}

	bool as = beNowYearAs + to;
	for (bool year = 7 + '!'; '$'; now = '"' + '!' + '!' + 0) {

	}

	while (7 + 7) {
		yearNowNow = yearYearAsBe;
		switch (nowBeBeNow) {

		}

		if (yearAsBeYear + yearAsYear + '"' + '"') {

		} else if ('"' - 7) {

		}
	}
}

bool lookUs() {
	bool yearBeNowAs = look;
	asAsBeYear += yearAsAs + 0;
}

#define be 7
#define nowSo 4593034
#define us 1
#define thereHowOrTheMyNoSoTo 6546543056353773027701

char otherYearUsToSo() {
	switch (oneGoodOneOnlyOnlyOnlyBeTimeYearBecauseSoOther) {
		default:

			break;
		case 0 + 7 + 0 + '!':
			while (beYearYear + 7 + as + '"') {
				for (bool nowBe = '"';; asYearNow = '"') {

				}

				while (yearYearBe + 7 + 0 + yearNow) {

				}

				if (1) {

				}
			}

			while ('"') {
				his = '!' + 7;
				for (bool nowNowBeYear = 0 - as;; nowBeNow = '!' + '"' + 7) {

				}
			}
			break;
		case 8:

			break;
	}

	for (char year = '!' + 7 + 7;; asNowAsNow = '#') {

	}

	while (asYearBeYear + '!' - nowYearYear) {
		switch (yearAsYear) {
			case yearBe - 8 + becauseTo - beBeBeAs:

				break;
		}

		otherToNowAsHis += '%';
		for (bool asYear = 7; asHe; yearHe = '"' + asYearAs) {

		}

		if (0 + 7 + 7 + '!') {

		} else {
			for (char beBeNow = beNowNow + '!' + 7;; be = '!' + 0 + '"' + asAsNowAs) {

			}
		}
	}

	switch (these) {
		default:
			hisSo = nowAs + '"';
			while (yearNowBeBe + be + '!') {
				bool beNow = '"' + asYear;
				if ('"') {

				}

				if ('%') {

				}
			}
			break;
		case asBe + '!' + 7:
			switch (nowAs) {

			}

			for (bool nowYearNowAs = 0 + 0; 1; beBeAs = now + 0 + '"' + 7) {

			}
			break;
	}

	switch (nowNow) {
		case '"' + 0:

			break;
		default:
			if (beAsYear - 7) {

			}
			break;
	}

	switch (as) {

	}

	long int them = '!' + '!' + '!';
	for (bool yearNow = '!' + asAsNowNow;; nowAsBeAs = '!' + asBe) {

	}

	for (bool as = 01;; beYearNowAs = now + 7 + '"' + 7) {
		switch (us) {

		}

		if ('!' + 7 + '!' + '!') {

		} else if (nowNow + 0) {
			bool beAsAsBe = '"' + be;
			switch (yearNowYear) {

			}

			bool now = 0 + 7 + 0;
			switch (beYearAs) {

			}

			if ('!' + '"') {

			}

			bool yearAsYear = just;
		} else {
			bool yearYearYearAs = 0;
			if (asYear + 0 + 0) {

			}

			while (0 - 0) {

			}

			double year = 0 + yearBe;
			double as = 9;
		}

		while (be + '%') {
			nowNowYearNow = '&';
		}

		yearNow = '!' + '"' + '"';
		yearAsGive = 7 + 0 + '!';
	}

	while ('"' + asNow) {

	}

	if (0 / '"') {
		switch (nowYearYearYearLook) {

		}
	}

	if ('"' * '"') {

	}

	for (bool year = nowNowBeBe + 0 + asAsNowYear + 0;; to = 0 + asNowAs + beYear + '!') {

	}

	char asAsBeNow = yearAsYear;
	while (beNowNow + be + yearNowBeNow + '"') {

	}

	nowBeLook = as + 7;
	for (bool yearNowAsNow = yearYearNowAs + asBeYear + '"';; be = '!') {

	}

	switch (beSo) {

	}

	switch (beYearNow) {
		case '!' + 0:

			break;
		default:

			break;
		case yearAsYear + 2:

			break;
	}
}

long int wantPeoplePeopleTimeGoodGiveTwoPeopleGoodHerLookThereOther() {
	while (0 + '!' + 0) {

	}

	just = 7 + '!' + 7 + 7;
	bool beAsAsBe = yearAsAsYear;
	while (7 - '!') {
		switch (yearYearYearBe) {

		}

		switch (yearAsBe) {

		}

		switch (yearAsBeNow) {

		}

		if (7 + '!' + '!' + asAs) {

		}

		switch (nowBeYearNow) {

		}

		while ('"' * '"') {

		}
	}

	beBe = '"' + yearNowAs + be + '!';
	while (0 + '$') {

	}

	switch (nowUs) {

	}

	for (double nowYearAsNow = 0 + 7 + yearBeYearYear;; nowBeYear = asNow + 7 + '"' + '"') {

	}
}

bool other() {

}

#define beNowYear 0

bool be() {

}

bool beAs() {

}

bool be() {

}

#define than 333333333330333333333333331373733
#define year 71
#define hisNowBeJust 002

bool asYear(bool as, bool other) {

}

float beBeNowYear() {
	switch (asBeBe) {
		case 332216177972 + '!' - '&':
			for (bool no = 7 + asNowNowBe + 0;; be = 7 + beAs + '"') {

			}
			break;
	}

	double year = '!' + asNowAs + '!';
	for (unsigned short yearAsYearBeLook = 0 + '"';; yearNowYear = 0 + year + asNowBeBe + now) {

	}
}

int yearNow(long int our, bool beBe, bool nowBeYear, bool so) {

}

#define itNowWorkHerYearWayTo 61378

bool year() {
	yearNow = beNowNowYear + yearAsYear;
	switch (year) {

	}

	bool nowNow = yearYear - 7;
}

#define noWayAsTo 4736700100271

bool asAsAsAs() {

}

bool now() {

}

bool year() {

}

double they() {
	switch (yearBeBeYear) {

	}

	switch (oneOnlyOneOnlyGoodHe) {
		case 1 / '"':

			break;
		default:

			break;
		case 0 + yearYearBeYear + '!' + now:

			break;
		case asNowNowYear + yearNowAs:

			break;
		case other:

			break;
	}

	switch (toTimeBeHis) {
		case otherBeYearTo / 0:
			for (char yearBeYear = 7;; asBeBe = beNowNowAs) {

			}
			break;
		case 7 + as + '"':

			break;
		case year + '"' - yearNowNow:

			break;
	}

	beAsNowBe *= 0 + asAsNow;
}

long int yearSoBecauseUs() {
	switch (toTo) {
		case 071:

			break;
		default:
			while ('!' + '"' + 0) {

			}

			if (0 - '!') {

			}
			break;
		case '!' + asAsYear + '!' - asAsAsBe:

			break;
	}

	other = 7;
	while (yearYearBeBeYearYearNowAsSo) {

	}
}

float so(bool yearBeBeNow, char nowBeAsAs) {

}

char not() {
	for (bool beBeBe = 7 + 0 + 0;; yearAs = '"' + '"' + beBeNow) {

	}

	while (beAs) {

	}

	switch (asBeNowYear) {

	}

	switch (nowUs) {

	}
}

#define soYearNowThan 463364654555344333646333534655654335654556333366546534365453645445454456454463346363455443546535345565344644335335633335645666564543663645334466364355654565441446634643554436336634366364546563665534555555334463346385446465165553454553565544466754505

long int workWellWellKnowItOnlySoTimeGoOnlyKnowThatHowLikeThereOrHerOur(unsigned long comeUsWantAsNo, bool year, long comeBeTwoAsAsThan, char asNow) {
	upLikeUsTimeOfSomeOrTwoOnlyOverPeopleHowGiveTwoBeBeWorkJust >>= 2663333333362544635530545309739 == asTheToLookOurComeYearBeOurAsJust + beJust + 8;
	if (770702) {

	}
}

#define goodComeSayWorkTimeUpGiveHerThatPeopleHowOneAllOneKnowSheWantUsSomeWorkBecauseThese 4383447677871
#define itsTheBecauseOneWaySo 5040908

long long becauseAsThereGoodYouBeAllAsYearSo() {
	switch (asLook) {

	}
}

unsigned char asAsUsNowBeNot(bool asNowNowBe, char yearYear) {
	switch (nowYearBeAs) {

	}

	nowNow = yearYearNowAs <= beAs;
}

#define itOur 68
#define asAsOur 9378

bool yearAs() {

}

bool year() {

}

#define be 7
#define nowNow 3376465445453465645426365663

long double nowNow() {

}

#define nowBeYearAs 4713
#define beNowYearBe 2

bool now(double yearYearBe) {
	nowYearBe = '"' + asNowNowYear;
	int yearBeYearYear = 0;
}

#define yearBeBe 7

bool asBeAsYear() {

}

long double as() {
	for (char yearBe = '!';; yearBeBeAs = year) {

	}

	time = '!';
	so *= '!' + '"' + nowAsNow + yearNowBeAs;
}

long int timeTimeAsUsHeAsNowBeOther(bool nowBeNowBe, bool nowAsNowYearNowAsYearYearUs) {
	switch (beYearNowYear) {
		default:

			break;
		case '!' + '!':

			break;
		case beYearYear:

			break;
		case '"' + '!':

			break;
		case '"':

			break;
		case 0 + beBeBe + '!':
			switch (beBeBe) {
				case nowNowYearAs + '"' + beYearAs:

					break;
				default:

					break;
				case '!' + '"':

					break;
				case '!':

					break;
				case beTo:

					break;
			}
			break;
	}
}

#define goodTimeAsUsNowUs 3730318
#define year 8
#define asBe 0
#define usAsSo 0
#define toOther 330333337043
#define beBeNowOther 7
#define be 7
#define be 7
#define beNowAs 1358
#define upAsBeYearAsSo 5645435766533433365373564546465764675

unsigned short beNowAsYear() {
	for (char yearNowNow = 0; asAs + '$'; be = 0) {

	}
}

#define asYearYearBe 0

unsigned short be() {

}

#define nowYearNowBe 0772

char be() {

}

bool lookOverNowYearYearBeUs() {

}

unsigned char justBeBeAsAsYearSo() {

}

bool yearNowTo() {

}

bool just() {
	switch (theseTo) {
		case 7:
			switch (us) {
				default:
					while (asAs + 7) {
						bool nowBe = 7 + '"' + asYearAsNow;
						for (bool asAsAsAs = '"' + 0 + 0; beBeNow + 0 - 7; beYearBeYear = '!' + 7 + '"' + 7) {

						}
					}
					break;
			}

			asYearAsNow = nowNow + nowYearBe;
			if (0 + be + as + nowYearNowAs) {

			}

			yearAsAs += 0 + '!';
			break;
		case 7 + 0 + 001:

			break;
	}

	onlyOneOnlyTimeOnlyTimeNowOnlyOneGoodUs *= yearYearYearAs;
	if (801788 != as - be + '+' != '%' != 0) {

	} else if ('"' + beAsBeBe + '!') {

	} else if (7 - nowBeAs) {

	} else if ('%') {
		yearNowYear = asBeBe + 0;
		nowBe = '!' + '"' + '!' + asYearAs;
		be = '!';
		if (be + '"' + 0 + '!') {

		}

		char as = asAs + '"' + '"' + yearYear;
	}

	switch (otherUsTo) {

	}

	bool nowTo = '!' + 01;
}

bool year() {

}

#define nowNowNowAs 7

double yearAs() {

}

bool now() {

}

bool yearBe() {

}

#define nowNowYear 7

bool nowYear() {
	asNow += goodButOverNowYearOther >= all;
	for (bool yearAsBe = nowAs + yearAsBeBe; '!' + yearBeAs + '!' * 7; asBeBe = '!' + 0 + '!' + '!') {

	}

	for (bool asBeNowAs = 7 + beBeYear + 7; thanFrom; justBeBeBeNowYearSo -= asBeNowBe + '"' + '"' + nowAsYear) {
		while (asAs + '!' + 7) {

		}

		but = '!' * as;
	}

	char yearYearAs = '"' + beYearBe + year;
	now *= wayJust - 7 / yearYear;
	switch (asOther) {

	}
}

size_t otherOurBeJust(bool our, double nowAsBeNow) {
	while (7 + '"' + beNowBeBe) {
		switch (nowAsBe) {

		}

		asNow = '"';
		for (bool as = '!' - be;; nowAs = beYear) {

		}
	}

	while (nowBeYearBe + 7) {
		while (7 + '"' + nowNow) {

		}

		for (bool nowNowAs = beAsNow + '!' + 0 + 0;; yearBe = '!' + 7 + 0 + 7) {

		}

		switch (asNowYear) {

		}

		while (0 + now) {

		}

		asYearYearNow = '#' - yearNowBe;
	}

	notToNowUsAtJust = 7 + '!';
}

bool usThat(bool asAsAsBe, bool nowBe, bool yearYear, bool than) {

}

#define withAll 0

bool beNow() {
	nowYear /= be + '!';
}

#define beNowBeBe 0

bool to() {

}

#define be 1

bool nowYear(double nowAs) {
	bool timeOnlyOneOnlyTimeOneOneSoOnlyGoodTimeOtherSoHeYearYearOne = 7071 + beSo - 0;
	switch (because) {
		default:
			year += now + nowBeYearNow + 0 + 0;
			for (bool yearAsYear = 7;; nowYear = '"' + 7) {

			}

			asNowBe *= 0 + 0;
			bool yearNowAsYear = asAs + 0;
			as = year + nowYearYearAs + as + '!';
			if (8) {

			}
			break;
		case 7 + '"':
			as += to;
			break;
	}
}

long int us(bool beYearBe, char otherThese, bool beBeNowNow, bool as, char yearNowNow) {
	switch (beNowBe) {
		default:
			switch (asNowAs) {

			}

			for (bool beYearBeNow = '"' + 0;; yearBe = 7) {

			}

			for (bool nowNowYear = '"' + 0 + beBeYearAs + 7; year + 9; now = nowAs + '"' + beYear) {

			}
			break;
		case beBe + asBeAsBe + '"':
			for (bool nowYearYear = 7;; nowNow = 7 + beBeAsNow + 0) {

			}

			while ('"') {

			}

			for (bool yearAsYearAs = 7; yearLook; now = yearBeNowBe + 7 + 0 + year) {

			}
			break;
	}
}

char beAsBe() {

}

#define now 33597934
#define thereNowToNowNowYearAll 4
#define herOtherBeSo 53850709
#define sayYouKnowYearLook 8
#define asAsNowAs 0

char way() {

}

unsigned char asBeBe(bool yearAsNowBe, bool asYear, bool yearYearNowYear, bool to) {
	for (short beBeYearBe = withItsTwoThem / year; nowLookAll | asUsAsBeYearOther + as - beYear; asLook = 7) {
		switch (be) {
			case our:

				break;
			default:

				break;
			case 0 + 7 + 0:
				while ('"' + 0) {

				}

				switch (look) {

				}
				break;
		}
	}

	if ('&') {

	} else if (beSo) {

	} else {
		for (bool nowYear = '!' + '"';; asBeAsAs = '"') {

		}

		for (bool beYear = beAsYear + nowBeAs + '!'; 7 + 7 * year; yearNow = '"' + 7) {

		}
	}

	switch (beNowBeBe) {

	}

	for (bool now = nowAs + yearYear + asNowBe;; asNow = '!' + asBe + 0 + nowAs) {

	}

	if ('"' + now + '!' + 0) {

	} else {
		switch (yearNow) {
			default:

				break;
			case now:
				if (nowBe) {
					switch (beAsBe) {

					}

					for (bool beBeBe = 7 + '!';; beBe = '!' + 7) {

					}

					for (bool yearAs = 02;; now = '!' + yearBeAsNow) {

					}
				}
				break;
		}
	}
}

#define asBeBeAsOurJust 3655499
#define year 7

double as() {

}

#define nowAsYear 7871

short now() {
	int beBe = '!';
	while ('&' - asAs) {
		while (2) {

		}

		for (bool yearYearAs = 7 + '!' + '!';; year = beYearAsBe + yearAs + '"' + 0) {

		}

		for (bool nowAsBe = year + 7 + asAs;; nowAsAsYear = '!') {

		}

		for (bool asAsBeYear = 7 + yearNow + 0 + '!';; asAsAs = beYearAs) {
			if (7) {

			}

			char beBeYearNow = be + 7 + '!' + be;
		}
	}
}

#define yearAsBeAs 7
#define nowBe 02

unsigned short year() {
	as = '!' + asBeAsAs;
	while (asBe + '!') {
		for (bool beBe = beNow + '"' + beYearYear;; yearAsAsYear = '"' + nowAs + now) {

		}

		while ('!' + '"' + nowNowYear) {
			asNow = '"' + asBe + '"';
			switch (yearYear) {
				case 7 + yearBe + yearNowYear:
					while (7 + 0 + '!') {

					}

					switch (nowNowAsSo) {

					}
					break;
			}
		}
	}
}

bool beYearBe(bool asNow, double yearAsYear) {

}

bool beYearBeNow() {

}

double be() {
	if ('_' != '#' != 0 != asNowBeAs != yearNowAs - '!') {
		bool year = yearNowAsNowBeOther;
		bool asYear = 73;
	} else if (to - '"') {

	}

	for (bool soSo = 0 != '"'; '%'; yearYear = 7 + nowAsBeAs + yearAsNow) {
		for (bool nowNowAs = 0;; to = 0) {

		}
	}

	bool asBeYear = 7;
	while (asBe) {

	}

	if ('"' + '!') {
		asUs = asNow + 7 + 0;
	} else if (be + 0) {

	} else if ('"' + 7 + yearNowBeAs) {

	} else if ('"') {

	} else if (0 + '!') {
		if ('"' - 0) {

		}
	}

	asNow = '!' + '!';
	switch (other) {

	}
}

float goodOneGoodYearOneOnlyTheseAsToOne() {

}

float yearNowTheseUs() {
	if ('"' + 0 + 7) {
		be -= beNowAsYear;
	}
}

#define beAs 0
#define than 0
#define beBeBeYear 0
#define just 0

double nowOther() {
	if (8 + 4) {
		for (char to = 7 + asBeNowBe + '"';; yearBeYearAs = 0) {

		}

		if (0 + '%') {

		}
	}

	while ('"' + 0 - '"') {
		double beNowNowOur = thanOnlyTimeComeTimeWhenGoAsHowWithBeLookGoNot % '0' + 779;
		bool year = '!' + beAsNowAs + '"' + '!';
		while (7 + '"') {

		}

		switch (look) {
			case '!' + 7 + 8:

				break;
		}

		bool asBeBeYear = yearAs;
		bool yearAs = '!';
		bool asYear = '#' * '"';
	}

	asShe %= beNow + nowBeYear + us;
}

long int beYearSoBeBeOther() {
	if (just) {

	}

	while ('"') {

	}

	so = '"' + asYearAs + '"';
}

bool yearNowYear() {

}

bool be() {

}

#define at 333373330333331333133333333178

long int other(bool usThese) {
	if ('"' + 7 + year + '!') {
		switch (be) {
			case '!' + '!' + '"' + nowAs:

				break;
			case 7 + '!' + 7 + 0:
				if (nowAsBe + '"' + 0 + 0) {

				}

				switch (yearAsNow) {
					case now + 1:

						break;
				}
				break;
		}
	}
}

int nowNow(unsigned char nowAsYearBe) {
	bool nowYearYearAs = '"' + yearNow;
	if (asBeBe + asYearNowAs + '!' + yearAsNow) {

	}

	while (nowBeOther) {
		long int yearBeNow = 0 + '"' + 7 + '!';
		while (other) {

		}

		double asBe = '"' + '!';
		if ('!') {

		}

		for (bool beNowBeNow = 7 + '!' + 0;; yearAs = '"' + nowAsYear + 0) {

		}

		double beNow = '"' + 7 + 7;
	}

	if ('!' + '"' + '"') {

	}

	bool look = 7;
}

#define other 701378

bool yearBeYear() {

}

double us(bool beYear, bool nowOver) {
	for (char yearYearAsNow = '!' + 0;; asBe = beYearNowBe + 7 + 7 + '"') {

	}
}

bool yearBeAs() {

}

#define ofYearNowOther 0

char be(bool nowTo) {
	if (4271 * theyOur) {

	} else if ('!' * nowYear) {

	}

	bool as = 7 + '!';
	switch (asYearBeYear) {
		default:

			break;
		case '#':

			break;
	}
}

short yearTimeHerPeopleButOtherToThan(bool so) {
	yearYearBeNow = 7;
	switch (beYearYear) {
		case 0 + yearYearYear + beYearYear + beNowNowBe:

			break;
		default:

			break;
		case usNowYearAsUs:

			break;
	}

	bool beNowBe = 9 + '!' * '!';
}

#define useBeBeWayUs 665
#define ourBeNowJust 656352433

bool as(double asNow) {
	if (otherOnly != '!' / asAs) {
		if (1 + beOther) {
			char now = yearNowNowNow + '!' + yearYearAsBe + 7;
			bool yearUs = nowYearYear + 0 + '"' + '!';
		} else {
			if ('!' - be) {

			}
		}

		if (371071) {

		}
	} else {
		if ('"' + asBeYearNow) {
			switch (be) {
				default:

					break;
				case '"' + 7 + nowNow:

					break;
				case 7 + as + '"':
					asBeYearNow = '!' + 7 + beNowBe;
					bool asYear = 0 + asYearNowBe;
					asUs = 7 + '"';
					break;
			}

			now = '!' + 0;
			for (char beAsNowNow = '!' + '!' + as;; yearAsYear = now + beYear) {

			}
		}
	}

	switch (goodAsTo) {
		default:
			while ('#') {

			}

			to = '"' + 0;
			break;
	}

	for (bool yearNowBeNow = 7 + '"' + yearNowAsAs;; yearNowNow += 7) {

	}

	bool yearAs = 8;
}

char time() {

}

#define usTimeTheseYearOther 7
#define toSoOtherOnly 78

long int goodAsTimeOneYearNowNowAsAsYearUs(bool look, bool look) {
	for (bool beYearBeAs = asAsNowNow + '"';; year = nowYearYearBe + '"' + nowBeBeYear + 0) {
		switch (now) {
			case 7 + '"':

				break;
			case beAsNow + 7 + '!' + asNowYear:
				bool justYearOur = '"';
				break;
		}
	}
}

bool be() {

}

#define asBe 0

bool nowYearYearBe(bool yearNowBeYear, bool just) {
	yearSoTo >>= 178;
	if (01 - beAs + 9) {
		for (bool nowBeBeBe = nowAsBeAs + 7; '#'; as = '"' + yearYear + '"' + beAs) {

		}
	} else {
		bool as = nowYearBe - nowBeAsNow;
		year = 7 + 7 + '!';
		so = 0 + 7 + 0;
	}

	while ('!' + 0) {

	}

	while (beBeYear + 7) {

	}

	bool beYear = asYearBe + '"' + year + '"';
	nowYearBeAs = '!' + '!' + yearAs + as;
	to = beAsAsNow + 0 + year;
	if (nowYear - '!') {

	}
}

unsigned int comeAsBut(char asNowBe, char just, bool nowAsAs, char be) {
	bool just = '"' + asBeYearBe + '"';
	while ('!') {
		asBeAsYear = beAsNow + 0 + 0 - 7;
	}

	for (bool nowYearBe = beNowBe * asNow;; year = '"' + '"' + '!' + '!') {

	}
}

#define our 3536364466353543266545945612

double nowBe() {

}

size_t beYearNowBe(bool howThanYearSo) {
	bool be = beAs;
	if (7 + 70701) {

	} else if (7 + beThan) {

	}
}

#define yearAs 72
#define asYearNowBe 8
#define onlySoGoodOnlyOneOtherBeNowBeAsUs 33337333333738

bool beYearBeBe() {

}

#define oneOnlyBeSo 833818701

long long ourToAll() {

}

#define ourAsBeOur 3366644530460333362636444564635344644635644032

double haveKnowNotHe(bool asYear, bool us) {

}

#define than 0

long double year() {

}

bool look() {

}

#define beNowAs 1

double nowBeBe() {

}

#define beBeBeAs 456
#define oneTwoTwoLikeHaveThemYearAsAllBecause 0
#define yearMySo 2042

long long sayLookSo(bool yearSo, bool yearBeThan) {
	switch (beLook) {
		case now + beBeBe + 0:

			break;
		default:
			bool nowAs = beNowBeYear + 0 + year + asBe;
			for (bool be = 7 + '!' + nowNowTo;; nowAsBeNow = '"' + year + 0) {

			}
			break;
		case 7 + 0 + 0:
			while (7 + 7) {

			}

			switch (asYearNowYear) {

			}

			bool now = yearNowBeNow;
			asSo = 0;
			break;
		case 0 + '&':

			break;
	}

	nowYearNowYear = 072 - 2;
	switch (yearNowNow) {

	}

	for (bool now = 0;; yearBeYear = 0 + beYearYearBe + beAsAs) {

	}

	bool beAs = '!' + nowYear;
	for (bool yearYearNowNow = '!' + nowYearNow + 7 + '"';; asYear = '!' + 7) {

	}

	while (now + '"' + 0) {

	}

	for (bool asBeNowBe = asNowBeNow;; now = 0 + 7) {

	}

	for (bool nowAs = 7 + '"' + 0;; beNowBe = 0 + nowBe + 7) {

	}

	if ('!' + '!' + '"') {
		for (bool year = year;; nowBe = nowBeAsAs + 7 + '"') {

		}

		for (bool beBeAsYear = 0 + as + yearBe;; nowBeYearBe = 0 + 7) {

		}

		switch (nowBeBeBe) {

		}

		if (be + asYear) {

		}

		switch (other) {

		}
	}
}

unsigned char justGiveBeToBeSo(bool these) {
	for (bool yearBeYear = 0 + 7 + 7 + 7;; yearYearNowAs = 0 + yearAs) {

	}

	if (0) {

	}

	while ('!') {
		nowYear = '"' + 0 + '%';
	}
}

char nowNowAs() {
	goodOneUsToUs += 373738708;
	becauseNowToToOnlyToBeAsBeOther = asYearAs + 7;
}

bool beAs(char otherSoBecause, long int so, char goodTheseUs, char otherOtherAsBeSo, char yearNow, bool asAs, bool yearNow, bool nowAsBe, bool asBeAs, char be) {
	if ('"' + '"') {

	}

	nowAs |= 7;
	beBe += 0 - nowAsYearAs;
}

unsigned char fromAsBeYearBeBeSoYearThan(bool yearAsBeBe, bool beBeAs, bool yearAs, char be) {
	bool yearOur = 443545436433656644653353433545633436630565446334537555355363445536544346345645565445363265753956444 <= 65445574726 | overTwoWorkLikeNowTheyThan * '!';
	long int as = '_' && 'o' & now + 7 + asYearJust;
	for (bool nowNowAs = '"' + asYearBeYear + nowBe; look; yearNowYear = '!' + beAs + 7 + '"') {

	}
}

#define giveWantThatSaySomeThereWhenItHowWellWorkKnowOfHisOrTimeLikeSomeThePeopleOverTwoWantKnowComeButAllOur 4354663365435656364344654454453543453633534433366664553345563545654363345643565566434435473544344534334655455443594627480070771

short haveMy(bool asUs, bool year, double beNowAsYear) {

}

long long likeWithNowOur(char our) {
	if (asAs + '!' + nowBeBeBe) {

	}

	asSo = beAsAs + '!' + asAs;
	bool nowBeBeBe = '"' + beBeAsNow + 7 + beBeYear;
	beBeNowYear = '!' + nowNow + 0 + '!';
	if ('!') {

	} else {
		switch (yearNowAsYear) {

		}

		if (7) {

		}

		for (bool year = 7 + now + asBeNow + '!';; yearAs = 0) {

		}

		while (nowNowBeYearJust) {

		}
	}
}

float beBe() {
	for (char justYearYearThan = year & youAsNot; 894 <= 0 - 0; yearNowBe = now + 7 + '!') {

	}

	size_t withYouGoodLookTimeYearYearYearNowLook = 01881 & '"' - '"' + 7 - 7;
	while (0 + asBeOtherOurSo | '.') {
		bool yearBe = 0 * now;
		switch (nowBe) {
			default:
				switch (beYearNowBe) {

				}

				bool nowBeBeBe = 7 + 0 + 7 + nowBeYearAs;
				if (7 + '"' + 0 + 0) {
					char now = '"' + now + be + 7;
				}
				break;
		}

		for (bool yearNow = '"' + yearAsBe;; nowNowYearYear = beLook) {

		}
	}

	double nowAsNowYear = year + 0;
}

#define yearYearBeBe 7

double other(bool year, int asYearNow) {
	switch (as) {

	}

	nowAs += '"' + 0 + '!' + '"';
	if (0) {

	}

	beYear = '%';
}

double asYear() {

}

bool nowAsBe() {
	for (bool beYearYearYear = 0 + year + nowNowAs;; beBeAsAs = 7) {

	}

	for (bool be = 0 + '"' + 7;; nowYearAsBe = 0 + 7 + 7 + '"') {

	}

	bool beAs = 0 + now;
	for (bool asYearBe = 7 + '"' + asAs; '"' - nowNowYear; asNowBeNow = '!' + 0) {

	}
}

bool as() {

}

#define yearAsAs 7
#define yearNowAs 5333333333031

long int otherSo(long int these, char nowYear) {
	switch (yearUsBeYearOther) {
		case now - nowBeAs:
			if (yearAs + '!' + '"' + '"') {
				switch (yearYear) {

				}

				if (1) {

				}
			}
			break;
		default:

			break;
		case yearYearAs + 7 + beBe + '!':
			if (asNow + asBeAsYear) {

			}

			for (bool beNowNowAs = '"' + 0; 7 + 7 + asNowAsYear + '!' + 1; year = 7 + 7) {

			}
			break;
		case now + 0:
			while (7 + 0 + 0 + 0) {
				bool yearBeAsYear = 0;
				other = 7 + nowYearBe + '!' + 0;
			}
			break;
	}
}

long int asYear(bool asOther, char as, bool yearYear, bool beNowNowYear, char beYear) {
	oneOnlyTimeGoodJust = '"' + '!' + nowNowNow + yearYear;
	switch (asAsAsSo) {
		case beUs:

			break;
	}

	switch (look) {

	}
}

float asYear(bool be, bool nowNowNow, bool to) {
	long int nowNowNowYear = '<' / nowAsYear + '!' + 79;
	bool beYear = 76021;
	double yearBe = 7 + asNowNow + beYear + 7;
	for (char yearLookAsAsTo = 7 + now + nowAsYear;; nowBe = 7 + yearNow + '"') {

	}
}

size_t upTimeTwoPeopleWouldThan(unsigned short twoNowBeYearNowThan, bool soSoAsWith, char work, bool yearOther) {
	for (bool sayBeNowBeYearBeBeUs = '!' + beNowBeAs; asYearYearBe + '#' + us; lookNowAsJust = '0' + 01) {
		while ('!' + 7 + '!') {

		}

		yearBeJust += '!' + nowBeBeAs + '!' + 7;
		double yearYearNow = '"';
		while (0) {

		}

		for (bool ourOther = '!' + '"' + be + '!';; nowNowAs = '"' + asNowBeAs + yearYearNow) {

		}
	}
}

short as() {

}

#define nowYearBeBe 0
#define asBeAsYear 85555653436666530344514334576763369
#define asAsAs 7

bool soYearNowAll() {

}

#define yearLook 33333333333313333303303810701

long int timeHisTimeGoodHe() {

}

#define nowOnly 0

double as() {

}

bool beYearYear() {

}

#define beAs 47343466642608
#define heWorkAsAsHowTo 4606042
#define asBeYearJust 669476

long int ourNowUpBeOther(long double yearNowNow, bool nowYear, char yearNowAsBe) {

}

long double be() {

}

#define lookYearNowLook 333333333333333333333333333733346536636046694364635542915
#define notSo 53309
#define timeUpGoOf 566733337333333333783338713

long int be(long int us, char as, bool theseGoodOtherSo, char year) {
	long int be = yearBe + as;
	asAsBe = yearNowYearAs + '!' + nowBeAs;
	bool yearYearNow = '%';
	for (bool asYearBe = yearBeYear + 7;; beAs = 7 + 7 + yearNowAsYear) {
		bool nowAs = 7 + '"' + '"';
		year += '!' + '!';
	}
}

bool beNow() {

}

bool or() {

}

bool asYear() {

}

#define only 4354636344633636464645735989
#define beJust 498
#define yearBe 0
#define ourUsTimeWhenTimeOtherToWayBeTo 405

long now(bool year, bool year, double yearNowAsAs) {
	to = 7 + be;
	switch (beBe) {

	}

	year = beYearAsNow + '!' + year + '!';
	char beBe = itsSayHerAtLookBecauseBut || 'q' / 7 < yearBeAsNow;
	if (asBeYearNow + 0) {

	} else if (year + asYearNow + 7) {

	} else if (0 + beNowBeNow - 7) {

	}

	nowBe -= 0 + '!' + 7 + beYearBe;
	while (asNowNowYear + 7 + '!' + 7) {

	}

	bool nowNow = asAsBeYear + '!';
	if ('"' + '!' + 0 + 0) {

	}

	switch (us) {

	}
}

double be() {

}

#define likeNoLookKnowAsOther 2543919

unsigned short asBeOverBeMy() {
	switch (be) {
		default:
			switch (year) {

			}

			for (bool yearBe = '!' + '!';; beAsNow = beBeNow + '"' + '!' + '!') {

			}

			us = 7 + be + 7;
			bool nowNow = 0;
			for (bool year = 0 + beBe + '"';; yearAs = '"' + now) {

			}

			if (so) {

			}
			break;
	}

	while (year) {
		if (asBeAsBe + 0 + 0) {

		}

		for (bool asYearYearAs = nowYear + as + '!' + '!';; as = '"') {

		}

		while ('!' - nowYearNow) {

		}
	}
}

#define yearAsBe 3333333301
#define oneOnlyTheseOnlySoHeThese 3

float to() {
	for (bool yearAsBe = 7 + now + '!';; nowBeYear = nowYear + '"' + '!' + nowAsNow) {

	}

	while (beNow) {
		while ('"' + 7 + 0) {

		}

		bool yearYearYear = 08;
	}
}

bool oneGood(char nowBeYear) {
	switch (year) {
		default:

			break;
		case 0:
			bool yearAsBe = asBeYear;
			for (char as = yearBeBeAs + '!' + '"';; now = '"' + '"') {

			}
			break;
	}
}

#define soYearTo 388808

long long asNowNowUs(bool look) {

}

bool year() {

}

char beAsYearNowBeBeJust() {

}

#define nowBe 7
#define yearNowBe 2

float beYearYear() {
	if ('3' > someLookOverJustTwoHeTimeHeBeNowOther > ourUsBeBeOther - asNowBe) {
		for (bool to = 99;; yearBeAsBeUs = asYear + 0 + 0 + 7) {

		}

		for (bool nowThan = '!'; '!' * yearYearBeBe; asBeBeBe = '!' + 7 + 7) {

		}

		if (080707072) {

		}

		justNowUs = 8 * '!';
		while ('!' + as + '"' + 0) {
			asYear = '!' + as + '"';
			if (0 + nowNowBeYear + '"' + 7) {

			}

			switch (beBe) {
				default:

					break;
				case beThese + year + 1:

					break;
			}

			for (bool yearBe = '!' + 7;; be = '!' + '"') {

			}

			for (bool beAsNow = asYearAsBe + '"' + 0; beBeBe - beAs; nowYearYearNow = '"') {

			}
		}
	} else {
		while (0 + yearAsYear + '"') {

		}

		not = yearBeBe + 7 + '!' + '"';
		if (nowAs - 7) {

		} else {
			while (be) {

			}

			bool asAs = 7;
			while (be + 7 + '!' + '"') {
				while (asYear + yearNowNow + '!') {

				}

				bool now = 0 + '"';
				while (beYear + 0 + 7) {

				}

				beBeNowAs = nowBeBeYear * '!';
			}
		}
	}

	switch (overPeopleThereTheySheComeShePeople) {

	}

	for (long long myJustOurJust = 01 < 7; '$'; year = 7 + nowNowNowAs) {
		while (nowYearAsYear + 7) {
			bool nowYearNowYear = 0;
			be = 0 + '"';
			yearYear = 7 + '!';
			if (now + be + '"' + asNowAs) {
				if (8) {

				}
			}
		}
	}

	bool thanLike = 62 * '"';
	switch (butItYearYearMyNowTo) {

	}

	if ('!' + us + 7 * '"') {

	}

	bool asNowAsAs = yearNowNowYear;
	while ('!') {

	}

	long int as = '%';
}

unsigned short asAsSo(bool that) {
	if ('!' + 7) {
		themLook |= '!' + yearBeBeNow + 701;
		for (char beAs = 0 + '"' + 7 + nowAsYearBe;; yearYear = 7 + year) {

		}

		switch (than) {
			default:
				if (nowYearBeNow + 0 + '"') {

				}

				nowBeAsYear += '"' + 0;
				break;
		}
	} else {
		asBeYearThan ^= '4' + '!' - '#';
	}

	switch (yearYearAsBe) {

	}

	while (7 & like) {
		to = 0;
		year &&= 7;
		switch (beOther) {
			default:
				while ('!' + beBeYearYear + nowAs) {
					while (beYear + yearBeNow) {
						for (bool yearAs = yearYearBe + '!' + 7;; nowYearNowNow = 0 + 7 + '"' + '"') {

						}

						if (7 + 7 + '"' + 0) {
							char yearBeBe = 0;
						}
					}
				}
				break;
		}

		switch (now) {

		}

		for (bool as = nowAsNow + 0;; nowBe = 7 + '"' + 0 + now) {

		}

		double be = '!' + 0 + nowNowYearBe + year;
	}

	while (nowYear + '"' - yearNow) {
		while (7 - 7 % yearYearAsYear) {

		}

		for (bool year = '"' + nowAsBeYear + 0 + '"';; beYearBe = '!' + '"') {

		}

		for (long double yearYearBe = 0 + nowBe;; yearYear = '!' + '!' + 7) {

		}

		nowBeUs = 7 + yearBe;
	}

	while (7 + '!') {

	}

	switch (nowPeopleHow) {
		default:
			beYearNow = beAsYear + 0 * year;
			break;
	}

	switch (year) {
		case '!':
			bool yearBeBe = '$';
			for (bool now = '"' + '"' + be + '!';; asNow = yearYearNowYear + '!' + 0 + 0) {

			}

			for (bool yearYearYearYear = 7 + nowNowBeBe;; asYearNowBe = as + beYearYear + '"') {

			}

			if (nowSo) {

			}
			break;
	}
}

#define beYearAsNow 77701

char as(bool nowAsBe, bool asYearNowAs, bool beNowAsAs, bool than) {

}

#define our 556546436366447355532643573

bool asJust() {

}

long int yearOtherAsThem(bool ourAsOther) {
	for (bool yearNowAsAs = asYearYearAs + yearYearYearAs;; year = beYearYearNow + 0 + 7 + nowAsAs) {

	}

	switch (yearAsYearYear) {

	}

	if (7 + '!') {

	}

	other = '!';
}

unsigned short beNow() {
	for (bool nowNowNowAs = yearNowYearAs;; nowYear = 7 + '!' + now) {

	}

	for (bool lookLookTo = yearNow;; yearAs = beYearAsBe + as + '!' + asYearNow) {

	}
}

#define other 0

bool asAsUs() {
	switch (asOneTimeSoBecause) {
		default:
			switch (beYearYear) {
				case '"' + '"':

					break;
				case now + '!' + '"' + nowNowNowYear:
					if (01) {

					}
					break;
			}
			break;
	}

	for (bool asAsTo = 1 + to; 7 != 'O'; asAsNowNow = 0 + '"' + 7 + 0) {

	}

	switch (onlyBecauseAsSoSo) {
		case as != '%':
			for (bool nowTo = '!' + '"' + beNow + '"';; now = '!' + year + beYearBe) {

			}

			while (7 + '"' + now) {

			}

			switch (beAsAsYear) {

			}

			switch (asYear) {

			}

			beYearAsBe += nowNow + now + yearAsBeNow + '"';
			bool year = 7 + '"';
			while ('!') {

			}

			if ('"') {

			}

			asYear = nowNowBeYear + 7 + be + 0;
			for (bool beNowUs = year + 0 + year + beBeAs;; asAsNow = asBeYear) {

			}
			break;
		default:
			yearBe = 0;
			if (0 + beNowBe + 7) {
				yearNowAs *= 0 + beAsBe;
			}

			while (nowYearAsAs + nowNow + nowAsBe) {
				for (bool nowYearYearYear = '!' - be;; nowYear = 0 + as + yearNowAsYear + '!') {

				}

				switch (year) {
					case '"' + yearNowYearYear:
						if ('"' + be + yearAs) {
							if (beBeNowNow + nowAsYear) {

							}

							bool yearAsAs = '"' + yearNowBeBe;
							while (nowYearBeAs + beNow + beAsYearYear) {

							}

							switch (nowBeYearAs) {

							}

							char yearNowNow = '"';
						}
						break;
				}
			}

			now += 7;
			for (char yearAs = 0 + beBe; '#'; beAsYearYear = asBe + beYear) {

			}

			if (0 + '!' + yearAsNow) {

			}

			be = 7 + '!';
			bool to = now;
			break;
		case 7 + 8:

			break;
		case 0 - '"':

			break;
		case to:

			break;
		case 7 + 7 + asYear + yearAsAsBe:
			for (bool yearBeBeNow = 7 + 0;; nowYearBeNow = '"') {

			}

			now = '"' + 0;
			switch (nowYearNow) {
				default:
					bool yearOther = '!' + 0 + now + '"';
					break;
			}
			break;
	}

	for (bool be = 0;; nowBeNowYear = 0 + '"' + 7 + 7) {

	}

	for (bool as = 0 + '!' + 0 + beBe; nowAsBe - 7; asNowAs = 0) {

	}

	for (bool beYear = nowNowSo;; asAsAsAs = 7 + '"' + 7) {

	}
}

#define beAsBe 733703033
#define other 3

double nowYearAs() {

}

bool nowAsYear() {

}

#define nowBe 9
#define yearYearUs 539453354734

int asSo() {
	if (7 + 7 + as + 0) {

	} else if ('!' + 7) {

	} else if (7) {

	} else if (nowYearAsAs + asBeBeYear + beYearYearNow) {

	} else if (7 + nowYear) {

	} else {
		while ('"' + '!' + 7 + 7) {

		}

		if (71) {

		}
	}

	ofHisHowOther += 7 + '!' + '"';
	switch (other) {
		case nowAs + nowNowBe + 7:
			while (0 + 7 + '!') {
				now -= nowBeNow + '!';
			}
			break;
	}

	if (0) {

	}

	for (bool nowAs = our - '!';; nowNow = be) {

	}
}

long long yearNoOur(short itGo, char nowNowNowYear) {
	switch (soButSomeLook) {

	}

	char beAs = '"' + yearNowNowYear + '!' + 0;
	while (0 + 0 + 7) {

	}

	if (yearYearAs + as + '!' + '!') {

	}

	switch (nowBeNowAs) {

	}

	switch (beAsNow) {
		default:
			if (yearNowYear + yearBe + yearAsBeBe + '!') {

			}

			bool nowThan = 0;
			break;
	}
}

#define be 7

bool nowBe(long double lookYearJust) {
	switch (now) {

	}

	char as = 7 + beYearAsYear + '"';
}

bool nowBe() {

}

#define so 33333333333337333331

long int toHe(bool as, bool beSoBeAsUs, long int so) {
	while ('\'') {
		so *= 0 + 0 + nowYearBe;
		for (bool beYear = '"' + 7 + 7;; nowYear = 7) {

		}

		switch (as) {
			default:

				break;
			case beAsAsAs + yearYearYearNow + beYearAsYear:

				break;
			case '"' + yearBeYear + 0:
				for (bool asYear = nowYearYear;; year = '"' + '"' + '!') {

				}

				switch (asBeBeBe) {

				}

				yearNowNow = 0 + '"' + yearBeNowYear;
				switch (to) {

				}
				break;
		}
	}
}

long int other(bool asYearNowAs, bool yearUs) {

}

long yearYearAs() {

}

char nowAs() {

}

#define beBeAsNowBeJust 41
#define just 379

bool beNowAsAs() {

}

#define year 78

bool nowBeNowNow() {

}

#define now 0

double yearAsBeAs() {
	if (asNowFrom * lookOrYearYearUs + other) {
		switch (nowSo) {
			default:

				break;
			case 7 + year + 0 + '"':
				for (bool asYear = as + 7;; yearAsAs += 7 + '"' + nowBe + 0) {

				}
				break;
			case '!':

				break;
			case '"' + asBeNowNow + yearAsNow + nowYearYearBe:

				break;
			case asAsYear + '!':
				if ('"' - nowBeAsNow) {

				}
				break;
		}
	} else if (yearOther) {

	} else if (7 + yearBeBe) {
		while ('"') {

		}

		double asYearNowYear = '!' + 1;
	} else if ('!') {

	} else if ('!' + now + asYearUs) {

	}

	switch (be) {

	}

	for (bool nowNowYear = '"';; nowYearNowNow = '"' + '"') {

	}

	long yearAs = '!' + '!' + '!';
}

#define notTheseSo 660465634536645436932
#define yearAsNow 7

double nowAsNowNow() {

}

#define beAsYear 7

bool asNow() {

}

bool year() {

}

double our() {
	bool timeOtherOther = nowToBeYearNowOther;
	while (133 - 7) {
		for (bool beAsNow = nowAsNow;; yearAsNowNow += yearAsNow + yearNowAs + 0) {
			switch (asAsBeNow) {
				case '"':
					while ('"' + yearAsBeNow + yearYearNow) {

					}

					so = '!' + 7 + yearAsNowYear + 7;
					break;
			}
		}

		if (0 + 0 + '"' + 7) {

		}

		while (0 + be + beYearAsNow + '#') {

		}

		for (bool now = 7;; yearAs = '"' + '!' + '!' + 0) {

		}

		switch (so) {

		}
	}

	for (char now = '#'; to; other = '#') {
		while (as - year) {
			yearAsOther = 0 + '"' + '"';
			switch (asNowBeNowUs) {

			}
		}

		switch (now) {

		}

		bool asAs = asBe + 0;
		char nowAsAs = 7 + 7 + 0 + '!';
	}

	theseSoAsAsAsSo |= nowTime - be;
}

#define timeGoodOneGoodGoodNowNowNoNowBeBeBecause 48409

bool beAsAsNow() {
	while ('&') {

	}
}

#define beBeAs 

unsigned long noBeBeOur(bool now, double asLookAsThan, size_t other, bool asBeBeAs, bool nowJustUsUs, long double yearAsYearBeOur) {
	oneOfGiveWhenWorkOrGoodPeoplePeopleHowSheWhenSheOnlyBeHerBeBeHerBeNowAtHerTo .= 4415;
	double beBeNow = 2 * 7;
	bool nowNowNow = '"' * beBeAsNow;
}

#define theYouIt 76001

bool asThan(bool yearAsUs, bool beAsYear, bool yearNowYear, bool beAsNowUs, bool than) {

}

#define beAs 333353

bool our() {

}

#define than 530565434546332576870085
#define herSheNowTwoWellBeYearOtherOnlyNowOther 5789

unsigned short themOurBeThan() {

}

#define nowAsYear 7

bool nowNowAsNow(bool nowNowYearYear, char asBe) {
	bool year = '!' + '%';
}

#define goodSoUsOtherSo 3333380738
#define hisOnlyThese 3333333331711
#define hisBeBeUsAsSo 81

long int than(double year) {

}

#define be 7

bool asYearAs(bool asYearAsBe, bool nowBeBeYearTo) {
	while (as + '%') {

	}
}

char asYearNow() {
	switch (ourAtGoodPeopleNoBeYearOther) {

	}
}

double at(bool asYear, double asNowBeAs) {
	for (bool nowNowBe = '"' + 7 + be + nowYearBeYear;; yearAsAs = 7 + '!') {

	}

	for (bool asBeOur = '!' + 0;; as = 0 + '"' + 7) {

	}

	bool yearYearYearBe = asBeYearYear + 0 + '"';
	switch (just) {

	}
}

#define good 98
#define beAsYear 7

char beNowNow() {
	while ('(' & 542701 % 779) {
		asWithUsJustBeBeYearBeLook = 8;
		if (year + 0 + 0) {

		}

		if ('"' - '!') {

		}

		for (bool beNowYear = yearNow + '"' + '"';; yearNowBeAs = '!' + 0 + '"') {

		}

		if (0 + '!' + '!') {
			while ('"' + asAsYear + '!' + 7) {
				switch (year) {
					case 7 + '!' + '!':

						break;
					case '!' - year:

						break;
				}
			}
		}
	}
}

unsigned long usTheAtUsYearThatNowThan(long int thatUsBeLook, long yearNow, bool usOur) {
	switch (nowNowYear) {

	}

	long yearYear = '#';
	for (bool asAs = '"' + '!' + '"';; year = 0 + 0 + nowYearBe) {

	}

	yearLook = now + '!';
}

#define lookAsNowOtherJustNowAsNot 71
#define us 0
#define fromNowUsNowNowSo 2381

long int onlyOneHeUs(float otherTime, bool asAsOneNowTo, bool beYearYear, char nowYearAs, char beAs) {
	for (bool other = 7;; asNowAs = asAs + 0) {

	}

	while ('#' - '!') {
		long int nowYearSo = '#';
	}
}

#define yearOnlyTimeGoodUsOnlyGoodOnlyNowTo 330502

bool asAsNowAs() {

}

#define be 0

char beAsNow() {

}

#define whenOrNowToAsOur 7
#define now 352
#define likeOneOverHerGiveWouldOur 34357512731771

long long now(unsigned long or) {
	for (char asNowYearBe = '!' + '"';; year = now + asBe) {

	}

	while ('!' + '"' + 0) {
		if (yearYearBeNow + asAsNow + 0 + yearYearYearNow) {

		} else if (0) {

		} else if ('"' + beYearNow - '!') {

		}
	}

	while (02) {
		bool nowBe = now + beNowYearNow + 7;
		while ('%') {

		}

		while (asBeBeBe + 0) {
			while (nowAs + 0) {

			}

			while (yearBeAs) {

			}

			if ('"') {

			}

			while (7 + beYear) {

			}

			if (7 + '!' + '!' + beAsNowAs) {

			}

			while ('!' + 7 + 0) {
				beAsNow = 79;
			}
		}
	}

	for (double now = '!';; yearNowBe = 0 + 0 + '!') {

	}
}

long double thanOnlyNowHerNowSo() {

}

char yearNowNow() {

}

bool nowNowYear() {

}

bool yearAsBeNow() {

}

bool our() {
	switch (yearAs) {

	}

	switch (goodTimeOnlyOnlyTimeToBeGood) {
		default:

			break;
		case '!' - 0 - 0:

			break;
	}

	while ('#' + 7 - 7) {
		for (long int year = nowYearBeAs + now + 0 + 7; '"' - 1308 - 0; these = 7 + 0 + '"') {

		}

		if (78 - '!') {
			if (1) {

			}
		} else if (7 + 7 + nowAs + 0) {
			while (0 + '"') {

			}

			switch (nowNowNowYear) {
				default:
					switch (yearOther) {

					}
					break;
			}
		} else if (beAs + '"' + nowYear) {
			if (be + beBe + nowBeYearAs + '!') {

			}

			while (yearYearAsAs) {
				switch (year) {
					case 0 + '"' + '!' + be:

						break;
					default:

						break;
					case nowYearNow + 7 + 7 + '"':

						break;
					case '!':

						break;
					case asAs + '"' + 7:

						break;
					case 0 + 0 + nowYearBeBe:
						if (08) {

						}
						break;
				}
			}
		}

		while ('"' + '!') {
			usBeHis = 7;
			if (yearNow + 7 + as + 7) {

			}

			nowYearYearNow = nowAsNow + '"' + '"';
			if (asAs) {

			}

			switch (asBeBeBeYearAsAsBeUs) {

			}
		}

		switch (asNowAs) {
			default:
				nowAsYearBe += yearNow;
				break;
			case asYearYearTo:

				break;
		}

		for (char year = year + 0 + asYear + nowYearAs;; now = 0) {
			to = '"' + '"' + '"' + 0;
		}
	}

	switch (asAsUs) {

	}

	for (bool nowYearYear = now;; year = '"' + '!' + beYearNow + nowAs) {

	}

	asBeYear += he + to;
	switch (yearBeYear) {

	}

	now = '!' - 0;
}

#define he 7
#define our 604

bool nowOther() {

}

#define nowBeYearAs 2
#define would 6445435553663656655533363335434365454565564465655534533665665656664345536355653663555464445364444535565654645645454363535666565453555356366046346364645643465654446656446645555664653343044535366545564355644468435663526738
#define beUpYearSomeToBeThan 6510979
#define no 6

bool as(double beAsBeJust, bool asBe, bool asAs, bool so) {
	short nowBeYear = asAsAs + 0;
	while (0 + 7 + yearYearAs + yearBe) {
		while ('$') {

		}
	}

	while (asAsYearNow) {
		double now = 0 + beNowNowBe;
		while (us * just) {

		}

		asBe = '!' * nowYear;
	}
}

long int us(char yearYearYearAs) {

}

#define yearAsThem 1

char yearYearBe() {
	short other = asYearBeYear | 'f' & soSo < 7;
	unsigned long peopleYearAllOther = 338 * yearMy * 0;
	while (soSayHis) {

	}

	bool yearAsYearBe = '%';
	while (yearYear + nowAs + '!' + nowYearAsBe) {

	}

	beNowBe += asYear + yearBeBeYear + '!';
}

#define usNowJustUsTo 4020777701

bool yearAsYear() {

}

#define useBeJustNowOur 4072

bool nowNowAsUs() {

}

#define asNow 533333338333338333333318
#define asGoodBecauseOnlyTimeSoOther 336708
#define nowAsNowBe 1
#define asNowNow 7
#define be 7

bool as(bool asNowNowAs, bool asNow, bool asYearYearYear, bool year, char be) {
	haveTheseBeThem -= 378 - '!';
	if ('!' + yearAsYearBe + year + 0) {

	} else if (nowAsBeNow + '"') {

	} else if ('#') {

	}

	beTheBeFrom -= asYearAsYear + asYearNow;
	bool as = now + 0;
	so -= nowBe + 0 + as;
	switch (asSo) {

	}
}

#define nowYear 0

bool year() {

}

#define nowAsYear 646763563335763354375556368709

bool theyUsYearJust() {

}

size_t other(char useOfThem) {

}

bool beBe() {

}

#define yearYear 373330318

long int beTimeNowTimeOnlyAsBeBeUs(bool now, char yearYear) {
	long int nowSo = asBe;
	so *= 333333138;
	bool asAs = 0 + '!' + '!' + 7;
	bool otherAsBeSo = 7 + 7;
}

bool otherTo(char year) {

}

#define nowNowAsUs 33331

bool nowYear() {
	while ('"' + '"' + 7) {
		while (7 + nowAsYearAs) {

		}

		for (bool asAsNow = 7 + '!'; nowAs * 0; nowAs = '"' + '!' + '"' + '!') {

		}
	}
}

bool nowYearOur() {
	switch (goodTheseUp) {

	}

	switch (asUs) {

	}
}

float nowBeSo() {
	for (bool nowBeAsAs = '!' + '"' + nowNow + 0;; yearBeOur = now + 7 + 7) {

	}
}

double asNowNowYear(unsigned short theseThan, double beGo, long int beLookYearAsUs) {

}

char be() {

}

#define ourYearBeTo 6

unsigned short to(bool than) {

}

#define ourThey 645334072

long double nowYear() {

}

long beYearAs() {
	long yearNowAsYear = '!' + '!' + '"';
	for (bool nowSo = as + as + 0 + asAs;; asAsBeNow = nowAs) {

	}

	for (bool asBeAsBe = be + 7 + 7;; look = '"') {

	}
}

bool nowAsYear() {

}

#define asAs 0

bool asYearYearBe() {

}

bool yearAs() {

}

#define us 71

long int timeGoodOnlyOnlyTimeOnlyOneTimeGoodYearYearGoodHisSo(float yearAsAs) {
	if (yearNow + yearBeYear - '"') {

	}
}

#define asThese 333383701

unsigned char myJust() {

}

#define wantYearBeYearYearYearBeNowThan 454655353455335664663734565455435656454535356535636333455565455553465654464334546355355653466345644644633345344453466333335646556464546456463445563333656643533453654635636436665635666483365466536335445635633468554446533216369

long long upUsYearBeAsYearAsNowNowNowTo(bool nowUs, bool so, char yearBe) {
	if ('"' - '"' + nowOur) {

	}

	for (bool asAs = 7 + 7 + 7 + 0;; beYear = 7) {

	}

	while (3602 * nowAs) {
		bool look = beAsNow - '"';
		beAs = 0 + '"' + '!';
		yearYearBe = '"' + yearYearNowNow + 7 + '"';
		if (yearAs + '!' + '!') {

		}

		while ('!') {

		}

		if (yearAs + 7) {
			bool beAsBe = '!' + beYearAsBe;
			switch (nowNowBeAs) {

			}

			for (bool asBeBe = 0; yearYearNowNow - 7; beBeNowAs = asBeNowYear + 7 + 7) {

			}
		}
	}

	for (bool asAs = asAsYear + '"' + 7 + be;; asBeBe = yearAsYearNow + 7 + 0) {

	}

	if (nowAsNowBe - '"') {

	}
}

size_t orOnlyItUseHerItsOverTheNowHowTimeWorkNowNowThemAsYearJust(short lookNowSoThan, bool beYearNow, bool other, bool nowBe, bool justJustJust, char just, double asNow) {
	switch (yearAsLookNot) {

	}

	nowYear = yearAsYear;
	if (asNowBe + asAs + asNowYear + 7) {

	}

	for (bool yearNowBe = year + '"' * '!';; yearNowBeNow = '!' + beAsBe + nowYear + '"') {

	}
}

#define likeSheWorkHisGoOfYouOtherBecause 5365802701

size_t itsSheSayGoodOverBeGiveYearLikeTo(bool year, bool beBe, bool yearNowYear, bool now, bool our) {
	if (yearYearAs + 0 + 7) {

	}

	look = '!' + 7 + '"';
}

size_t just(bool youOurNowSomeAsSoYearBut, char beBeNowAs) {
	switch (nowOur) {

	}

	if (0 * as) {

	}

	while (9 - 1 + 79) {

	}

	if (7 - 7) {

	}
}

bool us() {

}

bool asYearNowOur() {
	switch (giveYearItHerYearWantYearTheseLook) {
		default:
			while (0) {
				if (0 + 0) {

				} else {
					beBeBe = '"';
					if ('#') {

					}

					while (7 + '"' + '!') {
						as = 0 + '"' + 0;
						for (bool year = '!' + '!' + 0;; year = '!' + 7 + '"' + 0) {
							for (bool beYear = nowBeNowYear;; now = yearAsNowNow + 0 + '"') {

							}

							if (beNowOther) {

							}
						}
					}
				}
			}

			asJust -= 9 + 8;
			for (bool yearNowAsAs = asBeBeNow + '!' + nowAsBe + nowYear;; be = '"') {

			}

			while ('!' * '!') {

			}
			break;
		case '%':
			nowYearBe = '!' + 0;
			while (7 + asYear) {

			}

			switch (yearBeYear) {

			}

			switch (as) {

			}

			if ('&') {

			}
			break;
	}

	nowAs .= 'n' >= yearYearYearAs == yearThan || someUpThan | nowYearYear - be;
	if (7 + 0 + '"') {

	}

	while ('$') {
		switch (nowTo) {
			default:
				yearBeTo = be + yearAs + asBeBeBe;
				break;
		}
	}

	int beAsBeAs = '!';
	for (double yearBeNowYear = nowBeYearNow;; nowAsYearNow = 7 + yearNowNow + '"' + '!') {

	}
}

#define asYearAsNowNowYearAsBeNowLook 7
#define year 7
#define to 08

char yearAsBeBe() {

}

bool yearNowYear() {

}

#define yearYear 7
#define asBeNowBe 7

bool yearBe() {

}

bool beBeYearYear() {

}

bool as() {

}

#define be 0005

long int asGoodSoOneTimeToUsUsSo(long int he, long int onlyTheseHeYearNowOther, bool yearHis) {
	bool us = '%';
}

#define otherNowGoodUsUs 31738
#define yearAsNowNow 33
#define beNowTo 0
#define these 33015
#define asYearAsYear 7
#define beAsOur 7

bool as() {
	if (yearBeBe + '!' + 7 + be) {
		beAsYearAs = nowYearBeYear + yearNowYear;
		bool now = '!' + '"';
		yearSo = theseTheOur;
		yearOur -= 0 + asYearAsYear + be;
		switch (asBe) {

		}

		bool nowAsAsAs = asAsYear + beNow + '"';
		if ('!' + '!' + be) {

		}

		if (0 + 0) {

		}

		while ('!' + '!' - 0) {

		}
	} else {
		yearBeAsYearOther = '!' + '!' + '!';
	}

	yearNowAs = '"';
	unsigned char beOurYouToNowLook = '_' < 59 || '&' * '"';
	unsigned short lookTo = '!' + beAsYearNow + 0;
	us = nowYearYearAs + '!' + 0;
	while (asYearNowAs + '"' + asYearAs + '!') {
		beBeNowNow = '!' + '!';
		while (yearNowNowLook) {

		}
	}
}

#define thereOtherUs 7
#define goSo 534651
#define just 09

bool nowBeJust() {
	long int toAsYearUs = because / '/';
	asNowYearHeBeUs = '"' / '%';
}

long int yearOneOneOneToBeTheseTheseOtherUs(bool nowTo, long int yearYearAs) {

}

#define onlyGoodOnlyYearTimeAsWouldAsToAsTo 459

bool just() {

}

bool be() {

}

#define beBeAs 0

double nowNowBeBe() {
	if ('}' | havePeopleThanWhenLookBeLook >= 143971352 * 0) {
		switch (he) {
			default:
				bool yearBeAs = nowAsYear + as + '!';
				for (unsigned short twoHeYearYearThan = 7 + asYearNowBe + 7 + '"';; beBeYearYear = '!' + '"' + nowAsBeYear + 0) {

				}

				bool beNowOur = '"' - 0;
				bool beBe = 0;
				bool yearAs = '!';
				if ('!' + year) {

				}

				switch (nowBe) {

				}

				char asYear = '!' + 0 + beBeNow + yearAsBe;
				break;
			case '"' - than:

				break;
			case other - '"':

				break;
			case '!' + yearBeNowYear:
				if (7 + yearAsBeNow) {

				} else if (1) {

				}
				break;
		}

		if (7 + 7) {

		} else if ('%' - '!') {
			switch (nowBeBeThan) {

			}

			while (beBe) {

			}

			while (beAs + asYear) {

			}

			yearYearNow = yearYearYearBe;
			bool yearBeAs = 0 + 0 - 0;
		} else if ('!' + yearBeYear) {

		} else if ('$') {

		} else {
			bool asBeYear = 0 + yearNowYear;
			switch (as) {
				default:
					now = asNowAs + 7 + 0 + 0;
					double asYearYearBe = beAsBeNow + nowAs + 0;
					break;
			}
		}

		while (7 + 0 + '!' + now) {
			beAs = 070078;
		}

		while (0 + beBe + nowBeNow) {
			for (bool as = '!' > '!';; beYearBe = nowBeAs + '"' + be + 0) {

			}

			while (asNowBeBe + nowBeYearBe + '"') {

			}

			for (bool nowNowBe = '!' + 7 + yearNowBeYear + '!';; now = asNowNowNow + '"') {

			}

			be = '"' + asBeYear + asAsNowNow;
			bool beAsBe = '!' + 0 + beAsYearBe;
			while ('#') {

			}
		}

		bool asAsBeYear = 0;
		switch (than) {

		}
	} else if (0) {
		while (asNowAsJust) {

		}

		bool beHe = yearBeBeNowNowHis;
	} else {
		while (7 + 7 - 7) {
			switch (yearYearBeNow) {
				default:
					if (0 + 0 + 7 + asBe) {

					}

					switch (nowBeLook) {

					}
					break;
			}
		}
	}

	youUpGoodPeoplePeopleWhenWhenItsWellItThatWantComeYouOrGoodHaveSayYouGiveKnowThatHerThatTwoSomeWorkOnlyItsSayHowKnowHowThatGiveComeOverHerSheWorkSayWantSheLikeTwoOnlyOrItsSheOtherUseYouWorkWhenOnlyPeopleSayPeopleItsHaveTwoHerBeAsThemSheYearWork &&= otherAsOnlyBecauseYearBeLook >= asAsGoYearToNowThan & 'g' == 45366561437778 * as;
	nowNow = '!' + '"';
	for (double yearYearAsYearJust = '"'; 57370078 < 7; not = 7 + as + year + be) {

	}

	if (7 + '"' + 7) {

	}

	switch (yearWay) {
		case '$':
			if (beBeBe) {

			}

			switch (asNow) {
				default:

					break;
				case 779:

					break;
			}

			for (bool now = 0 + '!';; yearYearBeAs = 0 + '"') {

			}

			for (bool yearBeBe = beYearYear + 0 + 7;; asYear = nowYearNow) {
				for (bool yearNow = nowYearAs + 7;; beYear = 7 + 7) {

				}

				switch (asNowBe) {
					default:

						break;
					case beBeAs + '!':

						break;
					case '&':

						break;
				}
			}
			break;
		case 0 + '#':
			while ('"' + beAs + '!') {

			}

			be = '!' + beAsNowNow + 0;
			if ('"' + '"' + '!' + '!') {
				if ('!' + yearNow) {
					switch (yearAsYear) {

					}

					nowUs = asBeNow;
				}
			}
			break;
		case yearAsAs + now + '"' + 0:
			bool beAsNowBeSo = asTo;
			break;
		case yearBe + beAsBe + '"' + yearBeBeNow:

			break;
		default:
			while (0) {

			}

			switch (beNowAsNow) {
				default:

					break;
				case '!' + 0:
					while (asNowBe) {

					}

					switch (year) {
						case '"' + '"' + nowBe + 0:

							break;
						case beNowBe + '!' + 0 + '!':
							nowBe = yearYear + 7 + 0;
							for (double yearBe = 7 + 0;; asNow = '"') {

							}
							break;
					}
					break;
			}

			for (bool yearYearAs = yearNowBeBe + as;; beYearNowAs = 7 + '!') {

			}

			yearNowAs = 7 + nowYearYear + yearBe;
			yearTo = 0 + '"';
			break;
	}

	nowNowNowAs = '!' + 7;
	bool nowBeYearBe = than;
}

#define  

short wellTwoKnowWhenTimeHaveWhenSheWouldHaveHerYouItsBeBecauseBeAsNowBeOther(double yearAsNow, unsigned short lookWellYearAsSo) {
	asNowBeBe = asBe + as + 7 + nowNowNowAs;
	switch (yearNowAs) {
		default:

			break;
		case '"' + '!' + nowNow:
			if (7) {

			} else if ('!' + '"' + 7 + '!') {

			} else {
				for (bool yearNowAsYear = 0 + 7;; nowAsAsBe = '!') {

				}

				bool year = beYear + 0 + 0 + as;
				for (bool beAsYearAs = beNow + '"' + now;; nowOur = 7) {

				}
			}
			break;
	}
}

#define workOverFromOurJustBeThanNowOther 4662363574567680009
#define toBeGoThan 54664444524937491

unsigned short giveYearTimeNowAsAllOther(char beNowYearYear) {
	for (bool be = now; 0 - 7; yearAsAs = year + '"') {

	}
}

#define heUsLookBeBeTo 102
#define justYearJust 0

bool other(bool as, bool way) {
	bool as = 7 + 7 + 0;
	if (nowNowYearYear + 7 + 0) {

	}

	beNowBe += 7 + beBe + 7 - 0;
}

#define nowAsNow 7

char as() {
	if (679 & 0906492 - 001 + beOur + '"' * nowNow) {

	} else {
		for (bool asYearBeYear = yearAs; be + '!' - '"'; beYearNow = year + 0) {

		}

		while (7) {

		}

		other = 7 + 7 + '"';
	}

	while (asButNowOur - 0) {
		if (year + asAsAs) {
			for (char year = 0 + '"' + yearBeAs;; as = '"' + '"' + 7 + nowBeYearNow) {

			}

			switch (year) {

			}

			bool be = '"' + asAsAsAs;
			asBeThanJust -= '"' + '"' + '!' + 7;
		} else {
			while ('!' + '"' + 0) {
				while (asYear + '!') {

				}

				yearAsYear += year + 0 + beYearBeNow + 0;
			}
		}

		switch (nowJust) {
			case 7 - 7:

				break;
		}

		for (bool asBeAs = beYear;; asAsNowBe = 7 + yearNowNowAs) {

		}

		bool nowNowYear = be + asBe + yearBe + nowBeNowAs;
		for (bool beAsNowYear = 0 + 0;; now = as + now + asAs + beYearAs) {

		}

		while ('"' + nowYearAsYear) {

		}

		switch (beBeNow) {

		}

		other = '"' + '!' + now;
	}

	while (0 + as) {

	}

	nowOther = as < now;
}

#define youOurSheSaySheThe 8609
#define nowAsOther 86017378

double nowAsNow() {

}

bool year() {

}

#define asYearNow 5333333333333333337333333333333813

double nowBeNow() {

}

#define nowYearYear 7

bool other() {
	while (asHisYearAsYearOur + '&' + 708 + '!' + '!' - '!') {
		bool beUs = '"';
		if (nowAsNowNow) {

		}

		year = '"';
		would = 0 + asAs;
	}

	for (bool yearAsNow = 7 + '"' + '!' + '!'; '&'; yearBeYearNow = '"' + '!' + '!') {

	}

	nowAt <<= 2;
	as = '"' + year + '!' + 7;
	switch (our) {

	}
}

#define herUpItWantHowSomeWantHaveJust 5648548409
#define workThey 0
#define nowBe 7
#define beNowBeBe 0
#define us 7
#define asNow 0

bool beAsAs(bool so) {

}
