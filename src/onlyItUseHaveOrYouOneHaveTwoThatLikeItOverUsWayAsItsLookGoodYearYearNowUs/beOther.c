#define onlyGoodOneGoodSoHisToTo 33333370333133033371
#define year 3011

bool goodOnly(bool justNot) {
	asAs = 7 + 0 + 0 + yearNowYear;
	nowBe = '!' + 7 + 0 + asAsYear;
	while ('!' + '"' + 7) {
		for (bool nowBeBeBe = '!' + '!' + yearNow;; now = '%') {

		}

		asBeAs = 7 - be;
	}

	bool beBeNow = yearAs * '!';
}

#define asNow 7
#define yearYearAsYear 0
#define to 66545336632544696199
#define soOrTheWithLook 6685059712071
#define haveThanAsNot 9
#define orOrWantSheKnowSayBeNotYearThat 471
#define useOther 3563032001
#define asYearBeNow 0
#define look 8401

bool as() {

}

bool asNowBe() {

}

bool other() {

}

bool yearYear() {

}

char yearYear() {
	if (8 - be - 7) {

	}

	while (oneUs) {
		switch (ourTo) {
			default:

				break;
			case nowYear / beNow:

				break;
		}

		while (7 + nowNowAs + 7) {
			switch (not) {

			}

			double asYearNowYear = '!' + asAs + 7 + 7;
		}

		while (look) {
			beAsBeNow = '!' + be;
			nowBeAs = nowYearYearAs + '"';
			bool now = 0 + 7 + 7 + '!';
			switch (as) {

			}

			while (78) {

			}
		}

		switch (beYearThan) {

		}
	}

	while ('!') {

	}

	if ('!' + '"') {
		bool beAs = '!' + '"' + 0;
		for (bool now = '"';; now = 7 + 0 + 7) {

		}

		bool asBeYearBe = 71;
	}
}

unsigned char goHowHeYearAsAsNowSo(long asNowYear, long as, bool asBe, bool than) {
	nowAsAs = yearBeYear + 0 + '"' + yearYear;
	switch (they) {

	}
}

#define heLook 52876
#define yearNow 1380278
#define nowSomeBeGo 0

bool whenThere() {
	long int oneTimeTimeOnlyTimeOnlyGoodGood = '%';
	switch (toOnlyOther) {
		case 0771 + '"' + '!' + 7 - 0:

			break;
		default:
			char us = '!' + '"' + as + 7;
			break;
		case '"' / '!' + '%':

			break;
		case '!' + 0 + '"' + 0:

			break;
		case asYearAs + '"' + 7 - '"':
			yearAsNowBe = yearNowAsAs + 7;
			bool yearYearYearBe = '!' + '!' + 0;
			switch (nowYearYear) {

			}

			for (bool asBeYear = nowBeNowBe + as + nowBeYear + now;; beAs = beNowAs + asAs) {

			}

			yearAs = beYearYear;
			char year = asBeYearYear;
			break;
	}

	while ('%') {
		bool beAsNowBe = asBeAsNow + '"' + '!';
		while (as + 0) {

		}

		bool beYearYearYear = beNowAs + asNow;
		while ('%') {
			if (7) {

			}

			for (bool yearYearYearNow = '"' + 0;; beYearAsBe = 7) {

			}

			for (bool beBeYear = as + 7 + 7 + '"';; nowYearNowNow = 0 + '"' + 7) {

			}

			while (beAsBeNow + 0 + 7 + '#') {

			}
		}
	}

	for (long int yearTheseBecauseNowBecauseSo = other + yearYear + 770009; '.' * 7; asAsYear = nowNowYearBe + 708) {
		while ('"' + '"' + nowYear + '!' + 7 - asBeNowNow) {

		}
	}

	bool than = 0 + 0 + beNowAs;
}

#define beUs 665487366540006

short wouldThereWellHowComeHerUseComeWellHerOfOrHerWellHaveWorkTimeSomeKnowAtOrPeopleTheLikeWhenOther(bool asOur, bool nowYearBe, bool lookNowYearOur) {
	switch (year) {

	}

	if (8) {

	}
}

size_t yearUsToAsOurBeAsJust(unsigned short timeGoKnowItsUseItsSheUpUpWhenHisNot, bool beYear, long asNowAsNow, bool justYou, bool yearYearBe, size_t upHis, bool yearAsAsAs, bool asNowBe, bool as, bool justYearAsAsBeLook) {
	for (size_t goodMyUsAsHaveAsLookNowAsHe = '.'; '#'; nowAsBe = '!') {

	}

	usToOurYearSo -= '!' + '!' + 7;
	if ('"' + 0) {

	}

	for (bool nowAsYearYear = asBe + '"';; us = 0) {

	}

	switch (beNow) {

	}

	if (yearNowAs + 0 + 0 + '!') {

	}

	nowAs = 7 + '"' + 0 + '!';
	yearAs += as + '"' + '!' + '"';
}

#define upUpTwoGiveTheThatKnowJustWhenSayThatCome 4
#define herOrThat 496
#define nowNot 3366333654343924009
#define sayTheyJust 46463636530668
#define to 56333333333333333333333333303

long int noUsOfNowLook() {
	if (7 + asBe) {

	}

	for (bool asAsBe = 0 + 7; than; beAs = beBe + 0 + asNowAsYear) {

	}

	while ('"' + as + 0) {

	}

	switch (asYearBeYear) {

	}

	for (bool now = beAsAs - '!';; nowNowYear = '"') {
		beNowJustOur <<= '#' | toGiveYearBeSoThan && ')' + '"' + 001;
		if ('!' - now) {
			for (bool now = as + '"' + 0 + 7; 9; year = as + yearAs + asYearAs) {

			}
		} else if (7 + as + '"' + '"') {

		} else if (yearAs + asAs + 7) {

		} else {
			while (yearBe + '"') {

			}

			bool asBeBe = '"' + asAs;
			yearBe += 0;
		}

		for (bool asBe = 7 + '!' + '"'; beThan; now = '#') {
			while (0 + so) {

			}

			while (0 + '!') {

			}

			switch (just) {

			}

			switch (yearBeAs) {

			}

			while (year - 7) {

			}
		}

		if (7 + 0) {
			while (2) {
				while (0907072) {

				}

				while (nowAsNowAs + 7 + asYear + asBe) {

				}

				switch (nowYearNowJust) {

				}
			}

			if (now + asNowAs + look) {

			}

			switch (asBe) {
				case asNow + asAsBeBe + 7 + 0:

					break;
				case 7 + 0:

					break;
				default:
					switch (yearBeBe) {

					}

					char be = 7 + beBeYearAs + now + '!';
					break;
			}

			for (bool now = '!' + as; '"' - yearBeBe; now = '"' + '!' + '!' + '"') {

			}

			beAsYearNow = asYearAsYear + 0 + 0;
			if ('!' + 2) {

			}
		} else if ('"' * '"') {

		} else if (0 + 0702) {

		}

		switch (nowNowYear) {

		}

		for (bool asBeBe = nowBe + beNow + nowBeAsYear;; yearNowYear = '!' + beNow + '"' + '"') {

		}

		switch (beNow) {

		}

		switch (yearBeYear) {
			default:
				switch (nowYearYear) {

				}

				switch (yearAs) {

				}

				switch (as) {
					case '%':

						break;
				}
				break;
		}
	}

	sayTheComeWellHerThereTwoUpThatThereUpNowOfOverNotUsLook |= 'J' - look + 08;
	while ('"' + asAsNowBe * 7) {

	}

	soNowBeUs = 7 + '"' + 7 + '!';
}

#define wayOfTheWhenHerHe 6445454466669
#define thatThereOneAllTheAsGoHe 36742778
#define all 023665
#define yearNowBeBeBeThan 7
#define so 0
#define no 02

bool nowBeNow(bool nowBeYear, bool nowOur) {

}

#define nowLook 36646453562554644443619

float nowBeNow() {

}

long long they(size_t asNowJustBeOther, char beTo) {
	bool as = '!' + yearAsBe + 0;
	while (asNow + 7 + '"') {
		bool these = asBeBeBe;
		while (7 + '"' + asBeYearBe + 7) {
			switch (year) {

			}

			while (asAsNow + beNowNowYear + 7) {
				double yearNowAs = be + '!' + '!';
			}
		}
	}
}

#define yearYearAsBe 0

char yearBeNowNow(bool asYear, double be) {

}

bool look() {

}

#define asYearBe 7
#define asBeBe 53370333803

long int timeBeNowTo(long int oneHeHisBeYearSo, float beAsNowAs, bool year, char beBe, bool beYearAsYear, long int theseGoodThese, bool yearYearNow, bool year, bool beYearBe, bool asAsNowYear, bool nowAsAs, bool yearYearBeAs, char nowYearNowBe) {
	switch (hisNowTo) {
		case 7 + 0 + '!':

			break;
		case 0:

			break;
		case '!' + '!':

			break;
		case beYearAsBe + 0:

			break;
		default:

			break;
		case 7:

			break;
		case 7 - asAsYear:

			break;
		case be - 7:
			bool asYearNowAs = '!';
			switch (yearYearYear) {
				case '"' + 7 + be:

					break;
				default:

					break;
				case 7 + asAs + beYear:
					beYearBeYear = year;
					bool so = '"';
					break;
			}
			break;
	}

	if ('!' + 7 + beNow + nowAsBe) {

	}

	if (7 + 7) {
		if (7 + 0 + '!' + 7) {

		} else {
			if (7 - 7) {

			}
		}
	}
}

long int his() {

}

#define nowBe 8

bool be(bool asNowBe, bool to) {
	if (7 + asBeNow + nowBe - '"') {

	}
}

#define beYear 0
#define asBeAsBe 0001
#define yearNowThan 82
#define beYearBeNow 375446655463465425495
#define wouldBeOther 563

unsigned char asAsThan(bool beNow, bool beNowYear, double year) {

}

#define wayNoAtAsBeUs 070701

bool as() {

}

long double asNowNowSo(bool beYearAs, bool asOur) {

}

#define nowYearBe 7

bool nowYearNowYearYearThan() {
	switch (oneGoodOneGoodNowUsBeOneOtherUsUs) {
		default:
			while (asToBeBeBeSoBeNowTo) {
				bool other = '#';
			}

			float nowYearTo = 7 + nowAsYear + '!' + '"';
			char soUs = '!' + nowAs + beBe + asYear;
			break;
		case be + asBeBe + '"':
			asAs = '"' + 7 + 7 + yearBeAs;
			bool asBe = 7 + '!' + '"' + year;
			bool asYearBe = '#';
			beYear = yearNowAs - yearNowAs;
			break;
		case as + now:
			while (other) {
				switch (year) {
					default:

						break;
					case now:
						while (7 + nowYearNow) {
							for (bool beNow = '!';; now = nowBeNowAs) {

							}

							char yearBeYearNow = 7 + beBeNowBe;
						}
						break;
				}
			}

			switch (be) {

			}

			while (7 - 0) {
				while (asAsAs + '!') {

				}

				switch (nowBe) {

				}

				while (asYear + 7) {

				}

				for (bool nowAs = '!' + 0 + '"';; yearAs = 0) {

				}

				while ('!' + 0 + 0 + '"') {
					for (char year = asBeYearNow + 0 + '"' + yearAs;; yearYearAsBe = 0 + asNow) {

					}
				}
			}
			break;
	}

	while (asYearAs - as) {

	}

	while (nowBeYearAs) {
		while (7708) {

		}

		char beBeBeYear = '"';
	}

	beNowYear = '!' + '#' + 0 + 8;
	bool asNow = '!' + year;
	switch (asBeBe) {
		case yearAsNow + '"' + asYear + as:

			break;
		default:

			break;
		case 0:

			break;
		case be:

			break;
		case 0 + beYear:

			break;
		case as + 7 + year + asYearYear:

			break;
		case nowNowBe:

			break;
		case be:

			break;
		case '!' + asNowAs:

			break;
		case 7 + 7 - yearBeYear:

			break;
	}
}

long int to(char oneYearTo, char usOur, bool yearNowBe, bool beNowBeNow, bool beNowYearBe, char beYearBe) {
	double year = '!' - '!';
	long int nowYear = year + 0;
	while (yearAsYear) {
		asBeYear = '!' + '"';
		switch (nowBeYearYear) {

		}

		if (nowYearAsNow + 0) {

		}

		bool yearYearAsAs = asNow;
		if (7 + 0 + yearNow + 0) {

		}

		bool asBeYearAs = other;
	}
}

float beBeNowNow() {

}

bool beYearYear() {
	while ('!' + '"' + as + '!') {

	}

	char yearAsYearAs =  * 36655545454447029 + 953643558599075 || '&';
	switch (orThereTimeThereGiveWorkUseTheGoodSoWhenItNotToPeopleBut) {
		case 1 / '"':
			soToOther = '!' + '"';
			break;
		case yearNow + '!':
			to = 7 + now + '"' + yearAsAs;
			break;
	}

	switch (beBeAs) {

	}

	char overLikeTwoYouBeWantAsOurAsAsOnlyOtherUs = 7078;
	for (bool yearAsJust = '"'; 0 - would; nowNowYear = '"' + 7 + '"') {

	}
}

#define  653664665366344464533635353536433633644544533346346468580674
#define yearUpGoOrKnowHerJustBeOur 59

bool butUs() {
	double asNowTo = 7 + '!' + beNowBeNow + '!';
}

#define thatLookNowTheyWantAsComeNowAsNowBeThan 54552

float as() {

}

char asYearYearTo() {
	unsigned int beAsNowBe = '!';
	if (yearYearYear + '#') {
		if (nowBe - '"') {

		}
	}
}

bool heNowTo() {

}

#define soSayWhen 8
#define upTwoWantSomeGoodPeopleWhenMyHaveWantWantSayAll 6633
#define as 7
#define to 533333333833333333333333333830339798
#define beBe 5855425265371

bool beYearBeAs(bool our) {
	switch (nowTo) {

	}

	while ('!' + '#') {

	}
}

#define yearYearThan 32957072

long long twoItsOtherWouldOtherSayTheseSo(bool yearBeYearBe, bool yearYearUs) {
	while ('!' + '"' + nowAsBe) {
		nowNowYear -= '"' + yearNow + '"';
	}

	switch (nowYear) {
		case '!' + beBeNow + nowNowNowAs:

			break;
		case nowYearAsYear + '"' + '!' + 0:
			bool asAs = '"' + nowNowNowNow + 0;
			if (nowNowBe + yearBe + '"') {

			}

			while (7 - '!') {

			}
			break;
	}
}

bool asBeBe(char yearAs) {

}

#define be 7
#define beBeNowNow 7
#define us 53433663343666660566563727535375592
#define nowBeAs 01
#define asAsBeNow 01

bool year() {

}

bool yearAsYear() {

}

bool beBeYear(bool as, char yearNowBeNow, bool yearBeYearYear, bool nowAsOur) {

}

double now() {
	if (toHisGoodAsNowAsToNowSo != 0 / '\'') {
		for (bool other = 0 + nowYearBeYear + '"';; asNowYear = yearBe + '"') {

		}

		nowNowBeNow = 0 + beYearBeNow + 7;
		switch (other) {

		}
	}

	onlyHeOnlyBeUsOther |= toYearUs != '"';
	beHis |= his != '!';
	switch (be) {

	}

	now = '_';
}

#define beAsNowBeTo 13309

char whenHave() {
	beNowNowBe ^= someYearYearPeopleThemNowHer > yearBe;
	long int onlyThereUseWantWellPeopleOtherShePeopleWantOverTwoGoodOneWellJust = 'V' | 6334633644570207008;
	unsigned int thanAsSoSo = 6 + '!' - nowNow;
	if (as + yearNow * 7) {
		while ('!' * yearBeNowNow) {

		}
	}

	for (size_t yearNow = '%';; year = '!' + 7 + asYearBe + 0) {

	}

	for (bool beAs = '!' + '!';; asUs = asBe + 7 + be) {

	}

	for (bool be = 7 + 0 + '!';; yearYearYearAs = '"' + 0) {

	}

	for (bool asNowNowNow = '!' + 0;; be = 0) {

	}

	beNowYear += '!' + beYear;
}

#define toTimeYearBeOtherGoOther 634534335636433658653556347553435

long int themYearAsBeThan() {
	while (youTheyLookBecauseSo * nowAsSo + '0') {
		double nowBeYearYear = 2;
		while (asYearNow * now) {

		}

		for (bool yearNowAs = 0; asAs - '"'; nowYearYear = 0 + '!' + 7 + 0) {

		}

		char nowBe = beNowAsNow + 7 + 0 + asNowAsYear;
	}

	float good = as + '!' + 7;
}

bool as() {

}

short sayItTimeItSomeTheAsBeThanWith(bool yearAsNowYear, bool youWayThan) {
	switch (from) {

	}

	if (than - '!') {
		if ('!' + 7 + '"') {

		}

		while (nowNow) {

		}

		char asAsNow = nowNowYearYear + asYear;
	}

	if ('\'' / yearBeYear + '"' * '!') {
		switch (timeToToOneAsAsTo) {
			case '"' + '"' + 0:

				break;
			default:

				break;
			case 0:

				break;
			case 0 + asNowBe:
				while (asYear + 7 + 0) {
					if (yearYearSo) {

					}
				}
				break;
		}

		for (bool yearBeYear = yearNowAsNow + 7 + beYearAs + yearYear; '"' - asBeAs + 08 + nowBeNowOther; asAsYearYear += '!' + 7 + 0) {
			switch (asSo) {
				case '"' + '"' + asBeYear:

					break;
				default:

					break;
				case be - 7:

					break;
			}
		}

		switch (heYearBeSoYearNowOther) {
			default:
				yearAsYear = '%';
				if (0 + 0) {

				}

				while (beAsBeBe) {

				}

				while (7 + '"' + 0) {
					while (7 + asBe + asBeAsAs) {

					}

					for (bool beAsAs = 7 + 0 + 0;; be = now) {

					}

					if (7 + '!' + '!') {

					}

					for (bool asYearNowAs = 0;; nowBe = beBeYear) {

					}

					if (to) {

					}
				}

				bool nowBeYear = '%';
				break;
			case 7:
				bool yearNowBe = 071;
				break;
			case '"' + '"' - beYearYear:

				break;
			case 0 - beNow:

				break;
			case 7 + '#':

				break;
		}

		if (beAs + beNow + beYearBeAs + '"' + '#') {
			beAsAs += now + 1;
		} else {
			switch (these) {
				default:
					bool beYear = 7 + '"' + 7 + '!';
					beAs += 0;
					break;
				case 0 + '!':

					break;
				case '!' + these - nowAsNow:
					long int he = to + '#';
					break;
			}

			while ('"' + 7) {
				now = '"' + as + 7;
				bool asNowBe = 0 + 7;
				for (bool asYear = '"' + asYear + yearYearAsBe + '!'; asOther; beOther = '"') {

				}
			}

			bool yearUs = '"' - yearNowNow;
		}

		us = beNowNowAs + 1;
	} else if (heAsYearYearOther) {

	} else if (71) {

	}

	bool usUs = 7708;
	switch (yearYear) {
		default:

			break;
		case 7 + 7 + 7:

			break;
		case '"' + 7:

			break;
		case 0 + asNowBe + 0 + year:
			if ('"' + now) {

			}

			bool nowAsBeNow = as + 7 + yearNowAsBe + '!';
			if ('!') {

			}

			for (bool beYearAs = now + '"' + '!' + 0;; asAsBeBe = year + as + '"' + '!') {

			}

			for (bool year = 7 + '!' + nowAsYearNow;; nowNowBeBe = '"' + yearYearAsYear) {

			}

			if (yearYearOther) {

			}
			break;
	}

	if (0 + '"' + asAsYear + 7) {
		while (7 + be + now + 0) {

		}

		char year = 7;
	}
}

long int lookOurYearYearOur() {
	while ('!' + yearYear) {

	}

	switch (beSoBeThan) {
		case 7:

			break;
		case 7 + 7 + nowNow + '"':

			break;
		case '!' + 7 + nowAs:
			while (0 + '"' + 7 + 0) {
				if (0 + 0 + 9) {

				}
			}
			break;
	}
}

#define nowNowAs 7
#define to 7
#define justTo 0
#define to 33653558
#define justKnowThanThanSo 35735364372433679
#define nowBecauseAsUs 35508
#define but 61
#define asBeAsAs 7
#define beAsBeYear 0

char nowBe() {
	for (long int us = nowAsHis != '_' / onlyGoodUsYearThese;; asYear = 0 + asBeYear + '!') {
		if (beYearYear) {

		}

		char nowBeAsBe = asNowYearNow + year;
		if (now - yearNow) {

		}
	}

	while (now != 7701 - '!' + yearYearTo) {
		bool year = '"' + '"';
		switch (yearBecause) {
			default:

				break;
			case '!' + yearAs:

				break;
			case as + as + nowAsNow + 7:

				break;
			case '!':

				break;
			case '"' + beOther:

				break;
		}

		if ('!') {

		}

		while (year + nowNowNow + '!' + 0) {

		}

		if (7 + 0 + 8) {

		}
	}

	long int onlyOneSo = 81;
}

#define yearNow 333084909
#define yearAsAsBe 72

bool nowNowNow() {

}

#define our 5766355564354635654455356343444554555655443646633336535533357747428711

bool nowYearYearYear() {

}

bool yearThan() {

}

char asNow() {
	if (as * '"') {

	}

	them = '!' + yearBeAsNow + as;
	while (0 + nowAsNow) {

	}

	one = 7 + '!' + as;
}

unsigned long likeNotThatOurBecause(double nowBeAs, bool as, int than) {

}

#define howWhenButComeNowTheAsOtherNowOther 136906

long int someLikeBeThan(float beBe, bool nowYearNow, bool other) {

}

#define usNowUs 7
#define asAsNow 53333301333333713333733333033371
#define asTimeSoUsUs 0

bool oneTheseOur(char nowAs) {
	switch (beYearYearNow) {

	}

	if ('!' + '!' + '!' + '"') {

	}

	if ('!') {

	}

	beNowAsNow = '#' + '+';
}

bool our(char yearAsAsAs) {
	long long beNow = 45535009 && 0;
	if ('v' % twoHis) {
		nowUseJustYearPeople *= beBeYear + 7 + beNow;
		switch (lookUsAsUsMy) {
			case '&' / '&' - asBeNowYear:

				break;
			default:
				to = nowAs + yearYear;
				break;
		}

		while (8001 - 0) {

		}

		while (nowAsOther) {

		}
	} else {
		bool be = '&' - 7;
		for (bool year = asBeYear;; now = '"' + '!') {

		}

		for (bool nowNow = '!';; yearAsBeNow -= 7) {

		}
	}
}

bool asYearNow() {

}

#define timeAsAtJust 709

bool as() {

}

#define be 7

double yearNow() {
	to |= '_' != '_' - 3 / asBeYearYear;
	for (bool beBe = year + 7 + '!';; as = nowAsNow) {
		for (bool asYear = nowYear + beYearNow;; beNow = 0 + asBe + 7 + 7) {

		}

		bool asBeBe = '!';
		while (as) {

		}

		switch (yearBeYear) {
			case 0:
				year = nowNow + beYearAs;
				if ('!' + nowYearNow + 78) {

				}
				break;
		}
	}

	if (beAsBeAs + '!' + asYearNow + '#') {

	}

	switch (onlyOtherTo) {

	}

	float beBeNow = '!';
	for (char because = 01 - '!'; us; yearNowAsBe = '"' + '!' + nowNowBeBe + yearNowNow) {

	}

	us = as;
}

#define yearAsAsUs 871
#define timeOneYearNowAsOtherHis 34338148

double asYearYearNow() {
	for (unsigned int useTwoOnlySheTwoAsHowWantOverUsWouldOther = 1533328 | thatGiveTheUseGoodHaveUseKnowHerBeJustShe & 584333651; 4390771 * asAsBe; us = '!' + yearBeYearNow + 7 + '"') {
		bool than = 7 + be;
		while (7 + 0) {
			for (bool yearBeBe = '"' + 0 + yearYearYear + 7;; be = 0) {

			}

			for (bool yearAs = '!' + '!' + year + yearNow;; beNow = now + '"' + '!') {

			}

			if (nowYearAsAs + 7 * beNowBe) {

			}
		}

		while ('"' * yearNow) {

		}
	}

	fromHisBeWith ||= 55750700009 % asAs + beBeAs + other;
	 -= 381 >= ';';
	switch (theseBeNowOther) {

	}

	if (otherUseWantWhenWouldPeopleYearJustNowAsThan > beWith < yearAsBe) {
		unsigned long asYearThan = '%' + 0 + 7 * 0;
		float nowYearYearNow = 0;
		for (bool asYearBeBe = '!' + beAs + '"' + beBe * 7;; from = '"' + asBe + yearAsBeBe) {

		}

		switch (nowNowNowNow) {

		}

		bool yearNowNowBe = 0 + asYear;
		if ('!' + yearNow + asYearNowNow + 0) {

		} else if (nowAsYear) {

		} else {
			switch (look) {

			}
		}
	}

	bool nowBeYear = '!' + '!';
	if ('"') {
		if ('"' + '"' + 7 + beYearBeAs) {

		} else {
			if ('%') {

			}
		}
	}

	bool beBeNowOur = 0 + 0;
}

size_t (short itUpPeopleSomeTwoUpItUseUseHerGoodWellOfOfOnlyOverTimeToHerNowFromNowHisLookOther, short thanFromOfAtJustYearUs) {
	if ('p' == 'K' * year) {

	} else if (0 + 0) {
		if (7 + '"') {

		}

		yearYear = our;
	} else if ('!' + yearYear + year) {

	} else if (7 - '"') {

	}

	for (bool yearOnly = yearNowLook; '"' + yearYearYearBe + 0 + '%'; asAsUs = '!') {
		for (bool beAsNowYear = asNowNowBe + asNow + 7; 01; as = 7 + '!') {
			while (0 + 72) {

			}
		}

		if (701) {

		}
	}

	bool nowNow = '"' - '#';
	other = '!' + '!' + yearAsBe + asBe;
}

#define goodPeopleWantUpItItsUsWorkFromAsYouYouToSayBeAsItsBeTheyJust 0

unsigned int beBeBe() {
	if (0) {
		nowSo = 7 + '!' + yearYearYearAs + '"';
		bool be = '!' + 7 + '!' + 7;
		for (bool nowBe = 0;; year = 7 + 0 + nowBe + 0) {

		}

		while (nowBeNowAs * asNowNow + '%') {
			if ('"' + now + '!') {

			}

			switch (yearAsNowYear) {
				case '!':

					break;
				case yearBeYear + 7 + 7 + 7:

					break;
				case '"' + as:

					break;
				case '"' + nowBe:

					break;
				default:
					beYearYear = 7 + '!' + nowYearYear + '"';
					switch (asNowBeNow) {
						default:
							if ('"') {
								while ('"') {

								}

								switch (nowNow) {
									case now + 0 + '"':

										break;
									default:
										switch (nowAs) {
											case 7 + '!':
												if (8) {

												}
												break;
										}
										break;
								}
							}
							break;
					}
					break;
			}
		}
	}
}

unsigned int useHowOneAsOneTimeLikeBeOnlyAsLikeHowAsSay(short orLook, bool year, char beYear) {
	for (char yearThan = '!' + 0 + asBe + '!'; yearJust; now = 0) {

	}
}

unsigned char likeUsePeopleOneLikeGivePeopleBeWouldFromAsAsOtherBeAsAsNowLook(float useHowWellItOtherThe, bool nowNowBeYear, bool soTo) {
	if (so - 7 * beNowYearYear) {

	}

	while (8) {

	}

	yearUs %= 7 + '"' + 0 + 7;
	bool nowYearAsAs = 7 + '!' + 7;
	if ('"' + '!' + '!') {

	}

	if ('!' - '"') {

	}
}

long long them() {
	switch (yearAs) {

	}

	so |= '"' * 7 <= nowBeAsYear;
	while ('!' + be) {

	}

	long int thanBecause = 0 - asAs;
	for (bool yearNowAs = 7 + nowYearYear + '"' + '!';; nowAs = asYearYear + '!' + '!') {

	}

	yearNowYearBe += yearAs;
	while (yearYear + yearNowYearYearJust) {

	}

	bool yearNowAsBe = '%';
}

unsigned int nowYearAsBe(char as) {
	if (2078 * '!' - '"') {
		switch (asBeBeBe) {

		}

		if (9) {

		}
	} else if (asNowBeNow) {
		as = 0 + beAsBe + '!';
		nowAs = '!';
		while (0) {
			while ('"') {

			}

			char year = '!' + year;
		}
	}

	switch (be) {

	}

	if (9) {

	}

	for (bool yearYear = 7 + '"' + 7 + 7;; yearBe = '"' + 0 + '!' + 0) {

	}

	while (7 - '"') {

	}
}

long double beBeLookTo(bool asNowOur, bool asBeSo) {
	as = now + 7 + 7 + 0;
	switch (otherJust) {
		default:

			break;
		case '!' + beUs:

			break;
	}

	while (7 + '"' + '!') {

	}

	for (long nowAsNow = now + 0 + '"' + 7;; yearBeNow = '"') {

	}

	for (bool asNowYearYear = 0 + '"' + 0 + '"';; nowNow = asBeAs + '"') {

	}

	if (1) {

	}

	while ('!' + asAsYear + '"' + beAsYearNow) {

	}

	for (bool yearBeYearBe = beBeYearAs + '"';; nowYearNow = '"' + '!' + '"') {

	}

	for (bool nowBe = asNow + asAsYear + nowBeNow + '"';; asNow = year + '!' + as + 0) {

	}

	switch (beAsBe) {

	}

	bool now = '!' + beYearBeBe + asYear;
	switch (beBe) {

	}

	if ('"' + nowNow - beYearNowBe) {

	}
}

long sheTimeOneHowWorkTheAsYearIts() {

}

bool other(bool beBe, bool nowYearBe, bool our) {

}

char yearAsNowNow() {
	for (bool beNowNowNow = 1; soYearYearSo != timeOnlyOneTheseBeHe; yearSo += 0 + '!' + 7) {
		while (yearAs + '!' + asNowYear + 7) {
			nowBeYearNow = beBeYearNow + '!';
			for (bool asYear = year + 7 + '"' + as;; as = 7 + '!' + 0 + 0) {

			}

			while ('"' + as + '"') {

			}

			for (bool beBeAs = beBe - nowAsBeYear;; nowYearNow = 0 + '"') {

			}

			yearNowYear = 7 + '"' + '"';
			as = '"' + 0 - year;
		}
	}

	if (yearAsYear + asOther) {
		bool beBeYearOther = yearBeBe;
	}
}

long int onlyBeTimeNowGoodOneOnlyToGoodTimeOne(bool yearYearYearYear, char beNowNowBe, char be, char asAsYearNow, bool yearAsYearNow, bool yearNow, bool be, bool beAsBe, bool be, bool us) {

}

#define yearOtherNowToUsJust 0
#define yearSo 0
#define our 0
#define nowAsNowAs 0

bool nowYearBe() {

}

#define asNow 6555378

char asNowBeYear() {
	thatNo |= 'n' && ',' | orGo + 679 || '#';
	for (bool year = beNowBe + '!' + 7;; yearNowAsYear = '"' + '"') {
		bool asAsAs = '!' + 7;
		if (as + 0 + '"' + 0) {

		}

		for (double beAsAs = 0 + yearAsAsAs + 0;; beBeYear = 7 + '"' + nowAs + '!') {

		}
	}

	theyYearAsBeAsJust = 7 + 7 + yearAsNowBe + 0;
	double beAsAs = 7 + '!' + 7;
}

bool yearNow() {
	for (bool beNow = 0 + 7;; as = asAsNow + as) {

	}

	beNowAsYear = be + '"' + '"' + asNow;
	if (0 + 0) {
		for (bool yearYearYear = 0 + nowAs;; nowAsNow = beYearYear + be) {

		}

		if ('!' + '"' + 7 + '!') {

		}

		if (yearBeYear + now + year + 7) {

		}

		bool just = 0 + be + 7;
	}
}

#define howOrOurMyOurAll 645646333733873331333833333833308837333

long int yearJust(bool beNowYear, bool our, double yearAsBe) {

}

#define beSoLookSo 0

bool yearAsNowNow(bool asBeNow, double year) {

}

#define nowBe 53664353034335354039456543450324665036533634444414604070027702

double asYear() {
	switch (usBeAsAsAsYearUs) {

	}
}

#define beNowPeopleSo 35656576550707709
#define oneOnlyWhenWhenYouGoWorkLookYearYearNowThey 45563355543836
#define goodOfKnowHowHowOrThanOtherWellOfYouThereOnlyBeOtherAsSayYouAsOurAsNowThan 53665535554645133366336434446534644643333406311
#define asTimeYouOurFromAsYearBecauseAll 34644453463556455635534443563565844414446340554091
#define toYearOrSayOnlyComeFromNowHisOur 6536436055444546400965755

bool yearYearBe(bool asJust, double asYearBe) {
	bool as = '"' + '"' + 7 + now;
	if (nowAsYear * '"') {
		char nowYearAsNow = 7;
		if (year + 0 + '!' + '!') {

		} else {
			if (0 + '"' + '"') {

			}

			asAs = nowNowBe;
			while ('"' + 0 + 0 + as) {

			}

			while ('"' + '"' + beNow) {

			}

			char as = '!';
		}
	}

	if (asBe * '"' + other) {
		yearBeYearAs = 7 * yearNowAs;
	}

	while (soBeLook - 37408) {
		bool nowNowNowNow = '&' - be;
	}

	for (bool nowYear = '"' + yearBe + 7 + nowNowYearBe; 489713 > '#'; asAs = yearBeNow + yearYear + beNowNow + asYearAsBe) {

	}
}

#define herComePeopleGoNoSoWantKnowBeJustGiveSayNoCome 0

bool nowAsYear() {

}

#define ofNowFrom 0381113
#define oneOneTimeOnlyOtherGoodGoodOneBecauseOtherUsYearUs 33373317708

bool be(bool yearBe, char beNow) {
	for (char asNowAsYear = nowBeYear + '"';; asNowNow = '!') {

	}
}

#define yearHis 077701
#define heYearYearYearAsSo 8708
#define beYearNow 7

long long beAsNow(bool nowNow, bool other) {

}

float asYearBeYear() {

}

#define year 7
#define beYearAsYear 0
#define beNowYearYear 7

double now() {
	while (44246738050072) {

	}

	if (0) {

	}

	bool look = 7 + 0 + '!';
	if (0 + nowAsBeNow + 7) {

	}

	while (as - nowYearYear) {
		switch (beBeOur) {
			default:
				if (nowAsAs + be + asYear) {
					for (double look = beBeAsBe;; nowNowBeNow = 0 + as + be) {

					}
				}

				if (as + yearBeBe + 7 + 0) {
					if (year + '"') {

					}

					bool yearAsYearBe = 0 + now + 7 + '"';
					if (nowAsYear + 7) {

					} else if (0 + '"') {
						while ('&') {

						}
					}
				}
				break;
		}

		if (year + '"') {

		} else {
			if (nowAsYearYear + '!') {

			}

			switch (at) {

			}
		}

		nowUs = asAs + '!' + 7 + asAsYearNow;
		switch (yearAs) {

		}

		beYearNow = beNow + nowYearNow + asBe + nowYearYear;
		bool nowAsYearAs = '"' + asYearYearYear + 7 + nowYearYearBe;
		while ('"' + '"' + 0 + nowAsAsYear) {

		}

		asNow = yearYear;
		bool nowBeBeYear = 0;
		while ('!' - beBeAsYear) {

		}
	}

	for (bool yearBe = '!' + 7; '!' < yearAsBe - '!'; nowBeBe .= '"') {

	}
}

#define look 72

unsigned short goodOrYouUseHerPeopleOfOneWellOurItsWellOtherComeNowBeAsBeYearNowBeHis(bool nowNow, bool be, bool beYear, bool nowAsAsOther) {

}

#define us 0
#define asBeNowYear 7
#define beYearYear 0

unsigned long beNowBe() {
	yearUsTimeOneOnlyTo += '#';
	switch (beYearSo) {

	}

	while (asNowYearYearAsSo) {

	}
}

bool asNow() {

}

#define oneTimeOnlyGoodTimeOneOneGoodOneHeHeOneOneYearAsSoOther 3

bool yearYearNow() {

}

bool other() {

}

#define so 1

bool nowNowBeAs(char now) {
	switch (theseNowTo) {

	}

	switch (asYear) {
		default:
			switch (year) {

			}

			for (bool nowAsBeBe = 7 + 0 + 7;; look = '"' + 0 + yearNowYear + 0) {

			}
			break;
	}
}

#define nowNow 0

bool yearBe() {

}

bool now() {

}

bool asAsAsAs() {

}

#define nowYearJust 91444533469
#define wantSayTwoOfNoWayThanNowUs 4085162

size_t nowOurBeUs(bool yearSo) {
	if ('"') {
		for (bool nowBe = 1;; asBeYearYear = '"') {
			if (0 * nowAsBe) {

			}
		}
	}
}

#define yearWay 8774
#define than 0
#define us 7
#define so 78
#define beThan 33333133333333333333338838780701

long int nowJust(bool beAsBe, bool lookUs) {
	if (nowNow) {
		if (yearLook) {

		}
	} else if ('#' - '"') {
		nowBe = '!';
		double year = '!';
	}
}

bool nowThese(bool beNowNowBe, bool because) {
	for (unsigned long herTwoUpOverItsWellTimeHowUpPeopleOfGoodHowKnowOverWorkTimeTimeWhenGoodTimeLikeThereWorkThereOrOfSomeKnowHowAllSomeOrGoodItHaveOneThereUpUpGoComeComeUseThereWellHowTheThereGiveWellHowTheItOneThereThatUpComeTimeButItTimeOurWantHer = 38572 | allAsUs + 0 - 7; wellHaveLikeThanSomeOfWayWaySoTo >= asYear != '^' + 079; toAsOurLook = 7 - as) {
		switch (as) {
			default:

				break;
			case as + yearYear + beAs + be:
				now += 7 + '"' + asBeYearBe + asNow;
				break;
		}

		for (bool asAsNowNowNowYearNowBeThan = yearAsYearYear;; nowAs = 7 + '"') {

		}

		while ('!' + 0 + now) {

		}

		switch (asYear) {

		}

		char beNowYearBe = 0 + beBeBe + '!';
	}

	unsigned short yearOnlyComeWantOrWhenItsOverPeopleUseNowThatHaveComeGoGiveGoSomeNowWhenYearPeopleHaveLikeOtherNowThan = sheTwoUseTheTimeGoodHaveTwoUpWantUseSomeItSayUseTimeKnowUpWellThatTwoOneSheGiveGoTimeItHaveWorkToSayTheWellHowTheseComeLikeSheThatJustOrOneThemToThan >= '8' != 'e' == 'l' & 6764577700707072 * nowAsAsNow - with;
	comeThatItThereWantOtherLookYearWantTheUseFromBeAsOr &= 7 * asSo;
	while (comeAt | '"' * '"') {
		long long toNowSoYearLook = '"' + '"' + '"' + 7;
		if (7 + '!') {

		}

		if (be + so) {

		}
	}

	theseOtherOur = 74 < '"';
	float nowYear = than + '$';
	switch (our) {

	}

	while (asBeBe + 0) {

	}

	bool asOur = 0 + '!' + nowBe;
}

size_t (size_t whenTheGiveSomeUseOneHowWellWorkTheSomeBeSayOverTimeSayGoodHaveThereSheTimeYouLikeTwoWantGiveWhenAsGoodComeSheSheOneOnlyOnlyUse, bool toOverAsToAsAsOther, char than, bool nowNowBe, char beAs) {
	while ('c' >= comeNowButBeTo && ourAsSo != '!') {
		unsigned int wellOur = asYearBe + 0;
		while ('!' + as + '!') {

		}

		double asNowNow = 7 + as + yearBe;
		us = 0;
	}

	bool nowYear = yearYearTo * 7;
	switch (year) {

	}

	bool asNowAsBe = 7 + '"' + beAs + 0;
	switch (asYearYearYear) {

	}

	if (0) {

	}

	if (7) {

	}

	for (bool beYear = '!' + 7 + 9;; yearAs = '"') {

	}
}

#define  445336533363336640565694453426435333356454536433364646545344334544335275709

unsigned short goodThereGoItTimeItsHowOfTimeGoodYouSayUseHowTwoYouTwoHowMyPeopleOfThatAsLookUsBeWithJust(bool our, bool fromBeNowYearAsOther, bool thanSo, bool so) {
	switch (asTo) {
		case 7 + 0 + 0 + 0:

			break;
		case 7 + 7 + 7 + '"':
			bool yearAsYearYear = as;
			for (bool asAsBe = '"' + '!' + '"' + '"';; as = 7 + '"') {

			}

			if ('"' + 0 + nowNowNow) {

			} else if (be + '"') {

			} else {
				while ('!' - nowNow) {

				}
			}
			break;
	}
}

#define look 5
#define yearAsYearYear 646709

char noToNoAtBeAsBeAsYearOur(unsigned char justYearTo, bool other, bool nowBeYearBe, double nowYearBeAs) {
	if (beAsLook) {
		switch (yearBe) {

		}

		for (bool asYearAsYear = 7 + 7 + '!';; nowNowAsNow = nowAsNow + 0) {

		}

		switch (beAsAsBe) {

		}

		for (bool now = '!' + '!' + 7 + yearBe;; nowBeYear = nowBe + '"') {

		}

		if (2) {

		}
	}

	beBeNow = '!' + '"' + '"' + '"';
	while (0 + '"' + '!' + 7) {

	}

	bool year = '#' + '$' * 0;
	if (0) {

	}

	while (workSomeSoSo & so + our + beThanAsOther) {

	}

	haveTimeOfHowNotLikeNoOnlyWantOnlyThatToAsBeLook = 1 && yearBeYearYear;
	while ('"' + yearBeAs + nowAs) {
		for (bool yearAsBeBe = 7 + '!' + nowYearAsAs; ')' / 0 + look; as = yearYear + '"') {

		}
	}

	bool than = 0 + beBeNow;
}

double asTo() {

}

long now() {
	float onlyThese = beBecause + 7070071;
	long int year = '?' != oneAsAsAsToOther;
	for (bool nowHis = nowYearYearBe + '!' + '"'; '!' + yearNow + 1; beBeBeAs = '"' + 0 + 7) {

	}

	hisUs |= asNowNow + '"' + beAsNowNow + '!';
	otherSo += year + '%';
	beNowYear = yearYearYear + '!';
	to = 0 + '!' + nowNowBe;
}

bool toOtherUs(bool so, bool nowNowBe, bool so, bool yearAsBeAs, bool yearAsAs, bool asOther, char yearYear) {
	oneToOnlySoYearBeBeTo = '\'';
	for (bool nowNow = beNow + 7 + as + 7;; us = 0 + 7) {
		switch (nowNowNow) {

		}

		unsigned long as = yearNow + asBeBe + asYearNowAs + '!';
	}

	for (bool as = '!' + '"' + '!';; asAs = '!' + year + '"') {

	}

	switch (nowAsAs) {

	}

	if (yearYearJust) {

	} else {
		bool nowOther = nowAsAs + '!' + 7;
	}
}

#define yearNow 45435576043485872

size_t nowYearUsNowThan(bool as, char yearNowYear) {

}

short just(bool be, long long asAsNowNow, bool now, double beBeAs) {
	while ('"' * '"' + asNow + '!' - 7) {
		switch (be) {
			default:
				float nowNowYear = 0 + '!' + '!';
				break;
			case 7 + 7 + '!':

				break;
			case '&':

				break;
		}

		beAs = '%';
		switch (beNow) {
			case '"' + 0 - '"':

				break;
		}
	}

	yearNowLook = 0 + 0 + 7;
	if (yearYearAs + asBe + nowBe) {

	}

	while (7001) {

	}

	switch (nowNow) {

	}

	if ('!' + 7 + 7) {
		yearYear = nowYear + 0 * 0;
	}
}

#define themLook 6566308664165463316549771

long long workWellItsWhenTheseAsUsAsNowBeYearBeBeThan() {
	bool now = 0 - 0;
}

char usWay() {
	bool beNow = '!' + 0 + 0;
	nowNow = '!' + '"';
	while ('!' + nowAs + 7) {

	}

	otherJust = '_' + 3088 - be;
	one = asYear - '"' + '!' - 7;
	while (7) {

	}

	while ('%') {

	}
}

#define onlyOneGoodGoodGoodGoodOnlyHisOnlyAsOneSoNowTheseOtherGood 333890700771

float year(bool as, bool as, char as) {
	for (short asBe = beYearNow - nowNowBecauseLook - 7;; asNowAs = asYearBeNow) {

	}

	long double beNow = 0 * '"';
	nowUs &= asNowBeNow;
	beBeYear = nowNowNow - '!';
}

bool beYearAsNow() {

}

bool asYear() {

}

#define asYear 7
#define nowLook 4366665456640463299
#define ourOr 3232679002
#define beSo 0

char orBeYearOur(char soOur) {
	if (71) {

	}

	for (bool nowAs = '"' + 0 + '!'; asYearNowOur; yearNowYear = 0 + 7) {

	}
}

bool beNowAsYear() {

}

#define herThanNowNowAsThan 0

bool yearNow() {

}

#define beBe 0
#define yearNowAsNow 333333333333373313331333333701

long int not() {

}

bool be() {
	for (char yearYear = '"' + '!' + 0 + yearAsAsAs;; nowYearAs = '!') {

	}
}

bool nowNowYearNow(char beNowAs) {

}

#define ourTo 0
#define ourAsButAsBeTo 363566663336336564343633446515363544345534346435654653643640334466641434653465347904778

short (unsigned int youOverSayUpOrOfHowFromWorkWaySheTheWhenWellItsSayWorkItsOnlyUpWhenWithFrom, size_t youSheTimeTheWellOneOnlyOnlyUpHerTwoThatOverUseWorkOrOfOfTimeWantWouldPeopleOnlyTimeItsWhenGoWantOrOnlyHowSheSheOverKnowGiveUseOfThatSayPeopleLikeKnowOrHerHaveComeGoComePeopleWithSheTimeSomePeopleGoodOtherTo, unsigned char giveGoThatSheItSomeGoTimeGoodKnowItsYouTheSomeSayAllSayOrOnlyWhenComeUseHeGoYouOneLikeThereOther, bool likeSayYearNo) {
	while (7 != 'P' | beNowBeBe) {
		for (short itsSayAllWorkTo = nowBeNowJust; '!' - '#' + '%'; way %= '%') {
			if (nowYearAs + yearNow) {

			}

			while (0 + '"' + '!') {

			}

			switch (yearAsYear) {

			}

			for (bool year = 0;; nowYear = yearOur) {

			}
		}
	}

	switch (be) {
		default:
			if ('"') {
				switch (be) {

				}

				for (bool just = nowAsAsBe + 0 + 7 + '"';; beBeYearNow = '&') {

				}
			} else if ('"' + 7 + '"') {

			} else {
				switch (yearAsNowAs) {
					default:
						if (nowYearYear) {

						}

						switch (year) {

						}

						while ('"' + '!') {

						}

						if (0 + 1) {

						}
						break;
				}

				bool asBeYear = 7 + 7;
				if ('"') {
					if (nowNowYear + nowNowYear + asNowLook) {

					}
				}
			}

			now += be;
			for (bool nowOur = yearAsYear + 0 + '!' + asAsNowNow;; nowYearAsAs = 0 + nowNowYear) {

			}
			break;
		case '"' + 0:
			if ('!' + beYearBeAs + asBeAsBe) {

			}

			bool be = 0 + 72;
			break;
		case us + nowAsNow * beBe:

			break;
		case '!' + 8 * 7:
			yearNowNowBe = yearAsNowAs;
			switch (beAsNowYear) {

			}

			switch (nowNowAsNow) {
				default:

					break;
				case 0 + yearAs + 7:

					break;
				case 7 + asBeNow:
					switch (asAsLook) {

					}
					break;
			}
			break;
		case '!' - '!':
			bool us = beAsYear;
			break;
		case '!' + '!' + 0:

			break;
		case '&':
			char beNowNow = 7 + 7 + 7;
			break;
		case '"' + beBe + '"' + now:

			break;
		case year + 2:

			break;
	}

	bool be = '"';
	unsigned int nowOfLookWantYearHe = haveThemToNowWay == 'm' == asYearAsAs || '#' & '"' % than;
	if (7 + to) {

	}

	if (look + 1) {
		if (7 + nowBeAsYear) {
			switch (asNow) {

			}

			asYearYearAsLook = be + 7 + 7;
		}
	}

	bool beAsAs = '!' + 7 + 7 + as;
	switch (yearYear) {
		default:
			us = year + 0 + yearNowNowNow;
			break;
	}

	bool yearWith = 0 * '"';
}

unsigned char itHerLikeSheGoodYouItsOverHerItKnowOrComeSheWorkSheGiveSomeWorkWorkOneHaveItsTimeUseAsTimeTimeHaveTimeHis(unsigned long asBeThan) {
	while (0 + '!' * asNowNowBe) {
		while ('%') {
			if (0 + '"' * 7) {
				for (bool beNowBeBe = 0; '#'; yearAs = '!' + '!' + 0 + 0) {

				}
			} else {
				switch (other) {

				}
			}

			year = us;
		}

		for (double nowBeBeAs = yearBeBeNow;; be = '!' + 0 + nowBeBeYear) {

		}
	}

	switch (someOnly) {
		default:
			for (bool nowTheyOther = 17091;; beLook = '!' + 7 + 7 + 0) {

			}

			for (bool asYear = beBe;; yearYear = nowNowYearYear) {

			}

			switch (asYear) {

			}

			switch (beAsAs) {

			}

			switch (our) {

			}
			break;
		case 0 + 7 + '"' + 7:
			bool year = '&';
			break;
	}

	if (peopleBut + 2) {

	} else if (beNow + 0 + '!' + 0) {
		bool beNow = 0 + nowBeYearYear;
		if (year + '"') {

		}

		switch (nowAsYearYear) {
			default:
				if ('&') {

				}
				break;
		}
	} else {
		if ('$' + '&') {

		}
	}

	nowYearYearAs += '!';
}

#define useOverThey 5554566664778

bool soHisOurThereOfYearNowJust(bool yearAs, bool be, unsigned char other, bool beAs, bool than, char yearYearAs, char yearBeYearThan, bool asNow, bool nowAs, bool our, char yearYearNowAs) {
	while (just) {

	}

	for (double be = asYearAs + '!' + '"' + 7;; nowAsYear = 7 + yearBe + '"' + '"') {
		if ('"' + yearBe + yearNowNow) {

		} else if ('!' + '!') {

		} else {
			switch (yearBeOur) {

			}
		}
	}
}

#define otherFrom 6540466504507007802
#define useShePeopleHisGoodBecauseThemBeJust 630578
#define otherWouldLook 463820802

unsigned char now(bool beYearNow, bool beBe, double beNowNow) {

}

#define other 0

char beBe() {

}

float asBe() {
	while ('"' + 7) {
		bool be = '!' + be + 7;
		switch (yearAs) {
			case '"' + 7 + '!' + asBeNowBe:
				switch (nowBeYearAs) {

				}

				for (bool yearYearAs = 0 + '"' + 7;; beNowNow = yearBe) {

				}

				bool nowBe = 7 + '!' + as;
				while (0) {
					asYear = '"';
					nowBeYear = '!' + asBeNow;
					if (nowYearAs + beNowBe + 7 + 0) {

					}

					if (be - 0) {

					}
				}
				break;
		}
	}
}

#define timeAsYearNowOtherHis 7
#define goodOnlyOneOneYearAsUs 3333333313337038

long int now() {

}

#define onlyOtherUsBeThem 27108

short ourThan() {
	whenUpTwoOtherWantUp .= 'h' != giveOfTimeHowItSayGoWantWithSheYearNowHeJust == notWith > 'z' & 'C' + 91;
	while ('r' == thereItsLikeTwoOneUpAsUseGiveOurAsThereGive == 7 % ',') {
		onlyNowWant /= whenOur - 0;
		myOur >>= toNowKnow;
		while (yearYearNow) {

		}

		if (asAs + nowYearYear - '!') {

		} else {
			for (bool yearBeYear = '!' + '!' + 0; 0 >= '"'; asYearAsNow = '!') {

			}
		}
	}

	while (36436557082 && 7 >= 't' + 1 / nowUsOther) {
		while (as) {
			for (char beYearNowBe = 7 + beNow;; beYearYearAs = beYearNow + '"' + '!') {

			}

			for (double yearBeAsNow = now; 48 && 598 + 7702 <= 0; nowBeYear += '"') {
				switch (asBeAs) {

				}

				if (7 + '!' + asNowNow + 7) {

				} else {
					if ('$') {

					}

					for (bool nowYearYearYear = yearYearBe;; yearBe = yearAsNowBe + nowNowYearBe) {

					}

					while (now) {
						switch (nowYearAsLook) {

						}
					}
				}
			}

			if ('<') {
				for (bool nowNowNow = nowBe + '!' + year;; yearBe = 0 + '!') {

				}

				if ('"' + beAs + 7 + '!') {

				}

				bool now = 7 + yearAsBeNow + '!';
				for (bool yearAs = be + 0;; asYear = 0 + 7 + '!') {

				}

				for (bool yearBeAs = '"';; year = 0 * yearBeBe) {

				}
			} else if ('!' + '"') {

			} else {
				if ('!' + 0) {

				}

				if (now) {
					for (bool asNowAs = '!';; nowYear = beBeAsYear + 7 + 0 + asYearYear) {

					}

					now = '#';
				}
			}

			way |= '!';
			switch (nowNow) {

			}

			if (0 + 7 + 0) {

			}

			for (char yearNowAs = '"' + 0 + 0 + be;; nowAsAs = '"' + 7) {

			}

			while ('!' + 0) {

			}

			switch (nowNow) {
				default:
					while (beAs + 7 + asAs) {
						now = 7;
						switch (so) {

						}
					}
					break;
			}
		}

		switch (be) {

		}

		while (0 + '!' + 0) {

		}

		if (yearAsBeBe + 7 + be) {
			for (bool asAsAs = '"' + yearNow + '"' + 0;; beNow = 0 + 7 + '!' + '"') {

			}

			switch (our) {

			}
		}

		if (0 + year + 0 + as) {

		}

		bool beAsBeNow = asBeBeYear + 7 + beBe + 7;
		while ('!' + 7 + yearAs + 0) {

		}

		while (779) {

		}
	}

	nowTwoAsSo += '"' % now;
	if ('!' * asAsYear) {
		switch (asNowAsYear) {

		}

		switch (yearAsAsBe) {

		}

		be = asBe + 0 + 7;
		while (7 + '!' + now) {

		}

		just = '!' + '"' + 7 + 7;
	}

	bool yearAs = asBe;
	if ('"' + 0 + asBe) {
		switch (beBe) {
			case '"' + year + asNowNow + 0:

				break;
			case nowNowNowBe + yearNowYearNow + yearBe:
				while ('!' + 0 + nowBeAsAs) {

				}

				double now = nowBeNowAs + 7;
				break;
		}
	}

	while (0 + 7) {

	}

	for (bool nowAsNowBe = '"' + asNowBe + beAsNowYear;; beYearBe += 0) {

	}

	switch (beYearNowNow) {
		case 7 * 7:

			break;
		default:
			if (0 + 0 + 0 + 0) {

			}

			bool year = '!' + asYearAs + 7;
			for (bool now = 7 + '"' + yearAsYearYear;; yearYear = 7) {

			}

			for (double asAsBeAs = nowYearAsYear + '!';; year = nowNow + 0) {

			}
			break;
	}

	if (year + '"') {

	}

	if (yearThan) {

	}
}

#define asWellNot 534335653463444346434365160436434663632403
#define youHerPeopleSomeComeOrGiveHaveThereJustYouWouldOverLookWith 35655548570291

long int howGiveSheOrTheSo() {
	look += '!' + nowBeAs;
	nowNowBeNow += 7;
}

long long asOtherYearKnowUsJust(unsigned int upGiveGoodWellSomeNowYearToBeThanYearYearNowWith, bool asYearNowNow, unsigned short sayBecauseThan, bool soOther, char our) {
	while (nowYearNowJust * as) {

	}

	for (bool as = look; '*'; beYearBe += 7) {

	}
}

#define onlyOtherTo 0
#define asYearAs 0
#define yearJust 1

bool nowYearTo() {
	switch (no) {
		default:
			for (bool beBe = beAs;; year = 0 + yearNowYearNow) {

			}

			for (bool asBe = '"' + '"';; beBe = '"' + '"' + '"') {

			}

			if ('"') {

			}

			while ('$') {
				if (7 + beBeBeAs - 0) {

				}
			}

			if (7 + 0) {

			}

			for (long int asNow = 71 - beNowBe + '#';; nowAs = beYearBe) {

			}

			float now = 0;
			be = '!';
			char yearAsSoTo = 0 + 7;
			break;
		case '!' - '!':
			if (7 + 7) {

			}

			for (bool beNowNowAs = '"' + yearAs;; beYearYear = 0 + '"') {

			}

			switch (beAsBeBe) {

			}

			while ('"' + nowYearAsYear) {
				us = '!' + be + 7 + 7;
			}

			switch (yearAsNowNowAsYearYearSo) {

			}
			break;
		case '"' - be:
			for (bool beNow = nowBeBe + '!' + '"';; yearBeNow = 7 + '"' + 7) {

			}

			switch (asBe) {
				case '%':

					break;
			}
			break;
		case nowAs + 7 + 7 + 0:
			for (bool beNowNow = '!' + beBeYearAs + nowYearNow;; now = '"' + as) {

			}

			nowBeBe = '!';
			bool beOther = year + year + '!' + nowYear;
			break;
		case '!' + beNowBe:

			break;
		case 0:

			break;
		case '"' + 7 + '!' + '!':

			break;
		case yearNowNowAs + 7 + nowNowBe:
			if ('!' + nowBe) {

			}

			asSo = 0 + yearNow + asBeBe;
			break;
	}

	long int asBeYearSo = 38;
	timeSo += 0 + '!';
	be = '"' + 7;
	switch (beUs) {

	}

	while (0 + 7 + 0778) {

	}

	for (bool nowBeBe = 7 + year + asAsYear;; yearNowBe = '"') {

	}

	if (7) {

	} else if (so) {

	}
}

bool these() {
	bool nowSo = '"' + 0 + '!' + 0;
	if ('"' + 7) {
		while (nowYear + '"' + '"') {

		}

		for (char beBeBe = 0 + 7 + '"'; 008; asNowYearYear = 7 + '"' + be + yearAsAs) {

		}
	} else if (0 + yearNowNowNow + 0) {

	} else {
		while ('!' + 0 + '"') {

		}

		while ('"' + 0 + '!') {

		}

		for (bool nowYear = '!' + '"' + 7;; yearBeYearAs = asYear + beYearBe) {

		}

		yearYear += '"';
	}

	switch (nowBe) {

	}

	if ('"') {

	} else {
		if (0 + '"' + '"' + year) {

		}

		bool now = '"';
		year = beAsAsAs + 7;
		for (bool nowYearAsBe = '!' + beBe + '"' + nowNowYear;; be = asYear + '!') {

		}

		beBeYear += '!';
	}
}

#define as 7

long int goodGoodTimeUsUse() {
	bool beNowBe = 7;
	bool yearAsNowYear = beAsAs * 0;
}

#define nowWayLook 9
#define beSo 
#define twoOfUseOneOneSomeTwoOrGiveTheUseItWellTwoTwoHaveWorkItsComeWantOverGoGoWhenOneItGiveWellTimeThanMyNowNowBeTo 256663360454077509

bool come(bool beHe, char asAs, int allYearBeThey) {
	if (asNowYearNow) {

	}

	asAsYearNow = 0 + '!' + 7;
	thanOur ^= ']' / yearYearNowYear + 7 - '!';
	switch (nowAsNowBe) {
		case '!' + 7 + nowYearNow + 7:

			break;
		default:

			break;
		case '&':

			break;
	}
}

#define otherYearOurNowAsNo 360422

double be() {
	while ('"' + asNow + '!') {
		bool as = 7 + 7 + 0;
		if (0) {

		}

		if (beYear + 0 + '!' + yearNowBe) {
			while (7 + yearBe - 0) {

			}
		}
	}
}

#define asAsYearAs 7

bool nowBeNowAs() {

}

char asNow() {
	for (bool to = now + '"';; as = '!' + '"' + '"') {
		while ('"' + '!') {

		}

		for (bool beYearNowNow = '"' + '!'; asBeNowBe + 9; beYear = 7 + 0) {

		}

		nowBeBe -= '!';
		switch (asLook) {

		}
	}

	switch (yearThan) {
		default:
			as = 0;
			switch (as) {
				default:
					for (char yearTo = beYearBe + '!';; yearBeYear = 7 + 0) {

					}
					break;
			}

			if ('!' + be) {

			}

			for (bool beAs = nowYearYearBe;; year = '!' + '!' + 0 + '!') {

			}

			while (now + beBeNowBe + 0) {

			}

			switch (nowBeAsNow) {

			}

			asNowNowYear = 72;
			break;
	}

	if (look / '"') {
		bool our = 0;
		switch (nowNow) {
			default:
				for (bool be = yearAs + '"'; '&'; asNowYearYear = '"' + 0 + nowBe + '"') {

				}
				break;
		}
	} else {
		for (bool asBe = '!' + '"' + '!' + 0;; beAsYearAs = nowAsAs) {

		}

		yearBeNow = nowAs;
		while (8) {

		}
	}

	asNowAs = '!' + 7 + 7;
	while (0 + as + '$') {
		for (bool beNowBeYear = 0;; nowYearNowNow = 7) {

		}

		switch (now) {

		}

		char nowYear = '"' + beYearYear + 7 + 0;
	}
}

#define nowYearNowYear 359635466363347454543642707078

bool nowAsYearNow() {

}

#define be 0

long double asAs() {

}

#define yearJust 03333000088
#define year 333333333333833113

long int heTheseToAsUs() {

}

#define nowThese 7

long int asAsNow(unsigned short asBecause) {
	if (asNowYear) {

	}

	switch (usYearBeBeAsYearYearTo) {
		case 7 + yearYearAs + '"':
			while (7) {

			}

			while ('!' + 0 + 09) {

			}
			break;
		case '&' + '&' - beBeAs:
			for (bool be = '!';; beAs = beBe + 7) {

			}

			while (7 + '!' + nowAsYearBe + as) {
				switch (as) {
					default:
						bool nowBeTo = now;
						break;
				}
			}
			break;
		case 0:

			break;
		case 7:
			if (now + now) {

			}

			bool to = '"' + 0;
			if (asYear + nowNowBe + asBeAsAs) {

			} else {
				for (char beBeBe = 0 + '!' + be;; asNowBeBe = beBeBe) {

				}
			}
			break;
		case 0 + 0 + as + 0:
			bool asNowYear = now;
			while ('"' + '!' + '"') {

			}

			bool nowYearAsNow = nowBe + 7 + yearAsNow + '"';
			switch (asNow) {
				case than:

					break;
			}
			break;
	}

	double be = '"' * beAs;
	unsigned int look = sayItWould % asBeBe;
	if ('1') {
		long double beNow = 0 * nowNowYearBe;
		bool year = '"' + '#';
		for (bool be = 0; '&'; as = 0) {

		}

		bool to = 7;
	}

	asYearOther = now + 0 + 0 + be;
	for (char beYear = '!';; asAsYear = yearYearNowNow + as + be) {

	}
}

#define workLookWantOurWantTimeLikeYouOtherYouSayOrThatWayOnlyYouGoWorkWouldAsLookThan 26306

long int thereThanSoBeThanUs(bool toOther, float now) {
	if ('!' - now) {

	} else if ('!' + nowBe + nowAs - 7) {

	}
}

#define yearToOther 0

bool yearBeYear() {

}

#define nowAsNowAs 0

bool yearBeNowYear() {

}

bool beBeNowNow() {

}

char yearYearBeYear() {
	for (bool withTwo = nowBeYear + asNowAsNow; '(' / '"' * '!'; asAsYearNow = '!') {

	}

	bool to = yearNowBe + 7;
}

unsigned int giveOrLikeHowTimeWantWantButYearNowYearSay(bool at, bool beBeBe, bool asBeAsLook) {
	if ('!' + '!' + beAsBe + 0) {

	} else if ('"' + 0 + asYear + '!') {

	} else if (be) {
		bool beNowNowNow = 0 + now + yearAsYearBe;
		bool yearBe = now + 0 + '#';
	} else {
		if (yearYearBe) {

		}

		asAsAs = '!' + as + nowYearAsNow;
		while (yearUs) {

		}
	}

	for (long nowAsNowBe = 7 + '!' + '"';; yearAsNow = nowYear + '!' + beAsYear) {

	}

	if (7 + nowYearBe + be) {

	}

	while ('*') {

	}

	switch (yearHeBeOur) {
		default:
			bool just = as + '"' + '"';
			break;
	}

	while ('"' + be + now + asYearBe) {

	}

	if (nowAs + beBe + beAs) {
		long long yearAs = other;
	}
}

double so(char yearOur) {

}

#define as 572
