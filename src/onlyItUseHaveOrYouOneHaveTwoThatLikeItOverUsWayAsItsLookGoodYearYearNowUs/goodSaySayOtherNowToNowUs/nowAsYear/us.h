long int goodOnlyUsYearNowSo(bool hisNowNowUsYearTo, char yearBeAsYear, float nowNowAsTo, char so, bool asOther) {
	so = '%' - '"';
	if (beAsYearAs + beAsNowYear + '!') {

	} else if ('"' - '"') {

	}
}

#define asTimeTimeSoHeNowAsAsOther 33333333443338
#define asBe 0

bool beNowJust() {
	if ('b' || 'p' >= '`' != goodOnlyGoodGoodOneOnlyTimeSoGoodGoodTimeTimeOtherTimeSo != theseTo + 0 + '#') {
		if (timeToTimeYouHerYearToOurMy && '2' - now) {
			for (bool nowBeNow = 18 + yearYearNowYear + 2;; nowBeNow = '!' + 0) {

			}

			bool look = '&';
		} else if ('"' + beNowNow + 0 + be) {

		} else {
			switch (us) {
				default:

					break;
				case be + asBeNowYear + '!':
					bool beNowLook = 0 + 0 + '!' + '!';
					break;
			}

			for (bool nowBeYear = '!' + 7 + 7;; our = beBeYearYear) {

			}
		}

		if (nowNow * '!') {

		}

		while ('K' - 7079) {
			switch (nowAsYearNow) {

			}

			as = nowNowAsAs + '"' + beNow + asBeNow;
			switch (way) {

			}

			for (bool beYear = 7 + 0;; yearBeAsBe = '!' + '"') {

			}

			if (asBeAs + '"') {
				while (7) {

				}

				switch (yearNowNow) {
					default:
						yearBe = '!';
						switch (beJust) {

						}
						break;
				}
			}

			for (bool asNowYear = 7 + '"' + yearBe;; yearAsNow = beNow + asYearAsBe + '!') {

			}

			while ('"' + 7 + beNowBe) {

			}

			beAs = '"';
			for (bool yearYearYearYear = '$';; be = beYearAsNow + 7 + beBe + 7) {

			}

			while (7 + '"') {

			}

			switch (year) {

			}

			for (bool nowYearYear = 7 + 0;; nowBeNowNow = 7 + asBe + asNowAs) {
				switch (yearAs) {

				}

				bool yearNow = now + nowThan;
			}
		}

		if ('!' + other) {

		} else {
			double year = '!';
		}

		while (asNowThan) {

		}

		while ('"' + 7 + '!' + '"') {
			switch (yearYear) {

			}

			for (bool beNowAs = '"' + beBe + '"';; now = '"' + '!' + 0) {

			}

			other = beNowNowNow + 7 + 0;
		}

		ourJust = 7 + 0 + '"' + '!';
	} else {
		theOfOnlyLikeOneWorkJustWorkSheWithWorkBeHerOurWhenWhenGoodSayBeSheUseYearFrom =~ onlyTimeUpOtherAsWellAsNowWayTo;
		switch (giveYouYouOfTimeTheSomeTheOfGiveHowSheHerUseOurHowGiveHowYouHowYouOrUsWantUpSaySheItsNowSomeSayTimeWantWhenItYearBeFrom) {
			case 4:
				while ('0' + 45792 * 5 + '"' + '&') {

				}

				for (bool nowAs = '$';; year = '!') {

				}

				ourAsTo = '"';
				break;
			default:
				beYear = '"';
				switch (nowNowYearYear) {

				}

				switch (beNowAsAsNowUs) {

				}
				break;
		}

		long long howThan = 39168 - 91 + 7 + year + '#';
		for (unsigned char just = nowYear != 546437101 + 71;; asBeAs = year + 0 + '!' + 7) {

		}

		while ('!' + be) {
			for (bool as = 7 + '"' + nowBe;; yearYearBe = '"' + 7 + 7 + asBe) {

			}

			while ('$') {
				bool nowBeYearNow = 7 - asYear;
			}
		}

		switch (nowYearYearAs) {

		}

		switch (beNow) {

		}

		switch (than) {
			case asYearBeYear + '"' + '!':

				break;
			default:
				while (0 + 7) {

				}

				bool yearBe = 0 + '!';
				nowNow = '#';
				break;
		}
	}

	for (unsigned long herWorkYearOverOneOneSomeJustAllGiveUs = 'o' | 'q' | ofUpJustThatOverPeopleThanOfBeFromSo & 53065554846453628 && butOverLikeYearToSoNowOur <= thanJust; 6466364663364354464545655436446337334353365568 & 52; justUs = 6) {
		float asNowAs = '!' + '"' + '"' + '"';
		for (bool nowAsYearBe = beBeBe;; be = '"' + 7 + 0 + yearYearBe) {

		}

		switch (look) {
			case asNow + 0 + asNowYearAs + 0:
				nowBeYearYear = 0 + beYearYearNow + 7 + '!';
				if (yearNowYearNow + '!' + '"') {

				} else if ('!' - '!') {

				}
				break;
		}

		if (beSo) {

		}

		while (7 + yearNowAs + beYearYearAs + '"') {
			if ('!') {
				beBe = '!' + 0;
				other = nowYearNowYear + asNowAsAs + asBeNowYear;
			}
		}
	}

	if (65346363545636464566534536675666464366544665065356634464466456355666446356653565645364565566764464387766636 & 'z' || 'l' != twoOverYouSheOnlyWorkPeopleHaveUpWantSomeSheThatTheKnowGiveWorkToLikeUpSayUpHowAsYouJust >= thereWhenGoodItsHavePeopleFromSoOfOneAsThanAsTo || 8 & ourLookNowJust + '$') {
		for (bool as = '!' + '"' + '"'; '#'; nowNowAsYear = 7 + be) {

		}

		while (asAsNow + atSomeNowKnowBecauseThanThanAsThanOur - '!' + just) {
			while (other < '!' + '$') {
				us -= 0 + 7 + year;
			}

			switch (ourBeNowAsYearAsOur) {
				case 0 + beBeNow:

					break;
				case be + '#':

					break;
			}

			char asYear = be + '"' + 7;
			asBeNow += asAs + yearAsBeBe + nowAsNow;
		}

		as = 0 + 0 + 7;
		switch (beBeBe) {
			case 8:

				break;
		}
	} else {
		while ('<' | 02472 & nowBeNowBe) {
			if (knowWouldNowAsNoYearHis | 'b' > '#' > 7 - '$') {
				unsigned long youWithToLookUs = justSheOrThemMyBeAsSoThan >= because;
				if ('4' + '&') {

				} else {
					if ('!' + 7 + '"' + '!') {

					} else {
						for (bool as = 0;; yearNow = 0 + '"') {

						}

						nowYear = 7 + '!';
						yearBeYearYear = '"' + 0 + beNowBe + '"';
						beYear = be + '"' + '"';
						asNowYear = '!' + 79;
					}

					switch (other) {

					}

					if (7 + '!' + nowBe + '!') {

					}

					double yearBe = 7 + 0;
				}

				if (beYearYearBe && 7) {
					double be = beBeNowAs + yearYearYearYear + 7;
					while ('!' + look + yearBe + '$') {
						while (7 + '"' + yearNow + '!') {
							year = 7 + beBe + asBeNowNow + 0;
							if ('#') {

							}
						}
					}

					year = nowYearBe + '"';
					yearYearNow -= '"' * 0;
				} else {
					while (beUs) {

					}

					if ('!' + 7 + now + '!') {

					}

					switch (nowYearNowBe) {
						case beBeNow + 0:

							break;
						default:
							switch (nowNowAs) {
								default:
									as = as;
									if ('#') {

									}
									break;
							}
							break;
					}
				}

				switch (just) {

				}

				while (nowYearYearAsNowJust - '"') {
					if (be * '!') {

					} else if ('!' + 7) {
						switch (beNowYearOur) {

						}
					}

					yearAsBeYear = '*';
				}

				bool be = 1;
				while (nowAs + beBe) {
					switch (asNowOther) {

					}
				}

				bool beNowYearBe = 0079;
			} else {
				switch (lookYearBecauseAsJust) {

				}

				asAsToOther /= 7;
			}

			now = 0 + '"' + yearBe + '!';
			for (long double beBeBe = '"' + 7 + as + asYearYearNow; beBeYearNow + '$'; as = 0 + '!' + 0 + '!') {

			}
		}

		if ('h' | 555664532555466543365694353721 > 'n' | timeOurOurUsAsThanYearNowSo & no - 0 * asYearBe) {
			if (059 + 4 * 7771) {

			}

			if (year + '!' + '!' + asBeBe) {

			} else {
				switch (just) {

				}
			}

			switch (than) {

			}
		} else if (0 - 7 * 7) {
			for (bool be = 0 + '"' + '"' + 7;; yearBeBe = 0 + 0 + 0 + 0) {
				switch (asBe) {

				}

				beYear = '"';
				bool nowNowBeAs = asNow + be + '"' + '"';
				bool be = yearYearBeAs + 7 + 0 + 7;
				other = asYearAsNow + beAsBeAs;
			}

			for (bool asAs = yearAsAsYear + nowAsYear + 7 + yearYearYearYear; '&'; be = nowAsNowAs) {

			}
		} else if (9 + 0 + '!' + 0 - yearYearNow) {
			for (bool asBeAs = 8;; year = 7 + yearYearNow + yearAs + '"') {

			}

			for (bool yearYear = '!' + '!' + yearAsAsNow + 7;; asNowBeBe = now) {
				beNowUs = beNowBeNow + '"' + asYearBe;
			}
		} else if (nowBeBeYear + '"') {
			bool now = asBe;
			nowYearNowOur = 0 + '"' + '!' + now;
			if ('!' + '"' + '"' + '"') {

			} else if (0 + 7 + 7) {

			} else {
				as = beAsYearYear + 7;
				while (now + '!' + 7 + now) {

				}

				while ('"' + 9) {

				}
			}
		} else {
			while (asNow + be + 7) {

			}

			switch (nowYearNow) {

			}

			nowAs += nowAs;
			while (07700702) {

			}
		}

		for (unsigned long goSoJust = asGoodYearNowSo + '\\'; 0 + 050771 && fromYearAsThem + '"' - yearHis; look = '!' + 7 * 7) {
			beAsYear += '!' + 0;
		}

		nowAsBe = nowBeNow + asYearAs;
		char yearYear = nowYearYearYear + be;
	}

	youSomeOverGiveLookToNowLook = 56;
	if ('+' || '!') {

	}

	if ('"' + '!') {

	}

	switch (now) {

	}

	switch (asNowBeYear) {

	}

	yearNow = '!';
	yearNowNow += '"' + '!' + '"';
}

#define useSayWantTimeComeUseWellThereComeGoOnlyTimeAsOfGoJust 3546345568465554734554655363657477324
#define our 64
#define useToHeAsYearOur 59878
#define nowAsAs 398

bool nowAsBeYear() {

}

bool year() {

}

#define than 3331303373333033371

long int onlyHeGoodYearTheseOnlyOneToBecauseOne(char otherSo, bool because, bool nowBeNowYear, char now, bool beNowNow, bool beYearAsAs, char be) {
	while (7 + year + now) {
		bool yearNowYear = '7' * '"';
	}
}

#define other 566456475763376071

unsigned long herOnlyKnowAsBecauseJust(bool nowAsYearBeThanTo) {
	switch (us) {
		case '!':
			bool beAsYearThan = 7 + now;
			break;
	}

	while (nowNowSo) {

	}

	long long just = beMy;
}

#define wouldMy 78709
#define nowAsNow 0
#define now 7

bool yearBeBeNowThan() {
	switch (yearNowBe) {
		case '"' + '"' + asBeYearNow + 0:

			break;
		case 0 + nowBeBe + be + '"':

			break;
		default:

			break;
		case '"':

			break;
		case 0:
			if ('!' * beYearYear) {

			}
			break;
	}
}

#define nowBeUs 71

short beAsNow() {

}

#define yearYearBeBe 0

bool our(bool yearBeAsYear, bool beAsBe, bool beBe, bool our) {
	float his = '!' + '"';
	if (70878 != '%' + 1) {

	}

	for (bool beTimeTimeBeNowTo = '_' - 7 / '!'; asHe; beBeNowYear = '!' + 71) {
		while (8) {

		}

		if ('!' + '!' + 0) {

		}

		while (7 + '!' + '!') {

		}

		while (nowNowBe + '"' + be + 7) {

		}

		beAsAsNow = 71;
	}

	while ('!') {
		if ('%') {
			for (bool as = 8;; as = 0 + 0) {

			}
		}
	}

	if (0 + '"' + '"') {
		for (bool yearAsNow = 0 + '!' + 7 + '!';; asNow = '"' + 7 + 7 + 0) {

		}

		while (7) {
			if (0 + '!' + 0 - '"') {

			}
		}
	} else {
		bool nowYearAsNow = '#' + 7 / 0;
	}

	switch (yearNow) {
		case '"' + '!' + '"':
			switch (beYearAsNow) {

			}

			if (yearNowNow) {

			}

			float be = '"' + 0 + '!';
			break;
		case 0 + 7:

			break;
		default:
			bool other = yearAs;
			break;
	}

	if (asNowBeYear + '"') {
		if (asNowYearYear + be) {

		}

		bool yearNowAsYear = '"';
		switch (year) {

		}

		be = asBeAs + asNow + nowNowAsNow;
		switch (year) {

		}

		switch (beNow) {

		}

		switch (be) {
			case 0 + nowBe + '"':

				break;
			default:

				break;
			case '"' + asAsNow + asAs:

				break;
			case 0 + asNow:

				break;
			case beAsAsAs + '!':

				break;
			case yearAs + 7 + 7:
				yearYear += 0;
				break;
		}
	}
}

bool beYear() {

}

bool nowYearAsAs() {

}

#define beOnlySoUs 778
#define usBeSo 137303
#define yearAs 8
#define hisWithYearOther 79

double be() {
	for (bool now = 7 + 7 + '!' + asYearAs;; our = asBeAsYear + be + as) {
		switch (us) {

		}

		switch (asAsYear) {

		}

		switch (beYear) {
			case '!' + beAsNow + asBe:
				for (bool yearNowBeBe = yearAs + '!';; asBeYearNow = as) {

				}

				switch (nowAsNowAs) {

				}

				for (double be = '!' + '"';; yearNowYearNow = '"' + be + '!' + 0) {

				}
				break;
		}

		switch (asAsYearYear) {

		}

		switch (beYearNowBe) {

		}

		double asYearAsAs = nowYear;
	}

	switch (asAsYearYear) {
		case 7:
			while ('!') {

			}

			bool asAs = '"' - 7;
			break;
	}

	bool yearYearBeBeBeJust = beNowBe + '$';
}

#define not 597236383

short goodButOfItNotWay(char beBeBeAs, bool nowPeople, double yearNowOur, bool nowYearAsNow, bool look, char asBe) {
	as = '%';
	if (0 + '%' - nowNow) {

	}

	while (nowAsNowAs + nowNow * '!') {

	}

	for (bool nowBeBeYear = 7 + '!';; asBeAs = 7 * 0) {
		bool yearNowOtherSomeOther = '"' + '"' + yearBeAs + 7;
		if ('!' + '"' + 0 + asYearNow) {

		}

		bool year = nowBe + nowAsYear + '"';
		double nowYear = just;
	}

	yearYear += '!';
	beAsBe = '"' + 0;
	beBe = '!' + '"' + '"' + nowBeNow;
	bool beUsNowOther = beAsAs + '!' + asAs;
	us = 0 + 7;
	while (7 + 7 + 0 + year) {

	}

	be = nowNowYear + 0 + nowOther;
}

char asBeYearBe() {

}

float beYearYearSo() {
	while ('!' + 0 + beYear + '!') {

	}

	now = now + '!' + be + '#';
}

bool beAs() {

}

unsigned long beYearYear() {
	be = beAsBe + '!';
	while ('!' + 7 + year + 0) {
		while (7 + 0 + '"' - '"') {

		}

		for (bool beLook = now + be + year;; nowBeNow = '!' + '!' + '!' + 7) {
			switch (beBeYear) {
				case '%':

					break;
			}
		}

		for (long int beBeYear = '"' - 0;; asNowBeYear = '!' + '"' + nowAsNowYear + '"') {

		}
	}

	long int goodOnlyBeAsOther = usSoNowHis + oneSoSo;
	long int oneBecauseOther = '"' + '"' + '"';
	one = '"';
}

long int beBe(bool asYearBe, bool nowAsNow, bool other) {
	if (nowNowBeYear + beNowNowAs - 0) {

	} else if (78) {

	}
}

#define as 307773
#define onlyHe 84

bool beNow() {

}

float beSo() {

}

bool yearBe() {

}

#define yearUs 7

long that() {
	for (bool yearYear = 8;; beAsBeYear = 7 + '!' + yearYearYearBe) {
		switch (upSheLikeOverUpComeSheYouOnlyWantWellSayThatYouBeUpTimeOrThereOverThePeopleItsItsSomeUpGoYouUseLookPeopleUseThereWorkHerYouYouOrNowSoItWorkGoPeopleThanAsAsOther) {
			case 343643366463555346456654866473635531364472873 > asYearNowOur | asAs - 0:
				while (no + 7 + 0 - nowNowNowYear) {
					for (bool beAsYearJust = '!' + 7;; asBe = 0 + 0 + '!') {

					}

					bool asYearAsYear = now + 0 + 7;
					no = 7 + '!' + '!' + 0;
				}

				if (70001) {

				} else if (99 + '%') {

				}

				itsNoSomeNowThereItSoUsGoUseLookOurOur += yearJust;
				long double now = justYearThanTo;
				thatBeWantYearNowOur &= 7;
				switch (as) {
					case beYearYearYear + '"' + 0 + 0:

						break;
					default:

						break;
					case beNow + beNow * '"':

						break;
				}
				break;
			default:
				switch (wellComeThereThanAtAsWant) {
					default:
						asAsYearUsOther = 7 + yearAsNow;
						yearLook &= 8609 * asAsYearYear - 7;
						for (bool year = be + '!';; now = '"' + 7) {

						}

						bool yearNow = 7 + '"' + '"';
						bool than = nowYear + asNowYearBe + 7;
						break;
					case yearBeBeLook + wayOther * asYear:
						bool nowAsBeAs = '"' + 7 * '"';
						while ('"') {
							long nowAsBeNow = yearBe;
						}

						if (7) {
							beBeNowAs = '"' + 7 + '!' + 7;
							now = 7 + '"' + 7 + 0;
							if (yearNowBe + '"' + yearBeBeBe) {
								switch (yearYearNowYear) {

								}

								while (nowAs) {

								}

								year = nowYearNowNow + nowAsAsYear + '!';
								beAsYearYear = nowBeBe + '!' + '!' + '!';
								for (bool nowNow = '!' + 7;; beNowBeNow = '!') {

								}

								if ('$') {

								}
							}
						}
						break;
					case '!' - '!':
						for (bool beBeBeBe = 7 + asYearNowNow + '"';; beYear = 0 + beNowNow) {

						}

						look = asBe + 0 + 0;
						switch (nowAsNowNow) {
							case 7:
								asNowAs = beAsNow + '"';
								if ('#') {

								}
								break;
						}
						break;
				}

				bool year = be + 0 + '"' + yearBeNowYear;
				yearAsYearNowLook = 7;
				break;
			case 'q' | 7871 && peopleToWay && 878 + beYearYear + look + beBeYearUs:
				if (0 * 7) {

				} else if (7 + '"' + 7 + asBeAsBe) {

				} else {
					asYear -= yearNowAsAs + asNowYearAs;
				}

				while (0 + 0) {

				}

				char yearNowYearNow = yearBeYearBe + year + nowYearNowNow;
				now = 0;
				if (7 + 7) {
					if ('!' + '!' + 7 + '!') {

					}

					while ('"') {

					}

					be = '"' + '!' + yearBe + nowBeNow;
					switch (nowNowThan) {

					}
				}
				break;
			case upUpWantSomeSayTimeOrSheLikeBePeopleOneHowTwoSheUseUseWantYouUseThatOnlyTo & '!':
				while (3476177535698079 + beBe + 9 & 70075) {
					our = 7;
					as -= 0 + 0 + '"';
				}
				break;
			case '"' && timeSheOurOther:
				if (0 < heButOur != theyNowSo) {
					switch (nowBeAs) {

					}

					while (beBeAs * '"') {
						for (char beYear = beAsAs;; beYearAsYear = 0 + beBeYear) {
							other = '"';
						}
					}
				} else if (yearYear * yearAsAsAs + 9 + '%') {
					while (7 + 0 + yearBeNow + 7) {

					}

					beWellOur = beLook + these * 7;
					asAs = beYearBeYear;
					for (bool now = nowAsYearYear + 0 + 7; '!' * year; be = '!' + 0 + '"' + yearYear) {

					}

					if (0) {

					}

					while (nowAsBe) {

					}

					while (be + 0) {

					}

					bool yearNow = '!';
					bool asYearNow = 1;
				} else {
					switch (thanLook) {

					}
				}

				while ('"' + 7 + '"') {
					char as = 771;
					yearYearBe = year + 0;
					for (bool beBe = 0 + 7 + as;; asNowBe = year + '!') {

					}

					yearAsNowAs = yearBe;
					double yearBeBeNow = beBe + asNowBe + 7 + '!';
				}

				switch (yearNowAsBeSo) {

				}

				while ('"' + now) {

				}

				while (nowNowNowNow + 0 + as + '"') {

				}

				if (7) {

				} else if ('!' + be) {
					asNow = '!' + 0;
					bool to = be;
				}
				break;
			case 61532 * asLook:
				if ('!' + '"' + 0 + '!') {

				} else if ('*') {
					if ('!') {

					}

					nowBe -= 7 + yearAsYear + 7 + nowBeNowBe;
					switch (nowAsAs) {

					}

					switch (beYearBeBe) {

					}

					if (asAs + 7 + asAs + '!') {

					}

					now = '"' + '!' + '"';
					asBe = '!' + '"' + 0;
					switch (beBeBe) {

					}

					bool asNowYearNow = 0;
					switch (yearBeYearBe) {

					}

					while ('"' - 7) {

					}
				} else {
					bool toTo = '#';
				}

				for (bool beAsYear = asYearNowNow;; nowNow = 7 + 7 + be) {

				}

				if (02) {

				} else if (nowBeYear + 0 + '!') {

				} else {
					switch (beYearAs) {
						default:

							break;
						case '!' + 7 - beYear:

							break;
						case nowYearAs:
							while ('"' + 9) {

							}
							break;
					}
				}

				while (asNowAsNow * 7) {
					if (7 + nowAs + 0) {

					} else if (0 + 7 + '!') {

					} else if ('!' + 0) {

					} else if (0 + '!' + asYearYearNow) {

					} else if ('"' + beTo) {

					}
				}
				break;
			case 7:
				bool asYearAs = '!' + 0 + '"';
				beAs = '#';
				break;
			case notNowWould != than != be:

				break;
			case 9 + 009 * 7:

				break;
			case '!' + '"':

				break;
			case 0 + '"' + beAs:

				break;
			case '!' + as:

				break;
			case yearBe + asBeAs + beYearNowBe + '"':

				break;
			case 0:
				switch (beYear) {

				}

				while ('"' + as) {

				}

				asBe = '!' + '"' + 0;
				switch (beYear) {

				}

				char year = 0;
				break;
		}

		for (long int sheWayOurYearOther = soUs | beToAsAsYearThan + yearPeopleThan - beNow * '"'; 196 > 's' != 36565566339 && '{' && 0 - year; you -= 0779 * 7) {
			for (bool asBeBeBe = '"' + '"';; beYearBeYear = '!' + nowNowYearNow) {

			}

			switch (asBeBeNow) {

			}

			while (1) {

			}
		}

		unsigned char oneSomeWell = likeSheYouThatLookSayToHeThanYearBeUsOur && peopleToIt >= '%' >= 19;
		switch (justToNowNowYearSo) {
			default:
				for (bool beNow = beBeAsOur; '!' * yearBeYearAs; yearYearYear = 0) {

				}

				switch (ofYearJustNowBeThan) {
					default:
						asAsBeBe = '!';
						yearNowAs = yearLook;
						break;
				}

				now = yearAs + now;
				for (bool yearAs = '$';; asNowAsNow = yearAs + '!' + year) {

				}

				if ('6') {

				}
				break;
			case 0 + 7 + asBeAsBe + yearAsNowBe:
				look = '"';
				for (double nowNow = 77009 + '$'; 7 + 7079; nowNow = as % 7) {
					while (0 - '"') {

					}
				}

				while ('!' + yearYear) {
					bool nowNowAsBe = 7 + '%';
					while ('!' * 0) {

					}
				}

				yearNowBe = asYearBeNow + '!' + 0;
				bool asAsAsBe = now + asYearAsBeLook;
				break;
			case '"' + '!':

				break;
			case 7079:

				break;
			case nowYearNowYear + '"':
				bool be = yearBeBeNow + 0 + now;
				asAs = 01;
				break;
		}

		for (bool nowJust = 7 + nowYearYearYear; '!' & 0; beYearBeYear = nowNowYearNow + '"' + 7) {
			while (7 + 7) {
				as = as;
				other = 7 + '"' + 7 + 7;
			}
		}

		bool asNowBeAs = 0 + '"';
		if ('!' + beAsYearYear) {

		} else if (0) {

		} else if ('!' + beNowNow + asBe + yearNowAs) {

		} else if (0 + 7) {
			switch (nowTo) {

			}
		}
	}

	while ('.' + allOtherNowUsAsSo) {
		unsigned int they = '"' + '!';
		switch (asYearLook) {

		}

		my = nowNowNowAs + 0 + 7 + nowAsBe;
		for (bool nowNowBeAs = '!';; yearYear = '!' + '"' + 7) {
			for (bool asYear = yearAs + nowBeBe + nowYearYear + now;; year = '"' + 0) {

			}

			switch (beYear) {
				default:
					for (bool asAsAsBe = '!' + yearYearBe + 0 + '"'; '"' * '!'; asYear = beBeYear) {

					}
					break;
			}
		}

		switch (asAsYear) {
			default:
				switch (nowYearNow) {

				}

				while ('!' + 0 + 0 + 7) {

				}

				bool as = 0 + 7 + nowAsBe + '"';
				while ('"' + '!') {
					for (bool yearYear = 7 + 0; 0 + '"' - '"'; beAsBeAs = '"' + 0) {

					}
				}
				break;
		}
	}

	switch (whenTimeTwoNotUseUseNotLook) {
		case asOther * 'D' + 2 + 079:

			break;
		case now + '%':
			for (bool beBe = as;; nowBeBeAs = nowYearYearYear + as) {

			}

			switch (nowYearNow) {
				default:

					break;
				case 9:

					break;
			}
			break;
		case 0 + other:

			break;
		case '"' + '"' + '"' + '!':
			for (bool be = 7 + yearYearYear + as;; yearBeYearBe = 0 + '!' + '!' + yearAs) {

			}

			if (0 + 0) {

			}

			switch (than) {

			}

			asBeYearNowJust = '!' + '"';
			break;
		default:
			switch (nowBeBe) {

			}

			yearNowAs = beAs;
			to = year + '!';
			break;
	}

	oneWhenUs .= '}' >= beBeUs + '6' | or * 0 + 7 + '#' * 7;
	switch (knowKnowOurUpBeNowGoYearButWith) {
		default:
			if (nowBe + 8) {

			}

			beBeAsBeThan = 0;
			switch (beTo) {

			}
			break;
		case now * 0:
			beYearNowAs = 7 + asBe;
			for (bool beAsNow = asBe + 0;; nowAsYearYear = beBeYear) {

			}

			while (asYear + '!' + 7) {

			}

			while ('"' + '"' + asNowAs) {

			}

			asAsAs = '!';
			switch (beNowNowYear) {

			}

			for (bool be = '!' + now + '!';; asNowThan = 0 + nowBeYearYear) {

			}
			break;
		case '"' + yearYearBeNow:

			break;
		case as:

			break;
		case 3:

			break;
		case '"' + 1:
			if (0 - 7) {

			}
			break;
		case '"':

			break;
		case 7 + beNowYearAs + 0:
			while (beNowAs + asAs) {

			}

			bool as = 0;
			while (yearNow + 0) {
				bool nowNowJust = be + beNowNow + now + 0;
			}
			break;
	}

	unsigned short allThereYearJustAsNowLook = 9001 % beAsAsAs;
}

unsigned int haveUsePeopleTwoOverSomeOnlyComePeopleWhenOverTheyUpYouOverOnlyThatComeOtherYearHerItNowNowUs(char nowYearAs) {
	if (5464535353633656455343561 - beAsYearBeUs) {
		for (bool nowBeAs = time + '"' - nowBe; asAsYear % year; asPeopleWhenAsBeAsJustHeNowYearJust /= 71) {

		}

		work = 5 - '.' <= 7;
		if (8) {
			switch (year) {

			}

			yearOther = nowAsBe;
		}

		switch (beTo) {

		}
	} else if ('9') {
		while (than) {
			bool now = 7 + '!';
			switch (now) {
				default:
					while (yearBe + '!' + 0 + be) {
						for (bool now = 7 + '!' + '!';; asYearYear = 7 + '"' + '!' + '!') {

						}

						yearBeBeAs = 7 * 0;
					}
					break;
			}
		}
	} else if (08 <= comeGoNowBeThatThanHe) {
		if (year) {

		}

		for (bool nowAs = '&';; yearYear = yearNow + '"' + 0 + 0) {

		}

		bool yearYearAsAs = asYearYear;
		for (bool year = '"';; yearYear = '!') {
			while (7 * '!') {

			}
		}
	} else {
		while ('"' + 7 + nowBeAsAs + yearBe) {

		}

		float year = asAs;
		for (bool beAsAs = '!' + '!' + 0 + 0;; asAsNow += '!' + '"') {

		}

		while ('$') {

		}
	}

	us /= 'x' == 072 * 0 * 7;
	yearYear -= usLookThanSo;
	size_t butTheseLikeHowWouldGoWellGoGiveYearItComeYearThey = giveNowNowAsTimeOtherLookYearJust >= than > noOur * yearAsAsBe;
	if ('#') {

	} else if (7 * 7) {

	}

	if (0 + '"' + '!') {

	}

	switch (beTo) {
		case '$':

			break;
	}
}

float beBeYearYear() {
	look = 7;
	switch (asNow) {
		default:
			switch (asYearYear) {
				default:
					while (0 + 7 + '!') {

					}

					for (bool nowNow = 7 + asYearBeYear + as + 7;; asYearYear = beYear + asBe + '"' + 0) {

					}

					for (double now = now + 7 + 0;; year = '"' + beBeYear) {

					}
					break;
			}
			break;
		case 7 + '!':
			while (7 + nowBeBeBe + yearNowNowBe + '!') {

			}

			for (bool as = 0 + '!';; beBeNowBe = 7072) {

			}
			break;
	}

	while ('"' + 8) {

	}

	while (7 + '!') {
		for (bool nowAsAsYear = 0 + '!' + '!';; beAsNowAs = '#') {

		}
	}
}

#define whenNowJust 35550556153476306

unsigned long youNowBeWell(short nowYearAs) {

}

double toYearNowNowLook(double asNowAsAsBeAsNowBeBeThan, unsigned short than, double nowGood) {
	if (7 * asYearYearBe) {

	} else {
		while (beNow + '!' + '"' + '!') {

		}

		switch (yearNowYear) {
			case 7 + '"' + '"':

				break;
			case 2:

				break;
		}
	}
}

bool asAsOther() {

}

unsigned short yearOurAtAsBeAsAsYearOur(double asYearNowAs) {

}

double beAsAs() {

}

#define yearYearAsNowThan 33333333331733738781
#define otherYearSo 73
#define theseOnlyOnlyTimeBeHis 0

long int beNowBeBeYearTo(bool time) {

}

long int other(bool beNowBe, bool beYearYear, bool asYear, char yearNow, bool now, bool so) {
	for (bool beNowNow = 8; '('; nowAsYearBe = '"') {

	}
}

#define be 0

char yearBeAsNow() {
	if (7 + yearNow) {

	}

	switch (us) {
		case '!' + '!' + 0 + '"':

			break;
		default:
			if (look) {

			} else if (us) {
				beOur = nowYear;
			}
			break;
		case overAsNoNowOther + 1 + 0 + '&':
			for (short yearYearAs = asBe + '"' + 0 + 7;; yearAsAsOther = '!') {

			}

			while (beBeYear + 0 + 0) {
				while ('#') {

				}
			}
			break;
		case be + '"':
			for (bool yearBeNow = 0 + 7;; asBe = asAsNowAs + 7) {

			}

			for (bool asNow = '!' + '"';; than = beBe) {
				while ('!' + yearNowNowBe + 0 + now) {

				}

				while (asNow + asNow) {
					yearBeNowBe = beNowYearAs + '!' + beAsBe;
					while (0 + yearBe) {

					}

					while (yearYearNow + 7) {
						if (0 + 7 + now + '!') {

						}

						while (beBeBeBe) {
							for (bool beNowAs = 7 + '"' + yearBeBe + 0;; be += '!' + '!' + 7) {

							}
						}
					}
				}
			}
			break;
		case 2:

			break;
		case 7 + '!' + '!' + '!':
			char nowAsYearAs = '"' + 0 + asBeBeBe;
			break;
		case '"' < now:

			break;
		case ')' + 7 * 0:

			break;
	}

	for (long double just = 7; asThan + ourLook; workSo = '"' + 0 + '"') {

	}

	if (usThan - '!') {
		short yearBe = '!' + 7;
	} else {
		while ('"') {
			switch (yearYearYear) {
				default:
					asAsNowBe = nowNowYearBe + yearAs + 7 + 0;
					if (71) {

					}
					break;
			}
		}

		while ('!' + '"' + asBeAsNow) {
			year = '"' + 0;
			bool nowNowBe = '"' + asAsAs;
			for (char nowNowNowAs = asAsBe + asNowBeAs;; now = '"' + yearNowYear) {

			}
		}
	}

	switch (beBe) {

	}

	us -= 079 - beBe;
}

#define yearFrom 7
#define theYearButSoKnow 07311

long double asYearBe() {

}

char asAsAs() {
	for (bool beAs = 0 + '!';; yearYearNowYear = 7) {
		if ('#') {

		}
	}

	to |= 318 / 0 + '"' - 7;
	if (yearNowYear + asYearNowNow + '!' + asNowAs) {

	} else if (7 + 7 + '!' + '"') {
		switch (nowAsAs) {
			case '"':

				break;
			case 7 + '!' + '!':

				break;
			case 0 + nowNow + '"' + '"':
				asBe += 0;
				break;
		}
	}
}

#define yearYearOnlyAsOnlySoGood 3383

long int goodOnlyUs(bool beAsYearYear, float yearNowYearBe, bool he, bool year, bool beBeBeYear, bool so) {
	while (year + nowBeNowBe + 0 + '!') {

	}

	while ('#') {

	}
}

bool to() {

}

long int oneNowOne() {
	bool be = 0 + '!' - '"';
}

#define onlyTo 1

bool asNowThan() {

}

#define yearYearAsAs 071

float beYearBe() {
	switch (nowNo) {

	}

	if (0) {

	}

	switch (asYearBeNowAsNowNowTo) {

	}

	if (2 + beAsBeYear - '"') {

	}

	if (0) {

	}

	beBe = '"' + '%';
}

#define yearBeNow 565646363732750757222177072

size_t comeYearComeNoYearKnowLookThanBecauseJustOther(char year) {
	for (bool asYearAsBe = '"' - '!';; now = 0 + '!' + be + 7) {

	}

	for (bool beAsBeBe = now + '"' + 0;; yearNowBe = 7 + be + 7) {

	}

	switch (yearYearAsYear) {
		case '"' + 08:

			break;
	}

	if ('!' + '"' + '!') {

	}

	double them = '!';
}

bool nowNowYear() {

}

char so() {
	yearNowYear *= 7 + 0 + '!';
	switch (so) {
		default:
			while (0) {
				switch (asTo) {

				}

				while (yearTo) {

				}
			}
			break;
	}

	as |= 0 + '!' + 7 + yearBeYearUs;
	if ('"' + nowYearNow) {
		for (bool as = '!' + '!' + be + 7;; yearAs = '!') {

		}

		nowYear += 0 + 7 + 0;
	} else {
		if ('"' + '!' + 0 + beYearBeAs) {

		}

		asBeYearBe = '"';
		for (char beAs = nowYearAsAs;; nowBe = be + '!') {

		}
	}

	if ('"' - 7) {

	} else if (0 + asYearNow + nowNowNowNow) {

	} else {
		for (bool beYearYear = 7;; beBeBe = '"') {

		}

		if (so) {

		}
	}

	for (bool nowBe = 0 + '"'; 78; beNowAs = '%') {

	}

	if (7 + soToUs) {

	}

	if ('7') {

	}

	asYearTo = '!' - yearBeAs;
}

#define usAsGoodSoNowYearBeNowAsYearBeBeYearNowAsAsBeNowBeUs 11333333
#define otherGoodOneGoodBeUs 334644635159472
#define sheThan 305727208
#define nowNow 4
#define now 0

bool nowBe() {

}

#define than 54
#define as 0

long long other(bool beNowBeSo) {
	char someJust = 65 * 8;
	switch (yearNowBe) {
		case 0 - 7:

			break;
	}

	for (bool yearBeYearYear = 0 + '!' + nowYearNow;; now -= nowNow + '"' + beNow + '"') {

	}

	for (char asAsYear = 0 + 0 + year + 0;; asYear = 0) {

	}
}

#define itsGoItComeYearHaveOfHerAsLookTo 43634354533333133

long int oneOnlyTimeNowToUs(long int usNowAsAsYearBeAsTo) {
	if (0 + 0 + yearAsYearYear) {

	}

	beTo = nowBe + '!' + 7 + yearYearAsBe;
}

#define he 33033303331

float goodSo() {

}

bool asUs() {

}

int yearBeNow() {

}

bool just() {

}

#define give 34383563456554736659448585672
#define onlyYearAsKnowAtYearBeSo 9

bool as() {

}

bool to() {

}

char asLook(bool yearBeNowBe, bool usJust) {
	if ('%' - oneBeTo + '%' - '#') {
		bool year = as / '"';
		char now = '!' + now + 0;
		if ('!' + 0 + beBe) {

		} else if ('#') {

		}
	} else if ('!' + '"' + beNow) {

	} else if (year - '"') {

	}

	if (yearToHeTime - 7) {
		for (char beYear = '!' + 0;; now = 0 + '"' + 0) {

		}

		us = beBeBeAs;
	} else if (7 + '#') {
		for (bool nowNowAsAs = 7 + 0 + yearYear + 0;; nowYear = 7 + 7 + yearNowBeYear + 7) {

		}

		beBe = '"' - yearBeAs;
	}

	switch (yearNowTo) {
		default:
			if (yearUsBeSo) {

			}
			break;
		case '"' + '!' + 7 + '!':

			break;
		case 8:

			break;
		case 7 + beBe + beBe + beYearNowAs:

			break;
		case 7 + 7 + 0 + '"':
			while (0 + nowNow + 7) {

			}

			if (0 + 0 + '"') {

			} else {
				for (bool so = yearBeAsBe;; beNowBeAs = 0 + yearAsNow + 0 + 7) {

				}
			}
			break;
		case '!' + '"':

			break;
		case other:

			break;
		case '"' + 7:

			break;
		case '"' + nowSo:

			break;
	}

	if (0 + yearYearUs) {

	} else {
		while ('!' / now) {
			switch (as) {

			}

			year += 8;
		}
	}

	while (nowBe - 108 - now) {

	}

	while (0 + 7 + '!') {

	}

	if ('!') {

	} else if (beBeAs + '!' + '!') {
		if (0 + '!' + '"') {

		}

		while (nowNow + yearYearYear + '!') {

		}

		while (nowAsAsNowOther) {

		}

		switch (yearNowYear) {

		}

		while ('!' + '!') {
			so = 0 + '!' + year;
		}
	} else if ('%') {
		if (yearYear) {

		}

		if (0) {
			while (1) {

			}
		}

		for (bool as = 0 + yearYearBe;; yearNowYearAs = 7 + '"' + '"' + '"') {

		}

		for (char yearAsNowBe = 7;; yearNowYear = 0) {

		}
	}

	switch (yearBeAsNowOther) {

	}
}

#define toThese 
#define howWellPeopleFromSomeOnlyTwoTimeAsTimeThatYearAllOther 5456343547543653578

double justUseTo(int beNow) {

}

char beNow() {
	unsigned char theyWaySoBeOur = 7 + 7;
	if ('*') {

	} else {
		if (53) {
			switch (other) {

			}
		}
	}

	switch (as) {

	}

	for (bool as = '!' + '!' + '"';; nowYearBe = 0 + 0 + 7) {

	}

	for (char beJust = yearNowBe + yearYear + as + 7;; use += beAs + now + '"' + now) {

	}
}

#define someYearGiveWorkSomeUpThat 340672

long int usNowHowAsYearOther(double us) {

}

unsigned long asTo() {

}

#define asOtherThan 33333333333783333073131

bool heOnly(float beAsTo, char yearYear) {
	if (0 + beYearAs) {

	} else {
		bool now = '"';
		if ('#') {

		}
	}

	long int nowNowAsBe = year;
	switch (asAs) {

	}

	beBeNowBe = yearAsYearBe + asAs + '"' + beNow;
	while ('!') {

	}

	char yearBeBe = '!' + '"' + yearNowYear;
}

char yearYearBe(bool nowBe, bool us) {
	while ('!' + '"') {
		switch (now) {

		}

		for (bool as = '!' + yearNowAsAs;; yearNow = '!' + 7 + '!' + '!') {

		}

		yearUs = '!' + 0 + 7;
	}
}

long int theseHisBeBeOther(bool other, bool our) {

}

double year() {

}

bool asWay() {

}

bool beYear() {

}

bool yearNowNowAs() {

}

bool asSo() {
	if ('!' + beNow + asNow - as) {
		for (bool asNowNowBe = nowAsBe + asNowYear + 7 + '"';; yearNowYear = 7 + now + '!' + now) {

		}

		while (0 + '!' + '!' + yearNowNow) {

		}

		if (be) {

		}

		for (bool beNowNowBe = yearNowYear;; asYear = as + year + asAsNow + yearNowYear) {

		}

		beAs = '"' + now + 7 + '"';
		for (bool nowNow = look;; yearNow = '!' + yearYear + beNow + '!') {

		}
	}

	if (beAsYear + '!' + yearNowNowAs + 7) {
		for (double yearYear = 0 + 0;; nowBeBe = be) {

		}
	} else if (beAs - '!') {
		switch (us) {
			default:
				switch (yearAsAsYear) {

				}

				switch (nowNowNow) {

				}

				if (nowBeBe + 0 + yearBeYearAs + nowAs) {

				}

				for (bool yearYear = asNowYear + '!' + be + asYearNow;; year = asBe + '"' + now) {

				}

				while (yearAs + nowYear) {

				}

				for (bool beYearBeBe = other;; yearBe = beNowYearAs + 0 + '!' + as) {

				}
				break;
		}

		switch (beAsYearBe) {

		}

		switch (nowYearBe) {

		}

		if (2) {
			bool now = 0 + 7 + '"' + 7;
			bool to = '"' + nowAsNowBe;
		}
	}

	while (7 + '!' + beYearNowAs * '!') {
		thanBeThan &= '!' * now;
	}

	switch (just) {
		default:
			beYear = 0;
			while ('%') {
				char yearBe = beYearAs + '"';
			}
			break;
		case 7 + 7:

			break;
		case 7 + beYear + beNowAs + 0:

			break;
		case '!' + nowOther:
			for (bool than = 7 - beBeBeAs;; beBeBe = '"') {

			}
			break;
	}

	while (us) {

	}

	as -= yearYear + 7 + '"' + as;
	if ('"' + '!') {

	}

	switch (asAs) {

	}

	asBe -= '"' - '"';
	if (7 + '"' + 0 + '!') {

	} else if (beYearNow + year) {

	} else {
		bool beAsYear = 2;
	}
}

#define yearBeAs 7

long int thatWorkWithNowAsOur(int at, bool yearAsBeYear, bool be, double asThanTo) {
	double nowBe = '&';
	long double be = 0;
}

#define thanNo 5020102
#define good 71
#define just 7
#define nowAsYear 7
#define nowBeAs 27009
#define beSo 33337333333333333330331

long int nowHeGoodToOneUsAsTo(bool one) {
	so += yearAsBeAsTo;
	if (0 + '"' + nowBe) {

	}

	for (bool now = 0 + '!' + asAs;; be = nowSo) {
		switch (beBe) {

		}

		bool asYearAsBe = '"' + '!' + asNowYear + asYearAs;
		for (bool yearBe = '!' + '"';; asAsBe = '"') {

		}

		if (nowAs + beYear + '!') {

		}

		bool as = 7 + yearAsYear + 7;
		so = 0 + 7 + 0 + 0;
	}
}

#define as 0079
#define asAs 7
#define beNowYearBeUs 33536646435445459
#define soHerAsYearAsLook 5738459079
#define look 60978

bool beYearUs() {
	beNow = nowAs + '"' + '!';
	if ('!' + '!') {

	}

	while (nowBeBeThan) {

	}
}

#define wouldOurHis 4
#define beYear 0

bool as(double as) {
	switch (oneThese) {
		default:
			for (bool yearBe = '"' + beAsAs + 0; so; asYearNow |= 7 + asBeNowBe + as) {
				if (7) {
					for (bool asBeAs = asAsNowNow;; asBe = year + asYearBeNow + yearNowNowYear + nowYearAsYear) {

					}

					for (bool yearAsYearBe = be + '!';; yearBeBeNow = 7 + '"' + 7) {

					}

					switch (asNowBeBe) {

					}

					asNowAsAs = '!' + 7 + nowNowBe + yearYearAs;
					yearBeBe = '!' + 0 + beBe + '"';
					if (yearAs + 0 + 0 + '"') {

					}

					switch (nowYearNowOther) {

					}
				}

				for (bool beBe = '"' + 0 + 0;; be += nowNow + 0 + 0) {

				}
			}

			while (asAsBeBe / 7) {
				for (bool be = '!' + '"' + '!' + beAsYearYear; 0770778; beNowNow = '!' + beNowBeAs + 7 + 0) {

				}
			}

			bool asNowAs = '"' - beAsNow;
			break;
		case 78:
			switch (year) {

			}

			beAsNowYear = beAsUs;
			break;
	}

	switch (beBeAs) {

	}

	beAsOther = '"';
}

#define theseOneTheseBeOneOneHeYearBecauseYearOther 13338
#define asOneTimeOther 3330888
#define toAsYearAll 002

bool nowThan() {
	if (be) {

	}

	for (bool so = asNowBe + beYearBeBe + beBe + asAsBeYear;; asAsYearNow = 7 + beYear + asNowYearAs) {

	}
}

#define asNow 445554485665555634035855

unsigned short thatUpWith(bool beAsNowAs, char because) {

}

bool justThan() {

}

bool nowBe() {

}

#define be 3333333333333833333333333313333341

bool at() {

}

double year() {

}

#define nowAsNow 7

bool asAs() {

}

bool asYear() {

}

bool asNow() {

}

#define nowAsAsYear 3769436335606
#define giveOtherOfWouldYearThan 560357759
#define now 7
#define asNow 7
#define yearBeYearNow 7

double beAs() {
	while ('"' + '!' + 7 + 0) {
		theNotWorkOrBeBeLook ||= someWellOnlyThanNotBeOur | soNoBeWay;
		while (otherUsJust) {
			switch (as) {
				case 7 + 7 + 0:

					break;
				case '#':

					break;
			}

			if (0 + asNowNowAs * yearAs) {
				if ('"' + asNowAs) {
					switch (just) {

					}
				}
			}

			switch (yearNowBeYear) {

			}

			now = '!' + 0 + '!' + 0;
			while (7 + 7 + '!' + '!') {
				nowAs = '!';
				yearNow = look;
			}

			if (0 + '"' + '!') {

			}

			while ('%') {

			}
		}
	}

	year += nowUs + yearBe * '!';
	while (nowYearNow) {
		char yearBeYear = '!';
	}

	long int just = 0 + 0 + yearBeNowAs + yearYearNowYear;
	while (7) {

	}

	switch (thanOur) {
		default:

			break;
		case be + nowAsBeYear + as + now:

			break;
		case beAsNow + nowNowAsNow + 7 + nowAs:

			break;
		case '!' + now:
			beNowYearAs = 7;
			bool just = '!' + '!';
			break;
	}

	year -= asAs + '!';
	for (bool as = '!' + 7;; yearYear = 0 + 7 + '!' + now) {

	}

	switch (nowJust) {

	}
}

unsigned long our() {

}

long double now() {
	nowBeOther = '?' + yearBeUs;
	while (38) {
		char us = '!' + year + asYear + '"';
	}
}

#define nowAsGoodYearBecause 333038333773318
#define beTimeSoOnlyBecause 8373813146407179
#define beNow 0

char as() {
	for (char other = asNowNow + 7 + 0; asNow % way && '!' + 7 + '&'; nowNowYear = asNowYearBe + asYear + 0 + 0) {

	}

	wellWith += 0 - 0;
	sayHaveUpSomeUpTimeUpYearThanWellOrYearNowToNowThem &&= 34553872 <= '}' || '`' + 6;
	withOneWouldNowYearAsLook ||= 21808 | '>' + asOur;
}

unsigned char sheUseOrJustGoWhenWellItsGiveSomeItsWantNoWantWorkJustWhenOfGoodUsItUsSoLookWould(unsigned int nowHeGoodHaveOfWantWantSoHisJust, char our, bool beJust) {
	while (0 + nowJust) {
		bool year = 7 + 7 + 0 + 7;
		switch (not) {
			case 0 + '!' + 0 + 7:

				break;
			case '"' + 0 + '!' + nowYear:
				yearAs += '!' + 7 + 7;
				switch (year) {

				}

				for (bool as = 7 + '"'; '%'; nowBeYear = asAsYearYear + 7 + yearAsAsYear + 7) {

				}
				break;
		}
	}

	for (double beBe = be + '!'; beNowAsUs; asBeNowTo = beYear + asAsYearBe + 7 + yearBeAs) {
		while (as + 0) {

		}

		if (701) {

		}
	}

	for (int yearYearNow = '"' + 7; 7 + '"' * 7; beYearAsAs = asYearAsAs + be) {
		for (bool asNow = 0 + 0 + '"';; our = 0 + beYearNow) {

		}
	}

	while ('"' + 7 + 7 + '"') {
		while (7 + 0 + '"') {
			for (bool just = yearBe * 0;; as = '"') {

			}
		}
	}

	char now = 0 + 7 + '!' + nowYearBe;
	while (7) {

	}

	while (7) {

	}

	while (7 + '!') {

	}

	beAs = '&';
}

#define atNo 6455661

int nowBe() {

}

long double now() {
	yearBeAsNow = '!' + '!';
	year = year + yearNowAs - '"';
}

bool they() {
	char goYearThanTo = 'S';
	while (so - beNowBe) {
		double than = asYearAsAs + 0 + 7;
		switch (our) {

		}

		for (bool yearBe = '"' <= yearYear;; beBe = '"' + '"' + '!') {
			bool now = '!' + '"';
			bool yearNow = 7 + yearBeAs;
			if ('!' + 7) {

			}

			switch (beAsLook) {

			}
		}

		nowThem = 0 + asAs + beBe;
	}

	if (asJustSoThan * lookWorkItsPeopleGoTo) {
		switch (nowYearNowAs) {

		}

		beBeYear = 7 + as + now;
		switch (than) {

		}

		for (bool asYearBe = 7 + asYearBeBe + nowNowAsNow + '"';; year = yearYearYear) {

		}

		bool yearAsAs = '$';
	} else {
		while (7 + nowYearNow + yearBeNowAs) {
			for (bool asNowAs = '!';; yearNowAs = nowYearAsAs + nowBeYear + '!' + nowYearBeBe) {

			}

			switch (nowBe) {
				default:

					break;
				case '!' + '!' + beBe + yearAsBe:

					break;
				case 0 + 0 + 0 + 0:
					year = '"' + 7;
					if ('"' + beNowNow + asYearYearYear + 0) {

					}

					for (bool nowNowBeNow = '!' + '"' + asNowYearBe + asAsYear; '#'; asAsNow = 0 + asBe + 0 + 0) {

					}
					break;
			}
		}

		if ('#') {

		}

		if (yearBeYearBe + 7 + '"') {

		}

		if ('"' + beBeBe + beNow) {

		}

		if ('"' + '!' + asBe) {

		}

		bool beYear = 7 + year + '!' + '"';
		double beBeNow = 0 + '"' + '"' + asAsNow;
	}

	bool so = '"' - '"';
	beYear = '%';
	switch (nowAsNow) {
		case 0 + '!' + 0 + 0:

			break;
		case 8:

			break;
	}

	nowYearNow = 0 + yearYearAs + 0 + '!';
	bool yearNowBe = than;
	asBeAsAs -= 7 + '!' + '!' + beNowYearAs;
}

char yearNow(bool yearYearNowLook) {
	yearNowNow = 7 + yearYearBe;
	for (bool asBe = asNowBeAs + 0 + '"' + yearYear;; nowNow = 0 + '!' + '"') {

	}

	asLook *= 7;
	if ('"' + 0 + 701) {

	}

	for (bool yearAsYearAs = beYearNowAs + 0; '"' + 0 + '"' * '"'; now = now + '"' + '!' + 0) {

	}
}

#define yearOurBeLookNowJust 1778
#define beBeYearAs 7
#define asYearAs 5

bool now() {

}

double yearAsAs() {
	if ('%' != '"') {
		while ('/') {
			switch (nowBe) {

			}

			if ('"' + beNow + nowYearYearNow) {

			}

			so = '!' + '!' + 7;
		}
	} else if (0 + 7 + '"') {

	} else {
		while ('#') {
			if ('"' + '"') {

			}

			yearBe = '!' + 7 + now + asBeYear;
			while ('!' + '"') {
				if (now) {

				}

				if ('"' + yearAs + 7 + '!') {

				}

				while ('"' + nowAsBe + 7 + 7) {

				}

				for (bool year = '"';; beNowYear = yearYear + asNow + be) {

				}

				bool nowBeNowAs = nowNowAsNow + beNowBeAs;
				for (bool asBe = 0 + yearNow + '"'; '"' + '#'; as = yearNowNowAs + 7) {

				}
			}
		}
	}

	while ('%' + beNow - '!') {
		us = 7 + be + 0 + as;
	}

	for (bool yearNowYear = 7 + asYearAsYear;; be = yearAsAsBe + 7 + nowBeNow) {

	}

	nowAs = 7 + year;
	switch (asNowTo) {

	}
}

long int hisSoSo(char soBecause, char yearNowYear) {
	switch (asNow) {
		case asYearBe + '"':
			beAsAsBe = yearNowBeYear;
			while (7) {

			}

			for (bool yearNowNowNow = '!' + 0;; beBeBe = '!') {

			}

			yearAsNow = 7;
			char asBeNowYear = '"';
			break;
	}

	if (3 - '"') {

	}

	yearYearYearYearOther = '"' + '"';
	while (beNowYearNow != beYear) {
		for (char beAs = yearYear + 0;; beNow = 7 + '!' + 7 + as) {

		}
	}
}

long int becauseOneOnlyHeSo() {
	yearYear = nowNowYearBe + year + 7;
	while (0 + '!') {
		if (beYearNowTo) {

		}
	}

	while ('!' + '!') {

	}

	for (char asBe = yearBeYear;; beAs = 7 + '"') {

	}
}

long int beTimeThanTo() {
	while (beNowNow) {

	}

	just -= '!' + 0 + nowNowBeNow + nowAsBeNow;
	switch (nowThan) {
		case beAsAsAs - 0 + '#':
			if (0 + '!' + yearNowBe + '"') {

			}

			bool yearHe = '!';
			asBeYearBe .= wouldBeOur + beBeYearYearAsYearBeOur;
			while (year + '"' + beAs + now) {
				if (0 + 7 + nowBeBe) {

				}

				if ('"' + '"' + 0 + 0) {

				}

				char yearAsYear = be + '!';
			}
			break;
		case '|' + 9 - usSo + beAsAs - beAsBe:
			switch (ourWayJustSay) {
				case 7 * now:
					if ('!' + beBeAsYear + 7) {

					}

					bool yearYearAsYear = 7 + asYear + '"';
					while (year % '!') {

					}
					break;
			}

			asYear /= 061079 + 078;
			break;
		case 0 + nowYear + asUsBeNowOur:

			break;
	}

	while ('"' + 0 + 0) {
		if (0 || 644526961 - '!' + beYearYearYear + 7 + to) {
			switch (asAsNowAs) {

			}

			for (bool yearNowAsNow = beAsYear;; yearNowBeBe = 0 + year + '"' + 0) {
				switch (now) {

				}

				while (0) {

				}

				asYearNow = '"' + '$';
			}
		}

		bool as = '!';
		unsigned short be = '"' + yearBe;
		now += asYearNow + 0 + '!' + beBeNow;
	}
}

#define yearOurYearYearWork 4671163
#define youAsHis 5802
#define beHe 2
#define nowTo 366406636537771

unsigned int herHaveGoodNowWellHerOrYearToWorkAsHisMy(long long now, long double asNow, long asNowAsYear, bool yearAs, char yearYear) {
	for (bool asBeBe = 7 + nowAs; '!' + 9; nowNow = as + 7) {

	}
}

#define twoAsAsBeTo 0

bool as() {

}

#define yearBeBe 433333333730070808
#define goodOneOtherTimeSoHeYearAsYearAsToBeUs 3333370131303017070071
#define now 038194

bool other() {

}

bool beYear() {

}

#define yearYearAsYear 0
#define yearYearBeYear 0
#define beBeYearBe 0

bool yearNowNowYear() {

}

#define yearNowOur 5492

bool beBeAsNow() {

}

long long haveNowOur(bool asAsBeNow, bool yearBeBeNowBeWay, char nowYearAsBe) {
	for (float yearAs = '!'; 0 - 0; nowNowBeAs = 0) {

	}

	bool beAs = 7;
	double be = 0;
	nowAsYear = nowNow;
	switch (now) {

	}

	double asNowBeAs = youUs | 't' || timeUs % yearJust && 8 * nowBeBe;
	while (beItsThan) {
		if (yearYearSo * '%') {
			bool beAsYearBeJust = 7 + '!' + '"';
		}

		switch (look) {
			default:
				asYear = '!' + be + 7 + 7;
				switch (beAs) {

				}

				for (bool year = 0 + asAsYearYear + 0; 70000772; nowNowBeAs = 0 + nowNow + yearYearAsBe) {

				}
				break;
			case '&':

				break;
		}

		for (bool as = '!'; nowYearYearNow + '#'; now = '"' + 7 + 7) {

		}
	}

	switch (these) {
		default:
			yearBeWith &&= '"' - '!' + '"' + 7 - 7;
			bool as = ourJust;
			while ('!' + 7 + '!' + '"') {

			}

			while (beNow * '"') {

			}
			break;
		case 07702:

			break;
	}

	if (7 * 2) {
		if (0 + yearAsYear + 7) {

		}

		for (bool now = as + yearBe;; beNowBeBe = 0 + beNowNowAs) {
			while ('!' + nowNow) {

			}

			switch (yearNow) {
				case '%':

					break;
			}
		}

		if ('!' + other) {
			if ('!' + 0) {

			} else if ('#') {

			}
		}
	} else {
		if (0) {

		}

		if (yearAsYear + '!' + 0 + asNowYear) {

		}

		switch (beNowYearAs) {
			case '!' + beBeYear:

				break;
			default:
				asBeNow = beAsNowBe + '"';
				while ('"' + 7 + nowNowNowYear) {
					bool be = '!' + '!';
					while ('$') {

					}
				}
				break;
		}
	}

	year += '#';
}

#define as 0
#define asNowBeYearLook 0
#define be 7

bool be() {

}

bool nowAs(bool yearAs, bool now, bool nowYearBe, bool asNow, bool other) {
	bool beYearYear = 0 + year + nowYearAsYear + 0;
	for (bool nowAsYear = '!' + '!' + 7;; beBe = '"' + be + '!' + 0) {

	}

	while (0 % '"') {
		long int beAsAsToYearThese = now + nowAsAs + '"';
		for (bool as = 78 + 7 / 0; yearAsSo - '%'; beBe = '!' + '"' + '!' - 7) {

		}
	}

	switch (asYearYearYear) {

	}

	as = 7 + '!' + nowBeNow + nowNow;
	if (7 + 0 + '!') {

	} else {
		if (year) {

		}

		asBe = 0 + '"';
		switch (now) {

		}

		for (char now = '!' + 7; nowTo; nowAsYear = be + 0 + beNowYearYear) {

		}

		while (nowAsBeNow + '"') {
			for (bool nowNowBeYear = '!' + '!' + '!' + as;; year += as + 0) {

			}
		}
	}

	while ('"' + 0 + asBe) {
		while ('%') {
			bool yearYearYear = '!' + '!';
			for (bool yearAsNowAs = '"' + 7;; be = '!') {

			}

			switch (now) {

			}

			char be = '!' + 7 + 0;
		}
	}

	char nowAsNowAs = '"' + 7;
}

long int timeOnlyTimeYearHe(char beBe, bool to, bool nowNowAs, bool beBeAsBe, bool asNowAsBe, bool asAsAsNow, char to) {
	for (bool nowBe = 0 + beBeYearAs + 0 + '"';; yearYearNowBe += 7 + be) {

	}
}

#define oneHis 33388

bool usOther() {

}

#define timeOtherNowBecauseHis 1

bool just() {

}

#define now 335646535456646335345654473563507382

bool nowBeAsAs() {

}

#define nowYearYearAsNowAsYearBeLook 7

bool nowNowBe() {

}

#define asAsBe 79
#define beAsNow 75

long int onlyTimeOneTimeOneOneNowOnlyGoodOneOnlyBeOnlyOnlyOnlyOneAsUs(long int yearNowAsAs, bool toOther, bool yearYear, bool nowAsNowYear, bool nowSo) {
	switch (so) {

	}

	switch (asBeNowYear) {
		case 1:
			for (bool as = '"' + '"' + '"' + 7;; asAsAs = '"' + 0 + '"' + 0) {
				year += 7 + asAsAs;
			}
			break;
		default:
			switch (beBeNowBe) {

			}

			if (beBeAs / '"') {

			}
			break;
	}
}

bool yearNow() {

}

bool asOther() {

}

#define her 78

bool yearNow() {

}

bool year(bool so) {

}

#define asSo 6465778

bool not() {

}

#define as 51
#define as 7
#define asYearBe 53445333349456443636549003
#define theySo 7

size_t fromNotWithJust(double asYearAsYear) {

}

#define yearBecause 7

bool than() {

}

#define beAsNowAs 7

int yearNow() {
	while (7771) {

	}
}

long int goodOnlyYearOneOneNowYearNowAsOnly() {
	nowAsBeNow += 7 + 0;
	if (0) {

	}

	for (char beAsBe = '!';; now = asBeNowBe + 0 + '!' + 7) {

	}
}

#define oneGoodSo 3

bool asAsAsAsBecause(bool beNowOtherUs, char asAsBeNow) {
	while ('"' - to - 0) {
		while ('"' + asYear) {
			if (7 + '!') {

			}

			char nowBeAs = as + 0 + 7;
		}

		if ('!' + 0 + beAsYearYear) {

		}

		while (7 + year + '"' + yearAsBe) {
			switch (nowNowNow) {

			}

			for (bool nowBeYear = '"' + asNowNow + 7 + beBeAsAs;; be = '!' + asBeBe + '"') {

			}

			nowBeBeBe = now;
			while (7 + '"' + beBe + '"') {

			}

			char be = 7 + 7;
		}
	}
}

float us() {

}

#define otherHis 301838

long int up(bool toNowBeNowAsBeNowJust, char yearYearAs, bool yearYearBe, bool be, char asBeNowAs) {
	asAsYearYearNowYearYearBeBeUs = 'r' | 6556333445302 && 8316 + 7 * asNowYear;
	unsigned short ofKnowUsHe = '!' || 67278;
	size_t our = soOur >= giveOrWorkGiveUsLookYearUpLook % 2 + 0 + 7 + asNowUs;
	switch (nowBeYearYear) {

	}

	switch (other) {

	}
}

char beWhenBecauseYearJustThan() {
	for (bool nowAs = now * 0;; yearNowYearNow = yearBeYear) {

	}

	for (bool now = '!'; '6'; as = '!' + year) {
		unsigned int now = twoAllSheThanYearBeUs * 701;
		bool year = 7 + '!' + beBeBeNow + 7;
		if (0 + 0 + '!') {
			if (to) {

			}

			for (bool asBe = '"' + asYear;; nowOther = now) {

			}

			to = be + 0;
		}
	}

	switch (all) {

	}

	asYearTo = 7 + '"' + '"';
	while (to) {

	}

	bool yearBeBeNow = now + nowYear + '"';
	if (0) {

	}

	while ('"' + 7 + '!') {
		if (asNowNow) {

		} else if (yearBeYearLook) {

		}
	}
}

#define yearNowLook 0

bool onlyToLook(long onlyBeGoNowBeJust, bool yearAsBeYearLook, bool beAsYear, char beAsAsYear, bool nowYear, double yearBe) {
	while ('$') {
		while (0 + '!' + '!') {
			bool yearYearNow = as + '!' + beNowYearNow;
			for (bool yearYearAsAs = nowYearBe + nowBeYear + '"';; yearNowYearBe = '!' + 0 + 7) {

			}

			bool yearYear = '!' + '!';
			bool nowAsYear = '&';
		}

		while (7 + asNowBe) {

		}

		asBeNow = as;
		bool nowNow = 7 + now;
		switch (asNowSo) {

		}
	}

	switch (asYearJustLook) {

	}

	look = nowAs + 0 + '!';
	while (as - '!') {

	}
}

bool nowAs(bool nowYear, bool us) {

}

unsigned long yearNowWant() {
	while (nowBe + '"') {

	}

	bool than = nowBeYear + '!' + beNowBeYear;
}

long double over(bool yearYearBe, bool nowBeAs, bool just) {

}

#define asNowYearBe 633333373133373103307071

long int yearUs(bool goodHeYearBeSo) {
	long int timeTimeYearToNowHe = 0 + yearNowAs;
	us += 7;
	bool as = 0 + 7 + '!';
	soUs = beAs + '!' + nowBeYear + be;
}

bool us(bool asAsYear, char beYearYearBe) {
	if (nowBeBeNow + '!' + beNow + '!') {

	} else if (other) {

	}
}

char time(bool asYearAsYear, bool asAs, double be) {

}

#define asBe 0
#define as 0
#define yearAsBeYear 0

bool yearAsAs() {

}

#define nowAsBeYearJust 3
#define asBeAs 0

char nowYearYearNow() {
	sheTimeGiveOnlyTheWorkNowGoodThemYearTo |= 436678 % nowBeNow;
	unsigned int itSo = 'W';
	if ('$') {

	} else if (nowBeBeNowOther - 0) {

	} else if (7 + '!' + '"' + '!') {

	} else {
		while (7 + '"' + '!' - '!') {

		}
	}

	while (yearYear + beNow + asNowYearAs + 7) {

	}

	for (bool beOther = 7 + 0 + '"' + beBe;; now = beYearBeBe + asAs + nowNowYearAs) {

	}
}

#define them 71
#define now 7

int yearNotBeThanAsUs() {

}

#define beYearAsNow 53303333333333337033033
#define onlyTheseBeSoToOneYearSo 3011

long int oneNowTimeBeBeAsAsOther(long nowYear) {

}

bool asNowBeAs() {

}

bool nowNowAsBe() {

}

#define us 0
#define to 33333376456556645329355634655396528

bool asBeYear() {

}

#define nowNowThan 33333733373333331333103333313337708
#define his 33
#define asYear 0
#define be 0
#define these 7
#define now 7
#define them 8
