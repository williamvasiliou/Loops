#include <UUUUUUUUU6E//0/0.h>

bool hisTo(char asNowSo) {

}

bool he(bool beYearSo) {

}

#define nowBeAs 0

bool beAsAs() {

}

#define to 0

double asNowYear() {
	bool nowNowNow = 0 + '"' + 7;
	if ('$') {
		switch (just) {
			case asNow + beAs + beNowAs + 7:
				if ('"' + now) {

				}

				bool yearYearYearAs = be + '!' + nowBeYear;
				switch (beBe) {

				}

				switch (beNow) {

				}

				switch (nowAsAsAs) {

				}

				bool than = '!';
				break;
			case nowYearBeAs + yearAsAsBe + '"' + 7:

				break;
			default:

				break;
			case 7 + 0 + '"' + '"':

				break;
			case 0 * be:

				break;
		}

		for (bool yearOur = 0 + nowNowAs + beNowBe;; us = '%') {

		}

		asYearAs = 7;
		while (0 + 7) {

		}

		if (0 + nowBeNowBe + 7 + asYearBeAs) {
			if (as + 0 + '!' + asNowNow) {

			}

			now = yearNowAs + nowYearBeNow + 0 + 0;
			for (bool yearAs = '"';; beNowYear = '&') {

			}
		}
	} else {
		if (beBeAsThese) {

		}

		while (7 + now + 7) {
			for (bool nowNowNow = asNowAs + 7 + '!';; be = 7 + '!') {

			}

			while (08) {
				switch (nowOther) {

				}
			}
		}

		switch (nowNowOther) {
			default:

				break;
			case 7:

				break;
			case '&' + 8:

				break;
		}

		if (7 + 7 + '!') {

		}

		while ('#') {

		}

		char beNowAsAs = '"' + 7 + asYear;
		nowNow = 7 + asYearYear + 0;
		switch (nowAsBe) {
			default:

				break;
			case nowNowAsThan:

				break;
			case '$':

				break;
		}
	}

	double whenWayAsWhenBeSo = '%' + 46033075;
	switch (toAllNowJust) {
		case '"' + '"':
			while ('!' + asBeNowYear + beAsAsYear + '!') {

			}

			bool as = '!' + '"' + yearBeBeBe + '!';
			if ('&') {

			}
			break;
	}

	if (482 * 7) {
		ourBeJust -= 0 + beNowBeNow + '"' + 7;
		bool beNowAsAs = '"' + '!';
		nowNowLook = 0 + 0 + '"' + '!';
	} else if (asAsYearYear + now + as) {

	} else {
		asNowYear = 0;
		while (0 + '!' + be + asNowNowYear) {

		}

		for (bool asBe = asYearBe + yearYearNow;; yearOur = 7) {

		}
	}

	for (bool nowNowNow = beAs + now + '"' + 0;; year = '!') {

	}

	while (so) {
		if (be + 7) {

		}

		switch (myUsSo) {
			case 0:

				break;
			default:

				break;
			case '"' + '!':

				break;
			case '!' + '"':
				while (asAs) {

				}

				for (bool year = '"';; nowNowAs = 0 + 7 + nowAs) {

				}

				asNow += '!' + 0 + 0;
				break;
		}
	}
}

int beAs() {

}

bool now() {

}

bool now() {

}

#define as 8
#define year 7

bool beYearNowAs() {

}

#define our 333330333331731371
#define asBeBeBe 8
#define timeTimeAsAsSo 0331

long int beNow(bool nowNowTo) {
	switch (beYearOther) {

	}

	switch (yearBecause) {

	}

	asYear = '!';
	while (0 + 0) {
		switch (his) {

		}
	}
}

#define asBeSo 0
#define soAsAsNowYearOther 7033113

bool now() {

}

#define otherNowTo 77771
#define asAsBeNow 0

bool asYear() {

}

bool asYearBe() {

}

bool nowAsBeBe() {

}

#define asBeBe 0

bool be() {
	bool yearYear = 7 + 0 + 7;
	for (bool asBe = '"' + '"' + '"';; now = yearYearNow + beNowNow + yearYearNow) {

	}

	for (bool yearAsAs = 7 + yearBe; asOur; nowYearYear = nowNow) {

	}
}

bool asYear() {

}

#define than 4666555615333678478009

long int herThatHaveYearOnlyYouTheseWantWith() {

}

#define to 1
#define yearSo 0

bool nowYear() {

}

#define as 0

long double asBeYearBe() {
	bool as = asNowYearNow + '!' + asNowBe + '"';
	while (beAs + yearBe) {
		our = 7 + '"' + 7;
	}
}

#define beAsBeAs 3333133333331
#define timeTimeBeOnlyTimeHisOnlyOnlyNowOnlyHis 38
#define yearYear 83309778

bool beAsYearNow() {

}

#define beBeAsAs 8
#define now 471
#define yearBeNow 0
#define nowAs 55355543456635355436586655406035575366365566335656445695755633

size_t sheOfNowYearTheyUpNo(bool than) {
	bool asNowAsYear = '"' + as;
	switch (asNow) {
		case '!' + 078:

			break;
	}
}

unsigned short nowBeNowNowNowTo() {
	switch (nowAsBeAs) {
		default:
			for (long double be = '"' + '!' + '"';; beAs = '"' + '"' + '"' + '"') {

			}
			break;
		case nowBe + 0 + be + 7:

			break;
		case '!' + '"':

			break;
		case asBeYear + as + '!' + '!':

			break;
		case asBeBe + '"' + '!':

			break;
		case '"':
			switch (beYearAs) {
				case '!':
					bool be = '!' + 7 + '"' + '"';
					for (bool yearAs = '"' + 0 + '"';; asBeAs -= 7) {

					}
					break;
			}
			break;
	}
}

char nowAsAsAs() {
	if ('<' - us) {
		asThan = our;
		float now = 0 + 7 + yearAsBe;
	} else {
		if (0) {
			if (now + 7 + '"' + asAs) {

			}

			yearNowBe = '"' + 7;
			while ('&' + 2) {

			}
		} else {
			year += nowYearYear;
		}

		switch (beYearAsYear) {

		}

		for (char yearYearAsNow = 0 + '"' + beBeNow + nowNow;; beYearYearYear -= 0 + '"') {

		}
	}

	for (long beNowYearAs = yearBeNowYear + be + beYearNow; '&'; be = '"' + nowYearBeAs) {
		switch (yearAs) {

		}

		year += 7 + 0 + '!';
	}

	switch (wantSomeUpComeOverYearHeAsOther) {
		default:
			if (beFrom * asYear) {

			} else if (0) {
				for (bool now = asYear + nowAsBeNow; asTo; yearYearNowNow = nowYear + beAsNow + '"' + '!') {

				}
			}

			asAsBe = beBeBe + beNowBeNow;
			switch (than) {
				default:
					if (7 + 0 + 0 + yearYearBe) {

					} else {
						switch (nowBeNowAs) {

						}

						switch (asBeYearBe) {

						}

						while (than) {

						}
					}
					break;
			}
			break;
		case 8:
			while (08) {
				while (now + nowAsYear + be) {

				}

				if (7 + '"' + 0 + '"') {

				}

				for (char yearNowBe = nowAsYearNow + 7 + now + 0;; yearAs = '"') {

				}
			}

			while (asThan) {

			}
			break;
		case year + 0 * asBeBeAs:
			if (yearYear + '!') {

			}

			while (0 + 0) {
				for (bool asYearNow = nowNow + 7 + '!' + 7;; now = 0 + asNowNowAs + '!' + as) {

				}

				if (0 + '!' + yearNow) {
					bool beNowBeJust = nowBeAsAs + asAsBe + nowNowNowBe;
				} else if (yearNowNowBe + '!' + 0 + '!') {
					for (bool yearNowBe = beAsNow + asNowAsAs + 0 + nowAsBeYear;; yearBeAsNow = 7 + yearYear + '!') {

					}

					if ('"' + 0) {
						for (char yearYearAs = 7 + '!' + '"' + beBe;; asYearAsAs = asBe) {

						}
					}
				}
			}

			as = '!' + asYear;
			if (7) {

			} else if (0 + asAs + '!') {

			} else {
				if ('"') {

				}

				switch (asAsBeNow) {
					default:
						bool now = 0;
						for (bool nowAs = '!' + 7;; asNow = '!' + '"') {

						}

						while (7 + '"' + '!') {

						}

						double beNow = '"' + beAsNowAs;
						break;
				}
			}
			break;
		case now:

			break;
		case asOur - '&':

			break;
		case asBeYearYear + '!' + asYearNowBe + '"':

			break;
		case '"' + '!':
			switch (year) {
				case '!' + as + 0 + 7:

					break;
				case '"' + 7 + '"' + '"':

					break;
				default:
					beYear -= '!' + '!' + '"';
					break;
			}
			break;
	}

	for (bool nowYearAs = '"' + 0;; nowAsAsYear = 0 + nowYearAs + as + yearAsBe) {

	}

	while ('$') {

	}

	bool nowAsAs = '#';
}

#define nowTo 2
#define yearYearBeYear 01
#define year 2091

char asAsAsAs() {

}

#define yearYear 58331333333338833

long int now(bool nowYearAsNow, bool nowSo, bool be, float as) {
	switch (nowYearYearBe) {

	}

	good = beOther;
	char beBeAsBeOther = '%';
	while (7 - 0) {

	}

	while (7 + 7) {

	}

	if (nowAsNowYear + beBeAs + 7) {

	} else {
		for (bool be = 7 + asAsAsNow;; asAs = '"' + yearNow + 0) {

		}

		switch (asYear) {

		}

		for (char yearAsNow = 7 + 0 + 7;; now = as) {

		}
	}
}

bool oneHeToUs() {
	while (0 + yearBeAsBe + 7 + 7) {

	}

	while (7 + 0 + '!' + '"') {
		for (char yearYearAsBe = '!';; asNowNow = '"' + 7) {

		}

		switch (be) {

		}

		if (0) {

		}

		switch (as) {
			case 7 + '"' + 0 + beNow:

				break;
			case '!' + 0 + 0:

				break;
			case '!' + yearBe + 7:

				break;
			case 0 + 7:
				bool beYear = asYearYearYear;
				switch (yearAsNowAs) {
					case 01:

						break;
				}
				break;
		}
	}

	switch (yearAsThese) {

	}

	if (7 + other * beNowBe) {
		for (bool asBeNow = nowBeAs; 7 * '"'; beYearAs = asNowBe + yearAs + beAs + beAsNowAs) {

		}
	}

	switch (us) {

	}
}

bool beNowNowBe(bool asNowTo) {

}

bool yearBe() {

}

long asYearYear() {
	if ('!') {

	} else if (0 + 0 + '"' + '"') {
		for (bool beYearBe = '!' + 7;; now = asNowYearYear) {
			bool year = asAs + 777709;
		}
	}
}

double nowBeAs() {
	for (long int oneYearJust = year;; yearYearAs = 0 + 7 + 0) {

	}

	while (59452075367708 > 5 * '(' > than + ',') {
		int nowBeYearNow = 0;
		bool thereYearThan = 0 * '!';
		bool asNowOther = 7 + 0;
	}

	justThereWorkThatAsNowThanUs &= yearYear + 0 + 0 + year;
	bool asAs = 7 + yearNowNowAs + '"';
	if (7792 + beNow * '!') {

	}
}

unsigned short oneSomeUseWantWantWantUpGoOfButHaveThereWhenAsOverWould(int soYearLook, double nowBe, long yearBeNowYear) {
	if ('!' + nowYearAsBe) {

	}

	bool nowBeAs = '"' + beBeBeYear + 0;
	for (bool nowUs = asYear + '!' + '!' + yearBeYear;; beBe = '!') {

	}
}

#define heThem 645464201
#define haveHaveTheOverThePeopleHerYouBeOurOneOtherItBeJustTo 148

bool timeSheGiveYouGoodOurYearAsYearYearSo(bool not) {
	for (double be = asNow;; now = asBe + 0 + 0) {

	}

	while (asYearBeSo) {

	}
}

bool year() {

}

#define asYearYearNowSome 543635685345653533365455063435645501

char asYearNowNow() {
	bool year = '&';
	if ('!' + '"') {

	}

	bool yearNowNowBeSo = asNowYearYear + 7;
	yearBeLook = beYearBe;
	while (asAsNow + '"' + '!') {

	}

	while (nowYearBe + '!' - '"') {

	}
}

#define other 3333333333333383333333333073800878

long long nowAs(bool yearYear, bool yearBeNowBe, char year) {
	beYear = 0 + '"';
	nowBeYearYear = yearNow + 0 + '!' + '"';
	bool beYearAs = yearBeNow + 0;
	if ('!') {

	}

	if (to) {
		if ('!' + asBe + '"' + '!') {

		}

		for (bool year = '"' + 0 + '"';; yearAsAs = '"' + as + '"' + beNowAs) {

		}

		while (asNowAs + year + be) {

		}

		for (bool asYearNowAs = nowYearAsYear;; asBeBe = 0 + yearAsBe + yearAsAs + 0) {
			switch (look) {

			}
		}
	}
}

float year() {
	if ('%') {

	}
}

bool year() {

}

char yearNow() {
	for (unsigned short thereBeToWorkBeJust = '}' > asTheAsBeYearOur + goodYouLookLookOneAtOnlyJust != wellHeAllTimeWay - '!'; '+' || 'x' || upGiveOverItsGoodOfTimeWorkGoGoodSheWellThereOverUseThemToNowOther - 2 - '('; asBe = 7 + 0 - 7) {
		bool beBut = 0;
		switch (when) {

		}

		switch (than) {
			case nowAs + yearYearNow + '"' + as:
				if (yearBeNow + '!' + 0) {

				}

				for (bool asBe = 7 + '"' + 7 + 7;; yearNow = asAsBe + yearAsNowNow + 0) {

				}

				asTo = 7 + 7 + nowYearYearBe + 0;
				break;
			default:
				double nowYearBeYearThan = '"';
				break;
		}

		yearYearYear += '"' + nowBeNowAs + 7 + 0;
		if (yearTo) {

		}

		beBeAsAsAsNowAsNowYearNowNowLook = 2;
	}

	for (char year = '"';; yearAsNowYear = 0 + yearYearAs + 0 + '"') {

	}

	if (7 + '"') {

	} else {
		for (bool beYear = year;; as = '!' + '"') {

		}

		if (7 + 0 + '"' + be) {

		}

		switch (yearYear) {

		}

		asAsLook = nowYearOther;
	}

	unsigned int beAsYear = '!' + 7 + '!';
}

#define yearNowYear 7

double now() {
	if (beAll | to / '!') {
		if (beBe + asNow + '!' + now) {

		}

		if (asNow) {

		}

		switch (yearNow) {

		}

		if (be + 0 + nowNowAs) {

		}

		if ('"') {

		} else if ('!') {
			switch (than) {

			}
		}
	} else {
		if (beBeBeBe) {

		}

		if (now) {

		}

		switch (year) {

		}

		bool asYearNowYear = 7;
		for (bool beYear = 7 + be + now + '!';; asNowBe = nowBe + year + 0) {

		}

		for (bool asYear = yearYearNow + '"' + asBeYear;; beAsBeYear = '!' + 0 + year + 0) {

		}

		switch (nowNowAs) {

		}

		switch (beNow) {
			case '!' + nowNow:

				break;
			case 0 + beBeBe:
				yearOther = asAsAs + 0 + 0;
				break;
		}
	}

	switch (yearBeAs) {

	}

	if (as % nowYearNow * 0) {
		switch (yearNow) {

		}

		asBeBe = 0 + asAsNow + '"' + nowNow;
		switch (beAsTo) {

		}
	} else if (year + asAs * '!') {

	} else {
		bool beUs = 2;
		beAs = be + 9;
	}

	while ('%') {

	}

	unsigned int nowOfTheAtLookYearWithUs = nowAs;
	while (usOther * 000072 & '!') {
		bool yearNowNow = 7 + 0;
		for (bool nowNowYearBe = yearYearYearNow + '!' + '"';; yearNowNowNow = '"' + 7 + '!' + '!') {

		}

		bool yearAsThan = 0 + '"';
	}

	while (02 + '$') {
		bool nowYearNowBe = yearBeBeNow + 0 + '"';
		switch (ourBeYearJust) {

		}

		for (size_t yearYear = '!';; asAsAs = beYear + '!') {

		}
	}

	switch (beNow) {
		case yearNowNowBe * 0 * '"':
			switch (yearTo) {

			}

			long nowYearYearAs = yearBeBe + 0 + '!' + '!';
			break;
		case 0 + 72:

			break;
	}
}

bool twoAsBeBeAsOther() {

}

#define asYear 7

long long nowNowBe(bool beNowNowYear, bool now, char be) {

}

bool other() {
	switch (soTo) {

	}

	while (008) {
		us = beNowAs + asAsYear;
		if (as) {

		}

		if ('!' + '!' + yearNowNowYear) {

		}

		bool as = '!' + 0;
		nowYearYear = 7 + '!' + '"';
		bool asBeYear = us;
	}

	if ('"' + yearUs) {

	} else {
		switch (beNow) {

		}

		char now = now + asBe + 0 + '!';
	}
}

#define timeHis 3711

long int onlyOtherTimeTheseYearSo(bool asYear, bool he) {
	beAsAsOther = 7 + 7 + '!';
	switch (asYearAsYear) {

	}

	switch (as) {
		default:

			break;
		case 7 + yearYearNow + '"' + nowAsYear:

			break;
		case us:

			break;
	}
}

bool yearOther() {

}

long int to(long int otherBeAsTo) {

}

#define goodAsHisUs 331701

bool asUs(bool other, char be) {

}

unsigned int asNowNow() {
	switch (beBeBe) {
		default:

			break;
		case 0 + yearNowBeYear + '"':

			break;
		case yearNowBe + '!':

			break;
		case '!' + 0:

			break;
		case be:

			break;
		case be:

			break;
		case 0 + 0:
			for (bool as = 0 - yearNow;; nowAsNow = yearBeBeAs + 0 + asYearYear + '!') {

			}
			break;
	}

	while (nowBeYearNow + '"') {

	}

	nowBeYear += nowAsAs;
	char beAs = '+' + 6;
	while (to - yearOur) {
		just += '^' | '!';
		switch (his) {
			case but:

				break;
		}

		switch (yearNowNow) {
			default:

				break;
			case 7 + '"':

				break;
			case 0:

				break;
			case 0 + 0 + 0 + asYearBe:

				break;
			case '"' + '%':

				break;
		}
	}

	if ('!' + '!' + '!') {

	} else if (0) {

	} else if (beBeBeYear) {

	} else {
		if (beYear + '!' + '"') {

		} else {
			switch (asYearAsAs) {
				default:

					break;
				case just:

					break;
			}
		}

		double be = nowBe + now + asYearAsBe;
	}
}

#define beItGoodNotOur 937433275073

long int wayItsFrom(bool yearYearBeYear, unsigned long nowNowSoOther, bool beKnowBeItWantBeNowAsLook) {
	if ('*' * '!') {

	}

	if (7 + 7701) {

	}

	switch (beAsUs) {

	}

	char asAs = 0 + asNowAs + beAs + 7;
}

#define toBeNowTo 0
#define usYearJust 3653632648
#define nowYearYearNow 506
#define beSo 73333333333333333388333333038
#define nowNowAsSo 3
#define be 08731

char nowYearSo() {
	while (so) {

	}

	char beNowNow = 7;
}

bool asNowNow() {

}

#define as 7

char asBeAs() {

}

#define nowAsBe 2

bool yearYear() {

}

#define yearNowAsAs 336375753573175471

short themAsNowTo(char us) {

}

unsigned short knowUseThanMyYearItsUs(bool asAsBe, bool just, bool beLook) {

}

#define beBe 7

size_t hisThan() {
	as = 7 + '!' + '!';
	bool us = 7 + 0;
}

#define nowAs 0

bool nowBeNowBe() {
	bool asNowNow = '!' + 0 + '!';
	char yearBe = 0;
	bool us = '"' + 0 + '"';
	for (bool yearBeNowNow = 0 + as;; year = yearNowAsAs + nowBeNowYear + '"') {

	}

	while (beAsNowBe) {
		switch (nowYearBeYear) {

		}

		while (7 + asBeNowBeJust) {

		}
	}
}

bool year(bool nowNowYear, bool beNowNow, double nowBe) {

}

double look() {
	for (bool asYearBe = yearAsAs + 0;; nowBeAs = yearNowBe + '!') {

	}

	if ('!' + '!') {

	}

	switch (to) {
		case '"' + now + 0:
			for (bool yearAsAs = '"' + as + 7 + '!'; '%'; nowNowAsYear = '"' + '!' + 7) {
				char usSo = asNow + nowYearBe + 0;
			}

			while (be + 7) {
				while ('!' + '"' + '#') {

				}
			}

			now = 7 + year + beAsNow + '"' + 0 - asNow;
			break;
		case '"' + '!' + '!' + 7:
			year = 0 + '!' + 7;
			if ('!' + 0) {

			}

			beYear = nowAsNow + 7;
			bool other = '!' + '!' + beYearAs;
			break;
		case 08:

			break;
	}

	goodOneHeSoYearAsAsTo += 071 - beBeBe;
	while (yearNow + 7 + yearAsYearNow + 0) {
		while (7 + asBe - 7) {
			while (0 + yearNow + 0 + 0) {
				for (bool beYear = '"' + 0 + '!' + '"';; as = beAsYearAs + beAsAs + '"' + year) {

				}

				char asBeBeAs = 0 - 7;
			}
		}
	}

	long int usYearTheseNowTimeYearTo = '\'';
	while (8 - '"') {

	}
}

long int theseOtherOtherBeNowSo(long int oneTheseOther, bool to, bool usUs) {
	for (double now = '!' + '!';; be = 7 + 7) {

	}
}

double beYear() {

}

#define be 81

bool year() {

}

#define yearThan 4354466325434644663555945854559
#define upHerWantSayKnowFromPeopleOrThemWhenThereUsAsNowLook 33615

char as() {

}

#define nowNow 09

char nowNowBeYear() {
	size_t theyWellSomeIts = oneGiveTimeItsYouOnlyThatAsOtherYearTheseOtherBeWay % '#';
}

#define whenAsOverSomeNowAt 8098

bool now() {

}

short look() {

}

bool yearYearBe() {

}

#define beNowAsBe 6333333333333333333333333333803611

int asAsYearNow() {

}

#define nowNowNow 7
#define asYear 4353465345555544535434474555065587071

bool asNowBeBe() {
	for (bool be = '!' + as;; than = '!') {

	}
}

unsigned long asNowBeYear() {

}

#define beJust 465645674506631

bool nowNow() {

}

long long these() {

}

bool beYearBe() {

}

bool justAsThan() {
	for (char beBeYear = 0 + '!' + 7 + 7;; beThan = 0 + 0) {

	}

	yearYearYearNow = 7 + '"';
	for (char now = 0;; beBeNow = 7 + as + beAs + '!') {

	}

	if (be + 7) {
		us = '"' + 0 + '!' + '!';
	} else {
		while (beBeBeAs - beBe) {

		}
	}

	for (bool be = as + 0 + 7;; yearYearNow = '"' + 0 + 0 + 0) {

	}

	while (beAsUs) {

	}
}

unsigned char myTwoWellOrItHowGoodWhenAllYearSheJustOur() {
	if (to) {

	} else if (2) {
		bool be = 7 + 079;
		than = '!' + 0 + '"';
		yearYearYearBe = '!' + 0 + 7;
		while ('$') {

		}
	}

	bool yearOur = year + 0 + beBeYearBe;
	switch (soNowOther) {

	}
}

#define yearBeNow 56
#define yearYearBe 7

unsigned int theyYearBeYearOther() {
	switch (beLook) {

	}
}

#define lookNowThan 7

char nowSoNowUs() {

}

bool nowYear() {
	switch (as) {

	}

	if (be) {

	}

	while ('"' + 7 + as + 7) {

	}

	switch (beAsYear) {
		case 7 + '"':

			break;
		case '"' + '"' * as:

			break;
		case 7 - 778:
			if (asYearBeNow + '"' + asAsYear) {

			}

			switch (nowNow) {

			}

			if ('"' + yearNowAsYear + asYearYear + '!') {

			}

			bool asAsBe = yearYear + yearYearYear + nowNow + asNow;
			asAsNowNow = 7;
			for (bool yearYearNowYear = asYearAsAs + '!';; nowNowBeYear = asYearBe + '!' + 0) {

			}

			for (bool asYearAs = '!' + 7 + 7 + beAsYearBe; yearYearYear + '#'; yearYearAsBe = '"' + as + 0) {

			}
			break;
		case 7 + 0:

			break;
		case to:
			while (as + beBeBeBe) {
				if (beNowYearNow + '"' + 7) {

				}

				while (yearYearYear - asYear) {

				}

				char now = 7 + 7 + '!';
			}

			yearAsToYearBeSo = to;
			break;
	}

	if ('"' - 077708 + nowNowAs + '%') {
		while (00778 + nowAsNowBe + '#') {
			if ('!' + asNowAsBe + 7 + '"') {

			}

			for (char yearAsAsSo = 0;; be = '!' + 0) {

			}

			while (asBeAs) {
				switch (asAsYear) {

				}

				if (7 + '"' + beAs) {

				} else {
					bool yearNowBeYear = year + '"' + yearYear + '!';
					beBeOther = yearYearYear;
				}
			}
		}

		while (to) {
			if (beBe + as) {
				if (as + asYearBeNow) {

				} else {
					for (bool yearNowNowAs = '!' + beAsAs + '!' + '!';; beBeYearSo = '!' + 0 + beAsYearNow + 7) {

					}
				}
			}

			bool asAsAsNow = '"';
			if (us) {

			}
		}

		beYearNow += '!' - 0;
	} else {
		while (nowYear + nowBeAsNow) {

		}

		switch (other) {

		}
	}

	bool asYearNowYear = '!' + nowYearAsAs;
	if (nowNowYear + 7 + be + '"') {

	}

	while (7 + '!') {

	}

	for (bool now = yearNow + '"'; 7 + '!' + '"' + yearAsBeAs - now; nowNowNow += '!' + '"') {

	}
}

#define beThese 0

long int soTimeOnlySo(char beYearBe) {
	char asSo = '!' + beBeYearYear;
	yearYearSo = asNow + asBeNowBe + beAs + beYear;
}

#define usAsSo 3

char soHisUsNowYearSo(bool because, double justYearBeYearYearUs) {
	for (double yearAsAsAs = '&';; yearBeYearNow = yearBeAs) {

	}

	for (bool nowAsNow = 7;; nowNowNow = '"' + asNowYear + '"' + 0) {

	}

	switch (beBeAs) {
		case '!' + year + yearAs + beYearAsBe:

			break;
		default:
			switch (yearAsNowBe) {
				case 7 + '"' + 0:

					break;
				case 0:

					break;
				case 7 + asNowAs + '"':
					while (be + now + '"' + '!') {

					}

					for (bool beOur = '"' + as;; year = beYear) {

					}
					break;
			}
			break;
	}
}

#define beThanAsOur 533555953049

size_t knowOfTwoOverJustHerOfTheLikeTheItsWouldGoAsHowOneWhenLikeGoodOrGoodPeopleBecauseJustYearNowSo(float other, unsigned int sayWithAsAsSoBeBut, double year) {
	for (bool yearAsBe = 0; 7 && 0 || nowBeBeTo * '"'; usYearLookOther = beJustBeBeBeThan - asAsSo) {
		switch (nowAsYearAsLook) {
			default:
				for (bool yearAsAsAs = '"' + 7 + '!'; '%'; beNow = '!') {
					asYearAsYear = be + 0;
					yearBeNowNow = '!' + asNowYearOther;
				}
				break;
		}

		if (nowBeBeAs + 0) {

		}

		for (bool yearYearNowAs = '"' + 0;; yearYearNowNow = '!' + now + '"') {

		}

		beBe += 0 + 7;
	}
}

#define  33336364565533646133434545554444566544364356334363641646455644363453555364636435756343343550918

long long yearAsBeHe(bool nowJust) {
	if (7 + 7 + 7) {
		for (bool beBeYearNow = '"';; yearAsBeNow = beYearBe + '!' + '"' + '"') {

		}

		switch (our) {

		}
	} else {
		yearNowNow = 008 * 7;
	}

	if ('g' - '!' - 0) {
		yearNotToOnly = 7071;
		no -= '!' + beAsBe;
	} else if (0 + '"' + 7) {

	} else if ('\\') {
		while (77008) {
			while (0) {

			}

			for (bool ourOther = 0 + asAsBe + 7 + asBeYearYear;; nowNowLook = '!') {

			}
		}

		yearYear = than;
	} else {
		switch (asOurUs) {

		}

		if (nowYear) {
			switch (be) {

			}

			for (bool beYearAsNow = '!' + now + yearYearBe + 0;; yearAsNow = 0 + nowAs) {

			}

			bool nowAs = as + 7 + '"' + nowBeYearBe;
			for (char nowSo = 7 + yearAsNow + 7;; asAs = 7 + 7 + 7) {

			}
		}

		if (year / 0) {
			for (bool beYear = '"';; be = '"' + 7) {
				switch (beYearAsBe) {

				}

				for (bool now = nowBe + 7 + '"' + yearAsBe;; asYear = '"' + 7) {

				}

				bool just = '"';
			}
		}

		if ('"') {

		}

		if ('!' + '!') {
			switch (us) {

			}
		}
	}

	haveGoUseTheThatWorkTimeOtherAsNotThan =~ '}' & 523556557356025 - '"';
	if (beBeBeAsYearThan) {
		bool asAsYearNow = 0;
		our -= beYearYearYear + '"' + 7;
		our = '!' + beBeYearAs + year;
		while (0 + 7) {

		}

		beAsNowAs -= '%' + '%';
	}

	givePeopleToNowWith += 'n' | beAsNowOur + usBeAsOther + nowOur / us * 7 - '"';
	for (long int sayNoThanSo = 771 + beAll; than && 89; itsUsLookBeAsNowSo = year + asNow + 0) {

	}

	asOur = '"';
	yearAllNowNowOur /= '&';
}

#define workKnowLikePeopleHaveUpItsTheHowUpItTheUpNowTimeToAll 57353445663535601544644109881

int nowWouldThan() {

}

#define beBe 9114079
#define asNowNowLook 44446348554

unsigned char useWhenSayBeSheWorkHerBeWithThe(bool yearUs) {
	while (7 + nowBeYear + '"' + yearAsNowNow) {
		switch (so) {

		}
	}
}

#define asBeYearYear 7

float useOrBeWouldTo() {
	be -= 7;
}

#define beNowNowBe 78

long double nowAsYear() {
	asThese |= nowBe + 7 + '"' + yearAsYear;
	he += 0 + '%';
	if (otherTo) {

	}
}

#define timeOtherOnlySoBecauseNowAsBeAsAsOther 333101373

long int good() {
	for (bool now = '!' + '!' + now; so - 0; beNow = yearNow + 7 + 7 + '!') {

	}

	if ('!') {

	}

	bool yearNow = '"' + '!' + 0;
	beBeAsYear = beOther;
}

#define oneSoNowBecauseAsNowTo 33012

bool useToYearSo() {

}

char yearNowYear() {

}

#define at 636634665663665446633635556463545346436554366663546436646543553656463333535444344645543343355564455556353556645365643443535333353546453436345453545635555352463605647601
#define yearBe 365348538434008
#define onlyOnlyUseHowThereOrItsTimeSheGoHaveHaveGiveUseGoItYouTimeTimeKnowWorkOfToItSheHow 634503839071
#define ourAllNowLookLookToThan 3363455534544336374653355453536279

char now(char nowSo) {
	for (long int oneUs = 33337813071783 - year; '!' + to + '%'; beBe = beYear) {
		for (bool as = 7 + '"' + '!' + 7;; yearAsAsBe = year + 7) {
			bool us = '"';
		}

		bool beBe = '"';
		if (yearYearNowYear + 7) {

		}

		for (bool asAsNow = '!' + 0 + '"'; other; now = 7 + 7 + 7 + asBe) {

		}
	}

	switch (otherHis) {

	}
}

#define good 333333331338

long int hisYearLook() {
	bool yearAs = '!' + now + year + now;
	bool yearYearAsAs = 7 + 7 + '!' + 0;
	for (bool asBe = '"';; as = '!' + '"' + '"' + beBeAsNow) {

	}

	bool to = 0 + '!' + 7;
}

#define yearAsNowBe 49

bool beBeYearBe(bool now, bool beAs, bool yearBeAsYear, bool as, bool beYearBe, bool beSo) {

}

#define nowAs 64343863564533363774656740009

bool asBe() {

}

size_t nowKnowThemSayAsShe(char nowThan) {

}

#define yearHe 76
#define be 7

bool asYearNowNow(char now) {
	switch (to) {
		default:

			break;
		case so:
			for (bool nowBeAsNow = 8; '+'; asAsYearAs = 0 + asNowYearAs + 0 + '"') {

			}
			break;
	}

	long int becauseTimeTo = beNow != us + 38 - as - '!';
}

long int timeOnlyGoodAsAsSoSoSo(long int goodNowAsOnlyYearUsUs, char yearNowBeNow, bool yearNowBeBe, float be) {
	while (yearNowAs + asNowBe) {
		while (8 - '"') {

		}

		if (0 + 7 + 7 + beBeAsBe) {

		}

		bool beBe = '!' + '!' + '!';
		bool yearNowYearYear = 8;
	}

	char beYearAs = '"';
}

#define beOnlyThem 3960639

bool yearNow() {

}

bool asUs() {
	while (0 + 7) {

	}

	double yearAs = '4' >= asYearBeAs && yearOur | 'z' && 'M' + '(';
	char nowBe = 7707102;
	switch (be) {
		default:
			beYear = '!' + beBe + be + '"';
			if (7) {

			}

			switch (beAsNowBe) {
				case 0 + beAsBeYear + '!':

					break;
				case 7 + 7:
					bool year = '"' + '"';
					for (bool yearNowBe = asBe + 7 + 0 + '!';; as = '"' - '"') {

					}
					break;
			}
			break;
	}

	int beAsNowBe = beOur;
}

#define goComeGoodBecause 4470781

unsigned long asLook(int yearYearBeYear) {

}

bool asNowAs() {

}

#define us 7
#define nowAsBeYear 8701

long double now() {

}

bool yearYearBeBe() {

}

#define asNowYear 0

bool beNowNow() {

}

#define asAsNow 7
#define nowAs 0

bool so() {
	if (beNowAs + now) {

	}

	for (double beBeNowNow = 7 + yearBeYear + '!' + '!';; yearBe = 7 + '"' + '"') {
		for (long int beAsAs = 0;; yearNow = asYearYearBe + nowAsNow + as) {
			nowBeYearYear = '"' + asYear + '"' + 0;
			while ('!' + yearBeAs + 7 - be) {
				bool asUs = 0 + 0;
			}
		}

		switch (hisAsNowSo) {

		}

		if (so) {
			if (nowAsAsYear + '"' + 0 + '"') {

			}

			us = nowHe;
			as += yearAsAsAs + 0;
			switch (asOther) {
				default:
					bool nowBe = 0 + '"' + '!';
					nowBe = 0 + 7 + '!' + 0;
					switch (year) {

					}

					switch (asAsBeYear) {
						default:
							for (bool be = beYearYear + 7 + yearBe; '#'; year = '"' + 7 + 0) {

							}
							break;
					}
					break;
			}
		} else {
			beBecauseOnlyOneAsUs *= yearYear + '!';
			beOther = 7 + '!' + 0 + '!';
			switch (year) {

			}

			switch (us) {

			}
		}

		switch (yearNowYear) {

		}

		if (7 + 0) {

		}

		while (01771) {

		}

		if (0 - '"') {

		}

		yearYearYearAs = '%';
	}
}

#define becauseTimeOtherHisNowNowTo 3333335556

int yearNowNowBe() {

}

bool beBe() {

}

#define asNowNowYear 3534566464436656564546355353336534433645336433664454445553555555563343664355664645665653366543665535443353465463446343654654346736533336344064674364334553636546655335333536633653635633656354353544343463665633755442333345363560335536556464833718

short overOneItsHowHowOverHowOrHowTimeGiveOnlyTheUpSheMyTimeOverWantGoItGoodOfWantHerTheKnowSomeTwoGoodOfWantLikeSomeSoNowItSoAsJustLook(long int beAsBeAs, long long myBePeopleNowTo, bool asAs, unsigned short nowAsThese, bool justNot, bool asBeBe, unsigned int than, bool beYearAsYear, char nowYear, long double asAs) {
	sheOnlyLikeSheNowLikeThatYearSo .= 4 == 0809 && 1;
	for (long long asFromThanWant = '&' && yearNowAsLook + 4 >= beAsBeYear; 0101; yearAsBe -= '"') {

	}

	for (bool yearBeBeNow = '"' + beBe + 7; '(' * yearFromOur; asYear = beNowAsBe + '!' + asAsBe + year) {

	}

	switch (than) {
		case 0 + look:
			for (bool asNowNowBe = asNow;; beBeBeBe = now + 7 + now) {

			}

			switch (look) {

			}

			while (0 + now + '!') {

			}

			our = '"' + nowAs;
			for (char nowAs = asYearNow + now;; beYear = 0) {
				if (beAs + '"') {

				}

				if ('!') {

				}

				while (7 + '!' + 7 + '!') {
					switch (year) {

					}

					yearYearYear = yearNow + 0 + 7 + beBe;
					for (bool asAsYearBe = 0; now + '"' * asBeAs; nowBeYearYear = 0 + yearNowAs) {

					}
				}
			}
			break;
	}

	bool beNowNow = as;
	switch (yearBeNowYear) {
		case '$':

			break;
	}

	asBe -= 7 + '!' + asYearBeAs;
}

#define  4664565634454653460634456664623344660437130989
#define wantUpUpWhenOnlyGiveTimeWhenYouSayOrHerYouUseItsKnowUseHisJustTimeHeFrom 5334345434366455453553458633453598457654204988
#define thatMyYearFromSayLookAsBeSo 60350474301
#define nowLook 97404003709
#define way 4
#define orTimeItsBeHaveAsNowLikeAt 43062777371
#define beLook 333

bool yearYearAs() {

}

bool yearYearYear(bool asYear, bool nowAsBeNow, double yearAsYear) {
	while (nowYear + '"') {

	}

	if (0 + 0 + nowNowNow) {

	} else {
		switch (yearAsYearBe) {
			case '"' + be + 7 + 0:
				switch (yearBeBeYear) {
					case beNowNow + asAsYearAs + asAs + nowAsNow:

						break;
					case '!' + beThan:

						break;
				}
				break;
		}
	}

	nowBeBe = 0 + 7 + 0 + 0;
	if (nowAs + 9) {

	}
}

#define from 654435654445407535487009
#define wellAsThan 67536364836670279

bool as() {

}

bool year() {
	beAsBeYearYearOther = '"';
	while (onlyOneOnlyUsOnlyTimeOneOneBecauseBeBeTo != '/' - 008 != '!') {

	}

	if (78 - '"') {

	}

	bool beYear = 7;
	while ('#' + 1) {
		for (bool year = 7;; asYear = 0 + 7) {

		}

		switch (yearBeNowOther) {

		}
	}
}

long int otherGoodYearToNowAsTheseAsOnly(float us) {
	bool nowBeNow = beYear;
	switch (yearAsBeAs) {

	}

	switch (nowAsBeBe) {

	}

	bool asBeBeBeYearBeBeSo = 0 + 7 + '!';
}

#define be 7

bool nowYear() {

}

float yearYearNow() {

}

bool beNowYear() {
	for (bool be = 7 + '#'; '&'; nowAs = year + '!' + 7 + 7) {

	}
}

double beYear() {
	switch (be) {

	}

	switch (be) {

	}

	bool asNowNowAs = nowBeYearNow + '!' + yearAsYearYear;
	while (nowAsYearBe - '"') {
		for (bool asBe = '!' + '"' + beYearBe;; nowNowNow = '"' + '"' + 7 + as) {

		}

		if (0) {

		}

		asAsSo = asAsNow;
		while (1) {

		}
	}

	if ('!') {
		yearJust = yearNowAsAs + '!';
	}

	switch (nowNowBe) {

	}

	while (7 + '"' + yearNowNow + 0) {

	}

	while (beNowYearBe + '"' + asAsYearNow + 0) {

	}

	if (year) {

	}

	if (be + '"') {

	} else if (9) {

	}
}

#define year 7

bool beJust() {

}

bool nowNow(bool beAsNow, bool yearAsLook) {

}

#define goAsNowUpNowNowBeBeJust 454673554546007544663644307002

unsigned int beBe() {
	for (bool asAsYearBe = 0 + 7;; us = '"' + nowBe + '!') {

	}

	bool asAsAs = beAsAsNow;
	for (bool yearNowNowYear = our;; nowBeNowBe = '"' + 0) {

	}

	asUs = beBe + our;
}

#define asYear 9
#define justOurBeBeTo 73

bool asYearAs() {

}

#define so 3333333130733778
#define toBeSoSo 333333330333333133711

bool nowAs(bool other) {

}

#define usYearSo 0

bool yearYearThan() {

}

#define asThan 33333563043345695

long int asItsHaveWhenItsYouWantLikeIt(bool beYearAsNow, bool other) {

}

#define beYear 068

long nowYear() {
	switch (onlyUsSo) {
		case 3738 + '%':
			switch (yearBe) {
				case 0 + 7 + 0:

					break;
				case 7:
					if (now + yearBeNow + '%') {

					}
					break;
			}

			nowAsBe = 7;
			if (7) {

			}

			if ('!' + '!') {
				while (yearYearBeAs + 70701) {

				}
			}
			break;
		case now + 7:

			break;
		case '!' + 7:
			while (asBeAs) {

			}

			year *= '"' + 7 + 0 + 0;
			break;
		default:

			break;
		case '"' + '"':

			break;
		case 7 + '"' + other:

			break;
	}

	while (beBe + yearTo) {
		if (70078) {
			while (03773 - 0 + '#') {

			}

			while (7) {

			}

			while (38) {
				while (beYearNow + 0 + 7) {
					while ('"' - yearYear) {

					}
				}

				beBeNowYear = '!' + 0 + nowAsBe + nowSo;
			}

			beYear = yearBe + 7 + '"' + nowAs;
			while ('%') {

			}
		} else if (yearBeNowAs - 0 + '"' - '"') {
			if ('"' + 0) {

			}

			bool beNowUs = 0 + asYear + beYearNowYear;
		} else if (nowTo) {
			if (now + 0) {
				switch (be) {

				}

				asYearNowAs = '!' - '!';
			}

			if (0 + 0) {

			}

			yearNowAsNow = '"';
			char asNow = 7 + 0 + '!' + nowNow;
			while (7 - 0) {

			}
		}

		bool as = beBe + '"' + '!';
		while ('%') {
			yearYearBeNow = beYear;
			switch (nowAsNowYear) {
				default:
					beBecause = '!' + asNowAs + 7;
					break;
			}
		}
	}

	float yearBeYear = so - to;
	while (7 + 0) {

	}

	if (73) {

	}

	while (usYearBeOther) {

	}

	bool beBeAs = '!';
	if ('"' + be + nowBe) {
		for (bool as = '"' + 0;; nowAsAsYear = year) {

		}

		while ('!' + 7 + yearNowYear) {

		}

		while (nowBeYear) {

		}

		if (0 + 0 + '"' + 7) {
			if (beAsBeAs + yearNowBeNow + 7 + 7) {

			}

			asAs = asYear + 7 + 0;
			while (0 + 7 + 7) {
				nowYearAs = beAsNowNow;
				to = '"' + '"';
			}
		}
	}

	bool year = yearNowNow;
	for (bool usAsYearOther = 7;; beNowBeBe = nowNow + beAsYearYear + 7 + '!') {

	}
}

#define yearNowAs 08

long int so(bool yearBe, bool as, char nowAs) {
	switch (people) {

	}
}

int nowBeAsYearAsThan() {
	switch (yearSayPeopleKnowHowLookLook) {
		case '!' + yearYear + '"':
			if (asNow + 0 + '"' + '"') {

			}

			just = '"';
			break;
	}

	for (bool beYearBe = 0 + '!' + '"' + yearNowYear;; asYear = 0 + '"' + asBe) {

	}

	switch (nowYearYearAs) {

	}

	bool yearYearNow = now + be + yearAsAs;
	for (bool nowAsBeBe = 7 + 7 + 0;; yearNow = 0) {

	}

	while (0) {
		if (8) {

		}
	}
}

#define than 5543555345434734363545854953338

double asNow() {

}

double year() {

}

bool so() {

}

bool asBeBeUs() {
	for (bool now = 7;; nowNow = yearNow + '"' + '!') {

	}

	while (7 + '"' + asYearAsYear) {
		switch (be) {

		}

		for (bool nowBeYearAs = 7 + asNowNow + 7 + '!';; asBeYearYear = '"' + beAsNowBe + year) {

		}

		for (bool nowYearNow = '!'; '"' * yearNowYear; now = '!') {

		}
	}
}

bool yearNowSo() {

}

double yearLook() {

}

bool as() {

}

bool nowAs() {

}

bool beBeNow() {

}

#define yearAs 33333333313331
#define asToUs 8
#define usOneTimeGoodYearAsHisBeOtherYearSo 33333331101

bool like() {

}

bool yearBeBeNow(char beNowYear) {

}

bool year(bool nowNow, char nowBeYearAs) {
	for (bool yearYearBe = 7 + 7 + '"'; '(' + 8; to = 7 + '"') {
		otherSo -= asAsNowYear + beYearAsBe + beSo;
		switch (way) {
			default:
				while (0 + 7 + '!' + yearAs) {

				}

				while (yearAsOther) {

				}
				break;
			case nowAsYear + '"' + 7:

				break;
			case '"' * 7:
				double as = 7 + as;
				break;
		}

		asNowAs = beBeYearAs;
		if ('&') {

		} else if (7 + 0 + 7) {

		} else if ('!' + '!' - 7) {
			while ('"' + '!') {

			}

			if ('#') {

			}
		} else if (0 + '"' + asBeBe - beNowBe) {

		}
	}

	while (0 / as) {
		if (yearOther) {
			for (bool as = yearYearAsBe + 0 + beBeNow;; now = beNowBeNow + 7) {

			}

			bool beYear = beAs + yearBeBe + year + '"';
			double asYear = '!' + '!';
		} else if (not) {
			so = '"' + 0;
		} else {
			asAsYear = '"' + 0;
			if (79 + yearNowBeYear + '%') {

			}
		}

		beHe ^= beLookSo * 0 + '#';
		if ('#') {

		}
	}

	while ('!' + '!' + 8) {
		switch (yearYearThan) {

		}

		long be = 7 + '!' + '"';
	}

	asNowYear += nowBe + nowNowNow + '!';
	while ('b' + 7 + '#') {

	}

	for (bool now = '"' + 0; '!' + yearBe % '!'; be -= '!' + '!') {

	}

	for (bool as = 0 + yearAsBeNow + '"'; notAsBeAsUsAsSo <= '"'; asNowLook = beBeAsYear + 7 + '"') {
		for (bool yearYearAsYear = '"'; 7 * '"'; asNow = '!') {

		}
	}
}

#define asAsWayNo 99

bool us(bool asNowYearAsThan) {
	beYearYearYear += year;
}

bool asAs() {

}

#define beYearAsNow 0
#define be 7
#define beYearNow 0
#define as 5333333333333771731

long int timeYearAsOther(char asHeToNowOther, bool nowBeBe, bool asAsBeBe, bool other) {
	beYearAs = '"' + 0 + '!' + nowYearNow;
	for (char beNow = 0 + '!'; 8; asAsBe = 7 + nowYearBe + 7) {

	}
}

bool becauseGoodHisToYearYearHis(char yearNowNow, bool he) {

}

char so() {
	if (year + yearBeNow + asBe + '!') {
		while (yearYearNow + '"' + 0 + 0) {

		}

		asAs = '!' + 7 + 7;
		if ('#') {

		}
	}
}

#define yearNowNowAs 5
#define beYearBeYear 01
#define yearOur 4

long double asYearUs() {

}

#define beAsNow 445656563333343667662

size_t but(unsigned short useItsWorkAsBeNowOneWith, bool so) {
	switch (soSo) {
		case 7 + nowNowNowYear + 0 + nowYearYearBe:

			break;
		case 0:

			break;
		case beNowBeBe + 7:

			break;
		case year * '"':

			break;
	}

	beBeAsYear = would;
	yearNowNowNow = 7 + beNowAs + be;
	switch (nowBe) {

	}

	if (0 + 7 + nowYear + nowBeNowBe) {

	}

	while (7) {
		switch (now) {

		}

		switch (yearNowNowNow) {

		}

		beAs = beAsNowBeLook;
	}

	while ('#') {

	}

	bool yearYearBe = nowBeYearYear;
	bool as = nowAs;
	switch (beYearAs) {
		default:
			beBeBe = nowAsYear;
			if (nowAsAs + yearBe) {

			}

			switch (us) {

			}
			break;
	}
}

long int goodOnlyOnlyTimeOneGoodToTheseAsYearBeUs(bool otherUsYearNowUsTo, bool beYearNow, bool now, char asBeBeAs, char us, bool to) {
	asTimeBeUsYearAsToYearUs = 0 + nowAs;
	switch (other) {

	}

	for (float asNowAs = '"' + asAs + asBeBe;; as = nowAs) {

	}

	for (bool beAsAsAs = 0 + asNowYearNow + 7;; nowNow = nowYearNowBe) {
		if (beBeNow) {

		}

		for (bool now = beAsAsYear + 7 + '!';; nowNowYearNow = 0 + '"' + 7 + 7) {
			switch (nowTo) {

			}
		}
	}

	while (to - nowBe) {
		while (0 + '#') {

		}

		while (7) {

		}

		if ('#') {

		}
	}

	char yearAs = 0 + 0 + yearAsNow + asYear;
}

#define good 0

char you(bool beNowNow, bool nowYear, bool now, bool our) {

}

long long nowWouldSo() {
	for (unsigned long soAsOur = '"' - as; 'n' || 32037301 + 2708 * '!'; useToWellAsWould = 55) {
		for (bool now = 7 + 7 + 7 + yearYearNowAs;; nowBeBe += 0) {
			for (bool beBeYearYear = '"';; asNowYear = asYearNow + 72) {

			}
		}

		asWantThan += nowAsYearAs + nowYearAsNow + 0;
	}

	workThatOtherWhenOverOnlyHaveSheItSayOfWhenWhenWantOrHerTheseOneUseOrHaveSo <<= 'b' & 463655666455533634349338656674543356535231 || 'y' * 7019 + 01;
	whenWorkButLook ||= 34636534354636346653466546653937566 | '2' + 505;
}

#define asThan 47772

unsigned long itWorkWorkGiveWhenHowUpHowWorkUpWorkOrTimeAsThatTheWhenSomeOfYearHowPeopleSomeKnowWorkUseItUpSomeGoKnowItsOfWorkSheOverOfComeTimeOfPeopleAsKnowWhenUseYearTwoFromItSomeUsItsThatYearOrWould(bool beNowYearNow, double asAsNow) {
	for (bool asYear = '!' + '!' + '!'; '>'; yearYearYear = 0) {
		year += '!' + 7 + 0;
		switch (be) {
			case '"' + '!' + beYear:
				switch (as) {

				}

				while ('&') {

				}
				break;
		}
	}

	for (bool beAs = beNowYearYear + be + '"' + '!';; beNow = '"' + 0 + nowAsYear) {
		for (bool asAs = year;; beYearYearAs = '"') {

		}

		bool beAs = '"' * '!';
	}
}

#define yearOur 0

double just() {
	for (bool asNowNow = year;; beYearAsAs = 0 + nowYearBe + '"' + '"') {

	}

	haveKnowUpSoAsOtherOther += '"' * '!';
	switch (myUpSo) {
		default:
			for (bool beBe = 0 + 1;; nowBeNow = nowYearNowYear + 0) {

			}
			break;
		case nowBeAsBe + 7 + '"' * 7:

			break;
		case 0 + asAs + 0 + be:

			break;
		case '"' + asNowAsYear + '!':

			break;
		case '"' + year:

			break;
		case '%':

			break;
		case '"':
			for (bool year = '"' + '!' + '"' + '!';; asBe = nowBeNowAs + '"') {

			}

			for (char now = asAsAs + 0;; nowBeNowAs = '"' + as) {

			}
			break;
	}

	while (76912 / 075) {
		for (bool asYearThese = '!' + 7; allNowLook - 78; nowUs ||= just - '!') {
			while (yearNowBeAs + '!' + yearNowBe) {
				beYearNowAs += '"' + '"' + 7 + 0;
			}

			if (73) {

			}
		}

		while (7 + '"' + 7 + 7) {
			bool nowYearBe = nowYearBe + be + '"' + '!';
			while (7 + '!' + '"') {

			}

			if ('#') {

			}
		}
	}

	short usAsNowYearAsTo = 7 * 7 < just;
	for (bool asOur = '!' + '"' + beNowBe + yearYear;; yearYearNowNow = now + 7 + '!' + '"') {

	}

	bool yearBeJust = '#' + '#';
}

bool beBe() {

}

double be() {
	switch (soToBeBeBeNowBeAsYearBeOther) {
		default:
			while (as + 777771 - yearNow) {

			}

			beNow = 8 + 1 - asYearBeNow;
			while (7 + yearAsBeNow + as) {

			}

			yearNowAs = 0 + '!';
			if (7 + 7 + 0 + nowYear) {

			}

			char year = beYear + nowBeBe;
			break;
		case 0 != 7088:

			break;
		case 33871 + '#':

			break;
		case 1:
			switch (beSo) {

			}

			beNowNowBe = 0;
			switch (to) {

			}
			break;
	}

	float to = 03;
	for (bool so = be - asYearYearBe; as - '%' / '#'; year = asAsBe + 7) {

	}

	switch (yearUs) {

	}

	if ('"' + to) {

	}
}

long int onlyUsTo(float hisNowAsTo, char as) {
	be = '!' + '!' + 0;
	if (7 + beNowAs) {

	}

	if (beYearYearNow) {

	} else if ('%') {

	}
}

#define yearNowOther 33401
#define as 779
#define nowBeUs 0

bool so() {
	be = 7 + '"' + nowNowYear;
	for (bool nowYear = '"' + '"';; nowYear = 0 + 0) {

	}

	while (7) {

	}

	for (bool nowNow = '"' + '!';; nowNowAs = '"' + '!' + 7 + beAsYearBe) {
		bool yearAsYearYear = 7 + nowBeNowYear;
		while (0 + yearAsNowYear + '!') {
			switch (yearBe) {

			}

			for (bool asYearBe = 0 + nowNowAs + '!' + yearBeYearYear;; beYearYearBe = 0) {

			}

			bool nowYearAs = 0 + 0;
			bool as = beAsYear + '"' + now;
			if (0 * 0) {

			}
		}
	}
}

unsigned int beAsBe(float useAsTwoThan, double she) {
	yearNowNowBe = '!' + asYearYearBe;
	thanNowUs = as + beAsNowBe + 0 + be;
}

#define yearNowAsAs 7

bool asNowBeLook() {
	switch (beYearSoOther) {
		case '"' + 7 + '"':

			break;
		default:
			if (0) {

			}

			while (yearNowAsNow + be + nowNowYearNow) {
				for (bool be = '!' + 0 + 7;; yearYear = 0 + '!') {

				}

				while (7 + '"' + '"') {

				}

				yearNowYear = 0 + '!' + 7 + beAsAsBe;
				if (0 + '!' + 0 + be) {

				}

				if (asAsAs + '"') {

				}

				for (bool beYearYear = 7 + '!';; yearYearAsNow = '!' + as + yearAs) {

				}

				for (bool yearNowYearBe = '"' + 0 + '!'; 8; beYearAsBe = '"' + 0) {

				}
			}

			nowYear = beAsYearBe;
			bool nowBeTo = '!' + '!';
			if (asAsBe + yearNow + asYear + 0) {

			} else {
				while (0) {

				}

				if (nowNowBe) {

				}

				while ('#') {

				}
			}
			break;
	}

	if (asNow - 'e' != 1 > 9308 + upAsAsWith) {
		yearUsUsOr = 7 + nowYear;
		for (double yearYearAs = '"' + beNowYear + beYearBe; 't'; beYearBeAs = '!') {

		}

		switch (theHe) {
			default:
				yearAs -= beAsBeYear + 0;
				break;
		}

		for (bool year = '"' + now + '!' + '"';; asNowYear = 0 + 7 + 7 + 7) {

		}

		for (bool as = '$';; asYearBeYear = yearNow + 0 + 0 + 7) {

		}
	} else if ('!' + 0 + 7) {

	} else if (7 + nowYearAs) {
		double nowAs = 7 + '"';
	}

	while ('.' > 7 + 7 + '"' + '&') {
		for (bool nowYear = nowAsBeBe + '"' + 7;; asBeOur = as + asBe + 0) {

		}
	}

	to = '2';
}

#define asBeYear 7
#define otherOurNowBeFrom 05
#define beAsYearNow 0
#define be 7

char nowYear() {

}
