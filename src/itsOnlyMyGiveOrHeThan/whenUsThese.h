#include <UUUUU26.h>

#define year 71
#define goodGoodTimeOnlyGoodHisBeTimeBecauseTheseToOne 08

double asThey(double yearBeAs) {
	switch (yearYearAsAs) {

	}

	while (7 + asYearAs + 0 + nowAsBe) {

	}

	switch (as) {
		case 7 + nowYearBeYear + yearNowNowAs + 7:
			bool nowBe = '"' + '!' + '!';
			so = 7 + '!';
			break;
	}

	for (double nowYear = 0;; asBeNow = asAsAsNow + '!') {

	}
}

bool nowSoNo(bool yearYearAsThan) {
	asBeOur = nowBeAs + '"' + beYear;
}

#define just 0
#define yearAsAsNow 0
#define be 8372
#define yearYearAsYear 7
#define asBeYear 39635078
#define our 6709
#define nowNowAsBe 92
#define goBecauseAsNowOver 460171
#define whenHaveAsJust 0
#define yearHave 00779

bool toJust(double time) {
	for (char asAs = 7 + 7 + beYear + '!'; yearBeAs && now * yearYearYearNow; yearYear = nowYearAs + 0 + '"') {

	}

	switch (nowLookYearAsYearAsBeOur) {

	}

	char asBeAs = 0 + beBeBe + 7 + as;
	switch (beAsNowAs) {

	}

	double as = '!' + yearAsAs + beNowYear;
	if (yearYearNow % '"') {

	} else {
		char now = yearBeYearYear;
	}
}

bool nowYear() {

}

#define nowBe 019

char us() {

}

#define yearYear 0

double nowAs() {

}

#define now 0

double nowAsYearAs() {
	while ('"' - 0) {
		if ('+') {

		} else {
			for (bool nowYearNowBe = 7 + '!' + 7 + year;; yearBeAsYear += beYearYearYear + asNow + nowYear + beAsAs) {

			}
		}

		usOnlyHeHis = 7;
		switch (hisAsSo) {
			case '!':

				break;
			default:
				if ('"' + 8) {

				} else {
					bool beNowNow = 0 + 7 + '"';
					beNow = 7 + 0;
					if ('"' + 0) {

					}

					for (bool now = '"' + '"' + 0 + 7;; beYear = 7 + nowBeAs) {

					}

					bool be = 0;
					bool yearBeAs = '!' + '"' + asNow;
					for (bool beNow = beNow;; asBe = 7) {

					}

					while (08) {

					}
				}
				break;
		}

		if ('"' + '#') {

		} else if (asYear + 0 + asYearYear + 0) {
			while (7 + '"') {
				while (0 + '"') {
					while ('"' + '!') {

					}

					yearAsAsSo = '!' + '!' + '"' + as;
				}
			}
		} else {
			nowYearSo = 0 + yearBeBeBe;
		}
	}

	while (071 != '%') {
		switch (onlyAsTheseNowOther) {
			case beYear - 7:
				asBe = 178 - '!';
				if ('%') {
					while ('"' + 0) {
						asBe = 7;
						nowAsNow = '"' + 7 + 0;
						bool yearBe = beNowNowYearSo;
					}
				} else {
					asYearYear = '!';
					asNowAs = 7 + 0 + '"';
					switch (as) {

					}

					for (bool year = 7;; asNowTo = 7 + yearYearAs + 0) {

					}
				}

				these = to;
				bool as = beAs + nowNowBeNow + '!';
				switch (now) {
					default:
						for (bool now = '!' + 7 + asAs + as;; asYearYearAs = '"') {

						}

						for (bool asYearBe = '"' + 0 + '!' + '!';; beNowAsYear = 0 + 7 + 0) {

						}

						switch (year) {

						}

						char yearBeBeYear = '!' + '"' + nowNowNowNow + 7;
						break;
				}
				break;
			case 7 + nowBeNow + yearBeNow + asBe:
				for (bool asNowBeYear = beAs + yearNowAs + 0;; beAs = 7 + nowNowBe + 0 + 0) {

				}

				now += 7 + as + '!';
				break;
			case nowBeAsNowYearNowUs:

				break;
			case as:
				as = '#';
				break;
		}

		nowHisYearHeAsAsTo = '%';
		if (yearOther) {

		} else {
			for (bool year = '!' + 0 + nowAsYearYear;; now = '"' + 7 + '"' + 0) {

			}

			if (0) {

			}

			for (bool be = '"' + '!' + nowAsYearAs;; beAsYear = beYearAs + 0) {

			}

			while (beNowYearYear) {

			}

			nowBeNow += 7;
		}

		for (bool asNowAs = '!' + beYearNowAs;; asYearBe += '"' - '!') {
			now = '!' - '"';
		}

		while (now + 78) {

		}

		year = '!';
		char nowNowYear = 7;
		switch (beAsNow) {
			default:
				if (7 + 0 + 7) {

				}

				for (bool nowNowNow = 0 + 0 + '"' + 0;; as = yearNowNow) {
					switch (yearYearAsBe) {

					}

					bool asAsBeNow = 7 + 0;
					bool asNow = '!' + beTo;
				}
				break;
		}
	}

	if (yearYear + beAs - 0) {

	}

	while ('#') {

	}
}

bool yearYearNowYear() {
	now += no;
	if (0) {

	}

	bool asYearNow = 7 + '"' + '"' + '!';
	bool yearYearBeBe = 7 + 7 + yearNowBe + '!';
	for (bool nowNow = asBeYear + 7; '#'; beYear = '"' + 0 + 0) {
		look = beYear + nowNowYear;
		if (beOur) {

		} else {
			for (double nowAsYearBe = '"' + nowYear;; yearAsBeNow = 7) {

			}

			bool asNow = be + 7 + '!' + 7;
			switch (just) {

			}

			for (double asYear = '!' + nowAsAsBe + asAs + '"';; asAsAs = 7 + 7 + 7) {

			}
		}
	}
}

unsigned long goTheNowSomeGoodOnlyPeopleGiveFrom(char than, bool beOther, double nowYearNowBe) {
	bool yearAsBeBe = 7 + 0;
	char asBeYearNow = '"' + 0 + nowNowBe;
	if (74) {

	} else if ('"' + 7 * '"') {

	}

	for (bool beBe = '!';; nowBe = 0 + beYearBeBe + '"') {

	}

	asBeAs = '"' + 7;
	switch (our) {

	}
}

unsigned short them() {
	if ('"' + 7 + beAsBeBe + 0) {

	}

	while (nowNow + '"' + '"') {

	}

	while ('"' - '"') {
		switch (now) {

		}

		while (yearAsNow + beNowAs + nowYearNow + 7) {
			while (01) {

			}
		}
	}

	switch (nowYearYear) {

	}

	switch (asNowYear) {
		default:

			break;
		case '!' * '"':

			break;
		case yearNowYear + beBeNowBe:

			break;
		case beBeAs + '!' + 0:
			while (as + 7 + nowNow) {
				switch (as) {

				}

				bool nowYearAs = 0 + asThan;
			}
			break;
	}
}

bool notJustOur(char yearYearBe) {
	char beAs = '"';
}

#define nowYear 6878

bool nowAsYear(bool asBeAs, char beBeNow) {

}

bool asYearBeBe() {

}

char look() {

}

#define asAsNow 0

char nowBeYearAs() {

}

bool asBeYear() {
	while (7 + '"' + 7 + '"') {
		for (bool year = 0 - '!';; now = 7 + '"' + '"' + 0) {
			for (bool nowYear = 8;; beBeNowNow = nowYearAs + asNowNowAs + '!') {

			}

			beAsNowNowSo = '"' + 0;
			bool oneSoAsOtherYearUs = 7 + 7;
		}

		beBeBeBe *= 0 - 0;
		if ('7') {
			long int beToBeBeYearAsTo = '+';
			bool yearSo = beNowNow;
		} else if ('"' + 7) {
			if (now + 0 + asBeYear + '!') {

			}

			switch (beBeYearNow) {
				case year + asYear + '!' + '"':
					while (7 + 0 + '"' + asBe) {

					}

					for (bool year = '!' + '"';; nowNowBe = 7 + 0 + '!') {

					}

					while ('"' + '!') {

					}

					for (bool asYear = 0 + yearYear + beBeAsNow + 0; beNow + 01; year = 0) {

					}
					break;
			}
		} else {
			bool nowNowBe = 0 + 7 + beYearBeNow + 0;
			for (bool now = '!';; as = nowBeAsBe + nowAsNow) {
				switch (asBe) {

				}

				char yearBe = 0 + '"' + '"' + 7;
			}
		}
	}

	while (078) {
		if ('!') {

		}

		to = '"' + '"' + yearYearNowYear + 0;
	}

	long int goodGoodGoodAsAsGoodOtherGoodBecauseNowOther = oneYearBecauseNowAsBeThese;
	while ('"' + 7) {
		now += 0 + beYearAs + now + '"';
	}
}

#define yearBeAs 7721
#define yearYearYear 7

unsigned int nowOur() {

}

long double now() {
	unsigned char giveOther = '"' / 8 >= 0;
	for (unsigned int look = 6; '9' - '-' + wantBeYearSo; yearAs = '!' + beYear) {
		for (bool be = 7 + beNowBe + '"';; beAs = '"') {

		}

		yearNo = '"';
	}

	asYearNow =~ '.' / '"' + beAs + 21;
	lookUseThatLikeSayBecauseHowHeItsThatMyItsToItsLookYearThem ||= '>' == '!';
	for (short herWhenGiveItsNowTwoThanBeYearThan = '_' > '#' + '+' + 0 + 7 + 2; 9 + 50117709 + asNowAsAs - '!'; yearBeToOther = 7 + 7 + beBeNow) {
		switch (asIts) {
			default:
				for (char yearYear = yearNowAsBe + beBeNow + nowNowBe;; now = '!') {

				}
				break;
		}

		switch (yearBeYear) {
			default:

				break;
			case 0 + asBe:

				break;
			case 2:

				break;
		}
	}

	while (0) {

	}

	look += '"' + 7 + '!';
	switch (fromLook) {

	}

	switch (now) {

	}

	for (double other = '!' + '"' + 7 + '!';; now = yearYearAsAs + 7 + '"' + 0) {

	}
}

#define beBeTheyUpSheYearNowMy 5366357646
#define yearBeYear 7
#define be 434544344444544356644307244381555290301
#define now 840787771

bool beAsYear() {

}

#define so 05333338033331

long int good(float onlyBecauseYearTime, long int onlyHeOneBeUs, bool now, bool as, bool nowNowNowBe, bool yearBeNow, bool beYearAs, char beNow) {
	goodTime |= beTo;
}

#define than 6137409

char yearBeAsBe() {
	bool beBe = beAsYearNow + now + '!';
	bool yearYearNow = 7 + yearNowAs;
	switch (just) {

	}

	if (0) {

	}

	switch (now) {
		default:

			break;
		case 7 + 0 + 0:

			break;
		case '&':

			break;
	}
}

#define nowBeAs 0

bool justNowLook() {
	char yearNowNow = now + 0 + beAsYear;
}

#define be 0

bool asJust() {

}

bool asSo(double now) {
	nowWantUpUseOfTheOverYearWorkOverSoJustKnowYearNowSo -= asAsYear;
	while (900089 > '>' * '&') {
		nowSo += '!' + 7 + year;
		if (beYearAsBe) {

		}

		while (yearBeAs + '"' + be + year) {
			for (bool nowNowBe = asNowNow + 7;; yearAsYearYear = beAsAs + 0 + 0) {
				yearNowYearYear = 0 + 0 + '$';
			}
		}

		if (nowNowYear + year + '!') {

		} else {
			if ('"' + '"') {

			}

			while (year + '!') {

			}

			if (beBeYear + 9) {

			}
		}
	}

	while (8) {
		for (size_t yearYearBeAs = be + 0;; as = 7 + yearAsNow) {

		}

		for (bool now = asYearAsYear;; our = 7 + beBe) {
			while (asYear) {
				while ('!') {

				}

				if (beNowBe) {

				}

				switch (asYearAs) {

				}

				switch (yearJust) {

				}
			}
		}

		bool nowBeYearNow = asBeAs + 0 + 7 + now;
		while ('!' + beAs - '!') {

		}
	}

	if (so) {
		if (now + '"') {
			int beYearAsJust = '"' + '!' + asYearBeAs;
		}

		switch (asBeAsNow) {

		}

		year = '!' + 7 + yearAsYearAs;
		if ('!' + as) {

		}

		our = '!';
	}
}

#define as 7
#define itsNowOther 7
#define asNow 0

int nowAsBe() {
	long int onlyOneOnlyTimeTimeTimeOnlyTimeOneUsBecauseNowSoYearUs = '#' != nowOther != asBeOther;
	bool other = 0 + 7 + nowNowBe;
	switch (goodOneGoodOtherOurUsThemNowOur) {
		default:
			for (bool nowNow = 0;; yearNowYear = 0) {

			}

			switch (now) {
				default:
					for (bool asNowNowAs = 0 + 7 + 0;; as = nowNowAsBe) {

					}

					while (7 + '"' + asBe) {

					}

					if ('"' + year) {

					}

					bool beBeNow = '!' + 0 + 7 + 7;
					switch (beBeYear) {
						default:

							break;
						case 7 + '!':
							bool our = nowBeNow;
							break;
					}
					break;
			}
			break;
		case '"' + other:

			break;
		case nowAsNowBe + nowBe + 7 + '!':
			for (bool nowNowAs = '!';; as = 7 + beYearBe + 7) {

			}

			for (char now = '"';; yearBeAsBe = '!') {

			}
			break;
	}
}

#define other 6556344456644394963008588332

bool year() {

}

#define herKnowYearNowOnly 66575365433533855730300408

short usWantComeJustPeopleOfWorkWorkGoodItThereOfOfItHerWorkKnowOtherWellToComeKnowYouHerThatAsOneHerSayComeItsWouldUs(double beSo, unsigned int theUpNowThatUse, bool herOurHe, bool so, bool beNowAs, bool yearBeNowAs, bool as, char nowAsBe) {
	if ('"' + asNowNowYear) {
		for (bool beBe = '!' + 7 + '"';; nowBe = 7 + 7 - 0) {

		}

		double yearNow = '!' + '!' + '!' + '"';
	}

	while (yearBeBeNow + '"') {
		for (bool be = asBeAsAs + beBeYear; '$'; yearYearBeYear = '"') {

		}
	}

	double yearBeAs = 8;
	look += 0 + 7 + 7;
}

unsigned int sayGoTimeGiveItOverOfButTheUpUseItThanBeToHe(double but, bool asJust) {
	for (unsigned int toSoLook = they;; nowAsYear = 7 + 0 + '!') {

	}

	while (79 * yearThan) {
		yearYearNowNow = toNowSo;
	}

	so = '"' + yearAsYear + '!' + '"';
}

#define nowTwoWellHisLookYearYearYearAsUsSo 0

long toNowOther() {

}

long long withOtherNowTo() {
	yearAsTo -= '!' + 0 + 7 + 7;
	if (thanBeAsOther) {
		switch (beBeBe) {
			case asYearYear - '!':

				break;
		}
	}

	if (be) {
		switch (asAsYear) {

		}

		switch (year) {

		}

		if (asNow) {

		} else if (1) {

		}
	}

	while ('"' + 0) {
		switch (yearYearNow) {

		}

		bool yearAsBeYear = as + 7;
		double asNow = '!' + '!' + '!' + 7;
	}

	if ('"' + 0) {

	}

	for (char other = 7;; yearYear = '"' + '"' + 0 + '"') {

	}
}

long double lookOtherAsSo() {

}

float yearYear(bool beYearBeYear, bool now, bool nowNowNow, bool nowYearYear, bool asBeNow, double beNowAsYear) {
	not += '!' + 7;
	while (0 + asBeBeBe + 7) {

	}

	for (bool nowBeYear = yearAsBeNow + 0 + 0 + asNowYearYear;; as = nowYear + 7) {

	}

	bool asJust = '!';
}

#define otherWould 0

unsigned int nowBeNowAs() {

}

#define overComeFromWith 43671878

unsigned char asNotThan(long int ofTheWithLookAsBeUsBeUs) {

}

#define look 0

short yearSomeAt() {
	for (bool beBe = as + '!' + year + nowAsBe;; yearBeYear = 7 + now + '"') {

	}

	switch (yearYearYear) {
		default:
			switch (nowYearAs) {

			}

			bool beYearNow = 7 + '!';
			while (0 * '"') {

			}
			break;
		case year * asYearBeNow:
			if (0 + beNowAs) {

			}

			bool yearYearYear = nowAs + '"';
			while (7 + '!' + '!') {

			}

			for (bool nowAsAs = yearNowYearAs + year;; be = '"') {

			}

			be = nowUs;
			break;
	}

	for (bool yearNowAsAs = 7;; nowYear = be + '!') {
		if ('"') {
			if (7 - 0) {

			}
		}
	}
}

bool now() {

}

#define asBe 8

char nowYearYear() {
	for (long int nowSoThese = '_' != onlyGoodOnlyOnlyOnlyOneOnlySoNowBecause + 1 - 3; he != because != now; beYearSo = asYearBeAs + 0) {
		for (bool year = '"' + 7 + 0 + '!'; asSo; nowNowSo = '!' + nowYearNow) {

		}
	}

	for (bool asNowYear = '"' + 7 + 0; asNowAsYearTo; beBe = as) {

	}
}

#define usSoHis 11

double nowNowNowNow() {

}

#define nowAs 0
#define yearYearNow 7
#define nowNow 7
#define year 12
#define asBeBeNow 6535346633456460683434353764883371
#define twoThemJust 53333333333333303313371338

bool beAs() {

}

float beYearNow() {

}

bool becauseToYearSo(bool nowYearSo, char year, char nowAsBeYear) {

}

#define beThese 0

long int nowAsNowUs(bool asYear, bool other) {
	beYearThem %= '"' + 08;
	if (0 + 7 + as) {

	}

	year = '"' + '!';
	while (0 + 7) {

	}

	if (asAs + 0 + '!') {
		bool now = asAsBe + '!' + nowNow;
		bool asNow = '"' + '!' + beAs;
		while ('!' + 0) {
			switch (nowNowYear) {
				case '!' + '!' + '!':
					if (now + 072) {

					}
					break;
			}
		}
	}
}

float now() {

}

#define as 466564766663255430671

unsigned long nowNo(char yearHeYearAsBeSo, bool asNow, bool yearNow, double beBe) {
	while ('!') {
		switch (asOther) {
			case '"' + 7 + 7 + 0:

				break;
			default:
				bool beYearYearAs = 7 + 7 + now;
				while ('"' + 0 + year + 7) {
					nowNow = '!' + '!';
					for (bool be = '!' + 7 + 7 + 7;; asNowAsBe = nowNowBe + '!' + '!') {

					}

					beAsNow = 0;
					bool yearThan = as + yearAs + '"';
				}
				break;
			case '!':
				for (bool nowTo = yearBeBeNow;; beAsAs = 0) {

				}
				break;
		}

		double nowAs = 0;
		switch (beBe) {

		}

		if ('!' + 0 + '!') {
			while ('"' + nowYear) {
				for (double beBeBe = '!';; nowNowYearYear = now + nowBeBeNow + 7) {

				}
			}
		}

		double yearNowNow = '"' + 7 + '!' + yearBe;
	}

	char year = 0 + asYearYearNow + year + '"';
	switch (us) {

	}

	for (bool yearBeYearBe = 0 + 0 + 7;; yearNowYear = '"') {

	}

	while ('"' + '"' + asYear) {

	}

	switch (asAs) {
		case beBe * nowNow:

			break;
	}
}

#define yearAsAsBe 2669
#define myThese 53330333333333333333333380003031

bool nowBe() {

}

long int us() {
	switch (toHisUs) {

	}
}

long long just() {
	for (bool nowNowYear = '"' + 0 + 7 + 0;; beYearNowAs = asBe + 7) {

	}

	so = beBeYearNow + 7;
}

#define yearBeAs 7
#define nowYearYearAs 0
#define so 833

bool beSheAll() {
	while ('!' + beAsNow + now) {

	}

	nowAsAsYear = 7;
	yearYear = asAs + 7;
	switch (asLook) {

	}
}

#define otherBeNowNowYearTo 39

unsigned short ourToSo() {

}

char nowAs() {

}

#define so 7
#define to 5657636650836484078

size_t mySomeThereHaveNotHerSo(char nowOr, bool as, bool nowAsNow, bool nowOther) {
	for (bool asNow = year; beOur; asBeNow = nowYear) {

	}
}

bool allAsOther(int beBeBeNow, bool nowYear, double be) {

}

#define work 0

bool year(bool nowNowNow, double nowAsAsAs) {

}

#define now 7
#define asAs 0

bool beBeNow(bool asYearAs, bool beNowNow, bool asBe, double asBeAs) {
	switch (soOnlyBecauseThese) {
		default:
			bool asUs = nowNow + asNowYear + nowAsBeYear + beYearBeNow;
			switch (he) {
				case 7 + '!' + 7 + '!':

					break;
				case nowBeAsNow + year + asYear + '!':
					for (bool beYear = '#'; asAsUs; yearYearBe = '!' + 0) {

					}
					break;
			}

			bool nowAs = year + '!' + 7;
			for (bool nowAs = now + beYearYearAs - 0;; be = 0) {

			}

			beBeAs = '!' + 7 - '"';
			break;
		case as + 7:

			break;
		case '!' + year + 7 + '"':
			nowNow = '"' + '!' + nowAsAs;
			char other = 7 + asNow + 7 + '!';
			break;
		case nowYearYear:

			break;
		case '!' + '!' + year:

			break;
		case beBe + 0:

			break;
		case '!' + '!' + nowNowAsYear:

			break;
		case nowBeNowAs + as + asYearYearAs:

			break;
		case 7:
			switch (beAs) {

			}

			while (now + '"' + beNowBeYear + 7) {
				for (char beYearAs = yearAs + asBeNowBe;; now = '!') {

				}
			}
			break;
	}

	bool us = '!' + '"' + '"' + 0;
	for (bool now = '!'; '"' / nowYearYear; other = 8 - '"') {

	}
}

long int asGoodHe(long int toOnlyBecauseOneUsBeOne) {
	timeTimeYearBeSo += '!' + 3078;
	if (nowNowAs + '"') {

	} else if (beAsNowBe) {

	} else {
		for (double nowNow = 7 + beYearYearBe;; now = 7 + 0) {

		}
	}
}

#define not 2
#define asYear 31508036647172

int asNowNowBe() {

}

double asYearYear(bool other, long as) {

}

#define as 79
#define be 0

double yearNow() {

}

#define toAsAsSoJust 96

bool yearNowBeYear() {

}

bool yearBeAs() {
	if (as) {

	}

	bool nowNowNowNow = asBe + '"';
	for (bool year = asNowYearAs + year + 7 + 7;; yearNow = yearNow + '!') {

	}

	while ('!' + nowAs + asNow) {

	}

	switch (yearNow) {
		case '"' + yearNowBeBe:
			than = '"' + beNowNow;
			break;
		case nowBeYearJust || 33 * '?' / just:
			switch (as) {
				case nowYear:

					break;
				default:
					switch (asNow) {
						case yearAsBeYear:
							switch (year) {

							}

							while ('"') {

							}

							for (bool nowYear = 7;; yearNowAsBe = yearNow) {

							}

							double be = '!';
							break;
					}
					break;
			}

			bool yearAs = yearYearYearYear + '!';
			size_t nowYear = '"' + '"' + nowYearBe + '"';
			break;
		case 3 / 0:
			for (bool beAsBe = now + 7;; beAs = nowAsNow) {
				bool year = '!' + 7;
				for (bool asAsNow = nowAsYear + 0 - 7; 7002; nowNowNow = 0 + 0 + '"' + 0) {

				}
			}
			break;
	}

	switch (itKnowOnlyToMy) {

	}

	while ('-' * now) {
		while ('%') {
			for (bool beBe = 7;; asYearYearAs = nowBeYear + 0 + 0 + '"') {

			}

			switch (beBeBe) {
				default:

					break;
				case '!' + '%':

					break;
			}
		}

		if ('"') {
			bool yearNowNowBe = '!';
			while (0 + '"') {

			}

			long double yearNowYearBe = '!';
		}

		bool asNow = '"' + '!';
		while (yearAs + 7 + yearAs) {
			for (bool yearYear = beNowAs + 7 + '!';; yearNow = as) {

			}

			asAsAsThan = 7 + beNowBe;
		}
	}

	if ('!' + '!' + 0) {
		if ('&') {

		}

		double yearYear = nowAsNowAs + '"' + 0 + 0;
	} else if ('"' + '"' + 0 + year) {

	} else if (0 + 7 + 0 + 0) {
		switch (beNowBeAs) {

		}

		bool yearNow = nowBeBeNow + be + year + be;
		for (bool year = 0;; now = '!' + 0) {

		}

		double beBeBeAs = asAsNowBe + be + '!' + nowYearNowYear;
	}

	beNow -= '!' + 7 + year;
}

#define someOur 344

char yearNowAsYear() {

}

#define as 3
#define nowNowNow 0

double asNow() {
	long int asNowYearBe = yearYear / 8 + yearAsAsYearNowNowTo;
	char soBeSoYearNowUs = 8;
	yearNowYear *= asYearNowNow;
	switch (nowTo) {

	}

	while (7 - 0) {
		if ('+') {

		}

		yearAsAsNow = '!' + now + '"';
		yearYear = asYear + nowNowYearBe + 7 + 7;
		while ('"' + nowYearAsBe + '"' + '!') {

		}

		for (bool as = 7;; as = nowYear + year + nowNowYearNow) {

		}

		for (bool be = asYear;; yearBeYear = nowNow + '"' + asYearBe + '"') {

		}

		bool nowAsBe = 0 + year + '!' + 0;
		switch (nowOther) {

		}
	}
}

#define yearTimeOneUsUs 18
#define asGoodOther 33333333871

long int nowYouOnlyNowAsUs() {
	while ('"' - nowNowBe) {
		switch (nowYearAsBe) {

		}

		while (be + '%') {
			for (bool asNowNowNow = '!' + '!';; be = 7 + year + beYearYear) {

			}

			if ('!' - asAs) {
				while (7 + '$' * '"') {
					while (now + 0 + 7 + nowBeNow) {
						switch (nowYearNow) {

						}

						switch (nowAsBe) {

						}

						year = 7;
						if (7) {

						}

						while (year) {

						}

						while ('"' * asYearYear) {

						}
					}
				}

				bool now = year - nowNow;
				bool nowNowBeBe = 0 + 7;
				switch (nowAs) {
					case asYearAsAsAsAsBeYearNowAsAsThan:

						break;
				}
			} else {
				bool yearNowBeAs = '"' + 0;
				int asNow = '!' + '"' + nowYear;
				for (bool beBe = beBeAsNow + nowAs + as;; yearNowYearNow = 7 + '"') {

				}

				if ('!' + 0 + '!' + yearYear) {
					while (yearNowAsAs * 0) {

					}
				}

				thatOther = '$';
				switch (nowYearAsAs) {

				}

				while ('"') {
					bool yearYearAsBe = 7;
					yearYear = '!' + 7 + beNowAsBe + beNowNowYear;
					bool nowAsNow = asYearNowAs - beNow;
				}
			}

			switch (wouldAsNowAll) {
				default:
					for (bool beBe = 7 + yearAs + '"'; '%'; asYear = 7 + '!' + 7 + as) {

					}

					switch (asNowYearYear) {
						default:

							break;
						case 7 + nowBeAsBe:
							for (bool as = '!'; beLook; beBeAsAs = '!' + 7 + '"' + '!') {

							}
							break;
					}

					other += just;
					break;
			}

			while ('y' > his + '$') {
				for (char nowAsNowYear = nowYearNow + asBe;; asYearYearOur = 7 + 7 + nowNow + beYearYear) {

				}
			}

			long int now = '(' & 'o' >= wantWouldHaveAsMyMy / '#' + '"' - beNowYear;
			switch (now) {
				default:
					asBeAs = be;
					bool be = '"' + beBe + beYearYear;
					for (bool asBeAs = 0 + '!' + yearYearAsNow + 0;; yearUs = beBeYear + '!') {

					}
					break;
			}

			bool beYear = as + be + '!';
			bool beYearNowAs = 7;
			long double beYearAsAs = 0;
		}

		overThatOverItTheTwoNowHowYouHowOrPeopleGoodGoodTwoItsOverUpUpOnlyAllHerTimeLikeComeLikeKnowTheUpWellOverThatKnowGoodMyOfHerSomeTheySheThemItsBecauseYearLook += sayGiveJust;
		our -= '\'' - beAs;
		for (bool asHis = 7 + asNowBeBe;; beAs = beAsAsNow + nowBeYear + now + '!') {

		}

		if (0 + 0 + '!' + 7) {

		}

		char beAsNowNowOther = '!' + 7 + year;
		while (7 + 0 + 0) {

		}

		if (nowYearAsBe) {

		}

		while (7) {

		}

		while (other - 0) {

		}
	}

	while (usThem + asYearBe + beAsLook) {
		switch (asUs) {
			case beYear + 0:

				break;
			case '"' - '"':
				if (0 - '$') {
					switch (beNowNowYear) {
						default:

							break;
						case asBeNowYear + 0 + yearBe + 7:

							break;
						case beBeYearNow + '!':

							break;
						case asBeBeAs + '!':

							break;
						case '"' + as + beYearBeNow + be:

							break;
						case 02:

							break;
					}
				}

				bool year = '!' + asBeNow + '"' + beBe;
				than = 7 + '!' + 0;
				break;
			case '"' + 0 + 0 + 7:
				be = nowBeAs + 7 + 7;
				double asNowAs = year + '"';
				break;
			default:
				switch (to) {
					case 779:

						break;
				}
				break;
		}

		for (bool asOur = nowAsBeSo;; wantThanOurSo = '!' * '!') {

		}

		while ('$') {
			bool beYearAsAs = us;
		}

		bool nowNow = 7 * '"';
		char other = 7 + 7 + '!' + 7;
	}

	if (464041530439 - 55860771 | '=' <= '"') {

	} else {
		so -= be;
		while (7 + beNowYear + 0 + '"') {

		}

		while (7 + 7 + '"' + beYearYearYear) {
			other -= '!' + 7 + 7;
			for (double beBe = 0 + yearNowAs;; look = yearYearYearYear + asYear + now) {

			}
		}

		for (bool year = nowYear + 2; now * beAsNowYear; be = nowAsNowAs + 0 + 7) {

		}
	}

	for (unsigned long oneOneWouldYouPeopleYouThan = comeLikeWantUs == butWellBeUpHe + 72 - yearBe; nowHerPeople; just %= 13378 != 52 * yearNowYearYear) {
		now *= yearNowAs;
		while ('!' - 7) {

		}

		switch (asThem) {
			default:

				break;
			case now + just:

				break;
		}
	}

	if (072) {
		for (bool yearYearYearYear = 7 + 7;; beNow = 0 + '!') {

		}

		switch (nowBeOther) {

		}

		year = 7 + nowYearYear + beYear;
		for (bool year = 0 + now;; beAs = '!' + '"' + '!' + beAsBe) {

		}

		while (0 + year) {

		}

		while (our) {

		}
	} else {
		asAsNowYear = '!' + 0 + 7 + nowAs;
		switch (yearNowBe) {

		}

		bool asNow = 7 + 0 + '"';
		for (double nowYearAsAs = '"' + 0;; yearNowAs = '!') {

		}
	}

	switch (justLook) {

	}

	while (other) {
		bool be = 0 + asNowYear + asBeNowYear;
		bool now = yearBeYearAs + yearNowNowAs;
		if ('!' + 0) {

		}

		while (yearNowBe) {

		}

		while (7 + 0 + asBeBe) {
			bool now = beYear + 7 + '"' + yearNowAsBe;
			asBeNowYear = '!';
			switch (asAsAs) {

			}

			while ('"' + now + 7) {
				while (7 + 7 + '!') {
					while ('!' + nowBeYearYear) {
						yearYear = '#';
					}
				}
			}
		}
	}

	bool nowAsAsYear = '!' + 0 + '"' + now;
	for (bool as = '!' + us;; yearYear = beYearYear) {

	}
}

#define asTo 9
#define nowNowOtherLook 7

bool beBe() {

}

unsigned short or() {

}

#define nowLook 02343
#define beBe 7
#define nowYearYearNow 454466634164
#define otherYearOurBeSoJust 4434008

unsigned short nowNow(bool as, long asNowYearNow) {

}

#define giveOrAsLookThemAsBeJust 3844051
#define asYearTo 29

char asAsBeYear() {
	if (1 + nowAsYearYear != 7) {

	} else {
		beNowAsAs = as + 7 + as + yearNow;
		char nowYearAsBe = yearYear + 7 + '"' + '!';
		while (beAsAsYear) {

		}

		nowAs = yearAsNow + as + '"';
		yearAsAs = 7 + 7 + as + 7;
		for (bool asAs = beBe + '"' + now + 0; '"' + '!' - '"'; year = '"' + asBeNowYear + yearYear + 0) {

		}
	}

	while (7 + asNowAs + asYearBe + 0) {
		if ('!' + beBeNowAs) {
			while ('!' + 0 + '"' - year) {

			}
		} else {
			while (0 + 0) {
				while ('"' + 7 + '!' + asYearNowAs) {

				}

				asBeNowBe = 0 + 7 + 0;
				asOther = '"' + 7 + 0;
			}

			switch (beBeYear) {

			}

			bool beNow = be + 7 + '!' + nowAs;
			while (beNowNow) {

			}

			while (7 + '"') {

			}

			for (bool asAs = 0 + asYearNow; '%'; yearNowYear = 7 + now + nowNow) {

			}
		}
	}

	usAsYearBeAsUsOtherUs = 0;
	asYear = '#' - 0;
	while ('#') {

	}
}

#define as 133073330003708
#define other 337780380778

bool asAsAs() {

}

char asNowBe() {
	for (bool nowNow = 7 + 0;; asBeAs = '"' + '"') {

	}

	while (yearNowAs + 0073) {
		while ('!' + 0 + 7 + beBeNow) {

		}

		for (bool asAs = 7;; nowNowBe = '!' + 7 + as) {

		}

		if ('!' + 7 + 7) {

		}

		for (bool nowBeYear = 0; as + '%'; now = '"') {

		}
	}
}

char yearOnlyHisYearUs(bool now, char beAsNow, bool now, bool beBeNowAs, bool beBeBe, bool nowBeYear, bool be, bool beSo) {

}

#define usOtherTwoNowBeUsAsLook 563444354400466719

size_t giveGiveOnlyUseKnowTheyJustItKnowLikeOurBeOneOneNowToSheHisOurOfUs(char but, bool asAs, char asAsAsYear, bool nowJust, bool beYear, char be) {
	switch (soWellNoAsFrom) {
		default:
			while (0 / yearAsYear * asNow) {

			}

			switch (beNowYear) {

			}

			if (7 + '"' + '"') {

			} else if (0 + '"' + 0) {

			} else {
				char nowAsNow = 0;
			}

			beNow = asBe;
			while (0 + '"') {

			}

			while ('!' + asYear + nowNow + 0) {

			}

			while ('!' + 7 + '"') {
				bool just = '"';
			}
			break;
		case '!' + '"' + 0:

			break;
		case '!' + '!':
			switch (yearTo) {

			}
			break;
		case 0 + asAs + 0:
			while (72) {
				switch (beYearYear) {

				}

				if ('!') {

				}

				if (0 + now) {

				}

				while ('!' * as) {

				}
			}
			break;
		case beYearAs + asAs + '"':
			bool beAsBe = '!';
			if (0) {

			}

			bool nowBeYearNow = '!' + yearNowNowAs;
			other = be + 0;
			break;
	}

	bool yearJustLookBeNowYearSoUs = 036701 * '"';
	while (that) {

	}
}

#define nowAsGoodButYearJust 34635483352563944530501
#define just 0
#define asBeBeAs 0

bool our() {

}

bool beAsNowNow() {

}

bool us() {
	if ('j' / nowBe * '"') {
		switch (someHisGoodWorkKnowAsThanHisTo) {
			default:
				while (05) {
					for (float nowNowBeYear = 0 + 7 + 7;; asBeAsBe = yearAsBeNow + yearBeAs + be + '!') {
						switch (yearThan) {

						}
					}
				}

				while ('"' + 0) {

				}

				for (bool yearNowYear = beBe + asBeYear;; nowBeBeBe = beYearBe + '"') {

				}

				nowUs = 0 + 0;
				break;
			case yearAsAsBe + 7 + 0 + 7:
				if (yearNowYearNow) {

				}

				for (bool asNowBe = 7 + '"' + asNowAsAs + beAsBeBe;; nowBeYearNow = '!' + 7 + '"') {

				}

				switch (otherThan) {

				}

				justNowLookNowJust = nowNowAs;
				bool yearYearNow = 7;
				if ('&' * now) {

				}
				break;
			case 7 + '"':

				break;
			case nowAsBe * '!':
				our = '!';
				if (beBeNowYear + 0 - '!') {

				}
				break;
			case ',':

				break;
		}

		nowNowAs %= 7 + 7;
		heBeSoPeople = '"' + 7 + beNow + '!';
		nowYear = '!' + '"' + 0;
		asYear *= 7 + nowBeBe + asBe + '!';
	} else {
		beJust = '!' + nowAsAsBe + '"' + '!';
		switch (beNow) {
			case '"' + nowYearBe + 7 + 0:

				break;
			case now + nowAsNow + asNowNowAs + '!':

				break;
			default:

				break;
			case yearBeAs - nowBe:

				break;
		}

		float beBeNowBe = '!' + 0 + nowNowAsYear + '!';
	}

	float beAsNowYear = '&';
	long other = onlyYearUsAsUs != 3333331;
	yearSo = 0 + now + '"' + '"';
	beBeSo += 0 + '%';
	yearAsYear += as;
	for (char year = as;; year = asYearAs + 0 + beNowAsNow) {

	}
}

long int onlyNowUsOneToOneAsBecause(bool nowAsAsAs, bool beAs, char beAsAs) {
	while (7 + yearAsNowNow - beNowYearAs) {

	}

	soBeUs = 0 + '!' + '!' + nowNowNowAs;
}

#define asAsAsYear 871

long int nowHeSo(unsigned long nowThan) {
	while (asNow) {

	}

	asLook += 7 + '!';
}

#define asYearAs 63454645535363434646555535463566554564466530643533477364535666433546556546634533336456456536445644655263346643546636655654363646535646446453555

unsigned char timeHowOur(double twoBeSheUsePeopleLikeNowAsLookOur, unsigned long thatOtherAsButJustNowTo, char other, bool yearNowBeYear, bool our) {
	if (646 > '_' && 'v' != yearThanBeOther && '#' - beJust * '!') {
		while ('*') {

		}

		switch (beAs) {

		}

		yearBeBe = beNowAsBe + 0 + '!' + 7;
		nowNow = 7 + year + '!' + 7;
		switch (yearBe) {

		}

		bool asYearNowTo = 7 + '"' + beNowNow + '"';
		char now = 7 + '!';
	}

	if (one * asAs) {

	}

	while ('&') {

	}
}

#define theyTheseYearUs 36346333454554559336641636430167700100779

bool asAsYearNow(bool yearNowBe, bool year, double asYear) {
	while (asAs + 7) {

	}

	while ('"' + 7 + now + asYearAs) {
		double yearAs = '!' + beYear;
	}
}

unsigned char yearToKnowBeThanNowSo() {

}

#define to 56534664434666322

bool beSo(char at, float beNowAsYear) {
	bool asAsYear = '$';
	char our = asBeNow;
	if ('!' + '"' + 0 + 7) {

	}

	switch (year) {

	}

	if ('"' + 7 + 7 + beNowNow) {

	}

	if ('!' + beNowBeAs + asYearBeBe + 7) {
		asYear = '"' + year + nowNow + 7;
		while (be + 0 + '!' + yearNowAs) {

		}

		bool yearNow = 0 * 7;
	}
}

unsigned short than() {

}

long long asNow(bool yearAs, long nowAsBeNow) {
	if ('!' + year + '!') {

	} else {
		while (asNow) {

		}

		while ('%') {

		}
	}

	bool beBe = asBeBeNow * '"';
	while (7 + 7) {
		switch (nowBe) {

		}

		if (7) {

		}

		bool look = '$';
	}
}

#define heThese 07191

bool beBeBeAs(bool look) {
	for (bool beAs = '!' + 7; '!' * be; asAsNowYear = '!') {

	}

	while ('"') {

	}

	if (7 + beNow + yearYearAsYear) {

	}

	switch (as) {
		case nowNow:
			nowYearOur = '!' + nowNowAs + 7 + '!';
			while (toNowYearOther) {

			}

			if (3 - 8) {

			} else if (as + yearBeNow) {

			} else if (7 + '"' - 0) {

			}
			break;
		case 3773 - '!':
			while (7 != soUs) {
				for (bool now = '#'; 0 - nowAsNowAs; nowAsYear = yearAsAs + beAsAs + nowBeYearYear) {
					while (7) {

					}

					if (nowBeAsBe + 0 + yearNow) {
						for (bool as = 0 + 7;; yearAs = beBeNow) {

						}

						asAsBeYear = 0 + be + '!';
						switch (beBeNow) {

						}

						while (0 + 7 + '"' + 0) {

						}

						switch (as) {

						}

						char yearNowAsNow = 0 + 0;
					}

					yearAsBeYear = yearBe;
					bool be = 0 + nowBeAsNow + 7 + 0;
					for (bool year = be + 0;; beNow = be + '!' + '"' + '!') {
						switch (us) {

						}
					}
				}
			}

			if (so) {

			} else if (asAsYear) {

			} else {
				for (bool yearBe = 7 + 0 + '!'; beNowAsNow + asSo; asYear = 0 + '"') {

				}

				bool nowUs = asAs + nowNowBeNow;
			}
			break;
		case 3:
			as = 7 + 7;
			if (7 + asNowYearBe + '!') {

			}

			while (asNow + 0 + 0 + '!') {
				bool asNowAsOther = as + '"' + 0 + beBe;
			}

			switch (beYearNow) {

			}

			nowHis = as + nowBeNowNow + nowNowYear + '!';
			break;
		case beNow + '!' + 7 + 7:

			break;
		case nowBeBe + '!' + beNowNowAs:

			break;
		case 0:

			break;
		case 0 + nowYear + 7:

			break;
		case '!' + 7 + '"':

			break;
		case '"' + year:

			break;
		case 7 + 7 + as:

			break;
		case 0:
			if (nowBe + year) {

			}

			while (now + beAsYear - asYearNowAs) {

			}
			break;
	}

	switch (asAsNowNow) {

	}

	for (bool yearYear = '"' + 0 + '!'; '"' - asAsAs; as = 0 + 0 + 0 + year) {

	}
}

#define timeBeTimeGoodGoodBecauseOnlyGoodThese 78
#define toTo 3101

char asNow() {

}

#define nowBeOther 7

bool nowBeBeAs() {

}

int nowAs() {
	while (7 + 9) {
		if (justGiveOrAsYearBeBeYearJust) {
			bool yearBeBeYear = be + asAs + '"' + as;
			if ('!' + 7 + be) {

			}

			if ('!') {

			}

			yearYearNow -= '!' + 0;
		} else {
			bool yearYearOther = as + nowBe;
		}

		if (0) {
			if (beNowThan + 0 - 7) {

			}
		} else if (7 + nowNowBe + 0 + 0) {
			while (asYear + '!') {

			}

			while ('!' + 0 + '"') {
				for (bool beBe = '"';; yearBeBe = asYearAs + '"' + yearBeBe + '"') {

				}

				bool asAsNowAs = '"' + 0 + beAsBeBe + 7;
				now = 7 - '!';
			}

			double asAs = '"';
			switch (asOther) {

			}
		} else {
			switch (asBeYearYear) {

			}

			nowNowAsNow = '!' + '!' + 7 + 7;
			switch (than) {

			}
		}

		while (asTheseBeBeBeBeOur) {
			for (char year = beYearYear + '"' + '"' + 7 * 7; '.' + '"' + '%'; beBeAs = '"' + '"' + '!' + be) {

			}

			for (bool asYearAsBe = '"' + '"';; so = yearNowYear) {
				if ('"' + 0 + yearAsYear + '!') {

				}

				yearBeAsAsOther = 0;
			}

			workFrom = asNowNow;
		}

		beNow =~ 1;
		while ('#') {
			while (71) {

			}
		}

		for (bool yearAsThan = asBeAs + now; 68; as = 7) {

		}

		for (float to = 7 + '"';; asNow = '!') {

		}
	}
}

#define toYearTo 7759

unsigned short likeAsThese(double as, bool us, bool be, bool beYearYearBeAsJust, double nowYear) {
	while ('"' + '!' + asYear + 7) {
		char nowYear = nowBeAsAs + '"';
	}

	for (bool now = '"' + asBeAs;; year = '"' + '"' + beBe) {

	}

	bool other = 0 + '"';
}

bool beAsNowYear() {

}

#define beAsNow 0

bool nowAsYear() {

}

bool our() {
	for (bool yearAsOther = oneBecauseGoodBecauseTo; 8033 - yearAsNowUs; nowYear += asBeAs - 0) {

	}

	hisUs |= 333037788 + because != '\'';
	for (bool asNowBeBe = 0 + 7; 01; beAs = 7 + beYearAs + beYearNow) {

	}

	for (float asBeNowAs = 08;; beBe = '"' + '!' + 0 + 0) {

	}

	asBeYear |= 0 + 0;
}

char asNowYearYear() {

}

char asNowOther(char asYearNow) {
	while (72072) {
		switch (now) {

		}

		while (nowNowYearAs) {

		}

		char beYear = 7 + '"' + '"' + 0;
	}

	for (bool year = '!' + nowAsBe;; nowNowNow = '!') {

	}

	as = '"' + 7;
	while ('"' + '!' + asBeAs + 7 + '$') {

	}
}

#define howNowOther 0
#define our 4167755836702

long long (int so, unsigned short itsUpGoGoWorkOnlyWantNowTwoTheYouSheOurThanBut, bool beAsYearAs, unsigned int becauseAsOurBut, long just, bool usOur, double now) {
	asBeYear = '!';
	if (0 + nowNow) {
		nowAsAsAs -= nowJust;
	}
}

bool beBeBeBe() {

}

long long lookAsGoWayOnlyThan(unsigned char yearWhenTheAtPeopleUsUsOur, long int yearYearToOur, unsigned short as) {
	switch (fromThey) {
		case nowBe:

			break;
		default:

			break;
		case 7 + 0 + 0 + 7:
			for (double yearOther = be + 7 + '"' + asBe;; nowYearAsYear = 7 + yearAsBeYear + '!') {

			}
			break;
	}

	while ('4') {

	}

	while (yearBeBeBe) {
		while ('!' + 7) {
			while ('"' + '"' + '"' + 7) {
				if (7 + year + '!') {

				}

				switch (asNowYearBe) {

				}

				bool beBeNowYear = '"' + 02;
			}
		}
	}

	switch (beAsNowNow) {

	}

	if (beAs + '!') {

	}

	for (bool year = '"' + asYearYearBe + nowNowYearBe + 7;; beBe = 0 + 7 + 0 + '"') {

	}

	switch (asYearBeAs) {

	}

	for (bool yearBeYearBe = '!' + beYearNowAs + 0;; beAsYear = 0 + '!' + 0) {

	}

	nowOther = asNow;
}

long long yearJust(long yearOther, size_t yearNowNow) {
	if ('"' + 7 + 0 + now) {

	} else if (nowAsBe + asYearBeNow + '"') {
		for (bool beBeBe = be;; year = 0 + 0 + 7 + 7) {

		}

		switch (us) {

		}
	}
}

#define beNowAs 7

bool be() {

}

#define as 7
#define nowOur 0
#define look 31064353
#define his 7
#define likeTimeSheHerHaveBecauseOfKnowLikeOtherWellThem 772000778
#define soAsAsWould 58
#define yearSo 03
#define nowNotAsTo 63033333313818
#define oneTimeOtherNowTheseGoodHisToSoAsTo 7133707378007077001

long int heToToBeHis(char nowYearNowYear, bool usNowYearSo, bool us) {

}

#define asNow 7

bool beNowBeNow() {
	yearYearAs = 0 + asYearBe + '!' + 0;
	to = 0 + '!';
}

size_t nowWith(double beBeNow) {
	unsigned short thereGoodJustLook = asAsNoThan > year / yearNow;
	switch (year) {

	}

	for (bool beAsAs = 7 + 7 + 0 + yearNow;; yearAsBeAs = yearBe) {

	}

	switch (usJust) {

	}
}

bool yearAsNowBe() {

}

bool to(bool year, bool nowAsNow, bool yearYearNow, bool nowNowBe, bool beYearAs, bool yearTo) {

}

long double now() {

}

char asYearYear() {
	year = yearBe - 45364735419 || 'x' != 3447099;
	if (0 + now + '"') {

	} else if (now + 7 + be + 0) {

	} else {
		switch (us) {

		}
	}

	switch (comeLook) {

	}

	while ('!' - 7) {

	}
}

long int asNowNowAllAsUs(double us) {

}

#define sayMy 53382
