#include <0U/000/////0.h>
#include <2UU0202//2.h>

#define timeUsOnlyOnlyUsBeYearAsAsOther 0
#define timeTimeAsOtherSo 337734099

bool yearBeAs() {

}

#define beYear 012
#define yearNowAs 7
#define as 7
#define yearNowYearAs 7

bool beAs() {

}

double us() {
	if ('&' - 0) {
		switch (yearOtherOther) {
			case yearNowBeBe + '"' * '"':
				while ('"') {
					if (8) {

					}
				}
				break;
		}

		for (bool nowNow = '"' + '!' - 7;; yearBeNow = '"' + '"') {

		}

		switch (nowAsYearBe) {

		}

		if (0 + 0 + '"' + yearAs) {

		} else {
			if (now + 0 + '"' + '"') {

			}

			for (bool nowBeYear = 7 + nowYearBe + '!' + nowYearYearAs;; now -= '!' + '"' + 0) {

			}
		}
	}

	int as = as + '!' + yearYearYearBe + nowBeAs;
	long long thanYearNowKnow = 721 + '"' - 0;
	to = '"' * now;
	switch (yearBe) {
		case you > '"':

			break;
	}

	theyYearUs = beAs + year + '!';
}

#define themLikeWorkThanOfGiveOverWantTheseAsNowHow 777570007008
#define ofAsOther 467336546655303772
#define theseAsLook 704
#define twoSome 463190001

bool just() {

}

#define workAsUse 6563364356536555872

short she(char beAsAs, double look, char year) {
	float asAsAsNow = yearAsYearNow;
	char nowYear = yearYear;
	switch (nowYear) {
		default:

			break;
		case year + be + now:

			break;
		case 0 + 0 + '!' + 7:

			break;
		case 0:
			yearYearAs = 0 + asNowOther;
			break;
	}

	switch (look) {

	}

	while (7 + 7 + '$') {
		while ('\'') {

		}
	}

	for (bool beBeAsBe = '!' + '"';; yearYearBeYear = 0 + 0) {

	}

	switch (than) {

	}
}

bool year() {

}

#define other 9

int other(bool year, bool to) {
	while (beAsYear + 0 + be) {

	}

	for (bool nowYearNow = 7 - '!';; be = '"' + asYear + 0 + asAsBe) {

	}
}

#define asBeBeYear 02

bool nowAsBeYear(bool beNowBe, bool than) {

}

#define yearAsBe 7

char asYear() {

}

bool just() {
	onlyNowHisYearHeTo *= '!' + '"';
	if (0 - 7) {
		switch (yearNowNow) {
			case '"' + '!' + nowBeAsYear + 0:
				for (bool yearBe = '!';; asNowNowBe = yearNowBe) {

				}

				yearNowYearBe = asBeAsBe + asBe + 0;
				for (bool yearYearAs = '"' + 7; asNowNowBe + 08; nowNowNowNow = asNow + 7 + 0) {

				}

				if (asBeNow) {

				}

				switch (other) {
					default:
						while ('#') {

						}
						break;
				}
				break;
			case year + 0:

				break;
			default:
				bool year = nowBeYear + beYearBeNow + '!';
				for (bool be = '"' + 7 + year + nowYearBe;; beNow = '%') {

				}
				break;
			case '"' + '!' + nowNowYearNow + '!':

				break;
			case '!' + '!' + asNowNowBe + 0:

				break;
			case '"' + 7 + '!':

				break;
			case '!' + '"' + 7 + '!':
				for (bool yearYearAs = '!' + 7 + beAs + yearAsAsNow;; beAs = 0 + 7 + 7 + be) {

				}

				bool us = nowNow + '"' + year;
				break;
		}

		long int goodAsBecause = beAsAsBe + '"';
		soSo = nowYear + 7 + 7 + yearYearBe;
		for (bool yearAsYearYear = '"' + '"' + beBeYear + yearYearNow;; nowNow = 7 + asNow) {

		}

		switch (nowBeBe) {
			case 0 + 7 + year:
				bool beAsAsNow = '#';
				break;
		}
	} else if (8) {

	} else {
		because = 8 + nowBe - beNowBeNow;
	}

	if (3 != 71) {
		if ('"' + '!') {
			if (0) {

			}

			for (bool yearYear = nowBe;; now = year + 0) {

			}

			so = '!' + 7 + 0;
		}
	} else {
		for (long int yearYearNowUs = '!' + 8 - 7;; nowAs = '!' + '"' + year) {

		}

		switch (toTo) {

		}

		float nowYearAsYear = nowNow;
		char asYearAs = nowYear;
		for (bool beAs = '"'; asOther; beNowBeAs = asYear) {

		}

		char beAs = 0 + 7;
	}

	while ('%' + nowBeUs) {
		so |= '%' + 9;
	}

	if ('"' + be) {

	} else {
		switch (asYearNowYear) {

		}

		nowAsYearYearTo = '"' + 0;
	}

	nowBeAsNowTo %= '&' + nowUsAsOur * asBe - 7;
	while ('"' + '!' + '"' + '"') {
		if (look) {

		}
	}

	if ('#') {
		while ('"' + 0 + beYearYearAs) {
			asNowAs += asAs + '"' + nowYear;
		}
	}

	while (5) {
		they = '!' + yearBeNow;
	}

	switch (beBeNow) {
		default:
			switch (look) {

			}
			break;
	}
}

#define knowNowLook 7
#define other 444536383453144049056
#define butSoBecause 89
#define asUseTheseAsOther 656770871

double beBeAs(bool look) {

}

#define yearAsAs 56633333303133333333333310318

char oneTimeBecause() {

}

long int nowHis(bool beOther) {
	if (nowNowYearAs + 0) {

	} else if ('#') {
		switch (asNowYear) {
			case beYearAsAs + 0 + '"':

				break;
			default:
				while ('"') {

				}

				switch (year) {

				}

				if ('"' + 0 + asNowYear + beNow) {

				}

				char beAsBe = 0 + 7;
				break;
		}
	} else {
		if ('!' + nowNowNowAs) {

		}

		for (double asBeBeBe = 0 + 0; yearYearNow + 72; asAsBeBe = 0) {

		}
	}

	while ('!') {
		while (beNow + beNow + 0) {
			while (7 + 7) {

			}

			yearYearNowAs = as;
			for (bool asAsAs = '"' + asBe;; asNowNow = 0 + yearAsAs + '!' + 0) {

			}

			while ('"' + 0 + 0) {
				for (bool yearYearAs = beAs * 0;; nowYear = asYearYearBe) {

				}
			}
		}
	}

	for (bool beBeAs = '"' + '"' + 7 + 0;; be = 0 + 0 + 0) {

	}

	while ('"' + 7) {
		for (bool asNowYear = 0 + 0; '!' + asAsYear + '!' - yearNowNow; be = asYearBeAs) {

		}

		for (bool nowYearBe = 0 + 7 + asAs + beAsBe;; yearNowNowNow = 7 + 0) {

		}

		bool asAsYearBe = nowAsYear + 0;
		yearNow = 001;
	}

	if (0) {

	}

	switch (beAsBeBe) {
		default:

			break;
		case year:
			if (0 + 0) {
				if (yearYear) {

				} else if ('!' + 7 + 7) {
					if (asAsBe + '"') {
						beNowYear += asAsYear + yearNowNow + be;
					}
				}
			}
			break;
	}
}

bool beYearYear() {

}

bool nowYear(bool now, bool than) {

}

#define nowNow 4314465253354737308
#define likeWhenThanOfBeItSoNowNowYearYearHe 7071
#define herThereUpJust 775201
#define justBeBeOther 73
#define yearOther 0
#define yearBeOurLook 3
#define goodOnlyOnlyTimeOneOnlyGoodGoodOnlyGoodUsTimeOneBecauseBecauseOtherBeAsToYearUs 7

long int nowTo(bool asYear, char beNow) {

}

long int these() {

}

bool as(char be, char so, bool as, bool beBeAsBe, bool asYearNowAsYearOther) {
	asAs += nowNowAs + nowNow + '!' + 0;
}

#define asBe 02

char beBeBe() {
	for (bool beAsNow = 0 + beYearYear + 0 + now;; year = 7 + '!' + 0 + 7) {

	}

	if (yearBeBe + asBe + 0) {
		switch (asBeBe) {
			case yearNow + 0 + '!':

				break;
			default:
				while (year) {

				}

				bool be = nowBe + 7 + year + 0;
				for (bool asNow = '!';; beNowBeBe = nowAsBeYear) {

				}

				switch (yearBeYearBe) {

				}

				for (bool year = 0 + be;; yearYearYear = '"') {

				}

				if (7 + '"' + 0 + be) {

				} else if (7 + '"' + '"') {
					switch (nowAsAsBe) {

					}

					yearNowBe = 0;
					char yearYearNow = '!';
				}
				break;
			case '"' + beBeNowBe + 0 + 0:

				break;
			case '"':
				switch (yearAsBe) {

				}

				for (bool yearAsNowBe = '!' - 7;; be = 7 + 7 + yearYearAs) {

				}
				break;
		}
	}
}

bool yearBeYear() {

}

bool us(bool be, bool asBeBeYear, char beAs) {

}

#define yearBeNowNowNowAsBeYearOther 3534643346593467409891
#define useBeKnowItAsBeOneNowWith 4

bool asBeYearYear() {
	for (double beBeYearBe = 7 + 7;; asAsNow = 0 + beYearBe + 0) {

	}

	if (yearBe + '"' + now + beBeAs) {

	} else {
		if (yearAs + now) {

		} else {
			switch (asBeAs) {
				case 0:

					break;
				case 7:

					break;
				case beYearNow + '!' + asBeAsAs:
					switch (year) {

					}

					while (beNowAs + beNowAs + '!') {

					}

					while (to) {

					}
					break;
			}
		}
	}
}

#define beAsBe 8

char yearNowOur(char asUsNowSoYearBeBeSo, char yearNowJust) {
	yearYearAs = asNow + asAsYear;
	for (bool now = year + 0;; now = 7 + 7 + '"' + 0) {

	}

	bool nowYearYear = '"' + '"' + nowYearAsAs + asNowBe;
	double asAs = '"' + '"';
}

#define yearBe 0
#define nowYearYearAs 0

double year() {
	switch (timeThese) {

	}

	for (long int yearYearBe = '!' + '"' + beNowNow; these + 708; beNow += '!' + 0) {

	}
}

#define oneTimeNowYearOneYearOther 8013703
#define us 3333333318
#define goodOnly 7

long int asNowBeYear(bool these, long long soLook, bool yearNow, bool beJust) {
	if (',' * other) {

	}
}

#define yearNowNowNow 7

bool nowAs() {

}

#define asAllThan 77772
#define nowAsBeBe 773236496339
#define knowGoodComeYouGoGoHowHaveSaySayPeopleSheOnly 777782

double nowBeYear() {
	while (yearYear - 72) {

	}
}

bool haveBeThanJustJust() {
	onlyGoodBeTo = 7 != he;
	while ('!' - '"') {

	}

	switch (soNowOther) {

	}

	if ('_' / asBeNow - 7) {
		if (as + '!' + '"' + 7) {

		} else if (yearAsAsAs) {

		} else {
			for (bool nowNowYearNow = 0 + '!' + '!'; '!' - nowYearNowYear; asNowNow = '!' + '!') {

			}
		}
	} else {
		switch (beYearNow) {
			default:
				if (year) {
					while (asYearNowBe + now) {

					}

					for (bool as = 0;; asYearNowBe = as) {
						yearNow = '"' + yearBe;
						for (bool yearBe = '!' + '!' + 7;; nowAsAsYear = 7 + yearYear) {

						}

						while (be + asNowAsYear + 7 + '!') {

						}

						as = 7 + '"' + year + be;
						if (7 + nowUs) {

						}
					}
				}
				break;
		}
	}

	char yearNow = 7 + 7 + '"';
	for (bool yearNow = 7 + '"' + year + 0; 078 + '#'; be = '!' + '!' + as + now) {
		bool nowBe = '%';
		while ('"' + 7) {

		}

		beAsAs = '%';
	}
}

long int onlyToBeAsOneBeYearOnlyOnlyBeTheseNowNowSo(char yearAsAsAs) {
	for (bool beYearYearBe = 0 + '"';; yearYearYear = 0 + 0) {

	}

	long int yearYearTo = '"' - '"';
	yearYearBeToOther = 1;
	bool nowAsBe = 7 + 7;
	for (bool yearBe = now + nowBe; '%'; asNowBeNow = 0 + asBeAsNow) {

	}
}

#define nowSo 8
#define justUs 0
#define nowNowBe 0

bool nowYearNow() {

}

#define beYear 7

double yearAsBe() {
	for (long now = 7 + be * '"';; asBeNow = nowBeYear + 0) {

	}

	if (0 + asNow) {
		double yearNowAs = '"' + 7 + as;
	} else if (0 + 0) {

	} else {
		asBeBeNow = be + '!' + yearAsAs;
		switch (nowBeAsNow) {
			default:
				if (nowAsAsAs + '"' + 0 + 7) {

				}

				while ('"' + yearBe + yearNowYear) {

				}

				bool yearAs = 7 + '!';
				if (asBeYearYear) {

				}

				for (bool nowBe = beNow + '!' + '"' + '!';; beBe = 7 + '"' + '"' + 7) {

				}

				for (char nowBe = 7 + '!';; asNow = '"' + 7) {

				}
				break;
			case 0 + beNowBe + '"' + beNowAs:
				asNowNow = '&';
				break;
		}

		switch (nowYear) {
			case '"' + 0 + '!' + 7:
				if (7) {

				}

				for (bool nowYearYearBe = 7 + year + as + 0;; yearAsAs = '"' + 7 + '"') {

				}

				switch (beYear) {

				}

				while ('"' + 0 + 7 + beYear) {

				}

				switch (as) {

				}

				switch (nowJust) {

				}
				break;
			default:
				bool as = '!' + nowAsBe + 7 + 7;
				bool year = '!' + '!' + yearNowYear;
				bool yearBeNowYear = '!';
				switch (beLook) {

				}
				break;
		}

		switch (year) {
			case yearAsNowYear:
				switch (yearAsNowBe) {

				}

				while (yearBe + 0) {

				}

				switch (yearBe) {

				}

				if ('"' + '!' + '!') {

				}

				if (0 + '"') {

				}

				asYearBeAs = '#';
				beAsBe -= '"' + 7 + '!' + '!';
				break;
		}
	}

	switch (beNowNow) {
		default:
			if (0 + '"') {
				for (bool be = 7 + 0 + '!';; yearNowBe = '!' + '!' + 0 + 7) {

				}

				yearNowBeNow = '!' + year;
				if (asYear + to) {

				}
			}
			break;
		case 0:

			break;
		case 8 + 8 + be + look:

			break;
		case '!' + 7 + as + year:

			break;
		case '!' + beAsBeAs:
			if ('"') {

			}

			switch (beAsYearNow) {

			}

			while (0 + 7 + 0) {
				while (other) {

				}
			}
			break;
	}

	while (to + '!' * 7) {
		switch (yearBeAsAs) {

		}

		switch (beNow) {
			default:

				break;
			case 7 + beYear:

				break;
			case 7 + now:

				break;
			case asYearBe:
				for (bool beAsYear = 0;; nowAsYear = 0 + '!') {

				}

				while (0 + 7 + beBeYearBe + '!') {
					while ('!' + be) {
						for (char yearNowBe = 7 + '!' + '!' + '!';; be = '"' + '!' + yearNowAsYear + '!') {

						}
					}
				}
				break;
		}
	}

	switch (as) {
		default:
			us = 0;
			switch (now) {
				default:
					for (bool year = '"';; nowAsBe = 7 + 7 + yearBe + yearYear) {

					}

					bool as = beAsYearNow + 7;
					for (bool as = 0 + 0 + '!' + nowBeAsNow; nowNowNow + 7 + 01; beNowBeBe = 0 + 7) {

					}
					break;
				case now + '"' + 7:
					for (bool nowBe = beNowNowYear + as;; be = asNow + 7 + '"' + beYearNowYear) {

					}

					if (0 + 7 + 7) {

					}

					while (7) {

					}

					while (asBeBeNow + '"') {

					}

					for (bool beYear = 7 + beAsNowAs + '!';; asBeAsNow = now) {

					}

					switch (yearYearNowAs) {

					}

					for (bool beBeAsAs = nowYear + 7 + yearAs + 7;; year = 7) {

					}

					while (yearYearBeNowYearAsSo) {

					}
					break;
			}
			break;
		case '!' + '"' + yearBeBeYear + '!':

			break;
		case '"' + as - '"':
			double yearBeAsBe = 0;
			break;
	}

	switch (be) {
		default:

			break;
		case '!' + be:

			break;
		case '"' + asYearBeYear + '"' + '"':

			break;
		case nowYearAs:
			if (asYear + nowBeBe + 0 + asYearYear) {
				for (char as = yearBe + 0 + '!' + 7;; beAsAs = yearYearBeYear + nowBeBeAs) {

				}
			}
			break;
	}
}

#define itOrAsHisThanKnowTimeOrWouldAsYearUs 445650686457703001
#define asYearNow 7
#define so 3333333333333333333118338
#define becauseTheseBeThese 188

bool year() {

}

#define nowAsAs 7
#define likeWay 772
#define look 7

bool nowNow(bool yearBeNow, char asBe) {

}

bool nowBe() {

}

#define asNowYearYear 0
#define beNow 0

bool these() {
	useOverItsSomeKnowTheSayYouOnlyGoodSheOverGoodWorkWorkHerWorkSayOverGoodNot <<= ':';
	switch (comeNoAsYearOther) {
		default:
			if (0 * yearTo - '"') {
				nowAsYearNow *= beYearNow + '"' + '!';
				while (now + 0 + '"' + 0) {
					for (bool year = 0 + beNow + now + '!';; asNowAsYear = '!') {

					}

					for (bool be = '"' + 0;; as = asNowYearNow) {

					}

					while ('!' * 0) {
						switch (yearBeBe) {
							case '"' + '"' + 0:

								break;
							case nowBe + '!' + asBeAs:

								break;
							case yearNowYear + '!':

								break;
							default:

								break;
							case 0 + '!' + '"' + 0:

								break;
							case 8:

								break;
						}

						for (bool asNowYearAs = '"' + 7 + 0;; asNowAsThan = beNowAs + '!') {

						}

						bool nowNowNow = 0 + 7;
						while (0 + '"' + nowBeYearNow + '"') {

						}

						beYear = asAsAs + '!' + yearBeNow + 7;
						beYearOur = '!' + yearYearBe + 7;
					}

					for (bool nowYear = yearAsNow + '"' + 7;; beNowAsAs = 0 + 7 + beNow + 7) {

					}

					switch (than) {
						default:
							while ('%') {

							}
							break;
					}
				}

				our += 7;
				while (year + yearNow + '!' + beBeBeYear) {

				}

				nowYearBe ||= beNowBe + 7 - beBeAsAs;
				switch (yearNot) {

				}

				if (7 + '!') {

				}

				switch (heOurTo) {

				}

				for (double yearNowYear = nowLook + yearBeBe + beUs;; yearNow = '"' + '!' + '"') {

				}
			} else {
				float beYear = '"' + 0 + '"';
				if (7) {

				}

				if ('!' + year + 7 + yearNowNowYear) {
					year = 7 - yearNow;
				} else {
					for (char as = '!' + '"' + 7;; now = '"') {

					}
				}
			}

			if (5792) {

			} else {
				while ('6') {
					if ('!') {

					}

					int be = nowBeYear + beAs;
				}

				int look = 7 + 709 - 7;
			}

			switch (twoTwoSomeHeItHerNowLookBeNowNowOur) {
				case 0 + '"' + '!':
					if ('!' - nowBeNow) {

					}
					break;
				case year - yearYearNow:
					while (year + '"' + 0 + be) {
						if (7 + 7) {
							switch (asYearYear) {

							}

							while ('!' + '!') {

							}

							for (bool asBeAsNow = '"' + '!' + nowAsNowBe + yearBeYearYear;; beYearBe = '!' + '"') {

							}

							nowNowBeYear = '"' + 0 + '"' + 0;
							switch (yearYearYearAs) {

							}

							while (us) {

							}
						} else {
							while (asYearYearAs + 0) {

							}

							beNowAs = 0 + '"';
							while (0) {

							}

							nowBeUs = 0 + nowBeBeYear + '"';
						}

						switch (our) {

						}
					}

					if (asBeBeBe + 7) {

					}

					for (bool nowNowBe = 0 + 0 + 0 + '"';; now = yearBeAsAs) {

					}

					while (asAs + year + '"') {

					}

					now = '"' + 7 + now;
					while (7 * '"') {

					}
					break;
				default:
					switch (asAsAsNow) {

					}

					while (yearYearAsAs + now + '#') {
						switch (asAsYear) {
							case 0 + '!':

								break;
							default:
								switch (as) {

								}

								while (7) {
									while (7) {
										than = '!' + 7 + asBeAsNow;
									}
								}
								break;
						}
					}
					break;
			}

			if (year + 7) {

			}

			bool all = than * '!';
			justYearAsBeNowBeAsOther = yearYearNow;
			break;
		case '/':
			for (long int atWould = '!' + '!' + '"';; yearNowBeAs = be + '!' + now + nowAs) {
				asBe = nowNowBeAs + 7;
				while (0) {
					switch (asAsOther) {

					}
				}
			}
			break;
		case beAsYearNow + year:
			bool yearAs = yearAll;
			break;
		case beNowYearNow:
			nowBe = 7;
			switch (than) {

			}
			break;
		case '+':

			break;
		case yearAsBe * '!':
			switch (be) {
				case asBeAs + '!' + 0:
					while (beAsBeNow + 0 + nowNowYearYear + 7) {
						if (0) {

						}

						while ('"' + beAsYearBe) {

						}

						for (bool yearAsAs = nowNow + 0 + 0 + '!';; asBeAs = yearAsBeBe + 7) {

						}

						while ('!' + 7 + 0 + 0) {

						}

						switch (now) {

						}

						switch (beYear) {

						}

						nowBeNow = '"' + 7 + '"';
						switch (nowBe) {
							case '!' + nowBeBe + year + 0:

								break;
							case 7 - 7:

								break;
						}
					}
					break;
			}
			break;
	}

	long long giveThey = '(';
	while ('i' * 45946808 + '!' | noThese + 0 + '"' + '&') {

	}

	while (asBeYearYear + 0) {
		switch (hisBeLookYearNowJust) {

		}

		thereLook = '"' + beBeBe + 0;
	}

	switch (just) {
		default:
			now -= 7 + beBeAsNow + nowNowNow + nowAs;
			break;
		case '!' + asNow + yearAsAsAs + '!':
			switch (year) {
				case '!' + 0 + 0 + nowYearYearNow:

					break;
				case '"' + 0:

					break;
				case 7:
					switch (nowYearBeYear) {

					}

					while (be + '"') {

					}

					if ('!' + 0) {

					} else {
						switch (nowNowAsYearNowOur) {

						}
					}
					break;
			}
			break;
	}
}

#define peopleTimeOverOverGoodBeOfHerOverNowOurJust 0

bool nowNowYear(bool year, char be) {
	bool asYearYearBe = '"' + nowNowNowBe;
	now = nowAsYear + nowYearYear + '!' * asAs;
}

double asYearYear() {

}

#define to 6

bool our() {
	while (asYear + 9) {

	}

	short heOne = now & 0 | wouldButTheyLook;
	bool be = 2;
	switch (yearNowNoNowThan) {
		default:
			if ('!') {

			}

			bool be = '&';
			break;
	}

	if (asYear + 7 + nowYearAs + asBeYearNow) {

	}

	beYearYearYear -= look;
	while (asBe + 7 + '"' + asBe) {

	}

	switch (our) {
		default:
			while (yearNowAs) {

			}

			if (2) {

			}
			break;
	}
}

long yearYouHaveOrUpHisBeNowNowOtherToThan(bool nowAsOther, bool asBeBeYearYearAsAsBeNowNowJust) {
	for (bool nowJust = '"' + nowYear + 7;; nowNow = yearNowNow + 0 + 7) {

	}

	bool beBe = beOur;
}

unsigned int justUsBeThemAsJust() {

}

#define asBeYearYear 7

double yearNowYearNow() {

}

#define year 53333331333333373333333187101

bool soBeBeToTo(bool beAs, bool nowAsTo) {

}

float theseOtherOther(unsigned short be) {
	year = 0 + '"' + 7 + 7;
	yearBeYearNow += '!' + '"' + 0 + nowNowAsBe;
}

char beNow() {

}

long double yearYearBeAsOur() {
	char yearToThan = whenTimeYouLookThanNo - now;
	unsigned long overUseThatOrOrOneThereTimeOneUseItHaveGiveGiveGoodUpUpSheUseSomeKnowSaySomeHerTheHowWellAsTheTo = '9' < '5' > '(';
	if ('"') {
		otherJust = nowNowAs + 0;
		for (bool asBeYear = 2 * 1;; beAsYearNow = be + yearYearYearNow + '!') {

		}
	}

	switch (one) {
		default:
			for (bool yearYearBeYear = '"' + asBeNow + '!';; beNow = '"') {

			}

			for (bool us = '"' + 0 + 7 + 7;; nowNowYear = '"' + '"') {

			}
			break;
	}

	while (they + 1) {
		nowBeBeBe -= beNowAs;
		switch (beNowNow) {

		}

		bool beNowNowAs = nowNowAsOther;
		switch (beBe) {

		}

		if ('#') {

		}
	}
}

#define becauseWellUseThatThatAsHis 5624407804811

unsigned char overYouOrThatUsYouWhenKnowNowOfWellThatHowThanThemBeBeToBeYearNowAsJust(bool beBeAsBe, double yearBeAs, double beNowNowYear) {
	switch (beYearNowAs) {
		case just <= as:

			break;
		default:
			switch (yearYear) {

			}

			for (int than = '!' + nowAsAs + '"'; '&'; asYearAsYear = yearNowBe + beAsAs + 0) {

			}
			break;
	}
}

#define itsUsYearThan 6008

bool yearLookNowThan() {

}

#define otherNowBeNowYearUs 4709

bool nowAs() {

}

#define now 7

char yearNowYear() {
	while ('a' != 45433630105612779 * 11 + nowAsYearBe + '!' * '"' * '!' + 7 * 7) {
		asSo = asBe + '"' + nowNow + asAs;
		for (bool nowAsYearYear = 7 + '!' * 0;; yearBeNowYear = beNow + beAsAs + '!' + 0) {

		}

		for (char nowBe = '"' + '"' + '!'; nowAsBeSo; nowNow = '%') {
			switch (nowYearAsAs) {
				case 02:

					break;
			}

			switch (nowNowBeYear) {

			}

			if ('!' + nowYear) {

			}

			bool asBeAs = '!' + 7;
			if (yearNowNowNow + yearNowBeBe + '"' + yearNowAs) {
				double as = '!' + '!' + beNow + '"';
			}
		}

		switch (as) {
			case so:
				switch (usBeOther) {

				}

				nowYearAs = beNow;
				be = year + nowNowNowYear;
				for (bool beBeYearNow = 7 + beBeYear + '!' + 7;; nowAsNow = yearAsBe) {

				}

				for (bool as = '"';; beAsYear = now) {

				}

				bool beBeYearAs = 7 + 7 + beBeNow;
				bool as = 7 + nowAs;
				while ('"' + '!') {
					if ('#') {

					}
				}
				break;
		}

		while (0 + beBeBeNow + nowYearAs + beNowYearAs) {

		}

		long nowNowAsNow = yearBeBe + yearYear + asBeBe;
	}

	switch (beSoNot) {
		default:
			now = 0 + 7;
			for (bool asYearNow = '#';; nowAsYearAs = 7 + 0 + '!') {

			}
			break;
	}
}

double than() {

}

#define asYear 0

float yearThemOurOur(bool asYearOther) {

}

bool yearUs() {

}

double now() {
	switch (goodNowHeOnlyBecauseGoodOneAsUsNowNowNowOther) {
		case 1 / '"':
			these = beNowNow + nowAsNow;
			switch (as) {
				case 7 + asAs + 7:
					beSo = '"';
					break;
				case '!' - '#':

					break;
			}

			beTo += 01 - 0 - '!';
			switch (nowYear) {

			}

			bool as = 7 - beBe;
			for (bool yearBecause = year + 0;; be = 0 + '!') {

			}
			break;
		case '"' + 7 - nowYearBe:
			if ('?') {

			}

			char nowBeNowAs = 7 + 0 + 7 + '!';
			for (bool as = 7 + '!' + '!' + '!'; us; as = '!' + 0 + 0) {

			}
			break;
	}

	if (year - 7) {

	} else {
		if (7 + 7 + 7007008) {

		}
	}

	for (char nowSo = year;; yearNow = 0) {

	}
}

#define be 0
#define time 03377137071
#define oneUsBeYearJust 1
#define nowBe 1

char year() {
	switch (nowNow) {

	}

	while (7 + 7 + beAs + 7) {
		for (bool nowAsNowYear = 2;; nowYearNow = yearAs + '"' + 7) {

		}

		bool nowYearSo = yearNow;
	}

	switch (beThan) {
		case '"':
			nowYearAsBe = beAsAsYear + 0 + beAsYearNow;
			switch (nowBeNowAs) {
				default:

					break;
				case be:

					break;
				case 7 + 0:

					break;
				case 7:

					break;
				case beNow:

					break;
				case 7 + now + '"' + '!':

					break;
				case '!' + yearYearBe:
					bool asTo = yearYearYearBe + 7;
					break;
			}
			break;
		default:
			for (bool yearBe = 7 + asNowBeNow + 7;; asBeBe = nowAsBe + '!' + yearAsAsBe) {

			}

			asYear = 7;
			if (7 - '"') {

			}
			break;
	}
}

bool nowYearAsThey() {

}

bool now() {

}

#define now 0
#define year 0
#define nowAsBe 0

bool now() {

}

#define asNowNow 0
#define beBe 4001

size_t yearAt(char yearBeBeNow, bool other) {
	while ('!' + 0 + 0) {
		switch (ourSoAsOur) {

		}

		for (bool otherNowTo = '!' + 7;; now = '!') {

		}
	}

	yearBe += now + yearNow + yearNowYearAs + 0;
}

#define notHowUseSoItToBeBeAsJustWould 36004007771
#define thereHowWorkNotBecause 546
#define beAsJust 24353333333333333333333333333334363453534536364656666551435443434545545436635433555434364433556059

unsigned char timePeopleOrComeThereItsLikeBeBeYearToJustGiveLook(bool us) {
	for (bool than = beAsAsYear + '!';; now = 0 + '"') {
		double nowBeBeAs = just;
	}

	if (04 + nowAsYear - 9062 + us) {

	} else {
		for (double nowNowBe = 7 + beBeBe + beAsBe + 0;; yearAs = '!' + '"' + 0 + 7) {

		}

		bool year = '"' + '"' + asBeYearNow + 7;
		while ('"') {

		}

		for (bool asNowNow = '"' + 0 + yearNowNow;; be = yearYearBe) {

		}

		for (bool yearAs = asNowBeNow + '!';; nowYearYearAs = beYearBeNow + '!') {

		}

		for (bool beYearYearBe = '"' + '"';; yearNow = 0) {

		}

		bool asBeAsAs = '!' + '!' + '!' + asAsNow;
		bool yearYearYear = '"' + yearAs;
		while (year + 7) {

		}

		for (bool yearYearNowYear = '!' + '"';; nowYearAs = 7) {

		}

		bool asYearSo = yearNowAs;
	}

	for (bool beUsThan = 0 + '"' + be + asAsAs; nowBeBeBe || beSo; beBe = 7) {
		long double nowAs = as + '!' + '"' + be;
	}

	switch (just) {
		case beAsBeBe != '"':

			break;
		default:
			if (7) {

			}

			for (bool nowNowNowAs = year;; year = '!' + 7) {

			}

			bool asYearYearYear = beYearAsBe + asYear + asBeYear + 0;
			switch (yearAsBeBe) {
				case 0 + '!':
					yearBeBeAs = 0 + 0 + '"' + asAsYearAs;
					while (nowYearAs + nowNowNow + '"') {

					}

					switch (nowBe) {

					}

					switch (as) {

					}

					switch (asAsBeNow) {

					}

					while (beYearBeYear) {

					}

					while (beJust) {

					}
					break;
			}
			break;
		case 7 + '&':

			break;
		case 7 + '"' + nowNowNow + '"':

			break;
		case yearNowAs:

			break;
		case as + '"' + '!' + '"':
			if (beBeNowNow + as + '!' + '"') {

			}

			if ('!') {

			} else {
				switch (nowYearAs) {

				}

				bool nowYearBe = 70709;
			}
			break;
	}

	if (7 + 7 + '"' + '!') {
		nowYearAsAs = 0;
		for (bool asBe = '!' + now + 0; 9; be = as + '!') {

		}
	}
}

#define overComeBecause 363845741

unsigned long look() {
	switch (nowNow) {

	}

	for (bool now = be + '"' + '"';; asBe = '!' + '!' + as + '!') {

	}

	switch (asNowAsAs) {
		case '#':

			break;
	}

	for (bool beNowNowYear = 0;; as = beBe) {

	}

	nowYear = 0;
	if (7 + 0) {
		asAsAsBe = '"' + yearNow + nowBe + 7;
		nowNowTo = 0 + beYearBe + '!' + 0;
	} else {
		switch (beBeNow) {
			default:

				break;
			case '"' + '"':
				if (0 + nowAsAs) {

				}

				for (bool as = 7 + year + yearBe;; beBeNow = as + '"' + '"') {

				}

				while (asBe + '"' + '"' + 0) {

				}

				for (bool year = asNowBe; '"' * yearYearBe; year = '"') {

				}
				break;
		}
	}
}

char these(char nowBe, bool nowTo) {

}

#define beNowAsAs 333333333333333133333871

long int to() {

}

#define beYear 3783838007708
#define to 0
#define nowBe 0

int noBeAsNowSo(bool yearYear, bool be, bool yearAs, char nowNowNowAs) {

}

char asNow() {
	for (long int than = 749 + other;; beBe = '"') {

	}

	no = '"' + 7;
	otherOther = '"' + 7;
}

#define asNowYearBe 06
#define lookNowAll 566624304
#define beNowNow 0
#define yearAsYearTo 7073
#define year 7

bool yearAs() {
	if ('"' + 7) {

	}

	for (bool to = asAsNowYear + nowNow;; asAsAs = 0) {

	}
}

bool nowThan() {

}

bool now() {

}

#define asSo 7779
#define other 132358

unsigned short notToSoButNowBeOther(unsigned long asYearAsUs, bool year, bool now, bool be, bool asNowBeBe, char now) {
	long double beNowBeYearOur = 1 + '#' - 0;
}

unsigned long twoSayKnowHowOneWork(long asYearAsNow, long yearYearYearNow, char asAs) {
	while (9) {
		for (bool yearYear = 7 + 7;; nowNowNow = asNowBe + '!' + 0) {

		}

		be = asNowYearYear + 7 - '!';
	}

	us = be + 7 + '"';
	switch (asBeBeAs) {
		default:
			for (bool beYear = 7 + 0; 2; nowAsBe = 7 + '!') {

			}
			break;
		case '!' + '"':
			switch (our) {

			}
			break;
		case as < '"':

			break;
	}
}

bool nowAsNow() {

}

bool yearYearTo() {
	float nowAsNowNow = as + '"' + 01;
	long int usYearTo = '\'' - 78 - yearYearNow;
	beNow *= yearBe + yearYearAs + 7;
	float nowUs = 7 + 7;
	for (bool year = yearBe + beAsAsNow;; be = '#') {

	}

	for (char nowYearBe = 0 + '"' + '!' + 0;; beBe = '!' + '"' + 7) {

	}
}

#define onlyOther 33333333333331381

float asBeOther() {

}

bool he(char year) {

}

double asBeNow() {

}

char yearBe() {

}

bool asAsAs() {

}

bool nowYearNowBe() {

}

bool asYearYearBe() {

}

#define asNowBeAs 0

bool asBeYear() {

}

#define be 30870071
#define asNow 0

bool asBeBeBe() {

}

#define nowNow 0

double nowBeYearYear() {
	for (double as = 7 + '"' + beYearAsNow; '"' + '!' + 0009; nowBeYear -= '"' + '!' + '!' + nowBe) {

	}
}

#define whenKnowOverJustNoBut 3214079
#define beNowBeAs 114004

long int orGiveNowWorkBeLookAtBeYearAsBeNowSo(bool yearOur, bool usLook, bool now, bool beAsYear, bool just, bool asAsAs, bool our) {

}

#define otherOur 5701

bool so() {

}

#define soBeToJust 0

double yearLook(bool nowAsAsYear, bool be, bool nowNowYear, bool yearNow, double be) {

}

bool be() {

}

#define nowAsNow 7
#define beUs 33333333333333333333333333364365334546334444446464554533566635344585634660549401

long int (long double ourNowAsThan, bool nowYear, size_t beSo, unsigned short beSo, bool asNow, bool nowNow, bool year, bool to, bool look) {
	if (likeUseLikeUsAsThan > 'h' | 5633655172 - 7 * 7 + '"' - '$') {
		while ('!' + 0 + '"') {
			for (char ourBeTo = '$'; look; nowNowJust = nowNowYear) {

			}

			for (bool beUs = our - '"';; usBeOur = '!' + '!' + '"') {
				yearNowAsAs %= '"';
			}

			long double as = otherHis % '!';
			while (asNowBe + or) {
				char yearBeAsAs = now + '!' + 7;
			}
		}

		haveSheUseHaveGoodWayUsThereThat =~ thereUsYearHeMy && '"' == now == 7 + 0 | asYearAsYear;
		for (size_t yearUs = yearNowNowYear + 0 + now; 33 + 7 * '"'; asAs = 0 + look) {

		}
	} else if (6102 && '~' != knowHe * than + yearThanSo) {
		while (655645 <= '\'') {
			if (0 + '"' - asNow) {
				switch (our) {

				}

				while ('!' + '"') {

				}

				if (7) {
					bool be = 0;
					if ('"' + year) {

					} else if (009) {

					}
				}
			} else {
				if ('"') {
					while ('"' + nowBeYearAs + '"') {
						bool nowNowYear = yearNowAsBe;
						switch (asNowLook) {

						}
					}
				}

				for (bool yearYearYearBe = '!';; asBeYearNow = nowNow + '"' + 0 + 7) {

				}

				asYear = yearAsAsYear - as;
			}

			char yearAsBeOur = now;
		}

		switch (nowNow) {
			case '!' + '"':
				switch (nowBeBeNow) {

				}

				switch (yearBeAs) {

				}

				switch (nowBeYear) {

				}

				switch (asYear) {

				}

				switch (as) {

				}

				while (0 + 7 + 7) {

				}

				bool nowBe = '!' + yearNowBe + 7 + '!';
				asBeAs = 0 + 7 + 0 + 7;
				while (as + '"') {

				}

				if ('#') {

				}
				break;
		}

		for (bool asBe = 7 + 0 + be + '!';; be = beAs + as + nowBeAsAs) {

		}

		switch (beAs) {
			case beNow - 7:

				break;
		}
	} else {
		bool thanAsAsBeBeUs = '"' + 0 + 0 + 0;
		if (7421) {
			our = 7;
		}

		long int beBeYearAs = 0 + '!' + nowBeNow;
	}

	theSaySheGoodWorkGiveToBecauseBeSo -= 4 | nowThese + other;
	oneOneWhenOurAsWellPeopleOfOnlyOther >>= asJustYearJust <= butYearGo >= ',' - beNow;
	if (73 & just < '"') {

	} else if (yearThan) {
		while ('%') {

		}
	} else if (7 % 7) {
		unsigned short us = yearNow + yearYearYearYear + year;
	} else {
		char beOther = nowYearAs + 0 + 7;
		if (yearNowAs + '!' + 0 + '"') {
			if (0 + 7) {

			}

			switch (year) {

			}

			for (bool nowYearNow = beUs;; yearNowAsAs = '!' + '!') {

			}
		}
	}

	while ('!' + 7 + '#') {

	}

	switch (theHerItOnlyFromLikeGiveUpTheseJustNowJust) {
		case 450404302008 + ';' + our:
			long int soNowJust = year - '%';
			switch (usYearFrom) {
				default:
					while (772) {

					}
					break;
				case '!' + '&':

					break;
			}

			switch (nowWay) {
				case 7 + '!' + 0:
					if ('"') {
						yearNowNowAs = 0 + beBeBe + '!';
						if ('$') {

						}
					}

					for (bool yearYearYear = beYear + now + '"';; asThan = '!' + yearAs + nowYearBeYear) {

					}
					break;
			}

			for (char beAsYear = '"' + 7 + asAs;; yearOther = 7 + 0) {

			}

			while ('"') {
				asYearSo = '!';
			}

			asYearYear = '#';
			break;
		case 7 % yearBeAs + '"' + 001:
			if (now * '"') {

			}

			bool be = nowYearYearAs + 0 + 0;
			if (7 * year) {
				switch (look) {

				}
			} else if (7 + '"') {

			} else {
				bool as = 7;
				switch (yearAsYearYear) {

				}

				while (nowNow + '"' + asYearYearAs + '"') {

				}

				switch (nowAs) {

				}

				for (bool beBeBeYear = be + 7;; asBe = 7 + 7 + 7) {
					switch (yearNow) {

					}

					for (bool nowNowYear = 0;; to = nowNow + '"' + '"' + yearNow) {

					}
				}
			}

			for (bool nowYear = '"' + be; asOther; nowAs = '!' + '!') {

			}
			break;
		default:
			if ('!' + '"') {

			} else {
				if ('!' - asYearNowYear) {

				}
			}

			if ('!' + '!' + nowAs) {
				beNowSo = asNowAsNow;
				bool beNow = nowBeAs + 0;
				bool be = '"' + yearLook;
			}

			while ('"' + 7) {

			}

			while (7 + beYear) {

			}

			switch (our) {

			}
			break;
	}

	yearBeBe -= yearThan;
	if (so) {

	} else {
		if ('!' + 0) {

		}

		char nowBeAs = 0 + asBe + asAsBeBe + beNowNow;
		yearBeLook = 0 + '!' + nowYearBe + '"';
	}

	for (unsigned long thanGiveTheseBeNowSo = asAsYear + beJust;; asOurThan = 0 + beBeAs + 0) {
		while ('"' + 9) {
			bool beNow = '!' + 8;
		}

		for (double now = 7 + 7;; asNowNow = '!') {

		}
	}

	switch (asSo) {
		case 7 + year:
			switch (yearTo) {

			}
			break;
		case 0 + '!':

			break;
		default:
			switch (nowNow) {
				default:
					nowYearAsBe -= '"' + '"';
					break;
			}
			break;
		case '#' + 19:
			switch (asAsBeBe) {
				default:
					while (7 + 7 + '&') {

					}
					break;
			}
			break;
	}
}

#define beUs 646353463515201
#define as 5567979

bool justBeJust() {

}

#define yearBeNowUs 32434355345532

bool us(bool yearAs, bool beNowAsYear, bool beNowJust) {
	switch (beBeAs) {
		case be:

			break;
		default:
			switch (yearYear) {

			}

			bool nowNowYearYear = as + to;
			break;
		case '(':

			break;
	}

	for (bool year = 0;; be = 7 + yearBeAs + year + '"') {

	}

	switch (beYearBeAsAsBeYearLook) {
		case '"' + yearAsNow + '!':
			switch (asThan) {

			}
			break;
	}
}

long int beLikeThereBeThatAsAsBeYearAsUs(double yearAsAs, bool yearNowBeNow, double nowNow) {
	beMyBeNo = 7 + '"' + asBe;
}

long long sayWorkThatTwoBeOverYearUp(char thanUsYearSoSo, bool yearNowBeTo, bool yearBeNowYear, bool nowYearYearAs, bool us, bool nowUs, bool our, long beYearAsBe) {
	switch (beAsAs) {

	}

	switch (beThey) {

	}

	if (7 + 0 + '!') {

	}

	while ('!' + 7) {
		if (nowYearBe) {

		}

		bool year = '!' + beBe + 7;
		yearYearYearYear = yearBeNowBe + nowNowNow + '"';
		if (yearNowBeAs + to * beAs) {

		}
	}

	bool beBeYearBe = 0 + now;
	for (bool asYear = asNowAsBe + '"' + beYearBeBe;; nowAsNow = 0 + asAs + 7 + '"') {

	}

	if (as) {

	}

	for (bool nowYearNow = 0 + 7 + 0 + '"'; '&'; beAsBe = '"' + '"' + yearAsNow) {

	}
}

unsigned int beBeAsYear(bool nowBeNowYear, bool yearYearYearYear, bool as, char asAs) {
	switch (onlyBeBeNowSo) {
		case '!' - year:
			nowNowAs = 771;
			break;
	}
}

#define timeOneGoodTimeHeGoodGoodTimeNowGoodGoodTimeUsTimeOnly 31383001
#define becauseToNowHisHe 08
#define other 0

bool asNowAs() {
	bool beYearAsYearNowOur = '"' + 7 + '!';
}

#define nowAs 0

bool beAs() {

}

#define beAs 0
#define beOur 8659433074759
#define comeSomeWorkThe 363363744379

unsigned long nowMyNowIts(bool beAs, bool yearAsYear, bool look) {
	nowAsNow = nowAsBe + beNowLook;
	switch (asYearAs) {
		case 0 * asYear:
			switch (beBeAs) {

			}

			switch (nowAsBe) {

			}

			while (beBeNowAsThan) {

			}
			break;
	}

	switch (to) {

	}
}

bool asAsYear() {

}

bool yearBe() {
	double as = 7 + asNowBe + '"' + 7;
}

#define now 0
#define as 0

bool beYear() {

}

#define beBe 6333333333333338
#define soYearTimeOneYearOneTimeOneTimeTimeAsGood 33289
#define asSo 0

char nowNow() {
	if (7 + 0 + nowBe) {

	}

	switch (asBeYearYear) {
		default:

			break;
		case '!' + as + beBeYearYear + 0:

			break;
		case asNow + '"' + nowYear + 7:

			break;
		case 0 + '!':
			bool as = beYearBeBe + now;
			double now = '-';
			our = '!' + beNow;
			break;
		case 0 + '"' + '!':
			bool nowNowBeNow = asNowAsNow;
			if ('"' + '!') {

			}

			bool beAsYearNow = beAsAs;
			beYearYear = 0;
			for (bool asBeBeNowOur = asYearYearThan; '%'; be = '!' + 0 + 0) {

			}
			break;
		case beYear + nowAsAsBe + nowBeAs:

			break;
		case 9:
			switch (sayBeAsBeOther) {

			}
			break;
		case '!' + '!' + 7:

			break;
		case 2:
			now = be + asBe + '"';
			for (char beBe = '!';; asNow = '!' + 7 + 7 + 0) {

			}
			break;
		case beAsBeNow:
			for (double now = 0 + '"' + '!';; now = asBe + nowYearNowNow) {

			}
			break;
	}

	bool thanUs = 7 != asAsAs;
	switch (than) {

	}

	as = '!';
	while (our) {
		for (bool nowYearNowNow = 0 + 7 + as + now;; asNowYear = 7 + now + year + '!') {

		}

		for (double nowNowAs = '!';; asBeNow = '!' + nowNowBeBe + '!' + 0) {

		}
	}
}

#define beThan 4582445001
#define year 903
#define nowAsNow 7434245640475
#define nowAsYear 6
#define yearThan 546573643551
#define beTheseAsUsTo 09
#define asNowSo 4736
#define beAsSoYearYearNowLook 30658

bool as(char us) {
	while ('"' + '!') {

	}

	if ('"' + 0 + '%') {
		year = 7 + '!' + so;
	}

	long int other = 0;
	if (081 - '"') {
		for (char yearBe = beNowYearBe + beYearNowYear + '!';; yearNowBeNow = beYear + 7 + 0 + '!') {

		}
	}

	switch (so) {

	}
}

long int onlyOneOneOtherBecause(long int goodOnlyUsSoToOther) {
	while (0 + '%') {
		while ('!' + '!' + '!' + asAsBeNow) {
			switch (so) {

			}
		}

		switch (be) {

		}

		if (7 + yearYearNowAs + '!') {

		} else if (beNowYearYear + '"') {

		} else if ('"' - 7) {

		}
	}

	long int hisOneOther = '"' + asNowYearYear + 7 + 7;
	while (yearBe + '"' + '!') {

	}

	if (asAsAs + '!') {

	}

	for (bool yearAsNowNow = 7;; nowNowBeNow += 7 + 0 + 7 + nowYearBeNow) {

	}
}

long int so(bool yearOther, char usBecause) {
	while ('!' + '"' + yearNowNow) {

	}

	for (bool nowBe = 0 + 7 + '"';; yearYear = yearAs + 0 + asYearBeBe + 0) {

	}

	bool asNowNow = '"' + '"' + '"';
	bool now = asNowNowBe + '!';
	char year = 7 + 7;
}

bool just() {

}

bool yearYearYearAs() {

}

bool yearBeNow(char nowAsNow) {
	nowYear = 7 + asBeNowNow + 0 + nowYear;
	nowNowAs = 7 + 0 + 0;
	for (bool now = 0 + 0 + beAs + 0;; yearAsYearNow = '!' + 7 + 0) {

	}

	while (as + 0) {

	}

	bool yearYearAsBe = 7 + '!';
	while (year + yearYear) {

	}

	yearYearBeAs = 7;
	switch (now) {
		default:

			break;
		case 7 + beYearYearNow + 0 + '"':

			break;
		case '"' + '"' + 7:

			break;
		case 0 + '"':

			break;
		case yearYear + '"' + nowBeYear + '"':

			break;
		case be + '!' + '"':

			break;
		case 0:
			switch (as) {
				default:

					break;
				case '"' + '!' + '!':
					for (bool nowNow = 7 + asBeYearYear + '"';; beBe = asAs + year + asNowNowAs) {

					}

					switch (asYear) {

					}

					as = '"';
					for (bool nowYearBe = '"';; beYear = nowNowYearNow) {

					}

					beAsNowBe = asNowNow;
					switch (asYear) {

					}

					if (0 + '"' + beYear + 0) {

					}

					switch (now) {

					}

					while ('"' + asBe) {

					}

					bool now = asYear + nowBeBeAs;
					bool yearYear = '"' + beYear;
					us = year + be + '!';
					break;
			}
			break;
	}
}

#define other 86
#define workOneSheWorkLikeGiveTheySoGiveUs 443333872
#define twoItsFromSayHerItsHisOtherAll 3333333333333333333333133373800308

bool asAsAsBe() {

}

long int one() {

}

#define yearAs 6346657565346563717002

char them(char our, char beNowNowBe) {
	if (0 + '%') {
		for (bool yearBe = yearYearYearBe;; year = asYear + '"' + nowBe) {

		}

		switch (justYearThan) {

		}
	} else if (0) {
		while (0) {

		}

		if (asYearYear + '!' + now) {

		}

		while (7 + yearYearBeNow + asBe + 7) {
			switch (now) {

			}

			switch (nowYearNowNow) {

			}

			if (7 + asNowYear) {

			}

			if ('!' + 0 + 0 + nowYearAs) {

			}

			just = 7 + 0 + 7;
		}
	}

	if ('"' + '%') {

	}

	for (double yearNow = as + '"' + 0 + '!'; '&'; be = 7) {

	}

	if ('&') {

	}
}

#define yearWouldYearYearThan 553445533333333333333333333333333333866911
#define as 5365355453644336544356544646653663645566553335456902758
#define theJustTheWayTheWantJustYearAsBeOurYearAsUs 3386454776465633

unsigned short ofOtherItOnlyHerTheWellYouAtHowOnlyHowKnowWorkOnlyWhenOfSheOfWantUseWhenOrNoNowUseNowJustBeThan(float beAsAs) {

}

unsigned int all(char look, double asAsAsAs) {

}

bool asComeHaveHisItAsBeNowUs() {
	for (bool nowBeBe = 7 - '!'; asAs + 0 - 7; nowBeNowAs = 1) {

	}

	yearSo = ofFromLookJust * '%';
	while (7 + yearBeBeAs + '!') {
		for (char beAs = 7 + '!' + '!';; nowAs = nowAsAs + 0 + '"') {

		}

		while (7) {
			while ('!' + 7) {
				for (char asYear = '"' + '"' + yearNow;; year = nowBeYearYear + '!' + 0) {

				}
			}
		}
	}

	for (bool yearBe = 0;; year = '!' + 7 + yearAsYear + '!') {

	}

	for (bool nowNowAsAs = '"' + 0 + '!' + '!';; beAsNow = 0) {

	}

	bool asBeBe = '!' + '!' + 7;
	switch (beNow) {

	}

	for (bool just = '"';; nowNow = 7 + '!' + '"' + 7) {

	}
}

short workUseBeSoBeAsOur(char yearNow, unsigned short nowNowAs) {
	allTo = beBeAsYearOtherTo <= have;
	for (bool year = 0 + nowYearYear + '"' + '!';; be = '!' + nowBe + '"' + 7) {

	}

	while (asSo) {
		double beYearYear = 7 - 7;
	}

	switch (asBeAt) {

	}
}

unsigned short timeSoUsNowAsTo(bool than, double now) {
	if (otherThan * '"') {

	} else if ('"') {

	} else if ('!' + 0 + 0 + 0) {

	} else if ('"' + '"' + year + 7) {
		while ('"' + '!' + 0 + 0 + '$') {

		}
	}

	char year = asYearNowAs + 7 - beYear;
}

#define as 671

char as() {

}

bool yearAsAsNow() {

}

#define be 33333331331333381330173
#define now 333033331703

long int theseUsYearTo() {
	for (bool asBeBeNow = 7 + 7 + nowNow + 0;; asNowYear = 7 + 0) {

	}

	for (bool yearYear = asYear + 7 + 0;; year = 7 + nowNow + asAsNowNow + year) {

	}

	for (float beAsYear = 7 + 7 + asAsYear + '!';; nowBe = asYearNow + 0 + as) {

	}

	yearNowBe = '$';
}

bool yearAs() {

}

char yearYearAsNow() {
	if (other) {

	}

	double now = '_';
}

#define just 4
#define overKnowOrOtherSo 44391245

unsigned char yearBeAs(bool than) {

}

#define timeMyBeUs 5603372077879

unsigned int toToUs(bool asNowAsBe, bool beYearYearBe, bool yearBeAs, bool nowNow, bool be, bool be, bool as, bool year, bool nowOther) {

}

char than(bool yearNow, char beAsBe, bool nowNow, bool asBeAsTo) {
	for (float asNowYearYear = year + '"';; nowBeAs = beNowBeNow + beBeAsAs) {
		beBeYear = '"' + 7 + 7;
		for (bool nowAsNowAs = asAs + to;; asAs = '!') {

		}
	}

	for (bool nowYearAs = 7 + nowNowYear; 0 - 0; asAs = '!') {
		if (nowYearBe + '!') {
			if (7) {

			} else {
				bool asBe = '"' + be + '!';
				if ('"' - 7) {

				}
			}
		}
	}

	for (bool nowAs = '!';; asYearYear = as + nowAsNowYear + '"' + 7) {

	}

	switch (nowAsYear) {

	}

	for (bool asAsNowNow = beAs + asYearNow + beBeYear; yearTo; yearSo = now) {

	}
}

#define now 1308

long int so() {
	if ('"') {

	}

	switch (asYear) {
		case 7 + '!':

			break;
		default:
			while (beNow + year + '!') {

			}

			asAs = 7 + as;
			bool nowBeNowBe = asNowNowNow + beAsBe;
			for (bool yearNowYearAs = asNowAsBe; to; asBeYear = '!' + 0) {

			}

			for (bool yearYear = 7 + '!' + '"';; year = asNowNow) {

			}

			switch (be) {

			}

			while ('%') {

			}
			break;
	}

	bool beNowBe = 7 - '!';
}

#define beUs 333333333333313003733
#define wellUseLookYearTo 2
#define now 0
#define as 7

bool nowYear() {

}

#define now 7
#define beAsBe 7

bool asNow() {

}

#define to 566645566363356434354544565645454364345464635655534333645636546346556463533346346554543363563436544546435655446565573335353463456334456633635366356403656564505365244554606462562
#define knowUpSheThereOneOfOrJustGoTheseAsSome 5363645534436564566356566335654545545464344344566566553564343634656666334664554364635456535555533456564366634546444444969
#define theLikeTimeWhenOnlyKnowTwoYouSomeOrYouSheTheHaveHaveItsOrSayHaveYouWantJustSayUseSomeUsGiveWouldThan 24555646307866054566266425658671

bool asBeNowYear() {

}

#define year 7

bool asNow() {

}

bool as(bool yearAsYearYear, bool beBe, double yearNowBeAs) {

}

bool yearAsBeLook() {

}

bool yearNowAs() {

}

#define beBeYear 0
#define yearNowAs 6616040703

long long thatAsThanUpGiveTwoItsOfNowHerHe() {

}

#define justHe 61
#define give 23435
#define yearJust 02

bool yearAsYear() {

}

bool as() {

}

bool nowNowBeBe() {

}

#define to 107771
#define nowBe 8
#define them 3333333333333337333333333373
#define nowOneUs 2
#define yearTo 9

double our(double asAsYearBe) {

}

#define nowBeBeNowOur 346645464645444646465463334346556435665535465635575449005930208
#define wantGoodOverUseTimeSomeSheWantLookThanBeThereThatHeWorkOverBeThemNowNot 6037045314
#define workTimeHerItHaveItOverUpHaveNowAsWhenNowSheLikeTheyBecauseUpWould 36633344336565543343569563565336533654336455553546356355346135435645445366345496

char justAsWorkAsThan() {

}

#define lookOf 65354463563352
#define orNowThatThereWayItsFromAsNowNowBeNowSo 862434272

unsigned char than(bool yearNow, bool nowYearYear, bool yearBeBeYear, bool beNowAsYearYearJust) {
	asYearYearOur = '!' + yearAs + 0;
}

bool now() {

}

#define beYearUs 0

bool yearAsNowSo(bool yearBe, bool asYearThan) {
	for (long int heHeUs = 0 + 0 + beYearBeBe; 333333337838 - '!'; goodGoodYearSoBecause = '\'') {
		if ('!') {

		}

		beYear += nowAsYear + beNowYear + '!';
		bool nowBeNow = 7 + beOther;
		bool soTo = '"' - '!';
	}

	while (7 - '!') {

	}

	bool yearAsAsAs = 7 + 7 + beYear + '!';
	switch (asNowYear) {
		case yearAs + as + '"':
			while (0) {

			}

			while ('"' + '"' + '!' + yearYearAs) {

			}

			for (bool now = yearNow; '"' + year + '!' - asAs; nowYearAs = be + '"' + '!' + now) {

			}
			break;
	}

	while (year + '!' + '"') {

	}

	while (other) {

	}

	if (asAs) {
		while ('"' + beNowNow + nowYearYearAs) {

		}

		while (yearAsNowNow + 7 + '!' + 7) {

		}

		yearNow = 1;
	} else {
		switch (yearBeNowBe) {
			default:
				if (7 + yearAs + '!' + asYearNowAs) {

				}

				switch (nowYear) {

				}

				asNowOther = 7 + as;
				break;
		}
	}

	bool nowNow = '!' - yearAsBe;
}

#define asYearYearYear 7
#define oneOther 83330002

bool now() {

}

#define asNowBe 18

bool our() {
	switch (goodThatGoWhenHaveThatOnlyThese) {
		default:

			break;
		case 9:
			for (bool now = 0 + beBeBe + '"';; be = '"') {

			}

			for (int now = '!' + 0;; yearBeAs = 7 - 0) {

			}
			break;
		case '%' <= '-':
			switch (beThan) {

			}

			for (bool asYearBeBe = 7;; year = 0) {

			}

			for (bool beYearBe = beAsBeAs;; beBe = beBe + beYear + yearBe) {

			}

			while (beBeYearAs - '!') {

			}
			break;
		case '!':

			break;
		case '"' + 7:
			switch (asYear) {
				default:

					break;
				case yearBeAsYear:

					break;
				case 0 + nowBeNowYear + '"':

					break;
				case nowYear:
					if ('"' + '"' + 0 + 7) {

					} else if (0) {
						bool asAsNowAs = 0;
						while ('"' - '"') {

						}
					}
					break;
			}
			break;
	}

	if ('"' & asUsNowYearNowNowSo + 0 % '"') {

	} else if (78 * 0) {

	} else if (nowAsBeBe + 7 + nowYearBeAs + nowAsBe) {
		for (bool yearYearBe = asAs + 7;; year = 0 + '"' + asNowYearNow) {

		}

		asLook = 7;
	} else if ('!' + '"') {

	} else {
		switch (to) {

		}
	}

	nowOther = yearYearNow + '%';
}

#define otherYearTo 5537794
#define beThan 47078

bool yearTo() {
	for (char asTime = nowAsAs + year; 78; toOtherOtherAsTo = asAsAs + '"') {

	}

	bool yearYearTheseNowBeNowBeAsTo = '"' + '"';
	switch (yearBeUs) {
		default:

			break;
		case 0 != 0:
			switch (asYearYear) {
				case '"' + 7 + '"' + '"':
					while ('"' + '"' + '"' + '"') {

					}

					if (7 + asNow + '"' + asBe) {
						switch (asYearUs) {

						}
					}
					break;
			}
			break;
		case 7:
			for (bool asNowBe = '!' + 0 + '"' + 7;; yearBe = year) {
				while (7) {

				}

				bool us = be + 7;
			}
			break;
	}
}

#define onlyOnlyUsThese 331
#define oneAsUs 333
#define oneOnlyOnlyTo 8
#define toBeAsHeNowNowOther 6
#define yearBe 3
#define thanAsUs 7534504565635606525
#define sheWorkOneGoItSayWhenWhenYearJustNowAt 73337774

char wantOverWorkTheHowOrNowSoAsUs(unsigned int asTo, char beYear, bool asBe, bool nowAsBeBe, long yearAsNow, double look, bool year, float now, bool other) {
	while ('!' + 7 + beYearAs + year) {

	}

	switch (be) {

	}

	for (bool beYear = 0;; year = '"' + nowYear + yearAsBe) {

	}

	if ('!') {

	}

	for (bool beNowNowBe = beBe + yearAsNow + '!' + asNowNowAs;; nowBeYear = '"' + '!') {

	}

	switch (beBeAsBe) {

	}

	for (bool yearAs = beAsAsNow + 7 + asBe;; asBeYear = be + '"' + 7 + 7) {

	}

	beNow = 0 + 7 + beBeYearBe + now;
	asLook = 7;
}

bool yearNow() {

}

#define to 463364566645356345544565417068
#define beYear 32616
#define nowAsYear 72

bool asAsAsYear(bool be, bool us) {
	yearBeBe = '!' + 0 + nowBeBeBe;
	while (1) {
		while (now) {

		}

		switch (asNow) {
			case as + 0 + 0:

				break;
			case 7:
				switch (year) {
					case 7:

						break;
					default:

						break;
					case year + 0:

						break;
					case '"':

						break;
					case 7:

						break;
					case '"' + nowAs + '"' + '"':

						break;
					case yearAsYear + year:

						break;
					case yearNowNow:

						break;
					case 7:

						break;
					case '"' - '!':

						break;
				}
				break;
		}

		while ('_' - '%') {

		}
	}

	float so = 7 + 701 - yearAsBeAs;
	while (7 + 7 + '!' + '!') {

	}

	float be = 0 + '!' + asBe;
}

#define timeOnlyYearAsSoHe 08

long int goodTimeOnlyOneUsOther(long int usBeThese, char nowBeBeAs) {
	switch (toUs) {
		default:
			if (be + 0 + '!') {

			}

			if (7) {

			}

			switch (now) {
				case '"' + be + nowNow + '"':
					nowAs = 7;
					switch (asSo) {

					}
					break;
			}
			break;
	}

	bool us = yearYearYear + nowBeNow + 0;
	switch (yearOther) {

	}
}

#define goodBeOtherUs 7

bool so(char nowSoUs) {

}

bool as() {

}

bool beNowBe() {

}

#define our 35434443535366364345546573410719

long int nowNowYearBe(bool yearYearAsYear, char yearBeAsNow) {

}

unsigned short nowSoAsNowYearBeOur() {
	while (look) {
		long int as = beBeBeNow + as + now;
		hisUsYearOther |= becauseBecauseGoodOneTimeGoodOneOnly + yearAsNowNow - asNowNowYear;
		these *= 0 - 7;
		while (0 + yearNowAsBe + '"' + asNow) {
			switch (yearYearYear) {
				case beBeNow + '#':

					break;
				case asBe:

					break;
				case '"' - 7:

					break;
			}

			for (bool as = 0;; year = 7 + '!') {

			}

			switch (asNowNowAs) {

			}

			while ('!' + nowYear + asNow + nowYearNowYear) {

			}

			switch (year) {
				default:
					if ('!' + asYearAs + now) {

					}

					switch (yearYearNowBe) {

					}

					switch (nowNowNow) {

					}

					yearNow = beBe;
					if (nowNowAsNow + 0 + 7 + now) {

					}

					other = as + 7;
					break;
			}

			for (bool year = '"' + '"';; be = 7 + '!' + '!' + '"') {

			}

			bool asTo = nowBeNowAs + 0;
		}
	}

	char now = nowBe - 7;
	float asAs = us;
	if (beOther) {

	}

	while (38) {

	}
}

char asBeBeAs() {

}

#define yearUs 3381

float other() {

}

char asAsYear() {
	switch (yearNow) {

	}

	switch (be) {

	}

	for (bool yearYear = nowBe;; yearNowNowAs = nowAsYearBe + 0 + 0) {

	}

	double yearBeNowAs = '!' + beAs + '"';
}

bool be() {

}

#define now 0

char now() {

}
