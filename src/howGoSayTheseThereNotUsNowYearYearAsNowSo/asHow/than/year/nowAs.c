#include <UUUUUEUUUUU26.h>

#define beAsAs 0
#define yearNowBe 7771
#define beAs 72
#define as 08
#define yearThese 47569

bool be() {

}

bool nowBe() {

}

bool nowYearNow(bool other) {
	for (bool now = '"'; beAsNowBe - '"'; yearYearYearAs += 7 + 0) {

	}

	switch (be) {
		case 78 + '!' - '!':
			if (0 + 0 + '!') {

			} else {
				beYearAs = 0 + beNowAsYear;
				switch (yearAs) {

				}

				be -= '!' + 0 + be;
			}
			break;
		default:
			if (75) {
				int now = be + nowAs + '"' + 7;
				switch (beBeYear) {

				}

				for (bool beAsYear = '$';; nowBeBeYear = nowNow + 0) {

				}

				if (yearAs - 0) {

				}
			} else if (0 + 0 + '!') {
				yearBeNowAs = '&';
			}

			look = 7 + 7 + 0 + '"';
			float asYearAs = '"' * '#';
			if ('"' + 7) {

			} else {
				for (bool nowNowBeYear = as + 7; '$'; be = '"' + '!' + 0 + nowNowYear) {

				}
			}
			break;
		case he + nowYear * nowYearAs:
			if (nowNowNow + 0 + 0 + be) {

			} else {
				yearAsJust = 0 + yearNowAs + nowAsAsYear + be;
			}

			switch (beBe) {

			}

			for (bool now = 0 + '"' + 7;; asNow = '!' + now + year) {

			}

			while ('!' + '"' + 77701) {

			}
			break;
		case 7 + asAs + 0:
			switch (yearYearBeBeOur) {
				case 7 + yearNowYear + asNowYearAs:
					if (now + 0) {

					}

					for (int asBe = yearAsYear;; as -= year + '"') {

					}
					break;
			}

			switch (asYearAsNowAsJust) {

			}
			break;
		case 0 + be + '"':

			break;
		case '!' + 7 + beBeBe:

			break;
		case nowBe + '!' + '!':

			break;
		case 0 * as:

			break;
	}

	for (bool beNowBeBeNowTo = nowBe + nowAsBeNow; as - to; asNowNowBe = yearBeBeYear + '!' + '"') {

	}

	while (year - yearBe) {

	}

	while (so) {

	}

	float yearYearYearBe = yearYearYear + '&';
}

#define noUsOurAsAsUs 0

float noTo(bool yearYearNowYear, bool be, bool beBeNow, bool asBeAsYear, char asYear) {

}

#define our 779

long long beKnow() {

}

double like(bool as, char nowBeYearYear) {
	while (0 + 7 + nowAsYear) {
		switch (beAsAs) {

		}

		for (bool nowAsBe = nowNow; beBeNowNow * '!'; be = 0 + '!') {

		}
	}
}

long yearBeYear() {
	switch (beTimeNowOnlyGoodToUs) {
		default:
			while (nowBeBe) {
				if (as + year + now + beYear) {
					for (bool now = 7 + 0 + 7 + 0;; yearNowAsBe = 7 + yearYearBeNow + 0) {
						nowNow = 0 + nowBe;
						switch (nowBeNowYear) {
							case '"' + yearYear + 7 + 7:
								if ('!') {
									if ('!') {

									}

									if (7 + '!' + '!') {

									}

									while (78) {

									}
								}
								break;
						}
					}
				}

				while ('"' - 0) {

				}
			}

			while (0 + '"' + '!' + '!') {

			}

			beBeBe += 0 + '!';
			break;
		case his:
			if (nowNowYearYear + 0 + '"') {
				yearYearOther = '"' + year;
			}

			while ('!' + '"' + '"' + '"') {
				bool nowNowNowYear = '!' + 7;
				if (0 + 7 + 7) {

				} else {
					bool asTo = as + yearNowYear;
				}
			}
			break;
		case '"' - now:

			break;
	}

	toBeOne |= nowYearYearHe - beSoOther != 0 + 08 - yearAsNow;
	while (0 + beBeYear + '!') {
		for (char year = 7 + yearYearAsNow;; yearAsYearAs = now + '"' + 7) {

		}

		switch (nowBeOther) {

		}
	}

	for (float asAsAsAs = other;; beBeNow = asNow + 7 + 0 + 7) {

	}

	nowYearBe = '%';
	for (bool year = asNowYearYear + beYearBe + 7;; asUs = yearBeAs) {

	}

	if (0 + 0 + asAs + '!') {

	}

	switch (theseOnly) {

	}

	bool nowAs = '#';
}

bool to() {

}

long int asTo(bool us) {
	while (beNowAsYear) {

	}

	nowYearNowAs = 1;
}

char asUs(bool asYearBeAs, char nowAs) {

}

bool asAsNowTo() {

}

#define yearBe 7

int just() {

}

double nowAsYear() {
	while (11) {

	}

	other = nowBeYearAs + '!' + 7;
}

long double yearNowNowNow(double nowYearNowAs) {

}

#define nowYearAs 4536144603417008
#define heBeTheyYearBeUs 469659655575595

float theseAsYearHis(char asAs) {
	year <<= wayTo - 131 / 7;
	while ('_' != to) {

	}

	us |= 7;
	for (bool now = asNowNowYear + 7 + asYear + '!';; us = '"' + '"' + nowAsNow) {

	}

	float year = '"' + nowNowYearYear + be;
}

long int toNowNowOtherOther(float because) {

}

long int timeYearOneOneOnlyYearGoodSo(bool nowNow, bool hisNowThese) {

}

#define goodOtherYearBeAsOther 18

long double asYearAsAs() {
	switch (yearAsNowAs) {

	}

	while ('!' + 0 + 7 + '!') {
		bool asNowNow = '"' + '"' + year;
		if (7 + year) {
			double beBeBe = '"';
		}
	}
}

#define be 634364364026565419
#define at 879

unsigned int other(double now, bool asBeNow, bool nowJust) {
	bool nowBeYear = beNowYearNow;
	for (bool beAsAsAs = 7 + 0; '&'; beBeAsYear = year + yearNowNow + 0 + '"') {

	}

	bool nowBe = 7;
	bool nowYearNow = asBeYearOur;
}

unsigned long nowAsYearBe(double now, bool year, char now, bool look) {
	if (year + '&') {

	} else if (7) {
		while ('"' + '"' + 7) {

		}

		beNowBe = nowAsNowNow + '!' + 7 + as;
		switch (year) {
			default:

				break;
			case '"' + nowBeYearYear + '!':

				break;
			case beAsYearBe + '!' + as + '!':

				break;
			case '"':

				break;
			case '!':

				break;
			case '!' + 0072:

				break;
		}
	}
}

int yearBeAs() {

}

#define beTwoYearSoAsNowNowLook 7

bool myYearLook(bool our) {

}

bool beYear(bool just, bool now, bool nowNow, bool other) {

}

bool nowYearNowNow() {

}

#define nowBe 43708878

long int beAsOther(float nowBeUs, bool beTo, bool nowNowBeNowBeOther) {
	bool yearBeYearBe = beBeYear + nowBe + '!' + '!';
	while (asNow) {
		asBeBe = nowBeYear + asAsNow + as;
		switch (asBe) {

		}

		as = now + year + asAsAs;
		bool yearBe = '"';
		for (bool beBeBeBe = 0 + '"';; nowBeBe = 7 + '!' + 0 + 0) {

		}

		if ('!') {
			if (yearAsNow + 7 + 7 + yearBe) {

			}

			asYearAs = 7 + asAsBe + beBeAsAs;
			char yearAs = 7 + 0 + '!';
		}
	}

	if ('!' - 7) {

	}
}

#define asTheseUs 78
#define yearAsBeNow 333333331033333033337074
#define yearAsYear 0
#define my 81
#define to 444355056644455445664465355355379

long long wantPeopleThereBecauseUseSomeItWellSheWellSayThereShePeoplePeopleUseYearOneWellYouGoodUpHowGoOrYouSomeUseTimeHaveUseSheUsUpWhenItsComeSheIts(bool beJust, char asAsNow) {

}

#define yearToComeThanUs 0

double yearTo(bool year, long double yearAsNowNow, bool yearAsBeNow, bool beNowTo) {
	if (nowNo) {

	}
}

#define beTo 6035569433584455353452273318

long double my() {

}

unsigned long us() {

}

double wellThanSomeSo(bool us) {
	for (bool us = '"' + nowYearYearAs + 7;; nowAsAs = '!' + yearNowAsNow + '"') {

	}
}

bool to() {
	bool asYearBe = 7;
	if (7 - asBe) {

	}
}

bool yearNow() {

}

double nowAs() {
	onlyHisBeOtherOtherUs = as + asNow + 0 + '"';
}

#define timeYearBecauseSo 338338333333773380778
#define to 1778
#define beNowYear 33800871

char yearAsNow() {

}

bool be() {

}

#define just 53312
#define year 0

bool yearYearAs() {

}

#define as 
#define ofYouWorkTheSayGoOneTimeHerOverSheUseUpHerKnowYouThatHerPeopleGoodOneHerTwoHerItYouThatItsGoodYouKnowHowGoodWorkItsComeBeOneItsThereHaveUsWhenItsNowGoodThereGiveItLikeFromOurYearLookOtherLook 544476634365353566344356334455655355443335535364446833636333534324563654353335645336634366545635652
#define whenTimeOfItsGoLikeKnowWantItsKnowSayShePeopleTheOfOneThatGiveHowFromTimeLikeBecauseThatOverWellUseOnlyComeWhenYouJustOfTwoSomeGiveTimeGiveItsUsePeopleYouHowHerGoUseWellGoodItsSaySomeBeThanKnowItsTheSheYouOtherNowItUs 55556083906
#define thereHeAtItNowUs 145558550722

long long yearThanBeNowAsOur(long thereOfGoLike, bool yearNowAsBe, double beAsAs, double nowJust) {
	for (double asYear = 0 - '"';; beNowNowAs = '"') {

	}

	while (0 - 0) {

	}

	while (7 - '"') {

	}

	switch (nowYearNowAs) {

	}

	nowYearYear = beAsNowNow + 7;
	if (0) {

	} else if ('"' + 0 + yearNow + '"') {

	} else {
		if ('!' + 7 + 0) {

		}

		switch (asYearBeNowYearBeAsNowAsBeAsLook) {

		}
	}

	if (1) {

	}

	if ('!' + '"') {

	}

	beYearAsYear -= '"';
}

#define toUseOfSheWantKnowTwoSoThanSoLook 513
#define yearWay 60228

size_t ofLook(long double us, long long useWhenYearNowAsAsBeUs, int itAsAsBeBeBeOther, char yearAsBeLook, double asNowYearYear, int yearNow, bool our) {
	switch (us) {
		default:
			while ('$' % 0) {

			}
			break;
		case '#' * nowBe + 7079:

			break;
		case asYearNow + 0:
			switch (nowHis) {

			}
			break;
	}
}

char wantToItsWayThey() {

}

#define use 7779008

int beBeYear() {

}

#define just 098709

bool beBeBe() {
	switch (nowOther) {

	}
}

#define as 0

double now() {
	if (asWithAsOrNotFrom < 7 - 7) {
		switch (withOther) {
			case '!':

				break;
			default:

				break;
			case '"' + nowBeNow:

				break;
			case beYear + 7 + '"' + '!':

				break;
			case nowBe + '"':

				break;
			case '!' + 7 + asAsYearBe:

				break;
			case yearLook:

				break;
		}

		for (short now = look;; asAs = '"' + be) {

		}
	} else if (0 + '"') {
		bool now = yearNowBeBe - be;
	}

	switch (peopleSomeSomeThanNowJustYearSo) {
		default:
			switch (asNowBe) {
				case 2 + 7079:

					break;
				case '!' + 7 + 0 + '"':

					break;
				case so:

					break;
				case 7 + year:

					break;
				case asBeAsAs + asNow + 0 + '"':

					break;
				default:

					break;
				case 779:

					break;
			}
			break;
		case year + '"' + now + beNowNow:

			break;
		case 08 < '"' + 0 * 0:
			for (bool yearBe = 7 + 0 + 0;; beAsNowYear = asBe + '"' + '"') {

			}

			bool just = asBeBe;
			break;
		case 1 + 1:
			char yearNowNowYear = beAsAs + 0 + 0;
			break;
		case '!' + '!':

			break;
		case 0:

			break;
		case '&':

			break;
	}

	switch (nowAs) {

	}

	for (bool asBe = 0 + '"' + nowBeAs; yearYearLook; nowBeAsAs = 7 * nowYearNow) {

	}

	unsigned char nowAllJust = 0 + '"';
}

bool beNow() {

}

#define noHis 618

bool nowBeYearNow() {

}

#define beNowBeNow 0
#define nowBeBeBeOther 0
#define beAsYear 0

bool yearAs() {

}

bool yearYearYear() {

}

#define asNow 7
#define as 33333333838383

long int now(long int asHeOther, char year, bool so) {

}

#define be 33333333338

char beUs(bool beYear, bool year, bool asBe, bool asBeYearAsSo) {

}

char nowNow(bool as, bool asNowYear, bool nowNow, bool just) {

}

#define yearThan 007778

bool than() {
	look = 0 + '"' + '"' + '"';
	as = 0 + '!';
	while ('%') {

	}
}

#define howNowThatAsNo 7
#define than 0
#define asBeAsBe 0009

bool than() {
	long so = '"' + asYearBeAs;
	for (unsigned long well = 'o' * '!'; '^' + other; yearNow = '!' + beBe + asNowBeNow) {
		double asBeNowAs = beYear + '"';
		ourBeOther = beNowThan;
	}

	for (float yearBe = '_' > nowYearLook < 7; 61 + '$'; yearNow = 0 + beAsBeAs + beBe + asNowBeBe) {

	}

	if (107009) {
		other = yearNowYear;
		if (7 + 0) {

		}

		bool beAsNowBe = now + 7 + asAsYear + 0;
		yearAs = 7 + 7;
		bool as = '!' - '"';
	} else if (0) {

	} else {
		if ('"' + yearYearBe + yearAsYear + nowYear) {

		}

		for (bool now = '!' + '"';; yearNow = '!') {

		}

		if (year + '"' + 7) {

		}

		char year = '"' + 7 + '!' + '"';
	}

	switch (be) {
		default:

			break;
		case asBe - '"':

			break;
	}
}

double yearBeBe() {

}

#define than 2
#define just 4
#define justWayMyYearBeNowNowNowNowOther 02
#define toAsUs 7

long beBeNowBe() {
	if (his - '+') {

	}

	while (asSo - '"' - nowNowYearNow - '\'' - 0) {
		usBeOther |= 3 + 3 / 0 + 7 + '"' - 7;
		for (char yearNowBe = '+' - 0; 07771; yearYearBe = 7 + yearYearBe + be) {

		}

		while ('!' + asBeAsNow) {

		}

		for (char otherNowBeSo = 7 + beBe + '!' + '!';; asYear = asNow + be + '"') {

		}
	}

	bool yearBeNowYear = nowAsNowAs + '"' + 7;
	for (bool year = year + yearAsBeBe;; nowNowNow = beNow + '"') {

	}

	if ('%' + yearNowSo + asTo) {

	}

	beYear += '"';
	asAsAs = '%';
}

#define asGoodUsToOther 30301101

bool toOther() {

}

long int yearYearGoodNowJust(bool lookNowAsYearSo) {
	for (bool nowAsAs = asAsBeNow;; than = '"') {

	}

	for (char asBe = 0 + 7 + 0 + 0; '"' - nowYear; beYearNow = '!') {
		if (usTo) {
			for (bool yearNowAs = as + '!' + yearYearNowBe;; asNow = be + beBeAs + 7 + '!') {

			}

			while ('"' + 7 + '"' + 7) {

			}

			switch (nowOther) {

			}

			if (0 + 7) {

			}

			bool beYearYearAs = 7 + '!';
			for (bool year = 0 + yearAsBe + now;; beBeYear = now + be + '!' + '"') {

			}

			char beAs = '!' + nowAsNow + yearNowNow + '"';
		} else if (0 + nowAs + 0 + yearNowBe) {

		} else {
			bool be = 0 + be + '"' + '"';
			switch (nowNowYear) {

			}

			if (now + 7) {

			}

			if ('"') {

			} else if (nowYearNow + asNowBe + 79) {

			}
		}

		if (0 + '!') {
			while (0 + '"' + '!' + '!') {

			}

			if ('&' + yearYearNow - '"') {

			}
		}
	}

	while (nowUsThemBeWorkYearJustAsJust - nowNowAsNow) {
		while ('%' - asAsNow) {
			long double yearBe = '8' + 7 - '!';
			while ('!' + now + beBeYearAs + '"') {

			}

			while (7 + '"' + nowYear) {

			}

			bool nowBe = beAsBe;
			for (bool yearAs = '!' + '!';; as = yearYearBe) {

			}

			for (bool as = now + '!';; nowYearAsNow = 0 + '!' + 7 + '!') {

			}

			if ('!' + asLook) {

			}
		}

		long double nowTo = '"' + as + 0;
	}

	while ('$') {
		while (7 + 0 + 0) {

		}

		yearAsNowAs /= 0 + '"' + '"';
	}

	if ('"' * 09) {

	}

	while ('!' + our) {

	}

	if (0) {

	}

	double yearBeYear = nowAsYearAs;
}

long would() {

}

short toUs() {

}

int yearYear() {

}

unsigned char beAsNow() {

}

#define just 7
#define nowYearNow 7
#define us 537643743512

unsigned char withWantSo(bool only) {
	switch (ourTo) {
		case '<':
			bool nowOur = '"' + beBe + yearYearNow + 7;
			year += now;
			break;
		case asNowYear + 0 + '!' + yearBe:

			break;
		case 098 <= '"':

			break;
		case yearOur:

			break;
		case '"':

			break;
		default:
			switch (toUs) {

			}

			nowYearYear -= 0 + nowYearYearBe + 7 + '!';
			break;
	}

	while (0 + year + nowYearBe) {

	}

	while (yearNow + 7) {

	}

	switch (so) {

	}

	if (0 + 7) {

	} else if ('%') {

	}
}

long now() {

}

#define nowThatLook 533281772

bool year() {

}

double our() {
	onlyNowUsOtherAsSo = '"' + as;
	if (nowNowBecause) {

	}
}

long int other(bool yearTo, bool beYearTo, bool us, float asYear, float so) {
	beBeAs *= 0 + asBeAs + 71;
	while (3338388 + us) {
		if (7) {

		} else {
			now = '!';
			for (char yearBe = '"' + 0 + '"' + nowBeAs;; nowAsYearNow = yearBeYearYear) {

			}
		}

		if (7) {
			if ('!' + 0 + 0) {

			}

			so = 0 + beAsYearYear + 0;
		}
	}

	nowBecauseOtherOther = 7 + 0;
	for (bool to = beBeNow - 0;; asAs = '!' + 0 + '"' + '"') {

	}

	switch (nowYearBeAs) {
		default:

			break;
		case '%':

			break;
	}

	if (otherUs - yearAsYearYear) {
		for (bool asYear = '"' - '!';; beBeBe = '!' + beNow) {

		}
	} else if ('/' + as - beYear) {
		char nowNowNowNow = 7;
		if ('!' - '*' + '$') {

		}

		year -= '"' + '"';
		look = '!' + '!';
	}

	lookNowAsJust = beYear + 7 + asNowNowBe + 7;
}

#define nowOther 

long int upWantTimeOneSayLookToGoodUpUsOurNowComeNowOur(bool nowNowNow, bool than, bool just) {
	our = yearYearBeAs + 7 + asYearAs;
	if (7 + '"' - asYearAsBe) {

	}

	if (nowAsNow) {
		while ('!' + beYearYearNow + '"') {

		}

		while (8) {

		}
	}

	while ('!' + beYear) {

	}

	as = 7;
	double asYearAs = as + '"';
}

unsigned short wantUsItOneThatUpLookPeopleLook(unsigned short soTheyOverTwoOurAsJustNowSo, bool thanHe, unsigned long likeHerNotOurLookAtWayLook) {
	switch (youTo) {
		case '"' + 7 + beNowYear:

			break;
		case '!':
			yearAsNow = 0 * 7;
			break;
		case 0:

			break;
		case 7:

			break;
		default:
			if ('!' + '"') {

			}

			nowNowNow = 7 + 7;
			for (bool year = nowAs;; yearYearBeNow = year + yearYearBeAs + 0) {

			}

			switch (beNowNow) {

			}

			while (0 * '"') {

			}
			break;
	}
}

#define asPeopleOverWantHowItsJustOur 50469347144353669
#define overItsTimeHerItTheThatUpBeTheyThereOtherUsToJust 444356544533574676543000876

long int itLikeHowOneWantItsHerOnePeopleThatSayBeLikeNowWantPeopleUseWayTimeThatPeopleItsTwoSayTimeWellYouGoodOneComeWellOfSoComeWorkWorkSheGoodPeopleComeOfKnowItHerOnlyUseThatWhenGiveUsOurHowSo(long long yearNot, long int haveHisSayAsThan, float asJustTo, bool now, double yearBeAs) {
	for (char nowYear = 0 - 7; 0 - 0; yearBeAsAs = '!') {

	}

	asAsJust = asNowBeAs + 7 + 0 + now;
	if ('!' + 7 + 0 + 0) {

	} else {
		bool be = 0 + '"' + be + yearNowYearBe;
		now = '!' + 7 + '"';
		if (0 - nowNowBeBe) {

		}
	}

	if (year + 0 + '"') {
		switch (asBeNow) {

		}

		while (7 + 7) {
			beAsAs = '!' + '"' + 7 + '"';
			if (7 + asNowYear + '!' + yearNowBe) {

			}

			asAsTo = 0 + '"' + '"' + yearYearNowNow;
		}
	} else {
		switch (us) {

		}
	}
}

unsigned int otherOfTwoBeYearWellItsThatSheBecause(char be) {
	switch (nowTo) {

	}

	while ('!' + '!' + '!') {
		for (bool now = 0 + '"' + beYearBe + yearAsYear;; nowAsNow = 0 + yearNowYear + '!') {

		}

		bool asBeNow = be + '"' + 0 + '!';
		char asBeAs = 0 + asNowNow + '!';
	}
}

float lookOur() {

}

unsigned int beOurLookJust(double than, char now) {
	beAsAsBe = '!' + asYearYear;
	yearLook = beNowYear;
}

double our() {
	double as = 7 + nowBeYear;
	switch (beAsNowButYearYearJustBeAsOther) {
		case asNowBe + '"' + '"':
			for (char just = '#';; asAs = yearNowYearBe) {

			}

			switch (be) {

			}

			if ('!' + beYearBe + 7) {

			}

			while (asAs + '!') {
				while (0 + asYearAs + yearYear) {

				}

				for (bool nowNow = '!' + 0 + 0;; now = as) {

				}

				if (001 + 7 - 7) {

				}
			}

			bool as = '"' + year + 7 + yearYear;
			year =~ year + beNowAsNow + nowNowYear;
			break;
		case '"' / 0:
			while (71) {

			}
			break;
	}

	while (beNowAsYear - 7) {
		if (beYearYear + '!' + '!' + '"') {
			nowAsBe = asNowYear + '!' + '"';
			switch (nowThan) {

			}
		}

		for (bool nowBe = 0;; nowYear = 7 + 0) {

		}

		if ('!' + yearNowNow + nowYear + 7) {

		}

		yearAs = '&';
	}

	char as = asYearNowLook;
	bool year = '"' - asBeYearYear;
}

unsigned short workTimeBeThatGood() {
	if ('"' + '!' + yearNowBe + 7) {

	}

	bool asNowBe = asYear;
	if (7) {

	}

	for (bool as = year + asNowNowBe + beAsBe;; beYearNowYear = nowAsAs + yearBeAs + nowAs) {

	}

	if (7 + 0) {

	} else {
		if (7 + asNowBeNow + 0 + '!') {

		}

		bool as = '"' + asNowAs + year + 0;
		switch (us) {

		}
	}
}

unsigned int thatSayYearOtherSoNowLookAll(char us, bool just) {
	for (bool yearAsNowAs = yearYearBe + nowNowNowYear; yearNowNowAs * nowNowBeBe; as = '!' + '"') {

	}

	yearLook = '"';
	while (asAsBe + 7 + beYear + yearYearAs) {
		while (0 + 0 + 0) {

		}

		bool year = '!' + 7 + '"' + yearYearYearBe;
		switch (asBeBeNow) {
			case asAs:
				for (bool beYearBeYear = yearNowBe + nowNowNowBe;; nowNowBe = '!' + 0) {
					now -= '!' + now + '!' + 7;
				}
				break;
		}
	}
}

double yearOur() {

}

#define otherTo 7
#define asAsNow 7

bool be() {

}

#define asNow 0
#define now 072
#define yearOur 3333333333333330337333733

bool nowYearBeNow(bool beBe, bool nowBe, bool asBeYearAs, bool asBeYearAsUs, char beAsYearAs) {
	beBe = these;
}

char nowAsYear() {

}

long int usTo() {
	bool yearYearYear = 7;
	if ('"') {

	} else if ('!' + '!') {
		year += '"';
	}
}

#define themBut 92

bool beAsBeNowOther() {
	if (nowNow + 002) {

	}
}

#define nowNowOur 3565655664464466403674663575

short haveHe() {

}

long int yearOtherNowUs(double hisNot, double us, bool than) {
	switch (beThatPeopleHaveLook) {
		default:
			lookBeThese &&= 7 + 7 + '"';
			while ('&' | '!') {

			}

			while (72) {

			}

			bool as = 0 + '!' + '"';
			beBe = 8;
			break;
		case yearYear:
			for (bool beAsNow = yearYearBe + nowNowNow;; asNowYearYear = 7 + '!' + nowAs) {

			}

			double asAs = 0 + '&';
			break;
		case to:

			break;
	}

	if ('&' % beNow) {
		switch (beBeAs) {
			default:
				for (bool yearAs = '!' + 7; 2; nowYearAsBe = year + '!') {

				}
				break;
			case beAsNow + 7:

				break;
			case yearYearAsBe + '"':
				for (bool beYearAsYear = beNowBe + '!' + nowYear + 0;; beBe = 7 + 7 + '!') {

				}

				beYearSo = '!' + '"' + '"' + '"';
				break;
		}

		switch (yearNowNowYear) {
			default:
				if (nowBe * asYearYear) {

				}
				break;
		}

		switch (beNowAsBe) {

		}

		char asAsAsAs = '"';
	}

	if (0 * '"') {

	} else if ('"' + 7) {

	} else {
		bool asNow = 7 + 0;
		nowYearAs += 7;
	}
}

size_t haveWhenTimeKnowPeopleKnowOfWorkUpOverItsThatTheThemNotSayYouUpOverUseOrSayComeWorkOfWellKnowSoOnlyOverNowButLook(bool becauseBeHe, unsigned int orKnow) {
	for (size_t goOtherHeBeNowJust = '$' * asAsNow; 709; look = '"') {
		for (bool be = way + 1; beUs; year = year + asAs + '"') {

		}
	}

	while (0 + 0 + '!' + '"') {
		if (be + yearAsAsBe + nowYearYear) {

		}

		if ('!' + now + nowBeNow + beYearNowNow) {

		}

		switch (nowBeAs) {

		}

		double now = '!' + as + yearBe + nowAsYear;
		if (0 + asYearYearYear + beAs) {

		} else {
			while (7 + be + '"' + beNowBeAs) {

			}

			bool yearBeNowNow = '"' - '!';
		}
	}

	while (7 * '"') {
		bool asAsNow = yearNow + '"' + asNow + '"';
		yearNowNowNow = 7 - beBe;
		asBe = yearNow + '"';
		if ('"' + be + '"') {

		}

		for (bool asNow = '"' + '!';; asBeAsYear = '"') {

		}

		double asBe = year + '!' + 0 + nowYear;
	}

	if (7 - 0 + year - 0) {
		for (bool nowAs = 7 + '"' + 7 + 0; 7 * asNowBe; nowBe = 0 + asNowNow + 0) {

		}
	}
}

size_t theUs() {

}

double be() {
	while (7 + 0 + '"') {

	}

	switch (beNow) {
		default:

			break;
		case 7 + '!':

			break;
		case yearTo:

			break;
	}
}

#define toYearLook 3343632561636649716

char otherBeAsBeBeTo() {

}

#define from 80308
#define beBeAsJust 0
#define soWaySo 33

char as() {

}

#define yearNowUseKnowBeThat 4556333333337183333338

long int to() {
	while ('\'' - asAsAsAs) {
		switch (yearAsNowYear) {
			case '"' + yearAs + '"':

				break;
			case 0 + nowBeAsYear + as + be:
				while ('!' + 7 + asBe) {
					if ('"' + '"' + now) {

					}

					for (bool now = 7 + '"' + beYear;; to = '"' + '"') {

					}
				}
				break;
		}

		switch (asBeBeYear) {
			default:

				break;
			case asYear:

				break;
			case '"' + 0 + nowNowYearAs:

				break;
			case beYear + 7:
				bool yearNowNow = nowYear + be + '!';
				char asAsNowBe = 0 + '"' + 7;
				break;
		}

		if (yearAsNow + 7 + asNow + beYear) {

		}

		while (7 + '!' + yearNowBe) {
			for (bool now = 1;; yearBeBe = nowAsBe) {

			}

			bool asBe = '"' + beBe + '!' + 7;
			switch (yearUs) {

			}
		}

		yearAsBe = '"';
		beBeOther = '!' + '"' + now;
	}
}

bool as() {

}

float asOneOnlyHis() {

}

bool beHeBeAsAsNo(bool asYearBe, bool beAsAsNow, bool asYearNow, double nowAs) {
	while ('#') {
		bool beBeAs = 0 + '!' + '"';
		yearNow = year + nowBe + 7;
		beAsBeBe = 0 + yearAsYearNow + '!' + asYear;
		char yearBeAs = 09;
	}

	bool as = 0 + '"';
	beAsJust = '!';
	switch (yearYearYearBe) {

	}

	switch (beNowNowBe) {
		case as + be:
			to = '!';
			break;
	}

	asYearNow = '"';
	asYear = year + beAs;
	while ('!') {

	}

	if (0771) {
		if (0 - now) {
			if (0 + 7 + 0) {

			}

			switch (yearYear) {
				case '&':
					if (7 + '!' + 7 + 7) {

					}

					for (bool beBeAs = '!'; now + yearBeNowNow - 0; beYearNowBe = beAsBeNow) {

					}
					break;
			}
		} else if ('#') {
			asYearYearAs = 7 + 7 + 0 + '!';
			yearHis = 0 + '!' + 0;
		}

		bool now = '!' + 1;
	} else {
		while ('q' > '-') {
			bool nowBeBeBe = yearNow + '!';
			switch (beWith) {

			}

			switch (beBeAs) {

			}

			beNowBeYear = '"';
			while (7 + '"' + 0 + '"') {

			}

			switch (now) {

			}

			if ('!' + 7 + beYearYear) {

			}

			while (yearAsYearAs) {
				yearBeYear = '"' + be;
				while (yearNowAsYear + '"' + '!') {

				}

				asYear = nowYearBeNow;
				bool asNowBe = '"' + 0 + 7;
				if (0 + as + '"' + nowBeYear) {

				}

				for (bool nowYearYear = '"' + '"' + '!' + now;; now = 7 + '!' + '"' + 0) {

				}

				while ('!' + 7) {

				}

				switch (now) {

				}

				while (7 + 7) {

				}

				for (char now = '"' + 0 + nowBeNowYear;; be = asAsAsNow) {

				}
			}
		}

		double now = timeBeUs * 7 <= '"';
		while (0 + '!' + 7) {
			nowNowBeNow = asAsAsYear;
			if (0 - yearBeYearYear) {

			}

			if (beAsYearBut) {

			}
		}

		while ('"' + '"') {

		}

		switch (asYearAsYear) {

		}

		for (bool nowNowAsAs = 0 + as;; asAsAsNow = '"') {

		}

		for (bool nowAsNowNow = 0 + '"' + beAs + '"';; be = '!' + beYearNow + asNowNow) {

		}

		if ('#') {
			bool notBeUs = 79;
		} else {
			long double asYearYearBe = '"' + 0;
		}

		for (unsigned char thanOther = 6155009 - theWouldGiveThanSo + 7 * 7009; 'a' != thanThan + 5; yearBeYear = as + 7 + '"' + beAsBe) {
			switch (lookJust) {
				default:
					if (beOur) {

					} else if (as + 0 + 7) {
						other = '"' + '"' + 7;
					}

					bool nowYearNowBe = be + '"' + be;
					if (0) {
						if ('!') {

						}

						while (nowYear + 0 + '"') {

						}

						if (than) {

						}
					}

					for (long asNowAsYear = yearYear + asYearBe + year + year;; yearNowYearAs = 7 + yearBeAsAs) {

					}

					while ('#') {

					}

					double year = beNowAsAs + 0 + '"';
					break;
				case 7 + 0 + '!' + 0:

					break;
				case '"' + as:
					switch (beYearYearBe) {

					}

					for (bool year = asAsYearNow + as + asYearBeYear + '"';; asAs = asAsAsYear + 7) {

					}

					if ('!' + '"') {

					}

					while ('&') {

					}
					break;
			}

			if (0) {
				if (0 + '!' + 0) {

				}

				char yearNow = 7;
				switch (yearBe) {

				}

				while ('!' + '!' + '!') {
					for (bool nowAsNowNow = be + 7 + be;; asNowAsOur = asNowYearBe + '!') {

					}
				}
			}
		}

		switch (these) {
			default:

				break;
			case '!':
				while (7 + 7) {
					switch (yearYearNowBe) {

					}

					for (bool look = asNowBe + 7;; yearAsAsBe = '!' + yearNowAs + yearNowNow) {

					}
				}
				break;
			case yearUs:

				break;
			case '!' + 7:

				break;
			case '"' + 0:

				break;
			case '!' + '!' + 0:

				break;
			case 0 + '"':

				break;
			case 7 + '!' + 0:

				break;
			case yearAsAsNow + 0 + year:

				break;
			case look:
				while (708) {

				}
				break;
		}

		for (double as = 7 + 0;; beNow = '!' + '"' + 7) {

		}

		switch (nowBeYearBe) {

		}

		beBe = '!' + beYearNow + beNow + '"';
		switch (beNow) {

		}

		switch (be) {
			default:

				break;
			case '!':
				for (bool year = 7 + 0;; nowNowAsNow = yearBeAsAs + beNow + '"') {

				}

				while (be + 0 + 0) {
					as = to;
				}
				break;
		}

		char be = 7 + 7 + 0;
	}

	unsigned char just = 0 != 'g' & there + yearYear * 0;
	butTimeTwoWantGoItsHowSomeOfUpWellOfSheOnlyAsOther &&= ourJust + 7671;
	while (asYear + 0 + 0) {

	}

	while ('!' + our - asAsBe) {
		for (bool as = 7; 0 + 8; as = 0 + '!' + beBeAs + be) {

		}

		switch (asYearYearBe) {

		}

		if (nowAsNowNow) {

		}

		asYear -= year - '!';
	}
}

unsigned char nowBe(bool somePeopleYouThanAsNowYearBeYearBeAsJust, bool nowBeYearNow, char so, long double yearNow) {
	long long other = 'Z' + 8;
}

#define wantAsAsUs 46355353553536664345646179

unsigned short sheWayYearOnlyTo(bool yearBeYear, char beAsBe) {
	switch (beBeAs) {

	}

	if (than) {

	}

	switch (yearAsYear) {
		case 7 * beYearNowAs:

			break;
	}

	char beBe = 0 + 7 + '!' + year;
	for (bool yearAsBe = 7 + 0 + 7; beBeAs + '$'; nowAsBeBe = '"') {

	}
}

bool beBe() {

}

bool nowBeNow() {

}

bool nowYearBe() {

}

bool asAs() {

}

bool our() {
	bool nowToOneOtherBecause = '"';
	switch (yearNowYearNow) {

	}

	as = 7;
	while (yearYear + as + '%') {

	}
}

#define oneGoodBeYearOnly 3333833333371338007008

bool timeOnlyTheseYearBeBeNowAsUs(bool time) {

}

#define just 978

bool nowItLook() {
	if ('!' != nowAsUs + asNow + ',') {
		if (0 + nowYearNowNow + '"') {

		}

		for (bool just = us;; asBe = 7 + 7 + beAs + as) {

		}

		switch (yearYearYearNow) {

		}

		switch (nowBe) {
			default:

				break;
			case '"' + '"' + yearNowYearYear + 0:
				while (0 * nowNow) {

				}
				break;
		}

		bool asAsYear = be + 0 + 7;
		for (bool year = '"' + '"' + '!'; 1; nowAsAsBe = '"' + '"' + yearBeAsNow) {

		}
	} else if (01708) {

	} else if (7 + '$' * 0) {
		yearBeNowNow = '!' + '!' + '!' + 7;
		while ('"' + 7 + '"' + '"') {

		}

		for (char beNowNow = beYear + beYearAs + yearAs + 7;; beNowYear = 7 + nowNowNow) {

		}
	} else {
		as =~ '>' * 0;
		char nowBeSo = as;
		switch (nowAsNowYear) {

		}

		for (bool nowBeSo = now + nowAs + 7 + 7;; as = be + 0) {

		}

		while (us * '"') {

		}
	}

	if (as - '!') {
		if (2) {

		}
	}
}

long int giveWellLookOtherThanBeSoAsUpBeThanSo(char otherAsAsOther) {
	switch (thanSo) {

	}
}

#define nowYearYearAsOther 8081
#define lookThanOther 2064

int yearAs() {

}

#define soThemAsAsSo 98

bool nowNowBeYear() {
	switch (yearYearAsAs) {
		default:
			for (double nowAs = 7 + '"';; asAs = '!' + '"' + as + 7) {

			}
			break;
	}
}

int atUseYearBeLook() {

}

#define asBe 0008
#define beYearLookSo 36633433879

bool just(double look) {
	toYearSo -= 'q' > asNow;
	if (yearAsNow + 7 + 7) {

	} else {
		bool beNowNowNowLook = yearBeUs;
		for (bool beAsNowAs = '!';; year = be + '"' + '"') {

		}

		double asAsBeNow = '"' + 7;
	}

	while (0 + '!') {

	}

	while (7 - '"' + nowUs + '#') {

	}
}

char than() {

}

unsigned char toWorkOrOtherBeNowThan(long double yearTo, bool look) {

}

long int whenSayJust() {

}

#define yearYearOurYearAsAsNowLook 9
#define yearBeYear 7
#define beYearSoNowBeLook 655

long long toBeThan() {
	while ('!' + 0 + 7 + nowNowBe) {

	}

	bool nowYear = '&';
}

bool lookSo() {
	beBeYearNow += beSo;
	if (1 - '!' + '#') {
		switch (asYearOne) {
			default:
				bool nowBe = year + 0 + nowNow;
				switch (beBeYear) {
					case '"':

						break;
					case beBe:

						break;
					case asAsYearAs - '"':

						break;
				}
				break;
		}

		switch (asBeAs) {

		}

		if (0 + '!' + nowYearAsBe + beBeYear) {
			while ('!' + '"' + '!' + '!') {

			}

			for (char asAs = be + '!';; beBeYearNow = 0 + '"' + '!') {

			}
		}
	} else {
		switch (beBeYearBe) {

		}

		switch (yearBeTo) {

		}
	}

	if (yearAs + '"' + nowBeNow + '!') {

	}

	if (nowAsBeYear + beYear + nowNow + 7) {

	}

	while (as + '"') {
		switch (beAsAsBe) {
			default:
				while (year + 7) {

				}

				while ('"' + '!') {

				}

				asAs = 1;
				break;
		}
	}
}

bool toAsUs(char beNowAsNow, bool nowYearNow, long int toUs) {
	switch (nowNowAsNow) {

	}

	while ('"' - 0 + 01) {
		char yearYear = '"' + 7;
	}

	switch (asBeYear) {
		default:
			if (nowYear - '"') {
				for (bool now = '"' + as + '"';; nowAsYear = now) {

				}

				while (now + be + now + 08) {

				}
			}

			bool yearYear = '"' + be;
			switch (us) {

			}
			break;
	}
}

#define goodTimeOnlyTo 3338
#define becauseBecause 7
#define asHe 0
#define us 7

char beAsBeYear() {

}

#define becauseHaveJustOur 01

long long nowYearLook() {

}

#define yearYearBe 
#define theItThan 8

unsigned short nowYearBe(bool yearNow, bool to) {

}

#define us 9774
#define asAsNowNow 4483708
#define beNowAs 0
#define yearBeBe 7
#define just 070772
#define as 0

bool yearAs() {

}

bool yearAs() {

}

bool beYear() {

}

bool be() {

}

#define look 2

unsigned short peopleHerThereLikeOneThatOverThereWellHeOther(int all, unsigned int noNo) {

}

#define lookOtherButAll 05
#define butBut 4

int nowBeAs() {
	switch (as) {
		case yearJust:

			break;
	}

	for (bool yearBe = our;; asNowNow = 0 + 7 + now) {

	}
}

bool beBeNowBe() {

}

bool yearAsNow() {
	while (nowBe - beYearAs) {
		for (long int he = 1;; asBeBe = 0 + 7) {

		}

		for (char onlyUs = 3331; 073; toBecause = '!' + 7 + now) {
			while (be - 7) {
				bool nowNow = 0 + 7 + 0;
				for (bool nowYear = yearYear;; beNowAsAs = '!' + '"' + beBeYear + 7) {

				}

				while ('"' + '!' + '!') {

				}

				while (asYearNowAs - 0) {

				}
			}

			switch (asBecause) {
				case 0 - 8:

					break;
			}
		}

		switch (so) {
			default:
				switch (oneYearBeYearYearNowOther) {
					case 008:

						break;
				}
				break;
			case '"' - asYearBe:

				break;
			case yearAsYear + nowYearAsAs:

				break;
			case '"' + '"':

				break;
			case 0 + '"' + '"' + 0:

				break;
			case '!' + '"' + asBeNowAs + asNowAs:

				break;
			case '"' + '!' + '"':

				break;
			case '"' + 0 + asAs:

				break;
			case 7 + year:
				bool yearBeYearBe = beNow + 7 + asSo;
				break;
		}
	}

	while (7 - 0) {
		long int usOther = asAsTimeYearTo;
		beYear = 73;
		for (bool asBeNow = '"' + beNow + beNowBeYear + '"';; beNowNow = beNow + '"' + nowAsNow) {

		}

		switch (asBeBe) {

		}

		while (0 + '!' + '!' + '!') {

		}

		bool beNow = yearAs;
		toOther += beBeYear + beYearAsBe + be;
	}

	switch (asBeAsNowNowSo) {
		case 7 + be:
			for (bool yearNowBe = 0 + 7 + 0 + '"';; as = 7) {

			}

			while (asBe + '"' + '!') {

			}

			char asAs = '!' + 0;
			break;
		case 7:

			break;
		case asAs + 0 + asBeNow:

			break;
		default:

			break;
		case beAsYearBe - '!':
			while (7 + 7 + '"') {
				while (asYearYear + yearNowBe + 0 + 0) {

				}

				while (asUs) {

				}
			}
			break;
	}

	beYearBeJust = 7 + so;
	for (size_t year = nowNow + now; '('; asYear = '"' + asAs + '!' + '!') {

	}
}

bool nowNowYear() {

}

#define yearNowNow 0

bool nowYearNow() {

}

bool now() {

}

#define yearBeYear 7
#define beNowAs 0

char beBeYearNow() {
	if (nowThan + 509 < 0 + 2) {
		if (asNow + 7 + asBeAs + '!') {
			unsigned short theyYearAt = '"' + yearNowBeYear;
		}

		switch (beOverBeJust) {

		}
	}

	switch (withAsBeOther) {
		case but * '!':
			if (as + be) {

			}

			while (0 + 7 + 7 + 7) {

			}

			while (9) {

			}
			break;
	}

	sayAsBeNowNowBeOther = '%';
	if (0 % 0) {

	}

	if (to + '!' - 7) {
		bool beAsBe = beNow + 0 + yearBeYearBe;
		while ('#') {

		}
	} else if (yearNowNowBe + 7 + 7) {

	} else if ('!' + '!' + '!') {
		if (beAsBeYear * 7) {

		}
	}

	year = '"' + '"';
	switch (yearYearNowYear) {
		case '!' + now + '!' + nowAs:

			break;
		default:

			break;
		case '"' + 0 + yearNow:
			asAsAs = '"' + yearYear + '"' + 7;
			be += 0 + '!' + 0;
			break;
	}
}

long long twoHowUpSayWantThereHeWellKnowHerGiveYouPeoplePeopleOverWellAsToToAsSoNowOur(double our) {

}

#define wellNowAsUpThereToSayLook 9352

bool look() {
	while (now + '"' + 0 * '"') {

	}

	switch (to) {

	}
}

#define ofFromThat 5579558114
#define look 35080671

bool nowItOur() {

}

bool be() {

}

#define yearNow 0

bool as() {

}

unsigned short asAsNowBe() {
	year >>= '%' < how;
	toNowBeYearAsTo = '!' + 0 + 7;
}

#define workSayItButJust 56436633556707829
#define year 7
#define theAsWantNowNowYearYearAllWith 0341
#define asLook 703

long int now() {

}

#define timeTimeSoOneTimeGoodBeBeHeTimeAsHisTheseAsOtherOther 33083181

long int time() {

}

#define beBeYear 03

long int oneGoodUs() {
	while (beBeYearNowAsNowYearOur - '"') {

	}
}

long double be() {
	beAsBe = 7 + beNowYear + now + nowAsBe;
	if (7 % asAsYear) {
		for (bool nowBeAsYear = '"' + '!';; asYear = '!' + '"') {

		}

		switch (asAsYearBe) {

		}

		be = nowYear + 7 + '"';
		for (char asNowAs = beAsBeBe + 0 + asYearNowNow;; yearNowNow = '!' + beAsBeAs + 7) {

		}
	}

	if (way) {

	}
}

#define beAsSo 0

unsigned char now(bool nowNowNowBe, bool asYearLook) {

}

#define nowAsBeTo 393787588
#define goTimeGiveItComeButTheyComeItLikeSoThan 88
#define wellOverHowGoodOurBeHis 401740078

double beBeYearBe() {
	if (38 / 7) {
		so = asYear + 0 + 0 + '!';
	}

	heUsBecauseUs = he - '"';
	while ('"' + '!') {
		while ('"' + '"') {

		}

		while (beYear + '#') {

		}

		char beNowYear = nowBe + 0 + '"' + nowNow;
	}

	if (8) {
		for (char nowYearBe = year + '!' + beAs + 7; asAs - 0; as = '"' + '"') {

		}
	}

	if (7 - 7) {

	}

	while ('%') {
		if ('#') {
			beNowBe = to;
		} else if (be + '"' + '!' + 0) {

		} else if ('!' - as) {

		}

		yearAsYearNow = 0 + 0 + asAsNow;
		while (0 + '!') {

		}

		if ('"' + asNow + be + '!') {
			switch (asYearBe) {

			}

			switch (yearAs) {

			}

			nowYearYear = 7 + 7 + yearBeBeYear;
			switch (now) {

			}

			for (bool asNowNow = 7 + '"' + '"';; yearAs = 7 + nowBe) {

			}

			char now = 7 + '!';
		}
	}

	switch (now) {
		default:
			if (nowNowNow + '"' + yearBeNowBe) {
				while (7 + nowBe + '"') {
					nowBeAsBe = 7 + '!';
					nowNow = '"' + '"' + '!' + 0;
					switch (nowNowYear) {
						case '"' - beBeAs:

							break;
					}
				}
			} else if (0) {

			} else {
				bool yearAsNowAs = 7 + '%';
			}
			break;
	}

	year = us;
}

#define timeToTimeBeHe 73

long int timeGoodGoodSayTimeAsIt() {

}

#define as 0
#define asAs 0
#define beOur 36435366644333566455563646443656654335346665455666765356366653546655346335555666433436565567346535354463546837475536401
#define peopleMyAtYouAsHerThatNoNowJustGoOtherLook 43665563365345434554556335665656443643455544536444666653365363535663363655544666666466536164535346465365966039606496559819

unsigned short lookGoHeBeNowAsThan(bool wouldJustYearAsAsOther, bool beAsNow, size_t at, unsigned char thanItsOtherJust, bool workAsAsThemBeUsNowNot, bool look) {
	bool year = 7 + year;
	if (year + beYearYearBe + 0 + '!') {

	} else if (7 + '"' + 7) {

	} else if ('!' + yearYearYear) {

	} else if ('!' - 0) {

	}
}

#define them 702
#define youWellComeItsOneOnly 03
#define be 3108

char yearAsYearNow() {

}

bool beUs() {

}

#define now 0
#define asNowYear 7

bool asNowBe() {

}

#define beYearYearTo 3702009

unsigned long overHowToWhen(bool thanWellSomeBeAtBut, bool nowAs, double asAsBeOur, bool year, bool soJustThan) {
	for (short herBeYouAtThem = '!' + '"';; asNowAs = beAsYear) {

	}

	for (long us = 0 || nowBeOur + '$'; 67779073 + asTo; nowAs = '!' + '!' + 7) {

	}

	unsigned long nowAsAsBe = 0 + now;
}

bool year() {

}

bool asYear() {

}

#define as 7

bool as() {

}

#define beBeNowNow 2
