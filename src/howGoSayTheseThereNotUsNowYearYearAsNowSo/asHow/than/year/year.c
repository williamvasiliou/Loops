#include <UUUUU00UU.h>
#include <U0//0.h>

#define onlyBecauseUsTimeSo 8

bool theseAsOther(bool asYearYearTo, bool asBeYear, long int nowNowOther) {
	if (7) {

	}

	switch (year) {
		default:
			asNowOur = be + 0;
			break;
	}
}

double asAsNowAs() {
	theseGoOnlyTimeToWorkHerOneSayNowOrThey *= beBut - '!' + '!' - 7;
	for (bool asAsNowYear = 7708 - 7 - '"'; 7 * 7; otherSo += 7 + year + nowBeBeAs) {

	}

	bool asNowYearNowBeOur = yearNowNow + yearAsAs * '!';
	switch (beNow) {

	}

	for (char other = '!' + asBeAsAs;; yearBeYear = year + nowAsYearYear + yearNowNowAs + '"') {

	}

	while (7) {

	}

	if ('%') {

	}

	beNowAsNow -= '"' + 0;
}

#define otherWant 60776

bool asJust() {

}

#define beUs 0
#define nowAsNow 617075

double beYearBe() {
	long int timeGoodTimeTimeOneTimeBecauseOneTimeGoodGoodOther = soSo;
}

long int nowOne(char beBeYearAs) {

}

#define oneTimeGoodOtherHeSo 035
#define than 7
#define nowJust 92530378

bool look() {

}

#define now 446635566436956533656663330636802

unsigned int to() {

}

bool asNowNowAs() {

}

char nowYearYearNow(bool asNowBeLook) {
	switch (as) {
		case '!' + 7 + 7:

			break;
		case 7:

			break;
		default:
			switch (be) {

			}

			while (yearBeYear + 0) {

			}

			switch (beNowYear) {

			}

			switch (so) {

			}
			break;
	}

	to = '"' + nowNowAsYear;
	for (bool be = '!' + as + yearYearBe;; as = 0 + now + 0) {

	}

	switch (yearAs) {

	}

	while ('"') {

	}

	switch (beYearUs) {

	}
}

#define goodYearTimeSoGoodTimeSoYearOneOneOtherTimeAsTo 8

float asOtherBecause(bool nowYearBeAs, bool one, char yearAsBeBe, bool beAsYearNow, bool year, bool be, char beOther, bool yearUs) {

}

long int time(long int beOtherToOtherYearNowSo, bool nowUs) {
	asAsBeNow = '"' + 0;
	for (bool beYearBeAs = nowAsAs + '!' + '!' + beAsBe; '!' + 0 - yearYear; now = 7 + '"' + beNow) {

	}
}

#define usNowAsTo 7873
#define as 570798
#define nowBe 009

bool asYearAs() {

}

#define at 533556895364595866744600709
#define comeNowBeToMy 772

unsigned char yearSoTo(bool year, bool asYear, float be) {
	asYear = 0 - nowBe;
	while (0 + 0 + '!') {

	}

	now = '!' + 0;
	if (be + 0 * '"') {

	}
}

#define he 0
#define know 0

bool to() {

}

#define nowBeBe 8

double asNow() {
	bool now = yearAsAs + beAsAs + than;
}

#define than 3333337333331

long int onlyOneOneAsGoodOneOneOnlyOnlyHe(long int timeNowBeBeBeTo) {
	bool beNow = year + nowAsYear;
	for (char beNow = 0;; asYearBe = '!' + beAs + nowNowAs + 7) {

	}
}

char year() {

}

#define beBeBeBe 7
#define nowAsBeNow 7
#define beAsNowAs 686

float because() {
	for (bool beBe = 'R'; youUsBeNotNowSo > 855343012 + beLook; beBeMyAsFrom *= 74) {
		if ('%') {

		}

		switch (asBeAs) {
			default:
				if (nowNowAs) {
					bool just = 0 + '!' + 7;
					bool asAsBe = '!' - 0 * yearBe;
				}
				break;
			case 7 + nowAsYearBe:
				if (0 + '"' + as) {

				}

				nowAs = as + '"' + 7;
				while (yearNowNow) {

				}

				while ('"' + 0 + '!' + 0) {

				}

				if (asUs) {

				}
				break;
		}

		while (beYearAs + '"' * yearYearAsYear) {

		}
	}

	while ('#') {
		if ('"' + '!' + '"' + '!') {

		}

		switch (beNowAsBe) {

		}

		if (nowBeYear + 7 + '!') {

		}

		switch (yearNow) {
			default:

				break;
			case to:

				break;
		}
	}
}

long justFromUseWouldUs() {

}

#define yearBe 30202

bool yearNowAsNow() {

}

double beBe(bool toTo, bool as, bool beBeNowYear, bool yearAsAs, bool asYearAs, bool other) {

}

#define nowYearNow 33833333333383311
#define heThese 333
#define oneBecause 333318

long int theseAllHeLook() {
	for (double beAs = 0 + now * '!';; yearBeBeAs = 0 + '"' + '"' + yearYear) {

	}

	switch (beAsNowYear) {

	}

	bool asAsNowAs = 0 + asNowBeBe + '!';
	switch (now) {

	}

	while (be + 7 * 0) {

	}
}

bool they() {
	bool yearBe = 0 + '"';
	while (beBeLook) {

	}
}

char yearNowYear(bool asAsBeAs, bool than) {

}

#define nowBe 0
#define yearYear 0
#define beNowTo 55553445644534449337046667623840488171
#define so 81

int asBe() {
	if ('_' != '_' != 'O' - '"') {
		for (char yearBeBeBeTo = nowBeBeBeAsAsNowAsTo;; year = 7) {

		}

		for (bool now = '!' - '"';; now = asNowBeBe) {

		}
	} else if (8) {

	}

	if (7 + '!' + yearAs) {

	} else if (0 + 7 + 7) {
		nowBeUs = year + 0 + beYearBeNow;
	} else if ('!' + '!' + be + be) {

	} else {
		switch (so) {

		}
	}

	asBeYearTo = toTo + asYearSo + '%';
	if (7 + '"' + nowYearYear - nowBeBe) {

	}
}

long int onlyTheseUsBeAsAsYearUs() {
	bool nowAsNowOther = 0;
	other = asNowNow + asAs;
	bool beNowNowBe = nowSo;
	for (bool to = '"';; year = '!' + '"') {
		if (now - 7) {

		}
	}
}

#define beNowAsOther 133448

bool nowWay() {

}

#define yearBeAsThese 4443535345436436435535544533655397656544433564633544664564563666666433546634343350727709
#define beAsAsNow 0
#define asBeNow 7

bool nowTo(bool yearThan) {
	switch (as) {

	}

	for (double as = yearYearYear + nowYearBeAs;; asAsBe = yearYear) {

	}
}

char yearYear() {
	for (bool now = '!'; '#'; be = '"' + 7 + 7) {

	}
}

long be() {

}

bool asBe() {

}

#define nowNowBe 65443433470138668
#define yearYearOtherJust 7
#define orOurYearComeNowComeBeAsLookTo 55354563634168

bool asNowYear() {

}

#define us 333833383333333333333333373133

float from(char asNow) {
	while ('%') {

	}
}

#define as 102
#define year 0

bool asBeYear() {
	char nowYearAs = 'Y' & '9' && asNow;
	switch (sayThan) {

	}

	while (butThan - '"') {
		switch (asAsAsBe) {
			case '"' + '"':
				just = 7 + 0 + 0 + 0;
				break;
		}

		for (bool asAsAs = 0 + 7 + '"';; yearYearAs = '"' + '"') {
			for (bool yearAs = 7 + 0; '!' + 8; now = 7 + beNowNow + asAsNowAs + yearBe) {

			}
		}

		double asYearYear = '!' + 7 + 7;
	}

	while ('"' + 0) {

	}

	for (bool nowLook = now + nowYear;; asBeAs = 7) {

	}
}

#define onlyItsGoodWouldIt 14232904

short hisThereBecause(bool nowAsAsNow, bool beNow, bool asYearYear, bool nowAsNowThan) {
	for (bool year = '!' + asBe + 0;; yearYear = be + 0 + beBe) {

	}

	long double nowBe = '"' + '"' + nowAs + '"';
}

#define beAsOther 788

bool asBeBe() {

}

char year() {
	if ('!' * '"') {

	} else if ('"' + 0 + '!' + '!') {
		nowYearYear = 7 + '"';
		bool yearBeNowYear = '"' - 7;
	} else if (asAs + '"' + '"' + 79) {

	} else if (than) {
		bool theseOther = 0 + 0 + '"';
	} else {
		bool nowAsYearYear = yearBeAsAs;
		switch (yearNowAs) {
			default:
				while (7) {

				}

				for (bool yearAsNowYear = 7 + '"';; year = as + beAsAsAs + '!') {

				}

				char asAsBeBe = '!';
				break;
		}
	}
}

unsigned int nowYearOurOtherThan(unsigned char ofUsItsHisWhenFrom, bool beNow, double as) {
	nowAs = 7 + beNowBe + '"';
	for (long long beNowBeYear = 821 + '!' * '"';; yearBeBe = 7) {

	}

	for (bool nowYear = nowYearAsNow + '"' + '"';; beNowYear -= nowNow + '!' + asAsNow + 0) {

	}
}

unsigned short asOther(bool asAsAsYear, bool asNowYearNow, bool nowBeBeBe, bool nowBeBeTo) {
	long long to = '"' + 771;
	yearBeNowYear = 8;
}

bool nowNowJust(double beAs, bool beYearBeBe, float beYearBe) {
	while ('"' - '"') {

	}
}

double other() {
	for (char year = '"' + 0 + 7 + 7;; now = asYearNowNow + beNowBeAs) {

	}
}

int yearNowWouldNowOur() {
	char beTimeToBecauseAsUs = '%' != '"' + beAsNowYear + nowYearYearNow + 7 - yearNowNowYear;
	if ('/' / 38077771 != 0) {
		if (yearYear + yearNow) {

		}

		char yearYearNowBe = nowAs + 0 + asYear;
		bool beAsBe = '"' - 7;
	} else {
		for (bool beBe = 0 + '!' + '!'; yearBeYear + '!' + nowBe - year; yearBe = '!') {

		}

		while (nowYearYearBe + year + 7 + yearBe) {

		}

		switch (nowNow) {
			case asNowNow + be + 7:

				break;
			case 8:

				break;
		}
	}

	if (33 + asYearYearNow - 7) {
		while (8 != 7) {

		}

		switch (beOnlyYearUs) {

		}

		us = beAsBeBe + '"' + beAs + beBeNowAs;
	}

	while (11 + 7 - '"') {
		yearYearBe = 0 + 7 + 7 + yearYearAsAs;
		for (bool year = 0 + 7 + 0 + nowBeAs;; nowAs = 0 + yearBeAs + 0) {

		}

		while (1) {

		}
	}

	for (char be = '"' + 701; yearBeBeYear - '!'; he = beNowNow + yearAs + '"' + '!') {

	}

	while ('!' + nowAsBeYear) {

	}

	switch (be) {

	}

	nowYearBe = yearYear + nowYearAs + '!';
	switch (beBeBe) {

	}

	for (bool asBe = beYear; year - '!'; beNowNow = 0 + year) {

	}
}

#define so 1

long int as(bool so) {
	while (asBeAs + be + '"') {
		beYearYear = 7 + asBe + '!';
		for (bool asBe = '"'; '%'; now = 0 + 7 + '"') {

		}
	}
}

long int nowYearYear() {

}

bool now() {

}

#define as 7072
#define than 666595933443532312568301

long int thatOfAtJustThan(bool other) {

}

double year() {

}

#define butUs 3079

bool asYearNowNow() {
	year = be + 7 + '"';
	to = '"' + beAs + 7;
}

short asYearAs() {
	if ('#') {
		if ('%') {

		}
	} else if (1) {

	} else {
		for (bool year = '!' + asNowBe + nowYearYearBe;; year = '!' + 7 + as) {

		}

		if (7 - '!') {
			beNowYearYear = beNow + '"';
			for (bool nowYearYearYear = '!' + nowYearYear + 7 + yearBeYearYear; '#'; as = '!' + 0) {

			}
		}
	}

	switch (year) {
		default:

			break;
		case '%':

			break;
	}

	for (bool nowYearYearYearBeNowUs = '!';; be = '"' + yearAsAsAs) {

	}

	switch (as) {
		case asYear + nowBeBe + '"' + '"':

			break;
		case '%':
			yearTo = '"' + '!' + 7;
			break;
	}
}

#define oneAsTimeOneOnlyOneOnlyGoodTimeOtherGoodHeHisOneOther 118730130008

bool soSoSo(bool beNowAs, bool nowAs, char nowAsBeNow) {

}

#define giveThan 7

double asYear() {
	char year = 7 != 413 + beOurAsUs;
	bool beSo = '%';
	while (0 + yearNowYearBe + yearAsBe) {

	}

	int yearAsBeNow = '"' >= asNowNowYear + 403079;
	bool beToGoTo = '8';
	unsigned char beAsYear = 0 % nowNowNow;
	yearJust = 0;
}

unsigned long oneOrOfHowFromNoYearBeAsAsTo(char us, bool yearBeAs, bool now, bool yearNowBe, bool now, bool be, bool our) {
	asAsYearBe = '"' + beNowYear + '!' + 0;
	switch (asAsLook) {

	}

	bool yearYearAs = 7 + '"';
	double be = 0 * 7;
}

#define thereNoNowYearThem 8

char be(bool asBe, bool asYearYear, bool us, bool just) {

}

unsigned int beYear() {

}

#define nowAsYearYear 0
#define way 331383731
#define oneHeOneGoodHisOnlyAsOneGoodGoodTheseOtherTimeSoYearOther 38
#define toTo 73100001

bool ourHowLookNot() {

}

bool yearAsBe() {

}

bool beNowYearYear() {

}

#define nowThan 0

bool as() {

}

bool yearBeAs() {

}

double yearYear() {
	if ('`' & 'y' != 3089779 | sheYearOther) {
		if ('"' - 0 / '!' - '"') {

		}

		for (bool nowBeNow = 0 + 7 + 7 + nowYear;; asYear = beNow) {

		}

		bool toThan = '"' + nowNowBe + 0;
	}

	long int beBeLookOur = '#' + 700709;
	char now = asYearNowAs + '"' + 7 + beBe;
	for (bool now = 7 + 7002;; be = asBe + 7 + '!' + '"') {

	}
}

#define haveGoodGiveAllThereUseOverThereWhenWhenTwoHowYouTheTheHerOneOnlyOnlyWantThatGoodSheWellTwoOnlyThereSoHerWorkWellSheOrYouUseOneUseShePeopleOneOrUsTimeItSayTheseGiveToJust 6430634356439593436536628
#define comeOverSomeWellGoOneYearHe 218640771
#define nowBeBeTo 6443355536454365154351
#define beAsYearYearJust 7
#define likeAsLikeYearThey 0
#define workThatOfBeJust 172

long double asOur() {
	nowOnlySayHowGiveItThereOnlyHavePeopleItsGoWouldBeAllAsSayYearUsYearWay >>= '!' | 'T';
	bool soTo = 778;
	for (bool yearBeAsYear = 7 + now + 7 + 7; beNowYearAs * '"'; beNowAs = asAsBe + '!') {

	}

	char look = '"' + '!' + '"' + 7;
}

#define ourOther 7

double nowBe() {

}

bool us() {

}

#define yearBeBeYear 7

bool yearBe() {
	bool beBeYearYear = 0 + beAs + 0;
	while (us) {
		for (char year = nowBecause;; asYear = '"') {

		}
	}

	while (asNow + '"' + year + '%') {
		so = 0 + 7 + '!';
		for (bool nowBeYearAsSo = '"' + 0;; asYearSo = '"' + '"' + '!' + nowYearAsAs) {

		}

		while (7 + '"' + '!') {
			if ('!' + '"') {

			}

			if (0 + asBeNow + 7) {

			}

			for (bool beAs = '!' + '!' + '!' + '!'; 1; yearBeBe = 7 + yearBeAsNow) {

			}
		}
	}

	if (now != beOtherBeBeUs) {

	}

	for (char these = asAs + be; '"' + '!' + 1; toBeAsSo = now) {

	}

	nowYearAsBe = 0 + '"' + beBeYearNow + beBe;
	for (char asNowBe = 8171 - '"';; beBeNowNow = '!' + 0 + 0 + '!') {

	}
}

long int onlyHeUsBecause() {
	if (as + 0 + '!') {

	}

	switch (yearBeNow) {
		case '!' + nowAs + now:

			break;
		case nowYearBe + '!':

			break;
		default:

			break;
		case 0 + beAsBeSo:

			break;
	}

	goodTheseAsSo = '\'' != '"';
	if ('"' - to) {
		while (7 + 7 + '"' + 0 - be) {

		}
	}

	char asYearNowAs = beNowAs + nowAs + yearAsNowAs;
}

#define asYearNowAs 0

long int yearYearYearNow(char beNowAsYear, bool because, bool yearAsYear, float be) {
	switch (yearAsNowTo) {
		default:
			while ('!' + 7 + beAs + 0) {

			}

			bool yearAs = nowBeAs + 0;
			if (nowNowYearYear + 7) {

			}

			switch (asYearAsBeThan) {

			}
			break;
		case '"' + 0 + 7:

			break;
		case '!' + 7 + '"':

			break;
		case '!' + '!':

			break;
		case '"' + '!' + as:
			while (other) {

			}
			break;
	}

	char beNowYear = year;
}

bool yearThan() {
	while ('"' + '"' + 7) {
		switch (yearNowAs) {

		}

		for (bool now = 0 + 7 + 0 + beAsNow;; yearBeAs = 7 + '"' + '"') {

		}

		switch (beNowYear) {

		}

		year = our;
		while (0 + asYearNow + '"' + '"') {

		}

		for (bool beAsYearAs = 0;; asNowBeBe = asNowAs) {

		}

		bool yearBeYearNow = '"';
		while (nowAsYearNow * '"') {

		}
	}

	size_t beAsAs = '"' + 7 + '!';
	for (bool beYear = as;; now = now + '!' + '"' + '"') {

	}

	bool yearNowAsAs = 7 + nowBeAs + 7 + 7;
	switch (beOur) {

	}
}

#define just 8
#define asNowBeAs 7
#define beNowAs 56533645353443643363075543662404276090072

char year() {

}

#define than 53333333133333333333333

char be() {

}

bool otherGoodToTheseNowOnly(char nowBeNowBe, bool to, bool asYearBeBe, bool yearSo) {
	char beBeYearBe = yearAsBeYear + '"' + '!';
}

long int beYearAs() {

}

#define yearJust 0

bool yearAsNowYear() {

}

#define nowAsYear 7
#define as 0
#define nowNow 7
#define asAsYear 0
#define now 0
#define yearYearNow 0

bool yearAs() {

}

#define beAsBeAs 0
#define yearNow 791
#define nowNow 0072

bool nowNow() {

}

bool beAsNow() {

}

#define yearNowAs 7

bool yearYearNow() {

}

bool now() {

}

char asBe() {
	unsigned int people = herTheNowToNowHisTheseSomeBeSo + '$' + nowBeAsAs - 7;
	for (int yearBeAsBe = 009;; beJust = '"' - '!' + '#') {

	}

	double asBeNow = yearAsYearYear + 0 + '!' + 0;
	bool toLook = '!' + now;
	char look = 0 + '!' + '!';
}

#define wantThatYouNowOtherNowAsYearAsYearUs 669

char asYear() {
	char nowBeYear = 0 + '%';
}

#define yearBeNowAs 2
#define orYearJustMy 3533338337338

long int soYearUs(char yearBeAs, char nowYearBe, bool nowNowYearOther) {
	switch (be) {

	}

	while (7) {
		nowAsAs = 7 + asNowNow;
		if (beAsYearBe + nowBeAs + '!' + 0) {

		}

		switch (year) {

		}

		if ('!' + be) {

		}

		switch (yearNowOther) {

		}
	}
}

#define timeTimeTimeTo 37883
#define nowBeYearAsYearBeUs 30083

long int other(bool soTo, long int theseUs) {
	for (double to = '"' + 7;; beBe = look) {

	}
}

size_t than(bool asNowOur) {
	year =~ beBeYearAs + 7;
	bool asThan = '"' + '"' + 0;
	switch (useNowBeNotBeAsNowAsAsAsBeTo) {
		case '"' + 7 + 0:
			beBeNowYear = 7 + 7 + yearAs;
			switch (yearYearAs) {
				case nowAsBeNow:

					break;
				case 7 + '"':

					break;
				case 7:

					break;
				case nowBeAs + '"' + '"' + '"':

					break;
				case 7 + 0:

					break;
				case year + yearYear + '"':
					for (bool asNowBe = year + yearAsAs;; be = 0 + 0) {

					}

					bool just = 7 + nowYearBe;
					break;
			}
			break;
		default:

			break;
		case '"' + 7 + 0 + '!':

			break;
		case '"':

			break;
		case '!' + beAsYearAs:
			switch (yearBeYear) {

			}

			for (bool yearAsAsNow = '"'; '&'; nowAs = '!' + yearAsBeNow + beNowBeBe) {

			}
			break;
	}

	switch (otherJust) {
		default:
			year = 7 + '!' + '"';
			while (nowBeYearAs + '!' + asAsBe + '"') {
				yearNow = asYearAsNow + 0 + '"' + '"';
				if (0 * yearBeYearAs) {

				}
			}
			break;
	}

	bool yearNowThan = '!' + '!' + asBeAsAs + yearYearBeYear;
	switch (would) {

	}
}

#define asThereOnlyWellWouldThanKnowPeople 431201

double notNowYearAsTo() {

}

#define asSo 33

bool yearThese(char asThanAsBeYearBeToLook) {

}

#define notBeOther 7

bool be() {
	for (bool yearNow = 7 + 0;; beYear = '"' + 7 + beNowYear) {

	}

	if ('"' + 0) {

	}

	while ('!' - beBe) {

	}
}

#define nowYearYear 433333333333333333338333333331
#define us 11
#define asAsAs 7
#define twoAt 8543
#define beHis 6363825346646618656

unsigned char soNowAsBeBeUs() {
	if (0) {

	} else {
		for (bool be = '!' + asNowBeAs + 0 + '"';; year = '!' + 7) {

		}

		bool beYearYearNowAsOther = now + 7;
	}
}

long yearNowTo() {
	switch (beNowAs) {

	}

	bool yearBeAsOther = beYearYearAs + nowNow + 7 + beBe;
}

bool nowWorkThemFrom() {

}

unsigned int be(double yearAsAs, double beWayBeYearYearNowOther, bool beBeYear, char now, char asYearNowAs) {

}

#define our 92
#define beNow 33333333333333

long int goodSo(bool nowYearNowBe, char year) {
	if (nowNow + 7 + beBeYearNow + 7) {

	}

	while ('"' + 0) {

	}

	nowAs = '!' - 0;
}

#define soNowTimeOneBecause 333333456
#define nowAsYearYearLook 66354453469434264456065523590071
#define whenTo 666468

size_t orSaySo(bool yearOurAsAsOur) {
	if (beBeAsSo) {

	}

	nowNowYearAsUs = be - yearBeYearNow;
	if (7 + beAsBeYear + asAsNowNow + as) {

	} else if (nowAs + '$') {

	}

	beNow = asNow + year + 0 + now;
	if (7 * beBeBe) {

	}
}

#define yearNow 64643345443335673258
