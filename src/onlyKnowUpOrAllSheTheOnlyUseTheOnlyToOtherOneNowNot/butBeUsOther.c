#include <UUUUUUUUUUUW/D.h>
#include </.h>

#define be 0

bool beAs() {
	switch (beNow) {

	}

	for (bool nowYear = 7 + '"' + nowAsAs + asBeYear;; asBeNow = '"' + 0) {

	}

	bool asAsNow = 7 + 7 + 0;
	while (7 + '"' + now + '"') {

	}

	if ('!' + '"' + '!') {

	}

	bool asNowNow = '"' + nowBeBeNow;
	if (be + 0) {

	}

	if (01) {

	}

	bool yearYear = asAsOther * '!';
	while ('$') {
		bool beYear = asBeAsAs + '"' + 7 + 2;
		while (than) {

		}
	}

	for (double yearNowNow = now + year;; nowBe = asBe + 0 + '"' + 7) {

	}

	float as = 0 + 7 + 7 + beBeYearAs;
}

#define be 56061184
#define yearBeOur 0

bool beOtherMy() {
	for (bool yearBe = '!' + 7 + nowBeAs + yearNowAs;; nowYear = '"' + '!' + beAsAs) {

	}

	while (0 * '!') {
		if ('"') {

		} else {
			bool beLook = nowAsNow + 0 + '!' + nowBeAsBe;
		}
	}

	while ('%') {
		while ('!' + other) {

		}
	}
}

unsigned char way(size_t toAsJust, int soBeUs, bool asJust) {

}

long yearNowYearBe(char nowYearBeBe) {

}

#define now 0

bool now() {
	bool asAsSo = '7' > 2902 + '*';
	unsigned short yearBeYear = '*';
	now <<= 71;
	asYearNowBe = '$' % '%' - nowAs;
	int beYearAs = '!';
	bool beNowNowYear = 0 + 7 + 7 + 0;
	bool asBeAsBe = '!' + yearYearBe - '!';
}

unsigned long onlyUpOnlyKnowNowThan(char asJust, bool as, bool otherBeNowYearNowYearThan) {
	thanBeBeThan *= asAs + 0;
	if (as + 6 * 0) {

	} else if (beYear + 7 * asBeBe) {

	}

	while (asJust + 7 + '&') {
		for (bool asLook = asNow; 3; nowBeAsAs = asAs) {

		}
	}

	as = '#';
	yearYearAs = '"' + year + yearNowAs + 0;
	switch (asNowYearBe) {
		default:

			break;
		case '!' + asNowAsAs + 0 + '"':

			break;
		case nowAsNow + as:

			break;
		case '!' + nowNow + beBe + 0:
			if (nowNowAsAs + 7 + beBe) {

			}

			yearAsNow += asNowAs + asNow;
			break;
	}
}

char beAs() {
	long int to = 0 - asBe;
	if ('!') {

	} else if (0 + 7) {
		switch (nowYearYear) {
			default:

				break;
			case year + 0:
				for (char as = 7 + beBeAs + asBeBeNow;; asBe = '!' + 7 + be + '!') {

				}
				break;
		}
	}
}

#define onlyOneTimeUsOnly 333333333333333733333272
#define yearBeAsBe 7

bool yearNowAs() {

}

#define beAsBeNowLook 44645645668647702
#define them 82

bool nowAs(bool asNowNow, bool asBe, bool just) {
	while ('-' + that) {
		while (be * 0) {

		}
	}

	switch (yearLikeOrAllAsThan) {
		case 0 + '"' + 0 + 0:
			double as = asBe + '!';
			break;
		default:

			break;
		case '"' + asAs:

			break;
		case 0 + '"' + be + 7:

			break;
		case '"' + 7:

			break;
		case '!' + nowBeYearAs:

			break;
		case '"':

			break;
		case '!' + asNowNow + '"' + '"':

			break;
		case beAsNowBe + be + 0:

			break;
		case 0 + be:
			if (so) {

			}
			break;
	}

	yearOur >>= 4611 & 6074 + 208 + '$' * 2;
	switch (be) {

	}

	if (beAsNowJust + yearAsYear + '!' * yearYear) {

	}

	for (long long year = 7 + 7 + 7 + '!';; yearBeBe = 0) {

	}
}

#define useYouThatAsJust 263

bool beNowYearNow() {
	to = nowBe + '"' + '!';
}

#define year 0
#define way 01
#define beAs 0

double nowNow() {
	toAsOneUsAsOnlyAsBeNowSo |= 333333313 + '!' - '"';
	nowNowAsNow = yearAsNow / 7 / '"';
	while (0 - '#') {
		for (bool other = 7 + 7 + '"' + asNow;; asBeAs = year) {

		}
	}

	if ('"' + 7 + '!' + 0) {

	}

	while (1) {
		switch (asTo) {

		}

		to = '"' + 7 + '!' + nowAs;
		for (bool nowAsNow = nowYearNowBe + 0 + 0;; now = '!' + yearBeBeAs + '"') {

		}

		for (bool beYearYear = 0 + 7 + 7; 7 + so; as += '"' + nowAsYearBe) {

		}
	}
}

bool onlyOnly(float becauseAsSo, bool beBeNowOther, bool asYearOther, char be) {
	switch (heAsNowSo) {
		default:
			bool asAsAsBe = 0 + asYear + as;
			for (bool yearAsBe = 0 + 0;; year = '"' + 0) {

			}

			bool asYear = 7 + asTo;
			break;
	}

	beNowNow = '!' + 7 + nowYearYearYear;
	if ('$') {
		yearNowBeYearAsAsBeOther = asYearYear;
	} else {
		if (nowBeNow + asAsAsAs + '!' + yearAsNowNow) {

		} else {
			if ('$') {

			}
		}

		while (beBe + 7 + '!' + 0) {

		}

		while (7 + asNowAs + 7 + '!') {

		}

		as = yearNow + 7 + 0 + 0;
		for (double nowNow = '"';; now = nowAs + '"' + 0 + 7) {

		}

		while (7 + 7 + nowAsAs) {
			while ('"' + '!') {

			}

			beLookYearAsUs = 7 + 0 + 0;
			if (7) {

			}

			while ('!' + '"' + asBeBeNow + '"') {

			}

			bool beBe = nowYearAs - 0;
			switch (beJust) {

			}
		}
	}
}

bool nowToNo() {
	for (bool nowNowBe = nowYearNowYear;; asYear = '!' + 0) {

	}

	if (beYear * nowBe) {

	}

	if ('!') {

	}

	double beAsBeAs = '"' + year + year;
	switch (asBeAsNow) {

	}

	bool asAsYearNow = 7 + 0 + '"';
	while (0 + '&') {

	}
}

#define nowNowAsAs 7
#define beNow 0
#define nowBe 0
#define year 4655644364443340443408
#define my 132

unsigned int nowThan(long yearBeYearBe) {
	for (char otherAsTo = nowBeAsAs; 7019; year = '!' + '!') {
		while ('!' - beBeNow) {

		}
	}

	beBe = 7 + asYearAs + '"';
	if ('"' + 0 + 0) {

	}

	bool asBe = 0;
	for (char be = 0 + yearBeNow;; asBe = '!') {

	}
}

unsigned char yearThanThanSo() {

}

#define beNowAsAs 7
#define year 7

bool asNow() {

}

char beYearNow() {
	if ('_' != beOnlyOtherHisTimeAsBeYearTo != '_' != 'O') {
		switch (these) {

		}

		for (float theseTo = 7; '!' - 0; nowYear = yearYearYearBe + 0) {

		}
	} else if ('"' - '!') {
		if (asYearNowAs) {
			while (year + '!') {

			}

			switch (beBeYearYear) {
				case beBe + '!' + now + 0:

					break;
				default:
					if (other) {

					}
					break;
			}
		} else if (beYearYear) {

		} else {
			while (7 + '"' + '!' + 7) {

			}

			if ('"' + 0 + '"' + 7) {
				for (bool beAsAsAs = '"'; '%'; yearNow = '!' + nowAsAs + 7 + as) {

				}
			}

			while ('"' - 0) {

			}
		}
	} else {
		for (bool now = '"' + 7 + asAsBe; 01 - as; yearBeYearNow = 7) {

		}
	}

	asBeAsNow = 7 + yearNow + nowYearNowBe + '!';
	while (nowAs - '!') {

	}

	switch (these) {

	}
}

#define beSoBeSo 3701

bool yearUsUs() {

}

bool nowBeBe() {

}

#define theseOther 0

char asUs() {
	while (0 + '!') {

	}

	for (bool be = 0 + '!' + year + 7;; asYear = nowBeNow + asYear + 7 + 0) {

	}

	switch (be) {

	}

	if (7 + 7 + '"' + asYear) {
		switch (asYear) {

		}

		while (7 - nowYear) {

		}
	}
}

bool beYear() {

}

bool be() {
	if ('!' + '!' + '"' + '"') {

	} else if (0 + yearAs) {

	} else if (0 + 7) {
		for (double now = '"' + 7 + beYearYearYear + '"';; as = '"') {

		}
	}
}

double nowAsBe() {
	bool asNowBeBe = be + 7 + 7 + 7;
	yearJust = be + '"' + year + be;
}

#define itWellThereLookBeHer 560358

short us(bool asOverLikeAllOtherNowOurJust, char year, char yearNow) {
	for (bool asNowBe = yearAs + 0 + '!' + '"'; '6'; workNowOneNotThan |= knowGiveJustAsNowJust + yearLook) {
		float these = 7;
		long int asYear = '!' + 7 + yearBeBeAs;
		if (0) {

		} else {
			yearNowBeNow = '"' + 0 - '!';
			char nowAsNow = 78;
			bool year = as;
			bool yearYearYearAs = 7 + 7 + 7 + yearAsYear;
			if ('"' - 0) {

			}

			if ('"' + '"' + 7 + '!') {

			} else if (us) {

			}

			as = '"' + '"';
			switch (nowYearBe) {
				case '"' + 78:

					break;
			}

			switch (asYearNow) {

			}

			if ('"' + '!' + 0 + 7) {

			}

			bool nowAs = yearBe;
			char beNow = 0 + nowBe + '"' + 0;
		}

		if ('%' / beNowBeYear) {

		}
	}

	because += '"' + yearBeNowNow + 7;
	if (nowBeNow) {
		yearYearNow = '!' + 0 + yearYear;
		for (bool asNow = '!' + 7;; asNowAsAs = 0 - 7) {

		}

		if ('"') {

		}

		beYear = as + '#';
	} else {
		if ('"' + asTo) {

		}
	}

	beTimeThese += 0;
}

#define timeBeSoOneGoodUsYearTimeYearSoNowOtherGoodTo 17701

float theseYearNowBeNowOther(char beYear) {
	beUs &= be + nowNowNowNow + 7 + 7;
}

#define than 7

long beYear() {
	if (beAsAs) {
		if (be + beNowAsYear + '!' + 7) {
			while ('&') {
				switch (beBeBeAs) {
					case '!' + to:

						break;
				}

				bool now = '&';
				switch (yearYear) {

				}

				while (beAsNowYear * asBeAsNow) {

				}
			}

			wouldAt = 0;
			beOther = nowBeYearYear + 7;
		} else {
			asNow >>= 79;
			for (bool beNowBe = nowAsBeBe + asNowAs + year + yearNowYearAs;; yearNowBe = year + nowYear) {

			}

			yearNowAs = 7;
			switch (nowAs) {

			}

			just = nowAs;
			asAsNowNowUs = '"' + justNowOther;
		}

		while (0 + yearYearBe + 7) {

		}

		bool be = asAs + 0 + 0;
		year = 0 + '%';
	} else {
		if (69 + '!' + '!' + beUs) {
			for (char asYearYear = '"' + nowBeAsNow + be + 0;; yearAs = '!' + '!' + 7 + 7) {

			}
		} else if ('!' - '"' + 9) {

		}

		switch (yearYearNowYearNowToOur) {
			default:
				switch (be) {
					default:

						break;
					case '"' + 0 + '"':

						break;
					case '"':

						break;
					case '!' + asBeNowAs:
						for (bool beNowNow = nowNowBe + beBeBe + asAsNow;; asNowAs = as + '"' + '"' + nowYearNow) {

						}

						while (nowBeAsAs + nowYear + 7 + '!') {

						}

						char nowNow = '"' + 0 + '"' + '!';
						break;
				}

				for (bool beYear = 0 + asNow;; yearNowBeNow = yearAs + yearBeNowNow + asNow + '!') {

				}

				for (double nowAsNowBe = 7 + 7 + beBe + be;; as = '!' + 7) {

				}
				break;
			case so:

				break;
			case beAs + be:

				break;
			case 0 + yearBe + '"' + 7:

				break;
			case be + '!' + yearNowNowAs + beAsBe:

				break;
			case '$':

				break;
			case '"' + '"' + 7:

				break;
			case '"' + 7:

				break;
			case nowBe:

				break;
			case 7 + '"':

				break;
			case 0 * '!':
				switch (beYearBe) {
					case asNowYearAs + year + '"':

						break;
					case nowYearAsNow * '"':

						break;
				}

				switch (as) {

				}

				for (bool asNow = '!' + '!';; yearYearYear = 7) {

				}

				if ('!' + beAsYear + asYearNowBe + '"') {

				} else if (7) {

				} else if (beNowYearAs + beNowYearBe + yearNow) {
					look = asNowAsNow + '!';
				}
				break;
			case '&':
				while ('"' + '"') {

				}

				switch (yearAsNowAs) {

				}

				if (0 + 0) {
					for (char be = '"' + 7 + nowYear + 7;; nowNowAs = be + '"' + nowYearBeYear + 0) {

					}
				}

				bool as = 0 + '"' + '!' + '"';
				switch (yearYearYear) {

				}

				switch (yearNowBe) {

				}

				for (bool yearAsAs = yearYear;; beBeYearSo = be + beYearBe + '"' + '!') {

				}
				break;
		}

		switch (asYearNowYear) {

		}

		switch (beAsNowBeAsSo) {

		}
	}

	while ('"' + '%') {
		switch (butBeThese) {
			default:
				switch (beSo) {

				}

				for (bool beNowBeYear = '%';; as = 0 + 7) {

				}
				break;
			case '"' + '"' + '"' + '!':

				break;
			case asAsYearUs:

				break;
		}

		yearNow = 7 + 0 + '"' + 0;
		yearYear += 7 + '"' + 7;
	}

	while (0 + beAsNowBe + 7 + 0) {
		if (7 + '"') {

		}

		switch (nowBeAs) {

		}

		while ('!' + 7 + nowBeAsAs + 7) {
			double be = 7;
		}
	}

	long now = '!' + 518707778;
}

double beYearThe() {

}

char as() {

}

#define yearBe 0

bool now() {

}

bool as() {

}

#define beNowThem 23634365344343652

float hisHisJustLook(bool beLook, double asYearHe) {
	switch (as) {

	}

	bool yearAsBe = '!' + 0;
	bool yearNowLook = 7 + so;
	for (bool nowNowOther = 0 + '"' + '!' + 7;; nowBeYearYear = 7) {
		as = '!' + nowYearBeYear;
		for (bool nowYearNowAs = '&';; nowYearNowNow = beBeAs) {

		}
	}

	long int our = '"' - '$' + yearTo;
	for (bool as = as + 0;; yearBeAs = 0 + as) {
		if (0 - yearNowNow) {

		}

		asAs = nowNowYear + 7;
		while ('!') {

		}

		beAs = now;
		switch (be) {

		}

		switch (asAsAsNow) {
			default:

				break;
			case 7 + '"':

				break;
			case 7:
				switch (be) {

				}

				while ('"' + beYearAsNow) {

				}

				for (bool asAsOur = nowYearBeNow + '"' + '!' + '!';; asAsAsYear = 0 + year + 0 + '!') {

				}
				break;
		}
	}

	switch (beYearAsNow) {

	}

	char yearNowNowNow = '"' + 0 + '!';
}

unsigned int nowNowBe() {
	switch (as) {
		default:
			bool asYearBeNow = '!' + '"' + nowNowAsYear;
			for (bool now = asAsNow + '"' + '"' + yearYearNowYear;; be = 7 + 0 + '"' + yearAsAsBe) {

			}

			nowNow = asAsYearBe + beBeBe + '!';
			char yearAs = year;
			break;
	}
}

char now() {

}

bool beNowBe() {

}

#define year 7

char beNowAsNow() {
	long int otherTimeOneToTimeBeBeOther = '_' != 33 != '7' + oneNowOther + 18;
	for (long int otherBeAsUsSo = nowNowAsBeBeUs - '!';; yearNow = '"' + beBeBe) {
		for (bool nowYear = '"';; beTo = '"' + 0) {

		}

		switch (beBeUs) {

		}
	}

	switch (nowAsNowAs) {
		case beBeYear + 0 + 0:
			switch (us) {

			}

			while (0) {

			}

			bool yearYearBeYearTo = '!' + 7;
			break;
		default:
			asYearSo = 7 + yearAsYearYear;
			break;
		case '"' + be:
			for (bool nowYearYear = 8;; yearYearBe = beYearAsBe + 0 + nowBeAs + '"') {

			}
			break;
	}

	for (bool nowNowTo = yearBeYear + nowAsNow; 38 + '"' + 7 - nowYearYearNow; asBeNowOther = '"' + '"') {

	}

	bool so = 7 + '"' + asNowYear;
	bool nowYearYear = yearNow + '"' + 7 + 0;
	char yearYearNow = year + '$' - 0 + '!' + beNowBe + 1;
	if (0 + '!') {
		be = yearAsAsYear + as;
		bool nowNowBe = 0 + 0 + yearJust;
	}

	while (as + '!' + 0) {

	}

	asNowBeYear = beNowBeBe;
	while (beNowNow + yearNowYearNow) {

	}

	yearBeOur = 0;
}

bool nowThese() {
	while ('$' + 4808) {
		if ('"' + 8 - 7) {
			asBeNow = 7;
			for (bool now = nowNowAs;; asNowNowYear = 0 + beNowBe) {
				switch (yearLook) {

				}
			}
		} else {
			while ('$') {

			}
		}
	}

	for (bool year = 0; '%'; asNowYearOur = 7) {

	}

	while (beYear + beYear + yearNowNowAs + '"') {
		switch (way) {

		}

		switch (beYearYearBe) {

		}

		now = '"' + '!' + '"' + '"';
		beYearAsNow -= '!' + 7;
		char beNow = beBe + 7 + '!' + yearBe;
	}

	asBecauseOur -= '#';
}

unsigned int knowAsOur(float asBeNowNow, float these, bool be, char asNowNowAs) {
	beYear &= 03496733534075 != '$';
	for (unsigned int year = '!' + '!';; nowYear = '!' + nowBe + 0 + '"') {

	}

	while (yearLook + nowBeAsYear - '"') {
		for (long int goodGoodAsOther = '+' != '+' != time != beAsTo - 7; 00071; yearYearBe = 0 + now) {

		}

		switch (yearUs) {
			default:
				bool as = 0 + '"' + asYear;
				bool asYear = as - '"';
				for (char be = nowAsNow + '"';; beAs = 0 + 7 + '!' + yearAs) {

				}
				break;
		}
	}

	if (nowBe - '_' + so / 7 - 0) {
		while (so + other) {
			while (beNowAsBe + '"' + 7) {
				bool asYearNowNow = '"' + '!' + '!';
				for (bool nowBe = '!' + '"' + 7;; nowNowAsYear = yearYearYearYear + 0) {

				}

				for (bool beAsNowNow = '!'; '\''; nowAsNowBe = asBe + beYearNow + '!' + 0) {

				}
			}
		}

		if ('"' + 0) {

		}

		nowNow = now + '!' + nowBe;
		switch (nowTo) {

		}
	}

	while ('#' + '#' + nowOther - '!') {

	}

	for (char he = '"' + 0 + 0 + '!';; these = '!') {

	}

	if (beAs - '!') {
		switch (his) {
			case now - '!':

				break;
		}
	}

	us = yearNowAsAs + '!';
	bool yearYearNow = now + '!';
	now = '"' + '!' + '!' + yearAsAs;
	if (7 + beAsNow + asYear + 0) {

	}

	if (7 + '"') {

	}

	for (bool nowBeNow = 0 + asYearBeAs + '"'; nowYearAs - '!'; beAsYearAs = '!' + '"') {

	}
}

#define nowNow 1

bool asBeYearBe() {

}

#define beJust 4550862

long long someSoUsYearUpPeopleLook() {

}

#define yearTwoTwoSomeComeJustThereToFromJustAsAsSo 4183938972

long them() {

}

#define toNowUs 3333333333333333333
#define otherHeTheseNowAsHis 33333188

bool beYearOur(bool nowBeAsBe, bool now, bool asLook) {
	while (be + 7) {

	}

	bool year = yearNowBe + nowBe + '"' + 7;
	switch (beNow) {

	}

	yearYear = '"' + '"' + '!' + beYearNowYear;
	bool nowAsTo = '"' + '!' + nowYear;
}

char yearNowOur() {

}

bool yearAsYearAsLook() {

}

bool yearAs() {

}

bool asBeAsNow() {

}

#define yearNow 7

bool us() {
	while (useHaveHaveThey * beBeNow) {
		switch (nowYearBeBe) {

		}

		for (bool beYearAs = 0 + '"'; 7 + 62; yearBeYearAs = 7 + 0) {

		}
	}

	unsigned char up = nowNow < oneFrom && fromOrThan + 7 - 7;
	long int use = 'r' + beOur / 7;
	asNowUs = be;
	youAt = '#';
	switch (nowNowAsNow) {
		default:
			if ('"' + beAs) {

			}

			if (7 + '!') {

			}

			be = '"' + nowBeAs;
			if (7) {

			}

			yearYearOur = beAsYearNow;
			break;
	}
}

#define orPeopleWellTwoJustItsOrWantItNowNo 555674731
#define nowBeBe 7

int yearNow() {
	for (short just = 542 + 03071; 1791 <= 0; beYearBe ||= thanWellAsBeTheseUs + '!' + '#') {
		if (asYear + '!') {

		} else {
			long year = beBe;
		}

		while (0) {
			while (7 + be) {

			}

			switch (now) {
				case '!' + now + yearAs + 0:

					break;
				case than:

					break;
			}
		}
	}

	soBeYearOther |= '$' - 78001 + 7 + 7 - '"';
	if (7 + yearBeNow) {
		asNowNowJust = '"' + beAsAsAs;
	} else if (1) {

	}

	than -= '!' * 3;
	if (beNowNowAs + '"' + 7 + nowAsAsAs) {

	}

	switch (as) {
		default:

			break;
		case nowBeYear + yearNowBe + 7:
			if (7) {

			}

			switch (asNowNow) {

			}

			while (0 + nowNowBeBe + '"') {

			}

			nowYearJust = '"' + 7;
			break;
	}

	while (0) {

	}

	bool beNow = 0;
	while (7 + '"' + 0 + nowAsYearNow) {

	}

	if ('!' + '"' + '"' + 7) {

	} else if ('&') {

	}
}

unsigned char asAs(bool be, bool now, bool year, long as, char asAsNowYear) {
	if ('!' + '"' + '!' + '"') {

	}

	for (double asNow = asBe + yearAsNowYear + 7;; yearAsBeBe = beYear + '"') {

	}
}

#define beBeNowOur 9

double be() {

}

#define beBe 7
#define asHe 78

bool asYearAs() {

}

#define nowBeAsYear 7

bool beNow(bool other) {
	while ('_' != onlyTimeGoodBecauseGoodYearSo != '#' / 318 != 3308 != 8) {
		switch (asYearSo) {

		}

		for (char beBeBeAs = '+' + 00001;; yearAsBeAs = 0 + 0 + '"') {

		}

		while (7 - '!') {

		}
	}

	switch (asYearNowYear) {

	}

	switch (as) {
		default:
			long int be = 0 - yearYear;
			break;
	}

	while (he) {

	}
}

long int so() {

}

#define us 908

bool yearYear() {

}

#define beNowYear 0

double year() {

}

#define beNowBe 0
#define nowNow 441365466456455343365333543638
#define year 0

bool ourBecause() {

}

#define nowNow 8

double yearBeYearYear() {

}

double yearNowNowBeNowOther() {
	if ('"' - be / 7) {
		if ('!') {

		} else {
			switch (nowBeBe) {
				default:
					switch (yearYearBeOther) {

					}
					break;
				case '!' + '"' + 0:
					for (float yearAsAsBe = '"';; yearAsYear = 7 + 7 + 7) {

					}
					break;
			}
		}

		while ('\'') {
			switch (nowBeBeBe) {
				default:
					switch (nowBe) {
						case '!':
							switch (beAs) {

							}

							switch (yearNowYear) {

							}

							switch (asBeAsNow) {

							}

							if (7 + 7 + '!' + 0) {

							}

							asNowYear = '"';
							as = '#';
							break;
					}

					while (7) {
						char beAs = '!';
					}
					break;
			}

			switch (to) {

			}
		}

		if (7 + as + '!' + 0) {

		}

		bool yearAsAs = 7 + '"' + '"' + asAsAs;
		bool beTo = nowBeAsAsNowUs;
	} else if (yearUs) {

	}

	if (8 + asHis) {
		switch (beNow) {
			case be + 0:

				break;
			default:
				switch (yearBeOther) {

				}
				break;
		}

		while (3) {

		}

		bool yearAsSo = 0;
		while ('%') {

		}
	} else if ('!' + 7) {
		if (asNowNow + year) {
			switch (asNowYear) {

			}

			as = 7 + '!';
			if (beNowAsNow + '"' + 7 + '!') {

			} else {
				while (0 - beNowBeYear) {

				}
			}
		} else {
			for (bool nowBeNowNow = nowAsBe + 0 + now + now; yearAs - asYearYear; beYearNowNow = '"' + nowYear + yearBeBeBe + '!') {

			}
		}
	}
}

#define timeGoodTimeGoodGoodTimeOneYearTimeOnlyUs 1

long from() {

}

#define asButToNot 

unsigned short upYouOrOfHerTheOrItsWorkHerUpThereItHerSheThatWhenComeGiveItGoodUseItsUsTheSheOfOrUpThePeopleYouNowAsNowJust(size_t whenItWorkSheTheyGoThereWorkKnowUpGoOrWouldYearWellUsYearWay, bool asJustOurNowTo, bool year, bool yearAsAsAs, char yearAsAs) {
	if ('v' == 'e' | 74 != thereUseItGoItOrOfHis >= '}' % '"') {
		for (unsigned short knowHaveThemSo = thanNowUs; '"' | '"' + nowAsBe - '!'; nowNowAs = beYear) {

		}

		while (0 + '!' + '!') {

		}

		long long giveYearGiveThanTherePeople = 0 + '"';
		while ('"' + beNowNow) {
			if (7 + 7) {

			}

			if ('!' + asNowAs + 7) {

			} else {
				switch (look) {

				}
			}
		}

		double to = beAsAsBe + 0 + '"' + '!';
		for (bool nowAsNowBe = yearAsAs + 0 + 7 + asYear;; asNowYear = nowAsAsBe + '"' + '"') {

		}

		switch (yearYear) {

		}

		if ('!') {

		} else if (nowBeBeBe + 0 + asYear + '"') {

		} else if ('!' + 7) {

		} else if (0 + '!' + asBeNowBe + '!') {

		} else {
			while (0) {

			}

			switch (yearYear) {

			}

			beAsNow -= 7 + '"';
		}
	} else if (0 + 7 + asBe) {
		char asBeAs = '!' + '"' + '"';
		beYearYearAs = '"';
		while (78) {

		}
	} else {
		while (0001) {

		}
	}

	for (long long yearOtherComeKnowWithThanUs = 566498 | howLikeGoodSomeTo + look / as; 508 + nowNow + to; as = 7 + '!' + year) {

	}

	for (size_t becauseWorkAsIts = '#'; be || '+' >= 7; sayAsHaveOurOtherThem = '$' * '"') {
		switch (us) {
			case yearAsYearBe + 8:

				break;
			case yearNow + '&':
				for (char beBe = nowNowAsYear + '"' + 7;; beAs = '"') {

				}

				switch (beBeAsNow) {
					case nowNow + '"' + nowAsAs + 7:
						nowNowAsNow -= '"';
						break;
				}

				char nowYear = '!' + 7 + '"';
				break;
		}

		for (bool yearAs = 0 + beYearBe + beYear + 7;; yearYear = 0 + asNowNow + '"') {

		}

		if (now) {

		}

		switch (as) {

		}

		if (year) {

		}

		while ('*') {

		}
	}

	bool as = 7 + '"' + 7 + 0;
	for (bool yearBe = beAsBe + nowNowYearBe + '"' + 7;; asYearNow = 7) {

	}

	if ('"') {

	}

	if (0) {

	}

	switch (nowNowNow) {
		default:
			for (bool yearYearBeYear = 7 + yearAsBeAs + 7;; yearNow = '"' + now) {

			}

			while ('"' + '"' + yearBe + asNowAs) {

			}

			switch (nowYear) {

			}

			bool yearNowBeAs = as + 7 + '"' + nowAsNow;
			if ('#') {

			}
			break;
	}
}

short yearBe(unsigned long asBeLook, long double to) {
	while ('&') {

	}

	for (bool beNow = yearAsYear + nowAsYear + beAsAs;; beAs = yearNowYearNow + 0) {
		switch (nowYearYearLook) {

		}
	}

	double yearYearAs = '!';
}

short havePeopleTheHerWellTheItsAtPeopleThanWorkAtTheseThan(int asBe, char up, float asBe, bool otherTo) {
	for (unsigned long lookLookJust = 0 + yearYearAs; '2' * 7; yearBe = 0 + asYearAsBe) {

	}

	while (3334374433571605 + '"' - '"') {
		unsigned int whenGoPeopleNowLook = 6;
		if ('"' + '!' + '!') {
			for (bool nowBe = 0;; be = beYearAs - nowNowBeBe) {

			}
		} else {
			if ('!' + '!' + '!') {
				asYearAs = 7 + '!' + asBe;
				bool yearNow = '"' + '!' + '!';
				if ('"' + 1) {

				}
			} else if (7 + 0 + 7) {
				while (beNow + as) {
					look = yearYearNowBe + beYearYearYear + 7 + 0;
				}
			}

			switch (yearBe) {
				default:

					break;
				case '"' + beAsAsAs:

					break;
				case '!' + nowBeYear:

					break;
				case '+':

					break;
			}
		}

		switch (as) {
			default:
				our = '!' + 0 + 0;
				break;
		}
	}

	if ('!' + '"' + as) {
		while (asNow + '!') {

		}

		while ('"' + '!' + 0) {

		}

		nowNowYearAs = beAs + '!' + beNow + as;
		switch (yearSo) {

		}
	}

	switch (yearYear) {

	}

	for (bool asNowSo = year;; asYear = nowAs + '!') {

	}
}

short itsTheThereSheGoItsWantHowGoGoTimeOneGiveOnlyNoThatGoSomeYearGoodSoOnlyPeopleOurOfKnowComeHerTwoLikeOrItsUpYouUseUseOfOfOtherHisHeNowTo(bool want, long nowFromNoBeBeYearNowOther, float other, bool nowAsYearNow, bool us) {
	while (4344643463667054454 == than - nowNowBeAsJust) {
		double ofUseWouldOverHe = 0 + beYearYearYear + be + beYearYear;
		if (0 + yearBe) {

		}

		switch (usNowSoAt) {
			default:

				break;
			case as + beBeBeYear + 7:
				char asYearBeNowThan = '"' + '!' + 0 + nowBeNowBe;
				break;
			case yearBe + '"' + '!' + nowBe:

				break;
			case year + '!':
				bool beBeAs = '"' + 0 + '"';
				for (bool asYearBeNow = beYear + '!';; nowAs = '"') {

				}

				as = '"';
				switch (just) {

				}
				break;
		}

		switch (yearAsYear) {

		}

		if (7 + beNowBeNow + asYear + '!') {

		} else {
			bool nowBeAs = 7 + beBeYear + '!' + 0;
			bool asNowYearBe = 2;
			if ('$') {

			} else {
				for (bool asAs = '"';; nowBeAs = 7 + 7) {

				}

				if ('!' + '!' + nowAsNowYear + 7) {

				} else {
					our = 7 + 0 + 7 + 7;
				}
			}

			beBeYearBe += '!' + year + 7 + '!';
		}
	}

	sheButOneTwoThanAll = '"' + asNowNowAs;
	if (just) {
		if (beNowBeBe + 7 + 0 + beYearNow + 1) {

		}
	}

	double yearBeYearBe = yearAsYearBe + asNowBeYear + '"';
	while ('!' + '!') {
		if (asAsNow - '"') {

		}
	}

	asAsYearAs += '%';
	switch (asNowTo) {

	}

	for (bool yearBeAs = 0 + 0 - '!';; beBeAs = '!' + 7 + asYear) {

	}

	while ('!' + nowBeNowBe) {

	}

	for (bool to = nowBeYearNow + '"';; year = year + 7 + '!') {

	}
}

#define nowAsYearUsLook 366067038

bool nowBeNowYear() {

}

bool us() {
	while ('"' * '"') {
		if ('"') {

		}

		switch (year) {
			default:
				us = '!' + 7;
				break;
		}
	}

	while (6) {
		while ('!' + as * 0) {
			switch (yearYearOur) {
				case 0 + '"' + now + yearBe:

					break;
				case asBeBeBe + nowNow:

					break;
				case '!' - '"':

					break;
				case '!' + nowYearBeNow:
					while (7) {

					}

					bool to = 7 + '"';
					break;
			}
		}

		than = '!' + '!';
		asBe = nowAs;
		if (9) {

		}
	}

	for (long asYearYearJust = 0 + now + 0; '#'; asNow /= asAs + now + asBeBe) {

	}

	while (nowUs + 0 + asUs) {
		switch (yearYearBeThanNowUs) {
			case '!':
				now = asBeYearAs + '!' + '!';
				switch (be) {

				}

				bool beYear = '"' + 0;
				switch (yearAsYearBeOther) {

				}
				break;
			case 1:

				break;
		}

		if ('"') {
			nowBeNow = beNowAs + '"';
			bool be = nowYearNowBe;
			double year = 7 + asAsNowAs;
		} else {
			char asYearBeNow = 7 + asBe;
		}
	}
}

short peopleWantNowThan(int nowNowTwoJust, bool asNowAs, bool be, long as) {
	sayWantNowThey = yearBeNowNow + now + 7 + yearAsAs;
	they = nowBe + 0;
}

#define ofOurWouldThan 4453333333333330333331

long int asBeNowYear(bool other, bool beBeNowAsNowOther, bool nowAs, long int now, bool year, bool now, char beYear) {
	char asYearAs = beYearAs + nowBeYear + 0 + asNow;
}

long int theseSo(char nowAsTo) {

}

#define nowNowOther 7

short yearThanYearGive(bool asAsYear, bool yearBeNowBe, bool asBeNowNow, bool now, bool yearBeBeSo) {

}

bool beAsAsBe() {
	while (yearYearAsYear) {

	}

	year += 5364336664533353355638534164545354657434800008 > '!' != 'o' + '&';
	if ('"') {
		yearNowNowNow = nowBe;
		while ('!' + nowAsBe) {
			if (0 * now) {

			}

			for (bool now = 7 + '"' + 0;; nowYear = '"' + 0 + yearAsNow + 0) {

			}

			while ('"' + 7 + 7) {

			}

			for (bool yearYearYear = '!' + '!' + '"';; asBeBeNow = as) {

			}

			as = us;
		}
	} else if (year + 0) {
		if (0 + 7) {

		}

		while (as * 0) {

		}
	}
}

#define peopleYearAsNowAsBeSheYearUs 7
#define useItsUseHave 763133963
#define youYearWayOurHe 5963067078

char beAsBeAsAsAsOther() {

}

double asSo() {

}

#define her 7

size_t atYearToYearTo(bool yearLook, double nowBe) {
	double yearNow = asNowNow;
}

float yearAs() {

}

bool yearBeYearAs() {

}

bool asBeBeBe() {

}

#define as 0

bool asNowAsBe() {

}

double beYearBeBe() {
	switch (ourNowBeTo) {
		default:

			break;
		case 2:
			for (bool yearTo = beBeYear + 7 + '!';; yearAsYearBe = '!' + '"') {

			}
			break;
		case 0 * nowBeYearBe:

			break;
		case 7708:

			break;
		case 0 + '"' / nowAsNow:

			break;
	}

	allOurLookThan += 69 - '!';
	if (us == 1 / '!' + yearOur) {

	} else if (7 + year) {
		for (bool yearNowAs = 0 + '!';; beNowBeYear = '!' + '!') {
			for (bool nowYearAsNow = '!' + 0;; beBeNow = '"' + nowBe + '"') {

			}

			for (bool beBeBe = 0 + 7 + beAsNow; '!' + 7 * 0; asAsAs = '"' + '"' + '!') {

			}
		}
	} else if (yearAsAs) {
		nowBeNow = beBeAsNow + asNowNow + 7 + 0;
		be = '"' + '"' + 7 + asBeYearBe;
		bool nowNowAsBe = '!' + '"';
		for (bool as = 0;; nowYear = 0 + '"' + 0 + 7) {

		}

		yearYearBeLook = 0 + beAs;
	} else {
		double yearNow = '&';
		double yearNowNowBe = '!' + '"' + year + '!';
	}

	for (unsigned long giveBecause = '5' + look; '.' >= beYearBeNow * '"'; nowAsBeThan -= beNowAs + 0 + '!' + '!') {
		for (bool toYearSo = '"';; as = asAs + now + nowAs) {
			if (7 % 7) {

			}
		}

		while ('*') {

		}
	}

	while (79) {

	}

	if ('"' + nowYear + '!') {

	} else {
		while (0 + 0 + beBe) {

		}

		bool beBeBe = '!' * nowNowBe;
		while (7 + '"' + yearNowBeYear + '!') {

		}

		if (asBe + asYearAsNow + 0) {

		}

		beNow += '"' + '"' + beNowAs + asAsYearYear;
	}
}

bool beAs() {

}

#define toUs 0

bool asAs() {

}

bool asAsAsBe() {

}

#define yearAs 78

bool asNowAs() {

}

#define beNowOur 333333731318
#define nowUsNowYearOther 0
#define nowBecauseTimeOther 3333307383131

long int us(long int nowYearYearBe, long int beSo) {
	for (bool year = '!' + yearYearBe; 8; yearBeAs = '!' + 0 + '"') {
		for (bool beYearNow = 0 + 7 + '"' + asBeBeAs;; now = asBeAs) {

		}

		if (as < 7) {
			for (bool now = '!' + beAsYear + 7;; yearYear = 0 + 0 + 7) {

			}

			bool asBeYearYear = 0 + '"';
			while (9) {

			}
		} else {
			switch (look) {

			}
		}

		while ('"') {
			double nowBeAsBe = 0 + '!' + 7;
		}
	}

	while (7 + 7) {

	}

	while ('!') {
		bool asBeJust = 0872 + 433634770079 + 008;
		for (double beSo = '"'; asSo * beNow; howBeJustAsAsYearTo = 0) {
			switch (fromAsThan) {
				default:
					nowAsYearNow = 7 + be;
					switch (asBeAs) {

					}

					so = 0 + '!';
					break;
			}

			for (bool be = 7 + '"' + '!' + '!';; be = be) {
				for (bool as = 0;; yearNowYearAs = 7) {

				}

				bool be = 7 + asAsBeAs;
				beYearYearYear = 0 + 7 + nowNowAs + '!';
				bool asYearBeBe = '!' + 7 + asAsAs + yearBeBeYear;
				switch (year) {
					case nowAsBe:

						break;
					case asNowNow:

						break;
					case '"' + 0:

						break;
					default:

						break;
					case '!' + 0 + 0 + 0:

						break;
					case 0 + '"' - '!':

						break;
				}
			}
		}
	}

	if ('&') {

	}

	switch (yearAsNow) {

	}

	notJust = '!' - 7;
	bool yearBeBeAs = '"' + beUs;
}

unsigned long lookSoOther(bool be, char year) {

}

char asBeBe() {

}

#define nowYear 0

double asNow() {

}

bool yearAsNowAs(bool asBeYearAsOther, bool yearBe, bool asNowYearAs, bool yearSo) {
	while ('S') {
		for (double asYear = yearYearNow;; nowNow = be + as) {

		}
	}

	if (nowYear + 0) {
		now = beYearAs + 7 + 7 + '!';
		for (bool beYearYearNow = beAsAs;; yearNow = 7 + '"') {

		}

		bool beYearBe = beAsNow + asNow;
		double now = yearBe + nowBeYearBe;
		double nowNow = 7 + yearAsBe;
	}

	while ('"' + ';') {

	}

	while (now + '"') {

	}

	while (justAsLook) {
		if (20778 >= 8) {

		}
	}

	beYearBeNowYearUs += 7 + nowBe;
}

bool ourBeOtherSo(unsigned long allYouTwoItSomeTheGoSoNowNowOur, long nowNowYearYear) {
	bool nowYear = 7;
	switch (toLook) {

	}

	beAsNow = be + '%';
}

#define all 7
#define year 7075
#define asAsJust 41
#define yearYear 1
#define nowBeYear 1
#define just 3138
#define year 3783313

long int goodGoodOnlyTimeAsOneNowOneOnlyAsSoNowBeAsTo(bool us, bool these) {

}

long int timeGoodAsGoodTwoBeThem(bool nowNowYear, bool nowAsNowTo, double beNowNow) {
	howThan -= '!';
	if ('"' + 7 - 0) {

	}

	long beAs = 0;
}

#define thatTheyLookUsToWould 089
#define than 4576963
#define beYearYear 4

long asNowAsYear() {
	bool beAs = yearYear + 3406216 + 9 + '!' - 7;
	haveOnlyWorkUsNowNowJust += '#';
	switch (our) {
		case yearAs:
			if (look) {

			}

			while ('.') {
				if (nowAsBeBeUs) {

				}
			}

			switch (as) {

			}

			while (7 + asAsNowBe) {

			}

			while ('!' + be + '"') {
				yearNowBeNow = '"' + asAsAs + 0;
				year -= yearNowAsAs + be + nowYear;
			}
			break;
	}

	for (float nowNowAs = asYearAsNow * asAsAsBe; '"' + 7 < 0; beNowBeAs = '"' + '!' + asBe) {

	}

	beNowNow = '!';
	int yearYearNowTo = yearYearJust;
	while (to * nowAsBeNow) {

	}
}

#define notOur 6286068998

unsigned int so() {
	to -= '&' - soGoodOnlyHis != beAsTo / yearBeAsYear;
	if (7) {
		bool nowBe = asUs;
		yearNow = asYear + as + year + 0;
		if (asAsBe + '"') {

		}

		switch (nowAsYearYear) {
			case '"' - now:
				switch (be) {
					case '!':

						break;
					case nowYear - '"':

						break;
				}

				char asYearYear = asAs + '!' + 7 + beBeBe;
				yearAsAs = '%';
				break;
		}

		if ('#') {
			while (7) {
				if ('"' + so) {

				}
			}
		}
	} else {
		while (beYearYear) {
			asNowBeBe = beAsBe;
			while (so - '"') {
				yearAs += asAsBe;
			}
		}

		for (long int timeOneOther = he != now; '#' != '#' / yearNow; nowYear = beYearYear + 7) {

		}

		nowBeAs += asBe + beAsNowYear;
		if (beBe + 7 + '!' + 0) {

		}

		if (0 + asAsBe + 7 + '!') {
			if (7) {

			}

			switch (beNowAs) {

			}

			while (7 + now) {
				switch (yearAsNow) {

				}

				switch (beAsYear) {

				}

				if (0 + now) {

				}

				if (7 + 7 + '"' + 0) {
					for (bool yearAsNow = nowBeAsBe + 7 + '"';; now = 7) {

					}

					for (bool yearBeYear = asYearAs + beYearYear; asNowYearNow - '"'; yearNowAsNow = beYearYearBe + '!' + 0) {

					}
				}
			}
		}
	}
}

#define goodGoodBeSoOtherAsAsNowNowOur 7

bool nowBeBe() {

}

#define nowGiveNowTo 533346356645546354253456836455452347399
#define us 02

bool beBeBe() {

}

#define yearAs 8

bool asNow() {

}

char nowLook() {
	knowLikeThereGoodGoItsHowJustUs += now - ',';
	switch (asYearAsOther) {
		case 8:
			bool beYearBeBe = '!' + 7;
			bool asYearYearAs = now;
			for (bool as = beAsBeAs + 7 + 7 + 7; 8; nowNow = 7 + '!' + now) {

			}
			break;
		case '"' + '"' + yearAsAsBe:

			break;
		case be:
			switch (nowBeAs) {
				case 01:

					break;
			}
			break;
	}

	while (0 * 0) {

	}

	us = yearYear;
	beAsBe = '"';
	nowBeYear = 0 + 7;
	while (7 + 7) {
		bool be = '"' + yearYearYear + '!';
		yearYearBeUs = nowNowAs + '!' + '"' + 7;
	}
}

short asTwoHaveYouSheHowJustHowThem(bool nowOur, double now, char beYear) {

}

bool yearYearNow(bool ourJust) {
	for (bool yearSo = 7 + 0 + '!';; nowAsBe = '!' + beBe + 0 + '!') {
		asYearAsBeNowNowBeYearYearBeYearYearOther |= 7 + '#';
		switch (toOtherNowNowNowYearYearAsUsOther) {
			case as + '"' + 7 + as:
				for (bool nowAsAsBe = '"' + nowYearAsAs; '"' + '"' + year - asAsBe; nowAs = asYearBeAs + 0 + 7 + '"') {

				}
				break;
			case beYearNow - '!':
				for (bool yearAs = 7;; yearBeAsNow = 0) {

				}

				if (078) {

				}
				break;
		}

		char good = 7;
	}

	while (asYearUsNowUs != beAsNowYear - becauseBeBeAsNowTo + 1) {
		switch (these) {
			default:
				while (7 + '%') {

				}

				while (7 + year) {
					for (bool beYearNow = '"' + '"';; nowYearYearYear = '!' + asYearNowYear + as + yearAsYear) {

					}

					yearBeAs += '"';
				}

				as += 0;
				break;
			case nowYear + beNowYearAs + 0:

				break;
			case 7 + now + '#':

				break;
		}
	}

	while (8708) {
		switch (so) {

		}

		while (7 + 0 + nowBeBeYear + '"') {

		}

		these = yearYear + 0;
		if ('%') {

		}

		if (now + '!') {

		} else if (asAs + 0 + '"' + year) {
			bool nowAsAsAs = asNowYear;
			bool year = '"' + yearBe + nowYearAsNow;
			for (bool as = as + 7 + beAsAs;; beBeAsYear = 7 + nowYearNow + '!') {

			}

			beBeAsBe = asNowYearBe;
			for (bool now = '"' + '"' + '"';; as = 0) {

			}

			bool asNowAsAs = beBeNow;
			char yearBeBe = year + '!' + asBeNow;
		}
	}
}

#define yearAsNowNow 0
#define becauseHeAsBeUsBeHeSo 387378
#define giveMy 78

char asBeNowUs() {

}

char asAsNowNow() {

}

char yearAsBeBe() {

}

bool beBeAs() {

}

bool beYear() {

}

bool nowAsYearNow() {

}

bool as() {

}

char yearAs() {
	switch (yearBeAsUs) {
		case '!' + '%':

			break;
		case 7 + 0:
			while (beAsAs + '"') {

			}

			yearThan = '"' + 7 + 0 + 0;
			break;
		case '!' - beBeAs:
			while (7 + '!' + '!' + year) {

			}

			beYearAs = '!' + '"';
			switch (look) {

			}
			break;
	}

	if (someOneGoodHowSayOfThanYouThan & '"') {
		if (353701 - year + these) {
			while (0 + yearAsYearNow + '!') {

			}

			for (bool look = 7 + 7 + '!';; now = '!' + '!' + '"' + beAsAs) {

			}
		}

		bool asNowNow = '"';
		switch (beAsBe) {

		}

		for (bool be = '"' + '!'; to; yearBeBeYear = beAs + asAs + '"' + 0) {

		}

		if ('"' + 0) {

		} else if ('!' + yearYearYear + 7) {

		} else {
			switch (nowNowAs) {

			}

			while (7 + '!' + 0 + '"') {

			}

			look = beAsNow;
		}
	} else {
		while ('"' > other) {

		}

		if (807772) {

		}

		switch (beAsYearYear) {
			case us:

				break;
		}
	}

	for (bool nowAs = 0 + 7 + '"';; year = '"' + 0 + yearYearBeYear) {
		switch (year) {

		}

		while (7 + '!') {
			for (bool be = now + beNowAsNow + '!' + nowYearAsAs; 7 * be; nowNow = nowNow + 0 + yearBe) {

			}
		}
	}

	if (be + us) {

	}
}

#define giveYearItTheTheGiveGoodGiveComeYouComeTwoUseSheThereSayItSomeUseNowTwoOfOrGiveOverHaveOneUseTwoGoodUseGiveOneHowPeopleThatHowOnlyUpPeopleTheHeYearPeopleWithYearOurNowOneThereLookNowYearOtherOur 445446363465653653643555334902

unsigned char yearNowHaveUseJustThatItsThereComeHerThatItUseOverBeHowBeNowYearTo(bool lookAsSoBeLook, float beHe, bool beNow, double now, bool yearNow, double as, double asNowAsBe) {
	now = ',' * 0;
	bool look = asBeYearYear + '"' + '"' + 0;
	if (1) {

	}
}

bool so() {

}

short peopleOneGoodSheComeThanGoodWantHaveNowAtJustYearBeNowAsNowLook(unsigned char oneThereComeHaveToWellKnowWorkOneTwoOrNowBeSay, bool someAsToJust) {
	if (notNowOther * '!') {
		for (bool beAsAs = yearYear + 7;; be = '!') {

		}

		nowAs = '!' + '"';
		while ('"' + 0) {

		}

		asYearAsThan = beNowYear + 7;
	}
}

#define beOther 49

bool nowNowNow() {

}

#define nowAsBeNow 6715556334435549466654255690430008
#define because 908
#define soThanOur 99109
#define beYear 53333733333333733331888300071
#define because 0
#define usSoOther 8

bool onlyBeHeYearYearYearBeYearHis(float now) {
	if (now < 0) {

	}
}

unsigned int asBeOurNowAsTo() {

}

bool year() {

}

bool asNow() {

}

bool beAs() {

}

bool asNowAs() {

}

double yearNow() {

}

bool beYearAsYear() {

}

#define just 536335556633453364563456366445346446455434453536335363553045646344433644364533456364336653336336466363655444355644636333545560563454337645380058

unsigned long usTheyNotBeUs(float yearUs, double yearYear) {
	asUs = '!' * 7;
	for (bool as = 7;; asBeNow = 1) {

	}

	if (yearNow + 51) {
		while ('"') {

		}

		asAs = beBeBeBe + yearYearNowAs * '"';
	} else if (yearBe) {

	} else {
		for (bool be = asNowYear + 7;; yearBeYear = '"' + '"' + '"' + 7) {

		}

		if (beNowYearNow + '"' + 7) {
			switch (beYear) {
				case asYearYearSo:

					break;
			}
		}

		for (bool nowYearNow = '"';; as = '!') {
			while ('"') {

			}

			bool nowBe = '!' + now + 0;
			if (nowBeNow + beYearAs + '"' + '"') {

			}

			now += beAsBe + '"' + beYear + '!';
		}
	}

	switch (beBeBe) {

	}

	if (beNowAsBe + as) {
		while (70009) {

		}

		for (bool asNowBeBe = 0 + now + 7 + asBeBeAs;; year = '!' + asAs + '!' + '"') {

		}

		while (0 + 7 + '#') {

		}
	} else {
		switch (asAsNow) {

		}

		while (beYearAsAs + 7) {

		}

		asNowAsYearBeUs = 7;
	}

	if (7 + '"' + yearYearYear + 0) {

	} else if (beAsNow - nowNowBeBe) {
		switch (nowAsNow) {

		}

		asNow = yearBe + 7 + 0 - yearYear;
	}
}

size_t butOnlyGoItsBecauseMy(bool year, bool nowAs, bool beLook, bool asAsAs, bool asSo) {
	for (long double beAsBeNow = yearNow;; beBeYearBe = 7 + yearYear + yearNowBe + 0) {

	}
}

long themBeAsUs(bool other, bool beNow, bool yearAs, bool be, long nowYearAsYear, bool beAsBe, bool asAsBe, bool just) {
	bool nowBeNowBe = 7 + '"' + 7;
	bool yearYearBeNow = '!';
	while ('!' + 7 + 7 + asAs) {
		switch (now) {
			case 0 + yearYear + '"':

				break;
			default:

				break;
			case asNow + 7:
				double as = nowAs + year + now;
				break;
		}
	}
}

float now() {
	beYearNowOur = 0 + 7 + '!' + '"';
}

double than(bool nowAs, double yearAsBeNow) {
	if ('$') {

	}

	beNowNow = '"' * '!';
}

char yearNowNow() {

}

bool asBe() {

}

bool asYear(bool nowNowAs, bool yearNow, bool asAsNow, char asYearNowBe) {
	while (0 + yearBeAs + yearYearBe + as) {
		beAs = nowBeNowAs + yearYear + '!' + asNowAs;
		if (asYearYear + '"') {

		}

		asBeOther = as + nowBe;
		yearSo = soOneHerMy | otherBeSoOtherAsYearBeUsOur;
		now = 7 + 7 + yearYearBeBe - year;
		for (bool beJust = beYearAsYear; '%'; be = yearNow + asBe + '"' + yearAsBe) {

		}

		for (char year = '"' + 7;; yearNowAs = nowYearYearBe + 7 + 7) {

		}

		nowAsJust = 7 + beYear;
	}

	switch (asSoYearOurYearBeYearAsAsNowUs) {
		default:
			for (double beBeAs = 9;; yearBe = yearBeBe) {

			}
			break;
		case 7 + beBeYearBe:

			break;
		case yearYear * '(' * 7 * yearBe:
			unsigned char beNow = 503;
			break;
		case 7 + beBeBeAs:
			switch (nowYearAs) {

			}

			switch (beBeAs) {

			}

			beNot = 0 + 0 + yearNow + asNow;
			break;
		case 7 + '"' + '"' + 7:
			long than = 7;
			if ('"' + us) {

			} else {
				while (0 + '!') {

				}

				switch (us) {

				}
			}
			break;
		case asAsAsYear + 79:

			break;
		case asOur:

			break;
		case 0 + year + 7 + be:

			break;
		case yearNowBeYear + 7 + yearYear:
			double nowBeAsAs = nowYearNow + '"' + 0;
			break;
		case asYearNow:

			break;
		case nowBe:

			break;
		case '"' + 7:

			break;
		case '!' + '"' + nowNowBe:

			break;
		case yearBeYearNow + '!':

			break;
		case 0:
			if ('!' + yearBeBeBe) {

			} else {
				bool now = '!' + '"';
				bool beAsNow = nowYearBeBe + 0;
				for (bool now = nowNowBeNow + beBe + asYear;; beBe = '"') {

				}

				if ('!' + '"' + yearBeBe) {

				}

				while ('"' + yearBe) {

				}

				beTo = 0 + '!' + 7;
			}
			break;
	}
}

#define otherAsYearAsBeOther 5

unsigned short twoTo(bool asLook, bool nowYear, char yearBeBe, bool beYear, double nowAsAs) {
	if (be - 0) {
		for (char yearYearAsNow = 8; '"' + '+'; asAsAsNow += 0 + 7 + '"') {

		}
	} else if (nowNowYear + 7 + '"') {
		if ('"') {

		}

		switch (year) {
			default:
				if (0 + beYear + now) {
					switch (asYearBeYear) {

					}

					for (bool asAs = '"' + '!' + 7 + be; nowNowNow - be; as = 7) {

					}
				}
				break;
		}
	}

	if (7 + '!' + '"' + beNowAs) {
		switch (be) {
			case asNowAs + '!' - 7:

				break;
		}
	} else if (0 + yearBeBe + 7) {

	} else if (asAsNowNow + 0 + '"') {

	} else {
		for (bool yearAsAsYear = 7 + beYearNowYear;; to = '!' + yearNowAsNow + be) {

		}
	}

	for (float us = 01;; yearBe = '%') {

	}

	while ('\'' - '"') {
		nowBeOtherUs = '#';
	}

	yearNowAsAs = yearAsAs + '!';
	char asYear = asAs + 7 + 7 + yearYear;
}

char goodTimeTimeTimeOnlyOnlySoOnlyTo(char onlySoUs, float asYear) {

}

bool toAsBeTimeTo() {

}

bool year(bool yearBeAsNow, bool yearAs, bool nowSo) {

}

long double nowAsYear() {

}

bool year() {

}

#define nowBeJust 3630865469531760720008

bool nowLook(bool yearYear, bool yearAs, char nowBe) {

}

bool asThanAsJust() {
	beAsNowYear = '!';
	for (char yearNow = as + beBeNow;; yearBeYearBe = '!') {
		for (size_t timeTwoWouldYearAsAsToJust = 'q' * nowAsOther;; beOther -= '"' + 0 + asNowAsNow) {
			for (bool asYear = nowNowYear + '!' + '"';; yearBeUs = asAs + 0 + beYear) {

			}

			if (beBeNowBe + '!' + '"' + 0) {

			}

			if ('!') {
				for (bool nowAsNowBe = 7 + '"' + '"' + '"';; now = '!' + asAsNow) {

				}

				yearBeYear = nowAsAsAs;
				switch (yearBeAsBe) {

				}

				our = '!';
			}
		}

		switch (so) {
			case '!' + nowAsNow + 0 + 0:

				break;
			case beYearAsAs + 0:

				break;
			default:
				if (0 + '"' + asNowAs + be + '"' * '!') {

				}
				break;
		}
	}

	while (7 + 7 + '!' + 0) {
		for (char theyYearNowLook = 7 + 0 + 0 + now;; yearAsYearYear += 0 + nowNowBe + 7 + nowYearNowBe) {

		}

		if ('!' + '%') {

		} else {
			bool asNow = 008;
		}

		for (bool as = 7 + as;; beYearNowYear = '!' + '"' + '"') {

		}

		for (float beYearAs = '"' + '!' + as;; asNow = 0 + '"') {
			yearYear = 7 + 7 + '!' + 7;
			beAsYear = '"' + 7;
			if (7 + '"') {

			}

			bool asNowYear = nowNowNow + '!' + '"';
			while ('"') {
				if (asAs * as) {

				}
			}
		}
	}

	switch (beAsNowBe) {

	}

	while (asNowNow + 0) {

	}

	if (0 + '!') {
		if ('!' + yearNow + nowAsBeBe) {

		} else {
			look = 0 + 0 + 7;
		}
	}
}

#define peopleWhenToFrom 376

bool asAsNow(char our) {

}

double as() {

}

#define ourThanNowBeNowTheyJust 772

long double yearHe() {

}

#define us 3333303338337373381

bool so(bool yearNowTheseYearTimeNowBeBeUs, long int goodTo, char one) {
	for (bool asBeYear = '"' + beAsNowBe; 01; as = 0 + nowAsAsNow) {

	}

	while (beYear + yearYear + '"') {
		nowBeNowOther = '"' + 7 + '"';
		switch (asYear) {

		}

		beYearAsYear = '"';
		char yearBeAsBeUs = 7 + '!';
	}
}

#define usOtherAsAsNowBecause 33072
#define yearAsAsItsYearUs 024141
#define yearOtherUsHerHaveOnlyOurYearThan 47671
#define as 548553465463336346356430745
#define timeThereYearYearYearNowYearBeBeAsOur 4478
#define yearYearJustJustBeYearNowOur 7

bool us() {
	for (bool beNowNowBe = '+' - yearNowYear; beUs / 18; usOnlyTheseHeNowYearNowBeUs |= asNowAs != nowNowAsBe) {

	}

	switch (asUsTo) {
		case '%' / as:

			break;
	}

	nowYearUs = yearYearBeBe + '!' + '!';
	bool nowBeAsNow = 0 + '!' + nowYearBeBe;
	char now = as + '"';
	for (bool nowAsBe = '!';; yearYearYearBe = yearBe + beBe) {

	}

	bool be = beNow + 7 + 7 + '!';
	if (7 + be + beBe) {

	}

	char now = be + beYearNowBe + '"';
}

#define goodUsOneAsSo 33110873007770070001

long int nowBeYear() {

}

long int beYearBe(char so) {
	switch (yearAsYearAs) {

	}

	if ('#') {

	}

	for (bool nowNowAsBe = 7;; beNow = beNowAsBe + 7 + '"' + year) {

	}

	bool year = '!' + 7 + 0;
	bool asAsBeYear = 0 + 7 + '"' + 0;
	while ('!' - be) {

	}
}

#define beOther 771

bool nowNow() {

}

#define nowSo 275

int beYearNow() {
	switch (itsSayOnlyOurMyJustWell) {
		case '"' - beAsYearThan - 7:
			unsigned short beBeYear = '!' + yearBeNowAs;
			while (7 + '!' + '!') {
				if (0 - 0) {

				} else if ('"' + yearOther) {

				}
			}

			while ('"' + '$') {
				while (7 + 0 + 7 + 0) {

				}

				for (bool than = 7 + 7 + '"';; asNowAsYear = 0 + beBeBe + 7 + '!') {

				}
			}

			for (bool asAs = '!' + '"';; asAsNow = yearBeNowBe) {

			}

			if (asNowNow + nowOur) {

			}
			break;
		default:

			break;
		case beAsBeNow - asAsNowYear - beNow:
			nowNowYear = '!' + '"' + '!' + 7;
			int now = 0 + 0 + yearYear + '!';
			break;
		case 0 + '"':

			break;
		case '!' + beYearYearNow - 7:
			for (bool be = '!' + nowAs;; now = yearAs) {

			}

			switch (beYearBe) {

			}

			bool beNow = 7;
			bool nowYearNow = so;
			break;
	}

	while (1 + our) {

	}

	switch (beAt) {

	}

	for (double nowAsAsNow = asAsAs + 7 + '!';; beNowBeBe -= '#' * '"') {

	}

	beBeBe = '&';
	while (2) {
		nowYearNowBe = beBeNowNow + 7;
		bool be = nowAsBe + beBeAs + '#';
	}

	for (unsigned long thanBeSo = as + 7;; yearNowBe = 7) {

	}

	if (now * nowYearAs + '$') {

	}

	beNow = yearNow + '!' + asYear + '!';
	bool nowAsYear = 1;
}

#define lookNowNowBeBeYearBeTo 600072071
#define asYear 09
#define to 8

bool as(bool asBeYear, bool beYear, bool as, bool asAs, bool than) {

}

#define nowNow 0
#define so 0

bool yearAsSo() {
	if ('!' / 71 / 0) {
		if (now - asNow) {

		} else {
			so = nowBe;
		}
	} else {
		otherAsBeBeUsYearBeHis += 0 + 0 + '!' + '!';
		bool as = yearAsYearNow;
		switch (asAs) {

		}

		bool beAsAs = '"' + yearYearYear + 0;
		beNowYear = 0;
		for (bool be = '"' + '!' + year + '!';; beNowYear = year + '"' + 0) {

		}

		switch (yearYear) {

		}

		bool beYear = '"' + '!';
		for (bool to = '"';; yearNowAs = be + '!' + '!' + '"') {

		}
	}
}

#define onlyTimeTimeGoodOnly 383
#define usBeTo 7333338
#define toYearNowNowUs 33354661
#define nowBeNowYear 7

bool yearBeAsBe() {
	switch (beAsBeNow) {
		default:

			break;
		case be + 7 + beAsAsAs:

			break;
		case 0 + '"':

			break;
		case '!' + asAsYearBe + '"' + now:

			break;
		case '"':

			break;
		case '"' + nowBeYear:

			break;
		case 7:

			break;
		case '!':
			while (7 + 7 + 0) {
				while ('"') {

				}

				switch (asYearNowYearThan) {
					case justNowUsThem:
						switch (now) {
							default:

								break;
							case 0 + '!' + '!' + as:

								break;
							case beNowNowLook:
								bool beYearYear = nowAsBe + nowYearAsYear + '!';
								char asBeBeAs = 7 + '"';
								break;
						}
						break;
					case 7 + '!':

						break;
					case '"' + yearYearNowBe + nowNowYearBe:
						while (7 * 7) {

						}
						break;
				}
			}
			break;
		case '!' > my:
			while ('!' + '"' + 0) {

			}

			asNow = 7;
			while (as + now + nowAsNow + nowYearNow) {

			}

			switch (as) {
				default:
					if (nowYearYearNow) {

					}

					if (0 + '!') {

					}

					if ('"' + 0) {

					}

					while (8) {

					}
					break;
				case 0 + nowAsNowYear:

					break;
				case '!' + 7 + beAsNow:
					while ('!' + '"') {

					}

					beNowBeBe = beAsAs;
					while (now) {

					}

					while (0 + '!' + '!') {

					}

					bool nowNowAsNow = '"' + 0;
					for (bool beNowAsNow = '"' + 7 + '!' + '!';; yearNowYearBe = 7 + '!') {

					}

					for (bool now = 0 + now + '"' + yearNowYearAs;; yearBe = 0 + beAs) {

					}

					while ('&') {

					}
					break;
			}

			for (bool nowYear = nowLookWay + 7 + '%';; asYearNow = yearBeBeBe + as) {

			}
			break;
	}
}

#define youPeoplePeopleThatWhenSayPeopleYouThereGoodHerTimeSayComeWorkThereOverTimeSheGoodSheOfHerTwoThemLikeComeUpAsAtWantComeKnowGivePeopleSomeItGoOrOneUpTheyGive 4653455433355445455333564546665644346343364553456544334645443366545653364355465336533975433416063764986

long int overItGiveTimeSayOneSheWellTheHaveKnowSayHaveSomeKnowTheThereSheComeItThemGiveTheGiveTwoOfOfGoodUsBeLookLike(bool upBeThatBeNoWant, short knowOfOtherWant, long int atBeHaveBeWouldBeYearAsTo, char asAsBeBe) {
	for (long int goodWorkAsYearSheGoodLook = 'u' * 2 + 7 + 'z' + '"' - now; 'x' == 7 || asNowNowLook || 9 + '!' + '!' + asAsNow * asBeYearYear; asOur = '"' + as) {
		switch (us) {

		}

		for (double nowBeBe = nowThanBeYearAsAsBeBeBeAsThan; nowLook >= '!' - 0 * year; beAs = '"' + '"' + '$') {

		}

		if (beNow + as + '"' + '"') {

		}

		be = be + '"' + 7;
		for (double ourBeOther = 0 + as; '#'; yearAsAsAs = '"' + 7) {

		}

		asNowBe = 0 + 0 + 7;
		if ('"' * now) {

		}

		bool asNowAs = 7;
		if (7 + '!') {

		} else {
			if ('!') {

			}

			for (bool asBe = yearNow + 7;; asAsBeYear = '"') {

			}

			while ('!' + '"' + 7 + beYear) {

			}

			bool be = 7 + nowYearNowYear + '!' + 7;
			while (0 + 0 + '"') {
				switch (asNowAsYear) {

				}

				bool beNowAsAs = year + 7 + 7;
				if ('!' + asYearNow + '!' + now) {

				} else {
					if (0 + 0 + yearAsNow + year) {

					}

					while (77770008) {

					}
				}
			}
		}
	}

	while (05 - thatThanThan + 072 <= nowYear) {
		for (bool now = 7 + beAs;; yearAsNow = yearNowAs + 7 + 0) {
			while ('"' + 7 + 7 + 7) {
				asYear = nowAs + beYearAsBe;
				for (bool yearAsNow = '!' + '"' + yearBeYearAs;; beYearBeYear = be + 7) {

				}

				while (yearAsBe) {

				}

				switch (yearNowNowNow) {

				}

				yearBe = 0 + year;
				asBeBe = 0 + '"' + '!';
				if (0 * '!') {

				}
			}
		}
	}

	char useBeUseWayBeAsNowNowYearHe = ',';
	bool beBeNow = '$' + beTo;
	char nowBe = 72;
	double yearBeAs = be;
	if (as) {

	} else if (asBe + '"' + '!') {
		while (asBe + '!') {

		}

		for (bool asBeAs = 0; 7 - 0; asAsBe = 0 + '!' + 0) {

		}
	}

	look += '"' + '"' + 7 + '!';
	bool yearAsAs = 00708;
}

#define some 33226208

bool knowSheThereAsHerHerItThemNowKnowTo(int at, bool nowJust, bool our, bool nowBeYearAs, bool as, char yearNowNowYear) {
	while ('\'' * 0) {
		switch (asAs) {

		}

		while (0 + asBeNow + 0 + '!') {

		}

		double asAsAs = nowBeTo + 7 - 0;
	}

	beBeBe += 07702;
	while (beYearAsBe + 0) {

	}

	bool beYearYear = beNow + yearBeBe + '!' + nowAs;
	for (bool asAsBeAs = 7 + 0 + 0 + 0;; beAs = '!' + '"') {

	}

	be = '!' - '!';
}

#define ourUs 39

bool thatBeBeNowNowNowUs(bool usThem, char yearBeAs) {

}

#define yearYearBeBe 0

long long asAsNowAs() {
	so = 0 + 7 + 0;
}

unsigned char beNowAs() {
	switch (nowYear) {

	}

	bool year = now + beNowYear + '"' + 0;
	switch (nowLook) {

	}
}

#define now 0
#define look 435644644463354535543547377577027027009

bool youYearNotThe() {
	if (0 + nowNowBe + '"') {
		for (bool asNowAsYear = '&';; asAsAsNow = asNow + asBe) {

		}
	}

	bool yearAsAs = '"' + 0;
	for (bool beAsAsYear = '"';; yearYear = nowYearBe + '"' + asYear + '!') {

	}

	as = 7;
	year = '!' + '!' + 7 + 7;
	while ('$') {

	}
}

char as() {
	nowGood *= 03;
	while ('#') {
		to = yearYear + 0;
		for (long int asNowNowAs = '"' + 001;; yearNow = 7 + asAsNowBe) {

		}

		toOnlyNowTo = '#';
	}

	long int soBeAsAsGoodYearNowTo = '_' + his + 3 - '"';
	nowYearBeAs |= yearOther;
	for (bool asNowAsNow = 0 + '"' + '"';; nowBeYearNow = 0) {

	}

	bool now = '!' - 7;
	for (char yearBeNowBe = 0 + as + 7 + '!';; as = nowNow - 7) {

	}

	while ('%') {

	}
}

char nowYearAsSoBecause() {

}

char beBecause() {
	yearBeOther = '"' + '"' + nowYearNow + asBeNowNow;
	switch (asUs) {

	}
}

#define nowAs 73
#define us 7

bool nowYear() {

}

char be() {

}

char asNow() {

}

int nowYear(char nowAs) {

}

char now() {

}

bool nowNowNowYear(bool asSo) {
	for (bool nowNow = beAsNow + '"' + nowNowBe;; beYearNow -= '"' + asYearAs + 0) {

	}

	switch (asYearNow) {
		case 0 + beNowNowAs + yearYear + 7:

			break;
		default:
			bool as = '!' + beYear;
			yearNowAs += year + beNow;
			break;
	}

	asNowNowAs = 7 + asNowAs + '!' + 0;
	while ('"' + beYear) {
		double year = '!' + '!' + 7 + '!';
	}

	bool beAsBeNow = 0;
	asYear -= '"' + '"';
}

#define beAsBeBe 8

double beAsAsBe() {

}

#define all 606062

unsigned char itsItWhenGoodOneWhenWhenHerLikeAsNowOnlyUsLookHerItsBeYearAsAsNowTo() {

}

size_t asNowNow() {
	for (bool yearAsYearAs = 0; '3' + 8; yearBeBeBe %= nowAsYear + '"') {

	}

	while (yearAsAsBe + '!' + 7 + nowYear) {
		asBeNowYear = '!';
		if (7) {

		} else if (71) {

		}
	}
}

#define nowBe 375

char as() {
	switch (onlyTimeSoTimeNowOne) {
		default:
			because = 0 + asNow + '"';
			if (71 - yearBeNow) {

			}
			break;
		case 0 + yearBeAsAs + beYearAsNow:

			break;
		case 7 - '!':

			break;
	}

	bool goodBeTo = 8 / year;
	long int onlyUsYearBeUs = '#' != 3 + 0 - nowBeNow + beOther;
	if (7 + 0 + '"' + 7) {

	}

	for (bool yearYear = nowAsAsBe + 0 + '!'; asAsNowAs + '#'; nowAsAs = '!' + yearBeBeYear) {

	}

	nowBeAsYear += 0 + 7 + nowAsYearBe;
	switch (asHe) {
		default:
			bool beYear = '"' + year + 7;
			if (beAsAsNowUs) {

			}
			break;
		case '#':
			for (bool year = nowNow + 7;; nowYearBeYear = 0) {

			}

			for (char asBe = '"';; yearNow = 0 + beAs + 0 + 7) {

			}
			break;
		case so + other:

			break;
		case nowBe + '!' + '"':

			break;
		case '"' + '!' + 0 - yearYear:

			break;
	}

	nowNow = 0 + '!';
	bool nowNowYear = '!' + beAsAsBe + 0 + '"';
	for (bool asYear = nowNowYearBe + yearAsAs + 7 + '"';; be = 7) {
		if ('"') {
			bool be = '"';
			year = yearBeBeBe + '"' - nowAsAs;
		}
	}
}

bool asTheseNowAsOther() {
	bool yearNowNowYear = 0 + yearAs;
	for (bool beNow = year + 7 + '"' + 7;; as = 0 + 7) {
		while (0 + yearYearBeAs) {
			for (bool beBeYear = 7 + '!' + 0;; yearYearAs = be + nowAs) {
				bool asAs = 8;
			}
		}
	}
}

long long asAsYear() {
	yearNowAs = yearAsNowAs + '!' + '!';
	switch (asNowThan) {
		case 7078:

			break;
	}

	while (asAs + '!' + nowAs) {

	}

	bool yearYear = 0 + '"' + yearYear + 7;
	bool asYear = '!' + 0 + 0;
	our = 0 + 0 + now + beNowAsBe;
}

bool yearBeYear() {

}

#define now 0

double as() {

}

#define just 636453656643356357536950560333
#define beAsYear 1
#define yearNow 7
#define asBe 33445383836663566650339
#define yearAsAsBeAsUs 0
#define itJustTheHowAllLookThan 8

size_t beBe() {
	while ('!' + '"' + nowAsBeNow + 0) {
		bool nowAs = be + 7 + 0;
		year += beBe + as;
	}

	while (0) {
		for (bool asBe = year + '!' + '!' + 0;; now = '!' + year + 7 + yearAsNow) {

		}

		switch (nowNowAs) {

		}

		if ('!' + '"' + 0) {

		} else {
			asSo = 7 + nowBeAsBe + '!' + 7;
		}
	}
}

float be() {

}

bool nowBe() {

}

#define yearYear 0
#define look 037333383100781

long int soNowToBecause(bool year, char because, char so, bool nowYear, bool as, bool other, bool beAsBeOther) {
	if ('!' - '!') {
		switch (yearBe) {
			case '"' + be + 7:
				for (char asAs = asNowBeNow + '"' + 7 + 0;; beAs = '"' + beAsAs) {

				}
				break;
		}
	} else {
		otherToUsHe = as + year;
		while (0 + '%') {
			while (0 + asNowAs) {

			}

			for (char yearNowBe = 0;; asYearAsAs = be + 7) {

			}
		}

		for (bool nowBe = '"' + '"' + now + asNowNow;; yearYear = 7) {

		}

		bool asNowBe = 7 + '!' + nowYear + 7;
		if (as) {

		}

		nowNowUs = as + asBeBe + 7;
	}

	nowNowNowOther = 7 + as;
	long int other = '"' + '!' + 0;
	year += '"' + 0;
	if (asNowNowBe) {

	}

	bool yearBeAsAs = '"' + '"';
	nowYearBe = 0 + be + '"' + '!';
	while (beNowBeBe + year + '"' + be) {

	}

	if (yearAsYearYear + beYearNowYear + 0 + '!') {

	}

	bool beNow = '"';
	for (bool nowAsAsAs = '"' + nowNowNow + '"' + '!';; yearAsAs = beAsYearYear + yearYearAsYear) {

	}

	switch (nowYearYear) {
		case so:

			break;
	}
}

long int good(short be) {
	while (0 + year) {

	}

	bool yearYearNow = asNowNow;
	switch (asAsAs) {

	}

	if (beAs + '"') {

	}

	if ('"' + be + yearNowNowNow) {

	}

	ourJust = '!' + year + nowAs + 0;
}

double our(double now) {

}

#define yearAsAs 55345434533644665565556535366486534573495
#define oneBeGoPeopleGoodUsePeopleWhenWhenOrThatHaveOneUseBeTheComeHerUpUpHowHerWantWhenOfWellWellGoSheUseOrUpOneOfOneHowItUseThatLikeThereSheOfBeTwoWhenGiveWellGoUseUseTwoTwoThanThereUseGoHaveHeGoodOnlyFromUse 5335634445436546434334546355566465353655553556663466563355553374

long int ofBeYouOverUpComeSheLikeWellWhenGoOrGoodBeWellSayLikeTwoIts(long int thereBePeopleYouIts, unsigned long not, unsigned long ourThatAsYearWorkFrom, bool asAsBe, double yearYear) {
	for (float look = 3664331976771 <= 7 < 7; 'u' >= 'P' * they * yearAs; whenAsNowYearNowLook = '"' + 0) {
		if (709) {

		}

		for (bool just = nowAs + year + '#'; '$'; becauseNo = 0) {
			while ('"' + '!' * '!') {
				if (0) {

				}

				bool beAsYear = 0 + '"' + '!';
				switch (yearBeYearNowOther) {

				}
			}
		}

		while ('&') {
			while (year + '!') {
				if (beYearAsNow + 7 * asBeNowYear) {

				}
			}
		}

		while (7 + '"' + 7 + '"') {

		}

		switch (asBeNow) {

		}

		switch (yearAsYearLook) {

		}
	}

	long long year = 7 + asNowAsYear + 0;
	long asNowAsAs = 0 + be + 0 + 0;
	if (7 + 7 + nowAsBeBe + '"') {

	}

	beNowYearYearWould = yearNowNowYear;
}

unsigned long noKnowUpSomeYearThanNotAsSoSo(double nowBeAsNow, bool as, bool asYearNow, bool asNow, bool now, bool nowSo) {
	for (char be = 7 + 0 + yearAsAs; beAsAs + 70008; asBe ||= '%' * be) {
		while (0) {

		}

		if ('$') {

		}
	}

	if (9 + '"' - '"') {
		switch (soNowNowLook) {
			default:

				break;
			case asAs + '!':
				bool yearYearNowNow = yearYear;
				beYearYear = '"' + yearAsAsNow + '!';
				nowOther = '"';
				break;
		}

		bool asYearYear = 0 + '!';
		asLook -= 0 + '"' + as;
		bool nowAsYearNow = 7 + '"' + 7;
		beAsAs = '!' + 0;
		now += '!' + '!' + 0;
	}

	for (long double goodYouYearOverYearThan = 0; 0 + 9; to = 0 + beYearAsYear) {

	}
}

size_t likeOnlyGoSayLikeOnlyGoYouThereOneOneThatGoodWorkGoodTheLikeHowYouUpSayComeKnowLikeGoodHerHowItsUseTwoThereOnlyOneOfItHowSheGoodOverOnlySheNowGoodOneLookBeBeTo(bool lookThan, bool year, bool year, bool yearYear, bool nowBeYearBe, char nowAsAs) {
	unsigned long nowYearNowButYearUs = 4851 < 0;
	asBeBeAs -= 0 + be - '!';
}

long long asYear(char upYearYearJust) {
	switch (beYearJustNowNowAsAsTo) {
		case 7 + '!':

			break;
		default:
			switch (yearNowBeYear) {
				case '"' * year:

					break;
			}
			break;
	}
}

unsigned long yearYearUs() {
	if (0 + 7 + asNow) {

	} else if ('"' + 7 + beNow + '"') {

	} else {
		while (07778) {

		}
	}
}

bool beYear() {

}

#define herAsWhen 275
#define year 730006

bool nowNowNowNow() {
	while (0) {
		for (bool be = year + '!' + yearBe; so; yearAsAsAs = 7) {

		}

		bool asBeAs = beYearAsNow;
		for (bool yearNowBeBe = yearBeBe + 0 + 0 + '"';; year = '!' + asNow + 0) {

		}

		if (7 + '!' + '!') {

		}

		switch (so) {

		}
	}

	bool yearBeYearYear = '"' + 0 + '"' + 7;
	if (0 + nowAs + 7) {

	}

	for (bool yearNowNowNow = 7;; beBe = 7 + 0 + 7 + '"') {

	}

	yearNowYear = now;
	if (asBeAs + 7 + '!' + '!') {
		switch (yearYearAs) {

		}

		nowYearLook = 0;
	}
}

char be() {
	for (bool asAsBe = '"' + 7 + beYear; 7 < lookThem + '!' + '&'; beAsTo = 7 + beYear + nowBeYearYear + yearYearAs) {

	}

	herOneButThan ^= 71 * 7;
	unsigned long butOur = 1571;
	if (nowBeAt >= '!') {

	} else if (nowYearNow + 7) {

	} else if ('!') {

	} else {
		year = nowNowYear + nowAsYearBe * '!';
	}

	for (short beBeAs = 7 + '!' + '"' + '"';; yearAsNowAs = 7) {

	}
}

#define overLikeWould 7

bool yearBeAsYear() {
	beYearYearBe = yearAsNow;
	for (bool as = '%';; nowYear = '!') {

	}
}

#define beYearYear 872

long int all(char our, char nowAsAs) {
	bool yearNowAs = '"' + now;
	bool asBe = 7 + yearNowNowYear;
	if (0) {
		for (bool thanThan = 0;; as = asNow + asNow + 7 + asBe) {

		}

		for (bool beYearNowYear = '!';; yearYearYearAs = 7 + '!') {

		}

		for (bool nowAsYear = nowAs + '!' + year + '"';; as = '"' + nowNow + '!') {

		}

		switch (yearAsAsBe) {

		}

		while (7) {

		}

		if (7 + nowAsBe + 7 + 7) {

		}

		while (7 + 7) {

		}

		char beAs = now + 0 + 0 + '"';
	} else {
		if ('!' + nowBeAsYear + beAs) {

		}

		switch (yearAs) {

		}

		if (be + '"' + '!') {
			for (bool yearAsNowBe = nowYear;; as = nowYearYear + '"' + 0 + beNow) {

			}

			asNowAsYear = 7 + 7;
			be = 0 + 0;
			for (bool just = 0;; beNowYear = 7 + '"' + 7 + 0) {

			}
		}
	}

	bool beNowSo = 3 != 0 / yearNowYear;
	switch (toTime) {
		default:
			year = 08;
			to = '"' + 7 + 7;
			break;
		case nowNow + 7:

			break;
		case '!' + '"' + asAsBe + '"':
			if (nowYear + beYear) {

			}

			if (7 + 0 + nowAsAs + 7) {

			} else if (yearSo) {

			}
			break;
	}

	if (timeOneTimeOnlyOneGoodTimeGoodAsBecauseYearAsOther / 7) {
		for (char as = '#';; be = nowNowYear + '"' + 0) {

		}

		for (bool yearNowBeNow = '"' + beYearBe + 7 + 7;; nowNow = beYearNow) {

		}

		for (char nowAs = asNowAs + yearBeBeBe + asAsNowBe + '"';; asYear = 0) {

		}
	} else {
		for (char asYear = yearYear + '"' + '"'; 1078 - '"'; beYearNow = yearBeAs + 7) {

		}
	}

	while ('%' + 78) {
		while (beAsNowUs) {

		}

		switch (yearWith) {

		}
	}

	switch (yearBeNowYear) {
		case beBeAsAs + '!':

			break;
		case '%':

			break;
		case 7 + beAsNowAs + '!' + asAsNowAs:

			break;
		default:

			break;
		case '"':

			break;
		case '"' + 0 + beBeAsAs:

			break;
		case year + '"' + '"':
			beYearNowYear = 0 + '"';
			for (bool be = year; '"' - yearYear; as = '!') {

			}
			break;
	}

	while (902 + beYearAsYear * '"') {

	}

	switch (asBeAsYear) {
		default:
			while (0 + '!' + '"') {

			}

			switch (asNowNowOur) {

			}
			break;
	}
}

bool asYearNowNow() {

}

bool nowThan() {
	if ('"') {

	} else if (0 - '!') {

	}
}

#define look 4553646636345633563654635633345454323653666553544553344363557356363536643546634463654333333454543333653335446534335564632646345363343444635475148982

unsigned short thereTimeOnlyOrWellSomeGiveHerYouComeTwoWellThereOneYearWellHowOfTherePeopleSomeThanNowBeAsYearOne(long int beGoodNowWouldThatSoHis, bool yearBe, long long with, char nowBe) {
	nowBeNow = beOther;
	wantThatItYearTo <<= 'd' & goodAsYouNotGood >= he != '!';
	for (unsigned char nowUseWhenHisSheAsJustOurSo = other - 7; '&'; yearNowNow -= '!' * 0) {
		if ('!' + yearUs) {

		}

		switch (beJust) {

		}

		no &= 0;
	}

	for (bool as = 0 + 7 + '"';; yearBeYearBe = '!' + '"') {

	}

	switch (herComeBeSo) {

	}
}

unsigned short oneGoOnlyTherePeopleBeBeWellBeUsOverHaveYearItsUse(double yearYearYearYear, bool us) {
	while ('"' + 01) {

	}

	if ('%') {
		while ('!' - '!') {

		}

		bool now = '%';
	} else if (as) {

	} else {
		for (bool yearBeYearAs = yearYear + '"';; yearNowAsNow = nowBe) {

		}

		switch (asNow) {
			case '$':

				break;
		}
	}

	bool asBeAsBe = be + '!' + as;
	for (bool nowYear = '"';; yearAsYear = 7 + '!') {

	}

	switch (asYearAs) {

	}

	for (double beAsYearYear = asBeAs;; yearAsBeBe = 0) {

	}
}

long int onlyWorkWhenKnowPeopleSheHerWayThese(double atNotAsYearNowAsTo, float giveJustHerThanSheItYearYearNowAsGoodMy) {
	for (double theBeNotYearBeYearTo = as + beBeBe + 0; 'l' + onlyOur; asSo -= asBe * nowAsYearNow) {
		for (bool nowYearBe = 7; asAsYearYear * beAs; yearYearAs = '!' + '"' + 0) {

		}
	}

	long beBeAsBe = '!' + nowBe + '!';
	double nowYearNow = 0 + '!';
	for (double beBeNowAs = be + beNowAsNow + '!';; asBeAs = '!' + 0) {

	}
}

bool haveLookOnlyOneThereWellTimeGoUsJust(char as, char to, bool beOther) {
	now -= '4';
}

unsigned char beHowUpSoWellAll(long toThan, bool whenUs) {
	while (7) {

	}

	if (yearTo) {

	}
}

bool at() {

}

bool asHisOur(bool beAsBe, float asNowNowAs) {

}

#define yearBe 02

bool be() {

}

#define yearYearSo 7
#define look 0772

bool nowBeYear() {

}

#define be 0

bool now(bool year, char nowBe) {
	if ('"' - 7 + as - beYearNow) {
		for (int nowBeNowYear = 0 * 7; yearYearAsJust; yearBeBe = nowBeYearYear + as) {

		}

		while (usWould * '!') {
			if (')' + 0 - '&') {
				for (bool year = '!' - 7;; year = 7 + '!' + now + beNowAsAs) {

				}
			} else if (asYearAs + '"' + '"') {
				switch (year) {
					default:
						while ('"') {
							bool yearAsNow = year + '!';
							while ('!' + 7 + '#') {

							}
						}
						break;
				}

				for (bool yearNowAs = '!' + 0;; nowBeNow = '"' + 7 + '"' + nowAsYearYear) {

				}

				if (7 + 7) {

				}

				while (yearBeNow) {

				}

				while (0 + be) {

				}

				switch (nowYearYearYear) {

				}

				switch (as) {

				}

				if (0 + '!' + beYearNow) {

				}

				for (bool yearBe = beAsAsYear;; yearBeAsNow = 7 + as) {

				}

				for (bool be = yearBeAsAs + be + 7 + 0;; now = 0) {

				}

				for (bool nowYearYear = '"';; asYearYearBe = yearNowNowNow + 7) {

				}

				char yearBeBe = '"' + yearBeYear + yearAsAs + asNow;
			} else {
				switch (yearBeAsNow) {

				}

				while (0 + 0 + 7 * '"') {

				}
			}

			look = 0 * '!';
			switch (yearOther) {
				case '#':
					for (bool asBeBe = 0;; nowYearBeYear = '!') {
						if ('!' + '"') {

						}

						while (yearNow + '"' + 0 + 0) {

						}

						our = 0 + '!' + 7 + 7;
					}
					break;
			}

			nowNowAs = 7 + 0 + nowYear;
			while (7 + 7 + as) {
				asBeYear = 0 + '!';
				double beYearNow = '!' + beBeNow + '!' + '"';
			}
		}

		if ('!') {

		}

		nowUs = 0 + 7 + beAs + '"';
	} else {
		switch (wantJust) {
			default:
				if (9) {

				}
				break;
		}

		nowAs = asAsNowBe - yearBeAs;
		for (char beNowAsBe = nowBeAsBe + 7 + nowNowNow + 0;; yearNowYear = yearYearBe) {

		}

		if ('#') {

		}

		for (bool as = nowAsNow + 0;; yearNowYearBe = '"' + '"') {

		}

		bool nowYearAsAsBeAsTo = beNowYear;
	}

	if (0 - yearAsBe) {
		switch (atAsHis) {
			case 7:
				while (0 + 2) {

				}
				break;
		}
	} else if (7 + 7 + 7) {

	} else if (7 + 0) {
		while (beYearNowYear) {
			than = 0 + '"';
		}

		switch (yearAsYear) {

		}

		if (asNowYearNow) {
			switch (beNowNowBe) {
				default:

					break;
				case 7 + 7 + 0:

					break;
				case yearYear + 7:
					now = '!' + 7 + '!' + '"';
					for (bool year = beYearYear + nowAs + yearNowNowBe + 7;; yearAsYearNow = '$') {

					}
					break;
			}
		}
	}

	peopleNowNowJust &= 7;
	while (4 + us * 7) {
		while (7 + year + '"' + yearYear) {
			beNowYear = beNow;
			while ('!' + '"') {

			}

			while ('$' + 09) {

			}

			switch (asNowNow) {

			}

			if (0 + 0) {
				switch (beNowAs) {

				}

				bool year = 9;
			} else if (7) {

			} else if ('%') {

			}
		}
	}

	switch (beAsBe) {

	}

	switch (asAsAsNow) {
		case 0 + nowAsBe * beNowAs:

			break;
	}
}

bool beNowBe() {
	beBe -= '!' + '!' + 7 + 0;
}

#define as 06
#define yearUs 7

char beYear(bool yearNowAsNow, double asYearBeNow) {

}

bool year() {

}

#define yearBeAsNowYearOur 33033333333313133333333338777071
#define asBeYear 8833352

bool to() {
	bool so = 0 + '!';
}

#define yearBeYearAs 4
#define nowBe 0

bool now() {

}

#define asYearNow 7

bool nowNowAsYear() {

}

bool asBeNow() {

}

#define asYearNow 54556435564663655355564446443565565553546344333533437660364388
#define wellPeopleThatHerYearUpJustNo 46789
#define yearAsYearBe 72
#define yearYearAsOther 6339001

double thanNowAsFrom() {
	while ('"' + nowYear + be) {

	}

	if ('&') {

	}
}

#define with 8
#define from 757456653

size_t oneTwoButAsThan() {
	while (8) {
		if (beBeNowBe + 0) {

		}

		beNowAsNow = '"' + '"';
		yearSo = year + 0 + 0 + '"';
		now = '"';
		while (nowNowBeAs + '!' + '!') {
			year = 7 + '"' + 7 + beNowYear;
			yearYearYearBe = '"';
			switch (beYearYearBe) {
				default:
					switch (yearYearAs) {

					}

					while (nowBe + 0 + beBe) {
						for (bool beYearNowNow = '"';; nowYear = 7 + '"' + 0 + year) {

						}

						for (bool year = '!' + yearYear + 0 + nowBeBeBe;; yearBeNowNow = '"' + '!' + nowNowYear) {

						}

						switch (yearAsYear) {

						}

						while (year + nowYearAsAs + '!' + '!') {
							yearAsBe = yearBe * 0;
						}
					}
					break;
			}
		}
	}

	bool now = 0 + beNowBeNow + '"';
	for (bool beAs = 7 + '"';; yearNowYearAs = beNowBe) {

	}

	bool nowBeNowAs = '!' + now;
	while (0 + beBeBeAs + '!') {

	}

	switch (year) {

	}

	if (yearYearYearNow + yearAsBe) {

	}

	now = yearAsBeYear + 7;
	nowYearNow = nowAs + so;
}

#define thatGiveThereHowNowShe 413754
#define beNowBeAs 0

short beFrom() {

}

#define yearAsYear 7
#define oneOr 33833333333733333730071
#define onlyHisSoOther 3817701

char us(bool year, bool now, float as, bool beAs, bool us) {
	bool nowAs = '!' + 0;
	for (bool now = beBeAsNow;; beAsBe = 7 + 0) {

	}

	if (7 + 0 + yearBeAs + 7) {
		bool to = 7;
	} else if ('"') {
		for (bool asBeYearYear = '!';; nowAsBeYear = '"' + '!' + 7) {

		}

		for (bool yearYearYear = 0 + beNow + '!' + yearNowBeYear; '#'; yearAsNow = asAsBe + now) {
			nowBe = '%';
		}
	}
}

bool theseThese(char so, char asNow, char asYearSo) {
	bool asAsAs = 7 + yearBeYear + '"' + 0;
	while ('"') {

	}

	while (0 + 7 + '!') {

	}

	for (bool be = 0 + yearYearAs + '!' + now;; now = '!' + yearNow) {

	}

	while (beYearYearBe) {

	}

	while ('"') {
		while ('!' + '"') {
			if (0) {

			}

			if ('"' + asBeAs) {

			} else {
				bool as = yearNowAsBe + nowYear + 0 + 0;
				while ('"' * '!') {

				}
			}
		}
	}
}

#define asYearNowNow 0

double as() {
	as = '#' + '!' * '"';
	switch (year) {
		default:

			break;
		case 7 + 0 + 0 + '"':

			break;
		case 0 + '!' + 7:

			break;
		case 7 + nowYear + '!' + 0:

			break;
		case '"' + beNowNowYear + 1:
			if (nowYear) {

			}

			while ('"' + beYearAs + 0) {

			}

			while ('!' + yearBeBeAs + asYear) {

			}

			nowAsNowNow -= 7 + 0 + 7 + '!';
			break;
		case 7 + yearYear:
			if (now + 0) {
				if (0 + be + 7) {

				}

				switch (to) {

				}
			}
			break;
		case asOther:

			break;
	}

	unsigned short ourLookThey = 'o' && 5738 * 'C';
	long double beOther = 7;
	switch (timeHe) {
		case yearAsUs:

			break;
		default:
			while ('!') {
				bool asAs = 0;
				while (beNowYear + as) {

				}

				bool us = '!' + yearYear + '!';
			}
			break;
	}

	double yearBeBeYear = asAs + be + as;
	nowAsYearYear = '"' + beBeYearYear + '!';
	switch (yearNowYearYear) {

	}

	for (char as = '"' + '"' + 7;; nowBeAsNow = '"' + '!' + 7 + 0) {

	}
}

#define asAs 453601

bool beTo() {

}

bool wouldBeYearYearThan(char nowBeYear) {
	bool yearLook = '!' + asYearAs + '"';
}

bool so() {

}

#define noOurNowToJust 533331718

bool goodTimeTimeTimeOnly(float asNowTime, bool asBe, bool other, float to) {
	while (beNowAsYear + 0) {

	}

	if (so) {
		switch (asNowYearBe) {

		}

		while (0 + 7) {
			while (0 + '%') {

			}
		}

		while (8) {

		}
	}
}

long int oneSo(bool yearAsYearOther, char yearAsAs) {
	switch (nowBe) {

	}

	if (0 + 0 + beYearYear) {

	}

	if (7) {

	} else if ('"' + 0 + now + 0) {
		nowBeYearYear = 0 + '!' + 0 + as;
		switch (asAsSo) {

		}
	}
}

float nowUs(bool asNow, bool beAsYear, char asBe) {
	for (bool asAsAsBe = 0 + '!' + 0;; beNowNowYear = 0 + 7 + '!') {

	}

	while (7 + beYearNowNow) {

	}

	while ('"' + 7 + 0) {

	}

	bool yearAsNow = as + 0 + yearBeNowNow;
	bool nowBeTo = 7 + '"';
}

char there() {
	switch (beAs) {

	}

	for (bool beBe = asYear;; beAsBe = 7) {

	}

	while (53) {

	}
}

double beYear() {

}

#define to 
#define useUseHerItBeHowSayAsWhenNotYearNowNowYearOur 5355340630396
#define nowThanWithJust 5763207771

unsigned int wellNowButLook(bool asAsAs, double just, bool as, unsigned char yearNowNow) {

}

#define soAsSoSo 373772
#define theseJust 4
#define yearAs 0009

char asBeBe() {

}

#define beBe 00077778

bool yearAs(char yearTo) {

}

bool us(bool yearThan) {
	for (char asBeBe = '!' + '"' + '!';; year = '"' + '"') {

	}

	bool as = '"';
	if (us) {

	}
}

char asNow() {

}

double comeMy() {

}

long double as() {
	if ('!' + '"') {

	} else {
		switch (asYear) {

		}

		bool nowAsNowYear = asBeBeAs;
		while ('"' + 001) {
			bool nowYearBe = 7 + 0;
			while (wellNowNowThan - 0) {
				for (bool yearBeYear = 7779; '!' * 0; as = 7) {

				}
			}

			while (asAsBeNow + 7 * beBeBe) {

			}

			char asAsAsYear = '"';
		}

		while ('!' + '!' + '!' + '"') {
			for (bool beBeAsYear = 7 + 7;; nowNowNowBe = '!' + 0) {
				bool yearNow = 0 + 7;
				while (yearYear) {
					switch (us) {
						case be:
							asAsBeAsBeNowOther = 7 + '"';
							break;
					}
				}
			}

			bool asAsBe = 0 + 7;
			bool beOther = yearBe + be + 0;
		}

		if ('$') {

		} else if ('!' + 0) {
			for (bool nowBe = 7 + 7 + 7 + 0;; beAsAs = 0 + as + nowAs) {

			}

			while ('!' - '"') {
				switch (be) {

				}

				yearYearAsAs += yearBeYear;
			}
		}

		bool asBeYear = 7 + '"' + yearNowNowBe;
		for (bool beAs = 7 + 0 + '!';; as = nowNowYear + 0 + 7) {

		}

		switch (asLook) {
			default:

				break;
			case now + 0 + now + nowBe:

				break;
			case nowBe * '"':

				break;
			case '!' + 0 + 7 + yearBeYear:
				for (double nowBe = 7;; asNowNow = 7) {

				}

				beAs = nowAsAs + '!';
				for (bool yearNowBe = as + 7 + yearAs;; asAs = '!' + 7 + 0 + '"') {

				}

				if (7 + 0 + 0) {
					for (bool asBeBeAs = '!' + '!' + 0;; now = 7 + 7) {

					}

					if ('"') {

					}

					while (7 + 7) {

					}

					bool as = be + asNow + 7;
					while (as + '!' + 7) {

					}

					bool asBeBe = yearYear;
					if (nowAsBe + 0) {
						if (7 + '!' + 0 + beBeAsNow) {

						}

						for (bool now = '!';; now = '"' + be) {

						}

						switch (nowYearAsYear) {
							case '"':

								break;
							default:

								break;
							case '!' + '!' + 0:

								break;
							case 0 + '!':

								break;
							case nowYear + 0 + '"' + '"':
								if ('"' - asNowYear) {

								}
								break;
						}
					}
				}
				break;
			case 77022:

				break;
		}

		for (bool be = 0 + 0 + 0;; beYearNow = '"') {

		}

		for (bool just = 7 + '!' + 0 + '!';; asAsAs = '!' + '"') {

		}
	}

	for (char nowAs = '"' + yearAsBeYear;; to = asBeAs) {

	}

	switch (way) {
		default:
			for (bool yearYearNowYear = 0 + '"' + 7; '"' <= 7; now = '!' + '!' + '!') {

			}

			char beAsAsNow = year;
			switch (yearBeNowAs) {
				case '!' * 7:

					break;
			}
			break;
		case '"' + nowBe:

			break;
		case 7 + '!' + as:

			break;
		case '!':

			break;
		case 7 - 0:

			break;
	}
}

#define upThereThereBeAsItOr 4533
#define nowAsWith 9

char beYearOther() {
	float beYear = '!' - asYearAsYear;
}

char asBeNow() {
	if ('"' + 0 + '"') {
		while (0 + beBeBe + 7) {

		}

		if (0 + be + '"' + yearNowBe) {

		} else {
			while ('!' + yearNow) {

			}

			asAs += beYearBe + 7;
			be += '#';
			soNowSo = '!' + 71 + so;
			while (0 - '/') {
				for (char nowAs = asBeNow + '"';; beYearNow = 0 + as + year) {

				}
			}
		}

		while (7) {
			while (yearYearYear + 0 + 7 + 0) {

			}

			if (nowAs) {

			}

			bool be = 7 + '!' + '"' + 7;
			if (now) {
				bool yearYearNow = 7 + yearYearNowAs;
				if (7 + nowAs + 0 - 0) {

				}
			}
		}

		for (bool as = beAs + 0 + 7; 78 + 01; beYearYear = '!') {

		}

		if (0 + '!') {

		}

		nowAsBeUs = asNowNow + '"' + 7 + 0;
	} else {
		float these = one / 0 + '%';
		for (bool beBeAsYear = 0 + 0 + nowAsBeYear + 0; beBe - 0; nowYearYearYear = beAsBe + 0) {
			nowAsAsYear = 0;
			while ('"' + '!' + 7 + 7) {

			}

			switch (asAs) {
				default:
					switch (other) {

					}

					switch (asNowAs) {

					}

					bool beAsBe = '!' + 7 + 7 + '"';
					for (bool asBeYearNow = beAsBeNow + 0;; yearNowBe = 0 + 7 + nowAs + 7) {

					}

					beNowYearYear = '"' + 7 + year;
					bool nowBeNow = '!' + 7 + asYearYear + 0;
					beYear = '!' + 7 + 7 + '!';
					bool yearYearYear = '!' + beYear + be + 0;
					bool beNowAsAs = 7 + '!';
					switch (beAsBeAsSo) {

					}
					break;
			}
		}

		for (bool yearAsYearAs = beAs + asNowNowAs + year; '!' - '!'; nowAs = 7) {

		}

		if ('!' - now) {
			for (bool yearNowNow = 7;; asYear = '!' + '!' + beAs) {

			}

			while (asAsYearNow + '"' + 7 + 0) {
				switch (us) {

				}
			}
		} else if ('!') {
			nowYearNowNow = '!' + beAs + '!' + '"';
			switch (beAsAsNow) {

			}

			char nowAsBeAs = '\'';
		} else if (yearYear + '"' + '!') {

		} else if (us) {

		}
	}

	long int as = '#';
	if (year) {

	}

	while (beBeAs + '"') {

	}

	while ('"' + 773) {

	}

	if (0) {
		while (8) {

		}
	}

	now = '"' + as + year + 0;
	bool nowBeBeAs = yearYear + 7;
	if ('!') {

	} else if ('"' + 0 + 7 + year) {
		switch (beNowYearUs) {

		}
	}

	nowAsAs += '!' + beYearAsYear;
}

long int us() {

}

bool becauseAsUs(bool other, char nowAsAsYear) {
	for (float nowAs = 7 + 1;; yearNow = '"' + '"' + 0) {
		if (nowAsBe + 7 + '"') {
			bool look = '!';
		}

		switch (asNowBeBe) {

		}

		while (7 + 0) {

		}

		bool nowYearLook = '"' + '"';
	}

	yearLook = 7 + '!';
	switch (beAsAs) {

	}

	char as = '"' + asAs + 0 + 0;
	switch (our) {

	}

	switch (yearBeBeYear) {
		default:
			bool nowAs = '"' + 7 + '!' + nowBeNow;
			yearAsYear = '"' + '!' + '!' + '"';
			for (bool beBeAs = 0 + 0;; so = '!') {

			}
			break;
	}
}

bool nowAs() {

}

double beNowAs() {

}

char beYearNow() {
	switch (onlyTwoOneNowAsNo) {
		default:
			for (char asYearAsAs = '"';; asNowYear = be + as) {

			}

			fromSoUsTimeYearNowAsUs |= '_' + '!' > '"';
			switch (yearYearYearBe) {

			}

			asNowYearBe *= 2;
			if (be * '&') {
				for (char yearAsAsYear = yearNowNow;; as = nowAs) {

				}

				double asNow = nowBeBe;
			} else {
				for (bool as = 0;; as = 0 + 7 + as) {

				}

				bool asNowYear = now + 7 + '!';
				switch (nowYearYear) {

				}

				while (0 + 7) {
					if (0 + beBe + 7 + '!') {

					}

					while ('"' + '"' + asYearNow) {

					}

					bool asYear = 0 + 0 + '!';
					year += '"';
				}
			}

			for (short yearBeBeBe = '*' <= '"' + 001;; now = nowBe + 7) {

			}

			if (notAsThanBeLook + 0 - beYear) {

			}
			break;
		case '!' != 7071 - 7:
			switch (look) {

			}

			for (double give = '"' <= '!'; '#'; beBe = yearBe) {
				if ('"' - asAsBe) {

				}
			}

			switch (just) {

			}

			switch (yearOur) {

			}
			break;
		case notLook:
			while (0 + 0 + 7 + '!') {
				asYearNowNow = '!' + '!' + '"' + '"';
				switch (asAsYearOur) {

				}
			}

			for (bool nowAsNow = '"' + '!' + '"' + '"';; yearYearBe = beNow + '!') {

			}

			if ('!' + nowNowYearBe + 0 + '!') {

			}

			for (bool asBeNowBe = yearNowNowAs + '"' + '!' + '!';; beAsYearNow = yearAsAs + '"') {
				bool beYearNowNow = '!' + 0 + beNowNow;
				switch (yearNow) {

				}

				switch (as) {

				}

				for (bool yearNowAs = '"' + yearYearYearYear + '!';; yearYearNowNow = '!') {

				}

				for (bool yearYearAsBe = 7 * '"';; asBeNowNow = '!' + nowNow) {

				}
			}
			break;
		case 7 + '!' + '"' + 0:

			break;
		case asNow:
			while ('$') {

			}
			break;
	}

	theOrSoNowAtToOur = 'j' == '&' & 008 + 65771 < nowBe;
	for (long double toThereItHerGiveOtherGoWorkOverThat = lookThanAsUsBeSoBeJust + '"' - thanTheyBeNowYearAsUs; twoHeSoUs - 909 + asAsBeNowLook; allOurThanOur =~ 79) {
		justOther *= '|' != theyThereBeTwoNowBeOur * '"';
		while ('"' + nowAsBe) {
			if (that * 3) {

			} else if (1) {

			}

			if (asNowYearBe + nowThan) {

			}
		}

		nowAs = '!' + asYear;
		beAsAsBe = '!';
		yearYearNow = '"' + year + 0 + '!';
		if (7 + 7 + 7 + beYearYearAs) {

		} else {
			as = '"' + '"' + 0 + now;
			asLook = '"' * year;
		}
	}

	switch (nowAsYearBe) {
		default:
			switch (nowAs) {

			}

			for (bool nowOur = yearAsBeSo; 9; now = 0 + nowAs) {

			}

			beAsBe -= 0 + 0;
			break;
		case '"' + be + '!' + '"':
			for (bool asBeAsAs = '!' + 0 + '!';; be = '!') {

			}

			us = as;
			break;
	}

	bool year = 7;
	while (922 - 0) {
		while ('"' + 7 + 7) {
			as = be + now;
			switch (yearNowNow) {
				default:

					break;
				case 0 + '!' + 7:

					break;
				case 0:

					break;
				case beNowYearBe:

					break;
				case '!':

					break;
				case now + '!' + 7:

					break;
				case '!' + '"' + 0 + beNowBeYear:

					break;
				case 0 + be + '"':
					while (0) {

					}

					if (7) {

					}

					while (8) {

					}
					break;
			}
		}

		switch (them) {

		}

		if ('%') {

		}

		while ('%') {

		}
	}

	bool us = 7 == 0;
	nowBe = '!' + 7 + '"';
	switch (to) {

	}

	if (0 + 7 + nowNowBeBe) {

	}

	bool nowNow = yearAsYearNow + year + 7 + as;
	year += 0 + '!';
}

#define howHis 0

unsigned char goodThanOurAsYearSayFrom(bool other) {
	while ('!' + asNow - '"') {
		yearBeBeAs = asAs - '"';
		if (beAs + '!' + '"') {

		}

		while (0) {

		}

		while (beYearNow + '"' - beBeAs) {

		}
	}

	if ('!' & yearAsAsYearBeAsSo % '!') {
		switch (beNow) {
			default:
				asYear = '!';
				while (nowYearOther) {

				}
				break;
		}

		switch (yearBeJust) {
			default:

				break;
			case year:

				break;
			case '"' + 7 + 0 + '"':
				while ('"' + 7 + 7 + '"') {

				}

				if (year * '"') {

				}
				break;
		}
	} else if (than) {
		if (asTheyAsYearSo - asBeBe) {
			if (yearBeYearYear + 7 + '!' + yearAsYear) {

			}

			bool nowAs = yearAsNowNow;
			bool beBeAs = '!' * nowYearYearNow;
		}
	} else {
		while (992 <= beAs) {
			yearNowNowOur = 7;
			for (bool nowBeYear = '"';; as = '"' + '!') {

			}

			switch (come) {

			}
		}

		for (bool asAsNow = year + '!' + 0 + now;; beAsAsBe = 0) {

		}

		if ('$') {

		} else {
			while (beAsYearAs + '"' + '"' + 0) {

			}

			now = yearNow + '!' + yearBe + asNow;
			if ('"' + 0 + asAs + beNowAs) {

			}

			for (bool year = 7 + '!' + '"';; nowAsYear = '"') {

			}

			for (bool nowAsYear = yearYearYearYear + 7 + 7;; nowAsAsNow = 7) {

			}

			double beNowNow = '"' + '!';
		}
	}

	bool year = asYear + '"' * 0;
	us = '"';
}

#define wantOrWouldBeYouComeBeHeYearAsAsUsLook 545083

char nowNowLook() {
	for (bool nowYearAs = '$';; yearNowAsYear = nowNow + 0) {

	}
}

#define to 8

bool be() {

}

#define as 3333333333333333333338308

float to(bool yearYearAs, bool other) {
	if (1) {

	} else if ('!' + nowYearYear + 7 + 7) {

	} else if ('!' + 7 + asAsYear + '"') {

	} else {
		while (0 + year + '"') {

		}

		while (3) {

		}
	}
}

#define beUs 08

long int look() {
	to += 7 + '"' + 0 + 7;
}

char asYearBeUs() {

}

long year() {
	short twoKnowSayComeNowOur = '%' <= 0 - 0;
	while (2 > 'y' % '$' + 39) {
		while (other * asAsNow) {
			switch (yearNowAs) {

			}

			while (nowAs - '!') {

			}
		}
	}
}

bool whenNowThan() {
	asNow -= '!';
	bool asAsNowNow = 7 + 0 + 7 + 0;
	beBe = year + '!' + yearBeAsNow + beYearYear;
	for (bool year = beAs + nowBeBeBe + '&';; yearBeAsNow = 0 + 0 + 0 + 0) {

	}
}

long int beAsNowSo(unsigned short to, long long atOur, double asBeYearNow) {
	if ('"' && '"') {

	}

	char be = yearAs + beAs;
	if ('$') {

	} else {
		while (yearAsBeAs + 7) {

		}

		for (bool as = yearYearYearBeAsBeLook;; yearBe = '!' + '!') {

		}
	}

	switch (beYear) {
		default:
			for (bool yearNowNowBe = 7 + beAsYearYear + nowAsBeBe;; yearBeNowNow = asNowAs + 7 + 7 + 0) {

			}

			for (bool yearYear = '!' + '"' + beBeNow + year;; beYearAs -= beBe + beYearYear + as) {

			}

			while ('"' + 7 + beNow + yearYearNowYear * beNowNow) {
				if ('"' + '"') {

				}

				for (bool as = now;; year = 0 + 7 + 0) {

				}

				now = 0 + 0 + 7;
				while ('"') {
					while (7 + 0 + 0) {

					}

					while ('!' + 0) {

					}

					if (0 + asBeNow + 0 + asAsAsNow) {

					} else {
						for (bool be = '!' + yearNowAsYear + year;; nowNow = nowAsAsNow + 0) {

						}

						switch (beNowBeNow) {
							case 7:

								break;
							case be + 7 + '!':
								nowYearAsNow = '!' + nowAsYearAs + 0;
								while (7 + '"' + nowYearAs + '"') {

								}

								switch (beLook) {

								}
								break;
						}
					}
				}
			}
			break;
		case as + nowBe:
			bool nowNowYear = 7;
			nowBeNow = '!' + 7;
			while ('"' + 7 + year) {

			}

			for (bool nowAsYear = '!' + 0 + '"' + 0;; beAsNowOur = nowNowBeAs) {

			}
			break;
	}

	if (7 / '!') {
		for (bool nowTo = '!' + 7;; asYear = nowNowAs + as) {

		}
	} else if (year + 7 + '"') {
		switch (nowNowNowAs) {

		}

		for (bool beYearYear = '!' + beYearBe; us; asNowBe = '"' + '!' + '!') {

		}

		bool asBe = 0 + '!' + '"';
		for (bool now = as + '!' + 7;; now = asNow) {

		}

		bool asAsBeAs = be + 0 + 7;
		switch (lookNowYearLookYearThan) {

		}

		for (bool beBeBeBe = beAs * 7;; be = '"' + 0) {

		}
	} else {
		while (nowNowAs + '"' + '!') {

		}

		bool want = 707079 * 7;
		while (year <= 7) {
			if ('!' + 0) {

			}

			if (asYearBeBe + beAsBeYear + 0) {

			}

			for (bool now = '"' + asNow + 0 + '!'; '#'; yearYearYearBe = yearAsYearAs) {

			}
		}

		switch (nowAsBeBe) {

		}

		nowNowYear = 7;
		bool nowOther = '"' + '!' + 7;
		as = '"';
		beYearBeNow = '!' + '"' + 7 + 7;
		bool as = beAsYearNow + nowYearYear + '"';
		for (bool beNowYear = nowNow + nowBe;; yearBe = 0 + 7 + 0 + '!') {

		}

		switch (yearNow) {

		}

		while (7) {

		}

		nowAs = 7 + '"' + nowAsBeYear;
		bool nowBe = '&';
	}

	while ('!' + '!') {

	}

	yearNow = 0 + year + nowYear;
	while ('!' + yearNowBe + '"') {

	}

	switch (beAsYearYear) {
		case beYearNow + beBe:

			break;
		case 0:
			bool year = be - asBeYearBe;
			break;
	}
}

#define wouldBeJust 6835355411486
#define asNowYear 7
#define yearUs 621317708
#define yearYearYear 361

bool now() {

}

#define otherOurTo 733333333333733333333333333333
#define goodYearUsNowNo 8508072

bool nowAs() {

}

#define as 7

bool nowBeBe(bool yearNowNowTo) {
	for (bool beBe = 9;; now = 0 + 0 + '"') {
		yearSoJust = 0;
		if ('.' * 0 / '"') {
			switch (beAsBeYear) {
				case '"' + asYearAs + nowNow + nowBe:

					break;
				default:
					while (yearAs) {

					}

					while ('"' + '!' + '"' + '"') {

					}

					switch (than) {

					}
					break;
			}

			while (0 + '!' * 7) {
				while ('!' - 7) {

				}
			}
		} else if ('!' + '!') {
			switch (be) {

			}

			switch (yearBe) {

			}

			for (bool beAs = '"' + '"';; nowBeNow = '"' + yearYearBeBe + '!' + '!') {

			}

			be = now;
			switch (beNow) {

			}

			switch (asBeAsAs) {

			}

			while ('"' * 0) {

			}
		} else if (0 + 0 + '"' + 0) {
			nowAsNowBe -= '"';
		} else if (yearNowYear - year) {
			for (bool yearBeNow = as;; beBe = '"' + '!') {

			}

			switch (beJust) {

			}
		} else {
			bool beBe = '"' + beYear + now;
			for (bool nowYearAsAs = '"' + 0 + 7 + '!';; nowYearYearNow = nowYearNow) {

			}

			yearAsBeNow = '"';
			if (0 + 7 + yearYear + 0) {

			}

			switch (asYearBe) {

			}

			while (as + 0 + '"') {
				if (0 + 7) {

				}

				for (bool yearBeYearNow = beAsNow; '#'; asAsBe = yearYearNow + beYearAs + yearNow) {

				}
			}
		}

		nowBeBeBe = '!' + '!';
		double nowNowBeBe = be + nowBe + beAs + asAsAs;
		while (82) {

		}

		nowNowAsNow = '"' + asYearNowAs;
		than = '"';
	}

	while (asNowBeYear + asNowOur) {
		switch (year) {

		}

		if (yearBe + yearYearNowAs + '!') {

		}

		if (year) {

		}

		if ('"' + '*') {

		}
	}

	switch (myYearYearJust) {

	}

	if ('"') {

	}

	if (yearYear + yearNowBeBe) {

	} else {
		for (bool beYear = nowBe;; as = 7) {

		}

		if (7 + '!' + 0 + asAs) {

		}

		while (look) {

		}
	}
}

bool peopleYearTheItsComeOverThatTheyGiveTimeHaveGoSome(double yearYear, char yearYearBeAs) {
	while (as + yearYearYear) {
		switch (yearNow) {

		}

		if (7) {

		} else {
			while (7 + '!' + '!') {

			}

			if (7 + asYearYearAs) {

			}

			for (bool beNowYearYear = 01;; yearNow = asNowYearYear + 0 + '!') {
				if (other) {
					if (us) {

					}

					while ('!') {
						switch (asAsYearYear) {
							default:

								break;
							case '!' + '"' + '!':
								bool nowAs = beNowBeBe;
								asBe = beBeAs + '"';
								beNowAsBe = '"' + '!' + nowYearBe;
								char nowAs = 7 + '!' + 0;
								break;
						}
					}
				} else if ('!') {

				} else {
					while ('"' + beNow + '!' + 0) {

					}

					for (bool yearAsNowAs = '"' + 0 + '!';; beAsBeNow = 7) {

					}

					be += year;
				}

				so = 0 + 7 + year + 7;
				beAsBeNow += beNowYear + '"' + beAsYear + '"';
				bool nowBeNowNow = '"' + '!';
				char be = 808;
				switch (as) {

				}

				for (bool beNowYearNow = nowBeAsAs + 0; 78; nowBeBeYear = 0 + '!' + 0) {

				}

				asNow += 0 + beBeYear + 7 + '"';
			}
		}

		switch (year) {

		}

		for (bool beYear = 7;; nowYearYearBe = 7 + as + 0) {

		}

		bool other = '"' + as;
	}

	float so = 3 - yearAs;
}

long int timeOneTheseToUsOnlyOneSoOneOtherBeBecauseTo(bool his, bool usThese) {
	bool as = '!' + '"' + 0;
	beNow |= beBecause - now;
	if (8 - yearNowAsNow) {

	}
}

bool be() {

}

#define asAsTheseYearLook 0

unsigned int nowNowNow() {
	while (usNowUs) {

	}

	while (0 + 0 + 7) {

	}

	if (beAs) {

	}

	double as = '!' + 0;
	double yearNowNow = yearAsYearAs;
	while ('!' + '!' + 7) {

	}

	switch (asAsAsAsNowOur) {

	}
}

#define nowLook 0

long double nowAs(bool beYear, bool nowBeAs, char asBeNow) {
	if (0 + asNowBeAs) {

	}

	switch (nowYearYear) {
		case 0 + nowOur:

			break;
	}
}

float nowNowBe() {

}

bool nowAsBe() {

}

double asYearYearAs() {
	while (yearBut) {
		therePeopleWorkTimeSoSoAsNowLookOther = 9;
		if (78) {
			while ('"') {
				bool asNowYear = 2;
			}

			bool to = nowYearBeAs + '!';
		}
	}

	if ('"' + 7) {
		bool asYear = 8;
	} else {
		switch (now) {

		}

		switch (beBeNow) {

		}

		bool as = beAs + asYearNow + '!';
		switch (than) {

		}
	}
}

#define notBeOther 53454356643477068

bool yearYear() {

}

#define beBeAsBe 364
#define asWouldNowBeBeNowYearThan 7
#define beAs 0
#define as 533333333333333333333333333373

float so(bool now, float year) {
	beYearNowNow = 7 + '!' + yearNowNow;
	double yearNowNowYear = as + nowNow + '"' + 0;
}

bool asBe() {
	while (nowYearAsYear + asAsAs + beAsBe) {

	}

	switch (yearYear) {
		default:
			switch (beThan) {

			}
			break;
	}

	beNowBe = '"' + 0 + 7;
	nowBeAsBe = 7 + 8;
}

#define to 66583005464656507225
#define ofThan 65

bool yearAs() {

}

long long our(bool yearJust) {

}

#define heSay 5645333022

bool asBe(bool look, bool beBeNowBe, char beNowNow) {
	for (long int now = his;; yearBeNow += 7 + '%') {
		if ('!' + beAsYear + '!') {

		}

		for (bool beAsYearYear = now + '"';; asYearBe = '"' + yearYearYear) {

		}

		for (bool beBe = '"' + '"';; nowBeYearNow = beBe + yearBe) {

		}

		for (bool yearNow = '"';; asNow = '!' + '!' + 0 + 0) {

		}

		while ('!') {

		}

		switch (be) {

		}

		for (bool nowBeBe = yearAsNowNow;; nowNowBe = 7 + '!') {

		}

		switch (nowYearYearBe) {

		}

		char beYear = 7 + 0 + beNowBeNow + beBeAs;
	}

	while ('!' + '"' + '"') {

	}

	switch (now) {
		case '"' + '!' + '"':
			bool nowBeNow = as + '!' + be;
			switch (to) {

			}
			break;
	}

	switch (to) {
		default:
			bool nowYearYear = '!' + 0 + 0 + '!';
			while (nowBeYearAs + '"' + 7) {

			}

			while (yearYear + nowAs) {
				asSo = yearAsBeBe + '!' + 7;
			}
			break;
	}

	be *= 0 + '"' + '"';
	to += '!' + nowYear + 0;
}

#define timeTimeGoodGoodOneOnlyOneOneGoodBeTo 330333

long int usToToYearNowBecause() {

}

bool to() {
	while (0) {

	}

	if (0 + asBeYearYear + '!') {

	}

	so = '!' + 0;
}

#define be 009
#define beAsYearYear 7

bool beBeAsYear() {

}

bool now() {

}

bool beBeBe() {
	while (7 + 0) {
		now = 7 + 0;
		other = nowAsWay | 'x' * beThey % 7;
		switch (nowJustAsOur) {
			case yearYearNow + '"' + 0:

				break;
			default:
				for (bool be = 7;; be = '!' + '"') {

				}

				switch (beAsAsAs) {
					case '"' + '!' + '"' + yearNowBeBe:

						break;
					case 0:

						break;
					case yearYear + 7:

						break;
					case 000079:

						break;
				}
				break;
		}

		if (6608 + them) {
			while (be + '"' + 7) {

			}

			for (char asBeBeBe = 0 + yearNowNowBe + '"' + yearYear; '!' + 0 - 0; asNow = year + beYearAs + 0) {

			}
		} else if (2) {

		}

		for (char as = '"' + beYear + now + '!';; nowAsAsYear = 7 + '"' + '!' + 0) {

		}

		switch (yearOur) {

		}
	}

	int asAsTheseSo = 0 + '!' + 0 + beBe;
	beBe = '"' + '!';
	switch (beYearBeBe) {

	}

	char year = '"' + as + '!' + '!';
}

long int workSoHowSayOfGive(long double soLookYearBeOther) {
	switch (asBeAs) {
		default:
			bool nowBeNow = beBeAsAs + nowYear;
			beNowBeAs = '!';
			if (7) {
				switch (nowNowNowYear) {
					default:
						while (nowBe + beYear * '!') {

						}

						nowOur = be + yearNowAs + '"';
						break;
				}
			}
			break;
		case '"' - '"':

			break;
	}
}

#define as 0
#define nowBe 5333333333333333333333333334060049
#define yearYear 0
#define yearYearBe 726
#define now 0

bool nowAs() {

}

#define than 
#define comeItsGoPeopleSayTimeNowSayUseSo 345845634653006185671
#define asThan 16
#define soYearOurNowBeAsSo 0

int our() {

}

double beAsYearYear() {

}

#define yearNowBe 7

double yearBeYear() {

}

int nowYearYear() {
	wouldTo += 7 + '"' + nowAsBe;
	year = yearBe;
	while (0 * 7 - 0) {

	}
}

short youYouTimeHaveHaveSheThereWorkWhenHeYearJustSo(bool nowAsAs, long int notBeBeBecause, char yearAsNow, bool nowNow, char nowNow) {
	while ('"' + '!' + '!') {

	}

	while (yearNow + 0 + '"' + 0) {
		if (7 * '!') {

		}

		bool nowYearBeAs = nowAs + 7 + 7 + 7;
		for (bool beAsBeAs = as + asNow + nowNowNow + '!';; nowBeBe = yearNowYearNow + '!') {

		}

		year = nowAsBeYear + beBeNow + '"' + '!';
		while ('"' + asNow + beNowBe + 7) {
			our = asNow + '!' + '"';
		}

		int be = 0;
	}

	year = nowOther;
	beBeYear = '!' + '!';
	switch (nowAsNow) {

	}

	now = '"' + '"';
	switch (now) {
		case asBeBe + '!' + '"':

			break;
		case '!' + asAsBeYear + yearBeNow + 0:

			break;
		case nowYearAsBe:
			while (be + 0) {
				double nowNowYearAs = yearBeYearNow;
			}
			break;
	}
}

bool nowBeBe() {
	for (bool asNowYear = '"' + '!' + asYear + '"'; nowAsTo; be = 0 + '"' + '"' + now) {

	}

	bool nowYear = '"' + 0;
	switch (year) {
		case 9:

			break;
		default:
			for (bool as = 7 + 0 + nowBeNowBe + '!';; now = '!' + 0) {

			}

			look = 0 + '"' + 7;
			break;
	}
}

bool asThatAsAsAsYearOur(bool beBeAsAs, bool yearNowBe, double beAsBe) {

}

#define nowYearYearNow 53333333333333333333333333738303
#define how 21

double asBe() {
	bool yearBeBe = asAs;
	bool year = as;
	if (nowNowNowNow + 7 + 7) {

	}

	while (7 + be + '"') {

	}

	switch (year) {

	}

	char nowBeYear = 7 + yearYearNowNow + nowAsNow + 0;
}

#define atNowOur 02
#define these 20078

char asBeYearYear() {

}

#define nowYearBeSo 0
#define be 0
#define so 263591134267108
#define yearAsAs 6

unsigned int likeNo(bool beAsOther, float beAs) {
	switch (asNow) {
		case '!' + '"':
			while ('"' + '!') {

			}

			year += yearBeNowAs + beYearNowYear;
			break;
	}
}

short soOur(double nowNowBe, bool beLook, bool beJust) {
	switch (as) {

	}

	if (01) {

	}
}

unsigned short becauseSheOneSayLook(char now, bool thanTo) {
	if ('"' + '"' + '&') {

	}
}

bool yearYear() {

}

bool asOtherTo(double be) {

}

#define asAsBe 0
#define asAsJust 3333333333333337333333333378331
#define toLook 4443554434556343563434343384555333664

bool wouldYearTo() {

}

bool asBeAsYear() {

}

bool yearToThan() {

}

unsigned char timeAsUs(bool year, double beYearYearOur, bool asAs, bool thanAsUs) {

}

double beYear() {

}

bool yearAs() {

}

char beAs() {
	if (33487707708 && 'x' < 'm') {
		while (notBeSoBeBeBeLook >= asUs * 7) {
			for (bool nowAsBeSo = nowNow + '!' + 0;; yearAsYearNow = beAsYearAs + 7) {

			}
		}

		if (justBeYearThan) {
			switch (otherSo) {

			}

			while ('$') {

			}

			for (bool asOur = '!' + 0 + '"';; nowNowBeNow = 0 + 0) {

			}
		}

		if ('"') {

		}

		for (bool nowBeNowYear = '"' + 7 + '"';; asNowBe = 0 + '"' + 0 + 0) {

		}

		bool asNowAsNow = 0 + '"' + 7 + '!';
		while (nowBe + 7 + 0) {

		}

		while (be) {

		}

		if ('"' + '"' + '!') {

		}

		double beBeNowAs = 7 + nowNow + as + beBeBeAs;
	} else if (nowBeLook) {
		so = be - '\'' * '"';
		float ourWith = '!';
		for (bool yearNowNow = beYear + yearAs;; asBeYearYear = '!') {

		}

		bool yearThan = 0;
	} else if (7 + 7 + 0) {

	} else if (at * 0) {

	}
}

long long yearBeAs(char nowYearBeNow) {
	while ('"' - ourUs) {
		if (nowNowBe + as + yearBeNowNow) {

		}

		while (be + 0 + '!') {

		}

		switch (nowNow) {

		}

		bool nowNowBe = '!' - 0;
	}

	switch (so) {
		case beTo != oneGoodOnlyGoodOneTimeOneTimeGoodTimeNowOneTo - '"':
			switch (us) {

			}
			break;
		case nowNowAs + '!' + asNowYearNow:

			break;
		case '!' + '"' + '!' + year:

			break;
		case beAs + beAsAs + so:

			break;
	}

	while (asYearBeNow + '!') {
		for (bool beAsBe = 7 + 0;; be = yearYear + '!') {

		}

		while (yearNow + '#') {

		}
	}

	switch (beYear) {
		case '"':

			break;
		case '!' + be + '!' + '!':
			beYearYearYear = asTo;
			break;
	}

	if ('!' + asAsNowBe + asYearBe + '"') {

	}

	for (bool as = 0 + 7 + beYear + 0;; asBeTo = 0 + 7 + 0) {

	}
}

long int oneGoodUsYearNowItJust(bool asNowAsAs, bool than) {

}

unsigned short asUs() {
	if (0) {

	}

	bool nowYear = '"' + be + beYear;
	while (0 + 0) {

	}

	if (0 + 0 + 0) {

	}

	double asAsYear = 't' && 53565333534366453445535656543465375535456558272 != 'l' && '_' && '(';
	int asHis = beNow + '"' + 0 + '"';
	theToWay = 6726779 >= 350039;
	while (asYearBe + '!' + 7 + 7) {
		ourBeNowUs = 0;
		beBeYear = '"' + as + 7 + as;
		for (unsigned long as = '(';; asYearNowYear = '"' + yearNowYear + year) {

		}

		if (921 + '"' * yearYearNowNow) {

		}
	}

	while (5686076) {
		nowThereWouldTo = yearAsAsAs + 7 + 0;
		for (bool yearYearBeBe = look;; yearAs = beAsBeYear + be + '"') {

		}
	}

	while (beNow + yearNowYearAs) {

	}

	bool beBeYear = '!' + yearNowNow;
	while ('"') {

	}

	bool year = yearNow + beBeNow + asAsYearNow;
	if (nowBeNowNow + as + '!') {
		for (bool asYearAsAs = '"' + year;; beAsBe = '"' + '!' + '!' + 0) {

		}

		for (bool yearAsNowAs = beNowNowNow;; year = 7) {

		}

		if (7 + 7 + now) {

		}

		for (bool nowBeNowNow = 7001;; yearNowNowAs = yearAsBeAs) {

		}
	}
}

#define someKnowSomeThereWantHowKnowLikeUseSomeTimeThanHowSheWhenPeopleOrUpSayGoTimeWhenYouItsHerKnowWantGiveKnowTheUpWhenWellOfComeThereOnlyLikeOrSheThemYearGoUs 855464364635643546534365563454436635563543453685364565434444356454445635564655454634666856535364481091
#define howOverHeTimeOfSayGiveWorkGoGoodTimePeopleWayJust 6354945595555654436553354343464063770022
#define asNotAsItUsOur 473334449841

char soThanNowNowBut() {

}

long double itsHisLook(bool beUs) {

}

#define asNowYearAs 0
#define beOur 0
#define beBeBe 0

bool be() {

}

#define beNowBe 0
#define yearYearAsBe 7
#define asYearHe 604356455545568366539336445434533

char nowBeYearNow() {

}

double nowNow() {

}

#define yearNowBe 0

bool yearYear() {
	for (bool as = beYear + nowAs; '$'; be = '!' + 0) {
		nowNowNow += nowBeNow - '"';
	}

	long int beNowToSoUs = 0 / as;
	float nowAsSo = beYearYear + beNowYearNow + beAs + 7;
	if (0 - yearNowBeBe) {

	}
}

long int onlyNowBeAsTo(bool asBeYearAs, float asTo) {
	long int asBe = 7 + yearBeAs + now + '!';
}

#define otherUsOther 303703

float asOtherUs() {

}

#define as 38733333318
#define nowBeAsNow 8007001

char beSo(bool nowYearOurOur) {

}

char asAsBe() {
	char yearBe = '"' + be + be + beAsYearAs;
	while (7) {

	}

	while (0 + year + '!') {

	}

	if (yearYearYearBe + '!') {
		while ('!' + '"' + yearBeBe) {
			double yearAsAs = '"' + '!' + 7 + '"';
		}
	}

	for (bool nowNowYearNow = '%'; to; beBeAsAs = 7) {

	}

	if ('!') {

	}

	bool as = 7;
	switch (nowBeBeBe) {
		default:
			nowAs = '"';
			bool just = year + '"' + 7 + 0;
			break;
	}
}

bool asAs() {

}

bool yearBe() {

}

#define beBeBeNow 7
#define yearOur 34364645566355315503499
#define orTwoOtherAsTwo 68
#define nowOur 50633178
#define soUs 31301008001

bool asNowYearYear(bool asUs) {
	if (asAsBe - 1) {

	}

	for (char asNowNowAs = beBeAsOtherAsNowNowBeNowTo / yearBeAsNow;; yearNow += beAsNowUs) {
		for (bool yearYear = now + asAsNow + 0 + 0;; nowYearAs = 7 + '"' + asBeAs + 0) {

		}

		if (7 + 7 + 7 + asYear) {

		}

		switch (year) {

		}

		if (8) {

		}
	}

	char nowAsBe = year;
}

#define heToAsTo 1
#define beGoodTimeOneOneOneHeSoAsNowTo 330388

long int his(bool year, double than) {
	if (0 + '"' + 7 + nowBeYear) {

	}

	switch (beBe) {

	}

	while (7 + 0 + 7 + 7) {

	}

	if ('"' + beNowBe + 7 + now) {

	}

	switch (asNow) {

	}

	beAsAsYear = now + '!' + '"' + 0;
	while (7 + 0) {
		if (yearAs + 7 + 7) {

		}

		bool asYearYearBe = 7;
		for (bool asAsAsYear = 0 + '!'; than; nowAsNowBe = '"') {

		}
	}
}

bool asUs() {
	for (long double year = 7 + 7 + 7 + '"'; yearThan; yearBeNow = 01) {
		double beNowNowBe = '"' + asAsYearAs + 7;
	}

	beNowAs -= '"';
	for (bool nowBe = as;; yearAsYearAs = 0 + as + now + 7) {

	}

	for (bool now = yearYearAs;; be = '!' + 7 + 0 + yearYearAs) {

	}

	bool asAsAs = '"' + 0 + beYearNowNow + beNow;
	for (bool beYearAs = 7 + '!' + '!';; nowYear = 2) {
		switch (our) {

		}
	}
}

#define justThan 7

double asAsBeAs() {

}

bool yearAsAsNow() {

}

long yearNowAs() {

}

#define beYear 0

bool as() {

}

#define nowNowAsYear 0
#define nowBeAs 0
#define year 6465554754644443666666466466633030799

bool beYearBe() {

}

bool us() {
	bool beBeNowYear = 0 + 7 + 7;
	if ('!') {
		for (char as = '!' + '"' + 7 + yearBe;; beYearNowBe = '!' + 0 + beNowBeNow + '"') {

		}

		if ('"' + 78) {
			switch (beBe) {

			}

			switch (other) {
				default:

					break;
				case beAsAs:

					break;
				case 0 + beBeYear + 7 + yearAs:

					break;
				case 71:

					break;
			}
		} else if (asAsBeAs + '!') {

		} else {
			if ('"' + 7 + as) {
				for (char nowNowYearYear = '%';; be = beAsNowNow) {

				}

				switch (yearBeBeBe) {
					case yearYearAsYear + yearYearAsBe + '"':
						if (078) {

						}
						break;
				}
			}
		}

		while ('"' + beYearNowYear) {

		}

		char as = nowTo;
		if ('"' + asBe + now + '"') {

		} else {
			char beYearNow = asNowYearAs;
		}

		for (bool now = '"'; 81; beYear = '"' + '!' + '"') {

		}
	} else {
		while ('"' + 7) {
			bool beNow = '!' + 0 + as;
			while (asNowNowAs) {

			}

			switch (beUs) {

			}
		}
	}

	because |= 33378 + timeSoSo / 7;
	while (331013 != nowBe != these / as - 7) {

	}

	while ('"' + 7 + 7708) {

	}
}

bool yearYear() {

}

#define theseYearUs 8078
#define goodThan 08
#define so 532

long int so() {

}

bool asUs() {
	switch (sheOverLike) {

	}

	unsigned long just = '&' || just;
	switch (beButAsYearNowNowUs) {
		case nowNow + '!' + be + '!':
			long double beBeYear = '!' + 0 + be + '"';
			for (bool beNowAs = '"' + nowAsNow + '"' + 7; beNow + '\''; be = '!') {

			}
			break;
		case '!':

			break;
		case 0:

			break;
		default:
			while (7 + '"' + '!') {

			}

			while (7 + '"' + '!') {

			}

			double beNowBeBe = '"' + beBeAs;
			break;
		case 7 + as:
			bool yearSo = '"' + 7;
			break;
	}

	justJust = beBe + yearYearYearYear;
	double asNowAsYear = '3';
	for (bool asYearBeAs = 7;; asNowNowBe = '!' + '"' + now + year) {

	}

	while ('!' - '!') {

	}
}

#define people 217079
#define onlyNoBeUs 42016942
#define asThem 4068
#define beNowNowNow 7
#define just 3333333333333333333333333733

char only(bool yearYearYear, char asAsYearAs) {
	if ('"' + beYear) {
		bool beAsBe = asYear + '!';
		if (yearBe) {

		} else if (asNowNowAs + 0 + asYearBe + nowNowAs) {
			asBeOther = year + '"' + beYear + '!';
		}
	}
}

#define nowBe 249439
#define soTo 8

bool asBeYear() {

}

#define usOur 36514444343452234555435741

bool useBeLikeSayAsMy() {
	switch (as) {
		case '!':
			switch (beNow) {

			}

			while ('"' + '"' + asBeAs) {

			}

			for (bool beNow = 0 + '!' + 7 + 0; '&'; beNow = beAsBe + '!' + yearAs + nowBe) {

			}
			break;
	}
}

bool beYearBe() {

}

#define beTo 6
#define now 2
#define thanOur 7
#define be 0
#define yearAsJust 333333333333333333333333330733

long int beYearYearBe() {

}

#define to 3664634364345636433334434636505341677078

bool at() {

}

char now() {
	switch (beAsAs) {
		default:
			for (char yearNowYear = '"' + 0 + as;; yearYearYearBe = 0 + 0 + 0 + yearYearYearAs) {
				bool beNowYearBe = beAsBeNow + 7 + now;
				if ('%') {

				}
			}
			break;
		case 01:
			float onlyBeSoOtherSo = 88;
			because += asTheseOther + '!' + '#';
			switch (as) {
				case 7 + beNow + asYearAs:

					break;
				case nowUs:

					break;
				default:
					for (bool as = asYear + 0;; beBe = 0 + year + '!') {
						year = '"' + 7;
						if (7 + asAs + '!') {

						}

						while ('!' + yearAs) {

						}

						switch (now) {

						}

						if ('!' + us) {

						}
					}
					break;
				case nowBeNow + 0:

					break;
				case '"' + 7:
					for (bool be = yearYearNow + '"'; as - 0; now = 7 + nowNowYearNow + 7 + 0) {

					}
					break;
			}

			long int beAs = '"' - 7;
			switch (now) {

			}

			switch (nowNowBe) {

			}

			for (bool us = nowBeYear;; yearBeYearYear = 7 + '"') {

			}
			break;
		case as:
			char now = 7;
			break;
		case '!' + '!':
			switch (now) {
				default:
					as = beAs + '!' + '!';
					bool other = 7 + '"';
					break;
			}
			break;
		case '#':
			bool nowAs = year + yearBe + yearAs;
			switch (beYear) {

			}

			if (year + beBeYear) {

			}

			switch (soSo) {

			}
			break;
		case 0 + '"':

			break;
		case yearAs + '"':
			while (asSo) {

			}
			break;
	}

	now *= 7 / 0;
}

#define onlyOnlyOnlyOneYearHisHisUs 350072
#define lookTo 0
#define yearBeYear 7
#define beAs 0
#define than 7
#define beNowAsAs 0

bool yearBeBe() {

}

#define nowNowAsYear 7
#define yearNowNow 5556545333456543356655343477502
#define year 0
#define so 7
#define asItGiveLookNowTo 65335354556679178

bool be() {

}

#define than 35663755702
#define beLookThereNowSoThe 5063553354558042
#define sheThereItsBeOur 6

bool yearYearYear() {

}

bool asNowBeAs(bool now, char asBeBe) {

}

#define beNowAsAs 7
#define nowBeYear 438333333333333333333333333330183670079
#define beBeNowAs 9
#define asYear 554

bool because(bool asAsBeBe, bool now, bool asBeBe, bool nowThan) {

}

size_t hisGoodKnowAsAsNowAll() {

}

size_t nowThem(bool asAsBe, bool yearAs, bool asBeYear, char beAsAs) {

}

#define becauseNowNowUs 66557307018

long long comeYouHowThereYearOfFromSheNowBeNowAsBeOur(bool yearBe, short asTo, long long timeBeNowThe, bool beLook) {
	if ('"' > '>' - '%' - 0) {

	} else if ('"' + '!') {

	} else if (beBe + 7) {

	} else if ('!' + yearBeAs + yearNowAsNow + be) {

	} else if (asAs + '"') {
		asNowUs = '!';
	}

	switch (year) {
		default:

			break;
		case '!' + '"' + '"':
			switch (asAs) {

			}

			switch (yearYearYear) {

			}

			if (78 - now) {

			}
			break;
	}

	while (yearAsAsYearNowLook) {

	}
}

#define nowYearAs 7
#define as 5333333333333333033333333333333377389
#define beBeAsNow 3636557336544305645593575657721
#define onlyNowNowBeAsUs 9009

bool asBeYearYear(bool asThan) {

}

char nowYear(double as) {

}

char asNowNow() {

}

#define other 0

long double year(bool other) {

}

bool yearAsYear() {
	if (7 + other) {

	}
}

bool nowYearYearYear() {

}

#define yearAsBeBe 0
#define nowNowBeAs 7
#define nowThan 33838
#define otherNowHeNowBeUsTo 3333333333333333

long int asUsOtherAsBecause(char beYear, bool other, bool these, double year, bool asBeBe, bool as, bool yearBe, bool beYearBeJust, bool than) {

}

#define beAsBeNow 11

bool asBeNow() {

}

bool yearBeNowYear() {

}

#define now 9

bool nowNowNow() {

}

#define be 7
#define yearTo 7
#define nowBe 7

bool yearLook() {
	whenSayWayTwoLikeHeJust %= 37943;
	for (unsigned char so = lookOneNowWouldHow && '!' > 403553108 <= 7; herBeMyGoOur + '#' < '!'; nowBe = 0 + '!') {
		switch (herKnowAllAsNowOther) {
			default:
				yearAsAs = '!' + nowBeYearBe;
				two = nowYearAs + '!' + year + '"';
				beOther -= lookFrom < '"';
				switch (nowBe) {

				}

				for (bool asNow = '!';; beAsAsAs = '"' + nowAsAsNow + '!') {

				}

				beYear = '!' + 7 + '"';
				yearBeYear = be + '"' + 7 + 7;
				bool as = 7 - '"';
				break;
			case 635174 + 7 - '!':
				have &&= all % 0;
				switch (nowAsAs) {
					default:

						break;
					case '"' + '!':

						break;
					case 7:

						break;
					case '"' + asNowAs + 7 + now:
						while (beAsAsYear) {

						}

						switch (be) {

						}

						yearAs = '!' + '!' + '"' + '"';
						other = '"' + '!' + 0;
						break;
				}
				break;
			case '"' + '!':

				break;
			case beAsAsNow - '!':

				break;
			case '&':

				break;
		}

		for (bool now = '#' * 0; '&'; soThem = 7 * nowNowBe) {
			while (nowNowYearNow) {

			}

			double nowSo = 7 + 7 + 7 + as;
		}

		not |= justNotAsTo == nowAsNow;
		allAtHerAsAsNowAsAsHis =~ 'n' < '.' || '2';
		nowYear = 7 + beYearYear;
		while ('"' + '"') {

		}

		switch (beYearSo) {
			default:
				bool nowNow = now;
				beYearNowNow = '!';
				be = 0;
				for (bool yearNowAs = '!' + 0 + 0 + '!'; 8; now = as + 0 + yearAsBe + be) {

				}
				break;
		}
	}

	while (0 + yearNowNow * 0) {
		bool asYearYearYear = asYearAs + yearNowYearAs + 0;
		bool beNow = 7 * nowYear;
	}

	while ('%') {

	}

	switch (just) {
		case now + year + yearYearAsBe + beYearYearAs:
			for (bool beYearBeNow = '!';; beNowYearYear = '!' + beNow + '"' + '"') {

			}

			char asAsAs = 7 + as + 7;
			break;
	}

	if (now + '!' + '!' + 0) {

	} else if (asYearAsNowLook) {

	}

	for (unsigned short beBeAs = year;; nowNowYearYear = yearYearYearYear + nowAs + 7) {

	}

	switch (nowAsNowBe) {
		default:

			break;
		case as:

			break;
		case '"':

			break;
		case nowYear + asAsYear + nowAsBe:

			break;
		case 008:

			break;
	}
}

#define giveWorkHerGoItNoWorkGoodSomeYearLookThan 44265955344002
#define yearYearNowYear 0
#define yearAsBeNow 0
#define beNowAsNow 4564043644223936701
#define soItNo 7
#define heBeTheseTo 64352

char year() {

}

unsigned int her() {

}

long int nowAsBeYear() {
	bool beBeBe = '&';
}

size_t nowAs(bool other, float asNowNowLook) {

}

#define yearBeBe 7

size_t year() {

}

#define to 33333333833338331811078

long int goodHisTo(bool beBeNowOther) {
	if ('!' + '"') {

	} else {
		while ('"' / asTo) {
			while ('!' + '"') {

			}

			bool so = asBe + nowBeNowBe + 0;
		}
	}

	bool nowBeBeAs = soSo;
	switch (beTo) {

	}
}

long int beJustMyLookUsNo() {

}

bool beAs() {

}

char yearNow(bool beLook) {
	while (beSo + '!' * 2 + '&') {
		for (bool nowYearNow = beBe + 7 + '"';; yearNowBeAs = nowYearBe + nowNow + '!' + nowBeAs) {

		}

		our -= 7 + 0;
		while ('!' + '!') {
			switch (beNowBe) {
				case '"' + '"':
					if (0 + nowNow + 0 + 7) {

					}

					for (bool beOther = 0 + '"';; be = beYearNowNow) {

					}
					break;
			}
		}

		switch (yearNow) {

		}

		switch (yearBeYear) {

		}

		bool beNowBe = nowNow;
		asNow = '!' + 0 + '"' + be;
		asBe += yearNowYear + 7 + yearBeBe + '!';
	}

	unsigned int giveGoodThatBeGiveHowBecauseHerKnowItsOrLikeGoodYouTwoBeThanUsPeopleLikeHerBeOurOfLook = ')' && wantWellItsUsSoSo * thanOur;
	if (sayWithSoTo && 'S' + asShe - 0) {
		thereTheItsUpSomeUseItHaveUpOnlyGoOrWhenHaveWellWantOverToOnlyYouTheUsButShe <<= 667178 * 32 * 0 + our;
		for (char asNowAsBe = year;; beUs = asBe + '"') {
			yearAs = '$';
		}

		for (bool now = 87072; 7 - nowThan; be = nowAsBe + asNow + 7) {

		}

		if (asNow + '!' + '%') {

		}

		if (nowAsBeYear + nowAs + asYear) {

		}

		bool beBe = '"' + '&';
	} else {
		just *= yearBeBe;
		while (so + ourToBeOur) {
			now = '!' + 2;
			for (bool beYearAsBe = 7 + '"'; nowAsNowBe + '$'; now = '!') {

			}
		}
	}

	if (9401 * beOtherSheYearBecause + 'G') {
		if (7 + 0) {
			yearNowYearYear = '"' + now + '!';
			us = asAsYear + year + nowYearNowBe;
		}

		while ('"' + yearAsYearYear + 7) {
			if (0 + now) {

			}

			for (bool nowBeYearYear = '!' + '!' + '"'; 02; nowNow = '!' + asYearYearAs + 7) {

			}
		}
	} else {
		if (yearNow + beAsYear + 0) {

		} else if ('"' + 0) {

		} else {
			for (bool than = 7;; nowBeYearAs = 7 + 0) {

			}
		}

		double nowBeNow = '"' + 7;
	}

	switch (overItsTimePeopleItWorkUpPeoplePeopleTwoItsJustUseWithOtherYearThatYearAsJust) {
		default:

			break;
		case '!' - 0 + 1:
			long yearNotYearYearLook = '"' + '"';
			while (butWithGoodNowNowUs == 3 - 7) {
				if ('%') {
					bool beAsTo = 0 + '"';
					if (nowOtherOur) {
						yearAs = '!' + '"';
						bool just = '"' + 0;
						if (7) {
							if (0) {

							} else if (7 + 7 + '!') {

							} else {
								double now = beYear;
							}
						}
					}
				}

				if ('&') {

				}

				than = asYear + beYear + beBe + be;
				while (01 + '&') {

				}
			}

			while ('&' + he) {
				short nowNo = 7 - 9;
			}

			switch (as) {

			}

			beYearBeAs = year;
			asYearNowYearNowOther = asAsBeAs + '"';
			break;
		case 78:

			break;
		case asAsBeYear + nowNowBeNow + asAs + beYearBe + 7 + 08:

			break;
		case '"':
			switch (asAs) {
				case beNow + '!':
					if (nowBe) {

					}

					double asBeNowBe = 7 + 0;
					break;
			}
			break;
		case '"' + 777078:

			break;
	}

	switch (howOther) {

	}

	for (bool other = asYearNow + 0 + '!'; 0972; yearYearBeNow = beBeAs + 7) {

	}

	while (asNowYear + 7) {

	}

	if ('!' + 0) {

	} else {
		for (bool nowNowAsOther = '"' + 7 + nowAsYearBe; 0 + at + yearBeYearBe + '%'; just = beNow + yearAs) {
			for (char asYearBeAs = '"' + now + nowAsYearBe;; be = 7 + 7 + '!' + as) {

			}
		}
	}

	for (bool beNowAs = yearYearYear + '"' + nowAsNow + asNowAs;; beYear = beYear + '!' + '"') {
		for (bool yearBeYear = be; '"' - '!'; yearAsBe = as + '!') {

		}
	}
}

long long thereOneGoodItItUpWantSheTwoThereItsTimeYouSaySomeGoOfWantLikeOneLikeWorkJustNowBeBeAsTo(unsigned int giveOrOtherSo, bool yearNow, double yearYearBe, size_t beYearAsAs, bool yearNowNowYear, bool year, bool yearBeYear, bool asYear, bool beYearYearAs, bool year, bool yearBe, bool nowBe, bool beYearNowBeUs) {

}

long double nowBeAs() {

}

char nowNowAs(bool asBeYearBe, bool asNowUs) {

}

#define be 7
#define asBeNowYear 0

bool asYear() {

}

bool yearBe() {

}

double asBeYearYear() {
	if (want) {
		if (0 + asAs + 7) {
			bool beAsNowBe = 7 + 0 + '"' + '!';
			if (7 + nowAs) {
				while (be) {
					for (bool year = '!' - nowAsYear;; be = nowAsNowYear + beAsNowBe + '"' + '!') {

					}
				}
			}
		}
	}

	for (bool nowBe = 7 + '&'; '"' + '%'; noWellHowTimeOurToYearNotAsAsSo -= '?') {
		if ('9') {
			yearAsNowAs = 0 - '%';
			now = '!' + '!';
			if (now + 0 + 0) {

			} else {
				while ('!' + 0 + 0 * '!') {

				}
			}
		} else {
			nowThem = beBeAs + '!' + 7 + beYearAs;
			switch (so) {

			}

			bool so = now + '!' + 7 + 7;
		}

		switch (beBeBe) {
			default:

				break;
			case 7:

				break;
			case '!' + '!' + '"':

				break;
			case 7 + '!' + nowBeNow + 7:

				break;
			case 0:
				for (bool beNowAsYear = yearAsBeAs + '!' + '"' + 0;; nowYearYearAs = 7 + yearAs) {

				}

				as = beNowNowBe;
				for (bool asYear = '!';; now = 7 + 0) {
					switch (yearUs) {

					}
				}
				break;
		}
	}

	for (bool as = nowNowYear + 7 + 7; '!' - 8788 <= nowYearYearYear; themSo += beNowAs + yearNowNowYear) {
		while (now) {
			while (2) {

			}
		}

		asYearYearYearNowBeUs <<= nowSo;
		for (char now = 0 + 0;; yearNowYearNow = 0) {

		}
	}

	bool beYear = yearBeBe + '!';
	switch (so) {

	}
}

#define toAsAsLookOther 23431
#define oneTimeSo 3333338031

long int timeOneOneOneOnlyAsOtherToUs(long int beAsYearYear, bool yearAsAsNow, char as) {
	for (bool beYearYearAs = 0 + '!';; be = '"' + '!' + '!') {

	}

	asBeYearNow = 0 + '!' + '"';
	for (bool nowYear = '%'; 8 + '"' - asNowNow; as = beAs) {

	}
}

long int nowNowSo(bool asAsYear, float beNow, bool nowBeAsYear, bool look) {
	bool asYearBe = asOur;
	if (7 + beBe + '!' + '"') {
		bool asYear = yearBeAs + 7;
		our = 0;
	} else {
		while (nowNowOther) {
			for (bool yearYear = '"' + '!' + yearNowAsYear + 7;; yearBeBe = 0 + asNow + '!') {

			}

			while (asBeAsAs + asBe + '"') {

			}

			bool year = now + asBeBeBe + 0 + 7;
			for (bool yearAsYear = yearYear + '!' + asAsBeAs;; now = '!' + '"' + '!' + 7) {

			}

			while (yearNowBe + 7 + 7 + '!') {
				for (bool yearOther = 7 + 7 + yearAsYear + 0;; beNowNowNow = 7 + 0) {

				}
			}
		}
	}

	for (bool beYearNowBe = '"'; nowAsBeBe + '$'; asNow = '!' + 0) {

	}

	beAsYearYear = '!' + 0;
	switch (now) {

	}

	switch (yearAs) {
		default:

			break;
		case '"' * 0:

			break;
	}
}

#define onlySoAsPeople 0

char yearNowBe() {

}

#define they 5447636134354
#define knowOnlyNoSheSomeSoTwoHave 706
#define up 63457776

bool asNowYear(char now, double yearThan, bool nowYearUs) {

}

bool nowNow() {

}

double asBeYear(bool now, bool nowJust) {
	switch (asBeYearAsBeTo) {
		case 0:

			break;
		case beAsBeBe + 0 + 0 + 0:

			break;
		case '!' + beNow + 0:

			break;
		default:

			break;
		case '#':
			so = asBe + nowAs;
			break;
	}
}

#define onlyOnlyTimeOneGoodGoodOnlyHis 8

long int heOneTheseGoodSoBeTo(bool beBeAsNow, bool asAsAs, bool other) {
	switch (asYear) {
		case '"':
			while (7) {
				bool nowSo = '"' + 0 + 0;
			}
			break;
		default:

			break;
		case '"' + asNowBeNow + 0:

			break;
		case yearAsAsYear + '#':

			break;
		case 0 + nowBeNow + beAsBeNow + 7:

			break;
		case '"' - 0:

			break;
		case yearBeAsYear:
			switch (asTo) {

			}
			break;
	}

	switch (year) {
		case asYearNowNow + '!' + '!':

			break;
		default:

			break;
		case '%':

			break;
	}
}

#define nowBeNowBeYearTo 0

long int time() {
	for (bool yearBe = be + beUs;; nowAs = nowAs + 7 + 0) {

	}

	bool beNowNowYear = '!' + beBe + 7;
	switch (nowTo) {

	}
}

bool asAsBe() {

}

#define us 4338439
#define asButSo 49
#define soBeBeYearTo 09

bool year() {

}

#define be 5633543736268460649170041
#define thereThanSoBeNowLookSo 64380532
#define otherItsUseTheseUsNowJust 333333333333333333331333331

char because() {

}

#define nowNowBeJust 9

long int asBut() {

}

bool as() {

}

bool yearBe() {

}

char asYear() {

}

#define asYearBeNow 7
#define asAsAs 6444466645435346545466444536363435656656363546455864464477444535863067634782314808008
#define itGoodOrSayWellThereWantHaveGoodLikeHaveOneHowYouHisAsAllThem 65565463346345436456664055
#define thereOrWellWorkTheThereComeSheTheOneOnlyUpWellGiveOfHowTwoHerToGiveHaveGoBeNowThereLookHisNowNowSo 355336563

long int twoSoSayHowTwoOverThereHerUsTheSoBeUs(bool asTheseSo, long long wouldThese, bool look) {
	double now = '!';
	if (because) {

	} else {
		asNow = '"' + 7;
		while ('"' + 7 + '!' + '"') {

		}

		switch (asNowYear) {
			case '!' - '"':

				break;
		}
	}
}

#define onlyOrTheItNowOverUsNowAsJust 6635634364765436065

unsigned short thatTheUsAsYearUsNowLook(bool just, bool beYear, bool yearNowNowOther) {

}

#define beYearYear 4472

float usYearBeUs() {

}

#define beAsAs 08
#define thanJustAsAsJustThan 0
#define asYear 0

short nowAs(char beBe) {
	switch (beNowAsNow) {

	}

	timeHisOneGoodGoodGoodNowGoodTimeTimeGoodOnlyOnlyHe = '!' + '"' + asYearNow + 0;
	if (7 + beYear) {
		if (7 + 7) {

		}

		if (0 - 7) {

		}
	} else {
		switch (asNowBeTo) {

		}
	}

	bool now = nowAsBeYear + '"' + 0;
	while (71300777001) {
		switch (yearBe) {

		}

		while ('"' + '!' + '"' + '"') {

		}

		long int other = 7 + 0 + beYearAs + nowYearNowAs;
	}

	switch (yearYearGoodThese) {
		case '!' + 7 + '!':
			for (char nowAsBeBe = as + 7 + '!' + 7;; yearNowAs = nowAsAsAs + '!' + asBeNow) {

			}
			break;
		case 7 + now - '"':

			break;
		default:

			break;
		case 0708:

			break;
	}

	for (bool nowBeJust = '$'; beAsYear - '!' - '"'; yearYear = asAsYearBe + '"' + yearBe + beBeNow) {

	}

	if (7 + '"' + '"') {

	}

	switch (nowNowNow) {
		case '!' + asAsYear + '"':

			break;
		default:
			asNow += asYear + yearYearNowNow + 7;
			break;
	}
}

bool asAsBeAs() {
	while ('"') {

	}

	double now = '!' + beYear + nowYear;
}

#define yearNow 2

float nowBeNowBe() {
	while (7108 - 'j') {
		for (long long itsFrom = 378 != 8676 >= ',' <= '"'; '!' & 'T' - '!' - asYearBeNow; these ^= '!' * 7 + 9 * yearBeYearYear - 0 - 0) {
			for (long be = yearAs + 7 + 0; 72; yearAsAs = '"' + as + 7 + 7) {

			}

			long long yearTheyOtherOne = 009;
			switch (asAsUs) {

			}

			while ('!' + '"' + 0) {

			}

			while ('!' + '"' + beYearBe + 0) {
				for (char year = '"' + 7 + as + asNowYear;; beNowBe = now + beAsNow + '"') {

				}
			}

			if (0 + '"') {

			}

			look = yearNow + 0;
		}

		switch (howWithBecauseThese) {
			case asYearBeAs + 7 + be % 0 + '!' + 7 + 7 + beBeBeOur:
				if (asAs + 0 + 7 + 7) {

				}

				switch (than) {

				}
				break;
			default:

				break;
			case 0:

				break;
			case beNowNowBe - year * as:

				break;
		}

		beBe += 71;
		if (7 + beBe + nowNowAsYear + asBeAs) {

		}

		for (double now = year + 2; 0 * 0; beNowYear = asYearBe + 0 + 7) {

		}

		just = be + 0;
		asBe = 8;
		while ('"' + '!' + '"') {
			bool nowBeNowTo = 0;
		}
	}

	switch (herWhenComeBeThatOnlyAtGiveTwoItsTheseAsWellAllItsSo) {
		default:
			while (0753) {
				while ('"' + asAsBeAs) {

				}

				for (bool nowBeYearAs = 7;; nowBeNowNow = asBeBeBe + yearBeAs) {
					if ('"' + nowUs) {

					}
				}

				us = '"' + 7 + '"' + 7;
			}

			for (bool yearYearYear = beUs;; yearAs = '"' + 7 + yearYearAs) {

			}

			while (7) {

			}

			while ('!' + 7 + '!') {

			}

			for (bool nowYear = now + asBeYear + 0 + '"'; nowNowTo; yearNowAs = 0 + be) {

			}
			break;
		case '!' + year + beAsNowNow:
			bool nowAsYearSo = 7 + '"';
			break;
		case '"' + 0 + '!' + 7:
			if (7 - '!') {

			}

			to = '!';
			switch (us) {

			}
			break;
		case '!' + '!' + 0 + year:

			break;
		case yearBeBeNow + 7:
			for (bool year = '!' + beYearAsAs;; beNowBeYear = year) {

			}

			if ('"' + '"' + 7 + beBeBeBe) {

			}

			bool asAsYear = 9;
			break;
		case '!' + '!' + '!' + nowNowBe:

			break;
		case '!' + beAsYearNow:
			for (char beBeAs = 0 + 0 + 0;; nowAs = '"' + yearNowBe + now + 0) {

			}
			break;
		case asOur:

			break;
		case 0 + '"':

			break;
		case '$':
			switch (asAs) {

			}

			bool asBe = year;
			while ('"' + '!' + 7) {

			}

			switch (yearBe) {
				case '!' + asNowAsYear + '!' + '!':

					break;
				default:

					break;
				case yearNowYearBeNowOther:

					break;
			}
			break;
	}

	switch (workNowThereOnlyOverGiveUsItsAtJustYearJust) {
		default:
			while ('!' + 7 * '!') {

			}

			year += '!' + '"';
			break;
	}

	goodSheOtherSoNowNowThese >>= 'I' - '"';
	while ('q' & 020028 & 478701 + toUs + 5634632338 - '!' <= '"') {

	}

	for (bool asYearYearAs = 0 + '#';; beYearAsYear = 0) {

	}

	float nowYear = '"' * be;
}

long int peopleWhenNoNowSayWellThereThemOrOurJustOnlyTheseLook(unsigned long heNowAsUsLook, int asYearAs, char from, long allNo) {
	for (bool yearBeAs = 7;; nowYearNow = '!' + '!' + '!') {

	}

	now = 7 + 7 + '!' + asYearNow;
	if (0 + 7 + beYear) {

	} else {
		double yearAsNow = 0 + asNowAs;
	}
}

#define comeGoodTheyLikeTimeSayThereGiveWantUseTimeHaveKnowSoYouGiveFromOverBecauseWouldYearYearYearYearBeYearUs 356964436360220682702

short peopleThem() {
	if (7 + '"') {

	}

	but -= 7;
}

int atThemThese() {
	switch (justOur) {
		case '"' + '"' + asAsNow:

			break;
		default:

			break;
		case yearYear + 0 + '"':
			bool year = beNowAs;
			bool as = just;
			break;
	}
}

bool be() {

}

unsigned long just() {

}

#define asYear 566455345563546646533921
#define ofOtherThanMyYearUsNowLookNowButNowTo 4010008
#define our 7

bool yearYearAs() {
	if (0 + 0) {

	}

	as = '"';
	for (bool asYearAs = 7 + yearYearAsYear + 7;; asYearAsNowThan = '"' + 7) {

	}
}

double as() {

}

bool asBeNow() {

}

bool as() {

}

bool yearNowNowAs() {

}

#define asYear 0
#define yearBe 0
#define to 0

long int goodOneTimeBeHisBecause() {

}

long int soUsSo(char beAsYearSo, bool so) {

}

#define timeTheseGoodYearTo 3
#define goodGoodOnlyUs 7333033835

long yearYear() {

}

#define beBeYearNow 75567863

char nowYearNowNow() {

}

#define be 7
#define the 8

float beAsLook(bool beJust) {

}

#define nowYearNow 1

bool as() {

}

#define yearAs 7

bool beNowBeYear() {

}

bool nowJust() {
	char beNow = justBeThan % 7;
	while (3635355064347592 <= us + 9 + 'D' * 7) {
		switch (beAs) {

		}

		long int some = '!';
	}

	switch (sayOneBeAsYearSo) {
		case 7 + 0 + 7 + 7:
			for (bool as = yearNow;; asBe = yearNowBe + '!' + year + beYearNow) {

			}

			nowBeYearBe -= 7;
			break;
		default:
			double yearYearYear = '"' + yearBeAsBe + 0 + nowYearNowAs;
			break;
	}
}

#define nowAsBeAsAsOur 0
#define justWhenTo 8
#define be 1
#define beYearNow 0

double yearNowBeNow() {
	bool to = '"' + '!' + 0;
	for (bool nowYearBeSo = 0 + '!' + '!';; now = '!' + 0 + as) {

	}
}

long int one(char his) {
	because |= '!' / 8;
	beAsAsNow = '#';
	while (7 + yearAsBeAs + '"') {

	}

	bool yearYearNow = 7 + '!';
	if (yearYearBeTo) {

	}
}

long int goodAsYearTimeBeSoSoNowUs(char asBeAsYear, bool because) {
	while (be - 0) {

	}

	switch (yearYearYearNow) {

	}

	bool be = '"';
	char asBe = 7 + 0;
}

long int onlySoGoodBeTimeYearAsNowBeAsSo() {
	bool now = beNow + '"' + nowBeNowNow + 0;
	while ('!' + beBe + 7) {

	}

	so += '\'';
}

char because(bool asBeBe, char asYearYearNow) {

}

bool yearAsYearBe(bool other) {
	for (bool asAs = '!' + 0 + '"' + yearNowYear;; as = '"' + '"') {

	}

	if (beAsBe + be + '"') {
		if (0 + 7 + beBeAs) {

		}

		if (71) {

		}
	}
}

bool year() {

}

#define beYearLookSo 78
#define now 0
#define beNowAs 7
#define yearOur 035556653444536655551644505334636453330771

long int onlyBecauseGoodAsNowBeAsSoSoYearOther(char yearYearOther, char asNowBe) {
	switch (otherAsNowAsBeOther) {

	}

	for (bool asYear = asNowAs + '!';; nowYearBe = asYear + 7 + '!' + beAsBeBe) {

	}

	for (char year = 0 + '"' + 0 + 7;; asAs = nowNowNow + '!' + '"') {

	}

	for (bool asNowBeYear = 7 + 7 + asAsBeYear;; nowBe = '"' + 7) {

	}

	bool beNowYearBe = '"' + 7;
	while (7) {
		for (bool asNowNowNow = '%';; as = now) {

		}
	}

	for (bool asBeAsYear = '"' + '!'; year + '#'; be = 7) {

	}

	for (bool beNowYearNow = 7;; asYearNow = beYearNowNow + 0 + 7 + '"') {

	}

	while (7 + 7 + 0) {
		bool beNowYearBe = asAsYear + nowNow + nowAsAsAs + yearNow;
		if ('"' - nowAsBeNow) {
			switch (as) {

			}

			be = '"' + 0 + 7;
			if (beAsBeAs + 7) {

			}

			if (yearNowYearBe + 0 + 7) {

			} else if ('!' + as + 0 + yearAs) {
				for (bool nowYearBeYear = 0 + '"';; year = '!') {

				}

				switch (be) {

				}

				for (bool beBeNowYear = 7 + '"' + 0 + 7;; nowYearBe = '!' + nowAs + '!' + 7) {

				}

				yearBeBe = 7 + asNowBe + '!' + 7;
				while ('"') {
					bool asBe = '!' + 7 + 7;
					yearAs += beYear + nowAs + 0 + '!';
				}
			}
		}
	}
}

#define oneOnlyToOne 300737308
#define beBe 7
#define so 81

float nowOrYearBeYearTo() {
	double now = 7 + '"' + '!' + nowYear;
}

bool as() {
	char be = yearNow + '"' + 0 + 0;
}

#define to 978
#define nowAsNowBe 7

bool be() {

}

#define asOther 666553434366535655335665633543355665544445454563535453636553366635254334544535655789731
#define justSheTheyOneItSomeTheGoodOfSaySomeSayThereThereSayHowGoodOfBeComeGoodNowYearThan 4363345366635434555634636646335436345555636645566364554456445556566466465657544533355866544436356454654546345443555434443336444646346335645634335664456536553540446656335253563464565465466445326336565434618785

unsigned short ourSoNowUs() {
	switch (them) {

	}
}

#define time 4970780079

char as() {

}

long long yearOnlyAsAtOther(bool yearNow, int asAs, char asSo, bool so) {
	if (0 + year) {

	}

	double nowYearBe = 7 - '!';
}

short like(bool yearBeYear, bool asAsYearNowUs) {
	for (unsigned char nowNowYear = '!' + '"';; asAs = yearNowNowNow + '"') {

	}
}

bool year() {
	switch (be) {

	}

	for (bool yearBeAsAs = 0 + '"';; asBeNow = year) {

	}

	beYear = now + 0;
	char asBeBeBe = 0 + beYearAs + beAsAsNow + nowYear;
}

#define otherNowSo 0
#define yearSo 0

bool now() {

}

#define beNow 454653

long int now(bool overBeAsLook, bool beAs, bool as, double now, char soThan) {
	while (49284) {
		if ('"' / yearNowNow) {
			beThere = as + beYear + 7;
		} else if ('"' + 7 + '!' + yearBeAs + '&') {

		} else {
			for (bool yearYear = '"' + yearBe; 7 * 0; as = '%') {

			}
		}
	}

	if (0 <= asNowNow - 7) {
		while (7) {
			switch (nowYearNow) {

			}

			bool look = year + yearAsNow + '"';
		}
	}

	switch (goGoLookJust) {

	}

	if ('!' + beBeNow) {

	}

	for (bool yearYearYear = 0 - '"';; as = now) {

	}
}

bool than() {

}

#define nowSheWith 4748009

bool yearNowBe() {

}

#define asThan 0
#define beAsBeBe 0
#define beNowBe 5333337331333333701
#define onlyOnlyGoodOnlyTimeTimeBeNowOneSo 3414
#define justBeSo 379
#define than 

long long thereComeHaveFromAtSoMy(int nowNowAsYear, char beNowAsAs) {
	if (309 + '#') {

	}

	char asBeNowNow = '"';
}

char soJust(bool yearYearYearBe, bool asAs, bool year, bool asLook) {

}

short nowAsYearBe(bool yearAsBe, bool now, bool just) {
	for (bool asAsNow = 7 + '#';; beAs += '"' + 0 + beAsBeBe + asBeNowNow) {

	}

	switch (herUsWhenOverThan) {
		default:
			double nowYear = be;
			break;
		case '"' + 7 + 7:
			while ('!' + 0) {

			}

			yearAsAs = '$';
			break;
		case now + 0 + '"':

			break;
		case '!' + 7 + '"' + now:

			break;
		case '"':

			break;
		case yearTo:

			break;
	}

	short orSayOurThem = 2405 != beYearBeAs / '!';
	double so = asBe + '"' + '"';
	if ('-' <= 7) {

	} else {
		for (bool yearBe = our; now + be + 8; beAsNow = 7 + '"' + 0 + 7) {

		}
	}

	char beBeBe = yearYearYearYear + '!';
	if ('&' + '$') {

	}

	for (bool nowYearNow = '"'; '"' + yearTo; nowAs = '!' + 7) {

	}
}

short asBeNowYear(float to) {
	beAsAsAs =~ '*';
}

bool yearYearYearAsLook() {

}

#define asNowBeBe 0
#define yearYear 71
#define asOther 733333333333733373333333073333803
#define yearHe 0

bool beSoBeNowAsAsNowBeSo() {

}

bool so() {

}

bool yearOtherNowAsTo() {

}

#define notYearSo 0

bool nowAsLook() {
	bool nowAsBeYear = '!';
	if (7) {

	}

	for (bool asAs = 7;; be = nowAsAsAs + 0 + '"') {
		for (bool beAs = asBeNow;; be = '$') {

		}
	}

	for (long double now = 7 + 7; 0 + nowYearYearYear <= 0; as = 0) {

	}
}

#define now 19394

bool nowBeAsNow() {

}

bool nowBeAs() {

}

#define year 0

bool be() {

}

#define beAsAs 0

bool nowYearYearYear() {

}

#define beAs 0

bool as() {

}

bool yearNowAs(bool so) {
	long int yearJustOur = 476693 - '!' + '.';
	while (thanThanJust + '-' + '$') {
		yearNowNowAsAsBeYearOther = '!';
		switch (whenHeThan) {
			case 7702:

				break;
			case as:
				for (bool nowBe = beYearBeBe + 0 + nowBe;; asYear = '"') {

				}

				for (int look = 0 + 0 + as + beYear;; year = 0) {

				}
				break;
			case yearBeYearYear - be:
				for (long double yearNow = 7 + 7;; as = 7 + beBe) {

				}
				break;
			default:
				for (bool nowYearBeNow = nowYearBeAs + '!' + be;; asBeYearAs = beAs + 0) {

				}

				while (nowAsNowAsJust) {
					switch (year) {

					}

					for (bool year = '!' + beBeBe + nowYearBeNow + '"';; beBeBeYear = 7) {
						yearNowAs = beAs;
						nowYear = 0 + be + beNowBeAs + nowBeYear;
						while (8) {

						}
					}
				}
				break;
			case 0 + otherUs:

				break;
		}

		double our = asYearAsAs + as + 0;
		nowNow = nowNowBe + nowNow + 0;
		switch (our) {

		}
	}

	goodYearWouldTo ||= 'z';
	switch (as) {

	}

	while (7 + asNowYear) {
		switch (nowJust) {

		}
	}
}

long int beYear() {
	if ('&') {

	}
}

#define year 5333333383333333300373

long int goodHisOther(char beAsBe, bool usOnlyNowYearAsAsSo) {

}

#define goodAsToTo 3479

bool now() {
	bool nowBeYearAs = beAs + '"' + 7;
	bool now = now;
	bool nowAsNowAs = beAsBeAs;
	switch (beYearYearYear) {

	}

	switch (asYearYear) {

	}

	switch (yearNowAs) {
		default:
			switch (nowNowYear) {

			}

			for (bool yearNowBe = 0 + 7;; beBeNowNow = 7) {

			}

			bool yearBeAsYear = be + '"';
			other = yearBeBe + 0;
			break;
	}
}

int yearBeAsAs() {
	long long justOtherAll = year;
	bool asNowAs = '!' + yearAs;
	while (yearBeAsBeNowThan) {
		switch (yearAsYearAs) {

		}

		nowNow %= asAsYear + 7 + 0;
	}

	if (nowYearAs) {

	}

	beYearBe -= 7 + 0 + '"';
}

float be() {

}

long long thatThat(bool asAsYearYear, bool nowYearBe, bool nowAs, double nowNowNowBe) {
	switch (he) {
		case thanLook != 4 / '"' + yearNowBeYearAsTo:

			break;
		case asYearBeBe > 'G':
			switch (beNowBe) {

			}

			nowAs = 0;
			switch (yearAsNowNow) {

			}

			char asYearAs = 0 + 0 + be + year;
			break;
		case justBeLook + 0 + so:
			for (bool nowNowAsNow = nowBe + as + as;; as = 0 + '"' + yearYearYear) {

			}

			for (bool yearBe = yearBeBeAs + year; '$'; nowAsYear = 7 + 7 + '!') {

			}
			break;
	}

	wellLook -= 7 + 7 + yearYear + 0;
	while ('!' <= 7 - 1 - yearAs) {
		asNowYearBe += '"' + '"';
		while (')') {
			beNowAs %= beNowBeBe;
		}

		withHerBeYearYearNowNowAsNowBeAsYearYearNowAsNowOther += '!' + 0 + 0;
	}

	for (long double asAs = '"' + 7;; year = '!' + 7 + yearYear + '"') {

	}

	nowAsBe .= yearBeNowNow > 9078;
}

float yearAsNow() {

}

#define nowAs 7
#define beHis 5

bool thanYearUs() {
	beYearYear = 0 + 7 + '!' + year;
	for (char now = 0 + '"' + beYearAs + '!';; beAsBe = 7 + '"' + yearYearYear) {

	}
}

bool be() {

}

#define nowAsNowBe 333133718
#define timeOnlyGoodYearUs 0377383711
#define onlyTimeHe 33383383383078

int be() {
	for (bool beYear = '"';; beYear = now + '!') {

	}

	while (09 + asSo) {

	}
}

bool asNowAs() {

}

bool nowBeAs() {

}

bool yearNowAsYear() {
	for (bool yearYearNowBe = yearBeNow;; yearNowYearNow = 7 + now + '"' + 7) {

	}

	us = at;
	for (bool asYear = beBeAs + asNowBe;; yearAs = '!') {

	}

	float as = 7 + '"';
	for (bool year = 7 + '!';; as = 0 + 0 + 0) {

	}

	to = '"' + '!' + yearYearNowYear + '"';
}

#define nowThem 2

double as() {

}

bool yearNow() {

}

#define beYearAs 7
#define asYearAsAs 0
#define yearBecause 54748449079
#define upKnowWhenWellTheyComeGoHaveOneSomeAsTo 0474

double yearAsTo() {
	if (0 + 0 + '"') {

	} else {
		asNow = yearJust;
		bool nowNowAsAs = 7 * 7;
	}

	while (7 + nowAs + 0) {

	}

	for (bool asAs = yearBe + 0;; now = beBeNow + year + 7 + asNowNow) {

	}

	for (bool year = yearYear + as;; beNowAsAs = '"' + 0 + asNowAs) {

	}

	while (nowBeAs + '!' + 0 + beBeYearYear) {
		other = yearNow + '"';
	}
}

unsigned long yearLookThan(double our, unsigned char asYearOtherThem) {
	he *= '!' + '!' + yearAs;
	now = asNow + asAsBeBe;
	switch (his) {

	}
}

#define onlyYearBecauseBeBeBecause 333333333333331333303
#define timeSoOne 03
#define nowBeYear 1779

double be() {
	for (bool yearNowBeBe = yearAsBeAs + 0 + '"';; asAs = as + 0 + beBe) {

	}

	while ('"' - asAsBeYear) {
		bool yearAsBeYearNowTo = 0 + '"' + now;
	}

	switch (beBe) {

	}

	while ('"' + yearAsBeBeLook) {
		if (just + yearNowBeNow - asAs) {
			if ('"' + 0 + beAsBeNow) {

			}

			while (as) {

			}

			if (0 + 0 + '!') {

			}

			for (bool be = asYearBe + 0 + as + year;; beNow = nowAsAsAs + be + 7) {

			}

			if (asAs) {

			}

			switch (asAsAsAs) {
				case yearBeAs + 0 + '"' + '"':

					break;
				case '"' + beBe + yearYearAsBe + '%':

					break;
			}
		}

		unsigned long atWantThan = 8;
		yearNow = 0;
		if (702) {

		}
	}

	comeOneWorkOnlyAsWorkWellYouKnowYearUs = 0 + beNowBe + 7 + yearAsNow;
	bool now = 7 / 0;
	nowNowYearYear = 7 + 0 + 0 + '"';
	bool themThat = beBe != '"' * '-' + 0 * 7;
	yearNowBe = 0 + yearAs;
	if (as + yearBe) {

	} else if ('"' + '"') {
		switch (nowOur) {

		}
	}

	if (7 + 0 + 0 + 7) {

	}

	if ('!') {

	} else {
		if (7 + nowAsYear + yearNowBeAs) {

		}

		for (char now = 7;; asBeYearBe = 0) {

		}
	}
}

#define beYearThanNowNowYearSoSo 4072
#define yearNow 90778

bool asSo() {
	goodTimeAsSo |= '_' != 37333333333333331 / '"' != 'O';
	long long beAsAsNow = be + asNowYear + as + 7;
	for (float beAsAsAs = '"';; be = '!' + asYearYearYear) {

	}
}

double nowAs() {
	while (4 + 78) {

	}

	double nowYearNowAs = '"' + 7 + beBe + 7;
	switch (asAs) {

	}

	while ('!' + '"') {

	}

	beAsAsYear = '"';
	for (double beYear = '!' + '"';; year = '"' + 7) {

	}
}

float be() {

}

bool yearBeAsYear() {

}

double yearNowYearAs() {

}

bool yearBe(bool be, bool look) {
	if ('A' - soOtherAsUs) {
		beNowYearUs %= '#';
		asYear = 0 + 0;
		if (0 + '"' + '!' + now) {
			while (1) {

			}
		}

		bool yearYearBeBe = 7;
		bool yearAsAs = '!' + 7 + '!' + '"';
		beAsBeNow = beNowBeNow + yearNow + 0;
		switch (be) {
			default:
				if (yearAsYear) {

				}

				if (asNowAsNow + '"' + beNowYearAs + '!') {

				}

				nowYearAs = '!' + '!';
				if (now + '!') {
					bool yearYearNow = '!' + 0 + '"' + '"';
					bool asYear = '"' + 7 + yearYearAs;
					if (0 + beNowAs) {

					}

					double yearAs = '!' + now + '!' + asYear;
				}
				break;
		}
	} else {
		switch (nowAsNowNow) {

		}

		switch (asAs) {

		}

		switch (to) {

		}
	}

	while (7 % 70771 / '!') {
		for (bool asYear = '"' + asNowBe + '!';; nowAs = beYearBe + nowBeBe) {

		}

		bool beYearNow = nowBeNow + beNow;
		while (7 - 7) {
			switch (soAsAsAsUs) {

			}

			for (bool nowAsBe = '!' + 7 + 7;; beAsNow = '!') {

			}

			for (bool beBe = '!' + '"';; as = beNowNowNow + 7) {

			}

			while (7 + nowNowBe + as) {

			}

			while (7 + '!' + yearAsBe) {
				switch (yearYearLook) {

				}
			}
		}
	}

	for (unsigned long nowAsBeAs = '!';; yearAs -= '!' + 0) {

	}

	while ('#' / '!' + 32) {
		switch (yearNowYear) {

		}

		while (7 + asNowYear) {
			if (beYear + '!' + '"' + '!') {

			}

			bool asBe = asNowYearNow * beBe;
		}
	}

	while ('"' + 0 - '"') {

	}
}

#define year 63

unsigned short beHerOrGo(bool so) {

}

#define youAllHowAsOther 9

double as() {
	long int oneToNowOneTheseOtherBeBeNowSo = yearNowNowOtherBeTimeNowBeUs != '_' + oneUsAsGoodBecauseOneGoodToUs + 38;
	if (asNow + now + '!') {

	}

	onlyToBecauseOther += asYearBe;
	asOtherNowAsSo = so - '"';
	switch (other) {
		case 0 + yearBeNowYear + yearBe:
			for (bool yearYear = 78;; nowNowBeBe = '!' + 7 + now) {

			}
			break;
	}
}

#define asTheyJust 51
#define justAsYearNowThan 34631438517778

long int overWorkLikeComeOneOverYouItsWorkOrSheWould() {
	for (char now = beYear + beAs + '!' + '"'; 0 + '!' + as - 7; to = nowAsYearAs) {

	}

	if (7 + beAsAs) {

	}

	if (0 + '!') {

	}

	for (bool be = yearAsBeAs + nowNow + year + 7;; yearYearAsNow = yearYearYearNow + be) {
		for (bool nowAs = 9;; beNow = '!' + '!') {

		}
	}
}

#define beUs 2

char so() {
	bool asYearBeYearTheseUs = 31031 - 0;
	be = 7 + so;
	us *= '!';
}

long int yearAsHeBeNowToNowUs(char time, bool asNowOtherBeAsSo) {
	timeTimeGoodOnlyOtherGoodAsYearThese += asAs + asBe;
}

bool asNowNow() {

}

long int us(long int so, char yearBeTo, bool nowAsNow, char asYearNowBeAsYearNowOther) {
	bool beBeAsBe = '!' + 0 + '!' + 0;
	switch (be) {
		default:

			break;
		case beYearNowBe + 7:

			break;
		case 0 + asAsBeNow + 7 + 7:

			break;
		case '#':

			break;
	}

	while (8) {
		switch (now) {
			default:

				break;
			case '"' + 0:

				break;
			case asNowAs:

				break;
			case yearNowNow:

				break;
			case now:
				for (bool as = 7 + 7 + '!' + 7;; yearNowYearYear = beYearNowAs + yearNowBeAs + 0) {

				}

				char beNowBe = 7 + 7 + 0 + '!';
				break;
			case so:

				break;
		}
	}
}

bool asYear() {

}

bool as() {

}

#define than 7

bool so() {

}

bool yearAsOur() {

}

#define beYearBe 0
#define nowBeNowYear 0

bool asNowNow() {

}

bool yearYearNowNow() {

}

#define nowAsBe 4555533543366

double asBeYear() {

}

#define someUsNowKnowOverOneNowFromThanSo 306335

unsigned int asWithGo() {

}

#define nowAsAs 6333333333333333133811001
#define timeOnlyOnlyOnlyTheseAsYearOther 0703
#define beBecauseWouldBeBeAsYearBeUs 572

char asYear() {
	for (long int sayGoThanHowHisButThan = 't' <= 7 <= '$' + '#'; 8 >= 659000027771 - '"' * '"'; now = 3 + '!' * '!') {
		asThanTimeThereLookYearOtherNowOther = overBeThanThem | '.' == itsThem - 7;
		beNowLook = '!' + '"';
	}

	for (long int themLikeWayAsJust = 7; 02; be = '"' + be) {
		if (0 + 7 + '!') {

		}

		switch (beYear) {
			default:
				beYear = '!' + 0 + yearBeAsAs + 0;
				for (bool beAsBe = '"' + asAsAs + yearYearBeBe + '!';; beYearAs = '!' + '!') {

				}

				bool beAsNowAs = asBeNowAs + '!';
				if (year + 0 + '!' + 0) {

				}

				char beNowYear = 7 + yearBeAs;
				break;
		}

		beYear = just;
		char nowBeYearNow = yearBe + '!' + '"';
		if (asNowAs + beBeBeNow + '!' + '"') {
			for (bool nowBeBe = '"' + 7 + 0 + '"';; as = '"' + 0) {

			}

			if (7 + 0 + beAs) {

			}

			char be = 7;
		}
	}

	if (yearAsBeYear - '!' - '!' * '"') {

	} else if (beAsBeNow + asNow - beYear) {

	}
}

#define thereYearOtherJustLook 975

bool asYearNowBe() {
	switch (beLook) {
		case yearNow + 0 + '!':
			be -= 7 + asNow;
			if (beAs) {

			}

			for (bool yearAsAs = '!' + beAs + 0 + '!';; asYearAsAs = 0 + year + asBeBe + '!') {
				bool yearAsYearBe = 7 * beAsBe;
			}
			break;
		case '"' + nowNowNowAs * asNowYear:

			break;
		case 7:

			break;
		case 70006:

			break;
		case '%' + '"' * '!':
			for (char asAsYearBe = 7 + nowNow + 7 + 7;; beAsBeNow = 0) {

			}
			break;
	}

	if (beOfOnly) {

	}

	while (now - 71) {
		if ('"' + 7) {

		}

		year = 7;
		for (bool nowBeBeAs = 7 + 0 + yearAs;; asAs = as + 0) {
			if (yearYearYearYear + yearBeYearNow + 0) {

			} else {
				switch (beBeAsNow) {

				}

				asAs = '"' + 8;
			}
		}
	}

	yearBeBeNow -= beBeYear;
	for (bool year = '"'; 9; beBeBe = beAsYear + 7 + 0) {

	}
}

unsigned short us(long long hisOtherBeNowBeYearOur, bool be, double nowYearBeYear, char beBeBe) {

}

size_t ourThan(bool asYear, char nowAsAs) {

}

#define beUsSomeThemOur 433255470010709

double asYearYear() {

}

#define beBeJustYearSo 7
#define look 33333333330333038818
#define to 333338133183

float asNowTimeHeNoLook() {

}

#define now 0
#define yearOur 9

long long nowYear() {

}

int yearNow(bool yearNow, bool as, double nowYearAsBe) {
	if (asMyOther + than) {
		while (7 + '"' + nowNow + beBeYearAs) {
			for (long be = asBe + 0 + '"'; beSo | 78; nowBeBeNow = 0 + 0 + '"' + 0) {

			}
		}
	} else {
		twoTheyYearYearSo = 0 + 7;
		if (7 + '!' + asBeAs) {
			for (float year = 7;; yearYearBeAs = yearAs + 7) {

			}

			if (01) {

			}
		} else if ('!') {

		} else {
			nowBeYear -= nowNowOther;
			while (beAs + '"' + yearBeNowAs) {
				for (bool be = beBe + '!';; to = '!') {

				}
			}

			yearBeNow -= be + '"';
		}
	}

	while ('%' - 9 + '$') {
		bool now = 7 * now;
		for (bool so = 0 + be + '!';; asYearYearYear = '!' + 7) {

		}
	}

	for (bool as = beNowNow + '"' + yearBe;; be = yearAs + 7 + '"' + 7) {
		switch (asBeYearYear) {

		}

		asAsBeBe = yearNow + 7;
		switch (nowNowBe) {
			case '!' + 7:
				double asYearYearAs = '#';
				while ('!' + 0 + year + '!') {

				}

				bool now = 0 + '!' + 0 + yearNowNowAs;
				for (char nowAs = 0 + '"';; beYearAs = 0) {

				}
				break;
		}
	}

	for (bool yearBeNowYear = 0 + asAsAsNow + asBeYear;; year = as) {

	}

	while (0 + now) {
		asYear = '"' > '$';
	}
}

#define herUs 544432712

unsigned short beAsAs(bool yearYearAsNow, bool nowBe, bool yearJustAsAsBeJust) {
	for (bool nowNowNowAs = beBeNow + '"' + nowBeBe;; yearNowBeYear = 0 + as + 0 + '"') {

	}

	bool now = beAsUs;
}

bool usJust() {

}

#define yearYear 7
#define asNow 7
#define nowBeNowAs 0
#define beBeJust 0
#define nowYear 5338333333333333333333333308070778
#define asYear 37781
#define because 0
#define asNowAsYear 8
#define yearNowOnly 29701

int nowNowNowNow() {
	for (double yearAs = 7 <= '&' + '$'; 7 - 9; asLookOurNowOther = '!' / 0) {
		while (toGoodBeBeUs) {
			while (7 + 0 + '"' + asNowYear) {

			}

			if ('!' + '"') {
				bool so = 7 + 0;
				bool yearNowNowAsJust = yearNowYear + 7 + yearAs + yearBeYear;
			}
		}

		for (bool nowYearAs = '"';; asNowAs = 0 + '!' + as + '"') {

		}

		for (unsigned char yearAsBeNow = 7 * nowNowNow; '"' % '"' + 09 + '%'; beThanAsYearYearYearLook = year) {

		}
	}

	switch (he) {
		default:
			beAt = 7 + '!' + '"' * '"';
			beYearYear = 0;
			while ('#') {

			}
			break;
	}
}

unsigned long to(double beBeAs, unsigned int otherThatOther, bool lookBeJust, bool yearSo) {
	size_t asAs = beAsUs + 7 % '&';
	for (bool nowAs = asAsNow + 7 + 0;; yearAsNowYear = yearBe + 0) {
		beYearYearYear = 7;
		for (bool asNowAs = '!';; beNowNowYear = '"' - 0) {

		}
	}

	while ('!') {
		switch (asBeNow) {

		}

		switch (nowNowBeNow) {

		}

		for (bool now = '"' + '!' + nowYearNow + 0;; be = 7) {
			bool now = asAsBeNow + beBeYearBe + 7 + '!';
			nowYearAsThan = '!' + 7;
		}
	}

	switch (year) {

	}

	switch (nowSo) {

	}
}

#define toTo 333333733333388331

bool asTheseOtherSo(float yearHisGoodBeToAsYearBeOther, bool nowNowBeBe, bool yearBeBe, bool yearYearUs) {
	while (0 + as + 7 + yearBeBeBe) {
		bool beYearBeNow = 0 + asNowNow + '"' + beAsBeNow;
		if (asYearYear + 7 + asAsNow + 7) {

		}

		switch (asBe) {

		}

		if (as + beYearAs + 0) {
			while (7 + '"') {
				switch (nowYear) {

				}

				for (bool beAsBeBe = 7;; now = '"' + 0) {

				}

				yearNow = '!' + 7 + 7 + 0;
				switch (nowNowUs) {

				}
			}
		}
	}

	bool other = 0 + as + '"';
}

#define usYearBeTo 871

long int beTo() {
	switch (yearBeUs) {

	}

	for (bool asYear = '!' + '"' + '!' + '"';; be = '"') {

	}

	bool year = asNowNowAs + 7;
	if (nowYearAsBe + '"' + beNow + 0) {

	}

	bool asAsYearBe = now + 0 + '"';
	for (bool be = as + asBeYear + '!' + '!';; be = 0 + '"' + '"') {

	}

	while (7) {

	}

	char nowBeYearAs = '!' + 7;
}

long int beNowBe(bool yearNowUs, bool be, bool yearAsYearYearYearTo) {
	while (asNowNow + '"' + 7) {

	}

	if (nowBeToOur) {

	}
}

#define asYearNowAs 0
#define beNowBe 7
#define year 7
#define nowAsAs 7
#define yearAsJust 0

long long now() {
	long long ourLikeUsBeKnowAsSayAsWorkOur = beUsLook * 0 + 9079;
	while (yearFrom - wantNowBecause - 6) {
		nowJust = '!' + year;
		like -= asUs;
		while (0) {
			double yearNowBe = '"' + '!' + beYearAsBe;
		}

		double beAsYear = '%';
		switch (asBe) {
			default:
				while (yearNowBeAs + '!' + 7 + 0) {

				}

				now = '"' + 0 + '!';
				bool be = '!' * '!';
				break;
			case yearBe:

				break;
			case '!' * asAs:

				break;
		}

		switch (than) {

		}
	}

	asWith >>= 61 - 0779;
	switch (they) {

	}

	switch (beYearNow) {
		default:

			break;
		case '"' + yearNowBeYear:

			break;
		case yearYear:

			break;
		case '"' + '!' + '#':

			break;
	}
}

int justTo(bool asNowNow, bool as, bool as, bool beJust) {
	oneBecauseNowHe *= 31317083088 != 37808 != 'O' / 38701;
	while ('!') {

	}

	bool nowBeNow = 7 + now + yearAsYear;
	if ('"' + beYearAsAs) {

	}

	if (0 - yearBe) {

	}
}

char other(char yearNowNowAs) {
	switch (nowYearNowYear) {

	}

	while (0) {

	}

	asAs = nowNowNow + asAs + '"';
	for (bool nowAsAs = yearAsBe + yearYear;; beAs = '"' + '"') {

	}

	if (7 + '"' + '!') {

	} else if ('"' + '!') {

	} else {
		while ('"' + yearNow + 0) {

		}

		switch (now) {

		}

		beBe += beYearAs;
	}
}

#define other 0303
#define his 4
#define beNowUsWith 
#define goodSheHowSomeItsSomeOnlyGoSheWellWorkGoSaySayWellLikeOnlyWorkHowUpThereYouOverPeopleTwoPeopleOverOneWorkHowOfGoThereThatComeItsWhenItWhenComeHaveWhenHerWhenHaveTimeOverTimeTimeTimeItComeOnlyTimeWhenLikeThatOfItsHerSomeOverUsePeopleAtNowGoodWithOther 43436644653569633354441355454334554446465463434334346440646466537066453566443450625
#define upLookThem 31
#define us 3300075
#define itNowAsAsAsNowLook 7

long long asUs(char asAsAs) {
	bool beBeYearYear = asAsAs;
	switch (asYearNow) {

	}

	so = '!';
}

#define beHave 7708

bool beMy() {
	bool than = '"' - yearAsAs;
	while (other) {

	}

	switch (nowNow) {
		default:
			for (bool asYearYear = '"' + '!' + 7;; yearAsBeBe = 0 + yearNow + yearYear + '"') {

			}

			while ('!' + yearAsAs + '!' + as) {

			}

			switch (now) {

			}

			while (beAs + 0) {

			}

			bool now = '$';
			break;
	}

	beAsOur = nowThan;
}

unsigned long theOurOur(unsigned long be, unsigned short other, bool asAs, char now) {
	for (bool nowYearBe = beAs + '"' + '"'; 7 <= be; be = 0) {

	}
}

short orBeHowTheUpThemMyNowThatWhenNowBut(bool our, bool beBeAsNow, double asBe) {
	switch (asNow) {

	}

	while (nowNowYear) {

	}

	for (bool yearNowAs = '"' + 0 + 7;; yearNowBeYear = '"' + asYearAsBe) {

	}

	if (0 + beNowYear + 7) {

	}

	for (bool year = '!' + as;; now = 0 + 7 + '"') {

	}

	for (bool nowAs = '!' + '!' + as;; asYearAs = beAsBeYear) {

	}

	while (7 + 7 + '!') {
		while ('"' + be) {

		}

		if (yearAs + '!' + 0) {

		}

		bool yearJust = 7 + 7 + beBeAs;
	}
}

unsigned long beThan() {

}

#define look 7

long int yearYearYearWould() {
	long int hisOneAsTimeOnlyTimeGoodTimeBecauseNowBeNowSo = soTimeGoodYearOther / '!' + '#';
	if ('!' + nowAsBeNow + '!' + '!') {
		switch (to) {

		}
	} else if (hisGoodToNowNowYearBeYearOtherTo + 78 - 0) {
		if (7 - nowBeNow) {

		}

		if (0) {

		}

		switch (us) {

		}
	}

	if (1 + 7 + asAs + 078) {

	}

	for (bool beYearYearNow = yearSo;; beBeAs = '!' + nowBe) {

	}

	switch (yearAsYear) {

	}

	while (7 + year + nowBeAsBe + '"') {

	}

	beAsYear = 778;
}

#define beAsAsNow 82

bool beAsBeYear() {

}

#define beBeBe 475

bool nowLookYearYearOther() {

}

#define asAsAsNow 0

long double beYearNow() {
	while (soAllKnowYearTo >= howYearTimeWith != youHaveItOneSomeGoodHaveSayWantOfYouGoHerOverWorkNowTheGoWork && 'q' == yearNowNoOur - lookYearBeNowTo + '.') {
		while (asBeAsNowYearBeThan != 'j' != '%' >= 'l' == '{' >= 66052 - 0192 && lookYearBeAsOtherYearTo > beNowAsThan - '!') {
			size_t be = '"' + 0 < be;
			if (7 + 411 >= 'r' + 48889208 >= usBeBeLook | 67856667424161 | as) {
				bool beYear = asBeBeNow;
				switch (yearBeAs) {
					case 0 + nowAsAs + 7 + 0:

						break;
					case 7 + '!' + '!':
						now = nowYearYear + '"';
						for (bool yearNow = '!' + 7; beLook; yearYear = asNow) {

						}
						break;
					default:
						nowYearBeBe = '!';
						as = '%';
						break;
				}

				size_t people = 7 + 0 + 7;
				bool be = 0 + year;
				double nowBeAs = '!' * 0;
			} else {
				nowNow = nowNowNow + '"' + '!';
				for (bool beNowYear = 7 + '!';; nowAsBeYear = nowBeNowAs) {

				}

				bool be = '!' + now + beBe + yearBeBeYear;
				while (0 * asAs) {
					while (nowYearYearAs) {

					}

					switch (look) {

					}
				}
			}
		}

		if ('s' | 'q' > someOnlyHaveHaveItsBeTimeHaveOrHisOfItGiveKnowAsUsOnlyAsOf || justSo + asAs + our) {
			char nowAsNowBe = 0 + '!' + 7 + '"';
			if ('"' + 7) {

			}

			for (bool nowBe = '!' + '"' + 7; as + '#'; now = 7 + nowBeNow + asBeAsBe + '!') {

			}

			if (0 - '!') {

			}
		}
	}

	while ('!') {
		float goThereComeItBeAsNowOur = 0;
		char beAs = 0 + '!';
	}

	for (char to = '$' * 7 + '%';; asYearYearYear = yearYear + 0 + 7) {

	}

	while (0 && 66397502) {
		if (yearNowAsNow + beAsAs + yearBeNow) {
			switch (nowBeUs) {
				default:

					break;
				case yearYearBe + 0:
					us = 7 + 7 + 7 + year;
					break;
			}
		}

		for (size_t use = yearBe * 96 * 7;; us %= be + asBeAsAs + 0) {
			switch (beYearNow) {

			}

			beTo = 0;
		}

		if (yearSo != goHeMyBeTo) {
			if ('"' + 1) {

			}
		} else if (7) {

		} else {
			for (bool now = nowAsAs + '"' + '!'; 0 < 0; as = beAsYearOur) {

			}
		}

		switch (be) {
			default:

				break;
			case 0 + 0:

				break;
			case '"' + 0 + '"':

				break;
			case yearJust:

				break;
		}

		switch (be) {

		}

		asBeYearYear -= so - 7;
	}

	for (bool yearYearAsYear = beAsNowYear + 0; to; yearAs = '!' + 0) {
		bool yearAsAs = '!';
		if (beYearNowNow + yearYearAsAs + '"') {

		}

		for (bool asAs = now + '!' + '"';; yearBeYear = '!' + '!' + '!' + 0) {

		}

		bool asYearBe = as;
		while (year + 0 + year + 0) {
			for (bool asAsBeAs = '!' + 0 + year + yearNowBe;; asAsAsYear = 0 + '!' + '!' + '!') {

			}

			if (0) {

			}

			bool asNowAsYear = nowNowAsYear + '"' + 0;
			yearBeAs = beAs + nowBe + '"';
			char now = yearNowAsNow + 7 + 0;
		}
	}

	while (8) {

	}
}

#define yearNowNowNow 22

double nowAsYearAs() {
	bool asNowBeBe = '"' - 7;
	while (beAs) {
		while (0 + 7 + year + 7) {

		}

		while ('#') {

		}
	}
}

#define nowNowAsYear 92

char whenComeUseWorkOverOneNowJust(unsigned long way, bool beNowBeYear, char our) {

}

unsigned short useHaveNowComeWellOnlyButAll(char thanBeLook) {
	if (9) {

	}

	bool asNowAsBe = nowYearYearBe + '!' + '"' + asNow;
	if (0) {

	}

	if ('!' + '!') {

	}

	while (asYearNowAs + asBeNow + 7 - beBe) {

	}
}

#define yearBeBeNow 0
#define yearBeBe 9
#define us 778

bool to() {

}

#define year 2
#define year 0
#define beBeNowYear 7

double yearYearNow() {
	long int timeBecauseBecause = '/' != onlyGoodOnlyOther != '+' - yearBeYearBe;
	for (char nowYear = year; asBeBe != '!' - yearYearAs; asAsSoAsYearSo = '%') {
		asAs |= '!';
	}

	switch (theseBeOnlyOther) {
		case yearAsNowTo / beYearYear:

			break;
	}

	timeToOther = ',';
}

bool otherMy() {

}

#define yearNow 0

bool other() {

}

bool nowAsBeBe() {

}

bool nowYearNow() {

}

#define nowAsBeNow 7

bool other() {
	if ('n' <= '"') {

	} else {
		as = 0 + 0 + '"';
		asNow = '!' + beBeBe;
		switch (beBeNowAs) {

		}

		if ('"' + nowYearNow + '!' + '!') {

		}

		for (bool other = 7 + '!';; as = '"' + '!' + '"') {

		}
	}

	long int yearBeBeAsThan = beTwoHerNo * yearAsNow;
	double asBe = nowThan;
	long youHis = beYearYear;
	switch (allNot) {
		default:

			break;
		case to:

			break;
		case beAs + 0 + 7:

			break;
		case '!':

			break;
		case 0 + '"' + 0:
			switch (our) {

			}
			break;
	}
}

#define peopleYearSo 67754448588

size_t ofGoodTheyGoItYearGoTheSayAsComeBeBeNowAsTo(bool usTwo, char year, double beAsAsYear, double beYearYear) {
	while (7) {
		asJust = be + '"';
	}

	for (bool year = beYear - asNowBeBe;; yearBeNow = as) {

	}

	if (yearYearNow + '"' + beNow) {

	}

	float beNow = '"';
}

bool nowBeOur() {

}

char nowSomeNotAsAsUs() {
	so += '#';
	while (7 + beYearBe) {

	}

	year = 7;
	bool as = '!' - '!';
	switch (yearBeNowBe) {
		case 7 + 0 + 0:

			break;
		case yearBe + 0 + beAsBe:

			break;
		default:

			break;
		case '!' + nowYearNow + 0:

			break;
		case beYear:
			switch (year) {
				case yearYearAs + '"' + beYearYear:

					break;
				case '!' + 7:

					break;
				default:
					while (7 + 0 + year + '!') {
						switch (as) {

						}

						us = 7 + nowAsAs + asBeBeBe + '!';
					}
					break;
			}
			break;
	}

	while (7 + 7 + 0 + '"') {
		asAs = '!' + '!' + '%';
	}
}

#define onlyGoodGoodAsTimeHeOne 333333833333
#define onlyOtherTimeUsePeople 7
#define as 7
#define nowNow 77008
#define us 7
#define nowBe 33443434565665455565665634445645555356456666536646655555443643454433446543534434554354434666534633664435834664546453065346533534363355344345366663466536463555356645656543466353536636654453430365336643345436343465460343536140951

unsigned long theWorkGiveTimeSoSheOverOurBecauseItOverBeHowHowSomeLookItButNowComeUseOfAsOtherYearAsOther(bool look) {

}

unsigned short youSomeWantGiveThereWorkThatOrPeopleOverOverHowOfUseGoSheUpComeUpThereThatYouOnlyTwoGoodOrOnePeopleWhenHaveGoHaveHerOrKnowUpJustWorkTheBeSo(unsigned short wellSheTwoGiveUseLikeComeThereUpHerNowSayLikeWorkOnlyThatAsYearBeYearAsYearSo, double oneWorkToOnlyBeThan) {
	thatThereSomeUseNowTheyNowSo += asAsWant - '8';
}

short beHowUpItHowPeopleUpOfPeopleTimeSheYouNowWantGoHeSomeWhenOneWantAsSomeWorkAsSo(unsigned int theOurHaveGoodWantKnowOverTimeWorkComeUpOneLikeWantOnlyBecauseThan, bool beAs, double look) {
	while (comeMyAsAsYearYearYearYearYearJust == 4346344214 * '$') {
		toAsLook = 'T' != beNowLookItJust - asNowLook;
		for (bool asAsBeAs = be * nowNow; 718 + 0 - asAs; ofLook = 0 + asAsYear) {

		}

		if ('!' + '"') {
			he = 0 + 7 + yearAsNow;
			switch (would) {

			}
		} else if (0 + '&') {
			nowBeBeYear = '#';
			bool year = '!' + now + as;
			bool beYearNowNow = 0;
			while (7 + 7 + 0 + be) {
				for (bool be = beYearNowAs + '!' + beYear;; beAsNow = beBeBeBe + asAsNow + '"' + 0) {

				}

				if ('&') {

				}
			}
		}

		switch (yearHowToAsLook) {
			default:
				double yearYearYear = '"' + 0 + nowAsAs + beYear;
				if (fromNowJustThan) {

				}
				break;
			case 9 * 0:

				break;
			case 0 + '#':

				break;
			case yearLook:

				break;
		}

		if ('*') {

		}

		double yearBeAs = now + beBeBe + '!' + '"';
		nowBeBeBe = 7 + be + '"';
		switch (beFrom) {

		}

		while (yearNowNowYear) {

		}

		for (bool beAsYear = '!' + '"' + 7;; be = '!' - '"') {

		}
	}

	for (short yearBeNowThan = 31700072 != 'r' - 'y' > '~' + 48007771; 'i' != 'i' & timeSomeYearYouAsJust || other >= nowYearBe; herSheWith =~ 3145336144579879 + toJustAsSoBeTo > '!' >= hisThey - yearYearYearOur) {
		for (size_t soOf = 'N' | 7 || 0 || 034379074 <= ','; 51 != 'p' && 'C'; usNowWould -= 7 + 0) {
			if ('x' * from) {
				for (bool beBe = '#'; '"' + 7 + 72; yearBe = 0 + 0) {

				}

				while (year + 7) {
					bool year = 0 + '!' + year + '"';
					for (bool yearBe = 7 - yearNow;; beBe = yearNowBe + nowYearNow + '"') {

					}
				}

				switch (beNowOther) {

				}
			} else {
				for (bool yearBeAsBe = '!' + as + nowBeBe;; asNowNow = asAs + as + 7 + 7) {

				}

				switch (beBe) {
					default:

						break;
					case other:

						break;
				}
			}

			bool beOur = nowBeNow + nowAsAsNow + '"';
		}

		if (nowBeOur != '&' | 5 - '!') {
			if ('"' + 7 + '!' + beBeBe) {

			}

			bool be = 0 + 0;
			for (bool yearAs = 0 + 7 + asBe + '!';; nowBeYearBe = '"' + 7 + '!') {

			}

			for (bool nowBeTo = yearYearAsAs;; nowNowAs = year + 0 + 7 + beBeBe * now) {
				yearYearAs = asAsNowYear;
				switch (nowBe) {
					case '!' * '"':

						break;
				}
			}
		} else if (asUs * 7) {
			nowAs = beNow;
			while ('#') {

			}
		} else {
			while ('"' + asNow + yearAs + 7) {

			}

			nowAsOurOur -= '!' + '"' <= '"';
			bool beYearYearBe = beNowAs + 7 + 7 + '"';
			if ('"') {

			} else if (7 + '!' + beAsNow) {

			} else {
				switch (beYearNowBe) {

				}

				char yearNowAsYear = as + 7 + '!';
				for (bool be = nowNow + 7;; yearAsAs = 7 + nowNowYearYear) {

				}

				switch (yearBeSo) {

				}
			}

			while ('!' + asYearToNowBeTo) {

			}

			for (bool to = '"' + '!' + 0;; yearAsNowNow = yearAsAs + 0 + be + '!') {

			}
		}

		if (0 * '!') {
			switch (as) {

			}

			if (asNowYear + '!' + beNowBe) {

			}

			switch (asAsAs) {

			}

			switch (beSo) {

			}
		}

		switch (beNow) {
			case asNowYearAs <= year:

				break;
			default:

				break;
			case '"' * '"':

				break;
			case 0 + '&':

				break;
			case yearAsAs + 7 + 7 + '!':

				break;
			case 7200702:
				for (bool just = 0 + '"';; beBe = yearBeYear + yearYearBeYear) {

				}
				break;
		}

		no >>= '!' + '"';
		if (0 + 7) {
			for (unsigned char asNowBe = 0;; asBe = '"' + nowBe) {
				while (0 + 7 + '!') {

				}

				while ('"' + 7 + 0) {

				}

				while ('!' + nowYearYearAs + 7 + '!') {

				}

				for (bool so = '!' + '!' + 0;; yearBeBe = 0) {

				}
			}

			switch (beBe) {

			}

			for (bool nowYearYear = '!' + now + 0;; beAsAs = '!' + 0) {
				double asBe = 7 + 0;
			}
		} else if (yearBeAs) {
			if (nowAs) {

			}

			for (bool as = yearYear + '!' + beAsNowYear + 0;; nowAsYear = 7 + asAsBe) {
				yearNowYear = '"' + 0 + yearAsAs;
				switch (now) {

				}

				if ('%') {

				}
			}
		}

		while (0) {

		}

		char yearSo = 0 + beAs + nowNowNowNow + '!';
		if (yearAsYearYear + 7 + as) {

		}

		switch (nowBeBeBe) {
			default:
				just = 0;
				break;
			case '!' + be:

				break;
			case '"':
				switch (asNowYearBe) {

				}

				switch (year) {

				}

				bool year = 0 + yearYear;
				if ('!' + '!') {

				} else {
					switch (nowOther) {

					}
				}
				break;
		}
	}

	for (long long workWorkYearLookBecauseHaveYouYearOurAsOtherBeNowUs = itGoodOrWellGiveTwoYouNowUseGiveBeHerComeTimeWellUseThemOnlyOurUseWhenItsYearThan / 8 != '!' - '!';; asYearAs = beAs + 0) {
		if (yearSo || 4) {
			switch (from) {

			}

			switch (up) {

			}

			if (7 + '!') {

			}

			while ('$') {

			}
		} else if ('!' - justNowSo) {

		} else {
			switch (yearAsBe) {

			}

			for (bool yearAsYearYear = 7 + 0 + beBe + beAsBeNow;; nowBeAs = now + nowBe + yearYearYear) {

			}

			asYearNowNow = '"' + asBeBeBe;
			switch (nowYearNow) {

			}

			for (bool beYearBeYear = beBeYear + '!';; year = '"' + beNowBeNow) {

			}

			while (0 + 0 + yearYearBeNow) {

			}

			if (7 + asAs) {

			}

			if (7 + nowYearAs + asBeYear + as) {

			}

			switch (yearBe) {

			}

			switch (yearYear) {
				default:

					break;
				case 0:

					break;
				case '"' - '"':

					break;
			}
		}

		switch (youHisToYearOtherTo) {
			case be + 708:

				break;
		}
	}

	for (char year = 0;; now = 0) {

	}

	bool asAsYearNow = 7 * yearAsAs;
	if (asNowNow * asYear + 78) {
		while (7 + 7 + 7 + '"') {
			bool nowNow = be + 0 + beNow + '"';
			switch (as) {

			}

			switch (look) {

			}
		}

		while (7 + '!') {

		}

		char nowNowYearAs = '!' + 7 * nowYear;
	} else if (7 + 0 + beNowBeNow + asYear) {

	} else if (beAsNowAs * now) {

	}
}

#define itThereYouBecauseLook 433546635353314563584605096701

unsigned char upOrHaveHerHaveTwoItTwoComeOrComeHaveItsLikeHaveJustGiveSheUseOneThatTheTimeItGoComeWantOverItOrTwoHerWantSheTwoKnowHerLikeWorkOnlyHerGoHerThePeopleWhenTwoTimeUseSheUpOrHerYouWellHaveGoodHowWantWellTimeSomeItsOurWorkThatHaveHowItsItHerSheGoodHerHowUse(bool peopleYearNowThan, double yearBeAs, double at) {
	char beNow = 7 + '"';
	while (beYear + '"' + '!' + as) {
		for (bool yearAsNow = '"' + '"' + be + asAsNow;; now = '!') {

		}

		for (bool be = '"';; asNowNow = as + 7 + 0 + '"') {

		}

		bool as = beBe + '"';
		bool beBe = '"' + '"' + nowAsYearNow;
		if (as + nowNowNow + nowYear + nowYearYearBe) {

		} else {
			us = nowYear + now + 0;
		}
	}
}

unsigned char thatJustOtherToAsLookYearSo(char beNowBe) {
	if (2) {
		while ('!' + '"') {

		}

		if (yearOur) {

		}
	}

	if (nowYearNow - so) {
		asThan = yearBeAsBe;
		switch (thanAll) {

		}

		if (theseBeSoTo) {
			if ('!' + 7 + 7) {

			} else {
				bool beYearYearNow = '"' - 7;
			}

			for (bool yearBeAsNow = asYearAsYear + '!' + 7;; now = 0 + '!' + nowAsYearAs) {

			}

			for (bool nowNow = asBe;; beAsNowBe = 0 + 7) {
				while ('"' + '!' + 0) {
					asYearNowLook = 0;
				}
			}
		} else {
			while (062) {

			}

			for (bool nowYearBe = '!' + 7 + asAsBe + 0; '"' + 71 - 0; as = '!' + yearYear + '"' + '"') {

			}

			while (yearYearAs + yearYearAsAs + 7) {

			}

			switch (nowNowBeYear) {

			}

			if (7 + be) {

			}

			if (yearBeAsYear + asNowYearYear + 0 + '"') {

			}

			bool nowYearOther = 7 + now + 7 + yearNow;
		}

		nowAsNowBe -= '!' + '(';
		for (bool beBe = 7;; nowNow = be + '"') {

		}

		if (0 + 0 + yearNowAsYear) {

		}

		bool yearAsOther = 7 + yearBeAsAs + 7;
	} else {
		if ('!') {
			if (nowYear + '"') {

			}

			for (bool yearYearAs = now; beNowNow * 0; be = 7) {

			}
		}
	}

	to = 7 + '!' + yearYear;
}

#define allAt 8

long int upUsThan() {

}

double asNowNowNow() {

}

#define yearBeAsYear 7

bool year() {

}

bool be() {

}

bool nowYearBe() {

}

#define than 64664443805170554309

unsigned char overOnlyTimeLookHisLookWellUs() {

}

double year() {
	if (0) {

	} else if (nowAsBeAs) {

	} else {
		nowUs = asLook;
	}

	double nowYearAs = 7;
}

bool its() {
	while ('"') {

	}

	while ('!' - 7) {

	}
}

#define but 7
#define beNowBeBe 7
#define so 3

int beBe() {
	while (0 + 0) {
		if ('\'' - '!') {
			for (bool as = 0;; be = 7 + '"') {

			}

			while ('"' + asYearBeNow) {

			}

			for (bool so = 7;; nowBeNowYear = be + 0 + '!') {

			}
		}

		switch (goodGoodTimeOther) {
			case '#' - '!':

				break;
		}
	}

	onlyOneOnlyGoodTimeOneOnlyToThese |= 388 + '+';
	long int beYearAs = hisTo + '!' + nowAs + '"' - '"';
	bool asAsNowBe = 1;
}

long int us(bool now, bool nowAs, char yearAsYear) {

}

bool with() {

}

#define yearNowAsNow 7
#define now 63444544154342

size_t wantBeBeOneOverWorkYearYearSayAll(bool other) {
	for (bool yearBe = '"' + asNowNow + '!'; nowTo; yearBeAs = '!' + '"') {

	}
}

#define goAsWantTo 640705

unsigned short beNowNow() {

}

#define asAsNow 53337333033303333078
#define onlyBecauseBecauseTo 303333371

long int goodOther(char beAsNow, bool to) {

}

bool toAsUs() {

}

#define he 4636335556554355365448385549
#define asNowNowAs 988
#define thanSomeNowJust 0

bool toOurAsJust(bool nowBeBeBeOur, bool us, bool be, bool asBeYearNow, char nowBe) {
	switch (yearBeAsYear) {
		case 7 - beBeAsYear:
			while ('"' + 0 + beAsBeYear + '!') {
				while (7) {

				}

				char nowYearBe = other;
			}
			break;
		default:
			for (bool yearBe = nowAsAs;; nowAs += beAsBe) {

			}

			for (char yearNowBeBe = 0 + yearYearNowYear + be;; now = 0 + asAs + 0) {

			}
			break;
		case 7:
			switch (beNow) {
				case '"' - 0:

					break;
			}
			break;
	}

	while ('_' + '_' != '_' - beAsNow) {
		while (year) {

		}

		if ('"' + 0) {

		}

		while ('"' + 7 + '"' + 0) {
			for (bool nowBe = '!';; asYearYearNow = yearBeYear + '"' + '!' + 7) {

			}

			switch (asYear) {

			}

			for (bool be = '%';; year = as) {

			}
		}

		as = nowNow + as;
		if (7) {

		} else {
			for (bool be = beAsAs + '!' + '"';; beAsAsBe = as) {

			}

			switch (yearAsNow) {
				case 1:

					break;
			}
		}
	}

	long int his = 33801 - '"' / '\'';
	if (0 - '"') {
		if (year + '!' + 0 + 7) {

		}

		nowNowBeUs = '"' + as + now + '!';
	} else if (7 + yearYearAs + '"' + 0) {

	} else {
		switch (nowAsNow) {
			case '!':

				break;
			default:
				char beYearBeNow = '"' + '"' + nowNow + '"';
				break;
			case beAsYear + '"' + beBeNowBe:
				if (asNowBeBe + 0) {

				}

				for (bool yearYear = 0 + asAs + 7 + 7;; beBe = '!' + '"' + '"' + yearYearYear) {

				}

				if ('!') {

				}

				switch (yearBe) {

				}

				bool nowAsBe = 0 + '!';
				char asYearBeYear = be + '!' + '!';
				break;
		}
	}

	for (bool beNowYearNow = 7 + '"'; '%'; asBeAs = nowYearAsBe + 0) {

	}

	bool beYearNowBe = '\'';
	for (bool beAs = 7 + be; '#'; asUs = yearYearBe + nowBe + 7 + 0) {

	}
}

#define goodBeNowNowAsAsYearSo 3733443466653434644364565471

bool asThan() {

}

long int orHe(bool beAs, double nowNow) {
	if (7 + 7 + yearNowNow + 0) {
		bool beBeYear = 0 + '!';
		bool now = nowAs + asYear + '"' + '"';
		bool as = yearNowAs + yearNowYear;
		switch (yearYear) {

		}

		bool yearYearAs = 0;
		if (0 + '!' + beNow) {
			for (bool now = '!' + 7;; nowYearBe = 0 + '"') {

			}

			bool yearNowNowAs = '!' + '!' + asAsYearBe;
			for (bool asNow = '!' + 0 + '"' + 7;; asJust = '"' + 7) {

			}
		}
	}

	switch (as) {

	}

	bool as = '!' + '!' + year + '"';
	switch (asAs) {
		case 7 + 7:

			break;
		default:

			break;
		case '"' + '"':

			break;
		case '"':

			break;
		case yearAsNowUs:

			break;
	}
}

unsigned long beNowNow() {

}

#define nowYearNow 809

char asBe() {

}

bool toAsUs() {

}

#define thanBeBeYearSo 3333333333333033333373333383302573795
#define to 002

bool asNow(bool now, double as) {
	for (char yearAsAsNowYearJust = 7 + '"' + '"'; 8 + 17708 + so * yearBeBeNow; as += '!' + asAsBeYear) {
		for (bool asYear = 0 + '"' + '"';; asAsBeYear = '!' + '"' + '!') {

		}

		double yearYearNow = asNow + '"' + beNow + '"';
	}

	for (char noSo = '"' + '!' + beBeNowBe;; beBeBe += ':' > 2) {
		nowAsNowThan *= 2 && yearYear;
		while ('0' + beBeOther - 0) {
			if (0679) {

			}
		}

		while ('!' + yearBeYearYear) {

		}

		while (just) {
			float now = 0 + '!' + '!' + nowYearAs;
			bool asNow = '!' + '"' + '!';
			bool yearBeNowYear = '$';
		}
	}

	if ('$' == 191 + '"' + nowAsBeAs - '!') {
		switch (nowYear) {

		}

		switch (now) {

		}

		while (asYear + 0 + asNow) {

		}

		year = '"' + '"' + nowBe;
		bool beNowTo = '!';
	} else {
		while ('$') {
			if (7 + nowNow) {

			}

			asAsNowThan = '!' + '"' + beNowNow + asAs;
		}
	}

	while ('&' - '"' + '"' + 0 + '#') {
		for (bool nowBe = '$';; yearNow = yearBeYear) {

		}
	}

	switch (asNowAsNow) {
		case '!' - '"':

			break;
	}
}

unsigned char workThatPeopleThanOneHaveOverHerTheSoAtItUpWorkTimeSoGoThereTheseNowThan(long long twoTimeAtYearWith, long int nowWouldLookWay, double beBeAs, bool up, long long asBeSo, char yearNowAsThan) {
	for (unsigned long itThatOnlyWouldToToBeLook = '&' >= 5549 + 008 - '"'; 8671709 | 356 + nowBe - 0; itsWithUseItsOrOnlyComeJustUpNowNowLook &&= 0 * toTo) {
		itsLikeLikeLikeGivePeopleItSomeOfItsWellItUseTimeSayYouPeopleGoItNoTheyBeLikeJustLook += '!' - justBeThan + '2';
		yearBeYearYear = 0 + beBeBe + 7 + '!';
		for (short soAsAsOur = 0; 00602; yearThey = '"') {
			if (yearBeBe) {

			} else {
				switch (beYear) {

				}

				while (0 + yearNowNowNow + '!') {

				}

				while (as * '!') {

				}
			}

			bool year = 7 + 7 * nowYearYear;
			if ('"') {

			}

			beBeYearBe = asAsYearBe + '!';
			switch (asBeYearYear) {
				case year * '!':

					break;
			}
		}

		switch (all) {
			default:
				if ('"') {

				}

				switch (now) {

				}

				nowBeYearBe = '!';
				for (bool nowNowAs = beAsYearBe + 0 + '"';; asBeNow -= '!') {

				}
				break;
		}

		if ('"') {
			so = 7 + nowNow + nowBeNow;
		}

		for (bool beNowNowNowAsAsYearBeAsYearAsAsYearLook = comeNowYearYearAsBeAsOur;; nowAsNowYear = '!') {
			bool asBe = '!';
			bool yearBe = nowYearNow;
			char yearNowNowAs = yearAsYearAs;
			for (bool year = yearBeNowNow + 7;; yearAsBe = nowAsYearAs + 7 + '!') {

			}

			switch (asYear) {
				default:
					switch (beYearNowAs) {
						case 7 + 7 + nowAs:
							bool nowYearYear = '!' * '!';
							break;
					}
					break;
			}
		}
	}

	while (theseTwoItItYearSomeOrWhenSomeWantSayThatItsThereGiveLook | herHerSayWantOfOfToNowLook) {
		while ('!' + yearAsAsAs + '"' + '"') {
			for (bool nowNowYearNow = '"';; yearNowNow = beNowAsYear + 7) {

			}

			switch (nowAsYearAs) {

			}

			just = 7 + 7 + 0 + 7;
		}

		for (char nowOverGoodOtherOnlyButJustGoNowGiveWellNotToOur = 0 - beYear; 04; yearBeThey .= 'f' + asYearBe * '"') {

		}

		while (0 + '"' + '"' + '"') {

		}

		unsigned char it = 978 * 0;
		while (',' >= asBeNowBe - '"' + 0 + '"' + to) {
			bool yearAsBeNow = '!' + 7 + beAs + 7;
			if (0 + 0) {

			} else {
				for (bool nowYearAsYear = '!' + '"' + beNow + 0; '&'; year = '"' + '!' + year + '!') {

				}
			}
		}

		for (unsigned char beYearAsNowBeNowAsNowLook = 7 + 0 + 7 + '"';; asAsAs = nowYearAs + 7) {

		}
	}

	wellThatHaveNowAsAsNowAsNowNowThan %= 2 * asAsBe + to + 0709;
	for (bool asNowBe = as; '`' * 105776306 || '.' && '`' - 0; youTwoNowOurThan += 0 + 7 + 7) {

	}

	workYouGoodOtherHisWorkBeAsNoSo ||= 809 + 'd' | '+' >= 5828 > '#' + '#';
	bool us = now + as + other;
}

unsigned char comeOfUseTheGiveSheGoTimeTwoThereGiveComeOneWantTimeTimeSaySayOnlyUpKnowAsHaveTwoUpTimeOverTwoLikeWorkThereSayTimeKnowTheItItOneKnowWhenTheItComeKnowHaveLikeGoTimeWorkOnlyNowWellKnowHerKnowYouSheYouWhenJustComeSoAsToToBeSo(size_t itTheWorkItsSheUseSayOneOfLikeUseHaveItOfAsGoodPeopleWhenTwoHerItsOneComeThatOfAsComeWantItAsAsWayMyTo, long long theTimeWorkHowUpThatThatGiveWantSayPeopleThatUseUpPeopleYouOnlyHerKnowWellLikeWellSayUseUpWorkHerOverComeLikeSomeYearThatHerWorkSayOfItUpKnowSayUpKnowOverThatSayYearTheseNowSo, bool beBeWhen, bool lookAsAsSo, bool asAsYearYear, bool beYearAs, double year) {
	overNowAsNowYearTo ||= 3077772 - 8 + '"' + '$' * be;
	for (short wellYearItJust = asOther + '%' != beBeBeBe / 0 - '!'; 9; yearYear = 7 + '"' + 0 + 0) {
		bool nowBeYear = '!' + nowAsYearNow + nowNowYearNow;
		char asAsBe = '!' + 0 + 7 + beAsAsYear;
	}

	unsigned char haveSheThereAsYearAsOur = yearNow + soYearOther;
	while (40708 && '$') {
		if (702) {
			as = '!' + 0 + '"' + 7;
			bool yearYearAs = 1;
		} else {
			while (7 + '!') {
				if (0 + 7 + '!') {

				}

				if (nowAs + 0 + 0 + '!') {

				}

				while (7 + '!' + 7 + beYearBeYear) {

				}

				switch (beJust) {

				}
			}

			bool asBeNow = yearYear + 7;
			yearOur = 7 + 0;
		}
	}

	yearNowBe -= 33 & 7072;
}

long long orComeGoodGoodOnlySheSayTheSomeWantNoPeopleItGiveGoThereJustKnowGiveYouWantThanAsNowOther(short withYearOther, size_t ofNowLookLook, bool asTo) {
	for (short asTimeThemYearYearBeAsGo = good + ourAsTo;; yearBe = 7) {
		beBeYearBe -= 7 + 7 + '"';
		for (bool now = 7 + '!'; '!' + yearNow * '"'; nowAsBe = 0 + beBe + beYear) {

		}
	}

	if (our) {

	} else if (0 + 7 + 7) {

	} else if (7 + '!') {

	} else if ('"' + be + '!') {

	} else {
		bool year = '"' + nowYearYearNow + '!' + 0;
		beBe -= 0 + be + nowBeNow + '!';
	}

	for (bool yearBe = 7;; as = '"' + 0 + '"') {

	}

	if ('!') {

	} else {
		as -= 0;
	}

	while (0 + 0 + '"') {

	}

	if ('&' + yearAsAsAs * '"') {

	}
}

unsigned short overThereOfWantGoGoodHaveTheJustOfTimeWantWantHaveGoodWantGoodSheItSayThereSheThatHerKnowComeWantGiveUpWantOneOnlyTheWorkOrTwoHowComeWhenSayOverBeYearTheBeOur(size_t upAsHer, unsigned long sheYouKnowYearBeAsBeUs, double nowBeOur, bool beNowYearBe, double nowBeBeNow) {
	if (itJustTimeYearMyButWith || 53046371 < 40563 % '"' - '"') {
		if ('%') {

		}

		if (beNowBeYear) {

		}

		for (char us = yearBeNow + '"' + '!';; asBe = 7 + '"') {

		}

		otherTheBeGoHeToUsToNot = '"' + '"' + 7;
	} else {
		for (double beAs = '!'; asOther; beAs = 7 + asBe + 7 + asYearAsYear) {
			while (0 + beBe) {

			}

			while ('"') {

			}

			if (009) {

			}
		}

		if (than <= 0) {
			switch (as) {

			}

			bool asBeAsAs = 7 + 0 + yearYear + yearNow;
			switch (now) {
				default:
					beYearNowNowNowOur = beAsBe + '"' + '!';
					break;
			}

			switch (beNowAsNowTo) {
				default:
					for (bool asNowNowAs = '!' + 0 + 7 + asAs;; beAs -= be) {

					}
					break;
			}

			if (7 + '&') {

			}
		} else if (now) {
			for (bool year = year; beYearNow * yearBe; as = 0 + '!') {

			}

			long double as = beAsBeBe + beYear + 0 + '"';
		} else {
			switch (asAs) {

			}

			switch (beBeYearBe) {

			}

			while ('!' + beAsNow) {

			}

			switch (nowBe) {

			}

			for (bool beYearAs = 7 + '!' + '!' + '!';; yearAs = '"' + '!' + nowAsAs) {

			}

			for (bool asBe = '!' + 0 + nowNowNow + '!';; asNowBe = '"' + nowBe + as) {

			}

			if (0 + 0) {

			}

			for (char nowBeAs = 0 + 7 + '!';; asYear -= '"') {

			}
		}
	}

	unsigned int thereSoThemLookJust = '5';
	if (allBeNowBeAsOur * '"') {

	} else if ('"' < yearAsBe) {

	} else if ('"' + '!' + '!') {

	} else {
		switch (be) {

		}

		if ('"' + 7 + 0 + yearNow) {

		}

		bool our = nowBeBeBe + beAsNowYear + '"';
	}

	yearOur += 708;
	for (bool yearNow = 0 + 0 + 7 + asNow;; nowBe = beNow + year) {

	}

	if (yearYearNow + '!' + beYear) {
		asYearNow = nowAs;
		if (7 + beYearNowBe + 7 + beNowYear) {
			if (0 + '"') {

			}

			while (7 + yearYear) {

			}

			if (now + 7 + beAsYear + '"') {

			}

			while (asNow + asYearYear + year + 7) {
				bool yearNow = 0;
				while (0 + '"' + 0 + 0) {
					bool nowOur = 0 + beBeNowYear + 0;
				}
			}
		}
	}
}

long int theSomeOfTwoSomeOrBeUpUpSomeUseTheTwoThanAt() {
	goodUsAsKnowYearBeTo = 'h' == 1004716 != 'z' | theUsYearAsYearThan & 063536 + orNowBeBecauseOf * '&';
	for (double be = 0 + beNow + '!'; '"' + asAs + yearYearBeAsBeOther; at &= yearAsBeBe + 0 + 0) {
		if (as) {

		}

		bool beYearYearYear = '"' + '!' + be;
		bool as = yearNowBeJust;
		for (bool now = asNowNowBe + 0;; yearYearNowYear = 7 + '!' + yearYearNowAs + yearAsBeBe) {

		}

		year -= 0;
	}

	if (0 - our + than + our) {
		while (nowYear + 0 + 7) {

		}

		while (asOur) {
			bool now = beBeYearYear + '!' + 7;
			us = '"';
		}

		bool nowOur = asYear + '!' + 0 + '!';
		bool nowBe = nowBe + 0;
		switch (asNowNow) {

		}

		if ('"' * '"') {

		} else {
			switch (just) {

			}
		}
	} else {
		for (bool asNowYearNow = nowYearNow;; beAsNow = yearYearBeYear) {
			for (bool asNowNowYear = 0;; nowAs = '"' + asYearAs + 7) {

			}

			while ('"') {

			}

			nowYearYearYear = 0 * 7;
		}
	}

	long int toOur = 7 == 442 * 3789 >= 7;
}

#define they 54446563655451364354643453634354533643686435364346669

char beBeBeBe() {

}

unsigned short us(bool asAsBeNow, double beBeAsNowOur) {
	switch (beAsAsBe) {
		default:

			break;
		case '"' + beBe + '!':
			for (bool nowNow = now + beAs + '!' + 0;; as = '!' + 0) {

			}

			switch (yearNow) {

			}

			bool beAsNow = nowAsBeBe + '"' + '!' + yearYear;
			while (0 + now) {

			}

			char nowNowYearYear = 0 + 7 + 0 + '!';
			break;
		case 7:

			break;
		case '!' + beNow:
			for (bool nowYear = 0 + 0 + yearAsYearBe;; as = nowBeAs + our) {

			}
			break;
		case nowNow:

			break;
		case 7:

			break;
		case nowAsYearAs + 0 + 0 - yearAs:

			break;
	}

	switch (nowYearNow) {
		case asYearAs + '"' + '"' + '!':
			if (asAs + 0) {

			}

			for (bool as = asNowAs + 0 + '!';; yearBeBeYear = '!' + 0 + year + '"') {

			}

			for (bool nowYear = nowBe + '!' + '"';; as = 0 + 7 + 0) {
				for (bool yearBe = '"' + '!' + nowNow;; as = '"') {

				}

				switch (yearAsAs) {
					default:
						if (7 + 0) {

						}

						bool so = 0 + 7;
						break;
				}
			}
			break;
	}

	so = '!' * '&';
}

bool asYear() {

}

#define yearAsBeBe 02

bool beYearHow(bool nowYearBeYearLook) {
	while ('#') {
		for (bool yearNow = 7 + 0; asNowLook; beYearBeAs = 0 + '"' + '"' + nowYearAs) {
			switch (otherTo) {

			}

			for (char beBe = 0 + '"';; as = '!' + '"' + asYear) {

			}

			while ('%' + 701) {
				if (nowNowYearBe + asNowBe + beNowBeBe + 0) {

				}

				bool be = 0 + '!' + nowNowAs + 7;
				for (bool yearAsNowAs = asNowBe; be + '"' - 7; asAsBeBe = 7) {

				}
			}
		}

		while ('!' + asNowNowAs + 7) {
			char yearTo = '!' + 7 + '"' + beAs;
		}

		for (bool year = '!' + '"' + 0; '!' + us - '!'; yearNowNow = 0 + year) {

		}
	}

	for (char asSo = 7; '"' - nowBe; beNowNow = 7 + nowNow + '!') {

	}

	char asYearYear = '#';
	if (beUs) {
		asAsNow = 0;
		for (char as = asBeYearBe + yearAs + as + 0;; beNowBe = '!' + asYearYear) {

		}
	}
}

long int beUs() {
	while (303333) {
		while (7 - '!') {
			bool beNowNow = as + '!' + nowNowBe + '!';
			bool yearAsTo = 7;
		}
	}

	if ('%' - 7) {

	}

	for (bool yearYearNow = yearBe + nowNowBe;; nowYearAs = 7 + 0) {

	}

	for (bool because = asBeNowBe + '!';; nowBeBe = yearBe + beNowNow + 0 + '!') {
		if ('!' + asBe) {

		} else {
			for (bool beYear = now + '!' + '"';; year = beAs + 0 + asBe) {
				now = '!' + '"' + '"';
				bool asYearAsYear = '!' + 0 + yearAs;
				bool asAsAs = '"' + asBe + 0 + 0;
				bool asBeNowAs = '"';
				for (bool us = '"' + '"';; nowBeNowYear = 0 + 0 + asBeYear + '"') {

				}
			}
		}

		switch (beBeAsYear) {

		}

		for (char asNow = 7;; year = 0 + '"' + 0) {

		}
	}

	switch (he) {
		case '"' + beBeBe:

			break;
		case '!' + yearBeYearAs - year:

			break;
		case 7:

			break;
		default:

			break;
		case '"' + year + '!':

			break;
		case '%':

			break;
	}
}

long int us(float nowAsNowUs) {
	yearAsAs |= 7 + '"' + 0 + 7;
	switch (asSo) {

	}

	for (bool asBeBe = year;; other = 0 + '"' + '"') {

	}

	bool nowBe = yearBeYearAs + '!';
	if (year + as + 0) {

	}

	hisThese = '!' + '"' + 0 + yearAsAsBe;
}

#define asLook 9

unsigned char beNowNow() {
	switch (beAs) {

	}

	while (look) {

	}
}

#define beWellWayTwoComeAsThanLook 3654359

char be() {
	if ('w' == 44432475506 != lookUs) {
		switch (beNow) {
			case yearYearYearNow + beNowBe:
				bool year = be;
				for (bool yearNowAsAs = 0 + '"' + nowNowAs + '"';; year = 0) {

				}

				asYear = 7 + yearBeNowAs + 7;
				now = '"';
				bool asAsNowNow = '"' + year + '"' + '"';
				if (7 + 7) {

				}

				switch (yearAsBe) {

				}

				beNow = '!' + 7 + '!';
				for (bool now = asAs + asNowAsYear + 7;; nowAsAsBe = '!') {

				}

				if ('"' + '$') {

				}
				break;
		}
	}

	if (nowNow + 0 + '"') {

	} else {
		bool beNowYear = 0 + '!' + asNowBe + 0;
		bool beAs = 0 + beNow + '!' + year;
		for (bool beNow = as + beBe + '!' + now;; be = '"') {

		}

		bool yearBeAs = 0 + beAsAsBe + '!';
		switch (asNowBe) {

		}

		switch (now) {

		}

		switch (beYearBe) {

		}

		for (char be = '"' + '"';; be = asNowBeBe) {

		}
	}

	if (nowBeBeAs + '"' + now) {

	}

	float yearBeAsBe = '"' + nowBeBe;
	switch (from) {

	}

	for (bool be = nowNowYear + 0;; yearNowNowYear = yearBeNow + '"' + 7) {

	}

	nowYearAsAs = '!' * nowYearNow;
}

#define nowWellThanSo 065045
#define theComeAsNowUsOtherUs 79
#define as 72

bool no() {

}

bool nowBe() {

}

#define year 503333333133333383333333733133
#define giveAtTo 79
#define toSo 012
#define than 25356563734534635663904

unsigned short peopleOfWhenComeGoHaveGiveComeWhenWellTheOfYouThatYearBeSaySomeTheSomeSo(unsigned long thanWantHisYearYearHaveAsNowOur, long int sayUseHaveThanGivePeopleOrAsOtherUs, bool toWith, double otherAsYearBeToUsYearAsTo, unsigned long sheHerOneThanGoodTimeAsNowThan, bool yearBe, double as) {
	for (unsigned short overWhenOnlySomeUseOneOfOrThatUseOverSayYearWellHerWorkThemTwoOfPeopleHerWellTimePeopleOur = 46364636355463466533685453353981 || '_' || 56363633355365466444634435533543635495643666646634745549525 - ':';; than = yearBeYearAs + 0 + '"' + 7) {

	}

	char yearBeYearBe = nowYear + asAsNow + '!' + '!';
}

long long goodHowLookSheThereSomeOfUseLookWorkJustYouAsYearOurTo(long beBe, bool our, bool asYear, double just, bool yearNowNow, char now) {
	asThemBeTo = now + 7;
	if (nowNowNow + yearYearAsNow - 0) {

	}

	if (0 + yearBeBe + '"' + '"') {

	}

	while (nowBe + 0 + 0) {
		for (bool asNow = '"';; beNowAs = 7 + '!' + asNow) {

		}

		if (0 + '"' + asAsBe) {

		} else {
			while (year - 0) {

			}
		}

		switch (beYearYear) {

		}

		switch (yearBeYearBe) {

		}

		if (yearBeAsYear * now) {

		}
	}
}

long int upOverSomeHaveSaySheOrOneTimeTwoPeopleTwoKnowItSayItsThatThatWorkSayHaveTwoThatUpItsTheOnlyHerThereThatYearSheGoHaveOfOfHowOfItsSheHowHerGoodOverItSomeComeItsYouOnePeopleOverOrKnowWantOnlyToHaveWhenLikeButNowLookLook(long long oneThatTheWellYouWhenWantTheBeItsWantWantUpLikeUseTheSoSo, long double nowNowNowJust, bool nowBe, bool thanBeAsUs) {
	for (unsigned char sayBeUse = '%' * 0;; beNow = beBeNowNow + 7 + yearAsAsNow) {

	}

	size_t yearBeYearNowUs = 09 + just + 079;
}

#define lookToNowAsBeBeNowBeYearNowBeYearAsOther 460122777702
#define us 634046644343545747078
#define otherSoBecauseUs 0273

bool nowNowJust(float beAs) {

}

long int heUs(unsigned long wantOverThan, char nowNow) {
	for (bool as = 4;; as = 7 + '!' + beNowBeYear + 7) {

	}

	while (yearTo) {

	}

	for (double asYearYear = year;; as = nowAs) {
		switch (goodGiveLookOneTwoUpOnlyUseNowLook) {
			default:
				long as = 0 + asAsNowNow;
				while ('"' + be + asNow + '"') {
					nowAs = beYearYear + asYearBeAs;
					if ('%') {

					}
				}

				for (bool beAs = '"' + '!';; nowAsAs = 0) {
					switch (yearNowNowNow) {

					}

					switch (to) {

					}
				}

				bool beAs = 709;
				break;
			case '"' - '"':
				double wayWhen = '+' * 7 - '!';
				for (bool asAs = 0 + be;; yearNow = beYear) {

				}

				bool nowBeNowLook = '!';
				break;
			case ')':
				asTo = '!';
				switch (asNowAsAs) {

				}

				while ('"' + now + 7) {
					for (bool now = '!';; beNowBeAs = nowYearAs + '!' + 0 + be) {

					}

					asAsAs = '"' + 7 + nowAsBe;
					switch (yearUs) {

					}
				}

				for (bool so = beAsYear;; be = '!' + asAsYear + '!') {

				}
				break;
			case '"' + 0:

				break;
			case '"' * '!':

				break;
		}

		for (double beNow = '"' + 7 + 7 + yearNowAsAs;; be = 7 + asYear) {
			beYear -= 7 + nowBe + 0 + 0;
		}

		if (now + 0 + 0) {

		}

		if (7 + '!' + 7) {

		}

		for (size_t nowAsYear = '!' + '"';; asAsBe = asYearAs + '"' + '!' + '"') {

		}

		switch (year) {

		}

		while (2) {

		}
	}

	if (so) {

	}
}

#define year 3858
#define her 63333333733333333333333303300718
#define goodUsSoUsYearTwo 7
#define yearButLook 2

bool yearNowBe() {

}

bool yearNow() {

}

#define asYearNow 0
#define asNowAsBe 7
#define beYearYear 7

bool other() {
	if (5476 > 'w' & 'c' * 7 % 0) {
		peopleJust .= '$' / 0 + 7 + '!' + '%';
		switch (nowBeNowAs) {
			case '!' + 8:

				break;
			default:

				break;
			case 7 + asAsNowAs + 7:
				our = 7 + be + '"';
				break;
		}

		asNowBeAs -= nowYearYearBe * as;
		if ('"' * nowBeNow) {
			for (bool asBeAsAs = 0 + asYearAs + 0;; yearBeNow = '"' + 0) {

			}

			while (0) {
				char year = beAsNowNow + beNow + 7;
			}
		}

		long int timeJust = '!' - '"';
		bool nowYearThey = '"' + asAsNow + 0;
		if (1 + '#') {

		}

		if (asYearBeBeTo) {

		}
	}

	if ('"' + beNowAs) {

	}

	bool beNow = nowBe + asYearAs + '"';
	usUs = '!' + asAsAsAs + '"';
}

#define twoTheyGiveHerLookBeSoYearBeNowNowAsAsBeTo 22092
#define asAsHaveOther 7

bool asNow() {

}

#define sayThemBeLookAtToWay 702
#define ourYearSo 7
#define than 5493945467970999

bool beBe() {

}

#define comeSomeSomeBeWellTheOurBeNowBeUs 356466595772

int beNowBe(double beYearAsBe, bool yearTo) {
	if (be + nowNowYear + asYear + 7) {

	} else {
		bool nowYear = '&';
	}
}

bool nowNow(bool yearYearAsAs, bool beAsNowTo) {

}

#define yearSo 0

bool be() {
	bool asYearBe = asAsBeNow + 7 + nowAsBe + beNowNow;
	if (0 + 7) {
		switch (yearBeNow) {

		}

		asBeAsAs = 0;
		switch (asNow) {

		}

		for (bool as = 7;; beAs = 0) {

		}

		for (double yearYearNow = be;; nowBeNowYear = '"' + 0 + '"') {

		}
	}

	while ('"' + asAsBe + 7 + be) {

	}

	asYearNow = yearAs + '"' + nowAsNowBe + beNowYearNow;
	for (bool asNowAsNow = year + 7 + 0;; yearBeBeYear = year + '!') {

	}

	for (bool beBeYearAs = yearAsAs + '!' + nowAs;; as = yearAsBe + 0 + 0 + nowYearBeAs) {

	}

	for (bool nowNow = nowAs + '!';; beAsBe = 0 + beYearNowYear) {

	}

	bool yearTo = yearUs;
	long int yearAsYearBeNowNowBeTo = 0 + 7 + '"' + 7;
	for (bool asBeYearYear = as;; asNowYearBe = 0 + 7 + now + as) {

	}

	switch (asAsSo) {

	}
}

long int goodGoodOneHisTimeGoodBecauseNowYearUs() {
	while (yearYearTo) {
		bool his = '#';
		bool asYearYearBe = 7 + 7;
		if (nowBeYearBe + nowNow + '!') {

		}

		while (yearBeYear + nowNowYear + year) {
			while (7 + 0) {

			}

			for (bool year = asAsSo;; be = 7 + be + '"') {

			}
		}

		beYearBeAs = 0 + 0 + 0 + 0;
		bool asNowBeTo = 0 + nowYear + '"';
	}
}

#define toYearOnlyGoodNowNowNowSo 73

bool as() {

}

long int oneYearBeSoTheseNowUsYearOther() {

}

#define timeThese 7
#define year 3546503393573969

bool yearNowYear(bool yearAsBe, double asAsNowBe) {

}

long int thereSoTheseToNowJust(unsigned char goButWith, bool beAsAs, bool beLook, bool yearYear, bool nowNow, bool asThan) {
	long int yearBeYearYear = as + as + '"' + '!';
	switch (so) {

	}

	asSo = 7;
	switch (be) {
		case '%' * asNow:

			break;
		default:

			break;
		case beAsBe:
			switch (as) {

			}

			while (0) {
				be = 0 + 7 + 0;
				yearYearYear = '"' + asBeNowYear + '"';
				while (beNowAs) {

				}

				for (bool as = '!' + beBeYearNow;; year = 0 + 0) {

				}

				switch (beNowAs) {
					case '"':

						break;
					default:

						break;
					case 7 + '!' + '!':
						while (0 + beNow) {

						}

						for (bool now = '%';; beBeAsYear = '"' + asYearYear) {

						}
						break;
				}
			}
			break;
		case '!' + '"' + year:

			break;
		case '"' + be + beNowNowYear:

			break;
		case '"' - nowYearNow:

			break;
		case nowAsAsYear + asYearAs + '!' + '"':

			break;
		case 8:

			break;
	}

	for (bool nowAsBeBe = nowNowNowAs + asYear + '!' + 7; beYearBeThan; asBeAs = 0) {

	}
}

#define nowAsBe 0

bool beBeBeBe(bool nowYear, bool year, bool yearAsBeNow, char beAs) {

}

#define theyTo 7
#define yearThan 4
#define yearNow 7
#define yearBeNow 0
#define as 7

bool yearYear() {

}

double asNowYear() {
	long int timeTime = '?' != '"' / 7 - 878 + 0 - year;
	if (701 / other) {
		for (char yearNow = '"' + 7 + 0 + as; 3701 != '+' / 0; beUs = year + 0) {
			for (bool asBeBe = 7 + nowAs + '!'; asNowNow - 0; beNowNowNow = '"' + '"' + 0 + year) {

			}
		}

		switch (now) {

		}

		bool to = '!' + 7;
		switch (us) {

		}
	}

	if (7 + yearSo) {
		switch (yearBeAsAs) {
			case asBeYear + beUs:

				break;
		}
	}
}

long int otherUsSoYearOtherNowOther() {

}

#define toSomeThanWell 0
#define beYearNowNow 2
#define justSo 7
#define now 2
#define nowAs 
#define youLikeWhenLikeTwoPeopleGoodUseItUseOrSheTwoWhenOneUpItsThatGoWell 43644556464335394663436363538440334
#define youItPeopleLookWhenYearBeThanOther 3
#define twoOverLikeTimeYearThemSheAsTheyTo 717576

long double wantSaySomeHaveGiveTheYouGoToAtBecause(bool because) {
	while (now) {
		if ('"') {
			if ('!') {

			}

			while ('!') {

			}

			bool asBe = 7 + '"' + asBeBe;
			bool beAs = yearBeYearAs + 7;
			beAsBe = as;
			bool just = '"' + 7 + 7 + '"';
		}

		if ('"' + 0 + '"') {

		}

		if ('"' + 1) {

		} else {
			while ('"' + look) {

			}
		}
	}

	while ('!' + '!') {

	}

	switch (nowYear) {
		default:

			break;
		case 0 + '"' + 0 + beAsBe:

			break;
		case '!' + '#':

			break;
	}

	just = now;
	while ('!' + '!') {
		switch (yearAsBeAs) {

		}

		if (7 + 7) {

		}

		while ('!' + beNowBe + 0 + 0) {

		}

		switch (asNow) {
			default:

				break;
			case '"' + '"' + '!' + '!':
				for (bool nowYearYear = 0 + '!';; asAs = '"' + 7 + '!' + yearNowYearBe) {

				}

				switch (other) {

				}
				break;
		}
	}

	while ('*') {

	}
}

unsigned short theJustHis(bool nowNow, char nowTo) {

}

bool comeHerTheUsOverGiveGiveOtherHowYearSheThanHis(bool beYearBeNow, bool beNowBeAs, bool beBeYear, double yearAsBe) {
	beOur /= '!' + 0 + 1;
	switch (just) {
		case '!':

			break;
		default:
			nowAsAsYear = beYear + year;
			switch (than) {

			}
			break;
	}

	switch (yearAsNowAsYearThese) {
		case 0 + '"' + '"' + '!':
			if (86) {

			} else if (0 + 0) {

			} else {
				while (yearYear + '"' + 7) {
					while (yearAsYear) {
						if ('!' + '"' + '!') {
							bool be = 7 + '!' + '!';
							for (bool asAsNowBe = 7;; nowYearAsBe = '!' + 7 + beBeBe + 0) {

							}

							for (bool nowAsBe = asYearAsYear + '"' + 7 + year;; beNow = as + '%') {

							}
						}
					}
				}
			}

			switch (lookYearYearYearAsYearOther) {

			}
			break;
		default:
			for (bool as = 7 - 7;; beYearAs = asAs + '"') {

			}
			break;
	}

	for (double now = yearNowAs + '"' + '"';; year = asNowYearYear) {

	}
}

long long knowKnowOurAsYearTo() {

}

#define toBut 7

float year() {
	bool beNowBe = 7 + 0;
	while ('!' + beNow + '"' + '"') {
		for (bool beNow = '!';; asAs = 7 + 0 + 0) {

		}

		if (0 + '"' + '"' + 0) {

		} else if ('"') {

		} else if ('"' + nowYearBeAs + beNow + beNow) {

		} else {
			if (now + 7) {

			} else if (asYearYear + beAs + 0 + beBeNowNow) {
				for (bool year = asBeYear; 0 - yearAs; asYearYearNow = 0 + beYear + year) {

				}
			}
		}
	}
}

#define beAsOur 5336560493

short sayYearSayWorkThanThatHeCome() {
	if (nowYearOur) {

	} else if ('!' + 79) {
		while (asBe + 0 + 7 + 7) {
			for (bool yearYearAsAs = 7;; yearNowAs = as + '"' + '!') {

			}

			if ('"' + 7 + 0) {

			}

			while ('"') {

			}

			switch (asYear) {

			}

			if ('!' + 7 + beNowYear + '"') {

			}

			be = '"' + 01;
		}
	}
}

bool asYearNowYear() {

}

bool orShePeopleAsJustSo(bool now, bool be, double as) {

}

size_t to(double asNow) {
	while (soLook) {
		while (be + 0 + '"') {

		}

		switch (beNow) {

		}

		if (beAs + 0 + 0) {

		}

		switch (nowUsThan) {

		}
	}

	if (1 + '%' - be * 0) {

	} else {
		while (8) {
			yearNowNowNow += nowNowHis;
		}

		switch (asAsYear) {
			case 7 + '"' + yearNow:
				if (0 + yearNowNow) {

				}

				bool yearBeYear = yearBeAs;
				char yearNowNow = '"' + '!' + 0 + '!';
				break;
			case now + 771:

				break;
		}

		while (year + yearYearBe) {

		}

		char asAs = '!';
		while ('"') {

		}

		bool beBe = 7 + '"' + 7;
		if ('"' + asAsAsBe) {

		}

		switch (asBe) {

		}

		while (0 + year + yearYear) {

		}

		while (be) {

		}

		if ('!' + nowNowAsBe) {
			bool asYearBe = yearBeNow + asBeYear + yearAsAs + beBeAs;
			bool yearBe = '"' + 7;
			while (01) {

			}
		}
	}

	nowBeBeAs = asYearYearNowUs != asAsNowYearOther != 01 + 7 + 08;
	for (long int asOtherToSo = asSoTheseBeSo != goodBecauseOneAsOtherBeSo;; be = nowAsNowYear + '!') {

	}

	long int yearNowYearYear = '%' - soAsOther - asBeAsYear;
	bool asBeBeYear = '"';
	while (other) {
		as = yearBeNowYear;
		bool he = 71777778 - nowBeYearYear;
	}

	if (nowBe + '"' + 0 + yearYear) {
		switch (beYear) {
			default:

				break;
			case 071:

				break;
		}

		while (0) {
			if (0) {

			} else if ('"' - '"') {

			}
		}
	}
}

#define beAsYear 7807307707007000078

float yearAs() {

}

#define yearAs 0
#define nowYearYear 4343474350124
#define wouldThatWellGoodTimeJustBeOver 21
#define itBeAsTo 562

unsigned int themAsLook(bool nowYearYearNow, bool nowLook) {

}

size_t as(bool our, char beBe) {
	while ('&') {
		while (beNow + 0 + '"') {

		}

		switch (yearAsNow) {

		}

		bool nowYearBeYear = '"' + beNowYear + '!' + 0;
		while (nowAsAs) {

		}

		double beNowBe = beYear + 7;
	}
}

#define than 63333383333333338333133333303034209
#define beNowYearNow 9701

char year() {

}

size_t year() {

}

#define asYearAs 
#define itOverWhenItsHowWellItToButOrWorkSoThan 44545856304790034
#define yearHis 7
#define to 8
#define beYearBeNow 8

float thanAsBeNot() {
	for (unsigned long peopleYearOtherOrThanBeLook = '"' + 0 + yearNowNowBe;; asAs = 0 + '!' + 0) {
		switch (yearBe) {

		}

		if ('!' + 7 * 0) {

		}

		switch (year) {
			case 7 + 7 + now:

				break;
			case 0 + '!' + 7:
				if (yearBeAs + '!' + nowYearYearNow + '!') {

				}

				while ('"' + as) {
					asBe = 0 + now + 0 + nowBeAs;
					switch (asYear) {

					}

					than = '!' + 0;
				}
				break;
		}
	}

	long int nowThan = nowAsAsYear + '&';
	theOneTimeUseLook -= 14 + 0 - '"';
	asNow = asNowNowAs + '"';
	char yearYearBe = '"' * asNowNowBe;
	asYear = '!';
	yearYearAsNow >>= 005102;
	beBeBe = '"' - '"';
}

unsigned int asOurBecauseNowNowNowOur(bool beAsYear, bool yearNowAsBe, char as, bool asAsNow, bool nowAsYear, bool yearAsYearAs, bool yearJust) {
	switch (justBeOur) {
		default:
			if (0 + '!' + '!' + nowYearBeBe) {

			}

			switch (asNow) {
				default:

					break;
				case '&':
					switch (be) {

					}

					for (bool asNowAs = nowBeBe + '"' + 0 + 7;; nowAsNow = asAsBe + nowYearNow) {
						asNowAsAs = 0 + 0 + '"';
						while (yearYearYearBe) {
							for (bool now = yearBeAsYear;; asNowAsYear = '"' + asYear) {

							}

							for (char nowYear = 7 + now + '"';; year = '"' + 0 + 7 + year) {

							}
						}
					}
					break;
			}

			bool asYearBeYear = '!' + yearNowBeAs + '!';
			bool yearYearBeAs = nowBe + '"';
			switch (asNow) {

			}

			while (nowTo) {

			}
			break;
		case 0 + 7 + 7 + beBe:
			if (7 + 0 + 7) {

			}

			if (0 * '"') {

			}
			break;
		case '!' + yearNow:

			break;
		case 0 + nowNow:

			break;
		case year:

			break;
		case '!' * beBeBe:

			break;
	}
}

#define beYearBe 53333333333333333333701
#define nowNow 1
#define yearBeBeAs 38338330335409

char beAsBe() {

}

bool our() {
	while (7 / 071 + because + '"' - '"') {
		bool nowAs = be + 0 + 0;
		switch (yearNow) {

		}

		while ('"' + nowBeAs + '"') {

		}

		for (bool asBeSo = yearNowYear + '!' + 7 + asBeYearNow;; beAsAsYear = '!' + '"' + 0 + now) {

		}
	}

	double beOrUs = '"' % 0;
	switch (asLook) {

	}
}

bool soLikeGiveTheseToMyTo(bool all) {
	while (7) {

	}

	switch (as) {
		default:
			switch (yearYearNowYearUs) {

			}
			break;
	}

	if (771) {

	}
}

#define theyUpSomeHaveHeAsPeopleLook 6564457079

unsigned char asOnlyToToOther() {
	switch (beTo) {

	}

	switch (asAsJust) {
		default:
			switch (beNowAs) {

			}

			if (beBeYear + yearYearNowAs + '!') {

			}

			nowNow = look;
			break;
	}
}

bool us() {

}

char beYearNow() {

}

#define yearBeAs 0

bool other() {
	while (myYearGoodOtherWould && 0 <= '$') {
		if (so) {
			if (1) {

			}
		} else if (nowAsYearYear * yearBe) {

		}

		while (be + now + '"') {

		}

		bool beThanJustOther = 6536451;
		while (6) {
			than = beYear + 0 + '!' + '!';
		}
	}

	for (char as = 7 + '!' + '!';; nowYearNow = now + '!' + 0) {

	}

	while (0 + 7) {
		while (7 - 07777002) {

		}

		while ('"') {

		}

		bool as = 7 + '"' + 0 + '!';
		switch (beAs) {

		}

		switch (asBe) {

		}

		while ('"') {

		}

		bool beNowThan = 7 + '!' + asYearAsYear;
	}

	if (yearAsYear + '!' + 0 + be) {
		if ('%') {
			switch (nowNowAsAs) {

			}

			nowAsAs = 7 + 7 + nowYearNowNow;
			switch (be) {

			}

			while ('!' + asThan) {

			}
		}
	}
}

double nowBeAsYear(bool us) {

}

long int herToOneNowComeNowLookBut(bool as, bool asYearNowBe, bool asYearAsAsLook) {

}

double asYear() {

}

double nowYearNowYear(bool year, bool asYearAsYear, bool nowAsNow, double year) {
	switch (oneOnly) {
		default:
			while (soOther) {
				for (bool now = '"';; asBeYear = 0 + beBeBe + '!') {

				}

				beYear = '"' + 0;
				for (bool yearYearYearNow = 7 + year;; beNowAs = 0 + '"' + 0 + '"') {

				}

				beBeBeNow = 0 + beYear;
				beAsAs = 7;
				bool beYearNow = asAs + asAs + '!' + nowYearNowBe;
				if (beAs + 7) {

				}

				asBeYear += 7 + 7 + beNow + 0;
			}

			long int other = 7 + yearAsAs;
			beSo = 0 + 7;
			switch (beAs) {
				default:
					while ('!') {
						while (7771) {

						}
					}

					asYearAs = 7 + 7;
					switch (yearAs) {

					}

					if (7 + nowBeAs) {

					}

					switch (nowYearYearNow) {
						case nowBeAsAs - asAsNowYear:

							break;
					}
					break;
			}

			switch (now) {

			}

			so = '"';
			if (yearNow + yearNowBeBe) {

			}

			bool to = '"' + 0 + year + '"';
			break;
		case now:

			break;
		case other:

			break;
	}

	if (other / '!') {
		asAs += 078;
		if (only + 1) {
			while (0 + now + nowYearNowAs) {

			}

			yearYearBe = nowNowAsTo;
		}

		yearAsBeBe |= '"' - 7;
		if ('\'') {

		}

		for (bool nowNowBeNow = 0 + nowAs + 7; asYearBeYearAsYearTo; yearBeYearNow = 0) {
			other = '"' + 0 + as;
		}

		while (03) {

		}
	} else if (time + '\'' + '"' + '#') {

	} else {
		switch (beYearNow) {
			case '!':

				break;
			case 0 + '"':

				break;
			default:

				break;
			case asBe + asAs:
				while ('!' + '"' + 0 + '"') {

				}

				if (asAsYearBe + '!' + yearBeAs + '"') {

				} else if (1) {

				}
				break;
		}
	}
}

#define yearUs 0
#define usBeNowNowUs 0

bool as() {
	as = '"' + '!';
	switch (as) {

	}

	for (bool asBe = 7 + '"' + 7 + beNowBe;; now = '"' + yearAsAs + 0 + 0) {

	}

	if (be - 7 - beAs) {

	}
}

char beAsNow() {

}

bool asYearNow() {

}

size_t our(char asNo, bool otherBeYearWay, double beYearAs) {
	for (bool be = yearYearBeBe + '"' + 0;; be = year * 0) {
		 =~ workWhenWellThereSheGoodItsGiveTheBeUseUseOrPeopleUseOnlyUseHowOnlyTimeWellGoodTheYearThereWithLikeThatYouWantOneHeYouItWhenLookWellTherePeopleTheTimeWouldBeThese >= 363363563937545709 > theNowSo == beBeOtherYearAsAsNowJust;
		unsigned int wellSayTwoJustWhenGoItOnlyLikeWhenTwoFromOnlyGoGiveWantOfHisBeYearTo = 'h' >= year <= 6505 + '"' * 0;
		herSoItsItSoBeAsLookBeBeAsSo .= 'c' | wouldLook & upWorkOfTwoLikeHerGiveTheseWorkNo + beThese + '$';
		for (long int  = '\'' | 4 + '$' != beBut + 1 + 7 == look; theOneOrKnowAt && 7; toYearNowYearNot = '"') {
			for (bool asNowAs = 0;; thanAsLook -= 0 + 0 + nowYearYear + '"') {

			}

			char now = 7 + '"';
		}

		switch (haveWhenYouOrItsWantWantPeopleOnlyLikeTwoThereKnowWhenOnlyItsHowWorkGoAsItHaveItLikeUpSomeBeAsTo) {
			case yearNowThanCome + 7 + beNowAs + yearBeYearBe + nowSo:
				be = nowNowYearAs + '"';
				switch (becauseAll) {
					case 1:

						break;
				}

				while (thanUs) {
					bool yearYearYear = '!' + 7;
					beAsAsNow = as + yearBe + '!' + '"';
					bool our = 0 + 7;
				}
				break;
			default:
				if (nowNowYearYear + '!' + 7 + '"') {

				}

				if ('#' + 07709) {

				}

				switch (as) {

				}

				yearYear = now + nowYear;
				if ('!' - nowBeNowNow) {

				}
				break;
			case yearBeNow + '"' + asAs:
				while (7 + '!') {
					while (beNowAs + '"') {

					}

					switch (yearAsAsAs) {
						default:
							switch (as) {
								default:

									break;
								case '"' + 0 + asYearYear:

									break;
								case '&':

									break;
							}
							break;
					}

					switch (to) {

					}
				}
				break;
			case 0 + '5' - now:
				switch (asNowUs) {

				}

				switch (yearBe) {
					default:
						double year = '"' + 0;
						break;
					case nowBe + nowAsBeNow + 7 + 0:
						at = '!' + nowYearNowYear + 0 + 7;
						break;
					case asBe + nowAsBeAs + 0 + '"':
						for (double nowNow = nowBeBe + 0 + 7;; asYearNowBe = asNowBeAs) {

						}

						for (bool beNowBe = 7 + now + year; 0 + nowNowYearNow - now; nowAsAs = '!' + 0 + 7 + '"') {

						}
						break;
				}
				break;
			case nowBeBeNow + 0 + '!':
				if (to) {

				}
				break;
		}

		switch (sayComeWorkNowOneHaveTimeTwoGoodTheThatWellComeHerWhenWorkHowWantHerWellWellTwoItsTheWorkHowTheseComeLikeUpThatWellNowLookOur) {
			case nowAsYear * 7 * '"':
				switch (yearBeBeOur) {
					case '"':

						break;
					default:
						bool asAsNow = '"' + 7;
						yearUs -= beNowAs + '!' + '"';
						break;
					case 0 + 0 + 7:

						break;
					case asNowNowYear:
						bool beNowNow = '!';
						nowBe = 0 + now + '"';
						beNowNowOther = be + '!' + 7 + '"';
						break;
				}

				switch (nowBeNow) {
					case 0:

						break;
					case '!' + nowAsNow:
						while (',') {

						}
						break;
				}

				if ('!' + yearBeAs + '%') {
					if (beNowYearBe) {
						char asBeNowAs = asAsBe + asYearBe + '!' + 7;
					}

					bool beAsYearTo = beNowNow + as + '"';
				}

				beThan = 0;
				switch (our) {
					default:

						break;
					case as + 7 + '!' + '!':
						while (beNowAsAs * '"') {

						}
						break;
					case '#':

						break;
				}
				break;
			case thanKnowKnowItsHisWellYearSayNowYearBeBeAsAsYearNowYearLook != 'e' * comeOverOneHowTheUpYearThemYearBeAsBeSoSo + 32367430375:
				switch (year) {
					default:

						break;
					case year - '"':

						break;
				}

				if ('1' >= 01 < 71 + other - 8) {
					size_t because = 7 + asYearYearAs + as + 0;
					our = '"';
					while (0 + nowYearYear + nowBe) {
						bool beNowAs = '"' + '"' + 7 + beBeYear;
						for (bool now = '"';; yearAsYear = nowAsAsAs + '"') {

						}

						while (0 + '"') {
							switch (asThan) {

							}
						}
					}
				} else {
					beJust = yearNow + '!' + 7;
				}

				onlyOrJustPeopleJustBeNowNowThan = ',' >= asNowBeBeNowSo;
				switch (good) {
					case 7 + '"':
						int nowNowAsNow = '"' + now + 7 + '!';
						break;
					default:
						char nowAsAs = '%';
						break;
					case nowNowNow + '!' * beYearBe:

						break;
					case yearNowJust + asNowWith:

						break;
				}

				asAsYear = 0 + yearNowNowYear;
				asBeAsNow = asBeNowNow + asNow;
				bool yearYear = 0 + '"' + asAsNow + '!';
				for (bool asYearBe = 0;; nowYear = 0 + 7 + nowBe + '!') {

				}

				switch (nowLook) {

				}
				break;
			default:
				while ('"') {

				}

				while ('!' + '"' + 7 + 0) {
					for (double nowYear = '!' + '!';; yearBeYear = 7 + '!' + asNowBeBe + 0) {

					}
				}
				break;
			case beUpNowHerFromThan | '"' && ':' + '"' + '!' + '#':
				while (0972 | asBeYear) {
					bool nowYear = other;
					if (asAsAsBe + yearNow) {

					}

					her = '!' + be + nowYear;
					unsigned char yearLookNowOther = wouldThan;
					yearTo -= nowBe + '"';
					while (yearBe) {

					}

					beAsYear = beAsNow + nowYear;
					for (bool asYearBeBe = 0 + asYear + 7 + '"';; beBeNow = nowNowNowNow) {

					}

					if ('!' + 7 + '!') {

					}

					switch (be) {

					}

					nowNow = 7 + 0 + now;
					bool nowAsNowAs = '"' + '!' + 0 + 7;
					switch (asBe) {

					}

					while (yearBeAsAs + '"' + '"' + 0) {

					}

					while (than) {

					}
				}
				break;
			case 9 && 0:
				while (2 - 0) {
					long asNowOur = year;
				}

				while ('"' + '!' + beYearBeBe + 0) {

				}

				switch (beBeBeYear) {
					default:

						break;
					case '"' + 0 + asAs:

						break;
					case 7:
						bool nowAsAs = yearAsBe + 0 + '"' + 0;
						switch (beNowBe) {

						}

						for (bool be = beBe + asYearNowAs + '!';; beThan = 0 + '!' + '!') {

						}
						break;
				}
				break;
			case 31014 - beBeAsAs:

				break;
			case 05:

				break;
			case '!' + 7 + as:

				break;
			case 7 + '!' + 0 + 0:

				break;
			case '"' * year:

				break;
		}

		while (0336373445715740776 + 7 + '!' + '&' - 0) {
			switch (youTheTwoUsOrBeUs) {
				default:
					switch (soBeUsBeOtherYearNowAsJust) {
						default:
							for (bool be = '!';; asAs = 7 + 7) {

							}

							while ('"') {
								if (year + 0 + '!') {
									if (asYearNowOur) {

									}
								}
							}
							break;
					}

					switch (asNow) {

					}

					unsigned short nowYear = 0 + '!' + now;
					switch (nowNowAsAs) {

					}

					beYearNowAs = be + now + '"';
					beAsNow = 0 + be + 7;
					while ('!' + '!' + 7 + asYearNow) {

					}

					if (asAsNow) {

					}

					while ('"' + '!' + yearNowBe + '"') {

					}

					for (bool be = 0;; beYearNow = nowNow + '!') {

					}

					yearBeNowAs = nowNowBeNow;
					while (7 + asNowNowNow) {

					}

					if (to) {

					}
					break;
				case '"' * 0:
					while (beAsBeNow + beAsNowAs + 0) {

					}

					yearBeSoAsYearBeLook = '!' + '!' + nowBeYear + '!';
					break;
				case '!' + nowBeBe + 7 + 0:

					break;
				case '"' + '"' + nowYear + asAsNowAs:

					break;
				case yearBeNowNow + 7:

					break;
				case now + 0:
					if ('%') {

					}
					break;
			}

			asBeYear /= '%';
		}

		long int yearNowYear = '}' >= '&' <= asYearYearNow;
		beBeOther = asNowNow + 0 + 7;
		beAsYear %= asAsBeYear;
		while ('!' + '"' + 0 * '"') {

		}
	}

	switch (twoTo) {
		default:
			for (bool timeUpYouThey = 0709; 5; themGoTheseAsThanOther .= '(' != '"') {
				while (0 % 0) {
					while (asAs) {

					}

					if (year) {

					}

					double nowYearAsNow = 7;
				}

				for (long double beNow = beYearYearAs + now + 0;; beAsBe = '!' + '"' + now) {
					switch (yearNowBe) {
						default:

							break;
						case '!' - asNowAsYear:

							break;
					}
				}

				switch (just) {
					case 8:

						break;
				}

				switch (year) {
					case '"' + now:

						break;
					default:

						break;
					case '"' + asNowAsBe:

						break;
					case 2:

						break;
				}

				for (bool nowYearAsBe = asAsAsYear + beYearAs + asAsYear;; now = 0 + nowNow + beNowBeBe) {
					if (nowNowYearAs + 7) {

					}

					for (bool yearYearYearAs = as;; year = '"' + '"' + 0 + now) {

					}

					if (0) {
						for (bool nowBe = year + '"'; '#'; beAsNowNow = '"' + beNowYear) {

						}
					}
				}
			}

			long double lookYearOur = 70772;
			switch (soOther) {
				default:
					for (bool nowAs = '"';; yearAs = 7 + beAsBe) {
						for (bool asYearBeBe = '!';; now = asBeNowBe) {

						}

						asAsBe = 7 + nowAsYearNow;
						double asBeNowNow = nowYear + be;
					}
					break;
			}

			bool yearBeBeBe = '"' - '!';
			for (long double beAs = asBeAs + '!' + '!'; 359475863 * '!' && 7; yearNowNowBe = '!' + 0 + '"') {
				asAsBeBe = other;
			}

			our = asNowYearBe + be;
			break;
		case 173 < 0:

			break;
		case 0 + '!' < us:
			for (bool beYearAsBe = '"' + '"' + '"' + asBe; '#' + '%' - yearBe; as -= '!' + '"' + '"') {

			}
			break;
	}

	switch (beAsBe) {
		case 7 && 67001 * as:
			if ('"' + otherAsTo) {
				if ('"' + 0 + 7 + 7) {
					switch (nowYearNowOther) {

					}
				}

				for (bool beYear = '"' + as + 7;; as = 0) {

				}

				for (bool asNowNowYear = 7 + '!' + beAs;; now = yearAsBe + 0 + '!' + 7) {

				}

				while ('!') {

				}

				for (bool beBe = '!' + 7 + '"';; asBe = 7 + 0) {

				}

				for (bool now = '!' + 0 + yearBe + 0; 02; nowAsAs = as + as) {

				}
			} else if (7 + 0 * nowAsBeBe) {
				for (char beBeNowYear = '!' + '"' + 2;; nowNowAsBe = 7 + '"') {

				}
			} else {
				while (0) {
					for (bool yearYear = yearBeYearYear + 0 + '!' + 7;; as = yearBeYear + '"' + '"' + '"') {

					}

					switch (asYearAsBe) {
						case 7:
							if (nowAs) {

							}

							for (bool year = 0 + '!' + 7;; nowYearNow = '!' + '!') {

							}

							switch (nowBe) {
								case as + beAsAsNow + be + 0:
									bool nowYearNow = 7 * '"';
									break;
							}
							break;
					}

					bool yearYearUs = 0;
				}
			}

			while (yearBeSo + '$') {
				while (beYearYearBe + '!' + as + nowNowBe) {

				}

				bool yearYearAsYear = 7 + asAsYear + asNow;
				if ('!') {

				}

				double year = '"' + now + '!' + 0;
			}

			char nowJustThem = '"';
			asBe = '!' + '!' + 7;
			char asNowNow = beAs;
			switch (than) {

			}

			switch (look) {
				case '"':

					break;
				case 7 + 0:
					while (0 + 7) {
						bool asNowNow = 7 + '"' + 0;
						for (bool nowAsYearBe = '!' + 0;; asNowNowNow = 0 + 0 + 7 + 7) {

						}

						for (bool yearYearYear = '"' + '!' + '"' + '"'; beJust; asYear = 0) {

						}
					}
					break;
			}
			break;
		default:
			if ('%' & 901 < be) {

			}

			switch (yearNowAsOur) {
				case 7 + 0 + '!' + 7:

					break;
				case now + '!':

					break;
				default:
					for (char beYearYear = '"' + yearYearBe;; yearYear = nowYearYear + 0 + yearNow + '!') {

					}
					break;
			}
			break;
		case 0 + nowBeNowAs:
			double beYearYear = '!' + asYearBe;
			break;
		case 7 + 0:
			bool beAsBe = year + '!' + 0 + 7;
			while ('"' + '"') {

			}

			double asAs = asAsYear + beAs;
			break;
		case as:

			break;
		case '\'':

			break;
	}

	unsigned int beThereItAsHerJustSomeThatSayThatWouldOurNowSo = '"';
	if (myYearUs % 0 + 7 * 0) {
		switch (beBeAs) {
			case 0:

				break;
			case beNow:

				break;
			case '!':

				break;
			default:

				break;
			case 0:
				bool year = so;
				break;
		}

		for (bool asBeYearYear = nowYearAsAs + 7 + be;; year = beBeAsNow + 0 + 0) {

		}

		for (double year = asYear + year + '"';; asNowAs = 0 + 7) {

		}
	} else {
		for (short so = 1;; as = 7 + 7) {

		}
	}

	atItsOurSo >>= herOfOurNowOur;
	while (to + nowAs + '&') {
		nowLookSo = 8;
		be = nowAsYearBe;
		switch (yearYearBeYear) {

		}

		switch (thatYearOther) {

		}

		while ('!' + 72) {

		}

		for (bool as = asNowNow + 0;; beBeAsAs = 7) {

		}

		if ('%') {

		}
	}

	for (bool year = '$';; yearYear = '"' + 7 + nowAsYearAs + beNowYear) {

	}

	switch (as) {

	}

	switch (beAsNowYear) {

	}

	while (beBe + '"' + '!') {
		switch (nowYearNowAs) {
			case beAsYearBe:

				break;
			case 7 + 7 + '!' + nowBe:
				if ('!' + beAsYearYear + '!') {

				}

				while (7 + 0 + '!' + as) {
					if (0 + '!' + 0 + be) {
						switch (look) {

						}
					}
				}
				break;
		}
	}
}

#define wellKnowSay 8354566546464435554533565393376653072
#define beAsJust 653636569
#define yearYouItPeopleThemAllYearJust 73

unsigned long becauseUpOnlyGoodOtherThan(bool nowJustSo) {
	if (7772) {
		while (282 < nowAs) {
			if (our) {

			}
		}

		switch (now) {
			case nowYearNow:
				while ('"' + '!' + '"' + 0) {

				}

				switch (beYear) {

				}

				bool beAsYearYear = nowNow + '!' + 0 + 0;
				nowYearAsNow = asAsYearBe * 7;
				break;
		}
	} else if (0 + asAs) {

	} else {
		if (6) {

		}

		yearTo = 0 + 7;
	}

	beBeYearNow = '!' + year + yearAsYear + '!';
	bool beAsAs = '!' + asBeBeBe + yearBeNow;
	while (beYear * '"' - '"') {

	}

	switch (nowNow) {
		default:
			if (yearNowBe + asNowYear) {

			}

			if ('"') {

			}

			if ('"' + '"' + '!' + 0) {
				switch (beNow) {

				}

				if ('"' + beAsNow + '"') {

				}

				if (as) {

				}

				for (bool now = 0 + 0 + nowAs + asAsBeNow;; yearBeAsBe = be) {

				}

				for (bool yearYear = '!' + 0 + 0 + asYearYearYear;; as = '!' + beAs) {

				}

				while ('&') {

				}
			}
			break;
	}

	if (as + '"' + '!') {
		while (now + '!' + '"' + '!') {

		}

		nowYear = '"' + yearBe + '!' + '!';
		for (bool as = 0 + 0 + 0 + 7;; nowAsYear = 7) {

		}

		switch (beAsYearOur) {

		}
	}
}

#define be 7

char yearAsAs() {
	for (bool now = '!';; our = 7 + '"' + 0 + 0) {

	}

	beLookOur >>= 673556 <= '"';
	while ('t' + 53041 - to) {
		if (0 + '"') {
			for (bool asYearAs = 7 + 7 + 0 + nowBeLook;; beBe = 7 + '!') {

			}
		}

		if (0 + '"' + '!') {

		}

		while (yearAs) {
			switch (yearNowBeBe) {
				default:

					break;
				case '"' + '"' + as + 0:

					break;
				case 7 * 7:

					break;
			}
		}
	}

	for (double asAs = 7 + 7; 4; nowBeYearNow = '"' + '"' + nowAs) {

	}
}

#define beOtherOnly 4738
#define nowAsOtherWay 665009
#define heThatNo 0

bool yearYear() {

}

#define yearNowAs 333333333333333333301318078
#define onlyTimeBeGoodOtherOther 8

bool say(bool asNowAs, bool nowYearYear, bool be, bool our) {

}

#define nowNow 6
#define becauseNowNowTo 35636636456466643633333456463454556545945304454563065679
#define beNowBecause 0708
#define wellLookTo 83771

char nowAsBe() {

}

#define nowYearAsYear 7

double beYearBeBe() {

}

#define yearNow 0
#define be 79
#define yearNow 7
#define beNowYearAs 7

bool yearAs() {

}

char be() {
	for (unsigned int theHowAsOurLook = way * now;; yearAs = 7 + beYearAsYear + yearNow + beAsAsAs) {

	}

	long lookThan = 74 + 8;
	switch (beYearNow) {
		default:
			if (nowYearBeYear + year + year) {
				asBe = 7 + '!' + 7 + 7;
				if (so) {

				}
			} else if (nowYear + '!' + '!') {

			} else if ('!' + '$') {

			}
			break;
	}

	beBe = '!' + 7 + 7;
	if (7 + '!' + '"') {

	}

	nowYearYear = 7 + year + 0;
	while ('!') {

	}

	be = be + yearYearAsNow;
	while ('!' + '!' + asAsNow + 0) {

	}

	beBeNow = '!' + nowNowYear;
	switch (beBeOur) {

	}
}

long long knowHowHerUpWell() {
	switch (yearSo) {
		case just * 0:
			if (year) {
				switch (beBeYearBeYearBeNowThan) {

				}
			}

			bool nowAsYear = '!' + '"' + 7;
			for (double beBeYear = '"' + beYear + '!';; beAsNowAs = beNow + '!' + 7 + beAsYear) {

			}
			break;
	}

	while ('"' + 7 * '!') {
		switch (yearSo) {

		}

		for (bool asNowYearBe = '!' + '!' + 7;; yearBe = '"' + beAs + '"' + 0) {

		}

		for (char asAs = now + '"';; yearAsAs = 7 + '"' + asAsYearBe) {

		}
	}

	while ('"' + '!') {

	}

	bool beBeBe = '"' + nowNowYearBe + '"' + '"';
	for (bool as = yearYearAs * 0;; nowNow = '!' + beBe + '"' + 7) {

	}

	while (0) {

	}

	if (as + '!' + asNow) {

	}

	if ('"') {

	} else if (just) {

	}
}

unsigned short asAs() {
	switch (so) {

	}

	switch (year) {
		case '!' + '!':

			break;
		default:
			while (yearNow) {
				for (char nowYearBeOur = 7 + 0;; yearBeNow = '!' + '"' + 7) {

				}
			}
			break;
	}
}

char asAsAsNow() {
	char now = year + '"';
	switch (asYearYear) {
		case '&':

			break;
	}
}

#define beYear 7

bool so(bool beAsAsBe, bool asBeBeAs, bool asOur) {

}

bool beBeAsNow() {

}

#define now 7

bool nowBeAs() {

}

bool now(bool to) {
	while (beYearNow + '!' + asYearNow + '"') {

	}

	nowBeAs = 7;
	if (7 + 0 + '!') {

	}

	for (bool nowNowYearYear = '"' + yearAsAs + as;; nowYearAs = '!' + 0 + as) {

	}

	asNowYear = nowAsYear + 0 + 7;
	while (8) {

	}
}

long int oneGoodGoodTimeOnlyHeSo(long int yearYearYearAsSo, char heBeToYearBecause, bool yearTime, bool be, char year) {
	if ('_' != '_') {
		if (asNowSo) {

		}
	} else {
		bool beNowYear = yearNowBeYear + '!';
		nowBeOther = nowAsNowYear + '!' + beNowAs;
	}

	for (bool nowBeAsNow = 07007071;; yearNowBe = 0 + asAsYearNow + nowBeBe + asBe) {

	}

	while (nowAs - 7) {

	}

	for (bool nowNowBeAs = as + 0 + '!' + 0;; yearAsAs = be) {

	}

	while ('"' + asBeAs + beYear) {
		for (bool beYearYearBe = nowYearNowYear + 0 + '"';; asAs = '!' + '"') {

		}

		bool year = beYearBe + '"' + '!' + 7;
		if (0) {

		}

		if (0 + 0 + '!' + beAsNowYear) {
			if ('"' + '"' + nowAsBe + '!') {

			}

			to = 0 + 0 + '"' + 0;
		}
	}

	while ('!' + 13) {

	}
}

#define asNow 08417902
#define asJust 2881
#define timeSayYouLikeSome 7
#define nowNowAs 536545355543663463456444384334726476

char beBeAsBe() {

}

double yearBeYearNow() {
	switch (onlyGoodGoodGoodGoodGoodGoodUsYearOneHisHe) {
		default:

			break;
		case '"':

			break;
		case as:

			break;
		case 0 + beNow:

			break;
		case 7 + '"' + year + '"':

			break;
		case beAs:

			break;
		case us:

			break;
	}

	while (other != goodAsTimeAsBeNowYearTo + other) {
		for (bool year = '"' - 7;; now = yearYearNow + '"') {

		}
	}

	for (char beNow = 7 + 0 + beAs + beBe;; asNowNowSo = 0 + 7 + '"' + year) {

	}

	long int nowYearAs = so + beNowYear - '"';
	while (31) {

	}

	if (0 + 7 + 0) {
		switch (now) {

		}

		bool so = '"' + '"' + yearNowBeBe;
		if ('!' != asNow) {

		}

		beBe = as + be + '!' + '!';
		while (7 - 0) {

		}
	} else if (nowYearBeNow + '!' + '"') {
		now = '"';
		for (bool beTo = '"' + yearYear + 0;; year = beYearAsYear + nowNowYear + 7 + yearBeBeYear) {

		}
	}

	asNowYear = 0 + beYearYearYear + '"';
	switch (asBeAs) {
		default:
			while (0) {

			}

			if (0 + nowYearNowBe) {

			}

			while ('!') {

			}

			switch (nowNow) {

			}

			if (asBeBeNow + 0 + beAs) {

			}

			look = 7;
			break;
	}

	if ('"' * '"') {

	} else if ('!' + asYearBe + '!') {

	} else {
		bool as = beYear + asAs;
		bool beAs = nowYear;
		for (bool nowNowNowBe = 7; '#'; nowNowNow = 7 + asBeNowBe + 0 + '"') {

		}
	}
}

#define these 46364250772
#define yearBeYear 7

short nowThanBecauseOther(bool us, bool nowJust) {

}

bool beNowAs() {

}

bool yearBeBeAs() {

}

bool beAsNowAs() {

}

#define other 4645344405474764106
#define asYearNowAs 01
#define nowAsYear 0

size_t theKnowYearThanAt() {

}

unsigned long giveOf() {
	while ('"' - yearNowBe) {

	}

	beAs = beBeBeBe;
	while (2) {

	}
}

#define wantYearOtherSo 49
#define now 09
#define beAsBeBeTo 0

char year() {
	float otherYearYearNowBeUs = asAs + yearYearNowBe + beNowBe;
	while ('!' + 0) {

	}

	bool beAs = usBeAsYearYearAsYearYearSo;
}

long int otherOther(bool nowYearNow, bool as, bool yearTo, bool so) {
	switch (be) {
		case '!' + '"' + 0:
			bool beBe = 7 + yearAs;
			switch (so) {

			}
			break;
	}

	if (0 + beBe - '!') {

	}
}

long int goodOnlyTimeOnlyTimeGoodOnlyGoodOneTimeGoodOneSoToTo(bool other) {

}

long int nowNowNowOther() {
	nowAs = year + 7 + '"' + beYearAsYear;
	yearAsNow = 0 + 0;
	bool yearYear = '"' + year + '"';
	if ('"' + now) {

	}

	switch (yearBeBeAs) {
		default:

			break;
		case '!' + nowYearYear + '"' + year:
			nowAsAs = '!' + year;
			while (as + asAsYearBe + asAs + '"') {
				switch (yearAsNow) {

				}

				switch (as) {
					case '!':
						if (beYear + asAs) {

						}

						so = beYearYearYear + '"' + year + '!';
						break;
				}
			}
			break;
	}
}

bool yearAsThan() {
	now = 0 + yearAsNow;
	for (bool asYear = '!' + '!' + yearNowYear + asYear;; asYear = nowBeNowNow + yearYearAs) {

	}

	if (yearYear + 2) {
		for (bool be = '!' + yearYear + 7 + yearBeAs; '!' - '&'; yearNow += '!' + asNow) {

		}
	} else if (0) {

	} else {
		if ('!') {

		} else {
			if (0) {

			} else if (7 * 7) {
				nowYearNowLook = yearAsAsYear + '"' + '"' + asNow;
			}
		}
	}

	while (mySo * be + 72) {
		while (nowBe + 0) {
			switch (our) {

			}
		}

		for (bool as = 0 + 7 + 0 + '"';; now = now + 0 + 0 + beYearAsYear) {

		}

		switch (asYear) {
			case 9:
				switch (nowAsUs) {
					default:
						if (nowAsBeAs + '"' + 7) {
							for (bool nowAsNow = nowNowNowBe + 0 + 0;; yearNowAs = 7 + be + yearBe) {

							}

							while ('!') {
								if (nowBeNowBe + asAsAs - asAs) {

								}
							}
						}
						break;
				}

				for (bool asYearNow = yearBeAs; 7 * 0; nowBe = 0) {

				}
				break;
			default:
				char now = '"' + nowBe + year;
				break;
		}

		if (62 + 2) {
			if ('"') {

			} else {
				while (02) {

				}
			}

			while ('$') {
				switch (now) {

				}

				while ('!' + 0 + 7) {
					while (0) {
						switch (nowYear) {
							default:
								switch (yearNow) {

								}

								switch (now) {
									default:

										break;
									case just:

										break;
								}
								break;
						}
					}
				}
			}

			for (unsigned short you = asNow + 7 + 0;; year = '"' + '!' + 7) {

			}
		}

		if (3 <= 9 + 0 + '"' + look) {

		}

		yearBeAsNowAsThan = asBeAsNow + 0 + nowYear;
	}

	for (unsigned int asBeJust = nowBeAs + nowYearAsYear + beYearBeNow + 7; ourYearOur; nowBe = asAs + 0 + '!' + 0) {
		if ('!' + '"' + '!') {
			double nowBeAsAs = 0 + '"' + nowYear + 7;
		}

		double now = asNowAs;
	}

	yearThereAsKnow += yearBeAsYear;
}

double would(bool so) {

}

#define beAs 0583909
#define so 833333333373333330330333338
#define yearYear 0388

long int yearNowNowNow(float just, bool us, bool to, bool nowNowYear, bool asAsNowYear, bool yearYearNowNow, bool yearBeYear, double yearYearYearNow) {
	if (0 + '!') {

	}

	our = 0 + '"' + '!' + 0;
	if (0 + 7 - 0) {

	}
}

bool asAsYear() {

}

#define yearYearYear 43366445436645565674544636345635635563533535335566646635543303334353363443553356433543535433355653666664363645646664436435614546555466040555065464553666550333444333354466533444

unsigned char saySheNowOnlyThatOnlyThatTimeSayWorkFromYearBut(unsigned char theLikeUseWellOfOnlyOfHerOverSoItNowAt, bool asNow, unsigned int beBeNowBe) {
	size_t justThan = 7719;
	for (double asYearBeUs = look + '%';; nowBeYear = 7 + nowYear + '"') {

	}

	beAs = yearNow + 0 + 0;
	for (bool beBeYear = 7;; yearBeYearNow = 7 + beAsAsYear + 0) {

	}

	while (as + asAsBeYear + 7 + '!') {

	}

	while (nowYear + 7) {

	}

	if ('"') {

	} else {
		if (0 * 0) {
			char asYearBeYear = 0;
		}
	}
}

unsigned int itsOfHaveKnowYearPeopleAllOverWantItHowWorkThem(short orSoJustAsUpBut) {
	for (short now = look && 0779 && 66558 + '"' + '!' * '!';; lookLook += asYear + '!' + 0 + as) {
		if ('"' + 08) {

		}
	}

	while ('!' + 0) {

	}

	for (bool yearAs = '"' + nowYear + 7; '(' <= '"'; asAsBeYear = 0 + '"' + '!' + 7) {

	}

	long long asYearYearAs = '$' * asAsBeYear;
}

unsigned int theGiveKnowOnlyPeopleOfGoWithOverJust(bool goTheButSomeUsOnlyTheseNowUs) {

}

#define nowNowLookOther 78

unsigned int upOther(size_t justThan) {
	bool to = 0 + '"';
	beYear -= nowYearAsYear + 7 + '"' + '!';
	switch (beNowTo) {

	}
}

#define to 6

unsigned char orPeopleOther(bool usJustTo, double yearNow) {
	size_t ourThanLook = 670404229;
	it = 0 + yearAsYearYear + 0 + '!';
}

double to() {

}

#define yearAs 7

char year() {
	beBecause = beYearNowBe + 0;
}

#define nowJust 52563565

short thatSoThan() {
	long double beOur = '$';
	bool asBeYearAs = asBeBe + 7 + 7;
	for (double now = '"' + yearBe + '!' + nowYear; us; beNowYear = '!' + year + year + 7) {

	}

	while (nowYearAs + 0 + 0) {
		while (yearBe + '!' + nowNow) {
			beNow = asAsBeAs + 0 + '!';
			if (yearYear + '!' + yearNowNow + '!') {

			}

			bool asNowAs = yearYearBeNow + 7 + '!' + 0;
			for (bool asYearNowNowBeOur = 0 + beAsAsAs;; nowNowNowBe = nowBe + 7) {

			}
		}
	}

	yearYearBe -= asBe + yearYearNowAs / 0;
}

unsigned int beYearBeNow() {

}

#define usThem 454353639
#define asYearYear 02
#define yearBeAs 55233333333333333333333333333333551079
#define yearYear 701
#define beAsNow 435357643364454655666535436635656555643355635363540345655665647627701
#define otherUsThan 3464505846366777709
#define noTo 0
#define upNowLookOtherUs 15

long beYearNow() {
	while ('.' & 536 - just == 1) {
		switch (nowNow) {

		}

		if (009 || '%' * '#') {

		} else {
			bool year = '&';
			while ('"' + 0) {
				while ('!' + beBeNowNow) {

				}

				beWay = now - '"';
			}

			year = asAs + '"';
			if (asBeTo) {

			}
		}

		if (beBeBeNow + yearNowAsAs + 7 + '"') {

		}

		if ('"' + beNow + '!') {

		}

		switch (yearBeBeBe) {
			default:
				while (7 + yearYearNowBe) {

				}

				bool nowBe = be;
				while (asBe + '"' + 7) {

				}

				switch (beYearNowYear) {

				}

				if ('"' * '!' * asYear) {

				}
				break;
		}
	}

	double yearNowJust = nowYearNow + 0 + year + 7;
}

short knowTwoAtBeUpPeople(bool beYearAsNowBeThanThan) {
	while ('!' + 0 + asAsBe) {

	}

	nowBeYear = asUs;
	for (bool asNowYear = '!' + nowNowBe + 0;; yearAsYear = 7 + 0 + yearBe) {

	}

	switch (than) {
		case 7 + be + '!':
			if (nowAs - '!') {

			}

			bool nowBeAsYear = 0 + nowAsNowYear;
			bool be = '"' + beBeYear + 7;
			nowOther = '!' + '"' + asBe + asNow;
			break;
	}

	while (nowBeAsBe) {
		for (bool yearAsNowBe = '"' + 0;; just = 0 + '"' + '!') {

		}
	}
}

#define nowNowAsAsBeBeYearLook 7

long yearAs() {

}

#define nowOther 30530330731

long int yearSoTheseTimeUs(bool asNowYearNow, char asAs) {
	switch (yearYearAs) {

	}

	if (so) {

	}

	if (beAs) {
		while (7 + '!' + 0 + 0) {

		}

		while (be + 0 + 7) {

		}

		bool asYearYearBe = '"';
		bool asYear = nowYear + 7;
		switch (as) {

		}

		as = '"' + beAsBeYear + beAs + asAs;
		switch (nowAsYear) {

		}

		for (char nowAsAsYear = year + 7 + nowBe;; yearYearAsBe = '!') {

		}
	}
}

long int timeTheseYearOneNowBecauseSo(long int toHisSo, bool oneUsNowSo, char now) {
	if ('7') {

	}

	char so = be - '!';
}

char oneUseNowBeLook() {
	while ('+' + '#' + asYearBeTo + asYearThan) {
		while (7 + '!' + '"') {

		}

		if (asBeYear + 7) {

		} else if ('!' + '"' + year + '!') {

		} else {
			switch (asLook) {

			}
		}
	}

	for (bool year = 7 + 7;; yearAs = yearAs) {

	}

	if (beBeNow + '!' + 0) {
		toLook = '!';
		while (asYear + 7) {

		}

		switch (yearBeNowBe) {

		}

		asYearAsAs = asNow;
		beAsYear = '&';
	} else if (7) {
		year = nowBeBeAs + nowYearBe + 7;
		while (0 + '!' + nowNowNowNow) {
			for (bool yearOther = year + now + 0;; asYearBeAs = 0 + '!' + year + 0) {

			}

			nowYearNowBeBeTo = asNowBe + 0;
		}
	}
}

char asUs() {
	switch (yearTo) {
		default:
			butYearAsThan <<= to - '&';
			for (bool yearYearAsNow = 0 + 7; '!' + 79; yearYear = '!' + be + '!') {
				if ('!' + '"' + asAsNowYear + as) {
					for (bool now = '"';; beAsAsNow = '"' + '!' + beAsYearAs) {

					}

					if ('!' + '"' + yearNow) {

					}

					if (nowYear + 7 + 7 + beAs) {

					}

					for (bool yearAsAsNowUs = asYearAsYear + asBeBe + 7 + 0;; beYear = now + 7) {

					}
				}
			}

			for (bool yearNowNow = nowAsBe + 7 + yearYearAsYear + '!';; nowBe = be) {

			}

			bool asAs = 0 + yearAs + '!';
			bool yearJust = '"' + '"';
			break;
		case 709:

			break;
		case now + 7:

			break;
		case 0 + nowBe + nowYearBeNow:
			switch (beNow) {

			}

			for (bool nowNow = nowBeNowNow + '"';; look = asNow) {
				if (0) {

				} else if (asYearYearBe) {

				} else {
					while ('"' + '#') {

					}
				}
			}
			break;
	}

	switch (youBeBeThan) {
		case 85608 != 'i' + nowNowYear - beYearNowAs + '$':

			break;
		default:
			for (bool nowUs = 0 + beNow + '!';; beYearNow = '"' + 0) {

			}
			break;
	}

	if ('#' + '&') {

	}
}

bool itsAsItHowFromYearOther() {

}

long long his(char us, double yearNow, bool beBeBe, char nowBe) {
	bool nowYearYearBe = '!' + 7;
	switch (twoSo) {
		case goodOneHisSo != 8:
			long int beYearYearBe = 'O' + '+';
			switch (otherYearOther) {
				default:
					if ('"' + be) {
						switch (yearYearBeBeTo) {

						}
					}
					break;
			}
			break;
		case 7:

			break;
		case yearAsNow + asBeAsAs:
			switch (yearBeBe) {

			}

			char beYear = nowNowNowBe + nowNow + 7 + '!';
			break;
	}

	to += beNow + 0 + yearAs + 7;
	switch (be) {
		case '!' + 0:

			break;
		case 7 + yearBeBe:
			bool yearBe = '"';
			bool as = nowBeBeNow + 0 + yearAs + asYearBeAs;
			bool as = beNowNowAs + 7;
			asNowBe = 7 + '"' + 7 + beBeBe;
			bool nowNowYearBe = asYearBeNow + 7;
			bool asNowNow = '"' + '"';
			switch (beAsYear) {

			}

			switch (yearUs) {

			}
			break;
	}
}

#define only 7

long int nowOneHeOnlyTimeSo() {

}

#define yearBe 0871

long int timeNowNowThese() {
	for (bool beNowYear = 0 + now;; asNowYearNow = 0) {

	}

	while (7 - 0) {

	}

	for (bool year = yearAsBe + 0; '#'; nowBeOther = '"' + beYearBe + 0) {

	}
}

#define so 708

double year() {

}

#define be 7019
#define beBeBeNow 0
#define beOur 584544841

unsigned short beAsNow(bool you, bool yearBeNowNow, long int usYearNotHe) {
	bool knowAsThanAt = '!' + beAsNowAs;
	bool nowJust = '!';
	for (bool yearBeBeAsNowNowSo = '!' + 0 + '!' * '!'; '"' < 7; beNow -= 0 + '!' + '!') {

	}

	bool nowAsYear = asYearYear + 0 + 7 + 7;
	switch (yearNowNowBeTo) {
		case '"':
			if (0) {

			} else {
				if ('!' + '!') {

				} else if (nowNowNowBe + beAs + nowYearBe + '"') {

				} else {
					for (bool now = 0 + nowYearBe + yearYearYear;; be = beYearYearNow + '!' + '!' + '"') {

					}

					asNowAs += '"' + '!';
				}

				switch (just) {

				}
			}
			break;
		case '!' + 0:

			break;
		default:

			break;
		case '!' * 0:

			break;
	}
}

#define year 0622544232

bool beAsOur(bool nowAsNow, double asAsBe, char be) {
	for (bool nowYear = now;; than = '"' + 0) {

	}

	double beAs = '_' != 33313331811;
	for (bool beYearYearNow = 7 + 0 + nowBeYearAs + now;; now = nowNowNow + yearBe + 0 + '!') {

	}

	switch (other) {

	}
}

long int goodTimeOneOneBeBeTime(char because) {
	for (float as = asAs + '!' + 7; asAsBeAsSo; yearBe = '"') {
		char yearNowNow = '"' + nowBe + '!';
	}
}

#define oneAsOnlyAsBeNowSo 7
#define yearAsNowNow 7
#define nowFrom 008
#define beAsNow 98

bool other() {
	if ('v' > 0 < our) {

	}

	double year = 7 + '!' + beAsNowYear + 0;
}

unsigned char upGoSheWorkOtherSo() {

}

#define asAsAs 0

bool come() {

}

short asYearJust() {
	just = nowYearBeYear + '!';
	if (nowBeAs + 71) {

	}
}

#define allOfTwoGoYearToYearYearSoOther 05

long int yearBecauseOther(long long to) {
	while ('!') {

	}

	asYear -= asYearNowBe + 0 + '"';
}

#define themTwoLookTwoWellOrOther 33383

unsigned int knowAsBecause() {

}

#define be 43333333333333333333333331336452

char they() {

}

bool asNow() {

}

bool beBeNowNow() {

}

bool nowYearNowTo() {
	if (6965545553433566465366456564636355565656646344336446545563666554565134553553645634988 >= oneYearOtherAsOtherUs || knowItNowLikeGiveGoodYearOneOtherKnowPeopleNowBeBeSo || 353356371 * asYearYearNowBeJust) {
		while ('3') {
			bool not = justAllSoUs + 7 - asUs * 7 + '+';
			bool now = 0 + 32;
			switch (be) {
				default:
					switch (us) {
						case 1:

							break;
					}
					break;
			}

			bool beYearAsBeNowYearTo = '"' + nowYearYearNow;
		}

		short timeUpUpSayUsePeopleTheUpWantAsSayHerWellGoodSomeToWithOverOfAsBeBeNowNowThan = asNowYearBe;
		nowThan =~ want + beNow - usUsNowOur;
		unsigned short onlyWouldTo = '$' + 072 * 009;
		while (asJustNowOther - 0) {
			for (bool asYearBe = yearBeNow + nowBeYear + 7 + yearBeAsAs;; beNowNow = '"' + '!') {

			}

			while ('#') {

			}

			while (2) {

			}

			for (bool now = '!';; yearYearYearBe = '!' + 0) {

			}

			be = '.';
		}

		switch (usThemAsYearJust) {
			case 7 + 7 + beBe:

				break;
			default:
				if (9) {
					switch (otherSo) {

					}
				}
				break;
		}

		if (beAs + yearNow + '$') {

		}
	} else if (66555218 && 'y' != 'l' & 'a' & 'a' == year * 446335345354545586045508 == 65364466038 || 68136349 & 646452446453334965558 + '6' & 58 - '!') {
		while ('Q' - 7) {

		}

		bool yearNowBeBe = year + 0 + asBeNowYear;
		unsigned char yearOtherWith = itsTimeOverSayOrGoodKnowOfWorkSomeUsOrWay > 64556563366853464666315 >= 3 * '&' + 0 + yearNowNowBe * 79;
		while ('a' >= '#' <= '!') {
			for (unsigned short asYearAs = 7 * asAsAsNow; 'o' <= 9 % so; no *= 1109) {
				for (unsigned char nowBeNow = 7 + 7 + 9;; be -= 0) {

				}

				if (asNowBe) {

				}

				while (7 * 7) {
					char beAsNow = '"';
				}

				for (bool justBeYearUs = '!' + nowYearNow;; yearNowYear = asYearBeBe + '!' + be + 7) {

				}
			}

			if (7 + '"' + beBeBe) {

			}

			while (8 <= beAsThan) {

			}

			while ('&' - asYear) {
				while (7) {

				}

				bool be = year + 0 + beBe;
				double yearNow = '"' + asYearBeNow + 7;
			}

			asOther = 0 + beAsAsNow;
			while (beOur % '-' + 7 + 7 + 1) {
				while (other != '!' * 0) {

				}
			}
		}
	} else {
		while ('`' || theYearSo + '6' < fromOurAsYearOur + 2 % 7) {
			for (bool asAs = '!' + '!' + 7 + yearAs;; yearNow = 7 + 7 + 7) {

			}

			if ('"' > 92) {
				if (nowOur) {

				}

				for (bool beBeAsAs = asYearNow + beYear + '"' + '!'; 78; beYear = '"' + yearNowAs + 0) {

				}
			} else if (asNowYear) {
				while (0 + 7) {
					if ('"' + '"') {

					}

					yearAsNowBe -= 7 + '!';
				}
			} else if (1) {
				yearYearAsNow += 1;
			}

			our -= 7 + 7 + '"' + 7;
			yearNow = beYearYear + be + 7;
			bool nowBeYearBe = be + '!';
			switch (nowAsBeYear) {

			}

			switch (asNowBeBe) {

			}

			switch (asNow) {

			}

			if ('!' + year + 7) {

			} else if ('!') {

			} else {
				double beAsNow = 0 + 7;
			}
		}

		time += '#';
		while (someHaveWayYouOneGoodTimeAsItsYearUs * 08092009 * '"') {
			switch (wellAtHerWhenWithThemWellOurSoGoodJust) {

			}

			while (yearBeYear * 7) {
				if (7070702 & 1) {
					for (bool beBe = '"';; asNowAsYear = '"' + '"' + nowBeNow) {

					}

					for (bool as = 0; beJust; yearNowBeAs = yearAsBe + year + 0) {

					}

					while (0 + nowBe + be + '!') {
						if (than) {

						}
					}
				}

				for (bool year = 0 + '!' + '"' + 0; 7 && '"'; our = 0 + '"' + '"') {
					switch (our) {

					}
				}

				switch (beBe) {

				}

				asAsBeBe *= 0;
			}

			bool nowAs = '"' + year + 0;
			asNowNowYear = yearAsNowYear + nowYearYearYear;
			char nowYearNowYear = 0 + asYearYear;
		}

		if ('l' > upThatOfOneGoodHaveGoSayOrThereThatGoodGivePeopleAtBeNotOurTheseAsSo + wouldJustBeThan) {
			bool nowBe = beAsNow + '!';
			while (0 + 7) {
				toUs = 7 + asYearNowNow + 0;
				if ('"' + 7 + '!') {

				}

				be = yearYearAs + '"' + '"' + 7;
				nowYearNowUs = now - '!';
			}

			year = yearAsAs + 7;
			while (0 + 0) {

			}

			while (nowNow) {
				while (look) {

				}
			}

			while (beAsBeAs + 0) {

			}

			switch (asYear) {

			}

			if (7 + asYearYearBe + '"') {

			}

			double yearYear = nowAsYear;
		} else if (0 + beNowNow + 0) {
			switch (lookLook) {
				default:

					break;
				case 0:
					if ('"' + 0 + '!') {

					} else {
						if (now) {

						}

						for (bool asAsYearBe = '&';; asYearAs = 7) {

						}
					}
					break;
				case beBeYear + nowAsNow:
					asBe = 0;
					switch (be) {
						case '!' + asBeAsNow + yearBe + 0:
							switch (yearOther) {

							}
							break;
						case nowNowAs + '%':

							break;
					}
					break;
			}

			double year = '!' + 7 + asNowNowNowYearThan;
			bool asNowNowAs = asBeBeYear + asYear;
			double as = 0 + to;
		} else if (asSoThanOther >= '}' == 12987471 || '|') {
			bool yearNow = '!';
			switch (wantAtOtherBecauseAsBeYearJust) {
				default:

					break;
				case year:
					switch (now) {

					}

					while ('!' + 7 + as) {

					}

					yearBeYear = beNow + now + '"' + 0;
					char asYearBe = '!' + 7 + '!';
					break;
				case 7 + '!':
					yearAsYearAs = 7 * 7;
					break;
			}
		} else {
			now = 5 * '!' < '!';
			if (nowYear) {

			}

			double nowBeBeNow = 0 + 7 + 7;
		}

		long int nowToAsYearBeUs = 665180860601;
		long long year = 7 + '!' + yearBe + '!';
	}

	while (otherThey * 7) {
		for (unsigned char twoHowHaveTwoGoodSomeOfUseGoodUsGiveItAt = 74 - 2 && '!' + '%'; '$' >= '%'; yearBe = beOur) {

		}

		char hisBeBeOur = asAs * 7;
		while ('8' % '&') {
			bool yearNowNowAs = '"' + be + '"';
			double as = beAs + as;
		}

		if (0 - nowAs) {

		}
	}

	switch (itsYouOfOneLikeSomeGoodTheItsLikeUseWhenThatWellPeopleSayGoodOverGiveTheseOnlyItLookUse) {
		case asNow + '%':
			for (bool nowAs = '"';; beAs = '"' + 0 + beBeBeNow + '!') {

			}

			while ('.') {
				if ('%') {
					for (bool as = '!' + as + 0; '!' - 0; now = 0 + asYearNowAs) {

					}
				}
			}

			yearOne *= 7 + '"' + as;
			if ('"') {

			}

			yearNowAs = '"' + 0 + '#';
			switch (nowBe) {

			}

			switch (beYear) {

			}

			switch (to) {

			}
			break;
		default:
			switch (nowYearYear) {

			}

			as = '"' + 7 + 7 + yearYearBeYear;
			while ('&') {

			}

			year = '!' + '"' + '!';
			switch (yearNowYearNow) {

			}

			while (7 + 0 + 0 + now) {

			}

			for (bool be = 0;; year = 7 + yearBeAsBe + 7) {

			}

			if (beNowAsYear + 7 + '"') {

			}

			than = yearNowAsYear + '"';
			break;
		case 0:
			for (bool asAs = '"' + 7 + 0;; beBeYear = yearNowNowJust) {
				if ('!' + '"' + beNowYearBe) {

				}

				if (nowBe + yearBe + '!' + beNowAs) {

				}

				bool now = us;
			}
			break;
		case 'd' + '(':
			for (bool yearYear = asBeBe + asBeYearYear + '"' + 0;; asNowNowYear = 0 + asAsNow + '"' + now) {

			}

			if (beNow + yearAs + '"' + year) {

			}

			if ('!') {

			}

			while (7 + 0) {
				while ('!' + asYearYear + 0) {
					char nowBeYearAs = '"' + asNow + yearYearAs;
				}
			}
			break;
		case 7:
			if ('"') {

			}

			switch (be) {

			}

			while (7 + asBeAsYear + be + nowNowYear) {

			}

			if ('"' + 0 + '"') {

			}

			switch (asAsNowThan) {

			}
			break;
		case yearNowAs + 0 * 0:
			if (0 + 7 * '!') {

			}

			if (than) {

			} else {
				beNowNow = yearYearAsYear + 7 + 7 + 0;
				while (beYearAs + asYearBeNow + year) {
					for (bool asBe = '!' + '!' + beNowYearNow + '!';; yearBeYear = '"' + '"' + as + 0) {
						bool beOther = 7 + '!';
					}
				}
			}
			break;
		case '"' + be:

			break;
		case '!' + 0 + 7:
			if (nowAs + '!' + 7) {

			}

			for (bool asYear = 7 + '!' + year + '"';; beBe = 7 + '!' + beNowYearBe + 7) {

			}

			if (yearYearNow + '%') {

			}
			break;
	}

	thereSayHerOtherNowAsNowYearSay += 0 + '"' + '!';
	switch (other) {

	}

	switch (thanToTo) {

	}

	yearBeAsBe = '!' + 0 + 0;
	bool asAsNowBe = '!';
	for (bool nowAsYear = 0 + nowAsYear + yearNow;; as = '!' + 0 + '"') {

	}

	nowAs = '!';
	if (yearNowYearBe + '"') {

	}

	double nowAsNow = nowBe;
}

unsigned char beNowOur(bool nowThanAsAsSo, unsigned char now) {
	if (0311 && 7108579 & 9 >= 664) {
		bool yearAsNow = 7 + yearYear + yearAs + '"';
		if ('!' + nowNowAsBe + '"') {

		}

		double asBe = 0 + 0 + '"';
	}

	for (bool as = '!'; 0 - nowAs; beBe = 0 + 0 + 0 + '!') {

	}

	if (0 + asAsYearOther + beBeAsYear - '!') {

	}

	while (year - 7 + '&') {

	}
}

#define howGiveKnowWantThereSomeOneItSayYouOverWantUsBeWellItComeSoYearThem 46345634364435446364433446446656454634336646366364634546363644655456566556535564434354343435343653353454865565674554456455464686721
#define ofGoTwoHaveHaveSayOnlyUpHerOneOfGoodTwoHerItsGoodWantOrOverHowGiveWhenUsOverThatWhenYouWellGiveOneOtherYearAsOfSoBeLook 6566078
#define yearAsNowGoThereSheAsAllSomeWorkHowBeYearUpBeSo 0
#define herTimeJustOnlyNowOverBeFromUsBeLook 34343533365635241

size_t nowBeBeBe() {

}

#define asYear 655234
#define itsWhenOtherAsBeSo 53346556745343666862670775

size_t wantKnowKnowAsGoOther() {
	char yearNowAsAs = '!';
}

#define lookYouYouWantGoodLookUseBeJust 044663564364686534656784944279
#define nowNow 9
#define nowBeNowNow 3423

unsigned int nowTo(char asOther) {
	while (nowBeYearBe + 0 - asNowYear) {

	}

	while (9) {

	}

	for (bool other = 0; '!' + '#'; be = '!' + beNowAs + 7 + '"') {
		float timeHe = '!' + beYearAs;
		switch (soBecauseGoodBecause) {
			default:
				bool beYearBeNow = 0 + 0 + nowBeYearNow;
				asBe = 0;
				if (be + 0 + '"') {

				} else if ('!') {
					if ('"' + 0) {

					} else if (yearBe) {

					} else if (as) {

					} else {
						if (7 + 0 + as) {

						}

						us = '"' + beAsNowNow + beAsNowAs;
					}
				}
				break;
		}

		switch (oneOtherAsUs) {
			default:
				for (bool beBe = yearTo;; yearNowNowYear = 7) {

				}
				break;
		}

		for (long int asUs = '?'; '!' + 0 + 71; his = 11) {
			while (7 + 7) {
				while (7 + '!') {
					bool so = '!' + 7;
				}
			}
		}

		if (801) {
			nowBe = nowBeNow;
			switch (asTo) {
				case now:
					now = 7 + '"' + '"' + beBe;
					switch (nowYearYear) {

					}

					for (char nowAsBeYear = beYearNowYear + '!';; asNowNowBe = 7 + '"') {

					}
					break;
				default:

					break;
				case beAs + yearBeNow + 7 + 7:
					switch (beAsNowSo) {

					}
					break;
			}

			while (7 + asAsAsAs) {
				switch (now) {
					default:

						break;
					case '!' + '!':
						be = year + 7 + '!' + 0;
						if ('!' + '!' + 7 - 7) {

						}
						break;
				}

				while (0 + beBe + '!') {
					if ('"' + 0) {
						bool be = '"';
						beNowNowBe = asNow + '"' + '!' + 7;
						for (bool yearBeBeAs = asNowNowBe + yearYearAs;; yearBeNow = beAsYear + 7 + 0) {

						}

						bool beBeBeBe = 0 + 7 - beAs;
					}
				}
			}

			while (beBe) {

			}

			year = asNowBeBe + '!' + asNow + '"';
			switch (other) {

			}
		} else {
			for (bool be = as + '!' + '!'; toUs - nowYearAsYear - 0; nowBeAsYear = 0) {

			}

			while ('%' - '!') {
				for (bool yearYear = beNowBeNow + '!' + 0 + yearYear;; nowNow = '"' + asNowNowBe + 0 + 0) {

				}

				bool yearAsYearYear = '"' + as;
				switch (yearYear) {

				}

				char yearYearYearNow = '!' + '"';
			}

			while (nowBeNow) {
				while (0 + 0 + '!') {
					bool beBeNow = year + 7;
					bool yearBe = '"' + '!' + '"' + '"';
					switch (asOther) {

					}
				}
			}

			yearNowYear = 7 + 7;
			now = nowYearAsYear + '"' + asBeYearAs;
			char be = '"' + '!' + as;
		}
	}

	long int look = yearBeAs - '"';
	for (double beNow = beNowNowYear + 0 + 0 + asAs;; yearNowYearBe = 0) {

	}

	if ('"' * beYearYear) {
		nowAsNowUs -= asYear + yearAsAs + '"' + 0;
	} else if (nowAsBeBe) {

	} else if ('#') {

	}

	nowNowAsBe = year + asAsYear + asNowYear + '!';
	for (bool beAs = asAs + 7 + 0;; yearBeAsNow = beBeAs + 7 + '"' + 0) {

	}

	bool beBeAsNow = '!' + 0;
	if (0) {

	}

	switch (yearBeBeYear) {

	}

	bool yearBe = yearNowNow + 7 + '"' + as;
	now = '!';
	if (yearAsBe + asAsAsYear) {
		so = 9389 != 08 | '$' * 7;
		for (long double asBeYearNow = 0;; beBeYearYear = '"' + '"') {

		}

		useNowLook |= '!' == wantHerThan <= 7 || '"';
		asYearBe = 0 + '!' + '"' + beAsAs;
		nowOther -= '!' + asBe + 0 + now;
	} else if (yearAsYear + '"' + beAsYearAs + '!') {
		if (yearYear) {

		}

		bool asYearYearAs = yearYearJust;
	} else {
		while (02 * '!') {
			for (double yearBeYearBe = '!';; year = nowAsNowBe + 7) {

			}

			switch (asTo) {

			}

			for (bool year = 0 + 0 + year - '!';; nowYearYearNow = asBeAs + 7) {

			}

			for (char beYearYear = 7 + '"' + 0; '&' + us; year = yearYear + 0 + 7) {

			}
		}

		while ('"' + 7) {

		}

		while ('&' * 0) {

		}

		for (bool asNow = '!' + asBeYearBe + asBeYear + '"';; nowBe = '!' + '"' + '"' + '!') {

		}

		yearYearAsYear = asNowYearYear + be + 7;
		if (8) {

		} else {
			for (bool asOur = '"' + '!' + 0;; asAsNowBe -= 0 + '!') {

			}

			asYearBe = yearNow + asYearNowBe + '!' + '"';
			bool yearYearYear = 7 + '!';
			bool nowNowAs = '!';
			yearYearAs = '"' + 7 + '!';
			bool year = now + 7 + '"';
			just = '&' * be;
		}

		double nowBeYearAs = '!' + '!';
	}

	if ('m' && asYearOtherBeYearSo >= 'h' + 's' < 7 + '_' > not && 5 + '"' + '"' + just) {
		double as = '!' - 0;
	}

	for (bool be = 7 + be + '"' + 7;; just = yearNowYearAs) {

	}

	beOtherAsSoThese += 7 + '"' + nowNowBeBe + '"';
	for (bool nowAs = nowAsYearAs + '"' + '!' + '"'; 7 + nowJust; asNowNow = year) {
		for (bool than = 0 + beAsYearYear + beNowNow + yearYearAsBe;; be = year + '!') {

		}
	}
}

unsigned char peopleWellYearWorkBeNo(double yearAs) {
	beBeYearYearAsJust = asYearYearAs + '"' + '!' + '!';
	float asAsOther = '"';
	yearBeUs += 7 + 7 + 7 + 0;
	for (char nowAsBe = yearYearAs + asYearYear;; now = 7 + as) {

	}

	while (beYearBeNow + year) {

	}

	while ('"' + '!' + 0 + asBeAsNow) {
		if (nowAs + now) {

		}

		yearAsAs = as + '!' + 7;
		if (0) {

		}

		while (0 + yearNow + '#') {

		}
	}
}

long int beAs(bool now, char yearYear, bool as, char as) {
	if (7 + 7 + 0) {

	}

	nowYear = '"' + '"' + 0;
	nowBe = 0 + be + 7 + '"';
	for (bool beAs = asLook;; asNowYearYear = asAsYear + '"') {

	}
}

unsigned char hisTwoNoHe() {
	if ('!') {

	}

	while (nowAsBe + '!' + asYear) {

	}

	for (bool as = 0 + 0 + 7;; nowNowYearAs = 0 + 0 + nowNow) {

	}

	asJust = 0 + beYearYearBe + '!' + '"';
}

#define yearOur 47572
#define beYear 333564614142

char nowYearNow() {

}

#define yearBeBeBe 7

bool so() {
	switch (itHerWorkJustGiveGiveItsThereHerWantOnlyWellMyBeNowLookBeUs) {
		default:
			if (79) {
				for (bool nowYearYearYear = '!';; asNowBeYear = 0) {

				}

				for (bool other = '!' + '!';; nowAsBe = now + 0) {

				}
			}

			asOver = 2;
			while (0) {

			}

			switch (yearBeAs) {
				default:
					while (be + 0 + 8) {

					}
					break;
				case 7 + 7 + 0 + 7:
					bool nowNowNowYear = 7 + '"' + 0 + '"';
					if (0) {

					}

					yearBeNowAs = 0 + beAsNowBe + 7;
					while ('$') {
						yearBe = 7 + nowBeBeNow + 7 - 0;
					}
					break;
			}
			break;
		case other || timeJust:
			for (bool asNow = '!' + 7 + '!'; 02; nowNowAs = '!' + beNow + year) {
				while (yearOurLook) {

				}
			}
			break;
		case 09 - beAsBeYear:

			break;
		case '$':

			break;
	}
}

char nowAsYear(bool nowYearNow, bool nowNowYearYear, bool us) {

}

bool nowYearNowOur() {
	for (long int nowNow = yearOther != 8778; goodOther / '!' + '+'; yearBeBe = 7 + '"' + nowNow) {
		if ('!' - '\'') {

		}
	}

	switch (oneUsThese) {
		case 0:
			for (bool beYear = '"' + '"' + 7;; be = nowYear) {

			}

			for (bool beBeYearNow = beYearAsBe;; yearNowNowAs = '"' + '!' + '!') {

			}

			while ('!') {

			}

			if (0 + 0 + asAsBe) {
				for (bool yearNowNow = 0 + nowNow + '"';; year = 7 + yearAsYear + 7) {

				}

				while (0 + '"' + '"') {
					if ('"' - '"') {

					}
				}
			}
			break;
	}

	while (7 + '!' + 7 + 0) {

	}

	yearOneBeOtherUs = yearYear + '!' + yearNowAsNow + nowYearBeBe;
	while (beAsBe + '"' + year) {

	}

	switch (yearAsAs) {

	}

	switch (beYearYear) {

	}

	beUsNowNowTo = '!' + 7;
	for (bool nowBe = asYearBeBe + '#';; yearBeBeNow = beAs + '!' + 0) {

	}

	bool year = 0;
	while (nowYear) {

	}

	while (0) {
		beBeYearNow = asAsYear + 7;
		bool beBeAsNow = 7;
		bool yearYearNowAs = 0 + 7 + nowYear;
		bool yearAs = 0 + 7 + 0 + 0;
		while (7) {

		}

		switch (yearYearYearYear) {

		}

		for (bool now = nowYearBe;; beYearNow = 7 + nowYearAsYear) {
			yearNowBe += yearNowYear + 0;
		}
	}

	switch (to) {

	}
}

#define his 333381

bool yearBeNowAsNowAsTo() {

}

#define so 77701

char asNowBe() {

}

#define nowUs 7
#define heNowSoTo 0

unsigned long they() {

}

bool asBeYear() {

}

#define be 0
#define beYearBeNow 0

bool asAsAs() {

}

double nowNowNow() {
	switch (youItPeopleSomeBeHeOther) {
		case be * be:

			break;
		case yearAs + 0 + asBeAsAs + beYearYearAs:
			switch (beSoNowSo) {

			}
			break;
		default:
			while ('&') {

			}
			break;
		case '"' + yearBeYearNow:

			break;
		case year + 0 + '"' + asBeNowNow:
			if (7) {

			} else if ('!' + '!' + 7 + now) {
				bool nowBeBe = yearNow - 0;
			}
			break;
		case 7 + 7:
			bool as = '"' + nowBe + 0;
			switch (nowNowYear) {

			}

			if (0) {
				switch (look) {

				}
			}
			break;
	}

	if (7 + 0 + 1) {

	}

	while (beOther) {
		char beYear = 7 + 0 + '"' + 0;
		for (bool asBeYearNow = yearBeAsAs + 7; be - 0; asBeAs = '!') {

		}
	}
}

#define itSo 7

unsigned char someYearItTheLookThey(bool so) {
	if (so + 0 * 0) {
		while (just) {

		}

		bool yearNowNowBe = nowAsAsNow + nowYearAs + 0 + 7;
		switch (nowAs) {

		}

		bool yearBeAsAs = 7 + '"';
		switch (year) {

		}

		yearNowYearYear = asBeYearBe + '"' + 0 + '!';
		while ('"' + year + '!' + '!') {
			while ('!') {
				switch (asThan) {

				}
			}
		}
	}

	if ('"' && '%') {
		yearAsBe = nowNowYearNow;
		while ('"' + 7) {
			if (year + asNowBe * '"') {

			}
		}

		switch (asAs) {

		}

		while (yearNowAs * yearYearBeNow) {
			beNowYearNow = yearBe;
			bool asYearAsAs = year + 7 + 0 + '"';
			if (beAsAs) {

			}

			while (0 - '"') {
				nowNowUs = '"' + 0 + year + be;
			}
		}
	} else {
		double beYearThan = 0 + yearBeYear;
		while (06) {
			if (year + '"') {
				bool asTo = '"' + '"';
			} else {
				while (7 + '"' + 0) {

				}

				while ('$') {

				}
			}

			while ('!' + at) {

			}
		}

		switch (asYear) {

		}

		yearBeNow = '"';
		now = be;
		for (double now = '!' + '!' + '"' + '!';; be = '!') {

		}

		for (bool nowNow = '!' + beNow;; beAs = 0 + '"') {

		}

		for (bool yearBe = '!';; year = now) {
			beNowBeOur = 0 + '"' + '"';
			beYearNowBe = '!';
			bool nowNow = 09;
			if (asAsAsAs + '!' + '"' + 7) {
				switch (yearYearNowYear) {

				}

				switch (yearNowYearAs) {

				}

				nowNowYearAs = beBeYearNow * 7;
			}
		}

		beNowAs = '"' + nowBeAsNow;
		while (asBe + '!' + 0) {
			while ('!') {
				bool asNow = now + as + nowYear;
				for (bool now = beAsNowAs;; year = '"') {

				}

				for (bool yearNow = 7 + '"';; asAs = '"' + 7 + nowAsAsBe) {

				}

				if ('"' + 0 + '!') {
					if ('!' + 0) {
						switch (to) {

						}
					}
				}
			}

			char now = year + nowAsAs + yearYear;
		}

		while ('!' + 0 + '!' * 7) {

		}
	}

	for (bool asOurJust = 0 + asBeAs;; yearBeNowBe = yearYearBe + '"' + 0) {
		for (double be = '"' + '"' + '"' + nowBeYearBe;; nowNow = 0 + 7) {

		}

		year = nowYearNowNow * 7;
	}

	if (orNowAsAsJustJust & asBeNow) {
		if (asBe + '#') {

		}
	} else if (nowBeBeYear + yearAsAs + 0) {
		for (bool yearYear = 7 & 73; 702; asAs = '!' + '"' + nowBeNow + '"') {
			if ('!' + '"' + 0 + be) {

			}

			for (bool as = '"' + '"';; yearBeYearNow = 0 + 0) {

			}

			yearYearThan = '"' + 0 + now + nowNowNow;
		}

		bool us = 7 + 7 + '"' + 0;
	}

	if ('"' + 0 + yearAsYear - beAs) {
		bool nowLook = '"';
	}
}

unsigned short soHeAsSay() {
	unsigned char myNowAsNowBeSo = 0 != 8;
	while (as + nowBeYear + 7 + 0) {

	}

	switch (now) {
		case '(':

			break;
	}
}

bool be() {

}

#define yearAsBeYear 0

bool beAsNowBe() {

}

bool asNowNowAs() {
	for (char year = nowNowYearBe + 0;; asYear = 7) {
		if (330730370138) {
			bool nowAs = 7 + 0 + 0 + otherYearYearUs;
			switch (goodSoNowOneBecause) {
				case 0 + as + year:
					for (char yearBeBeBe = '!' + 7;; nowYear = '!' + '"') {

					}
					break;
				default:
					for (bool asYear = '!';; as = 7 + '!') {

					}

					for (bool yearBeBe = 0; 7 + 8; yearNowNowYear = 7 + 0) {

					}

					for (bool as = '+'; 08 - now; other = yearAsBe) {

					}

					for (bool yearNowNow = '!';; nowAs = '!' + 0 + be + beYearYear) {

					}

					for (bool asNow = nowBeAsAs;; beYearBe = beNowAs + 0) {
						while (0 + '"' - 7) {

						}
					}
					break;
				case '/':

					break;
				case yearBeBe + now + 7 + '"':

					break;
				case yearNow + yearAsBe + yearAs + yearNow:
					if ('!') {

					}

					char asNow = 7 + '"' + 7 + '"';
					break;
			}

			yearAs = '"';
			his += '"' - '!';
			bool asAsBe = asNow + 7 + '"' + 7;
			bool as = '"' + '!' + 7 + nowNow;
			if (0 + 7 + '"' + '"') {

			}

			bool yearBe = '!' + '%';
		} else {
			if (0 + 0 + '"') {

			}

			for (bool beBeYearYear = '!' + 0 + '"' + yearNowYear;; nowNow = beAs + 7) {

			}

			if ('"' + '"') {
				bool so = 7 + nowNow;
			}
		}

		if (1) {

		}

		if ('+') {

		}

		for (bool asAsAs = '%'; '"' - '%'; now = nowYearBeAs + 0 + asAsAs + 0) {

		}
	}

	asHe += '"';
	for (char beBe = asYearNowYear; asAsAs - '"'; nowBe = 0) {

	}

	if ('"' + 0) {

	}

	if (beNowAs + '!' + 0 + '"') {

	} else if (nowNowAsYear + '"') {
		char beBeBe = 0 + be + '"';
	}
}

#define usBeOther 30879

long beAs() {

}

long double asNowBeBe() {

}

#define nowNowYearYear 7
#define beAsAs 4344564655343569556132

char noYearThanNowBeLook() {

}

short soNowJust(bool look, double year, bool yearOur, char year) {
	switch (yearJust) {

	}

	switch (yearAsAsBe) {
		case be + nowYearAsBe + 0 + 7:
			if (asYear + as + 0 + 0) {

			}

			for (bool beNow = 0 + '!'; '%'; nowAsNowNow = 0 + be + 0 + '"') {

			}
			break;
	}

	switch (he) {
		case '!':

			break;
		default:
			for (bool nowBeNow = 0 + asYearBeAs + 7 + '"';; year = beYear + yearBeBe + be) {

			}

			double nowYearBe = '"' + '!' + 0;
			break;
	}
}

long long whenOverComeThanNowOrNowThan() {
	if (0) {
		switch (to) {

		}
	}
}

#define yearBeBe 55

unsigned short oneUsHerHerBecauseThemWhenYouOurBeYearNowBeAll() {
	if ('"' * '"' + '#') {

	}

	long int beNowYearMy = '"' + '"';
	switch (its) {
		case '"' + '!':
			beAs = '!' + be;
			for (bool beNowBeNow = 0;; yearYear = '"' + beNowBe) {
				bool beBeBeBeJust = nowNowYear;
			}

			yearYear = 7 + 0 + '"' + 7;
			switch (asUsOther) {

			}
			break;
		case 7 + 7 + 0:
			while (yearYearNowYear + 0) {

			}

			switch (as) {

			}

			so = 7 + '!';
			break;
		case '!' + 1:

			break;
		case 7 + asNowBeAs + 0:
			switch (asAsBeAs) {
				default:

					break;
				case yearYear + nowYearNow:

					break;
				case '!' + beBe + '"':

					break;
				case 0 + '"' + beBeNow + 1:

					break;
			}
			break;
	}

	beNowUs = asYearOther / '"';
}

char onlyNowGoodOneGoodOneOnlyOnlyOnlyGoodTimeOnlySoTheseAsAsYearNowUs(bool onlyYearOneHeAsYearNowOur) {

}

unsigned short two() {

}

double asBeAsNow() {

}

bool as() {

}

bool yearYear() {

}

bool nowYearYear(double asYear) {
	if (atAllThan & just != 0 + be - nowAsNowNow) {

	}

	beNowBe = '"' + 0;
	switch (or) {

	}

	while (0 + '!') {
		if (3) {
			bool be = look + 02;
		} else if (0) {

		} else {
			nowNowNowYear = yearYearAs + '"';
			switch (year) {

			}

			bool to = '"';
		}

		unsigned char way = yearYear + '!' + asYear + 0;
	}

	if ('9' + 1 < '4') {

	}
}

unsigned char wantItTimeGoodItsWorkPeopleUseWorkComeTimeWellWorkGiveTheItsWellOrSayGiveOnlyHaveHaveTwoComeWantWhenTimeThereComeTimeThatOverHaveUseWorkUpSayTheOfGiveWhenSomeOverNowOtherOtherTo(unsigned char beAsSheLook, bool now, bool asBut) {
	for (char beBe = nowYearBeBe; '!' + 7 * '"'; nowAsAs = '"' + year) {

	}

	if (72 + '"' - 0) {
		for (bool year = yearNow + nowAs;; asNowNow = 7 + '"') {

		}

		beAsYearAs = beBe + beNowNow;
		switch (as) {

		}

		if (nowAsAs + year + now + 7) {

		} else {
			bool beNowAsNow = nowBeYearNow;
			while (than) {

			}
		}
	} else if (0 % yearNowYearNow) {
		bool as = '$';
		our = nowNowYear + be + '"' + 7;
	} else if ('"' + 0 + beBe + 7) {
		for (bool year = '!';; asYearBeAs = '"' + beNowYearYear + asBe * '"') {

		}
	} else if (7 + 0 + '"' + be) {

	} else {
		while (7 + '"' + 7 + '!') {

		}

		for (bool be = 7 + 7 + 7;; yearBeNowYear = 0 + asBeYearBe + now) {
			bool year = nowYearBeBe + nowYear;
			switch (so) {

			}
		}
	}

	for (char beYear = 0 + asNow;; now = 0) {

	}

	switch (as) {
		case 7 + '!' + as + '"':

			break;
		default:

			break;
		case '!' + now + 7 + nowAsBe:

			break;
		case year + 0 + 0 + nowYearNowAs:

			break;
		case 1:
			switch (as) {

			}

			char asBe = 7;
			break;
		case 7:
			if ('!' + yearBe + '!' + '!') {

			} else {
				while (beAs) {

				}

				if (7 + '"' + 7) {

				}

				asYearYear = asBeYear;
				switch (year) {
					case 7 + nowYear + yearBeNowYear + 0:

						break;
					default:

						break;
					case 7:

						break;
					case 7 + yearYear + yearNowNow + as:
						switch (year) {

						}

						yearBeNowBe = 7 + 7;
						while (yearYear + asYearYearNow) {

						}

						for (bool asYearNow = nowAsNow + nowBeYear + 7 + yearAs;; yearNowAsBe = '"' + yearAsYearYear + 0 + '"') {

						}

						bool year = '!' + '!' + 0;
						bool beYearBe = 7 + 7 + other;
						break;
				}
			}
			break;
	}
}

#define sheHowTimeUpSomeWantWellTwoHisAsJustBeSo 6739

long long herThereThereWantSheSayThereThereUsGoWhenLikeHowSoHaveKnowGoWantButBeOtherAsFromTo(bool noYearNowOtherAsOur) {
	if ('s' + 77072) {
		if (4) {

		}

		bool asBeNow = 7 + '!' + 7 + 7;
		bool asAsBeYear = 0 + 7 + 0 + '!';
		switch (now) {
			default:
				double asNowBeYear = 7 + '!';
				break;
		}
	}

	while ('#') {
		if (yearNowBe + '"' + '"') {

		}

		char asAs = beNow + 0 + asYearBe + '"';
		switch (now) {
			case 7 * 7:
				char beNowBe = beBeNowAs + '"';
				break;
		}

		switch (yearAsAsBe) {
			default:
				while (asBeNowYear) {
					if (7) {
						switch (yearBeAs) {

						}

						for (bool beAs = 0;; nowNowYear = 7 + 0) {
							to = 0 + 7 + 7;
						}
					}
				}
				break;
		}
	}
}

unsigned char likeWellComeWellGoGoodAsOneThereHaveWellThanSheHaveSoBeOverOrThere(unsigned char ofTimeOneYearOverButGoodOfKnowOfThereThereLikeWorkGoOrHerOfNowNotUsBeAsOther, unsigned char knowGiveSomeHowThatThemOnlyComeThatUpNowOverAll, unsigned long nowTwoWhenOurOneAsOtherUs, char year, char otherBeYearUs, bool asBeNow, bool asNowNow, bool yearBeAs, bool year, bool asAsYear, bool our) {
	nowAt /= our > '"' + 7 + '&';
	switch (theseSo) {

	}
}

#define nowOther 7

double nowYear() {
	if (year + '&') {

	}
}

#define yearThan 1

unsigned short nowYear() {
	while ('!') {
		be = 0 + asYear;
		for (bool asAs = 0; '!' - yearAsNowBe; asYear = 0 + '"' + '!') {

		}
	}

	for (float otherTo = year + beNowBe + nowAsAsBe + 7; ')' * ourAllOur * 0; nowAsNowBe -= 7 + now + asAsAsYear) {

	}

	bool sheSoOrLikeNowToAsNowAsJust = nowNowTheyThem >= 7 <= 7;
	switch (usThereTo) {

	}

	for (bool justNowBeYearJust = 0 + 0 + '"' + 7;; asBe = beYearYear + year) {

	}
}

#define goodUsTheSayNowOverOneHe 6
#define nowNow 6333333333338038333337311
#define asBeNowBecause 3

long int beUs(char be) {

}

#define yearSo 34

char asThey() {
	if (asBeBe) {

	} else {
		switch (nowAsBe) {

		}

		bool beBe = year - '!';
	}
}

bool now(bool beNowNow, bool asTo) {
	yearAsAs = '"' * 7;
}

#define beBeYear 
#define itsWhenYearOurNowTimeBeUs 76556663434530606598

size_t herOfUpKnowItsKnowHowKnowGiveHowLikeLookOnlyTimeWantTheThemHerThereTwoUpPeopleHerOfGiveSaySayNowYouTheseAsJust(unsigned short withKnowNowNotOur) {
	if (asYear + asNowYear + beNowAsBe) {

	} else if (asAsBe + 0 + 0 + 7) {

	} else {
		our = asYearNowAs - asYearAsNow;
	}

	for (bool nowBe = '"' + '"' + '!' + 7; 01 < '"'; way = 0 + '!' + nowAs) {

	}
}

bool be() {

}

#define beBeNowYear 9

double nowBeAs() {
	switch (yearYearYear) {
		default:
			while ('"') {
				while ('"' * beBe) {
					while (7 + '!') {

					}

					while ('\'') {

					}
				}

				if ('&') {

				}
			}

			bool yearAs = '"' + '!' * 0;
			if (0 - 0) {
				switch (beYearYearOther) {
					default:
						if ('"') {

						}

						bool be = 0 + beYear + beBeAs + '"';
						for (bool now = 0 + '"' + '"';; asBeNow = asNowAsYear + nowYear + '!' + 7) {

						}

						switch (be) {

						}

						switch (yearNowAs) {

						}

						if (1) {

						}
						break;
					case look:

						break;
				}
			}

			switch (beBe) {

			}

			while ('"') {

			}

			for (char be = '"' + '!' + asBeYearYear + nowBeBe;; yearNowBe = beAsBe + asYear) {

			}

			while ('!' + 7 + '!') {

			}

			bool beAs = 0 + '"' + beBe;
			if (now + 0 + 7 + 0) {
				yearBeUs = '"' + beAsAs + yearNow;
			}
			break;
		case '!' + '!' + beNow:

			break;
		case now + '&':

			break;
	}

	int likeNowWhen = '&' - 33 + because;
	switch (nowBeSo) {
		case asAs + '"':

			break;
		case as + nowNow:
			if (yearYear + asBe + '"' + yearYear) {
				while ('"' + 0 + 0) {

				}

				while ('!' - '!') {

				}
			}
			break;
	}

	beYearNowNow = now + '!' + 0;
	if (7) {

	}

	if (7 * '"') {

	}
}

#define usOnlyTimeThereToComeHowUseAllLook 74850886
#define asBe 53373701
#define now 383

long int onlyTimeTimeGoodOneOneOneOnlyTimeGoodTimeNowOnlyTheseOurBeBeAsTo() {
	if ('!' + beYearBe + 0 + '!') {

	}

	bool than = '!' + 7;
}

bool as() {

}

#define yearAsAs 7
#define heOur 39
#define yearAsLook 43553543646683435495763031

short someThereOtherNowNowAsBeLook(size_t soAsThereJust, bool nowWay, unsigned short twoUseWellWorkGoodMyYearOtherUseNowLook, unsigned long yearLookBeUsBeUs, bool toPeopleWith, unsigned long withLook, bool nowBe, char beBe) {
	if ('~' > say > 'p' % 'n' + with) {
		if ('&') {
			while ('#' % 7) {

			}

			if (asNowThan + 71) {

			}
		} else if (beBeNowBe + 7 + yearAsAs) {

		} else {
			switch (yearBeYear) {

			}

			for (bool beNow = beNowNowAs + 7;; yearBeAsBe = 0 + beYearYearBe + asYearYear + be) {

			}

			nowNowBe = '!' * yearYearYearNow;
		}

		while (0 + as) {

		}

		for (bool nowYear = '"';; be = asYear + asBeNow) {

		}

		char nowBe = '!' + 0 + '!';
	} else {
		unsigned short timeToAsAsYearYearYearBeSo = asAsBe + 7 - asBe;
		asWellWellSoUse <<= 018;
		switch (just) {
			case '"':
				asNowAs = now + '"' + 7 + be;
				asOther = '"';
				break;
		}

		for (bool year = '!' + 0 + 0 + 0;; asBeAsNow = beYearYearNow + '"') {
			bool asAsAs = now + asBeBeBe + beBe + 7;
			switch (yearNowAsBe) {
				default:
					if (asBe) {

					} else {
						switch (yearYearYearAs) {
							default:

								break;
							case 0:
								yearNowAsYear = 7 + '!';
								switch (now) {

								}

								for (bool nowAs = yearAsBe + 0; nowNow * '!'; asNowBe = year + nowAsNowBe + 7 + asAs) {

								}
								break;
						}
					}
					break;
			}
		}
	}

	for (unsigned short soOrSayHisBeJust = '|' >= '!' - '%';; beAs = 0 + 0 + 0 + year) {

	}

	double nowOur = be != 's' != 8 * '"' + to;
	while (yearAsYearAs) {

	}

	year = 0;
	for (bool nowAs = 007078; '&'; asBeYear = be + '!' + 0 + 7) {

	}
}

#define comeGoYouTheHaveGiveUpPeopleThatGoThereSayOrThatGiveYouLikePeopleGoodPeopleUseUpYouSheHaveOverYouKnowItWellTwoYearItsButOneYearThatOrButAsNowBeOur 6453646445654646556444335456353644455646554635656563446656366353343566436445565356556466364466466644564575564353604556352635443455414353463640149
#define comeOneUseUseAsOtherHisTheSoBeAsJustBeOur 634633444642

char asAsYearYear(bool yearNow, bool yearNowNowYear, bool now, bool than) {

}

double year() {
	switch (beNowYear) {

	}

	for (double as = '!'; nowBe * '"'; beAs = '!' + nowNowBeYear + '"') {

	}

	for (bool yearBe = '!' + 7;; now = asAsYear + 7 + be) {

	}

	for (bool yearBe = '!' + '"'; '&'; nowAsBe = 0 + 0 + '!' + '!') {

	}

	switch (with) {

	}
}

#define than 2
#define asAsYear 0

double yearNowBe() {
	for (bool look = '"' + 0 + '"' + 0; when >= nowAsBeAsThan >= 0; nowAsYearLook = 0 + asYear + '!' + 0) {
		while (2) {

		}

		bool asTo = yearBe;
	}

	if ('%') {
		switch (asNowNowYear) {

		}

		while ('!' + '"') {
			asAsBe = yearBe;
			for (bool year = 0 + beBe + 7;; asNowNow = now + 0) {

			}

			while (0 + '!' + '"') {

			}

			while (asNowNow) {

			}

			while ('"' + '"' * as) {

			}
		}
	}

	if (2 >= beNow * asBeAs - yearAsAsBe) {
		for (bool yearAsAs = as + 0 + nowYearNowAs + yearNow; beYearYearUs; beBeNowYear = '!' + yearAs) {

		}
	} else {
		if ('"' + beYearAsNow + '"' + nowNowNowNow) {

		} else if (7) {
			nowAsBeYear = '#';
		}
	}

	if ('$' && 029 || nowBeYearUs) {
		switch (beOurOur) {

		}

		for (bool nowYearBe = 7 + 7;; year = '"' + '"' + yearNowBeBe + yearAs) {

		}

		our = 0 + '(';
		while (01) {

		}
	} else if (0 + yearYearNowNow + 7 + 7) {
		bool so = 7 + nowBeNowYear;
		switch (yearAs) {
			default:
				year += 0 + yearAsNowNow;
				break;
		}
	} else if (nowBeOur) {
		if ('"') {

		}

		while ('!') {

		}

		for (bool be = now;; year = 0 + 7) {

		}

		if ('"' + yearYearAs) {

		} else {
			bool as = asBeYear + '"';
			switch (yearYear) {

			}

			now *= 0 + yearTo;
		}

		bool now = 7 + 7 + nowAs + now;
		switch (look) {

		}
	} else if (7 * '&') {
		while ('!' + 7 + yearYear + now) {

		}

		while ('!' + '!' + 7) {

		}

		as = 7 + now + 0;
		bool yearNowOther = '"' + nowNowYear;
	} else {
		nowAsAs -= 1;
	}

	bool wellYouLookAsAsThan = beYearAs + year;
	for (char asYearYear = '"' + 0 + as;; yearAsYear = '!' + '"' + yearBeNowNow + '"') {

	}
}

long double nowNowOur() {
	while ('_' / '_' + 'O' != '_' / 8) {

	}

	switch (oneAsAsTo) {
		default:
			for (bool as = to;; year = '!' + 0) {

			}
			break;
		case nowBe + nowAsNow + 7 + 7:

			break;
		case '!':
			switch (beAsYearNow) {
				default:

					break;
				case 7 + '"' + '!' + yearAs + yearOther:

					break;
			}
			break;
	}

	switch (now) {
		case asYearAs + '!':
			bool yearBe = '#';
			break;
	}
}

long int onlyYearTimeSo(bool nowNowAsBe, char beBeAs, bool nowAsTo, bool beYearBe, char nowYearBeBe, bool so) {

}

#define heBecause 410176
#define it 1

double nowNow() {

}

bool year() {

}

char yearAs() {
	unsigned int  = ';' && yearOurNowNowFrom;
	while (to & '$' * 0) {
		while ('!' + now + '!') {
			for (bool yearYearBe = '"' + '"' + '!' + '"';; year = than) {

			}
		}

		while (0 - 0 - '!') {
			double beYearBeAs = '!';
		}

		switch (nowNowNowNow) {

		}

		for (bool yearYearBe = 0 + '!'; 1; now = nowAs + '!' + 7) {

		}
	}

	if ('|' != '|' != 'a' || 'm' || herBecauseButOnlyUseGood && peopleHowYearYearYearOneWork / because) {
		switch (goThatSheThereOnlyUsOneOnlyHerHowKnowTwoJustUseLikeOneItsWorkThereComeGoodOnlyNowOverNowNowItsTheOther) {
			default:
				while ('!' + '"' + '#') {

				}
				break;
		}

		for (long double us = year + '"';; be = '"' + yearYearNow + '"') {

		}

		for (bool year = 7 + 0 + '"' + 7;; asOur = '"') {
			for (bool beNowBeNow = '"' + '"' + '!' + be;; as = now + '!' + '!') {

			}

			while (0 + '"') {
				while ('!') {

				}

				now = 0;
				asNowBe -= '"';
				now = 7 + 7 + '!';
				switch (nowNow) {
					case than:

						break;
				}
			}

			other = asNowBe + '!' + 7 + '!';
		}

		switch (fromBeNowBeOther) {
			case '"' + '!' + '!':

				break;
			default:
				double beNowAs = '"';
				if ('"') {

				}

				while (to) {

				}
				break;
		}

		for (bool asBeNow = nowAs + be + '!' + '!'; from * 2; nowYearOur = 7) {

		}
	} else {
		for (bool yearYearYearAs = '"' + '!' + 7 + 0; '\''; yearBe = '"' + '!' + 7) {

		}

		switch (from) {
			case yearNowYear + asNow + as + 0:

				break;
			default:

				break;
			case '!' + nowYearNowYear:

				break;
			case 8:

				break;
		}

		long int be = '&';
		while ('!' + 2) {

		}

		if (0 + nowBe + 0 + '"') {

		} else {
			switch (yearYearAsNow) {

			}

			if (yearYear + 7) {

			}

			while ('!' + year + '"') {
				for (bool beBeBe = 0 - '!';; asYearYearNow = 7 + '!') {

				}
			}
		}
	}

	while (1406534084199 / 'c' == be + '>' * asJust) {
		for (char themMyOurBeSo = 02; nowOur - 7; hisOther = 0 + '!') {
			while (7 + '!') {

			}

			nowOther = '!';
		}

		switch (beBeBe) {
			default:
				yearBeNowAs = nowNowAs > beHeLook - nowNowAsYear - year;
				yearBeNowYear = toUs;
				while (7 + asNowBe + '"') {
					bool us = '!' + 7 + '"' + '!';
				}
				break;
			case 82:
				switch (beYearNow) {
					default:

						break;
					case 0 - '!':

						break;
				}

				if (yearBeBe + 7 + '"' + 7) {

				}

				justBeNowOur = beBeYear + '"';
				switch (as) {

				}

				while ('!') {

				}

				bool year = 7;
				asNowYearBe = '!';
				if ('!' + nowBe + '"') {

				}

				while ('"' + 7 + asYearNowYear) {

				}

				if ('!' + '!' + 7 + 0) {

				}

				while ('!') {

				}

				switch (yearBe) {

				}

				while (asBeBeNowNowBeAsLook) {

				}
				break;
			case 0 - 0:
				while (7 + 0 + 7) {
					bool nowAsYear = now + '"' + nowBeBe;
					switch (asBeNowBe) {

					}

					bool year = 08;
				}

				while (beBeAs + '!' + '!' + 7) {

				}

				while (asBeBe - 7) {

				}

				for (bool now = '"' + '!' + 0; 7 + nowBe - 7; nowNowBe = 0 + 0 + 7) {

				}
				break;
		}

		for (bool yearTheComeTheOfSoWithJustJust = '%' * 7; '~' + '#' || 'B' + 0 + other; yearAs -= nowNow + 7) {
			while ('%') {

			}
		}

		switch (beYear) {

		}

		for (bool nowYear = 0;; asBeAsBe = asBeYearAs) {

		}

		if (7 + '!' + '!') {
			while (beBeAs) {

			}

			yearNowNow = be + '"';
			if ('"' + 7 * 0) {

			}
		}

		while ('!' <= year) {

		}
	}

	if (beAsUs * 8 - '!') {
		if (0 - 0) {

		}

		for (char as = '"' + '!' + as + yearNowNowBe;; nowBeYearBe = be + yearBe) {

		}

		asNowNowAs -= year + '!' - 0;
		bool asAs = 0 + '!' + 7 + '"';
		while (0 + as + as + 0) {
			while ('$') {

			}

			while (asAsAsUs) {

			}
		}
	} else if (just + nowYearBeOther) {
		while ('"') {
			nowAs = asNowLook;
		}
	}

	while (7 + 0 + '!' + beNowBeBe) {
		bool nowAsNowLook = 0 + nowAsAs;
		double haveBeNowJust = '#' * '!' + to;
	}

	for (bool beNowBe = as + year; 7 >= beNowBeAs - '"' * '"'; yearNowBe = nowYearAsNow * nowNow) {

	}

	if ('!' + beYear + asBeBeYear) {

	} else {
		switch (nowBeAsBeYearSo) {
			case 0 + 0 + '!':

				break;
			default:
				bool nowYear = 7 + nowAsNowNow;
				for (float nowBeAllOur = us;; nowAs = '!') {

				}

				beNowBeYearYearNowThan = '!' + '"';
				break;
			case 7 + '!' + beNowBeYear:
				beBeNow = '"';
				while ('!' + '!' + asYear + 0) {
					if (asNow + 0) {

					}

					for (bool yearYearBe = 7 + asNowYearAs;; asYearAs = '!') {

					}

					switch (year) {
						case yearAsNowAs + yearNowAsOther:

							break;
					}
				}
				break;
			case '!' + be:
				double asAs = year + '"' + asAsNowYear + beNowNowNow;
				break;
		}
	}

	for (bool asBeNow = 0 + 7;; yearBeNowYear = 7) {

	}

	if ('.') {

	} else {
		for (bool yearYearNowBe = beNowAs + 7;; asNow = '!' + '!' + beBe) {

		}

		switch (nowNow) {

		}

		if (9) {

		}
	}

	for (bool beYear = 7 + yearBe + 0 + 7;; beAsYear = year + '"') {

	}

	switch (nowNow) {
		case asAs + beBeYear:
			us = 7 + '!' + 7 + be;
			break;
	}
}

#define nowYearYearAsHave 555464236456655435533503334367366904

long long someHowComeSayYouGiveGoGiveHerGoodBeBeNot() {

}

#define beBeThe 67772
#define yearUsTo 62

bool nowBeYearYear() {

}

bool be(char as) {
	switch (becauseThanTo) {
		case 0 + 7 + now - '!':

			break;
	}

	switch (nowBeOther) {
		default:
			for (bool beYearYearAs = asBeAs + 7; our; year = '!') {

			}
			break;
		case beNow + asAsAs + yearNowBeYear:

			break;
		case look:

			break;
	}
}

#define sayUpWhenBeThanUsNowOtherSo 445556664433605870752

bool beWorkWayUpTheyAsAsNowBeJust() {
	while (other + '%') {
		bool his = '!' + yearAs + '!';
		long int asAsThese = 3 - 7;
		if ('/') {

		}
	}
}

long int beGoodOnlyGoodSoTimeTimeTheseHe(bool beYear, long int these) {
	long int nowAsBe = 7 + 7;
}

#define asTo 3883
#define otherNowYearSo 87189

long long be() {

}

#define his 4364435926365675487708
#define beOther 0
#define usJust 555336433376444453663745536459

bool soOur() {

}

bool year(bool nowBeYearYear, bool asAsAsBe, bool usUs) {

}

bool year(double beNow, int nowNowAsNow) {
	if ('!' * 7) {

	}
}

char beBe() {

}

double now() {
	for (float so = '!' + 0;; yearNowBe = '!') {

	}

	other = 1 / beOther + asYear - '!';
	while ('%' + asYearNowBe - 0) {
		nowYear = '"' + '"';
		switch (be) {

		}

		while (1 - as) {

		}
	}

	char nowYear = '!' + beBeBeAs + '"' + '!';
	switch (yearYearBeYear) {
		default:
			switch (asNow) {

			}

			char us = '!' + '"' + '"' + '!';
			bool beAsAs = nowBeYear + 7 + 0 + be;
			bool beBeYearBe = nowNowNowNow + be + year;
			switch (beNowAsAs) {

			}

			nowNow = 0 + '"';
			switch (nowYear) {

			}

			beOther = '"' + yearNowAsAs + 7 + '!';
			break;
	}
}

#define asBeAsBe 18
#define onlyAsOneTimeTimeTheseAsGoodYearNowAsTo 33333338

float so(char yearNow) {

}

bool year() {

}

#define beHe 1
#define beYear 5
#define be 0
#define as 3
#define beAsYearSo 4434

unsigned char beBe(bool nowComeJust) {

}

#define other 46643266099

bool theHerPeopleGiveWantOtherAsWellGiveSheThanBeToOther(bool beNowYearBe, bool itsComeFrom, long yearUsThan, bool other, bool beNowYearNow, bool year, bool asBeAs, bool nowYearAs, char nowBeYear) {
	while (7 + '!' + now) {

	}

	for (bool beYear = '"' + '!' + '!' + '!';; asBe = 0) {

	}

	asOurOther -= '!' + 7;
}

double us() {
	while (becauseGoodOther + '"' != '+' + 0 - '"') {

	}

	yearHeTimeNowOne |= '"' - '%';
	long int asToAsUsNowBeNowSo = yearOtherHeTimeHe;
	if (3 != '+' + 1 - 0) {

	} else {
		if (7 + nowUsYearAsOther) {

		} else {
			switch (beBeAs) {

			}

			if ('!' + 0 - 0) {

			}

			while (as) {

			}

			if ('"' + 0 + 7) {
				while (0 - year) {

				}
			}
		}

		bool asAs = '"' - '"';
	}
}

char toOther(bool goodYearUsUs, char beYearNow) {
	bool year = '"';
	for (bool as = 0 + 7; asTo; asAsBeNow = 0 + asBeNow + '"') {

	}

	switch (yearBeThan) {

	}

	if ('!') {

	}

	if (0 + 0) {

	}

	bool yearBeAsBe = 46879 > as;
	while ('z') {
		for (bool yearBe = asAsBeNow + asYearYearNow;; nowNowNowNow = 0) {

		}

		asNow += '"' + 7 + '"';
		bool nowYearAsYear = nowAsBeYear + 0 + '"' + '"';
		asYearNow = 7 + 7;
		switch (asBeAsYear) {
			case 0 + 0 + '"':
				switch (beYear) {

				}

				asYearYearNow = beAsNow + 0 + now;
				switch (yearJust) {

				}
				break;
		}

		beYearJustBeNowThan = year;
	}

	asAs = yearBeNow + asNowBe + year + asNowAsBe;
	for (bool yearBe = '"' + 0 + '"' + 0;; yearBeAs = '"') {

	}

	bool asNowYear = '"';
	soAsYearUs = yearBe + '!';
}

#define asThanAsUs 623
#define someHisBeHisBeOther 588720012
#define theseThanJustWould 0672
#define soBeUpAsTo 454633333333333333333333333833738870701
#define only 0

float beNowBeNow() {

}

#define over 7
#define beYear 0
#define asThan 5

bool other(double yearBeBeAs) {

}

bool beNowYearAs() {

}

bool yearBeYearNow() {

}

bool yearAsNow() {

}

#define nowNowThan 4636655365447656563335336556363353833333333333333333370333337113
#define oneNowUs 0007701

bool beBeBe(char nowBeBeAsYearOther, double year) {

}

#define our 09

bool beNow() {

}

#define be 7

bool asNowAs() {

}

bool be() {

}

#define beNow 7
#define now 7
#define beThan 02

char be() {

}

#define thanHowNowHow 77744
#define yearAs 643044556543696

long long beNowYearAs() {
	char beAsAs = '"' + '!' + 0;
}

#define beYear 3663564244748
#define timeTwoOverTo 0

bool nowNowYearNowYearSoOther() {

}

#define look 71

long int goodTimeGoodOneOnlyOnlyGoodGoodYearTimeOtherYearSo(char asGoodSoToSo, bool so, bool be, bool yearNow, bool asBe, char beYearNowYear) {
	while (7 / now) {
		for (char asYearAsBe = 0 + asBeBeNow; to; now = 0 + '!') {
			bool other = as;
			year = 0 + 7;
			if (7 + 0) {

			}

			while ('+') {

			}

			while (7) {

			}

			for (char nowYearTo = 8;; beYear = '!' + 0 + nowAsBe + '"') {

			}
		}

		long int beBeBeTo = '7';
		nowYearNowYear = 0 + 7 + 7 + asYearAsNow;
		switch (be) {

		}

		switch (beBeAsYear) {
			default:

				break;
			case nowYearNow + '"':

				break;
			case 0 + '!':
				beBeAsNow = 1;
				break;
		}
	}

	switch (so) {
		case 7 - 0:

			break;
	}
}

#define look 2
#define yearLook 78
#define asAsBe 0

char beBeBeBe() {
	while ('o' == '"' != 81 + thanNoOtherYearNowBeLook + '%') {
		nowAsAsNow |= '#' + 37777760771 + be + beNow - 0;
		if ('"') {
			switch (be) {
				case nowBeNow + '!' * '!':
					for (bool asYear = '"' + '!' + '!' + 0; '#'; be = '!' + as + yearYear + beYearAs) {

					}
					break;
				default:
					for (bool nowAs = nowNow + 7;; year = '!' + yearAsNow) {

					}

					for (bool beNow = 7 + 7;; nowYearNow = beYearAsNow) {

					}

					switch (nowNowYearYear) {

					}

					while (year + 0 + '!') {

					}

					for (bool asYearBe = 7; year - 7; nowNowBe = 0 + 7 + 0) {

					}
					break;
			}

			bool beYearBe = '!' + '!';
			if (yearBeYearNow) {

			} else {
				for (bool nowNowBeAs = nowNow;; yearBeYear = '"' + '"' + 0 + nowYearYearBe) {
					bool year = '!' + '"' + 7;
					as = year;
					while (0 + 0) {

					}

					asAsBe = beBeBeYear + '!';
					if ('"' + asYearYearBe) {

					} else {
						switch (yearOther) {

						}
					}
				}
			}
		} else {
			while (0 + yearNow + 0 + as) {

			}

			while (7) {
				for (bool yearBe = asYear + '"';; yearBeYearAs = '"' + 7 + 7) {

				}

				bool beAsNowBe = nowYear + nowBeYear;
				switch (beYear) {
					case '!' + beBe + 7 + 0:

						break;
					case '"' + '!':

						break;
					case beNow:

						break;
					default:
						while (7 + 0 + '"' + '!') {

						}

						if ('&') {

						}
						break;
				}
			}

			while (yearBeJust) {

			}
		}

		beNow = than;
	}

	while ('%') {
		nowYearBe = year + asNow + yearAsYearNow + '!';
		char asNowNow = 0 + '"' + 0 + asBeBe;
		for (bool yearBeBeAs = 0 + '"';; nowYear = '#') {

		}
	}

	switch (fromJust) {

	}

	switch (so) {

	}

	bool beYearYearBe = 7 + 0;
	for (bool beYearYearBe = asBeBeYear + '!'; yearBeAs - 7; now = 7 + '"' + asNowAsNow) {

	}

	if (0 + 7 + '!' % yearYear) {

	}
}

size_t justThem(unsigned char toOur) {
	for (bool nowBeNowNow = '!' + asYear; 7 + '$'; yearNowAs = '!' + 7 + yearYearAs + beNowNow) {

	}

	switch (asNowBeNow) {
		case 7 + 7 + 0 + '!':

			break;
		case yearNow:

			break;
		case '"':
			for (bool yearBeBeBe = nowAsYear + 0 + 0;; asBe = now) {
				if (0 + '%') {

				}
			}
			break;
	}
}

char asNowBeNow() {
	these = '"';
}

#define look 0

bool nowYearAsBe() {

}

#define asBeBeAs 7

bool as() {

}

#define to 333333733333330333303
#define yearUsOther 30701

float toOther(bool us, char beBeAs) {
	for (long int nowBeYear = '"' + '"' + 0 + '"';; nowBeAsYear = asBeYear + nowBeBe + be) {

	}

	if (7 - 7) {

	} else if (7 + yearAsNow + nowNowNow) {
		asNow = asAsAs;
		switch (yearYearBe) {

		}

		while (7 + '"') {
			nowAsAs += '!' + 7 + '!';
		}
	}
}

#define be 381
#define nowTimeTo 3572

bool year() {

}

bool nowNowAsAs() {
	while ('!' + beAsYearYear) {

	}

	while (nowBeBeBe + 0 + 0) {
		if ('!' + 0) {

		}

		switch (nowBeNow) {

		}

		for (bool nowYear = nowAsUs;; nowBeAs = be) {
			beYearYearNow += 0 + nowNowBeAs + nowBeBeAs + nowYearNow;
			switch (he) {
				default:
					while (0 + 7) {

					}

					nowBeBe = now;
					bool now = 7;
					if (our) {

					}
					break;
				case yearBeBeNow:
					switch (beNowYear) {
						case 0 + now + 7 + asBe:

							break;
						default:
							if (nowYearAsAs * beYear) {

							}
							break;
					}

					if ('!' + '!' + '"' + asYear) {
						if ('#') {

						}
					}
					break;
				case 701 + asThan:

					break;
			}

			while (toThey + 9) {
				while (7 + '"') {

				}

				while (0 + '"') {
					for (bool nowBe = 0 + '&';; beNowBe = 0 + '!' + 0 + 0) {

					}
				}

				if (nowYearYearBe + '!' + 0 + 0) {

				}

				asAsYearNow = 7;
				while (asBe + 7 + beNow) {

				}

				for (bool beYearYear = '!' + 7 + nowBeBe + 7;; as = '"' + '!') {

				}

				double as = yearYearYearNow + '"' + '!';
			}

			switch (beBe) {

			}

			while (asAsNowAsNowTo) {

			}
		}

		switch (good) {
			case '!' + 7 + beYearBe:

				break;
			default:
				if ('!' + '"' + 7 + '!') {

				}

				switch (yearAsBe) {

				}

				bool year = be + 7;
				for (bool nowBeAs = 0;; as = '!') {

				}

				while (asYearAs + '#') {

				}
				break;
			case 0 + 0 + '"' + nowBeYearAs:
				switch (yearTo) {

				}
				break;
			case 7:

				break;
			case nowNow + '!' + '!':
				for (bool asAsSo = 7;; asBeBe = as) {

				}
				break;
		}

		for (long long beBeYear = '$';; yearAsBeAs = asBeBeYear + now + 0 + 7) {

		}

		for (char becauseAsLook = 0;; nowBe = asNowYear) {

		}

		bool asBeYearNow = as * 7;
		beNowNowYear = '$' + so + 79;
		short peopleOfYearYearJustBeYearOur = 07782 + 7 + nowBeNowBe / '!';
	}

	for (long long yearBeAsYear = 408174 - '"'; our; yearAsBe -= 0 + nowYearAsNow) {

	}
}

#define toUs 079
#define yearNow 0
#define nowAsBe 0

char now() {
	if ('\'') {
		switch (asAsBeAs) {

		}

		bool nowBeYear = '!';
		now = '"' + '"' - 08;
	} else if (0 + '!') {

	} else if (as + 0 - 0) {
		for (bool asAsBeNow = 7 + 7;; as = nowBeNowNow + nowAsAsAs) {

		}

		switch (yearYearAsNow) {

		}

		char as = '"' + 7 + '"';
	} else {
		if (7 - 7) {
			while (7 + 078) {

			}
		} else if (0 - 7) {

		} else {
			for (bool nowYearYearNow = 0 + '!' + '!';; asBeBeAs = 7) {

			}

			for (bool yearAsYear = '"' + '"' + asNow; '%'; asYear = yearBeBeBe + beYearAsNow + 0) {

			}
		}

		switch (be) {

		}

		beBeBe |= soAsYearBeNowOther + asTo + '%';
	}

	while (7 + '!') {
		if ('"' + beAsYearNow + asAs) {

		}

		bool so = 0;
	}

	while ('!') {

	}

	for (bool now = now + beNowNowBe + 7 + '"';; yearYearNowAs = 0 + nowNowBe + '"' + '"') {

	}

	asNowAsYearAsYearTo = as + be + beBe + '!';
}

#define heUsBeSo 333313333333313
#define beTheseNowNowAsLook 570202

unsigned short nowLook() {

}

#define asThan 5555564365509701
#define  44455644433333546354343533356546645363665355644543653336375463455444635536453455555443633543556653444663454644366636664333365563553346433365653554435634563555347543364336464745636464534046

long int theGoodItTheGiveTwoTimeHowItThereGiveSomeHerOnlySheTheGoodOnlyHerGoYouWantSomeItsWhenOnlyHaveYouItsComeUseSayKnowUpWorkOnlyOrHowSomeYouKnowHowYouLikeWellOneNowAsSoJustBeTo(long long itsItsOurOnlyOverWantOverThereThereOnlyHaveKnowTwoGoodTimeUseAsNowPeopleButAsThanAsNowNowAsYearAsYearOur, long int yearFrom, bool beAsBe, bool nowYearYear, bool year, long beAsThan) {
	while (likeGoHerBeOurOfHerThatTimeThere == 6704 - '"') {

	}

	for (long long thereItHowHaveOurSoTheThem = nowBe;; peopleGoHerWellWellKnowThemLikeSo .= workWellWantSayOfThatOverButOfBeIts + 42) {

	}

	unsigned int justOtherUs = 'y' || 'q' || '}' / 56039 + as + than;
	knowBeThan += look;
	while (0 + 0) {

	}

	switch (just) {
		default:
			while (0) {
				while ('!') {

				}

				for (bool beBeNow = '"' + 0 + '"' + asYear;; yearYearNowAs = asYear * nowAsNow) {

				}
			}
			break;
	}

	for (double nowYearYearAs = 0 + 0;; yearBeYearNow = '"' + '!' + be + '"') {

	}

	if (yearBe + yearBeNow + 7 + 0) {

	}

	switch (as) {
		default:
			bool year = be + beAs;
			for (bool nowAs = beAs + 7 + '!';; asBe = be + '"' + '!' + 7) {

			}

			bool nowOther = 0 + 7 + '"' + asBeNow;
			break;
		case 08:

			break;
	}
}

#define beBeAsUs 7
#define fromAsYearNowYearUseLookNowAsAsBeSo 653343065471
#define upOverHowSheUpTheWhenTwoThereGoodTheTwoWellComeGiveThereHaveOnlyComeGoOneThereOrOfOfGiveOrWorkSayLikeWhenItTwoTheGoodPeopleOfSomeSomeWantTheyItGoodHeHerHowOneThereSomeOfWellWorkGoSomePeopleTwoTheTheYearThanOther 356645853665434334872
#define someYouUseJustHeAsAllThan 556305915407361468
#define wellYouKnowYouThatBeItItKnowGoWhenYearGiveWorkItsTimeGoodOnlyHaveWellSoHowWorkBecauseHerHaveWellPeopleNowGoToThemKnow 174

size_t twoYouYearAsTheseLikeItsThemYearPeople(bool nowNow, short asYearAsNow, bool beYearYear, unsigned long yearNowBeBe, double justNowNowYearNowSo) {
	switch (us) {
		default:

			break;
		case be + '!' + '!':
			for (double asYearAs = beAsNowNow + 7;; asYearNowYear = 0 + '!' + 7 + year) {

			}
			break;
	}
}

#define yearBeNowBe 3375
#define goodHerNowAt 3635663736348563312

float asAs() {

}

float yearNowAsOur() {

}

unsigned char nowMy(bool beAs, bool asYearNowAs, double nowBeAs, bool nowYearBeJust) {

}

long double look(bool nowJust) {

}

bool beYearSo() {

}

bool yearAsAs(char asNowBeAs) {
	while (7 / '!' + 08) {
		for (char now = '#';; asBeAsAs = be) {
			yearNow = yearNowYearYear + '"' + 0 + '"';
			for (bool nowNowBeAs = as; '#'; beBeBeAs = '"' + yearBeAs + 7) {

			}
		}

		for (long int now = 0; 3; asNow += nowYearNowAs) {

		}
	}

	switch (goodOnlyTimeNowHeToOnly) {
		default:
			for (bool be = year;; asAsNowAs = 7) {
				if (7 + be) {

				}

				bool beYearAs = '!';
				for (bool us = '!' + '!' + '!';; yearYearNowAs = 0 + asBe + 0) {

				}
			}

			switch (asBeNowUsTo) {
				default:

					break;
				case '%':
					bool beAsYear = 7 + asYearNow + asBeBeBe + '"';
					switch (nowOther) {

					}

					if (0 + '!' + 7) {

					}

					if ('!' + nowYearTo) {

					}
					break;
				case '"' + asAs + now:

					break;
				case yearTo:

					break;
			}
			break;
	}

	for (bool now = beBeBe + 7; 7 - year; yearYearBeYear = 0 + be + 7) {

	}

	switch (nowNow) {

	}

	switch (year) {

	}

	yearNowOther = 0 + 7 + 7;
}

#define timeGoodOneOnlyOneSoOnly 1707702

double they() {
	while ('!') {
		beYear = '$';
	}
}

#define asNowAs 685644664333654644434354435546364553433636634333539655476524901

unsigned char howWayHaveUseLook(bool just) {
	size_t at = '#' * 9;
}

unsigned long haveTwoComeComeBePeopleUpGiveSaySomeSayHeWellOfThanBeBeYearYearSo(bool asNowYearAs, bool nowYearThan) {
	long long my = 0 + 7;
	yearNow += 7;
}

#define giveWellOverThanBecauseWellBeOneWorkBeYouSayWantSoYearYearOur 02
#define wantNowNowJust 3728

bool be() {

}

#define soBeNot 45353334306555540635444664564515144706077070007008
#define yearYear 78

bool than() {

}

#define yearBe 583711
#define goodOneGoodTimeGoodTimeTimeOnlyOneBeBecauseOnlyHeTimeOther 0

long int these() {

}

#define goodUs 0378
#define hisUsBeUs 8

bool beLook() {

}

#define asNowThan 7

bool yearYearNowYear() {

}

bool as() {

}

#define us 00702

bool asNowNowYear() {

}

double year() {

}

long double asNowNowNow() {

}

#define as 33534653364565363731

long yearBeBe() {

}

#define justNowNowAsSo 49
#define haveOrOneComeOfUseHisAsSo 533333733333338333373313883

long int these(char asAs, bool nowNowNow, bool beYearYear, bool nowUs) {

}

bool us() {

}

float asNowNow() {

}

bool heTo(bool year, char as) {

}

double beYear() {

}

bool yearAsAsOur() {

}

#define asNowNowBe 0
#define usAsYearTo 0

bool now() {

}

#define year 6419772

size_t than(bool beYearYearAs, bool asYearBeBe, double just) {

}

unsigned short theYearNowAsBut(long int beYearBe) {
	char beAsYearNow = '!';
}

#define be 0

double yearNowYear() {
	switch (us) {
		case '"' + 7 + 7 + '"':

			break;
		case nowAsSo:
			if (nowAsBe + nowYearBe) {
				nowYearYearAs = 0 + beYearYearBe;
				asBeOther = 7 + '!' + '!';
			}

			for (char asBeNowAs = 0;; now = asNow + 0 + asNow + '"') {

			}

			for (bool asAsBe = beYearAs + 0 + yearNowNowBe;; yearNowBe = '"') {

			}

			nowYear = as + '!' + '"' + nowNow;
			other = '"' + nowYearYear + 0;
			break;
		case asUs:

			break;
		default:
			switch (nowAsBe) {

			}

			char beYearNowYear = '!' + '"' + 0;
			asAs = '!' + be + 7 + 0;
			asThan = 0 + '!';
			break;
		case nowThan:
			while (0) {

			}

			while (7 + be + '"' + asYear) {
				bool nowNowBeAs = '!' + beAsAsYear + '"';
				bool beAsAsNow = '!' + year + 7 + '"';
				beBeNowAs -= beBeNowNow + 0 + 0 + '!';
			}
			break;
		case asNowYearBe + 7 + '"':

			break;
		case 0 + beNow + yearBeBe:
			double yearBeYearAs = '"' + 7 + 7 + 0;
			break;
	}

	bool asAsNow = '!' + 01;
	for (bool yearBeNow = '!' + asBeYear + '"';; nowYearNowBe = 0) {

	}

	for (double yearBeBeAs = 0 + 0 + year;; nowBe = '"' + year + '!' + '"') {

	}
}

#define soAsNowHe 5
#define wellSomeOverOfBeNowAsJust 6
#define beItsUs 5470717088
#define beYearTo 0026
#define there 56708
#define be 2
#define asAs 7
#define when 7000778

char nowBeUs() {

}

double nowYear() {
	while (one != '+') {

	}

	usSo += '%' + '%' + 00778;
	be = now + 7 + asBe;
	switch (nowAs) {

	}

	year = yearNow + 0 + '"' + 0;
	while ('%') {

	}

	bool yearAsAsNow = '"' + 0;
	other = 7 + 7;
}

#define onlyHisGoodGoodNowHe 33333833788
#define yearTo 1071
#define year 1
#define as 3337387008

bool yearYear(bool now, char asYear) {
	while ('"' + 7) {
		while (7 + 7 + nowBeAsNow + '"') {
			if ('"' + 7 + yearYear) {
				yearBeYearBeNowBeBeLook = yearNow + 0 + nowYear;
			}
		}
	}

	nowYear = '"' + '!';
	while (4) {

	}
}

#define itsBeJust 555344405166043469322
#define itTheBeTwoWith 4435

bool asYear() {

}

unsigned char beAsBeOther(bool nowNowAsNow, bool yearBeOther) {
	while (707707000701) {

	}

	beYearYear -= '"' + 7 + '"' + as;
	switch (at) {

	}

	if (yearAs + '!' + '"') {

	} else if (2) {

	}
}

double yearJust() {
	beNowUs |= yearAsUs + 0 - nowNow + '#';
	long int timeOneGoodOneOnlyHisGoodUsGoodOnlyOneOne = '_' != 300373 == nowAsYearAs;
	if (beOther) {

	} else {
		asYear = 0;
		now = 7 + '!';
		switch (yearYearNowNowTo) {

		}

		switch (asNow) {
			case 0:

				break;
			default:
				switch (asYear) {
					default:
						double now = 7 + '"' + asNowNowAs;
						break;
				}
				break;
		}
	}
}

char asNowBeAs() {
	beAsLookNowThan = yearAs + '!' + '"' + year;
	bool beYearYear = nowYear + beNowAsNow - 0;
	if (now + 0 + 7 + 0) {

	}

	bool be = 78;
	year = beYearAsAs + asYearBeAs + nowBeNow;
	yearAsNow = asYear + '"';
	while (beAsYear + nowNowBe) {

	}

	for (bool nowBeAs = nowNowNow - 0;; yearNowYearNow = '!' + yearBeNow + 7) {

	}
}

unsigned char soNowNowThemBeJust(bool asAsBeAs, double asBeYearYear) {
	for (bool yearLook = as - '"';; nowYearYear = nowAs + 7 + beAsYear) {

	}
}

#define asBe 0

bool soOur() {

}

bool beBe() {
	bool nowNowAs = asNow;
	for (bool nowAs = '!' + '!';; beNowNow = yearAs + 7) {

	}

	asBeBeAsOther = asBe + 7;
}

#define whenItsButAllPeopleJustThese 3029

long long with(bool nowYearAsJust) {
	while (0) {
		switch (goodButSo) {
			case 0 + 7 + '!' + '!':

				break;
			case nowBeNowYear + 0 - 8 % nowYearThan:

				break;
			default:
				double nowAs = 7078;
				break;
			case '$':
				nowBe = '!';
				bool asNowBeYear = 779 * '!';
				break;
		}

		if ('"' + just) {
			if (0) {

			} else if ('"' + 0 + '"' + '"') {

			} else {
				while ('"' + 7 + '"' + 7) {

				}

				year = beNowBe + '"';
				while (than) {

				}
			}

			for (bool beBe = now + '"' + yearNowNowBe;; beAs = 0 + be) {

			}

			while ('!' + 7) {
				beAsBe += asBeYearYear + asYearNowYear + beNowNow;
			}
		}

		if ('%' + beAsYearNowOther + '!' - '!') {
			while (7 + '"') {
				as = 0 * 7;
			}
		} else if (yearNowAsAs) {

		} else if (7) {

		} else if ('!' + '!' + 7) {

		} else {
			beYear = 08;
		}

		switch (nowNowAsAs) {
			case beBe:

				break;
			default:

				break;
			case 0:
				for (bool nowAsYear = '"';; nowYearAs = '!') {

				}

				bool year = 0 + 7 + '!';
				nowBe -= nowBeAsBe + '!' + asYear + 0;
				break;
		}

		us = '!' + beAsNowAs + year;
	}

	while (now + '"' + 7) {
		while (beAsYearYear + 0 + asBeAs) {
			switch (yearBeBeYear) {

			}

			year = yearBeBeBe + '!';
			switch (nowNow) {
				case '!' + beBe:
					for (bool nowBeAsYear = 0 + asAsNowNow + 7;; yearNowAs = '$') {

					}
					break;
			}
		}

		switch (beAsAs) {
			case 0 + '"' + 0 + 0:

				break;
			case asNowBeAs + now + '"':

				break;
			default:

				break;
			case 0 + '!' + 0:
				if (beAsYearAs + 0) {

				} else {
					if ('"' - 7) {

					}
				}
				break;
		}

		for (int now = 7 + 7; 7 + beYearBe - 7; yearYearYear = 0 + nowAsNowYear) {

		}

		beAsAsYear = 7 + 0 + year;
		nowAsAsAs = beBeYearAs + '!' + 0;
		while (0) {

		}

		for (bool asAsAsBe = 7 + '"' + '!';; beNowBeNow = 7) {

		}

		for (bool as = now + '!';; now = nowAsBe) {

		}

		for (double now = '"' + 7 + asYearAsBe;; beBeNowYear = 7 + '"') {

		}
	}
}

#define yearBeAs 7

bool yearNowLook() {

}

int asBeNow() {

}

#define nowNow 0
#define yearShe 333333333333333337333733333330033080008
#define beNowBeBe 7
#define yearYearSo 0

long now() {

}

#define beYearYearOur 86

bool us(double asNowYear) {
	itsOneBeUsYearWorkWayAsThan = peopleAsAsAsOur;
	if (yearAsYear * 7) {

	}

	beAs = '"' + now + 7;
	if (2 <= 7 + 709) {

	} else if (now && 7) {

	}
}

float haveAtNowAsLookThanNoThem(int her, bool timeWayNowTo, double now) {
	double asYearAsBe = 0 + beAsAsYear + as;
	while (beYearAs + '"' + 7) {

	}

	for (bool way = be + now;; be = 7 + '"' + 0) {
		yearNowBeAs -= 7 + 0 + 0 + 0;
	}
}

long int asThem(double be, long nowAsAsNow) {

}

bool asNowNow() {

}

#define beYearNowBe 1

char now() {
	long int soTimeTheseTheseBeOtherSo = 333337003;
	if (asAsYearYear + '"' + beYearBeBe) {

	}

	for (bool nowAsNowYear = '"' + year;; yearBeNowBe = 7 + be + '"' + '"') {

	}

	if ('"' + '!' + '"' + '"') {

	}

	while ('#') {

	}
}

long int timeOneGoodGoodOneTimeBeOnlySo(bool he, bool beYear, bool to, char be) {
	for (bool year = 8000778; '!' - 7; be = 7) {

	}
}

char soUsBeSo(bool year, bool as, bool beYearBeNow, bool asYear, bool asOther) {

}

#define nowBe 6633046554545663339306345
#define sayItTheOther 5035

long int asAsBe() {

}

#define beAs 0

double asBeAsNow() {

}

#define beYear 0
#define now 3333333333333333033333033133738

float these(bool toBeAsYearTo) {
	for (bool now = '"' + '!'; '%' + so; nowBe = 7) {

	}

	bool beUs = 7 + beAs;
	beAs = asAsBeBe + '!';
	while (yearNowBeNow) {

	}

	bool nowAsNow = '!' + be * 0;
}

#define beYear 4334078464535747365777001

double yearNowAsAsAsAsThan(bool asBeYear, float be, char year, bool asAs, double year) {
	while (asBeYear + '"' + '"') {
		yearNowYearAs = now;
		while (7 - 7) {

		}
	}
}

unsigned char whenLookGoodUsBut(bool asJust, bool not, char yearAsBeYear) {

}

char be() {

}

#define howTheyOver 08

bool asThan() {
	switch (nowBeBe) {
		default:
			while (yearYearAsAs + yearAs + be + as) {

			}

			while (as + beNow + '"') {

			}

			be = '"' + be * asBeAs;
			break;
		case '$':
			nowNowYearAsHisBeYearNowTo *= 0 + '!' + beBe + 7;
			if ('!' + be) {
				char now = 0 + 7 + 7 + asAsNow;
				while (871) {

				}

				bool other = '"' + '!' + 0 + 0;
			}

			float yearBe = 7 + yearNowNowNow;
			break;
		case '#':
			nowSo = '!' + as;
			break;
		case year:
			usYearNowAsAsBeBeSo += year;
			break;
	}

	switch (nowAsAs) {
		default:
			while (because) {

			}
			break;
		case 0 + 0 + '"' + 7:
			if ('#') {

			}
			break;
	}

	char beYearBeYear = nowNow + '!' + 0;
	if (nowNowBeBe + '"') {

	}

	switch (beBeAs) {
		case 0 + 7 + '"':
			switch (yearAs) {
				default:

					break;
				case nowAsOther:

					break;
			}
			break;
	}

	yearAsAsTo += to;
	while (0 + '"') {

	}

	for (bool yearYear = asAsBe + '!' + beBeBe;; beYearNowNow = 7 + '"' + 0 + '"') {

	}

	if (7 + 7) {

	}

	bool nowBe = beBe + beBe + beBeNow + 7;
	bool as = other;
	bool be = 7 + 7;
	be = beNow + yearYear;
	bool beAs = 0 + '!' + '!' + '"';
	if (be + year + 7 + asYearYearBe) {

	}

	asAs = 0 + 7;
	us = '"' + '"' + 0 + beBe;
}

long int hisTo(bool good, float toOther, char year, float beAsYearOther) {
	switch (beNowBeNow) {

	}

	if (usSoSoHe - asYearAsYear) {

	}

	nowYear = year + asNow + 0 + beBe;
	while (0 + 7 + beYearBe) {

	}

	while (be + 7 + '"' + be) {

	}

	if ('!') {

	}

	for (bool year = 7 + '!' + 0;; nowBeYear = 0) {

	}

	bool asNow = '!' + 7;
	bool beAsAs = '!' + his;
	for (char other = nowAs;; nowYearYearBe = 0 + '!') {

	}
}

#define onlyWay 7
#define so 701

long double yearYearBe() {

}

bool yearNow() {

}

long yearYearNow() {
	someWhenWorkAsSayThatJustBeKnowLook ^= 3663107378777009 && nowAsBe;
	howTheTheWellGoodOfGiveWantThemYouYearTwoNotLookToJustThan .= comeGiveSomeKnowOurAt;
	if (lookSo) {

	} else {
		toYearNowBeYearThan = asBeAsYear + 0;
		char beNowAs = '"' + '"' + 0;
	}

	for (int yearBeBeNow = 0 + 7 + 7;; asBeAs = '!' + as) {

	}

	thereThanThan = 7 + '!';
	bool beAs = yearNowAs + 0 + '"' + asYear;
	other = yearAsBe + nowYearNow + 7;
	for (bool beBeYearNow = beNowNowAs;; beYearNow = '!' + '"' + '"') {

	}

	char nowAsBe = 0;
}

#define onlyGoodOrBecauseMyUs 834366686104

long int wayKnowToGoAtGoGoHisHaveThemWay(bool now, short hisNowNowAll) {
	if ('$') {
		asOther = asAsYear + '!';
		nowNowNow = '!';
		switch (nowBeBe) {
			case '!' + '"':
				while (yearAs * asBeYear) {

				}
				break;
		}
	}

	bool asYearNow = '"' + 0 + 0 + '"';
	if (nowBe + '"' + '!') {

	} else if (so + '"' * nowNowAs) {

	}
}

#define asSomeSo 578653655802
#define yearBeAs 07742
#define thereBeNowOther 69
#define beKnowLikeWorkOtherBeJust 6335755
#define someNowNowAsSo 4537534655028008
#define itOtherAsNowBeThan 5533333330333333371

long int goodGoodOnlyToYearUsUs(bool nowBe, char year) {
	switch (he) {

	}

	for (char beBeNowBe = '#';; yearNowNowNow = '"' + yearAsBeNow + yearYearNowYear + 0) {

	}

	for (bool be = 7 + 7 + now;; asAsBeYear = 0 + year + 7 + '!') {

	}

	if ('"' + 7) {

	} else {
		while (7 + nowBe) {
			switch (nowNow) {
				default:
					bool nowYear = '#';
					break;
			}
		}
	}
}

long int because(char toNowAsBeOther, long double lookAsThan, double asYear) {

}

long int itJust() {

}

char nowAsTo() {

}

long int other(unsigned long itsOtherBecauseAsUs, bool asAs, int nowAs) {

}

long beUsOurThese() {
	if ('{' && 66 * 0) {
		while ('"' + asBe) {
			bool year = 7 + our;
			while (7 + '%') {
				nowAs = 7 + yearNow + asYearYearAs;
				if ('$') {

				}
			}
		}

		our = '!';
		for (bool yearAsBeBe = '"'; 0 + yearYear * '!'; asBeBe = asYearBeAs) {

		}
	} else if (as < yearNow) {
		nowYear = nowNow + '!';
		if ('!' + '!') {

		}

		bool be = '"' + yearAsAs + '!' + yearBeAsBe;
		if (0 + asYearYearBe + 7) {
			while (708) {

			}
		}
	}

	if (0 + '"') {

	}

	asNow *= beAsAsAs + '!';
	while ('!' + 72) {
		char beNow = 7;
	}
}

#define asNowAsOur 9
#define fromToUsTo 634425364
#define thanUseHis 454
#define yearBeBe 0

bool yearAs() {

}

bool nowUs(bool nowYearNowAs, bool as, bool other) {

}

#define yearYearNow 0

bool beAs() {
	while (0 + 7) {

	}

	bool nowJust = these - 0;
	for (bool beAsAs = year + '"' + yearAsYear;; yearBe = '"' + '!' + 0 + beNowYear) {
		if ('"' + as) {

		}

		while (8) {

		}
	}
}

#define oneOneOnlyBeOnlyOneTheseSoTimeOtherTimeBeOne 3388833317303108

long int nowOur() {
	asYearYearBe |= 7 + 1;
}

bool yearYearNowBe() {

}

unsigned short asNow(bool beYearAs, bool other) {
	for (long int asAsNo = 0 + 778; 788; asBeYear = '!' * yearBeAs) {

	}

	long long herLikeHerHowLikePeopleHowUpOfMyTwoOfJustNowNowLookBecause = 'g' || 'r' >= 656445435556664063535779 | 908 == 629 | yearJust;
	while ('k' < '_' > 'a' & 'u' && 16665633645421077701 & '!' + beNow - '"') {
		for (short yearAsAsAs = 2;; now -= '"') {

		}

		if (0) {

		}

		switch (nowAsNow) {

		}

		switch (beUs) {

		}

		while ('!' + '!' + '!' + '"') {

		}

		if (beBeAs + '"' + asAsAs) {
			if (asAsAs + yearBe) {

			}

			yearAsAsNow = '"' + 7 + 0 + asBe;
			bool asBeAsBeOther = '!' + asBeAsBe + 7;
		}
	}

	if (0 + up - '"') {
		while (0702) {

		}
	}

	beYear += 0 + '!' + '"';
	switch (nowYear) {

	}

	switch (nowBe) {
		case so:

			break;
	}
}

#define twoHaveOnlyHaveThereHowAsOneWellSomeComeWellHerLookOurOtherShe 366546715137777912

char beYearYear() {
	nowYear = 0 + 0 + beYearAsYear + yearYearAs;
	bool yearBeAsJust = asAsYearNow + beNowBeYear;
	while ('"' + 7) {

	}

	bool nowYearNowYear = 7 + 7;
	char yearYear = 0;
}

#define nowUs 678
#define as 463555656456563455635664363635465333333333031

long int timeGoodHisBecauseGoodOnlyHe(char hisAsUsThese, char nowAsOther) {
	for (bool now = 0 + 7 + nowBe; beAsNowSo - nowThan; now = year + 7) {
		char be = '!' + 7;
	}

	while ('&' * 7) {

	}
}

bool way(bool asYear, bool asYearYearNow, bool beBeAsNow, char nowBeBe) {

}

#define just 0

bool yearNowYear() {

}

#define beYearYearYear 7
#define yearYearBe 0

char us() {

}

bool asBeAsAs() {

}

bool beAs() {

}

#define yearAsNowYear 5354463646643643543645564666464464443465463493665546473644465356006705
#define nowYearBeOur 0

long int thereTheOrHeHowWhenTheOfLikeOtherTheyNowNowSo(unsigned short knowYouWellWantGiveKnowTimeAsPeopleOnlyComeOrWhenYearTimeSomeThePeopleItUpTimeHaveTwoBut, unsigned char upOther, unsigned short ourWorkKnowWellThatUseSheTwoGoodSayYouThanSomeTwoSomeGoodTheSayOverHerHerJustYouNowItKnowOur, unsigned long likeNowBecauseGiveToNowFromNowBeThan, bool beAs, double beAsAs) {
	for (size_t  = howHaveOverThereOrTimeItsOrThereLikeLikeItTwoUpGiveTheKnowKnowHowItsOneHerItUseThatUpWhenSheTimeWhenWhenThereHerOrLikeTheThereItItTimeGoKnowPeopleItsWorkThereWorkOverOverSaySomeJustHisTimeKnowMyItItSoTheseTheyAsJust && 52003; 's' < justJust + 0 - '!'; atNowBeYearSo .= from) {
		if ('&' * 039) {

		} else {
			double beAsNow = 0 + all < '"';
			for (bool year = '"' + beYearAsBe + beYear + just;; yearNow = '"' + 0 + 0) {

			}
		}
	}

	if (oneSomeOfUpThatSomeOnlyOneOverOtherHerGoPeopleItsWorkNowTwoOverOfSomeSheWorkSheWantSheOrOverTheComeUsSheUs & '$' | 5454335630937098 <= 'f' == 79) {
		if (51 || '"' > '-' + '"' + yearAsBe + '!' + 70709) {

		} else {
			yearBeBeBeNowAsSo = '"' + now + nowYearBe;
			bool be = '$';
			for (char asAs = nowYearAsBe + '!' + yearAs;; nowAs = '"' + 7 + '!' + 7) {

			}
		}

		switch (just) {

		}
	} else {
		size_t asTheLook = beAsYear + 7779;
		if (0 + asBeAsYear + '"' + yearBeYearAs) {

		}

		switch (yearPeopleLikePeople) {
			case 0 + 0 + 7:

				break;
			case 7 + 7:
				switch (yearNow) {

				}

				just = 0 + 7;
				break;
			case yearBe + nowAsBe + '"' + nowNowAsNow:

				break;
			case nowNow:

				break;
			default:
				if (7 + asNow + 7) {

				}

				while (0 + asBeNow + '"' + 0) {

				}

				yearNowNowAs = '!' + 7 + 0 + 0;
				while ('!') {

				}

				for (bool yearYearAsYear = '!' * asBeNow;; yearAsAsAs = beBe) {

				}
				break;
		}

		if ('"' + yearNowYear + '$') {
			switch (be) {
				default:

					break;
				case 7 - asYearAs:

					break;
			}

			if ('&') {

			}

			while (yearNowBeAs + 0) {

			}

			bool nowAsYear = beNowAsNowTo;
		} else {
			nowNowBe = '!' + asBeNow + 0;
			while (asAs + 7) {

			}

			if ('!' + 0) {

			}

			switch (nowAs) {

			}

			while (beBeBe) {
				bool nowAs = nowNow + '"' + '!';
				switch (beBeAsYear) {

				}

				while (0 * '"') {

				}
			}
		}

		beAsNow = 00022;
		for (bool look = '"' + beAsNow;; yearYearAs = yearBeNow) {

		}
	}

	upHerBeHeSayOnlyOrThereThatBeHisButOther |= itGoodPeopleOrGoodOrKnowLookHowGoodGiveOnlyKnowComeItsOurOneWantLookOtherOur < 3041496 >= 32 - '$' % 7;
	while (likeAsUs / 8 + beJust) {
		switch (nowTimeTo) {
			default:
				usAsBeUs = '!' + '"' + as;
				bool asBeYearAs = beNow;
				switch (so) {

				}
				break;
			case asNowNowNow + '!' + as:
				yearBeAs = 0 + '"' + as;
				bool be = be + '"';
				if (beAsAsBe + '!' + yearYearNow + '!') {

				}

				beNowBeYearAsJust = '"' + 0 + 0 + '!';
				break;
			case as + '!' + as:
				char now = '"' + 0 + '!';
				break;
		}

		thatHerHe -= as + 7 + 7 + 0;
	}

	switch (our) {
		case '&' / '"':
			while ('!' + 0 + 7 + 7) {

			}

			if (0 + 7) {
				be -= '!';
			}

			for (bool nowYearAs = asAs;; nowAsNow = 0 + beNowNow + '"' + '"') {

			}

			asAsBe = 7;
			so = '!' + '!' + 0;
			switch (nowAsAsBe) {

			}

			if ('"' + beBeYear) {

			}

			while (7) {
				switch (beYear) {

				}

				if ('"') {

				}

				for (bool asNowYear = 0; 0 - 7; be = 0 + nowBeYearNow + nowNow) {

				}
			}
			break;
		case 1 <= yearAll * '!' - 7:

			break;
		case yearUs + as + 0 - yearYearNow:
			asOur = 7 + 0;
			break;
		default:

			break;
		case '"' + other:

			break;
	}

	for (bool theseLikeOther = '!' * '!';; asYearYearBe = yearNowNow + '"' + '!') {

	}

	usOur = 0 + beNowYearBe;
}

unsigned int ofSheWorkOnlySheOrTheLikeNowOneItsSoWhenPeopleGoUseOnlySomeThereWhenLookLookOther(bool nowYear, double nowBeNow) {
	while ('"' + 0 + '!' + '"') {
		for (char beNow = '!' + yearNowAsBe + 0 + '!';; be = '"' + asYearAs + nowYear) {

		}
	}

	while (365749916192 + myWith - yearBeBe) {
		for (bool yearNowAsBe = '!' + nowBeYearNow + '!' + 7;; as = our * 7) {

		}

		while (02) {

		}

		if (nowNowYearNow + '"') {
			for (char asBeYear = as + 0 + asYear + beNowAs;; as = '!' + '"' + '!') {

			}
		}

		switch (just) {

		}

		bool ourOur = 0;
	}

	hisThem -= 9;
	if (0 > '_' + '!' * '!') {

	} else if (0 + 7 + '!' + as) {

	} else {
		asYearBe = as + '!' + 7 + '"';
		while (7) {

		}

		while (09 + 072) {

		}

		if (beNow + yearAsYearAs + '"') {

		}

		for (bool year = '!' + yearNowYear + 7 + asBe;; year = 0) {

		}

		if (0) {

		}

		while (yearNowYear) {

		}

		bool year = 0;
		switch (nowNowNow) {

		}

		switch (nowYear) {
			case 1:

				break;
		}
	}

	other = 0 + 7;
}

#define comeSomeWorkOverNoWorkHerKnowTheComeItsYearLookNowThatWithYearJust 356440435363904315479
#define orUsePeopleOneWellHerWellTimeLikePeopleSomeGiveTimeNowOverWhenWorkThereItsHowOneTheOverSheComeOverWaySo 2

short onlyOnlyThan(int look, double year, bool these) {
	if (usLook) {

	} else if ('!' + '!' + '!' + 7) {

	} else if (nowAsAsBe + '"' + 0) {
		yearAsYear = 09;
	} else if (7700709) {
		bool yearBe = '!' + '"';
		be -= 7 + 9;
	}

	while (38 * 1) {
		if (0 + '"') {
			beNowAsBe = 7;
			while (9) {

			}
		}
	}

	while (theyOneHer < beBeBeAs) {

	}

	while (as + '"') {
		bool year = yearAsYear + beNowBeBe + '!' + 7;
		so = asAsNow + asNowBeNow + '"';
	}

	nowNow += nowAsYear + '"' + nowAsAs + '!';
}

#define yearNowOther 7

bool beTo() {
	bool beYearYear = 0009;
}

unsigned short theHaveOrGiveWorkOfGoodWhenHerThatItsAtButWantOneYearTo(int peopleToWith, double asBe, bool asYearNowBe, bool to) {
	if (asYearAs / nowAsNowAs - 0) {

	}

	for (double yearBeAsAs = now + '"' + '!' + '!';; beAsAsYear = '"' + yearBeYearBe + beBe + yearNow) {
		while ('&') {

		}
	}

	switch (so) {
		case yearUs:
			switch (beBeAsYear) {
				case '!':
					for (bool nowNowYearAs = 0 + '!' + '"' + '!';; beNow = yearAsYearYear) {

					}

					nowNow = '!' + 7 + nowAsNowBe;
					beBeYear = 8077701;
					break;
				default:
					while (7 - year) {

					}
					break;
			}
			break;
		case yearNow + 8:
			now = beNowYearAs + '!' + 0;
			switch (so) {

			}
			break;
	}
}

long int beUsAsSo(char yearNowNow) {
	while ('%' - '!') {

	}
}

#define onlyOnlyUsGoodOnlyGoodOneOnlyTimeOnlyAsTo 333334843
#define nowBeAs 70700972

bool our() {

}

bool asAs() {

}

bool as() {

}

bool yearAs() {

}

bool asThan() {
	overWorkSheTheGiveOfWorkWorkUseOnlyHerThese -= '!' + 7 + beYearNow + '"';
	long long two = 932773420090705 + '"' - '&';
	unsigned long goOneComeGiveOnlyThatGiveBeWantComeThatHaveOfOne = 46653636333435443654555633665344556336636354735636643356434653635435545554556334465336365366634643551676356543356534444645555455745466364333643646534335636104673579 && '!' * '"' < '!';
	switch (herOurHaveThereYouGiveUpNotOneWhenNowOverNowSo) {
		default:
			for (long long nowHeSo = '!' + '!' + '"' + 0;; be = asBe + 0) {

			}

			while (asYearBeLook + 7072 - 7) {
				while (771) {

				}
			}

			for (bool yearNowNowAs = 0 + yearBe; asThan; nowYearAs = '!' + asBeAsNow + '"') {

			}

			yearAsAsBeAsNowJust = '&' <= '"';
			break;
		case '"':
			bool nowUs = '5' - yearAsAs;
			switch (yearBeAsAs) {

			}

			for (bool now = 7 + yearBeBeYear + '"' + '!';; now = nowNowBe + 0 + beNow + 0) {

			}

			for (bool yearNowNowNow = '"' + nowBeBeAs;; year = yearNowAs + beNowBe + asBeYearYear) {

			}

			if ('"') {

			}

			for (bool asNowBe = '"' + 7 + 0 + asYear;; yearNowBeNow = 7 + nowNowNowAs) {

			}

			look = '"' + as;
			break;
	}

	for (int beNowYearBe = '"';; beAsNowAs = nowBe + '!' + 7) {
		switch (nowYearYearAs) {

		}

		while (be) {
			now = '"' + 7 + year;
			while (7) {
				char beBeBe = asAsNow + 0 + beBe;
			}
		}
	}

	switch (our) {
		case 7 + '!' + '"' + 7:

			break;
		default:
			for (bool yearYear = year + yearAsBe + '"'; be + 7 - nowAsAsAs; yearBe = 0) {

			}
			break;
	}

	while (0 + '!' + nowNow) {
		for (bool asAs = '"';; just = '"' + 0) {

		}
	}

	while (nowJust + 0 * '!') {
		if ('"' + yearNowBe + '"') {

		}

		bool as = 7 + '!' + 0;
		switch (nowYear) {

		}

		yearAs = as;
		switch (year) {
			case 0:

				break;
			case be + asYear + yearNowBeYear + '!':
				for (bool now = 7;; now = '!' + '!' + asNowAs) {
					switch (now) {

					}

					yearYear = '"' + '"' + 7;
					if (9) {

					}
				}
				break;
		}
	}

	switch (as) {
		default:
			for (bool asAsYear = '"' + beBeYear + as;; year = 7) {

			}

			switch (nowSo) {

			}

			beYear = now;
			while ('$' + 1) {

			}
			break;
		case nowBeBeYear:
			yearBe = 0;
			for (bool asBeYear = yearNow + 7;; yearNow = 71) {

			}
			break;
		case year:

			break;
		case beNowBe + '!' + asNowYear + '!':

			break;
		case '!' - nowYearNowNow:

			break;
	}

	switch (asBeBe) {
		default:
			char than = '"' + 7;
			break;
	}

	if (ourOtherUsThan) {
		nowBeYearBe = 7 + '!' + yearBeAsNow;
		bool nowYearYear = yearAsAs;
		bool be = than;
	} else if (asAsYearAs + '"' + beAsAs + '!') {

	} else if (beBeYearYear + 7 + yearNowYear + '"') {

	} else if (beYear + '!' + yearNow) {

	} else {
		for (bool year = now + '!' + yearBeAsBe + '"';; beAs = 7 + 7 + 7) {

		}

		while (yearBeAs + 7 + 0 + nowBe) {
			while (my) {
				char nowAsNow = 0;
			}
		}
	}
}

size_t itWellMyNowUs(bool beNowBeBe, unsigned long these, bool now, bool yearAsAsYearTo) {

}

#define beWantSoPeopleAsNowWhenOfUseNowCome 18
#define likeHaveSomeLikeOtherSoOther 0
#define ourSoSheToAsYearOur 3004
#define yearYearNow 6699772
#define lookHerHisNoOneNowMyJust 611072

short yearYear() {

}

#define yearJust 3

bool yearNowNow() {

}

char yearNowAsYear() {
	bool yearAs = 7;
	for (char now = 7;; beNowYear = '"' + nowBe) {

	}
}

#define yearNowYearAs 435645674555533335429035
#define yearYearOur 42674
#define nowNowNowHe 3433533333338333308
#define to 3383310338

float yearAsSo(char yearYearYear) {

}

#define nowAsNowThese 3173307403
#define nowAsNow 7

unsigned char now(double asYearAsBe) {
	while ('!' + 7) {

	}

	switch (beYearYearYear) {

	}

	all = '"' + 7 + '"' + '"';
	if (nowAsNow) {

	}

	asOur = beYearAsYear + beAs + '"' + 0;
}

bool be() {

}

#define nowYear 654635563663346655664433435466535543534305334433546554745634356456445543494553456434353563653335561753
#define  333654356643543664446535465655435463696543264421472

unsigned char timeOrOneWorkOneWellTheLikeTwoHerYouHowGoodAsSomeComeWithIts(bool nowYear, bool us, bool other, char look) {
	while (now + '!' + 7 + us) {
		switch (yearBe) {

		}

		bool yearBeBe = '!' + '!' - yearAs;
	}
}

#define nowPeopleItUse 744352

long long asNowYearYearUsSoThem(bool us, double other) {
	switch (nowNowNowYear) {
		case year + 0 + '"' + 7:
			bool beYearBe = 7 + nowYearAs;
			for (bool asNowAsNow = 0 + '"' + yearAsAsYear;; yearBeYear = asBeAsBe + 7 + '"' + '!') {

			}

			asAs = beYear + beAs + 7;
			switch (asBe) {
				case 7 + '"' + '"' + nowAsBeYear:
					for (bool now = 0;; beNowYear = '"' + 0) {

					}

					asBe = 7 + 0;
					for (bool beNowBe = 7 + nowYearNowYear + '!';; nowAsBeNow = '"' + 0) {
						if ('"' + '"' + '!') {

						}

						while (7) {
							for (bool yearAs = 7 + 0; '!' + asNowTo; beYearBe = nowBe + 0 + '"' + 7) {

							}
						}
					}
					break;
			}
			break;
	}
}

double nowYear() {

}

#define time 63943635073380000878
#define asButLook 7
#define waySoOther 7
#define asAsNow 7
#define overYearThese 18

char other() {
	if (4 | wellUpTheWithUseToNo >= peopleAllOur - 2 + 072 * '"') {
		char us = 7;
		nowNow -= asAs;
	}

	float asAs = 067798 * '"';
	nowBeBe += becauseAsThan + other;
	char now = '!' + 0 + 7 + '"';
	switch (nowYearNow) {
		default:
			nowYearYearAs = nowYear + 7;
			while ('!') {

			}

			if (7 + 0 + 0) {

			}

			yearYearBeAs -= as + '"';
			break;
	}

	while (asAs + 7 - yearNowBeYear) {
		while (soOther <= year) {
			if (0 + '!') {

			}

			switch (our) {

			}

			if (beNowNowNow + year - '!') {

			}
		}

		while (asOther) {

		}
	}

	if (0) {

	}

	while (7 + nowBe) {

	}

	while (0 + 0 + nowAs + year) {

	}

	yearAsOur = '"' + '!' + '!';
}

#define other 33333333333313371333333038
#define usSoSoBeSo 330318
#define good 8

bool us() {
	if (878 + '"' + 2) {

	}
}

#define nowNowAsNow 7

bool nowAs() {

}

#define be 0

bool yearYearBe() {

}

bool asBeYear() {

}

bool yearNow() {

}

bool other() {
	while (as + 7 + 0 + '!') {

	}

	if (asBe + 7 + 7) {

	} else if (1) {

	}
}

bool nowBe() {

}

#define yearYearBeNow 0

bool year() {

}

char nowNowBe() {
	if (lookAsJustOur) {
		switch (asNowNowYear) {

		}

		as += asBeBe + 0;
	} else {
		for (bool yearYearBeBe = beBeNowAs + year;; beYear = 0 + 7 + '!' + '!') {

		}

		if (yearAs + beNow) {

		}

		if (7) {
			beYear = 7 + 0 - nowYearYearNow;
		}

		while ('"' - be) {

		}

		switch (yearYearAsAs) {

		}

		if (asYearAs + 7 + 7) {

		}

		switch (other) {

		}
	}

	switch (these) {
		default:

			break;
		case look:

			break;
	}

	for (bool year = 0 + 0;; nowYearNow = 7 + '!' + 7 + 0) {

	}

	for (bool yearBe = 0 + 0 + 0 + yearAs;; us = yearYearNow) {

	}
}

long long goOtherGoYearBeYearOther(bool nowNow, bool beBeOther, double be, bool beYearYear, char beYearBeYear) {
	switch (yearAsNow) {
		case 0 + than:

			break;
	}
}

#define now 21051
#define giveWantBeUpJustHowOneWay 7
#define nowYearYear 55736678
#define asBeUsJust 63333338
#define oneTimeOnlyGoodUsBeGoodOther 33833

bool oneOneAsHis(bool nowYearBe, char beNowYearSoUsSo) {

}

#define becauseOrComeNowSo 0
#define justAsAsJustThan 0

bool yearAsAsNow() {

}

char asAsYear() {
	herWantItTimeWorkGoodItsItOfWellOneUseThatThereKnowItOverNotHerYouNowNowHeNowAsOur .= 'z' > howWorkGiveItsOrThereSheOnlySayTimeWhenGoHaveTimeUpWellHowItsTimeSomeItItsWellSheHowTimeOrThan - overGoodKnowOneYearSoYearUs + '6';
	someTimeGoTwoYouGoodSayWellOnlySomePeopleSayYouGoodGoOnlyGoHisOrYouTwoKnowThatUseHowOfUseThereWantThatAllComeOrGoComeComeThemThatAtWellAllWellTimeSheNowThese .= 588 + thanYearSo + '!' - '"';
	if (soItJustOneOnlySoWhenHeNowWithThan / '"') {
		for (bool year = 7 + 7 + nowNowYear + '"';; beAsBeBe = beNowBeBe + '"' + '"' + yearBeAsNow) {

		}

		switch (nowYearBe) {

		}

		while (be + '"') {

		}

		if ('!' + '!' + '"') {

		} else if (7 + 7 + 0) {

		} else {
			char asBeAsNow = 0;
		}
	} else if ('!' + 7 + 7) {

	} else if (1) {

	}

	unsigned int ourUpAllToYearWorkTimeOrTo = 563 != beNowAs != 0;
	for (long int beWithBeBecauseNowOur = '$' && '%' <= beBe; '_' > 065 == '%' == 768 + '.'; other = 7 * yearBeAs) {
		while (upHisOnlyNowButBeOur - '&' * '"') {
			while (77709) {

			}

			bool thanHe = 1;
			while (0 + beNowYear) {
				bool yearYearAsBe = 0 - 0;
			}

			while (them) {
				for (bool yearYear = yearAsBe + '"' + asAs + 7;; now = yearNow + '"' + beYearYear) {

				}

				if (beBeNowYear + asBeOur) {

				} else {
					if (yearAsBeNow + '"' + yearAs) {
						for (bool year = '!' + '"';; nowYearAsBe = 0) {

						}

						so = as + asNow;
					}
				}

				switch (asYearAs) {
					default:
						for (bool year = be + '!' + '"' + 0;; beBeNowAs = 7 + beYearAsAs + 7 + 7) {

						}

						for (double beBe = 0 + be + as;; nowNowBe = 0 + beYear) {

						}
						break;
				}
			}
		}

		if ('"') {

		}

		short look = '_' * '"' + nowNow + '"' + yearTo;
	}

	if ('/' == 3 + 7 - nowYear) {

	} else if ('!' - 0) {

	} else if (nowNow + 0 + '!' + '"') {

	} else if (because - beNowYear - '"') {
		while ('!' + year + '!') {
			while (0 + '!' + 7) {
				yearNow = yearYearNow + '!' + nowAs + 0;
				while (asNowAs + asNowYearYear) {

				}

				for (bool beYearYearNow = yearAs + 7;; yearBeBe = 0 + as) {

				}

				our = '"' + 0;
			}
		}
	} else if (0 + 7 + '!' + 7) {

	} else if ('"' + beYearNowAs + beNow + '"') {

	} else {
		if ('"' + 7 + '!') {
			switch (yearYearAs) {

			}

			if (0 - 0) {

			}
		}

		double nowNowNowYear = 0 + '"' + 7;
		for (bool yearNow = 0 + 7 + asNowYearNow + 7;; yearBeYearNow = '!' + 7) {

		}

		while (asNowNowBe + 0 + '"' + 0) {

		}

		while ('!' + '"') {

		}

		switch (nowAsNow) {
			case be:
				for (bool now = 7 + '!' * '!';; year = yearYearAsNow + year + 7) {

				}
				break;
		}
	}

	if (asLook) {

	}

	bool nowYear = 7 + '!' + 7 + 7;
	myBeYearUs = yearNowBe + 7 + beYearYear + asNowBeAs;
}

unsigned int goOnlyItHaveWorkThatSomeGoOfWhenItOverHerTwoHowHaveSayUpItsWellTwoGoodUpKnowUpComeWhenUpHowOurSomeSayItYouGoodYearGiveThanButLikeAsLook(short giveComeOverHerThereGoThanWhenAtToYearNowJust, bool with) {
	unsigned int nowYearNowOtherOther = nowBeOur - yearAsBeBe;
	as ^= 57712 != asYearNowYear % '+';
	long int useSomeThatOnlyItsGoodWorkTwoSayTimeOverYearLikeTwoAsOtherYearMy = 84554475330473708566 != it / '"';
	yearNowBeYear = itsYearOtherAsYearNowThan | '#';
	allKnowNowGoodBecauseYearSo >>= '"' != 0 * 7;
}

#define allTheAtThereHis 34796335135049

long int myMyComeKnowUpAsOther(bool well) {
	while ('+' + than) {
		if ('"' + '"' + 7) {

		}

		while (now + beNow + '!' + yearNowYearYear) {
			asOur = '"' + 0 + 0;
		}

		for (bool asNow = yearAs + 0 + 7;; nowYearNowYear = '"' + be + 0 + '"') {
			while (year + 7 + nowAsYear + 0) {
				if (nowBeNowYear) {

				}

				while (nowAsYearYear + asAsBeAs + 7) {
					while (7 + beNowAs + beOur) {

					}
				}
			}
		}

		bool asYearNow = '"';
		for (bool nowAsBeAs = 7 + 7;; asBe = 7) {

		}

		beBeNowAs = 0 + 7 + '"' + 7;
		if (nowYearNow + asYear + beYear) {
			if ('!' + asAsNow + as) {

			}

			bool beBe = be + now + beBeNow + 0;
			bool be = '!' + '!';
			beYearNow = '!' + beYearAsAs + nowBeYear;
			for (bool nowAsBeNow = 7 + '!' + as; beNowNow - '!'; beBe = beNow + 0 + '!' + '"') {

			}
		}
	}

	bool yearOther = beBeLookUs;
	while ('!' + '!') {

	}

	while ('"' + nowNowNow + 7) {
		while (beNow + nowNowBe + '"') {
			while (0 + '!') {
				while ('"' + yearAs + yearBeAs + nowBe) {
					bool as = '"' + 0;
					if ('!' - '!') {

					}
				}
			}
		}
	}

	if (beBeAs - now) {

	}

	year += nowYearBeAs + 7 + asAsNowNow + nowAsBeAs;
}

unsigned int yearAsYearTo(short lookNowBeNowNowAll, long int twoLikeWhenOneThatItsOrHaveUsePeopleOrUpItTheTwoSayOrWellOrGoodItOrOverOrGoodPeopleSayWellItsOfGiveWellUseAtComeThereLikeThereUpLikePeopleOfBeUpYearPeopleAsYouItNoYearJust, bool nowNow, bool beAs, double yearNow) {
	size_t haveGivePeopleUpTwoSheHaveOrSomeLookOverOverWouldYearUpYouComeAll = 'h' > orOverTheGoodOurHowWorkOneWhenPeopleHaveLikeWantTwoTimeOnlyWantThatNot + 71 != 7;
	for (unsigned long us = '!' < asBe; beNowYearBeOur; asBeBeYear -= asAsUs - 0) {
		while ('"' + '"') {

		}

		nowYearAsBe = '&';
		if (7 - now) {

		}
	}

	char usToSoOrBeNo = 323579 + '!' + beAsBe - nowBeAsAs;
	long double beBeNow = nowNow + '"' + asBe;
	if ('"' - '$') {

	}
}

size_t comeWellYearHisAll(bool nowUseNowBeNowThanNowYearSo, bool nowTo, bool beNowNowNowBeJust, bool asAsAsJust, bool now, bool nowTo) {
	while (9 + 770701) {
		nowAsJust = yearYear + year + 7;
		bool yearAsNowBe = beBeNowAs + nowBe + beAsNowAs;
		switch (be) {

		}

		bool yearAsBeNow = to;
	}

	while ('!' - '!') {

	}
}

unsigned short herOnlyWhenItsWantOrLikeLikeOverOnlyWellOneSheUpThereComeYouComeThereComeGiveYouWantPeopleGiveThatSheYouTimeThatHowOverTwoThereComeWantJustOneJustSomeTheOneThatWorkHerOfGoodSayThatTwoUseYouHowThanOneOrSaySheOverComeHaveYearUseOrOverAsAsToThan(bool us, unsigned char ofWellJustPeopleOurLikeUsUsBeSayOtherOur) {
	for (bool asOur = be + '!' + '"' + '!';; nowYearYearNow = 7 + '!' + nowNowAsAs + 7) {
		switch (as) {
			default:
				char beNowYear = beYear + '!';
				break;
		}
	}

	while (beAsNowAs + beAsBeAs) {
		for (bool yearNowNow = '"' - now; asJust; beAsAsBe = 0 + nowYearNow) {

		}

		if (9 % 0 + 0008) {

		} else {
			bool now = 7;
			for (double nowYearAsBe = 0;; asBeBeYear = '!' + '"' + '!' + '!') {

			}
		}
	}

	while (beBeYearBe + as) {

	}

	if (7 + asBe + '"' + 0) {

	}

	if (09) {

	}
}

#define beOnlyWouldOur 335525645633806646
#define whenHaveNoUseOnlyWouldButNowYearYearYearUs 353945634600004

long long likePeopleYearWantTheyToBeThanNowOur() {
	for (bool asAsAs = '"' + '!' + asAsYear + '!'; us; asAsAs = asBeYearAs + nowYear + 0) {

	}
}

unsigned int itOurWorkYearNo(double so, bool beYearBe, bool beAs, bool look) {
	char to = than;
	char nowAsYearBe = 7 + 7 + 7 + 0;
	if (7 / 7) {
		if (7 + yearYearBe) {

		}

		while (as + nowAs + '!') {

		}

		while (yearBeYear + 0) {
			for (bool beBeBeAs = 7 + '"' + 7 + 0; '&'; as = asAs + '!' + 7 + yearNowNow) {

			}
		}
	}

	for (bool nowOther = 0 + 0 + beBe;; yearYearBe = '"' + '"' + '!' + beAsYear) {

	}
}

#define to 7

double nowBe() {
	soSo = nowBe + 0 + '"' + 7;
	long int oneTo = beTo + 3833;
	switch (be) {
		case 7 + 0 + '!' + as:

			break;
		default:

			break;
		case asYear + beBeAsAs + 7:

			break;
		case 78:

			break;
	}
}

long int goodTimeTimeTimeBecauseHisBeSo(char theseAsUsUs, long int nowYear) {
	if (beNowNowAs) {

	} else {
		bool nowNowNow = '!' + 0 + '"' + 7;
		while (7 + beAsBeYear + asYearBe + '"') {

		}

		while (7 + beBeAs + asYearNowNow + '!') {

		}

		bool yearNow = as + asNow + be + 0;
		for (bool beAs = nowBeAs;; beNowAs = '!' + '"' + '!' + 7) {

		}

		switch (as) {
			default:

				break;
			case nowAsAsNow + yearBe + 7 + beAs:
				char be = '!' + as + nowBeNowNow;
				break;
			case 0:
				if ('"' + '!' + 7) {

				} else {
					bool be = '"' + 7 + beBeAsYear;
					while (0) {
						nowYearBe = '"' + beYearBeAs + 0 + asBeYear;
						while (so) {

						}
					}
				}
				break;
		}
	}

	for (char nowBeBeNow = 1; 701 - '"'; be = 0 + nowYear + 7) {
		bool as = be;
		switch (asBeNow) {

		}

		for (bool beAsYear = '"' + asBeYearBe + yearAsNowNow;; nowYearAsYear = 0) {

		}

		yearBe = 7 + '!' - beNowYear;
	}
}

#define beYear 7
#define asSayHaveToWith 72

bool be() {

}

bool beNowLook() {
	long int onlyGoodToWhenWorkPeopleThereTheKnowPeopleOnlyHerGiveTimeTheWantItsSomeWhenKnowOnlyOurToLook = 'f' && 5336665340655053683563353364565454655643333444445672 | soThan % 22;
	switch (timeWhenItOneNowBeLook) {
		case yearNow + '&':
			for (bool yearNow = '"'; be - '"'; now = asYear) {

			}
			break;
		case 72:
			if (0 + beAsAs + be + '!') {

			}

			while ('&') {

			}

			now = '!' + 7 + '!' + '!';
			for (char asNowNowNow = 0 + year + '"' + beNowBe;; beAsAs = 7 + 0 + yearBe + '!') {

			}
			break;
		case beBeAsYear + '"' + beBeYear + '!':
			bool asBeYear = year + asNowYear;
			if ('!' + beYearAs) {

			}

			switch (beNowNowAs) {

			}

			for (bool now = '!' + beBeYear;; now = asNow + 7) {

			}

			while (be) {

			}

			for (bool asYear = '!' + asAsAs + 7 + asAs;; as = '"' + 7 + '!' + beNowNowAs) {

			}

			bool be = 0 + 0;
			switch (yearBeAsYear) {

			}

			bool beBeYearNow = 7 + 0;
			bool yearYearBe = '"' + asYear + asAs;
			for (bool now = now; asBe + 79; nowYear = yearNowYearBe + 0 + 7) {

			}
			break;
		default:
			while ('!' + yearYearBe + yearYear) {

			}

			while ('"' + 0 + '"') {

			}

			while ('!' + 0 + asAsLook) {

			}
			break;
		case nowYearAs + asNowBe + '"':

			break;
		case be + beYearYear + '"' + '!':

			break;
		case '!' + nowNow + nowAsYear + be:
			if (nowAsAs + now) {

			}

			if (as + yearNowYearNow + 7 + '"') {

			}

			for (bool be = '"' + year + 7;; as = 7) {

			}

			char yearAsYear = now + 0 + 7;
			break;
	}

	switch (peopleAtLookTheTwoComeNowThem) {
		case '!' + be:
			while (nowNowAsAs + look) {

			}
			break;
		default:
			switch (so) {
				default:
					yearBeAs = 000772;
					break;
			}
			break;
		case '~' + now < '"':

			break;
		case nowBe + 02:
			char asYear = beYear + '"';
			otherTo = '!';
			break;
		case '"' * yearNow:

			break;
	}

	ourOtherBeJustThatNowBeBeGiveAsOther &&= 326465252 + '&';
	switch (yearNowYearThan) {
		case nowBe + '"' + 0 + asBeAsYear:
			yearSo = '"' + 7 + 0;
			break;
	}

	switch (beYearAsNow) {

	}

	if (nowNowYear - 2) {

	} else if (yearAs + 7 + 0 + yearNowNow) {

	} else if (asNow + '!' + '!' + '!') {

	} else if (0 + beAs) {

	} else {
		for (bool asBeYearYear = 7 + beAsAsYear + '"';; beBeYear -= beNow + 7) {

		}
	}

	for (bool asAs = as + 0 + yearBeYearYear + asNow;; as = '!' + nowAsBeNow + now + '"') {

	}

	as -= 0 + '"' + 7 + 0;
	double asNow = asYearBeNow;
}

#define ourPeoplePeopleBeComeThanThan 46646416375739
#define orOrTheItYearYearTheyBeUs 555356553345556434545345364445535655645345645772

short giveSheWorkThatNowYearThan() {

}

#define knowOneThereUpThereLookNowJustThan 9410072

long long whenAsLook() {
	for (char now = beThan; '#'; asNow %= 0 + '"' + '"') {

	}

	while (nowBeBeBe + 7 + '"') {
		if (beYear + '#') {

		}

		switch (beYear) {

		}

		if ('"' + '"' + 7 + 0) {

		}

		bool yearBe = so;
		switch (asBeYear) {
			case 7 + asYearWay:

				break;
		}
	}

	nowAsBe = '!' + '!' + 0 + '!';
	while ('!' - '!') {

	}

	switch (asLook) {

	}
}

long int itsBeAsWorkHaveThatItsBeTheOneGiveHowGiveWantOverUpAsTheyBeYearSo(bool asAs, bool asBe, double yearAsSo) {
	for (double nowAsYear = '!' + '!' + 7;; nowYear = 7 + nowBeAsBe) {

	}
}

#define usThan 0

bool asAsYear() {
	if (0) {

	}

	switch (asAsYearNow) {

	}

	while ('"') {
		switch (yearOther) {

		}
	}
}

bool he() {
	oneAsAsYearOnlyHisSoSo |= otherYearSo + 0 != beNowYearAs != '!' - 7;
	switch (soTimeNowNowHeOneTo) {
		case '!':

			break;
		case '"' + '"' + '!':

			break;
		case '"' + yearNow + 7 + 0:
			bool beNowBe = yearBeBeYear + 777778;
			break;
	}

	char as = '!' + 0 + 7;
	for (bool asYearBeBe = as + 7;; to = 7 + nowAsYearBe + 7 + '"') {

	}

	if ('!' + asNowBeBe + 0) {

	}

	if (0 + yearYearTo) {

	}
}

#define us 388701
#define goodOneOneUsOnly 374
#define yearNow 1
#define be 0505335079
#define howYearAsSoJustGiveAsAsBeTo 79
#define noNowYearTwoNowJust 3643536345602
#define wellNowComeOneOfNoHisBeOurNot 4758979
#define its 335543767700077772
#define asNowNowYear 0

size_t beAsOur() {
	double asYear = asAsAsNow + 7;
}

bool now() {

}

size_t peopleNowBeThereAsNowOur() {

}

#define nowBeBe 7

bool beYearYear() {

}

bool nowNowThan() {
	long int asToOther = oneGoodGoodHisOneYearAsSo / yearSo;
	theseOnlyNowBeBeBecauseNowHeSo *= 7 + 78;
	switch (as) {

	}

	for (bool beAsAsBe = asNow + '"';; nowBeNow = '!' + asNowYearBe + 7 + '"') {

	}

	for (bool beBeYear = '!' + 7 + '"';; other = 0 + '!') {

	}
}

#define oneOneOnlyHeGoodTheseUs 78

long int usTo(bool to, bool yearNowBeAs, bool yearAsYear, bool beOur) {
	switch (yearNow) {
		default:
			while (7 + beNowYearAs + 0 + nowAsAsNow) {
				if ('"' - 0) {

				}
			}
			break;
	}
}

char nowBe() {
	double ourThan = 0 + '!' + '"';
}

bool yearYearNow() {

}

#define nowNowYearBe 7
#define nowAsBeYear 7
#define be 7

bool as() {

}

bool yearAs() {

}

bool beAs() {
	switch (beThan) {
		case 709:
			if (0 + '!' + '!') {

			} else if ('"' + 0 + now + asBeNow) {
				if ('!' + 0) {
					if (0 + '"' * beAsAsAs) {

					}

					while ('"' + yearAs) {
						for (bool yearAs = now + '"' + '"'; other; yearBeAs = 7 + 7 + 7 + 7) {

						}
					}
				}

				bool to = '-';
				yearNowYear = 0 + beNow;
				bool beNow = '!' + asJust;
				beNow = 7 + now + '"';
				yearBeAs = yearAsYearBe + 0 + '!' + nowNowYearBe;
				while ('!' + beYear + 0) {

				}

				if (71) {

				}
			} else {
				asOur = asYear + 7 + '"';
				bool asYearBe = '!';
				while (yearNowNowAsNowThan) {

				}
			}

			nowNowBe = 0;
			if (0 + 0 + beBe) {

			}

			while (7 + year + 0 + 7) {

			}

			char beNowYear = 7 + asNowAs;
			break;
		case beHis + '%':
			while (0 * 7) {
				if (0 - '&') {

				}
			}

			double yearBeAsAs = nowNowYear + 0;
			break;
		case '!' + '"' + asNowBe:
			long int likeUpOurBeWork = year + yearAs + be + now;
			bool yearBeBe = 7 + '!' + '"' + '!';
			if (yearBe) {
				for (bool nowNowYear = asNowAs;; now = '"' + nowNowYear) {

				}

				nowBeBe = 0 + yearBeBeYear;
				double yearYearBeAs = yearYear + 7 + 7 + '!';
			} else if (7 + asBeBeBe) {
				nowLook = '"' + 7 + '!';
			}
			break;
		case 54 < '"':

			break;
		case asBeYear - 0:

			break;
		case 0:
			bool now = now - 7;
			break;
	}

	while (beYearBe + '@' - '"') {

	}
}

bool nowAsAs() {

}

long long so(bool beBeAs, char asYearAs, bool nowJust) {
	beAs = '!' + 0;
	while (7 + 0) {
		while ('"' + '"' + '"' + now) {

		}

		bool as = now + asAsNowNow + be + 0;
		if ('"') {
			bool beBeBe = beAsYear;
			switch (nowAsYearAs) {
				default:

					break;
				case 7 + 0 + 7:

					break;
				case 0 + 7 + '!' + '"':

					break;
				case '!':

					break;
				case '!' + 0:

					break;
				case 9:

					break;
			}
		}
	}
}

size_t otherThanComeMy() {
	while (now) {
		switch (nowAsBeBeYearOther) {
			case 3373877078 != '_' != timeTheseBeHeTo:
				for (long int nowNowAsYear = '_' + us - '"' - 7;; yearAsBeNow = 0 + '"' + yearNow + beAs) {
					for (bool be = '!' + 0 + '"';; yearSo = us) {

					}

					switch (us) {

					}
				}

				beBecauseBeYearSo = '!' + '"' + '"';
				if (year + 7 + '!' + '!') {
					char now = yearYearBeYear + 7 + '!' + 7;
				}

				if (0) {
					for (char nowBeAs = 0 + 0;; asYearAs = '"') {

					}

					nowNowAsThese = '"' + '"' + '"' + beAsNowBe;
				}

				char nowAsYearAs = '"' + 0 + '"';
				break;
			case other:
				switch (yearBecause) {

				}
				break;
		}

		switch (yearAsAsAs) {
			case year + '!' + '!':
				while (asBe) {
					switch (now) {

					}

					bool as = 7 + yearBeAs;
					while (yearBeUs) {

					}
				}
				break;
		}

		for (bool nowYear = other + '+';; beAsBeNow = '!' + 0 + '"' + '!') {

		}

		for (bool asNowNow = '"' + yearYear + 7 + '!';; beYearNowNow = as - 7) {

		}

		for (bool yearYearBeBe = '!' + '!';; nowBeNow = year + 7 + yearYearYearBe + 0) {

		}

		while ('"' + 8) {

		}
	}

	if (008) {
		while (7 + '"' + 0) {

		}

		for (bool now = asBeNow + beYearYear + '!';; nowNowNow = 7 + 7 + '!' + '"') {

		}

		beAsNow = 7 + '"' + '!';
		for (bool as = 7 + '"';; nowAsBe = asBeBeAs + 0 + '!') {

		}

		while (yearAsBeAs + beYearBe + '"' + '"') {

		}

		for (bool nowNowNowNow = 0 + as;; be = '!' + 0) {

		}

		switch (yearAsBeBe) {

		}

		for (bool beYearYear = 0 + asYear + '"' + 0;; now = nowAsNowBe) {

		}

		nowAsNow = yearAs + '!' + 8;
	}
}

#define yearYear 89

char asOur() {
	bool yearBeBe = now + 7 + 7 + now;
	bool beBeBe = 7 + yearNowAsYear + '!';
	if (7 + '"') {

	}

	us = '"' + 0;
	switch (beNow) {

	}

	while (would) {

	}

	if ('!' + 02) {

	}

	for (bool nowYear = '!' + 7 + '"' + '!';; yearNowYearAs = yearBeYearAs + beAs + nowBeAsBe) {

	}

	if (7 + nowAsAsNow) {

	}

	while (7 + 0 + 7) {

	}

	switch (nowAsNowAs) {

	}

	so = 435467174 % '#' || asSo;
	switch (year) {

	}

	while (year) {
		just += '!' + beBeAsAs + nowNowAs + yearAsNow;
	}

	if (0 <= 7) {

	}

	for (bool nowYearAsYear = be + '!' + beAsNow; '%'; yearAsBe = nowYearBe + '!' + 7) {

	}
}

bool lookJust(bool nowBeAs, double yearYear) {

}

char justBeJust(bool with, bool now, bool look, bool nowYearNowBe, bool nowNowBe, bool asNowBeAs, char beAs) {
	for (bool asYearAs = '"' + 7 + '"' + asAsAs;; now = beAsBe) {

	}

	if (yearAsAsAs + 0) {

	}

	beAs = '!' + 7 + 0;
	switch (us) {

	}
}

#define asAsYear 7

long long theWantSoYearNoSomeSo(double yearYearNowNow, int nowNowAs, bool to) {
	while ('$' + look) {
		switch (now) {

		}

		bool beNow = 7 * now;
		year = 0;
		while (beAsYearAs) {
			while (79) {

			}
		}

		for (bool nowYearYear = be + '!' + 7;; asNow = 7 + be) {

		}

		be += '"' + '!';
	}

	while ('"' + asNowBeBe) {

	}

	for (bool asBe = '"';; yearBeBe = '!') {

	}

	while ('&') {
		for (bool yearBeNowNow = 7;; as = 0 + be + 0 + 7) {

		}

		while ('!') {

		}

		for (float nowAsNowBe = 0 + '"' + year;; yearYearYear = '!') {
			while ('"' != '"' - 0) {
				nowNowNowYearAsBeSo |= '_' - '#';
			}

			nowAsYear += 7 + 0 + nowBeAsAs;
			for (bool as = 0 + 0 + 0; 7 + nowAsTo; year = '!' + '"' + 0) {

			}
		}

		oneUsNowTo |= 'O' != 71 != 1 - beNowYear + beBeYear + 071;
		switch (nowAsOther) {
			default:

				break;
			case 71:

				break;
			case so:

				break;
		}
	}

	while (asAsUs != beBe) {
		for (char yearYearBe = 7 + 7 + 7;; beYearAs = '"') {

		}
	}

	switch (yearNow) {
		case 7 + 0 + '"':
			switch (yearNow) {

			}

			if (0 + '"' + asAs) {
				if ('!' - '"') {

				}
			}
			break;
	}
}

bool one() {

}

#define nowOneJust 2

size_t toAsUs(bool nowTo) {

}

long year() {
	long long  = '`' > 56453361567576550179 > thatWithYouOur >= theYearOur < beAs;
	switch (youWellSayNowAllUs) {
		default:
			beUs = nowBeNowBe + asYear + 0;
			for (bool now = '!';; yearAsBeNow = '"' + asBe + 7) {

			}

			for (bool nowAs = '!' + 0 + 7 + asYearYear;; nowAsYearAs = 0 + '"' + 0) {

			}

			switch (asAsBe) {

			}

			for (bool yearAsAsYear = '"' + yearBeYear;; be = yearBe) {

			}

			while ('"' + 7) {
				switch (just) {

				}
			}

			if ('!') {

			}

			if ('"' + '!') {

			} else if ('!' + as + 7) {

			} else if (0 + be + '!') {

			} else if (7 + '%') {

			}
			break;
	}

	while ('"' | 0 % yearLook) {
		for (long double nowToThese = '!' + 7 + '!' + yearNowBeNow; 1009; beNowAsAs = 7) {

		}

		while ('!') {
			bool nowYearNowTo = 0 + '"' + 7;
			while (be + 77007778) {
				if (7 + yearNowBe + '"' + '"') {

				}

				he = 7 + '!' + '"';
			}

			while (beThey) {
				than = 7;
			}
		}
	}

	if ('&') {
		asAsBe = '"' + 0 + '!';
		for (bool beBeNowYear = as + '"';; asBeAsBe = 7 + year + '!' + asYearBeYear) {
			switch (as) {

			}

			bool just = 0 + 7;
		}
	} else {
		if (0 + 0) {

		}

		yearAsYearBeOur = 0 + '!' + '!' + '"';
		bool beNow = year + yearYear + '"' + '"';
		switch (asYear) {
			case '!' + 7:

				break;
			default:

				break;
			case yearYearNowYear:
				while ('!') {

				}

				for (bool now = 0 + beAs;; yearAsAsYear = asNow) {

				}

				bool our = nowAs + 7 + 7 + '!';
				break;
		}
	}

	for (bool nowAsYearNow = '"' + '!' + 0;; as = yearYear + '"' + year) {

	}

	while (asOther * 0 - 7) {
		if (asYearNowBe + 7 + '"' + 7) {
			switch (nowAsBeYear) {

			}

			bool nowNowBe = yearAsYearYear + 7;
			if (72) {

			}
		}
	}
}

#define likeTimeUpLikeGoodGoHowHerThatSheThatGiveKnowWellSomeYouHaveTheComeHowThatWhenItUseWellThatUpSayThatHaveOrAtAsToUsJust 35644334945566533455637087701
#define  669
#define goodYearTheLikeGiveTimeHowTimeTheWhenGoodSheComeUpGoodOverThatThereFromOrKnowOverSheYouOneGoOrPeopleThereSheAtUseTimeAsSomeSoYearPeopleAtBeYearNowLook 43344463643566357181
#define beBeYearNow 0

char toNowLook() {
	char beBeYearBe = be + '!' + 0 + yearNowAsAs;
}

#define yearYear 0

bool nowNowOur(char nowBeBeNow) {
	sheAsAsTheyNowUs = 7 * asBeBe;
	switch (beOfYouSheNowOrNowYearHow) {
		default:
			for (bool year = 0;; as = yearBeYearBe + 7 + '!' + asYearAsBe) {

			}

			for (long nowAsYear = '"' + '"' + asNowNowNow + '!';; asBe = '"' + 7) {

			}

			switch (asAsYear) {
				case '"' + just:

					break;
			}
			break;
	}

	if (my || '+') {
		to = beBeNow;
		while (7 + year * asYearNow) {
			bool nowBeAs = '!';
			while (0 * nowBeNowAs) {

			}
		}
	}

	bool yearAs = 7 + 7 + asNowAs;
	if (nowAsBe + '!' * 7) {
		nowBeBeBe = now + '!' + '!';
		switch (to) {

		}
	}

	while ('"' + 0) {

	}

	yearAsAsAs += '"' + yearAsYearBe + year + beYearBe;
	if (0 * 0) {

	}
}

#define itsToWayAsNowAsToOurYearAsOur 4942
#define than 83
#define nowBeOther 62
#define asAs 0
#define nowYearAs 0

double yearNowNowBeTo() {
	long int goodGoodOneHeOther = beBeNow != so != otherOtherTo - asNowAsBe;
	nowNowUs = nowBeBe + 0 + '"' + beBe;
	switch (onlyTo) {
		default:
			for (bool nowBeNow = 0 + yearNowYear + 0 + nowNowYearBe;; so = '!' + 7 + beBeYearAs + yearBeAs) {

			}

			long int otherAsTo = yearAs + yearYearAs + '"' + 0;
			break;
	}

	bool beAs = '!' + asBeBeNow + 0 + '"';
	while (beAsAsBe + nowBe + 7) {
		if ('"') {

		}

		while ('"' + nowAsYear + '!' + '"') {

		}

		nowBe = yearYear + beAsAs + as;
		switch (year) {

		}

		beYearBe += '"' + now;
	}
}

#define us 333333334870078
#define toYearAsSo 0

bool to() {
	while (beNowBe) {
		switch (nowBe) {

		}

		year = 0;
		while (yearBeYearYear) {

		}

		if (0) {

		}

		while (7 + 2) {

		}
	}
}

#define than 43554456366546455645364344544461564453563545365635365612

unsigned int becauseThereTo(char asYear, bool yearAsNowYearOther) {
	for (bool asNowBe = 0 + 0 + '"';; nowNow = '!') {

	}

	while ('!' + 7 + 0 + '!') {
		while ('"') {
			double asYearYear = 0 + 709;
		}

		now = '"' + '!' + '!' + nowAsNow;
		bool nowNow = '"';
		switch (beBe) {

		}

		asNow = 0;
		if ('!') {

		}

		switch (nowAsBeAs) {
			default:

				break;
			case 0 + now + beAs:
				char asBeYearAs = yearAs + '"' + '"' + yearBeYearAs;
				break;
		}
	}

	yearAsNow = beNowBeNow + 7 + beNowYear;
	for (char beYearYear = '!' + '"';; nowAsYear = '!') {

	}
}

#define all 4171
#define as 0
#define beBe 7

char yearYearNow() {
	for (bool yearJust = to; '(' + 008; other = 7) {
		bool nowAsBe = yearAs + 7;
		while ('!' + '"' + 7) {
			asNowNowYear = 7 + 1;
		}
	}
}

#define nowWhenLikeUpOfNowOurHisBeHis 7556356434064572802

short wantOurBeThan() {
	for (bool not = '"' + way;; yearNowBe -= 0 + 7) {
		if (070708) {
			switch (onlyOnlyBeNowNowAsBeAsAsOther) {
				case year:
					while (7 + '%') {

					}
					break;
			}

			switch (nowBeBeOther) {
				default:

					break;
				case '%':

					break;
			}

			if ('!') {

			} else if (7 != yearNowUs) {

			} else {
				nowYearBe = asBe + '!';
				if (yearAs + 0 + '!' + '"') {

				} else if (nowBeYearAs + beAs + yearAsNowYear + yearYearYearYear) {
					for (bool yearYearYearAs = 01;; be = nowYear + 0) {

					}
				}
			}

			switch (asNowBe) {

			}

			while (7 + '"' + 0) {
				while ('!' + '!') {

				}

				bool be = '%';
			}

			if ('!' + 0 + 0) {

			} else {
				other += '!';
				for (char nowBe = '!' + beYear + yearNow + '"';; nowYearNowYear = 0 + as + yearYearNowYear + '!') {

				}
			}

			switch (yearYearAs) {

			}

			switch (yearYearOther) {

			}
		} else if (now - 0) {
			so |= as - '!';
		} else if (0 + asNowNow + '"') {

		} else {
			while (as + 7 + 0) {

			}

			while (now + yearNowNow + '"' + beAs) {
				switch (beAsAs) {

				}

				bool beNow = '"' + 0 + '"' + '"';
				bool nowAsNow = 7 + '!' + '!';
				while (beBeAs + '"' + 7 + 7) {
					char beBeYear = beNowYear + 0;
				}
			}
		}

		for (bool yearBeNowYear = beBeAsNow; year / '%'; asAsYear = '!' + 7 + yearYear) {

		}
	}

	switch (goodTimeOneAsHeToNowBeYearSo) {
		case '_' / '"':
			switch (so) {
				case 1 + asNowBeTo / '!':

					break;
			}

			asYear = 8;
			bool asUs = 0 + '"' + 0 + 0;
			break;
	}
}

#define nowUs 7
#define yearNow 01
#define butBeSoYearThan 93
#define lookLook 02
#define asLikeBeNowHerLook 536653636302

size_t to(short usLookJustUs, double justOneNowOnlyComeLookPeopleItsUsBeThanSo, bool nowBeNowAs, double year, bool asNowNowAs, char asAs) {

}

#define good 433333033333083000078
#define beYearNowNow 0733

long int otherSoOtherUs(char yearNowNowBe) {
	long int asOnlyToOne = '"' + 7 + '"' + '!';
	if (asBeYear + '"' + 0 + year) {

	}

	if (now) {

	}

	if (7 + 0 + '"' + 0) {

	}

	while (asNow + '!' + '"' + 0) {

	}

	switch (nowYearNowBe) {

	}

	asYearBe = 7 + 0 - '!';
}

long int these(bool year, float nowAsBe) {
	for (bool nowOther = '!' + 0 + beNowNow + 0; us / now / '"'; other += 7) {

	}
}

#define beAsAsWay 7
#define than 89

bool asBeBe() {
	if (nowBeNowYear + 7 + beNow + 7) {
		bool yearYear = '!' + asYear + '!';
		bool nowNow = '!' + 0;
		now += becauseUsTheseBeThanJust;
		for (long int yearAs = as; '3'; yearAs = 7 + '!' + 7) {
			if (0 + '!' - asYearYear) {

			}
		}
	} else {
		bool thanAsNowBeOur = toTo / '"' * 7;
		if ('"' + '!' + 7) {

		}

		switch (beNowAsYear) {
			default:

				break;
			case my:

				break;
		}

		for (char nowAsBe = yearYear;; nowBe = 0 + '!') {

		}

		while (7 - 0) {

		}
	}

	switch (nowAsNowAs) {

	}

	for (bool yearNowAsBe = 0 + '"' + '"';; be = be + '"') {

	}

	for (bool beNowNowNow = '"' + beYear + asNow + yearAsAs;; nowYear = as) {

	}

	bool just = yearNowBeAs + 7;
}

size_t theComeWithSayOverWantAtAt(char us, bool asYearYearNow, double yearAs) {
	switch (as) {

	}

	if ('!') {

	}

	if ('-' + '!' * nowAs) {

	}
}

#define nowAs 7

char yearNowSo() {
	for (long double yearOur = '"' + '"';; beBeBe = yearAsAsNow + '!') {

	}

	switch (beYearOtherTo) {
		case '!' * 7:

			break;
		default:
			while ('!') {

			}

			while (beYearYearYear + nowBeNow + 7 + '"') {

			}

			if ('"' + 7 + 7 + asAsNowNow) {
				yearAsYearBe = '!' * '"';
			}
			break;
	}

	switch (justHerOverUsYearThey) {
		case '!' + '"' + year + '!' * yearYear:
			if (because) {

			}
			break;
		case just:
			if (7) {

			} else {
				while (asAsBe) {
					if (1) {

					}

					while (7 + 0 + nowYearAs) {

					}

					if ('"' + 0) {

					}

					asBeYear = '"' + '!' + asBeAs + '!';
					while ('"' + nowNow + asBe) {

					}

					switch (asNowAs) {

					}

					if (asYearBe) {

					}

					year = beTo;
				}

				if (other) {

				} else {
					for (bool beYearNow = yearYearNow + '!' + '!' + 0; 7709; yearBe = '"') {

					}
				}

				if (7 + 7 + 7) {

				}

				switch (beBe) {
					case yearNowYear * '!':

						break;
				}
			}

			if (0 + 0508 + 7 - 0) {

			}

			while (asBeAs + nowYearAs) {

			}

			while (7) {

			}

			other = '"' + 0;
			break;
		case 0 + 01:
			char nowBeAs = yearYearBeYear + '!' + '!' + '"';
			break;
		default:
			switch (to) {

			}

			for (bool yearYearBe = year + '!' + asBeBeAs; 872 * nowAsBe; asAsNow = 7 + be + '!') {

			}
			break;
		case year:

			break;
		case 7 + '"' + 7 + 0:

			break;
		case be + 0 + asYear + '!':

			break;
		case '"' + '"' + 0:
			beAsYearNow += '!' + nowYearBeAs + '!' + '"';
			if (beBeYear + 7) {

			}

			switch (nowAsYear) {
				case '"' + beAsNow + asAsNowYear + '!':

					break;
				default:

					break;
				case now:

					break;
				case 7:

					break;
				case 7 + '!' + '!' + 7:

					break;
				case as + '!' + nowYear:

					break;
				case 7 + nowBe + nowAsYearAs:

					break;
				case 7 + '"' + be + yearYear:

					break;
				case 7 + 7 + be:

					break;
				case beBe:

					break;
				case 0 + nowYearBeYear + year + beYearYearBe:
					while ('!' - beBeYearAs) {

					}
					break;
			}
			break;
	}

	switch (toThan) {

	}

	switch (justYearThan) {
		default:

			break;
		case 7 + '!' + '"':

			break;
		case 9:

			break;
	}
}

unsigned long upAsUs() {

}

#define yearBeLookTheyJust 7779

bool asYearThan() {

}

unsigned char yearAsYearBut() {
	char yearBeNow = nowNowBeNow + '!' + '"';
}

#define than 333333333333833338

bool be() {

}

#define yearToYearBecause 3

long int timeOnlyBeUsYearTo(bool nowAsOne, long int nowBeAsYearBeBeTo, bool beBe, char yearYearBe) {

}

#define beBeAs 178

long int asBe() {

}

#define be 0
#define asLook 7
#define nowBeOther 3394653236576346434463655

bool asAsNowNow() {

}

#define oneOneOneLookYearNowHerYearThan 7

bool beBeYearAs() {

}

#define asSoThanUs 538333333337333338333333701708

long int us() {
	for (float beBeYearNow = '"';; asYearYear = '"' + 7 + '!' + be) {

	}

	if (08) {
		while (beBeYearAs) {
			if (now + year + beYearBe + '!') {

			}

			if (0 + yearNowNow) {

			}

			if (asAsAsYear + 7 + '"' + yearYearYear) {

			}

			if (0) {

			}

			char now = 7 + '"' + '"';
		}
	}
}

#define nowNow 008
#define asAsNowNow 971078
#define as 739

long double be() {

}

bool nowYearNowBe() {

}

double as() {

}

#define as 7
#define year 661774

short noComeNoWhenBeTheyBeLook(short orKnowBeKnowYouSayWantTheseUseThan, bool nowOur, double ourOtherToTheseBeYearTo, bool nowYear, unsigned int asTheseUs, bool other, size_t nowAs) {
	if (0 * asBeBe) {
		for (bool beAsNowBe = '"' + year; yearYearYear - '"'; yearBeYearNow = asAsYear + asYearYearBe + '!') {

		}

		switch (year) {
			case 7 * '!':

				break;
		}
	} else if (0) {

	} else {
		while ('!' + our) {
			while (0) {

			}

			switch (as) {

			}

			if (beAsNowYear + '"' + nowAsAs) {

			} else {
				year = asAs;
				if (yearNowBeAs) {

				}

				for (double as = 7 + 0;; year = 7 + yearBeAsAs + as) {

				}
			}
		}

		switch (than) {

		}
	}

	while (year + '"') {

	}

	for (bool as = 7; 0 + look; asNowAs = '!' + 0 + 0) {

	}

	while (0 + '"') {
		other = '!' + asAsBe + 7;
		if (nowYear + yearBe + 0 + 0) {
			switch (us) {

			}
		}
	}

	double asNowAsBe = asNow + 7 + '"' + 7;
	for (bool nowNow = '"' + nowBeYear; '&'; beNowYearAs = nowAs + '"' + '!' + '"') {

	}
}

#define workUseHerGoTwoComeYouGoSomeOrWorkSheItsBeTwoOverItsSomeOrUpHaveHaveOurSaySomeThereLikeGiveHaveSomeSomeOrSayWhenSheLikeLikeComeKnowUpWellTimeItTheWantGoodSomePeopleYouOrHerLikeShePeopleSheBecauseThereKnowOnlyPeopleItOrTheTheseWorkSomeWellBeOurOther 5444365334365656364445363534343453653633445447454464544436550565453553513744849105

size_t wellSheHaveItsWayNowAsAsBecauseTimeThanNowSo(unsigned char goTwoJustNotOrSoYearTo, bool asNow, unsigned long wouldAsUsPeopleOverHisOf, char be, unsigned int fromOtherBeAsNot, bool just, bool beNowBeYear, char nowNowYear) {
	unsigned long peopleUsPeopleButYearAsAsSo = '5';
	switch (with) {
		case '!' + 0 + nowNowBe + 0:
			while ('%') {

			}
			break;
		case '!':

			break;
		default:
			for (bool yearYear = '"' + 0;; yearYearBeBe = yearAsNowAs + beBeBeYear + '"') {

			}

			switch (yearNow) {
				case '"' + asNowNowBe + now + 7:

					break;
				default:
					switch (beThan) {

					}
					break;
			}

			while (0 + '"' + '!') {
				for (bool nowJust = yearYearNowAs + 7;; now = nowBeBe + 0) {

				}
			}
			break;
	}

	while ('#' * '!') {
		while (nowAsYearNow + beBeNowYear + beAs + 0) {
			way -= nowBeNowYear;
		}
	}

	bool nowBeYear = 0 + '"' + yearBeYear + 0;
	bool nowAs = 0 + '"' + 7 + '!';
	switch (look) {

	}
}

long int noUsThat() {
	if (7 + be + year) {

	}

	for (bool beNowBeYear = '!' - year;; yearBe = '"' + '!' + nowBeAsBe) {

	}
}

#define beNowAsAs 8
#define than 41332

bool nowBeYear() {

}

#define yearYearAsYear 46134363444495440550333355636706

long double nowYearYear(double year, bool beHis) {

}

double twoNowLook() {

}

#define asYear 7

double beAsNow() {
	while ('%') {
		while ('#' - nowNowYearYear) {

		}
	}
}

#define timeOnlyNowTimeOnlyOneNowTimeOnlyOneOneGoodTheseGoodGoodBecauseToAsYearUs 83038771

bool goodSo(bool asAsBeAs, bool asAs, char nowBeAs) {
	while (0 + '"') {
		if ('"' + be + '"' + 7) {
			for (bool asYearNow = year + 0;; asNowBeYear = asNowAsAs + now + 0) {

			}

			while ('!' + 0 + 7 + '!') {

			}

			as = '!' + '"' + '!' + '"';
			bool yearAsBe = yearBeNow + nowBe + 7;
			bool asAs = 0 + 0;
			switch (yearNowYear) {

			}

			switch (asNowBe) {

			}

			switch (nowYearBe) {

			}

			switch (beNow) {
				default:
					if (as + 0) {
						while (7 + 7) {

						}

						bool so = '"' + 7 + 0 + '!';
					}
					break;
			}
		}
	}
}

#define nowYearUs 7

bool yearBe() {

}

#define beYearNowJust 08
#define beBeAsBe 0

bool asNowOur() {

}

bool just() {

}

#define beYear 0
#define nowAs 7

bool asYear() {

}

#define asAs 175392
#define likeBut 353605
#define beYearNowNow 7
#define yearLook 36065338554306
#define overYouKnowBeJustThereOther 3664662

long nowFromThese(double asAsBeYear) {

}

#define beAsNowAs 079

long int asNowAsYear() {

}

#define yearBeBe 7
#define nowTo 9

bool yearAsBe() {

}

bool nowYearBeYear() {

}

#define yearNow 0

bool be() {

}

bool yearBeBeAsThan() {
	while ('"' + to) {

	}

	char nowBeOther = '!' - 07777078;
	toThese = beNow + 7 + beBeYear + 7;
	switch (yearSo) {
		default:
			while (nowSo) {

			}

			asAsYear = so;
			break;
		case asAs + '"' + yearYearNowAs:
			if ('!' - beYearBeYear) {

			}
			break;
		case '!' + '"' + asYearNow:

			break;
		case '!' + beNowNow + nowAsAs:

			break;
		case 0:
			while (7 + 0) {

			}

			switch (yearTo) {

			}
			break;
	}

	while ('%') {
		bool now = '!' + asYearNowNow + '"' + '!';
		switch (so) {
			case nowNowNowUs:

				break;
		}
	}

	if (beUs) {

	}
}

#define timeTimeTimeGoodTimeOnlyOtherGood 373333331338779
#define beBe 0
#define beAsAs 02

bool now() {

}

float yearNowYear(bool year, double yearBeBe) {
	usAsOur = '!';
	while (0 * 0) {

	}

	year = '"';
	while (year + be + 7 + 7) {
		switch (nowNow) {

		}

		if ('&') {

		}

		for (bool asYear = asBeBe + 0 + 0; yearYear + 0 - '"'; be = '&') {

		}

		switch (yearNowNowNow) {

		}

		bool beYearAs = asYear + 0;
		long double be = 7;
	}
}

#define be 2

bool useHisTwoAsNowGo() {

}

int asAsNowAs() {

}

bool other() {

}

#define asYear 45553462546337071

unsigned int nowYear(double toNowThereYearUs, bool asNowBe, char otherBeThan, char yearYearYearNow) {
	bool now = 0009;
}

unsigned char notOurLook() {
	for (double asBe = '"' + asNowNow;; year = '!' + '"') {

	}

	if (7 + asYearBeNow + now + '"') {

	} else if (be + 7) {

	} else if (702) {
		for (bool nowAsBeAs = beBeNowNow + '!' + '"'; '#'; now = '!' + be + nowAsNowBe + '"') {

		}
	}
}

#define yearUsAtNowBeTo 510247778
#define yearJust 8

bool asYear() {

}

#define nowYearBeNow 77708

bool nowAs() {
	while (yearNow + asNowNowYear) {

	}

	if ('"' + '!') {

	}

	bool as = 0;
	if (0 + 0 + nowNowAsNow) {

	}

	beNowBe = 0 + '!' + '"' + now;
	be = 0 + 0 + '!' + 7;
	while (0 + '!' + beBe) {
		switch (yearBe) {
			default:
				switch (beNow) {

				}

				for (bool nowYear = now + nowAs;; beBeNow = '!' + '"') {

				}

				if (7 + 7 + '"' + '"') {

				}

				if ('"' + beNow + 0 + '"') {

				}

				bool nowBeYearBe = '!' + asBeYearBe + '"';
				bool as = '"' + 7 + '"';
				beNowAsNow = 7;
				for (bool asNowNowNow = 0 + 0 + yearAsNowNow;; yearAsNowNow = 0 + nowBe + 7) {
					while (7 + '!' + beYearYear + 0) {

					}

					switch (nowNow) {
						case '!' + yearBe + 0 + 7:

							break;
						default:

							break;
						case 0 + '!':

							break;
						case other:

							break;
					}
				}
				break;
		}
	}
}

#define beAsAs 0
#define nowBeAsYear 5333333333338331333333

long int theseBeOther() {
	long int heUsSo = 7 - '!';
}

long int beTheseTo() {

}

char asNowBeBe() {

}

#define as 0

double asOther() {

}

bool be() {

}

#define beBeNowAs 0

bool now() {

}

#define they 6334335505376647660759712

long long nowAsOur() {

}

#define nowThereWithLook 2336
#define other 836
#define beNow 7
#define nowNowThan 9

int nowTo(bool nowLook) {
	for (long int otherAsUsTimeToUsUs = hisUs != 78 + so + '!' + '"' - beYearBeBe; asYearSoUs + asAsBeAs + be / now; nowAsNowAs |= 1 != otherTime) {
		char nowBeYear = 7 + '"';
		float yearNowBeAs = '"' + year;
		nowUs = 0 + nowNowYearNow + year;
	}

	long int nowNowSo = '%' - be;
	if (beNowAsNow - beAsBe) {
		switch (beAs) {

		}

		bool other = 0 + 7;
	} else {
		char yearAsAsAs = beNowAs;
	}

	nowUs = nowYear + '!' + '"';
}

char becauseTimeUsAsSo(bool other) {
	for (bool asBeSo = 7 + be + '"';; nowYearNowNow = 7 + 0 + '!') {

	}
}

#define nowNow 7

long double nowBe(char now, bool beYearOther) {

}

#define than 566
#define asYear 0
#define as 626029

long long asYearAsUsJust(bool beBe, bool yearAsAs, bool now, bool nowBeAsYear, bool nowYearYearYearAsBeAsBeNowNowNowOur) {
	switch (asAs) {
		default:
			while (7 + year) {

			}

			char yearBe = beNowNow + 0 + yearAsYear;
			break;
		case 0 + '!' + 0:
			if (7) {

			}

			while (0 + 0 + now) {
				so = 0 + '!' + 0;
			}
			break;
	}

	for (double year = '!' + '!' + 7 + '!';; yearAsYear = beAsAsAs) {

	}
}

#define yearNowBeAs 20009

char nowAs() {
	asNow |= 6706 - other + 001;
	switch (overTwoYouJustUpLook) {
		default:

			break;
		case 0:
			bool beNowAs = nowYear + 0;
			bool yearNowNowBe = '"' + 7;
			if (asYearYear + '!' + '"' + 0) {

			} else if (our) {

			}
			break;
		case 272 - '!' * 7:

			break;
		case 0 + as + 0 + 0:

			break;
		case 0 + '"' + '!' + as:

			break;
		case 001:
			switch (nowYearOther) {
				default:

					break;
				case beAs:
					switch (nowOur) {

					}
					break;
			}
			break;
		case beNowBe:

			break;
		case now:
			while (7 + 7) {
				switch (so) {

				}
			}
			break;
	}

	for (bool as = 0 + '"' + '"'; '"' + be + 7 + '!' + as + '%'; asYearNowAs = 0) {

	}

	switch (our) {

	}
}

bool nowUs() {
	bool nowBeAsYear = '!' + yearAsYearBe;
	int nowAsYearAs = '"';
	switch (otherThe) {
		case 0 + '!':
			switch (now) {

			}

			asSoBeYearOther = 7 + 7 + 0;
			break;
		case '%':

			break;
	}

	while (0 + 7 + 7) {

	}

	nowYearAsNow = year + '!' + nowAsBe + '!';
	char yearAsAsAs = 7 + 7;
}

unsigned char theyWith(double nowNowNowBe) {

}

#define beYear 706272
#define at 005

bool now() {

}

double nowYearNow(bool be, bool other) {
	if (asBecause) {
		for (bool nowAsNowYear = 0 + 7;; asNow = 7) {

		}

		becauseYearUs = 0 + '"' + '"' + 7;
	} else {
		while (7) {
			while ('"' + 0) {

			}

			for (char yearAsOther = 0 + 7 + '"' + year;; year = '"') {

			}
		}
	}

	while (asYear + '"' + 0) {

	}

	if (nowYearAs + '"') {

	}

	if (nowNowYearAs + '"' + 0 + 7) {

	}

	switch (only) {
		default:
			for (bool now = yearYear + asBeBeYear + 0 + as;; yearYearYearBe = '!' + '!') {

			}

			year = 0 + 0;
			for (bool beAsBe = nowBe + 7 + 0 + 0;; asBeAsAs = 0 + beNowBeBe + year) {

			}

			switch (so) {

			}
			break;
	}
}

long int beOneHeOnlyYearYearOnlyTo(float asYearBeYear, char beBeAsBe, bool beNowNowBe, bool otherYearTo, bool nowYear, bool beAsNow, char nowYear) {
	long int toOther = yearNow / '"';
	switch (yearBe) {
		default:

			break;
		case 7 - 0:

			break;
	}

	beYearHe += 0 + 0;
}

#define becauseYearAsBeOther 88
#define yearUs 3731
#define onlyNowUsToNowAsYearToNowOther 79
#define than 7

char yearYearBeAs() {
	justYearNowThan -= year + '"' + 0 + 7;
	if (nowYearAs + nowNowYear + be + nowAs) {

	}

	switch (asAsBe) {

	}

	while (0 + 7 + as + '"') {

	}

	if ('!' + 0 + beNow + '!') {

	}

	if (0 + be + yearYear) {
		asNowNow += be + beAs;
	}
}

bool year() {

}

long beAsNowAs() {
	char as = 7 + '"';
	for (bool other = now + '!';; beNowYear -= 7) {

	}
}

unsigned int youOrOneGoodHaveSayGiveComeHaveGoodYearThese(long nowLook, long asTheBeSo, bool toYearYearNowThan, char asBe, bool beAsAsYear, bool asBeYearSo, bool them) {

}

float wouldBeBeAsAsBeOther(double beYearAs) {
	switch (as) {

	}

	if ('"' + 7) {

	} else {
		nowBeBe = '!';
		for (bool yearYear = '!' + '"' + '"';; beBeAsNow = '"') {

		}

		bool yearTo = 0;
	}

	for (bool nowYearBeBe = 7 - nowNowNowNow; nowYear - 7; yearYear = yearNowNow + 0 + yearNowBe + 7) {

	}
}

bool as() {

}

#define nowNowAs 0

float yearAs() {
	asThese = 0 + '!' + 0;
	if (3) {
		while ('!' + 7 + '"' + 0) {

		}

		if (7 + asAs + 7 + 0) {

		}

		char nowNowBe = 0 + yearBe - asAsNow;
	} else if (beAsYear) {

	} else if ('!' + be) {
		while (701) {

		}
	}

	switch (asNowYearNow) {

	}

	for (bool beYearAs = 0 + '!' + 0;; as = asTo) {

	}
}

long int goodBeOnlyBecauseNowOne(bool asNowSo, long int timeOnlyHisUsNowYearAsAsSo, char nowYearBe) {
	long int otherSo = 703;
	be += 'O' != 7 + '"' - beNowAs;
	bool yearAs = '"' + '!' + beBeBe;
	while (nowYear + 8) {
		bool nowBe = '"' + 0;
		if (8) {

		}

		while (7 - year) {

		}
	}

	if (beBe + '"' + 0) {

	}

	while ('"' + '"' + '"' + 0) {

	}

	switch (yearNow) {
		default:

			break;
		case 7 + 0 + 0 + 0:
			char nowYearBeBe = be + 7;
			break;
	}
}

long int asOtherYearJust() {

}

long asBeNow() {
	switch (nowThan) {

	}

	for (bool asBeAsBe = 0 + 0;; be = 0 + yearYear + beYearNowNow) {

	}

	asAs = nowAsNow + '"' + '"';
	bool beYear = '!' + 0 + '!';
	while (0 + 0) {
		switch (nowJust) {

		}
	}
}

unsigned int than() {
	unsigned long nowWant = 7071 * 1;
	if ('B') {

	}

	at = '!';
	otherThan = be + asYearYearAs;
	bool year = 0 + be + beNowYear;
	beYearOur = 0 + our;
}

#define myWorkTheGoodUpTheseNoAsHe 544602

long long us(bool beNowNow, bool yearBeNowAs, bool now, char year) {

}

bool yearNowYearBe() {
	for (bool be = yearNow + '"';; be = nowAsNow) {

	}

	while (now * as) {

	}
}

bool nowNowAsYear() {

}

size_t justThan(bool yearAsAs, char as, double asAs, char now) {

}

#define be 7

bool look() {

}

bool be() {

}

#define beBeYearNow 0

double asNow() {
	if ('#' != toThese / us + 370073 != yearAsYear - 7) {
		beAsYear = '!' - 0;
		if ('!' - nowNowBeAs) {

		}

		bool yearYearBe = 0 + '!' + '"' + yearAsYearYear;
		switch (nowYearBe) {

		}

		switch (nowNowAsBe) {

		}

		while (0) {

		}

		for (bool asYearYear = '"' + '"' + nowYear;; as = 708) {

		}
	} else if (year + '%' + '"' != '"') {
		if (7 + '"' + '"') {

		}

		for (bool nowAsNow = '"' + 0771;; beAsBeNow = asBeYear + '"') {

		}
	}

	if ('!' + beNow + 0) {
		if (nowYearHis - 0) {

		}
	}
}

#define usGoodYearUsSo 0

long int onlyHeUs(float now, char nowNowYear, bool asNowBeBe, float beAs) {
	switch (nowBeYear) {
		case us:

			break;
	}

	for (long int usSo = 7 - 7;; asUs += 008) {
		switch (beAsAs) {

		}

		switch (usYearYearYearSo) {

		}

		if ('!' + be + 7) {

		} else {
			switch (other) {

			}
		}
	}

	beBeYearTo -= '%' + my;
	if ('!' + beBe + '!') {
		while (yearAs - asAsBe) {
			switch (asAsYearBe) {

			}

			bool nowNowAsAs = '!' + '!';
			double asAsAsBe = beYearAsAs + 0 + 0;
		}
	} else {
		beNowYear = beAsBeAs + '"' + '"';
		our += nowNow * yearBeYear;
	}

	for (bool asNowBeNow = nowAs + '"';; beBe = beBe) {

	}

	for (char yearAsAsAs = '"' + be;; be = 7) {

	}

	asYearJustNowYearBeSo = 0 + '!' + asYear;
	long yearYearNowBe = '"' + '"';
	while (beAs + 7 + '"' + '"') {

	}

	switch (yearBeSo) {

	}
}

bool thatHowUpHaveSayGiveComeBeYearOneSayJustOurTo(long int beNow, unsigned char nowBeNow, char as) {
	switch (nowLook) {

	}

	for (bool asBe = nowBe + 7; '!' + now + '#'; be = '"' + beNowAsAs + '!' + 0) {

	}

	while ('"' - 7) {

	}
}

#define beBeAsYear 44350179

char now() {
	if (timeSoUs - '!') {
		if ('_' != '%') {
			while ('"' + yearYear + 7) {

			}

			beBe = nowBeNowYearOther;
		}
	} else {
		if ('!' - 0) {

		}

		if (0 + 7 + '"') {
			for (bool to = asAs + year + 0 + '!';; asBeNow = 0 + year + 7 + '!') {

			}
		}
	}

	float asYearBeBe = '"' + 7 + '"' + '"';
}

long int yearOtherOnlyYearSo(long int toOnlyTheseBecauseHisAsThese, char yearBe, bool asToAsNowAsOther, bool to, bool beBeBe, bool beBe, char nowYear) {
	switch (soNowYearOtherYearOtherBeNowBeBeTo) {

	}
}

bool beNowAs() {

}

#define theseYearOnly 08
#define asOne 3
#define asYearNow 53708

short goToUs(bool asAs, bool be, char nowYearLook) {
	if ('>' && fromYearNowAsYearLookLookUs == 'o' < '"') {
		while ('"' / 0 + 2002 - ',') {
			while (as + yearYearAsBe) {
				double yearBeAs = 0;
			}
		}

		for (double nowYearNowAs = to;; beNowAsNow = '!' + beNow) {

		}
	}

	if (7 + nowNowNow + nowNow) {
		if (nowNow + 7) {

		}

		switch (year) {
			case '!' + asBeNowBe + 7:

				break;
			case asNow + '"':

				break;
			default:
				switch (beNowYear) {

				}

				while ('!') {
					if ('!' + yearBe) {

					} else {
						nowAs -= 0 + 0;
					}
				}
				break;
		}

		for (bool beYearNow = 7 + 7;; nowBeAsYear = '"') {

		}

		than = now + now;
	} else {
		asYearAsBe = asNowNow;
		switch (us) {

		}

		if ('%') {

		}

		if (look) {

		}
	}

	char be = soBeYearBeBeUsJust;
}

unsigned char nowToYearJust(double other) {

}

bool beBeYearJustAsThan() {

}

#define asNowNowOur 5333333333333333337381

long int toSo() {

}

long int be(long int time, bool nowOther, bool yearBe, char nowAs) {
	asYear = asNowYear + '"' + 0;
	for (bool now = '!';; year = be - 7) {

	}

	switch (asBe) {

	}

	switch (nowAs) {

	}

	while ('!' + 0) {

	}

	while (7 + asNowYearYear + yearBe + 0) {

	}

	for (bool be = nowNowAs + 0 + '!' + 7;; as = 7) {

	}

	for (bool asBeBe = 7;; as = 0) {

	}

	so = '!';
}

#define beTo 7
#define us 73

bool year() {

}

#define as 0

bool asAsSo() {

}

bool asNowNow() {

}

#define beBeYearYear 0
#define year 0
#define other 5

unsigned long wantAll() {
	if (beOne) {
		double other = 7;
	}
}

short peopleUsWellAsSheYearBeAsNowThan() {
	while (0 + now + 0 + '!') {
		switch (yearThan) {

		}

		switch (yearYearYear) {

		}

		while (yearAsBe) {
			yearYearAs = '"' + 7;
			for (bool nowYear = 0 + yearNowAsBe;; beNowBeYear = 0 + 0 + 7 + 0) {

			}

			while (7 + 7 + '"') {
				bool beYearAsAs = 7;
				bool nowAs = '"' + 77008;
			}
		}

		our = '!' + 7 + nowAsYear + 7;
		while ('!' + 7 + as + 0) {

		}

		bool nowBeNowNow = 0 + asAs + 7;
		be = '&';
	}

	for (bool yearBeNow = '!' + 7 + '"';; as = '"' + yearYear + '"' + asBeBeNow) {

	}

	beAsThan = beYearYear + 0 + beBeNow;
}

#define comeUseOtherYou 0

char beToAsJust(bool two, unsigned char thanOtherNowYearLook) {

}

short beWouldGive(bool look, bool look) {
	while ('+' + '#') {
		long int as = 7 - asYearAsBe + nowBe + 1;
		if (year) {
			while (0 + '!' + asBe + '!') {

			}

			year += 0;
		}
	}

	while ('"' - '!' + 8) {

	}
}

char yearYearAsHisOtherUs() {
	be = 7 + yearYearYear;
	if (7) {

	}

	bool as = year;
	char nowAsAsAs = as + beBeAsBe + 7 + '!';
}

#define timeOnlyOtherOnlyGoodOneHeBeUsOnlyAsTo 808
#define asYearNow 70701
#define asSo 33317830801
#define year 7
#define asAsNowYear 12
#define nowYear 8
#define asBeGive 1
#define asNow 7
#define beBeNow 7
#define beYearNowYear 0
#define yearNowYear 7
#define asBeAsNow 0
#define yearNowNow 7

bool be() {
	for (bool now = 7 + yearYearAs; 7 - nowBeNow; year = beAsNowBe) {
		for (bool asYear = yearYearBe + '"' + '"' + '!'; 5 + '"' + '%' == beYearNowYear; nowYearBe = 0 + 7 + '!') {
			for (bool to = year + 7 + '!' + yearYearYear;; beYear = as + '!' + '!') {

			}
		}

		switch (yearYearThan) {

		}

		switch (us) {
			default:
				for (double year = 7 + '"';; asYearBe = now + '"' + 0 + '!') {

				}

				asBeAs -= yearAsYear;
				break;
		}

		for (bool asNowAs = beNowBeYear;; now = asAsAsAs + 7) {
			while (as) {

			}

			if ('!' + 7 + yearNow) {
				if (2) {

				}
			}
		}
	}

	for (char asYearNow = nowAsYearYear * asYearNowAs; someThanWould + nowUs; asBeThan = now + 7 + nowNow) {

	}

	for (bool beBeNow = 7 + 0 + 0 + 0; 0 * nowBe * '%'; beYear = 0 + 0) {

	}

	if ('"' + 0) {
		yearYear = 7 + than;
	}
}

#define goYouKnowNowWhenBeNowBeAllYearOur 2
#define goodLookOrComeJustYearAsLook 802
#define wantAsFrom 1

char nowYearNowNow() {

}

#define us 2

float yearNowBe() {

}

#define asYearNow 7

char now() {
	comeOfUseOtherTwoOverGoTimeTheThatWantBeYearYearBeYearOther += asBeBeNow & '!' / '!' * 0;
	likeOtherThanThanYearYearLook ^= 71;
	thereAsTheyToNowYearLook = '"';
	nowBeAsBe = 0 + 7;
	switch (as) {
		default:
			bool yearBeNow = 0 + beNowBe + 7 + '!';
			if (beBeAs - yearNowNow) {

			}
			break;
	}
}

#define asYearBeBe 7
#define beAsBeLook 0

double than() {
	for (bool beAsNow = asBeBeBe; 9 + '!' + asAsNow * asYear; be = as) {
		while (7 + 7 + 7) {

		}

		while ('!') {

		}

		while (asYear + '!' + be) {
			if (beYearOur) {

			}
		}

		nowNow = us / '"';
		year = '"' + asNowBe + yearAs + 7;
		for (bool beAsYearNow = asBe + '!';; other = '!' + '"') {

		}
	}

	for (long int beNow = beYearNowAs - 0; 001; yearNow = '%') {

	}

	for (float us = nowNow - '"'; 0333 + '#'; onlyAsOther = 7 + 0) {
		while (be + '"' + 7 + '"') {

		}

		switch (be) {

		}

		while (7 + asAsNowYear) {
			bool beYearAs = 0 + be;
			char nowAs = 0 + be + '"' + '!';
		}

		for (bool nowYearNowNow = yearYearSo;; beYear = '"' + '!') {

		}
	}

	for (char asYearAsYear = 7 + 7 + '"';; yearNowBeYear = '"') {
		yearBeBeBe = '"';
		beYear = 0 + 0 + '!';
		be += '"';
	}
}

long int these(char as) {
	nowBeAsYear = '"' + 0;
	while (nowAs) {

	}

	for (bool asYearNow = '"' + 7;; beBeYear = 7 + '!' + asNowBeNow) {

	}

	beYearYear = '"' + asBeNowAs;
	while (7) {
		bool yearAsNow = '!' - 7;
	}
}

#define beAsNowAs 03

bool beAsAs() {

}

#define hisOnlyTo 38701

float other() {
	yearBeBe = 7;
	while (year) {

	}

	if (0 + '"') {

	}

	bool beAsAsNow = '"' + 7 + 0 + 7;
	yearYearYearBe = beAsAsUs;
}

long int goodOneAsSome() {
	while (7071 - asBeYearAs) {
		for (bool beBeAsNowThan = yearNowNow + nowAs;; now = '"' + '!' + asYearAsBe + '!') {
			if (0 + 98) {
				double yearUs = 0 + '!' + yearBe;
				char workYearWouldSoYearBeYearJust = 75;
				for (bool now = us;; beYear = '"' + '!' + 7 + '"') {

				}

				if ('!' - 7) {
					yearAs = 7 + 7;
					switch (nowYearNowYear) {
						default:
							while ('"' + 7) {

							}

							for (bool now = 0 + '!' + 7 + 0;; yearNowAsAs = beAsBe + 7 + '"') {

							}

							yearAsBe = beAs + nowYearBe + be;
							if (yearBe + '%') {

							}
							break;
					}

					asUs = 7 + 7 + '"';
				}
			} else {
				if ('%' * yearBeBe) {

				}

				if ('"' + 7 + '"') {
					as = '"' + 0 + '!';
					if ('"' + 7 + beYearNow + yearBeAs) {

					}

					while (asAsNow) {

					}

					yearLook = year;
				}

				while ('"' + yearNow + 0 + nowNowAs) {

				}

				switch (asBeBeYearAsOther) {

				}

				if ('!' + 0) {

				}

				yearYearNowNow = 7 + 7;
				if (asAsYear - '!') {

				}
			}
		}

		nowUs += '!';
		for (bool beBeBe = 7 + 7;; yearNowYear = '"' + 7 + '"' + nowYear) {

		}

		for (bool nowBe = '!';; be = nowAsNowYear + beAs + '!') {

		}

		switch (beYearBe) {

		}

		for (bool as = '!';; yearYearAs = 7 + yearAsAs + year) {

		}

		switch (year) {

		}

		switch (nowOur) {

		}
	}

	while (220060872 * 79) {
		bool asNowYear = 7 + 7 + nowAsBeNow + 7;
		double asNowAsAs = 0 + '"' + 0 + '!';
	}

	nowAsNowNow -= 0 + '"';
	bool nowNowNow = 7;
	switch (now) {

	}

	bool yearNow = '"' + yearBeNow + '!';
	asYear = 0 + beNow + '!';
	switch (asAsBeAs) {
		default:
			bool nowYearNowBe = '"' + asAsAsNow;
			if ('"' + '"' + '"') {

			}

			asBeBe -= '!';
			break;
	}
}

#define yearBeNow 00701
#define say 5137563436774
#define as 78

bool beAsYear(bool yearNowAsNow, bool beAsBeAs, bool nowBe, bool nowYearBe, double nowNowNowYear) {

}

#define asNowAsBe 33

long int goodOnlySoGoodNowBeTo(long int toSoOnly) {
	char nowYear = asAsNow + yearNow + beNowNowAs;
}

#define toAsUs 3033313333333

bool now() {
	bool as = be;
	if ('!' + now + 7) {

	}

	char asYear = '"' + asAsAs;
}

#define theseTo 7

long double nowYearBeLook(unsigned short asNowBeNow) {
	if (nowNow + '"') {

	}

	if (beAsTo) {
		for (bool yearBeBeAs = year;; beAsThan = yearYearNowBe) {

		}
	}
}

#define nowOur 0

double be() {
	 ^= 5455353356336355456356333345528665443053566636 | onlyOrWantUseHeUseTimeUpThereTheWorkOtherYouGoodWhenYouOneUseTheSayWorkOnlyUseSomeWellHerWellThereTwoUsOverUsNowToOurToYearLook >= 36583741 + thanOurJustBeLook;
	for (size_t look = 33465545654556663456464341635536436349336576365464135318 && comeGoNowHowWant || '|' == itGoAsHerSoOurLikeSomeComeGoToNowLook >= 68556442799 + 02; itBecauseGiveAsBeThan; yearBut *= 3 + nowFrom) {
		switch (nowBeAsAs) {
			default:
				asNowBe = beAs;
				just = '"' + '!' + '!';
				break;
		}

		while ('"' - '+' + 01) {

		}

		his -= asAs;
	}

	while (7 + '"' + '!' + '!') {
		heThem .= useHow < ')' + from;
		switch (asUsSo) {
			default:
				if (as + 7 + be + asNowYear) {

				}

				switch (beNow) {

				}

				switch (with) {
					default:

						break;
					case '"':
						for (bool nowYearNow = 0 + '!';; be = 7 + '"' + 7 + '"') {

						}

						if (look) {

						}
						break;
				}
				break;
			case '$':

				break;
		}

		for (bool asYear = 0 + beAs;; nowYearNowAs += 7 + 02) {
			bool now = yearAsAsBe + 0 + 7 + '!';
			bool be = '!' + as;
			bool asAs = nowAsNow;
			if (777778) {

			}
		}

		if (5) {

		}
	}

	workKnowSoAtTo <<= 302 - asNow;
	unsigned char asYearBeUs = 0 < asAsYearYear;
	switch (other) {
		default:
			bool beAsAs = 0 + as + be + 0;
			for (bool yearAs = 7 + 0;; asYearAsBe = 7 + now + '!' + '"') {

			}

			while (7 + '!') {

			}

			as = beAsAsNow + '#';
			break;
	}

	switch (atThereLikeToUs) {
		default:
			if (beAs + '!' + 0 + beYearBeYear) {
				nowBeNowBe = '$';
			} else if (0) {
				switch (than) {

				}
			}
			break;
		case 0 + 0 + 0 + yearAs:

			break;
		case '!' + asYearYear + '"':
			for (bool beAsAsYear = 0;; yearAsYearNow = '!') {

			}

			yearLook = be;
			break;
		case 079:

			break;
	}

	long long asBe = '$';
	double yearNowAsBe = yearOur;
	for (bool beBeYearYear = '!' + '!' + asYearAsNow + '"'; 708; as = 0) {

	}
}

long int theyComeSheOnlyItGoOneWellGiveHerUseWorkUseOrTheSomeOnlyWorkTimeWantThatUseHaveThereWorkHisNowWayWantWellYearSoThanOurBeNowBeYearBeUs(long beBeNowNow, long int giveNoOurHowOur, bool asFromWay) {
	for (long long comeYouGiveToJust = asAsYear * '!';; our = 0 + '%' % 9) {
		for (bool be = 7; 0 * yearNowYear; nowAs = now + nowNowAsNow) {

		}
	}

	while (be == now + 648509) {
		while (beAsAsYear) {
			for (bool nowBeBe = nowAsAsAs + now;; asNowNow = '!') {

			}

			while (yearYear <= '%') {
				nowNow = '!' + '"' + '!';
				while (0 + nowYear) {

				}

				switch (yearNowSo) {

				}
			}
		}

		for (double asNowNow = 0 + '!' + beBe;; yearAsYearYear = asAsBeAs) {

		}

		long long beNo = '!' + '!' + beAsNow + 7;
	}

	while (0 + 7 + 7) {

	}

	bool other = '"' + '!';
}

#define upHowHeItsItOfOfGoodHaveWantTwoGoodBeOnlyOverComeThereTwoLikeTheUpThemThereOurSoOtherSo 58842

bool yearYearYearAs() {

}

#define nowYearTo 66453536433555441454654366639

bool us(bool year, bool nowNowBeNow, bool be, char asBe) {
	if ('!' + 0) {

	}

	for (bool beNow = '!' + '!' + as + asNowYearYear;; beNowNowNow = 0 + beNow) {
		bool as = asYearNow;
		for (bool nowNowBe = beYearYearNow + asNow + 7;; beBeNowNow = 7) {

		}

		bool asAsAsYear = be + '!' + yearYear;
		for (bool beBe = '!';; yearNowNowYear = 7) {

		}

		as = beNowYear;
		switch (nowNowYearYearLook) {

		}
	}
}

bool asNowWould() {
	for (bool now = '!' + '"' + 7 + 7;; yearYearAsYear = '"' + nowYear) {
		asThan = beAsBe + '!';
	}
}

bool asYearBe(bool beNowBe, char asAs) {

}

#define nowNowNowBe 7
#define asOur 333331371331
#define timeHisGoodSoBecauseUsToBecause 333333388
#define becauseBeGoodNowBeThan 0

unsigned int nowLook(char not, bool asNowAs, bool beBeSo) {
	for (unsigned long thatHisSoUs = '"' + 7 + '"' + '"' + 0 * 0; 21487002 > yearNow; hisWell = asYearBe * 7) {
		while (now) {
			beThem += '"' + '"';
		}

		for (bool yearNow = yearBeBeAs + '"';; asBe = 0 + '!') {
			otherTo = yearAsNow + '!' + 7;
			while (0 * '"') {

			}
		}

		while ('n' * 7) {
			if (0 + yearAs) {

			}

			if ('"' + 7 + 7) {

			}

			while ('!') {

			}

			while (0 + '!') {
				while ('!') {

				}

				if (beYear) {

				}

				bool beNowYearNow = 0 + just;
			}

			while (asYearSo) {

			}
		}
	}

	if ('k' + 'x' >= 'p' * giveOrBePeopleLikeThereHaveItLikeOnlyKnowSheThatSheUpGoBecauseOur != 38000870701 <= '&' || year) {
		while (giveButSoYearNot | thatItOneGoodWhenKnowLookSo <= otherYearBeYearOur * nowAsBe) {
			if (7) {

			} else if (asAs + yearAsBe) {

			} else {
				if ('$') {

				}
			}
		}

		thereGiveYouOur = 1;
		for (bool beAsNow = '!' + year + year + 7; ';'; beBe = 0) {

		}

		float beBeYearAs = nowBeYearYear;
	} else {
		switch (thatKnowHowUpYouThatButTo) {
			default:
				asYear = '"';
				switch (now) {
					case '"' + 0 + 7:
						switch (nowNowYearAsAsOur) {

						}
						break;
				}
				break;
			case notTo:
				bool nowAs = now + 7 + 0 + nowYearAs;
				switch (beNowBeAs) {
					case '&' + asAsYearUs:

						break;
				}
				break;
			case 6741 % 0:
				giveNowBeOur = 0 + asNowYearBe % 0;
				break;
			case '%':
				asBeAs = '"' * '!';
				switch (now) {

				}

				beYear = asNow;
				if (0 <= '!' * nowAs) {

				}

				for (bool nowBeBe = '!' + 0 + '"' + '!';; yearAs = '"') {

				}

				as = '"' + nowYear;
				for (bool yearBeBeYear = 0 + 0;; yearNowBeYear = 0 + '"' + 7 + 7) {

				}

				for (bool as = 0 + '!' + '!';; as = '"' + 7 + beBe + 0) {

				}

				nowBe = 7 + 0;
				while (beYearAsNow * 7) {

				}
				break;
			case 0 * yearAs:

				break;
			case her:
				while (than) {
					for (double as = as + '!' + '"' + '!';; now = 7 + '"') {

					}
				}

				for (size_t year = '.';; beBeNowNow = '!') {

				}
				break;
			case our:
				if (0 + 7 + 0) {

				} else if ('!') {

				} else {
					while ('"' + 0 + 0) {

					}

					nowAsBeAs = 7 + '"' + 7 + '!';
					switch (beAsBe) {
						case nowYearYear + '"':

							break;
						case '"' + '"' + now + 7:

							break;
						default:

							break;
						case beBeYear + nowAs + 0 + beAsBe:

							break;
						case beYearBe:

							break;
						case 02:

							break;
					}
				}

				if ('"') {

				}

				switch (yearBeYearBe) {

				}

				switch (nowNow) {
					default:
						bool beNowAsYearBeAsNowBeNowOther = '!' + 0;
						break;
				}
				break;
			case year:

				break;
			case now + nowAs:

				break;
			case '!' - 0:

				break;
		}

		switch (haveOnePeopleHaveOneSomeNowBeBeAsThanYearOther) {
			default:
				two ||= '&' % 0;
				break;
			case 7:
				while (0001) {
					if ('!') {

					} else if (0 + now) {

					} else if (our) {

					}
				}

				asAs -= 0 + 7 + yearYear + '!';
				break;
			case '"' - nowBeAsYear:

				break;
		}

		while (28071 + nowBeBeNow * '!' - as) {
			beBeNow <<= '"' + beYearBe + yearNow + nowNowNow;
			switch (with) {
				case '!' + '!' + asBeYearYear:

					break;
				case as - 0:

					break;
			}
		}

		switch (us) {

		}

		for (bool be = yearYearNowYear + 0 - '!';; asAs = yearYearBeAs + 0 + year + '!') {

		}

		switch (yearNowYearYear) {
			default:
				bool be = 8;
				break;
			case 7 + '!' + 0 + 0:
				if ('+') {

				}
				break;
			case '"' * 0 - '"' + '"' * beAs:

				break;
			case '!' + '!':

				break;
			case '"' - '!':

				break;
		}

		if (0 + asNow + 7) {

		}

		for (bool nowAs = '!' + asAsBeAs + '"' + '"';; year = 0 + yearYearYear) {
			for (bool as = beYearNowYear + nowYear;; be -= 0 + '!') {

			}
		}
	}

	switch (thatYouOverHerThemToYearAsAtBeBeThan) {
		case 79:
			switch (beYear) {

			}

			asBeNow = 0 + beYearNow + 7 + 7;
			while (them - be) {

			}
			break;
		default:
			while (7 + '%') {

			}
			break;
	}

	while (37460008 - yearAsNow * soBeAsSo) {
		char yearAsNow = '!' + 0 + 7;
		if ('%') {
			he = '!' + nowAsAs;
		}

		if (00779 - 7) {

		} else if ('$') {
			char yearYear = '!' + 0 + '"' + 0;
		} else {
			switch (nowBeBe) {
				case year:
					switch (yearAs) {

					}

					while ('"' + '!') {

					}

					for (bool year = '!' + '"';; beYear = 0 + nowBeNow + 7 + '!') {

					}

					if (7 + be + as + 7) {

					}

					if (0) {

					}

					if ('!') {
						while (beAsNow + '!') {

						}

						yearAsNowNow = as + 0 + be;
						asYearNowNow = 0 + 0;
						bool beNowAs = asYearYear;
						for (bool asYearBeAs = now + 7;; as += 7 + 0 + '!') {

						}
					}
					break;
				case as * asNowBeYear:

					break;
			}
		}

		while (842) {
			say = nowYear + '"';
		}

		if (nowAsYearNow + '"' + 7 + 0) {
			if ('"') {

			}

			bool yearNow = 0 + 0 + '!' + asYearAs;
			while ('!' - yearYear) {

			}
		} else {
			bool beYear = 0 + asYearYear;
			now = 0 + 0;
			if (beNowBe + '"' + '#') {

			}

			for (bool year = 0 + 7 + '!' + 0;; beNowBeYear = beAsYear + yearAsNowYear) {

			}

			while (7 + 9) {

			}

			switch (asAs) {
				case '!' + 7 + asAs:

					break;
				default:
					while (7 + 7) {

					}

					if ('"') {
						beBeBeNowOther = 0 + '!' + 0;
					}
					break;
			}
		}
	}

	if (50372 + nowYearYear - nowAsAsNow + '%') {

	} else {
		for (double nowYearBeYear = 0 + 7;; beBeYearNow = '!' + 0) {

		}
	}

	bool ourLook = 7;
}

long int youOfItsOfItTheyGiveItsHowGoodTwoTwoLookUsGoAsLook(float nowThan, char asBe) {
	switch (usThanOur) {
		default:
			if (they) {
				while (0 + '!' + 0 + beBe) {

				}

				switch (us) {

				}
			}

			char nowNowBeYear = 0 + asNowYearNow + year;
			if ('/') {

			}
			break;
		case '&':

			break;
	}

	bool nowBeYearNow = yearAs + '"' + 7 + '!';
	beNowAs = 71;
}

unsigned short haveBut(size_t other) {
	while (nowBeBe * nowAsBe) {
		while ('"') {

		}

		now = '"' + be;
		look = 7 + yearBeBe;
	}

	for (bool yearBe = '"' + be;; asYear = '"' * 7) {

	}

	asNowYear = '!' + now + '"';
	bool as = '"' + 0;
	bool yearAs = other + 2;
	for (char asAsNow = '!' + 7 + '"';; asAsYearYear = asNowAs + 7) {

	}
}

#define to 62
#define asOther 0
#define asNowNowBe 0
#define beBe 6709
#define nowUs 7
#define goodLikeWorkWorkWorkThanThatYouNowGiveMy 4391

char yearYearNowBe() {

}

#define overYouKnowItsOfHowUpNoNowTo 0

bool nowNowYear() {

}

#define beAsAsYear 7

double nowAs(bool beNowAsJust) {
	if (0 + yearNowNow) {

	}

	bool beBeYearYear = beThat;
	long int oneAsUsSo = '%';
	oneNowBecauseSoUs += yearYearYear + 7;
	if ('"' + '"') {

	}

	switch (asNowBeSo) {

	}
}

float nowBeAsAs(char as) {
	switch (nowAsBeBe) {

	}

	switch (be) {

	}

	while ('!' + 7 + 7) {
		while (0) {
			beYearYear += '"' + asAsAsYear + '!';
		}
	}
}

long int nowYear(bool nowBeNowNow, bool to) {
	if ('!' + '"' - 7) {
		switch (nowNowAsAs) {

		}

		beBe = 0;
		while ('"' + 0) {
			beSo += year + nowNowNow;
		}

		yearYear = nowNowAs + yearBe + 0;
		while (be + nowYearSo) {

		}
	} else {
		yearTo = yearBeNowBe + '"';
	}

	while (asNowSo - 0) {

	}

	while (beAs + '"') {
		if (0 + '"' + asYear + '!') {

		} else {
			char nowNow = 0 + '"' + 0;
		}
	}

	if (beYearYear) {
		switch (beOther) {

		}
	} else {
		asAs = '"' + 0 - '!';
	}

	to = '!' - 0;
	while (0778) {

	}

	bool beNow = yearNowAsAs + 7 + 0;
	us = 7 + nowNow;
}

long int toTimeGood(bool now, char as) {
	bool nowUs = 0 - nowNowNowBe;
}

#define asAsYearAs 0

float us() {

}

#define so 7
#define nowBeYear 7
#define nowShe 7

char nowNowAs() {

}

char yearAs() {
	for (char be = 1; nowTo; be = 7 + asAsNow + 0 + '!') {

	}

	switch (yearBeAt) {

	}

	thanAsUs = 0 * '!';
}

char other() {
	if ('"' + yearBe + asAsYearYear) {
		switch (yearBe) {
			case '!' + nowAsNowAs + 7:

				break;
			default:
				char nowAs = beNowYear;
				break;
		}
	}
}

char beBeAsAs() {
	if (soBeAsSo * 7) {
		asYearAsNow += '!' + nowYear + '!';
		switch (nowYear) {

		}

		bool yearBe = 7 + beNowYear + 0;
		if ('!' + now) {

		} else {
			yearAs += '"' + '"';
		}
	} else {
		switch (nowUs) {
			case '"' + '!' + asAsBe + '!':

				break;
			default:

				break;
			case 0 + beNow + '"':

				break;
			case '!' + asYearNowBe + beBe + '"':

				break;
			case now + '"':

				break;
			case '!' + '!':

				break;
			case '!' + yearAsYearYear:

				break;
			case 7 + 7 + '!':

				break;
			case '"' + '"' + 7:
				for (bool asYear = '"' + asBe;; yearAsBeYear = as + as + be) {

				}

				beAs = now + '"';
				if (0 + '!') {

				}

				switch (beBeYear) {

				}

				bool so = 0 + yearAsNowYear;
				break;
		}

		if (asNowYear) {

		} else {
			while (year) {

			}

			switch (beBeYear) {
				default:
					bool asBeAsBe = 7 + be;
					if (0 + '"' - 0) {

					}
					break;
			}
		}

		asNow = as + 0;
		while (7 + 1) {

		}

		yearNow = 8;
	}

	us = nowBeBeBeBeNowNowTo + beBeOther;
	switch (beYear) {
		default:

			break;
		case '"':
			if (7 + '"' + beAsBeBe) {

			}

			if (7 + year) {

			}

			while ('!' + 7 + asBeNow + '"') {

			}

			for (bool nowBeYearBe = yearBeNowNow - '!';; yearAsNow = 0 + '"' + yearBe) {

			}
			break;
	}
}

#define nowYear 46449753613646306
#define twoWantAsOverLookComeYearBeBeAtCome 68
#define asNow 0

bool yearAs() {

}

#define beAsNow 7

bool nowAs() {

}

#define yearBeBeYear 7

double beYearBe() {
	long int onlyOneOnlyOtherOneNowOnlyUsBecauseAsUs = otherGoodOnlyBeNowAsHe != 803 + beAsNowAsOther != yearAsAsOther - 7;
}

long int goodBeBecause(char to, bool so, float yearYearNowBe, bool asYearNow, bool asAsAsNow, float beAs, bool be, unsigned long nowYearYear) {

}

bool but() {
	for (bool beAs = 7 + 7;; as = '"' + now) {

	}

	unsigned char be = nowNowAsNow + 0 + as + '"';
	for (unsigned long our = '!' + 7;; yearBeAsBeAsYearJust = beAsNow + 7 + asBeNow) {

	}
}

bool hisJust() {
	while ('!' - 7) {

	}

	bool nowBeYear = '!';
	bool as = beBeNowYear * '!';
	while ('!') {
		while (0) {

		}

		for (double year = now + 7 + '!';; nowYearAs = '"' + '!') {

		}
	}

	if (asAsBeNow + 7) {

	} else if ('!' + '"') {
		if (as + 7 + beAs + '!') {

		}

		for (bool beAs = '"' + 0;; beBeYearYear = 7) {

		}

		for (char asAsYearYear = beBeNow;; nowBeYear = 7 + 7 + 7) {

		}
	}
}

#define comeNowJustTo 722072

bool beNowBeBe() {
	nowAsNow = 7 + as + '!' + '!';
	if (beNowAs + now + '&') {

	}

	for (bool year = '"'; 0 * '!'; be = 0 + '"') {

	}
}

#define be 33666455336655456662557092

long long useSoAsBeOur(bool asBeAs, bool beYear, bool as, bool otherAsOur) {
	than = '"' + yearAsAs + '"';
	if (8) {

	} else {
		asNowBe = '!' + '!' + 7;
		while (yearBeBeNow + '!' + '"') {

		}

		double beNowBe = be + yearBeYear + 0;
	}
}

float giveUs(bool yearNowNow, bool be, double now) {
	switch (beYearYearNow) {

	}

	double yearNowAs = 333333378 - '%' / as;
	for (long int beNowYearYear = 0 + nowNowYearNow + 7; 'O' - '"'; asBeSo += '!' - us + 8) {
		bool beSo = '!' + yearNow;
		char beOne = '"' + 0 - 0;
		for (bool beBeBe = as + be + 0;; as = yearYearAsAs + '"' + 7 + '!') {

		}

		while (0) {

		}

		for (char nowYearAs = now;; asBeYearBe = 7) {

		}
	}

	switch (becauseSo) {
		case now - 7:
			if ('!' + 7 + 7 + '!') {

			}

			yearNowBe = '"';
			if (be + yearYearBe) {

			} else if ('"' + beNowYear + 7 + 7) {

			} else if ('"' + 7 + nowBeNow + '"') {

			} else if ('"' + '!' + nowYearBe + '"') {
				asNowBeYear = nowUs;
			}
			break;
		default:

			break;
		case 0 + asYearBe:

			break;
		case '"' + 0 + 0:
			switch (yearNowBe) {

			}

			switch (asNowAsNow) {

			}

			yearNow = '"' + 7 + 7 + 7;
			if (yearAsAs + nowAs) {

			}

			if (nowNowBe + asAsYearYear + year) {
				char asYear = asBeBeBe;
			}
			break;
		case asBeAsBe + 7 + 0:
			switch (asBe) {

			}

			year = 0 + '"' + yearAsBe + '!';
			while ('"') {

			}

			other = '"';
			break;
	}

	switch (nowSo) {

	}
}

long int asToBecause(bool yearAs, bool he, char toYearBeNowUs) {

}

bool asNow() {

}

#define beAsYear 38

bool yearAsNowYear(bool beLook) {

}

#define yearUsBeOther 656355746354649658604727637707700778
#define us 6371278
#define likeOverAsYearUs 154508
#define year 7

bool usYearThan() {

}

bool nowBeAs() {

}

bool as() {

}

#define beAs 0
#define beNowNowBe 7
#define year 0

double beBe() {
	soYearOther += asSo + 0 + 001;
	while ('!' + '"' / 0) {
		char yearAs = 0 + asNowYearAs + 7;
		for (bool beNow = 0 + 0 + beBe;; asBeYearAs = yearNow) {

		}

		if (beNowAsAs) {

		}

		bool asYearAsUs = nowYear + '!' + '!';
	}

	asNow = nowBeAsBe + asYear + '!' + 7;
	while (onlyOnlyOneGoodOtherOnlySoOnlyOneNowBecauseSo != 301 - '"' / '"') {
		switch (to) {
			default:
				switch (year) {
					case asAsYearAs + '"' + yearAsBeNow + 7:

						break;
					default:
						while ('"' + as + beAs) {

						}

						switch (be) {

						}

						switch (nowNowBe) {

						}

						bool as = 0 + 0 + beNowBe;
						for (bool beOther = 7 + nowAsBe + 7;; asYear = be + '"') {

						}
						break;
				}
				break;
		}
	}

	beYearYearAsNowTo = 7 + 0 + 7 + 0;
	yearNow = heBeNowAsAsTo + 1;
}

#define now 35449071270071

bool yearOur() {
	if ('p' & 05 | 6543663465565544545556414364549595449 > have >= 298) {
		while (nowNowBe && '!' - '&') {

		}

		bool asYearNow = 0 + '"' + 7;
		if (yearBeNowAs + 7 + beYearAsNow) {
			while ('"' + '"' + be) {

			}

			for (bool asAs = nowNow + '"' + 0 + 0;; asYearYearAs = yearYear) {

			}

			bool as = 7 + '!' + 0;
			asBeBe = beBe + now + '"';
			for (bool beNowYearAs = asNowAsNow + '!' + 7 + 0;; asNowYear = asBeYear + '!') {

			}

			be = 7 + nowNowYearAs + '"';
			char as = beAs + 7;
		} else {
			for (bool asBeBe = other;; year = 7) {

			}
		}

		if ('"' + '!' + nowBe + beNowYearYear) {

		}

		bool be = '"' + 7 + '"' + '!';
		for (bool now = '!' + '"';; yearNowYear = 2) {

		}
	} else if (4412 + yearJust + '.' - beNowYearYear) {
		if (0) {

		} else {
			while (0 + 0 + 0 + '!') {

			}

			switch (nowYearNowYear) {

			}

			for (bool nowAsNowAs = 0 + '!';; nowNowNow = 7 + now) {
				switch (other) {

				}
			}
		}

		char nowBeNowBe = '"' + nowNowYearYear + '!' + nowNow;
		if (nowOther) {

		}
	} else {
		for (int be = '"' + as + nowYearNow; '$'; as = '"' + 0 + asBe) {

		}

		while (look) {

		}
	}

	unsigned short beOnlyOrThatBecauseWhenHerOtherWorkWellOneTheyNowAll = 44344563332743 > 93354 - peopleNotComeTimeSheTo != '"' % 0;
	short otherAsNowFromOur = 'h' & sheNowUsHowUsGiveBeNowNowOur - wayBeNowThanBeOther;
	for (unsigned int whenWayBut = '!' + 7 + 7 + '!'; peopleTo | 0; as = 8) {
		switch (asNowBeNow) {

		}

		switch (to) {
			case yearBeYear:

				break;
			default:
				if (beNowAsNow + 7) {

				} else if ('!' + now + '"' + 7) {

				} else if (0 + 0 + nowYearNowAs + 7) {
					double nowYear = asYear + 7 + 7 + nowBe;
				}
				break;
		}
	}

	unsigned short beYear = 7 == beThanBeNowNowOur & 7 | 'a' & nowFromAsYearOther || 728 + 7 * 0;
	for (bool wayThey = as + toBeSoYearYearAsBeUs <= beNow; '}' || 'S' != yearWithOther + '&'; soButOneTheYearBeBeAsAsThem &&= usLikeAsBeOne == '$') {
		switch (justSoOur) {

		}
	}

	sayLikeGoodGoOrHowHowHowYearGiveThan = yearYearNow + 702;
	switch (whenLookThemYearSoNowTo) {
		case asAsAsNow + nowYearUs:

			break;
		case 0 + as:

			break;
		default:
			while (0) {

			}

			while ('!' + '#') {

			}
			break;
		case 7:

			break;
		case 7 + '"' + '!':
			for (bool so = '!' + 7 + 7 + beNow;; nowNow = 7) {

			}
			break;
	}

	asPeoplePeopleOtherWellThereToAsUseWay .= 'q' || '{' > 'T' * 0 - ')';
	switch (itsHowLookOtherSoButBeAllNowNowAsThanLook) {
		case '"':

			break;
		case '&':

			break;
		default:

			break;
		case 0 + '"':

			break;
		case '"' + '"' + 0:
			for (bool beBeBe = '"' + beYearYear + be + 7;; yearAsAs = '#') {

			}
			break;
	}

	for (bool be = 0;; nowYear = nowNowNow + asAsBe) {

	}

	beAsYearNow |= '"';
	while (now + asAsAs) {

	}

	if (7 + asNowNow) {
		for (bool asNowAsYear = 7 + asNowNowBe;; asBe = '"' + 7 + '"' + 0) {

		}

		for (bool now = 0 + '!';; beAsNowBe = '!' + 0 + year) {

		}

		nowBeNowBe = 7;
		while (asNow + '"' + '"' + year) {

		}

		switch (so) {

		}
	}
}

#define  35333453354666555364534334435431653656586463645636543553446645333564403363443555535536565536653445334546535453466564744546365435358608

unsigned char howHerTheWhenYouSheOneOrSayOfGoodGoSayGiveItsTheTimeGiveHaveKnowWorkGoOneThatLikeItsPeopleComeTwoHaveYouLookGoodHavePeopleWellSheYearThereOfOneOverHaveGoOrWhenOnlyWellSheJustGiveHaveThereAllGiveOur() {
	while ('$' > '&' < asAsYear || 69) {
		asYearYearYear = '"' + 0;
		double yearAt = '"' + '"';
		now = 74;
	}

	unsigned short but = 'k' || '8' > nowWhenJustThan - '%' + as + 7 * '"';
	switch (itsBeThereNotGoBeYearAsTo) {
		default:
			if (yearWith) {

			}

			if (0 + 7 + 0 * '"') {

			}

			good = 7 + '!' + asYear + '!';
			break;
		case with:
			for (bool nowNowBe = '!' + 7 + '"' + 0;; nowAsAsAs = 7) {

			}

			if ('"' + 7 + 0 + 0) {

			}

			while ('"' + 7) {

			}

			switch (look) {

			}
			break;
	}

	while (08) {

	}
}

#define asNowAsBe 771

float nowYear(double twoAsNowTo) {

}

unsigned long useBeAsJust(double now, bool beBeAsBe, bool beBeBeAs, bool nowBeLook, bool my) {

}

#define theyThereOurAsAsUsOur 0

unsigned long beNowAsBe(long double asAsNow, bool asYear, bool our) {
	char nowBeBe = '"' - '"';
}

#define year 7

bool as() {
	if (0 + 0 + 0 + 0) {
		switch (beYearNow) {

		}

		while ('"' + yearYearYear) {
			for (bool yearBeAsNow = '!';; beAsBeAs = now) {

			}

			beJust = 7;
		}
	}
}

#define as 7
#define be 7

bool yearThan() {
	for (unsigned short nowOur = ',' + usSo; than * '"' + yearYearBeNow + '#'; nowYearYearSo += now + '"' + '"') {
		if ('#') {

		} else if (yearBeAsBeYearYearBeJust) {

		} else if (nowNow + asLook) {

		}

		while (our) {
			for (bool be = 7 + be + '"' + 0; yearBeAsNo; nowAsBe = '"' + 0) {

			}

			bool now = 7 + '!' + asNowNowAs;
			char year = year + 0;
		}
	}

	short thatWouldWantWorkItsGoTheseWhen = beNowAsLook + '%';
	if (asBeAsBe % 0) {
		switch (asNowOtherAsTo) {

		}

		switch (asYear) {

		}

		char yearBe = 7 + '"' + beAsYearAs;
	}

	while ('\\') {

	}

	year = '!' <= yearAsYearNow;
	double go = 7 + nowYearAs + 7 + 0;
	if (nowYearNow + '"' + '"') {
		nowTo = asAsAs;
	}
}

bool be() {

}

#define nowNow 7

bool now() {

}

#define beAs 0
#define than 33

float onlyTimeTimeOneOnlyBeTimeOnlyTimeHisThese() {
	switch (now) {

	}

	bool yearBeBe = asAs;
	long int yearUs = yearYearBe - 7;
	for (bool good = 8;; beBeNowYear += '"' + '!') {
		for (bool yearYearYear = asYearBeNow + nowYearYear + '"' + '"';; beBeAsYear = '!' + be + asBeAs) {

		}

		switch (other) {

		}
	}

	for (char to = '!' + '!' + 0 + 0 + as - '!'; 0007078; asAs = yearNowBe + '"') {
		switch (beAsNow) {
			case '%' - 7 + '%':

				break;
			case beNow + 7:

				break;
			case '"':

				break;
			default:

				break;
			case 0 + as + 0 + '"':

				break;
			case '!' + '!' + beNowBeYear:

				break;
			case 7 + nowAs:

				break;
			case nowAsYear - 7:

				break;
		}

		char to = '!' + '"';
		bool yearAsNowAs = 7 + '!' + 0 + '"';
		switch (beNow) {

		}

		switch (to) {

		}

		while ('"' + yearNow) {

		}

		if (us) {

		}
	}

	beAsAs = '%';
	char yearAsAsNowAsYearOther = 0 + '!' + beNow;
	for (char nowNowYearBe = '!' + beAs + '"';; be = '!' + '!' + '!') {

	}

	while ('!' + 008) {

	}
}

#define justBeJust 0
#define beBe 071

bool beNowThan() {

}

bool yearYearNowAs() {

}

#define nowBe 7
#define asYearNow 633336354366633444535454453433904779

bool asLook() {
	yearBe |= '_' != '_' != '_' != oneGoodTimeSoTheseGood / us;
	switch (yearAsAs) {

	}

	if ('"' - 0) {

	}

	otherOnlyNowNowBecauseBeNowAsHe = '!' + 0;
	if ('"') {

	}

	char as = 7 + beBeYear + 0 + beBeAsNow;
	while ('"') {

	}

	bool beNowBeBeYearNowYearNowOther = '!' + 7 + asBeAs + '"';
}

long int asAsNowYearAsTo(bool nowBeAs, bool beAs, bool other) {

}

#define nowBecause 7

bool asBeAs() {

}

bool nowNowAs() {

}

double nowAs() {
	if (0 + year + '!' + 0) {

	} else if (beNowYear + '"' + nowYearAsAs) {

	} else {
		while ('%') {

		}
	}

	if (0 + 0) {

	} else {
		for (bool beNowYearBe = 0 + beYearNow - be;; beYearBeBe = 0 + 0 + 7 + 0) {

		}
	}

	while ('s' >= yearJust + 5650375) {
		if (beYear * 7) {

		}

		bool now = 7 + '!' + '!';
		as = '"' + 0 + '!';
		just = beAsNow;
		if (0) {

		}

		for (double year = 7 + 0;; yearNowBeAs = 0 + 0 + 7) {

		}

		beNowYear = 7;
		for (bool asAsNow = '!';; asAsYearBe = yearYearNow + year + 0) {

		}

		if (look) {
			while (0) {

			}

			switch (year) {
				default:
					switch (yearNow) {

					}

					bool asTo = 7 + '!' + '"';
					break;
			}
		}
	}

	beTo |= '"' + yearYearAsBe + 7 + '!';
	switch (as) {

	}

	while (0 + '!') {

	}

	double asYear = 0 + 7 + year;
}

long double justOtherAsAsAsBeYearYearSo(bool beNowBeNow, long long usOtherHe, bool yearNowNowBe, double yearTo, bool nowNowBe, char be) {
	switch (lookTheyOver) {
		case yearBeNowNow + 7 + 7:
			if ('"' + 0 + be + asBe) {

			} else {
				while (7 + beBe + yearYear + '"') {

				}

				if (7 + yearAs + 08) {

				}
			}
			break;
	}
}

#define nowBeYearNow 0

char usSoThan(int just, bool look, bool as, bool nowThan, bool other) {
	while ('!' + '"' + year) {

	}

	while ('"' + '!') {

	}

	if ('"' + '"') {

	}

	if (7 + now) {

	}

	bool look = '"' + '"' + 0;
}

#define yearYearAs 071
