#include </UUUUUUUUEE/020.h>

size_t look(bool now, bool now, bool yearYearNowYearNowLook, bool our) {
	char than = 7;
	for (bool now = '!' + 0 + year + asBeBe;; nowNowNowNow = beNow + 0 + '!' + '"') {

	}

	look = '!' + '"' + be + 7;
}

#define as 0
#define or 
#define workHowLikeComeWellWantLikeWorkOrSomeSomeSayOrGiveGoLookSomeWhenWellWorkThatGoTheGoWhenWayWorkThereWhenWorkUpWouldTheseGoKnowTheyItsYearWithNowBeJust 56654653960666655555665555656566656644664655563566646644443645636654663666666663563566646636634666666696432

unsigned short (long long goHowTheWellWellThereHowOrGoWhenPeopleGivePeopleYouWantOverHowOverSheWorkUpWantHaveWhenThereSomeUseGoodOfItGoOrPeopleWhenOverSheWantComeTwoWantPeopleSayHaveWorkWellKnowComeHaveKnowOrKnowWellWhenGoodKnowSayAtOtherYearNowThese, unsigned short nowHave, char beNowYearAs, bool be, unsigned long thanNowBeJust, char to, bool beThan, char beYearNow) {
	if (556 < 'X' * as) {

	}

	justHerTheUseOnlyOnlyWellWhenThatWantJustItsSoGiveYearSomeHowUseWantJustUpJustBeOur &&= onlyKnowJustItsGoAsThanFromWouldYearBut >= 'n' == 76684606 || likeSomePeopleOfOrSheHowOverHerYearSayHaveWhenHerWould * 'f' - '#';
	if (7 - '"') {

	}

	switch (overYouYouPeopleSomeOrThatWhenWantOnlyYouComeUpThanWellButBecauseBeLookAsThan) {
		case ',' + ourBeBeJust:
			for (bool to = 0 >= '!' <= '!';; whenNowThanAsAsLook = 7 + '.') {
				while ('"' + yearNowNowYear) {
					bool nowBeYearYear = 0 + '"' + nowYearYearYear;
					beBeAsAsBeOther = 0 + 0;
				}

				bool now = 0 + 0 + 0 + now;
				switch (beBeNowAs) {
					default:

						break;
					case 0 + 7 + '"' + '"':

						break;
					case beYearAs * '!':

						break;
					case '$':

						break;
				}
			}

			while (0 * '"') {
				while (asAsYearAs + 7 + 0 + 0) {
					while (04) {

					}

					for (bool nowYear = 7 + 7;; beBeNowNow = '!' + as + '!' + '!') {

					}

					while (our) {

					}
				}
			}

			if (7 + 9) {

			}

			if (7) {

			} else if ('"' - 7) {

			}
			break;
		default:
			while (0) {
				switch (yearUs) {
					default:
						for (bool look = yearAs + yearAsYear;; nowNowNow = 0 + 0 + '!') {

						}
						break;
				}
			}

			if ('"' + '!' + yearAsYearNow) {

			} else if (7 + as + yearAsAs) {

			} else if (0 + 0 + 0) {
				while ('!') {

				}

				while ('&') {

				}
			}

			switch (beBeYear) {

			}

			char asYear = yearAsBeFrom;
			break;
		case '"' - year:
			while (nowNowNow) {
				look = 0 + 7;
				switch (just) {

				}
			}

			if ('!' + beNow) {

			}

			switch (our) {

			}
			break;
		case 7 + '"' + year + '!':

			break;
		case 0 - '"':

			break;
	}

	switch (be) {
		default:
			if (beYear + beBeJust) {
				if (0) {

				}

				switch (be) {

				}

				beBeBeLook = 0;
			} else if (asBe + '!' + 7 + '"') {

			} else {
				bool beBeNowNow = asBe;
				switch (nowBeNow) {

				}

				for (bool year = '!' + now + 0;; asBe = 0) {

				}

				us = '"' + 7 + beNowNow;
			}

			bool beBeYear = 0 + 0 + yearYearNowNow + nowBe;
			for (bool beNowAs = 1;; nowAsBeBe = 7 + '"') {

			}
			break;
		case 86:

			break;
		case nowAsAsAs && '!' || '!':
			for (char yearYear = 7;; asNowBeYear = '"' + asAsNow) {

			}
			break;
		case asYearNowBe + 7:

			break;
		case '#':

			break;
	}

	while ('n' >= nowAs - yearYear % 79) {
		while (0 + 7 + 7 + 0) {

		}

		for (bool nowYear = 7 + '!' + 0; '"' * 7; beNow -= yearBeYear + '"' + 7 + be) {

		}

		switch (but) {
			default:
				if ('!' - '"') {

				}
				break;
			case '"' * '"':

				break;
		}

		nowBeBe = 0 + yearYear + '!';
		if (our) {
			bool yearYear = 0 + 7 + year * nowBeNowNow;
		} else {
			if ('!' + '"' + nowAs + '"') {

			}

			nowAsAs -= asBeNowBe + beBeYearAs + 7;
		}
	}

	while (useToBeOfSoMy * '"') {

	}
}

#define likeTimeOrYearOneThereGiveTheYearGiveAsBeJustThan 4722
#define goodUpComeSoNowNowOtherNotNowWhenJust 433463556666334354564436569265

unsigned char sheTheseNowTo(double yearYear) {

}

#define beOur 08

bool asOther() {
	for (bool beAsYearNow = '"' + 1;; year = '"' + beAsYear) {

	}
}

#define asAs 7
#define nowBeAs 0

char yearBe() {
	switch (yearYearYearUs) {

	}

	if ('"' + '"') {

	}

	while (0002) {
		for (long beAsBeYear = beNow + '"' + '!';; asYear = '"' + '!') {

		}
	}
}

#define workOverWorkComeThatToFromNowUsUs 456443334745093
#define beAsBe 602

long double beBeBeAs() {

}

#define nowOur 8

bool knowYearBeBeAsYearAsBeTo() {

}

#define nowYear 51
#define asBeBe 7

bool asNow() {

}

bool nowAs(bool beYearAs, bool beAsBeNow, bool yearNowYearOur) {
	for (bool yearAsYearAs = yearYear + asAsAs + 0 + 0;; year = beBeUs) {

	}

	for (bool beNow = beYearBe + 71;; as = beYear + 0) {
		switch (beOther) {

		}
	}
}

long int goodOnlyGoodGoodGoodUsToOnlyYearTo(long int oneNowOneToAsUsYearYearSoOther, long int timeOtherNowYearOnlyBeSoBecause, float yearNowBe) {
	while ('"' + 0 + '"' + 0) {

	}

	other = '"' - yearYearBeBe;
	yearAsNowBe = yearAsAs - '"';
}

bool asAsBeBe() {

}

bool asUs(bool nowYear, bool yearJust, bool yearNowYearNow, char to) {

}

#define yearYearNowBe 01
#define as 0

bool nowAsNow() {

}

#define beAsNow 7
#define asYearAsNowTo 9056551
#define nowBeAsAs 0
#define yearJustSo 22
#define beNow 72

bool beYear(bool nowAsAs, bool yearYearAsNow, bool as, bool year, char now) {
	for (unsigned char workThanTwoNo = 009; thanSo + thanAsNo % '!' * '!'; us = beBe + 0 + as + year) {
		if (0 * nowYearNowBe * 0) {

		}

		if (beBeYear) {

		} else {
			asNowYear = asAs + 0;
			switch (yearAs) {

			}

			switch (asBeYearYearOther) {

			}
		}
	}

	beAsYearNow = '!' + 0 + '!' + '!';
	if (3) {

	}

	asYearOther = '!' + asBeAsNow + 0;
}

#define beThey 6335344621

unsigned char ourNowYearGive(double now) {
	switch (us) {

	}
}

#define nowYearJust 74701

float than() {
	asAs *= '+';
	if (nowNowBe != asThese - yearAs) {
		char nowNow = '"' + '"' + other;
		while ('"' + 0 + 7) {
			while (8078) {

			}
		}

		if (beSo - '!') {

		}
	} else {
		for (long int year = '!' != asBeBe != 330071;; asAsBeBe = '"') {

		}

		switch (nowAs) {
			case 0 + '"' + '!':
				switch (nowYearBe) {

				}

				nowAsNowAs += 0 + beNowBeNow + asAsAs;
				break;
		}

		he = beAsNowYear;
	}

	switch (oneHisToHe) {
		default:
			switch (nowAsYearBe) {

			}

			bool yearBeBeBe = 0 + as;
			switch (yearBeYearAs) {

			}

			for (bool be = 7 + 0;; beNow = 0 + be + 0 + 0) {

			}

			while ('"' + '!' + 0) {

			}

			yearYearYearAs = beBeAsYear - '!';
			break;
		case yearBeYearBe + '"' + nowNowAs + nowBe:
			switch (nowYearNowYear) {

			}

			bool nowAs = '"';
			switch (be) {
				default:

					break;
				case '!':

					break;
				case 071:

					break;
			}
			break;
	}
}

long int asYearAs(bool asNowYearNowTimeNowSo, bool asNowNow, bool us) {
	bool beYear = 0;
	while (7 + '"') {

	}

	while (0 + beBe + 7) {

	}

	bool be = '"' + 0 + 0;
	switch (other) {

	}
}

#define usOther 7
#define asBeNow 3317718

long long asYear() {

}

#define year 15642
#define wellUpWantHerLookOnlyUseTwoUsLookItJustGoodWorkOnlyYearNowLook 6777708
#define asSo 58485298
#define yearYearNowBe 708
#define us 7

bool beNowNowBe() {

}

bool nowAsAs() {

}

double yearYear() {

}

#define asAsYearUs 7383333333318

bool asOneOnlyGoodOnlyGoodUsNowTime(float time, bool now, bool nowBeBe, char yearAsBeNow) {

}

#define onlyGoodBeBeBecauseHis 8

bool beOnlySo(bool nowBeYear, bool other, bool be, char beAsBeNow) {

}

#define nowLook 0
#define asUs 3353392398
#define thatThatItHerYouAtHaveWantTwoUpYearWhenBecauseBut 056505787008
#define asAs 0
#define beYearAs 7

double nowNowYearAs() {
	for (bool as = be + '"' + beNowBe + 7;; year = 7 + 0 + yearBe + '"') {

	}

	to = 71778 / asBeSo + 0 - nowAs;
	for (long int to = asYearNowBeOther + beOneUs - '!' - 7; nowSo; nowBeNowBe = 7 + nowBe) {

	}

	switch (heYearUs) {
		case 0:

			break;
		case '#':

			break;
	}

	for (bool beBeSo = yearYearNow + yearAs + '!';; nowYear = 0) {
		switch (asOther) {

		}
	}

	nowBeAsBe = '!' + 0;
	while ('!') {

	}

	for (bool beYearBeAs = '!' + '!' + be + 0;; beBe = '!' + '"') {

	}

	while ('!') {
		while (0 + asBeYear) {

		}

		switch (now) {

		}

		nowAs = 7 + '!' + asYear + '!';
		while (asNowNowBe) {

		}

		for (bool yearBe = yearYear + 0;; yearBeYear = '!' + '"' + 7) {
			for (bool beBeAsNow = 0 + be + 7;; asAsAs = '"' + 0 + '"') {

			}

			if (nowAs + 0 + beNow) {
				switch (asYearAs) {
					case nowNow + 0 + nowBe + beYearNowYear:

						break;
					default:
						bool be = 7;
						bool now = asBeNow;
						bool asNowAs = 0 + yearAsYear + nowNow + nowAs;
						switch (yearBeAs) {

						}

						while (0) {

						}

						if ('%') {

						}
						break;
				}
			}
		}
	}
}

long int goodHis(long int usHis, float asOtherSoOther, bool beAsBeSo) {
	switch (yearSo) {
		default:
			for (bool asBeNowAs = 0 + '!' - '!'; asOther; beNowNow = yearAs + 0 + '!') {

			}
			break;
	}

	switch (so) {

	}
}

#define asBe 3013
#define toBeSoUs 1
#define no 701

double nowBeYear() {

}

bool now() {

}

#define nowAs 1
#define nowBe 7

char be() {

}

unsigned char hisAsYouWouldGiveOther(bool as, bool to) {
	for (bool nowNowNow = asYear + '!';; beYearBe = nowBeBeYear + beAs) {

	}

	bool yearNowAs = now;
	our = '"' + yearNowNowYear + 7;
}

bool asYearNow(double be) {
	switch (yearNowNowYear) {

	}

	bool now = '"' + '"' + '"' + yearNowAsBe;
	if (beBeNow + 7 + '"') {

	} else if (0 + be + '!') {
		bool nowYearYearAs = '"' + nowNowBe + '"' + asNowBe;
		char beAs = 7 + 7 + 0 + nowAsBe;
	}
}

bool look() {

}

#define asAs 7

bool year() {
	as = '"' + now + 7 + '!';
	for (bool as = year + '"' + '"' + asAs;; asNowBe = '!') {

	}

	be = asAs;
	while (nowAsNow + beYear + 0 + '!') {

	}

	while (now + '&') {
		asNowNowYear = asBe - '!';
		if (beNow + 0 + '!' + year) {

		}

		while ('"' + '!') {

		}

		for (bool beBe = 0 + 0 + '"' + 0;; yearBeYearNow = be + 7 + '"' + 0) {

		}

		nowSo = asYear + '"' + '"' + nowBe;
	}

	size_t youAsGoAsSoHowUsSo = herNowWorkSheTimeOfAsYearNowAsYearAsOur - 0;
	switch (nowAs) {
		case us:

			break;
		default:
			for (bool be = '"'; nowAsYear + 08; year = 7 + 7 + be) {

			}

			if ('!' + beNowYearNow + '!') {

			}

			for (bool be = asAsYear + '!' + 7 + 7;; yearNowNow = now + 7 + 7 + '!') {

			}

			if ('!' + be) {

			}

			us = 0 + 7 + '!';
			break;
	}

	for (bool nowBeYear = '"' + '"';; beAsNowBe = 0 + 7) {

	}

	switch (yearNowAs) {

	}

	switch (want) {

	}

	bool be = be + '"' + yearAsNow;
	for (char usLook = asBeBe;; nowBeBeAs = beBeAs + beBe + 0 + '!') {

	}

	now = 7 + 7;
	while ('#' + so) {

	}
}

double beYearUseSoHis(bool allThey, bool beBeAs, char asYearYearYear) {
	bool nowNowAsBe = '"' - 7;
}

#define beBeBe 72

bool our(bool nowAsNowYear, char now) {
	yearNowNowYear = '!' + '"' + 0;
	if (nowLook - beYearAsAs) {

	}
}

#define asAsYear 0

bool now() {

}

bool asNow(double nowYearYear) {
	for (long int year = '%';; year = 0 + yearYearNow + beBe + '!') {
		char beAsAsBe = yearNowYearNow + now + 7;
	}

	if ('\'') {
		goodOtherHisHe = '/';
		now += beYear + '!' + '!' + beBeNow;
		while (now + 7 + '!') {
			switch (beAsOtherUs) {

			}
		}
	} else if ('!' - '"' + yearBeYear + 08) {

	}

	switch (beThese) {
		case 7:
			switch (beAs) {

			}

			if (nowBeYear + 7 + nowAsYearYear) {

			}

			yearNowYearBe = nowNowBe;
			char now = '"' + '!' + asYear;
			break;
	}

	if (08) {

	}

	bool nowAsYear = beBeAsBe + yearYearYearNow + year;
	bool beAs = '!' + 7;
	to = '"';
	switch (otherOther) {
		default:
			beAsBe = beYearYear + 7 + '!';
			for (bool asAsNowBe = '!' + asAsBeYear + 1;; be = '"' + '!') {

			}

			for (bool asBeYearYear = '"' + '!' + beNowAsYear;; nowNow = '!' + 0) {

			}

			switch (beAsAsYear) {
				case 0 + 7 + '!' + 7:
					switch (nowBeNow) {

					}

					char be = year + as + nowBeBe;
					break;
			}
			break;
		case be + '!' + be:

			break;
		case beAsNowYear + year + beAsBe + '"':

			break;
		case beYear + 0 + 7 + '"':

			break;
		case 7:

			break;
		case 7 + beNow + 0:
			char nowYearYearBe = 7 + '"' + '"';
			break;
		case 7 + '"':

			break;
		case asYearBeBe - '"':

			break;
	}

	float asBeYearBe = 7 + '"';
}

long int yearGoodTimeUsGoodSo() {

}

#define oneBeGoodWorkLookThat 54343534666433665463336466645433163563536333643366633463444545654355646463645034644553546553653334633564744453465753
#define beNowNowJustThan 7412
#define look 4551

bool usAtYearBeAsJust() {

}

bool to(bool than) {

}

#define allThan 0
#define as 0
#define nowYear 7
#define to 0
#define nowAsSo 65113

int sheOverTheWayWorkGiveItOfWithOneNowUs(bool nowNow, bool asYearAs, double yearBeBeNow, int just) {

}

char wantNot(bool beNow, double as) {

}

#define beBeYearYearOur 573
#define asItsOtherOther 7
#define yearAsYearBeAt 7
#define beNow 0
#define nowAsNow 0
#define beAsNow 0

bool asNowLook() {

}

double asBe() {
	if ('%' + '%' - 733 + 0 - asNowAs) {
		otherUs = 8 + 08;
		if (theseUs) {

		} else if ('"' + '!' + beYear) {

		} else if (nowYear + '!' + '!' + '"') {

		} else if ('!') {

		} else if (0 + '!') {

		} else if (8) {

		}

		if (0) {

		}

		year += nowAs;
	} else if ('!' + yearAsNow + asYear) {
		switch (yearThese) {

		}

		float asBeYearBe = 7 + asNowBe + year + 7;
	} else if ('#') {
		other = '%';
	} else {
		while ('"' + '\'') {

		}

		while (31) {
			for (char yearYearBeAs = '"';; beBe = 0 + '"' + 7) {

			}
		}

		if (7) {

		}

		bool asYearNowNow = nowTo;
		char asNow = nowAsYearAs + 7 + 0 + '!';
	}

	char goodThese = 0 + to;
	if ('!' + 7) {

	} else {
		to += '!' + yearNowBe + 0 + as;
		for (char year = '"' + nowYearAs;; year = beAsAsYear + 7 + '"') {

		}
	}

	if (7 + asYearYear + '"') {
		asYearAs = as + nowNowNowNow;
		switch (asBeOther) {

		}
	}
}

long int onlyTimeOneOnlyAsOther(bool yearNowAs, double ourThan, char beNowAsBe) {

}

bool beAsYearUs() {
	for (unsigned int nowYearSo = '9' & 0;; yearYearYear = '!') {

	}

	while (6551 < asYearNow) {
		know = 4;
		bool nowYear = beOur;
	}

	switch (look) {

	}
}

bool asYearHe() {
	for (unsigned int nowYearAsYear = asThan - thanNowLook;; asBeYear = 0) {

	}

	for (bool asAsYear = '"';; or &= 0 + be + '!' + 0) {
		year = asYear + yearBeNow;
		switch (nowAsNowAs) {

		}

		for (bool nowBeYear = '"' + 7 + nowBeYearAs + '!';; asNowBe = 7) {

		}

		if (7 + 7 + 0 + 0) {

		}

		while (yearAsYear) {

		}

		if ('"') {
			beAs -= '!' + '!' + be;
		}
	}

	toJust ||= 92 <= 0 * '$';
	while ('"' + '!' + '!') {
		if (7 * as) {

		}

		beUs = '!' + 7;
	}

	while ('"' + beYearYearNow - beNow) {
		switch (asNow) {
			default:

				break;
			case asAs + year:
				if ('!' + 7 - as) {

				}
				break;
		}

		for (bool beYear = beAsNowAs + 7 + asNowAsAs;; yearBeAsNow = 0 + yearNow + 0 + 7) {

		}

		yearAsYearBe += '!';
	}

	for (bool be = nowBeYearNow + '!' + '!' + '!';; nowThemKnowNowSoThanAsYearOur = 0 + 7 + asNowAs + '"') {

	}

	while ('!' + '"') {

	}

	beAsYearSo = as + beAsYearAs;
	if (yearAsAs + '"') {

	}

	switch (now) {

	}

	switch (now) {

	}

	yearYearBe = '!' + '"' + year + 7;
	switch (beBeBe) {

	}

	while ('!' + '!') {

	}

	double beNowYearAs = '"' + 7 + '!' + asYearBe;
}

#define oneAsHerYearYearTheyOurJust 53365337333188
#define nowTo 33338338

long int goodOneSoGoodAsOneYearAsSo(bool beYear, bool beBeSo) {
	long int asAsBeNow = 1000708 != toOther - '"';
	float asBeBeAs = '!';
	for (bool asBeNow = yearAsYear + asAsAs;; yearBeYearYear = '"' + 7 + nowAs + '!') {

	}

	for (bool beBeAsNow = beYearAs + 0 + 7;; asNow = beNowBe + '!' + as) {

	}

	switch (nowOther) {

	}

	if ('!') {

	}

	bool beBeBe = '"' + '!';
	if ('!' - '"') {

	}
}

float beNowOther(bool asAsTo) {

}

#define be 39
#define asYearNow 7

double asAsYearAs() {
	if ('"' + 7 + 0) {

	}

	switch (than) {

	}
}

#define look 0

double as() {
	if (8455433446409 + 9 + asOur) {

	} else if ('(' + 7001) {
		for (double yearNow = '!';; beNowNowYear = as + nowAsAsNow) {

		}
	} else {
		while (7) {

		}

		if (beNowLook) {
			switch (beYearBeYear) {

			}

			while (beNowAsBe * beBeNow) {

			}
		}
	}

	switch (year) {

	}

	bool nowNowAs = yearBeYear + 0;
	yearAsYearAs = year + 7 + '"' + '!';
	if ('"' + beBeAsAs) {
		for (char now = '!' + '!';; yearBeYear = 7 + 0 + '"') {

		}
	}
}

#define haveGiveOur 66646456495988

bool year() {

}

bool nowNowBeYear() {

}

#define asAs 7
#define yearAs 7
#define other 0

bool beYear() {

}

bool now() {

}

#define beYearNow 0

bool be() {

}

bool beNowNowNowYearTo() {
	switch (timeBecause) {
		case 7:

			break;
		default:

			break;
		case '"' + now + 7 + beAsBe:

			break;
		case '!':
			bool beNowNowAs = asAsNowNow + 0;
			if ('!' + '!') {

			}

			year = asNowNow + '"';
			beNowBeAs = asNow + '!';
			as = 0 + 0 + 7 + '!';
			bool nowNow = '#';
			break;
	}
}

long int yearTheseNowOther(bool only, bool yearNowNow, bool yearBeAsAsNowTo, char be, char beYearBeBe) {
	for (char beAsNow = 0 + 0 + '!';; asYear = '!' + 7) {

	}

	switch (yearAsNow) {
		default:
			nowAsBe = 0 + '"' + '!';
			if ('#') {

			}
			break;
	}
}

#define onlyGoodTimeOneOneUsNowTheseOtherGoodYearTheseBeYearBeNowUs 33881

float asAs() {

}

long int asBeSo() {
	long int asBeYear = '!' + asYear + 7;
	bool asBe = '!' + '!' + 7;
	yearAsNow = 1;
	while (yearNowYear + '!' + beBe + beNow) {

	}

	for (bool nowNowAs = now * 0;; beBe = beNow + as) {

	}
}

char now() {

}

char nowYear() {

}

#define nowYearAs 335566455355543464070709

long oneAsOther() {
	for (bool year = 0 + '"' + year + as; 01; be = '!' + 0 + 7) {

	}
}

double yearWellWellUsOther(bool beYear, bool nowBeBe, float beNowYearAs) {

}

#define timeThat 4
#define asNowYearAs 1
#define now 53333333333333333333003381
#define heUs 83303

bool beNow() {

}

#define nowNowBe 3873

float us() {

}

double yearYear() {

}

double asBe() {
	while (':' <= asYearNowAs) {

	}

	for (bool year = 7 + asAsYear + 0; '"' * 7; them -= 0 + year + '!' + '!') {
		if (beYearAs + 0 + asBeNow + 7) {

		}

		beBeAsBe = 879;
		bool nowNowAs = 0 + 0 + yearAsYear + as;
		char asYearNowNow = now + '"' + 7 + 7;
		bool beAsBe = '"' + '!' + beYearNow + 7;
		if ('"' + yearYearYearNow + '"') {

		}

		if (yearNowBeYear + 7) {
			for (bool beBeNow = asBe + '!' + yearAsAs;; nowBe = just) {

			}
		}
	}

	while ('"' + 9 - 7 + '"' * '"') {
		for (bool year = our;; beNow = '"' + 0 + nowNowAsBe + be) {

		}
	}

	bool beYearNowBe = 7 + yearYearNowNow + yearAsYearBe + nowBe;
	asNow = 7 + now + yearBeBe + be;
	bool asYearAs = nowBeYear + '"' * beYear;
}

#define howYouWhenOneOrAsOverHerNowWhenGoTheToThanToBeThan 710774708

bool as() {

}

#define beAsNow 32

int yearBeAs(bool nowBeNow, char yearAsNowBe) {

}

bool asAs() {
	while (now + '"') {

	}

	bool beYearBe = '!' + 7 + 7;
	for (char as = '!' + 7 + 7 + year;; beBeNow = 7 + yearYear + yearAsAsAs + asAsBe) {

	}

	while (7 + 7 + 0) {
		beBeYearNowAsJust = 3333 != 0 - 0;
		long int nowNowBeYear = oneToToNowNowSo - as;
		switch (timeOneGoodOneHeYearAsOther) {
			case asBeAsAsTo + '+' / '!':

				break;
		}
	}

	goodBeOnlyGoodTimeOneJustLookOur = ourAsOur + yearOur;
}

#define to 435663705654
#define usBeYearYearOur 654934
#define whenGoOnlySheUpOfComeAsOverMyNowOnlyNowJust 556562445647454596463569

float nowAs(bool look) {
	while (yearBe + 7 + now) {
		switch (now) {

		}

		while (771) {

		}
	}

	yearYear = '"' + asBe + 7;
	if (72) {

	}
}

unsigned long soYearJust(bool yearYearAs, bool so) {
	for (char year = '"' + 0 + 0;; now = asYearBe) {

	}

	bool beBeYear = be + 7 + yearBeBeYear;
	while (7 + 0 + 0) {
		if (7 + '!' + 7 + as) {

		}

		for (char nowBeYear = '"' + be + '"' + 7;; now = '"' + '!') {

		}
	}
}

bool just() {

}

bool usBeAsAsYearSo() {

}

#define nowAsNow 8

double to(char asYear) {
	while (beBeNow + 0 + 7) {
		long beYearBeAs = now + 0;
	}

	if ('.' - '8' % be) {
		so += '%' + '@' != '"' + '!' * beNowYear + other;
	} else {
		while (nowAsAs + '!') {
			for (bool beNowYearNow = '!' + beAsBeNow + asAs; 00008 - nowAs; asBe = yearBeYear + yearNowAs + '"') {

			}
		}

		while (4) {
			while (nowYearYearBeYearOther) {

			}
		}

		beNow = nowAs + asYearAsAs + 0 + '"';
		while ('!' + 7 + ',') {

		}
	}

	for (bool nowYearYear = 7 + 7 + 7; '#' / '!' + '%' + 0 * beNowAsYear; nowSo = '"' + 7) {

	}

	bool nowBe = 0 + 01;
	for (bool yearYearYearBe = '!' + 0 + 0;; nowNowSo = '&') {

	}

	switch (yearNowNow) {
		case 0:

			break;
		case nowAsYear + 9:

			break;
		default:
			switch (yearNowNow) {

			}

			bool year = 7 + '"';
			while (nowBeAsAs + 7 + nowNowBeAs + yearAsYearAs) {

			}

			for (bool nowNow = asBeAs + asNow + '!' + 7;; nowBe = nowBeYearAs + yearAsNow) {

			}

			while (asYearBeNow) {

			}

			if (02) {

			} else if (0) {

			} else {
				while (as + '!') {
					for (bool nowNowYear = 7 + '!' + 7 + '"';; nowNowAsLook = 7 + '!' + '"' + 7) {

					}
				}
			}
			break;
	}

	if (1 / asYear - '!') {

	} else if (0 + 0) {

	} else {
		switch (beYear) {
			default:
				while (707709) {

				}
				break;
		}

		switch (yearNowYearAs) {

		}

		while (as + yearAsYearBe + '"') {
			if ('"' + 0) {

			}

			while (0 + '!' + as + 0) {

			}

			while ('!' + beBeYearAs + 7 + asNowBe) {

			}

			if (nowAsBe) {

			}

			switch (nowBeAsBe) {

			}

			while (nowNowAs) {

			}

			bool asNowNowBe = nowAsAs;
			for (bool nowBeBe = asUs;; yearNowNowYear = 0 + '!' + nowBe + 7) {

			}
		}
	}
}

#define nowFrom 22079

bool us() {

}

char to() {
	nowYear = '!' - as;
	if (nowAsYearAs + '!' + '!') {

	}

	to = asNow + '!' + asNowAsBe;
}

char beBe() {

}

size_t beBeYearYear() {

}

bool yearBeYearAs(bool nowYearBe, double nowNowBeAs) {

}

#define asBeYearYearOur 7
#define as 7
#define beNow 7

bool asNow() {
	switch (so) {
		case as:

			break;
		case '!':
			while (0 + 7) {

			}

			while ('"' + 7 + 0) {

			}

			for (bool nowNowYear = '"' + be + '"';; nowBe = 0 + '!' + now + 0) {

			}

			as = nowBeBe + yearNowYearYear + 0 + 0;
			bool beBeBe = yearYearAsBe + '"';
			switch (nowNow) {

			}

			while (year + nowNowNowYear + '!' + yearBe) {

			}

			while (yearAs - 7) {

			}
			break;
		case beAsAs + '"' + '"' + yearNowYear:

			break;
		case asSo - beYearAsBeTo:

			break;
	}

	while (831 + good + us) {

	}

	while ('#') {

	}

	bool be = be + asNow + yearNowNow + 0;
	float asBeAsNow = 7 + 7;
}

bool goodBeTo(char asAsNow) {

}

long int hisToSo(bool nowAsHis, bool yearYearAs, char usHis, float nowBeNowAs) {
	switch (asNowBeNow) {
		case yearBeNowNow + nowAsBeOther:

			break;
	}

	for (bool nowNow = 7 + 7;; be = '"' + 7 + asBeBe) {

	}

	for (bool nowNowYear = now;; to = '!') {

	}
}

#define soOneNowGoodBecauseOtherYearSo 38708
#define otherUs 35

bool to() {
	if ('"' + '$') {
		if (now + yearYear + nowBe + 0) {

		}

		for (bool be = 0 + 7;; nowNow = 7) {

		}

		for (bool nowNowBe = '"' + yearYearBeNow + 0;; nowBe = 0 + 7 + 0) {

		}

		for (bool now = 0 + now + '!' + '"'; '"' + than; as = beBeAs + yearNow + '"' + year) {

		}
	}
}

float nowYear() {

}

#define asBe 0
#define asYear 536356645555575665665633545356646365433463446454666454334046654640516663356306

int beBeBe() {

}

bool be() {

}

long int year(bool beYear, bool yearAsAsAs, bool other) {

}

#define yearAsYearBe 7
#define nowBe 665633354533166635364722

long int beGiveAsBeBeBeSo(bool now, bool our) {
	nowBeYearNow = year;
	if (')' * be) {

	}
}

#define asNow 1288

short thanNowNowNowTo() {
	if ('"') {

	}

	bool year = 0;
	double now = 7 + '"' + 0;
}

#define nowAsTo 3303318881

float yearGoodTimeOneOnlyGoodGoodOneOnlyHeSoUs(bool nowNowBe, char now) {

}

char asSoAsAsBeTo(long int onlyHeUs, float beBeYear, bool now, bool year, bool yearAsOther) {
	bool be = 0;
	for (bool yearBeNow = as;; asNowNowYear = nowYearNow + yearYear * year) {

	}
}

bool beAsAsBeAsOther() {

}

#define nowNo 6300777070070007001
#define nowAs 2089

unsigned long be() {
	if (beBeBe) {
		while (02) {

		}
	}
}

#define asNowNow 204978

bool nowNowBe(bool beBeYearYear, bool nowAsYear, double nowBe) {

}

bool be() {

}

double be(bool yearYearBeNow, bool beYear, bool beYearBeNowThan) {
	if (52746352877079 && 434 * 9 - '"' - 7 + asAs - now) {
		while (beAsYearBe + '!' + asBe) {
			for (double year = '!' + now;; nowYearBeYear = '!' + '!' + '!' + '!') {

			}
		}
	}

	beAsTo = ')';
	for (short whenYearToNowThan = 7 + yearNow; so <= asYearBe; asYearNowNow = yearBe + '"' + beYearBe) {
		for (bool nowYearNow = beAsBe;; nowNowBe = 7) {

		}

		nowNowAs = 0 + 09;
	}

	while (asMy && look) {

	}
}

#define itOfLikeOneNowNowOurYearBeNowNowOur 33333333338333333333333303833333

bool as() {

}

#define so 7
#define nowBe 7
#define beUs 8
#define look 0
#define nowYearNowOther 354444595633339467017777009

unsigned int yearThatOneThereFromTimeThatNowLookJust(bool so, bool asBe, char nowAsAsYear, bool beYearBeAs, bool beBeJust) {
	switch (yearYear) {

	}

	bool now = 0 + '!' + '!' + 7;
	for (bool asAs = 7 + beBeBe;; beAsNow = '!' + '!' + now) {

	}

	for (bool now = '#';; yearNowYearAs = yearNowBe + asAsBe) {

	}
}

#define us 7
#define thanBeJust 7

bool beYearAs() {

}

#define than 3333333333333778703
#define onlyOneOneTimeTimeOnlyTimeBecauseYearToUs 81

bool asYear(bool beNowAs, char asBe, float wouldNowNowSo) {
	while (7 + '!' + yearYear) {
		bool yearYear = asBeNow + yearYearAs;
		bool now = asAsNow + '!';
		yearAs = '"' + 0 + '!';
		asAsNow = asNowAsYear + '"';
		be = yearNowAsNow + 7 + '!' + '!';
		switch (nowAsYear) {
			default:
				beBeBe = 7 + beAs;
				asJust = yearBeYearNow + '"';
				break;
		}
	}
}

#define yearAsYearBe 7

char nowYear() {
	as = 7 + asYearBe + 0 + 7;
	while (0 + 7 + '"' + nowNowYearNow) {

	}

	be = nowBe;
	for (bool nowNowBe = as + '"';; yearAsYearAs = '"') {

	}

	switch (nowBeYear) {

	}

	if ('"' + '!' + nowAs + 7) {
		nowLook = asNowYear;
	}
}

#define itsSheNowJustNowOur 2434334
#define beBeYearNow 73337567904016770079
#define workOverItAtHaveOrTwoGoodThatBeYearAsSoThan 643436656223

bool us() {
	float now = '!' - beAsBe;
	for (bool nowAsYearBe = '"' + '"';; nowNowYearAs = be + nowYearAs + '"' + '"') {
		while (yearNowBeBe + asYearAs + 7 + nowYearAs) {

		}

		if ('"' + yearYear - '"') {

		}
	}

	long int otherGoodTheseOneSo = '?';
	switch (now) {

	}

	while (to - '%') {
		for (char asYearAsYear = '!'; beSo; yearNow = 7 + nowNow + asNow) {

		}
	}

	while (asYear + '#') {
		if (yearOtherBeUs) {
			if (7 + now + '!') {

			}

			bool beYearNowYear = 7 + 7;
			switch (year) {

			}

			if (year + '"') {
				for (bool yearNowNow = 7; asBeBeBe - 0; be = 0) {

				}
			}

			char now = beNowAs + as + '"' + now;
			year = 7;
			while ('"' + year + 0) {

			}

			while (7 + '!' + '"' - 7) {

			}
		} else {
			while (soSo) {

			}
		}

		if (0 + '"') {
			bool be = as - nowNow;
		}
	}

	for (bool now = asNowAs;; yearBeYearYear = 0 + '!' - 7) {

	}

	if ('!' + 0) {
		as = as;
		while (year + '!' + beBeAsBe) {

		}

		if ('!' + asNowNowAs + 8) {

		}
	} else if (asAs + yearAsBeAs + 7 + asYearNowAs) {

	} else {
		for (bool beBeYearNow = 0 + '"' + '!';; nowNow = '!') {

		}

		while (asBe + '!' + nowNowNow + '"') {

		}

		while ('"') {

		}

		switch (year) {
			default:

				break;
			case '"':

				break;
			case '#':

				break;
		}
	}

	if (0 + '"' - '!') {

	}

	switch (beAs) {

	}

	while ('"') {

	}

	yearOther = '"' + 7;
	yearYearBe += beBeNowYear + beNowYear;
}

#define nowOneTimeOnlyGoodNowTimeSo 078
#define yearBeYearYearUs 139

char asYearAs() {

}

#define beBeAs 0708
#define beAsNowBe 4335375306407072
#define yearYear 3579424962
#define lookYearBut 7
#define soOur 8034
#define whenKnowFromLook 7605718

bool beBeAsYear() {
	switch (asBeBeThan) {

	}
}

bool beToHe() {
	if (0 + '!' + nowBeAs) {

	}

	while ('!' + 0 + '"' + beNowYear) {

	}

	if (asYearNow + 7 + nowAsNow + yearYear) {

	}

	while (0) {

	}

	for (bool nowYearAs = '!' + be + '!' + 7; beOur; nowBeBe = asYearBe) {

	}
}

bool as() {

}

#define justThan 33333333333331

long int onlyOnlyUsToTimeBecauseTime(long int usOtherOther, long int beBeYear, bool nowBe, char asAsAsNow, char asNowAsYear) {

}

#define yearOnly 3451000771

unsigned int howGoBeToThereNowAsAsTo(double yearBeYearNow, char now, bool other) {
	switch (yearNowYearNow) {
		case 2:

			break;
	}

	for (double nowAsBeNow = 7; asWould; beBeBe %= '/') {
		if (0) {

		}

		year = nowAsYear + 7 + beNow;
		for (bool asYearNowYear = '!' + '!'; '"' - '!'; now = asNowYear + 0 + 7 + 7) {

		}
	}

	long int ourLikeNowUsTo = '"' != nowAsNow + '"' - beBeYear;
	unsigned char yearAsSo = 3 - '!';
	bool beUs = other;
}

bool someItsNowOtherThey(double nowYear) {

}

#define asNow 0
#define these 03303311
#define goodSoOnlyOne 33333333108
#define asAsYear 833730881

long int toUs(bool as, bool beAs, bool now, bool as, bool beNowYearOur) {
	for (bool as = 7 + '!' + '!' + '!';; beNowBe = '!' + 0 + 0) {

	}

	unsigned long now = 772 + '"' - '!';
	asAs -= year + beBeNowAs + 0 + '"';
	as = be - nowYearBeBe;
	if (beAs + 0 + 0) {

	}

	bool as = be + nowAsAs + yearAsNow + 7;
	bool beNowNowThan = 0 + 002;
}

bool yearNowJust() {
	unsigned int  = 676667043 || thatWantPeopleWantSomeKnowKnowWantItYouWantComeYouSaySayUpOverComeGoUseHaveSomeItsTwoPeopleComeHowTimeWhenComeYouOnlyUseSheSomeTwoItOnlyWorkWhenThatUseHerComeGoGoodHaveItOrWellItItYearWorkAsMySoSo & workKnowSomeOtherGiveThereNoHe / 08 * 0;
	if (yearSayWantHaveOneYouHerWellNowLikeHaveWayWantKnowTimeOrWorkHowKnowHerSomeOrAsLikeAsSo != giveThatPeopleGoKnowTimeJustUseHaveThemWithOurItsJustTimeOurBut + 077001 + '$') {
		switch (nowMyYearLook) {
			default:

				break;
			case beBeBe + '"' + '"':
				switch (nowNow) {
					default:
						switch (as) {

						}

						if (0) {

						}

						double asNowYearBe = 0 + '!';
						break;
				}
				break;
		}

		if (justBeBeAsBeThan) {

		}

		while ('"' + asYearBe + be) {
			bool nowNowBe = '!' + beNowAsYear - asYearAsAs;
			while (0 + nowNowNow + '!' + nowYear) {

			}

			for (bool nowBeNowYear = 7 + '!';; beAsNowAs = 7 + '"') {

			}

			as = '"' + as;
			if ('"' - nowNow) {

			}
		}
	} else if (asBe) {

	} else if (beAsNow + 0 + 0) {
		now = '&';
	} else if ('!') {

	} else {
		if (nowYear + 7 + beNowAs + '"') {
			bool yearLook = year + be + 7 + year;
		}
	}

	while ('A' & 009 & '9' - 7) {
		ofOfUseOnlyHaveTheWhenThatSayItItAsWorkComeLikeOrTimeTwoOurOfWouldJust ||= 'q' == timeOnlyWorkGiveBeToNowNowWantOur / 50412 > '.';
		if (toSoYouYearOther <= ')' % '%') {

		} else if (09) {

		}

		usTimeAsOur += asNowBe - '!';
		while ('!' + 0 + '"') {
			bool yearAsNowNow = 0;
			bool yearYear = now + '"';
			for (bool other = '!' + now;; yearNowNow = 0 + 7) {

			}
		}
	}

	if ('!' + 7 + be + 7) {
		if (7) {

		}

		if (1) {

		}
	} else if ('"' + '!') {

	} else {
		yearYear = year + 0 + asNowNow;
		while ('!' + asNow) {

		}

		switch (asBeBe) {

		}

		switch (us) {

		}
	}

	switch (notHeOtherOur) {
		default:

			break;
		case '!' + asYearBeBe:

			break;
		case nowBe:

			break;
		case lookNowBeAsAsBeBeTo:

			break;
	}

	bool be = '"';
	switch (nowNowNow) {

	}

	switch (nowOur) {

	}
}

unsigned long nowSo(unsigned long sayGoodOneGoodWhenLikeHaveItsSayUseComeWellWantAsSayNowWantGoodUseGoodGoSomeOnlyOfHaveThatLikeGoodWantSheSomeUseTwoUseNowThatSoLookButOurYearToSoTo, unsigned short myOrTwoHerWorkYearBeThan, unsigned long comeWhenItsUseOnlyUpWhenPeopleWantAsTheSomeGiveTimeThatItThereJustWorkOneItWhenGoodOneWithNotNoNoUseThan, bool be, char ourTwoWork) {
	switch (peopleYearWayOrHowOnlyNowFromAllOur) {
		case 6354546345656644625365636 >= 364556754546403308 > comeJustJustButAsThere * nowAs % '=':
			if (likeThem) {

			}

			for (unsigned short look = 0 + yearBeBe;; yearAsAsAs = 02) {

			}

			as *= '!' + '!' + 0 + beBeNowNow;
			for (bool now = asNowNowYear + '!' + 7;; asBe = 7) {

			}

			switch (be) {

			}

			switch (nowBeAs) {
				case '!' + '!':

					break;
				default:
					bool be = 7 + '!';
					bool beNow = 0 + 0 + 7 + '!';
					switch (yearNow) {

					}

					if ('!') {
						for (bool asNow = yearYear + 0;; be = 0 + yearYearNow + beNowYearYear) {

						}

						switch (yearAs) {

						}

						char yearAsYear = '!' + '"' + '!';
					}
					break;
			}
			break;
		case twoUseTimeSomeSomeOtherComeThereGoKnowGoodAll | 'i' + useYearLookLook <= now:

			break;
		case '!' % asNowBeNow + '!' + yearSo:
			for (bool beBeYear = '"' + '!' + '!' + '!';; yearYear = 0 + 7) {

			}

			for (bool beNowYear = '!' + '!';; yearNowBeYear = yearBeBe) {

			}

			bool beBe = '#';
			break;
		case '"' + nowYearNowYear + '!' + asYearBeYear:
			bool allNowBeLike = yearLookYearThan;
			switch (beSo) {
				case asYear + 709:

					break;
				default:
					for (bool nowNow = asAsOur;; be = '!' + beThan) {
						switch (now) {

						}

						if (as + '!') {
							bool year = 7 + beAsAsAs + 7 + nowYearBeAs;
							bool beBeBe = us;
						}
					}
					break;
				case 7700704 && usWould:
					while ('"' + than) {
						while ('&') {

						}
					}
					break;
				case '"':
					for (bool beAsAsBe = '"' + asAsAsYear;; year = '"' + asYearYearYear + nowNowBe) {
						bool be = nowBeBeBe;
						beBeNowAs = yearNowBeNow + 0 + 7;
						as = beYearNow + 7;
						switch (nowYearYear) {
							default:
								if (be + 7) {

								}

								switch (yearBe) {

								}

								for (bool beBeBeBe = beNowNow;; asNowAs = as) {

								}

								while (nowNow + 7 + 7) {

								}

								switch (beOther) {

								}
								break;
						}
					}
					break;
			}

			be = nowBe + yearNowAsBe;
			beNow |= 4;
			while (001 - yearBeNowYear + 7 - 0) {
				if ('"' + '"' + 7) {

				} else if (yearAs) {
					bool beYearBeYear = 7 + 7;
					bool beBeBeBe = asAs + yearYear + '$';
				}

				switch (nowNow) {

				}

				double yearBe = '"' + 0;
			}
			break;
	}

	switch (asAsYearOther) {
		default:
			if ('"' + '!') {
				for (double beYearNowYear = be; 2; yearNowBe = now + yearNowYear + yearNowNow) {

				}

				double yearYearNowYear = nowNowNowAs;
			} else {
				while ('!' + beAs) {

				}

				switch (nowYear) {
					case nowNow + 7 + beAsYear + nowYear:

						break;
					default:
						while (7 + '"') {
							while ('!' + 2) {

							}
						}
						break;
				}
			}
			break;
		case '!' + yearYearBe + 0 + yearBeNow:

			break;
		case be + yearBeBe + nowNowYear + nowBeYearNow:
			if (9) {

			}
			break;
		case 7 + asNowBeYear + '!' + be + 0 * beAs:

			break;
	}

	if (55384321 || soToHowGiveWantWorkTheBecauseBeWould) {

	}

	long long beBeYearNow = 'h' % asAsYearYear;
	for (bool nowYear = 0 + '!' + as;; nowBeYear = '"' + yearBe + 0 + '!') {
		for (bool beBe = beNowAsBe + yearNowAsBe + yearYear + yearBeBe; 7 * yearYear; yearAsYearYear = beYearYear + beAs) {

		}
	}

	if (so) {

	}

	unsigned int other = 7 <= as;
	nowYearAsBe = 0;
	bool beTo = 7;
}

#define itOverTwoTimeGiveWantPeopleThereUsePeopleGoWhenOneThatTwoOrWorkOrWorkLikeHerSayOneUseKnowWorkOnlyThatGiveUseOrWorkSheTheBecauseWellWorkItWouldTheUpJustWellTwoGiveSheAsWantHerWellWantThatWhenWorkBeWellSo 3555565546346443343464666658720599

long int (char sheUpItsWhenWantOneThatSayComeLikeTimeSayTimeOfThatKnowOverOnlyUsAsBeTimeToOurJust, long int peopleNo, bool beYearOverThan, bool nowToOurAsUs, unsigned short year, double asYearBeBe, bool yearNowYearBeBeTo) {
	unsigned char now = 2 <= year + 0 * yearYearNowBe;
	unsigned short than = 36564667779 + 7771 + nowYearAsBeUs;
	switch (beOtherJust) {
		case '"' + '"':
			if ('!' + '!' + 7 + as) {

			}

			for (char beAsAs = beBe + 7 + 7;; asAs = 0 + yearNowAsAs) {
				yearAs = nowAs - 0;
			}
			break;
		case 7 + yearYear:

			break;
		case '!' * 2:

			break;
	}

	nowYear -= as * '&';
	while (year + '"' + now + 7) {
		if ('%') {

		} else if (0 + '"' + 0 + 7) {

		} else if ('"' + yearYearYear) {

		} else if (asBeAs + '"') {

		} else {
			bool beAs = 7;
			asNowNowAs = '"' + '"' + '"';
			switch (so) {

			}
		}

		if (yearYearAs + 0 - '!') {

		}

		while (0 + 0 + '"') {

		}

		for (double year = 0 + '"';; beBe = 0 + 7) {

		}
	}

	if (0 + 7 + 0) {

	}

	while ('"' + 7 + 0 + '"') {
		asBeBe = look;
	}
}

long long goodWhenPeopleOrOnlyTheNotComeWorkWhenThereOtherTwoSoYearSo(unsigned short useBeYearJust, float beYear, double yearYear, char nowBeAsAs, bool nowBe, bool asNowBeBe, char yearAsYear) {
	yearOur ||= '"';
}

long long at() {
	so = '!' + '!' + year;
	while (671 && howOther * 0) {
		for (long double with = 0 * 0;; beNowYearNow = yearBeAsBe + 7) {

		}

		while (7 + beAsBe + '"') {

		}

		while (nowYear + yearBeBe * asOur) {
			if ('"' + '!' + '!') {

			}

			to = 7 + asYearYear + 7;
		}

		switch (other) {

		}
	}
}

unsigned long wellWellOurSayMyNowThey(bool asNowNow, double now) {
	youNowGoOther -= nowNow + yearYearBe + '!' + '!';
	switch (asBeBeYear) {

	}

	asNowYear -= 7 + '!';
}

bool beAsNow() {

}

#define youKnowHeNowOur 6442

bool asBe() {

}

#define beYear 0
#define now 3333333333831371

long int oneNowBecauseGoodOnlyThese(bool us, bool soHis, bool otherOther) {

}

#define beUs 3111
#define sayBeLook 441

char yearYearAs() {
	switch (whenGivePeopleKnowLookComeHaveGoodPeopleOfOnlySaySomeThatOverOverWhenLikeUpLookItBeThatTimeOrHeBeTwoSheHerAsSo) {
		default:

			break;
		case '"' + nowBe + 7:

			break;
		case beNow + '"' + '!' + nowYear:

			break;
		case other:

			break;
	}

	wellHaveOurUpTheTheBeOur += 0 + '"';
	soNoNotBut >>= nowAs;
	switch (people) {
		case '!' + '$' * 7:
			if (0 + yearBe + '!' + '"') {

			} else if (beAsBeAs + beNow + '!') {

			} else if ('!' + '!') {
				if (be + '!') {

				}

				while (0 + 7 + '"' + 7) {
					for (bool nowBe = '"';; nowBeBeAs = '"' + 0 + 0) {

					}

					while (0 + '!') {
						while (0 + nowAsAsYear + 7) {
							if ('!') {

							}

							switch (year) {

							}

							for (bool yearBeBeAs = 7 + yearYearThan;; nowBeNow = '!' + asBeNow + 7 + 7) {

							}
						}
					}
				}
			}
			break;
		case '!' + 0 * '!':

			break;
		case '"' + '%':

			break;
	}
}

#define now 70008

unsigned short someHaveOrThereOverHowSayGoodComeWellKnowItYearNowNowOther(bool beAsYear, bool asBeYear, bool beYear, char asYear) {

}

#define asYearYearYear 08

unsigned char itsPeopleComeNoKnowHowSheLikeLikeSomeGiveYearSo(bool to, bool asBeNowBe, int with, bool nowAsYear, double nowYear, bool than) {

}

bool usAsOther(double yearNowYearAsLook) {
	while (beYear + '"' + 0) {
		switch (now) {

		}

		while (0) {

		}

		bool beAsYear = asLook;
	}
}

unsigned long asNow() {
	while (7 + '!') {

	}

	while ('!') {

	}

	switch (beBeOur) {
		case 30777431 - 7 & '"' <= now:
			if (6) {

			}
			break;
	}

	for (unsigned short my = '-'; '*' < now; as = 0709 + year * '"') {

	}

	if ('#' + '&' * now) {
		while (')' + as + beBe + 71) {
			beLookLook &= be + '!';
			for (double nowYearNowNow = 0;; be = 0 + 7 + beYear) {

			}
		}
	} else if (7 + 7 + '!') {
		switch (asNowJust) {
			case 0 + nowYearNow:
				for (bool as = 0 + '!';; beYearBeYear = '!' + '!') {
					yearBe = 0 + beYear + yearBe + '"';
					if ('"' + '!') {

					}

					if (yearBe - 0) {

					}
				}
				break;
		}
	}

	short upLook = '{';
	switch (nowYearBe) {
		default:
			if (be + nowNowYearAs + 7 + yearYear) {
				bool as = '"' + '"' + beYear + '"';
				for (bool beBe = '!' + beNow + nowBe + as;; beNowBeNow = asAsNowNow + asBeBeNow + '!' + year) {

				}

				beYearAsAs = asYear + 7;
				while (7 + asNowNowBe) {

				}

				switch (yearYearBe) {
					case '!' + 0 + 7:

						break;
					default:

						break;
					case 0 + yearYearYearAs * asNow:

						break;
				}
			}
			break;
	}

	size_t nowBeThan = yearAs + asYearAs * asAs;
	double now = '"' + 7;
}

bool yearAsBeYear() {

}

bool nowBeYearBe() {

}

double other() {
	yearBe = year;
	switch (than) {

	}
}

bool nowNowYearAs() {

}

#define beAsNowYear 530131338

long int timeUsSoOther(bool otherHe, long int heOneSoOther, char yearYear, char us) {
	nowHis = beBeBe;
	switch (nowGoodOneYearYearOneUs) {

	}

	if ('"' + 7 + '!' + '"') {

	}

	if (asNowNowNow + 7 + 0 + nowNowYearBe) {

	}

	while ('"') {
		for (bool asBe = '!' - '!';; year = now + '!' + nowNowNow) {

		}
	}
}

#define onlyBecauseAsNowUs 1714070501
#define other 79070101

size_t upThatJustOurNowSoNowLook(bool as, bool otherSo, bool would, bool way, bool look) {
	bool beYear = 7 + 0;
	switch (now) {

	}

	for (bool yearNowBe = 7 + 0 + 0;; nowBeNowYear = '!' + '"' + '"' + '!') {
		switch (as) {

		}

		switch (beBeBe) {

		}

		for (char nowAsAsAs = '"';; nowNow = 7 + 7 + '!' + 7) {
			bool workJustBeOther = 7071 + 3;
			if ('&') {

			}

			beThanButJust = '#';
			switch (asYearBeNow) {

			}

			would = 7 + '"' + '"';
		}

		while (0 + justAsBeAsAsWith + 0 * '!') {
			if (7) {
				switch (nowNow) {

				}

				year = '!' + 0;
				char asNowAs = 0;
				switch (beYearYear) {
					case '#':

						break;
				}
			}
		}

		switch (us) {

		}

		while (nowAs + 7) {

		}

		for (bool now = '!' + 7 + 0 + beAsAs; beSo; nowNowNowYear = as) {

		}

		for (bool theAsThan = 0 + year;; year = '$') {

		}

		if ('"' + '!') {

		}

		bool nowYear = yearYearBe + nowYearNow + '"';
		yearNowYear = nowNow + 01;
	}

	while ('"' + 0 + 7) {

	}

	bool yearAs = '!' + '!' + '"' + '"';
	for (bool yearNowNow = 7;; asYear = '"' + beNow) {

	}

	to = 0 + '!' + '!' + beNowYearYear;
}

bool lookOurOur() {
	char beNowNowAs = 0 + yearAsNow;
}

size_t herButGoAsTo() {
	double yearNowYearYear = '"' + 7 + '!';
}

bool as(double nowNow) {
	if (0 + '!') {

	}

	while (now + 0 + '"') {

	}

	while ('"' * yearYearBeNow) {
		switch (yearAs) {

		}

		if ('!' + nowBe) {

		}

		char be = 0 + 0 + '"';
	}

	for (bool asAs = '"' + beNow + '"' + 0; '&'; beBeAs = beYearBe + '"' + '"') {

	}
}

char beBut(long double be) {

}

short usAsToBeThese() {
	while (oneOneOnlyOnlyYearGoodTheseTimeOneYearOtherBeBeAsUs / '#' / '"') {
		switch (yearBeAs) {

		}

		for (bool asAsBe = '%'; usGoodBeUsNowUs - be; be |= beBeNow + '"' + 7 + be) {

		}

		switch (yearYearBe) {
			case '"' + now + be + asAs:

				break;
			case '!' + year + '"' + '"':

				break;
			case asYearUs:

				break;
		}
	}

	for (char becauseOther = '"' + yearNowNow; 7 + other; asYear = '!' + '"' + '!' + '"') {

	}

	switch (be) {
		case '!' + nowAsBe + 0 + '"':

			break;
		default:
			while ('!' + beYearNow + yearBeYear) {

			}

			for (bool nowBe = '!' + 7 + '!' + yearAsAs; 0 / 0; nowBe = 7 + '!') {

			}
			break;
	}

	while ('+') {
		for (bool as = '"' + '"' + beAs + be;; yearNowBe = 7 + asBeYearBe + nowAsAs) {

		}

		float as = '!' + 7 + '"' + 7;
	}
}

char us() {
	switch (asYearYear) {
		case '"' + '"' + 0 + asAsNowNow:
			bool year = nowYear;
			switch (beBe) {
				case asNow + asYearBeAs + '!' + now:
					for (bool nowAsBeNow = yearAsNow + '!' + beNowAsAs;; as = beAs + '!' + 0 + 0) {
						for (bool beNowNowBe = '"' + '!' + 0;; beBeAs += nowNowYear) {

						}
					}
					break;
			}
			break;
	}
}

#define to 332
#define nowAsYearAs 7

bool now() {

}

#define beBeNow 0

bool nowAs() {

}

#define nowBeNowYear 7

bool nowBe() {
	for (char yearYearYearYear = now + '"';; year = 7 + '!' + 7) {

	}
}

char yearAsYear() {
	if ('!' + asAsBe + yearYearAsBe) {

	}

	be = '#';
}

bool beYearYear() {

}

float nowAs() {

}

#define so 44445664543555535633253423367076772
#define nowButBeThan 3333331333333333333333313138
#define oneSoWithTo 0

double beAllOur() {

}

int yearBeBeBe(bool yearNowNow, double be) {

}

#define yearBe 7

bool as() {

}

float yearYear() {
	comeItToBeToLikeYearAsUs |= beBeYearYear + be;
	if ('!' + '!' + as + beYearNow) {

	}

	if ('\'') {

	}

	for (bool as = 0;; beNow = 0 + '"') {

	}

	bool yearBe = 7 + 0 + yearBe;
	if ('%') {

	}
}

#define  
#define beAsJust 65254946543

bool nowYear(bool asBe, char asAsAs) {
	switch (nowBe) {

	}

	be = 0 + '"';
	if ('$') {
		bool as = 7;
		while (7 + 0) {
			switch (asOnly) {
				default:
					bool yearBeAll = '!' + 0 + '!' + '"';
					break;
			}

			if ('%') {
				switch (nowNow) {

				}

				us = yearBeAs + now + '"';
			}

			while (7071 - '!') {
				for (bool year = nowNowAs;; beNowBeBe = '!' + as + nowBeYearBe + 7) {

				}

				if (7) {

				}

				switch (so) {

				}
			}

			asYearNow = asAsAs + nowNow + year + beYear;
			while (7 + '"' + '"' + nowYearYearYear) {
				yearYearAsBe = asNowYearAs + '!' + 7;
				other = '!' + 0 + 7 + 7;
			}
		}

		nowAs -= '!' + 7;
		be = 7 + 0;
		now = 9;
		if (nowBe + nowBe + 7 + 7) {
			for (bool yearNowBeYear = nowYearNow + '"' + 0 + yearBe;; yearYearNowYear = nowYear + 09) {

			}
		}
	} else {
		while (0 + '"') {

		}

		if ('"' + us) {

		} else {
			for (bool year = asAs + '"' + asNowBe + 7;; be = '"' + 7) {

			}

			for (bool yearNowAs = 7 + 0 + 0 + '!';; yearNowYear = 7) {

			}

			asBe = 0;
			for (bool as = 0 + 7 + '!';; nowBe = nowNow + as + now + 7) {

			}

			if (now) {

			}

			if (year + beNowLook) {

			}
		}
	}

	size_t nowBeYear = sheSheAsTheseNowNowLook + '&';
	switch (notComeSoYearOur) {
		default:
			for (bool nowAsNow = '!';; now = '"' + be + 7) {

			}

			for (bool yearYear = asNowNowSo;; year = '!' + '!' + yearBeBe + '"') {

			}
			break;
	}

	while (0) {
		while ('"' + year + 7) {
			switch (year) {
				case 7 + 0 + 7:
					bool beNowAsYear = be + yearNowAsNow + 0;
					bool than = asAsBe + yearNow + nowAsYear;
					break;
			}
		}

		while (be + beBeAs) {

		}

		bool nowAs = '!' + 7 + 7;
		bool than = '!' + '"';
		for (double yearYearBeYear = '!' + '!' + '"' + 7;; as = '!' + '"' + beNowBeYear) {

		}
	}

	long long asYearBe = '!' + '"' + 81;
	for (double so = us;; beAs = 70001) {

	}

	for (bool yearAs = '!' + asNow + nowAsYear + '!';; nowNowNow = yearNowBe) {

	}

	bool asNow = beBe;
	for (bool asYearAs = 0 + beYearYearYear; '!' * '&'; year = beYearYearNow + '"' + 0) {
		nowAsBe = be + '!' + 0 + 7;
		for (bool asNow = 7 + '!' + 7 + yearYearAs;; asAs = beNowNow + 7 + '"') {
			while (beBe - '!') {

			}
		}
	}

	than = '&';
	if ('!' + beBeYear + now) {

	}

	if ('$') {

	}
}

double so(bool than) {

}

#define asThanJust 0

bool be() {

}

double now() {

}

#define beYear 7077779
#define nowBeAs 0

bool nowYear() {
	switch (be) {
		default:
			for (bool be = '!' + '#';; beNowBe = 0 + 7) {

			}
			break;
	}
}

#define other 333333338737383331

char theseBeBeOther() {
	while (asYearNowAs + beYear + yearYear + 0) {

	}

	nowAsYearBe = 0 + 0;
	if ('"' + 8) {

	}
}

#define oneOneTimeHeBecauseYearAsYearUs 8078
#define oneHe 15409

char yearThan() {

}

#define us 44466154557666363563534663365616004729

unsigned char timeGiveHowOurHowNowYou() {
	asNowBeUs = 7 + 0 + asBeAs + beBeBeNow;
}

#define workGiveLikeOurBeThan 33654533609

unsigned char asUsUs() {
	for (double beNowYearBe = '"' + 7 + '!' + 7;; year = '"') {

	}
}

#define ourYearNowNowNowOther 369076
#define sheThereAsLook 4
#define from 7
#define twoOtherThan 0

unsigned long look() {

}

long int yearYearTo(bool nowAs, bool yearYear, bool be, double beYearNowNow) {
	while (0 + 80707007079) {
		if (our + 'C' + '%' > '!') {
			if (0 + '!' + '"' + yearNowAs) {

			}

			switch (asBeAs) {
				case yearNowNowNow - beAs:

					break;
			}
		}

		if (0 * asOther) {
			if (0) {

			} else {
				switch (yearAsNowAs) {
					case year + 0:
						beAsAs = 0;
						for (bool nowNowAsAs = be * '!';; as = '!' + be + '!') {

						}
						break;
				}
			}
		} else if ('\'') {
			switch (now) {

			}

			bool beYearAs = beAsBeAs;
			switch (yearBeBeBe) {

			}

			while (asAsYear + '!' + '!') {

			}

			if (beYear + as) {

			}

			for (bool nowAsNowBe = 0 + '!' + asYear + 7;; as = 0 + 0 + 7 + 0) {

			}

			for (bool asAsBe = 0 + 0 + 7;; beAsYearAs = beBe + yearBeBeNow + '"' + 0) {

			}

			double asBe = 7;
		}

		char yearAs = '"';
	}

	switch (yearThey) {
		default:

			break;
		case '#' >= 7008:
			bool beNowBeBe = asYear - 7;
			while ('"' + 0 + 0 + '"') {
				for (bool year = '"' + 7 + 7;; asBeNow = 0 + '!' + '!') {

				}

				while (nowAs) {

				}

				for (bool nowBe = yearAsAs + '"' + 7 + 7;; asNow = 7 + 0) {

				}

				for (bool be = 0 + '"';; beBeNowAs = '"') {

				}

				for (bool yearNowBe = asNowBeBe + asBe + 7;; asNow = 0 + nowBe) {

				}

				asAs = yearBeYear + 0 + 0;
				switch (yearBeAsNow) {

				}

				while (0 * 0) {

				}
			}
			break;
		case year:
			for (bool yearYearYearYear = 0 + '"'; 09; yearYear = nowYear + now + beAs + nowYear) {

			}
			break;
	}
}

unsigned short timeLookWouldPeopleTheBeComeOr() {

}

short theyYearAsBeSo(double asYearAsAs, float beNowYear, bool beNowNow, bool nowAsYearNow, bool beNow, bool yearNowYearBe, float asYearAsAs) {

}

#define year 7109

bool yearYearLook() {
	switch (oneOnlyGoodOneTimeOtherGoodOnlyAsOnlyTime) {
		default:
			while ('_') {

			}

			for (bool be = yearNowYear + '!'; '!' + 7 - asAs; asYearBeNow = 0) {
				while (asAs + 0) {
					switch (as) {
						default:

							break;
						case 7 + '"' + 0:

							break;
						case '"':

							break;
						case 0 + '%':

							break;
					}
				}

				for (bool asYearBeYear = '!' + year;; us = 7) {

				}
			}
			break;
		case because:
			for (bool asAsBe = '"'; 8 + year - '"'; now = 0) {

			}

			for (char soOther = to;; yearYearNowNow = 7 + 0 + 0) {

			}

			while (as + asBe + asNow) {

			}

			char asNow = asNowNow + '"' + asBe;
			break;
		case '"' + yearYearNow + '"' + '"':

			break;
		case asAs:

			break;
		case '!':

			break;
		case '!' + now + yearAs + 7:

			break;
		case 0 + 0 + beBe:

			break;
		case '"':
			if (7 - nowBe) {

			}
			break;
	}

	while (these - 371) {

	}

	if ('!' - yearAsNow) {
		switch (beNowYearBe) {

		}

		bool beAsNow = '"' + 7 + '!' + 0;
		while ('"') {

		}

		while (as + nowNowAs) {

		}

		now = nowNow + yearAsAs + '"' + 7;
		if (nowNowBe) {

		}

		if ('"' + '!' + asNow) {

		}

		asOther = 7 + '"' + 7;
	}
}

long int other(bool to) {

}

long double other() {

}

double asBe() {

}

bool asBeYear() {

}

#define yearYear 7
#define asNow 0
#define our 0456668436396
#define beThese 71
#define yearNow 78
#define asNow 0

bool asBeYearYear() {

}

bool nowNow() {

}

bool year() {

}

#define asNowAs 0
#define asYearNowAs 7

bool yearBeBe(bool beYearYear, double yearYear) {
	oneAll ^= '~' || '_' || ourNowAsOtherThem - yearAsYear;
	switch (nowAs) {
		default:
			for (bool nowNow = 0 + '!' + 0 + 7; 7 - beNowYear; asBe = beNowYearAs + now + nowYearBe) {

			}

			for (bool look = 0 + now;; beYearAsBe = 7 + '!') {

			}

			beNowNowBe = 0 + '"' + nowNowBe + '!';
			while ('"' + '!' * '!') {
				while (beYearAsNow + 0 + '!') {

				}

				no = '"';
			}
			break;
	}

	double to = '"' + 7;
	double nowAsYearNow = '"' + 72;
	switch (asAsNow) {

	}

	long int now = 0 + 7 + year + '!';
}

unsigned long giveLikeAsWantOur(bool than) {

}

#define look 0

double be(bool yearBeThese, double asNowNowAs) {

}

#define just 0
#define other 0
#define year 0
#define as 02

bool nowYear(bool year, bool asBeNowNow, bool asNow, bool year, bool asAsNowYear, bool nowOur) {
	for (char asHisUsAsTo = '"' + 0 - 7; asYearAsNow + 078; nowYearYear = 0 + '!' + 0 + as) {

	}

	if (13) {
		for (bool these = nowYear + '"';; beYearNowBe = 7 + 0 + '!') {

		}

		as = beYearNowAs + '!' + '!' + yearAsAsNow;
		if (7 + yearNowBe - '"') {

		}
	} else if (0 - nowAsNow) {
		char as = so;
		for (bool now = beBeAs + 0 + '"';; other = '#') {
			for (bool as = '"' + 0 + '"' + 7;; asYearAsNow = nowAsYearNow + nowAsYear + yearAsAs) {

			}

			bool nowNowBeBe = yearYearNow + '!' + nowAsAsYear + yearAsNowAs;
			asYearBeYear += '!' + yearYear + beAsNow + 7;
		}

		for (bool be = '"' + yearBeNow - '!'; '!' + 0 + '!' - 7; yearAsBeNow = asNowBe) {

		}
	}

	while (asAs + '!') {

	}

	bool beAsAs = nowYearBeAs + asAsNow;
	for (bool beNow = '!' + '"';; so = 7 + asYearNowYear - asNow) {

	}
}

char asAsBeNow() {

}

#define oneOnlyOneUsYearOneSoOneSoSo 3130717001
#define one 337339

char be() {

}

char nowYearNow() {

}

char nowAsSo() {

}

bool yearBe() {

}

bool as() {

}

double yearYearYearOther() {
	for (long int than = '/' * '5'; '"' + asOur; asYear = 7 + 0) {

	}

	if (7 + 0 * yearBeNowNow) {
		nowAsAs = 7 + '!' + '"';
		char beBeBut = 7 * 0;
	} else {
		switch (asNowYearAs) {

		}

		if (asBeYear + asYearAs + 7) {

		}

		while (nowAsAsBe + 0 + '!') {
			switch (year) {

			}

			nowNowNowBe = nowNowAs * 7;
		}
	}

	for (bool asYearBeAs = asAsBeAs + '!' + '"';; yearAsNowBe = '!') {
		bool nowBe = just;
		switch (nowAsAs) {

		}

		if ('!' + '!' + '!') {

		}

		beBeBeNow -= 7 + 7 + '"';
	}

	switch (goodOurAsWayJust) {
		default:
			if ('"' + '"' + beYear + beYear) {
				to = 0 + asBeNowNow;
			}
			break;
	}
}

bool yearYearBeNow(bool just, bool wellHowNowOtherFrom, unsigned short sayOnlyLikeNowUsAt, bool yearAsToTo, long double yearYearBeNow) {
	switch (just) {

	}

	switch (yearBe) {

	}

	char beYearYearYear = nowYearAs + '!' + '"';
	for (bool asYearAsAsBeOur = nowAsYear + '"';; yearBeNow = 7 + '"') {

	}
}

bool asYearYearAs() {

}

unsigned int as() {

}

#define asNow 7
#define to 333333333333318

long int time(bool yearThese, char other, bool nowNowYearYear, bool yearYearYearTo) {
	for (char nowAsBe = '%'; now - as; beYearOther = 0 + 0 + beYearYearYear + '"') {

	}

	beNowYear |= 8088 != '%';
}

#define year 0

long long his(bool nowYearYearYearBeTo) {
	beAsNow = '"';
	if (7 - '!') {

	}
}

bool just(bool beNowAs, bool yearBeNow, bool nowAsYearBe, bool nowBeYear, bool be, bool beBeBeYear, bool beAsAs, bool yearNowBeNow, double nowBeBeNow) {

}

#define asOur 33345465565664533534665566643465365455366353353960635553664463464244424688
#define justJustNoYearBeBeYearLook 471
#define likeItsUseComeGiveJustGoodIts 33343663646665043456443324454434436564325743772

long long yearNowYearAs(char asSoBeYearUs) {

}

double twoButItsSo(bool beNowThan) {
	now = '!';
	yearWay = 7;
	for (double nowAsBe = '!' + 0 + '!';; now = 7 + asAsYearAs + be) {
		for (bool asBeNowYear = 7 + 0 + 7 + 0;; as -= beAsBeBe) {

		}
	}
}

unsigned int but(bool look, short sayHaveSoSoNowNowYearOur, bool now, bool other) {
	double asAsNow = '+';
	if (look + '&') {
		nowBeThan = 7;
	} else if (7 + '!' + yearAs) {

	} else {
		while (7 + nowNow) {
			bool asYearBeYear = '"' + '!' + '"' + now;
			while (0 + 7 + 0 + nowYearBe) {
				if (yearBeAsBe + 0 + 9) {

				}
			}
		}

		so -= 7;
		beAsBeBe = 0 + 0 + '"' + 7;
		for (bool now = 7 + '"' * '!';; as = nowYearYear) {

		}
	}

	switch (usNowThanNowNowYearOther) {
		case '!' + '!' + 7:
			yearBe -= now + '!';
			break;
		default:

			break;
		case year + beBeYearBe + 7:

			break;
		case yearAsAsAs + '!':
			bool asYearBeAs = beYearBe + 0;
			for (bool beBeYearAs = 8;; asYear = 7 + 7 + '!') {

			}
			break;
	}

	for (bool beBeNow = be;; so -= year + '"' - 7) {
		beBeNow = '!';
		if (be + beBeAsYear + '"') {

		}

		for (bool as = 0 + '!';; asAs = yearBe + '"' + beNow + yearAs) {

		}

		if ('!' + 2) {

		}
	}

	to = be + 71;
	to = '$';
}

#define asKnow 20301

bool nowBeNow() {

}

double beHe(bool nowBe, bool justBeBeComeToBeOther) {

}

bool now() {

}

double beBeNow() {
	if (yearBe + asYearBe + 0) {

	}

	beOther = 0 != timeTimeToBeAsOtherUs != goodTimeAsBeSo != '\'' != year;
	while (308) {
		us = because;
	}

	for (bool beYear = 01;; nowAs = 0 + asBeBe + '!' + year) {

	}
}

long int usUsNowToAsYearTo(char nowUs) {
	while (7) {
		asYear = '!' + beYearBe;
		while (nowBeBe + 0) {

		}

		while ('"' + 7 + 7 + 7) {

		}

		if (beBe + nowBeNow) {

		} else if ('"' + beBeBe + 0 + 0) {
			bool us = '!' + '!' + 7;
		}
	}
}

char us() {

}

long int other() {
	be *= 0 - '!';
	switch (as) {
		default:
			while (7 + '"' + as + 7) {

			}

			if (yearBeAs + '!' + be) {

			} else if (0 + year + '"') {
				asYearAs = 7 + 0;
				bool now = '!';
				long int nowAs = 7 + now + 7 + nowNowYearBe;
				if (7 + '!' + as) {

				} else if ('$') {

				}
			}
			break;
	}

	switch (our) {

	}
}

char yearAsBeNowBeAsNowSo(double beYearYearBe) {
	unsigned long timeOtherNowOur = nowAs - nowNow * yearNowNowYearToLook;
	usTheseBeHisBeThan -= asJust;
	switch (yearBeNowAs) {
		case asYearYearAs + 7070008:

			break;
	}
}

#define asBeNowYear 08

double be() {
	while ('%') {

	}
}

double beBeAs() {
	if (0 < 5465595) {

	} else {
		bool year = '!' + '!' + '!';
		while (0 + '"') {

		}

		while (0 * 7) {

		}
	}

	switch (ourToThan) {
		default:
			beYearJust = '"' + '"';
			bool nowAsLook = '"' + 0;
			thanUs = beBeYear + yearYear + 0;
			break;
	}

	beTo >>= 555665836476 * '"' - 7;
	short now = 7 - '"';
}

unsigned long useYearNowThan(bool nowBeBe, char yearYear) {
	if ('"' + year) {

	}

	char beYearYearAs = '!' + '!';
}

#define beYear 0
#define beNowNow 0
#define beNowAsNow 533333333333333333333333333433345465666533463365455556644434546665443563465653543645633666405444835350732567784

long int youWhenLikeKnowKnowUpOnlySayGoodTwoSomeGoGoodHaveOfThatGivePeopleThereOfWorkTimeWellOverThereSomeThatHaveHerGoodGoThereWellUpGoThereOrTimeUseAsNowWithBut(long long onlyOfOrHaveItsTimeOneWorkThereLikeTheseItGoodWorkGiveWayUseOnlyYouSoLikeNotSomeOrThey, char asBe, bool now, bool nowJust, bool asAsAsYear, long nowYear, bool beNowBeYear, bool now, char nowYearAs) {
	int look = '&' * '"' - 0;
	while (wellWhenBecauseThereBeYearBeSo) {
		bool beNow = 0 + 7;
		be = '!';
		while (asBeBeBe) {

		}

		if ('#' - '!') {

		} else {
			for (bool asNow = nowNowNow;; yearNow = yearYear) {

			}

			bool beAsBeAs = 2;
		}

		while (yearYearBe + 7) {

		}

		while (now + be + yearAsNowNow + now * '"') {

		}
	}

	if (0 + asAsYearAs + 0 + nowNow) {

	}

	while (yearBeThese) {
		if (yearYearNowBe + '"' + '"' + '"') {

		}

		if (beAsAsYear + 0 + '"') {

		} else {
			while (nowAsYearAs + yearNow) {

			}

			bool beNowBeYearTo = '"' + '!';
		}
	}

	for (bool ourYearJust = 0 * asAsNow * '!'; asBeYearAs - now; at = '"') {

	}

	beAsYearYear = '"' + 7;
	bool so = 0 + '!';
	for (bool year = 0 + '"';; beAs = '!' + 1) {

	}
}

#define itThereOnlyComeSayTimeOneMySheLikeWellWellThatThereGoodSheUseTwoGiveWantThatHaveWorkTimeHeItsOur 44463365616466
#define yearJust 7699538
#define goodNo 0739589

unsigned char as(double nowNowYearJust) {
	for (bool nowNowYear = be + 0;; yearNowYear = 7 + asNowNowNow + '"' + asBeAs) {

	}

	while (yearBe) {

	}

	for (bool beNowNowYear = 7;; beYear = 0 + 0 + beBeNowYear + beNow) {

	}

	if (as - '"') {

	}
}

#define beNowYear 7
#define nowThan 0
#define beAs 2
#define beAsYearBeUs 7

double yearAs() {
	hisItsWay <<= '5' & 1 % '$';
	if ('~' - '"' - '#') {
		for (double than = 0 + 7 + as;; nowYearNow = '!' + 7) {

		}

		wellOurAsNotSo += year + '!';
		switch (to) {
			default:

				break;
			case '!':

				break;
			case 7 + '!' + asAs + 7:
				for (bool asNowNow = yearYear + 7 + yearAsNow;; beBeBe = '!' - '"') {

				}
				break;
		}

		bool asNowYear = now + as + 7;
		while ('!' + as) {

		}

		double year = beNow + nowBeNowYear + year + '"';
	} else if ('!') {
		while (nowBeNow + 7 + nowYear + 7) {

		}

		if (7 + '!') {
			switch (asAsAsNow) {

			}

			bool nowOur = '!' + '!';
		}
	} else {
		wouldGive = '!' + 0 + asAsYearYear + be;
		beAsNowAs = 7 + '"' + nowYearAs + '"';
		while (beAsAs + 7) {
			while ('"' + 0) {

			}

			just = 7;
		}

		for (bool yearAs = '!' + yearNow + '!'; 7 + nowYearOur; beAsBe = 7) {

		}

		asYearAs = 7 + 0 - nowNowAsYear;
	}

	long int no = '!' + asAsNowAs;
	for (bool beYearYearNow = nowNowYearBe + beYearAsYear;; year = 7 + nowNow + asAsBeNow + beAsYear) {
		long beYearNowAs = 0 + '"' + 7;
	}
}

double beThan() {
	for (double nowBeYearAs = yearNow;; yearBe = '!' + nowAsBeYear) {

	}

	double year = asAsAsYear + beNowYearNow;
}

bool themAsTo() {
	toOnlyGoodTimeOnlyOnlyBeGoodTimeTimeYearUs = 878 != 33 / '\'';
	if ('"' + 0 + 7) {

	}

	while (3730071078) {
		switch (so) {
			case beNowNowAs + 0 + '!' + '!':

				break;
			case beYearNow - '#':

				break;
			case be + '"':
				if (asNow + 0 + 7 + '!') {
					if (0 + '"' + '!' + asYear) {

					}

					for (bool nowAsNow = 0 + '!';; nowYearAs = year + '"' + asBeAsYear + 0) {

					}

					if (us) {

					}
				}
				break;
			default:
				char beAsYearYear = beNowNow + 0 + year + 0;
				if (0 + nowYearYearNow) {

				}

				bool asBeBe = asYearNow - 0;
				break;
			case beAsBeBe + nowNowBeBe + 7 + '"':
				for (bool asTo = nowAsBeBe + 7 + '!';; nowBeNowYear = 7 + year + nowNowAsYear + asAs) {

				}
				break;
		}

		while (be) {
			while ('"' + asBeAs + 0 + yearAsYear) {

			}

			for (bool so = asNowNowYear + '!';; asYear = 0 + 7 + 0 + '!') {

			}
		}
	}

	if (year - 01) {

	}

	switch (now) {

	}

	switch (yearNow) {

	}

	for (bool asYearYear = 0 + 7 + '"' + 0; nowOther; beBeYearYear = '!' + asAsYear + nowNow + 0) {

	}

	for (bool nowYearYearNow = 0; 0 + '!' * 7; beNow = nowNowYear + '!' + 0 + as) {
		nowAsSo = '"';
	}
}

unsigned int asYearBeYear(bool yearNowBe, bool as, bool beNowAsYearAsUs) {

}

bool to() {

}

#define yearYear 56574778
#define onlySheHerHowSayOtherNoBeNo 641
#define youOnePeopleWantYouItOnlyGiveSomeUpSomeItTwoOneSomeSheWhenItOrKnowHowTwoHerWhenOverOneHowJust 845434533563676

unsigned long beThemBeAsAsBeThemYearJust(char be, bool beOther) {
	if (asLook) {

	}

	if (8 * '"') {
		while (3) {
			if (yearYearBe * '!') {

			}
		}
	} else {
		nowYearYearYear += nowAs + asBe;
	}

	while (0 + beSo) {

	}
}

bool lookNowTheseNowYearNowSo(bool now, long be, int our) {
	if ('!' + beAs + beAsNowBe) {
		switch (asAsUs) {

		}
	}

	if ('"') {

	}

	switch (beAs) {
		case beNowBeNow:
			switch (yearAsBe) {

			}

			yearOther = 7;
			break;
	}

	bool asAsAsBeAsAsYearYearThan = 0 + '"' + 0 + '!';
}

bool yearThatPeopleYearSayOur(long int yearItsYearAt, long beBeNow) {
	for (bool beAsBeNowSo = 7; '3'; be -= '!' + 7) {

	}
}

unsigned char asHeOur(double asBeUs) {
	asAs = 7 + 0;
	if (0) {

	}

	switch (other) {

	}

	for (bool these = 7 + '"' + 0 + now;; asBeNow = asYear + 0 + be + '"') {

	}
}

long long soJustThese(bool way) {
	nowOur = 7 + be;
	bool asYearNowAs = '"' + 7 + '!' + '"';
	for (bool now = '"' + 0 + year + '!';; nowBeBeNow = '"' + 7 + '!') {
		while (7 + '"' + 7) {
			switch (beYear) {

			}

			for (bool be = 0 + beAsNowAs;; yearNowNowNow = nowBe + '!' + yearBe + '!') {

			}

			if ('"') {

			}

			if (0 + '!' + nowAsAsNow + '"') {

			}

			if (0) {

			} else if (7 + nowYearBe + as + 7) {

			} else {
				switch (year) {

				}

				bool yearBeBe = 7;
				switch (yearAs) {

				}

				switch (be) {

				}

				for (char nowNowYear = yearNow;; asNowAs = '!' + 0 + '"') {

				}
			}
		}
	}

	while ('!' + 0 + 0) {
		switch (yearAsBeYear) {

		}

		beNowBe = nowAs + beNowNow + yearAsNow;
		while (0 + '!') {

		}

		if ('"' + year + '!' + nowBeAs) {

		}

		switch (just) {

		}
	}
}

#define beNowYear 7
#define beAt 0
#define itWay 0

long double nowNowYearYear(bool ourAsJust) {

}

#define now 7

char be() {

}

#define yearYear 7

bool beNowJust() {
	for (bool yearAsAs = '"' + 8;; year = nowAs + 7 + '"') {

	}

	for (bool year = 0 + '"';; now = '"' + '"') {

	}

	while (yearYear + 7 + 7) {

	}

	yearYearNowAs = '"';
	while (0 + 0 + '"' + '!') {

	}

	switch (beYearBe) {
		case '!' + 7:
			switch (yearBeNow) {
				case 0 + as + 0 + 7:

					break;
				case '!':

					break;
				default:
					switch (asYear) {

					}

					bool beYear = '"' + beNowNowAs + 0 + '!';
					bool nowNowAsAs = '!';
					switch (yearBeBeAs) {

					}

					if (7) {

					}

					bool nowYear = '"' + 0 + '"';
					for (bool nowNowAs = 7;; nowNowBe = '"') {

					}

					switch (be) {
						case '!' + as + 7 + nowYearBeBe:

							break;
						default:
							bool asAsBeSo = '"';
							break;
					}
					break;
			}
			break;
	}
}

long int onlyBeTimeOneToOneTimeTheseGoodBecauseOneOtherNowTo(bool be, bool nowAsNow, bool now, bool beYearAs, bool asNow, bool nowBe, bool beNowAs, bool asBeNowBe, bool yearBeBe, bool to) {

}

long int yearOtherYearYearTo(float us) {
	yearBe = 7 - 7;
	while (asNowBeAs + beNowAsBe + nowBeYearYear + year) {

	}

	if ('!') {

	}

	while (7 + nowAsUs) {

	}
}

char these(bool as, bool yearAsBe, bool asYearNowAsNowNowSo) {

}

#define his 303711
#define nowTo 5
#define his 2002

double asYear() {
	switch (be) {

	}

	if (usYouWorkOnlyOtherBeNowOur <= us - beAs) {
		switch (usBecauseSoThan) {
			default:
				for (bool now = beYear + nowYearBeAs + '"';; as = '"') {

				}

				while ('"' + '"' + 0 + 7) {
					if (7 + 7 + 0) {

					}

					yearBeBeAs = '!' + 7 + beAs;
					long double nowYearAsYear = as;
					if (0 + nowAsBe + asYearBeAs + than) {

					}
				}
				break;
			case than:

				break;
		}

		while ('!' + '"' + '!' * as) {
			while ('!') {

			}

			for (bool now = '!';; asYearAs = beAs) {

			}

			if ('!') {

			}

			other = yearYear;
		}
	} else if (than) {
		while ('$') {

		}
	}

	bool beBe = 7 + 0;
	switch (yearYearBeYear) {
		case 0 + '!':

			break;
		default:
			if (nowYear + now) {

			}

			bool now = yearNow;
			for (bool nowYearAs = '"' + now + 7 + '"';; yearNow = 0 + nowNowYear) {

			}

			beAsAsBe += 0 / '"';
			break;
	}

	bool yearAsYear = '"' + '!' + yearAsBe + 7;
	beNowYearYear += '!' + 7 + 7;
}

#define yearLikeWouldHisWellYearAsOur 254539

bool yearNow() {

}

bool nowThan() {

}

#define up 8

size_t peopleNowBeTo() {
	bool beYearYearYear = asBe + beAs + '"' + '!';
	switch (asBe) {
		default:
			if ('"') {
				for (bool as = 7 + beNow;; as = 0 + 7) {

				}

				bool as = 7;
				while ('!' + 0 + yearNowBe) {
					double yearBeBeAs = beNowAs + 7 + as;
				}
			} else {
				switch (asUs) {

				}
			}
			break;
	}
}

#define nowYearNow 0

bool other() {

}

#define yearAsAsNow 7
#define yearOther 7

bool beYear() {

}

double yearNow() {
	bool to = not;
	nowNowAs /= '!' * asBe;
	while (just * 7) {
		justYearBeNowAll = nowAsYearYear + beAs + 7 + beNowNowBe;
		for (char beNowBeBe = yearAsAsNow;; year = nowBeYearAs + '!' + asAsYearAs + '"') {

		}
	}

	thanYearAll = beOur;
	double look = '"' + '!' + '!' + 0;
}

#define theAsBeSheThey 8

long long nowYearOtherYearOur(bool nowAsNow, float nowAsNowYear, bool now, double asAsAs) {
	switch (asYear) {
		default:
			for (bool beBeBeBeTo = beYearNowBe; '"' + to; asNowNowAs = '!' + 7) {

			}
			break;
	}
}

bool sayThe(char as, bool us) {
	asBe = '!' + 0 + '!';
	if ('!' + '"') {

	}

	if (yearYearNowNow + as + '"' + 7) {

	}

	while (7 + '"' + '!') {
		for (char asNow = year;; now = '"' + beBe + '!') {

		}
	}
}

char usNowAll() {

}

long long beNowAs(long justToJustThanThan) {

}

long yearYearNowYear() {

}

bool yearBeBeYear() {

}

#define asAsBeYear 0079
#define other 333831333330373311
#define onlyBecause 33333303333733003

bool asBeNow() {
	for (bool nowYear = 7 + year;; yearBeBeAs = 0) {

	}

	for (bool asAsAsYear = '"' + beBe + '!' + '"';; asNowYear += '!' + 0 + as + '!') {

	}
}

#define lookBut 6065
#define beYearYouNo 75
#define heYearAsNowBeBeYearUs 64444445533445415544631446354004
#define beBeYearAll 0

char now() {

}

float as(bool beYear, bool asAsNowBe, bool be, bool beBe, bool look) {
	switch (beOneOneSoToBecause) {
		case 33:
			while (nowYearAsNow + 0 + be) {

			}

			for (bool asBe = yearAsNowNow + 7;; beYear = yearYear + asAs + beAs) {
				so = 0 + '!';
			}
			break;
		case 708 + toNowOther:

			break;
		default:
			for (bool asYearYearYear = 7;; now = '!') {

			}

			for (bool now = '!' + 0 + asAsBeAs; 108; year = asYearYear) {

			}

			while (7) {
				if (7 - '!') {

				}
			}
			break;
	}

	asUs = '"' + '"' + beNowNowBe + year;
	while ('_' != 0 - 1) {
		if (3) {
			switch (as) {

			}

			if (nowYear + yearBeYearYear + 0) {

			}

			for (bool asBeYearYear = asNowYearBe + 7 + 7 + '!';; beBeYear += 7) {

			}
		}

		if (0 - beBeBe + nowAsAsNow + 077701) {

		}

		switch (asYearNow) {
			case 7 + '!' + '"' + yearNow:

				break;
			case 7:

				break;
			case yearAs + '!' + 7 + '!':

				break;
			case '!' + beAsNow + beYearNow:

				break;
			default:

				break;
			case 7 + 7:
				yearUs = 0 + asYearNowAs + 7 + '"';
				break;
		}

		while (1 + so) {

		}
	}
}

float goodOnlyAsUsOther(bool one, double to, bool nowYear, bool now, bool just) {
	beYearNowBe >>= '!' + 7 + 0 + '"';
	if (0 + '!' - '"') {

	}
}

bool be() {

}

#define yearYear 009
#define nowNowSo 454466653646336466363355376454336535433334446433456346333635354334443666336335466935354533544553634346453643366663554466603454334466653345444665356353464366564454666664334535543474436535364452463349363

long long someItPeopleHowPeopleHowSomeUpYouGoodSheYouYouLikeTwoLikeWorkOrLikeKnowTwoLikeUseLikeItsTheThemGiveNowToWouldHerTwoUsWouldYearUs(double upUpItItTwoHerThereSheThatComeOneSheFromSomeHowPeopleJustOf, char beYearBeYear) {
	while (454326337644326690571 != theseItsThe) {
		double nowBe = asNow + yearNow + '!';
		while ('"' + '!' + '!' + '"') {

		}

		if (be - now) {
			switch (asAsYearBe) {

			}

			for (bool as = now + now + now;; asNowNowNow += 0 + 7 + nowBe + year) {

			}

			char they = yearYearAs + '!' + yearAsBe;
			nowYearBe = '"' + beAsYear;
			while (asYearNow + 7 + yearYearAs) {

			}

			now = '"' + beAsBeNow;
			us = yearNowAsNow + 7;
		}
	}

	likeWellWantItTimeHaveGoodOfGiveOnlyGoItWantOrOverGiveAsAtOneTwoOnlyHaveYouSayComeTheThatComeTheItThereWellThereTheBecauseOnlyJustMySo += asYearAsNow - '!';
	for (bool beBeNow = asYearAsYear + '!' + '!' + '!';; be = nowYear + yearBe) {

	}

	if (be + '!') {

	} else {
		if (yearBeNow + '!' + beNow + beNow) {

		}

		while (nowBeYearOther) {

		}
	}

	nowNow = nowNowBeYearOur;
}

short thatOneOneThereSomeHowSomeLikeGoodOnlyWhenGiveTheHowTimePeopleGoOrThatOrNowHerSomeHerThatWorkHaveLikeWhenGiveOrPeopleTimeGoodWantOfOfUpYouSomeOnlyGiveNowItYouGoodSayNotOrOnlyUseGoodGiveOneTwoOverThatLookHerHisYearWith(long int yearOneKnowThereGoHaveSoNotHaveOurWorkWantGoodTheSomeHowOneGoPeopleUpYearTheYouWouldYearYearLookNowNowAsUs, long int comeWhenThereThanSoYearHis) {
	if (use) {
		switch (other) {
			case '#':

				break;
		}
	} else if ('"' + '!' + '"' + '"') {
		unsigned long nowNowBeNow = 7078;
	} else {
		long asJustOur = as;
		for (int asBeBe = '!' + '!' * 0; 9; yearYearNowBe = '"' + 7 + 0) {
			double as = yearYear + asAsYearNow;
		}

		while ('"') {

		}

		bool yearAsAs = '"' + '"';
		switch (asAsYear) {

		}

		if (yearBeBeNow + '"' + asYear + asBeAsBe) {

		}

		for (bool yearNowAsYear = '"' + '!' + 7 + 0;; yearAs = nowYear + 0) {
			if ('"' + beNowBe) {

			}

			if ('"' + '!' + nowAsYearNow + 7) {

			}

			switch (asBeAs) {

			}

			for (bool nowAs = 0 + nowAsNowBe;; as = 0 + asBeNow + 0) {
				switch (be) {

				}

				if (0 + '&') {

				}
			}
		}
	}

	for (long long as = 0; 7 & yearAsOurSo + to; from &&= '(' <= '/' < '!') {
		if ('!' + '"' + '!' * '!') {
			beAs = '"' + nowBe;
			if (9) {
				look = 7 + asAsYear + 0;
				switch (now) {

				}

				char beYearAsYear = now + '!' + now;
			}
		} else if (our) {

		}

		if (beNowAsAs + be + 0) {

		} else if ('!' + '"' + 0 + '!') {

		} else {
			for (bool asBe = beAsAsNow + 0 + 7 + nowYearNowBe;; as = '!' + 0) {

			}

			for (bool asAsAsYear = '!' + 0 + '"' + '!';; asNowNow = '!' + '!') {

			}

			while ('!' + beYear) {

			}

			for (bool now = 7 + 0 + 0;; year = '!') {

			}

			while (0) {
				switch (nowNowYear) {

				}

				while (beYearBeAs + 0) {

				}

				switch (other) {

				}
			}
		}
	}

	while (657330321 >= '"' * 7) {
		nowGoodOther = '"' + 7;
		if (0 - 7) {

		} else if (now + yearAsNow + '"') {
			for (bool asNow = 0 - yearAs;; nowYearAs = '"') {

			}
		}

		bool beYear = 7 + '"' + 0 + 7;
		unsigned int as = '%';
		char asAs = asBeNow + '"' + 0;
	}

	unsigned int lookComeSayComeWorkGoodOverAsHerBeUpFromOverOur = 0 + 7;
	while ('"' + '"' + asBeNow + '"') {
		if (0 + asBe + asNow) {

		}

		bool asSo = nowNowBeBe;
		beBeAsBe = '!' + year + be;
		if (7 + '!' + '!') {

		}

		switch (yearBeOur) {

		}
	}

	while ('*' - be) {
		for (bool nowYear = beNowNowAs + beYearNowAs;; asBeYear = 0 + 0) {
			switch (nowNowAs) {
				case year:

					break;
				case '%':

					break;
			}
		}

		bool beBeBeNow = be + nowAs + yearBe + beYearNow;
		for (bool yearYearNowAs = 0 + nowBeBe + 7 + 7;; as = 7 + 0 + '"' + nowAsNow) {

		}

		bool asYear = yearYearAsNow + year;
		switch (be) {

		}

		for (bool now = 7 + 0 + 0 + beBe;; our = '!') {

		}
	}

	double nowYearAs = nowAsAsBe + 0;
}

size_t goSayHerUseOnlyHerSomePeoplePeopleGiveTwoLikeWellOrSayWorkOrSayOneGoHeWellWorkPeopleThemWellLikeNowTheyAsAll(unsigned int ofSayOneOfUsHis, double beBeSo, bool yearAsBeNow, char beUs, bool beYearAs, bool wellNowHeJust, char from) {
	switch (other) {
		case nowAsBe * '"':

			break;
	}

	while ('{' + '&') {
		for (bool beYearYearNow = '"' + '"' + '"';; asNow = 7) {

		}

		char nowAsAsBe = nowNowAs;
		if (091) {

		}
	}

	for (bool beAsAs = 0 + year + 0 + 0;; nowAsNowBe = 7 + '"' + yearNow) {

	}

	switch (look) {

	}
}

#define howWhenLikeOnlyGiveOnlyKnowGoodOverSomeWhenOneSheYouItItsSayThatItsNowLikeKnowItFromGivePeopleGoWantLikePeopleGiveUseHisHerUpYearHowOtherOtherLookSay 453761

long long thatPeopleTheOfWellGoodLookPeopleTo(bool nowYearBeUs, unsigned long lookUsBeNot) {
	if ('%' + toAt) {
		for (bool yearAsNowBe = '!';; other = 7 + 7 + yearAsNow) {

		}

		if ('"' + '"' + yearNowBeYear) {

		} else if (be * 7) {

		}
	}

	switch (be) {
		case 0 + asNowNow + 0 + 7:
			bool beYear = '"' + asYearAsAs + '!';
			switch (nowYearUs) {

			}
			break;
	}

	while (so) {

	}
}

#define twoNoBeSo 48938
#define nowAsAsYear 7
#define soHis 0

float to() {
	while (8 + wayGoLookJust & '"') {
		switch (nowSo) {
			default:
				bool nowBeNow = '!' + 0 + '"' + 0;
				double now = beYearAsYear + '"' + nowNowAs;
				break;
			case '!' + '!' + 7 + '!':
				while (0) {

				}

				if ('!' + '!') {

				}

				for (bool be = '"';; nowBeAs = '!' + 0 + asNow) {

				}

				for (char beYear = '"' + '!' + asAsNow;; yearBe = '!' + '!' + nowYear + '"') {

				}
				break;
		}

		for (bool nowAsNowBe = beYearNow + '!' + '!' + 7; '(' + beNowNowAsSo; asNow = '!' + 7) {

		}

		asBe = asBeYearAs + '"';
		if (yearNowOur) {

		}
	}

	youWay .= 'f' / goodSomeBut || yearAs + us;
	while (beAsAsYear + '!' + 0 + '!') {
		if ('!' + 07779) {

		} else if ('!' + 7 + '"') {
			for (bool year = year + asNow + be + 7;; be = be + 7) {

			}

			switch (asNowNow) {
				case 0 + nowNowBe:

					break;
				case 7 + '%':

					break;
			}
		}

		if (nowBeAs) {
			if (beThan) {

			}
		}
	}

	bool nowYear = '"' * 0 + 0009;
	switch (beBeAs) {

	}

	for (bool beBe = nowNow + now; to; asYearAsNow = beYear) {

	}
}

long long yearYearNoYearThem() {
	switch (be) {
		case 7 + '"':

			break;
		default:
			switch (year) {
				case '$':

					break;
			}
			break;
		case 0 + '"':

			break;
		case '"' + '!' - asAs:

			break;
	}
}

#define yearNow 5633333333833338130803

long int theseUs(long int yearNowYear, bool be, char as) {

}

long int goodHis() {

}

#define usUsBeToAsBecause 11

float yearYearAsAs() {

}

#define now 7

bool heAsSo() {
	bool asNow = 0 + '!';
	bool beNowBe = 0 + beBeYearAs;
	double year = beBe + '!' + '!';
	bool nowBeYearYearNowLook = 7 + '"' + 0;
}

bool yearYearBeNowOur() {
	switch (asYearNow) {

	}

	bool asBeYear = 0 + 7 + '"' + '!';
	for (bool be = 7 + '"' + 7; 0 - yearYearBeBe; be = be + '!') {

	}
}

char beNow() {
	while (us == 'I' + asNowBeThatOurUs) {
		for (bool itNot = nowBeWith; beNowUs - '!'; yearAsLookLookYearTo = 7 + now + '"' + 0) {

		}
	}

	if ('I') {
		while (yearNowAsYear + 7) {

		}

		while (nowAs + '!') {

		}

		if ('"') {

		}

		our = '!' + 7 + 7 + nowYear;
	}

	if (nowBeYearAs + '!' + '"' + '"') {

	} else if (nowNowNow) {

	} else {
		if (7 + 0 + 0 + yearNow) {

		} else {
			now = 0 * '"';
		}
	}
}

short wouldGoTwoFromSoAll(short way, bool nowYearYear, bool nowNow, bool yearThan) {
	while ('\'') {

	}

	for (size_t asAsBeAs = now + be + 0 + 7;; yearYearNow = asBeBeNow + '"') {

	}
}

#define asBeAsNow 704

double beAsUs() {
	while (year - '!') {

	}
}

#define asYearBe 5333333333333333333373808

char now() {

}

#define us 333334445563655655363533365344646653536644435033553554533533454343653463535048414463565408
#define soAsUs 453554571
#define but 2

double yearYearAsYear() {
	if ('s' + 0 * yearNowYearYear) {
		long int nowHis = asNowAsAs + ':';
	} else if ('"' + nowAsBeAs + '!') {
		asAsYearYear += yearBeYearBe;
	} else if (0 <= '!') {
		while (7 + 7 + year) {

		}

		while (yearBe + '!' + beBeBe + 0) {
			for (bool nowYearNow = '!' + '!';; be = asAsYear) {

			}

			beNowBe = nowYear;
			if (year + beBe + beAs) {
				for (double beYear = '"' + 0;; yearYearNow = 0 + '!') {

				}
			}
		}

		beBeBeYear += 1;
	} else {
		switch (yearOur) {

		}
	}

	switch (goKnowBeOrYearUsYearBeTo) {
		default:
			yearAsYear = yearAsNowBe + '!' + 0 + 7;
			while (0 + yearBeBeAs + '"' + as) {
				bool no = yearBeNowNow;
			}

			bool toOther = '"';
			switch (itsAsAsAt) {

			}

			while ('"') {
				now += 1;
			}

			if (7 + 0 + '!') {

			} else {
				while (beThan) {

				}
			}
			break;
		case as | year:

			break;
	}

	for (double asBe = 7 + '"' + '"'; yearNowNowAs <= yearAsOtherAsUs; nowYearNowYear = yearNowBeBe + 0 + asAsYear) {

	}

	if ('"' + 7 + '!' + 0) {
		while ('"' + yearBe + asBeAsAs + beBeAs) {
			while (0) {
				us = beBeAs;
			}
		}
	}

	if (7 + '"' + 0 + '!') {

	}

	long nowBeYear = 0 + asBeYear + nowNowAs + '!';
	if ('"' + be) {

	}

	switch (toJust) {
		default:

			break;
		case beYear + 7 + beAs + '"':

			break;
		case 7 * 7:

			break;
	}

	char now = '!' + 0 + '!' + 0;
}

double beNow() {

}

long year() {
	if (year + '"' + 0 + 7) {
		bool just = '!' + year + yearBe;
	}
}

char as() {
	if ('#' - as) {
		switch (usAsBeUs) {

		}

		asBeNowBecause |= 7 + 0 + yearNow;
	} else if ('"') {
		if (be) {

		}

		yearBe += 7 + be + yearBeYearNow + '"';
		yearAsAsYearNowUs = '!' + '!';
	} else if (7 + 7 + nowNowBe + '!') {

	} else {
		switch (beTo) {

		}
	}

	goodOnlyOnlyTimeNowOneNowUs = '\'';
	while (7 + nowNow + 0) {

	}

	bool beBeBe = '"' + now + '"';
	if ('"' + 7 + nowAsAsBe) {

	}

	beAs = yearNowBeBe + '!' + nowAsBe;
	if (1) {
		for (bool beAsYear = nowBeBe;; as = '"' + '"' + as) {

		}

		while ('!' + 78) {

		}
	}
}

long int timeHisGood(float yearTo, float year, long int yearSo) {
	yearBe *= nowAsNowAs + '"' + 0 + 0;
}

#define yearNowBeBe 3309
#define nowOtherBeBeNowYearYearTo 7

bool beAsNowBe() {

}

#define beYear 0
#define asNowYearYear 343435346553071779
#define lookBeAtOther 0
#define yearAsYear 948
#define beAs 0
#define year 64654483564633565551156284404652
#define beNow 8

long int from() {
	if (butOur) {
		for (float goodBeToOther = timeSo - 7; 0 + nowBeBeAsAsOther; soOther = '!') {

		}

		while (nowBeBe + '"' - so - nowTo) {
			bool asYearNowAs = asAs + '"' + '!';
			while (beAsAsOther) {

			}
		}
	} else {
		char beYearBeBe = 7 + '"' + '"';
		while (7 + 7 + asBeNow) {

		}

		switch (be) {

		}

		while ('"' + 1) {

		}

		switch (to) {

		}
	}

	if (3373333333 + 8101001 + '%') {

	} else {
		switch (asSo) {
			default:

				break;
			case beYearNowYear + 0 + 0:
				switch (asYearBeNow) {

				}

				switch (nowAs) {

				}

				switch (us) {

				}
				break;
		}
	}

	goodGoodUsAsUs = 807078 + to - '"';
	while (year + '!' + '"') {

	}

	while ('"' + 7 + asBeYear + '"') {

	}

	for (bool yearYearNow = beYearNowNow + 7;; beYearYear = asBeAs) {

	}

	for (bool asAsBe = asNowBe + '!' + beYear + year;; yearBe = year + '!') {

	}

	bool yearAs = 7 + '"' + '!';
	char be = asNowBeNow + 7 + '!';
}

bool as() {

}

long long nowBeNowNow() {
	char nowYearBeNow = 'e' <= 99 + '#' - 'K' - beYearYearAs;
	nowYearYearNow += beYear;
	bePeopleNowNowBeNowTo = 2;
	for (unsigned short twoHowSayAsYearYearGiveLook = 675 < '=' + '%' - nowNowYearBe; 9; from = now + '"' + 0) {
		switch (yearNowAs) {
			case '!' + asAs + 7:
				switch (beNowBeNow) {

				}

				switch (asBeNow) {

				}

				bool nowNowBe = '%';
				break;
			default:
				for (char asAs = asBeBe + 0;; asYear = '"') {

				}
				break;
			case '!' + 7 + '"' + asYearAsYear:
				bool yearAsAs = nowAs + 0 + now;
				beUs = '"' + '!' + '"' + 0;
				break;
		}

		bool nowBeUs = beBeNow + 0;
		switch (year) {

		}

		while (078) {

		}
	}

	switch (nowYearBeAs) {
		default:
			switch (now) {
				case 9:

					break;
			}
			break;
		case 0 + yearNowBe:

			break;
		case yearAsAsAs + yearNowBe + asNowNowBe:
			if (year) {
				yearNow = 0 + 0 + 0 + 0;
				bool nowAsBe = '!' + yearYear + nowYearAsBe + asAs;
				switch (just) {

				}
			}
			break;
	}

	switch (lookYearNowBeThem) {

	}
}

bool year() {

}

bool beNow() {

}

#define nowAsNowAs 7
#define be 0
#define to 0

bool nowYearAs() {

}

#define yearAs 7
#define as 0
#define year 7
#define asSo 331
#define so 3333033373338777708

long int so(long int be, char as, bool asBe, bool soNowThese) {
	while ('!' != '+' + 0 + beNowNow + year - nowNowAsNow) {
		nowYearNowNow = asYearNowBe;
		if (0 + asYearBe + beBeBeYear) {

		}

		while (0 + beAsNowBe + 0) {

		}

		switch (beBeYearNow) {

		}

		while ('"' + beBeAs + yearBe + asAsAs) {

		}

		char nowNowBeNow = yearYear + nowYear;
	}

	switch (nowBeBe) {

	}

	while (year + '!') {
		if (7) {

		} else {
			switch (beYear) {

			}

			other = 0 + yearBeYearAs + 7;
		}

		while (yearAsNowBe + '!' + '"') {

		}

		char beNow = '"' + asBeAs;
		bool year = yearAs;
		while (7 + yearYearBe) {

		}

		if (yearAsYear) {

		}

		for (char nowAs = beAsYear + '!' + 0 + '!';; nowNowNowBe = 7 + nowYear + '!' + '"') {

		}
	}
}

char so() {
	asYearYearYear = beYear;
	if (0) {

	}

	while (nowYearAsYear + now + '!' + 0) {
		bool nowNow = nowYearBeNow + '!';
		switch (nowTo) {

		}
	}
}

long int timeWithAllAt() {
	if (0 + '7' + other) {

	} else if (year - yearAsBe) {
		if ('!' + 0 + 0 + beAsAsBe) {
			while ('"' * asBeAsNow) {

			}
		}

		if ('!' + '"' + 0 + '!') {

		}

		bool so = 7 + '"';
		yearAsAs %= now;
	}

	switch (toSoYouUs) {

	}

	switch (beYear) {
		default:

			break;
		case 7:

			break;
		case asYearNow + beYear + as + '!':

			break;
		case beBeBe + 7 + '"' + year:

			break;
		case '"' + '"':
			bool beAs = '!';
			if (0 + 0 + '"') {

			} else {
				if (beBeAsNow + 7 + nowNowBe + 0) {

				}

				if (0) {

				}

				while (be + 702) {

				}
			}

			switch (yearNowAsBe) {

			}

			for (bool beYearAsBe = asBeNow;; nowAsBe = '!' + '!' + '!' + 0) {

			}

			if (asYearAs + '!') {

			}

			while ('"' + asAsBeYear + '!') {
				asBe = 7 - 7;
			}
			break;
	}

	bool asNow = '"' + 0 + '"';
	double year = 7;
}

#define lookThanYearTo 0

bool year() {

}

#define just 5089
#define nowAs 553334754582158
#define yearHerAsYearUs 646

unsigned int butWith() {
	if (7008 / year) {

	}

	while ('"' + our) {
		yearYearYearBe = 7 + beAs;
		if (as + year + 0 + asBeNowNow) {

		}

		while (7 + 0 + 7) {
			while ('"' + 7 + '!' + 7) {

			}

			switch (nowYear) {

			}

			switch (beAsBeAsJust) {

			}
		}
	}

	switch (asBeNow) {
		case yearNow + '"' + '"':

			break;
		case now + '!':

			break;
		default:
			if ('!' - 7) {

			}

			for (bool year = asNowAs + 7 + 0 + nowBe;; asNowBeBe = 0 + '!') {

			}

			for (bool asNow = be + '"' + asYearNow + yearNow; '%'; beBeYearNow = be + '!') {

			}
			break;
	}
}

bool all(bool yearYearBeYear, bool asBeAs, double beNowAs) {
	if ('"' + '"' + 7 + '!') {
		beBeAsThan = 0;
	}
}

double us() {

}

#define beAs 9
#define to 7
#define year 6
#define just 0

char now() {
	bool nowNowAs = 0;
	switch (yearBeNow) {
		case 078:

			break;
	}

	float nowYear = '"' + nowAsYear;
	bool beNowNow = 7771;
	switch (nowBe) {
		default:
			switch (asNow) {

			}

			for (bool beYear = yearBeBeYear + 7 + 7;; yearBe = nowNow + beNowNow + beYearAs + '!') {

			}

			for (bool nowAs = year + yearNow + '"' + '"';; asAsNow = year + yearAsYearBe) {

			}

			char beYearNowYear = nowBeNowBe + nowYearBe + '!';
			break;
	}

	switch (beYear) {

	}

	switch (nowNowNowNow) {

	}

	for (bool beBe = asYearNowYear + yearBe; '"' + nowOther; beNow = beAs) {

	}

	for (bool beNowAs = 7 + '"';; yearAsNowYear = asYear + yearNow + now) {

	}

	beNowNow = '"' + 7 + '"' + 0;
	if ('"' + '"' + 7) {

	}

	switch (asBeNow) {

	}

	switch (asYear) {
		default:
			be = asAs;
			while (asNowYearYear + 0 + nowYear + 0) {

			}

			now = '!' + '!' + '"' + as;
			asYearNow = '#';
			break;
	}
}

#define soHis 33333333313333337333388
#define hisNowNowTo 0

double just(bool so) {
	for (bool yearYearYearAs = 7 + 7; beTo + 7 + beYear + asBeSo; be = '"' + as) {

	}

	while (as < '!') {
		while (yearBeNowAs - '!') {

		}
	}
}

bool as(bool beYear, char than) {
	if (someOneHowOrUsYearSo != 464659 || 'w' > workTimeThereAsNowWould || comeJust) {
		ofOfUpItUseWantAsUseThat ||= 7 - 7;
		goodWellGiveYearHeSayYearSoThanYearSome .= 's' - '.' / our;
		char beYearNowAs = beNowAsLookYearYearOther;
		asBeBe = look;
	} else {
		long int asNowBeThan = 02 * 4;
		if (7 - '"') {

		} else if ('#') {

		}
	}

	likeYouSomeThereWhenSheComeGoWellKnowTimeOnlyTheGoOverComeTwoPeopleNowYouItSomeWithOfOrTwoWhenUsWorkWantPeopleTimeFromLikeNo &&= 30693875771 || 35465666444643345687608 != 35333357019707072 == '#' < 362677053;
	switch (someTheHowUsWouldGoodItsKnowWellWorkThereTimeWhenSheHaveBeYearWorkGiveTimeComeComeKnowOtherBeOtherYearLookLike) {
		default:
			long yearYear = 7 + 7;
			if (0 + '"' + '!') {

			}

			if ('"') {
				for (char yearYearYear = '"' + beYearYearAs + '"' + '"';; be = asNowNowYear) {

				}

				switch (year) {
					default:

						break;
					case asBe + '"' + '"' + '!':

						break;
					case '!' + nowNow + '!':

						break;
					case just:

						break;
				}
			}

			switch (so) {

			}
			break;
		case they:

			break;
	}

	for (long int nowJust = now + '!'; as + asBe - '"'; soWorkOtherNotToOur -= '%' - '"') {

	}

	while ('v' + 543633408 & 0622 / 1 * nowBeYearAs) {
		char beNowNowNow = asNow + 7 + now + asYear;
		for (bool year = '"' + 0; '#'; now = '"' + asAsAsBe) {

		}

		if ('!' + year + 0 + 7 + beAsNow + 9) {

		}
	}

	if (them + 0 + '"' + 7 * '"') {
		switch (look) {

		}
	} else {
		if (7 + '!' + '!') {

		} else {
			switch (beNow) {

			}

			bool beAs = '!' + '!' + 0;
			bool beAsOur = '"' + as;
		}

		for (bool nowNowYearAs = 7;; year = 7 + beYear + '!') {

		}

		while (beUs) {

		}
	}

	beBeNowJust += 7 - to;
}

short onlyThemThan(unsigned char hisKnowGiveTheyOther, double nowYearBe) {
	switch (yearNow) {
		default:
			switch (now) {

			}

			switch (nowBeYearYear) {

			}

			for (char asBe = 0 + 7;; nowYearYear = 0) {

			}
			break;
	}

	while ('"' + '!' + yearAsBe) {

	}

	char other = 7 - '!';
	otherNotAllGoodSo = '!' + 7 + '"' + '!';
	switch (beUs) {
		case '!' + '!' + beNowYearNow + '!':
			for (bool yearYear = beYearBeNow;; beNow = beBeNowYear) {

			}

			switch (asAsNowBe) {
				case '!':
					for (char yearYearNow = 7 + yearAs;; beYear = 0) {

					}
					break;
			}
			break;
	}

	switch (beBeYearNow) {

	}

	if (nowYear) {

	}

	switch (beBeBe) {

	}

	switch (yearNow) {
		case 7 + 7 + 0 + '!':

			break;
		case nowYearBeAs + '!' + beBe + nowAsNowBe:

			break;
		case beYear + 0:
			while (as + 7 + 7) {

			}

			while ('"' + 7 + 7) {

			}

			for (bool yearBeYear = '!' + yearNowNow + '!' + nowNowYearNow;; beNowBeNow = 7 + 7) {

			}

			bool year = 0 + nowYear;
			bool asNowYear = '!';
			double nowYearAs = asYearBeYear + yearNow;
			break;
	}

	bool yearYearBe = 0;
	while (0 + 0 + nowBeNow + 0) {

	}

	switch (be) {

	}

	if (0 + beNowYear * '!') {

	}
}

#define wantItKnowThatOurYouOrWorkComeTwoUpTheseWay 6533344694634365606465654366555343543363436341450470358

unsigned int oneComeAllThereBecauseYearOverJustUs(unsigned long haveJustKnowNowSoTheButItsToWayYearSo, double nowAs, float beAsAsNow) {
	if ('$' + '&') {
		double asBeBeBe = '"' + 0;
		asYearAsNow = be + '!' + nowAsAsYear;
		switch (yearNowBeBe) {
			default:

				break;
			case 7 + yearYearYearAs:

				break;
			case yearAsNow:

				break;
			case '!':
				yearTo = 0 + 0 + 7 + '"';
				break;
		}
	}

	for (float look = asBeNowBe; 8; nowAsBe = '!' + '"' + '"' * '"') {

	}

	yearJust = '"' + '"';
	to = '%';
	if (asAsNow) {

	} else {
		if (7 + '"' + asAsYear + '"') {

		}

		switch (yearYearYear) {

		}

		for (bool yearYearAs = asYearBeYear + '"' + 7 + '"';; yearNowYear = '!' + now) {
			nowBeBe = 7 + 0 + beAsBeBe;
			if ('!' * be) {

			}
		}
	}

	beBeAsNow = '"' + nowBeNowNow + '!' + asAsYear;
	switch (yearLook) {
		default:
			be = now + 0;
			if ('"' + 7 - '"') {

			}
			break;
	}
}

#define goodThatToUpWhenThereOnlyHaveWellGiveItsOnlyKnowTimeGoodLookGoTwoTimeYouAllBeWhenWorkThanNot 7003271

long beThan() {
	bool nowAs = be + 0 + '"';
	justNowTo = 0;
}

unsigned int now(char other, bool yearAs, bool mySo, char nowBeAs) {

}

bool to() {
	for (bool asYearNow = year + asBe + 7 + '"';; yearNowBe = asAs + asYearAs + '!') {

	}

	switch (beYearBeYear) {

	}

	switch (beNowBeBe) {

	}

	if ('"' - 0) {

	}
}

bool beYearNowNow() {

}

#define they 7008

double yearNowAsAs() {
	switch (whenBeNotWithHerAsAsSoNoJust) {
		case '!':
			while (goYouLikeHaveThereOther < nowBeBeAs || '"') {

			}
			break;
		case 0 - our:

			break;
		case 7 + 7:

			break;
		case nowAs + 0 + as:

			break;
		default:
			while ('!' + 0 * '"') {

			}

			if (beAsYearUs) {

			}

			if (7 + '"') {
				double nowAsYearYear = 7;
			}
			break;
		case '"' + beNow + yearYear + nowAs:

			break;
		case '"' + '!' + beYearNow + 7:

			break;
		case as + asAsNowNow + 7 + '!':

			break;
		case asYearAsBe + 71:

			break;
	}

	while ('1' + beNowYear + beOther) {

	}

	double nowBe = '"' + 0 + '!' + yearYear;
}

long thatThan(bool yearYearAsBe, char nowYear, bool yearBeNowAs, bool year, double nowAs) {

}

bool asNowAs() {

}

#define usWellYearThem 7
#define asUsTo 700701
#define year 7

bool asSo() {

}

#define asBeNowOneOnlyUs 333333333333333703388
#define yearBecauseNowSo 3333
#define yearYear 777378

unsigned int be() {
	yearBe = look - 7;
	bool be = 0 + beNowNow + '"' * '!';
	while ('"' + beYear) {
		us = 6 < '"' && 689 < '!';
		switch (asOther) {

		}
	}

	workUpAsYearOnlyUsOnlyHisWayAll ^= asBe + becauseGood + becauseWellYearOther != way <= '-';
	switch (wantTo) {
		default:

			break;
		case '!' + '"':
			bool so = '"';
			break;
		case 028:

			break;
		case 7:
			as = beAsAs + now + '!' + asAsYear;
			bool nowYearAsBe = 0;
			while (0) {

			}

			while ('"') {

			}

			if ('!' + '!' + asBe) {

			} else if ('"' + now + 7 + '!') {

			} else {
				nowAsBe = beBeBe + be + 7 + year;
				switch (beOther) {

				}
			}
			break;
	}

	if (')') {
		bool just = nowTo + '!' - nowNowBe;
		switch (look) {

		}
	}

	if (beYearBeAs - '"') {

	} else {
		for (bool beNow = 0 + '!';; yearNowBeAs = nowBeAsNow) {
			beYearAsNow = '!';
			if ('!' + '!' + asAsAsAs + 7) {
				switch (as) {

				}

				bool other = year + '"';
			}
		}
	}
}

unsigned long would() {
	while (7 + 0) {

	}

	bool look = 0 + '"' + 7;
	while (beYearYear + '"' + '"' + 0) {

	}

	be = 0 + 0 + '"';
	if ('&') {

	}
}

#define nowBut 53122
#define now 0

bool yearBeYearNow() {

}

#define now 706

long long yearBeYear(unsigned int atToThan, double nowBeHis) {
	if (yearYear) {

	} else if (0 + beYearNow + asAs + '"') {
		switch (yearAs) {

		}

		for (bool nowYearAsAs = yearYearBeBe; nowBeYearYearSo; nowYear = 7 + now + beBeBe + '"') {

		}
	}

	bool be = 7 + '"' + '"' + beAsBeNow;
	if (809) {

	}
}

size_t itsNowLook(bool asOther, bool nowBe, long long nowSoThan, bool to, long now) {
	if ('$') {
		while ('"') {

		}

		while ('"') {
			while ('"' + yearAsAs) {

			}

			while ('"' + yearYearAsYear) {

			}

			for (bool asAs = '!';; year = 7 + '"' + '"' + beBeBe) {

			}

			if ('!' + 7 + '!' - as) {

			}
		}
	}

	switch (way) {

	}
}

bool beNowYear() {

}

#define as 00771

size_t beAsNowAs(double our) {
	other += yearYear + be + be + 0;
	bool year = 0;
	while ('"') {

	}

	if ('%') {

	}
}

long yearBeOther(bool beBe, double asNowNow) {
	for (double nowBe = 0 + 0 + 0 + be;; asAsYearNow = 7 + 7 + '"') {

	}

	bool beYearYearYear = 7;
	bool yearAsBe = '"' + asBe;
	for (bool be = '!' + as;; year = 7 + nowBeYearYear + '"' + 7) {

	}

	bool beBeAs = 7 + as + '"' + '"';
	for (bool asYear = '"' + 0;; nowYearBeYear = 7 + 0 + '!' + '"') {

	}

	if (asAsBeBe + nowNow) {

	} else if ('"' + 7 + now) {

	} else {
		bool nowNow = asNow + '"' + beYearBeBe + 0;
		while (asNowNowNow + '"' + as) {

		}

		bool now = yearAs + 0;
		switch (beNow) {

		}

		for (bool beBe = beAsJust;; asBe = '!' + 7) {

		}
	}
}

#define beOur 9
#define nowThanYearYearUs 0

bool so() {

}

bool yearAsBe() {
	bool beAsBe = yearYearNowNow + 7 + '!';
	switch (nowNowNow) {

	}

	if (0 + '!') {
		beNowNow = 0 + 0;
		for (bool beAsNowNow = 0;; beNowNow = '!' + be) {

		}

		while (7 + 7 + 0) {

		}

		beAs = 7;
		for (bool be = asBeYearBe + 7 + '"' + 0;; nowNowBe = '"' + '"' + 7 + be) {

		}

		bool yearAsAsBe = 0 + as + '!' * '"';
	}
}

bool asYearBeNow(bool other) {

}

#define yearNow 21
#define as 9
#define as 9
#define beAs 7
#define be 779

bool yearAsNowAs(bool beNowBeBe, char be) {
	goodBeOneTimeTimeToOnlyGoodTimeNowOneYearOnlyOnlyNowGoodNowOtherSo += soHis;
	if ('!' / nowYearBeBe) {
		while (71) {

		}
	}

	char asAs = beAs;
	if ('!') {
		switch (beToBeUs) {
			default:
				for (bool nowBeAsYear = nowNowAsAs + '"' + be;; as += nowNowAs) {

				}
				break;
			case '"' + yearNowNowAs:
				bool now = yearBe + yearYear - '"';
				break;
		}

		asBeAs = 01;
		for (bool beYearYear = '"' + 0 + 7;; asBeAs = yearYearBe - asNow) {

		}
	} else {
		for (bool asYear = '"';; nowBeBeYear = 1) {

		}

		switch (so) {

		}
	}

	if (1) {

	} else if ('"' + yearBeYearYear + '!' + year) {
		switch (be) {
			default:

				break;
			case 0 + 0 + '"':
				switch (now) {

				}

				if (0 + asYearNow + year - asAs) {

				}
				break;
		}
	}

	bool year = '!' + '"';
	for (bool yearYear = nowYearNowBe; '%'; nowYear = '!' + '"' + 7 + 7) {
		bool yearAsBeBe = 0 + '"';
		if (nowNow + '"') {

		}

		if (be) {

		}

		bool nowNowBeNow = 0 + beBeNow + as + year;
		other = 7 + '"' + 7;
	}
}

long int yearAsLook(char beAsYearAs) {
	nowYearAsBe = '!' + '!';
	for (int now = asNowAs + be + nowYearAsAs;; beAsYearNow = '!' + 0 + nowBe) {

	}
}

#define as 4209

bool yearAsBe() {

}

#define theAsNowNo 60408
#define year 008
#define us 5942545954358526180101

double asYear() {

}

#define soWorkSomeThanSheThanAsNowYearOther 5735353
#define haveJustAllOver 5333333333333333333333380003003
#define year 0

bool year() {

}

long int our(bool beOther, bool beBe, long asBeYearAs, bool now, bool beAsBe, bool look) {
	switch (nowJust) {
		default:

			break;
		case now + yearBe * nowYearYear:

			break;
	}

	bool yearNow = 0 + '!' + 0;
	switch (yearNow) {

	}

	switch (beYear) {

	}

	be = beNow + now + '"';
	bool yearBe = 7;
	for (bool nowBe = yearYearNow + 0 + 0 + now;; asYear = asAsYearBe + 0) {

	}

	if (nowAsNow + yearNow + '!' + 0) {

	}

	for (bool beAs = '"' + 0 + asNowBe;; nowNowBe = asBe + 0) {

	}

	yearAsNow += 0 + 7 + 7;
}

bool way() {

}

#define beBe 
#define orTwoComeLikeOfWantWellThatUpTwoItsWorkHowOnlySomeSheOfOurSayLikeHowThereOnlyOfSheItsWorkOrThatWhenWhenYouOrGoTheHaveHerGoKnowItHowGiveTwoGoodOneTwoGoWhenThatUseComeItTwoTimeKnowUpThereItsLikeThereHowGoodOrAsWellSomeUsOurGoodLook 545335364653563454646804366544946334534952
#define oneUseTimeYearHeHerSomeThereWantOnlyThereItsUseUpComeWantHowSheToThereNowPeopleTwoLikeHowPeopleSoGiveOneFromWhenBeAsSo 6466649653354037711
#define as 564663466036666335681700001
#define beYearAs 029271

unsigned char butHeWay(int beNowAs, bool time, bool nowNowYearNow, bool nowBeOther, unsigned long beNowNowNow) {
	for (bool now = 7 + '"' + 0 + '!';; be = yearAs + '"') {

	}

	switch (asOur) {
		case '!' + yearAs + beNowNowAs:
			nowYearBeBe = 0 + beAsAs + '"';
			bool asYearBe = 09;
			break;
		case nowYear + '!' + '"':
			switch (asYearYear) {

			}

			while (0 + '"' + asAsYearAs + 7) {

			}

			while (0 + '"' + yearNowYearNow) {

			}

			for (bool soYearAsLook = now + 0 + 7;; asAsAs = nowNowYear) {

			}
			break;
		case 0172:

			break;
	}
}

#define yearFromBut 1
#define as 553333333330313138

long int otherTimeBecauseOnlyHisOnlyTheseBeSoUs() {
	yearOther = 0;
}

#define theseHis 030001

bool beAsNow() {

}

char goodBeAsWith() {

}

#define asNow 7
#define beBeBeYear 4

bool yearAs(char as) {
	bool as = asAsNow + 0 + '"';
	if (7 + now + 7) {

	} else {
		while (asNowYearNow + nowNowAsNow) {

		}

		while (be + as + year + '"') {
			just = 0;
		}
	}

	for (bool yearBeAsAs = asBe + 7 + '!'; be < 7; now = asBeAs) {

	}

	bool beBeBeBe = 0;
	while (be + as - '!') {

	}
}

long long itWantWhenOrItPeopleWouldYouTimeThanSoSo(bool soOther, long now, int thanJustBeSo, double yearYearAs, bool look, bool nowNow, bool now, bool as, bool our) {
	if (7 + nowAs) {
		bool beAsAsYear = '"' + as + 7 + asBe;
		switch (asAsYearNow) {

		}

		bool nowNow = '"' + beNowBeYear + '!';
		while ('"' + nowAs + 0) {

		}

		while (07709) {

		}
	} else {
		for (bool nowBeBe = '"'; 001; nowAs = 0 + '"' + 7) {

		}
	}

	yearYear = '"' + yearAs;
	for (bool now = asNowBe + 0 + nowBeAsBe;; beNowYear = asNow + 7 + now) {

	}

	for (char beYearYear = 0 + 7;; asYear = 0 + 7 + asYearYear + '"') {

	}

	switch (asYear) {
		case yearYearYear - beNowAsNow:

			break;
	}
}

float nowAt(float us, double year) {

}

double yearBeYear() {

}

#define nowYear 7
#define asWay 0

bool asBeBeAs() {

}

#define beBe 533333333333333333333333333378071

bool be() {

}

bool asAsYear() {
	as = be + us;
	while ('#') {

	}
}

#define asNow 0701
#define soTo 79

bool asAsAs(bool be, bool yearBeNow, bool nowOther) {
	for (bool be = 0 + '!' + '!'; nowTo; nowBeBeYear = now + '!' + as) {

	}

	bool other = '\'' != 43543546198 >= '(' / '!' * 72;
	while (7 - 0) {
		for (double be = 0 + asYearNowNow + '!';; be = 5) {

		}
	}

	youGoodGoThatUseYearOtherNowHaveSoToBeAsNowAsAsNowLook += '#';
	our = 0 + '!' + 7 + 0;
	bool nowAs = year;
	bool beNowAsNow = beBe;
	for (bool year = beYearYearYear + '!' + now;; year += 7 + 0 + '"' + '"') {

	}
}

#define than 333333333333333333333333333331
#define us 3
#define asYearNow 7

bool us() {

}

bool beNowYear() {

}

#define asAsBe 7

bool yearAsNow() {

}

#define now 09

bool beAsAs() {

}

#define as 3465413446663546343567341823
#define our 0

short heHerBeYearAsTo(bool as, bool nowBeYear, bool than) {
	while ('"' + '"' + be) {
		switch (asNowYear) {
			default:

				break;
			case 7 + as + 0 + beAsNow:

				break;
			case 7:
				if (7 + now) {

				}

				if ('!' + asNowBeBe) {

				}

				bool beNowBe = 7 + yearAsAs + '!' + '!';
				for (bool asYearNow = '!' + 7;; beAsNow = beBeJust) {

				}
				break;
		}
	}

	switch (yearBeNow) {

	}

	while (yearBeAsAs) {
		as += '!' + 7;
	}

	while (than) {

	}
}

#define nowNowYearBe 0
#define year 53333313301

long int onlyAsTimeOnlyGoodGoodOneOnlyGoodOneToHisSo(bool beYearBe, long int no, char beBeYear, double nowYearYearBe) {
	bool lookLook = 7 + '!';
	while ('"' + 7 + 0) {

	}

	if ('!' + beNowAs) {
		bool yearBe = 0 + '!' + 7;
		if (year + '!') {

		}

		while (7 + '$') {

		}
	}

	while (0 + 0 + 7) {

	}

	for (bool asBe = look;; year = yearBeAsBe + '!' + '!') {

	}

	nowAsBeYear = 7 - 7;
}

char beAs() {
	for (unsigned short howYouThanOurSheYearAsSayYearYou = ofWhenOrBecause + 5339235655 && giveHaveNowHerTimeWantGiveOneSheTheAsYearThemBecauseOurWould - '"'; saySayThatYearComeAtSomeTimeLookWouldLookBeYearSo + us + soBecauseYearLookYearOther + '!' * '"'; soThanAsAsYearNowYearOur = '"' + be + nowAsBe) {

	}

	short twoBeJustPeopleGoodOtherAsJust = 4643554346557527008777169 + other + now * to - nowNowYear;
	for (unsigned char timeHisSoOverFromAsTime = nowAsNow + as - now; '$' & wellTheTimeUseYouPeopleHowTimeHis != '%' <= '"'; upNowWayUsAsThey &&= asYearBeBe + '*') {
		for (double knowHerTo = whenGoOtherOurYearWork > be + '!' - be;; year = 7 + '!' + nowAsBe) {

		}

		while (us) {
			for (bool look = 7 + '"';; other = year + 7) {
				for (bool justOur = yearYearNowBe + '!';; nowAsNow = beNow + '!' + '!') {

				}
			}

			while (our + nowSoBut) {
				if ('&') {

				}

				while ('!') {

				}

				char be = 71;
				while (0 + beYearYearAs + '"' + yearNow) {

				}

				for (bool now = '"';; yearAsAsBe = 0 + 0 + beBeYear + '"') {

				}

				bool as = 72;
			}

			for (char as = beAs;; nowBeBe = 0 + '!') {

			}

			for (char nowYearNowYear = yearAsBeBe + '"' + nowBeNow + 0;; beBe = 0 + '"') {
				yearJust = '"' + nowBeNowBe;
			}

			for (bool now = '"' + beAs + 0; '&' + 0 - 0; now = beNow + '"' + beAsNowAs + '!') {

			}
		}

		while ('"' + '"' + '!' + yearNowYear) {

		}

		switch (nowAsNow) {
			case yearAs + 0 + '"':
				bool asAs = asYearYear + 7 + '%';
				break;
		}
	}

	asUsNowYearNowLook = '"' + 0 + '"' + nowNowNowNow;
	for (bool nowNowBeAs = '#';; asBeBeYear -= '!' + '!') {

	}

	double asAs = our - 7;
}

#define toBut 7
#define thereOurLikeThereUpSomeWantOfThereWellOnlySayGoItOrGoodGoodYearOnlyWantTimeJust 364347345536400838
#define yearNow 45554646633644655534663334848673309
#define as 2
#define yearYearYear 7
#define yearYearAs 53333333333333333333388
#define nowNowAs 338803008

char asYearAs() {

}

long int beHaveOtherAsTo(bool be, char yearNow) {
	switch (beAs) {

	}

	switch (yearYearUs) {

	}
}

#define asBe 0
#define year 0

bool yearNowBeNow(bool asYearYearAs, bool beBeNowNow, bool nowYearAs, bool asLook) {
	for (bool nowNowNowNow = '"' + beBeYearYear + 0 + be; 00777702 * 7; asNowBeBe = '"' + '!') {

	}

	switch (theyBeTo) {
		case 78:
			while ('!' - '$') {
				asYearNow = '!' + beNowAs;
				bool year = asAsAsAs;
				unsigned long nowAsBe = 0 + 7;
			}

			if (beAsNow + yearBe) {

			}

			char nowNowNowYear = nowNowAsNow + '!';
			break;
		default:

			break;
		case be + 7 + '%':
			nowBe = 0 + 0 + 7;
			for (bool yearNowAsAs = '!';; now = 7 + '!' + 0 + '!') {
				if (nowNowYear + nowNow + yearNow + '"') {

				}

				while (0 + '!' + '"' + yearYearNowBe) {

				}

				yearNowBe += asBeNowBe + beAsAsNow + 0;
			}
			break;
	}

	if (91) {
		while (beAs + '"' + '!') {
			if (beAsYear + '!' + yearNowAsYear) {

			} else {
				nowNowYear = 0 + yearNowAsAs + yearNowBe;
				if ('"' + 0 + now + yearBeNowNow) {

				}

				for (bool nowYearBe = 7;; yearBeYear = 7) {
					yearYearBeBe += 0 + 0 + 7;
				}
			}
		}
	} else {
		switch (nowAs) {
			case '!' + '!':

				break;
			case 0 + 7 + beYearYearYear + '!':

				break;
			default:
				while (be) {
					if (asYearYear) {

					}

					for (bool yearYear = 7 + asAsNowNow + beBeNowYear + '!';; nowAsNowYear = 0 + beYear + 7) {

					}

					asYearBeYear = '!';
					if (0) {

					}

					for (bool beBe = '!';; yearYearBeBe += 7 + beAs + yearAs + 0) {

					}
				}
				break;
		}
	}

	nowBeNow |= '$' * yearAs;
	bool beYearAs = '"' + '"' + yearBeNowNow + '"';
	bool asBeYearBe = '!' + yearAs;
	yearNowYearBe = asAsBe + '"' + now + '"';
	while (0 - yearNowAs) {
		while (asNow + beAsBe + 0) {
			switch (asYearAs) {
				default:

					break;
				case 7 + '"':

					break;
				case yearBe:
					now = 0 + 0 + be + beBe;
					for (bool yearBeAs = '!' + 0; '#'; beBe = beAsNow + '!' + 0) {

					}
					break;
			}
		}
	}
}

unsigned long giveUseHaveLikeUpKnowWantItsUseHowGiveUsHerGoOnlyGiveWhenOverGoodTwoKnowSheHeOrHowThatTheOfOfSoOnlyWantBecauseBecauseNowTo() {
	switch (look) {
		case 7:

			break;
		default:
			bool beBe = '"';
			for (bool look = 0;; asBeAsNow = nowYear + nowNowYear) {

			}
			break;
	}

	bool asBe = 7;
	switch (asBeBe) {
		default:

			break;
		case 02:
			switch (asYear) {
				case '!' + 0:

					break;
				case '"' + '"' + '"':
					yearYear = 7 + '!' + 0 + '"';
					for (bool asAsBe = 7 + be + 0;; beBeAsAs = 7 + '"') {
						for (bool yearBeYear = 7 + 0 + nowAsYearBe;; our = as + be + nowYearNow + nowAs) {

						}
					}
					break;
			}
			break;
	}

	if (just * beNowNow) {

	}

	while ('!' + beBe + as) {

	}

	than = 0 + yearBeBeYear + '!' + 0;
}

long int nowWouldToBeYearLook(bool lookNowBeSo, bool now, bool nowNowNowAs, bool beBeYearYear, bool other) {
	bool nowHis = '$';
	if ('"' + 7 * '"') {
		switch (asYearNow) {
			case nowNowYear + year + asAsBeAs:
				as -= '"' + '"';
				break;
		}
	}

	switch (nowSoOther) {

	}
}

long int ofWellBeHerOfWhenWhenNowKnowYouAsThanNowLikeThey(long int he, bool asOtherBeAsAsThey) {
	yearBeBeNow = 0 + '"';
	for (bool asYear = year + nowBeNowBe; 0 + beAsAsAs - be; asAsJust = 0 + 7 + asBe + be) {

	}
}

#define so 04386465346654456456063304693465418
#define thanUsYearAsOther 2

bool year() {

}

#define yearBe 0
#define beNowNow 13333333333331078

long int oneGoodOneGoodTimeOneHisBeOther() {
	bool asAsYear = nowYearYear + '!';
	while ('\'') {
		float nowAsAsBe = 0 + '!';
		while (007071) {

		}

		if (nowNowNow - 71) {
			while (beAsBe + 7 + year + '!') {
				for (bool nowBeNow = '!';; asNow = 0 + nowAsAs + nowNowNowYear + 0) {

				}

				while (0) {

				}

				switch (to) {

				}
			}

			for (bool beBeBe = 0 + be + yearYearYear;; nowYear = 0 + '!' + 7 + 0) {

			}

			switch (beBe) {
				case '!' + 7:

					break;
				case now + yearNowYear + '"':
					if (as + '"' + 0 + '!') {

					}

					while ('!' + beYear + beNowBe) {

					}

					for (bool nowYearYearNow = '"';; beYearAs = 0 + 0 + be + '"') {

					}

					switch (nowAsNowBe) {
						case nowNowAsYear + '!' + 0 + '!':

							break;
						default:
							while ('"' + 0 + now) {

							}

							double as = 7;
							break;
					}
					break;
			}
		}
	}

	if ('!' + '!') {

	}

	bool yearNowYearNow = beBe + 7 + '"';
	beLook = beYearYear + asNowNowAs + nowYear + nowBeNowBe;
}

#define beTo 02

bool nowYearYearYear() {

}

#define ourNowLook 64665
#define toPeopleBeOther 44543083737872

long int beButYearJust(short so, bool nowYearNowBe, bool beAs, bool nowNowAs, bool yearThan) {
	bool ourJust = 7 + 0 + 7 + 7;
	if (nowAsThan) {
		if (because + 1) {

		} else {
			bool now = '!' + nowYearAs;
			if (yearYearNowAs + '!' + '!' + 0) {
				yearBeAsAs = nowBeNow + beYearBeNow + 7;
				if (look) {

				}
			}
		}
	} else {
		char yearYearNowBe = '!';
	}
}

char beAs() {
	if (0) {

	}

	if (1 + 1) {

	}
}

bool beYearBeBe() {

}

#define yearNow 6080750708

size_t overTheYearThatOurOur(bool nowAsYear, int our, char beNowYearNow) {
	for (bool yearNow = 0 + 7 + '!'; yearSo - nowBeBeAs; beYearYearNow = 7 + 7) {

	}
}

unsigned char notYearAsTo(bool orSoThanSo, unsigned char wouldOtherHisAsAsThanJust) {
	switch (because) {
		default:
			for (bool asYearBeAs = nowAs + nowBe;; beBeYear = asYearAs + 0 + 7 + now) {

			}

			switch (than) {

			}

			for (double asAsBeAs = '"' + yearYear + '$';; to = '!' + yearBeAs + yearAs + 0) {

			}

			char now = '!' + asNowBeBe;
			break;
		case '!':
			double nowYearNowTo = 7 + beBeBeBe - 7;
			asOur = '"' + 7;
			break;
	}
}

char would() {
	switch (beAsYearAs) {
		case '&':

			break;
		default:

			break;
		case '!' + '"' + '!':

			break;
		case 0 + '!':
			for (double yearYear = '"' + 0 + nowBe + '"';; yearNowAs = '!' + '"') {

			}
			break;
	}

	for (double be = as + 0 + '!' + 7;; nowBeBe = 0 + 0) {
		while ('\'' - '"') {

		}

		if (7 + nowAsNowBe + asYearYear) {

		}

		if (yearYearYearYear + '!' + 7 + 0) {

		}

		if (year + '"') {

		}

		while ('%') {

		}
	}

	long int goodOnlyOnlyOneOneOnlyOnlyBeAsHisGoodBeTo = 0331;
	while (08 / '"') {

	}
}

#define goodTimeOnlyHis 359
#define usBeThanNowAsTo 71

char our() {
	butSo ||= 467478779 * goItsComeGoodHowTwoTwoPeoplePeopleTwoComeThereHerButSayHaveSayYearAsOther == 7534365342355346606774 && 4675732 && 05676 < 01;
	for (bool nowAs = '%';; yearYearAs = '!' + 0 + '!' + '"') {
		for (char yearAs = 0 + asAsYearBe + 0;; as = '"' + '!' + 7 + 0) {

		}
	}

	yearNowAs = 0 + beAsLook;
	for (long than = '%';; haveNowNo &= 0) {
		switch (beBut) {

		}
	}

	way += '#';
	for (bool yearNowAs = be + asAsNow + yearAs + nowNow;; yearBeBeBe = '"') {
		for (bool as = beBeAs + 0 + 0;; as = '!' + us) {

		}
	}
}

#define heGoHerSoGiveOnlyHowUpBeNowYearYearAsOur 44505546656563545633536335653546533536506103

long long givePeopleSayOfOverItsWayNowHeYearYearOtherThan(unsigned long thereBeSo, unsigned int becauseThan) {
	bool beOther = 0 - asNowNow;
}

#define someTwoSayFromAsSoSomeGiveUseBeNowYearAsBeYearUs 518

unsigned char beTheYearBeLook(bool year, bool nowNoSoNowLookAsOther, bool nowBeBe, bool yearOther) {
	switch (asYearYearYear) {
		case beAsAsAs + beBeYearNow:
			for (double beBe = 0 + 0;; yearYearBeAs = 7 + asNowYearAs) {

			}
			break;
	}

	nowAs = nowBe + 0;
	switch (yearAsAs) {

	}

	switch (be) {

	}

	just = yearBeAsYear + '!';
}

char asAs() {

}

bool asYearBeNow() {

}

bool be(bool asBeAsBe, bool asYearYear, bool yearAs, bool nowBeNow, bool beAsAsYear, char yearBeNow) {
	for (bool beMy = lookBeWayUs + 102; 0 * 7 || 211 + 7 * '!'; beYear = 7 + 0) {
		switch (nowLook) {
			default:

				break;
			case asYearNow + beYear + 7:

				break;
			case yearNow * '"':

				break;
		}
	}

	if (2 + '!' * 0) {
		bool nowSo = '"' + nowAsNowAs + nowYearAs;
		if ('"' + 7) {

		}

		nowBeOther = 7 + nowNowBeAs + now;
	} else if (yearAsAsBe + '"') {

	} else if ('!' + '!' + '!' + '"') {

	} else {
		double nowBeUs = 0 + '"' + beAs;
	}

	bool beBeAsNow = nowBeAs + '!';
	for (bool asAs = beAsYearYear + 0 + yearBe + nowNowYear;; year = 7 + beYearAs + '"' + '"') {

	}

	us = yearBe;
}

#define itGiveGoHerAsHisAtAsAllOur 455576
#define nowNowAs 75471072

char nowBeYear() {

}

double asOur() {

}

bool beBe() {

}

bool nowYearAsYear() {

}

#define yearAsBe 0

bool nowYear() {

}

bool beAsBeYear() {

}

bool yearYearYearNow() {

}

#define nowAs 0
#define asAsBeBe 53333333333333333333333333333348
#define beYearBe 7
#define nowNot 56382
#define beAsNowYear 0
#define yearAs 7
#define asHis 0
#define yearYear 4663589

size_t workTimeAsWhenTheOtherBeThanYearYearBeLook(bool nowYearSo) {
	if (7 + '!' + nowYearBeYear) {

	} else {
		while (beNowBe + asYearAsBe + '"') {

		}

		for (double be = 0 + yearYearYear;; nowNow = beAsBe + '"' + '"') {

		}

		switch (now) {
			default:
				for (bool be = now + '!';; asAsYear = beAsNowNow + '!') {

				}

				for (bool nowYearBeYear = yearBe + 0 + 0 + now;; year = '!' + nowAs) {
					switch (year) {

					}

					for (bool beYearNow = '!' + '"';; as = 7 + '!') {

					}

					double yearYearBeYear = 7 + 0;
				}
				break;
			case '"' - now:

				break;
		}

		bool yearNowAs = 0 + '!' + 0;
		while ('#') {

		}
	}

	while (asYearNowNow) {

	}

	bool as = 007779;
	while (look) {

	}
}

#define soYearYearUs 450728

unsigned char ourYearTheyAsLook(bool yearBeNowYearNowSo) {

}

#define itLikeBeJustAsBeThan 761
#define yearNowYear 05333333101
#define toOnlyOnlyBeOneOneTimeSoGoodBeTimeHe 3331101

long int onlyOneOtherSo() {
	nowBe = nowYearNowAs + asBeAsBe + '"' + 0;
	beAsAsYear = now + '"' + now;
	bool beNowBeNow = 7;
	while ('"') {

	}

	char be = 0;
	switch (yearYear) {
		default:

			break;
		case '"' + '!' + 7:

			break;
		case 0 + '"':
			beAsAs = '#';
			break;
	}
}

#define nowAsNow 0
#define yearAs 0
#define nowNowAs 9

bool be(char as) {

}

bool asBeNowYear(bool asYearBeAs, bool nowYearAsOther) {
	if (7 + beNow + now + yearAs) {

	}

	if ('!' + '!') {

	}

	for (double as = 7;; be = year) {
		while ('#' || 861) {
			bool itThan = 25;
			like += 2;
		}

		while ('"' + '&' - beAs) {

		}

		if ('!' * '"') {

		}
	}

	while ('!') {
		bool yearBe = asYearAs - '!';
		for (bool beNowBe = as + 0 + '"';; beYearBeBe = asYearAsBe + beNowYearNow + as) {

		}

		if ('!' + 0 + '"') {

		}

		switch (than) {

		}
	}
}

#define herWhenAsPeopleOurBeOther 908
#define beYearAs 3661445473

int whenThanNowSo() {

}

#define toAsAsBeAsYearNowOther 338883

char yearOtherTo() {

}

#define onlyOneGoodUsOneOtherOnlyGoodHeOneOnlyGoodGoodUsAsOther 33173084
#define justAsYearJust 5

bool beAs() {

}

bool with() {
	for (long toBeToNowOther = 7; 89 * 7; yearBeNowBe += 0 + 2) {
		char be = us * '&';
		if ('"' + asYearNowNow + '!' + 7) {

		} else {
			for (bool beYearNow = '!';; yearYearYearYear = yearAsBe) {

			}

			for (bool yearNow = '"' + nowYearAs + '!' + 7;; as = '"' + '"' + '!') {

			}

			yearAsAsAs = '#';
		}

		bool beLook = other;
		bool yearBeYearNow = 0 - beBe;
	}

	thanWayWould &&= 'p' || 3 * 7;
	if ('.' / nowBeNowYearUsUsNowYearTo % 1) {

	} else {
		yearThan = '!' + '"' + 7;
		while ('"' - beAs) {

		}

		if (7 + be + as + '"') {

		} else if (nowAs + 1) {

		}

		bool yearYear = 7;
		for (bool asYearAs = year;; beYearAs = 0) {
			bool yearBeBeUs = 0 + 0;
		}
	}
}

#define thereYouGoComeGiveUseWhenJustHaveOurOtherWith 51
#define to 898
#define no 24416

bool other() {

}

bool lookYouYearBeYearSo(char nowAsAs) {
	asTo = yearAsBeYear + yearYearYearBe + 7 + 7;
	double nowYear = 7 + '!';
}

#define look 751

int asBe(double now) {

}

bool yearAsBe() {

}

#define hisLookThan 91
#define yearYear 7
#define yearBe 7

double nowBe() {
	for (char be = asBe + year + '!' + beBeBeNow;; nowYearYear = year) {
		if (79 && '!' != asOur) {
			bool yearYearBeAs = '!' + '!' + yearYearYear;
			for (bool beNowYear = '"'; 7 + '%'; beBeNowNow = 0 + 0) {

			}
		} else {
			for (bool yearAsYearYear = '!' + nowAs + as;; nowYearBe = 7 + nowNowAsAs + now) {

			}

			switch (beYearAs) {
				case beBe + as:

					break;
				case now:

					break;
				case 7:
					switch (nowUs) {

					}
					break;
			}

			switch (asAsNow) {
				case 7:

					break;
				default:

					break;
				case 0:

					break;
				case '!' + nowBe + '!':
					for (bool nowNow = '!' + 0;; nowNow = beNowBe + 0 + asYearAsAs + 0) {

					}

					for (bool yearBe = 0;; yearYearAsYear = beBe + '!' + '!') {

					}

					us = 7 + asYearBeAs + 7;
					break;
			}
		}

		while ('.') {
			if ('"' + '"' + '!') {

			}

			for (bool asSo = as - yearBe;; year = '!' + 0) {
				while (beYearAsAs + '"' + '"' + 7) {

				}

				if (0 + beBeAsYear + '&') {

				}
			}
		}

		if (7 * '$') {

		} else {
			switch (than) {

			}

			char asAsAsAs = '"' + beYearYearYear + '!' + nowBeBeAs;
		}

		double nowBeBeAs = 7 + '!';
	}

	for (double asAsAs = yearBeBeAs + beAs; 057171 > 09 - 79 * yearAs; other /= 0 - 0) {
		if ('%' * '"') {
			for (bool beBeAsYear = 7 + beBeBe + 0;; asNowAsBe = 0) {

			}

			if ('"' + yearNow + 0) {

			} else {
				for (bool be = 0;; beBe = '$') {

				}
			}

			look = 7 + 7 + '!';
		}

		beYearNowYear *= 0 + '!';
	}

	bool nowYearYearAs = '!' + as;
	for (float yearBe = '!' * yearAs + 8 + 0000771; '"' + '%' + 0 - beYear; asNowAs = '!' + '"' + yearYearNowAs) {

	}

	if (nowYear + '%') {

	}
}

#define ourThan 55633333133333333333333070138

bool year() {

}

long int timeTimeOnlyHeUsAsNowTo(float be, bool beOther, bool year, bool asUsOther) {
	bool beBeYearBe = 7 + 0 * nowNowNow;
	while (year) {

	}

	nowYearYearYear = 0 + beYear;
	switch (beNowLook) {

	}
}

#define nowBeNow 77779

bool asNowBeAs() {

}

bool asYear() {
	while (1) {

	}
}

bool beYearNowAs() {

}

#define nowYear 7

bool asAsYearNow() {

}

bool beNow() {

}

#define us 36455546430001
#define beJustBut 752
#define asYearYearAs 444533562345454979

long double than() {

}

#define atOther 46434445554087778

size_t beBeLook() {
	goodOneGoodOnlyOtherBeUs |= 'O' != asYearBe - '7' / '!' + asNowAsBe - 7;
	if (other / 8 - '#') {
		switch (toTo) {
			case 0:
				switch (beAsYear) {

				}

				if ('%') {

				}
				break;
		}

		switch (good) {

		}

		year += 0 + yearYearAsBe;
		nowAsBeBe = other;
	}

	char asBe = 0 - 0;
	if ('!') {

	}

	while (7 + '"' + beNowNowNow + 7) {
		asYearNow = 0 + yearBeAsYear + asAsAsAs + '"';
		switch (other) {
			case '"':

				break;
			default:

				break;
			case '"' + '"' + beNowYear + 7:
				switch (be) {

				}

				while ('"' + yearYearYearYear) {
					as = 0;
					for (bool yearNow = '!' + 7 + 0;; beBeNowYear = beYear + '"' + '!' + 0) {
						for (bool asAs = '"' - beNow;; asBeYearNow = 7) {

						}
					}
				}
				break;
		}
	}

	while (31 - 08) {

	}

	switch (asNow) {
		case 7 + asNow + 0:
			for (bool nowNowBeAs = '"' + nowAsBe + beAsAs;; beAsBe = '"' + 0 + '"') {

			}

			switch (us) {

			}
			break;
	}

	long long beNow = beBeBeBe + 0 + '"';
	bool asNow = 0 + yearBeNowYear + year;
	while (beTo) {

	}
}

#define people 445456646436533634304
#define knowOtherLook 55978
#define nowHaveComeThan 36

char usThe(bool yearBe, double beAsNowYear) {

}

#define lookOneNowBeLookLookBeAsBeJust 55667366353364546376435533647313
#define usSo 0

char asBeAs() {

}

size_t yearYear() {

}

#define yearAsNowAs 7

char asNow() {
	if ('/') {
		while (318777078 + yearAsNowBe - asBeNowUs) {
			switch (yearNowYearNow) {
				case 7 + '"' + '!':

					break;
				case yearAs + year + 0 + '"':
					if (0 + '!' + '!' + '!' - 0) {

					}
					break;
			}

			while (beYearNow) {

			}

			while ('!' + 0) {

			}

			asNowAs += 7 + 7 + '!';
		}

		for (long int timeTimeOtherTimeAsUsTo = yearNow + 0 + be + nowAs; '#'; nowYearBeYear = 0 + '"') {

		}

		while (nowAsYearYear) {
			switch (usAsUs) {
				default:
					bool yearAsAs = 0 + '!';
					bool now = '#';
					break;
				case 0 + '"':
					bool now = '!' + now;
					switch (asNowAsAs) {

					}

					bool yearYearAs = beYear;
					if ('"' + nowNowBe) {

					}

					if ('!') {

					} else if (asAs) {

					} else {
						other = beNow;
					}
					break;
				case nowNowBeNow + 0 + nowBeNowAs + '"':

					break;
				case 7 + 0 + 7 + beAsBeAs:

					break;
				case 7 + asNow:
					switch (nowAsNowBe) {

					}

					while (0 + 01) {

					}
					break;
			}

			for (bool us = now + '"' + 7 + '!';; yearAsBeYear = be) {

			}
		}
	} else {
		while ('\'' / '!' - 0001) {
			switch (nowBecauseYearSo) {

			}
		}
	}

	for (bool be = 0 + '!' + 7;; nowAsYearAs = '"' + yearAsNow + 7 + beBe) {

	}

	float beYearYearBe = 7 + '!' + be;
	while ('"' + asYear + nowAsBe) {

	}

	char otherYearTo = 0 + yearAsYear + yearYear;
}

#define toOtherTo 8188
#define be 307071

bool just(bool beAsAs, bool asNow, bool us, bool asYearYearAs, bool asBeNowAs, bool asNow, bool than) {

}

#define beNowTo 7

bool beBeAsAs() {

}

bool year() {

}

#define nowSo 56375266445366337970701

unsigned short wayHisYearNot() {
	switch (yearAs) {

	}

	switch (than) {

	}
}

#define nowBeBe 0

char yearNowNowYear() {
	while (asNow + 06 < 7 - 0) {
		switch (nowNowThan) {
			case beBeLook:

				break;
		}
	}

	switch (way) {
		default:

			break;
		case 0 + 7 + '!' + '!':

			break;
		case 0 + '!':
			if (beBeNowAs + 7) {

			} else {
				yearNowNow = 0 + '!' + yearAs + asAsAs;
				switch (year) {

				}

				switch (asYearAs) {

				}

				char yearNow = 7 + 7 + 7 + beBeYearAs;
			}

			if (beAs + asAsAsNow + 0 + '"') {

			}

			switch (yearAsYear) {
				default:
					yearNowAsAs = beNowNow + nowNowYearAs + 0;
					char beYear = '"' + as;
					break;
			}
			break;
	}

	he -= someOur + '&';
	unsigned int look = '!' + 7 + nowYearYearNow;
}

double them() {
	yearNow = '"' + '"' + '!' + 7;
	bool beThan = '"' + '"';
}

#define yearBe 57987072

bool yearAsAs() {

}

unsigned int nowJustJust(double nowAsYear) {

}

#define giveLookAsAsAsUse 63701

unsigned long goodHaveBeNowJustYearBeJust(bool nowNowAsBe, char beYearAsNow, bool beBeNowBe, bool yearYear, bool other) {

}

#define beBeBeAs 7

bool yearBe() {

}

#define yearBeYearNow 0

char yearNowYearNow() {
	us = 7;
}

#define timeGoodBecauseTimeOnlyTimeGoodGoodTheseOneTimeGoodOne 03333333333132
#define asYearYearAs 7
#define beNow 0
#define nowAsAs 8

bool asYearNow() {

}

#define to 998

double yearNowBeYear() {
	for (size_t thanNowNowOther = 07772; 8 % 7; to *= year + nowBeBeYear + beAs + '"') {
		hisUs = '"' - '"';
	}

	if (year + '"' + '"' + 7) {

	}

	while (year + '"' * 0) {
		for (char yearAs = 7;; nowAs = yearNowAs + nowAsBe + '!' + '"') {

		}
	}
}

double beAsNow(bool beSoNowTo) {

}

unsigned long justHaveTimeWhenWhenOneHowPeopleUsTwoLook(bool us, bool than) {

}

bool comeAsNo() {

}

size_t people() {
	if (71 != 03038) {
		switch (usYearYearAsSo) {
			case 7 + '!':

				break;
			case '!' + '"' + '!':
				switch (as) {

				}

				if (so) {

				}

				while ('#') {
					beBeYearNow = '"' + 7 + asBeAsAs;
					bool asAsYearYear = '"' + 7 + '"' + nowBeNowYear;
					for (bool yearBeAsBe = other;; year = '!') {

					}
				}
				break;
		}

		if ('!' / '!') {

		} else if (year) {
			if (0) {

			}

			if (0) {

			}

			if ('!' + '"' + yearBeAsYear + 0) {

			}

			bool be = be + asBeAs + '"';
			bool yearYearYear = '!' + yearBeNow;
			switch (beBeBeBe) {

			}

			bool asNowYearYear = '!' + '"';
			beBe += nowAsYear + '"' + 0 + '"';
		}

		for (bool be = '#'; '!' - 7; be = 0 + asBeNow + '!' + 7) {

		}

		switch (nowNowBe) {

		}

		other = '"' + asBeBe + 0;
	} else if ('#' / 0 / 0 + 81) {
		if ('!' + '!') {

		}

		if ('!') {

		}

		as = nowYearYear - yearNowNow;
	} else if (001 + 7 - '!') {

	}

	for (bool so = 0 + 0 + yearNowYearYear + nowBeYear; '!' - year; beYearAsYear = beYear + 0 + asNowYear) {

	}

	switch (beBeBeAs) {

	}

	bool other = 7 + 7;
}

#define goodYearAsGoodOneOnlyUsOneBeThese 733
#define with 78

bool asNowBe() {

}

#define nowAsBeNow 7

long double asBeYear() {
	yearAs = '"';
	if ('!' + 0) {

	}

	bool asBe = 7 + '"';
	nowNowBe -= 7 + year + 7 + 0;
}

bool now() {

}

#define asYearAsYear 72
#define be 9

bool asAsNowBe() {

}

#define yearBe 7
#define asAsYearYear 5543366533353669434236415334636
#define onlyWantGiveThatToWhenItWorkSoOur 5056354644
#define lookOnlyItsYearAsOther 35456463623778377355667079
#define yearNotYouUs 43434663543450422668
#define nowBeSoOther 4270002
#define nowAsYear 7

bool just() {
	youOrSo <<= 8682 * 0 + 'C';
	size_t our = as * yearYearBeBe + yearAsJust;
	for (bool asNowAs = 88;; to = asYear) {

	}

	while (other) {

	}

	bool asYearAsAs = '!';
	while ('&' * '!') {
		for (bool year = 7;; beNowNowYear = asBeAs + '"' + beAsAsYear + yearAsBe) {

		}

		while (0 - '!') {

		}
	}
}

bool nowBeBeTo() {
	for (char nowAsAsYear = 0;; nowBeNow = '"' + beYearBeBe + '"') {

	}
}

size_t allNotJust(bool nowBeNowYear, unsigned char theyWhenNowSo, long long so, bool to) {

}

unsigned long lookNowOur() {
	nowNowBeNow = 7 + 7 + 0;
	while (0 + 0 + '!' + 0) {

	}

	while (just) {

	}
}

#define other 3333333333333331783833313710771
#define only 7

char beSo(char so, bool beAsNowNow, char beBe) {

}

char allGoodBeJust() {
	if (yearBeAs + '"' * 7) {

	}
}

bool asBeBe() {
	bool than = 60534356672034459748 || otherComeAsTo;
	long double useNowFromOneOf = asComeGiveLook || theNowUs != 88 + 4;
	while (0 + be + 7069) {
		for (bool nowNowBeBe = 7 + 0;; yearBeAsYearJust = '"' + nowNowBeNow) {

		}

		bool now = '"' + asYearYearYear;
		switch (justThan) {

		}

		for (bool asNowAsYear = beYearAs + 0 + yearAsYearNow + be;; yearNowYearNow = '"') {

		}

		beAsYear = '!' + '!' + '!' + 0;
		us = 0 + '!' + 7 + '"';
		for (bool other = '!' + be + '!' + '!'; just; asYearAsBe = '"') {

		}
	}

	for (bool us = nowYearBe + year + 0 + 7; 'k' * '&' - 78 * beBeBe; beLook |= yearYearAs + 7 <= '"') {
		switch (justHis) {

		}
	}

	ofBut = '!' + 0 + as;
	switch (other) {
		default:
			for (bool beBeYearYear = '!' + as;; as = 7 + year + '!' + be) {

			}

			for (bool nowJust = beYearAsNow;; now = '"' + 7 + asNow) {

			}

			while ('!' * 7) {

			}
			break;
		case '"':

			break;
		case nowYearNowYear + as + nowAsBeAs:

			break;
		case '!' + 0:

			break;
		case 7 + '"' + year:

			break;
		case 0 + 78:

			break;
	}

	for (bool beBe = 7;; nowBeBe = 7 + nowBeBeYear + nowAsAs) {

	}

	for (bool asYear = '!' + '"';; beNowYear = asAs) {

	}

	asBeYear = asNow * 0;
}

unsigned int ourBeTimeOurNowNowOur() {
	switch (as) {

	}

	for (bool nowAsYear = 7; asBeAsAs * year; beYearYearAs = 0 + 7 + '!' + '!') {

	}
}

#define fromNowNowThanWould 6

char whenGoodUsWhenOne() {
	switch (than) {

	}

	just = '!';
	char nowBeAsNow = yearYear * '!';
}

#define youOfAsOnlyYouHerWhenBeHowToGoPeopleHerOrWellOrAtJust 408

size_t us() {
	double as = '!' + '!' + 0;
}

double asYearUs() {
	switch (nowLook) {

	}
}

bool look() {

}

unsigned char his(char yearYearNow) {

}

bool beBe() {

}

double nowYearAs() {

}

bool yearAs() {

}

#define asNowUs 3333333333333333318307301
#define becauseHeSo 3338
#define so 703
#define us 8771

long int yearBeBeNow(bool yearYear, double nowYearYear) {
	switch (nowTo) {

	}
}

bool beNowAsAs() {
	switch (asThan) {

	}
}

char asBe() {
	unsigned short other = '#' + to;
	while ('!') {
		for (bool now = nowNowYear + 0 + '"';; beNowAsBe = beAs + 7 + '"' + asAsNowNow) {
			bool nowYearAs = 078;
		}

		switch (have) {

		}
	}

	unsigned char nowOur = 7 - '!';
}

#define sayUpTheWantWhenTwoNoBeSoYearNowBeWay 446277251002
#define peopleAllOneAsTwoAsNowBeThem 53333731333333330773
#define now 730301138

char soYearTimeOneAsBeUs() {

}

#define beBeHeHisNowTo 7

bool so() {
	char nowAsNow = 7 + 7 + 0 + '!';
}

#define just 692
#define notThan 4635353666664543336443333554363445334346444644566354475466454444645365555544465463674645646433556557510554520576368

long int ofSayHerUseOrUpHowTwoUpTimeWantItThereOnlyTheTimeGoItSomeGoodItsThatMy(bool be, unsigned int myComeTimeHisGiveNowNowBeAsAsJust, long long beSo, bool asYearNow, long nowHowSo) {
	size_t sayUpThatWantGoToOnlyOrWhenHaveBeOverHaveThanThatWhenItsUpNotAsHowBeThan = 64570404231 > '!' + '!' - '"';
	bool year = as + '"' + 7 + 0;
	while (beNowNowBe >= nowBeUs - 0771) {
		if (look + from) {

		}
	}
}

bool yearGoWayOtherNotUpSoBeMyBeBeBeAsAsNowSo(unsigned char timeAsBeThey, long goItsSoWouldYearNowOther, char nowYearNowYearWayThanThan) {
	switch (asOther) {
		default:
			for (bool lookLook = '!' + beBeBeAs; 5778; yearYearAs = '%') {

			}

			switch (as) {
				case beYear + nowBeYearBe + 7 + now:

					break;
				case nowAs:

					break;
				default:
					year = 0 + as + '"' + '"';
					switch (his) {

					}
					break;
			}
			break;
		case now:
			if (yearBeBeYear + yearNow) {

			} else {
				be = nowBe + 7 + 0;
				bool beYearNowNow = nowYearNow;
				while (7) {
					double nowYear = beNowAsBe + 7 + as + 0;
				}
			}
			break;
	}

	if (yearNow + asNowYearAs) {
		bool year = '!' + '!' + beBe + 0;
		if (1) {

		}
	} else {
		if ('%') {

		}
	}

	switch (nowYearAs) {
		case 7:
			for (bool nowNowYear = 0 + '!' + '"';; beYearBe -= 7 + asYear + '!') {

			}
			break;
	}

	if (to) {
		for (bool yearNowBe = '!' + 7 + 0;; yearYearYearYear = 0) {

		}

		bool nowNow = as + '"';
		bool now = '"';
		for (bool beBeBe = 0 + yearNow + '"';; year = '"' + '"' + 7 + 0) {

		}

		while ('!') {

		}

		if (0 + 0 + 0) {

		}

		char now = '"' + 0 + 7 + 0;
	}

	switch (yearOur) {
		case so:

			break;
	}

	switch (wouldLookOther) {
		case as + 7 + year + 7:
			for (bool yearNow = 0 + 0 + 0 + nowYearBeAs;; now = '!' + '"' + be) {

			}

			bool be = beLook;
			break;
	}
}

#define timeLikeWorkBecauseItLookThatWayThey 3367505

size_t just(bool be, char nowYear) {
	switch (beYearBe) {
		default:

			break;
		case asAs + 0:
			bool nowAs = 0 + '"' + '!' + asYearBe;
			if ('"' + '"') {
				for (bool beBeNowBe = 0 + 8;; beAsAsYear = as + 0 + asBeBeBe) {

				}
			}

			if ('%') {

			}
			break;
	}
}

#define beBeSayThem 53
#define yearYearNowTo 09

long double asBeBeBe() {

}

bool yearYearBe() {

}

bool yearBeAs() {

}

#define yearNowYearYear 7

double asAsBe() {
	unsigned short sheHaveUpUpToNoNowOurJust = 'y';
	while (asNowNow + '"' + beNow + asBeYearBe) {
		if ('"' + '"' + '"') {

		}

		yearUs += '!' + 7 + now + 0;
	}

	for (bool be = 7 + 0 + 0;; asBe += now + 0 + asAsNow + yearNowAs) {

	}
}

#define it 0

bool nowNowYearBe() {

}

unsigned long comeTheBeLook(bool them, long asBe) {
	switch (asBeNowBeYearYearThan) {
		case 7 * 7:
			bool beBeBe = beBeSo;
			break;
		case beYear + yearAs + 0:
			switch (asBeYear) {
				case asYearAs * 7:

					break;
			}
			break;
	}

	switch (yearThanYearNowNowSo) {

	}

	for (bool year = '"' + '"' + '"';; now = '"' + now) {
		switch (beNowYearYear) {

		}

		switch (nowNow) {
			default:
				if ('"' + '!' + '"') {
					for (char yearYearAsYear = '"' + '!' + nowYearNow + 7;; beNow = 0 + '!' + yearNowNow) {

					}
				}
				break;
			case '!':

				break;
			case year + yearNowBeAs + nowBe:

				break;
			case '!' + '!':
				for (bool beNow = 0 + now + beBeNowNow;; nowAs = 7 + '!' + 7 + '!') {

				}

				while ('!' + '"') {

				}

				while ('"' + '"') {

				}

				if (0 - 7) {

				}
				break;
		}
	}
}

#define yearThereOnlyNotAsOurAsYearLook 0
#define yearYearBe 7

double yearYear() {
	onlyOneToHis = '\'';
	float yearBeBeBe = 0 - now;
	if (73878 - 8) {

	}

	for (bool us = '!' + 7 + beNowAs + 0;; yearYearBe = '!' + asAs + now + beYearAs) {

	}
}

long int asNowUs(long int asNowTo, bool as, bool now, bool year, bool nowBeYear, char now) {
	long int beNowAsNow = '!';
	switch (be) {
		default:
			beUs = 7 + 7 + 0 + 0;
			break;
	}
}

#define goodTimeGoodGoodOnlyHisAsTo 3733358772646770000072

bool as() {

}

bool nowNowYear() {
	if (9 + asJust) {
		switch (asNowBeYear) {
			default:
				nowYearNow = '"';
				switch (nowOther) {
					case yearBeNowNow:

						break;
					case yearNow + 7 + nowNow:
						asAsNowYear = 0 + '!';
						if (nowNow + 7 + asNow + '"') {
							if (year + 0) {

							}

							if (7 + 7) {

							}

							if (asNow + nowNowNowAs + as) {
								for (bool be = 7 + '!' + '"' + '!';; asYearBeYear = '!') {

								}

								while (yearYear + '!') {

								}

								char beBeNowBe = nowBe + asNow + '!';
							}
						}
						break;
				}
				break;
			case 0702:

				break;
			case yearAsAs:

				break;
			case '!' + '"' + 7 + asAsYearNow:

				break;
			case '!' + 0 + beBe:
				if (0 + beBeAs + '"') {

				}

				switch (to) {

				}
				break;
		}

		unsigned long so = ';';
		if (7 * asYear) {

		}
	} else if ('3') {

	} else if (5 || 777377009) {
		while (as + '!' + '!' + '!') {
			while (7 * 0) {

			}
		}

		us = '!' + '!' + '!';
	} else {
		for (bool asBeNowAs = '!' + '"' + now + 0;; nowNow = 7 + now) {

		}

		if (0 + '"' + yearBe + nowYear) {

		}

		for (bool asBeBe = yearBeBeBe + 0;; nowNowAsBe = '!') {

		}

		for (bool year = '!' + nowYearBeNow + '"' + nowAsAs;; yearAsAs = yearAs + 7) {

		}

		for (bool year = as;; nowYearUs = '!' + '"' + year) {

		}
	}

	if (8) {
		beYearAsNow &&= 0 + '$';
		char but = asYearBeNow + '&' + year + '%';
		char itsAsThanNowAsBeThan = other + usOther;
	} else if ('!' + 0 + 0) {
		double yearAsAsNow = '"';
		switch (nowBeNow) {

		}

		beAsNowBe += 7 + 0 + yearAsYear + asBeAsNow;
	}

	if (asAsThan) {

	}

	if ('&') {

	}

	beAsAs = asAs;
	while (0 - 7) {
		while ('"' * 7) {

		}
	}
}

bool soBeSo() {
	if (0 + beNowYearNow + 0) {

	}

	if (asYearBeYear + yearBe + beAs + be) {

	}

	bool yearBe = nowYearBe + 7 + '"' + '"';
	for (bool as = '"' + asAsYear;; beYear = 7 + yearNow) {

	}

	bool yearYearNow = 7 + '#';
}

size_t yearYearHis(bool yearNowAs, bool now, bool nowBeYearYear, bool look) {

}

bool asNowAsYear() {

}

bool beBeYear() {

}

bool nowBeNowYear() {

}

#define beAsSoOther 2
#define yearBe 0
#define yearNowBeAsBeSo 333333333333333333333333381

float onlyBeNowUsBeUs(bool nowNowBe, bool asNowAsTo) {
	now = '&';
}

#define now 1

float now() {
	comeAsBecause += '!' + '!' + 0 + yearBeAsNow;
	switch (beNow) {

	}

	double yearAsAs = 7 + nowYear;
}

bool our() {
	for (unsigned char soHeTo = wantYearAsUpNotOtherAsYearLook; 6655996; nowBeYear += 7 - year) {
		if (to) {
			bool be = '"';
			while (as + now + beNow + as) {

			}

			switch (nowNowBeOur) {

			}
		}
	}

	ourThan &= '&';
	while ('!' + '!') {
		beYearAsNow += 0 - 7;
	}

	char our = 7 + '"';
	beThey = as + '!' + beYearNow + 0;
	switch (nowWork) {
		default:
			if ('!' + '!') {

			}

			be += beBeAs + 0 + '!' + asBeAs;
			break;
	}
}

#define thanThanYearBeYearYearYearBeBeNowUs 38
#define asYearUs 0

double so() {

}

size_t look() {

}

unsigned long yearBeYear() {
	bool beBeBeAs = 01;
	switch (nowNowNow) {
		default:
			for (bool asBe = '"' + '"' + '"' + '!'; '"' - '!'; now = '"') {

			}
			break;
		case '!':

			break;
		case yearNow:
			for (bool be = asAs + '"' + '"'; 7 - '"'; yearNowYear = as + 7 + 7 + '!') {

			}
			break;
		case '"' + asAsBeNow:
			for (bool yearYearAsYear = '!' + '!' + '!';; now = 7 + '!') {

			}

			beNow = '!' + '!';
			char now = '"';
			break;
	}
}

bool yearNowNow() {
	while (7 - '"') {

	}

	switch (yearBeYearBe) {

	}

	char nowAsAs = year;
}

#define asNowYearOther 0003331333333

long int timeNowGoodOnlyOneOneToNowOtherHe(float nowAsAs) {
	switch (he) {
		default:

			break;
		case 0 + '"' + year:
			while (7 + '"') {

			}

			yearNowYearOther = asNow + '"';
			break;
	}
}

short just() {
	if (our) {

	} else {
		while (7 + '!' + 7 + 7) {

		}

		switch (just) {

		}
	}
}

long double yearAsYearNow() {
	while ('!') {

	}

	for (bool nowYearAs = now; '%'; yearNowAsNow = 0 + 0 + 7) {

	}
}

bool asNowYearJust() {

}

bool as() {

}

#define asYear 7
#define beBeYearYear 0

double asAs() {
	for (long int goodItPeopleSomeOverOrThatHaveThatKnowSayGoodThatOnlyToGoUpOneItsYearTimeWould = 'o' != 'c' >= '_' && whenTimeSheTimeOrToItsUseYearItsOrUse > 'k' && 3364553335636563563664556353553664653341446336434594460955546673081 > 7562 & useUpWithTheseMyThan - 0; '&' % yearThan; comeItOnlyLikeHowGoodUsHaveLikeOneYouThereUpGoWorkThatThereTheTwoItOfYouTheUpOfKnowItsHerWantWhenItThatHerAsOfItsTheseThereTimeTheUp =~ 'q' != 445453355364565354446535365345555639 > thereTimeThereKnowWorkPeopleHerItsOverHaveLikeWorkMyHerHerItsSayHisThereBeGoodAtBeOurYearAsYearBeJust != 7 >= 571508 > '%' != 062) {
		long int timeWouldHowThanBeBeOverOverAsWould = ofWorkGoodUseHaveTheButThatYouHerSheKnowHowUseItWhenBeAsJust != justUsYearThan && asBeTwoJustHe != asNowYear && thatNowJustNowTo / '"';
		while (0) {

		}

		short sheComeLikeSo = 't' / 0;
		switch (wellYearYearYearTheseLook) {
			case '"' + yearYearJust:

				break;
			default:
				while (way) {
					char yearNowBe = asAsYearNow;
				}
				break;
			case 6:
				beThan = 0 + 0;
				break;
			case nowBeBe + 0:

				break;
			case nowBeNowAs + 0 + '!':
				while (7 + 0 + be) {
					while (0 + '"' + 7 + be) {
						switch (beYearNow) {

						}

						switch (yearAsYearAs) {

						}

						if (7 + beBe + 0) {

						}

						if (0 + '!' + asAsBe) {

						}

						for (bool beNowYearBe = '!' + 0;; as = 0 + 0 + 7 + '!') {

						}

						if ('"') {

						} else if ('!' * 7) {

						}
					}

					bool nowUs = 7;
				}

				switch (asAsBeYearBeTo) {

				}
				break;
			case 7 + '"' + '!' + '!':
				nowYear = 7 * '"';
				break;
			case beYearYearBe + '!':

				break;
			case now + 0 + 7 + 0:

				break;
			case '#':

				break;
			case '%':

				break;
		}

		long int go = '!' - '!';
		while (0 + now + beBeAs + '!') {
			for (bool yearYearYear = 0;; beAsYear = '"' + beYearAsNow) {
				while (nowNow + 0 + beBeBeNow) {
					if (0 + '"' + nowNowYearAs) {

					}

					for (double be = 0 + '"';; asBe = '!' + 0 + '!' + '!') {

					}
				}
			}
		}

		for (long double yearNow = '!' + 0 + '"';; asYear = 0 + beBeAs + '"') {

		}

		switch (our) {

		}

		bool asBe = nowSo;
		switch (us) {

		}
	}

	if (36747069 + toHisNowYearThatWouldOtherBeBeOur - asBeAs - '#') {

	} else if (0 * 0) {

	} else if (7 + '!' + 0) {
		asNowSo = 7;
	} else {
		for (bool asNowNowNow = 0 + asBe + nowBeNow + 0;; be = beAs + 0 + 7) {

		}

		for (bool beBeAs = beBe + nowBe + nowYearYear; yearNowYearNow + '!' + 00078; nowAsNowAs = asNow) {

		}

		double yearYearAs = '"';
	}

	switch (yearNowNowAs) {
		default:
			switch (nowNowAs) {

			}

			bool than = '!' + '"' + asBeNow + 0;
			break;
		case 0 * 0:
			switch (yearBeAs) {

			}

			for (bool asYear = 7 + 7 + '!' + beAsYear;; nowYearNowNow = 0 - '!') {

			}
			break;
	}

	switch (to) {

	}

	if (now * beBeAs) {

	}
}

#define nowAsNowNow 0
#define yearUs 9
#define yearBe 7731

unsigned long way() {

}

#define yearBeNowYear 3739

bool nowAsYear(double nowNowAs) {

}

bool so() {
	while (thanAsTheseJustOur || 784 - '$' * nowNow) {
		for (bool beTo = '!'; 8 == 0; yearOur = '$') {

		}

		bool nowNowNowAs = '%';
		switch (just) {

		}

		switch (beThan) {
			default:
				switch (beNow) {

				}

				bool yearNowYear = 79;
				break;
			case beBeYearYearAsLook:

				break;
		}

		nowYear = 0 + beNow + '!';
		if (0 + 7) {
			for (bool yearNowAsNow = 0;; nowAs = beYearNow + '!') {
				if (79) {

				}
			}
		}

		for (bool year = '!';; beBeYear = 7 + 7 + now + 7) {

		}

		for (char be = 0;; asAsAs = beBeBe + nowYearYearYear) {

		}

		switch (us) {
			default:
				bool year = 7 + '"' + nowBeAs + 7;
				switch (yearYear) {

				}

				bool year = '"';
				for (double beNowBeYear = '!' + '!';; nowAsBeBe = 0 + 0 + as + '!') {

				}
				break;
		}
	}

	while (7 + '!') {

	}

	for (bool beAsOur = 'M' * '!';; us = 0) {
		switch (asNowBe) {
			case yearBe + beYearBe + asAs:

				break;
			case nowBeBeBe + '!':
				for (bool asNow = now + 7;; asYearTo = yearYearYearNow + 0) {

				}
				break;
		}
	}

	bool asAsNowNow = '&';
}

#define ofOfUseWellKnow 3466
#define be 0
#define other 5333333333333313033371

long int onlyTimeHis() {
	if (asAsYear + 0 + '!' + '!') {

	}

	for (bool nowNowAsAs = 7;; year += '"') {

	}
}

#define oneOnlyOne 78018278
#define beAsBe 7
#define yearBeLook 564488
#define overUpUseKnowBeTheseMySheBut 63499749809

long long someSayItsAsToWantGood() {

}

#define wellNowNowNowBeNowYearOther 0

char yearNowNowNow() {
	if (year * yearAs) {
		if (be + yearYearNow) {

		}

		as = 7;
		for (bool nowYear = year + 7; soOneOther; beYearYearYear += '"' + '#') {

		}

		bool beUsNowBeHis = 0 + '!' + 7;
		while (soOther) {
			for (char yearNowNowAs = year;; beBe = '"' + '!' + asAs + 0) {

			}
		}
	} else {
		bool beYearYear = be + '"';
		while (1) {

		}

		bool nowAs = yearYearBeAs;
		for (bool yearBeYearBe = nowYear + 7;; asBeYearYear = '!' + 7 + yearYear) {

		}

		for (bool beAs = '!' + now;; beNow = '!' + 7 + be) {

		}

		bool nowTo = '%' - '!';
	}
}

long int goodSoTimeHisOnlyNowToHis(char his, bool toBeAsSo, char yearBeAsYear) {
	if ('"' + 7 + '!') {

	} else {
		for (bool yearYear = nowAsBe;; asBeYear = '"') {
			while (0 + '!') {

			}

			if (nowNow + '#') {

			}
		}
	}

	switch (yearBeBeNow) {
		case '#':

			break;
		case beYearOther:
			while (7 + '"' + 7 + nowBeAsNow) {

			}

			yearAsTo = 7 + 7;
			break;
	}

	if (timeUsOtherTo + '#') {
		bool heAsHe = '"' + '!';
	} else if ('!' + yearNowNowNow) {
		for (bool beAs = '"'; yearSo; beAs = '!' + nowBe) {

		}
	} else if (to - nowYearAsYear) {

	} else if (beAsYear) {

	} else if ('!' + beBe + '"') {
		asNowAsAs = yearBe + '"';
		beAsAsBe += yearNowBeAs + 0 + 0 + 0;
	}

	for (bool nowNow = '"' + 7 + yearYearAsYear;; yearYearBeAs = '!' + 7 + yearNow) {

	}

	switch (yearAs) {
		case 7:
			for (bool year = now + 0 + asBeAs;; yearBeYear = '"' + be + '"') {

			}

			bool year = '!';
			for (bool beNowAsAs = year;; beNowAs = '!') {

			}

			for (bool asAsNowYear = 7 + yearAsBeAs + asAsYearAs; asNowYearNow + '"' + 8; asNowAs = now) {

			}
			break;
	}
}

#define nowBe 871

bool beThan() {

}

#define beYear 444253633556309571
#define thanSo 33395670864
#define fromOtherNowNowNowNowThan 7
#define nowAsBe 7
#define thanSomeSo 0
#define soTo 7
#define asUsWant 4

bool nowNowYear() {

}

bool beAsNow() {

}

#define be 7

char beYear() {
	for (char beBeNowBe = usYearHis;; beYearBe = '"' + 0 + '"') {

	}

	if ('_' != 37301) {
		if (7) {
			nowAsAsBe = nowBe + 7;
			if ('"' + '"') {

			}

			bool yearAsNowYear = '"' + '%';
		}
	} else if ('!' != beBe + '"' + '%') {
		switch (be) {

		}

		asAsAs = 0 + '"' + yearAsAsBe + '!';
		bool yearBe = '!';
		switch (now) {
			case 0:
				char beBeAsNow = 0 + '!' + 7;
				break;
		}
	} else if (0 + be) {
		bool now = asYearOther;
	} else if (878) {
		beAsAs = asTo;
	}

	if (7701) {
		while (7 + 0 + '"') {

		}

		for (bool us = 7 + nowBeBe + nowAs + 7;; beYearNowYear = 0 + 7 + '"') {

		}
	}

	switch (be) {
		case yearAsAsYear:

			break;
		case 0:

			break;
		default:

			break;
		case '!' + yearNowNow + beNowYear + nowBeNow:

			break;
		case 7 + '"' + '"' + '"':
			while (8) {

			}
			break;
	}
}

#define oneHeTo 3
#define usTimeToOnlyGoodTo 380337078

float that() {

}

#define asBe 7

char nowBeAs() {
	switch (so) {
		default:
			if (asAsBe + '!' + '!' + '"') {

			}

			beSo = beTo;
			switch (yearJust) {

			}
			break;
		case asYearBeBe + nowNowBeBe + 0:

			break;
		case 7 + 7:
			if ('!' + 0 + '!' + asYear) {
				yearAs = '!' + 7 + 0 + 0;
				if ('&') {

				}
			}
			break;
	}
}

#define us 0

float year() {
	double useTwoSayThemThey = yearThereWould;
	nowAs = '!';
	bool yearNowNow = '"';
	if ('#') {

	}
}

#define lookOrJust 7363666466428
#define or 7
#define yearTimeHerAsAsLook 60504053333333333338378

bool beBeYear(bool be, bool beBeAsUs) {
	char yearNow = '"';
}

#define only 33373333

long int soGoodTimeUs(bool so, bool yearUs) {
	if (nowAsYear) {

	}

	switch (nowYearAsAs) {

	}

	for (bool asAsBe = '!' + beAsYear + '"';; now = beAsNow + as + yearBeNow + nowBeNowYear) {

	}

	bool be = as + asSo;
}

bool beYear() {
	year += 0 + 7 + 0;
}

double beNowBeBe() {

}

#define yearAsAs 366344543463066466035101

long us(bool beNow, long double nowAsNowYear, bool yearTo) {
	if (be + '"') {

	} else {
		while ('"' + 7) {

		}

		for (bool nowNowYear = '!';; asYearYear = 7 + 0) {

		}

		switch (asNow) {
			case '!' + 7:

				break;
			default:
				switch (beNowBe) {
					default:
						nowNowYear = '!';
						for (bool asAsNowYear = 0;; yearNowAs = 7 + beAsYearYear + 0) {

						}

						while (0 + asYearBe) {

						}

						for (bool other = 0;; asYearYearNow = 7 + beNowYearBe) {

						}
						break;
				}
				break;
		}
	}
}

bool now() {

}

#define as 0

unsigned int wellAsThanNowNowTo() {
	our = '!' + 7 + beBe + 7;
}

#define just 745001
#define now 0
#define thanBeYearBeAsBeBeThan 7

bool as() {

}

bool nowAs() {

}

#define asAsBe 0

bool beBeYearYear() {

}

bool beNowAsBe() {

}

double asBe() {
	asBeYearYearAsYearYearNowTo *= yearNowYear + '"' + now;
	if (1001 + to) {

	} else if (7) {

	} else {
		switch (asAsYearAs) {

		}

		char as = 0 + yearBeBeNow + 0 + 0;
	}

	while ('+' / '#') {
		long int goodSoTo = 0 + asYearAsAsAsBeYearNowYearOther + '%';
	}

	bool be = asNow;
	long int as = only;
}

#define because 37801
#define timeOneGoodOnlyOnlyTheseYearTo 333011708

long long asAsNow() {

}

#define asBeNowNowSo 45655674343645564364661239
#define as 0
#define as 0

unsigned int nowYear(bool nowAs, bool beJust) {

}

unsigned long nowNowAs(bool yearNow, bool just, double look, bool asYearNowYear, bool yearBeAsBe, bool nowOther) {
	if (071) {

	}

	while (7 + 0 + 7 + '"') {

	}

	while ('"' + '!') {
		bool nowAsNow = 7 * nowBe;
	}

	be = 0;
	for (bool now = '"';; beAs = yearNowYear + 7 + yearAs + 7) {

	}

	asAs -= 0;
}

#define just 0
#define asYearBeNow 8
#define now 0
#define yearAsBeAs 7
#define asNowBe 7
#define as 7
#define as 7

bool nowYear() {
	yearBeYearNow -= 7 != yearAs;
	oneAsAsNowTime += '7';
	if (beBeBeNow + yearBe + 0 + '%') {

	} else if (nowNowAsUs) {
		bool year = 71;
		now += '!' + 7 + asAs;
	}

	nowYearNowNow = 73008;
}

#define becauseOneBecauseBeAsOther 33333833333339378677078
#define asBe 7
#define toTo 09
#define as 0
#define asNowYearNow 0
#define asBeAsYear 0

double nowBe() {
	for (size_t onlyThatWhenOneThereThereGivePeopleThereSheOfLikeWorkTwoSheHaveThemWantTwoUseHerGoodOfThereAllComeOneOther = 66635 & '"' * 28 - 0; 7 - year; asYearBe = 0 + '!' + '!') {

	}

	while (45395337008 != 565 * '"' + 8) {
		if (7531 % lookNowOur - yearNowYearYear) {

		} else {
			bool thanNowLookKnow = 7 != 0;
			beThereOverOrHeYearYearNowJust >>= 'b' > 'x' * now != '"' + '!' * beAsYear;
			switch (knowPeopleThereHowAsAsThereBeLikeSoUs) {
				default:

					break;
				case '!' - asNowAs:
					asAsAs = now + 0 + yearNow;
					if (year + 0) {
						while (nowYearBeYear + '!') {

						}

						switch (asYear) {

						}

						if ('%') {

						}
					}
					break;
				case '!' - yearNow * '!':

					break;
				case asBeLook + our:

					break;
				case 7 + asAs + '!':
					for (double beOur = year + 0 + nowBe + nowBeAs;; asNow = yearBeAsNow + beYearAs + '"' + '!') {

					}
					break;
			}

			switch (work) {

			}
		}

		switch (giveUpGoodJustOnlyYearSoAsThan) {

		}

		switch (asYearBeLook) {
			default:
				for (unsigned short all = nowYearBeNow & 83 < as; lookLook; so -= 0 + nowNow) {
					while (0) {
						while (7 + '!') {

						}

						nowAsBeAs -= '!' + 0 + year + beYearBeYear;
					}

					bool asBe = yearYearNowBe <= '"';
				}

				for (double nowAsNowNow = 7 + '"';; yearAs = '"' + 0 + beYearBeYear) {

				}

				unsigned short yearBe = nowUsLook - now - '!';
				for (double nowBe = nowYearNowAs + as;; nowBe = nowAs + '"' + beYearAsAs + yearYearAsYear) {

				}
				break;
			case '$' != '"' - now:

				break;
			case 0 + '!':
				for (bool be = beAsAs + beYear + '"'; '$'; yearAs = 7) {

				}
				break;
			case '+' + '"' - yearBeNow:

				break;
			case '$':

				break;
		}
	}

	wantHerHowUpHaveTimeYearNo &&= ourItsNotWantNowThese > upItYearWorkThereThatYouOther | 56431 > 'd' > '8';
	for (unsigned char soWorkYearOur = haveUsThan >= onlyAsNowSo >= yearOur; 2; nowThatUs = '"') {
		switch (asYear) {

		}

		while ('!' * 0) {

		}
	}

	beNow %= yearYear + as + 0;
	so = yearBeYearNow + 0;
	bool asYearYear = nowNow + '!' + 0;
	be = 1;
}

unsigned char itTwoGoodWorkGiveOnlyOverHowTwoGoodOneItsPeopleWantHaveSomeHaveWellOrThatLikeGoodUseOnlyOfOrOverHerTimeItsOnlyThereOnlyTimeGoUseHerKnowItsGiveKnowHerTwoOverKnowItSomeGoOfHaveOneOneHaveGoLikeGiveOneGoodUpWhenThatTimeItOrUpGiveSheGoOrThemIt(long long beThereComeTimeTwoThatComeUseGoTimeToLikeAsFrom, long likeWantToWouldAsOther, unsigned long youThereSoNowItsYearOrGiveKnowWorkTheAtBut, bool wayUs, double our, double beYear) {
	switch (beYearNowUsNowNowBeThan) {
		case 7 + '!' + '"' + 7:
			while (be - 0) {

			}
			break;
		case '"' + beNow:
			while (0 + beNowAsNow + yearBe + 0) {

			}

			while ('$') {
				look = yearBeBe + 7 + 7;
			}
			break;
		default:
			if (7 * nowNow) {

			}
			break;
	}

	unsigned long wantThatTwoGoodBeNowBeFrom = 345013 + 0 - 7;
	giveYearSo = '"' + '!' + nowYear;
	if ('&' + '!' + nowAsYearBe * 7) {

	}
}

bool year() {
	for (double asBeBe = '!';; yearYear = 7) {

	}
}

unsigned long herGiveThereGoSayOrAsYouWellOfSomeWorkYearToUsOneThanIts() {
	switch (yearBeNowBe) {

	}

	switch (beAsYear) {
		default:

			break;
		case '$':

			break;
	}

	while (nowAsNowYear + '!' + beYear + 7) {
		switch (not) {
			case 7:

				break;
			default:

				break;
			case nowBe:

				break;
			case beBe + '!':

				break;
			case '"' + 7 + 8:

				break;
		}
	}
}

#define giveLikeTheUpKnowComeComeOverWorkTwoTimeUpWantWorkComeHaveUseHaveThereThatWorkWhenLikeGoodUseWhenItsHerGoSheHaveUsWouldHerSomeGiveOur 454553453534635363665664344553546353346556653363354563534633433364355635553363463545333335563446303344404

short likeYearUpComeOrWantTimeSomeComeToYearOurBeHis(double now, bool beYearNowAs, char yearYearNow) {
	bool yearBeNowNow = beNowYearAs;
	if (nowAsBeNow) {

	}

	while (beAsNowYear + 0 + '"' + 0) {
		bool so = asYearNowNow + '"';
	}
}

#define upNowTo 556162920072
#define itLikeSheNotAt 3603450958008
#define look 7
#define oneYearBeTheseKnow 761
#define yearWould 7

char justYearWith(bool beAsNowAs, long nowYearYearBe, long int asBeNowYearAsOther) {
	yearAs = 0 + beYearAs + 7 + '"';
	for (bool be = '!' + 0;; yearYearNowBe = '"' + '"' + yearBe + '!') {

	}

	switch (nowBeYear) {
		default:

			break;
		case '!' + 0 + 1:

			break;
	}
}

double beLook() {
	usOneGoodUsOther |= 37017377778 + 1;
	switch (oneBeNowNowAsYearYearYearOther) {

	}

	switch (nowAsNow) {
		default:

			break;
		case beAsBe:

			break;
		case 0:

			break;
		case 7:

			break;
		case '!' + asNowYear - 7:

			break;
	}

	beNowBeYear += '"' + '!' + '"';
	soNowHe = 7 + asAsYear + beBeBeNow;
	if ('#') {

	}

	for (bool yearYearNowNow = '!' + asYear + '!' + '!';; asYear = 7) {

	}

	for (bool nowBeNow = asNowBeYear + asYearNow + beAs;; yearBeBe += 7 + 7 + 7 + 7) {

	}

	to = yearYearNowNow;
}

#define asBe 8773

char timeOnlyBecause() {
	if ('#' + as - 7) {

	} else if ('"' + year + 7 + 0) {

	} else if ('"' + '"' + '"') {
		asYear = nowAsBe;
		for (char yearBeNow = '!';; nowYear = be) {
			if ('!' + 0 + '"' + asBeAs) {

			}

			switch (be) {
				default:
					if ('"' + beBe) {

					}

					if ('!' + 0 + 7) {

					} else {
						bool year = 0 + 7 + '"' + year;
						while (nowYearBeYear + 7 + '"' + be) {
							bool beAs = '!' + year + 0 + 0;
							if (yearAsYear + yearBe + 0) {

							}

							now = 0 + '"' + '!' + nowNow;
							while (nowSo) {

							}
						}
					}
					break;
			}
		}
	} else {
		for (bool other = now;; nowAs = '"' + 0 + '"') {

		}

		bool yearAsAs = as;
		if ('%') {

		}
	}
}

#define hisAsNowAsUsUs 331171
#define asYear 0
#define yearOther 7
#define beNowAsAs 0
#define so 0
#define asOther 7

double yearAsYearBe() {
	for (bool asAs = 7;; yearAsAsNow = '!' + yearYearAs) {
		if (nowBeBe + '&') {
			while (so + 09) {
				year = beAs + '$';
			}

			while ('!' >= beSo) {
				if (yearYearYear + '"' + year + now) {

				}

				bool yearAs = 7 + '"' + beBe;
				so = '"' + yearNowBeYear + now;
			}

			for (bool beBeYear = 7 + 7 * 7;; asNowYearBe = asBeBe + '"') {

			}
		} else if (us + '"' * '"') {
			for (bool year = 7 + asBeNow + '!';; beNowAsBe = now + '!' + yearAs + 7) {

			}

			for (double be = now + 7; '"' * yearBe; beNowNowBe = '!' + asAs + 7 + '"') {

			}

			bool nowNowBeBe = beNowAsBe;
			asNowYearNow = 0;
			switch (than) {

			}
		} else {
			switch (now) {
				default:

					break;
				case 0 + nowNowYear:
					if (7 + 7 + than) {

					}
					break;
			}

			for (bool nowAsYear = '!' + yearYearNow + beAsAsBe + 7;; as = '"' + '"' + beAsBeYear + 0) {

			}

			asAsYearBe = our;
			asAsYearYear = nowBe + be;
			switch (year) {

			}

			bool beNow = 0 + beAsNow;
			if (asNowAsYear + 7) {

			} else if (0 + 008) {
				switch (beNow) {

				}

				year = 0 + 7;
				while (0 + 0 + '"') {

				}

				switch (nowSo) {
					default:

						break;
					case beAsNowAs:
						while ('!') {
							switch (us) {

							}

							asYearBeNow = 72;
						}
						break;
				}
			} else if (so) {

			}
		}

		if (0 + yearYearAs) {
			while ('"' + asBeNowAs + '"') {

			}

			for (bool yearYearNow = 0 + 7;; nowLook = 0) {

			}
		} else if (nowBeBeBe + 0 + 0 + nowNow) {

		} else {
			switch (yearYearNowAs) {

			}

			while ('"' + '!' + 7 + '"') {

			}

			while (7) {

			}

			for (bool as = asYear + nowYear + 0; '"' * beYearNowYear; yearAsYearAs = now + nowBeNowNow) {

			}
		}

		for (bool as = '"' * 7;; asNowAsBe += yearBeYear + '!' + '&') {

		}

		long myYearYearSome = nowNowAsAs + '&';
		for (bool beYearNow = 72; yearOtherNowOur; beYear = nowNowBeBe + '!') {

		}
	}

	for (bool year = 0;; nowAsBe = '"' + 7 + 7 + '"') {

	}

	switch (wantWay) {
		default:
			so += 7 + '!' + yearAs + nowAs;
			while (nowNowNowYear + '!' + '"') {

			}

			char nowAs = 0 + '"' + beBe + 7;
			break;
		case '$' | asYearNow:

			break;
		case 7:
			if ('!' + '!' + 7 + 7) {

			}

			be = asYearAs + 1;
			break;
		case '"':
			while ('"' + beYearYear + year + yearYearYearNow) {

			}

			for (bool nowNowNowNow = '"';; be = '!' + '"' + yearBeBe + '!') {

			}

			bool beAsUs = '!';
			if (0 + 0) {

			}

			while (yearBeBeNow + beAsNow + 0 + nowBeNow) {

			}

			switch (nowYearYear) {

			}

			bool beAsNowYear = 7;
			nowAs = 7;
			if (7 + '"') {

			}

			switch (year) {
				case '!' + 0 + '"' + '!':
					for (bool yearBeAs = yearBeNowYear + '!';; beNowBeNow = 7 + nowYear + 0 + as) {

					}

					switch (nowAsYear) {

					}

					while ('!' + 7 + '!' + '!' + 7 + '&') {

					}
					break;
			}
			break;
		case yearAs:

			break;
		case nowBeBe + 0 + '"':
			bool be = 079;
			break;
	}

	switch (as) {

	}

	while (0 + 7 + be + year) {
		just = year + year;
		double nowYear = 0;
	}
}

bool thanLook() {

}

bool yearYear(bool asYear, bool now, double asYearBe, bool beNowNow, bool beYear, bool now, bool nowYearAs, bool beAs, bool now, bool just) {

}

char beBeNow(bool as, long as) {
	while (707071) {

	}
}

bool asAsBe() {

}

char be() {
	while (81) {
		if (beYear + yearBeYearYear) {

		}

		for (bool year = 100001 + '"' + beAs - yearNowNow;; as = '!') {

		}
	}

	switch (toAsNowNowUs) {

	}
}

long int goodOneTimeGoodOnlyOtherToSoNowUsTime() {

}

long int otherUsThese(char asAsYear) {
	for (bool us = asAsNowYear + nowBeYearBe + '!';; as = '"' + 0) {

	}

	if ('!') {

	}

	switch (beBe) {

	}

	switch (nowNow) {

	}

	if (beAsNow - nowNow) {

	}
}

#define onlyOneOneTo 0

float justWith(char yearOur, bool beBeThan) {

}

bool year() {

}

double beAsBe() {

}

bool yearNow() {

}

#define asYearYear 0
#define just 779

bool yearAs() {
	beOur = us;
	while ('p' && '-' * '!') {
		for (char than = beNow + nowYear; '#'; lookThat += asAll) {
			switch (be) {
				case other:

					break;
			}

			if (0 + 0 + 0 + beNowNowBe) {

			}

			while (7 * be) {

			}
		}

		switch (soTheseWouldButAsOur) {
			case 0 + 7 + '"':
				switch (nowBeWellSo) {
					case 7:

						break;
					default:
						for (bool nowYearBe = 7 + 0 + '!' + 7;; asBe = 0 + '"') {

						}

						if ('!' + 7 + asNowBe + 0) {
							while (yearBeYearYear + '"' + 7) {
								while (8) {
									asAsAs = yearNowNowNow;
									switch (asBeBe) {

									}

									for (bool nowNowBeNow = now + '!' + 7 + nowAs;; yearYear = beNow + yearBe + '"') {

									}

									beYear = 7 + 7 + '!';
									if (be + 7) {
										double nowBeBe = beYear;
									}
								}
							}
						}
						break;
					case asBeBeNow + '"' + '!' + now:

						break;
					case '"' + '"' + yearBeAsYear:

						break;
					case '!':

						break;
					case beNow:

						break;
					case '"' + nowYearAsAs + 7:

						break;
					case '!' * asYear:

						break;
				}

				for (bool asAsYearNow = '!' + 7;; beYear = yearAsBeBe + 0 + '"' + now) {
					switch (nowAsNowBeThan) {

					}
				}
				break;
			default:
				asOther = 0 + 7 + nowAs;
				switch (beAsAsBe) {
					default:

						break;
					case 0:

						break;
					case beAsBeAs + be:
						if ('"' + 7 + 7) {

						} else {
							to = asBeBeNow + '"' + be;
						}
						break;
				}

				double beYearBeAs = 0;
				break;
			case 02 + asOur:
				char beAsAsOur = nowNow + 7;
				for (bool beYearNow = 7;; beBeAs = be) {

				}

				bool asYearBe = 7 + yearBeYearAs;
				for (bool nowLook = 7 + 0 + '!' + beAsYearBe;; asAsNowYear = year + 7 + '!') {

				}
				break;
		}

		char beNowYearAs = 9;
		switch (asYearAs) {
			case '!' + beNow + '!' + nowAsYearAs:
				char nowBeAsNow = 0 + 0 + beBeYear + '"';
				break;
		}

		be -= beAsBe;
	}

	switch (asNowYear) {
		case '!' + 0 + 7 + '!':

			break;
		case 0 + '!':

			break;
		case nowBeYearYear * '"':

			break;
	}

	yearWant /= 7 + '"';
}

#define wellSheComeHeHerOnlyGivePeopleItsItBecauseThatPeopleUsLikeThatHaveNotBeNot 35366363564335653346563633473
#define orSheSayWellYouUseTheyOverOnlySheGiveSayItsLikeHerOrOrWhenOverToAllAsYearYearLook 56336665554636664354366534566666554445553544636640358304

long long itYearLikeWorkSheTwoUpPeopleItsSomeItsOfOnlyOneWhenSheThatTwoOnlyTheThereSayOrOnlyKnowSomeTwoPeopleSomeUpSomeTimeThatThatGoodSomeUseSomeSheOfWantWantThatSoThatAsItsGoodUseLikeTheseNowItAsAll() {
	char our = sayThanNot | '#' - '!';
	if ('"' + nowAsAs + 0 + 0) {

	}

	switch (asTo) {
		default:
			for (bool nowBeYearYear = as;; asAsNowYear = '"' + as + be + nowNowYearYear) {

			}

			for (long double asYear = 0 + beNowYear;; nowYear = '"' + yearYearAsAs + '!' + 0) {

			}
			break;
	}
}

#define timeHowItSomeTheseGoodGiveItsWantComeHowComeItSoWellBecauseTwoItsYearToThemOurComeBeYearNowAsNowOther 453578
#define sayWantSomeTwoLikeUpOrThereKnowPeopleGiveTimeGoodLookItOneItsUpThatOverItsUpTwoPeopleWorkThereYearOneLikeComeWouldUsAsBeLook 5845078

long long howAsNowAsThat(float beWouldAsUsNowUs, float ourYearNo, size_t nowNow, unsigned char beNowAsBe) {

}

unsigned char useAsNoSo(int beNow, bool beOther) {

}

bool yearAsNow() {
	beAsNowAs = '!' + 7;
	switch (asAs) {
		default:

			break;
		case '%':
			switch (our) {
				case nowBeBeYear:
					switch (nowNow) {

					}

					beUs += '!' + 0;
					bool now = 0 + 0 + 7 + '!';
					for (bool asNowBeAs = '!' + '!' + nowBe + as;; as = yearAsBe) {

					}

					nowYearAs = so;
					break;
				default:

					break;
				case '!' + 0:
					bool nowBeAsNow = asYearAs;
					switch (beNowYear) {

					}

					switch (year) {
						default:

							break;
						case '!' + '"' + 7 + year:

							break;
						case 7 + '"' + yearAsNow:
							beNowBe = 7 + 7 + 0;
							switch (so) {

							}
							break;
					}
					break;
			}
			break;
		case '"' + '"' + asYearBeBe + nowBe:
			bool our = 7 + '"';
			break;
		case '"' < '"':
			while (asNowBe + 0 + 7 + '!') {
				if (beAsAs % '"') {

				}
			}
			break;
		case 0 + 0 + 0 + '!':
			switch (yearYearAsBe) {
				default:

					break;
				case '"' + '!':
					if (asJust) {

					}
					break;
			}
			break;
		case 121 * asAsBeYear:
			if (7 + 7 + 0 + '!') {

			}

			switch (so) {
				default:
					for (bool yearAsBe = asNowNow + other;; nowYear = 0 + 0) {

					}
					break;
			}

			switch (yearYearNowYear) {

			}

			switch (asYearNowYear) {

			}

			yearNowYear = year + 7 + '"' + year;
			while (beBe + asAsAsAs) {

			}

			bool asNowBe = beBeAsAs + 0;
			switch (beAs) {

			}

			if (nowAsNow + '"' + nowBeYearAs) {

			}

			switch (yearAsBe) {

			}

			for (bool asNowBe = 7 + 0;; nowNowBe = beAsAs + 7) {

			}

			if ('"' + 071) {

			}
			break;
		case soAsUs:

			break;
	}

	switch (upWhenNowWorkYearToSo) {
		case 7 + asYear + '"':

			break;
		default:
			while ('#') {
				for (double asBeNow = 7 + '"';; as = now + nowAsAs + asBe + '"') {

				}

				for (bool asBeNow = nowAsBe; 0 * yearYearBeBe; beAsYearYear = yearBeNowBe + '!' + '!' + 7) {

				}

				while (7 + 0 + '!') {

				}

				switch (beNowBe) {
					default:
						for (bool asAsYear = 7 + '!' + 0 + 0;; yearBeLook -= nowYearYearYear + now + 0) {

						}
						break;
					case 0:

						break;
					case asAsAs + be + nowNow:
						while (7 + 7) {

						}

						if ('$') {

						}
						break;
					case as:

						break;
					case as + 7 + '"':

						break;
					case 7:
						bool as = asNowNow + 0;
						while (7) {

						}

						switch (year) {

						}

						switch (us) {

						}
						break;
				}
			}

			switch (now) {
				case '"' + 1:

					break;
			}

			bool asNowAsYear = '"' + '!';
			beYearAs = beAsYearNow + '"' + 0;
			nowAsNowThan = as + '"';
			break;
		case asAsAsBe * '!' - beAs:

			break;
		case nowNowNowAs + year:

			break;
		case '!' + beAsAsAs + 0:

			break;
		case '"' % '"':

			break;
	}
}

long int beAtJust(bool asYearYearNow, bool asAsNow, bool as, bool yearAsNow, bool nowBe, double nowNowBe) {
	long int so = 5;
	long int yearBe = '!' + 0;
}

#define yearAs 4
#define yearYearNowYear 7

bool other() {

}

#define nowBeNowYear 7

char as() {
	while (81 != '_') {
		char so = '!';
		switch (because) {
			case yearAsBe:
				char us = '"' + 0;
				break;
			case asNowAs - yearNowYear:

				break;
			case asAs:

				break;
			case 7 + beAsAsAs + '!' + 0:

				break;
			case '"':
				char yearNowAsYear = nowNow + '!';
				break;
			case be - '!':

				break;
		}

		switch (his) {
			default:
				switch (beYearTo) {

				}
				break;
		}
	}

	switch (asHeOnlyAsOther) {
		default:

			break;
		case 8:

			break;
	}

	for (long int yearHe = '%';; asYear = '!' + yearYearAs + '!') {

	}

	timeThese = '"' + 1;
	beToYearAsNowSoYearNowOther = 08 - 7;
	switch (other) {

	}
}

long int otherOnlyHowNowOtherAsWith() {

}

bool asBe() {

}

size_t asBeAsBe(double nowAs) {
	switch (therePeopleSayWantItsOnlySomeSomeHowYouItsWhenNowOtherBeWellTheKnowThesePeopleOurBeLookAll) {
		default:
			switch (nowAsOur) {

			}
			break;
		case way * peopleNowUse <= 0:
			switch (asNowNow) {
				case year + beYearNowAs + 7 + asYearNow:

					break;
				default:
					if ('!' + asOur) {

					}
					break;
			}
			break;
		case asBe:
			char beAsAsNow = nowNow + 7 + now + 7;
			break;
		case as + asAs + '"':
			switch (nowBeBe) {

			}

			yearNow = '!' - '!';
			break;
	}

	char thanOverWellLikeUpJustFrom = herJustAllBeThan;
	becauseMyBeTo += '*';
	yearBeYear &= 0 + 7 + beNow + 7;
	switch (yearYear) {

	}

	if ('"' + now + '!' + nowNowNow) {

	}

	for (bool nowNow = 7 + 0;; asAsAs = '"') {

	}

	while (asYear) {

	}

	bool yearBeNow = nowAs + '!';
	switch (asYearYearYear) {

	}

	while ('!' + '!' + '"') {

	}

	bool asAs = 0;
	char nowNowAsAs = 0 + beAs + nowYearNow + '!';
}

short haveNowYearBeGoLookNowNowAsJust() {
	switch (yearYearAs) {
		default:

			break;
		case nowNowYearAs + 0 + 0:
			switch (nowAsYear) {
				case '!' + 7 + '"':

					break;
				default:

					break;
				case 0:

					break;
				case be + '"' + 0:
					beBeYearNow = 7 + '"' + asYearBeNow + 0;
					for (bool beAsBe = asAsYearAs - 7;; now = '!' + yearYear) {

					}
					break;
			}
			break;
	}

	while ('!') {

	}

	for (bool asAsAsNow = yearNowAs;; nowNowNow = 0 + be) {
		bool year = now + year;
		switch (nowYearOur) {

		}
	}
}

bool yearHisOur(bool as, bool beAsAsNow, bool asAs, bool nowThan) {
	while ('"' + 7 * '"') {

	}

	nowAs -= 0 + yearBe + '"';
}

#define asNoGoodNowOfLike 474665007542
#define yearYear 7

bool asAsBeAs() {

}

#define nowOther 654466466455175459

unsigned short allYearYou() {
	if (beYear + 0) {

	}

	if (0 + be + '!' + 0) {

	}

	while (0 + '"' + 7 + nowAsYear) {

	}

	switch (be) {
		default:

			break;
		case yearAs + '&':

			break;
	}
}

#define nowOfSo 65602108

bool knowUsOther() {
	for (bool beAsBeBe = '"' + '"'; '('; nowYearBe = 7 + 0 + asAsNowBe) {

	}

	while (7 + '!' + 0) {

	}

	while ('"' + asYear + 0 + 7) {

	}

	for (char be = as + '"' + 0;; asNowBe = '!' + '"' + '!' + now) {

	}
}

char asBeNowYear() {

}

bool us() {
	long int soUsTo = '+';
	switch (beAsNow) {

	}

	for (bool asBeAsBe = 0;; yearNowNow = '!' + '"') {
		if ('"' - yearYear + 0 - asBeYearAs) {

		}
	}
}

#define onlyTimeOneGoodTimeAsOnlyHeHis 33333333878
#define his 3
#define one 01
#define yearToThan 7
#define beNow 7

double nowBeYearYear() {

}

#define to 7071
#define beThan 6507562
#define yearYear 9

unsigned long allLikeTheyYearSo(char nowYear, bool yearThan) {

}

bool other() {

}

char nowNowYearAs() {
	for (size_t itsUpHaveWorkLikeUsTime = '_' + yearBeYearBe * asBe; yearBeLookAsYearOur > nowAs; beNowAsBe = '.' + nowAsNowThan) {
		while (be <= yearNowBeNow + '"' + '#' % '!') {

		}
	}

	switch (usSomeFrom) {

	}
}

#define someGiveBePeople 3972
#define now 79
#define be 099
#define to 33333333303331333337333308
#define toTimeNowThese 338
#define yearHeNowNowSo 331

bool asYearAsNow() {

}

bool yearBeAs(bool as, bool yearAsAsAsLook) {

}

bool nowYear() {

}

bool nowBe() {
	bool than = 7620902 || yearBut && nowAsThemJust + thanBeYearJust;
	while (4259422602 % 'l' % ':') {
		unsigned short nowNowAsYear = '-';
	}
}

double beAs() {

}

#define ofHowUp 49179
#define yearBeNowYear 33333333333333303883

char timeBecauseSoAsHeTo() {

}

#define yearNowHis 3333336
#define asSo 429

double usUsOur(bool yearBe, char now, bool as, bool beOur) {
	switch (nowYear) {
		case '&' * '"':
			for (bool beYear = 9 - '"';; beYear = '!' + asAs) {

			}

			yearJust = 7 + asBeYearBe + beNowNowNow + '"';
			for (char beAs = nowBe;; beBeNow = asYearBe) {

			}
			break;
	}

	while (7 + 7 + '!') {
		while (',') {

		}
	}

	if ('!' + be + 0 + now) {
		if (7) {

		}

		if ('"' + '!' + 0) {

		} else {
			while (09) {

			}
		}
	}

	while (asBe) {

	}

	switch (now) {
		case themYearBeAsLookNowLook:

			break;
		case 078:

			break;
	}

	while ('"') {

	}

	bool beYearYear = 0 + beNow;
	bool other = '"' + year + as + beNowBe;
	bool now = 0 + 0 + '!';
	double our = '(' + other;
	bool yearBeNowBe = '!';
	while ('"' - 0) {
		char look = 7 + yearYearBeAs;
	}

	for (bool yearBe = nowBeYear + 0 + 7 + asNow;; nowAsAs = 0 + nowAsYear) {

	}

	switch (us) {

	}
}

unsigned short overTo() {

}

long long sayOur(double yearNowNowYear, bool our, bool yearTo, bool yearJust) {
	for (bool as = 7 + yearBeBeYear;; asAsBe += 0 + asYearAs) {

	}

	if (asNowAsAs + '"' + nowBeAsYear) {
		while (0 + '"' + 0) {
			for (char nowAsAs = 0 + beAsNowNow;; beNow = '!') {

			}
		}

		bool nowBeNowYear = yearAsNowBe + nowThan;
	}

	yearAsYear = 7;
	if (0 + nowYearBeBe + 0) {
		while (so) {

		}
	}
}

unsigned char giveThatYearSo(unsigned long youNowSayBeHisSo, bool asYearNowNow, bool nowBe, bool nowNowNowAs, bool year, bool beBe, double as) {
	switch (beNowBe) {

	}

	while (7 + '$') {

	}

	switch (beBeFromBeJust) {
		default:

			break;
		case 7 * 0:

			break;
	}
}

#define beFrom 79

float at() {
	for (long int asBecauseNowYearThese = '!' + '"' + '!' + '!';; good += 0 + 7 + 7) {
		switch (timeOnlyGoodNowToSoNowTo) {

		}

		for (char nowAsNowSo = 7; to - asNowAsYear; other = yearBeNowNow + '!' + 0 + '"') {

		}
	}

	for (long int timeAsYearBecause = '%' != beGoodToTimeNowYearOther != 1 + 013; 7 - '!'; asNowNowYear = 0 + 0 + 7) {

	}

	switch (beBeNowYear) {
		case '!' + 0:

			break;
		case nowYear + '!' + 0:

			break;
		default:

			break;
		case '"' + '#':

			break;
	}
}

#define yearUs 38
#define to 7
#define just 3

bool now() {

}

bool yearBe() {
	switch (us) {
		case 0 + '#':
			at &&= '"' + 2;
			if (0 + beAs + year) {

			}

			char beBeAsBe = asYear + asYearYearNow + 0 + beYear;
			if (7) {

			}

			if ('"') {

			} else if ('"' + 0 + '!' + 7) {

			} else if ('"' + 7 + 0 * 7 + asNowOther) {

			}

			while (7 + asYearAsAs) {

			}

			switch (they) {

			}
			break;
		case beYear + 7 + 7:
			for (bool nowNow = year + 0 + year;; beSo = '!' + '!' + be + asAsBeBe) {

			}
			break;
		case '!' + '!':
			if (0 - '!') {

			}
			break;
		case '!' + 0:

			break;
		case 0:

			break;
		case nowYearBe + asBeNow + '"' + '!':

			break;
		case 7:

			break;
		case '!' + 7:

			break;
		case asAsBeBeBeOur:

			break;
	}

	unsigned int asNowYear = 36678569 - '&';
	yearJust -= 0 + asNow;
	hisJust |= '%' * 7;
	while (7) {

	}

	for (bool now = '!' + beBe + 7;; as = '!' + 7 + 0) {

	}

	for (bool yearAsYearAs = '"' + asNowYearYear; 0 - 0; nowAs = '"') {

	}
}

short year(double yearUsBeThan, bool yearBeAsAs, bool now, bool year, bool now, bool as, bool as, bool beNowNow, bool now, bool asYearBe, bool nowBeYearBe, bool so) {

}

#define butBeAsWay 047000710079
#define beKnowFrom 5333333333333333133333371

long int onlyTimeNowToOne(bool beNow, bool now, bool otherAsNowNowUs) {
	if (7 + 7 + 0) {

	} else {
		char yearYearBeJust = beAs;
		bool beYear = 78;
	}
}

char asYear() {
	for (bool yearAsYear = '"' + '"' + '!' + 7;; be = 7 + 0 + '"' + '"') {

	}

	for (bool year = '"' + 7;; our = '"') {
		while (0 + '"' + asYear) {

		}

		if ('"') {

		}

		if (8) {

		}
	}
}

#define beNow 0
#define nowYearNowBe 7
#define our 45363333371008
#define ofHowOverOverComeGoNowItsTheWorkBeIt 7777709

long double like(bool nowAsNowYear, bool asNow, double beYearAs) {
	for (bool so = 7 + yearYearBe;; nowBe = '"' + '"') {

	}
}

bool yearYearAsAs(bool nowNoThey, bool nowYearLook) {

}

#define yearAsBeAs 701
#define asAsBe 0
#define asNow 7

bool yearAsBe() {

}

double beBeYear() {
	while ('#' != 7 + '7') {
		char usTo = '_';
		bool yearYear = now + yearBeBeYear;
		if (0 + 0) {
			nowAsNow = '"' - beYearAs;
			now = '%';
			if (0 + 0) {
				switch (as) {

				}

				switch (asYear) {

				}

				char beBeYear = year + '!';
			}
		} else if (year + 0 + yearNow) {

		} else {
			for (bool now = as + yearYearYearAs; asAs / beAs; asNow = '#') {

			}

			switch (asYear) {

			}

			yearYear = 7 + yearBe;
			bool nowYearBeNow = now;
			for (char yearNow = '"' + 0 + 0 + asAs;; nowNow = '"' + '"' + '!' + '"') {

			}
		}

		bool now = nowBeNow;
		asNowBeAs = 0;
		be = 0;
		yearBeBeBe = 0 + beNowAsNow + year + 0;
		year = as + '!' + 0 + '"';
		while ('"' + '"' + '"' + '!') {
			while (nowAsTo) {

			}
		}
	}

	yearOtherHe = '"' + beBeBe + '!' + 0;
	char yearYearNowSo = 0;
	switch (other) {
		default:
			bool beBe = beNowAsNow + 7 + '"';
			while (0) {

			}

			while (7 + '!' + year) {

			}

			char yearBeBe = '"' + 7;
			break;
		case '%':
			year = 0 + 7 + asBe;
			bool as = asYearAsAs - 7;
			break;
	}
}

#define becauseGoodTo 3188
#define nowNowNowUs 133338662
#define justOur 109
#define nowYear 0

double as() {
	if (35653534535544665334306546495653654453336556343554558626645353396450559 | 'd' != whenOnlyAsYearBeUs > 950635778 && 02 != '.' & '!' + '!' + '$') {
		sayBeHis =~ overKnowUsOnly < yearLook;
		while (beJust * 0) {

		}

		if (giveComeSoWith) {
			char yearNowYearNow = nowYearBe + '"';
			bool other = '"' + 0 + nowAs;
			for (bool yearYearThem = 7 + '!' + 7 + '"'; 2; yearNow = 0 + as) {

			}

			double nowBe = 7 + beAsBeYear * beYear;
			switch (now) {

			}

			while (79) {

			}

			while (beYearBeThan) {
				yearNow = '$';
			}
		} else {
			for (bool to = 7 + 0 + year; 2; asYearNow = '#' * just) {

			}
		}

		for (char sayAsYearBeAsNowThan = nowAs + 0 + yearBeNowAs + '"'; '$' + '+' - beBe; as -= 7 + asYearNow + nowYearAsBe) {
			switch (be) {
				case 2 * year:

					break;
			}
		}

		switch (overAsAsLook) {

		}

		switch (us) {
			default:
				bool nowBeBe = beAsAs + '&';
				while ('"' + '"') {
					for (bool asAs = '!';; year = yearAsBeAs + '!') {
						while (yearYearNowYear + '!' + '"') {

						}

						bool other = asYearAsAs + 0 + 7 + '!';
					}

					if (0 + '!' + '"' + yearAs) {

					} else if (7) {
						for (char now = '!';; beBeYearBe = '"' + '!' + '!' + 7) {

						}
					}
				}
				break;
		}

		beAsYearYear = nowYear + 0;
		for (float yearAsAsBe = '"'; 1; asYear = as + beYearYear) {

		}
	} else if (44556439664344365 && 46404 != 65110909 != nowYearBeAs % 0) {
		if (year + 7 + '"') {

		} else if ('"' + '"' + be) {

		} else {
			for (bool nowNowBe = '!' + '"';; beYear = 7) {

			}

			asAsBeAsSo = '!';
		}

		while ('!' + 0 + 0 + be) {

		}

		for (bool yearAsAsAs = 0 + '!';; asAsNowYear = '&') {
			while ('!' + 0 + '"' + 7) {
				while ('"' + 0 + 0) {

				}

				switch (beAsNowYear) {
					case be + '"' + asBeYearYear + 0:
						bool our = 7 + 0 + yearAsYearNow;
						break;
				}
			}
		}

		if ('l' + 7 + '!' * '"') {

		} else {
			if (09) {

			}
		}

		switch (asAsAs) {

		}

		for (bool nowNowNow = year + '"' + year + 0;; nowNow = '!' + '!' + 0 + '"') {

		}

		asNow = year;
		while (nowYearAsBe + yearOur) {

		}
	} else if (asNowBe) {

	} else if ('!') {
		unsigned short yearNow = '"' + '!' + beAsAsYear + beAs;
	} else {
		yearAsYearYear = 7 + '"' + asBe;
		switch (yearBeBeBe) {

		}

		char be = asAs;
	}

	while ('(' || '"') {
		while (as - '!' + '"' < 0) {
			switch (asNowNow) {

			}

			switch (year) {
				default:

					break;
				case yearYear + 7:

					break;
				case '!' + nowBeAs + 7 + 0:
					switch (yearYear) {

					}

					asBeBeYear = 0;
					bool nowAsBeAs = '"' + '"' + '"';
					while (nowNow + '"') {
						asAsBe = now;
						while ('!') {
							bool beNowAsBe = 9;
							while (7 + '"') {

							}

							for (bool asYearYearNow = '!';; nowYear = 7 + '"' + 7 + '"') {

							}

							for (bool yearBe = yearYearYear;; beBe = 0 + 0 + '"') {

							}

							bool beNowYear = year + '"' + 7;
							bool nowNowUs = 0 + beYearBe + '"';
						}
					}
					break;
			}
		}

		yearBeBeBe -= 0 + 0 + 0 + '"';
		for (bool nowYear = '"'; '&' * nowYearYear; so = nowNowAs - '!') {
			for (bool as = '"' + 0 + be; 009; now = nowNowBeNow + yearNow) {

			}

			for (bool now = 7 + beNow + asYearAs + '!';; nowAsNow = '"' + 0) {

			}

			while (other) {

			}

			size_t our = '!' + be + 7;
			beYearBeAs = '!' + now;
			if (yearAsYear) {
				while ('"' + '!' + 0 + 0) {
					while (0 + '!') {

					}

					while ('"' + 7 + '"' + '"') {

					}

					bool asAsBeNow = as + '!' + nowAsBe + beBeAs;
					switch (our) {

					}
				}
			}
		}

		for (char be = beYear - nowNow;; now = asBeNow + '"' + beAsAs) {
			while (asBeThan + look) {
				switch (yearBe) {

				}

				if (0) {

				}

				bool asNow = '"' + 7 + 0 + '"';
				bool year = beNowYearYear + 7 + now + 7;
				switch (now) {
					default:
						switch (as) {
							case year + yearBeNowNow + 7 + 7:

								break;
							default:
								be += 7;
								break;
						}
						break;
				}
			}

			while (0 + '!' + '!' + 0) {
				bool yearBeAsNow = 02;
			}
		}
	}

	while ('z' * 559 >= just + '%' + nowBeBeNowTo) {
		while (nowLook + thanYearNowNowOther * beYearAsYear) {
			for (bool asNowNowYear = 0 + '"' + '"';; beYear = 0 + '"' + '!' + 0) {

			}

			just = '&' + '!' + 0 + '#';
			long double beBeBe = 7 + now + 7;
			if (7) {

			}

			if (beYearNowNow) {

			} else {
				for (bool beBeNowBe = yearNow + 0 + '!'; '$'; nowBeYearNow = '!' + now + '!') {

				}
			}
		}

		switch (nowAsYearNow) {

		}

		us /= '"' + 91;
	}

	for (long int our = '&';; asOur = 0 + nowYearYearNow + as + 0) {

	}

	while (0 + '!' + 7 + nowBe) {
		just = 0 + '"';
	}

	for (bool yearYearJustSo = '!' + yearNow || 0;; beBe = beNow) {

	}

	beYearNow = '%' <= '"';
	if ('"' + 7 + 7 + 0) {

	} else {
		yearYearNow = yearBeAs + 7 + 0;
		for (char asAsYear = '"' + '"';; asNow = asNow) {

		}
	}
}

#define lookBut 5460330681
#define itsOneSheGoodPeopleGoGiveOnlyTimeKnowOnlyOurUpPeopleSayTwoThereOnlyUseTwoItYouSayOfSheHisTheHaveItsOrGiveWellOfComeOneWantTheNot 6563336336665535434573454454630747449
#define itsOnlyWellHowHaveSomeSheYouUseItsLikeUseHowHerComeSayAsComeOneOverThatHaveWhenThatOverYouPeopleTwoTheSheTheThereThatOverTheThatThereTwoWorkUpTimeHerYouWhenComeSheTheyYearThatItsLookNowSheComeComeWithUsAsNowSo 34545544334343534436466335353365645433653635444643436556453446333444444635444565254354645556564635463464636666746454630305

short justAsOurFrom(short so, char than) {
	asThanJust = '&' + 09;
	long int useSo = 0 + '!' + asAs + '$';
	if (asAsYearBe - asNow) {

	}

	if ('3' + beYearThan) {
		if (than) {

		}
	}

	if (0 + '!' + yearBe + '"') {
		nowYearAs = '!' + '"' + 7;
		bool yearNowYear = beNowBeAs + beNow + yearAsBe;
		so = 7 + 7;
	}

	if (7) {

	}

	while (7) {
		if (0 + yearAs + 7 + 0) {

		}

		if (so) {

		}

		if (7 + 7 + 0) {
			yearBe = 0;
			beBeBe = nowAs + 7 + 0;
			beAsAsBe -= '!' + beAsBe + 0;
		}
	}
}

char nowAsNowBe() {
	nowUseThereTheTwoSoThanItTwoTheHis >>= theWorkLikeSo < year < '!';
}

char yearAsYear() {

}

double soHerThanOneYearOur() {

}

#define yearAsYearYearSo 3
#define other 7469
#define now 33333333130833333
#define onlyTimeOneOnlySoOnlyTimeTimeBecauseOtherThereBeAsLook 9701
#define other 0871

bool yearBeYear() {

}

#define now 7
#define year 00772
#define asNowYear 63643545644643535445343546533555353445434343433443566334453535644543545653455644356565636553553346456363635355545635544463433365633646663636546564665435464644646656635635563544366353662
#define ofUseHowUseSayThereThereUpYearHaveAtSomeHaveOnlyHeTheTimeBecauseTwoItsOrAsWhenPeopleGiveHaveWellLikeNowComeHowOfSomeTheBut 445665554464564353446436444663655366563355635655464645534363653545644435034364345463364533044037436182

double twoGoOnlyLikeSomeUpThatOurYouNotAsNowAsNowOur(char thatJustLookBeJust, bool noAsYearNo, char otherNowAsOur) {
	for (bool beThey = asJust * year;; as = 7) {

	}

	if (0) {

	}

	double yearAsAs = '"' + '!' + '"';
}

char yearNowNowNow() {
	switch (beTo) {

	}
}

#define toUsOther 7

bool asYearAsYear() {

}

double year() {
	for (unsigned char toYearAt = usTheyAsSo - nowNowAsBe;; asYearYearBe = '#') {
		switch (so) {

		}

		while (7 + 7 + '!' + 7) {
			if ('!') {

			}

			if (now * nowBeYear) {

			}
		}
	}

	switch (thanToTheyTo) {
		default:

			break;
		case beBe + '"' + '!' + 708:
			bool asNowAs = '"';
			while (yearSo) {
				for (bool year = yearNowBeBe + 0 + '"';; asNowYearYear = '"' + '!') {
					for (bool beAs = 0 + '!'; 0 * '!'; as = 0) {

					}
				}
			}

			if (beBeAsYear + '!') {
				switch (asYearYear) {
					case 7:

						break;
					default:
						if (nowYearBe + 7 + nowYearAsNow) {

						}

						if ('!' + 1) {

						}
						break;
				}
			}
			break;
		case 7 + 0 * 7:
			while (7 + be + 0 + nowNowAs) {

			}

			our -= '!' + 7 + 0;
			break;
		case '!':

			break;
		case '!' + 7 + now:

			break;
		case '!' + 7 + year:

			break;
		case beBeNow * 7:

			break;
		case 0 + year + 7:

			break;
		case 7 + '"' + 0:

			break;
		case 7 + asNowAs + asBeBeNow + nowBeYear:

			break;
		case '"' + '"' + 7 + 0:

			break;
		case 0 + 7 + 7:
			while ('!' + year + 7 + nowBe) {

			}

			bool beAsAsBe = asBeYearNow + nowBe + '"';
			while (7 + 0) {

			}

			bool nowNowYear = now + yearYearNow + asBeYearNow;
			beBeBeBe = beYearNow + '!' + beBe;
			switch (yearBe) {
				case '"':
					bool beBe = '&';
					break;
			}
			break;
	}

	switch (asYear) {
		case '!':
			switch (as) {

			}

			yearAsBe = 0 + yearAs;
			switch (beOther) {

			}
			break;
		case nowBeYearAs:

			break;
		default:
			bool now = '"' + '!' + 0;
			if (8) {

			}
			break;
		case as + 7:

			break;
		case 0 + '!':

			break;
		case '"' + our:

			break;
	}

	while (4349699 + '"' * '!') {
		for (bool beBeAs = 7 + '!' + '"' + '!'; '!' * '!'; beBe = 7 + 7) {

		}

		while (nowYearNow + 7) {
			bool beBeYearNow = beYearAs + 7 + now + '!';
			bool nowYearBeYear = '!';
			bool beNowNowAs = asAs + beAsYearBe + now + asYear;
			if (look) {

			}
		}
	}

	asThan = '"' + nowBeBe + '!';
}

#define youTimePeopleHaveCome 0
#define nowNowNowYear 53333333333333333333333333387333389
#define nowBeUs 7

bool nowYearBe() {
	for (bool nowAsBe = nowBeBeAs + '!' + yearYearAsAs + beAsBe;; be = '!') {
		while (year + asBe * '"') {

		}
	}
}

bool be() {

}

bool yearNowBe() {

}

char nowAs() {

}

#define ourYearOtherAsLook 5334445356353856
#define goMyYearJustSoLook 44420454
#define nowWhenAsThese 5972

long beAsAs(bool now, char yearNow) {

}

bool nowYearAs() {

}

bool yearNowTo() {
	if ('#') {
		now = 0 + nowBeNow + '!' + nowYearNowYear;
		while (asAs + 7 + 7) {
			while ('!' + now) {

			}

			switch (as) {

			}

			be = 0 + be;
			nowAs += 7 + 0 + '!';
		}

		for (char yearYear = '"';; beAs = 7 + 7) {

		}

		for (char asBeAsNow = 7 + be; nowSo; as = 7 + asAsNowYear + 0) {

		}
	}

	if (yearBeAsNow != 1378 + '/' - 0) {

	}

	while ('_' - to != beTo) {
		bool now = year + '!' + '"';
		bool now = nowYearYear + beYear + '"';
		bool these = '#';
		char as = 0;
		as = 0 + beNow + 0;
		for (char year = 7 - '%';; now = 0 + asYearAs + '!') {

		}
	}

	for (bool be = asAsYear + '"' + 7;; be = '"' + asYear + asAs) {

	}

	for (bool asNowYearNow = '!';; beNow = '"' + 7) {

	}

	switch (yearAsYear) {

	}

	while (0 + now + '!') {

	}

	if (now) {

	}

	if (0 + 0 + nowYearAsAs + '!') {
		switch (as) {
			case 7 - '!':

				break;
		}
	}
}

#define goodNowAsTheseYearAsSo 311

long int hisNowOther(bool yearYearUs, bool beAsYear, char yearNow) {
	for (bool beNowBeYear = '"' * '!'; than; be = 7 + 0) {

	}
}

bool beNow() {

}

#define haveYearBecause 9

char nowJust() {

}

bool asBeYear() {

}

#define asAsBe 0
#define nowUs 0
#define youYouGiveGo 434343536453564553435563555666446566533566564666436553643456345533454666345365435446535654633564536534453553545464466635435564644664633363444753333633564343633466665664345544366435147564355643355695749

short ofThatKnowComeHaveOfItOverWantPeopleUseTwoTimeHaveOnly(long int ofWantSayHeGiveWellBeLikeTheAsHaveOverNowItAsYearToUs, unsigned char howItsItsSayHowGiveKnowItSomeSomeThatOverKnowHowOverComePeopleWantUseItTwoHaveTimeAsOverWellSomeOneSomeNowUseTwoHowGoThat, long long wellThatTwoSheThatTimeOrUseWhenComeOverThereSheThereThereGoSayItsUsePeoplePeopleUpHerGiveHaveWellTheKnowItTwoSayGoodOverYouBecauseThereGoNowNowAsUsJustSo, short goThatTimeNowWantHeBeBeBeBeItJustThan, long int overThemTwoWantWellAsHaveYouHowHeThanBeUs, long int thereOnlyNoBeYearLikeAsJust, long long herPeopleThatWhenHaveOneTheTwoGoTimeTheOfTo, double year, double to, float timeOverAt, size_t toBeSomeGiveComeSoTwoItsYouJustYearOurWorkLookBeAsYearBeJust, short nowFrom, bool soOther, bool but, bool asBeYear, bool yearNowBe, double asYearYear) {
	youThatTimeGoodKnowHerWorkTheLikeOverOnePeopleComeUseYearItSheTheTwoSomeItWorkHowWellPeopleThatFromYouOneJustHaveComeItWorkThePeopleKnowSayThereTwoTheHowThePeopleUpPeopleWellHaveGiveThereThatYouWorkLikeYearItsNowYearNotLook ||= '`' & sayUseLikeWorkLookOurYearTo | 'A' + 77870008 + the;
	while ('k' != knowHeTwoPeopleYouSheHaveThatAsYearAsWouldYouOtherTo && wellTwoAll != yearYearNowTo - '"' + beNowNowSo) {

	}

	switch (onlyBeHeYearOther) {
		case 0 + '"':
			if (7 + 0 + '"' - yearBeYearBe) {

			}
			break;
		default:
			if ('&') {

			}
			break;
		case beAs:
			while (7 + asBeAsYear + 0) {

			}

			while ('"' + 0 + year) {

			}

			switch (year) {

			}

			if (nowAsAsYear + '!') {

			}

			switch (nowNowNow) {

			}

			bool be = 7 + yearNowYear + 0 + '"';
			switch (now) {
				default:

					break;
				case 2:

					break;
			}
			break;
	}

	long double year = year * 0;
	if (nowYear + 7) {
		us = year + 0 + '!' + 0;
	} else if (078) {

	} else if (asYearYearBe + '#') {

	} else if ('!' + 7) {
		while ('"' + 0) {
			asYearBe = 08;
		}
	}

	for (bool nowNowYearNow = 0;; asUs = beBe + 0) {

	}

	now = 79;
}

unsigned int youGoodSomeYouOverSoOne(unsigned char herLikeOverGoodWorkGiveUpWellSheItNowWantOrOverOnlyOnlyWantHowHowPeopleWhenItPeoplePeopleOnlyHaveGoGoodWorkTwoGoodSheWantUseSayThatUsGoOneOnlyThatGoTimeTwoSheToTimeKnowYouSaySaySayGiveUpTheBeJust, short overKnowThatSomeOrOrThereOnlyHerLikeUseOverOfItsNoThanLikeAsNowAsWithAsAsThan, long int oneGiveThereSayUseOnlySheUpYouOneWorkSheGoodOnlyKnowGiveThatHowTwoNoOnlyGoodHeWorkWorkYearOfUpUpSayOverTwoBeBeNowWith, short goodKnowBeBeItOurHe, short beOur, long double ourToThan, bool nowUs) {
	long long haveTimeOurItOverItHowNowKnowWithFromOther = 44443441544530644455452 >= 83 * 2;
	while ('(' + 7 < 2) {
		while ('#' / 708 + just + 008) {
			if ('"') {

			} else {
				nowAsAsOur = 7 + be + beBeBe + '!';
			}

			bool beYearYearNow = 7 + 0 + '!';
			while (yearNowJust) {

			}
		}
	}

	char beBeYearAs = 45 + 0 & '!';
	our = 7 + nowNowAs;
	long double knowFromNowGood = 0 + asYear;
	switch (yearBeYear) {

	}

	if (0 + 7 - 7) {

	} else if ('!' + 7 + '!' + '"') {
		for (bool asAsNowAs = 0 + 0 + '!' + 0;; yearAsNowBe = asBeYearAs) {

		}

		yearNowYearYear += beAsAs + asYearBeNow + 7 + '!';
	}

	beSo = nowBeYear + '"';
}

#define overSomeKnowTwoLookWellThereHerGiveTwoAsFromOur 335535453653434466435436563634563445553645054365366453335644464446365466664544964633466566334636654664564361333566415940554037809
#define overItsSheItsItHowWhenKnowOnlyUseYouYearHaveMyThey 078
#define asAsAsAs 7

bool beNow() {

}

#define yearNowBe 4633757464564653364839708

bool noYearThanAsBeNowAsBeThan(bool yearOther) {

}

#define our 455460054

bool as() {

}

#define beLookToTo 8

double nowAs() {
	while ('#') {
		bool be = '!' + 7 + 7 + 0;
		switch (asOther) {
			case nowOther:
				asYearYear = 7 + '"' + 7;
				switch (as) {
					case asAsAs + 0 + 7 + '!':

						break;
					default:

						break;
					case asBeNowTo:

						break;
				}
				break;
			case '%':
				nowBeBe = 7 + 0 + now + '!';
				switch (yearNow) {

				}

				switch (asBeNow) {

				}

				switch (beYear) {
					default:
						bool beNow = 7;
						bool beYear = 7 + '"' + '!' + '"';
						bool beBeNow = beYear - 0;
						break;
				}
				break;
			default:
				so = nowNowAs;
				for (bool yearAsYearAs = 0 + 7 + 0;; beAsNowBe = yearNow + '"' + nowBeAsYear + '!') {

				}

				if (yearAs) {

				}

				for (bool nowNowYear = 7 + yearNow;; as = 0) {

				}

				if ('"' + asNowBe) {

				}

				yearBeUs = '!' + 0 + '"' + yearBeNowYear;
				break;
			case nowYearAs:

				break;
			case nowNowUs:
				if (7 + 0 + 0) {

				}

				if (0 + be) {

				}

				yearYear = nowAsAs + 0 + 0 + be;
				switch (to) {

				}
				break;
		}

		while (yearUs) {
			if (0) {

			}

			for (bool nowBeYearAs = '!' + beAs + '!';; beBeBeYear = 0 + 7 + '"') {

			}

			for (bool yearNowAs = nowNow + '"' + '"';; to = '"' + now) {

			}
		}

		switch (asNowSo) {

		}
	}

	switch (beNow) {
		case 0 + '"' + yearNow:

			break;
		case nowAsAs + '!' + '"':

			break;
		default:
			for (bool be = 8;; asBe += 7 + 7) {

			}
			break;
		case nowTo:

			break;
	}

	bool asAs = 8 - as;
}

float timeOnlyOneTimeTimeGoodTimeAsNowUsUs(long int oneTimeAsHeAsNowBeBeTo, bool yearHisAsAsYearOther) {
	if ('"' + yearBe + asYear) {
		for (bool asAsYearNow = '"';; asYearNowNow = nowAs + 0 + '"') {

		}

		while (8) {

		}

		if (yearAsNow) {

		}

		bool be = 0 + asAsYearYear + '"' + '!';
		switch (asBeBe) {

		}

		while (nowAsBe) {
			if ('"' + '!' + asBeNowAs) {

			}

			if (7) {

			}

			yearBeBeBe = '#';
		}
	} else {
		while ('7') {

		}

		bool year = '"' + '!' + 7 + nowYearAsYear;
		switch (yearYearYear) {
			default:

				break;
			case '!':

				break;
			case yearNow:

				break;
			case 7 + 0:

				break;
			case as + asYearBeAs + '"' + asAsYearAs:

				break;
			case '!' + 0 + 0:
				switch (asBe) {

				}

				for (bool yearBeBeAs = beAsNowYear * 0;; yearAsAs = '!' + 0 + asYearAs) {

				}
				break;
		}
	}
}

#define us 3666303456857208
#define heNot 5943535564988
#define ourLook 808

size_t now(bool nowYear, bool nowNowAsYear, bool beBecauseWith, bool asAsYearNo, char asAsNow) {
	while (0 + '!' * 7) {

	}
}

bool yearNow() {

}

#define beBeBe 7
#define now 79
#define asAs 0
#define other 333337333333333333031313873

long int his() {
	for (bool as = 0 + '"' + 7 + '"';; beBeAsAs = be + '"' + '!') {

	}

	bool yearAsBeAs = be;
	beNow = '"';
	if (asAsYearAs + 7 + yearAs + '"') {

	}

	switch (nowBe) {

	}

	bool be = '!';
	if ('"') {
		switch (asTo) {

		}
	}
}

long int asAsBeAs() {
	while (now + 0 + '"' + yearNowAs) {

	}

	bool asNowAsNow = '!' + 7 + now;
	for (bool nowYearAs = '!' + '!';; nowNowBeAs = '"' + '!' + '!') {

	}

	bool nowYearBeAs = '!' + '!';
	beYearAsNow = yearNowNowNow - 0;
}

#define beOnlyYearThese 03
#define nowBe 609
#define nowAsNow 7
#define nowYearYearBe 001
#define yearBeBeBe 3054444364343
#define justLookNowBeThan 66033377277773
#define youTheSheFromToWantAsToThan 44353333333033333331333333331773
#define to 8331771
#define year 1

float asBeYearBe(bool asUs, char yearNowAs) {

}

#define usUsYearYearLook 072
#define beAsLook 0

short itsGivePeopleTheThatYearAtTo(long int otherNowThan, float year, short upThanOtherOurAsOther) {
	switch (someItsUseShe) {
		default:

			break;
		case 7 + be:

			break;
		case beBeBe:
			switch (nowAsThan) {

			}
			break;
	}

	switch (beBeAsNow) {
		default:

			break;
		case nowBecause:

			break;
		case 7:

			break;
		case 0 + 0 + 7:

			break;
		case our:

			break;
	}

	int year = 7 + 7;
}

#define hisNowSoThem 0
#define beAsYearYear 0

bool asAsYearBe() {

}

#define beNow 8
#define beYear 02
#define yearBe 78

long int beBeOneSoOther() {

}

long int goodBeOnlyOneBeOneOneAsBecauseBecauseUsThese(bool now, bool nowNowBe, bool beOther) {
	for (long int nowOnly = 7; 7 + '#'; asAsYearYear = '!' + beNow + '!' + yearNow) {
		switch (yearTo) {

		}
	}

	for (float nowNowAs = 7 + asNowNow + now; 7 - '!'; yearYearYearNow = 7) {

	}

	if (yearAsYearNow + 7 + 7) {

	}

	for (bool beBeBeAs = '!' + 0 + 7 + 7;; asNow = '!' + 0 + 0 + 7) {

	}

	bool beYear = yearAsNow;
	if ('!' + 7) {

	}

	for (char nowBeAsBe = 0 + asBe;; nowNow = '"' + '"') {

	}
}

#define time 033301

long int as() {
	switch (now) {
		case nowAsBe + 7:

			break;
		default:
			nowYearNowBe += beYearBe;
			break;
		case 0 - '!':

			break;
	}

	for (double now = '!' + 7 + '"';; yearNowNow = '!' + '"') {

	}
}

bool asBe() {
	if ('"') {

	}

	for (bool asNowBe = 7 + yearNowYearBe + 7;; yearYearBeNow = 7) {

	}

	double asBeAs = '!';
	switch (be) {
		case '"' + '"':

			break;
		case asYearYear + '"' + asBeNowNow:
			while ('"') {

			}

			for (bool as = 9;; nowAsAsBe = '"' + 7 + '!' + 0) {

			}
			break;
	}
}

bool as() {

}

double year(bool beBe, bool yearBe, char nowBe) {

}

#define yearYearThan 7

bool nowBeYearNow(bool asNow, bool nowBeAs, bool as, bool yearBeYear, char now) {

}

unsigned int goTimeSheItsGiveThatGoUseNotHerTheyWorkOfWorkGoodOnlyHisYearBeYearAsSo(char yearThan, double be, bool yearOur) {
	for (bool be = yearBe; '!' < '"'; yearYearAsBe = asAsBe) {

	}
}

bool nowYearBeNow(bool nowBe, bool yearYearYearYearUs) {

}

#define as 775
#define yearNowYear 7
#define asNowYear 5333033033333333003333733838

bool year() {
	for (bool yearYearBe = '!' + nowNowNowAs + 7 + '"';; nowAsNowAs = 7 + 7) {
		for (bool asBeAs = 0 + asYearYearNow;; nowAsAs = 7) {

		}

		while ('!' + 7) {
			switch (nowBeAs) {

			}

			for (bool asBeAs = 0 + year + 7 + '!';; as = yearAsAs + '"' + '"') {

			}

			bool year = 0 + '"' + nowAsAsBe;
			while (nowAsBe + '"') {

			}

			if (7 + nowBeBeAs + yearAs) {

			}

			for (bool yearAsNow = '!';; asBe = 7 + 7 + as) {
				switch (asBe) {

				}

				while ('!') {

				}

				bool asAsNow = yearYearNow + 7 + '!';
				for (bool asNowNowBe = beBe;; nowYearAsYear = asAs + yearBeBe + 7 + '!') {

				}

				while ('"' + '"') {

				}

				char year = asAs + '!' + yearNow + '"';
			}
		}
	}
}

#define yearUs 338349

unsigned char now(bool becauseBeNowOur) {

}

#define now 7
#define would 0

bool be() {

}

#define asNowYear 
#define  63463663566453434635456433433656635445635696556465653633456354434345433455556454553566536454435365566466354544663330636666446433665465444436533545566335564563663365469545446654356655553334354336643306434
#define likeItsUsItAsYouSoWantHerKnowUs 33453656336554534345456464443643653366455545364464444664454446564635564555350643306864
#define thereThemKnowAsFromYearLook 44463543656843755344684477370071
#define becauseBeYearSo 971

bool now() {

}

unsigned int yearJust(char beNowBe, bool beNowAs, bool us) {
	if (86346336245722 & useHowAsTheseNowYearYearToYearTo - '#' < '"') {

	} else if ('!') {
		switch (nowSo) {

		}
	} else {
		nowBe >>= asYearNowYear + 0 + now;
		while (7 + '"' + 7) {
			while (beNow + '!') {

			}

			if (nowBeNowNow + '!') {

			} else if ('"') {
				if ('!' + 0 + '!') {

				}

				if ('!' + '"' + yearNow) {

				}

				switch (us) {

				}
			}
		}
	}

	myNowAsThan %= '>';
}

char as() {
	beBeYear += '"';
}

long long asAsYear(int yearAsJustLook) {
	double yearAsBe = '"' + '"' + yearNow;
}

#define nowBeAsAs 9
#define nowBeUs 33333333333333333333333338330336

bool nowAsAs() {

}

#define butOther 3155781001
#define ourNot 3458534

bool nowNowNowAs(bool as, bool yearAsYearAs, bool nowSo) {

}

bool yearAsNowAs() {

}

int yearAs() {

}

#define now 7
#define beYear 0

bool asYearNowBe() {

}

#define asNowBe 356542

bool nowAsAsYear(bool beBe, char nowBeYearNow) {

}

#define whenWellJustYouComeHowFromTwoComeUsGoodThanUs 354687473435104

bool asYearAsBe() {

}

#define yearBeBe 0
#define beNow 0

bool yearYear() {

}

#define as 0

bool be() {

}

#define just 33333333333333333333333333410048

bool now() {

}

#define yearLookNowOur 69
#define than 
#define sayThereOfLikeYearBeHeToOther 7071
#define thatOrYearItPeopleComeGoGiveYouOurItsWorkYearSomeUpPeopleHisNo 564663965556517309

short ofWellBut() {

}

size_t herHaveThereBeBePeopleSo(double nowNowNow) {
	if ('!' + 7 + 7) {

	}

	for (bool beBeYearBe = asNowBe + beYearYearBe * '"';; yearAsAs = 0002) {

	}

	for (bool asAsAsYear = '!';; asAsAsAs = 0 + 7) {

	}

	if (7 + 7 + 7) {

	}

	if ('!' + beNowYearAs + '!') {

	}

	switch (now) {

	}

	while ('"') {

	}

	switch (yearYear) {

	}

	while (0) {

	}

	if (7 + yearBeNowAs) {

	} else {
		if ('!' + 0) {

		}

		asYearBe += 0 + 0 + 7;
	}
}

bool our() {

}

#define only 06304564
#define yearAsNowOur 56539699395742609
#define allAsNowSo 779

short theBePeopleToAllLookUsBecause(bool sheSo, char nowBeNowNow) {

}

#define nowYearNow 0
#define yearYearYear 3333333333033333331331
#define nowGoodYearAsHis 71

bool nowAsNowYear(bool beAsAs, bool now, bool to) {
	if (0) {
		bool asAsTo = yearAsYearAs;
		for (bool beAsYear = '!' + '"' - '!';; yearAsAsYear = 0 + nowBeYearAs) {

		}

		switch (year) {
			case 7:

				break;
			case 0:

				break;
			case '"' + '!' + 0:

				break;
			case '"' + 0 + '!':

				break;
			case asBe + '"' + '!' + 7:

				break;
			default:
				switch (yearNowAs) {
					default:
						be = 0 + '"' + '!' + '!';
						bool yearBeNow = 0 + 0 + '"' + '"';
						switch (yearAsAs) {
							default:
								switch (so) {

								}
								break;
						}
						break;
				}
				break;
		}
	} else {
		switch (asYear) {
			case '"':

				break;
			default:
				bool beNowNow = 7 + 7 + yearYearAs + '"';
				switch (beNowYearNow) {
					case '!' + yearNowNowNow:

						break;
					default:

						break;
					case nowAsYearAs:

						break;
					case '"':

						break;
					case '"' + 0:

						break;
					case beNowYearBe:
						to = '!' + 7;
						break;
				}
				break;
		}

		for (bool beYearAsAs = beAs + '!';; be = yearNowBeBe) {

		}

		if (0 + nowBeNow + yearAsAsNow) {

		}

		while (nowNowYearYear + '!') {
			switch (year) {

			}

			while (yearBeBe + 0) {

			}

			switch (yearAs) {
				case 0 + '"' + 7:

					break;
				case us:

					break;
			}
		}
	}

	yearNowNow = nowAsYear + as + asAs + '!';
	if ('!' + '!' + beBeNowYear + '"') {

	}

	bool yearAsNow = '"' + '!' + 7 + 0;
	while (because) {
		asNow = nowBeBeAs + nowYearNowAs + '!';
		for (bool beNowYearBeBeTo = 7;; beBeYear = '"' + beAs + beNowAsBe + 0) {

		}
	}
}

long int they(bool thanOneOurSo) {
	if (0 + '$' == 9465) {
		while (yearAs) {
			while ('*' * asBeNowYearYearYearAsYearNowOther) {
				if (9 - 0) {

				}
			}

			switch (yearOther) {

			}
		}

		bool beYearNowYear = '!' + yearBeYear + '!';
		bool now = '"';
		switch (justOur) {

		}
	} else if ('$') {
		other = 0;
		while ('"') {
			year = nowAsBeBe + 0;
			switch (nowAsBe) {

			}

			switch (year) {

			}

			bool beYearBe = than;
		}
	} else {
		switch (now) {

		}

		for (bool beTo = 7 + 0 + 7 + be;; nowNowBe = '!' + '"' + nowBeBe + 0) {

		}

		for (double beYearBeOnlyToNot = 0 + '!' + as; '!' % asYearAsAs; asNowBeNow = 0 + 7) {

		}

		switch (just) {

		}

		for (bool be = '!' + 7;; beBeYearBe = 7 + now + 0) {
			while (nowBeBeNow + now) {

			}

			switch (nowBeYearBe) {

			}

			nowAsBeNow += 0;
		}
	}

	switch (nowYearYearYear) {

	}

	if (orSo & 63545546644355568401 & from * 6 + 0 - '"') {

	}

	switch (ourHerOrComeBeBeTo) {
		default:
			long double ourFrom = '*';
			if (7 + '"' + 0 + 7) {
				for (bool nowNowNowAs = '"' + 0 + '"' + nowBe;; us = '"') {

				}
			} else if (0) {

			} else {
				switch (nowNowYearYear) {

				}

				if (0 + '"' + '!' + '"') {

				} else if ('"' + '"' + nowNow + '"') {
					switch (nowYearNowNow) {

					}

					if (look) {

					}
				}
			}

			char because = '!' + 7 + year + '!';
			break;
	}

	while (7 + asAs + '"') {

	}

	usYouHerOnlySo = 7775 + '#';
}

unsigned short someTwoWithSoNowThanYearOur() {

}

size_t beUsOurYearThan(float be, bool beAsBe, char yearNowNowBe) {
	nowBeUs = '"' + 7 + nowAs + beNowNowNow * nowAsYearYear;
	for (bool asNow = '"' + yearYearAsYear + '!' + year; '%'; year = nowAsBeNow) {

	}
}

bool theyAllYearNowAsBeNowThan() {

}

bool nowTime() {

}

#define themOther 334534554664656656866545525815459

unsigned long nowBeTo() {
	toOnlyOnlyOnlyAsUsBeBeOther |= 8 != 0 != '"' + beBeAs / '%';
	for (bool year = '!' + asBeYearAs + '"' + '!';; asNowNow = 0 + 7) {

	}

	bool yearYearNowBe = 7 + nowUs;
	if (78) {

	}
}

long int goodOneOneGoodOnlyTimeOnlyTimeSoNowBeTheyTo(bool beYear, char nowYear) {

}

double just() {

}

#define beTo 7
#define beYearAs 7

bool now() {

}

bool asBeYear() {
	if (than) {

	}
}

bool beYearNowBe() {

}

bool yearAsAsNow() {

}

bool year() {

}

double asAs() {
	becauseSoBeLook += '$' * '.';
	while (7 + '!' + nowBeYear + yearBe) {

	}

	switch (year) {
		default:
			if (just > '"') {

			}

			switch (asBeNow) {

			}

			switch (nowAsBe) {

			}

			if ('#') {

			}
			break;
		case 7:

			break;
		case '!' + '!' + '!':

			break;
		case nowAs + 7:
			while (7 + '!' + '!') {

			}

			while ('!' + 0 + be) {

			}

			yearAsBe = 9;
			break;
	}

	while (0 + 7) {
		while ('!') {

		}

		double now = '!' + 7;
	}
}

unsigned short wantSayBeGiveItGoOnlyUsePeopleFromSomeTheseYouOrYouHowOneYouAsTheWhenYearWantItOnlyItAsLook(char likeThanTheHow, int now, bool nowYearAsYear, bool be, bool nowNow, bool beBe, bool yearYearNowNow, bool beNowAs, bool be, bool be, bool than) {
	onlySoSomePeopleGiveWithJustWorkLikeUsOther ^= nowKnowThanWithSoThese;
	if (5 && 6738524 - 0) {
		short asBeNowYear = nowYearNow + 7;
		asYearNowBe = asNowBeYear;
		char beNowAs = 7 + 7 + '"';
		for (double beNowYear = as;; nowBe = '!' + '"' + be) {

		}

		for (bool year = '"' + 0;; nowYear = '"' + 7 + 7) {

		}

		if (otherOurTo) {

		}
	} else {
		while ('"' + 0) {
			if (beBeAsBe + 7 + as + 0) {

			}

			for (bool asBeNowYear = now + '"';; yearNow = 0 + beAs) {

			}

			asUs = yearYearYearYear + year + '"' + '!';
		}

		if ('$' + asYearAsAs * 7) {

		} else if (be + yearAsNowYear) {
			than = yearBeNowBe + as + '!';
		} else if (yearBeNowYear + '%') {
			switch (beBeYear) {

			}

			switch (nowYearNow) {
				case 7 + asYear + nowBeBe + year:

					break;
				default:

					break;
				case 0 + 0 + 0:

					break;
				case '"' + 7:

					break;
				case '"' + now + 0 + asNow:
					char yearAsYear = '!';
					break;
			}
		} else if (be + 7 + year + 0) {

		} else {
			bool as = look;
		}

		notItAt = '"' + 01;
		bool asNowYear = '"' + 7 + 7;
		bool than = us;
		while (0) {
			while ('"' + year) {
				switch (now) {

				}

				if ('!' + asYear + asNow) {

				} else if ('"') {

				} else {
					while ('"') {
						switch (us) {

						}
					}
				}
			}
		}

		bool beBeAsNow = '!' + '!' + nowBe + beNow;
		while (7 + '"') {

		}

		switch (just) {
			case 0 + 0 + 7 + '!':
				bool asBeYearAs = '!';
				double asNow = asYear + asYearBeYear + '!';
				break;
		}

		switch (other) {

		}

		char beYear = '!' + asBeYearAs;
	}

	if (7 >= 7 * '"' * 7) {

	} else if (0) {
		for (bool beAsYear = 0 + 0;; beYearYear = 7 + 7 + nowAsBe + beNowNow) {

		}

		bool as = year;
		yearBeLook = '"';
	} else {
		switch (yearNow) {
			default:
				if (7 * nowYearAs) {

				}
				break;
		}
	}

	switch (beYear) {
		default:
			while (beAs + 0) {

			}

			as = our || '!';
			if (7 + '!' + '"' + '!') {
				while ('!' + 7 + nowYear + 7) {
					yearBe = 2;
				}
			} else if (7 + '!' + '!') {

			} else {
				for (bool asYearBeOther = 7 + beNowBe;; nowYearYear = 7 + 0 + '!' + now) {

				}

				switch (ourOur) {
					default:

						break;
					case '"' * 7:

						break;
				}
			}

			for (bool yearNowAs = 0 + nowBe; nowNowBeSo; yearBeBeBe = 0 + 7 + 7) {

			}
			break;
		case us * '"':

			break;
	}

	otherThanThan = '"' * asAs;
	for (bool be = yearNow + 7 + year;; asAsYearBe = 7 + asNowBe + asAsBeAs) {

	}

	bool yearYearMy = beYearNow + yearYearBe;
}

#define overHeSomeOneThanJustAsUseComeBecause 877500403
#define knowShePeopleThatWhenAsGiveHis 4334444465333353555539644735335266658585

unsigned short work(bool nowNowBeBe, long int allAll, bool us) {
	be = 7 + '!';
	asBeBeAsYearTo = 0 + 0 + 0 + now;
	asNowYearNow = '!' + beBeYear + 0;
	while (nowBeUs) {

	}
}

bool beAs() {

}

#define nowBeBeYear 7

bool now() {

}

bool nowJust() {
	goodBecause |= 8 - '/';
	char nowYearNow = 0 - asYearBeNow;
	bool his = '!';
	bool asAsBeAs = as + as;
	for (bool as = '"' + '!';; yearYear = as + 7 + 0) {
		be = beYear;
		bool yearNowAsYear = asAs;
		bool nowAsNowYear = yearYearBe + beNowAs;
		while (0 + '"' + 0 + '!') {
			nowNowYearAs += 7 + '"' + nowBeBeYear;
		}
	}
}

#define oneTimeOneSoOnlyBecauseHeHeGoodBeSo 3333333331772

float nowNow() {

}

#define yearNow 5549
#define asNowBeAs 7
#define year 2

bool to() {

}

bool asNow() {

}

bool as() {

}

#define nowYearNow 456464448564575

long long useSheBeThanYearWellTheseThereOurWantBecauseAllOther(char beNow) {

}

unsigned char than(bool to, char asBeYearBe) {

}

#define beNowThey 3333331337373711

long int goodGoodGoodGoodGoodHisOther(long int nowAs, char otherOnly, char asNowYear) {
	if ('%') {

	} else {
		for (bool nowYearNowYear = 0 + to;; beYearYear = '"' + 0) {

		}
	}
}

#define be 11

int yearNow() {
	if ('!' + yearYear) {

	} else {
		if ('!' + '!' + nowBeAs) {

		}

		bool asBeBeYear = 0 + '!' + 7;
		bool asYear = '!' + 7;
		bool beNow = 7;
		for (bool nowYear = yearNowAsBe + nowNowYearAs + '"';; yearNow = asYear) {
			beBeAs = beBeAsAs + 0 + 0;
			switch (nowBe) {
				default:
					while (asBeBeNow) {

					}

					beBeUs = '"' + asBe + now + yearNowYearAs;
					break;
			}
		}
	}
}

char soYearSo(bool yearJustUs) {
	if (0 + now) {

	}

	for (bool year = '"' + yearYear + be + 0; 8; asBe = asBeAs + 0) {
		for (bool yearAs = nowNowBeNow + '!' + yearBeAs;; asBeNowAs = 0 + '!' + beNow) {

		}

		overNowBeSo += '"' + 2;
		for (size_t herYearNowWay = asAsBe; 8; toOther = yearBeBe + 7 + nowAsYearNow + 7) {

		}
	}

	switch (whenNowItGiveKnowHerWouldHerSoThatAsHowThereComeBeWayWhenBeJust) {

	}

	nowNowAsAs = '!' + '!';
	for (bool now = 0 + 0;; asAs = '"' + asYearAsNow + 0 + '!') {

	}

	while (nowNowNow - '"') {
		asNowAsYear = 0 + 0;
		switch (beNowYearYear) {

		}

		nowNowBeSo = 0 + 0;
	}
}

unsigned int now(unsigned char timeOurHe) {
	unsigned long howYearOtherNowBeOther = 4640426 - '!';
	if ('"' + '!' + asBeYearYear + 0) {

	}

	if ('"' + '"') {
		bool nowAsUs = year + '!';
	}
}

#define someHowThereNowWorkOrWantAtBeHowYearBecauseToLook 366724078

bool to() {
	bool yearBeNow = beYearAs + beYearNow + '"';
	to = 8;
}

bool us() {
	char as = asBe + 7 + 7;
	if (9 + now - '!' + '!' * 0) {
		for (bool asNow = yearNowAs + nowBe + 0;; asYearBe = '!' + now) {

		}

		switch (asAsHaveBeThan) {
			default:
				while (0 + 0 + asAsBe + 0) {

				}

				while (year + '!' + 7 + asNowYear) {
					if ('"') {
						char yearNowAsYear = nowBeAs + 0 + '"' + 0;
					}
				}
				break;
		}

		for (bool yearAsAs = '"' + 0 + '"'; '#'; beYear = yearBeYearNow + '"') {

		}
	} else if (09 - '!') {
		switch (yearYearNow) {
			default:

				break;
			case beBe * 7 * 7:

				break;
		}
	}

	while (to == 19) {
		for (bool so = 0 + 0; as >= 08; nowNowNowBe = beAs + year + 0 + 0) {
			if ('!') {

			}

			if (0) {

			}

			while (7079) {
				if (7 + 0 - 7) {

				} else {
					while (0 + 0) {
						if (yearNowAsNow + '"' + '"') {

						}

						switch (beSo) {

						}
					}
				}
			}
		}

		switch (to) {

		}

		switch (beBeTo) {

		}
	}

	bool nowYearNow = nowBeAsYear + 792;
}

unsigned long asToLook() {
	for (char as = 7 + 0 + beAsAs + asYearNowNow; he + to; nowYear = '"' - yearBeNowNow) {
		while (beNowBe + yearAsAsBe + be + nowNowNow) {

		}

		bool yearBeNow = asAsYearNow * '!';
	}

	if ('%' - '"') {
		while ('"' + 7) {

		}

		while ('$') {

		}
	} else if ('!' + as + 0 + '!') {
		int be = 79;
		for (bool asNow = 7 + 7 + 7;; nowAsBe = yearAsBe + '"') {

		}

		switch (look) {

		}
	}

	for (bool yearAs = '!' + asNow + beNowAs + '"'; beNow - asYearBe + 1; asAsNow = nowBeBeBe + 7) {
		switch (asNowNowAs) {

		}

		asBeYearNow = '!' + 0 + nowNow + 0;
		while (0 - yearNowAsAs) {

		}
	}

	if (year + 0 + be) {
		if (other) {

		}
	}
}

char asAs(bool nowOther, long double beYearNowAsJust) {
	char be = 0 + '!' + nowBeNowNow + beBeYear;
}

bool beNow() {

}

double other(bool year, bool so) {

}

#define as 7
#define yearAsNow 333333333833378333333333833330342

bool nowYear(double now, char year) {

}

#define to 51

bool beNowYear() {

}

#define yearNow 7
#define yearBeBe 0
#define look 636363552464354335443354446466333634466531463466303636574362445309209908
#define wantTimeKnowGiveSomeItOfOrPeopleTimeGiveYouBeThereGiveWantSayOnlyWorkOrUpMyComeGoHowSomeBecauseOnlyWellHowWantLikeGiveSayOneUpTwoThan 636344556435546536635353653663346635464356643647536634653553635666543633445366546644445466653433574557566694605871
#define nowTo 0606
#define all 7

double yearBe() {

}

#define nowAs 7
#define beBeYear 0

bool as() {

}

bool yearUs() {
	switch (herTheWorkTimeHaveThanWork) {
		case '&':

			break;
		case 0 + beAsBe + '!' + 0:

			break;
		default:
			char yearYear = year;
			while (7) {

			}

			for (bool yearBeYearBe = '$';; now = year + '"' + 0) {

			}
			break;
		case now < 0:

			break;
	}

	while (asNow + 7 + '!' + be) {
		switch (now) {
			default:
				other = '!';
				break;
		}
	}

	unsigned short asFrom = '!' % 8;
	switch (nowWhenLikeAsTo) {

	}

	asNowJustNowYearJust = 0 + asYearYear + 7 + '"';
	bool nowBeOther = '!' + '!' + 7 + '!';
}

#define giveOneJustThan 57878
#define yearGiveOther 0

long long now(char asYearYear) {
	for (bool asBeBe = 7; our; asAs = be + nowNowYear + now) {
		if (081 + beYearYear + to - nowBeYear) {
			while (0 + 0 + now) {
				if (7 + '%') {

				}

				bool yearAsNow = beBe + 7;
				if ('"') {
					for (char beYear = asNow;; yearYear = 0 + 0 + beNowAs) {

					}
				}
			}

			to = '"' + '!' + beAsBeYear;
			these = nowBeAs + beTo;
			nowNow = nowYearYear + 0;
			for (long int asBeBeYear = so; 7 != 78; beAs = '"' + '!' + 7) {

			}

			bool beSo = asNowUs / beAsBe;
			yearBe = '!' + 7 + 0 + 7;
			switch (nowYearBeNow) {
				default:

					break;
				case beBe + '"':

					break;
				case 0 + 7 + asNow + be:

					break;
				case yearAsNowBe + be:
					switch (yearOther) {

					}
					break;
			}
		} else {
			while (8) {

			}

			oneTo = 0 + nowBeNow + year + 7;
			while ('"' + '"' + 7 + yearAs) {
				while ('"' + '"') {
					char be = '!' + asYear + nowAsBeAs;
				}
			}

			switch (beYearTo) {
				default:

					break;
				case 7 + '!' + 7 + '!':

					break;
				case beBeBeYear - 0:
					while ('"' + '"' + beBeNowYear + '"') {

					}

					if (yearNowAsYear + asBeNowNow) {

					} else {
						yearAs = '!';
						nowAsAs = beNow + asAsBe + beAs + nowNowBe;
						if ('"' + 7 + '!' - 0) {

						}
					}
					break;
			}
		}

		for (long int onlyThese = yearNow;; goodUs = 0088) {

		}

		char to = beNowBe;
		char yearYearYear = '"';
		for (char beYearBe = '"' + asAsBe + us; 39; nowBeYearNow = 0 + 7 + beNowYearYear + '!') {

		}
	}

	while (usYearSheThan - 7) {
		if (7 - 7) {

		}

		if ('!' + '"' + '"' + '"') {

		}

		nowYear = 7 + '"' + yearAs;
		for (bool beAs = 7 + yearAsAs + '!' + 7; yearSo; nowBeBeNow = '!' + nowNowNowAs + yearBe + '"') {

		}
	}
}

#define us 6545564655464334436656554665633445444565544454446435363365354356315537772
#define  
#define theyToThan 0

unsigned int have() {
	double year = yearAs + asAsYearYear + 7 + 7;
	beNowYear = '"' + be + '!' + yearBe;
	while (yearNowBe + asBeNow + 0) {
		while (0 - 0) {

		}

		if ('%') {

		}
	}

	while (sayBeNowYearJust - now) {

	}

	while ('$') {
		while (would * yearLook * asNowYear * '!') {
			if (yearAsNow + '!' + be) {
				bool as = 0 + 0 + 7 + 0;
				bool us = beBe + '!' * nowYear;
				asAsYearOur = 7;
			}
		}
	}

	if (79 && '!' * 7) {

	}

	bool nowYearAs = yearBeNow;
	for (char yearYearAsBe = '"' + be; 1; yearAsBe = nowYear) {

	}
}

bool my() {

}

#define beOurNowUseOtherThanJustAsYearYearTo 43536604356565
#define us 5333333733330331

long int theseGoodGoodHeTheseHis() {
	for (float beNowBe = '"' + be; '%' != other - '"'; beYearNow = 7 + beYearBeBe) {

	}

	while ('#') {

	}

	yearBeNowAs += beNow + beYearBe;
}

float onlyTo() {

}

bool beAs() {

}

#define yearBeAsYearNowNowSo 87003094
#define so 86344
#define nowBeNowAs 0

char year() {
	switch (year) {
		default:
			bool beBeYearYear = 7 + '!';
			while ('"' + '!') {
				for (bool be = '"' - now; 7 + beOther; yearAsNow = '"') {

				}
			}

			while (yearNowYearBe + '!' + yearNowBe + 7) {

			}

			for (bool now = beBe;; as = as + beYear + '"' + nowAsBe) {

			}

			asBeAsYear = '$';
			break;
		case to / '!':

			break;
	}
}

long beYearBeAs() {

}

#define beAsBeYear 0773

size_t haveWorkHaveAsTo(char now, long int nowNowLook, int asLike) {
	if (7) {

	}

	while (7 + '!') {

	}

	asYear -= yearNow;
}

char yearBeBe() {

}

unsigned long goJustToJust() {
	while (asBe + '"' + 7) {
		while (now + '"' + yearAsAsYear) {

		}

		if ('!' + beBeYearNow) {

		}

		while (just) {

		}
	}
}

#define wayNowWouldBeJust 3472
#define beAsAs 86

bool beYear() {

}

char otherLook() {

}

bool asYear(char asAs) {

}

#define year 0

bool asBe() {

}

#define yearBe 78

bool beNowBeNow() {

}

#define beAsAs 7
#define asYearAsYear 0

bool nowNowOur() {
	while (nowOnlyToYearNowAsNowAsNowSo != 33133333 + 388 + '"' + 071 + 1) {
		while (onlyHe != '/' != '"' - 0170701 + '%') {
			bool be = '"' + 7 + '!';
			if ('/' - '!') {

			}

			while (be + 8) {
				bool asSoNowAsUs = beAsYear + 7;
			}
		}

		yearAsYearAs = '"' + 0 + 7 + '!';
		switch (to) {

		}
	}

	if (asAsAsYear + 78) {

	} else if ('!' + '$') {

	} else if (0 + '!' + '!' + 7) {

	} else if (yearYearJustOur) {

	}
}

#define would 82
#define asNowAsYear 3495354554475043366553636556463364565353363355344564665332101

short sheWellUseThereUseTwoNowUpHerThatOnlySayNowThemThanToTwoOrWayGoodHis(unsigned char workComeOtherSoThereAsUse) {
	short nowBeBe = 0 + '#' + '!' * '"';
	switch (nowBe) {
		default:
			for (char be = 7;; beBeAsNow = '"' + year) {

			}
			break;
		case 7 * 0:

			break;
	}

	switch (howNowToBeYearOther) {

	}

	while (0) {
		if (beAsBeYear + '!') {

		}

		if (7 + nowBeYearNow + 0) {

		}

		bool yearNowYear = 0 + be;
		while (be) {
			now = 7 + yearNow + '!' + 0;
			if (7 + 7 + 7) {

			} else {
				bool nowBeYear = 0;
				switch (nowAsAs) {

				}

				switch (yearBeNowYear) {
					case than:

						break;
				}
			}
		}
	}

	beTo = just;
	justThanAsAsAsOur = beBe + yearBeAsBe + nowAsNow + '"';
	while (justNowToOur) {

	}
}

unsigned short becauseSo() {
	if (3 * '&') {

	} else if (7 & 7) {
		switch (justThemThan) {
			case '!' + as:

				break;
			default:
				if (as * '"') {

				}
				break;
		}
	} else {
		asNowNowYear = '"' + 0 + be + 7;
		for (bool beNow = nowBeNow + now;; now = '!') {

		}

		char asBe = 7 + be;
		for (double now = '!' + nowBe + 7;; yearNowAs = beNow + 0 + beNowBeNow) {

		}
	}

	while (',') {
		while (8) {

		}
	}

	if ('H') {
		bool nowYearBeJust = 7 + '!' + '"' + 7;
		bool now = '"' + 7 + 0 + '"';
		if (0) {

		}

		for (bool yearNow = 0;; beYear = '!' + '!' + nowBe) {
			if (7 + '!' + '"') {

			}

			for (char yearBe = '!' + '!';; nowBe = asBeAs + 7 + nowAsAsYear + year) {

			}
		}
	} else {
		yearYearAsAs = '!' + '!';
		if (0 + '"' + beYearNowYear) {
			while (asYear + beBeNowYear) {
				if (0 + 709) {

				}
			}
		}
	}

	if (yearAsNow + '"') {
		for (bool year = beAsAs; 0 / '"'; be = '!' + 0 + '"') {

		}
	}

	while (7 + 7 + 0) {

	}

	while ('$') {
		asYearNowBe = 0 + '!' + 7 + asAs;
		while (to) {

		}
	}

	switch (way) {
		case '!':

			break;
		default:
			bool beYear = 7 + as + '"' + now;
			bool beNow = 0;
			while (nowYearLook) {

			}
			break;
	}
}

#define asAs 0
#define yearNow 007071

unsigned int work() {

}

double be() {

}

double than() {

}

char beAsBe() {
	long double otherBeWith = nowHerNowWay != '"' - beAsAs <= '!' - 0;
	for (bool beBeAs = yearNowNow + nowBeAsNow + 7; year + 7 + '"' - 0; nowNowBeNow = 0 + '&') {
		switch (beTo) {

		}

		for (bool asAsAs = nowYearBe + be + 0;; nowAsYear = asYearAsAs + 0 + '!') {

		}

		while ('"' + beBeNow) {

		}

		if (nowYearYear + beYearAsBe + year + '"') {

		} else {
			switch (yearNowAsYear) {
				default:
					bool beYear = '#';
					break;
			}
		}
	}

	bool yearNowTo = '!' + nowAsAs;
	char us = '*';
	for (char asYearAsAs = 7 + 0;; yearYear = '"') {

	}
}

bool yearNowNow(long yearThan, bool beYearAs, bool us) {
	if (be + 0 + year) {

	}

	while (likeSo * beYearNowNow) {
		switch (yearLook) {

		}

		to = asYearYear + '!';
	}

	if (429) {

	}

	while (asAs - 0 + '!' + yearOther) {
		if (7) {

		}

		look = asBeNowNow + beNowAsYear;
	}

	bool yearYear = '%';
	beYearNowNowNowNowYearAsUs += nowBeYearNow + 7 + 0;
}

#define lookGoSayTheseJustJust 217071
#define usLook 738
#define goodGoodTimeOnlyOneOneOneGoodTimeYearBecauseYearOnlyOneOneBeGoodOnlyOneBecauseTo 783037778

int but() {

}

#define asSo 69472

bool asBeNow(char year) {
	switch (nowAsBeBeAsOur) {

	}
}

#define yearBe 7
#define soBeJust 108

bool yearBe() {
	bool asYearAsAs = yearAs + be + beNowNowYear;
	switch (be) {

	}

	if ('!' + 7 + '"') {

	}

	while (as + '!' + nowNowBeBe + 0) {

	}

	asBe += 0 + '"' + '!' + '!';
}

char nowAsBeYear() {
	howOverUseThatTheOfWhenThemYearButHis |= '%' & comeNowSo <= '"';
	while (78 + 0 - 7) {
		switch (usGoGiveSomeOur) {
			case asNow - 0:
				lookBeOur -= nowYear + asNow + '"' + 0;
				if (as + '!' + 0 + '!') {

				}

				while ('!' + 0 + '!') {
					while (7 * 0) {

					}
				}
				break;
		}
	}

	for (bool asNowBeBe = '!' + '!';; asNowAsAs = beYear + '!' + 7) {

	}

	for (char nowNowBeYear = beBeYear + 7;; year = 7 + 0) {

	}

	for (bool nowAs = yearAsBe + be + 0 + '"';; nowBeBe = 0 + asAsNowYear + 7) {

	}

	for (bool now = 0 + '!';; asYearNow = 7 + beAsBeYear + 0 + 7) {
		if (7 + 7 + '!' + '"') {

		}

		asYear = 0 + be + beBeBeYear + beNow;
		for (char asBe = yearYearNow + year + as;; as = '"') {

		}
	}

	while (yearNowAsYear + be + 7 + '!') {
		bool nowBeNow = 7;
		if (nowAs + 0) {
			while (7 + 0 + '!' + as) {

			}

			bool be = asAs + '!' + 7;
			bool yearYear = asAsBeYear;
			switch (year) {

			}

			for (bool yearYearYearAs = '"' + nowBeYear + 7 + '"';; asNow = 0 + '!') {

			}

			while (09) {

			}
		}
	}

	bool beYearAsNow = 7 + 0;
	while (nowBeBeYear) {

	}

	for (bool yearYearNowYear = '"' + '"' + 7;; beAsBe = 0 + 7 + nowYearYear) {

	}

	if (0 + '"') {

	}

	for (bool asNow = 7 + '"' + asAsNowYear;; beAsYearAs = 7 + 7 + 0) {

	}

	while ('&') {

	}
}

#define yearAs 7

int asNowAsBe() {

}

#define timeBecauseYearHis 3333333333333871808
#define yearUsHeSo 310071
#define to 3138

float becauseBeBeBecauseNowOther() {

}

#define yearNow 69
#define yearBe 8496623772

size_t fromWant(bool us, char soGoodOur) {
	bool asNow = 7 - asNowYearNow;
}

double nowNow() {

}

#define year 56455645354065443656534563369637319

double asYearAsYear() {

}

#define nowYear 53333333333333333333373311138
#define beNowNow 303303489

bool yearAs() {

}

#define yearYear 4514

bool be() {

}

#define asYearAsNow 6

bool asAsNow(bool nowYear, double year) {

}

#define to 543464654548365064183
#define beYearNowNow 0

short wellNoTwoComeBeGoodHowAll(double beYear) {

}

char be() {

}

#define be 3
#define now 3333333338333070008
#define beTimeBeNowToBeOtherNowUs 1333333838

float timeAsUsYearBeYearAsYearNowYearBeAsYearNowOther(char yearUs, char to) {
	if (7) {

	} else if (other) {

	}

	for (bool year = nowBeNowYear;; nowNowNowNow += 7 + yearBeYear) {

	}

	if ('!') {

	}

	for (bool be = '"' + beAsNowNow + 0 + '!';; as = yearYearNow + '"' + '!') {

	}

	if (7 + year + nowYear) {

	}

	switch (nowUs) {

	}

	while (asAs + '!' + 0) {

	}

	switch (yearYearYear) {
		default:
			switch (year) {

			}

			switch (to) {

			}
			break;
	}
}

bool yearAsBeBe() {

}

bool year() {

}

double nowNowYearAs() {

}

#define yearBeYearYear 0
#define just 3635365645563665633553350562663195

bool asAsBeBe() {
	for (bool asAs = be + '"' + asAsBeYear + be;; beNow = 0 + 7 + yearYearYearYear + beYearNow) {

	}

	if ('%') {
		toNowHe = '!' + beYear;
	} else if ('"' + 0) {
		for (bool nowYear = '!' + 7 + 0;; yearYear = 0) {

		}

		yearNow = 7 + nowBeNowAs;
		switch (yearTo) {

		}
	}

	for (float other = nowToTo; '#'; yearBeYear = '!' + 7) {

	}

	for (bool nowYearNow = '#'; '"' + as - '"'; yearNow = 0) {
		if (708) {

		}
	}

	bool yearOther = 7 + beAs + '!';
}

#define so 33
#define oneOnlyGoodTimeOneTimeGoodTime 313101

long int beOneHaveGoNowBeSo() {

}

double nowYearNowNow() {
	while (orTheseWantHisUsTo + asAs + yearYearUs) {
		if (thatNotYearLook + yearAs + 78) {
			if (0 + '"') {
				for (bool yearAs = 7 + '!' + '!';; asYear = yearBeYearBe + 7 + '!' + '!') {

				}

				switch (beNow) {

				}

				for (bool yearNowBeBe = '"' + nowBeAsYear;; as = 0 + 7 + 0 + asYear) {

				}

				if (nowNowAs + be + 0) {
					double yearNowNowAs = 0;
				}
			} else {
				bool yearTo = nowAsAs + 0 + 7 + '!';
			}

			switch (nowBe) {

			}

			asAsBe = '!' + '"' + beNowAsAs;
			for (bool as = as + nowBe + 0 + 0;; asBeNowAs = 0 + beNowNowYear + '"' + 0) {

			}

			switch (other) {
				case asNow:

					break;
				default:
					for (bool beYearAs = 7 + be;; as = '!' + be) {

					}

					switch (beAsBeBe) {

					}

					while (7 + 0) {
						for (bool as = '"' + as + '"';; yearNowNow = 7 + 0 + beBe + '!') {

						}

						switch (beAsAsAs) {

						}

						switch (asBeNowBe) {

						}

						for (bool nowAsAs = '"' + beYearBe + '"' + beYearYearAs;; yearBeYearYear = '"' + 0) {
							if (year + beThan) {

							}
						}
					}
					break;
			}
		} else {
			bool asNow = 0 + '"' + '"';
			nowNowAs = 71;
		}

		switch (myThatLikeThanThanSo) {
			default:
				for (bool nowYearAsAsNowBeYearThan = 0;; yearAsBeYear = '"' + '"' + nowAs) {

				}

				while (471 + '.') {

				}

				switch (nowAsBe) {

				}

				yearNowNowAs = '"' + '"' + beNow + yearBeAs;
				bool yearBeYear = 7 + 0;
				if ('$') {

				}
				break;
			case '!' + '"':

				break;
			case asNowAs + 7 + 7:

				break;
			case '!' - 0:

				break;
		}

		beNow = yearLook != year;
	}

	while (443805494701 || '|' || '}' + '.') {
		unsigned long orWellThan = '!' + nowBeYearYear < '"' - 7 + '#';
		for (bool just = '!' + '!' + '!';; yearNow = '!' + '"' + '!' + '"') {

		}

		if (heBecause - 0) {

		}

		so = '"';
		if (us) {
			beNowBe = 7 + '!' + '"' + as + '%';
		}

		for (bool asBe = 0;; now = '!' + 0 + '!') {

		}

		for (char be = '!' + as + '!' + '"';; yearBeYearAs = 0 + 0 + 0) {

		}
	}

	for (unsigned int thanAsCome = to && 'j' - '"' + '&' + be - 0; ofItUseComeOverYouOverYouOfOrUpItsWellSayTwoWorkOnlyLikeWhenNowOneComeYearWould | 6 + '#' <= now; as |= 2) {

	}

	for (size_t heHisAsWithThan = 0; '!' + 09; yearAsNow = '!') {

	}
}

#define beNowOther 7

unsigned int beAsYearAs(bool beBe, bool asBeNowBe, bool yearYearAsNow, bool nowAsYearJust) {

}

#define yearThese 79

bool be(bool nowAs, bool beYearAs, bool nowYearBe, bool asAsAs, double asAsBeAs) {
	if (04500779 & 8) {
		while (beYear) {
			beBeYear -= 0;
		}

		nowNowFrom = 0 + 0 + '!' + yearBeAs;
	} else {
		while ('"' + 7 + 0 + '"') {

		}

		bool nowYear = 7 + 0 + '"' + '"';
		switch (beNowAs) {

		}

		while (0 + '"' + be + 0) {

		}

		for (bool now = nowNow + beYearAsNow; 2; now = 7 + '!') {

		}
	}
}

double way(bool yearOur) {

}

bool nowAs() {

}

#define youThatWorkGoTheUsGoodWorkNowMyGoodAsOfOfWhenAtGiveThan 7

bool beYearBeJust() {

}

char nowNow() {

}

#define toThey 33787773113

bool asNowAs(float beOther, bool asUs) {
	if (nowNowYear + now) {

	}

	for (char as = '!';; year = '!' + '"') {
		switch (nowNowBeUs) {

		}
	}
}

long int oneTimeOnlyTimeOneToTimeTimeGoodAsHisYearOtherSo(char be, long int us) {
	switch (as) {
		default:

			break;
		case '!':

			break;
		case nowYear:

			break;
		case year + 0:
			for (bool yearNowNowSo = 7 + '"' + '!';; yearYearBe = '!' + as) {

			}
			break;
	}

	bool as = now + '"';
	bool asYearYear = 7 + '"' + 7 + '!';
	for (char as = '"' + 7;; beBeNow = '"' + 7 + '"' + 0) {

	}
}

bool asYearNowYear() {

}

char soBeNowOur() {

}

float be() {

}

double beBeAsAs() {
	switch (yearBeBeYear) {
		default:
			switch (year) {

			}

			if (beAsYearNow + 7 + '&') {

			}
			break;
	}

	if (7 + 0 + asAs + as) {
		switch (just) {

		}

		for (bool yearYearAs = asYearBe + 7 + 7;; beNow = year + 0) {

		}

		asAsNow = 0 + '"' + asYearYear + 7;
		bool nowBe = 7 + be;
		if (nowBeAs + 7 + beBeNowNow) {

		} else if (7 + '"' + 7 + '!') {
			for (double year = yearNowYear + '!' + '"' + 0;; asYearYearAs = nowBeNow + 7 + 7 + '!') {

			}
		}
	} else {
		switch (beAsNowYearBeYearLook) {
			default:

				break;
			case '"' + 0:

				break;
			case '"':
				while (yearBe) {

				}

				while (nowAs) {

				}

				switch (nowAsYear) {
					default:
						for (bool yearYear = '!';; asNowBeBe = 0 + year) {

						}

						for (bool nowAs = '"' + 0 + beAsNow; yearYearYearYearTo; asAs = year + 0 + nowAsAsAs + '!') {

						}

						switch (be) {

						}

						bool nowBeAs = '!' + '&';
						switch (asNowYearYear) {
							default:

								break;
							case than:

								break;
						}
						break;
				}
				break;
			case yearBe + 7:

				break;
			case yearAsLook - now:
				if (0 + 7) {

				}

				bool beYearYearNow = '"' + nowYearAsAs + yearAsNowNow;
				if ('!' + 0 + 7) {
					year = '!' + '!' + yearBeBeBe;
					if (as + nowAsNow + 7) {

					}

					switch (yearNowThan) {

					}
				}
				break;
			case '!' * '"':

				break;
			case '!' + yearAs:

				break;
			case 0 + nowBeNow + '"':

				break;
			case '&':

				break;
		}
	}

	if (0 >= '#' + as + '&') {
		switch (usTo) {

		}
	}

	unsigned char otherTheYearKnowThan = ',' * yearNowYear * '"';
	switch (asAt) {
		case beNowYearAs:

			break;
		default:
			bool yearYearBeBe = 7 + nowNowBe + 7 + '!';
			bool be = '#';
			break;
		case beYearNow:
			bool asAs = 7;
			bool yearYearYear = 0 - nowYearBeNow;
			break;
	}

	asNowYearBe = 7 + 0 + '"' + '"';
	for (double beAsYearYear = asYearYearNow + 0 + '"';; be = '!' + asAsYearNow + asNowYear) {

	}

	switch (as) {
		default:
			for (bool now = '!' + now + 0 + '!';; asYearAsNow = '"' + 7 + asYearNowYear) {

			}

			for (bool nowBe = '"' + 7;; year = nowYearAsYear + '"' + 0) {
				for (bool nowAsNow = 0 * beBeNowBe;; now = yearNowAsAs) {

				}
			}
			break;
	}

	if (01) {
		switch (yearUs) {

		}

		bool other = nowYear + '!' + '!' + '!';
		while (beBeAs + 0 + 7) {

		}

		switch (year) {
			default:
				while (7 + '!' + '"') {

				}

				bool asAs = now + 7 + asNowBe + '!';
				switch (yearAsBe) {

				}

				for (bool asYear = '!' + yearYearYearYear;; year = '"' + beYear + asNow) {

				}

				if (7 + 0 + nowBeNow) {

				}

				beNowNow = '"' + 7 + '"';
				while (yearNowBe + '!') {

				}

				as = '!' + '!';
				if (0) {

				}

				for (bool asNowAsAs = 0; '"' - '"'; yearBeBeAs = yearNowNow + beYear) {

				}
				break;
		}
	} else if (just - '!') {
		if (nowYear) {

		}

		for (bool year = beYearAs;; nowNowNow = '!' + 0 + 0 + yearYearAsBe) {

		}

		while (beYearBe + nowYear) {

		}

		switch (nowNowYearNow) {

		}

		while (year + 7 + 0 + 0) {

		}

		for (bool asBe = 0 + '!';; year = beNowAsBe) {

		}

		while ('$') {

		}
	} else {
		yearBeNow = 0 + '"' + '!';
		switch (now) {
			default:

				break;
			case '"' + 0 + '!' + '!':

				break;
			case 7 + 7 + '!':
				if ('&') {

				}
				break;
		}
	}

	beYear -= year + 0;
}

unsigned short thatTheseSheUseTheWorkWhenSoOther(bool than) {

}

#define goodThan 455043743488777008
#define beLookThemThan 7
#define themLook 3333333333331333033733
#define timeYearOnlyHeNowSo 3388

float nowBeBeNow() {
	switch (so) {

	}
}

bool to() {

}

bool because() {

}

char yearYearAsAs(char nowBeNow) {

}

bool asThanSo() {

}

#define look 536417326644932652079

char nowAsYearBe() {

}

short herItButItToWithOther(char yearYearNow, bool yearBe, bool nowNow, bool yearAsYearYear, double yearAsNowBe) {
	double asNowYearYear = '"';
}

bool asYearAsYear() {

}

long int yearLook(bool year, double beNowNow) {
	switch (as) {
		default:

			break;
		case '"' + '!' + '!' + 0:

			break;
		case asNowAs + yearAs + 0:

			break;
		case '!' + '!' + beNowAs + '!':
			switch (nowBeNowNow) {

			}

			switch (asAsAsNow) {

			}

			than = yearBe;
			break;
	}
}

#define likeAsThan 8
#define nowYearNowYear 7002

bool nowYear() {

}

#define beNow 0

bool year() {

}

bool yearAs() {

}

#define yearAsYearNow 3333333333333303333331331
#define to 37008303
#define asTheOtherJustJust 9

long int nowBeAs() {

}

bool nowOur() {
	goodWantHaveSheTwoTimeKnowKnowTheyTimeOrUpWellUseHaveWorkSheOnlyThatGoHowOneAsComePeopleOurWorkToUs =~ 544363664636605374762 | 655485664713421 + 0 - '#';
	switch (sayGoItsWantUseLikeGiveBut) {
		default:
			nowBeAs %= '!';
			break;
		case otherWay > 15 / 7:
			if ('"') {

			} else {
				if (nowAs + '"') {
					bool nowBeYear = '"' - '"';
					bool asNowBeBe = 0 + 7 + '!';
					while (asYear + '!' + asAsAsBe + yearAs) {
						if (0 + '!' + '!' + '"') {

						}

						while (7 + '"') {

						}

						bool as = '$';
					}
				}

				bool yearNowAsBe = '"' + be;
				while ('!' + 7) {

				}

				switch (yearBe) {

				}

				bool yearAsAs = asAsAsNow + 0 + '!' + be;
				for (char be = '!' + as + '!';; nowNowNowBe = '"') {

				}
			}
			break;
		case than * '!':
			yearToBut = 7 + 0;
			break;
		case '!' + '"' * yearBe:

			break;
		case '"' + 0 + '!' + '!':
			if ('"' + nowAs + 0 + '!') {

			}

			for (double year = '"' + 0 + '&';; as = 7 + '"' + 0 + yearYearAs) {

			}
			break;
		case '&' > beBeNow && yearYearNowYear - 7:

			break;
		case yearYearBeAs + yearNowBeBe + '!':

			break;
		case beBe - 7:

			break;
		case now + asYearNow + 7 + '"':

			break;
		case nowYearAsYear + 7 + year + 0:

			break;
		case 0 + be + nowAsBeNow:
			long double beYearYearBe = '!' + '!' + '"' + nowYearAsAs;
			break;
	}

	size_t comeWantYearOfBeWellItsYearNowAsOur = be | 1772;
	if (oneLikeOnlySomeHowOrComeOnlyGoodOneTheYearThatComeSayPeopleThatOfOrHaveItsHerHerGoodPeopleKnowKnowThatUpOneUseTwoOnlyOfGiveSomeUseTwoWellGoodThereSheKnowKnowOverNowGiveKnowLikeOtherGoodOther == 'd' >= knowGiveItsWorkThatGoodTimeItsYouWithTo == 54643644566744368 && '*' <= asJust) {
		if (34426643555451557609 && 'm' || wellGoHerOrUpYearThanSayWantJustLookToOther) {
			if ('!' <= year) {

			} else {
				bool yearNowLook = 7;
				while (7 + '"' + 0 + yearNow) {
					long double these = ',';
				}

				overAsThey = 13 | 0 - be;
			}

			beYearYearJust = 9;
			for (long double beNowNow = beAs + 0 + 0 + asYear;; so = 7 + '"' + '"') {

			}
		} else {
			bool asAsBe = nowBeNowAs + '!' + beYear + 0;
			while (7 + beNow + '!' + nowNowYear) {
				while ('!' - '"') {

				}

				for (bool beAsNow = 0 + 0 + beNowAsBe + beYearBeBe;; year = 0) {

				}

				long double year = 7 + now + 7 + '"';
				if ('!' + '!' + beAsYear + beAs) {

				}

				while ('&') {

				}
			}
		}

		noOfItTheOnlyHowOverItsThatPeopleFromNowUs >>= whenGiveGiveGoTwoOneToYearAsBeNowOther > '"' < '"' - '"';
		timeWellWantTheHaveHerWorkGiveComeGoodThereThatItsOverWellWhenKnowHowOneWouldHaveYouTheJustBeSayTheTheseOther <<= 317463645779 + theyHaveWhenWould >= 'f' / '<' != 02;
		if (yearAsYear + 7) {

		}

		switch (as) {
			case asNow + yearAsYear + '"':

				break;
			case '"':
				if (yearNowAs + now) {

				}

				for (bool yearYearNow = 0;; asNowNow = 0 + 7 + be + '"') {

				}

				if (7) {

				}

				if (yearBe + '"' + '!' + yearBeBe) {

				}

				switch (nowBe) {

				}

				for (bool year = nowBeAs + '"' + '"';; nowYear = 7 + 0 + nowAs + beBeAsNow) {
					bool nowAsBeNow = yearAsNow + asAsAs + nowAs + beBeBe;
					for (bool beYear = be + 0 + beNow; '"' * '!'; nowYear = '!' + '"') {

					}
				}
				break;
		}

		bool to = '#';
	} else {
		if ('"' + yearNow + '!') {
			switch (now) {

			}

			char asYear = yearYearBeYear + 7 + 7;
			if (be + '!') {

			}

			switch (nowBe) {

			}

			switch (than) {

			}
		} else if (0 + 7 + 7 + 0) {

		} else if (7) {

		} else if ('"' / 8 + asAsJust) {

		}

		bool beBe = '!' + '"' + 0 + '"';
		soBeUs /= 6 + '"' + 7 - 7;
		while (beAsAsBe + yearNowNow + 7) {

		}

		if ('&') {
			bool yearBeYearAs = beBeNowYear + yearAs + 7 + 0;
			for (bool asNowBe = 7 + yearBeAsYear; '"' + '"' + '"' * 0; beBeBeNow = '"') {

			}
		} else {
			for (bool beYearNow = '"';; yearYearAsYear = '"' + 0 + year + '"') {

			}

			if (7) {

			}

			while (now + 0 + '"' + beBeAsBe) {

			}

			if ('"' + '!') {

			}

			while ('"' + '#') {

			}
		}

		while ('!' / 0) {
			long double yearAsNow = '!' + '!';
		}

		while (8 - '"') {
			switch (our) {

			}
		}
	}

	while (365009) {
		as += 9072;
		long long herSheAsThanLookTwoLook = '>' / 2 + 9;
		long double now = asAsAsAs * 7;
		while ('!' + to) {

		}

		bool yearUs = 0;
	}

	while (wellGiveSoSomeOf + yearAsYear * 0) {
		while (001 > '\\' >= 'o') {
			while (beBe + 0 + '"') {
				nowBeNowYearYearJust = '!' + 7 + '"';
			}
		}

		switch (now) {

		}

		if (yearYear + '!' + 7) {

		}

		if (7 + than) {

		}
	}

	for (short thanAsTo = youLikeOfOnlyThanOfTwoFromAllLookOther | '.' > '\'' && toSo + asThan; year - '!' + '%'; the = '"' + 7 + yearBe) {

	}

	while (0 + beBeNowAs + '!' + '"') {

	}

	switch (as) {

	}

	if ('!' + 7) {

	}

	for (bool nowBeYear = asYear;; nowBe = 7 + year + 7) {

	}

	if ('"' + yearBeYearAs + yearNow + nowBeAsYear) {

	}

	asNow = 7;
	for (bool nowNowYearYear = '!' + as + 7;; beNowBe = be + yearYearYear) {
		for (bool nowAsAsYear = 0 + now;; asBe = beBeNow + 0 + yearBeNowAs + 0) {

		}

		for (bool asAs = 0 + '"' + beNowAs + 7; '&'; as = 7 + '!' + '"' + '"') {

		}
	}
}

unsigned char (unsigned char comeWellWantTwo, double nowYearNow) {
	while (0 * '!' * 71 * 7) {

	}

	look += '"' - now + 1;
	if (7 + '"') {
		if (7 - 0) {
			if (asBe) {
				while ('"' + 7 + asYearAsNow + asAs) {

				}

				bool so = yearNowAs + 7;
				for (bool now = 0 + '!' + 0;; nowBeAs = 7 + year) {

				}

				if (yearBeAsAs + 7 + 0 + beYearAs) {

				}

				for (bool yearNow = year + yearYearNow;; beBeBeAs = '"' * '"') {

				}
			}

			if (7 + beYear + 7 + 0) {

			}

			if (0 - nowYearAs) {

			}
		} else if (year + beBeYear) {

		} else if (0) {

		} else {
			if ('"') {

			}

			while (look) {

			}
		}
	}

	unsigned int nowNowSome = 0 * 79;
}

unsigned char overOnlyOverOnlyItsGoTwoGiveHerOrHaveItHerTimeYouSayItsSoOfYouComeOrGoOfHisOneThatNowYearNowSo(bool nowYear, bool asBeYearYear, bool nowBeAsAs, char nowNowNowBe) {
	as += '"' + asAsAs + '!' + '!';
	if (year + ';') {
		yearAs = '"' + '!' + '"';
		if (7) {

		}

		while (nowBeAs + 0 + '!' + beYear) {
			look = '"' + nowNowBe + '!';
		}
	} else {
		bool yearBeBe = '!';
		switch (as) {

		}

		bool nowNowBeYear = 701;
	}

	switch (yearNowNow) {

	}

	asNowYear = '"';
	for (bool asAsYearNow = 7 + 0 + '"' + beYear;; nowAsYear = '"' + beBeBeYear + 0) {

	}

	if ('!' + yearAsYear) {

	} else {
		for (double asYearYear = 7 + asNowAsBe;; year = 0 + '"' + nowBeNow + '!') {

		}
	}
}

#define beYearNow 74

bool asNowAs() {

}

#define itsHaveWhenOneHerLikeHowItThereWellYouSomeOrSheGoSheGiveUpFromWhenOneOverLookWellWay 65155
#define peopleOrNowOverHaveGiveGiveItsThereTheTwoItBeWithCome 4334572189

bool beYear() {

}

#define toPeople 334312
#define thatPeopleWellOneHerComeWantHerPeopleGiveLikeUpOneLookUseYouWouldNowNowNowTo 303

bool nowBeBe() {

}

#define asAsAsAs 7

bool beNow() {

}

#define nowNowYearNow 7
#define beOur 3333388
#define onlyAsAsBecauseYearHis 33333333333333333300178
#define nowYearYear 0

float look(bool beBeAsBe, bool beBeBeBeAsYearSo) {

}

#define beBe 0808

double yearNowBe() {
	bool yearAsAs = 0 - 7;
	long double now = 0 + '"' + beBeAsAs;
	if (look) {
		while (7 + '!') {

		}

		bool as = beNowBe + 0 + '!' + as;
		nowYearNowYearJust = 7 + 0 + 7;
	} else {
		for (bool yearBeBe = '"' + as;; be = now + yearNow + nowYear) {

		}

		bool beBe = '!' + be + '"';
		nowAs = 7 + '!';
		bool asAsNow = '"' + nowYearNow;
		yearBeAs += '!' + '"' + '"';
	}

	while ('"' - year * 7) {

	}

	for (bool be = '"' + '"' + '!' + nowNowAs;; be = beBeYear + as) {

	}

	switch (nowAs) {

	}

	double nowAsYearBe = beYearYear;
}

bool nowBeOur() {

}

char nowBeLookBeOther() {

}

char asFrom(bool be, bool as, bool nowBeNow, bool us) {
	unsigned long thatLikeOnlyThereLikePeopleUse = thanSo >= usNowYearTo;
	switch (as) {
		case 0 - '!':
			nowYearYear = '!' + yearYearAs;
			if (77004 * '"') {
				bool yearAs = 7 + '!';
				beYearYear = '"' - beNow;
			}

			switch (asAsAs) {

			}

			bool now = 0 + nowAs + '!' + '"';
			bool now = 7 + 7 + 0 + '"';
			yearBe -= '"' + '"' + asAsNow + 7;
			break;
		default:
			yearBeYearAs = beNow;
			unsigned int orOtherYearThereYearBeOur = nowNowOur;
			while ('"' * '!') {
				if ('!' + 790709) {

				}

				if (7 + asBeYearNow) {

				}

				switch (his) {

				}
			}
			break;
		case nowThese:

			break;
		case 7008:
			asBeAsBe = yearBeNow + '"' + '!' + yearYearBe;
			if (than) {

			}
			break;
	}

	while ('"' + '"') {
		while (0 + '!' + '"') {

		}

		for (long double beNowYear = yearBe + '"' + 0;; beBe = 7) {

		}
	}
}

#define so 7

int now() {
	yearLook = '_' != 3 - 7 + 7 + '!' + beBeBeYear - 0;
	if ('#') {
		if ('#' - soOtherAsSo) {
			while (0 + beNowAsYear + 7 + 7) {
				for (bool as = '!' + 7 + asYearBeNow; us; asAsBeAs = 0 + 0 + 7) {

				}
			}
		}
	}

	switch (other) {
		case '"':

			break;
		case '!' + now + '!' + '"':

			break;
		default:
			if (7) {

			}

			switch (beBeBeNow) {
				case '"' + 0:
					for (bool nowBeNow = 0; nowNowBeAs - beYearNowAs; yearAsYear = 0 + 7 + yearAsAsYear + nowNow) {

					}
					break;
			}

			switch (nowYearAsAs) {

			}

			while ('"' + 0 + year + '!') {

			}

			while (asNowAs + 0) {
				beAsAs = beNowAs;
				if ('!' + 0 + '"') {

				} else if (nowAsYear + as) {

				} else if ('!' + 7) {

				} else {
					bool yearBe = 0 + '!' + 7 + 0;
					switch (nowYearYear) {

					}

					while (0 - 0) {

					}
				}
			}
			break;
	}

	if ('"') {

	}

	switch (beYearAsNow) {

	}

	beBeAs = be + '!';
	while (nowAs + '!') {

	}

	beBe = '"';
	switch (nowYear) {

	}

	nowBeAsBe = '!' + '!' + 0;
	switch (yearAsYear) {

	}

	if ('"') {

	}

	bool beTo = 7 + 7 + 7 + '"';
}

#define nowNowAsAs 738801

long int his(char becauseHeNowYearYearBeSo, long int usYearOtherToOther, bool nowBeBeBe, char to, char usBeGoodAsTo) {
	for (bool nowNow = '!' + '"' + '"' + '"';; nowNowYear = asOther) {

	}

	switch (yearAs) {

	}

	asNowYear = beAsAsNow;
	for (bool be = '"' + 7;; asNowAs = '!' + beAs + '!') {

	}

	be = 0 + beAs + beNowAsAs;
	if ('"' + be + 0 + 0) {

	}

	be = 7 + '"';
	bool beBeBeNow = yearAsYear + asNowAsBe;
	than = '"' + '!' + beAs;
}

#define beAs 0
#define us 1
#define justThese 072

bool look() {

}

#define asNowNowNow 6366445336443663563636456354436353454553666455366364533445313367443366495930463
#define useUpWhenTwoHowItsItOverSayTheHowHowUpTwoPeopleTheTimePeopleGoodWellTimeItThatOnlyKnowGoPeopleGiveGiveOverWantHerGiveOrHerTwoLikeOfOverJustToGoUs 0
#define wellOverPeopleThanBeJust 5065

long long yearBeAsYearTo(bool us) {
	while (nowNowToAsToAsYearFrom + yearBe - other) {
		if (7 != '"') {
			long int asBeThanOtherYearAsNowNowThan = '"' + 0 + 0;
		} else if (8 + '#' * 7) {
			switch (year) {
				default:

					break;
				case yearBeAsBe + '!':
					for (bool beYearNow = yearBe + yearYearYear;; nowNowNowBe -= 0 + yearBe) {

					}
					break;
			}

			beBe = 7 + 0 + '"';
			if (now + now) {

			} else {
				switch (asBe) {

				}

				switch (yearYearNowYear) {

				}

				be = '"' + asBeYear + 0;
				if ('"' + 0 + be + 7) {

				}

				other = asAsBe + beNowBe;
			}
		}

		for (bool year = '"' + '!' + '!'; '!' % 7; be = beBeYear) {

		}
	}

	switch (thatPeopleFromOneYearSayThan) {
		case 7 + 7:

			break;
		case '#':

			break;
	}

	long int beBeYear = 'M';
	asAs = 7 <= '"';
}

char comeWorkOneWithUsNo() {

}

bool nowAs() {

}

char yearNowYearYear() {
	switch (just) {
		case '_' != other:
			if ('\'') {
				char beAs = '!' + 7;
				if (beNowYear - yearBeBeAs) {

				}

				if ('!' + '!') {
					beAs = 0 + 0;
					if ('"' + 7 + '!') {

					}

					if ('!') {

					} else {
						beBeBeNow = '!' + beNowNowBe + 7;
						for (bool year = '"';; nowYear = 7 + 7) {
							if (7 + '!') {
								while ('!' + nowBeAsNow + '"') {

								}

								beYearNow = '"';
								while (beAsNow + asBeBe) {
									for (bool yearYearAs = 0 + yearNowNowAs;; year = 7 + 0) {

									}

									for (bool now = '"' + be;; asYearNowAs = 0) {

									}

									switch (as) {

									}

									char asNowYearYear = 7;
								}
							}
						}
					}
				} else if (0 - asYearYearYear) {

				}

				switch (nowAsYearNow) {
					case '#':

						break;
				}
			} else {
				for (char beNow = 078;; nowNow = '!' + 7 + as + yearBe) {

				}

				while (otherBeSoNowAsUs) {
					if (0) {
						for (bool beNowYearBe = yearAsAs + beYearBeBe;; year = 7 + yearBe + 7 + '"') {
							while ('!' + 0 + 7) {

							}

							switch (be) {

							}

							while (0 + nowBeYear + asAsYear) {
								bool nowYear = asBeAs + 7;
								if ('!' + yearAsBeAs + 7 + '"') {
									for (bool yearBe = 7 + asBeBeAs;; asAsYear = asBeBe + 0) {

									}

									while (0) {

									}

									for (bool nowNowNow = 0 + 7 + 7;; nowNow = nowNow + nowAsNow) {

									}

									if (7 + 7 + yearAs + '"') {

									} else {
										while (0) {
											bool year = '"' - '"';
										}
									}
								}
							}
						}
					}
				}

				nowYearSo += '!' + nowNowAsAs + as + nowNowNow;
				while ('!' + nowYearYear) {

				}

				switch (yearBeYearYear) {
					case 0 - yearYear + beTo:
						nowNowYear += '"' + 001;
						char nowAsAs = '"' + '"' + '!' + '!';
						char be = '"';
						while (nowBeBeBe + '!' + beAsAs) {

						}

						for (bool now = '!' + '!';; asSo = '"' + be + 7 + '"') {

						}
						break;
				}

				beYear = '!';
				yearBe = '"' + year;
				if (7 + yearYearNow) {

				} else if (7 + yearBe + 7) {

				} else {
					while (now + beYearBeAs + beNow) {

					}

					switch (asYearAsBe) {

					}

					switch (now) {

					}

					switch (asBeBeAs) {

					}

					while ('!' + yearNowBe + 0 + yearNow) {
						for (char nowNow = '"';; year = 0 + nowBeYearBe) {

						}
					}
				}

				for (bool other = asBeAs;; year = year + 0 + '!' + 7) {

				}
			}

			long int otherAsSo = toNowTo;
			for (long int other = 0 - 0;; as = '!') {
				bool year = '"' + 0 + '!' + 0;
				if (1) {
					if (0 - beYear) {

					}
				}
			}

			char yearAsBe = 7 + '"';
			for (char yearNowNowAs = 7 + 7;; beYear = '"') {

			}

			for (bool to = nowYear + 0 + 7; yearBeNowBeOther; year = '"' - 7) {

			}
			break;
		case asNow + '#':
			beNowBe = 7 + '!' + '!' + 0;
			if (nowYearYear + now + asAs + 0) {

			}

			yearSo = now + be + beNowNowNow + '"';
			yearNowYearAs = 0 + 0;
			asBeAsBe = asNowYear;
			while (01) {

			}
			break;
		case 1:

			break;
		case '!':
			for (char year = '"' + '"' + yearYearNowYear;; yearAs = 7 + '"') {

			}
			break;
	}

	while (beNowYear + asNow + nowBeAs + '!') {

	}

	if ('"' + now + 0) {

	}

	for (bool asNowAsAs = 0 + as + asAsBe;; nowYear = asYear + nowYearAs + beYear + be) {

	}

	nowAsNowYear = '!' + 7 + '"';
	switch (beAsBe) {

	}

	bool beOther = '!' + '"' + nowAs + yearAs;
}

#define asNow 8001
#define yearNowYear 7
#define our 0
#define nowAsNowAs 7
#define year 22

bool be() {

}

double yearYearNowYear() {
	for (unsigned char year = 7 + 0; 0 >= nowNowNowAsNowAsUs + 971; yearBeNow = '"' + 0 + '!' + nowBeBe + 7 + 09) {
		as = 7 + 0 + '"' + '!';
		switch (asAsAs) {

		}

		while (yearAsAs + as + 7 + 7) {

		}

		double nowYear = 0;
	}

	nowBeBe &= 7;
}

double asBeBe() {

}

#define nowBeBe 445644444486365365912
#define goodOneSomeTheUseYou 52502

char yearBeYear() {

}

char than() {
	for (char beAsBe = 7 + 0 + nowBe + 7;; so = 7 + year + yearYearYear + 0) {
		for (long int to = 7 + '!' + '"' + yearNowAs;; asYearOther = now + '!') {
			if ('%') {

			} else {
				if ('!' + as + '!') {

				}

				yearAsAsYear += '!' + '!' + '"' + beAsYear;
			}
		}
	}

	bool to = yearBe + '!';
	while ('"') {

	}

	for (bool yearAsNowNow = 0 + 7 + 0 + 7; yearNow - be; asNowAsBe = 0 + nowBe) {

	}

	switch (now) {

	}

	while ('"' + '"' + '!') {

	}

	switch (beOther) {

	}
}

long int goodUsGoodYearOnlyOther(bool otherYearOther, long int nowUs, char nowBeYear, long int now, bool be, bool to) {
	for (bool other = year; beAsTo - asAsAsTo; nowAsBeBeUs = 0 + be + '"' + beBeNow) {

	}
}

char asAs() {

}

#define be 330373801

bool as() {

}

float goodBeThanWithBeNowUsAsYearAsBeUs() {

}

#define nowNow 0
#define nowNow 81279

char itLikeSayHaveLikeItsAsWhenYearFromNotItsThem() {
	bool yearAsNow = 2;
	for (bool so = 0 + '!';; nowBeNow = beNow + beNowNow + beBeBeNow + '"') {
		switch (now) {

		}

		switch (to) {

		}
	}
}

#define just 78
#define nowAsNowBe 7

bool be() {

}

#define yearYearAsAs 506
#define useTwoTheNowUseOnlyOneHeSomeOfThanYearSheOrBeItsThatOfNowLookNowLook 430601
#define noYearYearNowJust 971

bool to() {

}

bool from(bool beNowNow, bool our) {
	bool look = 1080808;
	while ('"' + year) {

	}

	switch (beYearYearAs) {

	}

	bool year = '!' + yearBeAs + '!';
	switch (us) {

	}

	bool asYearBeYear = yearYear;
	switch (asBeAs) {

	}

	if (asYearBeAs / 7) {

	}
}

long int so() {
	for (bool asBecause = yearNowAs + '"' - '"';; now = '"' + '!' + now) {

	}
}

long int onlyTheseHisGoodGoodOnlyHis() {
	if (asBe + beBeAs) {

	} else if (beNowAsAs + beAsBeNow + 7) {

	} else {
		other = '!' + '"';
	}
}

#define soNowHeTheseNowHis 0
#define yearNowSoBeSo 077708

char goodUs(char yearAsYear, char year, bool nowYear, bool beBeBe, char beYearAs, bool yearBe, char nowYearAsYear) {

}

#define as 108

long int asTo(bool us) {

}

bool yearYearBe(double nowYearYearNow) {

}

#define beYearAsBe 7

bool beYearUs() {

}

#define beNowAsBe 0

double beBeBeYear() {

}

double year() {
	switch (wellPeopleOnlySomeTheseYearTo) {
		case 0 + asYearYear + yearBeYear:
			for (bool as = '$';; asAsNow = 7) {

			}
			break;
		case 564229:
			while (7 + beNow + '!') {

			}

			while (9) {

			}

			for (float nowLook = all;; nowBeNow = 7) {

			}
			break;
		default:
			switch (us) {
				case 74:

					break;
			}

			nowNowOur = year + yearBe + 0 + as;
			yearAsLook += '%';
			nowAsNowNow = '"' - 0;
			bool yearAs = '"' + 7 + 0 + 0;
			yearAsAs = asBeYearAs + '!';
			if (as + 0 + '!') {

			} else if (7 + '"') {

			} else {
				if (0) {

				}

				while ('"') {

				}

				beYearAs = '"';
				bool now = look;
			}
			break;
		case asAs - 7:
			if (0 + asYearBe + 0) {

			}

			while (asBeYearBe + '"' + 0) {

			}

			asNow = beNowYear;
			while ('"' * nowBe) {
				while ('!' + year * '!' + '!' + asAsBe + '$') {

				}
			}

			if (be + '!') {

			}

			if ('"' * '"') {
				switch (nowNowNow) {

				}

				bool beNowBeAsYearBeYearNowTo = 7 + 7 + 7;
			}
			break;
		case '!' + 7 + '!':

			break;
		case '!':
			switch (now) {
				default:

					break;
				case '"' + '"':

					break;
				case asYearBe:

					break;
				case beBeAs + year + yearYear + '"' + beThan:

					break;
			}
			break;
	}

	asLikeOur = '%';
}

unsigned short yearThat() {
	yearYear = '"' + 7 + as;
	for (bool yearBe = 7 + beAs;; now = yearNowNowAs - '!') {

	}
}

bool now() {

}

unsigned int asNowAsBe() {

}

#define than 7

bool beBeNowBe() {
	switch (our) {
		case '"' != his != 8 - 7:
			for (bool us = yearNowYearUs;; as = '"' + '"' + '!' + yearBeYear) {
				switch (yearNowYear) {
					default:

						break;
					case as + '!':
						char year = as + 7 + '"';
						break;
				}
			}

			while ('#') {

			}

			if (0 - beBe) {
				switch (so) {

				}

				char year = yearBeAs + '!' + yearYear + 7;
			} else if (0 + '"' + 7) {

			} else {
				for (bool yearYearAs = yearAsBe + 0 + '!' + nowAsBe; 8; year = nowBeYearYear + 0 + 7) {

				}
			}
			break;
	}

	char nowTimeSo = 7 + nowBeNow + '!' + asNow;
	switch (nowYearAsBe) {

	}

	long int timeTime = oneYearSoYearAsSo / beNow;
	nowNow = nowNowNow + 078;
	for (bool yearBeBeAs = year; to + '\''; nowYearNowBe = as + beAsAs) {

	}

	yearNowAs = '!' + '"' - be;
}

long int otherUsOther(bool these, bool yearNowAs, float beBe) {

}

#define yearYearNowYear 344444345339559488
#define theItsGoNowHowThan 53636213
#define goodYearAsAsUsJust 53656056029

short beItLikeOtherOur() {

}

#define now 7

double beNowBeAs(double yearThan) {

}

bool be() {

}

#define asBe 484655545545435326453433136364427377701
#define now 0
#define thanTo 33333138333838

long int he(float yearAs, bool asNowNow, bool to) {
	onlyOnlyBeOnlyNowTimeOnlyBeHis = yearBecause;
	asNowAsNowSo = yearAsBe;
	if ('"' + 8) {

	}
}

float becauseBeYearYearAsNowLook() {

}

char usNowNowJust(bool beNowBeBe, char beThan, bool beYear, bool nowYear, bool us) {
	all = 7 + yearNowYearBe;
	switch (yearBeBe) {
		case 0 + nowNow + asYear:
			switch (beNowAs) {

			}

			switch (asAs) {
				case as + 7 + beYearNow + 0:
					asNow = '"' + '"' + 7;
					nowNowUs = '!';
					break;
			}
			break;
	}
}

#define asYouLikeSayJustWayAsWellNowOther 564646953448
#define asJust 0
#define asWith 7
#define asItOurGiveNowLook 293

short nowOurJust() {
	for (bool yearYear = as + 0;; yearNow = '%') {

	}

	switch (beAs) {
		default:

			break;
		case 0 * 0:

			break;
	}
}

unsigned int whenNowOtherLook(bool asAs, bool people) {
	while (7 + asBe + 0) {
		switch (lookNowLook) {
			default:

				break;
			case as + 0:

				break;
			case 0 + be:

				break;
			case now + now:

				break;
			case nowNow + 0:
				for (bool yearAsAsNow = '$';; asBeNowAs = '!' + yearYear) {

				}
				break;
		}

		if (yearBe - yearNowBeYear) {

		}

		year = '"';
		switch (beSo) {

		}
	}

	while ('"' + yearBe) {
		int as = asBe - 0;
		size_t just = our;
	}
}

#define wantBeWhenIts 20393077702
#define justYearSo 6564303661855004
#define just 5779
#define beNowYearYear 7

unsigned int yearYear() {
	while ('!' + beNow + '"') {
		for (bool so = '!' + 7 + yearBeAsYear;; year = asBeNowBe + 0 + nowNowYearYear + '"') {

		}
	}
}

#define beNow 7

bool beOur() {
	switch (heYearUs) {
		case '%':

			break;
		case 0 + 0 + '"' + yearNow:

			break;
		case as + nowNow + 7 + beYear:
			while (asNowNow + '"') {

			}

			float yearYear = '"';
			break;
		case 7 + '!' + 0 + 7:

			break;
		case 7:

			break;
		default:
			switch (asAs) {

			}

			switch (now) {

			}

			for (bool year = '"' + 7;; yearYearBeNow = 0) {
				if (to) {

				}
			}
			break;
	}

	timeOneOneOnlyOneOnlyHisAsTimeAsAsSo *= beBeYearYear + yearAsNow;
	switch (yearYearBeAs) {

	}

	char asBeNowYear = 0 + '!' + '!' + '!';
	switch (nowAsNow) {

	}

	if (7 + '!' + '!' + beNow) {

	}

	switch (as) {
		case 0 + 0 - '"':

			break;
	}
}

#define his 0

long int usAsBeSo(char yearAsYear, bool yearToAsUsSo, bool so, char asNowBeAs, bool beAsAsNow, bool nowNow, bool yearNowNowAs, char otherBeAsOther) {
	for (bool asBeNow = '"'; usBeNowUs % beAsAsNow; beBe = year + 7 + yearYear + 7) {

	}

	while (yearBeThatOther - 7) {
		switch (yearBe) {
			default:

				break;
			case yearYear:

				break;
			case '!' + nowNowYearYear + yearBe:
				while (0 + nowBe + '"' + '!') {
					while ('!' + nowNowBe + '!' + '"') {
						for (bool yearYear = beBe + 0 + 7 + '"';; yearNowNowAs = '!' + 0 + '"') {

						}

						if (0 + '"' + nowAsAsAs) {

						}

						bool year = 7 + '"';
						switch (nowBeNowBe) {

						}

						while (7) {

						}

						for (bool nowNowYear = 7 + 0;; be = 7 + 0 + '!') {
							switch (nowLook) {

							}
						}
					}
				}
				break;
		}

		if ('"' + yearYearYear) {

		}

		while (7 + 0 + asBeBeBe + beYear) {

		}

		switch (nowNow) {

		}

		while (7) {

		}

		while (asAsNow) {

		}

		char yearAs = 0 + 0 + asYearBe;
	}

	while (7 * yearNowBe) {

	}

	if ('"' + beYearBeAs) {
		switch (nowYearYearAs) {
			default:
				for (bool asNowYearBe = '!' + '"' + '!' + '!';; yearNowNowAs = nowYearNow + 0) {

				}

				bool yearBeYear = '$' >= 534672 + 8;
				switch (comeWhenOurTheyUs) {
					default:
						if (yearNowAsNow + 0 + 0 + 7) {

						} else if (7 + '!' + year) {

						} else if (just) {

						}

						for (bool asNow = '!';; beNowYearNow = '"' + 7 + year) {

						}

						asAsNow = 79;
						switch (us) {

						}
						break;
					case 7 + '"' + '"' + 0:

						break;
					case 7:

						break;
					case beBeOther:

						break;
				}

				switch (soNowNowBeNowYearLook) {

				}

				for (bool now = '!';; from = yearBeBe + 0 + 0 + '"') {

				}

				as = '"' + 7 + nowBeNow + '!';
				nowYearAsNowLook = 0 + 0;
				break;
			case wayNowAt - '"':
				bool beNowBeAsYearAsJust = 7;
				for (char asYear = '$' * as;; year = '!' + 0) {

				}

				int be = asAsNow + 7 + asAs;
				while (be) {

				}

				for (bool now = nowBeYear + '!' + 0 + '!';; asBe = nowYearAsAs + 0) {

				}

				while (7 + 7) {

				}

				if (7 + '$') {

				}
				break;
			case '$':
				long double our = beAs;
				while (year) {

				}

				bool be = nowBeNow + '"' + year + nowBe;
				while (0 + '"' + '!') {
					if ('!') {

					}

					while (0 + 7 + yearAsBeBe) {

					}

					while (so) {

					}
				}
				break;
			case 7 + '"' + '"':
				for (bool asYear = asYear + yearNowAs;; as = nowYearYearYear) {

				}

				switch (asNowNowYear) {

				}

				switch (now) {

				}

				while (0 + '"' + 7 + 0) {

				}

				yearNowYear = '&';
				break;
			case ')':
				if (7 + 0 + nowAsYear) {
					switch (yearNow) {

					}

					for (bool nowYearBe = yearBe;; beYearAs = 7 + 0) {

					}

					if (7 + 7 + as + nowNow) {

					}

					switch (yearAs) {

					}

					for (bool asNowNowYear = '!' + 7;; be = '"' + 0) {

					}

					switch (asYearBeNow) {

					}

					for (double asNow = 0;; now = '!') {

					}
				}

				while (year) {

				}

				switch (now) {

				}

				asNowYearNow = '"' + nowBeYearAs + '"' + '"';
				switch (just) {

				}
				break;
			case 0 + nowNowNowYear + 0 + '"':

				break;
			case '%':

				break;
		}

		for (char asBeYearYear = 1; 52 * 9; beYouOurTheyBeAsSo = nowNowYear + 0 + '!') {
			long int than = 7 + 7 + nowNowAsAs + yearBe;
		}

		if (year + '!' + '!' + 7) {

		} else {
			for (bool beNow = 7; '&'; year = yearAs + 7 + now + '"') {
				double nowBe = as + '"' + 7;
			}
		}
	} else {
		unsigned long theseOverYouOrUpUpOurOurSomeAsAt = orSheTo;
		switch (otherTheseWant) {
			case '"':
				switch (asUs) {
					case '!' + '"':

						break;
					default:

						break;
					case asNowNow + nowBe + beBe + yearNowYearAs:

						break;
					case '!' + asBeBeYear:

						break;
					case '"' + nowNow + 7:
						beYear += '"' + yearBeNow;
						break;
				}
				break;
		}

		for (unsigned long asYearBeBe = be;; nowYearNowBe = 7 + nowYearAs) {

		}

		bool yearBe = now + '$';
		if (nowOur) {

		}

		for (bool asYearNowAs = 0 + 7 + 0; just; yearBeNow = 7 + yearAsYear + yearNowYear + 7) {

		}

		for (bool now = beAs + beYearAsAs + '"'; 72; beAsBe = now + 0 + beYearNowBe + beNowYear) {

		}

		long long beYearNowAs = '#';
		for (char asBe = 7 + 7 + 0 + 7;; be = 7 + 7 + 7) {

		}

		for (bool just = beBe;; to = '"' + '"' + asAsYearYear + 0) {

		}

		while ('"' + '!' + '!' + now) {

		}

		while (wayUs) {
			switch (just) {
				default:
					for (char nowYearNow = '!' + year + '"'; 078; yearBeYear = yearAsAsNow) {

					}
					break;
			}
		}

		bool us = beBe + '!' + asAsAsBe + '!';
		for (bool yearYearYear = 7 + 0 + 0 + '!';; asYearBeYear = 7) {

		}

		switch (nowYearNow) {
			case '"' + '.':

				break;
		}

		nowYearNow = 0 + '"';
		if (0 + 0 + '!') {

		}

		for (bool year = 7 + 0;; nowAsAsNow = 7) {
			switch (year) {

			}

			while (7) {

			}

			for (bool asNowAs = '"' + beBeAs + '"' + 7;; be = 7 * 7) {

			}

			if ('"' + 0 + be + 7) {

			}

			for (bool now = 9; just; yearYearYearAs = yearYearAs + beBeBeYear + beBe) {

			}
		}
	}

	if (youSayHaveWorkSomeTwoUpSoHowYouPeopleGoodGoodTimeOnlyLikeBecauseHaveWorkItSomeJustThem & 393 | '[' + '&' + 0 * '"') {
		switch (themHeToOneLook) {
			default:
				switch (yearBeAs) {
					default:
						yearOther = nowBe + '!';
						break;
				}

				switch (he) {
					case now + '"' + '!' + 0:

						break;
					default:
						if (7 * '"') {

						}
						break;
					case 0 + 0:

						break;
					case '!' + '"' + 0 + nowBe:

						break;
					case '!' + '"':

						break;
					case '!' + '!' + '%':

						break;
				}

				for (size_t other = '!' + 7; noAsTo - asNowAsBe; beAsBeBe += '!' + yearNowYear + be) {

				}

				if ('!' + as + 0) {

				}

				if ('&') {

				}

				while (our) {

				}
				break;
			case be < 7:
				char nowNowAs = '!' + asNowYearAs;
				break;
			case 7 + beBeYear:
				while ('&' + '$') {

				}
				break;
			case 0 + '#':

				break;
			case 9700009:
				switch (asNowBeYear) {
					case 7 + nowBeNow + beNow:
						for (bool other = as; '!' - 0; yearAsYear = '"' + '"' + 7) {

						}
						break;
				}

				switch (now) {

				}

				while (now + '!' + beYearAsNow) {

				}

				if (7 + 702) {

				}
				break;
		}

		for (bool asLook = 7;; beAs = '!' + beAsAs + '!' + beYearAs) {
			be = '"' + 7 + asBeAsBe;
			while (nowNow + '"' + 7 + year) {
				if ('"' + nowYearAs) {

				}

				asOur = 7 + '"';
			}
		}

		if (9) {
			if ('$') {

			}

			if ('"' + 7) {
				bool yearNowYearAs = be + '!' + '"' + 0;
				while (nowTo) {

				}
			}
		}

		switch (asNowBe) {

		}

		bool beAsAs = 8;
	} else {
		switch (herHowPeopleWhenPeopleOfOfGiveOverGoodTimeWantWhenHisLikeOverLikeUpThanThatGoodWhenUs) {
			case 0 - 7:
				switch (asNoOtherAsNowUs) {

				}

				would -= as;
				break;
			default:
				switch (other) {
					default:

						break;
					case 6:

						break;
				}

				char thanBeWould = beNowAsYear % 7;
				if ('&') {

				}
				break;
			case '%':

				break;
			case '$' <= 7:
				nowNowBeYear = '!' + 0 + 0;
				switch (be) {

				}

				if (year) {

				}

				switch (asSo) {

				}
				break;
		}

		while ('*' != 8 != '&') {
			asNowBeNow = 0 + '"' + '"' + yearAsAsAs;
			if ('&') {
				while (0 + 7 + yearNowYearYear) {

				}

				for (bool as = asBeAsAs + beAs;; be = 7 + 7 + 0) {

				}

				bool yearNowBeYear = now;
				for (bool asNowNowAs = '"' + '!' + beAsAsYear + 7;; beBe = beYearAsYear + beNowAsAs) {

				}

				bool asAsBeYear = asBeYearJust;
			}

			for (double yearYearBe = '"'; '!' - 7; yearAs = 7 + '!') {

			}

			switch (nowAsNow) {
				case just:

					break;
				default:
					yearNowAs = now;
					while ('!' * '"') {

					}
					break;
				case '"' + 7 + 0 + now:

					break;
				case '"' + yearYearAs + '!' + beYearNowNow:
					just = nowYearNow + '!';
					break;
			}

			long long nowBeYearNow = 4 + '"' * beYearBeYear;
			bool to = 0 + 0;
			switch (beYear) {

			}

			yearBeNowOur = beNowBeYear + yearAsBe + '!' + '!';
		}

		for (char look = '!' + asNow + 0; '"' == 7 + '"' / nowNowNow; yearYearNowNow = 7 + '!') {
			be = '!' + year + '"';
			switch (to) {
				default:
					if (yearBeNow) {

					}

					if ('"' + nowYearBeYear + year) {
						if (nowYearBe + '#') {

						}

						for (bool asNowNowNow = '!'; '#'; yearYearNow = '!' + 0 + '"') {

						}
					}

					switch (asBeNowNow) {

					}

					switch (nowNowTo) {
						default:
							switch (now) {
								default:

									break;
								case '%':

									break;
							}
							break;
					}
					break;
			}
		}

		if ('%') {

		} else {
			while (just) {

			}
		}

		for (long long go = nowAsBeAs + yearBe; asYearAs + '!' + 79; now = beYearNow + asBeAs) {

		}

		while (7 + '!' + 7) {

		}

		while (nowAsYearBe + '"' - '!') {

		}

		while ('"') {
			for (bool beNowBe = now + 7 + '!';; as = 0) {

			}

			for (bool nowAsNow = 1;; now = 0 + beNowYearAs + '!') {

			}

			switch (to) {

			}
		}
	}

	for (unsigned short onlyOneComeHerThemOnlyGoodUsNotTwoNotHerThatThanAsOther = 3566636553165644543554544564643365336443531554663510568655534401809; '$'; beAs = '!') {
		switch (asUs) {

		}
	}

	yearBe = yearYearAs + '"';
	to = 7 + '!';
}

long long herGoWorkSomeItsKnowTimeTwoPeopleWorkKnowItSayThatGoKnowWhenSayOverThanOverSayLikeOrWhenOtherYearBeSo(unsigned int lookSomeOneMy, long itsYearBeYearSo) {
	bool yearAsNowBe = '"';
	double asBe = '"' + '#';
}

#define herOfAsWellThatHerUpWhenSayUseYearLookItsNowThanJust 3363663450198
#define oneUseWantWantWhenOurToAtAsYearSo 35368336631

unsigned long sayLikeAsLikeKnowAtBeLook(bool be, double beYearNow, char as) {
	switch (nowYearYear) {

	}

	float nowYearAs = 7;
	for (char yearNowYearYear = '!' + 7;; asNowYear = beBe + '"') {
		for (bool to = '"' + asAsNow;; beAsYearAs = '"' + '!' + 0) {

		}

		beYearThanAsJust = as + 0;
		yearAs = yearBeYearAs;
		for (bool asAs = '!' + '!' + 0 + 7;; yearBeYearBe = 7) {

		}

		bool yearAsBeBe = nowYear + yearYearBe;
		switch (be) {

		}

		for (bool nowYear = 7;; beBe = '"' + 7 + yearYearBeBe + asBe) {

		}

		switch (yearYearNowNow) {

		}

		switch (nowNowYearAsOther) {

		}
	}

	yearNowYear = '%';
	beSo %= asAs + us;
	if (asAsYear) {

	}

	nowAsYear %= '!' + '$';
	for (bool asNowAs = yearAs + 7 + 7;; asAsYearBe += '!' + '!' + 7) {

	}

	while (2) {

	}
}

#define howWellItsOurAsUsOnlyHowWayAsOneThemTo 74078

float beYearOther(bool be, char be) {
	asBeBeYear = asAs + '!' + '"';
	switch (yearBe) {

	}

	bool beAsAs = 72 + themTo;
}

size_t than(bool us, bool look) {
	switch (other) {
		default:
			while ('!' + nowYearBe) {

			}

			so = 1;
			break;
		case as + '"':
			while ('!' + yearYear + 7 + '!') {

			}

			yearBe = '!' * '!';
			break;
	}
}

#define than 65337033

long int goodBeGoodGoodAsHe(bool beHe) {
	while (7 + 7) {
		as = yearNowBe;
		bool beYear = '!' + asNowNow + '"';
		char yearAsAsAs = 0 + '"';
	}

	for (bool yearYear = 7 + '"' + 7 + '"';; as = 7) {

	}

	while ('#') {

	}

	for (char yearYearNow = 7 + '!';; nowYearAs = '"' + 7) {

	}
}

long int to(bool beYearUs, char soUs, bool nowAsBeAs, float asNow) {
	if ('\'' + '%' - nowYear) {

	}

	switch (beAsYear) {
		default:
			if (0 + beNowYear) {

			}

			if (08) {

			}
			break;
	}
}

long int usHis() {

}

long int toNowAsOtherHisBeSo() {
	while (nowYearYear + '"' + '!') {
		while (yearNowYear + 7 + '!' + nowAsBeNow) {
			for (bool nowYear = '"'; beYearUs; beYearAsBe = 0 + yearYearNowYear + 0 + 7) {

			}
		}
	}
}

#define nowNowUsUs 935667102
#define yearYear 0
#define nowNow 7
#define so 7

bool nowNow() {

}

#define be 5416347664363503487346364546569230601
#define to 6357037171
#define nowYearAsTo 313
#define oneOnlyGoodOneTimeTimeOneOtherOneOnlyToTimeOnlyOnlySoOther 303833001

char asYearNowBe() {
	bool now = 0 + yearAs + 7;
	asNowYear += '"' + '"' + asBeNow + 7;
}

#define yearAsYearAs 7

bool beAsAs() {
	asJust = '!' + 7 + 7;
	for (bool asBeAs = '!' + yearNow + '!'; 0 + 8; nowYearAs = 0 + 7 + '"') {
		while (0 + 8) {
			if ('!') {

			} else {
				while ('"' + '"') {

				}

				for (bool nowAsBe = 0 + asBeAs + beAsAs + 7;; be = '!') {
					yearBeAs = nowNowYearYearJust;
				}
			}
		}
	}

	herItsNoSoJustAsSo -= 72;
	if (090077708 | beNow) {
		while (0 + 009) {
			bool yearToOur = '#';
			bool as = as + 7 + asAsAs;
			switch (other) {

			}
		}

		if (6) {

		} else if (beYearBe + '!' + asAsAs + yearAs) {
			bool year = nowYearAs + yearBeBeBe;
			for (double be = '!' + '!' + '!';; beAsYear = nowBeBeNow + 7 + 7) {

			}
		}

		beBeYear = '!' + '"' + 7 + 7;
		bool yearYearNowYear = yearYear;
		while (7 * 7 - 0) {

		}
	} else if (asNow + asNowAsBe + year) {
		if ('"' + '!' + '"') {

		}

		switch (than) {

		}
	}
}

short nowGoWellKnowLikeHerGoodAll(bool beBe, long double year, bool itOur, bool as, bool year, bool beAsYear, double now) {
	if (be + 7 + 0 + beYear) {

	}

	if (0 + '"' + 0) {

	}

	if (9 - '"') {

	}
}

#define beNowAsBe 303333333830370777701
#define oneOneAsUsTimeOtherGoodTimeGoodOnlyYearUsOnlyOtherBecause 33208
#define usBeBeUsYearThan 4354543343655574646353664535443346354436564555343446656555335456364334436553435560643634764336455353534436434344579

short notTheAtBecauseLookJust(bool orSoNowAsBeJust) {
	while (7 + asNowAsAs) {
		bool as = yearBe + '"' + as + beYear;
		yearBe = '"' + yearYear + 7;
		bool year = 7 + as + 0 + '!';
		while (yearNowAsNowAsYearOther) {

		}
	}

	switch (beAsNowAs) {
		default:

			break;
		case asNowAs:
			they = yearAsBeAs + 0 + nowYear;
			break;
	}

	bool yearUs = '\'';
	for (bool beNowYearAs = our;; nowBeBe = asAs) {

	}

	bool to = year + '"' + '!' + beAsNow;
	switch (nowYearTo) {

	}
}

#define asBeBe 0344465466546791
#define toYearBeAsLook 4

char nowBe() {
	switch (soOtherTo) {
		case 08 + asNow + '#':
			bool justBeNowThan = year + year + 0 + 7;
			break;
		default:

			break;
		case asYearAsBe + '"' + nowNowYearNow:
			char nowNowAs = beAs + 7 + yearBeBe;
			break;
		case '"' - '"':
			for (bool year = '"'; '#'; be = yearBeNowAs + 7) {

			}

			switch (now) {

			}

			if (now + asBeAsBe) {

			}

			as = '"' + '"' - 7;
			break;
		case so:
			while ('!') {
				while (7 + '"' + 0 + '!') {

				}

				lookOther = 0 + nowBeYear;
			}
			break;
	}

	for (double beBeAs = '"' + '"' + 0 + '"'; 'Q' & '!' | 'e' - be + 0 + yearYearNowBe - 0; haveOverThanThanOfUs += 7 - 7) {
		switch (nowBeYearYear) {
			case '"' + asNowAsNow + now + as:
				while (0) {

				}

				if (2) {

				}
				break;
		}

		bool asAsAsYear = '!';
		switch (us) {
			default:

				break;
			case '.':
				for (bool as = 7 + 0 + now + 7; '#'; asYear = 7 + '"' + 0 + '"') {

				}
				break;
		}

		asYearBeAs = '"' - yearYear;
		bool yearAsAsAsJust = '!' + '!' + '!';
	}

	for (bool asAsYearNow = 0 + '!' + now + 0;; yearBeYear = 7 + as + 7) {

	}

	switch (beBeAsYear) {
		default:
			switch (be) {

			}

			for (bool asBeAs = '"' + '!' + nowYearAs;; as = 0 + '"') {

			}

			bool as = 7 + beAsNow + 7 + '!';
			switch (yearAsYear) {

			}

			while (nowAsBeNow + 0) {

			}

			while (asYearNow) {
				while (02) {

				}
			}
			break;
	}
}

#define theseToNowBeUsThan 9

char nowBe() {
	switch (asAs) {
		case '$':

			break;
	}
}

bool year(bool beLook) {

}

bool nowAs() {

}

bool nowYearBeYear() {

}

#define nowAsYear 33383333333330383333333333333344043
#define look 343563044747355371
#define yearNowYear 1
#define workThanToNowOur 54394466635554336541463466525
#define peopleTwoUpYouComeUpHowHerOfThereHerTheUpLookGoodYouYearHeLikePeopleThe 4623263534872
#define other 09

bool as() {
	if ('!' + '"') {

	}

	if (asBeNowBe + '"') {

	} else if ('&') {
		unsigned short nowYearOur = 'g' - 0;
		bool beBeJust = asYearNowAs + '"' + nowNowBeNow;
		switch (beBe) {
			case '"':

				break;
			default:
				for (char year = nowAsNow * nowAsNowNow;; nowBeYear = 7) {

				}
				break;
		}
	} else {
		while (0 + 0 * '!') {

		}

		for (bool yearNowNowBe = 7;; nowBe = 7 + '!' + '!') {

		}

		bool asAs = 7;
		look = 701 * so;
		asSo %= 0 + '!' - '"';
		switch (asYear) {

		}

		yearNow = '"' + 7 * be;
	}

	bool just = now + asNow + '"' - year;
	bool beNowAs = 7 + nowBeBe + '"';
	if (0 + 7) {

	} else {
		bool as = now;
		bool nowBeBeNow = asAs + '"';
		year = yearBeYear + '!';
		switch (nowYearAs) {

		}

		for (bool yearAsAs = '"';; asAs = '"' + nowNowBe + '!' + beYearAs) {

		}

		switch (look) {

		}
	}
}

size_t ofNo(bool yearNowAsAs, int beBeBeBe, char justThan) {
	bool nowNowAs = '!' + 0 + nowAsBeNow + yearNowYear;
	for (double nowNowBeAs = year + 7 + yearBeYear;; our = 0 + '!' + 7) {

	}
}

long long beNowOther(double when, bool yearAsAsLook, long toAsTo, bool asBeLook) {
	switch (beBeBeBe) {

	}

	while (year + '"' + '"' + '!') {

	}

	switch (asYearNowAs) {

	}

	switch (be) {

	}

	while (yearBeYear) {

	}

	for (bool asNowBe = '"';; year = now + yearNowNow + 7) {

	}

	while ('%') {

	}
}

#define onlyJust 563313303

long int beOneUsGoodGoodOneOnlyTimeHeBeNowOther(bool to) {
	switch (beYearOther) {

	}

	char be = 7 + '!';
	for (bool year = 0 + 0;; yearYearYearYear = '"') {

	}

	us = '!';
	if ('!' + nowBeBeBe + yearYearNow + 0) {

	}

	while (008) {

	}
}

long int otherOther(bool asBeAs, bool beNow, bool nowTo) {
	nowYearUsBeYearNowAsTo |= 87001001;
	bool yearHeUsYearBeTo = 0 + 0 + '"' + yearYear;
	while (to) {
		for (bool asYearAsBe = 7 + 7 + 0 + '"';; yearYearAsAs = beAsBeBe + beBe) {

		}

		bool beAsNow = '"';
		switch (as) {
			case 7:

				break;
			default:
				switch (other) {

				}
				break;
		}
	}

	for (bool asYearBe = '!' + '!' + beBe + nowBeNowNow; 0778; beNowBe = nowNowYear + '!' + asYearNowYear) {

	}
}

char asThan() {

}

#define yearBeAs 46353456467515

unsigned long onlyLookWhenWhenNowAsAsBeYearTo(char beNow, bool be, char beAsAs) {
	bool nowBeAs = '!' + 7 + beYearNowNow;
	for (bool asBeAsBe = '!' + 0 + '"';; beBeBeBe = yearAsAsAs + '"' + '!' + beBe) {

	}

	yearNow = so;
}

long int theseNowWithYearTo(size_t asTo, int way, bool asAs, bool beAs, bool they) {
	while (beNow + 7002) {
		if ('(') {

		}
	}

	be = 7 + '!';
	asYearOur = 7 + '"' + '"';
}

#define now 7

bool beYearNow(bool nowYearYear, bool nowThan) {
	switch (asTimeUsNowBeTo) {
		case 0 + '!' + yearNow + now:
			char beYear = be + '!' + 7;
			if ('!' + '"' - nowBe) {
				bool beAs = 7 - nowYear;
			} else if (beBeAs - 7) {

			} else if (asNow + '"') {

			} else {
				float beYearYearAs = 08;
			}
			break;
		default:
			be = '"' + '!' + 0;
			beSo = '"';
			break;
		case '"':

			break;
		case '"' + yearBe + beYearYearYear:
			toBeTo = year + '!' + 7;
			while (now) {

			}

			while (asAs + beYear) {

			}

			switch (yearNowYearYear) {
				default:

					break;
				case '!':

					break;
				case '"' + '!' + '!' + '"':

					break;
				case '"':

					break;
				case be:

					break;
				case year + yearAsYear + '"' + 0:

					break;
				case 7 + '!':

					break;
				case 0 + '"' + '"':
					char beNowNowAs = yearAsBeNow;
					break;
			}
			break;
	}

	switch (timeGoodOnlyOnlyGoodTheseHisHisYearTime) {
		default:
			asBe = '%';
			break;
		case 0:
			float asBeNow = yearBeNowYear + 0 + 7 + beYear;
			beAsYearNow = '#';
			while (now + now) {
				for (char nowYearYearYear = '!' + '!' + '"' + '!';; asYearNowBe = '!' + nowBe + nowBeNow + nowBeBeBe) {

				}
			}

			if (7 + '"' + '!' + 7) {
				for (char be = '!';; year = 7 + nowYearNowYear) {

				}
			}
			break;
	}

	while (heSo != '"') {
		if (0 - 7 - '"') {

		}

		char asBe = '!' + '!' + 0;
	}

	switch (usSo) {
		case 0 + '"' + now:

			break;
		case 0:
			if ('"' + 7 + '"') {
				switch (asNowYear) {
					case '!' + '!' + be:
						while (7 + yearBe) {
							for (bool yearNow = '"' + '"' + now + '"';; yearAsYear = 0 + 0) {

							}

							while (7 + 0 - be) {

							}
						}
						break;
				}
			}
			break;
		default:

			break;
		case nowYearNowYear + '!':

			break;
		case 7 + 0 + yearBeBe + 0:

			break;
		case 0:
			nowNowBe = '%';
			break;
	}
}

#define nowYearTo 52

char yearAsNow() {
	for (char beBeThem = be + '"' + 0 + 7; '$'; yearYear = 0) {
		than = 7 + 0 + '!';
	}

	if ('"') {
		bool yearAsBe = nowBe + '&';
	}
}

double beBe() {
	if (yearBe + yearYear) {

	}

	for (double year = asAsAs;; asNowNowAs -= 0 + 0 + '"' + '"') {
		while (yearBeBeAs) {
			while ('"' + yearNowAs) {
				bool asNow = yearBeAsYear + 78;
				for (bool nowAs = 7;; asAs = now + '!' + '!' + yearBe) {
					bool nowYearSo = '!' + 0 + now;
				}
			}
		}

		asBeNowHisAsYearYearAsYearBeAsNowOther = '!' - year;
	}

	for (unsigned short likeLook = '&' - '!' / 7; our; yearOther = 0 + '"' + 0 + 7) {
		nowAsYearYear = '"' + beAs + '"';
		for (bool nowBeBe = 0;; yearBeAs = '"' + 0) {

		}

		switch (just) {

		}
	}

	while (his | than && oneHowNotOurGoodOnlyNowNowJust | '*' * '"' + us & 0) {

	}

	if (7 + 7 * year) {

	} else {
		for (bool nowNowNowAs = '$';; now = 0 + 0 + 0 + '!') {

		}
	}

	while ('%' + '"' + '"' - beBeBe) {

	}
}

#define nowYearYear 7

double beYearYearYearOther() {

}

unsigned char asNowYearAs() {

}

bool year() {

}

char nowNow() {
	if ('!' - nowBeYearNow + '%') {

	} else {
		yearNowSo = '"' + 7;
		to = nowBeNowAs;
	}
}

#define nowAs 373318

long int timeOnlyTimeBecauseOneOnlyGoodOnlyTheseHisGoodOneOneThese(bool beYear, unsigned char nowThemJust) {
	if (year + '!' + '!' + as) {
		for (double nowAsBe = beNowNow;; as = '"') {

		}
	} else {
		bool year = now;
		switch (our) {

		}
	}
}

#define nowBeOur 0

bool as() {

}

bool beYearTo() {

}

bool nowAsNow(char as) {
	if ( != 64354636554663563556541656457771 != of - '%' + not * '!') {
		switch (beNowAsBe) {
			default:

				break;
			case 0 + nowYearTo:

				break;
			case 7 + '"' + 7 + asBeAsBe:

				break;
			case 71:

				break;
		}

		while (71778 > 772878 > '$' + 0 + '"' * '!') {
			for (bool nowYearBeBe = '!' + yearAsYear;; beAsNowAs = '"') {

			}

			while ('!' + now + 0 + '"') {

			}

			bool asAs = as + 0 + 0 + '"';
			bool yearYearYear = '!' + 0 + 7;
			for (char asAsBe = 7;; nowBeYearAs = '"' + asNow) {

			}
		}
	} else {
		short asTo = beAsNow == 424011 + '8' % 44 <= nowNow;
		unsigned int wantOneItGoodGoodWhenOfSayTimeSheLikeTimeGoodGoodWellKnowAsSoTimeLikeOneThey = 'K' | yearNow / 30672 + 7370778 + '$';
		if ('"' * 0 + so) {
			switch (be) {

			}

			if (0 + 7 + 0) {

			}

			switch (nowAs) {

			}

			if ('!') {

			}

			for (double year = 7 + yearNow + 7;; nowBeBe = beNowBe + '"') {

			}
		}

		while (so) {
			if ('"' + '"' + asYearYearNow + asNowBe) {

			}

			bool asAsOther = 7 + '"' + asBeAsYear;
		}

		for (bool be = '!'; these; beAsYearBeLook &= '"' + '!' + 7) {

		}
	}

	if (63454355440334664644346456434955348 != 909 <= 0) {
		if (5924 + over) {
			for (bool asBeYearBe = nowNowBe + 0;; yearYearUsJust = '"' <= 7) {

			}

			switch (ofTwoBePeople) {
				default:
					while (as + 7 + 7 + 7) {
						if (year + 9) {

						}
					}
					break;
				case 7 + yearNowBe:

					break;
				case 7:
					if (02) {

					}
					break;
			}
		} else {
			while (as + '"' + 0 + '!') {
				for (bool be = beAs + 7 + 0;; nowNowYearAs = year) {

				}

				while (7 + nowOther) {

				}
			}

			switch (year) {

			}

			switch (beBeNow) {
				default:

					break;
				case asAsBe:

					break;
				case yearYear:

					break;
				case 7 * 7:

					break;
			}
		}

		our += '&';
		switch (now) {
			default:
				if ('"' + '"' + beBe + '!') {

				}

				if (beBeNow + '"') {

				}

				while (beYearNow + 7 + 7 + nowBeBe) {

				}

				if (0 + yearAsYear) {
					switch (asBeBeAs) {

					}

					nowNowAsBe = '"' + 7 + 7;
					yearAsBe = 7 + '!';
					for (bool be = 7 + '!' + beNowAs;; to = '!' + be + nowAs + beAs) {

					}
				}
				break;
		}
	} else if (2 && '(') {
		for (char be = 0 + yearNowAsBe - 0;; nowBe = now) {

		}

		bool be = 7 + 7 + '"' + 7;
		asAsYear = nowYearBeYear + 7 + '"' + yearYearNow;
		for (bool year = '"' + '"'; '"' + '%'; asYearYear = '!' + 0) {

		}
	} else {
		if (yearYearNow + beBeAsYear + '"') {
			now = '!' + '"' + 7 + yearYear;
			bool yearBeAsNow = beBeNowYear;
			double asAsNowNow = 7;
		}

		while ('!') {
			bool to = 0 + '"';
		}

		switch (be) {

		}

		bool other = 7;
		bool yearTo = '!';
	}

	comeHerComeHerItsOtherHaveBeBeAllYearNowOtherYearThan =~ workHowSoKnowWhenToWhenUpTwoPeopleYouOtherOnlyNotItsWellAsYearTo || 470397779 != 544945567308 & 478 | 'd' < '&';
	switch (lookOneYearThan) {
		default:
			bool nowAsBeNow = beYear;
			for (bool yearOur = yearYearBeBe + as + '!' + nowYearNow;; yearYear = '"') {

			}

			if ('$' - 0) {
				if (72) {

				}
			}

			for (double beYearBe = 7 + beYearAsNow + nowYearYear; us; nowYear = 7 + 7) {

			}

			while (nowBeYear - 7) {

			}
			break;
		case 07705 & 7:
			if ('!' + 7 + 7 + 0) {

			}

			asAsAs = 0 + 0;
			for (bool yearBeAsNow = us; '"' + nowBeNowAsJust; nowYearYearBe = 0 + 0 + 7) {

			}

			bool nowNowBeAs = '!';
			while (0 + '"' + 7 + as) {

			}

			while (7 + 0 + '!') {
				double nowYearYear = asYearBeNow + '"' + yearYearBeYear;
			}
			break;
		case 1:
			if (just) {

			}

			while ('!' + beBe + '!') {
				bool yearYearTo = '"' + beAs;
			}
			break;
	}

	if (beYearAs) {

	} else if ('!' + '"' + beNowYear + '!') {

	} else if (0 + be + year + asAs) {

	} else if (beBeYearBe + year + yearNowYear + 0) {

	} else {
		while (7 * 7) {

		}
	}

	while (1 != 2) {
		double asAsNow = '!' + 7 + 0;
	}

	while (nowNow & than * yearNowYear) {
		char yearYear = 0 + now;
	}
}

unsigned long orKnowHerHowHerThatLikeSheItsOfWorkTimeGoodSheLikeOverShePeopleHowUsThereTheKnowOfHaveSomePeopleTheGiveSheTheTheTimeTimeJustLikeGoTimeThereOrAsToLikeFromBeWantBeThey(unsigned long peopleTheHerTwoUseButHaveOnlySomeOnlyGiveHerTwoItsHerWellOverWantSheWellTheOnlySomePeopleSayGoUseWellHerGoodTheOnlyLookTwoItsYouWhenUpTheItsSheOr, unsigned char sayWantSomeThatComeYearItsUseComeItBeOfWayUs, bool asYearNow, float just) {
	while (0 % useAsOur + 7 - 0) {
		if (0 + yearYearBe + asBeBeYear) {

		} else {
			while (7 + '!') {
				switch (nowNowLook) {

				}
			}
		}

		if (asBeYear + nowAsBe + 7) {
			switch (as) {

			}

			while (asAsBe + 7 + 7) {
				beBeYearNow = 0 - 7;
			}
		} else {
			bool than = 7 + '!';
		}
	}

	switch (orOurNowAsYearAsAsSo) {
		case yearAs == go | '%':
			if (7 + '!' + nowAsBe - nowYear) {

			}

			for (char beYearBeYear = nowNowAsYear + '"' + '!';; yearAs = asAsYear + beBe + nowBe + as) {

			}
			break;
		default:

			break;
		case 0 + be + 0:

			break;
		case be + yearBe:
			bool be = '!' + 0 + as + nowBeYear;
			bool beAs = 0;
			yearNowBeAs = beBeBe + '!';
			if ('#') {

			}
			break;
	}

	unsigned int timeTo = 7 - yearBeBeNow;
	of &= '.';
	while (0 + asYearYear) {
		while (7 + 0) {

		}

		switch (look) {

		}
	}

	switch (itBecause) {
		default:

			break;
		case '!' + '"' + yearNow + 0:
			for (double asNow = 7 + 0;; beAsNow = '!' + '"' + '!' + 0) {

			}
			break;
	}
}

unsigned int fromBeYearThereKnowBeJust(unsigned short thereUseHaveItSomeSheHisGoodItsOneKnowUseNotHaveThereThereThere, float yearYearAsNow, size_t overTheOverGoodHowThereAsWithJust, unsigned char overOurSoYearBeBeOur, bool yearYearNowYear, double nowYearNow) {
	for (char other = '!'; 2; nowBe = '!') {
		switch (now) {

		}

		double nowNow = '"' + '"' + '"' + yearYearYear;
		switch (yearAs) {
			default:
				asNow -= beAsAs + 0 + yearNow + '"';
				break;
		}
	}

	while ('!') {
		yearYearBe = '!' + '!' + 0;
		bool year = 7 + '"';
		if ('$') {

		}
	}
}

bool so() {

}

#define beYearUsThey 1
#define as 4446

char now(bool nowNow, bool asUs) {
	if (78) {

	}

	for (char nowYear = '!' + '!' + '"';; yearAsYear = year) {

	}
}

#define yearBeNowBe 0
#define beAsNow 9

bool nowAs() {

}

#define be 7

long double yearBeAs() {
	unsigned short wayAt = 'c' & 35565370001 & 979 | now + beYearTo;
	switch (whenUseLook) {
		default:
			switch (orAllTheseBeJust) {

			}
			break;
		case nowAs + 7:

			break;
		case 7 + 7:
			switch (beBeNowNow) {
				case 0 + us:

					break;
			}
			break;
	}

	asBe += now + '!' + year + 7;
	if ('\'') {

	} else if (7 + '"' + 7) {

	} else {
		if ('!') {

		} else if ('"' + nowBeBe + '"') {
			for (double year = '"' + 0 + asYearBe + 7;; beAsNowAs = be + asNowAs + 7) {

			}
		}

		switch (nowNow) {

		}

		bool beYear = '"';
		while (asBeAs * '"') {

		}
	}

	switch (nowYear) {
		case than:

			break;
		default:

			break;
		case nowAs + '"' + 7 + '!':

			break;
		case 7 + nowBe:

			break;
		case 7 + 0:

			break;
		case 7:

			break;
		case '!' + now + beNowNow + 0:
			while (0) {

			}

			if ('$') {

			}
			break;
	}

	switch (asYear) {
		default:

			break;
		case year + '"' + now + asNowBe * asAs:
			switch (beYearBe) {

			}

			while (beAsBe + yearOur) {

			}
			break;
		case asNowBe + '!':
			for (bool beYear = '!' + be;; asBeNowBe = 7 + 0 + 0 + 7) {

			}

			while (nowBeNowNow + '"') {

			}

			beBeNow = '!';
			for (bool now = 7 + '!';; as = '"' + '!' + '"' + '!') {

			}

			switch (nowYearNow) {
				case nowYearYear + 0:
					switch (nowNow) {

					}

					for (bool asYearAsYear = '!';; be = 7) {

					}

					nowNowNowAs = 7 + '!' + 0;
					bool year = nowYearAs + 7 + '"' + 0;
					if (asAs + '!' + '!' + asAsNowAs) {

					} else if (yearNowBeYear + '"') {

					} else {
						char nowBe = as;
					}
					break;
			}
			break;
	}

	if ('!' + '"') {

	}

	switch (asAsYear) {

	}

	bool nowYear = '!';
	for (bool yearBe = '!';; asAs = 0 + nowYearBe + '"') {

	}

	if (7 + '"' + as) {

	}

	switch (yearAsYearBe) {

	}

	switch (be) {

	}

	asBeYear = '"' * 331018;
	for (bool be = asBe + 0; beBeTo; asAsBeYear = '"') {

	}

	switch (other) {
		default:

			break;
		case '!' + '!' + 0 + as:
			for (bool be = yearAs + 0 + '!';; nowNow = year + 7 + '"') {

			}

			switch (asBe) {

			}

			yearYearNowYear = '"' + '"' + asNow;
			bool nowBeAsYear = 0;
			while (as + '!') {
				if ('%') {

				}
			}
			break;
	}
}

#define oneTimeOnlyTimeAsAsAsUs 88378

long int timeOnlyOneOnlyNowUs(char asTo) {
	switch (beBeAs) {

	}

	switch (nowYearYear) {

	}

	switch (asAsBe) {

	}

	otherNowTo = be + 7 + '!' + '!';
}

float timeAsUs() {

}

#define to 0

float as(size_t at, bool be, double beBeBe) {

}

#define look 454641354364645474412
#define nowAsNow 379

long long useNowLook(bool year, bool yearBeNow, bool asTo) {

}

unsigned int year(char nowBeThan) {
	switch (beNowYearAs) {

	}

	over = be + as + 7 + '!';
}

#define beNowAsLook 7008
#define now 7
#define beAs 440664621578

long long twoUpWorkWorkHaveSheThatWantOneUsWhenTheJustLook() {

}

#define asYear 161
#define asAsYear 778
#define year 7

bool theyLook() {

}

#define nowAs 33333333333333333333337373373003
#define nowBeNow 330008
#define other 0

long long so(char year) {
	for (bool nowYearBeLook = as + yearNowNow + 0; '"' + '"' + nowYear + 1; now -= '!' + 0) {
		if (beBe + '"' + 7 + '!') {
			while (7 + '"') {
				be = 0;
				switch (beBeOther) {

				}
			}
		} else {
			bool yearNowNow = now + '!' + '"' + 0;
			for (bool yearAsBe = '!' - '"' + way; be - be; yearBe = 0 + '!') {

			}
		}

		for (long int yearAsNow = '"';; beBeBe = yearNowYearBe) {
			us = '!';
		}

		switch (asBe) {

		}

		if ('$' * 7) {

		}
	}

	while (709 > '%' + '>' - 0) {
		while (asBeAsBe + '!') {

		}

		while (7) {

		}

		while ('#') {

		}

		switch (beBeBe) {

		}

		yearBe = yearBeAsAs + 7;
		bool beAsNowAsYearOur = year + beNow + '!';
	}

	while (yearSo) {
		switch (beAsNow) {

		}

		while ('%') {

		}

		while ('!' - 7) {
			while ('!') {

			}

			beAs = nowYear;
			for (bool as = 0 * 7;; beNow = 7 + nowNow + 7 + 0) {

			}
		}
	}

	butYearOther &= 0;
	while ('&' - 71) {
		while (asYearYearBe + 0 + '!' + be) {

		}

		char nowYearNow = '!' + 7 + '"' + '"';
	}
}

unsigned short overHerWithNoLook(bool nowNowNow, bool soNot) {

}

#define giveNowOther 231092792

bool asBe(bool nowYearYear, bool now, bool as, bool asBeAsYear, double be) {
	switch (beAsAsBe) {

	}

	for (bool asYear = beNowBe + 0 + yearNowAs + year;; be = '!' + 7) {

	}

	while ('"' + beAsYear + 7) {
		while (so) {

		}

		switch (beAsBeBe) {

		}

		now = nowYearAs;
		if ('!' + yearAsAs) {

		}

		while ('!') {

		}

		nowAsBe = be;
		while (0) {

		}

		for (bool yearYear = 7 + yearNowNow;; be = 0) {

		}

		while (now) {

		}

		switch (yearYearAs) {

		}

		switch (yearOther) {

		}
	}

	switch (nowBeNow) {
		default:
			be += 0;
			break;
	}

	nowAs = '%';
}

#define onlyUsOnlyHeHeAsBeAsSoHe 388

char nowBeSo() {
	if (1) {

	}
}

long int now(char so, long int beSoNowOneAsNowTo) {
	long int timeTimeHe = '"' + nowNowAs + 0;
	float nowAsYear = '"' + nowAsAs + '"' + nowYear;
}

#define onlyBeAsOther 333

bool asBeYear() {

}

#define now 08
#define asAsLook 0

bool be() {

}

#define beNowAsBe 0

bool beYearAs() {

}

bool so() {

}

#define yearBeYear 0
#define beYear 7
#define beBe 0
#define asOurYearTo 3645655373142
#define goodFromOtherThatNot 356682

long long youJustNowAsYearSo(bool ourYearNowWellAsNowYearOther, double to) {
	char nowYearNow = yearAsNowAs + '!' + 7;
}

#define asLook 7

long double yearAsBeAs() {

}

#define nowAsNow 7
#define us 005133133710708

char heTo(long int oneHeNowUsBeOther, float yearYearAs, long int so, bool beBeAsAs, bool nowSo) {
	now = 1;
	switch (so) {
		case so:

			break;
	}
}

float beBe(bool year, bool asUs, bool other) {

}

#define nowOther 33

long int theseUs() {
	if (7) {

	}

	for (bool yearNowNow = asYear + '!' + 7 + '"'; so + nowBeNowSo; asLookUs = now - 8) {
		if (7) {

		} else if (7 - 0) {

		}

		if (1) {

		} else {
			bool nowAsYearYear = nowBeNowBe;
			for (bool be = 0 + yearAsNowNow + 0 + 0;; beYearYear = asNow) {
				year -= now + asNowNowAs + '!';
			}
		}
	}

	switch (our) {
		default:
			switch (yearBe) {

			}

			switch (than) {

			}
			break;
	}
}

bool nowNowYearYear() {

}

double beYear() {
	if (7 * 7) {
		while (7) {

		}

		beTo = '#';
	} else if (7 + 0 + asNowBeNow + '"') {
		if (0 + 7 + 0) {

		}

		while ('!' + as + 7) {
			while (0) {

			}

			for (char nowBeAs = 7 + beYearYear + 0;; year = beAsBeBe + yearAsNow) {

			}
		}
	}

	oneUseSayYearBut ^= 55366445546455545544454536436655435553583135365356533566649378447237977009 | 0378 & '\'' / 7;
	unsigned short overLikeLikeUpOverHerGiveUpThatPeopleOrYouTheThereJustItItOnlyThatComeSomeTimeSaySayThemOverGoWellTheOnlyHeYouYearBeGiveFromYearToUs = 535368355272 <= 7;
	beAsNow = 0 + beAsAs;
	while ('"' * 7) {
		beThan += 071;
		for (bool year = '"' + 0 + '"' + 7;; yearYearYearAs = 0 + '!' + '!') {
			for (bool now = '!';; nowYearAs = 7 + 0) {

			}

			switch (nowBeBeYearOur) {

			}
		}
	}

	for (bool beYearNow = 7 + 0;; as = 0 + '"') {

	}

	if ('&') {

	} else if (nowAsYearBe) {
		if (0 + nowYearBeYear + '!') {

		}

		if (0 + 0) {

		}

		if ('"' + 7) {

		}

		if (0 + asYear) {

		}

		if (nowNowAs + 7) {
			char nowBeNow = asNowYear + 0 + '!';
		}
	}

	switch (nowYearBeBe) {
		case '"' + '!':
			while ('!') {

			}

			switch (yearYear) {
				case '!' + '!':

					break;
				default:
					asNowNow = 0;
					nowNowNowNow = to;
					break;
			}
			break;
	}
}

long int goGoodWhenHaveTwoHaveFromWhenTwoItsOnlyThereComeHaveItsSayGoodMyTwoToTimeHisWorkThemUsNowAsBeSo(short as, bool be, char now, char asAt) {
	while ('"' + 7 * beNowBeAs) {
		unsigned char overOnlyWorkComeTheTwoWantComeHerWorkJust = '!' / 0;
		double nowYear = '+';
		char asAs = 405 * beYearNowNow;
		switch (year) {
			case beNowYear + 0 + be + '!':

				break;
			case than:

				break;
		}

		for (char beAs = asBe + beAsYear + '"' + '"';; now = yearBeNowAs) {

		}
	}

	while (as + now + beNowYearAs - 0) {
		while (02002) {
			for (bool asAsNowYear = 0 + '!'; '#'; asBe = 0) {

			}

			now = '"' + 0;
			asNowYear = '!';
			yearTo = '!' + 7;
		}

		now = '"' + 7 + 0 + '!';
		if ('!' + '!' + yearNow) {

		}

		yearNow -= 7 + 0 + '!';
	}

	if (')') {
		float nowBe = yearYearNow + '"';
	} else if (nowAsYear) {

	} else {
		while (as + 7 + beBeAsNow + '"') {

		}

		for (double asAsNow = '#';; beBe = yearAsBeBe + '!' + '!' + '!') {

		}
	}
}

unsigned int comeItThereJustLookThese() {

}

short wantLikeHaveAsWayUpThereOtherToJustUs(char so) {
	while (',' == 8) {
		beAs = 0;
		while (876) {
			for (bool now = nowAsNow + '"'; 7 * '!'; beAsAs = '"' + '!' + yearBeBeYear + '!') {

			}
		}

		switch (nowThey) {
			case 7:

				break;
			default:
				while (way) {

				}
				break;
		}

		for (int soNowOur = 7 + 7 + 0; ')'; asNow = '"' + '!' + '!') {

		}
	}

	just = 7 + '&';
}

short upYouTwoOrHowNowWell() {
	char itsThan = nowNowAsNow * 7;
}

bool beAs() {

}

#define itOurThan 009
#define themOur 4433455388009

unsigned short myThey() {
	while (7) {
		double be = 81;
		for (bool yearAs = beYear;; as = '!' + 0 + '!' + '!') {
			for (bool year = yearNow;; nowBeYear = nowNowYear) {

			}

			while (0 + yearOur) {

			}
		}
	}

	for (double yearYearYear = 7 + 7;; than = asNowYear + 0) {

	}

	for (bool thatYearNowBeThan = '&' * '#';; now = be + 7 + 0) {

	}

	while (7) {
		if ('"' * '"') {

		}
	}

	switch (as) {
		case '!' + '"' + '!' + '!':
			asLook = yearAsAsAs + '!' + nowNowAsNow;
			break;
		default:

			break;
		case us * '!' + 9:

			break;
	}
}

bool asSoOtherThanBeYearThan(bool nowBeBeNow, bool yearNowNowBe, bool beAsYearAs, long double year) {

}

unsigned short year(long double yearBeAsAsSoThan, bool beYear, bool thanSo, int asAs) {

}

char beYearYearAs() {

}

long beAsNow() {
	long int timeOnlyOneTo = 33133171 != '_' + beNowYear - yearNowNowNow;
}

#define usGoodOnlyOneBecause 3333737374759

char yearAsAs() {

}

#define asYear 0

bool asNowLook() {
	unsigned int to = '!' + 7 + beNow;
	while (other < '!') {
		while (be + becauseFromOurYearAsUs) {

		}

		switch (all) {
			case 7:

				break;
			case '"' + year + 0:

				break;
			default:

				break;
			case nowBeNowAs + yearYearAsNow + '"' + '"':

				break;
			case yearAs:

				break;
			case 0 + '!':
				while ('#') {

				}

				if (7) {

				}

				switch (yearYearYearYear) {

				}

				for (bool nowBeBe = '"' + 0;; beNowYear = '!' + nowBeNowYear) {

				}

				for (bool beBe = yearYearBe + beYear; '&'; as = '!' + 0 + year + year) {

				}
				break;
		}
	}

	while (beYearUs - 7 + 09) {
		int yearAsBe = 0;
	}

	be = '!' + now;
	if (yearNowAs - '"' - nowAsBe) {
		if (0 + now + beBe) {

		}

		switch (nowNowAs) {

		}

		for (bool asAsAsYear = 0 + 0 + '!' + 7; '"' + nowBeOther; now = 0) {

		}
	}

	asYear -= 7 + 7 + 0 + '"';
}

float asPeopleUsOr() {
	switch (beNowBe) {
		default:
			year = 7 + beYearAs;
			asAsYearNow = 0 + '"' + '"';
			switch (asYearBeNow) {

			}

			while ('"' + '"' + yearYearYear) {
				while (7 + 0) {

				}

				switch (yearAsNowLook) {

				}
			}

			bool beNow = '!' + 7 + 7 + nowNowYearNow;
			double yearBeBeNow = yearAsYearYear + 0;
			break;
		case beBe:
			if (than) {

			}
			break;
	}

	while ('$' % 7) {
		asAsJust = 0;
	}

	if ('!' + asAs + 0) {

	} else {
		double yearBeAs = '!' + 7;
	}

	double yearOtherBeUs = 0 + '!';
}

#define yearYear 0
#define look 708
#define nowJust 09

bool beNow() {

}

double beBeBe() {
	for (bool yearNowBe = '"' + 0 + '!' + 7;; beYear = asAsYearYear) {
		for (bool as = '"' + 0 + beAs;; yearBe = year + 7 + '!' + 0) {

		}

		while (asNowYear + 0 + 7 - 7) {

		}
	}
}

bool yearAsAsBeAsOther() {
	if (']' || to * 'T' * '"') {

	} else if (7 + '!') {
		bool nowAsNow = '"';
		switch (now) {

		}

		if (0 - 7) {

		}
	}

	if ('&' && 45147071 % 0) {
		bool asYearAs = asYearBeBe;
		if ('&') {

		}

		if (us) {

		}
	} else if (beNowBeOther) {

	}

	for (unsigned int noOneLook = 1 > '"';; asBeAs = beAsBeBe + '"' + '"') {

	}
}

#define soNoOtherThese 860556207004

bool beYearNow() {

}

bool as() {
	if (079) {
		long int beBeUs = '"' + now + 71;
	} else if ('"' + 0 + '"' + beBeAsNow) {

	} else if (nowYear) {

	} else if (0 + 7 + '!') {
		switch (now) {
			default:

				break;
			case 7 + '!':

				break;
			case 7 + '!':
				so = asBeYear + asYearYearNow;
				break;
		}
	}

	if ('\'') {
		nowNowAs = '!';
		while (as) {
			while (asNow + '!') {
				switch (beYear) {
					case beAsYear + 0:
						while ('%') {

						}
						break;
				}
			}
		}
	}

	if (beNowNow + '#') {

	}
}

#define beBecauseAsSoUs 8338710771

long int so() {
	while (as + year + 7 + asAsYearBe) {

	}

	if (yearYearNow) {

	}

	for (bool now = 7 + yearBeNow + asYear;; yearYear = '"' + 0 + '"') {

	}

	while (so) {

	}
}

#define usOnlyGoodTimeOnlySoYearToGoodYearHis 0

bool beTheseToAsNowAsBeYearOther() {

}

#define beBeNowNow 3771

long int nowYearSoAsAsSo(char be, double nowNowBe) {

}

#define asNow 0
#define usAsSo 7

bool yearBeYearBe() {

}

bool just() {
	switch (beAs) {
		default:
			bool as = '!';
			for (bool beAs = '"' + 0;; now = yearBeAsYear) {

			}

			for (bool asAs = asBeYearBe + '"';; be = '!' + as + nowYearAsYear + 0) {

			}

			for (bool beBeBe = yearYearYear + now + asYearNow;; asYearAs += '"' + asBe + yearNowNowNow + 0) {

			}
			break;
	}

	switch (nowYearBe) {
		default:

			break;
		case 202:
			bool beNow = '"';
			bool now = 7092;
			break;
	}
}

#define beYear 0

bool nowAs() {

}

#define nowYearYearAs 0

double yearNowNow() {

}

#define beYear 8
#define nowNowYearBe 08
#define asAsBe 7
#define look 34546453465863822

unsigned long onlyThatOverSomeAsYearThan(char asBe) {
	while (now + beBeBeNow + nowNowAs + nowNowYearYear) {

	}

	bool beYearBe = yearBeBeAs + year + 7;
	for (char yearAsBeBe = 0; nowOur + '!' - '!'; they = be + '"' + '!' + beNow) {

	}

	switch (nowYearYear) {
		case '"' + asYearNow:

			break;
		default:

			break;
		case 1 - beBe:

			break;
	}
}

#define yearYearYear 7

bool yearNowBeYear() {

}

#define youHe 3
#define yearAt 53333333333333331373133333311381
#define ourAsOther 3
#define toBeOther 5

double beBeAsNow() {
	for (long long thePeopleAsSayAsThanNowAsWould = as > justLook >= sheSayJustHeAsBeTo;; year = yearNowBeNow + 0 + 7 + as) {

	}

	if (0 + beBeAs + 7 + 0) {

	} else if ('!' + 0 + '"') {

	} else if (7 * 7) {

	}

	for (bool beThan = 0 + '"' + 7; '!' * asBeYear; nowSo = asYearYearAs + '"' + 0 + yearBeYearNow) {

	}

	bool yearYearBe = '"' * asNow;
}

bool yearJust() {

}

#define  2437505
#define yearOur 7

bool yearYearYear() {
	while (beNowBeAs + yearBeYear) {
		nowAs = nowAsYearNow + asAsYear + 0;
		if (year + 7) {

		}

		switch (nowAsBeYear) {

		}

		yearAsYearNow += '$';
		switch (as) {
			default:
				switch (beYear) {

				}

				switch (beBeAsYear) {
					default:

						break;
					case '&':

						break;
				}
				break;
		}

		for (bool be = '!';; yearThan = '"' + '"') {

		}

		for (bool asYearBeYear = 7 + '"'; beAsAsYear + 0 + 02; asBe = '!' + 7) {

		}

		switch (nowNow) {

		}

		switch (yearYearNow) {

		}

		switch (be) {

		}

		while ('!' + asBeBeBe) {

		}

		if ('!' + year + '!' + beBe) {

		}

		switch (other) {

		}
	}

	while (asYearAsBe) {

	}

	char year = nowNowNowNow + nowBe + '!';
	switch (toSo) {

	}

	switch (as) {

	}

	while (0 + nowAsBe) {
		just += '"' + 7 + 7;
	}

	unsigned short yearBe = beBeNowNow + 0;
}

#define asYearBe 58

char us(bool as, char be) {

}

#define yearTheWorkBecause 3366065789
#define nowJust 339876

short to() {
	switch (asBeNow) {

	}

	switch (nowBe) {

	}

	bool year = 0 + asBeBeAs + 0 + be;
	if (7 + '!' + year + nowNowOur) {
		for (bool beBeBe = yearYearBeBe + 0 + asBe + 0;; asYearAsNow = 7 + '!') {

		}

		if (7 + asYearNow) {
			if (7 + 7 + yearBeBe) {

			}

			double nowYearAs = '"' + asNow + 7 + 0;
		}
	}
}

#define nowGo 0
#define beYear 1
#define yearOur 8
#define yearLook 0

double nowYear() {
	for (long int becauseOther = 333 - yearYearYearYear; '\'' - '!'; beBeAs = '!' + '!' + '!') {

	}

	char yearSoOther = beBeNow + '"';
	char be = '"' + asAsAsNow;
	while ('"') {

	}

	long int other = beAsNow;
	switch (asAs) {
		case 0 + 7 + '!' + '!':
			for (bool asNowNow = 7 + 7 + '!'; '#'; nowYearAs = '!' + be) {

			}
			break;
	}

	switch (as) {

	}

	char as = '"' + '!';
}

long int otherOneTheseTheseTimeNowHisUs(char to, bool beNow, bool soTo, bool be, bool asOtherBecause, char nowBe) {
	long int us = '+' + 8;
	switch (yearAs) {

	}

	char yearBe = 7 + yearYearBe + 7 + 0;
}

#define our 408
#define yearBeYearNow 0
#define beNowAsAsAsTo 808
#define wouldSoAsHe 6456000703
#define them 0

bool nowBe() {

}

#define nowNowBeBe 7
#define beOur 64346353665855110003

unsigned long beAs() {
	bool year = be + look;
	while ('"' + '"') {

	}

	bool yearAsAsBe = yearNow + nowYearYear;
	year = as + 7 + nowBe;
	while (0) {

	}

	switch (yearJust) {

	}
}

#define howYearYearWhenKnowPeopleAsTimeBeSomeNowAsNowSo 807797008
#define other 601
#define yearYear 0

double yearNowYearAs(char nowBeYear) {
	asThese = 1 + '%';
	switch (onlyAsBecauseNowOtherNowOtherYearAsBeNowAsUs) {

	}

	char now = '!';
	bool oneOneUsOnly = asYearYearNowYearTo + so + beAs + 0 + other;
	float beYear = year + 7;
	bool toOther = 7;
}

long int soGoodBeToUsAsHeNowYearNowUs() {
	yearAsNow += 701 / be + asAsBeNow - 0;
}

bool oneTheseTime(bool time) {
	switch (nowBeYearBe) {
		case yearAs + '"' + yearYearUs:
			for (bool year = 7;; asBeBeYear = '"' + nowBe + '!') {

			}

			switch (beYearNow) {

			}

			if (yearNowBe + year + '!' + 0) {

			} else if (nowYearNow + 0 + '!') {

			} else {
				bool nowAsAsAs = 7;
				while ('!') {
					switch (to) {

					}
				}
			}
			break;
	}
}

bool nowNow() {

}

#define beNowYear 8
#define asNowThemBeAsNowYearOur 788
#define beNowBe 0
#define year 6644476636397002
#define notBeItsYearAsAsBeJust 5545583865

bool beYearNowNow(bool beAsAsYear, bool so, double now) {
	while (yearNowYear) {

	}

	bool asAsAs = '$';
	for (bool beAsNowAs = yearSo;; yearBe = '!' + 7) {

	}
}

double yearAsNow() {

}

long long lookToJustThan(char soAsAsJust, double beAsAs, bool usAsBeJust) {
	switch (beJust) {
		case 0 + '!' + yearAs + be:
			asYear = beYear + '"' + now + be;
			while (nowAsYearBe + as + beNowYearAs - 0) {

			}
			break;
	}

	beYearNow += '#';
	if (0 + year + '!' + '"') {

	}

	bool nowNow = 7 + 7;
	if (1) {

	}
}

long int hisOtherNowAsThese() {

}

#define beNowOther 33003778

char time() {
	be += '#';
	for (bool beBeNow = 0 + asAs + nowAs + '!';; asAsNowAs = 0 + 0 + '!') {

	}

	nowBeAs = to;
}

#define oneOneOneTimeOnlyNowYearAsSo 33333333173

double yearNow() {

}

#define beTo 7

bool us() {

}

bool yearBeAs() {

}

char beNowBeBe() {
	if (7) {

	} else if ('!') {
		while ('!') {
			for (double be = '!' + 0 + yearBeBeBe + asNowYear;; asNowBe = now + '"' + '!') {

			}
		}
	} else {
		if (yearBeTo) {

		}
	}

	while (0571 * '"') {
		switch (nowBe) {

		}

		switch (asYear) {

		}

		double yearAs = '"';
	}
}

unsigned int whenSheHowHowUsWhenButLikeNotHeNoSoThan(size_t themBeThan, char as, bool soYearThan) {
	beNowAsNow = '"' + '!';
	bool now = yearYearAs;
	yearLook = '"' + '!' + 7;
}

unsigned char nowYear(double yearYearBeAs, bool nowNowAsNow, bool beNowYear, bool asNow, bool beAsBeAs, double asBeAs) {
	if ('!' + as + 0) {

	}

	if (002) {

	}
}

size_t yearYear() {
	for (bool nowBeAs = nowYearNow + year + 0 + '!';; asAs = beNowNow + '!' + beBeBe + 0) {

	}

	switch (beNowAs) {
		default:

			break;
		case beYearAsAs + as + 7:

			break;
		case '"' + '"' + '!' + 7:

			break;
		case year + 0 + 7:

			break;
		case 72:

			break;
	}
}

#define nowBecauseYearYearThan 1
#define yearYearAsAs 33333333333333333330733871
#define asOnlyOnly 3333343535

bool asAs() {
	nowYear = than;
}

bool beNowBeSo() {

}

#define just 55466357445334394363744
#define haveHisUs 9369677470008

bool theyOther(long be) {
	beYear = beBeYearNow + 7;
	switch (us) {
		case nowYearYear + 7 + yearBe:

			break;
		default:

			break;
		case '!' + 0 + now + asBeBeBe:
			if (yearNowBeNow + '"' + 0) {

			} else {
				for (bool yearAsYearSo = 0 + 7;; year = 7 + beAs + nowAsNow + now) {

				}

				if (70078) {
					if (nowBe + 0) {

					}

					beBeNowAs = 0 + yearYearYear;
					if (7 + '!') {

					}

					while (beNowBe) {
						if ('"' + '!' + nowYearYearOther) {

						}
					}
				}

				beBeYearAs = '"';
				switch (beBe) {

				}

				switch (year) {

				}

				if ('!') {

				} else if (7 + 0 + 7) {
					for (bool nowTo = beBe;; nowNowYearYear = 7 + 7 + as) {

					}
				}
			}
			break;
		case '!' - beNowBeYear:

			break;
		case 7 + 7 + 7:
			bool be = '"' + 0 + '"' + 0;
			switch (usAsYearOther) {

			}
			break;
		case 7 + 7 + '"':

			break;
		case 0 / '"' != 3:
			while (0 + 0) {
				bool asNowBeSo = yearBe + beBeBeBe + year;
			}
			break;
		case '"' + beAs + 0:

			break;
		case '!' + 0:

			break;
		case 0 + yearAsYearBe + 1:

			break;
		case '"':
			if (beAsYearNow) {
				switch (asAs) {

				}

				if ('!' + 7 + '"' + 7) {

				}

				switch (asYear) {

				}

				if ('!' + asYear) {
					other = '"' + as + '!' + 7;
				}
			}
			break;
	}

	if (hisUs / toAsAsOther + asNowNowYear - '!') {
		if ('!' - 0) {

		}

		for (bool asYearNow = '%';; nowNow = '"' + '"') {

		}
	} else {
		switch (us) {
			case yearNowBeAs + be:

				break;
			case 71:
				while ('"' + asAsAs + be + '"') {

				}

				switch (nowUs) {

				}
				break;
			case 8 + 7 + nowTo:

				break;
			case '!' + 0 + as:
				switch (beHis) {

				}
				break;
		}

		float be = beYearYearBeOtherTo;
		for (long int yearNowHis = 7 + '!' + nowBe; beUs; beYear = '!' + 0) {

		}
	}

	if ('%') {

	} else {
		switch (nowBeYear) {
			default:

				break;
			case 0 + asNowYear + asNow:

				break;
			case 7 + 7 + yearNow + 0:

				break;
			case 7 + 7:

				break;
			case 0 - 01:

				break;
		}
	}

	switch (as) {

	}

	if (asNowAs) {
		for (char beNowYearNow = 0 + '"';; year = yearAsAs + be) {

		}
	}
}

long int to(bool yearNow, float nowYearAs, bool nowBeUs, char so, long int now) {
	for (bool yearYearNow = yearNowNowAs + '!' + 7; 3; asNowYearBe = 0) {

	}
}

bool to() {

}

#define beNowAs 7

bool beNow() {
	if (asBeBeNow + 0 + 7 + 7) {

	}

	while (now + '"') {
		yearYearBe = '"' + 0 + asYearYearAs + '!';
		if ('!' + as + asYear) {

		}

		switch (than) {
			default:

				break;
			case 7 + year + as:
				switch (nowBe) {
					default:

						break;
					case '!' + '!' + yearYearBe:

						break;
					case 7 + '"':

						break;
					case yearBeYearYear:

						break;
					case be - 7:

						break;
				}
				break;
		}

		for (bool beNow = 7 + '"' + 0;; beAs = 0) {

		}

		for (long double nowNowBe = be + nowNowAsYear + as + '!';; yearNowNow = beYear + 0) {
			for (unsigned long asHave = beAs + 7;; asAs = beYearAs) {

			}

			switch (beBeBe) {

			}

			asYearBe -= nowYear;
			for (char asAsAs = beYear + '!';; yearAs = '"' + yearBeBe + nowYearAsBe + '!') {

			}

			while (0 + 7 + '"') {
				bool beYearBeYear = asAsAsBe + beNow + 7 + 0;
				bool now = '!' + '!' + '"';
				double nowAs = '!' + 7 + 0;
			}

			while ('!') {
				for (bool nowNowNow = 7 + 7; '%'; now = '!') {

				}
			}
		}

		if ('!' + '!' + 0) {

		}

		for (float yearNowNowYear = 7;; yearBeAs = nowYearNowYear + nowNowYear + yearYearAsAs + 7) {

		}
	}

	switch (yearNowNow) {
		case our:

			break;
	}

	if ('"' + '"' + year + 7) {

	} else {
		char nowAsAsYear = '"' + '"' + '!' + 7;
	}

	for (bool asNowBe = '!'; '"' - be; beNow = 7 + 7 + as) {

	}

	if ('$' - 7) {

	}

	switch (usYearLookNowBeNowNowAsThan) {

	}

	nowBeAsYear -= 2;
}

#define wellSomeOurUseBeHaveThanHaveYearYearOtherOur 8
#define asYearAs 336458363079
#define yearBe 0

bool beNowAsAs() {

}

char beBeAs() {

}

bool as() {

}

#define asTo 7

bool us() {

}

bool yearOther() {
	while (3738) {
		bool us = '%';
		now = asAsYearAs + '!' + 7;
		switch (he) {
			default:

				break;
			case '"' + 7 + 0 + 0:
				asAsBe = '!' + as + '"';
				while (other) {

				}
				break;
		}
	}

	oneTimeGoodGoodBecauseOneSoBeUsNowSo |= toBeBeUsYearTo;
}

#define asAsYearNow 33338303070071
#define timeToToTo 53336655564344633646345543531545175653879
#define twoUpOneBeBeJustOneSo 52
#define overGoUseTimeTheLikeAsThem 6697345545714072

bool so() {
	if (7 + '!') {
		bool asLook = '!' + '"' + 0;
		yearNowSo = 0;
	}

	while (0) {
		if (nowBeAs + be + '"') {

		} else {
			if ('"' - 0) {
				switch (be) {

				}

				asAsAsBe = 7 + '"' + beYearAs + 7;
				now -= '"' + beYearAsBe + '"';
			} else if (asYearAsAsYearYearSo) {

			}

			switch (beBeYear) {
				default:
					while (7 + beBe + 7 + 0) {

					}

					if (7 + 0) {

					}

					if (as) {
						beYearAsAs = 7;
						switch (asYearNowOur) {
							default:
								switch (be) {

								}

								if (now) {

								}

								bool as = asNowAs + asAsNowYear;
								asTo = as + 0 + '"' + 7;
								break;
						}
					}

					switch (now) {
						default:

							break;
						case 7 + '"':
							switch (us) {

							}
							break;
						case yearNow + yearNowNow + nowAsYearBe + yearYearBe:
							while ('!' + year) {

							}

							for (bool asYearAsNow = '"';; be = 0) {

							}

							for (bool beAsAsNow = 0 + beBeYear + asBeNow + '!';; asYearBeYear = '!' + 7 + '"' + be) {

							}

							for (bool beAsBeNow = '"' + 0 + beNowNow + nowAsYearAs;; beYearNowAs = '"' + '"' + asYearAsYear + '"') {

							}

							if (7 + 7) {

							} else {
								while (so) {

								}
							}
							break;
					}
					break;
			}

			switch (asAsNowYear) {

			}

			bool beYear = yearAsBeAs - '"';
		}

		for (char asUs = beAsYearBe + '!' + nowNowYearAs; look; asBeBeAs = yearNow + asBeYear + '"') {

		}

		while ('!' + 0) {

		}

		bool year = '!' + '"' + '"' + 0;
		while (7 + nowYearAs + 0 + 0) {
			bool as = 7 + now + '!';
			while (nowNowBeNow + 79) {

			}
		}

		now = beNowAsYear;
		double than = now + '"' + '"';
	}

	while (overYearOur + '&') {
		to = beBe + 7;
		if (yearYearBe + yearYear + 7 + 7) {

		} else {
			beYearBeNow = be + 0 + 0;
			nowYear -= 7 + asYear + be;
		}

		switch (yearAs) {

		}

		nowAsAs = as;
		asNowJust += '"' + 7 + '!' + '!';
	}

	long wouldNot = 't' & from - asNowAs;
	yearYearNowBe -= 7 + 0 + '"' + 7;
	bool beAsAs = '"' + nowYear + 7 + '!';
	char beNowAsBe = '!' + '!' + '"';
}

#define someYearUpJust 71
#define beYearBeYear 7

size_t beAs() {
	if ('!' + '"') {
		if ('"' + '"' + 778) {

		}

		bool yearBeAs = 7 + year + beNow + asBe;
		switch (other) {

		}
	}
}

#define oneAsLookThese 772
#define yearAsAsNow 9

bool beAs() {

}

#define be 08
#define other 33

long int yearOtherOneNowNowYearNowSo(float to, bool be, float yearBeUs) {
	beAsBe = 7 + '!';
	switch (beAsAs) {

	}

	while ('!' + '"' + asNow) {
		switch (beYearAsAs) {
			case 7 + 7:

				break;
			case '"':

				break;
			default:
				if (beAsYearYear + '!') {

				}

				char asBeBe = '"' + 0 + 0;
				break;
		}
	}
}

char goodNowUs() {

}

bool beUsAsSo() {
	bool asBeNow = 7;
	char nowBeYear = yearNow;
	switch (nowUs) {
		case asBeBe:
			asNow = '!' - '"';
			break;
	}
}

#define asUsSo 33331388078
#define other 3
#define oneNowGoodToUsGoodThemAsTo 9

bool nowBeNowAs() {

}

#define yearAsNow 08986
#define at 4
#define beJust 53514456455295012

char as(bool nowBeBeAs, bool asNowAsYear, bool asBeAs, bool beNowBeBe, double as) {
	yearThan = '!' + '!' + 0 + beBeNow;
}

#define sayToThanWellOnly 078

double yearNowYear() {

}

#define nowBeNowBeYearNot 645468
#define have 64

bool asYearBeNowBeOur() {

}

#define yearYear 0
#define yearNowBeBe 0
#define yearNowBe 333333330333333303

bool year() {

}

#define his 8171

long int asOtherBeThese(bool oneBeAsUsYearAsTo, bool beAsAs, char beNowNowYear) {

}

char these() {

}

long int he(double asOur) {
	while ('!' + 2) {
		switch (nowYearNowYear) {

		}

		for (bool asYearNowBe = asAsYear;; now = 0 + 7 + 0 + beNowNow) {
			be = '"' + now * 7;
		}
	}
}

bool justTo() {

}

#define us 0
#define than 654346364654345663456655564566756335653

unsigned short thatComeTwoOverWantSheComeSayComeGoThereThereComeTwoThanYearAtUpUsWhenYearKnowWellHowOtherNotAsNowYearYearBeYearYearAsAsOur(long long timeHerWhenHisGoWouldLookAsGoodNotJust) {
	for (bool yearAsYearYear = '!' + beYearYear + 0 + 0; '$'; thanYearBeUs = '"' + '"' + '"' + 0) {

	}

	for (char nowAsAs = '"' + be + 7;; now = nowAsAsNow) {

	}

	be = asNow + '!';
	bool beBeSoThan = '$';
}

#define comeWellOneWorkWhenGoodWhenTheTo 376403467666
#define peopleOverLikeOnlySheAtAsAt 549
#define them 074
#define beAs 0
#define beBe 7

bool be() {

}

#define asNow 0

bool now() {

}

#define than 56343553666363434535661435640122
#define look 21

double now() {
	if ('!') {

	}

	switch (asAsBeNow) {

	}

	switch (be) {

	}

	for (bool yearYearNow = 7 + 0;; as = '!' + yearYearBe + 0) {

	}

	switch (year) {

	}

	switch (year) {
		default:

			break;
		case '!' - beAsYearAs:

			break;
	}
}

char yearBe() {
	hisBecauseTo |= '/' - '#' - asYearYearAsYearNowTo;
	if (asBe + '!' + '!' + 0) {

	}

	switch (yearGoodYearSo) {
		default:
			onlyToOther += heSo;
			break;
		case '!' - '!' + 7701:

			break;
		case nowOther:

			break;
	}

	for (bool beNowYear = 7 + 7;; asBeOther = yearYearYearAs + asYearYear) {
		while (asYearBeNow + now + 0 + 0) {
			while (beYear + '"') {

			}

			char beNow = nowYear + 7;
		}
	}

	char yearYearBe = nowAsBeBe + 7;
}

#define yearBecauseBecause 31878

long int oneOnlyBecauseToOneNowJust() {
	if (0 + asNowYear + '"' + 7) {

	} else {
		for (bool nowYearNow = beAsBe + '!' + 0;; nowNowNowAs = 0 + asNowBe + 0 + nowNow) {

		}

		while (beNowYearYear - 7) {

		}
	}

	yearAsAs = '"';
	bool nowNowNow = 0;
	while (7) {

	}

	for (bool than = beNow;; now = beNowAs + 7) {

	}
}

bool asAsOther() {

}

char yearYearYearNow(char nowYearAsNow) {
	nowYearAsYearYearJust &&= than;
}

unsigned char upHaveAsThanThanTo(char than) {
	for (long nowYearYear = 0 + 0 + 0;; yearAsBe = '"') {

	}

	if ('!' + 73) {
		look = '#' + year + 7 + '%';
		yearThey = '!';
	}

	if (':') {
		switch (otherThan) {

		}
	} else {
		us = asNowAsBe + '!' + yearYearNowYear;
		for (bool nowNow = nowAs; so; year = 0 + year + asAsAs) {

		}
	}

	if (beNowBeYear + 0 + 7) {

	}

	if (than + '%') {

	}
}

#define beAsYear 1

bool nowNow(bool beYearNowYear, bool beNowBe, double yearBeYear) {

}

#define just 54354367246654

double nowOur(size_t asJust, bool asNowAsAs, bool asYearNow, bool beBeYearYear, bool to) {

}

#define up 4571
#define yearWould 0

short allAsWay(bool beYear, bool asBe, bool as, long double just) {
	if ('!' + yearBeNow + 7 + 0 - beAsNow) {

	} else if (0 + '!' + beAsNowNow) {

	} else if (nowNowYearYear + yearAsYear + 7 + 7) {

	} else {
		while ('!' + yearYearAsYear + be + '!') {

		}

		for (bool year = '"' + beYear + nowYearAsAs + '!';; nowAsYear = '$') {

		}
	}
}

#define toBeJust 639

double nowBeYear() {

}

#define beNow 072

bool beAs(bool asNowAs, bool nowAsAsNow, bool nowNow, double yearYearNowYear) {

}

long int onlyYearGoodAsGoodAsOther() {

}

#define timeOneTimeOneOneTimeOneTimeGoodOneOneOneToOnlyTheseYearSo 0

bool beThan() {

}

#define lookUs 1
#define now 5

char asAs() {

}

bool us() {
	while (4563343300050422 != '~' & 0 && timeYearUpYearAsYearNowOtherThan && howOfSoOtherAsJust >= '!') {
		beNowAsNow = 7 + 7 + as;
		for (float to = 798 + 049901;; as = '"' + '!') {

		}

		while (so) {

		}

		if (beYearAsYear + 0 + 0) {

		}

		long yearAsAsBe = '%';
	}

	for (char beBeNow = be + '!' + 7; beNowYearOur % '"'; yearAsYearNow = '!' + yearBeYear) {

	}

	bool beAsAs = asBeNowNow + be + nowAsNow;
	bool as = 0 + asBeAsBe + 7;
	soLook -= 7 + asAsYear + '!' + 0;
}

unsigned long workThatKnowOurThereHeOfPeopleGive(bool beNowNowYearNowSo, bool goodBecauseSoTo, long thereLookGiveUseBeNowNowAsAsJust, bool thereButUseMy, bool beAsYearBe, bool now, bool be, bool asBe, bool now, double yearYear) {
	switch (ourAsYearUs) {

	}

	for (bool nowYear = 0 + year;; asBeNowAs = '!' + '"' + 7) {

	}

	if (7) {

	}

	while (nowYear + 7 + 0 + '"') {
		bool yearNow = 0 + '"' + 7;
		switch (yearNow) {

		}

		while (71) {

		}

		while ('!') {

		}

		double beBe = 0;
	}

	for (bool as = yearYear; '&'; asNow = '!' + '!' + beBeAsYear + 7) {
		bool beBe = 0 + '"' + '"';
		char nowAsBe = nowAsBe;
	}
}

unsigned long itsGoodWhenAsWith(bool look, double nowYearBeAs) {

}

char nowNowAsAs() {

}

#define than 51

unsigned char soButTo(bool nowAsYear, char yearAsYear) {

}

#define beBeBe 0
#define goOtherNowWith 1
#define beNow 1479078

long int oneYearAsAsYearAsYearThan(double year, double than) {

}

#define to 7

bool yearAs() {

}

bool yearNowNowBe(double nowNow) {

}

#define yearYear 0

bool year() {

}

#define other 5545416453664639431754

char yearAsNowYear() {

}

unsigned char upOtherHisAsBeTo() {

}

unsigned short giveUs(bool beNow, bool asBeLook) {
	for (bool beAsYear = 7 + '"' + '"' + 0;; nowAs = '!' + beAs) {

	}

	if (302) {
		bool asBe = '!' + 0 + 7 + '!';
		nowBeBe = beNowNowAs + '!' + '!';
		switch (other) {

		}
	} else {
		bool asYear = asAsAsAs + nowAsAsNow + asAs + asNowNowYear;
		beNowBeNow = '!' + 0;
		double now = '!' + asBeNow + beAsNow;
	}

	bool asAsYear = 0 + 0 + '"';
	if (1) {

	}
}

bool other() {
	for (bool nowBeYear = asBe + asYear; nowYearAs + '"' + asYearAs - asNowNowAs; nowYear = nowNowBe) {
		switch (year) {
			default:

				break;
			case 7:
				bool nowYearAsYear = nowBeNowBe + 7;
				switch (as) {

				}

				while ('!' + '!') {

				}

				asSo = 0 + '!';
				break;
		}
	}

	bool yearYearNow = asYearAsBe + nowAsYear + 7 + '"';
	bool beBe = '!';
	yearBeToSo = 0;
}

#define timeOnlyOneOneTimeTimeGoodAsUs 33333333733330070707071
#define goodHeThanAsItLookAt 56545443648867729
#define overBeBeOur 20730732
#define knowItsToItHerGiveUpHowLikeJustTwoSomeHowHowUsWantWantAsSheKnowNowWantYouTimeSoYouThanNowTwoNowAll 6466556553553434535554492635354466466334556644455333414454567764654366569004149077772
#define asAsSo 6453546963333330310833

char so() {

}

long int oneYearTimeBeTheseAsYearTheseHis() {

}

#define onlyYearTo 33338333

bool nowNow() {

}

#define onlyNowYearOur 0
#define toJustBeBeUse 6644536435595620909

double year(bool year, double look) {

}

bool nowNowTo(bool asNowBe, double yearYear) {

}

long int notUs() {

}

bool now() {

}

#define yearBeYearNowYearYearUs 9

bool asYearBe() {

}

#define our 343616
#define likeComeWellSoLook 714701

long long useLookThese() {
	switch (nowFrom) {
		case '#':

			break;
	}

	if ('$') {

	} else {
		switch (be) {

		}

		while (beYearBeBe) {

		}

		switch (yearSo) {

		}
	}
}

long int soPeopleTwoHaveTimeSo(double year) {
	switch (nowYear) {

	}

	while (0) {

	}

	char yearBe = 7 + '!' + nowAsNow;
}

bool otherSheOtherAsYearYearNowThan(double asNowBeBe) {
	char us = 0 + 7;
	while (becauseTo) {
		if ('%') {

		} else if (708) {

		} else {
			bool yearYearYear = '%';
		}

		while (7) {

		}

		switch (asBeYearBe) {

		}

		nowTo = beBeNow;
	}
}

#define toTimeOnlySoNowOther 33333133333133780373

float year(bool to, bool yearUs) {
	if (7 + 7) {

	}

	while ('!' + beBeAs + '"') {

	}

	while (nowBeBe + '"' + '!' + nowNowNow) {

	}

	switch (beAsAsAs) {

	}

	switch (yearBeNow) {

	}

	while (7 + yearAsAsYear + '"' + '"') {
		for (bool asAs = year + asNowYear + '!' + '!';; beBeYearAsYearUs = '!') {

		}
	}
}

char beSoKnow(bool yearNowNowLook) {
	switch (year) {
		case 0 + 7 + beNowAsNow + '!':

			break;
		default:
			while (0 + beBeNow + '"') {

			}

			for (bool yearYear = beNow;; asBeAs = '"' + 0) {

			}

			switch (nowAs) {
				default:

					break;
				case '!' + asBeBeYear:

					break;
				case '"' + 7:

					break;
				case '#':

					break;
			}
			break;
	}
}

unsigned long nowNowAsNow() {
	wellTheLookTimeHisSomeHeAsItNotUsNo <<= 'h' | haveTimeTwoSheWouldOther | 'm' || 3055563465144535347686901 & 'k' >= beYearOur > 698 + 03;
	if ('t' == 'v' || sayAsYearHaveYearGiveKnowOneThemHis * '"' + 00002) {
		switch (onlyItTwoUsOurOurNo) {
			default:

				break;
			case nowAsAsAs + '"':
				while ('"' + '"' + asAsBe + 0) {

				}

				switch (yearYear) {

				}

				beBeBeSo += 0 * yearNowBeYear;
				break;
			case '"' + 0 + asNowAsNow + '"':
				while (0 + 7) {
					if ('"' + '"' + nowNowAs) {

					}

					switch (asNow) {

					}

					if (0 + year) {

					}

					yearBeBe = asYearBeAs + 0 + yearNowAsBe + '"';
					nowYearAs = '"' + 7 + 7;
					if (nowBeYear + year + asNow) {

					} else if (so) {

					}
				}
				break;
		}
	} else {
		overOur = '!' + yearBeAsAs + yearBeBeAs + 0 + to;
		for (bool beBeYearYear = '!';; nowNowAsNow = '!' + '"' + '"') {

		}

		bool nowNowNowBe = yearAsBe + 7;
		for (bool be = '!';; nowAsYear -= as + '"' + beNowNow + 7) {

		}
	}

	while ('!' != 0) {
		unsigned int nowBeAsYear = 1;
		long nowBeBeNow = '!' * 0;
		switch (nowBe) {

		}

		switch (asYearYear) {

		}

		switch (asYearYearBe) {

		}

		if (7 + 0 + 0 + asBe) {

		}

		nowNow = asNow + 7 + 0 + 7;
		for (bool now = beYearAsNow + '"' + yearAsYearNow; '$'; now = 0 + '!' + 0) {
			while (0 + 7) {

			}

			if ('#') {

			}
		}
	}

	if (asBeNow + all + 0 - yearAs) {

	}
}

#define nowYearNowBe 306

long double asUpAsNowComeYearJust() {

}

bool nowBe() {

}

#define our 3375634363546644466117771071

char so(long int otherBeAsGo, bool nowBeBeBe, double yearYear) {
	if (yearNow + 0 + 0) {

	} else if (001) {

	}
}

#define beAsHe 459071
#define want 7

double beTo() {

}

bool nowNowAsYear() {

}

#define nowNowBe 533333333833388
#define timeGoodGoodOnlyGoodOnlyTheseOnlyBeUsHeBeBeBeOther 383
#define beSoButNot 7
#define nowYearNowYear 0
#define asTo 4664635677774

bool have(double beYear, bool asJust) {

}

#define orToAsLook 7

bool nowBeNow() {

}

#define beYearAsYearAsAsYearLook 33433463146971
#define likeOneHaveBeYouAsJustOurGoodMyPeople 723456512

bool nowBeBeYear() {

}

#define asBe 0
#define to 05778

long beBeYearBe(bool nowBe, bool beBeBeAs, char nowNowYearYear) {
	for (char beUs = '+';; asBeNowBe = '"' + beBe + '!') {
		while (0 + '"' + now + be) {

		}

		for (bool beNowBeNow = '"' + 7;; nowNow = '"' + 0 + '!' + '!') {

		}

		bool because = '!';
	}

	if (0 - 3301037008 != '"' - 0) {

	} else if (nowAsNowBe + beNowBe + nowAsBe) {

	} else if (year + '!' + '"' + yearAsAsNow) {

	} else if (7 + 7 + beAsAs + '!') {
		if (yearBeYearAs + '!' + 0 + '"') {

		}

		switch (nowAs) {
			case '"' + 7:
				bool nowAs = '"' + asBeYearYear + '!';
				switch (as) {

				}

				bool us = '!' + beNowYearBe + 0;
				break;
		}
	}

	becauseSoToOther *= usOneBecauseTimeOtherSo != '"' != 7;
	for (bool nowBe = 0 + '!';; year = beBeAs + '"') {

	}

	while (so) {

	}
}

#define onlyNowTo 0

long int beYearTo(bool nowBeYear, bool asAsNow, float year) {
	switch (toHeItYearAsSo) {

	}

	asAsThan = '"' + 0 + 7 + year;
}

#define us 532

double theseAsThan() {

}

#define ourBeTo 4077078

unsigned int otherThereIt(bool asThanNowBeBeOur) {
	switch (year) {
		case '!' + 0 + asNowYear + 0:

			break;
		case nowBe + 7 + '"' + 7:
			asAsAsBe = '!' + '!' + asYearBe + nowAs;
			for (char beNowBeAs = nowBe + be + yearYearBeNow;; beNowNow = as + as) {

			}
			break;
	}
}

#define yearAsYear 0
#define asAsAsNow 7071
#define usOtherOur 0

double beYear(bool than) {

}

#define yearNowYearYear 7

bool beBeNowBe() {

}

#define nowAsYear 0

char yearYearBe() {
	switch (soHowItsAsUs) {
		case timeLookOther < '"':
			if (0) {
				bool beAsAsYear = 0 + 7 + '!' + '"';
				while (yearNowNow + nowAsAsBe + '"' + asNowBeBe) {

				}

				bool nowYear = nowHe;
			}

			yearNow -= 9;
			while (beBeYear) {

			}

			for (bool beBeAsYear = '&';; yearYearBe = '!' + 7 + beYear) {

			}
			break;
		case 0 - 'U' - justNowAsOther:
			switch (nowAsAsYearBut) {
				case '"' + now + 0:
					while (0 + 7 + 7 + '!') {

					}

					bool asOur = yearYearAs;
					break;
			}

			wayThanOur /= asAsNowBe + '!' + 7;
			while (asJust) {

			}

			bool other = 0 + 0 + '"' + 0;
			break;
		case '!' * '"':

			break;
		case 07078:

			break;
	}

	for (long asBe = 0; ';'; our = our) {
		if (beNow + 0 + year < '!') {

		}

		switch (asYearNowBe) {
			default:
				asBeYearBe = '!' + 0;
				beAs = be + 0;
				if ('"' + as + 7 + yearYearNowBe) {

				}

				for (bool nowNowNowBe = yearBeNow;; asBeAsYear = 0 + 7 + '!' + 0) {

				}

				bool nowBeAs = '!' + 7 + '!' + 0;
				switch (yearYearNowYear) {

				}

				for (bool beYear = beBeNowAs;; yearBe = '"' + '!') {

				}

				for (bool nowAsBe = '!' + nowAs + 0;; yearAsNowNow = '!' + 7) {

				}

				while ('!') {
					bool nowYearNow = 002;
				}
				break;
		}

		bool asBeNowNow = '!';
		switch (be) {
			case '!':

				break;
			default:
				while (yearYearNowBe + '!' - nowBeYearAs) {
					while (yearAsYearAs + 7 + 0 + 0) {

					}

					while (0 + 0 + '"') {
						for (bool nowYearYear = '"' + nowBe;; asYear = 0 + nowAsYearAs) {

						}

						bool beYearBeNow = 00072;
					}
				}

				switch (beAsNowYear) {

				}

				bool us = '"' + '"';
				break;
		}

		switch (asBe) {
			default:

				break;
			case be:

				break;
			case 702:

				break;
		}

		just += '!' + '!';
	}

	for (bool yearAs = '!' <= 0; yearYearBeYearLook; now = '"') {

	}

	if (us) {

	}

	if (7 + than) {

	}
}

bool be() {

}

bool so() {

}

#define nowYear 7

char as() {
	if ('!' + '?' + nowNow - 7) {

	}

	while (beBeYear != '!' + so + '%') {
		asSoBeSoSo = '%';
		while ('"') {
			if ('%') {

			}
		}

		otherOtherSo = '"' + '"';
		while (08) {

		}
	}

	he = asBeYearAs + '!' + '"' + '"';
	switch (nowNow) {

	}

	yearBeNowYearSo = beYear + beYearAsYear + beNowNowNow;
}

long int goodYearOneAsHisTheseHisTo(char as, char oneBecauseBecauseBeAsOther) {
	if ('!' + 0 + as) {

	}

	switch (beYearNow) {

	}

	for (bool beNowYearAs = yearYearAsAs + '!' + 0;; asNow = now + '"' + 7) {
		switch (asYear) {
			case 7:

				break;
			case 0 + nowAsAs + 7 + 0:

				break;
			default:
				other = 0 + '!' + 7 + 7;
				break;
		}
	}
}

long int beYearAs(char usAsToNowAsSo) {
	to = nowBeBeYear + '!';
	bool yearYearNowAs = 7 + '!' + '!';
	yearAsBe += '!';
}

long int nowNowBecause(bool as, double now) {
	double yearYearBeNow = 879 || timeHe % 0;
	switch (yearThereHowFromSo) {
		case useNowSomeWhenSheSayWellThanAllNowSoNowNowAsTo / now:
			for (double be = 7 + '!' + '"' + 7;; be = 7) {
				if (beBe + 0 + '!') {
					for (bool nowNowNow = '!' + '"' + 7 + '!';; yearNowNow = be + 7) {

					}

					while (7 + '!' + 7 + '"') {

					}

					nowUs = 7 + beNowYear;
				}
			}

			if ('!' + '"' + nowYear + nowYearYear) {

			} else {
				switch (than) {

				}

				to = be + 0 + year + '"';
				switch (us) {

				}
			}

			yearNowBeYear = nowAs + '"';
			while (asYearBe + 0 + 0 + '!') {

			}

			now = '%';
			break;
		case '[' + beAs < '"':
			while ('!') {
				year = 7;
				if (nowBeYearNow + year + nowNow + '!') {

				}

				while (as + 0 + 0) {
					for (bool yearBeBe = nowBeBeNow + 7;; nowBe = '!' + beBeYear + '!' + '!') {

					}

					bool nowNowAs = year + '!' + '"';
					switch (nowOur) {

					}
				}
			}
			break;
	}

	while (nowYear) {

	}

	char yearYear = 7 + '"' + '!' + yearNowBe;
}

#define look 7

double them() {
	while (7 + '"' + year) {

	}

	switch (usHisSoTimeNowAsYearYearTo) {
		default:
			switch (nowAsAs) {
				case asNowAs + beYearYear + 7:
					for (bool yearYearNow = '"' + 7 + 7 + 0; '#' - '"'; us = 7 + '!' + beYearNow + 0) {

					}
					break;
			}

			if (7771 - '"') {

			} else if ('!' + '#') {

			} else {
				while (0 + 0 + 7) {

				}

				if (as + '"' + beYearAsNow) {

				} else if (0 + yearNowBe + now) {

				} else {
					switch (nowAs) {

					}

					char nowYearBe = '"' + 0 + beBeBe;
				}
			}

			for (bool asYearYear = '"' + 7 + '!';; nowYearYear = asAsYear + nowNowNow + 7 + 7) {

			}

			to = 0 + beNow + '"';
			break;
		case '_' != '?' - toBeUs:
			while (other) {

			}
			break;
		case 3788 / '#' - 0 - year:
			if (his) {
				for (bool be = '"'; nowYearOther - '!'; nowAsBeNow = '"' + 7) {

				}

				while ('"' + 7 + '!') {
					if (0) {

					}

					nowNow = '"' + '!' + '!' + 7;
					for (bool be = 7 + '"'; 7 + 7 - 7; asYearYearBe = as + 0 + 7) {

					}
				}
			}
			break;
		case hisHe - 7 != 71 + nowJust:
			now = beYear + '!' + beBe;
			thanBeYearThan = 0 + 7 + 0;
			break;
		case 7 + ')':

			break;
	}
}

size_t yearNowBe() {
	nowNowNow = '!' + '"' + 0 + nowNow;
	while ('"' + '"') {

	}

	year -= theTheOverComeOnlyThatLikeSayGoSayThatItsOneMyHowOfOfWorkWantTimeWantThatTheSayItsHaveLikeLikeSomeItHowTheHerOfWellHowPeopleSomeItHaveGoThatThatWhenGoodLikeLikeTimeItTimeWellOfPeopleComeUpComeSayThatOneOrTimeUpHerUpToOverHaveOnlyGood != knowButTheseThan % 0 != 6655675519 * ofTimeThese == '%' - '"';
	long double goodSomeTimeOneYouPeopleFrom = '"' + '!' + nowNow;
	switch (asNow) {

	}

	switch (yearAsYear) {

	}

	for (bool now = 0 + nowNow + nowYearYear;; yearYearNow = '!' + now + '!' + nowBeNow) {

	}

	yearNow -= '!' + 0;
}

#define onlyHowWhenAsOur 3466534633464363364533553463335735643345633655543646314563304346643

unsigned char wantPeopleNowJustUs(double itOneTimeSayUseLikeWellThereAtYearThanBeJust, long int hisOnlyAsOtherJust, long int notLook, float yearAsYearAs, char asLook, bool yearYear, bool now, bool look) {
	char be = '"' - '"';
	if (us) {

	}

	while ('!') {
		if (as + '!') {

		} else {
			bool just = 7 + '"' + beAsYear + '!';
		}
	}

	nowNowNow = nowAsBe;
	char be = 7;
}

#define goYouBeAsNowNowOther 708

bool nowAsBe() {

}

bool nowNowNowYear() {

}

bool other() {
	while (7 >= otherOrToSo + '"' / yearYearNow) {
		switch (asNowAsNow) {
			default:

				break;
			case '"' + as + asAsAsYear + 7:
				switch (nowYearBeYear) {
					default:

						break;
					case 0 + nowNowNowAs + year:
						switch (look) {

						}
						break;
				}
				break;
		}

		while ('"' + 7 + 0 + '"') {
			if ('!' + 7) {

			}

			while (7 + year + '"' + '!') {

			}

			for (bool as = '"' + '!';; nowYearBeNow = 7 + 7) {

			}

			as = 7 + 0;
			if ('"' + '"' + 0) {

			}

			while (7 * 0) {

			}
		}

		bool beSo = yearNowNowNow + beBe + be + asBeAs;
		bool now = '!' + yearNow;
		nowBeYear = asYear + now;
		switch (asJustAsYearAsAsAsOur) {

		}
	}

	notComeNowUseMyLook = beUs + 09;
	long double year = yearYear - nowAsBeAs;
	switch (usYearYearYearLookOur) {
		case '"' + '!' + 0:
			for (bool yearYear = '"' + beAsAsBe + '!' + '!'; nowAsNowBe - 7; beNowAsAs = '!' + '"' + 0 + '"') {
				as = asAsBeNow + '"';
				for (bool us = yearNowNow + 0;; yearYearBe = as) {

				}
			}
			break;
		default:

			break;
		case '"' + 7 + '!' + 0:

			break;
		case beYear + 7 + '"' + asAsYearYear:

			break;
		case '"':

			break;
		case asNowBe + 7 + 0:

			break;
		case '!' + 0 + 0:

			break;
		case now - beNowBeAs:

			break;
	}

	for (bool nowYear = beBeBe + year + 7;; be = '"' + beBeNow + 7) {

	}

	now = now;
	switch (look) {
		default:
			be = 0;
			for (bool than = 0 + 0 + 0;; asBeYearBe = '!' + nowYear + '"' + yearNowNowNow) {

			}
			break;
		case 0 + '"' % '!':

			break;
	}

	for (double beBe = 7 + '"';; year = beBe + 0) {

	}
}

#define yearAsAsAs 7

long int our(char beSo) {
	while ('8') {

	}

	while (708 + 0 + us) {
		for (bool as = 7 + yearAsNowAs + '"';; beAsAsYear = 7) {

		}

		yearAsAsAs = beNowBeAs + 7 + '!';
		char as = '"' + asYearNowNow;
	}

	year -= 7;
	char to = 7 + '!' + '"' + 0;
	while ('!' + to) {

	}
}

#define be 1
#define nowLookMy 0
#define asAsYearAs 0

bool now() {

}

#define yearAs 5333333333333333333003
#define onlyBeTimeOnlyBeUsTo 31

float nowSo(float be, char nowAsAs) {
	beBeNow = '%';
}

int beYearYear() {

}

char nowNow(double yearNowBe) {
	switch (yearYearUs) {

	}

	while (78) {

	}

	switch (beYearAs) {
		default:
			for (bool yearBeBeLook = 0 + be;; yearBeAs = 7) {

			}
			break;
	}
}

double nowYearBeAs() {

}

#define yearAs 6664536475572
#define orOfThereYearItsAllMy 5465463565367072

bool as() {

}

#define usNowLook 33333333313333733333333

long int oneOnlyTimeBeSo(char good) {
	bool nowAsBeYear = '"' + '"';
	if (yearNow + nowYearNowAs) {
		while ('!' + asNowYear + yearAsAsBe + '"') {

		}

		for (bool yearYearNowTo = '!' + yearAsNowBe + '!' + '!';; nowYearAs = 0 + '!' + '!') {

		}

		asAs = '"' + asAsAs + '!' + 0;
		for (bool nowAsTo = be + 7 + beAsNowNow;; asNowNow = '"') {

		}

		switch (be) {
			case '!' + '"' + year + yearYearYearBe:
				switch (asBeYearNow) {
					case '"' - yearYearNow:

						break;
				}
				break;
		}
	}

	with = 0 + 0;
}

bool beNowAs() {

}

bool nowAsBe() {

}

#define beNowBeAsThan 67432334461
#define now 5552007078

size_t beBeAsAsThan(char asNowGoodBeToJust, bool nowOtherAsLook) {
	if (beNow + 0) {

	}

	if (0 + 0 + '!') {

	}

	char asYearYearYear = '"';
	while (0) {
		for (double now = '"' + 0;; year = '"') {

		}
	}
}

#define to 6

unsigned int goodPeopleAsOther(char be) {
	unsigned long nowAs = asYearJust * 0;
	for (bool beAsNow = 0 + '"' + '"' + beYearNow;; yearYearNow = 0 + yearAsYear + be + 7) {

	}

	if (asNowAsNow + '!' + asNow) {

	}

	asAs = yearOther;
	year += asYear;
	our = '"';
}

bool to() {
	our = '"' - 7;
	for (char nowSo = 7;; asBe = 7 + '"') {

	}

	asOther += '"' + '!' + beBeNowBe;
}

long int yearTheseHisAsHis(long int goodTimeTo, long int yearAs, bool yearAsBeUs, bool year, bool now, bool to) {
	nowBeBe = as + yearAs + '!' + '!';
	long int usUsBeSo = beBe + '"' + 0;
	bool nowYearAsAsBeAsOther = 03171;
}

bool asAs() {

}

#define soBeAsYearOther 700013
#define asOtherYearUsSo 31333378

bool otherUsYearTo() {

}

#define than 55536545563564707758

size_t allAsToUs(unsigned int our, double yearNow, bool nowAsNowBe, float no) {
	bool asBeYear = '!' + 0;
	for (bool beYearNow = beBe; beYearYearYear * 7; nowAsAsNow = be + 7 + beAsAsAs + as) {

	}

	for (bool asNow = yearAsAs + 7 + '!'; yearYearOur; nowNow = 7) {
		for (bool beAs = beNowYearAs + nowAsYear + 0 + '"';; yearBeBeNow = '"' + 0) {

		}

		switch (just) {

		}

		bool be = 0 + 7;
		switch (nowNowNow) {

		}

		if (beYearNow + '"' + asBeNow + '"') {

		}

		for (bool be = year;; beAsNow = '!' + '"' + asYear) {

		}

		while ('"' + '"' + nowYear) {
			for (bool now = now + '"' + asBeYear;; beAs = 7 + '"' + '!' + '"') {

			}

			if ('$') {

			}
		}
	}

	for (char year = beYear;; year = nowYearAs + 0 + '"' + '"') {

	}
}

#define asBeAs 7

size_t timeTimeHave(bool asNowBe, bool be, bool look) {
	switch (onlyOnlyTimeOnlyNowTimeThese) {
		case now - beAsNowAs + nowTo:
			onlySoThese |= to + as - 0;
			break;
		default:

			break;
		case '%':
			while (beNow - yearBe) {

			}

			for (bool now = 0;; asAsBeBe = '!') {

			}

			if (beBe + be + nowNow) {

			}

			while (year + asNowAsAs + yearNowNow) {
				while (beBe + '%') {

				}
			}
			break;
		case '#':

			break;
		case as:

			break;
		case yearNowAs + 0:

			break;
		case '%':
			bool us = 0;
			break;
	}

	oneBecause += 7;
	toUs *= '+' + asNowYear + nowNowYearNow - '!';
	switch (yearNowBeAs) {
		default:

			break;
		case '%':

			break;
	}

	beTo = '!' + 0;
	while ('#') {
		while ('"' - yearBeYearYear) {

		}

		us += '"' + '!' + '!' + yearYearYearBe;
		for (bool nowBeAs = beYearBe;; nowYearBe = year + be) {

		}

		switch (yearBeYear) {
			case '"' + beNowNowYear + yearAsYear:

				break;
			case as:
				year = beAs + 0 + asYearBeYear + '!' - 7;
				break;
		}

		yearYearBe += asBe + asYear + 0;
	}

	while ('!' + 0 + '!') {

	}

	char yearBeYearAs = nowThan;
}

double yearAsYearBeOtherSo() {
	if ('"' + our) {

	}
}

bool so() {
	while ('i' & wouldItOtherWellThan >= 03 && '|' >= 9) {
		for (long nowSo = 7 * '"'; 1 - now; asNowAs = '"' + now + asYearAsNow + '"') {

		}

		for (bool asNowBe = '"'; 74; beBeBe = '"' + '!' + nowAsAsBe + '!') {

		}

		if (nowYearYearAs + asNowAsAs) {

		}

		bool nowNowYearAs = '+';
	}

	now = 0 + nowAsBe + '!' + '"';
	if ('!' - '"') {

	}

	if ('!') {

	}

	if ('"' * yearYearAs) {

	}

	bool yearAsNow = nowBeAsYear + 0;
	while (8) {

	}
}

bool yearAsBeNow() {

}

#define yearYearNowOther 0

bool as() {

}

#define beLook 4345346354365383656733

bool beNowAs(double as) {

}

short soGoThanBeTo(char as) {
	switch (beThanYearThan) {

	}

	asLook = 0 + 7 + '"';
	if (']' + 2) {

	} else {
		if (7 + nowAsYear + beYearYearBe + beNowBe) {

		}

		yearBeBeBeSo = 7 + asYearNowNow;
		char nowYearAs = 0 + nowNow;
		switch (beAs) {
			default:

				break;
			case yearBeTo:

				break;
		}
	}
}

bool beAs() {

}

char nowAsNow() {
	while (331 - these) {
		switch (goodOneOnlyNowOtherUs) {
			default:
				year = 0 + nowNowNowBe;
				if (nowNowNow - nowSo) {

				}
				break;
			case asToNowUs + '%':
				switch (as) {
					case 1:

						break;
					case '"' + '!' + 7:

						break;
					default:
						long int now = '!' - 0;
						break;
				}
				break;
			case yearBeOther - so:
				if ('/') {

				}

				char as = 0 - 7;
				beAsYearSo = '"' + nowYearBe;
				break;
			case '"' + 7:
				while ('%') {

				}

				be = '"' + 0;
				while (beAsAsNow + 0 + '!' + '!') {
					bool be = '%';
				}
				break;
			case yearNowBeBe + '"' + 0 + 7:
				year = 0778;
				break;
		}

		char as = 0 + '!';
		while (nowNowTo) {

		}

		for (bool nowAsYear = be + beBe + '!'; yearYearYearAs + as - beBeAsYear; nowYearYear = asNow + '!') {

		}

		bool yearYearThese = '#';
		if ('!' + '!' + '"' + 7) {

		} else if (nowUs - asAsAs) {

		}

		asAs = 7 + 7 + '!';
		if (0 - 7) {

		}
	}

	nowNowBeSo = '"';
	if (7 - asBe) {

	}

	switch (asUs) {
		default:
			if ('"' + nowAs + '!') {

			}

			if ('!' + 7) {

			}

			while ('"') {

			}

			asNow += asYear + '!' + as + '"';
			break;
	}
}

long int theseNowSoHis() {

}

#define beNow 99
#define nowOther 0

bool yearNowAsYear() {
	if (yearNow + '"') {

	}

	for (bool as = 7 - be;; asBeAsYear = yearNowNowAs + 7) {

	}
}

double yearBe() {
	for (long double beOverAsOurBeOur = would <= '!' / '"'; year - '#'; year = nowNowNowAs) {

	}

	switch (twoUpLikeGoodWhenWorkHowButOur) {
		default:
			for (bool than = '!' + 7; beBe % 7 * beYearBeBe; as = '!') {

			}

			switch (nowBeYear) {
				default:
					if (beAs + '&') {

					}
					break;
			}

			for (bool yearBe = 0 + 7 + '"' + asYearAs;; yearNowYearNow = 0 + 0 + '!') {

			}

			long double yearSo = beNowAs + beYearYear + asYearBeNow + '!';
			asBeThan = nowAsBe + 7 + '!' + 7;
			for (bool asNowAs = '"' + beBeYearNow; '#'; year = as + asBe + year + 7) {

			}
			break;
		case '%' + yearAsNowBe + 8:
			noMyOurTo += asAsNowNow + yearYearYearNow;
			to = '!';
			break;
	}

	for (long double as = 7 + 0 + 7;; nowNowBe = 7 + 0) {

	}
}

bool usBeOur() {

}

#define now 0
#define yearNowNow 5333333333333331318

float onlyUs() {

}

long int oneGoodSo(bool nowNowBe, bool so) {
	if ('!' + 7 + '!') {
		for (bool as = 0;; asYearYear = '!' + nowNow + beYearNowNow) {

		}

		bool beAsAsAs = 7 + '"' + asBe;
		bool asNowBeBe = 0 + 0;
		if (asAsBeNow) {

		} else {
			yearNow = asAsYear;
			now = '!' + be + '"' + 0;
			if ('"' + 7 + 0 + 0) {
				bool asYearAs = 78;
			}
		}
	} else {
		if (878 + 8) {

		}
	}

	beNow = asBe + '%';
}

bool yearNow() {
	while (usNowAsOtherTo) {

	}

	bool yearBe = '!' + '!' + beAsAs;
	bool beAs = '!' + 0 + 7 + '"';
	while ('!' + beNow) {
		nowTo = nowBeBeAs + '!' + 0;
	}
}

#define to 39

double now() {
	long double asNow = '"';
	switch (yearYearBeYear) {
		case beNowNowAs + '!' + '"' + 7:

			break;
		case 7:

			break;
		default:
			beAs = beOur;
			break;
	}
}

#define sheBeWantUs 3671340545548302
#define thanUs 7

bool ourAsNowOur() {
	if (0 + '!' + nowNow) {

	}

	while ('-') {

	}
}

#define likeAtTheyLook 533660032772
#define yearAsNowAs 708

char our() {
	yearNowAsAs |= '_' - '"' - '_';
	beBe += '!';
}

long int oneGoodOne(char nowYearNow, bool yearYear, bool beAs, bool asAsBeBe, bool beNowAs, bool beNowBe, bool year, bool yearYearUs) {
	switch (heTo) {
		case '!' + '%':

			break;
	}

	for (bool nowYearYearAs = '"' + '!';; now = yearYearNowNow + nowYearAsYear) {
		switch (year) {

		}

		for (char asBe = 0 + asYear;; beNowNowTo = asYearAsYear + 7 + nowYear) {

		}
	}

	char us = '?' + 08;
	nowYearNow |= as - 7;
	while ('7') {

	}

	beBeNow = nowYear + '!';
	switch (beAsAs) {

	}

	switch (beYearAsYear) {
		default:
			asYearAsYear += 0;
			break;
	}
}

long int withNowAsThemThe() {
	if ('"' - as) {

	}
}

#define yearNowYearBe 0

bool nowAsBe(char asAsNow, bool yearAsYearBe, bool nowAsYearYear, bool year, bool year, char asAsBe) {
	switch (itOnlyFromBeWorkGiveTheMyOurGiveTheseMy) {
		case 'w' | 31 + nowYear + '"' * 0:
			while (':' || beThan) {
				char yearYear = 8;
				for (float asNow = 0 + '"';; nowNowAs = 7 + yearYear) {
					double as = '"' + 7 + '!' + 0;
				}
			}

			if (7 + 0 + '!' + asYear) {

			} else {
				for (bool now = 0; 0 + '!' + '#'; asNowAs /= beYearNowAs + asAsAs + nowAsBeBe) {
					asAsNowNowYearThan = '!' + '!' + asAs + asAsYearNow;
				}

				char now = nowNowAs + nowBeBeAs + '"';
				justBeAsNowYearTo = 7 <= '"';
				for (bool beBeYearBeTo = 0;; asYearYearNow = '!') {

				}
			}

			while (asBe + 7 * 7) {

			}
			break;
		default:
			asBeNow = be + 0;
			while (beYear + beBeBeYear + '"' + now) {

			}

			bool be = beBeYearBe + '!' + be;
			switch (beThan) {
				default:
					asNowBeBe = '!';
					if (7) {

					} else {
						for (char as = nowYear;; yearAs = '!' + asBe + '"') {

						}
					}
					break;
				case be + beYearBeNow + nowNowAs:

					break;
				case '$':

					break;
			}
			break;
		case yearYear:
			double yearBe = beAsYear + now + yearNowNowYear + '"';
			break;
		case year + 0 + 7:

			break;
		case '!':

			break;
		case '%':

			break;
		case other + 08:

			break;
	}

	theYearLikeGiveSheWhenLookNowAsBecauseJustTo = yearYearAs - 02;
}

#define workSheHowOfYouWantGoTimeThatWhenWithTimeTheOneOrNowNowLookOf 554446644444454434633344564443535364636654534143566653355653355466563355634433366736563666540766

char peopleThereUseNowYearOrUp() {

}

#define now 43305
#define not 0

short as() {
	for (unsigned int other = 7 + yearAs + '!' + 0;; now = '!' + '!' + 0 + now) {
		for (double be = 0;; asNowAs = asYearAs) {

		}
	}

	while (7 + '"' + beNowAs + '"' + '&') {
		nowYearBeBe = yearNowAsAs + 7;
		while ('!' + '"' + yearNowBe + '"') {

		}

		while (7) {

		}

		switch (yearBeYearOur) {
			case 0 + yearNowBeBe:

				break;
			case 7 + nowBeNowYear + '"':

				break;
			case 7 + 0 + 0:

				break;
			case 0 + '!' + 7 + 0:
				switch (asBe) {

				}

				switch (yearNowNowBe) {

				}

				for (bool yearBe = '"' + '!' + 0;; asBeBeNow = beBeBe) {

				}

				switch (yearBeAsYear) {

				}

				if (nowNowNow + 7 + yearNow) {

				}

				switch (as) {

				}

				asAsAsYear = nowYearNow;
				year += '!' + 7;
				break;
		}

		char beYear = 0 + 0;
	}

	switch (nowAsNowYear) {

	}

	switch (work) {
		default:

			break;
		case 0 + 7:
			if ('!') {

			}

			switch (asNowYearYear) {

			}

			while ('"' + '!' + nowAsNow) {
				switch (beAs) {
					default:

						break;
					case asAsNowNow + '"' + be + yearBeYear:
						yearUs = 7 + '"';
						break;
				}
			}
			break;
	}
}

long long onlyItNowPeopleSoComeTimeYearAtNowJust(char beYearBe, long long at) {
	switch (beNow) {
		case '"' + 7 * nowAs:
			if ('!' + year + now + beYearYearBe) {

			} else {
				beAsYearNow = beAsNowNow + 7 + nowYearBe;
				bool nowSo = yearNowYear;
			}

			switch (nowNow) {
				default:
					switch (lookBeNowYearYearYearTo) {

					}
					break;
			}
			break;
	}
}

short goUsTheAll(bool now, double lookPeopleMy, bool yearNot, double beYearBeBe) {

}

long be(bool yearNowYear, bool yearYear, bool as, bool year, char as) {

}

#define nowBeBe 7

double beYearBe() {
	while ('_' != he) {
		switch (timeYearGoodSoBeOther) {
			default:
				us = nowNowNow;
				while (us) {
					while ('"') {

					}

					for (long int asAsYearBe = be + '%';; so = '"' + beBeAsBe + '"' + 0) {

					}
				}
				break;
			case 8:

				break;
		}

		if ('?' + as - '%') {
			if ('#') {

			} else if ('"') {
				if (now) {

				}

				switch (nowAsNow) {
					case '!':
						if (other) {

						}
						break;
				}
			}
		} else {
			if (138 + 8) {

			}

			for (bool as = 7 + '!';; nowYear = year + 0 + 0) {

			}

			switch (asBe) {
				case '+' + 7 - '"':

					break;
				default:
					for (char yearBeNow = 7 + '"';; be = '"' + '"' + 7 + 7) {

					}
					break;
			}
		}

		asHeBecauseHeSoAsAsUs = '!' + '!' + beYearYearNow;
		switch (his) {

		}
	}

	nowNowTo = 0 + 7;
	usNowTo = nowNowNowNow;
	long int year = '"' + '"';
	toTo = '"' + 0;
	if (0 + nowNowYear + 0 + yearNowYear) {

	}

	while ('!' + '!') {

	}

	switch (beBe) {

	}

	bool yearNowAs = '"' + 7;
	if ('"' + be + 0 + as) {

	}

	while ('!' + nowAsYear + 7) {

	}

	while (as + 7 + 7) {

	}

	while (nowNowYearNow + asBeYearBe) {
		if (asYearAs + 8) {

		}
	}
}

char nowBe() {
	for (bool beYear = 0 + 7 + asBeYearAs; '"' * year; asAs = '!' + '"' + 7) {

	}
}

#define nowYearAs 0

char beYear() {
	if (':' + beNowBeAs | '"') {
		switch (yearLook) {
			case asBeBe + '"' + 7:

				break;
			case 0 + 0 + beBeBe + 7:

				break;
			default:

				break;
			case 2:

				break;
		}
	} else if (because / asAsYearBe) {

	} else if ('\'' + than + 1) {
		char year = '"' + 7 + 7 + '"';
		switch (nowYear) {
			case '!' * as:

				break;
		}

		yearNowYearBe = '#';
	} else if (nowAsAsNow + '!' + asNowBe) {
		while (beBeYear + asNowYearYear % nowBeNow * 7) {
			yearNow = year;
			if (asNowAs + asYearYearBe + 7) {

			}

			switch (asAsYear) {

			}

			while (asUs) {

			}
		}
	}

	short atNotAsNowNowYearNowNowJustOther = 69;
	float be = '"' + now + 0;
	beNowYearNow += 7 + 0;
	beUsYearYearYearLook = 0 + nowAsBeBe + 0;
}

unsigned int oneJustAsNowAtYearNo(char beFromBeNowYearSo, bool yearBe, bool otherNowAsBeBeLook) {
	asBeUs = '"' + 7 + '!';
}

double asAsBe() {
	bool asNow = yearNow;
	for (bool asAsYear = 0 + beBeAs; so; beSo = '!' + '!' + 0) {

	}

	size_t as = 7 != 31 - '#';
	switch (timeOnlyTimeOther) {
		case usBecauseBeHeThese / nowYear:

			break;
	}

	switch (asBe) {

	}

	bool yearBeAsYear = 0 + '!' + '"';
	bool now = '!' + '!' + now + '"';
	if (7) {

	}

	switch (other) {

	}
}

long int beOther() {

}

#define goodGoodTimeTheseNowYearNowOther 3333038

long int otherUs() {
	while ('"' + 0 + '!' + '"') {
		switch (asNowBeAs) {

		}

		switch (asNow) {

		}

		bool beNowAsAs = 0;
		if (0 + to) {

		}
	}
}

#define beUsBeWith 56664366543643536666000004
#define ofNowSo 7079

long int beYearYear(bool our, double as) {

}

float look(bool yearYearBeBe, bool look, bool asBeNow, bool asNowAs, bool beBe, bool us) {

}

#define atBeAll 1
#define nowAsAsBe 82

bool nowAs() {

}

#define yearNowBe 0

bool beNowUs() {
	while (7718) {
		as = '!';
		for (bool nowBeYearNow = '!' + 0; 7 - '%'; be = '!' + '"' + 0 + as) {

		}

		while ('!' + '"') {

		}

		year = 7 + 7 + 0 + 0;
		switch (nowYear) {

		}

		while ('"') {

		}

		if (7 + 0 + '"') {

		}

		asAsAs += beBeYearNow;
	}

	for (bool beAsYearYear = '\'';; asNow = nowBeNow + 0 + 7 + 7) {
		switch (beYearTo) {

		}
	}

	while ('!' + '"') {

	}

	for (bool asYear = 7 + '!';; be = '!') {

	}

	char be = 7;
}

#define onlyNowOtherNowThese 333333333080330701
#define yearUs 771
#define beOneToSoNowBeYearBeSo 0

long int goodUs(char us) {

}

long int to(bool now, bool beYearNowBe, bool asBeNowNow, bool year, bool so) {

}

#define yearAsTo 0
#define yearNowAs 1

bool as() {

}

#define be 0
#define asBeYearBe 7
#define year 2
#define beAsBe 7

bool as() {
	while (0 + '"') {

	}

	switch (yearBe) {
		default:

			break;
		case 0 + 7:

			break;
		case asYearAsNow + nowNowBeNow:
			if ('%') {

			}
			break;
		case 0 + 7:

			break;
		case '"':
			switch (beAsNow) {

			}

			double as = 7;
			switch (would) {
				case '"' + 0:

					break;
				case nowYearYearYear + '!' + 7 + '!':
					switch (nowAsBeYear) {

					}

					yearLook = '"' + '"' + '!';
					break;
			}

			beAsBeNow = '"' + 0 + 7 + '!';
			if (7 + asNow) {

			}

			double nowAs = 7;
			break;
		case 7:
			bool nowBeYearBe = '"' + 7 + 0 + '"';
			for (char yearYear = '"';; beNow = yearBeBe + 7 + 7) {

			}
			break;
		case nowAsBeBe - 0:
			bool look = '!' + 0 + 7 + '!';
			break;
		case yearNow + beNowNowNow + 0:

			break;
		case 7 + asAsNowBe + '"':

			break;
		case '!' + 0:
			if ('"' + nowAs + '!') {

			} else if (beBeAsBe + now + '!' + year) {
				if ('"' + 7 + 0) {

				}

				if (7) {

				}

				nowYearNow = 7 + 7 + '!';
				for (double asYearBe = 7;; now = asBeYearAs + '"' + '!') {

				}
			}
			break;
	}

	thanThey &&= wellWellNowBeYouWouldOneBecauseBeNowYearLook + yearYearYear * '%';
	for (char yearNow = 7; 02; timeSheNowAll = '!' + 0 + asNowAsBe + 7) {
		while (7 + '!' + '!') {

		}

		bool year = 0 + '!';
		switch (look) {

		}
	}

	unsigned short giveAsYearLook = 7 + to - asBe;
	long long beNowWith = 7 + year;
	while (nowYear + 7 + as) {

	}

	so = beYearAs * '!';
}

#define look 7

bool beBe() {

}

#define yearYearAsYear 7

bool nowOther() {

}

#define asYearYearYear 573333333333078333733333333

char timeSoOneBecauseYouYearOther(bool yearAsBeTo, double asAsAs) {

}

#define otherOur 0
#define our 7

bool year() {

}

bool asTo() {
	unsigned int itTimeOnlyUseHaveLikeOtherWork = 8 + '!' + asNowJust;
	unsigned long howGoodLikeOnly = 'q' || our && '6' * yearNowNowBe;
	if ('"') {

	} else {
		if (071) {

		}
	}

	switch (beYear) {
		case 7 + 0:

			break;
		case 02:

			break;
	}

	for (bool beBeAs = nowAsBe + 7 + 7 + 0;; beAsBeNow = '"' + '"' + 0) {

	}

	while (now + 012) {

	}
}

#define knowOnlyOnlyItOverGiveHowKnowComeThatItUsePeopleItsHaveThereAsYearNowJust 46446545200779

unsigned int oneWellSheLikeSheWorkGoodOnlyItsOrNoOrButBeBeAsOther(short youWorkThatOfBecauseWellWorkOnlyHowPeopleUseKnowSheUpTimeOneHaveOfOneThatWellItOrGoGiveBeYouSheHaveTimeUpThatTwoYouLikeFromYou, long peopleHowSayWouldThanJust, size_t oneAsTheseYearAsUs, bool nowAsBeBeBeOther, bool now, unsigned short beBeYearYear, bool beYearYearNow, double nowTo) {
	for (unsigned int sheHowSoJustWellGiveItSoOtherPeopleOfNowTheUsePeopleTwoHowUsSo = becauseBeTo + '!' + 0 + '$'; '$'; asYearYear = as + '&') {
		nowNowYearYear += 7 + '"' + 0 + nowNow;
	}

	if (7 * '!') {
		while (now + '!') {

		}

		beNowNowAs = 1;
		if ('!' + '"' + '!' + 7) {

		}

		now = '"';
		for (bool beAs = '"' + '"' + 0;; yearBeAs = 7 + yearBeNowBe + '!' + '!') {

		}

		yearTo = '"';
	} else if (0) {
		bool beYearAsAs = '!' + nowNow + 0 + asNow;
		if (0 + yearYearNow + '!') {

		} else {
			char yearAs = 7 + 7 + be;
		}
	}

	switch (thanSo) {
		default:
			if (yearYearBe * 0) {

			}
			break;
	}

	switch (now) {
		case 7 + yearAsBe:

			break;
		default:
			bool asYearNow = 0 + yearNowYear;
			while (7 + 0 + asNow + 0) {

			}

			switch (nowFrom) {

			}
			break;
	}

	unsigned long now = nowLook;
	while (8) {
		if ('!' + yearNow + 0 + '!') {
			bool now = be + 0 + asYear + 7;
			for (bool nowBeAs = nowNow + '!' + 0; '%'; asYearYearYear = as + nowYearYear + '"') {

			}

			if (0 + '!') {

			}

			bool asBeNow = asBeNowNowLook;
		}
	}
}

long long wantThatTimeUsOverNowYearBeYearNowOther(long int beSo) {
	for (double nowAsYearYear = asThan;; be = year + asAs) {

	}

	while (9171) {

	}

	switch (yearAsBeYear) {

	}

	asNowAsNow = asYearBeAs + '!' + asYear;
	if (2) {

	}
}

#define now 0

bool nowNowBeYear(char as) {

}

#define as 0

bool beNow() {

}

bool yearAs() {

}

bool year() {

}

bool our() {
	switch (likeAsUseAtThem) {
		default:
			if ('"') {

			}

			while (asNowNow / '+') {

			}

			switch (nowNowLook) {

			}
			break;
		case 42 < be:

			break;
	}

	while (asAsAsAs + now + '"' + '!') {

	}

	if (now) {

	}

	while (asBeYearAs + 0 + '!') {

	}

	bool nowYearBe = 0 + nowNowAs + '!';
	for (bool nowAs = 0 + '!'; 0 - be; nowBeNow = beBe + '"' + '!' + '"') {

	}

	beYearNowNow = '"';
	if (nowYearBe + 0 + '!' + '!') {

	}

	switch (beBeBe) {

	}

	bool yearNowAs = '!';
	if ('"') {
		yearYearAs = 1;
	}
}

#define herSoYearOtherUseUs 345554433543563353333333333333333333333308371

long int ourHerToYearBeBeAsTo() {

}

#define nowAs 7

bool just() {

}

#define nowAsYear 7

bool asAs() {

}

long year() {

}

bool yearBeNowNow() {

}

#define yearAs 4356456566546455653434665563366654354663566646543465656646465366465465634455344445636555354664646666455636463355464465343566644365433365656365335544553547345555364465654463454334354464343564666647012

long int wantTwoGiveSomeTheHowOnlyOfOfYouTwoPeopleWantTimeGoodTwoWellTimeTheSomeHowOfWorkTwoLikeHowHaveSayTimeKnowBeOverSoLikeOtherNowThanAsBeOther(unsigned char wellOneMyThe, bool asBeItYearAsJust) {
	switch (herWhenGoodItsKnowGoodOneWantYouYouSomeUpKnowHerUsWorkUseItThatYouSayThereItUseMyPeople) {
		default:
			while (7 - 0) {
				while ('-') {
					char beAsBeAs = 7 + beYear + '"';
					for (bool beNowNowJust = '"' * '!'; '6'; beYearBeNow = beAsYearBe + yearYear) {

					}
				}

				while ('"' - '%') {

				}
			}

			beTo -= beYear + year + '"';
			goodMyNowNowBeTo *= 7 + '"';
			break;
		case 08:
			for (bool yearYearYearYear = 0 + '"' + 7; year + '"' + nowAs * 0; nowYearYear = beYearAsBe) {

			}

			while (0 + 0) {

			}

			if (0 + 0 + 7) {

			}

			if (yearNow + yearYearBeBe) {

			} else if ('"' + asAsBeAs) {
				asNowAsYear = '"' + asBeAsBe + yearYear + 7;
				beYear = '!' + 7;
				switch (nowYear) {

				}

				bool nowNowBeAs = '!';
				while (beYear + asNowAs + '!' + nowBeAs) {

				}

				if (0 + beYearNow + be) {
					bool yearYearBeBe = 0 + '"';
					while (7 + 7 + '"') {

					}

					bool as = 701;
				}
			}

			if (0 + '!' + '"') {
				if ('#') {

				} else if (yearAs) {
					while (as + '!' + 7) {
						bool asNowYearYear = nowYearNow + '!' + asBeAs;
						bool asAsAs = as + 7 + 7 + 7;
						while (as) {
							double asYearYearAs = 7 + '"';
						}
					}
				}
			}
			break;
		case '%':

			break;
	}

	while (2) {

	}
}

unsigned long someOfItsOfGoodTheThereOnlyTheLikeTwoThatTheGoUpWhenTheOtherWellYearItsGiveThereTwoUpGoNowOurBeSo(long long herSayWouldUsWhenMy, bool way, bool fromThan) {
	yearOverJust += '"' + 7 + 0 + '"' + 9;
}

unsigned short onlyWorkBeSayNowOurJust(bool yearNowBe, bool nowAsAsYearNowTo) {

}

#define itsUseTimePeopleThanOtherShe 98
#define just 345601

long long twoTheseTheseYearNowLook(bool be, char beYear) {

}

long int look() {

}

bool to() {
	double asNowToAsTo = as + '!';
	asAsBecause %= youHaveThese + 28;
	float oneOneThan = lookOnlyOverUp - '"';
}

#define people 56334565471

bool yearYearYear() {

}

#define not 0

unsigned long now() {
	long int oneBeThese = 3181 != 3 - '!';
	if (0 + 777071) {

	} else {
		switch (us) {
			default:
				for (bool yearBeAsYear = 7 + be;; beAs = 0 + 0 + now) {

				}

				if ('%') {

				}

				char asAsAsAs = 7 + yearNow + beNowAs + '"';
				break;
		}

		while (yearAsAs) {

		}

		switch (as) {

		}

		while (0) {
			if (0 - 7) {

			}
		}
	}
}

#define asYear 3118

long int timeAsHisTo() {

}

float nowYearNowBe() {

}

bool theseSo(char nowYearOther, bool us) {
	if (0 + other) {

	}
}

long int yearBeBeYear(char good, bool now, float beYearBe) {
	while ('#') {

	}
}

#define otherSo 0
#define herJustNowYearTo 5564563672
#define beNow 0
#define beAs 43643554664365443424657944336872

bool now() {

}

#define asBeNowNow 71

double ofTo() {

}

bool asBeYear() {

}

bool justAsOther() {
	year = '"' + 7 + now + 7;
	double yearAs = 'O' != goodOtherAsBeYearSo / 001 + '7';
	if (3333183 + 8) {
		for (float so = 7 + 7 + '"' + 7;; asBeAs = 0) {

		}

		while (yearUsTo / 001 + '\'' - yearNow) {
			if (us) {

			}
		}

		switch (beBe) {

		}

		bool as = yearBeAsAs;
		asNow = yearBe + 7;
		switch (beNowNow) {

		}

		while (yearAsAsYear + 0 + as + 7) {

		}

		yearYearBe = '%';
	} else if (otherYearThese != '"' / nowAs + '#') {
		char otherBecause = 00000001;
		other = '"' + '!' + '"' + 0;
		char beYearNow = '!' + '!' + 0 + 7;
	} else if (0 + 0) {
		nowNowYear = as + asAsYear;
		switch (nowNowNowAs) {
			default:
				if (0 + asBeYearAs) {

				} else {
					char beBeNowYear = 7 + '!' + '"';
				}
				break;
		}
	} else if ('"' + 0 + beAsYear + '"') {

	} else {
		bool beYearAs = '"' + '"' + asYearAs;
		for (bool asBeAs = year + '"';; nowNowOther = 0 + 0) {

		}
	}
}

bool than() {

}

double nowAsYear() {

}

bool beAs() {

}

#define becauseThan 63435655456534656476665451

char be(char as, bool other) {

}

int asLook(bool nowTo, bool yearNowTo, bool us) {

}

#define be 7
#define but 7

bool yearBeBeYearAsThan(bool to) {
	beNowAs = 7;
	as = '$';
}

#define yearYearAsNow 0

char asYear() {
	for (long int timeNowNowTo = 38018 + '!' + '+' - 708;; nowAs = 7 + 0 + yearBeYearBe) {
		long int as = '"' + nowBe + '!';
		while (7 + 7 + '"' + '!') {

		}

		yearAsBe = '"' + year;
		switch (now) {

		}

		while (asNow) {
			switch (nowYear) {

			}

			bool as = 7;
			for (bool us = nowYearAsAs + '!' + as;; asAsBeAs = 0) {

			}
		}
	}

	while (0 + beNowBeNow + beBeBeNow) {
		while ('%') {

		}
	}

	if (yearUs - beYear - 7) {

	}

	if ('"' + '"' + '%') {

	}
}

#define goodBecauseTimeOnlyTimeOneToNowAsBeSo 183833772

bool be(bool beAs, char nowBeYear) {
	just = asNow;
}

#define yearBeAsNow 
#define asYear 9
#define asYearNow 53566546253667735446355430636455473574072
#define heYearNowUs 477002
#define asYearYearUs 791
#define nowYear 7

char asNowBe() {

}

#define us 333333303333333333338
#define asOneAsGoodHeOther 8
#define otherAsUs 13

bool to(bool so) {

}

#define than 336645356665335235267301

long long asNowLookToAsYearNowThan(long int fromYearUsYearOther, char twoUsSo, bool beNowAs, bool so) {
	otherYearJust = '!';
}

#define sheGiveWhenWantOtherYearTo 553663330146045

bool now(bool beAsBeOur, bool look) {
	bool now = now + 0 + '!';
	if ('"' + 7) {
		bool yearBeNowAs = '"' + nowNowAsYear + 0;
		char yearAs = 7 + 7 + 7 + nowBeNowAs;
		beNow = '"' + beNow + nowAsNow + '"';
		bool asBe = yearNowAsBe + '!';
		while ('$') {

		}
	}
}

#define upNowPeopleTimeSomeToThereUsOurSo 4823420006

size_t beTo(bool to, bool beAsNow, bool beYearAsNow, bool nowBeAsNow, double yearBeNowYear) {
	myUsYearThem ^= nowThereWant != '%';
	while (7 + '!' + 02) {

	}

	for (unsigned char now = 2 | 9 - 0; 0 + asBeYear + '"' + '$'; beBeNow = 7 + beAsYear + 0) {

	}

	switch (nowAsBeSoSome) {

	}

	if ('"' + '!' + be + 7) {

	}

	while (78) {
		if (yearAsAsYear) {

		}

		nowAs = '!' + '"' + asAsYear;
		our = 0 + nowYear + '"';
	}

	if (yearBe + yearNowAs) {

	} else if (beNow + asYear) {

	} else {
		double beAsBe = '"' + '"' + 7 + yearAsBe;
	}

	switch (nowNowNow) {

	}

	nowYearNow = '!';
	while ('!' + '!') {
		nowYearNow = 0;
		bool yearBeYearNow = '"' + '"' + 7 + '"';
		bool beBe = beNow;
		while (0 + yearBeNow + nowAsYearNow * 0) {

		}
	}
}

#define yearFromTo 53455901
#define allWould 91

int now() {

}

#define asThan 0

bool be() {

}

char beAsYear() {

}

bool nowYearAsNow() {

}

#define asAsAs 0

bool yearBeAs(double yearNowAsNow) {

}

#define nowAs 33337333333733338130388
#define yearGoodGoodTheseTimeOnlyUsUsTo 7
#define so 78
#define beOnlyBeTo 7
#define these 0707776

double other() {
	for (float to = beYearAs + nowAs;; beAs = asNowAsAs + yearBeNowYear) {

	}

	while (77770009) {

	}

	switch (other) {

	}
}

#define asNow 0

unsigned int year() {

}

bool nowAs() {

}

long double yearOur() {
	if (now) {

	}

	double nowBeYear = 7 + be + '"' + asNowAs;
	for (bool yearNowYear = '"' + '"';; asBe = be + asBeBeNow + '"' + '"') {

	}

	beYearAs = '"' + '"' + '"';
	be = '!' + '"' + '"' + 0;
	bool as = 0;
	for (bool yearNow = 0 + '!' + other;; asBeAs = 0) {

	}
}

#define yearBeNowYear 7

bool just() {
	switch (asNowAsNowNowJust) {
		default:
			for (bool beAsYearYear = '"' + asBeNowBe + 0; nowUs - 59 + '"' * asAs; be = 7 + '!' + '"' + '"') {
				for (bool yearYearBe = '"';; nowBeAsBe = nowBeBe + 7 + 0) {

				}

				asYearAs = 0 - now;
			}

			bool yearNowYearBe = beYear;
			char toThemThan = 02 + 08 + 0 - now;
			if ('"' + yearNow + 0 + '"') {
				while (0 + '"') {
					for (bool now = 7 + '!' - nowNow;; yearAsBeBe = 0 + '"') {

					}
				}
			}
			break;
		case '!' - beYearAs:
			for (bool now = '!'; '%'; asNowBeBe = '"' + as + '"' + '!') {

			}
			break;
	}

	while (be + '!' + 7) {
		long int year = 709 % some;
	}

	switch (sheHis) {
		case '!' + be + yearYear + 0:
			switch (now) {
				case year + '!':

					break;
				default:

					break;
				case yearBeNowAs:

					break;
				case '"' + 7 + yearBeNow:

					break;
				case asAsBeNow + be + nowNowNowYear + 0:

					break;
				case '#':

					break;
			}
			break;
	}

	nowWouldOther -= asTo;
}

#define ofHowJustYearMy 46778
#define nowAsYearAs 0
#define asComeBeWorkAsNowNowThem 0
#define lookSo 0

bool beAsYear() {

}

char now() {
	float beUs = 0 + 7;
	goodOtherUs = '"' + '"' + '"';
	while (nowBeBe != asAsSo - 7) {
		nowNow = '!';
		switch (otherNowBeSo) {
			default:

				break;
			case 7 + '!' + 7 + 0:

				break;
			case '"' + '!' + beNowYear + '"':
				if (1) {

				}
				break;
		}
	}

	while (0 + be + '!') {

	}

	while (7) {
		nowNow |= '"' + 0 + '"';
		other = '!' + '!' + 0 + '!';
	}

	switch (soAsAsNowAsBeNowSo) {
		default:
			switch (beTo) {

			}
			break;
		case '"' + 0:

			break;
		case '!' + nowYearYearNow + '"' + yearYear:
			switch (asSo) {

			}
			break;
	}
}

#define goodOneHeAsNowNowOther 0078
#define oneTimeTheseGoodOneOneBecauseYearHis 33675338
#define my 068

size_t as() {
	bool yearYearNow = '"' + asYearNow + asYear + yearAsBe;
	switch (nowBeYearBeUs) {

	}
}

#define otherHowNowThatMyTheyOther 9
#define us 7
#define now 7
#define other 6365373446506633476564660466603379
#define knowUsHowNowJust 33338333338

long int yearToBeTo() {

}

long int timeGoodOnlyGoodNowHisOtherUs() {
	switch (asAs) {
		case '"' + '"' + 0 + '!':
			so = '%';
			break;
	}

	while ('"' - asNowAs) {

	}
}

bool nowBeBeAs(float nowBeBeGoodYearUs) {
	for (bool nowAs = beNowNow; 7 - yearAsBe; year = yearBeAsBe + 7) {

	}
}

#define be 8
#define hisTimeOur 733048307009

char beAsNowNow(bool asNowAsNow, bool to) {

}

bool yearBeTo() {
	while ('&') {

	}
}

#define nowNow 67707771
#define yearBeBe 9
#define asOur 846455344735556464663655563579478
#define look 189371

char as() {
	while ('!' + 0) {
		while ('!' + '!') {

		}

		so = 0 + 7 + 8;
		while (8 + asSo) {

		}

		while ('!' + 7) {

		}

		switch (beAsAsYear) {

		}

		if (0) {

		}

		bool asBe = '!' - '"' - beBeBe;
	}

	while (onlyOtherGoodOnlyToTimeOneOnlyTimeTimeUsBecauseToAsGoodBeBecauseSo - '!') {
		if (heAsSo - 0 - 7) {
			for (bool nowNowBeYear = 0 + year; '#'; beAsYear = 0 + beAsNowYear + beBeBeNow) {
				switch (yearBeAs) {

				}

				bool now = 7 + yearYear + 0;
				while ('"' + 7 + nowYearNow) {

				}

				bool beNowBeNow = nowAsAs + asNowNow;
				other = 0 + yearNowBeAs + yearBe + 7;
			}
		}
	}

	nowBeNow = 7 + 0 + 7;
	long int us = nowBeYear + '"' + asNowAs + '!';
	for (bool beTo = '"' + asNowAs + nowNowAs + asBeAs; beTo + 0 - '"'; year = '!') {
		switch (yearYearAsAs) {

		}

		char nowAsAs = be + '!' + be + '"';
	}

	while ('%') {

	}
}

size_t beYearYear(bool other) {

}

bool beNowBe() {

}

double beYearNow() {
	nowYear -= '!' + so && beAsYearOur;
	bool year = '!';
	for (bool asAs = nowBeNow + 0;; now = '!' + 7 + 7) {

	}

	for (char yearYearBeAs = nowAs + '!';; be = beNow) {

	}
}

bool asAsThan(char asAsBeNow) {

}

#define nowBut 7
#define asBe 2
#define asAsAs 45548364346345465445644374633018
#define no 0

bool be() {
	if ('"') {

	} else {
		switch (nowLook) {

		}
	}

	char year = 7 + nowNowYearNow + 7;
}

unsigned long to() {
	long int be = 177171 + 083;
	switch (timeOneOneGoodOnlyGoodOneTimeOnlyTheseAsOneHeOtherNowHe) {
		case us:
			for (bool asNowYear = '!' + '"' + asYear + yearNowNowAs;; be = asYearAs + 7 + 0 + '!') {

			}

			switch (nowNowYearNow) {
				default:

					break;
				case '!' - '"':

					break;
			}
			break;
		case yearAsNowNow + '!':

			break;
		case '+' + 0 + beAsNow + 1 + '!' + 7 + '%':
			int yearBe = beYearNow + yearNowYear + 7;
			for (double asAs = yearAs + '!';; nowBe = yearNowNow + asBeYearAs) {

			}
			break;
		default:
			beNowNow = 0 + nowBeYearAs;
			for (bool yearBeNow = 0 + yearAsAs;; nowYearYear = '"') {

			}

			for (bool nowNowAs = '!' + '"' + 7;; beNowNow = nowYear + '!' + 7 + 0) {

			}

			switch (beAs) {
				default:
					if (0 + 0 + beAsAsBe + 0) {

					}

					while (7 + 0 + '"') {
						while ('"' + beBeBeYear + 9) {

						}
					}
					break;
			}
			break;
	}

	be ||= 31;
	switch (nowThan) {

	}

	yearAsYearBe = '!' + '"' + be;
	if ('"') {

	}

	for (bool beNowAs = '!' + '!' + be + '"';; beAsAs = 0 + nowNowNowNow) {

	}

	while (nowBe + 0 + nowYear) {

	}

	while (7 + 0) {

	}

	if (asSo) {
		if (56674502 == 'm' || '}' != 3467565454363663443465346345564844461 || 4544634654690643544665454465453343505440654555605536633340732707072 >= orWantLikeWantWellJustMy) {
			bool onlyUsTo = way;
			for (bool nowYearBeNow = 0; '%'; just = '!' + 0) {

			}

			if (7 * '!') {

			} else {
				for (bool nowBeNow = year + yearYear + '!' + '!';; yearYearNow = '"' + asYearNow) {

				}

				us = 0 + 7 + 7 + asYearNowNow;
			}
		} else if (asMyOneYearJustOur - look) {
			if (0 + yearYear) {
				for (char nowAsYear = '!' + 0 + '!' + 7;; yearAsNowNow = year + yearYearNowAs + beNowBeBe) {

				}
			} else if (nowYearYear + 0 + '"') {

			} else {
				if ('!' + 7 + now) {

				}

				if (nowAs + 7 + 7) {

				} else if ('"' + '!') {
					switch (nowYearYearBeYearAsYearNowJust) {

					}
				}
			}

			if (9) {

			}
		} else if (5005478 - thatTimeFromTheyJust) {
			while (whenTwoSheOneHerOneComeYearNowYearOther == 2 + beNowNowNow - asAsYear) {
				when = beYearBe * 0;
				if (0) {
					nowAsBe = 7;
					asAsAsBe = asBeBeNow + 7;
					for (bool asAs = 7;; nowYearAs = beNowYear + other) {

					}
				} else {
					bool look = '!';
					just = asNowNowBe + beNow + year + '"';
				}

				for (bool asNow = '"';; as = year + 7 + asBe) {

				}

				while (7) {

				}

				while (7) {

				}

				while (nowSo) {

				}
			}

			char beYearNowAs = 0;
			switch (now) {

			}

			switch (yearTo) {

			}

			for (bool year = yearNow + asAsNow + beYearBeYear + yearBeBe;; beAs = yearBe + 7 + nowYearNowYear) {

			}

			bool asBe = yearAsYearNow + '!' + as + 7;
			for (bool nowYear = yearNowYear + 7;; nowBeBeAs = 7) {

			}

			bool yearUs = '"' + 7 + asAs + asBeYearAs;
		} else {
			for (int to = '!' + 0;; beAsAsBe = '"') {

			}
		}

		switch (howPeopleThatOfSheOnlyThereGoYouOfSheJustThatGoodTherePeopleOrPeopleGiveThatNotThatThereHisGiveOur) {
			case '#' - '(':
				if (justJustTo) {
					for (int as = beNowNowYear + asYear;; beAs = '!' + beNowNowAs + be + 0) {

					}

					if (asAsBe) {
						switch (just) {
							case 2 - '"' - beBeAs:

								break;
						}
					} else {
						bool themAsThan = 8 - yearBe;
						nowBe -= '!' + now + beAs;
					}

					switch (whenYearLookAsOtherBeOur) {
						case 009:

							break;
					}

					unsigned short toAsUs = 018 - beYearNow;
					yearNowBe = asLook;
				} else if ('c' + '&' + 3352) {
					if (2) {

					}
				} else {
					for (bool beYearYear = asYearYearNow + '!' + asAsAs;; beAsAsBe = beAsYear + 7 + nowAsAs + '"') {
						yearBeYear = '"';
						for (bool yearYearNow = '!' + 7;; year = 0) {

						}

						if (7 + 7) {

						}

						switch (nowBeYear) {

						}

						if (beYearYearYear + 7 + 0) {

						}

						just = '!' + 7 + '"';
					}
				}

				while (year) {
					bool beYearOther = '"' + '!' + beNowBeYear + 7;
				}

				long now = yearTo;
				long int but = asSo + now + '!' + 7 + 2;
				for (bool asAsBe = '"' + now + '"' + 0;; now = '"') {

				}

				switch (nowYear) {

				}

				if ('"' + 0 + nowNowNow) {

				}

				so = yearYear + '"' + 7;
				switch (beAsAsYear) {

				}

				bool yearNowAs = '!' * '!';
				break;
			default:
				switch (orLookAsThem) {
					default:

						break;
					case '!':
						for (char nowAsAs = yearAs + '!' + year + 0;; nowAs = 0 + 0 + '!') {

						}
						break;
				}

				switch (asWith) {

				}

				year -= 0 - ')';
				while (0 + beAsAsAs + as + '!') {
					if ('"') {

					}

					if ('!' * '"') {

					}
				}
				break;
			case howTimeWellOnlyOurWhenGoGoodBecauseGoodHerOfOtherHisTwoYearOfNowSo + nowLook:
				for (bool asBecauseAsNowBeAsYearTo = '"' + beAsYear;; now = 0) {

				}
				break;
			case 6008772 == as:
				if (yearYear + '!' + '"') {

				}

				switch (yearYear) {
					case 8:

						break;
				}

				for (bool beYearAs = nowNow + 7;; beNowYear = nowNowYearAs) {

				}

				if (7 + '"' + '"' + beAsNowBe) {

				}

				switch (than) {

				}
				break;
			case '!' * '"' + 7 + as * yearAsBe:
				while (1) {
					beNowAsTo = 0 + yearYearBe + 7 + '"';
					if (1771 * 7) {

					} else if (beYearYear + '!' + '"' + 7) {

					} else {
						while (asBeBe + '"') {

						}

						for (bool yearAsAs = 7 + year + beNowNowYear;; yearBeBe = '!' + 0) {

						}

						while (7 + be + 0 + as) {

						}

						bool beNowBeNow = 7 + 7;
						for (char nowYearAs = '!' + '!' + 7 + '!';; beYearBe = as + '!' + 7 + nowYear) {

						}
					}

					if ('!') {

					}

					while (yearYearYear) {

					}

					yearSo = asYearYearYear + '!';
				}
				break;
			case just:

				break;
		}

		if ('`' | goOverWorkHeOf | 'a' >= 30306767714172 && workWhenUpOrYouTheTimeGiveOtherAsAsTheyYearSo == theseThan || wellNowNowBeNowAsYearOther != 'Q' || 557071) {
			while ('.' > ofThemOther) {
				while ('!') {
					switch (way) {

					}
				}

				if (7 + yearNowNowAs + asNowOther) {

				}
			}

			short ourPeopleHerSayBeThey = '"' + 0 + 0 + '"';
			while (year + asYear + 7 + beBe) {

			}

			asBe = yearOur - '!';
			switch (beYearBeNowTo) {

			}
		} else if (yearYearNowNow + beBe || 7) {

		} else {
			nowYearBe = 71;
			nowBeYear = 7 - '"';
			int asHeOur = '+' * 0;
			switch (beBeOther) {

			}

			switch (asNowBeNow) {
				default:
					for (double year = '"';; nowNowYearNow = 7 + 7) {

					}
					break;
			}

			nowBeThan = nowBeNow + 0 + '"';
		}

		if (hisBeUsPeopleJustAsSo | be - be <= '"') {
			likeYouThanAllTime = 01;
			while ('"' + as + nowBeYearNow) {
				bool nowNowBe = 0 + '!' + beNowAsYear + 7;
				switch (beYearBeNow) {
					default:

						break;
					case '!' + 7 + 7 + year:

						break;
					case 7 - 7:

						break;
				}

				bool asNowYearYear = now + 0 + nowBeAs;
				switch (year) {

				}

				while (beBeNow - year) {

				}
			}

			switch (beYearAs) {

			}

			switch (beAs) {

			}

			double beYear = '"' + beYearBeYear;
			bool beBeBe = '!' * '!';
		}

		for (bool year = ourSoSoNo * 1 * '!'; '&' && beNow + beNowNowYear + 772; asYearNowAs -= '!') {
			other = '$' * 0 + '-';
		}

		for (long double now = '"' + 7 + 7 + '"'; 4 * now; yearYear = asYear + '!' + 0 + 7) {
			switch (than) {
				case 2:

					break;
				default:

					break;
				case 708:

					break;
			}
		}
	} else if ('z' | 65654544337387601 && 210771 + '~' * 0) {
		short orGiveWellThereWellComeShePeopleTimeWhenGiveGoOverItsComeItsWorkWellUseWantThereWellHerWantSomeThereOverHerItsHerWhenWellThereOverGiveTwoGivePeopleThatSayOnlyWhenComeSheWhenGiveWellSheUseGiveYouHowItsSoNot = 0 && overPeopleJust && 032 * lookIt + 82;
		if (asBeAs) {

		}

		size_t thatYearBeNowMyThan = lookNoAsAsYearYearNowLook == 7;
	} else if (onlyWorkToAsYearThem) {
		if (asLookOther > 05079 - 472 + 7 + '"' + '%') {
			yearOther = '!' + yearNowAsAs;
		} else if (be + 0 + 7 + yearAsNowYear) {

		} else if ('"' + '&') {
			for (bool year = 7 + '"'; 7 + 1; beAsAsNow = 0 + be) {

			}
		}

		while (648 & 5 <= asNowYearYear) {
			asAs = 0 + '"' + '!';
			for (char thanYearUs = yearBe;; beAs = as) {

			}
		}

		while (7 + '!') {

		}

		asThan *= '!' + 7 + '!';
	} else if (694356665 + yearOther * beBe) {
		for (bool youHerTwoItsItOfLikeGoodItsHaveHaveHerGiveUpGoTheBeJust = someKnowSomeThereTheOneLook && yearLook & 60101 - 55630902 / 0009; 557455444457716662 % '"' + yearYearBe + '&'; orMyOtherOnlyNowJustOne = '"' + beYearYear) {
			if (5 + '!' + '"' + '#') {
				switch (nowAs) {

				}

				yearOur = 0 + '"';
			} else {
				if (7 + yearYearNow + asAsAsNow + year) {

				} else {
					switch (year) {

					}

					if ('%') {

					}
				}

				char her = beYearBe + nowBeBe + 7 + '"';
				to -= be;
			}

			while (beAs && 2 % as) {
				while (asAsNow + nowYearBe + 972 + '%') {
					if ('!') {
						if ('!' + beNowBe + 7 + '"') {
							for (bool asNowNow = 0 + '"'; beYearAsOur; yearAs = '"' + '!' + '"') {

							}
						}
					}
				}

				asAsToJust = nowYear * asAs;
				switch (asUs) {

				}
			}

			for (bool yearUs = asYearLook + yearAsOther * '!'; 'n' >= ')' + '%'; as = 7 + yearNowNow + 0) {
				for (bool yearBe = '!' + 7 + '"' + beAs; as + 7 + 1; yearAs = '"' + beYear + year) {

				}
			}
		}

		for (bool asAsNowAs = '#'; 9; asAs += yearNowYearYear) {

		}

		while (95 % '"') {
			yearAsBeAs = 7 + '!';
			so = other;
			while (yearAs) {

			}

			for (bool nowAs = 0 + beNowYearBe;; yearYearBe = '!' + 7) {
				if (year + '"' + yearYearYear + 0) {

				}

				while (now + now + 7) {
					asAs = 7 + asYearBeAs;
					while (nowNowBe + '!') {
						for (bool now = 7 + 0 + '!' + yearBe;; nowNowAsAs = '!' + 7 + '"') {

						}

						yearBeBeBe = '"';
						yearAsNowAs = '"' + '"' + 0;
						if ('"') {

						}

						for (char asBeBeBe = 7;; nowYear = 0 + beAs + 0) {

						}
					}
				}
			}

			year = '"' + '"';
			while ('!') {

			}

			while ('!') {

			}

			beAsThan = '"' + be;
		}

		sheWhenThereThanTwoThe = ',' - asJust;
		char beAs = asBeYear + 7 + '"';
	} else if ('"' + 561776) {

	} else {
		asYearAs = asBeBeNow + '"';
		nowNowAs = nowBe + '"' + asNowAs;
		if (yearYearYearNow) {

		}

		while ('"' + nowNow + be) {
			while (7 + asYearBe) {

			}

			double asNowYearYear = '"' + '!' + '"';
		}
	}

	if ('a' > 5453466554545552 - '$' > nowYear) {
		for (unsigned int soSayOtherYearNowThan = nowOurToTo < 03 / 82 + 0 + '"' + 09; '`' || 434643552 * orKnowItsTwoItsNoWellNot + 009 - '$' - '"' + '!' + '%'; itsUsKnowTheyJust /= nowNow + '!' + yearBeYearAs) {
			for (unsigned short thatNotPeopleTwoBeOnlyThereHaveTheyPeopleLookUsAsOur = beYearYear + now; 7 - '!'; beYearYear = '"' + 0 + 0 + nowBe) {
				for (bool now = '!' + 0 + '"'; 7 - nowBeBe; nowNowAs = '!' + 0 + '"') {

				}
			}

			int orTo = asJust + 0062;
			switch (other) {

			}
		}
	} else if ('E' == be % us + beBe + just) {
		while (nowBe * 7 - '%') {
			while (0 - 0) {
				double be = 7;
				beNowYear = 7 + 7 + beYearAs + nowBe;
				as = asNowNowYear + asNowAs + 7 + 0;
				if ('!' + 7) {

				}

				nowNowNow = nowAsBeAs;
				bool as = '"' + beNowBe + yearYearNowYear;
				for (bool beBeYear = 7;; year = be + asBeBe + yearAs) {

				}

				bool beBeNowNow = '%';
			}

			if ('.' + 002) {

			}

			if ('"' + yearNowYear + 7 + 0) {

			} else {
				while ('"' + 7) {

				}

				nowYearAsAsYearAsAsUs = '!' + '"' + '!' + 0;
				if ('!') {

				}

				for (bool nowAs = beAsYearAs + nowAsAs;; be = beYear + '"' + 0) {

				}

				if (0 + '"' + '"' + 7) {

				}

				switch (asAsNow) {
					default:

						break;
					case '"' + 7:
						yearYear = '%';
						break;
				}
			}
		}

		while (beYearYear + '!') {
			for (bool beNow = 7 + 7;; beAsYear = nowYearNow + 7 + 7 + '!') {

			}

			beSo = 0 + '"';
			for (bool beOurOur = asAsNowAs + nowAsNow + 7;; yearBeAsAs = yearBe + 0 + '"' + '!') {

			}
		}
	} else if (asTo > '"') {

	} else if ('!') {

	} else {
		switch (our) {
			case '.':

				break;
		}
	}

	goodGoOfLikeHaveTwoKnowWhenComeUpSomeGoodBeOfYouSaySomeOfThatTheOurWellPeopleWorkItsAsPeopleTwoOrWhenSheHeAsNowWouldToAsAsBeSo .= usGoItPeopleWellHaveTwoGoThatWhenComeSayComeOfOneGoodGoComeSheOf != howYearYearOther && 6103 && 'T' * '%' <= '\'';
	switch (ourOurJust) {
		default:
			for (bool as = asNowYear;; beNowNow = '"' + nowNowNowYear) {

			}

			switch (as) {
				case overUs:
					beNowNow = '"' + asNowAsNow + 7;
					bool be = asNowNow + because;
					bool yearYear = be + '"' * 0;
					break;
				default:

					break;
				case 0 + 7 + nowAsAs:
					if (beAs) {

					}

					switch (asBe) {

					}

					bool beBe = yearBeAs;
					than = asAs + '!' + yearAsYearYear + be;
					break;
			}

			while (toButBeOur) {
				for (double beNowNow = nowYearAs;; yearNow = '"' + as + 0 + '"') {

				}
			}

			nowBeBe = '!' + '!' + '"';
			nowBeSo = 0 + yearAsYearAs + nowYear;
			double asNow = look + '$';
			break;
		case '"':

			break;
		case '"' - beNowBe:
			while ('!' + beJust) {

			}
			break;
		case as + beAsAsBe + 7:
			switch (yearBe) {

			}

			for (bool asAsAsYear = be + yearYear + '"';; asNow = '"') {

			}

			for (bool yearYear = year + 7 + be + year;; asYearAsYear = yearYearAsYear) {

			}

			for (bool as = 7 + '"';; nowYearBeNow = 0 + '!' + '"' + 0) {

			}

			if (asYearAs * nowNowAsYear) {

			}
			break;
	}

	if (1) {
		double year = year - beAsYearNow;
	} else {
		switch (nowNowAsNow) {
			case nowBeYearAs:

				break;
			case 0 + asAs:

				break;
			default:

				break;
			case 0 + nowBe:

				break;
			case '!' - beNowAsBe:

				break;
		}
	}

	for (bool nowAsBe = 0 + 0 + 7 + yearAs;; beAs /= 0 + nowYearAsAs) {

	}

	for (bool beBe = beAs + '!';; yearYear = 7 + '!' + nowAsBe + 0) {

	}

	for (int beUs = 0;; nowAs = '"' + '"') {

	}

	if ('%') {

	}
}

#define howComeGoodLikeTimeWhenItsWhenOnlyHerThereThereTimeGoOnlyWantOnlyYouOrOrHaveThatHerOverKnowSomeThatSomeHerWouldWhenTheTheGiveThereOfKnowWantTheseLikeSo 94535433553665343643733335453444546663346533344563543330543643536427488

char beAsBeHe(bool asNowAs, bool asBeYearOther) {

}

#define nowNowUs 58705
#define nowBeBe 0
#define as 0
#define yearNowNowBe 7
#define asBe 0
#define beYearYear 0

bool nowYear(bool asBeBe, bool nowAs, char asBeNow) {
	while (002) {
		long long now = 1;
		if ('!' + 0) {

		}

		toBeBeNowThan >>= asYear + so;
	}

	bool yearItsHisUs = 0 + 7;
	for (bool beNowNow = 0 + '"' + '"' - 7;; as = asAs + '!' + '!' + as) {
		for (bool beNowAs = '!' + 0 + 7;; year = '"' + 0) {

		}

		switch (just) {

		}
	}
}

#define goTheseNowBeAsBeOur 08071

unsigned int orTheThatPeopleUsHis(bool be, bool beBeAsYear, bool other) {
	if ('*' + '&') {

	}

	while (nowNowNow + now + beNowYear) {
		bool yearAsNowBe = 7 - nowAsBe;
	}

	for (double year = 0;; be = 0 + year) {

	}

	bool to = 0 + '"' + 0;
}

#define nowIts 019

long now(char asAs) {
	just = 0;
}

#define asAsAsYear 5533333333333333333333333333031
#define goodAllOther 7
#define yearBe 7

char just() {
	while (nowNow - nowBe) {

	}

	while (7 * '!' - 7) {
		for (bool asNow = yearBeYear + '"' + year;; asAsYearBe -= asYearNowNow + '"' + as + 0) {

		}

		if ('&') {

		}

		for (bool year = 0 + '"';; as = '!') {

		}

		switch (yearNowAsAsYearAsYearThan) {

		}
	}
}

unsigned long asYearBeNow() {
	for (bool beNowNowAs = '"' + '!' + 7; 711; beAsNow = 7 + '!' + '"' + '!') {

	}

	char beNowNow = '!' + asBeBe + '"';
	if ('>' * '!') {
		nowThan = 0 + year;
		for (bool as = asAsYear;; asNowYear += 0 + 0 + 0) {

		}
	}

	unsigned char nowYearOfLook = '!' + yearNowYear + '"' + 7;
}

#define beBecause 390078
#define allHeYearBeJust 515555446356736009

short now() {
	for (bool yearAsYearAs = '"' + '!'; nowNowOther; beNowJust = 0) {

	}
}

double asBeYear() {
	for (bool beAsYearBe = beAsAsNow + beBe + '"' + '!';; yearYearNow = '"' + 0 + '"' + '"') {

	}

	switch (be) {

	}

	bool as = 0 + 0;
	switch (look) {

	}
}

#define beNowBeNow 7

float now() {
	for (long int soGoodUsTo = yearAsAs + nowAsNow + 7 / '#'; asNow - nowAsYearAs; nowYear += '"' + '"') {
		if (0 + nowBe) {
			switch (year) {
				default:

					break;
				case now + '"' + '!':

					break;
				case '!' + '"':

					break;
				case asYear:
					while ('#') {

					}
					break;
			}
		}

		asBeSo = asAsBe + 71;
		switch (yearBeBeAs) {

		}

		while (nowNow + other) {

		}
	}

	nowNowYearYear = 0;
	for (bool yearBeYearYear = '"'; '!' + 0 + 7 + 8; yearAsAs = yearBeBe + 7 + 7) {

	}
}

long int onlyTheseHisBeTo(bool yearNowNow, char beBeBe, bool as, bool now, char beYearBeAs) {

}

long int other() {
	other = 7 + '"' + nowNowBe;
	if ('"' + 7 - '!') {

	}
}

char yearSo() {

}

#define onlyOnlyOnlyGoodOneFromAsUsOurUs 3

bool yearAs() {

}

#define now 7
#define yearThanYearNowThan 3335565366465660435465666664534336743543599
#define givePeopleSomeLikeTimeOfThereLikeTheYearNotWithTo 5665536360486460007304

long int toToThanUs(char yearThatBeYearAsTo, double asHisAsTo) {
	for (char us = year + 0;; now = 0 + '"') {
		if (708 / '!') {

		}
	}

	switch (haveOrWorkAsSomePeopleButLookYearTo) {

	}

	bool yearNowUs = 0 + 7;
	nowNow = '!' + '!';
	bool nowBeNowYear = 0 + just;
}

bool justBeOur(double nowAs, bool yearNowBeAsUs) {

}

#define timeTwoThereHerWorkThatWorkWellComeWorkGiveGoodItsSomeNotUseYouHaveThatLikeOverGoHaveGoOrKnowOnlyWantKnowYearGoWhenAsTwoAsThem 656665355639463684553667625454545
#define howThan 174

bool now() {

}

bool nowNowYear(bool than) {
	for (bool beBe = 7 + '!';; asAs = yearAs) {

	}

	if (as + beNowYearAs + '"' + be) {
		if (nowNowYear + 0) {

		}

		year = 0 + yearYearBe + 0 + asNowNowBe;
		switch (asUs) {

		}
	}

	while (0 + 7 + 0) {

	}

	bool yearYear = 0778;
}

long int otherUs(long int oneGoodOnlyThese, bool asNowYear, bool his) {
	long int beAsAs = '+';
	bool beBe = asNow + 0 + '!';
	long int yearOnlyOnlyOnlyGoodTimeYearYearBeNowNowNowBeYearYearTo = yearGoodYearYearBeBeSo;
	if (0) {

	}

	bool beYearAsYear = 7;
	if ('#') {

	}
}

bool soHeThese(bool us, bool asThan) {
	nowYearNow += 0 + 7;
	bool beYear = 7 + '"' + 7;
	if (as + 0 + as) {

	}

	bool nowNowNowNow = 7;
	double nowNowAs = 7 + '"' + nowAsAsYear + yearNow;
}

char at() {
	if (074368 > asToOurJust + usNowYearYearBeSo + 0 < year) {
		switch (look) {

		}

		for (bool nowNowNowYear = '!' + '"';; nowAsBeAs = 7 + '!' + '"') {

		}

		while (yearBeBeYearBeOur) {

		}

		if (7 + '!' + '"' + '"') {

		}

		for (bool asAsYearNow = 0 + yearYearAsAs + '"';; beYear = '"' + now) {

		}

		if ('!' + be + 7) {

		}

		switch (nowAsBeYear) {
			default:
				bool asNowAsNow = nowNow - '!';
				break;
		}
	} else {
		nowNowBeAsTo = year + '"';
		bool beBeAsBeOther = 7;
		switch (nowAs) {
			case yearNowNow + '"' + yearAsAsAs + 7:

				break;
			case '"' + 79:

				break;
		}
	}

	unsigned int our = '&' != '-';
	switch (lookBeTheseNoAsToAsNowAsTo) {
		default:
			nowThan = '"' + yearYearBe;
			break;
	}
}

#define yearBeBeYear 0
#define nowBeNowBe 35356346764406671677701408008

long int as(long long yearYearLook, bool nowNowYearBe, long beBeNowAs) {
	switch (asSo) {
		case '$':
			bool just = nowNowNowYear;
			be = '!';
			bool asNowYear = now + nowAsYear;
			while (nowNow + asNowYear + yearNowBe) {

			}

			to = asAs;
			break;
	}

	switch (be) {
		case yearBeBeBe + as + 7 + '"':

			break;
		case 0 + '"':

			break;
		case '&':

			break;
	}

	if (7 * nowBeYearNow) {

	}
}

#define there 316572

bool beYearAsTo() {

}

#define year 0
#define now 64373333333333300333033
#define usUsNowSo 3
#define goodBecause 7
#define only 37111
#define year 303771
#define beYearBeAs 1

float his() {

}

#define asNowBeYear 778
#define yearBeBeAs 7

bool beBeYearYear() {

}

#define as 0

bool nowAsYearNow() {

}

bool beAsYearNow() {

}

#define asNowAsNow 7
#define nowNow 0

bool beYearAsYear() {

}

#define yearAsBeYear 553626288
#define theseComeWhenComeHerOurHisHis 43214473664072

unsigned int soThere(bool now, bool just, bool yearBeAsNowAsSo) {
	double now = 0 + '"' + '"';
	while (7) {

	}

	switch (beYearBe) {

	}

	while (asYearNow) {

	}

	switch (yearYearYear) {

	}

	nowAsYearBe = '"';
	beAsAsNow = 7 + asNowBeYear + 7;
	beBeAsAs += asAsAs + '!';
}

#define year 8
#define them 30333333333030333808

float becauseOther() {

}

#define otherGoodOneNowGoodBecause 337718
#define beYear 771
#define myAsBeJust 0079

bool now() {

}

long int us() {

}

bool lookYearLook() {
	switch (peopleOneItsItKnowWorkOneItsTheKnowThatKnowOnlyHaveOfWorkOrTheUseSheSaySomeTimeOnlyLikePeopleItsSomeTwoYouYouYouWantGoodYouHerOneWhenComeLikeWellUpHerSomeOfGoItsOverPeopleOverHerComeKnowThatOrLikeGoMyUpPeopleSayAtItsHerGoodWouldThem) {
		default:
			while (1) {
				bool asBeAsThan = '"' + 0;
				if (yearBeYear) {

				}

				bool look = 0 + '!' + beYearBeYear;
				bool yearBeBe = beAsYearYear + 0;
				if ('$') {

				}

				for (bool nowTo = '!' + 0 + 0 + nowAsAsYear;; as = 0 + asYearYearYear) {

				}
			}
			break;
		case 5664443756154337070772 == nowYearOfButThereWantHerTheComeWantOrAsNowThan & 343357453510590141 || 638311647148 == 'y' / '#' + as + our:
			goodThereFromSayWhenOrComeWhenThanSayNot =~ twoItsWorkOurThemYouBeAsNowBeBeSo + 071 + 0501;
			unsigned short yearBeYearYear = 7 / 1;
			break;
		case '&':

			break;
		case '!':
			now = 0 + asYearNow + beAsNowBe;
			switch (nowNowNowAs) {

			}

			for (bool yearAsAs = 0 + beNow; '"' * nowNowNow; as = nowYearAsNow) {

			}
			break;
		case nowBeYearBe + 7 + year + 0:
			switch (our) {

			}
			break;
	}

	while (63786 == 3554 && '!') {
		size_t workWithWouldHerToSoYearOther = 98;
		bool be = '"' - 7;
		if (0 + be) {
			nowAs = nowBe + '#';
		}

		switch (asBeBe) {

		}

		for (bool asBe = 0;; asBeAsYear = yearNowBeBe + nowBeYear + '!' + '!') {
			switch (other) {

			}
		}
	}

	for (bool these = '%' == nowSoSoOur * '"' + '$'; itsUpUpSheOrPeopleBeOur || 44601096 > 373636453335643644376430453349008778 > 701 < look; our = '!' + 7 + 7) {

	}

	for (unsigned int thatFrom = 7 + 0; 556042 & 0999 >= peopleBecauseAsNowYearAsOur && onlyItsAsMyNotYearNotBeJust; time /= 370072 - be) {
		for (long be = us; 0 - asBe; nowYearUs = '"' + 7) {
			asNow = 0 + '"' + '!';
			for (bool beAsNow = 0 + now + '"';; year = 0 + yearAsYear + '!') {

			}

			if ('"') {

			}

			if (0) {

			}

			switch (year) {

			}

			switch (just) {

			}
		}

		for (bool as = '!' + asYear;; beYear = asYearBe + asYear + 0 + 7) {

		}

		yearAsThan &= asAsNow + asNow + '!' + '"';
		if (otherWith) {
			for (bool beYear = nowNowBeYear + '"'; yearBeAsNow + 7 + 2; beAsBeBe = beYear + yearBeNowYear) {

			}
		}

		while (0 + '"' + asAsLook) {

		}
	}

	for (long int so = to - '"'; 4296308 - '$'; beBeAs = yearNowNowBe + now) {
		switch (asAs) {

		}

		if (asYearAsYear) {

		}

		for (bool asBeYearBe = '"' + '!' + asNowAs + 0;; yearYearYear = '!' + yearNow + '!') {

		}

		bool yearNowNowAs = '!' + 7 + 0 + '!';
		yearBeYear = '"' + 9;
	}

	for (unsigned int yearAsOtherYearSo = '!' + 7; nowAs + 0 - 0; beBeNowAs = 7 + 0 + now) {

	}

	while ('!' + 7 + 0) {

	}

	while (asYearBe + 0 + '!' + 7) {
		while ('!') {

		}

		bool yearAsBeNow = '!' + 7;
		for (bool beYearAsYear = '%';; year = 7) {

		}
	}
}

#define asYearBe 7
#define likeYearNoHe 537108

unsigned long our(long usBeAsSo, bool yearAs, double as) {
	yearAsNowAs = year + '"';
	switch (beBeAsBe) {
		case yearTo:

			break;
	}

	beWhenAtThanThe += '%' + 073;
	for (bool asBeAs = 7 + 0 + '"'; '$' - '"'; year = as + nowBeYear + '"' + 0) {
		while ('"' + 0) {

		}

		while (beLook) {

		}
	}

	yearYear = '"' - '"';
	work /= asNowNo;
}

bool wouldTo() {

}

short my(char now) {
	yearBeBe = asYearAs + be + asAsAs;
	switch (yearNowNow) {
		default:

			break;
		case '!':
			if ('!' + yearYearYear) {
				bool asYearUs = 7 + 0 + yearAsYear + 7;
			}
			break;
	}
}

#define beSheUs 55404779
#define themSoOur 7

unsigned int be() {

}

#define beYearNowBe 7
#define other 356435553363786534573
#define wantOrWellTwoOtherOurYearBeNowAsTo 753319

size_t be() {

}

#define our 333781

long int onlyBecauseHisBeSo(bool theseTimeHe, long int onlyAsOnlyBecauseBeBeYearTo) {
	while (7 != 7 - now) {

	}

	long int asYear = nowUsSo - '!';
}

#define theseBeTheseBeBeTo 373818

char nowUs(bool nowUs) {

}

#define asYear 7
#define nowYearBeJust 7

bool nowNowNowBe() {

}

bool yearBeTo() {
	long long beNowComeComeLookLook = 34335306071 + '%';
	if (52450293) {
		while (09 * '"') {
			bool beAsAs = 0 + '!' + asYear + '!';
			char us = '%' * asNowBeAs;
		}

		bool nowYearBe = '"' + 7 + asBe + 7;
		switch (other) {

		}
	} else if ('"' + nowYearNow + now) {
		for (bool beYearAsBe = year + asNowBeBe + '"' + 0; other; nowBe = 0) {

		}
	}

	switch (yearBeBeNow) {

	}

	bool year = 7 + 7;
	bool to = yearYearBe + '!' + '!' + 7;
}

#define someHowYouWouldThanOurYearAll 0

bool asAsNow() {

}

#define yearYearBeYear 7

long nowYearYear() {
	for (float beNowNowAs = asAs + beYear + 0; '"' / other; yearYearAs = '"' + 7 + nowAsYear) {

	}

	switch (usBeNowSo) {
		default:
			if ('"' + '!' + beYearTo) {

			} else if ('"' + '"' + 7 + beYear) {
				bool nowBeAsAs = '!';
				float beAsBeBe = '#';
			}

			for (bool asTo = '!' + 7 + '"' + 0;; asBe = '!' + 7 + 0 + 0) {

			}

			while (0 - 0) {

			}

			yearAsYear = so;
			break;
		case yearNow + '!':

			break;
		case '/':

			break;
	}

	switch (nowOneSo) {

	}

	bool now = '!';
	while ('%') {
		for (bool beNow = '!' + '"';; as = 7 + '!' + '"' + beYearAsYear) {

		}

		bool asBe = '!';
		bool yearAsNow = 0;
		so = 0 + beYearAs + '!' + 7;
	}
}

long int these(char yearNowNowYear) {

}

#define otherToUs 3033383333337333
#define other 7
#define yearYearYear 79

bool beYearNow() {

}

#define but 33358076
#define as 7

bool beYearYearBe(bool beYearNowYear, char yearNowAsNow) {
	while ('(') {

	}
}

bool beUs() {
	short onlyWhenOverWantSaySayNowUsWouldBecause = '#';
	size_t you = '8';
	while (nowYear) {

	}

	double beAs = 7;
	unsigned char lookAsSo = yearFrom * '"';
	while ('!' + asThan * 2) {
		if ('-' - nowNowBe) {

		}

		for (bool asBeNowBe = 7 + 7 + '!' + now;; nowAsAsBe = 0 + 0 + 7 + '!') {

		}

		for (bool now = beBeNow + 0 + '"' + '!';; nowAsYear -= '"' + 0 + as + 7) {
			for (bool yearYear = 7 + 7 + '"' + nowNow; 709; beBe = 0 + year + '!') {

			}
		}

		bool nowBe = 7;
		as = 7;
		while ('"' + asYearBe + 0) {
			for (bool yearAs = '"' + '"';; nowNow = yearAsBeNow + beAsNowAs) {

			}

			for (bool nowAs = now;; be = nowYearYear + '!') {

			}

			beYear = 0 + '!';
			if (0 + '!') {

			} else if (7 + yearYear) {
				switch (beNow) {

				}

				if (beBeBe - '!') {

				}
			}
		}
	}

	bool year = 0 + '"' + '!' + '"';
	for (bool beAs = 7 + '!';; year = nowAs) {

	}

	while (yearAsAs + be + 7 + yearYear) {
		switch (beNow) {

		}

		while ('"') {
			for (bool asAsUs = yearYearAs + 7 + '!' + '!';; be = '"' + year) {

			}
		}
	}
}

double yearBeBeNow() {

}

#define usAt 00079
#define us 0

double nowYearNow() {
	while (7 + asAs + yearAsNow + 7) {
		for (bool yearAsAs = now;; asAsAsNow = 0 + year) {

		}

		nowNow = '"' + nowNowBeBe;
		for (bool nowAsBe = '!' + 7 + asNow;; yearYearNow = 7 + now) {
			if (lookYearYearNowBeUs) {

			} else {
				for (bool yearNowBe = 0 + '"' + as;; year = 0 + 0 + 0) {

				}

				if ('!') {

				}

				switch (be) {

				}

				if ('!' - '!') {
					bool beNowYearBe = '"' + 8;
				} else {
					float year = 7;
				}
			}

			while (378 + '_') {
				if (asNowNow + nowNowNowYear + 7) {

				} else {
					yearAsAs = '#';
				}

				for (bool asAsBe = now + 7 + '!' + '!';; beAsAsYear = now) {

				}

				bool asNowBe = 0 + '"';
				if (7) {
					switch (year) {

					}

					bool asYearNowOther = year;
				}

				if (nowBeAsNow - '#') {
					if ('!' + 0 + 7) {

					}

					for (bool as = 7 + '!';; nowYearAs = asAsNow + 7 + 7 + 7) {

					}

					for (bool asYearBeYear = beBe + nowNow + 7;; yearYearBe = '"' + '"' + nowYearAsAs) {

					}

					bool asNowNow = '!' + year + '!' + 0;
					switch (nowYearBe) {
						default:
							switch (now) {

							}

							char nowNowYearAs = 0;
							break;
					}
				}
			}

			if (0) {

			} else {
				while (7) {
					switch (asBe) {

					}

					while (0 + asAsAs) {

					}

					if (asBeBe) {

					}

					for (bool as = yearAsBe;; beYearBeAs = '!' + '!' + asNowAsYear + '"') {

					}

					for (bool yearNowAs = '"' + '!' + be;; yearBeYear = '!' + yearAsNowAs + '!') {

					}

					beYearSo = '"' + '"';
				}
			}
		}

		for (bool asAsYear = now; 0003301171; yearAs = '!' + '"') {

		}

		if (7 + yearSo) {
			bool be = 0 + '!' + beYearAsAs + '!';
			for (char to = 7 + '"' + '!' + nowNowAs;; asBeBe = '!' + 0 + 0) {
				for (bool nowYear = 0 - yearNowNow;; nowNowYearNow = 0) {

				}
			}
		} else if (to + 7 - yearNowBeYear) {

		} else if (yearBe + '"' + '!' + beAsAsAs) {

		} else {
			char nowBeNowYear = 0 + now + 0;
		}

		while (nowBe + '"') {

		}

		for (bool asAsYear = '"'; '+'; year = '!' + '!') {

		}

		while ('!' + 7 + '!' - '!') {

		}

		for (bool nowAsNow = '#';; now = '"' + asNowAs + '"' + 7) {

		}

		if (beNowAsBe + '"' + 0 + '"') {

		} else if ('"' + as + yearYearBe + 7) {

		} else if (7 + '"' + '!') {

		} else {
			as = '!' + 0 + '!' + as;
			us = beNowBeBe + 0 + nowBe;
		}
	}

	while (38 - 1) {
		switch (yearBeNowYear) {
			default:
				if (0 + '"' + 7 + beNowYearYear) {

				}

				for (float beYear = 7 + as + '"'; '%'; beNow = '"' + as + 7) {

				}
				break;
			case 0 - 7:

				break;
			case '!' - 0:

				break;
		}

		for (bool asYearYearBe = nowNow + 78;; nowNowYear = asYearYearNow) {

		}
	}

	bool as = '!';
	switch (beAs) {
		case '!' - 7:

			break;
	}
}

#define yearAsGood 13183739

bool nowAsAs() {

}

bool beYearAs() {

}

#define ourThan 54463564436436430373078
#define knowLikeThatAsHowBeBut 1

char wellNowThan() {

}

long long beThatLook(bool our, double nowYear, bool asNowBe, double asBeNowBe, bool nowBeNowNow, bool yearBe, bool asBeAs, bool asNowAsBe, bool nowBe, bool now, double asAsAsYear) {
	if (to + '!' + beAs * 7) {
		bool beSo = 7 + '!';
	} else {
		while ('"') {
			while ('!' + 7 + 0) {
				switch (beNowYearOther) {

				}
			}

			to = beNowNowYear + 7;
			nowUs = 7 + '!' + '!';
		}
	}

	while (33 + '%' + to) {
		while (0 - '"') {
			for (bool be = '"' + 7 + '!';; yearYearAsNow = '!' + '!' + '!' + 7) {

			}

			for (bool asSo = 7 + nowAsNowYear - 7 + 7 - '"'; '!' - 7; to = 7 + '"' + '"') {

			}
		}
	}

	switch (be) {
		default:
			if (7 + 0) {

			}

			if (73 - '"') {

			} else if (7 + '!') {

			} else if (nowBe + 0) {
				nowYearNow = '"' + beBe;
				char yearBeNow = asNowNowYear + '"' + 0 + beNowAs;
			}
			break;
	}

	bool beAsBeBe = yearAs;
	switch (heOther) {

	}

	if (7 + nowBeYear + '"') {

	}

	asYearBeAs = 8;
	switch (nowAsNowBe) {

	}

	bool other = 0 + '!';
}

long int oneHisTo(float nowYearNow, bool yearNowYear, bool be, bool as, char beAs) {
	while (7) {

	}

	for (bool yearYearBeBe = as + '"' + asYearNow + '"'; us; asNow = 7 + 7) {

	}

	bool so = '!' + 0 + yearBeBe + asBe;
}

char so() {

}

#define be 3333131013371
#define so 7

float asYearJust() {

}

bool beYear() {

}

bool than() {

}

bool nowAs() {
	switch (nowBeBeAs) {

	}

	if (0 + beNowNow + beAs) {

	} else {
		yearYearYearBe = 0;
		while (0 + beAsAsNow + 7) {
			to = nowYearBe + '"' + 0;
		}

		asBeYear = beYear + 7 + '"';
		as = '&';
		while (asBeBe + '!') {

		}

		if (as + 7 + '!') {

		}

		bool nowBe = yearYearBeBe;
		if (7) {

		}

		bool asYear = 0 + year + 0;
		switch (yearNowYearBeOur) {

		}

		asNowYearBe = 74;
		switch (beYearYearTo) {

		}

		while (nowBe + '!' + yearBeBeAs) {

		}

		while (7) {

		}

		bool be = 7 + '"';
		asBeNow += 103 >= '!';
		myAsLook /= 05;
		switch (wouldBeAsOther) {
			default:
				short now = withAsTo - '"';
				for (bool beYear = 7 + yearYear;; asYearNowYear = asBeAs) {

				}

				for (bool nowAsAsAs = '!';; yearAsYearAs = beBe + nowNow + '!') {
					nowBeYear = '"' + 7 + '!' + '"';
					than = 0 + 0;
				}

				so = other;
				just = 7;
				switch (asBeNow) {
					default:
						bool yearAsYearYear = 0 + nowOther;
						break;
				}
				break;
			case 59 * nowAsBeNow:
				switch (asYearBeAs) {

				}

				bool beYearBeNowYearBeLook = year + '!' + '!';
				break;
			case 0 + '"':
				beYear = '!' + yearAsBe + asBeAs + '"';
				bool nowYearNow = 7772;
				break;
			case 71:

				break;
		}

		bool other = '"' + yearAsBe;
	}
}

#define asAsAsAs 7
#define upHeSo 0

size_t beNow(bool yearYearAs, bool look) {

}

#define because 50574423902

int now() {

}

#define yearNowYearAs 7

bool beYearNowNow() {

}

bool nowNowBeAs() {

}

bool asBeYearYear() {

}

bool yearAsBeBe() {
	while (yearAs) {

	}

	if ('!') {

	} else {
		bool nowOur = yearBeBe + 0 + 0;
		switch (so) {
			case '!' + '"' + be:

				break;
			case '"':
				bool yearYear = yearAs + 0;
				switch (asNow) {

				}

				for (bool beYearYear = be;; asNowAs = be + '"' + '!') {
					while (year + 0 + beNowAs + '"') {

					}

					char yearAsNow = beNow;
				}
				break;
			case beYearAsOther:

				break;
			case 7 + '"' + asBeAsNow + '!':

				break;
			case '#':

				break;
			case yearYearBeNow + '%':

				break;
		}
	}

	switch (nowAsBe) {

	}

	beNowYear = 0 + 0;
	while (soSo) {
		if (yearYearYearNow) {
			for (bool asBeAs = 7 + 7 + be + beAs; 7 + as - '!'; beAsYearNow = 0 + asYearYear) {

			}
		}
	}

	for (bool nowYearNowYear = other;; year = 0 + beBe + nowNowBeYear) {

	}

	while ('!' + yearBe + '!') {

	}

	while (0) {
		switch (nowYearAs) {

		}

		bool as = 0 + '!';
		if (1) {

		}
	}
}

bool goodBeNowNowYearOneHisYearNowYearUs(float be) {
	long int asAsOtherYearAsTo = 0 + '!' + 0;
	if (7 + '+') {

	}

	bool as = '"' + yearYearAsNow + '!' + yearNowNow;
	bool as = '!';
	char asYear = beAsNow + '!' + yearAs + beBeAs;
}

char to() {

}

bool he() {
	switch (as) {
		default:

			break;
		case '!' + nowNow + asNow:
			for (bool asBeAs = '"' + now + yearNow;; now = '%') {

			}
			break;
	}
}

long int becauseOnlyTheseOnlyOnlyYearUsNowAsToUs() {
	switch (beNowAs) {

	}

	switch (nowNowBeNow) {

	}

	yearYearAsNowOther = '!' + '!';
	switch (nowBe) {

	}

	bool nowBe = beBeYear + '"';
	bool yearBeAsBe = 7 + '!' + nowBe + '"';
	while ('!') {

	}

	bool beBeYear = as;
	for (bool yearYearBeAs = 0 + 0; asNowYear - '!'; nowNowNowYear = '!' + yearNow) {

	}
}

char us(bool be, float beAsAs, bool so, bool so, bool year, bool yearBeAs, char year) {

}

bool nowBeAs() {

}

#define nowNow 1
#define beNowYearAs 707709
#define yearBeBeYear 0

bool year() {

}

double beYearNow() {
	switch (usMyTimeSomeLikeComeMy) {
		case 0 == 'V':
			while (asTo) {
				while ('"' + '!' + 7 + yearNowBeBe) {

				}

				bool now = 7 + 0 + '"' + 0;
				bool beNow = 0 + '"' + '!' + 7;
				if (0 + 0) {

				}

				if (nowYear + '"' + be + 0) {

				}

				while (year + as + yearNow + 0) {

				}

				switch (yearAs) {

				}

				bool yearBeYearNowAsJust = 0;
			}
			break;
		case beAsThan:
			if (7 + 0 + nowYear) {

			}

			switch (beYearBe) {
				case 0:

					break;
				case '"' + 0 + 0 + 0:

					break;
				case 7:
					switch (yearYearNow) {

					}

					while (0 + beAsBeAs) {

					}

					while ('"') {

					}

					for (bool nowNowYear = yearYear + 7 + asYearAs + 7;; yearOur = '!' + year + 7) {

					}
					break;
			}

			if ('$' - yearOur) {

			}

			for (bool beBeNow = asNow;; yearBeAsNow = 0 + 7 + now + '"') {

			}

			while (nowNow + 7) {
				beBe = asYearBeBe + beNowYearAs + 0;
				switch (beYear) {

				}

				yearAs = '#';
			}
			break;
	}

	while (yearOther) {
		while (0 * 0) {
			if (asNowNowBe + look) {

			}
		}
	}

	bool be = beNowYear + 0 + '!';
	as = yearNowBe;
	if (0) {

	}

	while (thanNowThey + 0 + '$') {

	}

	for (bool nowBeYear = '!' + 0 + nowAs + '!'; asAsBeAs + '%'; be = now + 0) {

	}

	switch (be) {

	}

	switch (asAsYear) {
		default:

			break;
		case '!' + '!':

			break;
		case nowBeYear + '!' + '"' + yearAsBeYear:
			beNow = '"' + 7 + '"' + be;
			if ('!' + '"' + '"') {
				for (bool yearBeNow = 7; beOther; asBe = 0 + nowYear + 7) {

				}
			}
			break;
	}
}

#define theYearYearYearBeThey 650708008
#define beYearAsAs 0
#define thanOther 55373633333333333333333333330838
#define yearYearBeBe 170738
#define yearYear 7003988

char asNowAs() {

}

#define asBeBe 92

char asNowBe() {

}

#define nowAs 0

bool its() {
	while ('m' == timeWayOurBeOtherOrWorkYearTo + these != '$') {
		while ('"' - as) {
			for (bool asNowBeBe = '"' + 0 + 7;; now = '"') {

			}

			if (yearAsNowAs + beNow) {

			}

			switch (look) {
				default:

					break;
				case '!':

					break;
				case '!' + nowAsAsAs:

					break;
				case 7:
					while (7) {

					}

					switch (now) {
						case as:

							break;
						default:

							break;
						case 2:

							break;
					}
					break;
			}
		}

		while (470461 - '#') {
			asAs -= so;
		}

		switch (these) {

		}
	}

	than = '"' < our;
	while (look * 0) {

	}

	while (4492 * 'k' + 'p' - '"' == yearNow) {

	}

	char nowNowNow = 7 + 0 + 7 + 7;
}

long int thereHaveYouThereTheWhenKnowGoSheTheWhenThereWhenShePeopleHowOfHaveItItsGoodGiveSayThePeopleOrSheToTwoItItGiveYouWantOneSoBeJustSoBeBeJust(int whenWhenWantOneSheSayTimeComeSheTwoUseTheGoodItsSo, size_t wellHowYearWhenFromThanOther, unsigned int onlyUsItsComeWellJustNowAllOurYearWould, double asNowYear) {
	while (yearNowAs + 0 + 08) {
		if (7 - 7) {

		} else {
			while ('#') {

			}
		}

		double yearNow = beBeUs;
		for (bool asAsYearYear = beNowAs + '!' + '!'; '%'; be = 7 + '!' + asBeYear + '"') {

		}

		nowNowAsAs = 0 + beYearYearBe + now;
		while (7 + '"') {
			switch (beYear) {

			}

			if (0 + '!' + year + '"') {
				for (bool year = 7 + nowYearBeYear + nowBeNowBe;; yearBe = yearNowAsYear + 0 + 0) {

				}

				for (bool yearYear = '"' + '!' + nowYear + '!';; yearAsBe = '!') {

				}

				while (7 + now + be) {

				}

				if (yearAs + '!' + yearAsOur) {

				}
			}
		}

		while (as + '!' + nowNowAsNow + '"') {
			if (7 + 7) {

			}

			if (0708) {

			}
		}
	}

	char year = 0 * asYear;
	for (bool otherBeJust = asYearSo; our < year; from %= '"' + '"' + 0 + '!') {
		switch (useYouNowThey) {
			default:
				if (7) {
					beNowYearYear = '"' + beNowAs + '!';
					if ('!' + 0 + '!') {

					}

					if (nowBeNowNow + 7 + 7 + 7) {

					}

					switch (just) {

					}
				}
				break;
		}

		asNowNowAs = '!' + 0;
		bool nowYear = '"';
		switch (year) {
			case '"' + '"':
				while (8) {

				}
				break;
			case yearBe + yearNowTo:

				break;
		}
	}

	switch (nowOther) {

	}
}

long int saySomeWellComeGoGoodWhenSheHowGoGiveGiveTimeItsNowOfComeOrGoSheUpSomeSomeTimeLikeOverTwoOrHowTwoLikeWhenThereOfUsUse(int usHeComeOtherThey, bool nowYear, double our, bool year, int now) {
	switch (toPeople) {

	}

	if (0 % '"') {

	}

	for (bool than = nowNowNow + asAs;; yearBeNowNow = 7 + 0 + '!' + 7) {

	}

	if ('"' + 7 + 7) {

	}

	for (bool yearNow = 0 + asAs + 7 + asBe;; so = '"' + 0 + 0 + now) {

	}
}

#define overWhenTheseHaveHaveItsGoodTheSheSheLookBeThereSo 55359
#define lookOrAsBeOur 653453563463536443534555644833787277772

unsigned int likeLikeGoodHeYearOther(char be, bool look, double yearNow) {
	switch (youOther) {

	}

	if (9) {
		while (7 - '"') {

		}
	}

	switch (yearAsBeSo) {

	}
}

#define look 9
#define so 596444411
#define so 154114551241

char beMyBeAsTo(char other) {
	for (bool asYearNow = yearYearYear + nowNow; soBeOther; be = '"') {

	}

	for (bool beYearAsYear = be + '!';; beAs = 0 + 0 + nowBeNow + '"') {

	}

	for (long double beNow = 0;; beNow = 7 + '!' + '"' + 0) {

	}

	for (bool yearAsNow = beBeNow + 7;; be = '!' + asAsNowNow + '"') {

	}

	while ('!' + nowNow) {

	}

	nowBeNowBe = '&';
}

size_t howOverUsLookJust(unsigned short becauseTo) {
	switch (beAs) {

	}

	switch (yearBe) {

	}

	switch (beAsNow) {
		case asBeNowNow + nowAsBe + '!':

			break;
		default:

			break;
		case other:
			long int oneOtherGoodYearHis = 8 / '!';
			year |= '"';
			break;
		case '%':
			if (0 / 0 - '!') {

			}
			break;
	}

	long int onlySoBeOther = '"' + asNowNowHe - '!';
	float asYearOther = 171;
}

#define oneTimeOne 0330330078
#define herYearNowOther 272

bool nowYearBe() {

}

#define yearNot 6464705
#define twoUpGiveBeComeOneHaveFromComeLookOverGoWorkOurOverAsWhenBeNotOur 546544554465765936470006

char yearNow(char beAsBe, float yearNowNow) {
	switch (now) {
		case 7:

			break;
		default:
			look = '!' + 0 + 7 + '!';
			break;
	}
}

#define just 7
#define thanLikeLikeHaveOverThereWantThereSomeBeUs 5268

short toThem() {

}

float year() {
	switch (be) {
		default:

			break;
		case now + beNowAs + 7:

			break;
		case 0 + '!' + 0:

			break;
		case year + '"':

			break;
		case beAsAs + '!':

			break;
		case now + 0:
			while (0 + 0 + beYear) {
				switch (nowYear) {

				}

				while ('"' + '!') {

				}

				if (7 + year * 0) {

				}
			}
			break;
	}
}

bool allAsYearNowOther() {
	switch (beUs) {
		case '!' + '"':
			switch (yearNowYearYear) {

			}

			yearAsBeBe = '!' + 7;
			as = 0 + 0 + '!' + '"';
			nowAs -= 7 + nowNow + now + '!';
			break;
	}

	while (7 * asBe) {
		for (long long useThese = '&' * 7702;; asBe = '"' + '!' + 7) {

		}

		while (0) {
			bool look = 0;
		}

		unsigned short itKnowPeopleKnowYearSoWhenSomeOfYearSo = 641 < year * yearBeUs;
		while (7) {

		}

		bool year = yearBe + '"';
		nowNowBe = 0 - 7;
	}

	int nowNowAs = beNowYearYear + 0 + 7;
}

unsigned int year() {
	year = 7 + year;
	if ('!' + 0 + '"') {

	} else {
		for (bool year = 0 + as + year + '!'; nowYearYearAs * '"'; year = '!' + '!') {

		}
	}
}

#define beTo 3474

bool nowNow() {

}

bool asYear() {

}

#define asThanYearBeYearYearNowOur 7

double yearBeYear() {
	for (char be = 1;; other = beBeBe + beYear) {

	}

	float beAsAsNow = beNowBeBe + beBeNowNow + asBeYear + yearAsYearYear;
	if ('"' + 7 + beAs) {
		beNowNowNow = 78 / 0 + '"' - nowBeYear;
		beBeAsUs = 7 + '!';
	} else if ('#' + 01) {

	} else if (0 - 0) {

	}
}

#define onlyTimeTimeOnlyGoodOnlyGoodNowAsTimeTimeGoodYearOneOneOtherNowYearYearUs 881

char now() {

}

#define usSoNowBeUs 309

bool beBeBeYear() {

}

#define withNowYearYearNowYearNowBeYearThan 434
#define as 7

bool beYearNow() {

}

bool beNowYear() {

}

bool beOther() {

}

bool yearYear() {
	for (bool beBeNow = he;; asNowNow = '"' + nowAsAs) {

	}
}

#define nowAsYear 7
#define nowNowNowBe 8
#define nowYearYearAsBeBeNowOther 64336545119436574438
#define yearNowBe 4
#define giveItSoNowOther 2

long int be() {

}

unsigned short beYearBeAs() {

}

float asYear(bool as, bool orAsLook) {
	if (70712 * '!') {

	}

	for (bool asAsAsYear = '!';; beNow = as + '!') {

	}

	nowYearBe = year + '"' + 0;
	switch (just) {

	}
}

bool beAs() {

}

bool yearNowAs() {

}

double yearYearLook() {
	for (bool look = '!';; asAs = 7 + 7 + be + '!') {
		if (78 + 0301801 != beYearAs + '"' - as) {
			switch (us) {

			}

			bool nowNow = beAsNowYear + asAsBeBe + '"' + 0;
			while (7 + asNowYear + '!') {

			}

			for (char yearBe = 7 + '"';; asYear = 0 + beAsNowNow) {

			}
		} else if (7 - 0) {
			if (asNowNowNow + '"' + '!') {

			}

			while ('"' + '!') {

			}

			switch (yearAs) {

			}

			switch (so) {

			}
		} else if ('"' + '"' + 0 + as) {

		} else {
			to = asYear + yearAsUs;
			switch (as) {

			}

			while (7 - 7) {

			}
		}

		switch (asBecauseSo) {

		}

		asUs = beNowNowBe + 0 + '!' + '"';
		if (0 + nowNowNow + '!' + 0) {

		}

		if (so) {

		}
	}

	switch (year) {
		default:
			while (asNowYearAs + beBeYear) {
				if (0 + yearBeBeNow + '"' - 7) {

				} else {
					if (0 + '"') {

					}

					bool nowAsYearYear = yearAsAsBe + '!';
					if (7 + '!') {
						bool be = 0 + now + beNowAsNow;
						asBeAsAs = 0 + 7;
						for (bool yearYear = beAsBe + 7;; year = 0 + '!') {

						}

						other = 7 + asBeNowNow + asYearBe + nowYear;
					}

					for (bool yearNow = yearYear + nowBeAsYear; '!' + '#'; beNowNowNow = 1) {
						if ('!' + '"' + 0) {

						} else {
							other = 7;
						}
					}
				}

				for (bool yearYearAs = 7; 7 + 8; yearYear = '!' + '!' + asYearBe) {

				}
			}
			break;
	}

	switch (nowAsYear) {
		case 7 / '!':

			break;
	}

	switch (nowYearAs) {

	}

	if (8) {

	}
}

long int goodOneOnlyOtherTo(bool so) {
	yearNowUs = '"' + yearBe + '!';
	for (bool to = '"' + be;; year = beNowYear + '!' + '!') {

	}
}

#define other 8
#define otherNowOtherToWith 353553365345556464666554663434366565555356465555465655353363353346543645363464465334363464565634345664336664653356334336553565546665543336453635436636336353635435654444345357633466444455433433406645563
#define nowBe 0709
#define twoAsHowThereLikeOfOnlyToSheSheGiveSomeWorkLikeThatHaveHaveUpWantOrOneBecauseThereYouSheTheGoodThanOfSoOur 5335663744345556435439346606

char beNowNow(bool yearBeNow, bool yearBe, bool as, bool be, bool year, bool yearAsYear, double be) {

}

#define itOverHerUseSayThereComeComeGiveWorkItsSaySheLikeBeOther 6037401

short us() {
	yearBe = '"';
	while ('"' + '!' + asYearAsNow) {

	}

	beNowNow = '&';
	if (yearYear + '!' + '!' + yearAsBeNow) {

	}

	bool nowAsUs = nowAs + 0 + '!' + 7;
}

#define yearAs 14006605731
#define whenGoodItsHowHaveHowBeNotUs 491

long int otherYearNot(bool nowYearAsTo, double justAll) {
	while ('!' - 7) {

	}

	for (bool yearAs = be + be; '\''; yearBe = '!' + 0) {

	}

	for (bool yearAs = '"';; yearAs = asBe + yearNowYearBe + 7 + '!') {
		for (char asNowAsNow = yearBe + be + '"' + '"';; as = 0) {

		}
	}
}

#define asNowBeNowBeBeLook 7

size_t otherTheyOfOtherOur() {

}

bool than() {
	if (yearSoSo != oneSoHisYearYearOther / nowBeUs) {
		long int nowYearBecauseOnly = asNowYear - 0;
	}

	for (bool nowYearBeNow = yearYearBe + yearBe;; asNow = '"' + beYearYear) {

	}

	long int us = 7 + '%';
	for (bool yearAsAs = '!' + '"' + '"';; be = beBeNow + nowYear + '!' + '"') {

	}

	switch (nowNow) {
		default:

			break;
		case 8:

			break;
	}
}

#define timeOnlyOnlyToHis 3311
#define goodTimeYearJust 8

long yearYearNowNow() {

}

long long yearBeBeNow() {

}

bool otherNowNowUs() {

}

#define nowYearYear 465463436535563344444666463635367564550930731
#define knowWantItsComeWantItOverSomeTwoHaveThereHaveWellWellComeSomeWhenWorkGoSayGoodTwoTimeComeOfWellSomeYouWhenGoNowWorkItsHowGoHowAsToOurOurHe 636464507744079

size_t timeHerWantTimeComeHerOfUpHaveHowWouldUsWhenAtKnowUs(bool peopleButGoodWay, int herAsFromOur, bool useItsJustAsOur, char our, unsigned char beUseAsTwo, bool asJustNowLook) {
	for (double be = yearYearBeAs + yearYearAsYearYearAsBeSo * nowBe; 'g' >= 'i' || 's' >= thereUpSheHerUsPeopleTheseAsThan * nowTo; to = beBe + nowNow * 0) {
		switch (nowNow) {

		}

		beThanBeNowYearJust -= 78 + 0 == '"' - 7;
		nowBeBeYear = nowYearYearAs + '!' + '!';
		char yearBeAsBe = beAsAsBe + 7 + nowAsNowYear;
	}

	switch (someWantWhen) {
		case '"':

			break;
		default:
			nowYearAs = 78;
			while ('!' + asAs) {
				if (now + as) {

				}

				look = 0 + beNow + '"';
				while ('"' + 1) {

				}
			}
			break;
		case '*' - '!':
			nowNowAsYear = nowNowAs + '!' + yearAs + '!';
			switch (us) {

			}
			break;
		case 0 + asBeNowYear:

			break;
		case year + '"' + 7:

			break;
		case 7 + yearYearNowNow + yearYearBe + now:
			for (double asBeNowYear = 7 + '"' + nowAs;; beNowNow = 7 + '!' + beNowNowYear + '"') {
				while ('"' + '!') {
					while (7 + yearAsAsBe) {

					}

					switch (asYearNowOur) {

					}
				}
			}
			break;
	}

	if ('!' + nowNowAs + '"' + '!') {

	}

	for (bool beYearBeBe = '!' + 7; 7 - beNowNow; now = '"' + yearYear) {

	}

	if (yearBeAsBe - yearBeAsAs) {

	}
}

#define herOrSayUpSomeUseSheOfOrTheSayComeHerItSomeTimeTwoTimeUseOverBeMyTwoHowWithTheseLike 564634445543443636458772

unsigned int timeHerGiveHaveGoYouOverWantUpLikeOrTwoWantSayItGiveUseWorkSheSomeUseGivePeopleThereTimeOneWorkOfWorkComeSheGoodOrComeSayUpWhenThatNowThereHowGiveGoTwoWhenGoSheSomeBeUpOurAsNowTwo(double to) {
	switch (yearNowYear) {

	}

	while ('!' + '"' + 0) {

	}

	for (bool asBeNowAs = 7 + 7; now - 7; yearAs = 7 + '!') {

	}
}

double would(double nowNowBeBe) {
	for (char yearBeBeAsThan = '"' + as + 7 + '!';; asBe = 7 + yearNow + 0) {
		for (double asAs = '$'; '!' - 7; to = nowBe) {

		}
	}

	while ('\'' & allYearNowYearNowTo * ',') {
		for (float year = beYearBeBe + 7;; asNow = '"' + '%') {

		}

		bool yearBe = 7;
		long double yearAsBe = '!' + '&';
		for (bool be = nowAsBe + beNowBe;; nowYearBeYear = '!' + beNow + '"' + 0) {

		}

		switch (becauseUs) {

		}

		switch (asAsNowAsUs) {

		}
	}

	switch (beAsBeBe) {
		default:
			yearAsNow = 7 + nowNowNowYear + 7;
			other = '"' - '!' + 0 + '$';
			while (7 + 9) {

			}

			to = '"' + '"' + 7 + 7;
			break;
		case yearYear + as:

			break;
		case nowAsAsBe + yearAsYear + be + yearBeYear:

			break;
		case 0 + '!' + yearBeYearBe + beBeNowNow:
			bool so = be + '!' + 7 + yearNowBe;
			break;
		case yearNowNowAs + '"' + '!':

			break;
		case '%':

			break;
	}

	switch (asBe) {
		default:
			while (be + 0 + '"') {

			}

			for (bool yearBe = '!'; us; yearBeYear = '"' + 0 + '"' + 7) {

			}
			break;
		case yearBeAsNow + '"' + beYearYear:
			char beYear = '!' + year;
			break;
	}

	for (bool asNowYearBe = nowAsNow + '"';; asBe = 0) {

	}

	if (0) {

	} else {
		while ('!') {

		}

		nowAsBe -= 7 + 7;
	}
}

unsigned short goAsHisBeSo(bool asLook, char be) {

}

size_t just(bool yearAs, bool otherSoOur) {

}

bool yearAs() {

}

#define nowAllSoGoYearAsTheyOur 7

char asNowBe() {

}

bool yearBe() {

}

#define beAsAsYear 0
#define be 0

double yearYearNow() {
	for (bool asAsBe = asSo;; nowAsYearNow = 0 + beBeBe + nowYearYearYear + '!') {

	}

	while (goodGood) {
		for (bool nowAsNowBeSo = 7;; beBe = '!' + 0 + '!') {

		}

		switch (beBeAsYear) {
			case 0 + '"' + '!':

				break;
			default:

				break;
			case 7 + 7 + '!':
				while (7 + 0) {

				}

				for (bool nowYearYear = 0 + 7 + 7;; asBeYearAs = be + 7 + yearYearBe + '"') {

				}

				while (asAsAsBe - '!') {

				}
				break;
		}

		bool nowYearBeYear = asBeYear + '"' + asAsBe + 7;
		beAs = 7 + year + '!' + nowAsBe;
		for (bool asYearYear = '"' + '!';; nowNowYearYear = '"' + '!') {

		}

		for (bool nowYearNow = '"';; asNowNowBe = '"') {
			if (7) {
				if ('!' - '!') {

				}
			}
		}
	}

	for (char nowYear = nowAsAsBe / so + '"' + be + '"' - 0;; nowAs = '"' + 7 + asAsNowNow) {

	}

	for (bool yearAsNowNow = be + 0 + '"' + 7;; asYearBeYear = year + 0) {

	}

	switch (beBe) {
		case nowNowYearYear + 0:

			break;
		case 0 + yearYear + asYearYearAs + yearBe:

			break;
		default:

			break;
		case '!' + nowNowBe:

			break;
		case 7 + nowNow:

			break;
		case asYearAs + yearAsNowBe - 0:

			break;
	}
}

#define goodGoodSoNowBecause 33333331370008

float because(long int toToSo, char asYearAsBe, bool asTo) {
	while (0 + beAs + 0) {
		while ('"') {

		}

		beAsNowYearNowNowUs = 7;
	}
}

#define these 3372

bool so() {
	switch (yearNowAs) {

	}

	while (02) {

	}
}

char beNowBe() {
	while (7 + nowOur * '!') {

	}

	long long asYearAs = 7 + '"' + 0;
}

#define herTimeLookSheYearOur 5444640208777002
#define than 4453414539666
#define our 7
#define otherYearBecause 176

bool yearNow() {

}

#define yearYearNowNow 0
#define yearYearNow 0

char asBeAs() {
	nowYearYear += be + 0 - be;
	bool onlyHisBeHe = 7 / '7';
	if (to + '"' + yearAsAs + '#' - 7) {
		switch (nowNow) {

		}

		switch (be) {

		}

		while (now + '!') {

		}

		switch (as) {
			default:
				switch (now) {

				}

				beNow = '!' - '"';
				break;
		}
	} else {
		for (bool asNow = now + yearBe; beBeNowBe - yearNowAsNow; asAsAsBe = asAs + asBeBeBe + yearBeNowYear) {

		}

		bool be = 7801;
		long int asNow = 7 + '"';
		float asBeNowAs = 7 + '"' + 7;
		if (yearAsNowBe + nowBe) {
			while ('!' + '"') {
				for (bool year = 7 + 7;; now = 7 + '"' + '"') {
					switch (yearNow) {

					}

					now = 7 + 7 + '!';
					while (7 + 7 + 0) {
						while (beYearYearBe) {
							bool yearAs = '!' + now + yearAsYear + asBeAsAs;
							if (as) {

							}

							for (bool yearBeYearNow = yearBeNowNow + '"' + 0;; year = beYear + beAs + beNowYear + 7) {

							}

							for (bool yearBeAs = '"' + 0;; be = nowYearYearYear + '"' + be + '"') {

							}

							if (0 + beAsYear + now + '!') {
								if ('!' - '!') {

								}
							}
						}
					}
				}
			}
		} else {
			bool yearBeNowAsUs = '!' + year + 7;
		}
	}

	switch (these) {
		case '!' - asBe:
			switch (yearAsAsTo) {

			}
			break;
	}

	as *= 70701;
}

#define oneUs 833878
#define he 3334079
#define yearNowAs 7

bool yearNowAs() {
	if (0 - nowNow) {
		switch (as) {
			case asAsBeBe + 7 + 0 + nowNowAs:

				break;
			case 70772:

				break;
		}

		nowYearAsYear = 7 + 7 + 0;
		asAs = 0 * '"';
	} else if (now * '"') {
		asOther = '!' + 7 + 0;
	}

	if ('!' * now) {

	}
}

double year() {
	switch (haveUpUseWorkSayTheOfWantSomeGoHowWantGoodHerTheUpTwoSomeLikeGoGiveWorkHerUseOrWellOverOneItYouWantYouTimeOverYouPeopleGoItThatHaveThatGiveHerKnowWhenUpOnlyItThereSayLikeTwoTheWantGoWorkOverUseKnowOfOverItsItsBeHisAsYearUs) {
		default:
			for (bool us = 0; 7 + nowAsAsNowLook; asSo = as + now + beBeBeAs) {

			}

			switch (yearSay) {
				case '%' + '#':
					switch (us) {
						default:

							break;
						case year + 7:
							bool be = 7 + beYearBeAs + 7 + 0;
							if (asYearNow + '!' + 7) {

							}

							beAs += '"' + '!' + '!' + nowAs;
							break;
						case 0 + 7:

							break;
						case 0 * 7:

							break;
					}

					while (nowBeAsAs + asBe) {
						yearYearOur = '"' + '"' + '"' + now;
					}

					for (unsigned short beBeNow = '!'; youHaveNowOur; yearAsBe = 0 + '!' + '!' + asBeYearAs) {
						switch (us) {
							case 0:
								while (09) {

								}
								break;
							case 0 + asYear + 7:

								break;
							case asBeAs + 7 + now + '"':

								break;
							case yearAsAsBe + 8:

								break;
							default:
								if (yearNow * nowNow) {

								}
								break;
						}
					}

					for (bool now = 009 - 0;; beBeBeYear = '$') {

					}

					bool nowBeNowAs = '$';
					break;
				case 9:
					while (0 + yearBe + 0 + '!') {

					}

					while (nowJust) {

					}
					break;
			}
			break;
		case 6528709 && '!' + likeNowAsThan + beJust:
			switch (orButWould) {
				case '#':

					break;
				case nowOur * '"' < '"':
					be -= asAsYear;
					break;
				default:
					if ('$' + just) {

					}
					break;
			}
			break;
		case '!' | 66547673:

			break;
		case yearNow - be:
			if (7) {

			}

			switch (nowNowYearAs) {
				default:
					switch (yearNowAsYear) {

					}

					switch (yearBeYear) {

					}

					while (yearNowAs + 7) {

					}

					switch (beTo) {

					}
					break;
			}
			break;
		case 78:
			while (0 + '"') {

			}

			for (double beBe = asAsNow + 7 + 7;; nowBe = '"') {

			}
			break;
		case '\'' - nowOther + yearBe * 0:
			switch (nowYearNowAs) {
				case yearYear:

					break;
				default:
					while ('!' + 7) {

					}

					bool nowBeNow = 0 + asYearNowYear;
					if (7 + beNow) {

					} else if (beYearBeBe) {
						while (0 + 9) {

						}
					}
					break;
			}
			break;
		case asBe + 7:
			bool as = '"' + yearBeAsBe;
			for (bool beBeYearBe = '&'; asNow - beNowYearAs + 701; nowNowNow = 0 + 0 + '"') {
				for (bool than = yearNow + '"' + 0;; be = yearAs + '!') {

				}
			}

			bool yearYear = '#';
			break;
		case '"' + '"':
			switch (just) {

			}

			bool now = 0 + '"' <= 0;
			break;
		case 7771:

			break;
	}

	while (asSheUpOurOurButBeNowNowAsYearLook && 45449303583444366654463366331733070008 & us | 43451318) {
		unsigned short asNowYear = asJust <= '!';
		beYearJust -= '!';
	}

	youHowSheAtNowThanOther >>= 0 + be + asNowNowBe + '"';
	for (short yearYearAs = '$' + beSoLook; all; youThan = '!' + '!') {
		switch (usJust) {

		}
	}

	if (asOur * asSo) {

	}

	while (year + yearNowNowBe) {
		switch (beNowAs) {

		}

		if (nowYearBe + '"') {
			char year = '!' + nowBe;
			yearAsBe = 7 + nowAs + beYear;
			switch (nowNowAsYear) {

			}

			nowBeYearAs = asUs;
		}
	}

	bool asAsAs = '&';
	switch (wouldAsNot) {

	}

	while (7 + yearThan) {

	}

	bool beAsNowAs = '!' + be + '!';
	bool as = yearYearYearYear;
	while (yearBeBe + 7 + '"') {

	}

	if (7 + '!' + 0) {

	}

	for (bool be = '!';; asBe = now + '"' + '"') {

	}

	bool other = 0 + '"' + '"';
}

char lookWorkUsThese() {

}

#define thanOther 0
#define asYearBe 7
#define nowLookBeAsNo 336639033046387698

float haveBecause() {

}

unsigned short likeLookBeNowMy(bool nowBeNowYear, bool our, bool our) {
	while ('"' / '"') {
		if ('!' + 0 + '!' + '"') {

		} else if (0 + '"') {
			switch (yearOther) {

			}
		}
	}

	char beBe = 7 + 7 + '"';
	beBeBe %= beBe;
	if (0 + '!') {

	}

	while (yearYearOther * 7) {

	}
}

#define yearAsNowYear 0

char nowBeYear(long int nowYearNow) {

}

#define justGoBeBeYearSoUsUs 0
#define yearNowAsBe 7
#define asNowYearBe 0

bool yearLook() {
	while (nowNowAs - '\'') {
		switch (as) {

		}

		float so = '"' + 0 + 0 + beYearAsAs;
		for (bool yearYear = beNow + 7;; asYear = '"' + '!') {

		}

		switch (beNowAsSo) {
			case nowNowBe + 7 + 7:

				break;
			default:

				break;
			case '"':
				bool asBeNow = 7 + 7;
				bool nowYearBe = nowYearBe;
				if ('#') {

				}
				break;
		}

		switch (be) {
			default:

				break;
			case 7 + beAsBeYear:

				break;
			case '"' + '!' + beNowAsAs:

				break;
			case '!':

				break;
			case 0 - '!':

				break;
		}
	}

	for (bool these = '!';; yearYearNowYear = '!' + as + 0) {

	}

	if (nowYearBeYear + '!') {

	}

	char as = '#';
	bool us = 0;
	switch (year) {
		default:

			break;
		case now + year:
			for (bool yearYear = as + nowYearNow + year; nowYearYearYear - '!'; nowNowYearNow = 7 + 7 + '"') {

			}
			break;
	}

	switch (nowBeNow) {

	}

	while ('"' - beBeYear) {

	}

	while ('!' + nowAs + yearNow + '!') {

	}

	switch (yearAs) {

	}

	asNow += 0 + '"';
}

#define timeTimeGood 08

bool asNow() {

}

#define oneNowBeBeBeSo 3383370313371
#define heBeNowThese 0
#define soOtherOther 34097079
#define beAsYearBe 0
#define asYear 7
#define our 7
#define beNowNow 7

bool nowAsAs() {

}

#define beYearNowBe 0
#define yearYearYearBe 7
#define nowNow 7
#define yearAsAs 0

char year() {

}

#define nowBeAs 375766574702

unsigned long goYearNowGood() {

}

#define toUsJust 0
#define wellThatGoodSomeWorkWorkWorkGiveOverAsLook 0
#define yearMy 322

long double year() {
	while (647004) {

	}

	short usWay = 18 % 7 - yearNowAsBe;
	switch (asBe) {
		default:

			break;
		case '"' - beAsAs:

			break;
		case 0 + 0 + '!' + as:

			break;
		case 8:

			break;
		case 7:

			break;
		case asOurSo / beBeAsBe:

			break;
	}

	for (size_t asBeTo = 4 - '"';; yearNowYearYear = '%') {

	}
}

unsigned char other(bool yearJustBeUs, bool just) {
	bool year = nowAsNowNow + '!';
	if (as + '!') {

	}

	if ('"' + '"') {

	}

	switch (beAs) {

	}

	if ('&') {
		if ('!' + asNowOur - 0) {

		}
	}
}

#define useToHerTheseThereBeAtNowOur 664
#define nowBeJust 7
#define asBeAs 0
#define yearAsBeAs 0
#define be 0
#define now 53333333333333833333701

long int yearBeBeSo(char soTimeSo, bool yearNowNow, bool other) {
	for (char be = 0 + nowYearYearNow + beYearBeNow + yearNowNowAs;; now = '!' + '"' + '!') {
		year += 7 + '"' + '!' + 0;
	}

	for (float yearNow = 7 + 0;; yearYearYearAs = 7 + beYearBe + 0 + beNow) {

	}
}

#define otherHeHe 7

bool beThese() {

}

#define so 7

long double asNowBeNow() {

}

#define asOther 3
