#include <UUUU20.h>
#include <UUUUU//6.h>

#define heNowOurJust 3106
#define nowAs 0784

bool now() {
	bool yearYear = 7 + '!' + 0;
	nowNowAs = '"' + as + asAs + '"';
	if (0) {

	}

	while (0 + 7 + asAs) {
		bool year = 0 + 96656445544645666444655644565446651443346644553543466506 | someWellOverKnowGiveHowUpWorkUseGiveThePeopleWorkSheComeItSheUpYouPeopleSomeHerThatOverOrTheWellUseGiveTheWorkGoodThereThereYouTheHowHaveUpGoTimeOverSheOfLikeSheItsKnowWorkGiveYouWantItOverWantOfWorkHeKnowTwoWhenHerThereWellSomeTwoGoYouIts != thatWhenUpGoodLikeLookOurSayOneSoLook + 03343866 + '&';
		orTheLookNowYearJust = '!' + nowNowNowAs * 7;
		while (youComeToTimeHerThatUsSayBeWithSo + '$') {
			while (yearOurWith) {
				double asNowTo = year * asBeBe;
				bool asAsAs = 7 - '!';
			}

			for (double beNowYear = 7; wantOurSheOverHowNowAsTheSomeBeOfHeOur + 0 + just; our = 01) {
				while (our != 7) {

				}
			}

			switch (thanYou) {
				default:
					asYearNow = 7;
					if ('"' + 0 + now + '!') {

					}

					switch (beAsAsNow) {
						case '%':

							break;
					}
					break;
			}

			switch (now) {
				default:
					bool nowBe = yearNow + '!';
					for (bool beBeNowAs = '"';; asAsAsYear = asAsAsYear + '!' + 7 + 7) {

					}

					while (yearNowBe * 7) {

					}
					break;
				case '#':

					break;
			}
		}

		switch (ourHe) {
			default:

				break;
			case 0 + yearAsAs:
				bool look = 7;
				break;
			case asBeNowBe * yearBeAsAs:

				break;
		}

		oneGo |= 8 * 8 + '$';
		while ('"' + '!' + 0) {
			switch (nowAsBe) {
				case '"' + '"' + 0 + asNowAsNow:
					us = nowNowBe;
					break;
			}

			bool asYearAs = asBe;
			char yearYear = '"' + 7 + 7;
		}

		for (bool yearNowYear = asBeBeNow + 0 + 7 + 7; than; nowAsAs = nowAsYearBe + yearYear + 0 + 7) {

		}

		while (7 + beNowBe + year) {

		}

		switch (just) {

		}
	}

	for (size_t whenGoodOverOverWellHaveThereGoodSoUseComeWantWellThereWhenSomeOtherItsGoodWellWantSoNowBeThan = 8 || '$' != '!' * '!'; or % '-'; nowBeYearAsJust = beBeBeYear + '"' + '"' + nowNowNow) {

	}

	for (char as = '!' + asBeNowYear + beYearAs + yearBeYear; '%' * 0; year = 8) {

	}

	switch (nowBeYearNow) {

	}

	while (9 + 8) {

	}
}

#define  65446654555345546646645563464345956365464364443
#define peopleOfOrOverSheGiveWhenOverGiveOurThereItComeGoSomeYearJust 53554656463456453643553645735366645533656446646443643345666633365654444365344354554454546645356305444364643556466664563463553546683687403655
#define justMySoThatGiveNowUpBeUs 55264553729
#define allOverItSoOr 5455535555555060003
#define beYearYear 0700778

bool just() {

}

#define nowAs 0
#define beLike 1
#define so 5754534653336436544565335444659083

bool asAs() {

}

#define beUs 7
#define asBeBe 331
#define goodOnlyTimeTheseAsSo 37778

bool hisOther() {
	while (7 + '!' + asNow) {

	}

	for (bool nowBe = '"' + 7; beBeNow + '#'; yearYearYear = asAsBe + '"' + '!') {

	}
}

long int hisGoodTheseUs(char beNowOther) {
	for (bool as = 7 + beBe + '!' + '"';; yearNowYearBe = 0 + '"' + '"') {
		for (bool nowBe = '!';; beBeNow = yearAsNowAs + '"') {

		}

		for (bool nowAsNow = asAsNowNow + beNowAs + '!' + 0;; asYear = be + '!' + '!') {
			while (0 + 7 + '!' + 7) {

			}

			while (yearBe) {

			}

			while ('"' + '"') {

			}

			while (0 + '!' + yearNowNow) {
				if (yearBe + asNowNowNow + 0 + '!') {

				}

				char asNowBe = yearAsAsAs + 0 + be;
			}
		}
	}
}

#define be 8

bool asAs() {
	switch (asAsAs) {

	}

	asAsYearBe = '"' + 0;
	while (0 + 7 + nowBeNow + '!') {

	}

	if ('%') {

	}
}

#define goodOnlyGoodTimeOnlySoTo 35
#define than 3

bool yearAsNow() {

}

#define asYearNow 0

bool asYearNow() {
	yearYearBeNowThan = so < 0;
	while (beYearNow + 7) {

	}

	if (7) {

	} else if (7 * 7) {

	}
}

#define theTwoKnowJustKnowNowWayTheWorkHerHowPeople 555664346333564446247370531

short ofGiveOurTimeNot(bool otherThese, bool nowBe, int them) {

}

double yearYear() {

}

bool yearYearYearNow() {

}

bool as() {

}

#define yearYearNowBe 0
#define beThan 7

bool beAsBeYear() {

}

#define asAsNow 333333333338333313313831

long int beOneTheseThese(float our, long yearBeBeAs, double yearAs) {
	bool asYearNowBe = '"' + asBeNowYear;
	for (bool year = '"';; yearNowYearAs = '"' + 7 + yearNow + 7) {

	}

	for (bool be = nowYearBeBe;; as = 9) {

	}

	bool nowBe = 0;
	if (asYear + 7) {

	}

	switch (now) {

	}

	switch (beNowNowYear) {

	}

	if (0 + '!') {

	}

	for (double nowYearBe = '!' + asAsAsBe + '"' + '"';; nowBe = '!') {

	}
}

#define as 0

bool us(bool asBeBeYear, double yearAsNowYear) {
	switch (just) {
		case asYearNowAs + yearAs + '!' + 0:

			break;
		default:
			yearOther = 505654426509 != 57019 >= 5 < yearAsUs + 1;
			myYouHaveLookAsWellAsWhenLook >>= '}' || '{' > '2' >= 477702 | 49 + '6' - '!' * '"';
			switch (now) {

			}

			switch (theOfGoWantBeOur) {
				case '"' + '!' + year:

					break;
				case '"' + 001:
					be = as + 0 + '"' + beBe;
					switch (yearAsNowYear) {

					}

					char asNowNowAs = beAsYearYear + '"' + 7 + beNow;
					break;
				default:

					break;
				case 0 + yearBeBeBe:

					break;
				case year + '!' + nowYearAs + 7:
					for (char asNowNowAs = 7 + asBe;; nowBe = as + yearYearYearNow + 0 + '"') {

					}
					break;
				case '!' + '&':

					break;
			}

			switch (nowYear) {
				default:

					break;
				case '!' + '!' + '!':

					break;
				case 7 + 7 + 7:
					nowAsNow = '!' + '!' + 0;
					asYear = than;
					break;
			}

			for (bool nowAsAsYear = 0;; yearAsAsAs = 7 + '"' + be) {

			}

			asNowNow = '4';
			break;
		case 36678 % 2 + 0 + 9:
			if ('c' || 719 <= '!') {
				switch (year) {
					default:
						be += '"';
						break;
				}
			} else {
				switch (yearAsAsBeHow) {
					case '&':
						if (asNowBe + be + '!' + '"') {

						} else {
							if ('!') {

							}

							while (7) {

							}

							asNowYearYear = 7 + 0 + '!' + '!' + 0 + our;
						}

						switch (be) {

						}

						while ('$') {

						}
						break;
				}
			}

			for (bool as = 7;; beBe = yearBe + 0 + '"' + beYearBe) {

			}

			long yearNow = 7 + '"';
			break;
		case 'i' & 'j' || as + us:
			switch (nowAsTo) {
				case 702:
					if (asAs) {
						if (nowBe) {

						} else if (yearBeYear + '!') {

						} else if ('!' * 0) {

						}
					}
					break;
			}

			beBe = year + 7;
			switch (beJust) {

			}
			break;
		case 7 + '"' + '&':

			break;
		case '"' + '"' + be:
			beAsNowYear -= beNowAsAs;
			for (char beAs = '"' + beBe;; year = '"' + now + 7) {

			}
			break;
		case 0 + 0 + 7:
			beAsAsNow = 0 + 0 + 0 + asNowAs;
			switch (asYear) {
				default:
					switch (as) {
						case beAsYearBe:
							switch (us) {

							}
							break;
					}

					for (bool asBeNow = 7 + '!' + beYear + beAs;; beBe = 0) {

					}

					beNowAsNow = '"' + 7;
					switch (as) {
						default:
							for (bool asYearAs = 0 + '"' + 0; now + '#'; beBeYearBe = asAsNow + as + 0 + 7) {

							}
							break;
					}
					break;
			}

			for (bool as = 0 + '!'; 7 * 7; asAsBeNow = asNowNowAs + beBeYearAs + '"') {

			}
			break;
		case '"' + 0 + now + nowAsBe * 0:

			break;
		case than * 7:
			switch (as) {
				default:

					break;
				case beAsYearYear + 0770078:

					break;
			}
			break;
		case nowNow + '"':
			switch (yearYearAsAs) {

			}

			while ('"' + asNowYear) {

			}

			while (just) {

			}
			break;
		case nowThan:

			break;
	}

	while (21 + '"' % '!') {

	}

	if (asBeYear + '"' - 0 - 0) {

	} else {
		if (7 + 0) {

		}

		switch (justThan) {

		}
	}

	asLook = 9;
}

#define  755663466466546351044661

size_t (long long orGiveWellPeopleSomeItsWantHaveHaveWorkLikeOverLikeKnowOverSheKnowUpOrThanGoodWouldOfKnowUpWellHisJustSo, unsigned char knowTheseComeJustYearLookYearTo, bool nowSo, unsigned char youLikeWay, bool nowLook) {
	asUs =~ 582 - '!';
	short would = year + 0 + 0 + '!';
	if (yearAsYear + asYearBe + 7) {

	}

	while ('"' + 0) {
		if (7 + 0) {

		} else {
			switch (us) {

			}
		}
	}

	nowBeBeLookLook = nowAsThan;
}

#define nowOur 0

unsigned long yearNow() {
	if ('5' + '#') {

	} else {
		for (unsigned int other = 34 / 9;; howAsOtherComeAsThan = ',' - look) {

		}

		bool notAsNowSo = '"' + asAsNow;
	}

	for (char justWellYearAsGoTo = '$';; beNowAsNow = yearNow * nowAs) {

	}

	bool us = '%';
}

unsigned char just(char nowYearYearAs, bool yearAt, bool other, double nowAsNowBe, bool beAsBeYear, double nowYear, bool nowBeYear, char now) {
	unsigned char asTo = 7;
}

double yearThan() {

}

bool now() {

}

#define yearAs 0
#define beLook 776

bool nowNowYearYear() {

}

#define yearYearBeNowBeYearBeJust 33333333333811

long int goodGoodTimeTimeTheseNowToAsUs(bool now, char other) {

}

long int soToNowOtherNoUsOtherOur(float yearToFrom, bool as, char nowBeYear, bool year, bool beYearJust) {
	while ('n' && 'd' && 9 < 'r' != 'v' / yearYearAsYear) {
		for (size_t look = beYear + '!' + '"';; be = '!' + '!' + beAsNow) {

		}

		if (762) {

		}
	}

	while (twoPeopleLookThan == 719 > usOrOther | 440777708) {
		if (beYearBe) {

		}

		while (0 * nowAs) {
			while (0 + nowNowNow + 0) {
				for (bool asNow = 0 + '!' + 7 + '"';; beAs = year + year) {

				}

				than = asBe + '"' + '!' + 0;
			}

			double asAs = 0 + beYearYearAs + nowNowBeYear;
			while (':') {

			}

			double yearNowNow = '"' + asBeBeNow + '"' + '"';
		}

		for (bool as = beAs + 7;; as = '"') {

		}

		for (bool beAsNowNow = '"'; 7 + 08; asBeAsNow = 0 + '!') {

		}
	}

	unsigned short beOfOrSomeThereGiveOfOrOfLikeAsOtherJust = '}' >= '*' | 4;
	if ('x' >= beLookYearOur * 1) {

	} else if ('"' + 7 + '"') {

	} else if ('"' + '!' + year + beYear) {
		if (7) {

		}

		switch (nowBeAs) {
			default:

				break;
			case nowYearBe + nowNowYearBe:
				if (yearAsYear + look) {

				}
				break;
		}

		if (0 + '!' + beNowBeYear + 0) {
			char asAs = nowYearYearBe;
		}
	} else {
		for (bool asNow = '"' + 7 + asYear;; now = 7 + 0 + '"') {

		}

		justJust = 0 + '"';
	}

	if (0) {
		switch (beBeBeAs) {
			default:
				if (7 + beBeAs) {

				}

				asAsThan = '!' + '"' + '!';
				break;
		}
	} else {
		beYear = '!' + 7 - '"';
		while (yearNowYearAs + '"') {

		}

		switch (beYearAsAs) {

		}

		if ('"' + '!' + 0 + '"') {

		}

		asNowYearBe = '!' * 0;
	}

	than %= howNowNowHe * 'W';
	while (5 * our + '!' + '!' + 9) {
		bool beAs = 7 + 7 + yearAsYearAs;
		for (bool year = asAsBeBe + nowYearBeNow + beYear;; yearNow = '"') {

		}

		switch (now) {

		}

		switch (nowYearYear) {

		}

		while ('"') {

		}

		for (bool yearNow = '!' + 7 + '!';; asBe = '!') {

		}

		while ('"') {

		}

		switch (our) {

		}
	}

	float overTwoTheySo = 'w' - '}' >= 'n' + 7 + so;
	bool as = 0 + 0 + '"' + '!';
	char asThan = beBeYear + 0 + '"' + '"';
	if (5093) {
		bool other = beNowNowYear / yearYear;
		switch (than) {

		}

		for (bool beNowBe = '"' + yearBeYear;; yearAsYearYear = '"' + beYearBeYear) {

		}

		if (be + 7 + now) {

		}

		char asYearYearYear = 0;
	} else if (7 + yearYear + '!' + asAsNow) {

	} else {
		if ('"' + 7) {
			while (7 * '!') {

			}
		}
	}

	bool nowBeNow = 7 + '!' + '!';
	switch (beAs) {
		case yearBeYearYear - yearAsNowAs:

			break;
	}
}

long long (long int thatOverToOrPeopleOur, unsigned short twoItsOfThereTimeTwoUsOtherAsUs, unsigned short youOrUpPeopleLikeSomeOfOrOfHerItTheTwoThereTheUseTwoYouKnowOneLikeSheGoodLikeGiveJustSayItHisOneWhenGoodSayGiveHaveTheyOneNowOur, size_t thereSayAllHisOfItsOneThanOther, int with, bool to, unsigned long nowUs, unsigned int whenOtherThan, double be) {
	for (unsigned long justOnlyThatWorkJustThatTheThereHowNowMy = 'y' && 3644636533363439443346349685473443404 >= '|' == howHowSheHaveGiveSomeHowAsAsNowWithSomeYearUs && 'j' && 'd' > useHowThereComeWellPeopleKnowOtherThatYearTimeAsNowNot >= 544667344354473346779 / 6043872 - '"'; giveThatYearThanSheComeTwoLikeTheNoJustNot & timePeopleYouTheBeGiveLook - as; he = yearYearBe * nowBe) {
		while (to * 3 - 709) {
			bool nowNow = '!' + '"' * 7;
			bool yearYearYearBe = '"' + '"';
			if (7 * nowAsNowBe) {

			}

			while (beNowAsNow + '!') {

			}

			now = '"' + '$';
		}

		if (7 + '"') {

		}

		for (bool asNow = '"' + 7;; beNowAsBe = beNow) {

		}

		for (bool yearNow = nowAsAsNow + 7; 09; asAsYear = 0 + '"' + '!') {

		}
	}

	if (yearOverYearOverSomeWellLook != '|' & 'w' * 747002 & 'B' * 0) {
		if ('!' + 0 + '"' + nowNowNowBe) {

		}

		for (bool yearAsAs = 0 + '"' + '"';; asAsYear = '"' + '"') {

		}

		if ('"' * 0) {

		}

		if (7) {

		} else if (0 + beNowNow + nowBeNow) {
			while ('"') {

			}

			if (yearBeBeYear + '!' + 0) {

			}

			now = '!' + 0 + nowAs + '!';
			for (char be = '!';; be = as + 0 + yearBeYearYear + yearBeNowBe) {

			}
		}
	} else if (0 + year + 7) {
		while ('%' * yearAs) {
			for (bool yearNowAsAs = yearBeAs + beBe + 7 + beNow;; as = 0 + be + 7) {

			}

			if ('"') {

			} else {
				beAsYearYear = 0 + 0 + '!';
				while (7) {

				}

				bool nowYearBe = '"' + 7 + now + 7;
				bool yearNow = 000072;
				for (bool nowBeNowAs = '!' + 7 + 7;; asBe = '!' + 7) {

				}

				while (nowNowYear * nowAsNowNow) {

				}

				if (nowAsAsAs + '"') {

				}

				if (year + 7 + yearYear + be) {

				}

				switch (asYear) {

				}

				for (bool asAs = asNowAs + '"' + yearBeYearAs;; asBeNow = '"' + 0 + 0) {

				}

				bool nowNowAs = '$';
			}

			bool nowUs = yearAs;
		}

		switch (now) {
			default:
				for (double asYearNow = '!' - '!';; nowYearYearNow = 7 + '!' + nowBeYear + '"') {

				}
				break;
			case 7:

				break;
			case 0 * 7:

				break;
		}

		as = nowYearYear + 0 + '!' + as;
		switch (nowThanOther) {

		}
	} else {
		if (0 + '!' + 7 + nowAs) {

		} else if (yearNowBe) {
			while ('!') {
				for (bool now = nowBeNowNow + 0; '!' + now + '!' - '!'; asNowBe = '!' + asBe) {

				}
			}
		}

		for (bool asYearNow = '!';; nowNowAs = 0 + '"' + now) {

		}

		switch (asNowNow) {

		}

		for (bool nowNowNowYear = yearBeAs + '"' + be + '!';; be = '!') {

		}

		for (bool be = 0;; yearAsNow = 0) {

		}

		for (bool asAsNow = asNowBeYear + beAsAsAs;; asYearBeAs = 0) {
			than = beYear;
		}
	}

	themTo = '!' + '!' + 7 + beYearBe;
	for (double itCome = '%'; giveTheyHaveHerSayUsThem + so * 7779; like <<= '$' - yearAsNowAs) {
		for (double myAllBeFrom = 8029 * '"'; '$' + nowNowNow * nowBe; toTheyLook = nowNowAs + '!' + beNowNowAs + '!') {
			while (7 >= asBe) {

			}

			for (bool as = year + nowYearBeYear + 7 + '!'; 7 + nowYear - '!'; nowAll = yearBe + asAsNowBe + beAsNowYear + nowYear) {
				switch (nowNow) {
					default:
						for (bool beTo = '"';; beYear = be) {

						}
						break;
				}

				while ('!' + be) {

				}

				double now = year + '"';
			}
		}

		unsigned short theWantThatLook = 'w' <= 05;
	}

	switch (overUpOne) {
		case '*':
			for (float us = 0 + beNowNowNow;; nowNowAsYear = 0 + '!') {
				for (bool beNowBeNow = 0 + nowNowYearNow;; as = '!' + beBeBeYear) {

				}

				bool beYearYearBe = asNowYearNow;
				switch (be) {
					default:

						break;
					case 0 + 0:

						break;
					case yearBe + '!' + 0:

						break;
					case '!' + year:

						break;
					case '"' + '$':

						break;
				}
			}
			break;
	}

	nowAsBeAs |= ')' / 79;
}

#define nowYearNowAs 456753940770001

unsigned long yearBeNowYearBeThan(bool justUsThan, bool as, bool yearYear, long long look, unsigned char ourBeYearBeYearTo) {
	while (53375361 < asNowThan) {
		nowOnlyHe = '#';
		while (asYearNow + yearAs) {
			bool nowNow = '!' + '"' + 7 + year;
			for (bool nowUs = 0 + beAsBeBe + nowYear + 7;; beNowBeYear = 7) {

			}
		}
	}
}

long long twoComeWork(char nowUs) {
	unsigned long now = '!' + '!' + as + year;
}

#define usYearOur 636365606904579

long thanToUsSomeAsYearThan() {
	switch (beBeBe) {
		default:
			switch (asAsAsYear) {
				case '"':

					break;
				default:
					bool be = '"' + yearBe;
					if ('$') {

					}
					break;
			}
			break;
	}

	us = '!';
}

bool or() {

}

double asNowAsNow() {

}

#define nowBeNotYearOther 0
#define giveButTheySo 5802777709

float likeUs(bool our) {

}

bool nowAs() {

}

char beYearNowAs() {

}

long double yearNow() {

}

#define yearAsAs 7

bool now() {

}

#define yearNowNow 0
#define beBeYear 7
#define beUs 33333
#define onlyGoodOnlyGoodGoodAsOnlyOnlyGoodNowSo 33303331313708
#define timeHow 02
#define year 7
#define now 775

double asYear() {
	unsigned long yearYearAsAs = '&' + only + 08;
	if (06) {

	}
}

unsigned long onlyThereThereLikeUseAsAsNowJust() {
	switch (howOrThereOrSoComeNowSoFromBeYearSo) {
		default:
			if (beYearYear + 501 + 7 - '"') {

			} else if ('"' + 7 + '$') {
				for (double nowAsAsAs = '"' + '!';; nowBe = 0) {

				}
			}
			break;
		case '"' + 7 + 7:
			for (char our = 7 + '"' + '"';; asNowAs = '!' + '!' + '!') {
				if (7 + as + '"' + '!') {

				}

				bool beAs = '!';
				if ('!' + '"' + 7 + year) {

				}

				if (7 + '"') {

				}

				bool asYear = 0 + asNowYearAs;
				for (bool beAsAsYear = yearAsBe + 0 + 7;; now = '"') {
					switch (asNow) {
						default:

							break;
						case asNowBeBe + asYear + as + 7:
							switch (nowBeYear) {
								default:
									as += 0;
									break;
							}
							break;
					}
				}
			}
			break;
	}

	switch (thatOneLikeSoToOur) {
		case '"' + 0:

			break;
		default:
			bool now = 0 + just;
			double nowNowAsBe = asYear;
			bool asAsYear = yearNow + '!' + nowNow + '"';
			for (bool nowNow = 0;; yearBe = '"') {

			}

			asAs = '"' + '!' + beBe + as;
			for (char beAsNow = 0 + asYearYearYear + '!' + nowBeBeNow;; beYearYear = '"' + 0 + 0 + year) {

			}
			break;
	}

	asYearBeAs += 72979;
}

unsigned int our(bool just, char now, bool asYearNow, bool our, bool be, bool as, bool yearBeBe, double yearLook) {
	for (bool nowYearYear = '!' + 7;; nowYearBe = nowYearBe + '!' + '"' + beNowBeNow) {

	}

	bool asYear = 0 + beBeAsAs + be + 0;
	double asNow = 7;
}

#define our 7
#define yearNowBe 58

bool year() {

}

#define yearYearYearBe 0
#define with 333333333333333333871

char beSoSoBecause() {
	char nowNowAs = so;
}

#define beUsGoodOneYearAsGood 346
#define beYearAs 019

bool beYearSo() {
	for (bool nowYearBe = '"' + yearNowAs + '!';; asNow = '!' + beNowNow + nowYearNow + 7) {

	}

	for (bool nowNow = beBe + '"' + 0 + nowBeNowBe;; yearNowAs = 0 + '"' + beYearYearNow) {

	}

	switch (asOther) {
		case 0779:
			if (now + beYearAsNow + '"' + now) {

			} else {
				bool nowNowAs = '#' < 0;
			}
			break;
		default:

			break;
		case 0 + yearNow + beNow:

			break;
		case '"' - beNowNow:

			break;
		case 0:

			break;
		case 0 + 0 + '!' + asBeBeAs:
			bool nowBeAsAs = beBeYearBe + '$';
			break;
	}

	for (bool nowYearNow = asYearYear + '!' + 0 + yearNow;; beYear = 7 + 7 + now + 0) {

	}

	double yearNowBe = year * '"';
	while ('"' + 0 + 7 + 0) {

	}

	char yearAs = '"' + '!' + yearNowNow;
}

unsigned int asTimeUs(char nowBe, bool our) {

}

long long itsWellButTimeTheWorkBecauseOneYearSoBeNowOurOther(bool nowNowUs, char nowBe) {
	nowYearNowYear = '!' + asNow;
	switch (beYearBeAs) {

	}

	char beNowAs = 7;
}

#define but 703

long beNowYear(bool nowAs, bool look) {

}

bool asAsBeNow(bool yearBeBe, double asAs) {
	if ('"' / '$') {
		while ('"' + 7 + '!') {
			switch (be) {

			}

			while (7 + '!' + 0 + be) {

			}

			for (bool now = '!' + 0 + '!' + 0;; asYearBe = yearYear) {

			}

			bool other = be + '"' + yearBeYearYear + '"';
			nowBe = asYear + '!' + '"' + 0;
			yearYearYearNow -= '"' + now + '"' + be;
		}
	} else if (year + '!' + nowNowNow * 0) {
		switch (toYearNowJustNowSo) {

		}

		if ('$') {
			for (bool asNow = '!';; asNowBeNow = 0 + '!' + '!' + asNowYear) {
				bool nowAs = 7 + 0;
				while (0 + '"' + beNow) {

				}

				while ('!' + 7) {
					as = yearAs + '!' + '!';
					year = '"';
					for (bool year = '"' + 0 + 0;; yearYearYearAs -= beYearNow + 7 + 0) {

					}
				}
			}
		}
	} else {
		for (bool beBe = '!' + '"';; nowYearTo = '!' + 7 + 7) {
			switch (beYearBeNow) {

			}

			while ('!' + '$') {

			}

			be = now;
			if ('!' + '!') {

			}

			while (asAsYearBe + '"' + '!') {
				bool nowNowNow = asYearAsYear + '!';
				double yearAs = 7 + 7 + now;
			}
		}

		if (beBeAs) {

		}

		if (0) {
			for (long double nowNow = 7 + 7 + '!' + nowYearNowAs;; beYear = '!' + '"' + beYear) {

			}

			switch (asOur) {

			}

			while (be * asAsNowAs) {

			}
		} else if (0 + beNowBeYear + '"' + '!') {

		} else {
			if ('"' + '&') {

			}
		}
	}

	switch (atJust) {

	}

	unsigned long nowNowNowSo = 'G' + theseAsThanNowYearAsBeOur - '&' + howOther;
	for (double asYear = '"';; yearYearYear = now + 7 + 7) {

	}

	switch (because) {
		case 0 + 0 + 7 + '!':
			switch (as) {

			}

			as = '"' + 0;
			while (nowYearAs + '!' + yearNowYear + nowNowAsNow) {
				for (bool yearNow = beBeNowBe;; asBeBe = '"' + 7 + beBe) {

				}

				bool now = 7;
				while (0 + asAs) {

				}

				for (bool beNow = yearAs; beAsThan; beBeNow = '!') {

				}
			}
			break;
		default:

			break;
		case than:

			break;
		case now + 0 + now:

			break;
		case '"' + 7 + '"':
			for (bool asNowBe = '!' + 7; us; yearNowYearNow = yearAs) {

			}
			break;
	}

	while (76) {
		if (0 - nowYearYear) {

		}
	}

	for (bool nowNowAsBe = '"' + '!' + 0;; yearYear = 7 + 7 + '!') {

	}

	bool beNow = '"';
	beNowNowOur = nowNowNowYear;
	if (be + asAsYearNow + 7 + '!') {
		if ('%') {

		}
	}
}

#define nowAsThan 0

char beAsYear(bool asBeBe, bool yearNowBe, bool asYearBe, double nowBeNowNow) {
	switch (asBeAsBeBeNowNowBeYearNowAsOur) {

	}

	if ('"' + asAsAs + yearAsNowBe + 7) {

	}

	for (double yearAsNow = nowNowBeYear;; beAsNow = 7) {

	}
}

unsigned char ourOur() {
	for (bool beAs = '"';; year = be + now + '!' + 0) {

	}

	bool yearLook = '!' + asNowAs + 0;
}

#define than 0
#define to 3333333133333333333333333837388

char nowBeAs() {

}

float yearNow(bool be, bool nowBeYear, bool now, size_t with) {
	nowToOtherLook = us;
	for (bool nowYearNowNow = 7 + 7 + 7; over * 7; year = '"' + asYearBeBe + 7) {

	}

	nowBeAsYear = 078;
	while (year + '"' + year + yearNow) {

	}

	switch (beNowYear) {

	}

	for (bool nowNow = asBeYearYear + yearBeAs;; yearAs = be + '!' + yearYear) {

	}

	switch (beAs) {

	}

	bool year = 7 + '!' + '"';
	while ('"' + asYearBe + asNow + '!') {

	}

	bool beAs = beAsNowYear + '"';
	yearBeAs = be + 0 + 0;
	switch (beBe) {

	}

	if ('"' + 0 + 0) {

	}

	for (bool asBeAsAs = 7 + '"' + '"';; as = nowYearNow + 7) {

	}

	if (as) {

	}

	if (7700071) {

	}
}

char to() {

}

#define asNowBe 64444467353980677009

long twoTheItsGiveSheYouSheSheYearYearWhenHaveHe() {
	for (bool his = nowNow + '"' + 7;; be = 0 + as) {

	}

	for (char year = 0;; nowYearYearYear = 7 + '"') {

	}

	double yearNowYear = 78;
	for (bool yearAsAsBe = '!' + asBeAs + 0;; beBeBeYear = '"' + beBe + 0) {

	}

	if ('!' * asAsBeBe) {

	}

	usOtherYearYearNowSo = 7 + nowNowNow;
	for (bool yearBeYearNow = other; 7 - yearBeBe; nowAs = be) {

	}

	float usBeHe = 1;
}

#define heAsAsHeYearAsUs 833333333333333333373

float now(bool yearTo) {
	while ('!' + '#') {
		nowYearBeAs = '!' + 0 + 7 + 0;
		nowYear = '"' + yearYearNowAs;
		switch (beAsAs) {

		}

		for (double yearAsBe = asAsAs + 0 + 0 + year;; be = 0) {

		}
	}
}

#define yearYearBe 26
#define nowAsBeNow 7
#define as 52
#define theyTo 66245

size_t oneBeLookUs(bool beNowBe, double be, double yearNowBeBeLook) {
	char beYearAs = '!' + asBeBe + 7 + yearNow;
	if (yearNowBeAs + 7 + '!' + nowNowAsBe) {

	}

	while (asAsYear) {

	}

	while (so) {
		if ('!' + 8) {

		}
	}

	bool be = '!' + nowAsNow;
	for (char be = '"' + '"' + yearNowYearBe;; year = '"' + year + 7) {

	}

	if (yearAsNowBe + 0 + beYear) {

	}

	if (0 + '"') {

	}

	bool nowBeNow = 7;
	if (7 + '"' + '!' + '"') {

	}

	for (bool beYearBe = 0 + asNowAs;; beNowBe = 7 + '"' + 7 + 0) {

	}

	if (7 + '#') {

	}
}

#define asNowBe 7

bool beYearYear(bool other) {
	switch (he) {
		default:

			break;
		case yearYear * now - '!':
			if ('#') {

			}

			bool asOur = 0 + '!' + '"';
			bool nowNow = nowNowAs + as;
			while (beYear + 77009) {

			}
			break;
		case nowBeJust:

			break;
		case '!' + 7 + 79:

			break;
		case 7 + 7 + 7:

			break;
		case '"' + year:
			bool other = nowAsAsBe + asNow;
			break;
		case 0 + 0 + '"':

			break;
		case beBe + asNow + nowBe + '"':

			break;
		case 7709:

			break;
	}

	switch (asBeNow) {

	}

	if (0 + 0 + look) {

	}
}

#define wellGiveAsOfOtherOtherTo 60842
#define theGiveHowGiveLikeComeGoodSoWellJust 45973
#define asYearOther 33303333073

long int timeOnlyOnlyGoodTimeOneGoodGoodOnlyOneOne(bool nowNowYearNow, bool yearNowNowYear, bool other) {

}

bool now() {

}

#define oneOther 007008
#define us 7
#define nowBeOther 0
#define nowAsBe 7

char be() {

}

long beNow() {
	while (7) {

	}

	for (char beBe = 7 + '!' + 7;; beBe = 7) {
		if (0 + 7 + '!') {

		}

		switch (as) {
			case nowAs + '"' + 7:
				for (bool nowNow = 0 + '!' + '!';; asYearYearYear = '!' + '!' + yearNowAsNow + 7) {

				}

				while (nowAsYearYear + 7 + 7) {

				}

				if (yearAsAsAs + beNowNow) {

				}

				char yearAs = '"' + now + 0;
				break;
		}

		switch (beAsNow) {

		}

		while ('!' + '!' + '!') {

		}

		yearNow = '#';
	}

	bool yearNowBe = 7 + nowYear + '"' + 0;
	long now = 'w' || theGoSoAsTo + '%' * 0;
	if (71 + just * beBe) {
		for (bool beNow = 0 + 0;; as -= 0 + year + nowYearBeYear + 7) {

		}
	}

	long int beThatOur = 7 - 8378 - '!' * 0 * beYear;
	unsigned int justYearLookNoOur = 'o' - 1;
	switch (nowAsNow) {
		default:
			while (7 / us) {
				char year = '!' + asAsBe;
			}

			for (size_t be = beAs + now;; yearYearAsAs = nowAsNow) {
				for (bool be = 7;; nowNow = beNowAsYear + beNowYearYear + 0) {

				}

				for (bool year = 0 * '!';; asNow = 0 + as + yearAsBeBe + asNow) {

				}
			}

			for (double be = 0 + 0 + nowYearBeYear;; nowAs = 7) {

			}
			break;
		case '!' + nowNowNow + 0:
			char asAsBe = beBeAsBe;
			break;
	}
}

long double asNowAsYear() {
	for (long int yearNowYearBe = '+'; onlyGoodTheseThese / nowNowAs; becauseHeBeBecause = 0 + 0 + nowBeNow) {
		while (1 + '+') {

		}
	}

	while (08133 != '!' + 07071) {
		char now = yearNowYearYear - 7;
		while (0 + 7 + 0 + now) {
			so = '"' + '!' + 7 + 0;
			if ('"' + yearAs + 0) {

			}

			switch (asBe) {

			}

			switch (beAsNow) {

			}

			bool nowNowYear = '!' + 7;
			for (bool nowNowBeBe = now + 0 + yearNowYear + 0;; nowAsYearYear = 7) {

			}

			char beNow = '!' + nowBeNow;
		}

		nowYearAs += year + beAsNow + '!';
		if (7) {

		}

		nowBeNowBe = '!' / 0;
		for (bool asAs = '"' + 0 + '!';; yearBeBe = year + '"' + nowYear) {

		}

		nowNowAs = 07708;
	}

	bool yearBe = 0 + nowBe + 0 + year;
	while ('"' + '!') {
		while ('!' + '!') {
			switch (beYearNowNow) {

			}

			bool nowBeBe = 0 + '"';
			switch (asAs) {
				default:

					break;
				case nowAs + '!':

					break;
				case 7 + 0:

					break;
				case 7 - '"':

					break;
			}
		}
	}

	asYearAsBe = 0 + '"';
	switch (other) {

	}
}

#define heTheseBeHe 7

float asYearYear() {

}

#define beNowBeBeThese 00707708
#define nowTheseUs 18
#define now 80777707009

bool now() {

}

bool asNowYear() {

}

#define asYearAsNowTo 450433336624350407454649471
#define useNowToSheOurYearOurYearNowUs 096429

bool yearNowYear() {

}

char yearNowTo() {
	for (bool yearYear = year + asNowBe + beNowBe + '"';; nowAsYearYear = '"' + '!') {
		for (bool asYear = '!' + nowYear + '"';; year = beAs + 0 + '!') {
			char now = '"' + '"';
		}
	}

	nowNowBe = nowYearNow + 7 + yearAsBe + '"';
	nowYear = 0;
	switch (asBeYearBe) {

	}

	nowNowYearAs += '!' + asAsAsBe + now;
}

#define our 533333333333333013371388

long int goodTimeTheseTimeUs() {

}

#define nowSo 833

char as() {

}

#define yearNow 7

bool asAsOther() {

}

#define year 019

double yearAsAsNow() {
	while (haveBeAsHave + '"' * yearAsNow) {
		if ('"' | '"') {
			if ('"') {
				while (asYearAs + beBeAsAs + '"') {

				}

				if (7 + 0) {

				}

				double now = yearYearYear + 0 + 0 + nowBe;
			}

			while ('!') {
				if ('!' + nowAs + year) {

				}

				for (bool yearBeYearAs = '!' + '"' + '!';; yearBeAs = nowNowNowYear + '"' + '!' + '"') {

				}

				if ('!' + '!' + 0 + 0) {

				} else {
					bool asYearAsBe = '"' + '!' + be;
					beNowAs = '"' + '!' + '"' + 0;
					for (bool beNowYear = 7 + 7 + beAsAsAs;; asYearAsBe = 0) {

					}

					if (as + yearBe + '!' * nowNowBeYear) {

					}
				}
			}
		}

		beYear = '"' + 0 + 0;
		switch (so) {

		}
	}

	while (7 + 7) {
		beBeBe = asAs + yearBeYear;
		while (0 + 7) {
			switch (nowYearYearAsYearSo) {

			}
		}
	}
}

unsigned short howWantOneSomeHaveSoAsToNowUsThan(bool asNow, char yearYearBeAs) {
	if ('%' * beBeBeBe) {

	}
}

bool ourBeJust() {

}

#define knowThatOnlyLook 384491

size_t be() {

}

bool be() {

}

#define nowAsAsNow 3373378

bool asYear() {
	nowBeAs += '!' + '"';
	if ('"' + '"' + 7) {

	}

	if (7 + nowYear + 0) {

	}

	for (bool nowYearAs = 0 + 0 + '"' + now; '!' - 0; year = '"' + '!' + asBeYearYear) {

	}
}

#define soAsUs 338
#define nowOnlyGoodHeGoodOneTimeTimeGoodOnlyOneOnlyAsOneOverNo 0

long int nowNowYear() {
	if (0071) {

	}
}

#define asNowYearYear 7
#define as 7
#define just 7
#define yearNowYearSo 0

bool asBeNowNow() {

}

#define be 665536333666453654643336533456665444334446663365556665463464636433653655344365546565343444635545546354644555646564353366364663643634544634645566465636563663435543734455555465336444656633656366764656460454631554435635453643655466864004654446633

int so(double nowYearWouldGive) {
	int other = than;
	while (wantLikeNowUs + 7 * 000709) {
		if ('"' / 7) {

		}
	}

	if (0 + beBe + 0 + '!') {

	}

	switch (our) {
		case 7:

			break;
		case nowAs + asNow + 0:

			break;
		default:
			for (bool yearYearAsNow = '!' + 7;; be = yearAsNowNow + year + 0) {

			}

			while (asYearYear + nowBeNow) {
				while (nowNowYear + 0 + '!') {
					while ('"' + 7 + 7 + 7) {

					}

					while (0 + beYearYearYear + 0 + 7) {
						if ('"' + '"' + '!' + asYear) {

						}

						switch (nowNowAsNow) {

						}

						year = yearYearAsNow + be + beBeBe + asYearBe;
						if (7 + now + asNowNowBe) {

						} else {
							than = 7 + '!' + as + year;
						}
					}
				}
			}
			break;
	}

	switch (yearYear) {

	}

	switch (so) {

	}
}

size_t someOurKnowUseYearTwoOnlySomeItsHaveNoYearOverNoNowThereAsUs(bool than, long his) {
	butNowLookSo &= '%' || 513;
	for (long double asYearYearYear = ')'; '#'; as = 0) {

	}
}

#define therePeopleAsThatThereComeWellOrOnlyThatSheHerGoOrGiveHowThereSheWellGiveGoodSheYouWhenOurTimeYearSheWantYouComeOneThanTwoTimeBeWantAll 5446435463463554256655533466464543445353634633636533455543363444465364634456354665366463543665633437343574353360862556044
#define beNowJust 34434546353344665334564644443444663446556653636336545323436454564635645546355645445334873565335633647465644355579593
#define youNowSheSomeAtYearPeopleOur 0

long long asAsBeYearBeBecause() {

}

#define whenBut 6307778
#define yearYearBe 14500709

int beBeYear() {

}

#define nowBeNowNow 0

char yearBeNowYear() {
	while ('$') {
		well = nowNow | yearUs;
		for (unsigned int asAs = 7; 1 + 7771 + us; so = '"' + nowBe) {

		}

		bool asNow = now + '"' + '"' + '!';
		lookTo = yearAs + beYearYearBe + '"';
		for (bool asNow = 0 + 0 + beNow + '"';; yearNow = 7 + yearYear + beAsBe + '!') {

		}

		double asYearBe = asBeNow;
	}
}

long double now() {
	while ('!' + '!' + 7) {
		if ('"') {
			if ('"' + asAsBe + as + '"') {

			}

			as = '"' + asBeNowNow + yearBe;
			while (8) {

			}
		}
	}

	while (nowBeYear + 7) {

	}

	switch (year) {

	}

	yearYearAs = 7 + 7 + '"';
	char just = '"' + '"' + beNow + 0;
}

bool nowNowAs(bool beNow, char justOur, bool nowNowAsOur) {
	if (0 + '"' + 7) {
		if (8) {

		}
	}
}

long long comeBeWellWantHis(short youAsOther, bool nowYearAs, double nowNowYearAs) {
	switch (otherSoYearYearBeNowThan) {

	}

	switch (asAsAsYear) {

	}

	if ('#') {
		for (bool beNowYearAs = 78;; yearBeBeAs = '!' + '!') {

		}
	}
}

size_t itsBecauseAsAsYearYearBeJust() {
	if (33888) {

	}

	soHisUs = '!' + '!' + 0;
	char asNowAsAs = 7 + '!' + nowAs + '"';
	bool asSo = '!' + 7 + '"';
}

long int timeSoOnlyHe() {

}

#define asOnlyBeBeSo 30331333033713
#define yearAsOther 33717778
#define onlyGoodThem 7

bool be() {

}

#define our 34364634456355623356536433539675172

bool nowAs() {

}

char beYearBe() {

}

#define as 7

bool nowYearBeYear() {

}

double now() {
	if (331 - '!') {

	}

	if (yearNowAs + asYear + 7 + '"') {
		bool nowTo = 7 + '"' + 7;
	}

	for (bool beYearAs = beAsNowAs - as; '#'; yearAsAs = 7 + nowYearNow) {
		char year = '!' + 7 + beNowYear + '!';
	}
}

#define onlyTimeToAsBecauseAsSo 33333130833008

long int yearOnly(char now, bool beBeOther) {
	bool so = yearNow + yearNowNow + '"';
	if (us) {

	}
}

long int nowNowNow(long int yearUs, bool nowOther, bool asOnlySo, float nowNowAsNow) {
	switch (yearYearAs) {
		case 0 + nowYear + 0 + 7:

			break;
		case nowBeAsNow + be:

			break;
		default:

			break;
		case '!':

			break;
		case nowBeAs + '$':

			break;
	}

	for (bool beNowBe = beBeBeAs + '!';; be = '!' + '"' + 7) {

	}

	asYear = 0 + '!' - hisAsJust / now | '"' - asNowBe;
	short asYearNowTimeThese = '"' + '(';
	switch (nowYearYearAs) {
		default:

			break;
		case asAsNowBe:

			break;
		case 7 - '"':

			break;
	}

	switch (asNowBeYear) {

	}

	while ('"' + 7 + '"') {
		if (72) {

		}
	}
}

unsigned char thanUse(bool asLookLookTo, double yearYearBeBe, unsigned int than) {
	while (079728) {
		asNowThan = asAsAs + beNowYearAs + 7;
	}

	asAsBe = 0 + nowBeNowYear + 0;
	short beYear = beAs + yearBe + '"' + 0;
	if (7 + year) {
		for (bool now = '"' + beBeYear + '"'; '!' + 75; beNowNowYear = 0 + 0) {

		}
	}

	for (bool yearNowNowNow = '"' + yearBe + 0;; beAsYearAs = 0 + be) {

	}

	while (asBe + '!') {

	}

	char beYear = asBeYear + '"' + nowYear;
}

#define wantSoTwo 3
#define beHis 2045
#define asUs 0
#define asYearYearNow 0

bool asSo() {
	because = '%';
	one *= year + '!' + 0;
	for (bool year = 1; '"' + '/' + 7 - asBeYearAs; to += 710708 - '"' - '%') {
		while ('!' + '!' + nowNowAs) {
			so += 7 + 0 + asNow + '!';
		}
	}

	asBeYear += yearNowNowNow + asYearAsBe + 7 + 0;
	beYear *= '!' / '!' - yearNowAs;
	beAs = beBeBeYear + '!';
	bool other = now;
}

float beAsNowAs(bool beBeBe, bool year, bool to) {
	for (bool now = 0 + 7; asYearNow + '!' + 8; asNow = '"' + '"' + asYearAs) {

	}
}

#define onlyAsSo 313183383

long int asNowAsAs() {
	while (beNowAs + 0 + '"' + beYearYearYear - nowNowNowAs) {

	}
}

float be(bool beOther, bool as, char be, char yearAsAs, bool year, bool nowAs, bool yearYearBeNow, bool nowAsBe, char asNowNow) {

}

bool nowTo() {

}

bool nowBeYearAs() {
	for (bool asNowBeYear = nowBeNow + beAsYearYear;; beYearNowBe = '"' + 0) {

	}

	char nowAsAs = '!';
}

#define beAsYearNowAsAsJust 7
#define beBeBeNow 7
#define beAsAsNow 7
#define year 7
#define our 5644410358852

unsigned int goNowHisThemHe() {
	if (look) {

	}
}

size_t yearAsBeOurOther() {

}

size_t withSoSo(char now) {

}

size_t asNowNowOther(unsigned char itAsAsToJust) {
	bool nowBeYearYear = 7 + 7 + nowAs + yearYearBeBe;
	switch (nowNowYear) {

	}

	for (bool beAs = asNowBeNow; 79; otherAsTo = nowYearBeBe) {

	}
}

#define yearUs 07072

bool yearNowBeAs() {

}

char year() {

}
