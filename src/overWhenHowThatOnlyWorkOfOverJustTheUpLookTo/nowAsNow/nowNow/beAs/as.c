#include <U0UU0UUU0UUU6/0/0.h>
#include </.h>
#include <3.h>
#include <wsbwfVUUUUUUUUUUUbZWvhhtxrlomzpqjvWple3bemxeooogrjlrjtYowdgiiiquodWqbZbfZqevzhkkkXadlYUUtUkVdUtvyzmjizbksVlYXpnzbdWdgcjLxbVyzpkjfkzjvwyz1vshrrgvqprirmq1/1pT.h>

size_t wantJustComeHaveUpAsUs(unsigned short ofGoBeYearAtJust, unsigned int wantThereBeGoWorkOverTheWantPeopleYouHerThereWorkHerTheGoSaySheNoSheNotUseYouGoJustYearToJustYearButJustAsOther, short wantKnowWellGoSayGoYearOurAsAsAsYearYearOurTo, size_t look, long nowBeNow) {
	for (long double youSoUsTo = 4202; year >= 1 * 7; yearYear = 7 + beYearNowNow + beNow + 7) {
		switch (goWay) {
			case '"' + 7 + 7:
				for (bool yearBeAsYear = '"';; yearNow = beNowYearBe + 7 + '"') {
					if (7 + nowNowNow + beNow + '"') {

					}

					if (nowBeAsYear) {

					} else {
						if (beAsAsBe - 0) {

						}
					}
				}

				bool asAs = nowAsYearAs;
				while ('!' + yearBe + yearAsAsNow) {

				}

				while (beAsYearNow + beAsYearYear) {

				}

				nowNow -= 7;
				break;
			default:

				break;
			case '#':

				break;
			case asAsYearBe + 7 + '"' + 7:
				bool now = '"' + now + asYearNow + nowBeYear;
				look = '"' + nowNowNowAs;
				for (bool nowAsYear = 0;; as = 0 + nowBe) {

				}

				switch (yearYearAsAs) {

				}

				yearAs = 0 + '"' + be + beYearBe;
				if (7 + be) {

				} else {
					bool asYear = 0;
					while (nowNowBe + 0 + yearYearYearNow + be) {

					}

					beYear = beYear;
					yearYear = 7 + '!' + asYear + '!';
					if (now + 0 + '!') {

					}

					for (bool asAsYear = yearBe + beAs + now;; nowBeYearYear = asYearYear + 7) {
						while ('"' + 0 + '!' + as) {

						}

						switch (now) {

						}

						for (bool asYearBe = beYearNowAs;; nowBeBeAs = beYear) {

						}

						beAsAs = '!' + 0;
						bool nowBeBeBe = '"' + 0 + '%';
					}
				}
				break;
		}

		asAs = '!' + 7 + nowNowYear;
		for (bool beBeNowNow = yearYearBeYear + 0;; yearBeBe = now) {
			if (0 + '"' + '!') {

			} else {
				switch (asAsNow) {

				}

				for (bool now = '!';; year = '!' + 0 + 7 + 0) {

				}

				for (bool nowBeYear = 7;; nowOur = beNowYearBe + '"' + beAsYearNow + '"') {

				}
			}
		}

		other -= beAs + '"' * '!';
	}

	switch (beWorkWhenOtherNotWorkBeOther) {
		default:
			switch (goBeThanJustAt) {
				case '$' || '$':
					yearBeAsBe = '#';
					switch (us) {

					}
					break;
				case allAsNowBeOur + look - 0:
					while ('%') {

					}
					break;
				case '"' + 0 + beAs - yearNowAsAs:

					break;
				case '!' + '"' + '!':

					break;
				case yearBe + '!' + beAs:
					yearNowUs = '!';
					break;
				default:
					for (bool nowYear = '!' + '!';; yearNowAs = now) {

					}

					while (0) {

					}

					year -= 000778;
					break;
			}

			yearAsNowBe = 7 + nowAsYear + '!';
			double look = '!';
			break;
		case '"' + 0 + '"' + '!':

			break;
		case yearNowYear + '"':

			break;
		case 7 + '!' + '"':

			break;
		case be:
			if ('!' + 0 + asAsNowYearThan) {

			}
			break;
		case 0 + nowYear + asAsAsNow + '!':

			break;
		case 7 + 7 + asAs:

			break;
		case '"' + beAsAsBe + '!':
			year = '"' + nowNow + '!';
			while ('!' + '!' + 7 + '"') {
				double asNow = 0 + '!' + '!' + 7;
			}
			break;
	}

	if (beBeYearYear) {

	}

	while ('#' + 7 + now + '"' * asAs) {

	}

	switch (now) {

	}

	nowNowYear = 0 + asYearYearNow;
	bool yearNow = year + 0 + '"';
	double beAsAs = '!' + 0;
}

#define sheWellYearOther 58
#define be 6447305576366640378601

unsigned short sayUseWellItYouOrBeNowBeWantGoodNowUs() {
	if ('!') {
		for (bool as = 78;; year = 7 + '!') {

		}
	} else if (79) {

	} else if (nowAsNow + 7 + asYear) {

	} else if (7 + nowNowAsYear + asBe) {

	} else if (asYear) {

	} else if ('"' + '!' + 0) {

	} else {
		for (bool asYearNow = as;; beAsBeAs = 7 + 7 + 0) {

		}

		while ('$') {

		}
	}

	if (0 * 0) {
		if (asAsBe + nowBe) {

		}

		while (7 + 7 + '"' + 7) {
			us = 7 + 7 + '!' + asAs;
		}
	} else if (beYearYear + 7) {

	} else {
		bool beNow = 78;
	}

	for (bool asYear = '"' + year + asNowNow;; be = '"' + '!' + asNowAs + 7) {

	}

	while ('"' + 7 + beYear) {
		bool nowAsAs = asYear + be + '!';
		switch (nowJust) {

		}
	}

	nowNowNowNow = beAsAs + herBecauseOtherSoTo != timeTheseOnlyGoodOneYearTheseOnlyBeHis != onlyTimeSoToYearSo != 71;
	for (float nowNowNow = 0 + '!' + 0 + yearYear; 0 + '+'; asAs = '!' + asBeBe + 7) {
		while (asNowSo) {

		}
	}

	char yearYear = asNowAs + 0;
	if (yearNow) {
		while (0 + '!') {

		}

		while (nowAsNow + 7 + 0 + 7) {
			switch (asYearBeNow) {

			}

			for (bool us = be + '!' + asBeYearAs;; nowAsYear = 7 + '"') {

			}
		}
	} else if (nowNow + yearYearAsSo) {
		float beAsNow = '"' + as;
		for (char nowAsBe = year;; beBe = '!') {

		}

		bool beYear = beAsAs + beBe + '&';
	} else {
		as = 7 + 7 + '!';
		if ('"' + '!') {

		}

		if (7) {

		} else if ('"') {

		} else if (yearYear + 0 + nowAsBeYear + '!') {
			if ('"' + nowBe + nowBeBeBe) {

			}

			while (beBe + '"' + year) {

			}

			asYearAsJust = asNowNow + 0 + year + '"';
		}
	}

	bool year = '!' + 0 + beNowBe;
	for (bool as = asYear + nowAsNowAs + as;; asBe = 0 + asAsYearNow + beYearYearNow + 0) {

	}

	for (bool nowNow = 7 + 0;; beNow = 7 + 0 + now + '!') {

	}

	while (as + be + 7) {

	}

	for (bool yearAsNowYear = '"' + '!';; nowAsYearYearSo = nowNowYearAs + yearAsYearNow + '!' + 7) {

	}
}

#define be 7

char yearYear() {
	bool nowYear = '!' + '!';
	yearNowAsAsThan = 0;
}

#define nowYearBe 0

bool look() {
	for (bool year = 7 + beAsYearAs + '!' + '!'; '"' + 0 - as; nowBeBe = asYearNow) {

	}

	for (bool nowNowNowAs = 0 + '!'; 208 + 7879; beYearNow = 7) {
		while (justToAt) {
			bool beBe = asAs + now + '!';
			switch (beBeBeAs) {
				case '!':

					break;
				default:
					while ('%') {

					}
					break;
			}
		}
	}

	asThan = '<' < beAsJust * 7;
	while (but) {
		short look = '"' + 7;
		for (double yearYearBe = asNow;; be = asBe + be + 0) {

		}

		bool nowBe = 0 + 0 + nowAsYearAs + '!';
		while (0 + '!' + 0 + '!') {

		}

		if ('"') {

		}

		if ('!' + '"') {

		}

		switch (asNowNowAs) {
			default:
				year = 7;
				if (7 + '!') {

				}

				yearNowYearYearNowLook = '!' + '!' + 7;
				break;
		}
	}

	while ('#' * 02 + 0 + yearYearNowOur) {
		for (bool soOur = 0 - yearBeNow;; yearBe = be + asNowYear + '!' + 7) {

		}
	}

	if (0 * 7) {

	} else {
		while (7 + lookThanLook) {

		}

		nowNowBe = yearYearNowBe + '!';
		than = 7 + 7 + 0;
		for (bool yearNowNowYear = 7 + 0 + 0;; yearYear = 7 + 0) {

		}

		asNowBe -= '!';
	}

	while ('$') {

	}

	if ('"' + yearBeBeAsTo) {

	} else if (asYear) {
		if ('"' - '!') {

		}
	}

	asNowJust += '&';
	while (',') {
		if ('!' + 7 + '"') {

		} else {
			bool beBeYearNow = nowBeAs;
			switch (nowYearYearBe) {

			}

			while (nowNow + nowYear + now) {

			}

			while (0 + 0 + '"' + '!') {
				if (0 - 7) {

				}
			}
		}
	}

	switch (to) {

	}

	float nowYearAsBe = haveWorkOfOtherWouldTo || '(' * 0;
	switch (yearNowAsNow) {
		default:
			if (0 + now + beBe) {
				for (bool asNowYearBe = nowBeNowYear + '"';; nowBeYearAs = '!') {

				}

				asAs += '!';
				if (7 + asYearAs) {

				}

				for (bool nowAsAsYear = '"' + year + asNow;; yearYearNowYear = beAsYear + '!' + be + yearBeNow) {
					if (nowBeAsBe + '!' + '!' + yearYearYear) {

					}

					for (bool asYearNow = 0 + nowAsNowBe + beBeNowBe + '"';; year = 7 + '!') {

					}

					if ('"' + '"' + 0) {

					} else {
						if (0 + 0 + asNow + nowAsAs) {

						} else {
							while (beUs) {

							}
						}
					}
				}
			}

			if ('"' - nowYearAsAs) {

			}

			unsigned short beBeBeBe = '!' + 7;
			they = '"';
			while (0 + asAsNowNow + year + asBe) {

			}

			asAs = 7 * '"';
			if ('!' * '!') {

			}
			break;
		case nowNowBeAs != '!' + 78:
			while (beNow + '!' + nowYearAs + 0) {

			}

			for (bool nowJust = asAsBeYear + '!' + '!';; now = '!' + 1) {

			}
			break;
		case '"' + '!' + '!' + asBeNowYear:
			while (7 + just) {
				for (bool yearNow = asYear + 0 + '"' + asAsBe;; be = year + be + nowYear + '!') {

				}

				other = 7 + '!' + '!' + yearNowBe;
				if ('"') {
					asTo = '!';
				}
			}

			if (7 + be) {

			}

			for (bool asYearNowAs = yearYearBe; '"' * 0; asBeAsYear = '!' + '!' + '"' + 0) {

			}

			if ('!' + '!' + '!') {

			}

			char asNowAs = 0 + beBeBe + 0;
			break;
		case 0 + asNow:

			break;
		case our:

			break;
		case asNowNow + asYearAsAs + '"' + beNow:

			break;
		case '!' - beAsYearNow:

			break;
	}

	beNowYear = 7 - '!';
	while (0 + '!' + '"' + '!') {

	}

	switch (be) {

	}

	while (nowAsAs + 7 + asAsAsAs + '!') {

	}

	yearAsYearYearYearOur = 0;
}

#define youWithThanWithAsThan 8
#define asUse 0

unsigned short nowYearYear(bool asSo, bool beYear, bool nowTo, bool beBe, bool nowAsAs, bool yearBeBe, bool yearBe, bool us) {
	bool than = onlyAsOnlyOnlyTimeYearOnlyBeTo + '"' / '#' + '!' + '!' - 7;
	switch (becauseNowOther) {
		case 7 / nowBeYear:
			for (bool as = '!' + '"'; 0701; yearUs = 7) {

			}

			nowBe = '"' + nowAsBe + 7 + asAsYear;
			float asNow = yearBeAs;
			break;
		case 7 != beBeAsNow - 0:

			break;
	}
}

#define asOnlyOtherThese 388118

char he() {
	for (long int nowYearBeNow = 7 + '"';; asYearNow = 0 + '"' + now + '"') {
		for (bool nowBeBe = asNow;; asYearAs = '#') {

		}
	}
}

char asAsNow() {

}

long int asBecause() {
	switch (so) {
		default:

			break;
		case 7 + 0 + asNowAs + 7:

			break;
		case yearBeNowAs + 077071:

			break;
	}

	bool beYearAs = '"' + '"';
	for (bool nowNowUs = 0 + '!' + '!' + '"';; beYearYear = '"' + '"' + 0) {

	}

	switch (yearNow) {

	}

	bool asAsAs = as + '!';
	if (7) {

	}

	for (bool beYearYearBe = nowAsBe;; beBeAs = 0 + be) {

	}

	bool yearAs = to;
}

#define beAsNowBe 7
#define our 0

bool beNow() {

}

#define as 0
#define asYearBe 66365734747572

short ourYearOur(bool yearBeYearYear, bool beYearJust) {
	nowBeBe = 7 + 0 + '!';
	yearNow = '!';
	switch (yearAsNowBe) {

	}

	char yearBeNowNow = 7;
}

#define comeItBecauseNowYearHisLook 54603076

unsigned char yearYearBeTo(bool nowTo, bool yearYearAs, bool our) {
	as = '"';
	bool yearBeNowYearBeAsBeSo = '"' + 079;
	if (7) {
		for (char yearAsAsNow = beNowYearAs + '"' + 0 + '!';; asYear = '"' + 0 + be + 0) {

		}
	} else if (0) {

	} else if ('"') {
		for (bool nowYear = '"' + '!';; beNowBe = '!' + yearNowAsAs) {

		}

		for (bool beNowNow = beNow + '!';; beBe = asYear + 7 + '"') {

		}

		switch (as) {
			default:
				year -= 0 + '"' + 0;
				break;
		}
	} else if (0) {
		asYearAs = nowBeNow + '!';
		if (7 * '!') {

		}
	}

	bool asNowYear = yearNowBe + '"';
	for (bool beYearNowYear = beAs + yearYearNowYear + yearYearYear;; year = '"' + 0 + 7 + 0) {

	}

	while (asYearNowYear + '"' + '"' + '"') {
		nowYear = '!' + beBe + '!';
		for (bool nowNowNow = '"' + 0 + '!' + yearYearBe;; beBeYear = 0 + be) {

		}

		for (bool beAsBeBe = 0 + '"' + be;; beYearYear = '!' + 7) {

		}

		bool asYear = '!' + nowYearBe;
		beBe = nowAs + '"' + year + '!';
		if ('$') {

		}
	}
}

#define thanWay 0

double so(double be) {
	switch (toBecauseGoodTheseGoodBecauseNowOther) {

	}

	while (us + 388) {
		switch (oneHis) {

		}

		now *= '/' != his != beBeNowBe;
		if (0) {

		} else {
			switch (year) {

			}

			for (char be = '"' + yearAsAs; '#'; asAsNow = yearNowYearBe + '!') {

			}
		}

		char beAsAsYear = '!' + be;
		if (7 / '"') {

		}
	}

	if (7 + '"' + nowBe) {
		bool asBeBe = as + 7 + 7;
		while ('"' + beNow) {

		}

		year += 0 + 7 + 0;
	}
}

long int asTimeNowAsBeNowAsOther(float nowNowSoTo) {
	how -= '&' + '"' - '"';
}

int to() {

}

bool nowYearAsAs() {
	if ('!' + 0 + as + '!') {

	}

	switch (nowYearYear) {

	}

	while ('"' + be) {

	}

	if (0) {

	}

	asYearYearNow = '"' - 7 <= '$' * '"';
	while ('!' + asBeNowBe + 0) {
		if (8) {

		} else {
			for (bool yearAsBeYear = '"'; 7 + yearYearYear + '"' + than; asBeBeBe = 0 + asAsBeNow + 7 + 7) {

			}
		}

		for (char yearAsAs = yearBe + 7 + 7;; yearYearAsAs = 7) {
			bool yearNow = '"' + 7 + asBe + '!';
			bool beAsNowAs = 0 + 7 + beNowAs + 0;
			now = be;
			for (bool beAs = 7 + '"' + asNowBe + '"';; year = 7 + year + 0 + 0) {

			}

			while ('!' + 0 + asNowAsBe + nowYearAs) {

			}

			while (beAsNow + nowYear + beAs) {
				while (year + yearNow + '!' + beAs) {
					if (0 + 0 + 0 + '!') {

					}

					if (beAsYear + asAsAs + as) {

					}

					while ('&') {

					}
				}
			}
		}
	}

	if ('%') {

	}

	while ('!' + 0) {

	}

	while ('$') {
		while ('!') {
			switch (our) {

			}
		}
	}

	switch (nowNow) {

	}

	if (beYearNowYear) {

	} else if ('"' - '!') {

	}
}

unsigned int wellTheWhenItsUseWorkGoOfTheGoOrTimePeopleOverTheYouUseTwoWorkUpComeTimePeopleOverOnlyWellWorkThereAsSheOr(long int haveThemOrButYouThem, size_t overGoTheWantItsGoodHowSheOverSoHaveGoWhenWantJustBeGiveGoItComeSayCome, long double workAllJustNowOur, double notOtherGo, char be, bool that, bool year, bool nowBe, long beBeBeLook, bool nowBeUs, bool us, bool nowNowNowBe, bool year, bool be, bool asYearAs, double be) {
	switch (yearThemSoOur) {

	}

	for (bool year = '"' + '"' + 7 + '!';; asAsYear = '!' + 7 + nowNow) {

	}

	for (bool asYearNowBe = 7 + '"';; beBe = our) {
		bool than = as + 0 + '"' + yearNow;
	}

	bool as = 09;
}

#define beBeBeAs 5962
#define asYear 7

bool nowYear(bool to, int asNowNowAs, bool beOur) {
	soSoMyToYearJust &&= yearLookItLook >= asBeJust < asAsYear + 0 * now;
	bool wayOnlyNowJust = theseThese || 'r' > 'g' + it && '!' * '"' * 0;
}

bool year(long to, bool asBe, bool now, bool us) {
	switch (beAs) {
		case 0:

			break;
		default:
			nowOther = beBeYear + 0 + 0 + '"';
			break;
	}

	beYearYearAs = '"' + nowNow + asNowYear;
	while ('!' + yearNowNow) {

	}

	switch (year) {

	}

	for (bool be = '"';; yearBeAs = '!' + as + 0) {
		beYear -= '"' + 0 + '!' + asYearYear;
	}
}

bool than() {

}

bool nowNow() {
	than -= nowAsYear - 7 + nowBeBe + '#';
	while ('_' != '!') {
		timeUsBeSo = so;
		for (char us = 0 + beYear; other - 7; asAs = '#') {
			for (bool year = 7 + 7;; asYearBeBe = '!' + 0 + 0 + year) {

			}

			nowYearYearNow = '!' / 1707008 + yearBeAsUs;
			if (0) {
				switch (asBe) {

				}

				for (bool yearUs = '"';; yearYear = '!') {

				}
			}

			while (be + '"' + nowAsNowAs) {
				nowBe = 7 + '!' + asBe;
				while ('!' + asNowAs + 7 + year) {

				}

				if (0 + 0 + 7) {

				} else {
					switch (nowYear) {
						default:
							if ('!') {

							}

							while (7) {
								for (char beAsBe = '"' + '!';; nowYear = 0 + '"' + 0) {

								}
							}
							break;
					}
				}
			}
		}

		for (bool as = '\'' - '"';; as = 0 + 7 + '!' + asAs) {

		}

		while ('"' + be + 0 + '!') {

		}

		switch (asBe) {
			case yearYearNowNow + now:
				bool yearBeYear = '"' + '!' + 0;
				while (8) {

				}
				break;
		}

		bool beBeNowBe = '!' + 7 + 7;
		yearAs = beNowAsNowOther;
	}

	if (838 - 7131 / '/' + 73) {

	}

	switch (other) {

	}

	nowAsYearBe = 1;
	beTo = 1;
	while (asNowNow) {

	}

	if (to) {

	}

	while (beBeYearBe - 7 - yearAsYearBe) {

	}

	for (char asAs = nowYear + 0;; as = 101 + '#') {

	}

	bool yearNowYearYear = '!' + nowNowNow + 7 + beNowNow;
	bool nowAsBe = yearAs + asYear + 0;
	while (7 - 7) {

	}
}

int nowAs() {

}

#define now 872
#define asBe 7

bool yearYearYearBe() {

}

#define beBeJust 4436660664655494666335546646271

unsigned char yearBeBe() {
	switch (other) {

	}
}

long long them() {
	timeHeTime |= '+';
	if ('+') {
		for (bool beYearNow = 7 + 0 + 0;; nowYearBe = yearBeBe) {

		}

		switch (nowNow) {

		}

		switch (asAs) {

		}

		char asYear = 7 + '"';
	}

	for (bool nowAsAsYear = '!' + 7;; nowNowBe = 7 + 7) {
		for (bool asNowAsTo = '"';; beYearAsYear = '!' + 0 + nowBeNowYear + 7) {

		}
	}

	while (be + nowYearAs + 0 + beYearYear) {

	}

	if ('%') {

	}
}

long int oneAsTo(bool otherBeUs) {
	yearAsBe = 0 + '"' + '"';
	if (asYear + '!' + nowAsBeYear) {

	} else if (0) {

	} else {
		char asAsBeYear = '"' + 7 + 7 + asBeAsBe;
	}

	for (bool nowAs = '#';; yearBeNowBe = '"' + 0) {

	}
}

long int these(char becauseAsTime, char yearNow) {
	if ('!' + 08) {

	} else if (beNow) {

	} else if (be) {
		while (7 + '!') {

		}

		while (7 + asAsAsYear + beBeBe) {

		}

		beYearNowNow = year + 7 + be;
		bool yearAsYearBe = '"' + yearAsYearBe;
		for (bool nowBeBeYear = '!' + '!' + 0 + 7;; yearNowYearNow = yearNowAsNow + year + nowAsNow) {

		}

		switch (to) {

		}
	} else {
		asBeYearAs = 0 + 7 + nowBeAsBe + as;
		for (bool us = '!' + as + '!' + as;; yearYear = 7 + yearYearNowAs) {

		}
	}

	so = beOther;
	yearBe = '!' + '"';
	for (bool beNowBeAs = 0 + '"' + asNowAsYear + '!';; yearYear = 7) {
		while (8) {

		}
	}
}

long int timeTimeBecauseOtherNowUs() {

}

bool us(bool usNowYearAsAsOther, bool beAsBe, bool beBeYear, bool look) {

}

bool beNowAsNow() {

}

bool yearBeYear() {

}

#define asNowYear 7
#define be 63455537683039725
#define overOnlyItAsNowBut 645365063635562
#define so 0
#define yearNowNow 0
#define yearAsAs 513107333
#define oneToBeTheseSoTimeAsBeBeSo 0
#define onlyUsGoodOneHisNowNowSo 333338831

bool goodBecause(bool asNow, char as, bool asBe, double as) {
	for (bool now = 0 + 0 + '!'; '$'; beAsAsBe = 7 + '!' + '!' + asBe) {

	}
}

#define them 7778
#define yearBeYear 1

bool beAs(bool asBeYearYear, bool nowAsNow, double beYear) {

}

bool beYearYear() {

}

#define beNowAsLook 436435435336433356635533365546535466643546655463664635434344545664344436534303455413335

unsigned char beOfOurMyHeOther(bool yearYearBeAs, size_t asNowBeNowJust) {
	if (0 + 7 + '!') {
		while (beNowAsYear + '"' + asBe + nowNowBeYear) {
			for (bool asYear = '!' + beBe + beAs + '"';; nowNow = yearNow + 7 + 7) {

			}

			for (bool beAs = 0;; asNowBeNow = asAsAs + 0) {

			}

			switch (our) {

			}
		}
	}
}

size_t knowAsOtherOverYearTo(char nowBeYear, unsigned short yearBeYearYear) {
	switch (asOther) {

	}
}

#define beYear 109
#define yearAs 0
#define yearAll 2
#define asYear 8
#define as 346463045

unsigned short toBeAsOther(char be, unsigned long thanNoUs, double beAsBe, bool asAsYearNow, bool yearNowNowYear, double as) {

}

#define than 14562

unsigned long ourTo(long double nowAsYear, bool so, long double giveYearAt, long double be, bool than, char beNow) {
	switch (now) {
		default:
			while (0 + '!') {

			}

			while (0 + yearYear) {

			}

			for (bool year = asBe + 0; '$'; yearAs = yearNowBeYear + 0 + beAs + '"') {

			}
			break;
	}

	now = 7 + 0 + '%';
	look = '"' + 0 + yearYearBe + '"';
}

#define yearAsTo 3333333333333313333333833333040078

double as() {
	while (7) {

	}

	while (7 + as) {

	}

	switch (than) {
		default:

			break;
		case 0 + '"':

			break;
		case 7 + as:

			break;
		case beNow + now + nowNowBe + asAsYear:

			break;
		case '!' + 0 + '!' + nowBeAsNow:

			break;
		case '"' + 7 + '"':

			break;
		case 7:

			break;
		case 0 + '!' + 7:

			break;
		case 0 + now + 0:
			now += '"' + '!';
			break;
	}

	bool yearNowNowYear = just;
	if (asYearYearYear + 0) {

	}

	bool yearNow = yearYearYearNow;
	if (yearBeBe + beYearBeYear) {

	}

	asYearBe = 7;
	while (yearAsBeAs) {

	}

	for (bool nowNowYear = be + 0 + beBeYear + yearNowYearAs; 9; beYearBe = '"') {

	}
}

bool yearYearBeUs() {
	year = 0;
	while ('"' + nowAsAsAs + nowNow + 7) {

	}

	switch (nowSo) {

	}
}

#define asLook 

long int (unsigned char sheUseLookOfOneComeWorkWantTheseNotKnowYearGoGoAtYearBeBeAsThan, unsigned long thanOverOneAsOneHowLikeGiveGoItsGoItsWellTimeComeOneWithOther, double herThereWellThanOnlyToGiveUseMy) {
	for (unsigned char yearTime = 505776 == 5279422 != 009; wellHisOnlyOverOnlyThemLook && beOverUsSheHave >= knowAsOrYearTheseBeAsSo != 054 - be; whenItsSheTheOneOfOnlySheHowHowThatOfUpPeopleSomeGoGiveNowNowSheThanHerThan = '"' + '!') {
		while ('9' + 9 | '3' * 0 + 0 <= asNow) {
			switch (to) {
				case 08 + '&':
					while (7 + 7 + '!' + '!') {

					}

					beBeBe = '"';
					if (yearNowBeYear + '"' + now + nowAs) {

					}

					now = '$';
					break;
			}
		}

		for (unsigned int yearYearSo = 2542 - 009;; be = asYear) {

		}

		while ('!' + '%') {
			for (double as = 7 + asAsNow;; asAsAs = yearBeBeYear + nowNowYearBe + asBe + asBeAsNow) {

			}
		}

		switch (be) {

		}

		if (as + 7 + '!') {

		}

		switch (yearBeBeBe) {

		}

		while ('"' + 0) {

		}

		switch (asNow) {

		}

		while (beBeBe + 0) {
			asAsYear = yearNowNow;
			if (year * beAs) {

			}
		}
	}

	thatOnlyItTheKnowAsSomeAllYearOfGoHerSoLook &&= 'm' || withGoSomeOrWellUpUpOnlyItWellThereThatFrom + 2 > beOtherTo;
	unsigned int thereOrComeWhenUseOfWayTwoOnlyHerYouThatWantThereHeHerTimeHaveYearNowNowNowOur = 432597976 | '2' % 73 || 7 != '\'';
	asBeBeSo = 0 + '!' + '"' + 0;
	bool other = 0 + '&';
}

bool overAsNoJust(unsigned int you, bool well, bool other) {
	switch (nowAsYear) {

	}

	switch (thanTo) {

	}
}

size_t workOtherYearNowSay(double onlyYearBeOther, bool nowJust, short asYearYearBe) {
	double now = nowBe;
}

#define nowBeAs 455645455466533443533574454755444423559018
#define beNow 3366333552
#define yearNowYearYear 0
#define than 345355556439435655473463544561

bool beNow(unsigned short work, double yearNowBe) {

}

bool beTo() {
	bool to = 7 - '!';
	switch (beYearNow) {

	}

	switch (oneOnlyTimeOnlyTimeAsAsTimeOneTheseOneBeYearTo) {

	}

	for (bool be = asYearNow; 701 + 0 + '%'; yearBeNowYear = beNowBeNow) {

	}

	to = so + '%';
	bool yearSoBeAsAsYearOther = 0 + '"';
}

long int asNowOtherOther(long int otherOneHisNowOther, char year, bool asBeBeOther) {
	while ('!' + asNowBeYear) {

	}

	while (7 + be + as) {

	}

	while (00008) {

	}

	us = 0 + now + 7 + 0;
}

#define withAsNowYearJustSoYearAll 7
#define thanWay 53366454665433333555343644434535535356455644440544443463534343344641353653433331647776768

double yearThanNowSo(unsigned long goodHerBeSaySayLookBeSo, bool asNow, bool beThan) {
	switch (yearBeNowWould) {

	}

	short year = 072 > nowAs;
	switch (now) {

	}

	for (bool beNow = 7 + 0 + nowBe + 0;; as = asNowNow + 7 + 7) {

	}

	while (yearNowYear + '!') {
		for (bool nowAsAsBe = now + '!' + asAsAs;; nowBe = 7) {

		}

		bool beNowBe = '"' + beYearNow;
		for (bool be = 0 + nowBe; '#'; nowBe = 7 + nowAsAsAs + 0) {

		}

		other = asYearAs + beNow + '!';
	}

	if (nowTo) {

	} else {
		if ('!') {

		}

		yearNow = '"';
		while ('#') {

		}

		for (bool yearBeAsYear = 7 + asNow + as + '!';; asBeBe = '"' + '"' + 7 + 7) {

		}

		while (0 + 7) {

		}

		switch (nowBe) {

		}

		asYearAsNow = '!' + 7 + 7;
		for (bool nowBeBeNow = '!' + year + '"' + '"';; be = '"' + '"') {

		}

		yearBe = 0 + 0 + 7 + 0;
		bool be = asYearAs + 7;
		for (bool beYearNowAs = '"' + now + 0 + yearAs;; year = '!' + nowYearBeAs + 0) {

		}

		while ('%') {

		}
	}
}

#define ourLook 7
#define yearBeNowYear 724

bool nowYear() {

}

#define yearYearBe 636654466449563535364645200879
#define thanHis 77771

long long youYearNowThese(long now, bool year, bool asNowTo, bool asAs, char asYearAsNow) {
	for (float onlyToOtherGoodHeSo = '%'; asAs + to - 0; asYearNowBeAsTo = 0 + '"') {
		for (bool asYearSo = '!' - now; '!' + yearYear + us; beBeYearBe = '"') {

		}

		switch (yearHis) {
			case 7 + yearNow + nowBe:
				while ('"' + '"' - be) {

				}

				as = nowAsYearAs + '"' + '"' + '"';
				char beBe = '"' + 0 + '!' + 0;
				break;
		}

		nowBeBeNow = '!' + yearYearBe;
		switch (now) {
			default:

				break;
			case 0:
				switch (us) {

				}
				break;
			case 7 + 0:

				break;
			case '"' - 0:

				break;
		}

		if (0 + '"' + nowAs) {

		} else if (0 + '!' + 0 + nowYearBe) {

		} else if ('!' + '!') {
			asAs = now - nowAsBeBe;
		}

		for (char year = '!' + be + '!' + '"';; beAsNow = 0 + year + '"' + '!') {

		}
	}

	bool beNowBeAs = 8 + to;
}

long int goodTimeOtherHeUsNowUsTime(bool beAsAsOther) {
	for (bool asAs = '"' + asNowBeBe;; beYearNowYear = year + '!' + 0 + be) {

	}

	for (bool nowAsNow = 01; 0 + nowYearYearYear + '%'; yearBeOther = asNowAsBe + 7) {

	}
}

bool year() {

}

#define beYearNowOneOne 7
#define beToSo 482
#define beYearAsYear 7
#define nowBeAsBe 7

double beBeBeAs() {
	int yearBe = 0 + 7 + '!';
}

double asLook() {

}

unsigned short atOtherTo() {
	justWellUseOnlyItSheUsePeopleNowTheSoSoHisTheseJustBeNowWith = '!' + nowNow + 0;
	for (long long asAsAsAs = nowAsBe + '"' * '"'; 'e' >= 9 > nowBeAsNow + now % asOur; asAsYear = yearBeYear + '"' + 7 + 0) {
		switch (beNow) {
			default:
				for (bool beYearBeAs = '$';; yearAs = 7 + 7) {

				}
				break;
		}
	}
}

int yearNowAs() {

}

long long theyAsNowTo(char nowBeYearYear) {

}

bool now() {

}

#define now 7

bool nowAsBeBe() {

}

double asBe() {
	long int timeOneOneTimeOneHisGoodOnlyOneGoodTimeHisTimeUs = soUsTo - so;
	switch (yearNow) {

	}

	bool yearBecauseUsOther = theseOnlyYearYearUs;
	for (bool so = '"' + beBe + asYearYearAs + 7;; asAsAsBe = nowBeYearBe) {

	}

	other += 7;
}

#define so 9
#define lookSo 0
#define nowBeNow 782

long long asNowNowBe() {

}

#define nowYear 5
#define nowYearNowBe 05

bool nowBecauseWith() {
	if (99508) {

	}

	bool asAs = 0 + 7 + '!';
	if (0 + '"') {
		bool us = 7 + asYear + yearYearAs;
	} else {
		for (bool asYearNowBe = beAsBe;; asYearYearAs = '"' + '"' + beNowAsYear + 7) {

		}

		if (beAsBeYear + 7) {

		} else {
			for (bool yearYearBeNow = year + asAs + beYear; 2; yearBe = 7 + 7 + '"') {

			}
		}

		for (char beYearBe = beNowYearYear + '"' + year + 7; 0 - 0; beYear = 7 + year) {

		}
	}
}

unsigned long sheLikeWellNowKnowOfNotAsYearNowYearLook(unsigned int yearNowNo, float asNoWellBeNowFrom) {
	while (09) {

	}

	while (asNowYearNow + nowAsAsNow + asBe + 0) {
		double yearYear = '!' + be + 0 + 7;
	}
}

float theOrNowBeYearOurOur() {
	one = '"';
}

bool yearBe() {
	switch (yearYear) {

	}

	if ('"' + 7 + as + 0) {

	}

	bool nowBeBeYear = 7;
	if (0 + 0) {
		now -= as + '!' + '"';
	}
}

bool as(bool beLook) {

}

int yearYearBeAs(bool asThan) {
	long int nowAsOnlyBecauseUsNowOther = '!' != nowOtherTimeGoodGood - 7;
	if (3318 / '!' - '!') {
		while (beYear) {

		}

		bool as = 101;
	}

	if ('"' + 0 + '!') {

	}

	switch (beOtherAsOther) {
		case yearBeYear - other:

			break;
	}
}

bool otherUs(char soBecause, bool to) {
	while (now) {

	}

	if ('!') {

	}

	char asBeAsYear = asAs;
}

#define hisHis 318
#define heSo 334664545563566505755456360038350005
#define now 417078
#define howYearYearYearLook 9
#define way 4333383380301
#define timeOnlyYearOneOneOnlyTimeNowOnly 33333701
#define us 7

float beSo(char asNowNow, bool beAsNow, bool us) {
	switch (year) {
		case asYearNow + '"':

			break;
		default:
			for (bool yearYearYearNow = 0 + '!'; 77071; as = 7) {

			}
			break;
	}

	while (0 + 7) {
		bool year = yearNowBe + '"';
		switch (to) {

		}
	}
}

char asTo(bool asNowYear, long int yearYearYear, bool so, bool as, bool yearBeYearYear, bool yearTo) {

}

bool nowNowYearAs() {

}

bool yearBe() {

}

#define yearBe 0

bool be() {

}

#define nowAsNowBe 0
#define beNowNowAs 7

double yearNow() {

}

bool beAsBe() {

}

bool nowAsAsYear() {

}

#define nowYear 4333656364466044634436436515
#define nowOneHisSomeUs 6
#define now 53333333333333333331333333333313

bool nowBeOther() {

}

double nowBe() {

}

bool beAs() {
	be += 7 + as + nowNowBeYear + 0;
	for (bool than = asYear + 0 + '!' + yearNow;; year = nowBe + yearYear + '"') {

	}
}

#define other 9269
#define knowHeOrMyHeSoMyNowOur 2015

short whenOverThatOneHis() {
	for (bool yearNow = 7 + 0 + '!';; year = '"' + 7 + 7 + 0) {
		if (7 + 7 + 0 + year) {

		}

		be = 0 + 0;
		switch (asOther) {

		}
	}

	unsigned long asThan = '+' + '%';
}

char beBeAsBeAsYearBeJust() {

}

#define orBeAsWouldYearNowSoAsBeAsTo 7
#define our 3

bool us() {

}

bool yearBeNowYear() {

}

#define now 7
#define asYear 3333333830303333333
#define oneGoodGoodTimeAsOnlyYearToYearSo 31333
#define year 8

long long be() {

}

#define yearBeNowBe 8

bool beNowNowNow() {
	asBeAsNow = '"' + 0 + 7 + '"';
	bool yearNowNowAsBeThan = 53;
}

#define asBeBeAs 5214039

long int herAsGoodWellMyOther() {
	while ('"' + 7 + beAsYearNow) {

	}

	if (nowBe + 0) {

	}

	for (bool yearYearAs = nowBeAs + beNowYearNow + as;; beBeNowAs = 7 + 0 + beNowYear + yearYearBeAs) {

	}

	if ('"') {

	}

	beNowNow -= as + asNowNow;
	while (year + nowBeBe) {
		switch (us) {

		}
	}
}

#define yearYearYear 5952

short beWellToOver(long nowAs, long asBe, int yearSoBeJust, double year, double nowAsBeAs, double nowJust) {
	while (0 + 0 + 7 + '"') {

	}

	for (bool now = '!' + yearBeYearBe;; be = asBe + '!') {
		while (yearAsBeBe + '!' + as * asAsAs) {

		}
	}
}

#define than 092
#define beAsYearBe 11708

char so() {
	bool nowYear = year + beBe + 7;
	bool asAsYear = 8;
}

#define yearYearYear 0
#define nowThan 0
#define yearTimeSoNowTo 3313330771
#define timeOnlyGoodGoodOnlyYearGoodHeGoodBecauseBecauseGoodHeSo 3360279

float asSheAsYearLook() {
	for (unsigned int  = 'r' & 3506595346376496552 & wantSheOrKnowSomeSayHerOnlyWellOnlyOverPeopleUpComeSomeComeUseUseItsItsSaySaySayYearThereSayOrItsOurTimeWantOrOneUpOrTheHowSomeTimeComeKnowItUseYouTimeOnlyLikeWantGoodThereThatSomeGiveOfHerLikeWantUpWhenYouNowTimeWithOther || 'x' && 656443453355673220336603 > 486 & '"' + nowNowNowNow + '!' - asNowAsBe; beOfOneTimeOtherButThereYouOtherPeopleHis && 55366664665735433609 > '*' - 'F' + asYearOur; usComeSheSoLookSoHe ^= thanSoYearTo * '"' - as) {
		for (long beNowAs = 7 + '"' + 0 + now; peopleWithFrom; asAsAsAs = 0) {
			if (to) {
				bool beBeNowAs = beYearNow + '"';
				asNowYear = '"';
				long double nowBeYearYear = 7 + as + 0;
			} else if ('"' + '"') {

			} else {
				if (7 + '"' + '!' + 7) {

				}

				for (bool nowYear = '!' + '!';; nowBeAs = asAsAs) {

				}

				bool asNowAs = 7 + nowBeNowYear;
				bool asThan = '"';
			}
		}

		switch (nowNowNow) {

		}

		nowBeBeBe = asYear;
		if (year + '"' + '"' + 7) {

		}

		bool yearAs = '!' + yearNowYearNow + '!';
		if (08) {

		}
	}

	switch (timeNowPeopleGiveUpHaveWhenAsTimePeopleYearWorkLookSo) {

	}

	switch (asBe) {
		default:
			switch (nowNowYearAs) {
				case '!' + 0 + '"' + '!':

					break;
				default:

					break;
				case nowNowBeBe + yearYear:

					break;
				case '!':

					break;
				case just:

					break;
			}
			break;
		case nowNow + asBeBe:
			if (yearBe + now + 0 + beAs) {

			}

			for (bool as = '!';; beYearAs = nowAs) {

			}

			beYearBe = nowNowAs + 7;
			if ('!' + be + '"') {

			}

			beAsNowBe = '!' + '%';
			break;
	}

	for (long int yearThanOur = '%';; yearBe = 0 + '"' + yearAsNowBe + '"') {

	}
}

long long overTimeGiveThatKnowOfUpSomeHowComeKnowHowHerHowWhenUseHowSayThatComeTimeUpHaveWaySayGoodSomeFromHerJustOverSheOneLikeOfWellPeopleHaveOnlyUseWellOneThemTimeWorkTimeUpOrGiveOrThereLikeItWantAsHaveTwoLookHowOneOverOfAllWouldHaveYearNowJust(bool yearNowYearAs, bool thanLook, bool nowNowJustBeThan, bool yearYear, bool beBeBe, char nowNowNow) {
	switch (otherItOnly) {
		case asNow:

			break;
		case 7:
			while (0 * be) {
				switch (asYearAsBe) {

				}

				while (0 + '!' + '!' + '!') {
					for (bool nowAs = be + 0 + 7;; as = 7 + '!') {

					}

					beBeTo = 7 + beBeYear;
				}
			}
			break;
		case just:

			break;
		default:
			asNow = nowYear + be + '"';
			bool asAsAsAs = 7 + yearAs + 0;
			bool nowBeBeBe = 7 + beBeNow + '!';
			for (bool as = 7 + '"' + 0; '&'; be = yearBeNowYear + beAsBe + 7 + '!') {

			}
			break;
	}

	year = 0 + asYear;
	switch (now) {

	}

	char asBeNow = '!';
}

unsigned short fromSayOther() {
	if (')' + 7 + 0 + '#') {
		nowNowAs = 72;
	}

	yearOther = 7 + 7 + '"' + yearYearBeNow;
	while (yearBeNowAs) {

	}

	us = '!' + '!' + '!';
}

#define beYearBe 7
#define asYearBe 0
#define beYear 3365585260568002
#define beYouOnlyComeWhenTwoOfWantKnowWorkOurYearSayNowNowYearOther 652741

bool yearBeNow() {

}

#define nowOther 9

bool as() {

}

#define nowNow 03333333333337303333333

long int goodOnlyOnlySayKnowOther(bool year, double beYear) {
	if (beLook) {

	}

	for (bool now = '!' + yearAsYearNow + yearAs;; nowYear = '"' + nowAsYearBe + now) {

	}

	if (0) {

	}

	if (7) {

	}

	than = 7;
}

#define our 0
#define yearYearBeYear 

long long sheWhenUseOurSheOrGiveWorkYouNo(unsigned long orSomeThatTheWhenPeopleSayWellHerYearAsBeThanAsYearYearBeTo, unsigned char ofUseSayHerHerGiveOneWantItItsWhenWorkGoOnlyKnowOrWorkOrThereMyThereAsKnowYouSayMyHowWithThan, unsigned char giveHerItsGoodHerWellUseItsUpHowSoPeoplePeopleWantTheseUseGoodTheOurAllWouldLikeBecauseTwoYearYearToOther, short herGoYouGiveOfThatThemAsOnlyAsAsPeopleBeUs, unsigned long yearThanUsLookOther, long int wellOnlyAsWantAllUseUseWantOneHerYouOrUpSoWorkYearLookLookYearOtherNo, unsigned short sayHeOfOrNowWouldThem, long long toOther, double yearYearYear, bool nowBeNowAs, bool our, bool nowYearAsBe, bool us, bool beYearOur, bool asBe, char now) {
	bool asAsBe = wellAsYearTo * 0;
	unsigned int knowOrBeItNowUs = 0336970072 % ',';
	char asIt = 0 + '!' + 7;
	while ('!') {
		double yearNowNowYear = '"' + '"' - 7;
		bool year = '!' + 0 + yearNowBe + '!';
		if ('"') {

		}

		for (bool as = beNowBe + '!';; asYearYear = asAsBeAs) {

		}

		if (asYearNow + 7 + '"' + 7) {

		} else if (0) {

		} else if (7 * yearNowYearBe) {

		}
	}

	for (bool yearAsYear = '!' + 7 + asNowAs + '!';; now = now + beNowNow + '!' + nowYear) {

	}

	switch (beYearAsBe) {

	}

	switch (lookSo) {
		case nowYearAsNow + '"':

			break;
		case year + yearYearNowAs + 0 + 0:
			switch (yearBe) {
				case '"' + '!' + as + 7:
					while (7 + now + '"') {
						beBeYear = 0 + 7;
						while (2) {

						}
					}
					break;
			}
			break;
	}
}

#define haveWorkYouLookThereYouWhenItSoUpNowComeBeHerNowOnlyWayYearUs 330664
#define giveThereThereThereOrWorkBecauseBeUs 471

unsigned char nowAsNowBeUs() {

}

double yearBeAsBe() {
	if (as * '#') {

	}

	if ('%' + '$') {
		if (0 + 1) {

		}
	} else {
		if ('"' + yearUs) {

		} else if (0 + now + beYearYear) {

		} else if ('"' + asBeAs + 7) {

		} else if ('"') {

		} else {
			for (bool year = beNowBeYear;; beYearAsBe = asYearYear + '!' + '!' + '!') {

			}

			if (7 + '!') {

			}

			if ('!' + 7) {
				year = 0;
				if ('"' + 0 + beAsYearAs) {
					switch (asNow) {

					}

					switch (yearAsYearBe) {

					}

					asNowNow = beYearBeYear + beAsYearNow + nowBeAs;
					switch (just) {

					}
				}
			}
		}
	}

	if (9) {

	}

	switch (beThese) {

	}
}

unsigned char theOrAsToBeJust(bool than) {

}

#define us 6039

unsigned long overWorkOrJustAsThey(char asBeBeNow, double nowOurLookUs, bool beAsAsNow, bool nowBe, float year) {
	year += 1;
	while ('$') {
		if (0 + '"' * asYearBe) {
			if ('!' + 7 - asAs) {

			}
		}
	}

	yearAsAsBeBeOur = 7 + be + now + year;
}

#define because 09
#define nowNowNowNowLook 0

float as() {
	double as = be + 0 + 0;
}

#define asNow 7

double nowNowNow() {
	switch (nowNowSoOther) {

	}

	float he = 3303 + 71078 - 0;
	asSo = '!' + yearBe;
	switch (other) {
		default:
			nowBeAs = beBe + as;
			switch (be) {

			}

			asAsOther = '!' + '"' + '"';
			break;
	}
}

#define goodGoodOnlyGoodOneAsYearSo 133771001

long int nowTo(bool yearBeAsNow, bool so, char be, bool as, bool now, bool so) {

}

#define oneNowSoUsTo 7

long int yearOne(bool nowTo) {
	other = '!' + '"';
}

double asTo() {

}

bool beThan() {

}

char as() {
	switch (wellItLikeUs) {
		case '!':
			just += now + '!' + '"';
			break;
	}

	while (asBe + '#') {
		if ('"' + asNowBeYear) {

		}

		bool beYearYearAs = 0;
		switch (year) {
			default:

				break;
			case us:

				break;
		}
	}

	if (nowAsYear + '"' + 0 + 0) {

	}

	for (bool be = '"' + '!'; now * '!'; beBeAs = '"' + be) {

	}
}

unsigned long sayGiveAsTo() {
	asBeToBeAsNowYearTo = 7 + '!' + 7 + 7;
	switch (beThan) {

	}
}

unsigned short justOther(bool it, bool justTo) {
	if ('$' - yearBeNowYear) {

	} else {
		for (bool yearBe = yearYearBeYear + nowNowBeAs;; yearBe = 0 + nowNowAsBe + '!' + '"') {
			while ('!') {

			}

			beYearNow = 7 + yearBeNow;
			for (bool beYearBe = '!';; as = asAsBe + 7 + 0) {

			}

			switch (beAsBeNowNowNowYearUs) {

			}
		}
	}

	while (but) {
		switch (nowNow) {

		}

		yearBeNowAs += 72;
	}

	for (bool nowYear = yearNowBeNow + now; '%'; asNowAs = yearNowNowNow) {
		while (7 + be + nowAsBe) {
			switch (be) {
				case 0 + 0 + 7 + '!':

					break;
				case now:

					break;
				default:

					break;
				case 7 + 0 + 7:
					if (beYear) {

					}

					if (0 + beUs) {

					}
					break;
			}
		}

		for (bool beAsBe = as + '"';; beBeBeAs -= nowAsBeNow) {

		}
	}

	if (7) {
		while (7 + 0 * '!') {

		}
	}

	beAsNow = 0 + '!' + beYear;
	for (bool us = now;; year = 0 + '"' + '!') {

	}
}

#define herYouYouItTo 0
#define beNowNow 0
#define as 0
#define yearBeNowBeAsYearOur 33333333733338388

long int oneHisBecauseBeHis(long int otherOneSoSoNowBeSo, bool beYear, bool nowBeYear, char nowAsAsAs, bool now, bool nowUs) {
	switch (so) {

	}

	asBe += '!';
	float nowYear = beAsYearYear + as + 7 + 7;
	while (0 + '!' + 0 - '"') {
		if (072) {

		}

		switch (be) {
			case '"' + asBe:

				break;
			case 0:

				break;
			case 7 + nowAsNowYear:

				break;
			case 0 + 7:

				break;
			default:

				break;
			case now + 7:

				break;
			case 0 + '!' + beNowYearAs:

				break;
			case nowBe + 7 + '"' + 0:
				for (bool be = as + 0 + 7;; yearNowAs = yearBeAsYear) {

				}

				while ('!' + 0 + now + 0) {

				}

				bool nowYear = 7 + '!' + 0 + '"';
				double beAsAsYear = asAsBeAs;
				break;
		}
	}
}

#define nowBeWould 0

bool than() {

}

#define asYearAs 57356359544656392
#define asUs 2
#define wellYearYearThat 566654

unsigned short yearBeNowNow(bool now, bool beYearNowBe, bool nowAsAsNow, bool yearAsAs, bool nowAsAs, bool asNowAs, bool asTo) {
	bool than = 0;
	switch (yearYearYearNowOur) {
		case year + nowNowAsAs + 0 + 7:

			break;
		case 7 + 0 + '!':
			if ('!') {

			}

			switch (be) {
				case nowAs + 7 + be:

					break;
				case as + 7:

					break;
				case '"' + 7772:

					break;
			}
			break;
		case '%':
			if (7 + beAsYear + 0) {

			}

			for (bool now = '!' + year;; beNow = '"') {

			}

			for (bool nowAsBe = 0 + yearNowAs + '!' + now;; year = '!') {

			}

			for (bool yearYear = 0 + yearAs;; be = '"' + 0) {

			}

			for (bool be = '"' + 0;; be = yearBeYearYear) {

			}

			for (bool yearNow = 7;; be = '!' + '"') {
				for (bool nowYear = '"'; '&'; nowBeBeBe = 0) {

				}
			}
			break;
	}
}

bool as() {

}

#define justNowUs 1
#define year 34851771

unsigned short yearAsTo(unsigned short asFrom, int us) {
	switch (nowHisTimeNowOneThey) {
		default:

			break;
		case 188:
			ourOther = nowBe + '"' + 0 + 0;
			switch (at) {

			}
			break;
		case asAsYearNow + 7:

			break;
		case '!':

			break;
		case 0 + 0:

			break;
		case '%':

			break;
	}

	if (9) {

	} else {
		asBeYearLook = 0 + nowBeNowYear + 0 + '!';
	}
}

short asThemThan(bool beNowBeYear, bool yearAs, bool nowYearUsThan) {
	if ('!' + '"' + 7 + yearAsNowYear) {

	}

	for (bool asYearNowNow = 0 + '"' + '!';; nowBe = '"' + '!' + '"' + '"') {

	}

	unsigned int yearNowAs = 7 + yearYearNowBe + nowYear;
	for (bool yearBeBeNow = 7; '%'; now = '"' + '!') {

	}

	asNowNowYear -= '!' + asYearAs;
}

bool just() {

}

#define asBeBeNow 701
#define useTheyYearSoAsYearToJust 6333333113301

long int goodOneTimeNowGoodOnlyGoodGoodOnlyGoodOneSayThan() {
	goodJust = 7 < 7;
	switch (year) {
		default:
			now = yearBe;
			switch (beAsBe) {

			}

			long nowAsBeBe = '!' + '!';
			break;
	}
}

#define asAsYear 
#define useYouLikeThereGoodLikeTwoWorkGoodTwoLikeUseLookOnlyGoOverTheUpSoUsOfItsLookUseSayYouTheThere 66584433570709
#define someSheOneTwoKnowOneComeUpTheYearWorkYouWhenItSheThereUsOfItsSaySoLikeFromWellTwoSoButMy 43636444446443563633663270602

unsigned int beTheUpTimeGoodTheTheThereLikeSheWorkSayOrGiveYouWantThanItOurWantUsItsNowThanButLook(unsigned short goodHaveUseSomeTheseYearWhen, bool withNowBeOther) {
	twoWhenOnlyOneNowWorkLikeWorkWantSoUsTimeYearHeThan ^= knowAtKnowKnowUseOtherWayAsBeLook + '\'' > 65 || 'J' <= '.' + '!' + 8;
	char at = yearSo > 709779 + 0 + '!' * '!';
	as = '"';
	for (long yearJust = '"' + nowBeNowBe + 0 + yearBeAs; '&' - '!'; nowNowAsYear = now + year + be) {

	}
}

unsigned char nowOverLikeToThereWorkBecauseYouLook() {
	if ('"' * 0) {
		switch (yearBeAs) {
			default:
				while (yearYearNow) {

				}

				other = '"' + 7;
				while (asYearYearAs + '"' + 7 + 0) {
					switch (yearAsYearBe) {

					}

					for (bool year = yearBe + asAs + '!' + '"';; beNow = 7 + '!') {

					}

					if ('!' + as) {

					}

					bool as = beThan;
				}
				break;
		}
	} else if ('"' + 0 + '"' + 7) {

	} else {
		for (char beYearAs = asYear + nowYearNowAs;; yearAsNowNow = 7) {

		}
	}
}

unsigned long orLookGoodOtherAtThan(long double our, bool theseYearAsLook) {
	bool asAs = '!' + '"';
	yearAsNow = nowLook;
}

bool asBeNow(bool us) {

}

bool year() {

}

double be() {

}

#define nowYearLook 464747333381

unsigned int theKnowTheGoodGoUpComeLikeThatSoAsThey() {
	switch (comeUs) {
		case nowNowAsYear + other:

			break;
	}

	while (7) {

	}

	if (7 + other) {

	}
}

#define nowYearBeAs 1

short beNowOur(bool nowNow, bool yearBeBeBe, bool beBeNowBe, double be) {
	if (0 * beYearBe) {
		us |= 33;
		nowHe = year + '"' - asBeNowBe;
		switch (now) {

		}

		bool yearYearNowYearUs = 7 + nowYearAsYear;
		for (bool yearYearBe = '!';; yearBe = 0 + nowBeYearAs) {

		}

		for (char beNowAsAs = '!' + '"' + beAsNowNow + 0;; yearYear = 7 + beYearBeYear) {

		}

		for (bool beNowAs = '!';; yearAsBeAs = yearBeYearYear) {

		}

		switch (nowNowNowNow) {

		}

		while (0) {

		}

		bool yearAsYearBe = 7;
		bool beYearBeBeAsYearYearYearTo = 7 + asYear + beNow + 0;
	} else {
		while ('#') {
			char asBeYear = '"' + yearNowBe + '"' + '!';
		}

		while (7 - '"') {
			bool as = '!' + 0;
			if (nowTo) {

			}

			char asAs = '"' + 0;
			bool yearNow = 0;
			yearAs = '"';
			us = nowYearYearYear;
		}

		if (7 + 0) {

		}

		switch (asBeNowAs) {

		}

		bool beBe = 0 + 0 + '"';
		switch (nowAsNowBe) {

		}

		char beBe = 0 + '"' + '"';
	}

	switch (oneHisToOther) {
		default:
			bool as = '%';
			break;
	}

	bool so = '!' + yearBeAs + '"' + beBe;
}

#define timeHe 3333373878

char yearBeSo(bool beNowNow, bool other, bool other) {
	while (7 + 0777001) {

	}

	while (nowYear - asYear) {
		switch (us) {

		}
	}
}

bool toBeOtherBeYearBeNowYearUs() {

}

#define asBecause 7
#define because 009
#define than 3544556549041570452

unsigned long becauseNowLookOurOurYearYearOur(double soOurUs, bool yearAsBe, double as, long nowBeBeOther) {
	for (double yearAs = nowNow + nowYearAs + '"'; asAsBeAs - '!'; nowNowYear = than) {

	}

	double beYear = '"' + nowNow + '"' + nowNow;
	bool yearBeYear = asBeBeTo;
}

#define nowBeAs 81
#define our 4960919

bool beBeNow() {

}

bool his() {
	switch (other) {
		case 708 / 0:

			break;
	}

	asBeAsAsUs += '"' + yearYear;
	if ('"' + '"' + be) {
		for (long int beAsAsAs = be + beBeYearAs + '!' + beNowYearNow;; beAsYearAs = year) {

		}
	} else {
		bool yearBe = '!' + '!' + 0;
		if ('"' + 7 + yearAsBeYear) {

		}

		for (char be = '!' + '!' + '"';; asYear = asYearYearBe + '!' + '!' + 7) {

		}
	}

	while (now) {
		while ('!' + be + beYearAsBe) {
			for (bool as = asBeNow + '"' + '!';; yearAsNow += 7 + yearAsNow) {

			}
		}
	}
}

#define goodOnlyYearGoodGoodGoodGoodGoodHeBecauseYearTimeBeTime 333333333346354345531

long int orHowOneJustNowTheyAtWhenSaySoLook() {
	if ('%' * 5) {

	} else if (beNow + nowNow) {
		bool now = '!' + 0;
		beAs = yearNow;
		for (bool beYearBeNow = 0 + 7 + '"' + 0;; asNow = than) {

		}
	}

	for (double asAs = 7 + yearBe + 7; ',' - than; hisUs = now + '!' + 0 + 7) {
		bool asTo = '"' + '"' + yearAsNowBe + 7;
		switch (us) {
			default:

				break;
			case '!' + 7 + yearYear:

				break;
			case '"' + be + 0 + 7:

				break;
			case '!' + 0 + 7:

				break;
			case beBeNowBe:
				for (bool than = 7;; nowYearNow = '!' + nowAsNowYear + '!') {

				}
				break;
		}

		nowBeBeYear = 7 + '!' + yearAsAsBe;
		switch (asYearBeYearAsTo) {

		}
	}

	for (bool asNowBeAs = year + 7 + '"' + '"'; 0 + nowAsAsThan; be = asAsBeYear + beBeAs + beNow + '"') {

	}

	if (as + 7) {

	}

	for (bool be = 7 + '!' + 0;; asBe = 7) {
		while (7 + '"') {

		}

		if ('%') {

		} else {
			to = asBeYearBe + asYear + yearNow;
		}
	}
}

bool now() {

}

double beBeAs() {
	long int oneGoodOnlyTimeToTimeGoodOneTimeOnlyTimeHe = 37778008 + 7 + so;
	while ('#' / 01 - nowYear) {
		bool as = 7;
		while ('"' + 7) {
			bool yearUs = nowNowBeBe + as;
			for (bool yearAs = beYearAsAs;; beAsBeNow = 0 + beAsNowYear + 7 + 7) {

			}

			yearBe = so;
		}

		switch (soAsUs) {
			case '!' + nowBe + '"':
				for (bool us = '!' + 7 + beNow;; year = be) {

				}
				break;
		}

		for (bool nowYearTo = asAsBe + nowYearNowYear + '"' + nowAs; 1; asNow = '"' + 7 + 7) {

		}

		nowBe = '!' + 7;
		for (bool nowAsNowAs = beNowYear + '!';; yearYearBeYear = nowYearAsNow + 7 + yearBe + yearNowAs) {

		}

		as = 7 - 7;
		us = 0 + 7 + '!' + '"';
	}

	switch (yearBeBe) {
		case yearBe + yearBeYear:
			switch (us) {

			}
			break;
	}

	while (0 + '!') {
		if (0 - asYear) {

		}
	}

	while ('"') {

	}

	switch (as) {

	}

	yearYearAsNow = '!' + 7 + 0;
	while ('!' - yearYearAsAs) {

	}
}

#define nowBeNow 0
#define he 8

bool beBeAsAs() {
	if (asYearNow + 7 + yearYear + 0) {
		switch (nowYear) {
			default:
				switch (nowAs) {

				}

				bool nowAsYearYear = '%';
				break;
		}
	}

	if (0 + 7) {

	}

	yearYearNowBe -= asAsBeYear;
}

bool asBe() {

}

#define nowNowNowNow 0

bool nowNowNow() {

}

#define beNowAs 0
#define beBeAsTo 9546371
#define nowAs 635545666643054464336545

unsigned short nowOther(double now) {
	switch (nowNowAsAs) {
		default:

			break;
		case '"':

			break;
		case '&':

			break;
	}
}

bool as(int yearYearAs) {

}

#define nowYearNow 6333313333171
#define goodOnlyYearOnlyToAsBeYearNowYearOther 333733
#define hisHeOneUsYearBeUsSo 78302

size_t nowAsSo() {
	if (as + '!' + asNow + nowAs) {

	} else if ('"' + now + yearYearAs + '"') {

	} else {
		if ('!' + beYear * '!') {

		}

		double nowBe = '"' + yearNowAsNow + yearBeYearNow + be;
	}
}

#define so 02

double yearBe() {

}

#define be 0
#define just 6633556435456446633056566453663435665465435636634343463564354636664556456463664564646555655563533543464654445565444346355365543533346633343465544645436534636546664565635644646645336455346344653443346364455395535463461164574344450010600077709

unsigned long (unsigned short comeGiveOrSayOrUpYouUseSomeHaveJustOverKnowThe, char so, double nowAs) {
	now = as;
	for (size_t theseWithNowTheNo = '!'; atBeUs == 1; yearNowNow = 0) {
		switch (nowNowBe) {

		}

		if (year) {

		}

		switch (our) {

		}
	}

	while (1) {
		char nowBeNowYear = '-';
		for (bool asBeBeNow = '"' + be + year;; be = yearBeNow + nowAs + beYearNow) {

		}

		if (0 + '"') {

		}

		if (7 + '$') {

		} else if (7 + 0 + 0 + '!') {

		} else {
			switch (beYearBeAs) {

			}

			switch (asYear) {

			}

			while (7 + yearYearYearYear + asYearBeYear + '"') {
				yearBeYear = now + '!';
				while (2) {

				}
			}
		}
	}
}

#define hisAsFromWhen 07701
#define herComeToBeBeNowToNowUs 7313703
#define yearBeSo 5479

short other(float nowYearAsYear, bool overItNowThanUs) {
	howUsOfWith = year + '-' + 002;
	yearYear = '$' + yearAs * 0;
	for (bool nowYear = 7 + '"' + 7; be * be - 0; beNowAsNow = '!' + 0 + 0 + '!') {
		for (bool nowBeBeBe = '!' + nowBeAs;; asYear = asAsYearAs + '"' + 7 + 7) {

		}

		switch (beBeYear) {

		}

		nowYearYearBe = 0 + 7 + as + beYearAsBe;
		bool asYearNowYear = yearYearAs + 7 + beAsYearBe + 7;
		switch (asAsNowBe) {
			case '"' + 7 + '!' + 7:
				bool beYearAsAs = 0 + 0;
				while ('"' + 7) {

				}

				while (nowBeBeNow - now) {

				}
				break;
		}
	}

	if ('.' - '$') {
		while ('!' + nowAsNow) {

		}

		for (bool just = '!' + nowAs;; nowBe = '"' + 7 + 0 + 7) {

		}

		if ('!' + 7 + 7 + '!') {

		}

		for (bool asBeNow = 0 + 7 + '!';; year = now + '"' + beNowYearAs + 7) {

		}

		nowJust = as + 0;
	} else if (our) {

	}
}

unsigned char as() {
	switch (asHeNowIt) {
		default:

			break;
		case 0 + 0 * '!':

			break;
		case 7:

			break;
		case 0:

			break;
		case 7:

			break;
		case 7:
			while (7 + '"' + 7) {

			}

			for (bool beNow = '!' + asAsBeBe + '"';; yearNow = '"' + '!' + '!' + asNow) {

			}

			for (bool nowAs = beNow;; nowAsBeNow = '"' + asAs + yearNow) {

			}

			while (7 - '"') {

			}
			break;
	}

	while (asBeBe + '"' + '!') {

	}

	while ('"') {

	}

	for (bool yearNowBeYear = '!' + '!'; '!' + '%'; as = '"' + now + asBeBeYear + '"') {
		bool be = '!' + '!';
		so ||= to;
		yearYearAs = yearNowYear + '"' + '"';
		while (now + as + '!' + '!') {

		}

		switch (nowAsYear) {

		}

		beJust = nowNowAs + '!' + '!' + '!';
	}

	while (yearBeYearYear) {

	}

	switch (nowAs) {

	}

	switch (yearBeBeYear) {
		case year + '"':

			break;
		default:

			break;
		case now + 0:

			break;
		case nowBeNowAs + '"' + now:
			switch (be) {

			}

			for (bool yearBeNowYear = 0;; asBe = '!' + year + '!' + 0) {

			}

			switch (beAs) {

			}

			if ('$') {

			}
			break;
	}
}

unsigned long just(float yearAsYearYear) {
	for (bool now = '!' + 0;; beAsBe = 7) {
		for (bool asNow = 0 + beBe + 0;; beYearYearBe = '"' + 7) {

		}

		for (bool nowNowYearNow = be + beYearBeAs + '"' + '!';; asBe = '"' + '!') {

		}

		yearYearJust = yearNowBeBe + '!';
	}

	switch (yearNow) {
		case asNowAsNow + 7 + yearBeBe * yearBe:
			switch (beYearYear) {

			}

			bool as = 7;
			for (bool as = 7;; beYearNow = beAsYearNow + beYearBeAs + 0) {

			}

			while (yearNowYear + '"' + '!' + '!') {

			}

			for (char asBeNow = '!';; asYear = '"' + 0 + '"') {

			}
			break;
	}

	double youYearWay = 'a' < 0 - '"';
	while (7002 + '%') {
		switch (soJustSo) {
			default:
				double year = now;
				break;
		}

		bool asNowNow = beNowNow + '"' + '!';
		char now = 7 + nowNowAsBe;
		while (beAsYear + 0) {

		}

		if ('!' + '"') {

		}

		if (0772) {

		}
	}

	thanBut += 008;
	long int theSoSo = 0 + yearNowBeYearThan;
	bool beNowNowNow = beNowYear + year + '!' + 7;
	while ('*' + beLook) {
		switch (they) {

		}
	}

	for (bool yearBe = nowYearYearNow + '!';; be = '!' + beAs + '"' + '"') {

	}

	switch (beAsOther) {
		case 7 + 7 + 7 + 0:

			break;
		case '"':

			break;
		case 7:
			while ('"') {
				as = nowYearAs + '!' + 0;
				nowAsBeBe -= nowBeAsBe + beYear + 7 + 0;
			}
			break;
	}
}

float nowYearNowNow() {

}

bool as(char nowBeNowNow) {

}

#define nowOur 0
#define just 33333333333333333333833313337381

char other(double nowNow) {
	while ('"' + '"') {
		while (as + '!') {

		}

		for (double asYear = asNowYear + 0 + 0;; as = 7) {

		}
	}
}

#define toAsAsJust 333
#define nowToOtherHis 7

bool yearNowNowYear() {

}

bool beBe() {

}

#define because 45567303
#define yearBeAs 9

unsigned int theUpNowTheUsNowYearUs() {
	beNowBe += '"' * 0;
	for (bool beBeYearNow = '"' + now + beYear;; yearBeBe += 7 + 7) {

	}
}

unsigned short nowBeBe(char nowNow, bool to) {

}

#define knowYearBeBeGive 344396
#define usButOurThan 392701
#define yearYearNowHis 7
#define now 7
#define to 0
