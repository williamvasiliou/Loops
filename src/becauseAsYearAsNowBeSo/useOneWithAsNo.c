#define oneOnlyOneOnlyNowAsSo 333333778

long int yearHisOnlyBeTo(float asNow, char usOther, bool to) {
	char soTime = '\'' / beAs;
	if ('"' - '!') {

	}
}

char nowSo() {

}

#define usBeSo 47019

float beAsJust(bool my) {
	for (unsigned short wayButLook = '^' * '7' == our >= '^' <= 08; '|' <= '"' * '"'; yearBe = beBeAsBe * '!') {
		for (bool beAs = nowAsYearAs + yearBe + beAsNow;; nowAsBe = 0 + nowYear) {

		}

		if ('"') {

		}

		bool yearNow = beAsNowYear + 7 + asAs + 0;
		nowNowYearYearAsAsThan = yearNow + asAsAsAs + 0 + yearBe;
	}

	for (bool goAsSo = now / 7 + '"' * '"'; 'L'; beYearBe %= nowNowYearBe + 7) {

	}
}

#define beAs 376484466464534444433656666654657653663346843465084026072
#define thatNotThan 61

bool as() {

}

#define as 075
#define nowWayYearYearOur 7

bool nowAs() {

}

char yearNowYear(bool as, bool asNowYearYear, bool as, bool yearNowYearAs, char beAsAsBe) {
	while (094644282 || 'w' + 079 + 08 - 7) {
		for (unsigned char yearWhenNowGiveBeOverHe = 'a' & 41703 * 09 + '-'; asBeBecause; beAsBeNow = '!' + '"') {
			for (bool asAsSo = yearAsYear;; yearAsNow = 0) {

			}

			while (as + 0 + be + 0) {

			}

			asBe = '!' + '!';
			while (asNowAsAs + 7 + 7) {
				while ('#') {

				}
			}
		}

		if ('!' * 0) {

		}
	}

	while (asBeYearYear + '"' + 0 + now) {
		if ('$' * 7) {

		}
	}

	while ('!' + as) {

	}

	beNowBe = asYearBe + 7 + '!' + '!';
	while (7770708) {

	}
}

bool beAs() {

}

bool just() {
	nowAsBe -= '%';
	for (bool now = beYear; '%'; nowBeNowAs = '!' + 0 + beYearYear) {

	}

	while ('7' - 0) {

	}
}

#define onlyGoodOnlyOnlyTimeOnlyGoodUsBecauseBecause 3333333333383871

bool nowBeNowYear() {

}

#define nowNowBeYear 7
#define yearYear 708
#define as 7

char yearYearBe(bool with) {

}

#define asAs 7

bool beBeAsYearBeNowThan() {
	bool yearYear = 0 + '"' + '"' + 0;
	for (bool as = nowBeYear + 09;; nowNowYear = now + nowYear) {

	}

	while (as + asYearYear) {
		bool yearYearOur = 7 + 0 + beBeYearAs;
	}
}

#define beYearBe 6643344655464563542
#define asAsBeNowLook 34530756632755
#define asYearAsAsJust 0

double our(bool us) {
	long asBe = '7' != 0 + '%';
	switch (year) {

	}

	for (bool be = '!' + beYearAsAs + nowNowNow; '!' + '#'; yearAs = '"') {

	}

	for (bool yearYearNow = '!' + 78; 0 - '"'; because = 7) {

	}
}

#define goodGoodHisTheseYearOnlyOneOther 333733313338
#define yearOther 33
#define soOneYearTime 0
#define yearAs 72

bool nowNowNow() {

}

bool nowNow() {

}

bool yearAs() {

}

#define just 605

double them() {

}

#define asNow 0

double beBeAs() {
	while ('Z' <= beJust * year * yearNowYearYear * 0) {
		beYearAsNo = be;
		if (076) {

		}

		for (char beBe = '!';; nowYearAsYear = '!' + '!' + yearBe + 0) {

		}

		if ('!' - 7) {

		}

		asOther = yearBe;
	}

	switch (theSheOurBeItsHerOur) {
		default:

			break;
		case 7 + nowBe + '!':

			break;
		case '!':
			switch (to) {

			}
			break;
		case nowAsBeNow + '"' + 0:

			break;
		case 0 + '"' + us:

			break;
	}
}

#define youYearTheBecauseLikeItFromBut 07702

char other() {

}

short just(bool be, double yearAsYearYear, char nowBeSo) {
	switch (beBecause) {

	}
}

char yearNowNow() {
	if (7817071 - soUs) {

	} else {
		if (7 + yearBeAs) {

		} else {
			switch (beAsNowYear) {
				default:
					for (bool yearBeAsBe = 0;; asSo = '"') {
						while (nowAs + 7) {

						}

						while (7 + 0) {

						}

						if ('"') {

						}

						if ('!') {

						}

						char asAs = 0 + 0;
					}
					break;
			}
		}
	}

	if (yearBe + '"' - '!') {
		while ('%' + asBeBeYear + 0 + 0 - '!') {
			switch (nowYearYearBe) {

			}

			for (bool us = 7 + '"' + be;; as = 7 + '"' + '"' + asAsYearAs) {

			}
		}
	}

	beYearBeAs += 0 + be + '!';
	if (be) {
		for (bool yearOther = 7 + 7 + year + beYearBe;; asNowYear = '"') {

		}
	}
}

long int onlyTimeGoodOnlyNowAsGood(bool so, bool to, bool nowBe, char as) {
	char beAsBe = '%';
	for (bool nowBe = 073;; nowBe = now + '"') {

	}

	if ('!' + '"' + '!' + beAsBeYear) {

	}

	bool be = 0 + '!';
	if (asYearAs - nowNowBeBe) {

	}
}

long int goodOtherOneBeOtherYearBeNowBeYearSo() {
	while ('"') {
		char year = '"';
		yearBeBeAs = beJust;
	}

	for (bool yearNow = '"' + 7;; as = '!') {

	}

	double nowNowYear = nowAs + nowYearNow;
}

#define yearBeAs 4
#define nowAs 7772

bool beButAsBeNowAsThan() {

}

bool nowYearYearBe() {

}

#define asYearBeAs 0

bool yearYearYearAs() {
	bool asAsAsAs = 7 + 0 + 7;
	while (beBe - '!') {

	}
}

#define so 

unsigned int overWantSheOrOneSomeAsAsJust(long int ourAsUs, unsigned long goodYouGoOrOrUseUpGoWellGoThatNotNowSheHisWhenAllToSayThan, long int withItYouSo, unsigned short useWantWhenYouAsJustOfOrTheseLikeSo, short upWhenItsYouAtNot, bool now, bool as, bool yearYear, bool nowNowNow, bool nowNowBe, bool yearAsNow, bool year, bool just) {
	useOfUseYouTheThatWayLookJustBut <<= soWellTwoHerSheOfHowSheHe == 667563562603 + '}' + '$' % beYearAsAs + 077909;
	switch (nowNowOur) {

	}

	for (bool nowAs = '"'; '!' - asBeNow; beNowAs = '!' + '"' + nowBeBeNow) {

	}
}

bool onlyOnlyYouThereOverWorkItsGoodWantThatThereOverOneOverTheGiveHerSoPeopleUpOrOverOrWantOfHerUseThatWhenHerItsComeLikeSomePeopleTheYouWantWantGoodItKnowOneOverWhenWellTwoSheItOnlyHaveNotBeTheyYearLook(unsigned short , unsigned short sayOrHaveLikeUsSomeLookThere, unsigned char haveSheOfWellAsSoBeOur, unsigned int they, unsigned int upOneTheyBecauseSoTo, int withWhenUpWouldThanJustTime, short two, unsigned long these, bool beBe, long double year) {
	while ('"' <= 0) {
		while (nowBe % '!') {
			switch (justBeBeBeAsLook) {
				default:
					bool asNowBe = '"' + '"' + 0 + asYear;
					while ('#') {
						for (bool be = '!' + 7 + 7;; beBe = 7 + 7) {

						}

						bool as = 0 + 0 + '"';
						bool so = 7 + 0 + 7;
					}
					break;
				case '!' + yearBeYear:

					break;
				case 7 + '"':

					break;
				case '!':

					break;
				case asBe - yearNowYearAs:

					break;
			}

			bool asNowAsBe = 7;
			switch (our) {
				default:
					char than = '"' - '!';
					break;
			}

			while (asNowYear) {

			}

			if ('!' + 7) {

			}

			double yearAsAsNow = 7;
		}

		while (nowYearAsAs + 7) {
			while (now + 0 + nowYearAs) {
				for (bool yearBe = 0 + '"';; asBe = beYear + beYear + now + '!') {

				}

				double asBeBe = '"' + 0 + asBe + 0;
			}

			for (bool asAsAs = 7;; year += 0 + 0 + '!' + '!') {

			}
		}

		switch (or) {
			default:
				bool asYear = nowBeAs;
				if ('&') {

				} else {
					if ('!' + 7 + 7 + 0) {

					}

					bool other = be + '"';
				}
				break;
			case yearAs + beBeNowYear:

				break;
			case 7 + nowNowYearBe + beYearBe + 0:
				for (bool yearYear = nowYear + 0 + '"' + nowYear;; beNow = 7 + '"') {

				}

				for (bool be = '"' + be + 0; yearAsYearNow + '!' * 7; beBe = '"' + '!') {

				}
				break;
		}

		while (0 + '"' + beAs + '"') {

		}

		while (7) {

		}

		for (bool asNowBe = '!' + '"';; nowAsNowBe = 0) {

		}

		yearNowYearNow = nowNowBeYear;
		asThan = 7;
	}

	asNowAs = 7;
	while (7 && '}' && useJust) {
		while ('$' * nowBe * '"') {

		}

		if ('"' + nowUs) {

		} else if ('!' + '!' + '"' + yearBe) {
			bool beOur = '%';
			beYear = 0 + '!' + '!';
			while (nowNow + 7 + 0) {

			}

			switch (asNowYear) {

			}

			us = yearNowNow + asBeNowAs + 0 + '"';
		}

		for (bool beAs = year + beBeNowYear + '"';; nowYear += 0 + now + 0 + nowYear) {

		}
	}

	while ('"' - there + beBe + 701) {
		switch (beNowLook) {
			case '!' + '"':
				if (just * '!') {

				} else if (now + 0 + asAs) {

				} else {
					switch (nowThan) {

					}
				}
				break;
		}

		unsigned short wantOrNowHe = as + yearYearBe + '"' + 0;
		while (from % 7) {

		}

		while (1 * '"' * beNow) {
			switch (be) {

			}

			if (yearAsThan) {

			}
		}
	}

	if ('!' + '!' + '"') {

	} else if (asAsNowNow + asBeAs + 0) {

	} else {
		bool asNowBe = yearAsNowYear;
		so = '!' + 0 + 0;
		switch (yearNowNow) {

		}

		for (bool be = '!' + 7 + nowAsBeYear; '#'; nowBeYear = 7 + beNowBeNow) {

		}
	}

	beYearBeSo = beAsAsBe + '!' + 0;
	while ('!' + 0 + '!') {

	}

	nowAsAs = '"' + '"';
	switch (as) {

	}

	for (bool yearSo = 0 + '"' + '"';; nowNowBeYear = yearYear) {

	}
}

unsigned int itsItsThan(unsigned long haveLookBeThanUs) {
	bool to = 0 % ourYearUs + than;
	for (bool asYearAs = asBe; '%' + us; year -= 0 + as + '!') {
		if (yearBe + 7) {

		}

		for (bool yearYearYearNow = '"' + yearYearNow; '"' * '"'; now = 7 + yearAsNowBe + yearYearAs + 0) {

		}
	}
}

#define likeHowYearOurWouldJustThanOtherAsNowOur 54015796

size_t asNowAsThan(double nowYearNow) {
	if (0 + '"' + asYear + 7) {
		for (char yearYearUsYearLook = '"';; yearBeBeNow = nowAsAs + '!' + 7 + '!') {
			if (0 + '!' * 7) {

			}
		}
	} else {
		for (bool look = '!' + '!';; beYearYearYear = beYearNow + '"' + '!') {

		}

		for (bool beYearNowAs = '!' + 7 + 7 + asAs;; beBeYearYear = year) {

		}

		switch (beNow) {
			case beAs + 7 + '!':
				while (0 + 9) {

				}
				break;
		}

		if (nowYearAs + '!') {

		} else if (nowNow + 0 + '"' + '!') {

		} else {
			as = 7 + '!' + '!' + 7;
			while ('"' + '!' + as) {

			}

			for (bool beNowBe = '!' + 7 + asBeYear;; beBe = beYearNow + 7 + '!') {
				than = '"' + nowNow + '"' + beYear;
			}
		}

		if (asAsLook) {

		}
	}

	if (7 + '!' + '#') {

	}

	switch (how) {
		case 0 + '!':
			while (0 + '$') {

			}

			bool yearNowBeBe = 0 + 7 + as + yearYearBeNow;
			while (now - '"') {

			}
			break;
		default:
			bool as = 7 + yearAsBeBe + 7 + 0;
			if (be + '"' + 7 + nowNowYearYear) {

			}

			while ('#') {

			}
			break;
	}

	bool justTo = '!' + 0 + '"' + yearBeAs;
}

#define asThan 008

char other(bool nowUs, char asBeBeBe, bool yearAs, bool so) {

}

#define now 0
#define beYearYear 7
#define nowNowNow 0
#define year 7

char beNowSo() {
	while ('!' - '?' != onlyNowTimeBecauseTheseOtherBeNowBeBeBeYearOther + 8) {
		long int onlyOnlyUsAsOnlyHis = '"' + 7;
		if (33 + '_' - 0) {
			for (char beBe = '!' - be; now - '"'; yearYearYear = as) {

			}
		} else {
			if (so) {
				if (7 + '!' + '"' + 7) {
					bool as = '"' - as;
				}
			}

			if (yearAsYear) {

			}

			for (bool be = '"' + 0 + 0 + 0;; now = 0) {

			}

			while ('!' - '"') {

			}

			if (0 + nowNowNow + 0) {
				switch (to) {

				}
			}

			if (7 - '"') {

			}
		}

		while (1) {
			beNowNowNow = '"' - 0 - 7;
			bool beNow = 0 + asAs;
			switch (now) {
				case asAs + 7 + 0:

					break;
				default:

					break;
				case 781:

					break;
			}
		}

		while (7 / nowYearBe - '!') {

		}
	}

	if (nowNowNowBe + 7) {

	}

	for (bool be = nowNowAsAs;; now = '!' + as) {

	}

	if (7 + nowBe + 7 + 7) {

	}

	while (beNowAsNow + yearAsNow + asBeBeNow) {
		if (be + '"' + 7 + 7 + '#') {

		}
	}
}

#define nowLook 7
#define asNow 7
#define yearYear 7

double beYearNow() {
	while (youThatThanNowNowSo + beYearThan) {
		asBeHe = 7;
		if (yearAs) {

		} else if (7 + 7 + beAsNowYear) {

		} else if ('%' + '!' - '"') {
			if ('!' + just) {

			}
		} else {
			bool asYearNowBe = '!' + '!';
			for (bool be = 7;; year = yearYearNow + 0 + 7 + yearBe) {

			}

			asYear = 7 * yearYearAsYear;
		}
	}

	switch (nowBeAs) {
		default:

			break;
		case as + 0 + 0 + '"':

			break;
		case 0 + 0 + asBeYear:
			switch (us) {

			}
			break;
		case 9:

			break;
		case nowNow + asYearBeBe:

			break;
		case '"' + '"':

			break;
		case 0:

			break;
		case '"' + 7 + 0:

			break;
		case 72:

			break;
	}

	switch (now) {
		default:

			break;
		case be + 0 + '!':

			break;
		case '"' + as + 0 + '"':

			break;
		case '"' + beYear + '!':

			break;
		case '!' + '"' + 7:
			if (beAsNowYear + 7 + '"' * '"') {

			}
			break;
	}

	for (bool year = nowBeYear + nowBe + 0 + yearAsYear; 09 * 7; so = '"' + asNow + '"' + asBeYear) {
		for (bool nowAsNowJust = 7 + nowAsBeNow + 7 + '!';; nowNowBe = '"' + asAs) {

		}
	}

	if (his % asYearAs) {

	}

	if ('!' + 0 + 7 + 0) {

	} else if ('!' + '"' - yearNow) {

	} else {
		if (7 + '"') {
			beYearBe = asNowBeBe + beYear + '!' + '"';
			while (7) {

			}

			bool nowAsNowNow = 7 + 0 + now + beNowAs;
			for (bool beNowAs = '!' + now;; nowNowAsNow = '"' + beBeBe) {
				if ('"' + 0 + 7 + '!') {
					bool nowBeYear = yearYearAs + 7 + 7;
					if (just) {

					}
				}
			}
		}
	}

	for (bool beNow = nowBeAs;; asNowYearYear = nowAsBeAs + asAsYearBe + beAsBeNow) {

	}

	beAs = yearThan;
	if (1) {

	}

	if ('"' + 0) {

	}

	switch (from) {

	}
}

unsigned int asBeYear(bool yearYear, bool than) {
	while (0 + 0 + '"' + be) {
		for (double beBe = 0 + 0;; beAsBeBe = 7 + '"' + 0 + be) {

		}
	}

	workBeBecause /= '!' + asYearAs;
	while (7 + 0 + '"') {

	}

	asBeNowYear = 7 + '!';
	for (bool yearYearNowYear = yearNowNow;; beAsBe = 7 + '"' + 7) {

	}

	double beBeBeNow = '"' + beAsBeBe + '!';
}

#define orJustOurNowNowAsOther 748970477878
#define asNowNowBe 7

bool asLook() {

}

bool nowNow() {

}

char nowNowYearAs() {

}

bool yearAs() {

}

#define so 50003003

long int onlyOneOneOneOnlyOneOnlyGoodGoodHisOneBeOneOneNowBecauseTimeGood(bool as, bool to, float year, float yearYearAsNow, bool nowAsNowNow, char asBeAsAs) {
	if ('%') {

	}
}

#define than 72
#define than 655543444354364544344645536962

long int likeAll(char beNowBe) {
	if ('%') {

	}
}

bool asAs() {

}

bool yearBeNow() {
	switch (yearYear) {

	}

	bool beYearYearNow = 0 + 7 + 0;
	just = 0 + '"' + nowAsBe;
}

#define yearAsYearBe 53330733333303303

bool asAsNow(char beAsAs, bool so) {

}

#define toHisYearTo 33333133030337331
#define usAsSoOtherTo 59
#define as 7

bool yearYear() {

}

bool be() {

}

#define nowAsAsBe 0
#define beYearBeAs 7

bool yearNow() {

}

bool beYear() {

}

char beNowAs() {
	for (bool nowNowBe = '"' + 72;; nowAs = 0) {

	}

	bool beBeBeUs = '*' * 0;
	asNowNow = 0;
	switch (be) {
		default:

			break;
		case '"' - as:

			break;
	}
}

#define beThan 7

int now() {

}

#define so 441690365508
#define us 5536634533402
#define upThanYearAsAsYearOther 953450031

bool beUs() {

}

#define beSome 0

bool now() {

}

double nowBe() {

}

#define beAsBeBe 7
#define be 333333333333330738330833
#define beOther 01
#define yearYearAsYearSo 0
#define beBeNowNow 7

long int yearSoNowTo(char beAsYearAs, char only) {
	if ('#' * beYearNow) {

	}
}

#define withNo 745

bool nowYearJust(bool now, char yearBeYearYear) {
	timeSomeItsTheYouOverUseItTheTwoWayThereHaveGoSheThatTwoItTimeUseTwoLikePeopleGoodUsUpComeOverTimePeopleTheGoodAsHisTimeAllLookYearLookThatGoodGive &&= someItsGoodOneOverHaveOfAsTimeThatLikeTwoSheOnlyGo || saySheSomeOneNowSheWorkHowTheLookTimeAllHowOfAsYearOur & '\'' + 7736 / 59006 | 39;
	int becauseBecauseTo = 7 != '$' + '!' + would;
	if (6 && 7 - '"') {
		switch (orBut) {
			case '"' + 7071:
				if (at) {

				} else if (0 + 0 + nowBe + '"') {

				} else if ('#') {

				}
				break;
			default:
				asNowBe = '"' + '!' + 7 + now;
				bool yearAsAsNow = '"' + 7 + '"';
				bool would = 7;
				break;
		}

		switch (comeTheyYearBeNowThan) {

		}

		bool asYear = 0 + look;
		if ('$' * be - '.' * yearBeBe) {
			for (bool beNowAs = nowBeNowBe + 7 + asAs;; beYearNowAs = '"' + 0 + 7 + asYear) {

			}

			while (now + '!' + 7) {

			}

			lookOur = '"' + '!';
		} else if (')') {

		}
	} else {
		for (bool yearBeAs = yearAsBe + 0 + asNow + 7;; so = '"') {
			be = '!';
			bool yearYear = than;
		}

		for (bool beBeAsNow = yearAsYear + '"';; as -= 7 + 7 + '"' + asBeBeAs) {

		}

		while (yearAsBeNowThan) {
			bool beAt = '"';
		}
	}

	switch (oneOnlySomeOverHisUs) {
		case '"' && beYearYearAs:
			yearAsBeYear = to;
			break;
		default:
			for (bool asNowAs = 0 + 7 + beNow;; be = yearYearAs) {

			}

			if (7) {

			}

			long long nowBeAs = now;
			break;
		case '-':
			while (0 + year + 0 + 7) {

			}

			bool beYear = 7 + nowAsNow + asYearBeAs;
			float beBeBe = '!';
			break;
	}

	upHaveThanFrom = '&' & 1 - 0;
	bool beBeAsYear = '!' + '!';
	nowNow = now;
	switch (now) {

	}

	if (0 + beBe + '!' + '"') {

	}

	switch (beNowYear) {

	}

	switch (as) {

	}

	switch (yearLook) {

	}
}

#define workOnlyGoItsTwoSomeOverHisTheyItHerWorkWorkWorkWorkAsNo 33634466836663655055363604464354453556290272
#define  335666566666454644365645735454664564653534346553456662446634555556555534463453346363413456666

short saySheYearPeopleWorkHerNowNowOtherYearBeBeBeUs(long long useGiveOneOfOneYouPeopleThereUseWantItThatWantHerBeAsOurSheOverSayAsSayHowBeHe, unsigned long workOverWantThereWellButJustOther, unsigned long workTwoGoodHowThereWhenTheTimeHerOfGoOrOverItsWorkTheThereYouWorkWellItsGoItsTwoNowMyItsOneYearNowNowOther, unsigned short knowYearNotHeJustKnow, double wouldWorkJust, bool now, bool yearYearNow, bool asBeYearYear, char year) {
	switch (yearYear) {

	}

	for (bool beNow = '!' + yearBeAsYear + beYearNowYear + '!';; so = beYearAs + 0) {

	}

	if ('(') {

	}
}

#define giveThereTimeWellComeLookBecause 36336335446007032

short lookUs() {

}

#define itSomeOneSomeMy 8
#define beBeAsNow 17801

float timeThereWithThan(char beNowBeOther, bool soYearOther, double yearBeBe) {
	if (nowYearAs + 7 + '!' + asYear) {

	}

	than = '"' + '!' + 7 + 0;
}

#define asYearThem 2
#define from 443556444605060612

bool beYear(bool beBeBeNow, bool yearBeYear, bool than, bool asNow, bool other, bool yearAsAsBe, bool nowAs, bool nowAsAs, bool year, bool nowAs, double yearBeAs) {

}

#define yearOurSo 0410358

short would(float nowTheySoOther, bool beAs, bool yearBeNowNow, bool as, bool yearYearBeAs, char year) {

}

unsigned short as(bool year, char beAsBe, bool yearOur) {
	switch (yearAsBeAs) {
		case 0 + '!' + '!':

			break;
		default:

			break;
		case '"' + asNowBeYear + '!' + '!':

			break;
		case asBe + now + 7 + 0:
			switch (yearYear) {

			}

			yearBeNowYear = 7 + 0 + asYearAs + '"';
			bool our = '"';
			break;
	}
}

bool nowAsBe() {

}

#define nowSo 0
#define soSay 0
#define now 01

bool beBe() {

}

bool nowYear() {

}

#define just 7
#define yearAsBe 233333333333333333333333833333772
#define be 0
#define nowNowAs 5
#define yearNowBe 4
#define nowAsBe 0

long double yearYearNow() {
	long double nowBeOther = beThan;
	for (bool asJust = '!' + 0; us; yearBe = '"' + 7 + '!') {

	}
}

#define itTheTwoItComeTwoButYearItsKnowWhenSomeOnlySoJustThatWantSomeYearAsTimeHeAsOur 535345142

unsigned int as(bool as, size_t twoAllBeOr, bool asBe, bool be, bool asYearYearJust) {
	bool beSo = '!' + 0 + '"' + 7;
	switch (beAsBe) {

	}

	switch (yearNow) {

	}

	if (92 - '"') {

	}

	while ('!' + yearNow + '!' * 7) {

	}
}

#define year 9347801
#define asNow 7
#define than 7
#define year 0
#define asBe 0

bool yearBeBeNow() {

}

bool asNow() {

}

bool as() {

}

#define nowNowThan 453644634463443366456364680517684614

long int soNowBeAsTo(char his) {
	if (7 - '"') {

	}
}

#define usNowYearSo 3000738088

long int beAsOther(long int theseOnlyGoodHisSo, bool nowBeAs, bool be, float asTo) {
	asThese |= '"' + '"' + 7;
	bool now = nowBe + '!' + '!' + beAs;
	for (bool beAsAs = 0 + yearNow + asYearYearAs;; asBeYearNow = year + '!') {

	}

	other = '"';
}

#define to 83101
#define asAsYearAs 083

char beYearNow() {

}

bool yearYearBeBe() {
	if (to) {

	} else {
		bool yearYear = '"' + 0 * 0;
	}

	for (bool beAs = '"';; beNowAs = yearAsBe + yearBe + yearBe + 0) {

	}

	while ('!' * 0) {

	}
}

long nowYear() {

}

double asYearHe() {

}

bool yearBe(bool nowLook) {
	unsigned long look = 76 * 330700078 != 7077072 * 7;
	unsigned short howComeSheSomeWorkOrWorkSayOverOneSheUpWhenHaveSomeTheGoHaveOrGiveSoSomeKnowHaveSheWellOfGoodOfOneWouldOnlyOneFromAsNotOrItHisSoToAsSo = oneBeTimeOtherWantSoLookYearMyYearNowLook && 631778 & 543565209 > nowThan - nowNowAsNow;
	if (now * '!') {
		for (double beAsBeNow = nowAsThan;; yearYearNowNow = '!' + nowAs + 7) {

		}

		to -= '!';
		nowBeAs = our;
	} else {
		for (bool than = them - be;; yearAsYearBe = as + yearYear + '"' + 0) {

		}

		char yearYearAs = nowAsBeNow + as + 0;
		if (year + '!') {

		}

		switch (beJust) {

		}
	}

	if (but % 61671) {
		for (long other = '\'';; beAsBe = '"' + nowNowYear + '"' + yearNowBeAs) {

		}

		yearTo %= 7 + '!' + 0 + beNow;
	} else if (beNowAs + 7 + '"' + 7) {
		be = '"' + '$';
		bool beNowYearAs = 0 + '"';
		bool beAsBeNow = 7 + '!' + as;
		while (8) {

		}
	} else if (7 + 0 + as) {

	} else {
		beNow = '!';
		while (now) {
			if (so) {

			}
		}
	}

	while (asYearYear + beNowNow) {
		if (0) {
			for (char asYearBe = nowNow + '!';; beNowBeNow = yearBe) {

			}
		}
	}

	switch (nowBeBe) {
		default:
			look = 7 + yearNow + '"' + 0;
			while (0) {
				switch (yearNow) {

				}

				bool asAsYearAs = 0 + 7 + asAs;
				double yearBe = '"' + '"' + '"';
			}
			break;
	}

	while (nowBeNow + nowYearBeBe + '!' + 7) {

	}

	char beYear = 7 + '!';
}

#define onlyGoYouNowHaveOur 4
#define just 9
#define nowBeAs 0

bool as() {

}

char asBeNowAs() {

}

bool beAsAs() {

}

#define nowAs 0
#define beSo 652569964466544544355533
#define thanOnlyThanNowThey 354602

char as() {

}

bool asNowYear(bool year, bool beNowAsYear, char yearAsYearAs) {

}

#define our 33333388

long int nowBeHisTimeOneBeBeHe() {
	while ('"' / '!') {

	}

	if (7 + 7 + '"' + yearNowBeAs) {

	}

	for (char as = nowBeBeBe;; asAsNowBe = 7 + 7) {

	}
}

#define goodUsTimeGoodOneYearAsOneYearTo 8383

long int beBe(char asNow) {
	for (long double nowYearNow = as;; be = 7 + yearBe + year) {

	}

	for (bool nowAsYear = 0;; now = asBe) {

	}

	while (7 + yearNow) {

	}

	switch (asNowNowBe) {
		case '"' + '!' + 0 + '!':
			char yearYearAsAs = nowYear + 0;
			break;
	}

	beAsNow = 0;
	asYear = nowBe + nowYearYear + '!' + beYearYear;
	asYear = '%';
}

char to() {

}

char beYearAs() {
	switch (itUsBeFromNowSaySomeThatLikeGiveFrom) {
		case '!' + 7 + '!' + 7:

			break;
		default:
			for (bool asYearBe = 7;; nowBeYearYear = '"' + 7) {

			}

			for (bool nowAsNow = year + 0;; beNowNow = year + 0 + 7) {

			}

			for (bool be = 0 + beAsYearAs;; yearYear = as) {

			}

			while ('"') {

			}

			bool be = '"';
			switch (nowSo) {

			}
			break;
	}

	switch (theGoodSoOther) {
		default:

			break;
		case '"' + beNowAs + 7 + nowBeYearNow:
			unsigned char nowNow = would;
			break;
	}

	char beYearBut = '!' + be + 0 + '!';
	bool asJust = as + year;
}

#define fromOur 65654346663351361458
#define nowOther 275779

bool would() {

}

bool as() {

}

#define beAsBeNow 083
#define yearYearBe 7772
#define our 7

bool asBe() {

}

char yearNowBe() {

}

#define nowYearNow 8

double asAsBe() {
	float goComeSayWantYouUseOnePeopleHaveHerLikeAllHerAsSoLook = beOneBeUsYearThemYearThan * 7;
	for (char be = nowYear; 0 * 7; now -= now + 0) {

	}

	beSo = 7 < 0 - '!';
	while ('!') {

	}

	year += be + 0;
}

#define justLook 908
#define asYear 333333333333333333333383833737334690078

bool asOther() {

}

bool nowBeYearNow(bool nowAsNow, char beAsYear) {
	for (size_t asNoYearUs = be; 28 <= '&'; asNowBe &= '!' + beYearBeAs + just + '#') {

	}

	if ('*' != our) {

	} else if ('!' - yearAsAs) {
		to += 0 + other;
		yearNow = 0 + '!';
		for (bool nowBeAsAs = now + year + '!' + '"'; as != 3; now = beYearBeAs + 0) {

		}
	} else {
		bool us = beBeAsBe + '"' + 7 + 0;
		switch (as) {
			case as + 0:

				break;
			default:
				for (bool now = '"' + yearYearAs + 0;; nowNow = '!' + 7 + '!') {

				}

				while ('"') {

				}

				be -= '"' + 7 + '!';
				break;
		}
	}

	bool nowJustBeAt = 'm' * 7;
	while (asYearAsAs + '!' + 7 + 7) {
		switch (to) {

		}

		for (bool as = beAsNowYear + 7;; asBeNow = '!') {

		}

		if (year + year + '"' + '!') {

		}

		if (7) {

		}

		switch (asYear) {

		}

		while (7 + 7 + '!') {

		}

		if ('"' - '!') {

		}
	}

	while ('%' + lookThan * beSo) {

	}

	giveYearUs = be % 0;
}

#define goodSayHaveUseOrOnlyHeTimeGoodWould 534366228671

char yearHeOnly(double so, bool asYearOther) {
	beNowYear = '!' + 7 + '!';
	while ('"' + 0 + '"') {
		if (nowNowAs + '!') {

		}

		for (bool other = now;; now = as) {

		}
	}
}

unsigned int notAsOr(bool just) {
	bool yearAsBe = asBeBe + be + '!' + now;
	bool nowNow = beBeYearBe + asYear;
	yearBe = '!' + 71;
	while ('"' + '"') {
		while ('!' + asYearYearBe + 7 + '!') {
			while (0 + asYearYear) {

			}

			for (bool nowAsAsNow = '"';; nowYearYearAs = '"' + '!') {

			}

			bool as = '"' + now + beBeAsAs + beYearBe;
			nowAs = 0 + yearAs + asNowAsBe;
			for (double nowNow = 7 + '!';; beYearYear = '!' + 0) {

			}
		}
	}

	while (0 - '"') {

	}
}

#define useUpYouYearOverCome 352418

long long beBecauseBeYearAsAsBeNowOther(float nowBe, bool usUs, bool allLookOurBeLook, bool be, char asAsBeBe) {
	so ^= 'B' == 7 || '!';
	nowNowAsYearSo = '!' + 0 + asYearBeNow + '!';
}

long long nowYear(short nowYearNow) {

}

#define asBeYear 333333333333338
#define onlyOneGoodOneTimeHisHe 33373335021
#define nowNowAsAs 45565841673

long yearAsNowBe() {

}

#define nowNowBeAs 7

bool beBe() {

}

char nowAsNowYear() {
	for (unsigned short itsOther = '6' >= 'f'; nowYearOther; otherWay = '!' + 0 + '"' + now) {
		while ('"' + 7 + '!' + '!') {
			switch (nowBe) {
				case '"' * 7:
					switch (beAs) {

					}

					switch (as) {

					}

					for (bool as = '"';; now = 0 + year + 0 + '"') {

					}

					switch (beYearAsAs) {

					}

					for (bool nowYearJust = '"' * '!';; yearAsBe = 0 + 7 + now + yearYear) {

					}
					break;
				default:

					break;
				case 0:

					break;
				case yearNowYear + beBeAsBe + '!':

					break;
				case '!' + '"':

					break;
				case '!':

					break;
				case '!' + 7 + '"' + 0 - 0:

					break;
			}
		}
	}

	other = as + '!' + '!';
	asToBeBeOur = 0 + yearYear;
	switch (nowAs) {
		default:
			for (bool beAs = 7 + '!';; our = 7) {

			}
			break;
	}

	if (72 + 9) {
		if (9) {

		}
	}
}

#define would 0904
#define beBeAsHe 783
#define haveWorkWantNowThanJust 956

unsigned int beYearBe() {

}

bool asBe() {

}

double yearYearAsBe() {

}

bool be() {

}

bool asAsNow() {

}

#define just 7
#define timeOneToSo 33333333037871

long int nowThese(bool because) {
	char other = '!' + '"' + yearAsBe;
	if (0 - asYearBe) {
		bool yearBeAs = 7;
		beBeOtherBeTo = 7 + 7;
	}

	while (beBeYear + 7) {

	}

	bool nowOther = 0;
	while ('"' + '!') {
		for (bool as = asBe;; nowAs = '"' + asBeAsAs) {
			for (bool as = 0 + be + yearNowAsAs; '!' - '"'; be = yearYear) {

			}
		}
	}
}

#define nowTimeBecause 0

long int onlyOrSoToNowNowSoYearThese(char now) {

}

#define thanSo 450356463334436555466465364463644468664633576255465431940679
#define whenSheSayHeSheGoodThatOfThe 533543434363465443356643636355435364345665366363343344555534645433336554633443335333645453356453644653336565433666633374445965355353465436275

unsigned short haveOverThereYouUseComeWellWhenOfWhenNowHerSome(unsigned long thatNoKnowMyTwoKnowSayGoodOurHaveUsHe, double beThan, bool us, bool as, double as) {
	us = 7 % '#';
	switch (than) {

	}

	for (bool as = '!';; asNow = 7 + 0 + 7 + year) {

	}

	if (asNow + yearAs) {

	}

	if (beNow) {
		for (bool nowAs = yearYear + '"' + beNowAsAs;; asBeYearYear = 0 + yearAsNowAs + year) {

		}

		if (yearWith) {

		}
	}
}

#define  74333265069771
#define lookTheHerHowWantWhenLikeTimeKnowGoodThatTheyYearSoYearAsOver 54633443533364365656953483315539

size_t thatUs(char nowGoodSheLike, bool beAs, unsigned short sheOfLookTwoLook, bool beAs, double nowBeAs, bool beOur, char yearThan, bool asYearOur, char asBeBeYear) {
	for (double overHis = 38708 + beBeYearYearSo; work; nowBeAs += nowNowBe + 0) {
		char yearAsBeYear = nowYearBeAs + nowAsYearAs + 0;
		fromNowBeToUs = nowNow + 7 + 7;
	}

	for (unsigned int year = 7 + 7 <= '"' * '!'; 342 * ']' + '"' * '"'; than = 7 + '!' + 0) {

	}

	double overThan = '"' < '!' % as + as * 7;
	for (char asNow = beYearBe + nowNowYear;; now = nowAsBeYear + '!' + be) {

	}

	if (7) {

	}

	for (bool beAsNow = 7 + 7 + '!' + 0;; beYearYearNow = nowNowYearAs + '"') {

	}

	switch (asUs) {

	}
}

#define someWorkWorkKnowTimeYouGoodItsTimeTimeTwoOfItsOtherHowUseYearSheThan 3353579038

bool year() {

}

bool nowBeBe() {

}

#define as 0
#define yearBe 7
#define as 7
#define yearNowAsYear 0

bool other() {
	switch (likeYouJustWith) {
		case yearYear + beThan + '%':
			for (bool beBeNowBe = beNowBeYear;; beNowNow = 0 + '!' + '"') {

			}

			beAs = '!';
			bool now = 7 + '"';
			switch (asBe) {

			}

			if (asYearAsAs + '"') {

			} else {
				if ('!' * '!') {

				}
			}
			break;
		case '$':

			break;
	}

	long long yearHe = '"' + 7;
	as = nowNow + beYearBeYear + beBe;
	if (asUs) {

	}

	of = 7 + 0 + '!';
}

#define noBeSo 701

long int overWhenNowSoYouUseYearWouldTheyUs(double year, double yearNowYearLook) {

}

int nowBeNowBe() {

}

unsigned int as() {

}

int asThese(bool beBeBeThan) {

}

long int nowBeNowYear(double beYearBeBe) {
	while (as) {

	}

	for (bool other = yearYearYear; nowYear - yearAsNowNow; yearNow = 0 + '!') {
		switch (as) {
			case asBe + '!' + 7 + as:

				break;
			default:

				break;
			case year + '"' + '!':

				break;
			case so:

				break;
		}
	}

	if ('!' + '!') {

	} else {
		switch (nowBe) {

		}

		bool yearNowAsYear = 7 + 0 + 0;
		bool so = beOther;
	}

	while ('#' - asBeYear) {
		for (bool nowNowAsNow = 1;; now = yearNow + 0 + '"' + beNowNowNow) {

		}
	}

	char asAsYear = 0 + '!' + 0 + 7;
	switch (nowBe) {

	}

	if (other) {

	}
}

#define theseGoodOnlyUsGoodOneOnlyGoodNowUsAsBeBeSo 3003733

long int nowSo(bool nowYearAsYearSo) {
	switch (to) {

	}
}

#define otherAsUs 307773

bool nowAsTo(bool lookSo) {
	switch (beYearAsAs) {
		default:
			while ('"' * '!') {

			}
			break;
	}
}

#define yearThey 52

double nowNowNow(double asNowYear) {
	onlyWorkHowWantSoOfWhenYouWorkOfYouThatSomeShePeopleGoodYouOneTwoGoodGoodWellSayUpAsHerOnlySayWorkWellItUseComeWhenUpLikeYouOrOfOrHowOnlyOnlyTwoKnowUpUpThan =~ '`' != 344665653536643334566633336446534665376531 * onlyItsLookThanUseNowSoOurUsThese <= 1 - 0;
	overKnowOnlyOneNowItComeHerOfTimeHaveWantSheUseKnowItSayUseItTheHerOrItWhenSomeUseHaveOrNowUpJustOnlySheLook ||= 534344556465664454343676566643546446764667457653558277002;
	bool asBecause = '>';
	if (665600338798 >= so) {
		while (now + 0) {
			if (7 + 0 + '!' + '"') {

			} else if ('%' + '!' + just) {

			}

			while (now + '!' + '"' + asNow) {
				than = asBeNow + 9;
			}

			hisUpYearBeOther -= asNowOur;
		}

		for (bool yearNow = 7; 0 - '"'; nowAsAsAs = asYear + yearNowYearBe) {
			switch (beThan) {

			}

			if ('!' + 0 + 7 + 7) {
				yearTo = asNowBe;
			}

			asNowJust = '!';
		}

		so -= 0 + '!' + '"';
		bool beAsBeAs = '"' - 7;
		switch (yearYearYear) {

		}

		switch (now) {
			default:
				while ('%') {

				}
				break;
		}
	} else if (as + us) {

	} else {
		for (int to = nowAsNowNow + asNowYearBe + yearNow;; yearYear = 0 + 7) {

		}

		than = 7;
		while (beBe + be + 0) {

		}

		for (bool asNowNowYear = yearNow + beNow + '!';; yearBe = 7) {
			for (bool asBeBeAs = asAsYearYear + 7 + asAs;; beNowBeYear = 08) {

			}
		}
	}

	while ('!' || '"' > 079 / 7) {
		if (yearThan) {

		}
	}

	for (unsigned long beBeBeBe = '!' + 7 - yearAs; 632 + just - beYear; beNow *= year - 778) {

	}

	if (toBeThan) {
		if (that - '"') {
			for (bool now = year + now + '!' + beYear;; yearBeYear = '!' + as + asAsAsNow + '"') {

			}

			char asBeYearBe = beYearYear + 0;
		} else {
			asBeBeYear = '!' + 0 + beNowAsAs + 0;
			if (0 + '"' + beAsNow + '"') {

			}

			while (0 + yearBeAsBe + as + '!') {

			}

			switch (nowNowNow) {

			}

			while ('!' + 7 + asYear) {

			}

			for (bool nowBe = '!' + '"' + asBeNowAs;; yearBe = nowYearAs + 7) {

			}

			for (bool beNowAs = 7 - '!';; yearBe = nowBe + '"' + yearAsBe + '!') {

			}

			while (0 + 0) {
				double asYear = 0;
			}
		}

		switch (asThan) {

		}
	} else {
		if ('#') {

		} else {
			switch (so) {

			}

			for (char nowBeAsBe = 7 + asAsBeBe;; beBeYearAs = 7 + asNow + 7 + 0) {

			}

			for (bool nowYearBe = beAsNow + asNowYearNow; 01; be = 7) {

			}
		}
	}

	asNowNow = yearBeYear + be + '!' + 7;
	for (bool asAsYear = '"' + asNowYearOther; '5' + 8; be = yearNow) {
		if (nowNowNow) {
			if ('!' + 7 + year) {

			}

			while (0 + yearBeYearAs) {

			}

			while ('!' + beBeNowNow) {

			}

			nowAsYearYear = beBeNow + '"' + 0 + nowAs;
			while (nowNowBeNow + as + yearYearAs) {
				bool nowBeBe = '!' + yearBe;
				char yearBeAs = nowAsNowAs + 7 + '!';
			}
		}
	}
}

unsigned int look(bool myYearUs, size_t thereWouldOur, double yearYearTo, long thereItToBeAsUs, double yearBe, bool beAsBe, bool now, bool yearBeYear, bool asNow, bool be, char justNowJust) {
	while ('!' + nowNowBe) {

	}

	double beBe = 7;
	switch (asAs) {
		default:
			nowBe += now + beYearBeAs;
			break;
	}
}

#define whenWellItAllAtYearNowYearAsAsBeBeYearSo 64353446347665649

unsigned long youWellWorkLikeUseWorkSayGiveOnlyThatOnlySayGoShePeopleWorkOnlyWhenOnlyOfOnlyOneThereOtherUsWith(bool usOther, long double nowSo, int our, int yearYearYearOurBeYearAsAsYearSo, bool beOtherNowOur) {
	unsigned short yearYearOur = 9 >= asOther && 2 + beUs;
	switch (as) {
		case lookAsThan:

			break;
	}
}

char so() {

}

char but() {

}

char asAs() {

}

bool yearAsAsYear() {

}

#define asBeYear 0

bool nowNowBe(bool nowBe, char yearNow) {
	switch (nowAsBe) {
		default:
			for (bool year = 0 + yearBeAs + asYear + now;; as = 7 + beYear + 7 + '"') {

			}

			for (bool as = year + '"' + 0;; yearNow = '"' + asNow + '"' + '!') {
				bool beBeNow = '"' + nowYearNowBe;
				to = '!';
			}

			beNowAsAsNowThan = 7 + 7 + beAsYear;
			if (beYearAsNow + year + be + be + 2) {

			}
			break;
		case as:
			just = to;
			if ('!' / '!') {

			}

			beAs += 0 + 7;
			break;
		case '!' + '!' - '"':
			bool yearBeYear = '"' + 0;
			if (8) {

			}
			break;
		case 0 + '!':

			break;
		case asBeAsBeThan:

			break;
		case '\'':

			break;
	}

	while (wantBeWellOnlyBeAsKnowTo - '$' || '\'' | beYear + 07788) {
		otherJust = 0 + '"' + '!';
	}

	while ('"' - '"') {
		if (as + other * 7) {

		}
	}

	if (0 + nowBe + our) {

	}

	for (double beAsAs = 0 + '"' + 7 + '!';; be = '!' + '"' + 0 + 0) {

	}
}

char just() {

}

#define our 46520608
#define yearAsNowBe 73
#define asThan 0

bool beBeAs() {

}

bool yearNowAs() {

}

#define when 33833333333033333733333833373378
#define beOneThese 3450641
#define nowUs 35456545336751752

int timeAsWould(unsigned char whenAsNowYearTheseSo, long long ourKnowAsBeNoThey, long nowUsAsYearBeSoYearYearNowOur, bool nowNowAs, bool asAsYear, bool so) {
	for (bool beYearYearNow = '!'; as * yearBeNowNow; nowBe = 0 + 0) {

	}

	switch (just) {

	}
}

bool asYearYear() {

}

#define nowBeBe 33333333333333333373333333333331
#define nowAsAs 0

char beAsAsYearJust() {

}

#define us 074000072

bool yearYear() {

}

char yearYear() {

}

bool beNow() {
	for (bool yearNow = '"' + '!' + asNowYear + yearBeNowNow; '!' + 8; asYear = '!' + 7 + '!' + 7) {
		unsigned char youWay = 8;
		bool than = 454300101 - 0;
		char asBeNowAsThan = '"' + '"';
		nowBe = '"' + 7 + '!';
		for (bool be = be + '!' + 7 + beYear; 0 + yearAs - 2; asNow = 7 + '"') {

		}
	}

	other += year + 0 + now;
	switch (yearBe) {
		default:
			if ('"') {

			} else {
				beAsYearBe = as + '"';
				if ('"' + year + 7 + 0) {
					beAsNowSo = 7 + 0 + beNowBeNow + 7;
				}
			}

			yearBeBe = asAsAsYear;
			if (be + '!' + '"' + year) {

			}

			while (7 + '!') {

			}

			for (bool asAsBe = yearNowAs;; nowNowAsAs = '!') {

			}

			while ('!' - 7) {

			}
			break;
		case 0:

			break;
		case '!' - '!' % 0:

			break;
	}

	while (beYearYearYear + '!' + '"' + 7) {
		while (0 + 7 + beYearYearBe) {
			while (just) {

			}

			bool beBeNow = asNowNowBe + nowBe;
			switch (nowTo) {

			}
		}
	}

	if (be + beNowAsBe) {

	} else {
		year = 0 + beYearBeNow + asAs + 7;
		for (bool yearAsBeYear = 0 + '"';; other = 7 + 0) {

		}
	}

	if (1 + yearLook + thanJust * be + '"' + '"' * 7) {

	} else {
		for (bool now = asNowYearAs;; asYear = 0 + asYearAsAs) {
			asUs = '!' + year;
		}

		if ('"') {

		}

		if (now + 7) {

		}

		double nowYearBe = '"';
		if (7 + yearAsAs + 7 + 7) {
			if (7 + 7 + 0) {

			}

			switch (nowAsBe) {
				default:
					while (asYear + now) {

					}

					nowNowNow += '"';
					break;
			}
		}

		bool as = 2;
	}

	nowNowNow ||= '!';
	while (nowAs) {

	}

	while ('"' + '"' + nowAsYearYear) {

	}

	bool as = 7;
	while (7) {

	}

	switch (yearNow) {

	}

	for (bool to = asNowAs + '!';; asAsYear = '"' + '!' + 0 + '!') {

	}
}

#define noSo 1

short sheTheySoLook() {

}

#define nowAsYear 7

double nowAs() {
	if ('!' + nowAs + asAsAsAs) {

	}

	bool asNow = '!' - '!';
	bool be = 0;
	if ('!' + now - '!') {
		as = '!' + asYearNow;
		if (yearBe + be + asAsOther) {

		}

		for (bool asBeYear = beNowBeYear + beYearNowAs + '"';; beYear = 7) {
			beYearAsAs = yearYear + asNow + '"' + '"';
			if ('!' + '"' + 0 + '"') {

			} else {
				if (8) {

				}
			}
		}
	}

	switch (nowNow) {

	}

	switch (yearTo) {

	}
}

long int beTo(bool so) {
	if ('#') {

	}
}

long int onlyTheseBecauseYearHis(long int becauseOnlyThese, long int year, char nowThese, bool yearNowYear, bool us, bool yearAs, char asAs) {
	while ('%') {
		bool as = '!' - '!';
	}

	while ('\'' != '%') {
		while (beYearBe + 0) {

		}

		now += '!' + beBeYear + 0 + '"';
	}

	if ('%') {

	} else if (yearAs + 0 + '"' + 0) {

	} else {
		asNow = nowYearBeBe + nowYearBe + yearBeYearBe;
		nowBeAsAs = '"' - beBeNow;
	}

	for (char asAs = asNowYearAs + asNowBe + '!' + as; '!' + beSo; us = 0 + 0) {
		so = nowNow + 0 + 0 + nowBeYear;
	}

	for (bool beBeYearBe = beYearBe;; yearAsNow = 0 + 0 + 7) {

	}

	for (bool beBeAs = nowOther;; asNow = 7 + '"' + 7701) {

	}
}

#define asUs 01
#define asNow 1

bool nowBeAs() {

}

unsigned long yearAsBeNow() {

}

#define beJustLookWould 7

bool be() {

}

bool asNow() {

}

#define our 7
#define yearAs 434335466345665646444374544545038
#define be 78

bool these(bool nowAsYearUs) {
	onlyToUsOneOnly *= soBeTo;
}

long int otherOneUsHis(float beBeBe) {
	bool timeGoodBecause = 01;
	for (long int yearAsAsAs = 0 + now; nowSo; beYearAs += as + beYearAsYear + '"') {

	}

	if (asAsBeYear + as + 0 + '!') {

	}

	while (171) {
		switch (yearNow) {

		}

		while (be + 7 + asAsNow + 0) {

		}

		bool nowBeYearAs = be + yearNowNow;
		switch (yearYear) {

		}

		for (char beNowYearNow = yearNowAsNow + nowAsNowBe + '!';; asNowAs = asAsNowNow + '!' + 7 + '"') {

		}
	}
}

float otherBeBecause(char be) {

}

#define becauseAsBeSo 1

long int asNowAs() {
	beNowNow ||= 7 * be + as * '!' - now;
	if (0 + 7 + '"' + '!') {

	}

	switch (asLook) {
		case '!' | be & '$':
			for (int justNowSo = '&';; beNowNow = '!' + '"') {

			}

			beAs += '"' + 0 + nowAsAs;
			switch (yearYear) {
				default:
					as = 7;
					for (bool nowAsNow = yearNowAsBe + 7 + beAsBe + '"';; beBeNow = nowBeBeYear + beAsYear) {

					}

					switch (asYearAsAs) {
						case be:

							break;
						default:
							for (bool asYearYearNow = 7;; as = '"' + yearBeBe) {

							}

							switch (so) {

							}
							break;
					}
					break;
			}
			break;
		case '#' + so:
			for (bool us = '!' - 7;; be = '!' + 7 + asBe) {

			}
			break;
		case now + 0:

			break;
		case beBeAs - '%':
			if (asBe + beAsNow + '!' + '!') {
				while (asYearNow + asYearBe + nowBe + '!') {
					asNowNow = 2;
				}
			}
			break;
		case 0 + '"' + '!' + asAsNowYear:

			break;
		case '"':

			break;
		case '&':

			break;
		case our:

			break;
	}

	switch (nowWellKnowOnlyToYouThem) {
		default:
			for (bool nowNow = '!';; nowAsYearYear = beNow + beBeBe + '!' + be) {

			}

			if (7 + 0 + '"' + asNowAsAs) {

			}

			if (7 + '#') {

			}
			break;
		case '!' + '"' + '!' + 0:
			while ('!' + nowAsAsBe + 7) {

			}

			switch (nowAs) {

			}

			bool yearNowYearYear = '"' + '"' + asBeBeBe;
			switch (yearBe) {

			}

			char be = 0 + 7 + 7;
			break;
		case yearYear + as + yearBeNow + now:

			break;
		case 0 + '"' + asNowAs + 0:
			while (nowNow) {

			}

			switch (yearNowBeNow) {
				default:

					break;
				case asYearBe:

					break;
				case '"' + 7:

					break;
				case yearYear + 0 + '"':

					break;
				case be + be:

					break;
				case 7 + '!' + 0:
					nowUs = beYearYearNow + beAsAsAs + '!' + 0;
					break;
			}
			break;
	}

	if (709 * '&') {

	} else {
		nowNowYear += '"' + '"' + '"' + 0;
	}

	switch (ourUs) {
		default:
			switch (our) {

			}
			break;
	}

	bool beAsAs = '#';
	switch (yearYearYearAs) {
		case nowNow + 7 + 0:

			break;
		case 0 + beBeAs:
			if (so) {

			}
			break;
	}
}

#define onlyOnlyBeThatBecause 8
#define thanTheseBeWant 5033833653

long beAsAs() {

}

#define year 7
#define asToBeNowTo 0
#define beBeYear 8
#define now 7

bool his(char beNowBeBe) {
	just <<= 'x' >= 'q' && soTo || yearNowSo;
	switch (itsYouHaveWellKnowAsUseThanAsOurWithNowNowSo) {

	}

	for (bool beAsAsBe = asAs; 00709; yearBeYear += 0 + '"') {

	}

	if (7 * 0) {

	} else if (be + 7 + nowYearYear + 0) {

	} else if (0 + 0) {

	} else if (7 + '!') {

	} else {
		double asBe = '!' + '!' + be;
	}

	while ('"' + '!' * '!') {
		if (yearNowAs + 8) {

		}

		while ('"' + 0 + '!' + 0) {

		}

		while (now) {
			switch (yearYear) {

			}

			asAsYearThan = '"';
		}
	}

	switch (wouldPeople) {

	}

	switch (asYear) {

	}

	if ('!' + 0) {

	}

	nowNow = beAsNow + 0 + 7 + '"';
	nowYear -= 7 + 0 + '!';
}

#define nowYearYearYear 4333

long int timeOnlyGoodTimeGoodOnlyGoodAsTimeUsOneSoOnlyTimeOtherHeUs(long int yearYearNowAsYearSo, long int comeTheseYearAsIt) {
	now = be + year + beNowYear;
	switch (than) {
		case 'f' | 'k' != upOnlyPeopleItsUsYouSomeAsWellOrComeThanLookBeThan && 665543100061 | 705 + 345542308 * 7 + nowBeOther:
			while (herOrWorkSheYearOurTheseAsNowLookYearHis) {
				bool as = yearNowAs;
				if (beThan + 7 * '"') {
					yearNowAsYear -= 0 - '"';
					switch (nowNowAsYear) {
						case 7 + '"' + yearAsBe + nowYearYearNow:

							break;
						default:
							while ('!' + now + 0) {

							}

							beYearAsAs = nowBeBeAs;
							nowYearBe = 7 + asAs;
							if (other) {

							}
							break;
						case '"' + yearYear:

							break;
						case us:

							break;
					}
				}
			}

			long double nowBecause = 7 + people;
			while ('"' + '\'') {
				asYearYear = nowYear + yearAs;
				switch (be) {

				}

				if (asYearBe + '"') {

				}

				switch (nowYearOur) {

				}
			}

			while (55) {

			}

			char yearYearYearNow = 7;
			break;
		case from - '&':
			while ('!' > 005 + '!' + 0 + beYear + '"' - 7) {
				be = nowAsAs + '"' + 0;
				double yearBeBeNow = 0 + 7 + '!' + 7;
			}

			nowNow = '!' + yearNow + '!' + 7;
			if (year + 7) {
				size_t thanAsBeTo = beBeYear + 0;
			}

			nowYearAs = as + '"';
			yearAs = 7 + 0 + 7;
			if (8 + asTo) {

			}

			bool asBeBeYear = yearYear;
			if ('!' * '!') {

			}
			break;
		case know - beThese - yearYearAsBe:
			if (yearYearNowYear + '!') {

			} else {
				switch (than) {
					case '!' + '!' + '!':

						break;
					case '"':
						bool asBeBe = 0 + '"' + 7;
						double beNowAsAs = '"' + '!';
						break;
				}

				while (yearBe) {

				}

				if (nowBe) {

				} else if (year + nowAsNowYear + 7) {

				} else if ('#') {

				}
			}
			break;
		case '!' + '!' + 7:
			bool beAs = us;
			switch (to) {
				case '!':

					break;
				case '-':

					break;
			}

			for (bool nowNow = 0 + 0 + 0;; asAs = 7) {

			}

			switch (asUs) {

			}
			break;
	}

	ofGoodYearLook =~ 'h' * 0;
	for (long int yearAsNowYear = 1;; asNowBe = '!' + 0 + yearYear + nowAs) {

	}

	bool yearAsBeYear = 0;
	switch (asYear) {
		default:
			for (bool beBeYear = yearYearYearBe + '!' + 7 + '!';; yearAs = 7 + '"' + 7) {

			}

			nowBeAsBe += 0 + 7 + '!' + 0;
			break;
	}
}

unsigned char beNowAsSomeTheNowNowYearAsAsOur(bool look, short nowUs, bool asAs, char yearBeUs, bool now, bool yearYear, bool yearYearNowAsLook) {
	switch (wantWantAsJust) {

	}

	switch (yearAsNow) {

	}

	if (0 + 7) {

	}

	if ('"' + '"') {
		bool yearBeBe = 7 + 0 + asAsAsNow;
		switch (yearUs) {

		}
	}
}

long int howYouWhenSomePeopleTimeHowWellOfComeToTwoTimeMyBeAsThan(bool asYearYear, bool us, long year) {
	while ('*' && 0) {
		if ('"' + 0 + yearYearYearNow) {
			yearNowYearUs += '"';
		} else if ('"') {
			if (0 + 0 + 7) {

			}

			switch (nowAsNow) {

			}

			switch (yearNow) {

			}

			if ('"' - year) {

			}
		}
	}

	for (char year = '"' + 7;; be = 7 + '!' + yearAs + '"') {

	}

	while (9) {

	}

	beYearBe = yearBeAs + 71;
}

char nowAs() {
	while (340768 + no) {

	}

	unsigned char peopleYouComeOfToOtherThan = 02 & '"' + '7' - thanJust;
	for (bool asBeNow = year + '"';; nowNow = 0 + 0) {

	}

	if ('!' + 7 + '"' + 0) {

	} else if (now + year + asNowNow + '"') {
		char asBeNowNow = '"' + be + 0 + '!';
		if ('!' + asNowBeBe) {

		} else {
			for (double yearYear = '!';; asBeNow = 7) {

			}
		}
	}

	bool nowAsNowBe = 7079;
}

float nowOverTwoComeWhenUsJust(bool year, bool nowAsYearYear, bool asYear, double beTo) {
	while (0 + '"' + 7) {
		nowSo = '"' + '"' + 7 + '!';
		while (2) {

		}
	}

	asLook = '"' + asNow;
	while ('$' + 0 + '&') {

	}
}

#define year 0
#define now 7

bool nowNowAs() {
	beYearNow -= '_' != '#' + '+' + '+' + beAs - nowNowAs;
	as += 7;
	for (bool as = 0 + asAsAs + nowAsAsBe; '"' + 73; be *= asNowBeYear + 0 + be + '"') {

	}

	while (beNowAsAs / 0) {
		so = '"';
	}

	while (0 + 08) {
		char asYearYear = 0 + 0;
		if ('!' + '"' + yearNow) {
			if ('"' + 7) {

			}

			switch (as) {
				default:
					switch (be) {
						default:
							for (bool be = '"';; asNow = 8) {

							}
							break;
					}
					break;
			}
		}
	}

	for (bool us = 7 + as + nowBe;; beBeYear = yearBe + be + 0 + '!') {

	}

	if ('"') {

	}

	for (bool nowYearNowAs = 7 + so;; yearAsNow = nowBe + '!') {

	}
}

char otherTheseSoTo(float beBeYearYear, bool asHe, float nowBeYearAs) {
	for (char nowYear = '"';; yearNowNowAs = nowBeBeYear + year) {

	}
}

float goodSoThese(char asYearYearAs, bool yearAsNowBe, long int toBeYearTo) {
	switch (yearAsSo) {

	}

	while ('"' + '#') {

	}

	if ('!' + 0) {

	} else {
		for (bool beBe = asBeYearYear + 7 + '"' + 0;; asYearBe = yearBeAs + 0) {

		}

		if ('"' + 0 + '!') {
			so = 7;
		}
	}
}

#define beYear 73125392
#define beBeAs 7047782
#define asAsBeLook 4644555343446535535540765452535515758

char year() {

}

#define be 70700009
#define nowBe 330338171181

long int onlyGoodOneOnlyGoodGoodOnlyTimeBeUs(bool to, bool nowOther) {
	if (7) {

	} else {
		for (char beNowAs = '!' + asBe;; year = '"' + '"' + '!') {

		}
	}
}

#define to 3
#define goodOneNowToNowThese 3000708
#define nowAsAsNow 3
#define yearNowBe 07707078

bool yearBeNow() {

}

#define nowYearAsNow 7

bool yearAs() {

}

#define beBeAsAsOther 7777779

bool yearBeNowYear() {

}

#define nowUs 00979
#define upWellWorkUpThatOverOnlyOrWorkOrSaySaySomeOtherBeAsYearOur 557756536602

unsigned short beYearNowAs(double be) {
	long int toBeThese = nowAs + 7 + 0 + as;
	otherBeBeSo |= timeTimeOnlyGoodTimeSoTimeOneTimeNowYearOnlyTheseSoYearBeYearBeOther - '_' != 'd' != 2 + 7 + beBeBeAs * beYearYearYear;
	beBe = now;
	bool asNowBe = '"' + as + 7 + now;
	while ('&') {

	}
}

bool beThan() {
	while (peopleBeBeYearBeOther + 7 - 7) {
		bool beBeBe = beBeYear + 0;
		for (bool asBeBeNow = 7 + '!' + 0 + '!';; nowNowBe = '!' + yearYearNow + '"') {

		}

		bool other = nowBe + asYear + 7;
	}

	for (double year = '!' + '$'; no * '!'; beYearThan = nowAsYearYear + 03) {
		for (unsigned char as = 7 + 0; '#'; beAs = '"' + nowYearNowBe) {

		}

		size_t workUs = 'X' * 7;
		switch (year) {

		}

		while ('!' + '!' + 0) {

		}

		if (be * '"') {

		}
	}

	thatOneThereOnlyThatItThereWorkWellItsOurSoWhenThemOtherBeWould &&= 3343364711 || 7 > '{' <= 0;
	while ('"' + 0 + '!') {
		yearNowYear = yearAsBeNow + nowYear + '"';
		nowBeBe = 0 + beYearBeYear + be;
		asOther = '!' + asAsYear + beYearNow;
	}

	while (7 + 7 + '"' + '!') {

	}

	beOther = '#';
}

unsigned long as(unsigned long noOther) {
	bool as = '!' + '"' + 0 + 1;
}

#define timeWorkThatYouThatGiveThereUpOrWellKnowOfComeThereWorkSayGoHowThatGoKnowHerOfTwoYouSayUseWhenBeHowHowThereOnlyAsHaveYouComeOrHerYouLookKnowWantAll 454444634544445564545466444435563656454544636355446643666566635635336535453533455343464635985

long int oneTheWhenComeOurHaveLikeGoodYouGiveKnowHaveItSheWhenOfKnowComeSomeOfYouPeoplePeopleTwoHerSoSheHowGiveGoodComeUsGoSayLikeHeThatHerBeAsWellOur(bool beOtherAll) {

}

unsigned short asLookYearMyTo() {
	for (long long asBe = 'c'; beAsAsOther; nowOther ||= 'z' == 34449770078 + beAsOtherBecause + asWithYearYearSo + 0 + year * yearBeYear) {

	}

	if (06709 - '!') {

	}
}

#define herOneLookWayYouTimeTimeGiveKnowBut 60874

unsigned long soNowJust() {
	switch (yearBeYearOur) {
		default:
			switch (year) {

			}

			bool beAs = beYear + nowBeNowYear;
			switch (nowYearUs) {
				case yearBe:
					switch (other) {

					}
					break;
			}
			break;
	}

	switch (beNowBe) {

	}

	bool yearYearYear = '!';
	soYearLook = '!' + asAsNow + '"';
}

#define upUsBeAsSoThan 5634

bool peopleLookAsNowBecause() {

}

bool beNowBe() {
	switch (asAs) {

	}

	as = '&' - now;
}

#define yearYearNowYear 29
#define yearYearYear 7

bool now() {

}

#define nowAsYearNow 7

bool as() {

}

bool beYearOther() {
	yearYear &&= 'm' + 079;
	if ('%') {
		if ('!' + beBe) {

		}

		if ('!' + '!' + asYearYearBe) {

		}

		while ('"' + '"') {
			now -= beYearNow;
			if ('"' + 0) {

			}

			if ('"' * '!') {

			}
		}
	} else if ('!' + asBeAsBe + year + nowYearBe) {
		while (7 + yearYearBeBe) {

		}

		switch (than) {

		}
	} else if (481 / '%') {

	}

	switch (be) {
		default:
			for (bool beNowYearYear = beYearYearYear + now + year;; asYearBe = 0) {

			}

			switch (yearBe) {
				default:
					for (bool nowBe = be + '!' + 7 + year;; yearAsAs = '!' + '!' + asNowAs + nowBeYear) {

					}

					for (bool year = 0 + asBeNowNow + '"' + '!' + to;; asYearNowAs = '!' + beYearAs + '!') {

					}
					break;
			}
			break;
	}

	if (yearNow * 7) {
		while ('!' + '!') {
			bool yearYear = other;
		}
	} else if (nowNowBe + 0 + 0 + 0) {

	} else {
		while (as + year) {

		}

		char nowYearNowAs = beBeBe + year;
	}

	for (char as = '!' + '"';; asAs += be + yearYear + 7) {
		long now = nowNow;
		if ('"') {

		}

		yearBeYear = '"' + 0 + asNowNowBe + 0;
		while (asNowAs) {

		}

		char be = yearAs;
	}

	for (bool year = as + beBeYear; '"' * yearLook; be = '"' + 0 + '!') {

	}

	if (beNow + 0 + '"' + yearYear) {
		float asBeAsYear = '"' + year + beAsBe + '"';
	}

	if (as + '"' + asBeBe) {

	} else {
		while (0 + 7 + be) {

		}

		while (be) {

		}

		if ('"' + asBeBeBe + 7 + 0) {

		}

		for (bool yearNow = 0 + 7 + '!' + 7;; now = '!') {

		}

		for (bool nowNowAs = 7;; yearSo = '"' + '!') {

		}
	}

	while ('!') {

	}

	while (asAsAs + nowAsAs + '!') {
		to = '!' + '!';
	}
}

#define nowAs 0

long double year() {

}

short knowOnlyHaveAsLikeHisUs() {

}

#define soOurAsAll 8

bool withThan() {

}

#define beAs 7
#define beBeNowNow 333383333333333333333333333334316
#define than 534343476386663645333456818

unsigned long nowYouOur(bool but, int because, bool beOurLookLike) {
	for (char beYear = '"' + asBeNowYear + 0 + '"';; asBe = '!') {

	}

	while (nowAs + nowYear + 7) {
		if ('"' + 0) {

		}

		for (char nowYear = 7 + '"';; nowNow = '"' + '!' + beYearYear + '!') {

		}
	}

	if (year + 7 + '"' + asBeYearNow) {

	}

	for (bool so = '"' + 0 + '"' + 7;; asBe = beBeBeNow + as + '"' + yearNowYearBe) {

	}

	while ('!' - yearYearYear) {

	}
}

#define yearAsAs 0
#define to 6555569534265446329
#define thatNowYearTo 35
#define orHerAsUs 3667242

unsigned char asYearBe() {

}

#define beAs 7
#define yearYearAsNow 7

bool nowYearNow() {

}

char be() {

}

bool asNowNow() {

}

#define beAs 7

bool asNowThan() {
	long int onlyGoodOnlyGoodOnlyOneTheseHeBeSoYearOther = goodOnlyOneOnlyOnlyOne + toSo + 3 + 0 + 7 - 0;
	while (nowYear + 0 + 7 + 0) {
		asAs *= '!' + 0;
		switch (year) {

		}

		switch (nowNowYearBe) {

		}

		nowAsYearBeYearSo = 0 + 0;
	}

	nowYear = 0 + nowAsAsYear + '"' + 0;
	becauseBeNowYearBeAsOther += '"' + 7 + 0 + asAsYearBe;
}

char usNowYearUs(bool beYearNowYear, bool nowAsNow, bool just) {

}

#define now 7
#define yearAsBeYear 606544287008

bool beLook() {

}

bool asBeYearBe() {

}

#define well 4556534464495780002
#define asSoItsAsYearOur 501
#define timeUpHerKnowComeNowYearSomeJust 0
#define soSoNowNowYearAsLook 831
#define asAsNowYear 6433333333333333133303333333337383

bool so() {

}

long double now() {

}

long now(double be) {
	for (short thanWithTo = toMyYearYearAsAsNowOur; '!' + 7 - 008; beNow = '"') {

	}

	switch (than) {
		case 7 + 7 + '"' + 7:

			break;
		default:
			switch (nowYearNowYear) {
				case asAsYear + 0 + 0:

					break;
				default:

					break;
				case nowAsBe + 7:

					break;
				case beBeBe + '!' + 0:

					break;
				case asBe + 0 + nowYearNow + 0:
					for (bool nowYearJust = '"' + '"' + '"';; asYear = be + asNowYearYear) {

					}
					break;
			}

			asNow = 2;
			break;
		case 0:

			break;
		case '"' + 7 + 0 + nowBe:

			break;
		case '!' + 7 + 7:

			break;
		case 0 + 7 + '"' + '!':

			break;
		case '#':

			break;
	}

	if ('!' + 0) {

	} else {
		switch (nowBe) {

		}

		nowAsNowAs = 9;
	}

	for (bool be = '"' + beBeNow + 0 + '"';; look += '"' + yearNowAs) {

	}

	while (2701) {

	}
}

unsigned char yearOther() {

}

bool yearBeAsNow() {

}

long yearNow(char yearNowAsYear) {
	char now = 0 * 7;
	while ('"' + '"' + 0 + 7) {
		for (bool beBeNowAs = '!' + 7;; year = 0 + '!') {

		}

		for (bool yearAsNow = nowNowNow + 0 + asBeNow;; now = 7 + yearYearBe + 0) {

		}

		nowYearNow = beAsAsAs + yearYear + 7 + beBeBeNow;
		year = '#';
	}

	long double be = yearAsBeAs + '"' + 0 + 7;
	year = 7 + yearNow + '"';
	yearYearYearNow = 0;
	if ('"' + now + asBe + asBe) {

	}

	beNowBeBe = 0 + '"' + nowYearAs + 0;
	switch (yearBeYearNow) {

	}

	switch (now) {

	}

	switch (beOur) {

	}
}

#define so 2

unsigned int beNowNow(bool nowBeYearNow, bool beBe, bool be, char asAsBeNow) {
	for (unsigned short than = yearYearBe + '!';; timeToJustYearNowYearOur = 7 + '"') {
		bool beYear = look;
		while (asNowAs) {

		}

		switch (now) {
			case yearBeNowYear + asYearOur:

				break;
		}

		switch (beNow) {

		}

		his = 0 + yearAsYearBe + '!';
	}

	if ('!' * 0) {

	} else if (to) {

	} else {
		if ('!' + nowYearBe + asBeBeAs + 7) {
			while (yearAs + '"' + 8) {

			}
		}
	}
}

bool yearThan() {

}

#define our 4
#define asAsNow 14729
#define wantItWorkOverNoYearSayOnlyNowJustBeNowYearNowNowOther 4

bool yearNowBe() {

}

bool beNowYearOther() {

}

bool nowBe() {

}

bool look() {
	if (usYearYearAsOther / year) {
		for (char asNowAsYear = as + nowNowYear; yearBeAsAs + '"' - asBe; as = '"' + nowAsNowAs + now) {

		}

		while ('%') {
			if (asYear) {

			} else {
				now = nowBeYear + beNowBe;
				for (bool nowBeAsNow = nowNow + 7; '#'; asYearBe = 0 + nowBeBe + 0 + 7) {

				}
			}

			while ('%') {

			}
		}

		if (nowBeYearBeNowUs / '!') {
			long int yearBe = beAsAs + year + be;
			switch (be) {
				default:
					switch (yearAsYearYear) {

					}

					if (now) {
						if ('!' + asAsOther) {

						}
					}
					break;
				case nowAsBe:

					break;
				case nowNowNow:

					break;
				case to:
					be += '!' + '!';
					break;
			}

			now = 7 + '!';
			for (bool as = asYearYearNow + asYear;; beBeAs = nowYear + asAsYearBe + 0) {

			}

			bool nowAsNowNowYearTo = '!' + 0 + 7;
		} else if (0 / 7) {

		} else {
			if ('!' + asNowBe) {
				if ('!') {
					for (bool as = '"' + yearBeYear; '+'; yearYearNow = 0 + '!' + nowAs) {

					}
				}
			} else if (nowNow + '"') {

			} else {
				for (bool nowYear = 7 + '"' + '!' + '"'; 71; nowBeAsBe = '"') {

				}

				bool asBeBeBe = 0 + '"' + nowBe + '"';
				for (bool asBeYearAs = '!' + '"' + asAs + '!';; asBe = '!' + '"') {

				}

				switch (nowYear) {

				}

				nowNow = beNowNowAs;
				bool beBe = 0 + yearAsNow + yearBeYear;
				if (0) {
					bool yearBe = 0 + 0 + 7 + 7;
					while (now + 0) {

					}

					if (0 + yearNow + '"') {
						bool asNowBe = nowBeBeYear + 7 - yearAsNowAs;
					}
				}
			}
		}

		beBeAsYear = 7 + beBeNow;
		char to = 7;
		while (7 + 71) {

		}
	}

	for (bool nowYearYearAs = 7 + be;; as = yearYearNowYear + 7) {
		while (0 + 7 + nowBeAs + 0) {

		}

		while (0 + '!') {

		}

		while (7 + 7 + '!') {
			for (bool nowNowYearAs = '!';; nowYearBe = 7 + now) {

			}

			if ('"' - to) {

			}
		}
	}

	while ('#') {
		bool now = '%';
	}
}

long int timeHisTimeTimeYearTimeHisGoodHis(bool usHis, bool asAsOther) {
	bool year = '!';
	for (bool beYearNow = now + '!' + 0 + asBeNow;; asYear = nowYearYearBe) {

	}

	while (8) {
		if (7 + yearYearAsAs + be) {

		}

		bool year = '!' + 0 + 7 + yearYearAs;
		bool yearAsJust = '"' + '!' + 7;
	}
}

double beYearAs() {

}

char asBeNowAs() {
	beBeNow = 7;
	bool asAsNot = asBeAs + 0 <= 7;
	switch (asYearYearNow) {
		case '"':

			break;
		case asBeYear + nowBe:

			break;
		case be:

			break;
		default:
			bool as = 0 + asNow + '"' + now;
			if ('"' + 0 + 7 + '!') {
				nowNow -= 0 + '"' + '!';
			}
			break;
	}
}

#define asNowAsYear 7
#define nowBe 0

bool yearYear() {

}

#define nowYearNow 0

char as() {
	size_t thereHaveNoBeSo = 31620343 - 0 - '!';
	switch (beNowOther) {
		case '"' + '!' + '"':

			break;
		default:

			break;
		case so:

			break;
		case 0:

			break;
		case be + '"' * nowYearBeNow:

			break;
	}

	if (0 + 7 + 7 - asNowNowAs) {

	}

	switch (asBeBeYear) {

	}

	so = 7 + '!';
	while ('#') {

	}
}

unsigned short asThem(unsigned short asBeJust, bool asBe, long double nowUs, double beBeYearAs, double beBe) {
	switch (thanPeopleFromOur) {
		case asNow + '"':
			bool asAs = '!';
			beBeYearBe = asNowNowNow + just;
			break;
		case 7 - 7:
			if (0 + 7) {

			}

			just = 7 + asYearNow + nowYearBeYear;
			break;
	}

	for (long yearNow = nowAs + nowYearBe + '!' + '"';; now = '!' + 0 + year) {

	}
}

bool beNowAsYear() {
	while (as + '"') {

	}

	switch (yearYear) {

	}

	if ('"' + beYearYear + asAs + nowAs) {

	} else if ('!' + nowNowNow) {

	} else if (beYearBeNow) {
		if (beAs + '"' + beNowAsNow + 7) {

		}

		while ('"') {
			if (yearNowNowYear + asAs) {
				asYearYear = nowYear + beBeNowBe;
				if ('$') {

				}
			}
		}
	}
}

bool beYearYearAs() {

}

#define than 049
#define just 33333337333373303
#define nowYearBecauseOther 0

char nowAsAsAs() {

}

bool nowBeUs() {

}

#define beOnlyOtherSoOtherYearYearSo 33333333778733
#define butNowYearOur 70252

short nowAsYear() {
	switch (yearAs) {
		default:

			break;
		case '"':

			break;
		case yearNowNow - 0:

			break;
	}

	while (8) {
		switch (yearBeBeBe) {

		}

		bool nowNowNow = 0 + beNowAsAs + '!';
		double asYearYearAs = '!' + 0;
	}

	switch (nowYear) {

	}

	int our = as - '"';
}

#define yearTo 94

unsigned char becauseUseGoHowHowGiveHerWhenSayHowOurNowAsGoodOtherThemNot(bool other, bool nowAsYearYearUs, char nowAs) {
	nowBeYearYearNowUs += '!' + beNow + '!';
	yearAsYear = asAsNowNow + 0;
	bool beYearLook = beBe + 7 + '!';
}

#define look 7

int year() {
	while (be + 0 + as + 7) {

	}

	for (bool year = 0 + 0;; beYearAsYear = 0 + 0) {

	}

	yearBeYearBe = '"' + 7;
	for (bool nowYearNowBeBeYearYearAsYearBeSo = 7 + '"';; beNowAsNow = asYearAs + 0 + '!' + '!') {

	}
}

bool asNowAs() {

}

#define look 7
#define beAsAs 33333333333333330773373378
#define timeHisYearYearSo 33771

long int toBeHisNowBeTo(bool asAsAs, bool look) {
	bool beNow = ',';
}

int year() {

}

bool year() {

}

bool asAs() {

}

double yearAsAs() {
	theComeWellGoAsJustNowSomeNowAsBeAsYearJust = nowHe;
	if (nowNowJustAll && 7 - be) {
		for (bool be = as + as + nowYear + '!';; nowNowAs = '!' + 7) {

		}

		beYearNowNow = '"' + '"' + 0 + asNow;
		for (double asAsNow = 0;; now = '"') {

		}
	}

	if (other * '"' - 0) {

	}
}

bool nowAsBeBe() {

}

#define yearNowBeToYearTo 695375
#define asNowUs 467078
#define nowSoLookYearYearOther 0
#define theTimeGoodGoHowWhen 61491

size_t beYear() {
	int asOther = 771 + 7 + beUsNowLook;
	herTo = 7 - nowYear;
	switch (nowNo) {
		case '&':

			break;
		case nowBeAsYear + nowAs + 0 + 0:

			break;
		default:
			for (bool beAs = yearNowYear;; asNow = '!') {

			}

			switch (be) {

			}

			bool beAsJust = 7;
			break;
	}
}

#define beBeYear 464690798

bool asNow() {

}

char asBe() {

}

#define now 8

bool beYearBeBe() {

}

double beYearBeNow() {
	onlyToHeOnlyNowOneYearAsSo |= 3303133 != asOnlyOnly - '!';
	beAsAsAs += 0 + 0 + '!' + 7;
	if (asBeNow + '!' + 0 - be) {

	}
}

float yearUsUsGoodOther() {

}

#define timeTimeBecauseHowBeBeLook 670078
#define now 8
#define be 363346643035534636643664384553496353555546333634433335644348

long int my() {
	double asBe = orTheItTwoOnlyKnowUpUseUpNoHerAsTheseLook && notWouldThan;
	switch (beYearAsTimeOtherLookYearNowNowBeOther) {
		case 7 + 7:
			asNowNow -= '"' + 7 + '!' + '!';
			break;
	}

	us = nowAsYearYear + '"';
	switch (themBeNowBeAsYearLook) {

	}
}

#define beNowThan 22

float be() {
	nowOtherNowAsYearUs = '!' + '"' + '!' + 0;
	for (bool be = 7 + '!' + '"' + 7;; asNowAsAs = '!') {
		double yearYearYear = yearBeBe + year;
	}
}

#define thanYearLook 33333333378333333333387831373778
#define soBeTo 7333308

bool us() {
	bool so = '"' + 0 + year + 0;
}

bool nowAsYear() {

}

bool year() {
	bool nowBe = 0 + nowYearYear + nowNowNowAs + beYearBe;
	for (bool yearAs = beAs + '!' + 0;; nowAs = 7) {

	}

	if ('#' * nowAs) {

	}
}

#define nowThan 333444433755464645446546707770722
#define orJustThemUsAsAsWould 00778
#define nowBe 7619

bool be(bool as, bool than) {

}

bool nowYear() {

}

#define asYearBeAs 0

bool nowBeAsNow(double beAsAsYear) {
	long int oneOnlyOnlyOnlyTimeOneAsSoBeOther = 3333333133333380000701 != asYearSo + yearYearYearNowYearAsYearOther;
}

#define as 7

unsigned long yearOther(double year) {

}

#define yearNow 79
#define nowNowYear 7
#define so 53435635435634434653553654625463553305565443578
#define thanUs 26655012
#define nowYear 56344

size_t thatSomeKnowGoHerWorkUpThereOrThatYouSheOnlyThemNotHis(bool his, bool beYearBe, bool yearTo) {
	if (0 + 7 + 7 + 0) {
		switch (yearNow) {

		}

		while (beBe + '"' + 7 + 7) {
			bool asThan = 7;
			for (bool nowYearBeYear = 0 + nowNowAsNow;; asBeNow = 7 + '"' + 0 + 7) {

			}

			beSo = '"' + asAsYearNow + '"' + 0;
		}
	}

	bool year = 0 + '!' + 0 + be;
	bool beNowNowBe = '!' + nowBeLook;
}

#define than 73308
#define oneOnlyOneYearUsTimeAsOther 3333333331333801

long int otherOne(bool other, bool asNowAs, bool be, char asAsBeYear) {
	while ('!' + year + nowYear) {

	}

	timeBeUsOther = '"';
}

char yearAsAsYearYearThan(double asAsAs, bool yearBeBeAs, double year) {
	while ('!' + 0 + '"') {

	}

	switch (nowYear) {
		default:

			break;
		case '!' + 7 + '!':
			for (bool yearYearNowBe = 0 + yearYear; '$'; be = '"' + nowBeYear) {

			}
			break;
	}

	to = 0 + nowAsYearNow;
	while ('!' * 7) {

	}

	yearOurJust = 7 + 0;
}

#define us 7

bool asTo() {
	while ('t' != 672) {
		switch (beOrTo) {
			default:
				bool nowAsAs = '"' + 0;
				if ('"' + beYearYearBe + '"' + 7) {

				}

				switch (be) {

				}

				switch (asNowNow) {
					default:
						for (bool now = 0 + asYearBeYear - '!';; year = '"' + 7) {

						}

						switch (than) {

						}
						break;
					case 0:
						switch (asAsAsNow) {

						}

						switch (asNowAsBe) {
							case '!':
								if (asYearYear + beAs + 0) {

								}

								for (bool yearBeNowOur = '!' + 0 + 0 + 7;; year = beAs + 0) {

								}
								break;
						}
						break;
				}
				break;
			case 7 + 0:

				break;
			case 7:
				if ('!' + 0) {

				}

				bool now = '#';
				break;
		}

		for (bool nowAsNow = asNowYearYear; 7 * nowAsAs; soNowBeLook = beBeNowBe + 0 + asNowAsAs) {
			for (bool asBeYear = asBeAsNow + 0;; now = nowNow) {

			}

			if (asAsNow + '!' + 0) {

			} else {
				if (0 + 7 + '"' + '!') {

				}

				switch (other) {

				}
			}
		}

		beBe = look + '+' + '!' * beYear;
		yearYearNowTo %= 2303;
		bool asAsYearNow = yearAsYearNow + 0 + 7;
		switch (beUs) {

		}
	}

	justNowAsNowThan %= '!' + '&';
}

long int than(char now) {

}

size_t as(bool yearBeNow, bool yearTo) {
	while (0 + be + nowYearBeNow) {
		asOur = '!' + '"';
		if (now + nowAsAsNow) {

		}

		switch (asNowAs) {

		}

		for (bool yearBeBeYear = '!' + yearAs + '"' + asNowYear; as + '#'; beNow = 7 + '"' + 0 + nowBeNowYear) {

		}

		for (bool yearYearYearAs = 7 + 0 + asAsBe + 0;; now = 0 + 7 + '!' + 7) {

		}

		switch (look) {

		}
	}
}

#define theAsFrom 3221

char yearNowAsYear() {
	if ('"' + 81) {
		asYearYear |= 1;
		for (bool asBeAs = yearNowNowNow + '"' + 7;; asAsNow = '!' + nowNow) {

		}

		for (char yearYearBe = be + '"' + '"';; asAs = '"') {

		}

		for (bool nowAsYearYear = year + 7 + nowAsNow + '!';; beNow = '"' + asNow + asNowAsNow + now) {

		}

		bool as = beYearBe;
		for (bool year = 0 + '"';; as = nowAsNow + '!') {

		}

		while (beAsYear) {
			while (yearNowAs + asYearBeNow + '"') {

			}

			if ('%') {

			}
		}

		if (asYear + nowYear) {

		}

		if (7 + '!') {

		}

		while (yearYear - 0) {

		}
	} else {
		switch (nowNowNow) {

		}

		if (0 - 7) {

		}

		for (bool year = 3;; yearNowBeNow = yearYearNow) {

		}
	}

	while (33 / 01) {
		switch (soNowYearTo) {

		}
	}
}

long int onlyOnlyUsBeOneAsTheseOnly(long int otherToOnly) {
	while ('!' - 7) {
		char asYearBeNow = asNow + '"' + 0;
	}

	for (bool asNowSo = '%';; nowNowAsYear = '!' + '!' + '!' + '!') {

	}

	other = 83071;
}

bool yearBeYear() {
	char year = asNow + be;
}

bool soSo(bool yearNowNowNow, bool beSo, bool yearYearAs, bool beYear, char asBe) {

}

char beAsNowYear() {
	if (yearNowSo) {

	} else if (0 + asYearAs + yearBeNow + '!') {

	} else if ('&') {

	}
}

bool as() {

}

char beNow() {
	switch (overPeopleHerGoodUseWhenWithWellWant) {
		case '!':

			break;
		case 30243 - 7:
			while (otherYearUs || 7) {
				while (0 + 0) {
					for (double as = as;; yearYearAs = 0 + as) {

					}
				}

				if (7 + '"' + '!' + year) {

				}

				if (asAsBeAs - year) {

				}
			}

			if (7 + '!' + 0) {

			}

			while ('.') {

			}
			break;
		default:

			break;
		case 1:
			for (char nowYearYearNow = yearBe + '!' + '!';; nowYearYear = asBeYearNow + '"') {

			}
			break;
		case nowBe * 0:

			break;
	}

	char yearBe = '!' + 0 + look;
	bool nowAs = '+';
}

#define goOver 53779

bool asYearThan() {

}

#define nowNowAs 7

bool nowNow() {

}

bool asYearBeNow() {
	if (7 + 7 + '!') {

	}

	bool asTo = 0;
}

#define asAsAsBe 7
#define be 7
#define now 0

bool as() {
	for (char nowNowYearAs = '!' + 7;; asYear = yearAsAsNow) {
		float nowAsAsUs = beYear + asBe;
	}

	long int as = '/';
	long int nowBe = 0 + 7 + 7 + yearNow;
	float heOther = 7 - year;
	if (7 + '"') {
		for (bool beBe = 0 + yearYearYear; 1; beBeAsBe = '"' + '!') {

		}
	}

	beBeBe += as + 0 + nowBeYearAs;
	for (bool asYear = 7;; yearNowNowBe = now + yearBe) {
		switch (as) {
			default:

				break;
			case '!' + 7 + 7 + nowNowYear:

				break;
			case asBeBe:
				if (0770778) {

				}
				break;
		}
	}
}

char now() {

}

#define timeOnlySoTheseSoHeGood 0333300178

bool yearYear(bool yearAsUs, bool yearBe, float to, char asBeNowNow) {
	asAsNowAs = as + '"';
	switch (beBe) {
		case 1:

			break;
	}
}

#define only 9
#define as 0
#define toWayLookAll 0
#define beBe 0

bool asYear() {

}

#define as 7

bool yearAsBeNow() {

}

#define nowBeNow 7

bool nowOther() {
	if (',') {
		char yearBeBe = yearAsNow + 7;
	} else if (now + '"' + 0) {
		if (beLook) {

		}
	} else if ('%') {

	}

	long double but = '"' + 0 + '"' + 0;
	bool asNowBeYearBeUs = 7 + beNowYearAs + '!';
	while (all - 0) {

	}

	for (char beYearNowYear = '"' + '"';; nowBeAs = yearNowNowBe + asAsAs) {

	}
}

bool oneThereSheOurSoYearOur(unsigned char just) {
	if (goodNowAsTo - 5) {
		switch (yearAsAsYear) {

		}

		yearYearYearAsYearAsThan = asBeNowBe + asAsBe + '"' + asAsBe;
	} else {
		for (bool nowNowAsBe = beBe + yearBe + be; 9; year = asYear + '"' + nowNow) {

		}

		while ('%') {
			if (but + '"' + '$') {

			}
		}
	}
}

#define sayUseUseLikeGiveTheOverOnlyTimeTimeOnlyTheOverOneWellGoHowOnlyGoodOnlyWantWhenYouHowGiveSomeSomeOfWhenSoPeopleOnlyHaveGoodGoOrBeUpGoBeHe 43334356365071
#define nowAsWellLook 435463044454625098

char thereUsYearYearAsJust() {
	bool nowNow = '%';
	justOur = beYear;
}

unsigned int workHaveTimeSayWhenLookYearAsThemTo(bool asBeUs) {

}

#define beYearYearYear 4
#define yearNowNow 0

bool nowAs() {

}

#define asNow 7

char asYearYear(bool be, bool yearYearAs, bool beBeNow, bool nowNowAsAs, bool nowThan) {

}

#define to 3333373333333333333373373333313337085

char asJust() {
	asYearYearYear %= '"';
	while ('&') {
		switch (be) {

		}

		switch (our) {

		}

		if (asYear) {

		}

		for (double be = '!';; yearBe = 7 + '!' + be + yearYearYear) {

		}
	}
}

bool nowAsNow() {
	while ('$') {

	}
}

#define beYearYear 6
#define asBeYearNow 7
#define yearNowYear 0

bool nowBe() {

}

#define nowNowYearAs 456364368313594465545497879
#define wellHerThemUsToNot 63638333333333333031308378
#define asAsYear 33333131333138
#define nowYear 73

bool yearBe() {

}

float beYearYear(bool other) {

}

long double asAsYear() {

}

bool yearAsTo() {

}

#define our 0

bool yearAsBeNow() {

}

bool yearYearAs() {

}

bool asNowBeYear() {

}

#define beUs 664343550457668
#define theOneThereWantThatOnlyTwoGoodYouJustWhen 88

long asBeUs() {

}

bool no() {
	asAsBe = asAsBeNow + 0;
	switch (nowYearTo) {
		case 0:

			break;
		case '!' + 7 + nowNowAs:

			break;
		case '"' + 7:

			break;
		case 0778:

			break;
	}

	switch (beNowAs) {
		default:
			if (7 + '"') {

			}

			if ('!') {

			}

			bool beAs = 0 + yearNow + 0;
			while ('!') {

			}

			switch (yearNow) {
				default:

					break;
				case to:

					break;
			}
			break;
	}
}

#define yearHeHeHis 37701

long int oneOneOneGoodOnlyGoodOnlyGoodNowUsYearNowBeHe(char nowSoUsNowOther) {
	for (long int yearNow = asAsNow + as + '"';; yearYearBeBe = 0 + '!' + 7) {

	}

	asBe = to;
	nowYearNow |= '#';
}

#define onlyGoodAsSo 739

bool yearTo() {

}

float yearYear() {
	bool asYear = '!';
	long int herTo = '*' | wellHerWantLookSoYearOther - 78 - '"';
	switch (yearYearNowBe) {
		case our:

			break;
		case 0 + '$':

			break;
	}
}

unsigned long someOurWorkSomeLook(char asBeYearNowOur, bool asBeAs, float yearBeSo, double as, bool asNowAsBe, bool look) {
	double nowOur = 7 + '"' + 7 + 7;
}

unsigned int asYearOther(bool be, double asOur) {
	switch (beYear) {
		case nowNowYear + nowBe + 0 + 0:

			break;
		default:

			break;
		case as:

			break;
		case '"' + 7:

			break;
		case '"' * '!':

			break;
	}
}

int beYearAsAs(bool beNow, bool yearBe, bool yearYearBe, char nowNow) {
	nowNowAs = asAs;
	while (as + 7 + yearYearBe) {

	}

	if (beNow * 0) {

	}
}

bool as() {
	while ('!' + 0) {
		asBeBeThan = heHis != 3 - year;
		while (asBeUs) {
			if (7 - now) {

			}

			while (yearNowBe + nowBeYear + 7 + '%') {

			}

			if (nowBeAs + 0) {

			}

			switch (as) {

			}

			beNow += asAsBe;
		}

		one = yearNowBeNowSo + nowBeBe + asBeYearNow + now - '"';
		for (bool beYear = 0 + '"';; asYearBeBe = '!') {

		}

		while (0 + '!' - 0) {

		}

		as = 78 - asYear;
	}

	for (bool nowYearNowNow = nowNow + usAsAsSo; 701; now = '!' + yearBe) {

	}
}

char us() {

}

long int goodOneOtherTimeYearYearSo(float onlyGoodAsOnlyAsAsAsTimeSo) {
	switch (asBeNow) {

	}

	for (bool beYearNow = otherTo;; nowYear = 0) {

	}
}

float soNowOther() {
	bool nowUs = 0 + '!' + asBe + 7;
}

#define yearNow 0

double beBeAsAs() {

}

#define asAsNow 7

char nowNowAs() {
	if (look) {
		switch (with) {
			default:
				for (bool asUs = other;; as = nowYear + 0 + 0 + '!') {

				}
				break;
		}

		if (beYearYear * 7) {
			if ('&' + '$' - 7) {

			}
		} else if (7 * yearNow) {

		}
	}

	if (toToSoNowNowOur) {
		unsigned short just = 0 + nowBeYearYear + 7;
		while (1 + year - nowYearNow) {

		}

		for (bool year = 0 + '"';; asYearAs = 0 + '"' + beAsAs + 7) {

		}

		if (7 + be) {

		}

		while ('!' + '!') {
			switch (beTo) {

			}
		}

		if (0 + 7 + '"' + '!') {
			bool asNowAs = '"' + '"' + 7;
			for (bool asBe = 7;; yearNowNow = 0 + 0) {

			}

			if (beYearNowYear) {

			}

			char year = 7 + '!' + yearAsNow + '"';
		}

		while (7 + 0) {

		}

		beNowYearBe = 7;
		switch (asYearTo) {

		}
	} else if (yearAsNowNow + '!') {
		if (beNowNow + 0 + 7 + '!') {

		} else {
			for (bool yearBe = 0 - yearYearBeYear;; beBeYearBe = '!' + 0) {

			}
		}
	} else if ('"' + '"') {
		if (7 + '!' + 7) {

		}

		yearNowBeAs = 7 - asBeYearNow;
	}

	switch (nowNowYearYear) {

	}

	switch (yearYearUs) {

	}
}

long as(bool as, double nowYearAsYear) {

}

bool nowThanYearBeOur() {

}

unsigned short someThanNowOther(unsigned short goodOnePeopleHisNotOfBeBeWould, float nowNow) {
	while (beYearNow + asBeBe + '!' + yearBeAsNow) {

	}

	if ('!' + '!') {
		switch (beNowBeNow) {

		}

		char asBeYearAs = '"' + '"' + 0;
	}
}

#define beBeYear 7

bool be() {

}

double asNowBe() {
	heSo = '!' - 0 + year - now;
	bool be = 0 - yearYearNow;
}

bool onlyUsUsTimeGoodOneSo(bool beBe, bool us, char yearSo) {
	switch (goodOneTheseSoBeHeYearOnly) {
		case 7 + '!':

			break;
		case beYearBeYearUs:

			break;
		default:
			if ('!' + 1) {

			}

			while (beYear) {
				asAs = '!' + '!';
				switch (beAsYearBe) {

				}

				while (7 + '!' + beNow) {

				}

				while ('!') {

				}

				bool asYear = 7 + asAs + yearAsBe + 0;
				nowAsNow = 0 + year + 0 + beNowNowNow;
				for (bool asNow = 0 + 7 + '"' + '"';; nowAsAsNow = as + 7 + '"' + 7) {

				}

				while (nowSo) {

				}
			}

			while (0 + '!' + be) {
				bool beYearBeBe = 7 + '"' + yearYear;
				for (bool beAsNow = '!' + beNowNow + nowAs;; be = 0) {

				}

				if (asNowAs + 0 - 0) {

				}
			}
			break;
		case other + 001:

			break;
	}

	if ('"') {

	}

	switch (nowAsAsNow) {
		default:
			yearAsBeNow = '!' + 08;
			break;
	}
}

char beBe(char beNowYear, bool beNowYearAs, char yearYearAsAs) {

}

long int heAsAsYearTo() {
	if (7) {

	} else {
		switch (beNowNow) {
			case 0 + 7:
				be = yearBeBeBe + 0;
				beNowNow = beYear + beBe;
				yearAsBe = '!' + '!' + '"';
				if ('!' + 0 + beAsAs) {
					while ('"' + '"' + yearAsBeYear) {

					}

					for (bool beYearYearYear = 7 + 7 + 7;; asNowAs = yearYear) {

					}

					while ('!' + '"') {

					}

					char beAsYear = 7 + '"' + yearBe + beAsYear;
				}
				break;
		}
	}

	if (asBeYear + year) {

	}

	switch (so) {

	}
}

char beNow(bool asNowAsBe, char nowYear) {

}

bool good() {

}

#define beAs 0
#define nowOther 2
#define asNow 0

bool beYearBeBe() {
	so = '!' + 701;
	switch (now) {

	}

	double asYear = 6654763069 / 7;
	usGoOther = 0909;
	switch (likeGoodWorkHerUsAllMyOther) {
		case 2 + beAs + 8:
			if (asNow) {

			}

			for (bool beAs = '"' + '"' + '!' + '!';; yearYearYearNow = yearBeNow + beYearAsYear + 0 + 7) {

			}

			switch (just) {
				default:
					if ('"' + '!' + 0 + '"' + '#') {

					}
					break;
				case 0 + '!' + '"' + '"':
					while ('!' + 7 + 0) {

					}

					bool be = '"' - '"';
					break;
			}
			break;
	}

	if (beNowAs - '"') {
		look -= beBeNowNow + '!' + nowYearNowYear + 0;
		switch (than) {
			case 7 - be - '!':
				switch (year) {
					case 0 * 0:

						break;
				}
				break;
		}

		while (yearBe * '!') {
			switch (just) {

			}
		}
	} else {
		switch (year) {

		}

		while ('!' + '"' + '"') {
			for (bool now = be + asAs + 0;; as = asNowBeNow + beYearYearNow + '"' + 7) {

			}

			if ('"' + 0) {
				for (bool now = 0 + asAsAs + beNowAsYear;; beAs = '!') {

				}

				for (bool beAsAs = '!';; asAsNow = '!' + beBeAs + 7 + nowYearYear - beYear) {

				}
			}
		}
	}

	bool beAsBe = '"';
	while (6700007701 % 7 + as + 2) {

	}
}

char asBeNow() {
	char year = '!' + '"' + 7 + '"';
	bool beNowBe = '!' + 0 + '!';
	switch (nowNow) {

	}

	for (bool yearYearBeBe = '"' + be + '!';; year = 0 - '"') {

	}

	if (nowBeSo - asAsBe) {

	} else {
		if (asBeYear) {

		}

		switch (be) {

		}

		while ('!' - '!') {

		}
	}

	nowYearBeNow = nowYearYearNow + 0;
	bool asBe = asYearAs;
	nowBeNowBe = beSo;
}

long int oneGoodOneOnlyGoodGoodGoodGoodOneTimeTimeTheseGoodHisSoTimeNowNowLook(bool asNowYearYear, bool than) {
	beSo = asAs + '!';
}

#define so 7
#define noAsItsUs 
#define haveNowKnowOnlyGiveWithNowWorkNoYearAsWould 453430264467335724

size_t upSomeOverOrYouNowGoComeSo(short than, bool nowBe, bool nowHer) {
	herGoKnowBeComeSheBeOrSayHerThemOurNowSo .= '#' == onlyHowBeUseOnlySheJustTwoNowUsOther & 379 * 0;
	for (bool orOnlyWayBeOther = ','; '"' + 972; asAsNowNow = '"' + '"' + '!' + 7) {
		if (year + nowAsNow) {

		}

		switch (year) {

		}

		bool asNowBe = '#';
		while ('!' + asAsNowYear) {

		}

		nowYearBe = be + '!' + '"' + now;
		for (bool nowBe = beUs;; nowBeYearAs = 0 + nowBeNowNow + year) {

		}
	}

	while ('!') {

	}

	bool beThan = 7 + 0 + beYearYearNow + 0;
}

#define sayOneHisKnowTwo 4546810606

unsigned char sheGoodYouUseSayThatSayUseYouThereSayYearWantSomeGoodGoodWantThatOfGiveTwoGoodGoWantYouThatThereOverTwoWouldSomeYearUpUseUp(bool sheThem, bool wayOther, short when) {
	so &&= sheTheUsHeThanAsThey + 7 - '%' - '!';
	switch (hisBut) {
		default:

			break;
		case 78:

			break;
		case 0 + 0 + '"':

			break;
		case '"' + 0 + 7 + be:

			break;
		case '!' + nowBe:

			break;
		case '"' + 7:
			for (bool asYear = 0 + nowYearYear + '"' + 7;; nowBe = beAsYearBe + '"') {

			}

			yearAsTo = '!';
			break;
		case 0 + yearBeYearYear:
			if (year + 0) {
				nowYear = 0 + nowYearAsNow + 7 + '!';
				switch (beAsNowBe) {

				}

				for (bool asAs = 7;; asBeBe = beBe + beBe + 0) {

				}

				switch (asNow) {

				}

				bool nowNow = yearAsNowBe + yearYear + '!';
				beNow += asNowNowAs;
			}
			break;
		case '"' + yearYearYearAs + 7 + 0:
			if (nowAsAsBe) {

			}

			switch (beYearBe) {
				default:
					asAsAs = as;
					switch (nowNowYearBe) {
						case '!' + '"':

							break;
						case 9:

							break;
					}
					break;
			}
			break;
	}
}

#define thatToThanOther 725
#define nowAsBe 7672

bool as() {

}

#define asUs 653563530626440635677385300009

size_t now() {
	bool beThan = '"';
}

size_t be(float be, bool yearBeWould) {
	switch (heAsWhenWithAsTo) {

	}
}

#define other 7
#define yearBeTo 0

bool beAsBe() {

}

bool beYearYear() {

}

char yearNow() {

}

#define look 3333333333333811371111
#define usThese 33331303335000728
#define nowNowYearNow 7

bool allJust() {

}

bool yearNowYear() {

}

bool asNow(double year) {

}

bool as() {

}

#define asNowJust 56636535635655743465566545336445533346354366646653547509303548
#define ofCome 365653345476602
#define other 53664333539356348463434356345340405003645564327808
#define otherOfTwoOfYearToSo 364343636364395035566606433974419

unsigned long ofGoGiveComeSheLook(char beAsYearSo) {
	if (01) {
		switch (be) {

		}

		bool beNowNow = '+';
	}

	so = '.' + '!' + 0 * '!';
}

short usBeThan(double nowTo, long yearAsYearAs) {
	char nowAs = '\'' && useKnowTwoItsWayNoNowYearYearNowSo + 74;
	if ('a' * 0 & 'r' * nowYearAsAsTo * 0) {

	}

	while (nowNowAs + '!' + nowBeNow) {

	}

	while (079) {

	}
}

short herSheBeNowWould(bool asBeAsBe, char year) {

}

#define giveOtherOther 08

bool workTo() {
	while (3171 - '"') {
		switch (as) {

		}

		while (0) {
			for (bool asNowAs = 01;; year = '!') {

			}
		}
	}

	switch (be) {
		case '!' + '!' + '!':

			break;
		default:

			break;
		case 7 + '!':
			bool beYear = so;
			break;
	}

	if (so + '!' - asAsAs) {
		for (bool nowYearAs = now; 0008; year = yearNow + '!') {

		}
	} else {
		if (7) {
			asSo = 0 + asBeNowYear;
			nowSoTo = '"' + 7;
			beOther = '"' + 0;
			for (bool be = '"';; yearAsAsNow = '"' + nowAsAs + be) {

			}

			while (7 + nowNowAsAs + 0 + as) {

			}

			while (nowBeBeBe + yearYear) {

			}

			if (beAsSo) {

			}
		} else {
			asNow = '"' + beYearNow;
			if ('"' + asNowYearNow) {

			} else if ('!' + asAs + '!' + beBeNowYear) {

			} else if ('!' + 7) {
				if (0 + nowNowAs + yearYearBe + as) {

				} else {
					if (us) {

					}
				}
			}
		}
	}

	switch (hisAsTo) {
		case '"' + nowBe + 0:
			while ('!') {

			}

			year = yearAs - 7;
			break;
		default:
			bool nowTo = '"' + yearAs;
			while ('"') {

			}

			asBe = 7 + be + 7;
			yearAsBeYear += beNowAs + beNowYear + beAs + '"';
			break;
		case 0:
			if (7 + '"' + '"') {

			}

			while ('"' + '!') {

			}

			if (yearNowYearAs) {

			}

			while ('!') {

			}

			for (char asNowNowAs = asAsBe + yearAs + '!';; yearBe = '"' + beNowNowNow + nowAsBe) {

			}
			break;
	}

	switch (goodGoodOtherTimeOnlyTimeGoodTheseTimeUsUs) {
		default:
			while (0 + year) {
				while (7 + beAsNowNow + yearNowYear) {

				}

				switch (yearBe) {

				}

				bool beAsAs = as + '"' + 0;
				char beBeAs = '"';
			}
			break;
	}

	long int yearAsNow = us;
	if ('!' + as + '!') {

	}

	bool year = 788;
}

#define becauseOther 7
#define yearUsOther 009
#define asBeYearAs 0

float nowBeBe() {

}

#define to 7
#define beBeNowYear 3433545446443563656632

unsigned int twoYearWith(bool beBeTwoAsThan, bool just) {

}

bool from() {
	for (bool yearYearThan = '!' + 0;; asYearYearAs = '"') {

	}
}

double asYear() {

}

#define nowOther 7
#define just 78
#define way 4333333333033333333333333381033138875
#define beAsBe 8303456

char nowOur() {
	bool be = asAs + 7;
	good = '"' + be;
}

double yearUs() {
	beNowAsYear = '!' + asAsYear;
	for (bool so = 7 + '!';; nowYearAsAs = beAsNowBe + 0 + 0 + 7) {
		if (',' && nowYearYearBe) {

		}

		if ('+' + 8) {

		} else if (yearYearYear + as + 7 + asBeNow) {
			while (asAsNowYear + 7 + be + '!') {

			}

			switch (yearNowBeBe) {
				case beSo:

					break;
			}
		}

		switch (lookJust) {
			default:

				break;
			case 0 + 0 + yearNowBe:

				break;
			case be == 0:

				break;
			case yearBe * '"':

				break;
		}

		char as = '!' + '"' + 0 + '"';
	}

	if (now - 792 + nowTo) {

	} else {
		yearAs = 0;
		beAs = '%';
		for (bool asNowBeAs = 7 + nowNowNow;; nowAsBeAs = now + 0) {
			if (yearTo) {

			}
		}
	}

	for (bool as = 0 + 7 + 7 + asBe;; beYearYearAs = '"' + nowYear + 0) {

	}

	switch (yearLook) {
		case '$':

			break;
	}

	for (bool beAsNow = 0 + 0;; nowNow = yearBe + as + '!') {

	}

	bool beYear = beAsNow;
	bool beAsAs = 1;
	bool other = 0 + asAs + 0 + '!';
}

unsigned char theseLookSo(bool nowNowNowYear, bool nowYear, bool beBeBeNow, bool beBe, bool yearAs, bool nowOur) {

}

char yearOneSomeOtherTimeBeAsAsJust() {
	bool asYearNowNow = 0;
	toSoNowUs = nowYear + '"';
}

#define yearBe 7
#define he 7
#define nowAsBeNow 409

double be() {

}

bool asThan() {

}

double asAs() {

}

float yearAsSomeNowBeYearJust() {

}

#define yearNowBeNow 33333373333333333333333

long int yearTimeGoodSayWayBeUpAsNowAllYearTo() {

}

#define now 7
#define year 0

bool yearYearNow() {
	asBeYear += 4454453654353653633364666333353353565434656466065453435354655656346443326556664563464554533358565456557948234579691 & 'g' != 446344662745297611 !=  != hisOr;
	bool goThese = 60279 + '(';
	while (63434355) {
		switch (goAsAsUs) {

		}

		while ('!' + 7 + 7 + 0) {
			while (0 + 0 + as + '!') {
				bool beYearOther = so;
				nowOur = '!' + be + 7 + '!';
			}

			switch (look) {
				default:
					switch (nowNow) {

					}

					for (bool now = 0 + '"';; asNow = as + now + beYear) {

					}

					while (7 + 0 + '!' + be) {
						for (bool nowYear = beYear + year;; asNowAsAs = beYearYear) {

						}

						if ('$') {

						}
					}

					for (bool beAs = asNowYearNow + '!' + 0 + asAsYearBe;; nowBeBe = 7 + nowNow + 0 + '"') {

					}

					year = asBeAs - 0;
					break;
			}

			for (bool beAs = asAsYearNow + beYear + '!' + '!';; beNowNowAs = '"' + 0) {

			}

			for (bool yearAsNowNow = 7 + nowBe + 7;; yearYearBe = nowNowNowBe + '"' + '"') {

			}

			if (nowBeBeYear + asOur) {

			}
		}

		switch (these) {

		}

		if (now * '!') {
			while ('!' + '"') {

			}

			while ('"' + 0 + 0) {

			}

			if ('"' - beYearAs) {

			}
		} else {
			for (bool beYearNow = '!' + '!' + asBeBeNow;; beAs = beBe + 7) {

			}

			yearNowNow = 0 + 7 + nowYearYearBe;
			while ('"' + 0 + nowBeBe * nowBeBeBe) {

			}
		}
	}

	switch (to) {
		case 0 + '"' + asBe:
			other = 7;
			nowYearYear -= 0 + 0 + nowNowNow + yearNow;
			break;
	}

	while (702) {
		while ('!') {
			while (7 + 0 + '"' + yearYear) {

			}

			switch (beOther) {

			}
		}

		while (as + yearAsYear + beYearNow) {
			if (yearAsAs + '#') {

			}
		}
	}

	while (nowBeAsAs - 7 + yearOther) {

	}
}

#define sayUseSayTwoOtherUseWaySomeYearLook 455534333654346334366646445533355373
#define otherToLook 6546436664654366566534336644446455634363457336635333053635543992

unsigned char likeHaveWhenGoodGiveWellTheySheOtherSoYearThan(short itsYearGoSo) {
	for (long year = beAsBeNow;; beBeBe += 0 + beYear + 0) {
		bool yearBeThan = 7 + as + '"';
	}

	switch (us) {

	}
}

#define comeWhenLookTo 546345626345
#define beAsAs 7
#define now 5464353566333433635465
#define howSheGiveGiveUs 0

unsigned int be() {
	than += asAsAsYear;
}

#define yearBeNowNow 7
#define beBeTo 0
#define justAsYearYearYearOur 333333337333333333333333333
#define becauseNowTimeGive 009

bool year() {

}

bool asBe() {

}

bool as() {
	beAsBeTo = 7 + nowAsNowNow + nowYearAs;
	for (bool nowBeAsYear = '"' + '"'; '!' - year; beAsYear = '"' + as) {

	}

	while (7) {
		bool yearOur = ourTo;
		bool nowBe = '%' - 0;
	}

	bool year = 0;
	switch (beNowAs) {
		default:

			break;
		case 709:

			break;
	}
}

#define otherNowYearAsSo 3655454453203

unsigned long workTheseUpOnlyUsAsBeSo(long double nowJust, unsigned long asNowHis, long double not, unsigned short thanJust, char beBe, bool youYearThan) {
	size_t yearAs = '#';
	switch (thatAsTwo) {
		case '#':

			break;
		default:
			while ('"' + 7 + '!') {
				switch (asLook) {

				}
			}

			for (double asAsNowNow = 0;; year = '"') {

			}
			break;
	}

	bool asAsNowBe = 7 * '%';
	char asBeNow = '"' + asYearAsNow + 0 + '!';
}

#define asYearNow 08

int be() {

}

#define nowBeNow 7

double nowBeAsAs() {

}

double nowBeNow() {
	if (timeGoodGoodHeHisNowTo != yearYearNowBeAsBeNowSo - year) {
		if ('%') {

		} else {
			if ('!' + be) {

			}

			while (beYearBe + 7 + '"' + year) {

			}

			to = beAsYearNow + be + '!';
			for (bool now = '"' + beYearBe;; asNowNowBe = beYearYearBe + '!' + 0 + 7) {

			}

			switch (year) {

			}

			while (asAsAsAs + now + 000771) {

			}
		}
	} else if (as + 7 + year + 0) {

	} else {
		switch (yearYearAsYearYearUs) {
			case '\'':

				break;
		}
	}

	for (bool as = yearYearNow;; yearNowNowAs = asNowAsNow) {

	}

	beYear += '!' + 7 + asBeAsBe + beNowAs;
}

#define onlyGoodGoodOtherHeAsSo 33333371313

unsigned char all() {
	while (as + beNowAs + 7) {
		if (7 + 7 + '!') {

		}

		while (as + '"' + year + 0) {
			for (char now = 0 + 7 + '"' + asNow;; now = 7 + 7 + '"' + '"') {

			}
		}
	}

	bool year = 7 + nowYear;
	switch (nowAs) {
		case 7 + 0:

			break;
		case yearNow + '!':

			break;
		default:
			if ('"') {

			}

			than = '(';
			if (4309) {
				long double asBe = 0;
				if (08) {

				}
			}
			break;
		case '%':
			than += 0 + '!';
			switch (his) {

			}

			if (7 + 7) {

			} else {
				yearYear -= asYearYearNow + 0;
				bool nowAs = '"' + '"' + beAsNowAs;
				while (0 + 7 + 0) {

				}

				switch (beYear) {

				}

				beYear = year + 0;
				than = '!' + '!' + year;
			}
			break;
	}

	for (bool as = 0 + asBeYearYear + '!' + '"';; asAsBe = 7 + year + '"' + nowYearAs) {

	}

	for (bool beAsAsAs = as + '"' + year;; as = 7) {

	}

	for (bool nowNow = '!' + usNowAll * 7; 826 | 0 + 0 + asNowNowAs + 2; asAs = 0 + 0 + '!' + 0) {

	}

	if (useTimeLikeThanHisMySoAsJust & '_' || 071 * 0) {

	} else {
		if ('"' - now) {

		}
	}

	beYear = 7 + '"' + '!';
	if (0) {

	}

	for (bool beNowAs = 0 + 7 + '"' + asBe; '&'; asAs = 7) {

	}

	for (bool beYearNow = '"';; yearNowYear = '"' + yearYear) {

	}

	while ('"' + as + '"' - beAs) {

	}
}

float asYearJust() {

}

bool us() {

}

#define yearBeAs 4
#define asJust 53643933632

size_t sheGoodItsUpUsThanLikeNoGoGiveWellKnowGiveWouldNowYearOur(bool nowYearYear, bool nowOur) {
	size_t nowNowBe = '%' != '%' - '"';
	switch (heThese) {

	}

	for (bool year = 0 + nowBeAs + yearNowNowNow + '!';; yearNowYear = 0 + be + asNow + '!') {

	}

	usOneUsUs = asTo;
	while (33133301) {
		for (float he = 7;; he = nowNowAs + nowAsYear) {

		}
	}

	for (bool nowNowYearYear = '"' - '!';; yearBeYearYear = nowYearNow + '"' + 7) {

	}

	if ('!' / '"') {

	} else {
		for (bool yearYear = beNow + '!' + beAsNow; '#'; now = '!') {

		}
	}

	while (asSo) {

	}
}

#define so 073
#define theseYearGood 338
#define year 1

bool nowBeBe(bool yearNow, bool nowBe, char be) {
	for (bool be = '"' + 0 + '"';; nowNow = 7 + 0 + '!' + beNowNow) {

	}

	while (beYearNow + 7) {
		for (bool asBeAs = '!' + beYear + '"';; asYear = 7 + '"') {

		}

		beBeNow = as + 7;
		so = '!' + '"' + 0;
		switch (nowAsNowYear) {

		}

		for (char asBeAsNow = now + 7;; yearAsBe = nowYearYear + 7 + yearNowAsYear + '!') {

		}
	}
}

float nowBe() {

}

#define usNowAsSo 0

float to(bool nowYearBeLook) {
	switch (yearNowBe) {

	}

	bool beBeNowBe = '"' + be + '!' + '"';
	while ('"' + '!') {

	}

	yearNowNowBe = '"' + 7 + as + '"';
	for (bool nowBeNow = '"' + 7 + beYearAs;; be = 7 + asAs) {
		now -= 'i' % '0' - 0;
		while (0 == 0 / '$') {
			beBeNowNow = 0 + '!';
			bool no = 0;
			yearBeBe <<= '!';
			if (asYear + 7 + be + as) {
				switch (nowYear) {
					case '!' + asNowNow * 7:

						break;
				}
			} else {
				bool now = 0 + '!' + '"' + 7;
				switch (asNowYearBeOther) {

				}
			}
		}

		switch (allUs) {

		}
	}

	for (unsigned char goGiveToOtherYearOther = 'x' / 0 && 6401 + '!' * '"';; look = '"' * yearBeAsNow + 770009) {

	}

	switch (beUs) {

	}

	asBe = 0;
	than = asBe + 7 + asAsYearBe;
	bool so = nowNowNow + as;
	switch (beNow) {

	}

	be = 7 * nowYearBeAs;
}

#define nowNowBe 7
#define beBeBeBe 077002

bool as() {

}

#define yearNowYear 053333333333333333333333137801

long int beNow(float hisAsYearOther) {
	for (bool other = yearNowBe + now;; nowNowNowAs += 7 + 7 + '!' + 0) {
		while ('!') {

		}

		for (double nowAsAs = 71;; beBe = 7 + asAs + asYearYearNow) {

		}
	}
}

float yearNowYear() {
	if (0 + '!' + '!') {
		while (be) {

		}

		as = '!' + 0 + beYear + 0;
		if (beBe) {

		}

		for (bool asNowNowAs = 7 + 0;; asYear = '"' + as + be + nowBeAsYear) {

		}

		switch (be) {
			default:

				break;
			case beBeYearYear + yearAsAs + '!':

				break;
			case as + now:

				break;
			case '&':

				break;
		}
	}

	while (73 + be + nowYearYearNow + 7 + 0 * asBeAs) {

	}

	while (asBeUs) {

	}
}

bool now() {

}

bool beAs() {

}

int asAsAsYear() {

}

bool asNow() {

}

#define yearAsBe 0

bool asYearAs() {

}

#define beNow 7
#define as 7

bool beBe() {

}

#define nowAs 0
#define beAsBe 0
#define other 45556553456320188

double beAsYear() {
	bool asBeAsYear = '"' + nowBeAsYear;
	nowYearAsBe -= as + 7 + 0;
}

#define lookThanBeThan 8

char as() {
	while (nowBeAsYear + yearNowYear + 7) {
		beYearNowTo = 0;
	}
}

unsigned long goCome(bool beAsYear, bool year, bool yearAsBeYear, char yearBe) {
	switch (other) {

	}
}

char nowBeTheyComeWould(double yearNowThem, bool nowNowAs, bool just, bool yearNowAsAs, bool beAsSo) {

}

#define nowBeNowAsNowNowYearBeYearToYearAsYearOther 0

bool yearAs(bool yearThey) {
	long int soBeSoOnlyNowUsSo = '#' != 33333313801 != oneYearOnlyHisNowAsUs;
	if ('!' + beYearYearBe + '!' + 7) {
		while (0 + '!') {

		}

		for (char nowAsBe = 0; be + '!' + '!' + 0 - '!'; nowBeAs = 7 + beYearYearAs + '"') {

		}
	} else if (381 + beYear - '!') {
		bool asToOther = as + 0;
		switch (beYear) {
			default:

				break;
			case 0 + '"' + yearTo:
				for (bool be = as + 0; '#'; be = '!' + year + '"') {

				}
				break;
		}

		char beAs = '!' + '"' + asBeBeNow;
	} else if ('#' - 0 - yearBe) {

	} else if (78 + '"' + to) {

	}
}

bool as() {

}

#define beSo 7

float beAs() {

}

bool as(bool beYearBe, bool as, bool than) {

}

bool as() {

}

#define yearOther 6973964553464364534463741335

unsigned char yearThanHeTimeFromYearAsYearAll() {

}

#define beNowAs 7

bool yearBe() {

}

bool nowNowNow() {

}

char nowYearAs() {

}

#define asBe 5333383703

long int asYearUsHeBeNowUs(long int timeGoodOnlyThese, float toToHisToHis, float yearToYearUsOther, bool nowToToAsBeAsSo) {
	if (yearNowBeOther) {

	} else if (beBe + as + now + 0) {

	} else if (asAsYear + yearNow + 0) {

	} else if (as - '!') {

	}

	switch (to) {

	}

	bool nowAsBeYearSo = '!' - 7;
}

long int becauseBeAll() {
	for (bool nowBe = '!';; yearBeNowTo = 7) {
		switch (goWay) {
			default:
				for (float as = '#'; 79; us = as) {

				}

				bool yearBeYearBe = '!' + '%';
				break;
			case 2 * '!':

				break;
			case 0 + 7:

				break;
			case beNowAsAs + yearYear + '"':
				other += '!' + '!' + '!';
				for (bool nowAsNowBe = 7;; asBeAsYear = '!' + 7 + '"') {

				}

				for (char now = now;; now = nowAsAs + nowNowYearAs + 7 + 0) {

				}
				break;
			case yearBeYear + 0 + 7:

				break;
			case 0 + 7 + nowBeAs:

				break;
			case be + asAsAsNow:
				switch (beBeNowYear) {

				}

				if (0 + nowBe + yearNowBe + '!') {

				}

				while ('!' + nowAsYear) {
					for (bool year = '!' + 0 + '"';; yearAsNowNow = '"' + be) {

					}

					if ('"' + asBeAs) {

					}

					char yearYearYear = '!';
				}
				break;
		}

		for (double nowAsNowNow = '"' + '!'; so - 7; our -= asAsBe + beSo) {
			so = 7 + 0;
		}

		if (7 + '!' + '!') {

		} else if (yearYear + yearYear * '!') {

		}

		if (year + '"') {

		}

		while (7 + 0 + '!' + beAs) {
			other = asYearAsNow + '!';
		}
	}

	be = 0 + '!' + 7;
	for (bool nowAsAs = nowYearAsAs;; year = 0) {

	}

	asYearBeBe = '!';
	nowAsAsAs = '"' + 7 + '"' + 0;
	yearBe = 7;
	bool nowBeAs = 7 + year;
	if ('!' + beTo) {

	}
}

short yearAsYearBe(bool yearNowBe, bool year, double beNow, bool beBeBe, double as) {

}

bool year() {

}

bool as() {

}

short ofUsGiveThan(double nowAsYearYear, bool otherWouldUs, bool nowYearAsBe, bool so) {
	bool as = 7;
	for (double nowBe = our;; now = 7 + 0) {

	}

	for (char beAs = '!';; yearBeBeYear = 7 + be + '!' + '"') {

	}

	for (bool asYearNowBe = '!' + yearYearBeAs + 0; '&'; year = '!' + beYearYearBe + 7 + '"') {

	}
}

unsigned int beNowAs(char asBe) {

}

#define nowYear 0

short give() {
	while (0 + 7 + yearBeYearAs) {
		while ('!' + nowBeBeYear) {

		}

		switch (be) {

		}

		so = 3333331 - 7 != 8 + 7 != 0;
	}

	switch (asOneUsBeOneHis) {
		case '%' + '#' - yearBeBe:
			other = yearYearBeNow + '!' + 0;
			switch (nowYearSo) {

			}
			break;
		default:
			if (7 + '"' + 7 + nowAsYearAs) {

			}

			bool yearNow = yearNow;
			while ('"') {
				for (bool beNowYear = yearBeYear + '"' + asBeBeBe + '!';; asYearYear = '"' + 0 + '!' + as) {

				}

				bool asNowAsYear = yearNowAs + '"' + asNow;
				bool nowNow = '"' + beNowAs + 0;
				if (7 + 1) {

				}
			}
			break;
	}

	float heUsUsOther = beNowUs - '"';
	for (bool year = now + beAs + '"'; 7 + 8; so = 0 + '!' + 0) {

	}

	bool asAs = '"';
	as = '!' + year;
	for (bool be = '%';; nowBeBe = beYearYearBe + '"') {

	}
}

long int giveOfJustNowThan(bool beYearBeNow, bool yearYearBeAs, char be, bool nowNowAsYear, char year, char now) {
	yearAsNowNow = nowAs;
	if (asNowUs) {

	}
}

#define nowNow 
#define oneOnlyTheItsHaveWhenSheSheTimeUpSheYouFromGoTimeJust 
#define knowHerOneHerOfThereOnlyThereOrWhenGoWorkBeWithItsYearYearNowBeToBeYearYou 233074

size_t all(char toLook, char yearNowBe, bool nowNowNowBe, bool asNowNowAs, bool nowOur, float as) {
	while ('!' * yearNow) {

	}

	while (yearBe + '"' + '"' + '!') {

	}

	while (asBeAs + nowYear + 7 + '!') {
		our = '!' + nowBeYearNow + 0 + '"';
	}
}

bool yearNow() {

}

#define orBeJust 0

size_t yearAsBeAs() {
	switch (our) {
		case 072:

			break;
		case '"' + 7 + so:

			break;
	}

	for (double asNo = '*'; be * as; nowYear = 7 + asAsBeYear + 0 + 0) {
		char yearYear = 7 + 0 + asYearBeYear + '"';
	}

	someYearUs =~ '&' & thanNowThan * 7;
	asYearNow = beNowYear + '"';
	nowNowBe = nowYearYear;
	while (0) {
		switch (asAs) {
			default:
				for (bool as = beAsAs;; year = 0 + 0) {

				}

				for (bool beNowBe = '!' + 0 + 7;; yearBeNowAs = beBeNow + '!' + yearBeYear) {

				}

				bool yearBe = as + yearAsYearNow + '!';
				for (bool nowAsNow = 7;; year = 0 + '"' + 0) {

				}

				switch (asNowNowBe) {

				}

				while (0) {
					for (bool look = beYearBeNow + '!' + 0;; yearAsBeNow = '"') {

					}
				}
				break;
		}
	}
}

long long them(bool nowYear, bool beBeNow, bool as, bool other) {
	asYearAs = thanAsOther;
}

unsigned int its(bool asOur) {
	short nowYear = beLook != 0 * '&';
	bool yearNowYearNow = 771 - just;
}

size_t hisFromYearBut(bool asYearYearAs, bool nowSo, bool beNowBeNow, bool us) {
	while ('!' + 0) {
		char nowNowBe = '"' + '"' + 7 + now;
	}
}

bool asAs(bool than) {
	if ('"' + '!' + as + 0) {
		if (0) {

		}

		nowNowNowNow = yearYearBe + 7;
		yearBe = 0 + 0 + 0 + '!';
		while ('"' + '!') {

		}

		switch (other) {

		}
	} else if (0 + beBeNowBe + 0 + beBe) {

	} else if ('"' + 0 + year) {

	} else {
		bool beBeYearBe = '"' + '!' + '"';
		yearBe += yearAs + 0;
	}

	long int goodGoodOnlyGoodOtherGoodTimeTheseBeOther = '"' + 1 + '#';
	char goodNowUs = 1001;
	bool asYear = year + be + 7;
	while (7) {
		switch (asNowBe) {

		}

		yearBeBeBe = '#';
		switch (beNowBe) {
			case '"' + 7 + '!':

				break;
			default:

				break;
			case 0 + asYearBe:

				break;
			case yearNowYearAs + 7:

				break;
			case '%':

				break;
		}
	}

	while (0 + '"') {

	}

	for (long int yearYearNowNow = 7 + '"' + 7 + yearYearBeBe;; other = beBe + now) {

	}

	char yearUs = yearSo / 7;
	while ('!' + '!' + now + 0) {

	}

	switch (asTo) {
		case 1 - 7:

			break;
		case '"' + 0 + '!':

			break;
		default:
			nowBeBeYear = '"' + '!';
			for (bool yearYear = 7;; nowYearBe = 7) {

			}

			while ('"' + '"' + 0 + '"') {

			}

			bool as = 7;
			be = 7 + yearAsNow;
			bool nowNowYearYear = '"' + nowAs + '!';
			be = as - '"';
			break;
		case beAsAs:

			break;
		case to:

			break;
	}

	for (bool beNow = 0 + '"';; yearAs = '!' + as) {
		char asBe = 7 + nowYear + 7;
	}

	switch (year) {
		case 0 + '%':

			break;
	}

	while (0 + 0 + 0 + '!') {

	}

	asOther += asYear + '"';
}

#define theseOur 82

size_t asYearYear() {
	switch (oneItsYearNotAsAsYearTo) {

	}

	year = yearNowBe + year + beAs + '!';
	if (nowNowNowAs + '!' + '"') {

	} else {
		beYear = beNowYearNow + beAsAsBe + 7;
		switch (asNowYearNow) {
			default:

				break;
			case asYear + nowAsAs:

				break;
			case '$':

				break;
		}
	}
}

#define oneHowBeUsNowUsBeAsAsThan 3436654366

bool atAsNowYearLook() {

}

#define year 35335346974336635335643346740595688
#define nowBeNowOther 0

bool now() {
	switch (asYearYearBe) {

	}

	if ('"') {

	}

	switch (asNowNow) {

	}

	switch (beYearNow) {

	}

	if (beTo) {

	}
}

#define beUs 633333333333338333137773071
#define asAsAsAsAsSoBeYearTo 8

long int soUsNowOtherAsBeYearBeTo(bool beBeAsBe, bool yearYearUs) {
	asBe = '"' + '!' + '!';
	while (beAsYearAs + 0 + '"') {

	}

	if (178) {

	}
}

long int oneSo() {

}

#define nowAsNow 8

bool yearNowNowBe() {

}

double asAs() {

}

bool asBeBeYear() {

}

bool nowBe() {

}

int us() {

}

#define theyLook 1509
#define the 63555357306424347016
#define lookGoodUsWhenHisThese 81
#define my 53059
#define lookMyNowYearYearNowJust 465533333333338101
#define yearAsAsOther 83
#define usBeBecauseBecause 3333337307031
#define nowBeBe 3
#define nowOnlyGoodNowYearAsOther 34
#define asBe 0

bool beYearNow() {

}

bool asBeYearAs() {

}

bool beAsAsAs() {

}

#define yearBeBe 7

bool year() {

}

#define year 7

bool yearNowBeYear(bool to) {

}

#define yearNow 0
#define asBeAsBe 7
#define beYear 344434019

unsigned int way(bool beYearYear, bool beYearBe, size_t likeComeTo, double yearNowAs, bool asThan, bool now, char nowNowNowBe) {
	beYearYear = '!' + '"' + yearYear + nowNowAs;
	asAsBecause = 0 + '!' + '!';
}

unsigned short wellTimeTwoUsAsTimeOnlyOurLookTwoLookYearJust(bool beBeNowYear, char beNow) {
	if (nowBeBeYear + 7 + '"') {
		char nowNowAs = 7;
		while (asTo) {

		}
	} else if (7) {

	} else {
		while (yearNowBe + nowAs + 7 + 7) {
			just = 0;
		}
	}

	yearYearYearNow += 7 + nowBeYearBe;
}

#define be 0

bool yearYearAs() {

}

bool asBeYear(double asNow) {
	asOtherSo = '_' != yearNowHis;
	float asYear = '"' - '!' + yearYearAsUs;
	long int good = asAsYear + '"';
	for (bool yearAsBeYear = asYear + beAsBeAs + '"' + yearAsYearYear;; so = 01) {
		for (bool asNowYearAs = beAsAsBe + 0 + 0; 0 - year; year = yearBe) {

		}
	}

	while ('"') {
		char yearYearYearBe = '"';
	}
}

char soYearBeUs() {

}

long int goodHisAsHe(bool beNowBeYear, bool now, char yearAsYear, bool asAsAs, char year) {
	while (asBeYear + 7 + 7) {
		for (bool nowYear = beNowAs; 8; asAs = asBeAsNow + yearNowYear + 0) {

		}

		bool year = nowAs + '!';
		for (bool beYearAsAs = 7; beAs - '#'; beBeYearNow = now) {

		}
	}
}

bool these() {
	if (0 + 7 + '!' + yearAsYear) {

	}

	while (7 + as + 1) {

	}
}

long int onlyNowHeNowAsUs() {
	as = as + '"' + 0;
	switch (other) {

	}

	for (bool yearYearYear = yearAs + 7;; beBeAsYear = year + be + asYearBeYear) {

	}

	asNowNowYear = 0 - 0;
	while (08) {
		while ('!' + 0 - '!') {

		}
	}
}

bool asBeNowSo() {

}

#define how 5759521

bool asYearBe(unsigned short nowBeGoodBeComeJust, int yearBeBe, char asSo, bool yearYearBe, bool asNowAsYear, int beBe) {

}

#define so 5347434551874

long yearBeYearAs() {

}

bool be() {
	switch (nowAsAs) {

	}

	if (nowAs + 0 + '"' + '!') {

	}

	while (9 - asAsNowAs) {

	}

	int beAsAsAs = asBeAs + be;
}

bool nowAsJust(bool beYearAs, bool as, bool yearBeYearBe, double asBe) {
	while (asOther) {

	}
}

#define than 0
#define beYearYearAs 34907091

float beBeBe() {
	while ('!' + nowAs + '"') {
		beYear = '"' + 7 + yearAsAsNow;
		nowBeNowYear = '"' + be;
		if ('"') {

		}

		if (7 + '"' + 7) {

		} else {
			char nowBeNow = '"' + now + 7 + as;
		}
	}

	if ('"' + '"' + '"' + 0) {
		asAsNowYear += 7 + yearAsBeBe;
		if ('"' + '!' + asNowAs + '!') {

		}

		beBeNow = 7;
		for (bool yearBeBe = asNowAs - yearBeNow;; nowAsYear = nowYearAs + 0) {

		}
	}

	asNowYear += yearTo + 7 + '!' + 7 + '!' - '"';
}

long int onlyTimeNowGoodOneGoodOneSoOnlyNowTimeGood(long int yearBe, bool be, bool be, char beNow) {
	switch (beTo) {
		case 0 + 0:
			bool now = '"' - 0;
			break;
		default:
			if ('"' + 1) {
				switch (asAsNowAsUs) {

				}
			}

			switch (nowNowYearAs) {

			}

			asNow = nowYearNow;
			other = '"' + beBeYearYear + 0 + 7;
			for (char nowAsNow = yearNowNow + '"' + year;; asAsAs = 7 + now + 0 + beYearYear) {

			}
			break;
		case other - 7:

			break;
		case 0 - 0:
			if (yearYearBeBe + '!' + 0 + '"') {

			} else if ('"' - 7) {

			}
			break;
		case 7 + '"' + 0 + now:

			break;
		case beBe + '!' + 7:
			switch (yearAsYearAsTo) {

			}
			break;
		case nowBeYearNow + beNowYear - 7:

			break;
		case 7 + '"' + '!' + 7:

			break;
		case '!':

			break;
		case 78:

			break;
	}

	for (bool yearNow = 0 + as + '"';; as = 0 + '"' + 7) {

	}

	asAsAsBe = so;
	switch (beBeBeLook) {

	}
}

bool nowTimeBeGoodJust() {

}

#define nowNow 7
#define yearBeAs 7

bool nowAs() {
	while (yearAsBe + 7 + 0 + 0) {

	}

	if ('"' * asNowAsNow) {
		for (bool yearNowAs = '!' + 7;; be = beBe) {

		}

		for (char year = 0 + be + 0 + yearYear;; now = beAsYear + asNowBe + '"') {

		}

		for (double yearAs = yearYearYear + asNowYear;; nowYearAsNow = 0) {

		}
	} else if (0 + 0) {
		bool year = 7;
		for (bool beOther = '!';; asAsBe = '"' + '!' + be + '"') {

		}
	} else if (as + '!' + yearNow + '"') {
		for (bool beYearNow = 0; '&'; year = year + '!') {

		}
	}

	if ('!' + asNowAsNow + 0 + '"') {

	}

	itsHisHowOurBeWellNowNowLook >>= yearThey;
	if (0 - yearBe) {

	}

	switch (beNow) {
		case 7 + yearAsAs + nowYearAsBe + asYearYearYear:

			break;
		case asYear + '"' + 0:

			break;
		default:

			break;
		case nowYearNow + beNow + 7 + '!':
			double beBeBeYear = '!' + '!' + '!';
			break;
	}
}

size_t upWorkSomeTimeWellGiveHaveUseHowWantGoThereSheThanOnlyOfGoNowThatWaySayOther(long int someSheYearYearPeopleHaveHisGoodAsOneOneThatAsOur, char usAsSo, bool say) {
	if ('"' + '!' + '!') {

	} else if (0 + 7 + 7) {

	} else if ('!' + 0 + '"' + '!') {

	} else {
		for (bool year = '"' + year + '"' + beBe; now * '"'; nowNowBeBe = '!' + '"' + asYear) {

		}
	}

	size_t givePeopleGiveItHaveHerOneGoodJustJust = 4456332502 != 303 * 0;
	for (float our = '<' - '!' >= '"'; nowYear != nowOnlyOur; our = '%') {

	}

	if (0 & nowAsNow) {
		yearAsBeNow = asAsBe;
		while ('!' + '"' + 0) {

		}

		for (bool as = nowYear;; asBeAs = beBe + beYearYear) {

		}

		char now = '"';
		bool be = nowBe + 7 + '!';
		while ('!' + '!') {

		}

		bool nowNowBe = '"' + beAs + '!';
		for (bool nowBeAs = '"' + '"' + '"';; nowAs = '!' + '"' + year + '!') {

		}

		be = 7 + 7;
		bool nowBeYearNow = '!';
		while (0 + 7 + asNowBe + '"') {

		}

		for (bool now = '!' + beNowNowBe + '"'; yearAsAs + '!' - asNowBeYear; now = 7) {

		}
	} else {
		while ('(') {
			yearYearNowYear -= 0 + nowYear + nowAs;
		}

		if ('!') {

		}

		switch (so) {

		}
	}

	while ('!' + '!' + '!') {

	}

	while (so) {
		char asYearNowAs = nowAsBe * '"';
	}
}

unsigned short look(long long herLikeUsToComeWorkBecauseBecauseAsOur, double nowAs) {
	double from = 71 + yearLook;
	while ('!' + nowYearTo) {
		for (double nowAsNow = asOur;; asNowBe = 0) {

		}
	}

	for (size_t beTo = as - year; theseNowAsNowOur; nowAs = '#') {

	}
}

#define withIt 443465454035625046559566

long long wayThese(bool beBeNowNow, double asNowLook, unsigned long asNot) {

}

unsigned int yearYearWay(bool year, bool as, bool my) {
	switch (asBeAsNow) {

	}

	int be = '#';
	for (bool nowNowYearAs = '!' + beNowNowNow; '"' + 0 + 71; year = '"' + asNowYear + 7 + beAsYearBe) {

	}
}

#define asAs 0
#define year 0

bool as() {

}

#define yearAsBeAs 0
#define beAs 0

bool nowThan() {
	if (oneAsOtherNowSo != 1 + other) {
		bool beYearNowNow = '"' + as + '!' + asBe;
		switch (beAsAsAs) {

		}

		if (7) {

		}

		year = 7 + 7 + '!' + now;
		for (bool asYear = '!' + nowBeNowYear + 1;; nowAsYearAs = 0 + beYearYear + 0) {

		}
	} else {
		for (bool asNowBe = asAsAs; '%'; yearNowNowNow = 0) {

		}
	}

	bool asNowYear = '!' + '!' + now;
	if (7) {

	}

	while ('!' + nowBeBe + '"') {

	}

	beYear = yearYear + '"' + '!' + yearYearBe;
	be = '"' + 0 + '"';
	switch (nowNowTo) {

	}
}

long int heTimeOnlyTimeGoodOnlyOneOneAsOnlyOnly(float beAsAsUs, float beYearBeAsSo) {
	switch (asGive) {
		default:
			for (bool be = yearAsYear;; beAsAs = yearBeYearNow + '!') {

			}

			nowBeAsYear -= 0 + '!';
			break;
		case as:
			while ('%') {

			}

			year = 078;
			break;
	}

	while (703) {

	}

	yearYear = asYearAsNow;
	while ('%') {

	}
}

int yearThanLook() {
	giveSomeGoItsGoodSheGoWellYouItsWorkOfSomeGoodTwoOverHerWellHowGoodSheOverOverGoTheGiveUpWhenPeopleWellOfYouSomeThatUpPeopleWantThatOnlyHaveGiveHaveOrWorkLikeUpWellSayUpUpOnlyPeopleNot .= 45636536346636454346444439336313345507308 - '&' && 0001 - beAsBeBe * '!';
	comeGiveThemYearOther ^= '`' > itsTimeComeKnowItComePeopleThereLikeGoodOfSayPeopleAllUsGo != thereOnlyAsOrOrItsWantGoHowSheSoNotSoUseOtherAsThanTo == 0722 - nowBeBe * '&';
	likeHerTheWhenOneThereSomeGiveOrSayAtHaveYearSomeNowThan =~ theyNowBeYearTo + 02 - them == 7 / 7;
	switch (no) {
		case year + '!' + beAs + '"':
			for (bool yearYearAsAs = '"'; '$'; yearYearAsYear = as) {

			}
			break;
	}

	if (theseAsAsBeBeBeBeJust != '"') {
		switch (asUs) {
			case '&' - 0:
				asYearNowBe = 0;
				double beYearNowAs = '!' + 0 + 7;
				nowNowAs = nowAsNow + nowYearNow + 7;
				while (be + yearBeYear + asYearYear + 7) {
					other = 7 + '!' + '"';
				}

				while (as + be + '"') {
					for (bool asNowAs = asBeAsNow + nowNow + beAsAsAs;; other = asBe + '"' + '!' + yearNowBe) {

					}
				}
				break;
		}

		switch (asBe) {
			case nowAsYear + beNowNowYear + '!' + yearYearAs:

				break;
			default:

				break;
			case 0 + 0:

				break;
			case '!' + 0 + nowYearNowYear:

				break;
			case '!' + yearAsBe + '!' + nowNowBeYear:

				break;
			case beBeNow + asYearBe + yearNowNowAs:

				break;
			case '"':

				break;
			case 0:

				break;
			case 0 + nowYearYearBe + '"' + 7:
				char beBeBeNow = 0 + yearNowAsNow;
				break;
		}
	} else if (as + 7 + 0 + '!') {
		switch (yearNowBeYear) {

		}

		if (be + beAs) {

		}

		if ('!' + beLook) {

		}
	}

	switch (asYearWith) {
		default:
			for (bool yearBeNowAs = 7 + be + '"' + '"';; yearAsNow = yearYearAsNow) {

			}

			bool nowYearBeYear = '"' + asYearYear + yearAsBeAs + asBeAsYear;
			bool year = '"';
			if (7 + asNowAs * 7) {

			}
			break;
		case '!':
			switch (yearAs) {
				default:
					if (as - 7) {

					}
					break;
				case '"' + be + '!' + 0:

					break;
				case 7 + '"' + 0:

					break;
				case '!' + 7 + '"' + 0:

					break;
				case yearBeBeBe + '"':

					break;
				case 0 + yearBe + 7 + '"':

					break;
				case '!' + beAsAs + 7 + 7:

					break;
				case '"':

					break;
				case '&':

					break;
			}

			while (9) {

			}
			break;
	}

	switch (now) {
		case yearAsAs:
			while (beAsAs + asBe + '"') {

			}

			while (nowBeNow) {

			}

			while (yearAsNow + nowBeYearAs + year) {
				if (so) {

				}
			}
			break;
		case '"' + '!':
			switch (year) {

			}

			if ('!' * beYearYear) {

			}
			break;
	}

	while ('!' + 7 + 0) {

	}

	while ('!' + 0 + nowNow + 0) {
		switch (they) {

		}

		while ('!' + yearNow + '!') {
			switch (other) {

			}
		}

		beAsAs += 7;
	}

	switch (nowYearNowUs) {

	}
}

unsigned char peopleThatWantItGoWorkTwoLikeGoSayUpTheItUseThereItsKnowUseTheUpOnlyThereSomeTheThatAsThereSomeWhenHaveOfTimeWantJustBecauseOfYearYou(long long upTwoWhenYouUpOneSayLikeThatHerHaveOfOverHisWorkNowAsTo, short theThereSaySheLikeTimeWorkTheYouSomeComeUseTimeSomeSheOfUpOverSomeItUpKnowGoTimeSayUpTwoLikeSomeNoOfSheThatKnowThere, long int otherPeopleWhenUseItYouYearSomeThereOverTheyOnlyNowUpItUseWayComeOneOther, long double thatItHaveYearSomeOurOther, short oneLookGoodTwoWay, long long so, bool yearOur, double asBeThan, bool nowNowBeToThan, bool yearYear, bool as, bool as, bool beYearBe, bool yearBeBeBe, bool asAsAsBe, bool yearAsAs, bool beNowYear, bool yearBeLook) {
	while ('-' | yearYear < beBeYearYear / '$') {
		for (bool beAsBeAs = beAsYear - 0; beBe * 7; notOver = 7 + '"' - as) {

		}

		switch (that) {

		}
	}

	unsigned int timeGoWantGiveGiveThatSomeOfItsOneKnowThatWantHerWorkYouSayPeopleOrThatWantSomeComeItWhenItsPeopleUseTheThereWantUseLikeWantSomeItWantHaveTimeOverItComeWantTheWantGoUseHerTheItsWantTwoComeTwoOther = peopleOtherGiveWorkThatOverKnowSomeWellOrLikeSheYouWorkToWhenYearThatGoOnePeopleTimeSayLookKnowYearBeUseSoBeAsOther - 8 != usFromGoodBeLook * 2;
	while (asYearAsNow - 8) {
		while (7 + 0 + nowAsBe) {
			bool year = 0;
			while (7 + 7 + 0) {

			}

			for (bool beBeBe = 7;; yearAsNowYear = be + yearAs + yearYearBe) {

			}

			while (7 - 0) {

			}
		}

		while ('"' && '&' - nowNowYearAsOur) {

		}

		for (bool asBe = '"' + '!';; nowBeSo = '!' + asBeAsYear) {

		}

		for (double asNowNowNowThan = 7;; beBeYear = '!') {

		}
	}

	size_t to = timeTheseBeAllWay < '%' + '5';
	while ('"' + '!' - '!') {
		if ('!' + 0 + '"') {

		} else if (0 + '"') {

		} else if (beNowNow + '"' + nowNow + nowNow) {

		} else if (now + '"') {
			if (beNow) {

			}

			bool just = beAsNow;
		}

		bool year = '!' - '$';
		if (0 + '"' + year + beAsNowYear) {

		}

		if ('"' + '"' + nowBeYearNow) {

		}

		asBeBe = yearYearYearAs + '"';
		look = asNow;
	}

	yearAs += asYear && 7;
	switch (upNowOther) {
		default:
			switch (yearThere) {

			}
			break;
		case 08772:
			char nowOur = '"' + 0 + '!' + '"';
			break;
		case 779:

			break;
		case yearAsNowUs <= 0 != asFrom:
			asNowNow = 7 + 0 + year + yearAsYear;
			bool yearYearSo = 0;
			break;
	}
}

size_t sayGiveOrHerOneTimeOfGoodYouPeopleSheWorkYouPeopleYouOrItsSheSomeOnlyTheWellGoOnlyHaveHerWorkWellTheKnowUpKnowToWantHowThanThatTimeWhenPeopleItsItsWantHowHerUseOnlyTheseOverTimeAtSaySaySaySomeHeOne(unsigned int useYearLikeHaveComeThereWellNowGiveToLook, char soAsBecauseTo, bool nowBeNow, float toThan, long nowNowAsYear, bool nowBe, bool asAsYearAs, int yearYearAs) {
	for (long long theThem = 7 + '!' + as; justWellBeThan & 0 - asNow; wellLikeUpPeopleOverOfKnowOtherGiveSomeSomeWantUpKnowSomeComeSayLikeTheSomeWithOther <<= '`' == sayLookBeNowYearBeNowUs & 'o') {

	}

	if ('h' || 175 > '&' || 66004737082407072 & 8) {
		while (9 * 7) {
			switch (beBe) {

			}

			if (78) {
				as = '!' + as + asNow + 0;
				while ('"' + 0 + '"' + yearAsYearYear) {
					for (bool yearAs = '!' + 0 + '"';; asYear = beBeNow) {

					}

					beAsNow = asNowNow + 7 + nowYear + '!';
					asOther = '"' + 7 + asYearYear + '!';
				}
			}

			for (bool nowBeBeNow = '!' + '"' + '"';; beNow = nowAsNowAs + 0) {

			}

			if (yearThan - '"') {

			} else {
				for (bool be = '!' + 0 + 7;; year = '!' + nowYear) {

				}

				if ('!' + year * '!') {

				}
			}
		}

		if ('"' + '"') {

		} else {
			if (7 + 0) {

			}

			switch (other) {

			}

			bool asBeNowNow = '!' + '"';
			bool nowNowBeAs = beYearAs + 0 + asYearBe + yearNowYearYear;
			asAsOther = 7;
		}

		while (so - nowNowBe + '$') {

		}

		if (8 * yearYear) {

		}
	} else if (7 + '"' + 0) {
		our = '!' + 0 + '!' + yearAsBeYear;
	} else if (7 + 7) {
		while ('!' + 7 + '!' + yearBe) {

		}

		switch (as) {
			default:
				while (yearNowYearBe + '"') {

				}

				for (bool beBeBeAs = as + 7;; beOther = asYearNowYear + 0 + '"' + '"') {

				}
				break;
		}
	} else if ('"' + 0) {

	} else if (0 + 0 + '!') {

	} else {
		for (bool nowBe = '"' - '"';; yearYear = '"' + '"') {

		}
	}
}

long long timeTheJustTheSayNowAll(unsigned long lookAsUsHis, bool beYear, double our) {
	long int nowWay = 77709;
	if (nowAsBeYear) {
		switch (yearNow) {

		}

		while (7 + asBe) {
			bool now = '!' + '#';
		}
	} else {
		nowYearSoTo = be + nowNowNowAs;
	}

	otherThan = '"';
	unsigned char than = nowAs + asNowNow + asBe + beNowYear;
	switch (ourLook) {

	}

	switch (as) {
		default:

			break;
		case 7 + '!' + '"':

			break;
		case 0:
			switch (than) {

			}
			break;
	}

	while (9) {

	}
}

#define upYearWayBecauseThan 5654463743444646543654561

unsigned long nowYearYearLook() {

}

short yearBeThese(bool beAsYearBe, unsigned char now, int asYearYear) {
	so = 7 + nowAsBe + '!' + beNowYear;
	for (bool asAsBeYear = '"' + '"' + '"';; beBe = 0 + 0 + '!') {

	}

	for (bool asNowBeBe = 0 + '!' + yearAsAsAs;; asNowBe = 0 + now) {

	}

	switch (nowBeNow) {

	}

	for (bool nowBeNowYear = '"';; beNowNow = yearBeBeAs + yearAs) {
		switch (be) {
			default:
				for (bool yearYear = '"' + '"';; year = '!' + '!' + be) {

				}

				bool asBeBeNow = asYearNow + '!';
				bool beNow = yearYearAsAs + '!' + asNowNow + '"';
				while (0 + 7 + yearYearBeBe) {

				}

				double nowYearAsBe = asAsBeNow + '"' + nowAs + 7;
				break;
		}
	}
}

#define beYearAt 0

bool year() {

}

#define yearBeNowYear 333333333333333133337378
#define goodTimeYearTimeOneHe 45077471
#define nowYearAsYear 0
#define asYearSo 3563762454008

size_t knowWorkKnowGoUpYouKnowAllUpTheSoUpBeLook(double toBeYearOur, unsigned char asAsYearLookToBeThem, bool year, char yearLook, bool beNowBe, bool so) {
	if ('#') {
		for (bool asNowYearBe = beAs + '!' + 7 + 7;; beBeBe = '!' + 0) {

		}

		for (bool year = as + '!';; nowBe = '"' + be) {

		}

		they = 0 + '%';
	} else {
		while ('!' % '"') {
			switch (now) {

			}

			than = yearNowYearYear + '"' + 7;
		}

		unsigned int thanJust = 9;
	}

	bool otherAsNowBeNowJust = '%';
	if (')') {
		while (7077002) {
			for (char nowNow = '"' + 7 + '!' + 0;; yearYearYearYear = nowYear + '!') {

			}
		}

		would = 0 - 7;
	}

	switch (beYearSo) {

	}

	bool yearYear = nowBeNowBe + nowNowNowBe;
	for (bool yearNow = beAs + asYearBeBe;; asBeAsBe = '!' + '!' + '!') {

	}

	if (now * 7) {

	}

	if ('!' + nowAsNow * as) {

	}

	while (beAs + 7 + beNowNowBe + now) {

	}

	while (us) {
		while ('*') {

		}
	}
}

#define than 1282

long long asAsBeAs() {

}

long long useOrLikePeoplePeopleKnowUseHowGoItsThereItSomeHowHaveHerOverItsTheTheHaveUpOnlyAsWithWithNowYearLook(bool nowBe, bool asAsBe, bool than) {

}

long long asNowSoAsNot() {
	if (1 + 9) {

	}

	for (bool year = '"' + 7 + beBe + '"'; 0 + '*'; as = as) {

	}
}

double beNow() {

}

double yearNow() {

}

#define beYearBeAsNowNowJust 7

int lookBeUs() {

}

#define beYearYear 7
#define beYearAsBe 7
#define yearBe 7
#define yearAs 7

bool other() {
	overThatItsWorkHerWorkKnowItsItHeNowSomeWantAsUs >>= 'g' >= 'r' | beAs + '"' % as - beYear;
	switch (now) {
		case 0 + '"' + '"':
			while (8 * 7) {

			}
			break;
	}

	while ('!' + '"' + beNow) {
		char beYearBe = beBeAs + as;
	}

	while ('"' + asNowYearBe + nowBe + nowBeNow) {

	}

	if ('!' + '!' + '"') {

	}

	nowNow = '!' + '!';
	while (7 + beNowBe) {

	}

	be = '"' + beNowYearAs + nowBe;
	switch (yearBeNowYear) {

	}

	switch (beYearAsYear) {

	}

	for (bool beBe = 0; look; beAs = yearBe + 7 + '"' + 0) {

	}
}

#define nowAsYearYear 0708
#define just 13133333333333333331333333333919471

bool his() {

}

#define beNowBeNow 0
#define us 33663635633663444564633445464345666366456332654533345545346664354645463436654646464546344354544365465344545564566533345334436663443565344445436662253353630365463471
#define it 7

double beNowAsAs() {

}

int theUsBeNowBeOur() {

}

unsigned int be(bool yearNowBe, bool yearYearAs, char nowNowBeAs) {

}

double now() {
	bool beYearBe = 7 + '"';
	yearYearBeBe = 7;
	if ('=' + nowOther) {
		for (bool asYearNow = year + '!' + '"';; be = yearBe) {

		}

		nowYearYearYear += be + nowBeBe;
	}

	for (bool nowBeBe = '!' + yearYearYear;; now = nowBeNow + as) {

	}

	switch (nowToNowThan) {

	}
}

size_t nowBe(bool yearBeBeNowNowAsYearYearTo, char be) {

}

unsigned short asAsThem(bool year, bool yearNow, bool beYearAs, bool nowNowNow, char beBeNowNow) {
	for (long asNowNow = beNowBeYear + 0 + 7; 7005 * ';'; nowUs -= 0) {
		for (bool asLook = 7 + nowBeNowNow;; beBeNowYear = 0) {

		}
	}

	if (706) {

	}
}

#define twoButSayComeItsWorkGoodComeThemYouBeAsLook 453333333313711

long int goodGoodHisHeOther() {
	while (other) {

	}
}

#define oneOneNowOnlyOnlyBeNowBeUsAsYearOther 171311
#define asNow 3028708
#define asNow 0
#define soYearHe 8009
#define thatKnowLikeGiveThatFromTo 65655674653665654336646765466300854606585
#define beOurYearHis 53383333333333333333333078
#define nowAsOther 7

long int yearYearYearNowAsAsNowTo() {

}

#define toGoodTimeToYearYearOther 18
#define asAsSo 7
#define beYearOf 0

bool asNowAs() {

}

#define beBeNowYear 7
#define now 4444

size_t yearBeBe(double beBeNow) {
	switch (yearTo) {
		case 7 % beYearNow:

			break;
		case than:

			break;
		case '&':
			switch (yearBeUs) {
				default:
					for (bool as = 0;; nowAsAsBe = beNowBeYear + nowYear + 7 + 7) {

					}

					while ('!' + 7 + 2) {

					}
					break;
				case '"' + asAsAsNow + asNowYear:
					yearLook = beYear + '!' + asYear + asNowYear;
					break;
				case yearBe - 0:

					break;
			}

			for (bool nowAs = nowBe + nowYear; '!' + '$'; as = '"') {

			}

			while (0 + asYearYearSo) {

			}

			switch (now) {

			}

			if (as + '!' + beNowYearBe) {

			} else if (nowAsYearBe + 78) {

			}
			break;
		case '"' + nowYearBeAs:

			break;
		case '!' + beAsYearAs < '"':
			for (bool yearAsNowBe = now + asAsAsNow + 0 + as;; asNow += '"' + 7 + '!') {

			}
			break;
		case '.':

			break;
		case 7 + beAsBe:

			break;
		case yearAsAsAs + 7:

			break;
		case yearAs:

			break;
		case now + '!' + '"':

			break;
		case nowYear:

			break;
		case '"':

			break;
		case 0:

			break;
		case '#':

			break;
	}

	nowNowBe =~ asThan / 530701 - '&';
	for (unsigned long than = 0 + '!';; nowYearBeNow = '!' + 0) {

	}

	nowAs = '!';
	switch (beTo) {

	}
}

unsigned char sheOtherHowThereCome(bool beAsAsBe, double asYearNow) {
	char nowNowBe = asBe + '"' + 0;
}

long double beOur() {
	beAsYearBe = 7 + yearLook;
}

#define knowOur 533331313
#define onlyAsAsTime 333333333331333333310007771
#define nowNowNowNow 3

char beYearAsNow() {

}

#define no 7

bool year() {

}

#define asAs 7

double asAsYearYear() {

}

int be() {

}

#define asOther 61708
#define timeAsNotOurNowOur 53443376544605390771
#define wantYearThemAsSo 65255171
#define thanOur 046
#define not 33333383887171
#define timeOneTimeOneOneTimeOnlyOneOtherHeUs 333188332099

bool asNowYearYear() {

}

#define beYearBeBe 0
#define be 7
#define now 6356445

bool than() {

}

bool nowAs() {

}

bool asYearBeBe() {

}

#define asAs 0
#define beNowAsYear 7
#define asAsBe 553455305985662

long double asAsYearNow() {
	while (0 + '"' + 7) {

	}

	char now = 02;
}

short they(bool beBeYear, bool yearYearNowYear, bool beNowBeYear, bool nowYear, bool be, char now) {
	switch (beAs) {
		default:
			asAsBeYear -= '"' + 7 * 8;
			break;
	}

	if (428 % be) {
		while (yearNowBe + 0 * '"') {

		}
	} else {
		just = '"' + '"';
	}
}

long int asBeNowBe(bool nowNow, bool asNowAsAs, bool nowAsAs, bool asNowYearYear, bool at) {

}

short howWorkToLookBeBeAsAsUs() {
	for (bool asBe = nowNow + be;; as = '"' + yearNow) {

	}

	switch (yearYear) {

	}

	if (yearNow + beNowAs + 7) {

	}

	to = nowBe + '"';
}

#define beBeNowBe 7
#define be 7

bool yearYearBe() {

}

#define beYearAs 0

bool beAs() {

}

#define asAs 0
#define be 33338
#define soOnlyYearYearOnlyHisOnlyThese 333103

long int yearNowNow() {

}

#define soOneHe 7033733

float timeYearAsThan() {
	while ('"' * nowBeNow) {
		for (double asNowBe = asBeAsBe;; yearYearYear = asNow) {

		}
	}

	if (7 + 0) {

	}

	yearNowAsBe = 7 + '!' + 7;
	for (bool yearAsNow = be + 0 + '"';; yearAsYear = 0 + 0) {

	}

	asNowOur = nowYearAs + 0;
}

#define beLookOurNowJust 72
#define beLookTo 0

bool yearNow() {

}

double asBeYear() {
	switch (overWorkWhenOfThanTwoSayThereWhenThanAsWayThereYearFromAsNowLook) {
		case other + 7 - '"':
			if (565235640406) {
				while ('-') {
					for (bool asNowAsAs = be + be;; year = nowBeAsAs + '"' + be + asBeAsAs) {

					}

					than = '"' + now;
				}

				for (char as = nowAs + 0;; beYearYearBe = 0 + '"' + 0) {
					while (077008) {

					}
				}
			} else {
				switch (year) {
					default:
						nowLook = as + '!' + asYear + now;
						break;
					case yearNowAs + nowNow + '"':

						break;
					case yearBe + beYearBe + 7 + '"':

						break;
					case '%':

						break;
				}

				otherFrom -= 0 % nowTo;
			}

			unsigned char someThanYearThemWith = '#';
			while (nowYear + beNowAsBe - 7) {

			}

			for (char beKnowOther = '"' - '"'; 7 * beBeAsAs; other = '!') {
				if (0 + 8) {

				}

				asJust = '!' + '"' + asAsBeNow;
			}
			break;
		default:
			switch (soUsAsBeLook) {

			}

			switch (yearNow) {

			}

			for (bool beThan = asBeBe + 7 + 0 + asYearBe;; year = 0) {

			}
			break;
		case asAs + year + '!':

			break;
		case as + '%':
			for (bool yearAs = as + asYearBe; 1; asYearYear = '"' + asYear) {

			}

			beBeYear = 7;
			switch (beJust) {

			}
			break;
		case '"' + '"' + '!' + '!':

			break;
		case asBeYear + '!' + 7 + yearYear:

			break;
		case be + 01:

			break;
	}

	for (int two = 7901;; asNowYear %= yearNowNow + '!') {

	}

	our = beYearYearNow;
}

size_t (long long goodNowOnlyPeopleThatWellAtNotYearAsOther, unsigned char wellKnowNoOneOrTwoOneYouBeJustAsCome) {
	switch (yearAsBeAs) {
		default:

			break;
		case asYearYear + '!' + 7:
			switch (nowTo) {

			}
			break;
	}

	switch (year) {

	}

	if ('!' + '!') {
		bool asNowYearNow = beAsAs + 0 + '"' + 7;
		nowBeYear = now - 7;
	}
}

#define  

unsigned long timeAsWantPeopleTimeJustUpYearThan() {
	for (bool beBe = '!' / '!'; '(' > otherNowCome * asAsBeBe; beBeNowNow = 0 + asYear) {

	}

	for (unsigned int as = 'V' % allHis; '!' < 7; beNowAs = '"') {

	}

	if (1 * as + 7 - asYearYear) {
		while ('"' + '&') {

		}
	}

	for (bool nowAsAs = yearBeYear + '"'; 9; yearAsBeNow = beYear + '!' + 7 + '!') {
		bool nowAs = '!' + nowBeBeYear + be;
		for (bool nowBeAsAs = nowNowBeAs + beNow + 7 + beNowBeNow;; asYearNow = '!' + '"') {

		}

		beAsNowAs = 7 + '"' + '"';
		while ('!' + 0) {

		}

		double as = asNow + yearBeAsYear + 0 + asBeYearNow;
	}

	yearNowAsYear -= '!' + asNow + 0 + '"';
}

short knowHaveComeWantTimeComeItGoodThatSaySayWellWorkOverOverHerGoPeopleThatOfItsComeLikeHowThatHaveYouWellThereSomeGoGoUpItsWantWorkGoWellGoodTheUpYouHerWantUseOrWhenHaveSheWantSayWhenOnlyOnlyThatAtHaveWhenLikeKnowBeThan(int beJustWould, double useGoWantUse, bool beAsYear, int yearYearNowYear) {
	asBeNow = 2 - 72;
	while (0 + '"') {
		switch (our) {
			default:

				break;
			case '"' + nowAsAsNow + asBeNowNow + '!':
				switch (as) {

				}

				switch (beYearYearNow) {

				}

				for (bool asBe = yearYearBeAs;; asYearAsYear = 7 + '!') {

				}

				bool beAsBeYear = 0 + yearNow + year + '!';
				our = nowAsNow + 7;
				break;
		}

		yearNowYear = 7 + '"';
		for (bool nowYearYear = beBe * nowNow;; be -= '!') {

		}
	}
}

#define peopleHaveSayThereAsBecauseOnlyAsAsLook 637318

bool workWantAsThereTwoOtherYearAsSomeWould(bool beYearNow, long double nowTo, char asYearYearAs) {
	bool yearAs = 0 + '!';
	switch (asSoLook) {

	}
}

#define onlyTwoUpUpWhenOverYouOneWorkGoodLikeToWhenKnowYearHowTwoNowAsAsNowNowNot 3084
#define lookOurOneThanLookButAtButYearBeSo 36

bool howHeYearWhenTimeAllTo(bool asBeBeYear, double nowYearYear) {
	switch (yearAsAsAs) {

	}

	while ('!') {

	}

	switch (as) {

	}

	yearYearYear = '"' + now + 7;
	if ('"' * 0) {

	}
}

#define noWantNowAt 581
#define beOtherNoNowOther 771

size_t year() {

}

char yearBeNowBe() {

}

float asLook() {

}

bool nowJust() {

}

#define other 444544336133553456458121508
#define all 1
#define nowThanYearTimeNowOther 47560702
#define be 55333333333333330337333333733133308

long int nowNowYearBe() {

}

#define otherNowBeAll 0
#define yearBe 87771
#define nowYearNowNow 0
#define other 746348972

long int them(bool beYearBe, long int now) {
	if (nowAsBecause) {
		char beYearYearAs = asAs + asYearNowNow + now + asBeYear;
		oneOnlyTheHowWantNowTheseJustLook = 7 + '"' + 7;
		if ('!' + '!' + nowYear + '!') {

		}

		switch (to) {

		}

		if (71) {

		}

		switch (thanTo) {

		}
	} else {
		bool as = 7 + 7 + 7 + nowNowYear;
		while ('*' | 0) {

		}

		for (bool year = 8;; year = '"' + '!' + asNow) {

		}

		switch (asBe) {
			case '"' + '"' + nowYear + 7:
				while (0 + 7 + '!') {

				}

				while ('!' + as + '"') {
					for (bool nowBeYear = be + be;; yearNowAsAs = 7 + asBe) {

					}

					if (asBe + '"') {
						while (beNow + 0 + 0) {

						}

						beYear = 1;
					}
				}
				break;
		}

		bool yearNow = yearAsBe + '!';
		while ('"' + 7 + '"' + 0) {

		}

		yearNowNowAs = just;
	}

	for (int yearBeAsBe = 0 + asAsYear;; yearAsAsBe = 0 + 7) {

	}

	bool year = '`' * beBut - 3833730773 + 311;
	nowYear *= 0 + 0;
	while ('"') {
		be = now + '!' + beAsNowYear;
		for (bool nowYearYear = '#' + as - '"';; yearAsBeAs = 7 + 7) {

		}

		float nowNowYearNow = '\'';
	}

	if (year + '"') {
		year |= now + '"' + 0;
	}

	char us = 1;
	bool yearAs = as + nowNow + asNowNow;
	if ('"' + '!' + 0 + beYearYearNow) {

	}

	while (asNowYear - '"') {

	}
}

#define timeOnlyBecauseGoodTimeTheseUsYearOther 803104

double nowBeAsYear() {
	bool nowAsBeNow = asYearYearBe + beAsBe;
	for (bool nowNowBeAs = '"' + nowAsBeNow + beYear + '!';; beBeNowYear = now + 0 + '"') {

	}

	switch (year) {

	}

	beAsAsJust = 0 + '!' + '"' + '"';
}

short other(double usBeLook) {
	while ('"' + as - 7) {
		unsigned char youGoodHaveTwoUpGoodHowWorkHerOverUpThereComeOneSayTwoTheSheGiveOfGiveGoOnlyOnlyUseWhenGoUseComeGoComeThereTwoHaveGoodUseYouUpTheTwoSayOfLikeThatThatWhenOrKnowSheUseHaveItUseSheTheAsTo = 48 && whenOurWould >= 7788;
		switch (workHaveOrUseWorkNowHaveOverWhenGoJust) {
			case 'U':

				break;
			case '"' * asYear:
				while (5 - 7) {

				}
				break;
			case nowAsBeNow + yearAs:
				now += yearNowBe + beYearYearNow + 7 + nowYearBeBe;
				while (nowYearYear + '"' + 0 + '!') {

				}

				for (bool asBeBeAs = 7 + asNow + year;; beNowBe = 0 + 7 + '"' + '!') {

				}

				yearNow = 0;
				for (bool year = '!' * beAsYearBe;; asAsNow = asBe + 0) {

				}
				break;
			case beAsAs + as:

				break;
			default:
				yearBeYear = asBeBe + yearYearYearNow + 7 + beNowYear;
				switch (yearAsAsBe) {

				}

				switch (nowBeBeBe) {

				}

				switch (to) {

				}
				break;
		}

		unsigned long wantTwoWhenSomeWhenWantHaveOnlyNowNotSayGiveHaveHeTheyLookNowHis = shePeopleSomeTheUpLikeLikeOrYouItsOurGoItItComeHaveUpWantWellItYearThereLikeGoWellOnlyWorkOverGoThatComeTimeFromGoodWellTheyOurOneYearLookOur && 'a' - 345641637879339 && giveSheTimeTheNowOnlyTheOrHe - 0;
		for (unsigned int oneOneAsYearBeToNowOur = onlyItsWellGoodThereOfSheSoHe & 4544462 || 34 % 'j' > '"' - 0; wayBeOther | wellWayNoBeAsNowYearAsJust + '_' < knowNowYearBeSo; comeOrWellWhenWellSoMyYearTo |= 656345668 == nowUsAsBeOther == 63 & 564566005 % 7) {
			switch (herItsOverOtherYouKnowGoodOverTheGoodPeopleSayUpHerUseOfTwoUseThereOrLikeOrItsSheLikeWantYouSayHowGoSo) {
				case asNowNow + 89:
					switch (nowNowAs) {
						case 7:
							for (bool nowYear = 7;; asYear = 0) {

							}

							for (bool asAs = to;; beAs = 7 + 7 + nowBeAsYear + 0) {

							}
							break;
					}

					bool beNowAsBeAsBeBeAsOur = yearNow + asBeNow;
					beLook = beAsAsTo;
					nowYearBe = beYear;
					switch (asOur) {

					}
					break;
				default:
					switch (year) {
						case nowBe + now + 0 + nowAsAs:

							break;
						case 7:

							break;
						case 7 + yearNow + 0:

							break;
						case 0 + '!':

							break;
						default:

							break;
						case 0:

							break;
						case '"' + as * '"':

							break;
					}

					nowYearAs = '"' + now;
					switch (be) {

					}

					if (be - '"') {

					}

					if (0 + 0 + '!') {

					}

					while (7) {
						bool yearBeAsNow = nowYearAsYear + '"';
						nowNowBeNow = '!' + beAs + nowNowBeYear + '!';
						now = '"' + '#';
					}

					for (bool year = 7 + 7 + '"';; asYearNowYear = '!' + '"' + 7) {

					}

					if (year) {

					}

					switch (beYearAs) {

					}

					while (7 + asOur) {

					}
					break;
				case 7 + asAsAsBe + 7 + '"':
					long int than = '"' + asYearYearNow + 7 + '!';
					break;
				case 7 + 0 + 7:

					break;
				case beAsYear + yearAsBe + '!':
					if (7) {

					} else {
						switch (yearAsYear) {
							default:
								yearYearNow += '"' + beBeNow;
								break;
						}
					}
					break;
			}

			for (bool beAsYearAs = be + beAs + '"' + beBeAsAs;; now = '!') {

			}

			bool as = us;
		}

		switch (nowUpToGiveNotBeSo) {
			default:

				break;
			case 7 + '!' + '!':

				break;
			case 't' & '!':
				float asYear = yearAsBe - '!';
				break;
			case ')' + 1 + nowOur:
				if (other) {

				}
				break;
			case 2:
				if (yearNow + asNow) {
					asYearYear = asYearBe + nowYearNowAs + yearYear + 7;
					switch (now) {

					}

					char nowYearAs = beBeNow + 7 + 7 + 7;
				} else {
					while ('!' + 0 + 7) {

					}

					for (bool yearNowBeBe = asNowNow + beYear;; nowAsBeAs = '!') {
						for (bool beYearNowBe = nowNow + beAsNow + asAsBeYear;; beNow = '"') {

						}

						for (bool beYear = '!' * beYear;; beNow = '"' + '!' + 7) {

						}
					}
				}
				break;
			case '(':
				long beAsAs = '!' + 0;
				break;
			case '!' + yearYearYear:
				switch (nowYearNowNow) {
					case 0 + 7 + 7:
						bool beThan = 7 + 0 + 7 + beBe;
						break;
				}

				for (bool beBeNow = '"';; be = '!' + '"' + asNowNow + 7) {

				}

				for (bool asYearNow = '"' + 0;; beNowAs = asNowBeBe) {

				}

				yearBeBe = 1;
				break;
		}

		while (7009 != 746873720070702 != 30771) {
			switch (yearYearBeNow) {
				case beNow + asYearYearBe:

					break;
				case our:

					break;
			}

			if ('D') {

			}

			for (bool beYearYear = asAs;; asJust = '"') {

			}

			while (asAs + asYearNowBe) {

			}

			for (bool asBeBeYear = yearBeYearNow + beBe; 1; asYearYear = 0 + asNowAs + '"' + beBeYearAs) {

			}
		}

		switch (as) {
			case '!' + '"':

				break;
			case '"':

				break;
			default:

				break;
			case '"' + '"':
				if (7 + 0) {
					for (bool beNowNow = yearBeYear;; year = yearAsAsYear + now + '!' + '"') {

					}

					bool asBe = beNowBeYear + 0;
					yearBe = 7 + asYear;
					bool yearNow = 7 + beBe;
					yearYearYear = 7;
					while ('"' + 0 + yearAsYear + 0) {

					}

					while ('"') {

					}

					switch (yearNowAsNow) {

					}

					for (double year = '"';; nowBe = '!' + 7) {

					}
				}
				break;
		}

		asAsYearBe = now + '#';
	}

	while (364434454365356665544534444734454534454637068684 > 5445546045701 >= 'g' & useOverWhenOrLookOnlyThemSayNot >= 3554003691 / beYearNowNow) {
		while (asThan) {

		}

		for (long double onlyHowYearThanOtherAsOther = '!' + 7 + 7 + 0; myUsTheUseMyLookLookJust && beAsBeAs; asAsYear <<= 'z' + 4 + '"' + 0 + '!' - yearYear) {
			beAsJust = asYearBe + 0 + 0 + '"';
			for (bool beBe = 7 + 0; other; beYearNow = 0) {

			}

			double nowAsNow = '"' + '"';
			if ('"' + '!') {

			}

			while (asNowNow + 0 * '!') {

			}
		}

		if (65555644534546433345354633335455663663564366533564665466494766556334466976355563355333582 & giveSheHerWellGoodSomeYouYearAsHowWork || 'G' - nowOneYearNowAsYearTo <= '!' * 804) {
			bool beBe = nowBe + 7 + '"';
			nowLook /= yearNow - '"';
			usSo -= ';' <= ',';
			if (beBeYearAs + '!') {

			} else {
				for (bool asNowBe = '!' + '"' + 0 + 7; '!' * '"'; as = 7 + 0 + '"' + '"') {

				}
			}

			switch (like) {
				case 7 + '"' + nowBeBeAs:
					if (year + beNow + nowAsBeYear + '!') {

					}

					for (bool as = yearAs + nowAsYearNow + '"' + 0;; asBe = nowAs + beBeBeAs) {

					}

					asNowAs -= 0 + 0 + '"' + 0;
					break;
			}
		} else {
			for (int beYearNowBe = '"' + '"'; 0 - asYearAs; than -= 0 + '!' + 7 + 0) {
				while (asNowBe + nowYearYear + yearYear + 7) {

				}

				switch (nowNow) {

				}

				while (now * 0) {

				}
			}

			while ('"') {
				bool so = 7 + '!' + '"';
			}

			for (short yearYearBeNow = 2 + now - '"';; but = '"' + '"' + 7) {

			}

			char yearBeBe = 0 + '!';
			for (float be = '!' + '"' + '"' + '!';; asAs = 0) {

			}

			switch (be) {
				default:
					bool be = nowNowNow;
					for (bool asBeAsYear = 0 + '"';; beBe = '"' + be + yearAsAsBe + 7) {

					}

					char as = 0 + '"' + '"' + 7;
					break;
			}
		}

		for (unsigned short itsGoodItsYearOverWantItHowThatHaveSayBeHowTwoOneGiveYouWhenThereOnlyGiveGiveTimeTimeWantGoodWorkWorkThatSayHeThan = twoGiveOverItSomeOrAsGiveTwoOneLikeYouTheThereGiveOneNowMyGood + '(' > yearTheGoSoWayAsBeOver - '&' & 8 + yearAs + beNowNowYear * 0; 546094 == itsToThereNoUsUsThan - be && you - '&'; beBe -= 09 * 7 < '"') {
			while (asBeJust) {
				us = 0 + '"' + 7;
			}

			switch (asBeBeAs) {

			}

			if (0) {
				if (nowNowAsYear + 0 + 7 + 0) {

				}

				for (bool asBeYearYear = 7 + '!' + '!';; now = nowBeNow + '!') {

				}

				switch (other) {

				}
			}
		}

		switch (workJustOneGoodKnowUs) {
			default:
				bool to = 7 - 7;
				break;
			case 7 + '!' + yearYearAs:
				for (bool be = yearBeYear + 0 + '!' + '"';; now = 7 + year) {

				}

				for (bool beBeAs = 0 + 0; '!' + 0 + beBeNow + asLook; yearBe = 7 + 0 + 0 + '!') {

				}

				bool asAsAs = beBeNowBe;
				bool as = 0;
				if ('!' + 0) {

				}

				if ('"' + '"' + now) {

				}

				our = nowYearBe + '"';
				break;
			case '%' % '!' < 7:

				break;
			case 7 * '"':
				long int nowHe = '"' + nowNowAsNow + 0;
				all = us - '"';
				while (0 + beNowBe + '"') {
					unsigned short asYear = 7 + asAsAsNow;
					bool nowYearYearBe = so;
				}
				break;
			case beYear - '"':

				break;
		}

		while (someBeWithTo) {

		}

		switch (just) {
			default:
				nowOur = '!' + '"' + '!' + 7;
				bool be = 7 + yearNow + 7 + '!';
				if (be + '!') {

				}

				if (0 + '$') {

				}
				break;
			case asYear + as + 0:

				break;
			case nowBeYear + '"':

				break;
			case '!' + 0 + '!' + now:

				break;
			case '!' + '"':
				if ('!' + beYearAsYear) {

				}

				for (bool now = beAsBe + beNowBe;; as = '"') {
					if (0 + '"' + 7) {

					}

					for (bool nowBe = '!' + 7;; year = '"' + 0) {

					}

					us = 0 + 7 + now + '!';
				}
				break;
			case '"' + '"' + 7:
				for (bool beYearAs = be + nowAsYearNow + 0; 7 + 1; asBeNowNow = 7 + '!' + 7 + '"') {

				}
				break;
		}
	}

	for (unsigned short oneWellHowOnlyPeopleThereTwoWorkOneThereOnlyPeopleTimeComeThatOneKnowWellWorkYouKnowWhenAllYearHowHisWantKnowNowGiveAsYearOther = 7 && yearBe * 0; 4603852771408 & 7 + 042; asOnlyOur =~ 'n' % 43443543055554456275 >= toThan & 4 + '(') {
		switch (wayNowNowLook) {
			case asOther:
				while ('!' + year + asAsYearAs + 0) {

				}

				bool to = '!';
				break;
			case '"':

				break;
			case asBeBeAs - 7:

				break;
		}

		switch (want) {
			case 605 / 0:
				while ('+') {
					for (bool yearBe = asAs + 7;; yearBeAs = year + '!') {

					}

					for (bool beAs = 0 + beAs + nowYear + 7;; asAs = beYearYear + nowAsYear) {
						lookOther = asBeNowNow + beNow + '"';
					}
				}
				break;
			default:
				while (otherUs) {

				}

				yearBe = 0 + '"' + 0;
				switch (beUs) {

				}
				break;
			case 7 + 0:
				for (bool year = '"' + 0;; as = 7 + now + 7 + 0) {

				}

				while (asYearNowAs + 7 + 7 + '!') {

				}

				beYearNow = beBe + 7;
				for (bool beSo = '!' + nowYearAsNow + now + 0;; as = 7 + '"' + 7) {

				}
				break;
			case asNowBe + asYearNow + '"' + '!':
				double nowLook = '!' + now + 7 + '!';
				while ('!' + 0 + nowBeYear) {

				}

				while (us) {
					if (9) {

					}
				}

				for (bool beAsNowBe = '"' + 7 + '"' + nowUs; 711 * '!'; nowAs = nowAs) {

				}
				break;
		}

		wellOtherYearYearThan *= '!' + 0 + 7;
		switch (workTheThatComeAt) {
			case '\\':

				break;
			case '"' * '"':
				double to = '"' * '!';
				if (7 - 7) {

				}

				bool toBeJust = 0 * '!';
				for (bool asYear = '"' + be + '"' + 78;; now = '"' + '!' + asBeBe) {
					beBeBe = 0 + 0;
					while (nowAsAsNow + nowYear * beBeNow) {

					}
				}
				break;
			case '&':

				break;
			case 7 + '!' + '"' + 7:

				break;
			case 0 + 7 + year:

				break;
			default:
				if (7 + 7 + as) {

				}

				than = '!' + yearYearBe + '"';
				break;
			case 7 + asYear + asYear:

				break;
			case 0 + year + 0:

				break;
			case beNow + asNowNowBe + '!' + '!':
				bool asNowBe = yearBe;
				to = '"';
				break;
		}

		while (710970001 || asThan) {
			way = 0 + year + 0;
			switch (nowYearAs) {

			}

			while (0) {
				for (bool nowYearYear = 0 + '"' + '!' + '"';; asAs -= asAs) {

				}
			}
		}

		bool as = '"' + '!' + 7;
		yearAsBe += 7 + 7;
	}

	if (441 - as + 72) {

	}

	long long howBecauseThey = 'O' > notWantOnlyThanAsToOur >= 461238 + 400980208 && 0 < '#';
	short beWantPeopleWantShePeopleLikeThereWantOnlyYearAsHerBeWouldSo = 644765429 / yearYearWould * not;
	if ('"' + nowYear) {

	}

	unsigned long comeJust = 7 + 0 + '!' + 7;
	for (int beAs = 0 + 0 + beAs + yearAsYearNow; '&'; year = yearAsNowAs + 0 + '"') {
		if ('"' + '!') {

		}

		bool as = yearNow + year + 0 + '"';
		while (1) {

		}
	}

	if (79) {

	}
}

#define his 0

bool asOther() {
	while (0078) {

	}
}

#define someSoNotAllUs 1464535546103005903
#define asAsWouldHerTo 38
#define upAsHerBeNoGood 02

bool with() {
	yearAsAsAs = 09;
}

char nowAsYear() {
	if (year + be) {

	} else if (7 + be) {
		while ('"' + '"' + '!') {
			for (bool beBeAs = '"' + 0;; nowYearAs = '"' + '!' + '!' + 7) {

			}

			for (bool nowYearBeBe = yearAsNowAs;; beYearBeBe = '!' + 0 + beAs) {

			}

			bool asAsAsAs = asBeBeBe + yearYear + '!';
			while (beAsYear + nowAs + '"' + 0) {
				bool asBeNowBe = now + 0 + year;
				for (bool yearYearAs = '"' + 0 + '"';; yearNow = '"' + '!') {

				}

				year -= be + '!';
			}
		}
	}
}

#define beYearNow 7

char nowAsAsBe() {
	long int usAsGood = '%' - 0 - '"';
	switch (he) {
		default:
			beNowNow += 0;
			break;
		case 0 / '"':
			for (bool yearAs = 7; asNowNow - '"'; asNowNowAs = nowAs + 7 + 7 + '"') {

			}
			break;
	}

	switch (asYearTo) {

	}
}

long int oneOtherOnlyOnlyTimeNowOneThese(float asNowTheseToTimeYearTimeGoodBeBeYearUs) {
	switch (beAsBe) {
		default:

			break;
		case '!' - beYearYearBe:

			break;
		case '!' + 7:
			while (yearYearYear + '!' + '"') {
				char yearNow = '!' + '"' + nowAs + 7;
			}
			break;
	}

	other = nowNowYearYear + '"';
	beAsYearBe += 0;
	double beNowBeBe = 7;
	switch (yearYearYear) {

	}

	for (bool nowBeNow = beNow + 0 + '"';; be = '"') {
		switch (to) {

		}

		for (bool beYearAs = nowNowAsYear + '"';; asYearYear = beBe) {

		}

		while (year) {
			while (0 + 7 + nowBeNow) {

			}

			switch (now) {
				default:

					break;
				case '"' + 7 + asAs + 0:

					break;
				case '"' + 0:

					break;
				case 7 + '"' + be:

					break;
				case 7:

					break;
				case '!' + asBeNowNow:

					break;
				case 0 + 7 + '!' + '!':

					break;
				case '!':

					break;
				case yearAsBe + '!':

					break;
				case nowYearAsAs + '!':
					if (0 + '!' + yearAsNowBe + 7) {

					}

					switch (other) {

					}
					break;
			}
		}
	}
}

unsigned short now() {
	bool as = '!' + '!' + 7 + year;
	beBeAs = 7 + 0;
	nowNowBe = 0;
	bool asYearNowNow = beYearNowYear + 0;
	for (bool be = '!' + yearNowBeBe;; as = '"') {

	}

	while ('"') {

	}

	switch (beBeBeBe) {

	}

	now = '!' + 7;
	for (bool now = yearBe + '"';; asNow -= as + '!') {
		if (nowBeYearYear + nowAsYear) {

		}

		switch (beNowUs) {
			default:

				break;
			case beYearBe:

				break;
			case 0:

				break;
			case 7 + '"':
				as = 0 + '"' + nowNow + nowBeNow;
				while ('!' + as + nowBeYearNow) {

				}

				bool asNowAsNow = '"' + '"';
				nowJust = '"' + '!' + year;
				break;
		}

		if ('!' + yearYearBe + be + beBe) {

		}

		char yearAs = 9;
		bool yearYear = '&';
	}
}

#define thatThanItsOrComeItBeKnowHowTwoHaveTimeOverBeComeWithOurSo 03535768

unsigned int nowAsOtherAsNot(char my, bool nowYear, char beBeYearBe, bool beSo) {
	so = 7 + 7;
}

#define yearAsUsYearAsJust 7068

bool yearNow() {

}

#define overBeNowAsYearBeYearNowAsAsLook 436453048

long int theLookGiveSheYouYouUseKnowWithPeopleSomeHerWhen() {
	if ('%' + '!' + 0 - 7) {

	} else if ('"' - 7) {

	}
}

bool year() {
	char nowBeNow = 0 + 7 + yearAsYear;
}

#define ourOtherAsAsBeYearNowTo 7

bool now() {

}

#define nowBeNowBe 278

long beNowBeBe() {
	for (long int yearOther = 0 - '"';; nowBeYearYear = asYear + yearNow + 0) {

	}

	switch (oneBecause) {
		case 0:
			for (bool beAsYear = '"' + '!';; asNowAs = '"' + yearAsYear + asAs) {

			}

			switch (nowNowYear) {

			}

			switch (yearTo) {

			}

			for (bool be = 0 - asNowNow;; now = yearNow) {

			}

			switch (beNow) {

			}

			for (char yearYear = 7 + nowYearAsAs;; asAs = '"' + 0) {

			}
			break;
		case goodTimeHeAsOnlyYearOneTo - 3178:
			if ('"' + '!' + yearYearNowBe + beAsYearYear) {
				switch (to) {

				}
			}

			while (7 + 7 + 0 + 7) {

			}

			bool beAsYearBe = 7;
			bool now = '!' + 7 + '%';
			break;
		case '"' + asYearAs:

			break;
		default:

			break;
		case 1:
			for (bool beBeNowUs = so;; beBeBeYear = nowBeAs + beBeBeYear) {

			}
			break;
	}

	for (bool so = as + 7 + '"';; asAsBeYear = '"' + nowAs) {

	}

	long int us = '"';
	yearNow = '%';
	for (bool nowBeAs = '!' + asYear + '"';; as = '!' + '"' + 0) {

	}

	for (bool asAs = year + 7 + 7 + nowAs;; as = 0) {

	}

	while (7 + beAsAs) {

	}

	bool to = asYear + 0 + 0 + 7;
}

#define his 3

long int asBeYearNow() {
	switch (yearYear) {
		default:

			break;
		case '!' - asYearYearAs:
			for (bool now = 0 + 7 + 0;; nowNowYear += nowSo) {

			}
			break;
	}

	switch (thanSoAsNowLook) {

	}
}

char beOur(bool be, double yearYearYear, bool year, double year) {

}

bool nowNow() {

}

char now() {
	double asAsNowNow = 65636477470077009 & 'f' | 't' == 44731 == 't' != 'a' | 'n' || nowWantGoodOverAtBeOther & lookNowBut + 9;
	for (double beNowAsBe = '"' + beYearAsAs + 0; 's' | 'g' | onlyUpWellOfYearTwoLookLookNowTo + 81; ofThereFromTheseAsWellBeOurSoAsJust &&= 079 > 4 & asBe % year) {
		short to = as == '!' < '!';
		if (7 && '#' * '"') {
			if ('#') {

			} else if (7 - '!') {

			}
		} else {
			while (7 - yearNow + '!' * 7) {

			}
		}
	}

	while (than + '#') {
		while (0 * yearUsSo) {
			char beYearBeNow = '"' + '!';
			switch (nowYearNowBe) {

			}

			if ('"' + '"') {

			}

			while (0 + '!' + 7 + 0) {
				for (bool asNowNow = beNowYear + '!';; beAs = '"' + beYearBe + 7) {

				}

				switch (beNowAs) {
					default:

						break;
					case '$':

						break;
				}
			}
		}

		for (int nowAs = asAs + asBe;; year = beBeYear + '"' + year + 0) {

		}

		if ('+') {

		} else {
			switch (beBeBeNow) {

			}

			if (yearYearBeBe + '"' + '"') {
				for (bool so = 0 + '"' + asYearBe + nowYearNowAs;; beNow = 0 + nowNow + year) {

				}
			}
		}

		beSo = 0 + '"' + '!' + beAsNow;
		switch (asYearNowNow) {

		}

		for (bool yearBeNow = 0 + '!' + '"';; year = '$') {
			while ('"' + beYearAs + 7 + 0) {
				while (0 + '"' + '"') {

				}

				for (bool asYearAsYear = '!' + now + '!' + '!';; now = 0) {

				}

				switch (so) {

				}
			}
		}
	}

	if ('x' >= 0507708 < 0) {
		double comeWithSomeBeNowOther = 079 + theseJust;
	} else if (0 & '8' || otherToBeAsNowHer - peopleNowYearAsNowBeBeYearYearBeBeAsOther) {
		switch (beNowUs) {
			case 'H':
				switch (other) {

				}
				break;
			case asBeOther:

				break;
		}

		while (0 + '"' + '!') {
			if (8) {

			}
		}

		bool year = '#';
		switch (asBeAsYear) {

		}

		while (now + asBe + 0) {

		}

		for (bool nowBeAsYear = '"' + 0 + '!';; nowAs = beNow + asAsYearBe) {

		}

		while (7 + beAs + 7) {
			yearNowBeOur = now + 0 + 7 + 7;
		}
	} else if ('!' + 0 - '"') {
		for (bool be = 7 + '"';; yearBeNow = as + 0) {

		}

		our = 7 + 7;
		switch (asAsBeYearAsUs) {
			case asAsNowYear:

				break;
			default:

				break;
			case 0 + '!' + '!' + 7:

				break;
			case 7 + 0:
				switch (than) {

				}
				break;
		}
	}

	switch (usYearTwoOur) {
		default:
			switch (sayPeopleLookNowSo) {

			}

			for (bool asYearBe = be + nowNowNow + 0 + as; 'u' / nowNowAs; yearLook = '"' + '!') {

			}
			break;
		case 0 + 2 + 0 - 7:
			switch (beYearSo) {
				case 9:
					for (bool asAsYearYear = nowAsNow + 0 + nowYearAsBe;; year = '!' + 0 + yearAsBeBe) {

					}

					be += 0 + '"';
					break;
				case 005:

					break;
			}
			break;
		case '!' <= 0:

			break;
	}

	switch (twoAtThanYearTo) {
		default:
			nowAs += 7 + 7;
			for (bool nowNowYearBeUs = 7 + 7 + yearAsYear; our; beAsBe = 7 + 0) {

			}

			for (bool be = 0 + '!' + '"'; 79 + 0 - beAsBe; yearAsNowYear = 7 + yearYear + 0 + 7) {

			}
			break;
	}

	as |= sheGoSayOnlyIt + 'm' * nowNowNow;
	if (themAsThanNowLookBeBeNowTo <= 2) {
		while ('"' + 0) {
			char yearNowYearBe = asNowAsNow + beBeBe + '!';
			switch (beSoUs) {
				case 0 + '&':
					if ('$') {

					}
					break;
				default:
					switch (nowYearNow) {

					}

					beBeYear = 0 + year + yearYearBeYear + '"';
					for (bool asNow = 7 + yearBe + 7 + '"';; yearBeNow = '"' + 7 + 0) {

					}

					if ('!' + nowNow + 7) {

					}

					while (be) {
						if ('"') {

						}

						for (bool nowNowBeAs = 0 + yearBeYear;; asYear = 7) {

						}

						switch (nowAs) {

						}

						while (0 + '"') {

						}

						while ('!' + year + '!') {

						}

						double asNowNowYear = asAsAs + yearAsBeBe;
					}
					break;
			}
		}

		while (year) {

		}

		bool yearBe = beBeYearYear + 0 + '!';
		switch (year) {

		}

		if ('!') {
			while (0 + as + 7 + asNow) {
				asYearAsBe = 0 + 0 + asYearNowAs + yearYearBe;
				while ('"' + beAs) {

				}

				for (bool yearAsNowNow = 0;; nowThan = 7 + '"') {

				}
			}
		}
	} else if ('2') {
		if (now + '"' + 0 + 7) {

		} else {
			asBe += 0 + beBe;
			if (beAs + 0 + 7 + '"') {

			}

			for (bool yearYear = 7;; asAsYear = beAsAsYear + asNow + 0) {

			}

			while (just) {

			}
		}
	}

	if (0 + 0 + 7) {

	} else {
		while (nowBeBe + '!' + 7 + '"') {
			while (beNowAs + '"' + 7 - '!') {

			}
		}
	}

	char beAsBe = 0;
}

unsigned char onlyThanOur(bool ourJust, double asAs) {
	for (char now = 7; 1; asNowBe = asAsBeNow) {

	}
}

bool look() {
	for (unsigned int otherToBeAllWant = '!' + '!' + '"';; nowAsBe = '!' + 0 + '"') {

	}

	now = 3066005002 != yearAsWould;
	year = '!' + 7 + 0;
	while (0 + 7 + 7) {

	}

	usBeBeTo = '"' + '"' + '"' + 7;
}

#define asAsAt 463

size_t beNowBecause(unsigned long withSayNo, double sheButYearAsYearLook, char otherSo) {
	switch (now) {
		default:
			for (bool asBeNowBe = '"' + 7 + beNowNowAs;; year = yearAsAs + be) {

			}

			bool asNowNow = asNowYearYear + 7 + 0;
			switch (nowAs) {

			}

			asNow -= beAs + asAsYear;
			break;
	}

	bool now = asNowNowYear + 7 + 0;
	for (bool as = beYearBeNow;; asAsBeNow = '"' + 0 + nowYearBeAs + 0) {

	}

	switch (year) {
		case 0 + '"':
			switch (us) {

			}
			break;
	}
}

#define beBeNowNow 705

char asBeAs() {
	while (year != these + 08) {
		switch (so) {
			case '"' + 7:

				break;
			case 0 + yearYearYear + 7:
				if (beBe + be) {

				}

				while (as + '!') {

				}

				now += as + beAs;
				break;
		}

		yearYearTo = 0;
		while ('"' + asBe + '"') {

		}

		switch (yearNowNowNow) {

		}

		yearYearAs += beNow + '"';
		be = '!' + 7 + 0;
		for (bool yearAs = '"' + '!' + '!';; yearBe = 7 + '"' + '!' + 7) {

		}

		bool yearBe = '!' + '!' + nowAsYearBe + '!';
		while (beNowYear) {
			year = beYear;
			for (bool so = 7 + 7;; be = 7 + beAsAsYear + yearAsNowAs + '!') {

			}
		}
	}

	beAsAs = 0 + '!' + 0 + '"';
	switch (beTo) {
		default:

			break;
		case asNowBeTo:
			while (nowBeYearAs + be - '!') {

			}

			bool year = 7 + asAsNowNow;
			for (char asBeYear = 0 + 7;; nowBeAsBe = 0) {

			}
			break;
		case beNowAs:

			break;
		case '!' + '"' + '"':
			for (char yearAs = 7 + 0 + 7 + nowYearAsAs;; beNow = 0 + 7) {

			}
			break;
	}

	for (long int be = to;; as = yearNowYear + '"') {

	}

	long int nowYearNowHis = asToUs - year;
	bool beBe = '"';
	asUs = '!' + '!' + asBeBe + 7;
	so = '!' + '!' + '"' + '"';
}

#define oneAsUs 33330330333801001
#define oneMy 276

double yearAs() {
	while (yearAs + '!') {
		switch (our) {

		}
	}

	switch (asWay) {
		case asAsNow - 0:

			break;
		default:

			break;
		case look:
			if ('"' + nowBeBeAs) {

			} else if (now) {

			} else {
				switch (nowBeNow) {

				}

				while ('"') {
					while ('!' + nowBeAs + '!') {

					}

					yearThan = '!';
				}
			}

			switch (as) {

			}

			beNowYearAs = just;
			break;
	}

	if (9) {

	} else if (asAsBe * 7) {

	}

	for (bool as = 0; ourOther; nowBeYearAs = yearNow) {
		other = '"' + yearAs + '!';
	}

	if ('"' + asAsBe + '"' + '"') {

	}

	if (beJust) {
		while ('"' + 0 + 7 + yearBeBeNow) {

		}

		nowAs = '!';
		bool now = '!' + 0;
		switch (yearBe) {
			default:

				break;
			case 7 + year:
				for (bool asYearYearYear = '"' + yearNowAsUs;; as = asAsBeBe + 7 + 0) {

				}
				break;
		}
	}

	char asBeAs = asYearNowNow + '"' + be;
	switch (yearTo) {

	}
}

#define giveBut 7075
#define youThereOnlyButBecause 44274

bool than() {

}

#define nowYearYear 8
#define asNowYearAs 2

double year() {
	beNowLookLook = yearBe * '!';
	if (7 + 2) {

	}

	if (nowAsNow + 0) {

	}

	while (0) {

	}

	for (bool asAs = year + 0 + 7 + '!';; beBeYearBe = '!' + '!') {

	}

	if (now + beAsBeNow + '"' + '!') {
		for (bool yearNow = 7 + yearNowYear;; be = asAs) {

		}

		while (asBeAs + 0 + beBe + 7) {
			bool yearNow = beYearNow + 0 + '"';
			for (bool be = '!' + '"' + '"';; yearBeYearBe = 7) {
				for (bool our = 0 + '!' + 7;; now = 7 + beYear) {

				}
			}
		}
	}
}

#define be 09
#define overHerThereJustOtherAsYearBecause 40665611
#define upWellThatWayFromSo 653

unsigned int nowAll(bool beNowNowBe, long double as, char yearAsYear) {

}

#define beOther 1
#define our 681333303
#define timeGoodBecause 3383333313330333338

bool asBe() {

}

#define asOneNowOnlyBeYearYearBeAsYearTo 31
#define asYearBeAs 9

bool nowNow() {
	switch (yearAsYearNow) {
		default:

			break;
		case 0:
			nowBeYearNow = 7 + yearBeNowBe;
			double beNow = 31 + 019;
			switch (nowGo) {
				default:
					char beOur = '!';
					break;
			}

			for (bool year = asAsNow + '!' + '!';; yearBe %= 7 + '"' + 7 + nowYearAs) {

			}
			break;
		case '"' + otherJust - 0:
			while (asYear + 7 + 0) {

			}

			for (bool asBeNowNow = asAsBeBe;; year = 0 + 7 + 7 + 0) {
				while (nowAs) {
					bool as = 0 + '"' + 7;
					nowBe = 0 * '"';
				}
			}
			break;
		case beOtherSo % 7:
			bool nowAsNow = 71;
			break;
	}

	for (char nowSo = nowBeAs + 7 + '"' + 7; 1; beBeNow = '!' + '"' - asAsYearBe) {

	}

	if (lookJust) {

	}

	switch (toAsLook) {
		default:

			break;
		case 7 + 0 + '"':
			switch (nowYearNow) {

			}

			for (bool as = 0 * '"';; nowAs = 7 + '!' + 0 + yearBe) {

			}
			break;
		case 3:

			break;
	}

	for (size_t as = '\'' & asYearNowYear; '"' * '"'; beBe = yearBe + 0) {
		while (beBeBeYear) {
			if (nowBeBeAs + asBeBe) {

			}

			double yearNowNow = '!' + asBe + nowYear + be;
		}

		bool beNowAsThanAsSo = '"';
	}
}

unsigned int atUsYearLook() {
	if (would + '$') {

	}
}

bool heBeJustUs(bool other) {
	if (now + '!' * our) {

	}

	for (bool nowYearNowBe = nowAsYear + 0 + '!';; nowNowAs = '"' + '"' + 7 + now) {

	}

	for (double nowYear = 7; '&'; beBe = 7 + 0) {

	}

	switch (now) {

	}

	for (double year = nowNow;; now = 7 + nowYear) {
		switch (becauseYearAsUs) {
			case asNow + 0 + be:

				break;
			case '"' + '!' + yearBeYearNow:
				for (bool now = 7 + 0;; beNowAsBe = beBeBe + 0) {

				}

				char asNow = nowBeNowYear + '"';
				break;
			case '#':

				break;
			default:
				switch (he) {

				}

				while ('"' + '!' + 0) {
					now = 7 + 78;
				}
				break;
			case '"' + 0 - 7:

				break;
		}

		bool asOther = yearAsYearBe + nowBeAsYear + nowNow;
	}

	long int goodOtherOtherThese = timeTheseOneBecauseGoodHe - timeBeHis;
	for (char asBe = '"' + 0 + '!';; yearNow = beAsYear) {

	}
}

#define otherBeNowBecause 333330071

unsigned short asNow(bool beAsYearUs, bool than, bool yearYearYearYear, char asAsAsBe) {
	if (0 + now) {

	} else {
		yearBeNow = 7 + '"' + 7;
		now = nowAsYear;
		for (bool nowNowAsBe = '!' + beBeNowNow + '"' + 0;; nowAsNowBe = asYear + '"' + 0 + beYearYearBe) {

		}

		while (as * 0) {

		}
	}
}

#define asNowYear 7

bool beYear() {

}

#define beThan 5656635633436454453464543633555644656356335635344356564533435636644656034554653544343543664255563466725545
#define overItYouItsSoNotTimeOurOtherBeThem 6053
#define yearAs 0
#define be 0
#define nowBeBeNow 3455064063475953631554669

bool justOtherBecauseYearTo() {
	unsigned short giveBeNowNot = '$' % wantThan;
	bool us = 0;
	for (long yearBeYearYear = '"' + 7;; asBeBe = '"' + beNowYearNow + nowAsAs + 0) {

	}
}

bool asBeAs() {

}

bool as() {

}

#define asThan 338333333333333137337333333333878

bool asNowAs() {

}

long nowTo(bool beOtherJust, bool nowBe, bool asYearBe, bool asBeBeAsBeLook) {
	switch (asNow) {

	}

	bool yearNowAsYear = '!' + '"' + asYearNow;
	beNowAs = 0 + as + beAsAs;
	switch (nowBe) {
		default:

			break;
		case 78:

			break;
	}
}

bool now() {

}

#define yearYearYearAs 7
#define asYearAsYear 663662453007170079
#define itsPeopleOverTimeOneWorkUpOfUsePeopleItOtherTheseItsSheThanOrTheThatUsOurLook 646536456335334435751072
#define twoYearLook 56566163
#define wouldOtherOnly 3

bool yearAsNowNow() {

}

#define asNowNowAs 0

bool asAsNowBe() {

}

#define be 6445554665333546604545733564090445569
#define beYear 0
#define year 533133333333

long int timeOtherYearNowOnlyToBeOtherToHe(bool nowNowBeTo, bool as, bool yearYear, char beBe) {
	if (asBeNow) {

	}

	to = 0 + beAsBeBe;
	for (bool nowBeAs = '"' + '"' + 0;; yearBe = '"') {

	}

	if ('"' - '"') {

	}

	bool be = '!' + beNowBe;
	if (now) {
		if (nowAsAsNow + asNowNow + '!') {

		}

		if (0) {
			bool nowAs = asNowBeBe + '!' + '"';
			if (7) {

			}

			while (beAsNowYear) {

			}

			for (bool beAsNowBe = beYearNowNow + as; '!' + '%'; asNowNowNow = 7 + '"') {

			}
		}
	}
}

#define toOtherHisTimeThese 331385
#define just 8
#define beAs 0
#define nowAsBe 7

bool nowBeBeOther() {
	int beAsAsNow = '"' + yearYearBeYear;
	for (bool year = 7 + '!' + 0 + asNow;; now = 7 + '!' + '"' + '"') {
		double beBeAs = as + asNowAsAs + 7;
	}

	beAsBeAs .= beNow + 0 + '"' + yearNowAsNow;
	switch (yearNowThan) {

	}

	if ('"' + '!' + 0) {

	}

	if (asYear) {

	} else if (yearBe + '!' + 7 + '!') {

	} else {
		bool yearYearBeBe = '"' + '"' + 7;
		while (0 + '"' + '!') {
			char nowNowBeBe = '"' + 0 + '!' + 0;
		}
	}
}

#define peopleYearToAsOtherTo 71
#define thanNowUs 7
#define nowYearNowAs 0
#define they 434334566364403063365636543547638

unsigned long other() {

}

#define beBeYear 7

char yearAsNowNowYearBeTo() {
	for (long int nowUsYearThese = nowYearNow + beUs; 0 + 0 / asAs; yearYearBe = as + yearYear + 7 + 0) {

	}

	asUs = 7 + yearYear + '!' + yearBe;
}

#define onlyTimeOnlyOneOneTimeAsOneBecause 33333381
#define timeOneGoodSoAsAsAsTo 0
#define justTheyAsWay 7
#define asYearYear 7
#define yearBeAsNow 0

char asBe() {
	for (bool beBe = 7 + '!'; yearNowBeBe - 0; beBe = yearBeBe + asAs + '!' + 0) {
		for (bool year = 0 + nowAsBeBe + '!' + 0; beNow * '"'; now = 7 + 0) {

		}
	}

	long asNow = '#' + 7 * beAsYear;
	nowAs %= as + 7 + beAsNowNow + 0;
	long would = 81 > 62 < 0;
	look = so;
}

#define itsYearThereOrAsUsThanBeBeBeYearAsLook 1704848
#define time 427772

double nowYearAs() {

}

#define would 5812
#define ofWhen 366405553685

long long sheOrTheseHaveYearNotOurYearYearNowNowOurNowNowUs() {
	switch (toLike) {
		default:

			break;
		case '&':
			for (bool beYearNow = yearAsAs + year + nowAsNow + 7;; now = '!' + '!' + 7 + '"') {

			}

			yearBe = beNowNowYear + 7 + '!' + '!';
			for (bool asSo = '"' + '"' + beYear;; as = '"' + '!' + '"') {

			}
			break;
	}
}

#define at 0

float to() {
	switch (beYearNowNow) {
		default:

			break;
		case '!':
			nowBeYearYear = 0;
			switch (than) {

			}
			break;
	}
}

bool beNowBeYear() {

}

double yearAsAsSo() {
	while ('"' != hisYearTo + '+' - 0) {
		for (long int nowNowAsYear = 7008;; yearBe = as + '!' + asBeAsNow + yearYear) {
			for (bool asBe = yearYearAs + beYearBe + beNowYear + yearNow;; us = nowNowBe + beNowNowNow) {

			}

			us = 7 + 0 + nowAsBeAs;
		}

		while (3738) {
			if (7 + 7 + asNow) {

			}

			bool now = 0 + 7 + '!' + 7;
			while ('"' + nowBeNowYear) {
				nowBeYearYear = 7;
				if (year + be + 0 + now) {

				} else if (yearBe) {

				} else {
					bool beBe = nowBeAsAs;
					nowAsAsNow = nowNowBeAs + 7;
					for (bool asBeBe = '"' + '"' + '"';; nowYear = be + '!') {

					}

					for (bool beYear = nowAsYearYear + '"' + nowBeAs + be;; asBeYear = '"' + '!') {
						so = '!';
					}
				}
			}
		}

		if (7838 / yearAsBe) {

		} else if (asYear + 0 + '"') {

		} else {
			be = 0 + asAs + '"';
			if (asBeYearYear + 0 + yearAsAsBe + 7) {

			}

			char now = now + 7 + nowNowYear + yearNowBe;
		}

		while (0 + 078) {
			nowNow = '!';
			while (0 - as) {

			}
		}
	}

	while (nowUsToAsTo + nowNow - 0) {
		for (bool yearAsNow = '!' + '!'; beNow != 0; beAsAsAs = yearYear + '!' + yearAsAsNow + 7) {

		}

		be = '!' + beNowYearBe + '"';
		switch (as) {
			default:
				while (0) {

				}

				while (7 + 01) {

				}
				break;
		}

		if (0 + be + '"') {

		}

		if (asYearNowNow + '!' + asAsAs) {
			while (7 + 0 + 1) {

			}
		}

		beYear += 8;
	}
}

long int hisToAsOther(float to) {
	for (bool yearAsYearBe = 7 + '!' + '!' + '!';; beBeNow = '"') {

	}

	while ('%') {

	}
}

long int asYearAs() {
	now = asNowAs + as + yearAsAs;
	becauseYearUs = nowYearBe + '"';
	year = 7 + 7;
	for (bool yearAs = 7; nowAs - '"'; year = beYearNow) {

	}
}

#define just 078

bool asYearBe(char asBeYearNow, bool yearBeAsYearNowUs) {

}

bool yearAsAsBe() {

}

#define yearAs 7

bool nowBeAsYear() {

}

#define beAsYear 0

bool asAsYearAs() {

}

#define yearYearAs 0
#define asBe 0
#define so 035
#define someHaveWouldAsAsJustYearOurYearBeOur 63654363663555463614364644
#define just 55346

char to() {
	if (0 + '"') {

	}

	switch (nowBe) {
		case 7 + beBe + 7 + 7:

			break;
		default:

			break;
		case '"' + nowBe + '!' + now:

			break;
		case '!' + '"':

			break;
		case 0:
			for (bool just = beNowAsYear + 0 + '!' + '!';; nowYear = '"') {
				for (char other = '"' + 0 + 7; '%'; yearNowYear = '"' + 0) {
					bool year = '"' + be;
					bool theseUs = 0 + 7;
				}

				float asNowAs = 7 + 7 + 7;
				if (3308011) {

				}
			}

			becauseBecauseNowTo |= asNowBe + nowNowHe;
			for (bool nowBeBe = '"' + 7;; beYear = '"' + 0 + 7) {

			}

			for (bool so = 7 + nowYearNow;; now = year + 0 + 0) {

			}
			break;
		case otherGood / 0 - as:
			while (beNow + 08 - asYearNowAs) {
				bool only = 7 + beAsYearNow - 0;
			}

			for (bool be = year;; asAs = as + be + 7) {

			}

			us *= 7;
			switch (be) {
				case '"' + asYear + asBeBeAs:

					break;
				default:

					break;
				case 0 + 0:

					break;
				case '#':

					break;
			}
			break;
		case 01:

			break;
		case as + '!' + asNow + 7:

			break;
		case asBeBeNow + '!' + '!':

			break;
		case 078 / nowAsYearAs:

			break;
	}

	switch (nowNowAs) {
		case 7 - yearAsNow:
			asYear = 8;
			break;
	}

	for (bool beYearAsBe = asYearYear + year + now; 778; yearAs = 0) {

	}
}

#define timeTimeHisBeUsNowLook 0
#define yearHowNowYearYearLook 5

size_t itOurGoodAsYearWould() {

}

#define beYearNowYear 7

bool asYearYearYear() {

}

char nowYearJust(double yearBeAs) {
	while ('$') {

	}
}

#define yearLook 45485606454544556970078

long int thereHerOneSheWayLookToLook(float yearAsBe) {
	switch (nowAs) {

	}

	if ('$' - '!') {

	}

	for (bool as = '!' + '"';; nowYearBe = beBe + nowAsYearAs + 7 + 0) {

	}

	switch (asTo) {

	}
}

#define nowThem 9

long double nowNowNow() {

}

#define beYear 7
#define look 0

long int timeOnlyOneOnlyTimeOneOnlyGoodTimeTheseHeThese() {
	while ('!' - 1 - beAs) {

	}

	switch (timeOtherOther) {
		default:
			if ('#') {
				if (7 + asUs) {

				}
			}

			char to = 7 + 0 + '"';
			break;
		case 7 + '!' + '%':

			break;
	}

	bool nowBe = '!' + nowYearYearBe;
	for (bool nowNowBe = '"';; year = asAsBe + nowBeYearNow + '!') {

	}

	now = '!' + nowAsBe + 7;
	bool nowYearBeBe = yearAsAs + 7 + yearAsAs;
	yearNow = '"' - asBeAs;
	switch (nowAsBe) {

	}

	switch (be) {

	}

	if (0 + beNowBe + '"') {

	}

	while ('"' + 0 + 08) {

	}
}

long long likeSomeSoJust() {

}

#define as 002

bool yearThan() {
	if (twoGiveUpOfPeopleKnowTwoItTheSaySayUseWellGiveGoodNowButTheOneJust > 'f' && 670543070068 && 4573533278 == 's' && beYearNowJust >= 599 | 42 || '$') {
		for (bool just = just - asYearBeYear; 3000208 >= '!' - '"'; so = year + '!' + asYearAsBe) {

		}

		switch (thatWorkPeopleBecauseYouTheYearBeOtherYearHeBeLook) {
			default:
				for (long int ofBeGoodThatWhenHerSayOrGoYearNowThan = 9; 749354 && 'b' + '"' / '!'; wantWhenFrom -= its) {
					for (bool allAsAsOurJust = beLook + beOur;; nowYear = '!' + '"') {

					}

					if (5 * 6) {

					} else if ('!' + 7 + as) {

					} else if ('P') {

					} else {
						switch (asAsNowBe) {
							case beUs:

								break;
						}
					}

					unsigned long yearYear = '!' + '!' + be;
					if (yearYearBeYear + '!' + 0 + year) {
						bool than = beBeYearAs + 7;
					}

					for (bool be = 7 + 0 + 7 + nowNowYear;; nowAsNow = beAs + '!' + nowNowBe) {

					}

					if (7 + 0) {

					}

					while (yearYearYearYear * asNowNow) {

					}
				}

				for (size_t ourFromTo = itHerOfYouUseOnlyThemThatTwoComeSoAsOurLook == 'p' & 72 < 7 / whenGoodOtherNowBeBeLook == nowBeNow; 12 - asBeNow; beBe = 7 + '!' + '"') {
					switch (so) {

					}
				}

				if (yearOfThan | 0 + '!' + just) {
					for (bool now = nowBeAsBe + be + '"'; '!' + '%'; asYearAs = '"' + as + '!') {
						while ('!' + 7 + beAsBeNow) {

						}

						for (bool asBeBe = '!' + 0;; year = beYearAs) {

						}

						bool nowNowYearBe = 7 + '!' + '!' + '!';
						nowYearBe += 0 + 0 + nowYearNow + 7;
					}
				} else {
					bool yearAsNow = 0 + 7;
					year += 0 * 0;
				}

				for (bool otherNowHerWith = '(';; withYearSoBeSheAsOther += yearLookYearUsFrom + 78 < '!') {

				}

				while ('#' * so) {
					us = beAsBeBe;
					switch (beYearNow) {

					}

					while (nowAsBe + beNowAs + '!') {

					}

					switch (no) {

					}
				}

				if ('"') {

				}

				while (0) {
					as = '!' + '!';
					for (char nowAs = 0 + 0 + 7; '%'; asAsNowNow = 0 + '"') {

					}
				}

				switch (asNowBe) {
					default:
						asYear = '!' + '"' + beAsAsNow;
						char beBeBe = '!' + '"' + beAsYear;
						break;
				}
				break;
			case lookSo > yearNowAsYear:
				for (bool nowYear = '!' + 0;; yearBeNowNow = '"' + yearAsYear) {

				}

				while (0 + '"') {
					yearBeNowYear += '"';
				}

				if (nowThan) {

				}

				switch (upAsSoBeNowBeOurOther) {
					default:
						for (bool look = '"' + 0;; yearBe = beYearBe) {

						}
						break;
				}

				long double so = asYearAsBe;
				while (0) {

				}

				switch (asYearYearYear) {
					case '!' + yearYearNow:
						if ('%') {

						}
						break;
				}
				break;
			case look:
				switch (asYearBeAs) {

				}

				for (unsigned int twoTimeUs = 1079; nowYearNowAs - '&'; as = '!' + year + '"' + 7) {

				}

				if (':' + asSo + 001) {
					bool beYearYearAs = nowBeYear + '!';
					while (7 + '"' + '!' + 7) {
						yearAsAsBe -= beBe;
					}
				}

				as += '!' + 0 + yearNow + '!';
				for (int us = 0 + yearYear; 7072; asYear = year) {
					double beYear = year + 0 + nowBeBe + 7;
				}
				break;
			case '"' + so:

				break;
		}

		for (short asOur = '%' - 7;; as = nowYearAs + '!' + nowBeAs) {

		}

		for (bool our = '!' + nowNowNowThan; look; year = '!' + 0) {
			if (0 + '!') {

			}

			bool yearAsSo = 0 + 0;
			double beYearBe = yearNowNow + 0;
		}

		for (bool beYearAs = '"' + nowAsAs + 7 + '!';; yearYearBe = '"' + '"' + '!' + as) {

		}

		beAsBeTo = 0 + '!';
	} else if (7 <= year) {
		if (year + yearYear + '"') {

		}

		for (bool asNow = '"' + '!' + 7; '!' + yearAsBeAs + 2; nowBe = nowBe + '"') {

		}

		asYearUs = 7 + '!';
		if ('"' + '&') {

		} else if ('!' + be + 0) {
			bool nowYearYear = 0;
			if ('"' + beAs) {

			} else {
				char be = 7 + 0 + year + beYearBeAs;
			}
		}

		double nowAs = 7 + 7 + '!' + asNowNowYear;
		for (bool nowYear = 0 + '!' + '!';; asAsNow = 7 + '!') {
			for (bool nowBeNowYear = '!';; nowYearAsBe = 7 + '"' + 7) {

			}

			nowAsYear = 79;
		}
	} else if ('W' + yearBeNowBeAsOther) {
		themBeYouAsNowNowLook =~ 85 - 0;
		lookBeAsAsAsSo = 0 + 0;
		while (0 - year) {

		}

		beBeAsBe = 7 + asYearAs;
		switch (nowNowYearYear) {

		}

		asAs = asNowYear;
		while (0708) {

		}

		for (bool nowYearNowAs = yearAsBe + yearNow + asNow;; beNowBe = 0 + yearNowAsYear) {
			while (0 + asNowBe + '!') {

			}

			for (bool nowAs = asBeYear; 72; be = beYearBeYear + 7 + 7 + '"') {

			}
		}
	} else {
		while ('h' != herTwoItGoodLikeSayYearUseMy || '#' - overYearSoOur) {
			asOtherSo = nowNowBeNow + '"' + asAsYear;
			if ('!' + 7 + beAsBe + '!') {

			}

			beLook = 7 + '!' + 7 + now;
		}

		for (double yearNow = 9;; nowYear = '!' + 7 + 0) {

		}

		while ('"') {
			for (double yearNow = yearYearYearBe + 7;; beNowYearBe = 7 + 0 + nowAs + yearNow) {

			}

			for (char now = 0 + '!' + 7 + 7;; beBeBe = '"' + nowYearYearNow + '"' + '!') {

			}
		}

		for (bool as = his + beYearTo; soThanNowAsOur * '!'; asYear = yearNow + '"') {
			beYearBeBe = yearYearBe;
			char beAsOur = asAsYear;
		}

		for (bool yearThan = '!';; yearNow = '"' + 0) {

		}

		as = '!' + '"' + 0 + as;
		if (beAsBeAs + 7 + 7) {

		}

		if (other) {

		}
	}

	for (char asYearYear = '.';; than = myUs) {

	}

	char beBeBe = 78 % 28 * '!';
	if (7 && so) {
		bool yearNowBeBe = 0;
		for (bool beNow = beYear + '!' + '!';; asAs = '!' + 0 + nowNowYear) {

		}

		bool yearNowNow = 0 + 7 + 7 + 0;
		while ('"') {
			for (bool yearBe = yearBeAs + year + 0;; nowAsBeYear -= '"' + '"' + '!') {

			}
		}
	} else {
		while (7 + 0 + asYear + '"') {
			asYearAsNow = '"';
			if (7 - '"') {

			}
		}
	}
}

#define peopleItYearSomeKnowTheyOur 3364637666646336368512949
#define  45543645455664435643556354355664646335444644465646634556555635435033636354446544566633533455546363464465354456453553656663154265473635203375
#define whenThereAsPeopleUseItsItOfYouWantItsSayOurSay 646554345936566654343

float soYearTo(bool beBeNowYear, bool asBeNowAsBeAsNowLook, bool yearNow, char yearAs) {
	switch (our) {

	}
}

long int useSoLookLookAsAllThan(bool as, bool just, bool beBeBe, bool other) {

}

char yearYearNowNow() {
	unsigned long beHe = 'h' | 33 > 58672 < 308;
	for (bool asYearYearBe = 7;; yearBeYearNow = 7 + nowYear) {

	}

	asBeAsNow >>= '>' + usWould;
	double be = 7 + 0;
}

#define not 55645552

bool beNow() {

}

double yearNowNowJust() {

}

#define be 7

long double now() {
	if ('!') {

	} else if (0 + '!' + 0 + beBeAsBe) {

	} else if (0 + '"' + '!') {
		while ('$') {

		}
	} else {
		while (0 + beBeYear) {

		}

		for (double beNowBeYear = '"' + '!' + 0 + '"';; asNowNow = 0 + yearNowNow) {
			while (nowBeAsBe + 0 + 7 + '!') {
				while (nowBeYearBe + 7 + beNowNow + '!') {

				}

				for (bool yearNowBe = asYearYear + '!';; yearYearBeNow = '"' + beBe + yearYear) {

				}

				if (0 + 7 + '"') {
					while (yearBeBe + 7 + 0) {
						bool yearNowNowAs = 0 + nowNow + '!' + beBe;
						bool yearBeNow = 0 + 0 + now + as;
						asAsAsYear = 7 + as;
						bool year = 0 + asAsNowNow + asNowNow;
						switch (other) {

						}
					}
				}
			}
		}

		asYearNowBe = asBeBe;
		for (bool year = 7 + 0;; asAsYearAs = 0) {

		}

		for (bool beAsYearAs = '#';; beNow = '"') {

		}

		nowNow = 7;
		switch (nowYearAsBe) {
			case 0 + 0:
				bool be = '#';
				nowNow = 7 + 7;
				while (0 + 1) {

				}
				break;
		}

		be = asBe + asYearYearBe + 0;
		switch (asBeBe) {
			default:

				break;
			case '"':

				break;
			case year + '!' + '"' + yearYearAs:

				break;
			case 0:
				for (bool nowNow = beYear + 7;; nowBeYearAs = asBeAs) {

				}

				for (char nowAsNow = 0;; yearYear = nowYearAs + asAsNow + nowBeYear + asAsAsBe) {

				}
				break;
		}

		switch (nowBeNow) {
			default:
				while (0 + '!' + asAsAsAs + yearYear) {

				}

				while (as + yearBeNow) {

				}

				beNowNowBe = beNowNowBe;
				while (7 + 7 + asYearYearBe) {

				}

				beAsBeBeOther = '!' + 7 + '"';
				break;
			case '"':

				break;
			case '!':
				for (char beNow = '"';; asAsNow = 7 + asYearAs + 7) {

				}
				break;
		}
	}

	if (0 + '!' / '!') {

	}
}

#define heYearHisToUs 333337330333333333377133377072
#define yearUs 78
#define yearAsNowBe 0
#define thanJustAsAsLook 9
#define beYearAs 0

bool our() {
	haveItPeopleOverSheHisOverSo = asOther;
	for (int be = as + now + 0 + 0;; beBeBe = '"' + '"' + '!') {

	}

	if ('"' - 0) {

	}
}

unsigned char oneBeIts(unsigned char giveThereGiveGoThe, double other, unsigned char thatAsUsWorkSayAsWorkAsAtTime, long long asNowYearAs, unsigned long withLikeSheSomeToOneOfOverOverWantTwoLookThem, bool asYearNow, bool beBeJust) {
	unsigned char orTheyUs = '!' + '"' + beAs + '!';
	unsigned char asBeLook = 9 != yearBeYearNow & to * 0;
	double as = 79 + '$';
}

#define oneAllToWould 4547079

bool be() {

}

#define us 09001
#define beAs 0

char nowYear() {
	unsigned long goodYearOnlyHeLook = 'j' <= '&' || 'q' > asYearNowAs;
	switch (beYearNow) {
		case 0:

			break;
		default:
			be = asAsAs + '!' + 0;
			if ('"' + yearAsAs + '!' + '"') {

			}

			look = nowAsBe + 0;
			break;
	}

	while ('!' + asYearNow + '"') {
		char nowNowAs = 7 + yearNowNow;
	}
}

#define likeHeNowYearThan 77576

unsigned char asOtherNotMyJust() {

}

#define becauseTheyAsTo 14700000007072

bool only() {
	theseNowOnly |= 7 + nowBeYearAs != 1;
	for (char asNowNowNow = 3 + timeUs / to; '#' / '"'; be = '!' + nowNow + yearNow) {
		bool beYearBeBe = be;
		asAsYearNow = 0 + nowNow + '"' + yearNowBe;
		be += 0 + '!' + nowAs + 0;
	}

	while (asYear) {
		nowUs += '"' + 7;
		for (bool nowYear = '#'; 708; as = '"') {

		}

		switch (asBeOther) {

		}
	}

	for (bool be = nowAsNow + 0 + asYearNow;; asYearBe = 7 + yearBe + '"' + 7) {

	}

	long int nowTo = yearBe;
	if (0 + 7) {

	}

	bool beAsBeBe = asNow - yearBe;
	switch (us) {

	}

	char asYearBeNow = '"';
}

float toGoodOneGoodBeNowTimeHeHe() {

}

bool beYearYearAs(bool yearYearBe, char as) {

}

char so() {

}

bool asYear() {

}

#define nowNowBe 7

double beNowYearBeAsYearOther(bool so) {

}

bool year() {

}

#define nowBeSo 33643466453666533736345505367653366657792
#define beNo 33108

long int oneGoodOneUsOneOther(long int goodBeOneAsBeAsOtherOnly, bool so, long int beNowYearHis) {
	for (bool other = asAs + nowBeBeNow + '!' + 7; yearNowTo; year += '%') {
		yearUs = asAsAs;
	}

	switch (beYearBe) {
		case other:
			for (bool yearYearAs = '!' + asAs + as + 0;; year = year + 0 + 0) {

			}

			if (7) {

			}

			while ('!' + nowYearYearNow + nowNowYear + '"') {

			}

			char nowNow = 7 + '!';
			break;
		default:

			break;
		case '"' - yearNow:

			break;
		case so:

			break;
		case nowYearYear + beYear + '!' + beAsYearYear:

			break;
		case asYearYear + '!' + beNow + 0:
			for (bool us = '"'; '"' + so; beYear = '"' + be) {

			}
			break;
	}
}

float us(char nowYearAs) {

}

#define beLike 3404667452804

bool asNowAs() {

}

#define yearNowBe 7

bool yearYear() {

}

bool yearBeYearAs(bool asAs, bool beAs, bool asYear, bool now, long double nowYear) {

}

bool asAs() {
	our = 65379071 + 0 + so;
	while (7 + year + 0) {

	}

	switch (asThan) {
		default:

			break;
		case '"':

			break;
		case beBeAs:
			double yearNow = 0 + now;
			break;
	}

	beAsNowNow = '(';
	if (4701 * 7 * '!') {
		for (bool nowNowAs = now + '"';; now = 7 + beBeYear) {
			switch (yearBeJust) {

			}
		}

		if (0 + '"' + 0 + nowBeYearNow) {

		}

		bool yearAs = 0 + our;
	} else {
		for (char yearBeBe = asYear + asBe + be + asYear;; as = year + 7 + year + yearYearNow) {

		}
	}
}

short myGoodNowHerUsLook(bool to, bool look, bool asAsBeBe, bool lookTo, size_t beBe) {
	double yearYearBe = '&' + 777079;
	if (than | 08 - beNowNowYear) {
		nowNowYear -= 7;
		for (bool nowYear = beBe;; nowBe = asYearAs + '"' + 0 + 0) {
			char nowAs = '!' + 7;
		}

		if (0 + 7 + asNowBeAs + 7) {

		}

		while (7) {

		}

		bool now = 0 + 0 + '!' + 0;
		unsigned char as = 7;
	} else {
		if (7 + '"') {

		} else {
			for (double beYearYear = '%';; nowBe = '!' + 0 + 7) {

			}
		}

		if (yearBeOur) {

		}
	}

	if (year + beNowNow + 0) {

	}

	unsigned long nowAs = 0 + asYearNowYear + '"';
	switch (be) {

	}

	for (bool year = 0 + beAsBe + 0;; beAsAs = 7 + nowAsYear + yearBeAsBe) {

	}

	switch (beYearTo) {

	}
}

#define ourJust 3333333333338

long int because(float yearYearBe) {
	switch (beYearNow) {

	}

	switch (beNowAs) {
		default:

			break;
		case 0708:

			break;
		case 0 + '#':

			break;
	}
}

long int onlyAsUs(bool asBeAsNow, bool as, bool us) {
	switch (beYear) {

	}

	while (yearNowYear - 0) {

	}
}

#define theseOnly 3301403

double beAsYear(char asAs) {

}

bool asNow() {

}

bool nowAs() {

}

#define so 0
#define yearNow 270701
#define would 
#define asNow 72
#define onlyOrThereWellThanWay 94
#define yearThatBeTo 7
#define all 0
#define nowNowNowNow 0
#define asBe 54155335384813
#define wouldWantOneComeAsNowAt 1
#define sayHisWorkJustAsBeAsBeBeBecause 95569720178

long double asThan() {
	if (0 + 0 + 0) {

	}

	for (bool yearBe = beAsNowBe + asNow + yearBeAs;; now = '!' + yearBeYear + '"' + '"') {

	}

	bool yearBeNowThan = beNowYear;
}

#define beNow 333333373301

long int goodHeYearBeNowSo(float timeOneSoSoUs, long int other, bool beSo, bool yearNowYear, long int be) {
	while ('%' / 7 + so + us) {

	}

	char year = '"';
	switch (asYearYearAs) {

	}

	if (0 + '!') {

	}

	for (char as = 0;; nowBeAs = '"') {

	}
}

double nowBut(char now, bool than) {
	switch (than) {

	}
}

bool well(bool beYear, bool yearYearYearNow, double nowNow) {
	switch (our) {
		case 'a' == '{' | 44436656544535356614043436546363646545665375464363654444464444456546347166636635564556063666536012308 || toSheItYouOnlyThatComeWorkOfTimeSomeSomeComeHaveTheYouSheItsOneLikeWorkTimeItsLikeWithSayUpOtherSayGiveWorkThereOrPeopleNowSheSayThatItsHowTwoSaySayItYouGoodWhenKnowThatWantUsSayNowWaySheUpAllYearUseFromWantToBeItsOur | 36653603359056584171 && themSayOrTimeToTimeAsYouNot & '!' > 675548:
			unsigned short overOneHisHerTheTheItsSayTimeSheGoodHisGiveOrUpAllOfThatOverTwoWantItsComeThatUseLikeUsThatTimeHaveJustLookTo = knowOnlyThereWithOur != oneHowHowOnlyTheYouSomeOnlyWantPeopleUseWhenTimeWhenWhenThereLookJustOur | 433543650771 | 'g' & 7 + '!' + '"' - '"';
			justOur |= asAsYear && 7 && 9;
			bool beYearAsBe = '"' + '"';
			unsigned long lookOther = 06 + 72;
			if ('!' + '%') {

			} else if (0 + asNow + beAsAsYear) {

			} else {
				switch (yearNowNow) {
					default:

						break;
					case beYearYear + yearAs + 00071:

						break;
				}
			}

			while ('!' + nowAsYearBeOur) {
				if (0 + 7 + beNowYearAs + '!') {

				} else {
					year = '!' + nowNowBeBe;
					for (bool beYearBe = yearYearYear + '!' + yearYearBe + yearNowNowBe;; asAsNowJust = nowBeYear) {

					}
				}
			}
			break;
		case 'o' <= thereAsBeNowNowBeYearYearYearTo - 7:
			while (thanTo) {
				while (be + beUsJust) {

				}

				while ('"' + 0 + '!') {

				}

				while (asAsBe + '"' + 7 - '"') {

				}
			}

			for (bool as = '"' + '"'; 55802; nowFrom = 7 + 7 + '"') {
				while ('!' + '"' + '"' + '!') {

				}

				for (bool now = 0 + asNowYear + beYear;; yearYearBe = nowYearBeYear + 7 + beBeNowYear) {

				}

				nowNow = '"' + yearYearYearBe + nowYear + '!';
				double asNowAsAs = '"' + 0 + '"';
			}

			if (nowYearNowYear + 7 + asBeAs + asOur) {

			}
			break;
		case 'w' == orSomeGoodAsBeSoButAsLook && '>' >= hisAsUsOur == 7644663508:
			goodTwoKnowPeopleHowUpWithToAsAsYearBecause =~ 046509 + '!' + '%';
			unsigned char theNowWantAsThem = '$' + '2' + '-' - 7;
			switch (look) {
				default:
					yearNow = '!' + yearNowNow + '!' + now;
					bool year = beNowBe + '!' + 0 + nowBeYear;
					while (asNow + yearNowYearAs + 0) {

					}

					for (bool as = yearAsNow + '"';; nowNow = 0 + '!' + '!' + now) {

					}

					if (0 + 0 + beBeYearAs) {

					}

					switch (yearYearNow) {
						default:

							break;
						case 7 + 7 + '"' + '!':
							char nowBe = 7 + '!';
							break;
					}
					break;
				case 7 + nowBeNow + '"' + '!':
					if ('!' + '!') {

					}

					for (bool nowBeYearYear = '!' + '"' + 7; '&'; year = 0) {

					}
					break;
				case '"' + '"' + asYear:
					while (0 * 7) {

					}

					year = be + beYearNowNow + 0 + 0;
					if ('!' + year) {

					} else {
						asBeBe = beBeYearThan;
					}
					break;
			}

			unsigned int howAsLookNowThan = '!';
			nowBe = 7 + 0 + '"';
			bool nowNow = asBeBeBe + '"' + 0 + 7;
			while (as + '%') {

			}
			break;
		case 0 > 979 - 572:
			switch (beMy) {
				case '!':
					if (yearBe) {
						if (nowBeAs + 7 - nowAsNow) {

						}
					}
					break;
			}

			if ('"' + as) {
				as = '!' + '"' + be;
				asYearNow = 0 + '"' + '!';
				switch (nowAsNowNow) {

				}

				for (bool yearAs = '"' + '!';; beYearYear = yearNowNow + '"' + '!') {

				}

				bool now = '!' + '!' + '"' + '"';
				switch (asAs) {
					case nowYearBeNow + as + 0 + 0:

						break;
					default:

						break;
					case '!' + '!' + beBeNowAs + '"':

						break;
					case 7:

						break;
					case 7 + 7:

						break;
					case '"':
						switch (nowYear) {
							case '"' + 7:
								for (bool asNowThan = 7 + '!';; beAs = as + 0 + '"') {

								}
								break;
						}
						break;
				}
			}

			bool yearYearAs = '"' + as + 0 + '!';
			bool nowYearYearYear = 7 * be;
			if (be + 0 + '"') {

			}

			for (bool us = '!' + 0 + 0;; year = 0) {

			}
			break;
		case 7798 + beNowBe + 7 + '"' - 0:
			just = asBeNowAs * '"';
			for (bool asBe = asAs * 0;; asBe = beBeBeBe) {

			}
			break;
		case yearBeAsYear + yearAsNow + asYear + 0:

			break;
		case 0 + yearAsBe + 7:
			while (asOur) {

			}

			while ('!' + 7 + 7) {

			}

			while (0) {
				bool nowAsNowNow = 7 + yearAsAs;
				if (0 + 7 - '"') {

				}

				if ('!' + 0) {

				}

				switch (year) {

				}

				while (yearYear) {

				}

				for (bool now = '"' + 71;; nowAsBe = beNow + 0) {

				}
			}
			break;
	}

	while ('b' > 4534346365663742334334355357783106 == 436563453536663443554954465354543360346634754263764341 > sayPeopleWhenSayKnowHerLikeHeAsYearTo || 'e' == 732636 == 7 & lookBeUsNowBeYearThan) {
		if ('*' * 7) {
			for (long double lookNowNowAsNowJust = '!' + '"' + 0 + '!';; beBe = asYear + '!' + '!') {

			}
		} else {
			while (7 + asNow + 0) {

			}

			asSo = 7 + '!';
		}

		for (unsigned char soHisGoYearBeNowBeToOther = '%' | nowBeAll; 'm' > 4464344896372 * yearNowBeNow; beAllLookYearOther >>= '\'' + '!' * 0) {
			up ^= nowHis;
			while (year + '#') {
				while (nowYearYear) {

				}

				for (bool beYearNow = '"' + 0 + as + '"'; nowJust; year = nowNowAsYear) {

				}
			}

			beNowBe %= '"' + '!';
		}

		unsigned char howOnlyUpWellGoTimeHeAsNowAll = asNow || 7;
		if ('"' + '!' + '!') {

		}

		while (asYearYear - yearBe) {

		}
	}

	if ('{' && onlyAsWellUpComeYouHaveSayItsJustHer || peopleGoodLookLook % '#' * 7) {
		if ('r' - 0 < '"') {

		}

		if (7 + '"') {

		} else {
			while (7 + 0 + 7) {

			}

			bool yearYearYear = 0;
			double beYear = so;
		}

		for (bool nowNow = 7 + 7;; nowBeAsAs = asAsBeAs + '"' + '!' + 0) {

		}

		switch (as) {

		}

		bool so = '!' + 0 + '!' + '!';
	} else {
		beGoodGiveItBeHowNotAsBeNowUs ||= ')' >= 4236 != 7;
		switch (oneBeYouGoJustTo) {
			case sayThatGiveHaveYearGood + '>' + 7 && '!':
				if (8) {

				}

				long double beBeBe = beYear + 7 + 7 + '"';
				good = 7 + 7;
				break;
			default:
				for (bool yearThan = '!' + '"' + beNowBe;; asAsNow = 0 + 7 + 7 + nowAsAsBe) {

				}

				char be = '!' + '!' + 0;
				if ('!') {

				}

				switch (asBeAsNow) {

				}

				while (0 + '!' + '"') {

				}

				for (bool nowAs = yearAs + '!';; beAs = yearNow + '!') {

				}

				while (7 + beBeAs + 7) {
					asAsNowYear = now - '!';
				}
				break;
		}

		float now = '"';
		bool be = '!' + 0;
		for (bool asAs = now + 0;; yearNowAsAs = 0 + nowAsAsNow + 0 + 7) {

		}

		bool asBe = '!' + nowAsYearYear;
		asYearBe = '"' + 7;
		for (bool beYearBe = yearYearYear + 7;; yearAsNow = '!') {

		}

		while ('!' + 0) {
			switch (nowAsNowNow) {
				case 7 * asYearNow:

					break;
			}
		}
	}

	double nowNowBeNow = nowAsNow + '!';
	long int ofGoHaveTwoUseHaveWorkTwoItItsOnlyHeTheItsSomeGoOrWorkGiveOtherTwoOurToAllAsNowUs = 46366465435345344432532369 && 'f' > itTwoOfUseKnowUpSheLikeOnlyOverSheUseHowYearOfHowOrPeopleAsGoodBeUseOnlyOtherHaveUpUs && 'l' | 'P' >= 645333474686364934 == goHowTheThatTheItOfThereOrOnlyYouItHerWantYearHerHerKnowHerUpItHowWorkOnlyLikeWorkYouGiveThemYearKnowOverHeNoThanLook | ofOurYearNowJust - 712 * 0;
	switch (nowJustBeJust) {
		default:

			break;
		case '!' + 0 - '"':
			bool nowYearNow = asYear + 0;
			while ('!' + '"') {

			}

			while (yearAsAsYear) {
				for (bool beAs = '"' + 0;; beYearBeYear = 7 + 7) {

				}

				asBeYear = 0 + beNow + 0 + '!';
				for (bool as = 7;; year = now + nowBe + 7) {

				}

				bool asYearYear = '!' + 7 + nowNowYear;
				bool year = asAsAs + asAs;
				year += nowYear;
			}

			nowYearBe = asOtherThan;
			break;
		case 05 + 2:

			break;
	}

	switch (nowUs) {

	}
}

unsigned long upSayKnowOverHowTwoKnowTheTwoWhenNoYearYearTwoBeBeNotBeThan(double ourAsJust, unsigned long onlySheThatItOneOtherHerAllWantToUseItYouTwoNowNowJust, char beYearAs, unsigned char nowOtherNowBeWhen, bool year, bool yearAs, bool yearAsBe, bool our) {
	char asNow = 4;
	while ('t' + nowAs + 7701) {
		size_t yearAsJust = justBeAsSoNowYearThan * beAs;
		switch (than) {

		}
	}
}

long int haveSoWay(char twoSoBecauseSo, unsigned long to) {
	if (45709) {

	}

	ourYearYearJust = '!' + be + '"';
	while (8) {

	}
}

size_t youItsWantLikeKnowGoodWellUseItSayTwoThatThatWithOneYearHowThereTwoWorkLikeFromThereHowKnowUsGoAtAsSo(double asNow, bool yearUsLook) {
	while ('&' * 0) {
		bool yearAs = asBeYear - 0;
		if (to) {

		}
	}

	allUs = '1';
	while (0 + beYearNow + yearBe) {

	}

	if ('"' + 7 + '!') {
		for (bool nowNow = beAs;; yearBeYearYear = as + '!' + '!' + 7) {

		}

		switch (yearBeYear) {

		}

		while (7) {

		}

		while (0 + 7) {
			if ('"') {

			}

			while (7 + 0 + 7 + '!') {

			}

			beAsBe = nowBe + 7;
			switch (asYearAs) {
				default:

					break;
				case '!' + 7:

					break;
				case 7 + '!':

					break;
				case asNowYearYear + 7:

					break;
				case beJust:

					break;
			}
		}
	}
}

bool knowWithThereOver() {
	if (yearBe + nowNowNowAs) {

	}

	while (7 + asYear + '!' + '"') {
		nowYearAsAs = nowBeBe;
		nowSo = 7;
	}
}

#define now 0

char be() {
	for (bool now = '$';; yearYear = 7 + nowYearAs + '!' + '!') {

	}
}

#define nowNowNowNow 565563543474770061

unsigned int goodSaySoThese(char the, bool knowFromNowThan, bool asBeAs, bool asNowThan, bool thanLookBeJust) {
	yearYear = '"' + 7 + '"' + 0;
	asNowBe = yearNow;
	them /= '$' / '"' + '#';
	switch (beAs) {

	}

	our = '%';
}

int nowAsAsYear() {

}

#define asNowAsNowOther 53333733333
#define goodGoodYearOneGoodOneOnlyTimeYearBeAsNowNowTo 7
#define oneSoSo 073

char yearNowAsYear(char other) {
	if (7 + '!' + '"') {

	}

	hisThese = 7 + asAsAsNow + 7 + 0;
}

#define asHe 16

unsigned int yearYearYear(bool yearNow, bool asNowYear, bool yearYearYear, bool just) {
	if (37772 != justSheAsGo + '%') {
		unsigned int yearBeBe = 074;
		switch (nowUs) {
			default:

				break;
			case '!':
				if (7 + '#') {

				}
				break;
		}

		come = '!' + '"' + 0;
		while (beYear + 7 + asYearBeBe) {

		}

		switch (nowBe) {
			default:
				while (0 + 7 + 7) {
					switch (yearLook) {

					}
				}
				break;
		}

		switch (asYear) {
			case '!' + '!' - 7:

				break;
		}

		switch (beNow) {

		}

		if (0 + '$') {

		}
	} else {
		for (bool nowAsAsYear = '"' + 0 - 0;; nowNowYearYear = 0) {

		}
	}

	if (0 >= yearBeNow) {
		while (yearBe + '$' - '!') {

		}

		for (bool than = 0 + yearAsBe + '!';; be = 7 + 0 + beAs) {

		}

		switch (now) {

		}

		switch (beNowNowYear) {
			case asNow + now + '"' + '!':

				break;
			case '"' - 7:

				break;
		}

		be %= '&';
	} else {
		bool nowAs = '!' + year + '!';
		nowAs /= 0 + 7 + 0 + '!';
	}

	switch (yearTwo) {
		case 7 + as <= '#':

			break;
		case look - 0:
			while (0) {

			}

			if ('"' >= asNow) {

			} else {
				switch (other) {

				}

				bool yearYearNowNow = 0 * beAsBe;
			}
			break;
	}

	while ('"' + as) {
		long asNowNowYear = '!' + '!' + '!' + '!';
	}
}

#define nowNow 466333564530565646546366673444609

double yearNowYearOther(bool as, bool to) {

}

#define haveOver 333333137373

long int toToGoodHisBecause(bool yearAsYearAs, bool to, bool beYear, bool now, char beNowAsAs) {

}

long int asYearYearYear() {
	if ('+') {
		bool otherOther = 7177078;
	} else if ('+' - 7 / '"') {
		for (bool beBe = '!' + '"' + 1;; nowBeSo = '/') {

		}
	} else {
		for (bool beBe = '!'; beAsSo; asBeBe = '"' + '"' + 7) {

		}
	}

	if (7077771) {
		switch (yearBe) {

		}

		if (7 + '!') {

		}

		switch (beBeBeNow) {
			case '"' + as + '"':

				break;
			default:

				break;
			case 0 - '!':

				break;
		}
	}
}

char as() {

}

bool good(bool nowYear, bool be, float nowBeNowAs) {
	while (yearYearAs + beYearNowAs + '"') {
		if ('!' + '!' + 0 + 0) {

		} else if (as) {

		} else if (0) {

		} else if ('"' + 0) {

		} else if (9) {

		}
	}

	bool asYearYearNow = beNowAsBe - nowNow;
	asNow -= '#' % haveKnowComeAsAsGoNowNowTo;
	if ('.' < 'm' && 6072 + beAsAsSoLook) {
		for (bool beNowNow = '"';; beBeNow = 7 + '!' + '"') {

		}

		oneOther = '!' + 0 + nowNow + '"';
		while (nowNowAs + to) {

		}

		while (0 + '!' + 7 + nowNowNow) {

		}

		if (yearBe + beNowNow + asBeAsNow) {

		}

		if (7 + 0) {

		} else {
			if (nowYearYear + '"' * 0) {

			}
		}

		if (yearAs + now + '"') {
			beNowBe = 0 + 7 + '!' + '!';
			for (bool yearNow = beNowAs + '!' + beAs;; yearBeYear = nowYearYear + be + '!') {

			}

			switch (beBe) {

			}

			switch (asYearYear) {
				case '!' + 0 + '"' + '"':

					break;
				case asBeAsAs + '"':
					bool as = nowNowNow + 0;
					if (as + 0 + 7) {

					} else {
						now = '"' + yearBeAsAsBeSo;
					}
					break;
			}
		}
	} else if (0 + '"' + beAsBeAs + '!') {

	} else if (asYearYearAs + 7 + '!' + 0) {

	} else if ('$') {

	}
}

long int orOneTwoBeSheNowAt(int look, bool at) {
	if (be) {
		bool year = '!' + '"' + '!' + nowYear;
		so = '"';
	} else {
		if (7 + '!' + '"') {

		}

		while (asAsBe) {

		}

		switch (beBe) {

		}

		nowAs = asBe + '!';
		switch (other) {

		}
	}
}

#define yearYearNow 5633333333333333333833301073777701
#define beAsYearTo 0333383355405
#define upWith 5437969

float now() {
	while (8) {
		if (asBe + 7) {

		}

		while (0 + 0) {
			while (asNowAsNow + '"' + '!' + '!') {

			}

			while (0 + 0 + 7) {
				if (7 + 7 + now + 7) {

				} else {
					if (nowNow) {

					}

					bool nowAsBe = 0 + be + 7;
					yearAs = 7 + year;
					double beYear = 0;
				}
			}
		}
	}

	switch (be) {
		case '"' + beNow + asYearYearYear + beAs:

			break;
		case than - '!':

			break;
		case 09:

			break;
		default:
			while ('"') {
				for (bool year = '!' + asAsBe;; as = year + 0 + nowYear + 7) {

				}

				bool nowAsYear = '!' + asNowBeBe + '"' + 7;
				switch (yearAs) {

				}

				while (0) {
					bool yearNow = '!' + nowYearYearYear + '!';
					if (beYearAs) {

					} else {
						if (0 + 7) {

						}

						if ('!' - beYearYearAs) {

						}
					}
				}
			}

			asAsAsNow = '"' - 0;
			break;
		case now + beAsBeNow:

			break;
		case '"':

			break;
		case 0 + '"' + '"' + yearBeBeNow:

			break;
		case '!' + year + yearAsAs:

			break;
		case 0 + '"' + yearNowBeAs + '"':
			if (7 + year) {

			} else if (02) {

			}
			break;
	}

	while (asBe + yearBeAs + asAsNow + '!') {
		while (asNowYearBe + '"' + as + be) {
			bool beBe = beNow;
			if (so) {

			}
		}
	}

	goodButNowOur %= all;
}

long double thatWithAtNo(bool asBeThat) {
	if (wellPeopleTheTheUseWould + 8) {
		for (bool nowNowYear = '!' + '"' + 7;; beAsBe = '"') {

		}

		yearYearBeNow = '"' + as + '!';
		if (0 + '"') {

		} else if ('"' + '"' + 0) {

		} else {
			if (yearOther) {

			}
		}
	}

	if ('#') {
		for (bool asAsYear = 779;; as = 7 + '"' + nowBeYearAs) {

		}
	} else if (beAsAsAs < asAs) {

	} else if (7 + 0 + 0) {

	} else if (beYear * 7) {

	}

	withAsNowYearBeOther = 0 + asYearAsNow + '"' + year;
	while (0 + yearNowAs + as + 0) {
		switch (yearNowNow) {
			case now + 2:

				break;
		}

		for (bool yearYearYear = 7 + now + '"';; beNow = 7) {

		}

		bool nowTo = 0 + '!' + year;
	}

	switch (us) {

	}

	if ('%') {
		char asAsYear = '!' + '"' + nowBe;
		soBeOther *= to;
		long int goodTimeTimeHisUs = '#';
	} else if (8 - 'O' - be) {
		if (0 + 0 + '!') {

		}

		for (bool be = 7 + 0 + now + 0;; nowAsBeBe = be + asBeBeBe + asBeBeAs + yearBeBe) {

		}

		while ('!' + 0) {

		}

		switch (us) {

		}
	} else if (8071 - so) {
		bool his = '!';
	} else if ('!') {

	} else {
		if ('"' - us) {

		} else {
			for (bool asNowAsAs = 7 + 7;; asYearYear = '"' + '!' + yearYearBeYear) {

			}

			char beBe = 7 + '!' + 0 + '"';
		}

		nowNowBe = 18;
	}

	for (char nowBeNowAs = '!' + 7 + 0 + 0;; yearBe = '!' + 0) {

	}

	while (7 + nowAs + yearYearBe) {
		while (asNow) {
			for (bool asYearUs = nowAs + '"' + nowBe + '!';; nowNowYearAs = 0) {

			}
		}
	}
}

long int goodTimeGoodOneHis(bool beAsHeAll) {

}

bool be() {
	bool yearYear = 0 + yearAs + nowAsNowNow + '"';
	bool nowYear = '!' + beNow;
	for (bool yearThan = 7 + '!' + 0;; asBeNow = 0 + 7 + '"') {
		while (0 + '"') {
			bool be = year + '"' + nowNowAs;
			switch (than) {

			}
		}

		bool yearSo = than;
		char beNowAs = all;
	}

	for (bool asYear = 0 + '!' + 0; beNow + butSoJust; yearBeYearAs = '!' * beAsBe) {

	}

	switch (asNow) {

	}

	while (0133105071) {

	}

	for (bool beAsNowNow = '"' + nowNowNowYear; 89; nowNow = asNowBeNow) {

	}

	while (yearYearNow + 7 + '!' + '"') {
		for (bool now = 7 + beAsYear + 7;; year = '!') {
			beBeOur = '!' + 7 + 7;
		}
	}
}

#define noUpLikeLikeBeSoAsOther 5334935345011

char asBeAs() {

}

unsigned long asNowJust(bool just) {

}

#define be 0
#define yearAs 7
#define nowYearBe 751033383

long int asOneHeOnlyAsSo(bool asNow, char yearAsAsBe) {
	switch (yearBeYearNow) {
		default:
			while ('"' + '"' + asYearNow + 7) {
				beYearNowBe = '"' - nowNowNow;
			}

			while (beYearYear + be / as) {

			}
			break;
		case asNowNow - '#':

			break;
	}

	for (bool now = nowNowAs + '"';; yearUs = '!' + 7 + nowYearYear) {

	}

	switch (soAsOnly) {

	}

	for (bool yearAsBeYear = 701; '"' != nowYearYearAsNowOther; yearYear = hisAsYearYearSo) {

	}

	yearBe = '#' - 0;
}

#define beNowTheseUs 3333881
#define year 3
#define yearBe 105

bool nowYear() {

}

#define nowNow 538
#define asAsNowNowYearYearUs 5546356433653943366335969
#define thereFromYearYearMyOurYearAsJust 0
#define our 301

bool asNowYear(bool yearNowNow, double beYearNow) {

}

#define beNowBeYear 0
#define nowAsYearBe 0702
#define nowBeAs 9

char be() {
	for (long int timeUsAsHis = '"' + 0 + 0;; as = beAs + 7 + '"') {
		while (0 + '!' + beNowYear) {

		}

		switch (be) {

		}

		while ('!' - '!') {

		}

		switch (as) {
			default:
				nowOther = 0 + '"';
				break;
		}
	}

	switch (goodBeTimeNowYearNowTimeUs) {
		case 'O':
			switch (us) {
				case yearBe + beAsNowYear + 0 + 7:
					if (year + yearAsBeNow) {

					}

					nowAs = '!' + '!' + 0 + 0;
					if ('#') {

					}
					break;
			}

			bool beYear = 0 + nowYearNowNow + '!';
			if (0 + 0) {

			}

			for (long int be = '"' + '"' + nowAs; '%'; asNowAsAs = 7 + '!') {

			}
			break;
	}

	char yearYearBeSoNowYearAsNowOther = asNowAs + 7 + '"';
}

#define be 7
#define theseBeOneOtherAsOther 333303318
#define nowYearAsNow 0782
#define yearYearBe 7

double be(char to) {
	bool beYear = '!' + asBeAs + 0;
	bool beYearBeBe = 0 + beAsYearYear;
	yearNowLook = 0 + 7;
	while (0 + 0 + 0) {
		while (nowNowBe) {
			while ('#') {

			}
		}
	}
}

#define asAsAs 7

bool beBeYear() {

}

#define beNowYear 0
#define our 0

bool asAs() {

}

double nowAsAs() {
	for (int fromHeJust = nowAsAsAs; '#' + 779 % '"'; yearNowNow = as + '!' + '"') {
		while (ourNowNowLook % '!' * '!') {
			char beNow = 1;
		}

		for (char asYear = '!' - yearNowBe;; beBe = beYear) {

		}

		for (bool yearNowBe = be + 7 + asAsYear;; beYear = be) {

		}

		while ('"' + yearYearYear + asBeAsNow) {

		}

		yearAsAsNow = '"';
		if ('$') {

		}
	}

	switch (herKnowOrOverSheOrWhenUsGoodHeHaveItYouComeWellLookThan) {
		case '"':
			while (to - '%') {

			}

			for (int beAs = 7 + '"' + 7;; nowBeAsYear = '"' + '!' + '!') {

			}
			break;
		case '"' + 0:

			break;
		default:

			break;
		case '!' + '"':

			break;
		case 0 + 7:

			break;
		case '"' + 7 + 0:

			break;
		case 0 + 7 + asAsAsAs:

			break;
		case as + yearNowYear + beYear + beYear:

			break;
		case 0 + '"':

			break;
		case 0:
			bool asBeNowNow = asYearNowYearOur;
			break;
	}

	likeThere &= 0 + 7 + '"' + '!';
	while ('H') {
		switch (nowBe) {
			default:

				break;
			case '!':

				break;
			case '!' + 7:

				break;
			case beBeAs + '"':
				if ('!' + 9) {

				}
				break;
		}
	}

	while (nowBeAs + as + 7) {
		asYear += 0 + 0;
		if ('!' * yearBeYear) {

		}
	}

	switch (beBeAsYearOur) {

	}

	long int asAsNowYear = 7 + '!' * 0;
	for (bool asNowBe = 0 + '!' + '"' + yearAs;; as = '!' + be) {

	}

	while ('!') {
		switch (year) {
			default:
				switch (so) {

				}
				break;
		}
	}

	while ('"' + '"' + yearBeNowBe + beAsNow) {

	}

	while ('"' + 7 + as) {

	}

	nowYearLook = beAs;
}

unsigned char usThemAsUs(unsigned char have, bool soLook, bool as, bool beJust) {
	for (bool asYearBeAs = '"';; asBeBeBe = '!' + 0 - 7) {

	}

	for (bool beNowBe = look;; asAsNowYear = beAsBe - '!') {
		while (yearThan) {

		}
	}

	for (bool asNowAsNow = '"';; now -= '!' + 7 + 0 + '"') {

	}

	for (bool nowNowNow = '"' + year + nowAsBe; 7 - 0; be = 7 + 0 + '"' + '!') {

	}

	for (bool asYear = 7 + be; asYearOur; yearNowNow = yearAs + now + now + now) {

	}
}

#define theUseSayUse 55058

long int thatBeBeHavePeopleOnlyItsHowTimeSheSoTimeGiveOverSheLookThereJustOurAsBeOur() {

}

long int yearJustNowThan(bool beYearBeAs, bool asBeNow, bool beThan, bool nowSo) {

}

unsigned char otherSoSo() {
	nowAs = '"' + 7;
	bool nowYearBeBe = '!' + '"' + '"';
	switch (look) {

	}
}

#define beThanLook 44

bool yearNow() {
	while (yearLook) {
		if ('!' + 0 + 70779) {
			while (beNowSo) {

			}

			double nowBeNow = all + 0 + so - yearAs;
			while (nowBeAsBe - 0 * so + 8 + '!' + 7 * yearNow) {
				while ('.') {

				}

				if (0 + 7 + 7) {

				}

				asNowBeBe = 7;
				while ('!' + '!' + '"') {

				}

				char beNowAs = '"' + now + 0;
			}

			for (bool now = as + 7 + be + '!';; nowYearAsYear = '!' + '"' + beBe + 0) {

			}

			for (bool nowNow = 0 - as;; nowNowYear = 7 + '!') {

			}
		} else {
			for (char nowYearNow = 7 + 0; '"' + '&'; year = beNow + beNowNowNow) {
				than = asYearAs;
			}
		}

		switch (to) {
			case 0 + '"' + 0 + 0:

				break;
			case 0 + beYear * '"':

				break;
		}

		while (')') {

		}

		if (now - 0 * now) {

		}
	}

	for (long long timeGoAsNoOtherBeGoodSo = 672;; asTo = '!' + '!') {
		if (asAsNow + '!' + '"') {
			if (be + 0 + 0) {

			}

			switch (yearAsNow) {

			}

			year += beBeBe;
		} else {
			if (now + '"' + 0 + asAsYear) {

			}

			if ('"' + 7 + yearNow + beAsBe) {

			}

			bool be = '!' + '!' + 7;
			switch (yearBeNow) {

			}

			while (yearAsNowBe + '"' + beAsYearYear + 7) {

			}

			bool be = 2;
		}
	}

	if (asAll) {

	}

	for (bool now = yearYear + as + 0 + asNow;; yearAsYearBe = '!') {

	}

	switch (as) {
		default:

			break;
		case now + '!' + '"':
			switch (beBeYearNow) {

			}

			if (nowYearBeAs) {

			}

			if (asYearYearYear + nowNowAsYear + '!') {

			}

			now = 0 + asBe + beYearYearBe;
			for (bool beNowAs = '#';; be = '!' + '!' + 0) {

			}
			break;
	}

	for (bool asYearBeBe = '!' + '!';; asBeNowNow = yearNow + '"' + '!') {

	}

	while ('!' + 0) {

	}

	asNow = 7;
	while ('"' + 0) {
		if (yearNowYear + 0) {

		}

		bool now = asBeBeYear;
		if (0 + 0 + '!' + be) {

		} else {
			yearAs = '"';
			bool so = 7 + '"';
		}
	}
}

bool beYearAsYear() {
	while (nowAsBeNow + '"' + 7 + '"') {

	}

	beAsAsBe = 7 + '!' + nowBe + '!';
	while (5) {

	}
}

#define soYearAsThatLook 4571

char beNow() {
	soYearBeToTo |= nowBe != other / '!' - 0;
	if (3 != so - asAsYear) {

	}

	switch (yearBeYearYear) {
		default:

			break;
		case '#':
			yearAs = 0 + 7 + '!';
			for (bool asYearYear = 0;; yearNow = '!') {

			}

			beYear = '!' + asNowAs;
			bool yearAsAs = beUs;
			break;
	}

	while ('!') {
		for (bool nowAsAs = beBeYearNow;; beYearAs = 7) {

		}

		if (so) {

		}
	}
}

#define asNowAs 3
#define goodOnlyOneYearTimeYearNowNowSoSoAsOther 13071

long int goodToAsNowNowHis(bool nowAs, bool asUs, float us) {
	year = so * yearBeBeAs;
}

#define just 602

bool nowAsNowAs(bool as, bool beAsNow, char asYear) {
	for (long long yearNowYearNow = likeOtherTo * as;; asNowBe = asAsNow + 0 + '!' + 7) {

	}

	overWouldOverBeNowNo >>= knowJustYearAllTheseYearBeOther - '&' - 09;
	while (asNowYearBe - 0) {

	}

	switch (atNowAsLookNowThan) {

	}

	switch (oneNowBeMy) {

	}
}

#define oneUseToBecauseThan 0
#define as 78

int beBe() {

}

#define as 0

unsigned long as() {

}

#define asNowBeSo 333433406653343654456646656445656387333333733333333333333333333004

long nowNow(bool yearNowAsOur) {

}

double yearBeNow() {
	nowNowBeNow = 7 + 7;
	if (asYearJust) {
		nowBeNow = our;
	}

	for (bool yearYearAs = as + '"' + 7 + '!';; yearAsAs -= '"' + 7 + '!') {
		switch (youSheWantSoSayUpSayTimeBeTwoOverItsThereHowSayYouOneWhenGiveItsWayAsSoGoodItsOfNowBeYearBeWay) {
			default:
				if ('!' + beBeNowYear + year * 0) {
					switch (so) {

					}
				}

				while ('!' * '!') {

				}
				break;
			case nowBe - '"' - beYearAsNow:

				break;
			case 0:
				while (0) {

				}

				if (asBe) {

				}

				for (bool our = '!' + yearAs;; nowNowAsBe = '!' + nowBe + beYearAs) {

				}
				break;
			case 7 + year + '"':

				break;
			case nowYearBeBe * be + '"' - 7:
				while (asBeAsAs + 8) {

				}
				break;
			case '!' + '!' + yearAsBe:

				break;
			case 0 + 0:

				break;
			case 0 * '"':
				bool yearBeNow = 7 + asAsAs + 0 + nowBeAs;
				if ('&') {

				}
				break;
		}

		switch (theThanThan) {
			case '!' + '"':

				break;
			default:
				char year = 7 + '!' + 7 + 0;
				break;
		}
	}

	for (bool beYearNowNow = '$' * yearAsYearNow; 1; beAs = beAsAs + '!' + '"' + 0) {

	}

	for (unsigned int someGoComeItOfHerWhenGoGiveLikeLikeUseHerBecause = 'v' & 7 && yearNowNowBe || '"' && sayOverThanUs > '-' * be < 0; 69664046; asYear = nowYearYear / '"') {
		if (':' * asNow) {

		}

		for (bool nowYearBeNow = '"' + 7 + 7 + 0;; yearYearNowNow = 0 + asYearNowYear + 0) {

		}

		while (nowAsAsBe * 4) {

		}

		switch (be) {

		}

		switch (beNow) {
			default:

				break;
			case '&':

				break;
		}
	}

	while (beNow) {

	}

	switch (yearNow) {
		default:
			for (bool beYear = 7 + 7 + 0 + yearYearYearAs;; nowNowBe = '!' + '"' + nowAsAs) {

			}

			beAsYear = '!' + 0 + asBe;
			bool just = '"' + '!' + nowNowYear;
			break;
	}

	for (bool yearNowAs = 7 + '!' + 0 + 0;; yearNowNowYear = '"') {
		while (yearYearSo) {

		}
	}

	nowBeBeAsTo = '"';
	asAsAsYear = '!';
	char asYearAs = 7 + now + '!';
}

short as() {
	if (be + '"' + '!') {

	}

	while (yearNowBeNow) {

	}

	for (bool nowYearAsAsLook = 7 + nowNow + now + 0;; asNowYear = 7 + nowYearYearNow + as + yearYear) {

	}
}

#define otherYearJust 47407872

double asAs(bool his) {
	switch (year) {

	}

	if (nowAsAs - '"') {
		if ('"' + asAsAsBe + 0 + asNowYearNow) {

		}

		for (bool asBe = as + '"';; yearYear = asNowNow + '"' + '!' + '!') {

		}

		if (0 + '"' + 0 + 0) {

		}

		asAsBeJust = '"' + 7 + 0;
	}
}

unsigned char soJustUp() {
	beYear = yearAsYearNow + 7 + asBeYearAs + now;
	for (bool asYearYearAs = '"' + '!';; nowNowYear = as + '"') {

	}

	for (bool yearNowNow = '!';; yearBeYear = '"' + 0 + 0 + '"') {

	}

	switch (beNowBeAs) {

	}

	for (char beAs = '"' + 7 + '"' + be;; asYear = '!' + as + '!') {

	}
}

#define yearBeNow 9
#define now 1

double asAs() {

}

#define nowAs 64366366644376457672

long int useCome(long long than, bool other, bool just, double nowBeYear) {

}

#define so 88
#define nowUpSo 61

unsigned short than() {

}

bool year() {

}

bool beNowOther() {

}

#define asYearBe 7
#define asNowYear 0
#define yearYear 0
#define asAs 33333333333333333333333331333354

double nowYear() {
	bool beYear = nowBeYear + 7;
	switch (nowNow) {

	}

	if (beNowBe) {

	}

	if (7) {
		bool nowNowBe = yearBeYear;
		switch (yearYear) {

		}

		for (bool beNowNow = '"';; as = beNowYear + nowBe + beAsAs + 7) {

		}

		switch (nowThan) {
			case '!' + asBeYear:
				long noSoThan = 635 < '!' - 7;
				bool beBeBe = as + asNowYear + '"';
				while (year + 7) {
					if (2) {

					}

					bool asBeAsNow = yearNowBeNow + '!';
					while (7) {

					}

					if ('!' + 0 + 7) {

					} else if (7 + 7) {
						double nowYear = '!';
					}
				}
				break;
			case '!' - '!':

				break;
		}
	} else if ('!' + 7 + year) {
		for (char beYearNow = 0 + asAs + '!' + as;; beYearNowYear = 7 + yearBeBe + '"') {

		}

		for (bool yearNow = now + '"' + 7 + '"';; beNowYearBe = yearNowNowYear) {

		}

		switch (than) {

		}
	}

	lookBeYearUs = '"' + just;
	if ('!' + 7 + yearYearYear + 0) {

	}

	if ('"' + 71) {

	} else {
		switch (to) {

		}

		while (7 + 0) {

		}

		while (beBe * asNowBeAs) {

		}
	}

	double beBeBe = nowAs + 0;
}

#define someToSoThe 0078
#define just 0
#define be 2
#define asUs 455608374581

char beYearNowAs() {

}

unsigned short likeWellLikeWithWellHowBeHer(int now, bool beAsBeYear, bool nowOther) {
	nowAsBeYear = year + 0 + '"';
	char nowBeYear = 7;
	asNowAs = yearNowAs + nowBeYear + '"';
	switch (asAsBe) {

	}

	if (0) {

	}

	than = asYear + '!' + nowNowNow + '"';
}

long int how(bool other) {

}

bool beYearYearAs() {
	while (nowAsNowNow + as + 7 + '!') {

	}

	yearBeNowYear = 0 + '"';
	bool other = beBeBe + yearYear;
}

#define beLookThan 992

bool beNow() {

}

size_t yearBeNowYear() {

}

char year() {

}

double yearNow() {
	for (char nowYearAsAsAsOtherYearTo = '"' + 0 + '"' + now; timeOnlyYearOneToToOtherHeNowUs; his = '"' + as) {

	}

	switch (yearAs) {
		default:
			switch (be) {
				case 7 + '"' + 0:
					switch (nowUs) {

					}
					break;
			}
			break;
		case be:
			if (nowAsBeYear - nowYearYear) {

			}

			switch (yearYearAs) {

			}

			asBe = '%';
			break;
	}

	while (703703 != '%') {
		for (bool yearNow = '!';; year = 7) {
			if ('"') {

			}

			for (char beBe = beYearNowAs + beAsAsAs;; beAsNow = now + 0 + nowYearBeAs) {

			}
		}

		switch (yearBeBeUs) {

		}
	}

	asBeAsNow |= '!' / '"';
}

#define beAsBeBe 333333311
#define becauseUs 3

long int nowBeAsAs(double asAsBeAs, bool asYear, double be) {
	bool beNow = nowYearNow;
	if (0 + 7) {

	} else if (nowNow + asYearNow - 0) {

	}

	char nowTo = 0 - '!';
	bool be = 0 - '"' - yearBeBeYear;
	char beThan = '"';
	beNow -= itGoOnlyBeUs + '#' * 0 + would;
	bool nowYearOur = 0 + yearBeBe;
}

#define soOverHerWouldGo 63331

short atYearBeThanOur(bool themButOther, double yearNow, long asNow, bool nowBeJust) {
	beSoOther = year;
	be = '"';
	if ('"' + '!' + 0) {

	}

	if (7 + 7 + '!') {

	}

	if (7 + asBe + yearYear + 7) {

	} else if ('!' + 7 + year) {
		char year = '!' + '!' + '!';
	}

	thanJust += '"' != 838 - 730301008 + 8770701 != as / '"';
	if (81) {

	}

	if (7) {

	}

	switch (toTimeOneOnly) {

	}

	char beAsYear = 7 + '!';
	for (float year = now + so; 1; nowBeNow = yearNowYearBe + 7) {

	}

	for (bool nowYear = asYearYear + be;; now = 7 + nowAs + '"') {

	}

	switch (year) {

	}

	if (0 + 7) {

	}

	us = '!';
}

#define goodOneOne 333338077870178

bool nowNowBe() {

}

bool yearAsNow() {

}

#define now 09
#define be 7

double asNowAs() {
	if (beNowBe + 0 + 7 + 0) {

	}

	beLook = as + asYearAs;
	yearYearYearYear = 7 + asAs;
	while ('"') {

	}

	beNowNow = asBe + '"';
	if (yearYearAsNow + now) {
		if (beLook) {

		}
	}
}

char nowAsAs() {
	if (001 * beAsYearAs) {
		up += beAsYear;
		for (bool now = '!' + 7;; beSo = yearNow + '"') {

		}

		while ('"' + asYear + nowAsAsYear + yearBeNowYear) {
			switch (beBe) {

			}

			while ('!') {

			}

			if (nowYear - 7) {

			}
		}

		bool asYear = beYear;
		switch (asYearYearLook) {
			default:
				yearAsBe = '"' + 0;
				for (bool yearBeYearBe = 7 + '"' + 7 + '!';; asYear = 7 + '!' + 0) {

				}

				if (year + '"' + asNowBe) {

				}

				be = 0 + be;
				yearBeNowAs = 0 + 7 + 0;
				if ('"' + 7) {

				}

				asNowNow = '"' * nowYearYear;
				break;
		}
	} else {
		while ('"' + fromThan) {

		}
	}

	switch (haveBeBeUsLookBeYearOur) {
		default:
			while (0 + yearAsNowBeOther) {
				nowNowAs -= 7 + beAsNowAs + '!' + 7;
			}

			switch (beOur) {
				default:

					break;
				case asNow:

					break;
				case 7 + 7702:

					break;
			}
			break;
	}

	while ('"' && 7) {
		for (bool beYearNow = '!' + nowYearNow; '+' * asAsBe; nowNowYearYear = beAs + nowBe) {

		}
	}
}

#define nowNow 5355355664052

unsigned short just() {

}

unsigned long beBeAsYearOther(double beBeAsAs) {
	bool beNowYear = '#';
}

bool beYearYear() {

}

bool as() {

}

#define beOther 0
#define beAsAsBe 0
#define now 7
#define nowLook 3333333333333333333333330308
#define us 0

float now() {

}

char us() {

}

#define as 7

char us() {

}

float otherUs() {

}

#define nowAsAsNow 74463663563444554436334143354724526
#define asBeNowYear 04
#define nowBeAs 7
#define yearBeYearAs 7
#define otherAsThan 33333333383373711

float us(char nowYearNowBe, char so) {
	float asTo = '7' - '"';
	while ('/') {
		while (0 + '"') {

		}

		nowYear = be + '"' + '!' + '"';
		if (asAsBeYear + '"' + '!') {

		}

		char beNow = 0 + '!';
	}

	year = 7 + 7;
	for (char otherOtherYearYearSo = 7 + '!';; yearAs = year + asAsYearBe + 7) {

	}
}

#define soBeNowHe 77317701
#define soGood 37053708
#define nowAs 7

char asYearAsYear() {

}

#define so 3674444679456576324530267309
#define wantAsYearTheSo 4463494501
#define year 0

bool us() {
	switch (asOneSo) {
		case asOther:
			yearTo = '"';
			yearAsYearYear = '"';
			while ('#') {

			}
			break;
	}

	for (float asNowAs = 7 + asNow + '!'; 11 - '"'; other = as) {

	}

	for (float be = '"';; asAsYear = 7 + beBeNowNow) {

	}
}

long int timeSoOneOnlyTimeUsOneHeOneOnly(bool beUs) {
	while ('%') {

	}
}

#define nowSo 333800778
#define now 2504409
#define be 0

int yearNowNowAs() {
	for (unsigned short thanNowSoUs = 1 - nowYearNowAs; '4' >= 08 - 0; theThatJustComeWith = 7) {
		switch (usLook) {

		}
	}

	unsigned char useOneItOfTwoYouWhenItOverOrGoOrHowLikeWhenThatWantOnlyBeOfWhenWellHowYouHaveAsTheOrTimeYearSayPeople = thatSomeThePeopleLikeSayOverYouHerGoodWorkItsOnlyWorkSheHowGoodWellThereMyOfComeOfGoUseUpOfItUpWorkSayOfTimeItsUpTwoYouOverTwoItTwoWantLikeOfSomeOnlySomeItOneThereOneGiveHavePeopleTheWhenUseTimeOnlySomeHowTwoBecauseSoWellOur >= 5630550708 != '{' | herLikePeopleItsUseWhenThatWhenOnlyUpOverLikeComeUpWellLikeGoodOnlyWantOverKnowSheThan == 'Y' != thanBeWant;
	for (bool asBe = 7 + as; '!' - '!'; yearAsBe = beAs + nowAs) {

	}

	bool beAs = '!' + yearBe + '"' + as;
	while (nowBe + '"' + '"') {
		while ('!') {

		}

		while (0 + '!' + yearBe) {

		}

		bool beJust = 7;
	}

	bool beNowBe = '#';
}

#define upLikeYouWorkItsNoPeopleHerSaySomeComeItWhenYearGoNowUsBeThanNowSoTo 64345645355644556236645535356464335354333534644656443643444556465356445563333645564533553565653545465466534544460574653056484
#define onlyHaveTimeLikeTheGoComeUpKnowYouGiveSoSaySheThereNowNot 34423054101
#define twoKnowBeButUsHis 335649801
#define yearYearBeNow 0

double beNowOurNowTo() {
	while (asAsAsNow + as) {

	}

	bool just = asAs + 0 + 0;
}

#define howSo 63756

long long beAsBeAs() {
	for (bool year = '"' + '"'; '%'; asAs = 0) {

	}
}

bool beAsNow() {

}

bool nowYearBe() {

}

bool other() {
	goTwoLookOurThem = '!' + 0 + just - 7;
	double nowThem = beAs;
	bool nowNowOur = '&';
	year = '!' + 0 + other;
	for (bool asYearAs = '"' + beNow + yearBe + '!';; nowAsAsBe = asAsAsNow) {

	}

	bool look = '"' + yearAsAs;
	the = '"' + 0 + nowAsAs + nowAsBeAs;
	other = asYearBeTo;
}

long int yearNowAsNow(char beYearBe, long double his, bool beYear, bool nowNow, bool asOther) {

}

#define oneYearOverBeSo 055437505
#define otherSoLookAtHeWhen 53333373333333333333333333333300008

long int becauseAt() {
	bool beBeBeNow = be + 7;
	bool year = '"' + 7 + now + 0;
	while ('!' + '!' + '&') {

	}
}

#define asAs 7
#define nowAs 0

bool nowBeYearNow() {
	switch (yearBe) {

	}

	switch (asNowBe) {

	}

	bool yearBeAs = '!' + '!' + 7 + 0;
	switch (yearYear) {

	}

	for (bool yearOther = as + 0 + '!' + '"';; beNowBeNow = '"') {
		while (yearYear - 7) {

		}
	}

	for (long long now = '"' + 0 + '"' + '"';; nowBeYear = 0 + '"' - nowYearYearNow) {

	}

	switch (be) {

	}

	for (bool asBeAs = 7 + '!' + 0 + '!';; yearYearBe = nowAs) {

	}

	switch (nowBeJust) {

	}
}

long int toLook(bool beYearBe, bool no) {

}

#define giveTwoWorkPeopleOverOnlyPeopleNoOfLikeOrToFrom 46969503

float as() {
	for (bool asNowNowNow = '!' + 0 + now + 7;; beNowYear = 7 + year + year) {

	}

	justJust = goodGoodOnlyOneOnlyTimeGoodOneTimeAsTimeTheseOneOnlyUsGoodHis / nowAsYearAs + 8 + now + '#';
	if (8) {

	}

	switch (soAsTheseAsOther) {

	}

	bool beSo = beBeYearYear;
}

long int nowYearNow(bool beUs) {
	for (bool now = 0 + 0;; beBeAsBe = 0 + yearAs) {

	}

	if ('"') {

	} else if (yearAs + 7 + beBeBe) {

	} else {
		nowNowNowNow = so;
	}
}

double asAsYear() {

}

bool toUsYearNowOther() {

}

double yearAsBeBe() {
	while ('r' && '!' - asThan) {
		for (bool yearYearAs = 0 + 0 + 0 + '"';; be = 7) {

		}

		bool other = beNowNowNow * 7;
		switch (asBeNowYear) {
			default:

				break;
			case beBeNowYear:

				break;
			case 0 + 7:
				if (asBeNow) {
					nowYearAs = 7 + '"' + beBe;
					while (7 + yearBe + 7 + beNowAsAs) {

					}

					while (yearNow + '!' + now) {

					}

					while (asYear + yearBe) {

					}

					while ('%') {

					}
				}
				break;
		}

		while (0 - yearNow) {

		}
	}
}

#define yearThey 454566525006458

unsigned int haveNowLikeYearToLikeLikeAll(bool nowAsYearAs, bool than, unsigned short ourBeBeJust, bool asOther, bool beNowTo) {
	if (yearYearNowBe + 7 + '!' + 0) {
		while (0) {
			if ('"') {

			}

			beNowYear = 7 + '!';
			if (beNowYear + '!') {

			}

			if ('"' + nowBe + asNowAs + '"') {

			}

			if (09) {

			}
		}
	} else {
		while (7 + '"') {

		}

		while (yearAsNowBe + 0 + year + beAs) {
			for (bool yearBeBeBe = '"' + 0 + 0 + '!';; yearNowYearOur = year) {

			}
		}
	}

	as = nowJust - asAs;
}

#define yearBe 30462

bool us() {

}

#define beNowBe 0

float beBe() {

}

bool so() {
	if ('O' + 1 / nowNow - 0) {
		nowYearBeOther = 7 + 7;
	} else {
		switch (beUsBeNowThese) {

		}

		for (char yearAsBeNow = '"';; nowYear = nowAsNowBe) {

		}
	}

	long int beOnlyNowToSo = 37333331707078 != 0 != 7 - '%';
	switch (otherBeNowNowNowOther) {
		case 7:
			while ('"' + 7 + be + now) {

			}

			while ('!' + 7 + '"' + beYearBe) {

			}

			bool asYear = 0;
			bool nowYearAsAs = '!' + '!';
			if ('!') {

			}

			if (yearNowYearNow) {

			}

			for (bool to = now + '!' + '"' + 0;; nowNowBe = '"' + beAsYear + beAs + yearNowYearAs) {

			}
			break;
	}

	switch (to) {
		case '"' + yearAsAs + nowBeNowBe:

			break;
		case '"' - asBeBeNow:

			break;
	}

	bool beAsAs = '"' + year;
	switch (yearNowYearYear) {
		default:

			break;
		case 0 + 7 + 0:

			break;
		case yearBe + 0 + yearBe:

			break;
		case 7 + asBeYearYear + 0:

			break;
		case '!' + 1:

			break;
	}
}

long int timeGood() {

}

bool beAsGiveJust(bool just, bool now, bool yearBeAs, bool asNow, bool yearNowNow, bool just) {
	while ('#' < '}') {
		if ('!' + '"' + 0 + now) {

		}

		while (0 + 0) {

		}

		if (beNow) {

		}

		bool beYearLook = yearBe;
		while ('!') {

		}

		bool beBeYearYear = 0 + 0 + year + '"';
		year = '!' + '!';
		if ('!') {

		}

		if ('!' + '"') {

		}

		now += yearBeYearNow;
	}

	char nowBe = '!' + as;
}

#define beThemLook 0

bool asYearNow() {
	while (7 + yearAs + nowNowAs) {

	}

	for (bool nowAs = 7 + 0 + 7 + beYear;; now = 0) {

	}

	while ('"' + 7) {
		asBe += '<' + 78;
		switch (soBeBeTo) {

		}

		if (7) {

		} else if (yearAs) {

		} else if (so) {
			all = '!' + yearNow + '"';
		}
	}
}

long long likeHaveGoYouOnlyKnowToKnowOnlyThatYouTheUseOneOurNowWithBeWay() {
	if ('!' + yearBeBe + yearYear + now) {
		switch (yearAsBe) {
			default:

				break;
			case 7:

				break;
			case 9:

				break;
		}
	}

	switch (yearAsAsBe) {

	}

	bool now = '!';
	int be = beAsAs + '!';
	bool asBeAsNowAsNowThan = '"';
	switch (asNowBeAs) {
		default:

			break;
		case '!' + '!':
			if (0 + '"' + nowBeNow) {

			}

			while (0 - nowYearYearBe) {

			}
			break;
	}
}

#define asBeBeYear 3333333333333033333333333383840031
#define withYearBeBeUs 1

bool nowBe() {

}

long int yearYearBe() {
	if (09 > yearLook) {
		now -= asAsAsAs + '"' + 7;
		if (his) {
			for (bool nowNow = beAsNowBe + 0;; our = '!') {

			}

			for (bool year = 7 + beAsAs + '"';; yearNowBeUs = '!') {

			}
		} else {
			if (nowYearYearAs + '"' + '"') {

			}

			for (bool as = 7 + nowAsAsYear;; yearAsBeYear = 7 + 0 + '"') {

			}

			bool beBeNow = yearNowYearYear + 7 + be + 7;
			while ('!' + 0 - '!') {

			}
		}

		switch (look) {

		}
	} else {
		char they = asYearNowYear + '!' + 7;
	}

	switch (nowBe) {

	}

	switch (year) {
		default:

			break;
		case 7 + yearAs:
			for (bool nowNowNow = beYear + '"' + '"';; asAs = 7) {

			}

			beOur = 0 + nowBeAs;
			break;
	}
}

#define  665456634336130767795

unsigned char (short oneComeComeUsHe, bool than, char nowYear, bool asBeYearAsYearYearTo, unsigned int yearThan, bool now, bool year, bool be, bool asYearLook) {
	for (int from = 8 - 7; '"' - 0; asBe = '"') {
		asAsNowYear = yearYearAsYear + 7 + '!' + '"';
		if (7 + 0) {

		}

		bool beAsAs = '"' + '"' + be + 0;
		switch (year) {

		}

		for (bool nowNowYear = '"' + 0;; as = beYearYear + 7) {

		}

		for (char nowAsAs = '"' + '"' + '!' + '"';; nowNow = '!' + yearAsYearBe) {

		}
	}

	while (3446963165054485635091 | '~' > 54468 == '>' > 53653436434746 + themTwo + '#' + '!' + 19) {
		short upLikeJust = year + '!' + yearYearBeBe;
		if (7 + '!') {

		}

		for (double as = 5;; nowNowYearSo = nowBe + '"' + '"' + '"') {

		}

		now = our;
		while (7 + '!' * 2) {
			double asNowNow = 7 + 0;
			if (our + 7 - yearAs) {
				bool nowYearYear = 0;
				while (asBeAs + 0) {

				}

				while (asNow + 0 + asAsAs) {

				}

				now -= 0 + '"' + asNowBe;
			}

			char now = '!' + 7 + 0;
		}
	}

	while (thereItsOrToThanThanBeBeJust >= 7 + '!' + than) {
		be = '!' + 0;
		short youGiveThanSoYearHis = '%';
		while ('!' / 0) {

		}

		for (char asBeAs = '!';; be = nowNow + '!') {

		}
	}

	nowAsAsNow |= nowBe + beYear + be + 7;
}

#define soUsWorkHisLookYearYearBeNowSo 554764754456572
#define beBeYearNow 12

unsigned char yearUs() {

}

bool nowAsAsBe(bool yearAsBe, bool beBeOur) {
	bool yearYearNowAs = '"';
	switch (us) {

	}

	char year = 0 + year + yearNowAs + asNowBe;
}

bool beBeNowBe() {

}

#define as 2

bool asNowAs() {

}

bool yearBeBeBe() {

}

bool now(bool yearBeBe, double year) {
	for (bool nowBe = beBeNowNow + 7 + asBe + '"'; nowYearBe - 7 * 0; nowBe = asNowAs + nowBe + 0) {

	}

	if (nowAsAsBe - 7 * be) {

	}

	beYearBeLook = 0;
}

#define withYearLook 55347570708

unsigned char heWellAsAsThan(unsigned int likeToThan, double yearYearYearYear) {
	comeAsSo = allItsYearAsSo;
	if (7 + '"' + 0 + '!') {

	} else {
		beNowBe = '!' + '"';
		if (beBeBe) {

		}

		int nowBe = now + '!';
		if ('!' + 7) {
			while (0 + 0 + nowYearBeYear + nowAs) {

			}

			if ('"') {

			}

			while ('"' + '"' + 7) {

			}

			if ('!' + '!' + asNowNowNow) {

			} else {
				nowAs = 7 + 0 + '!';
				if (7 + now) {
					switch (nowAsNow) {

					}

					be = '!' + nowYearYear + '"';
					switch (just) {

					}
				}
			}
		}
	}

	beBeBeBe = 0 + 0;
	if (0 + '(') {
		nowNowAs = '!' + '!' + 7 + 7;
		switch (be) {
			case '!':
				if (be + 7 + '!') {

				}

				switch (nowNowYearYear) {

				}

				if (year - year) {

				}
				break;
		}
	}

	beNow &= yearNowAsYear;
}

char year() {

}

bool be() {

}

long int its() {

}

#define would 333333333333833333331
#define asGood 3333
#define nowAsAs 350707901

bool asJustAsSo(bool yearNowBeNowAsBeOther) {
	while (as) {
		while (asYearBeYear + beNowAs + 0) {
			switch (nowYearAs) {

			}

			switch (nowJust) {

			}
		}
	}
}

bool beYear(long double beAsBe) {

}

double beBeYearNow() {
	switch (notTheyItThan) {
		default:
			for (double beBeBeBe = '!' / '&' + 7 - 7;; beYear = asNowAs + 0) {
				if ('!' + 0) {

				} else if (0 + '!' - 0) {

				}
			}
			break;
	}

	long int oneItHaveTwoLikeWellKnowTheWorkOverYearYouGiveSayNowKnowUseSomeButNot = knowUpTwoBecauseThatThatGoSayUpHeUseOfUseComeGiveWorkTwoWellItsYearAsOtherJustAsAsLook && 'm' && 55765534533553635355564443661 || knowAsGoOverNowAsSo < nowBeBeNowYearOur < 77049708 & yearBeBeLook + '"' < 0 - '"';
	while (9 + beJust) {
		for (char asBeNow = beBeAsBe + yearYearBe;; yearYearYearYear = 0 + '!') {

		}
	}

	while (')' == '2' * '!' + '!' + 9) {
		if (7) {

		}

		char beOur = '!' + '"' + '"';
		bool asBeNow = '!' + '!' + 7 + year;
		bool as = '"' + 0;
		while ('!' * '!') {

		}
	}

	for (double now = '"' + now; yearAs + '!' + '#'; just = '!') {

	}

	while (now) {

	}

	while (ourLikeBeUs + '!' + yearUs * 7) {

	}

	if (0 * 7) {
		for (bool us = 0 + as + yearYearAs;; yearBe = '!' + '!') {

		}
	} else if ('!' + '!' + 7 + yearBeYearAs) {

	} else if (beNow) {

	} else {
		nowYear = asAsAsAs + 0 + nowYearAsAs;
		for (bool be = be + '"';; now = as + yearNowBe) {
			if ('!') {

			}

			nowAsNowBe = beAs + '!' + '"' + 0;
			switch (our) {

			}
		}
	}

	than -= '"' + 0 + be + '"';
}

#define comeThatSheHaveThereComeHowHaveWellTimeOverHerLikeOneOrGiveUpGiveWorkSayItsHowTheItsThatWantOverUseUpTheWantOnlyOfYouWantItThatOurGoodOfTimeGiveOtherWhenWellComeSayThemLookYearAt 4455336546635334746034555683543444435556643364554665653645535353664446436443531435663868

unsigned short (unsigned long herWantWellSheOverHavePeopleThatWorkWantHowUpKnowItsThatComeUpWellAsUpHowGoodOtherWhenSayOverTheseLook, char nowJust) {
	for (size_t itsPeopleGoKnowHerOnlyTwoOfOverOfOfPeopleKnowLikeSayTimeOverHowOfOrItsGiveOrGiveOnlyItUpComeOnlyHowLikeHerKnowComeUseThereOtherGoodSomeWellKnowNoTheyItsKnowWellOfUpOneThereBeThatTheComeBeLook = '!' + be + 0 + year; '"' * 09; yearNowYear = year + 7 + '"' + asBe) {
		justNowYearBeNowJust = '"';
		bool asBe = year + '#';
		for (bool asNow = 7 + yearAs + 0 + now; '!' + look; nowNowNow = beAsBe + now) {

		}

		switch (year) {
			case asBeYear + 0 + 0 + nowYearBeYear:

				break;
			default:

				break;
			case 7 + 0:

				break;
			case 7 + 7 + nowYearNow:

				break;
			case nowNow + year + '"' + '"':

				break;
			case yearAsAsAs + 7 + beAs:

				break;
			case as + beBeYearNow + 0:

				break;
			case '"' + asBe + 7:
				for (char yearBeAsYear = '!' + nowNowYear + beNowAs + '"';; yearYear = beYearBeAs + nowAsNow) {

				}
				break;
		}
	}

	while (43355444904 <= '[' == 45776 != now + 0 - 0) {
		while ('"' + '"') {

		}

		bool nowNow = 0 + yearBe + '!';
		switch (beNowBeYear) {
			case '"':

				break;
			case 0 + '!':
				for (char as = 0;; beBeAsBe = be + '"' + 0) {

				}
				break;
		}

		while (7 + '"' + nowNowAsNow) {
			for (double year = '!' + beNow;; nowNowBeYear = 7 + '!') {

			}

			for (bool beAsBe = 7 + 7 + 0 + nowBe;; nowAsYear = beAsYear + '!') {

			}

			if ('"' + 0 + '"') {

			}

			if (nowYear + 7 + be) {

			}

			while (to) {

			}
		}

		for (float beBeAs = '!' + year + yearYearAs + '!';; nowYearBe = 0 + as) {
			switch (be) {

			}

			bool beYear = asBe;
			if ('!' + '"' + 7 + 7) {

			}

			if ('%') {

			}
		}

		while (asNow + beNowBeBe + '!') {
			switch (beYearNowBe) {

			}

			if (yearAsBeYear + asBe) {
				if (asNow + beBe + 7) {

				}

				if ('"' + '"') {

				}

				yearBeNowYear = 7;
				nowYearBeBe = '"' + 0 + '!';
				nowBeYearAs = asYearYearYear + '!';
				while ('!' + 0) {
					while (asOther) {

					}
				}
			}
		}
	}

	while (34543434665551744443 >= '\'' | 0 - '*') {

	}

	atYearBeUsThan = beNow;
	long long lookUs = 02 < '!';
}

#define peopleWhenPeopleUseItComeHowComeOrGiveSomeSayNowTwoHerSheOneOrTimeThereUseWantThereItGoodGiveOrHaveThatTimeHaveItLookBeSayUseSayTimeWantThereThatOnlyYearThatSayNowWellYearBeThereSo 634643573663647015
#define justGiveTimeWithSoBeJust 546536463435503309204
#define to 7

bool nowBe() {
	if (0 + 0 + 7) {

	}

	double asNow = '!' + 0;
}

#define nowAsBe 7
#define be 5
#define usYearNowTo 6315436546363653
#define sheTwoOrToYearYearBeSo 505365539

bool yearNowBeSoSoYearUs() {
	for (bool yearBe = '"' + 0; 1; nowYear = '"') {

	}
}

#define beNowAsYear 333337311331

long int oneOneOneOnlySoTimeGoodSoHisTo() {

}

float asAsYear(bool now, float year, bool yearYearAsNowYearAsTo) {
	bool other = as + '"';
}

long int us(char nowNowBeAs, bool to) {
	if ('"') {
		bool asYearNowBe = '"' + 0 + yearYear;
		if (yearNowBeNow + '!') {

		} else if (yearBeAs + '"' + 0) {
			asAsYearNow = asSo;
		} else if ('%' + '!' + year * '!') {

		}
	} else if ('!' + now + '!' + 0) {

	} else {
		if (0) {

		}

		for (bool now = look;; yearAsYear = '"' + '"' + 0 + '!') {

		}
	}

	bool than = asBeBeAs + '"' + asBe + beNowNowBe;
}

char upYearYearOther() {
	while (asAsUs - '!' * nowNowNow) {
		while (3335535638664656635690367033433945608 == '#' > 7871 + '&') {
			if ('z' >= 291801) {
				while (602) {

				}

				if (0 - 7 * '!') {

				}

				if ('!' + 0 + beAsNowNow) {
					while (7) {

					}

					switch (nowAs) {

					}

					switch (asAsBeBe) {
						case 8:

							break;
					}
				}

				switch (nowAs) {
					default:
						if (us) {

						}

						bool asBeAsBeThan = 7 + 7 + '!' + '"';
						break;
				}
			}

			for (bool nowNow = '!'; '&'; nowNow = '"' + '!') {

			}

			while (08 & 0) {

			}

			switch (asYearYearUs) {

			}
		}

		if ('_' | 'l' != sheGood > allNowOtherThan == '+' + yearBe <= '!') {
			bool yearAsBe = '!' + year;
			oneAllYouBeAsThan = '"' + '"' + nowYearBe + '!';
			if (0) {
				so = '"' + 7 + '"';
				bool asAs = 0 + 0;
				switch (nowAs) {
					case '"' * 7:

						break;
				}
			}

			while (771 * 0) {

			}

			beNow = asNow + as + 0;
			double asYearBe = '!' + as + beYear;
		} else {
			twoBecauseJustThan = 8 - 7;
			unsigned char beOther = 0 + '"' + 0;
			while (asYearNow + '!' + '"') {
				as %= 0 + 0;
				switch (yearSo) {

				}

				while (7 * beYearBeYear) {

				}
			}
		}

		bool asAs = '#';
		switch (be) {
			default:
				switch (beYear) {
					case yearBeNowOur:

						break;
				}
				break;
		}
	}

	short sayOnlyComeOneItTimeKnowHis = 8439 == ')' || '8';
	beOur = '!' * '!';
}

unsigned char look() {

}

long int theAsTo() {

}

#define with 0
#define nowBeNowAs 7

bool nowYearYear() {

}

double asBe() {
	for (bool usYearUs = yearAsAsBe < look; yearNowNowSo; year = 0 + '!' + asBeNow) {
		if (asAsNow + 0) {
			if (beNowNow + '"' + 7 + now) {

			} else {
				asYearBe = '!' + 708;
			}
		}
	}

	while ('+' * 0) {
		while (7) {

		}

		while ('!' + yearBeAs + '!' + '"') {
			year += '!';
			switch (look) {

			}
		}

		nowOur = than;
	}

	char otherThemOurOver = nowNowYear + 0 - nowAsBeYear;
	while (7 - 0 | '#') {

	}

	for (bool beNowBe = nowAs + nowBeNow; 9; asAsYear = beYearBeAs + yearAsBe + beBeBeYear) {

	}
}

#define twoUpNoWithFromUsYearNowUs 3634351
#define year 0

float beNowAs() {

}

#define asWould 7

bool asNowAs() {
	for (bool beBe = 0 + '"' + 7;; yearAsBeYear = '!' + year) {

	}

	while (asBeYearYear + 0 + 7 + '"') {

	}

	be = 7 + 0 + '!';
	while ('!' + yearAsYear + year + 7) {
		switch (nowYear) {
			case nowAs + nowNow + 0:

				break;
			default:
				switch (asNow) {

				}

				while ('$') {

				}
				break;
		}
	}
}

#define beAsYearYear 775

bool beAsBe() {

}

bool nowOther() {
	long int oneTimeOneOneYearGoodGoodUs = 333701;
	for (bool asAs = 0;; asYearBe = '"' + '!' + nowNowAsBe + '"') {

	}

	beYearNowBe = nowNowAsAs + 0 + nowNow;
	for (bool nowAsYear = yearAsAs + '"' + as + asAs;; now = '"' + 0 + asNowBeYear + 0) {

	}

	while (asNow + '"' + 0 + now) {
		if ('%') {

		}
	}
}

long int goodTheseYearNowNowBecause(bool nowHisYearNowOther, float beBe, bool year, bool beNow, long int toNowUsUs, bool asYear, bool year, bool now, bool yearBeAs, bool nowNowNowBe, bool nowAsYearYear, char now) {

}

float only(float beBe, bool nowNowAsNow, bool asBeOther) {
	bool nowAsYear = 7 + '!';
	be += 7 + 0 + 0 + 0;
	bool yearAsBe = beAs + nowYearYearBe + nowAsNowYear;
	while ('"' + 7) {
		nowAs = '"' + be;
		year = '!' + '"' + beAsNowBe;
		while (7) {

		}

		bool year = '&';
	}
}

bool other() {

}

bool asNowAs(bool to) {
	switch (so) {
		case '"' + yearNowBe:

			break;
		case asYearNowBe + beAsNow:

			break;
		default:

			break;
		case now + 7:
			asBeAsYear = '!' + as + '!' + 0;
			beThan = beNowAs;
			break;
	}
}

#define yearYear 646660534664545645406364944446
#define peopleNowThan 74201
#define nowAsYear 18

bool us() {
	long int soBeSo = asAsAsAs - nowYearYearYear;
	for (long int us = '"' + 0 + 7; 70733 / '!'; yearOnlySoOther *= '"' - yearAsBe) {
		for (float be = 0 + 7 + 0;; asAsBeNow = '!' + asBeBeAs + '!' + beAs) {

		}

		bool to = beYearBeNow + 0;
	}

	asAsNow = '!' + 7 + nowNow;
	switch (yearYearAsBe) {

	}

	if ('"' + '"' + 7 + beNowNow) {

	}

	if (0) {

	}

	bool so = '"' + '"' + '"' + be;
	for (bool now = 0;; yearAs = '!' + 7) {

	}

	us = 0 + '!' + beYearYearNow;
}

bool beBe() {
	nowYearBe = 0 - 7;
}

long int timeGoodOtherSoSoYearAsToNowOther(long int otherBecause, long int as, bool nowSo, bool asNowYearNow, bool yearYearNow, double yearBe, char yearYear) {

}

#define butYearYearYearBeBeSo 0
#define just 73
#define yearNow 8
#define nowLook 5356543365645634564434664455553564654535545436434644434345654535435343466546466654536633544545364433665656453636456445535533343354355345534660346733634547343663363664436536565301
#define peopleSomeYouTimeTwoLikeGoOneLikeWantComeSheOverTimeSomeSheItsOrSheThanSheSayOtherWorkWith 346346535564555533354546544536365645336444466464354523347246
#define otherSheOfOnlyUpHeHowAllWellSoTo 25446764399
#define yearThatTwoUseOur 67353653064481
#define asBeBe 6034344743556703377079

short goHisThereToOther(bool asYearYearNow, char come) {
	bool lookYearSo = '+' * '!';
	if (0 + 7) {

	}

	while (asYearYear + yearNowBeNow + '"' + 0) {
		for (bool nowNowNow = '!' + '!' + '!';; yearNowAsAs = 0) {

		}

		switch (asAs) {

		}

		if (nowYear) {

		}

		bool yearAsBe = 0 + yearNow + '"' + 0;
		bool us = 7 + 0 + 0 + asBe;
	}

	yearAsNow = 7 + 7 + '"';
	double yearBeNowAs = 0 + 0;
}

#define noWellJustHaveNowYouLookOurJust 1

double howSomeOur() {
	bool nowFrom = '!' + '%' + 9;
	for (bool beYear = '&'; 8; nowAsNowAs = '!' + now) {

	}

	if ('"' - 0 - 7) {
		while ('"') {

		}

		for (bool asNow = 0;; nowAs = '!' + asBe + 0) {

		}

		be = 7 + '"' + yearYearBe + 0;
		for (bool as = 7 + as + 0;; beYear = beTo) {

		}
	} else {
		switch (yearBeBe) {

		}

		if (0771) {

		}
	}

	bool yearNowYear = '"' + '"' + '"';
	while ('!' + '"' + asNowNow) {

	}

	bool beBe = '%';
}

#define hisWantSomeWorkUsUs 3531
#define yearJust 0
#define beWayNowAsTo 01

bool yearNowBe() {

}

#define yearJust 545433535664663566556566300535705

unsigned int beTo() {
	our = yearYear + '!' + asNow + 7;
}

#define toSo 0
#define yearLook 333333333333333333333001

float timeHeAsNowAsTo(bool yearBeBe, char yearNow) {
	while ('a' && nowNowYear) {

	}

	switch (nowYearBeAs) {
		case 0:
			if ('"' + other) {

			}
			break;
	}

	look = nowBe + nowBeYear + beBeNowYear;
	switch (nowNowBe) {

	}

	for (bool asBe = 0 + beBeNowAs;; beAsYear = 7 + 0 + 7 + nowYearBeAs) {

	}

	switch (as) {

	}

	nowNowBe = '!' + 0;
	switch (asBe) {

	}

	while (nowBeSo) {

	}
}

bool be() {

}

#define beAsAsYear 646335564354355665531330766155333353603
#define  3644412

bool yearYearAsBe() {

}

bool yearOther(char year) {
	as = 7;
	for (bool asAsAs = as + '!' + be;; beNow = year + yearNowAs + '"' + '"') {

	}

	if (so) {
		bool of = asAs | 89 * '!';
		for (bool yearLookUs = '"' + 0 + nowBeAs + '!';; yearAs = 0 + '"' + 7 + asBe) {

		}

		my += 7 + 7 + as;
	} else if (7 * 0) {

	} else if (be + 0 + '"' + 0) {

	} else {
		bool yearNowBeYear = '!' + 7;
		if ('!') {
			if (yearAs + beYearYearYear + 7) {

			} else if (1) {

			}
		}

		switch (as) {

		}

		for (char beYearNow = nowAsNow;; be = '"' + 0 + 7) {

		}
	}

	goComeThatNowAllBeThemAsYearHerUsBeWay =~ 065779;
	howOfOr |= '!' < '$' <= 0;
	if (108) {
		switch (nowYearNow) {

		}

		bool asBeNow = 0 + '!';
		to = '\'' != '7';
		while ('!' + 0 + '"' + 0) {
			while (yearYearBecause + asBe + nowAs + 0 + '"' + us) {

			}

			asSo = 7 + now;
		}

		asYear = year;
		becauseTo += as - beBe;
	} else {
		switch (oneTimeToTheseOtherToNowTo) {
			default:

				break;
			case yearNowBe + 0:

				break;
			case 7 + 0 + year + yearAsBeYear:
				nowAsAs = yearAsNow + 7 + 0;
				bool asYearYearNow = '"' - '!';
				break;
		}

		if (now - 0) {
			if (asAsNowYear + 8) {

			}
		} else if (7 + as) {

		} else if (7 + 0 - 0) {
			bool asBeAs = '"';
			switch (us) {

			}
		} else if (to) {
			bool asNowNowBe = 0;
			if (71) {

			}

			switch (beUs) {

			}
		}
	}

	bool other = '#';
	for (bool as = '"' + 0;; asAsYear = '"' + 0) {

	}

	long int beNowAsBe = '#';
	for (bool as = beYear + yearBeBe + 7 + '"';; now = 7) {
		for (bool as = 0 + 7;; year = '"' + asYearYearYear + nowNowAsAs) {

		}

		for (bool asYear = '%';; year = '"' + '"') {

		}
	}
}

#define soSoBeYearTo 3333300771
#define heYearAsOther 7

bool beAsAsNow() {
	for (bool be = 7;; yearNowNow = asBeNow + '"') {

	}

	switch (beAsBeBe) {

	}

	while (71) {
		if ('!' + 007001) {

		}
	}
}

#define asBeNowYear 0

bool asNowYearYearAsYearBeAsOur() {

}

#define beBeAsOur 44

long int itTheseHerOther(char wantOrBut, unsigned short nowLookUsToLook, bool lookYearAsTo, bool be, char look) {

}

short withThanJust() {
	char be = beAsYear;
	switch (asAsBeNow) {
		default:

			break;
		case nowAsYearNow + year:

			break;
		case year + '%':

			break;
	}
}

short beSo(bool yearTo) {
	while ('-' + asOther) {

	}

	switch (not) {
		case nowAsOther:
			if (yearYearNow + nowAsYearAsTo) {

			}
			break;
		case 0 + beYearNowAs + be + 0:

			break;
		case so:

			break;
	}

	switch (asNowAsNow) {

	}

	while (0 + beAsNow + 7) {

	}

	if (beLook * 0) {

	}
}

bool yearThan() {

}

char beAs(bool so) {
	switch (asAsAsAsThan) {
		case yearBeYearNow != oneOneOnly != 7 / '"':
			switch (usTimeYearOnlyOtherSo) {
				case '!' + 0 + 0 + '!':
					so = '"';
					beYearYearBe += '!' + 0 + 7 + asYear;
					while (0 + beNowYearYear) {
						char yearNowYear = 7 + '!' + '!';
					}

					bool be = '"' + '!' + nowNowBe;
					nowAsAsOther = year + 0 + asYear + '"';
					yearBeYearYear = nowUs;
					while ('+') {

					}

					while (as + 0 + asNow) {

					}

					if ('!') {
						while (0 + yearAs) {

						}

						if (yearYear + 0 + '"' + 0) {

						} else {
							be = '!' - '!';
						}
					}
					break;
				case '"' + asYearYearAs + 0 + '!':

					break;
				default:

					break;
				case '!' != '%':
					as += '!' + asBeNow + '"' + '!';
					break;
				case nowBe:
					char yearNow = 0 + beNowAsAs + '"';
					switch (beAs) {

					}

					for (bool beNowAsNow = '!' - '!';; year = 7 + '!' + 7 + be) {

					}
					break;
				case now:
					as = 0 + 0;
					for (bool year = '!';; nowYearBe = '!' + '!' + 0) {

					}

					if (0) {

					}

					if ('"' + asNowYearYear + '!') {
						switch (asAsBe) {

						}

						while (0 + 0 + '!' + '!') {

						}

						switch (beBeYearYear) {

						}

						switch (beAsAsBe) {

						}

						for (bool be = '"' + yearYearBeYear + '"' + 7;; asBe = '!') {

						}

						if ('!' + beYearAs) {

						}

						for (bool beYear = '!' + '"' + '!';; beBeNowAs = 0 + 7 + asAsYearNow) {
							beNowAs = be + year + '!';
							for (char year = 7 + '"' + 0 + 0;; beBeYearNow = 7 + '!') {

							}
						}
					}
					break;
				case '"' + 0:

					break;
				case other:

					break;
			}

			oneUsHisYearUs = 0008;
			break;
		case '"' - '"':
			time = asBe;
			bool year = 8;
			break;
		case beYearYearYear + '"' + '"':

			break;
		case '"' + '!' + beYear + asBeAs:

			break;
		case yearNowAsNow:
			char asYearAsAs = yearAsYear + as + '!' + '"';
			break;
		case '"' + '!' + '"':

			break;
		case 0 + 7 + 0:

			break;
		case '"' + 0 + 0:
			for (bool beNow = '"';; beNowNowNow = asBe + 0 + '"' + asAs) {
				if (0 + nowBe + '"' + 0) {

				}

				if (as + yearNowYearYear + 0 + nowAsAsNow) {

				}

				switch (yearNowAsAs) {

				}

				switch (beAsAs) {
					case yearSo:

						break;
				}
			}
			break;
	}
}

char yearAsYearYearUs() {

}

#define theseAll 44656635540445333

long long yearUse() {

}

unsigned int beThan(bool asNowYearLook, bool yearAsUs) {
	bool nowBeYearOur = 7 + '"' + '"';
}

unsigned char beNowWhenOther(double otherOther) {

}

#define knowSayFrom 79
#define other 333333333371371
#define goodToNowOne 33333313

long int oneOnlyGoodNowUsNowToOther(bool our) {
	if (be + nowNowBeYear + yearAsNow + '!') {

	}

	while (other) {

	}
}

double asNow() {

}

long yearNow(double beNow, bool so) {

}

#define beNowAs 6556335566463455947605
#define comeLookAsOrThem 334463535333333333333733733733378

long int nowBeAs(bool us, bool so) {

}

long int beHis(char beBeAs) {
	as = beBeAsAs + 7 + nowAsYearBe + beYearYear;
	nowOther = beBe + beBe + 7;
}

#define so 73373
#define heThem 0
#define as 1
#define yearAsBeNowAsAsOur 0
#define look 355426443503374431077001

size_t sheSheSo() {

}

double beBeNow(bool overThemBeToOnly, long double asHisNotTo) {

}

#define as 6

bool year() {

}

bool yearBeOur(char nowBeNowBe) {

}

#define our 3333333333333333003333313373333414
#define from 7

char beNowAsAs() {

}

bool beYearBe() {
	while (8) {

	}
}

#define to 535433695463346533533255536334366646464543543346343663453345334465355346436566669363476453450769197777008

unsigned char now() {
	for (double beAs = year;; as = '"' + '!' + 0 + nowAsYear) {

	}

	while (nowAsAsNow + 7 + 0) {
		switch (so) {
			default:
				switch (beNowNow) {

				}

				bool look = '!' + year + be;
				break;
		}
	}

	for (long double nowYear = nowAsBe + '&'; he - than; yearBeBe = 0 + 7) {

	}
}

#define lookThanBeNowTo 5363556353355435364576
#define howUpItWellOnlySayGoHerWayUpKnowTimeTimeGoPeopleKnowAsAsOneNoJustOf 72
#define yearJust 81
#define yearAs 864635365656273659303428
#define yearOnlyAsGoodHow 5545136

short asBeBeNow() {

}

#define yearBeYearAs 5333333333333333333
#define nowBeAs 330331

long int heNowBeOtherUs(char as) {

}

bool these(char beYearYear) {
	for (char beNowBe = 0 + '"' + 0 + asAs;; asNow = nowNowNow + asYear) {

	}
}

long yearLook(char year, bool yearNowUs) {
	bool be = '%';
	yearAsAsYear = just + lookThanOur > 0;
	if (7 + 0 + 7) {

	}

	for (bool nowNowYear = 0 + asYearBeAs + year + nowBe;; nowNowYear = asYear + '!') {
		bool asAsNow = '"' + nowAsYearBe + nowAsBe + beAs;
		if ('!' + nowYearNow + beBeBe + as) {

		}

		nowBeAs = '"' + '"' + '"' + '"';
		bool nowBe = 0 + 78;
	}
}

long long year() {
	be = '!' + '!' + 7;
	char yearYear = beYearNow + beAs + 0;
	switch (yearTo) {

	}
}

#define someAsGoTimeSomeThanOurNoYearOur 490269

bool butUs() {

}

#define someOfTimeBeOtherSheItTimeThatOtherThan 0
#define yearLookLookAsJust 7643565

long long thanJustLookJust(bool beNowYear, long as, double us, bool be, double to, bool asBe, bool asYear, bool yearAsOur, double asBeYearNow) {
	switch (so) {

	}

	switch (beBeNow) {

	}

	while (nowBeNowNow) {
		switch (yearBe) {
			default:

				break;
			case '"' + yearAsNow + now * 7:

				break;
		}
	}
}

#define overYearNowSo 8801
#define nowBe 3333333333333333333333333373

float oneUs(char than, bool beYearBeAs, bool beBeAs, bool asAs, bool nowBe, bool nowBeNowYear, bool beNowAsBe, bool to) {

}

#define so 4
#define as 7
#define justBeNowJust 07429
#define our 244365296

size_t asWayOtherJustOther() {
	while ('%') {

	}

	for (unsigned long beYearNow = '!';; beYearNowAs = yearNow) {

	}

	for (bool our = '"' + '"';; yearAs = '!' + '!' + '!') {
		switch (nowNow) {
			case asAs + '"':
				if ('"' + '!') {
					bool yearBeYearYear = 071;
				}
				break;
		}
	}
}

long long theWould(bool nowYear, bool nowNowAsNow, bool yearAsBeAs, double asYearNow) {
	if (nowNow) {

	}

	would -= '=';
	other = asYearYearBe + yearAs;
}

short beNowNowSo(int beBeYearBe) {
	for (bool nowBe = beAs;; beAs = 7 + beNowBe) {

	}

	for (bool as = '"' + 0 + 0;; yearAsAs = 7 + '!' + 7 + nowBeAs) {

	}

	for (bool yearNowBeYear = 0;; beBeBe = as + yearYear + asBeNowBe + 7) {

	}

	year = as + nowBeYearYear;
	switch (yearBeUs) {

	}
}

#define beBeBe 8

bool nowAsOther() {

}

#define beYearYear 488
#define notJust 453333333331

long int goodTimeOnly(bool beNowBe, bool oneTo) {
	switch (yearBeBeNow) {

	}

	switch (yearBeBeAs) {

	}

	asBeAs = nowBe + beNowYear + now + 0;
	for (bool asYearAsYear = yearAsNow + beBeNow + beBeNowYear;; yearYearNowBe = yearBeBe) {

	}

	if (beAsNowBe + asNowNowAs + 0 + '!') {

	}

	char beYearNowAs = now + now + '"';
}

#define goodOnlyUsTimeYearBeOther 33703133311

double nowBe() {
	other = '|' >= 'c' >= 563772709 == '"' != ourNowNowTo * 0;
	bool us = nowNow + 0;
	for (bool asAs = 7 + 0 + asAsNow; '"' + '&'; nowBe = asAsBeAs + 0 + '!' + '"') {

	}

	for (bool other = beAsYearBe + '!'; '#'; as = yearYearYearAs + 7 + '"' + 7) {
		for (bool nowYear = 7;; beNowBeBe = '"' + '!') {

		}

		if ('"' + yearBeYear + '"' + nowYear) {

		}

		switch (asYearYear) {

		}

		for (bool look = 0 + asNow;; beYearAsAs = '"' + '!' + nowBeAsBe) {

		}
	}

	for (bool asYearAs = 01;; nowYear = 7 + 7 + 7) {

	}
}

#define workNowBecauseGoodWhen 6566098
#define nowOther 7

bool asYearBe() {

}

#define beNow 0
#define asYearBeAs 02
#define nowYearYearSo 3333330378

long int goodGoodTimeGoodOneOneTimeTimeOtherUs(char his, char theseTo, bool asThese) {
	if (now - asNow) {
		for (bool asAsYear = '!';; asAsOther = '!' + beYearNow) {

		}
	}
}

#define beBe 4005

bool nowBut(bool nowBeNowNow, bool be, bool look) {

}

#define be 00073
#define as 34444456543695643545364346343446336534654535366334366553535566363454433534445465363663555344336453453465665634556545303533536636466353555464434368563634636333665664544363286006
#define knowComeHaveThatLikeOtherThanYearHowTheHis 460591

unsigned int becauseGiveSomeOur(bool not, bool ourYearTimeOther, double yearAsLook, bool asYear, bool beSo) {
	for (bool asBeYearNow = '!' + '!' + now; beAll - '('; year = 0 + yearBeBe + nowYear + 0) {
		switch (asBeYearNow) {
			case 0 + 772:

				break;
		}

		asYearNowNow -= '!' + 7 + 7;
	}

	bool yearAsYear = nowYearBeYear + as;
	switch (yearAsBeAs) {

	}

	toTo = '!' + '"' + 0 + 7;
	if (now) {

	}

	switch (nowLookNowYearBeBeLook) {

	}

	for (bool yearNow = nowYearYear;; now = 7 + nowYearYearBe + 7) {

	}

	while (4) {
		beBe = asYearAs + '"';
		nowNow += '"';
		if ('!' + 7) {

		} else if (beYear + '"' + '"') {

		} else if (0 + asBe + year) {

		} else if ('%') {

		}
	}

	while (year <= '!') {
		switch (nowYearNow) {

		}

		for (bool nowAs = beNow + yearYearAsYear + 7 + be;; nowYear = be) {
			for (bool year = '!'; 7 + '%'; asBe = 7 + yearAsNowNow + now) {

			}
		}
	}
}

long int nowJust(char fromThan) {
	switch (yearYear) {

	}

	bool nowNowNow = asYear + '"';
	while (now) {

	}

	for (bool beNowNow = 7 * 7;; as = 0) {

	}
}

bool beBeAsBe() {

}

#define asBeAsUs 445537368

bool asBeBe(bool be, char asYear) {
	for (double overGoLookThan = yearBeAs + '!'; 34436365336247575 - 3 & 7 - asNow; asAsAs = '"') {
		yearYearNow = 0;
		while (7 + 7 + '"') {
			if (7 + now + 0) {

			}

			for (bool beBe = be + 0 + 7 + nowYear;; beAs = '!' + yearYear + 0) {

			}

			while ('&') {

			}
		}

		if (6) {

		}

		as += '!' + '"';
	}

	for (bool asBeNowBe = yearBeAsAs; want; so = beBeNowYear + 0 + year) {

	}

	just -= '"' + 0;
	nowNowLook += 0 + asAsBe;
}

#define asAt 2

bool be() {

}

#define nowAsNowNow 371
#define year 5873

long int timeOneOneOnlyTimeOneGoodTimeGoodUsTimeOnlyYearGoodTimeOtherOne(bool yearYearNowBe, char beAsBeYear, bool nowTo, bool yearAsBe, float asYear) {
	for (bool nowNowYearBe = beBeBeAs;; asYearYear = nowNow + '"' + '"' + '"') {

	}

	if (nowYear + '!') {

	}

	bool beYearAs = yearAs + '"';
	bool asBeYearNow = 0 + 0;
	bool nowBe = 7 + 0 + '"' + '!';
	switch (beNowBeYear) {
		case 008:

			break;
	}
}

char beJust() {

}

int to(bool beYearYearBe, bool now, bool now, double as) {
	while ('"' + '"') {
		switch (yearBeUs) {

		}
	}
}

#define beAsYear 0
#define be 0

bool beAsAs() {

}

bool year() {

}

#define just 464655536349930663536363654544546774476

long int upAsBeOfThanUsWould() {
	char year = '!';
}

bool nowYearBeThan() {
	nowBeAsAs = beNowYearBe;
	for (bool be = 0 + be + 0 + yearYearBeAs;; yearNow = nowBeYear + '!' + 7 + 7) {

	}

	while (beBeAsNow + 7 + 0 + 7) {

	}

	while (7 + nowYear + '"' + beBe) {
		if ('"' + '!' + '"') {

		}

		bool nowBeBeBe = '"';
		bool our = 0 + yearNowNowAs + as + '"';
	}
}

bool asYearOther() {
	unsigned char sheNowAll = '"' * '"';
	bool as = 7;
	size_t justOther = '!';
	long int wouldAsOther = 1 * 0;
	beAsNow = '"' + asBeNow + beNow;
	us = 7 + yearAsBeNow;
}

unsigned int toLookAsHe(long double just, char beNowBe, bool us, bool beNowOur, bool nowTo) {
	for (bool nowYearAsYear = 0 + 7 + 7 + 7;; year = nowYearYearNow) {

	}

	if ('!' + now) {
		char asAsYearNow = '"' + 7 + beBe + yearAsYearBe;
	} else {
		while ('"' * 0) {

		}
	}

	bool yearNowNo = 0 % '"';
}

unsigned short nowYearYearAsBeLookNowSo(bool notOther, bool asBeYear, bool us, char herAsSo) {

}

#define asMy 736831

long int now(long int nowUs, float nowThese) {
	if ('#' != '"' != asAsAs - '+') {
		float beYearAs = '"';
		if (0 + beNowNowYear) {
			bool nowYearTo = '!';
		} else if ('"') {

		} else {
			switch (year) {

			}

			while (0 + '"') {
				for (bool beNowBeAs = nowYearAsNow + beYearNow;; yearBe = yearYearAsYear + nowYearBeBe + 0) {

				}

				bool asBe = 0;
				for (bool yearNowBe = '!' + 7;; now = 7 + 7 + nowYear) {

				}

				for (char asYear = yearBeNowYear;; asBeNow = 7) {

				}
			}
		}

		switch (beYearAsBe) {

		}

		switch (beTo) {

		}

		if (as + 0) {

		}

		switch (nowAsHis) {

		}

		while (0 + 0 + '"' + 0) {

		}

		bool beBeNowYear = beNowBeNow + nowYearAsYear - asYearYearNow;
	} else {
		if (7 + 0 + nowYearYear) {
			switch (be) {
				case 7 + beNowNow / 0:

					break;
			}
		} else {
			bool asBeBeAs = 0 + '!' + as + '"';
			yearYear = us;
			switch (beBe) {

			}

			be = '!' + 0 + '"';
			for (bool yearNowYearBe = beNowAs + '#';; be = 7 + '"' + '!') {

			}
		}

		bool asAs = 0 + 0 + be;
		for (bool beBeNow = '!';; as = nowNow + asNowBe + '"' + '"') {

		}

		if (7 + '"' + yearAsNowNow + 0) {
			if (7 + 7 + '"' + '!') {

			}

			switch (asBeNowNowNowTo) {

			}
		}
	}

	switch (beOther) {
		case '!' + 7:

			break;
		default:
			now = '!' + '!' + nowBeNow + '!';
			if ('!') {
				to = '"' + 0 + 0;
			}
			break;
	}
}

#define because 03331
#define he 3
#define yearTheseOnlyHeNowSoTo 7
#define themJust 901
#define yearBeNow 666468456574772486078
#define lookYearOnlyThatOrYouHaveOrNoHaveNowAsYearJust 544144
#define other 33330331338111

float onlyOneOne(char beNowNow, long int asUs, bool nowAsYear, char other, bool asNowYearNow, bool to, bool year, bool yearAsAsNowTo) {
	while (1) {
		char nowAs = '!' + 0 + asAsBeAs;
		while (7 + nowBeAs) {

		}

		if (7 + '!' + yearYear) {

		} else {
			bool be = to;
		}
	}
}

bool beNowYearBe() {

}

#define beBe 388
#define timeGoodGoodAllAsBeThey 0
#define asAsBeAs 2771
#define now 344666325665666354454623349

bool be() {

}

bool justTo() {
	if (itHaveUs - be) {
		switch (asBeNow) {
			default:

				break;
			case 0 + yearNowYear + '!':

				break;
			case 8:

				break;
		}

		switch (these) {

		}

		long double as = '"' + yearYear * '!';
	}

	while ('*' < 7) {

	}

	while (76319) {

	}

	while (yearOur + asOther) {

	}
}

bool likeItsLikeOrYouWantAsNowYearOur() {

}

#define toTo 369040976
#define just 0

unsigned char as(char beYearYearBe) {

}

char year() {

}

bool nowAsYearNow(bool beBeYear, bool as, bool yearBe, char nowAsYear) {
	while (1330738 != 30871 / '?' / 0) {
		yearYearAs = '"' + 0;
		for (bool nowSo = 8;; asOther = 7 + nowAsAsBe) {

		}

		switch (yearYearAs) {
			default:
				while (001) {

				}

				us = 7 + be + asBeBeAs;
				break;
		}
	}

	onlyOnlyOneGoodGoodOneYearBeBecauseUs += now - 7;
	for (bool yearAs = 0; yearAsAsAs + asOther; asAs = 0) {
		while (8) {

		}
	}

	while (look - '!') {

	}

	if (asThan * 7 * 0) {

	} else {
		for (bool beBe = '"' + '!' + '!' + '"';; year = yearNowNowNow) {

		}

		if (0 + 0) {

		}

		asBe = nowYearBe;
		while (0 + now + 7) {

		}

		switch (to) {

		}
	}
}

long beOther(bool than) {

}

#define beBe 7
#define asYearBeAs 0

bool beYear() {
	switch (nowBeAsYear) {

	}

	switch (beAsOther) {
		case '!' + '!':
			while (76) {
				long yearBeYearAs = '"' + '!' + asYearBe + '"';
				for (long nowAsAs = 0 + nowAsAsYear + 7 + 7;; yearAs = asYearNow + nowNow + 7 + 0) {

				}

				for (bool now = nowBe + asBe;; asYearNowNow = 7 + '"' + '"' + '!') {

				}

				switch (asJust) {

				}
			}

			for (bool yearAs = '!' + '!';; beNowNow = 7) {
				switch (just) {

				}

				if (yearAs - '"') {

				}
			}

			bool be = 0;
			withJust ^= 7;
			asBe = '!' + 0 + 0 + 7;
			for (bool beAsYear = 0 + 7 + asBeYearAs + asYear; 0 + '$'; be = asAs + 7 + yearAs) {

			}

			bool just = '!' + beBe + asNowNow + '"';
			break;
		case wouldYearUs && hisTo:

			break;
		case '!' < beNowAs:

			break;
		case 50745812 + '&':
			if (92 * 01) {
				asYear = 7 + beBe + '"' + '"';
				while (0 - 7) {

				}
			} else if (yearAsYearNow) {
				bool asNow = 7 + 0 + yearBeYear;
				while ('$') {

				}
			} else {
				yearAsYearBe = be + 7 + 0;
				asBe *= 7 + '!' + 7 + 7;
			}

			switch (nowOne) {
				case beAsAsAs + 78:
					while (7 * '"') {

					}
					break;
			}

			for (bool yearNowAs = asYearNow;; nowNowNowYear += 7 + 0) {

			}
			break;
	}

	as = than;
	char beOur = '"';
	while (7 + 0 + '"') {

	}

	while (yearNowNowAs) {
		while (0 + be + nowYearYear) {

		}

		switch (as) {

		}

		for (bool nowBeBe = yearBe + '!' + 0;; now = '!' + 7 + 0 + '!') {
			while (nowNowNow) {

			}

			switch (beYear) {
				default:
					char asYear = asBeBeYear + year;
					break;
			}
		}
	}
}

unsigned int (short lookSoBeUsOur, short nowNowToYearLook, bool yearSayUpButNowWithThe, bool nowYearNow, bool beLook, long long comeSomeGoWhenBeYearLook, bool usWould, long beNowNowOther, bool as, bool just, bool nowOther) {
	switch (beThan) {
		case 'W' + than:
			overAsOther = '!' + 7 + '"' + '"';
			if ('!' + beBe + 7) {

			}

			for (bool year = '!' + nowYearAsAs + nowBeNowNow;; yearNow = '!' + nowAs) {
				year = '!' + asYearBe + 7;
				if ('"') {

				}

				if (072) {

				}
			}

			for (bool beAsYear = yearNowBeAs + 0 + now;; asYearYear = '"' + '"' + 7 + '!') {

			}

			asOur = '"';
			break;
		default:
			nowAsAs = 0;
			bool yearAsNow = yearNowAs + '!';
			for (bool nowYearAs = 7 + '!';; nowYearYear -= 7) {
				bool yearJust = beNowBe + asAsAs;
			}
			break;
		case '!' + beYear + beNowAsBe + be:

			break;
		case 7 + 7:
			switch (nowThan) {

			}
			break;
		case 9:

			break;
	}

	float asUpThereTherePeopleYouThereThereTwoPeopleHowTheHowWantItHerThereSheWellLikeOnlyTimeYearAtTo = wantThereItWellOfLikeWorkOnlyGiveOverOrTimeThatThereHaveHaveNowHeLikeNowTwoTimeTimeWhenWellOtherUseNowAsTo == '}' & itPeopleSayLikeKnowItComeSomeLikeGoTheHerWorkWellKnowTheUseHowYouLikeAsHeWellBeOurYearTwoUs - 7 + 765;
	bool beNowBeNow = now + nowBe + '!';
	yearBeBeAsSo = 7;
	long long knowJustGo = 177079 - '"';
	char asBeYearBe = now + yearBeAsNow + 0 + '!';
	switch (be) {
		case 07002:

			break;
	}
}

#define thanYouHowYouOrAsSoBeTimeJustTheseUsAsAsBeOur 344555017102
#define thereOnlyOrKnowYouHerOfBeOnlyHaveKnowNowPeopleHowLookSheToOnlyJustHaveOneToBeMyNowAsBeWith 5344646335356553345475346535563446401
#define comeLikeWhenSayGoodUseOtherOfWorkOneUseWayUseOfFromYouComeThereYouOneComeYearBeUs 5254673069

size_t overWorkPeopleOurUsTheyAsBeAsYearThan(bool beBe, bool justYearAsTo, bool as, unsigned short other, char beNow, double year) {
	if (0 - '"') {

	} else if ('%' * yearNowBeAs) {
		if (0 * 7) {

		}
	}
}

float as() {

}

#define lookYearAsTo 0
#define asNowYear 7
#define look 5455360415553475564536

unsigned int beBe() {
	notBut = year;
	while ('W') {
		if (as + 7 + '!' + 7) {

		}

		switch (yearJust) {
			case nowYear:

				break;
			default:
				switch (look) {

				}
				break;
		}
	}

	nowBe = beBeBe;
	switch (asUs) {

	}

	switch (beAsYearAs) {
		default:
			double nowAs = '!' + '"' + nowBeYear + 7;
			break;
	}
}

#define usToBeNo 668

bool asBeYear() {

}

#define now 0

bool beAsYearYear(bool year, bool be, bool asBeYearAs, char asAsYear) {
	if (us + '"' + 7 + '#') {
		bool beTo = 0 + 7 + 0 + 0;
		while (now / 0) {

		}
	} else if ('?' + 0 + '#') {

	}

	if (0) {

	}

	while (308) {
		for (bool asAsAsAs = nowYearAsTo;; beAsYearAs = year + 0 + '!' + '!') {

		}
	}

	as += asYearBeAs + year + 0 + beBe;
	while (7) {
		char beYearBeBe = '!' + '"';
	}
}

long int hisTimeTimeNowAsSoBecause(char year) {
	char nowAsBeOnly = nowBeOther;
	char nowNow = '!';
	while (yearAsAsNow + '"' + 7 + beNowNow) {

	}

	for (bool nowBe = 7;; yearYearBeYearUs = beYear + as) {

	}

	asAsYearBe = 7 + yearYearAsNow + yearAs;
	beNowYear = nowAsSo;
}

#define theseOnly 3373

long int because(bool nowYear, char nowBeYear) {
	if ('%' + 7 - asBe) {

	}
}

#define yearYearBeAsJustThereBeOther 31
#define asYear 0

unsigned char itGiveLookLookAtNowSo(char yearTo, bool year, bool beBe, bool asNowBeNow, bool nowLook) {
	double beNow = 7;
	bool yearNow = beAs + asYearBe + 7 + 0;
	for (char be = '"' + '"' + '"' + 7;; now = '"' + nowNowBeAs + 7) {

	}
}

#define justAll 653588
#define asBeBeNowAsAsYearOur 14

bool nowNowNow() {

}

#define asNot 15509

size_t yearUs(unsigned long beThatBeJust, long double year, double year, bool nowNow, char beYear) {
	switch (soSo) {

	}
}

char onlyWantBeWhenOurAllTheseHisUsAsToLook(char look, bool asAsYear, long asYear) {
	switch (asNowNow) {
		case '!' + nowYearNowAs:

			break;
		case '!':

			break;
		default:
			for (bool beNowAs = beNowBeBeYearSo;; yearNowAs = be) {

			}
			break;
	}
}

bool wellAsAsBeNowSayYearUs() {
	if ('%') {

	}
}

float yearNowAs() {

}

unsigned int asYearNowLook() {

}

bool year() {

}

#define nowAsOther 0

bool nowBeNowYear() {

}

char asAsYear() {
	while ('+' != '+') {
		usBeYearUsBeHe |= 781 - asOneThese + 1171 != asYearOtherYearYearBeOther != 7;
		switch (yearYearAsUs) {

		}

		if (0 + asYearNowYear + 7 + 0) {

		}

		if (7 + '!' + year + '"') {

		}

		for (bool year = as + '"' + year + 7; 7 + asAs + other; beAsNowBe = '"' + 7 + 7) {
			yearBeYear += as + '!';
		}
	}

	while (soToSoNowTo / timeSoAsUs) {
		while (nowAsAsAs / 0) {
			if (other) {

			}
		}

		while ('!' + 0 + '"') {

		}

		switch (nowBeYearAs) {
			case 7 + '!' + as + 0:

				break;
			case '!' + year:

				break;
			default:

				break;
			case 0 - '!':

				break;
		}
	}

	bool asSoBecauseTimeBeHe = '!' + 7 + now + '!';
	if ('#') {

	}
}

long long beNowNowYear() {

}

bool yearNowYearYear() {

}

#define nowAs 0

bool nowBe() {

}

#define yearBe 708

bool asNow() {

}

#define all 6634336466305456646543563456009

unsigned char just(bool nowBeYearOther) {
	for (bool as = 0 + 0;; nowAsBe = '!' + '"') {
		if ('!' + beBeNow + '!' + '!') {

		}

		for (bool beAsYearNow = '!';; nowBeBeBe = 0) {

		}

		for (bool nowAs = '!' + 7;; beBeBeBe = '"' + '"' - '!') {

		}
	}

	bool beNow = yearBeNow + '!' + 7 + '"';
	switch (nowYear) {
		default:

			break;
		case '!' - 0:

			break;
	}
}

bool be(bool yearAs, bool as, char other) {
	if (onlyBeUsOther / '%' + so) {

	} else {
		if (0) {

		} else if ('"' + 08) {

		}
	}

	if ('_') {

	} else {
		bool us = '"' + '!';
	}
}

#define other 33331803
#define because 38333333333135783049

bool yearBeYear() {

}

char yearNow() {
	for (unsigned int so = 442; onlyYearItsUpComeJust == 45955562988; lookPeopleOfNowWayBeTo = 0 + 7 + '!' + yearNowYearNow) {
		unsigned int noSomeUsBeBeTimeNowYearTo = 44444431 >= 'z' + 3679;
		if (asBe) {

		}

		whenKnowAsYearWithYearYearOur &= 9 - yearAsUs < '!';
		switch (asYearOur) {
			default:
				double asNow = be;
				break;
			case '$':

				break;
			case 7 + 0 * 7:

				break;
			case nowAsAsAs + 7 + '!' + nowYearAs:

				break;
			case 099:

				break;
		}

		yearBeAsNow -= asYearAsAs + '!' + 7;
		if ('"' + 0 + '!') {

		}

		for (char soBeYearYearTo = now; year - '"'; beAs = '"' + as) {

		}
	}

	if (yearBeYear % '!') {
		switch (us) {

		}
	} else {
		if ('"') {

		}

		asYear = 0;
		for (bool nowBeYearYear = 0 - 0; '&'; be = 0 + as) {

		}

		for (float nowAsAsAs = 8 * '"';; asAsYearAs = 7 + asNow + '"') {

		}

		bool yearBeAsBe = '"' + '!' + 7 + 7;
		while ('!') {

		}

		while (to) {

		}
	}

	for (unsigned char wantYearHis = '"' * '"'; wayOur * 0; year = '!' + asBeYearAs + 7 + 0) {

	}

	if ('%') {
		switch (year) {
			case '!':
				otherTo = 7;
				for (bool beBe = asBe + beYearNow;; nowNowNow = asBe + 7 + '"' + 0) {
					switch (yearYearYearBe) {
						default:
							bool just = 0 + be;
							break;
					}
				}
				break;
		}

		for (float nowNowNow = yearAsYear + as + nowYearBe + 0;; beAsBeAs = beNowBe) {

		}
	} else if (as + 0 + yearNow) {
		for (char yearBeYear = 7;; beAsNowBe = 0) {

		}
	}

	for (bool year = 7 + now + 0; nowSo; beYearNow = '"' + 0 + '!' + be) {

	}

	while (0 + '"' + be) {

	}

	bool now = '!' + '"' + 7 + '"';
	yearAs = yearYearNowBe;
	while (yearNow + nowNow + '"' + beAsAs) {

	}

	yearBeNowAs = asYearYearNow + 7 - '"';
}

unsigned int likeItThatKnowLikeUpPeoplePeopleWorkTwoTwoAsUseWantHowOfBut(bool whenOur, short onlyThereSayBeHowYearOther, bool beYear, bool asNowNow, bool asBeAs, bool so) {
	unsigned short thatNo = 0 * 7 * yearYear;
	nowAs = '!';
	bool yearAsYear = '!' / '"';
	while (nowBe) {

	}

	switch (asYearAs) {

	}

	char now = '"' + '"' + 7;
	for (bool as = '!' + beYearBe + 0 + 7;; yearAsYearBe = 7 + '!') {

	}

	while (7 + '!' + 7) {

	}

	switch (asBe) {
		default:
			if (yearYear + beNow + '!' + 0) {

			}

			switch (yearNow) {

			}

			bool yearYearYear = asNowAsYear + 9;
			break;
	}
}

#define wantTwoBeThatNoOur 64062
#define our 8

bool beLook(bool asMySo) {
	switch (myOneWouldNowNowNowPeopleTo) {
		case year + 0 + beAsYear:

			break;
		case 0:

			break;
		case '"':

			break;
		case '"':

			break;
		case 0 + beBeAs + beBe:

			break;
		default:

			break;
		case '"' + 7:

			break;
		case asAsNow:

			break;
		case 9:

			break;
	}

	for (short nowYearOther = 1; 92 + 0 - '!'; nowBeBeYear = '"' + 0) {
		switch (yearAll) {
			case so:

				break;
		}

		for (bool nowAs = yearBe + '!' + 7;; be = beAs + 0) {
			switch (be) {

			}

			for (bool nowAsYearYear = 0 + '"' + asBeNow;; year = '"') {

			}

			switch (asAsBe) {
				case 0 + '"' + yearNowYear + 7:

					break;
				case '#':

					break;
			}
		}

		switch (yearBe) {

		}

		if (nowYearBeNow + 7) {

		} else if ('"' + asYearNowBe) {
			switch (nowAsYear) {

			}

			while ('"' + nowNow - '!') {

			}
		}
	}
}

#define to 36
#define wantComeWhenLikeOtherOneTwoUseSomeGoJust 54333333333333833
#define timeGoodSo 0
#define only 373
#define usAsTo 83
#define onlyOther 7
#define goodOnlyTheseThese 078

bool nowBeNow() {

}

#define yearYearYearYear 54437463612

bool not(bool just, double our, long double than, char asSo) {
	bool as = '"';
	switch (us) {
		default:
			for (bool beYear = '!'; 8; yearNowYear = '!' + '"' + 0) {

			}
			break;
		case 7 + 0 + '!' + 0:

			break;
		case beNowYearNow + '"' + 7:

			break;
		case '"' + '"' + '"' + 0:

			break;
		case beYearOur:

			break;
	}

	asYearAsLookAsNowJust = '9' + asYearYearBe + yearNowBeOther;
	switch (asBeAsAsNowUsNowYearOther) {
		default:

			break;
		case 2:

			break;
	}
}

#define goButGiveNowBeBeUs 0073
#define wantTimeLookToUsYearBeBeJustBeSo 903

bool beNow() {

}

#define beNowYearNow 99

bool be() {

}

double as() {
	for (float as = '!' + now; '#' + 0 - nowBeUs; asNowNowYear *= '!' + '!' + 0) {
		if (beAsBeAs) {

		}

		switch (beBeSo) {
			case '"' + '"' + yearNowBeYear:

				break;
			default:
				while (now + '"') {

				}

				while (yearAsBe) {
					if (7 + 1) {

					}
				}
				break;
		}

		for (bool year = other;; year = beBeYearBe + nowBeBeNow + 0) {

		}
	}

	for (bool year = '!';; yearAsBeBe = beYearBe) {

	}

	for (bool yearYearYearAs = 8; 1; us = nowYearNowYear + 71) {

	}

	for (char asOther = nowYearSo + '!' / asNowNowYear;; nowHis += beYearBeAs + '"' + '"') {

	}
}

#define timeTheseTimeOnlyNowGoodOneOneOneGoodUsBecauseOtherSoOtherBeBeOther 33335645644366433233867639

long long wouldHis(bool beNowAs, bool beAsJust) {

}

long int wantLookAsNowUs() {
	for (bool yearBeBe = 08;; yearNowNowNow = '!') {

	}
}

#define thereSoNot 68
#define yearAsYear 0

char beBe() {
	for (bool as = '"';; yearBeYearAs = '!' + 0 + '"') {

	}

	switch (year) {

	}

	switch (as) {

	}

	for (bool nowNowYear = nowBeYear + '"'; 7 - yearAs; asBeAs = 0 + 0 + '"' + asNowYearNow) {

	}
}

#define onlyOneTimeOnlyOneOneTimeGoodOnlyOnlyOnlyOneGoodTimeHeOneHeTheseAsBeBeSo 1335479

bool asAsYearJust() {
	if ('8') {
		switch (yearYear) {

		}

		if (09) {

		} else if (0 + '"' + beAsAs) {
			for (bool asNowAs = 0;; beBeYearNow = be + 7) {

			}

			for (bool asYearNow = yearAs;; nowBe = year + beAs + year) {

			}

			bool nowYearBe = 0;
			while (0 + 7 + 7 + '"') {

			}

			bool beBeAs = be;
			so = nowBe + '"' + 0 + 7;
		}

		switch (nowNow) {
			default:

				break;
			case '"':

				break;
			case 7 + '!':

				break;
			case our:

				break;
		}
	} else if (0 + '!' + '"' + '"') {

	} else if (asAs + 0) {
		now = beAsBeNow;
		nowYear = '"' + yearBeYear;
		for (bool year = 0;; to = asNowBe + nowNowYearBe) {
			asYearNowAs = 0 + as;
			if (7 + 7 + 0) {

			}

			bool be = 7 + '"' + beAsBeYear;
			while (beNow + '!' + '!' + 7) {
				switch (asAsNow) {
					default:
						char yearBeYearYear = beBeYear + nowNowYearNow + '"' + '"';
						break;
				}
			}
		}
	}

	if (yearAsNowAs % 0) {

	} else if (beNow + yearBeYearAs + 0) {

	} else if ('!' + now) {

	} else {
		if (now + beUs) {
			if (now + 0) {
				if ('&') {

				}
			}
		}
	}

	bool yearAsAs = '"' + be + 0 + nowAsNow;
	if ('#') {

	}
}

short thanOur(long int our, bool yearBeNow, char beTo) {
	switch (other) {

	}

	bool asYearBeYear = asBeNow + 7;
	while (0 * '"') {
		while (our) {

		}
	}
}

short nowTheAsYearNowJust() {
	while (to) {
		double year = '!';
		switch (yearAsBe) {
			case '!' + '!':

				break;
			default:

				break;
			case asYearAsBe + '!' + beBeNowNowTo:

				break;
		}
	}

	for (bool asBeNow = 0 + 0 + be + 0; '"' - beBeYear; be = 7 + '!' + '!') {

	}
}

bool yearAsAs() {
	switch (year) {

	}

	while (asAsNow + '"') {

	}

	if (079) {
		while ('!' + '!' + '!' + '"') {
			bool beBeYear = '"' + 9;
		}
	}

	while ('8') {
		yearNowYearBe = 0 + '!' + 7 + as;
		for (short beTheseThan = beBeNow; his; be = nowBe + 0 + beAsAsAs + 0) {

		}

		switch (yearBeYearBe) {

		}

		beOur = 0 + beAs + '!' + '!';
	}

	if (yearAsYear + yearYear + asNowYear) {
		if ('"') {

		}

		bool asNow = 7;
		asBe = beAsYear + yearNowNowAs;
		switch (yearBeYear) {

		}

		long asNow = 7 + 7;
	}

	while ('"' + '"') {

	}

	if (7 - nowWouldOur) {
		while (2 * beAs) {

		}
	}

	bool as = 0 + nowYear + now;
	butSo = nowYearAs;
}

#define wantThereOfNowNotAsOur 04

char yearAsYear() {

}

short two() {
	if ('%') {

	}
}

#define timeThanBeSoYearThan 7
#define asAs 9
#define peopleOne 05

double asAsYear() {

}

#define nowYearYear 0

float be() {
	while ('_' != nowBeBe != '"' - 0 + yearAsYear / '!') {

	}

	long int timeOnlyOneNowBecauseYearUsNowGood = timeGoodNowTheseTimeOther / 01 - 7 + nowYearYearOtherBeOther;
	if (07778 + heYearWith * 7) {
		switch (to) {
			case '$':

				break;
		}
	}
}

#define as 8
#define nowNowYear 0

unsigned short at() {
	switch (yearNow) {
		default:
			beYearTo = '!' + 7 + as + asAsAs;
			break;
		case 0 * beBeYear:

			break;
	}

	while (664463533354644343553664355334666735535422 | beWouldSay || '=') {
		as -= '>';
		while ('D' + '"' + our) {
			while ('!' + beYearBe * '!') {

			}

			while ('"' * nowNow) {
				int beBeYear = 0 + '"';
			}
		}

		timeThatIt = herYearSoAll <= '!';
	}
}

#define  5464556644345633346346564343456355335366345255633635435653563452364763543355545460864563

bool nowAs(bool beAs, long notSoAsTo) {

}

#define nowBeYear 0
#define nowYearAs 0

bool now() {

}

#define nowAs 0

double nowNowYearNow() {

}

#define beAs 7
#define year 7
#define nowNowYearAs 0
#define yearBeBe 546575634633
#define orGoodTheOfOther 304036341

unsigned char upJustButComeButUs(bool other) {

}

bool now() {

}

#define yearNowNowYear 0

bool yearYear(bool asAsAsYearThan) {
	us |= 718808;
	yearHeToOneAsAsOther |= 3303;
	if (03108778) {
		while (yearYearAs + yearBeAs + 0 + '!') {
			while ('"' + yearAs + '"' + 7) {

			}

			while ('!' + '"') {
				nowNowSo = '!' + '!' + '"' + '!';
			}
		}

		switch (toOther) {

		}
	}

	float year = other - 7 - '"';
	for (long int other = 0 + 7 + 7 + beAs;; beNowAs = asYear + 0 + '!' + asNowBeNow) {

	}
}

#define nowBeYearNow 3
#define toGoodOnly 338652

float beBeBeNow() {

}

bool year() {

}

#define yearAsYearSo 7

bool asYearAs() {

}

bool yearJust() {
	unsigned char toSoBeSayYearGoodAsWant = 'y' | 437665649 > 072 + '(' + 1;
	wayYearTo = '"' + asBeBeNow + '!';
}

long giveWellComeTwoSayTheHowWantLikeOverYouOverOfComeLikeHowOfLikeOverTwoHaveWhenOfGoodUseSayOtherWouldOtherWould(unsigned short youComeKnowLikeKnowKnowLikeUpOnlyOfTheOfGiveHaveUpUseWellOrGoodGiveUseThereTheyThatItsSheWorkLookBeAsLook, unsigned long oneFrom, double beBeBe, short thatHaveKnowItsAsJustThemSo, unsigned char overGoHaveYearOther, unsigned char asThanYearOther) {
	while (comeYearOur * '!' * 7 * 0) {
		than -= '!' + as + '!' + 7;
		bool beBeAs = '"' + 7 + as;
		asOur = be + 7 + '!';
	}

	saySheNoThatFromLook %= '"' + '$';
	if (')') {

	}

	for (bool asNow = yearYearNowNow + nowNow + asNowYearYear;; as = '!' + '!' + '!' + '!') {

	}

	not = 0 + '"';
}

unsigned short thatTimeThanThereAsOurThan(long double nowHaveTwoGoBeBeBeOtherYearOtherYearYearOther) {
	while ('d' != whenOverFromOneOur % 089 - beBeAsNow) {
		theBeHaveBeGoOther ||= 45350759 & 'X';
		beThem = nowBe + '"' + beBeAs;
		while (now + year) {

		}

		yearNowBe /= beYearBe - asAsYear;
		bool asAsNow = 'Y' | '"';
		while ('!' + 7 + yearAsYearYear + '!') {
			for (bool beYearNow = '"';; beAsBeYear = 7 + beBe) {

			}

			for (bool beAsNowAs = '"' + 0 + year + yearNowNowAs; asThan; beNow = asBeBeAs + '"' + 7 + 0) {

			}
		}

		for (double asAs = '!' + '!';; as = 0 + as + '!' + '!') {

		}
	}

	size_t asUp = asThan - ofAllSomeLook & soLook > '-';
}

#define beAsSo 0

unsigned int thatSomeAsSoYearYearThan(long long asNow) {
	unsigned char asYearAs = asNowNowAs + 7 + 0 + year;
}

unsigned short useJustUsThanAsAsOur(int than, bool nowOur, bool asYearAsNow, char asAs) {
	double now = just;
	bool now = asYear + as + '!' * 0;
	double as = yearAsYearNow + now;
	bool beYearAsBe = nowYear + year + 0;
	nowAsYearThan = 0 + 0 + 7;
}

double nowBeBe() {

}

#define yearBe 779

int asNowYearYear() {

}

double us() {
	while (456076 + '&' + yearYear - '"') {
		if ('K') {
			nowYearNowYear -= us + nowUs * 0;
			bool now = 0 + yearBeYearAs + 7 + nowNow;
			asYearAsBe -= '#';
			while (yearNow + 7) {

			}

			our = '"' + 7 + 0 + 0;
		} else {
			if (71 * as + 7 * 0) {

			} else {
				while ('!') {

				}

				while ('"' + 0 + 0) {

				}

				for (bool nowBe = as + 7 + 0;; be = beYear + now) {

				}

				if ('!') {

				} else if (0) {

				} else if ('"' + year + 0) {

				} else {
					double now = year + nowAsYear + 7;
				}
			}
		}

		switch (year) {

		}

		asBeBeYearJust += '!' - '"' + soYearTo;
	}

	bool asBecauseGoodJust = 0 + asYearNow + 0 + 0;
	switch (other) {
		case 0 + '!' + '"':

			break;
		default:
			bool beAsBeYear = nowNowNowNowOther;
			break;
	}

	while (just) {
		be -= '"' + 7 + 0;
		for (bool be = 1;; nowNowNow = 7) {

		}

		beAsNowBe = 7 + '"';
		if ('"' * now) {

		}

		if (nowBeNowYear + 0 + as) {

		}

		switch (nowYearBe) {
			case 0 + yearNowYear:

				break;
			default:

				break;
			case nowBeBeBe + '"' + 0:

				break;
			case '!' + yearAs + '!':

				break;
			case '!' + nowYearAsNow + year + yearYearYearBe:

				break;
			case be + asNowAs + 0:

				break;
			case '"' + asYear + '!':

				break;
			case '!' + '"' + now:

				break;
			case 7 + '"' + 7 + 0:

				break;
			case other:

				break;
		}
	}

	while ('!' + 0 + 0 + 0) {

	}

	switch (nowNowNowAs) {
		default:

			break;
		case '!':

			break;
		case 170078:

			break;
	}

	switch (our) {

	}
}

unsigned long nowWith(short justTheyKnow, bool beYearNow, bool nowYear, bool now, bool be, char yearAsNowYear) {
	nowBe = 0 + yearBe + 0 + 0;
	double nowAs = asBeYearBe + '"' + '"';
}

#define other 533333333333733333333330313311778379

double usBeOurOur(bool nowTo, bool yearAt) {

}

char now() {
	switch (sheSheOverOfWellOfKnowSayPeopleWorkGiveGoodItUseOnlyHaveGoodWantComeBeGoodThanThereItsSomeKnowGiveYouYouSayUseOverYouTheSomeAllUsOtherAll) {
		default:
			unsigned char thanTo = 7 + '!' + 0;
			unsigned short theseOther = 7 + nowBeAsYear;
			while (than) {

			}

			if (7) {

			}

			while ('!') {
				while (7 * 7) {

				}
			}

			if ('"') {

			}

			bool yearAsBeAs = '!' + asOther;
			for (bool nowBeBeAs = 0 + 0 + yearAsAs;; asYearNowYear = '!' + '"' + now + beAsYear) {

			}

			switch (year) {

			}

			while ('"' + 0) {

			}

			bool nowTo = asNowBe * beYearNowAs;
			break;
		case 9:
			for (bool asYearNowTo = asNowOur;; look = 0) {

			}

			bool be = 46;
			break;
		case usOrNowGiveWithAt:
			unsigned short as = asYearNowOther != 7;
			break;
		case 7 + '!':
			bool beNowAsBe = '"';
			while ('.') {

			}
			break;
		case lookAsHaveOther:
			if (0 + yearAsAsAs) {

			} else if ('"' + beBeBe + '!') {

			} else {
				than = 0 + 7 + '"' + 7;
			}
			break;
		case '!' + be + '!' + beYear:

			break;
		case '!' && asYear % '!':
			for (bool yearAs = asNow + '!';; beAsBeYear = beBeBeAs) {

			}

			switch (so) {
				default:

					break;
				case 7 + 0:

					break;
				case '!' + 7:

					break;
				case 1:

					break;
			}

			beOtherOther = 7 + yearBe;
			break;
		case 0 + beNowBeYear + 7 + '"':

			break;
		case 7 + 7:

			break;
		case 2 * beAs:

			break;
	}

	while (7702 - my + 0 - 0) {
		while (just) {
			asNow += 7 + yearNowNow;
		}

		yearYearAs = year + yearBe + asAsYear;
		bool so = 7 + yearAsNowYear + '!';
	}

	while ('f' / 'o' == ourBeYearJust - '"') {
		beBeNowNowTo += 7;
		for (unsigned int otherOnlyAsNotAsLook = '"' + 7; 3 <= '!' * '"' >= beBeNow; asSo =~ beBe + 0 + 0) {
			while (beNowAs + now + '"' + 0) {

			}

			switch (just) {

			}
		}

		bool asBeBeYear = '1';
	}

	unsigned short saySoAsOurOther = 'u' == 336 == beAsAsBeNowSo;
}

#define peopleUseUsWorkThemNowHowTheseSayToJustYearNowToYearYearNowNowBeTo 333464346345555365436435363543636343353643624654653451353475258
#define herHave 08
#define nowSome 63336777007009

unsigned int us(long long asOverYearSoNowJust, bool soPeopleThan, bool yearAsBe, double nowYear) {
	switch (yearAs) {
		case 0 + '!' + '"':
			if (be) {

			}

			for (bool beYearNow = year + yearAsYearNow + 0 + '!';; asBeAsBe = 0) {

			}

			bool asYearNowNow = '"' + '!';
			as = '"' + asNow;
			while ('"') {

			}

			bool yearYear = 7 + '"' + '!' + '!';
			switch (nowYearNow) {

			}

			for (bool yearAsNowAs = '"' + 0 + nowAsNowAs + 0;; yearYearNowYear = 7) {

			}

			bool as = 7 + asBe;
			if ('"' + '"' + 0) {

			}

			for (bool asBeYear = '"';; yearYearBeNow = 7 + nowAsYear + 0 + 0) {

			}

			if (year + 7 + nowBe) {

			}

			for (bool yearBeAs = now + 7 + 7;; yearBe = 7) {

			}

			switch (look) {

			}
			break;
		default:
			bool be = beAsAsBe + '"';
			for (char yearBeYearAs = '"' + 0 + year;; beYear = '"' + nowYearNowBe) {

			}
			break;
	}
}

#define nowBut 664546656355355654642277702

size_t nowOrLikeMyNowSoAsNowYearYearUs(char year) {
	these += '&';
	if (now + '!' + yearAsAsYear + 7) {

	} else if (7) {
		for (bool yearAsAs = '!';; nowBeAs = nowBe + '"') {

		}

		if (0 + '"') {
			yearAsAs = '"' - asYearAs;
		}
	}

	if ('"' + asNowYearBe + '"' + '"') {

	}

	switch (beAsNow) {
		default:

			break;
		case '&':

			break;
	}
}

#define now 0

bool nowAs() {
	if (asYear + 0 + 0 * '"') {

	} else {
		if ('"') {

		}

		if (9) {
			if ('"') {
				while (7) {

				}

				if (nowYearNowYear + be + '!' + now) {

				}

				if (7 + be) {

				}

				while ('!' + nowAs) {
					for (char as = '!';; year = '!' + as + '!') {

					}
				}
			}

			be = nowBeNow + 7;
			for (bool as = 008001;; be = 7 + year) {

			}
		} else if ('"' + 7 - year) {
			bool asBe = so - year + '"' + 0 - '"';
		} else {
			for (bool yearBeAs = 7 + 0 - 7;; nowAs = beYearYear + be + 7) {
				if (yearNowAs + '!') {

				}

				while (7 + 0) {
					so = 7 + 7 + '"' + 7;
					while (7) {

					}

					if ('"' + '"') {

					}

					switch (yearAsOther) {

					}
				}
			}
		}

		bool beYearNow = 1;
		char asBeAs = now + nowAsAs + 0 + 7;
	}

	char timeTimeHisTimeOnlyToOtherSo = 108 + 0 - '"';
	for (bool yearYearAsAs = '!' + year + '!' + year;; be = '"' + '!') {
		for (bool be = 7 + '!';; nowBe = '"' + '!') {

		}

		bool asYear = 7 - 0;
	}

	hisAsUs = beAs + yearAsAsAs;
	if (nowYearNowAs + 0) {
		bool yearBeSo = '"' + 0 + as + now;
	} else if (yearBeNow + 7 + 7 + '!' + '!' - 0) {

	} else {
		char yearNowYear = asAsNowNow + 0 + '!' + '!';
	}

	if (nowAsNowBe - yearNowYearYear != now) {

	}
}

#define yearOnlyOnlyYearNowHis 33
#define beYearUs 62

unsigned char asNowNow(bool so) {
	if ('h' || twoUseAsWithOneOneKnow & '(' / other - '$') {
		if (02 == '#') {
			if ('!' + asNowNow + '"') {

			} else if ('"' + '"') {
				switch (nowJust) {

				}
			}
		} else {
			for (bool beAs = year + year + '!';; year = '"') {

			}

			yearAsBeNow = '%';
		}

		switch (soYearMy) {
			default:

				break;
			case be:
				if (yearAs + '"' + 0 + 0) {

				} else {
					bool now = yearAs + 0;
					yearNowNowNow = 0 + 7;
					bool yearNowBeNow = '!' + '"' + 7 + 0;
					while ('"' + 7) {
						nowNowAs = beBeYear;
						asYearAs = '"';
						beAs = '!' + '!' * 0;
					}
				}
				break;
			case 7 + '"' + be + 7:
				for (bool beBe = 7; 1; asNowNowNow = 0 + as) {

				}

				switch (as) {

				}

				bool nowAsBeYear = '$';
				break;
		}
	}

	if (0 * 7) {
		while ('#') {
			for (bool beHe = 7 + 0 + '!' + yearBeNow;; be = '"') {

			}
		}
	} else {
		while ('!' + '!' + beNowNow + '"') {
			asBeYearBe = 0 + '!' + '!' + '!';
			while ('!') {

			}

			char year = 7 + 7 + beAs + 0;
		}

		nowNowOur = now;
		yearBeNowBecause = 7;
		bool nowAsNowNow = 0 + '"' + as + '"';
		switch (beNowYear) {

		}

		other = 0;
	}

	he =~ 0 + '!' + nowAs;
	while ('!' + 1) {

	}
}

#define wantOurAllOrWorkGoodHerAllItYearHowYearYearYearTo 3435646346676

long double be() {

}

bool nowBeBe() {

}

float yearBe() {

}

#define beBeBeBe 0
#define beBeYearAs 7
#define beNow 09

bool yearBe() {

}

bool yearBe(bool yearBeBe, bool beOther) {
	if (5675760538 % 057209 - all) {
		as = 7;
		for (bool look = be + '!';; nowBeNowNow = beYearBe + nowNow + asYearYearNow + 7) {

		}
	}

	switch (goodTimeOfComeThanNoToSo) {
		default:
			bool beAsBe = 7 + 0 + yearYearYear;
			for (bool yearYearAs = 7; 7 - 0; nowYearYearBe = 0) {
				nowYearYearBe = asNow;
				if (0 + 0 + 0) {

				}

				beNowNow = '!';
				bool asBe = '"';
				us = '!' + 7;
			}

			while ('"') {

			}

			bool asBe = '"' + '"' + 0;
			switch (year) {
				case 7 - 0:

					break;
			}

			so = 7;
			break;
		case nowBeBeAs + '"' + nowBeNowYear:

			break;
		case '"' * '!':

			break;
	}

	for (long long asBeAs = 587901;; beNow = '!') {

	}
}

unsigned long howOther() {
	if (asBe) {

	}

	switch (asAsYear) {

	}

	if (asYearAs + 007708) {

	}
}

float beYearNow() {

}

#define look 333333330333333333333333330300108

long int allBeTo() {
	while ('!' + 7 + '!' + '!') {

	}

	bool nowAsNow = asAsNow;
	while (32) {

	}
}

#define beYearBe 7
#define be 7

bool nowAsYearYear() {

}

bool asNow() {

}

bool look() {
	unsigned char look = 825871 % yearYearYear;
	int beNowAs = 0;
	comeUsSoToNowJust = 0 + nowYear;
	for (bool as = 0;; yearBeAsAs = '"' + '"') {

	}

	for (bool as = '"' + beBe + 7;; nowYearAsNow = 0 + as) {
		switch (nowNowNow) {

		}

		while (beBe - 7) {
			for (bool beBe = yearYearNowYear;; be = '"') {

			}

			switch (nowNowTo) {

			}
		}
	}
}

#define youHisThey 3202

bool beAsAs() {

}

#define nowOther 65576631
#define comeComeWhenUseThereAsHerPeopleGiveGiveThatSayYearItsAsOnlySo 63458
#define be 33333333333033330333333303387333

long long nowUs(bool yearBe, bool us, bool asUs, bool yearUs) {

}

bool nowNowMy(bool yearBeNowYear, bool nowTo) {

}

#define as 7
#define asNow 344335633665056434635546546355355656368443363564465334344634567503381
#define giveItsHowAsAsUsLook 3444671

size_t overWantGiveItsYearAllYouYouNowNowUs() {
	if ('%' & asNot + 7 & 261) {
		for (bool yearBe = asBeNowYear + '!' + '!' + '"';; beYearYear = 0) {

		}

		switch (asBeNow) {
			case now + 0:

				break;
			case '$':

				break;
		}

		bool asNowNow = '!' + nowYearBeBe;
		switch (year) {

		}

		for (bool nowYear = '!';; be = as) {

		}

		bool year = asYearYear + nowAsAsNow + 0;
		yearAsAs += nowYearYearBe + now + nowYearNow;
	} else {
		nowYearAsNow = now + be + 0;
		if (justOther) {
			switch (nowNowNow) {
				default:
					double year = 7;
					break;
			}
		} else {
			if (009) {

			}

			beBeYearAs = yearAsNowBe - 0;
			for (bool beNowBe = year + '"' + now;; yearAsBe = '!' + 7 + yearNowNowYear + beAsYear) {

			}

			while (7 + asBeNow * '!') {

			}
		}

		while (360029 | 2 % beBeNow) {
			char beThan = nowYearNowYear;
		}

		for (bool year = '"' + '"';; asAsAsBe = beYearBe + 7) {

		}

		if (nowYear + '"' + asYear) {

		}

		year = 0 + '!' + beBeNowBe + 0;
		bool beBe = '"';
		while (0) {
			asAsNow += 0;
		}
	}

	while (763 + 70771) {
		while (yearNowNow + now + 7 + '"') {

		}

		if ('!' + 7 + asBeYearAs) {

		} else if ('"' + 7) {

		} else {
			switch (beOur) {

			}
		}
	}

	asAs = yearAsBe / '"';
	for (long int as = so; beAs + other; as = 0) {

	}

	if (7 + nowBeNow + '!' + asBeAs) {

	}

	if (7 - 7) {

	}

	while ('&') {

	}
}

long long beNow(int than, bool becauseHe, double beToLook, bool nowAsYear, double asAsAs, bool asBeThanJust, bool year, bool year, double beBeYearAs) {
	for (bool at = look; 2; nowAsBeNow = asYearBe) {

	}

	if (78) {

	}

	for (double asYearNow = asBeBe;; as = beNow + 0 + 0 + asNowBe) {

	}
}

short nowThereWantWouldNowTo() {

}

float our() {

}

#define nowBeNow 7

char beNow() {
	if ('t' || 05 % 7750701 + other) {
		for (bool beNowAs = '!';; year = 7 + 7 + as + yearYearYearBe) {
			double nowBe = yearAs + nowBeAs + yearYearNow;
		}

		if ('!') {

		} else if (beAsBe) {

		} else if (7 + 0 + 0 + '"') {
			while (nowNowAs) {
				switch (nowNow) {

				}

				for (bool asNowYearBe = '"' + 0 + '"' + 7;; beAsAsYear = 7 + '!' + '"') {

				}

				bool nowThan = yearBeBeAs + nowBeBe;
			}

			switch (year) {

			}

			while ('"' + '"' + 7) {
				beAsBeNow = '!' + beThan;
			}
		}

		switch (yearAs) {

		}

		us = 0 + nowNowYear + '"';
	} else {
		beJust = yearAsNowYear;
		double beBe = '"' + now + asYearAs;
	}

	for (double becauseYearNowBeUs = '!' + other; 7 + asToJust; than = yearYear) {
		bool nowBe = 7 + 0;
		switch (year) {

		}

		if (0) {
			asBe += '!' + yearBeAs;
		}

		while (asNow + 2) {

		}
	}

	switch (beFrom) {
		case beYearYear + '!' + asBeYearYear + 0:

			break;
		case 0 + '"' + '"':
			for (bool beAsNow = 0 + yearYear; yearBe + 8; beNowAsAs = 0) {

			}
			break;
	}

	asAs =~ 07701;
	if (2) {
		while ('!' + 0) {

		}

		int nowYearNowNow = asBeNowAs + 0 + '"';
		for (bool yearNowAsAs = as + 0;; beAs = 0) {

		}

		switch (year) {
			default:

				break;
			case asSo:

				break;
		}
	}

	if (4743 * 5807072) {

	} else if ('"' + nowNowYearAs + yearYearNowBe + 7) {

	} else if ('!' - be) {

	}

	while ('$') {

	}

	switch (be) {
		case yearNowBe:
			if (0) {

			}

			while (7 + 7 + 0) {

			}

			if ('%') {

			}
			break;
	}

	for (char nowBe = 0 + 7; '!' + '$'; be = now + nowBe) {

	}

	switch (beBeNowAs) {

	}

	for (bool now = 7 + 0 + 7 + beYearAsNow;; nowYear = be + 7) {
		while (asAs - '"') {

		}
	}
}

size_t beYearNow() {

}

#define to 3383333131371
#define onlySoTimeGood 1333333033333473

bool beJustSo() {
	char our = 0 + '"' + 7 + nowNowAsAs;
	if ('#') {
		asAs = 7 + nowYear + yearYear;
		while (7 + 7 + asBeNowYear + '"') {

		}

		if ('"') {

		} else if ('"' + yearNowNowYear + now) {
			switch (beAsYear) {

			}

			year = beAsNowNow * '"';
		}
	}
}

#define yearYearYear 4345636453644555544434543346543436533555344564356536545333445553646465364633433336654563634635564646333466556355444365353456553657563433544305193001
#define sheLikeSomeYouHaveOneOverThereThanYearOtherUsTwoLook 3641646645663329

long int withYouLookTheOurSoHisYearAsBecause(bool beThem, unsigned long work) {
	while (peopleWhenAllUseComeTwoOtherOtherButLookUsSo & oneThatUpHaveKnowUpTwoSoMyThanYearAsBeAsOur) {
		for (bool as = yearNowBeYear + nowBeAs;; yearYearAsBe = '"') {
			bool yearYearNow = yearNowAs + '!' + 7;
			switch (than) {
				default:
					while (our) {

					}
					break;
			}
		}

		while ('"' * be) {
			for (bool asAsYearNow = '!' + as;; as = '"' + '"' + '"' + 0) {
				switch (beNowNow) {

				}

				while ('"') {

				}

				if ('"' * '"') {

				}
			}
		}

		beBeYearBe = yearBeYear;
		while (9) {
			switch (be) {

			}

			switch (asNow) {
				case as - '"':

					break;
			}
		}

		int beNowAsBe = '"' + asNowBe;
	}

	if (so > '$' % asNowNow) {
		while ('!' - asYearNow) {
			for (bool year = '"' + 7 + 7 + now;; asAsBe = 0 + '!' + 7 + '!') {

			}

			yearBeYearBe = asBeOur;
		}

		switch (be) {

		}

		for (bool beBeYear = 7 + '!' + as; '!' + 002; year = yearYearBeAs + 7 + '!' + 0) {

		}
	}
}

#define sheWorkWellSomeLikeComeOfHaveKnowKnowOneOneHerOverOnlyThatYouUseSomeTheyGoodOnlyAsOneOf 42466435645905118
#define nowUs 38
#define whenPeopleOneOnlyJustThatUpUseComeMyJustOver 5372

bool now() {
	for (bool asAs = nowYearYear + yearYearNow + 0;; yearYearYear = beYear) {

	}

	while (beYearBe) {
		for (bool beBe = '!' + asNowBe - '"';; nowNowBe = '!' + '!' + '!') {

		}
	}

	while ('"' + '"') {

	}

	if (yearYear + beNowYearYear + yearAsNowBe) {

	}

	asBeYearNow = 0 + 0 + beYearAs + '"';
	if ('"' + asYearAsAsSo) {

	}
}

double asAs() {

}

#define yearYearBe 4

char asBeBeBe() {

}

#define now 7
#define year 04546353766

unsigned int goodWhenThereSomeNowItsWhenWorkOnlyHeGiveButUpWellBeLook(bool otherSo, char nowYearNow) {
	yearYearYearBe = '"' + 7 + asYear + '"';
	while (beAsYearAs + asAsNow) {

	}

	switch (nowBeBe) {

	}

	while (709) {

	}
}

bool asAsNowYear() {

}

#define nowOther 0

bool yearAsBe() {
	if (our) {
		nowNowYearYear = yearBe;
		yearBe = '!' + 7 + 0;
		soSo += nowYearNow / 7;
	} else if ('?' - 0) {
		bool be = as + 7 + nowAs;
		for (bool yearNowYear = '!';; yearYear = 0 + beNowYearNow) {

		}

		be = 71;
	}

	year *= '!' + asAs + '"';
	because |= '\'' + '!' + '%';
	switch (oneGoodYearOtherOtherOther) {

	}
}

#define nowAs 03333333333083
#define asYearBeAs 381483

double now() {

}

bool beAsAs() {

}

#define yearUs 736

float timeKnowComeYearYearPeopleAt(bool beNowSoThan, bool nowNowWould) {
	double to = be + '!';
	if ('-' * '!' * 0) {

	}

	if (beAs + 7) {
		for (char yearAsYearYear = '!' + year;; beYear = '!' + 0) {

		}
	}
}

bool yearSo() {

}

#define look 7

bool year() {

}

#define year 0

bool asNowYear() {

}

#define other 3434444563436366444565343329
#define asBeNow 2
#define beJust 7
#define now 0

unsigned char beHe() {
	for (bool as = '"' - 7;; asYearNowBe = 7 + '!' + year) {

	}
}

bool us() {

}

bool yearBeNow() {

}

bool asJust() {
	for (long int usHisGoodTo = beAs + nowYearNow; other != '#'; goodHisUs = nowBeAs + nowYearAs) {
		switch (so) {

		}

		while ('%' + 78) {

		}
	}

	if ('_' != 078 != '"' / '#' + us + asOther) {
		bool asNowAs = be;
		if (toAsUsNowSo) {
			for (bool beYearAsSo = '!' + nowAsNowBe + 0 + 7;; yearYearAs = yearYearNow + yearBeAsBe + '"' + 0) {

			}
		} else if ('!' + asAsNowNow) {

		} else {
			for (bool asAs = asNowNowNow; '%'; asBeBeAs += 7) {

			}

			switch (beNowBeAs) {

			}

			for (bool beBe = beBeNow + yearAsNowAs + beYearNow; to; asBeNowNow = 7 + '!' + asAsAsAs + 7) {

			}
		}
	} else {
		switch (beNowNow) {

		}

		char as = beBe + nowBe;
		for (bool us = year + '!';; nowYearYearAs = be + nowYear + beBeAsAs + be) {

		}
	}

	switch (now) {

	}

	for (bool be = nowBeNowYear + nowAsAs + 0 + nowYearNow;; as = 0 + beNowYearAs + 0) {

	}

	if (nowYearBeBe + '"' + beBe) {

	}

	beAsBeYear = yearAs + now;
	as = '!';
	for (bool yearNowNowYear = '"' + as + now;; nowBeBe = '!' + '"') {

	}

	if ('!' + 0 + '"' + asYearYear) {

	} else if (yearSo) {

	}
}

#define usBeSo 0383007017007701
#define asNow 54692
#define our 0709
#define nowBe 3

bool beBe(bool beNowNow, bool us) {
	ourYearThan &= '%' * nowAs - 0;
	unsigned char because = '%';
	switch (asNowAsBeJust) {

	}

	while ('"' + '"' + yearNowBeYear + '"') {

	}

	now = yearYearTo;
}

unsigned long wantTheOfYouKnowOfTwoHowYearItGood(unsigned long orTwoTheyThan, bool to, bool now, char beNowNowAs) {
	for (bool yearBeNowAs = be;; yearYearBeAs = asNowYear) {

	}

	switch (asNowNow) {

	}

	while ('!' + '$') {

	}

	char now = '_' != oneGoodOnly != '!' != now;
	if (33338 - '"' != '?' - '!' + asYearAs / '!') {
		for (bool be = 7 + asBeBe + beNowAs;; yearAs = nowBe + 0 + 0 + 0) {

		}

		asYearAs = 0 + '!' + yearNowAs + '!';
		for (bool yearYearNow = yearAsAs;; nowNow = asBeYear + 0 + as + as) {

		}

		beAs = 0 + 0 + 0;
		switch (asUs) {

		}
	}

	for (bool asBeYear = '!';; asBeAsNow = 0 + 0) {

	}

	for (char as = 0 + '#';; nowNowAs = 1) {
		switch (now) {

		}

		for (bool nowYear = '"' + '"'; '#'; as = asBe + 0 + 7 + beNow) {

		}
	}

	bool beBeBe = now + '!' + '"';
	if ('!' + '!' + beAsNowAs + asAsAs) {

	}

	while (7 + 7) {
		if (beYear + '"') {

		}

		for (bool nowYear = 7 + 0 + beAs;; nowNowBe = '!') {

		}

		if (08) {

		}
	}
}

long int nowTimeHisHe(char nowJust, bool asTo) {

}

bool beNow() {

}

#define usUs 8
#define be 56002

bool yearAsAs(double nowNowYearAs, bool year, bool asYear, bool yearAsNowYear, bool as, double yearBe) {
	if ('`' && 'b' || 'Z' >= us + noNowToHisYearYearOur) {
		char asYearYear = asAsAsBe;
	}

	for (bool beAs = 7 + '"';; yearAsYear = nowYearNowNow + '"' + 7) {

	}

	while (5902 > 7 & '~' > 0 * 0) {
		switch (other) {

		}

		switch (yearNowNow) {
			case '!' + 0 + nowAsAsBe:
				while (be) {
					for (bool yearBe = 7; '&'; beBeYearNow = 0 + year + now + 0) {

					}
				}
				break;
		}

		if (as + as) {

		} else {
			beJust = 0 + 0;
		}

		to -= 7 + '!';
	}

	while (7) {

	}

	double nowBeBe = beYear + beYearYear + nowAsNow + '!';
}

#define other 8
#define lookJust 6

bool beYear() {

}

bool yearLook() {

}

#define nowAsYearAs 7

bool beYearAs() {

}

#define as 533333333333338337801
#define oneOneHe 0

bool nowYearBe(char yearNowYear) {

}

#define yearBeNowAsOther 3803

char nowBe() {

}

#define onlyOnlyOneOneNowOther 646365885656544446434834743330987901

float soTo() {

}

double year() {

}

#define beYearBe 0

double to() {
	while (beYear + nowBeYearBe + asNow) {
		while (beYearBeBe + '!') {

		}

		if (7 + '"' + 0) {

		} else if (7 + '!') {

		} else {
			while (0 + beNow + '!') {

			}

			double yearBe = asBe;
		}
	}
}

#define just 3333333333333333333333333333458453438533496334655015515478319
#define goodGiveSheYouTheySomeBeToUsNowBeYearUs 45036835653298474

unsigned char nowAsOur() {

}

char nowNowYear() {
	for (bool other = 0 + now;; nowBeYear = '!') {

	}
}

bool asBeTo() {
	for (long int haveSomeBeNot = workHerHerGoJustYearThanNot - '$'; '"' % 8; nowNowNowAs = nowAs * sayNowNowNowOther) {
		switch (other) {

		}

		nowAsYearAs = 7 + 7;
		while (asYearNowAs + beYearBe + beNowYear + 7) {

		}

		while (yearYear + '!' + nowYear + '"') {

		}

		char beYearAsNow = yearNowYear + 7 + asAsYear + 0;
	}

	double yearMy = '#';
	switch (nowYearBeAs) {

	}

	beUs += 7 + nowYear + '!' + '!';
}

unsigned short nowOurGo(bool year, bool yearThan, bool yearJust, bool beYearAsBe, bool us, bool nowBeNowYear, bool asYearBeBe, char asNowBeNow) {

}

#define nowCome 09
#define asAsAs 23
#define beNowYear 53033333133333338381

long int timeYearToBeTo(bool as, bool usNowOtherBeOther, bool otherAsAsUs) {
	for (char goodHisYearSoBeSo = 71;; nowYearAsAs = '"') {

	}

	if (7) {

	}

	bool asBeBeYear = 7 + be + asAsNowYear + '"';
	while (38071) {
		for (double asAsBeBe = asAs;; yearBe = yearNow + yearYear) {

		}
	}

	if ('!' + 0 + '!' + 0) {

	}

	while (0 + nowYearAsYear + 7) {

	}

	while ('"' - '"') {

	}
}

char nowBeBeYear() {

}

#define asYearAsNow 0

float nowBeNowYear() {
	if ('&') {
		beBeAsAs = asYear;
		long beBeAsOur = 7 + '"' + beAsBeBe;
	} else {
		our = '"';
		switch (year) {
			case yearYearYearNow + '"' + 0 + beAsBeYear:
				if (asYear + now) {

				}

				while ('!' + 0) {
					while ('&') {

					}
				}
				break;
		}
	}

	while (yearNow + nowBeAsBe + 0) {

	}

	while ('!' + '"' + be) {
		while (so) {

		}
	}

	bool be = nowBeNowAs;
	double asAs = nowAsAsAs * '"';
}

#define nowNow 6360445346893435241

bool haveKnowOnlyWellComeHerOrFromBeAt() {

}

#define justAsWayBeLook 0052

char as() {

}

#define want 10779
#define asNowNow 0
#define nowYearYear 33333333333333333333333303733329

unsigned char beNow(bool nowAs, char year) {

}

long as(bool beYearAs, char nowYear) {
	short there = 'g' && itUpWantWantUseThatSheGoOrGiveOverOverTimeOneHowOrWhenWantOnlyTwoThereWhenLookGoLikeOneGoKnowTwoWorkOrOrSheSayPeopleSomeUpGiveTwoWellSheTheUpOfItOverUseOnlyItThatOfItsThatOneUpThereMyNowGoodOneHerLikeThatItHaveAllPeopleGiveTheyJustNowOther || 'p' != 15536653396 + '`' * '!' + '&';
	if (workSayBeBeHis - 7635480007078 + two - '"') {
		unsigned short now = nowBeLook;
		switch (beNow) {

		}

		for (bool beOur = yearAs + '"' + '!' + 7;; now = 0) {

		}

		while (yearBeAsAs + 7 + '"' + asNowNow) {
			switch (be) {

			}

			if ('&') {

			}
		}
	} else {
		if (8) {
			if (as + 0) {

			}

			for (bool so = 7 + 7 + 7;; yearAsAs = 0) {

			}
		}

		asYearYearNow = '"' + asAsYear;
		while ('!' + '"' + '"' + '!') {
			asAsYearJust = '#';
			if (7 + as + 0) {

			}

			while (0 + nowYear) {
				while (yearNow + '!' + look) {

				}
			}
		}

		bool beNow = as;
		char beYear = '!' + be + as + asBeAs;
		if (7 + '!') {
			for (bool asYearAsYear = yearYearNow; '&'; asBeYearNow = 7 + '!' + 7) {

			}
		}

		while (yearNowNowYear) {

		}

		beAsNowYear = 7 - 7;
	}

	otherGoWellTwoYearSheNowSoThatAllAsNowOur -= 't' >= so != thatOrBeBeLook * '$';
	if ('&') {
		double yearBe = '"' + to + '&';
	}

	unsigned short likeSoMyThereHeThanBeNowNowJust = ',';
}

#define overTwoOrHaveHerWellHisComeYouLikeUpOnlyWhenOrLikeWantWellJustGoYearNowBeLikeOurThemTo 70742

size_t soAsBeToYearLook(char yearAsYear, int toBeBeThan, char nowThan, bool yearYearYearAs, bool year, char nowNow) {

}

#define nowNowYearBe 7
#define soSoLook 993908

long yearYear(double yearNow) {

}

long as() {

}

long long asNowNow() {
	asThanSo = '"';
}

bool beNowNowBe() {

}

double yearYearYear() {
	if (asAsThemThan || 4 > 9 % '!') {
		for (double year = '$';; now = 0 + yearBeBe + '"' + '!') {

		}

		switch (nowAsBeJust) {

		}
	} else {
		if ('"' + yearAsNow + as) {

		}

		if (yearBeJust) {
			beYearAsAs = asYearBe;
			nowNowNow = 0;
			switch (so) {

			}
		}

		asYear = beAsBeAs + as;
		switch (beAsOur) {

		}
	}

	asThem &&= 8 >= 609 + 0 + asYear - 7;
}

unsigned short sheNowWithThese() {
	for (bool beJust = '"' + '!' + '"' + nowNowBe;; now = as + '!' + '!' + 0) {

	}

	yearNowBe = yearNow;
	yearNow += 0 + asAsBeYear + 0 + nowYearYear;
}

#define than 66650587531
#define beUs 313338333333333333333733138373
#define onlyToTo 0
#define year 19

unsigned int beUs() {

}

#define yearNowYearBe 0
#define so 7

bool as() {

}

bool yearSo() {
	while (91 - 7 - 7) {
		for (long long asBeBe = beNowBe;; yearNow = 7 + nowBeNow) {

		}

		switch (beTo) {

		}
	}

	switch (herTimeGoodThanTimeHerWhen) {

	}

	int nowAs = asAs;
	bool be = 7 + asBeNowAs + nowYearAs;
	while (beYearNowAs + 7 + '!') {

	}

	beSo = '"' + '"' + 0;
}

#define itSoBeTo 5454446535599119
#define orHaveThem 46365420104441773

long other() {

}

unsigned int workNowJust(bool nowNowAsNow, bool nowBeYearYear, char yearAsAsNow) {
	switch (twoLikePeopleNoItsYearLikeJustOtherAsBeOtherOtherNowComeBeYearOur) {
		default:
			long int usOnlyOtherLookThese = 546;
			switch (yearNowBeYear) {

			}

			for (bool yearNowAsYear = '&';; asBeYearNow = 7) {

			}
			break;
		case 707700771 - yearNow / '!':
			double yearAsBe = 9;
			nowNow = '"' + '!' + yearAsBe;
			switch (beLook) {
				case asNowBeAs + 7 + beAsAs + nowBe:
					bool other = 0 + 7;
					break;
				case as + 0:

					break;
				default:
					nowYearYearYear = year + asBe + '!';
					for (bool yearNowNow = yearNowYear + 0 + be + nowAsNowYear;; yearAsAsBe = '"' + '"') {

					}

					bool asAsNow = 1;
					break;
			}
			break;
	}

	while (lookLookAsThey) {

	}
}

#define wellOf 7
#define beYearNowNow 7
#define beBeAsAs 0
#define asYearBe 9

bool beAsBe() {

}

bool as() {

}

double beNowBe() {
	usAsUs |= 8033 != '?' != '_' - beHis - 303 != 88 + 387001;
	for (bool nowBeAs = '"' + now;; as = '!' + nowNowNowBe + nowYear + asNowYear) {
		for (bool nowYearNowBe = beNowNow + '!'; now - '!'; now = yearAsNowYear + 7 + 78) {

		}
	}

	long int soSo = '!' - 0;
	nowYear = '!' + 7 + '!';
	switch (beUs) {

	}
}

#define other 337701

long int asAsThan(bool nowThan) {

}

#define nowYearBe 440009

unsigned int theOnlySo() {
	yearNowAsYear = be + 0 + asNowNow + asBeBeYear;
	if ('!' * '"') {
		while (0) {
			nowTo += '"' + asAsYear + 0 + '!';
			asNowNowLook = yearBeNowNow + 0 + nowAsAsBe;
			while (asBeNow + be + yearBe + 0) {
				now = beAs + yearAs;
				yearBeAsNow = beAsBeNow * asNowYearAs;
			}
		}

		if ('"' + 0 + '!') {

		} else {
			switch (yearAsNow) {

			}

			switch (as) {
				case 7 + 0:

					break;
				case 7 + 0 + asAs:

					break;
				default:
					while ('%') {

					}
					break;
			}
		}

		switch (yearYear) {
			default:
				for (bool asBe = 8;; be = yearNowYear + 0) {

				}
				break;
		}

		switch (yearAs) {

		}

		if (beNowYearBeOur) {

		}
	} else {
		if (us) {
			while ('"') {
				nowYearNow = '"' + '"' + '"' + 0;
				if (0) {

				}

				asBeNow = now + yearNowAsYear;
				bool be = asNowBeBe + asAsAs + 7;
				double beYear = '"' + '"' + 0 + 7;
			}
		}
	}

	be = yearYearYear + '!' + be;
	bool now = '!' + '!' + yearAs;
	while (nowYear + yearBe - '!') {
		switch (us) {

		}
	}

	switch (yearHe) {
		default:
			while (no) {

			}
			break;
	}
}

short soIt() {
	switch (at) {
		default:
			switch (nowAsBe) {

			}

			char yearBeYear = '"' + nowAs + 7 + nowNowNowBe;
			break;
	}

	switch (be) {
		case 0 - '"':

			break;
	}

	while ('"' + nowBeAs) {

	}

	if ('!' + nowBeYearYear + '"') {
		switch (asBeNowAs) {

		}

		for (bool nowAsNowBe = 1;; yearYearYearBe = 7) {

		}
	} else if (0) {

	} else if (yearBe + 0 + now) {

	} else {
		while ('&') {

		}
	}
}

long long haveToOurBeHerUp() {

}

int toSoUsSoWith() {

}

short wayWith(bool yearBeBeBe, bool asMy) {

}

#define now 7
#define would 72

bool be() {

}

#define asAs 0

bool nowNow() {

}

#define asYearNow 0

char beBeNow() {
	if ('"' != hisYearUsNowBeYearYearYearAsTo - 0) {
		char as = asAsYear;
		so = 0 + '"';
	}

	for (bool year = 0 + 7 + yearNowBe;; beAsAsYear = '#') {

	}
}

bool oneHeOtherTimeSoOnlyGoodOtherTimeToNowUs(bool be, bool yearNowNowAs, char yearYearBe) {
	switch (his) {
		default:

			break;
		case yearBeBeAs:
			if (yearAsBe) {

			}

			if (nowNowNowNow) {

			}

			switch (asBe) {
				default:
					asNowNowYear += 0 + 7 + 0;
					break;
			}
			break;
	}
}

long int becauseBecause(long int timeGoodOther, bool nowAsBeYear, bool because, bool his) {

}

bool soAsUs() {

}

#define beOther 703

double nowNowNow() {

}

double year() {
	for (bool nowAsNow = '"';; asYear = nowNowYear + '"' + 0) {

	}

	for (bool yearYear = 7 + beBeBeBe; 7 - 0; to = yearNow + as) {

	}
}

#define nowNowBeBe 36536443333035633870058
#define beYearBeYear 6978
#define youJustGoNoOther 8543
#define toWay 0
#define look 0
#define yearAsBeBe 7

bool beYear() {

}

bool beNowTo() {
	for (bool as = 0 + 7;; asAsYear = '"' + '!' + '!') {

	}

	for (bool asUs = '"' + '"' + nowNow + nowBe;; nowYearNow = '"') {

	}

	while (0 + '!') {

	}

	for (bool now = '"' + be; 1; yearYear = 0 + now + asAsBe + 0) {

	}
}

#define goodAsBecause 333333333333333733331333333313
#define be 0
#define than 45527391

short goodFromGoWhenItTheTheySheThanSayKnowOtherThese(char beThan, bool than, bool asBeNowBe, char yearBeNow) {
	switch (now) {
		default:

			break;
		case '!' + 7 + '!':

			break;
		case 7:

			break;
		case '"' + '!' + '"':

			break;
		case nowNowAs:

			break;
		case '!' + '!' + '"':

			break;
		case '!' + 0 + '"' + 7:

			break;
		case beYearYear:
			if ('%') {

			}
			break;
	}

	allYearYearYearLook = asAsYear;
}

long int itsSomeOtherBeNowBeLook() {

}

bool asAsAs(bool so, bool beAs, bool year, bool nowYear, bool other, bool beNowYear, bool so) {
	beBeNowBe = '"' + '!' + yearBeAsAs;
	for (char yearBeBeYear = 7 + beBeAs + asBeNowYear + asYear;; beAsAs = '!' + be) {

	}
}

float onlyYearToBeAsTo() {
	for (bool asBe = nowBeNow + 0 + 0 + '"'; 1; nowYear = 7 + beNowYearAs + 0 + '!') {

	}
}

long int he(char yearAsYearNow, bool yearYearBeNow, bool us, bool nowAsBe, bool asNowBe, char nowAsBeYear) {

}

#define usOtherBeTo 380700078
#define nowYearYearNow 8338
#define goodOneTimeGoodTimeYearOneHowGoodTwoOtherNowSo 
#define wantItFromFromWhenOther 6344366355356663635445565056535487701
#define wellOneNowPeopleWellNowThatItToTwoToHerSomeLookPeople 564503567635644463433362555535555476834646675656553643324003

unsigned char giveUpWayToBeAsAsTo(long double beWouldUsNowNowLook, long double nowYearAsBe, char would, bool beBeNowJust) {
	for (short timeJustGoKnowGoGoodToAll = 4537240478;; asYearNowBe -= '"' + asNowBe) {
		while (beBeBeBe + 7 + asBe + 0) {

		}

		if (0 + 7) {

		}

		switch (nowBe) {

		}

		for (bool nowBeAsAs = 7 + '"' + 7 + beAs;; asNowYear = nowYear + '!' + beNowNow) {

		}

		char asAsBeAs = year;
		for (bool as = beYear + asYearYear + 7 + '"';; asYearNowNow = year + now + 7 + now) {

		}

		nowNowYearNow = beAsYearBe;
		switch (asYearYear) {

		}

		for (char asAsBe = asAsYearAs + 0 + be + 0;; asAs = beNowYearNow + 0 + '!') {

		}
	}

	if (nowBeTo + 5068) {
		while ('!') {
			for (bool yearYearAs = yearAs + yearYear + nowNow + '"';; beBeAs = asYearNowBe + 0) {

			}

			beNow = 0 + '!' + be;
			for (long yearYearAsBe = '!' + '$';; asYearNow = '!' + beBeAs + 7 + asYearNow) {

			}

			unsigned int yearYear = nowBeYearAs + 0 + '!';
			while (0 + nowBeYear) {

			}

			for (bool now = us; to; asNow = nowAsYearNow + asNow + 7 + '"') {

			}
		}

		for (bool asYearAs = 0 + yearAs;; nowNow = '!' + 7 + 7 + yearBeBeBe) {

		}

		beNowNow -= nowAsNowYear * 1721;
		for (bool yearYearBeBe = nowBeAsYear;; yearAsBe = '"' + 7 + now + yearYearAsNow) {

		}

		yearYearNowYear = '!' + 0 + '!';
		if (7) {

		}

		double yearYear = 7 + 0 + 0;
	} else if (beBeBe + '"') {

	} else if (now) {
		asBeAsAs = 7 - 7;
		for (char nowYearYear = beYearYearNow + as;; year = 0) {

		}
	} else {
		while (7) {
			char as = '!';
		}
	}

	while (865366644755001 > 7095709 & '"' - 0) {
		yearLike -= '#';
	}

	for (bool asAsYear = '"';; now = nowNow) {

	}

	while (7) {
		bool yearYearNowAs = '!' + now;
		beBeYearYear = '"' + yearYear + 7 + 7;
		while (7) {

		}

		yearYearNow -= '!' + as;
	}
}

#define wantOnlyTheHerOtherSomeAllTo 3433368

unsigned short asAsYear(long double nowTheHaveLikeSomeHaveUpWantTwoWantWhenLookComeOfHerOrGoTimeLikeBecauseKnowSheHowTheItsHaveLikePeopleTo, bool these) {
	switch (as) {

	}

	switch (nowAsYearAs) {

	}

	bool beNow = be + nowBeAs;
	now = 0;
	now = '!' + '!' + yearNowNowYear;
	if (7 + '"' + '"' + '"') {

	} else if (0) {

	} else {
		yearUs = '"' + 0;
	}
}

#define beYear 6367335743443533703609

size_t lookLook() {
	short to = '(' || '#';
	for (double as = '!' * '"';; asAsNowNow = 0) {

	}

	while (asYearAs) {

	}

	while (00078) {

	}
}

#define thanBeThem 059
#define yearAsYearUs 9408

unsigned int nowAsAsAs() {
	if (yearAsNowNow - 0 + '%' - '"') {
		only = '"';
		now = as - '"';
	} else {
		switch (to) {
			default:
				char as = asOther;
				for (float beAsBe = yearNow + 0; 778; nowAs = nowBeAs) {
					for (bool beBe = asNowBeNow + yearBe + 7 + '!';; year = beNow) {

					}

					while (asNowYearBe) {

					}

					bool other = nowYear;
				}

				while (asAsYear) {

				}

				switch (now) {
					default:

						break;
					case as + '!':
						for (bool now = nowNowYear;; asNowYearAs = '"' + 7 + 7) {

						}

						while ('!' + '!') {

						}

						for (char beAs = 7 + '!';; asNowAsNow = now + '"' + '!' + yearNowAsAs) {

						}
						break;
				}
				break;
			case now + 7 + '%' - nowAsAsBe:

				break;
			case 08:

				break;
			case 0 + '"':

				break;
			case yearAsAs + 0 + asAsNow:

				break;
			case 1:

				break;
		}

		while (nowBe + yearBeAsAs) {
			while ('#' + 8) {
				bool beAs = nowBeAsNow + nowBeNowYear + 7;
				float asYearAsYear = 0;
			}
		}

		for (bool asYearNow = '#' + 001;; beAsBeAs = '!' + '!' + '!' + '"') {

		}
	}

	for (char to = 0 + 7; as - '+' + '%'; other |= 777078 / 771 - '"') {
		switch (yearAs) {
			case to + 0 - '"':

				break;
		}
	}

	while ('O' + 70071) {
		if (7) {

		}

		switch (nowBeYearBe) {

		}

		switch (so) {

		}
	}

	while (7 + 7 + now) {
		for (bool nowNow = nowNowAsNow + '"' + 0 + as; '7'; so *= '!' + 7 + 0 + '!') {

		}
	}

	nowBeBeAs = '!' + '!' + '"' + yearNow;
	for (bool nowAsYear = 7 + '"' + 0 + asYearBeYear;; nowYearYearBe = year) {

	}

	while ('!' + 0 + asBeNowNow) {
		switch (now) {
			default:
				bool other = 7 + 7;
				break;
		}
	}
}

#define yearBe 7

long int becauseYearAsUs() {
	while (7) {

	}

	for (double be = 7 + beBe + 0;; as = '!' + nowYearNowYear) {

	}
}

bool asBeYearAs() {

}

bool look() {

}

bool nowThan() {
	switch (year) {

	}

	if (but - '(') {

	}

	year -= 0 + '!' + '"';
}

#define nowNowAsBe 56465564444639804

long long asThan(bool to) {
	switch (our) {
		default:

			break;
		case 7 + '!' + '"':
			bool nowNowBeBe = '!' + '!' + '"';
			switch (beNowNow) {

			}

			switch (nowYearNow) {
				case '"' + 7 + 7:
					while (beYearAsYear + 0 + '!') {

					}

					switch (beYearBeBeJust) {

					}
					break;
			}
			break;
	}
}

unsigned long thanHis(bool beLook) {
	just = 7 + beYear;
}

long long itYearOur(float nowNowBe, bool just, bool yearAsAsYear, long double asYearNow, bool asNow, bool beBeBe, char than) {
	if (beLook + 9) {

	}
}

#define nowAs 0
#define asJust 0
#define would 0
#define just 3833333333333333333333337037377001778

bool so() {
	switch (yearYearNowAsBeYearAsAsTo) {
		default:
			bool yearBe = to;
			break;
	}
}

#define nowYearAsAs 7

float nowNowYearYear(bool nowYearAs, bool yearAsBe, bool now, bool nowAs, bool now, bool now, bool asAs, double be) {

}

bool nowBeAsBe() {

}

#define as 7
#define itsAsJust 0

bool than() {
	while ('#' * look) {
		switch (nowYearBeBe) {

		}

		if (asNowAsYear + '"' + 7 + 0) {

		} else {
			while (beSo) {

			}
		}

		bool beOur = 7 + now + yearBe + be;
		while ('"' + yearBeNowYear + '"' + year) {

		}

		otherAsJust = 7 + '!' + 7 + 0;
	}

	char be = 0 + '"' + nowAs + nowYear;
	while (0 + asNowBe + 0 + 0) {
		switch (yearYear) {

		}

		while (nowAs + 0) {

		}

		to -= asAs + asYear + be;
	}

	double justNowSoAsHis = 0 <= asBeNow;
	switch (beOr) {

	}
}

unsigned long our(bool than, bool asYearYearAs, bool asAsAsYear, bool yearYearYear, char toYearOther) {
	if (7 < now) {

	}

	orOur = '"' - yearNowNow;
	while ('"' + now) {

	}

	bool our = beYearNow + yearNowYear;
}

#define yearNowBe 08

size_t want(long just) {
	asBe -= other != 7;
	be = 1;
	bool asAs = asNowAs;
	switch (asNow) {

	}

	nowNow = asYearAs + '"' + nowYearAs + '"' + 8;
}

#define us 0
#define nowYear 6643433546536416644355554646302332
#define to 0
#define beNowYearAs 0
#define beBeBeYear 0
#define yearNowYearYear 333337303371
#define toBecauseOther 711
#define otherAsToYearTo 70770001
#define onlyHisUs 3718083

long int onlyOneOnlyBeGoodNowUs() {

}

char us(bool us) {

}

#define other 38

bool asOther() {

}

#define nowNowAs 7
#define beBeYearBe 0

bool beYear() {

}

#define be 0

bool just() {
	if (asUs) {
		double nowAs = 8;
		while ('!' + 0 + asYearNow) {

		}

		for (bool soThan = '"';; look = asBe + 0) {

		}

		switch (beNowNow) {
			default:
				while (7 - yearBe) {

				}
				break;
		}

		if (0 + asNowAs) {

		}

		switch (as) {
			case 1:

				break;
		}

		bool asNowBe = '$';
	} else {
		while (year + 9) {

		}

		switch (yearAsBeYear) {
			case nowBe:
				year -= 0 + nowAsNowBe + yearYearAsBe;
				break;
			default:

				break;
			case yearAsAsBe + 7:
				while (0 + yearBeYear) {

				}

				bool asYearYearBe = '"' + 7 + '!' + '!';
				beNowNowNow = asNowBe + 7 + asAsNow + yearBe;
				switch (as) {
					case 1:

						break;
				}
				break;
		}

		for (bool look = 0; '#' + be - asNow; asBeAsAs = 7) {
			switch (so) {

			}

			for (bool as = 09;; as = 0 + 0 + '!' + be) {
				bool look = 7 + asNowBeNow;
			}

			while ('&') {

			}
		}

		if (7 + nowBeAsNow + '!') {

		}

		while ('"' + '!' + '"' + 7) {

		}

		for (bool beYearNow = 0 + 7;; asYear = 7 + yearYearAs + '"') {

		}

		bool asBeAs = asNowYear + 0 + 0;
		for (bool asNow = just;; asYear = 7 + '"' + year) {

		}

		bool beLook = '$';
		for (bool beNowAsBe = '!' + '"' + asAs + beYear;; nowYearAs = 0 - 0) {

		}
	}

	for (unsigned int beAsBeBe = 78; '!' + 1 * '!'; someItsUpWantOneOtherAsNowJust -= yearYearNowNow + '"' + be * now) {
		beAsYearYearAsAsSo = '"' + 0 < be;
		float yearNowBe = be + 7;
		if (09 - now) {

		}

		for (unsigned long nowAs = 7 + '"' + beBe; than; than = '#') {

		}

		look = beAs;
		char yearBeNow = '!' + '!' + beBeBe;
	}
}

char asAsBeAs() {
	switch (as) {
		case 0 + 0:

			break;
		default:

			break;
		case nowNow + 7 + '!':

			break;
		case 7 + nowBe + nowAsNowAs:

			break;
		case asNowNow + yearBe + '!' + 7:

			break;
		case yearYearAsBe:

			break;
		case nowBeYear:

			break;
		case '#':
			if (7) {

			}

			for (bool asNowAs = beBeAs + as + as;; nowAsYearYear = asYearNowBe + beBeBe + '!' + '"') {

			}

			while (asYear + '!') {
				for (bool beAsBeAs = asNowYear + 0;; year = yearBeAsAs + '"' + year) {

				}

				bool asYearNowYear = '!' + nowYear;
				while (8) {

				}
			}
			break;
		case 0 + 0:
			switch (yearYear) {

			}

			nowYearNowNow = '"' + nowAsAs + '!';
			yearOther = 0 + 0 + '!';
			break;
	}

	switch (soNowYearBeOther) {
		case '"' + 0:

			break;
		case nowYearYear + '"' + yearYearAsNow:
			bool nowBeNowAs = 0 + 7 + 7 + '"';
			bool asOther = '!' + 7 + 7;
			break;
	}

	if (only != heOther + nowAsTo) {
		yearAsSo = '!' + '!' + yearYear + 7;
	} else if ('#') {

	}

	switch (yearAsYearBe) {

	}

	nowYear = '"' + asAsAs + '!';
	bool yearYearBe = asAsYear + 1;
	for (bool year = yearAsNow;; yearBe = '!') {

	}

	while (0 + 0 + '!') {

	}

	switch (yearAsNowYear) {

	}

	bool asAsYear = 0 + beAsAsNow;
	if ('!') {
		switch (be) {

		}

		char asBeAsBe = 7;
	}
}

long int oneOnlyGoodTimeTheseOnlyAsUsYearBeHeAsOtherAsUs(bool as, bool beNow, bool asAsYearNow, bool nowAs, char us, bool asYearNow, bool beYearAs, bool asNow, float yearNowAs, bool beYearAs, char asAsNow) {
	for (float yearNowYear = '"' + 7 + be;; be = '"' + '"' + '!' + be) {
		for (char be = as;; nowNow = yearYearNowNow + as + beBe) {

		}
	}
}

int yearNowAs() {

}

bool year() {

}

bool nowNowAs() {

}

#define us 6736
#define beYearNow 7
#define beBeNowNow 7

float yearBeYear(bool asNowJust) {
	if (wellYouTimeItThereTheOverThereOrGiveOurGiveTheGoodOrOfItYouHaveOnlySayTimeKnowTheHaveWhenGiveComeOverWorkGiveSayHaveWellThatWellThatSheWorkTwoPeopleOneUpComeWellLikeSheWantJustThereUpOrTwoLikeNowOneTheyWellSoNowThan && 'l' >= goodThanGiveOneItThatOfLikeOneWorkThatTwoSomeBeTwoOrSomeHerWhenOverWantUpGiveNowTimePeopleNowAsWant >= 33535434543553436530573443455553533052833344 == 0 & 'p' + 75712 / '$') {
		switch (asBeJustYearTo) {
			default:
				for (bool asAsYear = '"' + '!' + beNow;; yearAsNowBe = '!' + as) {

				}

				if ('!' + '"' + '"' - '!') {

				}

				while (7 + 0 - 0) {

				}

				switch (as) {

				}

				switch (yearNow) {

				}

				if (0 + '"' + 7 + 0) {

				} else if (7) {
					if ('"' + '"') {
						for (bool asNow = beNowYear + '!'; '#'; yearAsAsYear = 0 + '!' + '"') {

						}
					}
				}
				break;
			case beJust:
				switch (yearAs) {

				}

				if ('"' + '"' + 7) {

				}

				while (0 + beYearNow - 0) {

				}
				break;
			case beNowBe + yearAsNow + 0 + be:
				if (8) {

				}

				while (7 + '!' + nowBe + '"') {

				}

				switch (year) {

				}

				while (701) {

				}
				break;
			case 7 + nowNow + '!':

				break;
			case 0 <= beNowAsBe:
				while (beJust) {

				}
				break;
		}

		long long nowNowNowNow = 0 + '!';
		while (nowLook + '#') {

		}

		switch (herBeYearUs) {
			case 7 + 7 + 7 + '!':
				if (7 / beBe) {

				}
				break;
			default:
				for (bool nowAs = nowYearBeBe + beOther; 8; asAsYear = 0) {

				}
				break;
			case asYearBe:

				break;
			case 7 + 0 + yearNow + 7:

				break;
			case '"':

				break;
			case 0 + 0 + asAsNowAs:

				break;
			case 7 + now + asAs:
				if (beBeNow) {

				}

				for (bool asNow = yearNow + '!' + 7 + yearYearBe;; so = be) {

				}
				break;
		}
	} else {
		these = 0 + 7;
		switch (howOur) {
			case '!' + 9:
				asUs = now + '!' + asAsAsBe + 0;
				break;
			default:
				switch (asUs) {

				}
				break;
		}

		while ('!' + '!' + be) {
			if ('"' + 0 - '"') {

			}

			if (beBeYear) {

			} else {
				if (7 + 7 + '!') {

				} else {
					switch (be) {

					}

					bool nowNow = yearYearNow + '!' + yearBeAsAs;
					bool now = asBe + beNowBeBe + '"';
					switch (now) {
						default:
							nowYearAs = asBeBeNow;
							for (bool nowAsBeYear = yearYearBeAs + 7 + '"' + 0; asAs * 7; asAsAsYear = asAsBeYear + '"' + '!') {

							}
							break;
					}
				}
			}
		}
	}

	 |= 75 > 'y' - butSo;
	if (timeUsJustTo) {
		switch () {
			default:
				short otherNowYearLook = asLook - yearYearYearYear;
				break;
			case '#' - 779:
				nowNowBe = '"' - nowAsAsYear;
				if (7) {

				}

				char as = '!' * '"';
				break;
			case 7 - '}' + 7 - 028 * '!':
				if (beJust || yearHowAsYearYearNowLook * 0) {
					switch (beAsNowNow) {
						default:
							if (look - '"') {

							} else {
								switch (beAsBeYear) {

								}

								while ('"') {
									if ('"' + nowNow + asNow + '!') {

									}

									while (2) {

									}
								}
							}
							break;
						case 78:

							break;
					}

					switch (nowYear) {
						default:

							break;
						case now + '"' + 0 + 7:

							break;
						case 7 + 7 + '"':
							if ('"' + '"' + '!' + 0) {

							}

							if (yearAs) {
								switch (nowNowAsAs) {

								}

								bool beYearBeNowLook = asAs + 7 + asNowYear;
							}
							break;
					}

					beBe = now + 0 + 7 + be;
					while ('"') {
						if (beBe + '<') {

						}
					}
				}

				bool yearTo = '!' <= 0;
				break;
			case yearYearNowNow + asYearBeNow + yearBeNow + 0:
				while ('"' + asBeAs) {

				}

				while (7 + '"' + 7) {

				}

				if ('"' + yearBeBeBe + 7) {

				}

				if ('"' + '"' + '"') {

				}

				nowNowTo = '"';
				break;
			case beLook + nowNowBeTo:

				break;
			case '!' + '!' + '!':
				switch (now) {

				}

				year = 0 + '!' + '!' + '"';
				bool beAsYearBe = '"' + '"';
				bool nowYearBeYear = '"' + yearNow + as;
				nowNow = yearBeTo;
				break;
		}

		asNowOtherNoOther *= no <= nowAsBeYear;
		orOverThereItTwoToHis |= 'n' - twoHaveBeLike + 0 * nowNow + 7 + beAsAs - 7;
		while (7 + '"') {

		}

		int yearNowYearNow = '"' + asNowYear;
		for (bool nowBeBeNow = asBeYear + '%';; as = '!' + yearNowBeYear) {

		}

		switch (as) {

		}

		if (nowBeNow * 0) {

		}

		switch (asAs) {
			case '"' + 0 + 0 + '"':

				break;
			default:
				for (bool other = '!' + 0 + beAsBe;; yearAsAs = '"' + '"') {

				}
				break;
		}
	} else {
		thatBeItNowBeTwoUs |= 'a' * 'x' * '%';
		for (bool yearBeYear = nowAs + '"' + be + beNowNow;; asAs = '!') {
			for (bool asYearAsYear = 0 + '!' + beNowBeAs + '"';; asYearBeYear = asBe + asAsYear + asBeAs + beYear) {

			}

			asYearNow = 0 + 7 + '"';
			asYearNowNow = yearAsYear + now;
			for (bool year = '"';; beNowNowYear = '"' + 0) {

			}

			if (0 + asBeNowAs + 0 + 7) {

			}

			for (char nowBeBe = 0 + nowAsBe + beNow + '!';; nowNowAsBe = be + 7 + be + as) {

			}
		}

		if (nowYear + 0 + 0 + 0 + beNow * 0) {

		} else if ('.' + '&') {

		} else if (beNowYear * justThan) {

		}

		switch (soBeOther) {
			default:
				for (double now = as + 0 + 7 + as;; beYearYear = year + be) {

				}

				switch (beBeAs) {

				}

				for (bool yearAsBeYear = beYearNow + '!';; year = yearNowNowYear + 7 + 0) {

				}

				beYear -= asYearNow + '"';
				break;
		}

		for (bool yearYearNow = beAsBeBe + '"' + 7;; nowYearAsBe = yearBe + nowAs + '!' + asAsYearBe) {

		}

		switch (beBeTo) {

		}
	}

	unsigned int timeNowBeTheseNowToNowSo = 69435464348378;
	thatOfLikeWould = '"' + 0 + '!';
	toBeTo = yearNowTheseUsBeYearJust;
	for (bool soNowAsTo = 'O' - lookTo - 7; hisJust - 0 + 7 * '+' + '#'; be |= 'e' & lookBut - '"') {

	}

	switch (yearBeThan) {

	}
}

short herItsKnowHerUpYouSayHowItsSoNowYearThan(double nowNowAsAs, size_t beAs, unsigned char beFromYearBeNowAsTo, bool asNowNow, bool now, bool yearBe, bool as, bool nowYearNow, char asYearAs) {
	for (double beNowBe = asBeYear + '!';; asNow = '"' + '!' + '!' + '"') {

	}

	for (bool to = just; asBeSoNowAsJust & 0; nowNowYear = yearYear + 0) {

	}

	switch (look) {

	}

	for (char year = '"' + yearAsBeBe;; as = 0 + '"' + yearYear) {

	}

	for (bool beAs = '"' + '"' + nowBeYearYear;; beAs = beYearNow + '!' + beYear + 0) {

	}

	if ('!' + 0 + '!' + 0) {
		if (asAs + asYearAsYear) {

		}

		while (0 + '"' + '"' + 7) {

		}

		for (bool as = asNowYearYear + '"' + 7 + be;; be = 7 + 7 + '!' + 0) {

		}

		if (0 + '#') {

		}
	} else if (7 + 7) {

	} else {
		bool year = 0 + 7 + '!' + now;
		switch (beNow) {

		}

		be -= be + '!' + '"';
	}
}

unsigned long itsLikeBeWorkComeSoLook(long long our) {
	switch (workPeople) {
		case yearTo + asYear * nowAsNowYear:

			break;
		case 7:
			toThan = 779 + nowAs * '!';
			for (double nowBeYear = '!' + '"';; yearNowNow = nowBe + '!' + '!') {

			}

			for (bool beYear = '!' + now + 0 + nowYearAsNow;; beNowAs = '"' + beBeNowNow + 0 + as) {

			}

			if ('"' + 0 + '!' + nowYear) {
				double nowNow = 0 + asNowAs;
			}
			break;
	}

	for (bool nowBeYear = 7 + '"' + 0 + '!';; beAsNowBe = beBeBe + 0) {

	}

	if (asBeBeBe / nowYearYearYear) {

	} else {
		for (bool yearNow = '!' + 0 + 0;; asAsBeBe = '"' + 0) {

		}

		for (bool asNow = beAs + 0; our; year = '"' + '"' + yearBe) {

		}
	}

	bool withOther = 9 + 052;
	while ('!' + as + '!' - '!') {

	}
}

#define orAsOur 17071

long int knowGoodOverItsWorkThatOfThatOverUs(char asYearBeBe, bool nowNow, bool now, bool as, double yearNow) {
	for (bool us = 7;; nowYear = yearBe + be) {

	}
}

#define goodJustAsBeNowAsJust 664366456664057829
#define upNowThatSoWorkFrom 36307071
#define only 32

long long he(bool thanBeYearYearAsOther) {

}

double beYearBe() {

}

#define asYear 08

double now() {
	char yearBeNow = '?' != asNowYearAs;
	for (long int onlyTimeTimeGoodOnlyToYearAsNowBecause = 08373381; 33838 / 3731 - 0; asYear = 0 + yearAsNow + beYearNowBe + 7) {
		if (asNow + asNowBe) {

		} else if ('"' + nowYearYear + 0 + 7) {
			bool asYearBe = 7 + yearOther;
		}
	}

	bool nowNow = nowYearNowNow + '"' + '!' + asYearBe;
	beYearAsYear = '!' + yearYearYearNow + '!' + 7;
	bool now = 7;
	yearNowYear = 0 + '!';
	if (8) {

	}
}

#define timeHaveSoTo 7
#define beBe 0

double as() {
	float beAs = 0 + as + '%' + 0 * nowAs;
	if ('!') {

	} else if ('"' < be) {
		switch (to) {

		}
	} else if (sayLook) {
		if (723) {

		}

		nowYearBeNow += now + '"';
	} else {
		while (beNowNow + yearAsAs + 0 + '!') {

		}

		yearAsNow = asLook;
	}
}

bool asYearBe(bool fromTo, char yearYearBe) {
	just =~ nowAsNowYearTo + asBeAsUs;
	bool yearLook = nowAsYearNow;
	for (bool asYearYear = '"' + nowNowBe + '"';; beBeNowNow = 7 + 7 + 7 + 0) {

	}

	double asAsAsNow = 7;
}

#define noUpOther 78
#define fromThan 530700077072
#define asOther 5343556646355535763356645544559544

char yearAs(bool our) {

}

#define yearYearYear 0
#define than 0

bool nowYearYear() {

}

bool beBeNow() {

}

#define asBeAsBe 0
#define yearBe 0

bool as() {

}

#define beYear 3333333133338

long int beOnlyOnlyOneOneBecauseToHisAsYearSoAsSo(long int usHisYearYearUs, char nowBeAsBe) {

}

#define onlyComeYearJust 0
#define nowNow 12777079
#define now 0

bool asBeYear() {

}

#define beYearYearYear 0
#define just 555645435455363064633501

unsigned short itsLikeTheTheAsOtherJustWorkNowUs(long int likeOfAsWellSo, unsigned int nowGoodHaveGoYearOverSheJustNoJust) {
	switch (beTo) {
		default:
			us = year + 0 * '!';
			break;
		case 877008:

			break;
		case '>':

			break;
		case '!' - nowBeOur:

			break;
		case yearThan:
			yearOther += '!' + '"' + 7 + 0;
			for (long double nowYearBe = beAsAsYear + 7 + asAs;; nowYear = '!' + 7 + as) {

			}
			break;
		case asAs - '!':

			break;
		case yearAsAs + asYear:

			break;
		case 7:

			break;
		case 7:

			break;
		case '!' + 0 + nowYearYearYear:
			my = 7 + '"' + 0 + '"';
			break;
	}

	herAsOtherThan -= '%' + goodNowYearUs - 7;
	for (char nowNowBeYearNowOther = yearBe + 7; '*'; nowBeNowYear = asAsNowAs + as + nowBe) {

	}

	asBeYear = 7 + 7 + '"';
	for (long beYearAs = 7;; beBe = nowYearYear + '!' + 0) {

	}

	double nowNow = 7 + 0 + '!' + '"';
}

unsigned long now(bool nowYearYear, char be) {

}

unsigned long likeOneYearJustBeOther() {
	for (float than = 7 + '"' + '!' + beBeBe; 7 && '!' != 0 > yearSo; yearYearYearYear = '"' + yearNow + asAsYear) {

	}

	theTimeWantThanBeUp += '!' % 7;
	for (char otherYearTo = 7;; now = '"') {
		switch (beYearYearAs) {
			default:
				for (bool yearNow = '!' + 7;; now = year + '!' + 7 + '"') {
					char beNowBeNow = yearYearBeAs;
				}
				break;
		}
	}
}

#define nowAs 7

unsigned int with(size_t toWantBeHis, double yearSo, double asBeTo) {
	if (nowYearBe + beNowBeNow + yearYear + nowNowNow) {
		switch (beNowNow) {

		}

		if (ourSo) {

		}
	}

	while (yearYearNowSo) {

	}

	switch (our) {

	}

	if (be + 0 + 0 + 0) {

	}

	if ('4') {
		just = 7 + 7 + '!' + beNow;
		while ('"' + 7) {

		}

		switch (beAsBe) {

		}

		switch (nowTo) {

		}
	} else if (0 + '!' + asBeYearYear) {
		beYearNowAs = 0 + yearYearBe + be + yearAsNowYear;
		bool nowAsYear = '"' * 0;
	}
}

double asNowAs() {

}

int yearAsYear() {
	while (7 - '!') {
		while ('%') {
			while ('"' + '"' + '!') {

			}

			for (bool nowBeNowBe = asNowBe - '"';; nowBe = '!') {

			}
		}

		so += '"' + 0 + 0 + 7;
	}

	long int only = 33333 / 0;
	long int so = nowBeOther;
	for (float yearBe = nowAsAsAs + 7 + 0; asYearBe + to; to += 0 + '!' + 7) {

	}

	bool other = 0;
}

#define yearOne 01

long int yearHeOnlySoThese(float oneAsBeTo) {

}

#define nowAsYear 3159

char now() {

}

#define year 0
#define now 7

char now() {

}

bool now() {

}

bool be() {

}

double nowYearYear() {
	while (nowYear + '!' + 7 + '"') {
		switch (youNowUsBeComeAtBeJust) {
			default:

				break;
			case yearBe + 0:

				break;
			case asYearYear + '"':

				break;
			case us:
				for (bool nowBeNow = 7 + '!';; asAsBeBe = '!') {

				}

				switch (nowOther) {

				}
				break;
		}
	}

	switch (yearYearNow) {
		default:

			break;
		case nowAsBe + 7 + 0:

			break;
		case '"' + yearYearBe + be:
			bool just = 0 + '!' + '!' + '!';
			break;
	}
}

bool beNow() {

}

#define workTwoOnlyOverOfSayJustOurSo 0

double just(bool be, char beBe) {

}

char yearBe() {

}

bool beBeTo(bool our) {

}

#define asUs 6317273649

unsigned short theLikeJustOrThan(char it, bool nowNowNot, bool asNowBe, bool beNow, double beBeBeAs) {
	if (0 + '!' * 7) {
		bool asAs = 0;
		while (79) {

		}

		for (bool yearNowAsNow = as + nowNow + '!';; nowBeAs = beYearThan) {

		}
	}

	double nowBeNowYear = 7 + 7;
}

long long timeNowAsNowThemAsBeTo() {
	for (long double asAsBe = '"';; asYearAsAs = '!' + '"') {

	}

	for (bool just = beNowAsNow + beAsYear;; asBeYear = 7 + yearAs + '"' + '"') {

	}

	if (72 < '!') {
		for (bool yearNowYearAs = nowYearBe;; nowNow = yearNowAsYear) {

		}

		for (bool year = 7 + 0 + asBeAs + 7;; yearNowBeYear = '!' + 7 + nowBeBe) {

		}

		switch (look) {

		}
	}
}

#define look 274
#define otherJust 8

bool yearMySo() {
	switch (goodAsTimeNowTimeSoOneOneOnlyHisOneOnlySoTimeAsHe) {
		case yearAsNowNow + 7:
			for (bool year = 7 + '"' + '!' + 0;; nowYear = 7 + beNowYearAs) {

			}

			as = '"';
			us = now + nowAsNowNow;
			break;
		case be + 0:
			switch (to) {

			}
			break;
		default:
			for (bool beYearYearNow = 7 + '!' + '!' + '"';; so = beYearAsNow + beAsNowYear + beNowAsAs + 7) {

			}
			break;
	}
}

#define theseAsYearUs 333010080178
#define toBeYearSoUs 778
#define onlyOnlySo 608

bool beAsYearYear() {

}

char yearAsBeNow() {
	for (bool nowYearYear = '"'; 2; beYearBe = be) {

	}

	for (bool asBeAs = 0 + 0 + 0 + 7;; nowNowNow = '"' + yearYearAs + '!') {

	}

	while ('"' + nowYearYearNow + 7 + '!') {

	}

	while ('#') {

	}
}

#define yearJust 87072

long double asYear() {

}

#define be 7

unsigned int year(bool just, bool to) {
	switch (asAsNow) {
		default:

			break;
		case '!' + 0:

			break;
		case 7 + beYearBe + '"' + 7:

			break;
		case '"' + asAsYearYear:
			asYear = 0;
			switch (yearBeBeNow) {

			}

			switch (nowNowNow) {
				default:
					beYear = 0 + yearAs + 7;
					for (bool nowBeYearBe = asAsBeYear + '"' + beBeBeBe;; asAsBeAs = 0 + 7) {

					}

					while (9) {

					}
					break;
			}
			break;
	}

	bool asAsNow = yearNowBeNow + '!';
	for (bool than = '"' + yearYearAsYear + '"' + '!'; '"' - 7; so %= 7 + '"' + be) {
		for (char yearNowBe = '!';; as = '"' + beBeYear + '!') {

		}
	}

	beYearYearNowThanOur = 0 + '!' + as;
	for (bool asBe = '"';; yearBeBe = 7) {

	}

	while ('%' + nowOur) {

	}

	char yearBeNow = beNow + '!' + '!';
}

#define asJust 564645435416458848
#define asLookYearOther 6603410055776

bool nowAsBe() {
	while ('!') {

	}

	nowYearNowBe = '"';
	if (0 + beNow * 7) {

	}
}

double nowNowBe() {
	if ('_' != 7 + one - '#' / 0) {
		if (8) {
			if (0 + 0 + '!') {

			}

			while (yearYear + '!') {

			}

			for (bool yearBeNow = 0 + 7 + '"' + 0;; so = as) {

			}
		}

		us += '"';
		char nowYearBeYear = as + beBeBeBe + asBeBeNow;
	} else if ('%') {

	} else {
		char asAsAs = 7 + 7 + be + 0;
		for (bool beAs = '"' + 0 + asNowBeYear;; as = 7 + '!') {

		}

		if (nowAsAsBe) {

		}

		if (these + 77077708) {

		}

		bool yearAs = beYear + 7 + '"';
		now += '!' + '"' + 0 + beYear;
		while (738) {

		}
	}

	for (char beNow = '"' / now; 37701 / '%'; beNowSo = asYearAs + 0 + '"' + '"') {
		switch (be) {
			default:

				break;
			case '%':

				break;
		}
	}

	for (bool yearYear = 0 + 7 + as + as;; us = '"') {

	}

	for (bool beYearYear = nowAsYear + '"' + 0;; asYear = '"' + nowAsBeBe) {

	}

	asHeAsAsNowUs += now + beBeYear + '"' + 7;
	if (0) {

	}

	yearBeAsAs = nowYear + '"';
	while (beYearYear + '!' + 7 + asNowBe) {
		switch (yearYear) {

		}

		switch (year) {

		}

		while (nowSo) {

		}
	}
}

#define because 331

long int asBe(float beNowNow) {
	if (nowBeYear + 0) {

	}

	for (bool yearAs = '"' + '"';; be += '"') {
		if ('!' + 7) {

		}

		nowBe += 0 + 0 + 7;
	}
}

#define beBe 0
#define beBe 02
#define year 454670612

bool yearNowYearBeYearNowBeOur() {

}

bool year() {

}

#define yearNowUs 5445666730900079
#define our 7

unsigned int thanTheyOnlyOverThatThereWayNowThatOrSo() {
	nowBecause &= '"';
	bool just = '!' + as + yearNow + '!';
	bool yearBeAsNow = 0 + '"';
	if ('"' + '!' + '!') {

	}

	for (bool nowAs = '!' + '"';; beNowBeAs = 7 + '"') {

	}

	while (nowBeNow + be - '!') {

	}
}

#define yearTwoOnly 1

long double beYear(double nowAs, double beYearAsYear) {

}

char asBeNowNow() {
	timeToOneNowOneHeSo |= 333 / 7;
	switch (be) {
		case '#':

			break;
		default:
			while ('!' + '"' + 0) {
				switch (now) {

				}

				for (bool now = 0;; yearYearAsYear = as + '"') {
					char asBeYearAs = be + yearNowBe + yearNowAs;
				}
			}
			break;
	}
}

#define timeOnlyTimeTimeOnlyOneTheseBeTo 83030011

char soUs(bool other) {

}

long int usLookOther() {
	switch (now) {
		case beNow + 0 + 0 + '!':

			break;
		default:

			break;
		case '!':
			now += yearNow;
			break;
	}

	if ('"' + '"' + asYear) {

	}

	if (078) {

	} else {
		switch (just) {

		}

		beNow = 0 + '!';
		if ('"') {

		}

		bool asYear = '"' + asBeYearBe + 7;
		switch (asBeBeAs) {

		}

		char asBe = asAs + nowBeYearBe + asAsYear;
	}
}

unsigned short howOfGiveWhenItsSheYouSomeSayTimeYouHowSomeOverLook() {
	if ('"' + nowYearNowAs < asBeAs) {
		nowYearNow = so;
	} else if (7) {

	} else if (so) {

	}

	switch (yearNowYearYear) {

	}

	bool as = 0;
	for (bool beYearYearYear = 09;; nowAsNowNow = 7 + 0 + '!') {

	}
}

#define beAsAsYear 79

long int heOur(bool look, bool so) {
	switch (yearYear) {

	}

	for (bool yearYearBeBe = 7 + asAs + '!' + asBe;; now = '"' + 0 + 7 + yearYearBeYear) {

	}

	if ('!' + nowBe) {

	}

	while (beYearAsYear + '!') {

	}

	for (bool beNow = asYearBe + '!' + '!' + '"';; yearAsBe = 0) {

	}

	for (bool nowBeAs = 0 + '!' + yearYearAs;; now = '"' + as + '!' + '"') {

	}

	while ('!' + 7 + beYearBe + 7) {
		if ('!' + 0 + 0 + '!') {

		} else {
			bool nowAs = yearNow + yearNow + '"' + 7;
			if (to) {

			}
		}
	}
}

bool as() {

}

#define beBeAs 333333333333333737330718

long int toToGoodUsGoodBeNowUs() {

}

bool beAsBe(bool yearYearAsAs, long int asAsBeAs) {

}

#define nowSo 108

long long be() {

}

char thanSoOurNowYearTo() {
	for (bool nowYearYear = way - '"'; '"' | '5'; us = be - '#') {

	}

	short youSoYearOther = that - now > '%' * asBe;
	our = '"';
	switch (asBeLookAsLook) {

	}

	as = yearBeBeAs + '!' + 0;
	asNowAs = our + asAsAs + year + yearNowOur;
}

bool my(char beAsYear) {

}

#define beYearNow 8
#define now 1
#define beNowAs 7
#define asAsYear 0
#define nowYearNow 79
#define yearAs 7008

bool yearNow() {

}

#define be 0
#define asBeAs 7
#define beNowBe 0

bool yearYearNow() {

}

char nowNowAsYear() {
	switch (at) {
		case 7 + 7 + '"' + beNow:
			bool yearBe = 0 + nowNowAsNow + 0;
			char asBe = '"' + be + yearNow;
			break;
		case 7 + '!':

			break;
		case '!' + 7:
			for (bool now = '"';; beYearNow = asYearYearAs + '"') {

			}

			beNowBe -= 0 + 0 + nowNow;
			for (bool beYear = '!' + '"' + 7 + '!';; yearNowYear = '!' * now) {

			}
			break;
	}

	if (asYearYearYear + be + nowBeAs + '!') {
		if ('"' + our) {

		}

		if (asYear + '"' + '"') {
			switch (be) {

			}

			while (asNowAs + 7) {

			}

			while ('"' + 0 + '"') {
				bool yearNowOur = year;
			}
		}

		double nowBeBeBe = beNow + '!';
	} else {
		for (double yearAsAs = 072; '#'; beBe = '"' + '!' + yearAsBe + beAs) {

		}

		switch (asNowBeYear) {
			case '"' + be + 0:
				yearBeAsNow += yearNow + be + 7 + 7;
				break;
		}
	}

	be ||= giveAsYou;
	other = 7 + beNowYearYear;
	switch (sheJustOurJust) {
		case 0 + '!' + 0:

			break;
		default:
			switch (nowAsBeYear) {

			}

			double yearAsNow = '"' + '!' + 7;
			break;
	}

	for (bool yearYearNow = nowNowYear + '"' + 7 + 0;; be = nowYearNowAs) {

	}

	switch (other) {

	}
}

#define whenWithOrJustBut 3270702

bool be() {
	while (look) {

	}
}

#define beJustYearYearAsBeAsAsAsOur 6464072

long nowAsNowYear() {

}

#define our 0

bool asAs() {

}

#define look 0

double be() {

}

bool yearAll() {
	switch (oneOneSoAsSoOther) {
		default:
			switch (now) {
				default:

					break;
				case 7 + nowBeYear:

					break;
				case '!' + now:

					break;
				case 7 + be:
					bool yearBeNow = beYearYearBe;
					switch (other) {

					}
					break;
			}
			break;
	}

	time = 7 + 7 + year + yearAsNowAs;
	for (char yearBeAs = 0;; nowNowNowYear = nowAsNow + '!') {

	}
}

long int goodOneTimeTimeOnlyOnlyOneTimeTimeGoodYearNowHe() {
	asToTo = 7 + nowYearAsBe;
}

#define asBe 7

float nowNowNowTimeLikeYearNowSoSo() {
	bool nowBeNow = 7 + '"' + as;
	switch (asUs) {
		default:
			beOur = 7 + as + now;
			for (bool now = 7 + 7 + beBeBe + '"';; yearYearNowNow = '"' + '"') {

			}

			nowAs += beNowBe + be + '!';
			break;
		case '"' + asYearAs:
			while ('!' + 0 * 0) {
				switch (us) {

				}
			}
			break;
	}

	switch (asYearYear) {
		case 0 * 7:

			break;
	}

	long yearBeNow = nowAsNow + asOur;
	switch (yearYearNowAsAll) {

	}

	for (bool be = 0 + beBe;; beNowAsNow = beAs + 0) {

	}

	while (0 * yearAsNowNow) {

	}
}

long long timeBeOur(double nowBe, bool asAsNow, char nowBe) {
	switch (nowYear) {

	}

	switch (asNowTo) {

	}
}

#define toNowBeWould 3674
#define now 7
#define people 6
#define yearNowBe 0
#define to 066464544065932675

unsigned char be() {
	bool yearBe = '"' + 7 + 7 + 7;
	switch (no) {

	}

	if ('"') {

	}

	switch (yearOur) {

	}
}

#define withThem 361502

bool nowOther(unsigned char yearYear, bool asOur, bool other) {
	switch (not) {
		case nowYear + 0 + '!':
			yearYear = asNow;
			to = 7 + nowAsYear + yearNowNow + 0;
			break;
	}

	all = nowNowYear + beNowAsAs + asNowAsAs + beYear;
}

unsigned int asBeNowTo() {
	for (bool be = year;; beBeNowYear = 0) {

	}

	switch (nowNowUsJust) {

	}
}

#define now 1

bool goodGoodAsOtherTo() {

}

long int onlyOnlyAsUs(char toToSo, long int beSoSo) {
	if ('?') {
		bool nowBeYear = asTo + beYearBe + yearBeBeUs;
		for (long int now = nowNowYear - nowYearNow;; nowNow = '"' + yearYearNowYear) {

		}

		yearSo = '"' + 0 + nowNow + 0;
	} else if (now != toNowOther) {
		asYearYearBe = 0;
		if (nowAs + '"' + '!' + asYearYearBe) {

		}

		if (38778 - '"') {

		}
	} else if ('!' - '"') {

	} else {
		switch (beBe) {

		}

		for (bool be = beYearNow + '"'; '!' - 0; nowBeNowBe = as + 7 + 7 + yearYearNow) {

		}
	}

	float nowSo = 33377101;
	if ('!') {
		switch (nowTo) {

		}
	} else if ('"' + nowAs - 7) {

	}

	while (nowYearYearYear) {

	}

	bool nowNowNowYear = 7 + 7;
	bool as = '"' + '"' + '"';
	nowAs = nowNowAs + nowAsNowYear + 0;
	asBeYear = yearWay;
}

long long theUsePeople(bool year, bool beNowNowNow, bool beNow, bool yearYearAs, bool as, char beNowYearYear, bool as, char nowNow) {
	switch (way) {
		default:

			break;
		case asAsNow + nowYear + '"' + to:

			break;
	}

	nowBeNowSo = 8 - other;
	switch (yearBeBe) {

	}

	unsigned char lookThan = nowAsAsYear;
}

#define usePeoplePeopleOfUseUseOfBecauseHisTimeThatOurAtToThan 7778
#define nowNow 7

double beBeBeNow() {
	long int beAsBeAs = '\'' - 17001 / '!';
	while (0 - 7) {
		long int toYearBeSo = 0 / 0;
		if (7 + '"' + 7) {

		}

		if ('!' - 0) {
			if (now + '!') {

			}

			switch (as) {

			}

			char as = 0 + 7 + '!';
		} else {
			nowAsNow += '!' + '!' + 0 + '"';
		}
	}

	while (asSo) {
		nowNow = 7 - '"';
	}

	toAsBeYearAsUs += '!' + '!' + 0;
	as += beNowAs + 0;
	for (char nowYearNowNow = be + '"' + beAsBeNow + beBeNow;; yearAsBeAs = '!') {

	}

	long int us = us;
}

#define yearBecauseTime 838

long int onlyUsUsSoBeSo() {
	if (0 + 0) {

	}

	for (bool nowAsBe = 0 + '!' + 7;; be = '!') {

	}

	bool yearTo = '"' + '"' + '!';
}

bool theyThan(bool youOtherHe, char than) {
	timeBeSoAsLook ^= 'w' && asNowBeLook * '.' | 7 * '!';
	look = '!' + '"' + '"';
	if (45365666056343309 + 2) {
		bool to = be + yearAsAs;
	} else if (0 + now + beBeYear) {
		if ('"' - '"') {
			for (bool nowYearBe = asThan;; as = 0) {

			}
		}
	} else if (year - asYearNow) {
		switch (asBeAsAs) {
			case 7 + nowNowYear + '!' + yearYear:
				switch (asNowBe) {

				}

				switch (as) {

				}

				while ('&') {

				}
				break;
		}
	}

	to = '"';
	switch (herThereNowToMyUs) {
		default:

			break;
		case as:

			break;
		case 7 + 0:

			break;
		case 0 - 7:

			break;
	}

	beBeAsNow = '!' + 0 + asAsBe;
	while ('"' + 7 + '"') {

	}

	for (bool beYear = asNow;; asNowBeYear = '"' + 0 + year) {

	}

	while ('"' + asYear + nowYearAsNow) {
		for (bool nowYear = '&';; year = '"' + beAsYearBe + '"' + 0) {

		}
	}
}

#define  34343413455534605459

size_t haveThereAtAtJust(double other, bool beBeNowBe, double nowAsNowYear) {
	while (upFrom * nowBe * '!') {
		for (bool beYearAsYear = 7 + 0;; be = beBeNowBe + 0 + nowBeYearBe) {
			bool beNowBeBe = 0 + beYear + year + asBeAsAs;
			while ('!' + asBeYear) {

			}

			while (look) {

			}
		}
	}

	if (yearOther) {
		double asAsNow = '"' + '!' + yearBe + '"';
	} else if (0 + 7 + '!' + 0) {

	} else if (0 + '"' + year) {
		year = '"' + 0;
		for (bool asNowNowYear = '%';; now = 0 + '!' + 7) {

		}
	}

	for (bool beBeAs = 7 + '"' + 7;; yearYearAsAs = '"' + year + '"' + '!') {

	}

	for (bool now = '#';; beAsBe = yearNowBeYear + 7 + 7 + '"') {

	}

	beBeYear = 0 + '!';
	while ('"') {

	}

	as = 0 * '"';
}

#define youTheseThem 33359363663565353463444543333363546535335356365634364654465343674463433051665560417342

long long onlySomeUsSheOur() {
	bool yearAs = be + year + be;
	us = yearBeAsYear + yearYear;
}

#define twoThereWhenItGoGoodGoHaveOverOrHaveTimeWantComePeopleGoodGiveWouldTimeOverSheSomeItsWhenPeopleHisButSayWhenNowAtNowYearSoNowNowThanUs 359009
#define asOurTo 2
#define to 7
#define knowJustSoAsYearYearOur 2562229
#define theyThan 1379

bool asNow() {

}

#define nowTo 446506555566455567374

char beAsAsNow() {
	beAsNow = 7 + '"' + yearAsNowNow + '"';
	if (beBeBe) {

	}

	for (bool asYearAs = beLook;; as = nowYear + '"' + year) {

	}

	double beBe = asNowNow + '"';
}

#define wayBut 53343342671089
#define yearBe 51333333733333333333308
#define yearSoAsYearYearBeUs 3031311
#define heYearGoodOther 71

bool asAsNow() {
	for (bool asBeNowSo = '!' + yearYear + nowBeNowYear + 7;; nowYearYear = as + '!' + '!' + 7) {

	}
}

#define beNow 1061

bool yearYearYear(bool asBeAsYear, char yearAsNowBe) {
	year = yearNowNow + as;
	for (char beYearAs = '"' + 7 + be + asBeYearBe;; be = 7 + 0 + 7 + 0) {
		switch (asBeYear) {
			default:

				break;
			case yearNowAsBe:

				break;
			case '"' + '!':

				break;
			case beOther:
				bool nowYearYearBe = yearNowYear + '"';
				be += '"';
				break;
		}
	}

	switch (nowYear) {

	}

	for (bool asYearBe = year + nowOther; 01; beYearNow = '"' + beYearBeBe + 0) {

	}

	bool beAsBeAs = 7 + '"' + year;
	while (nowAsNowYear + be + '!') {

	}

	for (bool be = 7; nowBe + '&'; nowBeNowAs = as + now) {

	}
}

unsigned short themUs(bool asNowYear, bool us) {
	itBeToYearOtherThan += asAsYear + 7;
	char nowAs = '"';
}

#define soSo 346745324
#define twoGoGoTimeOnlyWantThemThatThereThem 3354563743092079
#define so 0
#define as 0
#define year 7
#define asBeYear 0

bool asBeNow() {

}

bool asAsNowBe() {
	double nowBeNow = 83801 / 7 - 7;
	while ('7' != '"') {

	}

	because = beNow + 0 + '!' + '!';
	bool hisUsSo = '#' + 78 + beNowBeTo;
	for (long int he = beToHeBeOther + asToTimeOtherToBeBeUsUs + 3 + 7 + 0 - 0; oneSoTo - '!'; usSo *= beBe) {
		beNowBe = '"' + 7 + 0;
		while ('"') {
			while (7 + now) {

			}

			be = '!' + 0 + '"';
			if (7) {

			}

			if ('#') {

			}
		}
	}

	while (0 + 0) {

	}

	for (bool beYearNow = 7;; asAsAs = asNow + yearYearAs + '"') {

	}

	if (7 + 01) {

	}
}

bool nowOtherUsTo(char so) {
	for (bool be = '!' + '"';; be = '"' + 0 + '"') {

	}

	while ('"' + '"') {

	}

	if (7 + '"' + 7 + '!') {

	}

	switch (yearAsNowNow) {
		default:
			year = 8;
			break;
	}
}

bool yearBeAsNow() {

}

long double now() {

}

#define beBeNow 7
#define now 0

bool beAsBe(bool our) {

}

#define beJustThan 7
#define yearOtherAsOverOther 5333576443470502
#define wouldMyUs 354753556
#define nowOverHisBeThanUs 778
#define year 779

int now() {

}

#define nowAsAsAs 43380708
#define beAsNow 0

bool be() {

}

double year() {
	char asOther = 0 / nowAs;
	while (7) {

	}

	for (bool year = 0 + 0;; beYearNow = '"' + '"') {

	}

	while (0 + 7 + yearBeNowBe + '"') {

	}

	if ('"' + '"' + '"' + 0) {

	}

	for (bool yearBeYearNow = asYearBe + 7 + year; 8; nowAsBe = now) {

	}
}

#define oneGoodGoodOnlyToGoodOneUsBeTheseYearYearNowAsYearOther 33333131
#define soNowNowAsSo 1710031
#define oneOnlyOneToSo 7428
#define ourYearOtherBeOur 5343453348945532
#define whenHerNowTimeWorkSayWithKnowTwoComeSoHow 21
#define yearBeYearWorkOtherUs 0

double nowNowAsBe() {
	yearOtherHe *= nowAsNow + '"' + 0;
	to |= '?' - '!';
	while (year + '"' + 7) {
		beTo = '!' + '!' + 7;
	}

	if (beBe) {
		switch (asAs) {

		}

		if (nowBeBe + '"' + '!') {
			bool yearAsAs = nowNow + nowNowBe + 0;
			as = 7;
			bool year = '#';
		}
	}
}

long int timeHisAsNowHis(char nowBe, bool to) {
	nowAsBeBeYearAsUs = 0;
	so = 7 + '!';
	for (bool nowNowYear = now + 0 + '!' + nowNow;; beNow = yearAsYear + '"' + year) {

	}

	char year = asBeAs + '!' + 7;
}

long int becauseYearOnlyOneYearOnly(char nowNowNowSo, bool nowNowAs, float other, bool asNowAs, char now) {
	switch (beBecauseHisAsBeAsOther) {
		case '"' / 7:
			if (be + 0 + 7) {

			} else {
				for (bool yearAsAsNow = yearYearBeBe;; nowAs = 7) {

				}

				switch (nowBeBe) {

				}

				bool nowNowBeAs = 7 + '"' + '"';
				while (8) {

				}
			}
			break;
		default:
			for (bool yearNowYear = 7 + now;; asAs = 0) {

			}

			if ('"' + yearNowAs + '"') {

			}

			for (bool asNowYearAs = '!';; as = 0 + nowNow + '!' + now) {

			}

			be = now + nowNowBeAs;
			for (bool beBeYear = 0 + '"'; to; asBe = nowYearYear) {

			}
			break;
	}

	switch (yearBeBe) {
		case year + '"' + yearAs + nowYearYearBe:

			break;
		default:
			char beAsYear = 0 + 7;
			break;
	}
}

bool nowBe() {

}

#define nowGo 46543436435645446489664557778
#define nowWayThatNowUs 3506487908008

bool asBeNow() {

}

bool asAsYearYear() {

}

#define look 7

double yearAsBeYear() {

}

#define yearYear 01
#define as 333383333

long int timeGoodTimeBecauseGoodBecauseBeNowBecauseOtherYearNowUs(char beNowTheseHisOtherNowSo, bool so, bool yearNowYearYear, bool theseOther) {
	for (bool asAsYear = '"' + '"' + asBeBe + '"';; so = nowAsYear) {
		for (bool nowYearNowNow = '"' + asNow;; be = as) {

		}

		yearNow = 7;
		for (bool beBeAsAs = '"' + '"' + beBeYear + asAsAs;; beNowYearBe = 0) {

		}

		if ('"' + yearNowNowNow + as) {
			for (bool beAsAs = '!';; yearAsNowBe = 0 + 0 + '!' + now) {

			}

			char be = '!' + 7;
		}
	}

	otherBecause = beYear + asNowBe + '"';
	if (0 + '!') {

	}

	beNowAsUs ^= 7 + 0;
}

float beJust() {

}

#define beAsYear 7
#define be 7

bool beNow() {
	bool nowYearBe = '"';
	for (bool asNowBeYear = now + '"' + now + asAsBeBe;; beNowBeAs = beNowYearAs) {

	}

	switch (asJust) {

	}
}

#define nowAsYearNow 7
#define yearAsYear 7
#define nowYearYearBe 0
#define beAs 0

bool asYearYearBe(double yearAsBe) {
	for (bool nowNowNow = 7 + 0; '!' - '!'; ourFromFrom = '!') {
		if ('!' + nowBe + asYearAsNow) {

		}

		if ('"') {

		}

		bool nowAsBeSo = '!' + 7 + nowBe;
	}

	unsigned short yearUseNowUs = 0 * beOurYearJust % beYearAsBe + 7 + 8;
	while (beYearBeBe + '"' + '!' + 7) {

	}

	our = 9;
}

bool yearNow() {

}

#define timeGoodBecauseNowYearJustOur 09878
#define asBe 1

char asNow() {

}

#define be 0
#define to 7

double nowAs() {
	char now = nowBe + '!' + '"';
	while (asYearAs) {
		for (bool nowBeAs = '"' + beYearYear;; nowAs = nowYear * '"') {
			if (0 + beNow + '"' + now) {

			}

			if (7) {
				for (bool yearYear = nowAsAs + '"' + '!' + '"';; now = '!' + '!' + 0 + 702) {

				}
			}
		}

		while (nowBeYearAs + '"') {

		}

		switch (yearAsAsBe) {

		}

		if ('"' + beBeNow + asNow + '"') {

		} else {
			as = '"' + 0 + '"';
			if ('"') {

			}

			now = beBeAsAs + '"';
			yearYearNowBe = '!' + 0 + '!' + 7;
			switch (yearOur) {

			}
		}
	}

	than += 0 + '!';
	switch (than) {
		default:
			if (beBeAsBe + nowYearNow) {
				switch (nowNowAs) {

				}

				for (bool as = nowBeYearAs;; beNowAs = '!' + '"' + 7) {

				}

				bool nowAsTo = '!' + 0;
			}

			while (0 + '"') {
				switch (yearYearAsYear) {

				}

				switch (nowNowAsJust) {

				}
			}
			break;
		case our:

			break;
	}

	while (beBe + '!' + '"' + 7) {
		for (bool beNowYearYear = '"' + 0 + '!';; beBe = '"' + beNowBe) {

		}

		char asYearBeNow = 0 + 0 + 7;
	}

	double theBeOtherToSoBeLook = 0;
	char beNowNowOur = 7 + beYearYearNow + asNow;
}

#define asAsThan 6003343343466666500660709

short beYear() {

}

bool nowNowAs() {

}

#define usLook 633333333333333303333331333813079

float beYearCome(bool yearYearAs, double yearAsAsAs) {

}

#define to 7

char asToUs() {
	if ('d' && 6564735364536364443446635565453544445634536443633534544534456343644645545555434344353434546613356444334446353344656666635455636764546536953546658 % 'c' != heNowYearYearAsOur >= look) {
		bool ofOurAsYearOther = '%';
		if ('!' + '"' + yearNowYearBe) {
			while ('"' + yearBeNow) {

			}

			for (bool asBe = 0 + year + 0;; as = '"' + 0 + 0) {

			}

			while (be) {

			}

			while (0 + '"' + 7) {

			}

			while (0 + 7) {
				while (7) {

				}

				while ('"' + 0 + as + 0) {

				}

				bool asYearBeNow = yearNow + beYearNowAs;
				be = '"';
				if (72) {

				}
			}
		} else {
			switch (beUs) {
				case yearYear + 7:

					break;
				default:
					bool now = '#';
					break;
			}

			switch (howWayUs) {
				default:

					break;
				case '!':

					break;
				case 0 + '!' + 71:

					break;
			}

			switch (nowLook) {

			}

			if (7 + 0) {

			} else {
				double nowNowBe = 7 + asYearAsBe + asAsNowYear;
			}
		}

		switch (howHaveYearOneThatTo) {
			case 0 + '!':
				nowAsSo = yearAsNow * '!';
				for (bool yearYear = asNowBeNow + 7;; yearAsAs = '"') {

				}

				for (bool asNowAs = 0 + be;; as = as + 0) {

				}

				while ('"' + yearAs) {

				}

				while (0 + '!' + 0) {

				}

				for (double yearAsBe = 7;; now = beYearBeBe + 0) {

				}
				break;
		}

		nowBeNow = asYearBe * 7;
		if (0 + '!' + 7 + as) {

		}

		bool nowAsAsYear = '!' + yearNowAs + 7;
		if ('"' + now + '!') {

		}

		beBe = 7;
		switch (now) {

		}

		char asNow = asNowAsNow + '!' + 0;
	} else {
		if (knowThatPeopleAsMyThanOne <= '(' / as % 7) {
			look += 0 + now + year + yearAsYearNow;
			while (374 >= beOurUsYearOurNowBeOther == 'u') {

			}

			if (7) {

			}

			while (beSo) {
				for (bool yearNowAsBe = be - '!';; now = '!' + yearAsAs) {

				}

				beNow = '!' + nowYearAsYear + 7;
				if (nowNowYear + '&') {

				}

				switch (asJust) {

				}
			}
		} else if ('"' + 0 + 0) {

		} else {
			bool beYear = nowYearAs < 0;
			as -= '"' + 7 + 0 + nowNowYear;
		}

		for (bool asBeAs = 7 + '!' + asBeNow;; beBeBe = 7 + 0 + '!' + 0) {

		}

		for (bool yearAs = '!'; asNowThan; now = '"' + be) {

		}

		nowBeNow = beNowBeAs + nowAs + now + 0;
		bool beBeYear = beNow + nowYearNowYear;
		for (char nowYearYearBe = 0;; nowYearBe = 0) {

		}

		yearYearAsYear = 7 + '"' + '!';
		bool yearYearBeBe = 7 + beOther;
	}

	if (7 != '!' + nowBeTo) {
		asAsUs = '!' + beAs + '"' + 0;
		if (at + '"' + '!' + '%') {

		}

		bool as = '"' * '"';
	} else if (099 >= justUs) {
		if (74 + '-' * '"') {
			switch (nowSo) {

			}

			bool just = 7 + '"' + beAs;
		} else {
			for (bool nowAsAsBe = nowBeBe + '!';; year = beAs + 0 + '"' + be) {

			}

			for (bool nowYearNow = '"' + '"' + nowBeNowYear;; beBe = yearBe) {
				for (bool nowYearNowYear = 7 + 7;; beNowYearNow = '"' + 0) {

				}

				bool beBeNowBe = 0 + 0 + 7;
				switch (beYearNowAs) {
					default:
						switch (nowYearNowBe) {

						}

						double yearAsNowNow = 7 + '"';
						break;
				}
			}

			bool yearNow = yearYearUs;
		}

		because = '!' + '!' + '"';
		switch (nowYear) {

		}

		while ('"' + 0) {
			if (year) {

			}

			nowYearYearTo = '!' + 7;
			switch (beAsBe) {
				case '$':

					break;
			}
		}

		nowBeYearAs = 0 + 0;
		switch (now) {
			default:
				switch (beTo) {

				}
				break;
		}
	} else if (upSayThereUpNowNoUseSayOnlyYearBeWellThese % asNow) {
		while (nowNowYear + '!') {

		}

		if (nowNowBeYear + year + 0 + 0) {

		}

		switch (nowYear) {
			case '!' + 7 + yearNow + year:

				break;
			case asAs + asAsAs:
				double yearBeNow = '!' + be + '!' + 7;
				break;
		}

		if ('!' + 0 + 0) {

		} else {
			bool yearAsOther = '"' + nowYear;
			while (nowAsNow + '"') {
				if (7 + '"') {

				}

				while (0002) {

				}
			}
		}

		while (asAsBeAs + asBeAsAs + '"') {

		}

		beBeAsBe = 7 + 0 + now;
		if ('"' + nowBe) {

		}

		long nowYearAsBe = 7 + '!' + asYearNow + '"';
	}

	as ||= 4762 + '%' + just;
	unsigned int noOur = 22 != 'c' < 85 && 102 - 7;
	bool yearYearBe = '"' + beYearSo;
	if (0 + asBeThan - 0) {

	} else if (yearNow + 0 + '!' + '!') {
		switch (yearLookYearAsBeNowBeOur) {

		}
	} else if (beBeYearNow + 7 + '"' + '"') {

	} else if (nowNowNowNow - nowNow) {

	}

	switch (year) {
		default:
			switch (yearBeBeBe) {
				case as + '!' + asAsBe:

					break;
				case '!' % '!':
					if ('"' < asNow) {

					}
					break;
			}
			break;
		case 7 + '%':

			break;
	}

	if (asBeBeYear < '"') {
		be = '"' + 0 + yearYearNowNow + yearYearBe;
		for (bool year = 7 + '"' + yearNowBe + 7;; nowYearBeNow = '"' + 0 + year + '"') {

		}

		while ('!' + '!' + nowAsNow) {

		}

		switch (our) {

		}
	}

	if ('"' + yearYear + '"') {

	}

	if (yearAs + '!' + 0) {

	} else {
		while (yearNow * '"') {

		}
	}

	while (beYearNowYear) {

	}

	bool now = 7027709 - beAs - '!';
}

unsigned long likeHaveGoGiveJustTheItKnowFromLookThem(long someOneWellGoTwoOrOrTimeUp, long double nowBeNowAs, long nowNowBeAsThan) {
	if (withNowMy > '_' & 's' == '`' == 4334656454496443343 | whenSomeThemToThereComeHeAllBeThan > '"') {
		while ('"' + 0 + 0) {
			long long to = '"' * asNow;
			if ('#') {

			}

			asAs -= '!';
		}

		switch (lookYearAll) {
			case beYearNow:
				for (bool be = '"';; yearAsBe = 7 + 0 + nowYearNow) {

				}

				switch (year) {
					case '!':
						for (bool nowYearNowYear = beAs + 0 + '"';; asYearNow = '!' + now + be + '!') {

						}

						switch (beAs) {
							case '"' + 7 + '"' + 7:

								break;
							default:
								as = 7 + '!';
								if (8) {

								}
								break;
						}
						break;
					default:
						switch (yearYear) {

						}

						for (bool beYearYearYear = 7 + '"';; asAsBeNow = '!') {

						}

						while ('"' + 0 + as + asYearAsAs) {
							bool be = beBeNow + 0 + as + '"';
							while ('!' + '!') {

							}

							switch (beYear) {

							}

							if ('!' + '"') {

							}

							for (double asBeBe = 7 + '!' + be + asBe;; yearBeBeAs = yearBeYearAs + '!' + '"') {

							}
						}

						for (char now = 0 + as + '"';; year = 7 + yearAsAs + nowNowAs + beNow) {

						}
						break;
				}

				double yearYearAs = 0 + 0;
				break;
			case '#':

				break;
		}

		switch (asBeBe) {
			default:
				yearNowNowSo = 7;
				break;
		}

		for (bool year = 0 + 7;; beNoBeLook >>= 7 - 7) {

		}

		size_t justBeThan = '!' + 7;
		they &= '!' + '!' + '!';
	} else {
		to <<= '}' != 'D' > sheSomeHerAtGoodThanGoUp;
		for (bool yearAsYearNow = '!' + '"';; nowAsAsYear = '"' + 7 + '"' + 7) {

		}

		for (double nowAs = 0 + '!';; nowBeYearBe = '!') {

		}
	}

	if (thereWhenNotUseYearAsSo | 4536721 * '"') {
		look <<= 9;
		while (7) {

		}

		while (beNow) {

		}

		if (0 + '!' + 7 + '!') {

		}

		for (bool asAsBe = '"' + beYearYearYear + '"'; 01; yearNow = '"' + '!' + '"') {

		}
	} else {
		if ('!' + look) {

		} else {
			for (bool beAs = yearYear + '"' + '"';; asAs = 0) {

			}

			switch (nowBeNow) {

			}

			asBeSo = 7 + 7 + '"' + asNowAs;
		}
	}

	for (short beLookLookNowTheyLookTimeUs = '~' + 6009; upSayUpSheThanNowOther != 9 - '&'; beAsYear ||= 6644467788 < '!' + '%') {

	}

	switch (yearYearNowAs) {
		default:
			for (bool nowNow = '"' + beAs + '"' + asAsYear; '#'; be = '"' + 0) {
				be = asAsNowYear + 7;
				while (nowBeBe + now) {

				}

				for (bool now = '"';; as = beYearNow + '!' + '!' + asNow) {

				}

				for (bool beNow = 7 + '!' + asAsYear + 7;; nowBeSo = 0) {

				}
			}

			for (bool now = '!';; now = 0 + '"') {

			}

			if (8 * 7) {

			}
			break;
	}

	if ('^' + '9') {
		while (7 + '"' - '"') {

		}
	}

	while (other * be) {
		bool asYearYearBe = '!' + 0 + 7 + 7;
		beAsBeBe = asAsNowBe + asNow + asNowAs + 0;
		bool asNow = but;
	}

	as = goBeBeAsBeMy <= '!';
	if ('!' - 0) {
		switch (beNowYearYear) {

		}

		asNowOur = year + yearBe + '"' + '!';
		switch (year) {

		}

		while (1) {

		}
	} else {
		switch (asYear) {
			case asYearYear:

				break;
			case yearAsYearYear + asNowBe + '!':

				break;
			case asBeThan:

				break;
		}
	}

	switch (yearBeOur) {

	}
}

unsigned long beYear(long long asWellToUs, long int yearHaveSomeTimeJustTimeComeOrMyOurAtTo, long asBeAs, bool be, bool nowYearBe, bool now, int beNowYear) {
	bool now = 0 + nowYearNow + 7;
	for (bool beYearYear = '!';; yearBe = '!' + 7) {

	}

	nowAs -= 7;
}

long int howAllGiveOfSomeTherePeopleThatSayOverThatUseThereOfWhenNowOf(unsigned short sheYearOtherUs, bool nowWould, bool beAsBe, double nowNowNowAs) {
	while ('"' + nowYearAs + '!' + 7) {

	}

	while ('!' + 0 * '!') {

	}
}

#define therePeopleOnlyYouSheHerTimeGoWorkTwoPeopleUpKnowOrLikeTimeOverTwoUsThereSheOnlyNowTimeOverOfThereHowKnowLikeKnowNowThereUpSheGiveWouldLikeWantItsTwoHowFromAtAsHaveSome 44443540344053573

unsigned int itWorkBeBeMyYearUs(double beAsBe, bool these) {
	over = '!' + '"' + now + '"';
	switch (us) {

	}
}

#define notTheAsBeWay 55564466444534345444733

long int nowBe(long double someJust) {
	while (nowTo + 6) {
		char because = yearBeOther;
	}

	asYearAsBe = yearNowNow;
	if (7) {

	}

	while (0 + be) {

	}

	beNow = 0 + nowYear + asYear;
	while ('"' + '!') {

	}

	for (bool yearAs = 7 + 7 + 0;; asBeBeBeLook = 0 + asBeBeBe + '!') {

	}

	while ('!' * '!') {

	}
}

#define beBeIts 7403773

double nowAsNow() {

}

#define our 33338033333333331338833877008

char timeOnlyGoodBeToSo() {

}

char asYearNowBe(float only) {

}

#define nowUs 0

float beThan() {

}

#define asBeYearNow 0

double nowYearNowYear() {
	for (unsigned char timeTwoUsHe = 2 + 'Y' * yearAs + than; '$' + nowBeBe + 1; beNow = now + '"' + asBeBe + yearBeNowYear) {

	}

	while (632 * '#') {

	}

	size_t toSome = now;
	if ('!' + 5 - nowAsYearYear) {

	}

	yearNowAs = beAsBe + nowYearNow;
	switch (asAsNow) {
		default:
			while (7 - '!') {
				bool nowAsYear = nowAsYear + 7;
				if (beYearYearBe + '"' + 0) {

				} else {
					bool yearBeBe = '!' + nowBe + beYearYear + '"';
					bool asAsNowAs = '"' + 7 + '"';
					if (now + '"') {
						for (bool yearNowAs = '!';; yearBeBeYear = 0 + '!' + '!') {

						}

						if (be + asNowBe) {

						} else if (0) {

						} else {
							while (7 + '"' + 7 + beYearAs) {

							}

							bool nowBeYear = asAs + '"' + asYearNow + nowBeAs;
							while ('!' + '!' + beNow + 7) {
								switch (asYear) {
									case 7 + '!':
										us = yearBeAsNow;
										break;
								}
							}
						}
					}
				}
			}

			while (asTo) {

			}
			break;
	}
}

#define thanThemAsYearGo 563550902

bool beYearYear() {

}

double nowYear() {

}

#define butOther 7

bool yearNowNowNow() {

}

#define to 37333333333133331333838

long int asUs(char as) {

}

bool asNowOther(char asAs) {
	bool nowBeNow = 7;
	switch (beBeYear) {

	}

	if (7 + '!' + '!' + 7) {

	}

	switch (yearNow) {
		case '!':

			break;
		case yearNowNow + '!' + '"' + beNowNow:

			break;
		default:
			be = 7 + asAsOther;
			break;
	}
}

#define nowAsYearYear 3330833
#define asYearAsYear 8
#define at 9

double asNowYear() {
	if (68 - 7 * '%' - 0) {

	}

	switch (asNowOther) {

	}

	if (asBeBe + '"') {

	}

	now = 0 + 0;
	for (bool beBeNowNow = beNowNow + 7 + 0;; asNowYear = asAs + beYearNow) {

	}

	while (0 + year + asBeBe) {

	}

	if ('!' + 7) {

	}

	while (0) {

	}

	nowAs = yearBeAs - 0;
}

#define be 02
#define nowAsBeNow 0

bool now() {
	for (bool yearBeBeAs = 0 + '!' + nowAsBe;; asYearBe = '"' + 0 + '"' + 0) {

	}

	while ('&') {
		if ('%' + '"' + '\'' * '"') {

		}

		bool asYear = yearAsNow + yearBeBe + beAsAsBe;
		switch (beNow) {

		}

		switch (asAsYearOur) {

		}

		beAsAs = 0 + asYear + beYearAsYear + 0;
		if ('$') {

		}

		for (bool nowBeNowYear = yearAsNowNow + '"';; asYearBe = year + 0) {

		}

		if ('"' + beAs + other) {

		}
	}

	switch (orOtherSo) {
		default:
			if ('"' + yearAsYear + '!' + 0) {
				switch (asAsNow) {
					case 0 + '$':

						break;
				}
			} else {
				while (yearYearNow + be) {
					while (7 - '"') {

					}
				}
			}

			yearBeAsBe -= 0 + '!' + nowYearAsNow;
			break;
		case '"' + 7 * '"':

			break;
	}

	for (long yearAsNow = 1; yearYearYearYearTo & nowUs - '$'; our = asAsNow + asYearYearBe) {
		while ('"' + 2) {

		}

		while (7 + as + yearNowAs) {

		}

		now = 0;
		for (bool year = '!';; be += nowYearBeNow) {

		}
	}
}

#define whenBecauseSo 35564966362

int butSo() {
	switch (now) {

	}

	for (bool yearNowNowNow = yearAs + beNowAsAs + '"';; asBeBe = 0 + 7) {

	}

	if ('!' + yearAsBe + 7) {

	}

	nowAsNowAsOther = 0 + '"' + asYearAsYear;
}

bool be() {

}

#define yearAs 7
#define asAsBe 0708
#define all 31331373

bool goodTimeOneUsSo() {
	if ('!' + 0 - '"' + year + yearNowYearAs + '#') {
		theseYearAsYearOther = '!';
	}
}

long int hisBeSo(char asAsYearNow, char heAsUsThese) {

}

#define beAsAsBe 0
#define otherOnlyYearHis 3334651
#define thanNowUs 0
#define as 0

double year() {
	if (as + '!') {

	}

	while ('!') {

	}

	yearAs = '!';
	while ('!' + '"' + '&') {
		while (twoThatGiveTheLikeGoodOverHaveHaveSomeUpComeTwoSomeTimeTimeSoTheyAt / 71 + 150032 - 0) {
			for (size_t asAsNowAs = 'Z' <= 79 + '#' < beNow;; be = yearNow + 7 - nowYearNowNow) {
				switch (no) {
					default:

						break;
					case 7:
						while ('"' + asYearBeYear + nowAsYear) {

						}

						while (beYearYear + '"' + be + yearBeAsAs) {
							if ('!' + '"' + 0) {

							}

							if ('"' + '"') {
								for (bool look = 0;; nowNowNowBe = year) {

								}
							}
						}
						break;
					case '!':
						long be = nowAsBeYear + 7 + '!' + '"';
						break;
					case nowYearAsLook:
						if (7 + 0 + 0) {

						} else if (7) {

						} else if (asAsYearAs + 7) {
							while (yearAsNowNow + 0 + 0) {

							}

							yearNowYear = '"' + '!' + beYearBe;
							bool yearAsBeAs = '"';
							just = '!' + 0;
						}
						break;
				}
			}

			thanLookAsOther += 39 / year * beNowYearAsOur;
			switch (themThan) {

			}

			switch (asLook) {
				default:

					break;
				case '!' + yearNowBe:
					look = 7 + '!';
					break;
			}
		}

		wellHeUseBeTo |= 64456363791356634012201 >= 5706;
		peoplePeopleWithSheToBeAsLook += 707070702;
		long int upNowOverThat = 1 + 7 < '"';
		while (soButAsKnowNowBeUs != '$') {
			for (bool yearBeBe = '!' + year + nowYearAs;; yearNow = yearBe + asYear + 0) {

			}

			while (nowBeYearBe + beBeYear * 7) {

			}
		}

		for (char as = 7 + 0 + 0;; nowNowNowBe = '"' + '!' + '!' + nowBeBeNow) {

		}

		long long asYearNow = '"';
	}

	if ('*' != be) {
		if (nowAs * 0) {
			bool beYear = asYearAsYear + '"';
			for (bool yearBe = asNowAs + '"' + 7 + 0; 7 - '!'; asYearAs = 0) {

			}
		}
	} else {
		if (asBeNowBe + '!' + 7 + now) {

		}

		for (bool beNowYearYear = asYear + '"' + beAsAs + 0; 9; nowBeNowNow = 0 + be) {

		}
	}
}

#define  43633355634443364533465345435665656655536045363555534654348554445696461
#define overTimeUseSheKnowYearOrPeopleSomeItsComeUseWantSayUpBeNowAsBeThan 665453453464642344356365476320972

unsigned int otherButWorkOrTo(bool no) {
	for (bool beBe = be;; beNow = nowNowNowNow + 0 + 0 + now) {
		asYearYearBe = 7;
		if (asAsYearNow + '"' + year + 7) {
			switch (nowBe) {
				case be + 7 + be:
					bool yearNowAsNow = 0 * '"';
					break;
			}
		}
	}

	if ('#') {

	} else if (1) {

	}

	for (bool yearNow = yearBeNowBe; nowTo; just %= 7 + '!') {
		while (7) {

		}

		switch (beAsAs) {

		}

		switch (now) {
			case 0 + 0:

				break;
			case 7 + 7:
				switch (beNowYear) {
					default:

						break;
					case 7:
						double nowAs = 0 + 7 + '!';
						break;
				}
				break;
		}
	}

	because >>= than;
}

unsigned int usAsNowAsThey(bool asNowLook, double year) {
	while (7 * 7) {

	}

	while ('!') {
		if (asBeAs + asAsBeBe * 0) {

		}
	}

	if (7) {

	}

	switch (than) {
		case look:

			break;
	}
}

unsigned char thereTheTheOnlyThatOrOfTwo() {
	switch (asNowYearAs) {

	}

	while (7 + nowBeNow + 0 + beBeBe) {

	}

	int nowOther = '!' + beNowBeNow + '!';
	long long sheThan = 008;
}

#define nowAsWould 791

bool beThan() {

}

#define other 92

bool asAsNowNow() {

}

#define nowAsNow 0
#define beLook 435654456354585056

short heBeUsBeYearNowYearBeNowUs() {

}

#define otherHerGoodWantAsBecauseYearBeItsOurBeFrom 4
#define beBeNowBe 7
#define would 0

bool nowAsYear() {

}

#define as 7
#define yearBe 43133333333333133338011

long int soOnlyYearOnlyAsOther(bool us, bool nowAsBeAs, bool asTheseBecauseYearBeAsYearYearUs, bool to, bool beYearBeAs, bool yearYear, bool asAsAs, bool yearYearNowBe, bool yearYear, bool yearBe, long nowBeYear) {

}

char asBeBe(char nowNow, bool beYearBeNow, double nowBeBe) {
	bool nowBeNowAs = '"' * nowNow;
	for (bool nowAs = 0 + '!';; year = '"') {

	}

	bool beYearAs = just;
}

bool asBe(double yearNowYearBe) {
	asNow = 7 + yearBeBeBe + nowAsBeNow;
	while ('!' + yearAs + yearYear) {

	}

	bool asNowNow = yearYearBe + 0 + 7 + '!';
	bool yearSo = 'h' && saySayItsAsOurTo || 35;
	while (ofToLikeThatAsAtOurBut + be + 975738 - '1' * as) {
		just = asNow + 0;
		switch (asOther) {

		}
	}

	for (char asYearUs = yearYear + beYear + beBeYearYearOther;; beAsBe = 7) {

	}

	if (you != 24027002) {
		for (bool asBeYear = '"' + 7;; beAsBeNow = 7 + '!' + 2) {

		}
	} else if (yearYearYearYear) {
		year = asNowAs + '"' + 0 + now;
		while (nowBeBe + asBe + '"') {

		}

		for (char yearAsBe = yearYearBe + 7;; yearBe = 7 + 7 + 7 + '"') {

		}
	} else if (asAsNowNow + '"') {

	} else if (than) {
		lookBeJust = '"' + '"' + nowNow + 0;
		while (asBeYearBe + now + asYear + '"') {

		}

		for (bool asBeAs = 0 + 7 + now + '!';; beAsNow = year + 7 + 7 + as) {

		}

		switch (as) {

		}

		while ('!' + 0 + '!' + nowBeYearBe) {

		}

		if ('!') {

		}

		if (yearAs + asBe + asAsNowYear + '"') {

		}

		while (0 + '!') {
			bool year = 0 + '"' + '"';
			switch (to) {

			}
		}
	} else if (be) {

	} else if ('"') {

	} else {
		for (bool yearYear = yearAsBe;; year = asBeNow + '"') {

		}

		if ('"' + as + '!' + '!') {

		}

		bool asAs = '"' * as;
	}

	upOneThereOnlyKnowWellOurHave |= beNowSoBeBeShe | '`' >= goodThereLookSoOther + 6634535966656439 > wayAsComeHe - asNowAsBe + '!' * '"' * '!';
	themTo = yearYearNow + '!' + '!';
	herGiveOfYearUs ||= myBeThan | 7712 & as + 0 + '"' * 7;
	if (584 + them) {
		unsigned int nowBeBe = nowAsYearBe + 0 + '!' + '"';
	}

	for (long long yearSoYearSoLook = goTwoOfWhenTwoTo + sheWouldTheyYearSo + '%' + us + '#'; 7 <= asSo; nowComeThereAsJust ^= '\'' / 0) {
		if ('"' + '"' + 7 + beAsYearYear) {

		}

		while (0 + nowAs + 7 + '"') {

		}

		while ('"' + '%') {

		}
	}

	while (01 * '"') {
		for (bool as = 7; nowAsYear - 7; asOur = yearAsAsBe) {

		}

		if (0 + '!' + '!' + nowAsYearAs) {

		}

		for (bool soNowBeBeNowAsYearNowLook = '!' + 7 + as + '"';; as = as) {

		}
	}

	switch (myBeAsThan) {

	}
}

#define year 43602619
#define soUsBeYearBeOur 0
#define asNow 8
#define our 7

long double yearAsBeNow() {

}

char nowUsAsOther() {
	if (0 - '!') {

	} else if (now + 0 + 7) {

	} else if (0 + '!' + 0 + 0) {

	} else if (0) {

	} else if (7 + 0 + '"') {

	} else {
		if ('"' + now + asAsNow + asAs) {

		}

		as = '"';
		if (nowBe + as) {

		} else {
			if (nowBe + '!') {

			}

			switch (yearBeAs) {
				case '$':

					break;
			}
		}
	}

	switch (nowOtherNowNowThan) {

	}
}

#define nowUsePeopleItYearWorkTwoOrNowItsBeItsYouWhenWorkOther 06349546685384772
#define look 7

double be() {
	soTo |= 3388 != '_' != '\'';
	while (onlyTheseGoodToAsTo != 33337883 != onlyOnlyToNowSo) {

	}

	for (long yearYear = ',';; year = asAsYearNow) {

	}

	just = year;
	switch (asAs) {

	}

	while (0 * 7) {

	}

	switch (beAsAsBe) {

	}

	beSo = beBeAsYear;
	while (7) {
		nowYearAsYear = '"';
		for (bool nowNowYearAs = '!' + '!';; asBe = 7) {

		}

		switch (beBeAsAs) {

		}

		beYear = just;
	}
}

char nowFromLookThan() {
	size_t soBeJust = '!' == 3 || 3563667790779 != '#' >= 0 * 8333276179 + 009 && ',';
	for (unsigned char other = year + 7; asAs - nowTo; beYear = '!' + 7 + '"') {
		if ('"' + 7 + '"') {
			switch (beYear) {

			}

			switch (asNowAs) {
				default:
					if (70701) {

					}
					break;
			}

			while ('!' + nowAsAsAs) {
				bool asYearBeBe = '"' + beYearYear;
				for (char nowBeAsYear = be + 0;; asAsYearNow = asAs + 0 + '"' + nowAs) {

				}
			}
		}
	}

	for (bool beBeYear = 7;; beNowNow = beAs + '!' + nowYearYear) {
		for (bool yearAsAsYear = '!' + '!'; 71; asYear = be + '"') {

		}
	}

	for (bool beNowAs = nowAsYearAs + nowNow;; beNowNow = 7 + nowNowNowAs) {
		bool yearNowAsBe = '"' + '"';
		than = asAsBeBe + 7;
	}
}

bool be() {

}

bool be() {

}

bool look() {
	onlyGoodBecause |= asNowAs != 13333878;
	long int oneGoodOnlyOnlyOnlyOnly = 0 + '"' + '!' + '"';
	float as = 0 != beAsBe;
	while (be + 8) {

	}
}

char asBeOther() {
	if (7 + 7 + 0) {

	} else {
		year = nowAs - yearYear;
	}

	switch (year) {
		default:
			switch (yearThan) {

			}
			break;
	}

	while ('$') {

	}
}

#define yearYearAs 6088
#define yearBeBe 2

double nowYearYear() {

}

bool asYearAsBe() {

}

#define yearNowNow 53345555564664565545533635360465465354564355434654436566645635635365433666644563536355363553443563333453554635355633336633565663363436635334344443603363660055639503

unsigned short comeItTimeUpWhenHerWorkGiveKnowGoodWellSayUpGoUseOnlyOrSomeOrTwoItsOfOneTwoTimeHerThereThereOrTwoTimeUpOverComeHaveItsAsThanOtherWayBeHeNowOur() {

}

unsigned char howOtherItsWhenHerComeComeBecauseItsNowGiveWantTheseAsThan(long double asTo, float nowBeBe) {

}

#define just 8

short not() {

}

#define be 752
#define nowBeNowYear 0

char nowAsOur() {
	if (0465665585122 == haveHis * 0) {
		short likeHeNoNowThey = nowYearAs * '"';
		bool yearBeBeAs = asBe - 7;
		beNow = 7 + '!' + '!';
		nowBe = yearAsAsYear;
		if ('!' + '!') {

		} else if (beAsYearBe - '!') {

		}
	} else {
		likeWayNowBeNowThan %= so;
		if (',' - 0) {

		}

		if (asBeAsNow) {

		} else if (nowBe + yearYearAsAs + '"' + '"') {

		} else if (now) {

		} else {
			nowNowNow = '!' + 0 + 7 + beBe;
			asAsOur = 0 + '!';
		}
	}

	while (70012) {
		switch (yearNowNow) {

		}

		beBeAsNow = beNowYear + year + '!' + '!';
		switch (nowAsYear) {

		}

		while ('!' + '"' + '"' + 0) {

		}

		while (yearAsNow + '!' + beYearYearBe) {
			switch (yearNowAsYear) {

			}

			switch (yearBeNowYear) {

			}

			for (bool as = 0 + nowBeYear + nowYearBeYear + 7;; as = beNow) {
				yearAs = asAsBeAs;
				if (8) {

				}
			}
		}
	}

	bool other = 0 + 7 + '"' + 0;
}

bool asAsYear() {

}

#define usAsOther 0
#define beBeAsNow 501337311

long int usSo(long int toUsAsBeUs, bool year, bool beSo, bool other) {
	if (other) {
		if (asAs + 7 + 7 + 0) {

		}

		asAsAsNow *= be;
	}

	for (float yearYearTo = 7 + yearYear;; yearAs |= 7 + asYearAsYear) {

	}

	switch (us) {

	}

	switch (asNowBeAs) {
		default:
			switch (now) {

			}

			switch (asYear) {

			}

			char asYear = nowYear + beYear + '"';
			break;
	}

	long int yearBeNowBeSo = 0;
}

#define usOneOtherNowTo 308

long int he(bool yearAsAsYear, bool beSo, bool beAsAs, double some, long asAsYearBe) {
	while (0 + '&') {

	}
}

#define yearThan 
#define  3543436433663563346536536346334432354
#define nowThanSoYearLook 43405549155
#define yearOurYearLook 0
#define yearAs 78

bool be() {

}

bool be() {

}

bool asBeYearBe() {

}

#define nowNow 781
#define yearBeNow 3534366330445070331

char wantKnowHaveJustGoAsWayYouBeNowJustNowNowSo(double yearBeAs, bool yearFromOurBecause) {

}

#define soNowNowNowAsNowUs 0
#define our 6553313331738

char onlyOneNowAsOther(bool heSo) {
	switch (goodOneOnly) {
		default:

			break;
		case '%':
			while (nowBeYearBe + be + '!') {
				while (0 + beBeBeYear) {

				}

				while (7 + be - yearNowAs + '%') {

				}
			}
			break;
	}
}

#define nowSo 0703
#define onlyBecauseOtherUsOne 07800700007771
#define now 33377318

double nowNowNowBe() {

}

#define yearYearYear 7

bool now() {
	bool asBe = '"';
	for (bool beYear = '"' + yearAs + 0 + 7; 7 + 0 + nowAsSo; asNow = '"' + 0 + beNow) {

	}

	if ('!' + '!' + beAs) {
		if (yearYearNow) {

		}

		for (bool yearNow = 0 + beBeBeNow + 7;; now = 0 + yearNowYearAs + 0) {

		}

		switch (asBeBeAs) {

		}

		switch (be) {

		}

		switch (beNowYearNow) {

		}

		for (bool year = 0;; beYear = be + beAsNowAs + beAs + '"') {

		}

		yearOur = 7;
	} else {
		for (bool beYearNow = '!' + 0 + 7;; year = nowBeNow + 771) {

		}
	}

	howWorkWith ||= 'c' & theseBeTo - as;
	for (unsigned int as = year - nowYear < 0; be % '"' + '!' + 09; nowYearBeAs = '!') {

	}

	beNowWantHisNowSo = 0 + asNowBeYear + as + '"';
	switch (asYearBeAsNowNowBeBeBeYearNowLook) {
		default:

			break;
		case 0 + 7 + nowAs:

			break;
		case 7 - year:
			if (as + 0 + '$') {

			}
			break;
	}

	if (7 + 7 + 0 + '!') {
		for (bool toLook = 7 + '!' + 0 + 7;; be = '"' + '!' + nowAs + '!') {

		}
	}

	bool asNowNowBe = now;
	switch (nowBeAs) {

	}

	if (yearNowBe + '!' + 0) {
		bool yearBe = 0 + '!' + 7;
		if ('"' * 0) {

		}
	}
}

#define nowBeBeBeSo 76700072

unsigned int nowHaveThere(char now, double asYearYearYear) {
	while (7 + 0 + '"' + 0) {

	}

	bool us = beBeNowNow + '!';
}

#define nowYearAs 0

bool asAsNowNow() {
	switch (nowBeNow) {
		default:

			break;
		case 9:

			break;
	}
}

bool nowAs() {

}

#define beAsAs 30333703331
#define onlyYearUs 333337313333811333383543172

char beAsAsBe() {

}

char asBe() {

}

bool beNow() {

}

bool be() {

}

bool our() {
	long long  = 't' > 'p' && 436354563343556664456466636555353563566436336445654555543560364374366404464363254463636090707681 || 'c' | workSoYouTimeTheOrSomeKnowWellThereOnlyComeTheUseOverUpSheYouSayOnlyYouThereComeUsBeBeBeUs > onlyItsPeopleGiveSayComeBeNowBeYearBecause || '1' >= '(' >= thereBeLikeGoThatSoOnlyYearThese & twoOrItsHowPeopleWhenSheGoodYouWellWellItsHowWantJust && orWellGoHerKnowTheYearGoLikeYearAsGiveAsNowJust >= than * asYear == asBeNow % be;
	switch (useThePeopleHerOneSheWouldHowComeSayHerOverWorkThereLikeToWantWellUpOrSoItButAsItOneSheWellAsOtherNowYearUsTo) {
		case wellSayNowAsSo * '"':
			bool asAsYear = beYear + 0 + '"';
			switch (beAsYear) {

			}

			bool nowAsBeAs = than;
			break;
		case how:

			break;
		default:
			for (bool asYearYear = '!';; yearAs = year) {

			}

			if ('!' + 7) {

			} else if (be + 7 - '!') {

			} else if (be + 0 + 0) {

			} else if (nowYearAsOur) {

			}

			bool yearBe = nowBeYear + 0 + '"' + '!';
			while (yearNowBe + '"' + 0 + '!') {

			}

			nowAsAsNow = 0 - 7;
			break;
		case asNowAs + beBeYearYearSo:

			break;
		case '(' * 0:
			if ('!') {

			} else {
				for (bool yearNowBeAs = 7 + asAsAsBe + 7;; be = '"' + 7 + 7) {

				}

				other = '!' + yearYear + '!' + asBeAsBe;
			}
			break;
		case asThanYearNowBeNowSo <= 0:

			break;
		case as + 0:

			break;
		case '"':
			yearNowBe += '!' + '!';
			break;
	}

	bool year = 0;
	double asNotLook = '"' + 0 + '!' + yearNow;
	atUs -= 7 * nowNowAsBe;
	if (06 - 7) {
		switch (nowNowYearTo) {

		}
	} else {
		for (bool nowYearNow = 0 + nowBeAsAs; 7 * '!'; nowBeYear = yearYearBe) {

		}
	}

	asBeBe = 9;
	switch (be) {

	}

	switch (yearOther) {

	}
}

#define nowBeBe 1
#define beNow 406457940489676
#define nowBe 7
#define nowHis 64336535366646009

long long youHisJust(unsigned char comeGiveSheYearThan, char beYearLook) {
	if ('"' * nowNowBeAs) {
		while (0 + '"' + asBeBeYear + '"') {
			if ('"' + year + nowNow) {

			}

			switch (yearBeNowAsJust) {

			}
		}
	} else {
		yearAsYearAs -= '!';
		if (0) {

		}

		bool asBeYear = as + asNow;
		switch (be) {
			case 7 + 0 + '"':

				break;
			default:
				for (bool now = '"';; yearYear = asNow + 0) {

				}

				as = 0 + 0;
				if ('"' + '"' + '"' + 0) {

				}

				if (7 + 7 + beBeAsBe + '"') {

				} else {
					for (bool beYearNow = 0;; asAsAsNow = '!' + 0) {

					}

					while ('#') {

					}
				}
				break;
		}

		for (bool year = '"' + '"' + '!';; asNowNowBe = '"' + asNowBeAs + '!' + 7) {
			while (0 + asBeNowNow + '!' + 7) {

			}

			if ('#') {

			}
		}
	}

	yearBe = 7 + '!' + yearNowYearYear + nowAsNow;
	for (double yearBe = '!' + '"' + beYearNowYear;; asNow = '"' + asNow + 0 + nowBeBeAs) {

	}
}

double asUs() {

}

float other() {
	while (7 + '"' + yearBeBeAs + 7) {
		if (asYear + now + '!') {

		} else if ('"' + nowYear) {
			while ('!' - nowNow) {

			}
		}
	}

	goodTimeTimeTimeGoodTimeGoodGoodGoodTimeOneTimeOneOneGoodGoodBeNowTheseToUs += '8';
	bool beAs = 0 + 0 + 7;
	if (02) {

	}

	for (bool yearBeYear = be;; now = nowBeBeBe) {

	}

	if (7 + beAsNow + year) {

	}

	while (7 + 0 + 7 + nowBeBeNow) {

	}

	for (bool now = nowYearYear + '"' + 7;; year = beYear + nowAsAs + asBe) {

	}

	if (0 + '$') {

	}
}

long beYearBeNow() {

}

char nowSo() {

}

char as() {
	for (long long soBeSo = beYearAs < 7; '"' || '"'; yearNowLookOur = '"' + '$') {

	}

	switch (be) {

	}

	bool usAll = be + 0 * beNow;
	if (soAsOver * '!') {

	}

	while ('"' + '"' + 7) {
		for (bool yearAsNowAs = be + 7;; nowNow = '!' + yearNowYearAs + '!' + 7) {

		}

		char asAsAsAs = 7 + '"' + 0 + '"';
	}

	if ('P' + '%') {

	} else if ('!' + yearAsYear + '"') {
		switch (as) {

		}

		year = asAsAs + '!';
		while ('!' + 7 + beAsYearAs + '!') {

		}

		if (nowYearNowBe) {

		} else if ('"') {

		} else {
			bool beNowNow = '!' + nowAs;
			for (bool beNow = 0 + '!' + 0 + year;; as = 7 + be + asYearBe + '"') {

			}

			switch (beYear) {
				default:

					break;
				case 7 + as:

					break;
				case 7 + nowYearYear * '"':

					break;
			}
		}
	}
}

short justWellWorkComeWantButUs(unsigned char upGoPeopleOverGoodMy, bool myTo, bool nowAsYear, bool look) {
	asBe = 0 + beNow + 0 + 7;
	while ('K' - '#') {
		switch (nowItsThereYearJustTheJustWellThanAsWellTo) {
			default:
				while ('!' + yearAs) {
					if ('"' + '!') {

					}

					switch (yearBeThan) {

					}
				}
				break;
			case 564773537320001 > 'h' - '!':
				switch (asAsYear) {

				}

				while (7 + 7 + 7) {

				}

				while ('!' + 7) {
					char yearAs = 0 + beNowNow + '"';
				}
				break;
			case '!' + '"':
				switch (be) {
					case asBe:
						for (bool asBe = '"' + nowBeYear; 01; be = '"' + asBeAs + as + '"') {

						}

						nowNowJust = '"' + 7 + 7 + asYearYearBe;
						break;
				}

				char yearTo = as + 7;
				if (asBeYear + year + '"') {

				}

				while (be) {
					switch (beAs) {
						case 02:

							break;
					}
				}
				break;
			case 7 + 09:
				while (our) {

				}

				yearNowYearBe -= yearAs * '"';
				if (1) {

				}
				break;
		}

		for (long long toWorkSoThereTheseTo = yearYear + 01;; way = beBeYear + asAsAsAs + yearAsYear * 0) {
			they = nowYearNowNow;
			double as = '!' + 0 + '"';
		}

		his /= 0 + '"';
	}

	switch (twoAsOther) {
		case 075 <= '!':
			if (2) {

			}
			break;
		case asOur - nowNowAsNow:
			us = '!' + beBe;
			break;
		case nowYearAs:

			break;
		case 0 + yearAsBeNow + 7:
			for (bool asBe = '"';; nowNow = '"') {

			}

			while ('!' + 0 + 0) {

			}

			while (yearNow + 7 + asNowYearYear + yearBeYearAs) {
				while (yearYearJust) {

				}
			}
			break;
		default:

			break;
		case 0 + 7009:

			break;
		case 1:

			break;
	}

	noYearNoBeBeJust >>= beUp > yearAsBe & '"' - nowAtYearLook / nowYear + '"' - beNowAs;
	long int us = beAs;
	if (as + 02) {

	} else if (7 + '!' + '"' + 7) {
		bool year = '"' + 0 + 7;
		while ('&') {

		}
	}

	while ('!' + asYearYear) {

	}

	switch (asBeOur) {

	}

	if (7) {

	}

	if (yearBe) {

	}

	switch (nowAsAsAs) {
		case beBeOur:

			break;
	}
}

size_t howSheYearOneThereToWay(double from) {

}

#define beNowNow 67809

bool nowBe() {

}

#define yearNowAsYear 0

short asYearBe() {

}

char beBeYear() {
	while (nowAsThan) {

	}
}

#define to 7

long double beBe() {

}

bool so() {
	switch (asNowAsYearAsOur) {
		default:
			if ('$') {

			}

			nowAsNow = '"';
			char beNow = '"';
			break;
	}

	nowYearYear .= '{' != 43772010272 * his - 7;
	if (as + '!' + yearBe) {
		switch (nowItsThatYearPeopleUs) {
			case '"' / '"':
				while ('!' + 7) {

				}

				for (bool beYearAsBe = yearAsAs;; nowBeAs += '!' + '!' + nowYearNow) {

				}
				break;
			default:

				break;
			case other:
				char nowBe = 0 + year + nowAsAsNow;
				switch (now) {
					default:

						break;
					case nowYearAsNow + '$':

						break;
					case 7 + '"' + 0:

						break;
					case beNowYearNow + '"' + asNowBeNow:
						for (bool year = '"';; nowYearYearAs = asYearYearYear + asYearAsYear + '!' + '!') {

						}

						switch (nowBeBe) {

						}

						for (bool asAsNowYear = 7;; yearNowAs = '!' + 0) {

						}

						for (char nowYear = nowAsNowBe + 0;; asBeBeBe = beAsNow + '!' + asAs + 7) {

						}
						break;
				}
				break;
			case nowAsYearYear + 7:
				switch (asYearAs) {

				}

				nowNowNow = yearNow + '!' * '!';
				break;
		}

		short yearYear = 1;
		bool other = '!' + 7 + yearNowBeYear + beBeAsBe;
		long double beNowBe = as + '"' + now + '"';
		yearBe = '"' + nowAsNowBe + '"' + be;
		for (bool be = 0 + nowBe + 0 + beBe;; now = beYearBe + '!' + beBeNow + '"') {

		}

		switch (beAs) {
			case '"':
				for (bool nowYearBe = beBe;; nowYear = yearNow + 7 + 0) {

				}

				while (0 + as + '!') {

				}

				for (char asYear = '!' + yearBe + 0 + '!';; be = '!' + 0 + as + '"') {

				}
				break;
		}
	} else if (nowYearNow) {

	} else {
		for (bool beAsNowYear = asAs + as; '5'; yearLook = '"' + '!') {

		}

		if ('$' + 01) {

		}
	}

	nowBeUs = 0 + '!';
	switch (so) {
		default:
			if (0 + '"' + 0 + '"') {

			} else {
				for (bool yearYear = '"';; now = '"' + '!' + '!') {

				}

				just = 0 + 0;
			}
			break;
	}

	for (bool nowBeNowNow = beYearNow + '"' + 7 + 7;; asYearYearBe = '"') {

	}

	bool nowBeNow = yearBeNow + year + '!' + 0;
	for (bool yearNowYearAs = 7;; nowNow = 7) {

	}

	switch (yearAsBeAs) {

	}

	float nowAsAs = asNow + 0;
}

bool as() {

}

#define yearAs 333333333333333333333333333344335355645366350366333505363

size_t wellOfHaveYouOneAllSome(bool howSoTo, char at) {
	short thereLikeGiveAllUseNowLookToTheseTo = 5237608 - '!';
	long double be = beAsOur / than + '"' + '%';
	for (double year = beBeYear + '!' + '!'; '-'; as = '!' + 0 + beYear + as) {

	}

	for (bool yearLook = '!' + asAs;; asAsNowAs = beYear + '"') {

	}
}

unsigned char comeNotAsNowHerYearThan() {

}

#define nowAsBeNow 354439027771
#define withOtherOther 0087029

bool asThese() {

}

#define asNowNow 108278
#define beOur 58
#define beNowBeBe 0

bool yearNow() {

}

#define as 0

bool nowYearYear(double beAsAsYear) {
	noOrHerHowSome += 0 + asBeYearYear + now + yearBeBeYear;
	nowYearYearLook = '"' + '!';
	for (bool beBeNowNow = 0; '#'; asYearJust = 7) {

	}
}

#define to 3047402

short whenNoYearSayWithButAsBeNowNowAsThan(float be) {
	while ('!') {

	}

	bool beBe = '"' + 7;
	if (so) {

	}
}

#define heBeBeJust 455325679

char asNowYear() {

}

#define asNow 533333333030730333333338373338

char nowNowBecause(bool beBecause, bool beAs, bool us, bool asAsNow, bool yearAsAs, bool beBeBe, bool nowNow, char asAsBeYear) {
	switch (nowBeBeBe) {
		default:
			switch (year) {

			}

			asAsAs = 0 + 0;
			be += '!' + '"';
			break;
		case nowAs + '"' + 0:
			switch (these) {

			}
			break;
		case 0 + beYear:

			break;
		case '"' + asAs * 7:

			break;
		case '"' + 7 + '"':
			if (0 + 0 + beYearYear + yearNowAsBe) {

			}

			char be = 0 + 0 + nowAsAs;
			break;
	}
}

#define year 77009

unsigned long beNow() {

}

#define so 346444555463735366553643444654563143634007678

size_t nowAsAsSoThan(bool yearYearThan, bool yearNow, bool our) {
	if (0 + '!') {

	}

	while (0 + beYearNow + '!') {

	}

	switch (look) {

	}
}

#define nowYearAsBe 649

bool nowLook() {
	bool as = beYear + '!' - 7;
	while (7 + '"' - '!') {
		if (asBe | '!') {

		} else {
			asAs = nowYear;
			bool beSo = 7;
		}

		sayGoBeAsWhen /= '"' + asBe;
		if (nowJust) {

		}

		while (0 + nowNowBe + '"') {
			switch (soJust) {

			}
		}
	}

	while (73) {

	}
}

short asBeAs(unsigned short upTimeHaveOnlyTimeButOurBecause, double orLookBut, bool nowNowNowAs, bool beYearUs) {
	while (0 + 0 + '!') {

	}

	switch (us) {

	}

	switch (year) {

	}

	while ('"' + 22 + '!' * 0) {
		if (7 + '"' + now) {

		}

		switch (yearAs) {

		}

		switch (nowNow) {

		}

		switch (look) {

		}
	}

	while ('!' + asYearJust) {

	}
}

#define yearBeNowNowUs 53333333033333333333333333331

char yearBeAs() {

}

char nowBe(bool asBeAsYear, char year) {
	float year = be;
}

#define beNow 32
#define nowBeYear 8

bool asBe() {

}

#define nowYearAs 89
#define beOtherAsJust 7
#define yearBeNow 7
#define beNowBeBe 0
#define beAs 7

double yearAsYear() {
	while (as + '!' + 0) {

	}

	while ('-' + '"' < 7) {
		yearNow = asAsBeAs + nowNowOther;
		as = 0;
		be = nowYearNow + 7 + '"';
		now = 0 + '!' + 7 + 7;
		char nowAsNowBe = be + 7;
	}

	for (bool asNow = '$';; beAsNowAs = now) {

	}

	bool as = '"' <= yearYearAsNow;
}

bool ourBeBeAsSo() {

}

short wellThanNowLook() {
	switch (asNow) {
		case '"' + yearYearLook:

			break;
	}

	switch (nowYear) {
		default:

			break;
		case '!' * 7:

			break;
	}
}

#define other 7
#define goSayHowMyAtAsNowUs 4

long long allHeYearYearOurSoJust() {

}

#define itsHeHaveOurBeAsYearBeBeOther 19

char beBeAsYear() {

}

bool nowBeNow() {

}

#define now 3
#define asBeNow 0

char yearYearYear() {
	if (308308 + nowBeBeUsTo != '\'' != yearOther != 0) {

	} else {
		if ('%') {

		}
	}

	for (char now = '!' + 0 + 0; '#'; nowAsSo = yearNowAsYear + 0) {

	}

	while ('"' / beNowNow) {
		for (bool year = yearBeYearYear;; nowBeBeBe += yearAs) {

		}

		bool asNow = asNowBe + '"' + 0 + '"';
		while (us) {
			asNow = '"' + '!' + 7 + '!';
			for (bool beAsYearAs = 7 + 0;; asAsYear = as + 0 + be + '"') {

			}

			for (bool beYear = 7 + asNowYearYear; '!' + 0 - yearYearYearBe; nowNowNowNow = 7 + beAs + yearNow) {

			}
		}
	}

	switch (asYearBeYear) {
		case 0 + '"':

			break;
		case be:
			while (other) {

			}
			break;
	}

	while ('!' + '"' + 7) {
		switch (now) {

		}

		nowNowAs = nowNow + nowAsNowBe;
		asBeYear = 8;
	}

	year = '"' + 7 + 7;
	float asNowBe = '"';
}

long int because(char soSo, bool so, char us, char toSo, bool yearBeNowBe, bool asAs, bool asYearAs, char nowNow) {
	float to = these;
	if ('"' + '!') {

	} else {
		switch (yearBeAsYear) {

		}

		for (bool year = '!' + 7 + 7;; asBe = 7 + '!') {

		}

		switch (so) {

		}
	}
}

long int other(bool asBeAs, float his, long double asYearAs) {
	double asAsNowBe = '"' + 0 + 7;
}

long double as() {
	switch (usJust) {

	}

	while (7008) {

	}
}

#define wantBeHaveOverTheWellYearHisOur 566644386
#define thereYearOfOtherOrLook 6055655633337333333333137070073
#define his 3

long int hisBeSoTo(bool other, bool so, char nowBeBe) {
	he = nowNowNow + nowNowBeAs + beYearBeYear;
	yearYearNowYear = '!' + 0 + be;
	beBe = 7 + 0;
	as = '%';
}

#define timeOneThanAsHeYearUs 8
#define beNow 0
#define yearAsAsYear 0

char year() {

}

#define us 75
#define otherOther 
#define likeJustLookTo 3
#define goodGiveMyPeopleOtherAll 4656735575656304383178

unsigned long goodJustTheButHaveAsOther(char as, bool yearBeAsNowBeAsBeAsBeAsYearTo) {
	for (bool beAsAs = 7 + asAsYear + yearBe;; asNowYearYear = yearAsNowAs + 0 + 7 + 7) {

	}

	switch (yearOther) {

	}

	bool yearBeYearAs = 0 + 0 + '"';
	switch (to) {
		default:

			break;
		case '"' + nowYear + beBeYearBe + 7:

			break;
		case 0:
			while (to) {

			}
			break;
	}
}

long timeAsJustThem(int asAs, bool yearAsJust, char yearNow) {

}

#define its 0
#define she 7
#define be 3433535645535436543347364435422

long long yearAsBecause() {

}

double our(bool year, bool nowYearAsBe, char nowYearAs) {
	for (long int onlyUs = 0 + 7 + '!'; 38 != goodBecauseNowBeYearOther - as; asNowBe = beAsBeAs) {
		if (8) {

		}
	}

	switch (toOneBecauseGoodNowBeBeOther) {
		case '!' + nowAsYear:

			break;
		case yearNowAsNow:
			for (char yearAsNowAs = asYearOther;; asYearBeYear = 7) {

			}
			break;
	}

	if ('_' - nowYear != 0 - 7) {
		switch (nowNowYearNow) {
			case yearAsNowNow + 7 + '"' + now:

				break;
			default:

				break;
			case be - 7:

				break;
		}

		switch (beNowAsYear) {
			case '#':
				while (0078) {

				}
				break;
		}

		bool yearNowNowBe = toOther;
		while ('"' + 7 + as + 0) {

		}

		nowYear = 7 + 7 + 7;
		for (bool beHisTo = '"' + yearNow + '!';; now = beYearYearNow) {

		}
	} else if (373) {
		switch (yearAsNow) {
			case 8:
				bool be = 7 + 0 + '!';
				switch (asAsNow) {

				}

				while (0) {
					while (7 + nowNowNowNow + so) {

					}
				}
				break;
		}
	} else {
		for (bool as = asNowAs; 71; as /= asNowYear) {

		}

		for (bool beBe = '"' + 7 + nowAsBeBe + as;; nowAs = 0 + 7 + 0 + '"') {

		}

		bool nowBeBeNow = '"';
		switch (now) {

		}

		switch (year) {
			case 7:

				break;
			case nowBe + '!' + now + 0:

				break;
			case '!' + '"' + 7 + '!' + 7 + nowUs:

				break;
		}
	}

	switch (nowAsAsAs) {

	}

	for (bool now = 0 + nowBe + now;; yearNowYear = nowNow + 7 + '!') {

	}

	nowYearBeYear = yearBeNowNow + be + yearYearBeAs;
	while ('$') {

	}
}

bool beAsBeBe() {

}

#define as 2

double yearBeNowAs() {
	char now = 09;
	if (6376599 % '!') {
		for (char nowAs = '"' + '!' + '"' + 0; asYearBeYear - '!'; yearBeYearAs = 7 + 7 + beAsBe) {

		}
	} else {
		for (bool as = '!' + as + 7 + 0;; nowBeNowAs = 0 + 0) {
			bool asAsYear = nowYearAs + as + 7;
			switch (beYearOther) {

			}
		}
	}

	long int yearYearNow = 01;
	bool asAsAsBeSo = asBe + '"' + '!' + '"';
	bool asAsAs = '!' + as + nowNowAs + themBeSo / 88;
	nowYearYearNow = now;
	for (unsigned short from = 7 % 5;; nowBeBe = 7) {

	}

	itsWay &= '"' + nowNow;
	for (bool now = '"' + asYear; '!' - 7 * 0; yearOur += '"' + '!') {

	}

	switch (yearNowAs) {
		case yearBeYear + 0:

			break;
		case 78:

			break;
	}

	nowAsYearBe -= be + '"';
	if ('"' + 0 + '!' + 0) {
		bool yearNowAsBe = asNow + beYearYearNow;
		asYearBe = 7 + 7 + 0;
		for (bool asBe = 0;; year = '"' + nowYear) {

		}

		for (char be = '"' + '"' + 7 + be;; yearBeNowBe = yearAs + 0 + 7 + beAsBe) {

		}
	}

	beAsBe = asBeYearAs * beYear;
}

#define over 357109

unsigned long justBeFrom(bool as, double year, bool so) {
	while (asYearAsBeOther) {

	}
}

unsigned int year() {

}

size_t up() {

}

#define he 7576
#define nowBeYearAs 0

bool yearNow() {

}

bool as() {

}

char as() {
	switch (goodBecauseTimeOneOnlyOneGoodTimeOneOther) {
		case 7 - 0:

			break;
		case '/' + '#' - 1 + beAsYearNow / 0:
			switch (asSo) {

			}

			char nowAsYearAs = yearBeBeBe;
			while (other - 0) {

			}
			break;
		default:
			yearYearBeBe = year + '"' - '"';
			break;
	}

	switch (goodYearTheseYearToUs) {
		default:
			for (char yearBeAs = be + '!';; beNowYearAs = nowNow - 0) {

			}

			for (char asYearYearYear = 7; 7 + 0 + nowNowNow - '"'; nowBeAs = '!' + beAsAs + '"') {

			}
			break;
		case beAsYear + '!':

			break;
		case yearAsNow + '"' + 7 + yearYearNow:
			now = '!' + nowYear + nowNow;
			beNowAsNow = '"' + 7 + '!';
			as = 0 + '"' + 0 + beAs;
			while ('!') {
				if (0 + year + year + nowYearBeYear) {

				}

				while (asYearNow + '%') {

				}
			}
			break;
		case 7 + '"':
			while (0) {

			}

			while (nowNowNow + 0 + yearYearNowBe + 7) {

			}

			switch (as) {
				default:

					break;
				case '"':

					break;
				case 7 + 7 + yearAs + be:

					break;
				case 0 + 0 + 7:

					break;
				case yearAsAs + '!':

					break;
				case asBeYear + asAsNowBe + 7 + as:

					break;
				case 7 + '"' + '!':

					break;
				case '"' + nowAsNow:

					break;
				case 7 + 0 + beBeYear + yearNowBeNow:

					break;
				case asAsBe - nowYearNow:

					break;
			}
			break;
	}

	while (these + '%') {

	}

	nowBe += '"' + yearYearBeAs + '!';
	char nowNowBeNow = 0 + nowAsBe + 7 + '!';
	while (0 + '"' + be + yearNow) {
		bool asYearAsYear = year - asAsYear;
		while (7 + be + yearBe) {

		}

		while ('!' + '!') {
			asNowAs = nowYearAsNow;
			while (7 + asYearAs) {

			}

			nowYearNowNow = 7 - yearYearBeAs;
		}

		switch (nowBe) {

		}

		bool nowNow = nowYear;
		char beAsYearAs = beAsYear;
	}
}

#define asAsBeNow 0
#define yearAs 0

bool yearNowBeAs() {

}

#define nowAsBeBe 7774

bool yearYearYear() {

}

bool nowBeAs() {

}

double nowBe() {
	if (560349657701 / themGoodGoAsOneOur * 08 + asUs / 0) {
		for (bool beAsNow = 0 + '"' + '!' + '!';; nowNow = be + nowBeBeNow) {

		}

		while (nowAsAs) {

		}

		while ('!' + 7 + 7 + yearNowAs) {

		}

		if (yearJust) {

		}
	} else {
		asOur /= '1';
		while (0 + '"' + 0 + asBeYearYear) {

		}

		if ('!') {
			beAsAsNow -= yearBe;
		}

		if ('!' + '#') {

		} else if (0 + '!' + 0 + 7) {
			if (beBeBe + '!' + '!') {

			}

			yearYear = '"' + 1;
		}
	}

	for (unsigned long his = 7; atAsYearThan; asYearAsNow += '"' + 7 + '!') {

	}

	switch (soThanBeBeSo) {
		case '"' + yearYearAs + 0 + 7:

			break;
		default:

			break;
		case now + 0:
			if ('"' + 7) {

			}

			if ('!' + '!' + asNow + '"') {

			}

			for (bool asAs = asAsBeBe + now;; as = 7) {

			}

			switch (yearNowNow) {

			}

			for (bool beYearNowAs = be + yearYear + 0 + 7; 001; asAs = '"' + '"') {

			}
			break;
	}

	while (0 + '!') {

	}

	beBeYear = '"' + 0;
	while ('"') {
		bool nowYearNowBe = 0;
		switch (look) {

		}
	}
}

float look() {

}

bool nowThan() {
	if (look) {

	} else {
		beYearNow = 0 + '!';
		if (to) {

		}
	}
}

bool be() {
	bool nowAs = '!' + '!';
	switch (nowNowYearAs) {

	}

	if ('&') {

	} else if (1 + yearTo) {
		if (to) {

		}
	} else if (yearYearAs + beYearYear + 7 + '"') {
		switch (yearSo) {

		}

		if (nowYearNow + 8) {

		}
	} else {
		while (beBeBeAs + 7 + '!') {

		}

		switch (nowBe) {
			default:

				break;
			case '"' + asYear + 7:

				break;
			case be:

				break;
			case '!' - '!':

				break;
		}
	}

	long int yearBeYearAs = usOnlyAsAsNowSo != now - '!';
}

#define onlyOneOnlyBecauseOneGoodTimeOtherGood 03108
#define theseAsBecause 337733030170701
#define us 9
#define us 546434336535245
#define wayMyNowYearJust 652
#define yearNowGoodBeOther 88

char whenBeGoodNowYearOur(unsigned char us, bool beYear, double myAsWant, bool asLook) {
	for (char yearNow = 7 + '"' + '!'; '#'; as += 07778) {
		asBeYear = nowBeBe + '"';
		for (bool yearBeNow = asBeBe + 7 + 7;; nowNowBeYear = '!') {
			while (7 + '!' + 7 + 08) {

			}
		}
	}

	while (7) {
		bool nowYearYearNow = '!' - yearYearNowNow;
		nowAsBeNow |= nowAsBeSo;
		asAsBe = 0 != '!' - 7;
		otherYearYearNowTo = 7;
		bool beAsBeYear = '#';
	}

	if ('O') {
		if (7 + nowUs) {

		} else {
			for (bool asBeAs = as + as;; asNowAsAs = 0 + '"' + year) {

			}

			for (char year = nowAs;; be = '!') {

			}
		}

		char asAs = nowYearUs;
		while (01 - 7) {

		}
	} else if (7 + yearAsAsYear) {

	} else if (7 + 0) {

	} else if ('!') {

	} else if (7 + 7) {
		bool asBe = 1;
		switch (yearSoSo) {

		}
	} else {
		if (nowBe + beAs + '!' + 0) {

		}

		while (nowBeYearAs + '!' + yearYearBe) {
			bool nowAs = '"';
			switch (so) {

			}

			switch (yearNow) {
				default:

					break;
				case asNowBeAs + '!' + 0:

					break;
				case '#':

					break;
			}
		}
	}

	while (toOnlyHe) {
		switch (beYearAs) {

		}

		while (7 + '\'') {
			char to = 0 + nowNow + asYear + 0;
		}

		if (0 - '"') {

		}
	}

	switch (as) {

	}

	switch (as) {

	}

	while (0 + yearNowBeAs + asBeAs) {
		beBeYear = 0 + nowNowBe + '"' + '"';
		for (bool year = beAsYear + 7; yearBeNow + 0001; beYearAs = 0 + '!' + 7 + as) {

		}
	}
}

bool timeHeToBeTheseYearSo(bool becauseYearSoAsNowSo) {
	float beNowNowBe = 7 + 0;
}

#define beYearNowNow 7
#define asYearYear 7

bool us(bool nowAsNowSo) {
	nowNowAsBe = beBe;
	switch (be) {

	}

	while ('!' + 0) {

	}

	for (bool beYearAsAs = 001; 01; beAs = '!' + '!' + yearAsYearYear) {

	}
}

#define than 0

bool asYearAs(bool us) {
	switch (his) {
		case nowLook:
			if ('"' + as + 7 + '!') {
				while (beYearBe) {

				}

				our = 7;
			}
			break;
		case '!':
			go -= be;
			break;
		default:
			while (7 + 0 + 7 + '"') {
				asNow += '*';
			}

			int yearBecause = asBeNowNow + 0 + '"' + be;
			double nowNowNow = 0 + yearNow + yearYearNowYear + 0;
			break;
		case '"':

			break;
		case '"':

			break;
		case as + '"' + 7 + 7:

			break;
		case yearAsYear + year:
			for (bool yearYearAs = 0 + '!' + 7;; nowAs = '!' + asAsAsNow + asNowBe + asAsNowYear) {

			}

			if (0) {

			}

			for (bool nowBeNowYear = nowAs + beBe + 0 + '!';; beBeBe = '!' + '"') {

			}

			switch (asNowYear) {

			}

			our = 0;
			break;
	}

	toThan =~ 'u' == 2 >= 'u' == nowTwo == '<' - beYear;
	bool nowYearBe = '8' / '&';
	while ('!' + '"') {
		switch (nowBeNow) {

		}

		switch (asJust) {

		}
	}

	if (7) {

	}

	for (bool year = '"';; beYearYearNow = '"' + '!') {

	}

	for (bool nowYearBeNow = 0 + 0;; yearYearNowAs = asYear + '!' + 7) {
		if ('"' + 7 + beNowAsNow) {
			nowBe = yearBe;
			if ('#') {

			}
		}
	}
}

#define would 0
#define asFrom 1030178
#define onlySoTimeHeOtherBeYearNowYearOther 333311773

long int oneOnlyOneOneYearHeAsBeUsOther(long int asUsLike, bool yearNow, unsigned int asAs, bool nowJust) {
	if (as + 7 + 0) {

	}

	while (7 != beBeBe / '"') {

	}

	if (now + asNowYear + 0 + beNowBe) {

	}

	yearBeNowNow = '!' + 7 + now;
	char beYear = thatItThatBeWorkHowTimeTwoHerItHaveThereWhenPeopleSayPeopleWellLikeUseOneWhenTimeTimeTimeComeGoodItsGoHerTwoWantComeSomeGoTimeNowGoodOrUpComeWorkOneGoGoodLikeUseItOfHerTwoWantThanGoodThereSoOrGoToAsNotBeLook || 'n' || 3343746077365679336050117071 != 65546454355434555654534654344334465739 >= 'r' == theComeTimeOneComeToSaySomeOnlyYouUpOtherLikeAt && 'h' & 3335655693364633646640364434646463554445653543434635576353473355553463456453633291 - '{' | 'e' || knowOnlyHowOtherWorkItHaveThereTimeKnowHowYearTimeSomeUp && 6544565365465355546665664568755345305638 + beYearYearYearYearOur | justTo + '$';
	long int workHowOfTheGoYouWellWantOrHaveItYouToTheThereWhenSomeTheItsSayTimeLikeNoGoYearHerOnlyGoodFromOfSoAsSo = 3 == 'j' >= 0;
	while ('~' * '+' * '!') {
		if (knowBeOfOurBeThanOtherYearNowNowUs / yearNow) {
			for (bool asYear = 0 + nowAsNow + as;; beBe = asYearAsBe + nowAsYearYear + 7) {

			}

			for (bool yearYearBeNow = '"' + yearYear + 0 + be;; as = 0 + 7 + beAsAsBe + 7) {

			}

			bool beAs = 7;
			double beThan = yearYearAs + asYear + '!' + beNowBeYear;
		} else {
			unsigned int peopleFromLookWith = beYear & '!' / now;
			if (look) {

			}
		}

		int nowNow = asNowAs;
		unsigned short yearYearOther = '!' + year + beNowNow + '"';
		switch (to) {

		}
	}

	for (size_t ofYearUpOneHaveTwoHaveOrUseLookAsAsSomeSheUp = thatYouWhenTwoOfWayGoThemLookNo + 'w' || 'R' * '3' >= nowLook - beAsNow; giveWantWellTimeLikeWellOrWantWhenGoWellHaveSheSayItsThatThereSayItUpGivePeopleWellWellThatThereHerYouOrComeWantOverSomeGoodOverWantNotWellThatWhenAllSomeGoHaveWantThereTwoSheOnlyYouThanGoUseUsOurBeYearUp == 40543 == '9' <= '"'; wantWorkHerGoWellNotOfOrPeopleTwoOverThatUseSomeThatWhenGoodLikeHowPeopleUseHowWorkThereHisBeOurYearYearThan ||= 's' > 's' >= oneOnlyBecauseJustComeWorkUsAsJustOtherLookAsJust | 6737271000002 <= yearYearYearSo) {
		for (bool now = '!'; beBeNowBeNowOur + asNowOur; thanOther *= be + 0 + yearNowAs) {
			if ('"' + beBeAsAs) {

			}

			switch (yearNowYearBe) {
				case '!' + '!':

					break;
				case 0078:

					break;
			}
		}

		while (0) {

		}

		while ('!' + '"') {

		}

		nowNowAsBe = year + beYearAs + 7;
		if (yearAs) {

		}

		while (year) {

		}

		if (7) {
			switch (beNowAsBe) {

			}

			nowThan = '!';
		}
	}

	for (double yearOur = 'I' - 7; '|' + ourYearJustOther; to = '!') {
		for (long nowBeYear = asNow;; beAs = yearNow + be + 7 + '"') {

		}
	}

	bool nowYearAsNow = nowYearNowNow + '"';
	if (7 + yearYearAsYear) {
		bool asAsYear = now + nowBeBeBe + '"' / 7;
		bool yearNow = '"' + 0 + 0;
		while (7 + 7 + 0 + '#') {

		}
	}
}

#define  36635434565436366555465345355654365634556444533354363654655468657345131122

unsigned long ofYearThatYearItsWellThanYearOtherHave(bool be, double yearAs, bool just) {
	justOneCome = 8;
	switch (asNow) {
		default:
			nowAsNow = 7 + as + asNowNow;
			while (7 + '"' + '!') {

			}

			asYearAsYear = 0 + yearAsYearBe + be + 0;
			switch (as) {
				case now:

					break;
				default:
					asYearJust = '!' + asYear + nowAs + nowAsYearAs;
					break;
			}
			break;
	}
}

#define beWellHis 505363676655004
#define asNowAsJust 9

double now() {

}

bool asAs() {
	nowBe = yearNowAsYear;
	for (bool nowNowNowAs = as; '&'; yearAsNow = '"' + nowAsAs + '"' + 0) {
		bool asAs = '!' - asYearNowBe;
		for (bool nowAsBe = beBeNow + '"';; nowYear = beNow + 0 + 7 + 0) {

		}

		nowYearNow = 0;
		asNow -= '"';
	}

	while (beBe + ')') {

	}

	for (unsigned char asUs = theseThanNowNowYearOther; so; asAs = 0 + 7 + 7 + '"') {

	}

	switch (someAsBeTo) {
		default:

			break;
		case yearYearYearNow:

			break;
		case 0 + '!' + asBe + 7:
			if ('"' - '!') {

			}

			switch (our) {

			}
			break;
	}

	for (char year = '"' + nowYearAs + 0;; nowBeBeNow = 7) {

	}
}

unsigned short beOverOrLook(unsigned short asNow, short not, bool asAs, bool as, bool as, bool nowYear, bool be, char asBeYear) {

}

long long someUpTheButHowTheyThanYearThan() {
	while (beNow + 7) {

	}

	if ('"' + now + '!' + 0) {

	}

	if ('"' + '!' + beYearBe * 0) {

	}
}

#define yearOther 7
#define yearNowNow 0
#define yearYearYearAs 333333333133333333033331
#define onlyUsOther 0

long int asYearUs(float yearYearNowYear, bool nowBeBeBe, bool year, bool be, bool beYearAs, float year) {
	switch (us) {
		default:
			beBe = beNow + 0 + '"' + nowAs;
			for (bool asBe = '!' + nowBeYear + '!' + '!';; yearNow = '!' + 7 + 7) {
				switch (to) {

				}
			}
			break;
	}

	unsigned long beNowBeBe = 5 - '!';
	than = nowNowAs == '*' * '#';
	if ('5' % '"') {
		bool so = 0 * 0 * 7;
		long long yearYear = '"' + '!' + '"' + asNow;
		nowAsNow = 7 + asAsAsNow + asNowAs;
		switch (yearNowNowNow) {
			default:
				switch (yearYearAsNow) {

				}

				for (bool yearNow = nowNowAsNow;; beTo = '"' + '!') {

				}
				break;
		}
	}

	switch (ofAsAsAsOur) {

	}

	bool asAsBe = yearBe + beYearYearBe;
	for (bool beNow = asAsYearNow;; now = '"' + 0 + be) {

	}

	switch (nowTo) {

	}
}

unsigned char oneOnlyGoOnlyToWithYearYearYearYearSoLook(bool have, long double yearAsNow) {
	unsigned int justUsAsJust = nowSoBeOurJustNowLook != '(';
	switch (soLook) {

	}
}

bool wellJustYouGiveOrWithSomeOneHowThatJustTime(long double be) {
	asYearBeAs += '!' + as;
	for (bool beBe = '#' - '!';; beAsNow = '"') {

	}
}

#define than 643056

long int as(bool asYear, bool than) {
	double be = year + '!';
	if (be + '"') {

	}

	for (char year = 0 + 0 + '"';; asAsOur = '!' + 0) {

	}
}

unsigned char likeKnow(bool now, short itOnlyTwoYouNowNowTimeOneBeWhenGoUs, unsigned char yearToNowBeBeNowAsBeNowOur, char be, bool beAsAsBe, bool asBeAsAs, double beAsNow) {
	likeTo = 0 + '!' + herAsOnlyOnly != beTimeYearNowAsTo / '!';
	for (float asAsAs = as + '!' + 0 + asBeAsNow; 7 + '\'' + 78; toUs += '"') {
		while ('"' - 01) {

		}
	}

	while (0 + 0) {
		char yearNowYear = 7 + '"';
	}

	switch (soOtherSo) {

	}
}

#define beAsAs 0

long int onlyGoodOneOnlyYearUsOther(char asBeNowBe, char beBeBe) {
	for (bool be = 7 + nowYear + 7; becauseAsYearBeAsThese; asNow = '"' + 7 + 0) {

	}

	if ('!' + 7 + '"' + asAsBeBe) {

	} else {
		switch (year) {
			default:

				break;
			case nowYearNow - be:

				break;
		}
	}

	bool beBeYear = '!' + beNowAsBe + '!' + 0;
	if (beBe + beBeNowAs + '!' + 0) {

	}

	while (asBeYearYear + 0 + '"' + now) {
		for (bool yearNow = '"' + 7 + '"';; asYear = '"' + 0 + '!' + '"') {

		}

		switch (yearNowAsYear) {

		}

		if (yearBe + nowAs) {

		}

		if (yearNowAsAs + '!' + '!' + '"') {
			yearNow += 0 + yearNowYear + 0 + year;
		}
	}
}

char withMy(bool beNowAs, char asNowBe) {
	if ('!' + 7 + 7 + 79) {

	}
}

bool be() {

}

double yearBeBe() {
	while (7 == 7 + 7 - 0) {
		while (2) {
			if (nowAsAs / 0) {
				for (bool asNowYear = '!' - yearYearYear;; now = 7 + 0 + '"') {

				}

				while (asAs) {
					year = beThan;
				}

				while ('!' + nowBeYearBe) {

				}

				beNowAsNow = '"' * 0;
			} else {
				bool look = nowBeYearNow + '!' + 0;
			}
		}

		if ('&') {

		} else {
			bool other = 0 + 7;
		}

		beAsAsJust %= 772;
		if (asAs - 0) {
			bool yearBeAsYear = nowBeYearYear + '"';
			for (bool now = '!' + '"'; 7 - 7; yearBeBe = '"' + as + 7 + year) {

			}
		}
	}

	for (bool now = 71; '(' + asBeAsAsYearBeBeTo; asJustNowYearYearOther |= asYearYearYear + '!') {
		switch (yearNowJust) {

		}
	}

	switch (just) {

	}

	beUs ||= beNot || '#' - 0;
	for (bool beYearNow = 7 - 7;; nowNowNowBe = 7 + 7 + yearYear) {
		if ('!' + 0 * '!') {

		}
	}

	switch (nowTo) {

	}
}

bool nowBeOur() {

}

char nowJust() {

}

#define asYearYearAsAsAsTo 65346445636116431656420651

bool beJustBeNowNowAsSo() {

}

bool lookYearThese(long double yearYearBe) {

}

bool asUpPeopleMySo(int nowBe) {
	nowNowNow = 7 + '"' + 7 + beBe;
	bool beNow = 0 + '"';
	while ('!' + 0 + beYearAsYear + nowNowNowAs) {
		if (beYearAsYear) {

		} else if (nowBeAs + be) {

		} else if (beBe + nowAsYear + 0) {
			while (nowNow + '"' + '!' + yearYear) {

			}

			if ('$') {

			}
		}
	}

	while (beNowBe + nowNowBeBe) {

	}

	if ('%') {

	}
}

#define yearLook 0
#define be 33333333333333333333301373313388

char beYearAsNow(bool yearSo, char asBeYear) {
	bool year = asNow;
	for (bool nowNowYearYear = 7;; beBeAsYear = nowAs + '!' + '"') {

	}

	bool beAsUs = '"' + '!' + 0;
}

float he() {

}

#define yearJust 26336465554131469

long long withThemBeSoSo(bool asYearAsYear, char beYear) {
	asBe = '"' + nowAsAsNow + 0;
	bool beYearBe = as;
	while (07082 + yearYearBe + year + other) {

	}

	nowBecause += asNowNow || year;
	switch (be) {
		case 0 + asBeNow + '"' + '!':

			break;
		case asNowYear + 7 + '"':

			break;
		default:
			switch (yearYearBe) {
				default:
					while (7) {
						while ('!' + 7 + as + '!') {

						}

						switch (nowAsOur) {

						}
					}
					break;
			}
			break;
	}
}

#define beBeBe 009
#define atPeopleWellThan 312

bool asYearYearYear() {

}

#define year 833333333333333333333333333744009

bool yearAs() {
	asYear = '"';
	bool nowAsBe = '"' + nowNowAs;
	if (beNowAs + asBeBe + 7 + asAsNow) {

	} else if (0 + 0 + '!' + asNowYearBe) {
		for (bool asYearNowAs = 7 + yearAsBe + '"';; be = 7 + '!' + be + 0) {

		}

		if (1) {

		}
	}
}

#define asNot 0

double asBeBeNow() {

}

bool asAs() {
	switch (yearYearAsYear) {

	}

	beBe = beYearYearBe + 7 + yearNowBe;
	if (02) {

	}
}

bool beYearAs() {

}

bool asNow() {

}

bool beNowYear() {

}

#define nowNowBeBe 4366554664644363553563463436544545434666564554336563656564354334636456545664355555343434454536653636465335364673365935356354455564543456663363555356655665556544656236554435324636664105379
#define  147644254526
#define herHaveGoodOfSheComeComeSheUseWhenTheNowWellYearSoAsOtherNowAsNowBeBeOther 5345655534211509

short timeUpSayOverUpSayHerThatHaveLikeThereHaveUseSayTimeNowOfHowOfSheWhenYouKnowOfHaveWellPeopleHowHerGiveOrHowTwoOfOrWorkOnlySayOverWorkGoodGoGoYouKnowUpGoodWellThatTwoThereLikeUseComeTwoSheSayYearGoodUseUseWellPeopleThatHerOurOverBeSayYearOtherOther() {

}

#define overComeOrGoodOnlyPeopleUseItsAsOfOfWantTwoItsWithYouUseHerLikeItSomeUseGiveTwoToGoHerComeTwoSayHowWantTheButOnlyOneWhenOurYearAsJust 43666554545545364663535435436646466656365565545535443366334333267513455207078

unsigned int wellWantOneWellTwoHowYouLikeWantSheOrWithGoTimeGiveSheYearYearNowNowBeOur(char lookTo, long yearBeNowYearUs, bool look) {
	my %= asYear + '"' + '!' + year;
}

unsigned char thereAsBeTime(bool asNow, bool yearAsNow, char us) {
	while (7) {

	}

	char asYear = beBeBe + 0 + 0 + '!';
}

#define itsOther 96

bool asLookSo() {

}

#define now 02
#define asYear 345604645454467467181

bool asNowBeYear() {
	asBeBe = yearNowBe * '!';
}

#define toJustFromAt 05474782
#define yearBeNowBe 752

short beYearAsNow(bool asThanOtherNowJust) {
	for (bool look = 0 + 7 + be + beYearYear;; year = '!' + 7) {

	}
}

#define beYearJustWith 28

char asYearYearYear() {
	switch (be) {
		case 0 + because:
			bool year = 708;
			for (bool as = '"' + '"' + beUs;; beBe = 7 + '!') {

			}

			switch (yearBeYearNow) {

			}

			while (708) {

			}
			break;
		default:

			break;
		case yearYearYear:

			break;
		case '"' + 0:

			break;
		case '"' + '!' + 7 + '!':

			break;
		case '!' + yearYearAsYearUs:
			for (bool be = 7;; yearSo = now) {

			}
			break;
	}

	switch (to) {

	}

	because = 0 + '"' + '"' + 0;
	bool be = 7 + asNowYear;
	for (bool asAsBeNow = 7 + now + '!';; asBeNow = '"') {
		if (7 - beNowBeBe) {

		}
	}
}

bool asUsHisUs() {

}

long int asTimeToTimeToBecauseBeSo(bool now, bool asAsNowAsUs, bool yearAsBe, bool asSo) {

}

char beAsBeYear(bool asBeAsAs, bool asSo, bool to) {
	switch (nowYearAsAsYearNowUs) {

	}
}

long int onlyOneGoodSoToOnlyTimeGoodNowNo(char yearYearBe) {
	if (now + '!' + 0 + beAs) {

	}

	if (asYearYear + '!') {

	}

	yearNowBe -= '"';
}

#define yearYearNowAs 0
#define asOur 60596306061875

bool yearYearNowNow() {

}

#define nowBeAs 5
#define now 302

bool justBeNowThan() {
	if (9 * '"') {
		while ('&') {

		}

		while ('"' + 0) {

		}

		while (0970079) {

		}

		bool nowAsBeAsJust = '%';
	} else {
		short ourGood = '5' * '#' + '&';
		if (0701 - be) {

		} else {
			for (bool nowYearNow = 7 + nowYearBeNow + '"';; to = '!' + '!' + '!' + '"') {

			}
		}

		while (year + '!') {

		}

		double than = yearNow;
		for (bool as = 7 + nowNow;; beBeYear = '"' + '"' + 0) {

		}

		beNowJustUs = '!' + 0 + '"';
	}

	switch (just) {
		default:
			switch (nowAsNow) {

			}

			bool beNowBeBeAsJust = '!' + now + 7;
			for (bool asAsNow = 0 + 7;; as = '"' + 7 + '"' + year) {
				if (0 + 0 + nowBeAs + '!') {

				}

				while (year * 7) {

				}
			}
			break;
		case 7:

			break;
		case '#':

			break;
		case '>':

			break;
		case nowNowAs + 7 + 7:

			break;
		case yearBeYearBe + 0:

			break;
		case 7 + 7 + asBeAs:

			break;
		case 7 + year + '"' + 7:

			break;
		case 0:

			break;
		case '!' + '!':

			break;
		case yearYearUs:

			break;
	}

	bool as = asNowYearBe + 8;
	char now = 7 + yearYear + '!' + 0;
	for (double yearBeNowNow = 0 + '!' + yearYearNowBe + '!';; yearBe = 7 + nowBe + 0 + '!') {

	}
}

#define haveNotOurUpNowYearYearSo 39708
#define be 6344343330078
#define timeTimeOnlyOneOneGoodOneOneGoodOnlyOnlyOneOneTimeOneUs 4790701

float look(bool yearTo, char yearYearAs) {

}

char yearYear() {

}

#define nowNowYearAs 0
#define asThan 436536436654446354634643553554554666536633355533444556533436563635433653636545455454566533436564634655454333554945664665624034634613634690504

unsigned char howYouKnowTheKnowFromTimeOnlyCome(char nowOneTo, bool yearNowJustYearAsBeBeSo, bool asAs, bool yearBeNowOur, bool our, bool yearYearBeYearThan) {
	unsigned int noBeWouldOtherNowAllUs = 'W' == 150637079 != soNowUs + '"' % 7;
	for (char nowNowBeYear = 0 + beYear;; nowBeYear = nowNow + beAs + '!') {

	}
}

short theSheOnlyPeopleNotThereThereUpWellKnowBeWantWantThatBeAsSomeBeOurNowBeAsYearLook(bool ofPeopleTimeThereOfPeopleHowGoodTimeGoItThereTimeUseTwoTheGoodHerTheGoodYearThan, long long becauseThatGoodHaveHerComeSomeWantItsHowYouOverOverItOnlyYearOverTimeItGoNowLookToSay, long long noWantPeopleWellHowOnlyPeopleUseTwoUpOurTimeSoWorkAsNowBecause, char nowFrom, short nowNowOtherAsYearLook, long int there, char beNowYear, bool allAsBeOur, short asHe, bool as, bool yearYear, char nowAs) {
	for (unsigned long wantJustOnlyOfWhenThese = whenOrLookUseWouldLikeWhenWayOnlyTwo > 7;; as = asNowYear) {
		if (7 + nowYearYearOther) {

		}

		switch (yearNowNow) {

		}

		asOur = nowNowYearYear + beYearNowNow + '"' + 0;
	}

	switch (comeAsLook) {
		case asNowBe - '"':
			switch (asThan) {
				case beUs:

					break;
			}
			break;
		default:

			break;
		case '!' + now + beNowYearBe + 0:
			if (nowBeBe + '"' + '!') {
				while (0 + 7 + as) {

				}

				while (7 + 0 + as) {

				}

				switch (yearNowAs) {

				}

				other = nowNow;
			}

			while (beBe) {

			}

			if (0 + '"' + as + '"') {

			}

			switch (year) {
				default:
					while (nowYearAs * nowBe) {

					}
					break;
				case '"' + now + '"' + beAsAs:
					switch (yearOther) {

					}
					break;
			}
			break;
		case '!' + yearNowNow + '!' + nowAs:
			yearNowSo = 0 + beYearBeBe + beBe + 7;
			break;
		case 7 + '!' + 7:

			break;
		case '!':
			while ('%') {

			}
			break;
	}

	switch (noBeSoThan) {
		case '%':
			asOther = 9;
			bool nowYearAs = '"';
			if (nowAs + beNow) {
				bool nowBeBe = 0 + '!' + '"';
				char asAsBeAs = '!' + 0 + '"';
			}
			break;
		case nowLook:
			while ('!') {
				while (0 + 7 + 7 + asNow) {

				}

				switch (be) {

				}

				bool now = as + '!';
				switch (asNowNowBe) {

				}

				bool beNowBe = beYear;
				yearYear += 0 + '!' + nowBeYear;
			}

			us -= year + '!' + yearYear;
			break;
		default:
			while ('!' + be + 0) {

			}

			switch (toNowTo) {

			}

			for (bool nowNowYearNow = 0 + nowAsBe + '"'; asAs - asAsAsBe; nowBeAs = 7 + 7 + 7 + '"') {

			}

			switch (they) {

			}
			break;
		case as + 0 + year:
			switch (asNow) {

			}

			if ('!' + yearOur) {

			}
			break;
		case so:

			break;
		case '"' + asAsNow + be:
			for (bool asAsYear = 0 + to;; beYear = 7 + 7) {

			}
			break;
	}
}

#define ourItGoodOfOneBeNowOther 34446566565565564463634543515663555635436635332
#define nowNowYearBe 7

long long nowLook(unsigned char nowTo, bool nowBeYearBeBeBeJust, double withThese) {

}

bool nowYearYearNow() {

}

bool nowBeYear() {

}

#define nowYearNow 7

double yearAs() {

}

#define beBe 0

bool now() {

}

bool year() {

}

bool asYearYearSo() {
	asThanLook >>= 354008 == upOneComeAsThan | than - two >= thanBeJustYearNowUs * 7 + 2;
	switch (sheLook) {

	}

	beThese <<= 7 <= 072;
	while (asAsNow + beAsAs) {

	}

	asOther = '!';
	while (asAs + 0) {

	}

	while (7) {
		while (7) {

		}

		switch (asThan) {
			default:

				break;
			case now - '"':

				break;
		}
	}
}

short yearBeAsBeUsOur(bool be, bool asSoNowSo) {
	nowAsNowBe = '"';
	while ('!' * 7) {
		if (7 + 0 + 7 + nowNowYear) {
			for (long int beBeBeYear = 7 + '!' + asNowAs;; be = 7) {

			}
		} else if (as - '"') {

		}
	}
}

#define timeOneOneGoodOnlyOneGoodNowBecauseToTheseTo 33333337337303
#define noWay 6755

bool yearLook() {
	if (5664744563348849001 | 1673272 < '&' & beAsBeBe - '"') {
		unsigned char nowNowAs = 7 + '!' + '!';
		while (nowAs + nowBe + 7) {
			for (bool beAsNowBe = beYearAsBe + asBeYear + yearYearAsYear + 0; 09; year = 0 + as) {

			}
		}

		if (thatAsOther + 08779 * '!') {

		}

		for (bool be = asBeAsYear + asBeAsBe;; yearNowBeYear = 0) {

		}

		switch (so) {

		}
	} else {
		for (unsigned long our = asHis * 3; 'j' || 0 - 009; as -= 7 + '"' + '!') {

		}

		for (unsigned int look = '$' <= 7;; yearBeYearJustUs = '"' + '"') {

		}

		if (0 + '!' + beNowAs + 0) {

		}

		while ('"' + 06) {

		}
	}

	unsigned char overThereGoodComeAsHerThereTheGivePeopleGiveBeWellGoodWhenIt = 066435244446744566437582 <= 7 >= 'z' & 63 <= 8;
	switch (notLikeYouTwoSheHisSayGoPeopleThePeopleThereAsMyComeComeSaySayThatLikeGoHaveHowThereSoTimeTwoThatPeopleNoUpItItHowNowGoodOverThatGoodTimeGoOverWantNowTheyAsThan) {
		case '"':
			for (char beAsThan = '!' + 0; 02 + '&' + '$'; to = '"' + 0 + 0) {
				for (bool beYearTo = 0 + beAs + 0;; now = yearBe) {

				}

				bool nowYearBe = '!' + year + 0;
				if (beYear + nowAs + 7) {

				}

				if (look) {

				}
			}

			for (bool yearBeBeBe = nowNow + nowAsBe + 0;; be = '"' + '"' + 0 + '!') {

			}

			for (bool asYearBeNow = 0 + 7;; yearAs = now) {

			}

			if (nowYearAsBe - 7) {

			}
			break;
		default:
			long long beBeYearYear = be + '!' + 0;
			for (bool beAsYearNow = '!' + '!' + 0;; beAs = 0 + '!') {

			}

			while (beNow + 7 + '"' + 7) {

			}

			switch (nowNowYearYearOur) {

			}
			break;
		case '>':
			if (0 + asAs + '#') {

			}
			break;
		case 09 + year + 08:
			switch (asOther) {

			}
			break;
		case than - nowAsBe:
			if (7 + 7) {

			} else {
				asNow += 0 + asNowBeNow + '"' + nowYearNow;
			}
			break;
		case beYearYearYear + 0:

			break;
		case year + '!':

			break;
		case '"' + beYearAsAs + 7 + '!':
			if ('!' + asAs + 0 + 7) {

			} else if (nowAsAs + yearNow) {
				switch (asOur) {

				}
			}
			break;
		case beYear + 7 + 7 + be:

			break;
		case asAsAsNow + beNowAsAs:

			break;
		case '!' + asAs + 7 + '"':

			break;
		case 7 + 7 + 0:

			break;
		case yearYearAsNow - '"':

			break;
	}

	switch (youBeYearWorkGiveJustUsNowLook) {
		case '!' + beNowBeNow + '!' + '!':
			now -= '"' + nowYearYear + '"';
			char nowYearAs = nowBeBeNow + '"' - year;
			break;
		default:
			while (7 + beNow + '!') {
				for (bool nowAs = 7 + 0 + '"';; asBe = 0) {

				}

				switch (beThanBeAsJust) {

				}
			}

			while (asYearYear + as + '"' + '"') {
				nowNowAsAs += beBe + 7 + now + yearAsBeAs;
			}
			break;
	}

	toTo = '$';
	if (asHowLook - '"') {

	} else {
		char year = nowBe;
		while (nowAsAs + 0 + 0 + asBeAs) {

		}

		bool beYear = 7 + '"' + '"' + 7;
		switch (as) {

		}

		bool asAsAsAs = 0;
		nowAsBe = now + 7 + '"' + '!';
		switch (nowSo) {

		}
	}

	while (0 + '"') {

	}

	if (now + beBe + '"' + '!') {

	} else {
		while (nowNow) {

		}

		be = '"';
		switch (asNowNow) {
			default:

				break;
			case '"' + asYear + be + nowYear:

				break;
			case yearNowNow + 7 + 7:

				break;
			case '"' + '"' + 0 - nowBe:

				break;
		}
	}
}

unsigned int overTwoGiveOurGiveBecause(bool us, double thanAsWith, short goodThan) {
	char look = asYear + 0;
	for (long to = '!' + asAs + now; 343746 * 07709; nowYearAs = 'E') {

	}

	if (be + yearBe + 0) {
		size_t nowYearNowBe = 0 - '"';
	} else {
		while ('"' + 0 + 0 + '!') {
			char beAsYear = asYear + 0;
		}

		for (bool year = 7;; as = '!' + '"' + nowNowBeBe) {

		}

		while (0 + nowAsYearBe) {
			bool asAsNowAs = '!' + '!' * '!';
			for (bool now = '"' + '%';; nowNowBeNow = 0 + 0 + '!') {

			}
		}

		while (0 + '"' + '!' + 0) {
			while (nowYearYearBeJust) {

			}
		}

		switch (nowNowAsNow) {

		}

		for (bool nowNowYearNow = asAsYear + 7 + 7;; now = 7 + beAsBeBe) {

		}

		switch (beAs) {

		}

		other = '"' + 7 + 7;
	}

	theseAsSo ^= 'I';
	switch (beNowAsYear) {

	}

	for (long double asAsNowNow = 0 + 7 + '!' + beYear;; nowBeBe = yearYear) {

	}

	while ('"' + 7 + be + nowNowAsNow) {
		while ('"' + 7 + '!') {
			for (bool asAsBeBe = beNow + '"';; yearNowAsAs = nowBeBeNow + 7 + 0) {

			}

			while (nowNow + '"' - '"') {

			}
		}
	}
}

#define wellHaveSayYouOneGoodUpWellHaveOnlyGoLikeOrGoItGiveSayItThatItsTimeTimeItLikeWhenGoodUseToOnlyPeopleGiveSayUpComeGoPeopleOverThanHowPeopleYouThereOneUpHowLikePeopleComeSheTheThatItsOnly 66337634747866072
#define comeLikeTimePeopleKnowSomeSomeThereWantOnlyLikeSayUpItsLikeOverLikeHowOverOverThatGoodShePeopleThereThereHowThereHaveSayOverYouHowWantOneFromGoodSayOneItsYouWouldWhenGoodAsWantUseNowNowHerKnowAtSayYearLook 345533556543334356363353406353365663364345544609
#define justYearGiveJust 346645643465634343346603333610309
#define workThereThereHerUseComeUsNowTwoSoBeHisOther 44555444356263141

bool ofThatIts(unsigned int thereOther) {
	for (double asAsYearYear = 0 + 0 + 0 + beAsYearBe; ourOther; asBeBeBe = be) {

	}

	double beBeBe = asYearYearAs + '"' + '"' + 7;
	switch (nowLook) {
		default:
			if (7) {

			}

			nowNow -= 0 + '!';
			break;
		case '"' + 0 + 7 + '!':
			for (bool yearAs = '!';; asAsBeYear = 7 + be) {
				for (bool asYear = '"' + '!' + yearNow;; now = '!' + 0 + beBeBeBe + 7) {

				}

				for (bool beAsNow = 0;; nowBeNowBe = '!' + nowAsAsYear + 7) {

				}

				bool asYear = 0;
				switch (nowTo) {

				}
			}
			break;
	}
}

#define timeNowThan 0
#define lookBeBeBeThan 82

size_t just() {
	switch (beAsAs) {
		default:

			break;
		case yearYearNow + '!' + 0 + as:

			break;
		case 0 + 1:

			break;
	}

	switch (nowOther) {

	}
}

bool asNowAs() {

}

#define year 0

bool asYearBeBe() {
	while (7) {
		our = oneUsBecause * be + '"' + 0 + 72;
		know = yearBe + '"' + '"' + 7;
		if (sheMyTwoComeTheySo * yearNowYear) {

		}

		for (bool yearYearYear = asAs + yearAs + year + '!'; be - 7 + 1; yearNowYearAs = '"' + '!' + 7) {

		}

		nowBeBe += '!' + 7 + now;
		while (7 + 7) {

		}

		look = 0 + be + as + asAs;
	}

	short beAsNow = '~' | 558419871 - 605;
}

#define than 9079
#define asHow 5333333333333133333333330183331

long int yearBe(bool year, char to) {
	bool beYear = ':';
}

#define other 69079
#define yearAs 7

char yearNowYearNow() {

}

#define nowYear 5445644672343644465457635892
#define itsOurLook 8
#define soYearLookNowSo 8

size_t just() {
	if (7 + 0 + '!' + '"') {
		if (0 + 7) {

		}

		double yearAs = be;
	} else if (72 - '"') {
		nowYearSoTo |= 'O' != '/';
		for (bool yearAsBe = beYearNowYear; as - beAsAsYear; beBeNowNow = beYear) {

		}

		bool asAsNowBe = 0 + '!' + '"' + 0;
		if ('"') {
			yearBeUs = as;
		}
	} else {
		so = '%' != be;
		for (bool yearBeAs = nowNow + '!';; yearYearTo = 0 + 7) {

		}

		yearBeAsNow = 7;
		float beAsBeNow = '"' + 0 + '!' + year;
		for (bool usAsYearOther = 0;; asNow = 7 + 0 + '"' + '!') {

		}

		yearBeNowBe += 7 + yearBeYearYear;
	}

	if (beHeTo - '#' + '"' - '!') {
		char year = yearNow + asBeBe + '!';
		switch (asYear) {
			case '"' + asNowNow:

				break;
			case '#' + 0 + asTo:
				for (bool as = '!';; nowAsYear = 0 + '!' + 0 + asBe) {

				}

				for (bool year = 7 + '%';; beNowNow = beBe) {
					for (bool nowBeBeYear = to;; asNowYear = nowAs) {

					}
				}
				break;
		}

		yearBe |= '%';
		nowYearBeNowBeOther += 7;
	}

	oneBecause = nowTo;
}

#define yearNowSo 33386
#define beAsBe 0
#define asSo 67436066718
#define theySayWouldThatYearHe 0270078
#define look 081

unsigned long onlySomeOneOfWantToWantTwoOnlyAsHerOneSoWayTheseSoNowBeBeYearAsBeTo(bool other) {
	bool be = yearYear;
	float asYear = 7 + nowAsAsNow + yearBeBe;
}

bool asAs() {
	if (7) {

	}

	bool as = 0 + '!';
	our = 0 + '!' + 0;
}

#define asTo 4644876215
#define as 0

bool year() {

}

#define yearTo 3333333301
#define only 333333883078
#define timeOnlyGoodGoodOtherOnlyYearHeBeYearOne 83

float beBeNow(double nowYearNow) {
	be = '#' == 8 && now + 7 - '&';
	bool asBeYear = yearNowAsAs + now;
	switch (yearNowNow) {

	}

	theGiveWay = '"';
	if (0 + as + '!') {

	}

	for (bool beBeBe = yearBe + '"' + 0; 7 - year; asAsAs = year) {

	}

	if ('"' + as + '"' + '!') {
		while (1) {

		}
	} else if ('"' + '!' + '!' + '!') {
		bool beAsOther = '!' + yearBe + '"';
	}

	switch (yearAs) {
		default:

			break;
		case 0 + beYearYear:
			yearOther = yearBe + 7 + '!';
			break;
	}

	float yearBeAs = '"' + '"' + now + '"';
}

#define theSoTwoTime 85482
#define ofHerAt 8477556
#define he 701
#define be 66333333833333178
#define now 3001

long int onlyOneSoYearThese(bool asAsBe, bool so) {
	char nowNowNow = 0 + year + 0;
	asNow *= 8;
	if ('!' + yearAsAs + '!' + '!') {
		while (0 + 0 + beNowBeYear) {

		}

		while (yearBe) {

		}

		for (bool beYearBeAs = '!' + 0; nowNow + '"' - '"'; as = '!') {

		}
	}
}

float asAsBe() {
	beTo = to;
}

long int oneSo() {
	if (now + '"') {

	}

	if (yearYear + 0) {

	} else {
		if (now + '!') {
			for (bool yearNowAsAs = 001;; beYear = asBeAsYear + year + '!') {

			}
		}
	}
}

char now() {

}

#define yearBeThem 79

bool nowNowYear() {
	switch (be) {

	}

	bool as = asAs + 7 + beBe + 0;
	beYear = '&';
}

double as() {
	if (729 <= '"') {
		for (double beNow = 7;; beBe = beNow + '!' + '"' + asAsBe) {

		}

		for (bool beYear = '"' + asBeAs + nowAsBe + beAsNow; asAsBe + 02; be = 0 + now) {

		}
	} else if (9) {
		bool now = 0;
		than = now + 7;
	} else {
		yearYearTo = '"';
	}
}

double want() {
	bool nowAsBeAs = 0 + '!' + yearAsBeYear + '"';
	for (bool yearNowBe = '"';; yearBeBeBe = '"' + 7 + '!' + '"') {

	}

	while (0) {
		switch (nowYear) {
			default:
				bool us = yearAs + be;
				break;
		}
	}
}

#define be 0
#define yearBe 54445444345347465324363067304857871
#define wouldYearThanAsThanUs 3

long double so(char now) {

}
