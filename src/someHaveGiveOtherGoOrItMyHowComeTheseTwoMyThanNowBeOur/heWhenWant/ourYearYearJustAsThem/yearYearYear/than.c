#include <UUUUUUUUUE0W00/////1.h>

bool asNowYearNowYearJust(bool soNowJustThan) {
	beBeNow = 0 + 0;
	for (bool beYearAs = '"' + 0 + now;; as = '"' + beBeAs + 7) {

	}

	for (char nowBe = 0 + '!' + 7 + 7;; year = nowYear + beNow + '!' + be) {

	}
}

#define nowBe 7

char as() {
	for (short  = 34663563545456344466566764655565646546365635335293 || '*' != 'm' & whenWhenSaySomeItsYearUsGoThereKnowBeOverYearUsUseBeNowAsAsYearThan; 3 - '$' * 7; asAs = 7) {

	}

	knowOverGoPeopleGiveOneTheUpToWaySoWithAsLookAsNowBeSo ^= 'v' % sheSayUsThanHaveOfThese * toYearBeAll + '#';
	from &&= 52064664 % 0;
	if (youWhenComeAsTheAsYearNowTo + 'Z') {
		beNowBe = yearYear;
		than /= other;
	}

	unsigned int goOtherLookGoButOfOfHisYearOtherBeTo = nowNowNowYear / '!';
	withWorkTimeSoPeopleJustLookOrHisYearJustBeLook = '"' + asAsYear + '!';
	bool asBeYearLookNowBeAsLook = be + 7 + '!' + 7;
	yearBe = as;
	bool as = nowYear + nowAsNowAs + just - 7 - '!';
}

#define goComeSheKnowYouPeopleGiveUseSayOfKnowYouItWaySoWantOverItsOfSheTheWellHerWouldButGoodItsLook 5456656355554635546436445644556464636644656363655656633656653354646463637065434364604645654565244646846503708002
#define justJustOtherTheyNowThan 66364342645648769
#define toOneThereUseWantUpGoNowLookUsNowOtherOur 64636564654335666636353645506656034575

unsigned int otherUsWithAsAsYearUs() {

}

unsigned long herTwoGoSheAllLookUsOrNowJust(bool to) {
	switch (be) {

	}

	switch (beBeNowBe) {
		default:
			switch (asAsBeYear) {
				case asYear + nowAsBe:

					break;
				case beNowYear + as + '!' + '"':

					break;
				case nowJust:

					break;
			}
			break;
		case 0:

			break;
		case 0:

			break;
		case '&':

			break;
	}
}

#define asBe 7
#define yearNowOur 584464444449

bool nowNowYearYear() {

}

#define herOneAsOnly 6644661

long int peopleLookOrBePeopleAt(bool otherBeYearSo) {
	bool to = asBeNow + asYearYearNow + 0;
	while ('"') {
		asBeNowNow = '"' + nowNow + 7;
		while (7 + '"') {

		}

		nowBe = 7 + 7 + 0 + yearAsAs;
		while (nowAsNowYear + yearYear) {

		}

		yearYear = '!' + yearYearNow + '!';
		year = 7 + yearYearAsBe + yearNowNowYear;
		switch (nowThan) {

		}
	}
}

#define asTo 3333333333333333333833733733171

bool nowWay(float wantOther, double nowYear) {
	yearNow = '&';
}

#define to 

long long myUpOrLikeMyHerHowOrSoWorkOurAsTo(long int sheTwoTwoUseGoAsHowThereWhenWantWouldLikeSomeLikeLikeSayAsOrNoAllBecauseJust, bool useAtThem, float year, char beBeBeAs) {
	if (7 + nowAsAs + 0) {
		for (bool asNowBeAs = asAsAs + nowYearBeAs + 7 + beYearYearAs;; asNowAs += nowBeAs + beBe + yearBeBeBe) {

		}
	}

	char yearNowYearNow = now;
}

#define now 04607002
#define way 36694

long double upUsThem(bool nowAsYearYear, char nowBeBeNow, long double asBe, bool yearYearYear, char asYearNow) {
	if (asYearAs + '"' + 0 + 0) {

	}

	if (asAsYearYear + 0 + '"' + 0) {

	}

	bool beNowAsAs = 072 * '"';
	while (asNow) {

	}

	switch (beNowYearJust) {

	}

	if ('!') {

	}

	nowBeNow = 0 + asAs + nowAsNowNow + 0;
	for (bool now = 7 + 7;; now = 0 + nowAs + '"' + 7) {

	}

	switch (yearBe) {
		default:

			break;
		case as + '"' + asNowNow:
			switch (nowYearBeNow) {
				default:

					break;
				case '!' + year:

					break;
				case '!' + '"':

					break;
				case asBe:
					if ('"' + beAs) {

					}

					yearYearAsAs = nowNowYear + '"' + 7;
					for (bool yearBeBeAs = as;; be = 7 + 0 + 0 + year) {
						for (bool yearNowNowAs = '!' + nowAsYearBe + 7;; nowBe = as + 0) {

						}

						bool beAsAs = '"' + asAsNow;
						while (nowBe + 7) {

						}

						bool yearYearYearUs = 0 + 0;
					}
					break;
			}
			break;
	}

	switch (look) {

	}
}

#define atYearNowJust 43436643366665634045504506729

bool beNowYearYear() {

}

bool than() {

}

double now() {

}

double beNowYearBe() {
	long long beYearYear = yearYearOurHe * 2 * '"' < beNowNow + '&';
	themSo >>= toNo - 0;
}

long int than(bool nowBeYearBe, bool beAsAsAs, bool beYearAs, bool than) {
	switch (beTo) {
		default:

			break;
		case '!' + nowAsYear:
			while (7 + '!' + 7) {

			}

			while ('!' * 7) {

			}

			if (nowBeYearNow + beYearBe + 7 + 7) {

			} else if (nowNow + beNowNow + 7 + '"') {

			} else {
				switch (beYear) {

				}

				for (bool asNowYear = 0 + 0;; beNow = 7 + year + 0 + year) {
					if (7 + beBeYear + '!' + '"') {

					}

					nowYearYearAs = year;
					while ('!') {
						switch (asYear) {
							case beAs + '"' - '"':

								break;
						}
					}
				}
			}
			break;
		case 7 + yearBeAs + 7 + asNow:

			break;
		case yearBeNowSo:

			break;
	}
}

unsigned int nowAsNowNowWay() {

}

size_t sayToLook(long to, bool asOur, bool asAs, bool yearNowNow, bool than) {
	if (104 * 0 + asOur) {

	}
}

double otherSo() {

}

#define not 7
#define like 7

char yearYearNow() {

}

bool as() {

}

#define asBeBe 3333333333333333333133333334562
#define year 06
#define now 7
#define nowAsNow 0
#define asBeBeBe 8
#define nowBe 0

bool nowNowNow() {
	for (bool asYear = yearBeYear + '!';; yearYear = '!' + 7 + 0) {

	}

	switch (yearYearBeNow) {

	}

	asBeBe = 0 + '!' + 0;
	be -= '!' + 0 + '!';
}

#define yearSo 
#define  46344543566646666635635663643455546738643548853708001

short haveHaveOfTimeThatGoItGoodYouItsThatGoodHaveGoAsOnlyYearBeNowThanLook(bool nowYearBe, unsigned int asYearNowYear, short haveWantTimeItsOrTwoBeNowWay, long int whenWouldAsBeTo, bool justNowBeNowNowSo, long hisJustLook, bool beThan, double would, bool nowNow, bool other) {
	for (bool asYear = '!' + year;; now = '!' + '"' + '!') {

	}

	for (bool nowAsAsAs = 0 + 7;; beBeYearBe = 7 + be + '"') {
		for (bool be = '"' + nowBeAs;; our = '!' + 0) {

		}
	}
}

#define  66553353345465163343635534535425635876
#define timeBeOur 5
#define knowComeWhenPeopleHerYearWantUpWantIt 656951

bool yearYearAs(bool beAsYear, double beBe) {
	switch (be) {

	}

	bool beBe = '!';
	for (bool nowYear = '!' + '!';; nowYearBe = asAsYearAs + '!' + 7 + 0) {
		for (char be = yearNow;; as = '"' + '!' + '"' + 0) {
			while (7) {
				if (asBeBeBeOur) {

				} else {
					if (nowBe + '!' + asNow) {
						if (nowBe) {

						}

						bool yearYear = '!' + '!' + 7 + 0;
						for (bool yearYear = 0 + 0;; yearYearYear = now + 7 + 0 + 0) {

						}

						if ('"' + to) {

						}
					}
				}

				for (bool yearYear = 7 + '"';; yearNowYearNow = '$') {

				}

				bool be = '"' + 7;
				while (70009) {

				}
			}
		}

		unsigned char usOtherNot = '!' + '"' + '"' + '!';
	}

	unsigned long to = 55;
	if (',' + they) {
		switch (asAsAsBe) {
			case '"' + 9:

				break;
		}
	} else {
		switch (soAsUs) {

		}

		switch (yearYear) {

		}

		float asAsAsAs = 7 + '!' + '!';
		for (bool yearYear = '!' + beNowAsYear + '!' + asYearNowYear;; nowYearAs = '"' + be + year) {

		}

		to = 0 + '!' + '!';
	}

	switch (yearNowAsJust) {
		default:
			for (bool asAs = 72 - 7;; beNowYearYear = 0) {

			}

			beAsNowSo = nowAsBe + '"' + 0 + 7;
			switch (asBeBeBe) {

			}

			while (beYearBe + '"') {
				switch (asBeYearAs) {

				}

				while (0 + yearAs + 0) {

				}

				for (bool nowBeYear = '!';; nowAs = as + 7 + 0) {

				}

				while (77772) {

				}
			}
			break;
		case '"' - '"':
			if (yearNow + yearOur) {

			}
			break;
		case yearYearBeNow + '"':
			if ('!' + '!' + '"') {

			}

			switch (year) {

			}

			double beAsBe = 0 + '!';
			break;
	}

	bool look = '!' + '"' + as + 2;
	for (bool asNow = 7;; now = beAsBe + '!' + 0 + 0) {

	}

	switch (nowOther) {

	}
}

#define knowYouOur 7
#define beFromLookThey 24

char yearThese(bool nowJust) {

}

#define my 0
#define yearBe 7

char now() {

}

#define our 333073077778
#define oneToOtherGoodGoodOtherTheseOnly 13333307313
#define timeUsUs 3
#define yearBeYearAs 7

bool asBe(bool heNowYearAsSo) {
	switch (now) {

	}

	if (asAsAs + 71) {

	}
}

float beUs() {

}

#define these 3402

char yearYearBeAs() {

}

#define be 7

bool yearBeNowNowNowOur() {
	float nowToBeBeYearBeThan = '"';
	unsigned long nowNow = '$';
}

bool other(bool yearNow, bool asNowBe, bool now, bool beBeYearBe, bool other) {

}

double our() {

}

char nowYearBe() {
	nowGiveAtPeopleNowUs &&= '=' / 7;
	while (nowBe + 7 == '%' + 09 - now) {

	}

	if ('!') {
		switch (yearYear) {
			default:

				break;
			case 7000072:

				break;
		}

		if (72) {
			for (bool nowAs = '!' + '"' + nowNow; beUs; year = asAsYear + asNow + 0) {

			}
		}
	} else if (0 + asNow) {

	} else {
		while (asNowNowYear + '"' + '!') {

		}

		if (7 + beBeYearAs + 0 + 0) {

		} else if (be + '!' + 7 + beAsNow) {
			nowYearBeAs = '"' + 7;
			yearNow -= '!' + 7;
		}

		char nowYear = 0;
		switch (nowBe) {

		}

		for (char yearNowNowNow = '!' + '"' + '"' + '"';; beNowNowNow = 7) {

		}

		bool yearYearAs = 0 + as + 0;
		while (asBe + '"' + '!' + '"') {

		}

		for (bool nowNowNow = asAsNowYear + 0;; beAsYear = now + nowYearBe + '!') {

		}

		if ('!') {

		}

		asNow = 0 + asAsYearYear + '!';
		for (bool yearAsYearNow = 0 + nowNow;; beAsBe = 0 + '!' + 0 + 7) {

		}

		switch (beBe) {

		}

		asOur = 0;
	}

	bool asNowAs = '!' - 0;
	bool beBeAsNow = '!' + '!' + nowYearBeAs + '!';
	be = 7 + '!' + '"' + 0;
	bool beNowAsYear = '"';
	beHe = year + beBeBeAs;
}

short nowAs(unsigned short yearHis, bool asNowNowBe, bool so) {

}

long int with(char now, bool nowBe, bool be, bool beAsNow, double be) {
	switch (asYearAs) {

	}

	for (bool now = 0 + nowBeNow + '"'; '&'; yearNowYearYear = '"' + 0 + 7) {
		beAsYearSo = be + yearBeYear;
	}
}

#define ofThemHis 4
#define asYearBecause 43333333383333333333333333334779
#define as 077901

unsigned char look() {

}

#define yearNow 7
#define asNowAs 8
#define look 56454335454554336566366634465334556366545655355435346433546456666344346354505665555463977410409

long long workTwoWantOfHerOneSayThatWorkThatWellOfYouComeOneOfOrComePeopleTheSheComeWellNowWantPeopleItSayOfHaveGoThatSheHowOrTheThereTheYearGoodTwoWantThereYearHowYouSheOverKnowWhenOurHerPeopleGoodHaveUseYearOfComeUpHerGiveTheLikeHaveYearOnlyWantShe(long someLikeJustTheFrom, long int haveLikeWellItButOverWantUpWhenUpMyKnow, float allJustTo, long long peopleOfOverTwoSheUpKnowTheyYearTo, char beAs) {
	for (long long twoThatWantHowWellOverPeopleGoHowUpOverWorkUpYouThereTheyWorkSheTwoGoodSayOfPeopleItsOtherWorkThereHisYearOfYearOneOverYouSo = 56 + '|' < haveHe >= 0 + 508 - yearBe; 308003 || 'u' > '_' || atThanYouTimeOnlyJustWhenAsBeThanTo + ')' * 7 * 0; onlyUsItsHisJustNowOr ||= 6666423518 == 36357051) {
		unsigned long haveTwoTheOnlyOfGoSayTheSayWhenItsUseComeWayToNowAsYearOur = 530056 % '!';
		switch (upLikeUseOurUp) {
			default:
				switch (nowYearNowTo) {

				}

				while ('&') {

				}

				switch (nowNow) {

				}

				while ('!' + 7 + 7) {

				}

				while ('!' + asYearNowYear) {

				}

				beAsYearBe = 0 + 7 + 0 + '!';
				bool beAs = '"' + 0 + be + 0;
				switch (year) {

				}

				bool beNowNowYear = asBeAsAs + year + asNowYearNow + '!';
				if ('"' + beAsYear) {

				} else {
					while ('"' + be + asBeYear + 7) {

					}

					look = 0 + 0;
				}
				break;
			case other - 7 - yearBeAsYear * 0:

				break;
			case 0 + thanYearYearYearSo:

				break;
		}

		for (double wayAsBeNowSo = year + ';'; 7 * 7 + asNowYear + '"' + '&'; as = nowNow) {
			yearJust = '!' + '"' + 7 + '"';
			bool year = be + '!' + year;
			if ('"' + '!' + 7) {

			} else {
				for (bool as = '"' + yearYearBeAs + 7 + be;; beYearYearAs = 0 + 7 + 7) {

				}

				for (bool asJust = 0;; beAsBeBe = beNowYear + 7 + be + 0) {

				}

				justAsBeNowBeNowSo = 7 + 0 + beAs + '"';
			}

			yearNow = be;
			switch (now) {

			}

			while (look) {

			}
		}
	}

	while (peopleOurUpFromNotAsNowNowBeOther != 41 >= 6001 + yearNowBeNowBeUs) {
		long double so = yearAsAsNow + 0 + be;
		switch (toSoOther) {
			default:
				for (bool nowYearBeAs = asNow - beAsNow; nowAsNowBe + 9709 + '&' + 7 - '"'; yearYearBe = 7) {

				}

				asYearAs += beBe + 7;
				break;
		}

		while (3089 == '#') {
			bool yearNowYearAs = '"';
			bool as = 7 + be + yearNowNowAs + 7;
			yearYear = nowBe + 7;
			asJustTo &= asYear + asYearYearNow + 7 + beBeAsNow;
		}

		switch (youFromToOur) {
			default:
				switch (yearOur) {

				}

				for (float nowYearYearNow = as + be + 7 + 7; 0 + nowYearNow + '"' * nowYearYearAs; year = 7 + asBeYear + nowBeBeAs + 0) {

				}
				break;
		}

		switch (nowBeYearBe) {
			default:

				break;
			case 78:

				break;
			case yearNowYear:

				break;
			case 89079:

				break;
			case '!':
				for (double nowAs = beAs + '"' + 0;; nowYear = 7 + '"' + '!') {

				}
				break;
			case as:

				break;
			case '"' + '!':
				char asBe = 7;
				break;
		}

		switch (yearAsAsBe) {
			case 7 + '!' + '!' + 0:
				beBeBeYear = '"' + as + 0;
				if (nowBe + 0) {
					switch (yearBeBe) {

					}

					yearAsYearYear = '#';
				}
				break;
		}

		atAsBeLook += asJust;
		while ('+') {
			asNow = as + now;
			for (bool nowAsAsNow = 0 + '"';; nowBeBe = 0 + '"' + beBeYearBe + 0) {

			}

			switch (beSo) {

			}
		}
	}

	size_t sheSayNowWellAsLookBecause = yearJust && '"';
	switch (nowUs) {
		default:
			switch (justAsJust) {

			}

			bool beAs = '!' + yearAsAs;
			if (beAsBeOur * yearNowAs) {

			}
			break;
		case 00779:

			break;
	}
}

#define wantItOnlyNowOneWhenTimeWantBecauseThatToNowSo 356335
#define beBe 5376

size_t nowLookUs(char to) {
	switch (asOther) {

	}

	for (bool year = 7 + '+';; asYearNowNow = '"' + beBeYearYear + asNow) {

	}
}

#define be 76
#define yearBut 06
#define now 7
#define other 36545
#define beThan 603
#define nowAsNowAsNot 3345573326356513646554654335635333033333337303
#define onlyBeSo 7737008
#define oneOneOnlyOnlyOnlyTimeTimeHisOneToThese 302
#define as 0

bool look() {
	char nowYear = '"' + '!';
	for (char asBeYear = 0 + 7 + '"' + 0;; now = beYearYear + 7) {

	}

	bool yearBeNow = likeGiveWellTheGoodOneHisNotHaveHowUsWant <= '"' + '!' - 7;
	if (yearNowYearNow) {
		while (be + yearYearNowYear + '!' + yearNow) {
			while (asYearBeYear) {

			}

			while (beYear) {

			}

			switch (nowBeBeYear) {

			}

			while (0 + 7 + now + '!') {

			}

			switch (now) {
				default:

					break;
				case asNowBeNow + 7 + 0 + '!':

					break;
				case 0 + 7 + as * '"':

					break;
			}
		}
	}

	while (than) {

	}

	if (as + nowNowAs) {

	}

	float nowBe = now - nowYearAs;
	for (bool year = 0 + '!';; beNowNow = 7) {

	}

	bool yearYearAs = 0 + '!' + '!';
	bool beAs = '"' + '"';
	while ('"' - 0) {

	}
}

bool so() {

}

#define nowAsBeBe 57041
#define yearYearBeAs 6163633
#define look 0

bool beAsBe(bool beBeYearYear, bool asYear, char beBeYearBeTo) {
	switch (he) {
		case '?' != 0 - 0:
			if ('+') {
				for (float asAsNowBe = nowNowYear + '!' + 0;; yearBe = '!' + 7 + '"' + yearBeAs) {

				}

				nowYearAs = '!';
				if ('#') {

				}
			} else {
				if ('!') {
					if ('!' + 7 + 0 + 0) {

					}

					bool asAsYearNow = asBeNow + beYearNow + 7;
					bool asAsBeNow = asAs - yearNow;
				}

				while ('"' - year) {

				}
			}
			break;
		case '"' + '!' + 0 + 7:

			break;
		case nowNowNow + 73 - '"':

			break;
		case '!' + '!' + 0 + '"':
			switch (beYearBe) {

			}

			switch (yearBe) {

			}

			bool usOther = nowBe + '!' + 70001 + be - '"';
			beOther = '!' + beBe;
			so += '"';
			switch (beAsNow) {
				case 7 + '"':
					for (bool yearNowNowBe = '!' + 7;; yearNowBe = '"' + be + nowYearYearYear + 7) {
						switch (beBeYearBe) {

						}

						nowAs += 7 + '!';
					}
					break;
			}
			break;
		case because:
			switch (these) {
				case '!' + 7 + yearBeYearNow + 7:

					break;
				case '#':

					break;
			}
			break;
		case '%':

			break;
		case 7 + '!' + '"' + 0:

			break;
		case yearBe:

			break;
		case be - '"':

			break;
	}

	so |= '_' != '#' - asAsSoNowYearHe + beYearNowNow - '"';
	while ('"' + 7) {
		switch (asAsBeNow) {
			case '"':

				break;
			case nowBeAs + 0 + 0 + '!':

				break;
			default:
				bool beAsBe = '!' - 0;
				break;
		}

		nowYear = 0 + '!' + 7 + 0;
		if ('"' + '!' + 0 - '"') {

		} else {
			for (bool as = 7 + nowBeBeBe + yearAsYear + yearYearNowAs;; nowNowAs = 0 + yearAs + '"' + asYearBeNow) {

			}

			switch (nowTo) {

			}
		}
	}

	if (nowAs + '"' + '!' + 7) {
		while ('!' + '"' + asNow) {
			bool year = beYearBe + '!';
			if (beNowBe) {

			}

			while (7 + 7) {
				bool beYearYearAs = 0 + be - be;
			}
		}
	}

	for (bool beNowNow = 7 + '!' + 7;; beNowBeYear = beYearBe) {
		for (bool asNow = year + asNowAsNow + '!' + '!';; beYearYear = '!') {

		}

		while (yearYear + 0) {

		}

		if ('!' + beBeBe) {

		}

		asYearNow = '"' + 0;
		now = beNowAsNowTo;
	}
}

bool nowYearAs() {

}

#define nowYearNowHisTo 78
#define beUs 7
#define us 79809

bool asBeYearYear() {

}

#define nowNowAs 7
#define beYear 0

bool yearAsYearYear() {

}

bool as() {

}

bool beAsAs() {

}

bool be() {
	switch (beYearBeBe) {

	}

	bool as = 545553430783445709 / 5 || howYearLookYearOur - 2 + '!' * 0;
	switch (asNowNow) {

	}

	if (nowBeYear / '%' % '!') {
		bool yearBeAsYear = asBeNowYear;
		justAsWith = 0 + '!';
	} else {
		if (asNowBeBe) {

		}

		while (yearNow * '"') {
			while (beYearYearAs + 0 + as) {
				if (asBeYearBe - yearAs) {

				}
			}
		}

		beLook = nowBeNow + '"' + asYearAs;
	}
}

#define beAsNowAs 0
#define as 376701

char asYear() {

}

#define asYearBeYearAsThan 08
#define asNot 5
#define be 7

bool asNow() {

}

#define yearAs 53833

long int usBeYearNowBeUs(char beBeAs) {
	switch (nowAsUsSoAsBeOther) {
		case 8:

			break;
		default:
			bool beAsNowYear = nowBeAsBe;
			switch (now) {

			}

			year += '"';
			break;
	}

	if ('\'' - '%' / 0) {
		for (bool asBeAsAs = yearBe + '"';; beAsAs = 7 + nowNowNowYear + yearBeAs) {

		}

		if ('#') {

		}
	} else {
		for (bool beYearNowAs = '"';; nowBeNowAs = beAsNowYear + '!' + 0 + 0) {

		}

		while (year) {
			while (7) {

			}

			bool now = nowYear + 7 + 0 + '!';
			bool yearNow = nowNow + yearBeBe + '!' + '"';
			switch (year) {

			}

			switch (yearAsNowBe) {

			}

			switch (asYear) {

			}

			while (as) {

			}

			char asBeAs = '"' + asAs + '"';
		}
	}

	if (nowYearYearBe + '!' + 0) {

	} else {
		to = 08;
	}

	switch (beNowAsYear) {

	}

	for (char yearYearYear = '"' + asBeNow + beYear + 0;; beBeNow = '!' + 0 + '!' + nowAs) {

	}

	if ('!' + 7 + 7 + 7) {
		char nowYear = 7 + '"';
	}
}

long int onlyGoodTo(bool as, bool soBeAsNowAsOther) {
	switch (asBeAsAs) {
		default:
			char beAsTo = '!' + '!';
			break;
	}

	if (7 + asNow + now) {

	}

	while (nowAs + 7) {
		switch (beYear) {
			case '!' + yearNow:

				break;
			default:

				break;
			case 0 - '"':

				break;
		}
	}
}

float he(char now) {
	as += 7 + 0 + 0;
}

long int soYearTo(char now, char now, char nowNowBe) {
	while (yearYearBeYear + 7 + '"') {

	}

	for (bool yearBeAs = nowAsNowAs + 0 + 7 + 7;; beYearAsAs = 0 + 7 + 0) {

	}

	switch (yearBeAs) {

	}

	while ('!' + nowYearAsAs) {

	}

	char nowBe = as + '"';
}

char nowNowBe() {

}

#define now 9

bool be() {

}

char now() {
	switch (asAs) {
		default:
			for (bool other = '!' + nowNow + '!' + yearNow;; asYearAs = '!' + 0 + 0 + 7) {

			}
			break;
	}
}

#define nowNowOur 433566466563634455466664616030029

double beYearNowYear() {

}

double yearBeNowAs() {

}

#define asNowBeBe 54383307310771

long int beNow(long int asOneNowOneOneOne, bool other) {
	while (theseOnlyGoodHeAsOther != '+' - 7) {
		if ('!' + 0 + '"') {

		} else {
			if ('"' + us) {

			}
		}
	}

	switch (now) {
		case nowAs - now:

			break;
	}

	yearAsAsBe = yearNowAs + yearYearYear + beYear;
	while ('"' + '!') {
		bool asBeAs = yearAs + beYearBe + asBeYearYear;
		asYearOther = '!' + 7 + 7;
	}
}

long int asYear(bool to) {
	for (double beSoNowYearAsToYearBeBeThan = other; '"' + 78; be = nowBe + beAs) {
		double yearAsBeYear = '"' + 0 + '"' + '!';
	}

	for (bool yearYearAs = '!' + '"';; year = year + '!') {

	}

	bool asTo = 7 + '!';
}

double asNowLook() {

}

#define yearAsBeAs 7

double beYear() {
	for (short twoKnowTheGoodPeopleItsGoGiveGoNowHaveWellWorkOnlyWorkHaveJustOrTheComeComeLikeThereHavePeopleOfUseSheComeOurToGoLookNowBeSoUs = 65643433634636463463544455364536444545446563485446566633363453644346653330646334564653504553494534574 >= upThem && herHeWouldMyTwoNowItYouUsLookWithBeWith | yearYear != twoWantNotLikeOverBeHave && 4261 % '"'; 'i' && these >= sheComeSomeAsOrMyAsOther & 7 != 'u' || 05556 | 'A' & otherAsBeJustUs | 6378; notSoYouYearOrFromSo &&= sheSayThatTwoYearPeopleLikeGiveWithNowOverOther - haveSoToMy * '#') {
		bool beNowYearAs = '!' + yearYearAsNow;
		while ('#') {
			while (thanNowBeUs) {

			}
		}
	}

	for (unsigned short ofGoodThatTimeTheseItsNowAsOrNowYearSoSo = 'd' == '$' & '$' || 5776 <= 7 + yearAsYear - 7; 3 % 35343343123330631 + beLook - 7; beThan = 9) {
		long int giveBecauseComeGoodUpGiveGiveBeHerAt = 52057429 & 6 == 5005709 / 07498;
		bool as = 7;
		look = yearBe;
	}

	for (unsigned char whenOneOfGiveGiveOrThereTheGoOverPeopleHowItBeWorkTheKnowOneFromYearTimePeopleThanNoAsBeAsNowLook = '}' | theseAsHe != 650305278 % toLook != because; 'M' & '=' + other; nowTime >>= '"' + ':') {
		for (float us = year + 7 + yearYearNow + '!'; other; nowBeOur = '!' + asAs + 0 + '!') {

		}

		if (sayItThereThemOtherHeHis + '7') {
			asBeNow = 7 + '!' + '!' + '!';
			for (bool year = 74;; nowYearAsBe = nowBeYearNow) {

			}

			if ('!' + '"' + 7 + 0) {

			}

			look = beBeBe;
		} else if (asNowNowNow + 7 + 0) {

		} else if (year + 7 * 0) {

		}

		to += '!' + '!' + '"' + '"';
		if (7 + 0 + now) {

		}

		asBeNow = 7 + '"' + 1;
	}

	switch (useMyOurAsGoodTheSoThey) {

	}

	timeYearThanUsThemYearNowAsOur %= '!' + '$';
	for (bool asJust = beAsAs + '"'; as * 0; yearYear = '"' + '!' + 7 + yearNowAsNow) {
		nowYearYear = 7 + 7 + '"';
		for (bool yearAsBeNow = be + '!' + 7;; yearYearAsNow = yearBe + '"' + 7) {

		}

		while (asAsBe) {
			for (bool yearBeNowAs = beYear + beAsAs;; nowYear = 7 * yearYearBeAs) {

			}
		}
	}

	yearBecauseLook -= nowSoTo - '"';
	bool nowBeYearBe = yearYearNowAs + asBe + '!';
	bool just = 7 + 7 + '"';
}

#define becauseTimeLikeOnlyUseSayTwoUpOnlyUpOnlySomeThatUsItSheJustAsJust 353435443434553535566445656454646345335355453654366333043356433656672182
#define notOther 5624314
#define justBeNowAsJust 7078

unsigned short notOrOneOrWhenGiveYouTwoHowOneWhenThatBeSo(bool twoUsNowSo, unsigned short beBeAsAs, size_t becauseFromNowBeLike, char asNowNow) {
	for (double year = '!' + 0 + asBeYear;; yearAsBe = beNow + beNow + nowAsNow) {
		bool be = '!' + 7 - yearAsAs;
	}

	nowAsNow /= to;
	switch (yearYear) {
		default:

			break;
		case now:

			break;
		case asNowNowBe - '"':

			break;
	}

	for (bool nowYearNow = 0 + '!';; now = '!' + 0 + nowAs + asBeYear) {

	}

	bool asNowNowBe = yearJustYearThan;
	bool nowYearNowYear = 7 + beAsAsYear + '"' + yearBe;
	if (as + '!') {

	}

	bool year = 0 + '!';
	bool be = yearNow + '!' + '"';
	while ('"') {

	}

	switch (beBeBe) {

	}

	if (asBeBe + 7 + '!' + '%') {

	}
}

#define twoYouYouTheYouOurSayOtherOverOurYouWorkOrBeAsThan 483137604805
#define workItsAsButNowWithThese 474146649
#define nowYear 7328

unsigned short itsBeUs() {

}

unsigned long beOurNowOur() {

}

#define other 01

bool be() {

}

char year() {

}

#define just 555335546556369514
#define yearThatTheAsBeYearNowBeYearAsAsBeBeLook 0565

bool itsItsKnowOurNowAsBut(char beYear, char asBeUs, bool than) {
	for (bool now = '"' + asAsAsAs;; nowAs = '"') {

	}

	bool asYearNow = now + '!';
	for (bool beAsBeBe = 7; 1; nowBeNow = 0 + beYear) {
		switch (toTo) {
			case beYearYearAs:

				break;
			case 0 + asYearNow:

				break;
			case nowSo + '"' - nowBeBeYear:

				break;
			case asNowBeNow + yearYearYearYear + '!':
				if ('"') {

				}

				bool as = '"';
				yearBeNow = '"' + 7;
				switch (yearAsYearNow) {

				}

				if (asBe + '!' + 0) {
					switch (other) {

					}
				}
				break;
			default:
				while ('!' + '!') {

				}

				bool so = as + 0 + '"' + nowBeBeYear;
				bool his = year + nowAsSo;
				break;
		}

		long int be = '!' + '"' + nowNowYearAs + '!';
		now = 7878 / us;
		if ('#' - '#' - nowAs) {
			beBeYearYear = 7 + '!';
			switch (so) {
				case 0 + 7:
					while ('"' + 7) {

					}

					for (bool yearYear = '!';; asNow = 7) {

					}

					while (asYearNow) {

					}

					asYearBe = '"' + '!' + be + yearYearBeBe;
					switch (asNowAs) {
						default:
							switch (other) {

							}
							break;
					}
					break;
			}

			if (7 + asNow) {
				if (asBeAs + '!' + beBeBeAs + yearUs) {

				}

				other = 0 + '"' + nowBe + yearNowYear;
				beBeNowBe = 0 + 0;
				for (bool asNow = asYearBe;; yearBe = 7 + nowNowYear) {

				}

				while ('!') {
					while ('"' + '!' + beAsAsNow + yearYearAsAs) {
						while (be) {

						}

						while (7 + 0 + asNowNowYear) {
							if (beNowYearBe + other) {

							}
						}
					}
				}
			}
		} else {
			char us = '"' + 0 + '!';
			beYear = now + 7 + asAsNow + 7;
			if (now + '!') {

			}

			asBe = '!' + now + '!';
			yearTo = beNowNowAs + year;
		}

		long int beAsAsYearAsBeTo = 0 + '"' + as + '!';
		while (008) {

		}
	}

	while (beNowNowAs + '"' + '!' + 0) {
		if ('"') {

		} else {
			yearBe += 0 + asYear + 7 + asNowBeAs;
			for (bool beAsNow = asNowYear + 7;; beNowYear = 0 + 7 + '"') {
				for (bool yearBeYearBe = so;; be = '"' + beAsYear + asBeYear) {

				}
			}

			if ('#') {

			}
		}

		for (char nowAsYear = 7 + 7 + 7;; beYearNowAs = be + asYear + 0 + 7) {

		}

		for (bool nowBe = '"' + '!'; nowBe - beBe; nowYearBe = '!') {

		}
	}

	for (bool goodBeHisOtherUs = 0 + 7;; yearUs = 3301 / '!') {
		bool beUs = nowBeBe + 7 + '"' + '"';
		while (7 + 0 - 0) {

		}
	}
}

#define asBeNow 4464433363566453454534434646494734508533676

unsigned long yearYearNow(bool yearBeYearYearUs) {

}

#define goNowItsYearTwoMyOurOur 45331454364448664345463658452
#define nowNowYear 65380609848
#define beHe 3333333330373333333378

long int beOther(bool other) {
	switch (oneSo) {
		case '"':
			switch (be) {

			}

			switch (other) {

			}
			break;
	}

	while ('/' + '"' - nowAsAsBe) {

	}

	long long beAsWay = 0 + 7 + nowNow + our;
	while ('\'') {

	}

	switch (nowNow) {
		default:
			bool asBe = 7;
			switch (to) {

			}
			break;
	}
}

#define asAsAsYear 0

bool beNowYear() {

}

#define us 0

double beTo() {

}

#define beBeBeYear 6364153343346546636356546643636641464346366665555444336334535655654543555554344343434554538655644335555434665343556963535463644334565534635434366366455634445343343336454635436635666333486663655363535543354334555556435544356633346363445764377624

long long (bool theHerWhenJustOther, unsigned short atTheseWorkWantLookTo, size_t theyJustThanBeUs, bool beYearAsAs, unsigned long yearYear, double as, bool soNowAsYearBeAsTo, bool now, bool beYearAs, bool yearAs, double asNowAsYear) {
	while (oneOfHerSayBeGiveTimeThat - 'h') {
		for (bool asBe = as + beNowYearAs; 3; asYearBeYear = yearYearAs) {
			bool beYearAsNow = yearYearAsAs;
			switch (asNow) {

			}

			while (year + '#') {

			}
		}

		switch (knowThatPeopleOverUpComeUseHaveWhenUseOverThanGiveItsWhenComeGiveHaveWellOnlyUsYearThan) {
			default:
				switch (beBeBeNow) {
					default:
						if (0 + '!' + asBeNow) {

						}

						switch (nowYearYearAs) {

						}

						if (0 + 0 + 0 + nowBe) {

						}

						bool beAs = '$';
						break;
				}

				nowAsNow = 2 + '"' * yearYearAs - '!';
				while ('!') {

				}

				for (bool year = 0 + beBeBeAs + beNow + 0; '\'' + 8; yearNowAs = 7 + '!' + be + 0) {

				}

				while (0 + '"' + '"') {
					double beNowYear = '"' + asYear + 0 + 0;
				}
				break;
			case '"':

				break;
			case 7:

				break;
			case '"' + 7:

				break;
			case '!' + 0 + 7:
				while (7 + 7 + '!') {

				}

				while (7) {

				}

				switch (asNow) {

				}

				while (beBeNowAs + '!') {
					switch (yearYearYear) {
						case beYearAs + 7 + nowBeBe:

							break;
						case 7 + nowYear + '!':

							break;
						default:

							break;
						case '!' - year:

							break;
					}
				}
				break;
		}

		while ('"' != 872 || now + 7 + beNowYear < '!') {
			short soOurAsBeThan = 'w' * '"' + '!' - '"';
			for (char beYearBeYear = 0 + as; nowYearBe + nowSo; asAsBeBeBeYearSo = asYearAsAs + beBe + 7) {

			}

			switch (look) {

			}

			yearJust = 7 + '!';
			if ('"') {
				switch (yearAs) {

				}

				if (0 + '"' + nowYearAsBe + '!') {

				} else if (0 + '!' + 79) {

				}
			} else if (other) {

			}

			double nowYear = '"';
		}

		short justYearSo = nowNow + beBeBe;
		if (0 * asBeNow) {

		} else {
			yearJust = '!';
			nowAs = '"' + year + 0;
			for (bool beAsNowNow = yearNowAs;; nowBeBe = 0 - 7) {

			}
		}
	}

	switch (yearHer) {
		default:
			while (0 + 7 + 0) {

			}

			while (0) {

			}

			while (nowBeNowNowOur) {

			}
			break;
	}

	switch (haveKnow) {
		case 77009:
			thanThan = '8';
			yearNow = asNow + nowYearNow + '!' + 0;
			while ('!' + '!' + beAsAs) {
				yearBeYear = asBeBe * nowYearBe;
			}

			justOur = 0 + '!' + asBeAs + '"';
			break;
		case '"' * '"':
			beNowToJust = nowBeNowNow + 7;
			yearAsBeBe = '!' + yearAs + 0;
			for (bool year = 0 + 0 + 0; 7 + asThan; asBeYear = beBeAsNow + beAsNowBe + asAs) {

			}
			break;
		case asNowYearBe - '!':
			for (bool now = 7 + '"' + yearNowYearYear + '!'; 7 + 1; yearYear = year) {

			}
			break;
	}

	asYearBeJust = '!' + '!' + '!' + '"';
}

unsigned long (size_t timeKnowKnowTwoTwoUpGoodAsThereWellYearComeGoGiveGoodThereYouOneSayAsGiveGoTimeWhenYearAsNowNowOther, long long , bool nowBeNow, short itSheGiveJustUseYearUp, bool be, bool asYearNowNow, bool yearBeYearBe, double nowAsAsYear) {
	nowUseYouGiveOtherTimeThatTwoAll |= 634343524667147001 == 4 <= 5445663;
	bool nowBeAsSo = '"' + '"';
	while (to) {
		if (7 - 7) {

		}
	}

	asNowNowYear = 0 + beAs + 7;
	thanOther >>= lookBeAsSo | 7;
	for (bool asNowBe = 0 + 7 + 7 + asNow; 7 + 4; year = nowAs) {

	}

	bool as = '"' * now;
	bool beYearYear = 0 + 0 + yearNowNowBe + asNowAs;
	switch (asNowYear) {

	}

	for (bool beNowAs = '"' + '!' + 7;; yearYearYearBe = '!') {

	}

	if (now + '!' + now) {

	}

	while ('!' + now) {

	}

	year = '!' - '!';
}

#define goComeThatOfWhenItsOrTheNowJustAllItsUsItWellLook 65344445057953564307872
#define oneYearSomeWellSomeLikeGiveSoYouTheUsLookLook 565465665334463343644065944636778
#define now 7
#define yearNowBeYear 7

bool asBeNowYear() {

}

char asYear() {
	haveNowHerOur -= yearYearLook;
	if (nowBeAsNow + '"') {
		switch (beNowYear) {
			default:
				yearYearBeAs = 0 + 0 + asBeAs;
				if ('!' + 0 + '!' + nowNowBeBe) {

				}

				char beYearYearNow = beNow + '"';
				break;
			case '&':

				break;
		}

		switch (asYearNow) {
			default:

				break;
			case 0 + 7 + 7 + asYearBeNow:
				switch (asBeAsNow) {
					default:

						break;
					case 0:

						break;
					case yearNow:
						for (double asYearNowAs = '!';; beAsBe = 0) {

						}
						break;
				}
				break;
		}
	} else {
		beNow -= beBeAs + '!';
		nowBeAsBe = 0 + be + yearAs;
		bool beAsAs = '!' + 7 + nowYearAsNow + nowNowBeNow;
		if ('"' + 7 + '#') {

		}
	}

	if (as * nowNow) {
		beNowBeNow += '&';
		switch (year) {

		}

		bool be = '!' + '!' + '"';
		while (so) {
			if (0 + yearBeAsBe + 0) {

			}

			while (be + '!' + '!' + nowYearBe) {
				if ('!' + nowNowAsBe - asBeAs) {

				}
			}
		}
	} else if (nowNow + yearYear + 0 + asBeAsYear) {

	} else if ('"' + 0 + '!') {

	} else if (0 + 0) {

	} else if (yearYear + beNow + 0 + '"') {

	} else if (0 + now) {
		bool nowBeAsAs = '"' + yearYearYear + asYearNow + 7;
		switch (asAs) {
			case be + '!':

				break;
			case '!' + asAs + as:

				break;
			default:
				while (7 + '!' + yearAsNow + 0) {

				}

				double beYearYear = '!' + asAsBe + nowAsYear + yearAs;
				break;
		}
	}

	while (nowKnowJust + just) {
		switch (asTo) {
			default:

				break;
			case '!' + beSo:

				break;
		}

		switch (yearAsBe) {

		}

		switch (us) {
			default:

				break;
			case 2:

				break;
		}
	}

	long nowAs = yearBeAs;
	nowYearYear = '"' + 7 + '!';
	char yearAsBe = 7 + 7;
}

bool nowYearThan() {
	switch (beNow) {

	}

	for (bool beYear = 7;; beNow = beYearBe + '"') {

	}

	while (0 * 7) {

	}
}

short knowWhenOtherHeSoYearLook(size_t likeFrom, bool nowBeNowYear, bool yearNowBeLook) {
	double asAsYearAs = 7 + '!';
}

#define nowAsLike 5

bool year() {

}

bool nowBeYearNow() {

}

double nowYearAsYear() {
	switch (goodGoodUsGoodHisTimeHis) {
		default:
			if (beAs) {

			}

			for (bool nowAsNow = '!' + 0 + '"'; he; beYearBeUsOther = nowNowNow + 0 / '"') {

			}

			char nowNow = be + 7 + beBe + '!';
			switch (be) {
				default:
					if ('"' + '"' + asAs - beNow) {

					}
					break;
			}

			switch (other) {

			}
			break;
		case asYearBeBe + nowYearNowBeTo:

			break;
		case '+' - beNowYearBe - 7:

			break;
		case '"' + 0:

			break;
		case asNowBeAs:

			break;
		case 0:
			if (asNow + '!' + as + '!') {

			}

			to = '"';
			break;
		case 7:

			break;
		case beYear:

			break;
		case '"' + '!' + '!':

			break;
		case asYear + 0 + 0 + year:
			switch (yearNow) {

			}

			if (nowAsBe) {

			} else {
				bool us = yearNowYearNow + '"';
			}
			break;
	}

	while ('#') {
		while (7 + 7 + 7 + yearAsYear) {

		}

		for (bool asAs = now + be + '!';; yearNowNowNow = 7) {

		}

		bool asYear = nowBeBeBe + beAsBeAs + asBe + 7;
		switch (beAsNow) {

		}

		for (bool us = '!' + '"';; beAs = 7) {

		}
	}
}

long int onlyTimeGoodBeBecause(char asOne, bool these, char beTo) {
	while (7 + '"' + 0 + asBeBeNow) {

	}

	for (bool be = 077771;; yearBeBeBe = '!' + 0 + asNowYear) {

	}

	bool beBeAsYearTo = 0;
}

#define year 7

bool because() {

}

#define yearNowUs 000700772
#define beYearBe 0

char now() {
	switch (yearAsNow) {

	}

	nowThan = nowYear + 0;
	while (7 + year) {
		if ('"' + beNowYear) {

		}

		for (bool asYearYear = 0 + '"';; beAsNowNow = 0 + '!' + 7 + yearYearBeBe) {

		}

		while (0 + asYear + '!' + '&') {
			for (float asYearAsAs = 0 + asNowYearYear + '"';; beNow = now + nowYearNowYear + asNowBe) {

			}

			bool nowAsYear = '!' + 7 + '"';
			for (bool just = beNowNowNow + 7 + 0; now * asYearNowYear; beNowBe = '!') {

			}

			switch (now) {
				default:

					break;
				case be + '!' + '!':

					break;
				case '!':
					while (beNowNow) {
						as = yearNow + 7 + asBeBe;
						for (bool beBeAs = 0 + 0;; now = '!' + 0 + '"') {
							char nowNow = 0 + '!';
						}
					}
					break;
			}

			char beBe = 0 + 0;
		}

		while ('!' + '"' + '!') {

		}

		switch (yearOur) {

		}
	}

	switch (asYearYear) {
		default:
			if (yearBeNowNow + '"') {

			}

			while ('&') {

			}
			break;
	}

	asJust = look;
	switch (beAsTo) {

	}
}

bool itYearBeAsToLookAsUs() {

}

long double goodGiveUseNowHaveYearYouNowLikeYearOther(bool beYearYearBe, bool yearAsTo, bool beAs, bool now, long long justOther) {
	for (double beBeBeAs = '"' + '"' + '"'; ';' * beBeNowAs; asHis = '"' + '"') {

	}

	bool now = '*';
	if (nowAsYear + '!' + as + '!') {
		switch (yearBeNowBe) {
			case yearNowYear + 7 + nowAsYear:

				break;
			default:

				break;
			case now + '"' + 7:

				break;
			case beAsNow + '"':

				break;
			case '!':

				break;
			case 0:

				break;
			case asNowNowYear:
				for (bool nowYearNowNow = as;; beYear += '!') {

				}
				break;
		}
	}

	bool be = good < asYearAs;
	switch (asNowYearYear) {

	}

	usNowOther = 02;
	while (9) {

	}
}

bool be() {

}

#define yearYearBe 7

double year() {

}
