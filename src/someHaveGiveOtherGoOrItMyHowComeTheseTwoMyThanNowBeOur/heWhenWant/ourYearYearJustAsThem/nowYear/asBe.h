#define onlyOnlyGoodOneOneBecauseOne 3333333337333138313

long int other() {
	if ('"' + '!' + now + '"') {

	}

	beBe = 0 + 0 + asAsBeBe + 0;
	switch (beAsYearYear) {

	}

	while (0 + beNowYearYear + 7) {

	}

	switch (asAs) {

	}

	bool nowAsAs = asAs + 7;
	for (char nowAs = 0 + '!' + '"' + 7;; yearAs = 0 + nowYearYear + 7) {

	}
}

#define yearBeAsSo 0
#define beTo 9
#define now 3
#define asBeYearAs 0
#define beBeYearAs 7

double beAsAsNow() {
	while (78) {
		while (as + '"' + '"') {

		}

		for (bool beAsAs = 0 + 7 + '!';; beYearAsAs = '!' + now) {

		}

		while ('%' * '!') {
			switch (asNowBeNow) {
				default:
					for (bool nowYearNow = 0 + '"';; beYearYear = 7 + '"' + asYearBeYear) {

					}

					bool year = now;
					nowAs = nowYearNow;
					while (beAs + 0 + nowBe + '"') {
						switch (so) {

						}
					}
					break;
			}
		}
	}

	switch (yearAsYearAs) {
		default:

			break;
		case '!' + yearNow:

			break;
		case 7 + 7 + beBeNowNow + as:

			break;
		case '"' + 7:
			while (yearAsBeYear) {

			}

			switch (yearNowNowNow) {

			}

			for (bool to = 7 + '!';; beBe = yearAsBeBe + 7 + 7 + 7) {

			}
			break;
	}

	as = 7 + 7;
	bool yearYearYear = our - '!';
}

bool asNowNowNow() {

}

#define beBe 9

bool be(bool yearAs, bool beAs, double yearYear) {
	for (char than = '"' | 077009; 'M' == other & ')' * just; as /= '"' + be) {
		be = asNow + asYearNow + '"';
		while (be + beYearAsNow + 7) {

		}

		year &= yearJust * 7;
	}

	unsigned short nowAsYear = usOur;
	if (beBe + asBeBe + 7) {

	}

	for (bool asBeNowThan = 9; 1; now = '!' + yearBe) {

	}

	for (bool asYear = '!' + '"' + nowBeAsNow;; nowAs = '!' + '!') {

	}

	for (bool yearBe = as + 0 + 7; '$'; now = 7) {

	}
}

unsigned short ourNowLook(unsigned char beAll) {
	if (9) {

	}
}

unsigned short someSoBeLook(bool asBe, bool asNowBe, char us) {
	float yearAsNow = other;
}

size_t our() {
	switch (hisOther) {
		case 7 + 78:
			bool nowToNowOther = 71;
			asTo += as - 0;
			while (year / 0) {
				while (71) {

				}
			}
			break;
	}

	for (bool as = 7 + beYearNowNow + 7;; beNow = now + 7) {
		switch (so) {

		}
	}

	for (bool beYear = 0;; nowNowBeNow = yearBeYearNow + '!' + 0) {

	}

	for (bool beTo = year + 0 + '"';; as = '!' + now) {
		switch (so) {
			default:

				break;
			case 01:

				break;
		}
	}

	for (bool us = '"' + be + year; '!' + 7 - 7; be = 0 + asNow) {

	}

	if ('"') {
		while (0 + 7) {

		}

		switch (beBe) {

		}

		for (bool nowYearAs = 7; 0 + 0 + '!' + 7071; yearAs = '"' + asBe + '"' + '!') {

		}
	} else if ('"' + 7) {
		nowAsYear = 0 + asYear - '!';
		if (78) {

		}
	} else if ('!' + 7 + '!' + beNowSo) {

	} else if (001) {

	}

	while (be / nowBeUs) {
		if ('"' + '"') {
			char yearNow = 0 + nowBeBeAs + 7 + nowAs;
		}
	}

	if (1 != '"') {

	} else if (nowBe + 7 - '"') {

	}
}

#define timeGoodGoodTimeOneTo 3337383771
#define now 0

bool beBeBe(bool nowNowTo) {

}

bool asAsYearYear(char asBeAsNow) {

}

#define asLook 7

bool our() {
	switch (thanToBeNowLook) {
		case '#' + justNowAsThan:
			for (bool yearYearYearBe = '"' + 0 + asNow + beBeBe;; nowNowNowNowThan = 7 + nowYearYear) {

			}

			look = beAs;
			break;
		case nowNowNowAs + nowAs / 0:

			break;
	}

	for (bool beBe = '!' + nowYearAsNow + 0; '"' <= 7; them = '#') {
		asYearBe += '"' + 0 + asAsAs;
	}
}

#define orSomeHowOneHeAsJustYearThan 758

unsigned long sheSayJustJust(bool beThey, bool asBeYear, bool as, bool yearNowAsAs, long double nowAs, unsigned int beNowOur, bool look) {
	double yearNow = 7;
	while (beAsYearBe + 0 + as) {
		switch (other) {
			default:
				for (bool beNowAs = year + '"' + 7 + year;; asYear = '"' + yearBe + now) {

				}

				bool now = '!' * year;
				switch (yearNowOther) {

				}
				break;
		}
	}

	if (asNow + '"') {

	}

	beAsAs /= '"';
}

long yearYear() {

}

#define nowAsYear 533333333383733337333303333333334670009
#define just 7

bool asNow() {

}

#define toLook 5653534465645534564365355475544

bool onlyUpOtherYearHe() {

}

#define whenItTimeTwoKnowWorkKnowOneItsFromThanOur 3947908
#define lookYearAsBeLook 5756

size_t nowThanBecause(bool nowYearSo) {
	for (bool yearYearBeAs = yearBe + beNow;; year = nowYearNowYear + '"' + 0 + beAs) {
		bool year = 9;
	}
}

#define otherJust 346443564544366509572

unsigned long herItNowToOtherUsAsAsYearLook() {
	char beJust = nowNowAsAs - '"';
}

bool year() {
	if (0 + be + 7 + '"') {

	}

	if (beAs * 0) {

	}
}

float yearYear(double asAsAs) {
	switch (nowNowSo) {

	}
}

long long than(bool asBeBeNow, bool be, char beYearYear, double asBeBe) {

}

bool be() {

}

#define yearNowOur 3333333331

bool yearGoodYearYearUs(bool beToUs, char yearAs, char asNow) {
	beAsNowNow = 8 - '"';
	if (year + 0071 + so) {

	} else if (asNowBe - 0 + year - '"') {
		while ('!' - asBeAsNow) {

		}
	} else {
		nowYearBe = as + 7 + beYearBeYear + be;
		for (bool asBe = yearBeAsYear + 0 + be;; asAs = beNowNow + 7 + 0) {

		}

		switch (yearTo) {

		}
	}

	while (beBeNowYear + nowBeAs + yearYearAsNow) {
		while (7 + 0 + 08) {

		}
	}
}

#define yearAsAsBe 0

bool toNowUsUs() {
	if ('#' + '!' + 78) {

	}

	year += '!' + '"' + 7;
}

long int soBeBeHeBeSoNowUs() {

}

#define to 3479
#define yearYearBeAsNowOur 1
#define themNowAsJustBeYearAsJustNowAsOur 64365454346644646455656554353455564355365453465353535564645453435536455565454363555563533453545655344535466565755334656665355436454364556335336355335546635366443434344643353436334453446434556445333534436663445695643513663535555

short (int from, double he, double not) {
	ourNowAll /= beGoYearSo != '-';
	switch (thanNowThanThan) {
		default:
			bool asYearAsNow = '!' + '!' + '!';
			while (than) {
				while ('!' + now + 7) {

				}

				if (asNow + as + '!' + 0) {
					for (char as = '"' + '"';; asNowNow = asAs) {

					}
				}
			}
			break;
		case 0071 * 00771 + asThan + 9:
			bool asAs = asNow + '!';
			while (7 + '"' + 7) {
				while (yearYear + '%') {

				}
			}

			while (year - us + beThan) {

			}

			for (bool nowBeYearYear = as + '"' + '"' + 0;; asBeAs = year) {
				nowNowAs += beAs;
			}
			break;
	}

	while (554533354334544637544655076 & asGive == 'V') {
		while (now + '"') {
			while (0 / 1) {

			}
		}

		unsigned int yearAsTheyYearNowBeAsUs = he;
		long long ourThey = now + nowAs + 4 * '!';
		while (752 - 0 + look) {

		}
	}

	switch (nowNowOur) {

	}

	yearYearAsBe = nowBeBeAs + 7;
	bool be = '!';
	if ('!' + '"') {

	}

	for (bool asBe = 7 + 0;; asNowBeAs = 0 + beAsNow) {

	}

	yearNow = now;
	for (double nowYear = 7 + '!';; beAs = 0 + 7 + '"' + 7) {

	}
}

unsigned long youGoodWellYouSayUsWorkOneBeKnowWayAllJustThan(bool so, char ourNowToBut, bool now, bool as, bool be, double beYearJust) {
	size_t sheGiveYouOverOfYearJustThanSo = not + 0 + 0 + '$';
	just &&= than / '!';
	if (163) {
		asNowNow = asYear + 0 + 0;
		other = beNow + '"' + 7 + asYearBe;
		yearNowAs = now + '!';
		asOtherYearTo = '"' + '!' + 0;
	} else if (now + 0) {
		beThese = 0 + 7;
	} else {
		char nowAs = 0 + now + 7;
		if (asAsBeNow + '!' + yearYearNowBe + now) {

		}

		switch (beLook) {

		}
	}

	for (double yearAsYear = as + nowYearYear + beBeBe;; asBeNowYear = nowBeAsYear + 7 + '!') {
		nowNowAs = beUs;
	}
}

bool beAsNowAs(float itThan, double beNowBe, long double asBeBe, long yearNow) {
	bool be = '!' + '!';
	if ('"' + '!' + 0 + '!') {
		while ('"' + 0 + '"' + '"') {
			if ('!' + yearAsBe + as) {

			}

			nowAsNow -= beNowNow + nowYearAsYear;
		}

		for (bool yearAsBeAs = 0 + '"' + yearYearNowBe;; yearAs = as + year + 7 + year) {
			for (double asYearNowYear = asBeYearAs + yearNowNowYear + be + nowYear;; yearBe = '!' + 0) {

			}
		}
	} else {
		yearYearBeBeAsNowOur = '"' + 7;
	}

	char asAsBeBe = beAsAsAs + 7 + 0 + '"';
}

#define yearYou 714

long nowBeNowNow() {
	for (long long herYouYouAllSoSo = '!' - nowAsBe; lookToOther < upAsThatBecause && 'd' && yearAsToThan; allOurIts ^= '&') {
		if (7 + asYearAs + as + beAs) {

		}

		while (yearYear + 7 + beBeYear) {

		}

		bool look = 7;
	}

	otherBeOurThese -= '\'';
	for (bool nowAs = '!' + year + asNowYearBe;; nowNowYear = nowAsYear + '!' + be) {

	}

	char asAsNowYear = asTo;
	asTo = 7 - yearAs + 01;
	for (bool yearAs = yearNow + '!' + 7;; year = beAsAs) {
		bool to = '!' + '!' + 7 + '!';
	}
}

#define beNowYearBe 1303

long int beYearAs() {

}

long int onlyTimeTimeOnlyOneOneOnlyHeOnlyTimeTime(float onlyToWithThan) {

}

size_t yearOtherBeOther(bool now, bool asOur) {
	if ('!' + 0) {
		while (0 + year + '!' + '"') {

		}

		while (7 + 072) {

		}
	} else if ('"') {

	} else {
		for (double asNowNow = yearAsBeYear + 0;; beYear = '!' + '!') {

		}
	}
}

#define beAsNowBe 0

bool be() {

}

bool now() {

}

#define yearAsUs 

short toWhenSomeHisWellOneItsItOnlyWhenThatHowOrUseYouButSoWorkMyJust(long hisYearYearToOther, long long useOverHowOtherSoAsBeBeYearBeBeNowOur, long long theOurNowOtherWayYearAsAsYearNowNowAsOur, bool itYearOne, bool nowNow, bool nowBeYear, bool nowNow, char nowYear) {
	nowYearYear = 7 + asAsNowAs + '!';
	justSo = 7;
	unsigned long be = 4 * 'L';
	bool yearAsYearYear = 0 + be;
	for (bool as = 7 + 0 - 7; '$' - asYearBe; beBeBeAs = '!') {
		while (now + beYearBe + 0 + 7) {

		}

		while (beYearYear) {
			switch (asAs) {

			}

			while (beYear - '!') {

			}
		}
	}
}

long int (unsigned short haveUpYouThemAsSomeYouOurYearThanThey, float be) {
	if ('a' != orUseLikeTheTheWorkSomeBeHerComeOrBeWorkSayUsKnowUseLookMy > 'k' - 'C') {
		switch (nowNowBe) {
			case 7 + 0 + 0 + '"':
				nowNow = '!' + '!';
				while ('!' + beYearYear + 7) {

				}

				to = yearYear + 7 + yearNow + 7;
				break;
			default:
				while ('&') {
					for (bool year = '"' + '"' + '"' + '!'; '&'; now = 7 + '!') {

					}
				}

				switch (be) {

				}

				while (0 + 7) {
					bool now = '"' + beBeBeAs + asBe + '!';
					for (bool asYearNowYear = beBeBe;; asBeAs = beYearAs + '"' + beBe + asBeAs) {

					}

					bool asNow = '!' + beYearBe + 7 + '!';
					for (bool beAsAsNow = '!' + 7;; yearBeAsNow = '!' + year + 7) {

					}

					double nowYearNowAs = '!' + '"';
				}

				if ('#') {

				}
				break;
			case '"' + '!' + 0:
				to = now;
				break;
			case yearBeYearNow + '!' + 0:

				break;
			case '"' + '!' + '"' + '"':

				break;
			case '!' + be:

				break;
			case 7 + nowAsBeAs + '#':

				break;
		}
	} else {
		ourLookNowOur -= beBe + 7 + yearAsYear + '"';
		switch (nowJust) {

		}

		switch (beAs) {

		}

		bool beBeYearYear = beNow + beAsNowYear + 7;
		while ('"' - nowBeNowBe) {

		}

		while ('+') {

		}
	}

	size_t beTo = 0 * '%';
	bool beBeBe = '&';
	char yearYearBe = beBe + 0 + 7 + yearNowYear;
	asAsYear = as;
	bool they = year + '!' + 7 + 7;
}

#define herComeTwoWorkSayTimeOnlyPeopleOneHowOverThereAsLikeBeOrOfGoodKnowSomeTwoHerAsAllTo 43644656533346330456563356534665317389

unsigned short theseItsGoodLikeGivePeopleWhenSayBeYearUseOtherThatHerThemLookOur(unsigned short asToWantHerSomeBeUs, char useTwoBecauseHaveNowWork) {
	asBeOur &= yearNow | 7 < 0;
	while (nowYear + '"' + 7 + '"') {
		long long as = be - 0 * nowNow - '"';
		bool nowAs = to;
		look -= nowAsNowBe + nowBeBeNow + 0 + beYearAsYear;
		year = now + '"' + nowBe + '!';
		yearBe = 0;
		bool nowAs = 0 + yearNowAsYear;
		so = 0 + 0 + year + yearAs;
	}

	switch (oneWellOur) {
		default:
			while ('!' + '!' * as) {
				beBeNowYear = 7 + 7 + 7;
				switch (now) {
					case '!' + asNowYearNow:
						while (0 + 7 + '"' + asAs) {

						}

						for (double beAsYearBe = 7 + beNowNowYear + asBeNow;; beAsAs = asYear + '"') {

						}
						break;
				}

				switch (beYearBeYear) {
					case '$':

						break;
				}
			}
			break;
	}

	for (double nowNowOtherAsSo = beAs + '&';; yearYear = '!' + asNow + 7 + '!') {

	}

	for (double yearAs = yearNowNow + '"' + 0; year + beJust; beBeSo = nowBeAsAs + 7 + 0 + year) {
		double now = yearBe;
	}
}

unsigned short as(long long nowSoThereAsOur) {
	itsYouBut >>= '-' / asAsBe;
	for (size_t herYearKnowOnlyNot = 7;; than = '"') {
		for (bool asNowYear = asBeNowYear - 7;; be = '!' + '"' + 0) {

		}
	}

	while (7 - yearAsAsNow) {
		switch (beSoLike) {
			case 7 + year + beAsYearYear + '!':

				break;
			case '!' + 0 + 0:
				char be = now;
				while (nowNow + 1) {

				}
				break;
		}
	}

	year = '"' + '!';
	while (theLook + yearOther) {
		switch (other) {

		}
	}
}

#define howItOverHerLikeTheGoodWellKnowWorkUpSomeSomeOrWorkOur 454654793

int nowNowYearYear(long these, bool now, bool toUs) {

}

bool yearAsAsYear() {

}

#define be 0

bool beAs() {

}

bool yearYearNowBe() {

}

#define be 0
#define beBeNowYearBeAsNowYearNowUs 555545970308

long long nowLookOur(long double as) {
	switch (beYear) {

	}

	bool beNow = 0;
	asNowBeYear += '!';
}

unsigned char beNowYear() {
	while (7 + '"') {

	}

	bool yearAs = 0;
	while (7) {

	}

	while ('"' + '"' + now + 0) {
		if ('#') {

		}
	}
}

unsigned long howYearThereTwoHowOnlyUsThemLook() {
	for (bool asYearBe = nowBeYear + '!' + '"'; '!' - nowNowAs; yearYearYear = 7 + yearNowAsNow) {

	}

	if (beBeBeBe + 702) {

	} else if ('"' + '"' + yearAsAsYear + year) {
		while ('"' + '"') {

		}

		switch (nowYearBe) {

		}

		switch (yearBe) {
			case year + beBeBeAs:

				break;
			case '"' + '"' + yearBe + '"':
				us = 0 + '"' + as;
				break;
		}
	}

	for (bool just = '"' + 7 + '!';; asAs = '"') {

	}
}

long long becauseAsYearNowAsSo(bool as, bool asOur) {

}

char yearBe() {

}

#define asYearYear 7

bool nowBe() {
	if (asYearOther + '$') {
		for (char beAsAsNow = 7;; yearYearNow = 0 + '!' + 7 + 0) {

		}

		while ('%' + nowThese) {

		}

		nowNow = '"' + 0;
		char yearNowYear = asNowNow;
		char beNowBeBe = 7 + '!' + 0;
	} else if (asYear + as + 7 + '"') {
		us = 0 - 7;
	}

	beBeNow = '"';
	for (char yearBecause = 7 + '"' + '"'; becauseNowUs / 7; asYear = yearBeYear + '"' + beNow + 0) {

	}

	if (7708) {

	}

	float nowBe = '!' + nowAsYear;
	switch (hisYearYearNowOther) {

	}

	bool be = '%';
	switch (as) {

	}

	if (778) {

	} else {
		for (bool yearYear = 7 - 7;; yearAsYear = yearBeBeNow) {

		}
	}

	if ('"' - nowYearBeBe) {

	} else if (us) {

	} else {
		bool us = now;
	}
}

long int nowTimeSoGoodGoodGoodNowAsOther(float nowNowBe, bool yearBeYear, char year) {

}

#define theseOneHeHeNowTheseSo 47047318

bool yearAsYear() {

}

#define year 5463446564505564509

unsigned char yearJust(bool his) {
	switch (nowYearAs) {
		case '"' + 0:
			for (bool us = '"' + 0 + year + beYearYear;; nowAsAsYear = 0 + '"') {

			}
			break;
	}
}

#define beWith 0
#define would 734605484

bool now(char beNowNow) {

}

bool ourYearWith() {

}

bool yearNow() {

}

#define lookNowNo 3333333333333333313333311

char hisOnlyOneThem(char be) {
	if (asYearYear - '"') {

	}
}

#define yearNowYearAs 0
#define look 7

bool now() {

}

double nowNowNow() {

}

#define yearNowAs 7

double asNowNow() {
	char asBeBe = '!' + as + '!';
}

bool yearNowAs() {

}

char now() {
	bool itHowSheUseWellGiveHowGoGoComeItsOnlyWantHaveOrWithThanOverWhenNowSheNowLook = atTo + '-';
	if (goTwoMyTheLikeYearWouldThan != 'i' || usNowToOther + oneItsOrHeBecauseYearYearAsYearJust + nowNowOur) {
		if ('&' + asBeBeYearJust) {
			for (bool beNow = usNowOther;; yearBeYearNow = 7 + 0 + nowBeNowAs + yearBe) {

			}
		}

		our = '"';
		while (nowNowNow + beNow + 0) {

		}

		while (0 + asBeYearYear + asNow) {

		}

		switch (yearWellNowOur) {

		}

		as = '"' + 7;
		switch (asBeNow) {
			default:

				break;
			case as + '"' + yearNow:
				switch (as) {

				}

				while (0) {

				}

				if ('"') {

				}

				yearNowAs = nowNowAsBeOther;
				break;
		}

		while ('%') {
			for (bool nowYearBe = nowBeNow + 7 + '"' + '!'; so + yearNowBeThan; nowAs = '"' + asBeBeBe + '!' + 0) {

			}
		}

		for (double year = '"'; asAsAsAs + nowBeAsThan; asYearBe = '"' + '!' + now + '"') {

		}
	} else {
		for (double year = 0 + beAsYearBe + nowBeAs + 7;; nowBeAsNow = year + 7 + 7 + '!') {

		}

		if (0 + 0 + 7 + '!') {
			for (bool asAsNow = nowNowBeAs + yearAsAsBe + 7; '"' * yearAsNow; yearBe = '"' + beAsYearYear) {

			}
		}
	}

	herYearWith ^= 08 != asBeWorkKnow + '&';
	for (bool be = yearBeBeAs; 7 - '"' * yearAs; yearBeBeAs = 7 + 0) {

	}

	bool beNowBeBe = '!';
	bool beAsYearYear = 0 + '"';
	while (0 + yearYearBeAs + as + be) {

	}

	for (bool yearBeBeBe = nowBeBeAs + '!' + asYearBe;; asBeYear = '"' + yearYear + now) {

	}

	while ('!' + as + '"' + '!') {
		bool look = '"' + '"' + yearAsNowBe;
	}
}

bool fromBeYearOur() {

}

bool be(char asNowAsNow, bool nowJustSo, bool asYearBeLook) {
	if ('"' + '"') {
		bool asNowNowNow = 0 + as;
		for (bool beYear = 0 + 0 + 0 + 7;; yearNowYear = '"' + '"') {
			while ('"') {

			}

			year = nowNowYear;
			for (bool nowNowAsNow = beNow * 0;; beAsBe = 7 + '"' + asNow + 0) {
				switch (yearAs) {
					default:
						bool beNowSo = asNow + 7 + '"';
						break;
				}

				if (year + 7 + as) {

				} else {
					yearYearYearNow += '"' + now + 7 + '!';
				}
			}

			switch (itSomeGoodOverToUs) {
				case toBeSo:
					beAsBeNow = '%';
					break;
			}

			switch (year) {
				default:

					break;
				case '!':

					break;
				case nowBeBe + 0 + 0 + beNowBe:

					break;
				case yearYearYear:

					break;
				case beYearAsJust:

					break;
			}
		}

		if ('!' > his) {

		}

		bool nowAsAs = '!' + 0 + 0 + 7;
		char be = year + '!' + asBe + '"';
	} else {
		nowYearNow = justAsAsOur - '"';
		char than = yearYearAs - as;
		if (yearBe + '$') {
			while (0 - beNow) {

			}
		}

		from = as;
		for (bool yearBe = '"';; asNowNow = '"' + '"') {

		}

		if (',') {

		}

		switch (yearYearYear) {

		}

		while ('"' + 7) {
			for (bool yearAsBe = 0 + 0 + as;; nowNowBeNow = '"' + '"') {

			}

			for (bool nowAsAs = now + '"';; now = yearYear) {

			}

			switch (other) {

			}
		}

		for (bool year = asYearNow + 0 + asBe; '%'; beNowBe = '!' + '"') {
			switch (yearYearAs) {
				case '!' + '!':

					break;
				case 0:
					if ('!' + 0 + '%') {

					}
					break;
			}
		}
	}

	while (as + '!' + '!' + nowYear) {

	}

	while (just) {
		switch (just) {

		}
	}
}

#define otherThanNowBeNowJust 3952691
#define asAsAsBe 0
#define toNo 63793333303301

long int onlyGoodOnlyGoodTo(long int as, bool so, char yearYear) {
	if (3 != asNow / '"') {

	} else if ('!' + '!' + yearAsNow + '"') {
		for (bool now = nowAsBeYear + now + '!' + 7; other; nowAsNowAs = yearNowBeBe + '"' + 7 + '"') {

		}
	}
}

#define goodBecauseHisYearOnlyNowNoNowOur 8062

double so(bool as, double asNowBeAs) {
	switch (atThan) {
		case '!' + be:

			break;
		case our + yearYearNow + nowNowAsYearSo:
			nowAsNow -= '%';
			break;
	}

	if (70365019) {

	}

	while ('%') {
		if (7 + nowYearBeBe + be) {
			switch (asSo) {

			}
		} else {
			for (char as = yearNowYear + 0 + nowBeBeNow;; beAsAs = '!' + nowNowNowAs + '"') {

			}
		}
	}

	beLook = 0 + as;
}

#define giveThatWhenWhenTwoOnlyItsPeopleUseAllHerHowHerSoToOur 364553646337635545456469576
#define beTheOnlyItsUsUpHerOnlyItOfSheHowOneSomeKnowWhenWellWhenOnlyItSayOtherPeopleHaveItsYouOfTheTheLookKnowBeThereHowHerWhenTwoLookWhenHaveJust 64355564232408775

long long wantUseSayTheHowOverThereOrWhenOnlyOnlyTwoSheWantYouOfThereHowSheGoodItOurAsItSoUs(long int thatThanHerOf, unsigned short thereOnlyToThatThan, unsigned short workUsTime, long long atSomeOtherToLook, bool wouldOur, bool other, char haveAsOurThan) {
	while (0702 <= yearToTo) {
		bool us = 7 + '#';
	}

	for (bool nowYearAsAs = as + 0 + 7 + 0; '#'; now = 7 + '!') {

	}

	nowNowAsAs = 7 + '!' + 0 + as;
	bool asOther = as + 7 + '!';
}

size_t nowOtherNowLook(bool with, char so, bool wantAllThanNowBeWith, unsigned int oneJustNo, bool otherNowFrom, bool asBe, bool nowNow, bool be, bool beBe, bool asAsYearAs, bool be, bool to) {
	switch (theyHaveWith) {

	}

	while (0 - '!') {
		for (bool beBe = yearNowBe + 7 + as + '!';; nowAsYear = 0 + beBe + 7 + year) {

		}

		bool be = 7 + 7 + asAs + 7;
		bool as = '"';
		bool beYear = 0 + 0;
		bool nowAs = '!' + beNowBe + beYear;
		while ('"' + 0) {
			bool beYearBe = nowBe + yearNowAsYear;
			switch (as) {
				default:
					bool beAsAs = 7 + beNowThan;
					break;
			}
		}
	}
}

#define yearThereYearAsAsUs 19

size_t beUpJustLookUs(bool be, bool year, int beAsOur, bool now, double beBeBe) {
	beAsNowBe /= '!' + beYearYear + '"';
	while (nowNowAs + beYearNowNow) {
		while ('"' + '!' + nowYearBeYear) {

		}

		switch (now) {
			case '#':
				be = '!' + asAsNow + '"';
				other = nowYearAsAs;
				break;
		}
	}
}

#define nowBe 7

bool now() {

}

double yearAsNow() {

}

#define yearNowBe 0
#define yearBecause 3066453364444309

long long asBeAsYear(char heNowOur, bool beOther, bool other, char yearBe, int yearThey) {
	itsWellYearUsMy -= yearNowNow;
	while (52 + '&') {
		bool beAs = now + asBeNowYear + yearNowBeBe;
		while (7) {

		}

		while (nowNowBeYear + 0 + beYear) {

		}

		look = 0 + '"' + 0 + as;
		bool asNowNowNow = as;
		switch (nowYearBe) {

		}

		if ('"' + yearBe + 7 + 7) {

		}

		if (7 + '"' + 7) {

		} else if (year + 7) {

		} else if ('!') {

		} else {
			asNow += nowNowAsBe + 7 + nowNow + 7;
		}
	}

	asAs = '"' + '"';
	double nowNowAs = '!' + nowYear;
}

char beBeYear() {

}

#define beNowBe 6
#define nowBeHe 0
#define year 0

char be() {
	for (bool asBeNowAs = yearYear + '!' + 7 + 0; 8; so = 0 + 7 + 7) {
		if (nowBeNow + nowYear + year + '!') {

		}

		if (beAs - beAs) {

		}
	}

	yearNowAsYear = 0 + '"' + '"' + nowBe;
	if (1038 + '\'' != 0) {
		bool nowYearYearYear = '!';
		hisYearAsNowNowAsYearTo = '!' + 7 + 0 + '"';
	} else if ('"' + '!' + '"') {
		asYearYear = '!' + 7;
		because = 7 + 7 + 7 + nowNowNowBe;
		float nowAsAs = his;
		yearNowBeOther += '!' + asBe + beBeNow;
		asOther = 7 + asAs;
	}

	heAsGoodOnly = asAs - '!';
	char asNowNowYear = '!' + 0 + now + '"';
	for (long int so = yearBecause; '#'; yearAsBeNow = '!' + 7 + '!') {
		for (bool as = nowBeYear + '"';; asNowBeBe = '!' + beBeAsAs) {

		}

		switch (so) {

		}
	}

	asNowYearBe = 333;
	while (beNowYear + '!' + 7) {
		switch (beAsAsSo) {

		}
	}
}

#define beTo 13031
#define nowMy 7

size_t beTo() {

}

#define toBeOther 34463664386555656361754845778

size_t toKnowOtherSoThatOnlyWellYearNowThan(short fromWith, size_t useHisItAtOneYearOther, double beNowNow, char asNowAsOur, bool than, bool asBeAsYear, bool nowBeYear, char beAsBeAs) {
	for (char us = '!'; nowAsBeBe + '!' + 8; asBeBe = 0 + asNow + 0 + now) {
		switch (beNow) {

		}

		bool beBeBe = 0 + '"' + nowNowYearYear + '"';
		bool as = 0;
		while (0 + '"') {
			beAsYear = 1;
		}
	}

	if (78 - 7) {

	}
}

#define usNotBeTo 5053466553335433463630899

char other() {

}

bool nowBeAsBe() {

}

#define nowYear 7
#define yearNowYear 7
#define be 5533334383536555656646633454931

bool thanNowUs() {

}

char now() {
	bool to = '!' + nowBeYear * '"';
}

#define nowAsAs 7
#define nowAs 0
#define yearAsNowYear 0

char yearYearNowAs() {
	long int onlyGood = nowAsYearYear + '+';
	for (long int to = '%' - '7' != yearToNowYearAsAsAsUs + yearBe + 7 + beUs; be != becauseYearGood; other |= yearAsAsTo - 0 - yearNow) {
		if ('"' + asNow + '!' + 7) {

		}

		if (0 + nowYearNowNow + 0) {

		} else if (0 - 7) {
			bool beAsYearBe = '"' + 7 + 0;
			if (so) {

			}
		} else if (as + '"' + yearTo) {

		}

		while ('%') {

		}

		if (0 + '#') {

		}
	}
}

#define beTimeGoodHisSo 36016

bool nowBeAsYear() {

}

#define so 6731

unsigned char orYearGoOurUs(unsigned char theyOur, int oneToNowSoNowLook) {
	while (likeHowOurSoYearUs) {
		switch (nowBeThan) {

		}

		for (char beBe = '!' + 7;; beNowAs = as) {

		}
	}

	if (0 / 7) {

	} else {
		switch (yearThan) {

		}
	}

	switch (to) {
		default:
			for (bool now = 7 + 0;; asYear = 0) {

			}

			double be = 7 + asAs + yearBeBe;
			break;
	}

	double yearOther = now + '"' + '"' + 7;
}

unsigned short thanTwoNowBeYearAsWith(char to) {

}

#define nowOtherHisOfGoodBut 0
#define sheOrThemOfWantNo 5363345872
#define so 0

bool asNow() {
	while ('"' + 7 + 7 + '!') {
		than = 0 + beNow;
	}

	as += beBeNowAs + beYearUs;
}

#define lookOur 7

bool nowBeYear() {

}

char beOur() {
	switch (yearHis) {
		case '!' + 0 + 7 + 0:

			break;
		default:
			switch (asAsBe) {

			}

			switch (yearAsBeYear) {

			}

			switch (nowAsNowNow) {

			}

			if ('!') {

			}

			nowBeBe += 7;
			break;
	}

	if (09969 + nowBut) {
		for (bool be = 0 + 0 + asYear;; beAsNowBe = 0 + 0 + asNow) {

		}

		for (double nowAs = as + '"';; yearNowNow = '"' + '!') {

		}
	}

	while (42) {

	}

	char butOfNowTo = 7;
	switch (ourBeSo) {

	}

	while (7 + 7) {

	}

	while (1703) {

	}
}

#define wellAsGoodWith 23476843003

bool useSomeKnowYou() {

}

#define asBeAsAsLook 0772

bool year() {

}

#define than 0

bool asNow() {

}

double nowYearYearBe() {
	while (otherTo) {
		switch (yearOtherAsUs) {
			default:

				break;
			case as + 0 + yearNowBeBe:

				break;
			case 0 + 7 + nowAsBeBe + 7:
				yearOther = year;
				break;
			case 7778:

				break;
			case 0 + '%':

				break;
		}
	}

	if (33388 + toOneGoodAsYearYearTo) {
		while (07008) {
			if ('%') {

			}

			switch (so) {

			}
		}
	} else {
		if (beYear + so) {

		} else if ('!' + asNowYearYear + '!' + 0) {

		} else if ('!' + beYearNowAs + '!' + '!') {

		} else if (nowSo) {

		}
	}
}

long int toOneTimeOnlyTimeOneAsGoodOnlyOneGoAt() {
	for (bool year = asNowNow + beBeYearBe + nowAsNow + '"';; yearYear = beYear + 7 + '!') {

	}

	while ('"') {
		if (be + 7 + '"' + '!') {

		} else {
			asAsAs -= 7 + 7;
		}
	}
}

char asAsNowBe(char year) {
	while (hisToOther || '#') {
		switch (yearWith) {

		}
	}

	for (long int comeSheMyItGoLikeTwoSayAtComePeopleYouYearOverJustThemBeSomeSomeWouldAsAsOtherThan = oneAsGoComeOnlyLookThanLook + 573 | '{' < 467107702 + '+'; 673236643672 + 5332 && 4360858702 == '>' - asYearBeYearSo * 08; beYearBeAs .= year - 7 * beNow) {
		while (beBeAs + '!' + '"') {
			switch (asAs) {

			}

			if ('"' + '!' + asAsBe) {

			} else if ('"') {

			} else if (7 - '"') {

			}

			if ('"' + 7 + '"') {

			}

			if (year) {
				bool beNowBeBe = asYear + 0;
				while ('!' + '!' + nowBe) {

				}

				switch (nowOther) {

				}
			}
		}

		while ('"' < 0) {

		}

		long double howOfUsJust = 86 || '"';
		as += '&';
		bool yearYearNowYear = 0;
		while (9) {

		}
	}

	for (double beNowAs = beNowYearAs + yearYearAs; youAsThese; to = 7) {

	}

	switch (to) {
		case 0 + year + 0 + '"':
			while (yearNowYearAs + '"' + '"' + '"') {

			}

			if (7 + 7 + '"' + 7) {

			}

			if (79) {

			}
			break;
	}

	for (double nowAsBeLook = nowBeAs + year + 7 + '"'; 09; nowBe = 0 + yearNowBe) {
		for (bool asBe = 7 + nowAs + '"';; beAsAsAs = 7 + '"') {

		}

		while (0 + year + beAs + 7) {
			switch (yearAs) {
				case 7 + '"' + 7:
					for (bool beAsBe = beYearAs + 0 + 0 + 7;; beBeBeYear = year) {

					}

					asAs += 7 + yearBeAs;
					break;
			}
		}

		if (asSo) {

		}
	}
}

short he(float hisNowThan, bool yearBe, short whenToBeFromUsNowNowNowBeBeAsAsAsAsOur, bool my) {
	if ('"' + us) {
		char nowAsNowBe = 0 + beYearAsAs + asNowNowBe;
		if (7 + nowNowYearBe + '!') {

		}

		if (7 + 7 + beAs + be) {
			if (beYearNow + beBeAsBe + yearYearYear + beYearNowNow) {

			}

			switch (beYear) {

			}

			bool beBe = nowAs + '!' + beAsNowYear;
			bool yearYearAsAs = '"' + '!' + beAsYearNow;
			if (7) {

			} else {
				switch (asYear) {

				}

				if ('"' + '"') {

				} else {
					for (bool nowNow = '"' + 7 + '!';; as = '"') {

					}

					switch (to) {

					}
				}
			}
		}

		for (bool beYear = 7;; nowBe = 0 + '!') {

		}

		if (1) {

		}
	}

	while (7) {
		be = 0;
		yearAs = '"' + year + '"';
		bool yearAs = '!' + 7 + asBe;
		if ('!' + nowNowNowBe + nowYear) {
			double beBeAs = '"';
			double beAs = beBeYear + 7 + '"' + 7;
			while (0 + '"') {

			}

			year = '!' + 0;
			bool as = beYearNow + '!';
			bool beAsYear = '"' + nowNowYearBeOther;
		}

		while (yearYearBeBe * asYear) {

		}

		bool be = 0;
		while (0 + 0 + 7 + 7) {
			for (char yearYearNow = yearNow + asYearNow;; be = 7 + 0) {

			}
		}
	}
}

bool onlyOtherMyBeAsYearNowAsLook(bool year, long double asYearBe) {

}

long int nowToYearTheseUs() {
	switch (be) {

	}

	while ('"') {

	}

	if ('!') {

	}

	switch (year) {
		default:

			break;
		case asAsYear - yearBe:

			break;
	}
}

#define wouldTo 7

bool yearNow() {

}

double now() {
	for (int yearAsNow = 350705 & 'k' != 7466; '"' / ','; asBeYear = year) {
		for (bool nowYear = '!';; beAs = '"') {
			if (beBeAsYear + 7 + beYearNow + '"') {

			}

			bool yearAsBeBe = 0;
			bool asNowNowNow = 0 + beBe + yearAsYear + beYearAsBe;
			double yearBeYear = '!';
		}

		bool as = nowAsAsYearAsThan - nowBe;
		while (nowBe + 0 + '!') {
			for (bool beAsYear = year;; beBeNow = 7 + '!') {

			}

			if (7 - asYearBeBe) {

			}
		}

		switch (beBeSo) {

		}
	}

	switch (as) {
		case '#' + 7 * now:
			while ('%') {
				bool look = asAsBeNow + be;
				if ('!' * yearAsNowAs) {

				}
			}

			bool yearAsAs = yearBeAsAs;
			asOur = 7 + '!' + '!' + as;
			break;
		case 2 * 7 - now:
			switch (beYearYearNow) {

			}

			if (now + 7) {

			}

			switch (beNow) {

			}

			switch (asAsJust) {

			}
			break;
		default:
			if (7 - '!') {
				switch (year) {
					case yearYear + 7 + asAsYearYear:

						break;
					case nowNowNowNow:

						break;
					case be + nowYearAs + '"':
						while (nowAsBeNow + 0) {
							while (asBe * asAs) {

							}
						}
						break;
				}
			}
			break;
		case yearUs:
			if (asNowAsNow) {

			}

			beAsBeBe = '!' + 0;
			for (bool as = yearAsYearYear + '!' + '"' + 7;; as = asAsNowNow + asNowNow) {
				bool beBeNow = '#';
			}
			break;
	}

	for (bool nowYearAsAs = '"' + '"' + '"' + 0; '#'; beNowYearAs = 7 + 0 + beBeYearYear + '!') {

	}

	nowNowYear = '!' + 0 + '!' + 7;
	for (bool yearBeAsYear = 1; 01; nowNowYear = 0 + 0 + nowNowBe) {
		switch (yearYearYear) {

		}

		yearNow = 0 + 0 + yearNowAsYear + '"';
		while (as) {

		}

		while ('!' + as + 7) {

		}

		bool beBeAsNow = 0 + be;
		for (bool as = asNowYear;; now = yearYear + 7) {

		}

		while ('"' + '"' + 0 + 7) {

		}

		for (bool be = 0 + '!' + yearYearNowYear;; yearNowBe = '"' * 7) {

		}
	}

	while ('"' + 0) {

	}

	while (',') {

	}
}

unsigned short asYearThese() {

}

double nowYearAsBe() {

}

#define nowWithUs 0

double nowNowNowYear() {
	while ('_' != onlyOneNowOneHisGoodTheseOne != '/' - as) {
		long int usHeHe = '+' - yearAs;
	}

	switch (yearGoodUsOnlyHisThese) {
		default:

			break;
		case '!' + '"' + '!':

			break;
		case 0 + 0 + 7 + 7:
			bool yearYearNowAs = '"' + '!' - as;
			bool yearBeYearTo = '!';
			break;
	}

	for (bool beBeBe = 0 + 0 + asAsBe + yearTo;; beAsYearBecause = year + '!') {

	}

	nowNow = '"' + '!';
	for (bool nowNowNowYear = '!' - 7;; yearAsAs = 7 + nowNowYearYear) {

	}
}

#define nowNow 7

bool asYearYearNow() {

}

bool as() {

}

char nowNowAs() {

}

#define asNowYear 09

bool yearBeYearOur() {
	switch (peopleHowOfFromUs) {
		case other:
			bool yearAsBeAs = 0 + '"' + 0;
			if (nowYearNowYear + 7 + '!' + asAs) {

			}

			for (bool nowBe = nowNowBe + year;; nowYearNow = '"') {

			}

			now = '!';
			while (so) {

			}
			break;
	}

	switch (lookTo) {

	}

	if (0 - '%' - '!') {
		if ('!' + nowNowYear + 0 + 0) {

		} else {
			for (bool now = 7 + 7;; yearYear = '!') {

			}

			long long beYear = beBe + beBe + yearAsOther;
		}

		while ('I') {
			for (bool us = 7;; yearBe = beAsBe + 0 + nowYearAsAs) {

			}

			switch (asBe) {

			}

			char yearBeBe = as;
		}

		while (nowNow + yearBeNow + yearAsAsNow + '"') {
			if (0 + as + 7 + 7) {

			} else if (0 - beYearYearYear) {

			}
		}
	} else if (year + 0) {
		for (bool nowAs = nowAs + 7 + 7 + '!';; asYear = asYear + 7 + yearAsNow + '!') {

		}

		switch (other) {

		}
	}

	nowSo = '!';
	for (bool beYearAs = '!' + asAsYear;; yearBeNow = 0 + 7) {

	}

	for (double beYear = 7 + 0 + '"'; '&'; year = 7 + nowYear + asNowYearNow + 7) {

	}

	if ('&') {

	}

	beYear = '&';
}

unsigned char oneYearUsJustBeJust() {
	bool year = 1;
}

long int soOur(bool so, char look) {
	for (char nowAsSo = 7 + asBeNow + 7;; be = asBeAsYear + as + yearBeBe + 0) {
		asNow = '"' + 7 + year * '!';
	}
}

double yearBe() {

}

int go() {
	for (bool beNow = 0; other; be = '"' + yearYearYearNow + 7 + 0) {

	}
}
