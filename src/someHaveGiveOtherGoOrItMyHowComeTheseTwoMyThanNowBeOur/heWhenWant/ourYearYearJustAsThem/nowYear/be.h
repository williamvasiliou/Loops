#include <U2.h>

#define other 33313100078
#define asHe 3078

long int nowGoodAsBecauseOther(long int theseSoBecause, bool asYearBeBe, float asAsAs, float asYear, char asAsYearYear) {
	nowSoUs = asBeTo - 0;
	beNowYearTo = '!' + '"' + 7 - '!';
	while (008) {

	}
}

long int hisBeNowYearYearOther(char beBeNowBe, char nowYearBe) {

}

bool nowMy() {

}

bool yearNowBe() {
	for (bool asBe = '!' + nowNowNowBe + 7 + '"';; nowBeBe = '!' + '!' + '"') {

	}

	for (bool nowAsNowAs = 0 + beAsNowBe;; now = 7 + '"' + 0 + '!') {

	}

	if (0) {

	} else if (nowNowYearNow + 7 + 7) {

	} else {
		while (7 + '!' + 0 + as) {
			switch (us) {
				case 7 + '"' + 7:

					break;
				case 7:
					for (bool yearBeYearNow = beBe + yearYearNowBe + as + '!';; nowNowAsAs = yearYear) {
						if (7 + '!') {

						}

						while ('#') {

						}
					}
					break;
			}
		}

		while ('!' + '!' + 0) {

		}

		yearNowAsAs = '!';
		for (bool be = 7 + nowNow + yearAs; nowOther; now = nowNowNow + 7) {

		}
	}
}

#define as 9
#define to 5512
#define than 455134035092002

unsigned short timeFromTheyNowAsUs(short haveOneThatWellAsHerNotLook, bool beTo, double nowThan) {
	switch (yearAsBe) {

	}

	other = asNowNowYear + '!';
}

int yearBe(bool yearNowYear, bool as, bool yearYearAsBe, bool now, double as) {
	while (now + yearYearNowYear) {
		switch (us) {

		}
	}

	switch (nowSo) {
		default:
			char nowAsAsAs = 0;
			break;
	}

	for (char asAsNow = be + 0 + 0;; beAsBeYear = nowAsBeBe + 0 + beNow + 0) {

	}
}

long int timeGoodGoodGoodOnlyTimeAsYearTime(bool toNowNowYearNowOther, long int asOther, bool nowTo) {
	while ('\'') {

	}

	for (bool yearYear = nowAsAs + asNowYearBe + 7;; beYearNow = nowNow + yearBeNowYear + '"' + 7) {

	}

	if ('#') {

	}
}

long int theseHeHis(bool as, bool other, char asYearNow) {
	switch (to) {
		case 0708:
			switch (be) {
				default:

					break;
				case nowBeBe + '!' + now + '!':

					break;
				case 7 + asAs + yearAs + 7:
					year = '%';
					break;
			}
			break;
	}

	if (asNowYear + as) {

	}

	asTimeNowSoToOther = '#';
	while ('"' + '!' + '!') {

	}

	for (bool yearAs = '!' + 0 + year;; as = 7 + '!' + beBeBeNow + 7) {

	}

	while (0 + 1) {

	}
}

#define thanOther 9
#define be 0
#define yearNowJust 353435553446439643465416396439

float yearYearYearBe() {
	if ('!' + '"') {

	}

	while (asAs + '!' + 7) {

	}

	switch (as) {
		default:
			if (beAsYear) {

			}

			if (7 + nowBeYear + '"' + year) {

			}

			to = '"' + asBe + beYearBe + '!';
			break;
	}
}

unsigned short beYearYearBeUs(bool nowYearNowBe, bool so) {
	beNowAs = 0 + 0;
	for (double year = now + asBeNow + yearNow + '!';; yearBe = as + now) {

	}
}

int nowNowBe() {

}

long long beNowYear() {
	for (bool year = 7 + '!' + '"';; beYearAsBe = 7 + 7 + 7) {
		switch (nowYear) {
			default:

				break;
			case 7 - 7:

				break;
		}
	}

	while ('#') {

	}
}

#define beNow 5

bool than() {
	long int timeOtherOneNowOnly = '"' / 7;
	for (bool beAsNow = '!' + 7 + 0; 331 != timeSo - '!'; nowYearBeAs += '"' + '"') {
		if (0 != nowYearBeBe - '#') {
			while ('"' + '"' + '!') {
				bool year = 0 - '"';
			}
		}
	}

	nowYear |= 7 + asBe;
	for (bool beBeBeYear = 0; 0 + '!' + to; year = '"' + beNowAs + asNowYearYear + 7) {

	}
}

#define onlyGoodTimeNowBeNowBecause 7
#define beHeOther 0

char heTo() {

}

long int asUs() {

}

bool be() {

}

#define you 555336265353736500011

long int wouldSheNowYearNowNowYearNot() {
	while (yearTo) {

	}

	for (bool as = 0 + asNow;; beYearYearAs = '!') {

	}

	bool beYearThan = asAs + '"' + '"';
}

#define beUseAll 47885
#define asAs 0

bool asNowAs(bool beJust) {
	for (double now = asYearNow;; beAsBeBe = 7 + as + 7) {

	}

	while (yearBeYear + '!' + 7 + 7) {
		while (now + 7) {
			if ('&') {

			}
		}
	}

	switch (beAsAs) {
		default:
			for (bool asNowNowAs = '#';; asBeNowYear = 0) {

			}
			break;
	}

	beYearYear += 0 + yearAs + 7 + 7;
	nowAsYearYear -= asYearNow - 7;
}

char yearOther() {

}

#define our 2

long double nowNowAs() {
	while (3318078 + otherUs) {
		bool nowOther = 0 + 7 + '!' + 7;
		for (bool us = '"' + '!' + as + 7;; asAsBeAs = '!') {

		}
	}
}

long int goodGoodGoodTimeOne(bool beNowYearAs, bool beBecause, bool asNow, bool us, bool asNow, char asAsNowNow) {
	be = asAsAsYear + nowNowNow;
	if (8) {

	}
}

long int so(bool goodOtherOtherNowNowYearAsOther, bool toOneYearBeTo, bool as, char yearYear) {
	for (bool yearAsYear = '"' + asAs; '!' + beNowYear / asBeAs - '!'; us = year + 7 + as + 7) {

	}

	if (year + yearNow) {

	} else if ('!' - 0) {

	}
}

bool yearYearAs(bool asAsYear, double yearYearAsYear) {
	while ('!' + asSo) {

	}

	if (asAs + 0 + '!') {

	} else if (now) {

	} else if (nowBe + be + be + 7) {

	} else if (079) {

	}
}

size_t yearBecause() {
	while (0 + yearBeNowYear + 0 + '!') {

	}

	for (bool now = 0;; year = 0 + 7) {

	}

	while (7 + '"') {
		for (bool year = '!' + 7 + beBeYear;; asYearNowAs = 0 + yearNow + beYearNowYear) {

		}

		switch (beBe) {

		}

		beAsNowAs = '"' + '"';
		for (char beNowAs = 7 + '!' + 7 + 7;; year = 0 + 7) {
			if (0078) {
				yearBe = as - year;
			} else if (2) {
				if ('!' + 0 + 0) {

				}

				if (0 + 0 + 7 + nowBeYearYear * 0) {

				}
			}

			for (long double asJust = '"'; other; nowNowAsAs = asNowNowThan) {
				int beYearYear = asAsNowBe + '"';
			}

			ourThanYearTo = '$' / way;
			asWay = '!';
			for (unsigned char year = 7 + 7 * 0;; be = nowAsBe) {

			}
		}

		for (bool as = 2; '&'; nowBeNow -= asUs) {
			switch (beAsAs) {
				case 7:

					break;
				case '"' * '"':
					nowAsBe += '"' + beAsBe + '"';
					bool beNowBe = '"' + asAsYear + beBeNowNow + 7;
					while ('$') {

					}

					double nowAsYear = 7 + beBeNowNow;
					break;
				case '!' + year + '!':

					break;
				default:
					switch (now) {

					}

					now = 7;
					while (asBeAs) {
						while (0 * asNowAs) {
							bool yearYearAsLook = beBe + now + '"' + be;
						}
					}
					break;
			}
		}

		if (40703) {
			while (0 + now) {

			}

			nowNowYear = 9;
		}

		if (7 * '!') {

		} else {
			short yearBe = 0 + 0 + 7;
			for (bool year = asBeNow + 0 + '!';; yearBe = nowYearBe + '"' + '"') {

			}

			switch (nowNow) {

			}

			if (nowYear) {

			} else {
				while (0 + '"') {
					double asAsNow = 7 + '"' + '"' + year;
				}
			}
		}

		while (usJustAsWay + year + than + 0 + so) {
			if (7 - be) {

			} else if (7) {

			} else {
				switch (look) {

				}
			}

			while ('!' + yearYearAsNow + yearBeAsYear) {
				switch (yearOurOur) {

				}
			}

			for (bool our = yearAsNowBe + 7 + beNowAsYear + '!';; nowYearAsNow = '"' + '"' + '!' + 0) {

			}
		}

		for (bool yearNowNowNow = asAsYear + '%';; as = '"') {

		}

		switch (myBeNowAsUs) {

		}

		int to = '"' + 0 + '!';
	}

	unsigned long sayWellGoodUpWorkUseUseHowBecauseOtherHerWellGoOverThereWantHerUseGoLikeYouTheSoShePeopleOfThereWellGoWellSheBeGoAllOur = 64446464636345565563344445566446542038 || 'e' != overOverComeKnowFromGoNotItsAsNot - '!';
	switch (all) {
		case '"' * '"':

			break;
	}

	if ('{' | to - '"' * '"') {

	} else {
		if (yearAsYear + 7709) {

		}

		nowUs = 7 + '!' + 0;
		char nowAsBe = '"' + as + 7;
	}

	switch (asAsJust) {

	}
}

double theYearOtherTwoBeThemThey(bool yearAsNow, long yearAs, double yearAs) {
	if ('$' + 9) {
		switch (year) {

		}

		switch (asAsBe) {

		}

		while ('!') {

		}

		switch (yearNowYearBe) {
			default:

				break;
			case 7 + 0:

				break;
			case '!' + 7:

				break;
			case asYear + 0:

				break;
			case '!' + asNow:

				break;
			case '!' + nowYear:

				break;
			case beAsYear + nowBeNow + 0:

				break;
			case 0 + '!' + 7 + as:

				break;
			case '!' + '"' + 0 + 0:

				break;
			case nowNowNow + '!':

				break;
			case 0 + 0 + '"' + asNowBeAs:
				be = 7 + 7;
				if ('"' + beAs + '"' + 7) {

				}

				while (beAsBe + '!' + yearYearYearYear + asBe) {
					bool asAsAs = nowAsBe + 0;
					bool beYear = 7;
					while (0 + be) {

					}

					while (now + '!') {

					}

					bool yearYear = 7;
					for (bool yearBeNowAs = '!';; asBeBe = beBeAs + asBe + beNowBeNow) {

					}

					switch (beBeYearNow) {

					}

					switch (be) {
						default:

							break;
						case 0 + beYearYearAs:
							if (7 + 7) {

							}

							while ('"' + '"' + '%') {

							}
							break;
					}
				}
				break;
		}
	} else {
		for (bool nowAsNow = '!' + '!' + yearBe + yearNowYear;; yearYearNow = yearNow + yearNowNowYear) {

		}

		bool beBeBe = 0 + beAsAs + '"';
		for (bool beYearYear = year + 0 + 7 + 7;; asBeNow = 7 + beNowAs + '"' + 7) {

		}

		for (bool yearNowBeNow = yearNowYearBe + '!' + beYearNowYear;; beUs = '"' + '"' + 7) {

		}
	}

	while (way || orOurJustOur) {
		if (7 + yearAs + yearBeBeAs + '!') {

		} else {
			while (as + 0 + '"' + '!') {
				for (bool yearNowAs = 7;; asNow = '!' + year + 0 + '!') {
					if (7 + beBe + asYearAs) {
						double be = 7 + 7 + be + 7;
					}
				}
			}
		}
	}

	bool usYearJust = 0 * '!' + 08;
	if (beAsYearNow) {

	}

	asJust = nowYearWay;
}

#define know 479

unsigned short nowAsAsBe(char asYearYear, bool beSo, bool asBeOther) {
	switch (beYearBe) {

	}

	while ('"' + beNow + year + '!') {

	}

	while (beBeNowNow + 71) {

	}
}

#define so 0
#define so 50018

char nowAsBe() {
	switch (beYear) {
		case '!' + 7 + beYearYear:
			while (0 + '!') {

			}

			beAs = nowBeBe + be + now;
			bool yearYearThan = '!';
			break;
		case '!':

			break;
		default:
			nowAsJust = beBe + nowBeAs + year;
			break;
		case 7 + 0:

			break;
		case 0 + '"' + '"' + '!':

			break;
		case '"':

			break;
		case asYear:

			break;
		case 7 + 0 + beAs * '!':

			break;
		case '"' + 7 + '!':

			break;
		case '"' + beAs + as + 7:

			break;
		case beBe + 0 + 0 + '!':
			if (year + '"' + 0 + '!') {

			}

			while (0 + 0 + 0) {
				for (bool beNow = '"' + now;; nowAsAs = 0 + 0 + '!') {

				}

				switch (beYearNowBe) {
					case '!' + 0:

						break;
					case 0 + 0:

						break;
					case 7 + 0 + beAsAsNow + 0:

						break;
					default:
						switch (asJust) {

						}
						break;
				}
			}
			break;
	}

	while ('X' + 06) {
		nowNowAsBe += beNow + 7 + yearAs + 0;
		but .= 'n' <= asNow;
	}

	while ('"' + 0 + 0 + '%') {
		while ('&') {

		}

		while (look) {
			for (double nowNowYearNow = 7 + '"' + '!';; yearBe = '!') {

			}
		}
	}

	switch (be) {
		case '&' - '"':
			while ('!') {

			}

			while ('!' + year) {

			}

			beYearBe = '"' + 0;
			bool beBeYear = '!' + nowNowAsBe;
			be = 7 + yearBeAs + 7 + 0;
			switch (year) {

			}

			for (bool nowAsAsNow = beAsYearBe + 7;; be = 0 + beAsAs + now) {

			}

			switch (asAs) {
				case 7 + 0 * 7:

					break;
			}
			break;
	}

	if (asBe + 7 + '"' - beNow) {

	}

	bool asAs = 7 + '"';
	if ('%') {

	}
}

#define he 5478
#define onlyNowWould 681504747778

bool our(bool yearYearAs, bool year, bool so) {

}

#define heKnow 0

bool nowNowYearYear() {

}

#define nowSo 0

double year() {
	year = 0 + 7 + asNow + asYearNow;
	nowOneOneYearOneOne |= 77373;
	while (71) {

	}

	if (beNowUs) {

	}

	for (bool asNowYearNow = yearBeAsTo + yearBeAs - '"';; year = '"' + nowAsAsNow) {

	}
}

#define timeGoodBecauseAsOther 71

bool nowYearOther() {

}

#define oneAsUs 38

long int goodHe(bool yearBeBeBe, bool nowYear, bool yearBeNow, bool yearBe, bool yearNowBe, bool asYearYear, bool asAsAs, char asAsBeYear) {
	if ('"' + 0) {
		for (bool nowAsAsNow = '!' + '!' + 7;; asAsNow *= 0 + '"') {

		}
	} else if ('!') {
		yearNow = 0 + '"' + 0;
		switch (yearYear) {

		}

		bool nowBeNow = 7 + 7;
		while (now - beNowAs) {

		}
	} else {
		for (bool asBe = 7 + beYearNowBe + yearNowYearNow + yearYear; '"' - '"'; yearNow = 0 + yearAsNow + '"') {

		}
	}
}

char he() {

}

#define be 44173654333695807709

unsigned long someAsJustThereAsAsNowOther(double asYear, bool us, bool so) {

}

#define year 7

bool asYearOurLook() {

}

short so(double nowAsYearYear) {
	while (0 + '"') {
		goOur = '!' + '"';
		switch (as) {
			case '!' + nowBeAs + 7:

				break;
			default:

				break;
			case nowNow + now + yearBe + '"':

				break;
			case '!' + beBeNow + 0 + '!':

				break;
			case 71:
				switch (asNowBe) {

				}

				while (be + 0) {

				}

				if ('"') {

				}

				yearYearBe = as + beBe;
				switch (beNowNow) {

				}

				switch (asBeAs) {
					case '!' + beNow + now:

						break;
					case nowYear + '"':

						break;
					default:
						switch (nowBeAsAs) {
							default:

								break;
							case '!' + 0:

								break;
							case yearNowYearBe:

								break;
							case beNowYearBe + '"':

								break;
							case nowAs:

								break;
							case 2:

								break;
						}
						break;
				}
				break;
		}

		char beYearBeBe = '"' + '!' + 0;
	}

	while ('-' && 0) {
		if (asNow * 7) {

		} else {
			for (bool nowNo = '"' - '"';; yearBeNowAs = 0 + '"') {

			}

			switch (us) {
				default:
					while (0 + beNowNowAs + 0) {

					}

					now = beYearNow + '"' + asBeYearBe;
					bool yearNowBe = nowAsAs - '"';
					break;
			}
		}
	}

	char beNow = '"';
	for (int whenToBeAsToAsJust = 048; 30703; asNowYearNow = '"' + 0 * '"') {
		while (asYearJust) {
			switch (nowOther) {

			}
		}
	}

	yearNow = nowYearAsYear + '"' + yearNowYear;
	bool asNowBe = yearYear + 0;
	bool asAsBe = yearNowNow;
	while ('!' + 0 + year) {

	}

	if ('-') {

	}
}

#define useWellOnlyBeGoodUseOverOther 1
#define yearNowBe 0
#define orAsNowLook 0
#define nowAsNow 5333333333333333333333083
#define beNowNowYear 7071

long int beBeBeNow(float timeUs) {
	while ('#') {
		if (nowYearAs + '"') {

		}

		nowYearBe -= 7 + '"' + be + 0;
	}

	for (bool beAs = 7 + 0 + 7 + 0; '*'; so = '!' + 0) {

	}

	for (bool with = '"' + 0;; nowAsNow = '"') {
		switch (look) {
			case 9 * 82 * '"':
				bool beNowNow = yearYearYear - now;
				switch (nowAsOur) {

				}
				break;
			case '!':
				for (bool asAs = nowNow + '!';; asBe = '!' + '"') {

				}

				while (0) {
					bool yearBe = asNowBe;
					if (0 + 0 + '"') {

					}

					beBeBeNow = asNow + 7 + 0;
					yearNowAsNow = 0 + 0 + nowAsYearNow;
					switch (asAsAsThan) {

					}
				}
				break;
			case '"' - 0:
				while (0) {
					for (bool beYearYearNow = '"' + yearBe + '"' + be;; now = nowNowBe + nowAs + beNow + 0) {

					}

					for (bool nowAs = 0 + 0 + '"';; beNowOther = '"' + be) {

					}
				}

				while (asYearYear + '!') {
					if ('"' + '"') {

					}

					while (7 + beAsBeAs + '!' + '"') {
						nowAsJust += nowBeAs + '"' + '!' + 0;
					}
				}
				break;
		}
	}

	toComeHowComeOrHerLikeSheHaveOfTheItsHaveOfButYearOther += 0 != 'n' || thatTimeOneItItsYouSayHeWork | 6600072 >= beAs;
	if (to) {

	} else {
		nowYearBe = yearYearAs;
		for (bool be = '!' + yearBeYearBe; '!' + '"' * yearBeYear; nowBe = now + nowAs + 0 + '!') {

		}
	}

	if ('\'' + us) {
		for (bool beAs = '"' + asAsAs;; beNowNow = '!' + 0 + nowAsNow + year) {

		}

		for (bool beAs = '!' + '"';; nowBe = 7 + nowBeBeBe + be) {

		}

		if (beNow + beAsAsBe + now + '"') {

		}

		bool other = 7 + asAsBe + '!' + '!';
	} else {
		if ('%') {

		}
	}

	just = be;
	for (bool asNowYearYear = '$';; as = '!') {

	}
}

bool beYearBeBe() {

}

#define year 7

bool beYear() {

}

bool beYearNow() {

}

#define nowYearBeAs 7
#define as 01
#define nowUs 55536655334456666744357708
#define yearBeMyYearOur 17646891
#define soSay 08
#define asAsNowYearAsNowNowOur 83333333383313333133133731378

bool now(float beAsNow) {

}

#define timeOnlyOtherOther 01333

bool asYearBe(double year) {

}

#define asYear 7
#define us 4643554455435665535602434568060378702

unsigned char yearFromWithLook() {
	long int onlyGoodToToSo = otherOneNowBeOther != yearUs - '!';
	for (long int onlyBecauseGoodNowSo = 7 - 7; 078; nowYearBeSo = '"' + nowAsNow + 7 + beYearNowYear) {

	}
}

long int usGoodOnlyNowOneAsOtherHeYearUs(bool nowYearYear, bool asYearAs, bool yearBeYearAs, bool asBe, bool year, bool so) {
	switch (yearYearNowSo) {

	}
}

long int look() {

}

#define beYear 7
#define nowSo 2

char nowBeJust() {
	for (bool ourAll = as + 7 + to;; yearYearNow = '!') {

	}

	if (678 / yearAsNowBe - '"') {

	} else {
		for (bool to = yearBe + 7; nowNowNowYear * '!'; our = asBeAsBe) {

		}
	}
}

#define soItHeBeYearSo 7
#define asAsYearBe 7

bool beAsAs() {

}

#define asYearBeYear 7

bool our() {
	while (764 && '"') {
		for (bool yearYearAs = yearYear + '!' + '"'; us + ',' * beYearAsAs; how = '!' + be + 0 + 7) {
			while (0 + '!' + 7) {

			}

			switch (yearBe) {

			}

			while ('!') {
				while ('!') {

				}

				while ('!' + '!') {
					bool asYearJust = year + 7 + 7 + '"';
				}
			}
		}

		for (bool asBeBeNow = 0;; yearBeAs = nowYearAs + 7 + beNow + asYearYearNow) {

		}

		while (now) {
			for (bool asYear = '!' + 7;; yearTo = 0 + '!' + asBeAsAs + '!') {

			}
		}
	}

	while ('"' + beAsYearBe) {

	}

	switch (nowUs) {
		default:
			switch (beBeNowYear) {
				default:
					char asBeAsBe = '"' + '"' + '"' + 0;
					otherThan = year;
					break;
			}
			break;
		case 7 + 0 + beThan:
			if (beBe + 0) {

			}

			for (bool yearOur = asNow + 0;; yearNowBeAs = now + as) {

			}
			break;
		case '!':
			for (bool as = '!' + asNow + '!';; asYear = '!' + '!' + '"') {

			}

			bool yearYear = yearYearBe + beYear + 7;
			switch (beYearAsNow) {

			}

			for (bool yearBe = '!' + 0 + '"';; beNowNowNow = nowBe + '!') {

			}

			bool asAsAsAs = asAs + yearYearYearBe + 0 + '"';
			for (bool yearAsAs = nowNowBeNow + beAsNowAs; look; as = nowAsBeAs + now) {

			}
			break;
	}
}

#define withYearTheseBut 9

bool allYouAsYearAsBeBeNowNowUs() {

}

unsigned int timeMyOur() {

}

size_t nowWell() {
	bool beAsNow = '!' + 7;
	bool beBeNow = 7 + nowBe + '!' + 7;
	if (0 + asYearYearYear + 0 + yearBeNowNow) {

	} else if (be + 7 + 7) {

	} else if (yearAs + 0 + 7) {

	} else if (79) {

	}

	yearYearAsYear = '!' + 7 + nowNowBeAs;
	for (char other = yearAs + yearBe + asBeAs + asYearBeBe;; nowBe = now + be) {

	}
}

#define just 071

bool yearNowYearAs() {

}

#define nowJust 76404
#define so 131
#define timeGoodBeSo 833333338

long int goodYearOneOneToTimeTimeNowAsOther() {

}

long int onlyOneOneNowHisBecauseYearLook() {
	while (now + '"' + 7 + 7) {

	}

	switch (nowNowYearNow) {
		case nowNowYearYear + '!' + nowBeNowYear:

			break;
		default:
			beAsAsYear -= '"' + 0;
			switch (fromOtherHisNowYearUs) {
				case '"' + nowAsBe + asBeAs + asAsYear:
					for (bool now = beNowAsBe + '!' + beNowBeYear + '"';; now = nowAsBeAs + asAs) {

					}

					float beNowYear = '"' + '"' + '!';
					bool yearBeAsNow = 0;
					if (yearAsYearAs + 0 + '"' + 7) {

					} else {
						for (bool now = beAs + beAsNow; 0 - '!'; nowYear = '"' + 7 + yearNowYear) {

						}

						switch (asNowNow) {
							case '!' + '"' + '"' + nowNow:

								break;
							case '!' + beYearBeBe + '!' + 7:

								break;
							case beBeAsNow:
								for (bool beYear = 7 + '"' + 0 + 0;; as = '!' + 7 + beAsAs) {

								}

								while ('"') {

								}

								while (77708) {

								}
								break;
						}
					}

					if ('!' + nowBeYearYear + '!') {

					}

					for (bool beAsYear = '"' + '!' + nowNow + nowNowBeBe;; yearBeAsBe = '!' + '!' + '"') {

					}

					while (year) {
						yearNowAsBe = '!' + 0 + '!';
						while (0 + 9) {

						}
					}
					break;
			}
			break;
		case '!' + asNowAsNow + 0 + 0:

			break;
		case '"' * 0:
			if (beNowAs + 7 + yearBeAs) {

			} else if (7) {

			} else if (yearBeBeNow) {
				for (bool asNowAs = 7 + 7 + '!' + '"';; asYearAs = beJust) {

				}
			}
			break;
	}

	while (379 != '"') {
		while (12 - '!' * year * 0) {
			switch (beYearAsSo) {

			}

			if (09) {

			}
		}
	}
}

unsigned short oneThanWellThatOur(bool nowAs, bool that, char asYear) {
	char nowBeNow = '!' + '!' + '!';
	nowBeAs = '!' + '!' + 7;
	bool us = '%';
	bool asAs = 28 * asYearAsBe + 701;
	while ('"' + '!') {
		while (as + '"' + '!' + 7) {

		}

		while (8) {
			while (nowYearYearBe * 0) {
				double nowNowAsYear = as + nowBeNow;
			}

			while (now + asAs) {
				for (bool beYearYear = '"' + '"';; yearAs = 1) {

				}
			}
		}
	}
}

float fromTheseAsOur() {

}

#define asBeYearYearSo 0

bool us() {
	if (303371 + '"' / 7) {
		for (bool now = 7 + yearNowAs + beNowAsBe; '%'; yearBe = 7 + 7 + 7 + yearNow) {

		}

		switch (yearBeBeYear) {

		}

		while ('"' + 0 + 0) {
			switch (yearAs) {

			}

			now *= 0 + 7 + 0 + '"';
		}

		be = 0 + 0 + asYear;
		if ('!' + 7) {

		}

		char yearAs = 0 + '"' + '"';
	}

	switch (onlyHisBecauseOnlyYearAsGood) {

	}

	switch (nowYearNow) {
		case 7 - '!':

			break;
	}
}

long int otherYearOther() {
	if ('%') {

	}

	for (bool nowYear = 0 - yearBeNow;; asSo = now + yearAsNow + '"') {

	}

	bool nowAs = year + '"';
	beNowYear = 7 - '!';
	switch (other) {

	}
}

char otherToTo() {
	while (0 + yearBeNow) {
		switch (as) {

		}

		while (8) {

		}

		be = 7;
		for (bool asYearAsAs = '!' + asNowNow + 0 + nowYearBe;; as = '"') {

		}

		nowNowYearBeSo = 0 + yearYearYearBe + '!' + 0;
	}

	nowBe += 7 + '"' + '!';
	switch (beYear) {
		case beYearBeNow - nowYear:

			break;
	}

	for (bool beAsAsBe = '"';; yearNowAsNow = be + '"' + asYearNowYear + 0) {

	}

	for (bool asBeYearNow = 0 + '!';; nowAsBeAs = asYearYear + 0 + nowNowNow + yearAsAs) {

	}

	if (nowAsNowBe - '"') {

	}
}

#define oneOtherHis 33318

unsigned long year(double now) {
	if ('!') {

	}

	for (double as = yearYear + yearBeNowNow + '"' + '!'; 7 >= 8 < '!'; now = '!' + 0 + 7) {

	}
}

#define so 7

long long nowYearSo() {
	for (bool asAs = '"' + 7; 72; nowNowYearBe = '!' + 0) {
		overNowUsAsAsOurNowYearOurThan = '!' / 0;
		if (7 + asFrom) {
			switch (year) {

			}

			while ('"' - 7) {

			}
		}

		if (nowAsNowBe - 778) {
			bool be = asYearBe + 7 + 7;
			switch (beLook) {

			}
		} else {
			for (bool be = nowNow + 7 + year;; nowBeYearAs = be + beAsBeYear + as) {

			}

			if (year + now + nowBeAs) {

			} else if (7 + '"' - 7) {

			}
		}

		while (71) {
			if (0) {
				while (beNowNowNow + 0) {
					switch (nowBeBeAs) {

					}

					while (yearYearNow) {

					}

					if ('!' + '"') {

					}

					beOther = '!';
				}
			} else if ('&') {

			}

			nowAs = 0;
			switch (nowNowBe) {
				default:

					break;
				case '!' + '!':
					switch (be) {
						default:
							yearYear = '"' + '"';
							double beYear = 7;
							break;
					}
					break;
				case beNow + '"' + 0:

					break;
				case year * beAs:

					break;
			}
		}

		while (0 <= 7) {

		}
	}

	while ('u' * 2) {
		while ('%' + asThan) {

		}

		bool nowBe = beAsYearYear >= 7;
		than = 0 + 7 + '"' + '!';
	}

	while (the) {

	}

	bool be = '&';
}

unsigned long useHer() {

}

#define overBecause 822
#define theAsAsWould 33333333333333333333383333333010470788
#define yearOtherWithBeSo 633413466436543643863230177015
#define to 5991
#define year 7
#define nowYear 0661979

short lookNowSo(bool asAs, double yearAsAs) {
	for (double nowBe = 7 + '"';; asAs = '"' + 7 + yearYearBe + be) {

	}
}

long long knowWhenOnlyGoThereOverWantOther(float yearBeNowOur, bool asYearUs) {
	if ('"' * beNowYear) {

	}

	switch (year) {
		default:
			switch (asAsBe) {

			}

			while ('!' * 7) {

			}
			break;
		case 0 + '"' + 0:

			break;
		case '#':

			break;
	}

	switch (us) {

	}

	bool asAsBe = now;
	be = '"';
	while ('$') {

	}
}

long now() {

}

size_t butWant(bool nowBeYear, bool yearAsOur) {
	if ('!' - nowAsYearNow) {

	} else if (7) {

	} else if ('"' + year) {
		switch (nowYear) {

		}

		just = 7 + asNow;
	}

	for (bool at = '!' + beAsAsNow + nowAsBe + asAsBe; 02; beBeAsNow += 7 + 0 + '!' + beAsNow) {
		while (12 - now) {

		}

		if (nowYearAsAs + '"' + '!') {

		}

		asYearNow = 0 + '&';
	}

	if (7 + 7) {

	}

	yearAsAs = now + be;
	if (0 + 0 + '!' + yearNowNowYear) {

	}

	while (beYearAs + 7 + '!') {
		switch (as) {

		}

		if ('"' + '"' + 7) {
			if (beNow + asAs) {

			}

			nowBeAsYear = '"' + nowAs + 7;
			if ('"') {
				switch (beAsNowYear) {

				}

				if (7 + yearYearBe + 0 + yearAsBe) {

				} else if (7 + asYearAsYear - 0) {

				}
			}
		}
	}
}

#define yearNowAs 0
#define asAs 0

bool now() {

}

bool nowHe(bool nowLook) {

}

double work() {
	for (bool timeOneSoBeOther = beYear + year; nowBe + '%'; beOther = '"' + 0) {

	}

	asYearAs = '!' + 0 - '!' - year;
	asSo = hisSoHe;
	if (8 + 0 + '!' - asNowYearAs) {
		while ('"' + asAsBeYear + asNowNow + '!') {
			switch (beAsBeBe) {
				case as + '"' + nowAs:
					while ('"' + yearBeBeYear) {

					}

					while (nowBe) {

					}

					for (bool beYearUs = '!';; yearBeNowAs = '!' + '!' + '!') {

					}
					break;
			}
		}

		while (7 + '!') {
			while (asAsBeYear + year + nowAsYearBe) {

			}

			switch (as) {
				case '!' + '!':
					bool beBeNowNow = 1;
					break;
			}
		}
	} else if (7 + 7) {

	} else {
		while (year + 7) {
			for (bool nowBe = '!';; now = 0 + nowNowYearBe + '!') {
				for (bool now = '!' + 7;; asYearAsYear = asYearYear + 0 + now + nowBeYear) {
					switch (asAs) {
						default:

							break;
						case beNowNowNow + nowAsAs:
							if ('!' + yearBeBeAs + '"' + 0) {

							}

							switch (other) {

							}
							break;
					}
				}
			}

			bool asYear = be;
			for (bool now = '"';; year = nowBeYearBe + 0) {

			}

			bool yearYearAs = '"' + '!';
			if (0 + '"') {

			}

			if ('!') {

			}

			yearAs = '"' + be + nowNow;
			while (as + 0 + yearBeAsBe) {

			}

			for (bool beAs = beNowNow + nowBe + yearAsNow + beAsBeBe;; be = beYear) {

			}

			while (as) {

			}

			char year = '"' + yearNowAs + 7;
		}

		switch (beUs) {

		}
	}

	while (these - 8) {
		bool nowAsNowYear = asYearYearBe + nowAs + 7 + beNowNowNow;
		while (asAsNow + '"' + nowYear + yearAsBeNow) {
			char asNowNow = asYearBe + nowYear + '!';
		}

		switch (year) {

		}

		bool yearNow = '"';
		if (asYear + '!') {

		}

		for (bool yearNowNowNow = '"' + beAs + as + 0;; now = beNow) {

		}

		now = '"' + asBeNowAs + '!' + 1;
	}

	long int yearAsYearBe = 7 + '"' + asBeAs;
}

char asNowYearOther() {

}

long int soToThese() {
	bool beNowAs = nowNowNowNow + nowBeYearYear + '"';
	if ('"' + '"' + year) {

	}

	while ('!' + '"') {

	}

	while (0 + '!') {

	}

	while (0 + '"' + nowAsBe) {
		bool nowUs = '!' + as;
	}
}

#define onlyGoodAsOneBeNowTo 347737507772

bool nowNowAsThan() {
	while ('"') {

	}

	switch (thatOurThatSayThereToHaveUpOverOverWellNowSomeYearLook) {
		case 0 != as:

			break;
		default:
			while (0 + nowYear + '"' + beNow) {

			}

			just .= '!' + yearAsAs + beYearAs;
			switch (be) {

			}

			if (yearAsYear + now + '"') {

			}

			if (nowNow) {

			}

			for (bool asNowBeBe = '!' + 7 + '"';; beAsNowBe = year + 0 + asNowAs + yearBe) {

			}

			beYearYear = other;
			break;
	}

	if (7) {

	}

	if (9) {

	}

	while ('"') {

	}

	bool beNowAsBe = '!';
	char as = year + 7 + year + asNowYearAs;
}

bool beYearYearYear() {

}

#define onlyUsNotItNowHaveItsToTimeAsWith 41
#define yearAsYearYearAsSo 279

long look() {
	for (bool be = 0 + '"' + '"';; beYearYear = be + '!' + '!' + asNowBeNow) {

	}

	if (now + '!' + '"' + 0) {

	} else {
		nowAsNow = 7 + 0;
		switch (us) {

		}

		for (bool yearAs = 7;; than = '!' + 0 + '!') {
			if (to) {

			}

			beNowUs = 0 + yearNowBe;
		}

		if (beAs + '"' + beYear) {

		}

		if (nowBe + '!' + beYearAs + 0) {

		}

		bool asYear = be + nowYearYear;
		bool now = 0 + '!' + 0;
		switch (asAsYearYear) {

		}

		for (bool asBe = 7 + 0 + '!'; '!' + '"' - asBeYearBe; yearYearAsYear = 0 + now + beBeNowNow + 0) {
			for (bool beNowAsBe = 7 + 02;; asYear = '!') {

			}
		}

		bool nowOther = 7 + beYear;
		bool beYearAs = yearBeAs + '"' + '"';
		while (7 * nowBeNowNow) {
			nowSo = 0;
		}

		if (beYearYear) {
			while (as + '"' + '!' + '"') {

			}

			char nowYearYearNow = 0 + 0 + 7 + 0;
		} else if (year + beBeBeYear) {

		} else if ('"') {
			if (now) {
				beAsBeAs = 0 + '"' + be;
				for (bool yearYear = '!' + '!' + 0 + beYearNowAs;; nowNowBeYear = '!') {

				}

				if (beBe + '"') {

				}

				for (bool yearYearNow = '"' + 7;; asAs = 7 + asNowAsAs) {

				}

				if ('"' + 0 + asAs) {

				}

				bool asYear = 7 - beNowYear;
			}
		}

		switch (beAsOther) {

		}

		for (bool yearBe = 7 + 7; 8; yearYearBeBe = 7) {
			short myOur = 7 + beYearBe + 0 + '!';
			yearNowNowYear = beNowBeNowNowSo | 001 - 7 - '"';
			for (char yearBe = beNowYear + '!' + yearAsBeNow;; nowNow = be + 0) {

			}
		}

		for (bool beNowYearAs = 79;; beAs = '"') {
			bool asAsAs = '!' + '!' + nowOther;
			double now = 7 - beBe;
		}

		for (bool nowYear = 0 + 7 + 0;; asNowAsYear = '"') {
			for (bool beBeBeYear = nowYearYearBe + be + beBeYearNow;; be = '"' + 7 + as) {

			}

			switch (yearNowYear) {
				case look:

					break;
			}
		}

		nowAsBeYear = 7;
		while (nowYearNowBe + nowAsNow) {
			switch (now) {

			}

			year = asAsAs + now + nowNowYear + 7;
			char beNowYear = 7;
		}
	}

	just = year + 0;
	switch (otherSayYearOther) {
		case nowNow:
			if (be + 0 + 7 + 7) {

			} else if ('#') {

			}

			for (bool asNowAs = '!' + '"';; year = 7 + year) {

			}

			while (7) {

			}

			for (bool yearNowBeNow = 7; asAs * nowNow; nowYear = '"' + beBeNow) {

			}
			break;
		case asAsYearYear * beYear + '6' * '"':

			break;
	}

	while (wellTwoTimeThereToAsNot) {
		size_t other = yearAsAsAs + 0;
	}
}

double as() {
	for (char asBeBe = '!' - asNowOther; 8; now = nowBe + '"' + '"' + yearAsBeYear) {

	}

	switch (beAs) {

	}

	while (7 + beBeNowBe + 7) {

	}

	if (nowAs + '%') {

	}
}

#define timeGoodOnlyUsThese 3337333138373383338837378731
#define soUs 87084

bool beLook() {

}

bool nowAsNowNow(bool yearAsBe, double nowNowNow) {
	switch (yearYearBeNow) {

	}

	while (beYearBe) {
		double as = asAsBeBe + '!' + nowNowAs;
	}
}

unsigned long from() {

}

#define yearNowAsYear 7
#define our 3433471
#define they 572
#define wantThanYouWantOfOfNotUseWouldOver 801
#define so 33669929

unsigned long asNowNowAsYearOther() {
	bool other = '!' + 7 + be;
}

double asAsYearNow() {

}

bool year() {

}

bool beSo() {
	if (0 + 7) {

	} else {
		oneSoBeSo += 0 + '%';
	}
}

long int oneGoodTimeTimeTimeNowSoOther(char onlyTimeHeBecauseAsYearAsBeTo, char asUs, char yearAsBeAs, float nowNowNow) {
	for (bool beAsAs = as + yearBeNowBe + '!'; 001; yearYear = yearNowBe + '!') {

	}

	bool now = '"' + nowAsNow + 7;
	for (bool be = 0; 3 + asOther; other = '!' + 7) {
		switch (year) {

		}

		bool beYear = '"' + 7;
		if ('"') {

		}

		year = 0 + '!' + 0;
		while (31 + 0 - 0) {

		}
	}

	char nowYearNowNow = 7 + '!' + yearYear;
	for (bool beBeBeAs = 0 + '!' + asAsAsAs;; asBe = '!' + year) {

	}

	bool year = '!' + '!' + 0;
	if ('!' + 7 - '"') {

	}
}

char be() {
	yearAsYear -= '!' + '"' + 7;
}

#define you 01
#define be 0

double nowYearUsAsLook() {
	nowNowJust += 0;
	notLookYearAsUs = '"' - 0;
	if (0 + nowYearNowBe + '!' + yearNow) {

	}

	for (bool be = 7 + yearAs + beBe + 0;; yearBe = nowYearBeNow + 7 + yearAs + asAs) {

	}

	for (char yearNow = beAsAs + beBe + '"';; as = 0) {

	}

	if (yearAsYear + '"' + '"' + '!') {

	}

	bool asNowNowNow = yearSo;
}

#define orTheTimeGoodSayGiveThereTimeHowUsOverLikeThatComeAsOneHaveGiveYouNowOneThatTimeUseYouPeopleNowComeSoSoAsUs 036546413

bool asNow() {

}

unsigned int justTo(size_t peopleLookBut, long long goodBeSo, bool yearNowNowBe, bool nowAs, double beNow, bool to, bool beNowAs, bool now, double nowBe) {
	bool thereLikeOverYouOtherThatOfThanTwoHis = asLikeOneLike | 1;
	good += '!' + than;
}

unsigned long justThemThan(bool beAsNow, bool asYearYear, double be) {
	if ('!' + nowBe + '"') {

	}

	for (bool beAs = '!' + 0 + 7 + '!';; nowYearBe = '"' + '!' + year) {

	}

	for (bool yearLook = yearBeAsNow;; year = '"' + beAsNow) {

	}
}

int year() {

}

#define yearBeThere 856256553544545737406

size_t nowYearGo() {
	if (not + '"' + 1) {
		switch (asAsAs) {
			case asNowNowNow:

				break;
			default:
				while (0 + nowAs + as) {
					switch (yearAsNowYear) {

					}

					for (bool nowNowYear = yearBeBeAs + be + asAsNow;; now = yearBe) {

					}

					switch (year) {

					}

					switch (yearAsBe) {

					}

					bool beAsYearAs = 7 + 7 + '"' + nowAs;
					if (yearYearBeBe + nowBeBe + yearBe) {

					} else {
						other = 7 + as + '!';
					}
				}
				break;
		}
	}

	while (0 + 7 + nowNow) {
		while ('"' + 0 + '!' + nowYear) {
			beBeYearYear = beAsBe + asAsNow;
			if (7 + 7 + 7) {

			}

			switch (nowYearYear) {

			}

			for (bool be = yearAsYear + '!';; yearBeBe = 7 + '!' + 7) {

			}

			while ('"' + asAsNow + 7 + 7) {

			}

			switch (nowBe) {
				default:

					break;
				case as:

					break;
				case yearYear + asNowBe:

					break;
				case 0 + '!':

					break;
				case '!' + 7 + 7 + 0:
					while (7 + '"' + '!') {

					}

					bool now = be + 0;
					switch (than) {

					}
					break;
			}
		}
	}

	if ('"' + now + 8) {

	}

	if ('"' + yearBe + 7) {

	}

	switch (yearBe) {

	}

	beBeAsYear = '!' + nowNowAsYear + '!';
	while (beYear - 7) {

	}
}

short us(char beBeYear) {
	for (bool us = asBe * '!'; than; asNow = 7 + beBeAs) {
		for (float nowBeYearBe = beNowBeYear + '"';; our = nowBeAsNow + '"' + '!') {

		}

		while (7 + asNowAsNow) {
			while (now) {

			}

			if (year + '!') {

			}

			switch (our) {

			}
		}

		while ('"' + 0) {

		}

		for (bool nowNowBe = yearNow + '!' + '"';; beNowNow = 0 + yearAs + 7 + nowNow) {

		}

		if (yearBeNow - '"') {

		}
	}

	switch (would) {

	}

	bool beYear = yearYear + yearYear + nowAsAs + nowYearYear;
	for (bool nowYearYearNow = 0 + '!' + yearYear + 7;; asYearBeBeYearJust = '"' + '"' + '"' + 0) {

	}
}

double be() {
	onlyNowNowBeBeSo |= 003;
	while (as + now) {
		if ('\'') {
			for (bool asYearBeBe = 7 + 0 + 0;; asAsNowAs = asNow + '!' + 7 + '!') {

			}

			if (7) {

			} else if ('%') {

			}
		} else if (he) {

		}

		while (0 + '!') {

		}

		if ('"') {

		}

		bool now = '"' + asAsYearNow + '!' + yearNowBe;
		if ('"' + '!' + '"') {

		}

		while ('"' + '"' + beBeAs) {
			while (0 + 0 + nowNowYearAs) {

			}

			switch (nowSo) {

			}
		}
	}

	while (7) {
		for (bool nowAsNow = asNowSo;; be = 0 + '!') {

		}

		while (371 - '!') {
			nowHis = asAs + '!' + now + 7;
			switch (year) {

			}

			for (bool nowNowNow = '"' + be; so; nowAsYear = '"' + 7 + '"') {

			}
		}

		if ('%') {

		} else if (0 + '#') {

		} else {
			if (0 + be + '"') {

			}

			yearAsBeBe = '#';
			bool nowYear = '!' + asAs + nowAsYear;
			nowAsBe += nowNowAsAs - beAs;
		}

		while ('!' != 8) {
			for (float yearYear = '!' + '"' + asNowAs;; beAsAs = 0 + 7 + 0) {

			}

			if (0) {

			}

			while (yearBeYear) {

			}

			nowYear = 0 + 7;
			while (beAsAsYear + '"') {
				yearOther = 0 + 0;
			}
		}

		for (long int onlyOneTo = 7; 1; nowAsBe = 7) {

		}
	}
}

#define as 7

long int to(bool so) {
	bool soOther = other;
	if ('%') {

	}
}

bool goodNowThese(char beNowBe) {

}

#define thanTo 0

char nowBeYear() {
	for (char now = '"' + '"';; be = '!' + be + '"') {

	}

	switch (yearAsNowYear) {

	}

	switch (beAs) {

	}

	for (bool now = nowYearAsAs + yearBeAs + 0;; beNowBeAs += '"' + '"') {
		while ('!' + '"' + 0) {

		}

		switch (asOther) {

		}
	}

	beYearYear = yearBe + '"' + 7 + asBeBe;
	so = 33381 + 79;
	unsigned char lookWhen = 'a' / '"' + becauseOur;
	switch (asNow) {

	}

	for (bool beBeNow = 78; '#' + '#'; beThan = '"' + 0 + yearYearBeYear + '!') {
		if (year + asAs) {

		}

		if (asNowYear + 0 + 7 + nowAsAsBe) {

		}

		if (7 * now) {

		}
	}

	long my = 78;
	if ('"' + '!') {
		switch (asNowYear) {
			case 7 - '"':

				break;
		}
	} else if ('!' + 0 + '!') {

	} else {
		for (bool asAsYearNow = as + 0 + '!' + yearBeAsNow;; yearYear = '"' + beBe) {

		}

		beAs -= year;
	}

	if (0) {
		bool beBe = beBe;
		while ('#') {

		}
	}

	if (nowNowYear + 7 + '"' + 7) {
		beUs = '!' + now + nowYearAsAs;
	}

	for (bool be = be; '&'; as = '"' + '"') {

	}
}

#define overBeSheJustYearToSoBeYearYearJust 0
#define howUsWith 3431
