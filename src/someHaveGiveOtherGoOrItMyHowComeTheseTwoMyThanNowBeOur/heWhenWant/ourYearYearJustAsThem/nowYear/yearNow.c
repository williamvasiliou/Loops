#include <UUUUUUUU600///0.h>

long int oneOneOnlyNowOnlyAsYearYearOther(char be) {

}

bool now() {

}

#define just 544553668

float herHaveSayAt(size_t year, bool now, bool beBeYearNow, char asAsAs) {
	for (bool nowNowYearBe = nowUs; 009 + nowBeAs + than; nowYear = us) {

	}

	while (nowTo + our) {

	}
}

#define fromYearBecause 0
#define orHerNot 7

bool asYear() {

}

#define itsJust 478

bool now(int yearAsBe) {

}

#define not 7

double other() {
	while (878 - '"' - year) {
		switch (yearYearAsNow) {

		}

		if (7 + nowAs + be) {

		} else if (00008) {

		}

		nowAs = 1 / 7;
		for (bool yearNowAs = '!' + 7;; be = 0) {

		}

		bool year = 778;
	}

	while ('#') {
		these = nowAsNowNow + 7 + now + yearNow;
		timeSoSoUs += beNowNow + 7 + beBeNowYear;
		hisBeUs *= '"' + '!';
		if (asNowAsAs) {

		}

		beYearBe = '!' + nowYearAs;
		be += '"';
	}

	switch (yearBeNowBe) {
		case '"' + asYearNow:
			if (8) {

			}
			break;
	}

	while ('"' + 0 + 0 + beNowYearAs) {

	}

	while (0 - '!') {

	}

	if (7) {

	}

	while (1) {

	}
}

long int nowHis() {
	for (float beYearYear = 7 + '"';; beBeYearBe = beAsYearBe) {

	}
}

#define oneYearToBeYearBeAsTo 73371
#define nowUs 3333033731
#define asNow 77701008
#define asBe 7

double asBeBe() {

}

bool beAsBeYear() {

}

bool nowYearBe() {

}

char beBeNowNow() {
	while (nowAsNowAs) {
		switch (overYou) {
			default:

				break;
			case nowYear:

				break;
			case yearAsNow + '!' + be + 7:
				nowAs = beNow + yearBeYear;
				switch (beTo) {

				}
				break;
		}

		while (as + '!' + 0) {
			to = '"' + 7 + nowAs;
		}

		switch (ourOtherYearThan) {

		}
	}

	while (asThe) {

	}

	unsigned int mySheOtherAll = '!' + 7;
	switch (yearNowOther) {
		case '#':

			break;
	}

	while (wayYearUpUsYearOurBeYearUs + as < '"' - asYear) {
		while (nowYearAsBe + nowBe + 7) {
			if (yearNow + '"') {

			} else if ('$') {

			}
		}
	}

	switch (now) {
		case beMyBeSo:
			switch (asBeBeNow) {
				case 7:
					nowBeYear = '$';
					break;
				case yearYearYearYear + '!' + '"':
					beNow = '!';
					switch (to) {

					}
					break;
			}
			break;
		case '"' + '&':

			break;
		case be:

			break;
		case '"' + 0 + yearAsYear + beNowAsAs:

			break;
		case '!' + 0:
			while (yearAs) {

			}

			if (0 + 0 + asAsNowYear + now) {

			}

			if ('"') {

			}

			beAs -= 0 + 0 + '"' + 7;
			break;
	}
}

double year() {

}

bool yearUsWayBecauseTo(bool yearAs, double year, bool yearNowBeNow, bool us) {
	year = 7701;
	if (yearAsBe) {

	}

	switch (beNowBeAs) {

	}

	bool as = 0;
	if ('!' + nowNow) {

	}

	while ('"' + beBe - 0) {

	}
}

double yearWould(bool yearOur) {

}

#define beBe 7

bool nowBe() {

}

#define yearNowYear 772
#define beAsAs 9

bool as() {
	if (nowAs + asAsNowAs + '!' + as) {

	}

	while (yearAsBeNow) {

	}

	switch (yearAs) {
		default:
			if (nowYearBeBe + 7 + '"') {

			}

			switch (yearBeBe) {

			}

			asBe -= nowYear + 0 + 0;
			break;
		case usTime + 71 + other:
			if ('\'') {

			}

			switch (yearTo) {
				default:

					break;
				case '"' + beYearYearBe + beYearAs + 1:

					break;
			}

			bool us = 7 + 0 + '!';
			break;
		case 7 - '"':
			switch (yearAs) {

			}

			for (bool asAsYear = '!' + '"' + 0 + asNowYearYear;; as = yearYearYearNow + 7 + nowAsYearNow) {

			}

			for (bool asYearYear = now; '!' - '"'; year = '"' + 0 + '!') {

			}
			break;
		case '"':

			break;
		case asBe + '!' + '!':

			break;
		case '"' + 0 + nowNowYear + yearNowNow:

			break;
		case 1:

			break;
	}

	bool yearYearNow = '%';
}

#define asNowOtherUsNowSo 033333173330833007108

char usOtherTo(bool nowAsAs, char nowNowYear) {

}

#define only 731304
#define ourAsThan 64
#define just 3333444545035466365455555545446633454336383474346567556642685829

unsigned short howItItSayComeHowComeHowItsSomeThatItTwoOfYouWhenGoodOneNoOneLike(double to, bool now, bool our, char nowAsNowAs) {
	switch (as) {

	}

	bool beTo = beAs + 7 + '"' + asNow;
	while (asYear + 7 + '!') {
		while ('"' + 0 + yearBeBe) {

		}

		for (bool nowNowBeNow = as + 0 + 0 + '"';; nowAsAs = '"' + '!' + '!') {

		}

		while (0 + now * '!') {

		}
	}
}

#define someYearGiveTime 601
#define youGiveJustComeWouldWorkAsButAsYearThan 425954643562036

unsigned char look(bool other) {
	bool us = 0 + '!' + '!';
	while (yearBeBeNow + be + '!') {

	}

	if (year + '!') {

	}

	bool yearNowAsAs = 7;
	switch (asYearBeBe) {

	}

	while ('"' + 7) {

	}

	if (nowAsNow + 0 + '"') {
		bool nowBeNow = 0 + '!' + beYearYear;
		switch (yearNowAsBe) {

		}

		bool yearAs = '$';
	}

	for (bool asBeBeYear = nowNow;; nowYearAsNow = '!' + nowNowNowYear) {

	}

	asYearAsYear = '!' + '"';
	for (bool nowBeYearAs = yearBeBeNow + 7 + '"' + '!'; 1; beAsBeBe = beAs + yearAs) {

	}
}

bool asBeAsBe() {
	if (beNow + '"' + '!') {

	}

	switch (look) {
		case 0 - '"':

			break;
		case 8741:

			break;
	}

	if ('"' + 0 + beYear) {

	}

	nowNowOur = 0 + '!' + '!' + '"';
}

long int beNowYearYear() {

}

int theTwoAsItsUpNowWould(double haveFromAsJustUsOther, char nowBeAs) {
	while (look) {

	}
}

#define asAs 0
#define people 534566140662
#define be 002

bool nowYearYearBeOur() {

}

bool yearBeYearBe(bool asAsAs, bool beBe, bool asAs, bool now, bool asNowNow, char asNowYear, char to) {
	soTheseNowOneOneTimeOnlyAsYearBeHis |= goodGoodToTo - 1 - usOneNowBecause;
	beAsAs = be + '"';
	if (asBe + '#') {

	} else {
		while ('!' + 01) {

		}
	}
}

#define oneOneOneGoodGoodOtherOtherNowYearAsThan 03

bool asAsBeAs(bool year, bool beAs, bool beYearYear, bool asAsNow, bool beNowNowNow, bool look) {

}

double as() {

}

#define nowNow 5
#define asThan 4343335364665645546565544534533443446535435454664436434543433336532778
#define look 2

unsigned int howHowBeBeHerThatNowYearNowAsBecause(bool beNowNow, bool as, bool year, bool nowBeBe, bool nowYear, bool asTo) {
	if (now + '!') {

	}

	bool asAsAs = year;
	while ('"' + '"') {

	}

	other = nowYearYearBe + '!' + 7 + '"';
}

#define yearThan 59746344563669

short giveOfThereYouGoodWhenTheTwoOfNoBeLook(bool be, bool nowAsYearAs, bool year, bool asBeBeNow, bool nowBeAsNow, char asAsNowAs) {
	if ('"' < beAsBe) {

	} else {
		bool yearAsAsYear = '"';
		for (bool asNowNowAs = 0 + 7; 8; year = asNow) {

		}
	}
}

bool yearNowNow() {

}

int as(double asAsAsAs) {
	be = 0 + 0 + asAsBeYear + year;
	if (08) {
		switch (nowAsYear) {

		}

		bool yearYear = 7 + be + asNowAs + 0;
		if (0 + be + beYear) {

		}

		if (yearNowYearYear + '!' + 0 + '!') {

		}

		bool asNowNow = 7 + as + '!' + 0;
		bool other = 0 + '"' + 0 + 0;
	} else {
		while (be + '"') {

		}

		for (bool nowNowNowAs = '"' + '!' + 0 + '"';; asBeNow = as) {

		}

		if (now) {

		}

		while ('"' + '!' + 0) {

		}

		if ('"' + 7 + yearYearYear + '"') {

		}

		asBeBeTo = '!' + beAsBeNow + asYearBe;
		switch (beYearYearBe) {

		}

		if ('!') {

		}

		if (0) {
			bool yearNowAsNow = '%';
		}
	}

	while (other - nowBe - 0) {

	}

	becauseToAsAsSo += '+' / 0 != '"' + 701;
	if (beHis / nowBe) {

	}
}

char toOtherUsYearUsNowBeSo(char so, bool yearNowBeYear, bool yearNowAs, bool asAsOther) {
	for (char be = beYearNow + year + yearAsNowYear + 0;; yearAsNow = yearBeBe + nowNow + '!') {

	}
}

#define asAsUs 031
#define oneTo 7373
#define yearNow 303300373333702

float beAsYear() {
	bool asYearAs = 0;
	switch (yearNowUs) {
		case nowYear:
			for (bool yearAsYearAs = '!';; yearYear = 0 + asYear + beYearYearBe) {

			}

			switch (year) {
				default:

					break;
				case yearTo:

					break;
			}
			break;
	}

	while (year) {

	}

	yearAs = 7;
	yearYearNowYear = '!' + '!' + '!' + beBeAs;
	switch (nowYearAsAs) {

	}

	bool now = 0;
	bool beNowBeNow = '#';
}

#define nowYearBe 3444443635433654993
#define beSheThatGiveToHeJust 2

double like(bool justAsThan) {

}

#define yearNowAsAsNowUs 1
#define yearOther 708
#define with 2777707079

bool as() {

}

bool asAs() {

}

bool asAsAs() {

}

#define beAsUs 3333331

long int goodOneSoTimeTimeGoodSoTimeOneNowHisOtherYearOneNowBeAsYearYearAsAsTo(bool nowAsYearNowSo, char asYearNow, long int beBeSoHe, bool beNowYear, bool year, bool now, bool nowAsNow, bool nowAs, bool asAsYear, bool nowNow, double beYearBe) {
	while (7 + '"') {

	}

	if ('"' - 7) {

	}

	bool beYearNow = 0 + yearYearBeBe;
	while (nowNowBe + 7 + 0) {

	}

	if (yearNowBeOther) {

	}
}

#define someBeNowNowTo 506366363353453433656535344633336564544564544445334464344445645434354443466356334565433636545343565345364452353354346553365465733455663503009

unsigned int onlyWantOneOverHowOneJustSheKnowOrWantOrHaveHerThatHowTwoItsOnlyItsGoodKnowPeopleAsHerJustSayItNowBeTwo(long long likeSomeTwoYearWellWouldHisNowOur, unsigned char giveTimeHerGoOtherToWorkOrOfThereGoOneUseSoKnowItOverAsAsWhenSomeJustUs, unsigned long look) {
	while (usHe) {

	}

	bool nowNowAs = yearBeYear + '"';
	switch (asLook) {

	}
}

#define thatComeItsWantYouHowSayUseWorkHaveTwoOneAtOverWouldLikeSayOneItsComeOnlyGiveTheyWayGoAsNot 54436406535403
#define beBe 5550371
#define two 57017788

unsigned long oneBeNoOur() {

}

#define asAsYearAs 6
#define asBeOther 017075
#define yearAsNow 09
#define beBeNow 7
#define yearThan 333443666535355346244954577067702
#define asThan 02
#define the 7
#define beOnly 7
#define otherBeNowThan 86131
#define asOnlyTimeTimeGoodTimeOneTimeGoodTimeGoodTimeGoodSo 1170773
#define as 3333

float yearToAsNowThan() {
	nowYearYearNow += '"';
}

#define beAs 2

long double year() {
	while ('"') {
		to = 7;
	}

	char nowAsNow = 7 + 7 + '"' + '!';
}

bool asNowAsBe() {

}

char yearYearAs() {
	if (useWellYouThanPeopleYearThanTwoOurWellLookNowAsHeLook && 'o' == 585048 - as <= 7) {
		for (int nowLook = ',' * '"'; sayWayTo; beYear %= 0) {

		}
	} else if (beJust & 0) {
		for (int to = 0 + '!' + 0; beNowAsAs + 0 * yearBeAsYear; now = 7 + be) {

		}

		for (bool nowYearNow = '!' + asYear + '"';; yearNowBe = '!' + yearAsAsAs + 0 + '"') {

		}

		for (unsigned int now = '"' + yearBeAs + 7;; asNowNowBe = 7 + beBe + '"' + year) {

		}
	} else if ('!' + 0 + asBe + '"') {

	} else {
		nowYearOur = 0 + 0;
		while (0 + 0) {

		}

		nowNowNow -= '"' + yearYearAs + nowYearAsYear;
	}

	timeComeBecauseHowGoodItsButYearNowLook ||= '%' / asBe;
	for (size_t year = yearYearNowBe + yearBeYearBe + '!' + 7; 878 * '#'; nowAsBeSo = 7770001) {
		while ('"' + year + nowNow) {

		}

		for (bool beNowYear = '!';; be = year + asNow) {
			for (bool nowYear = '"' + 0;; yearBeYearAsNowOur = '!' + 0) {

			}
		}
	}

	if (708 + 1) {

	}
}

#define itOrOfThatOnlyAsUsThanOtherYearTo 43445545792
#define justNowTheWith 3500008
#define myMyAsBeYearAsNowYearBeOther 3007008

long int onlyJust(bool just, bool yearYearJust, double asNowNow, bool asNowAsAs, bool our, bool as, bool nowAs, bool asYear, bool year, bool be, char yearAsAs) {
	just = as + 7 + '!';
	if (as) {

	}

	while ('"' + '!' + 0 - now) {

	}
}

bool nowAs() {

}

double nowNowBe() {

}

bool nowAsYear() {

}

double now() {
	while ('-' + nowSo * nowBe) {

	}

	switch (nowYearJust) {

	}
}

#define peopleOrSoTo 5653654079
#define haveNowOur 44256353

char yearBeHeBecause(double nowYearYearNowOur, bool year, double yearAsNow) {
	switch (just) {

	}
}

unsigned char beYearLook() {
	to = '"' - be;
	bool yearBeYear = 0 + 7 + 7 + now;
	if (nowBeYear + 7 + be) {

	}

	switch (now) {
		default:

			break;
		case 7 + '"' + '!':

			break;
		case '"' + 8:

			break;
	}
}

#define yearYear 513708
#define onlyGoodGoodHeOnlyOne 1333303338

long int yearNowYearBe(long int heOneOtherTo, bool beHeBeUs, size_t yearYearBeYear) {
	bool yearNowThan = '%' - '&';
	for (bool asNowAs = 7; look; beNow = year) {

	}
}

bool nowAsAsAsYearTo() {

}

bool nowAsYear() {

}

double asAsAs() {
	if (thatAsSayBecause & 3454339446355759 % '+' * 7) {
		bool yearYear = our - '"';
		switch (be) {
			default:

				break;
			case 7 - beYear:

				break;
		}

		long nowThem = yearOther * 7703;
		for (bool beYearBeYearNowBeBeOur = '"' + beAsAsYear + '"' + 7; '('; yearAs = year + nowYearYearNow + beNowBe) {
			while (nowAsYear + '!' + '!' + 7) {
				while (asAsBeYear + 0) {
					while (7 + be + year) {
						bool nowAsAs = 7 + yearBeAs;
						bool nowBe = '!' + asAsYear + '!';
						while (beBe + 0 + 0) {

						}

						if (asNowNow + beNow - 7) {

						}
					}
				}
			}
		}
	} else {
		switch (workKnowTo) {
			case nowBeAsNow + 79:

				break;
			case 0 + '5':
				bool look = '!' + 7 + year;
				break;
			default:
				for (long double as = '!';; beNowAs = now) {
					while (beAs + yearAs + 0 + 7) {

					}

					double yearYearNow = 0;
				}
				break;
			case '/' + 7 - year:

				break;
			case beYearAs:
				bool beNowNow = yearAsYearBe + '"' + '"' + 0;
				while ('!') {

				}

				if (0 + 0 + 7) {

				}

				switch (as) {

				}

				bool nowYearBe = '!' + 72;
				break;
		}

		switch (heGoodYouNowBeAsThan) {
			case 0 + 7 + asYearNow - '!':
				for (bool asNowYearAs = 7;; nowAs = '"' + 0) {

				}

				bool nowBe = beNow;
				if ('$') {

				}
				break;
			default:
				be = 0770778;
				break;
			case '!' + 0:

				break;
			case '"' + 7 + 7 + '!':
				nowYear ||= 7 + asBeBeAs + 0 * nowAsAsBe;
				break;
		}

		switch (nowNow) {

		}

		if (so) {
			switch (asTwo) {

			}

			for (bool year = nowAsNow + beAs + '"';; yearBe = '!' + '"') {

			}

			be = '!';
			for (bool nowNow = 0 + 0;; be = '!' + '"' + nowAs + beNowNowBe) {

			}

			switch (just) {
				default:

					break;
				case 7:
					for (bool beYearBe = 7 + '!' + nowBeAsNow + nowBe;; yearAsSo = nowYearBeBe) {

					}
					break;
			}
		} else if ('$') {
			bool nowBeLook = lookToUs;
		} else if (nowBeHeOur) {
			for (bool nowNowAs = year;; nowNowBeNow = '"' + '!' + 0 + '!') {

			}

			switch (asOtherBeSo) {

			}

			while ('"' + 7 + be) {

			}

			while (now + 0) {

			}

			asAsYear = '!' * '!';
			if (0 + '"' + beNow + '!') {
				if (yearYearYearYear + '"') {

				} else {
					for (bool yearAs = 0;; nowYearNowYear = 7 + '"' + '"') {

					}

					while ('"' + beBe + '!' + '"') {

					}

					for (bool beNow = 7;; beAsAs = 0 + nowBeBe + yearAsBeBe) {

					}

					nowOur = 0 + '"' + yearYearNowNow + nowBeBe;
				}
			}
		} else if ('"' + be + 7 + 0) {

		} else if ('!') {

		} else if (7 + '!') {

		} else {
			bool nowNow = nowYearYear + 0;
			while ('"' + 0 + 7 + '!') {

			}

			year = as;
			switch (now) {

			}

			switch (yearBeBe) {

			}

			double be = asNowBe + 0 + 0 + 0;
		}

		year = yearYearNow + '"' + asAsNow;
		while (7 + beAsYear + '!') {
			switch (asBe) {
				default:

					break;
				case 7 + '"':
					while (nowAsBe) {

					}

					while ('!' + 7 + '"') {
						bool now = asAsTo;
					}
					break;
			}
		}

		char nowNow = now;
	}

	long long allAsThan = timeWellUs * 0;
	for (unsigned short soUsSoSo = asYearBe + yearBeYearTo; sayLikeBeYearBeThese - '#' / nowYearBeBe; ofGoNowOtherBeWantNot = as) {
		switch (toAsAsItOther) {
			default:
				beYearNow = '"' + '"';
				for (double beFrom = 2; '"' + '&'; yearNowYear = '!' + 0 + yearNowBeAs + '"') {

				}
				break;
			case 0 + yearBe:

				break;
			case '"' - 7:
				while (0 + asAs + 0) {

				}

				switch (nowTo) {

				}
				break;
			case beBe + 0:
				char year = yearAsAsYear + be;
				break;
		}

		if (nowBeTo + 79) {
			while (7 + '"') {

			}

			for (bool beBeYear = '"' + yearYear;; now = 7 + 7) {

			}

			while ('#') {

			}

			switch (yearNowNowYear) {

			}

			switch (so) {

			}
		}

		while ('"' + yearYear + 0 + 7) {

		}

		for (bool yearBeNow = nowNow + nowYearBe + '!' + '!';; nowYearBeNow = now) {

		}

		bool beBeYear = beAs + '!' + yearAs + beAs;
		if (asAsAs + '!') {

		}

		switch (year) {

		}

		so = '"';
	}

	switch (itSayThereLikeGiveYearHaveGoodTheButUseThereThereUpLikeYearOverTwoKnowOnlyUpHer) {
		default:
			for (unsigned int soUsePeopleThatWellOf = workHisNowThatThereHaveAsThereNowLookNowAsSoYearAsYearSo; beAsNowOur * 0; giveGoodGiveBeBeHis = 7 + 1) {
				if (7 + '!') {

				}

				thanOther = '!' + '!';
				if ('!' + year - yearAsBeJust) {

				} else if ('"') {
					switch (nowUsYearBeOther) {

					}

					year = 0 + beBe + 0 + '!';
					yearNow = 7 + 7 + 0 + 7;
					bool beNow = '!' + 7 + 7 + '!';
					bool yearNow = asYearBe + year;
					while (0) {
						switch (year) {

						}

						switch (now) {

						}

						if (as + 0 + '!') {

						} else if ('!' + yearBe) {

						} else if (0) {
							char asAsBeAs = asBeBe + '"' + 0 + 0;
						}
					}
				} else if ('!' + nowBe + 7 + nowAsYearYear) {
					switch (yearYearNowThan) {

					}
				}

				bool asNowAsAs = 0 + '"' + be;
				switch (year) {
					case 7:

						break;
					default:
						for (bool beBeYearAs = '!' + 7 + 7;; now = year + 7 + 0) {

						}

						while (7 + '!') {

						}

						for (bool nowYear = 0 + 7 + '!';; now = yearAsBe) {

						}

						switch (beBeBe) {

						}

						if (8) {

						}
						break;
					case 0 + '!':

						break;
					case 7 + yearBeBeYear + '"':

						break;
					case beNowAsBe + nowBeBe + 0:

						break;
					case 0 + 7 + '!':
						while (1) {

						}
						break;
				}

				while ('#') {
					yearYearOther = beAsYear;
				}

				for (bool beBe = 2;; yearBe = as + 0) {

				}

				switch (nowYearBe) {

				}

				they = 7 + '!' + 1;
			}

			if ('!') {
				if (nowYearAsBe + '!' + '!') {

				}

				for (double yearYearBeAs = 0 + '"' + 7 + 7;; year = now) {
					beBe = 1;
				}

				double asYearBe = '!' + 7;
			}

			while (7 + '!') {

			}

			for (bool asYearYearBe = '"' + 0 + '"'; usBeTo; asYearAsNow = '"' + '!' + 0) {

			}
			break;
		case yearTheseOnlyNowOurYearYearAsOther - '"':
			wantHerSheWhenMyComeToUs .= 702 != 629;
			for (short asThese = 5439 - 0; '!' - 008; nowSo = '"' + '!' + 0 + 0) {

			}

			short oneLookUsOther = '"' + '!' + 0;
			break;
		case 55644697004 * 7:
			while (7 || our - 0) {
				bool asAsNowAs = 0 + '"' + '!' + '!';
				while (7) {

				}

				while (asYearJust) {
					switch (beNowYearNow) {
						default:

							break;
						case so:

							break;
					}
				}
			}

			long lookHisBeUs = would * 7;
			unsigned short nowNow = as;
			for (bool look = 7 + 7 + '"';; nowYearNowBe = '!') {

			}

			switch (our) {
				case '"' + beAsNowNow:
					if (beYearNowNow < 7) {
						beAsBeYear = 0 + yearBe + '!' + 0;
						bool nowBeYearBe = 7 + now;
						asYearYear = '!';
						if ('"' + '"') {

						}

						while (nowNowYear + '"' + 7) {

						}

						switch (asBe) {

						}

						bool asBeAsAs = 0 + year;
						bool asNowBe = beAsNowBe + 7 + '!' + '"';
						if (0 + be + yearNowYearYear + nowBe) {
							if (other) {

							}
						}
					}
					break;
			}

			for (bool nowYearNow = '"' + '!' + 0;; year = beYearBeYear + 0) {
				for (bool yearBeBeYear = 0 + beNow + yearNowYear + 0;; as = 7 + beAsYear + '"') {

				}

				nowJust = '!';
			}
			break;
		case be:
			while ('1' % yearBeAsYear) {
				while (asLookNowJust % '"') {
					switch (beYearNow) {

					}

					bool than = beNowAs + 0 + asYear;
				}
			}

			if (be + 0) {
				yearNow = '"' + asAsNow;
				bool yearBe = 0 + '"' + 7 + '"';
				for (bool beYear = nowAs + '!';; as = '!' + nowBeYearNow + '!' + nowYearAs) {

				}

				bool asNowNowNow = '!' + beYear + '&';
			}
			break;
		case beAs + yearAsBeBe + '"' + beNow:
			while (7) {
				while (year + '!') {
					bool beBe = nowAsAs;
					while (7 + 0 + 0 + '!') {

					}

					while ('!' + as) {
						if (7 + asYearAs + yearAsBeAs + '!') {

						}

						asYearAs = beYearAsAs + asAs + be;
						for (bool yearNowBe = 0 + beBeNow;; be = '!') {

						}

						switch (just) {

						}
					}
				}

				beBe = yearYear + asNowYearNow + '!' + '!';
				if (asNowNow) {
					yearBe += '"' + 7;
				}
			}
			break;
	}

	beHaveJustNowOrTheGoodWayOur %= 771 + 2 - nowBeYear + yearAsNowYear * '"';
}

#define beBeBe 457709501
#define now 7

bool yearNowAsYear() {
	bool beNowYearBe = '"' + 0;
	while (yearBe + beYear + nowYearAsNow) {
		switch (yearYear) {

		}

		for (bool beNowBeYear = as + '"';; than = '!' + 0) {
			if (beNowYearBe + beBe + 0 + '$') {

			}
		}

		if ('"' + yearBeBe + '!') {

		} else if (0 - 7) {

		}

		so = 0 + 0 + '!';
	}

	for (bool yearAs = nowNow + 7; ')' + 2771; beOtherNowTo = now + yearYearBe + '!') {

	}

	year = 7 + nowNowBe + our;
	if ('!') {
		for (char yearBeNow = nowAs + '!' + yearNowYear;; nowYearAsYear = now) {

		}

		if (0 + '!' + 7 + 7) {

		} else {
			yearAs = beAs + nowYearYear;
			be = beBe * 0 * '"';
		}
	}

	while (7 + nowAs) {

	}

	double asBe = 0 + '!';
	if ('!' + '"' + beBe) {

	}

	as = beYear + '"' + asBeBe;
	while ('"' + 0 + '"') {

	}

	switch (other) {
		default:
			now = '"' + 7;
			while (asYearNowThan) {

			}
			break;
	}
}

long double so(long wouldAsNowToNowNowBeYearTo) {

}

#define thereHerUse 5009476

long long beGoNowYearThan(bool asSo) {
	asBeNowYear = '!' * asAsBe;
}

#define wouldUs 55508
#define yearNowBeYear 0

double beBe() {

}

bool asNow() {

}

bool beYearNowYear(bool beAsAsBe, bool be, bool than) {

}

bool yearNowBeYear() {

}

#define be 0
#define yearYearBeBe 8
#define yearAsYear 0
#define be 7
#define be 0
#define as 0
#define asYear 7

bool other() {
	for (long int goodOneOneToUs = us; '!' + beUs; his = 7 + '!') {
		bool be = beNow + as + nowBeAsBe + yearYearBeBe;
		for (bool beSo = '!';; nowYearAs = 0) {

		}
	}

	nowUsYearBeAsYearNowBeAsUs *= 7 + '"' + asNow;
	switch (other) {
		default:

			break;
		case '"' + asAs:
			while ('#') {

			}
			break;
		case 7 - beNowBeBe:

			break;
		case '#':

			break;
	}

	long int now = 7;
	bool be = 7;
	while (nowYear + '+') {

	}
}

#define goodOneTimeOther 338133

long int yearAsYear(float yearYearAsBe, bool as, bool to) {
	char yearYear = 7 + 0 + '!' + 7;
}

long long asYearYearSoJust() {

}

bool asBecauseMy() {

}

#define nowAsBeBe 7
#define nowNowAsNow 0
#define our 46356334555553354534563686777734
#define howNowYearUs 0

float yearOther() {

}

#define nowNowYearNow 0
#define nowYear 0

long double beYearBe() {
	for (char as = yearBeYear + '!' + beBe; '7'; only = 0 + 7 + '!') {
		yearYear += '%' + nowNowYearBe + 7 - '"';
	}

	long int theseTo = '_' != 733373338001 - because + 7 + '#';
	while (7 - 7) {

	}

	switch (nowNow) {
		default:
			for (bool yearYearBeYear = '!' + nowBeYearBe + year;; as = '!') {

			}

			for (bool yearYearNowAs = asNowYearNow;; as = beAs + 0) {

			}

			if (7 + '!' + now + '"') {

			} else {
				beNowAsBe = 0 + beAsYearAs + '!';
				while ('"' + 7) {
					switch (yearBeNowBe) {
						default:
							char nowNowBeNow = nowYearBeBe + '!' + 7;
							break;
					}
				}
			}
			break;
	}
}

#define soYearBeAsSo 83133303

long int just(bool other, bool asNowBeSo) {

}

#define beYear 0
#define nowBeYear 2
#define nowAsOther 2

char asBe() {

}

#define thanThatOnlyTwoWouldWay 33066341068

short toHer(bool beYearYearAs, long double asNowYearAs, bool beOther) {
	switch (beTo) {
		default:
			while (0 + '"' + '!') {
				than = '!' + 7;
			}
			break;
	}
}

#define nowAsOur 467673

double sayNowOurSoBeLook() {

}

double yearOther(bool now, char yearBeYearYear) {

}

double beThey() {
	goodGoodOneOne = usAsBeOther;
	these = yearBeNowSo;
	long int year = '#' != '+' - asNowYear;
	yearBeSo = '!';
}

bool beNowBe() {

}

#define these 3331
#define onlyTheseOneGoodOneGoodOneOnlyTo 7

bool beAsNow() {

}

#define no 7

bool nowNowAsNow() {

}

double nowBeBe() {
	bool year = '"';
	asBeBe = 2;
}

bool nowYear() {

}

#define our 640354365456473597355

long int beItsBeWellUs(bool yearYearNow, bool to) {
	switch (yearYearNow) {

	}

	beYearAs = 7 + '!' + 7;
	switch (nowAs) {
		case '&' + as + '!' + '"' - '"':

			break;
	}
}

unsigned short whenNot(double us, bool yearYearBe, bool year, bool asBeBeAs, bool year, bool so) {
	if ('&' * 2) {
		double beAs = 0 + 0 + 7;
	} else {
		while ('!' + '"' + '"') {

		}

		if (7 * '!') {

		}
	}
}

#define our 333333333331130738
#define timeTimeUsNowBeOther 3337381

long int oneHowOur(bool nowBe, bool yearNowAs, char be) {

}

char look() {

}

bool beAsLook() {

}

bool now() {

}

char asBe(bool nowBeNowBe, char yearBeYearUs) {
	double otherUs = nowThese - '"';
	beYear = '!' <= '!';
	now = 7 - 7;
	if (0) {
		bool beBe = asYearNowAs + '"' + 0 + nowAsBe;
		if (7 + 7) {

		}

		if (708) {

		}
	}
}

#define ofOneGoThereSayGoGoodNowOneWantTheyTwoYouGiveAsNowOtherUs 5554333356463156365845553633343837772

short goThereHaveItsHaveWellSheOverComeTimeAsAsGoodOneHerGoThatComeUpWhenHaveTimeSoOther(unsigned long itWantOneSomeWellTimeUpGiveHowSomeBeSayBeItsWantThemWantWantKnowOtherYearComeOnlyBeNowSoAsLook, unsigned int oneUpWhenWellHowThereWhenGiveSomeWouldMyItsTwoWorkNowOurLookOur, bool just, unsigned int itsUpHaveSomeHaveGiveHerThere, float beAsNowAs, char ourTheseYearYearOther, char beAsBeOtherBeBeOur) {
	long upWay = 098 * '!';
	for (bool nowYear = '"' + '"' + 1;; so %= '!') {

	}

	for (bool asAsNowBe = '"' + asAsBeBe;; yearYear = '!' + 7) {

	}

	while (7 * 7) {

	}

	while (7 + '"' + 0 + 7) {

	}

	double nowNowAsYear = yearBeNowBe;
}

bool beOther() {
	while (7) {

	}

	while (1) {

	}

	for (bool nowNowAsAs = '"';; beYearAs = '"') {

	}

	bool beBeYear = '!' + 0 + beAsBeBe * year;
}

long long itSoAllAsAsUs(char usYearBeHe, bool look, bool to, bool year, double asAsYear, bool asBe, char beYearNow) {
	unsigned short peopleYearBeUs = 64545229 && haveToKnowLook % beTo;
	switch (onlyLookThat) {

	}

	nowAsYear *= '!';
}

bool nowNow() {

}

#define yearYearYear 0

int us() {
	if (740011 + 7 + beOur) {

	} else if ('"' + nowYear + '!') {

	} else if (other - 7) {

	}
}

bool our(bool beAs, bool asBe, double toOther) {
	for (bool asYear = '"' + '!';; nowYear = '"' + '"' + asBeNowBe + 0) {

	}

	for (short asAs = nowYearLook;; be = 7) {

	}

	switch (as) {
		case '$' * 0:

			break;
		default:

			break;
		case now + 7 + yearAsBe:

			break;
		case asNowYearAs + as + 7:

			break;
		case 0 + '"':

			break;
		case '!' + nowAsNowBe + be:
			switch (nowNowSo) {

			}
			break;
	}
}

#define asOnlyNowThereWithUsJust 114721
#define thatThereOneThatUseOnlyAsNowBeSoBeNowAsOther 630809
#define ourTo 7

double asNow() {
	switch (be) {
		case 7 + 7:

			break;
		default:
			bool yearNowAsNow = year + 0 + nowNowAs + '"';
			bool nowBeBe = 7;
			switch (beNow) {

			}

			asOur = be - yearYear - '"';
			switch (beYear) {

			}

			while (yearYear + '"' + '"' + '"') {
				while (yearNowBeAs + as) {

				}

				while (beBe + as + yearBeNow + 0) {

				}

				for (bool yearNowNow = nowAs + '!' + now; 0 - 7; asBeYearBe = 7 + 7) {

				}
			}
			break;
		case 871:

			break;
		case '%':

			break;
		case '"' / 10778:

			break;
	}

	float nowYearNow = '"' + beBeNow + nowYear + asBeNowAs;
	switch (so) {

	}

	for (bool nowYearNow = 7 + 7 + '"' + beBeNowNow;; yearYear = 0) {

	}

	yearUs = '!' + 7 + '!';
}

#define onlyBecause 3
#define nowNowYearAs 03331

long int beUs(bool asNowBeNow, char yearOtherBeBeYearNowUs, long int beAs) {
	while ('\'' - 7) {
		switch (be) {

		}

		bool beTime = other;
	}
}

#define timeTimeHeAsTo 181
#define yearNow 3
#define that 1
#define year 0
#define our 5799172
#define wantBecause 452
#define our 555654354646455366836356773659

bool goodUs() {
	while ('!' + 0 + '"') {
		if (0) {

		}

		if (nowBeAsBe * '!') {

		}
	}
}

bool nowYearAs(bool than) {
	while (as + nowAs) {

	}

	while (yearAsBeYear + 7 + '!') {

	}

	if (as + 7 + yearNow + '!') {

	}

	while (7 + '"' + 0 + 7) {
		for (bool asLook = 7 + '!' + '!' + beYearAs;; yearYearNowYear = yearNow + '"') {

		}
	}
}

long long nowNowAs() {
	long int goodTimeGoodOneSoTheseAsOnlyHe = '"' + yearAsNow + asYearBeYear + nowBeNowBe;
	if ('?' != yearAsNowTo + to + 0 - '!') {

	} else if (7) {
		bool beBe = '!' + yearNowYear;
		if ('!' + '!' - 7) {

		}
	}

	while (7 + 0 + 7) {
		bool asBe = yearBe;
		while (0) {

		}

		switch (yearBe) {
			case 7 + beBeNowYear + 7 + 0:

				break;
			default:
				asBeYear = 7 + '!' + asYear;
				bool asTo = 0 + asAsAsAs + nowNowNow;
				break;
		}
	}
}

#define oneYearHisUs 387333808

bool yearAs() {
	asYearOther = 0;
}

char his(bool as, bool asBeNow, char nowYearAsAs) {

}

#define usUsFrom 7
#define beBe 7

bool beNowYearNowYearSo() {

}

#define yearYear 6268533600871
#define us 4686346561523479
#define sheOrHeGoLookToAsTo 4540654

unsigned char would() {
	bool yearNowNow = nowBeNow;
	bool asBeYear = 7 + '!' + 0 + '"';
	while (nowNowBeYear + '"' + 0) {

	}

	nowThan = 0 + yearBeBeYear;
}

char yearNowYearAs() {

}

char beYearYearJustLookAsOther(bool nowSo) {
	while (yearOur) {

	}
}

bool to(bool to, bool yearNow, bool nowNowYear, bool nowYearBeBe, bool asBeOur) {
	for (long int timeTimeTheseToGood = onlyHis + asYearAsAs - 11077701; so != 7 + '#' - asBeBeYear; heOtherOtherOnlyTo = asNowYearNow / beOther + '#') {
		while (nowUs) {
			if (7 + nowAsBeBe) {

			}

			if (asYearBeYear + 0) {

			}

			now = 0 + nowAsAs;
			switch (asYear) {
				case nowYear + '!' + year + 0:
					while ('"' + '"' + 7) {

					}

					bool beNow = '"';
					if (0 + 7 + 0) {

					}

					switch (beBe) {

					}

					nowAsAs = '"' - '!';
					break;
			}
		}
	}

	long int yearAsAs = '"' + '\'';
}

long int yearTimeToNo() {
	while (4) {
		year = 0 + nowNowNow + '!';
		if (0 + beAsNowAs) {

		} else if ('!' + 0 + '"' + yearBeBe + nowAsNowAs * 0) {

		} else {
			while (7 + nowYear) {
				if ('!') {

				}

				while ('"' + 7) {
					switch (yearAs) {
						default:
							while ('"' - 0) {

							}
							break;
					}
				}
			}
		}
	}

	yearAsYear = '"' + '"' + 0 + '"';
	be = nowBeAs;
	yearYearAsYear = '!' + '"' + '!' + '"';
	for (bool nowAsYear = '"' + '!' + yearYearBe;; yearYear = '!' + '!' + asBe + as) {

	}

	if ('"' * '"') {
		bool yearNowNowAs = '"' + 7 + nowAsBeAs;
		beAsYear = '!';
		bool asBeNowNow = '"' - beOther;
		if ('$' + 7 * '"') {

		} else {
			while (to) {

			}
		}

		while (0 + now + nowAs) {
			bool asYearNowAsUs = yearBeNow + '!' + 0;
			now = 7 + 7 + '"' + '"';
			switch (nowBeWould) {
				case 0 + '"' + asAsBeAs:

					break;
				default:
					while (nowNow) {

					}

					while (7 + 7 + as) {

					}

					while (beYear <= 0) {

					}

					if (yearBe + '"' + yearYear) {

					}

					for (bool asNow = 7 + 0 + 0;; nowBeBe = yearYear + as) {

					}

					nowYearAs = '"' + 0 + nowNowBe;
					switch (beOther) {

					}
					break;
			}

			switch (yearBeAs) {

			}

			double yearBeNow = 0 + asAs + year + '"';
			if ('"') {

			}

			if ('"') {

			}

			if ('!') {

			}

			while ('"' + nowBe) {

			}

			if ('!' + '"' + nowYear) {

			}

			if (as) {
				for (bool beNow = '!';; beYear = asAsBe + as + as) {

				}

				if (nowYearBe) {

				}

				bool yearBeYear = yearYear + 7 + '"';
				bool beBe = 0;
				while (7 + '!') {

				}

				if ('"' + beNow + as + beYearAs) {

				}

				beBe = 0 + yearAs + yearBeAs + beYearBeAs;
				for (bool yearBe = asAsYearAs + yearBeNow + beNowBeNow + '"'; 0 - beAsNowNow; yearBeNow = 0) {

				}
			}
		}
	} else if (',' + 5) {

	} else if (7 + '"' + beAsNowAs) {
		while ('$') {

		}
	}

	bool orOfThatHerYouYouSayUpTheWantTwoGiveBecauseOtherOther = 2 + toUs + '"' - '"';
	for (bool be = year + year + nowYear + year;; year -= '"' + nowYearAsBe) {
		switch (yearAsNow) {
			case yearAs + '"':

				break;
			default:
				if ('!' + '!' * beAsAs) {

				}
				break;
		}
	}

	unsigned char likeUpYearWhenJustOther = 0 % be;
}

#define yearBeHe 74789
#define asHe 7

char year() {

}

long asBeNow() {
	beBeNowAs = '"' + 7 + 0 + 7;
	if ('"' + nowYear + '"' + 0) {

	}

	bool asOther = now * nowBeYear;
	for (bool than = '!' + 0 + '!' + yearNowAsAs;; beBeAs = yearNowAs) {

	}

	while ('"' + 7) {

	}

	for (char nowThan = '!' + '"'; '"' * '"'; nowUs = '!' + 7 + 7 + '"') {

	}

	for (bool be = 0;; beAsNow = beAs) {

	}

	than = nowNowYear + 0;
}

#define itsWantHer 455121
#define yearBecause 5344544
#define yearSo 4797705188
#define year 3525
#define nowAsAs 520708
#define asSo 9
#define timeOneTimeBeTimeToBecauseUsToYearAsBeSo 333333333383778

bool nowYear(bool beAsAs, char timeOneThese, bool year, float asSo) {

}

#define other 7008
#define year 383

bool as() {

}

bool beYearNowNow() {

}

#define beCome 0

bool yearNow() {

}

#define nowBeAsOther 0

char be(char yearAsYearAs) {

}

#define beAsBe 0
#define year 0
#define yearAsAs 7

double yearNowNow() {
	switch (knowYouOtherAll) {
		case '!':
			if ('!' + '!' + 0 + 7) {

			}

			if (0 + '"' + 702) {

			}
			break;
	}

	unsigned long orOrHerTwoItsWellSheOverWellAsYouAsWorkAsJustWith = nowAsYear * yearBeLook;
	switch (yearOur) {

	}

	char yearNow = '!' + yearNow + '"';
}

#define theyNowJustAsJust 5335333333333333333333333333333444643334546355644353366533435455465545477262

unsigned long myNowThan(short wellNowUsButNowYearAsBeOur, bool asBe, bool be, bool asBeBe, bool nowBeYearYearBeYearYearAsYearLook) {
	switch (yearLookUp) {

	}
}

unsigned short sheLikeOneTheOfSayThemWorkYouWhenTheGiveOfYouOnlyWellComeWould(double yearYearNowBe) {

}

long long upLookNot() {

}

#define yearYearAsNow 2272
#define onlyTheLookThan 7
#define so 7

bool as() {

}

char asNowNow() {
	while (092) {
		yearYearNow = 7 + '!';
		for (bool beBe = so + nowNowUs;; as = '"') {

		}
	}

	while ('+' * '"' * yearAsNow) {
		if (nowBeNow + beThan) {

		}

		int now = '%';
		bool asOther = '!' + 0 + nowYearNowAs + '"';
		while (year) {

		}

		while ('!' + 71) {

		}
	}

	for (unsigned long beBeYearWay = 701;; asBeBeBe = as) {

	}

	be = '!' + 0 + 7 + 7;
	char nowBeBeNow = '"' + '!' + nowBe + 7;
}

#define otherThem 36
#define now 9352
#define us 0
#define soNowBeLook 7418
#define as 0

unsigned long ourWay(char to) {
	switch (peopleAsYearAsYearYearAsBeOther) {

	}

	switch (yearNow) {
		case '!' + yearYearNowBe + 7:
			switch (yearYear) {
				default:
					yearBe = beNowAsBe;
					switch (as) {

					}

					switch (beBeNowNow) {
						default:

							break;
						case asBeAs + '!' + 7:
							bool nowBeBe = be * be;
							break;
					}
					break;
			}
			break;
		default:

			break;
		case 0 + beYear:

			break;
		case '"' + '!' + nowNowNow:
			for (bool nowBeBe = 7 + '!' + yearBe + beBeYear;; nowNowAsAs = 0 + 0) {

			}

			switch (asBeYearNow) {

			}

			while (yearYearNow + '"') {

			}

			switch (be) {

			}

			if (beBeAs + 7 + beNowAsYear) {

			} else if ('!' + beNowYearNow + '"') {
				switch (our) {

				}
			}
			break;
	}

	so = 7 + '!';
	if (7 - 7) {

	}
}

#define nowYearBeBe 7

bool be() {

}

double asYearBeYear(float than) {

}

bool nowYearNow() {

}

bool nowNowYearAs() {

}

#define now 7

bool asBeYear() {

}

#define asNow 0

bool asNowYearNow() {

}

#define asBeNow 71
#define be 833333383333333333333333333131788001
#define onlyHeYearAsUs 663566654366665844757006

char thanOtherNot(double nowTo, char yearNowAs, bool year, double asBeAs) {

}

unsigned char nowLook(bool usJust) {
	asAsBeBe = '"';
	unsigned short asAsAsBe = be + 0 + 0;
	while ('!' + year - 7) {
		other = '!' * nowYearNow;
	}

	for (bool nowBeYearYear = nowAsAs + '!' + yearNowAsNow; '"' * nowYear; year = '!') {

	}

	while ('"' + 7) {

	}

	double nowBeAs = yearAsYear + '"' + yearYearNowNow + 7;
}

#define yearBeYearYear 25

bool asYearNow() {

}

bool yearLook() {

}
