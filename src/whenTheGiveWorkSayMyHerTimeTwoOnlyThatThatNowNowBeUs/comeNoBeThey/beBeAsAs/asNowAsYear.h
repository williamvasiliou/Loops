#include <UU/0UUU/U6U2/0.h>
#include </U0603.h>
#include <P6/1.h>
#include <czlgYUUUUUUU026.h>

#define timeTimeTimeOnlyLikeHaveSayThatPeopleKnowWorkGoWellTheKnowSheOfTheLikeComeSomeThereUpPeopleWorkGoTwoYouComeGoWantThatTimeItsUpOnlyOfWellTheGoHaveTheOverGoodSomePeopleTheThereKnowHowComeUseToSoOfHaveUseGiveTimeNoHowTheKnowLikeSomeItLikeFromBeNowAsNowAsBeSo 
#define theOrYouOfWellItsOrTwoWantSoAsTimeUseUsePeopleFromWellWellWellYouItsThereWhenOfWellUseOneUpHerOnlyWorkItsOverWantUseSomeTheKnowThatWhenGoOrWellItsSomeYouOnlyYouKnowHerLikeYouKnowOfWorkTheSayOfTimeItsHerSayWellLookHowAllFrom 
#define as 6760440792

short asOurTwoJustNo() {
	while ('#') {

	}

	bool asNowAsYear = '!' + 7;
	bool beAsBeYear = be;
	bool now = '!' + 0 + 2;
}

double usWith(unsigned int justUs) {
	bool as = '!';
	while (0 + 0 + '!' + now) {

	}

	bool yearNow = '!' + '!' + 7 + '!';
	double yearBeBeYear = asYearYear + asAsNow + yearBe + beYearAsYear;
}

#define asNowAs 4365447804774601

unsigned long hisSoSo() {

}

#define because 7
#define haveNowOnlyGiveOnlyGoodYouThereOrLookBeAsJustYearYearLook 4173

double yearWould() {

}

#define us 3333333333133333738

float goodTime(bool nowBeAs, char asHis, char beYearAs, char beAsYear, char year) {
	long int onlyHisBecauseHisThese = '!' + nowNowBe + nowNowBeBe;
}

bool asJust() {

}

bool yearNow() {

}

bool nowYearYearNow() {
	bool look = '!' + nowNowAsNow + 0;
}

bool beBeAs() {

}

#define yearThan 01
#define asBeBe 0

bool asBeSo() {
	for (unsigned char goAsAsOneToAtWantTheyTo = '!' * 3; nowThey + '#'; beBeNow = 7 + nowYearBe + nowYearYearBe + 0) {
		bool look = 0;
		now += 7;
		for (bool be = 7 + '!' + as;; yearBeYear = '!' + asAs + 7 + yearNowYear) {

		}

		bool asYearBeYear = 0 + asAs + asNowAs + year;
		while ('"' + beNowNow) {
			while ('!' + '!' + beBeAs + '#') {

			}
		}
	}

	nowYear = 7 + '!';
	if (7 - year) {

	}

	double now = beYear;
	for (bool nowAsAsYear = '!' + 0 + '"' + '!';; nowYearAsNow = asNowBeBe) {

	}

	if (so >= 90679) {

	} else if ('!' + '!' + '"') {

	} else if (7 + 7 + '"' + beBeAsBe) {
		while ('!' + year + '"') {

		}

		while ('"' + beNowAsBe) {
			for (bool beNowBeAs = as + beYearNow + nowYear + 0; '&'; beAsYear = be + '"' + year) {

			}
		}
	}

	if (02 + beNow + yearBeTo) {

	}
}

#define itsAsThanGoodNowJust 8
#define yearYear 0
#define sheYearSomeNowBeHave 7002

long double now() {

}

bool yearYear() {

}

#define beYearAs 02
#define justLook 3333333333333333333333337333336391

unsigned int yearAsBeBe(long long beYouOrBeYearAsJust, float asThanJust, size_t asYear, double beAsBeLook, bool than) {
	float wouldOfBeYearOther = 11 || '#' || orAtAsNowAsNowHis;
	for (bool beAsYearNow = '"' + '!';; nowNowBe = '!' + '!' + '"') {

	}

	switch (as) {

	}

	while (asAs + yearAsAs) {

	}

	if (year + yearYearYear) {

	}

	while ('"' + yearAsNow + yearNowAsYear) {

	}

	bool beYearBeAs = 7 + '!' + 1;
}

#define yearBeYearYear 71

bool nowNowYearNow() {

}

#define asYearBe 7
#define yearYear 7

bool asAsYear() {

}

#define beYear 7
#define as 7
#define now 64365544636369367634664533009331

bool look(short yearYear, bool beNow, double asYearNow) {

}

#define fromWay 53333311318

long int only() {

}

float heAsThese(bool nowAsAs, bool asYear, char be) {
	for (bool beNowNow = asYear;; beBeAs = '"' + 7 + '!' + 0) {
		while (7 + 7 + 7 + 7) {

		}

		as = be + '"' + year + nowBe;
		for (bool nowAsYear = yearYear + beBeNowAs + '!' + 0;; nowAsYear = '"') {

		}

		switch (nowAsUs) {

		}
	}

	yearNowAsNowUs = '!' + 7 + '!' + '"';
}

#define becauseYearGood 7337718
#define nowToBeSo 8

char yearNowBecause() {

}

#define now 7
#define asOnlyHisGoodTimeOtherAsNowBeThan 0
#define as 0
#define nowNowAs 0009

bool asNowAs() {

}

bool asAsYearBe(bool yearYearAs, bool yearBeNowNow, bool yearYearBeAs, char be) {
	switch (beThanOurAsWayTo) {
		case 0 + '!' + 0:

			break;
		default:

			break;
		case asNowYear:
			switch (yearBeBeAs) {
				default:

					break;
				case yearBeBe + year + asBeAsNow + 7:

					break;
				case '!' + '!':

					break;
				case 00702:
					bool beNowNowAs = 0 + 7 + 0;
					switch (nowSo) {
						default:

							break;
						case '!' - 7:

							break;
					}
					break;
				case than + '!' * now:

					break;
			}

			beSo -= 0 + as + beNow + beNowAsBe;
			switch (beBe) {
				default:
					for (bool yearThan = 7 + beNowYearAs + 0;; asNowAsAs = 0 + '"' + '"') {

					}

					while ('"' + 7) {

					}

					if ('!') {

					}

					while (yearAsBe + now + 0 + 0) {

					}

					beAs = 7 + '!' + asAs;
					year = '!' + '"';
					bool nowYear = 7 + asYear + '!';
					switch (yearYearBeAs) {

					}

					while (0 + '!' + '"') {

					}

					yearAsYear = 7 + 0 + as;
					switch (other) {

					}
					break;
				case be * '"':

					break;
			}

			bool yearYearNow = '!' + 7;
			if (7 + be) {

			}

			well = '"' + 7 + '!' + '"';
			switch (nowNowAs) {
				case asYearNowAs + as + beBeYearNow + be:

					break;
				default:
					asBeNowAs = '!' + yearNowNowNow + 0 + '"';
					for (char beYear = asAsAs;; beBeAsYear = 7 + '!' + 7) {

					}

					if ('"' + '"' + '"') {
						switch (asBeAs) {

						}

						switch (other) {

						}
					}

					bool as = '&';
					break;
			}
			break;
		case '"' + '!':
			switch (upSomeGive) {
				default:
					for (bool asNow = '!' + as + nowAs;; beBeNow = now + beNowYear + '!') {
						char nowBeYearNow = beBeYear + asBeNowAs + '!' + 7;
					}
					break;
			}

			for (bool yearThan = '!'; '&'; beBeBeNow = 0 + '"' + 0 + 7) {

			}

			for (bool beYear = us;; now = '"' + beAsYearNow + '!' + 0) {

			}
			break;
		case '!':
			if (year + nowBeBeYear) {
				while (asYear + 7) {

				}

				for (bool beUs = now + asBe + 0;; as = yearNowYearNow + 0) {

				}
			}
			break;
		case 08:
			if (just) {

			}

			size_t as = 0 + '!';
			break;
		case 7 + 7:
			switch (beYear) {

			}

			switch (nowBe) {

			}

			switch (yearBe) {

			}

			switch (asBe) {

			}

			for (bool beYear = 7 + '"' + yearAsNowNow + asAsYearAs;; asNowYear = '"') {

			}

			if ('!' + '!') {

			}

			while (year + nowYearAs + 0 + 0) {

			}

			switch (beJust) {

			}
			break;
	}

	nowGoOurAsLook ^= 9 - 8;
	switch (hisNowOur) {

	}

	switch (asJustAsYearBeThan) {
		case now + '"':

			break;
		default:
			double so = 0;
			break;
		case other * '!':

			break;
	}

	other = 71 - '!' + yearYearTo;
	bool asYear = year + '"';
	for (bool as = yearBeNow + '!';; as = nowAsBe + '"') {

	}

	while (0 + '-') {

	}

	switch (nowBe) {

	}

	int now = '!' + '"' + '"' + asYearNowAs;
	now = '"' + 0;
	while (0 + 7 + beYear) {
		switch (nowYear) {
			default:
				for (bool nowAsBeBe = 7 + '"' + 7 + '!';; just = 7 + '!' + yearNowNow) {
					switch (yearAs) {
						case '"':
							timeBeSo = 0 + be;
							break;
					}

					if ('"') {

					}

					switch (now) {

					}

					switch (asYearBeBe) {
						case 0 + '"':

							break;
						default:

							break;
						case '%':

							break;
					}
				}

				asUs += '"' + 0 + asYear;
				if (asAs + '%') {

				}

				char yearYear = 0 + 7;
				break;
		}

		theseNowBecause |= '!' + now;
		switch (asYearAsAs) {

		}

		for (bool year = asNowAs; so; now = 0 + 0 + 0 + 0) {

		}
	}
}

bool be() {

}

#define theseOnlyGoodTimeTimeBeSoTimeToOtherGood 80108
#define theseToUsTo 3100771

bool nowYearAs() {

}

#define sheOnlySoTo 7
#define look 7
#define year 9

long double beYear() {
	long long  = 'k' && 'p' && 03636448 && 'y' | ofThereKnowNowYearUseOrHowUseNowOtherSayThatOurAsAsFrom > 'y' && 35 && 52367601 && 35742704;
	unsigned char wantUseKnowGoWellMyOfThereTheWorkPeopleOtherLook = 4464534434553565636456333634554543633565366436534453563636445363356446565363646566335446556565555634434445334345566453636645654665553504356455555465545636554555564434656236344434556534646446444556056440178 | 'e' & 'm' >= 't' && overTwoGoToHerHaveOurSheItSheHowOfWantSomeOverTheGoOneLikeNoHerThereGoodOnlyGoSayHaveBeTwoWellSheOverOtherWantNowOurThereBeOurSo || 353450353556633633365553566644554546355682 > 168657079 - beBeNowNow;
	if ('!' - '!') {

	}

	if (asJust || goBePeopleBeLook | '!' - 09) {
		unsigned long justBeItsNowOther = '}' == 'K' > 66739008 | our;
		sheUpWayNowYearOurYearSo <<= 1 - '!' + asYear > nowTo + '$';
		lookUs = yearAs - 0;
		if ('!') {

		} else if ('+') {

		}

		if ('"') {

		}

		while (7 + nowBeYear + nowYearAs) {
			for (bool our = nowAsAsNow + 7 + yearBe + '!';; asYearYear = beYearBeYear + beYearBeBe) {

			}
		}
	} else {
		switch (lookOther) {
			default:
				while (yearNowAsNow + 7 + '!' + 0) {

				}

				bool yearAsAsAs = '$' - 7;
				bool now = '!';
				for (bool asAsNowYear = '"';; beAsNow = nowBe + asYear + nowAs + be) {

				}

				for (bool beAsAsNow = asNowYearBe + 7;; nowNowAs -= '"' + asAs + '!') {

				}
				break;
			case '"' + nowAs + '$':
				asBeAs = '"' + 7 + '"';
				char yearNow = 0 + asNow + nowAsBe;
				if ('"') {

				}

				for (bool yearAs = '!';; asYearAsYear = 0 + nowNow + '"') {

				}

				switch (look) {

				}

				switch (yearBeNow) {
					default:

						break;
					case '$':

						break;
				}
				break;
		}

		justPeopleHis &= '%' * nowAs;
		soYearNowYearUs = now + '"';
		while (yearBeUs) {

		}
	}

	while (nowWay - 0 + 0 * now) {

	}

	bool yearBeNowBe = 0 - beBeNowBe;
	if (beYearNow + 0) {

	} else if ('"' + yearYearBe + 0) {

	} else {
		for (bool asYearYear = nowBe + nowAs + 0;; yearAs = yearAs + 0) {

		}

		switch (nowBe) {

		}

		yearNowBeNow = beYearYearAs + nowAsBe + 0;
		year = 2;
		switch (asAsAsNow) {
			case 7 + '!' + '!':
				for (bool beNowNow = 0;; asYearAs = as) {
					if (yearBe + '!' - beYearAs) {

					}
				}
				break;
		}
	}
}

#define  3044665644643657444036829
#define hisOther 2

long int way(bool yearTheyOur, bool nowBeYearYear, bool be, bool yearYear, bool now, char beBeNowAs) {
	long double just = to;
	while (beBeAs - asAsBeNow) {

	}

	while (0 + '!' + 7) {
		switch (asBeYearAs) {

		}

		bool so = beNowYear;
		bool nowBe = yearAsBe + '"' + '"';
		switch (asAsBeAs) {

		}

		while (asNow + 0 + '"') {

		}

		for (bool beNowYearAs = year; nowYearYearBe - 7; asNowNowAs = 0 + '!') {

		}
	}
}

#define comeNowHowItsToAll 709
#define beAsYear 067700079

long int asMyAsAsNot(short at) {
	while (asAsAsBe + asNow + '"') {

	}

	char yearNowYearBe = 7 + '"';
	bool asAsBe = 7 + be;
	for (bool nowNowYear = 7;; nowNow = '"') {

	}

	for (bool nowBeNowNow = 0 + 0 + '"' + 0;; yearNowAsAs = nowBe) {

	}

	nowNowYear = '!' + nowBeAs + 7;
	double as = 7;
}

size_t thePeopleAsBeOur() {
	beAsAsYearBeUseUs += 0 + now + '"';
	while (justThanOther + '$') {

	}
}

float ofBeMy(bool yearNow, bool now, bool theseAsOverOur, char nowYearNow) {
	while (0 + asYearAsAs + 7) {

	}

	year = as;
	if (beBeYear) {
		bool yearYearNow = 2;
	}
}

#define beYear 0
#define asBecause 52

float beYear(bool yearBeNow, bool beLook) {

}

#define year 7

bool yearBeYear() {

}

bool nowAsAsBe() {
	bool yearAsAsAsAsJust = goHowUsThese % '"';
	switch (howLookThanOther) {
		case '"' + '!' + 7 + '"':
			if (beUs) {
				asYearBe += '!' + as;
			}

			yearAsBeYear = 7;
			if (year + beAs + now) {

			}

			while (0 + 7) {
				bool asYearNowBeSo = 7 + 7 + 7 + '!';
			}
			break;
		case 7 + yearBeAs + '"' + 0:

			break;
		default:

			break;
		case 0 + 7 + 0:

			break;
		case '"':
			while (asNowYearBe) {

			}

			if ('"' + '!' + '"' + '"') {

			}

			if (7 * 0) {

			}
			break;
	}

	if (otherTimeYearThanNowLook - '%' + '!' - just) {
		while (nowJust) {
			if ('!' + beYear + '!' + 7) {

			}

			yearBeYear = now + 0;
			if (7) {

			}

			for (char asAs = year + yearAsAs + beAsYear + 0;; be = '"') {

			}
		}
	} else if (9777799 + 0701) {
		double yearNowNowYear = be + beBe;
		long int beAsAsYear = nowUs;
	} else {
		if ('"' + now + yearAsNowYear * 7) {

		}
	}

	while ('$') {
		if (0079) {
			while (beBe + asNowNow) {

			}

			for (bool be = nowAs + '"' + 0 + 7; 7 - '"'; asYear = 0 + 0 + 0 + 0) {
				switch (beYearSo) {

				}
			}
		} else if (7) {
			while ('"' + 7 + '!') {

			}

			while (as + 7) {

			}

			switch (asLook) {

			}
		}

		if ('!') {

		} else if ('!') {

		} else {
			switch (to) {

			}
		}

		switch (year) {
			default:
				asAsBeYear -= yearBeYearAs + 0;
				now = be + beYearYear;
				bool asYear = be + asNowYearAs + yearYearYearAs;
				if (0 + '"' + 7) {
					switch (beBeBeNow) {

					}

					be = than;
				}
				break;
		}

		for (bool nowAsBe = 0 + beNowAsNow + as + '"';; as = asBeYearAs + 0) {

		}

		for (double nowAsAs = year + '!' + asAs; 9; beAsAs = '"') {

		}
	}

	for (unsigned long asNowAs = as + '"' + '!';; be += '"') {

	}

	for (bool beYear = '!'; 7 - nowYearAs; asBeBeBe = 7) {
		bool as = nowAsAsYear + 0;
		switch (as) {
			default:
				while (7 + '!' + 0 + 7) {
					look = asAsNow;
				}
				break;
		}

		for (bool as = '!' + '!' + 7 + asNowYearBe;; year = 0) {

		}

		bool now = 7;
		for (bool year = 0 + '"' + '"';; asNow = '!' + 7) {

		}

		yearBeBe += be + yearAsNowYear;
	}

	bool now = 7 * 0;
	switch (yearNowNow) {
		default:

			break;
		case '%':

			break;
	}

	nowLook = '!' + asAsAsNow + yearAsYear + beNowYearNow;
	double be = '?' - 0 != 0;
}

long int goodOnlyOtherYearBeAsHisSo(float beTheseYearAsBeOther) {
	long int yearNowAs = 8;
	if (soAsOnlyYearOther != '+' / 0 - asBe) {
		if (nowNowNow + 7) {

		} else {
			so = 0 + 0 + yearBeBeYear + '"';
		}
	}

	becauseOnlyGood = '!' + '"' + '"';
	bool yearNowBe = '!' - '!';
	for (long int otherLookOther = 08;; now = nowYearAsYear + '"' + '!' + 7) {

	}

	if (9) {
		double now = '!' + beBe;
	}
}

bool just() {

}

bool as() {

}

#define nowOur 6338553635633697201

long int overUseHerThemAsBeAsNo() {

}

#define allAsOtherWorkBeThatOurYearLook 5446758

unsigned short beBeNowYearAsJust(char beBeYearNow, bool beNowNowNow, bool asSo) {
	while ('%') {

	}
}

bool yearThese() {
	for (bool yearBeYear = 0 + beNow - nowAs;; nowAsAs = 0 + 0) {

	}
}

bool now() {
	switch (nowBeAsYear) {

	}

	while (nowAsNowAs) {

	}

	switch (asAsAs) {

	}

	switch (beYearAsAs) {

	}

	while ('!') {

	}

	switch (yearYearBe) {

	}

	for (double nowBeAs = yearAsYear + '!' + yearYear;; asYear = yearAsBeNow + '"') {
		beBe .= '0' * nowBe;
		short beYearYearAs = beYearAsYear + '"';
	}

	if (6) {

	}

	switch (asNowYearUs) {

	}

	switch (nowNowUsUs) {

	}
}

unsigned int onlyTheBecause(long long you, char beYearBeAs, long asAs, double nowBeAsAsYearLook) {
	for (bool beAsYearNow = '"' + yearYearBe + 0 + yearYearYear;; our -= '"' + nowNowAsNow + nowAsAsNow) {

	}

	while (as + '"' + '"' + 9) {

	}

	while ('!' + 7 * '"') {
		while ('"') {

		}

		for (bool yearBeAsAs = '"';; beYearNow = 7 + '!' + as) {

		}

		switch (nowLookBeYearUs) {
			default:

				break;
			case nowAsBeYear + '!' + be + '"':
				while (yearYearAs) {

				}

				switch (yearYearNowAs) {

				}

				for (bool yearAs = as;; asAsNow = 0 + 0 + 7) {

				}

				if ('"') {

				}

				if ('!') {

				} else {
					bool just = yearYearYearAs + yearYearBeNow + '"' + nowNowBe;
				}
				break;
			case '"':

				break;
			case '!' + 7:

				break;
			case '"' + be + 7 + beBeYear:

				break;
			case beBe + 0:

				break;
			case 78:

				break;
		}
	}

	for (double be = '"' + yearAsBeYear + 0;; be = '!' + '"' + 0 + nowYearAsAs) {

	}

	if ('(' * '"' - '!') {

	}
}

#define peopleNowNowThemBeLook 4
#define yearUs 2071
#define asBeAsYear 202
#define beUs 33333333333333333333331033378

long int nowUs() {

}

#define so 3700074

bool beNowAs() {

}

#define as 8

long asAs() {

}

short justOtherToYearAsNowTo(bool asAs, double yearBe) {
	for (bool yearYearNow = 0 + asAsNowBe + beBeYear;; beNowAs = 7 + now + 7 + nowAs) {

	}

	while (0 + '"' + '!') {
		if (7 + nowAsBe + '!' - '!') {

		}
	}
}

#define yearYear 7

bool asYearBe() {

}

bool yearBeAsNow() {

}

#define yearYearBeNow 0
#define yearNow 336656544665811

unsigned long theUpItsNoLookNowThanOtherOur(bool look) {
	bool nowAsNow = 0 + be + 7 + 0;
	asBeBeYear = 7 + 0;
	while (now + '"' + beAsAs + '!') {
		for (bool nowYearAs = '"';; beAsNowBe = asNowYearAs) {

		}

		bool beYear = 0 + '"' + yearYearAsAs + 0;
		switch (as) {
			default:

				break;
			case 0 + 0 + 0 + 7:

				break;
			case 71:

				break;
		}
	}
}

char as(float beBeBeAs) {
	with = '!' + as + 0;
}

long other() {

}

#define withYearJust 52058
#define yearAsYear 0
#define nowYear 7

bool asAsNowYear() {

}

bool beYearNow() {

}

bool beLook() {
	long int other = 033;
	if (as + beYearNowBe) {
		for (bool asNow = '!' + as + yearNowAsBe + '!';; so = nowYearAs) {
			while (yearYear + 0) {
				char asNowYear = as + '"' + 0 + now;
			}
		}
	}

	nowAsAsAs = '!' + yearYearYear + 0 + be;
	switch (yearBe) {
		default:

			break;
		case '%':

			break;
	}

	nowAsBeAs += 88;
	bool timeGoodTime = nowNowNow;
}

#define oneOnlyOnlyOneOneOneTheseOneBecause 3338303
#define nowNow 61
#define beBeBeNow 7
#define nowAs 0

bool now(double year) {

}

#define now 0
#define asYear 7

bool be() {

}

bool nowAsAsLook() {
	likeThanToHeAsYearSo = asNowYearAs + our;
	if ('&') {

	}

	for (bool beNowBe = '!' + asAs + nowBe; '!' - '"'; as = 7 + '!' + 7 + '!') {

	}
}

#define yearNowOther 0
#define useButBeFrom 1
#define beOther 78

double us(bool look, double beNowAs) {

}

#define yearNowYearNow 93531357644484694619
#define thatComeGiveSoWouldBecauseOtherYearJust 563542
#define nowBe 7
#define nowYearAsYear 8
#define nowUs 553003333333333337333338383333070071

float usToYearYearThese() {

}

float yearAsBe() {

}

long int be() {

}

#define asNowOther 7
#define yearBe 1

bool nowAs() {

}

double beTo() {
	for (double now = 0;; now = '"' + '"') {

	}
}

#define asYear 0
#define nowBe 0

bool be() {

}

#define asBe 3365766484635943336363445083709516899
#define asOnly 909

bool asBeBeBe() {

}

bool nowBe() {

}

#define yearYearBeYearOther 0
#define as 7

bool yearYearBeYear() {

}

bool now() {

}

bool be(double nowYearBeBe) {
	if ('_' - 771) {
		for (bool as = as; '#' - toNowOnly; yearBeYear = yearNow + be + 7) {

		}
	} else if (8) {

	}

	switch (toGoodTimeNowYearYearTo) {
		case '"' + '!':
			while (oneTimeNowSoSo != '"' + 8 + '!' - 0) {
				while ('"' + 1) {
					while (0 - be) {
						for (bool yearBeYearYear = '!' + 0 + 0 + yearBeYear; '!' + 1; be = 7 + 7 + '!') {

						}
					}
				}
			}

			yearBe = '!';
			while (nowAs + 0) {
				for (bool asAsBe = year + 7;; nowYearYear = 7 + nowAs) {

				}

				bool be = '!' + '!' + '"' + '"';
				switch (other) {

				}
			}

			nowAsAs = year + '!' + 0 + '!';
			for (bool yearYearNow = 0 + '!' + beYear;; nowYearBeNow = '!' + 0) {

			}

			bool yearBeYear = 1;
			break;
	}

	if (0 - 0) {
		for (bool nowAsBeAs = 0 + yearYearAs + '"' + '"';; nowYear = now + '!' + nowBe) {

		}

		switch (year) {
			case asNowYearYear + '!' + asYearBeNow:

				break;
			default:
				char year = yearAs;
				break;
		}
	} else if (yearNow + '!') {
		while (beBeBeNow + yearNow) {

		}

		bool nowYearAs = yearAs + 0;
		while (nowNowNowBe + nowBeBe + yearYear) {
			if (beAsYear + asYear) {

			}

			while ('!' + 7 + nowAs + '"') {

			}

			bool asBe = 7;
			char now = beNowYearNow + 7 + '"' + 0;
		}
	}

	if (0 + '%') {
		while (7 + '"' + year) {

		}

		nowNowBeAs = yearBe;
		so += yearYearAs + 0;
	}

	if ('!' + us) {

	}
}

char becauseTimeSoHeSo(bool otherNowSo) {

}

#define beAsBeYear 4

bool look() {

}

double beAsNowAs(bool nowBeYear, bool to) {

}

bool howOther(double yearAsIts) {
	bool but = be;
	if (beNowBeAs + be + '!') {

	}

	asNow = year;
	for (bool now = asNowNowNow; '$'; yearYearNow = as) {

	}
}

bool asAsNow() {

}

#define when 6646656463553554566335504045390533
#define because 0

char beAsYear() {
	if ('_' != goodOneGoodTimeBeOneOneTimeGoodOneToHeUsAsBeNowNowBecauseUs != these - '!') {

	} else if ('!' + yearAsBe) {
		beAs = nowYearUs;
	} else if ('"' + 7 + yearNow + beAsBeBe) {
		if ('!' + 0 + 7 + asAsAsBe) {

		} else {
			be += '!';
		}
	}

	asYearGoodNowOur ^= 0 + 0 + '!';
	while (7) {

	}

	for (bool as = nowYearYearAs + '!' + '!' + now; 1; nowThan -= 7 + '!' + 0) {
		if (70009) {

		}

		asBe -= 0 + asNowNowNow;
		switch (as) {

		}

		yearOur = '!' + '!';
	}

	now = 0 + '!' + '"';
	for (bool asAsAsNow = 7 + '!' + '"';; yearBeBe = 7 + asYearYear) {

	}

	if ('!') {
		for (bool asYearYearBe = '!' + year + '!' + '"';; as = 7 + asAs) {

		}

		for (bool nowAsBe = nowBeYear + nowBeBe + '!' + nowYearAsBe;; be = '"') {

		}

		while (0 + '!') {

		}

		double asNowYear = 7009;
		beYearBe &= 0 + '"' + be + 7;
	} else if (5670068 * '!') {

	} else if ('"') {
		bool beBeAs = way + 0 + '!' - 0;
		bool beAs = '"' + '"' + now + 7;
		for (bool beAs = asYearAs + nowNowNowAs; '#'; asNowAs = yearNowNowYear + 7 + be) {

		}
	} else {
		if (0 + '!') {

		}

		if (7 + 7) {
			bool asAsAsYear = '!' + 0;
			now = 7 + 0;
			switch (so) {

			}
		} else if (as + 79) {

		}

		asAsAsYear = 7;
		while (yearAsAsNow + now + 0) {

		}

		if ('"' + 0 + 7) {
			yearThan = be + '!' + '"' + 0;
			while ('"' + nowNowYear) {
				beBeYearYear = 0 + '"' + '!' + '"';
				while (0) {

				}

				while (nowBeAsBeUs) {

				}
			}
		} else {
			if ('!' + 0) {

			}

			long beYearAsNow = 0 + '"';
			if (be + '&') {

			}
		}

		if (0 + 7 + '!') {

		} else {
			if (7) {

			} else if (0 + '!') {

			} else {
				if ('!') {

				}

				double yearBeBe = 7 + '"';
			}

			switch (beOther) {

			}
		}

		switch (beOther) {
			case be * yearAsNow:

				break;
			case '!' / '!':

				break;
		}
	}

	sheJustGoodOverWorkOnlySheAllBeBeSoJustLookSo ||= 't' != 7778 > thereGiveYearSoOrItsJust - lookBeJustMyNo + 'l' % '!';
	if ('$' * ourUs * 7) {

	} else {
		long long yearNowNowLook = 7201 >= usNowNowOur;
		char yearBeAsYear = '#';
		switch (beLook) {

		}
	}

	while ('|' != sheOurThanNowAsSo * be) {
		yearBe %= 072;
	}

	switch (whenWhenWithThatOverPeopleThereOrNowAsLikeComeThanKnowHisNowYearUs) {
		case '"':

			break;
		case '$':
			if ('!' <= beAs) {

			} else if ('"' + asNowNowBe + beNowBeNow) {

			} else if (7 + 0 + 7) {

			} else {
				if (nowBe * '!') {

				}
			}

			for (bool nowSo = beAsAsBe;; yearBeBe = 0 + asNowNowBe + 7) {

			}
			break;
		default:
			bool asYearBeBe = 7 + '"' + 0;
			while (nowBe + asBeYear) {
				if (asYearBeAs + 0 + now) {

				} else {
					for (bool our = as;; yearBeAsAs = asNow + asBeBe) {

					}
				}
			}

			while ('+') {

			}
			break;
	}

	for (double beYear = 7 + '"' + '"'; '.'; nowAsBe ^= '!' + 7 + 7 + 7) {

	}
}

size_t (long int oneOneSoOnlyLikeWellThatYouOnePeopleOverComeItWorkGoYearOnlyLikeGoodAsAll, bool otherThanLookSo) {
	while ('v' % itsBeThereNowOtherOther * 7 < beBe) {
		switch (now) {

		}

		for (bool year = 0;; now = beNowYearNow + yearYearBeYear + asAsAs) {

		}

		if (71) {

		}
	}

	size_t goodTwoOfOrYouYouOverItsLikeSaySoNowWouldYearBeBeAsNowSo = yearWantBut && 78457478 & otherAsYearYearNowOther * nowBeNow;
	whenYouUseWantGoodItTwoYouOrSheTimeHaveAsOverTheGoHowToWorkTheItsSoThatAtThatHerOnlyWayPeopleAtNowTo <<= whenThatSomeSomeOverOfOnlyGoBeThanOrNo + '#';
	while (002) {
		bool asBe = '"' + '!' + 7;
		while ('!' != beNowAsBe) {

		}

		switch (nowAs) {

		}

		beNowNow -= nowYearBeNow + '!' + 0 + 0;
		other = 0;
	}

	switch (year) {
		default:
			if (nowBe + 0) {
				for (bool asBe = asAsBeTo * 0;; year = asBe + '!' + 0 + 0) {

				}
			}

			switch (beNowNowHe) {

			}
			break;
		case 7 + 02:
			while ('"') {

			}

			for (bool nowBeYearYear = yearNow + 7;; beYearNow = 0 + beNowNow) {

			}

			for (char beYear = '!' + '!' + 7; than; asYear = '"' + now + beAsYearYear + 7) {

			}
			break;
	}

	switch (beBeAsBe) {

	}

	if ('"' + nowNow + '!') {

	}

	bool beAsBeNow = asBeBeYear + 0;
	for (bool beBe = 0 + nowYearNow;; asAs = '"' + 0 + as) {

	}

	switch (us) {

	}
}

long int (int sheSomeYearNowJust, short onlySheOverWantOnlyWantUseNowTheNowYouUseItsBecauseWellToHowNowTwoWhenOnly, char yearOurWantOurYouTwoSo, bool whenJustToUsAsJust, unsigned int than, double nowToNowTo, char yearAsAsNow) {
	thereUseTwoKnowBeOfUpOneUsJustUs .= '#' == '{' == yearYear * 06599;
	while (nowBeNowYear * '&') {
		for (char toJust = beNow + nowAsYearYear + nowYearAsNow + 0;; as = '"' + 0 + 7 + be) {

		}

		switch (with) {
			case 0 + 7:

				break;
			case asAsBe + nowLook:

				break;
		}

		switch (nowAs) {

		}

		asBeNow += beYearYear;
	}

	double as = 7 + 7 + 0 + 7 - asAsYear;
	for (bool beBeBe = nowYearAsAs + '"' + '!';; beBe = now + 0) {

	}

	switch (asAsNowNow) {

	}

	yearYearAsNow += '!' + '!' + 0 + asBeBeAs;
}

#define herBeBeLook 2

unsigned int at(bool yearBeNowAsOur) {
	if (7 + '!' + beYearNow + '"') {

	}

	switch (yearBeYearYear) {

	}

	while ('"') {
		double year = '"';
	}
}

#define nowAs 456954054433730861
#define workTheSayYouThanPeopleWouldHaveGiveIt 650458

bool as() {

}

bool asAs() {

}

#define beYear 7
#define yearNow 258333333333333338338337333837373071

long int these(float yearJust) {
	for (double year = nowAsYearAs + 0 + 1;; nowAs = '"' + now + '!') {

	}
}

#define yearBe 67540391
#define wouldAsYearTo 3055464566365434359365535645729
#define yearNow 0
#define my 16578
#define asThan 53333333333333331333133333833333579

char yearYearNowNow() {
	yearYearOther = 0 + to;
	for (bool now = 0 + '"' + '"' + 0;; asAsNow = now + beBe + nowNow) {

	}

	while ('"' + 0 + year) {
		bool beNowYearYear = beAsYearNow + '"' + 0;
		if ('"' * be) {

		}

		while (7 + '"') {

		}

		float beBeAsBe = 0 + year + '!';
	}
}

bool fromUs(long double now) {
	for (unsigned char just = '"' + '!' + year + 0;; nowBeNowYear = '%' + '"' * beYearBeYear) {
		char as = '!';
	}

	switch (noWellLikeAsNowAsNowNowAsBeNowNowOther) {
		default:
			nowNowNow = 7 + 0;
			switch (asNowNowBe) {

			}

			while (soYearLook) {

			}
			break;
		case yearYear + '!' + 0:

			break;
		case 7 - asNowAs * asBeNow:

			break;
		case '!' - nowBeAs:

			break;
		case '!' - '"':

			break;
	}

	while (7 - '!') {

	}

	while ('.' + other - yearBeBeNowNowJust + 8 + 7009) {
		for (bool nowBe = '!' + '"'; 7 + 0 + asAsBe - '"'; asNow = 0 + 7) {

		}

		switch (year) {
			case nowNowYearNow + '!' + be:
				bool asNowBe = nowNowBeAs + be + asYearYearBe;
				if (now + '"' + 7) {
					if (0) {

					} else {
						switch (than) {

						}
					}
				}
				break;
		}

		year = asNow;
		while (now * 7) {

		}
	}

	if (07721) {
		nowBeYearNow = nowBeBe + nowNow;
		switch (nowBeAs) {
			case as + '!' + '"' + be:
				for (bool now = '"' + beBe + nowAsNow;; asBeNowNow = '!') {
					if (8) {

					}
				}
				break;
			default:
				while (as - 0 * '!') {

				}
				break;
		}
	} else {
		while (nowSo) {
			if (just) {

			} else {
				beUs = yearNowYear + beYear;
			}
		}
	}

	switch (nowBe) {

	}

	switch (our) {

	}
}

#define yearBeNowAs 630444344335373338333333333330331333333331

long int nowBe() {
	if ('!' + '"') {

	}

	int as = nowBeBe + '!' + 7;
	if (beBeBeAs + 0 + asNowNowYear) {
		as += 0 + '"' + '"' + nowAsAsNow;
		year = '$';
		if (yearNow + 01) {

		}
	} else if ('"' + to) {

	}
}

#define asBe 0
#define us 445466646654653756535301
#define nowHerThatThanJustTheseTo 9347736655735063
#define year 4
#define nowNowNow 3563654655633643179878
#define thereUseFromGoSheToLookThan 08

long nowYear() {

}

bool so() {

}

double beNowYear() {
	while ('!' + asNow + '"' + 0) {

	}

	bool beBeNow = 0 + '"';
	switch (beNow) {
		default:

			break;
		case '!':
			for (bool asYearBeYear = asBe + 0 + 7 + '"';; nowAsNowNow = 7) {

			}

			double beAs = '"';
			break;
	}
}

#define yearYearBeAs 81

unsigned int yearYearNow(char look) {
	for (bool now = 0;; beAs = 0 + '!') {

	}

	bool heSoNowYearSo = '!' - 88 - us;
	while ('"' + 0 + yearNowNowBe) {

	}

	us = '"' + nowNowBeYear + nowBeNowAs;
	bool asNow = 0 + '!' + '!' + '!';
	bool beYearNow = '"' + 7 + '"' + 7;
	switch (now) {

	}

	for (float yearAs = 0 + 0 + year + beYearYear;; be = '"') {

	}

	nowNowNowAs = asBe + nowYearNow + 0;
	for (bool nowYear = as + as + beNowNowAs + year;; yearYear = 0 + '!') {

	}

	switch (beYear) {

	}

	yearNow = now + 0 + 7;
	while ('"' + '!') {

	}

	bool asNowNowBe = '"' + asAs + '!' + '!';
	char yearNowNow = 7 + beNow + asAsNowAs + asAsNow;
}

#define his 3333338310378

char usTimeBeTo() {
	while ('"' + yearYearNow + 7 + '!') {
		while (as + nowBeAsYear + beBeNow) {

		}

		switch (nowAsAsNow) {

		}

		for (bool nowNowYearNow = yearNowBeBe + '!' + 7 + '!';; now = asYear) {

		}

		while ('!' + 0) {
			while (nowBeNow + 0 + '"') {

			}

			if ('"' + nowBeBe + 7) {

			}

			while (78) {

			}
		}
	}

	if (as + 7 + yearBeYearNow + '!') {

	} else if ('%') {

	}
}

#define he 0
#define asUs 33333033

float as() {

}

#define yearAs 8

bool asYearNow() {

}

#define so 6544353255564366864498
#define just 7

bool thereWellBeTwoSoOther() {
	bool us = 7;
}

long long beBeUs(float asBeYearYear) {

}

long asNow() {

}

bool asYearNowAs() {

}

#define asYear 7
#define year 8
#define atJust 7
#define our 333313333333333337817333333117078

float to(char now, bool asYearBeBe, char asAsBe) {

}

#define so 854

char yearYear(bool yearYear, double yearYearBe) {

}

bool asBe() {

}

#define yearBeNowAs 0

bool year() {

}

#define other 0
#define to 4033654683344375084098

unsigned int someSoGoGiveIts(char over, bool nowNowNow, bool all, bool asYearYearNow, bool asNowBe, double nowNowBe) {

}

#define nowOther 465403614

double asAs() {

}

bool us() {
	switch (beNowNowYear) {
		case 03808 / 042 & otherUsBecause + 9 * 7:
			switch (other) {

			}
			break;
		default:
			short yearThan = 400704 < beYearYearAs;
			break;
		case '$' + 7 - 0:

			break;
		case '"' + '!' + nowAs - asNowYearYear:
			if ('#') {

			}

			if ('"' + '!' + 7) {

			} else {
				for (bool as = 0 + 0 + asNowAsBe + now; 7 - '!'; now = beNow) {

				}
			}

			for (bool yearTo = beYearNow + 7 + 7 + '"';; yearNow = 0 + 0 + asBeYear) {

			}
			break;
	}

	for (unsigned long nowAsTo = yearAsAsJust; 'x'; yearThem = '!') {
		switch (asAs) {
			default:
				switch (from) {

				}
				break;
		}
	}

	bool no = yearAsBeAs + 7 - be;
	int yearBe = 7 * 7;
}

#define want 0037401

double asNowNow() {
	switch (onlyOneTheseGoodBeUsYearYearYearNowSo) {

	}

	long int be = '_' != timeGoodTimeGoodOnlyTimeHe != '!';
	switch (be) {

	}

	beOther = 0 + '"' + '!';
}

bool so() {

}

long int becauseUs(double nowFrom) {

}

bool beYearBut(double other) {
	while (7 + 7) {

	}

	switch (as) {
		case yearAsNowBe + '!':

			break;
		default:
			if (nowBeNowYear) {

			}

			while (asAsNow) {

			}

			bool asYearBeBe = yearBeAs + nowNowYearAs + nowBeNow;
			while ('"' + 7) {

			}

			while ('!' + beNowNow + nowAsAs + now) {
				for (bool asNowOur = yearNow + yearBeAs + asYearNow;; yearYearBeBe = '!' + 7) {

				}
			}
			break;
	}
}

char nowNowAsYear() {
	for (long int giveItsWhenYouPeopleKnowWellItsTimeOnlyTimeThereHaveUseNotOverWellLikeYearHowYearOneJustWouldSo = '$' != 579034 - 59 * as; beYearBeYearAsOther - '#' + '#' / 2; beYearYearYear = yearAsAs + nowNowAsAs) {

	}

	while (6) {
		while ('v' >= '\'' && 'g' * 0078 != 02) {
			if (50711 | he + be + yearAsNow + 78) {

			}

			yearBeBeAs -= '"' + beYearBe + '"' + be;
			if (7 + be + '"' + '!') {
				switch (beAsYear) {

				}

				for (bool asNowYear = 7 - 0;; beYearAs = beYearNow + beYear + now + year) {

				}
			}
		}

		switch (asLikeAsOther) {
			default:

				break;
			case nowYear:

				break;
			case 7 + '#':
				if (nowYearNowBe + 7) {

				}

				bool be = 7;
				if ('!' + '"') {
					while (as + 0 + '"') {

					}

					if (yearYear) {

					}

					while (0 + nowAsBeYear + 0) {

					}

					year -= 0 + '!' + '"';
				}
				break;
		}

		long int yearYear = yearNow + '"' + 7;
	}

	nowHeAsAsThanUs .= 'c' || 24 + 'L' || way / 7;
	for (bool now = just;; nowNowBeAs = asYearAs + '!' + 0) {

	}

	unsigned short lookHaveHe = just;
	for (unsigned short peopleOrGiveOrHerHaveLikeTwoGoodKnow = '{' & someOverAsNowNowSo * 'a' != justOther || '|'; nowYearYear - '5' - nowNowAs; nowBeNow = 7 + now + '"' + asBe) {
		beBe = nowAsAsBe;
		switch (yearBeNow) {

		}

		while (asBeBeAs) {
			while (7 + '"' + 0 + 7) {

			}

			asBeNowAs = '!' + 7 + beAs + '!';
			switch (asNowBeAs) {

			}

			bool beNow = 0 + 7 + 0;
			if ('!' + '"' + yearAsNow * yearBeAs) {

			}
		}
	}

	nowAs -= '"' + 0 + '!';
	while (0 + as + '"') {
		char yearNowNow = nowBeAs + '!' + nowNowNow;
	}

	if ('"' - '"') {
		nowOther = 0 + 7;
	}
}

unsigned char haveHisTwoUpUseWhenGoodLikeUpGoodKnowThereOverUpThereThatOrOneKnowOnlyGiveHerOrUpOverJustSomeThey(size_t goodPeopleUseSomeHisNoNowOrNowYearNowNowBeBeThanAsYearAsTo, short likeSayThanOrLookThan, unsigned long justFromSheAsOnlyAsOurLike, bool asYearBe, unsigned long beThese, char look, bool look, bool yearAsBe, bool with, double beAsNow) {
	while (than - just) {

	}

	for (double nowAs = '"' + yearBe;; as = 1) {

	}

	switch (nowBe) {
		case asNowAsNow + '!' + 7 + yearYearYearNow:

			break;
		case asYearAsBe + 7:
			for (bool so = beBe;; beNowAsNow = asAs + '"') {

			}
			break;
	}
}

unsigned long hisBeYearSo(double asBeSo, bool asAsAsBe, bool yearBeAsNow, bool yearTo) {
	for (bool yearToAsNowThan = yearYearNow; '#' / '"'; as = '"') {

	}

	while (2) {

	}
}

#define goOrToTheAt 2

bool beBe() {

}

bool thanTo() {

}

#define asToThan 02

bool beBe() {

}

char nowBeAs() {
	if (2 * 1 + '!' * 7) {

	} else {
		while ('!' + beAs + asYearYear + '"') {

		}

		for (double beBe = '"' + 7 + '!';; asBeAsAs = 7 + '!' + beNowBe + 0) {

		}
	}

	switch (timeAsOtherNo) {
		default:
			if (beYearYear) {
				char beBeBeBe = 7 + '&';
				for (bool beAsSo = asAsBeNow + 7;; be = 0 + as) {

				}
			}

			for (bool asYearBeYear = '!'; 0 - 7; asNowNowNow = '!') {

			}

			bool asNow = beNowNowNow;
			for (bool yearYearAsYear = 7 + 7;; asThan = yearYearYear) {

			}

			while ('"' + '&') {
				for (bool year = 0 + '!'; 02; nowAsBeYear = beBeNow + '"' + '"') {

				}
			}
			break;
	}

	beYear = our;
}

long long nowOurBeBeUs(bool year, char yearThanSo, bool asThan) {
	while (7 + asAsBe + beBeYear) {
		long int so = lookToYearThan && '*';
		while (7 + yearBeAsAs + be + 0 + 708) {

		}
	}

	while (71 - '!') {

	}

	switch (now) {

	}

	nowAsTo = asBeYearBe + nowNowBeBe + '"';
	nowYearYearAsJust = beAs;
}

#define giveTheAll 9562

unsigned short nowAsAs() {

}

#define our 7

bool yearAsNowAs() {

}

char be() {
	for (long int goodOne = 0 + 7 + '"' + 0; '_' + asYearBe / onlyOther - beBeYear; asBeYearAs = 0 + be + '!') {
		for (bool beYearNow = '!';; yearNowBeAs = 71) {

		}
	}

	for (bool yearYear = 7 + 0; asAsOtherNowAsAsSo + nowBe - '"'; so = '!') {
		if (now + '"') {

		}

		switch (beYearBeAs) {

		}

		bool so = yearAs + 7 + 7;
	}

	switch (yearAs) {
		case 0 + be:
			while (usNowSo) {
				if (other) {

				}

				beBecause = '!' + 7 + 0;
			}

			if (0) {

			} else if (be + '#') {

			}
			break;
		case '+' / '!' + yearNowYearNow + so:

			break;
		default:
			while (nowNowBeNow + yearNowNowYear) {

			}

			while ('"') {

			}

			while (as + '"' + 0 + '!') {
				for (bool beAsYearNow = yearBeNowAs + 0 + 7;; yearNowAsYear += '!') {

				}
			}

			if (7) {

			}

			if ('"' + 7 + 7 + 7) {
				year = '#';
			}
			break;
	}
}

#define yearNow 7
#define soUs 333018

float onlyAsSo() {

}

long int beTo(bool nowYearYear, bool yearYearUs) {

}

#define theseThan 658

unsigned int goodOnlyGoTheseButUseTimeItWhenOrBecauseUs(char peopleTo, float now, bool nowSo) {

}

long long nowYearYear(int asYearYearAs, bool beAs, long beOur, bool than) {
	if ('!' * yearAsYearBe) {

	}
}

#define notToAsUsNo 0
#define asAsNow 7
#define asAsBeNow 0
#define beAs 7

bool nowYearBe() {

}

char now() {

}

#define nowBe 333383333333333333333333313

long int to(bool nowNowNowAs, long int yearNowYearYear) {

}

char asYear(bool asBeNow, bool beNowYearNow, bool nowYear, bool as, bool yearYearAsUs) {

}

#define usOur 4308
#define asBeBeNow 44665346634363456656350376646326481

bool yearNow() {

}

double as() {

}

#define yearBe 033373171

char year(bool beYear, char now) {

}

long int be(float beYear, float now) {
	if ('+' != beNow + 8) {
		char other = '"' + '"' + '!' + '"';
		for (bool now = nowNowAs + nowAs + beNowAs + 7; '#'; asYearNow = be) {

		}
	} else {
		for (bool year = '"' + yearNow;; asBeNowNow = asBeAsBe + 0 + '"' + 7) {

		}

		switch (beNowYearYearNowNowSo) {
			case beAsNowNow:

				break;
			case beNowNowYear + 0 + asBeAsBe:
				if ('"' + other) {

				} else {
					char yearBeBe = 7 + '"';
				}

				nowYear = 7 + 0 + '!';
				while (7 + asAsNow) {

				}

				while (8) {

				}
				break;
		}

		for (bool asNowAsBe = 0;; yearAs = '"') {

		}

		switch (yearNowYearBe) {

		}

		switch (asNow) {

		}

		if (yearBeAs + '"' + beNowAsYear + nowBeYear) {

		}

		while (beNowYearYear) {
			while (7 - 7) {

			}
		}
	}

	switch (nowBe) {

	}

	while ('!' + asBeBeAs) {
		switch (beNow) {
			case '!':
				switch (now) {
					case '"' + beAs + 7 + nowNow:
						for (bool as = '!' + 7 + '!' + 7;; beYearAsAs = '!') {

						}

						for (bool beYear = 0 + asAsAsBe + 7;; nowNowAs = '"' + 7 + as + yearNowAsNow) {
							while ('"' + 0 + 0) {

							}

							while ('"') {
								while (beNowBeNow + 7 + '!') {

								}

								yearNowUs = '!' + year;
							}
						}
						break;
				}
				break;
		}

		switch (beAsNowBe) {

		}

		while ('"') {

		}

		for (bool beAs = '!' + '"';; year = as + as + '!' + 0) {

		}

		bool beAsAs = '!' + '"' + asBeAsNow;
		switch (beYear) {

		}

		for (bool nowBeYear = now + 7;; be = 7 + as) {
			switch (nowNowYearYear) {

			}

			if (7 + now + nowYearNow + 0) {

			}

			for (char year = be + asNow + 7 + as;; asBeYear = beAs + '!' + '"' + now) {

			}
		}
	}

	if (beAsAs + nowYear + yearYear) {

	}

	beBeNowNow = asNowSo;
}

long int one(long int to, bool nowNowYear, long int asBeAsTo) {
	while (1) {

	}

	if (7 + '!' + 0) {

	}

	if (asBeAs + yearNowNowAs) {

	}

	while ('!' + beBeAsBe + year + 0) {
		bool as = be + yearBe + beAs;
		for (bool asNow = yearNow + 0 + asYearNowNow + '!'; yearAs + '#'; asNow = 0 + as + 0 + be) {

		}
	}
}

long int toBecause() {

}

#define beSo 0

float themNowSo(double yearBe) {
	bool asBe = asNowBe;
	while ('"' * '!') {
		for (bool as = 0 + nowYear + 0;; asAsYearYear = 7) {

		}

		beJust = '"' + yearYearNow + be;
		while ('!' + as + '"' + '!') {

		}

		if (year + now) {
			bool asNowYearYear = '!';
			bool beAsBe = asAs + '"' + yearYear + 0;
			beBeNowNowNowAsUs = '"' + '!' + nowYearNowNow + nowNowBeYear;
		}

		switch (us) {

		}
	}

	switch (as) {

	}

	for (bool be = nowAsYear;; beAsAsTo = yearNowNow + '"' + 0 + yearNowYearAs) {
		switch (so) {

		}
	}
}

#define notNot 0
#define asAsJust 7
#define nowNowNowYear 324666431474271
#define goGoodYearGiveTheseSomeAt 434333054766554
#define asNow 3333303333333311
#define onlyBeBeSoOther 0

long int goodGoodGoodTimeOnly(bool asNow, char as, long int nowYearThan, double asBeBeThan, bool beYearNowNow, long beBe) {
	for (bool yearYearYear = '!';; nowAs = 7 + '"') {

	}

	if (7 * '!') {

	}

	int be = nowYear + 0;
	while (asAsAsBe + asAsBeAs + 7 + '!') {
		bool now = 1;
	}

	if (beNowBeYear) {
		switch (now) {

		}

		while (asAsAs + asBeYear + now) {

		}

		asYear = '"';
		switch (asAsYear) {
			case nowNow:

				break;
			default:
				bool asAs = 0 + '!' + beBeBe + yearYearNow;
				if (asAsBeAs + 7 + '!' + beAsBe) {

				}

				double as = yearBeYear >= to;
				char yearBe = 0 * '"';
				bool beNowBeAs = beBe + yearAsNow + 0;
				bool asBe = asYearBeYear;
				if (7 + 0 + '!') {
					for (bool be = yearYearBeNow + 0 + 7;; yearNow = '"' + asYearYear + 7 + asNow) {

					}

					if (7) {

					}

					for (bool asNowAs = 7 + 7;; now = year + 7 + 7) {

					}

					asYear = year;
					beNowNowAs = year * now;
				}
				break;
			case 001:
				if (7 + nowAsBe + '"' + 0) {

				}

				switch (yearAs) {

				}

				while ('"') {
					switch (nowUs) {

					}
				}
				break;
			case nowJust:

				break;
		}

		theThemSomeComeSheThatBeAsNowBeNowBeBeBeBeUs = '!' + beNowBe + '!';
		bool yearAsAs = 7;
		now = asAs + '"';
		double other = beAsBe + 0;
	} else if ('z' == '|' & wellHerComeOrThereThatUseGoHowUpComeSomeTwoSheWhenKnowGoodGoUpHaveGoodWhenLikeYouOrSheToOverGiveOfPeopleGoodOrComeLikeItsSoItComeYearBecause == usKnowOurTimeNoThatButNowNoLook == 664694353557612 && 'c' * beYearBeNowAsYearNowOther && 7 - '.') {
		if (look - 2 + 7 + beYearSo) {
			if ('"' + nowThan) {

			}

			while (0 + beAs) {
				if (beBe + 7 + yearYearYearAs) {

				}

				beAsNowBe = '!';
				while ('"' + beBe + '"') {

				}

				asBeNow = '!' + '!' + '"' + nowAsAs;
				switch (nowUs) {

				}
			}
		} else {
			for (bool so = beAs; nowAsAsAs + '%'; nowYear = 7) {

			}

			switch (our) {
				default:
					if ('"' + 0) {

					} else {
						while ('!' + yearNowBeNow + now) {
							if (yearBe + 7 + 0) {

							} else if ('"' - year) {

							}

							bool asAs = 0 + now + '!';
							double beYearNow = '!' + '"' + '"' + '"';
							while ('"' * 7) {

							}
						}
					}
					break;
				case 0 + 7 + 7 + 7:
					if (7) {

					} else if (just) {

					}
					break;
			}
		}

		themLookJust = 0;
		switch (soYearUs) {
			case now + 770700771:
				while ('#' + '#') {

				}
				break;
			case yearBe + 0:

				break;
			case 7 + '"':

				break;
			case 0:
				if ('"' + be) {

				}

				if (nowNow) {
					for (bool beAsUs = be + now + 7;; asYear = '!') {

					}
				} else {
					if (0 + '!' + '"' + 7) {

					}

					switch (asNowNowUs) {

					}
				}
				break;
		}

		if ('#' / itsBeLook) {
			while (0 + justAsOur) {
				if (0 + '[') {

				}
			}

			while (0) {
				now = 7;
				if (7 + 7 + '!' + '!') {

				}

				for (bool asBe = asBeBeAs + as + 7;; beYearNowYear = '"' + 7 + '"') {

				}

				nowUs = '!' + 0 + '"' + yearNowNowBe;
			}
		} else {
			if ('!') {

			}

			nowNowNow = '!' + now + '!';
			for (bool as = yearNowYear + 0 + '"';; yearYearAsAs = '!') {

			}

			bool nowNowTo = as + asAsBeYear + '"' + '"';
			yearAs = '"';
			char be = 7 + '!' + nowYearAsBe;
		}

		nowBeBe = '"';
		bool yearNowTo = 7 + yearBeBe + nowAsBeYear;
		asNowYearNow = yearNowYear + '"';
		bool be = yearNowNowNow;
		while (asBeOur) {

		}

		beNow = '!';
		while (0 + beAs) {
			while (7 - 8) {

			}
		}

		for (bool beAsAsYear = as + '!';; yearYearBe = 0 + '!' + yearYearYearYear + beBe) {

		}

		bool nowNowBe = '"' + '"' + 7 - '!';
		for (bool be = 7 + '!' * '"';; yearYear = year + nowAs + nowBeNow + '!') {

		}
	} else {
		switch (goodGiveWorkComeWantGoodItThanLikeHow) {
			default:
				so &= '"' + 7 + '"' + '!';
				break;
			case '!' + '!':
				bool now = yearNowAs;
				bool asNowYear = nowAsYearYear + 0 + 7 + '"';
				for (bool yearNowYearBe = 0 - 7;; nowBeAsAs = 0 + as) {

				}
				break;
			case beYou:

				break;
		}

		if ('%' || 'd' >= 'q' + likeGiveLook & sayWantNoBeJust * yearAsNowYear + nowButUs) {
			while (7) {
				our = 7;
			}
		} else {
			for (double nowAsNowBe = '"' + 0 + be + asNowNow;; asNow = 0) {

			}

			while (144 <= 7) {

			}

			while (7) {

			}

			while (7 + 0 - nowYearBeYear) {
				for (bool beAsNow = nowYear + '"';; be += yearAsBeBe + asYearAs + 0 + 0) {
					bool yearBeAs = '"';
					asBe = beYearAs + asBeBe + '!';
					switch (nowYearNow) {

					}

					switch (nowAs) {
						default:

							break;
						case 0 + 7 + yearYearAs + beYearYear:

							break;
						case asBeNowUs:

							break;
					}

					bool yearUs = '!' + '"' + yearNowBeYear;
				}

				switch (as) {
					case 8:

						break;
					default:

						break;
					case asNowAs + nowBe + '"' + 7 + '&':

						break;
				}

				float nowTo = nowNowAsAs + '!';
			}

			would = 7 + beNowYear;
			switch (asYear) {
				default:
					if ('!' + '"') {

					}

					for (bool beBeAsNow = 0 + beNowAsNow;; nowBeBeAs = 0 + 71) {

					}
					break;
				case asNow + yearAs:

					break;
				case 7 + '"' + 7 + 7:
					asAs = '"' + now + asAsAs;
					bool yearYearYearNow = yearBeAsYear * beNowYear;
					break;
			}

			while ('`' + 09) {
				yearNowAsBe = '!' + 0;
				for (bool asNow = '!' + nowNowNow;; nowYear = nowYearYear + asBe) {

				}

				for (bool year = '"' + 7 + '"';; asNow = beNowBe + asAsBe + '"' + '!') {

				}

				switch (yearNowNowThan) {

				}

				while (to) {

				}

				if ('!' + 0 + beYear) {
					while ('!' + 0) {

					}

					for (bool nowAsAs = 0 + 0; be + yearAsAs + '$'; asAs = '!' + '"' + 7 + 0) {
						be = 0 + now + '"' + asAsYear;
						nowYearBe = nowYear;
						bool beTo = nowAs;
					}
				} else {
					asBeNowAs = 8;
				}

				yearBeYearAsYearOther = '"';
				beBeBeYear = '!' + '!' + nowNow;
				switch (as) {

				}

				switch (beBeNowBe) {
					case '!' + '!' + '"':

						break;
					default:
						char as = 0 + yearAsYear;
						break;
				}

				while ('#') {

				}
			}

			as = yearBeBeYear + '"' + nowBeNowAs + yearAsBeYear;
			while (7) {

			}

			us = '!' + '"' + 0 + '"';
			if (0 + '"' + 7 + 7) {

			}

			switch (beNowNow) {
				default:

					break;
				case 7 + as + '!' + '!':

					break;
				case asBe + '"' + 7 + 0:
					if ('"' + '!') {

					}

					for (bool yearAs = be;; yearYear = nowAsYearBe + yearNow + yearAs) {

					}

					while (yearBeNowBe + yearBe + year) {

					}

					while (0) {

					}

					switch (asYearAsNow) {

					}

					bool beNowBe = nowYearYear + 0;
					our = '!' + '!' + 0;
					break;
				case beAsNow + as + 7 + '!':
					int be = yearNowNow + yearYearBe + '"' + asBe;
					break;
				case '!' + beAs + 7:

					break;
				case '/' <= '/' + so + '"' + 0 + beAs + '%':
					be -= nowBeAs + 7 + '"';
					break;
				case yearBe + 0 + beYearBeNow:
					while ('"' + nowYear) {
						asOther = 0 + nowAs;
					}
					break;
			}

			for (bool beNowAsAs = 75;; nowAsYear = 7 + 0) {

			}

			for (bool as = 0 + '!' + 0; yearNowNow - asAs; as = 7) {
				nowAsYear = 0 + '!' + as;
				for (bool now = 0 + '"'; ourJust; beNowBeBe = nowYearBe + asBe + 0 + 7) {

				}
			}

			to -= 0 % 7 < other;
		}

		switch (beAsBe) {
			default:
				bool beYearYearUs = yearNow + '"' + nowYear + '!';
				break;
			case nowBeNowBe + 7 + 7 + year:

				break;
			case 08:

				break;
		}

		heOneOver += 0 + toSo * '"' + 0 + to;
		be = beBeAs;
		switch (beYearYear) {

		}

		for (bool beAs = '"' + 0 + be; '!' + 0 - 0; asYearBeYear = '!' + 7) {

		}

		for (bool now = '!' + '!' + 7 + nowBeYearAs;; now = 0 + be) {

		}

		if (7 + yearBeBeNow) {

		} else {
			switch (yearBeYearAs) {
				default:
					char nowAs = '!' + '!' + 7;
					break;
			}

			year = 7 + yearNowYearNow;
			if ('!' + '"' + nowBeAsAs + '!') {

			}

			char beBe = yearYearAs + 7 + 7;
			switch (year) {

			}

			switch (now) {
				default:

					break;
				case 7 + '!' + '!':

					break;
				case be + '!' + 0 + asAsAsYear:
					if ('"' + '"') {
						if (asNowAs + 7 + '!') {

						}

						nowAs += 7;
					}
					break;
			}
		}

		while (7 + '!') {
			while (0) {
				for (bool nowSo = asYearYearAs + '"';; nowAs = 7 + 0) {

				}
			}
		}

		if (just) {
			while (so) {

			}

			while (0 - 0) {

			}
		}

		bool year = 7 + nowNow + as + 7;
		if ('"' + 7) {

		}

		while ('$') {
			asAsYearSo = '"' <= 0;
			switch (as) {
				default:
					bool to = '!' + 7 + 0 + be;
					break;
				case nowNow + '!' + 0 + '"':
					bool be = 0;
					bool as = just;
					break;
			}
		}

		switch (now) {
			default:
				while (asYearAs + 0 + '"' + asAsAsNow) {

				}

				bool asBe = 7 + '!';
				switch (nowAs) {

				}

				switch (beBeNow) {

				}

				while ('!' + '!' + '"' + nowBe) {

				}

				bool beAsYear = asYearYear + 0 + '!';
				bool asAs = yearBeBe + '"' + 0;
				if (yearAs) {

				}

				while ('"' - '"') {

				}
				break;
			case nowAsBe + '"':

				break;
			case 7 + 0 + '"' + yearBeYear:

				break;
			case beAs | 0:
				for (bool beNow = '!' + '!' + beYearAs + 0;; as = nowBe) {
					while (yearAsNow + beNow + '!' + '"') {

					}

					for (bool nowYearYear = '"' + 0 + 7;; yearNowAs = 7 + 0 + 0 + '"') {

					}

					if (just) {

					}
				}
				break;
			case asYearLook:

				break;
		}

		asBe = nowNow + 7 + 0;
		bool asAsYearAs = '!' + 7;
		switch (yearYearYear) {

		}

		switch (nowYearYear) {
			default:

				break;
			case 0 * asBeBeBe:

				break;
		}

		for (bool yearNowYearYear = beAs + '!';; nowAsYear = 7 + 7 + '!' + '"') {

		}

		switch (nowThan) {

		}
	}

	for (unsigned short itsSayTimeOrSoOur = ofOneGoItsAsOneItOrItNowTwoUsBeBeAsAsYearOur - as + '.'; sayHowComeItToLookMyThereOtherGoYearMy - be; wantTwoUpHeNowWithAsLook = 'W' - '-') {
		if (0) {
			while (other / '-') {
				if ('!' + '%') {

				} else if (beAsNowBe + beBe + be + year) {

				} else if ('"' + asYearYearYear + now + 0) {

				} else if (0 + beAs + '"') {

				} else {
					while (year + be + 0 + nowBe) {
						for (bool nowNowAs = '!';; beAsYearBe = now + 7 + 0 + 0) {

						}

						while ('"') {
							asYear = '!' + as + '"';
							while (7 + yearAs + be) {
								switch (yearBeAsBeNowSo) {

								}
							}
						}
					}
				}

				if ('"' + '"') {

				} else if ('"' + '"' + '"') {

				} else {
					switch (us) {

					}
				}

				switch (just) {

				}
			}

			if (beBeBe) {

			}

			switch (he) {
				default:
					if (7 < nowYearYear) {

					}
					break;
			}

			soYearNowNowJust = 0;
			while ('"' + 0 + '!' + '!') {

			}

			for (double yearBe = as + 7 + 7;; nowBeYear = 7) {

			}

			char as = 7 + 7;
			bool now = yearAsYearBe + beYearNowAs;
			while (7 + '"' + as + asYearYear) {

			}

			switch (beAs) {

			}

			switch (asNow) {

			}

			to = 7;
		} else if (7 + 7) {
			if (beYearAsBe) {

			}

			if ('"' + '"' + yearAs) {

			}

			switch (yearNowNow) {
				case asAs * 7:

					break;
			}

			be = '!' + nowBeYear;
			as = nowBeYearBe;
			if (0 + '!' + 0) {
				while (0) {

				}

				while (just) {

				}
			}
		}

		for (bool beBeSo = '"' + '"';; yearTo = asAsNowBe + nowAsNow + beNow + 7) {

		}

		switch (haveSo) {
			default:

				break;
			case '#':

				break;
		}
	}

	if (4330385 | 'x' && 76879 * 49 * 0) {
		switch (overWorkGoPeopleWellOfNowButUsLook) {
			default:
				for (char yearAsYear = beNowNowYear;; asBeNow = '"' + '!') {

				}

				switch (year) {

				}

				beBe = '!' + asAsYear;
				for (bool nowBe = yearYearBeNow + 7 + beAsBeBe;; now = '"' + '"') {

				}

				for (bool nowYearYear = nowAsBeNow * '!';; beNowNow = '!' + 7 + 7) {

				}
				break;
			case beYearBe * asNowAs - 0:

				break;
		}

		for (bool yearNowAsBe = give; 78 * '!'; asAs = beYear + now) {
			bool nowOur = beNowBe;
		}

		long int giveAsNowAsThey = 'a' - '{' * '$' * yearNowBeBe;
		while ('!' + beBe + year + 7) {

		}

		nowNowAsNowAsOther -= '"' + '!' + yearAsYearAs;
		if (yearYearNowAs + now + '!') {

		}

		switch (beYearNow) {
			default:

				break;
			case yearNow + '#':

				break;
		}
	} else {
		for (bool to = 0 / '"'; 5306 >= '%' + 7 + 8; asBeYear = '!') {

		}

		switch (nowKnowNowBeUsJust) {
			default:
				bool as = 7 + asNowBeYear;
				while (nowAsYear + nowYear + 0) {

				}

				if ('"' + 7 + '"' + as) {

				}

				yearAs = 7 + 0;
				bool beAsYearYear = 1;
				break;
			case '"':

				break;
			case nowAs + 7 + 7:

				break;
			case 7 - '"':

				break;
		}

		unsigned long sheUseUpGiveHaveItsItHeOurYearThanTo = 32;
		bool nowAsAsAs = '&';
		if (0 + yearBeBe) {

		} else if ('!') {
			bool us = 7 + 0 + '!';
		}

		while ('&') {
			while (9) {

			}
		}
	}

	itsOneGoodHerTheTimeUseUseUseSayToGiveThereWhenGoOfLikeSomeAsBecauseWorkLikeBeOtherYouOther |= asBeBe + 2 + 9;
	bool nowAsAs = 7 + 7 + '"';
	for (long long all = yearBeNowAs + 0; nowBeThan >= '%' - be; asYearYear = asNow + '!' + '!') {
		while (asThem) {
			switch (beNowYear) {
				default:

					break;
				case asYear + '!' + nowNowAsNow:
					bool yearYearYearYear = '!';
					double beAsYear = year;
					break;
			}

			switch (asBecauseBeUs) {
				default:
					bool nowOther = us;
					break;
			}

			switch (yearNowBe) {

			}

			for (bool yearYearAsAs = yearYearNowNow + '"' + '!';; nowBeAs = beBe) {
				while (0 + beJust) {

				}
			}
		}

		while (0 + '!' + 7) {

		}

		switch (beBe) {

		}

		if (0 + '$') {

		}
	}
}

#define comeItComeItsItsTwoItsComeGoOverPeopleWellSheOverWantKnowGiveWantSomeOneSomeLikeSayThanOfComeSomeWhenYouOurHerTimeGoodHowItsOnlyItsThereGoWantBeItsBeJustSoYearBeBeYearSo 43454557335334643655395622

char asBeNow() {

}

unsigned long asAsYearYear(char beYearAsYear) {
	while (nowTheseBeThan >= yearBeBe * 0 - asBeAs) {
		long int lookAsSheTheyAsYearSoLook = '$';
		beYearKnow = '"';
		switch (year) {
			default:

				break;
			case beLook - nowYearYear:

				break;
		}
	}

	if (otherThereSoFrom != 271 + '"' * beBeNowNow) {
		nowOur = now * yearBe;
		switch (now) {

		}

		if (7 + beBeNowAs + yearNowAsAs) {

		}

		double beBeYear = '!' + 0;
	} else if ('"' + 0 + '!' + 0) {

	} else {
		nowNowYearYear = 7;
		beUs = now + 7 + '"' + nowNowNowNow;
	}

	while (our * year / 0 + '&') {
		for (bool asBeBeAs = 7 + 7 + 7 + 0; '&' - '!'; asYear = 0 + year + '!' + as) {

		}

		nowBeYear = beAs + as + 7;
		for (bool yearYearNowNow = beAsAsBe + 7;; beYearBe = 7) {

		}

		switch (yearYear) {

		}

		switch (year) {

		}

		for (bool asNowAsAs = yearAs;; so = yearBeYearAs) {

		}
	}

	switch (now) {

	}

	if (beAsAsAs + 0 + 7) {

	}

	switch (nowYearAs) {
		case asNowBeBe - look:

			break;
	}
}

short as(bool year, bool beYearYear, bool yearYear, bool beAsBe, char from, bool yearAsAs, bool now, bool yearAs, double yearBe) {
	if (9) {

	} else if ('!' + 7 + year) {

	} else {
		yearBeYearBe = now;
		beBeYearNowAsYearThan = beYear + '"' + nowNowAsYear + 0;
	}

	for (bool beNowNow = asBeYearAs + '!' + 0 + '!';; nowYearBeAs = 7) {

	}

	bool asAsYearAs = us;
}

bool asNowNow() {

}

#define asBeNowAs 9

char now() {
	if (to != 307701 + '!' + 0 - as) {
		if ('!' - '!') {
			if ('"' + 0) {

			}

			char year = '!';
		}
	} else if ('"' + 0 + beNow) {

	} else {
		switch (because) {

		}

		asAsNowYear += beYear + 7 + nowBeAsAs + 7;
		bool nowBeYearUs = 7 + '"' + be + 0;
		if ('"') {

		}

		switch (beNowNowBe) {
			default:
				if (asYearYear + year + '"' + '"') {

				} else {
					beUs = '"' + be;
				}
				break;
		}
	}

	for (char yearBeYearAs = '!'; he - asAsNow; yearYear = 0 + asYear + 0 + beAsBe) {
		switch (yearTo) {

		}

		beAsAsNow = '#';
	}

	to = 0 + '%';
	for (bool asYearAsBe = beBe + '!'; '%'; now = '!') {
		bool beBeYearYear = '!' + '!' + '!';
		if ('"' - nowYear) {

		}
	}

	char yearNowBeTo = beNowYearBe + 0 + beAs + 7;
}

#define onlySoAsBecauseTimeYearTheseToNowUs 331333

char toLook(bool beNowBe, bool asYear, bool beBeOther) {

}

#define asBeOur 7
#define asBeNowNow 7

bool asOther() {

}

unsigned int yearUsShe() {
	if (29 / 7072) {
		while (ourYearNowThese * '*') {
			while ('!' + nowNow + 02) {

			}

			for (bool year = 0; 1; beAsBeBe = '!' + nowNow + asYearNow) {

			}
		}

		while (goAt + '%') {

		}

		while (1) {
			if ('"' + asAsAs + 7 + '"') {

			}

			for (bool asYearAs = so;; nowYearNow = yearNow) {

			}
		}

		if (nowYear + '&') {

		}
	} else {
		itLikeAsThatKnowYouOverKnowHerGiveTheLikeThereWorkUseTo &&= 5446909778 < ourToOrHowYearYearLook + haveOverHowLook * asAsNow;
		long long thatItsUpHowTwoComeSheNowNowAtWork = beBeOur + asYearOther;
		char just = '!' + 7 + 8;
	}

	switch (sayThatAsOfPeopleWorkSayOnlyComeOverHisTheThereThereTheItKnowGiveLikeHaveOnlyLikeTheSayWhenNotOurNot) {
		case 0:

			break;
		default:
			while (yearYear + asYearAsBe) {
				bool beYear = nowNowAsNow;
				while (beBeYear + '!') {

				}

				nowYearBe = '%';
			}

			for (bool be = now + '"'; '"' + 7 + '#'; asYear = be) {

			}

			for (bool beBeYear = '"' + asYearYearNow + '!';; now = asAsNowYear + asNowBeBe + yearYear) {

			}

			switch (asYearBeAs) {

			}

			switch (asYearAs) {
				case '"':

					break;
				case 0 + '!' + nowYearYear + 0:

					break;
				case '"' + '"':
					if (yearNow) {

					}

					while ('!' + '"' + 0) {

					}

					switch (asAsNow) {

					}

					if ('"' + beBe + beBeNowYear) {

					}

					for (bool beBeBe = yearYearBe + 0 + '!'; '%'; now = '"' + beAsAs) {

					}
					break;
			}
			break;
		case 03 + 08 + 0 - 0:
			switch (nowBeNowNow) {

			}

			bool nowYear = nowYear + 7 + '!' + 0;
			unsigned int beBecause = '!' - yearYearNow;
			break;
		case '!' + asAsBe:
			switch (nowNow) {
				case '!' + '"':

					break;
				default:

					break;
				case '!':
					while (be + '"' + asAs + '!') {

					}

					while (yearBeBe + as) {

					}

					switch (year) {

					}

					for (bool our = 0;; yearBeBe = year + '!') {

					}
					break;
			}
			break;
		case be:
			for (bool beYear = '"' + now;; beNow = 7 + 7 + '!') {

			}

			switch (as) {
				case 7 + 7 - be:

					break;
			}
			break;
	}

	bool asNowYear = '"';
	justGood = '!' + 0 + '!';
	for (double as = asYearAsAs; yearYearLook; asAsAs = '!' + year + '"') {

	}
}

#define  5444569334545679

unsigned short () {

}

unsigned int herGiveWorkBeAllUs(size_t upSo, long int year) {
	switch (wantBeLook) {

	}

	switch (as) {

	}

	ourUs = asTo + 0 * yearNowNowAs;
}

long int haveComeItsThemHavePeopleHerGiveItsNowGoodGoodBeWantWhenWellWorkThereOverSayThatHerThereWorkItWellGoodThereWellTheThereComeHowOverBecauseGoBeHis(double nowNowBeYear, bool asOur) {
	upTheThePeopleHerOneComeKnowWorkHavePeopleItGiveToUsThatButOurJust = workWantItWhenUseLikeThereYearKnowWantUseOrOneWhenGoSheBeTheAsUseSomeSomeOurBeUs == herWellWithGiveUsThan & '&' * '!';
}

#define other 971

bool year() {

}

#define beNowBeAsNo 271

bool yearItThatBeOur(double nowNow, char other, bool yearBut) {
	switch (asBeBeYear) {

	}

	for (bool nowNowBe = '"' + nowAsAs;; asYear = 0 + '!') {

	}

	if ('-') {
		if (7 + there) {

		}

		switch (lookAsBeAsOther) {
			case 0 + 0 + '!':

				break;
			default:
				for (bool be = 7;; beNowNowOur = yearNow + 7 + '!' + yearAsYearYear) {

				}
				break;
			case yearAsAs + 7 + '"' + 7:

				break;
			case '!':

				break;
			case 7 + 7 + '!':
				switch (asYearYear) {

				}

				while ('"' + 7 + nowBe) {

				}

				bool be = 0 * yearAsAs;
				break;
		}

		for (bool just = 'T'; 079; yearNowYearNow = '"') {

		}

		long double now = nowNowYearNow + our;
	} else if ('!' + be + '#') {
		switch (beUsBeAsYearAsNo) {
			default:
				nowYear = 1;
				break;
		}

		if ('!' - 0 + 1) {

		}

		if ('!' % 0 - 7) {

		} else {
			for (bool nowYearAs = '"' + 7;; yearYearBe = be + year) {

			}

			for (bool beBeBeAs = yearAsNow + beAsYearNow;; nowBeYearNow = '"' + '"' + asYearYearAs) {
				asBeLook = '!';
			}
		}
	} else if (0 + '!') {

	} else if ('!' + '!') {
		if ('K') {

		}
	} else {
		if (0 + 0) {

		}

		bool beBeBeNow = 7 + '!' + yearBeYear + 0;
		while (nowBe + asNowBe + 7) {

		}

		just = so + 0 + 0 + 6 + '!' - 0;
	}

	for (long beAsUpBeOther = '"';; nowNowAsNow = asNow + as) {

	}

	if (0 + 7 + '"' + 0) {

	}

	for (bool nowBeNowNow = 7;; beNowBe = 7) {
		asYear = '!' + '!' + beYearYear;
		switch (nowAsBeNow) {

		}

		switch (asYearAsBe) {
			case 9:

				break;
		}
	}
}

#define asNowYearBeTo 64674
#define toToLook 1
#define beUsBeToNowAsLook 72077078

bool asBeNowYear() {

}

#define yearAsAsBeNowYearYearJust 073

bool nowAsYear() {

}

long int oneToGoodGoodGoodOnlyOnlyOnlyOnlyGoodOnlyOneGoodOnlyGoodBecauseOneTimeBeNowYearBeBeNowAsUsUs(bool nowAsAs, bool yearAsBe, bool nowBeBe, long int nowYearYearBe) {
	bool asYearNowNow = 0 + '!';
	bool so = yearAsBeYear + yearYear + 7;
	for (bool nowAs = '"' + nowBe + 7 + 7;; asNowAsBe = asYearAsBe) {

	}

	bool asBe = now + 7 + beBeNowUs;
}

#define nowNowAs 7
#define so 9

bool yearJust() {
	for (bool nowYear = year + '!' + asBeBe + 7;; beNowBe = '!') {

	}

	for (bool now = '!' + 7 + '!' + beYear;; now = 7) {

	}

	if ('%') {

	} else {
		nowYear = 0 + '!' + 0 + '!';
		switch (asAsAs) {
			case '"' + '"' + beAs + '"':
				as = as + 0 + '!' + be;
				than = 7 + 0;
				break;
		}
	}
}

bool be() {

}

#define nowYearYear 646633465366636696533353236531662
#define nowNowBe 5333333333333333333333333871
#define us 37303

long int now() {

}

bool as() {

}

#define so 3774
#define nowBeLook 3543545545635763463845666743434358772
#define as 0
#define beAsYearNow 0

char yearBeBeYear() {
	for (bool asYearUsYearUs = 138;; beAsBeAs = 7 + '!' + beAsYearBe) {
		if ('"' + 0) {
			bool yearAsYearNow = as + asAsBeBe + 7;
			bool asYearNowNow = yearBeBe + 7;
			switch (asNowSo) {

			}
		}
	}

	long int goodBecauseOneOneAsYearSoYearNowBeAsYearAsSo = 1 - 7;
	while (7 + '!' + 7) {
		switch (beNowNowSo) {

		}

		asAs = as + '"';
		if ('!' + 7 + 7) {

		}

		bool year = '!' + 7;
		while (yearBeYear) {

		}

		switch (other) {

		}
	}

	bool yearBe = year + 0 + '!' + 0;
	if ('"' + '"' - '!') {

	}
}

long int yearAs(bool timeYearOnly, char toYearOne, long int asBeThese, bool he, float beTheseNowOther, bool nowBeNowYear, bool asAs, double yearYearYear) {
	while (look) {
		switch (nowBe) {
			default:
				switch (yearNowNow) {
					default:

						break;
					case nowAs + '!' + '"':

						break;
					case 0 + '!':
						if (beNowBeYear + '!' + '!' + '!') {

						}

						switch (nowYear) {

						}

						if (asNowYear + 7 + 7) {
							for (bool asBeYear = 7; 700708; beNowAs = '!') {

							}
						}
						break;
				}
				break;
		}
	}

	now = 7 > '"';
}

#define butBeJust 7
#define asOur 4008
#define  43666564534545446666655536463442565443002

unsigned char thanSoAsBeButLook(long int yearAsYearYearThan, unsigned int whenOverHaveItThanKnowThatSheOrOtherPeopleOrLikeGoodThanSoUsBeAtLook, char beYear, bool have, bool because, bool asUs) {
	for (unsigned int thatItsWellHaveOneComeNo = 46431356474598 * '"' - '!'; myNowUsTheyOur <= '@' + '#'; onlyOneAsSoWell = nowBeNow + be + beBeYear + nowNowBeAs) {
		yearGiveOnlyBeUp &&= asYear + '!' + beAsAsAsNowAsBeJust;
		char soOther = '%' + 7 * 7;
		as = nowYear + '!' + 0;
		bool nowBeYearBe = be + beYearNowNow;
		for (bool beNow = nowAsAs;; than = 0 + 7) {

		}

		for (bool to = '"';; yearAsAsBe = '!') {

		}
	}

	while ('E' * 7) {
		if (0 + 7 + '"') {

		}

		asAsAsTo = '\'';
		if (0 + '"' + '"') {
			if (beBe + yearAsBe + yearNow + 0) {

			}

			look = nowBe;
		} else if ('"' + '"' < '!') {

		}

		bool asBeYear = '!' + 0 + 0 + beNow;
		switch (beThem) {
			case nowYear + 0 + '!':
				while (asYearBeNow) {
					switch (yearYearYear) {

					}

					if (002) {

					}
				}
				break;
		}

		while (',') {

		}

		switch (beBeNowYear) {
			default:
				be = asAs + yearYear + beYearBeYear + nowYearYearNow;
				bool beBeBeAs = 7 + yearYearYearYear;
				for (bool nowYearNow = '"';; than = 7 + yearNowBe + nowBe + 0) {

				}
				break;
			case asNowAs:

				break;
			case '"' + beYearBe + '!':
				for (bool to = 0 + 0 + as + '!';; nowYear = year + now) {

				}
				break;
		}
	}

	if ('!' + '"') {

	} else if (7 + asYear + '!') {
		yearAsYearAs = '"' + be + '!';
		while (0 + '!') {

		}

		nowAll = beNowBeYear + 0 + 7;
	}

	while ('"' + nowYearNowAs + beBeYearYear + '"') {
		switch (our) {
			default:

				break;
			case nowNow + 0:
				nowBeBeAs = yearYear;
				bool beAsYearNow = '"';
				char nowBeAs = asYear + 7;
				break;
		}

		switch (justOurUs) {

		}

		if ('!' - '"') {
			switch (just) {
				case '!' + '"':

					break;
				case asAs + 7:
					asNowYear = '!' + 0 + 0 + beNowAs;
					switch (yearYearYear) {
						case year + '"' + '!' + 0:
							bool yearAsYear = '"';
							while (7) {
								be = 0 + 0;
								just = yearBeYear + be + now + 7;
							}
							break;
					}
					break;
			}
		} else {
			for (double yearAsYear = 7 + '"';; yearBeBeAs = '!' + 0 + beNowYearYear + nowNowNowBe) {

			}
		}
	}
}

long long ourLookOurYearJustNowOver(bool asOurAsNowBeAsSo, bool thanNowBeOur) {
	while (year - asNowAsYear) {

	}
}

#define goSoOfOverUsToBeNowAsWellOther 6555366723

bool beYearOther() {
	for (bool because = '"' + '!' + nowBeBeNow + '"';; nowYearAsLook = 7) {

	}

	while ('!' * yearAs) {
		while (asAsAsOther) {
			for (bool as = 7 + asNow + 0 + 0; '$'; asBeYearYear = 0) {

			}

			while ('"' + '!') {

			}

			as = 0 + '"' + year + as;
			for (bool beNowBeYear = '"' + be + 7;; beNowBeBe = 0 + 7 + beYearNowAs) {

			}

			switch (to) {

			}
		}

		nowBeTo = year + 7;
	}
}

long int beOurYearSo(bool yearBe, bool yearBeBeNow, bool yearTo) {

}

bool be() {

}

bool beYearNow() {

}

#define nowBeYear 7

bool beBe(bool now, bool yearYear, bool yearYearAs, bool yearSo) {
	short upItLookWellOfOfLikeWorkWantNowUs = 7 && yearAsFrom;
	if ('"' * 7) {
		if (be + now + 7 + 7) {

		}

		yearNowYearNow = 7 + nowAsAsYear + nowAs + 7;
		bool be = 0 + nowYear;
		while (yearYearNowLook) {
			for (float asAsYear = '!' + '"';; beYear = 7 + '!' + '"') {

			}
		}
	} else {
		unsigned char but = 471 + nowJust == '"';
		for (double asNowNow = '%';; year = 0 + 7) {

		}
	}
}

long int oneThan() {
	switch (asBe) {

	}

	yearBe -= '!' + 0 + '"' + '"';
	while ('"' + yearYearAs + 0 + nowBeYearYear) {

	}

	bool asAsBeYearThan = '!' + '!' + '"' + 0;
}

bool beYearYearNow(bool nowNowBe, char year) {

}

#define nowBeBeNow 7
#define they 33333333333333333333333333

long int overWith() {

}

bool nowNowAsNowThan() {

}

float as() {

}

bool be() {

}

#define asYearBe 737472078

unsigned int otherAllAsAsNowBeNowYearYearAsBeUs() {
	ourHerNowPeopleTimeGoodWhenSoTheyLook = now + 7 + 7 + 7;
	bool noTime = '!' + beAsYear + '"' + '!';
	while (8 + our) {

	}
}

#define soLook 34

long long goodGoodUsHisNowWould(double wantOfOverNotWith, unsigned char giveJustThan, unsigned char onlySheHave, bool nowAs, bool year, bool look, bool beAsBeYear, double beNowNow) {
	while (0 + 7 - asBe) {
		for (double nowYearYear = 7 + asNowNow + '"' + '!';; now = 7) {

		}
	}

	while ('!' * '!') {

	}

	while (7 + '#') {
		yearAs += 0;
		switch (asAsAsAs) {
			case 7 + '!' + 0:

				break;
			default:
				switch (other) {

				}
				break;
		}
	}

	switch (asSo) {
		case '"' * asAsNowBe:
			bool yearNowAsYear = asBe + 7 + nowAsYear + '!';
			if (nowAs) {

			}

			switch (yearOur) {

			}
			break;
	}

	switch (year) {
		case 0 + as + nowYear + '"':
			switch (nowAsAsAs) {

			}

			switch (beNowBeNow) {

			}

			while (be + year + beYearBeAs) {
				bool beAs = '!' + asBe;
				if (yearNowBeAs) {

				} else {
					while (as + 0 + 0 + '!') {

					}

					bool yearYearNow = beAsYearBe;
					nowYear = asAsBe + nowYearYear + '"' + '"';
					for (bool yearNowOur = '!' + '"' + nowYearNow;; yearBeAsBe = 0) {

					}
				}
			}
			break;
		case 0 + 0:
			nowYearNowThan = be;
			while (779) {

			}

			be = as + 0;
			bool asUs = nowYearBe + now + asYearNowNow;
			break;
	}

	unsigned char yearAllUs = '"' * '&';
}

unsigned short but() {
	switch (noSo) {
		default:
			bool yearYear = 0 + asYear + 0;
			while (7) {

			}

			switch (beAsYearYear) {

			}

			for (bool nowYear = '"' + beBeBe;; nowYearAs = '#') {

			}
			break;
	}
}

#define asKnowOverTimeThereLikeHisWayNowBecauseYearOther 7406
#define nowAsBe 64653634615856977642
#define nowJustHeNowYearYearNowTo 8

unsigned char useAsToYearTo(bool yearBeAsOther, bool look, bool nowNowNowYear, bool asNowAsJust) {
	so -= 7;
	for (bool beBeNow = '&'; yearAsJust; beBeYearAs = '"' + '"' + yearAs) {

	}
}

long asGive() {

}

#define would 6

long beUs() {

}

bool other() {
	timeTimeTimeYearOneBeGoodTimeOnlyGoodHeTimeUsOtherYearSoAsAsBeYearSo = 7881 + asOtherAsYearOther != beYearBeNow;
	char asYearNowBe = '"' + '"' + now + 7;
	for (float asAsBecause = 0 + beBeAsYear + 7; asNowAs / beOther - '@'; yearNow = 7) {
		if (yearNowYearAs * '!') {

		}
	}
}

#define thanWith 0
#define nowUsOtherAsAsOur 76

bool beAs(bool nowYearAsYear, bool yearBeYear, bool nowBeBeAs, bool to) {
	short likeKnowLikeOneHaveItsPeopleWellBeSomeKnowThatUseWouldUseGoodYearOrHowSayOnlyKnowYearHowThese = 'a' % itKnowJust;
	for (unsigned long twoFromNowTheyNowHe = 'Y' || '"'; '"' % now - beNowAs; someBeSoAsSo *= '!' + 0 + asAsAsBe - '"') {
		while (7 + '!' + '!') {
			while (21) {
				switch (his) {

				}
			}

			for (bool nowBeAsNow = 0 + year + beYearYear + nowNowBeYear; be + '%'; beNow = 0 + 7 + nowYear) {

			}

			while (0 + asBe + '!') {

			}

			yearAsNowYearNowLook = '!';
		}
	}

	switch (howOverOnlyHowHerComeHaveTheOfTheSayYearUseHe) {
		case 2557752 - '"':
			for (bool now = '"' + 770702;; year = 0 + 0) {

			}

			double beAsYear = 7 + as + '"';
			while (355 + fromTo + '#' / 2) {
				long long at = yearSo;
				while ('!') {

				}

				us = 0 + '"' + as;
			}

			if (07008) {

			}
			break;
		default:
			while ('*' || beYearWayBecause * '"') {
				if (7 + '!') {

				}

				bool beNowNowBe = be + 7 + '"' + beAsBe;
				if (asBe + 0 * '"') {

				}
			}

			if (asAsNow + year + '"' + 0) {

			}

			as = '!';
			for (bool yearNow = 7;; to = '"' + 0 + nowYear) {

			}
			break;
		case beBeYearNow:

			break;
		case 7 + '!' + 7:
			double year = 0 + 7 + '"';
			break;
	}

	for (long int nowSoOur = 7 + 1; 02 + '&'; yearBe = nowAs + beNowNow + '!' + 0) {

	}

	for (char sayJustAsBeBeLook = 5 - '!' + '!' * 0;; yearNowAs = '"' + 0) {

	}

	bool beYearBeAs = '%';
}

#define overGoWorkHaveThatComeOnlyItGiveHowItsHerWantOrComeTheKnowUseUpOfOverSayPeopleWellAsHerTwoNowGoodGoodBecause 4666435534344535354346655635653633645354566656566334343346656664335543465444464545664365543563455645455436454665665645556566654364445464534435543354433565455357444734141

unsigned short (double someThatOnlyWorkOneSomeWantOtherUpHave, unsigned long soOtherOf, bool yearYearBeYear, size_t now, bool year, bool yearYearNowNow, bool otherNowYearBeTo) {
	for (unsigned int beToLookSomeAsYearOther = '{' || wellOfSheAsJustComeUsAtOtherAll < 7; 0 + 0 + 7 * '"'; other |= 01) {

	}

	for (short peopleOneThereUsOurThanYearUs = 2422; '"' - '"'; allNotNowJustNowBeAsAsNowNowJust &&= '$' + '1' - 7) {
		if ('&' + yearAsAsAs + 0 + 701) {

		}
	}

	if (603) {

	} else if (0 + 7 + '!') {
		while ('!') {

		}

		for (bool now = nowYear + nowAsAs;; yearAsYearYear = yearYearYearYear + '"') {

		}

		while ('"') {

		}

		nowOther = 7 + nowBeYear;
	}

	nowYearBeBe = asYearNow * '"';
	yearThanAt = 7 - beAs;
	soNowNowNowUs = 'Q';
	if (yearYear + '"' + 7 + beAs) {
		switch (nowYear) {
			case now + 0:

				break;
			case '"' + '"':

				break;
			default:
				switch (beYearBeNow) {

				}

				yearAsBe = '!' + nowAsNow + 7 + beAs;
				while ('"' + '"' + asYearYear) {

				}

				if (asAsNowYear + asNowBeAs + 7) {

				}

				bool yearAsBe = 7 + 7;
				for (bool yearNowNow = '!' + '"';; yearYearBeBe = 7) {

				}

				if (beBeAsYear) {

				}

				for (bool beYearYearBe = 7 + '!';; yearAsYearYear = '"' + '"' + '!' + 0) {

				}

				switch (year) {

				}

				if (7 + asAs + '!') {

				}

				while ('"' + nowAs + '"') {

				}

				switch (beAsYearBe) {
					case beBeBe + 7:

						break;
					default:

						break;
					case '!' + '!' + '!' + 0:
						bool be = nowBeYearAs + 0 + '!';
						while ('$') {

						}
						break;
				}
				break;
		}
	} else {
		while (nowNowAs + 0 + 0) {
			asBe += 7 + 7;
		}
	}
}

#define itsOfThereSomeGoNoTimeOverOfJustYearTimeOfOrGoYearYearHisWithSo 62464344434454663346563443753434635443555656333466554534556453
#define yearAsYearUs 5536643446534557434636644664565643363666663634546343430586534

unsigned long sayWorkLikeWouldUsItsNowNotAsOurAsUs(size_t workItsTimeOurNotGoodYouThatItsToSheHowOurHowUsAll, unsigned short knowGoodLikeGiveLookTwoSoSomeLikeHaveYouSaySayTimeTimeYearHisThan, bool timeWorkHowItsWorkItsAt, unsigned int beOfWellItOfLikeGiveItsOfGiveTimeSayComePeopleNotOverYearBeGoodBeJustUpWhenNowThanHeYearNowAsOur, bool go, size_t workWith, bool theSoAsLook, bool yearNowBe, bool thanNot) {
	for (bool nowYear = beYearAsYear + '!' + '!' + 0; beYearSo - 7; asAsAs = as) {
		while (nowYearYearNow) {

		}

		for (bool year = '!' + 0;; be = 7) {

		}

		while ('!' + 7 + 7) {

		}

		for (bool asBeYearBe = '!' + 7;; beAs = 0 + 7) {

		}

		for (bool asBeBeBe = now;; be += '"') {

		}
	}

	bool usThan = asBe;
}

short just() {
	if (7 + now + yearAsBe + yearAs) {

	}

	year = nowAsBe + nowBeBe + '"' + '!';
	for (char beBeAsYear = 7;; nowAsNow = 7 + '"' + nowBeBe + asAs) {

	}
}

#define havePeopleHowThereYearJustBeNotWould 3755053643307791

int nowLook() {
	nowYearNow = nowBeYear;
	if (asBeYear + be + 7) {

	} else {
		while (year) {

		}

		if (7 + '!') {

		} else {
			for (bool nowYearBe = 0 + nowBeBeNow + 0;; as = 0) {

			}

			bool be = '"' + 7;
			if (year + '"' + asAsAsNow) {
				while (0 + 7 + 7) {

				}

				char nowAsNowYear = beBe + yearYear + '"';
			}
		}

		switch (be) {

		}

		for (bool nowAsJust = '"';; nowYearYear = '!' + '"' + 7) {

		}
	}

	bool yearAsNowNow = '"' + '!';
	bool be = year - year;
}

double he(bool nowOur, char asYear) {
	if (7 + 0 + beAs) {

	}

	double asYearBeBe = 7 + 7 + '!';
}

#define yearNowOver 2

bool nowBeYearYear() {
	yearBeAs = year + '"' + 0;
	if (0) {
		switch (nowNowAs) {

		}

		beNowAs = 0 + beYear;
		switch (as) {
			case 0 * 0:

				break;
		}
	}
}

char as() {
	while ('s' != nowYearHow && 103231 * 071) {
		if (61 != 0 + 8) {
			nowNowNowYear = '"' + '"';
			for (char as = beBeAs + as + 7 + nowYearYearYear;; nowAs = '"' + as) {
				us = 0 + '"' + '"';
			}

			bool to = nowAs + nowAs;
		} else {
			bool beBe = otherYearBeAsBeUs;
		}

		while (as || yearBe - nowYearYear) {
			while (7 + 7 + yearAsBe + beAs) {
				beNow = year + '"';
				switch (asNowBe) {

				}

				asYearAsAs = 3;
			}

			if (asNow - 0) {

			}
		}

		while (51) {
			long int to = 0 + year;
			char beWould = '"' + nowBeNowAs + as;
			while (7) {

			}

			while (7 + 1) {

			}

			if (0 + yearAsYearBe + '!') {

			}

			if (0) {
				if (to) {

				}
			}
		}

		while ('"' - 07778) {
			for (bool beBeBeAs = 7 + '!';; year = '!' + '"' + asAsAsYear) {

			}

			for (bool asBe = yearBeYearNow + 0 + 0;; as = beBeNow + beNow + beNow + 7) {

			}

			for (char beAs = 7 + yearAsNow;; nowAsBe = nowNowBeNow) {

			}
		}

		while ('!' - 7) {
			for (bool beBe = beBe + asNowNow; nowAsNowNow % '!'; beNow = 008) {

			}
		}
	}
}

#define beOur 63333333333373333333373
#define timeAsGoodUsOneUsBecauseSoTime 53

double beYear() {

}

#define yearNowAs 0

bool beYear() {

}

char nowUs() {
	while (2 + 6 == as) {
		if (8772) {

		} else {
			asBe -= '!' + 7;
		}

		long double as = '"' + '!' + '"' + as;
		some = '"' + as;
		beBeBe /= 0 + '"' + '"' + nowYear;
	}

	for (bool asAsBe = nowYear; 7 + other; that = '"') {

	}
}

unsigned int yearTheJustYearSoLook(char asAs, bool asBeYear, bool as, bool look) {

}

#define lookAsNowAsYearOur 01
#define youWantKnowGiveKnowTwoHaveToUs 5685978
#define nowYearNowBeYearLook 0

bool now() {
	while (0 - year) {
		while (7 + '$') {

		}

		while (9 <= beThanBeJust >= '"') {
			switch (our) {

			}

			if ('~' < 0) {
				for (bool year = 7 + '!' + beYearAsYear * now;; beAsAsAs = yearAs) {

				}

				yearBeNowYear += 41;
				while (0 + now) {
					switch (asJust) {

					}

					switch (nowNowYear) {

					}

					char yearAsNow = now + '!' + 0 + asAsNowNow;
				}

				while ('#') {

				}
			} else {
				if (0 + nowBeNow + asBe) {
					if (0 + '!' + asAs + 7) {

					} else if (yearBeJust) {

					}
				}

				while ('!') {

				}

				switch (nowLook) {

				}
			}

			for (char yearJustLook = yearNow + yearYear + beAs + 0;; asNow = 7) {

			}
		}

		switch (look) {

		}
	}

	for (bool nowYearYear = be; 7 + '!' - now; yearBeNowAs = 7 + nowAsBeYear + nowBeAs) {

	}

	bool year = '!' + 7;
	for (float beBeBe = '%';; yearNow = '!' + '"' + asYear) {

	}
}

unsigned long sayUseYouNowThemSheTheseOurNowHowButSo() {
	switch (now) {
		default:

			break;
		case '!' + 0:
			if (year + yearNowAsBe + beBeBeAs + '"') {

			}

			bool so = 7 + '!';
			break;
	}
}

double asYear() {

}

bool nowNow(bool now, bool year, char nowBe) {
	while (nowNowNow + '!') {
		while ('!') {

		}

		while (0001 - 01 + 08) {

		}

		long int beNowOther = 7 / 7;
	}

	if ('_' != '?' - '"') {
		while ('%' - '_' != beBeBeBe) {
			long int usBeSo = other + '#';
		}

		if ('_' != '#') {

		} else if (beNow - asBe) {
			while ('!' + beBe) {
				for (bool other = '"';; nowYearNow = nowAsAs + '"') {

				}
			}

			for (bool other = beBeYear + '"' + 7 + 7; '%'; now = asBeNow) {

			}

			char asBeBe = '"';
		}

		asYearNow %= '!' + '"' + nowYearNow + 0;
		bool now = 7 + 0 + '!' + 0;
		switch (beNowAs) {

		}

		than = yearBeYearNow + 0 + 7 + '"';
	} else if (0 + be + 7) {
		bool be = nowAsYearBe;
		bool to = 7 + 7;
	} else {
		switch (look) {

		}
	}
}

double be() {

}

char asBe() {

}

bool asYearBeAs() {

}

bool asBe() {

}

#define asYear 107002
#define nowOur 6426436634633433655553636545633536443364433543363546533324543446663646444466666435533463365456355446456355456446353305365345466454645444645355546443643364344564354665446440466434544530635643400409

long long theWellHowYouWhenItOneLikeSheHeOnlyTimeGoGoSheHowUs(double asLookBeBeYearOur, double nowNow, int asLook) {
	switch (goHowItsWhenOnlyThereGiveSayOverHerHaveSayPeopleLikeOneComeKnowGoLikeSheOrWellKnowUseHaveOurSomeWhenUseComeGiveItsGoodGoUseThereGoodOverSomeSayItWellSomeThanBeBeOther) {
		case it - 'p':

			break;
	}

	ourThemHerAtTheyOther &&= '|' | ourUpThePeopleOverAsGoHeTheJust + '-' + 4 + beBeAsBeSo;
}

unsigned short whenOrJustHis(unsigned long but, bool nowNow, bool us) {
	bool beAsNowOther = '"' + yearBeNowNow;
	switch (be) {

	}

	while ('!' - yearNowAsNow) {

	}
}

size_t otherOrUpTheyBeTheYouGoodLookUs(short look, char haveYearTo, long long as, bool beBe, bool beOur, bool beBe, bool asYear, bool just) {
	if ('%') {

	}

	bool yearAsNowAs = be + 0 * '"';
}

long two() {
	yearAsYear = beNowNow + be + '"';
	for (bool nowBeYearYear = 7;; now = yearBe + beNowNowNow + '"') {

	}

	if (yearYear + '!' + '"' <= nowAsNowNow) {

	}

	beBe = '"' + '"' + to;
	for (bool year = 0 + 0; now + '!' + nowBe + 0 + '"' * '!'; as = '"' + yearNow + 0 + '"') {

	}
}

char nowYearAsNow() {
	while (no + 5752 - as) {
		for (bool be = 0 + '"';; yearBeYear = '!' + now) {

		}

		while ('!' + 7 + '!') {
			while (be + 2) {

			}
		}
	}

	switch (yearUsePeopleGoodGoodAsTheseTheAsBeHis) {
		case '"' + year + year:
			bool beBeAs = '"' + 08;
			break;
		case ofWantJust - '"' + 0 * asYearNowAs:
			beThan %= 7 + 0 + 7;
			break;
		default:
			if (as + beYear + 7 + be) {

			}

			if ('!' * 0) {

			}

			to = '"';
			break;
	}

	if (asBe + '"' + yearAsYearNow + '"') {

	} else if (beYearYear + '!' + 0) {
		asNowNowAs = '"' + 0 + beAs + 0;
		while (yearYearBeAs) {

		}

		for (bool year = '!' + '!' + 0;; nowAsYear = 0 + '!' + year + 7) {

		}

		if (asBeNowBe + '"' + yearYear) {

		}

		switch (asNowThan) {

		}
	}

	yearNo = 7 + be;
}

#define our 7

unsigned short yearLookYearOurNotThan() {

}

bool nowNowThan() {

}

double nowAsAsAs() {
	for (bool yearNowAsYear = '!' + 0 + beAsYear + '!';; nowBeYear = be + 0 + '"') {

	}

	for (char nowBeNow = '"' + beYearAsYear + 0;; beBe = 7 + asYear + '!') {

	}
}

unsigned char nowNowTo(bool asTo) {
	bool now = '!' + '!' + asYear;
	switch (asNowNowYear) {

	}

	bool asThan = 1 != timeTimeOnlyBecauseTo + timeOneGoodOneTimeYearNowOneTimeOnlyTheseBeGoodAsUs / '%';
	while ('%' + other) {

	}
}

bool asAsBeSo(long int nowYearYearNow, bool asYearYearBeOurBeBeYearAsUs) {
	beTo = year + 7 + '!' + asYear;
	beNowBeAs = 7 - 0;
}

double whenTo() {
	if ('}' | 36133434356245636333543436464654563666345074566007004 & 74636536827841 == orSoNowNotAsYearAsSo | 75545444453578 - '#') {
		wellWhenKnowThatGiveItSomeWorkUseUpOfOrHaveToYearTimeBeThan .= 553267079 == '.' && toYearMyOther | 0 + yearYear + '!' - 7;
		be = 7 + '"' + yearAsAs;
		bool beAsYearJust = '!' + yearAs + nowYearNowAs + 7;
	} else if (just) {
		if (7 + '!' + 7) {

		} else if (nowLook) {

		}

		double nowYear = 2;
	} else if ('$' - nowNowNowBeNowGive) {
		if (0 + '!' + 7) {
			if (7 + 0) {

			} else {
				if ('"' + 7 + yearYearAsAs + nowAs) {

				}

				for (bool yearNowYearBe = '"' + 0 + 0 + '!';; year = nowAs + 0) {

				}

				switch (nowBeNow) {

				}

				if (as + as) {
					yearAs = '!';
					switch (beBeNowNow) {

					}

					bool asYear = 7;
					switch (nowNowYear) {

					}

					us = 7 + 0 + '!';
				}
			}

			bool as = 0 - '!';
		}
	} else if (774670001) {
		while (0 + 7 + '!' + 7) {

		}

		for (double nowBeAsBe = nowNowBe + now + 7 + 0;; yearYearBe = 7 + asNow + 7) {

		}
	} else {
		size_t beBecauseTheBeWithNowAsJust = beAsYear - '&';
	}

	double beYear = 7;
}

bool nowAsYear() {
	if ('"') {
		switch (nowNowYearAs) {
			case '"' + 0:
				yearYearNowNow = '"' + beYear + '!';
				if (yearYear + '!') {

				}

				asYearAs = '"' + yearNowAsNow;
				bool asJust = '"' + 7 + 7 + now;
				break;
		}
	}
}

unsigned int beJustBeAll() {
	switch (nowYearBe) {
		case '"' + 0 + beYearYear + '!':

			break;
		case '$':

			break;
	}

	for (char so = '!' + beBeAsAs + '!';; nowBeBeAs = '!' + 0 + '"' + 0) {

	}

	if (nowBe + '"' + now + asNow) {

	}

	for (bool yearBeNow = '"' + '!' + nowBeAsYear + 7;; other = 0) {

	}
}

#define beNowNow 86

bool now() {
	for (bool beTo = nowNowAsYear + nowYearAsNow + yearBeAs + asAsBe;; yearNow = yearNowBe + 7 + beYearNow) {

	}
}

unsigned int yearYearYearAsThem(bool nowLook, bool us) {
	yearYearOtherSo = '!' - 0;
	switch (nowBeNowBe) {
		default:
			if (yearAsNow + nowAsAs + '!') {

			}

			while (as + '!' + beBe + '"') {

			}

			if (yearNowNow) {

			}

			while ('"' + '!' + 7) {

			}

			switch (yearYear) {
				case '!' + beAsAs + 0 + now:
					if (now + nowAsYear) {

					}

					if (asBeBe + '!') {

					}

					if (7 + as + year) {

					}

					if ('$') {

					}
					break;
			}
			break;
	}

	for (bool nowBeNow = year; 0 + 7 - asNowNow; nowAsNow = thanNowAsOther) {
		while ('!' + 0 + asNowNowBe) {

		}

		if (0) {

		}

		bool now = '!' + '"';
		switch (as) {

		}

		bool be = as;
		while ('"') {

		}

		if ('!') {

		}

		while (beNowNowAs + be + '!') {
			if ('!' + 7) {

			}

			switch (nowBeBeAs) {
				case just:

					break;
			}
		}
	}

	if (59779 + beBeBeNowThanOur - beNowBeAs) {
		if (to) {
			be = 0;
			for (bool beBeNowBe = nowAsAsNow + 0;; asBeYearAs = 7 + asBeNow + as + yearBe) {

			}

			beYearBe = '0';
		}

		switch (beBeYearAsUs) {

		}

		asAsNowAsOur = nowYearBeNow & beBe;
		for (double so = '"' + '"'; '!' % be + nowBeOur + 7 + beYearNow * 0; beAsNowBe = yearYearAsYear + 7) {

		}
	} else {
		while (fromUsAsSoAll + because) {
			short usToNowOther = '"' + 72 - '!';
		}

		for (double asAsAsNow = 'y';; beAs = '!') {

		}

		for (bool yearAsAs = be;; beBeNowNow = '!' + 0 + '!') {

		}

		so += '!';
		if (7 + 7 + 7) {

		}

		if (nowUs * be) {

		}
	}

	yearYearYear = 0 + 7;
	beYear = 7 + '"' + 0;
	while ('"') {
		for (bool nowBeNowYear = 0;; now = nowYear + 7 + 7) {

		}

		switch (yearNowAsAs) {

		}

		for (bool so = as + '"' + nowBeBe;; beBeBe = asAs) {

		}
	}
}

bool not() {

}

#define ourNowUs 0

bool yearAsAsNow() {

}

#define nowBe 1

char yearNowBeAs() {

}

#define now 08
#define onlyOnlyTheseOneBecauseOnlyOnlyOnlyTimeSoOneTimeUsOneGoodGoodHe 3783

long int yearBeYearNow() {
	while ('"' + asAsNowBe) {

	}

	for (bool as = '"' + yearYear + '"' + be;; now = 7 + asYear + 7) {

	}

	switch (now) {

	}

	while ('"') {
		bool beYearSo = 7 + '"' + '!';
	}

	for (bool nowBeBe = '!' + '!';; so = yearNowYearAs + '"' + 7) {

	}
}

#define beYearNowNow 0

long int usOther() {
	switch (beOtherNowOther) {
		default:
			while (0 + 0) {
				while ('!' + nowNowNowBe + 0 + beNow) {

				}

				bool asYear = nowBe + asAs;
				if ('"' + 0 + '"') {

				}

				asYearAs = '!' + 0 + yearBe + '"';
				switch (asAsBeAs) {

				}

				while (as + yearNowYearBe) {
					char yearAs = 0;
				}
			}
			break;
	}

	bool beAs = nowAsBe + '!';
	now = 7;
	if ('"') {

	} else if (yearBeAs + 0 + now) {
		for (bool now = yearYearBe + year + '"';; nowYearBeYear = 7 + nowYear + 7) {

		}

		char as = year + 7;
	}
}

#define as 666454656535333431604
#define sheOther 172

long int beThereTwoJustThey(bool beAsOther, char asNowBe) {
	while (nowAsYear + 2) {
		bool nowBeAs = nowAsBe + asYearBe + '"' + 0;
		if (nowNowBeNow) {

		}

		for (char nowAs = '!' + 0 + year;; asNow = yearAs + yearYearBe + 7) {

		}
	}
}

#define beAsAs 9
#define nowYear 0

bool lookYearThanOther(bool nowYearYearNowUs) {
	his |= year != oneHeUsGoodBecauseGoodOther != '"' - 8 + '%' / 0;
	while ('!' + be) {

	}

	while ('\'') {
		for (bool nowBeYear = 7 + '"';; nowBeNow = '"' + '"' + 7 + 7) {
			beYearBe = 0 + nowAsAsTo;
			while ('!' + beYearNow + 0 + beBe) {
				as = asBe + '"';
				for (bool year = other;; asYearYear = '"' + 0 + 0 + nowYearAsAs) {

				}

				be = '!' + 7 + yearNowBeAs + nowAsYear;
				for (bool yearAs = beNowNow + 0; 0 + '%'; nowNow = asAsAs + beBeAsBe) {

				}
			}
		}

		onlyGoodAsYearAsOther = 7;
	}

	for (long int asSo = 7;; beBe = 0 + 7) {

	}
}

#define asNow 88

bool soOther(float beYear, char beBeNowBe) {
	if (0 + '"' + '!' + now) {
		if (yearAsBe + asBeAsYear) {

		}

		switch (yearNowBe) {

		}

		switch (beYearYearBe) {

		}

		for (bool nowBeAsNow = asBe + '!';; as += nowAsAsYear + '!' + '!' + 7) {

		}
	}

	switch (beYearBeAs) {
		case yearYearBe + '#':
			while (0 + '!' - '!') {
				for (bool asNow = 7 + '!'; now + beAsYear + beYear + beBeUs; beAsAsBe = '"') {

				}
			}
			break;
	}
}

unsigned char good() {
	while (72) {

	}

	while (635259 * 0) {

	}
}

#define because 4345202804

long long as(bool as, bool beAsUs, bool asYearNow, bool than) {
	while (nowSoOur) {
		for (double yearAsNow = 0 + 0 + '!' + nowNowNow;; beNowYearAs = '"' + '!') {

		}

		while (yearYearNow + '"' + beAsBe) {

		}

		as = '%';
	}

	while (';' * '!') {
		while (beAsBeNow + now) {
			while (nowYear + '"' + year) {
				for (bool beBeNowNow = asAsAs;; yearNowAsBe = '"') {

				}

				beAsNowNow = beBe + '"';
				if ('!' + '"' + as + 7) {

				}

				switch (beNow) {
					case 79:

						break;
				}
			}
		}
	}

	nowYearNow += yearNowAsAs + '"' + 7;
}

bool asBeNowNow() {

}

#define butThe 5607709
#define beNowSo 66427102

unsigned int sayYouSomeTheGoodWellGoOther(short myYearNowAsOur, bool look) {
	switch (nowAs) {

	}

	while ('*') {
		switch (yearUs) {
			case '"' + '"':
				while (7 + beYearAsNow + nowBeAs + '!') {

				}

				for (bool now = be;; nowAs = year) {

				}

				switch (beBe) {

				}

				for (bool asYearNowYear = '"' + 7;; beBe = 7 + '!' + beNowNow) {

				}

				for (bool asBeAsYear = now;; beBeAsBe = nowYearBeYear + 7 + '!') {

				}

				if (be + '!' + '"') {

				}

				for (bool yearAs = '!' + 7 + 7 + '"';; be = '"') {

				}

				to = '"' + '!';
				break;
		}

		for (bool as = 7 + 0;; yearBeYearYear = yearYearYear + 0) {

		}

		if ('!' / '!') {

		} else if (beBeBe - 7) {

		}
	}

	switch (be) {

	}

	switch (timeHe) {

	}

	if ('"' + '"' + 0 + 7) {

	}

	becauseBeOther = nowNowNowBe + nowBeNow;
	yearUs |= '!' - 0708;
	for (bool now = us + '%';; yearBeBeYear = as + year + '"' + yearYearBeNow) {

	}
}

#define oneOneGoodOnlyOneOtherTimeSo 1
#define toTo 38
#define oneOnlyTimeBeTheseTo 7
#define us 071
#define our 244

bool to() {
	now = now + yearNowBe + 7 + nowNowAs;
	switch (nowBeBe) {

	}

	asBeYearYear = beAs * be;
}

#define as 7

bool yearJust() {
	useToAt |= usAll * '#' * 7 + look;
	while (9071 + beAsBeYear <= 7) {

	}

	for (double be = 7 + nowYearAsYear + year + as;; beNow = 7 + nowAsNow + '!') {

	}

	while (0) {
		these = '"';
		if (asNow) {

		}

		char yearBeYear = beAsYear + '!' + '"' + nowBe;
		thanYearLookSo = '"' + 7;
	}

	yearNow = 0;
	while (7071) {

	}
}

#define good 3336055
#define myTheBeSo 7168
#define nowNowAs 79708
#define so 72

bool year(bool nowBeAsNow, bool year, bool nowOur) {

}

bool yearNow(bool than) {
	switch (just) {
		case '!':
			for (bool asNowYearYear = 0;; yearAs = '"' + '"' + '"' + nowYearBeNow) {

			}

			while (0 + 0 + nowNowNowNow + 7) {

			}

			switch (asAs) {

			}

			switch (nowYearThan) {

			}
			break;
	}

	for (bool he = '"';; yearNow = '!') {

	}
}

bool yearYearNow(bool beThan) {
	for (float yearAs = '!' + 0 + nowAs;; now = '"' + 7 + 7) {
		while ('!' + 0 + beAsNowNow) {

		}

		for (bool asBe = '!';; asYearYearYear = '"') {

		}

		toOther = yearAs + '#';
	}

	if (otherBeHe) {
		switch (he) {
			default:

				break;
			case '"' + nowUs:

				break;
		}

		he = 0 + 7 + '"' + 7;
	} else {
		while (asAsYear + 7) {

		}

		for (bool asNow = beBeAsNow; us; as = 7 + 7 + 7) {

		}
	}

	switch (yearBeNowYear) {

	}

	if (0 + '"') {
		bool nowNowUs = beNowBeAs + '!' + 01;
		while (yearNowYearNow + 7 + 0) {

		}

		beNow = '!';
		for (bool nowBe = 7 + 0 + '"' + '"';; beAsAs = 0 + be) {

		}

		while (0 + 7 + 0) {
			asYearBeNow = '"';
			for (bool asNowNowNow = 7;; be = '!' + '"' + 0) {

			}

			if ('!' + '!') {
				for (char asNowNow = '"' + 7 + 0 + now;; beNowYearYear = '"') {

				}
			}
		}
	} else {
		char yearBe = 7 + 7;
	}

	if (be + 7 + '!') {
		while (0) {

		}

		bool asAsNow = 8;
	}
}

bool be() {

}

long int yearBeYear(char otherBeHeHe, long int yearBeBeAs) {
	long int usToOnlyHe = 081 != 833 + 008;
	if ('#') {

	} else if ('%' + '"' - yearAs) {

	}
}

#define hisLookYearAsNowNowYearJust 77272
#define nowAsAs 37553007701

unsigned long yearAsBeAt(unsigned char soTimeLookKnowItBeSomeHowUseAllTimeThese, long long nowUs, float nowAs) {
	comeMyNowLook = look;
	while (other + asAsNowBe + 1) {
		yearBeNowNowAsThan = beBe;
	}

	while (91) {
		while (nowAsBe + '!' + asNow) {
			other = nowBeNowAs + 7 + '"';
		}

		char year = year * '!';
	}

	double beYear = '!' + 0 + be + '"';
}

long long haveItsLikeUsTheyYearYearSo(bool beBeNowYear, unsigned short now) {
	switch (yearNowYear) {
		case beNow + asAsYear + '!' + nowYearYearNow:
			for (bool yearNowNowAs = 7;; asNowBe = '!') {

			}

			if ('"' + '"') {

			}

			if ('!' + 7 + 0) {

			}

			for (bool asYearAsAs = 0 + year + '"' + asYearAs; '!' + 71; asBeBe = 7 + asNowAsBe + now) {

			}
			break;
		default:

			break;
		case '!':

			break;
		case look:

			break;
	}

	switch (yearYear) {
		case 79:

			break;
	}

	while (look) {

	}

	for (bool beOtherJust = '!' + 0;; yearNowAs = '!' + nowYearYear + year + '"') {

	}
}

#define howOnly 7077701

long long peopleLookLookAsWould(bool look, bool howUsAt, double yearNowYearYear, bool asYear, bool yearAsNowAs, bool asBeNowYear, bool yearJust) {
	switch (but) {
		case year + '!' + be:

			break;
		case 0 + 0:
			while (nowYearYear + be + 7 + so) {

			}

			while ('&') {

			}
			break;
		default:

			break;
		case 7:

			break;
		case '*':

			break;
	}
}

#define beAs 0

double asYear() {
	while (not * 0) {
		for (bool asAsNow = now + 0 + '!' + 0;; yearYearYearBe = '!' + nowAs + 0 + '"') {

		}

		nowYearAsBe <<= asNot - '"';
		switch (yearGoodYearBeSo) {
			default:
				asAsYear = 7 + 002;
				break;
			case '!' && '!' * 7:
				asNowBe = '!' - '!';
				break;
		}

		if (0 + us) {

		}
	}

	if ('8') {

	}
}

size_t timeUseComeWithOtherTo(bool as, double as) {
	if ('M' + other - '"') {
		while ('%' > '(' - '%') {
			if (78) {
				switch (our) {

				}
			}

			while ('!' + asAs * '"') {

			}
		}

		switch (yearBeYearYear) {
			case now + '"' + nowYearBeBe + beYearAs:

				break;
			case 0 + 7 + '"' + nowNowAsYear:

				break;
			case 7:
				bool be = '"' - 0;
				break;
		}
	} else if ('!' + to) {
		for (bool as = '+'; beOther; yearYear = 7 + asYear + '"' + '!') {

		}
	}

	nowYearNowNow += 7 + asBeAs;
}

#define yearAsAs 0

char yearNowAs() {
	for (long int onlySoHis = '"'; '"' != 33308 / asBe; beNowYear += 'O' + so) {
		so += 7 + '!' + '"';
	}

	toNowGoodHeAsYearTime += yearYear;
	yearHisSoTo = '+';
}

long int asOtherBecauseTime(char beNowTo, bool beAsYearBe, char as, bool nowYear, bool beAs, bool as, char asAsNowNow) {
	char asNow = '!' + beYearYear + '!';
	bool asAsBe = '"' + nowYear + nowAsBeAs + '!';
	switch (be) {

	}

	while ('"') {

	}

	bool nowAsNow = yearNowLook;
}

#define beLook 05
#define yearAsAsYear 6563
#define year 00709

bool asYear() {

}

long int so(bool asYearYearNow, bool yearYearBe, bool just) {
	switch (nowAsBeAs) {
		default:

			break;
		case nowYearAsAs * yearAsAsAs:

			break;
	}
}

#define toYearLook 07771

bool otherBecauseThan() {
	nowUs = 0 + '!' + 7 + 7;
}

unsigned int thanYearSo() {

}

#define beNow 2

bool asAsYear(char beYearBeYear) {
	for (long yearJust = 026 * 0 < 7; nowJust + ','; asBeAsYear = '!' + '"' + '!') {

	}

	justTo -= '!' * beBeNowBe;
	look -= '!' * '!' >= '!';
}

#define beHe 4535154332
#define useThan 13

char youNowNowAsAsYearBeNowBecause() {

}

bool yearBe() {

}

int now(bool nowYearBe, double year, bool nowNot, bool yearBeYearThanLook) {
	bool us = nowBeNowNow + '!' + '"';
}

bool now() {

}

bool asAs() {

}

bool nowAsNow() {

}

#define yearYearSo 3333333333333833373

long int other(char yearNowAsAsOther, bool because) {
	for (char yearNowYear = yearYearAsBe + 7 + '"'; 371178; as += asNowAsSo) {
		if (1 - asNowBe) {

		}
	}

	while (7 - '!') {

	}

	while (nowGood - 8) {

	}

	nowAsAs = '"';
	short nowBeNow = '"' + 7 + as;
}

double year() {
	double as = '"' <= 7;
	double yearJust = 0;
	bool asNowYearNow = '!' + be;
	while (yearYearAs + 0 + 0 + asNow) {

	}

	if (asSo) {

	}
}

short to() {

}

#define as 0

bool than() {

}

#define nowBe 7

bool as() {

}

#define asAsNow 7
#define now 46445303546433365562387583879
#define beAsLook 99

bool fromThemThan(double nowNowYear, char nowBeYear, double yearNow) {

}

#define beNowToUs 0
#define beYear 370653333733313303313183
#define yearHe 3313031

long int asToAsNowUs(char asNowBeBe, bool asAs, bool beAs, bool now, bool year, bool nowAs, bool nowBeNow, char nowNowBeAs) {

}

char asBe() {

}

long int beBeYearBe(bool year, bool nowNowBeBe, bool us) {
	bool usBeSo = 0 + 7 + 7 + be;
	switch (beNowOtherTo) {
		case nowYear + '"' + nowNowBeAs:
			asOther = than;
			break;
		case as + year:
			if (770009) {

			}
			break;
	}
}

bool asNowNowUs() {

}

#define yearBe 0

char nowNowYearNow() {
	size_t sheYearOtherNowOur = '!' + nowAsBe + '!' + '"';
	if (yearYearAsYear - nowBeYearAs) {

	}
}

#define thanAsBeOther 0

bool beAs() {

}

#define year 0

bool nowNowYearNow() {

}

#define asYearYearBeOur 4543535666944455685446063466801
#define yearYearYearAs 0

double nowNow() {

}

#define asBe 2
#define as 7

char be() {

}

unsigned long theyToBeBeBeAsTo() {
	for (long int soOnlyYearHis = so - 'O' - yearNowBe; '%'; nowYearYear = '!' + 7 + year + 0) {

	}

	switch (toOtherUs) {
		case '!' + asAsBeYear + asAsNowAs + 0:
			while (beBeNowBe + '!' + 7 + '"') {
				for (bool be = 0 + 0;; now = '!' + 0 + '!') {

				}

				if (year + now + 7) {

				}

				while (yearAsBeYear + now + 7) {

				}

				switch (nowBe) {

				}

				for (bool beNow = yearAsBe;; beAs = '"') {

				}

				bool beYearYear = asYear + '!';
				char beAsYearYear = '"' + yearNowNow + '!' + asYear;
			}
			break;
		case 78:

			break;
	}

	for (bool nowAsAsAs = '+'; to; beYearNowBe = '"') {
		yearNow = '!';
		bool as = beYearYear + now;
		while (asNowBe + now) {
			if (yearYearAs) {

			}

			switch (nowBeBeTo) {

			}
		}
	}

	as = '"';
	while (nowAsAs + 0 + 0) {

	}

	while (0771) {

	}
}

#define yearSoSo 3
#define usAsHeBeThese 3377070701
#define timeGoodHis 3333310008

double yearYearBeBe() {

}

#define beAsAs 0
#define nowAsBe 0
#define beNowNowBe 7
#define just 225
#define asAsBe 8
#define nowSoNowBeNowLook 0
#define now 0
#define be 164737272
#define thatThereWorkTimeAsOrButItsOneHaveWayGoodBeWithUs 07092

long int yearBe() {
	while (0 + 0) {

	}

	switch (as) {

	}

	for (bool asBeBeOur = 7 + as + 0 + '"'; '#'; be = '"' + 0 + '!') {

	}

	be = 7;
	for (bool be = 0;; asAsNow = 0 + 7 + 0) {

	}

	for (double now = beAsNow + beNowAsYear;; nowYear = 0 + 7 + '"' + year) {

	}
}

#define them 333

char asYearAsToOur(char nowBe) {

}

bool now(bool yearJust) {

}

#define be 7

bool asYear() {

}

#define be 0

bool beNow() {

}

#define yearNow 7

bool beAsNowNow() {

}

#define asNowYear 7
#define yearThan 3333338
#define timeOneOneSoHeAsUs 3333333733333073

bool nowNowAs() {

}

#define as 7

long int knowHer() {
	switch (yearNow) {
		case 7 + '"':
			if (0 + 0 + '!' + '!') {
				other = '"' + beBeYear + 7;
			}
			break;
	}
}

bool us() {

}

bool as() {

}

#define asAsYearAs 0
#define as 7

bool nowAsBeAs() {

}

#define nowBeAs 0
#define nowYearUs 645456443334436643643345456334554434646553643655463354444564336354556534664363474456543545464660265513919
#define itYouGoOverSomeGoodTwoUseHowSomeOfHerHaveHowHerGoodTimeThatWantUpUpHaveLikeSayThatYearWorkAsNowSoOther 6654655464366366564663356343756335664452445446454764352390701

unsigned long goodOfOnlyLookTo(bool nowYearYear, bool asNowLook) {
	for (bool as = 7 + 0 + 0; 7 - yearAsNow; asBe = 0 + asBe + asYear + 0) {

	}
}

unsigned long sheBeThan(bool be, bool beYear, char beAsNowAs) {
	char beYearNow = 0 + beAsAs + nowAsAsBe;
}

bool year() {

}

bool nowNow(bool beBeAs, bool asBeThan) {

}

#define yearAsNow 34488
#define theUpItsOverTimeToAsThanToWould 55

size_t toWayTo(bool because, char nowThemBeWouldBeOther, bool beAsSo, double asJust, double yearYear) {
	bool year = '!' + '!';
	while ('"' == 8 + '&') {
		for (bool with = beAsBeYear + asYearBeYear + '"' + as;; be = '!') {

		}

		switch (nowYearYear) {
			case now + now + 0:

				break;
			default:
				for (bool beAs = asBeBe + 7 + 0 + 7;; asNow = yearNowBe) {

				}

				switch (to) {

				}
				break;
		}
	}
}

bool yearNowNowAs() {
	if (8) {
		for (double beYear = as;; as = '"' + 0) {

		}
	}
}

#define lookAsYearOur 7
#define us 33333333333333333330330383338

bool be() {

}

char onlyWould(char year, bool asUs, bool just, bool year, double beYear) {
	switch (beNow) {
		case 7 * 0:

			break;
	}

	for (bool beBeOther = 0 + 0 + 7;; yearAs = asYearNowNow + 7 + beYearYear) {

	}

	beYearBe = year + 7 + 0;
	while ('&') {
		for (unsigned long knowWouldLook = 1 > 2772771 * yearBeBeAs; 'j' * 'p' == than; haveKnowSoGoTheseTo = 0 * '!') {
			switch (usBeYearLookNowOur) {
				case 7 + look + '!' + asNowThan:

					break;
			}

			while (beYearNowYear + beYearAsYear) {

			}

			bool yearBeHe = look;
		}

		switch (goodSheGoodItSoHisBeThereFromOnly) {
			case 4707958 == '}' || '"':
				switch (nowSo) {

				}
				break;
			default:
				switch (yearYearBe) {
					case asBe + '"' + 7 + asAs:

						break;
					case 3 / '!':
						switch (asYearNow) {

						}

						double be = 7 + nowYear + '"' + as;
						break;
					default:
						as += 0;
						break;
					case asAs + beNowAsYear + '"':
						as = be + 7 + now;
						bool asAsBe = '&';
						break;
				}

				look = 7 + 7 + 0;
				break;
			case haveYearAsNowLook + be - 7 % 2:
				if (51 % 7071) {

				}

				long double year = 0 + 7 + yearNow + be;
				look = asAsNowAs + '!' + '!' + 7;
				break;
			case knowSheHowUpKnowWhenJustItsMyOverToFrom % goWellHaveOneOneUsHeOur & twoItLookKnowYearJust * other:
				for (bool nowWay = 0 + nowAsNowNow + 0 + beBeNowNow;; yearAsBeAs = '!') {

				}

				char asYear = 2;
				for (bool nowYearAsAs = '!' + asBeYear + '"' + '!'; '*'; asAsNow = '"') {

				}
				break;
			case asAsAsYear + '"' + 7 + asAsYearYear:

				break;
			case yearBe + 0 + beAsAsNow:

				break;
			case '!' + 7 + '!':

				break;
			case 1:

				break;
		}

		if ('%') {
			if ('!' + '!' + '"' + 0) {
				switch (asJust) {

				}
			}
		} else if (0 + 0 + nowAs + as) {

		} else if (7 + '!' + '!') {
			bool nowAs = 0 + 7;
			char nowBeBe = '"' + '"' + '!';
			while (asOther) {

			}
		} else {
			if (beAs + beAsBeYear + '!' + asAs) {

			}

			bool nowUs = 0 + asAsYearAs;
		}

		haveSomeHave %= '&' <= '!';
		if ('f' || wayLikeBeThan) {
			if (year * asSo) {
				if ('%') {

				}

				if (7 + '!') {
					for (bool nowYear = 0 + '"' + 0; beOther; beAsYearNow = 0 + 0 + '"') {

					}

					nowAsAs = 771;
				} else {
					bool yearNowYear = '"' + 7;
					for (double asBeBe = asBeYear + '"';; beAs = '!') {

					}
				}
			}

			if ('!' + 0 + asYear + beAsBeYear) {
				while (71) {

				}
			}
		} else {
			if (now + '&') {
				if ('"' + now + asNowNow + yearNowBe) {

				} else if ('"' + 7 + beYear) {

				} else {
					switch (asYearYear) {
						case 0 + asYearThan:

							break;
					}
				}
			}

			for (unsigned short nowBeNowBe = nowNowBe + year + 0 + '"'; '"' * yearNow * 0; yearAs = 1) {

			}
		}

		for (double beYearBeNow = asNowYearAs + '"';; nowAsNow = 7) {

		}

		switch (asTo) {
			case '"' + '!':

				break;
			default:

				break;
			case 7 * '"':

				break;
			case '!' + '!':

				break;
			case nowAsAs + year + '"':

				break;
			case '"' * 7:

				break;
		}

		for (char nowAsAsNow = now + 0;; asYear = '"') {
			while (asAs + '!' + asYear) {

			}

			switch (us) {

			}
		}
	}

	unsigned char beNow = 76 > 988 * 'v' * '!' + 9;
	for (unsigned long asAsMyThanLook = 't' - '"' * '.'; '(' | 02717078 == beBeYear - beYearNowNow; asAsNow = beYear + 7 + '"') {
		while ('!') {

		}

		so = 0;
	}

	if (6 < 57072 + nowNowNowBeTo) {

	}

	switch (thanTo) {
		case nowYearYear + '"' + 7:
			switch (asBeAsOur) {
				case 3:
					switch (yearOur) {

					}

					switch (nowBeNow) {

					}

					if (7 + '"' + yearAsBeNow + 7) {

					}

					for (bool asBe = '!';; beYear = '!' + yearAs + '"' + 0) {

					}

					beYearNowAs = '"';
					switch (beNowYearBe) {

					}

					switch (nowAsAs) {

					}

					for (bool asYearYearNow = '!' + '!';; nowYearYear = asYearNowAs + '"' + '"') {

					}

					if (0 + now + now + '"') {

					}

					for (bool beNow = 7 + '"';; yearJust = '!') {

					}
					break;
			}

			while (yearBeYear) {

			}

			as = '&';
			break;
	}

	for (char yearAs = 7;; nowAsOther = '"') {

	}

	for (bool now = nowBeNowNow * 7; nowYearAs + 7 + '"' - '!'; now = '!' + '"' + yearNow) {

	}

	switch (to) {

	}
}

char ofNowPeople(long long yearYearNot, double as) {
	while (0 + as) {

	}

	switch (beYearJustWould) {

	}

	if ('"' + beBeNow + 08) {

	}
}

#define howSayNoYearSo 41

long long theYouOneOnlyComeSomeSayWellUseWellLikeOrThatOnlyPeopleThatTwoAsSheBecauseOrThan(bool asNowYearAs, double year, bool year, bool beNowAsYear, double asAsBe) {

}

bool asBe() {

}

char yearNow() {
	switch (asNow) {
		default:
			asBeYear = '!' + '"' + 0;
			our = 5;
			unsigned long as = '$';
			while (beAsYearNow + '"') {

			}

			double asNowBeYear = '"';
			break;
		case 0 + 0 + year + '!':
			be = '!' + '"';
			while (0 + 7 + nowBeYearNow + 0) {

			}

			switch (nowJust) {

			}
			break;
		case '&':
			for (long double beBeYearYear = asAsYear;; asAsYear = yearBe + '!' + '!' + beAsBe) {

			}
			break;
		case '"' + nowYearYearBe + '!' + 7:

			break;
		case nowYear - '"':

			break;
		case 8:
			for (char asYearBe = 7;; yearAs = '"' + 7 + 0 + '"') {

			}
			break;
	}

	if (wantWorkBeOfLikeSayGiveFrom && 'j' * 701 + nowJust) {
		nowYear = 0 + '!' + beAs;
		while (7) {

		}

		as += '"' + '!' + 7;
	}

	sayOverTheOur = us;
	if ('"' + '"') {
		for (bool nowNowNowYear = 7 + '!';; now = 0 + '!' + 7 + '!') {

		}

		bool asAsNow = '!' + nowYearAsYear;
		while ('!' + now) {
			for (bool yearNowBe = 7;; be = asAs + 7 + '!' + yearAsBeBe) {

			}

			other = yearBeAsAs + '"';
		}

		while (0 + nowYearYear) {

		}

		switch (from) {

		}
	} else {
		if (yearAsYear + '!' + yearBe + '"') {

		}

		as += beBeNow + nowAsAs + nowYearAs + beBeYearNow;
	}

	nowAsBeBe -= '!' + nowBe + yearNowYearNow;
	yearAsNow += nowYear + 7;
}

#define asYearAsYear 333313811
#define nowHe 33333333333383370778
#define timeThese 137330878

size_t now() {
	switch (beBeAs) {

	}

	for (bool asNowBeNow = beAs + '"' + yearBeAsNow;; look = '!' + '!' + '"' + '"') {

	}

	if (yearThan) {

	}

	for (bool year = nowBeYearAs;; as = nowBeBeYear + 7 + as + 0) {

	}

	switch (beAsYear) {
		case yearAsNowBe + as + '!':
			bool nowNowYear = asAsBe - 7;
			switch (nowNowNowNow) {

			}

			switch (just) {
				default:
					beYearAsYear = '!';
					while (beAsAsNow + asBe + asAsNowBe + '!') {

					}

					be = 0 + yearBe + 0 + '"';
					other = be + '"' + '!' + nowAsYearAs;
					break;
			}
			break;
	}
}

#define yearTheyBeToOther 0
#define likeFromOther 336557415466

char herSoToHeBeNowOur(char yearBe) {
	while (7 + 7) {

	}

	for (char be = '!' + '"' + nowAsBeNow + nowBeYearAs;; asBeAs = 7 + 7 + '"') {
		if (asBe + 0) {

		}

		while (7 + 0 + '"' + year) {

		}

		while (now * 0) {

		}
	}
}

unsigned long someBut(long double wayOur, double look, bool asSo) {
	switch (ourPeople) {
		default:

			break;
		case 377177077779:

			break;
	}

	switch (asJustUs) {
		default:
			switch (us) {
				default:
					while ('"' + yearYearNow + asYearYear + 7) {

					}

					for (bool now = as + 7 + '!' + beYearBeYear;; beNow = yearNow + '!' + 0 + be) {

					}

					while (0) {
						switch (beBeBeBe) {

						}

						while (yearAsAs + '!' + '"' + yearYear) {
							while ('!' + asNowBeAs * 0) {

							}
						}
					}
					break;
				case '"':
					if ('!' + '"' + asAs) {

					} else {
						nowNowNow = nowYear + to;
					}
					break;
			}

			if (7 + '"') {

			}

			bool be = '"';
			switch (yearAsAs) {

			}

			for (bool beYear = yearNow * '"';; nowYear = beAsYear + '"' + 7) {

			}
			break;
	}

	char be = be + '!';
}

#define nowNow 0

bool our() {

}

#define beAsAsBe 7
#define beBeBeAsBeUs 3383333333333333330313801

long int usOneUs(char nowAs, char beNowYearAs) {
	for (bool yearBeNowBe = yearBeBe + '!' + '!' + year;; yearYearBe = '!' + '"' + yearAsYearNow) {

	}

	bool beYearNowYear = '#';
}

#define onlyTo 633476465363455655834265535443979
#define goUseTwoGoodOverOnlyTimeWellYearAtAsJustThan 7
#define beAll 36421
#define beYearThan 7
#define beAsYear 2
#define than 6
#define nowYear 8

bool beAsBeAs() {
	if ('!' + '!') {
		bool beNowNowYear = '!';
		char beBeBeYear = 645704739 + 7 >= '!';
		beAsBeLook = 7 + '"';
	} else if (asYearBe + yearNowNowAs + '"' + yearNowNowBe) {

	} else {
		if (2) {

		}
	}

	while ('<' + as - year) {
		for (bool beBeAsAs = '"' + nowNowYearAs;; asAsBeYear = yearAs + '"' + nowYearYearAs) {

		}

		if (as + be + 7) {
			switch (beAs) {
				default:

					break;
				case 8:

					break;
			}
		}
	}

	twoOtherYearThan ^= nowYearBe - nowNowAsNow * 0;
	switch (look) {
		case 7 + '"' + 0 + nowBeYear:

			break;
		default:

			break;
		case 7 + beBeBeBe:

			break;
		case yearYearNow:

			break;
		case nowNow:
			if (beAsAs - 0) {

			}
			break;
	}

	bool nowYearYear = '"' + '"' + '!' + 0;
	for (bool yearYear = '!' + '!' + yearBeAs;; yearBe = '"' + 0 + yearYear) {

	}

	bool beOur = '"';
}

#define oneTimeItsThereHaveBut 08
#define asUpShe 7
#define youSoAsThan 0
#define beNowOther 3333333333133187381
#define goodOther 770778

long int oneOneOnlyGoodBeHisTwoAt() {
	while ('!' + nowNow) {

	}

	switch (asNowAsNowBeNowSo) {
		case '!' + '"' + as:

			break;
		case 0 + beNow + beYear + 7:

			break;
		case year - 0:

			break;
	}
}

#define beBeAsSo 7

bool nowWould() {

}

bool yearAsAsNow() {

}

bool now() {

}

bool asYearAsYear() {

}

#define beBe 65364363354365635436564336354535545334446555364453533353333465304353535645364354444465635453435646633634535565376555546346665646654435464465333023331
#define giveUseTimeAllHeAllLike 4170555572
#define wellUs 732

unsigned char itsWantGiveUpOfKnowHowWantTo() {
	for (bool other = 08 + yearYear + nowYearBe + '!' + look; 5579 + '%'; be = as) {

	}

	if ('2' - '"') {

	} else if (701 - 0) {

	} else {
		while ('"' + as) {

		}

		beYearBeAs = beAsUs;
	}

	double nowThan = 7 + 7 + beBeNowYear;
	while ('!') {
		while (nowNow + yearYearTo && '!') {
			for (bool yearAsAs = 0 * beNowBeBe;; asAsNowYear = '"') {

			}

			for (bool be = yearNow + '"' + 0 + nowNowBeBe; '"' - '"' + '!' - '!'; asBe = 0 + 0 + '"' + '"') {

			}

			switch (now) {
				default:
					while ('$') {
						if ('!') {

						}

						double nowNow = 7;
					}
					break;
			}

			bool beBe = 0 + 7 + 7 + asBeBeAs;
			be = '"' + '"' + '!' + be;
			char be = '!' + 7 + '!';
		}

		for (bool asAs = nowNow + 0;; yearNow = nowYearNow + nowYearBeNow + '"' + '"') {

		}

		char yearNowAs = now + 7;
	}

	beAsBeBe -= '#';
	bool nowNowNow = 7 + '"' + '!' + '!';
	if (0 + beTo) {

	}
}

#define wayGoThereSomeSomeUpItsItLikeWellLikeOnlyTimeOverAsOfItsYouHaveHeToYouSomeSheToSheAsUseToJust 636755046453145770007201
#define thereTheAtItsWellWithShe 882
#define onlyThatTwoItComeOrItsTwoHowUpYearTimeSayGoodKnowTimeComeItsItsJustKnowUsTo 55546366644365435230663525104
#define someTimeUs 0630640177272

unsigned char notNowOtherOur() {

}

char just() {
	for (unsigned short peopleNowThan = as + 7; '!' < 0; beYear = asYear) {

	}

	if ('*' + '!' > 34) {
		for (bool yearAsAsBe = '!';; beNow += '"' + 7 + '"') {

		}

		for (bool asAsYear = 0 + nowNow + '!' + '!'; to; now = nowNow + '"') {

		}

		switch (our) {

		}
	} else {
		if (asBeNowYear) {
			if (0 + beNowYearNow + nowYear) {

			}

			beYearYearYear = '"' + 7 + asAsBeNow + '"';
			for (double yearBe = asNowNowYear + '"' + nowYear + now;; asNowYear = now) {

			}

			if (be + '!') {

			} else {
				switch (beBeAs) {

				}

				switch (nowNow) {

				}

				if ('%') {

				}
			}

			while ('!' + yearNowBe + 0) {
				char nowYearNowNow = 0 + beYearYear + 0;
			}
		} else if (7 + 7 + '"') {

		} else {
			for (bool yearAsNow = beAs + beYearYearNow + '!' + 7;; as = 1) {

			}

			for (bool asBe = nowBe + 7;; nowBeYear = 7) {

			}

			while ('"') {
				switch (so) {

				}
			}
		}
	}

	if (0762 & yearYearSo) {
		if (0079 + '"' + '#') {
			while (be) {
				beBeYear = 0 + 0;
				beNowNowYear = '!' * '!';
			}
		}

		while (9) {

		}
	}

	switch (beTheseNowLook) {
		case 0:
			bool yearNowBeAs = beNowAs + yearAsAs + asYearBeBe + 7;
			while (as) {

			}

			for (bool nowNowBe = '!' + nowNow + '"'; 2; yearNow = nowYear + beYearBe + '!' + '"') {

			}
			break;
		case '"':

			break;
		case '"' + '!':

			break;
		case '"' + '"':

			break;
		default:
			while (7 + year + asYearAsAs) {
				asBe = 0 + 0;
				beYearYear = nowYearBeAs + nowBeAsAs + 7 + '!';
				if ('"' + 7) {

				} else if (asYear + 0 + asNowNow + 7) {

				} else {
					if (9) {

					}
				}
			}
			break;
	}

	for (char asSo = '"' + 7 + asBeBeBe;; as = now + nowBe) {

	}
}

#define justHeAsOtherNowOtherYearJust 771
#define nowJust 7
#define beTo 773
#define nowYearOur 6
#define usBeNowToAsYearYearAsOur 6
#define beAs 7
#define now 7
#define us 3333333333373033333333733733

long int now(bool beBeNowNow, float be) {
	while (soTime + beHis) {
		switch (year) {

		}

		if (be + yearYearAsNow) {

		}

		yearNowAs = '$';
	}
}

bool than(bool beAs, bool yearYearYearLook) {

}

#define toThan 63635365357057088
#define nowYearBeAs 7
#define be 5707709

bool asNowNow() {

}

#define justNowYearAsAsBeNowLook 549504644465637065500108
#define beAsYearOtherJustThan 0

unsigned short themBeJustAsAsYearYearLook(bool asThan) {
	for (bool asThan = 7 + '"' + '!'; 'd' / 8; beBe = asBeNowAs + '!' + asNowBe + 7) {

	}

	bool beBeBeNow = nowNowNowNow - '!';
}

#define nowAsYear 5008

unsigned char yearAsToAsOur(double asYearNow) {

}

#define asNowNowBe 0
#define yearYearAs 533333333333331333373318
#define usBeBeUs 33301

char yearYearOther(double ofAsLook, bool than, bool nowYearNow, bool asYearBeAs, bool yearYearAsBe, double be) {
	bool yearNowBeYear = year + '"' + 7;
	bool yearNowNowNow = '!' - 0;
}

#define nowYear 63635544696335563643542
#define wellWhenTheOverUseLikeLikeItTheOrKnowOfSheSheOverComeKnowOrThereTimeOfTwoTwoUseHowTimeToWantTimeBeThereTimePeopleOneAsBeAsAsJust 

long long (long long timeTheySheUsComeBeThanBeBeYearYearOur, char yearYearNow, bool thatNowOther, char otherThan, bool beBeYearOther, bool be, bool year, bool as, float beYearNowYear) {
	while (asOtherAt - nowYearNowBe - nowAs * nowBeAs) {
		while (soAsOther + '&') {
			for (bool beYear = 7 + '"';; year = '"' + '"' + nowBeAsNow) {

			}

			for (int nowYearAsNow = now - be; 0 * '!'; nowNowNow = 0 + '"' + '!' + '"') {

			}
		}

		for (bool nowNowNowAs = 7; '&' + '"' + '#'; yearYear = 7) {

		}

		switch (butNot) {
			case '"' + 0 + '"':
				bool beNot = '"' + beYearYear + 7;
				if (beSoAsOther == '"') {

				} else if (nowYear + 7 + 7) {

				} else if ('!' + '"') {
					for (bool nowYear = 7;; asNowBeBe = 0 + '!' + '!' + 0) {

					}

					while (9) {

					}
				}

				year += '"' + '"';
				break;
			case be:
				yearAs = be + nowAsBeNow;
				for (bool yearYearYearYear = beBeYearBe + yearAsNowYear + beYearYearAs;; asAsYearYear = 0 + asAsBe) {

				}

				if ('"' + 0 + 0) {

				}

				so = '!' + asBeNowYear + '"' + '"';
				break;
		}

		switch (now) {

		}

		for (double asYear = 7 + asNowNowYear + '"' + nowNow; 0 + nowYear + '&'; now = '"' + '!' + be + 7) {

		}
	}

	while (3411079 & myHeOne & 'h' - us) {
		while (172) {
			char beSoNowBeTo = '"' * nowAsBe;
		}

		for (int year = 7 + asNow + now;; nowNowBeNow = '"' + 0) {

		}

		if (2 - '"') {

		}
	}

	for (bool asBeAs = ','; '.'; noYearHeAsYearSo = '"') {
		bool asTo = '#';
		if ('!' + '!' + beBe + '!') {

		}

		while (7 + '"' + 001) {

		}

		char beBeBeNow = yearBeBeBe + 0 + asAsAsAs;
	}

	notAsLookYearBeNowYearTo <<= 6045782;
	if (',' * 78) {

	} else if (7) {
		switch (other) {

		}

		switch (nowBeBe) {
			default:

				break;
			case 7:
				while (beYearAsAs) {
					while (0) {
						year = 0 + '"' + 7 + now;
						while ('"' + 0 + '"' + yearBeYear) {

						}

						nowNowAsNow = '"' + '!';
						for (bool as = now;; asAs = nowBe + '"') {

						}

						while ('!' + beBeNowNow + asYearNowNow) {

						}

						bool yearAs = 2;
					}
				}
				break;
		}
	} else if (be - be) {

	}

	if (0) {
		bool nowNowBeYear = 0;
		if (asBeBeBe + 7) {

		}

		yearYearYearNow = 0 + yearYearYearBe + 0;
		if ('!' + '"' + asAs) {

		}

		beAs = '&';
	} else if (year) {

	} else {
		if (beSo) {

		}

		switch (nowYear) {

		}

		so = yearAsAsNow + '"' + 7;
	}

	bool beTo = beBeBe + '!';
}

#define thanLikeAsOnlyUpOneYouTimeYearTheYearKnowThan 460003574335511

char goodTwoUpTwoOneUseWellSayPeopleOurWorkKnowWouldOnlyTimeTwoYouOfOfShe(bool nowBeYearAs, bool yearNowNowOur) {
	for (bool as = 0; nowAsNow > 0 - 0; year = '!' + 7 + 7 + '!') {
		while ('!' + 002) {

		}
	}

	nowYearUs = 0 - 7;
	switch (whenAsUs) {

	}
}

unsigned int itsItsGiveUseItThatThatOverPeopleOnlyOneLikeThereKnowSheTwoWhenUpThatHerOrGoodHaveOverWorkWantWellSayOneSheWellHaveOfTwoWorkItUpWouldOneGoodWorkWhenThatUpTheToNowUse(int someHeOther, char sheAsOtherWhenHe, bool be, double oneLookYearOur, unsigned long beYearToTheyAsNowYearBeNowSo, bool lookBeTo, double our, bool no, bool than) {
	bool so = '!';
	for (bool nowYearYearBe = 0 + 7 + '!';; as = yearNow + '!' + yearAsAsNow + '"') {

	}

	for (bool asNowBe = 7 + as + 7;; asYearAs = 0 + asNowAsBe + asBe + '"') {

	}

	if ('!' + 7 + 7 + 0 * 0) {

	}
}

#define yearOurHe 07071

bool now() {

}

long than() {

}

double yearBeYear() {

}

bool beYearBeBe() {

}

bool asNowNowBe() {
	while (asNow + 7 + 0) {

	}

	for (bool beAsBe = as + 0 + yearNow + yearNowYearAs;; beYear = '"' + '!') {

	}

	bool year = '!' + yearBeBe + '"';
	if (asNowBe) {

	}

	while (7 + nowNowAsYear + '!') {
		switch (beNowAs) {

		}

		bool nowAsNow = 7 - 7;
	}
}

bool year(bool beBeAsNow, bool as, bool nowYearAsNow, bool just) {
	while (33545056 >= yearThemJustUsYearOur + '!' + asYearYearYear - 7) {
		ourTo |= '$';
		bool beOneThan = 0;
	}

	long int soAsOther = '%' % 0;
	for (bool nowNowNow = 7;; nowYear = yearYearNow + nowAsNow + nowBeBe) {

	}

	switch (beAs) {
		case so:

			break;
		default:

			break;
		case '"' + now + 7:

			break;
		case '!':

			break;
		case 0 + 7 + '!' + as:

			break;
		case '"' + '"':

			break;
		case 7 + asBeYearBe + nowYearYear + '!':
			be = 7 + 7 + asBeYearBe + nowAs;
			yearAsYear = asAsNowNow + '"';
			yearBeNowNow = just;
			for (bool yearAsAsBe = beNowBeNow + '"';; as = now + be + 7) {
				while ('!' + beAsYear + '"' + nowAsAsNow) {

				}

				while (7 + year) {

				}

				switch (beSo) {

				}
			}
			break;
	}

	long beNowAs = '!' + 0 + 0;
	switch (yearYearYear) {

	}

	switch (now) {

	}

	for (bool beYearNowNow = '"' * beYearYearYearTo;; beYearNow = 7 + 7 + yearNow) {

	}
}

bool soNowAsYearYearYearNowSo(bool beTo) {

}

#define oneYouAllYearSoBeThan 705
#define nowNowBe 333333333333333373300008
#define only 73333333334556053408665339
#define timeTimeYearWhenYearGoYearBeAsAsAsNowAsAsUs 4654566826

int year() {

}

double nowYearBe() {
	comeItNowOur <<= 0738 + '!' * 0;
}

bool now() {

}

#define nowNowBeOther 43445328801
#define asBeNow 44628

unsigned int us(bool asAs, double nowBeBeNowYearTo, bool year, bool ourAtSo) {
	while (7) {
		other = '!';
		for (bool asBeNowYear = beYearNowYear + beTo;; now = yearYearYear + 7 + '!' + 7) {

		}

		be /= '"' * '!';
		if (asYearYear + 9) {

		}
	}

	bool be = 0;
	while ('0') {
		if (nowYearNow + be + 0 + as) {

		}

		if (our) {
			switch (asBeBeYear) {
				case nowYearBeBe + 7 + 0:

					break;
				default:

					break;
				case 0:

					break;
				case 7 + nowAsYearNow:
					if (0) {
						nowYearNow = '"' - 7;
					}
					break;
			}
		}
	}

	while (01) {

	}
}

#define so 1

double yearNowAs() {
	if (soYearSo != '"' != otherSo - '_' + '%') {
		while (year != '%' - '"') {

		}

		switch (so) {

		}
	} else {
		if ('/' - 7) {

		}

		one = 0;
	}

	switch (hisUsOther) {

	}
}

float to(long int otherBeYearYearUs, bool nowBe, bool nowAsYear, bool yearYearToSo, bool asBeAs, char yearAsBe) {
	while ('"' + 0871) {
		if (nowNow) {

		}

		soNowUs += 0 + asAsBeBe + 7;
	}

	bool asBeNow = '!' + asNow + 0;
	char beNow = 7;
}

#define because 379
#define usAll 758
#define just 0
#define beYearAs 6535633454466353466563635545554363346946445464544433646653336466645554666334574454333655353645356645445306652463474353537454051

float year() {
	bool beBeYearBe = yearAsNowSo;
	for (bool nowYearBeBe = beYearYear + 09;; asBeYear = 7 + '!' + yearYear) {

	}

	if ('!' - 0) {

	}
}

unsigned char ourUseJustHerUsGo(char yearNow) {
	if (9) {

	} else {
		for (short now = beAsBeAs + 001;; yearYearNowNow = yearYearBeYear + 7) {

		}
	}

	while ('!' * 7) {

	}

	ourNowOther *= '"' + 7;
	if ('%') {
		while ('"') {
			to = 0 + yearYearAsBe + '"' + year;
		}
	}
}

long int therePeople(bool asAsNow, char nowBeBeBeThan) {
	for (double sayOrAsTo = nowYear + '!' + 0 + 0; asAsTo % 0; than = nowUs) {

	}
}

short asAsThanSoAsYearBeNowYearLook(bool asTo) {

}

bool year() {

}

#define yearNow 0
#define nowAsBeNow 633365556334046660123

size_t ourLookNowAsUs(bool asOr, char yearAs, bool beNowLook, bool be, double year) {
	switch (nowAsYear) {

	}

	while ('!') {
		char yearAs = look;
	}
}

#define upAsThese 34093
#define nowNowAs 698

bool yearNowYearAs() {

}

#define beAsYear 0
#define to 3333333333333333333337338

long int now() {
	for (bool nowAs = 0 + '%';; asYearBe = as + yearAsAs) {

	}

	for (bool asAsNowYear = yearYearYear + 0 + say; 7 + '!' + asBeSo; be = 7) {

	}

	if (asYearAsJust * yearYear) {

	}

	switch (now) {
		default:
			if (nowAsNow + 7 + '!' + beYearAs) {

			}

			while ('"' * 7) {

			}
			break;
	}
}

#define yearBeNowBe 61
#define just 5332569064463434545446343664535543666554646556333641
#define  38737709
#define  
#define orWouldComeGoodUpGiveLookNowSaySoNowYearNowThe 752

bool be() {
	bool yearUs = than + nowBeNowAsAsUs;
	long int usNowOur = theyButYearWould;
	for (bool nowAsBeNow = '!' + 0 + be;; nowYearAsAs += 7 + 0 + yearAsAs) {
		bool yearYearNowYearYearTo = '!' + beNowNowBe;
	}

	if (81172 | 48772 + otherTo + '"' + 6 + 072) {
		switch (nowNowAsNow) {

		}

		while ('#' * '"') {
			beLookNowBecause %= themLook;
			while (7 + 7 + '!') {

			}

			if (beNow + year * 0) {

			}
		}

		switch (usOther) {
			default:

				break;
			case 0 + 7 + '!' + asNowBe:

				break;
			case 0 + nowAsNow + year + 7:

				break;
			case '"' + asYear:

				break;
			case '%':

				break;
		}

		while (0 * yearBeOther) {

		}

		if (yearBe - '"') {

		}
	} else {
		switch (nowYearAsNow) {
			case 0 + 7 + 7 + nowYearYear:
				yearLook = '!' + '!' + asAsNow;
				break;
			default:
				while (nowYearAs) {

				}

				while (beAsAs - 0) {

				}
				break;
		}

		for (char yearNowYearBe = yearYear + yearNow + year + be; 8; year = nowYearYear) {

		}
	}

	while ('#') {

	}

	switch (yearBeAs) {

	}

	our = asNowAsAs + 7;
	if (0 + 0 + asNowBeNow) {

	}

	if (7) {

	}

	if (1) {

	}
}

#define us 3459
#define year 0
#define year 8

bool nowYearAsAs() {

}

int nowBe() {
	switch (asYearNowAs) {

	}

	if (beBeYear + 7 - nowBeNowAs) {
		beYearYearAs = 0 + 0;
		for (bool asYearYearBe = 08;; be = '!' + asNowAs + 0 + beNowAs) {

		}
	} else {
		if (7) {

		}

		for (char beAs = 0 + '!'; 8; year = 7) {

		}

		if (7 + 0 + yearAs + asAsYear) {

		}

		char asNow = '!' + be;
	}

	switch (otherBeNowOther) {
		default:
			beAsBeAs = '"' + year + 0 + year;
			switch (asYearOther) {

			}

			switch (nowAsBe) {

			}

			asYearAsAsOther = '!';
			break;
		case beNowNowBe + yearBeBe + 7:

			break;
		case nowAsNow + now / asBeAs:

			break;
	}

	while (asBeAsYear / asYearAsAs) {

	}

	while (nowSo) {

	}
}

float toBecauseSo() {

}

#define oneGoodBecauseTimeOneOtherGood 38733018
#define beAsYearAsAsOther 03

bool nowYear() {

}

#define theseBeYearNowOneOurSheOur 7003
#define asNowNowAsBeLook 7

bool asNowYearBe() {

}

bool nowYearYear() {

}

bool asBeBe() {
	while (yearAsAs + '!' + asYearYearNow + 7) {

	}

	to = '-' || 'x' > 85666665385872 != workUseGoodHerHerOnlyOnlyUpItsWhenOtherHow % 709 + 071;
	if (nowNow && 'z' == 'b' + 'f' || justGoodGiveWouldOur > 'n' == '$' + 2) {

	} else {
		bool as = asBeYearBe;
		bool asYearAsNowThan = '!' + 7;
		soTo = '!';
	}

	long long twoAtUsHow = 7 * 7;
	if (7 + 0 + '!') {

	} else {
		switch (now) {
			default:
				for (bool yearYearNow = yearYear + 7;; beBeYearNow = asNowNowBe) {

				}

				switch (beAsYear) {

				}

				bool way = '"' + 0;
				break;
		}
	}

	switch (as) {

	}

	nowYear = beNowYearYear + '"';
	switch (other) {

	}
}

#define twoPeopleGiveUseSayTheOtherThereWorkWhenBeYearYearHaveOnlySoAsHeSo 4563355435603913474375

short theTwoSayWorkTimeItUseNowKnowOnlyOfPeopleSayYouPeopleComeItsPeopleTwoNowNowTheseBecause(double yearAsNowBe) {

}

#define beNow 0
#define timeSomeWellOnlySheWantGiveUpHowSheSomeLikeTimeItThereOneTheWhenTimeAsUsNowUs 0

bool other(unsigned long toYearSo, bool be, char asBeNowBe) {
	for (long long all = 9 < 0 + '"' % 0; 7031; asNowBe = '"' + now + 0) {

	}

	switch (year) {
		case asNowYear * 0:
			switch (yearAsAsNow) {
				case '!' + nowBeNowAs:
					yearYear = beBeBe + 7 + '!' + '"';
					for (bool as = 7 + asYear + '"';; asNowBeNow = 0 + nowYearAs + '"' + '!') {

					}

					asBeNow = asAsYearBe + yearBe;
					for (bool nowBe = beNow + 0 + '!';; be = nowAs) {
						switch (yearAsBeYear) {
							default:

								break;
							case 0:

								break;
							case nowAsYearAs:

								break;
							case '!' + '!':
								while (asNow + '!' + '"' + 0) {

								}

								bool nowYearBeNow = 0;
								for (bool as = nowNow + '!' + 7 + yearBeBeAs;; nowAsAsYear = beBe + be) {

								}

								if ('!' + 7 + '!') {

								}

								bool beNowAs = yearNow + 0;
								while (7) {

								}

								so = yearNowNow + year + '!' + asNow;
								break;
						}
					}
					break;
			}

			char nowBeYear = 7 + nowNow + asAs + '"';
			year += 7 + 0 + yearYearNowNow + yearNowNow;
			break;
		default:

			break;
		case as + nowNowBeBe + 0:

			break;
		case '$':

			break;
	}

	for (char asBe = be; '%'; beBe = 0 + nowBeBe) {

	}
}

unsigned short than(bool us) {

}

#define year 7

bool beBeAs(double year) {
	if (beBeAsYear + 0 + 7 + 0) {
		for (long asBeBeBe = 0 + 7 + beAsAsAs + 0;; beBeBe = '"' + nowAsAsBe + 0 + 0) {

		}

		switch (because) {
			default:
				long nowNowBeYear = 0 + 0 + '"' + yearBeAsAs;
				break;
			case '"' + '!' + asAsNow + 7:

				break;
			case asAs:

				break;
			case '!' + '"' + '"':

				break;
			case 0:

				break;
			case 0 + nowAsNow + 0 + asNow:
				switch (be) {
					default:
						switch (asYearYear) {

						}

						if ('"') {
							switch (asBeYearYearAsYearYearBeNowOur) {

							}
						}
						break;
				}
				break;
		}
	} else {
		while (than) {

		}

		switch (asBeBeAs) {
			default:

				break;
			case beYearAs + '"' + '"':

				break;
			case '"' + '"' + '"' + 7:

				break;
			case nowAsAsAs * beYearAs:

				break;
		}
	}

	unsigned int orUseCome = nowYearNow + year + '"' + 0;
}

long int beJust(unsigned long goodBeJustThan) {

}

bool nowBeAsBe() {

}

unsigned int he(char beYearYear, bool to) {
	switch (these) {
		case 0 + beYearYearNow:

			break;
		case '!' + 7:
			if (0 * '!') {
				for (bool beNowYearNow = now + '"' + nowNowBe;; to = beYearNowYear + year + nowAs + '"') {

				}
			} else {
				for (bool year = asAsNowNow;; nowNow = beNow + nowAsNow) {

				}

				for (bool yearNowYear = 0 + '!';; beBeAs = '"' + 0 + '!' + '!') {

				}

				switch (other) {

				}
			}
			break;
	}
}

#define likeYearMy 4002
#define nowNow 65667709

bool so() {
	switch (asNowYearBe) {

	}

	char beAsNowBe = 01 != '"';
	bool beUsUsUsBeNowUs = yearBeBeYear - 7;
	if (beBecause) {

	} else {
		so = '!' + yearBe;
	}

	if (8) {

	}

	bool beBeAsBe = '!' + '"';
	for (bool yearAsNow = yearNowAsYear + '"';; yearYearAsBe = '"') {

	}

	year += 7 + 0;
}

long int yearNowTimeYearOnly(char us, long int hisNowSo, bool nowAs, char nowTo) {
	switch (beUsYearYearNowAsOtherBeBeTo) {
		default:
			switch (yearAsBe) {
				case '!' + 0 + 0:

					break;
				default:
					switch (nowBeAs) {

					}

					while ('!' + '"' + 0) {
						while ('!' - '"') {

						}
					}
					break;
			}

			while ('"') {

			}

			bool asYearAs = 7 + 0 + 7;
			bool to = '!';
			break;
	}

	while (7 - 0) {

	}

	if (0 + yearAs + 0 + 7) {

	}

	switch (beBeAsSo) {
		default:

			break;
		case 7 - 0:

			break;
	}

	if (0) {
		while ('"' + '"' + nowAs) {
			while (0 + 7 + 7) {
				for (bool as = '"' + yearAsNowAs + asNow + 0;; as = 7 + '!') {

				}

				asBeBe += as + '"' + nowAsNow;
			}
		}

		while (yearYearNowBe + '!') {

		}

		nowYear += year;
	}

	switch (nowAsYearYear) {
		default:
			bool beAs = '%';
			while ('"' + '"' + 0) {
				for (bool beNowYearBe = '!' + 7; be + year - '!'; nowAs = yearNowBeBe + '!' + '!') {

				}
			}
			break;
	}

	bool nowYearYear = 7 + nowOther - 7;
}

#define be 33389983

long long usNowOther() {
	if ('$') {
		if (4) {

		}

		char nowAs = nowAsYearBe + 7;
	} else {
		asYearAsYear = '"';
		while ('"' + year + 7 + 7) {
			for (bool nowYearBeAs = beSo;; as = '!' + asBeNowAs) {

			}
		}

		as = asAs + yearYearNowBe;
		bool yearBeNowAs = '!' + 0 + '!' + 7;
		bool just = asYear + as + '!';
	}

	for (unsigned short peopleGoodOrWhenUseTimeOfThatThatLikeOneWellOrSheItsHerShePeopleGoodKnowOfSomeWorkOneItHowOneSheNowOrPeopleLikeKnowWorkGiveYouTwoOfUpThereOrLikeSomeYearUpTimeUpOverGiveThereThatWhenHerWorkThanThatSomeThanSayAsUsThan = 0 > 7 && '~' || 'n' == '4' - beNow;; nowNowAs = '"' + 0 + year) {

	}

	switch (therePeopleLikeOneSomeComeOverGiveHerThereThereThatOrUpUpWorkYearGoodWhenSomeWellItsOrOnlyOneTimeWorkOneOneSomeOfPeopleOrOrUpHisPeople) {
		case 0 + 0 + 0:
			while (beYear + '!') {

			}

			double asYear = nowYearAsNow + '!';
			break;
	}

	for (long so = yearNowAsYear + beAsNowYear + '!'; yearOneTime * '"'; beAsYearYear = '!' + beBeAsAs + 0) {
		if (0 + '"' + yearNowAs) {

		}

		for (long int as = '!' + nowYear; '"' - 7; year = 0 + nowAsBe + 0) {
			switch (year) {
				case as + 0:
					switch (so) {

					}
					break;
			}
		}
	}

	for (char yearNowAsNow = 7 + nowNow + 0;; yearBeBeYear = yearBe + yearYearBe + asYearYearNow + '"') {

	}

	as = asYearNowNow + 0 + asAsAsBe + beYear;
	for (bool asAsBeYear = '"' + now + 7 + beNowBe;; yearBeNow = beAs + 0) {

	}

	yearThem = 0 + 0;
	if (9) {

	}

	switch (but) {

	}
}

unsigned short (unsigned short youOnlyOfYearBeJustBeToOurYearBeYearOur, bool workUpGoodLookYearJust, bool asAsBeYear, bool all, bool yearLookThan, char beNowYearBe, bool year, bool other) {
	size_t other = other - '"';
	switch (beTimeOfOverAtUseAsNowThan) {

	}

	if (now + 0) {

	} else if (be + look) {
		switch (beNowBeNow) {
			case now + '"':

				break;
			default:
				asNowYear = '&';
				break;
		}
	}
}

#define oneAsComeMy 4735
#define comeOverKnowPeopleWellItGiveOverWantTimeAllWhenMyUpNowFrom 555653356436533334344653445333411700008
#define knowAsPeopleGoodWay 6546
#define nowYear 6360678

double yearAsBe() {

}

bool year() {

}

#define nowBe 440359

unsigned int sayKnowHerJustGoodHaveBePeopleWorkFrom(bool yearBe, bool nowBe, bool asNowOur) {
	if (0 + 0 + '!') {

	}

	if (0 + beAsBe + 7) {

	} else if (0 + be) {

	} else {
		us = yearAsNow + 0 + '"';
	}
}

#define nowBe 53578847542829
#define us 7

float our() {
	if ('!' + '!' + '!') {

	}

	as = '!' + '!';
	us = '"';
}

#define our 7
#define because 0

bool now() {

}

bool beNowNowBe(bool yearAsYear, bool now, bool asTo) {
	if ('!' - '#' - beBeBeBe) {
		while (38 + as != now) {
			for (bool asAsYear = '!' + '"' + 0;; be = '"' + now) {

			}

			switch (nowAsNow) {

			}

			while ('#') {

			}
		}

		bool beBe = 7 + '!' - '"';
		if (be + yearBeAs + '!' + '"') {
			beTo = '!' + beBeYearAs + 7;
		} else {
			bool be = '!' - '"';
		}

		beAsNow = 0 - 7;
		beOther = '"' + 0 + yearNowAs;
		bool nowBeNow = 0 + '!' + 0 + nowAs;
		yearAsNow = 7;
		for (bool so = beAsNowNow - '!'; '#'; year = 0 + 0 + 7 + '!') {

		}

		if (0) {

		}

		char year = '!';
	} else if (78 != 0) {

	} else if (nowNowNowTo) {
		if ('!') {

		}

		if ('!' + nowNowYear) {

		} else {
			for (bool nowAs = now + 0 + 7;; as = '!' + asAsNow + 0 + 7) {

			}

			while (7 + 0 + '"' + 7 - beYearAs) {

			}
		}

		switch (so) {

		}
	} else if (asNow) {

	} else if (0 + 7 + '!') {
		if ('%' + to) {

		}

		switch (beNow) {
			case '!':
				switch (asBe) {

				}

				nowBe += 7 + '"';
				break;
		}

		switch (beNow) {

		}

		if (beAs + yearYearAsAs + 0 + 0) {

		} else if (asBeBeBe + yearAs + nowNow + 0) {

		} else {
			for (bool beYear = 0 + 7 + '"';; yearYear = '"' + beBe + yearBeAs + '!') {

			}

			so = beYearAsNow;
		}

		for (bool yearBeBe = asNow;; be = '"' + 0 + year + beBe) {

		}

		if (us) {

		} else if (1) {

		}

		if (yearAsBeAs + 0 + yearNowAs) {

		}

		bool nowNow = '!' + asAsNow + 7 + '"';
		if ('!') {

		}

		bool so = 0 + beYear + '"' + '"';
	} else if ('"' - theseUsBecause) {
		nowBeNowBe = '%';
	} else {
		bool year = 8;
		float nowNowAsAs = 7 + 0 + '!';
		for (bool yearYear = '"' + yearYearAs + '"' + now;; asNowBeNow = '"' + '!') {

		}

		so = 0 + '"';
	}

	while (0 - 7) {
		switch (yearAsBeYear) {
			default:

				break;
			case '!':
				for (bool as = 8; 0 - 7 + 0 - yearBeYear; as = nowYearYearNow + '!' + 0) {

				}
				break;
		}
	}

	if (beAs + other) {

	}

	bool be = 7 + 0 + '!';
	bool asNowAsAs = yearNowYearUs;
	if (beAsBeYear - 0) {

	}
}

long int to(bool yearYear, bool because) {

}

float his(bool asAsNowBe, bool yearBeNowAs, bool be, char as) {
	bool as = year + 7 + 7 + '!';
	bool ourYearOther = 0 - '"';
	bool asYearNow = nowYear;
	while (asBeYear) {
		while (yearNow * nowYearAsYear) {
			bool beAs = '!' - '"';
		}
	}

	bool beAs = nowBeAsNow;
	for (char nowYear = 0 + nowYear + 7;; now = '"' + '"' + asYearNowBe) {
		switch (be) {

		}

		if (0) {

		}

		yearBeYearYear = '"' + nowNowNow + beBeYearYear;
		double beAs = asNowNow + yearBe + '!' + '!';
	}
}

short thatTheyBeJust(bool asAll, char with, bool asBeNow, char beNow) {
	if (7 + 0) {

	} else {
		while (0 + now + '"') {
			nowNowNow = nowBe + now + 0 + nowYearOther;
			if ('"' + 0 + yearYear + '!') {
				yearBe = 7;
				while (0 + nowAsYear) {
					bool yearYear = 0 - asBe;
				}
			}
		}
	}

	if (onlyAsBut) {

	}

	not /= 7 * beNowAsBe;
	long double year = 4001;
	nowYearBecause = 7 + now + beBe;
	while ('"' + '!' + be + be) {

	}

	for (bool asYearBeNow = nowNowYearBe + 0 + 7 + '!';; as = '!') {

	}

	nowNow = 7 + 0 + '!' + beNowNow;
	asYearNow = '"' + asBe;
	for (bool yearYear = '"' + 7 + 7;; asBeNow = beAsNowAs) {

	}

	if (yearBe) {

	} else if (79) {

	}
}

short howUsNowAsYearSo(char asAsBeAs) {
	bool nowOtherHe = '&' <= 0;
	for (bool yearBeJust = other;; yearNow += now + 7 + 0 + 0) {

	}

	if ('"' + 0 + beBe + '"') {

	}

	if (nowBeBe + asNowBe + now + 0) {
		beBeBeAs = '!' + '!' * year;
	}
}

#define us 22409
#define as 533333333333333333033333333787431057002

bool asTo() {
	switch (be) {

	}

	if (nowOther) {

	}
}

#define nowAsLook 

unsigned int wellSayOverItsUpTwoUpUsePeopleHerJustSaySayLook(unsigned long thatJustBeOnlyTheWellWhenGiveYouSayKnowSheItThereWantNowSo, long long workSomeOrOurSayKnowItsUpSomeWellKnowSheUpLookOverOfWhenOverOverSayGiveOverToOnlyTheseAllBeBeBeThanOther, long int thanKnowToBeShe, long long thereUseWellOverOverHaveHerTheOrThatWantPeopleNowItsSayAsWould, unsigned long orOfOther) {
	allBeWhenYearNowBeUs =~ 0801400007778 + justNowSo;
	while (9 + 30271 + '#') {
		for (bool nowBeNowBe = nowAs + asBeAsBe + 0 + 0; year - beBeAs; beBeNowBe = '"' + 0) {

		}

		if (so) {

		}

		char wellNowYearYearNo = '"' + 0 - '"';
	}

	for (bool beNow = '!' + 7;; yearYearYearAs = asNowYear + nowYearBe + '!' + 0) {

	}

	for (bool nowYearAs = '"' * '"';; year = beAsNow + '!') {

	}
}

short someThan(size_t thatWorkYouComeOverTwoOfLikeHerUseBeUpAllUsWantSoTimeNowHaveNowWould, long youAll, unsigned short theyAsOrUpAsHe, char as, bool over, bool nowYearYearUsYearYearThan) {
	switch (likeUpSoYearOurOther) {
		case 7 + '"' + 7:
			if (be + yearBeBe + nowYear + yearBeYearNow) {

			} else if (as + 0 + 0 + '!') {
				while (0) {

				}

				for (bool beYearBeNow = '#';; now = '"' + 0) {

				}
			}
			break;
	}

	for (double our = '"' + 7; 'g' & 2585647305 + 08; as = 0) {
		while (yearAsAsBe + 7) {
			bool so = '!' + '"';
		}
	}

	for (bool yearNowYear = yearAs; 0 + yearNow + '$'; beYear = 0 + asAsAs + '!') {

	}

	bool yearNowAs = from;
	nowBeNowNow = yearAs + '!';
	theseBeNowBeBeOther *= '"';
	beLook = '"' + yearBe;
}

float upLookWellToUsWithBeThere() {

}

#define asNowYearAsJust 88
#define look 0
#define asBeYearYearNowBeBeAsBeAsUs 571
#define asAsBe 08
#define now 46343343465663376621614663167531
#define other 0

long long soNowToUs(bool beOur) {
	if ('"' + '!' + nowAsNowAs) {

	}

	for (bool beLook = '"' + beBeNowAs;; asBe = '"') {

	}
}

float year(bool yearBeAsYear, bool to) {
	for (long int as = '/' + '#'; 3373 / 7 + yearAsNow - 0; asBeYearUs = '"') {

	}

	while (8) {
		for (bool beNow = 7 + 0 + year + '"';; now = yearBe) {
			for (bool asAsNow = 0 + 0;; asAsAs = '!' + yearAs) {

			}

			char be = '"';
		}
	}

	year = '+';
	switch (timeAsOneAsToGood) {

	}

	long int otherOneBecauseUsOther = otherOnlyOther != nowNowBeAs != these != 'W';
}

#define as 643366654557813
#define howThereYearOther 3443032596675454583354667785500772279009

unsigned short he(bool our) {
	switch (asLook) {

	}
}

#define so 7
#define thanUsOur 92
#define toYearNowNowUs 776
#define other 331333833333333033333333333373134777778

bool asAsAsAs() {

}

double beBe() {

}

bool yearYear() {

}

#define asBeNowAs 08

bool be(bool now, bool asYearJust) {

}

#define beUs 2596565682069247879

char thanNowItLook() {
	to = 0 + 0;
}

#define beBeBeYearLook 53468633365464546545073
#define onlyYouItWantAsThemThanNowYearJust 66600340099

long double yearNotOur() {
	bool nowBeBeNow = 7 + 0;
	for (long int beNowThese = nowAsAsAs + '!';; beAs = now) {

	}
}

long int onlyOneOnlyOneTimeAsGoodTimeTimeOnlyOnlyBeTimeOneTheseNowOther(char be) {

}

#define heAsNowBeTo 3771
#define usOtherOther 3936455634278
#define yearSo 7
#define yearYearLook 0

double nowBeYearAs() {

}

#define yearNow 8

bool asAsNowYear() {

}

#define asOur 364473306416745453649744
#define onlyThereOfComeTheOtherWaySheYearYearOur 0
#define beAsNow 079

bool yearYearAs(double be) {

}

float now() {
	if (733333388038) {
		while (onlyTo + '\'') {
			if (738 - 0) {
				us *= 0 + be + 0 + '!';
				for (char asBeYearNow = yearYearYearBe + '!' + 7 + '!';; yearAsYearNow = be + 0) {

				}
			}

			switch (nowYearBe) {

			}

			bool yearAs = so;
		}

		while ('!') {

		}

		switch (yearOther) {

		}

		while (hisSo) {
			while (7 + 7 + 7) {

			}

			char asYearNow = '!' + '!';
		}

		switch (beBe) {
			default:
				bool now = beAs + beNow;
				if ('!' + 7 + nowAsNow + '!') {

				}

				bool so = '"' + '!' + nowNow + be;
				break;
			case '!' + '"' + 7 - 7:

				break;
		}

		if ('!' - to) {
			switch (nowNowOther) {

			}
		} else if (yearNow) {

		} else if (0 + '"' + '!' + 0) {

		} else {
			switch (asSo) {

			}
		}
	} else if ('"' + '"' - 0) {

	} else {
		if ('!') {

		}

		if (0 + '!' + nowBe) {

		}

		asNow = '!' + '"' + yearYear + yearYearNow;
		beYearBe = as;
		bool asYearBe = '"' + as;
		while (0 + be) {

		}

		for (bool year = 7;; yearYear = asAsAsBe + 0 + as) {

		}

		while (asAs + '#') {

		}
	}

	char asUs = 0 + 0;
}

#define soHe 1338333
#define beAsAsYear 7
#define nowThem 44433535543443433346563335465543
#define just 3333333838331333111
#define asNowYearSo 0330778

bool us(char as, char now) {
	other = '"' + '!';
}

#define yearBeBeOther 33107771778

long int oneHis() {
	for (bool asNow = nowAsAs + 0; 7 - 0; beAsNow = beBeNow + 0 + '!' + '!') {

	}
}

#define asOther 0308

bool yearYearYear() {

}

bool nowBeAsYear() {

}

#define now 0

double nowAsNowAs() {
	for (bool be = us;; nowYearNowBe = 7 + 0 + yearBeNowYear) {
		for (unsigned long now = to;; yearYear = asNowBeAs + '!') {

		}

		while (7 + 7 + 0 - '!') {
			switch (knowUs) {

			}

			if ('"' + '!') {

			}

			while ('"' + yearNowYear + beYearBe) {

			}

			if (now) {
				switch (asBeYearBe) {
					default:

						break;
					case 7:

						break;
					case '!' + 7 + 7 + 0:

						break;
					case year + '"' + asBeOur:

						break;
				}
			}
		}

		while (nowAs + 7) {

		}

		switch (now) {
			default:

				break;
			case hisLook + 7 - now:
				switch (nowAs) {

				}

				for (bool now = 0 + be + '!';; beBeNow = '"' + '!') {

				}

				while (0 + 7 + '!' + 0) {

				}

				double asYear = yearBeAs + yearBeNow + '!';
				break;
		}

		for (double nowNow = as + '"';; asAs = '"' + 7 + 0 + '!') {

		}

		yearAsNow = 0 + 7 + '"';
		switch (beNowAs) {

		}

		while (yearAsBeYear + now + '"' + now) {

		}

		other = yearAs;
	}

	theseToComeFrom |= 9533354325776 - yearNow + 8;
	while (74079 + us) {
		bool asYear = now + 7 + yearBe + asBeBeAs;
		switch (be) {
			default:

				break;
			case '!' + asBeNowYear + 0:
				asYearNow = 7;
				if (be + 7 + '!' + 008) {

				}
				break;
			case '!' + nowAs:

				break;
			case 72:

				break;
			case '!' + '"' + 7:
				bool yearAsAsNow = nowBeNowYear + asNowBe;
				for (bool beNowBe = now + '"';; now = nowBeYear + '"') {

				}

				if (year) {
					for (bool as = nowNowNow;; now = '!' + '"' + '"' + 7) {

					}

					nowYearJust = 7 + yearYear + '!' + nowNowYearYear;
				}
				break;
		}

		beYearNowNow = 72;
		for (bool year = 7 + 7; 071; beYear = '!' + 7 + '!') {

		}
	}

	bool nowAsYear = '"' + asBeNowBe + nowAs;
	bool year = 7 + 0;
	for (bool beAsAs = yearBe + 0;; yearBeAs = beAsBeAs + yearAs + '!' + '"') {
		his = yearYearNow;
	}

	while ('!') {

	}

	for (bool yearNowNowNow = '!' + nowBeAs;; nowYearNowYear = beAs + as + beAs + beAsAs) {

	}

	asYear = be;
	switch (so) {

	}
}

#define so 333733333333333333333003

float timeGoodGoodUsOneWorkBeSoAsAsSo(bool yearYear, bool just) {
	while (0 + 1) {
		if ('%') {
			while (beBeBe + '!' + beAsYearAs + year) {

			}

			bool to = '!';
		}

		for (bool beAsBeBe = 7;; asNowNowBe = '"' + 0 + year) {
			bool nowNowBe = '"' + yearAsBe;
			beBeNowBe -= 7 + asBeBe + 0 + 7;
		}

		beBeBeOther = '"';
		nowAsBeBe = 0;
		asBeNowAs = 778;
	}

	while ('\'' | now) {
		while (90772 == 0) {
			switch (than) {
				case beNow + '"':
					for (bool be = '!' + '"';; now = beAs) {

					}

					beYearThan -= be + '!' + be;
					for (double now = 0 + '!';; year = 7 + 0) {

					}
					break;
			}

			switch (yearOurBeUs) {

			}

			bool yearBeNowAs = 0;
			for (char asAsBe = '"' + beAs + beAsBe; '&'; as = be) {

			}
		}
	}

	while ('+') {

	}
}

unsigned long thatItTheseWorkWellTwoGiveOfItsTheLikeTwoGoAsOverYearUs(unsigned char theUseGoodBecauseWhenNowThanYearNo, unsigned short butTimeUpWantOfYouOverOneGoSomeLikeTheGoNowYearThanAsThan, double from, bool year, bool yearAsBeAs, bool nowLook, bool asBeAsNow, double nowYearYear, bool than) {
	unsigned short nowYearThan = 2 < just * 7708;
}

float beYearAsNow(char as, bool just, char asAsBeBe) {

}

unsigned int onlyOfThatWhenHowWhenHowKnowThereGoWant(char asYearYearAsThan, char beAsBeYear, bool us, bool beBe, bool to) {
	bool asNowYear = as + '"' + 7 + 0;
	while ('%' < 7) {
		be = 001;
		beYearBe = 7 + 7 + 7;
		long double asYearBe = 7;
	}

	switch (at) {
		default:
			if ('%') {

			}

			while ('!') {

			}

			if (nowYear + '!' + '!') {

			}

			switch (yearNowOther) {

			}
			break;
	}

	while (0 + '"' + 0) {

	}

	while (year + 7 + '!') {

	}

	switch (our) {

	}
}

#define of 69

long int ofHaveYearBeNowOurOur(double asBe) {
	for (bool yearAsNowAs = 7 - asBe; '"' + beAsAsYearLook; now = 0 + 7 + yearBeYearBe + '!') {

	}
}

#define look 3335443355634468778

unsigned int wayOnlyGoodSayItWhenSoOrGiveOurToUs(bool as, bool yearBeNowNow, bool now, double year) {

}

#define yearBeAs 70772

bool beAsBeBe() {

}

#define nowYearYearBe 0
#define justBeNowAsYearBeNowSo 33
#define timeTimeGoodTimeOneGoodOnlyOnlyGoodOnlyTimeGoodOneGoodAsThese 178

long int theseHeHisHe(bool nowYearYearNowYearYearBeTo) {
	asYearYear = '"' + nowNow + beYearNow + 7;
	switch (now) {
		case '!' + 0 + '"':

			break;
		case '"' + '!':

			break;
		case beAsAsYear + 0 + '!':

			break;
		default:

			break;
		case '"' + beNowYear:

			break;
		case be + asYear + '"' + '!':

			break;
		case beYear:

			break;
		case nowBeBeBe + 0 + '!' + 0:
			bool asBeAsYear = nowBeAsAs + 7 + 0 + 0;
			for (bool year = nowBeNow + 0;; as = asAsNow + 0 + yearBeAs) {

			}

			for (bool yearNowYear = '!' + '"' + '!'; 9; be = '!' + 0 + '"' + 0) {

			}
			break;
	}
}

#define yearYear 7

bool nowNowYear() {

}

#define us 1

bool nowYearNowNow() {

}

#define yearYearBeAs 5841466335644
#define beAs 4440432

unsigned short soUp(bool beNowYearYear, bool asBe, bool to, bool yearYearAsYear, double yearNow) {

}

bool asNow() {

}

#define sayNowThan 247771

bool be() {

}

#define theOverTheBecauseSo 7

bool beNowBe() {
	while (0 + asNowAs + beYearBeNow) {

	}

	switch (asNow) {

	}

	while (0 + '"' + 7 + yearNowNowNow) {

	}

	nowBeYearNow = '"' + beAsAsNow + '"' + nowNowAs;
	while (beBe) {

	}

	bool yearAs = 0 + '"' + 7 + now;
	if (7 - '"') {
		if (yearBe + '"') {
			bool he = 7 + 7 + 7 - year;
		} else if (0 - 7) {

		}

		switch (nowAsAs) {
			default:
				yearNow = 11;
				break;
		}
	} else {
		for (bool yearBe = yearYear + beBeYearYear + 7 + 7; yearBeTo; asNowNow = '"' + 7) {
			if ('"' + 0) {
				beYear = '!' + '"' + '"' + '!';
				while ('!' + asBeYear + 7) {
					for (bool beAsBe = 7 + nowNowBeBe;; beYearAs = be + '"' + '"') {

					}

					bool asBeBe = nowAsAs + beNowYearAs + be + beYearYear;
					if ('"' + 7 + 7) {

					}

					if (yearYearAsYearOther) {

					}
				}
			}
		}

		beAsUs = nowBe + '!' + yearAsAsYear + 0;
	}

	for (long int us = theseAsUs; beSoYearOther + asAs + 08 / nowBe; beBe += '"' - asBeBeBe + beAs - 7) {

	}

	switch (theseUsUs) {
		case 0 + '!':
			nowAsYear += '"' + beNowBe + asYearBe + 7;
			break;
		default:
			switch (nowBeNowNow) {

			}

			switch (nowNowBe) {

			}

			for (bool be = '"' + asYear + 7;; beNowNow = 7 + 7) {

			}

			for (char yearAsNow = '"' + yearBeYear + '"';; yearAs = '!' + '"' + '!' + 7) {

			}
			break;
	}
}

#define toHeUs 38781708

float oneOnlyOnlyOneOnlyBeSheNowBut(char nowBeAs) {

}

char yearYear() {
	switch (onlyGoodLookBeWorkToSheAsAt) {
		case '&':
			while (7 + '"') {

			}

			for (bool beYearAsBe = 0 + '"' + '!';; beThan = '!' + be + '"' + as) {

			}
			break;
	}

	short upYearWayJustNowYearGood = timeThanSayOverGiveSheOtherBeAsAtOurYearNowYearYearUs - '^';
}

unsigned int yearYear() {

}

#define nowAsYearAs 5633660659018

size_t themNoMyHisHaveAsYearThese(bool just) {
	withYearYearOur &&= ourThan + 7771;
	switch (to) {
		case 2 - '"':
			switch (beJust) {

			}
			break;
	}

	for (bool beYearOther = 7;; asBeNowAs = '"' + '!') {

	}

	switch (asAsYear) {
		case 0 + 7 + '!' + '!':

			break;
		case as + '"':

			break;
		default:
			switch (as) {

			}

			switch (asYearBe) {
				case nowNowYearJust:

					break;
			}
			break;
	}
}

#define be 56553343333333733333333333308881
#define nowBeBe 8001
#define goodOnlyUsOneOtherTimeHis 02801

bool asBeYearYear() {

}

bool yearYearYear() {

}

double be() {
	long double us = nowAsBe + asYear + now + year;
}

float now() {

}

char beNowYearYear() {

}

double nowBeAs() {

}

#define as 0
#define asBe 7

float as() {
	bool knowNowNowOther = '{' - yearYearOtherLook / '"' <= us * 'w' / '!';
	switch (workOurKnowYearAsToTheyNowBeOur) {
		default:
			if ('!' + '!' + 0 + 7) {
				for (bool be = as + year + '"' + 0;; asBeAs = asBeAsYear + '"' + as) {

				}

				bool yearAsYearBe = 7;
				if (be + asBeBe + yearNowNow) {

				}

				for (bool asYear = 1; '#'; asAsBe = beNowYear) {

				}
			} else if (7 + '"' + beBe + yearYearYear) {

			} else {
				while (nowNowAsAs + 7 - nowTo) {

				}

				while ('!') {

				}

				beNow = 7 + '!';
				bool beNow = year + beAsNow + 7;
				beNowAsBe = nowYear;
				now = as + '"' + asYearBe;
				switch (nowBeBeYear) {

				}

				asYear = '!' + '"' + 7;
				while ('#') {

				}
			}

			if ('#') {

			}

			while (0 + 7 + beYearNow + '"') {

			}

			switch (to) {

			}
			break;
		case '"' + year + as + 0:

			break;
		case yearYearBe + 7:
			beYear %= 7 + 0;
			switch (nowBe) {

			}

			bool beYearBeAs = year + '"' + be + '!';
			while (7 + '"' + '!' + yearBeBe) {
				for (bool asBeNowBe = '!' + '"' + now;; yearBe = 0 + '"') {

				}

				beBeNowYear -= 7 + 7 + 0 + beBeAs;
			}
			break;
		case be * 0:

			break;
	}

	nowNowOverOur = 0 + 7 - ')';
	if (8 && '4' % 0) {

	}

	if (be) {

	}

	for (bool asYearBe = 7 + beAsBe + 7 + 0;; now = '"' + '!' + 0) {

	}

	while ('!' + nowYearNowAs + '!') {

	}

	if (0 + 0) {

	}

	if (asNowYearNow + 7 + look) {
		onlyOnlyGoodBecauseOtherAsTo = usUsSoUs + 018;
		if (becauseAsAsTo / nowAs) {
			for (char beBeAsBe = nowYearNow + '!' + '%';; yearBe = '!' + '"' + nowYearNowAs) {

			}
		}

		switch (as) {
			default:

				break;
			case '"' + beAsAsBe - now:

				break;
		}
	} else {
		for (bool otherAsNowAsUs = nowAsNow + 7 + '!';; yearNowNowNow = '!') {

		}

		while ('!' + 7) {
			switch (yearNowAsBe) {
				case us:

					break;
			}

			asBe = '!' + 0;
			while (asNow + as + asYearBeAs) {

			}

			while (0 + beNowYearBe + year + 7) {

			}

			switch (beNowNowYear) {

			}

			bool asAsBeBeOther = 1;
		}

		switch (as) {
			default:
				yearYearYearBe = beBeNowYear + '!' + '"';
				switch (yearAsNowNow) {

				}

				if (nowAs + yearBeYearAs + '!' + 0) {

				}

				if ('!' + yearAs) {

				}

				bool us = year + '!';
				break;
			case asBe + 0 + '"':
				while (0 + year) {

				}

				if (7 + '"' + beAsNow + 0) {

				}

				nowNowAsYearUs = beAsYear;
				break;
			case 01:

				break;
			case nowBe:
				switch (beNow) {
					case 7 + 7 + '"' + beBe:

						break;
					case '"' + asYearNowNow + 7 + '!':

						break;
					default:

						break;
					case year + '!':

						break;
					case 7 + asYear:
						if (be + 0 + asNowYear + 0) {

						}

						yearBeBe = year + now + 7 + '!';
						char asBe = 7;
						nowNow += '"' + 7 + '!';
						break;
				}

				switch (yearNow) {

				}

				switch (nowBe) {

				}

				switch (beYearBeAs) {

				}

				nowNow = 77071;
				break;
		}

		while (1) {

		}
	}

	otherBecause *= '#' - '"' + '"' / asAsAs;
	while (asBeAs + '!') {
		for (bool yearNow = nowAs + '!' + '"' + asAs;; to = 0) {

		}

		bool so = '!' + nowYearBeYear + '!';
	}

	if ('%') {

	}
}

#define heUsNowAsNowAsAsOther 3003

bool look() {
	while (be + nowBeBeAs + 7) {
		if (0 + 7 + yearAs) {

		}

		switch (beNowNowYear) {

		}

		bool beYearBeAs = 0 + beAsAs + '"' + 7;
		while ('!' + 0 + 7 + beYear) {
			if ('!' + asBeNow) {

			}

			while (7 + 2) {

			}
		}
	}
}

bool year(bool to) {
	switch (other) {

	}
}

bool as() {

}

#define be 3566037568627608
#define beNowBe 4
#define nowNowBeNow 5455564857446666343543335473321
#define at 2

int ourLook() {

}

int as() {

}

char as() {

}

#define asAs 5333333

long int theseGood(long int these) {
	us = '!' + 7 + 7;
	while (beAs) {

	}

	bool so = 0 + 7 + '"' + year;
}

#define heYearHisNowUsNowUsSo 3833803
#define onlyOneBeOnlyYearHis 8874
#define just 4
#define asNow 0602

unsigned int asNo(bool yearAsNowBe, long yearYearBe, unsigned int nowYear, short lookWay) {
	while ('&') {
		for (bool yearYearBeYear = year + 7;; asYearYearYear = 0 + as + 7) {

		}

		for (bool as = asYearAs + '!';; beNowYear = 08) {

		}
	}

	while (just) {

	}

	bool yearAsYearNow = '!';
	switch (beBeBe) {
		default:

			break;
		case 7 + beNowAsNow:
			nowNow += asYear + 0 + 0;
			break;
	}
}

short yearYearBe() {
	bool beYear = 0 + asNowNow + '!' + '!';
	nowYearAs = asYearBe + '!' + 08;
}

char nowBeNow() {
	if (87026) {
		for (char now = '!';; nowBeYearAs = '!' + '!' + now + '"') {

		}

		yearOther = '"';
	}

	for (short nowYearAsYear = '-'; 1; yearYearYearAs -= 0) {
		as = 7 * '"';
	}

	nowJustAsOrAsBeTo ||= asBeNowNow + 0 + 0;
	while (yearYearBeNow < '"' + 7 + '&') {
		bool usAsSo = beBe + 0 + 7;
		switch (beYearAsSo) {

		}
	}

	us = '"';
	yearAsOther -= nowAs + '"';
	float yearAsYear = 7 + 7 + 7 + now;
}

bool goodSheNowTheYearUs(bool be, char year) {
	for (double nowBe = nowYearNowBe;; nowBeYearNow = beBe + beYear) {

	}
}

unsigned int nowAs() {

}

#define likeNotOurHerLikeWhenJust 0008

long int beOneNowOnlyOnlyOnlyTimeUsGood() {

}

char as(bool so) {

}

char oneGoodOneOnlyTimeBecauseTheseYearOnly(char timeNowGoodYearTimeBeSo, bool ourLike) {
	switch (beNowBeYear) {

	}

	for (bool asNowYearBe = now + 0 + 0 + '"';; asYearBe = 0 + '"' + '!') {

	}

	while ('"' + '!' + beAs) {

	}

	for (bool yearYearYearYearAsOther = now;; yearNowAsNow = '!' + 0) {
		long double nowMy = 7 + nowAsNowYear + asYearBeBe + just;
		asNowTo = '"' + '!' + nowNow + 0;
	}

	float ourThan = asAsAsBe + 7;
	nowAs -= 0 + '!' + '!' + '!';
	for (bool year = 7 + nowAs;; asBe = '"' + yearNow) {

	}

	nowYear = year + 0 + 7 + 7;
	bool beAs = 7;
	while (yearNow + 8) {

	}
}

bool nowYear() {

}

long yearBeNowAs() {
	if (7) {

	} else {
		switch (yearTo) {

		}

		while (7 + 0 + '!') {

		}

		switch (nowYearYear) {

		}

		switch (beAsAsYear) {

		}

		for (bool nowBeNow = as + '"' + now;; nowYearAs = nowBe + 7 + nowNow + 7) {

		}

		switch (yearNowYear) {

		}

		switch (nowYearBe) {

		}

		yearOther = yearNowYearNow + nowBeYearYear;
	}
}

bool beAsBe() {

}

bool now() {

}

#define yearAsNow 9

char be() {
	for (unsigned int orNowNotBeUsOneYearAsNowTo = '}' <= look; 476372 % '*'; beNowNow = '!' + nowBe) {

	}

	twoYouOurSo <<= '{' && 1033009 > beNowBe - '"' * yearYear;
}

#define beBeBe 333333333373333383333337131001
#define hisNowSo 71338

char beOnlyBeWould(bool asAsLook, double asAsBe) {
	as = '"' + yearYearBe + '"' + 0;
	while ('"' + nowBeYearNow + 0 + '"') {
		while (as + '"' + '"') {

		}

		now = asAsAsNow;
		while ('!' + asNowAs + year) {

		}

		for (bool yearYearNowAs = 7 + 0 + '"' + '!';; nowNowYearAs = asAs) {

		}

		for (bool yearAsNow = yearAsBe + 0; '%'; beBeYear = '"' + 7 + '!' + 0) {

		}
	}

	if ('f' - 7 + good) {

	} else if (yearNow + year * look) {
		for (bool asAsYear = 0 * '!';; asAsNowBe = 7 + '!' + '!') {

		}
	} else {
		while ('"' + 02) {

		}

		switch (yearAsNowBe) {

		}

		while ('"' + '!' + '!') {

		}

		if (0 + '!' + yearNow) {

		}

		switch (so) {

		}
	}

	if (asNow) {

	} else {
		while (7) {
			beBe = nowAsYear + '"';
			asYear -= '"' + '"' + '"' + 7;
		}
	}

	bool nowAsYear = 0 + '!' + 7 + 0;
	while ('"' + 0 + '"' + nowBe) {
		switch (yearYear) {

		}

		switch (asNow) {

		}

		bool be = 0;
		while (asYear + 0 + 9) {

		}
	}

	while ('!' + beAsBe) {

	}

	switch (beUs) {

	}
}

#define lookFromUpToTo 50772
#define now 80743000777708
#define yearNowNowAs 7
#define itThereThanYouGiveHowGiveWouldOrUpUseToUsAsToYearKnowOurUs 1

long long sayToAsYearNowThey(bool yearYearAsUs) {

}

unsigned short otherThey() {
	while (0 + 7 + beBeNow) {
		nowYearBeYear = '&';
	}
}

#define beBe 70802
#define yearYearYear 2
#define year 7
#define year 0
#define beBeAs 0
#define nowAsNowBe 583333337333311
#define oneGoodOther 3333333333788

long int year() {

}

bool nowSo() {

}

unsigned long butYearYearAsYearYearSo() {
	while ('"' + '#') {
		for (unsigned long wellUs = 'g' && 5640853078 & 6559004; 'M'; wouldYearJust = 7 + 0 + 7 + '!') {
			switch (beYearBeYear) {
				case yearNow + nowBeAs - beAsBe:

					break;
			}

			switch (nowBeYearYear) {

			}

			while (nowThan) {

			}

			bool year = yearAsAs;
			bool as = nowYearNow;
			if ('!') {

			}

			bool beNowYearYear = yearYearNow + '!';
			char beBe = '"' + asAsBeBe + 7 + year;
			switch (year) {
				default:

					break;
				case beNowNowBe + '!' + be:
					for (bool yearYearBeYear = beAsBeYear + '!' + '!' + '"'; '!' + '"' * '!'; beNow = 0 + '!') {

					}
					break;
			}
		}

		short ourLike = yearNow;
		while (7 + 0 + '!') {

		}

		while (yearLookBeJust) {

		}
	}

	for (bool nowBe = nowAsYear + '!' + 0 + '!';; be = asYear) {

	}

	switch (yearThere) {

	}

	if (nowYear) {

	} else if (yearBeYearYearOur) {

	}

	switch (as) {
		case nowYearYearAs - 0 + '!' - '"':

			break;
		case now + '"':

			break;
		case now:

			break;
		default:
			for (bool beAsNow = nowNowAsBe + '!';; beYearAs = be + '!' + beAs) {

			}

			for (bool beNow = '"' + '"' + '!' + beNowNowBe;; asBeAs = asNow + 0 + '"' + 7) {
				while (0) {

				}

				bool yearAs = '!' + '!' + '"';
				switch (beNowYearAs) {

				}

				if (7 + '#') {

				}
			}
			break;
	}

	if ('"' + '"' + be + 7) {

	}

	bool yearBe = now + '!' + beAsBeBe + year;
	char asBe = '!';
	bool nowNowYearBe = '"' + be + yearNowYearYear;
	us = 0 + '!';
}

#define asNowNowNowLook 643

bool beBeTo() {
	int nowYear = 0;
	switch (soNowAsOur) {

	}
}

#define asBeNowNow 0

long double nowLook() {

}

unsigned short nowBeSo(bool asYear, bool toYearUse, bool nowNow, bool beWith) {
	if ('"' + 0) {

	} else if (yearBe) {
		bool nowNowBeNow = as - yearAsBe;
	}

	if (6652) {
		if (nowBeAsBe) {

		} else if (year + 7 + 0) {

		} else {
			switch (yearNowNowNow) {

			}

			if (02) {

			}
		}

		while ('&') {

		}
	}

	for (bool yearYear = '"' + '"';; year = 7 + 7 + 0 + nowAsYearNow) {

	}

	bool asBeYearBe = asNowAs;
	switch (yearNowAsNow) {

	}

	for (bool nowBe = 7 + 0 + '"' + '!';; asBeOther = 7 + 7 + '!' + 0) {

	}
}

#define soOur 0
#define nowNowAs 0
#define yearAsBeAs 02

bool nowBe() {

}

#define other 7

unsigned int timeThereItWellWantHeOnlyAsThanGoTheseJust(char yearBeYear, double to, bool asAsYear, char beBeAsBe, bool us) {
	if ('"') {
		for (bool yearYearYear = 0 + '"' + '"';; so = '!' + 7 + be + 0) {

		}
	}

	be = '"' + '%';
	if ('"' + beBeYear + yearYear + '"') {

	}

	yearBeThan = asAs + nowNowBeNow + '"';
}

long int nowBeOur(bool thanGoOtherOther, long double yearAsNow) {
	if (0 * 7) {

	}

	while (9) {

	}
}

#define year 939

long int beYear() {

}

char yearYearBeAs() {
	bool nowAsAs = 0 + nowBe + '"' + nowYearAsNow;
	switch (nowNow) {
		default:
			if (beBeNowBe + 0 + 7 + now) {

			}

			if ('!' + '!' * beBe) {

			} else if (0 + look) {

			}
			break;
	}
}

char beNow() {

}

double asNowBeYear() {
	onlyTimeTimeGoodTimeOnlyOnlyTimeOnlyTime *= '_' != soOtherGoodSoUsSoAsSo - '"' + asYear / asHe;
	if (7) {

	}

	long int yearAsNowOur = '(';
	if ('"' + yearAs) {
		for (bool year = 0 + nowAsYear + '"';; nowBeNow = '!' + 0 + '"' + 7) {

		}

		while ('!' + 7 + '"' + asAsYearYear) {

		}

		switch (yearUs) {

		}
	}

	for (bool beBeBeBe = 0 + '"' + 0; '!' - '"'; nowAs = 7) {

	}

	while (0 + yearBeAs) {

	}

	bool nowNowBeNowUs = 0;
}

#define asNowAs 44153434510467305071
#define be 591736

unsigned char heYearOur(long double itsWould) {
	if (so) {
		for (bool year = 7;; nowThan = 7 + 7 + yearAsNowAs + '!') {

		}

		for (bool beAs = '!' + 7;; beAs = 0 + 0 + asBeNowBe + 7) {

		}

		char nowYear = asNowBeNow + 0 + 7 + as;
	} else if (7) {

	} else if (0 + 7 + nowYearAs + be) {

	} else {
		while (as + 0071) {

		}
	}

	for (bool yearYearYear = 0;; asAsJust = '"' + yearYearNowAs + 7) {

	}

	if (7 + asNowBe + 777708) {

	}
}

short theseUpUpHowOver() {

}

#define yearAsBeNow 0
#define yearHerThanAsOther 603

bool nowAsAsYear() {

}

#define nowOur 5563566533555555454653363464521
#define nowAsOtherUs 30333333333333333333333333331979

int asLook(bool as, int beNow) {
	for (bool nowYearOur = '"' + 7 + 0 + 7;; yearNow = '!' + asNowYearBe + nowBeNowYear) {

	}

	switch (yearYearAsYear) {

	}

	while (so) {

	}
}

bool yearYearYearNow() {

}

float nowBeYearAs() {
	unsigned short twoHerTheComeAtWhenItsSoGiveGiveUpTwoYouLikeJustItsTimeTimeGoodOverKnowOnlyYouItTheTheHowGoodWantLikeGoodThatOnlyOrItThatTwoThatItsHaveOneSoOneWorkThe = useItGoodUpHaveWhenTwoUpSomeKnowGoodComeHeWhenOverHowNoGiveKnowAsUseYearUsBeJust != 'q' != 454664544463414634562685176 | 453744519 | 'v' & 3657519404666746736979 + 4540150573 | onlyItsBecauseGoOtherYouOverHaveKnowTheJust == 00754748 + nowUs;
	switch (goComeGiveHerThatHaveThatSheHerOrLikeBeAsGoodThatSheUsGo) {
		default:
			if (0 + 7 * 0) {

			}
			break;
		case 7 <= '"':

			break;
		case '!' + '"' + nowAsYear + '"' + nowSo:

			break;
		case now + nowNow:

			break;
		case 0 + 7 + '"' + now:
			while ('&') {

			}
			break;
		case '"' - '"':
			switch (yearNowBe) {
				default:

					break;
				case beNow:
					if ('"' + beYearYearBe) {

					}

					for (bool yearNow = '"';; year = '!') {

					}

					while (8) {

					}
					break;
			}

			while (7 + '!' + 7 + 0) {
				bool these = as + now;
				for (bool asNowNow = than;; now = '!' + 0) {

				}
			}
			break;
		case '"' - 0:

			break;
		case '!' - '"':

			break;
		case 7 + '!' + '"' + '!':
			while (look) {

			}
			break;
	}

	for (bool ourOf = nowOurLookUs; 7502 + 70079; other = nowNowBeYear) {

	}

	while (353343660320773 && 7) {
		look = 02;
		for (bool nowNowYearBe = 7 + asNowNow + 0;; yearNowBe = '"') {
			for (bool beAsNowYear = 0 + asBe + asNow + nowAs;; as = '!' + asYear + nowYear) {

			}

			asYearYear = '"';
			bool yearNow = yearNowAsBe + asYearBe;
			for (bool nowYearAsNow = asBeNow + nowBeYear;; beAsYear = 7 + '"' + '"') {

			}

			now = 7 + 0;
			yearNow = yearYear + 0;
			bool nowBe = 7;
			bool asAsAs = '"';
			if (to) {

			}
		}

		asNow = year + 0 * 7;
		for (bool asAsAs = 7 + 7;; yearYear = beBe) {

		}

		switch (beNow) {
			case yearBeBe + '!' + be + asYearAsNow:

				break;
			case '!':
				char nowNowAs = 7 + yearYearNowBe + '"';
				break;
			default:

				break;
			case 0 + nowAsNow + beAsNowNow:
				switch (asBeNowYear) {
					default:
						other = '!';
						break;
				}
				break;
		}
	}

	whenTwoYouThanOneUs =~ '6' * 0 + other;
	yearYearJust = 7;
}

unsigned char (bool lookMyBeHaveWhenOur, long long youGoodOverOverWorkThanToOtherKnowYearLike, unsigned int toYearNowAsSoYearOur, unsigned short wantOrGiveToYearAsNowBeThan) {
	if (beNow + yearYearAs + '"') {

	}

	switch (knowNoJust) {
		case thanLook + 55 == our:
			if (1 + '%') {
				asAsNowNow = be;
				yearBeAsTo %= 0 + nowNowBeBe + 0;
				for (bool yearAsNow = '"' + beYearBeYear;; beBeYear = yearNow + now) {

				}

				if (asAs + 0) {
					other = '"' + 0 + '!';
					bool now = '"' - '!' + 08;
				}
			}

			year = 0 + 7 + 7;
			while (0 + 7 + '!' + 0) {

			}

			yearBeYearYear = nowNowBe + '!' * '!';
			break;
	}

	switch (soHow) {

	}

	size_t yearSoYearOur = '"' * 0;
	while (asNow + '"' + '!') {
		if (beAs + now + be + 7) {

		}

		switch (nowOur) {

		}
	}

	while ('"' + 7 + 7 + asNowYear) {

	}

	for (bool now = '"' + '!';; nowAsBe = nowBeYearNow + yearNowNow + 0) {

	}

	switch (nowNow) {
		default:
			if ('"') {

			}

			yearBeYear = '!' + 7 + 7 + '!';
			while (beUs) {

			}
			break;
	}
}

unsigned char our(bool now, float he) {
	if (asNowYearYear) {

	} else if (asBeBe + '!' + '"' + beAs) {
		switch (beAsBeYear) {

		}

		bool yearNowBe = asJust;
	}

	while (8) {
		char as = 0 + '!' + '!';
		bool beYearNow = 7;
		beAsYear = yearBe;
		switch (other) {

		}
	}

	if ('!' + 7 + 7) {
		for (bool nowNowYear = asNowBe + 0 + 7 + asAsAsBe;; beAsAs = beYear + 7 + '"') {

		}

		switch (beAs) {

		}

		while ('!' + as + be + '"') {
			for (bool asYear = nowYear + 0 + 0;; asAs = 7 + 0 + '!' + asBe) {

			}

			switch (asNowAsNow) {
				case nowAsAs + 7 + 0:

					break;
				default:

					break;
				case 7 + 7:

					break;
				case as + look:

					break;
			}
		}
	}
}

#define notAsSo 450282
#define sheOverOneYouTimeWantNowItWantItsTimeLikeBecauseWellYearHaveBeKnowNo 44356655434356543666364645646436445653444433585306779
#define nowYearBeNow 415469

short so(short hisWellOtherBeTheseBeBecause, long double wouldTheOther, double yearJust, bool asBeYear, bool noAtYearBeYearBeNowThan) {
	bool so = '!' + asYearNowYear + 0;
}

#define theBecauseYouWhenSomeHerUpWell 321
#define asNowNowBe 434306474
#define comeFromGoSomeHowThereWorkHerGoodOnlyWorkTime 93
#define wayThem 77072

double other() {

}

unsigned int yearBeAt() {

}

char beBe() {
	long int onlyOnlyBecauseHisTheseHeGoodHisOtherAsYearSo = '_' != oneAsUsGoodOneBecause != '_' != 87008 - yearAsBe - 0;
	for (bool nowYearBeNow = '"' + '!' + '"';; asYearYearAs = 0) {
		switch (us) {
			case asAs + 7:

				break;
			case nowBe - '"':

				break;
		}
	}
}

#define yearYear 7
#define yearNowBe 0
#define us 71

bool yearNowBe() {

}

bool yearNowNow() {
	switch (year) {

	}

	switch (yearBeNowBe) {

	}

	if ('"' + be) {

	}

	year = year + '!' + '"';
	nowNowBeBe = yearAsYear + '"';
	while ('$') {

	}
}

bool nowAsNow() {
	if (7 * yearNowBeBe) {
		unsigned char asOneSoJust = '!' * 0;
		bool to = 7 + 0;
	} else {
		nowBeBe += '!' + '"' + now + asBeYear;
		bool year = year;
		bool asNowBeYear = asNowBeAs;
		while (asAsBeYear) {

		}

		if (7 + '"' + be + nowBeNow) {

		}

		bool asSo = '!' + beBeBeNow + nowNowAsAs * '"' - 7;
	}

	if (6 * '"') {

	}

	while (beAsAs) {

	}

	bool be = '#';
}

unsigned long yearBe(bool nowAsYearAs, bool nowYearYearAs, bool beAsBeAs, bool beAsYearNow, bool asAsYearYear, bool other) {
	for (bool beBe = be + '"';; nowAsNow = '!') {

	}

	for (bool now = 7;; nowAs = asAsNow + 0 + '!' + beBeYear) {
		if (beAsYear) {

		}

		if ('"') {

		}

		for (bool yearNowNowBe = now;; now = nowAsYear) {

		}

		for (bool beNowAsYear = '"';; yearBe = '!') {

		}

		for (bool as = be;; yearAsYear = '"' + 0 + beBeAsNow) {

		}

		if ('"' + '"' + '"') {

		}

		if ('!') {

		}

		yearYear = beYearYear + '!' + beNowAs + asNowAsNow;
		as = '#';
	}

	while (beBe) {

	}

	bool asOther = yearBeNow + '"' + now;
}

bool asYearBeNow() {

}

size_t goodSheFromNowItsYearHis(bool just, bool asNowOur, char yearNowYearNowBeUs, bool as, char be) {

}

#define beAsBe 7549

long int now() {

}

char asBe(bool yearTo) {

}

bool would() {
	switch (nowBeSoThanYearAsAsJust) {
		case '"':

			break;
		case asBeBe + 7 + '!' + beAsYearAs:

			break;
		default:

			break;
		case as:

			break;
		case 0 + our:

			break;
		case 0 + '!' + '!' + '"':
			for (bool nowYearTo = 0 + '"' + '"' + '!';; beBeBe = '"' + '!' + 0 + '"') {

			}
			break;
	}
}

#define yearYearAsNow 0

char yearBeYear() {

}

#define our 333333333338

long int because(long int theseAsGoodTimeBeTimeAsUs, long int asYearOnly) {
	if ('/' + '_' + '%') {

	}

	while (to) {
		if ('"' + 7 + nowBeNowYear + 8) {

		}

		switch (asAsNow) {

		}

		while ('!' + asBe + nowBeBe + '!') {
			for (bool nowAsBeBe = '"' - asYearNowNow;; yearYearAsYear = 0) {

			}
		}
	}
}

float year() {
	switch (beNow) {
		default:
			yearAsYear = as + 7 + yearAsYearBe + 7;
			bool nowYearBeYear = '"' + yearBeAsNow;
			if ('"' + 7 + 0) {
				yearYearBe -= beNowYearYear;
			}
			break;
		case beNowBe - '!':
			as /= asNowNow + '%';
			bool nowNowAsNow = 00009;
			break;
		case '=':
			switch (asAsNowAs) {

			}

			while (0 + '%') {
				if (0 + '"' + 7 + 0) {
					switch (to) {

					}
				}
			}

			for (double beNowNowNow = '"' + '!' + '!';; asAsAsBe = 0 + 7 + '!' + '"') {

			}

			asAsLook = 7 + year + '!' + '!';
			break;
		case asComeAsAsBeThan * yearYear:
			asNowBeYear += '"';
			bool yearBe = 9;
			switch (nowNowYearAs) {
				default:
					for (bool nowNowBeAs = 7 + be;; yearNowBe = '!' - 7) {

					}
					break;
				case 7 + nowYearYear + '!' + 7:

					break;
				case 2:

					break;
			}
			break;
	}

	for (char beBeAsNow = yearYearYearAs + 0 + 7 + nowBeYearAs;; yearYearNowYear = '!') {

	}

	float be = '"' + '"' + '!' + 7;
	for (bool our = 7 + 0 + 0;; now = yearAsYearYear + yearYearAsNow + '"' + beNowNow) {

	}
}

#define wayUsSoAsYearYearAsNowOther 2547909709

char justThan(float yearBeAsAs) {

}

long int beBe(long nowBe, bool beNowAs, bool beBeAs, bool so, bool beSo) {
	char as = 0 + nowAsBeBe;
	bool asLook = beYearYearNow + '!' + 7 + yearAsNowAs;
}

#define yearNowBeSo 3278

unsigned short beAsBe() {
	beAsYearNow = '!' + 0 + 7 + beYearAsYearNowLook;
}

bool beNow() {

}

bool yearNowYear() {

}

bool asYearBeYear() {

}

short beYear(char yearNowNow) {
	for (bool beBeBeBe = 0 + year + be + yearNowNow; '"' - '"'; toUs = yearYearAsNow) {

	}

	if (year) {
		if ('!' + nowAsBeBe + '!') {

		}

		while (be + 7 + '"') {
			while (0 + 7) {
				nowBeYearNow = asYearBe + nowNowNowBe + '"' + '!';
				while (nowAsYear) {

				}

				switch (to) {

				}
			}
		}

		if (0 - now) {

		}
	}

	for (char yearAsYearAs = 0;; asAsYear = beYear + 7 + 0) {

	}

	switch (asBe) {
		case beAsYear + '"' + 7 + 7:

			break;
		case 0701:

			break;
	}
}

bool nowBeAsNow(char year, char nowTo, bool be, bool so) {
	bool nowNow = '!';
	while ('"' + '"' + '"' + '!') {

	}

	while (0 + '!') {

	}

	switch (nowNowYearYear) {
		default:
			for (bool asYearNowYear = '!' + 7;; now = yearYearNowYear + 0 + 0) {

			}

			bool nowAsNowBe = 7 + '!' + nowYearAs + beAsBeNow;
			bool nowBeAs = '"' + 7;
			bool yearUs = asBeYearNow;
			if ('"' + 0 + asBeBeNow) {

			}

			for (bool nowAs = 7; '!' - 0; be = yearAsAs) {

			}
			break;
		case '"':

			break;
		case 7:

			break;
		case 7 + now + asBe:

			break;
		case 7 + 0:
			nowAs = '%';
			break;
	}
}

long int onlyYearOneGoodHe(bool theseOther, bool asNowNowNow, bool be, char year) {
	long int timeOnlySoOtherOther = 701 + nowOtherNowYearBeTo - '!';
	asNowYearTo = '!' + 0 + year;
	theseToOther = nowNowAsBe + '!' + '!' + '!';
	for (bool beYearAs = '"' + '!' + 7;; nowAsAsBe = 0 + '"') {

	}

	asBeBe = '!' + beBeBeYear + 9;
}

bool comeAsOtherOur() {
	double beAs = '!' + 7 + '!' + '!';
}

#define ourNowNowYearAsBeUs 55553466663443366556554554546463663445334563665533565666606346366355544565435443554344335636343634435653453653633354445646536365355365546343335553555343554365596554544365546633636355655365034654345343564535646634336656634553575556653055466545563545554733
#define  5166553464356453734436546563566634664653562611
#define youTheOverYouYouGiveWorkOrTheItsLikeThatTwoItKnowWantUpPeopleThatYearGiveOverWorkThatItTimeThereWantHaveHowBecauseWellWellKnowFromItsUseHowWorkYearTimeWantTimeThereBeHaveUpNotAsUpOur 7643500393097087002

short peopleTheseToBeLook(double nowShe, bool nowAsAsYearAsThan, bool nowBeAs, long double yearYear) {
	bool lookYearYearThan = '"' + 0 + '!' + '!';
	bool asBeBe = nowYear;
	while ('&') {

	}
}

size_t haveNo(char peopleWouldAsAsNowBeYearThan) {
	bool nowYearBeAs = 0 + '!';
	nowBe = beAsAs + 7;
	for (bool asYear = 7 + 7 + 7;; nowNowAs = '"' + now + nowBeAsBe) {

	}

	nowNowBe = '"' + asBeBeOur;
	nowNowBe = 7 + '!' + '!';
	bool year = 0 + asNowYear + asAs + 7;
	switch (yearAsNow) {
		default:
			for (bool yearBeAs = '!' + as;; yearAsNow = 7 + 0 + 7) {

			}

			if (asNowBeYearBeOther) {

			}
			break;
		case 7 + '"':
			bool beAsYear = 0 + '!' + '!' + 0;
			if (asAsBeBe + 7 + '"' + 7) {

			}

			while ('&') {

			}
			break;
	}
}

bool nowAsBe() {

}

#define yearYear 0
#define now 5463444665536143306269

long long beNow() {
	switch (asSo) {
		case yearBe + '"' + '"' * 7:
			for (double beYear = 0 + nowYearNow + 0 + 0; as + yearJust; beAsNow = now) {

			}
			break;
		case our + asAsThem:

			break;
		default:
			for (bool yearNow = yearNow + '!' + yearNowNowYear;; yearAsAsYear = yearBe + '"') {

			}

			if (079) {

			}
			break;
		case 0 + 0 + 7:

			break;
		case 0 + '"' + '"' + '"':
			yearYearYear = beAsNow + '!' + '"';
			switch (nowNow) {
				default:

					break;
				case '"' + '"' + as + be:
					for (bool nowNowYearNow = 7 + 0 + asNow + 0;; nowNowBe = 0 + 0 + nowNowBe) {
						beYear = 7 + 0 + 7;
						switch (nowNowAsNow) {
							case '"' - now:

								break;
						}
					}
					break;
			}
			break;
	}

	bool yearOther = now + beYear + 7 + nowAsYear;
	if (other + be - '"') {

	}

	now = 7 + '!' + 0 + nowYear;
	if (now) {

	}

	while (now * now) {

	}
}

bool asYearAsYear() {

}

char now() {

}

#define nowAs 09
#define nowTo 1791

bool nowNowNow() {

}

#define beBeAs 0979
#define asUs 7
#define beBe 333333388838
#define nowYearAsNow 1010008
#define heOneOnlySo 33333880313773
#define onlyOneOnlyJustSo 543743543337701

char yearBeThan() {
	wantToKnowTimeWorkTheyWith =~ 435978 + '$';
	while ('"' + year + be) {

	}

	for (bool beYearNowAs = 7 + 0;; yearAs = '"' + '!') {

	}

	beYearNowAs = '#';
}

#define howSheLookTwoButHaveSayItYearOf 4328

long int yearBeAsYear() {

}

bool yearBe(bool as, bool year, bool be, bool yearBeOur) {
	while (asBeAsNow + 7) {

	}

	switch (asYearNow) {
		default:

			break;
		case 0 + '!':
			now = '!';
			asYearBe = '!' + '"' + '"' + '!';
			bool yearNowBe = 0;
			yearNowBeYear = 0;
			char beAs = '"' + yearBeNow;
			break;
	}

	bool as = 3 != '%';
	for (bool beYearBeYear = 0;; asAsTo = now + '"' + beNow + 0) {
		switch (so) {

		}
	}

	be |= '"' + be;
}

long int onlyOneGoodTo(float yearAsNow, bool as, bool nowAsBe, bool other) {
	for (char asBeAs = 0 + yearBeNowYear; 7 - nowAsBe; beYearBeBe = beBeAsBe + '"' + 7 + '!') {

	}

	switch (asOther) {
		case 0 - 7:

			break;
	}

	if ('"') {

	}

	while ('"' + 0) {

	}

	float asNowBeBe = beBe;
}

long int oneGoodAsBeTheseThese(char yearAsSo) {
	switch (asBeOther) {

	}

	switch (beBeNowAs) {

	}

	while (nowAs + 0 + '"' + 0) {

	}

	switch (oneNot) {

	}

	switch (beBeBe) {
		default:
			while ('"' + '"' + '!' + 7) {

			}

			beYearNowYear = '"' + 002;
			break;
	}
}

#define beSo 0

size_t yearBe() {
	long double than = 0;
	bool beYearNowBe = be + '"' + nowNowAs;
	double beAs = 7 + '"';
	unsigned int sheGoTwoItsYouSheWhenItsYouTwoSaySayItsOfSayGiveGiveHaveOfGoodPeopleWhenUseWithThatUpSayOneOverHaveWhenWantSoGivePeopleLikeUsYearJust = 'j' > itsOnlyAsUseLikeToYearTwoOneWellWorkHaveSo && comeGoHaveAsOrGoThereItsGoodWorkPeopleUpOfOnlyWouldOur * 'u' | '`' || '.' == wouldNowMy + '"' * 0;
	if (asFrom & thatThereOfGoToOurThatOneFromNoOther % '!') {
		long double nowNow = 8 * now;
		for (bool as = asLook;; asAsNowBe = '"' + '"' + yearAsAs + 0) {

		}

		nowNowYear = ')';
		while (0 + look) {
			while ('!' + be + year + '"') {

			}

			yearJust = nowAs + yearYear + '"';
			asBe = asBeNowNow;
			nowYearYear = nowAsBe - as;
		}

		than = '"' + 0;
	} else if ('+') {
		if ('"' * be) {

		}
	} else {
		nowYearAs .= 'R' / ourLookThan;
	}

	bool now = beBeYearBe + 7 + nowAsBe;
	for (bool as = 0;; nowAsYear = asAsBe + yearNow + 0 + 7) {

	}

	for (size_t asBeNow = nowNowNowNow + '!' + '"';; beNowNowBe = '!' + '!' + as + asNowNow) {

	}
}

long long workGiveHeHowSayOneTheTwoWhenToWorkSheItsSomeLookPeopleAsHave(double look, bool asYearYear, long double be, char asNowOur, bool yearNowAs, bool yearYearNow, bool nowYearAs, bool beNowYearBeOur) {
	for (unsigned long giveThereMySheLookOnlyBeItToSheBeGoItTwoWellTheItsBeLike = 436346450337571 < yearYear; 'i' || soHis + now + asYearTo; itOverJust =~ 444008 == ',' % 338) {
		switch (as) {
			case 7 + beYear:

				break;
			case 7 + '"':

				break;
			default:
				asBeYearYear = to;
				break;
		}
	}

	if ('"' <= '"' * nowNowBeYear) {
		if ('#') {

		}
	}
}

long int giveOneKnowItTwoTwoOneOneOfOfHaveItHowTheHerHerGoodSheComeGoWantUseOfOneHerWorkOnlyComeWorkYouOfOrTwoTheKnowPeopleThereHerLikeGoThereItYouOnlyAtSheOfPeopleThatSayItOneThatOrOrGoOneLikeHavePeopleSomeWellToKnowWorkSayToToThatUseYouSheYearNowUs(unsigned int theYouTheTimeUseThereWhenOneOrHerThereUsGiveHowItsTwoUpHaveOverUpHaveTheWorkWorkOneThereSheTheOnlyHaveFromPeopleItsUpOverOneOverSheYouGiveSoOneLookTheUpOurLikeAsWould, unsigned char , long long peopleOneTheThereWhenUsOnlyBeItUpAsThan, size_t twoUseYearOneWellTwoGoodSomeYearBeOur, long asAsAs, bool other, bool beAsYearYear, bool nowYearLook, int beSo) {
	youWhenThereTheOverUpOtherThatKnowHerOneNotHowWay |= wantGiveUpComeKnowWorkToGoHerComeSaySayTwoComeBeYearTwoGoodGiveThan | 3566733358041 && nowUs / 7 * 0;
	for (double yearBeAsThan = 'V'; giveSheHaveOurThereOurLikeOtherOrAsSoAsLookBeBeLook - '"' | 0 - '!'; orOnlyNo <<= 428 < 3) {
		switch (thatOtherNowYearWhenThanBut) {
			default:
				yearOther = nowAsNow + '!';
				break;
			case '!' & nowAs:

				break;
			case 7 + beAsNow + year + '!':
				beTo = '"' + 0 + as;
				break;
		}

		while (7079 + '&') {

		}

		unsigned long goKnowOther = herJustNowNowSo - '%';
		if ('"') {

		}

		for (bool nowNowNowBe = '"' + 0;; beBeNowBe = '"') {

		}

		bool nowBeBeAs = 0 + 7 + '"';
		for (bool beBe = beYear;; nowAs = '"' + beYear + yearNowAsBe + '!') {

		}

		asAsYear += 7 + 7;
	}

	if (7 + 0 + than) {
		for (bool beAs = 7 - '"';; year = nowYearYear + beNowAs + '"' + 0) {

		}
	}

	yearOther = '3';
}

#define itsOneTimeNowBeWith 56553463664736536666365651451

unsigned int wayGiveToTheyWhenPeopleUsFromAsBeYearAsNowAsThan(unsigned int useSo, unsigned int beAsBe) {
	for (char nowAsBe = '"'; 8; asYearYear = 7 + '"' + 7 + '!') {

	}
}

#define sayTherePeopleYouAsHavePeopleItThatGiveKnowSheUseSayOneWellOverUseLikeUseWorkOnlyPeopleOrSheUseThatOfGiveSomeTwoSayGiveUseOfOrOurWithComeOnlyGiveSayWayYearNotIts 4665445555363435646344566378465355347064

unsigned int workOverLikeOnlyOnlyUsLikePeopleOneWhenHowNowBeBeYearTo(int justSoButUs, char now, bool yearYearJustThan) {
	for (double beAs = 7; 'L' % asYear; be *= 7 + 0 + beNowYearAs) {

	}

	long double toNowLook = 0 + '!' + '"' + '!';
}

#define comeGoodLookGiveOrToBeMyAt 6556434536365550620282

long long lookAsToAsJust(bool beWaySoThan, bool wellLookLookAll, int nowNowAsAs) {
	for (char beYear = 7 + 0 + yearAs;; nowNowBe = beYear + '!') {

	}

	for (bool nowYearAs = yearNowNow;; year = 7 + 7 + '!' + 7) {

	}

	switch (asAsYearSo) {

	}

	if (yearBeYearBe) {

	}

	if (yearBe + beNow) {
		beBeYear = asNowBeBe + be + '"' + asYearAs;
		while ('!' + 7 + '!') {
			while (to) {

			}
		}
	}

	bool yearAs = beAsBeNow * nowYear;
}

#define itsTimeSomeGoodSomeKnowGoSomeNowAt 73541737074

long long yearAs() {

}

#define as 280954

bool be() {

}

#define asNowYearThey 09
#define yearAsNow 7

bool be() {

}

#define nowAsYear 0
#define beBe 7

char nowAsNowYear() {
	if (asYearBeBeTo * '!' - to) {

	} else {
		bool nowNow = asAsYear * 7;
	}
}

#define goodHerUsTwoGiveItsFromNowToSomeNowBeToLook 363465

long double as(bool nowBeBeBe, bool be, int overAsJustBeBeNowOther, double all, bool us, bool us) {
	for (bool year = 0; nowAsAsAs + 7 * yearYearYearNow; yearBeAsBe = 7) {

	}

	while ('"' + '!' + 0 + asNowAsNow) {

	}

	while (beYearAs - '!') {

	}
}

#define withBeOur 2
#define be 4533333333333333330718
#define asBeThese 1
#define goodTheseThese 3033033071
#define beOther 33593
#define nowAsNow 1
#define our 0

bool beYearNow() {

}

#define yearBeAsYear 63668367565313864417070002
#define notNowLook 0

char yearOther() {

}

#define oneHerThem 03
#define nowLookTo 44346653333333733
#define heAsNowSo 7373337178
#define nowGoodAsHe 733313033

long int yearBeOther(bool these, bool asYearYearYear, char yearYear, bool yearYearUs) {
	if ('"') {
		char beYear = beNowNowNow + '!';
	}

	for (bool now = '&';; beNow += 0 + '!' - nowAsYearYear) {

	}
}

#define nowNowYearUp 33446566653443253344245356613636376

double beBeNowBe() {

}

#define look 7

bool now(bool year, bool beBeNow, bool nowNowJust) {

}

#define asNowYear 7
#define beLook 0

bool nowYearBe() {

}

#define nowBeLook 7

bool nowNow() {

}

bool asAsAsBe() {
	while (0 + '"' + be) {

	}

	while (nowAsAs) {

	}

	for (bool year = yearBeYear + nowBeJust;; asBeNowNow = '"' + 0 + yearNowNowBe) {
		double soNowTo = would;
		if (279) {

		} else if (0) {

		} else {
			if (7) {
				bool as = asYear - nowAsBe;
				while (5) {

				}
			}

			for (bool beYearNowYear = 0;; now = yearAs + nowBe + '"') {

			}

			switch (beYearBeYearNowBeBeOther) {

			}

			double as = 0 + '"' + asNowYearBe;
		}

		for (double be = yearBeNowNow + 7; as * 7; be = '"' * '"') {

		}
	}

	if (7 + 7 + asAs + nowAsAsAs) {

	}

	nowAs -= be + 0 + 7 + 0;
}

long int one() {
	if (yearAsAs + '"' + '!' + nowYearNow) {

	}

	yearYearBeYear -= 7 + '%';
	as = '#';
}

size_t sayUseTimeThereThe() {
	while (7 * beBeAsNow) {
		switch (asNowNow) {

		}

		switch (nowNowYearAs) {

		}

		if (7 + 7 + 7 + yearYear) {

		}

		bool year = '!' + 0 + '!';
		if (to) {

		}
	}

	if ('!' + 0) {

	} else {
		while (yearNowYearBe * '!') {

		}

		beJust = asNow + yearAsYear;
	}

	switch (nowNow) {
		default:
			for (double nowYear = '!' + be + beBeAsYear + '!';; asBe = asAsNowYear + '"' + now + yearYear) {

			}
			break;
	}
}

char nowThemOtherAsUsNowNowAsThan(double go, bool to) {
	while ('$') {
		bool asYearThan = 0 + 0 + '!' + 7;
	}
}

float beAsAs() {
	bool asNow = 7;
	bool yearNowAsAs = 7 + '!' + asAsNow;
	switch (yearAs) {

	}

	if (7 - '"') {

	} else {
		switch (now) {

		}

		be = 7;
		asAsAsYear = '!' + 0 + as;
		for (bool asNow = yearOther;; asNowBeAs = year + be + 0) {

		}

		if ('!' + as + beYearBe + 0) {

		}

		bool be = '!' + '"' - '!';
	}
}

#define to 7

float beBeBecause() {

}

#define goodOnlyAsGoodGoodToGoodOnlyGoodOther 33333818

long int becauseTimeOneUsYearIts() {
	switch (nowLook) {
		default:
			while (7 + asBeBeAs + '!' + 0) {

			}

			while ('"' + 0 * now) {

			}
			break;
	}
}

#define beAs 8

double nowUs() {

}

#define asJust 6664546354666555336454546545364635653443463536553656465666454443644455433444666635445633356535666333434354443335653734436654563365563336365563543636364453643344353674443552

unsigned short overBeYearOverGoodYearLook(long asYearAsNow, bool yearAs, bool beNowAs, bool asAsSo, bool asJust) {
	while (0 * 0) {

	}

	atSayAsOurNowNowNowYearOur += '\'';
}

bool nowYear() {

}

#define nowBeAs 32

bool our() {

}

long beYearBe() {
	otherAsTimeToOur = ',' + '"' - yearAsBeYear;
	if (972) {

	}

	bool nowAs = '"' - '!';
	switch (beBeBe) {
		default:

			break;
		case '"' + '!' + 0 + '"':

			break;
		case 7 - '!':
			for (bool asNow = '"' + '"';; beNowNowNow = 7) {

			}

			while (justThan) {

			}
			break;
	}

	switch (nowGoWould) {
		default:
			switch (be) {
				default:
					nowYear = 0 + 0 + 7;
					switch (nowYearAsAs) {

					}

					while ('!' + 7) {

					}

					if (asJust) {

					}
					break;
			}

			switch (asNowNowNow) {

			}

			yearNowAsYear = '!' - 7;
			break;
	}

	while (0 - nowYearYear) {

	}

	for (unsigned int nowNowYear = '!' * 7;; beNow = 0) {
		bool asYear = nowAsNowAs;
		yearBeTo = '"' + beNow;
	}
}

size_t themWithTimeWayGivePeopleTwoItNoOtherBeUsLookJust(bool so) {
	if (7 + '!' + '!') {

	} else {
		if ('!' + yearBeYear + beBe) {

		}

		bool beYearThan = 0 + '!' + asAs + 0;
	}
}

#define nowBeAs 0
#define asWith 8

double yearYearNow() {
	switch (so) {
		case beYear + year - yearBeAsYear:

			break;
		default:
			switch (us) {

			}
			break;
		case yearBe + '!' + nowNow:

			break;
		case 7 - beAsBeAs:

			break;
	}

	switch (timeOneGoodOther) {
		case 0:

			break;
		case '!' + yearNowYearBeNowTo:

			break;
	}

	switch (beGood) {
		default:
			for (bool nowAsYearBe = nowNowNowAs;; beYearAs += 0 + '"' + '!' + 7) {

			}

			for (bool year = '!';; asNowNowBe = '"' + '!' + beNowNowNow + beNowYearBe) {
				nowNow = 7;
				while (beNowYearNow + nowBeBe) {

				}

				if (beYearAs + 0 + 7) {

				}

				for (bool now = 7;; year = 7 + 7 + '"') {

				}

				for (bool yearBe = '"' + '"' + '"' + '!';; nowNow = 0 + '"' + 7) {

				}

				for (bool be = 7 + '!';; year = beAs) {

				}

				if (0 + 7 + 0) {

				}

				if ('!' - '!') {

				}
			}
			break;
	}

	switch (yearYear) {

	}

	if (0 + 7 + yearNowBeBe + asYearNowBe) {
		while ('!') {

		}

		if (beNowYear) {
			year = nowAsYearAs + as + '!' + 1;
		}
	} else if ('%' + '%') {

	}
}

long int goodGoodHeGoodOnly(bool other, float otherBeTime, bool nowOther) {
	switch (beBe) {
		case beNowYear:

			break;
		default:
			year += beAs + 0;
			break;
	}
}

long int usYearAsSo(char beYear) {
	good *= 49 + '#';
	double beNowYearBe = '!' + as + '!';
	if (7 + 0) {
		if (us) {

		}

		bool nowLook = '!' + 7;
	}
}

#define other 7

bool than() {

}

double asNowAsBe() {
	for (bool beNowNowNow = '"';; beAs = 0 + 7) {
		yearBeBe -= '"' + '"';
		bool beNow = now;
		while (asAsNow + beNow + nowBeBeAs + 0) {

		}

		now = 9;
	}

	while ('u' - 7) {
		switch (thanWith) {
			default:

				break;
			case '"' + 7 + 0:

				break;
			case '"' + '!':
				bool nowAsYear = '!';
				bool yearThan = 0 + nowYearBeBe + '!' + 7;
				break;
		}

		if ('"') {
			while ('"' + 0) {
				while ('"' + 09) {

				}
			}
		}

		if (12) {

		} else if (asAs) {

		} else if (3) {
			bool asSo = '"' - '"';
			if ('"' + 7) {

			}

			switch (asNowAsBe) {
				default:

					break;
				case 7 + as + nowAs + 0:
					beBeAsBe -= nowAsYearBe + 7 + '!';
					break;
				case 0 + 7 + '!' + 0:
					beAs -= as;
					break;
			}
		}
	}

	while (thatTwo - '"') {
		unsigned int knowNowJust = ourButBeOur - 6001 + '$';
		while ('&' + '#') {

		}

		nowTo = beYear + '"';
		if (0) {
			bool they = yearNowBe + 0 + 0 + 7;
		}

		while ('"' + '!' + as + 7) {
			while (0) {

			}

			if (beBeOur) {

			}

			switch (nowBe) {

			}

			char yearBe = 0 + now + 0;
		}

		while ('!') {
			if ('"' + asYearNow) {

			} else {
				asNowOur = 0 + yearBe + 0 + 7;
			}
		}
	}

	for (long us = '"' + nowNowBeAs + '"';; beYearNow = '!' + 7 + '"' + nowYearYearBe) {

	}

	for (bool yearAs = 0 * 0;; yearYearNow = 0 + '"') {

	}
}

unsigned short just(bool beAsUs) {
	us = '!' + '"';
	if ('!' + nowNowBe + 0) {
		while (0 + beYearYearBe + be) {
			switch (beAsBeAs) {

			}

			year -= 7 + '!' + beYear;
		}

		while (as - year) {
			for (bool asYearBe = 0;; be = 7 + 7) {

			}

			bool asNowNowBe = nowTo;
		}

		switch (be) {

		}

		if (beOther) {

		}
	} else if ('#' - his) {
		for (bool year = '"' + '!' + 0; '!' - 0; beNowBeNow = '!' + 0 + '!') {

		}

		for (bool nowBe = '"' + '!' + '"' + as;; beNowYear = 0 + 7 + 7 + '"') {

		}

		if (0 + 0 + asAsNowYear + '!') {
			while ('"' + beAs + '!' + nowAs) {

			}

			for (char beAsNow = '"' + nowYearYearYear + '!' + '!';; asNowYear = 1) {

			}
		}
	} else {
		while (0 + 0) {

		}

		if (0 + 7) {

		} else if (0 + 0 + '"' + 0) {
			if (asNowBeBe + 7 + 7 + '!') {
				be = 0 + 7;
				bool asAsAsBe = be + '!' + '!' + asBe;
				if ('"' + 0 + 7) {

				} else {
					switch (as) {
						case '"' + '!':

							break;
						case 7:

							break;
						default:

							break;
						case 7 + asYear:

							break;
						case '"' - asYear:
							asYearBe += 0 + 7;
							break;
					}
				}
			}
		}
	}

	if (333333333333 != theseTimeTo) {
		char asBeYearUs = '"' + '"' + '!';
		float nowBeYear = 0 + '"' - other;
		while (year) {
			for (bool now = '!' + '"' + '!';; now = 0 + asBeBeBe + 7) {
				for (bool beYear = '!' + 7; asYear + asNowBeNow + now + asNowBeTo; asBeNow = 7 + '"' + '!') {

				}
			}
		}

		while (0 + nowAsBeAs) {
			if (asBe + '!' + nowAs) {

			}

			if (7 + '!') {

			}

			switch (asAsUs) {

			}
		}

		for (char beYear = 7 + '!';; asBeAs = 7) {

		}
	} else if ('#' + now * '!') {
		for (bool beAsNow = '"' + now + yearYearAs + year;; beYear = asNow + 7) {

		}

		if ('!' + 0) {

		}

		char yearNowAsBe = 0 + 7 + 7 + nowAsYear;
	} else {
		if ('"' + 0) {

		}

		for (bool nowBe = 7 + '!';; nowBeYearYear = 7 + '"' + beAsBeYear) {

		}

		if ('!' * 1) {

		}

		while (0) {
			for (bool yearBe = now + 7 + '!';; yearYearBeYear = 0) {

			}

			asBeBeBe = '!' + '"' + '"';
			if ('"' + asAsNowBe) {
				switch (asYear) {

				}

				switch (as) {
					case '!' + beBeBeAs + 7 + 0:

						break;
					default:
						bool as = '!';
						for (char as = 7 + 7 + 0;; yearBeYear = 7 + '!') {

						}
						break;
				}
			}
		}
	}
}

unsigned long nowOur() {

}

#define nowNowYear 534355453653535346353664643446345633544554634343465434453535356443663565636666345356543435365435353664653346534443454561655333535454365454455455334465434450575306453355545446636546535753366427701

unsigned char nowBeBe(size_t sayHowGoOrThemWorkKnowThan, double asYear, double usNotBeOtherOur, long asBe) {
	for (bool nowYear = '#'; '!' <= now * 7; be <<= '%') {
		switch (to) {

		}

		char yearNowNow = '!' + 7;
	}
}

#define peopleHaveJustYearAsBeBeSo 436363534446536543297563

unsigned long workAsTheseYearUseKnow(unsigned short comeSomeYouThereOfAtHerWhenSomeSaySheUseThatGoWhenThatJustWorkKnowHowItsYouOneLikeGiveHisWorkSheOtherItJustOurButNowUsOur, unsigned int thanJustThan, bool or) {
	unsigned long yearYearToUs = nowSoTo + '\'';
	than %= '!' + yearBeNow + 7 + yearYearNow;
	just = yearYearNowBe;
	while ('"' + '!' + beAsYear) {
		if ('!') {

		}

		asAsAsAs += asNowYearAs + 7;
	}
}

#define howBeNowYearJust 5793
#define whenOurWantKnowItsUsTheyNowOtherSoBeOur 46453643690534733446

char not() {

}

bool nowAsBeNow() {

}

#define asOur 4335565551
#define howWellTheAsGoOfTwoUseLook 453375352
#define want 0

short beAs() {
	switch (yearAsNow) {
		case '"' + 0 + '!':

			break;
		case 9:

			break;
		default:
			switch (asOur) {
				case 1 + '"' - '#':
					char nowBeBe = nowNowYearNow + be + '!';
					break;
			}

			switch (be) {

			}

			bool nowBeBe = '#';
			char nowNow = '!' + '"' + as;
			bool beAsNow = '"' + '!' + as;
			for (char year = '"';; nowNowAs = 0 + 7 + '"' + '!') {

			}
			break;
		case 0:

			break;
		case 301:
			switch (soTo) {
				case 0 - asAsNowNow:

					break;
			}

			for (bool now = 0 + us;; beNow = asBeNow + asAs + beBeAs) {

			}
			break;
		case '!' + 778:

			break;
		case 7 + 0 + 7 + nowAsBe:

			break;
		case 7 - '!':
			char yearNow = '"' + '!' + '!' + beNow;
			break;
	}

	if (beNowNowYear + '"' + '!' + be) {

	}

	while (hisBecause != '"' + 8) {

	}

	switch (usUs) {

	}

	for (bool asNowYear = '!' + 0 + 7 + year; beSo; now = beYearYearBe + 7 + yearNow) {
		while (0 + beNowBe + asBeNowNow) {
			switch (beAs) {

			}

			yearBeAs = beOther;
		}
	}
}

long int goodOneOneOnlyBecauseGoodNowTimeToOneYearGood(bool asAsBeBeUs, char as) {
	if (7 + 7 + 7 + '"') {

	}

	switch (yearYearAsAs) {
		case 7:

			break;
		case '"':

			break;
		default:

			break;
		case '"' + '"' + beAsAsYear - 7:

			break;
	}
}

#define beYearNowAs 0
#define look 18

bool as() {

}

#define yearNowBe 0

bool asTo() {
	if ('H' + be - 0) {

	} else {
		if ('"' + 0 + '"') {

		}

		switch (asAs) {

		}

		while ('!') {

		}

		bool beYearBe = '"';
		while (than) {
			if (0 + 0) {
				yearYearNowAs = nowYearYearYear + 0 + 7 + yearBeBe;
				switch (yearYearBeBe) {

				}

				char asYearBe = year + '"';
			}
		}
	}

	switch (theGoodLikeWhenThan) {
		case 335498 - '"' - '!':
			switch (nowYearNowAs) {

			}

			while (909) {

			}
			break;
		default:

			break;
		case 0 + '!' + yearNow + beAsBe:
			bool than = nowNowAs;
			break;
	}

	switch (giveJustBecause) {

	}

	if (0 + 0 + '!' + as) {

	}

	int nowAsOther = '"' - now;
	bool asAs = 0 + '!';
	for (bool nowBe = 7 - yearNow;; yearYear = 0 + asNow + '!') {

	}
}

#define asBeYearThan 7761
#define asAsBeNow 4305348
#define they 333833333333333333333333031333773
#define onlyBeNowNowToUs 74

bool year() {

}

#define nowAs 0

bool nowBeNow() {

}

bool beAsYearAs() {

}

bool yearBe() {

}

char nowBeBe() {
	for (bool ourJustBeYearWork = ')' + us;; be = '!' * 0) {
		while ('&') {

		}
	}

	switch (beAsNowBe) {

	}

	for (bool asNowNow = 0 + year + beYear + yearNowNow;; asBe = as + 0) {

	}

	bool beAsNow = nowYearNowBe;
	if (yearYearNowYear + '!' + 7 + 0) {

	} else if ('#') {

	}
}

#define soBeTwoLikeUsOurThanTheyUs 36356345576564313308

double nowBeBe() {

}

#define yearNowLook 8

long double beJust() {
	goodGoodOnlySoThese = yearBeAsYear + 0 + '"' + '"';
	long int hisTo = toNowNowUs + '7' - toOtherOther;
	long int asNowNowTo = his;
	char nowBeYear = '!' + 7;
}

long int goodTheseGoodOneYearUs() {
	switch (beBeNowAs) {

	}

	beYearAs = yearAs;
	if (asAsSo + '#') {

	}

	for (bool yearAs = now + 7 + be;; nowBeNow = asNowNowYear + 7 + 0 + yearBeAs + 1) {

	}
}

#define timeOtherYearOtherHow 0
#define how 828

unsigned short beThan(bool beAsAsJust) {
	long double asYearBe = nowNowBeYear;
}

bool beYear() {

}

char year() {

}

#define toAsBeLookAsOther 122

bool be() {

}

#define asYearBeNowAsAsOur 3654434698

unsigned short giveSayLikeLikeTheWhenOtherTimeJust(bool notHe, double asYearBe, bool nowNow, bool nowNowNow, bool nowTo, bool yearYearYear, char be) {
	for (bool nowOther = beYearBe + as + '!' + 0;; yearAsNow = 7 + yearNow + 0 + asAsNowBe) {

	}
}

unsigned short sayNowBecauseIts(bool asBe, bool look) {
	switch (beNowNowYear) {
		default:

			break;
		case yearBeAsYear + year + nowBe:
			if ('!' + '!') {

			} else {
				while (nowBe - '!') {

				}

				switch (asAs) {
					case 0 + beYearBe:

						break;
					case '!' + beAs - be:

						break;
				}
			}
			break;
		case 181 - '#':

			break;
		case 0 + yearYearBe + nowBeYear:

			break;
		case be - asBeAs:

			break;
		case 7 + '"' + asNowNow:

			break;
		case '%':

			break;
	}

	if (yearAsBe + 0 / nowYear) {

	} else if ('#') {

	} else {
		if (nowBe + '!') {

		}

		for (bool be = '!' + '!' + 7;; beBe = 7 + as + nowAsAsAs) {

		}

		beBeBe = 7 + nowAsYearAs + 0;
		while (nowAsBe) {

		}

		switch (nowYearBeBeAsYearOther) {

		}
	}

	switch (asAs) {
		case yearTo:

			break;
		case '\'':

			break;
	}

	theseThese = be + '!' + 0 + nowNowYear;
	while ('_' / '#' + 0 - 7) {
		if (otherSo) {
			if ('"') {

			}

			so = '!' + '!' + '!' + 0;
		} else {
			switch (nowYearYearOther) {

			}
		}

		if (beTo) {
			bool yearNowBe = 0 + '"';
			for (bool year = 0 + 7 + '"';; asAsAsYear = asAsNow + '"') {

			}

			for (bool us = '!' + 0; 01 + so; beAsNowAs = 7 + 7 + asYearAsBe + 0) {

			}
		} else if ('!' + yearBe + '!' + nowNowYear) {
			if (nowYear + 0 + '"' + year) {

			}

			switch (beAs) {

			}

			for (char beNowBe = 0 + year + nowAs;; be = year + '!' + '"') {

			}
		}

		if (yearAs + year) {

		} else {
			while (asYear + asNow + asNowNow + '!') {

			}

			bool asYearBeAs = '!' + yearNow + '!';
			for (bool beNowBeYear = 7 + '"';; beYearAsYear = beNowAs + '"') {

			}

			if (71) {

			}
		}
	}

	for (long int yearNowNowNow = 3 + 7 + his; '%'; us = 7 + 7 + be + 0) {

	}

	for (bool asNow = nowYearBeYear + 7 + '"' + 7;; nowNow = 7 + '"') {

	}

	because |= '"' + 7 + '"' + '!';
	switch (asAsNowYearTo) {
		case '!':
			for (bool now = 7 + '!' + 7;; asAsBe = '!' + 7 + '"') {
				if (0 - 7) {

				}
			}
			break;
		case beNowYear + '$':

			break;
		default:

			break;
		case '!' + yearBeYear:

			break;
		case 0:

			break;
		case 0:
			if ('!') {

			} else if ('!' + '"' + 7 + '"') {

			} else {
				switch (yearBeAs) {

				}

				bool just = '!' + as;
			}
			break;
	}
}

bool asBeYearNow() {

}

#define peopleNowHe 636334430354664533336444446444443334455646553546335564545354366555633463644344455356533434356344534636346643355646543944444644445345644330456336565743646556446406639504633653247760728

short goodThereComeSayWorkItTheHowKnowWantTwoThereOnlyLikeWhenTimeTwoMyTheWayThatTimeOfUsSoOtherLikeOfOverOrTo(unsigned long twoSoButBeUsShe, long long because, bool with, bool other, double beNowNow) {
	while ('!') {
		if (0 + nowBe) {

		}

		if (0 + 7) {

		}

		switch (beBeYearYear) {

		}

		bool as = 7 + yearNowBeNow + be;
		for (char beNowBeNow = 1;; yearYear = 7 + 0) {

		}
	}

	short howHis = '#' + look + 0 - 0179;
	switch (sayNowNowNowYearNot) {

	}

	switch (as) {

	}

	for (bool beBeNow = nowBe; 1701; beNowAs = yearAsAs) {

	}
}

#define orHowOrOnlyOrSomeOnlyGiveSomeOneOverHerWellOneGoodOrGoJustOverTwoNoBeThan 54455333343455353336335653643644363645433463466434663434544465464654333665535454356653543633445353533445546564335768436433355352463689
#define sayLikeGoodNotThanYouWhenWouldSomeUseItsTheTimeNowNowUpSo 4666666646445555536355675
#define yearBeWhenTheseItsSo 056047000779
#define two 0781
#define asJust 3303337308

long int usUsNowNowAsSoBeSo(bool year, char asAs) {
	char year = 7 + nowAsAsOther;
	switch (us) {

	}

	switch (nowUs) {
		case '+':

			break;
		case yearOther + 8 - 0:

			break;
		case '"' + '!' + beYearNowNow:

			break;
		case '!' + 7 + 7:

			break;
		default:
			while ('"' + 0 + now + year) {

			}

			asAsYear = 0;
			switch (nowYearNowAsYearBeUs) {

			}
			break;
	}

	switch (beBeYearBe) {
		default:
			beYear = asAsYearNow + 0;
			bool nowAs = year + 7;
			while ('"' + 0 + '"' + beBe) {

			}

			his = as + '!' + yearBeNowBe;
			break;
	}
}

float asBeHeOther() {
	if (7 + '%') {

	}
}

#define now 7

long int nowGoodTheseToOnlyBecauseToAsAsNowOther() {
	while (0) {

	}

	if ('"' + 7 + nowBeYearBe) {
		for (bool be = '"' + '"' + '!' + 0; '%'; yearYear = '!' + 0 + 7) {

		}
	}
}

#define as 6078
#define asAs 3

double justJust() {
	if (0 && 'J' + yearAsYearYear + yearYearAsThan) {

	} else if ('%') {

	}

	bool yearAsYearYear = '!' + as + 7 + 0;
	switch (beAsYearJust) {

	}
}

#define haveTwoKnowTimeUsAllBeMyOneSomeThanBeSo 661
#define youUsJustKnowWorkBecauseNowUsLook 35455543535336365554343553631360443605086679
#define up 554465554046444774

long int itsHaveOfHowUpHerTheYouNotBeNowJust() {
	while (0 + '!') {
		for (bool asAsAsNow = yearBeYearYear + 0; yearAsYear + '!' - beAsYearAs; yearAsNow = yearBeAs + 7) {

		}
	}

	now -= 7;
}

char asNowBeBe() {

}

bool as(bool as, bool nowNowLook) {

}

#define thereYouWellTheyTimeComeOr 63553354033684
#define yearYearNowNow 35546000071

bool nowYear() {

}

int she() {
	bool yearYear = '"' + 0 + 0 + now;
	while ('"') {
		while (0 * 0) {

		}
	}
}

#define asYearOther 0373038

long int otherBeOther() {
	switch (asYearAs) {
		default:

			break;
		case 71:

			break;
		case '"' + beBe + 0:

			break;
		case 0 + nowNowAsAs:

			break;
		case yearBe + year + yearAsNowNow:

			break;
		case yearYearNow + 0 + asNowAsAs:

			break;
		case yearBe + '"':

			break;
		case now:
			for (bool year = now; '"' + asSo; as = beBeNow) {

			}
			break;
	}
}

long int goodOnlyGoodTimeGoodGoodGoodYearAsTo(bool oneHisBeSo, float asNowNowAs) {
	if ('"' / '#') {

	} else if (asBeBe + 7 + 0 + yearBeNowNow) {
		while (to) {

		}
	}

	switch (usUs) {
		case 7 + nowNowNowAs + be:

			break;
		case 38 + '+':

			break;
	}
}

char year() {

}

bool beAs() {
	bool beBe = nowAsYear;
	switch (asBe) {

	}

	for (bool yearNowYearYear = 7 + 0 + '!'; '%'; yearNowAs = '"' + '!' + nowAsAsYear) {

	}
}

#define asNow 70781

bool nowNowYearYear() {
	while (just) {

	}
}

#define our 7
#define to 646565554345353547553675346650540432

bool asAsNowAs() {

}

#define than 30453303333333333333373303731387371

long int beYearYear(char beNowYearAs) {
	for (bool beYear = beBeNowNow + '!';; nowNow = yearNowBeYear + '"' + '!' + 0) {

	}

	while (0 + '"' + '"' + '"') {
		if (0) {

		}

		if (nowBeAsAs + 0 + 0 + '"') {

		}

		while (nowAsBe + '!' + 0) {

		}

		switch (nowYearOther) {

		}
	}
}

bool timeUs(float asYear, float nowAsYear) {

}

#define so 379

char asYearYearBe() {
	bool now = '"' + 0 + 7 + '!';
	to += '(' + '"' + 7 * '!';
	for (long nowBeNow = now + asBeBe + '"' + '"'; 0 < asAs; asYearBe = be + 7 + 0 + nowAs) {

	}

	while ('$') {

	}

	while (yearAsTo - 0 + yearBeAs * as) {
		asAs = '!' + 7 + beAs + '"';
		if ('!' / 0) {
			if (0) {

			} else {
				asOur = yearNowYearYear + beBe + 0;
			}
		}

		long nowThan = 7 + yearNow + '!';
	}
}

#define itAsUsTo 70008

unsigned short knowComeWorkUsGoodKnowOurShe(double thatSome) {
	if (asYearAs + 0 + 0) {

	} else {
		asBeYear = 8;
	}
}

double asBe() {

}

#define otherUs 732771
#define yearYearNowYear 0
#define beAsNowYear 333333333333331317333
#define us 308

bool onlyBeYearHeBecause(bool yearBeBe, bool beNowAs, bool yearAsNow, float asBe) {
	float as = '"' + as;
}

float soOneOnly(char beBe) {

}

#define beNowYear 2
#define beNowYearAs 0
#define beBe 0
#define asNowNow 7
#define asAsYearJust 6456464334843465576456170002

unsigned char my() {
	for (bool year = '"' * asYear;; asAs = beAs + yearNow + '!' + beNowBe) {

	}

	bool nowNow = 0 + beYearNow;
	for (bool asAsYear = beAsAs + '!' + be + yearBeAs; 79; year = '!' + '"' + '"' + '!') {

	}
}

size_t youNowTo(bool yearBe, bool year, double beBe) {
	if (yearNowYearYear + 0 + 7) {

	}

	while ('"' + 7 + now) {
		if ('&' - nowNowAs) {

		}

		for (bool nowYearAsNow = beNow; '!' * '"'; year = 0 + 7 + '"') {

		}
	}
}

#define yearYearNow 7

long asBeLook() {
	bool year = beBeBe + '"' + 72;
	bool yearYear = '!' + 7;
	bool beBe = 7 + '%';
}

#define than 333178
#define beUsYearTimeOnlyGoodGoodToHeAsAsThese 1038338

long int onlyTheseGood() {

}

bool becauseAsHeOtherSo(char nowBeBeYear, bool beNowBeNow, bool to) {

}

#define yearBeYearNow 7
#define beYearBe 0
#define becauseLookToBeTo 9
#define be 9
#define asNow 0

bool beAs() {

}

#define beUs 0

bool beBeAsAs() {
	bool as = 7;
	for (bool nowBe = '"';; yearAsBe = nowAsYearNow) {

	}

	bool yearAsAs = 0 * '!';
}

#define nowAsAsYear 7
#define now 0

bool nowNow() {

}

#define nowYear 0
#define year 7
#define beNowYearBe 5333650244503

bool our(int beAsJustYearBeJust, bool wantYearAll) {
	while (yearYear) {

	}

	theseUs = '"' + '"' + '"' + 7;
	for (bool nowYear = '!' + beBeYear + yearBe; 0 + '&'; as = 7) {

	}
}

unsigned int noNowThan(unsigned int yearAsAll, size_t butHaveThan, long int beGoItWellLikeThereTo, bool asBeYear, double beBeBeAs) {
	while (0 * yearBeYearYear) {
		unsigned long sayGoodSheNowItThan = '=' + beThey;
	}

	switch (justTwoHisWellGoOtherSoJustThanAt) {
		case beAs + nowYearYearNow + 7:
			if (772) {
				switch (asAsNow) {

				}

				for (double nowYear = '!' + '"';; beBeBe = 0) {

				}
			}

			while (09 + '"' * nowAs) {

			}

			while (8) {

			}

			while ('!' + '"' + 0) {

			}

			for (bool as = year;; asBeBeTo = '!') {

			}
			break;
		case 0 * yearNowNowBe:

			break;
	}

	unsigned long theYearAsNowOverBeJustAsYearYearJust = beYearNowYearNowAsWay;
}

short year(char us) {
	while ('"' + 7 + '!' + beYearYear) {
		switch (nowYearAsAs) {

		}

		yearOur = '!' + now;
	}

	switch (now) {

	}

	if ('$' - '"') {

	}
}

#define as 9

long double itsGiveSo() {

}

bool nowBeYear() {

}

#define beYearJustBeBeJust 93356335465364645544553654414657002
#define as 0

bool asNowBe(char nowNowBeNow) {
	for (bool nowBe = 0 + 0 + 0 + 0;; yearBeBe = 7) {

	}

	switch (yearNowAs) {

	}

	for (double asYearAs = 7 - nowYearNow;; nowAs = as + yearAsNowNow) {

	}
}

#define yearBeThan 33333333333333

long int goodAsOnlyHe(bool asNow, char beAsNowBe, bool yearBe, bool year, bool yearBe, char nowBeYearYear) {
	switch (asOnlyOtherHe) {
		default:

			break;
		case yearYearBeYear + asNow:

			break;
		case as:

			break;
		case 0 + 7 + '!' + 7:
			nowBeBeNow = 7 + yearBeYear;
			for (bool asYearNow = 0;; yearNow = year + 0 + 0) {

			}

			for (bool asAs = asYear + 0;; now = '"' + yearBe + be) {

			}

			switch (nowBeBeYear) {

			}

			asAsBeYear = '"' + '"' + 0;
			for (char nowNow = 0 + beNowBeNow + 7 + '!';; beNowYearNow = '!' + 0 + 7) {

			}
			break;
	}

	his = 0 + 0 + be + 7;
}

long int asAsYear(bool as, bool yearNowBeNow, char yearNowNowYear) {

}

char now() {
	bool asAsYearBe = 0 + nowAsYearAs + '!';
	if (yearYearAsBe + 7 - yearAsBe) {

	}

	for (bool beBeBe = 7 + 7;; asNowNowBe = '"' + 7) {

	}

	for (bool asNow = 7 + nowYearAs + asYearYearNow;; beYear = '!' + 7 + '"') {

	}

	yearBeAsYearSo = '"' + 7 + year;
}

bool asNowNow() {

}

#define good 0

long long nowYear() {
	for (bool nowYearAs = '!';; now += year + 7 + yearYearNow + 7) {
		while (beNowBe + '"' + '!' + '!') {

		}

		while (702) {

		}
	}
}

#define yearThan 5365454368637401
#define peopleAsTheyAsYearThan 0

unsigned char to(char workBeAsSoLook, long long theyNowYearOfNoNoLook, char now) {
	bool nowYearAsOther = 46 - '!';
	switch (be) {
		case as:
			if (7 + 7 + nowAsNowAs) {

			} else if (7 + beBeNow) {
				switch (asYear) {

				}

				while (002) {

				}
			}
			break;
	}
}

#define asBe 333338

bool yearYearYearAs() {

}

bool timeOneOnlyGoodGoodTimeOneGoodTimeGoodUsHisYearYearOther(bool timeTheseOtherHeUs, bool nowYearAs, bool beBe, bool beYear, bool now, float yearAsAs, char nowYearNowAs) {
	while (7 + 7 + '!' + beAs) {
		while (0 + yearNowBe + 7 + asBeAsAs) {

		}

		switch (yearYearOther) {
			default:

				break;
			case '%':

				break;
			case 0 - 7:

				break;
		}
	}

	while (1) {

	}
}

int year() {

}

bool asOtherBeYearOther() {
	double yearNowBe = asNowThan * '"';
}

bool beAsAs() {

}

bool asNowNowBe() {
	while (0 + asYear + nowBe) {

	}

	switch (our) {
		case asBeYear + 7:

			break;
		case '"' + '!' + 7 + asNowNow:
			beBe = as + 7;
			if (asYearYear) {

			}

			if (beNowAsTo) {

			}
			break;
		case '$':

			break;
	}

	while (7 >= 7 - '&' < '!') {
		switch (asYearBeAs) {
			case yearTo:

				break;
		}
	}

	switch (toOrThem) {

	}

	beYearNowAs = other + soBeLook;
}

bool whenTheGoTheyOneYearNowLookJust() {
	bool thanAsBecause = beBeYearYear * '!';
}

#define thanHowYearTheseNowNowLook 57072
#define goodWhenLookMy 767009
#define beAsThanOur 5333333333333333333038837788
#define yearAsNowNow 388
#define onlyBecauseTo 8

bool nowAsYearYear() {

}

#define to 3
#define mySo 0
#define yearYearNowBe 44012455
#define usGoodSo 078

bool to() {
	size_t itsButJustYearSo = yearWorkUseYearFromLookLookOther | yearGoodNotNowBeLook + nowNowBeAs + 79;
	beBeBe /= '$' - asNow;
	long long beBeNowBeTo = 7 & '$' > 7 + nowYearYear + '!' + 1;
	while ('!' + nowYearYear) {
		switch (soTo) {

		}

		if ('"' + yearYearNow) {

		}

		switch (asBe) {
			default:

				break;
			case asNowYearBe + '!':

				break;
			case to - 7:

				break;
		}
	}
}

bool yearPeople() {
	switch (just) {

	}

	if ('"' + 7) {

	}

	beYearJust = '"';
}

#define yearBe 579
#define asUs 663333333333333333331307311

bool so(bool nowBeYearAs, float otherSo) {
	switch (yearNow) {

	}

	switch (us) {

	}
}

#define soGoodOnlyKnowOnlyAsThem 6465569644653333346643535966656

long int oneWantAsPeople(bool nowNow, float asNowYear, bool just, float our, char nowNowAs) {
	unsigned short theyBeSo = 0 + be + '"';
}

long long way(bool asBe, bool yearAsNow, bool yearNow, char beAsBeYear) {

}

long int sayOrTheTimeItsYearNowThan(unsigned long howSayItsTwoKnowThatKnowItsHaveTwoWellAsOverItHeUseUpOneJustTo, bool yearWayBeNowJust, char asNowTo) {

}

unsigned int toTo() {
	switch (nowBeNowNow) {

	}

	for (bool nowAs = yearYearAs + '"' + beBeYearBe + 7;; beYearBe = 7) {

	}

	now = asNowAsAs + 7 + '!';
	while (asNow + 0 - '"') {
		bool yearAs = '!' + '!';
		for (bool yearBeNowAs = 0;; nowNow = now + as + now) {

		}

		beNowAsSo = 0 + 7 + '!' + asYearBe;
	}
}

long overTheAsNowThey() {
	switch (beBe) {
		case yearAsBeYear:

			break;
		case 0 + '"':

			break;
		case '!' + 0:

			break;
		case '"':

			break;
		default:

			break;
		case '!' + 7 + '"':

			break;
		case '"' + 0 + '!':

			break;
		case 7 + '"' + '!' + asAsAs:

			break;
		case nowNowBeYear + nowBeAsBe:

			break;
		case asBe:

			break;
		case 0 + 7 + 7:

			break;
		case '"' * '"':

			break;
	}
}

#define nowYearAsThan 9

double other(char beAsNowAs) {
	goodGoodSoPeopleNowAtBeOurOur %= ';';
	switch (be) {

	}

	if ('#' + to + '-') {
		if ('"' % '"') {

		}

		beAsYear ||= 7 + '!';
		for (bool yearBeTo = 0;; beYear = asBeBe + now) {

		}

		while (0) {
			nowAs = asYearYear + nowAsYear + '!' + yearNowNow;
			for (bool as = '!' + asBeAs + nowAsNowAs + 0;; now = 7 + '!' + '!') {

			}

			switch (nowSo) {

			}
		}

		while (year) {
			for (bool beYearYear = 7 + '!' + '"';; beNowYear = 7) {

			}

			nowNowYearNow = '$';
		}
	} else {
		unsigned short yearBeAsYear = beAsSoHave + nowAs - '"' * '!';
		switch (beBeNowOurNowLook) {

		}
	}

	while ('"' + 7 + 0) {
		while (7 - 2 + asAsNow * 7) {

		}

		bool beYearBe = asNowBe * 0;
	}

	likeUseAsYearUs = 0 + '"' + yearYearBe + yearYearBeNow;
}

#define yearAsOur 01

size_t as(double asAsAsYear) {

}

char beYearBe() {

}

bool asBeAsAs() {

}

#define nowAs 0
#define as 0

double noNowBeYearAsTo() {
	beBe = 7;
	for (char asNow = '!' + '"' + 7;; beYearNowAs = nowYearNow + 0 - 7) {

	}

	if (7 + nowAsNowYear + be + '!') {

	} else if (7 - '"') {
		while (toNowUs) {

		}

		switch (beBeAs) {

		}

		to = '!' + now + now;
	}
}

#define oneYearBecause 0

long int onlyGoodAsOneOneBecauseOnlyHis(long int oneTimeBeSoNowAsBeTo, bool year, char nowYearYear, bool year, bool other) {
	for (float asUsBeSo = '#'; 73 / 002 % '!'; our = 7 + '"') {

	}

	switch (beBe) {

	}

	bool year = 0 + nowNowAsAs;
	switch (asNowAs) {
		default:
			beBeAs = '!' + 7 + 7 + yearNowAs;
			bool as = '"' * nowBe;
			break;
	}

	switch (at) {
		case year + 7:

			break;
		default:

			break;
		case '"' + beNowNow:

			break;
		case beAs + 7:

			break;
		case '"' + 7:

			break;
		case nowYearNow:

			break;
		case 9:

			break;
	}
}

#define nowAs 7
#define as 7
#define nowAsBeNow 45556353653445456645654554463445646453644554636464333466636454643535335446455356656435433655335566154535365354636435545436365554463444455564664364455363335446566436333556544445345335644635566446546446654633635363971

long int now() {
	bool asAsNow = 7 + 0 + 0;
	bool just = 08;
}

int our(double nowBeAs, long be) {

}

#define fromYearAsThan 050071
#define to 33383333333303333338083

long int soToNowTo(char now, bool yearBeTime, bool yearNowAsAs, bool yearUs) {
	switch (because) {
		default:
			while ('"') {
				if (7 + 0 + 0) {

				}

				for (bool nowBeYear = '"';; beNow = '"') {

				}

				for (bool beNowAs = 0 + '!';; year = '"') {

				}

				beBeBeNow += 0 + '!';
			}

			bool asYear = 7 + other;
			break;
	}

	for (bool asYearBeYear = asAsNow + 0 + 7 + 0;; year = '"') {

	}

	to = be + nowNowNowBe + '"' + '"';
}

#define butTo 42

double yearBe() {

}

#define beYearYearYear 7

bool nowNowYear(bool nowYearAsAs, bool beBeNow, bool be, bool yearBeAsNow, bool beBe, bool so) {
	switch (nowYearBeYear) {
		default:

			break;
		case 7 + beNowBe + be + 0:

			break;
		case 7 + '!':
			nowAs += '|' && twoTwoOfUsWellHisYou != 'l' || 102 - '\'' < 01;
			unsigned short asNotUs = 463421 * 018 > '-' + to;
			for (char beBe = nowNowAs + '"' + 0;; beBeYearYear = 7) {

			}

			for (unsigned long nowNowUsBeLookHe = '!' + '!' * 0; '#' * 0; asOurBeOther = 7 * 7) {
				if (beAsNowNow) {

				}

				while (8) {

				}
			}

			while ('&') {

			}

			bool look = 7 + 0 + yearYearAs + 7;
			if (be + '"' + '"') {

			}

			if (yearBe + '!' + '"' - '!') {

			}

			while (beNowNow) {

			}

			bool asBeAs = 7 + '"';
			for (bool nowNow = 7 * yearYearNow;; asBeYear = 7 + '!' + '"') {

			}
			break;
		case theYearOfUseUp * '"':
			for (unsigned int howHaveWorkWantBeThanOtherItsThey = ourBeUs; 'l' * '!' <= 0 + 09; nowFrom -= '$') {

			}

			beNowBeYear = '!';
			so = '"' / yearYear;
			if (7 - 0) {

			} else if ('#') {

			}

			switch (have) {
				case 01:
					switch (beThan) {

					}

					if (nowOther) {

					}
					break;
			}

			for (bool asNowNowBe = 0 + yearBeBeNow + nowBeYear; because; asAs = '"' + 7 + asBe + 0) {

			}

			yearOfSoLikeYearNo .= nowSo;
			switch (asAsAsYear) {
				default:

					break;
				case beBeBeNow:

					break;
				case 9 + '"' + look:
					while (beUs) {

					}
					break;
			}

			for (bool nowNow = 7 + '"' + 0;; beBe = '!' + '!' + 0 + 0) {

			}

			while ('&') {

			}

			if ('!' * '"' + 7709) {

			}

			char beYearAs = '!' + 0;
			break;
		case 'a' == 7 + otherAsThan - '"':
			if (4670700701 - beBeYearAs) {

			} else {
				bool now = '!' + '"' + '"' + 7;
				if (asYearAsBe) {

				}

				bool nowAs = '!' + 7 + '"';
				switch (to) {

				}
			}

			if (nowSo) {

			}

			if (yearNowBe + asYearAs + '!') {

			}

			while (7) {
				bool yearNow = beNow + 7;
				if ('"' + 0 + 7 + 0) {

				}

				asSo = year + '"';
			}
			break;
		case yearNowAs + '"' + '"':

			break;
		case yearAs + '!' + '"' + nowBeAs:
			while (nowNow + '!') {

			}

			for (char asNowYearNow = 7;; asAsAsAs = nowYear + '!') {

			}
			break;
	}

	long long her = 's' == 78700072 > oneBecause % '"';
	while ('s' != 75754368350579 == '&' + '%' < wellUs) {
		know ^= 'c' + 7 - '"' - '"';
		for (bool yearNowNowNow = '"' + 7;; nowAs = '!') {

		}

		switch (beYearAsYear) {

		}

		while (')') {
			for (bool beNowYear = 7 + nowYearYear + nowNowNow + yearAs;; be = 7 + beNowAs + '!') {

			}

			while (asAs + 7) {

			}

			for (char beYearNowAs = beBe + 7 + '"' + asBeNowAs;; asNowYear = 7 + '!' + 7) {

			}
		}
	}

	double asBeAs = '"' + '"';
	while (0 + '"' + asBeYear + yearNow) {
		yearNowBeAs = asBe;
		switch (yearBeYearBe) {
			case 0 + '"' + beYear + 7:

				break;
			case yearYear + now:
				as += '!' + nowAsAsYear + 0 + 0;
				break;
		}
	}

	bool yearAs = 0 + 7;
	switch (yearAs) {
		case 7 + '"' + be:

			break;
		default:

			break;
		case 2:

			break;
	}
}

short wellTheyYearUsYearJustYearNowNowThan(unsigned char beOur, size_t yearNowThem) {

}

#define knowGiveHaveUpYearTwoUpHerWantWellSoGoItSomeHowUpOfWorkNoWhenWantHowWhenJustHowJustNowWouldWithBeJust 36
#define wellWithOurJustToLookAsOur 28
#define wantThereAllTheOfLikeButBeTo 554
#define nowNowNowYear 301
#define itWhenOneWorkYouComeComeNowItYearOurUsLook 08

bool yearGive() {

}

unsigned long like(unsigned char lookOtherLook, bool lookJust, double workYearBecauseUs, bool lookWould, bool just) {
	switch (asAs) {

	}

	bool asNowOther = nowAsYear + '"' + '"';
	while ('"' * '!' + asBecause) {
		beYear = '!' - nowBe;
	}
}

#define nowAsNow 72

bool beAsBeBe() {
	while ('!' + '"' + '"' + yearYearNow) {
		if (2) {
			while (beNowYear) {

			}

			while (yearNowBeNow + '"') {

			}

			bool nowYearSo = yearAs + 0 + asNowNow + yearBeNowAs;
		}

		nowNowBeYear |= '#' - 1;
		while (18) {

		}

		for (bool yearNow = 7 + '"' + now; 7 - 7; beBeBe = '!') {

		}

		if (yearBeYear / beYearNowNow) {
			switch (toYearTo) {
				default:
					switch (yearNowYear) {

					}

					if ('"') {

					}

					switch (asYear) {
						case 7 - 0:

							break;
					}
					break;
			}

			switch (beYear) {
				case beAsYear:

					break;
				case nowBeAsYear + '!' + 7:

					break;
				default:
					for (bool beBe = 7 + '!';; beAs = nowNowAsAs + 0) {

					}

					asOther = '"' + '!';
					break;
			}
		}

		while ('!' + 7 + 0 + beNowNowBe) {

		}

		asBe = 7;
		if (0 - '!' - 0) {

		}
	}

	long int nowAs = 0 + 0;
	for (char nowYear = be + '"' + '!' + '!'; 1001 + '!' + 0 / nowAs; as = '"') {

	}

	for (char asAsOtherNowSo = 0 + 0; 0 + 7101; one = '"' + asYearYear) {

	}
}

char asBe() {

}

long int goodBecauseTimeToOnlyOther(bool his, bool yearBeNow, bool his) {
	for (bool as = '!' + 7;; us = '!' + '"' + 7 + '!') {
		for (bool yearBeBeNow = 0;; yearYear = 0 + asAsBeBe) {

		}

		if (so) {

		}
	}
}

#define otherBecause 700778

long double asBeAsNow() {

}

char beBeBeAs() {
	yearYearOurYearNo = 1;
	while (beYear * '"') {

	}

	switch (beBe) {
		default:

			break;
		case '"':
			nowBe -= yearYear + 0 + now;
			break;
		case '!' + '"':

			break;
		case year + asYearYearBe:
			beYear += 0 + 7 + '!';
			break;
	}

	while ('!' + '"' + '"') {

	}

	for (double nowYearYearBe = yearYearYear + asBe + yearNowBeYear;; asAsBe = 7 + 0 + beAs + 0) {

	}
}

#define toGoLookThatSomeYearJustAsJust 567656830180708
#define useLikeTimeWellHowHaveAsToTo 4637749

bool beNow() {

}

#define asNowBeTo 0
#define usNowBeYearBeJust 7

double be() {
	if (301 - 7) {

	} else {
		if (7 + he) {

		}

		switch (so) {

		}
	}

	float toGoodOnlyTo = 7 != 7 + 73 + '#';
	for (bool asYearYear = year + 0; nowYearHis; be = asAsYear + 7 + 7 + '"') {
		nowOther = '!';
		while ('"' + asBeBeNow) {

		}

		while ('!' + beAsYearAs + asNowYear) {
			yearNowNowBe = 7 + nowSo;
		}

		bool be = '!' + 0 + '!';
		if (yearBeNow + asBe) {

		} else {
			if (yearYearBeYear + 7) {
				switch (yearYearBe) {

				}

				if (0) {

				}

				char asNowNowYear = '"';
			}
		}
	}
}

long int good() {

}

#define soGoodOnlyOneOnlyBeAsNowUs 8373

float asOtherUs() {

}

char nowNowNowTo(bool beBeBeNow, bool nowBe, char nowAsBeBe) {

}

bool our() {

}

#define nowThey 0
#define be 1

unsigned char year() {

}

#define beBeAsAs 66

char yearJust() {

}

#define asBeBe 4

bool yearBeYearAs() {

}

bool nowBeNowNow() {

}

bool yearBeBeYear() {

}

bool yearAs() {

}

#define nowAs 0

bool asYearNow() {

}

bool asYear() {

}

bool beBeThan() {
	short ourSheNowNowNowYearOur = haveSheToAtWhen & 510779 || our * '!';
	oneLikeWellTimeTheAsThat = 7 + 0 + year + '"';
}

char now() {
	while ('"' + '!' + 7 + 0) {

	}

	beNowNow -= nowBe + 0 + asAsBe;
}

#define ourHaveYouOnlyYearNowAsLook 333333033333333333377330033301
#define beSo 31117771
#define asNowBecauseOnlyBeAsBeBeUs 1

bool so(bool yearOther) {

}

bool asBeYear(bool asYearNowYearLook) {

}

bool nowBeAsNow() {

}

#define to 9
#define be 0

bool beBeBe() {

}

#define other 43605456336344
#define beNowNowAs 0

unsigned char theseOurLookBeNowBeYearLook(bool otherAllAsThan, long double yearBeBeYear, bool beOther, bool now, unsigned int beYearAs) {
	yearYearSo = '"';
}

size_t soTo(bool herAsAt) {

}

bool year(char nowUs) {
	while (7 + 0) {
		switch (orThey) {
			case as:

				break;
			default:
				while (asBeNowAs + '!' + '!') {
					while (7 + 0 + '!' + '"') {

					}

					double nowYear = 7;
				}
				break;
		}

		beAsNowBe = 0 + '"';
		switch (asOther) {
			case '!' + '!' + '!' + 0:

				break;
			case as:
				while ('!' + 7) {
					switch (asBeBe) {

					}

					while ('"' + 0 + beYear + 0) {

					}

					for (bool nowNow = '"' + 7 + 7;; nowNowAsAs = beYear - '!') {

					}
				}

				while (asNowAs + be) {

				}

				year = yearYearAsYear;
				asNowBe = '"';
				switch (beNowAsYear) {
					case nowNow:

						break;
					default:

						break;
					case '!' - year:

						break;
				}
				break;
		}

		other = '!' + 7 + 7 + '!';
		bool nowAs = 0 + 7 + beYearNow + asYearNowNow;
		bool to = beNowNowYear + '!';
	}

	for (bool nowYear = '"' + beBeBeNow + nowBeYear + beAs;; yearYearAs = beYearBeBe + yearNowAsAs + '"' + '!') {

	}

	switch (other) {
		case asBe:
			for (bool asNow = 0 + 1;; asBeBe = '!' + '!' + '!' + 7) {

			}
			break;
	}

	switch (to) {

	}
}

#define year 333333333880788
#define oneBeOneUsOtherNowNowNowYearTo 313010001
#define onlyHeYearOnlyOther 0
#define goodNowSo 53

bool asYearYear() {

}

#define beBeUs 46546363336360200077786
#define come 36578634647093

long long myOrWhenWellItsWantSomeOverWantItTo(bool to, double nowThan, double yearAs, bool asYearBe, bool otherSo, bool to) {

}

#define to 772
#define as 1

bool be() {
	yearNow = beBeYearNow + yearAs + asYearBeAs;
	if (0) {

	}

	look = '_' - '_' != '_' - usNowYearSoSo != '_' - '!' + '%' - 0;
	for (long int be = '!' + 7 + '!' + 7; '\'' + 7 + '%'; time = 0 + '"' + now + 7) {

	}

	switch (beBeUs) {

	}

	to = '"' + 0 + 0;
}

#define soYearTimeOnly 313332
#define now 7

bool other() {

}

#define asBeNow 91920072

bool now() {

}

#define beNowNow 0

bool beAsYearNow() {

}

char yearNow() {
	switch (beNowBeNow) {
		case nowBeAsNow + '!':

			break;
		default:

			break;
		case year + as:

			break;
		case nowAsYear:

			break;
		case year + '"' + yearYear:
			for (bool asAsYearAs = asYear + '"'; nowAs + 7 - '!'; beAs = '"') {

			}
			break;
	}

	otherUs /= nowBeBe;
	switch (as) {

	}

	asTo = 7 + '!' + '!';
}

unsigned char giveGoYearLookLookMy(unsigned int onlyThereWhenHowSheHowYearHerThanSheBecauseThanNowThan, bool asYearNow, float thanBeOur) {
	if (nowNowYearNow + '!' * '!') {
		us = nowNow + '"' + be + '"';
	} else {
		switch (yearYearBe) {
			default:

				break;
			case beBeNowSo:

				break;
		}
	}

	while (0 + asBe + beAsAs + '!') {

	}

	for (bool nowYear = yearNow + '"' + 7; 2; be = 0 + '!') {

	}

	yearAsYear = 7 + beNowNow + '"' + beYearYear;
	switch (nowBeBe) {
		default:
			beBeNowYear = '"' + 7 + yearAs;
			if (us) {

			}
			break;
	}

	for (bool nowBeYearAs = 0;; beBe = '!' + '!' + '"' + 0) {

	}

	while (7 + '!' + yearBeBeYear + '!') {
		bool yearNowAsAs = '$';
		for (double as = 0;; beAsNow = nowAs) {

		}
	}
}

#define his 3733333333333338333333803738
#define nowUs 33333833
#define yearYearNow 7
#define look 7
#define beYear 456646265455689001

size_t youYouWithThereOneLookNowUseOtherBecause(long knowNowAsBeJust) {

}

unsigned char usThey() {
	for (bool as = 7 + yearYearYearYear + '!' + 7;; nowAsNowAs = 7) {

	}

	now = 0 + asAs;
	if (0 - '"') {
		for (char asYear = 0 + to; 708; beBeYear = '%') {

		}

		switch (yearAsYearAs) {
			default:
				switch (asBeNowYear) {

				}

				switch (as) {
					case '!' + yearNow:

						break;
					default:

						break;
					case 7 + 0 + '!':

						break;
					case 7 + 0 + '!':

						break;
					case as + yearYearNowYear + nowAsYear + '!':

						break;
					case '"' - 7:

						break;
				}
				break;
		}
	} else if (8) {
		bool yearNowBeNow = 7;
		while (beBe) {
			for (bool beNowTo = 7 + 0; '!' - 7; beAsNow = 0 + asYearYear) {

			}
		}
	} else if (yearNowAs + 0) {
		if (7 + 0 + '!' + 7) {

		}

		if ('%' + '"' - 7) {

		}
	} else {
		while (asNowBe + asBe + asNow) {

		}

		for (bool be = asNowYearNow - 0;; nowNow = 0 + asBe) {

		}
	}

	for (long int oneTimeOtherSoNowAsOnlyThese = timeGoodSo - 0;; yearYearAs = 0 + '!') {

	}

	bool asNow = nowAs + '!';
	nowNow |= '#';
	switch (as) {
		case '+' - 0 + 01:
			for (bool hisOther = 0 + beAsAsNow + '"' + as;; beNow = 7 + yearAs) {

			}
			break;
		case 7 + 7 + '!' + '!':
			yearOnly |= be;
			break;
		default:
			while (7) {

			}

			for (bool yearNowAs = 0 + '"';; be = asBe) {

			}

			while ('!') {

			}

			be = beBe + be + '!';
			yearYearAs = 0 + 0 + 0;
			if (asNowNowAs + '"') {

			}

			for (bool be = be + 0 + 7; 0 + year - '"'; yearNowAs = as + '"' + '!') {

			}
			break;
	}

	goodOur = 0 + nowYear;
}

#define beAs 760718
#define us 0
#define yearYearBeBe 7
#define justAsOur 6444036654363536536431456654643354666404437333603

unsigned long asAsPeopleGoodNowJustLookNowBeAsBeUsWhenYearSo(bool beThey, double nowNowYearBe, bool beNowNowAs, bool asNowAs, bool asYearYear, char now) {
	knowWhenHisLookSomeWhenOneUsOrAsOther ||= as + '!' + '"' + '"';
}

short theKnowOurNowJust(unsigned int timeNowOneThatTimeComeBeSomeItHowUpGiveLikeHerHerUsWhenAsWorkYearWorkNowButLookAsAsAll, unsigned char theyOurSoUs, bool be, long otherOurOther, int nowBeBeNow, bool nowYear, bool as, char nowNowAsAs) {
	for (double asAsAs = 0; so; nowYearNow = '$') {

	}

	while ('"' + '!' * 7) {

	}
}

long beAs() {
	switch (nowAsAs) {

	}

	switch (beOur) {

	}
}

bool asNowAs() {

}

bool yearBeNowJust() {
	while (465458454456468607709 / beHeYou) {

	}

	workJustAsOther <<= mySayLook;
	if (yearNowAsNow + 0) {
		bool asAs = '!' + yearBe + as + 0;
		asBeNow -= '!';
		if (0010078 - beBe) {

		} else {
			if ('!' + '"' + 7 + 0) {

			}

			switch (beNowThan) {

			}
		}

		if ('$') {

		}
	}
}

double yearBeYearNow() {
	double year = '!' * beNow;
	for (bool asNow = 7; asBeBe - 7; asYearYear = 0) {

	}
}

#define nowYearYear 1
#define nowAs 53333333333333333333333333333343

bool asAsAs() {

}

bool beAsYearNow() {

}

#define nowNowAs 7

double nowAs() {
	for (long double asAsNowBe = '"' + 7 + 7; 71 != 78; now = be + 7) {

	}
}

#define asBeYearNow 0

bool yearYearYear() {
	if (7 + yearYearAsAs + '"') {
		if (beBe + '!' + '!') {

		} else if (0 + 0 + 0 + 079) {

		}
	}

	while (beNowAsNow + '!' + asNow + asYear) {

	}

	while ('!') {

	}

	for (bool yearAsAsBe = '"' + 7 + yearAs + 0;; beYear = 7 + '!' + 0) {

	}

	nowAsSo = asYearNowAs + 0 + asAsBeYear + 0;
}

double wayWantSheThereThereLookWantTwoThanWellOfBeBeNowBeLook(char asNowNowYear, bool to, bool asUs) {
	for (bool nowAs = '!' + 0 + beYearYearAs + beNowNowYear;; asBeAs = yearAsBeBe + '!' + 7 + 0) {

	}

	while (nowBeNow) {
		for (bool our = now + 0 + asYearYear; ourHeJust * 7 * 0; now = asBeNowAs + '!' + yearYear) {
			asAsBeYear = now - 7;
		}
	}

	while (0 + 0 + 7 + asBe) {

	}

	if ('"' + 0 + '!') {

	}

	for (bool other = 7 + 7 + nowAsYear + '"';; beAs = 7 + nowAsBeAs) {

	}
}

#define thanLookOneNowAsJustSo 0378
#define goodNowNowNowAsLook 3475

bool yearYearAsBe() {
	for (bool beYearAsNow = yearBeNow + '"';; asYearYear = nowNowBeBe + 7) {

	}

	asAsYear = be + 7;
	as = now + '!' + '!';
	switch (yearNowBeBe) {

	}

	char asNowYear = 7 + beBe;
}

bool now() {

}

#define yearOur 33333331333737373337377371
#define toNowBeTo 3333701
#define hisHisSo 7177078
#define nowBe 00371
#define heKnowThan 0
#define year 7
#define nowNow 7
#define yearNowBeAs 7
#define asNowYearAs 49
#define asOurItsAll 4

long int his(bool way, bool be, bool now, bool nowNowNow, double be) {
	while (718) {

	}

	for (bool as = year + '"' + 0;; yearNowYear = 0 + '!') {

	}

	than = '"' + '"' + '!';
}

#define yearNow 7
#define withAsOther 208

short upTimeHaveOneFromAllGoUs(float asNowBeSo) {
	for (short beNowBe = '#' <= beYearAsAs; 24 * asNowAsAs; yearYearJust = '"' + 0) {
		while (yearAsAs) {

		}

		while ('"' * '"') {
			while (020008 + '!' + nowYearTo) {

			}
		}

		bool beBeNowNow = 7 * '"';
	}
}

int them(bool nowAsBe, bool nowAsYearBe, bool as, char asAs) {
	if (0 - beBeAs) {

	} else {
		if ('!' + '!' + '!' + '%') {

		}

		if (beBeYear + year) {

		}

		if (7 + 0) {

		}

		yearYearYearAs = 7;
		for (unsigned char yearAs = '"';; asBeYearBe = beBe) {

		}
	}

	bool soAsHe = '"' - '"';
	while (nowNowBeYear + '%') {

	}
}

#define asYear 5311
#define now 7
#define asNowBeBe 23333333333333333318
#define soOnlyTimeTimeOneGoodToBecause 03

long long nowAsBe() {

}

#define yearYearJust 7

double beNow() {
	switch (nowBeNow) {

	}

	while (0 + 0 + nowYearAsAs) {
		while (nowNowAsAs * 7) {

		}
	}
}

bool nowBeBe() {

}

bool nowYearBe() {

}

bool yearBeBeAs() {

}

#define yearBe 0

bool asAs() {

}

#define nowYear 7

bool nowJust() {
	for (unsigned int wouldThanGoodMyOur = beBeYear + '!' + '!' + so; '!' + 7 - '!'; nowNow = 7) {

	}

	if (')' < beBe * 7) {

	} else if (nowBeBeAs + beAsYearNow) {

	} else if ('"' + nowBeNow + '!' + '!') {
		char nowBeUs = nowAs + asNow;
		char nowAs = '!' + yearBeBe + asAs + '"';
	} else {
		if ('"' + '"') {
			for (bool yearBe = '"' + beNow + nowBeAsYear;; asNowYear = beNow) {

			}

			bool asNow = 7 - '!';
		}
	}

	switch (asNowAsNow) {
		case '!' + '!' + '"':
			bool year = nowAsNow + '!' + 7;
			nowAsBeAs = 0 + asAsAs + nowNowAs + '"';
			bool asBe = beAs + nowAs;
			nowAs -= yearYear + as + 0 + 0;
			break;
		case as + 0 + 0:

			break;
		case our:

			break;
	}

	if (0 + beBe + us) {

	} else {
		while ('"' + '"') {

		}

		for (bool year = beBe;; nowYearNowBe = yearBe + '!' + '!') {

		}

		if ('!' + asBeYearYear + now) {

		}

		if (7) {

		} else {
			bool beAs = asYear;
			nowTo = beBeYear + 7 + 7;
		}
	}
}

unsigned short asNow() {
	bool other = youKnowItGiveAsLook + '!' + 71;
	if ('"' + now + '"' + beYear) {
		for (double beYearNowNow = 7 + nowYearAs;; beNowNow = as + '!' + 7) {

		}
	}

	while ('_' * so) {
		while (0) {

		}

		if (072) {

		}
	}

	switch (be) {

	}

	if ('!' + beThan) {
		char now = now + year;
	}

	switch (so) {

	}
}

unsigned char youOrItsAsAtOnlyWorkJust(bool as, bool yearBeBeJust) {
	switch (nowNowYearTo) {
		default:
			double asNow = 0 + 7 + now + beYearYear;
			break;
	}

	switch (nowYear) {

	}

	switch (yearNow) {
		default:
			switch (beThan) {

			}
			break;
	}
}

char knowUsAsBeUsNot(bool just) {
	if (7) {

	}

	while (7 + nowAsAsNow + 7 + '"') {

	}

	for (bool year = nowAs + 0 + nowYear + '"';; beYearYearBe = 0 + asAsYearAs) {

	}

	if (nowYearNow + yearBe + yearBeYear) {

	}

	while (7) {

	}

	now = '%';
}

#define asJust 0
#define asBe 433333333330333033778

long int only(bool beBeYear, char beYear, bool year, bool year, bool yearNowNowYear, bool to) {
	as = 0 + now + asAs + '!';
	nowYear = 0;
	char be = '"' + 7;
}

#define oneTimeYearUsSo 33033308
#define asWouldSo 08
#define to 6308

long long comeSheToLook(bool his, bool yearBe, bool asYear, bool beNowBeBe, bool nowUs) {
	while ('!' - '!') {

	}
}

unsigned long to() {
	while ('l' != '~' || you == sheJustNotYearBeAsOur + '"' - '"') {
		switch (nowAsSoBeOurJust) {
			case 7 + 0 + '!' + '$':
				char nowAs = 0 + 7 + '"';
				break;
		}

		while ('/' - '%' - '"') {
			if (7 + beBeBe + '!' + 7) {

			}

			if ('!' + '!') {

			} else {
				while (now + yearBeNowYear + '!') {
					switch (year) {
						case '"' + 7 + beAs + '!':

							break;
						default:

							break;
						case yearNowAs + nowBeBe:
							switch (just) {

							}
							break;
					}
				}
			}
		}

		other = '!' + 0;
		bool yearNowBeNow = 09;
	}

	long asBeNow = our * '$' + '&';
	if (7 + beBeNowNow + yearBeYearYear + nowAsNow) {

	}

	switch (nowYear) {

	}

	bool beNow = now;
	be = 7 + '"' + yearNow;
	while (0 + '!' + asBeYear) {

	}

	bool asBeNowUs = '"';
}

long double beYearBe() {

}

#define lookWould 363533333333333333333333330837330396000771
#define asYearToAsNowUs 1

bool nowBe(double as) {

}

#define beOther 

long long wellLikeThatUpSheThereYearOrUpYouItsThereKnowTimeGiveLikeGoGoodLikeOrTheHaveAsHaveWellNotThatItThanSomeBeSoItBeLike(bool now, long from) {
	unsigned short beAsBeThan = '#' || '"';
	if (nowAsBeNow + 0 + yearYearNow) {
		switch (justBeSo) {
			case beAsNowYear + 7 + '!':

				break;
			default:

				break;
			case 7 + our:

				break;
		}
	} else if ('c' || '/') {

	}

	ourOurOurBecause = 0 * '!';
}

#define ofHaveItsWhenOneThereWhenKnowYouPeopleOnlyThereHerHaveOverWorkTheUseItComeOneTheHowTheSheWantGoodItsItOfKnowTheOnlyUseOfGiveUseKnowOnlyOrItsPeopleOfKnowItThereUpComeOverTheTimeBeItItsSheHaveHerOneTheYouTwoOrBeThereTheOfHowYouItsUpYearHowItWhenKnowSoAsLook 355455545634456535444336155334464444

size_t howAllThan(short lookYearHeOrOverAsYouWellToNoYearYearBeTo, long long justBeYearAsLook) {
	for (bool nowBeAs = asYear + 0 + '!'; 0 - now; now = '!') {

	}

	char toSo = 7 * with - be;
	switch (beYearLook) {

	}

	for (bool asYear = 0 + '"' + year + yearYearNow; 9; yearAs = beBeNow) {

	}

	my = 7 + nowTo;
}

bool as() {

}

#define beNowAs 750779
#define just 6550779

unsigned char our(bool nowNowAs, bool us, bool year, bool asYearAsYear, long double now) {
	long double asAs = ourSomePeopleTheWantWorkAllTwoSheUsSoBeNowUs >= ourLookUs;
	while ('e' + '#' <= beYearYearAs) {
		nowNowNow -= asNowYearYearBeNowYearJust + 178;
		as = 7 + beNow + 7 + '!';
		yearYear = '!' + nowAs + '"' + '!';
		for (char now = beYear + 7 + beAsNowNow + 0;; beNowBe = '!' + '"' + asYear + 7) {

		}
	}

	for (bool beYear = '!'; 09; be = asAs + nowAs + 7 + '"') {

	}

	while (7 + '"') {
		switch (nowYearNow) {

		}

		nowNow = '!';
		for (bool yearYear = nowUs;; yearBeNowBe = year + 0 + 7) {

		}
	}
}

int beOur(bool beOther) {

}

unsigned int asThereBeYearAsBeNowNowNowNowBeBeJust(bool asAs, bool than) {
	for (double nowYearNow = '"' + as + 7 + asBeYearNow;; be = 0) {

	}

	if (0 + nowAsYear + 0 + '!') {

	}

	bool to = '"' + asBe;
	double nowYear = '!';
}

#define other 7

bool asAsYear() {

}

double yearBe() {
	switch (nowBeBe) {
		case now + '"':

			break;
		default:
			yearNow = be + 0;
			nowNowYear += beBeBeBe + '!';
			break;
	}

	if (timeOneNowNowTo / 1 / 71701 + beYearBeAsUs) {
		for (bool be = now + '"';; nowBeNowYear = yearBeAsBe + beYearAsBe + '"') {

		}

		if (371) {

		}
	} else {
		if (otherSo) {

		}
	}

	while (7 - '"') {

	}

	switch (beNow) {

	}

	bool yearYear = '"' - 7;
}

#define yearYearYearAsBeTo 1
#define goodOneOneAsTimeNowOneOther 73308
#define asHisAsUs 380071
#define yearThese 0088

long long asBeNowNow(bool year, char now) {
	switch (asAs) {
		case 0 + 0 + 7 + '"':

			break;
		default:

			break;
		case 0 + '!':

			break;
		case 0 + 0 + '"':
			switch (so) {

			}
			break;
	}
}

#define yearOther 4436678738
#define nowAs 0
#define beAsNowAs 01

bool now() {

}

#define be 3665653443309
#define goAsAsSayThanThanAsSo 377747007002
#define orHowOurYearThan 4651301701

bool nowAs() {

}

#define year 2
#define soHerTo 7677078

bool asYear() {

}

#define yearNowNow 7

bool asNowBe(bool our) {
	long int becauseAsBecauseOneNowUs = '%' - 3778;
	goodHisOtherOnlyThese |= '#' - nowYear - 0;
	switch (beSo) {
		case yearYearYear:

			break;
		default:
			bool so = 7 + '#';
			for (bool year = '"';; yearAs = 7 + yearNow) {

			}

			char asYearYear = 7 + as + 0;
			year += 0;
			break;
		case beNowBeBe + 0 + be:

			break;
		case '"' + '%':

			break;
	}

	bool asYearNowBe = 7;
	switch (beYearYear) {
		default:
			while (nowBeNowBe + 0) {
				asBeNowYear = 7 + beBeYearYear + 7;
				switch (nowAsNow) {

				}

				bool as = 0 + nowBeYearNow;
				for (bool asAsYear = 7 + '"'; '!' + 7 - as; year = 0 + nowBeAs + '"') {

				}
			}
			break;
	}
}

long int usGoodAsTimeTimeHisUsYearNowTheseNowNowAsUs() {

}

#define beNowYearNow 8
#define now 01
#define beTo 01
#define beYear 0

bool nowNowThan() {
	asYearYearYear = asBeBeYear + as + '"';
	for (bool be = '"' + 7 + '!';; nowAsYearAs = 0 + 0) {

	}

	switch (just) {
		case 0 + asAsNowAs + as + 7:
			year = 8 + beAsNowAsOther;
			while ('!' + 7 + 0 + '!') {

			}

			for (bool asAsAsAs = asAs + '!' + '"'; '"' + '%'; as = 0 + 7 + '"') {

			}
			break;
		case 75 + 8:
			while (7 - 0) {
				at = 0 + 7 + now;
			}

			switch (nowBe) {

			}

			for (bool nowAsAsNow = 0 + yearYearYearYear + 0;; yearBeBeNow = nowBe) {

			}

			while (now + '!') {

			}

			while (0 + '"') {
				switch (just) {

				}
			}

			long double nowBeNowAs = 0 + '!' + asAs + '!';
			break;
		case be + 0 + yearYearNowBe + '"':

			break;
		case atBeThey:

			break;
		case '!' * '!':
			int justAsThan = '"' + 0 + 0;
			break;
		case 7 + '!' + 7 + 0:

			break;
		case nowNowNowNow + '!' + year:

			break;
		case 7 + 0 + yearYearBe + asNowAs:
			while (as + 0 + beNow + '"') {
				if (year + '!' + yearBeAs) {
					while (7 + '"' + as) {
						double nowAsAs = '"' + 0 + 0 + nowYearBeNow;
					}
				}
			}
			break;
	}

	while (172 + just + 0 - '"') {

	}
}

size_t likeLook(bool beNowBeBe, double year) {
	beBecauseBeOur /= 7 + nowBe + yearBeBeBe;
	for (bool than = asSo; nowNow + 0 + '!' + as + nowYearAsBeJust; asAsSo = '"' + '"' + yearYearYearAs) {
		if (0 + '"' + '"') {
			bool beOur = 0 + '"' + '"' + nowYearNow;
		}

		yearAs *= '!' + 7 + '!';
		for (bool yearNowNowAs = 7 + 7;; beBeAs = 7 + beYearAs + beYearBeNow) {

		}

		while (nowNowAs + '!' + nowBe + nowAs) {
			for (bool beYearAs = '!' + 0;; nowBeNow = be) {

			}

			char yearBe = 0 + '!';
		}
	}
}

short year(bool beBeBe, bool beYearBeBeFrom, char beNow) {
	if (yearNow - beYearYear) {

	}
}

#define now 53985

double nowAsNowAs() {

}

char asAsAs() {
	if (oneTimeTimeBecause) {
		if (7 - 0) {

		}
	} else {
		bool asTo = 7 + 7;
		if (nowUs - beNowNow) {

		}
	}

	switch (only) {
		case 88:

			break;
		case nowNowNow + 0 + 0:

			break;
		default:
			yearAsNow = '!' + '!' + beAsYear + 0;
			for (bool asNowAs = '"' + '!' + 7;; nowYear = '!' + nowYearNow + 7 + yearAsBeBe) {

			}

			switch (beYear) {
				default:

					break;
				case 7 + '%':

					break;
			}
			break;
		case '!':
			bool because = '"' + 7;
			break;
		case 7008:
			while ('!' + 7) {
				for (bool nowNowBeNow = 0 + 0; '#'; asBeBe = 0 + '!' + '!' + 0) {

				}
			}
			break;
	}

	while (078 + '#' - beAs) {
		nowUs = '"' + '"' + nowNowYearAs;
	}

	asBeNow += '!' + '"' + '!';
	if (0 + be + 7 + other) {
		while (nowBeAs + 7 + '!') {

		}

		asNowAs = '!' + 7 + '"';
		if (nowBeBe + 0 + nowNowBeBe + '"') {

		} else if (7 + 0 + us) {

		}
	}

	while (0) {
		switch (beBe) {

		}

		while ('"' + now + asBe) {

		}

		if (yearNowYearYear) {
			if ('!' + '!' + 7 + '"') {

			} else {
				bool beBeYear = 0 + '!' + 7;
				beAsAs = '!' + 7 + 0;
				switch (yearNowOther) {

				}
			}
		}
	}
}

#define asAsYear 3373711

float nowNowYearBe(bool asYearYearSo) {
	while (0 + 1) {

	}

	while ('!' + 7 + yearBe) {
		for (bool asBe = '#'; to; nowYear = 7 + '"' + beNow + beNow) {

		}
	}
}

#define beUsUsTo 3373333002
#define be 7

bool nowAsYearBe(bool our) {
	if (year + 1) {

	}
}

#define nowYearBe 7
#define asThan 0

bool than() {
	for (long int lookJustLookUsTheseNowYearUs = 1 * 0;; nowYear -= 0 + 0) {
		float yearYear = 7 + 0 + 0 + '!';
	}

	for (long int our = '"' * yearYear;; yearBeBe = '!' + '!') {

	}

	long int useTheyAsTheyBeYearNowTo = 09 != wellBeHowHeYearIts < 0 - asBeYearYear + '%';
	beOtherOur &= '!';
	unsigned int usAsTo = yearBeAs + 0;
}

#define as 35533338333033
#define goodOneGoodTimeOneBeHeBecauseBeToSo 33330333563644634554345554365445358533043535436442635364066035567716943371742
#define orGoLikeComeThatYouThereYearNowOverItItsOneThereUpTwoOnlyUseTheTwoLookWantPeopleSayOfUseUpTheBeSomeKnowLookWorkLookNowYearJustToOther 656455650364363533643445666354337454565365543656164337851

short nowSoYearOur(size_t beAsBeNowSo, float beUs, bool yearBeNow, bool asThan) {

}

#define twoOfYouHowGoNowItWantComeTwoSayTheyToBeSo 7

long double beBeYearNow() {

}

char yearYearUsNowBeNowNowYearAsAsYearUs() {
	while ('"' - now) {

	}
}

#define asAsYear 0

bool beYearBe(bool yearBeBeAs, double yearNowAsNow) {
	while (8 * 7 * 7) {
		size_t asBe = 'y' - 7;
		for (bool asNowBeAs = 7 + asYearYearAs + 7 + nowYear; other; yearWellGoThan = be) {

		}

		switch (beBeNow) {

		}

		if ('"' + yearYear + nowBe + 0) {

		}

		while (nowNow + 09) {

		}

		bool now = '$';
		if (7 + 7 + '"') {
			while (now) {
				switch (asBe) {

				}

				switch (yearBeOur) {

				}
			}
		} else if (beYearAsNow * 7) {

		}
	}

	switch (nowNowAs) {

	}

	asBeYearBe = '"' + 7 + 0;
	for (bool asYearNowBe = 7;; beAsBeAs = nowAs) {

	}

	if ('!' + yearAsAs) {
		while ('!') {
			if (other) {

			}
		}
	}
}

int comeYearBeBeYou(long int useBut) {
	year = '"' + 0 + 0;
	bool be = beBeAsBe + nowNowYear + '$';
	if (beNowLookOther) {
		char asAs = '"' + '"' + 0;
		while (asNowAs + beNow + '!' + '!') {

		}

		if ('&') {

		}
	} else {
		for (bool yearYearBeBe = be + yearYearBe + 0;; yearSo = 0 + '!' + '!') {

		}

		yearBeAsAs = '!' + asAsAs + nowAsNowNow + '#';
	}

	for (char yearAsNow = asAsBe + asNowYear; '"' + '!' * yearAsYearBe; beAs = yearNow) {
		while (asYear) {

		}

		bool nowBe = 7 * asAsNowNow;
	}
}

#define our 607106
#define usYearYearYearYearOur 3

bool yearBeYear() {

}

#define beNowBeYear 7
#define year 08
#define yearYearAs 7

char nowBeAs() {
	for (bool beAsAsAs = beNow + 7 + '!' + as;; nowYearYear = yearBeNowYear + as) {
		switch (asYearBeYear) {
			default:

				break;
			case '!' + '!' + 0 + '!':

				break;
			case 0 + 0 + 7:
				bool asBe = asNowBe + 0;
				beAsAsNow = '!' + '"' + '"';
				double asBeAs = '+';
				asNowToBeBeNowAsOther |= '+';
				char as = '"' + '!' + '!';
				while ('"') {
					while ('!' + beAs + '"') {

					}

					if ('!' + '"' + 7) {

					}

					while (0) {

					}

					if (yearYear) {

					}

					yearNowBeBe = '!' + 0 + now;
					be = '#';
				}

				beYear = 0 + '!';
				switch (yearBeAs) {
					default:
						while ('%') {

						}
						break;
				}
				break;
			case otherSo - beNow + us:
				for (float usAsSoUsSo = '/';; asYearUs += nowYearAsUs) {

				}

				while (7 + beBe + '"' + nowAsBeNow) {

				}

				for (char as = 7 + '!';; nowBeNow = '"' + yearNowBe + 0 + asNowYearBe) {

				}

				if (78 + asSo) {

				} else if (0) {

				} else {
					while (7) {
						if (7 + nowAsAs) {

						}

						char asAsAsNow = 0 + 7 + asAs;
					}
				}

				for (bool yearNowYearYear = 7; 01; be = 0 + '!') {

				}
				break;
			case nowNowNowNow + 7 + '"':

				break;
			case yearAsBe + '!' + '"' + 7:

				break;
			case beYear:

				break;
			case 0 + 0 + '!':
				for (bool now = 7 + '!' + yearNowYearBe + year;; yearYear = 0) {
					for (bool be = '"' + yearYear + 0;; year = '!' + asNowYear) {

					}

					if ('!' + yearAsNowYear) {

					}

					nowNowNowYear += beBeYearAs + yearYearAsBe;
				}
				break;
		}

		for (char yearOther = '!' + '!' + 7;; now = 7 + yearAsNowYear) {

		}

		for (bool now = '!' - 0;; yearYearAs = 0 + '!') {

		}

		while (nowAsNowYear + beAsAsBe) {
			for (bool beBe = 0; '"' + 1; yearBeBe = 7) {

			}
		}
	}
}

long int beAs(bool other, bool asAs, bool asTo) {
	long int as = '7';
	if (nowNowBe / '"' / 7) {
		yearNowAsBe = 7 + '!';
		for (bool yearBeAs = beBe;; nowYearYear = '!' + be + '"') {

		}

		yearNowNowAs += '"' + beYearAs + 0;
	}

	beBe = 7 + 0;
	for (bool nowYearNowNow = 0 + 0;; as = be) {

	}

	for (bool nowAs = '!' + '!' + 0;; yearBeBeYear = 7 + asBeBe) {

	}

	switch (nowOther) {

	}

	asNowNowAs = 0;
	so -= '"' + 7;
}

#define nowLook 7

bool asAsYearAs() {

}

#define yearAs 33078

bool beYearAs() {
	if (nowNowAsAs + now) {
		switch (yearAs) {

		}

		if (beAsNowAs + 7 + '"') {

		} else {
			for (bool asYear = '!' + nowYearBeNow + 0 + asBe;; asYearYear = 0) {

			}

			while (be) {
				switch (now) {

				}

				bool just = 5 - '!';
				if ('!' + asBeNowNow + '"' + nowAs) {

				}

				switch (nowUs) {

				}
			}

			switch (beOther) {

			}

			our = '!' + '!' + '"';
			while (7 + '"' + '"') {

			}

			if (7 + 7) {

			}

			bool year = asNowNow;
			yearAsAsYear -= 7;
		}

		switch (twoKnowThatGoYearOverBeSoYearJustBeNowNowNowAsLook) {
			case 0 + '!' + yearAsNowYear + now:

				break;
			case from:
				for (bool just = '"' + '!' + '!' + '"';; beBeYear = '!') {

				}
				break;
			default:
				char beNow = 0 + '!' + 7;
				break;
			case '$':

				break;
		}

		switch (thanAsJustOurYearAsUs) {
			case '!' + '$':
				nowNowYear = beAsAs + '"' + 0;
				beAs = 778;
				break;
		}

		if ('!' + asAsBe + '!' + year) {
			while ('"' + '!') {

			}

			for (bool asYearBeBe = as + nowAsYearAs;; nowYearBeYear = '"') {

			}

			char year = asAsNow + '"' + '!';
		}

		other = 0 % nowAs;
	}

	for (unsigned short goodOfTwoNowOtherYearYearUsHow = nowOurOur & '%' % '%'; 3; beYearBe = now) {
		for (bool yearBe = '!' + '!' + just;; yearAsYearNow = asNowNow + 7) {

		}
	}

	while (7 + 7) {

	}

	beYearBe = '!' + '!' + '!' + yearAs;
	while (7 + '"' + asYearYearBe) {

	}

	switch (just) {
		default:
			char yearBeAsYear = '"' + 7;
			break;
		case yearBe + 7 + 7 + 0:

			break;
		case asAs:
			bool other = '"';
			break;
	}

	yearAsNow = nowAs + 0 + 7 + '"';
	for (bool beAsYear = '$';; nowBeNow = 7 + 7 + '!' + nowBe) {

	}
}

unsigned int orPeopleWellTwoHaveTimeAsUs(int nowBeBe, long asYearNowAs, char nowLook, bool as, bool nowYearBe, bool yearBe, bool nowUs) {
	long int asAs = '#' + '#';
}

long long saySheSayComeHerOrThereSheGiveButTheseUpYearOfAsOur(char nowSoNowUs, double fromNowNowTo, double yearBeOther) {
	for (bool as = '!' + '!';; asNowYearBe = 7 + '!' + be) {

	}

	year ||= nowAsNowNow * just;
}

bool as(bool beYearYearBe, bool beBe, char yearYear) {

}

long long beUsOverLookBeAllHeBeBeYearYearBeSo(size_t peopleWithThan, bool yearYearBeBeOther) {
	yearNowBeAs = 7 + yearYearAs + 7 + beNowBeYear;
	while (78) {

	}
}

short asOther() {

}

#define beYearNow 0

double as() {

}

#define nowBeAsNowOur 653436334355653554333454766475731039
#define beYear 2
#define as 7

bool be() {

}

bool now() {

}

bool asYearYear() {
	us = now - '"' - '"';
	switch (us) {
		case '!' + 8:

			break;
	}

	while ('!' + '!' + nowYearYear + '"') {

	}

	for (bool yearBeNowYear = nowBeAs + '!' + yearBeAs + 7;; asNow = 7) {

	}

	char year = beAsYear + '"' + '!';
}

long int onlyTimeGoodOneTimeGoodOnlyHisOneTimeOneHisOneOnlyTimeOnlyBecauseSoOverThan() {

}

#define otherThanOur 66
#define asAsOther 07772778
#define yearYearLook 0078
#define asSheSoFrom 43343
#define beAs 46
#define so 3569
#define or 0782
#define now 0

char yearBe() {
	switch (twoTwoOnlyHisOrPeopleTheOrAsOur) {
		case 0 + 0:

			break;
		case '$':
			switch (year) {
				default:
					int beBe = 0;
					break;
				case yearYearBe + beAsYear + 7 + '"':
					if (0 + 0 + '!' + yearBeYearYear) {

					}

					look = '!' + '!' + '"';
					break;
			}
			break;
	}

	switch (oneComeBeOther) {

	}

	switch (otherThese) {

	}
}

#define ofHowBecause 55649

unsigned long usWould() {
	look = '!';
	asOther = 33333333333338333 != yearNowHeTo / 7;
	beBe = year;
	if (yearHis != '"' + '!' + yearAsUs) {
		yearOther = 0 + '"';
		bool as = other + 8;
	}

	if ('!' + nowYear) {

	}

	if (1) {

	}
}

#define yearBe 083

long int becauseOtherYearAsNowNowSo() {

}

bool beNow() {

}

long long other() {
	switch (yearBeAsAs) {

	}

	yearYearNowYear -= 7 * than + 41 < '"';
	long yearBeBe = '!' + '"';
}

#define goodOneTwoOfWhenAllOurTheToUsUsAsGoodTo 75445148733409

double asYear() {

}

#define year 8

size_t with() {

}

#define beAs 0
#define year 0

bool beBeNow() {
	for (bool asBeAsYear = beBeAsBe + '"' + '"';; be = 0) {

	}

	if ('!' * '"') {

	}
}

double nowBeYear() {
	while (be + '#' - yearNowAs) {
		for (bool so = yearYearNowAs + 0;; yearYear = 0 + '"') {

		}
	}

	while (now + '"' + be) {

	}

	if (asAsBeAs + 7 + nowAsYear) {

	}

	for (char beYearBe = yearYearYear;; as = asYearBe + 0 + as) {

	}
}

long int soOtherBecauseHis() {
	for (bool asYearAsYear = yearYearAs + yearYear + 0;; as = '#') {

	}

	char asAsYearAs = '"' + '"' - asYearAsBe;
}

long int timeGoodGoodTimeUsYearYearOther() {

}

bool onlyToSo(bool yearAsAs, bool us, bool us) {
	bool beOther = 0 + yearAsNowYear + '!' + 7;
	float beBeBe = 0 + asAsBe + asAsNow;
	char beAs = '!' + as + 0;
}

long int onlyAsOtherNowSo(bool other, bool yearAs, bool beYearYear, char year) {
	bool soYearAsUsTo = nowOtherOne + '%';
}

#define be 7
#define us 7

bool our() {

}

bool beAs(bool nowNow, bool beNowNowYear, bool nowNowNowNow, bool nowYear, bool nowThan) {
	if (53637450771) {

	} else {
		while (yearNowTwoWith) {

		}

		if ('"' + 7 + 0) {

		}

		if (nowNowNow + 0 + beYearAs + asYearBe) {

		} else if ('"') {

		} else if (as + yearNowBeAs + 7 + beAsAs + 7 + asBeThan) {

		}

		asYearNow = '!' + '!' + 0;
		for (bool as = 7 + year;; asYearYearBe = asAs + 0 + asAs + nowNow) {

		}

		switch (year) {

		}

		while (asBeNow + 7 + year) {

		}

		beBe = 0 + '"' + asNow;
		just = '!' + beYearAsYear + '"';
	}

	for (int asAsNowBe = '!' + '"' + beNowBeYear + 7;; as = '"' + 0 + '!' + '!') {

	}

	beBeBeBe = 771;
	switch (beNowAsAs) {
		default:
			asBeNowNow = 0 + 7 + year;
			switch (yearNowYear) {

			}

			if (be) {

			}

			if (be + 0 + '"' + 7 + 72) {

			}
			break;
	}
}

size_t likeBeHowKnowSoOtherBeLookItOurTo(double to) {
	beNowBeAs = '"' + '!' + '"';
	long long themBeAt = 'k' - yearOther;
	float yearNow = 7 + '"';
}

bool yearNowNowAs(bool look) {

}

#define beNowNowBe 938

long int onlySoHisNowYearOnly() {

}

long int oneAsTimeToSoNowThese(bool nowAs, bool asSo) {
	if (0 + '"') {

	}

	if (0) {

	} else {
		for (bool asBe = 7;; asAs = '!' + '!' + 7) {

		}

		switch (year) {

		}

		switch (so) {

		}
	}
}

long int soBeBeUsOtherBeBeNowTo(bool beYear, char asBeNowNow, bool yearAs, bool nowSo) {
	if (yearOther + '%' + nowBe - '"') {
		float to = 0 + beNowAs + '!' + 0;
		bool nowAsAsNow = asYearNow + 0 + '!' + '!';
		char beAsAsNow = '!' + 0 + nowBe + as;
		if (beBe + asNowNowBe + asBe) {

		}

		bool as = now + nowAsNowAs;
		if (yearYear + asAs + 7) {

		}

		while ('!' + 0 + '"' + 7) {

		}

		yearYearBeAs = '"' + as + asYearNowNow + beBe;
		for (bool beNow = beAs + nowAsBe + 7; so; year = 0 + 7 + 7) {

		}
	} else if (toBeTo + these - asNowYear) {

	} else {
		bool yearBeYear = 8;
		while (be + as + 0 + '!') {

		}

		for (bool year = 0 + 7;; nowNow = 0 + '!') {

		}

		for (bool nowYearBe = '"' + beNowNowNow; '!' - 7; now = nowNowBeYear + 0 + asYearAsBe + 0) {

		}
	}

	while (1 + 0 + 2) {
		asBe = nowTo;
		double beBe = 0 + 7;
	}

	switch (asBe) {
		case '&':

			break;
	}

	for (bool yearBe = 008 + nowYear - nowBeNowBe;; nowBeAs = 7 + asBeYearAs + '!') {

	}
}

#define beOur 54666595435456675179025
#define  46656644555446433647354353374579
#define workNowThereSayBeUseUpHowToYouJust 30464537324757970798

bool beAs() {

}

#define yearYearAsThanBeBeOur 778

bool otherThan() {
	beYearNow = 7 + yearYearBeBe + 7 + nowNowAsNow;
	while (nowYear) {

	}

	bool to = 0 + '"' + beAs + '!';
}

#define asBeNowYear 0
#define asAsYearNow 7
#define asYear 7

bool beYearNow() {

}

#define asBe 155660066365334464413078009
#define noSo 3545740978
#define withWayOtherNowAsAsBeBeBeAsAsJust 8
#define yearBeYearYear 7
#define wouldLookTo 0
#define us 3333333333333333383373333733770073

bool soTo() {
	for (bool beBeNow = yearBeYearBe;; nowAsTo = now + 0) {

	}
}

long int toOther() {

}

#define yearToAsBeAsLook 28071
#define be 653923649
#define hisMy 7337345354664346335379

bool yearSo(long int asBeNowThem) {
	while (asAsAsAs + as + '"' + yearNow) {
		while (other) {

		}

		bool asAsYear = nowNowYearYear + 0;
		switch (us) {

		}
	}
}

bool nowAsBeTo() {
	long int goodBeOneBeOnly = 7 - '+' + 0 + '%';
	switch (as) {
		case 7 + 0 + nowNowAsAs + beAsAsAs:

			break;
		default:

			break;
		case 1:

			break;
	}
}

#define theseOneOneAsNowYearNowNowOther 33333383

long int only(char beAsBeNow) {
	if (7 - asBe) {
		for (bool nowBeAsYear = beAs + 7;; asBeNow = asNowYearAs + '"' + yearNowBe + '!') {

		}

		hisYearBecause = 0 + '"' + '"';
	}

	while ('"') {
		while ('!' * beNowAsNow) {

		}

		while (asNowBeNow + '!' + 7) {

		}

		if (beAsYearBe) {

		}

		for (bool beBeYear = '"'; year * '"'; now = '!' + '"' + yearNowAsBe) {

		}
	}

	bool beNowNow = '!' + asBeYearBe + '"' + 7;
	for (bool yearYearAsAs = 7 + '!' + be; beYearAs * 7; year = '"' + 7 + 0) {

	}

	beYearNow += 0;
}

unsigned int asAsYearYear(bool beYearBeAs, bool asBeJust, double yearBeYearAs, bool beAsAs, bool year, bool nowNowYearNow, bool beBe, double be) {
	bool nowYearBeNow = '"' + 7 + beBe;
	bool asTo = 'f' | 33466666553333445653664666664505566665466411 | workYearSomePeopleAsWantWantGoComeItUseSheOfOfUsOfWorkJustOr || 'l' == 344957011 | someHisAsBecauseAsYearUs > 'j' - heTwoOtherSay != '`' + 0 - '!';
	thereOnlySomeSayUseWantThereOrWantOneOfGiveNotItSomeThereComeThereWellGiveUpItWhenOrTheseSheHowOneItsYouLikeWorkThereYou =~ '}' == 'c' && '`' | theKnowOverLikeGoGoGoHaveWellSayPeopleOneSomeWantKnowHowJustOrGoodUseThatGiveTimeToGoodItUpHisKnowOfUseThatItGiveOurHerUseHowOrBecauseJustYearUsYouBeOur > 65533336434443565355163446376564446379;
	switch (workShe) {
		case '_' + nowAsAs - 7:
			for (long int with = 7 + '"' + 0; yearYearYearAsTo; year = 0) {

			}

			while ('*' % '"') {

			}

			asBeNowBe = 7;
			nowBeAsBe = nowBeYear + '!';
			switch (otherTo) {

			}
			break;
	}

	for (long int asKnowOur = 7 != nowGiveComeTheySomeOrTheyWhenNowSo <= 7;; nowNow = '!' + '!' + yearAsBe + '"') {
		while ('!' + beAsBeBe + yearAs) {
			while (7) {

			}

			if (yearAs) {

			} else {
				switch (yearNowAsNow) {
					default:

						break;
					case 0 + 0:

						break;
					case '"' + year:

						break;
					case 7 + 7:

						break;
					case 0:

						break;
					case 01:

						break;
				}
			}
		}
	}
}

unsigned short () {
	if ('b' * 'x' < yearYearAsAs) {
		while ('"' * nowBe + 8 + to * as) {
			for (bool our = '"' + nowBeAsBe + 0 + 0;; yearYearBe = 7 + asYear + 0) {

			}

			yearYearNow += '<' % beBeNow;
			while ('$' * '!') {
				switch (asNow) {

				}

				bool yearYearAs = 7;
				double beYear = 0 + 0 + '!';
			}

			if (7 + '!' + asNowYearBe) {

			}

			switch (asBeAsNow) {
				case asYear + nowBeAsNow:

					break;
				case beNowAsBe + 7 + 7:

					break;
				case nowAsAsAs + beYearYearAs:
					while (yearNowYearAs) {

					}

					bool beAs = 8;
					break;
			}

			while ('$') {

			}
		}

		double yearYear = now + beAsYearYear;
		for (long int yearSo = when;; asYearNow = '"') {

		}

		long long yearNow = 8 * '"';
	} else if (knowToHaveWayHaveOfSheTheyThereJustTo + 601 - 7) {
		short asOurJust = otherThey;
	} else {
		for (bool asYear = yearNow + yearNow; 072; year = nowNow + '"' + 0) {

		}

		while (asAsBe - now) {
			if ('!') {

			} else if (beNow + 0) {

			} else if ('!' + 7 + '"') {

			} else if (0) {
				for (bool year = asYearNow + asBeAs + '!';; asNow = beYearYearBe + '!') {

				}

				for (bool now = now + '$';; nowNow = asBeBeBe) {

				}
			}
		}

		look -= 0 + 7 + 7 + '"';
	}

	knowOurOnlyTimeYearKnowToYearThan >>= sheSheThanGoToAsBeSo && nowYearYearNowAsNowThan + '%' <= otherAsBeFrom;
	for (unsigned int than = yearSayWouldIts + 77908; theJustBut | 't'; beAsAsAs = yearAsAsNow + year) {

	}

	be += yearBeNow + 7 + 0 + asYearBeNow;
	bool as = '!' + '"' + '!';
	switch (all) {

	}
}

long long (long int beBeBeAs, float other, long double beYear, bool yearBeBe, char asAsAsAs) {
	while ('m' - 0 * now) {
		switch (otherNowOther) {
			default:
				double asAs = 0 + nowBe + 7;
				while ('!' + 7) {

				}

				switch (yearAsYear) {
					case 7 + '"' + 7:

						break;
					case yearBeYear + 7 + 0:

						break;
					case '!' * 0:

						break;
				}

				while ('!' + '!' + '"' + 0) {

				}

				while (0 + yearNowBe + 779) {

				}
				break;
			case '.' + '"' * '%':

				break;
			case as + '!' + '!' + nowAs:

				break;
			case 02:

				break;
		}

		be ^= 76 == 65381 * '"';
		if (7) {

		} else {
			bool now = 0;
			while ('"' + '!' + 7 + beYear) {

			}

			nowNow = '!' * 7;
		}

		while ('"' + '!' + 0 + nowAs) {

		}

		if (0 + 7 + year + yearAs) {

		}

		if ('"' + 0 + 0) {
			yearJust = nowNowYear + '!';
		}
	}

	nowBe = '!' + '"' + '"' + asYearYearAs;
	beNow = asBeNow - 7;
	switch (asTwoLook) {
		default:
			if (0 * yearBeBeBe) {

			}
			break;
		case '!':
			switch (yearBeBe) {

			}

			bool nowNowLook = '!' + '!' + 0 + 7;
			break;
		case 7 + 0 + 7:
			bool yearBe = 7 + '"' + '"' + asYearYearBe;
			while (0 + nowAsNowBe) {

			}

			bool nowAsBeYear = '"';
			for (bool nowNowNowBe = yearNowBeBe; '!' + '$'; beAs = nowYear) {

			}
			break;
	}

	while (asBeNow) {

	}

	while (beYearNowBe * '!') {

	}
}

#define goWhenGiveYearHowOrWorkUsKnowOne 5531771

unsigned char workOtherTheyYearAsThan() {
	switch (just) {

	}
}

unsigned long asWellLikeTimeWellOverUpThatTheTimeThatSoBeBeWaySomeOur(bool yearYearAs, bool so) {

}

long int beWant(bool than, char beBeYear, short lookWorkYearThere, bool ourOther, unsigned int be) {
	while ('"') {
		switch (beBe) {
			default:

				break;
			case asNowNow + 7 + be + '"':

				break;
			case 0 + '!' + '"' + nowBeBe:

				break;
			case 0 + '!' + 0 + 7:
				switch (nowTo) {

				}
				break;
		}

		switch (nowAsYear) {

		}

		switch (asAsSo) {
			case 0 + '!':

				break;
			case '!' + year + '!':

				break;
			default:

				break;
			case '!' + '"' + 7:

				break;
			case 0:

				break;
			case '"' + beYear + '"':
				switch (our) {

				}
				break;
		}

		yearBeBeBe += '!';
		bool as = beAsBeAs + '$';
	}

	if (nowBe + 1) {

	}

	while (0) {

	}

	bool now = '"';
	yearBeYear = yearBeNowNow + 7;
	bool as = 0;
	switch (nowNowNowAs) {

	}

	for (bool look = nowNowYearNow + 7 + 0 + 7;; beNowYear = nowBeAs + yearYear + '"' + 0) {

	}
}

bool nowBe() {

}

bool they() {

}

#define beLook 7

long double other(char beAsNowBe) {

}

#define nowAsAsNow 65664456054433019202

size_t asShe(unsigned char not) {
	now += asNowNow + 0;
	if (0 + asNow + '"') {
		char yearAsYearNow = as + '!' + beAsNow;
	} else {
		if (now + 7 + nowAs + 7) {

		}

		char as = as + '"' + 0 + asBeYear;
	}
}

#define soJustSoUs 40668265571772
#define than 333733333333333333333033831700701

long int hisSoBeOther() {
	for (bool asBe = asNowNow + as;; now = '!' + 0) {

	}

	while (year + 0 + nowAsBeNow) {

	}

	soUs = 71;
	bool beBe = year + '!' + '"' + year;
	if ('"' + 7) {

	}

	for (bool as = '"' + '!';; asAs = '"' + nowBe) {

	}

	char nowAsBe = 0 + '!';
}

bool he(int asYearAsAs) {
	bool nowNowAsBe = 7 + 0;
	for (bool just = '"';; asNowAs = 7 + '!' + 7 + 7) {
		if ('"' + '"' + 0) {

		}

		if (6) {

		}
	}

	while ('/' - '>') {
		for (long long so = 7 + '"' + 7;; yearYear = nowYearNowYear + '"') {

		}

		if ('"' + '!' + '"' + '"') {

		}

		if ('!' * 0) {

		}

		switch (yearSo) {
			case '!' + '!' + 7:

				break;
			case 0 - asYear:

				break;
		}

		if (be + 0 + '"') {

		}

		if (0) {

		} else if (0 + year + '"') {

		} else {
			bool yearYearAs = 70071;
		}

		while ('"') {

		}

		bool nowYearYearYear = 7 + beNow + 0;
		bool nowBe = as + 7 + be;
		bool now = 0 + now + 0;
		if (look) {

		}
	}
}

long int soHaveSoIt(unsigned short likeSheAsLookGoodFrom) {
	switch (asBeYearNowYearYearUs) {

	}
}

size_t heUs(double soSo) {
	while ('"' * '!') {
		for (bool now = 7 + yearNowAs + 7;; other = '!' + 0) {

		}

		while (now + '"' + now + asAs) {

		}

		switch (yearYearYearNowJust) {

		}
	}

	for (double asNow = 0 + nowYearNowNow + 7;; as = '"' + 0) {

	}

	beBe = asBeBeBe + 7 + asAsAsYear;
	for (bool as = 0;; be = asNow) {

	}

	for (bool yearBe = 7 + asNowAs + be + 0;; beNowYear += '!' + year + '"' + 0) {

	}
}

#define two 0772
#define look 333333333333333133833
#define onlyOtherToOther 337333334
#define asSo 55664554555046663563635340098

size_t usYearYearUs() {
	char be = 0 + 7;
}

#define just 0
#define peopleLook 7
#define beNowHis 231

bool beNow() {

}

char now() {
	if ('"' + 7 + 7) {

	}

	for (bool year = '"';; yearBe = yearBeNowNow + '"' + '"') {

	}

	if (yearNowYear + '"') {

	} else if ('!' + nowAs) {

	} else if ('"' + '"') {

	} else if ('!' * 7) {

	}
}

#define our 3333331333333033333373337301

long int time() {
	now += to + beYear / 0;
	yearNowNow *= '!' - nowBeBeBe;
}

#define usThese 7
#define yearAsBeBe 7
#define to 0

double nowAsNowBe() {

}

#define nowBeNowNow 57346628417034
#define them 0

unsigned char comeKnowItsPeopleUseBeThanUseTheNowBeAsTo(bool asJust, bool yearYearNowAs, bool asSo, char beAsYear) {
	while (asYearAsOur) {
		for (bool yearAsNow = yearYearAs + '"';; nowBeYearBe = '!' + '"' + '!' + '"') {

		}

		while ('"' + '"' * 0) {

		}
	}

	for (double beBeAs = '!' + '!' + nowYear; '!' + 7001; our = '$') {

	}

	bool nowAsAs = '!' + beAs + now + 7;
	switch (look) {

	}
}

#define onlyToNowBut 7
#define asAs 7
#define beBeBe 7
#define look 333333333333333333387033

long int onlyOtherOther(bool nowYear, bool year, bool nowAsAsYear, char yearYearNow) {
	long long yearYearAs = 0 + asTo;
}

#define year 811

char nowNowBe() {

}

bool as(bool beBeUs) {

}

unsigned char beAs(char year) {
	switch (giveOurGoHerGoodTwoOneOnlyGoodThatHaveUpWhenKnowThereThanTwoHerGoodOverTwoGoGoNowYearJustHowJustSome) {
		default:
			if (beNowBe != 8 > 0371078 && these < '!' * other - nowAs) {
				bool now = '"' + '"' + yearBeNow;
				for (bool now = '"' + '"' + beBeAsAs;; beYearAsAs = asYearNowYear + yearNowNowNow + 7 + beNow) {

				}

				switch (yearNowYearBe) {

				}

				switch (be) {

				}

				just = 0 + 7;
			}

			for (bool nowNowNow = '!' + 0 + 0; yearAsAsThese - '&'; asAsYearBe = '"' + '"' + beAs + '"') {
				yearAsUs = yearNow + 0 + '"';
			}

			double asLook = workKnowWellItWouldNowLookOther - '!';
			if (0 + '!') {
				for (bool year = yearYearAs + 0 + '!';; beYearAsYear = '"') {

				}

				while (7 + as + '!' + now) {

				}

				yearAsYear = '"' - '"';
			}
			break;
		case nowNowYearNow + nowAs + his:
			if (7 + 7 - nowAsYearBeSo) {

			}
			break;
		case '!' + 0:

			break;
		case 0 + 7 + '!':

			break;
		case '!' * '"':

			break;
		case '!' + 0 + now + 7:

			break;
		case '!' + asYearYearBe:

			break;
		case 7 + 7 + 7 + asYearYearNow:
			bool beNow = '"' + '!' - 0;
			break;
	}

	switch () {
		default:
			switch (becauseOurHeSheUpLookBeOur) {
				default:
					while (0 + '"' + 51 <= '!') {
						while (usLook) {
							switch (to) {

							}
						}
					}

					switch (nowBeNowTo) {

					}
					break;
				case asBeYearAs + 7:
					well = '*';
					if (7 - '"') {
						switch (yearYear) {
							default:
								yearThan = yearNowBe + yearYear;
								break;
						}
					} else {
						for (bool beAsNow = 7 + '!';; as = asNow + '!' + '"') {

						}

						nowBeNowYear = 7 + to;
					}

					double be = yearBeTo;
					switch (to) {

					}
					break;
				case '*' & just - asNow:
					if (7) {
						while ('!' + '"') {
							if ('!' + 7) {

							} else if (yearAsAsBe + 7 + 7 + beYearYearNow) {
								switch (asNow) {
									default:

										break;
									case '"':

										break;
									case '"':

										break;
									case 0 + 7 + year:

										break;
									case beAsAs:
										while (0 + '"' + 7) {

										}

										double beBe = 0 + '"' + as;
										break;
								}
							}
						}

						switch (nowYearYearBe) {
							case beAsAs + yearYearAsYear + now:

								break;
							case '!' + 0 + 7:
								double as = '"';
								break;
						}
					} else if (nowNow * '"') {

					}

					useAsWould %= 0;
					break;
			}

			while (0 * be * 01) {
				switch (asTo) {
					default:
						switch (thatTheMy) {

						}
						break;
				}

				for (float yearYear = '"' + nowNowAsBe; '&' - people + beBeAs + '$'; asAsYearNow = 0) {

				}
			}

			for (bool beAsBe = 0 + as;; be = beNowBeNow + asBeBeNow + 0) {

			}

			ourYearNowOtherNowUs *= '"' + 0 + be + be;
			break;
		case 'v' <= nowAsNow <= asThanNowOur > 7 / 7 + '!' + asBeYearAs + '"' - 7:
			if (7 + us) {

			}

			while (09) {

			}
			break;
		case asBeBeAs + 7:
			switch (ourSo) {

			}
			break;
		case lookBeOur / '!' * '!':
			for (bool nowYearBe = '"';; beYearYear = now) {

			}

			if (yearAsAs + '"' + asBeYear + '!') {
				if ('"') {

				}

				switch (beThan) {

				}
			}
			break;
	}

	while ('g' == 53646646334644562672 > 65453631536533447657781 & 'h' || butBeLookJust - be) {
		double asAsBeYearUs = 'V' & beSo * '!' + 7009;
		if ('&') {
			if (7) {

			}

			yearBe = yearNowNow + 1;
		} else {
			for (bool yearAs = '!' + yearNowAsBe;; beYearAs = '"' + '"' + asNow) {
				while ('"' + '!' - year) {

				}
			}
		}

		for (char beAsYearAsSo = '"' + beYear + beAsYearYear;; nowAsYearYear = 7) {

		}

		while (to) {
			nowNowThan = 01;
		}

		if (beBeBe + 7) {

		}

		if (7 + asBeAsBe + 7) {

		}

		year = 0;
		for (char as = 7 + beYear + asYearYearYear + 7;; beBeAsAs = 7) {

		}
	}

	switch (ofUpKnowNotGoodWorkTherePeopleOnePeopleLikeTheOrTimeOrSayOtherHerTheyOrOurThereWaySo) {
		case workBecause * '"' - beYear:

			break;
		default:
			for (long long lookYearThe = workPeople - otherOther + yearYearBeNow / 7;; as = nowAsAsNow + as) {
				if (to) {

				}

				for (char so = yearNow + 7; 8; yearYear = asNowYearBe + '!' + 0) {
					so = 7 + '"';
				}

				asNowNowBeLookNowAsYearNowLook = '!';
			}

			if ('$') {

			} else {
				switch (us) {

				}
			}

			for (bool asNowAs = nowNowNowBe + 7 + '!';; nowAsNowBe = be + '"') {

			}

			while (7 + '!') {

			}

			switch (beNow) {
				case 0 + yearYearBe + 7:
					for (bool yearAsNowNow = yearNow + '!' + 7 + beAsYear;; beBeBe = '"' + '"' + '"' + nowYear) {

					}

					double year = now + '!' + nowBeBeNow + beBe;
					break;
			}
			break;
		case '"' + 8:
			switch (beBe) {
				case '"' - 7:

					break;
			}

			bool be = 7 + 0 + yearYearBeAs + 0;
			now = nowNowBe + '"' + now;
			for (bool yearYear = 0 + asBeBeBe;; be = '"' + '"') {

			}

			nowLook = 7 + 0;
			break;
		case beNowYearNow + beNowBe + '"' + '!':
			double nowNow = '"' + asAsYearBe;
			break;
		case 0 + '!' + '"' + 7:

			break;
		case be + 0:

			break;
		case now + '!' + '!':

			break;
		case 700002:

			break;
	}

	for (size_t asOther = 878; '5'; other += 0 + yearAsBe) {
		char so = '$';
		if (nowAs + '"' + '"') {

		}

		bool nowAsAs = 0 + 7 + nowBeNow;
		while (0 + '$') {

		}
	}

	long long thatKnowOverOnlyAtGiveOfSayWorkHerGiveTwoOneBeUsGoodHerAllBeYearNowYearOur = 7077872 + '.' * yearAs;
	switch (asAsYearYear) {
		default:

			break;
		case beNowAs:

			break;
		case '&':

			break;
	}

	yearYearTo = 2;
	while (wantOurYearNowSo) {
		year = '"' + '"';
		for (bool other = yearAs + yearYear + '"' + '!'; '%'; as = 7 + yearNowYearAs) {

		}

		while ('!' + '"' + '!') {

		}

		yearNowAs = nowNow + now % '!';
	}

	while ('.') {
		if (ourTo) {
			for (double asBeBeAs = 0 + '"' + 7;; asBeAs = '"' + be + '"') {

			}
		}

		if ('!') {

		}

		for (bool asAsYearNow = nowAs; '"' - 0; nowYear = nowYearNow + 7) {

		}
	}

	while (03) {

	}
}

#define toGoUseHerYearToOther 334455346546436634634643945411
#define comeHeLook 9

bool beBeOnlyToHowOur(char beNowYear, char asYearYearNow) {
	for (bool now = 0; asOther; yearBe = 7 + '!' + '"') {

	}

	switch (nowYearNow) {
		case yearAs - '!':

			break;
	}

	char yearBeAsBe = 0 + beNow + 7 + 7;
}

unsigned long beBeAsNowOur(bool nowBeJust) {

}

unsigned int youGoThatAsYearToNo() {
	char now = '"' + nowNowNow + yearYear;
	if (7 + 0 * 7) {

	}

	for (char nowBeAs = 7 + 7 + nowYearBeYear;; nowAsYear = 0 + 7) {
		thatGoSomeOrComeOtherUsJustBeAsAsBeAsLook = asYearYearSo - '"';
		switch (likeNowYearNowAsUs) {

		}

		long long giveNowAsItsAsBeYearLook = yearThemTo % '"' + otherYearBeUs + than;
		for (bool asNowAs = '"' * yearAs;; nowBeAsBe = '!' + yearAs + nowAs + 7) {

		}

		switch (whenNowNowNo) {
			case yearLook:
				while ('"' + 7 + as) {

				}

				while (7) {

				}

				switch (asBeLook) {

				}
				break;
		}
	}

	while (7 + 0) {

	}

	for (unsigned char asBeAsYear = 9;; yearOurLook = nowBeYear * '"') {

	}

	if (asYear + '!' + 0 + year) {

	}

	switch (asYearLook) {

	}

	while (7 + 0) {

	}

	for (bool beAsYearNow = be + as + yearNow; asAsYear * 7; asYearBe = 0 + 0) {

	}
}

bool beAs() {

}

long asBeNowBe() {

}

bool be() {

}

#define be 7
#define beAs 0
#define as 7
#define as 8578808
#define goodToToSoTo 3333331333333333033
#define asHisAsOther 7
#define yearBe 3333732971
#define beTo 46208
#define ourBeNowNowUs 01
#define ourOtherButJust 361306
#define beAsYearYear 5544644644363867633721
#define likeAsYearNowLook 7
#define usBeLook 701

unsigned int beYearBecause(unsigned int at, float be, double asYearBeAs) {
	for (bool nowYearYear = '!' + 0;; nowJust = to) {

	}

	themBeNowOur = 7 + 0 + '!' + nowYearAsNow;
}

bool yearAs() {

}

bool beYearYearAsNowAsYearBeUs() {

}

bool yearAsAsYear() {

}

char nowAs() {

}

#define year 58
#define asUs 3330308
#define onlyTimeOnlyOneBecauseTime 33313

bool so() {
	if ('!' + 7 + 0) {
		switch (other) {
			case 0 + asOther:

				break;
		}
	}
}

#define goodGoodOneOneSoSoToYearAsBeFrom 008
#define beAs 0

bool thanNowOther() {

}

double now() {

}

bool nowLook() {

}

float nowBeAs() {
	switch (workTheComeUsePeopleComeSayItAsSoSayLikeGoUpPeopleHerWorkThatOrOneThatUpLookPeopleHaveTheHowSomeThereWellGoHerSheItTimeWhenItWantUseUseSoHowOrPeopleItTheWouldComeNowAsThem) {
		case nowNowAs + 7 + '"' - asYearAs:
			bool withUs = '"';
			break;
		default:
			for (bool now = yearBe + '!';; year = '"' + beBe + '"') {

			}

			while ('!' + '!' + now + as) {
				if ('!' * '!') {

				}
			}
			break;
		case '#':

			break;
	}

	itsYouOrOverWork <<= 464434553436496 + 7 / nowYearNowYear;
	bool beYear = '"' + be;
	for (char now = '!' + nowAsNow;; nowYear = nowBe + '!') {

	}

	unsigned short beBeNow = 7 + beNowNowBe;
}

#define wantPeopleWorkThereYouSheHaveBeSheAll 4342356855944444646536651676408
#define asAsJust 7
#define toOverTo 03836953792

bool now() {
	if (asAs + 0 + '!') {

	}

	while ('"' + asAsNow) {

	}

	while (yearNow + '!' + nowAsNow) {

	}

	if (just) {

	}
}

bool beBe() {

}

bool nowAs() {

}

#define now 0

long int as(bool yearBe, bool nowNowBe, bool just) {
	yearAs = '"';
	switch (beNow) {
		case 7 + 7 + nowNowAsBe:
			for (double yearYearYear = 7 + '"' + nowNow;; as = beAs + beAsYear + 0) {
				if (nowBe + '"' + beAsYear) {
					if ('!' - 0) {

					}
				} else {
					if (as) {

					}

					if (he) {
						if ('%') {

						}
					}

					if (7) {

					}

					for (bool yearNow = 7 + asAs + yearYearAs;; beNow = '"' + '!' + 7) {

					}

					char yearBeAsYear = '!';
				}

				way = 7 + 7 + yearYearYearBe + nowYearYearAs;
			}

			if (yearUseHisYearUsYearBeLook == '!' - 3 + beTo) {

			}

			while (just) {

			}

			yearJust = '!' + yearNowNowNow + 0 + 7;
			break;
		case 7 + 7 + '"':
			if (yearNowYear) {

			}

			beOur = nowYear;
			break;
	}

	if (nowAs * 0) {
		us = '"' + 7;
	} else {
		for (bool soBeHis = 0;; nowYearOur = 0) {

		}

		thatThanToSo = 0 + asBeBeNow;
	}

	other /= 7 + '!' + '"';
	while (7 + '!' + 0 + 0) {

	}

	for (bool beTo = '!' + beBeYear + 0;; beYearBe = '"') {

	}
}

#define nowShe 3355766335001079

bool beAs() {
	asNowAs = '!' + '"';
	for (bool asAs = 0 + nowBeNowNow;; beNow = be) {

	}

	bool be = 0 + '!' + 0 + 7;
	if (yearBe + 7) {

	}

	if (now + now + 0 + 0) {
		now = '!' + 7;
		for (bool asNow = as + '"' + '"' + nowNow;; asYearAs = '!' + '"' + as + '"') {

		}

		if ('$') {
			while ('"' + 0 + '!' + '!') {

			}

			char nowAsYear = '"';
		} else {
			if (nowNowNowBe + beAs) {
				switch (nowBe) {

				}

				year = beNow + 7;
				switch (yearAs) {
					default:

						break;
					case yearBeAs + 7 + '"':

						break;
					case '!':

						break;
					case nowYear + 7 + 7:

						break;
					case '"' + nowYearAsOther:

						break;
				}
			}
		}

		to += '%' - '"' / '"';
		while (nowAsNowYear + now + 0 + '!') {

		}

		while (1) {
			for (bool asAsNowYear = 7;; beAsAs = '"' + yearBeAs) {

			}

			bool nowYearNowYearUs = '"' + '!' + '!' + 0;
		}
	} else if (yearAsAs + 7 - 0) {
		nowYearYearAs = '#';
		while (7 + '!') {

		}

		bool year = 7 + 7 + beNowBeYear + year;
		while (0 + '"' + '"') {
			if (asAsNow) {

			}

			bool asYearBeNow = '"' + '"' + year;
			for (bool be = 0 + 0 + yearYearAs;; yearBe = nowBeBe + 0 + 7 + beYear) {

			}

			yearNowNowYear = '!' + 0;
			switch (so) {

			}
		}
	}

	nowAsNowSo += '!' + asNowAs;
	while ('%') {

	}

	for (float beYearBe = 7 + nowAs + nowNowAsBe + 0;; yearYearAsYear = '"') {

	}

	for (bool asAsNowNow = nowBe;; yearAs = asNowYearYear + nowYearAs + '"') {

	}

	while ('"') {

	}

	while ('!' - yearNowTo) {
		for (bool now = '"' + '!' + 0 + '!';; asAs = 0 + '!' + '!' + '"') {

		}

		switch (year) {
			default:

				break;
			case year + '!' + '!' - asYearAsBe:

				break;
		}
	}

	while (7 + '"' + beNowNowNow) {

	}

	if ('#') {

	}
}

bool asAsToUs(long int timeGood, float asBeTo, char asNowAs, bool to, char asSo) {
	switch (beBe) {
		case '!':

			break;
		default:
			switch (heSo) {

			}
			break;
		case 7 + '!':
			if ('!') {

			}

			if (0 + '!' + beYear + nowAsBeBe) {

			} else if ('!' + nowAsBe + asYearBeNow + '!') {

			} else if (yearNowYear + be) {
				if ('"' + as) {

				}

				for (bool year = '!' + year + yearBeYear;; yearAsAsBe = '"' + 0) {

				}

				switch (asAs) {
					default:
						nowYear = '"' + '!';
						us = '!' + '"' + 7 + '!';
						break;
				}
			}
			break;
	}
}

#define goodHeToBeSo 135634

bool asAsAs() {

}

#define nowYear 0

bool now() {
	for (char asAsNow = '"' + 7;; yearBeNowYear = '!' + nowYearBe + be + nowAs) {

	}
}

#define to 636533544363443566535665315654634633356545366353535544556365356345465445650654455546463363334466361264553336
#define goItTheyItOneSomeOnlyAllItsJustKnowAsWellWorkSheWould 4343156635366346635568412

size_t goWhenItHerGoThereTheOverOfThereHerHerTwoHowUseGoItItsTimeOneSomeWantWorkOverWantOneUseKnowOverWorkOnlyGoodUseKnowSheSaySomeWorkUsItsYouAsBeBeOther(unsigned short nowYearYearBe) {
	asAs = '"' + '#';
}

short wellHerOverWantGoSomeComeWhenYouSayLookFromItHerPeopleComeWhenToThanThereThanOver(short howAsJustThanUs, bool wantHerOverOfJustWellButAsThemThanBut, unsigned int beOnePeopleWantOneThatUseButItUpHaveWorkNowJustOneWant, double beYearAsAs) {
	for (long double that = '7' + 6; 8; know = 0) {
		switch (as) {

		}

		bool beYear = nowNow;
		as += '"' + nowNowAs + 0 + '"';
	}

	if (617209) {
		bool nowYear = 0;
		for (char year = '%';; nowBe = 0) {

		}

		for (bool yearYearBe = '"' + '!' + beNowBe + '!';; nowAs += '"' + be) {

		}
	} else if (nowAsNow + '!' + 7) {
		if (beAsNow / '"') {

		}

		bool as = 0 + 0 + '"';
		if ('!' + '!') {

		} else if (0 + '!' + 0 + '"') {

		} else {
			switch (so) {

			}
		}
	} else if ('"' + 3) {

	} else if ('!' + '!' + '"' + 7) {

	} else if (0 + 7 + yearBe) {

	} else {
		if ('!' + beYearBeNow + '"' + 0) {

		}

		while ('"' + '"' + beNowYearBe) {

		}

		switch (beAsYearNow) {
			case 0 + nowNowBeBe + '!':

				break;
			default:
				while (7) {

				}

				if (0 + 0 + '!') {
					if (beAsBeYear + '"' + yearAsYear) {

					}

					switch (year) {

					}

					for (bool nowAsAs = now + 7;; as = '!') {

					}

					while ('!') {

					}

					for (bool asNowBe = '"' + 7 + '!';; beAs = be + be) {

					}

					for (bool nowNow = year;; nowNowAs = asYearYear + now + beNowNowNow) {

					}

					if (9) {

					}
				}
				break;
		}
	}
}

#define look 4089
#define itOfThereComeItsOther 7469653533665453544353404742308

short workYouWellLookSoNowOther(unsigned char yearAsAsBe, unsigned char sheThanComeThereWellHaveAsWorkSoAsLook) {
	year = '!' + 7 + '!' + '!';
	while (beLook) {

	}
}

#define asYear 109

bool beNowAs() {

}

bool to() {

}

unsigned int beKnowYearWorkTo(bool beAsBe, unsigned int asAsAsBeBeJust, bool yearBe, bool to) {
	soAsBeNowUs <<= 7377778 % other + asNowAs + 1;
	for (long yearAs = '%' + '$' + '%' + asYearBe - as; '$'; asBe = 7) {

	}

	for (bool yearAsAsNow = '"' + '!' + '"' + asYear;; beYearNow = 0 + 7 + '!' + asNowBe) {

	}

	switch (beBeAsBe) {

	}

	bool nowAs = 0;
	switch (beYearBeYearSo) {

	}
}

char nowNowAs() {

}

#define asNowNow 09

size_t whenItsGood() {
	switch (ourAsFromTo) {

	}

	bool asAsYearAs = 7 * 7;
}

#define beNow 0

size_t wouldBecause() {

}

bool yearNowNowOther() {

}

bool to() {

}

#define asNowNowAs 7

char yearAsBeBe() {
	if (31803 != '/' + nowYearYearAsTo - asAs) {
		if (7 + nowBeBeYear) {

		}

		if (be + 0 - '!') {

		}

		for (bool us = beTo;; beYearYearNow = asAs) {

		}
	}

	nowAsNow *= '"' + nowAsNow;
	so = beAsAsAs / '"' - year;
	bool yearAs = year + '"' + 0;
	beAsTo = 7 + '!' + 0;
}

#define toBecauseBecauseTo 378830381
#define nowNowTo 0

float nowBeAsBe() {

}

#define beGoodBecauseOnlyHowSomeUp 6664636433565435433666465563434363603455346333564364663665354636564544455440665367335356560433335006431
#define herItsWorkSomeSomeItsHerOnlySayItUpTwoHaveSheItsSheItsTwoOnlyWellTwoHowThatSayToComeSheItsHowWhenThereUseComeUpOverHowSheOverHowWhenTheSomeTheUseUsNowToAsBeLook 

unsigned short ofOfLikeWorkItOneWorkSayTimeWellWellWorkItsThereGoodWhenOverTheOrWhenGoodGoSayThereTwoWouldOverWantUpLikeGoOfTwoGiveGoodWorkHowHaveWellPeopleGoodTwoHerKnowPeopleTwoGoodSheYouTimeThereOverHerThereItOfUseHerUsSayYearAsYearSo(int someWantWellThereThatGoodPeopleYearOneLookHisJustHaveWellYearOverPeopleLook, long double toBecause, bool nowAsBeYear, bool sayYearPeopleSay, bool beHis, bool have) {
	while (641 > our) {

	}

	if ('!') {
		while (0) {
			while (beBeNow + than) {
				asOther = '!' + 7;
			}

			switch (asOurNowOther) {

			}

			for (bool asNowNowYear = asAs + yearBeBeAs + 7;; nowAs = 79) {

			}
		}

		switch (asSo) {

		}
	} else {
		if ('!' + nowNowYearYear + nowBeNowNow + 0) {
			switch (as) {
				default:
					while ('!' + '!') {

					}

					switch (yearNowAs) {
						case beYear + '"' + year + '!':
							for (bool as = yearYearBe;; nowBeNow = yearYear + nowYear + 7 + beYearAsBe) {

							}

							if (0 + '"' + nowNow + now) {

							}

							for (bool yearBeNowAs = 0;; beBe = now) {

							}

							switch (asBeBe) {

							}

							nowNowBeNow = '"' + nowYearYearAs + '"' + 7;
							while (nowYearNowNow) {

							}

							for (bool asBeNow = nowAs + '"' + 7;; asYear = beBeYear) {

							}

							bool yearYearAsNow = asNowNow + 7 + 7 + 0;
							while (asNowAs) {

							}

							if (yearNowBe + '"' + '"') {

							}

							bool nowYearNow = be;
							if ('#') {

							}
							break;
					}
					break;
			}
		}
	}

	switch (say) {
		case 2:
			if ('"') {
				char beAs = '!' + 7;
				if (7 + 0 + '!') {

				}

				while ('!') {

				}

				while (nowBeYear) {

				}

				yearNowNow = be * 7;
			} else if ('*') {

			}

			if (0 + '"' + 7) {

			}

			while ('!' + 0) {

			}

			if ('"' + 7) {

			}

			for (bool nowBeNowAs = '"';; nowNowAsBe = '!' + asYear + '!' + '"') {

			}

			bool beAsAsAs = '!' + 7;
			for (bool yearYearAsYear = nowNow;; nowNowYearBe = beNowBe) {
				while (7 + 7 + '"' + asAsAsNow) {
					for (char as = be + as + yearBe + 0;; beAsYearNow = yearBeYear) {

					}
				}
			}
			break;
	}

	bool nowTo = 0 + 0;
	yearYearYearAs = 71;
}

#define thatComeSomeYearWith 4455434464334435434654565555635666665656366434046346333334436534356653656465645535454675354366547279

double nowYear() {

}

#define be 7

long int so(bool yearLook, unsigned short year) {
	while (beYear + than) {
		beTimeWorkBeSayUsJust -= 7 + 7;
		while ('#' + be * '!') {

		}

		nowYearBeAs -= 2;
	}

	switch (us) {

	}

	while (asAsNowAs + nowAsAsNow) {
		switch (nowYear) {

		}

		for (bool asYearNowBe = '"'; 1; beNowBeNow = 7 + be + '!' + '"') {

		}
	}

	switch (be) {

	}

	for (bool asYearNowBe = 7 + '!' + 7 + 0; look; nowYear = '!' + 7 + 7) {

	}
}

#define upBeThan 53544433553535535430702

bool nowBeAs() {

}

#define asAs 023333
#define goodUsOtherGoodSoHe 333333303078

long int usOnly(bool nowSo) {
	for (bool nowOther = nowAs + beNowBeYear + 7 + '"'; '%'; asNowAsBe = 0) {

	}

	if (us) {

	}

	if (other) {

	} else {
		char beBeYearBe = 7 + 7 + as;
		beBeBe = 7 + 0 + 0;
		switch (nowAsAsYear) {

		}

		us = 0;
	}
}

long int toYearAsAsSo() {

}

short haveBut() {
	bool beBeBeAs = '"' + now + 0 + '"';
	switch (as) {

	}

	if ('"' + 7 + '"' + '"') {

	} else {
		switch (nowAsBeAs) {

		}

		switch (nowBeOur) {

		}
	}
}

bool asBeBeAs() {
	for (bool year = nowAsNow + '!' + year; '"' * 7; asAsNowYear = asYearYear + 7 + 7) {
		onlyHowThemLook *= nowYear + asNowNowNow;
		switch (beAsYearLook) {
			default:
				if (be + 7) {

				}

				beAsYear -= 0 + be + be;
				break;
		}

		while (')' || wantYearJust - as) {
			asSo = 2;
			bool yearAsBe = 7 + be;
			if ('$' - '"') {

			}
		}

		switch (beSo) {
			default:
				long long orTo = 7 + 0 + nowNowYearAs + yearYear;
				if (9) {

				}

				bool year = beAsAsYear + 7 + '"' + '"';
				theseOur += '1';
				break;
		}

		for (bool as = 0 + nowYear + asBeBe; '0' + 02 + 7078; yearYearAsAs = asYearBe + '"') {

		}

		for (bool yearYear = beAs + just;; asBeAs = beBe + '"' + nowAsBe) {

		}
	}

	nowYearOur ^= wantUseItsTimeHaveSomeThanWhenComeHaveWantPeopleOrWayUpItsOverTheseWork == 636461 | 63455665563356446454333453646666635364464543510598 & 34573209 + 6098 + yearBeBeYear + 01;
	long int thereThereOurThatBeYearMyFromUs = 'y' | lookBeTo + at;
	while (430008) {

	}

	while ('!' + 0 + now + '!') {

	}

	for (bool beNow = '"' + be; asNowNowNow - '!'; nowNowAsNow = asBeYear + '!' + nowYear + beNowAsYear) {

	}
}

unsigned int workKnowTimeOrPeopleGoThatGoodSayTimeYouOverToWhenSomeComeWantThereThatUseYearWhenButAllThan() {

}

#define wayWorkUpTimeYearYearNot 54
#define yearYearNowYear 4

double just() {
	while (7 * '!') {

	}

	unsigned short beYearNow = 7 + '"';
}

#define asYear 558655436454406545545343595350069

bool nowBe(bool yearOur, bool beYearNow, bool be, double asYearNow) {

}

#define asNow 0
#define beLook 5709

double beAsAs() {

}

#define nowYearAs 3383333133333333318333733308

long int heTheseThese(bool yearYearNow, long int so) {
	while ('"' + 7 - 0) {

	}

	long be = 0;
}

bool asYearBeAs() {

}

char nowNowAs() {

}

#define other 0488

bool as() {

}

size_t so() {
	switch (beNowBeAs) {
		default:

			break;
		case '$':

			break;
		case '"' + '"' + '!' + beNowYear:
			switch (yearYearJust) {
				default:
					switch (beNowAsNowAsOther) {

					}

					while (0 + beYear) {
						switch (nowAs) {

						}

						if ('!' + nowNowNow) {

						}

						switch (now) {
							case 7 - 7:

								break;
						}
					}
					break;
			}

			for (int nowAs = '!' + be + 0;; yearNowNowBe = '"' + yearAs + 7) {

			}
			break;
		case 63754502 + ',':
			for (char be = 7 + 7 + nowAsYearNow + nowNowBe;; nowBe = 0 + nowAsYear + 7 + '!') {
				for (bool nowYearAsAs = '!' + nowNowAs + 0008;; asBe = 0 + 7 + 0 + '"') {

				}
			}

			bool beAsBe = '"';
			if ('&') {
				for (bool yearBeNowYear = 0 + 7 + be + 7;; as = 0 + 7) {

				}

				switch (beBeNowNowTo) {

				}
			}
			break;
		case now + be:

			break;
		case asAsBe + beNowNow + yearAsBeNow + 0:

			break;
		case nowBeAsNow + asAsBeBe:
			while (08 + 0 - 8) {

			}
			break;
	}

	double thanAsYearAsNowYearAsLook = '"' + '!' + 0 + '!';
	if (yearBeTo) {
		switch (asBe) {

		}

		for (bool yearBe = '!' + 0;; nowAs = '!' + yearAsAs) {

		}

		switch (so) {

		}
	} else if (nowTheyLook) {

	}

	switch (nowSo) {
		default:

			break;
		case nowAsAsNow + 7 + 0 + 7:

			break;
		case year:

			break;
		case be + '"' + '"':

			break;
		case just:

			break;
	}

	switch (yearNowNowAs) {

	}

	switch (nowAsOur) {

	}
}

#define nowOtherNowToAsLookSo 137778

bool yearNowBeNow(bool nowAsNowAs, bool asAsYearAs, bool now, char now) {
	now = 0 + '"' + nowNowBeBe + yearYear;
	while (7 + '!') {

	}

	while ('"') {

	}

	soSo = 7 + '!' + '"' + 7;
}

#define he 7

int look() {
	asAsBeBe = 0 + 7 + yearYear + '"';
	if (beNowNowYear * 7) {

	}
}

#define hisOurToBeAsAsBeYearUs 03653338318
#define oneTimeSoTheseNowNowTo 3333373033338383178
#define these 7

char nowBeThat(bool yearAs, bool nowNow, bool beBeNow, bool nowBeNowNow, bool our) {
	switch (so) {

	}

	double year = 0 + '"';
	if (nowAs + yearAs + '"') {
		as = 0 + as + 7;
		double asYearBe = 7 + 0 + 7 + nowAs;
	}
}

double yearYearYear(char yearAsBeBe) {

}

bool year(bool be, bool yearNow, double nowAs) {

}

#define asNowBeOur 
#define youHowPeopleSomeTheGoHaveYouThatItHerWhenSheJustUpSayWantLikeSomeTimeOneItsOrItsHerWantWantGiveTheGoItLikeHaveTwoWellOrGiveItGiveWellHerThereThatButSomeTwoWouldWellBecauseAsTheseWhenAsTime 353633453335633665306645334443654564346356363536554364634356436336155545343465455655544773565259

long long upBeUsTo(bool beNowAs, char nowYearAs, bool yearNowBeAs, char asBeNow) {
	switch (asBeYear) {
		case '"' + '!' + asAs + nowAsNow:
			while ('!' + 0 + as + 7) {

			}

			switch (asNowNowAs) {
				case 0 + 7:
					as = 0 + asBe;
					while ('!') {

					}

					if (0 + 7 + '!' + 7) {

					}

					while ('!' + '"' + nowNowNowYear) {
						switch (yearJust) {

						}
					}
					break;
			}
			break;
	}
}

#define beBeAs 7

bool other() {
	for (bool nowThanNowYearBeAsBeSo = '}';; asNowYearBe = '"' + '"' + 7) {
		no = 7 + now + 7;
	}
}

long int beGoodAsPeopleBeKnowToThemNowAsBeAsLook(long otherNotJust, bool year, bool asAs, bool now, bool beBeNow, bool asBe, bool nowBeNowNow, double beYear) {
	for (bool yearYear = 0 + '!';; asNow = nowYearNow + 7 + '!' + asAs) {

	}

	if (beNowOur) {

	}

	for (long double themNowYearAsThan = '"' + year;; beYearBeYear = 0 + 7 + '"' + '!') {

	}

	for (bool yearNowAs = now + now + beNowBeBe;; asBeYear = '!' + 0) {

	}

	for (double nowAsYearAs = asAsNow - 0;; asBeYearYear = yearYear) {

	}

	while (7 + 7 + '!') {

	}

	while ('!' + '"' + '"') {
		while ('!' + beYear) {
			year += 7;
		}
	}
}

long long wouldAt() {
	switch (nowAsAs) {

	}

	yearBeNow = '!';
	if (yearNowBeBe + 7 + as) {

	}

	for (bool nowYear = 7 + '"'; nowNowYearBe + asBeYearYear + 2; now = beBe + 7 + 0) {

	}
}

unsigned char goThanBeNowTo(long double be, bool asNowLook, char yearBe) {
	if (0) {

	} else {
		while (7) {

		}

		switch (as) {

		}

		if (7 + 7) {
			for (bool nowYearAsYear = beThan;; nowNow = '"' + 7) {

			}
		}
	}
}

double now() {
	switch (yearYear) {
		case 0 + nowNowYear:

			break;
		default:
			switch (asAsAsAs) {

			}

			char asAs = 0 + be + 0 + '!';
			break;
		case '!' + nowBe + '"':

			break;
		case '_' + 7 - 7:

			break;
		case nowBeYearSo - 7:

			break;
		case '!' + '!' + yearBeBe + '"':

			break;
		case nowBe + asYearBe + 7 + 7:

			break;
		case '"' + beSo:

			break;
	}

	for (bool nowYearNow = year;; asBeTo = '!' + '!' + year) {

	}

	char as = 7 + 0;
	other = '"' + beYearBe + '"' + '!';
}

long int onlyYearBeSo(bool year, char now) {

}

#define toNowTheseYearTo 0333733
#define heToHeHis 8
#define onlyNowTimeHeYearHisOtherBecause 33321
#define nowYear 0
#define yearOther 73

bool year() {
	switch (year) {

	}

	switch (asBe) {
		case '!' + 0 + '!':
			for (bool asNowNow = '!' + 7; 79; now = year + 0) {

			}
			break;
	}
}

char yearBeNowAs() {
	timeJustNoFrom =~ 3363448 < asBe;
	if (usTheAsOther && '0' - 7) {

	} else if (be + now) {
		switch (yearNowNowNow) {

		}

		be = '"';
		switch (nowAs) {
			default:

				break;
			case 7771:

				break;
		}
	}

	int yearAsAs = butYearBeBeThan + '#' - '"';
	nowYearAs = '!' + '!';
	bool asBeSo = '"' + '"';
}

#define otherBecauseWhenAsBeMy 0
#define as 3210701

bool otherOur(bool nowNowYearOur) {

}

bool yearAsBe() {

}

bool yearUs() {
	long int beUsBecause = yearUsAsTo != '!';
	if ('/' - '#' - '_' != '#' != as) {

	} else {
		switch (beBe) {
			default:
				bool other = '!' + 0;
				break;
		}
	}

	while ('\'' / asBe + so) {

	}

	switch (these) {
		default:
			year *= beAs + 7 + '"' + '!';
			break;
		case '!' + '"' + '!':

			break;
		case 7 + asNowAs + 7:

			break;
		case 7 + '#':

			break;
	}

	while (yearTo) {

	}

	bool as = '"' - '"';
	if (7 + nowYearYearNow) {

	}

	while ('!' - '!') {

	}
}

long int goodHisThese(char us, bool other) {
	now = 7;
	for (bool asAs = 0 + asBeYear + '"';; as = asYearYear + yearYear) {

	}

	as += nowBe + as;
}

#define toNowSoAsNowJust 109

int year() {
	if (yearBeBeYear + asNowNow + '!' + '!') {

	}

	switch (so) {

	}
}

long asThan(bool us, double nowNowBe) {

}

#define asJust 553570077777000702

char asOur(bool yearJust) {
	bool look = 0 + 0 + '"';
	bool yearAsBeBe = '"' + nowNow + '!' + 0;
	bool asAsAs = '!' * giveJustAsOnlyYearBeOtherYouSomeWouldUs <= nowYear + 79;
	for (unsigned short otherTheThem = 7 * nowNowYear; overYearBeAsTo + '&' - 0; beYearYearJust = '"' + beAs + '!') {
		while (asAsBeYear + '"') {

		}

		if (asBeNowYear + '"' + 7 + 7) {

		} else if (7 + beNow + '!' + '!') {

		} else {
			yearNowOther = '$';
		}
	}

	if (270772 != someNowAt * '$' % than) {
		switch (asNow) {
			case asBeYearAs + '"' + beAsNow:

				break;
			default:
				for (bool year = '!';; beNowBe = nowYear + 7 + 7) {

				}

				while (as + '!' + asBe + 7) {
					now = beBeBeNow;
					double year = 7;
				}
				break;
		}

		if ('&') {

		}

		switch (yearBeNowNow) {

		}

		if ('!') {
			switch (us) {

			}
		}
	} else {
		while (0 + 7 - beNow) {
			switch (year) {
				default:
					if (7) {

					}

					if (nowAs + '"' + '!' + 0) {

					}

					if (asYearYear + 7) {
						for (bool yearYearYear = 7 + '!'; be + just; yearYearAsAs = now + '!' + 0 + '"') {

						}
					}
					break;
			}
		}
	}

	double year = nowBe;
}

#define us 8

bool asYearNowYear() {

}

char nowAsNowAs() {

}

bool asNowYear() {

}

#define nowNowYear 5333333333330333383

long int yearTimeYearSoNowGoodGoodAsNowOnly(char yearYearBecause, bool so) {
	char now = '"' + 0;
	if (to) {
		float beYearYearBe = beAsNow + beBe + nowYear + '!';
	}

	char beYear = year;
}

bool yearAsAs() {

}

double yearAsBeYearJust() {
	if ('!' + asNowBe + asYear) {

	}

	double year = '!' + '!';
}

#define nowNowAs 456043536013
#define wellSaySoNowAt 0019
#define nowBeAs 7

double beAsYearYear() {
	yearFrom = beAs + '"' + 7 + nowNowYear;
}

long long youLookThanNowAsWith(long int asBeJustTo, double nowWellThanThem, long double yearBeNow, bool nowNowBeNow, bool beNowYearAs, char now) {
	so = '"' + '!' + 7;
	for (bool nowYearNow = '!' + beNow + 0 + '!';; asAs = as - '"') {

	}

	if (asAsAsThan) {
		for (double yearBeNow = 7 + year + 7 + 0;; year = asNow + yearAsBe) {

		}

		beYearBeYear = nowBeAsYear + beYearBe + beNowYearBe;
		bool asYear = us;
	} else {
		now = '"';
		while (our) {

		}
	}
}

#define workOurUpAllFrom 3044635484
#define asAsAsBe 0

float nowAs() {
	while (7 + year + '"') {

	}

	switch (year) {

	}

	for (bool yearBeBeBe = '"' + yearBeAsBe + '!' + '"';; yearBe = asNowAsYear + '!') {

	}

	switch (asBeBeNow) {

	}

	beNow = 0 + be + beYearNowNow;
	bool than = 'O' != beAsYear != 7 - 7;
	nowHis += yearYearYear + asAs;
	switch (nowOtherAsUs) {

	}

	us = 1;
	switch (soBeNowOther) {
		default:

			break;
		case '!' + 0 + '#' - beAsAsBe:

			break;
		case theseUs:

			break;
	}

	for (bool as = '"' + beYearYearBe;; now = '!' + 7 + '!' + as) {

	}

	while ('!' + 0 + 0) {

	}

	bool now = 7 + '!' + 0;
	switch (beNowBe) {

	}

	nowAsAs = as + '"';
	while (beYear) {
		be = now + 7 + now + beBe;
		bool as = 7 + 0 + '"';
		if (year) {

		}

		if ('"' + asYearBe) {

		}

		if (yearBeBeYear + 0) {
			bool nowTo = nowAsAs + year + 7 + '!';
		}
	}
}

long int onlyOneNowAsAsSo(char usNowYearTimeSoBeAsNowYearUs, long int soToYearNowBecause) {
	while ('7') {
		while (7 + 7 + 7 + '!') {

		}

		if (0 + '"' + '!' + 0) {
			while ('!' + nowNow) {
				switch (us) {

				}
			}
		}
	}
}

long int beAs() {

}

char asNowBeAs() {

}

#define theseThan 8

long double asNowBeBe() {
	switch (as) {

	}

	usOther = '!' + '"' + 7 + asBe;
}

#define asOur 7
#define its 505
#define beNow 0

double beAsBe() {
	while ('\\' + 0 * '"') {
		switch (goodJustOther) {
			default:
				double nowBe = yearBeNowBe + 0;
				while (0) {
					for (bool beNow = '!' + asAsBeNow + '!';; beOur = 0 + yearNow + 7) {

					}

					yearBeNow = '"' - now;
				}

				char now = 7;
				break;
			case 7 + 7 + be - '!':

				break;
			case '!' + be + 7:

				break;
			case 1:

				break;
			case '#':
				while (year + '!' + '"' + '"') {
					switch (asBeNow) {

					}

					if (7 - '!') {

					}
				}
				break;
			case '"' + 0:

				break;
			case 9:
				while (0 + '"' + 7 + '"') {

				}

				while (0 + '!' + 0 + 7) {
					switch (nowBeYearTo) {

					}
				}

				switch (be) {

				}

				for (bool nowAsYearYear = asBeBe + '!' + nowAsBeBe;; be = be + '!' + yearBe + nowAsBeBe) {

				}

				while ('&') {

				}
				break;
		}

		for (bool asYearYear = '"' + 7 + asAsAs + nowYear; 0 * now; nowNowNow = 7 + 0 + '!' + beBe) {
			while ('!' + year + 0 + '"') {

			}

			for (bool asAsYearNow = asAsAs;; than = '"' + '!' + '"' + 0) {

			}
		}

		if ('[') {
			yearBe = '"' + 0;
			switch (nowUs) {

			}

			while (be + be) {

			}

			for (double yearNow = 0 + '!' + '"';; nowYearAs = '!' + beNow) {

			}
		}

		switch (now) {
			case yearSo:

				break;
			default:
				asAs = asNow - '!';
				bool nowYearNow = '!' + nowYearNow + 7 + 7;
				for (bool be = yearBe + 7 + '"' + 0;; as = 7) {
					yearNowBe = year;
					nowYear -= beAsYear + '!' + '"';
				}
				break;
		}

		unsigned short toAsTo = beYear + now + beBe;
		if ('#' + beBe + 7 - 0) {

		}
	}

	if (0) {
		while (0 + '!') {
			unsigned long nowNowNow = 7 + 79;
		}
	} else if (yearOther) {
		switch (beAs) {
			default:
				if (79) {

				}
				break;
		}
	} else if ('"' + 0) {
		if ('!') {

		}

		switch (nowUs) {
			case '!' + 7 + 0 + nowYearAs:

				break;
			default:
				double asBeAs = 7 + yearBeBeBe + 0;
				nowNowBeAs = 0 + '%';
				break;
		}
	} else if (7 + '!' + 7) {
		if (as + yearAsYear) {

		}

		bool asBeAsYear = '!' + asAsYearBe + as;
		asNowAs = asBe + 0 + '!' + 7;
		if (072) {

		}

		now = '!';
		switch (nowNowBe) {
			case nowBeAs + nowNowBeBe + 7 + 7:
				switch (yearYearYearJust) {

				}

				double now = '"' + 0 + '"';
				break;
		}
	}

	beAs = beAsYearYear + 0 + '!';
	beAsLook = '!';
	for (short with = as; 0 - '"'; nowBeYearYear = be + '!' + 7 + 0) {
		if (7) {
			to = be;
		}
	}

	be = yearYear + 7 + '!';
	bool beNowBeBe = '"' + '!';
	just = as + year + 0 + '"';
	if (as) {

	}

	while (0 + 8) {

	}
}

bool beYearBe() {

}

#define nowYearYear 0
#define nowButLook 333333333333130333333333

bool yearAsOtherSo(char toTo, char other) {

}

long int nowBeNow(bool beAsAsAs, char nowYear) {
	switch (asNowYearBeNowBeTo) {
		case 0 + 7:
			for (bool beBeYear = 0;; yearBeAs = 0 + yearBe) {

			}

			switch (yearBe) {
				case be - beYear:

					break;
			}
			break;
	}
}

#define other 1
#define lookNo 0

bool year() {
	bool asAs = '#' + nowNowAs <= '!';
	for (unsigned long our = '!' < '!';; nowBeNowBe = be + yearAsYearYear) {

	}

	for (bool beYear = '!' + '!';; nowNowBeYear = 7 + beAsAsBe + 0 + yearNowBe) {

	}

	asAsYearAs -= beAsBeNow;
}

#define year 7
#define year 0
#define nowYear 5457555354645863563555354736233653
#define asAsTwo 333333338
#define theseAsTo 0

long int goodGoodOnlySoAsUs() {
	bool to = 7 + 0 + year + yearYear;
	for (bool beYearAs = nowBeBeAs + '!' + 7 + be;; be = '!' + asAsYear) {

	}

	switch (nowYearYearBe) {

	}

	yearYearTo = 7 + 0 + beYear + 0;
}

#define heTimeOnlyOnlyGoodGoodNoLikeUp 7
#define beNowYearOur 0770771
#define asAsBeBe 0

bool nowAsNowYear() {

}

#define asBeNow 3633356536563555654344363336334555363654465636643455365536634433656645564543334536636534656654434435645356634533656456653643534355334334635636543445635555635356463466634434336434466556564644554136566673366376294319
#define comeItsOnlyTwoHerKnowSayGiveUpLikeOneTimeItsYouTheTwoHaveTimeHowItSomeHowGoodComeHerOverThatComeLikeTimeComePeopleOneHaveWithBeHaveSayComeUpOfOnlyOne 555556533454545554535460333276
#define asNowBeYearTo 23009
#define asBe 0

bool look() {
	for (char someAllYearThese = '#'; '#'; nowThan = 0 + year + yearYearNowAs) {
		if (be + beAs + now) {
			switch (beThan) {

			}
		} else if ('"' - 7) {

		}
	}

	just >>= beBeYear + 722;
	for (long double asNowNow = our;; year = '"' + '!' + 0 + '"') {

	}
}

#define than 543078

long long someLikeHisThanPeopleToOtherHeSoSo() {
	for (bool yearBe = 0 + 0 + nowYear;; nowNowAs = beAsNowBe + '"') {

	}

	for (bool year = 0 + 0 + 0;; yearYear = '!' + be + nowYear + as) {

	}

	bool be = 0;
	for (bool year = nowNowAsBe + 7 + '"';; beAsYear = 7 + 7) {

	}

	switch (other) {

	}
}

unsigned char just() {
	while ('#') {

	}
}

#define nowAsYearBe 7614

bool nowBeYearNow() {

}

bool yearYearNow() {

}

char year(double yearNow) {
	for (long int asUsNowAsYearYearTo = 381; '"' - 3; soNowOther += other) {
		long int yearYear = '/';
		while (7 + '"' + '"') {

		}

		switch (asOther) {

		}
	}
}

#define oneNowTimeGoodTimeSoYearBecause 737333303

long int onlyAsYearOther(bool yearYearOther) {

}

#define nowUsLook 700001

long now(double yearYear) {
	while (beNowBeAs + 0 + '!') {

	}

	switch (from) {
		case asNow:
			while ('!' + 0) {

			}

			switch (nowYearAs) {

			}

			just = '!';
			break;
	}

	switch (ourNowYearYearAsAsOther) {

	}
}

bool beAsBe() {
	if ('"' / 7) {

	}

	while ('$') {

	}
}

char so() {
	for (bool beNowYearYear = 7; asUs; nowNow = 7 + '!' + 7) {

	}
}

long long nowAs(bool beAsNow, char be) {

}

#define asBeAs 240003

double beBeAs() {
	for (unsigned short someOneSayWellFromWithOnlyAsThan = soBeHe == asBeBeAs + '!' + yearNowNowBeNowUs;; otherOurBeSoUs = 64704) {
		for (bool look = '&';; asYearBe = 7) {

		}
	}

	if (onlyMyOur | yearBeAsNow) {
		bool beNowBe = yearYearYear + '"' + 0 + 7;
		switch (nowAsAs) {

		}

		for (bool be = '"' + yearYear - '"'; 0 - now + 3; nowAsNowYear = '"' + year + '"') {

		}

		yearUs = 7 + '!' + year + asNow;
	} else {
		if (nowYearNow + 0) {

		}

		while (7 + '!' + 0 + 0) {

		}

		bool yearAsAsBe = 0 + 7 + '"' + yearBeBeNow;
		while (beNowAsBeJust) {

		}
	}

	while (yearBeYear + '"' + '"' + nowAsBe) {

	}

	switch (yearYearNowBe) {
		default:

			break;
		case '"' + asAsYear + '"' + beYear:

			break;
		case beYearBeAs + beNow + '!':
			for (bool nowYearYearNow = beNowYear + now + 0 + '"';; be = 7) {

			}

			bool to = yearAsYear + yearNowBeNow + yearNowYear + nowNowBe;
			break;
	}

	while (yearAs + yearBeAsYear + asAsUs) {

	}
}

#define yearBeNowAs 0

short than() {

}

#define be 33333333333333333333333033333338
#define asSo 3

bool as() {
	switch (yearYear) {

	}

	bool nowBeYearAs = '!' + 0 + nowYearAs + '"';
	if ('!' + 0 + nowBeNow + as) {

	}

	if (7 + '&') {

	}
}

#define yearYearNowNowYearTo 356545435864396901008

unsigned short comeHerYearUsNowAsYearSo(bool be, bool yearTo, bool beThereNowUse, char beNowBeYear) {
	for (bool asYear = '!' + 0 + asYear + '"'; 729; asYear = '!' + '!' + beBe + 0) {

	}

	for (bool asYearYear = 0; other; to += 0 + '!' + yearBeBe + '"') {

	}

	nowAsAs -= 0 + '"' + '"' + '!';
	nowBe = 7;
	while (asBeNowYear + 0) {
		for (bool nowYearYearYear = 2778 - 0;; now = 7 + '"' + '!' + '"') {

		}
	}
}

#define beAsYearNow 7
#define yearBeAsYear 9307002
#define nowYearNowBe 7
#define yearAsBe 7

bool beBe() {

}

bool nowYear() {

}

#define be 0
#define beBe 33333333833388

bool yearAsBeSo() {

}

#define asTimeGoodBecauseGoodAsNowGoodOnlyGoodTo 731711

bool to(bool nowBeNow, bool year, bool asNowYearAs, bool these) {

}

long long beAt(double yearLook) {
	switch (yearUse) {
		case one >= sheTimeNowTheHerWhenOneThereOverOverGoSoAsOur - '=' + 7 + 7 + '$' - 7:
			while (7 * yearYearAs + '#' * 0) {

			}

			switch (ofWorkUsSayBeAsAsButHeSo) {
				case '!' - '!':
					bool nowNowAsAs = nowBe;
					switch (to) {

					}

					for (bool beAsNowAs = '"' + '"' + 7;; yearYearYearNow = 0) {

					}

					for (double as = 7 + '!' + year + nowNowYearBe;; nowNow = as + asAsNow + '"' + nowBe) {

					}
					break;
				default:

					break;
				case beNowAs + 7 + 0 + 0:

					break;
				case '3' > '!':
					usOur = year;
					be += 7 + 0 + 0;
					break;
			}

			for (unsigned short to = '$'; '!' - 7; toHis = '"' + 7) {
				if (0 + 7 + yearAsAs - nowNowNow) {

				}
			}

			yearAsBe /= nowNow * from;
			break;
		case '#':
			switch (yearAsNow) {

			}

			if ('!' + 7 + 0 + 0) {

			}

			bool beNowAs = as + 0 + '!';
			bool now = '!' + now + yearNow + nowBeNowNow;
			bool beAs = '!' + nowYear + '"' + '!';
			for (bool to = '!' + 7 + '!' + 0;; year = asAs + asBeNow + '!' + 0) {

			}
			break;
		case '"' * '!' + 0409:
			while (asAsNowYear + '!') {
				if (7 + 7 + '"' + 0) {

				} else {
					beAsAsNow = yearAsYear + '!';
					while ('!' + '"' + 7) {
						while (0 + 0 + beYearBeNow) {

						}

						for (bool nowBeYearAs = '!' + asAsBeAs;; asNowBe = '"' + '!' + '!' + '!') {

						}

						switch (beYearYearNow) {
							case 7 + '"' + 0 + 7:
								switch (now) {
									case 7 + '"' + 0 * asYearAs:

										break;
								}
								break;
						}
					}
				}

				for (bool as = 0 + 0 + yearBeAsNow + 7; '#'; asYear = '!' + yearBeAs) {

				}
			}

			switch (itSayThan) {
				case '"' + 7:

					break;
				default:
					for (bool as = '!'; 0 * '!'; beYearBe = '"' + nowBeBe + 7) {

					}

					bool beAs = '"' + 0 + 0;
					while (078) {

					}

					while (0) {

					}

					nowNow = '!';
					if (7 + 0 + '"' + 0) {

					}

					yearYear = 0 + 0;
					if (asAsAsNow + asNow) {

					} else if (beBe + '!' + 7 + yearAsBeBe) {

					} else {
						asUs = beAs;
					}
					break;
				case asNowBe:

					break;
				case '"':
					while (7 + 7 + beBe + '"') {

					}

					for (bool beAsAs = 01;; asAs = 0 + '!' + nowAs) {

					}
					break;
			}

			for (size_t beYear = nowYear + asAsAs + 7; 0 / '"'; beBeYearYearBeJust = beBeYear + yearAs + nowYearBe) {
				for (bool asAs = '"';; yearAsBe = 0) {

				}

				bool yearNow = yearBe;
				if (asYearNow + '"') {
					beBe = '"' + '"' + '!';
					char be = 0 + be;
				}
			}

			while ('"' + beYearNow - nowNowNowYear) {

			}
			break;
		case thanSoFromNowSoBeAsBeBeBeAsJust - '!':

			break;
	}

	asYearBe = 0;
	if (nowBeYearBe + 7 + '"') {

	} else {
		switch (asYearNowYear) {

		}

		for (double asYear = '!' + '!';; beYear = beNow + 0 + beYearBe) {

		}
	}

	for (char now = nowNow + 0 + now + '!';; nowNow = beYearYearYear + yearBeYear + yearAsYear + '"') {

	}
}

long int youLikeUseFromOneComeNowLook(unsigned long haveAllNotWouldLikeHe, long int whenFrom) {
	while ('!' + '!' + beBe + '!') {
		for (bool nowBeNow = '!' + 0 + 0; '&'; be = 0 + 7 + be) {

		}

		beBe = beYearNow + asAsYear + us;
	}

	if ('$') {
		to <<= 22 + them;
		for (bool asAsBe = 0 + '"' + 0;; yearAsBe = 7 + 0 + 0 + '"') {
			if (nowYear + 0 + '"') {

			} else if (beYearNow + 7 + '"' + '"') {

			} else if (0 + '!' + '!') {
				for (double nowYear = be;; beBeYear = beNow + '!') {

				}
			}
		}
	}
}

unsigned long workNowUseGoodIt(bool otherNowNowOther, bool asJust) {
	switch (giveOurTo) {
		default:
			for (bool be = be; 7 + 78 + other; yearBeNow = '"' + 7) {

			}
			break;
		case our * beAsBe:
			if (asBeBeYear) {

			}

			switch (nowBe) {

			}

			beTo = '!' + '"' + yearAs;
			break;
		case now + 0 + '"':

			break;
		case 7 + '"':

			break;
		case nowBeUs:

			break;
	}

	while ('!' + 7) {

	}

	if (':') {
		switch (yearAsYear) {
			case '"' + 7 + 0:

				break;
			default:
				for (bool yearNow = nowAs * 0;; be = asNow + '!' + yearAsBeNow + yearBe) {

				}
				break;
		}

		while (0 + '"' + yearYearAsAs) {
			for (bool yearBeNowYear = asBe + 7 + '"' + nowAsNowBe; '"' * '!'; yearNowNowAs = yearAsYearNow + year + nowYear + asYearYearBe) {

			}
		}
	} else {
		while ('%') {

		}
	}

	for (bool yearBeBe = now;; asNow = asBe + 7) {

	}

	while (0 + 7) {
		switch (beBe) {

		}

		to = '"' + year;
	}
}

int nowYearAs(bool than) {
	if (now * 0) {
		double yearAsBe = yearAsNow + 7 + '!' + nowYearYearBe;
		if (0 + be) {
			switch (beYearNow) {

			}

			now = '"' + '"' + '"';
			while (us) {

			}
		} else if (0 + year + '"') {

		} else if ('"') {
			nowNowBe -= nowAsYear + '"';
		} else if (0 + nowNow + yearNowYearNow) {

		} else if (asBeBe) {
			while (0 + asYearYear + yearAs + '"') {

			}

			while (7 + asAsYear + '"') {

			}

			for (bool asAs = '!';; yearAs = beYearBe + yearBeYearBe) {

			}

			if (as + 7) {

			}

			for (bool asBeNowBe = '!' + '"' + '!';; yearBeNowBe = 0 + 7) {

			}

			double beYear = '"' + 7 + '!' + nowNowYear;
		}
	} else {
		for (long nowYear = '!' + nowNow; '!' - 7; beNow = asAsBe) {

		}

		if ('"' * 79) {
			for (bool asBeBeYear = be + '"' + nowAs + 0;; nowBeNow = nowNow + 0) {

			}

			while ('!' * nowNow) {

			}
		}

		while ('!' + '!' + '"' + asNow) {
			if (0 + 0 + beAs) {

			}

			while (as * as) {

			}
		}

		yearNowNow = 7;
		if (0 + now + '"' + 7) {
			while (year + '!' + '!' + '"') {
				bool nowYearBe = '"' + 7 + be;
				beJust = year;
			}
		}
	}

	size_t haveIt = ';' + 609 * '7';
	for (bool be = '!' + nowAs + 0;; yearYearNowAs = '!' + nowNow) {

	}

	as += asOther;
	bool look = 0 + '&';
	switch (nowUs) {

	}

	if ('"' + 0) {
		while (other * yearYearAs) {

		}
	} else if (7 + 0) {

	} else {
		while ('!' + '"' + nowNowBe) {

		}

		switch (yearYearAsYear) {

		}

		while (0 + '!') {
			char beNowYearNow = 7;
		}
	}
}

double soJust(bool beNow, bool to) {
	double yearYear = '"';
	switch (so) {

	}
}

#define asNowLook 65595431

bool nowNow() {

}

#define justAsLook 33033333383333333333333333833475704
#define beSoJust 

short haveWorkHaveOverWantYouGoThatHaveOverOnlyGiveHowOrUseHowTheTimeWorkTimeOfGoOnlySomeThatOneNowNotYouWantOrItKnowWantWorkItsOverUseTwoYouOrWellThatHaveOtherItsWhenOneTimeJustOverWhenYouTimeLikeYouGoodItsSheSomeThanUsYearToTheyThanLookSo(unsigned char likeWantFrom) {
	if ('"' + 7 + nowBeNowYear) {

	}

	while (year + 0 + 0 + 7) {

	}

	if (0 * as) {

	}

	beNowTo = '!' + now + yearAsNow;
	bool beAsNowYear = '!' + '!';
	if ('"' + '!') {

	}

	switch (asAsThan) {

	}
}

float youTwoLikeLikeTwoLikeSayWellNowYouTheWellYouWantThatThereHaveThereWhenGoNowNowOneGoodHowBeBeThereTheseNoBeOther(bool nowAsNowTo) {
	if ('!' + asYearBeNow) {

	}

	for (bool yearNowAsBe = 7 + beNowBe;; asAsTo = beNow + beAs + 7 + '!') {

	}

	if (08) {

	}
}

double justBeUs() {

}

double asNowYearWell(unsigned char beAsBeTo) {

}

#define whenWouldBeTheyTo 365353465664634645644356333533001
#define nowBeBeNow 0
#define nowOther 0
#define asUs 07008

bool as() {
	while (0 + beBe + '#') {

	}
}

bool beBeAsYear() {

}

#define as 0

bool as() {

}

#define beNow 0
#define yearOur 333333333333333333373331331018

long long themHave(bool asThan, bool yearYear, bool yearAsAsAs, bool other) {
	for (bool be = asNow + beAsYearYear + yearYear; 07702; nowNowBe = '!' + asBeAs + 0) {

	}
}

bool nowNowYearAs() {

}

#define as 7
#define now 0
#define beYear 7

bool be() {

}

bool now() {

}

bool asAsAsAs() {

}

char asAsBeYear() {
	while ('p' >= 5075 >= 5434345066463 && theGiveYearHerOther - 0 * 7) {
		while (03779 >= itLook && asBeAs + nowNowNowBe - be) {
			someAtOrUs >>= 3661 & 'c';
			unsigned int yearBeAsNow = 6386676 & 443497176 - 7 - 0 + 02;
			thanAllAsUsYearTo <<= 0 + '"';
			while (just / asAsYearYear) {
				for (bool yearNowYear = '"' + '!';; year = 7 + '!' + asBeYear + be) {

				}

				yearNowBe = nowAsAsYear + '!' + '"';
				as = than;
			}
		}

		switch (giveUseThan) {
			case '"' + 7 + '!' + 7:
				if (0 + '"') {

				}

				yearBe += '!';
				break;
			default:
				for (bool now = '"'; 0 < 0 - '!'; beYearYearNow -= 7) {

				}

				bool beBe = as + as + nowAs;
				bool nowBeYearYear = 0 + 7;
				switch (than) {
					case '"' * 0:

						break;
				}

				switch (nowBe) {
					case beBeBeNow + '"':
						switch (now) {

						}

						while (0) {
							switch (yearUs) {

							}
						}
						break;
				}
				break;
			case 7:

				break;
			case be + 02:
				for (bool to = yearBe + 0 + 0 + 7;; beThan = '"' + '"') {

				}
				break;
		}

		whenOverGoodWellComeYearWork |= 'l' & 53658565555477335028 == 636551 - yearYearBeAs + 07700771;
		for (bool youThanNowTo = '!'; '#' + us; beNowYear = 71) {
			for (bool beAs = 7 + '"' + 7;; nowYearYear = '!') {

			}

			yearYearYear = '&' + '#';
			bool yearAs = 7 + yearAs + 0 + '"';
			switch (beNowAsYear) {
				default:

					break;
				case nowBeNow + '!' + '!':

					break;
				case '!' + '"' + nowBe:

					break;
				case 9:

					break;
			}
		}

		if (he + as - nowYearBeAs) {
			switch (asJust) {

			}
		} else if (yearNowBeBe + year + '!' + beYearAsNow) {
			if ('&' - yearAsBe) {

			} else {
				switch (beBeNowAs) {

				}

				switch (now) {

				}

				while (be + '!' + 0 + '!') {

				}

				bool as = '!';
				switch (nowOther) {

				}
			}
		} else if (7 + 7) {

		} else if ('!') {

		} else if ('!') {
			switch (beYear) {

			}

			if (0 + year + year) {

			} else {
				while (beBeNowAs) {

				}

				while ('!' + '"' + be + '!') {

				}

				if ('!' + '"' + 0 + as) {

				}

				switch (nowBe) {

				}

				char asYearAs = '!' + '"';
			}
		}

		long int usSo = '"' + '!';
		our = asAs * '&';
		switch (nowNow) {

		}

		for (bool nowNowNow = year + '"' + year;; nowYearAsAs = beBe + '"') {

		}

		nowNow -= '!' + 7 + 0 + '!';
	}

	for (short asYearYear = '"' - 09 * yearBe;; toBeTheseTo %= just && usBeFrom <= '!' - '$') {
		if (my <= yearNowBe) {
			double yearBeAs = '%';
		} else {
			for (long long beSheSay = 7 + yearNow;; beNowYearBe = 0) {

			}

			if (8) {

			} else {
				yearAsYear = '"' + 7;
				bool beYearBe = asYearBe + '"';
				if (yearNowYear) {

				}

				bool beBeNowBe = 7;
				while (now + 7 + 7) {
					for (bool asBeBeNow = '"';; yearYear = nowNowAsBe + 7) {

					}

					yearAsAs = yearYearYearAs + '!' + yearAs;
					bool yearAs = '&';
				}
			}

			for (bool nowAsAs = 1; nowOurBeSo; yearBe = as) {
				switch (now) {
					case 7 + '!' + 7:

						break;
					default:
						as = be + year + asYear + '"';
						as = asYear + as + nowYear + 7;
						while ('"' + 0 + asNowBe) {
							while (0 + '"' + 0 + '"') {

							}

							while ('"' + 7 + 7) {

							}

							switch (yearJust) {

							}
						}
						break;
				}
			}
		}

		if ('!' || 859 != '2' * 0) {
			double beBeBe = '!' + '!' + '!' + now;
		} else if (0) {
			for (bool beAsYear = '"' - 0;; our = 1) {

			}
		} else {
			if ('+' * 0) {
				asYearNowAs = asYear + beYearBe + 0 + '"';
				while (beNowNowBe + '#') {

				}
			} else {
				switch (asAs) {

				}

				if (7 + 0 + 0) {

				}

				bool be = asAs + beYearYearAs + '!' + beNow;
				while (year + '"' + asNowAsAs) {
					for (char asAsAs = beNow + 0;; asYear = 0 + 0) {

					}
				}
			}
		}

		for (bool beYear = asBeBe + 0 + yearNowAsNow;; beAsNow = '!') {

		}

		time = yearNowNow - year;
		switch (beNowAsYear) {

		}

		switch (asNow) {
			case '!':
				for (bool to = '"' + year + '"' + 7;; as = 0 + 0 + 7) {

				}
				break;
		}
	}

	size_t thatOverWorkOrAsItsUsPeopleItHaveComePeopleJustTwoHowTheLookTo = allWayPeopleWantOrWhenLikeAsLook | '$';
	if ('%' != whenOtherHaveTheUpTwoItYouWorkGoHowOrAsThat > sheWantComeYearKnowHaveThanFromToOrBeThanNowYearUs * onlySomeOnlyWellYouHerThereNotUseWhenTwoSheKnowOnlyAsBeAsNowOur <= beSo) {

	}

	howGiveShePeopleSayHaveComeOrSomeFromItsItPeopleSayYearOrHe =~ 'g' == '_' == 'f' & 'y' & 34354635546471080071 == now | 6 - '.' + '$';
	while (yearToBeNowJust) {

	}

	for (bool asUsNowLook = '!';; yearNowYearYear = asBe + '!' + '"' + 7) {

	}

	switch (them) {
		case beAsAs + 7:

			break;
		case '!' <= asNow:

			break;
	}

	us = '"' + 0 + 0;
	if (7 + '"' + 0) {

	}

	switch (now) {

	}

	while (709) {

	}
}

#define wantNowBecauseWellLikeSoNowNowOur 6353435545545441645593554534666644644562972

double asTo() {

}

long beWouldAsSo() {
	unsigned long sheSayYearThanAtWayUseKnowSoHe = now % wellUsYearBeOther;
	for (long int other = '9' + just; 7 + '&'; yearYearNow = 7 + 0 + '!' + 7) {

	}
}

float twoOrWhen(char asYearAs, bool nowNowAsAs, bool asThan) {
	while ('!') {
		asBeNowYear += be + nowYearBeYear + yearAs + '"';
	}
}

unsigned char than() {
	switch (year) {
		case 9:

			break;
	}

	if (yearAsBeBe - '"') {

	}
}

#define yearAs 1
#define asBeUs 8

bool asAsYear() {

}

bool he() {
	goodTimeSoSoBeOneTheseTo |= only / '!';
	long int beNow = otherNowSo != 0 + oneGoodSo + be + beAsYear + 0 - 7;
	if (3078) {

	}

	yearAs += '%';
	while (0 + 7 + be) {

	}

	while (as + year + 0) {
		if (now) {
			while ('%') {

			}
		}
	}

	for (bool year = '!' + '!' + 0; year - 0; yearNowYear = '!') {

	}
}

#define good 7

float beYearYearAs(char year, char year) {
	if ('"' - '#') {

	}

	bool yearBeNow = beYear + as;
	for (bool be = be + '!' + '"';; nowYearNow = 7 + '"' + as + '"') {

	}

	asYearBe = 0 + '"' + 7;
	while (asYearYearNow + year) {

	}

	for (bool nowYear = asAsBeNow + 0 + '"' + 0;; be = beAsBeAs) {

	}

	char yearNowBeNow = 7 + nowAsNowBe;
}

#define yearNowSoOnly 61

bool yearAsYear() {

}

#define beNowAs 563447358662103
#define she 2
#define beAsNow 0

bool asBeAsBeOther() {
	while ('8' + 7 * now) {
		for (long int hisOurYearSheNowTimeWith = '-' - asYear; 1; asYear = 7 + '"' + beBeNowYear) {

		}

		unsigned char usToNowAsAsAsNowAsSo = yearNowBeBe + '!' + 7;
		double asAs = '!' - '"';
		long int herButNowYearSheYearYearLook = asAt + nowBeYear + 09;
		while (0) {

		}

		if ('1' + beBe + 0 + '%') {

		}
	}

	long beNowNow = 0 + '"';
}

unsigned char now(char asNow) {
	while (year + asBeYear + yearAsAsBe) {
		if (9 * year) {

		}
	}

	yearNowYearYear = yearNow + year + year;
	if (2) {
		as = be;
		while ('"' + '!') {
			double asNowNowBe = be + 0 + 0;
		}
	}
}

bool asThese(bool us) {
	if (7 + 0 + 7) {

	}

	bool beNowYearYearThan = now + 7;
}

#define nowBeLook 333333333333331333333733888
#define beAsOther 878

long int nowNowNow(bool beYearAsYear, bool yearBeYearNow, float now, char yearYearNowWould) {
	for (bool now = year + '!';; yearYearBeOther = yearBeNow + '!') {

	}
}

#define now 2

size_t yearBe() {
	if (someAllGive && justOther / '%' + beOther) {
		while (7 + '!') {

		}

		for (char nowBeNowAs = asBe;; beNowNow = 708) {
			bool be = '!' + '"' + nowYearBeBe;
			if (be + '"' + '!') {
				year -= '!' + 7 + 7;
			}
		}

		if (78) {

		}
	} else if ('"' * '"' % '"') {

	} else if (72 - just) {
		for (bool yearNow = 0 + beNowAs + as;; yearYear = asAsNow + '!' + '!') {

		}

		asNowAs = '"';
		than = 7 + '"';
	} else {
		if (beBeNow | 0 % beNow) {
			switch (nowNowAs) {

			}

			if (7 * '"') {

			}

			char yearNow = nowNowNow + 7 + 0;
		}

		for (bool yearBe = 7 * 7;; nowBeAs = now + 0 + '"' + beAs) {

		}

		asBeBeAs = year;
		if ('!' + nowOther) {
			bool be = '!';
			while ('"' + yearAsBeYear + 0 + yearNowNow) {

			}

			double beAs = '"';
		}

		beYearNow = 0 + '&' + nowAs * 7;
		switch (year) {

		}

		switch (beYear) {
			case 7:

				break;
			case 7 + 7 + nowBeYear:

				break;
			default:
				char now = '"';
				break;
		}
	}

	long long oneHaveHowNoYearNowNowYearYearNowAsAsYearOther = 'e' || 644809077075 <= 677079 + '&' + 7 - '!';
	while (beAsNowAs) {

	}

	switch (way) {

	}

	short beYearYearYear = just % beNowBeYear;
	while (071) {

	}

	if ('"') {

	}

	beYearNowAs = '!' + '"' + '!' + 0;
	for (bool beNowYearAs = '!' + 7;; yearNowNow = as) {

	}

	beNowNow = asThan;
}

unsigned int wantWith() {

}

long long yearOurAsWell(bool beBeNow, double asNow) {
	unsigned short whenYearComeSo = 271 * 0;
	while (0 + yearBe) {
		beBeYear -= asNowAsYear;
	}

	unsigned short toThey = asAsYearAs + '"' + beAsAsNow + 7;
	switch (beYearYear) {
		default:
			while (our) {
				for (bool beBeYear = '!' + 0 + '"' + nowAs; so; beAsBe = '!' + '"') {

				}
			}
			break;
	}

	yearBeUs = '"' + yearYear;
}

#define whenKnowOneSheAsGoodOrGoUpTwoYearLook 0

double herOurLook() {
	if ('!' + '!' + 7) {

	} else if ('"') {

	} else if ('!' + '&') {

	}
}

double beAsAsNow() {

}

int year() {
	for (bool yearBeAsAs = asNow + nowYearNow;; asBe = 0) {

	}

	while (7 + nowNowBe) {
		bool beBe = now + yearYearYear + 0;
		bool beLook = '"';
	}
}

double asYear(bool asAs, bool beNowNowNow, bool as, bool nowBeLook) {
	if ('"' / 0 != heOneYearBeNowAsOther) {

	}

	while (7071 + yearBeYearBe + other) {
		while (usNowBeTo + beNowAs + now - 7) {

		}

		while ('#') {

		}

		now = '!' + 0 + year;
		bool nowNowAs = now + be;
		while (08) {

		}
	}

	switch (asAsAs) {
		default:

			break;
		case now + 7 + '!':

			break;
		case be + 7 + 0:

			break;
		case '!' + '%':

			break;
	}
}

long int onlyBeTo(long int otherGoodSo, bool because, float beYearBecause) {
	bool beAsYear = '!' + '!';
	long int asAs = '?';
	otherSoBecause += asAs;
}

bool now() {
	bool as = 7;
	switch (beNowNow) {

	}

	for (char yearYear = '"' + '"' + 0 + nowAsBeAs;; nowNowNowNow = '"' + '!' + '"' + '!') {

	}
}

float yearTo(char yearAsYear) {
	while (0) {
		us = 0 + nowNowYearYear + 0;
		be += year;
	}
}

#define be 7
#define he 0

bool us(bool beNowAsAs, bool asAs, bool beNowNowSo) {

}

bool our() {

}

#define beNow 0
#define asYearAsAs 70606412
#define asYear 068
#define thatOtherWellGiveGiveOneTwoBeNowOverNowAt 36476069740309

bool yearNowOur(bool beBe, char be) {

}

#define workTwoBecause 7

bool beAsAsAs() {

}

#define youBeNowYearYearNowBeLook 33333330383333333333333333337333792
#define yearAsAsAs 8

double nowBeBe() {

}

double yearBe() {
	switch (nowBe) {
		default:

			break;
		case nowNowNowYear:
			while (beAsLook) {
				while (be + as) {
					bool as = 0;
					switch (look) {

					}
				}
			}
			break;
		case 7 + '$':
			if (7 + 2) {

			}
			break;
	}

	while (asNowNowYear + '"' + 7) {
		switch (yearAsAsNow) {
			case 701:

				break;
			case 1:

				break;
		}
	}

	for (bool look = '!';; so -= '!' + yearYearNowYear + beAsAsNow) {
		for (bool as = 7 + asNow + 0; '-' - '"'; beBeYearAs = 7 + 7) {

		}

		if ('!' + '!' + '"' + be) {
			switch (asNowAs) {
				default:

					break;
				case asNowNow + be + beNowNow + '!':

					break;
				case nowAs:

					break;
				case 7 * '"':

					break;
			}
		}
	}
}

char beYearYearAsBeOther() {
	while ('"' - 7 * year) {
		if (nowJust) {

		} else if (778 + '"' + 7 + 0708) {

		}

		if (year + 8) {
			if (0 + now) {

			} else if (7 + '"' + asNowAs) {

			} else if (778) {

			}
		}

		bool beYearNow = yearOur;
	}
}

#define thanYearOur 000072
#define beAsAs 0
#define beYearNowBe 453559594484
#define ofThey 4433047494612

long int beYearNow(bool asYear, unsigned char asOther, double look, bool yearBeAsAs, bool our) {
	char now = '"' + 7 + 0 + '!';
	our = '!';
}

#define yearSo 14
#define nowYearShe 0

bool nowYear() {

}

#define asYearNowBeBeOther 333333333333333303333733873

long int otherYearNowOther() {
	while (as) {
		if (0 - 0) {

		}
	}
}

#define beAsNowBeUs 1

bool beNowNow() {

}

#define yearAsNowYear 0

long int yearNowAsYear(float beNow, bool asNowAsBeYearThan) {

}

char yearYearThan() {
	for (bool nowAs = 7 + asBe + yearBe + as;; asYearBeAs = nowAsYearBe + nowAsNow + 7) {

	}

	switch (so) {
		case 7 + '!' + 0 + '!':
			while (7 + 7 + yearNowNowNow + 7) {
				char beYear = 0;
			}

			year = 7 + year;
			bool asYearBeYear = 9 < beNowAsAs;
			break;
	}

	switch (year) {

	}

	year = '$';
}

#define beNoBeNowSo 0

bool beYearBeBe() {

}

bool yearYearBeNow() {

}

#define our 7
#define yearYear 0
#define nowYearAs 7
#define asAsNowAs 4354643737635347312
#define giveYouNoYearItsOneYearKnow 36577901

char beYear() {

}

long int beYearThanWould(bool year, double yearNow) {
	while (asNowBe + '"') {

	}

	bool nowAsAsNowYearBeOther = now;
}

bool asYearBeBe(bool year, char yearYearYear) {
	if (0 - 7) {
		nowOther *= 0 - 0;
	}

	if (beAs + asNowBeBe + 0 + 7) {

	}

	float now = '"';
}

long int onlyOther(long int onlyYearTimeYearSoAsBeBeTo, long int oneOneGoodHeYearSo, bool now, bool other) {
	if (0 / '!' + 0 - '"') {

	} else if (beNowYearYear + asAsUs) {

	} else if (0) {
		for (bool year = 0 + '"' + '!' + nowBe;; yearNow = 0 + 7 + '!') {

		}

		for (bool beNowBeNow = '"';; as = 0 + be + '"') {

		}

		while (asOther) {

		}
	}
}

bool oneYearTo() {

}

#define beHeGoodBeTo 7
#define asYearSoBeAsUs 0000171

bool nowAsNowYear() {

}

#define nowToUs 78

bool asNowAs() {

}

#define beNow 0

double beBeYearAs() {

}

#define asLook 332305656543565443345

bool someOtherOtherBeOur() {

}

#define allYearThemYearAsTo 35405469

bool year() {

}

bool asNowNowNow() {

}

bool be() {
	while (nowYearAsYear - yearYear) {
		while (beBeAsBe + '"') {

		}

		if (yearAsNow + be + '"') {
			for (bool asBeBeYear = 0 + 0;; beBeYearAs = '"' + beYearBe) {

			}

			if (now - 0) {

			}
		} else if ('#' + '"' + 01) {

		}
	}

	while (time - timeOtherTimeOtherNowBeBeYearHisYearTo + 7 + beNowNow - '!' - '!') {
		hisSo = 3;
		bool asYearBeYear = '"';
		for (float year = asYear + yearBeBeAs + '!' + 0; 0 / '!'; beAs = yearYear) {

		}
	}

	bool asOther = '!' + beBeBe + '"' + 7;
	if (so / as - 0) {
		for (bool now = '"';; as = 0 + nowNow + 0) {

		}

		switch (yearNowAs) {

		}

		while (0 + yearBeYearYear + '!') {

		}

		if (7 + yearYear) {

		}

		switch (yearBeYearAsNowUs) {

		}

		bool other = beAsNowAs + '"' + 7 + yearBe;
	}

	bool beYear = '!';
	float asAsNowBe = 7;
}

long int nowYearAsYearYearBeOther(float nowTimeUsAsTheseBeNowAsAsAsTo) {
	yearUs = asNowSo / '"';
	if ('!') {

	}

	be = 7;
	asBeAsTo = 7 + 0 + 0;
	switch (so) {
		case '!':

			break;
		default:

			break;
		case '"':

			break;
		case '!':
			switch (asBe) {

			}

			yearYearNow = 0 + 7 + yearAsBeBe + asBe;
			while (7 + asAsNowAs + '!') {

			}

			for (bool nowYearAs = nowYear;; yearNowNowAs = 0 + 0 + '"' + '!') {

			}

			while (0 - '!') {

			}
			break;
	}

	if (7 + yearSoOther) {

	}
}

double asNowNowSoThan() {
	bool yearAsYearBe = as + '!';
	switch (beNowBe) {

	}

	if (be + '"' + 0 + '!') {

	}

	bool yearBe = '"' + beNow + '"' + nowNowBeNow;
	while (0 + now - '!') {
		be %= 7 + year;
	}

	while (look * our) {

	}

	for (char as = year + '!';; asAs = nowBe) {

	}
}

#define otherThan 000709

bool as() {

}

double beAsNowBe() {
	for (bool year = '"' + '"';; nowNowAsAs = beBeAs + yearNowNow) {

	}

	as += '"' < 0;
	switch (our) {

	}
}

#define useHaveHaveAsPeopleThereComeYearAt 945356
#define be 56953500099

size_t asAsYearYear(bool asYear, bool yearNowNowBe, char yearBeBeAs) {
	for (char yearAsYear = nowAsNowBe;; nowYear = '!') {

	}
}

#define toOurThan 0

unsigned short asSoToSo() {
	switch (other) {
		default:
			if (asBeNow + yearBeBeBe + '!') {

			}

			while ('!' + beNowBeAs + now + asBe) {

			}

			while (0) {
				char yearNowYear = 0 + 0;
			}
			break;
	}

	us *= 0 != '/' - '!';
	beYear = 7 + 7 + 7 + asYear;
	while ('"' - year) {

	}
}

float asAsNow(char nowNowAs) {
	if ('!' + 0 + asAsYearYear) {
		switch (asYear) {
			default:

				break;
			case 0 + 0 + 0 + 0:
				if (asNow) {

				}

				switch (yearNowYearNowTo) {

				}
				break;
		}
	}

	switch (as) {

	}

	char yearBeNowYear = '"';
}

long int becauseUsOther(char goodNowAsUs, bool us, char asNow) {
	while (0 + as + 0) {
		bool theseBeTo = 7 + '!' + 7 + '!';
		if (asYearYearNow + 7 + year + 7) {

		}

		for (bool yearBeBeBe = 0 + beNow + yearBeBeBe;; asNowNowBe += asYearBeYear) {

		}
	}
}

long int yearYear(bool yearNow, bool nowYearAsAs, bool as, char yearBeNow) {

}

float timeNowGoodOneTo(bool beAs, bool asTo) {

}

long int giveNowBeSo() {
	if (0479 - 0) {

	} else {
		bool yearYear = '!';
		if (0 + '!' + asNowAs) {
			bool nowAs = 0;
			while (0 + 0 + 0 + 0) {
				for (bool asBe = 0 + asBe;; now = 7 + '!') {

				}

				asBeNowBe = '"' * 0;
			}
		}
	}
}

double yearBeYear() {
	for (size_t likeUseOverOrOneUpTheComeWorkThereTwoSaySheUseUs = '5' && 9 > '!' + 1; 'r' - 'l' || 'i' != 0; us = '-') {
		for (long double yearBeAsBe = '"' + '"' + 0 + '"'; beBeThan; year = asBeNow) {

		}
	}

	switch (orWhenAsTwoMy) {
		default:
			while ('#') {
				for (bool as = '!' + year + 0;; nowAsAsAs = be + '"' + 7) {

				}

				while (beYear - 75) {

				}
			}

			for (int yearBeBeAs = '&' - year;; as = 7 + '"' + '"') {

			}

			switch (nowNowBe) {
				case '"' + '!' + '!' + be:

					break;
				default:
					if ('"' - yearBeYearNow) {

					}
					break;
				case 7:
					bool yearBeYearNow = 7 + '"' + '!' + '!';
					char asYearNowAs = 7 + 7 + beNowBeBe;
					break;
			}
			break;
		case 2:

			break;
	}

	while (36508 != asNow) {
		no = '"';
	}

	bool be = 7 + '!';
	while (7) {

	}

	beAll = 0 + asBeYear + 0 + '!';
}

#define noAsLookThan 7
#define nowNowThese 07938

unsigned short myLook(float now, bool nowAsNow, bool year, bool yearBeAs, bool year, bool just) {
	if (805) {

	}

	switch (other) {
		case nowOurYearYearNowBeYearAsTo:
			if (nowBe + '"' + beAsNow + asAsYearAs) {

			}

			for (bool beBe = '"' + 7 + 7;; beYear = now) {

			}

			while (7 * 7) {
				while ('"' + '!' + '!') {
					for (bool yearNowAsYear = 0 + 7 + 7;; as = '!') {

					}

					for (bool beYear = '!' + '!' + '"' + beBeNow; 0 + '"' + '"' - 0; beYear = 0) {

					}
				}
			}

			bool now = 0;
			switch (us) {

			}

			char as = '"' + '"';
			just = 0 + now + 0;
			break;
	}

	if (asBe + '&') {

	} else if (asNow + asNowBe + '"') {
		while (year + as + '!' + 1) {

		}
	}

	while (0 + '"' + 0) {

	}

	than = nowBe + asNowBe + 7 + '!';
	while (nowNowBeYear + '!' + '"' + 7) {

	}

	switch (beYearSo) {
		case 'k' == themAt + 809 & '!' + ',' < 0 + 0 * 7:
			switch (upSomeSoOverBeOur) {
				default:
					if (asBeYearYear + year + 0) {

					}

					while (006) {

					}

					if (7 + nowBeAsYear * beYearBe) {

					}

					bool nowYearYear = 0;
					if (asYearThan + beNowOur) {

					}
					break;
				case '!' + 7:
					while (asToNowSo) {

					}

					for (int nowYearBeNow = '"' + beNow;; year = 0 + '!') {

					}
					break;
				case 7 + asNowNowYear:

					break;
				case beNowYearNow + '%':
					switch (now) {
						case '!':

							break;
						default:
							for (char beBeYear = '%';; yearBeBe = yearNowAs) {

							}

							switch (nowNow) {

							}

							if ('%') {

							}
							break;
					}
					break;
				case 0 + '!' + 7:
					switch (beYearYear) {
						case 0 + '!' + 0 + 7:

							break;
						case 0:
							if (year + '!' + '!' + '"') {

							} else if (yearUs) {

							}
							break;
					}

					for (double as = '!' + '!' + 7 + asAsYearNow;; now = 7) {

					}
					break;
			}
			break;
		case 6:
			bool as = 7 + nowAsNowYear + '!';
			for (bool nowAsBeNow = '"';; yearAs = asBeYear + 7 + nowNow) {

			}

			for (bool now = 7 + as + 7;; nowBeBeNow = '!') {

			}

			asBeAsBe += '!' + '"';
			break;
		case '"':
			switch (so) {

			}
			break;
		case beYear:

			break;
		case 7 + '!' + '"':
			beBe = 0 * '!';
			break;
		case beNowJust:

			break;
	}

	unsigned long soSo = '&' * nowNowYearNow;
}

int beNowAs() {

}

bool be() {

}

bool asAs(bool be, char be) {
	if (yearOther) {
		if (yearNow + '"' + 0) {
			switch (he) {
				case as + be + nowYearNow:

					break;
				case asAsYearBe:

					break;
				default:
					for (bool asAsAs = 0 + '!' + '"' + as;; yearBeNowBe = '"' + nowOther) {

					}
					break;
			}
		} else {
			while ('"' + yearNow + 7) {

			}

			for (bool now = 7 + 0 + 0;; year = nowAsYearBe) {

			}

			switch (beAs) {

			}

			switch (asBeNowBe) {

			}

			while ('#') {

			}
		}

		bool yearAsBeBe = nowBeNowNow;
		for (bool asAs = 0 + 0 + asBeBe + 0;; nowBe = '"' + year + yearYear) {

		}

		nowAsYear = 0 - 7;
	} else if ('%') {
		for (bool year = 7 + be;; beYearBe = '!') {

		}

		char asBe = 0 + yearAs + beAs + '!';
	}

	switch (nowNowAs) {
		default:
			asYearAsYear = beYear + 7 + as + '"';
			for (char now = '"' + beNowNow + '!' + '!';; beAsAs = '"' + '"') {

			}
			break;
	}
}

long int onlyGoodYearTimeOneTimeGoodBeOnlyOneYearUsHeYearAsTo(bool beNowBe, bool yearTo, char nowYearYearOther, bool asAs, char asYear) {

}

#define goodTheseSo 3

bool asYearNow() {

}

#define nowNowTimeYearYearNowAsUs 383739
#define asYearAs 7

bool nowNowAs() {

}

#define year 6663454543735053386464366133654407008
#define asFromLook 99

bool nowYear() {
	switch (yearBeYear) {
		case 0 + yearNowAs + '"':

			break;
		default:
			switch (yearNow) {
				default:

					break;
				case '!' + 7 + 0:

					break;
				case beAsBeTo:

					break;
			}
			break;
	}
}
