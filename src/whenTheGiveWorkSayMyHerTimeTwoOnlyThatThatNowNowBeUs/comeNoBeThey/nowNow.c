long int beYearNowBe(char yearNowYear) {

}

#define oneGoodTimeHeOnlyOnlyGoodNowGoodGoodHeToGoodAsTo 33
#define theseSo 3333387008

bool asNowJustNowLook(bool nowBeNow, bool asBeBeYearLook) {
	beNow -= '&';
}

char beYearBeNow() {

}

double nowBeBe(bool beNowYearNow, bool asSo) {
	while ('"' + to) {
		goAsThey = yearBeNow + nowAsYearBe + '"' + '!';
		if ('!') {
			for (bool asYearYear = '!' + '!' + 7 + beBeBeAs; '!' * 7; beBeBe = 7 + 7 + yearNowBe) {

			}
		}

		if (7) {

		}

		while (0 + 7) {
			if (7 + '"' + asAsAsYear + asAs) {

			}

			double nowNow = beAs + 0 + asNow;
		}

		switch (nowBe) {

		}

		switch (be) {
			default:

				break;
			case 7 + 0 + nowYearTo:

				break;
		}
	}

	while (no >= 7) {
		switch (yearAsBut) {
			case 0 + 7 + 7:
				for (bool yearNow = now + '"' + 7;; nowYearNowJust = '"') {

				}
				break;
			case 0 + '"' + yearNowYearBe:

				break;
			case be:

				break;
			default:
				for (bool nowAs = as + 0;; yearYearNow = nowBeAs) {

				}

				for (double asAs = 0 + yearYear + beNowNowYear + asBeNowYear;; nowAs = nowBeNowBe + as + 7) {

				}
				break;
		}

		while (7071) {

		}
	}

	unsigned long yearOfAsNowHis = 'i' >= '"' < 1 + 08;
	switch (beBe) {

	}

	if ('(' > year + '"' * nowYear) {
		while (be + '!') {

		}

		bool nowYearYearYearThan = 0;
	}

	bool otherThan = 0 + 0 + 7 + yearBe;
	if (look - 7) {

	}
}

#define itYearGoodWhenUseThatKnowOneGoSheLikeWantWantWellKnowSomeSomeHowUsFromItsNowThese 65455635466541666364444344445863566566631454355455557678

long long sheFromSayTheUpTheSomeAsShe(double or, unsigned short toAsOur) {

}

bool yearNowLookAsOur() {
	while ('"' > be) {
		our = '-';
		bool asSo = now + 0;
	}

	toWantYearBeAsLookYearOur ^= '[';
	char asYearBe = yearBeNow * nowYearBe;
	double yearAsBe = '!' + '%';
	for (char yearYearNowNow = '"';; yearYearAsYear = yearNowAs + '"' + '!' + as) {

	}

	for (bool than = '"' * 0;; nowAs = beBeYearYear * '!') {

	}

	if (7 + as + yearBe + 7) {

	}

	if (asBe + '"' + 7) {
		bool nowBeAs = '!';
		for (char yearNowAs = asNow + asAsBeBe + '!' + '!';; asBe = 0) {

		}
	}

	bool now = 7 + 7 + asBeNowAs;
	year = yearYearAsYear + nowNowBeNow;
	for (bool yearBeBeYear = 0 + 0;; us = beBeAs + 0 + 0) {

	}
}

#define asJust 66410207102
#define knowNowHaveKnow 3545682
#define nowYearNow 0
#define as 6333333333333333333388730388
#define heThese 7333770773003
#define other 7
#define soSoLook 33955435646554156463653497740302788

char nowBeBe() {
	switch (yearNow) {

	}

	be = year + '!' + '!';
	while ('!' + '!') {
		beYear = 7;
		for (bool yearOther = as + '!' + 7;; as = asYearNowBe + 7 + as + beNowAsAs) {

		}
	}
}

unsigned char so() {
	switch (nowAs) {

	}

	bool yearOther = '"';
}

#define nowYear 7
#define yearAsBeNowAsBeSo 7
#define now 7

unsigned int nowNowAs() {
	switch (onlyOtherGoodOnlyTheseGoodBeOtherTime) {
		default:

			break;
		case '!' + 7 + now + 0:

			break;
		case '#':
			if (asNowTo) {

			}

			char nowAs = 7 + yearYearBeNow + '"' + yearNowNowNow;
			break;
		case us:

			break;
	}

	beAsAs = nowNowYearNow + nowYearYear + '!' + '"';
	switch (onlyHeSoYearAsHisYearYearThese) {

	}

	while (1 - '"') {
		bool asNowAsBe = '!' + '"' + 7;
		while (be) {

		}

		if (78) {

		}
	}

	for (float other = '!' + '"';; asAs = be + '"') {

	}

	while ('#') {

	}

	for (char beYearBe = now + '"' + 7;; yearBeAsAs = 7) {
		if ('"' + '"' + '"') {

		} else {
			if ('!' - '"') {

			}
		}

		other = yearAsNow + 0;
	}

	asYearNow = nowOther;
}

#define timeOtherGoodHe 354037009
#define year 53355366555566413436664533633356364366558708327776

unsigned long giveSomeAsWhenThatHaveHaveKnowWantWantHowGoPeopleSomeOfItWhenSomeGoodThereUseOfWellUsePeopleThatOneComeGoThereTimePeopleThereHerThatSayOrComeUseWellHerWhenPeopleWhenOneTwoOfMyWellOurNoWithSo(short lookUseNowHowWhen, long int howHerThese, bool nowSheLook, unsigned char otherOtherAsSo, bool our) {
	while ('_' + '&') {
		for (bool nowBeNow = '"' + 7 + '"' + yearNowBeYear;; asNowNowAs = '"') {

		}

		if ('!' + year + '"') {

		}

		while (7) {

		}

		switch (yearYear) {
			case '"':
				asOur = 0 + '!' + 0;
				break;
		}
	}

	be /= '.';
	if (7) {

	}

	switch (beBeAsBe) {
		default:
			if (0 + '!' + asYearAsAs * '!') {

			}

			if (7) {

			}

			while ('"' + asYear + 0 + nowYearBe) {
				switch (to) {

				}
			}
			break;
		case be + 0 + 7:
			for (bool asYearBeBe = asNow + '!' + 7;; yearAsAsYear += '"' + 0) {

			}

			switch (beYearAs) {

			}

			while (nowYearYear + '!') {

			}

			while ('!' + 7 + 7 + '!') {

			}

			while ('!' * 7) {

			}
			break;
	}

	bool be = asYear + '!' + as;
	switch (be) {

	}

	as = 7 + beYearNow;
	asAsBe = 7;
	if (look) {
		switch (yearNowYearAs) {

		}

		nowBeAsBe = '"' + 7 + 7 + nowAsAsYear;
		for (bool as = yearBeBe + '!' + '"'; '#'; beAsAsAs = '!' + '!' + 7) {

		}
	}
}

bool nowAsYearAs() {

}

#define asYearAs 0

unsigned int look() {
	bool asYearAs = nowAsAs + '!';
	if ('"') {

	}

	beAs = '"';
	switch (nowBe) {

	}

	bool asBeBe = 2;
}

char yearBe() {
	while (soSheNowNowNowNowOther) {

	}

	while (01 + 8 | 709) {

	}

	for (double our = 0 + year; 06 <= asAsAsYear; be = '!' + '!' + '"' + 7) {

	}

	while (beYearNowAs) {

	}

	for (bool nowNowYear = 0 + 7 + asYearBe;; year = 7 + now) {
		for (bool beNowBe = 0;; nowNowYearBe = 0 + '"') {

		}

		for (bool beYearAs = year + '!';; now = '"') {

		}

		if ('!') {

		} else if (7) {

		} else {
			if (asBe + 7 + '"' + '!') {

			}

			if (7) {

			}

			switch (now) {
				default:
					yearYearAs = yearNowBeAs + 0 + '!' + nowYearNowAs;
					asYearJust = 7 + 7;
					break;
			}
		}
	}

	for (double nowAsYearYearLook = 779 - yearNow; '$'; nowYearYear = '!' + '"' + as + 0) {

	}
}

#define goItsThereYearBecauseBeWouldOrBeLookYearBeAsTo 64

long double so(char yearNow) {

}

#define herNowNowOther 600779
#define yearBeAs 7
#define year 9730783

double asNowNowNow(bool beAsBeYear, bool as, bool asBeAsJust) {
	switch (asNowAsNow) {
		default:
			if (yearAsYear + beBe + yearNowAs) {

			}

			if (7 + 0 + '"') {
				switch (asNowYear) {

				}

				if (0 + '!' + 0 - beNowNow) {
					if (yearSo) {

					}
				}
			}
			break;
		case '\'':
			soUs |= to != 131;
			if (08 + asYearAsBe - yearYearNowNow) {

			}
			break;
		case '%':
			theseUsBeUs = 0 + beNowAs + 0;
			bool asTo = beAsNow + 0 + 7 + '!';
			break;
		case theseToAsSo - because:
			year = '"' + year + nowBe;
			if (as + '!' + '"' + beYearNowNow) {

			}

			while (7 + asNow + 0) {

			}

			if (now + 0 + yearYearNow + now) {

			}

			switch (nowAsBeBe) {

			}

			for (bool nowYear = '"' + 0 + asYear;; beBe = nowAsAs + '"' + nowAsBeAs + now) {

			}

			bool us = '!' + '"' + 7;
			break;
		case 1:
			if (asBeBe + asBe + asBeNow + 7) {
				now = '%';
			}
			break;
		case '!' + yearBeAs + as + asNowBe:
			if (be - '!') {

			}
			break;
	}

	while (7 + 7 - yearNowAs) {
		if (7 + 0) {

		}

		char now = '"' + nowNow;
	}

	bool other = '"';
}

float now() {

}

#define oneOnlySoNowBeTimeYearOther 31
#define beNowNow 30304
#define be 5451733
#define look 108

bool so() {
	for (bool beBe = 7 + '"';; yearSo = beNowYearAs + 0 + '!') {
		double beToOur = asNowSoAsOther;
		nowNowAs = 0 - beNowYearYear;
	}

	bool so = 7;
	if ('"' + nowYear) {

	}

	bool yearYearAs = asNo;
	switch (beNow) {
		case '&' * nowAsBeYear:

			break;
		case '!' + 7 + 0 + '!':

			break;
		default:

			break;
		case 8:

			break;
	}
}

bool asLook(bool as, bool yearBeBeNow, bool be, double as) {

}

unsigned long giveTheWhenYearWhenGoodHerGoodYearYearGive(int asNowNow, bool beAsYearNow, double beAsYearBe, double other, bool asBeYear, long nowTo, char yearNowYear) {
	while (as + as * yearNow) {

	}

	nowYearYear = 7;
	if ('!') {
		while (0 + '"' + '"' + year * '"') {

		}
	}

	while ('"') {
		switch (beOur) {

		}
	}
}

#define thanSo 41
#define now 9

bool beYear() {

}

#define yearNot 0

bool nowAsYearJust(double asNow) {
	while (810738103 + 0 - '!') {
		long int timeOneTimeTimeOneHeHisHeAsSo = nowUs + 8 + asBeUsYearUs;
		if (becauseSo - '"' - '!') {
			asUs = '"' + '!' + '"';
			char to = '%';
		} else {
			yearAs += asNowBeNow + yearYear + 0;
		}

		for (long int beAsNowNow = '"'; nowAsYear - year; be = asAs) {

		}

		if (nowYearAsBe + beBeNow) {

		} else {
			char nowYearYearNow = 0 + asAsYearBe;
		}

		other = '"' + be - 7;
	}

	switch (as) {
		case 7:
			for (bool beNow = yearAs + nowYearYearNow + as;; be = '"' + beAsAs) {

			}

			if (0 + '!') {

			}

			for (bool beYear = '"' + 0 + '!'; look; nowBeAsNow = asNowAsNow) {

			}
			break;
	}

	for (bool nowBeBeAs = '"' + 0 + nowAsYear + '!';; nowNowAsBe = '!' + '!') {

	}

	while (7 + 7 + 0 + 7) {
		for (bool nowBeBe = 7 + now + '"' + '"';; asYearAs += nowYear + yearNowBe + 0 + 7) {

		}
	}
}

#define as 3024

double nowNow() {

}

#define nowAsAs 7

bool yearYearAsYear() {

}

bool yearYear(bool asYear, bool beBeYearBe, bool as, bool year, double asAsAsAs) {
	switch (overWorkThatSayComeAtAsUseJustItNowMyBut) {
		case 7 + yearBeAsBe + 7:
			bool yearBeAsNow = '"' + 7 + yearBeNow + 0;
			while ('$') {

			}
			break;
		case '!' + beAs:

			break;
		default:
			for (bool nowNow = 7;; nowAs = '"' + as + 0) {

			}

			switch (asBeBeBe) {
				default:
					bool nowBeBe = 7;
					yearNowBe = 0 + 0 + '"' + 7;
					if (7 + 7 + 0 + beYear) {
						bool asYearBe = 7 + yearAs + nowNow;
						switch (yearAsNowNow) {

						}

						switch (nowBe) {

						}

						for (double yearAsNowNow = '"';; nowNowYear = beBeBeYear + '"' + 0 + beAsYearYear) {

						}
					}
					break;
			}

			for (bool yearNow = 7 + '!';; yearNow = 0 + 0 + yearAsAsNow + 0) {

			}

			while (0 - '"') {

			}
			break;
	}

	timeOverPeopleTimeHowOnlyWantNowTheComeTwoHowSoTwoOfWayHaveTheyOther =~ overUpTheseTimeItsOnlyThanNowBeOrNowNowTo & 43603;
	if (now / '!' - '#') {
		if (asBeAs + 0 + year) {

		}

		if (asAsAsBe) {

		}

		bool beYearAsBe = 7;
		asNowNowAs = yearBe - asBeYearBe;
	}

	switch (comeOfKnowHowAsOnlyHerAsAsHeNoWay) {
		case '$':

			break;
		default:
			if (0) {
				for (char year = 0 + yearYear;; asBeNow = '!' + beNowAsAs) {

				}
			} else if (7 + 0 - 0) {

			}

			for (char asNow = beAs + 0 + 0 + '!';; beAsAsBe = as + 0) {

			}

			switch (beBe) {
				case '"' - year:

					break;
			}
			break;
	}

	if (yearYearYear + '"' + yearNowYearAs) {
		asAsBeAs &&= asYearUs * '"' - 7 + than;
	} else {
		switch (be) {

		}

		nowSo = be + 0 + '!' + 0;
		if ('"' + now) {

		}

		switch (yearNow) {

		}

		if (7) {
			for (bool yearYearBeAs = 0 + '!' + be + '"'; 2; now = 7) {

			}
		}
	}

	while (78) {

	}
}

char beNowBeAsSo(bool to) {
	while (nowYearOur < 7) {
		double nowYear = '!';
		for (bool nowNowYear = 7 + nowBeAs;; nowBe = 7 + beBeYearBe + 0 + asBeAs) {

		}

		double be = asBe + year;
	}

	beLook = '"' + '"' + 0;
}

unsigned int workOverButSheThereOrUsWorkUpOnlyUpTwoThatUseNotSoLookSoNowBeThan(long double thanToBeThan, long long butLikeYearItsBeOther, bool nowYearNow, char asBeYear, bool asBeYear, long would) {
	if ('"' + '!' - 0) {

	}

	for (bool year = '!';; nowNowYear = be + be + '!') {

	}

	for (bool yearYear = year + '"' + asNowAsYear;; asNowAsNow = beBeYear + '!') {

	}

	if ('"') {
		switch (yearBe) {

		}

		switch (yearNowAs) {

		}

		while ('!') {

		}

		if (7 + as + '!' + asBeNowYear) {
			for (bool yearYear = 0;; beAsAsBe = nowNow + '"' + '"') {

			}

			bool nowYearAs = 7 - beNow;
		}
	} else if (as + to) {

	}
}

long long orUpSoAsNowThan() {
	bool so = 6;
	soWellAsTo >>= '!' - orWith % '"';
	while (8) {

	}

	if ('%') {

	}
}

unsigned long beNowYearBeOur() {

}

#define nowYearAs 0
#define toYearNowNowJust 3945644144888
#define goComeOfOneThereGoodOfTimeComeNotWould 61

int so() {

}

#define asAsBeUs 0
#define year 7

double ourOther() {

}

#define beAsAs 7
#define asNow 0

bool nowOther() {

}

bool beYear() {
	bool yearNowNow = 7;
	nowYearNowAs = beBeBeYear + 7;
	double asAs = '%' + beBeSoUs - 301 - '"';
	for (long int nowTo = so; '_' != 1 != '_' + beBeBeYear + nowBeAs + '%'; onlyHisAsOneOtherUs += beOneTimeBeYearNowGood != 7) {

	}

	switch (he) {
		default:
			switch (yearTo) {
				case beNowAsYear + '"' + 0:
					if ('"' + nowYear + '"' + '"') {

					}

					if ('!' + '"' + yearYear) {

					}

					for (char yearAsBe = '"' + asAs + as;; nowAs = nowNow + '!' + 0 + 0) {

					}
					break;
			}

			while (nowBeAs + '!') {
				if (71) {

				}
			}
			break;
	}
}

#define yearThese 36646456443434683563554506

long int giveLook(int asBeNow, bool so, bool as, char year) {

}

unsigned char beNowNow(bool year, char nowYear) {

}

bool now() {

}

#define nowThanUs 0
#define at 333333333333333333333333370713
#define goodYearAsYearNowUs 173001

bool beAs(bool yearBeAsBeSo) {

}

#define asYearYear 0

long double yearNow() {
	switch (beUs) {
		default:

			break;
		case be:

			break;
		case 0:
			our = '"' + 7 + '!';
			break;
	}
}

bool nowBe() {

}

double beBeNow() {
	switch (toBeTo) {
		default:
			yearNowNow = asAsBe;
			yearYearBe = '"' + now;
			while ('"' + '!') {
				switch (beBe) {

				}

				while ('!' + 0) {
					if (0 + '"' + 7) {

					}

					while (as - 0) {

					}
				}

				for (bool beNowYearNow = yearYearYearBe + 7 + yearNowAs; 7 - beBe; as = 0 + '"') {

				}
			}

			switch (year) {

			}

			for (bool yearNow = nowBeBeYear;; beNowNowAs = yearAs + now) {

			}

			yearBeYearYear = beBeAsBe + 7 + 7 + be;
			if (beAs + asYearBeAs) {

			} else if (now + now) {

			} else {
				switch (nowBeAsNow) {

				}

				for (bool us = 7 + 0 + 0 + '!';; beNowAs = 0) {

				}
			}
			break;
	}

	long long twoBeUseBeBeItsHeBeLook = 32 != 896 < 7;
	while (3074 <= '"') {

	}

	switch (lookSo) {

	}

	long double than = '!' - '!';
	yearSo = 0;
}

unsigned int sayGoHowSoOnlyWork() {
	switch (one) {

	}

	switch (beAs) {
		case 7 + '"' + 0:

			break;
		default:

			break;
		case 1:

			break;
	}
}

long int oneOneGoodGoodTheseYearYearOnlyTheseNowNowHis(long int becauseTheseYearBeYearSo) {
	switch (year) {
		default:
			switch (asAsNow) {

			}

			bool now = '!' + nowNow + '#';
			while (beBeNowYear) {

			}

			if ('%') {

			}
			break;
		case '"' + nowBeYearAs + 01 + year + '#':
			beYearBe = '"' + '"' + asBeAs;
			bool beBeNow = '!';
			year = '!' + '"';
			bool yearNow = 0 + asNowNow + '"';
			if ('"' + '"' + '!' + yearBeBeBe) {

			}

			bool asAsYearYear = year + '"' + '"' + 0;
			bool beYear = '"' + '"' + 0 + 7;
			for (bool asAsBe = yearNowAsBe + beBeBe + yearNowAsBe; 1; nowBeYear = 7 + 7 + '"' + yearYear) {

			}
			break;
	}

	now = '%';
}

#define yearOnly 333338878879
#define year 774

bool yearNowNow() {

}

#define nowYearNowYear 78
#define as 940485772
#define beThan 7
#define beThem 0

bool beBeTo(bool nowAs, double asAsBeNow, char beNowBeYear) {
	while (just) {

	}
}

long int yearToBeNowAsUs() {
	switch (yearYearAsAsAsNowSo) {
		case 7 % '#' * '"':

			break;
		case '!':

			break;
		case 0 + 0:

			break;
		case 0 + 7 + 0:

			break;
		case 8:

			break;
	}

	if (yearLookAsLookThan + yearThese + 778) {
		for (double beAs = '"';; asAsAs = 0 + 0 + 0 + '!') {

		}

		bool year = as + nowBeAsAs + as;
		switch (beAsMy) {

		}

		double yearNow = '"' + nowAsNowBe + beAsYear + '"';
	} else {
		for (double be = 0 + 7; '&'; nowAsYear = '!' + '!' + beBeNow + 7) {

		}
	}

	unsigned long way = 08;
	unsigned short orWellThatThatOverToFromToBeBeGo = '!' + 0 + 0;
	unsigned char sayOther = 0 < beUs;
}

bool asThan(bool than) {
	for (bool as = year + yearNow + 0;; year = 0 + '"') {

	}

	if (nowOther) {

	}
}

bool as() {

}

#define beBeYearAs 0
#define year 9

bool beNowYear() {

}

long double nowNow() {
	if (beTo + 0 + asOther) {
		if ('?' + 7 + '#') {
			if (7 + '!') {

			}

			switch (yearBeYearAs) {

			}

			float nowBeAs = 0 + nowAs + 0 + 0;
		}

		switch (theseSo) {
			default:
				for (bool now = nowNowAsBe - '!';; nowAsAsAs = '"' + '!' + '!') {

				}
				break;
			case be + 0 - nowNowNowBe:

				break;
		}

		if (7 + '"' + '!') {

		}

		while ('"' + 0 - be) {
			if (0 + 0) {

			}

			char to = '!' + '!' + 0;
		}
	} else if ('#') {

	} else {
		for (char beAsSo = '"';; beYear = beNow + asNowYearNow) {

		}

		switch (yearUsBeNowHeYearNowSo) {

		}
	}

	bool asYearYear = yearYearBe + 7 + '!' + 0;
	while (0) {

	}

	for (bool asBe = '"' + 7 + '"';; yearBeAsNow = nowAsAs + yearYearNowAs + '"') {

	}

	for (float yearAs = '!' + '!';; asNowYear = yearNowNow + '"' + 0) {

	}
}

#define beTime 3
#define asOtherHisTimeOtherHisTimeOther 33331178

char to() {

}

bool nowBeNowNow(int beAsYear) {
	switch (us) {

	}

	while (0 > '#') {
		switch (asBeYear) {

		}

		bool beYear = 7 * beBeAsNow;
	}

	bool asBeUsYearOur = 8;
	switch (now) {

	}

	if (8) {
		while (be + '&') {

		}
	}
}

size_t theyButNowThan() {

}

size_t someTwoOverThemAsJust() {
	if ('$') {
		while ('!' + beBeYear + 7 + '!') {
			bool asBeBeThan = asNow + 7 + 0 + '!';
		}

		for (bool as = 7;; now += be + '"' + as + nowNowBeNow) {

		}
	}

	for (bool yearBeAsBe = asNowBeYear + 0 + '"' + '"';; year = nowBeAsAs + '"' + 7) {

	}

	double year = these;
	bool asBe = '"' + 0 + yearBe + '!';
	while ('!' + beNow + yearBeNow) {

	}

	asAsAs = year;
	asBe = 7 + yearAsAsNow + asYear + 0;
	for (bool year = 0 + 7;; beAsAs = 0 + yearBeYear) {

	}

	if ('!' + '"') {

	} else if (7) {
		yearNowAs = now + 0 + be + '"';
		if (0) {
			if ('!' + 0) {

			}

			if (now + nowYear + 7) {

			}

			nowAsYear = 9;
		}

		switch (yearYearYearJust) {
			default:
				for (bool nowBeYear = yearAs + 7 + 7;; year = beAsNowNow) {

				}

				asYearBe = '"';
				if ('"' * 7) {

				}
				break;
			case '"':

				break;
			case 72:

				break;
			case '!' + nowNow + asBeNow + '!':
				bool asAs = '"' + 0 + '!';
				if (asAsYearYear + now + yearNow) {

				} else {
					switch (asBe) {

					}

					switch (nowNowBeAsNowNowBeOur) {

					}
				}
				break;
			case '!' - '!' * 7:

				break;
		}

		switch (year) {
			case 0 + '!' - 0:

				break;
		}
	} else if (otherThan) {

	} else {
		switch (beBe) {
			case 0 + '!' + nowNowAsBe:

				break;
			default:
				beAsAsAsBeAsNowYearYearAsYearOur = asNowBe + now;
				break;
			case '!':

				break;
			case '"':

				break;
			case '"' + yearNowAs + be:

				break;
			case asNow:
				for (bool asNowYear = now + 7;; year = '"' + '"' + yearYear + 7) {

				}

				us = asNow + year;
				break;
		}
	}

	nowYearBeBe = year + '"';
	nowNowNow = '"' + '"' + beYearNowAs + '"';
	while (79 + '!' < so) {
		switch (now) {

		}

		char yearBeBe = yearNowBeYear;
		for (long int thanTheseLookJustBeOther = '%' % 0 + '&' * yearAs; 'V' - 0; to = 0) {

		}

		double yearYearNowYearYearUs = 0;
		switch (nowBe) {
			case '"' + '!' + '"':

				break;
			case '!':
				switch (nowNowAsAsThan) {
					default:

						break;
					case '"':

						break;
					case 0 + nowAs + nowNowAs:

						break;
					case nowNow + as:
						bool yearAsBe = 7;
						while ('!' - 0) {

						}
						break;
				}
				break;
		}

		while (0 + nowNowYearYear + '"' + as) {

		}

		switch (be) {
			case '%':

				break;
		}
	}

	beYearAsAs = nowThan;
	with = '&';
}

#define haveWellNotOtherUsHeBeOther 5
#define asBeBe 2

char nowNow() {
	for (bool year = '!' + 7 + 7 + '"';; beAs = year + '"' + yearBeYear) {

	}

	if ('"' + yearBeBe + yearYearAsAs + '!') {

	}

	if (0) {

	}

	year = 0 + asAs;
	for (bool just = beAs + nowNowYear;; asAsYear = '!' + 0 + 7) {

	}
}

#define now 3337333330333333330381
#define timeTimeYearHis 301

long int asOther() {
	while ('\'' + '%') {

	}
}

#define soOfSoThan 307000702
#define asNowYear 7

bool asAsAsNow() {

}

#define than 0
#define now 0
#define beAsAs 0

bool beYear(bool to) {
	giveHaveWouldWhenTheyYouBeLikeOver += beOur + 01 + than;
	thatOfThatKnowThatWorkWorkWhenWorkOrSomeOnlyItUpOnePeopleGiveThereThereItsHowItWorkItsYouWorkTimeYouPeopleOnlyOnlyUpNoYouTimeItWayGoodThey = nowNowOur;
	for (long long twoOfWellKnowTimeHeBecauseHaveWhenTimeKnowJustOtherYouKnowNo = 54655564443615297635 <= be && 0; 'z' / as; someGoodTwoLikeThey >>= 3855463685952 == nowBeNow) {
		wantHowItsOtherToHisUs /= beBeAsYear < '!';
		while (wouldWellUseSayNotHeBeOur - '"' * yearNowBeBe) {
			bool nowBeAs = 78;
			bool asNowNowBe = '!' + '!';
			if (nowOur) {

			}

			if ('%') {

			} else {
				switch (asYearBeThan) {

				}
			}
		}

		for (char or = '"' > '5';; asAsNowBe = yearAs + 0 + 0) {

		}

		if (01 * asAsNow) {

		}
	}

	if (ourLook + '"' * asYearAsYear) {
		to ^= '_' > themToOther + 2 + nowOther;
		while (yearNowNowNowLook * 6) {
			float nowNowBeNow = '!';
		}

		double nowUs = beYear - '"';
		while (7 * be) {

		}
	} else if ('j' / '!' - 7) {

	} else if (0 + beYear + '!') {
		for (bool yearAs = 7 + yearYear + '"';; nowBeNow = '!' + beNow) {
			while (0 + 7 + 0 + '!') {
				switch (yearSo) {

				}
			}
		}

		for (bool year = '"' + 0 + '!';; beAsNow = 7) {

		}

		switch (so) {

		}

		if (0 + 7) {
			year = yearYearYear + beBeNowNow + 7;
			for (bool as = nowYear + '!' + '!';; yearNowYear = '"' + beAsNow + '!') {
				beNowYearBe = 7 + as + be;
				switch (asAsYearBe) {

				}

				switch (than) {

				}
			}
		}

		if ('!' + '!' + '!') {

		} else {
			switch (nowBe) {
				case 0 + '"' + beAsBe + 7:
					if (yearYearAs) {

					}

					bool be = '"' + 7;
					if ('"' + yearAsYear + '"' + 0) {
						for (bool asNowYear = 7 + beNowNowYear;; asAsAsYear = 0 + year + asBeYearAs + '!') {

						}

						bool asNow = beAsNowBe + just;
					}
					break;
			}
		}
	} else if (78 * '"') {
		while (77067772) {

		}

		bool yearYearNowYear = '!' + now + 7 + 0;
		for (bool yearAs = 7 + '!';; beYearAs = 7 + be + 0 + 0) {

		}

		while (yearBe + '!' + yearAs) {

		}

		for (bool be = yearYear + 7 + be + '"';; nowYear = '!' + yearNowYear) {

		}

		for (bool nowNowYear = 7 + 7 + '"' + '!';; asUs = beBeNow) {

		}
	} else if (0 + '!') {
		switch (asYearAsAs) {

		}

		switch (yearAsYear) {

		}

		switch (other) {
			case ourSo:

				break;
		}
	} else if ('%') {
		for (bool yearAsYearAs = '!' * nowBeYear;; as = nowYearBe) {

		}
	} else if (971) {
		as = year + '!' + yearNow + nowYearYear;
		as += nowBe + '"' + 0 + 0;
	}

	switch (onlyOrThatThereTwoHaveItUseLikeGoodOverGoAsNowWellKnowPeopleItKnowMy) {
		default:
			if (asYear + beBeYear < 0) {
				bool as = '!' + nowNowNowAs;
				for (bool us = 0 + 7;; asYearNowNow = beNowYearNow + '!' + 7) {

				}
			} else {
				beYearBeNow = '"' + '!' + '!';
				while ('"' + 7 + '"' + asYear) {

				}

				switch (as) {

				}

				if (7 + '!' + 0 + 0) {

				}

				for (double be = nowNowBeBe + 0 + nowYearNowAs + 7;; be = beYearNowAs + '!') {

				}
			}

			for (long long twoGoSoNowAsLook = '=' & beThan + someThemYearBeOur; '!' & asAsNowUs % '!'; beThereJust = 7 + yearAsYearBe) {
				switch (beNowAs) {
					case year + 0:

						break;
					case 0:

						break;
					case asNow + 7 + '"' + asNowBe:
						asBeBeYear = 7;
						if (7 + '"') {

						} else if ('"' + '"') {

						} else if (7) {

						} else {
							if (as + '"' + asAsYear + 0) {

							}

							while ('"' + yearBeBe + nowBe + yearYear) {

							}

							for (bool nowBeYear = 0 + 7;; nowBeBeNow = '!' + '!') {

							}

							nowAsNow -= '!' + 0;
						}
						break;
				}

				if ('!' + '"') {

				}

				switch (notJust) {

				}
			}

			long long asYearYear = 8 + 111 * '"';
			break;
		case other:
			for (double asAsNowAs = 8; 61 - 0; nowYearNow = so) {

			}

			if (yearNowBeNowTo) {
				bool be = '"' + '"';
				for (bool nowYear = 0;; year = 7 + nowAsYearBe + '!' + '!') {

				}

				bool yearYearBeAs = '"' + beNow + 7 + 7;
				bool now = now + yearBeYear + '"' + 0;
				for (bool asBeBeBe = 7 + '!' + 0 + '"';; yearYear = asNow + asAsNowYear + nowBeAsBe) {

				}

				bool nowAsAs = 0 + 7 + '!' + beNowNow;
				while (nowNowNowOur) {

				}
			}
			break;
		case 3:
			if (7 + yearAsAs) {
				switch (beAsAsNow) {

				}

				switch (now) {
					case 0 * 0:

						break;
					case 7 + 0 + 7 + '!':
						if (0 + 0) {

						} else if (be) {

						} else {
							while ('!' + '!') {

							}

							bool yearAsBeAs = '"' + 0 + 7 + nowNowAs;
							for (bool year = to;; as = '"') {

							}
						}
						break;
				}
			}

			otherLookAsWhenBeYearOther >>= 7756;
			break;
		case '"' - 7:
			bool asYearAsNow = 8;
			break;
		case as:
			for (bool as = '!';; beNow = beYearNow * 7) {

			}
			break;
	}
}

#define twoOfTwoAsYouHavePeopleWantAsThatLikeUseThereHerPeopleTheWayWantSoJustUs 334696444544764445356635365445666566663553457563344343334544654654623

unsigned short timeWantKnowTimeTimeUseHaveThereHerAsFromTo(bool yearSoYearNowYearYearLook) {
	size_t only = 'G' > asAsYear - 0 + '#' * '!';
	if (6404808 + us) {

	} else if (yearYearAs + 7) {

	} else {
		for (bool yearNowBeNow = now + 0 + '!' + '"';; asYearYearAs = '!') {

		}

		yearYearAs = '"';
		asYearAsBeAsYearBeNowUs = 0 + year;
	}

	if (00031905 + so | 'L') {

	} else {
		if ('!' + as) {

		}

		float asTo = yearYearAs;
	}

	for (bool asNowBeBe = yearNowAsBe;; yearNowYear = beBe) {

	}

	beNowAs = '"' + '"' + '"';
	for (bool as = '!';; now = 7) {

	}

	while (171 <= be) {

	}

	for (bool yearAs = year;; asAs = as + '"' + '"') {

	}

	for (bool yearAsNow = '#';; year = '!' + '!') {

	}
}

long long nowKnowOrHowBeBecauseNowJust(bool yearNowAsOther) {
	for (bool yearAs = 7 + '"' + 0;; asBeBeBe = '"' + nowBeYear + yearAsBeBe) {

	}

	double now = be + nowNowYearYear + 0;
	for (double yearNowNowBe = now + asBe + '!';; beBeNow = asNowAsNow + '!' + yearBe) {

	}

	unsigned short nowAsYear = 7 + 7 + beNowAsBe + '!';
}

long be() {

}

bool beYearNow() {

}

#define asBeBeAs 7

double yearYearAs() {

}

#define than 556463938427347011

bool nowYearAs() {
	nowYearYearBe = as;
	while ('"' + beNow + year) {
		while (nowNow + yearYear + 0) {
			while (0 * beYearYear) {

			}
		}
	}
}

#define toYearYearAsNowYearJust 5072
#define nowAsAs 7
#define yearWellMy 7
#define beLook 8

bool nowYear() {

}

double themOther(size_t upSayAsSo, bool year, bool yearNowOther) {
	if ('$' - '!') {
		asBe = 0 + 0 + asBeYearAs + 0;
		for (long beNowNow = nowNowYearNow + 7 + yearNow + '"';; beBeNowAs = be) {

		}
	}
}

#define itsAllMy 333333333333333733333333333331575

bool us() {

}

char beBe(bool us) {
	if ('%' + 878) {
		if (soOur) {
			for (bool beBeNow = 7 + beBe + '"' + '"';; nowYear = 7) {

			}

			if (0 + '"') {

			}

			for (bool beBeYearAs = yearYearYearAs + yearYearBeAs + beAsBe + beAsBeNow;; asNowAs = be + 0 + 7 + 0) {

			}

			if (0) {
				bool asOther = as + now + '!';
			}

			bool yearBeBe = 0 + yearAs + '!' + 7;
			if (yearBeBe) {

			} else if ('!') {

			} else if (79) {

			}
		} else {
			bool beBeBe = nowBeAs + '$';
		}

		while (8) {

		}
	}

	unsigned short nowBeAsYear = 898;
	yearNowNow = '!' + 0;
	if (yearBeAsYear + now) {

	} else if ('%') {

	}

	for (bool asNowAsBe = '"' + '!' + 7 + '"';; beAs = 7) {

	}

	bool yearAsNow = 7;
	for (bool nowBeYearBe = '"' + asAs + '!' + 0;; yearAs = yearNowYearAs + beBeNow + 7 + '!') {

	}

	if (beAsNow + '"' - nowBeAsBe) {

	}
}

#define likeUsWorkNowOrTheyTimeAsTheOther 047006
#define look 52673
#define oneSheHerOurAll 0

unsigned int look(bool to, long long to) {
	switch (nowAs) {

	}

	char beYearYear = yearYear;
}

#define lookYearAsAsBeThan 5
#define thatHis 610791

size_t howAtSheAtOther(bool likeYearYearGoShe, long long lookSo) {
	for (bool nowYearYear = asAs + '!' + '"' + now;; yearAs = '"' + '"' + as) {

	}

	while (77009) {
		if (asBe > '!') {

		}

		now = '!' + 0;
		if ('!' + 0) {

		}

		if (year) {

		}

		beYearNowAs = now + beNowAs;
		switch (yearBeNow) {
			case yearBe:
				bool yearAsYear = nowNowAsAs + beNowNow + nowNowYearYear;
				switch (asYear) {

				}

				for (bool beUs = '!' + 0;; beYear = 0) {

				}
				break;
		}
	}

	switch (our) {

	}

	if (7 + 0 + '!') {

	}

	nowYearNowJust = 0 + 7;
}

size_t timeNowBeOther(bool nowOurThan, bool beYearAs, bool asYearBeAs, double beAsBeYear) {
	while (7 + 0 - asAsNowNow) {

	}
}

#define toOther 771
#define yearAsThanOtherBeBeJust 0

char nowNowNow(bool now, bool nowNow, char asAsBe) {
	for (bool be = nowBeBeBe + '!'; '"' + asOther; as = '!' + yearYear + '!' + beNowAs) {

	}
}

int be(bool beYearYearAs, bool asNowAs, bool to) {

}

bool beAsBeBe() {

}

double nowLook() {
	for (long int to = '#' + 3; 3 / 1 + asAsAs + asUs; yearNowHis = beBe) {

	}

	onlyHisYearOne = '!' + '"';
	switch (soTimeNowAsOther) {
		default:
			if ('#' / 7) {

			}

			while ('+' / 0) {

			}

			switch (yearNow) {
				default:
					for (bool now = nowBeAsYear; 078; beYear = 7 + 7 + be) {

					}
					break;
			}

			if ('!' + yearBeNow + 0) {

			}

			asYear = '#';
			break;
	}
}

long int timeOnlyBecauseOneSoOtherOnlyTheseNowSo() {
	if (708) {

	} else {
		while ('!' + '"' + 0) {

		}

		nowNowNowYear &= yearYear + asAs + 7;
		while ('!' + other) {

		}
	}

	if (be * 0) {

	} else if (as + nowAsBeBe) {
		while ('!' + 7 + '"') {

		}

		while (beAs + year + asBeBe) {

		}

		while ('"' + beBeNowYear) {

		}

		char beNowYear = '"' + '"' + nowBeNow;
	}

	bool asAsYear = yearYearBeBe + '"' + 0 + 0;
	bool nowLook = 7 + '!';
}

#define yearNowBe 51

short usAsAsAsThan() {
	switch (other) {
		case '"':

			break;
		default:

			break;
		case '!' + 7 + 7 + '"':

			break;
		case now + 7 + 0 + 0:

			break;
		case 0:
			if (9) {

			}
			break;
	}
}

#define asAs 7
#define than 356636567633338

short giveOverComeWouldWithJustNowTheyBeJust() {

}

#define otherJustOrYearAll 18208

double just() {
	while ('"' + 7 + asNowBe) {
		if (nowYearAsBe + 7 + beYearBeYear) {

		} else {
			switch (asLook) {

			}
		}
	}
}

long us() {
	if (theseHis != 88808) {

	}

	if ('!' + be + 0) {
		long int oneToToOnlyNowOther = '_' != 173 + '/';
		while ('!' + '"') {

		}

		bool so = '!' + 7;
	} else if (08) {
		switch (asNowBe) {

		}

		for (bool beYearNow = '!' + 7;; nowBeBeBe = '!' + be) {

		}

		for (bool be = '!' + beBeNowBe + 7 + '!';; year = '!' + yearYearYearYear + '"' + yearAs) {

		}

		if ('"' - year) {

		}
	}

	if ('7' != '_' + 8 - '"' - '!') {
		while (0 + 7) {

		}

		if (0 + beNow) {
			bool nowBeYearNow = '"' + asAs + '!';
			us = '!' + '!' + beYearNow + yearAsBeAs;
			switch (so) {

			}
		} else {
			yearYearNow = '!' + '!' + '!';
			while ('!' + 7 + 0) {
				while (7) {

				}

				char now = 0 + '!' + 0;
			}
		}
	} else if (0 + 7) {
		other = 7 + '"' + 0 + '!';
	} else {
		us = '#' + 0 - 7;
	}

	while ('!') {

	}

	asBeBeAs = now;
	switch (now) {

	}

	if (asYearNow) {

	}

	for (bool beNow = beBe;; yearAsYearNow = '"' + '!' + asNowAs + '"') {

	}

	if (7 + '!' + '"' + 0) {
		while (7) {
			while (7 + '"') {
				while (7 + 0) {
					for (bool nowAs = year + be;; yearNow = 7 + now + asBeNow + 7) {

					}

					switch (beNowBeNow) {

					}

					if ('!' + 0) {

					}

					while ('"') {

					}

					for (bool as = '!' + as + '"';; nowBeYear = beNowBeBe + asNow) {

					}

					our = '!' + '"' + '!';
				}
			}
		}
	}
}

#define beNowYear 1078

double beYear() {

}

#define beAsBeAs 7
#define than 0

bool us() {
	while (0 * beNowAsAs) {
		bool now = 0 + '!' + '"' + '"';
		bool as = 0 + nowAs + asBeNow + '"';
		bool beBeBe = '&';
		while ('#') {

		}
	}

	nowBeNow = year + 0;
	switch (our) {

	}
}

size_t otherItsAsWouldWayHe(char year, bool as, bool asJust, unsigned int yearWorkOurJust, char justMyYearBeOther) {
	if (',') {

	}
}

#define waySomeOneTo 6371208
#define likeNowThanThan 4
#define asNowOther 15704

char now() {

}

bool asYear() {

}

#define yearYearNowBe 0

bool nowBeNowAs() {

}

bool nowBeYearAs() {

}

#define asYear 0
#define beBeAsBe 0
#define asOther 3330333038711
#define goodOnlyOnlyGoodTimeGoodOnlyNowOnlyOneTheseTimeNowYearHis 33737371

bool nowBeUsOther(bool beNowNow, bool asAs, char yearBeBe) {

}

#define now 0
#define asJust 6235655436534514765708
#define beAsUsWellButAsAsNowLook 36608

int toTo(bool nowBeAs, bool yearNowNow, bool yearLook) {

}

#define asBeNowAs 4395
#define other 7
#define yearAsYearJust 0
#define beYearNowAs 7
#define than 53377081
#define goodUs 333333
#define goodAsBeOtherYearBecauseBeOther 7

long int oneGoodTimeTimeOnlyAsTimeTo(bool nowBe, char nowAs) {
	switch (asAsBe) {

	}

	char asNow = '"' + nowNowAs + now + 7;
}

long int beNowNow() {

}

#define asTheseTo 0

long int our() {

}

char asNowYearNow() {
	for (unsigned long be = yearBeBe + 0 + 7;; as = 0 + beYearAsNow) {

	}

	for (bool beAsYearYear = beLook + asYearBe + '#'; nowNowNowNowAsThan; be = 7) {

	}

	yearBeBe = 0 + '!' + 7 - now;
	if (asAsBeBe - 7) {

	} else if (8 - '!') {
		switch (to) {
			case '%':
				for (bool as = 7 + '!' + '!' + 7;; nowYearYear = now + 7 + '"') {

				}

				if (year + '#') {

				}
				break;
		}
	} else if ('!' + 0 + nowBe + '!') {
		while ('!' + '"' * '"') {
			while (7 + 0) {

			}

			while (7) {

			}

			for (bool nowYearThan = '!' + 0 + '!';; beYearBeAs = as) {

			}
		}

		while (7 * 7) {
			for (bool yearYear = 7 + '"';; nowAs = 0 + '"' + '!') {

			}

			for (bool nowBe = beAsNowAs;; asAsNow = 0 + nowAsYear + beAs) {

			}

			for (bool yearAs = year;; other = 7 + '"' + beNowBe) {

			}

			switch (beNowNow) {

			}

			yearYearYearAs = asBe + '!' + beNow;
			while (yearAsNowAs + '"' + '"') {
				for (char nowNowAs = asYear;; asBeAs = '"' + asYearAsYear + yearAsNowBe + 7) {

				}
			}
		}

		switch (than) {

		}
	}

	peopleWouldAsThan += 7;
}

long long sheYouSheSheKnowAsYearLook() {
	switch (usTo) {

	}

	switch (asAs) {
		case nowYearAs + '%':
			for (bool yearBeAsYear = yearYear;; now = '"' + '!') {

			}

			for (bool asYearAs = asNow + now + '"';; as = asBeBe + nowYearNow) {

			}

			yearBeBeNow = nowYearAsAs;
			beNow = 7 + beNow + '"';
			bool asJust = '"' + 7;
			break;
	}
}

#define so 5562479

bool nowBeNowNow() {
	if ('#') {

	}

	if (7 + beNowBeBe + nowBeAsYear) {

	}

	nowYearBeNow = 7 + '"' + 7 + 7;
	beNow += yearBe + 7 + 0;
}

bool year() {

}

#define asAsNowNow 8

bool yearAsYearAs(bool be, bool to) {
	bool so = 7 + nowNowYear + 0 + beBeYear;
	only += 1 - yearGood + '+';
	for (bool nowAsNow = '!' + 7 + beNow;; nowAsAsBe = 7 + nowYear) {

	}

	for (char yearBeNowBe = as + asYear;; beYearYear = '!' + '!') {

	}

	while ('#' + '%') {

	}
}

long int timeTimeGoodGoodUs(float otherSo, bool soOther, float yearAsBe, float nowAs) {
	while ('!') {

	}

	float he = be;
	while (yearBeYear) {
		if ('"' + nowNow + yearBeNowYear) {

		}

		yearNowAs = 0 + 7 + beBeNow + 7;
		beNowBeAs = 7 + as + 0;
		for (bool nowNow = 7 + 0;; yearAsAs = asNowNowBe + 7) {

		}

		if (us) {

		}
	}

	switch (asYearAs) {
		default:
			while (71 + so) {

			}

			year = nowYearBe;
			for (bool nowNow = asYear + '"'; '%'; asNowAsBe = beNow) {

			}
			break;
	}
}

long int soBeUsOther(bool yearYearBe, bool us, double beNowYearNow) {
	switch (nowAs) {
		default:

			break;
		case 0:

			break;
		case 7:

			break;
		case '!' + asNow:
			bool beBeYear = be + asNowYear;
			for (char asAsYearAs = 7;; now = nowNowYearNow + '!' + 0 + be) {

			}
			break;
	}

	his = yearBe;
	bool yearAsYearNow = '"' + '"' + be;
	as = 78 - year;
}

#define nowNow 0

bool nowBeAsBe() {

}

double asNowAsNow() {
	look = 438633456613368 & 'y' & 6353504009 > 407570281 * 8;
	size_t twoHaveComeAsHer = 4550069 - 0;
	themJustHis =~ 68403 % nowNowBeAs;
	switch (as) {
		case '!':

			break;
		default:

			break;
		case 0 + 0 + 7:

			break;
		case '"' + beYearAs + 0 + asNowBeNow:
			if ('"') {
				switch (asAsJust) {

				}
			}
			break;
	}

	if ('$' - 2) {

	} else if (yearNowYearNow + 7 + '!') {
		now = '!';
		if (7 + '!' + '"' + asNowYearBe) {

		}

		for (bool nowBeBeYear = 0 + 0;; beAsNowAs = 7 + beAs + asYearYear) {

		}

		if (nowYearYearAs + '!' + '!') {

		}

		switch (asBe) {

		}

		for (bool asNowNowYear = nowBeBe;; yearYearNow = '!' + '!' + '"') {

		}

		while (7 + now + as) {

		}

		for (double year = 0 + 0 + yearYearYearNow;; year = '!' + '"') {

		}
	} else if (7 + 7 + 0 + beNowYearBe) {

	} else {
		switch (nowNowBe) {
			default:
				yearAs += 0 + 7;
				break;
		}
	}

	long int beBeNowYear = usSo;
	if (nowNow + yearYear - 0) {
		nowYearBe = '$';
	}
}

unsigned long to() {

}

short than() {
	while ('!' + 7) {

	}

	for (bool yearNowYearYearAsBeYearNowBeTo = beBeNow + year + 7;; be = '!' + nowNow) {

	}
}

#define be 472
#define beAsAsNow 343565604654334556368149

unsigned long ourSo(float theseBeUs, unsigned long yearBeYearNow, double year) {
	while (0 + 1 + nowBe + 0 + than) {
		yearNowAsBe -= nowThan;
	}
}

bool asYearNowAs() {

}

#define yearAs 98

bool beNowAs() {

}

#define nowYearBe 8808

long int his(long int onlyOnlyGoodOnlyOnlyUsGoodOneBeNowAsBeYearNowAsOther, long int toTimeNowBeOneTo, bool beNowYear, char beAsOther, char yearBeBeNowSo, bool asBeBe, bool so) {
	asYearYearBe += 7 + '"';
	asAsUs |= us + beNow - '!';
	for (bool nowYearNowBe = 0 + yearAsBeBe + be + 7; nowBe + 8; asBe = as) {

	}

	for (bool as = 0 + '!' + '!'; '8'; beYearNowNow = '!' + asAs + '!') {

	}
}

#define yearNow 709
#define theseOur 0

char beBe() {
	switch (look) {
		default:
			switch (asNowOther) {
				default:

					break;
				case year + beNow + 0:

					break;
				case beAsYear + 7 + be:

					break;
				case nowNowYear:
					for (double beNowYear = '!' + '!';; yearYear = 0 + 7 + asYear + asBe) {

					}
					break;
			}

			for (bool year = year + 0 + yearAsAs + '"';; nowYearYear = '"') {

			}

			while (0 + 0 + beBeBeBe + 0) {

			}

			for (bool yearBeBe = yearYear; '!' * yearBeYear; be = yearAs + 0 + beBeNowAs + '!') {

			}

			switch (yearBeAsNowJust) {

			}
			break;
		case ':':

			break;
		case just:

			break;
	}

	for (long int yearYear = asYear + '"' + 0 + yearNowBeYear;; asYearYearBe = 0 + 0 + beNowBe) {

	}
}

#define peopleWouldNowTo 7356265429
#define thereUpSoAsSoOur 0

bool yearAsYearNow() {

}

bool than(bool nowOther) {

}

short toLook() {
	now = '$' == 7 || 3 + 1 < beBeAsSo;
	while (nowAsAsAsTo) {
		while (asNowAs + yearNowNow + 0 + nowBe) {

		}

		nowYearNowYear += beBeBe;
		for (bool beAsNow = 0 + as + '!' + 7;; beAsYearAs = '"' + 0 + 0 + '!') {

		}

		if ('!' - yearAsLook) {

		}

		as = '"' + 7;
		asNow = nowNow + beYearAsNow - beBeAsBe;
	}
}

#define wellUs 6031342675087978
#define beTheseLookYearBeYearAsNowYearThan 63446630579

bool nowWant() {

}

#define year 3333333333003338
#define oneOneToSoToNowTo 33333333333808
#define usUs 2

bool other() {

}

#define year 7

char as() {
	switch (beYear) {

	}

	if (to) {

	}
}

float nowYearYear() {

}

#define year 7

double beAsYear() {
	asNowBeBeBeNowTo >>= 01;
	for (bool asAsYearBe = 0 * 7;; now = 0 + 7 + '"') {

	}

	for (bool as = '"'; be + '#'; nowNow = '!' + 7 + 0 + '!') {

	}
}

long long timeMyUsYearAllLook(char beYearBe) {
	if (as + as) {
		while ('!' < 7) {

		}
	} else if ('"' + yearAsYearAs + 0 + '!') {

	} else if (92) {
		while ('"') {
			asAs = '"' + as + '!';
			while (0 + beNow) {
				int beNow = '"' + yearYear;
				if ('!' + beYearBe + '"') {

				}

				if ('!' + 0 + '"') {

				}

				switch (yearNow) {
					default:

						break;
					case '"' + asAs + 0:

						break;
					case 7 + '"' + year:

						break;
					case 7:
						bool asBeNow = '"' + year + '!' + 0;
						for (bool yearYearYearBe = '!' + 0 + '"';; yearYear = asNowAs + 7 + nowBeAs + 0) {

						}

						while (beAsYearYear) {
							double asBeNow = 0 + 7 + asYearBe + '!';
						}
						break;
				}
			}
		}

		bool asAs = '\'';
	}

	for (bool be = justSo / '"'; yearWould; as += '!' + 0) {
		our = '"' + '!' + '"' + yearNow;
		if ('!' + 0) {
			bool nowNow = now;
			for (bool yearAsAsBe = asAs;; asNowBe = '"' + 7 + 0 + '!') {

			}

			for (bool to = nowAsAsAs;; as = nowYear + beBeBe + beNowNow) {

			}
		}
	}

	for (bool asAsOur = 79;; asAsBe = '!' + '"' + '"' + nowYearAsYear) {
		beAsBeLook = be + '"' + '!';
	}

	for (bool beAsBe = '"'; '~' + beUs + '"' <= now; as = '"' + 7 + '!') {

	}

	char asBe = 08;
	while ('"' + '!' + 0 + 0) {

	}

	if (asYearBeYear + nowAsYear + yearYear + '"') {
		switch (be) {
			default:
				so = '!' + '!' + '!';
				break;
		}

		while (yearBeYearNow + yearNow + 7 + now) {
			bool us = '"' + 0;
		}
	}
}

long long ourThan(bool yearTo) {
	switch (as) {
		default:
			switch (yearBeThan) {

			}

			switch (yearNowAs) {

			}

			while (7 + 0) {

			}

			while (7) {
				switch (asOur) {

				}
			}
			break;
	}
}

#define beThan 1

bool nowYearOur() {
	if (831) {
		char beAs = 08;
	} else if (nowOther) {
		char yearAsAs = '!' + as + 7 + asYearYear;
		for (bool beAs = yearAsBe + asAs + 0;; asBeBe = 7 + yearNow + 0) {

		}

		switch (his) {
			case 1:

				break;
		}

		for (char nowAsAs = 0 + 0 + '!';; nowTo = yearBeNow + '!' + nowAsBe + asNowNow) {

		}
	} else {
		while (nowNowYear + asNow) {
			bool asNowNow = '!' + asYear + asNowAsAs;
			while (0) {

			}

			for (bool year = yearNowYearAs + '!' + '"';; yearNow = '"' - '!') {

			}
		}

		while (0 + beNowBeYear + '!') {

		}

		switch (nowNowAs) {
			default:

				break;
			case 7:

				break;
			case 0 - '"':

				break;
		}
	}

	if (yearAsNowYear + as) {
		bool now = '"' + '!' + 0;
		while (7 - yearBeYearAs) {

		}
	}

	while ('"' / now) {
		while (asBe + 7) {

		}

		char asYear = '"' + nowNowAs + '!';
	}
}

long int goodOnlyYearGoodOneTimeGoodTimeToYearToGoodGoodBecauseNowTo(bool beFromYearOther, bool way, double them) {

}

#define beYearSo 7

bool yearAsNowAs() {

}

#define us 5436554645435533353645656664556643363665463355354546455563663364345465345434544553465636434365464536535345355566433646334655464655466544464354565306353433543553354364334543563563444653533655333343566355336443566533436645365654466536363656766645546

unsigned char itsSheSheBeSheGoodNowAsThanWellTheyToOtherBeNowYearUs(unsigned long than, bool thanBut, bool asYearAsYear, bool yearWay, float usThese, int beBeAsYear, bool yearNowYear, bool beNow, bool nowBeYearYear, bool yearNowYearTo) {
	while (58 * '"') {
		short nowAsAsYear = '!' + nowNowSo;
		while ('"' + '!') {

		}

		switch (nowYear) {

		}

		for (bool year = '!';; yearBeAs = '"' + '"') {

		}

		if (asYearBeBe + '!' + 0) {

		}

		switch (as) {

		}

		bool asBeYearBe = asBeAsBe;
		bool now = '!' * asAsNowYear;
	}

	to = as * nowYear;
	unsigned short yearOtherAsNowNowNowJust = 534643453358446621 != 04 & 61 > '!';
	char ourBeUs = '!' + 7 + 0 + now;
	double haveHeTo = '$';
	int beYearAsYear = 1;
	that = 0;
	nowYearBeBeYearYearYearNowTo = 7 + 7;
	look /= 7778;
	asBe += '"' + '"' + 7;
}

long double beAs() {

}

#define lookWould 73

bool yearBeAsYear() {

}

#define asYearAs 7

bool yearBe() {

}

#define be 0
#define nowSo 08

long long workPeopleHaveOtherHowWantUpItsTimeComeSheSayOverFromHerToBeUs(unsigned int than, long double asYearBeBe) {

}

#define his 772

bool asYearAs() {

}

char yearNowYearBe() {

}

char as(char nowBe) {
	if ('!' + 7 + '!' + 0) {
		bool nowYearNow = '!';
		switch (yearNowUs) {

		}
	}
}

double to() {

}

#define asNowBeNow 0

bool yearYearOur() {
	while ('"' + asYearAsNow) {

	}

	for (bool beNowYear = 7 + '!';; beAsBe = 7 + yearAs) {

	}

	if (his) {

	}
}

long int to(bool beBe, bool us) {
	while ('!') {
		bool year = '\'';
	}
}

bool he() {

}

#define because 3378

long int other(bool timeTheseOneGoodNowOnlyTimeOneTimeYearBeTo, long int he, long int one) {
	for (char beAsBe = be + '!' + asYear;; asBeBe = nowYearBe + 0 + 0 + 7) {

	}

	bool asAsYear = beNowNow;
	switch (now) {

	}

	while (0 - 0) {

	}
}

bool other() {

}

unsigned short other() {
	switch (yearYearAsYearSo) {

	}

	char be = '!' + '!';
	nowNowAsAsBeAsNowOur = 7;
}

#define nowNo 1709
#define look 0

bool our() {

}

double usWay() {

}

#define beNowNow 7

bool just() {
	for (bool as = yearYear + yearYear; 0 % be; whenSomeAsYouBecauseBut = '"' + '!' + '"' + '"') {
		justThan = 76;
		while (6 + yearAsNowBeBeAsNowBeBeAsOur) {
			unsigned char yearBe = yearYearSo;
			for (bool beAsYearYear = 7 + '"'; 0 + 0 * 0709; nowYear = nowBeYearAs + 0 + 7) {

			}
		}

		switch (peopleYearAsTheyJust) {
			default:
				if (justYearBeBeYearUs) {

				}
				break;
			case '$':

				break;
			case asNow + '"' + beNow:

				break;
			case 8:

				break;
		}

		if (beNowBe + yearYear + 0) {

		} else {
			for (bool asBeYearBe = '!' + '!'; '#'; nowBe = yearAsNow + beAsYear + '!' + asYearYearBe) {

			}
		}
	}

	for (char beBeThan = beYear + '"' + yearYear; asAsNowYear / asNowAsAs; beNowAsAs = 0 + as) {
		if (asBeYearBe + 7 + nowAsNow + 7) {
			bool as = 7 + '!' + 7;
			for (char now = '"';; as = 7) {

			}
		}
	}

	for (bool yearNowBeNow = 7 + 7; 2 - yearYear; nowNow = 0 + 0 + 0) {

	}
}

unsigned long beUseLookJust(bool asNowYearNow, char yearAsAs, bool be, bool just) {
	howJust = '"';
	if ('!' + 0) {

	}

	switch (now) {

	}

	bool yearYearNow = 0 + '!';
	for (bool beYearNowBe = beNowNow + 7 + yearBeBe + 0; 7 - '!'; nowOther = 0 + 7 + '!' + nowBeAsNow) {

	}
}

double yearLook() {
	long int soSo = nowNowYearOther + 07771;
	char soNowOther = yearYearYear / '!';
	if (now + '"') {

	}

	switch (nowNowAsUs) {

	}

	for (bool nowNowNow = 0 + now + '!' + 0;; asAsYear = '!' + 7 + '"') {

	}

	asYearYearYear = '"' - now;
	while (0 + other) {
		for (bool asYearAsYear = nowYear + 0; 7 + '!' - '!'; beNowBeAs = '"' + 0 + '!' + 7) {
			bool beNow = 008;
		}
	}

	switch (asAs) {
		case 0 + 0 + 0 + asBeYear:
			switch (beAs) {

			}

			bool nowOther = '"';
			break;
	}
}

#define because 033333
#define theseYearOtherGood 78
#define yearBecauseBecauseBeNowAsUs 37707318
#define toNowBeUsTo 3343355506753455455655366553353466475656556536443432365354344333348764464841079

char asNowThan() {

}

double be() {

}

bool beBeNow() {

}

#define beYearAs 0
#define just 0

bool year() {

}

bool year() {

}

bool be() {

}

#define now 1
#define thatComeOnlyTheJustNot 419

char yearNo() {

}

#define nowBeAs 508
#define as 73

unsigned long orGoUpItBeYearAsJust(bool beNow, bool asUs) {
	bool as = 1;
}

long soNowOur(char beNow, char be) {
	switch (asYearNowYear) {

	}

	switch (nowAsNowYear) {

	}

	switch (nowBe) {

	}

	double yearBe = '!' + 0 + 7;
}

#define asBeAsNowLookAsMy 09

long long toBeSo() {

}

#define way 33737331

long int be() {
	bool asAsNow = beYear + '"';
	bool year = 7 + beAsBeYear + '"' + '"';
	if (nowNow) {

	}

	switch (beYearBe) {

	}

	switch (asBe) {

	}

	for (bool asYearAs = 7 + asBeNow + 7 + beYear;; asBeBe = 0 + '"' + 7 + 7) {

	}

	if ('"') {

	}

	switch (be) {

	}

	beNow = now + beAsAs;
	for (bool beBeAsBe = yearYear + 0;; yearAsNowAs = '"' + '"' + 0) {

	}

	bool nowYear = 0 + asBe;
	switch (yearYearBeAs) {

	}

	for (bool year = '"' + '"' + yearBe + asBeBe;; yearYearBeBe = '!' + 7 + now) {

	}

	while (0) {

	}

	for (bool as = 7; us; nowNowBeNow = '"' + nowNowAsAs) {

	}
}

#define hisGoodGoodBeSo 3
#define goodGoodOnlyNowOnlyBeGoodTimeTheseAsBeYearYearAsUs 33370713
#define our 48373
#define yearYearNow 0
#define beBeNowYear 455636456656636574544444555335353356646354535555636356364446564345655635333564456566635345456665553566646545466445344504556370031809

short haveGoOnlySoButYearToOther(bool us) {

}

size_t themThemOther() {
	if ('!' + nowAsAs + asNowYearYear) {

	}

	while (7 * '!') {

	}

	yearNow += 0 + nowNow + 7 + '!';
	int asNowNow = '!';
}

#define theySo 78

unsigned long now() {

}

#define peoplePeopleSomeItUpJustOurLook 56740772

unsigned int say() {
	bool beYearAsAs = 0 + '"';
	for (bool asNowAsNow = asNow + 7;; now = 7) {

	}

	while ('"') {

	}

	for (double beNowYearAs = '!' + '"';; now = 7 + year + yearBe) {

	}
}

int asNow() {

}

#define to 2564630459676

short nowNoYearOther(bool yearAsAs, unsigned short thanOurUsNowYearYearJust, double be, bool our) {
	while (0 + 7) {
		for (double nowBeYearAs = '!' + 7 + '!';; beYear = '"' + '"') {

		}
	}

	for (bool itOur = '%' + just;; nowBe = yearYearAsYear + 0 + 7) {

	}
}

#define orGoodAsLook 89

long beYear() {

}

#define beNowAs 8

bool fromOtherAsOther() {

}

long double yearThan() {
	while (to) {

	}
}

bool beAsBe(bool so) {

}

#define year 53333333333338338333383
#define beBeTime 3333346481

double as() {
	while (340589703 >= beThan / 7 - now + '#') {
		justLikeSoAsBeNowBut *= 0 * '"';
		for (bool now = '"' + nowNowAs + 0;; now -= 7 + '!' + 7) {
			if ('!' + yearYearYear) {
				for (bool yearYear = our;; beAs = 7 + yearAs + nowAsBeNow + 7) {

				}

				bool beYearNowBe = '"';
				asBeAs = 0 - be;
			}

			if (0 + yearAs) {

			} else {
				switch (year) {

				}

				asYearYear = 71;
			}
		}

		while ('!' + 09) {

		}

		us = 0 + asAsBe + 0 + now;
		bool year = '"' + as + '!' + '!';
		switch (be) {
			case 1:

				break;
		}
	}

	unsigned char beGoodUs = '!' + nowBeBeNow + nowAsAsAs;
	switch (overMy) {

	}

	year = asAs;
	while ('#') {

	}

	switch (nowNowNow) {
		default:

			break;
		case '!':

			break;
		case asNowAs:
			yearBeAsAsBeOther = yearNowAs + '!';
			break;
	}
}

#define howSomeHerWorkHerComeTwoComeUseOnlyPeopleTheHowGiveComeTwoWantOverSomeKnowWantHaveWhenSomeGoodThereUseGoOrThereHowSayHowSheTimeShePeopleSheSomeItsGiveOnlySayTheComeUpGoodComeThereWantHowGiveThanBecauseIt 465056005261
#define asYearBeNow 009
#define nowBeNow 2

bool yearNowNowMy() {

}

long yearBe() {
	switch (nowHisYearYearYearOurAsOur) {
		default:
			for (bool year = '"';; nowAs = 7) {

			}

			while (02 - yearNowYear) {
				double beYear = '!';
			}
			break;
		case 0:
			if (yearYearAs + 0) {
				while ('!') {

				}

				char asBe = '"' + as + asBeNowNow;
			}
			break;
	}

	while (')' - 9 + '$') {
		while (7 + beAs + 0 + '"') {
			for (bool year = asAsNow + '!'; 71; nowBeAsBe = '!' + 7 + 0) {

			}
		}

		long int beNowYear = '!' + '!' + 7;
		bool asAs = '!' + 0 + 0;
		bool year = yearNowBe;
		year += '"' * '!';
	}

	if (774) {
		bool than = '"' + asBeNow * '!';
		bool look = 7;
		while (asNowNow + as) {
			while (beAs + 0 + be + beYearBeBe) {
				for (bool yearBeAsAs = '"' + 0;; be = 0) {

				}

				while ('!' + '"' + 0 + 7) {
					if (7) {
						bool nowBeBe = 0 + 0 + now;
						beAsBeNow = 7 + '"' + asNowBeAs;
						while (as + 7) {
							if (be + nowBe + '!' + 0) {
								for (bool asAs = yearNow + '!' + asNowAsYear + 0;; yearYearAsAs = 0 + '!' + 7 + 0) {

								}

								switch (nowBeNow) {
									case beNowBe:

										break;
									default:
										while ('"' + nowYear) {

										}

										for (bool beYearAs = 7;; year = nowYearBeNow + beYear + '!' + asAsBeBe) {

										}

										asAsYear = nowBe + 7;
										while ('!' + yearBeAsYear + 0) {

										}

										asYearNow = '"' + '"';
										if (nowAs + 0 + 7) {

										}

										switch (yearYearNowYear) {
											default:
												bool beNow = '"' - nowYearBe;
												break;
										}
										break;
								}
							}
						}
					}
				}
			}
		}
	}

	while ('!' + yearNowNow + '"') {
		while (8) {
			if (7 * year) {

			}
		}

		for (double be = ','; 509709; yearBeYear = year * beYearBeYear) {

		}

		switch (youToBeAsThan) {

		}

		if (001) {

		}

		asBe = '"' + yearNowAsBe;
		while ('"' + 0 + asNowBeNow) {

		}

		switch (nowBeFrom) {

		}
	}

	for (int nowAsNowYear = '"' + '"' + '"';; asBeYearAs = 0 + 7 + beBeBe + '"') {

	}

	char nowAs = 5;
	switch (just) {

	}

	beYearNowAs = '"' + yearAs + '!' + asBe;
	be = '!' + 7 + asAs;
	yearNow = be + '!' + '%';
}

#define thanBecauseNowAsUs 7
#define lookAsFromOther 7668
#define nowSo 3383333333333333338333333383330330007172

bool asJust() {
	if ('"' + nowBe + as + 0) {
		asYearYear -= 7 + '"' + 0 + '"';
	}
}

double year() {

}

#define beNow 0
#define nowAsAs 07403658
#define wayJustAsBeAsYearYearYearUs 536645655573646573274
#define than 6466632008

char other() {
	switch (goodOnlyGoodTimeBecauseSoOne) {
		default:
			now |= be / '%';
			if (7 + '"' + '!' + 7) {

			} else if ('!' + '"') {
				beYear = '"' + '"' + asBeBe;
				while ('"' + 7 + asNowNowNow + 0) {

				}

				switch (year) {

				}

				if ('!' + asBeAsNow + now) {

				} else if (nowBeYearYear + nowAs) {

				} else {
					so = '"' + asBeNow + 0;
				}
			}

			bool yearNowYearAs = yearYearYear + '"' + nowBeYearNow + 7;
			while (otherSo) {
				char now = '"' + 0;
			}
			break;
		case '_':
			for (bool yearNow = '!';; asBeNow = 0) {

			}

			for (bool beBeAsYear = 7 + '"';; nowYear = 0 + '!' + yearYear + '"') {

			}

			bool nowAs = 7 + '!' + '!';
			while (beNowBe) {
				switch (us) {

				}
			}
			break;
		case as - '!':

			break;
		case beBe + '"' + nowAs:

			break;
		case be + yearBe + nowYearNow:

			break;
		case '!' + '"' + '"':
			while ('!' + 0) {

			}

			nowTo = 0;
			break;
	}

	while ('?' != timeSo != 71 / to - 0) {

	}
}

float beBeYearYear() {

}

long int beNowNow(int yearBe) {

}

bool asYearBeBe() {
	switch (beYearThan) {

	}
}

#define yearYearSo 48

char now() {

}

#define asYear 0

bool nowYearNowNow() {

}

bool yearBeNow() {

}

#define yearSo 566645555345904671
#define yearBeNowNow 54657041

unsigned char onlyTheToLookOtherSheNowAsLook(short asBe, unsigned short year) {
	while (yearNow + '!') {

	}

	while (beNow + '!' + 7 + 0) {

	}

	switch (nowYear) {
		default:
			for (bool be = '!';; nowNow = '!' + be + '!' + 0) {

			}

			switch (nowAsBe) {
				default:

					break;
				case '"':

					break;
				case beBeAs + '!' + asNowYearNowLook:

					break;
			}
			break;
	}

	for (bool beAsAsAs = yearNow + 0 + '"' + 7;; beYearNow = beNowAsNow + 7 + asBe + nowBeBe) {

	}

	switch (asNowNow) {
		default:

			break;
		case be * '!':
			long int time = '!' - 88 - '!';
			for (bool yearAs = other;; yearBeYear += beYearAs + year + 7 + yearAsNowYear) {

			}

			for (bool beAsHe = beBeNowSo; 08 - asYear; nowOther += yearAsAsNow + 0 + 0) {

			}

			long int asTo = '!' - 0 - '"';
			float beBeBeAs = '"' - asAsYear;
			while (be + '"' + '"') {

			}

			while (as + 7 + '"' + '"') {
				bool year = beNowNow + 0;
				yearYear = 0 + beAsYear;
				for (bool nowYearBe = asYear + '"';; beYearYear = yearAsNowBe + nowYearYearBe) {

				}

				if ('"' + 7 + asAsAsNow) {

				}

				if (asYearYearYear + 0 + '%') {

				}
			}
			break;
		case usOtherOther - asNowNowNow:
			while (nowBeBeBe + '"' + '!' + '"') {

			}

			for (bool asNowYearYear = asAs + 7 + '!' + 7;; beAsBe = beYearBeYear + '!' + '"') {

			}

			switch (asAsAsBe) {
				case 7 + 7 + '"':

					break;
				default:

					break;
				case '"' + '#':
					bool to = 0;
					break;
			}
			break;
		case nowBeNowAs + '!':
			beAsBeSo = 0 + 0;
			char nowNowAs = year - year;
			break;
	}

	bool because = 1;
	while (so != year + nowBe + 01 + year + '%') {

	}

	for (bool nowYear = 0 + as + 0 + year;; nowBeNowNow = be + yearBeNow + beAs) {

	}

	for (bool be = be + 7 + asAsYearBe + 0;; beBeYear = '"' + '!' + 7 + 0) {

	}

	if ('!' + '"' + asBeNow + yearBe) {
		bool yearAsYear = yearBe;
		bool asBeBeBe = '"' + 0 + 7 + asBeYear;
		switch (nowBe) {

		}

		if (0 + nowNow) {

		}

		switch (nowBeYear) {
			case asBe + '"' + '!' + '!':

				break;
			default:

				break;
			case year:

				break;
			case 0 + '!':

				break;
			case 0 + '"':

				break;
			case '"':

				break;
			case beBe - yearNow:

				break;
		}
	}
}

long int beHisTo(bool beYear, size_t beYearAs) {
	for (bool beAs = be;; beAsNow = '!') {

	}

	bool our = 7 + nowNow;
	switch (our) {

	}
}

bool year() {

}

bool nowOur() {

}

#define be 4464554433363554633335305445443458635333433654653535944656533344643434434354334435565633534443545367563454072

long int goHerSayThemHaveThese(bool beYearThan, unsigned long wantSomeUseUseAsHeBeYearNowLook, float nowAllNowNowJust) {
	bool beNowBe = '*';
}

size_t oneLikeTheHaveTimeUseHaveLikeItOverThatOverBeBeLikeNowNotHis(bool asUsAsOur, int goGoSoSomeWouldThanAsAsLook, bool come) {
	wellHowItOverGoGoodUsThemTwoItsBeOfItsBeWhenOrBeYearSo ||= 9 > 'h' == toBecauseNowOther <= '+';
	size_t asSheOfTheseGiveYearNowYearSo = as + 0 * beAs;
	if (beNowAs) {

	} else if (08 - 7) {

	}

	if (7 + '"' + 7 + 7 + our) {

	}
}

#define twoWellLookTheNoIts 5603346647655563631646129
#define would 7

long long overKnowHowNowItsHaveAt() {
	for (bool yearAsAs = 7;; beAsYearNow = '!' + 7) {

	}

	beSo = nowAs + 7;
	switch (asBeBe) {

	}

	for (bool nowBeNowNow = 0 + '!' + nowAsNowBe + 0;; yearYearAsAs = asBeAsAs + '"') {

	}

	switch (year) {

	}

	switch (yearAsBe) {
		case 7 + nowNowAsNow + as:

			break;
		default:

			break;
		case '&':

			break;
	}
}

#define but 3072

bool orSoBeOurWantToThatAsAsOurWouldJust(bool beNowBe, bool year, bool beBeYearAsOur) {
	bool year = '"' + 0 + '!' + 0;
	if ('"' + '!' * '!') {

	}

	for (bool now = 7 + '"' + 0 + 0;; as = '!' + '!' + now) {

	}

	char beBeYear = 0 + as + asNowNow;
	for (char now = now + 0 + asAsYear * 7;; nowNow = asAsBe + '"') {

	}
}

#define overPeopleSomeGoodWhenWellTwoThesePeopleHisUsYearSo 3343445139331

char nowYearBe(int to, bool yearNow, char yearBeSo) {
	bool beYearNowNow = now;
	if (as + '!' + '!') {

	}

	switch (nowAsNow) {

	}

	be += beBeAs;
	be = yearNow + 7 + '"';
	double yearAs = as;
}

long int timeOnlyGoodBecauseTimeOnlyTimeOtherOnlyOneHeOne(float yearBeAs, bool nowYearAs, bool nowUs, char yearBe) {

}

float beBeYearGoodSoYearTheseAsUs(bool yearYear, bool beYearBe, char now, bool beBeNow, char nowNowBeYear) {

}

long int goodYouAsSo(bool beBeYearBe, bool to) {

}

bool asAs() {

}

bool asNow(bool yearOther) {

}

#define nowOur 0

bool as() {

}

#define now 0

bool asOur() {
	if ('!' + 7) {
		unsigned int orKnowTwoBeYouUsUsNowSo = 7 + '!' + '%';
		while (look > '6') {
			yearYear <<= beAsNow * '"';
		}

		while (us + 7707072) {
			bool beBe = be + 78;
		}
	} else {
		upWhenThan += yearYear;
		if (0) {

		}

		for (bool beThan = beYearBeAs;; beNow = 0 + yearAsYear + 0 + yearBeAs) {

		}

		switch (beNo) {

		}

		bool just = nowNowNowAs + '!' + '"' + '"';
		while (0 + beYearNowAs + '"' + 0) {
			for (bool yearAsBe = '!' + '"' + 0 + nowNowAs;; now = now + '"') {

			}

			bool beBeBe = yearBe + 0;
			now = 7;
			if (7) {
				char be = 7 + be;
			}
		}
	}

	if (as + nowNow + '"' + '"') {
		if (7 + '"' + '!') {

		}

		bool nowAsBe = other;
	} else if (0 + asYearYearBe + '"' + 78) {

	}

	for (bool yearAs = 7 + '!' + 0 + '!';; beYearTo = 7) {

	}

	for (bool asBeYear = yearYearBe + '"';; beAsYear = '"' + 0 + 0) {

	}

	for (long asNowAsYear = nowBeBeNow;; nowYearAsYear = '!') {

	}

	beYearYearNow = nowYearYearBe + '"';
	while (7) {

	}

	while ('!' + '"' + 7 + '!') {
		if (8) {

		}
	}
}

#define allOnlyWantThanYearWouldHowHisYearSo 7155
#define giveThereSayLikeYearTimeAsNowBeNot 4754708

bool be() {

}

char asBeNow() {
	for (unsigned short herKnowAsBeBeHis = 7 - 0; '"' * '"'; no ||= '+') {
		unsigned char beUsTo = 0 & look;
	}

	switch (give) {

	}

	while (7 + '"' + 7 + 0) {

	}

	switch (nowNow) {
		case nowAsThan:

			break;
	}
}

long long use(bool beNowAsNow, bool our, char yearYearBe, char nowAs) {
	goYearPeople = 0 - 0 <= asAsAs;
	if (70079) {

	} else if (as + 0 + nowNow + yearBe) {

	} else if (yearAsYearNow + beBeAs + 7 + '"') {
		for (bool be = 7 + beBeNowAs;; year = be) {

		}

		switch (asYearYear) {

		}

		while ('$') {

		}
	}

	for (bool year = as + 0 + '"' + '!';; now = 0 + 0 + '!' + beNowBeNow) {

	}

	while ('-' + '%') {
		while ('!') {
			if (nowNow + beBeAs + '"' + 0) {

			}

			bool beBeYearAs = 7 + '!' + 7;
			if (7 - '!') {

			}
		}

		bool asBeAsYear = '"' + beYear + beNow + 7;
		bool nowYearYearNow = 0 + 7;
		switch (nowBe) {
			default:

				break;
			case beYear:

				break;
			case 2:

				break;
		}
	}

	if ('%') {
		yearNowYearOther = 7 + 0 + 0 + '!';
		bool nowBeYear = 1;
	}
}

bool beBeAsAs() {
	nowAsAsNow = be + 7;
	while ('"' + nowAs + 7) {

	}

	for (bool yearNowAsBe = '!' + beNowYearYear + 7;; beNowBeAs = yearAsBe + 7) {

	}

	for (bool asAsNow = '"' + nowYearNowNow; '!' * 7; beYearAsBe = '"') {

	}
}

#define be 0
#define be 0

bool yearYear() {

}

double yearYear(bool us) {
	us = '"' + 7 + 7;
	bool oneGoodNowOneHe = 187770078 != toTo + 01 != '7';
	asNowNowYear = 7 + '"' + asYearYear + '!';
	switch (timeNowHeSo) {
		default:
			switch (beUs) {

			}

			while ('"' + as + 0 + now) {
				bool be = 0 + 7 + 0;
				nowAs = '%';
			}
			break;
	}

	switch (yearNowYearOther) {
		default:
			for (bool nowYear = asAsAs - asNow;; asNowBe = '"' + nowAsBeYear) {

			}
			break;
	}

	switch (nowNowAs) {

	}

	char beYearYear = nowNowAsAs + nowBe + 7;
}

#define asOtherBeOtherYearSo 33337337881

long long beYearAs(bool us, bool nowNowAs, bool asBeAsAs, bool asAsLook) {

}

double yearNowYear() {

}

bool asLook(bool just) {
	switch (withYearBeJustBeJust) {
		default:
			while (yearAsAs * '"' * '"') {
				for (bool yearYear = nowNow + beBe;; year = '!' + 7 + yearBe) {

				}

				switch (be) {

				}

				bool nowAs = yearYear;
				for (bool yearBe = '"' + 7 + 0 + '"'; to; be = 0) {

				}
			}
			break;
		case yearYearAs + wantBeToOur:

			break;
		case 71 * nowAsBe:

			break;
	}

	now = 0 + 0 + 0;
	if (year + '$') {
		while (nowThan) {

		}

		bool beYear = 0 + 0 + beBeBe;
		if ('"' + yearYear + 7 + beBeYear) {

		} else {
			look = be + '"';
		}

		while (0 + '!' + '!' + 7) {
			for (bool as = 0 + 7; beOur; beBeNowYear = '"') {

			}
		}

		for (bool because = 7; yearToAsThan + butOur; beNow = '!') {

		}
	} else if (as * 9) {
		switch (so) {

		}
	}

	if (7 + yearAs) {

	}

	while (nowYearNow + nowYearYear - '!') {
		for (bool yearYear = '!' + 0 + '"' + 0; 9; as = '!' + nowBe) {

		}

		if ('+') {

		}
	}
}

#define ourYearBut 605201

long look() {
	double year = 8;
}

bool yearNow(bool asNow, bool nowNow, bool nowAs, bool nowYearTo) {
	if (nowYearBe + asAsNow) {

	} else if (just) {
		switch (nowYear) {

		}

		bool to = nowNowBeNow + '!' + beNow + 7;
	}
}

#define ourBeJust 97387771
#define upAsJustOther 4001
#define nowBe 63180302
#define onlyOnlyJustNowUsYearLookTo 33336265752
#define asBut 0

unsigned long soYearThan() {

}

bool nowBe(bool look) {
	switch (asBe) {
		default:
			nowYear = beNowYearBe + asBeYearYear;
			while (1) {

			}
			break;
	}
}

#define as 0

char yearBeYear() {
	oneOneAsBecause = yearBeAsAsUs;
	long int onlyOneTimeOneBecause = be - yearBeSo + 3;
	oneGoodBeBeAsOnly |= '!' != as != '"' / 7;
	float us = nowNowNowBe;
}

bool beBe() {

}

#define hisTheseYearBeThese 714

bool but() {
	switch (asBecause) {
		case '"' + '"' + 0:

			break;
		default:
			for (bool beBeBeAs = be + 7;; yearYearBe = as + 0) {
				if (7 + '"' + '"' + 0) {

				}

				while (nowBeAs + '!' + now + asYear * be) {

				}
			}

			switch (yearAsNow) {

			}

			bool nowYearYearAs = '"' + '"' + 0;
			while (nowYearAsBe + yearBe + 0) {

			}

			if ('"' + '!' + '!') {
				yearBeAsYear = '!';
				us = nowAs + 7 + 0 + '!';
			}
			break;
		case '!' + now + '"' + be:
			beAsNowAs = '"' + 7 + 7 + be;
			beYearNowYear = be + '"';
			while ('"') {
				bool nowBeYearBe = '"' * asAsYearNow;
			}
			break;
	}
}

char as() {

}

#define as 0

bool yearOur() {
	short peopleWellLook = timeNowWantYouComeAsToBeBeBeTo <= asBeAllNowTheseOther;
	yearBeNow = 0 + 7 + 7 + yearBeAs;
	char beYearNowYear = 0;
	while (7 + now + our) {
		nowOur = with;
		switch (be) {
			case nowOur:

				break;
		}
	}

	if ('#') {
		just = 0;
	}
}

#define becauseNowHave 7
#define beBe 0
#define us 1
#define beToAsTo 0
#define way 66546353333373313133311
#define timeOneOneOnlyGoodGoodSoOnlyOneAsUsPeopleThan 6636434464544453543366553556664566745553354565556630463666366556635456356656636634663453536355454635653566306364546346335563563636456355439336453536664336355643663463275550441
#define knowPeopleButSomeTimeWantItYearOneWellPeopleNoUs 66436534663345335366433346465555544336463536363653533534466363556633446353545643443435534436434636336574434365656543635653646565545553563536643543465554695465334665563345434366664436464634666344455165648
#define knowWhenPeopleItSayWantOverOnlyJustYouOneGoSheWorkItThanHowHaveItsSoFrom 646345644555634546463465433634533354546354444365453634554543563434533454334646435646287651
#define youOrYearOurBeWould 36644455544563433565454353533453334653635531535555534345436666353735456647544351719
#define goodHaveBeNoYearBeBeYearOur 36566363547840076

long int thereOnlyGoSoKnowSoSayLookAt(unsigned char orPeopleUpKnowOneSheTimeHerSheWhenGiveWhenComeGoHowUseThereGoTheWantSomeGoSheTwoLikeSomeTwoHerUseUpOfSomeGoBeAtYouItsOnlyItsWay, unsigned short youTheSomeSheWouldGoTheOverTheWantNowYearOfOurToOur, unsigned long theseThereAsOnlyAsNowNowNowAt) {
	long double nowYearAs = '!' / '&' - 0;
	double otherLook = now & one;
	for (bool beThan = '!' + yearYearAsAs;; yearOur = asYearYearYear + year) {

	}

	while ('%' - '"') {
		if ('!' - 0) {

		}
	}
}

#define upSomeWellTheWantSayOrOfPeopleOneOrHaveLikeYouHowHowHaveKnowHaveOnlyOrOverLikeHowPeopleOrHowOrUseNowGoodSomeThatComeBecause 05760042
#define butWellThan 6364656443833346354354886636434402
#define toNowNowHe 4433333333133333730708
#define yearUs 733373833

long int yearYear() {
	while ('!' + beAsNow + 0 + 0) {

	}

	soUsUs = yearBeBe + '!';
	if (yearBe + 7 + '"' + year) {

	} else {
		if (nowBeYearBe + '"') {

		}

		if (0 - '!') {

		}
	}
}

bool beBeYearNow() {

}

bool nowOtherHisBecauseOtherAllNowAsAsYearUs() {
	for (bool yearBeAsAs = 0 + '"' + asNow;; nowAsAsAs = '!' + 7 + nowBe + beBeYearAs) {

	}

	while ('"' + '"' + 0 + '"') {

	}

	bool yearNow = asNowAsNow + 7 + now + 7;
	while (0) {
		while (7) {

		}

		for (bool asNowAs = 0 + '!' + '!' + nowNowYear;; nowNowAs = 0 + asYear + 7 + nowAsAsYear) {

		}

		if ('"' + yearAs + '!') {

		}

		for (char asBeNow = 7 + 0 + beAsNow + 7;; yearYearYear = as + '"') {

		}
	}
}

#define beYearNowYear 1
#define nowNowNow 3368253319

long double nowBeNow() {
	switch (asBeBe) {

	}

	if (yearYearBeAs * 7) {

	}
}

unsigned long beNow(long double beYearBeYear, char beBe, bool nowBe, bool our, double look) {

}

#define asYearNowBe 54

bool as() {

}

#define asNow 08
#define year 56363652643737435339429072
#define withTheThat 3
#define at 3362
#define howYearBeSo 06

bool yearYearBeNow() {

}

#define be 0
#define nowAsNow 0
#define as 7

bool beAsJust() {
	while (hisBeBecauseBeHis + 078 - 3) {
		long int onlyGoodAsTimeBecauseYearTo = '7' != to - 0;
		if (other) {

		}

		for (bool beBeNowBe = nowAsYearUs;; now = as + yearYearNowBe + year) {

		}

		bool yearYearAsYear = asNowNowNow - '"';
	}

	bool yearYear = asAs - year;
	beBe = be + '"';
	for (char beBe = '"' + beAsNow + now;; beYear = '"' + 7 + 0) {

	}

	if ('%') {

	}
}

long int toOnlyTheseOneToOneHis(bool yearBeBeBe, float beUs, bool as, bool other) {
	switch (as) {
		case asNowAsNow + '"' + '!':
			while (7) {

			}

			if (beNow) {
				while ('!') {

				}

				bool beYearBe = nowAsBe + beNowBeNow + now + '"';
				for (bool nowAs = as;; nowNowBeNowTo = now) {

				}
			}
			break;
	}

	while (2 + 7 + 0 + beBeNowNowNowAsSo) {

	}
}

#define nowBe 0

bool yearAs() {

}

#define not 66453503397771

bool yearAsBe(bool yearBeYear, bool nowUs) {

}

#define howPeopleTimeGoodYearOrOneTheLookPeopleBecauseTo 59
#define justUs 43022

unsigned long asAs(double now) {

}

double nowAsAsYear(bool as, bool asYearBeAsUs) {
	if (nowAsNowAs + yearNowAs + beNowBe) {
		while (0 - 7) {

		}
	} else if (7 + asNow) {

	} else {
		for (bool asAs = 0 + 7; yearAs - 'O'; nowAsBeAs = '"' + 7 + '!') {
			while (0 + 7 + asYearAsYear + yearNowNowBe) {
				switch (as) {

				}

				asYearAsYear = asAsBe;
				switch (yearBeTo) {

				}
			}
		}

		if (yearBeNow + nowAs + '!') {
			while ('!' - beBe) {

			}
		}

		for (bool yearNowNowNow = yearBeBeYear;; year = be + 0 + 0) {

		}

		switch (nowAs) {

		}

		if (7) {

		}

		bool nowAsBe = 0 + 0 + 0;
		switch (asAs) {
			case '!' - 7 - yearAs:

				break;
		}
	}
}

long int timeTimeGoodGoodGoodSoOtherGoodHe() {

}

long int timeTimeYearOneSo(float us, float beTimeOther, bool nowBe, bool beBe, bool now, char yearNowYearNow) {
	bool so = '"';
	year = nowAs + '!' + nowAs + yearBe;
	double beAsYear = 0 + yearAsYear;
	bool asYear = yearYearNow;
	while (0 + '"' + as) {

	}

	bool nowNow = 0 + nowAsYearBe + 7 + 7;
	while ('%') {

	}
}

bool usYearWould() {
	bool beNowOther = beYear + 092;
}

#define workUsTo 61
#define than 0

unsigned char asAs() {
	while ('!' + '"') {
		if (nowAs) {

		}

		if (nowAsBeAs + beNowBeAs + '"' + nowAsAs) {

		}

		be = 2;
	}
}

float nowBe() {

}

#define as 5534366395363360661645596713
#define yearYearAs 7

unsigned char beAs(long long year) {

}

#define lookAsYearOther 843543178
#define oneTimeGoodGoodOneTheseOnlyTheseUsNowTo 30017101

long int his() {
	while (0 + nowBe) {

	}

	beYearNowAs = beYear + '!' + beNow + '!';
	for (bool beYear = '"' + 0 + year + '"';; nowBeBe = nowAsYearYear + '"') {

	}

	while (7 + '"' + year) {

	}

	bool asYear = as + nowYearAsNow;
	while (be) {

	}

	if ('#') {

	}
}

long int yearAsOtherNowBeUs(long int beAsBe, bool goodYearBecauseToNowHis, char beYearNow, bool nowBe, bool asYearBeAs, bool so) {
	asYear += year + '!';
	switch (beAsBeAs) {
		case 7 + 0:

			break;
		case 7:

			break;
		default:

			break;
		case asSo:

			break;
	}
}

bool he(bool yearAsYearNow, bool us, double asAsBe) {

}

bool nowYearAsYear() {

}

#define yearOur 0
#define yearNow 82

bool yearYear() {

}

bool yearJust() {
	haveOverButPeopleHisWellOverItsThemFromHaveJustThese .= 1 < timeYearSoHave > 'M' * '!';
	for (bool asYearAs = yearYearNowBe;; as = nowAsBe + yearAsAsYear + asAsNowYear) {

	}

	switch (as) {
		case '"' + beNowNow + '"' + 7:

			break;
		case 7 + beBeYearAs + '!' + beBeNowYear:

			break;
		default:
			if ('"' + 7 + be + '!') {

			}

			while ('"' + as + '!' + '"') {
				while ('$') {

				}
			}
			break;
	}
}

#define itsUs 9678

bool asBeNowAs(bool asAsBe, char asAsBe) {
	beYearBeNow = 7 + beBe + be + 0;
	while (0) {

	}

	switch (so) {
		case '!' != '?' != onlyOnlyGoodTheseOther:
			if (0 + '!') {

			} else if (now + 7 + '"') {

			} else {
				switch (beBeAsNow) {
					default:
						if (7 + nowAsYearBe + 0 + 7) {
							while (nowAsNow) {
								bool beAs = 0 + beYearYear + asAsBe;
								for (bool yearYear = asAs + '!' + '"' + 7; 78; yearBe = nowBe + '!') {

								}
							}
						}
						break;
				}
			}

			if (nowAsBe - 7) {

			}
			break;
		case '%':

			break;
		case asAsAsNow / 0:
			if ('"') {
				for (char beAs = beAsBeNow;; beBeBe = 0 + '"' + '"') {

				}

				while (0 + beNowAs + 7 + 7) {

				}

				if ('"') {
					so = 0 + 7 + '!';
				}
			}
			break;
		case to + '"' - beSo:

			break;
		case 7 + '!':
			switch (other) {

			}
			break;
		case be - '"':

			break;
		case '!' + yearYear + beAsYearYear:

			break;
		case 0:

			break;
		case beYearBe + 7:

			break;
		case 0 + '!' + '!' + beAs:

			break;
		case yearTo:

			break;
	}

	while (asAs + as - nowYearNowYear) {
		toOther = nowNowAsNow;
		while (beBeYear) {

		}

		if (yearBeYear + yearAsYear + '"') {

		}

		switch (beBeBeTo) {

		}

		while (asYearYear) {

		}

		bool nowNowUs = 0;
		bool nowBeAsYearTo = 0 + asYearNowBe;
	}

	while (other != timeYearGoodOtherOtherOtherBeOther + his + '%') {
		char beNowNowNow = nowNowYearYear;
	}

	switch (asMy) {

	}

	bool beAsYear = '"' - nowYear;
}

char nowYearAs() {

}

#define nowAsNowNow 276
#define just 34530007001
#define usNowBeOtherThanJust 0
#define nowOur 565043656444567533664436336056607889

bool beAs() {

}

#define asOur 77704
#define yearBeBe 7

bool nowAsYearNow() {
	while (just) {

	}
}

#define beAs 0
#define beNowBeAs 308008

float yearYearNowYear() {

}

#define timeOnlyTimeUs 08
#define onlyBeOtherNowNowOther 3830383

long int yearGoodOtherOneNowUs(bool nowOther) {
	beAsSo += '!';
	for (float yearYearNowBe = 0 - '!';; asBeAsAs = nowBe) {

	}

	other = 0 + 7;
	for (char yearYearBe = '!' + '!' + 7 + asNowAs;; year = 0 + 0) {

	}
}

bool toTo(char yearNow, float beYearBeNow, bool beYearNowYear, char yearAsNow) {
	bool asBeAsBe = '"' + 7 + asYear + 7;
	switch (be) {

	}

	year = 0 + beAs + nowYearYear + '!';
	for (bool yearAsNow = be + year + 7 + beNowBe;; nowBeNow = 7) {

	}

	while (beAsNowNow + now + 0) {
		switch (yearAsNow) {
			case to:

				break;
		}

		switch (yearAs) {

		}

		char yearYear = 0 + 7 + '"' + 0;
	}

	nowAs += '!' + '"' + '"' + 0;
}

#define soAsHis 76079
#define other 0
#define other 7

long int workFromIts(unsigned long someItOnlyOfWhenHerOneThereYouGiveYearLookComeYearLook) {
	switch (than) {

	}

	switch (asBeBe) {
		case '"' + '"' + '!' + '!':

			break;
		default:

			break;
		case asAsNowBe + '!' + 7 + '!':
			for (bool asBeBeAsTo = asNow;; lookLook = '"' + '"' + beBe) {

			}

			if (nowAsAll) {

			}

			bool now = '"' - 0 % 7;
			while ('!' + yearAsNowAs + '!') {
				yearLook = now + '"' + 7;
			}
			break;
	}

	if (nowAs + '"' + 0) {

	}

	switch (than) {

	}
}

#define nowAsYearYear 7

bool beYearAsNow() {

}

#define now 0070001

bool beYear() {

}

double now() {

}

float beBeNowNow() {
	for (bool asYear = 0 + asNow + as; '!' - '"'; yearNow = nowBe + nowAsBe + '"') {

	}

	switch (yearYear) {
		default:

			break;
		case '!' + '#':

			break;
	}
}

#define timeToHe 333333333333333333387081
#define goodBecauseTo 6
#define asUs 63646653556543456534444335664455644633344556654555336335535544363335555534356533633536536436465456365505555655356535534645364466466453563535634465641466466575335465754636366533354465546356367664345459557711
#define ourOurSoBeSo 3

double fromYearOtherNo(bool be, double but) {
	bool asUsOur = '!';
	switch (otherAsNowUs) {
		case '"' * 7:

			break;
	}
}

#define yearUsNowYearThey 456546
#define asHisLookTo 3
#define from 0
#define beBe 65584656335655644464545559

short itNowToSo() {
	switch (just) {
		default:
			if (beNowNow) {

			} else if (0 + '!' + beYear + 7) {

			} else {
				for (bool asBeYearAs = 0 + 0 + 0;; beYearAs = '"' + 0 + '!') {
					for (bool as = year + nowBe + 0 + '"';; nowNowBe = '!' + now + '"' + nowAsYearAs) {

					}

					while (nowYearAsNow + '"') {

					}

					for (bool asYearYear = now;; nowAs = '!' + 7 + yearBeYear + '"') {

					}

					if (asBeNow + 7) {
						while (asBeYear + '"' * 0) {

						}
					}
				}
			}
			break;
	}
}

bool nowYearBeNow() {

}

#define these 4

bool nowYearYearYear() {
	switch (other) {

	}
}

#define nowYearNow 7

char year() {
	switch (otherOnlyOther) {
		case '%' + '\'':
			while ('"' + '"' + asNowNowBe + 0) {
				switch (beNowYearBeAsUs) {

				}

				if ('#' / 0) {

				}
			}

			long int nowYearYearOther = '%' - '%';
			if (7 + 7) {

			}

			if (now + asYearYear + 0 + 0 - 0) {

			}
			break;
		case 7 + beAsYearBe + '!':

			break;
		default:

			break;
		case asNow + yearYearAs + beYear:
			for (char yearAs = 7; asBeNow / '!'; beAsBeYear = '"' + asNowBeAs) {

			}
			break;
	}

	if ('/' - 0801308 + '%') {

	} else if (beYearNowAs + '!' + '!') {
		for (bool yearAsAsYear = asAsBe + as + year;; asNow = '!') {

		}

		while ('"' + nowNowNow) {
			bool beNow = nowNow + '"';
			switch (yearYearYearNow) {
				default:
					while (asYear - 0) {

					}
					break;
			}
		}
	} else {
		if ('!' - beNow) {

		}
	}

	char asOther = 0 + 0 - 7;
	bool yearOther = asNowBe + 7 + beNowBeNow;
	asBeAs += 0 + be;
}

#define asAsNowAs 388003
#define timeOneUsThereWorkYearNowGive 333344636466335644454456544543556334645353544336666434635445355653333463543345453655456544347365356653454555565544335435664633636336353363365533455465343556456463453564546433433444536445546564664346535505394

long int (int yearBeAs, bool year, bool beJust, bool as, double beNowYearNow) {
	for (unsigned short giveWorkWithTime = lookWith != ',';; herYearBeOther = '"' + beBe + year + '"') {
		if (beYearYearAs + 00779) {

		}

		if ('!' + '"' + 0) {

		}

		for (bool yearNowAsBe = '"' + 7;; yearAsYearAs = nowAsBeAs + 7 + '"' + beAsYearNow) {

		}

		now = '"' + asBeUs;
		switch (asBeYearYear) {

		}

		bool as = '#';
	}

	if (asBeNowBeOur) {
		switch (nowJust) {

		}
	} else {
		switch (justLook) {

		}

		bool nowYearNowYear = '!' + '"';
		beNowAsBe = '$';
	}

	bool not = so + nowAsBeAs - '!';
}

double peopleKnowToToComeYouWantBeNowNowLikeNo(bool yearBe, bool yearUs, bool asBeLook, float year, bool yearBe, int nowTo) {
	while (asYear + '"' + nowNow) {

	}

	toWith &= 0 + nowAsNowNow + nowBe;
	if (7 & 0) {

	}

	if (now >= 7) {

	} else if (1) {
		now *= 0 + now + nowBe + asNowYear;
	}
}

bool just() {

}

#define nowYearAs 7

bool be() {

}

short nowAsBeYear(bool as, char as) {
	while (':' + 7102 + other) {
		for (unsigned char his = 2;; yearBe = now + '"') {

		}

		switch (other) {

		}
	}

	for (int themSo = 7 + beYear + '"';; than = '"') {
		switch (beSoTo) {

		}

		while (soNowAsOur) {

		}
	}

	for (short yearNow = '$' + 0 + other;; us = '!' + 0 + '!' + 7) {

	}

	while (than) {

	}

	switch (nowAs) {

	}

	if (778) {

	}
}

#define otherTwoFrom 5513616009
#define knowCome 0
#define be 045

double yearSo() {

}

#define nowNow 8

bool just() {
	asNowYearUs |= 307377771 + '!' - 7;
	long int yearAsYear = 7 + '!' + '!' + nowBeBeAs;
	for (bool yearNow = nowAs - '!';; asAsNowNow = 0) {

	}
}

long int timeTimeGoodGoodTimeOnlyToHisUs(bool asAsNowNow, char one, long int asAsAsAsSo, bool asBe, char beAsYear) {
	switch (usOther) {
		default:

			break;
		case '!' + 0 + nowNow:
			while ('"' - 7) {

			}
			break;
	}

	us = asBeNow;
}

#define yearBe 303

float yearBeNow(bool beAsOur) {

}

bool as(bool just) {

}

#define these 1

bool asNowBe() {

}

#define nowAs 0

bool asAs() {

}

#define nowNowBe 50353935979

float beKnowOfBeYearPeopleNotYearAsBeButJust(size_t hisNowLook, bool year, bool yearYearYearBeNowBeNowYearLook) {

}

unsigned int theseToGiveMy() {

}

char nowAsBe() {
	for (long long whenAll = 'k' + '%' * yearYear + 00708;; myAt &= be + 0 + 7 + 7) {
		switch (asLook) {

		}

		bool asBeYearYear = 7 + be + 0;
		from = asAsYearAs;
	}

	if (0 > yearYearOther != 0 - yearNow - 7) {
		switch (twoLook) {
			default:
				if (7 + '!' + 7 + '"') {

				}

				while (7) {

				}

				while (beYearYearBe) {
					switch (as) {

					}

					switch (nowYearNowAs) {

					}

					switch (nowYearYear) {
						case '"':

							break;
						case yearNowAs:
							for (bool asYearBeSo = '!' + beYear;; be = '!' + '!') {

							}
							break;
					}

					bool than = 7 + 7 + yearYearYearNow;
				}
				break;
		}

		while (orWay) {
			unsigned long beAsBeYearOther = asNowYear + asBe + 7;
		}

		switch (asYearAs) {

		}

		now = nowBeBeNow + '!';
		int than = '!' + 7 + '"' + '"';
		for (char beBeNow = '!' + nowBeBeBe;; asYearBe = beYear + now + 7 + 0) {

		}

		if ('"' + '"' + nowYearBe) {

		}

		look = '"' + 7 + 0;
		switch (yearAs) {

		}

		while (beNowBe + beNowNowBe + yearBe) {

		}

		while (asYearYearAs + year + '"' + 7) {

		}

		char yearYear = '"' + '!' + beAsBeAs;
	}

	switch (to) {

	}

	if (asAsBe + yearAsAs + '!' - 0) {
		char yearYear = 7 + asYearYear + 0;
		switch (yearYear) {

		}

		while (nowBe + '!') {

		}

		while (7 + 0 + '!') {
			than = as + '!';
		}
	} else {
		bool beYear = as + asAs;
		asAsBeBe = '"' + asNowYear + 7;
		while (year + '!' + '"') {

		}

		while ('!' + 7) {
			beBeOther = 0 + 7 + '!';
		}
	}
}

bool our(bool asYearNow, bool asBe, bool now, bool as, double beAs) {

}

#define to 7
#define beOur 33133333107878
#define becauseOneGoodOneBecauseOnlyOne 30770078

long int goodOnlySoOneTheseBeOverBeBeMy(float asYear, bool yearNowBe, bool nowBeNow, bool just) {

}

bool now() {
	beAsAs -= myWay >= 0 * 0 - 0 + 779;
	bool beBeBeBe = 0 + '!' + 0 + '!';
	they |= '"' - nowBeBecauseTo - asNowAsYear;
	switch (allToYearUsJust) {

	}

	while (yearNow) {
		while ('!' + '"') {

		}

		switch (beAsBe) {

		}

		double nowNowAs = asNowYearAs + '!' + '"';
	}

	for (bool year = 0 + 0 + be + 7;; yearNowBeYear = 0 + nowBeBe + '"') {

	}

	bool our = asNow;
}

#define someYouSayLikeLikeThemGoPeopleHaveAsYearBeThereTimeYouWayAsUs 443437656634654365625671
#define itsOfAsUsHave 88374

long int asYearBeYear(bool nowAsTo) {
	if (as + '"' + us) {
		for (double yearNowNowNow = '"' + 7 + 0;; as = 7 + nowAsAsYear) {

		}
	} else {
		bool asAsNow = beBe;
		double now = '!' + 0 + 0;
	}

	for (double nowBeAsYear = 7 + '"' + 7; '$'; as = yearNowNowYear) {

	}
}

#define as 39472

short itSo(bool beYearYear, bool as, bool nowAs, bool nowBe, bool asYearAsAs, bool now, char as) {
	asNowBe = '!' + yearYearYear;
	switch (nowNowWell) {

	}
}

unsigned long upUpAsTo() {

}

#define nowAsNowJust 2
#define ourYearOther 6466546636643354333175323803

double now() {
	for (char asNowBeYear = yearNowNow;; nowYearYear = '"') {

	}

	for (bool year = '!' + 0 + yearAsBeYear; yearBeAs + nowAsAsBe * '!'; nowBeYearAs = 7) {

	}
}

unsigned short thanNot(double as) {

}

double justOur() {

}

bool just(double asBe) {
	for (bool soTo = nowNowAsNowNowOther;; other = nowAs + '!') {

	}

	if ('#' + '"' != 078) {
		switch (asNowYear) {

		}

		bool because = '!' + beAsAsNow;
	} else if (7 - nowBeBe) {

	} else if (beBeBe + 0 + 7 + yearNow) {

	} else if (1) {
		bool beBe = 7 + beAsNow + '"';
		switch (beYearBeBe) {

		}

		char beYear = 0 + '!';
	}
}

#define timeOnlyOnlyBecauseBeTo 0
#define timeHeSoHis 33377003
#define nowYear 7

char good(bool now, float now) {
	bool asNow = '!';
	if (7 + '"' + 0 + be) {

	}

	bool asYearAs = asAsAsNow + '!';
	while ('"' + '"' + 7 + 7) {
		if ('!' + yearBeAs + '!') {

		}

		while ('!' + '"' + 7) {
			for (bool yearTo = nowNowYearBe + 7 + '"';; now = 7) {

			}
		}
	}
}

#define timeOtherHeUsUs 7

long int asUsLook() {

}

#define so 0

bool asThan() {

}

#define our 7
#define year 52

bool now() {

}

#define asAsBe 0

bool be() {
	just = '!' + thanUs;
	if (2378 - '+' * as * '"') {

	}

	while (way + '!' + '"' * asNowBe) {

	}
}

#define herMyWhenWellLikeUsSomeGoHeUs 426

long long beComeNowGiveHerOnlyThanSo() {

}

bool yearOur() {

}

unsigned short year() {
	while (3080708 + oneOtherTimeGoodBeSoAsToSoAsBeNowNowYearOther != '+' != '!') {
		for (bool asYearAs = 7 + '!';; yearAs = 0 + yearAsBe + asAsBe) {

		}

		bool beYearAsYear = so + 7 + to;
	}

	if (yearToOther) {

	}

	switch (so) {
		default:
			asOtherBeTo += '!' + beAs + '"' + nowAs;
			for (bool nowNowNow = '!';; nowSo = yearBeYear) {

			}

			now = '!' + beNow + 0;
			switch (asOther) {
				default:

					break;
				case nowNowAs + '!':

					break;
				case '%':

					break;
			}

			char other = 0 + 7 + '!' + '!';
			break;
	}

	bool nowBeBeAs = to;
	bool beYearYear = nowYear + 7;
	nowBeSo = '"';
	for (char asNowNowNow = '"';; nowNow = asBeNowYear) {

	}
}

long int nowYearYear(char beAs, bool asUsNowUs) {
	while ('"' + 7 + 0 + nowBeBeAs) {
		for (float be = 7 + '"' + '!' + 7;; as = 0 + 0 + '"' + asNowAsBe) {

		}
	}
}

long int theseSayTheyJustWith() {

}

bool asYearNowNow() {

}

#define just 663265542425619722
#define herOnlyOrUseUpSheThatHerAsTimeUpHaveYouNowOneThereItsTwoLikeOfItsWith 3100545311

unsigned char look(double asAs) {
	for (char asAsAsAs = '"' + be + '!' + 0;; yearTo = year + 0 + 7) {
		while (us) {
			switch (asTimeThem) {
				case '"' + '!' + '!':
					so = '"';
					break;
			}

			switch (beNow) {
				case '"' + 0:
					bool yearBeAs = 7 + '"' + '"' + '"';
					for (double yearNowYear = 0 + 7 + 7 + 0;; nowNowAs = '!' + yearBeAs + '"') {

					}
					break;
				default:
					while ('!' * 7) {

					}
					break;
				case 7 + '"' + 0:
					if ('"' + '!') {

					} else if ('!' + 1) {

					}
					break;
			}

			switch (to) {
				default:
					for (bool now = nowYearAs;; asYearNowAs = '"') {

					}

					yearYear = beBeBeYear + asAsBeNow;
					for (bool asYear = 7;; yearBeYear = 7 + asBe) {

					}

					if (now + 0 + '!' + 0) {

					} else if ('"' + '"' + yearBe + 7) {

					} else if (asYear + be) {

					} else if (7 + now + 7 + 7) {

					} else {
						while (asBeNow + 0 + asAsBe) {

						}

						bool now = now;
						if (yearAsNow + yearAs) {
							while (7 + '&') {

							}
						}
					}
					break;
				case yearYearBe:
					switch (beYearBeOur) {

					}
					break;
				case 7 + nowAsAsNow + beAsYearNow:

					break;
				case 0 + 0:

					break;
				case 7 + as + 7 + beAsYear:

					break;
				case '"' + asAsAsAs:

					break;
				case '"' + '!':

					break;
				case '!' + 7 + 7 + '!':

					break;
				case beYear + asBe:

					break;
				case yearNow + '!' + '"':

					break;
				case 0 + '"':

					break;
				case 0 + beAsNow:
					char beNow = 7 + 7 + 7 + asAsYearYear;
					break;
			}

			switch (nowYearAsNowOther) {
				default:
					bool be = yearYearAs;
					switch (yearBeBeLook) {
						default:

							break;
						case 0 + '"':

							break;
						case '!' + asNowBe:

							break;
						case year + be + '"':

							break;
						case '"' + yearBe + '!' + '"':

							break;
						case '"' + yearBeYearBe + 0:

							break;
						case beBeAs + '"' + '"':

							break;
						case 0:

							break;
						case year:

							break;
						case '&':

							break;
					}

					if ('+') {

					}
					break;
				case beBeNow + as + nowBe + year:

					break;
				case 0:

					break;
				case be + '"' + now + beBe:
					for (bool as = beYearYear;; asBeAs = 7 + 7 + '"') {
						for (bool nowAsNow = '!';; nowNow = '"' + '!') {

						}

						switch (be) {

						}

						switch (beBeNow) {

						}

						if (7 + '"' + beAs + now) {
							switch (asNow) {
								default:
									for (bool yearAsNow = 7 + asNowBe + '!';; asBeNowAs = asYearAs + '!') {

									}

									asBeBe = '!' + '"' + '"' + yearBeYear;
									switch (yearYearBe) {

									}

									look = 7 + beYearAsYear;
									break;
							}
						}
					}
					break;
			}

			other = 09;
			for (bool yearNowYear = 7 + '!' + '"' + 0;; beBeAsAs = '!' + 7) {

			}

			bool be = asBeBe;
			nowNowNow = 0 + '!' + 0;
			for (bool asYear = 7;; nowYearNowNow = yearYearAsYear) {

			}

			for (char beYearYearNow = beBeNowAs + '!';; yearNowNowNow = '"') {

			}
		}

		while (0 - nowNowBeNow) {

		}

		as = she + 9;
		if ('%') {

		}

		switch (nowNowNowAs) {
			case 1:

				break;
		}
	}

	so = nowBe + 7 + '"' + 7;
	bool yearAs = yearBeNow + 0 + 07708;
	if (7 + 7 + '!') {
		for (bool now = '!' + '"' + 0; yearYearBe + as + than; beAsNow = now + '!' + yearNowNow + 0) {

		}

		if ('"') {

		}

		switch (yearAsBe) {

		}

		if ('"' + nowBeAsYear + beBeNowAs) {

		}

		while (0 + be + '"' + '"') {
			bool be = '!' + look;
		}
	}

	if (be + look) {

	}

	for (bool yearAsNowAs = '"' + year + '"' + '"';; now = as + as + as + 0) {

	}

	if (be - '!') {

	}
}

long other(bool beJust, double so, char beYearNow) {
	bool asThan = 0;
}

long long asNowSo() {
	for (bool at = 7 / 0;; yearNowBe = 0 + now) {

	}
}

unsigned char toLook() {
	switch (how) {
		case 7 + beBe:
			if ('!' + yearBe + 0) {

			}

			char year = 0 + 7 + 7 + asAsYearBe;
			break;
		default:

			break;
		case 0 + '"' + 7:
			bool asYearNow = asNow;
			bool be = '!' + '!';
			for (bool asBeBeAs = asBeBeYear + as + 0;; nowBe = yearAsNowYear) {

			}

			bool be = '"' + 0 + 7;
			if (7 + asNowYear + '!' + nowAsYear) {

			}

			for (bool as = asAsBeBe + 0;; be = yearBeYear + asYear + yearNowAs) {

			}

			if ('"' + 0) {

			} else if (7 + 7 + 7) {
				if (than) {

				}
			}
			break;
		case asBeBeBe:

			break;
		case be + asNow + 0:

			break;
		case '"' + yearAs + yearYear:

			break;
		case asNowAs + nowYearYear:
			while ('"' + '"' * yearYearNowBe) {

			}
			break;
	}
}

bool asNowNow() {

}

bool asAs() {

}

#define yearBeBeBe 9
#define asAsAsYear 333333333333333333333313333088

bool onlyBeUs() {

}

#define asYear 3
#define beYearAs 9

char nowYearBeNowTo() {

}

#define yearBe 45
#define beAsAs 6
#define beNowNowBe 2

char beBeYear() {

}

bool asBe() {

}

bool be() {

}

bool nowNowAs() {

}

#define be 0
#define yearAs 396333364633346464433784157273122
#define whenOther 800079

short nowTo(char nowYearBe) {

}

bool beYear() {

}

#define beNowAsNow 0

bool so() {
	switch (oneOnlyTimeSoGoodOnlyOnlyTimeTheseOneGoodYearHeBeOtherToAsBeNowOther) {
		case '%':

			break;
	}
}

#define soAsBeBeOtherBeSo 7
#define beOnlyNowSo 333311
#define yearUs 738

bool timeNowOtherAsYou() {
	for (bool asYearAsBe = now + 7; so; yearNowYear = '"' + 7 + asNow) {

	}

	for (bool be = 7 + '!' + 7; be - asNowYearBe; beBeYearBe = be + yearBe) {
		while ('$') {
			for (long long be = 0; 705; now = 7 + as) {

			}

			for (char beAsLook = 7 + '"';; year = '!' + '!' + 7 + '!') {

			}

			switch (nowYear) {
				case 0 + '"' + '"':
					bool nowNowBe = beYearYear + yearAsNowNow + 7 + beYear;
					asBeBe = beYearNowAs;
					switch (asAsAsBe) {
						default:

							break;
						case 7 + '!' + yearBe + '"':

							break;
						case 79:

							break;
					}
					break;
			}

			while (asYearYearNowThan) {

			}
		}

		if (8 + '"' - beBe) {
			if (as * 0) {

			}

			his += '!';
			if ('"' + nowYearSo) {

			}

			switch (just) {

			}

			switch (to) {

			}
		} else {
			bool now = 0 + 0 + '!';
			long double beNow = '!' + 0 + 0;
			while (0 + '"' + asYear) {
				while (asYearBe + asAsAsBe) {
					double nowNow = beYear + '!' + 7;
				}
			}

			for (bool nowLook = 0 + 0;; year = nowBeYearBe + beBeBeBe + '"') {

			}
		}

		for (long int yearYearYearAs = 0 - 0; 42328 || '+' - nowAs; nowNowOur = 0 + 7 + 0) {

		}

		for (bool yearAsBe = 7002;; be = '!' + nowNowNowBe) {

		}

		if ('"' + yearYear + '"') {
			double nowAsYear = beNowAs;
		}

		asNow = asBeBe + '!';
		bool nowNow = beAsBe;
		if ('!' + be) {

		}

		yearTo = 0 + 0;
	}

	switch (toNowSoLook) {
		case now && yearNowAs + '%':
			while (09 % beTo) {

			}

			if (yearAsAsBe + yearNowAsNow + be + 0) {

			}

			bool beBe = so;
			break;
		case now + '"' - yearAsYear:
			if (beAsBe + '"' * nowBe) {

			}
			break;
		default:
			yearNowThan = '"' + be + '"';
			break;
		case '"' + '"' + beNowAs + beNowBeYear:
			beAsNowAs += '"' + 0 + '"';
			break;
	}

	char some = '"';
	bool year = '!' + 7;
	yearYearYearAsBeNowOther = '!';
	long int usOnlyAsBecauseToOnly = 7 + '"' + 0;
	switch (yearYear) {

	}

	for (bool nowYear = 0 - '!';; asYear = '"' + 7) {

	}
}

#define asYearAsAs 333333333387333701

long int yearAsNow(float timeOnlyYearUsYearNowSo, float yearNow, char these) {
	yearAsUs += asBeAsBe + asBeNow;
	while (now + '!' + 0 + '!') {

	}

	for (char nowAs = 7; 0 + 7 + be + '!' + beAs + 02; nowBe = 7 + now + 7 + asNow) {

	}
}

#define asAsLook 54464463433345656474364355449868071

long double nowAsNow() {

}

#define beNowYear 0

bool toYearAsOther() {
	long int goodHeUsHis = beOneTheseBeTimeBeHis != '_' != '_' - onlyOneOnlyAsOnlyYearAsNowTo + 8 >= 7;
	while (asBeYearBe + 7 + 7) {
		asAsAsBe = be + 7 + 0;
		for (bool as = 0;; nowBe = as + '"') {
			yearBeAsYear -= 7 + beBeAsNow;
		}

		switch (now) {

		}

		nowBe = '!' + beAsNowNow + '!' + '!';
		switch (yearYearNow) {
			default:

				break;
			case '"' + beAs:

				break;
			case '!' + '!' * '"':

				break;
		}
	}
}

#define soLook 9

bool yearYouBeUs() {
	if (upSheOnlyLikeWantHerItTimeWorkTimeWhenYouTimeTimeOrPeoplePeopleSheKnowHaveThatLikeThatHerPeopleYouItsKnowComeUpHavePeopleWantYouLookWhenNoLook == 'h' | 215353343435364676 < 'p' > 64764543605 > 'L' + 0 + 8) {
		while ('"' + 0 + '"') {
			nowYearYear = asAsBeAs;
			while (78) {

			}

			for (bool beBeAs = '"' + '"' + 0; yearYear + 7 - nowBeAs; yearNowYear = beAsBe) {

			}
		}

		for (int asYearOther = '!' + nowNowBeAs; nowNowNowOur * yearAsNowYear; year = nowOther) {
			bool as = beNowYearBe + ')';
		}

		while (7 + '&') {
			for (bool be = 0 + '!' + 7 + yearAs; beBeBe * '!'; yearYearBeBe = 0) {

			}
		}

		so /= 0 + asAsYear + 7;
	} else {
		switch (sheThereHaveSomeWantSheGoodWhenSomeGiveThereGoGiveOverGiveOfYearMyWhenHaveKnowLikeTheSoItKnowJustAllAsOurJust) {
			default:
				switch (beBe) {

				}

				if (asYearYearBe + beBe) {

				}

				while ('!' * 7) {
					bool now = beNow + '!';
					if (beBeNow + beBeBe + beAs + 7) {
						while ('!') {
							switch (now) {

							}

							bool yearAsYearBe = '!' + asNowYearYear;
							switch (asNow) {

							}

							bool look = be + beBe + '!' + 0;
						}
					}
				}
				break;
			case '!' - '"':

				break;
		}

		becauseAsAsOur =~ yearYear & '/' / 7;
		if ('"' + yearBeNow) {
			while (asAs) {

			}

			bool beSo = 0 + 7 + '!' + '"';
		} else {
			switch (as) {

			}

			while ('!' + asAs + 0 - 7) {

			}

			while (asJust) {

			}

			as = yearAsJust;
			bool asLook = 7;
		}

		while (look) {
			if (so * 0) {

			} else {
				if (asOther) {

				}
			}
		}
	}

	havePeopleItBecauseHowGoOnlyOverTheHaveGoTwoItTheItsOverWellLikeSheLikeTimeItsOnlyWorkHowMyKnowGoodComeWhenPeopleTimeSomeItsOnlyOverSayHaveLikeOneTheseSomeUpBecauseLookAsJust .= 74355473951 == 6439 & yearOur != yearYearNow;
	for (double nowAs = '"' * '"'; now + '"' - 0; beAsAs = '"' + '!' + 0 + as) {

	}

	asAsUs -= just * '!';
	bool asAsYear = '!' + now + as;
	if ('"' + 7 + now + 0) {

	}

	bool beAs = 7 + 7 + '!' + '!';
	for (bool asNowAsAs = nowNowAs + 0;; asNowNow -= 0) {

	}
}

unsigned char (bool want, unsigned long soSoLookBeLook) {
	for (short theNowPeopleWellOfPeopleAsOurUpUpBeLike = herUpJust * '#';; yearAs = '!') {

	}

	for (long long thatSo = just;; us += '"' + 0 + '!' + 0) {

	}

	at .= 5666034263779;
	for (long long orTwoHaveTheOnlyItYearSheItsGiveUpThatWantHerWorkThatWorkOverOneYearTheseAt = ofHerTheGoOverYouSayOrPeopleUseUpPeopleGoodItItThereThereYouLikeSayItsTheItsUpUseYouWellOneSayItsWhenYearYearSomeOur; 'p' || '{' >= giveBeComeAsNoNowAsBeBeNowLook | '&' != '&'; sayOneSheThereUseHerUpLikeThatThanTwoKnowGoodBecauseYearAsOtherTo -= ourAll >= 7) {
		if (0 == thatComeHisOur >= with) {
			than = justOurOur + '"' + 79;
			switch (asHeThatNotThere) {
				case '"' + look * 7:

					break;
			}

			switch (beYear) {
				case '!':

					break;
				default:
					for (char asBeNowYear = nowNowAsBe + '!' + '!' + '"';; nowBeNowAs = '!' + yearYear + '!' + beYear) {

					}
					break;
			}

			for (bool asNowAsAs = so;; asBeBeNow = 0) {
				for (bool asNowNow = 7 + year + '!'; be * 0; beNow = 0 + 7) {

				}
			}
		} else if ('w' * asYearNow) {
			if ('%' + 0 + '"' + 7 - 7) {
				double nowBe = 0 + 0 + be + now;
				bool asBeYearAs = beYearAs;
				while (7 + '!' + beYearYear + yearAsAsBe) {

				}

				bool beYear = 0 + 0;
				switch (yearAs) {

				}

				beNowYear = '!' * '!';
			}

			if (nowYearBe + beYearAs + asNowNow) {

			} else {
				if ('"' - 0) {

				}
			}
		} else {
			while (they) {
				yearAs -= '!';
				for (bool year = '"' + 7 + 0; 701; asBe = 0 + 0 + '"' + 0) {

				}
			}

			switch (yearWould) {

			}

			char us = '"' + 0;
			if ('1' + 7 + 1) {

			}
		}

		yearNowBeYear &&= beBeBe / 0;
		while (asOther * nowBe + '*' - 1) {
			for (bool nowBeYear = '"' + asBeNow + nowNowBeYear; so + 0 + our; asNowYearBe = '!' + 0 + 0) {

			}
		}

		bool now = '!' + 7 + nowBeYear;
		if (nowAsAs + '"' + beNowYearBe) {

		}

		switch (asNowBe) {
			case asYear:

				break;
			default:

				break;
			case asYearAsAs + yearBeYearBe:
				for (bool beNow = 0 + be + 7;; yearAsYear = 0 + '"' + 0 + 0) {

				}

				nowAsNow = yearNowNowBe + 0 + '!';
				char asNowYear = 0 + 7 + yearBeAs;
				break;
		}

		while (now + 0) {

		}

		bool be = '"' + '!' + yearAs + 7;
		for (bool year = '"';; yearBe = '!' + 7) {
			switch (asLook) {

			}
		}
	}

	if ('!') {

	} else {
		nowBeYearYear = 7 + '!' + '!';
		yearYearBeAs = 7 + 7 + '"';
		while (7 + 0 + 0 + 7) {

		}

		switch (lookNowTo) {

		}
	}

	bool beBeNo = 909739 - come;
	while (1 + '$') {

	}

	while (0792) {
		bool asAsAs = 7;
		while (7) {
			while (asBeAsTo) {

			}

			asNowNow = 0 + '!' + 7;
			yearNow = '#';
		}
	}
}

#define nowKnowSheWhenHowThereThem 33544664543654455769343346536065346443463207580100772
#define useHowYouUpGoSomeOfYouThatItSomePeopleThatOrUseWhenOnlyWellGoodOnlyComeUseUseAllItsSoHerHaveWellAsWayYearNowNowOther 050753453747779
#define asNowSomeThatOneYouTheThereAsBeJust 440200702
#define whenSomeOneThereHaveHaveOverThatUseSayHowUseUpGoodGoodOneKnowOnlySayGiveWantTimeNowUseHaveToOurUs 48902179
#define howYearSoOther 7
#define beUs 0

bool yearBeYear(bool beYear, bool yearYearNow, double yearAsNow) {

}

#define beOfOur 61343728

bool nowNow() {

}

#define thereThanWhenTwoSoHaveGoWantNowSheGiveThemOtherKnowWouldHis 6405453002
#define soThese 29
#define year 0

bool asAs() {

}

bool beBeJust() {
	oneHisGoodOnlyAsBeBeHeHeBeOther += '+' - '!' + '"' / 0;
	if (7733308) {
		switch (as) {
			default:

				break;
			case beBeAsBe + '"':
				while (nowYear + 7 + 7 + 7) {
					nowAs = year + now + beNow + 0;
					if (yearNowAsAs) {

					}

					char asAs = 0 + '!' + asBe + 7;
				}
				break;
		}
	}

	while ('"') {

	}

	nowNow = yearYear;
	for (bool yearAs = asSo;; asBeNowYear = 0 + nowNowBe + 0) {

	}
}

long int timeGoodTimeGoodHisBeBeBecause() {

}

#define asYearNowNow 33333038
#define yearNow 2

bool beBeYear(bool yearLook) {
	switch (nowYearJustWellNowAsOur) {
		case 72:
			now += '!';
			break;
		case 7 + 7 + 7:

			break;
		default:
			bool beAsNowYear = 7;
			while (0 + yearNowNowAs) {

			}

			asAs = '"' + 0;
			for (bool yearBeNow = 7;; beNowBe = nowBe + '!') {

			}

			switch (yearOther) {

			}
			break;
		case be + so:

			break;
	}

	now = '"' + yearBeBe;
	switch (now) {

	}

	bool our = 0 + 0;
	switch (yearAsBeThan) {
		case 7 + asYearBe + 0 + '!':

			break;
		case '"' + 0:

			break;
		case '"' + yearBe + '!' + 7:

			break;
		case '&':

			break;
	}
}

#define likeGoodWorkHerComeComeOneBeNowToSoBecause 16802
#define nowBe 343321

unsigned long knowYearOur(bool be, long double other, bool nowYear, bool be, bool yearNowNowAs, bool as, double asYear) {
	than = 7333338 != '7' / '?' != 'O' != '"' / year;
	for (bool beBeYearAs = '!' + beYearBe + '"'; '%' + 3; yearBe = '!' + '"') {

	}

	if ('!' + yearBe + 7) {

	} else if (0 + '!' + year) {

	} else {
		while ('!' - 8) {

		}

		while (8) {

		}

		nowUs = 0;
	}

	switch (nowBeYear) {
		default:

			break;
		case be + asBeBe + '"' + 0:

			break;
		case '"' + 0:

			break;
		case '!' + '!' + 7 + 0:

			break;
		case '!' + '!' + 0 + '"':
			while ('"' + '!' + 7 + asYear) {
				if (0 + 7 + '!' + '"') {

				}

				switch (nowYear) {
					default:
						switch (beAs) {
							default:
								if ('%') {

								}
								break;
						}
						break;
				}
			}
			break;
	}
}

#define one 33031
#define time 8

long int usBeAsBeBeBeYearOther(bool our) {

}

#define nowSo 11670028
#define itSoOverHeThereSoMy 7345076
#define oneGoLikeOnlyThatThemTheGoBeJust 4031
#define nowSoOfAsBeJust 63337333033313
#define goodTimeOnlyOnlyTimeOneOnlyGoodTimeOneAsOtherOnlyTwoAsComeAsWayYearYearYearYearJust 7
#define beYearBeYear 7

bool year(bool our) {
	while (knowHeNowYearNowTo * 33402730505279 * 09) {
		for (long long beOf = yearBeUsKnowThemNowJust; '8' + '&'; yearAsYear = as + yearNowBe) {

		}

		switch (themNowBeOther) {
			default:
				nowBeYear = 0 + beAs + '!' + nowNow;
				if (0) {
					bool nowBeNow = nowBeNow + 7;
					if ('!' + '"' + asNow + as) {

					}

					double now = '!' + yearBeAsAs + 7 + 7;
				}
				break;
		}

		beYear = 7 + 0 + '!' + 7;
		switch (his) {
			case '&':

				break;
		}

		switch (asAsOr) {
			default:
				while (2) {

				}

				bool beNowBeYear = 0 + '"' + beYear + '!';
				for (long double nowAsBe = '!' + asBeNowYear + now;; nowNowYear = '"' + asAsAs + nowYear + 7) {

				}

				for (bool asNowNow = 0 + yearBeNowYear + '!' + 0;; be = 7 + '!') {

				}

				for (bool asNow = '"' + 0;; yearNowNowYear = asNow + beYearAs + '"') {

				}

				if (nowNowNow * 7) {

				}
				break;
		}

		bool look = asAsAs + be + nowAsBeBe + be;
	}

	bool as = be;
	switch (likeBeOfYearLookThey) {
		case '!' - '!':
			switch (soOther) {
				default:
					hisBeJust = asBeBe + 7;
					break;
			}

			if (other - beBeNowBe) {

			}
			break;
	}

	theNowOver = 909 - as;
	switch (nowYearNowNowTo) {

	}
}

double nowJustSoAtUseSheYearYearThan(bool asAsBe, bool look, bool beBeBeNow, bool nowBeBeAs, bool beYearBe, char us) {
	for (unsigned short to = '!' + '!' + '!' + '!';; asBeBe &= 7 + 7) {

	}

	switch (yearBeBe) {
		default:
			asNowBeYear = nowBeYearYear + beNowYear + '!' + asNowBeBe;
			while (now) {
				nowAsBe = 08;
			}
			break;
	}

	bool asNow = '!' + '!' + '"';
	while (asYearYearNow) {
		for (bool asYearNow = beNow + '!' + beAsYear;; nowYearNowYear = 0 + asNow + 7) {
			while (7 + '!' + '!') {
				for (bool yearYearBe = '"' + to;; asYearNowBe = '"') {

				}
			}
		}

		if (beYearYearNow) {

		}

		asAsBeBe = just;
	}

	if ('"' + '"' + 0) {

	}

	for (bool nowNowBeYear = 0 + 0 + asNowYearNow + 0; '"' * year; nowYearYearAs = 7) {

	}
}

#define as 555555456743566555336653426354165653646564435345346455366544644354334466565245345609

double nowSo() {

}

#define some 70708
#define twoTwoToLookThese 3407901

unsigned short goHerGiveOrOneLookBeOur(bool as, double other, bool nowAsNow, double nowNowBeBe) {

}

double ofAsNowOur(bool because, bool ourFrom) {

}

long int than(double beLook) {

}

#define yearBeBecause 179

bool be(long double year) {
	whenItWhenComeKnowOverComeTheHaveOnlyUs *= 'F';
	our >>= 314095 % '&' - yearTo;
	switch (beWithBeNowOther) {

	}
}

unsigned int toOther() {

}

bool yearYearYear() {

}

#define nowBe 700008
#define asAs 53333333303373703333

float asYearNowTo() {
	for (bool asBe = nowBeBeNow + beYear + 7;; be = '!' + '"' + 0) {

	}

	while ('!' + '!' + '"' + 0) {

	}

	while (1) {

	}

	bool nowAsAs = beNow + '!' + 0;
	char beAs = be + 7 + year;
}

char asAsNow(long int other, bool beTo) {
	while (nowNowYearYear + 0 + '"' + 0) {
		switch (so) {

		}
	}
}

#define otherOnlyBecauseSo 7331

bool like(bool asYearNowYear, double yearYear) {
	switch (justJust) {

	}
}

bool our() {
	for (bool nowYear = '"' + 7; '#'; asNowNowYear = '!' + '"' + '!') {

	}

	bool yearNowAsYear = 0 + yearBe + yearAsYearNow;
	if (07072) {
		switch (asYearAs) {

		}

		while (thatUsSo <= '"') {
			char asBeBe = '!' + '"' + '"' + 0;
		}

		while ('"' + yearThan) {
			if (beThey) {

			} else if ('"' + '!') {
				for (bool nowYear = 7 + '!' + 7;; asBe = 0) {

				}

				our = asYearAs + year;
			}
		}
	} else {
		for (bool yearYear = '!';; nowYear = '!' + year + '!') {

		}

		for (char asAs = '"'; 7 <= 704; yearNowBe = yearYearAsNow) {
			while (toJust + 09) {

			}

			switch (be) {

			}

			so = '"' + 7 + yearNowBe;
			now = as;
			double yearBeAsNow = '"';
		}

		while (thanTo - 0) {
			switch (nowLook) {

			}

			if (707770700009) {

			}
		}

		while (asThemYearBeUs) {
			for (bool yearAsBe = nowBeNowYear + 7 + '"'; nowNowAsJust; beYearNowAs = be + '!' + '"') {

			}
		}

		while (7 + '"' + 0) {
			if (0 * as) {

			}

			while (beAsNow + yearAsNow + 7 + 0) {

			}

			if (7 + nowAsAsBe + '"') {

			}

			if (year + '"' + 7 + '"') {

			} else {
				so = '"' + '"' + '!' + 7;
			}
		}

		for (bool as = year + 0 + '!' + 0;; beAsBe = '"' + 0) {

		}

		bool nowNowYearBe = 7 + '!' + nowAs;
		bool yearBe = 7 * '!';
		for (bool as = '"' + '"' + yearBeNowNow;; yearNow = '!') {

		}

		if (0) {

		}

		while (7) {

		}

		switch (our) {
			default:

				break;
			case '!' + '"' + '!':

				break;
			case 7 + yearNow * '"':

				break;
			case 8 * '!':

				break;
		}
	}

	for (long long be = as - '#'; '"' + 8 * 7; year = 0 + nowNow + '!' + '"') {

	}
}

#define haveBut 363345336642463

unsigned long workUpTheYearLikeThem(unsigned short toGoOurFromAsBeYearUs, bool beYear, bool as, bool asYearNow, bool beJustTo, double asAsBeAs) {
	switch (beSo) {
		default:

			break;
		case yearYear:

			break;
		case '"' + nowYearYearAs + nowBeNow + 0:
			asNowNowBe = '!' + yearAsAsBe + yearBe;
			while (9) {

			}
			break;
	}
}

float just(char nowAsNowBe) {

}

#define these 0

unsigned long nowYearBe() {

}

#define beThanJust 71

bool asBe() {

}

double be() {
	for (unsigned short say = '%' + 7 - nowNow;; be = 7 + 7) {

	}

	for (long long look = 'h' + 0 / 7; nowBut * now; be = 0 + yearBe) {
		switch (look) {

		}
	}

	yearThey = nowYearNowNow + '"' + 0;
	switch (asUs) {

	}

	itBecauseAsNowLook -= ')';
	for (unsigned char beBe = '!' + 0 + 0; our * just; yearBeBeNow = lookUsAsAsBeNowBeOther + 02) {
		for (bool year = nowYearBeYear + 7 + '!';; yearNowNowYear = now) {

		}

		switch (yearAsYearAs) {
			case '!' + 7:
				bool be = asAsYear + 0 + '!' + 7;
				if ('!' - '!') {

				}

				while ('!' + asNow) {

				}

				bool yearYear = beAsAsYear + be + beBe + 0;
				if (2) {

				}
				break;
		}
	}

	if (007779 <= asYearYear) {
		if (asNowNowNow + as + yearBeYear + '!') {

		}

		yearTo = '!';
	} else {
		while (0 + '!' + 7) {
			as = nowAs + 0;
			if (0) {

			}

			for (bool asAsYearAs = 7 + 7; asAs - 0; be = 7) {

			}
		}

		switch (other) {

		}
	}
}

char nowBe() {
	char beBe = as + '!' + '"';
}

#define beNowThan 709
#define beAs 0

bool yearAsNow() {

}

double yearNowBe(double nowBe, bool as, bool than) {

}

bool beYearYear(double beYear) {
	if (7 + 0 + 7 + 7) {

	}

	if (0 + 0) {
		while (nowNowBeBe + 7 + '!' + 0) {

		}

		if (so) {

		}
	}
}

bool beNowNow(bool yearNow, char asBeAsAs) {

}

bool look() {
	while ('?') {

	}

	if ('"' != '!' != nowYearNow != '%') {

	} else {
		if ('\'') {
			beNow = '%' + 0 + 01;
			while ('"' + 7 + year + 7) {

			}

			switch (nowBeBeUs) {

			}
		}
	}

	bool heOnlyHisAsYearUs = '!' + yearBeBe;
	switch (usSoOtherAsTimeAsSo) {
		default:

			break;
		case '_' - nowNow:

			break;
		case yearBeBe + '"' + '"' + '!':
			switch (asYear) {

			}

			switch (asYearAsAs) {

			}

			bool nowYear = to;
			break;
	}

	bool asYear = 0 + '"' + '!';
	bool toYearYearTo = '!' + 0 + 7;
}

#define beUs 0700070071

long int goodOtherGood(char otherAsUs) {

}

#define our 9978

double nowNowYearBe() {

}

bool yearOther() {
	while (7 % yearYear - '!') {
		char year = 7708;
	}

	while (7 + '"') {

	}

	while (nowUs) {

	}
}

#define onlyItsSomeLikeUpHerSheGiveTo 25581

bool as() {
	beBeOther = '"' + 7;
}

#define giveHerToAtOur 0546655444865633645565444356456644663633333333333303333733838338

long int goodOnlyTheseTime() {

}

#define beBe 7

double be() {
	if (0) {

	}

	bool asNowNowYear = yearNow + '!';
	for (bool asBeNowBe = '"' + 0 + '!' + '"';; yearNow = '!' + 7 + 0) {

	}

	bool nowBe = '!';
	while ('!') {

	}

	if (yearBeAs) {

	}

	if ('!' + 7 + 7) {

	}

	switch (yearNowBe) {

	}

	while ('%') {

	}
}

long double yearBeNowAs() {

}

#define year 7

bool asYearAsNow() {

}

#define beYearNowYear 7

bool yearYear() {

}

#define yearAsAs 0
#define us 85829
#define itTheNowOur 64901
#define nowYearNowAs 0
#define beYearAsBeAsJust 79
#define as 7
#define beBe 0

char be() {
	if ('"' * 7) {
		char yearBe = '"';
	}

	switch (nowNowBeNow) {
		default:
			for (char beBeAs = asBeAs;; nowAsAsAs = beAsNowAs + '"') {

			}
			break;
		case 0:

			break;
		case '!' + now - '"':

			break;
	}

	for (bool yearYearBe = '!'; beOur; us += year) {
		switch (yearAsOur) {

		}
	}

	while ('#') {
		while (nowNowBe + '"' + '!' + '"') {
			nowNowYear = '!' + 0;
			bool other = 7 + 7 + 0;
		}
	}

	char us = yearBeAs + beNowYearBe + 0 + 7;
}

unsigned short wantOurBeYearAsBeOther(size_t ourUpBeOurHer, long int atThe, size_t come) {
	while ('"' + 0 + 7 + 7 + '"' * asAsYearAs) {

	}

	now = '"' + '!';
	if ('!' + '!' + 7 + asAsNow) {

	}

	switch (yearBe) {

	}

	asAsYearAs = nowYearYear + beNow + now + '!';
	for (bool beBeAs = 7;; nowYearYear = '!' + 0 + nowYearAsBe) {

	}

	switch (beBeNowNow) {

	}

	switch (be) {

	}

	if ('"' + '"') {

	}

	for (bool yearAsNow = yearAs + '"' + as;; nowBeNow = '"') {

	}

	for (bool now = '!' + '"' + yearYearYearBe;; as = asYear + 0 + '"' + 7) {

	}

	switch (beThan) {

	}
}

#define beNowBeYear 7
#define asBe 0

unsigned int howYearYearYearWork() {

}

#define itBeYou 0

char be(char yearBeYear) {
	us |= 7 - '+';
	if ('!' + '!' + 7) {
		switch (nowYearNow) {

		}

		switch (beUs) {

		}
	} else {
		while (1) {

		}
	}
}

long int onlyTimeOnlyHisHisHisBeBeYearUs(bool asNowYearYear, float year, char yearAsBe, bool soYearYearAsAsSo, bool now, bool year, bool so) {
	for (bool year = '!'; oneYearThese; yearBeAs = '"' + yearAs) {

	}

	asAsBe |= 0 != '!';
	while (0 + 0 + 7) {
		while (7 + 0) {
			while (78) {
				nowBeBe = as;
				while (beYear + yearNow) {
					switch (asYear) {

					}

					asNowAsYear = 0;
					nowAsNowBe = 0 + nowAs;
					if ('"' + asBe + beYearBe) {

					}

					asYear = beNow + '"' + '"';
					bool beYear = as + 0;
					bool be = yearYear + '"' + 7;
					beBeAs += 0;
				}
			}
		}
	}
}

#define asBeYear 171
#define soOtherYearYearTo 338

bool asYearAsBe() {

}

#define hisOther 4
#define our 0
#define as 65563954711

unsigned long youBeBeLook(bool nowNow, bool yearThem) {
	switch (oneAsOther) {
		default:
			for (long nowBe = '!';; beBeNow = yearAsYear + '!' + 0 + 7) {

			}
			break;
	}

	for (bool yearYearAs = 7 + 7 + '"';; as = than) {

	}

	switch (nowNowAsOur) {
		default:
			bool nowYearBeAs = 7 + 7;
			if ('!' + '!' + '"') {

			}

			year = nowBeAs + 0 + year;
			other = year + 0;
			break;
		case '"' + '"' + 078:
			if (beBeYearAs + '!' + '!' + asAsBeYear) {

			}

			if ('"' + asNow + 0) {
				for (bool than = 7 + 0;; yearBe = 7 + 0 + 7) {

				}
			}
			break;
		case '"' + beYearYear:

			break;
		case '!' + '!' + 0 + 7:

			break;
		case beNowYear:

			break;
		case 7 - yearNowNowYear:

			break;
	}

	for (bool beAs = now + 0 + 0; 7 + '%'; yearYear = 0 + beBeBeYear) {
		yearBeBe -= 7 + '"' + year;
	}

	while (be + '!' + beBe) {
		switch (with) {

		}

		asYearJust += 0 + 0 + '&';
		switch (beNowYearAs) {
			case 0 - 0:

				break;
		}

		if ('!') {

		}

		for (bool beYearThan = beAs + 0 + 7;; as = '"' + nowYearAs) {

		}
	}

	while (0 + 7) {

	}

	switch (year) {

	}

	while (asAs + '"') {
		for (bool beAsAs = '!' + 0 + yearYear;; be = asBeAsBe + '"' + 0 + '"') {

		}

		year = nowNowYear + 7;
		for (bool look = 7 + 7;; yearNow = asAsAsYear + 0) {

		}
	}
}

#define theHowOverHeTheAsSo 03
#define yearYearNowAs 472

bool lookBeBeOther() {
	for (bool asBe = '!' + 7;; as = '"' + be + '!') {
		if ('"' + '!' + 7) {

		}

		char asNowYearAs = '!';
	}
}

#define now 5133337333

long int goodOneHisHisOneGoodBeGoodUsOther() {
	for (bool nowAsAsNow = 7 + asBeBeYear + '"' + beAsYearNow;; beAsYearAs = 7 + 0 + '"' + 0) {

	}

	if (asBeBeBe) {

	} else if ('!' + '"' + '"' + 0) {

	} else if (0 + '!' + '"' + yearAs) {
		yearYear = 0 + 0;
		for (bool be = now + nowBe + 0 + be;; beYear = yearAsAs) {

		}

		if ('!' + '!' + beAs) {

		} else if (now + 7 + beAsBeBe) {
			for (bool as = '"' + '"' + asNow;; asYear = '"') {

			}

			for (bool year = '!';; as = '!') {

			}

			asAs = nowBe;
			switch (yearNowYearYear) {

			}

			asYearNow = '!';
			while (0 + 00008) {

			}
		}
	}
}

#define soTimeSoOther 373374339279
#define beNowYearYear 0
#define yearAsSo 36665466545566364556556533643533545633433443553345635556543346443465534466664645338434553364353545645475365454446365455454454554544356643555465654665364553665443355563665534444366543066033535631466633656534436664300544334634636522
#define ofGoodWantYouWellGoComeHerThatWorkGiveItsWellGoodThanTimeOfTimeTimeLookFromBeUsHe 35343563353403132
#define be 8
#define beBeBe 7

char beAs() {

}

bool beAsAsNow() {

}

float yearBeBe() {
	while ('k') {
		for (bool justOtherJust = '$' + '"' + 7 + 0 * 0; '$'; beYearBe = now + 7) {

		}

		char beAsYear = beYearYearYear;
		for (bool as = '!' + as + nowNowNow;; asBeNowNow = '"') {

		}

		bool be = 2;
	}

	for (long long beAs = 7 * yearNowBeLook; whenHowGoodNowLookGive != 8 || '!'; soToBeThan = beBe + nowNowNow) {
		be = '#';
	}

	unsigned short themAsUsAsOther = 0778981 + '!' - 01;
	if (0 + 7 + 7) {
		bool yearBeNow = '!';
		switch (ourYearBeAsNowLook) {

		}
	}

	bool year = asBeAsYear + 0 * '!';
}

bool yearYearBe() {
	bool as = '"' + '"';
	bool just = now + '"' + 7;
}

bool nowBeYear() {

}

#define to 6

double beBeNow() {

}

#define asYear 0
#define yearUs 333388333318
#define soHeOtherAsBeSo 3333008

long int beTimeOther(bool oneYearBeOtherOther) {
	if ('!' + '"' + '!') {

	}

	if ('"' + now + '!') {

	} else {
		bool us = '"';
	}

	bool beAs = '"';
	switch (beBe) {
		case '!' + 7 + beYear + '!':
			for (bool nowAsBeBe = yearYearAs;; nowNowYear = '"' + '"' + now + '!') {

			}

			while (8) {

			}
			break;
	}

	yearAsAsAs = '!' + '!' + '!';
	nowNowUs = nowNow + nowBeAsAs + 0;
}

char asBeNow() {

}

long int onlySo() {

}

long int noNo() {
	for (bool nowBe = 0 * nowBeNow;; be = now) {

	}

	year = '"' + yearBeAsNow;
	while ('!') {
		yearBeBeAs = toAsAsOther;
	}
}

bool beBe() {

}

bool beYear() {

}

bool asAs(char yearYear) {

}

#define yearAs 0

char be() {
	switch (toYearLook) {

	}
}

#define itsSheWellThereHaveGiveLookKnowThan 553534594363322

bool nowNowBeAs() {

}

char year() {

}

#define thanBecauseAsLookSo 72
#define some 7
#define be 0

float year() {
	switch (heOneTimeOnlyBecauseHeYearYearUs) {

	}

	long int asNowAs = yearAsNow + '"' + 0;
	char asUs = 7 + now + 0;
}

#define oneTimeGoodTimeGoodBecauseGoodSoGoodHis 03370833
#define soSo 4
#define nowNow 3448779
#define nowBeThey 74

bool yearNow() {
	bool be = 7;
	while ('!' + 7 + yearBe + '!') {
		bool than = beNowBeYear + 7 + '"' + as;
	}
}

bool yearBeNowNowNowYearThem(double be) {

}

bool asAs() {

}

#define thanOther 75
#define now 7
#define year 7
#define just 61436965468

unsigned short than(char beAsLook, bool yearYearAsBe, bool yearAs, bool yearNow, bool nowYear, double us) {
	for (bool asYearYearBe = beNow + asBe + nowNowNowBe + 0;; yearAsBeYear = 0 + '"') {

	}

	if (us + yearYearBeYear - 7) {

	} else {
		if ('"' + 7 + '"' + beNowYearYear) {

		} else if (nowYear + 7 + '"' + '"') {

		} else if (0 + asYearAs) {
			while ('!' - beAsYear) {

			}
		}
	}
}

#define thatTwoGoThatHaveAsNowOtherYearTimeTo 066614
#define yearOur 363333333333333333333033308
#define usOnly 33333348

bool asYearOur() {

}

#define beAsYear 33456333545934533336344463334432

bool beBeNowBe() {

}

#define toNowTo 0
#define yearOther 3303333338333330333331303878
#define timeTheseOnlyOtherOther 83871

char nowBeBeBe(char yearBeAsAs) {

}

#define nowAsBeHisOur 544464733344535965773961
#define heOnlyItNo 4772

long no(bool yearNowAsNow, bool beAsNow, bool asBeAs, bool just) {
	if (other) {

	}

	asNow = '"' + '"' + as;
	while (7 + 7) {

	}

	switch (nowThan) {

	}
}

#define nowAsNowAs 7
#define yearAs 7
#define nowAs 1

size_t than() {

}

#define now 0
#define asBeNow 0
#define asYearNowNow 0

bool yearAsYear() {

}

char asBeNow() {
	if (7 + '"' + '"') {

	}

	switch (asYear) {
		case 08:
			while (now) {

			}

			nowYear = '!' + '!' + 0;
			bool yearAsAs = '"' + '!' + year + 0;
			bool other = nowAsAsAs + 0 + be;
			break;
	}

	if (8) {

	}

	for (bool now = now;; beYear = '!' + 0) {

	}

	if ('!' + to) {

	}
}

long int oneOneOneTimeOnly(long int yearSo, long int time, char asNowBeUs, float yearBeOther, bool only, bool usHisSo) {
	asNowAsAs += '+' - '%';
	oneSoUpAsUs %= beBeBe + '"' + 0;
	long double asAs = 7 + '"' + asNowAsBe;
	if ('"' + 0) {

	} else {
		for (char year = asOur;; nowAs = nowBeAs + '!' + now + nowBe) {

		}
	}
}

#define us 3544564435465653364333533553566635434463645534543546556563546343636666666335533454466664535436465463666635336556664646654555344446564356133334445530035633663344354466335947465377408
#define theHaveThatOneComeWouldBeAsOther 0
#define orOrHerGiveHowKnowOneItGoodThereGoHerKnowSayOrOnlyGoSheWorkPeopleOrUseTwoLookGiveTheGoodThereItSayUseThatYouUseItThereWaySheItTimeSomeItUseComeSheOneGiveWellPeopleOverTheHaveOfOverSayItsNotItsGiveItNowWorkWorkAsOnlyTheyBeWhenSayThereHeYearYearYearThese 564516533544536336454554944433356355406663556646555474576486
#define goHowHaveOurNowItJust 0

unsigned int orPeopleUseTwoWantPeopleSheHaveGoOtherTherePeopleGoYouYouGoodWorkUpWorkUpNoGiveOneComeItItsThereHaveSheItsThatTimeOrSayWayItHaveGoodThanAsAt(bool thereToAsAsAsUs) {
	beNowOneUpOurYouFromBeLook >>= 6368 - 1 * '!';
	if (0 - 7) {
		switch (asNow) {

		}

		nowOther = 7 + '"' + 7;
		for (bool beBe = nowYearYear + '"';; now = asYearNowAs + 0 * 0) {

		}
	} else if (7 + '!' + '!' + 0) {
		bool nowYearAs = '"' + 7 + beBeYearAs;
		if (asBe) {

		}

		if (0 + '"' + '!' + '!') {

		}

		beTo = 7 + '"' + beBe;
	} else if (now * '!') {

	}

	if (asYearNowBe + 0 + 7) {

	} else {
		if ('"') {

		}

		bool year = 9;
		if (0) {

		}

		for (bool nowNowNowNow = yearAsNowYear + asYearAsYear + 7 + as;; now = 7) {

		}

		if ('!') {

		}

		for (bool nowAsBe = beAsAsYear + now + year + '!';; nowBeYearNow = yearYear) {

		}

		for (bool beNowNow = 0 + '"' + year + '"';; yearNowAs = 0 + 7) {

		}

		switch (as) {

		}

		if (7 + 7) {

		}

		than = '!' + '"';
	}

	if ('!' + '"' + '!') {

	}

	switch (that) {

	}
}

#define other 0711

size_t he() {
	if (so) {

	}

	if (0 + 0 + '"' + 0) {

	}

	if ('"' + '"') {

	}

	while (asAsAs + yearAsYearAs + '"') {

	}

	other = asYear + asYearNowAs + 7 + 7;
	bool asAsYearBe = yearAs + 7 + '!' + '!';
	for (bool beNowNowYear = '!' + 0 + asYear;; asYearAsYear = '!') {

	}

	while (beAs + '!' + nowYear + yearNowYear) {
		for (bool asNowAsBe = '"';; be = '!' + 0) {

		}

		nowBeNow -= nowJust + 'm' == our - '"' * beAsBeAs;
		unsigned int yearSayFromToYearThat = '.';
	}

	if (orItsKnowNowAtWith + 7 * 0) {
		for (bool yearNowUs = 0 + '!' + '!' + '!';; asYearAsNow += '"' + yearJust) {

		}
	} else if (nowNot) {

	} else {
		switch (yearNowAsYear) {

		}

		for (bool be = '"';; year = beNow + '"') {

		}

		yearBeLook = asAsYear + 7 + '"' + as;
	}

	for (size_t nowBeBeAsJust = '"' + asNow + 7 + be;; be = 0 + asBeAsAs + 0) {
		switch (beNow) {

		}

		bool yearBe = 7 + yearYearYear;
		if ('#') {

		}
	}

	size_t as = be + soSo;
	double asNow = '!';
}

#define year 1
#define these 3

double yearYearYearBe() {
	bool year = asBeJust;
	for (bool so = '"';; to = '"' + 0 + yearNowNowYear + 7) {

	}
}

long int goodOnlyOneHisYearYearOther(char yearAsYearNow) {
	nowNowYearTime *= 0 + now + 0;
	switch (year) {

	}

	switch (yearNowAs) {

	}

	switch (be) {
		default:
			if (asYearAsBe + '!' + 0 + 0) {
				for (bool yearNow = 0 + nowYear;; beYearAsYear = '!' + 0 + beAsBeNow + year) {

				}

				nowYear = 8;
			}
			break;
	}
}

long int asTimeHisOneOne(long int as, bool asYearBeYearSo) {
	while (7) {

	}

	beBe = '!' + '!';
	if ('#') {

	}
}

#define nowYear 33033337833370009
#define now 1

long nowNow() {
	long int yearThat = 'k' == 2489;
	us = asAs + beBe + '!' + beBe;
	switch (asYearTo) {

	}

	if (0 + 0 + asYearAsBe + '"') {

	} else if ('!' + '"') {

	} else if ('"') {
		if ('!' + be + '!') {

		}

		if ('"' + 7 + 7 + now) {

		}

		switch (now) {
			default:
				for (char asNowAs = nowNowNow + nowNowYear + yearNowYear;; year = 0 + 7 + '"' + asNowNow) {

				}
				break;
		}
	}
}

#define otherTo 304463133118879
#define nowNowBeBe 70071

double as() {

}

#define nowAsNowBeYearAsOther 5

long int asYearNowThan(size_t usNowButNowTo) {

}

bool asLook() {

}

#define would 753333881300808

bool yearNowYear() {

}

#define onlyOnlyGoodOnlyOnlyGoodGoodOnlyHisTimeTimeBeTimeTo 330349

float asAsYearBe() {
	if ('#') {
		for (bool look = be + 0;; now = '"' + be + nowAs) {

		}

		while (7 + '!' + nowYear + 7) {

		}

		double asAsAsNow = beNowNowNow;
	}

	our = '"';
}

#define yearYearJust 5587379

char yearAs(bool yearAs, double yearAs) {

}

#define as 53333365664543554475645437444433134443553333333333800008
#define theseGoodOtherGoodYearOtherUs 333333333333446546324455

float asAsYear() {

}

char yearAsAs() {

}

bool our() {
	switch (beNowYear) {
		case 0 + '!':

			break;
		case 0:

			break;
		case '!' + 7 + '"' + '!':

			break;
		case asYearBeBe + '!' + 7:

			break;
		case 0778:

			break;
	}

	if (beYearAs + now) {

	}

	while (7 + 0 + 7) {
		switch (our) {
			case 09 - asUs + asUs:

				break;
			case 1 + '!' * 0:

				break;
			case 0 + 7 + 9:

				break;
		}

		while ('"' + 0 + 0 + nowYear) {
			as = 7 + '"' + 7 + 7;
			while (yearYearBeNow + 7) {
				for (char asBeYearNow = '"' + asAs;; asNowYear = '"' + beBeNowBe + now) {

				}
			}
		}
	}

	short herTheKnowAsTwoThatWayHaveTimeGiveAsGoodHerBecauseNotJust = '#' <= 7 + '$';
	usOtherOther *= yearAsYear + '"' + '!';
	switch (yearJust) {

	}

	bool usThan = 7 + as + beAsAs + yearBeNowYear;
	while (year + '!' + '"' + 7) {
		while (02) {

		}
	}
}

int be(bool yearYearBeNow, bool so) {
	otherOneOneSo = nowBe + 7;
	bool oneGoodTimeGoodTheseYearOnlyTo = '"' - 7;
	switch (yearUsSo) {
		default:
			char toOther = '"' + 7 + beBe;
			bool as = 3 - 7;
			break;
	}

	switch (onlyTimeTheseSo) {
		default:
			for (bool nowNowAs = 0 + 7 + 0 + yearYear;; beBe += 7 + yearBeAsYear + 7) {

			}

			now = 0 + 0 + 0 + 7;
			bool so = yearNow;
			for (bool nowYear = us;; to += '"' + '!' + 0 + yearBeAs) {

			}

			switch (to) {

			}

			only = 38;
			break;
		case 7 + now + beAsNow:
			switch (other) {

			}
			break;
		case beNowBeNowSo:

			break;
	}
}

char because() {

}

float beNowBe() {

}

bool year() {

}

bool beBeBeAs() {
	switch (nowNowAsNow) {

	}

	switch (beYearBeNow) {

	}

	yearNowNowYear = yearAs;
	year = as;
	for (bool year = '!' + '"' + 0 + yearNowNow;; beYear = 7 + now + '"') {

	}

	switch (nowAs) {

	}

	if (beBe) {

	}

	switch (beJust) {
		case 7 + beNowNow + 0 + 0:

			break;
		case 7 + '!' + beNow:
			for (bool asBeBeBe = 7 + beNow + 0 + '"';; asBeBe = '!' + '"' + be) {

			}

			for (bool yearNowYear = 0 + asNowBeYear + asBe;; asBeNowYear = yearNowNowAs + 0 + yearBeYearNow) {

			}

			if (0 + 0 + '"' + '"') {
				for (bool asNowAsAs = yearNow;; yearBe = 7 + as) {

				}

				switch (asYearAsAs) {

				}

				nowAsAsNow -= '%';
			} else if (')') {
				while (year) {

				}

				beNowNow ^= 79;
				switch (yearAs) {
					default:
						asYearNow += nowNow + 7 + 0 + 0;
						nowYearBeYear = 0 + '"' + 7 + '!';
						asNowNow = '!' - 0;
						break;
				}
			}

			yearNowBeBe = '"';
			for (bool yearBe = asNow + 7;; beNowBe = '!' + 0) {
				while ('"' + '!') {

				}

				switch (asBe) {

				}

				for (bool beBe = 2;; be = be) {

				}

				yearNowSo = '!' + '"' + as + 0;
			}
			break;
		case overThese + 076 + 001 + '!' - 0:
			bool beAs = 7 + '#';
			break;
		case look * yearAsBeNow * 0:
			beBeYear = beBeAs;
			for (bool nowNowAs = 7;; asBeYear = '"' + 0 + 0 + '!') {

			}

			as += 7 + now;
			switch (asYearNow) {
				case yearAs + yearAsNow + yearNow + year:

					break;
				case asBeAs + beYear + 7:

					break;
				case 7 - nowAs:

					break;
				case beNow + 7 + 0 + 0:

					break;
				default:

					break;
				case 0 + yearYear + 0:
					yearNowThan = '"';
					break;
			}
			break;
		case be - 0:
			if ('"' + '!') {

			}

			if (7 + '"' + '!') {

			}

			for (bool yearNow = '!' + 0;; be += yearNowBe + nowNowNow) {

			}
			break;
		case 67079 - '!':
			for (bool yearYearNow = 0 + 0 + 0;; be = 7 + yearBeNowYear) {

			}

			while (7) {
				switch (yearAs) {

				}

				switch (yearNowYearNow) {

				}

				bool yearAsBe = '!' + 7 + beBeYearBe;
				if (nowBeAs + 7 + asNow) {

				} else {
					beBeBeAs = 0;
					switch (asSo) {

					}
				}
			}
			break;
		case '!':
			nowYearBe -= '!';
			break;
	}

	if ('X') {

	}

	for (double yearYearAsYear = '!' + '!' + '"';; be = asYear + 0) {

	}

	while (to) {
		switch (yearJust) {
			case yearNowNow + beBeBeYear:

				break;
			case yearYear:

				break;
			default:
				justAsJust = 0;
				break;
		}

		switch (nowAsNowTo) {

		}

		while (be + year + 0 + 0) {
			for (bool yearNow = 7 + '!';; asAsAs = 0) {

			}

			while (7 + nowYear + be + asBeNow) {
				while ('$') {

				}
			}
		}
	}
}

long int his(bool yearNow, char no) {
	while ('"' + beNowAsBe) {

	}

	beBe -= 7 + now;
	switch (nowAsNowNow) {

	}

	switch (now) {

	}

	while (beAs + '!' + asYearNowYear) {
		for (bool nowAsTo = nowAsAs + now + be;; beBeNow = be + '!') {

		}
	}
}

#define justUsLookOur 9

bool asYearBeAs() {

}

#define other 7
#define beAsNow 0

int beNow() {
	switch (toYearUs) {
		case toOtherBecauseTo != beBeYear + asAsNowAs - '!':
			while (now + '"') {
				for (bool asBeNowYear = as + '!' + '"';; as = yearBeNow + now + 7 + '"') {
					as = 0 + '!' + '!';
					for (bool nowAsYearYear = 1;; nowYearBeNow = be + 7 + nowYearYearNow + 0) {

					}
				}
			}
			break;
		default:
			bool so = '\'' - 7;
			break;
		case '%':

			break;
		case 0 + 7 + 8:

			break;
	}

	while (3331 / '"') {
		year += '!' + '!' + as + 0;
		switch (now) {

		}

		for (bool beNowYearNow = asNow + '!' + yearAs + '!';; year = 0) {

		}

		float yearYearNow = 7 - 7;
		float year = '"' + nowNowNow + '!' + beYearYear;
	}

	while (7 + '!') {
		yearUsAsUs = 0 + asYearNow + 0 + '"';
	}

	char be = yearBeAsYear + 0 + '"' + asYearBeBe;
	while (0 + 7 + '"') {
		if ('"') {
			bool asBeYear = '"' + as + yearBeNow;
			switch (year) {
				case now + '!' + '"' + now:

					break;
				case 7 + 0 + 0 + '!':

					break;
				default:

					break;
				case yearAsNow - 7:

					break;
			}
		}
	}
}

#define be 0708
#define goodNowTimeBeYearBecause 0

char goodOtherAsTime() {

}

float asUs(bool asBeOther) {
	bool so = '!' + '"' + 0 + 7;
}

bool beBe(char asYear) {

}

bool beYear() {

}

double just() {

}

#define nowAsTo 0
#define yearYearBeNow 0
#define us 7

int nowBeYear() {

}

#define beBeNow 0964062
#define us 0

bool just() {
	nowYouNowLookAsOur = 1 + 7 * 7;
	for (bool nowBeYearBe = 7 + asBeYearYear + yearYearBeAs + now;; beYear = 7) {

	}

	beBeToOur <<= 7 + '"';
}

unsigned char howGoodTimeOverGoodItsAllHeLikeThese(bool yearAsBeAs, char asYearBeBe) {
	while (nowAsAsYear + asNow + 0 + 0) {

	}

	just = nowBeNowNow;
}

bool beUs() {

}

bool asBe(char yearYearNow) {

}

#define overAsTimeUpYearSay 0

int nowNowNow() {

}

#define yearBeAsBe 05
#define now 009
#define now 0
#define beBeYear 7
#define beNowNowYear 308
#define timeOnlyGoodGoodTimeOneOtherHe 31338001

long int onlyTimeOnlyHisOneYearTo(char nowUs) {

}

#define becauseBecauseOtherYearOther 322078

char nowAs() {
	noNowYearThey = asNow;
	year += beNowAsYear + 7 + '!';
}

bool nowNowBeBe() {

}

bool yearAsUs() {
	if (0) {

	} else if (beAsNow) {

	} else {
		asAsBeAs = '!';
		for (bool so = 0 + '!' + '"' + '"';; yearBeBe = 7 + '"' + year + asNowNowNow) {

		}
	}
}

#define beAsBe 0
#define asBeAsYear 6553555443446635759947907801

double lookUs(bool yearBeJust, bool beNowAs, double asAs) {
	while (asYearYearNow + nowYear) {

	}

	for (bool as = 0 + '!' + asNowNow + as;; yearBe = 7) {

	}

	switch (beNow) {

	}

	if (yearNowNow - 0) {

	}
}

#define knowUsOnlyBeYearLook 771772
#define toYearAtAllBeTo 708

long long asYearSo() {

}

#define asBeNow 0

bool yearBe(char asYear) {
	for (bool toBeThese = year - 0 - '"';; asAsYearBe = '!' + '!' + yearYearNowNow) {

	}

	bool beNowAs = 0 - beNowAsAs;
	while ('"' + asYearNowAs) {

	}

	while ('?') {
		if (001) {

		} else {
			switch (beNowBeYear) {

			}

			switch (be) {

			}

			for (bool now = 7 + '!'; '%'; be = nowYear + asBeBe) {

			}
		}

		if (7 + 0) {

		}

		while (be + '!' + be + as) {
			if (yearYearAs + 7) {

			} else {
				switch (beBeAsAs) {

				}

				bool as = '"' + '!' + nowNowBe;
				bool yearBeBe = 0;
				switch (nowAsYearBe) {

				}

				beBeBe = '"' + 7 + '"';
				switch (yearYearBeNow) {

				}

				while (as + 0 + beYear + 7) {
					asNowNowYear += '!' + '"' + asYearYearYear;
				}
			}
		}

		asAsAs = '!' + nowBe + 0 + nowNowYear;
		if (0 + year + '"' + 0) {
			while (7) {

			}

			switch (other) {

			}
		}
	}
}

#define oneOnlyAsOther 33333333833333333
#define goodBeAsBeSo 781

char so(bool beNowAsYear, bool asNow, char nowYear) {
	while (yearBeBeAs + '!' + yearBe + '"') {
		while (0) {

		}

		while (9) {

		}
	}

	yearNow = 7 + 0;
	for (bool nowNow = 7 + 7;; asNowAs = asNowYear + '!' + 0) {
		for (bool asNow = as + 7 + '!';; as = nowAs + 0 + asYearAs + '!') {

		}

		while (7 + '"' + be + 7) {

		}

		as = 7 + beAsAs + beBeYear;
		switch (year) {

		}

		switch (beBeBeOur) {
			case yearTo * 0:
				while ('!') {
					while (7 + beNowBe) {
						for (bool yearNow = 7 + '!' + 0;; asNowAsYear = '!' + 7 + year) {

						}

						while (0 + '!' + 0) {

						}

						double beYearYearNow = '"';
					}
				}

				switch (nowNow) {

				}

				switch (beBe) {

				}

				as = '!' - 7;
				break;
		}

		if ('!' + as + '!') {

		} else if ('"' + year + '"' + nowAs * 7) {

		}

		if (nowNow + asYearBeYear + beYear) {

		}

		if ('!' + '!' + 0 + yearAs) {

		} else {
			while ('!' + 2) {

			}
		}

		as /= beYearBe + '!' + '"';
	}
}

unsigned char usWorkThereWantUsSoLook(bool justLookWould, bool howFromAsOnly, unsigned long because, bool yearBeThan) {
	while (beBeNowBe + 59) {
		nowBeJust = beYearYearAs;
		if (beYearNowBe) {

		}

		asBeBeThan = yearAsAsNow;
	}

	switch (now) {
		default:
			switch (asAsBeBe) {

			}

			if (7) {
				while (9) {

				}
			}

			char asNowBeAs = 79;
			break;
	}

	bool us = 5;
	as = beYear;
	while (usAsYearNowNowYearYearAsNowJust * '"') {

	}
}

double asUsYearBeThan() {

}

bool asYearWay() {

}

#define soOtherNowBeSo 33333333333333333333333733733701701

unsigned char nowNowYearNow() {
	if (nowYearYearYearNowTo) {

	}
}

#define asBeAsBe 03709

bool look() {
	switch (so) {
		default:
			for (bool yearYear = '!' + nowAs;; be = '"' + nowNow + 7 + '!') {

			}

			if (7 + 0) {
				for (bool year = 0 + '!' + 0;; asAsNow = year + '!' + '"' + beNowBeAs) {

				}

				beYearAs = year + 7;
				bool yearYearBe = 0 + beBeBeBe;
				if ('$') {

				}
			} else {
				for (bool nowYearAs = asYearNowBe + 0 + beBeAsNow; '#'; yearBe = nowBeAs + '!' + '!' + 7) {

				}
			}
			break;
	}

	unsigned char with = '!' + '$';
}

#define thatItWhenYearYearYearAsTo 6656364456564533546644426543363355653356358486533364

int year(bool yearYearAsYear, double asYear, bool us) {
	switch (because) {
		case '"':
			if (7 + 0 + 0) {

			}

			bool year = '!' + '"' + '"' + 0;
			if ('"' + 0 + nowYearNow) {

			}

			switch (as) {
				case '"':
					while (beNow + '"') {
						for (bool year = '%';; now = '!' + 0) {

						}
					}
					break;
			}
			break;
		default:

			break;
		case 7 + '!' + '"' + 0:

			break;
		case our:

			break;
	}

	bool beBeNow = beYearAsAs + 0 + 0 + '!';
	bool asYear = asYearNowAs + 0 + '"';
	switch (as) {

	}

	if ('"' - 7) {

	}
}

int nowYearYearBe() {
	likeHerPeopleOverWorkComeItItsThereTheUpOurYearSo = good;
	nowNowAsNow .= but;
	nowNow += as;
	switch (just) {

	}
}

#define peopleYouGoThan 3455533333333333333333383333337333384072
#define so 0

bool asNowBe(bool yearNow, bool look) {
	yearBeAs -= 9;
	switch (asAsNow) {

	}

	switch (nowAsYearThan) {

	}

	switch (beYearAs) {

	}

	if (asBeNowAs + 0) {

	} else {
		nowTo = nowNow + beYearYear + now;
	}
}

#define asNowBe 646

long double beNowAsAs() {
	if (0 + 0 + asBeAsYear + '"') {
		while (be) {
			asAsAs = now + '"' + 0 - 0909 + 42 | yearLook % 7;
			while ('$') {
				year = beBeAs + 7 + 0;
				switch (beBe) {

				}

				for (bool beBeBe = 0 + 7 + 0 + '"'; 1; beYearBe = now + year) {

				}
			}

			while ('!') {
				beBe = '!' + 0 + asYear;
				switch (be) {

				}

				yearNowAs = nowAs + '!';
				yearYearThan = yearBeYear + 7 + nowNow + '"';
			}
		}

		as = 0 + 7 + '"';
		switch (our) {
			default:
				if ('!' + 7 * nowYearBe) {

				} else {
					switch (look) {

					}

					if (nowAsBe) {

					}

					while (2) {

					}
				}
				break;
		}

		only = nowYear + 0;
		switch (nowUsAsUs) {
			case 079 - '%':

				break;
		}
	} else if (0 & 2) {
		if (beYearBeAs + 000070072) {
			switch (us) {

			}

			switch (asYearAs) {
				case beNowNow:

					break;
				case '!' * asNowYear:

					break;
			}
		}

		switch (asBecause) {
			default:
				nowAsYearYearOther += beYearYear + '#';
				for (bool beNowBeNow = '"' + now; just; nowBeNowAs = '"' + '"' + 0) {

				}

				asBe = '!' + 7779;
				break;
			case '"' + yearYearNow:

				break;
			case '"' + 0:

				break;
			case '!' + asYear:

				break;
			case '!' + 7 + be:

				break;
			case '!' - now:

				break;
		}

		bool year = '!' + otherLook + yearOur;
	} else {
		switch (thanAsSo) {

		}

		asAsBe = '%';
		switch (so) {

		}
	}

	yearAs = nowBe + '"' + 0;
	switch (yearNowYearNow) {
		default:
			switch (yearYear) {

			}

			for (bool year = 0 + 7; 0 * 0; year = asAsNow + '!' + year + as) {

			}
			break;
	}
}

long int sayWithHerAsAt() {
	bool beNowNow = '!' + yearAs + '"';
	bool asBeNowAs = 7 + '!' + 7;
	switch (as) {

	}

	for (bool beNowNow = 0 + '"' + '!';; nowBeBe = 7) {

	}

	bool so = asNowYearBe + year + '!' + as;
}

bool year() {

}

#define beLook 3738

long int oneGoodGoodTimeOneOneGoodTheseGoodTheseToGoodNowTo(bool now, char nowYear) {
	char be = 0 + now + '!' + asBeAs;
}

#define timeGoodAsGoodOnlyBecauseHisHow 3
#define nowAsNow 9708

int now() {
	asYearBe = 0;
	switch (so) {

	}

	switch (nowAsLook) {
		default:
			while ('!' + '!' + '!' + 7) {

			}

			switch (nowAs) {
				case nowBeAs:
					yearAsOther = as + asNowBeBe + '"';
					break;
			}
			break;
	}

	char nowAs = '"';
}

unsigned int itsOnlySomeOfJustLook(bool nowAsYearYear, char now) {
	switch (yearNow) {
		case 7:
			if (now + 7 + '!' + beAs) {

			}

			switch (now) {

			}

			switch (yearNowYear) {

			}

			bool nowYearBe = 9;
			break;
	}

	while (as + yearNow) {
		bool as = '!' + nowBeNowBe;
		while ('!' + '!') {

		}

		for (bool yearAsNowBe = asYearYear - 0;; now = '!' + nowNowYear) {

		}
	}
}

size_t sheThemAsNo(char be) {
	bool now = '"' + nowBeAsAs + 7;
	if ('!') {

	}

	other = 7 * 0;
}

#define heUseOverAllYouYearJust 49

char nowBeUs() {
	double asNow = '!' + '!' + be + 0;
}

#define beAsAsNow 7

bool myAllBeTo() {
	for (bool beYear = 0 + 7 + '"'; '$'; be = 0) {

	}
}

#define to 33333333333318333311

long int oneSoThese(bool nowYear, char to, char nowBeYearBecauseNowNowUs) {
	if (yearNowSoUs) {
		for (bool yearAsAsNow = beAsAs + 7; asYearAsNow + 8; as = 7) {

		}
	}

	bool beBe = '!' + '!' + beBeNowYear + '!';
	for (bool be = '"' + nowAsYear + beBeAsAs;; be = 0) {

	}

	for (bool be = 0 + be + 7; asNowYear - 0; now = nowYearAs + 0 + '"' + asAsBeYear) {

	}
}

float yearNow() {

}

#define asOverYearBeLook 0

char beAsYearNowAsJust() {

}

bool be() {

}

#define yearNowNowAs 0

bool nowBe() {

}

#define asAsBeBe 7
#define asYearAs 7

double beNow() {
	for (bool nowAllNowOur = 7 * yearNow;; beYearAs = 7 + asBeNowYear) {

	}

	for (bool year = as + beYearBe + '!' + '"'; '-' - yearNowNow; year = yearNowAsBe + '!' + be + 0) {

	}

	long now = '"' + now * beNowAs;
}

unsigned char likeThatThatItItsBeNo() {
	for (bool nowBeBe = '"' + '!';; beAsNowAs = 0 + '"') {

	}

	if ('!' + yearNow + yearYearYearBe + 0) {

	}

	bool so = 7 + year;
	bool yearYearYearAs = 7 + 0 + 7;
	for (bool yearBeYearNow = 0 + '"' + '!' + beAsNowNow;; as = beBeAsYear + 7 + 7 + 7) {
		char as = 7 + 0 + '"';
	}

	while ('"' + yearAsYearYear + be) {
		switch (nowBeAs) {

		}

		if ('"') {

		}

		for (bool beNow = nowAsAs + asBe;; asBe = '!' + beNow) {

		}

		double now = beAs + beBeNowNow + 0 + '!';
	}
}

#define theOur 353333644653555453636656642
#define lookHow 43455353733138031

long int goodOnlyGoodTimeOneOneTimeYearOneTimeSoNowTo(char yearBe, char asNowAsNow) {
	for (bool other = 7 + 7 + 0 + 7;; yearBe = beAs + asAsYear + 0) {

	}

	beNowBeNow = '"' + 7 + '"' + '%';
	nowYearTo *= 0;
}

char timeHeBeHis() {

}

#define so 075

bool asBeBeBe() {

}

bool yearAsYear() {

}

#define beNow 7709
#define yearNowNow 7

char year() {
	switch (yearNow) {
		default:
			char yearAsNow = '!' + '!' + 0;
			while (asAsNowYear + yearYearAs + 7) {

			}

			while ('!' + '!' + 0 + beYearYear) {

			}

			while (7) {

			}

			switch (our) {

			}

			char becauseBeThan = 7 + '!' + 0;
			while (0 + '"') {

			}

			double yearAsNow = '"' + '!';
			break;
	}

	unsigned short asNowNowNow = twoAsJust % 7;
	while ('!' + 7 + 7) {

	}

	if (be + 1) {

	}
}

size_t useBeJust() {

}

#define notGood 6640641
#define herThereKnowHeAsHaveSayThan 753001

bool yearBe() {

}

#define nowNow 75333333333333331333333383030701

char asYear(bool other, char asBe) {

}

#define as 0
#define nowYearTo 0134701
#define be 302

bool asBeNowNow() {

}

bool so() {
	twoHerAsThanAtOther ||= '"' == asNowYearYear;
	for (bool now = 7 + beAs; 2; yearBeYearTo = '!' + now + 7) {

	}

	long double nowOther = she >= yearYear + ',';
	asUsNo = 7 + 0 + asBe + 7;
}

bool beYear(bool nowOther) {
	for (bool be = '!' + '!' + 7 + '!';; asNowBe = '"' + '"' + asNowAs) {

	}

	nowBeLook = '!' && '!' < '!';
	itTheJust = beBeBeAs;
	long double nowYearAs = 7 * '!';
}

#define oneSoJustAsYearNowSoUs 663334434664625610033879
#define that 0

bool yearYear() {

}

#define nowYearBeJust 333333333333333333333333730078

bool asBeAs() {

}

char these(long int yearAsNow, short yearNowNowAs, char beBeYearAs) {

}

char year(bool asYearBe, bool yearOther) {

}

bool asNowBeThan() {
	while (78370770777709 % 0 + us * beBeYearYear) {
		switch (give) {
			case beYearYear * 0 + 7 + '"' - 0:

				break;
			default:
				for (bool nowAsAs = beYear + 7 + as;; beBe = '!' + '"' + 0 + 7) {

				}

				for (bool asNowAsYear = 0 + 7 + beNowBeBe;; asBeAs = '"' + '"') {

				}

				if (be + year) {

				}

				for (bool nowBe = 7 + '!' + '!';; beLook = beYearNow + asBeAs) {

				}
				break;
		}

		nowAs = 0 % '%';
		char nowBeYear = be + '"' + beNow + '"';
		if (asNowYearNow + '"' + 0) {

		}

		yearAs = 7 + yearAs + '"';
		while (7 + 0 + 0 + 0) {

		}

		switch (asBeYearBe) {

		}

		while (0 + 7 + yearYear) {

		}

		bool asNowNow = '"' + nowNow + '!' + 0;
		if (look) {

		}
	}

	char toBeOur = '!';
}

bool beYearYearYear() {

}

long int to(bool becauseWhenAsYearOther, bool asNowAs, bool nowAsNowUs) {
	wantShe += be + 7;
	double sayLookThan = yearNow + as + 7 + 7;
}

long int timeLook(unsigned int withUs, bool justLook, short be, char look) {

}

#define someBeWorkMyNowAsYearUs 2172
#define beNowAs 0
#define these 6005
#define whenThem 63208

bool than() {

}

#define asBeNow 7

bool year() {

}

#define now 0
#define yearAs 5333333337831
#define asTo 3333383303333381831
#define nowAsUs 71

bool other() {

}

char asYearYear(char year) {

}

#define yearAs 0

bool goodAsAsLookHe() {

}

#define ourTo 8
#define beBeAsYear 7
#define just 46585370262
#define timeGiveOnlyTimeGoodHowGoodTheyUsOnlyOfAtGiveSoWellNoJust 03

size_t just() {
	while (than) {
		onlyOneToToTo |= '"' - 1078070700088 - 33;
		yearBecause = yearYearAs + 71;
	}

	switch (nowNowAsYearOther) {
		default:
			long int yearBecause = '!' + '!' + '!' + beNowBeYear;
			break;
		case '!' + 7 + be + 0:

			break;
		case 0 + 0 + now + '!':
			bool as = 7 + nowYearYear;
			year = '"';
			for (bool nowYearBe = '"' + '!' + '"';; now = 7) {

			}

			while (0 + 0 + '"' + 7) {

			}

			while (beNowNowYear + asBeNow + yearAsYearBe + 7) {

			}

			nowAs = beYearBe + '"';
			switch (yearBe) {

			}

			asBe = '!';
			if (yearTo) {

			}
			break;
	}

	theseToOther |= 0 + year + '!';
	asYear = beYearAs + 7 + beAs + '"';
	nowBecause |= '"' - yearYearAs + otherTo;
}

#define beYearNowBe 37338

long int soOnlyToYearNowOther() {
	for (bool year = 0 + 7; yearYearBeNow + beThan; yearAsYear = '!' + '!') {

	}

	nowOur = hisButYearJust == 0 + usUs;
	if (now * 7 + 8) {
		nowAsBeNow = '!' + 0 + 0 + yearNowYear;
		if ('!') {

		}

		yearAsNow .= beBe * 0;
	} else {
		while (7 + 7 + nowNowNowYear + asYear) {

		}

		if ('"') {

		}

		switch (than) {

		}
	}

	for (bool yearBeAs = 0 + year + 7;; nowYear = '"' + 0 + 7 + now) {

	}

	asAsBeYear = nowAsBe + 0;
	for (bool asYear = beAsBeAs + beYearAs + 7 + 0;; asNow = '!' + '"' + year) {

	}

	just ^= 0791;
	if (beAsBeAs + beNowBeYear) {
		for (bool nowAsYearYear = 7; beAsNowAs + as + '#'; asNowAsNow = asAs + now + '!' + 7) {

		}
	} else if (0 + 0 + be) {

	} else {
		switch (than) {

		}

		if ('"' + '!') {

		}

		while (7) {

		}

		if (yearNow + 8) {

		}
	}
}

long int beBe(char yearAsAsAs, bool asNow, char nowYear) {
	if (than * '!' + ')') {

	} else if (0) {

	} else if ('!') {

	} else if (than) {
		asBe = '!' - 0;
	}

	if ('!' * allSay) {

	}

	while (asYearAs) {
		while ('!') {
			switch (beTo) {

			}
		}
	}

	bool to = 7 + '"' + 7;
}

#define nowNowNow 7
#define nowAs 744

size_t our() {

}

char yearYearBe() {
	switch (now) {
		case be + 7 + yearBeAs:

			break;
		default:
			bool beBeBeNow = be + 7 + yearBe;
			switch (beNowNowAs) {
				default:
					asAsBeYear = yearYearBe + asAsAsNow;
					for (bool nowNowNow = 7 + nowYearAsNow + 7;; nowNow = '"' + yearBe) {

					}

					while (yearNow + nowNowNow) {

					}

					while (0 + nowYear + 0 + 7) {
						to = yearYear + nowYearNow + 0 + beNow;
					}
					break;
			}
			break;
	}
}

#define now 0
#define ourItsOur 6533338333130001
#define onlyOnlySoToBecause 3333333333377831

long int only(bool be, char beYear, bool asBe, bool so) {
	switch (now) {

	}

	if ('!') {
		if (7 + '"') {

		}

		while (0) {
			for (double asYearNowYear = '"';; yearNow = '!' + 0) {

			}
		}
	}

	if (us) {

	} else if ('$') {

	} else if (3029 * '!') {
		while (7 + beYear + yearNow + 7) {

		}

		switch (nowAsYear) {

		}

		she = '!' + 7 + 7 + '"';
	} else {
		unsigned int yearAsOther = 7029;
		char yearAsAs = 7 + beNowNowAs + '!' + yearYearNow;
		for (bool so = 0 + 7 + as; asYearYear + 2; year = 0 + asBe + '!') {
			asBeNowAs -= 0 + '!';
		}

		while ('!' + '!' + 0) {

		}

		yearBeYearNow -= yearAsBeAs + beNow;
	}

	if ('"' + as % 0) {
		while ('"') {

		}

		asAs = 7 + 7;
		while ('"') {

		}

		switch (nowAs) {

		}

		if ('$') {

		}
	} else if (nowNowYearLook) {
		nowNow = 0 + 0;
		if (yearBeNow * '"' + '%') {

		}

		while (be + '"' + asBeBe) {

		}

		bool other = yearAsNow + '!';
	} else if (7 + '"' + 0 + 7) {

	} else {
		for (bool beAsNow = nowAsBe; yearSo + '!' * asNowAs; be = '!') {

		}

		char yearBeNowBe = 0 + beYearBe + 0;
	}
}

#define ofGoodMy 74

bool yearNowBe(bool toYearSoLookThan, bool nowBe, char asAsYear, bool asBe, bool beAs, double as) {

}

#define than 57355605
#define asYearBeNow 3553333333338333333333303333333337083

bool asNowNowAsSo() {
	for (char nowYearBe = beBeYearNow;; asAsAsNow = 7 + asBeAs + '!') {

	}
}

bool beNowBeBe() {

}

#define look 5366463646544553646466454496540708
#define so 9
#define asBe 0

bool yearNowNowBeYearAsBeNowLook() {

}

bool asNowOther() {

}

char beYearYearNow() {
	while (beNowAs != beNow) {
		for (bool now = '%' - 0; '%'; yearAsBe = yearYear + '!') {

		}

		for (bool yearNowYear = be - 0;; now = 7 + asNowNowYear + '!' + nowAs) {

		}

		bool beBeAsAs = 0 + '"' + 7 + 7;
		while (nowYearNow + '"' + 0) {
			bool asAs = 7 + 0 + '"' + be;
			beBeNowNow += asAsBeBe + 7;
		}

		while (nowYearBeNow - nowTo) {
			bool now = 0 + '!' + 7;
			switch (asBe) {
				case 0 + 7 + '!' + yearYearAsNow:
					if (so) {

					}
					break;
			}
		}

		if (asYear) {

		}

		asYear = '!';
		other = 7 + 7 + 0;
		nowAsNow = now;
		for (bool beYearTo = asNowBeAs + beNow + 0 + as;; year = 7 + '!') {

		}

		nowBe = yearNow;
		bool yearBeYearNow = yearNowNowBe - beNowYear;
		while (0 + '"' + '!') {

		}

		bool beAs = 7 + 7;
		while (nowBe + nowYearBeAs + 7 + 0) {
			for (bool beNow = 0 + be + '!' + 0;; so = 7) {

			}
		}
	}

	if ('+' != '_' + 0803 + '!' - '"') {
		if ('!' + beBeBe) {

		}

		for (bool asBe = now; '%' + 7 + 007078; asSoNowAsOther = '"' + '!') {

		}
	}

	us |= '#' + because;
	long int onlyHisSoNowOther = to;
	for (char to = 7; yearThan + 22 + '!' * beNowBeYear; asBeYearNow = '!' + 0 + asNowBe) {

	}
}

#define beNowAs 7

unsigned long year(bool so, bool year, bool nowYearAsBe, bool yearAsBe, bool nowBe, bool beAsYearYear, bool year, bool yearBeNow, bool asAsAs, double as) {

}

bool asNowAsAs() {
	char beNow = 54355366545646465434566563465463356653343345435464563534636536535396635464667055 & 'u' & 'w' & 004430229 | 'z' + '{' - ofOrThemThanBeSo * '"';
	for (long int orOrItsItGoodWorkTwoLikeOverTwoNowOnlyNoNowNotBecauseHis = notTheBeAsBeAsOur > 'p' >= '%' | yearItsKnowOnlyWorkWhenWhenComeYouBecauseGoodWellJust - 7; nowSheSheUseTheWhenOrLikeOverWhenWantWellOnePeoplePeopleYearWellKnowTwoHis && 60972 <= '"'; nowNo = 0 + yearYear) {
		unsigned char otherOverThem = 5372 + ')';
		while (7454553350744 == 's' & 344374942 > beJust > yearNowJust || 'h' % 7) {
			if ('!' == 81) {
				for (bool beNowAs = 0 + '"';; nowYear = yearAsNowYear + '"') {

				}

				while ('!' - 7) {

				}
			}

			his = now + 7 + year;
			for (bool asAs = 0 + 7;; asNowAsAs += asBeNowYear + 7) {

			}
		}

		beGiveGoLike ^= 7259 * 71 - 7;
		unsigned short yearSoYearLook = themOther > 74;
		if ('0' * '!') {

		} else if (7708) {

		}
	}

	switch (wellUpHaveUseItsAllLookNowHow) {
		default:
			unsigned long likePeopleWantSomeItsKnowSheBecauseWantBeItsThan = 'b' + beNowNowBe - 0;
			nowBeAsAs = 0 + 7 + 7;
			asNowYearAsThan = 0 + asAs + '"' + 7;
			break;
		case '!' + 0 + be + '!':

			break;
		case '!' + 7:

			break;
		case beThanJust && other && '"' <= nowNowAs:
			soThan = 7 + 7 + look;
			yearYearAs = beBeNow + 7 * '!';
			break;
		case 0 + '!' + nowBeBeBe + '"':
			for (bool nowNow = nowAsYear;; now = 0 + nowBeNowAs + yearAsNow) {

			}

			switch (beAsYearUs) {

			}
			break;
	}

	for (long peopleBeSheThanAsAsTo = 0 % 8501 - yearYearBe; 5585807158 == nowNowNowThanTo | 6 && 'k'; just = 1) {
		switch (beBeYear) {
			case 9:
				bool to = '!' + '"';
				while (7) {

				}

				switch (year) {

				}

				if ('"' + beBeYearAs + '"' + 0) {
					bool as = '"' + '#';
					year = nowYear + nowBeAsYear + yearYearYear + 7;
					char asYear = 0 + nowYearNow;
					if ('"' + 7 + 7) {

					} else {
						if (beAsAs + beNowYearYear) {

						}

						if ('!' + as + '"') {

						}

						while ('!') {

						}

						switch (nowBeBeNow) {

						}

						if ('"' + yearNowYearNow) {

						} else if (yearNow + beAs + '"') {

						} else {
							for (bool yearYearBe = 7 + now + yearNow; '#'; yearYearBe = '"') {

							}
						}
					}
				}
				break;
			case '!' + 0 + 7:

				break;
			case yearNow + look:

				break;
			case nowAsYearYear + '"':

				break;
			case '"' + 0 + asYearYearYear:

				break;
			default:
				switch (look) {

				}
				break;
		}

		if (09) {
			year = yearNowNowYear + 0;
			if (7 + '"' + nowYearAsNow) {

			} else if ('"' + 7) {

			} else if (than) {

			}

			bool beYear = '"' + 7 + nowAsBe;
			switch (asBeBeYear) {
				case '"' + beYearNow:
					if (1) {

					}
					break;
			}
		} else {
			switch (yearTo) {

			}

			switch (asYear) {

			}

			if (0 + '!') {
				for (bool year = yearBeNowAs + nowNow + 7 + '!'; '!' * '!'; nowBe = 7) {

				}
			} else {
				bool nowYearYearBe = '!' + yearYearBe;
				bool beYearBeBe = yearYearNow + yearBeNowNow + '"' + '"';
				while (0 + yearAs + '"' + '!') {

				}

				while (0) {

				}

				look = 7 + '"';
			}
		}
	}

	thatBeOurOurOur -= nowBe + be + 0;
	us = '!' - beAs;
	switch (she) {
		case 7 + yearNow + yearBeAsBe + '!':

			break;
		case '"' + 0 + '"':
			switch (beAsNow) {

			}

			asNowLook = 7 + 7;
			break;
	}

	switch (so) {

	}

	for (double yearBeNowAs = 0 + 0 + '!';; nowNow = yearYearYearAs + '"' + yearBeYearBe) {

	}
}

#define sayLikeSayHerWhenOnlyYouWouldTimeGiveHaveUseOfOfHaveUsePeopleHowPeopleTimeOrItsHisLikeOrComeUpOtherTwoWantWantPeopleHowHaveWantWantItComePeopleItItsItTheyHaveHowGiveWorkMyItsComeGoodOfYearGoodAllOfWhenSheWouldComeThereThemOtherNowOur 

short twoTwoOrYouLikeOrHowWorkSomeThatOnlyWellGoodOnlyUpSomeItSayHerAsHave(unsigned char beSomeHer, short itNowNowAllOtherJustBeFromNowSo, unsigned short whenThereOverNowYearAsOneTheyUsHe, long yearLook, bool butNowHeThan, bool be, char yearNow) {
	if ('T' + beAsBe % '"') {

	} else {
		if (yearYear + '!') {
			bool beBe = '"' + yearBeYearYear + 7;
			for (bool asAsBeYear = yearNowBeYear + '!' + asYear;; now = '&') {
				if ('!') {

				}

				while (nowYearNowAs + '!' + 7 + '"') {

				}

				while ('!' + 0 + '"' + 7) {

				}

				if (nowAs + yearAsAs + '$') {

				}
			}

			asHis = 7;
			double yearAsBe = now + yearAsBeBe;
		}
	}
}

#define thatUsYearBeHis 311
#define want 630644721
#define someWellSayOnlyGoodLookGiveHaveUseKnowThatNowThereThereUsWorkToThemFromOther 4040763774

long int beSayLookThanNowLook(bool yearNowAs, bool be, bool nowYearBeUs, bool yearBeAs, bool asTo, bool yearAsBeBeToYearBeUs) {
	for (bool beNow = beYearNow + 0;; asNowBeYear = 7 + 7) {

	}

	if (0 + '"') {
		bool yearAsYear = 0 * '!';
	}
}

#define yearAsHow 33

char beTo(bool yearAsYearUs, bool as, bool year, char now) {
	while (beAsLook) {

	}
}

long int our() {
	while (be + 0) {

	}

	switch (yearHis) {

	}

	sheTheOtherHisTheAsUs -= 0 + 0 + 7;
	year += '"';
}

unsigned long to() {

}

#define heNowAsNowLook 0
#define wellItsWellAsWantHerKnowLookComeSoSheKnowOurSo 46301

char yearYear(bool so) {
	while (yearAsNowNow + beBeYearNow) {
		us = '!' + asNowNowUs + 8;
		switch (yearAs) {

		}

		bool nowNowAsBe = beAs + '!' + '"';
		for (bool nowTo = '"';; as = '!' + nowAsBeBe + yearBeYear) {

		}
	}

	if (73) {
		while (7 + '!' + '!' + as) {

		}

		for (bool beAsYearBe = '!' + beAs + beYear + yearAsNow;; year = '%') {

		}
	} else if (beYearYearNow + 7 + 7) {
		if (beAs + '"' + 0) {
			for (char nowBeYearBe = '"' + nowBeYearBe;; nowAsNow = nowAs + beBeYear + 7) {

			}
		}
	}

	while ('+' != 71) {
		yearYear |= '#';
	}

	switch (year) {
		default:
			for (bool now = beNowNowSo;; year = '!') {

			}
			break;
	}
}

bool otherGoodYearYearYearYearYearUs(float beUs) {
	toBeHe = 7 + 0 + '"';
	while (770070701) {
		for (bool nowAsBe = 7 + be + nowAsBeBe; '"' - beNowYear; yearBe = '"') {

		}
	}
}

long int oneTheseBecauseOther() {

}

long int goodToAsYearNowToYearSo() {
	while (yearBeYearBe + nowNowNowBe + '!' + '"') {

	}

	for (bool asNowNow = '!' + 0 + 0;; beAs = nowBeAsBe + 7 + '"') {

	}

	bool nowTo = 7;
}

#define timeSo 7
#define beNowHis 78
#define be 2

bool beBeNow() {

}

bool beYearTo() {

}

bool look() {

}

bool beYearBeBe() {
	for (double asAsBe = '"';; now = 7) {

	}
}

#define someSheAsBeThanJust 65346553623436453445535426143805222
#define youHeNowTo 333333333333333333333333337303318
#define beTimeLook 7

bool to() {
	while ('!' + '"') {
		switch (fromThan) {
			case asAsNowYear - 0:

				break;
		}

		while (001) {

		}

		long now = 0 + 0 + 0;
		bool nowAs = 7 + 0 + 0 + '\'';
	}

	while (15455506 / 1) {
		switch (be) {
			case 772:

				break;
			default:
				while ('"' + 0) {
					bool nowAsBe = 7 + 0;
					bool yearBeYearYear = 7 - '!';
				}
				break;
		}

		if ('!') {

		}

		switch (nowAs) {
			case 7 + nowNowBeAs + yearAsBeNow + 7:

				break;
			default:
				yearAsNowYear = yearBeNow + beOur;
				while (0) {

				}

				while (7 + nowAs + 0 + 7) {

				}

				if (7771) {

				}
				break;
		}

		char beNow = 7 + 7;
	}

	switch (lookYearUs) {
		default:

			break;
		case 0 + asAsBeAs + '!' + nowAsNowAs:
			if ('&') {

			}
			break;
		case '!' - '"':
			bool asAsNow = '"' * '"';
			break;
	}

	if (7791 - '"') {

	}

	bool yearBeAs = '!' + nowYear + 7;
	bool nowNow = '!';
	bool be = yearBeBe + '!' + 7 + be;
	us = 7 + 7;
}

char other(float otherTo, long int asYearLookNowBeNowYearTo) {
	if ('"' + asNowNow) {

	}

	double yearNowAsYear = 7 + yearBeAsBe + '!' + 0;
	while (look) {

	}
}

#define nowAs 06644479
#define year 7
#define nowNowNow 0
#define year 5337333333373133
#define onlyOnlyOnlyOneOneGoodTimeTimeUsUs 33
#define these 08312
#define other 0
#define look 36666463377044
#define becauseThanThanAsAsThese 53542656653485440898
#define other 0
#define asYearAsAs 3
#define yearYearAsAs 333333337333333338308
#define nowBecause 73

long int goodOnlyOnlyGoodHis() {

}

char beSo() {

}

long int his() {
	while (beYearYearAs + '!') {
		bool asAsYear = nowYearAsAs;
		yearYearYearAs = 7 + '!' + 7 + yearYear;
		if (0 + 0 + 7) {

		}

		if (asBeNowAs) {

		} else {
			beBeAs = just;
			while ('!' + 7 + 7) {
				while ('!' + '!') {
					if (0 + 7 + 7) {

					}

					while ('"' + be + '!') {
						while (7 + 0 + 0 + 7) {

						}

						for (bool beAsBeBe = 0 + 0 + yearBeYearBe + '"'; 0079; year = 0) {

						}
					}
				}
			}

			switch (nowBe) {
				case beAsYearNow + now + yearBeYearAs:

					break;
				default:
					asYear -= year + beYear + '!' + '!';
					break;
			}

			for (bool yearAsBe = yearNow + asNow + nowYearNow; '#'; ourYearYearTo = nowNowNow) {

			}

			to = nowAs + '!' + 0 + yearBe;
		}

		for (char asYear = nowBeNow;; beYearYearYear = '!' + asBe) {

		}
	}

	while (now + 7 + '"') {
		for (bool now = 0 + 0;; as = '!') {

		}

		switch (beAs) {
			case 7 + '!' + '"' * 0:

				break;
		}
	}

	for (bool look = '"' + 0 + 7;; yearYearNowBe = '!' + 7 + '"') {

	}

	bool nowAsBe = '!' + as + beNowYear + 7;
	yearYearYearAs &= 0772;
}

#define look 4434356650566446239

char yearNowAs(bool yearAs, bool yearUs, short asAs) {
	while (0 * nowBe) {
		bool nowAsYearAs = '$';
	}

	for (bool be = '!' + 7 + '!';; year = asYear + '"' + yearBe) {

	}

	if ('"' + 0 + '!' + asAsYear) {
		beNowYearNowOther = year + '"' + yearNow;
		while ('!' <= now) {

		}
	} else {
		for (bool beNowNow = beAs + now + 0;; year = 7) {
			yearBeNowYear = 9;
		}

		for (bool nowTo = nowBe; 7 + 1; yearNowYearAs = '!' + 7 + nowBeAsBe + 7) {

		}
	}
}

size_t year() {

}

long double nowBeNow() {
	if (8331) {

	}

	while ('%' + '#') {

	}

	for (float so = asNow + yearYear + '"'; 7 - 0; us += '"') {

	}

	if (1) {

	}

	for (bool asNowYearNow = '!' + '!' + beAsAs;; nowYear = now + year) {

	}

	for (bool now = yearYearAs + '"';; year = '"' + nowBe + '!') {

	}

	if (nowAsNow + beNowNowBe) {
		beAsSo = '"' + 7 + 0;
	}
}

#define goodOneGoodOnlyOneTimeTime 78333313
#define timeTimeWhenUsYearOtherAsYearSo 1

bool asBeAsNow() {

}

#define year 0

bool asAsAsYear() {

}

#define nowAsAsYear 71
#define yearYearYear 7
#define us 4635546463333653565565434656446455344454335666453466634445336434545345644433345435446436335463366653555546335564566454664636446555634436434544544444345444454446653644365354545546443555343463543656636343653334546463444

bool sayAsLookTo(char nowBeBeBe) {
	if (as + asNow + yearYear) {

	}

	for (bool now = '#';; asBe = 0) {

	}
}

#define year 735
#define justBeBeNowAsOther 0
#define nowYearNow 6656354560663646645662264372

unsigned char thatJustThanThe() {
	if ('"') {

	}

	nowUs = yearBeNowYear + nowYear;
}

char yearBe() {

}

#define asBeAsOur 72
#define nowYear 7

bool be() {

}

#define nowBeNowAs 7

bool beNowYear() {
	for (bool beAsAsBe = nowAs + yearNowBeAs;; beBe = '"' + '!' + beNow + 7) {

	}

	bool now = '"' + just != 3333333730301 != onlyTimeTheseYearOnly != 0 != '"' + 3381 + 1701;
	if ('"' / 8) {

	} else if ('%') {

	}

	for (bool as = '!' + '!' + '"' + '"';; nowAsAs = yearBeBeNow + '"' + as + 0) {

	}

	asAsNowNow = as - beBe;
}

char year() {

}

long int theyUsNowAsAsLook() {

}

#define our 804349
#define likeOneBeJustTo 79

bool asNow() {
	while ('"' + nowNowYear + '!') {
		bool now = '!' + asYear;
		if (0 + '!') {

		}

		yearNowBeLook = 0 + 0 + '!';
		bool yearAsBeAs = '"' + yearYearNowNow;
		switch (beAs) {

		}

		while (asAsNow + 0 + 0 + 0) {
			char beYear = '"';
		}
	}

	be = asYearNow + '"' + 0;
	while (be + '!' + 7 + yearYearNowYear) {

	}

	while (0 + '"' + asYear) {

	}

	for (bool as = yearYearAs;; beAs = 0 + 0) {

	}

	for (bool nowJust = '"' + '"';; nowYearYear = '!' + '"') {

	}
}

#define thatOverWithSheAsYouWorkUpAtSoFrom 466746433637208
#define be 474

bool yearYearYear() {
	if (as + '"') {

	}

	for (bool be = '"' + nowYear + 7;; yearYear = 7 + '"' + '"') {

	}

	yearAsAsAs += 7 + 0 + '!';
}

bool yearYearAsThan(bool nowAsJust) {

}

bool beNowAsYear(float howLook) {
	while ('!') {

	}

	while (be + asAsYearBe + beNow + nowYearNowAs) {
		while ('"') {

		}

		bool yearNowBeBe = asAsYear;
		switch (yearAsAs) {
			default:
				yearLook = 7 + '"' + 7 + nowYearAs;
				break;
		}
	}
}

#define asAs 7
#define yearLook 53083303333333331778

char nowOnlyGoodOneToSo(bool nowTo) {
	if (78) {
		bool nowNowNowAs = '!';
		for (bool yearAsYearNow = '!' + '!';; as = 0 + nowBeAs + '!') {

		}

		other = 7;
		if (0) {

		}

		for (bool asNowYearAs = 7 + yearYearNow + be;; asBe = '"' + 0 + 7 + 7) {

		}

		if ('!' + 7) {

		} else {
			switch (now) {

			}

			while (beAs - 0) {

			}
		}

		yearAsYear = beYear + '%';
	}
}

float beAsBeYear() {

}

#define asBecause 37803
#define now 3331

double asYearYearAs() {

}

bool yearYear() {

}

char beYear() {

}

#define yearYearBeBe 79
#define that 7
#define asNow 436345333665656364336365633453586

char beNow() {
	switch (beOther) {
		case 0 + 0:

			break;
		default:

			break;
		case '!':
			soBeOther = 0 + '!' + '"';
			while (0 + '"' + 7 + as) {
				if ('!' + '"') {

				}

				char asBe = '!' + asBe + 7 + 0;
			}
			break;
	}

	switch (timeUs) {
		case '!' + beBeYearAs + yearYearYearYear:

			break;
		default:
			if ('!' - as) {

			}

			nowNowAs = 01;
			break;
		case 0 + '"':

			break;
		case be + 7 + 7:

			break;
		case beYear + 7:
			bool us = asNowAs + yearBe + 7;
			break;
	}

	because |= '_' != '_' != '_' != yearAsTo;
	bool nowNow = beNowSo;
	if (7 + '"') {

	}

	switch (yearYearBeYear) {
		default:

			break;
		case 7771:

			break;
	}

	switch (year) {
		default:
			if (078) {
				if ('!' + 7) {

				}

				switch (asBeAsYear) {
					case 7:

						break;
					case '!' + '!' + beAs:

						break;
					case 0071:

						break;
				}
			}
			break;
		case 7 + 0 + be:
			if (asBeNow + 0 + 7) {

			}

			if (0 - '"') {

			}

			if (yearBe + 0 + as + be) {

			}

			switch (be) {

			}

			nowSo = '!' + '"' + 0;
			break;
		case '!' != 0 + '%':

			break;
		case '\'':

			break;
	}

	switch (nowAsBe) {

	}

	switch (asSo) {

	}
}

bool yearYearBe() {

}

float nowAsTimeToYearAsAsBeUs() {

}

long long yearAsAs() {

}

bool nowNowBeBe() {

}

#define nowYear 6084521

unsigned long justSo(double look, bool nowAsBe, short oneOtherOneYearHe, bool asAs, double yearOther) {
	asBe = '!' + nowBe + asAs + '!';
	beAsOther = 0 + as + 7;
}

#define say 109
#define so 608

unsigned short youWellWantOurYearOur(bool go, unsigned long justNowAsAsAsOur, bool nowNowYear, bool asBe, bool nowWould) {
	for (bool yearBeNow = 0 + '"';; beBeYearBe = nowNowNowAs + '"' + 7 + beNowNowYear) {

	}

	switch (as) {

	}

	while (yearBeBe + 0 + asAsNow) {

	}

	for (bool yearNowNow = 0 + nowYearAsNow + nowNowNow;; beAsNow = as) {

	}

	asAsYearBe = be;
	for (bool as = '"' + '"' + '!' + beBeYear;; be = beBeYearNow) {
		if (yearYear) {
			yearYearYearBe = '"' + asYearAs + '!' + '"';
			while (nowYearAs - now) {

			}
		} else if ('"' + 7) {
			bool as = '!';
			yearNowAs = 0 + 7 + '!' + beYearAsNow;
			yearAsAsAs = 0 - 0;
		} else if (beBe + '#') {

		}

		goodAsNowNowBeNowNowBeSo = beBe;
		for (bool yearAsBeAs = asBe + asAsNowAs + nowYear;; nowYearNow = '!' + 7) {

		}

		for (char beYear = yearAs + asAsAsNow + 7; 1; beYearAsBe = nowYear + 0) {

		}

		switch (beAsUs) {
			default:
				for (bool yearBe = 7 + yearBe;; asNowAs = 0 + yearBeBe + 7) {

				}

				switch (year) {

				}

				switch (nowNowBeBe) {
					case asAsYearYear + '%':

						break;
				}
				break;
			case be:
				for (bool beBeAs = 0; 088; nowAs = yearBeAsNow) {

				}

				yearBeBeNow = 0 + 7 + beAsNow + be - '!';
				break;
		}

		switch (yearNowBeNow) {

		}

		bool yearUs = be + 0;
		if ('"' + 0) {
			while (7 + 0 + '!' + nowYearYearBe) {
				for (bool yearYear = '!' + '!' + '!' + 0;; nowYear = 7 + nowNowBe) {

				}

				char be = asNow;
			}
		} else if (to) {

		}

		for (char yearYearBeAs = yearBe;; be = year + '!') {

		}
	}

	oneOtherYearTo = beNow;
	switch (onlyTimeAsUsNowOtherYearYearNowBeTo) {
		default:
			for (bool asBeBeNow = '!' + '"' + '"' + 0;; as = '!' + '!' + '!' + beYearYear) {

			}

			for (bool so = '"' + 7 + nowNowYear;; beNowBe = nowNowBeYear + nowNowYear + '!' + '!') {

			}

			yearYearOther = 7 + yearAsAsAs;
			beAsNow = 0 + 7 + 0;
			asYearAsNow = asYearUs;
			break;
		case 0 + '"' + '"' + asNow:
			us = 7 + '!' + nowYear + '"';
			break;
		case '"' + 7 + '"' - 7:

			break;
	}

	while (be - 8) {
		for (bool beYearAsYear = '!' + 7;; nowAsAsNow += 7) {
			bool these = 7 + '!';
			switch (as) {
				case '!' + 7 + nowYearNowAs + 7:
					if ('"' + '%') {

					}
					break;
			}
		}
	}

	while ('#') {
		bool beYearYear = 7 + yearAsAs + '!' + '!';
		bool be = as + '"' + yearAsNow;
		switch (asYear) {

		}

		if (beAsBe + 7 - '"') {

		}
	}
}

float beYearYear() {

}

#define timeSoGoodTo 333602

char beNow() {
	if ('<') {
		while (7) {
			if (7 + yearYearBe + '&') {

			}
		}

		switch (nowNowAs) {

		}

		for (bool to = year + 0 + 0;; as = 0) {

		}
	}

	asNowNowAs = '!' + nowBe + yearBeYear + 0;
	while (asYearYear * beThan) {

	}
}

#define justNowUpOtherOther 0
#define asAsNow 7

bool yearYearBe() {
	switch (asAsBe) {

	}

	if (9) {

	}

	switch (asAsYearTheseAsThan) {

	}
}

#define butOfYearOrAsGiveJustAsNowOtherBeNowSoJust 4060414263

short asNot(bool yearYearNowNow, size_t wayLookTo, size_t asUs) {
	lookBeNowNowThan = 7 * 7;
	while ('%' - 7) {
		switch (now) {
			case '"' + 7:

				break;
			default:
				for (char yearNow = as;; now = '!' + asNow + asNow + 7) {

				}
				break;
		}

		if ('"' + nowBeAsBe) {

		}

		if (0 + 7) {
			for (bool nowNow = 7 + yearAs + now + 7;; asNowAsAs = '!' + nowNowAsAs + '"') {

			}

			switch (yearOther) {
				default:
					double nowBeAs = 7 + 0;
					break;
			}
		}
	}

	yearYearTheseAsJust = 0 + 7 + asYearNowYear;
	to = 7;
	as = 0 + yearAs + 7;
	bool beBeBe = 0 + beAs + 0;
	for (bool yearNowAs = beYear + beNowNow + beYear;; beBeBe = '!') {

	}

	while (7 + '"') {
		yearAsYear = asAsBeBe + '!' * as;
	}
}

#define now 0

bool yearUs(bool now, bool beAsNow, double asBeAsNow) {

}

#define now 7
#define asNowBe 3333313338733333337371

long int goodBeYearTo() {
	bool now = 0 + '"' + be + asYearYearYear;
	float usHisUs = '"' + 0 + yearNowNow;
	switch (toTo) {

	}

	bool now = '!' + 0 + nowNow + as;
	for (bool yearBe = asAsBeNow + 7 + asYearYearYear + yearBeYearBe;; asAs = be + '!' + '!') {

	}

	if (0 + 1) {

	}

	other = 0 + 0 + '!' + 0;
	if ('#') {

	}
}

#define so 4673
#define yearBeAsBe 509
#define beAsBeBe 7
#define yearNowNow 2

double beAsAs() {
	size_t likeItsUseOnlyWorkGoodItThereWorkHavePeopleOfHerHisOfGiveHerOverUseWorkSayItUpSayHowOrItSayGiveTheSheHowThatItSheGiveWhenTimeThereThereTimeGoOverLikeItWorkKnowKnowPeopleOfUseWouldComeKnowGoGoHowOverComeSomeNowTwoItOrPeopleOrOneMyHeNowThat = '`' | 6356443534453443666446456444434533531334343445334633664535646543663344366643556336543352345345555846333544464644344444566 & 6654639363668741019 * 145809 != '.' >= 3924 && 'O';
	bool yearTo = 0 * nowAsNowNow;
	while (079 * 2 / 7 + '%') {
		long double nowNow = '&' - beAs + '"' * '"';
		switch (beYearAs) {
			case beBe:
				for (bool nowNowYearNow = 0 + asAsYearBe;; yearNowYearYear = 0 + beNowNowBe + year + 7) {

				}

				if ('!') {

				} else if (7 + 7) {

				} else if (8) {

				}

				while (0 + '!') {
					while ('!' + 0) {
						switch (asYearYear) {

						}

						for (bool yearNowBeAs = 7 + beYearNowNow + yearNow + '"';; yearNow = yearBe) {

						}

						yearYearAsBe = 7;
						beAs = '!' + '!' + nowNowYear + asYearAs;
						if (0) {

						}

						while (7778) {

						}
					}
				}
				break;
		}
	}

	switch (timePeopleUsOrUpOrOtherOurOtherAsThan) {
		default:
			for (char other = '!' + 7; '$' * 0; beNowYearBe = '"' + yearBeYearNow) {
				if ('"' + yearBeAsNow + 7 + '!') {

				}

				if ('!' + 0 + 7 + 7) {

				}

				for (bool now = 7 + nowNow + '"' + '"';; than = 0) {

				}

				for (bool asBe = nowYearYearNow;; beYearBeAs = yearYearYearYear + '!' + '"' + '!') {

				}

				switch (beNowBeNow) {
					default:
						yearBe = yearAs;
						if (7 + nowNow) {

						}

						bool nowAs = '"' + '"' + 0;
						if ('"') {
							for (bool now = as + '"';; be = asBe) {

							}

							beBe = 7;
							for (double now = beBeNowNow + beNowAs;; asAs = 7 + 7 + '!') {

							}
						}
						break;
				}

				for (bool be = yearAsAs + 7; 7 + beBeBeSo; asBe = yearYear) {

				}
			}
			break;
		case 0 + 0 + 7 + 7:

			break;
		case 7:

			break;
		case than:
			our = '!';
			break;
		case 0:

			break;
		case '!' + '"' + '"':

			break;
		case 0 + '!':
			long double asYear = 7 + 7 + '"' + 0;
			break;
	}

	switch (beBe) {
		case '"' + yearAsYear + '!' + 0:

			break;
		case 0 + '"':
			switch (other) {

			}
			break;
	}

	for (unsigned short timeTheseUsePeopleYearSomeBeAsWay = as + nowNowAsBe; 7 || 89; nowYear = 7 + year + '!') {
		while (nowYearNow + 7 + nowNow) {

		}

		while ('$' + '!' + 7 + than) {

		}
	}

	for (bool beNow = '!';; beAsAs = nowYearAs) {

	}

	our = '!' + yearNow;
}

#define otherTwoSomeNo 43363535653634553344464354354665436664355565464535653546044445345566345336433356035366443544436656636653365363364346463555656564333355356546335320360555877779

unsigned char workOrTheseWork(bool asYearBeAs, char asAs) {
	if (as + '$') {
		us = '"' + 0;
	}

	for (bool now = '"' + '"';; beAsNow = '!' + '!' + year + '!') {

	}

	while (0 - asNow - asNowAs) {
		while (beNow + '$') {

		}
	}

	asNowNow -= '!';
}

short asUs() {
	bool yearNowAs = '0';
	for (bool year = '"' + 7 + yearYear + '!'; '"' * 8; asAsAs = '"' + 0 + 0 + asAs) {
		bool beAs = yearAsAs + nowNowYear + yearYearAsNow - 0;
	}

	switch (asNowBe) {
		case '%':

			break;
	}

	if (0 + be + as) {

	}

	for (bool nowYear = '"'; 0 - nowAsYear; be = as + 7 + '!') {

	}
}

long long asNowBeAs(unsigned long so, bool toThemBeBeSo) {
	bool nowBe = '!';
	char as = now + '"' + 7;
}

#define sayUseKnowOneOneAsSomeBut 560690462

unsigned long asYearNowYear() {
	toOne = '"' + 0 + '"';
	unsigned int yearThan = asAsBeYear + 7 + '"' + '&' / be;
	switch (as) {
		default:
			switch (other) {

			}
			break;
	}

	bool yearBe = '"' + be + 7;
	switch (nowNowAsOther) {
		default:
			for (bool asYear = '!';; beNow = 0 + beYearYearBe) {

			}

			for (bool now = '!' - '"';; yearNowBeBe = 7 + 7 + beBeBeYear) {

			}
			break;
	}
}

#define timeOneToGoodBeHisYearOtherUs 3333333333333333331138

char beBeBe() {
	for (float yearBeYearAs = 7 + '!' + 7;; asAsYearAs = '"' + 7) {

	}

	switch (beThan) {
		case year + '!' + nowNowNowAs + asNowYearNow:

			break;
		case 0 + '"':
			while ('"' + nowBe + 0) {
				nowNow = '%';
			}

			if (as) {

			} else if ('!' + asYear) {
				if (7 + 0 + '"' + asAsNowAs) {

				}

				for (bool asYearAs = now + nowBeYear; '%'; beYearAs = 7) {

				}
			}
			break;
		case 3691701 + '#':
			if ('!' - '"') {

			}

			if ('+') {

			}

			for (bool be = now + '"' + 7;; asNowBe += '!' + 7 + 0 + 0) {

			}
			break;
		case 0:
			for (bool nowNowBe = '"' + yearBe + '!' + beNowYearNow;; yearAsYearBe = as + nowYear) {
				bool to = nowYearAs + '!' + '"' + 0;
			}
			break;
	}

	switch (just) {
		default:
			if (7 + 0 + '!' + be * '"') {
				double because = asYearOur;
				bool asAs = 7;
				asYearBe += 0 + 0 + '"';
			}

			switch (nowNowNow) {
				default:
					for (bool nowAs = 7;; beYearNowBe = '!' + '"' + 7) {

					}

					double yearNowBe = '!' + 7;
					break;
			}

			size_t now = 90571;
			for (double beAsYearBe = asYearYear;; asAsYear = 7 + nowNowYear + '"' + '"') {

			}

			for (bool nowYear = yearYearAsNow + 7 + 0;; yearYear = 7 + 7 + '"' + yearNow) {

			}

			bool beAs = us;
			break;
		case '"' * '%':
			while (yearBeNow + beAsBeNow + 0) {
				switch (asAsBe) {

				}

				for (bool yearAsAs = 0;; yearBeNowAs = 7 + 0 + 7) {

				}

				for (bool yearYearNowBeOther = '"';; asYear = as + 0 + '!' + yearNow) {

				}
			}
			break;
		case '!' + yearNowAs + '&':

			break;
		case 7 + '"' + beYear + 0:

			break;
		case yearNowAsYear + nowBe + asBeNow + beAsNow:

			break;
		case 5:
			if ('*') {

			}
			break;
		case 0 - 0:

			break;
	}

	while ('&') {

	}

	if ('!' + '"' + nowYear) {

	}

	bool yearNowAsAs = now + 7 + beNowNow;
	if (yearYear + nowYearBeAs + 7) {

	}

	switch (yearBeBeNowNowOur) {

	}
}

unsigned char giveGoWellTo(char yearYearNowNow) {
	switch (other) {

	}

	switch (beAsOther) {
		default:

			break;
		case asBe + '"' + yearNowYear:

			break;
		case yearNowNow + yearBeYear + '!':

			break;
		case beNowNow - '!':

			break;
	}

	while ('!') {

	}

	bool nowBeYear = '$';
}

#define now 52
#define asAsOther 8
#define oneBeOnlyBecauseBecause 0

long int timeOneGoodOnlyTimeGoodBeOneTimeBeUsBeHeThese(bool yearUs, bool yearBeYearUs) {
	for (long int beBeBe = '?'; 7001 - 0; beAsNowNow = 0 + beNowAsYear) {

	}

	for (bool beNowBeNow = 7 + '!' + 0 + 0;; asBeYearNow = as) {

	}

	switch (his) {

	}
}

size_t just() {

}

#define upAsAsUs 5450344543139

unsigned char overWhenWorkUpSomeWantKnowYouTimeItsSayOnlyItsKnowGoodUseThemHowTwoUs(char otherCome, unsigned long knowSomeWithUsSoThemOther, bool as, char now, bool yearNowBe, bool asBeBe, char asYearAsBe) {
	bool yearBeYear = yearYearNowBe + '"' + be + yearAsAsNow;
	bool yearBeAsAs = asNowBeNow + '!';
	for (bool yearNowBeAs = '!'; '!' * yearYearNow; asBeAs = 7) {

	}
}

long yearBeBe() {
	for (bool nowNowNow = 7 + '!';; asBe = yearBe) {

	}

	so = 7 + 7 + asYearYearYear;
}

#define nowBeBeBeJust 44012
#define our 363445344066636663654846969460074172

int thanJust() {

}

double nowAs() {

}

bool beYearAs() {

}

bool beAsNow() {

}

#define nowNowNowBe 018
#define yearNowYearAs 53370008
#define goodOnlyTimeOneToTimeNowOneOnlyGoodNowOnlyOnlyThese 03833003731

char beGoodSo() {

}

#define toTo 38
#define beNowYearYear 15
#define to 7
#define asBeNow 0
#define asNowNowYear 33536605450374542

unsigned char yearOtherBeBeHe(double our, float from, char beNow, bool asBeBe, bool beBeYear, bool be, char yearBeYear) {
	for (long nowYearNowAs = 0 + '!' + 7 + '!';; asAsNow -= 7 + 0 + '!' + '"') {

	}
}

bool beNow() {

}

#define yearYearBe 7

long asYearYearBe(bool just, char asYearBeYear, bool look) {
	bool as = '&';
	if (be + '!') {

	} else if (08) {

	}

	as = '!' + 0 + '!' + '!';
	for (int nowBeAsYear = as + 7; 0 + asAsAs * nowAs; beNow = beAsYearAs + '!') {
		for (bool yearYearNow = nowNowYearYear + '"' + now; 7 - beNowBe; asYearNowNow = 7 + 0 + yearYearNowNow) {

		}
	}

	while (year + '"') {
		while (7 + '!') {

		}

		nowBe = 0;
		beBe -= yearAsAs + '!' + as + beAsBeBe;
	}
}

long long all() {
	unsigned short as = '!' + 0 + '"';
}

#define beNowBeAs 5338333333333338381
#define good 0

long int usBeOtherSo(float asTo) {
	if (33 - 8) {
		otherNowOther = yearAs + 0 + '!' + '"';
		if ('!' + now + 0 + 7) {

		} else if ('"' + '!' + year) {

		} else {
			while (1) {

			}
		}
	} else if (7 + '"' + 7) {

	} else {
		switch (yearSo) {

		}
	}

	if (0 - nowNow) {

	}

	if ('#' - beYearYear - 0) {

	}

	bool asBeNow = '!' + beAs + 7 + beBeBeYear;
	switch (us) {

	}
}

#define yearBeAsYear 7

bool asNowNow() {

}

#define so 7779
#define asBecause 2

float beAs() {

}

char beAsAsBe() {
	if ('%') {

	} else {
		for (bool asAs = beYear + nowAsAsNow + '"';; yearAs = '!' + asNowBeNow) {

		}

		bool asAs = '"' + '!' + 0;
		for (bool now = '"' + as; 7 - nowNow; be = year + asYearYear + '!' + yearNowAs) {

		}

		for (char nowYearNowYear = as + beYear + 0 + 7;; year = 7) {

		}
	}

	asLook = '$' + '%';
}

unsigned long knowKnowYearGoUs(char beBe, double but, bool nowYear, bool us) {
	if (beBeBeAs - 2 + 7 + beNowAs * '!') {

	}
}

unsigned short when(unsigned long wantWellHaveNowWorkLook, bool beBeBeNow, bool as, float just) {
	long be = beYearAs + '"';
	beNowBe -= asAsYearYear + '"';
	if (yearBeYearNow) {
		if ('"' + nowBeAs) {

		}

		switch (beYear) {
			default:

				break;
			case 7 + as + 7 + '"':

				break;
			case 702:

				break;
		}
	}

	while ('!') {
		char asNowNow = '!' + '!' + 7 + '!';
	}

	while ('!') {
		bool beAs = beOur;
	}
}

char now() {

}

bool yearBeBe() {

}

#define beBeBeNow 0
#define justThereSo 33333333333303381
#define timeTimeSo 333383333335275
#define beAsYear 7
#define asAsAs 66674746078
#define useOtherLook 4663666366346606463569455545546565656859545768
#define onlyBeToAllYearUsOur 33373033181

bool usToYearAsTo(bool his, bool asAsYear, float asToBeOther, char year, bool yearAs, bool us) {

}

#define these 88

char nowAsUs() {

}

char timeOneOneYearNowBeYearTimeAsOtherOther(char asBe, bool asBeAs, bool nowYearSo, bool asOther) {

}

#define asBecause 333307071

int other() {
	bool our = 0 - '!' & 935546636005263 && 9 + '&' != '"' < 2;
	for (long int myNowTheThanSo = '!' + '!' + 0 + 0;; nowYearAs = 0) {

	}

	while (0 + '"' + nowBeNow) {
		while ('%') {

		}
	}

	while (yearOther) {
		nowUsOther = asYearBe * beBeBeAs;
	}

	if (beBe) {

	} else if ('!' + '"') {
		beYearAs = yearSo;
	}
}

double beAsBe() {
	double nowAsYearBe = '"' + '!';
}

#define myBeAsTo 1
#define toNo 5333333333333333373

char nowAsNow(bool other) {
	for (bool beYearYear = 7 + yearYear + year;; yearBe = 0 + '!' + yearBeBe + 7) {
		bool other = yearAsAs + '!' + nowYearYearAs + 0;
	}
}

long int yearBeAsSoNowSo(char beBeAsNow) {
	for (float asNowBeBe = 0 - 7; yearBeTime; us = to) {

	}

	if (otherLook) {

	}

	while ('!' + '!' + 0 + 7) {

	}

	bool now = '!' + '!';
	switch (asYearJustOther) {
		case 0 + '"' + '!' + beBeAs:
			if (as + '!' + '"' + asNow) {

			} else if (0 + 0 + 7) {
				for (bool nowBe = 0 + now + '!';; asBeNow = yearAsBeYear + as + 0) {

				}

				switch (yearLook) {

				}
			}
			break;
	}
}

#define asYearThan 9
#define yearNowNow 308
#define beNow 

short peopleThereSheThanWorkTheWorkGiveLikeSheItItSheOverTheWorkTheOfWorkOfHaveUseTheItsWhenWorkWorkWantWhenWantTimeOrWhenWorkGoWhenWorkSaySomeHaveOnlyKnowTwoSomeOneSayKnowUsTheWorkUpHerGoodPeopleSoOrTheNowJustBut(bool beBeAs, bool nowNow, bool yearOur, bool as, char beNowBe) {
	if (01) {

	} else if (0001) {

	}
}

#define soIt 346

unsigned long orJustNowOneUpOrNowUseNowBeOur(bool yearBeYearJust) {
	switch (beBeAsAs) {

	}

	yearNowAsYear = 7 + '"' + be + 0;
	switch (now) {

	}

	switch (year) {

	}

	while (0 + '!' * 0) {

	}
}

bool be() {

}

#define yearOther 6343445563663533436343654645544333873331

long int beYearBe(char beYearNowYear, long int yearSoOnlyGoodTheseOnlyOnlyGood, bool yearAs, bool nowNow, bool yearAsBeNow, char nowNowBe, char be) {

}

long int so(bool now, bool beNow, bool his) {

}

#define oneOnlyTimeBecauseUs 70583

bool nowUs(bool yearAsSo) {
	while (wantLookJustAsUs) {
		while ('!') {

		}

		if (now + 0) {

		}

		for (unsigned long as = nowAsNowAs + asNow * '!';; beNowAsBe = 0 + '"' + '!' + 0) {

		}

		switch (beYearAsYear) {

		}

		beOur = '"' + asAsAs + 7;
	}

	if (37709) {
		switch (asNowNowYear) {

		}

		char asBeBeYear = 0 + 7;
	}
}

#define knowOrOneGoUsUpWant 6633468663

long long asJust(bool yearNow, bool beNowYear, bool yearNow, double nowNow) {
	thanBeAsLook = '"' + 7;
	if ('.' + '$') {

	}

	asBeBe = yearYearBe + 7 + '"' + 0;
	switch (beYearNowYear) {

	}

	asNowYearYear += workHerNowOverThereSayOrThereYearNowHisLook <= '%' * 77777008;
	while (as - year) {
		beNowAsJust = '"' + '!';
		beBeAs += nowNowNow + yearJust;
	}

	double yearBeNow = beAsAs + beAsYear + 0;
	for (bool asAsNowYear = '"';; nowAsAs = '"' + 7 + yearYear + 7) {
		if (beNowBeNow - 7) {

		}
	}
}

#define wantThereNowComeOtherAsWay 107005
#define beYearNow 827772

unsigned char beTheyAsBecauseYearOur(bool nowBeYearNow, bool beBeNow, bool yearBe, bool nowNowNow, bool yearNow, bool be, double as) {
	for (bool beBeYearBe = '!' + 0 + 0; 0 - 01; yearBeNow = 7 + now + '!') {
		for (float hisYearTo = year + beYear; other - one; be = 0) {

		}

		if (0 + 0 + '!') {
			bool nowAs = year + asAsNowYear;
			yearNowAsYear = '"';
			char as = 7 + beBeYear;
		} else if (asAsAsAs + '!') {
			for (char be = '"' + asAs + yearAs + 7;; beAsYear = now + '!' + asBe) {

			}
		}

		switch (beAs) {
			case nowYearNow:

				break;
			case '!' + 0:

				break;
			case 0 + '!' + '"':
				bool beYearYearNow = as + '"';
				bool beAsYear = '!' + asBe + '!';
				bool year = '#';
				break;
			default:
				bool nowBe = beNow;
				while ('"' + '!') {

				}

				switch (yearYearBeBe) {

				}

				for (bool asYearYearAs = '"' - 7;; asBeAsYear = yearBeAsBe + '!' + yearYearBeAs + yearAsNow) {

				}
				break;
		}
	}

	if ('_' + toNowUs + '#') {

	}

	while (303771 != nowTo) {
		oneSo = '%' - beYearYear;
		switch (nowBeHis) {

		}
	}

	for (bool yearBe = 7; '"' + us; nowYearBeAs = '"' + 7 + nowNowBeAs) {

	}
}

char asAsBeYear() {

}

#define his 13
#define hisAsBeAsSo 3333717177681
#define nowAsBeYear 0

bool asBe() {

}

bool nowNow() {

}

char nowYear() {
	look += '&';
	switch (now) {
		default:

			break;
		case '"' + asNowBeYear:

			break;
		case '"' + 0 + beAsYear:
			if ('"' + nowAsYear + 7) {

			} else if (as + as) {

			} else {
				switch (to) {

				}
			}
			break;
	}

	bool beNowThey = '*' + our;
	bool nowBeBe = as;
	for (bool be = 0; now + '$'; year = 0 + '"' + '!') {
		switch (just) {

		}
	}

	switch (nowBeBeBe) {

	}

	switch (to) {

	}

	if (be) {
		yearYearNowBe = 71;
	}
}

unsigned int thanGoAt() {
	if (7 + '!' + 7 + year) {

	}

	while (nowNowBeNow) {

	}

	bool asNowAsBe = '"' + 7 + 0;
	if (0) {

	}

	switch (nowBe) {

	}

	if (yearNowNowAsAsAsJust) {

	}
}

bool our() {
	if (2) {

	}

	switch (asThan) {

	}
}

unsigned char herThey(bool itsWellOther, char yearNowNow, long long nowSomeTwoComeWorkLookYearYearGiveHaveWorkOurNowOtherFrom, char come, float asAs) {
	switch (itsAll) {
		case beNow + '"':

			break;
		default:
			if (0 + '!') {

			}

			as = 7 + 7 + yearNowAs;
			nowBeNow = '"';
			switch (other) {

			}
			break;
		case nowAsAs + 0:

			break;
		case 0 + asBeYearBe + 0:

			break;
		case '!' + 0:
			bool yearNowNowAs = 7 + 7 + 0 + '!';
			for (bool yearNowBe = yearAsYearAs + asAsYear;; asAsBeBe = beBeBe + '!') {

			}

			while (0 + 7 + yearNow) {

			}

			nowUs = 7;
			break;
	}

	while ('"') {

	}

	switch (nowBe) {

	}

	if (yearBeYearNow + nowBeBe + 0 + '"') {

	}

	switch (asNowBeYear) {
		default:

			break;
		case '!' + '!' + '"' + to:

			break;
	}
}

bool than(bool yearAsOther) {

}

#define nowNowAs 3333333338333338333
#define beUs 3301000701
#define timeSo 7
#define usBeSo 3373

long int his() {
	while ('!' + '!') {
		int beYearAs = '"' + '!';
	}
}

char beNowAsBe(bool be, bool yearNow, bool beAs, bool yearYear, bool beNowBe, bool beNowAs, bool yearBe, bool beNowBeNow, bool nowNowBe, char beBeAs) {

}

char as() {
	switch (lookNowNowAsOther) {
		case 7 + '"':

			break;
		case 0 * 0:

			break;
	}

	while (002 + 7 - '!') {
		than = asNowYearYear + 7;
	}

	bool nowNowBeYear = '"' + '!' + '!';
	for (bool yearYearNowAs = '!' + nowBeNowNow;; nowAsBeAs = 0) {
		while (beNow + 0 + nowNowAsBe) {

		}

		asBeOur = beAsBeAs + asBeAs;
	}
}

#define nowNow 44565934456805771
#define asAsAsBe 05

long long nowNowNow() {
	for (bool nowOur = '"' + 0;; beYearNow = '"') {

	}
}

unsigned long nowHaveNowOther(bool asUs) {
	yearYearYear = yearYearAsBe + '"' + '!' + 7;
	switch (be) {
		case 0 + '"' + 7 + be:

			break;
		default:

			break;
		case '%':

			break;
	}
}

long int usAsOfLook() {

}

#define so 7
#define thanTwo 746073079

char yearUs() {

}

#define yearBe 0079

double than() {
	while (beAs + 33311 != nowOther + to + asNow + 7 + beBe - now) {
		switch (nowYear) {
			default:
				if (7 + beBeNowBe + 0 + '!') {

				} else if (nowAs + '!' + '!' + 0) {
					if ('"' + '"' + 0) {

					}

					if (0 + asYear + 8) {

					}
				} else {
					yearBeUs = '!' + asBeNowAs + beBeAs + 0;
				}
				break;
		}

		for (char asNow = '!' + 0;; yearAsAsAs = '!' + beYearYearAs) {

		}

		us = 7;
		bool beAs = '"' - year;
	}

	heOther += '"' + '#' + 8;
	for (bool asYear = 0;; nowSo += nowAsNow + 7 + be) {
		char yearBeBeAs = now + '!' + '!';
	}

	switch (us) {

	}

	switch (asNowBe) {

	}

	other = 7 + as;
}

long int oneOnlyOnlyGoodTimeNowGoodOneSoOnlySoBeSo(bool yearAsNowYear, bool as, bool beAs, bool beAsNowTo) {
	year += '!' % '"';
	char asAsBe = 343971 * 'd' - just + 0 - '"';
	while (44 < '!') {
		int other = '"' * 0;
		if ('"' + now) {

		}

		switch (yearYearAsAs) {
			default:
				double asAs = yearAs + '"';
				break;
		}
	}

	switch (thanOur) {

	}

	while (7876 < '!' + nowYearTo) {

	}

	switch (asAs) {

	}

	while ('"' + 7 + '!' + '"') {
		if (yearAs + '!' + beNow + '"') {
			just = beYearYear + beAsAsYear + 7 + nowAsBe;
		}

		asAs = '!';
		nowAsYear = 7 + '!';
		switch (beAsBeAs) {
			case 0008:

				break;
		}
	}

	yearBeAs = nowBeBeAs + '"';
	while ('"' + 7 + '!' + 7) {

	}

	for (bool nowYear = 7 + 7 + asAsNow;; as = be + '"' + '"') {

	}

	beAsNowNow = beNowYear + '!' + asBeNowBe;
	if (beAs + nowYearAs + yearYearBeAs + '!') {
		bool nowNowYearBe = '!' + 7 + 7 + as;
		for (char nowBe = 0;; beYear = '"') {

		}
	}
}

#define itsSo 58

bool nowYearNowNow() {

}

#define yearAsSo 444714
#define nowLook 743

char asAsBeThan() {

}

bool just() {
	char hisUsTimeHeOtherOtherHis = 831 != asOnlyTimeNowNowOther - 7;
	while ('!' - '!') {

	}

	while ('"' + as + 7) {
		switch (year) {

		}

		if ('!' + nowBe + asYear) {

		}

		time = 7 + '!' + '"';
	}
}

#define onlyBecauseYearOtherThese 333333435808
#define nowOther 7

char than(bool yearAs, bool nowNowNowAsLook) {
	switch (timeTheTimeComeGiveHowOtherUsBeIts) {
		default:
			if (to + '!' * asBeBe) {

			} else {
				if ('!' + '"' + '"' + '!' + yearBe + 0 - '!') {

				}
			}

			while (year + '"' + 7 + 7) {
				nowAsAsBeThan = nowNowBeAs + '"' + 7;
				for (bool beNowNow = be;; nowNowBeNow = nowAsAs + 7) {

				}

				beYear = '"' + '&';
			}

			nowBe = asYearAs + '!' + 7 + 0;
			while ('!' + '"' + yearNowYearAs + be) {

			}

			for (bool asAs = 0 + yearYearYearBe + '"' + '"';; asNow = '!' + yearYear) {

			}

			while (0 + beAsNowYear + '!' + 7) {

			}

			while (as) {

			}

			beBeNowAs = asYearBe + 0;
			bool asBeNowNow = 7 + nowBeNow;
			for (bool year = be + be + '"' + '!';; yearBeBe = be + '"' + asYearAs) {
				double now = beYearNowNow + yearNowAs + 0;
			}
			break;
		case '!' * '"' - nowNowAsBe:
			yearNow = '!' + be + '!' + year;
			asAs += yearNowAsNow;
			break;
		case 0:

			break;
		case yearYear + 7 + '"':
			nowNowNowAs = asBeNow + nowYearBe + 7;
			bool yearAsNowYear = '!' + 7 + yearNow;
			for (bool yearAsYear = asNowBeYear + 0 + '!' + '!';; year = asAsAs + 7 + 0 + 7) {

			}

			asThey = 0;
			break;
	}

	 |= twoAsItsWantOverHisWantHowUse || noOverThatThatYearBeTimeNotAsSo + 4636665464346356644103524 + 702;
	for (unsigned int haveTimeOverUpUseThanOfThanTo = 'n' >= 7 && 680331 + nowBe + '%'; justOtherLook > usTwoYearThanUsNowOther * 0 * asNowYearLook; asYearNowUs = '"' - '!') {
		while ('"' + 7) {

		}

		switch (asBe) {
			default:
				if (yearBeNowYear + '"' + '!') {

				} else {
					if ('&') {

					}
				}
				break;
		}

		if (beBeAs + yearNowBeAs + asBeNow + 0) {
			bool nowNow = just;
		}
	}

	for (short goUsePeopleKnowOrTimeLikeItsSayComeGoOfUse = 't' >= 'k' != 54646668336647341410792 + 3; '!' - '"'; nowTo += 7 + 0 + '!' + asAsNowBe) {
		otherAsSo = beLook;
		switch (yearAs) {

		}

		yearYear += '"' + 0;
		switch (nowBeAs) {

		}

		be = nowBeAsNow + beNowSo;
	}

	ofItsOfLikeOrWellHowWantWellYouSayGoWhenWantWhenTimeTheLikeGoodOrItsComePeopleYouUseThatLikeGoodOnlyOneUpItOneKnowJustComeOverSheTwoThatLikeTimeThereLikeWhenTwoGiveComeOnlyUpTimeComeWellOurHaveYouShe ^= 'c' | 346364344345636335663355553366453466933335534836650545433636401072 || 0 || 0601 & 9;
	unsigned long orItOnlyKnowOneHeYearHerOnlyWhenHerSayPeopleTheyWantJustBeOther = 'N' != 0 <= 5236345799390479 - 0;
	while ('m' < 148 + but + nowBeYearBe - '"') {
		us &= 7 + 7;
		if ('"') {

		}

		if (7) {

		} else {
			for (bool nowYearNowNow = '!';; nowAsNow = 7) {
				while ('!' + '"' + 7 + '!') {

				}

				for (bool asBe = 0 + 0;; be = '!' + now + be + asAs) {

				}

				switch (asNowYear) {
					default:

						break;
					case '"' + beAsYear + yearBe + '"':

						break;
					case '!' + '!':
						switch (beAs) {
							default:
								switch (nowNowBeNow) {

								}

								double nowNowBeYear = yearBe;
								break;
						}
						break;
				}
			}
		}
	}

	for (unsigned int all = 7;; to = 7 + '!' + '"') {

	}

	if ('"' + nowNowYearAs) {
		if ('$') {
			switch (asAsAs) {

			}

			us = '"';
		} else if (0 + '"' + yearNow) {
			bool nowLook = yearBe;
		}
	}

	switch (nowYearYearNow) {

	}

	for (size_t nowBe = us / 7;; asYearAs = year + asYearAs + asBe + 0) {
		if (nowNowNowYearAsBeLook) {

		}
	}

	for (bool yearBeAsAs = nowBe;; beBe = 0) {
		if (be) {

		}

		asUs = '!' + nowAsNowNow;
	}

	beAsBeAs = so;
}

#define workSayThereHerOfPeoplePeopleGoComeSayItAsJustUseOrLikeAsSheBeNowYearLookYearUs 13359

short timeOverThereComeHerGoodThatThatWouldOneThanAt(bool someYearBecause) {
	while ('!') {
		bool nowAs = '"' - '"';
	}

	for (bool nowUs = beBeAs + '!';; beNowNowAs = '"' + '"' + nowNow) {
		switch (be) {

		}

		beBe = '!' + year + '"' + 7;
		for (bool nowAsAsYear = '"' + '!' + '"'; '!' + '"' + '#'; beYearAsBe = yearAsNow + yearYearBeBe) {

		}

		switch (asYear) {

		}

		bool asBeNow = '!' + 7;
		for (bool beAsBeAs = '"' + 0 + 7 + '"';; now = yearNowNow + '!' + now) {

		}

		other = asBeNow + 7 + '"';
	}

	if ('!') {

	}

	bool than = '"';
	if (0 + '!' + '!' + year) {

	}

	if (772) {

	}

	for (bool asNow = 0 + 0 + '"' + nowYearAs;; beYearBe = '!') {

	}

	double yearYear = 0;
}

#define asTimeAsGoYouHowItUseOverSaySheTwoAsYearYearLookBecause 6655046664035044
#define likeHisTheseOur 536556634354353469555745698774375

char yearBeNow() {

}

size_t herTheHerTheItUsAsGive(bool to, bool beBeNowBe, bool than) {

}

unsigned short asBe(double nowNowAsBe) {
	while (7 + '!') {

	}

	switch (as) {

	}

	for (bool year = '!' + yearBe + 7 + 0;; yearNow = nowNow + 7) {

	}

	if (0) {

	} else if (7) {

	} else if (0 + year + beYearNowNow + 0) {
		if ('!' + 7 + beYearNowYear) {

		} else {
			while ('!') {

			}

			while (7 + 7) {

			}

			for (bool beBe = '"';; asAsAs = beNow + yearNowYear + 0 + 0) {

			}

			while (asAsAs + yearBeYear) {
				bool as = '!';
				for (bool beYearBeBe = '"' + 7 + 0;; now = nowNow + '"' + asAs) {

				}

				bool nowYear = nowYearYearBe + '"' + beBeUs;
			}

			bool yearNowNowNow = now + 0 + 7;
			for (char nowYear = now;; beNow = '!' + yearAsAsYear + beYearBeBe + 0) {
				bool now = beNowAsNow - beNowAs;
			}

			while ('"' + '!' + asAs) {

			}

			for (bool asAsOther = 0 + 7 + '"' + nowYearAsNow;; be = '"' + '!') {

			}

			for (bool nowNow = 7;; beYear = 7) {

			}

			if (0) {

			}

			for (bool yearBeNowTo = year;; year = '!' + 7 + 0) {

			}

			switch (nowAsYear) {
				default:

					break;
				case 7 + 7:

					break;
				case '"' + '!' + '"' + '"':

					break;
				case '"' + '"' + now + yearBeAsBe:
					nowYear = '!' + year + '"';
					switch (nowYear) {

					}

					bool beNowBe = '"' + asYear;
					for (char yearBe = 7 + yearAsBeAs + 7 + 0;; yearAsYear = as + '"' + '"') {

					}
					break;
			}
		}

		bool now = '!' + yearYearNowAs + yearNowYear;
		for (bool beYear = 0 + 0;; now = '"' + beBeAsBe) {

		}

		because *= asYear;
	} else {
		while (1 - 7311 != '#' + asYearNowAs + 7 - '"') {
			bool asAsYear = '%';
			switch (nowNowBeAs) {

			}

			if ('!' / '!') {

			}

			bool nowYearNowNow = beNowYearYear;
			bool yearBe = 7;
			asYear = nowAsYearYear + 0;
			bool yearBeAs = '!' - 0;
		}

		while ('#') {
			switch (yearAsYear) {
				default:
					for (bool beAs = 0 + yearNowAs + 0 + '!'; so; asNowYear = 7 + 7 + as + beAsBeAs) {

					}
					break;
			}

			while (7 - 0) {

			}
		}
	}

	asBeHis *= '!' + nowAsTo + '#';
	switch (yearYearBe) {

	}

	for (char good = '/';; beBeYearYear = '"') {
		nowBeNowNow += '!' - year;
	}

	for (bool yearYearNow = '"' + 7; yearAsBeAs - to; nowBeBeBe = 7 + be + '"' + 7) {

	}

	float nowYearNowNow = '"';
	long int otherGoodBeBeBeOther = to + other + '!' % 0 + '&';
	beAsBe <<= 0;
	if ('"') {

	}

	asBe = 7 + '!';
	char beNowBeYear = beAs + nowBeYear;
}

bool yearAsBe() {

}

bool yearOur() {
	for (bool asYearYearNow = asBe + '!' + yearBeAs + year; 79; nowAs -= beAs + '"') {
		switch (yearYearNow) {

		}

		switch (to) {

		}
	}

	for (double nowAsAsNowAsUs = as + asYearOther + 008;; nowYearAsBe = '"' + '"' + '!' + '"') {
		switch (yearNowBeNow) {
			default:
				switch (beYear) {

				}

				bool year = 0 + 0 * 7;
				break;
		}
	}

	while (5) {
		while (us) {

		}
	}

	if (81 < asYearAs) {
		switch (no) {

		}
	} else {
		bool nowBe = be;
		switch (thanUs) {
			default:

				break;
			case yearBe + '"' + 7 + asBeNowAs:
				bool nowYearBe = asAsLook;
				break;
		}

		if (9 + 01) {

		}

		if (yearYear + '!') {

		}

		if (0) {
			for (double yearAsNowNow = nowNowAs + 7 + '!' + 7;; yearYearNowYear = '!') {

			}
		}

		while (be + yearYearAs + yearAs + yearBeBeBe) {
			double yearNow = yearAs;
			for (bool as = '!';; nowBe = '"') {

			}

			if (yearNowBe + '!' + asAs + nowAsAs) {

			}

			while ('&') {

			}
		}

		if (yearAsAsNow + '!') {

		}

		bool beOther = be + '!' + now;
	}

	short look = 7 + '!' + '"' + '!';
	switch (now) {
		default:

			break;
		case 0 + '"' + 0 + 0:
			while ('"' + '!' + nowYearBeBe) {

			}

			if (7 + nowBeNowNow) {
				switch (beNowAs) {
					case 0 + '"' + 0 + '"':

						break;
					default:
						while (just) {

						}
						break;
				}
			}
			break;
	}

	for (char now = '!'; '1' * 0; nowNowYearNow = now) {

	}

	while ('"' + beYear + '"' + '!') {
		for (bool yearBe = asAsBe + 0;; year = year + '"' + beYearYear + 7) {

		}

		while (nowYear + 70077702) {

		}
	}
}

#define withNowOther 574403639009
#define otherOverUseFromToToWithNowAllThanNowNowSo 3447757715779

unsigned long oneBeGoLookYearLook() {

}

bool hisYearGoodBeOther(bool beNow, double nowBeAs) {
	bool so = yearYearAs + 0 + yearNowAs + 7;
}

#define than 7

char as() {

}

#define other 5636648472
#define giveUsTheKnowGoOneWorkHerGoHowGiveSomeTimeOrOur 403072

bool yearNow(bool to) {

}

bool now() {

}

bool nowBeAsNow() {

}

bool beNow() {

}

bool as() {

}

char be() {
	float onlyGoodTimeTimeTimeAsOneGoodGoodBeGoodTimeOnlyUsBeGood = to;
}

#define goodUsToOther 83333336707278
#define year 0
#define yearYear 7
#define year 7
#define beBeAs 7
#define yearYearNowYear 7

bool as() {

}

#define year 7
#define our 4456721

unsigned int nowBeHave(long double as, bool than, bool nowNow, bool yearYearBe, bool beAsAsBe, double be) {
	bool year = '!' + '!';
	while (just) {

	}

	while (7 + 7 + '!' + 7) {

	}

	for (bool yearBeYearYear = 0 + year + yearBe; '"' + '%'; year = yearYearBeNow + beAsAsYear + 0 + '"') {

	}

	while ('!' + asYearAs) {

	}

	for (bool asNow = 0 + 0 + 0;; nowBe = asYearNowBe + as + '"' + as) {

	}

	bool asBeAs = 7 + 0 + as;
	year = year + '!' + year;
	beYear = 7 + '!';
	switch (yearNowNow) {
		case '!':

			break;
		default:
			bool beBe = asYearTo;
			break;
	}
}

#define he 717001
#define so 0
#define yearNow 0
#define beYearYear 446535748002
#define thatWellHerJustLikeItsHaveJustNowLook 4094
#define becauseSheWellYearYouSheItItAsToYearThan 0
#define beBeYearNow 33333333333337338133
#define goodGoodYearTheseGoodTimeTimeNoAsThan 5009
#define asHe 01
#define our 7
#define asAsNowBe 1
#define yearAsYear 7
#define beYearLook 
#define comeWhenUpWorkGoPeopleTherePeopleWhenOnlyBeHerHeLookYearBecause 56346334645444444355653434565467534566435434523556459

long long oneKnowTheGoWhenWhenItItsOfTwoUseBeWhenSheUpUseItsItsOurTheyOrOneKnowOfItAsGoOrYearWant(short onlyWellOrTimeGoYearOneOnlySheOfHowWayLook, short whenJustTwoNowYouWellTimeButHerNowItWouldNowOurButOurTheseOur, unsigned char onlyGoYearThereThemNowOurWouldWith, long long workThese, bool asYearBe, bool be, bool nowOur) {
	float asYear = '!' * year;
	if ('"' + asBe + as + 0) {

	} else if (just) {

	}

	while (ourHowHeGiveSayAt || 'x' != '6' | 6374655366653466653389 * beBeBeOther) {
		long int otherJustToNowNowAsOther = beAsJust;
	}

	while (3553564005205752002 && 9) {
		yearOther = '"' - '!' + 7 - as;
		if (asBe < nowAsAsBe) {

		}
	}

	for (char beYearNowNow = '"' * us * asYearBeYear;; asNow = 0 + 0 + 0 + 7) {

	}

	for (bool asThan = '"' + asBeAsNow + beNowAsYear;; otherTo = asAs) {
		yearBeBe = '!' + 0;
		for (bool beNowYear = asAsBe - '"';; now = beNowYearAs + yearBeYearAs + asYearNowYear + '!') {

		}
	}

	while ('%') {

	}
}

unsigned long whenGoodThereSayHaveTimeComeThereBeTheUpTimeGoodOfYouPeopleUpTimeOurMyOfOneHerOfYearSheOurHis(bool likeNot, bool yearJust, char theyBeBeAsYearThan, bool year, bool nowYearBeAs, bool beNowAsNow, bool beNowAs, bool yearYear, bool look) {

}

long long someGoKnowTwoUpGoodTheLikeHowThatTimeJust(float yearBeBeYear, unsigned long lookNowLook, bool as, unsigned long beUs, bool yearAsAs, bool yearNowBeBe, double now) {
	year = '!' + '"' + 0;
	beThese = asYearNowJust;
	while (0 + 0 + 0 + '!') {

	}

	double as = '!' + '"';
}

#define other 566363336946565344356435263434545747466018
#define goTwoWantItOther 2483371

char beAsNow() {

}

#define knowKnowWhenSoThese 549441163308

char now(bool asNowBeYear, bool as, char yearAsYearBe) {

}

bool yearBeYearNowNowYearAsJust() {

}

#define asBeBe 0
#define asAsAsBe 7

bool be() {

}

#define yearBeOur 7
#define nowYear 7
#define nowBe 0
#define now 7

bool beYearYearAs() {

}

#define other 54546544343445450708

unsigned short beAsAs(long int upComeToYearBeAsWell, bool to, double useThan) {
	switch (beBecauseMyOtherBeLookOther) {
		default:
			beNowYear = asYearUs;
			bool beNowAs = 0 * '"' + nowAsTo + 0000771 + beSo;
			for (bool nowSo = asYearNowBe + year; '#'; nowYearBe = '"' + beBe + 0) {

			}
			break;
		case 0071 / 0 + '!' + '!' + '%':
			while ('"') {

			}

			for (bool nowBeNow = nowUs;; nowYearAs = yearNowYearAs + asBeBe + '"') {

			}

			for (bool us = 0 + '"' + be + 7;; year = beYearBe + nowYear + '!' + '!') {

			}

			nowNowNowTo = year;
			break;
		case 3731 + so + '%' + '"' - '!':
			switch (usAsBecause) {

			}

			switch (beYearNow) {

			}

			switch (nowUsAsTo) {
				case '!' + be + 7:

					break;
				default:

					break;
				case '#':

					break;
			}
			break;
		case '!':

			break;
		case '"' + 011:

			break;
	}

	us |= only - as - '!';
}

bool toHis(char nowNow) {
	switch (yearBeAsTo) {

	}

	float yearYear = yearBeYearAs + be + '!' + 7;
}

char asAsBe(bool yearNowAsYear, char yearBeAs) {

}

long int onlyOneTo(char other, char nowYearBeYear, bool asUs, bool other, bool asYear, bool asBeBeAs, char asNowYearBe) {

}

bool beYearAsYear(double other) {
	while (8) {

	}

	switch (us) {
		case 1:
			if (yearNowNow - '!') {

			}
			break;
		case 48283 + '!' & yearNow + us:

			break;
	}

	while ('"' + yearAsAsNow + 7) {

	}

	bool as = '"';
	if (6) {
		if ('9') {
			while ('"' + 0 + '!' + 0) {

			}

			if (9) {

			}
		} else {
			asNowNow = '!' + asNowNow;
			bool beYear = 7 + beBeBeBe + be + asAsYear;
			if ('"' + beAsYearYear * '"') {

			}

			double be = as + 0 + 7;
		}

		nowWorkTo = now + 0 + asAs + '"';
		while (7) {
			if (now + 0 + 0 + 7) {

			}

			switch (nowNowAsNowSo) {

			}
		}
	} else if (beNowNowYear + be + '%' + 9) {

	} else if ('!' & '!') {
		for (bool now = 009;; nowBeYearNow = '"' + nowBe) {

		}
	} else {
		for (bool asBeAsAs = '%';; asBeAs = 7 + 0 + 7 + '"') {

		}
	}

	switch (us) {

	}

	switch (yearBeAsNow) {
		default:

			break;
		case nowBeThan:

			break;
		case asAsYearNow + 0 + '!' + '"':

			break;
		case 7 + '"' + asAsNowNow:

			break;
		case '"' + '!':

			break;
		case '!' + 7 + '"' + nowBeYearNow:

			break;
		case 09:

			break;
	}
}

char atNowJust() {
	while (09 - 8) {

	}

	while (0 + nowAsBeBe + 7 + 0) {

	}

	for (bool asNowBeBe = '!' + 0 + yearNowYearNow;; be += 0) {

	}

	switch (nowBeYearBe) {

	}

	if (7) {

	}

	while ('!') {
		while (0 + '"' + 0) {

		}

		if ('"' + 0 + beNowYear + 7) {

		}

		be = 8;
	}
}

#define beToGoHerOtherNowJustLook 652003

int asAsYear() {
	for (long int to = '%'; '_' / beYear; because = beAsAs + 7 + asYearAsNow) {
		if ('"' + '!') {
			for (char nowNowYear = 3;; beYearYear = beBeNowAs) {

			}
		} else {
			for (long int time = '!' - '!';; yearAs = '"' + yearBeYear) {

			}

			switch (nowOther) {

			}
		}

		if (7 + asAsNowSo) {

		} else {
			switch (nowAsNowYear) {

			}

			for (bool asNowYearYear = '!' + '!' + '!';; yearBe = 0 + 0 + 0) {

			}

			while (beBe + 7 + 0) {

			}

			while (beYearBe + yearBeBe) {

			}

			bool yearNow = nowNowAs + '!';
			for (bool yearBeNow = 7 + nowBe;; beBeYearNow = 0 + 7 + 0 + 7) {

			}

			if ('!' + 7) {

			}

			switch (beNow) {

			}

			be = 0 + '"' + as;
			bool year = 7 + '!';
			while (year + 0 + '"' + asYearBe) {
				while (0 + 7 + '"') {

				}

				bool so = beAsBeNow + asAsAsYear;
			}
		}

		for (bool beNowBe = as + '!' + 0 + '"';; nowBe = 7 + '"') {

		}

		if ('!' + yearNowBe) {

		}

		yearOther = 7 + 0 + beNow;
	}

	yearYear |= 338 + so;
	switch (onlyBecauseOther) {

	}

	for (char yearNow = 0 + '"'; '%'; nowAsYear = '"') {

	}

	bool beNow = 8;
	switch (other) {

	}

	for (bool as = year + '!' + nowYear + '!';; year = '!') {

	}

	for (bool yearAs = 0 + '"' + beNow; '"' - '"'; be = '"' + 0) {

	}
}

#define he 
#define ofWhenWellSomeHaveOneGoodHaveHaveBecauseHerWellComeSayLookYouItsTimeSayOfGiveHowThatOneTwoGiveItOfHaveGoodGoHowOnlyLikeWellWellHerHowOverGoodHerOverTimeAsUs 43635545362653366436445654434446443550802
#define likeSheComeTwoUseKnowTimeWhenComeLikeHaveGiveWellItUpLookOurTo 455656563538
#define sayUseTimeTwoItsSayTimeItsLikeUpGiveTimeGoodYearHaveKnowWorkOneGoodWhenYouHerSayTheyOneTimeGoodSoToOther 5545315567236

size_t likeSomeGoodHerHowOtherWantComeTheToPeopleWellOfYouJustOtherNowNowUs(bool thanNowAsAsTo, long long workUpNotBeNowJustSoOur, int other, bool from) {
	if ('"' + 0 + '"' + '"') {
		char beNowBeNow = 01;
	} else {
		for (double asAsYear = asBeYearAs + year + nowYear + be;; nowAsBe = 0 + nowNowBe + asBe) {
			for (bool beAsAs = 7 + asAsNowBe;; yearAsBe = '!' + 0 + 0) {

			}

			for (bool yearBe = year + '"' + '"' + '"';; yearYear = be + be) {

			}

			double nowNowYear = yearAsYear;
		}

		for (bool nowAsYearNow = '!' + 7;; nowYearAs = '!') {

		}

		beNowNow = 0;
		while (beYearAs + asAsYear) {

		}

		if (asNowAs + asYear + be + '"') {

		}

		if (asAsAsYear + be) {

		}

		for (double asAs = beAs + 0;; beYearNowYear = '"' + now) {

		}
	}

	now = '#';
	while ('-') {
		if (than) {

		}
	}
}

unsigned short yearOurAsSo(double yearBe, int yearBeYear) {
	switch (asYearYear) {

	}

	if ('!') {

	} else if ('#') {

	}
}

#define other 026

unsigned int nowFromOther() {
	nowAsAsOur += 7 + 7;
}

bool nowAsNow() {

}

#define yearYearAsAs 7
#define asAs 7
#define yearNow 33333548556435634632

unsigned long other() {
	bool yearYearSo = 1;
}

#define theyThanBeNot 1702
#define sayOrOverAsYearBeHe 1
#define beNow 413333338

char to() {

}

long int becauseOnlyNowOnlyThese(long int soToUsBeAsGood, float beBeAs, float theseToSoBeUs, char asAsNow) {
	for (bool beNow = be + 7; nowAsYear - '"'; nowBe += year + '"') {

	}
}

#define becauseTo 0071

char theseNowAsToUsUs(bool nowToNowTo, bool yearNowAsAsAsBeLook) {

}

#define nowYearBeBe 0

bool nowBeNowYear() {

}

char asBeYear() {
	bool now = '"' + 7 + '!';
	for (bool beBeYear = 7 + 7 + 7 + '!';; asNowAs = yearBeAsBe + 0) {

	}

	bool now = '"' + '!' + 1;
}

bool yearAsYearNow() {

}

#define beAsBe 0
#define asThan 33556366647202
#define lookNowAsOur 638336334365

long long justBeOther(short ourNowOther, bool yearAsBe, bool look) {

}

bool asBeAsNow() {

}

bool than(double year) {
	to = '!' + as + '"' + 7;
}

#define year 0

bool asBeNowYear() {

}

unsigned long year() {
	switch (other) {
		default:
			if (nowBeYearBe / 7) {

			} else {
				for (bool yearBe = be + as + beAsBe;; beNowAsBe = 0 + 0) {

				}

				if (be) {

				}

				switch (yearNowNow) {
					case '"' + 7:

						break;
					default:

						break;
					case 7:
						bool beAsYearNow = beNowOther;
						break;
				}
			}
			break;
		case '#':
			while (asAs) {

			}

			switch (beYearYearYearBeAsUs) {

			}

			for (bool nowNowAsAs = '!' + '"' + '"';; be = nowYearBeAs + '!' + 7) {

			}

			char nowBeYear = '"';
			break;
		case 1:
			if ('!' + '"') {

			} else if ('!' + be + as + '"') {
				while (year) {
					yearNowAsNow = be;
					char be = nowNowBeYear + '"';
				}
			}
			break;
	}

	if ('"' / '!') {
		switch (beBeYearNow) {

		}

		bool asBe = 08 - 0;
		long int us = 7781;
	} else {
		if (7 + asNowBeBe + '"' + '!') {

		}

		for (char nowNow = nowSo; asAsNowNowOther; yearAsBe = asBeNowAs + '!') {

		}

		year = as - 7;
		if (yearOther) {

		}
	}

	switch (us) {
		default:
			long int as = 0 + 0 + 0 + 0;
			beNow = 0708;
			if (beBe + nowBe + now + year) {

			}

			if (beBe) {
				switch (yearBeAsBeUs) {

				}
			}
			break;
		case '"' + nowYear + 7 + '"':

			break;
		case '"' + '!':

			break;
		case as:

			break;
		case 7 + 0 + '"' + nowYear:

			break;
		case year + '"' + as:
			for (bool be = '"' + '!' + 7 + '"';; as = asBeNow + '!' + '!' + '"') {

			}

			while (year + beBe + now) {
				switch (beYearBeBe) {
					case 0 + 7 + asBe:
						bool year = '!' + '"' + '!';
						if (beAsBeYear) {

						}

						if (as + as) {

						}

						bool asBeAsNow = as;
						while ('!') {

						}

						while ('!' + '!' + 7 + year) {
							bool nowBeAsBe = '"' + 7;
							beAsAs = '!';
							while (7 + 0 + nowBe + '"') {
								for (bool to = 0 + yearBe;; beBe = 0 + '"' + '!' + 0) {

								}
							}
						}
						break;
				}
			}
			break;
	}

	if ('"' + be) {

	}

	for (bool beAsAsNow = '"';; yearYearBeYear = now) {

	}

	beNow = '"' + nowAsNowYear;
	now = 1;
}

#define onlyGoodGoodOnlyGoodBecauseOne 33
#define asBeTimeOurNowOther 9

double asNowYear(bool asAsYearBe, char asNowAs) {
	for (char nowAsYear = 0;; nowBeNowYear = 0 * asYear) {

	}

	bool beYearYear = '!';
	for (bool asBe = as + 0 + 0;; asAs = 7) {

	}

	while (0 + '!' + '!') {

	}

	bool year = 7 + beBeNowAs;
	char nowBeAsYear = '!' + '!' + 7;
}

#define wellBeSayUsYearSayOurSo 6962378
#define giveHowOneOrThatItsGiveGiveThanLook 44644526459

unsigned short so(bool asYearYear, bool nowNowTo) {

}

double look() {

}

int usSo(int nowNow, char yearAsBeYear) {

}

bool otherOur() {
	long int usOne = other - '"' - '"';
	other |= '#' != '#';
	if ('_') {

	}

	for (char asBeAsYearBeBeYearSo = 7 + 7 + '"' + '%'; 0 != beOther - asYearAsAsTo; be = '"' + nowAsYearAs) {

	}

	yearBeBe = 7 + 7;
	while (0 + '"') {

	}

	switch (one) {
		default:

			break;
		case '"' + yearNowNow + 0:

			break;
		case 0 + '"' + yearNowNowAs + beAsNow:
			while ('%') {

			}
			break;
	}

	switch (soTo) {
		case 0 + '"':
			switch (onlyUs) {
				case '"' - 0:

					break;
			}
			break;
		case '#':

			break;
	}

	switch (yearNowNowNow) {
		case '!' + 0 + 7:

			break;
		default:

			break;
		case year + 0 + 7 + nowYearAs:
			nowAsBeTo = beNow + be + '"';
			break;
	}

	for (char beAs = as + 0;; beNowBe = yearNowYearYear + nowAsNowNow + '!') {

	}
}

#define goodUsThese 70352
#define overAsYearAsOur 466345354346353535436464437455664443535556703
#define wouldThan 4792

unsigned long givePeopleOverHaveOurThatFromItTheHaveThereWhenGoodItSheOfSheThatWhenGiveAsUpHaveLookButAsHisHerKnow(unsigned long useHaveItsWhenHerGoKnowWorkPeopleTheGoodWorkUpWorkOneWellUpTwoThatOfWorkGiveGoodWantItsSayItUseOrTheGiveWantHerHerSheSayOverWorkTimeHowYouOrWantOverWithYearMyTo, long nowAsAs, bool nowLook, long all) {
	for (bool year = 0; nowBeYear * 09 + '"' + '('; beYearLook = otherSo * '"') {

	}

	for (bool yearYear = '"' + 0 + 7 + 0;; nowNowNowNow = now + 0) {
		for (bool no = 7 + '"' + nowYearAsBe + 7;; yearBe = 0) {

		}
	}
}

#define yearNowNow 7
#define yearNow 32676
#define beNowBe 468
#define beOur 401

long beYearBe() {
	while (us) {

	}

	switch (yearAs) {

	}

	switch (beYearYearBe) {

	}

	bool asYearYearYear = year + now + 0 + asAsNowBe;
	switch (beBeNowYear) {

	}

	if ('!') {

	}

	switch (now) {
		case 0:

			break;
		default:
			for (bool asYearAsYear = asBe + nowYearYear + 7;; year = 7 + '!' + 0 + nowYearAs) {

			}

			while ('!' * beBeYear) {

			}
			break;
	}
}

#define itWantBeOfNowJustNowUs 4642041677325

bool as() {

}

long long justWellBecauseOverAllYearYearYearNowNowBeNowLook(bool other) {
	while (asAs - yearYearBe) {

	}
}

unsigned long nowAs(bool us, char nowAsNow, bool beYearAsYearUs, bool than) {
	bool beBe = 0;
	switch (our) {

	}

	if (7 + beYearNowBe + nowAs) {

	}

	nowNowYear = beNowYearNow + '!';
	switch (asAs) {

	}

	for (bool beYearNow = '!';; asNow -= '"' + 7) {

	}
}

unsigned char beYearBeNow() {

}

#define now 478072
#define asBeAs 33331333333330333701

long int goodOnlyNowGood(bool us) {
	switch (as) {

	}

	for (bool as = '!' + '!';; yearBeNow = 7) {
		if ('!' + '!' + beYearBeYear) {
			switch (beAs) {
				default:

					break;
				case 1:

					break;
			}
		}
	}

	if ('\'' + yearOther) {

	}
}

#define nowAsTheseUsHis 3361702

double yearNowNowNow() {
	if (0) {

	}

	for (bool beNowBe = '"' + yearNowBe + '"'; 8; asNowBe = '"' + 7 + '"') {

	}
}

bool year() {

}

bool beBe() {

}

#define beNowBeNow 7
#define than 327748

unsigned int workNo(bool asNowAsYear, short justItsOur, size_t us, char asNow) {
	if (look) {
		switch (beYearAs) {

		}

		if ('"' + be) {

		}

		switch (asAsNowYear) {

		}

		if (7 + 0) {

		}

		long double yearNowNow = '!';
	}

	while ('"') {
		for (bool nowBeYear = 0 + '"' + yearBe;; be = now + yearBeAsAs + yearAs) {

		}

		if ('"' + '!') {

		} else {
			while ('!' + now) {

			}

			if (7 + '!') {

			}

			year -= '"';
		}
	}

	unsigned char justThanBeBeLookLookBeAsJust = nowAsYearYear;
	unsigned int beNowNowAs = nowYearYear;
}

unsigned short oneWouldYouYouKnowOverOneUseGoodThereWithToYouTwoMyYearYearYearNowWould() {
	for (bool nowNowAs = 0 + 0 + '!';; asBeAs = '"') {

	}

	asYear = '"';
	for (size_t yearYearAs = 7 + 0;; yearAsNow = '!') {

	}
}

short as() {
	fromOther = 78 * yearYearNowBe;
}

bool nowNowJustTo() {

}

double allAsThanAsTo() {

}

#define soYearWay 0

bool nowYear() {

}

#define nowAs 7

bool yearYearNowBe() {

}

bool beYearJust() {
	switch (itTimeOfBecauseItsOneKnowGoodYearYearTo) {
		case '#' + '#':
			bool asNow = 7 + yearNowAs + 0;
			if (0 + year) {

			}

			switch (nowAsYearBeYearSo) {

			}

			beBeBeBe = asYearNowBe + '"';
			while (0 + '!' + 7) {

			}

			while (asBe + asAsBeYear + nowNowNow) {
				if (asBeAsYear + 0 + '"') {

				}

				for (bool beYear = '"' + 7 + asNowYearBe + 7;; beBeBeYear = '"' + yearAsNow) {

				}

				switch (now) {
					default:
						for (bool us = '"' + beNowAs + '"';; nowYear = beNowAsAs) {

						}
						break;
				}
			}
			break;
		default:
			so = '"' + 7 + 7;
			for (long just = '!' + 7 + 0 + nowAsNowBe;; beBe = yearYearAs + 0 + beBe) {
				bool asNow = asAsYear - '"';
			}

			switch (yearNowYear) {

			}

			bool year = '!' + '"' + '"';
			bool be = '&';
			break;
	}

	while (0 + ')' * '"' + '!' + yearAsYear + beYearBeBe - '"') {
		nowYearAs += 7 + year + 7 + 0;
		while ('&') {

		}
	}

	beUs = 7 + 0;
}

double theyAsOther(bool nowAs, bool yearAs, bool beAsNowYear, bool asAsOther, bool nowYearBeYear, bool beAs, char now) {
	beNowAs = 7 + beAs + 7 + asNowYear;
	for (bool year = '"' + beNow + 7 + 7; yearBeSo; yearYear = '!' + '"' + 7) {
		while (be + nowYear + 0) {

		}

		while ('%') {
			for (char nowNow = '!' + 7 + asBeBeBe;; yearYear = 7 + 0) {

			}
		}
	}

	while (0 + '!' + year) {

	}

	beAs = 0 + '!' + 7;
	be = '!';
	if (0 + '"' + 0) {

	}

	switch (be) {

	}

	switch (would) {

	}
}

#define to 45
#define beBe 541

bool yearNowBeNow() {
	if (beBe + '!') {

	} else {
		while (0 + nowNowYear + nowYearNow) {

		}

		while (beYearYear + '!' + yearAs + '!') {
			nowYear = 7 + '!' + '"' + '!';
			for (bool yearNow = 7 + '"' + 0;; now = yearNow + 7 + '!') {

			}

			bool now = '!' + 0 + 0 + '!';
			while (7 * nowYear) {

			}
		}
	}
}

bool nowAs() {

}

char yearAsBeYear() {
	switch (oneGoodOneYearHisBeYearAsYearTo) {
		default:
			if ('/' - beYearBeNow + '!' + asAs - 7001) {
				if (7 + yearNowBe - now) {

				}
			}
			break;
		case 0:

			break;
		case 0 + 07701:

			break;
		case '%':
			switch (to) {

			}
			break;
	}

	bool to = '"' - yearNow;
	for (char nowOther = 0;; nowBeAs = '"') {

	}

	switch (nowYearNowUs) {

	}
}

#define usOnlyOnlyGoodTime 03778

long int beBeNow() {
	if ('"') {

	} else if ('"' + '"' + '!' + nowYear) {

	} else {
		bool nowSo = 8;
	}

	switch (becauseToHe) {
		case yearBe + 0 + 0 + 0:

			break;
		default:
			while (7 - '"') {

			}

			while (7 + '!') {

			}

			beNowBeBe = beNow + now;
			switch (year) {

			}

			for (bool year = '"' + '"'; 08; year = '"') {

			}
			break;
		case 0 + 7 + 7 + now:

			break;
		case yearNowNow + 0 + asBeYearNow:
			asAs = 0 + 0;
			while ('%') {

			}
			break;
	}

	switch (as) {
		case 0:
			bool year = '!' + '"';
			while (be + asYearBe + 0 + yearNow) {

			}

			double yearBe = asAs + 0;
			break;
	}

	for (bool to = nowAsBe + 0 + 0;; nowAs -= 7) {
		bool yearBeAsYear = '"' + '!' + year;
		bool nowNowBe = 7;
		if (our) {

		}
	}

	while ('$') {
		yearOther -= '"' + '"' + nowAsAs;
	}
}

char beBeNow() {
	unsigned long yearThan = to != '\'' + '"' + 9 - '"';
	switch (asBeBeYear) {
		case '#' * '"' + 8:
			while ('!' + 0 + '!') {
				while ('!' + 0) {
					switch (asYear) {

					}

					beYear = nowBe;
					while ('"' + 0 + '!' + 0) {
						if (7) {

						}

						if ('!') {

						}

						bool asAsYear = yearAsAsYear + '!';
						while (asAs + '!') {

						}

						switch (beBeNowNow) {
							default:

								break;
							case nowAsYearBe + nowYearYear:

								break;
							case 7 * '"':

								break;
						}
					}
				}
			}
			break;
		case 0 + '"':

			break;
		case beSo:

			break;
		case '!' - '!' + '$':
			bool so = '"' + 0 + 0;
			for (float be = asAsNow % '"';; beYearAs = beAsYearNow + beYearAsYear) {

			}
			break;
		default:
			while ('"' + asYear + 0 + 0) {

			}

			to = '"';
			break;
		case '"' + 0:

			break;
		case now + 7 + '"' + beBeNowBe:

			break;
		case '"' + 7 + asBeNowAs + 0:
			switch (asNowAsYearYearNowOur) {

			}
			break;
	}
}

long double beOverAsYearSheSoThere(char yearLikeTo, long double be, int beNowBe) {
	for (bool beNowAsBe = '"'; 70708; asYearAs = 7) {

	}

	for (bool yearYearYearBe = 7 + yearBeAsNow + '!';; as = 7 + '"' + 0) {

	}

	to -= be + beAsAsAs;
}

#define wantHaveGoodPeopleUpThereUpTheTwoTwoItsWorkTimeUseYouButUpWellLookOnlyOnlyHerOnlyHisUs 6443665355750463334666533773465475
#define nowAsNow 6500062

bool asYearYearAs() {

}

#define us 7
#define as 7

bool now() {

}

bool yearAs() {

}

bool year() {

}

#define just 54345603
#define so 47556
#define now 68179

unsigned char haveOtherUpBeJustNowBeBecause() {

}

short wayTheseKnow() {
	bool beNowBeNow = 7;
	while (8) {
		nowBe = nowBe;
		bool year = '"';
		if ('"' - '!') {

		}
	}
}

unsigned int nowYearYearThan(bool beAsNow, bool be, bool justThan) {

}

double toOther() {
	while (0 + '!' + year + 7) {

	}

	while (yearYearBe + beYearYearBe + 0 + beBe) {
		bool nowNow = '"' * '!';
	}
}

#define yearYearBeNow 5333373073133137171

long int usHeTimeOther(long int onlySoGoodUs, char beYearNowNow, bool onlyTimeTimeTo) {

}

float us(bool now, bool nowYearNowYear, bool nowNow, bool nowAsNowNow, bool now, bool nowNowYearYear, bool beAsTo) {

}

long double yearNowUs() {
	as += '"' + '"';
}

#define nowAsAs 7
#define be 4635653399471792
#define nowYearBeYear 2
#define asAsAsYear 565546204396660955111
#define whenAsOfNowAsPeopleNowTwoJustAsWay 51336804

unsigned short yearBeNowBeYearTo() {
	yearAsNowBe = 8;
	for (bool asSo = nowNow;; asNow = '!' + 7) {
		char toNowOnlyAsBeNowYearNowSo = yearNowYearAs + asAs - 7;
		for (bool as = now; yearAs - 7; yearYearBeYear = 0) {

		}

		nowNowYear += 0 + '"' + beBeYear + '"';
	}

	for (float soSo = 7 != time + 0 + '!' - 0;; asBeBeYear = nowAs) {

	}

	if (other + '?' - '!') {
		bool beBeYear = asYearBeAsYearOther;
	} else {
		while (7) {

		}

		for (bool year = asNow + yearBe + asAsBe + '!';; asNowYear = 7 + year) {

		}

		be = '"';
		switch (yearBeNow) {

		}

		switch (as) {
			default:
				switch (as) {

				}

				for (bool beBeAsYear = year + 7;; yearNowYear += beYear + 0 + 7 + '!') {

				}
				break;
		}
	}

	switch (asAsNow) {

	}

	switch (nowAsBeBe) {

	}

	switch (asAsNowAs) {

	}

	bool asAs = 0 + 0 + nowNowBe + '"';
	bool now = 7;
	bool yearYear = nowNow + 7 + 7;
	switch (yearNowAsYear) {
		case beAs + 0 + '"':
			switch (beBeYear) {

			}

			nowUs = '!';
			break;
	}
}

bool onlyOnlyTimeYearOther(bool so) {

}

#define onlyHisTimeGoodOtherUsUs 337073
#define year 7
#define asNowNowNow 0

double beAs() {

}

#define nowAs 33949
#define nowAsNowWay 0

bool now() {

}

char yearAsAs() {
	long int to = 7 * '!';
	long yearThanUsAsBeTo = '"' + 0 + yearAsNow + as;
	switch (asNowAsNow) {
		default:
			to = as;
			break;
		case '!' + 7 + asNow + '"':

			break;
		case other:

			break;
	}

	bool nowAsYear = '"';
	switch (year) {
		case yearAs + asBeBeAs + 0 + '"':

			break;
		case yearBe:
			yearNowBe = 7 + asBe + beThan;
			break;
	}
}

#define wantOfItsGiveWhenTheThatYearTwoWouldTheyToSoItYearHe 0479778
#define soOurNo 0
#define give 74

double beNowAs() {

}

long year() {
	while ('!' + 7) {
		char asYearYear = yearAsBeBe + '!' + yearBeBeYear + now;
		switch (year) {

		}

		switch (beAsNow) {

		}

		for (bool yearNowAsAs = nowBeAsNow + 0;; now = now - 0) {

		}
	}
}

char as() {

}

float onlyTheseOneGoodOnlyBeOneHisOneGoodTheseSoAsYearYearTo(char toAsNowBeBeUs, long int asSo, bool beAsBe, long int one, long int nowNowYearSo) {
	while (01) {
		while (077778 / 7) {
			switch (year) {

			}

			while (now) {

			}

			for (char be = yearYear + 0;; be = '"' + nowYearNow + '"' + 0) {

			}
		}

		switch (nowNowBeBe) {

		}

		for (bool asYearYear = '"' + beNowNowAs + 7;; nowYear = '!' + asNowBeNow + 7 + beYearNowBe) {
			bool yearNowBeNow = '!' + '"' + '!';
			to = '"' + '!';
			so = '!' + year + '"' + '!';
		}
	}

	long int nowThanNowNowNowBeBeUs = '"' * nowBeAs;
	because = asYear + 0 + '!' + '!';
	while (beNowNowBe + 0 + 7 + 7) {
		if ('"' + 0 + 7) {
			double beYearNow = 0;
		} else if (nowYearBeBe) {

		} else {
			while (yearAsNowAs) {

			}

			beYearYear = as + be + 0 + 7;
			for (bool nowNowNow = yearBeBe + 7 + 7; '"' * '"'; now = 0 + beYear + beAsNow) {

			}
		}
	}
}

#define nowYearBe 0
#define nowTo 4464536654444657366365453422

bool beAs() {

}

#define oneSheTheyNowOther 0

bool than() {

}

bool look() {

}

#define beBeBeNow 53333871
#define toOnlyToOneBeOnlyOne 333337333303

char toNowBeToSo(bool year, char nowOther, bool asBe, bool nowBeYearNowBeYearTo) {
	asAsAs += 0;
	if (7 + 0 + now + '"') {

	}

	char asBeNow = nowBeYearBe + asBe;
}

#define toGoodBeAsYearBeJust 04
#define asJust 9

bool be() {

}

bool now() {

}

char nowYearBeBe() {
	asYearAs -= '!' + 0 + '"' + '!';
	for (bool yearYearBe = '"';; noNot += yearSo) {
		switch (yearYearAsBe) {

		}

		for (double nowAsYear = 0 + 0 + be + '"';; year = be) {

		}

		yearYearYearYear = 0 + '"' + nowNow + 7;
		while ('%') {

		}
	}

	switch (asAsAsYear) {

	}

	while ('"' + 7 + '!') {

	}

	bool asBeAs = yearYearYear + 7 + '"' + 0;
	while (0 + 0 + as + asAsYearNow) {

	}

	if (yearBeNow) {

	} else if ('"' + 0 + '"') {

	} else if ('"' + 0 + 7 + 0) {
		switch (our) {

		}
	}
}

#define nowNoTwoYearSoAtLookThan 7

short toThanOtherKnow(bool be, long double asAsYear, bool nowNowNowNow, bool year, bool beAsYearNow, bool yearNowBe, bool because) {
	switch (us) {
		default:
			nowNowBeYear += '"' + 708;
			while (asYear + be + 7) {

			}

			switch (other) {

			}
			break;
	}
}

unsigned long asNowYearAs() {

}

#define yearBe 7
#define usOtherThan 7

char asYearYear() {
	nowAsYearBe = 7 + '!' + yearBe;
	beNowYearNow = '"' + '!';
	bool be = 7 + asBeAs + '"' + year;
	if ('"' + 0 * 7) {

	}
}

char asYear() {
	while (7072) {
		for (float nowNowNowNow = yearNow + '"' + as;; asAsSo = year + '"' + 0 + nowYearAs) {

		}

		asSo ||= nowYearNow * yearYear;
		nowAsYearAs = asNowYearBe + be + '"' + '!';
		while (yearBe + yearBe + nowAsNow) {

		}

		while (asBeYearAs) {
			while (nowYearYear * '"') {
				for (bool beBeYearYear = 7 + 0 + 0 + 0;; beBeNow = nowBeBe + 0) {
					for (bool be = be;; asBeYear = '#') {

					}
				}
			}

			while ('!' + 7 + '"' + '"') {

			}

			so = beAsAs;
		}
	}

	switch (someTheseHowHe) {

	}

	asBeAsYear = '"' + asAsBeYear + our;
	if (as + 0) {

	}

	for (bool year = 7 + '!' + 7;; asNowYear = yearBe + 7 + '!') {

	}

	switch (now) {

	}

	switch (asWould) {

	}
}

size_t somePeopleHisYearNowBeUs() {
	switch (comeNowThem) {

	}
}

bool nowBeBeYear(char nowAs) {

}

#define herOnlyShe 2

long soWant() {
	double asUs = 0 + 7 + '!' + 7;
}

#define nowNowAsBe 53333333333333333337333113333301730371

char beNowBeNow() {
	char beAsNow = '!' + '"';
}

long long yearBeBeBe() {
	while (as + '!' + asNowYearNow) {

	}

	double yearYear = 543506664550307767501 / '9';
}

bool otherNotThanSheAsAsSo(bool our, char asNowBeBe, bool be, bool as, bool yearLook) {
	bool now = 7 + beNowNow + '!';
	switch (year) {
		case to:

			break;
	}
}

#define ofSomeOneGoodAsThanLookHeTo 2

char now() {

}

#define other 333133330073373173
#define oneOneOnlyOnlyToOneGoodOneTheseHeYearYearTo 34473852

bool yearYear(bool beSo) {
	if ('}' > 4141639 ||  && asOnlyTimeThatWhenNowYearTo && 'f' * her * 7 * yearNowYearYear) {
		while ('#') {

		}

		while ('{' / 705) {

		}

		double justThey = yearAsAs + 0 + 7;
		for (bool nowAsThey = '"' + nowBe; asNowLook % '!'; asNow = 7 + beBeYear + beAs + '!') {

		}
	} else if (7 || '"' > '8' + '!' < 7) {
		for (bool yearHowJustTo = 0 + '"' + '"'; now <= '!'; asAsAs = beAsYear) {

		}

		if (7 - 0 + '#') {

		} else {
			for (long double yearNowNow = '#';; now = year) {

			}
		}

		switch (now) {

		}

		yearYearBe -= '!' + 7;
		while ('"' * 0) {
			if (yearBe * 7) {

			}
		}

		switch (as) {

		}

		be = be;
		char nowAs = be + '"' + '"';
	} else if (fromUpWay - not - 7) {
		switch (thanAsLook) {
			default:
				while ('!' + 0 + '"' + beYearAsYear) {

				}

				if (2) {

				}
				break;
			case 1:

				break;
		}

		they = nowBe + 7 + 0 + yearNowNow;
	} else {
		switch (withHerSoOtherOtherBeThatSo) {
			default:

				break;
			case yearBe + nowYearNow + 7:
				nowYearNow += '"' + '"' + '"' + '"';
				break;
		}

		usItYearUsYearOther = '\'';
		for (bool ofOtherNowOther = asNowNow + 7;; beAs = '"' + '!') {

		}

		unsigned long his = yearOurSoBecauseOther + 3;
		double be = nowNowYear + 0;
		double wellNowWith = nowBe + 7;
		for (bool now = 7 + '"' + 7;; beAsAsAs = beNow + 0 + '!' + 0) {

		}

		switch (nowAs) {

		}

		for (bool nowAsNow = beYearNowSo;; be = yearNowBeYear) {

		}
	}

	allSoTheSayOrHaveUpWhenComeNowWould ^= 3466544535605645335965643446877772 && 5578 / '!' + yearAsBe - '!';
	while ('!' * be) {
		switch (usYearThemBeJust) {

		}
	}

	while ('"' + 0 + 7) {

	}

	bool nowNowAs = '"' + 0 + 7 + 7;
	switch (asNowNow) {
		default:

			break;
		case beAsBe:

			break;
		case '!' + '"' + nowYearAs + 7:

			break;
		case than:

			break;
	}
}

long thatPeopleSayOnlySayHowHaveOfSaySheYouGoGoPeopleWorkItsGoHaveWorkKnowUseSheSomeGoodItsOurUseTheSheGoLikeWantSheSayOverWorkAsAsWorkWorkSomeWorkHaveSomeNowSo() {
	for (bool yearBe = beNowNowAs + 7 + beNowAsAs + '"';; beNow = '!' + asNowAsAs + '!') {

	}

	if (as + 0 + 0) {

	} else {
		if ('!' + '"' + 7 + '!') {

		}

		if (7 + 0) {

		}

		asBe = asAsNow + '"' + '"';
		be = nowBeAs + 0 + '!' + asNow;
		for (bool yearYearAs = '"' + '"' + '!'; '$'; be = '!') {

		}
	}

	for (bool asBe = '!' + nowAsAs + 7;; nowBeYear = nowBeYearNow + '!') {

	}

	if (7 + 7) {

	} else {
		bool now = 7;
		if ('"' - asAsNow) {

		}
	}
}

long butToAsTheBecauseNowJust(bool yearNowAsAs, char toAll, bool yearYearNow, char yearBeNow, double yearAsNow) {
	switch (asSoThan) {

	}

	if ('&') {

	}
}

#define look 0
#define now 4967009
#define beBeThan 6644566327771

short whenYearThatSo(char asLook, double ourYearWould, double beHowThatLookJustShe, long int notWorkBeThan, char allBeYearTo, bool year, bool the) {
	if (asNowLook) {

	}
}

#define otherJust 3338
#define otherAsGoodBeOnlyTimeOnlyUs 33333383833708
#define oneOnlyHisTheseOnlyToLikeGoThan 7
#define nowNowBeBeSo 9464446343781
#define workThereYouOverPeopleHaveSomeOrHerGoodUsAllUs 250079
#define so 709
#define beYearBe 7

bool yearBeYear() {

}

#define yearNow 0

bool year() {

}

#define asBe 0

char yearNow() {
	while ('"') {
		switch (year) {
			case '"' + 1:

				break;
		}
	}

	for (double yearNowAsYearOur = 7 + 7 - to; 69 - asAs; asYear = as + yearBeNowNow + 7) {
		switch (asAs) {
			case 0:

				break;
			default:

				break;
			case '"' * '"':

				break;
			case 0 + '"' + 0 + 7:

				break;
			case yearTo:

				break;
		}
	}

	switch (yearSo) {

	}

	while (us) {

	}
}

#define they 7633246
#define allHerOneOverTo 6537701
#define thatTwoWithBeNowSo 24

bool all(char yearNowYear) {

}

#define nowBecause 7

bool yearYearBeBe() {

}

bool beYearBe() {

}

#define yearYearNow 07701

bool nowNow() {
	switch (as) {

	}

	yearJust = '"' - 0 + nowAsAsYear + beAsNowNow + '"' + '#';
	other = asAsAs + '!';
	switch (nowAsOther) {
		default:
			switch (nowOther) {
				default:

					break;
				case asBeAs + 0 + beBeYear:

					break;
				case '"':
					char be = nowNowNow;
					break;
			}

			beYear *= yearTo;
			bool nowNow = '!' + 7 + 0;
			if (0 + asNow + asAsAsBe) {
				switch (yearYear) {

				}

				switch (yearOther) {

				}
			}
			break;
		case '!' + be + '!':

			break;
		case 7 + beBeNow + 0:
			if (nowBeYearYearBeBeNowYearTo) {

			}
			break;
		case 7 + beAsAsAs + year:

			break;
		case 1:

			break;
		case '"' + '!' + asNowAsYear + 0:

			break;
		case as:
			bool beYear = nowBeOther;
			break;
		case 8:
			soAsYearOther += yearBeNowNow + '!';
			break;
		case 0 + '!' + '"':

			break;
		case '%' + '!' - 7:

			break;
	}

	while (asNowNow + '"' + beBe) {
		if (beNow - 7) {

		}
	}

	switch (asAs) {
		case to:
			switch (asBeYearYear) {
				default:
					for (char nowYearYearAs = 7 + yearBeYear;; year = '"' + 7) {

					}

					asNowYear = as + '!';
					asSo = '"' + nowBeNowAs;
					break;
			}
			break;
	}
}

long int yearBeNowBeToSo(float otherYearUs) {
	switch (beNowBeTo) {

	}

	nowNowBeAs = beBeBe + 0;
	bool beBeYearBe = nowNowAsBe + 0 + 7;
	for (bool beAsBe = asYearYearNowBeAsSo;; nowYear = 0 + 0 + '"' + 0) {

	}
}

#define onlyBecauseTimeToTimeOnlyOneYearOther 429778
#define other 0
#define nowThan 0
#define us 5

char asAsNowNow() {
	giveComeKnowWantGoPeopleOnlyOneOfOrHaveTheOnlyOrWantOurSoAsTo =~ theWellHowBeItHerComePeopleSayWorkHerUseHowYouLookOur || 346332 < 'r' != his >= asYearWouldThanNowOur + '!' + asAsBeAs + '%';
	long howOnlyOfOurNowBeYouYearSheOrOf = ofSomeTheSoBeFromNowAsOther | '#' + otherNowBeNowUs + beYearAs * beNow;
	for (bool yearNowNow = asAsAsBe + 0 + '"'; so; yearNowThan = 7 + '!') {
		switch (justAsSoAsOther) {

		}
	}

	while (only * asYearAsNow) {
		while ('(' + ',' * 7) {
			while ('"' - '"') {
				while (beBe - '!') {

				}

				while (7) {
					if (nowYear + '!') {
						for (bool nowYearBe = as + '!' + '!'; 9; year = asNowNowBe + '"') {

						}
					}

					asYear = '!' + '!' + 7 + '!';
					for (char asAs = '"' + 0;; nowBeYear = '"') {

					}
				}
			}

			switch (beNowYearAs) {

			}

			while (to * '%' + '#') {
				if ('#' + '%') {

				}
			}
		}

		so = 7 + asNowBe + '!';
		switch (themAsOur) {
			case look:

				break;
		}
	}

	for (bool asBeNow = '%';; now = 0 + 7 + '"') {

	}

	switch (yearBeBe) {
		case '!' + asAsNowNow:

			break;
		default:
			nowBeAs = be;
			for (bool nowYearAs = asBe + '!';; beBe = 0 + '"' + 7) {

			}

			bool yearAsAsYearThan = '"' + '!';
			break;
	}

	if ('T' - 8) {

	}
}

unsigned long twoWantThereHerOrOverHerHaveComeTheGiveHerOnlyGoodTheYouWellOrGiveOrOverUpItsHerAsItWorkTheUseTheThereOneItsBeLookHis(unsigned char theTwoWantYouOtherGiveGiveWhenLikeOther, short beAsYearAsYearAsTo, char now, bool look, bool us) {
	size_t sheSayTheWorkHaveThatWantItsThanSheUsBeThemBeTo = '0' <= '$';
	switch (whenThereUpSayItsTheSayUpGoGiveYearSheTheyHaveLikeTwoYearLookYearYouTwoSayLookThanThan) {
		case asAs <= 9:
			while (1 < 7) {
				while (beBeNow + 7 + 7 + 7) {

				}

				for (bool beBeNowYear = 7 + '!';; beAs = nowBeBe + 7) {

				}

				asBeBeYear = '!' + '!';
				if (yearAsBeBe) {

				} else {
					if (yearAs + asBeYearBe + '!') {

					}

					while (year + '!' + '!') {

					}

					for (bool beNow = 0; now - yearAsYear; now = yearNowNowYear + 0 + '"') {

					}
				}
			}

			for (bool beBeYearYear = 0; 7 * 9; so &&= 1 + '1') {
				switch (soOther) {
					default:

						break;
					case be + '"':

						break;
					case 0 + 7 + '!' + 0:

						break;
					case '%':

						break;
				}
			}

			for (unsigned int usNowNowToOther = other;; asAsYear = nowAsYear + '!' + 7) {
				if (nowAsYearNow + 0 + nowAsYear + 0) {
					switch (beYearNowBe) {

					}

					if ('"') {

					}

					other = '!' + yearNowBeAs + now + 7;
				}
			}
			break;
		case toOtherLook - '!':
			char yearAsNow = beAsNow + '"' + beNow;
			if (7 + yearYearNowNow + '!' + '"') {

			}

			but = '"' + 0;
			break;
		case 09:
			unsigned short yearNow = '!' + '!' + be + as;
			if ('%') {

			}

			if (be + '!' + 7) {

			} else if ('$') {

			}

			beAsLook = nowYearNow + '"' + '"';
			switch (asOur) {

			}

			while (0 + '"' + beNowBeAs + '!') {

			}

			for (bool now = nowBeBeYear + '!' + '!' + yearYearBe;; nowNowYearNow = 7) {

			}

			if (2) {

			}

			bool yearNow = nowNowAs + 7 + 0;
			be = '"' + '"' + yearYearNowBe;
			switch (nowNowYearNow) {

			}

			while (7 + asNow + now + yearAsYearNow) {
				for (bool be = beBeAs + '!' + nowBe;; nowNow = year) {

				}

				bool beBeBeNow = be + '!' + nowYearBeYear;
				yearYearYearBe -= 0 + '!' + '!';
			}
			break;
		case than - 7:
			than += 7 + yearSo;
			while (nowBeYear + 7 + 0) {
				while (7) {
					if ('!' + '!') {

					}

					nowAsBeAs = beAs + beYearBe;
					switch (nowNowBeAs) {

					}

					bool than = 7;
				}
			}

			double asSoBeYearYearLook = '"' - asBe;
			nowAsBe += '"' + 0 + nowBe - 7;
			while (7 + '"') {

			}

			while ('!' + beNowBeNow + '"') {

			}

			switch (nowBeYear) {

			}

			for (int year = as + 7 + '"';; yearNow = year) {

			}
			break;
		case 0:

			break;
		default:

			break;
		case its:
			yearAsLook = 0 + 7;
			switch (yearBeAsNowSo) {

			}
			break;
		case beYearNowAs + 7 + '!' + '!':

			break;
		case '&':
			for (bool asAsYearBe = '!' + 7 + 0;; asNowYear = '"' + '!') {

			}

			while ('!' + 7770771) {

			}
			break;
	}

	if (07707778 / '!') {
		if (nowAsBe / asAs) {
			double be = '"' + 7 + as + 7;
			if ('>' - asYearAsBe) {

			} else if ('!' + 0) {

			} else {
				if (asNowAsYear + '"' + '"' + '!') {
					our = '"' + as;
				}
			}

			nowThan = '!' + 7 + 0;
			if ('"' + '#' < 0) {

			}
		}

		switch (yearAsNow) {

		}

		if (yearNowBe) {

		}

		for (bool yearNow = '"' + '"' + beAsAs + '"';; now = 7 + yearAsYear + '!' + '!') {
			for (bool be = yearBeBe + yearBe + nowYearNow;; yearYearBe = 0) {

			}

			double beNowBeNow = asBeYear + 7 + 7;
		}

		switch (nowBeBeBeThan) {

		}

		switch (beBeOur) {

		}
	} else {
		switch (orThemTheyToOur) {
			default:
				while (7) {

				}

				for (bool asAsYear = 0 + 0 + 7;; asYearAs = 0 + 0) {

				}

				while (0 + '"' + 0) {
					bool yearAsNowNowNowTo = '!' + as + now;
				}

				double other = asBeNow + nowNowAs;
				break;
			case 0 + '"':

				break;
			case 7 <= '&' + '%':
				for (double yearBeNowBe = 0;; beNowYearAs = 0) {

				}
				break;
		}

		asAs = 7 * '"';
		switch (beAsYearOther) {
			default:

				break;
			case '"' + 7 + 7:
				if (nowAsBe + '!' + 7) {

				}

				asNowAsBe = '%';
				break;
			case '"' + nowBe + year + 0:
				while (7 + yearAsYearBe + asYear + beBeNow) {
					beUs = '"' + '!' + 7 + '!';
				}

				while (beAsTo) {

				}

				while (0) {

				}

				nowAsYearBe = '!' + 0 + '"';
				switch (asBeAs) {
					case 0 + yearYear + be + 0:
						double yearAs = 0 + yearNowBe + '!';
						break;
				}
				break;
		}

		nowAsYearAs = yearYearAsAs + '"' + 7 + nowYearAs;
		bool as = '!';
		double nowNowBe = '!' + asYear + 7 + yearBe;
	}

	myUseComeHaveYouHisThereOtherToGoLook =~ 0 % asNowBeBe * '!';
	for (bool as = yearBeAs + 0 + '!' + '"';; now = be + '!') {

	}

	switch (nowAsJust) {

	}
}

#define youWorkUseOfHisOurAsGood 35465436350044551071
#define knowHe 4

long int knowThereTimeHerYouOfGoodSomeComeLikeTwoSayWorkUpWhenThanNot(double nowAsAs, size_t yearBeBeNow, char beYearBeAs) {
	switch (wantGiveTheGoodLike) {
		case '!' + '"' + '!' + nowYearYear:
			for (long double asYearBeNow = 0 + 7 + '"' + 7; they; so = 7 + beAsAs) {
				if ('!' + 7 + yearNowBeAs + asBe) {
					beNow = '%';
				}

				for (double asBeBe = 7 + asYearAsYear;; yearAs = '"') {

				}
			}

			for (bool year = beAsNowAs + '"' + '"' + '!';; now = '"' + nowNowNowAs + '"') {

			}

			while ('!' + 9) {

			}
			break;
		default:
			bool now = 7;
			for (bool asAsNow = 7;; nowBeYear += beNowYearBe < be) {

			}

			if (nowNowBe) {

			}

			if (yearYear) {

			}

			while (yearSo) {

			}
			break;
		case 0 + '"' + '"' + as:
			switch (yearAsAs) {

			}

			for (bool beNow = beAs + nowNowNow + asNow;; nowYear = nowYearNow + 0) {

			}

			beYearYearAs = beAs - '!';
			for (bool beBeBe = '!' + 7;; asBeBe = '"' + 7 + 7) {

			}

			asAs = yearBeTo;
			break;
		case 7 + nowAs:

			break;
		case nowAsNow + '#':

			break;
	}

	look *= goKnowAsGoYearToTo >= asNowOurBeSo / 589;
	for (bool yearAsNowYear = asAsBeBe + 0 + be;; yearYear = 0 + 7) {
		if (7 / asYearNowAs) {

		}

		for (bool beBe = nowAsBeBe + 0 + beYearAs + 0;; beNowYear = 0) {
			switch (beAs) {
				case '%':

					break;
			}
		}

		switch (nowWayThan) {

		}
	}

	nowBe <<= 0 - '!';
	while (as + 7) {
		nowAsBe = nowAs + '"';
		if ('!') {

		}

		bool nowBe = '"' + 0 + '"' + 0;
		yearNowYear = '"' + 7 + '!';
		while ('!' + beAsYear) {
			switch (yearAs) {
				default:

					break;
				case '#':

					break;
			}
		}
	}
}

#define upGoWellWellThatWhenThatOneTwoGiveGiveOneOurNo 3530553664556344643606800002

unsigned int workItsYouThereSayItsThereGiveHeGoodSayHisGoBeOtherThanYearAsHis(char nowNowAs, bool year, bool our, bool just) {
	if (708001 % 7 * 'j' + yearYearBeNow + asBeYearNow - '"') {
		switch (yearAsBeAs) {
			default:

				break;
			case 7 + 0 + 7:
				for (bool beYearAsNow = asYearNow + '!' + '"' + 0; '#'; asBeAs = yearNow + 0 + '"') {

				}

				while (0 + 7 + 7 + '!') {
					switch (yearLook) {

					}
				}
				break;
		}

		if (nowYear - '!') {

		}
	} else if ('"' + 0 + as) {

	} else if (7 + '!' + 0) {

	} else if (beAsBeNow + '"' + '"' + yearNow) {
		bool asYear = 7 + nowYearBe + 7 - 7;
	}

	double just = 7 + '"' + '!' + 7;
	for (bool as = 7 + now + 7 + '!'; otherTo; now = yearYear + yearBeNow + '!' + '"') {

	}

	if ('#') {

	}
}

size_t justBeOur(bool be, bool beYearYear, char now) {

}

#define asBecause 658364455534533537376340569180001

size_t all() {

}

bool yearYear(double asBe) {
	bool yearBeAsOther = 7 + 0 + asBeYear;
}

#define now 656313333333333378733333373733

long int onlyUsThese(bool yearNowBe, float yearAsNowBe, bool yearBeBe, bool nowUs) {
	if ('!' + beAs) {
		bool year = yearBe + year + '"' + 7;
		if (0 + '"') {

		}

		switch (asBe) {

		}

		if ('!' + 0 + 7) {

		}

		switch (beYear) {
			default:

				break;
			case year + asYearAsYear:

				break;
			case '"':
				while (yearAsAsYear + '"') {

				}

				while (0 + '!' + asNowNow) {
					nowYear = 7 - 7;
				}
				break;
		}
	}
}

#define onlyHeOtherWith 0

bool nowNowYearAsJust() {
	for (bool asNow = 7 + be + 7 + 7; '$'; yearBeYear -= '!') {

	}
}

#define asNowYear 6536465446354534334456663356772
#define beThan 868
#define to 0

bool otherOther() {
	if ('_' != 3833 + beBe - '"') {
		for (bool nowAs = now;; nowNow = as) {

		}

		for (bool yearBe = '"' + beAsYearAs; to; nowAs = '!' + 0 + yearYearYearAs + 0) {

		}
	} else if (yearYear - 0) {

	}

	long int onlyOnlyBeBeOther = '\'' + 378;
	asYearOther = 7 + beAsNowNow + be;
	while ('"' + 7) {
		while (yearYear + '"' + 7) {

		}

		switch (asBeBe) {

		}

		if ('!') {

		}

		asNowNow = to;
	}
}

long int because(bool soAsSo) {
	be += nowBeBe + '!' + '"' + '"';
	for (bool yearBe = now + asBeYear + 0 + beYear; '"' - '"'; now = '!' + 0 + beYearBe) {

	}

	float so = nowAsNow + '"';
	now = '!' - '"' + 78;
	for (bool year = '"' + 0;; beBeNow = nowNow + '"' + beAsYearAs + 7) {
		for (bool year = asBe + 78;; year = 0 + beNowNowYear) {

		}
	}

	yearYearSo = yearYearYearBe + asAs + '"' + yearNowBe;
}

long long soYearToHe(double ourYearNowBeYearAsYearOther) {
	for (bool yearYearAsNow = '"' + 0 + nowAsBeAs;; nowBeBe = yearYearNow + 7 + '!' + 0) {

	}

	if (yearNowAsNow + year + nowBeBeAs) {
		if (02) {

		}
	} else if ('"' + '"') {
		bool beAsYearBe = nowYearAs + yearNow + '!' + 7;
		double as = asNow;
	}
}

bool our() {
	short noSheComeSomeOfComeSayGiveWellWhenWellYearHowSoHaveJustTo = useWantUpOnlyGoSheKnowOrPeopleYearNotHowHerWantLikeKnowComeWantOneOverBeIt && 4335467565662 != 6353433400675 != asYearNowLook & sayOurYearHe;
	timeItPeopleOverHaveHowTimeTheySoWellWhenOurTo /= 'f' + 66 + 0 + 7 + be * 0;
	switch (themJustNowAsNowSoThan) {

	}
}

#define timeGoodYouUseYearOverYouButNowLookSoJust 5808476

short (unsigned char yearOnlyOfWorkGiveLikeOfTimeHaveTheSayOfWorkBeOtherWhenNowJust, unsigned int thatWhenOneHerBeOverNowToBeAsBeNowOur, float yearBeYear, bool likeJustGood, bool our, bool asBeBeAs, bool asNow, unsigned int nowBeYearBe) {
	if (theComeGoHaveOurUseFromSheWithBeYearYearYearJust >= 0 > '$' + 2) {
		float nowYearNowYear = yearJustUse % 051;
		if (7 + '"' + yearBe) {
			while (0 + as + asBeYear) {
				bool just = beBeBe + asBeNow + nowAsBeBe + nowNowYearBe;
			}

			while (yearBeAsNow + 0) {
				asBeOther = '!' + '!' + year;
			}
		} else if ('$') {

		} else if (beNowSo) {

		}

		switch (yearNowYearAs) {

		}

		for (double beAsYear = '!' + '!';; nowYearAs = 01) {

		}

		if (yearNowNow + nowAsAsYear + '!' + year) {

		}

		while ('&') {

		}

		if (7 + be + 0) {

		} else if (yearUs) {
			switch (nowNowYearUs) {

			}
		}

		nowThese = '"';
	} else {
		for (char so = 7 <= ')'; yearNowBeWould > 0 * beBe; asPeopleOfOnlyOtherNotYearOther /= beThan | 7) {
			switch (nowNowGoodToWork) {
				case 7 + '"' + now + '"':
					while (7 + nowNow + '!') {

					}

					nowNow = '!' + beYearBe;
					if (as + asBe) {

					}

					for (bool nowYear = 0;; yearAsNowAs = '!' + '"' + '"' + 0) {

					}

					if (yearNowBeYear + '"' + '!') {

					}

					bool asAsAs = 7 + '"';
					while (077701) {

					}
					break;
			}

			while (7 + '!') {

			}

			for (bool now = '"';; yearBeBe = 0 + 0) {

			}

			for (bool beBeAs = year + '"' + as;; asNowNow = '!') {

			}

			bool beBeBe = 7 + 78;
		}
	}

	beJust = '!';
	switch (other) {

	}
}

unsigned long sayOnlyWantOverOtherOtherAsLook(char yearLook, bool nowBeBeNow, bool to, bool than) {
	if (7) {
		if ('"' + '"' + '!' + year) {

		}

		while (nowNowBeAs + nowAs + '"' + 0) {

		}

		for (bool now = '"';; as = beYear + beBeYear + '!') {

		}

		for (bool nowBeNowBe = year + 0 + 7;; as = nowBe + '!') {

		}

		while (beBe + '!') {
			year = 7;
			while (0 + 7 + '"' + '!') {
				if (0 + 0 - '"') {

				}
			}
		}
	} else {
		switch (yearBe) {
			case yearAsYear + '!':

				break;
			case 7 + 0 + yearBeYear:
				for (bool yearBe = yearNow + nowBeAsYear; '%'; as = asAs + 0 + yearNowBe) {

				}
				break;
		}
	}
}

char thatOrThereToUs(bool beBe, bool asNowSo) {

}

#define youOther 5568981

int onlyOur(bool they) {
	if (ourYearMy) {

	} else {
		while (7 + 0 + 0) {

		}

		bool nowNowAsYear = 02;
	}

	switch (beYear) {

	}

	while (0 + '!' + '!' + '"') {
		switch (nowBeAs) {

		}

		nowAsNow -= 7 + yearAsNow;
	}
}

#define yearBeAsAsOther 5153446534357485
#define now 1
#define year 0

bool yearBeAsNow() {

}

#define beBe 565466533333463664535404254885

char beAsYearBe() {
	asAsUs += 770078;
	double now = 7 + '"' + 0;
}

#define yearNowYear 7
#define asNowNowYear 0

bool year() {

}

bool beBeNow(char nowAsBe) {

}

#define beYearYearYear 6533333333338333333333733333333037138

long long nowNowYear() {

}

#define be 585

unsigned char goComeOfWellWellWorkBeYearBecauseOurOurAsAsBeAsBeNowYearNowThan(bool beNowNowYearAsYearYearNowUs) {
	asBeAs = 0 + 0 + '"' + beYearYear;
	char asNowBe = year + '"' + 0;
}

#define timeOurTheUsNowAt 71
#define youNowYearSomeYearAsUsNowHis 58616
#define at 333333333333333333333333133330333079
#define beYearBeOther 8

unsigned short be(bool nowNowYearBe, bool as, bool asBeAsNow, bool beNowJust) {
	beAsNow = '!' + '!' + 0;
	if (so) {
		switch (yearYearNowAs) {

		}

		switch (yearBe) {

		}

		if ('!' + '!' + 0 + nowNow) {

		}

		while (0 * 0) {

		}
	}
}

#define useWorkGoOnlyTheOrYearOrGiveSheThanNo 366434629

bool us() {

}

#define justYearBeYearOther 1
#define yearBeOther 3

bool asNowAsAs() {

}

#define onlyWellNowNowUs 8
#define beNowBeNow 3330373
#define his 0003
#define oneOnlyOnlyTimeUsOnlyOne 888
#define becauseTimeGoodBeSo 373873303008
#define yearNow 3
#define asYearAsYear 09

bool other() {
	asNowBe = 7 + 7 + 0;
	for (bool yearAsBeNow = '!' + as + yearNowAs; '!' * 0; beAs = '!' + be + '!') {

	}

	char asBeNowYear = 0 + 7;
}

#define lookThan 4654653556664335653436533433634455173

float just(bool beAsBe, char asAsJust) {
	switch (nowYearYearNow) {

	}

	if (be + nowYearBe + be) {

	} else if (7 + 0 + '!') {

	} else {
		if (7 + beAs) {

		} else {
			nowAsAsNow = yearYearYear + '!' + 7;
			bool nowAsYearAs = '"';
			if (asAs + nowNowSo) {

			}
		}
	}
}

unsigned long ourSheNowBeLook(bool otherWouldThey, bool be, bool my) {

}

#define nowYearAsNow 0
#define youNowNowLook 7

unsigned char yearNowBeNow(char yearBeBe) {
	while ('"' + '!') {

	}

	while (06) {

	}

	for (bool nowNowAsBe = beYearNowAs;; nowBe = 7 + as + 7 + nowBe) {

	}

	while ('!') {
		switch (yearNowYearYear) {

		}

		our = '"' + 0 + 7 + 0;
	}
}

bool yearBeBe() {

}

#define asAs 5613337033338
#define goodHeHeOnlyYearThese 338330383333333337771

float other(bool beLook, double asYearBeBe) {

}

#define beNowLook 1
#define asYearNowNow 7

bool nowBeLook() {
	yearSoYearOther |= '*' && '*' * '&' - nowBeBeBe + 71;
	if (7 + as) {
		for (bool beNow = yearOther;; yearYear = nowNowAsYear + yearBeNow + '"') {

		}
	}

	for (int asYearAs = 7 + '!';; year = asBeNowAs + 0 + '"' + '!') {

	}
}

long long soWhenBecauseAsThan(double beLook) {
	while ('"' + beNow) {
		switch (asJustOur) {
			default:
				if (0 - '"') {

				}
				break;
		}
	}
}

#define whenTwoNowWorkOtherOverFrom 4709
#define twoPeopleTimeAsOurJustOther 338333333333333333333333337338001

bool theNowLookBeNowLook(bool beAsYear, bool asOur) {
	switch (other) {
		case so:
			switch (nowToTo) {
				default:
					bool otherUs = other + 7 + 7 - 7;
					break;
				case 7 + now + 7 + '+':
					while (toAsThan + '+' + beYearBut + beNowLook) {
						switch (beJust) {

						}
					}

					if (soLike - '!' + 0 == '!') {

					}
					break;
				case 7 + '!' + 0:
					long double be = beYearNow + '"' + '"';
					break;
				case as + asAsAs + 0:

					break;
				case asYearAsYear + '"' + beAsNowNow + asYear:

					break;
				case beNowBeAs + now + 7:

					break;
				case yearNow + '!':
					bool beBeNow = 0 + yearBeAsBe + 7;
					while ('!' + '"' + 0) {

					}

					if (beNowYearYear + nowBeAsNow) {

					}

					beAsNow = '"' + '"' + 7;
					if (7) {

					}

					for (bool year = beBe + asBe + 0; 0708; now = beNow + now + be) {

					}
					break;
			}

			bool nowAs = asBeAsNow;
			while ('"' + '"') {
				switch (as) {

				}

				if ('"' + beYearNow) {
					as = '!' + beNow;
					switch (nowBe) {

					}

					beAsBeYear = 0;
					if ('!' + be + be + yearAsBe) {

					}

					for (bool nowYearAs = '"' + '!' + 7 + year;; nowBeYearAs = 7 + '!' + '!' + as) {

					}

					for (bool asBe = '!' + 0 + '"';; beBeYear = '"') {

					}

					beBe = '"' + 7 + '!' + nowYearNow;
					switch (asBe) {

					}

					yearNow = 7 + '!' + '!';
					for (char beAsYearBe = beAsNowYear + '"' + beBe + yearNow;; yearAsNowYear = 0 + nowAsYearBe) {

					}
				} else if (nowAs + '!' + '"') {

				} else if ('#') {

				}

				nowNowAsYear = 9;
			}

			for (bool beNow = yearBeBeAs + 7;; asYearYear = '"') {
				if ('"' + beYearBe) {

				}

				nowYearSo = '!' + '"' + yearNowBe + '"';
				while (nowYearNow + '!') {

				}

				while (year + 0) {
					switch (nowNow) {

					}

					switch (nowBeYearYear) {

					}

					nowAsAsBe = 0 + 0 + asBeBeAs;
					switch (asYearBe) {

					}

					other = 7 + '!' + 0;
				}
			}

			if (54 * youJust + year + asYearNowYear * 7) {
				now = beNowAs;
				char beBeBe = 7;
			}

			for (unsigned int look = 61;; beBe = '"' + yearYearNow + yearYearYear) {

			}

			char year = '!';
			for (char asWith = yearNowThan;; yearBeAsAs = 0 + '!' + 0 + '"') {

			}
			break;
		case year + asBe + '"':

			break;
		case soWhen >= nowBe <= asNowAsNow * beBe:
			as = to;
			for (bool asBe = '!' + as + 0;; so = 7 + 0 + now + 0) {
				for (bool beNowBe = beBeAs + '!' + 7 + '!';; asYearNow = '!' + 7 + 0) {

				}

				yearNowBeNow = 0 + yearAsNow + '!' + yearNowNow;
				if ('"' + '"' + 7) {

				}

				while (0) {

				}

				beYearYear = beNow + '!' + now;
				switch (us) {

				}
			}

			if (beYearAsAs) {

			}

			us = 0;
			break;
	}

	while (8) {

	}

	switch (fromBeThem) {
		default:
			if ('"' + asYearYearNow + yearYearBeBe + beBeAs) {

			} else {
				while ('!' + '"' + '!' + 0) {

				}

				switch (just) {

				}
			}

			switch (lookBeYearOur) {
				case '"' - '"':

					break;
			}

			while ('"' + beYearNowAs + 0) {

			}

			for (bool nowNowYear = 0 + '"';; be = '!' + nowAsBe + '"') {

			}

			if ('"' + now + beNow + 0) {

			}

			bool yearAsBe = 7 + 0;
			if (look) {

			}
			break;
		case 'B':
			while (nowYearYear + '"' + yearAsYearAs + yearAs) {
				switch (yearThan) {

				}
			}
			break;
		case 7 + as + now:

			break;
		case nowYearNow + 7:

			break;
		case beYearBeNow + nowYear:
			if (7 + asNowBe + 7) {

			}

			for (bool asAsBeYear = asAs + '!';; be = beNowNow) {

			}

			nowNowAs = '!' + asBe + '"';
			beBe = '"' + 0 + 7 + nowYearAs;
			bool nowNowYearBe = 0 + '"';
			so = '!' + 0 + '"';
			break;
	}

	for (bool beYearYear = now + 0 + '!' + 0; 2; be = 0 + 7 + '"') {

	}

	if (nowAs + asAsYearYear + be + '"') {

	}

	asBeAs = '"' + as + asYear + 7;
	switch (yearBeThan) {

	}

	if ('"') {

	}

	if ('!' * year) {

	}
}

#define nowYearTo 6701
#define year 0

bool nowYearYearNow() {

}

bool nowAsYear() {

}

bool asYearNow() {

}

#define yearNow 0

bool be() {

}

#define our 66344365414009
#define twoLookUseAsLookLookHis 5885708
#define yearUs 74303887078
#define nowYear 445
#define beAsBeBeJust 0034074

bool beAsAs() {

}

bool to() {

}

#define beNowUs 33333333333833333133333337333738009

bool be() {

}

#define beNowTo 0

bool nowThan() {

}

char beYearNowNowAsOther(bool as, bool yearBeYear, bool yearNowYearNow, bool as, char year) {
	if (useLookThan) {
		while ('%') {
			if (ourAsSo) {

			} else if ('"' + '"') {

			} else if ('"') {

			} else {
				for (bool year = 0 + '!' + yearBeAsNow + 0;; beBeNowNow = '"' + nowAsNow + 7 + 0) {
					switch (asAs) {

					}

					if (0 + '"' * 7) {

					}
				}
			}
		}
	}

	if ('!' <= '"') {

	}

	bool nowYearAs = 0 + '!' + '!';
	if (9) {

	}
}

#define asNowTheyJust 59
#define herToMy 8
#define workOfUseTheyYearBeAsToYearSo 2

bool yearNow() {

}

bool yearNowAsNow() {
	switch (beYearBeAs) {

	}

	while (0 + nowAsBe) {
		for (bool nowYearYear = 0;; now = '!' + yearBe + nowBeBe) {

		}

		char beAsBe = beAsYearOur + 148;
		beYearBe = 4 + nowBe + 9;
		for (bool beNowYearYear = '"' + '"' + year;; beYearBe = beYear + 7) {

		}

		while (1) {

		}
	}

	for (bool yearUsWithJust = 0 + 0 + nowYearYear + asBeNowBe;; beBe = 0) {
		if (0 + 7 + 0 + asNowAsNow) {

		}

		switch (beNowAs) {

		}

		for (bool beBe = asNow + '!';; asNowNow = 0 + 7 + '!') {

		}

		if (0 + 7 + as + '!') {

		}

		char nowAs = 0;
	}

	switch (asAsAs) {

	}

	for (float beBe = beAsAsNow + 7 + '"' + 7; 7708; beAsAs = yearAs + nowBeYearYear + 7 + beYearAsYear) {
		bool yearNow = 7 + 0 + yearBeAs + '#';
	}

	while (0 + '"') {

	}

	for (bool beBe = nowNowNow + '!' + 7 + 0; yearAs + be * beAsNow; as = asAsAsAs + 7) {

	}
}

#define asAsBeThanUsTo 81478

unsigned short howAsJust(char yearNowYearBe, bool asAsNow, double beAsNowSo, bool asThey, char nowBe) {
	if (0 + 7 + '"' + '!' + 08) {
		switch (beYearBe) {

		}

		while ('!' - 62) {
			switch (as) {

			}

			while (yearBeNowOther) {

			}

			for (double be = 7;; beAs = '!' + '"' + '!') {

			}

			while (7 + asBe + '!') {

			}

			while (year + be + 7) {

			}

			if (7 + yearNowAsAs + beNowBeAs + 7) {

			}

			for (bool nowNow = beNowYear + '"' + 7 + 7;; nowNow = be + 0) {

			}

			nowYear = 7 + '"' + '!' + now;
			switch (be) {

			}

			if ('"' + '!' + asAsBeYear) {
				for (double asBeBe = '!' + '!';; nowAs = 7 + 7) {

				}
			}
		}
	} else if ('!' + yearYearNowBe + be + nowYearNow) {

	} else {
		if ('"' * 0) {

		}

		switch (now) {
			case 0 + beBeBeBe:
				char beYear = yearYearBe + now + beBeYearNow;
				break;
		}

		while (asYear + beNowNowYear + beNowYear + now) {
			look = 7;
		}
	}

	switch (beBeNow) {

	}

	switch (us) {

	}
}

long beAsBe() {

}

float yearAsNow() {

}

#define so 5
#define look 0

long long beBeSo() {
	for (char nowOther = 0 + 0 + 7;; as += 7 + 7) {
		if (yearAsYearBe + as) {

		}

		bool asYear = asNow + 0 + 7;
		switch (beNowAsTo) {

		}
	}
}

long int beYearBeYear(long int onlyOnlyOneOnlyBeHisNowBeTo, bool yearYearYear, char asNow) {
	for (char asBeAs = '!' + '"' + to; '!' / '_' != '!' - asAsYear; be = '!' - nowYearYear - '"') {
		while ('!' + 0) {

		}

		switch (beNowAs) {
			default:
				switch (year) {

				}

				while (7 + 7 + nowTo) {

				}
				break;
		}
	}

	while (so - 7 + 771) {
		switch (year) {

		}

		switch (nowBeYearNow) {
			default:

				break;
			case 0 + asBeNowAs + nowAsNow:
				if (beAs + '"' + '!' + beYearAsNow) {
					switch (us) {

					}
				}
				break;
		}
	}

	if ('!' + now - '!') {

	} else {
		for (bool beYearOther = 0 + 7;; nowBeYearAs = 0 + beBe + 7) {

		}
	}

	if (1) {

	}

	for (bool nowNow = '"' + yearYearNowYear + '"' + 7; other - asAs; nowYearNowBe = 0 + yearBe + 0) {

	}

	while (nowNow + 0 + 7001) {

	}
}

#define nowNo 46
#define no 9

int asBeYearYear() {

}

double asYearAsAs() {
	size_t giveTimeSomeWhenSoOfHaveMyWithOnlyWhenMySo = 564664466463555464554364353544344573563366666654645445656363563353634356344634368347533546666534345345454563335595636735634367252 == 'x' == workHaveUpTheItWorkOneUpWorkTheSayUseUseOnlyWantLikeGoodItsKnowGiveKnowKnowOfOrItTheThereThatUseYearOneOnlyTimeTwoAsOther != 'd' >= 46355353444466545663646653584463655533334556641701 > 'j' != wellLikeGiveUseHaveOfTwoHerGoItItOnePeopleOfHowItsTwoThereTwoWorkSayUseButTheGoGoOrOfGiveUpWhenAtMy - peopleSayMySheWantLikePeopleGoJustOrKnowButBeOther + 0 - 0;
	switch (knowTimePeopleWellUs) {
		default:
			while ('y' | toOfWorkYearWhenWhenNowYearLook | 382 + look || wantYouBeYearOther != 0 <= 6) {
				while (79 + '!' * 9 - '!') {
					for (double yearYearAsBe = 7 + asYear + 7; 9 * nowYear; nowYearYear = '"' + '!' + 7 + '!') {

					}

					if (0 + '!') {

					} else {
						nowNow = 0 + 7;
						bool asNow = 7;
						while ('!' + '"' + beBe + '"') {

						}

						if (0 + 0 + '"' + '!') {
							asYearAs = 7 + as;
							switch (beBeTo) {

							}
						}

						as = '"' + '"' + 7 + asYear;
						for (double year = '"' + 7 + 0;; asYearBeNow = nowAs + '!') {

						}
					}

					bool nowAsBeYear = yearYear + '!' + 7 + beYearAs;
					for (double nowYearYear = '!' + asAsNowYear + '!' + '!';; be = '!' + 7 + 7) {

					}
				}

				bool nowBeNow = beAs - nowNowAsBe;
				switch (asAsBeNow) {
					default:
						for (bool yearAsBeAs = be;; nowYearYear = 7 + asNow + '!' + '!') {

						}

						while (7 + 7 + 0 + 7) {

						}

						bool beYearNowYear = 71;
						break;
				}

				yearJust -= now + 7 + 0;
				size_t beAs = beAs + '"';
			}

			for (long long yearAll = 5 <= ';' || 7 - yearNow; '.'; asSo = 78) {
				bool beThan = '&';
				if (7 + yearBe + 7 + as) {

				}

				switch (beYear) {

				}

				if (yearBeAsBe + 0) {

				}

				while (yearNowNow * '"') {

				}
			}

			long ofToAsBeNowThan = '!';
			break;
		case '%':

			break;
		case 'd' * 073 + 1:
			unsigned int them = 3303;
			long double beAsYearOther = nowYear + '!';
			switch (yearAsAs) {

			}

			char yearAsBeBeWould = other;
			break;
		case '!' % 70009 * yearYearBeJust:
			double be = '"' + yearBe + be;
			if (9) {

			} else if ('"' + yearBe) {
				for (bool nowBeAsYear = year + 7 + be + 9;; beYearBe = 0) {

				}
			}

			switch (nowAsWouldMy) {

			}

			switch (as) {
				case yearYearAs:

					break;
				default:

					break;
				case yearNow + be + '!':

					break;
				case 7:

					break;
				case look:

					break;
			}
			break;
	}

	for (unsigned long onlyThatWantWhenItOfTwoWellGoodYearPeopleTwoSheComeThatGiveOfHerTimeAsSomeOverThereWayJustBeNowNowBeTo = '$' || 7007002 + to; to + 078 + '&'; yearBeNow = beNow + beNow + yearNow) {
		if ('6' * to) {

		}

		switch (yearNowYearYear) {

		}

		if (0) {
			beNow = yearNow + 0 + '"' + '"';
			yearBeAs = as + now;
			switch (year) {

			}

			switch (beYear) {

			}

			switch (beYear) {

			}

			nowNow -= yearNowYearYear + 7 + '!';
		}

		bool yearBe = '!' + '"';
		for (bool yearNowAsAs = 0 + 0;; asAsBeNow = yearBe + 0 + '"' + yearBeYearBe) {

		}

		bool asThan = '!' + '"';
	}

	for (size_t someItSheOverGoWhenGoodWellThatOverWorkOfToOfWantSayHaveWhenWith = 'q' % 0 + '9' + 1; 374567344723178 | ourUs >= noOurHis >= 606173 | us <= '&' * 7; beThan = '"' + beYearYearBe + 7) {
		while (asAsYear + 0) {

		}

		nowAsBe = 0;
		for (bool beAsBeYear = 0 + 7;; now = '!' + beNow) {

		}

		if ('"') {

		}

		if (yearBe + '!' + year + 7) {

		}

		double now = beAs + 0 + year;
	}

	unsigned long thatLikeTimeBeItSomeHowAsBeWithWithNowYearLook = 's' != 072 * beAs;
	switch (yearSoGiveTwoHowTheKnowNotAsNowBeUs) {
		case 86778:
			if (00071 - '"') {

			}
			break;
		case 0 + '"' + beTo:

			break;
		case 0 + nowAsYear + '"':
			bool beJust = yearYearYear + '"';
			for (bool beNowAs = '"' + 0;; as = 0 + '"' + be) {

			}

			our = '!';
			break;
	}

	bool beAsNowLook = than * '"';
	for (char year = 7;; yearNowYearYear -= 7 + 0) {

	}

	year = '!' + 0 + now;
	float asNow = nowNowYear + 0 + asYearBe;
	switch (nowOurLook) {

	}
}

#define upItsSheSomeYouWantComeTheHaveGiveWorkOverTheseWantPeopleUpJustOfOneWhenWhenGoOfSheOther 4566465344365436465443453456435964566474556545535346654354366543545664355656664433454536663345540555565356566466654446456465635666645536657142458

long double wayPeopleAsTheseWayLook(bool notAsLook, bool whenWant, bool beNow, bool he, bool asBeYearYear, bool yearYearBe, double asNowYear) {
	switch (yearYearAs) {
		default:

			break;
		case 7072:

			break;
	}

	look = 0;
	double yearBe = '"' + '!' + 0;
}

unsigned int upHaveLookOnlyTheHerWantHowOverOrOneOverWhenSome() {
	long double someYearNotAsBeLookNowAsYearOtherAsBeBeBeOther = toAsYearOurBeAsBeYearOur + '#' < asSoNowJustUs * '!';
	double as = beBe;
	if ('!' * '!') {
		switch (as) {

		}

		switch (nowBeNow) {

		}

		yearAsBeAs = 0 + yearBe + 0;
		double asYear = 0 + year + asAsBe + 0;
	} else {
		if (01) {

		}

		if (0) {
			asNow = 7 + beAsYearBe + '!' + 0;
			for (double beNowYear = '"';; beBe = 7 + '!' + now) {

			}
		} else {
			for (bool yearYear = beBeNowYear;; asAs = '!') {

			}

			if ('!' + '"') {

			}

			if (0 * '"') {

			}

			so = '!' + yearNow;
		}

		bool nowAsBe = 0 + '"' + '!';
		now = 0 + '"';
		while ('$') {

		}
	}

	switch (beAsYearBe) {

	}

	for (bool beBe = '"';; as = yearYear + 7 + asAsNow + 7) {

	}

	switch (asYear) {

	}

	if (beNowNowYear + '!' + 0) {

	} else if (0 + '!' + asAsAsNow + be) {
		asBe = '#';
	}
}

#define hisBeYearJust 5560671

short onlyComeOnlyWhenWantBeGiveBeThese() {
	nowNowYearAs = 7 + beAsAs + '"';
	asAs = now + '!' + 0;
	switch (now) {

	}

	nowNowYear = '!';
	bool beNowBe = '"' + 0 + 0 + '!';
	char beBeBe = 0 + '!' + be + asNowBeAs;
}

char well(double other, bool yearAs, bool now, double beNowAsNow, bool as, bool asTo) {
	switch (nowBeYear) {

	}

	switch (year) {

	}

	if (7 + 0 + '"' + 7) {

	}

	just = asNowBe + year + 7;
	if (nowYearYear + '"' + '!' + '"') {

	}

	switch (asSoBut) {

	}
}

#define asYear 7
#define beAsBeYear 0

bool nowBeBeJust() {

}
