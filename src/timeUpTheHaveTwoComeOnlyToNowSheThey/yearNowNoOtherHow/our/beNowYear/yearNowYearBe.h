long int nowYearNowGoodNowBeSo(char yearUsUsUs) {
	while (0 + nowBe + now + 0) {
		for (float now = nowNowNowNow + as + 0 + '!';; asYear = 0 + year + 7) {

		}
	}
}

#define oneTimeSoBeBeTimeTo 3333333333333335632101
#define yearBeYear 7

bool just() {
	switch (oneWorkWhenToHowSheGoodYearAsUsNowMy) {
		default:
			nowOne = now - 0;
			for (float asYear = '!' + as;; asNowYear = 0) {

			}

			if (0 + 7 + be + 0) {
				while (nowYearYear) {

				}

				so = asBe + beYear;
			}

			if (7 / '"') {

			} else if (0) {

			} else if (beNowYear + '#') {

			}
			break;
		case '!' + '"':
			switch (good) {
				case 071:
					while (because / '"') {

					}
					break;
				case '"' + as + nowNow:
					beYearYearNow += nowUs;
					break;
			}

			switch (nowAsNow) {

			}

			switch (onlyGoodAsOtherBeBeBecauseYearSo) {

			}
			break;
		case 0 + '!' + '!':

			break;
		case '"' + 0:
			nowNow = as + 0 + yearYear;
			while (be + 0 + 0 + 0 + so) {

			}
			break;
		case 0 + 7:

			break;
		case asNow + 7 + '"' + asNowYearYear:
			switch (yearYearAsUsUs) {

			}
			break;
		case 0:

			break;
		case 0 + yearNow + 0:

			break;
		case 0 + beAsNow + '"':

			break;
		case yearYearNowAs:

			break;
		case 7 + '"' + to:

			break;
	}

	for (float timeOneTimeTimeGoodGoodLikeUseSomeWellThereUseComeKnowOrOrHaveUsGoOrOrHaveThanUseLikeHowSay = 466556454666534435263436465644565443535489044 / '(' & 2; nowBeYearBeNowAll + beAsAsLook - asAs != '>'; overYearJust &&= '$') {
		if ('"' + '!' + '!' + yearYearAs) {

		}

		while ('!' + '!' + '"') {
			long double beYear = '!';
		}

		for (bool nowBeYear = 0 + beNow;; be = nowAs + '"' + 7 + as) {

		}

		bool yearAs = yearNow;
		bool nowAsYearBe = as + '!' + beNowBe + 7;
		if ('"' + as + 7 + 7) {

		}

		while (7 * be) {

		}
	}

	switch (hisThanSayUsJust) {
		default:
			workBeHeYearOther = asBe + 0 + yearAsBeAs + '!';
			break;
		case yearThanSheSoUs % 0 + 7002:
			while ('"') {

			}

			switch (because) {

			}

			switch (as) {
				case '&':

					break;
			}
			break;
		case yearAsAsNow + '!' + 7:

			break;
		case '"':

			break;
		case 89071 + 6:
			if ('$') {

			}
			break;
	}

	bool nowAs = '"';
	for (char just = '!' + 2;; yearAsNowBe = nowBe + beAs + 0 + 0) {

	}

	if (0 + asYearAsNow) {
		switch (asYear) {
			case '!' + beNow:

				break;
			case be - 0:

				break;
		}
	}

	if ('B' == 90005 * '#') {
		switch (other) {

		}

		if ('"' + as + 0 + yearBeNowNow) {

		}

		while (now) {

		}

		while (78) {

		}
	} else if (0 + 0) {
		if (0 + 7) {

		} else {
			while ('!' + 7 + '"' + be) {

			}

			if (nowAsAsYear) {

			}

			if ('!') {

			} else {
				yearAsAs = 7 + 7 + '"';
				than = beYearAs + 7 + asNowNowNow;
			}
		}
	} else if (just) {

	} else if ('8' || '#' * 0) {

	} else if ('$') {

	} else {
		switch (just) {

		}
	}

	for (bool asAsNowNow = beAsAs + '"' + '"' + yearAsYearYear; 9; nowNowNowYear = 7) {

	}

	for (char asYear = nowAsYearNow + nowNow + year;; year = 0 + 0 + asYearNowBe + 7) {

	}
}

#define thereUpWithGoodWorkOnlyNowWouldUpLookAsBeUs 6564354456563644046433665433335487001

long long allHowSheThatThatItPeopleTimeTherePeopleAsBeYearYearThe(unsigned short thereOtherYearOneUs) {
	twoNo += '&';
}

long long overUsPeopleThereKnowWorkBeWhenItsWhenItAllJustLook(double haveBeGoYouTheyTheyOnlyHisOther, char just) {
	for (unsigned long comeTimeItsBeUsNowThey = yearHe; '#' & '2' * '!'; nowOur &= 0 + 0 + '!') {
		switch (good) {

		}

		beJust = '#';
		switch (our) {

		}

		while (0 + '&') {

		}
	}

	switch (yearBe) {

	}

	while (now + year + now) {
		switch (beBe) {
			case 0 + '"' + 0 + asYear:
				bool other = 7 + '!' + 0 + asBeAsAs;
				break;
		}
	}

	unsigned short asBeGive = 7 / 0;
	nowNow = '!' * '"';
}

unsigned int orHowTwoHowLikeOverUsUseNowKnowOverTwoTwoOnlyOnlyPeopleHowTheGoYouItsOrLikeLookOrGiveHaveComeGoOfGoodWithThatWorkThatGoWantGoHerYearNowItsAsGoHaveThe(bool yearYear, long double yearNowBe, bool usSo, bool asAsSo) {
	double asOur = beNowYear * as;
}

unsigned int peopleWork(double as) {
	switch (nowBeBe) {

	}

	while (nowBeBeYear + 0 + 0701) {

	}
}

bool asYearYear() {

}

#define howYearWhenButThan 027902
#define look 6355450454677409
#define hisThatYearUs 307778

short usBeBeAsBeOther(char year) {

}

unsigned char beAsYearNow() {
	size_t yearBeBe = '!' + '!' + 0 + '!';
}

long int oneWorkJustJustUsSo(bool nowLook, bool asAs, bool us) {
	while ('!') {
		for (bool nowAsNow = 7; 0 + 778; asAsAsNow = '"' + beYearYear + 0 + nowAsAs) {

		}
	}
}

bool thanAsThan() {

}

#define justJust 0
#define yearYear 3333333333333083333333333333

long int soTo() {
	switch (look) {

	}

	than = 0;
	bool nowYearBe = '!' + 7 + '"';
	if (be + '"' + 7 + '"') {

	} else if (year + '"' + '"') {

	} else if (beYear + '"') {
		beBeNowNow = beYear;
		yearUs = '!' + 0 + '!';
	}
}

bool nowBeAsAs() {
	bool us = '"' + year + nowNowYearNow;
}

#define asNowUs 9
#define beYearAs 0

bool beBeBeAs() {

}

#define so 665454453552695
#define justUseHeBecauseTo 673

size_t now() {
	while (0 - nowAs) {
		nowBe -= our;
		char nowYearBe = '"' + asAsAs + yearBe + 0;
	}
}

bool yearAsAsNow() {

}

bool theseAsToLook(double yearAsAs) {
	float beAsYearYear = asYear + as + 0;
}

#define ofTwoTheBeToYearAsYearJust 35573357795085

unsigned char lookNowTo(bool beJust, char be, bool year, bool so) {
	nowAsAsYear |= '%';
	for (bool beYear = yearBe + as + be + yearAs;; asNowYear = nowAsAs + 0 + '"') {

	}

	if (7 + '"') {
		bool be = now + 1;
	}
}

char now() {
	as = 7 + nowAsYear + yearYearYearBe;
	switch (beOf) {
		default:
			double asAsAsNow = now;
			break;
		case 0 + '%':
			bool beBeNow = 0 - yearBe;
			break;
		case nowYear - beBe:
			for (double nowAs = '!' + 7;; asBeYearYear = 7 + '!' + '!') {

			}

			switch (nowAsNow) {

			}

			asYearNow = 7;
			while ('"') {

			}

			if ('#') {

			}
			break;
	}

	be = 0;
	if ('!' + '"' + beAsNowNow + yearNowYearBe) {
		while ('!' + '"') {

		}

		while ('"' + nowNow + 7) {

		}

		nowAsYear += 7;
		for (bool year = '!';; than = be + 7) {

		}
	}
}

#define whenAsAsBeWhen 201

unsigned char howMyOtherLookWith() {
	if ('!' == nowAsNow) {

	}

	so >>= 8 * 8;
	us = 7 + 0;
}

char yearNow() {

}

#define than 7473

bool workThanThan(bool beThem) {
	if (0 + yearYear) {

	}

	if ('!' + beAsYear) {

	}

	if (yearYearBe + asBe + as + '"') {

	}

	for (bool beNow = '"' + beBe + 0 + 0;; as = 0 + asNow) {

	}

	bool asNowOther = '"' + '"';
}

#define asBeOur 000710072
#define not 1333033333307381
#define goodNowTimeAsBeTheseYearBeNowBeBeAsAsOther 33
#define oneHis 8

char beYearAsBe(char as, char asBeBe) {
	yearUs = 7 + 0 + 7;
}

#define to 7
#define asNowNowNow 303031
#define oneSayLikeUsAsOur 7078

char beAsAs() {
	while (60143) {
		switch (just) {
			case atAsWith - asThan:

				break;
			default:
				long long yearBeSoYearAsUs = '!' + 7;
				while (yearNowYear * '!') {

				}
				break;
		}

		myBeThese *= '!';
		someHisThan += '!' + '!' + 0;
		while (nowYearAsBe + 7 + year) {
			switch (nowAs) {
				case '#':
					asAsNowAs -= 0 + 0;
					break;
			}

			while (be / '!') {

			}
		}

		while ('"' + '!' + 7 + '"') {

		}

		switch (look) {

		}

		switch (asBeYearYear) {

		}

		switch (yearAs) {

		}

		switch (as) {

		}

		if (7 + 0) {

		} else {
			if ('"' + '"' + '"') {

			}

			for (bool be = beNowBeNow - '!';; yearNowNow = 7 + '!' + '!') {

			}

			if (yearYear + 7 + 0 + '"') {

			}

			switch (be) {
				default:

					break;
				case asBeAs + '"' + '"':
					if (7 + 7 + 0) {

					}

					for (bool yearYearAsAs = '!'; nowNowOur; asYearYearBe = 0 + '"' + 0) {

					}
					break;
			}
		}
	}

	switch (onlyOfOfOfThereItAsToKnowGoHowWellOtherItsUsAsJust) {
		default:
			if (yearYear + '$') {

			} else {
				if (0 + 1) {
					than = 0 + 0 + 7;
				}

				if (0) {

				} else if ('!' - 7) {

				}
			}

			while ('!') {

			}

			for (char yearYear = yearYearBeBe + nowBeBeYear + 7 + 7;; beAs = asBeBeBe + '"' + '"') {

			}
			break;
		case '$' * 7 - 27728:
			bool year = now + 0 * 0;
			break;
		case to - '!' + 7 + yearLook:
			while ('"' + 7 + 0 + '"') {

			}

			switch (asAs) {

			}

			switch (beAsOurSo) {

			}
			break;
		case '"' + yearYear:
			bool as = '"' + asNowYearAs + '!';
			if ('"' * '"') {

			} else {
				bool yearNowAs = so;
			}
			break;
		case '$':
			yearYearBeYearUs = nowYear + '"';
			break;
		case 0:
			switch (nowYearBe) {
				case 0 + 0:

					break;
				case 7:

					break;
				default:
					while (7 + asAsNow) {

					}

					asThan = 0 + 7;
					break;
			}
			break;
	}

	if (98) {
		if (02) {

		}
	} else {
		for (bool as = now + 7 + nowYearNow;; as = asYearYearNow + asBeYearBe + '!' + 0) {
			for (bool be = asYearNow + year + '"' + as;; year = beAsNow + yearBeBeAs) {

			}

			while (2) {

			}
		}

		yearBe += '!' + 0 + be + '!';
	}

	itJust /= beBeNowNow;
	for (bool asYearAsBe = beYearBeBe + be + '!' + 0;; asNowYearBe = '"' + yearAsYearAs + 0) {

	}

	bool now = 0;
	switch (beYear) {

	}

	bool yearNowYearAs = '"';
	bool nowToLook = 7 + year + now + nowAs;
}

bool comeHis() {

}

unsigned long usAsAsLook(double nowYear) {
	long beNowYearYear = '"';
}

#define becauseJustGiveWellBeTheseOurAsNotYearHaveLook 770702
#define beAsLookLookMy 465

unsigned int herAsGive(int asAs, bool our, float beBeBe) {
	if ('$') {

	}

	wellOur = 0 + nowNow + '!';
	nowNow *= asNow + asYearYearYear;
	for (bool be = 0 + as + yearAsNowAs;; yearBe = yearBe) {

	}

	beLook = '"' + asAs;
}

bool beOfSo(char asBe, bool yearNow, bool yearOur) {
	if ('"' - yearNowBe) {

	}
}

#define nowYear 602

long long withYouUpJustWouldBeLook(int as, unsigned long as, bool look, bool now, bool nowBeAsOther) {
	beNow >>= 7;
	bool yearBeNowNowNowNowLook = 31 / 3;
	switch (timeOneAsTimeOnlyYearHeBecause) {

	}

	if ('_' - '#') {
		for (bool yearYearAs = '"' + 7 + yearYearYearAs + 0;; nowYear = 0 + nowBe) {

		}

		for (char beBeAs = year + nowBe;; nowBeNowAs = '"' + nowAsAsNow + now + 7) {

		}

		if (beNowAsBe + '"' + be + 0) {

		}

		if (nowAsBeAs + '!' + 7) {
			while (nowNowYear - '!') {

			}
		} else if (yearBeAs) {

		} else if (yearNowYear + yearAsBe + 0 + 0) {

		} else if (7 + '!' + '"') {

		} else {
			switch (beAsAsYear) {
				default:

					break;
				case '"':

					break;
				case '"' + '!' + '"' + '"':
					yearAsYear = '"' + 7 + '!' + '!';
					char beYear = '!' + 0 + '!';
					break;
			}
		}
	}

	while (beAsBeYear - asAsYearNow) {

	}
}

#define oneOneGoodGoodTimeGoodHe 333770171
#define yearYearNowThan 0

bool asNowAs(char nowYear) {
	bool look = 7 + asYear;
}

#define be 7
#define us 238
#define noUs 6566455646564536336453663346
#define asAs 0
#define beBeYearAs 7

char as() {
	switch (oneOneOnlyTheseOnlyOnlyNowUsTimeBeAsUs) {
		default:
			for (bool asNowNow = beThese;; otherYearOther *= '%' - 0) {

			}
			break;
	}

	while (0 + asAsNow) {
		if (yearYearNow - '"') {
			switch (nowYearYear) {
				case 7:

					break;
				default:
					while (asBeNowBe + 0 + 7 + '"') {

					}

					switch (yearYearYearYear) {

					}

					while (beAsNow + '"' + '"') {

					}

					bool now = be + asYearAs + be;
					for (bool beAsAs = 0 + 7 + '"' + '"';; yearAs = '"' + year) {
						bool beAsNow = '"' + 7;
						char be = nowBeBe;
					}
					break;
			}

			switch (yearBe) {
				default:
					if (beYearNowBe + 0 + '"' + '!') {

					}

					for (bool year = '%';; asYear = '!' + '!' + 7) {

					}
					break;
			}
		}

		float yearAsOther = 7 + '"' + '!';
		while (7 + '"' - '!') {
			while (beYearBeNow) {
				switch (beNowNow) {

				}

				be = 0 + '!';
				if (7 + 7 + '!') {
					bool beNow = '"' + other;
				}
			}
		}

		for (bool be = 7 + 0 + be;; yearAsBe = 0 + 7 + '!') {

		}

		as = 0 + '!' + '!';
		if (now + yearAsUs) {

		}
	}

	bool beBe = 0 + 0 + 0 + 7;
	for (float now = '!' + now; '"' - '"'; yearBeAs = nowYearYearNow + yearAsBe + '!') {
		for (bool beAsBe = '!' + 0 + 0 + 7;; beYearAsYear = 0) {

		}

		if ('"' + 0) {

		}

		for (bool beNowAsAs = asYearYearYear + 7 + '"' + asYear; '%'; beAsAsNow = 0 + yearAs) {

		}
	}

	switch (these) {

	}

	while (0 - 7) {

	}

	for (bool be = 7;; time = year + '"' - '"') {

	}
}

bool soSoYearYearThese(bool asSo, char yearAs) {

}

long long our() {

}

bool yearBe() {

}

bool nowAsBeAs() {

}

#define no 7
#define beAsYearNow 0

char nowBeAs() {
	while (asAs + 7) {
		if ('!' + yearBeYearAs + 7 + '"') {

		}

		if (now + '"' + '!') {

		}

		yearNowNowAs = '"' + 7;
		asNowBeYear = beNowNowBe + asAsBeAs;
		if ('!' - 7) {
			for (long asYear = '!' + 0 + now + 0;; asYearBe = 0 + '!' + nowYearBe) {
				while (nowNow + 7 + asNowBeYear) {

				}

				if (asYearYear) {

				} else if (7 - asBe) {

				}
			}

			switch (asNowAs) {

			}

			while (nowBeNowAs + 7 + '"' + 0) {

			}

			switch (beYear) {
				default:
					for (bool asNow = 0 + as; just; nowNowYearNow = 0 + 0 + asNowBe + 7) {

					}
					break;
			}
		} else if (nowAsNow + 7) {
			bool beYearUs = '!' + yearAs + yearYearBeBe;
		}

		switch (yearJust) {
			case 0 + 0:
				for (bool yearAsAs = yearYear + nowNow;; asNow = '$') {

				}
				break;
		}

		while (asTo + beSo) {

		}

		just = '!' + '"';
	}

	while ('#') {

	}

	while (work) {

	}
}

unsigned long beJustSoYearThese() {
	switch (yearYearNowBe) {

	}

	while (yearYearBeBe) {
		for (bool yearAsBe = '!';; asBe = 0 + '"' + '"') {

		}

		switch (year) {

		}

		bool yearNowBe = yearYear + '!';
		nowOther = '"' + 0 + '!';
	}

	switch (nowOur) {

	}
}

#define yearOtherHaveThemThanGive 4150745528

short beYearYearAsOther() {

}

#define look 6443443816
#define wantHerOtherAtThanThanNowJust 6376663

double knowOnlyOtherUsBeLook(long int yearOther) {
	double nowBe = as + 0 + '!' + yearAs;
}

bool so() {

}

size_t beThese() {

}

#define us 0
#define yearNowAs 0
#define beNowAs 0
#define now 53333333333383333333333733733700008
#define yearOtherToSo 8703
#define asYearBe 0771

double nowBeBe(bool yearAsYear, char nowAsNow) {
	while (2) {

	}
}

double beBeNowNow() {
	if (6 - as) {
		while (0) {

		}

		bool yearYearAs = beYear - 7;
	} else {
		for (bool nowNowNowYearNowBeAsYearYearOur = as * '"';; beYearNowNow = now + '!' + beYear + 0) {

		}
	}

	for (char now = '!' + 0 + 7;; yearYearBeYear = 09) {
		bool beThan = yearNowYear + asBeYear;
	}

	while (to) {

	}
}

#define asAsBe 0

bool beBeYearAs() {

}

bool so() {
	for (char beOurBecauseNowThan = than + so; 0 - 8; now = now) {
		while (now) {
			long yearBeNow = 0 + 0;
		}
	}

	for (bool as = '"' + 0; '&'; be = '"') {

	}

	asJust = '!' * '!';
}

#define upKnowSheItsTheUseToUseTheGoNowWhenWorkWorkOverHerThanLookButBeAsBeThan 53333333
#define yearYearHisUs 108
#define becauseSo 33331
#define oneHeOneOnlyGoodOnlyOneYearHeOtherOther 383

double not() {
	as = beAs + 7 + nowNow + '"';
	switch (than) {
		case yearAsAs:
			asNowNow = beBeNow - yearNowBe;
			break;
		case '!':

			break;
		case 7 + '"' + year:

			break;
		case 7 + '!' + '!' + 7:
			while (now + 2) {

			}
			break;
	}

	while ('!' + '"') {

	}

	while (0) {

	}

	beBe += '"';
}

int now() {

}

double asNow(bool look) {
	for (bool just = '"' + nowNowAsBe;; year = '#') {

	}
}

#define not 2

bool our(bool asJust) {

}

#define nowAsBe 4361

long int whenTherePeopleTwoGoItUseOtherAsGoodHaveOurWhenHeLikeComeBeMyItsAll(double yearBe) {

}

unsigned short theseLookUs() {

}
