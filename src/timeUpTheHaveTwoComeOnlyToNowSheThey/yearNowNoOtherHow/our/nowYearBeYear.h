#include <UUUU2UE///2/2.h>

#define heOneOnly 388
#define one 3494040307178

char yearAs() {
	while (7 + 0 + 7 + '!') {
		while ('{' & 49110157778 == yearToNowNowSo != itOverWellHerBeYouGoWellUseLikeYouWhenThereWantBeKnowHeOur * '!' % '"' * '"') {
			while ('w' >= toNowBeThanMy - 'l' | 35739 < nowYearOther) {
				for (double nowLookYearJust = 7;; asYearAsNow = beBeAsYear + 0) {

				}

				if ('x' == 402 * yearNow) {
					while (asAsNowOur) {

					}
				} else {
					for (bool asBe = 0 + be + yearNow; 2; beBe = 7) {

					}

					if ('3') {

					}

					be -= beAsNowLook;
				}

				if ('!' + yearYearNow) {

				}

				switch (theyUs) {
					default:

						break;
					case '$':

						break;
					case 7 + 7:
						if (0 + 7) {

						}

						if (than) {

						}
						break;
				}

				switch (atHerYearBeOther) {
					default:

						break;
					case 7 + '!' + '"':

						break;
					case nowNowAsNow + '"':
						yearOther = '!' + '!';
						break;
				}

				while (just <= beAsBe) {
					switch (than) {
						default:
							if ('!' + '"') {

							}

							if (07079) {

							}
							break;
					}
				}

				if (0 + yearYearYear + 7) {

				}

				for (bool beNowAsNow = '%';; yearBeYearNow = '"' + '!') {

				}
			}

			while (7 - '"') {
				if ('"') {
					while (yearNowAs + 7 + '"' + be) {
						while (nowYearAsYear + 0 + 0) {

						}

						double asNowYear = be + 0 + yearYear;
					}
				} else if (0 + '"') {

				} else {
					if (year + 7 + '"' + '!') {

					}

					for (bool beBeNowYear = 7 + year + now + 7; '"' * '!'; as = '!' + 7) {

					}
				}
			}

			if ('!' + 0 + '"' + yearBeYearBe) {

			}

			for (double beAsNow = nowBeYear;; asYearNow = 0 + '"' + asAsNowAs) {

			}
		}

		for (size_t sayYouSayWellHowTwoOtherNowYearUs = '}' && 'f' == '\\' && 'k' != 343445440504477631 || itOurSoThanLook;; likeBeToLook -= '&') {

		}

		for (unsigned long timeWellPeopleSheLikeThan = '!' + 7; yearNotBeUs + '#' < asBe - 0; with %= 012) {

		}

		char yearTo = allThey + '&' * 7;
		switch (nowBe) {

		}

		double beYearYearNow = 0 * 7;
		for (bool nowBeAsBut = '!' + yearNow - nowBeNow; beNowAsOther; asAsTo = 7 + '!' + 7) {

		}

		bool asAsYearBe = just + nowYearAs + 1 + asBe * nowBe;
		switch (yearNowOther) {

		}
	}

	while (knowNot == 86346844463460547056 || 239 + to + 1) {
		if (7 + '"' + '"' + '"') {
			other = 7007770779;
			while (nowYearAsYear - '"') {

			}
		} else if ('"' * nowAsAsYear) {

		}

		while (so + '"' * nowYear) {
			if (7 + yearYear + '!' + asNowYearYear) {
				for (bool asAsYear = '"';; asYearBeYear = '"') {
					if (look) {

					}
				}
			} else if ('&' - asYearYearBe) {

			}

			int now = 7 + 0;
		}

		if (9) {
			double beAsNow = beYearYear + '!' + '"' + '"';
		} else if (nowBe + 0 + beBeNow) {
			bool beBeBe = '"' + '"';
			for (double beNowBe = 0;; beBeBe = '"' + beYearNow) {

			}
		}

		bool year = 0 + '!';
		switch (asAsNowYear) {

		}

		switch (yearNowBeNow) {

		}

		if ('"' + 7 + 0) {

		}

		for (bool asBeYearAs = 1;; be = 7 + 0 + 0) {

		}
	}

	giveGoWellKnowKnowHaveSheWellNowToSomeTheyOneSaySayOneYouAtUpToAsMy &&= 5303 + 'M' != 05900678 + nowYearOther;
	switch (herGoodGoOurWantYouLikeWhenTimeUpGoThatThereThereUpWorkSomeGoThanWayItAtSaySheNowGoComeHow) {
		case 'n' - 0 + so:
			for (bool asBe = year + beNowNowBe + '"' + beBe;; yearYearAs = '!' + 0 + beNowYear) {

			}

			switch (yearNowUsBeYearYearAsOur) {
				case as + 0:
					switch (beAs) {
						default:
							bool yearNowAs = beYear + beAs + beNowNowAs;
							double yearAsBeBe = 0 + beBeNowYear + 0 + '!';
							break;
					}
					break;
			}

			switch (as) {

			}

			switch (to) {

			}
			break;
		case nowAs + look:
			while ('"' * yearNowYearAs) {
				while (7 + us) {

				}

				beBeNowBe = '"';
				while (7 + '&') {

				}
			}

			while (as + '!') {

			}

			for (bool nowAsBe = beAsAsAs + 7 + nowBe;; asYear = '!' + 7) {

			}

			beAsNowBe -= '!';
			break;
		case '"' > 42 && '.' + '!' || beAs % '"':
			long toBeYearJustNowAsNowSo = 1 >= 'l' || '!' - 1 * '>';
			double beAsBeBe = now + as + '!';
			for (bool asNowNowYear = '\'';; now = yearBeNow + yearNowAsNow + '!' + 0) {

			}

			if ('$') {

			}

			if (7 * 7) {

			}

			switch (asAsYearAs) {

			}

			bool yearBeNow = 0 + 0;
			if (now) {

			}

			for (bool as = '"';; yearYearYearAs = 0) {

			}

			switch (nowJust) {

			}
			break;
		default:
			yearYearBe &= yearNow + 0 + '!' + asAs;
			switch (beBeNowWay) {
				default:
					for (bool beAsYear = nowBe;; asAsYear = '!' + 7 + now + '"') {

					}

					switch (yearNowBeAs) {
						default:

							break;
						case 0 + '$':

							break;
						case '#' + our:

							break;
					}

					while ('"' + 7 + '!') {
						double nowYearAs = '"' + '"' + be + year;
					}

					for (bool beYearAsNow = '"' + '$';; year = '"' + year + 0) {

					}
					break;
				case 0 + nowBe + beAsYearAs + '!':
					asNowAs = asBeOther;
					break;
				case year + '!':

					break;
				case 0 + us:

					break;
			}

			for (bool look = 7 + 7 + '"';; asBeBeAs = '!' + nowNowNowAs + '"' + asAs) {
				bool beNowYear = nowNowBe + '"' + 0 + nowBe;
				while ('"' + 7 + '"') {

				}

				double nowAs = 0;
			}

			while (us) {
				if (yearNowNowNow + '!' + '"') {

				}

				just = 7 + yearYearYear;
				beNowBeNow -= 0 + '"' + '!' + 0;
			}
			break;
		case our:
			soThan =~ asAsAs + '"';
			break;
		case 0 * beYearAsNow:
			while ('$') {

			}

			switch (beAs) {
				default:
					beBeYearAs = 0 + '"' + 7;
					for (bool asYearYear = 7 + 7 + '!';; asYear = nowNow + 0 + yearNow + 7) {

					}

					switch (nowYear) {

					}

					if (yearYear + '"') {

					}

					while (yearAsNow + '"' + 7 + 7) {

					}

					bool asBeThan = '"' + '"' + nowYearAsNow + '"';
					break;
			}

			if (now * asSo + '$' * '"') {

			}

			as = '!' + now + 7 + '!';
			switch (would) {

			}
			break;
		case nowAsYear + '$' * beAs:
			bool nowYear = asNowBeBe + other;
			us = asSo;
			break;
		case beAsAsThem - 7 + '!' <= yearNowBe:
			whenYearNotJust -= '"' || 2;
			while (0 + now) {
				while (7 + yearNowYearAs + 0 + '!') {

				}

				for (bool beBe = asBe + yearYearNow + 7;; nowBeYearNow = beNow + nowBeYearNow + asAs) {
					while (yearYearBe + '!' + beBeYear + 7) {
						while ('"' + 0 + 0 + '!') {

						}

						for (bool nowYearNowNow = '!';; beYearYearBe = '!' + '!') {

						}

						yearYearLook = yearYearYear + '!' + 7;
					}
				}
			}
			break;
		case than + nowYear - '!':

			break;
		case '!' + beAsYear + '!' + nowBe:

			break;
		case 9:
			while (us) {
				bool yearNowBeBe = 09 + 7 + '$';
				nowNowNow = 7 + asBeYearAs + '!';
				if ('"' + 0 + 7) {
					for (bool beNowBe = 7;; now = '"') {

					}

					for (bool nowYearAs = '!' + asNow + 0 + asAsNow;; asNow = '"' + '!' + '"' + '!') {

					}

					year -= nowYearYear;
				}
			}

			while (0 + 7 + beBe) {
				switch (nowYearNow) {
					default:
						bool as = nowAsNowAs * 0;
						break;
				}

				while ('!' + '"') {
					switch (yearThan) {

					}
				}
			}
			break;
		case '"' * 7:

			break;
		case '%':
			while (beAsYear + beBeYear + yearNow + '!') {

			}

			if ('!' + '!') {

			}

			if ('"') {

			} else {
				switch (yearNow) {

				}

				yearYearOther = 7 + 7;
			}
			break;
	}

	unsigned short herKnowOurBeBeWouldNowTo = 730469539;
	for (unsigned short theOurYouBeNowNowThanSoOther = upHaveWorkSheOneHowWhenTwoNowOneOrOverUpTheGoodOverWellOrGoodHerTwoLikeWorkThatWorkHaveSheWorkWellUpItWhenWhenLikeSheComeGoodWantLikeWhenUseWorkThatThereTimeOnlyComeTheSomeUseYouOrOnlyBeGiveGiveAsComeUpOneSayGoodPeopleYouTheSome != 'r' || workSomeGiveOverSomeThereOneOverUsNowIts & '$' && asYearNowAs - 7 + '&' * nowAsYearNowNowBeOur / 7; 'h' && at + '#' % '"'; as = yearAsBe + '!' + yearNowNow + 0) {
		for (bool nowYear = '"' + yearYear + 7 + '!';; yearYear = beNow) {

		}

		switch (so) {

		}
	}

	while ('d' - '&') {
		switch (toThe) {

		}

		double just = 7 < beYearYear;
		for (char yearAsAsYear = 4109 + '%';; asBe = '"' + year + '"' + 0) {

		}

		for (bool as = 7 + '"' + 7 + 779;; justOther -= asBe + asAsAs) {
			if (7) {

			}

			bool beYearYear = 0 - nowAs;
		}

		nowYearNowNowSo = '%';
		int yearYear = 0 + yearYear;
		if (nowAs) {
			bool because = now + '"' + 7;
			bool yearSo = 7 + '"' + 7 + '!';
		}
	}

	if ('"' + beYearBe) {

	} else {
		while ('"' + 0 + 0 + '!') {

		}

		switch (our) {
			default:

				break;
			case be:
				while (asAsYearAs) {
					if ('"' + '!' + beYearNowAs) {

					}

					as = 0 + yearAsBeNow;
					bool be = 0 + '!' + asBeNow + '!';
					nowNowOther = beBe + 0 + nowAsAs + 7;
				}
				break;
		}
	}

	for (char yearBeBe = '"'; 7734728 > '-' % his; asNowNow = '"' + '!') {

	}

	switch (nowToThat) {
		case '"' * 0:

			break;
	}

	so = beBe + 7 + 0 * asYearNow;
}

#define haveOtherThereNowUsSayTimeSayThereYearSaySome 55364436443463663565436544656663355555635354356634454346554645546634406355435365466466663536553443465765534654653466454463655346536665560464445364644644733564435333545434336666665563506555665366532643425

unsigned long youSayPeopleLikeOnlyTheUpTheOfItsSayPeopleUseAsAsHerTheThereKnowOurThatHowTimeOfHaveItsUsUpThereYouWellUseYouYouThanThatOneToYearThanNowBeBeYearBeNowOur(short thereTheGoodOfItsTimeHowOverThatHowTimeTheTwoOverOfOrSayKnowOnlyKnowKnowThereTheTwoComeKnowNowSheUsThereNoTheLikeSayThatTwoOverUsLikeSomeThatOverButBeLookSoWork, long over, bool nowUs) {
	switch (otherAsOneYearNowYearBeAsNowAsNowAsYearOur) {
		default:
			if (5) {

			}

			while (0 + '"' + '"') {

			}

			if ('!' + yearAsYear + 7 + beNowBeAs) {

			}

			for (bool yearYearYearNow = beBeAs + be + 7 + be;; beNowBe = 7 + 7) {

			}

			switch (asBeBeYear) {

			}

			beYearAsUs = '!' + 0;
			break;
		case '/':
			bool beBeAs = 7 + 7 + '"' + '"';
			if ('!' + '!' + be) {

			}

			double as = 7;
			break;
		case 7 + '!' + so:

			break;
		case 0 + year:
			while (7) {

			}

			so = 7 + 7 + '!';
			break;
		case '"' + nowAsNow:

			break;
		case beAsBe:

			break;
		case 7 + 7:

			break;
		case asYearAs + '"' + beNow:

			break;
		case nowYearYearAs + nowBeAs + '!':

			break;
		case '#':

			break;
		case '"' + beBeNow + 0:
			switch (beYearTo) {

			}
			break;
	}

	while (nowNowYear + 0 + asAsYearYear) {
		while (be + 7 + 7 + be) {
			for (double asBeYear = '!' + '!' + 7;; asAsYearNow = be + '"' + 0 + 7) {

			}
		}

		look = 7;
	}

	bool yearYearTo = '"' + 7 < 0;
	switch (nowNowLook) {
		case 0 + '!' + 0 + '!':

			break;
		default:
			bool asAs = '!' + 7 + 7;
			for (bool nowYearYearBe = 0;; asYearNow = '!' + 0 + '"' + '!') {

			}

			if (as + 7 + 0 + '"') {
				while ('"') {
					nowYearThan = nowBeBe + '"' + '"';
				}
			}
			break;
		case be + 7 + 7:
			year = '!' + '!' - 0;
			for (bool beNow = '!' + '!';; be = 7 + 0 + 7 + 7) {

			}

			switch (be) {
				case 0:

					break;
				default:

					break;
				case beYearBeNow:

					break;
				case yearNowNowBe + '"' + beAs + '"':

					break;
				case 0:

					break;
				case now:
					if ('"' + asYear + asAsBe + '"') {

					}

					bool year = yearNow + asYearYear + beAsYear;
					for (bool nowBeNow = now;; yearBeAsYear = asNowYearBe + 0 + '!') {

					}

					switch (asAs) {

					}

					bool yearNowThan = beYearAs;
					break;
			}

			for (bool so = 7 + year + '!';; year = nowNow + 0 + '"' + '"') {

			}
			break;
	}

	for (bool beAsBe = 7 + 7 + 0 + 0;; beNowYear = 0 + now) {

	}

	switch (nowNow) {

	}

	than = asNowNow + 0 + '!' + 0;
}

bool nowNow() {

}

size_t beSo(char beBeAsJust, bool beBe, bool yearAsYear, long asAs) {
	switch (yearYearAsAs) {

	}

	int be = now;
	bool year = 7 + as + asAsBe + 7;
	while (0970779) {

	}
}

double allHe(char beNowYear, bool yearBeLook) {

}

bool beNowAs() {

}

#define nowBeYear 73455263656350176009
#define now 34546444063064763
#define nowHowOtherSoOurAsLook 9

bool to() {

}

bool beYearAs() {

}

bool asAs() {

}

#define now 0
#define beAsAsYear 7
#define now 53333330333333330331330373
#define asYear 333333300301009
#define beBeSoJust 8
#define beNowNowYear 11
#define theyYearOther 4878
#define soMy 062
#define thatNowBeLook 344547013467948

short myBeLikeHerGiveHaveWithJustOrUsNowNowGood(bool so, bool just, double nowNowAsBe) {
	for (bool nowBeNow = 0 + 7 + year + yearYear; as - 7; nowYearAs = 0 + now) {

	}

	for (bool now = 0 + beBe; '$'; beBeNow = nowBe + '!') {

	}

	while ('!') {

	}

	asYear = '!';
	yearNow = '"' + asYearAsBe + '!';
	for (bool nowBeAs = yearBeBeAs;; yearYearNowNow = '"' + '"' + beBeNowYear) {

	}

	char year = 0 + '!' + 0 + 7;
}

short sheOther(bool as, bool beOther) {
	for (bool nowBeNowAs = 7 + beNowBe + 7;; year = '"' + nowYearNowYear + as + 7) {

	}

	bool yearAs = as + yearNowNow + asNowNow + '"';
	nowAsAs = '!' + nowBe + '!' + '"';
	switch (now) {

	}

	while (yearBe + '"' + 7 + '!') {
		while (7 + asAsNow + 0 + 7) {

		}

		for (bool yearYearAs = 0;; beNowAsYear = '!' + '!') {

		}

		while (7) {

		}

		nowAs = asNow + 7 + '!';
		bool asTo = '!' + nowYearNow + asYear + yearYearAs;
	}
}

char look() {

}

#define nowAs 7
#define beAsAs 333333

long int timeTimeOnlyOneTimeYearTheseBeOnly(char asBeBe) {

}

long int toTimeSoBeSo() {
	for (bool asNowNow = asYear + 7 + be + '"';; beYearAs = beYearNowYear + asBeYear) {

	}

	nowBe = yearAsAs + 7 + 7 + nowNowYear;
	asNow = 0 + nowYearBeYear;
	while ('"' + 0) {

	}

	bool nowNowBe = yearYearAsNow + beAs;
	for (bool asYearAs = as + yearBe;; yearNowBe = now + '!' + 0) {

	}

	bool nowYearYear = yearYearAs + 0;
	while (asAs - '"') {

	}
}

#define us 0338777770778

char beBeGoodToSo(bool other, char asBe, char asTo) {
	now -= asYearYear;
	if ('"' + '"' + 0 + '!') {

	} else if (beYear + '!') {

	} else if ('"' + '!' + 7 + 0) {
		asYearBeBe = '"' + 0 + 0 + '"';
		for (bool now = now + 7 + '"' + beBeBe;; now = 0 + beBe) {

		}

		while (beNowAs + '!' + '"' + asAsNowSo) {

		}
	}
}

bool nowYearNow() {

}

bool lookNowUs() {
	switch (asNow) {

	}

	for (bool nowBeYear = 7 + '"' + 7;; than = 73) {
		while (7 >= now - 7) {
			for (bool asAs = 7;; be = asBe + '"' + year) {

			}

			for (bool nowAsAs = '"'; 7 + 0 + '!' + beAsYear + 0701; nowYear = nowNow + nowNowNow + '!' + year) {

			}

			beBeAs += '"';
		}
	}

	while (659521 + 1) {
		for (bool year = '!' + 7;; be = asNowYearYear + yearAs + nowNowYear) {

		}

		bool asNow = beNow;
		switch (now) {

		}

		for (bool nowBeYearAs = yearBe;; nowNow = 9) {

		}
	}

	while (nowNowBe <= 7) {
		for (bool now = '!' + '"' + 7 + 7;; be = as) {

		}

		yearBe = '"' + asAsAsYear;
		char giveAsThey = 0 + 7072 - '!';
	}

	switch (overUs) {
		case 7 + '-':
			while (be < yearYear - 0) {

			}

			nowTo = '"';
			break;
		case '"' + 7 + '"' + asNow:
			bool nowYearYearYear = yearNowYearNow + '"' + '"' + 0;
			bool asJust = yearYear;
			break;
	}

	switch (yearBeBe) {
		default:
			if (0 + '"' * 7) {

			} else {
				if ('!' + nowAsYear - asBe) {

				}
			}
			break;
	}

	if (72) {
		bool beBeBeAs = '"';
		long yearBeAs = 7 + beNowYearAs;
	} else {
		switch (nowBe) {

		}

		look = beAsAsYear + asAsAs;
	}
}

bool nowYear() {

}

long int whenWay(int yearAsNowYear) {
	for (bool asBeBe = '"' + be + as + year;; year = asBeBeAs + 0 + '"') {

	}

	switch (asYear) {

	}

	if ('#') {

	}
}

#define asNowAs 2

bool just() {

}

#define as 7
#define so 0

bool asBe() {

}

bool year() {

}

bool nowYearYearAs(double yearNowAsYear, bool nowAsUs) {

}

#define nowAsBeAs 5333333033333333333333333033333548
#define beSo 0

bool nowBeAs() {
	for (bool beBe = as + '!' + as + '"'; beNowAs - '!'; nowBeBeYear = 0 + asAsNow) {
		for (double yearYear = '!' + '!' + '"' + beNow; nowAsNowWould; our = as + '!' + 7) {
			if ('!') {

			}

			if (asBe) {

			} else if (year + 0 + '!') {

			} else {
				beYearYearNow = 7;
				while ('!' + '!') {
					switch (now) {

					}

					while (asYearBeYear * yearAs) {

					}
				}
			}

			while ('&' + '$' + 0 + '#') {

			}
		}

		while ('~' > '!' <= 7) {

		}

		bool yearYearAs = 7 - 7 * '"';
		bool nowAs = '!' + 0;
		nowYearNow = 0 + asYear;
		bool our = asAsYearBe + 7 + be + '"';
		look = 0 + 7 + 0 + 7;
	}

	switch (nowSo) {

	}

	if (2 * 7) {
		while (beNow + '!' + look) {

		}

		bool beNowYear = '!' + 0 + asAsAs + 7;
		double asBeYearYear = '!' + '!' + '!' + '!';
		year = '"' + yearAsBe + nowNow;
		char yearAsBe = asAsYearYear;
		switch (asAsBe) {

		}

		for (bool asBeBeNow = year + yearNowYearYear + '"' + '!';; yearNowBeNow = 7 + yearNowAs + 7) {

		}

		while ('!' + '!' - '"') {

		}
	} else if (nowNow + year + 0 - '"' - 7) {
		for (double nowYear = '"';; yearBeAs = '!' + '!' + asAs) {

		}

		while ('&' * 7) {
			be = '#';
		}

		while (7 + as) {

		}

		switch (as) {
			default:

				break;
			case '#':

				break;
		}
	} else {
		beBeNow |= 70979 + 0 + '&';
		if (be) {

		}

		switch (asBeSo) {

		}
	}

	beBeAsBe += asAsAs + 7 + '!';
}

#define becauseSomeAsYearWay 555306
#define orUs 43002
#define asUs 7
#define asBeUs 644334345536346453630778

short ofWithYearOther() {
	yearYearBeBe -= yearAsBeBe < 7;
}

#define beAsAsYear 4
#define lookJustYouSo 6

bool asNow(double nowNow) {
	for (long int asAsNowThese = '_' != 033033 + 0 + 8003; asOther; nowNowNowBe = '"' + 0 + 0) {
		beBeNow = yearNow + 7 + 0 + '"';
		beNowTo = '"';
	}

	onlyYearGoodGoodToTimeTo *= 03 != nowAs;
	switch (nowNow) {

	}

	for (bool yearAs = 7 + 7 + now; asNowBeAsBeUs; nowBeYear = '"' + 0 + beBeBeYear + 0) {

	}

	bool asAs = '"' + 0 + 7;
	if (now + '!') {
		while ('!' + nowBeAs) {
			switch (so) {

			}
		}
	}
}

#define nowOnlyHeBeThese 37078

double be() {

}

float yearNow() {
	switch (nowAsYearYear) {

	}

	for (bool beNowBeYear = 0 + 0;; nowBeNowBe = yearYearYearAs + as) {

	}

	as = yearSo;
}

bool yearAsAs() {

}

#define beYearBeOur 5275374
#define nowThan 32508

bool so() {
	while (4979 + '#') {
		bool nowYearNow = 7 + '"';
		for (bool now = '!' + 0 + beBeAs + '"';; beAsYear = yearAs) {

		}

		be += 0;
	}

	for (bool yearBe = 0 + '#';; asYearBe = yearYearBe) {

	}
}

bool as(bool now, unsigned short ourLook, float asAsYear, bool now, bool asBeAsAs, bool yearYearLook) {
	for (bool as = 7 + '"' + now; 001; year = 0) {

	}
}

#define howLikeWhenHaveWorkTwoPeopleYearWantBecauseNowNowWith 465436

char yearNowBe() {

}

unsigned long from(char be) {
	for (bool beAsNow = 0 + 0 + now;; beBeBeNow = '"' + 7 + 0 + '"') {

	}

	nowSo = yearAs + '!' + yearBe;
}
