#include <EUU6U6UUUU/E0UR/15.h>
#include </0.h>

#define now 7

char nowNowAs() {
	for (size_t youTheLookOfAsOneWorkLikeTheOurMy = would; 1 + '!' - '!'; yearBeBe += '"') {
		than = '!';
	}

	switch (goNoOneHavePeopleTo) {
		case asAs + '!':

			break;
		default:
			while ('!' + nowBeAs) {

			}

			bool beNowBeNow = 78;
			break;
		case '"':

			break;
		case beBe + '!' + asYearAs:
			bool nowLook = 7;
			while ('!' + '!' + beYear) {

			}

			while (0 + 0 + nowNow) {

			}

			for (bool yearNowBeBe = '"';; now -= 7 + '"') {

			}
			break;
	}

	thanWellAsOurSo ||= 0 * toLook % 7;
	switch (nowAsNowTo) {
		case 0 + 0 + be:

			break;
		default:

			break;
		case '"' + nowAsNow + '"':

			break;
		case '!' * '"':

			break;
	}

	if (74) {

	} else {
		our += '"' + 7 + beNowNow + '"';
	}

	be = '!';
	switch (asHis) {

	}

	while ('$') {

	}
}

long int comeThanSayWantThereItsSheWorkOverThereTheUseItsKnowSheWellPeopleOneTimeHowTimeItsGoodItsNowWantPeopleThereTwoLookOnlyItMyItOfHerTheTheMyBeAsYearBeOur(short overOrComeHerWellItsGoWellTimeGoodMyUseItHaveTimeLikeOnlyWantOtherUpItWorkComeHowPeopleGoodSomeHaveSheGiveSayWantOrWayMyNowLookThanAsNowBeBeOur, long long yearThey, bool comeWantTwoKnowLikeKnowOverUpWellAtTwoSayItItItUseKnowLookYearBeBecause, size_t thereUseWhenYearNowAllOrAsThey, bool know, int orHerOneUseWhenOneHowUseItWayBeJust, double beNowYearNow) {
	for (bool heAsNowBeBeYearYearAsAsThan = 7 + '"' + '"' + '"'; 7 + so; nowAsBeYear = '!' + beNow) {

	}

	if ('%') {
		while ('$') {

		}

		for (bool nowBe = '"' + 0 + yearBeYearAs + 0;; nowNowNow = yearAsNow + yearAsBeBe) {

		}

		switch (yearJust) {

		}
	} else {
		if (1 && '!') {

		}

		for (char yearAs = asAsThan - '!';; beAsYearBe = '!' + '!' + '"') {
			than = '!' + 7;
		}

		if (knowOur * '"') {

		}

		yearYearBeJust &= ourAt;
	}

	nowNowBe -= 0 - '!';
	if ('"') {

	}

	switch (asYear) {

	}

	be = beBeBeNow + '"';
	while (7) {
		if ('"' + beAs + yearNow + '"') {

		}

		while (0 + nowNowNowAs + '!' + '!') {
			switch (yearBeAs) {

			}

			for (bool as = '"'; 0 * beBe; asNowYearBe = asNowAs + asBe + 0 + 7) {

			}
		}
	}

	while ('%') {

	}
}

#define nowAsAsNowUsLook 10977771

long double nowAsBeAs(bool asBeNowBe, char yearNow) {
	bool way = asYearYear + '!';
	while ('!' + '"') {
		switch (our) {

		}
	}

	while (7 + '"' + yearUs * 0) {

	}

	nowLookBeLook -= 0 + '"';
	while ('"' + '"' + '!') {

	}

	bool nowBeBeBe = yearBeLook;
}

unsigned int youNowYouBeToJust(int beHis, bool knowMyBeAsAsBeJust, double usYearBecause, bool beAs, bool so) {
	char use = asBe + asBeThan;
	if (yearSo) {

	}

	for (bool yearAs = asNow + asYearBe + 7 + 0;; now = yearAs) {

	}

	if (nowAsBeBe + '%') {

	}
}

long to() {

}

#define yearYearAsNow 5345431
#define to 0

bool as() {

}

#define now 4

char he() {
	while (now + '"' + '!' + '!') {
		char yearNowBe = yearAsYearBe - '"';
		bool nowAs = '"' - 0;
	}

	switch (to) {

	}

	switch (yearYear) {
		default:

			break;
		case 7:
			bool be = yearYear + 0 + asNowAs;
			if (7770701) {

			}
			break;
		case '!':
			switch (be) {

			}

			for (bool yearNowBeBe = beBeBeAs; '!' - 7; beNowYearBe = be + '!' + 7 + '"') {

			}
			break;
	}

	while (0 + 0 + as + '!') {
		while ('!' + beYearBe) {
			for (bool beNowNowNow = '!' + beNowNow + 0 + 7;; now = '"' + 0 + now + 0) {

			}

			for (bool to = yearAsNow + '"';; nowBeNowAs = 7 + 7 + 0) {

			}
		}
	}

	beBeUs = '!' + 0 + nowAs;
}

char now(bool so) {

}

#define onlyYearYearBeOther 3333333333333833708

char to() {

}

#define onlyYearYearSo 407600729

bool yearAsYearNow() {

}

char thatWith() {
	while ('!' + be + '"' + as) {
		ourBeAsNowTo = 7 + '#';
		switch (yearOtherBeBeUs) {
			default:
				switch (nowNowNow) {

				}

				switch (other) {

				}
				break;
			case 7 + '"':
				for (bool yearAsYearBe = '!' + 7; '"' + to; asBeAsNow = 0) {

				}
				break;
			case '"' - be:
				if (asLook) {

				}

				switch (nowNowYear) {

				}

				yearBeYear = 7 + '"' + '!';
				switch (asBeNow) {

				}

				if (yearYearBeAsYearNowTo) {

				}
				break;
		}

		yearSo = nowBeAs + as + yearAsBeBe;
		double as = 0;
		while ('!' + '"' + 0) {
			bool be = '"' + 0 + '"';
			for (bool beBeYearYear = '!' + asYear; '$'; be = 7 + beBe) {

			}
		}
	}

	if ('u' | 'v' | haveGiveGiveAllHaveOfHaveNotGoodPeopleGoodOneOverThatHerNotSoSayOnlyTimeUpGoWorkTheOnlyHerGoodOver || 3754553594381 - ';') {

	} else if (oneBecauseTheAsYearUs | 79 == 102) {

	} else {
		if (be + '!' + nowYear) {

		}

		for (bool nowNow = be + asNow + beAsNow + 7;; now = 7 + 0 + 0) {
			bool beNowNowYear = '!' + 0 + '!' + as;
			nowHis = nowAs + '"';
		}
	}

	if (1) {
		if (be + yearAsAs) {

		}

		for (bool yearNowAs = 0 + 0; '#' * '!'; so = '"') {

		}

		double asYearBe = 0 + year;
		onlyUpSo |= beThatPeopleThese % yearYearNowJust;
	}

	for (bool be = '"'; ']' | than; now = yearNow + '!') {

	}

	bool year = nowBeBe + asYearYearBe + '$';
}

long long orYouHowSheThatHerGoTimeSomeOnlyPeopleLikeOrOnlyHaveOneGoodGiveOrOverSheSayGoOnlyUseTwoSomeHaveKnowWellWellKnowYouItHaveOverOfTwoTimeItComeOrUseHowOrTimeLikeItsUseTimeWhenSheComePeopleOtherSoGiveThatToTimePeopleItAsWorkAsGiveOnlySomeTimeOtherNot(long long twoTwoTheHaveHaveGoodHowGoodOneYouSayHowOnlyLikeComeHerOfTimeComeOnlyOneLikeKnowToWorkSheWellLikeThatLookGoSayHisTimeWorkSheSomeYouItTimeTheTheGoodOnlyWhenBeLikeShe, unsigned char likeGoodAsOnlyPeopleSheThatUpItTheItsWantOrTwoOnlyUpPeopleNowThereOrOnlyTherePeopleUseUsComeWellToKnowKnow, unsigned char comeHerTheLikeUseNowNowSoJustAsLook, unsigned char twoHerItsGoOneTimeWorkHerHerTimeWithSoWellUsTheLook, long int giveThemItsWorkBeHaveNot, unsigned int thatBeWantComeSomeOtherYearComeHowWayBeNowUs) {
	double nowNowBe = 7 + yearYearAsAs + 7;
	if ('"' + '!' + as) {

	} else {
		for (bool yearBeNow = 7 + 0;; asNowNow += yearBeNowBe + '"' + yearYearAsNow + nowNowNowNow) {

		}

		while ('!') {
			switch (yearTo) {
				default:
					switch (asYear) {
						default:

							break;
						case '"' + '"':

							break;
						case '!' + 7:

							break;
						case nowAsBeAs:
							for (bool nowAsBeNow = beYearYearNow;; yearAs = nowNowYearNow) {

							}

							bool than = now + nowAsYear + 0 + 0;
							break;
					}

					nowNowNow = 0 + nowBeBe;
					asBeNowBe = '!' + 0;
					so = '"' + '"';
					break;
			}
		}

		bool now = yearAsYear * yearBeNow;
	}

	asNowYear = '%';
	long usYearLook = '"' + nowYear + 0;
	if (0 + 7) {

	}

	switch (beLook) {

	}
}

unsigned long (unsigned int , char giveGiveOurUseGoodBeHaveOverMy, long long peopleOnlyTimeUseThemNowSomeUpThereTimeWhenHowGoodGoGiveOnlyUpOverHaveItComeHaveTwoBeSomeOfOrSayUseOfOfWorkSheWouldThemTo, unsigned long ourHaveItsKnowHaveWellThanOtherOtherAll, bool knowWithAsYearOtherOurNowUs, double wouldLook, bool asAsLook) {
	if ('a' | 3335654665545444434433455333343645344444147675593633453654409 >= 645334345524 != 5479 + so - beNowYear) {
		if (0) {
			if (yearYear * '"') {

			}
		}

		while (7 + 7) {
			switch (yearYearYearNow) {

			}

			for (double year = 7;; asBe = '"') {

			}
		}

		long beBeNowBe = '!';
		switch (beAsOther) {
			case '!' + '!' + '!' + 7:
				if (asBeAsBe) {

				}

				for (bool asBeBeBe = yearNowNow;; nowAs = nowYear + 0 + 0) {

				}

				bool yearAsBeBe = '"' + '"' + '"';
				while (beAsAsNow + '"' + '"') {

				}

				for (bool yearNow = '"';; beAsNowNow = 0 + 7) {

				}

				if (0 + '"' + '"' + '"') {

				}

				if (year) {

				}

				for (bool beOther = '!';; as = '!') {

				}
				break;
		}
	} else if ('"') {
		if ('%') {

		}
	} else {
		while (peopleYearThan * 7) {

		}

		for (int asBeBe = 7 + '#';; yearYearBe = nowAsNowBe + '"' + 0) {
			if (beYearYearYear - 7) {

			}
		}

		switch (as) {
			case beNowAs + 7 + nowYearNowBe + nowBe:
				while (yearYearBe + '"' + '!') {
					for (bool asBe = nowNowYearBe + asAs + now; 7 - '!'; asAsYear = yearAsNow) {

					}
				}
				break;
			case 0 + '"' + asNowYear:

				break;
			default:
				for (bool nowBe = '"' + 0 + asAs + nowBeBeBe;; nowAsNow = '!' + beAsAs) {

				}

				while ('"' + nowBe + nowBe + '"') {

				}

				for (bool nowNow = 0 + now + '!'; 0 * '!'; beAsBe = 7 + 7 + yearAsYear) {

				}

				while ('!' + to) {

				}
				break;
		}

		while ('!' + he) {

		}
	}

	beAsYearYear /= 931;
}

#define other 494193

long long giveSayAtThatWhenTime(bool justNowYearUs, unsigned char goPeopleHaveOfSomeUseThereSomeGoodUseAsYearBeLook, short sheWould, double asHis, bool nowNow, long asUs) {
	beAsThanYearNot = '-';
	int just = 'H' + asUs * 0;
	if (nowYearLookThey + 88) {

	}

	if ('!' + asNowNow + 7 + be) {

	} else {
		while ('$') {
			while (beJust) {

			}

			now = '!' + '!' + 7;
			while ('"' + as + 7) {
				asBeAsAs = '!';
				beAsNowSo = 7 + 7 + '!' + '"';
			}
		}
	}

	while ('!') {
		no = beNow + 7 + year;
		switch (us) {

		}
	}

	for (bool yearBeAs = '!' + nowBeNowYear + 0;; yearBeAs += '!' + 7) {

	}

	switch (beBeSo) {

	}
}

short thatSoThatGiveKnowItsWorkSheOrHowYearWouldSo(unsigned long onlyWorkGoodWellSomeTimeOneOnlyUpWayItsAtNowYearYearNowThan, bool twoTheyHe, unsigned char sayItNoWayGoodNowNowOther, long int likeYearJustOurThan, bool know, double year, bool nowYear, bool nowAsNowNow, double yearBeNow) {
	if ('"') {
		bool nowNowAsSo = '!';
	} else {
		for (bool year = '!' + yearNowYearNow + '"';; be = '!' + 0 + 7 + '"') {
			now = 7;
			asYearNow = 9;
		}

		switch (yearBeYearNow) {
			case 7:

				break;
			default:
				switch (beYearNowYear) {
					default:
						bool year = as + 0 + be;
						while ('!' + nowAs + '!') {
							switch (nowAs) {
								case '"' + asBeBe:

									break;
								case nowYear + '!':

									break;
								case 7 + nowNow + '"' + 0:
									if (so) {

									}
									break;
							}
						}
						break;
				}
				break;
		}
	}

	switch (yearNowNowOther) {

	}

	if (',' - 7) {

	}
}

#define herGiveItOrKnowTheOfWorkHerSomeHerWorkOfHerLookNowLookBeTheyTo 3356666545634456543643644366654046545553666363553487647554616649778
#define haveYearThanOther 8

bool look() {

}
