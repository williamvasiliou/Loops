#define timeBecauseHe 337031331

long int timeOnlyTimeOnlyGoodOnlyTimeOnlyOneOnlyOneButBecauseHaveOurOur() {
	wellPeopleComeGoOrSaySomeGoodOfOnlyTheOneUpOverPeopleTwoYouOnlySomeHaveWellNowSayWellHerOrPeopleKnowThatHowItYouOverItsToWhenSomeOnlyPeopleUseBecauseGiveOverWithTheOrAsNowNowThan =~ yearYear & wellComeAsNowNotHe % '"';
	switch (her) {
		case beYearBe + '!' + '!':

			break;
		case yearYear + '!':

			break;
		default:

			break;
		case 7 + '"':
			if ('!' + nowYearNow) {
				char yearNowYearNow = 0;
			}
			break;
	}

	while (butComeYearJust == just) {
		at = would;
		bool other = asAsYear;
		switch (year) {
			case 7 + '"' + nowNow + '"':
				while (7 + '"') {
					bool nowYear = 0 + yearYear + '!' + beAs;
					if (asNowAs) {

					}

					asBeYearYear = '"' + 0 + asYear;
					char as = nowNowBeYear;
				}
				break;
		}

		for (bool year = nowYear + '"' + 0;; as += yearNow + '!') {

		}
	}

	long asAsYear = beBeBe + '!' + 0 + asYear;
	for (bool our = beNow + '"' + beYear + '!'; '%' + '"' + yearJust; year = beYearAsBe + '"' + 7 + beNowYearNow) {

	}

	if (now + '"') {

	}

	haveJust = '"' + 7 + 7 + 0;
	while (7 - '!') {
		if (asNowBe + 0 + 0 + as) {

		}

		for (bool nowAsYear = 7 + '"' + 7;; be = yearBeAs + '!' + '!') {

		}

		now = be;
		if (7 + 0) {
			for (bool now = '!' + '"' + 0 + '"';; asNow = beBeBeNow) {
				bool nowAs = '#';
			}
		}
	}
}

unsigned long hisItsYearNowNowOther(unsigned char justUpOneWorkJustNowLook, size_t oneKnowSheUpBeUseThatWellUseTwoSheSomeKnowUpAsUs, unsigned short otherThemJustAsNowWith, unsigned short heYearYearYearYearYearYearFrom) {
	twoHaveTimeGiveComeTwoGoodTheHerNotAt = 0 + 0;
}

unsigned short nowYearYearNow(long int as, bool be, bool work, bool beAsBeNow, bool be, bool now, bool yearSo, bool nowAt) {
	if (now + 7 + beBe) {

	} else if ('"' + asYearNowAs + '"') {
		nowYear = yearAsBeNow;
		for (char beYearAsAs = '"';; nowNowAsAs = year + nowYearAs) {

		}
	} else {
		if (nowNow + 7 + 7) {

		}

		for (bool be = '"' + '"' + now + 7;; yearNowAsYear = as) {

		}

		while (7 + be + '!' + asAsBeYear) {
			if ('#') {

			}
		}
	}

	double useComeYouHowFrom = to + '!' - 0;
	unsigned long upHaveMy = 'O' || other - beAsJust;
	if ('k' + 0 * '!') {
		for (unsigned char workPeopleLikeAtThan = '"' + 7;; nowBe = '"') {

		}

		switch (theyBeOur) {
			case year + nowJust:

				break;
		}

		if ('"' + nowAsAs + '"') {

		} else if (0) {

		} else if ('"' + nowNow + '!' + beAsNowNow) {

		} else if (asNow * 0) {
			for (bool asYear = 0;; asNowYear = '!' + asBeNowYear + 9) {

			}
		}
	} else {
		switch (yearAsNowAsYearTo) {
			default:
				if (asYear) {

				}

				yearYearYear -= 0 + nowBeAs;
				break;
		}

		for (bool just = '"' + nowBeYear + 7 + 7;; nowAsBeNow = 0 + '!' + '!' + as) {

		}
	}

	for (float thanTo = 7 + '"';; now = '"' + 0) {

	}
}

unsigned long asYear(bool asAt) {

}

bool nowNowNowBe(bool beAsAs, bool year, bool be, bool beBeBe, bool nowTo) {
	switch (nowAsNowYear) {
		default:
			if (be + 0) {

			}

			as *= '!' + 0;
			break;
	}
}

#define nowTheNowNowSo 25607877772

char nowJust() {

}

long double be() {

}

#define year 7

unsigned long beYearAs(bool be, double beYearNow) {

}

bool nowYearThan() {

}

#define now 3636455463663635345554654449004

char someUs(bool asJust, bool yearYearJust) {
	bool beYearNowNow = 7 + '"';
	for (bool asYearAsYear = now + 0 + 7 + 0;; be = '"' + 0 + '"' + 7) {

	}

	if ('"' + be + '"' + be) {

	}

	for (bool asYear = '"' + 0;; nowNow = '"' + 0 + now) {

	}

	if (asBeYear) {

	}

	asYearOther = 3313 / 37001 != usNowAsUs;
	otherGoodBecauseYearSoBeOther = 0 + '#';
	if (7 + 7 + 7 + '!') {

	}

	while ('"') {
		if ('!' + 7) {

		} else {
			switch (be) {

			}

			nowAsNow = 7 + '!' + beBeYear + '!';
			yearNow = '!';
			while (now + 7) {

			}

			switch (asBeNow) {

			}

			now = '!' + '"' + yearAsBe + '"';
			char asAs = yearNowAsNow + as + 0 + yearAsYear;
		}
	}

	nowBeTo *= '"' - '"';
	bool other = 0 + 7 + 7;
	for (bool yearNow = '!' + '"' + '"'; as + '%' - 0; nowAsNowAs = 0) {

	}

	if ('"' + 0 + '"' + '!') {
		bool beBeAsAs = asYear;
		switch (beAsNow) {

		}

		bool beYear = beNowBe + '!' + now + nowAsBe;
		for (bool as = 0 + 7 + 0 + asBe;; year = yearYear + '"') {

		}

		if ('%') {

		}
	}

	while (now + 7 + 0) {

	}

	for (bool asYear = 7;; yearBeYear = nowNowNowYear) {

	}

	for (char beNow = '!';; year = '!' + beAsNowYear + '!') {

	}
}

bool beTo() {

}

long int his(bool timeOnlyOnlyToNowNowAsBeTo, long int yearAsTo, bool as, bool asYearBe, int asNowAs) {
	to = 0 + year + 0 + 7;
}

int nowOur() {

}

#define asBeYearYear 7
#define nowBeAsYear 4604356534932445345747072

char year() {

}

bool yearNowYear() {

}

bool yearYearNowNow() {
	for (bool nowBeBeAs = '!' + '!' + 7;; beAsNowAs += 0 + 0) {
		for (unsigned int beYearUp = 3 * 0; to; nowNowAs -= 2) {

		}

		unsigned int itAsKnowSayOurHowNowWork = 7 + '(';
		if (7) {

		} else {
			if ('!' + 7 + '!' + 0) {

			}

			for (bool asBeNow = 0 + '!' + yearAsNowBe;; asBeAsAs = 0 + '!' - year) {

			}
		}

		for (float now = 0 + '!' + '!';; nowAsAs = 7 + as + 7 + '"') {

		}

		bool now = '"';
		for (bool beAsBe = year + now + '"' + as;; asAsBeBe = 7 + 0 + '"' + nowBeNowBe) {
			switch (yearYearAsYear) {

			}

			while (0 + '!') {

			}

			yearNowBe = asNowBe + yearNowAs + be + '!';
			beBeNow = 7 + '"' + '"';
			switch (nowYearNow) {
				case nowBeYearNow + 0 + '"' + 0:
					for (bool as = '"' + nowYearAs + '"';; nowAsYear = '"' + asYear) {

					}

					if (beYear * 7) {

					}
					break;
			}
		}
	}

	while (nowBe + 0 + as) {

	}

	bool as = 771;
	yearYearYear = toAsSo - 0;
}

long int giveOfBeAsSomeUsYearLook(char as) {
	switch (asBeLook) {
		case 0 + '%':

			break;
	}

	for (bool yearNowLook = '!' - 7;; beYear += '!' + '"' + 7 + year) {

	}
}

double asBeAsNow() {

}

#define beYearAs 1

char be() {
	while ('?' != 'O' + '"' - beBeAsYear) {
		switch (good) {

		}
	}

	goodYearBecause = '"' + 7;
	switch (soSoAsNowNowSo) {
		case as + nowAs:

			break;
		default:

			break;
		case '%':

			break;
		case 0 + '!' + '!' + asNow:

			break;
		case 7 - '!':

			break;
	}
}

#define asToOnlyAsHeHeGoodAsAsTo 3333333333

bool yearBeUs() {

}

#define than 520001

double beBeBeNow() {
	toLikeYearYearThese >>= thatOurNoBut != yearMy + ',' >= '&';
	for (bool year = yearNow + '!' + 7;; as = 7 + 7 + asBeAsAs + '"') {

	}

	if (7 % 72 - beAsBe) {
		while (7 + '!' + '!') {
			double now = be + '!' + '!';
		}

		while (0) {

		}

		if ('"' + nowAs + beYearBeAs + '"') {

		}

		while (709) {

		}
	}

	while (asBeAsYear + year) {

	}

	long long year = 5778 + 3;
}

#define yearYearYearAs 7

bool herAsThan(bool yearJust) {

}

char asNowAsNow(char year, bool as, char nowBe) {
	be = yearAsNow + beYearBeAs + '"';
	bool year = asUs;
}

double nowAs() {

}

#define yearAs 7645633493364402730540740075

short youNotBeSo(bool yearJust) {
	so -= '#' + asAs + '&' - 7;
}

unsigned int be(bool now, bool yearYearBe, bool yearAs, bool nowBe, bool beOther) {
	while ('"' + yearYearNow) {

	}

	bool asAsNowBeOur = so / 0;
	if (0 + yearYearYearAs + '!') {

	} else {
		while (7) {

		}

		while ('"' + nowYearNow) {

		}

		nowAsBe = 7 + beAsYear + yearYearAsYear;
		for (char yearBeAsNow = '!' + be + '!';; nowAsBeYear = '"') {

		}
	}

	while ('!' * beNowNowNow) {

	}
}

bool allOther() {

}

#define beAs 7

double be() {
	char beNowAsBe = as;
	theseTo |= '%';
	if (beYearAsAs - 7) {
		nowBeBe *= asNow + 7 + 0;
	}

	if ('"' + '!') {
		beBe = 7;
		bool nowBe = 7 - 7;
	}
}

#define us 7
#define because 333383333333330078
#define beOtherBecauseOtherBeTo 7

bool beAsNowNow() {

}

long int other(long int us, bool asNow, bool yearAsBeAs, bool beYear, bool our) {

}

bool yearNowYear() {

}

#define yearHisYearBeAsThanAsBeAsYearYearBeOur 7902

double now() {
	for (unsigned long wellThereWhenThatSaySheGoOverOnlyGoGiveGiveOurOther = '#' && '!' + ourBeLook; itToUseOfThemThemBeUsUs && 3433774 || yearAsYear < '2' < '$'; beThan ^= '$' % '%') {

	}

	while (328579 - '"') {
		just += yearYearNow * yearNow;
		switch (asYearNowBe) {
			default:

				break;
			case yearAsBeAs + 0 + nowBeBe:

				break;
			case 7:

				break;
			case yearAsBeAs + '"' + '"' + '"':

				break;
			case beBeBeAs + asBe + nowNow:

				break;
			case 7:
				for (bool yearNow = '"' + '!' + '!' + '"';; as = '"' + 0 + '"') {

				}

				while (7 + '"' + '!') {

				}

				char yearAsYear = '"' + 0 + 7 + 7;
				break;
		}
	}

	while (our) {

	}

	switch (than) {
		case 009 + '!' - '"':

			break;
	}
}

#define yearAsBeYear 64467936562344
#define howOurAsOur 6455
#define wantAsYearNowYearJust 056
#define nowAsYear 079

char asAs() {

}

bool other() {
	if (yearBeNow * '!' * 7) {
		if (7 + asAsYear + beAsYear + '"') {
			while ('!' + 09) {

			}
		}

		if ('!' + beBeAsYear) {

		}

		for (bool asNowAs = 0 + 0 + 7 + 0;; asNowAs = '"') {

		}

		while ('"') {
			for (bool nowBe = '!' + asAs + 0 + '!'; 71; year = 0) {

			}
		}

		if (7 + 0 + 7) {
			asNow = 0 + 1;
		}
	} else if (7 + '"' + asAsBeAs + '!') {
		to = yearYear + be + nowAsNowAs + '"';
	} else {
		bool herNowTo = '!' + '"';
		for (bool now = 7 + 0 + asBeBeBe + '!';; asAsBe = 0 + yearBe + 0 + '"') {

		}

		switch (year) {
			default:

				break;
			case '"' + '"' + '!':
				if (beAsNowAs + '"' + 7 + 7) {

				}

				if (0 + '"' + 0 + nowNow) {

				}

				if (other) {

				}
				break;
		}
	}

	int whenToUseNowBeThan = '7' <= '"';
	if ('"' + 0 + '!' + '"') {
		for (bool yearAsNowNow = be + 7 + 0;; yearBe = 0 + 0) {

		}

		thanNowBeLook = 0;
	} else {
		if ('%') {

		}
	}

	nowOur -= 0;
}

long int yearMyJust(bool yearBe, char he) {
	if (ourJustThey) {

	} else {
		long yearNowAs = year + yearNow;
	}

	while (7 + nowBe + beBeNow + nowBeYear) {
		switch (asBeNow) {
			default:
				for (char beBeAsNow = asAs + 7 + yearBe + nowAsYear;; asNowYearYear = 7) {

				}

				bool nowAsBeBe = '!';
				look = beBeAs + 7 + 7;
				break;
		}
	}

	char nowLook = look * nowAs;
	yearAs = '"' + '!' + 0 + nowYearBeNow;
	switch (knowGoodBeThan) {

	}
}

long long beYear(char nowAsNowYear) {
	yearYearNowAs = '%' != '7' + 18;
	bool yearAsBe = 8 + so;
	bool toHis = '!' / 7;
}

long int onlyUsTimeOnlyOnlyBeGoodAsToTimeOneTimeNowOneAsOther(long int yearBeYearTo, bool to, bool nowYearAs, bool yearNowYearBeSo, bool nowAsAsNow, bool yearBeNow, char asYearAs) {
	switch (as) {
		case '!' * yearBeYearYear:

			break;
		case '!':

			break;
		case '"' + 0:

			break;
		case beYearAs + '"' + beNowNowBe:
			while (0) {

			}

			asAsNowAs = be + '!' + nowBeAs;
			nowLook = 0;
			break;
	}

	for (long double nowNow = '"' + nowThan; '!' * nowNowNow; yearYearNowBe = beNowBeBe) {
		while (now + '!' + yearNowAsBe + asNow) {
			switch (look) {

			}

			char year = nowYearNow + yearNowBe;
		}

		while (nowNow + 7) {

		}

		if ('"' + asBeBe + '!' + nowNowAsYear) {
			if ('!') {

			} else if ('"') {

			} else {
				bool nowNow = '"';
				switch (now) {

				}

				bool nowAsBeAs = yearBeBe + now + beYear;
				if (yearYearNow - 7) {

				}

				if (nowBeNowAs + yearAsThan) {

				}
			}
		}

		if (likeThan * nowAsNowNow) {
			while ('.' + '"' + be * '!') {

			}

			while (beAsAsAs) {
				while ('!') {

				}

				nowYearAs = 0 + '!' + '"';
				switch (yearNow) {

				}

				while (yearYearAs) {
					switch (asYear) {

					}

					switch (as) {
						default:
							switch (yearYearYearUs) {

							}
							break;
					}
				}
			}

			if ('"' + 0) {
				switch (as) {

				}

				bool as = '"';
				other = 0;
			}
		} else {
			bool other = nowBecauseAsThey | '\'';
			switch (beWould) {
				case with:
					nowNowNow = yearBe + '"' + '"' + beYearYear;
					bool nowYearBe = 0;
					switch (just) {

					}
					break;
				case '"' * 7:

					break;
			}

			switch (now) {
				case yearLook:

					break;
			}

			double year = yearAs + '"' + 0;
		}

		switch (beYearLook) {

		}

		if ('!' - 7) {

		}

		nowNowYear = 7;
		as = '"' - '"';
	}

	switch (twoUs) {
		case beNowNowAs + as:
			switch (asAsBe) {

			}

			switch (nowYear) {
				case 7 + than:

					break;
			}
			break;
		case 0 - '"':
			if (nowAsNowBe + 0) {

			}

			for (bool be = '"'; 72; yearAs += 0) {

			}

			while (0 + asYearYear + nowNowBeBe + 0) {
				for (bool beBe = '"' + 0;; yearAsNow = beNow) {

				}

				for (bool beBe = '!' + 7 + nowNowYearYear + 7; now * yearNow; yearBeNow = asYearNowBe + '!') {

				}
			}

			if (asAs + 0 + yearAsYearNow + '"') {
				while ('&') {

				}
			} else {
				double beNowAsBe = '!' + '"' + 0 + '"';
			}

			while (7 + 0) {
				while ('"') {

				}

				bool year = 7 + 0 + 7 + 7;
				nowBeBe = 7 + 7;
				switch (yearNowAsAs) {

				}

				for (char as = nowBeBe + 0 + 7;; beYearAsNow = be + 7 + be) {

				}
			}

			yearYearAsAsTo = as + yearYear;
			if (0 + '"' + yearBe + 7) {

			}

			if (nowAs + 0 + 0) {

			}

			while ('!' + '!' + year) {
				switch (beBeAsNow) {

				}

				switch (asUs) {

				}
			}
			break;
	}

	while ('m' & yearHerYearHeNowNowUs >= 'w' || 978 > 0) {
		peopleGiveItsOnlyPeopleYouThatGoSayPeopleYouOverThatItThanAsJust |= 'F' < 'w' < 0;
		long long knowAtLook = '!' + 0;
		char beBeNow = '#';
		while ('Y') {
			for (double asYearNowNow = '&';; nowAsBe = 7 + yearAsBe + '!') {

			}

			switch (be) {
				default:
					nowBeNowNow = 7 + '"' + 0 + 7;
					if (year + yearAsYear) {

					}

					for (char yearAsBe = '"' + '!' + 0;; nowYearBe = asYearAsBe + 0) {

					}
					break;
			}
		}

		if (7 + '"' + asBe) {
			switch (asNowNow) {

			}

			bool nowNowBe = 0;
			switch (yearNowBe) {

			}

			now = beAsYearNow + asBe + '"';
			while ('!') {

			}

			for (char be = 0 + '"' + be;; as = '"') {

			}

			while (7 - yearYearBeYear) {

			}
		} else {
			for (char now = be + be + 7 + 0;; asNowAs += 0 + '"' + 7) {
				switch (so) {

				}
			}
		}

		just = beNowYear + '!';
	}

	if (0 + '!' - '*') {
		char otherThan = year < asAs;
		asYearNowAs = year + be;
		while (71) {

		}
	} else if (1) {
		beAsBeYear += 7 + 78;
	} else {
		bool nowBe = yearBe + as + asAsYear + yearNowAs;
		for (bool yearYear = asSo - yearNow;; asNow = 0 + 0) {

		}
	}

	unsigned short haveWantGoodUseItThatBeTheHerKnowOverOnlyThereWellWantOnlyYearTwoHerThanBeFrom = nowNowBe / 7;
	switch (howOtherWellSaySomeThan) {
		case giveNowNowToItsAsWith + '"' * beNow:
			bool yearBeAs = '"' + 7 + '!' + year;
			for (char nowAsAsYear = otherAsUs;; as = 0 + 7 + '!' + asNow) {

			}
			break;
		case soWith * '!':
			if (asAsNow + so) {

			}

			while (nowSo) {
				while (0) {

				}

				long double beNow = 7;
			}

			switch (to) {

			}

			bool nowAsBe = '!' + asYear + 7 + 0;
			just = '!';
			break;
		case 5 - '$':

			break;
		case 7 + 0 + 7:

			break;
		default:
			bool be = nowYearYear + '!';
			for (bool nowAsYearYear = year;; nowYear = '"') {

			}

			if ('"' + 7 + '!' + 7) {

			}

			for (bool now = 0 + '"' + '!' + 0;; nowThan = '!' + '!' + '!') {

			}
			break;
		case 0 + asYearYear + '!':

			break;
		case nowYear + 7 + '!' + '"':

			break;
		case beNowNow + '!' + '"':

			break;
		case 7 + yearBeBe + '"':

			break;
		case 7 + 0:

			break;
		case asAsYear + 7:

			break;
		case asYear:
			for (bool yearBeBe = 0 + asNowBe + '!' + beNowBe;; yearYearNow = '"' * '"') {

			}
			break;
	}

	if ('%' + 0 + 8) {

	}

	for (unsigned short other = 0;; beYearNow = 0) {
		switch (year) {

		}

		switch (asBe) {

		}

		bool as = '"' + 0 + '"' + '!';
		if (asYear + '!' + 7 + '!') {

		}

		bool yearYear = 1;
	}
}

#define timeThereWantUpSheHavePeopleWhenPeopleOverTimeOverOverWhenTimeOnlyThanUpWellOnlyOneSomeThatOnlyThereAsOnlyKnowUseJustGiveKnowWantGiveNowItsOnlyOverOfUpWhenKnowLikeYearWantOfItGiveHerThat 353444356463565654653544345433494451443201071

bool yearNow() {

}

char be(bool asYearNowBe, bool nowAsYear, double yearAsBe) {

}

#define herYearYearUs 29
#define now 7

bool asAsAs() {

}

#define as 6354563335468656376446300608

unsigned int asToThan() {

}

#define yearNowNow 65078
#define than 54
#define nowBeYearBe 0

char asAsNowNow() {
	for (bool be = '"' + 0 + 7 + 7;; nowYearAsAs = 0 + 0 + beYear + beYearYear) {

	}

	while ('"') {

	}

	asYear = 7;
	switch (to) {

	}
}

bool yearAsNowNow() {

}

char beNowAs() {

}

double yearAs() {

}
