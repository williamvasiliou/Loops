#define timeNowTimeHisOnlyHis 3831331

long int time(bool nowAs, bool beBeAsAs, bool nowBeUs) {
	bool beBeYear = 7;
	for (bool beNowYearYear = 7 - '!';; nowNowYear = '!' + '"') {

	}
}

long int to(long int other, char nowNow) {
	long int goodOtherNowUsOther = asNowBe != 7072;
	for (char now = asAsNowBe * 7;; beYear = '"' + '"') {

	}

	asBeNow += 7 + year;
	double asNowAs = 0 + yearAsYearYear;
	be += 7 + 7 + '"' + 0;
}

#define yearNowYearBe 0

unsigned long nowAsBeNow() {
	switch (now) {

	}

	for (bool asYearAs = '"' + '!' + yearYear;; nowNowBe = 0 + as + asAs + 7) {

	}

	bool yearBeBeBe = 0 + 7 + nowAs + yearBe;
	for (bool nowYearNowNow = nowAs;; beBeBe = yearBeAs) {

	}

	while ('!' + '!' + '!') {

	}

	beYear = 0 + '!';
	asOther = beYearNowYear + asAs + yearBeBe + '"';
}

#define asAsNowYear 7
#define year 0

bool now() {

}

double yearBe() {
	if (95676544553 >= 544362 + '\'' + 20703) {
		char overNowFromComeUsOtherOtherWhenAll = beWorkHeBeThan / '!';
		for (bool asNowYear = beBeBeBe + nowAsBeAs;; year = 7) {

		}

		unsigned int be = 7 * '"';
		while ('"') {

		}

		for (bool yearAsBe = 7 + yearNowYear;; nowBeBe = '"' + 7 + '"') {

		}

		switch (yearAsYear) {

		}

		beBe = asNowYearNow + nowYearAsYear + as + '"' + 7 - beAsAs;
	} else if ('!' + year + '!' + '!') {

	} else {
		unsigned char yearOurAsBeTo = 'u' / 594 - 2;
		long int nowAsNowYearBut = '"' < 7;
		while ('"' + 7 + '"') {
			just = 0 + '"' + 0 + '"';
		}

		switch (beNowYearNow) {

		}

		double asBeAsBe = 7 + '!' + 0;
		for (bool be = '!'; '\''; beNowAs = 7) {

		}

		switch (be) {

		}

		switch (beAs) {

		}

		while ('"') {

		}

		while (1) {

		}
	}

	 <<= '1' | '+' == useAsLookYearThan * nowNowNowYear >= 47449024 < as;
	for (long long goodTheWantSheWhenTheySheWithWhenGoodTheseSomeYearPeopleThan = 54346465654536453665354420342 - 77778 * '!'; '_' || knowThereWellOrAsTherePeopleButOneUseItsGoJust & 'd' & workWouldThereTwoSheOnePeopleHowPeopleWorkOneNowOnlySoOurThanLookBeThan >= 085 <= 9; upPeopleItYearOfAsItsSoAsWouldBeNowSo <<= '&' + look) {
		if (heHowBeAsNowAsYearSo != '!' * nowBeYearYear) {
			ourUs .= itItNowComeToGoodOur / asYearNow;
			if (justOtherYearTime + '!' * '!') {
				while (7002 + '"' - '"') {
					if (year + 0 + 0) {

					} else {
						switch (asJust) {

						}
					}
				}

				while (7 * nowYear) {

				}
			} else {
				for (double look = '!' + '"' + be;; beBeYearNow = 7) {

				}
			}

			if (78 - '!' * yearAsNow + '+') {

			}

			while ('!' + 7) {
				for (bool beBe = 0 + '"';; as = '!' + '"') {

				}

				switch (so) {

				}
			}

			nowBeAs -= 0 + 7 + nowNowYearBe;
		} else if ('!' * '&' + 8) {
			unsigned int overFromBeThey = now * 7;
			int nowBeAsAs = '!' + '!' + 0;
			for (long double nowAs = 7 + 0 + asYearAs;; beNowYear = '"') {

			}
		} else if ('$' > asYearToSoNowTo + 9) {
			switch (asBeNow) {
				default:

					break;
				case 0 + 7 + '!':

					break;
				case nowBeYear + 0:

					break;
				case 0 + asAsAsYear + yearBeNow:

					break;
				case now + 7 + be + be:

					break;
				case look:

					break;
			}

			bool beYearThan = 0 + nowYearNowYear + 7 + 0;
			bool other = '!' + 7;
			now = 7 + nowNowAs + as + '$';
			switch (beLook) {

			}
		} else {
			other = '!' + '!' + '"' + as;
			while (7 + '!' + '"') {
				switch (us) {

				}
			}

			char asNow = nowBe - soUs;
			for (bool than = 7 + beNowYearNow;; yearNowNowYear = 0 + '!') {

			}
		}

		switch (now) {
			case '"' + yearNowBe:

				break;
			default:

				break;
			case '#':

				break;
		}

		yearBeAsBe = 58;
		double yearNow = 7 + beBeNowNow + 7 + '"';
	}

	wantThereTwoOnlyOverTimeItTimeHowWantItsThanOneWorkGiveOurAsLook &&= onlySayHaveWellKnowGoodBeItsThereWhenOverNowSayOnlyBeToWayLookYearYearOther || '{' + 550345275633453363456170001 && 33366361536344246645644536244 >= ofWhenYearYouJustYearUs + 'g';
	while ('c' != itsOverThatWellUpThemUsOfWorkItsNowPeopleThemNowAsUs * orLikeOneOfYouOneGiveSheComeHowSayNowSoBecauseNot / 3985 >= beNowAs) {
		for (unsigned long wantGoodTwoItGoodSheWithBeSo = itUseSoItsSomeGoodOfHaveGiveThanOverOneComeLookOfItWouldGiveGoodToNowYearThemHisWould & 67719; fromAsAsBecause | howGoodHerWorkOrNotBeHowThemLookNowOur + 0 + nowAsYear * nowYearNowAs; nowMyBeNowOther = than - 0) {
			long beBeNowOther = as + nowNow + 7 + '"';
			for (bool as = '!'; 9228; beNowBeYear = asOther) {
				for (bool asBe = as + 0; 79; asYearBe = as + 7 + 0 + nowYearYearYear) {

				}

				while (0 + 7) {
					while (7) {
						switch (beNow) {

						}

						while (beBeAs + '!' + '"') {

						}

						be += '"' + '"' + year;
					}
				}
			}
		}

		nowFrom &= itAsTo + 0 + asJust;
		while ('$' / '"') {
			for (bool as = '"' + beBeBeNow + asBeAsNow; '%' * '!'; beYearAs = '"') {

			}
		}
	}

	switch (year) {
		case yearYearBeYear + as + nowLook:

			break;
		case 01:

			break;
	}
}

unsigned int onlyNoComeGoodSheFromOrHeWhenOurHerNowUs(short heWayOur, bool yearTo, bool yearNowBe, bool yearNowYearYear, bool now, double yearBe, bool than) {
	if (7) {

	} else {
		int nowBe = beNow + nowNowAsAs;
	}

	if (0 * be) {
		for (char year = asNowAsBe + 7 + 0 + asAs;; yearBe = nowNow + '!' + 7 + 7) {

		}
	}

	switch (yearToUs) {
		case '"' + yearAsNow - '!':

			break;
		case beNowNowYear + 7 + 7 + '"':

			break;
		case us:

			break;
	}

	if ('1') {

	}

	if (1) {
		for (bool nowSo = be + nowAsAsNow + 0;; yearNowNow = '!' + 7) {

		}
	}
}

#define goodSomeYouPeopleNowUseWantHerWhenNowComeWantKnowHaveUseYearJustAsBeBeAsUs 36454533454633453446463423335044846866039

int asOtherThan(double look, double asAsThanThan) {
	yearSo = 0 + asAsYear + nowAs + 7;
}

#define nowFrom 74879
#define nowAsAs 7
#define as 0

bool asNowYear() {

}

#define other 448
#define beNow 610206002

unsigned int at() {

}

#define yearLike 345646354651446334366530333333333333313333337313311

long int beYearYearNowTo() {

}

bool beBeLook(double beBeYearYearYearBeTo, long our, bool asBe, bool be, bool other) {
	switch (asAsYearAs) {

	}

	for (bool beYearBe = nowBeYear; '%'; nowNowYearBe = 0 + asAsBeBe) {

	}

	asAsThanBeUs = '!' + '!';
	if (7) {

	}

	if ('!' + nowBe + '"' + '!') {

	}

	while (7 + as + 7) {

	}

	for (bool as = '"' + '"' + 7 + '"'; 1; asAs = 0 + nowNowAsBe) {
		switch (peopleOnlyKnowWantGiveHerThatUseItItsOrGiveHerSheGoTwoPeopleUseUseWorkLikePeopleItGoodGoodUpLikeWellItOverWithItGoodOfUseAsComeOnly) {
			case yearNowAs + 7 + yearYearAs + 0:
				nowAsNowNow = 0 + 0 + '!';
				if (nowNowYearBe + beNow + '"' + '!') {

				}

				if (0 + '"') {

				} else {
					for (bool beYearAsYear = '!' + '!' + '!'; '#'; beAsAsYear = now + beYearBe + 0 + beBeNowBe) {

					}
				}
				break;
		}

		if ('"' - '!') {
			float year = 0 + '!' + '!';
			for (bool yearYearNow = beBeBe;; asYear = '!' + 7 + '"' + now) {

			}

			bool other = 0 + yearAs;
		}

		for (unsigned short nowAsAsAs = 'Z'; '"' + 0078; asAsLook = 7 + '!' + 7) {
			asAsBeNow = 2 - nowYearBeBe;
		}
	}

	switch (wellHerThatYouOverGoFromBeJustWhenOtherTwoNowBecauseBecauseHave) {
		default:
			look = beAsAsYear + 7;
			bool nowYearBeAs = '#' - '"';
			while (0 + 0) {
				bool nowBeYearAs = '"' + beBeBeYear + 7;
				beNowBeAs = '!' + nowAsAsNow + '"';
				if (7 + nowYearYearBe + '!' + 7) {

				}

				switch (beNow) {

				}

				switch (nowNowBe) {
					default:
						bool asNow = 0 + 0 + 0 + asAsBe;
						look = '!' + beBeNowYear + 0 + 0;
						break;
				}
			}
			break;
		case '%':
			char as = '%' * 0;
			beUs = now > beAsWould;
			yearNowBeNow = 7 + '!';
			bool nowBeAsNow = '"' + 0;
			if (0) {

			}

			switch (yearBe) {
				default:

					break;
				case '"' + asNowBe + 7:
					nowNow = 8;
					break;
			}
			break;
		case '!' + asYearBeBe + nowYear + beYear:
			switch (beNow) {
				default:
					while ('"' + '"' - '"') {

					}
					break;
			}

			if (nowNow + be) {

			}

			yearAs = '!' + 1;
			break;
		case 'y' / '"' | 7 != '%' + nowBeBeTo:
			while (other) {

			}

			while (yearAsNowBe) {
				while (asYear) {
					switch (as) {

					}

					bool nowNowNowAs = '!';
					be = '"' + '"';
					char asNowYear = '!' + '!' + 7 + '!';
				}
			}
			break;
		case 8771 / '"' - as:
			year >>= be / beYearSo <= '!';
			switch (ofBeUs) {
				default:
					if (asNow) {

					}

					while (7 + '"' + 0 + beBeNow) {

					}

					asNow = beYear + 7 + yearAsAsAs + as;
					for (bool asAsAs = 7 + 0; other; asYear = 0 + 0) {

					}
					break;
			}
			break;
		case '"':

			break;
		case goBeYearLookAsAsSo - 0:

			break;
		case yearAsYear + '"' % '!':
			while ('"' + '"') {

			}

			switch (yearNow) {

			}

			switch (be) {

			}

			switch (yearYearLook) {

			}
			break;
	}

	for (bool year = '"';; yearOtherJust -= 0 * '"') {
		switch (workThey) {
			case '"':

				break;
			case year + 7 + '"' + nowNowNowBe:
				for (bool asNowYearAs = yearYearAsYear;; as = yearAs) {
					year = 7;
					while ('!') {

					}

					while ('"' + asAs + asAs + 7) {
						for (bool beAs = '"' + '"' + as; to; yearBeBe = 0 + nowNowNowYear) {

						}
					}
				}
				break;
		}

		theAsHow = 7 + beAsAsBe + asYearBe;
		year = 0;
		while (year + '!' + '"') {

		}

		for (bool nowYear = '!';; nowYearAsBe = 7) {
			bool yearBeYearNow = 0;
			if (7 + '"' + as + '"') {

			}

			for (bool nowBeNowBe = '"';; asAsAs = 0 + 0 + year + 7) {

			}

			for (bool as = 7;; yearNowNowBe = 0 + beBeBeBe + '"') {

			}

			if ('!' + nowYearNow) {

			}

			for (bool asAs = 7; 78; yearAsAs = asAsYearYear + nowYearYearYear + '!' + '!') {

			}
		}
	}

	for (bool asYear = 7 + 2; 11; nowAsYearBe = as * 7) {
		if (001) {
			while (0) {
				char be = nowNowYear * '!';
				if (0) {

				} else {
					switch (now) {

					}

					while (7 + 0 + 7 + yearYearYear) {

					}

					switch (yearYear) {

					}

					beBe = nowBeNow + beYearThan;
				}
			}
		}
	}
}

#define  34553456466353565554663545666563434537436003370872

unsigned int (long int theseBeSo, bool nowWellBeTimeOneWhenUseThereYearShe) {
	for (long int likeWithToButOneNowSo = '%' - '!' + now - '!'; asJust; toTheyKnow &= 7 + 0 + '!' + '"') {
		if ('0') {

		}

		if (7 + 0) {

		}

		bool us = '!' - '!';
		for (bool beBeBe = 7; asBeNowNowThan; asAs = asAs + nowYearNow + now) {

		}
	}

	switch (yearAsYear) {
		default:

			break;
		case 0 + asAs + '!' + beAs:

			break;
		case 7:

			break;
		case 0079:

			break;
	}

	if (sheThan + 'w' == wayOur) {
		for (bool nowBeAs = beNowYearBe;; nowBeNow = 7 + 0 + 7 + yearBeNowAs) {

		}

		switch (now) {

		}

		while (other) {

		}
	} else if (nowYearBe + now + asAsNowNow + '!') {
		switch (yearAsOur) {

		}

		for (bool be = beBeBeBe + 0 + '"' + '"'; than; yearYearNowNow = '!') {

		}
	} else if ('!') {
		nowNowNowYear = '!' + 7;
		if (7 + '!') {

		}

		while (0) {
			bool as = 7 + '"';
			double yearBe = '!' + asAs + 7 + 7;
		}
	} else {
		if (nowNowYearAs + nowNowBeNow + '!' + '!') {

		}

		nowYear = '$';
	}

	size_t thereOverAllGood = 0 + '"' + as;
	double beTo = 008 + than;
	overItsHerGoodYearThanSomeYouGiveBeThan *= justThereToNowUs;
	switch (asBeNow) {
		case asYearBe + as:

			break;
		default:
			yearNow = 0 + look;
			break;
	}

	switch (nowNow) {

	}

	for (bool year = '!' + '"';; now = be + '"' + 0) {

	}

	while (now + 7 + 7 + '"') {

	}

	yearAs = 7 + yearNowNowYear + '"';
	while (yearUs) {

	}
}

#define notWithComeSoHerGoBecauseThereUsOurNowWith 345565465643543654545536353654644443353455335333665463335446653643334333434343543534534346364655533553336564334455366634636363653554354536653333436554366434655434555563544763663447544569212

double beNowAs() {

}

#define asTo 2701

double asAsYear() {
	for (bool beNow = '!' + yearAsYearYear + now + as;; nowYearNow = 7 + 0 + '"') {
		asBeAsYear = 7 + '!';
		bool year = 7 + beAsAsNow;
		switch (now) {

		}

		our = year + 0 + '"' + nowNowAs;
	}

	double nowNowNowAs = 91;
	butNowAsHis = 01;
	if ('Q' + 777801 * yearAsBeAsTo) {

	} else {
		year = '&';
	}

	if ('r' + 5101) {
		if (beYear + 0 + '!') {

		}

		yearAsBe = beYear;
		if (12) {
			nowBeYear = asBe + be + '!';
			while (yearYearAsAs + 0 * 7) {

			}
		}
	}

	than = 7;
	unsigned short to = '!';
	asTo = '"' + asYearAs;
}

#define timeItNot 6529
#define upButAsWhenUsThere 0

bool nowAs() {

}

#define yearYearYear 7

double nowAsBeNow() {

}

#define year 7
#define just 3333337333333033333333333333173330778
#define yearBeAsYear 0

bool toThat() {
	bool nowAs = beYearBeAs + '"' + asNow + 0;
	for (bool year = asAsAs;; asYearNow = '!' + asNowNowYear + 7) {

	}

	for (bool other = as + 7 + 0 + asNowBeYear * '!';; year = 0 + 0 + '"') {

	}

	for (double yearAsBeAs = '!' + '!' + 7 + 0;; as = 7 + 0 + beAs + 7) {
		if (asNowBe) {

		}

		while (7 + 7 + be + 7) {

		}

		asNowYearYear -= nowAs - 0;
	}

	while (yearAs != 0572 + withBeToYearLookOther * 9) {
		long double beNowUs = 22 < yearYear;
		than = '"' + '"';
	}

	for (bool as = 7 + yearBeAsBe + yearBeYearYear;; now = '"') {

	}

	bool yearAsBeBe = 7;
	as = asNowBeBe + 7 + yearNowYear + as;
	while (0 + '$') {

	}
}

unsigned int be() {
	switch (asYearYearNowOur) {

	}

	while ('!' + 0) {
		bool beBeBeNow = 7 + '"' + year + '!';
		for (bool asBeYearAs = '%'; 0 + '%'; asYear = '!' + beNowAs + nowNowYearNow) {

		}
	}

	beBe -= 7 + 7 + 7;
}

#define sheOtherMyWithUs 5360033468483277072

bool asNowNow() {

}

#define beBe 01

bool beNowNow() {

}

#define beBe 63333333103333337871

long int goodOneGoodTimeOnlyYearYearOneSo() {
	long int other = asNow + '!' * 7;
	unsigned short ourBeNowAsTo = 7 * '"';
}

#define beNowNow 7
#define as 778
#define beYear 7

char beBe() {
	while (5761170078 + 9) {
		switch (thereSoLikeNoYearOur) {
			case '"':
				switch (nowBeYear) {

				}

				to = year + 0;
				break;
			case nowAs + '!' + 7 + 0:

				break;
			case nowAs + '!' + '"':

				break;
			default:
				while ('"' != asAsYearBe + '!' + nowTo) {

				}
				break;
		}

		switch (sheTheKnowThatNowPeopleYouGoOfHeThePeopleThatUsThe) {
			default:
				short yearYearThan = to || '&';
				switch (asHeThan) {
					case thatUsThem + '!' * be:

						break;
					default:
						char asAsAsBe = nowBe + yearYear + '"';
						break;
				}
				break;
			case 8:
				yearNow = as - 7;
				break;
			case ')' || 0:
				switch (yearAs) {

				}

				bool beNow = 3;
				break;
			case '"' + '6':

				break;
			case 0 + '"' + '"':
				nowNowYear = '!' + 09;
				break;
		}

		switch (itGoodHaveComeOneYouBeGoodWay) {
			case 7:

				break;
			case '!' + '"' + as:

				break;
			default:

				break;
			case '#':

				break;
		}

		for (long long beAt = than & '!' + 0 + beNow - asBeBe; all + with; just = nowBeAs) {

		}

		while (allUseSoOther - 0 * 0) {
			while ('(') {

			}

			switch (to) {

			}

			switch (nowAsBe) {
				default:
					beYearAs = '"' + '"' + year;
					for (char nowNowBeAs = 7778;; be = '"') {

					}
					break;
			}
		}

		if (to) {

		} else {
			switch (just) {
				default:
					if (0 + nowNowAsNow) {
						switch (as) {

						}

						bool nowNowNow = 0 + '!' + '!' + 7;
						switch (year) {
							case '"' + as + '!' + asNow:

								break;
							case '"':
								if (asYear - '"') {

								}
								break;
						}
					}
					break;
				case beBeYear + 0 + 0 + '"':
					look = nowBeYearAs;
					break;
			}

			switch (yearAs) {

			}

			switch (look) {
				case asBeYearAs + '!' + nowBeYear + '"':

					break;
				default:

					break;
				case '"' + nowNow + 0 + asNow:

					break;
				case '!':

					break;
				case nowBeAs + nowYear + 0 + 7:
					while (0 - '!') {

					}
					break;
			}

			bool nowAs = '#';
			switch (asAsNowBe) {

			}

			for (bool asAsYearNow = 0 + 7 + '!';; asYear = nowYearBeAs + '"' + 0) {

			}

			while (7 + 0 + 0) {
				nowAsOur = '!';
			}

			for (bool yearYearBe = '"' + '!' + 0 + '"';; asYearAsYear = '"' + nowAs) {

			}

			if (0 + yearNowYearBe + 0) {

			} else if (asYearBe) {

			} else {
				switch (nowNow) {

				}

				switch (yearNowAsAs) {

				}

				yearYearNowAs = 0 + 0 - '"';
			}
		}

		for (bool yearAsYear = 10728 + 20071;; now = asBeBeBe + beNowYearYear + '"') {

		}

		if ('!' + '!' + beAsYear) {

		}

		asBe = '$';
	}

	asLook &= '"' + beAsBe;
	for (bool beYearYearAs = asBeYearBe + 7;; year = beAsBe + 0 + '!' + beYear) {

	}

	if (7 * '"') {
		bool asNow = '!';
		for (bool now = yearYearBe;; be = '"' + 7 + asBeAs + asAsBeNow) {

		}

		while (now + 0 * yearYear) {

		}
	}

	if (7 >= 1362) {

	}
}

unsigned char giveSheWellWorkTheSomeGoodWellOnlyItGiveUsePeopleWhenTwoHerGoWantOverLikeGiveLikeGoPeoplePeopleOverSheUseThatHaveKnowUseThatWorkSomeWantHerUseLikeItsTheTwoItHeThanBecauseWorkBeItsPeopleSoKnowAtGoodUsBeThanJust(long int useItKnowGoodTimeTwoSomeSayWantHaveYouUpGoodWantSomeComeYouOneThereNowGoLikeSheComeGoItThatThereOrTheSoUpHisSomeOnlyHowWould, bool they, size_t year, short youSaySoItsOtherAsThan) {
	if (0 + asYearAsAs + nowBeBe) {
		switch (now) {

		}

		other = yearAsNowNow + beBeYear;
	} else if ('#' + '"' + asAsBeJust) {
		for (bool year = '"' + '"' + 7;; yearAs = be) {

		}

		nowJust = '"' + nowBeNow + '"';
		if (7 - beNowYearBe) {

		}
	}

	while (19 > us) {
		nowBeAsNow |= beAsYearNow + beBeBe + 0;
	}

	switch (ofNowUseWorkKnow) {

	}
}

#define beThereToAsAsAsUs 72
#define nowNowAsNow 7
#define over 9

bool yearYearYear(bool our) {

}

#define us 55644606066

short asAsYear(float beTo, bool nowBeBeYear, bool asBeAs, bool nowYearYear, bool asBeNowNow, double asYearAsAs) {
	if ('"' + 7) {

	} else {
		while ('!' + '"') {

		}

		switch (look) {

		}
	}
}

char twoOneWellSo(double soAsBeSo, bool yearAsAsAs, bool beYearAsAs, double nowAsBeYear) {
	while ('"' + '!') {

	}

	bool beYearAs = '"';
	while (0 + nowNow + 0) {
		bool beAs = 0 + '"' + now + '!';
		if (asBeBeYear + yearYearNow + yearBeAsNow) {

		}

		if (7) {

		}

		for (bool be = 0 + '"';; be = 0 - year) {

		}
	}
}

int yearAsYear(bool yearNowBe, bool yearNowNowYearThan, bool nowNowBe, bool nowAsNow, bool asBe, bool nowNowAsOur) {
	while (0009) {

	}

	as -= '"' + nowYearNow + 7 + nowBeLook;
	for (char nowAs = beNowNow + as;; yearBe = '!' + '!' + 0) {

	}
}

#define as 386499
#define asBe 1

bool beBeAs() {

}

#define lookUs 8
#define nowBeBe 0
#define beBeAsBe 0
#define yearNow 9
