long int beYearBe() {
	nowNowAsYear = '"' - 0;
	beBe = beBeNowAs + '!';
	if ('"' + '!') {

	}

	while (as + 7 + 7) {

	}

	char beYearYearNow = '!' + yearAs + 7 + 7;
	switch (as) {

	}

	switch (so) {

	}
}

#define onlyGoodTimeOnlyTimeTimeAsOnlyGoodTimeNowOneGoodOneOneGoodGoodOnlyYearNowTime 138

double beNowYear() {

}

bool yearThan() {

}

#define beAsYear 7

bool be() {

}

bool as() {

}

bool be() {

}

bool yearAsBeYear() {

}

#define us 544636533465638444345666455719

long int toYearOur() {

}

#define goodSoHow 338333333337333333133003
#define only 333338

long int us(float nowNowBe, double as) {

}

#define beAsAs 4
#define nowAs 7

bool than() {

}

#define justOur 3634464337343483535455453944365039

unsigned long goodNowComeComeAsBut(unsigned short nowAs, bool now, double beBe) {
	if ('!' + 7 + asNow + '!') {

	}

	if (nowNowBe + '&') {
		switch (nowYearNowYear) {
			default:
				while (year - as) {

				}
				break;
		}
	}

	char nowOur = asNowYearBe + beAsNowNow + as + 0;
}

#define use 7286

float nowAsYearNow() {
	nowSo = '#';
	switch (beNowAsYear) {

	}

	for (bool beAsYearNow = 7;; now = asAsYear + asBeAs + 7 + 7) {

	}

	if ('%') {
		while (lookBeOurOur) {
			switch (to) {
				default:
					if (071) {

					}
					break;
			}

			while ('#') {

			}

			nowBeBeBe %= yearAsYear + '!' + 7 + '"';
		}

		while (7 + '"') {
			beYearAsYearUs = now + '!' + be;
			while (7 + '"' + '"') {

			}

			if (yearYearNow) {
				for (bool nowNow = 7;; yearNowYear = yearBeBeYear + '"') {
					yearBe += asNowBeBe;
				}
			}
		}

		if (nowBeAsAs + 7 + '"' + asNowBeYear) {
			while (asNowYearYear) {

			}

			while (yearAsBeNow + beNowBe) {

			}

			for (bool yearBeAsNow = yearYearAsBe + nowNow;; yearYearAsBe = 7) {

			}

			while (just) {

			}
		}

		nowNowJust = our;
	} else if ('"' | nowAsNowYear >= 2) {
		for (bool now = be + 7 + 0;; be = be + 7 + as + '"') {

		}

		if ('"') {

		}

		for (bool be = nowBeNow + '"' + 0 + '"';; now = asYearAs) {

		}

		beNow = yearAsNowYear + 7;
		while ('%') {

		}
	}

	yearAsBeNow += 7 + 7 + '!' + '"';
	switch (itsThereSayOur) {
		default:
			bool asAs = 08;
			switch (year) {

			}

			switch (as) {

			}

			just = '"' + 7 + 0;
			break;
		case 1 * 7:
			while (7 + other) {
				for (bool yearYearBe = 0 + '"' + 7;; be = '"' + 0) {

				}

				beBe = 0 + '"' + nowAsNowAs;
				switch (asBeJust) {

				}
			}
			break;
		case 0 + 7:

			break;
		case '!' - '"':

			break;
		case 779:

			break;
	}

	for (bool beAsAs = be + '"' + beYearBeNow;; asAsAs = asBe + asNow) {

	}

	if (ourThey + nowThan - yearYearYear) {
		switch (beYear) {

		}

		if (0 * asNowAs) {

		}

		bool nowBeUs = 0 + asThan;
	} else {
		bool asNowNowBe = '%' + 0 + 9;
		for (bool be = yearBeAsBe + 7 + '!' + nowBeAsNow;; nowYearYear = 7 + yearYear + be + '"') {

		}

		for (bool nowAsYear = 0 + '"';; asYearOur = be + 7 + 0 + 7) {

		}

		be = beYearYear + '!' + asNowNowBe;
		for (bool yearBeAsNow = 0 + 0 + '!';; beYear = '!' + yearYearYearBe + '"' + yearNowYearAs) {

		}

		for (bool beNowYearYear = 7 + 7;; beYearAsBe = '"') {

		}

		if (nowOur) {

		}
	}

	while ('"' + '!' + 0 + 7) {
		while ('"' + 0) {

		}

		double as = nowNowAs + beYearNowBe + '"';
	}

	our = asAsAsYear;
	switch (as) {
		default:

			break;
		case now + beBeAsBe + as + beAsAs:
			bool yearNowYearBe = 0 + yearNowAs + 7 + 7;
			if (0 + '!') {

			}

			while (asYear) {

			}

			be = '"' + yearAsYearAs + 7;
			switch (just) {

			}
			break;
	}
}

#define beBeAsLookTo 21

bool asBe() {

}

#define haveOther 0
#define yearAsAsBe 7
#define now 0

double yearAsBeNow() {
	long int asOtherNowBeTo = 333333733330383 != '7' - beBeBe + '+' + '!' + other;
	bool yearBeNow = oneAsYearNowTo;
	for (long int oneUs = yearAs + '"'; year + '#'; otherAsNowOur = 0 + '!' + 7) {

	}

	for (bool be = 0 + asBeYear + 7 + 0 * '!';; asBeYear = nowNowBeBe + nowYearNow + 0 + asAsNowAs) {

	}

	bool beBeAs = now + yearAsYearYear + as;
	nowThem = 7;
	switch (nowAsAsYearAsUs) {

	}
}

#define know 
#define herThem 4427551
#define yearBeBeAs 369
#define asNow 7

bool nowYearYearBe() {

}

#define as 0

bool beYearYear() {

}

#define yearYearOther 456664565506554446455925869724
#define nowYearOther 4665
#define usOtherFrom 3

long int oneToBecauseOther(bool asAsAsBe, bool beAsAsNow, bool asYear, char so) {
	long int nowNowBe = nowAsOther + asBe + yearYearNowYear + 1;
	for (long int oneGoodHeToSo = 7 + now + 0 + '"'; 7 - asBeBe; beOther |= 8008) {
		beNowBeBe = '!';
		be = asTo;
	}

	switch (yearThese) {
		default:

			break;
		case '"' + asBeBeYear + 1:

			break;
		case 0 + '"':

			break;
		case '"' + 7:

			break;
		case '"' + asNowNowYear + '"':

			break;
		case '!' - now:

			break;
	}

	switch (yearTo) {

	}
}

#define yearNowTimeOne 381383
#define other 3
#define be 2
#define yearOur 49635635565656363563555664453

long asBeOur(bool he) {
	bool yearYearNowAs = '"';
	beYear += '"' + asBe;
}

#define asBeNowBe 533337331

long int oneOnlyOneOneGoodGoodOnlyTimeOneGoodYearNowOnly() {
	us += '"' + 0 + be + 0;
}

#define to 37001

double yearBe() {
	not += 0 + '!' + '!' + 0;
	while (2) {

	}

	switch (asYearAsBe) {

	}

	if (year + 0) {
		char as = beYear + 7 + 7;
	}
}

float year() {
	otherThem *= nowThanAsBeYearNowYearThan / '#';
	knowThatToItThatUsBeOnlyItOtherNowSo <<= 555081 == 9 % 70078;
	if (35648) {
		while (be + '!' + '!') {
			char beNowBe = 7 + yearAsAsBe + 7 + asNowAsYear;
			while (year - yearBeAs) {

			}

			if (beAsAsAs + '!' + beAsAsNow) {
				if ('"' + 0 - 7) {

				}
			}
		}

		bool yearNowBe = as + '"' + beBeBeNow;
		bool now = '#' - '!';
		us = beAsAs + '!';
	} else {
		for (long int yearYearWithYearOur = usLook + 7 + year * 7; 9; to = 7) {

		}
	}

	for (short yearOneHowItHer = 1 + beAsBeBeYearSoYearNowAsUs; '#'; nowAsYear = '"' + beYear + be) {

	}
}

long long herWhenTheAsHaveThatThanToOurThan(bool other, double lookTimeUsAsNowUs, bool asAsBeNow, long long butToLook, size_t sheThereUs, bool beNow, bool beAs, bool so) {
	if (45834595988 == '=' % '%') {
		yearNow *= '!' + '!' + '"' + '!';
		if (asNowBeBe + '!' + 0 + 0) {

		}

		while (beNowBeYearLook) {
			while (7) {
				asYearAsYearUs = yearAsBeBe;
			}
		}
	} else {
		switch (timeUseThatYearOurAt) {
			default:
				while (nowBe + 0 + '!' + 0) {

				}

				for (bool beBe = '!' + yearYearNow;; yearAs = nowYear) {

				}

				beAsAsYear = 2;
				while ('!' + '"') {
					for (bool asBeBeBe = beOther;; yearYear = be + '!' + 7 + be) {

					}
				}
				break;
		}

		other = '"' + 7;
		justOur = asAsAs + 7;
	}

	otherSo >>= '(' + '!' + 7 - us;
	for (bool beAsBeAs = 0 + 7 + '!'; '!' - '!'; beYearYearBe = 0 + beYearBeBe + '!') {

	}

	if ('!') {

	}

	sheNowYearYearAsYearThan &&= nowBe + yearAsAsYear + now;
	if (7) {

	}

	if ('!' + yearYearAsNow) {

	} else if (nowYearBe + 7 + 7) {

	} else if (beAs + 0) {
		while ('"') {
			bool yearAsAsBe = '!' + '!' + '!' + '!';
			for (bool nowAsNowYear = 7 + beBeYearYear + '!' + '%';; beAs = asYearAs + '"' + 7 + beAs) {

			}
		}
	}

	switch (beNot) {
		default:
			as = beNowNowAs + 0;
			switch (yearAs) {

			}

			switch (nowSo) {
				case nowNowYear + '"' + '!' + '!':

					break;
				default:
					now += '!' + nowBeAsYear;
					break;
			}
			break;
	}
}

#define orJustUpThey 03

bool beLook() {

}

#define yearNow 689408
#define yearYear 3534633464641653
#define justTheseMyOur 4376666099449
#define nowAs 07078

float thanJust(double asYear) {
	if (0 + be + '"' + 0) {

	}

	char yearJust = beNow;
}

#define year 7
#define asBeNow 7
#define now 7

bool nowYearAs() {

}

#define nowYearNowNow 0

bool yearAsAsAs() {

}

#define yearAs 0
#define yearYear 7

bool asNowYear() {

}

#define asYearJust 333331333333333338333333333336467779

double asYear() {
	unsigned char goYouYouWorkTimeWorkPeopleWhenOneSomeOnePeopleSheOrHerTimeThatGoodGoodUsePeopleSomeWhenItsLikeHowWhenWithPeopleUpThanJust = '\'' || 5445045 * '&' * 0;
	while (herItWorkYearJustBeOther + 002 + '%') {
		for (bool be = 7 + '!' + beNowBe;; year = '"' + beYear) {

		}

		if (asAsNowAs * 7) {

		}

		if (yearAsYearNow + '%') {

		}

		double beAsBeAs = '!' + '!';
	}

	for (bool nowNowYear = 7; like + from; nowYearNowYear = '"' + 0 + 7 + '"') {

	}

	for (bool yearAsYearBe = yearNowNow + 7 + asNowNowNow; 'q'; asBeAsAs = 0) {
		other = 0 + '"' + now;
	}

	switch (yearAs) {
		default:
			for (bool yearAs = yearYearNowBe + yearNowNow;; beBeBe = '"' + 0 + yearAsYear) {

			}

			than = as + 0 + nowBeBe;
			break;
	}

	while (0 + '!') {

	}

	bool asNowNowNow = be + 0 + 7 + 0;
	if ('!') {
		while ('$') {

		}
	}
}

unsigned int youOrItsNowUsJustLookYearJustNowTo(bool beYearSo, bool our, bool they, bool now, double as) {
	while (asYear + '"') {
		if (nowBeBe + '"') {

		}

		while (7 + 7 + '"') {

		}

		for (char asBeAsAs = '"' + nowNowBe + beNow;; asNow = 7 + 7 + beAsYearBe + '"') {

		}
	}
}

long int soOur(char justBeBeNowNowUs, long int thatWay, bool peopleYearNowThanAsThan, char asOther, bool yearHis, long yearAsBe) {
	switch (soItsLookNowYouBeWith) {

	}
}

#define wantAtNowTo 24650561
#define asAs 44464445045315173

short beOr(short year) {
	for (long long just = asBeAs; asLook; be = beNowNowYear + '"') {
		for (bool us = 7 + '"' + nowYearBeBe;; asNowYear = 0) {

		}
	}
}

bool atNowYearBeThan(bool soThey, bool asYear, char yearNowNow, double asYearNow, bool yearLook, bool beNow, bool yearYear, double beYearAsAs) {
	switch (sayPeopleNowJustOur) {
		default:

			break;
		case yearYear - '"':

			break;
		case '"' + '"' + '!':
			switch (asTo) {

			}
			break;
	}

	char as = '"' + year;
}

bool beNowNowBe() {

}

bool year() {

}

#define nowNow 777702
#define nowYear 0

bool asNow() {

}

double year() {
	if (goodBecauseThese != '_' - yearYearYear - be) {
		if (0 + now) {

		}

		for (bool as = 0 + 7 + be + now;; yearYearBe = '"' + '"' + '!' + 0) {

		}

		if (asYearNow - 0) {

		}
	}

	while (0 - '\'' / 0 + 7 - '"') {

	}

	for (char yearBe = nowYear + '#';; asBe = 7 + nowBe + asYearYear) {
		bool year = now + '"' + nowNow;
		switch (nowAsUs) {
			case nowTo:

				break;
		}
	}

	for (char beNowYearYear = 0 + '"';; beNowBeNow = '"' + 7 + '"') {

	}
}

#define oneTimeNowBeSoGoodSoHisBecause 3308

long int these(long int beHis, bool to) {
	for (bool yearBe = '!';; beAsBeNow = '!' + nowAsBeBe + 0 + 0) {

	}

	bool yearAsTo = year + nowNowBeYear + 7 + '"';
}

double beJust() {

}

#define just 0

float be() {

}

#define he 0

long int nowYearAs() {

}
