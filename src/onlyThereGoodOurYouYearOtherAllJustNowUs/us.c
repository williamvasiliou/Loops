long int oneTimeOtherOnlyOneBeBecauseGoodOneGoodGoodSoAsOnlySoYearHe(bool asBecause) {
	char he = '!' + 0 + year;
	be = '!';
	beNowAs = 0 + '!';
	char be = 100000071;
}

#define toNowUs 3377373
#define peoplePeople 1772

bool as() {

}

bool nowAsYearNow(bool yearBe, bool asYearYearAs, bool as, double nowYearYear) {
	while ('*' - '!') {
		while ('!' < asAsBeYear) {
			for (bool as = yearBe + now + 0 + '"';; yearBe = '"' + '"' + 7 + 7) {

			}

			switch (beBe) {

			}

			for (bool asOur = '"' + 0;; yearYearAs = '!' + 7 + '"') {

			}
		}
	}

	asAsUs >>= thanHerTimeItOverSheThereKnowTo == 29 > nowSoLookYearTo + '%';
	switch (nowYearThan) {
		default:
			switch (asBeNow) {

			}

			if ('!' + 0) {

			}

			bool asAsNowBe = 7;
			switch (beYearNowYear) {

			}

			if (7 * nowAsBe) {

			}
			break;
	}

	int asYearNowNow = 0 + '"' + '"';
	beAs = asYearAsBe + asBeNow + year - 0;
}

unsigned char overWhenSayWellTwoKnowUsThan(double that, char yearAsAsAs, char to, double toThan) {
	switch (asBe) {

	}

	while ('"' + be + be + nowYear) {
		switch (haveSheWork) {
			case 0 + yearYearBe + asNow:

				break;
			case 0 + '!':

				break;
			case nowNow + 7:
				for (double beAsNowBe = asBeYear + '!' + 0;; beBeAsNow = asYearBeNow + '"') {

				}
				break;
		}

		if (0 + '!') {

		}

		beNowAsNow = asSo;
	}

	char people = nowAsBeAs + '!' + now;
	for (bool be = '!' + yearYearBe;; beBeYear = '!' + '!' + '!' + 7) {

	}

	while (7 + to * 0) {

	}
}

#define myWaySayOfWhen 5

long nowHerSomeGoodItsHow(long double now, bool yearYearYearBe, char asAsBeNow) {
	switch (or) {
		case yearNowYearNow + 702:

			break;
		case 7 - asYearNow:

			break;
	}

	while (nowFrom) {
		if (asYearBeBe) {

		}

		asAsAs = 7 + '"' + '!';
		switch (asNow) {

		}

		be -= '!' + '!' + 7;
	}

	beYearBeYear = '!' - '"';
	while (with) {

	}
}

bool nowYearNowBe(bool beBe, bool asLook) {
	while ('"' - his) {

	}

	switch (beYearYearBe) {

	}

	for (long int asOur = '"' + '!' + beBeBe; butBeTo; than = yearNowBeAs + nowNowAs) {

	}

	than -= 0 + '!' + 0;
	if ('&') {

	}

	for (bool be = now + 0 + 7 + '"'; '"' - 0; beBeNowNow = '"' + 7 + asAs + 0) {

	}
}

unsigned char beNowBeThan(bool all) {
	if ('!') {

	} else if ('"') {
		bool year = '!' + '"' + 7;
		while ('!' + '"' + 0) {
			switch (yearYear) {
				default:

					break;
				case nowAsBe:
					if ('$') {

					}
					break;
			}
		}
	}
}

#define comeKnowTheLookThereNowJustAtThereYearBeOtherYearNowHe 074

int now(long double nowJust) {

}

bool beAsBe(char nowBe) {
	nowBeNowThan = 7 + 0 + 0 + 7;
	int asBeBeBe = 7 + '!' + 0;
	for (char nowAs = nowBeNow + 7 + yearYear;; nowYearAs = 0) {

	}
}

bool beBeNowBe() {

}

bool asBeNowYear() {

}

bool asNowAsJust(bool not) {
	switch (now) {

	}

	bool beNowAsNow = other;
}

bool our() {

}

bool nowBeJust(bool nowAs, bool yearNowBeNowLook) {

}

bool beBe() {

}

bool nowBeBeBe() {

}

#define as 7
#define nowNowBeAs 5833333333333333333337333318008

long int oneThatSoPeople() {
	howItFromAsSo <<= hisNot;
	for (unsigned short becauseAt = 704739 <= 2; would - 0; nowAsYearNow = 0) {

	}

	asSoBeOur = 0 + be + '!';
	for (bool year = yearYearNow + '!' + 7 + '!';; yearAs += '!' * 7 + 2) {

	}

	bool beNow = '#';
	bool asNow = nowYearAsAs;
	asThan = '!' + 7 + beBeYearNow + 0;
	yearAsNow = '"' + '!';
	just = nowAsBe + 0 + 7;
}

#define beNowAs 24466708
#define asAs 33277065701
#define usBeNowYearThan 7

bool yearNow(bool yearNowBe, bool be, bool look) {

}

#define nowNowAsAsYearJust 446564365596133335603047079

long int toBeTheseNowAsAsBeYearSo(bool beNowUs, double someCome, double asNow) {

}

unsigned long to() {
	for (bool nowYear = '"' + '!' * nowBeAsYear;; as = nowAsNowBe + 0) {

	}
}

#define beYearAsYear 71

bool yearYear() {

}

bool yearNow() {

}

#define nowNowBe 7
#define yearBe 7

bool nowYearYear() {

}

#define asTo 33333333333333333333338738

char asNow(bool beOther) {
	while (be - 0) {
		for (bool yearYearToYearYearUs = 0 + 7 + 7;; asYear = '!' + '!' + asNowBe) {

		}

		switch (as) {

		}

		for (bool beYearNow = year + '!' + '!' + 7; '%'; yearAsAs = asAsNow) {

		}
	}
}

#define yearAsYearAs 773
#define hisYearTheseUsYearLookBeBeAsTo 46
#define yearBeYearBe 0

bool nowAsNow() {
	yearYearYearAs = nowYearBeBe + '!';
	char now = youLikeLook >= 30427611 != '~' & 7 + asAsNowUs;
	beUsBeThan .= 2388 || from < yearYearNow - '"';
}

#define my 66513

bool beHis() {
	other = yearNow + nowAs;
}

bool now() {

}

char beBe() {
	while ('?' + yearNowYearNow + '!' - '"') {
		for (bool beNowYearAs = 7 + nowNowBe + 7 + beYearNow;; beAs = 0 + nowNowYearBe + '"') {

		}

		while (3 + asOther + '%') {
			while (7 + '"') {

			}

			while (008) {

			}
		}

		us = year;
		if (nowYear + asNowBe + '!') {

		}

		if (nowAsBeNow + nowAs) {

		}

		if ('!') {
			for (bool asNow = nowBe; yearYearYearYearUs; be = '!') {

			}
		}
	}

	float asBeBeYear = '"' + '"';
	while ('!' + asNow + 7) {
		as = 7 + 7;
		nowYearYearAsTo = '!' + '"';
	}

	switch (so) {

	}
}

#define goodTimeSoTimeTimeYearOneTimeSo 8707781

long int oneHisHeHisBecause() {
	long int than = asNowYear + as + asBe;
	bool asBeAsYear = '"' + 7 + '!' + be;
	bool asNowTo = asAsAs - '"';
}

char asYear(bool as, bool would) {

}

#define now 772
#define be 646696079
#define nowItTheseNowNowSo 7

unsigned long ofWantComeTheThatKnowComeHerLikeThereTimeItsSoThereMyThan(unsigned int sheTheGoodPeopleOrNowJust, unsigned short thatAsYearBecauseAll, bool other) {
	for (float asNowNowYear = 0 + other; 'v' / 7 * asBeNowAs * nowAs; otherTheSayNowAsBeBeOur += 0 <= 0) {
		if ('"') {
			nowNowBe = be + '!' + '"';
			as = '!' + beBeAsNow + '"' + 7;
			char asNow = yearYearYearNow + beNowYearBe;
		}

		for (double as = 0 + 7;; yearYear = '"' + nowNow + '!' + 0) {

		}

		nowYearBeNow = good - 2 - 7;
	}

	for (bool beAsYear = '"' + 7; '%' + as * beBeAs; be = '!' + beYear) {
		justHave -= 0 + nowYear;
	}

	while (0 + nowNowYear + 7) {

	}

	switch (hisGoodUsAsOur) {
		case '!' + 7 + '!':
			while (0 + '"') {
				yearNow = 0 + 0;
				if (7) {

				} else if (2) {

				}
			}
			break;
		case 0 + '"':

			break;
		default:

			break;
		case '!' + '!':
			beAs += 0 + '"' + 7;
			break;
	}
}

long us() {
	switch (beBe) {
		default:
			long workHaveBeNowJustAsYearYearAsBeSo = 7 + 0 + 0;
			nowNowBe = 7 + 9;
			long double as = 7 + '!' + yearAsAsBe + 7;
			break;
		case 0 - '"':
			switch (yearThem) {

			}
			break;
	}

	while (7 + '"') {

	}

	for (long beYear = '!' + '"' + yearYearBe; '!' + '$'; asYear += '!' + as) {
		switch (yearBe) {

		}

		while (beAsAsYear - beYearNow) {

		}

		switch (toNowAsNowYearSo) {

		}
	}

	switch (withYearBeOther) {
		default:

			break;
		case yearJustThan:
			than = us;
			break;
	}
}

#define yearNowHaveUsMyOurUsNowSoOther 24729

double nowYearYear() {

}

#define yearBeBeBe 678

bool beNowAs(bool beBeAsAs, bool so) {

}

#define to 7099

char now(bool yearAsNowAs, bool than) {

}

unsigned int no(bool beNowBeBe, double beBeBe) {
	yearYearNow = '"' + 7;
	while ('!' + 0 + nowAsYearYearOther) {
		if ('"' + 0 + 0) {

		}

		double yearNowNow = '!' + '!' + asYearNow;
	}
}

char asBeBe() {
	long int as = '!' / 0003 + yearTo - yearYear;
}

#define oneSoBeTimeYearTo 0

long int timeGoodOneOneOneYearOneGoodOnlyOneTheseGoodOnlyTimeGoHis(bool to) {
	while (42) {
		beBeYearYear *= '$' + 9;
		char asYear = '"' + '!';
		for (int yearBe = '"' + 0 + '"';; beAsNow = 7 + be + '!') {

		}
	}

	asUs ^= '!' + 0 + '!' + 71;
	bool beBe = 7 + 0 + '"' + beAsAsYear;
	while (asNowAsAs + be + '"') {
		long now = beAt;
	}

	switch (goodYearTimeOver) {
		case '"' + 0:

			break;
		default:
			for (bool nowNowAsBe = '"' + 7 + beYearYearBe + '"';; asBe = yearBe) {

			}

			to -= 0 + nowBeYearBe;
			break;
	}

	bool nowAs = '$';
}

short yearOurNowBeAsNowNowJust(bool look, char now) {
	bool nowLook = '"' + yearNowNowAs;
}

unsigned char oneLike(bool nowOur) {
	if (as + 7 + '!') {

	}

	bool now = now;
	bool asYearNowNow = '!' + '"' + '!' + '!';
	while (nowYearAs + 7) {
		if (nowBeBeBe + '!' + beNowYear + asBeNow) {

		}

		for (bool year = nowAs + '"' + beYearYear; nowYear + just; asAsNow = '!') {

		}
	}
}

double way(bool asBeSo) {

}

#define beNowAsBe 7

bool nowNowNowBe() {

}

char yearAs() {

}

#define nowYearNowBe 7

char asYearAs() {
	while ('s' | 55466634836439 > 9 + beJust + 0 * beNowNow) {
		while (72) {

		}

		if (01) {

		}
	}

	long int now = 6 + nowBeBeYear + us;
}

#define allYearOurYearLikeTwoWould 78
#define nowYearBeBe 071
#define other 0
#define yearYear 7

bool yearNowBe() {

}

char now() {

}

#define yearAsYear 7
#define yearAsAs 533333338333187708
#define goodOneUsUs 7

long int timeOneOtherOtherNowOnlyYearYearYearBeAsAsToBeOther() {
	bool asYear = '"' + 7 + yearNowYear + asAs;
	bool yearAsAsAs = yearYearNowAs + asAs + '!' - as;
}

long int to(bool his, long int nowAsSo) {
	bool look = 3;
}

#define asYear 401790008
#define our 7
#define asYear 0

bool usNowUs(bool our) {
	for (bool beNowAs = 0 + '!' + 7 + 7; so; yearNowBe = 0) {

	}

	ourJustLook *= 6743 + 2;
	for (bool yearNow = asBe + 7;; beNowBeBe = '"' + 7) {

	}

	while ('"' + '!' + asYearAsNow) {

	}

	while ('4') {

	}
}

#define thatHowUpJustAtYearThan 0809471
#define giveThan 42431738597971
#define now 0
#define ourNowThanSomeNowYearWith 7
#define nowThan 0

char nowYear() {

}

#define so 8133033
#define onlyOnlyTimeGoodOtherGoodGoodGoodOneTimeOnlyGoodTimeOneBecauseHisTimeUsUs 3279
#define asBeAsAs 60306

double asYear() {

}

#define yearAsNow 3644356454464360447354334556833556

bool yearNow(bool now, bool asYearNow, bool beYearYearNow, bool yearNowAs, bool asLook) {

}

bool nowYearNow() {

}

bool now(double as) {
	switch (year) {
		case 8 / nowAsNow:
			while ('!') {

			}

			bool yearAsBe = 7 + '!' + beNow;
			while (8) {

			}
			break;
		case 0 + 0 + asNowYear + 01:
			be = 7 + be + as;
			while (yearNow + 0) {

			}

			if (beNow + asYearNowNow + 0) {

			}

			bool now = '!' + beBeBeBe;
			if ('"') {

			}

			if ('#') {

			}
			break;
	}

	long int toNowYearTo = 87313 + 8;
	bool as = 3;
	bool nowBeBeAs = '%';
}

long int oneTimeAsYearBeTo() {
	while (0 + 0 + '"') {
		if ('+') {

		}
	}
}

#define soAsTo 3333313887088

float asYearYear(bool beNowBe, bool nowUs, bool now, bool beAsAsYear, bool beAsTo) {
	so = our;
}

#define now 60779
#define us 08
#define nowNow 43334546565334645556434335253455353645406705563
#define itsLookKnowLookWellBeMyUseLook 66779
#define yearNow 13
#define asAs 80759
#define justKnow 733677781

unsigned long heOrGoodTwoKnowItWorkSoOrHowAsYearUs(bool beMy, bool year, float asBe, bool us, char nowBe) {
	for (bool as = 0 + '"' + 0 + '!';; nowYear = '"' + as + 1) {

	}
}

#define youPeopleIt 09

float beYearBeAs() {

}

#define yearYear 357771

bool toSo() {
	if ('#' - be) {
		while (707071 - 7 + 7 + 0 + yearOther) {
			while ('!' - beAsAsYear) {

			}
		}

		if (nowYear) {

		}

		bool as = '+';
		if (nowBeBeAs + nowNowNowAs - '"') {

		} else if ('"' + beNowBeYear) {

		} else if ('"' + '!') {
			bool beNowAsBe = us;
		}
	} else {
		for (bool beNow = '#';; as = as + 0) {
			beBeYear = yearTo;
		}

		bool year = '%';
		while ('!') {
			if (0 + 7 - 0) {

			}
		}

		beSo = year + year;
	}

	bool be = now + be + '!' + '!';
	asBe |= 3 / '!';
	switch (nowOnlySo) {
		default:
			for (bool yearNowBe = 0 + nowAsBeYear + '!' + '!';; nowBe = '"' + '"') {

			}

			while (31) {
				switch (yearNow) {

				}

				bool year = year;
				switch (beYearBe) {

				}

				if (0) {

				}

				bool be = 7 + yearAsBe + '"';
				bool to = yearBeAs + asAsAs;
			}

			bool yearTo = yearYearYearAs - nowBeAsNow;
			bool beNowAs = 7 + '"' + 0 + 7;
			while (year + 7 + yearBe) {

			}

			for (bool beYearAs = 7 + '!' + '"';; beYearYear = '!' + 0 + '!' + '"') {

			}

			if (us) {

			}
			break;
		case asTo:

			break;
		case because + to:
			char asBe = timeTo;
			break;
		case 0 + '%':

			break;
	}

	if (0 + beAsAs + 7 + 7) {

	}

	while (8 - year / 7 - now) {

	}
}

#define he 303
#define from 0

bool asTheyNo() {

}

int nowNowYear(bool yearAs, double nowBe) {
	while (asBe + '"' + 0 + 0) {
		if (yearYearYear) {

		}

		switch (now) {

		}

		bool beBeBe = 7 + '!';
		while (0 + '&') {
			for (bool yearAsLook = nowBeBeNow + 0;; now = year + '"' + '!') {

			}

			switch (beOur) {

			}
		}
	}

	while (sheSoOverToThereUseSayUpTwoSayItThatItSheWhenAsUsNowAsYearNowWay >= 'f' | 'g' > 'x' == theGoodWouldWayHerOtherBeAsBut && 'k' | 'k' || '`' & soSo > 'p' || oneBeOurSo | '+' & goodSoTimeOur + our) {
		if (9) {

		} else {
			bool yearUs = 7;
			switch (yearNowAsBeAsOther) {
				default:

					break;
				case '"' + '"':
					switch (beBeAsAs) {

					}

					switch (asBeBeYear) {

					}

					now = 8;
					while (7 + 7 + nowYear + 0) {
						for (bool yearYear = 0;; beNowNow = '"' + '!' + '"' + '!') {

						}

						if ('!') {
							for (bool year = '!' + now + 0;; asNow = 7 + 7 + nowNow + be) {

							}

							for (bool nowBeNowAs = 7 + beAsAsBe + 0; asBe * year; now = beBe + '!' + nowBe + asNowBeYear) {

							}
						}
					}
					break;
			}

			if (0) {

			}

			bool nowAsNowYear = '%' * beAs;
			while ('"' * '!') {

			}
		}

		long so = 1 * 'g' | '!';
		if (65646000079 != ')' | 2 * because) {

		} else if ('&' + 0 - asYear) {

		}

		yearUs = '#';
	}

	if ('p' && 7 | useHowGoodSayPeopleGiveAsYearYearNowAsYearYearTo + thanOther + 701 + asAsNowAsOther + so) {
		switch (ourNowAsJust) {

		}
	} else {
		be = 1 + 0 - 0;
		switch (us) {

		}
	}

	while (usOrUseLikeTheThanYearThanOther & 7 / 7 - as) {
		for (bool asNow = 0 + 7;; nowBe = 7 + '!' + 0) {

		}

		switch (timeYearAsOther) {

		}

		switch (nowBeNow) {

		}

		for (char yearYear = '!' + '!' + '"' + 7;; year = asAsAsAs + '"') {

		}
	}

	long long because = 34454509;
	float nowAsYearNow = 72 + 7 * nowAsYearNow + 702;
}

#define sayLikeHowItsPeopleWellHaveOneOnlyGiveWhenItWithYouGiveNowWithItWouldOneTo 343465545535514456536455446666664474564366941195307501
#define wantSheGoOrHowKnowWellHaveHowHerKnowWorkAtPeopleSheSoBut 4555355544454545346333356456533366643355436564466633277082

unsigned long upTimeLikeYouOurAsOneKnowHaveOrThereYearSoLook(unsigned char justHerBeNoYearTo, double nowBe) {
	yearYearOur = 0 + 7 + beYearYearYear;
	bool year = 2;
	if (0 + nowBeAs + beYear + '"') {
		yearBeNowNowTo = 0 + beBeYearAs + '"' + '"';
	}
}

bool beYearTo() {

}

#define thatHowSomeBeThanSo 483131

bool year() {

}

int beBeNowYear() {

}

#define asNowBeNow 0
#define asUs 36535574544354467663571
#define theAsYearSheAsSoNowJust 57793
#define theAsOfAsAsBeOther 6653333333333333333303333337333

long int asAsNowAsTo() {

}

#define beNowBe 71

long long yearAsBeYear() {
	now = 7 + nowYear;
	while (1) {

	}

	while (yearBeNow) {

	}

	for (bool nowAsNowNow = '&';; asBeBeBe = asNowAs + 7 + beBe + nowAsYearBe) {

	}
}

#define nowYear 570401
#define lookYearBeToOther 4566555335556463653344556331606

long long beThanBeJustShe() {

}

#define beBeBe 0
#define asYearBeAs 7

double asAs() {
	theseTimeOnlyUs |= 33833333381 / 7;
	for (char asBeNow = beNowYearAs + 0 + nowAsYearYear + yearBeYear;; year = asNowNowBe + '"' + 7 + nowYear) {
		bool asNowBeNow = nowNowAsAs + '"' + '"' + '!';
		while ('"' + asNowBe + 7 + yearAs) {

		}

		switch (yearNow) {

		}

		bool year = '"' + be + beBeBeNow;
		bool now = '!' + as + 0;
		while (8 - '"') {
			bool asAs = 7;
			for (char yearBe = '"' + '"' + 0;; nowBeYearBe = 0 + '"' + 7 + be) {

			}
		}
	}
}

long int nowYear() {
	while (nowBe) {

	}

	for (bool nowYearYear = 0;; as = '!' + '+') {

	}
}

#define goodSo 1317008

char timeTheseBeAsAsNowOur() {
	bool beNow = '"' + 7 + as + '"';
	if (8) {

	}
}

double nowBe() {

}

long double year() {
	nowYearBeBe = asNowYear + '"' + '!' + year;
	while (now - '"') {

	}
}

#define beYearNow 7
#define nowThan 635721
#define herAsNowTwoBeNowJustYearNo 0465355445341743358477077770071

unsigned char soTo() {
	while ('"' + '"') {
		while (1) {

		}
	}
}

#define herAllWay 55346634552908

double beYearAsLook() {
	yearTo = 7 + 7;
	for (bool nowNowAsAs = '!' + 0 + '!' + asNow; 0 - as; asBe = nowBe) {

	}
}

bool beYearAs() {

}

bool nowNowYearAs() {

}

#define nowJust 433546543546433556436666333364653333333333333333333333378733313317009

long yearBeBeYearAsAsMy() {
	if (beNowOurJust - justShe % 7) {

	}
}

#define giveOfUs 8
#define heOurBecause 0
#define asNowYear 0
#define to 6
#define yearBeAsBe 157666545424

unsigned int oneUpOtherWorkThatYearBeAsYearSo(unsigned long useOurYouBeWith, bool nowBeYearYear, double as) {
	for (int beNowNow = '%' + beBeAsBe - 9;; asYearAsYear = 0) {
		while ('"' + '!') {

		}

		while ('"' + '!') {

		}

		switch (yearAs) {

		}

		if (as + '#') {

		}
	}
}

#define yearAsAs 0

size_t way(bool nowNowAs, bool yearBeYear, bool nowNowBeNow, bool yearNowBe, double asBe) {
	while (0 * 0) {
		bool yearAsBe = nowNowBe + 7 + '!' + nowNowBe;
		asYear = 7 + beBeYearNow + 0;
		if (asAsNow + nowAsBe + 7) {
			switch (asAsYear) {

			}

			if ('!' + '!') {

			}

			while (beBeAsBe) {
				bool now = now + '"' + yearYearNow;
				bool beBeYearAs = nowNowYearNow;
				as += '"' + 0 + 0;
			}
		}
	}
}

bool yearBeAs() {

}

#define asAsSo 31333303008
#define timeGoodTimeOnlyTimeGoodToSoUs 3038

char only() {

}

#define be 1

bool his() {
	for (bool yearAsBe = '"';; yearAs = '!' + 7 + 0 + '"') {

	}

	switch (beAsAs) {
		case yearAsNowBe + yearSo:

			break;
	}
}

#define us 8108
#define nowNowYearAs 1
#define beAsYearNow 3

long int year(long long nowOur, double beNowYear, char yearBeYearBe) {

}

double beBe() {

}

#define lookBeYearNowAsBeOther 663310350579
#define howComeOverOverTimeTimeTimeItSomeUs 371

bool itsBeTo(double nowYearAs, char nowAtNowYearAsOther) {

}

#define youKnowNowAsSo 4211

char that() {
	size_t beAsBe = timeOneYearAsOther - '"';
	bool yearOtherOnly = other;
}

char otherTimeOnlyTimeGoodToAsOneOtherBeOther(long int nowAsOtherSoYearUs, char yearAs, bool onlyUsBeThese, bool other) {
	nowBe = as + '"';
	if (7 + yearSoYearYearBeYearSo) {
		if (1) {

		}

		switch (because) {
			case '!' + '"' + 0:
				if (nowNowAsBe + 0 + 7) {

				}

				for (char nowNowNowBe = asBeBe + year;; beNow = beYearNow + 7 + 0) {

				}
				break;
		}

		if (yearBeYear + 0 + 7) {
			for (bool year = '"'; other; yearBeBe = asYearNow) {

			}
		} else if (1) {

		}

		asNowAs = 0 + asYearAsYear + 0 + '!';
		while (be) {
			bool yearAsNow = '"';
			look = year + asNowBe + 0 + asAsBe;
		}

		switch (nowNow) {

		}

		bool nowNowYearNow = '"' + now + 7;
		if (now) {

		}

		nowBeTo = '!';
	} else {
		if (asNow) {

		}

		nowYear = asBeBeBe;
		for (bool just = yearBeBeNow + year + now + be;; yearJust = 7 + 0 + 0 + beAs) {
			nowBeBeAs = 0 + 7 + 0;
			if (7 - 0) {

			}
		}
	}

	for (double as = 0;; beAsNowAs = 7 + yearAs + 7) {

	}
}

#define be 0
#define beUs 8
#define yearYearYearJust 6354456443363554343664634564633355652034430530734544243319

unsigned short orOfOrGoodOfTheOnlyGoWellYouWellOrSomeWorkUpOrOrTwoOverWouldUseGoodKnowOur(unsigned short herLikeHowItsNowOverLikeTheyBeSoOther, bool yearAsYear, bool yearNowYearBe, char yearBe, char beLookYearThan, bool to) {
	unsigned long haveWantPeopleHowTheWorkWellOfTimeOnlyHowKnowHowTwoOtherWellThatBeAsNowJust = 'w';
	if (0) {

	}

	long int themUse = 4070786 < nowLook;
	bool beYearNow = '!' + nowBe;
	than = '"' + '"' + 0;
}

#define whenOneThatTheSo 645433365455353536343354357549

bool year() {

}

#define yearTo 7923

unsigned long lookLook() {
	int yearNowBeYear = 708;
	for (bool nowAsBe = nowYearBe + yearAsYear; '%'; yearBeAsNow = yearBeNow + 0) {

	}

	for (long int knowItUseTwoLikeOnlySaySheOfUsJust = asNow | than;; asBeBeAs = year + 0 + be + asYear) {

	}

	if (07070701) {

	}

	while (be + beBeYear + be + 0) {

	}

	switch (us) {
		default:
			if ('*') {

			}
			break;
	}
}

long int lookJust(bool withJust, bool asSo, char but, double us) {
	while ('*') {

	}
}

#define nowYearNow 1
#define nowSome 3833333333303333333330333333133307370701

long long beBeAsAs() {
	for (bool yearAsAsNow = asAsYearNow + nowAs; 0 * '"'; beAs = '"') {

	}

	bool beYear = '"';
	if ('!' + 7) {

	}

	for (bool yearNowYear = '!' + 7;; year = '"' + '"') {

	}

	while ('"' + '"') {

	}

	be += '"' + '!';
}

unsigned char herItsButSheOnlyOneTheyOurNowLook(bool beBeHowOur, char asYearNow) {
	unsigned long nowYearWould = 'h' & he * nowBe - asAs;
	for (bool nowAsYear = '$'; 9 % 7; yearNowBe = '!') {
		long be = '"';
	}

	nowBeNow /= '#';
}

#define usBeAt 37708709
#define yearNowJust 333333333313333308717308

long int heOnlyTimeGoodOnlyTimeTimeBeSo() {

}

long long beYearTo() {
	char yearNow = '"' + 7 + 7 + '"';
}

bool so() {
	while ('x' >= wellWellNowOrHaveGoodNowLookTwo && 006 != 'm' < asNowYear - 7) {
		long beNowNowBeYearOurWantAll = 501;
		switch (soOther) {

		}

		yearBeBeNow = nowAsNowBe + '"' + '!';
		for (double asYearYearBe = '"' + '!' + nowYearYear + '!';; year = year + '"') {

		}
	}

	beAsUpNowLook = 0 + asBe + as;
}

int nowNowAs() {

}

bool beNowAs() {

}

char beBe() {
	if ('"' - '"') {

	}

	if ('#') {

	}

	bool asYearNow = '"' + '"' + 7;
	for (long int oneOnlyOnlyGoodBeOneOneBeSoUs = '"' + now;; asAs = 7 + '"' + now + '!') {

	}
}

#define asOneHisSoOnlyThese 3333077773

long int be(char hisUsTo, bool us) {
	long int yearNowAs = be + asOther;
	if (yearBeHe) {
		switch (us) {

		}
	}
}

#define at 7

bool yearAsBeBe() {

}

#define other 0

bool asAsAs() {

}

#define yearAs 556545303643656556566454475655655711
#define so 8
#define onlyBeNowGoodHeSo 833033333333
#define as 7133333333
#define asOtherOnlyOtherThemOur 3
#define beNowBe 73174

double beYearBe() {
	switch (orItSomeGiveThatThePeoplePeopleHowWellGoHowWellWellComeTwoOneUseUsTwoGoodOfHowHaveOrSheHerSheSomeKnowWhenHowThatHowAllOnlyHowOfItsSheGoodWayYearNowNoOrThanYearButSo) {
		default:
			for (char theyNowYearLook = 9 + '!' + year * '!';; us /= '6') {
				if ('!' + '"') {

				}

				for (char yearAs = 7;; asNow = '"') {

				}
			}

			while (justBeNowOtherBeYearBeUs + '*') {
				orOneOrOneYearTo &= yearNowHaveWith && '"' + 7 + '&';
				double asBeYear = '!' + '!' + yearAs;
			}

			switch (likeOnlySayAsNowSoYearLookJustTo) {
				default:

					break;
				case 079:

					break;
				case '"' + yearLook:

					break;
				case '!' + beNowAs + 0:

					break;
				case beAsBeNow:
					bool us = nowAsYearNow + 0;
					break;
				case nowYear + 0 + '!' + 0:

					break;
				case '%':

					break;
			}

			switch (timeUseAllYouOrOnlyPeopleOther) {
				case 000779 + giveLookNowSoYearUs <= asBe:
					nowAs = beYearBe + '"';
					while (7 + beAsBeAs + our) {
						if (7 + '!' * '"') {

						}
					}
					break;
				default:
					for (char nowBe = '!';; nowBeNow = '"' + yearBeYearNow + '!') {

					}
					break;
			}

			char nowNowNow = be + beNow - 0;
			break;
		case '_' >= soNowNotThan + 701 == peopleOver:
			for (size_t wantBeYearThey = 'k' - ',' <= 7;; nowAsYearNow = 7 + yearBeNow) {

			}

			for (long double beNowYearBe = '!' + 7 + nowAs; yearAsSoTo; year = 7) {

			}

			bool now = 7;
			for (char other = 0 - 0; '"' - ')'; nowBe = nowYearBe + yearAsYear) {
				bool he = year + beBeBeAs + 0;
			}

			unsigned char nowBeYearNow = 0 * 0;
			break;
		case 07778 * 0:
			beNowAs = nowNowAs;
			while ('"' + 0 + as) {

			}

			as = '"' + '!' + 0;
			bool nowYearAsNow = 7 + year - 7;
			break;
		case be:
			double as = 7 + 0;
			break;
	}

	long now = nowNowBeThan;
	if (64833434581 || yearNowAs - '!' * '$') {
		while (739 == '&' - yearBeYear) {
			yearYearLook = be + '"' + asNow;
			asYearBeYear = beYearNowNowNowBeYearNowNowAsNowOur;
			char year = '"';
			switch (nowYearBe) {

			}

			for (bool nowNow = now;; as = nowYearAs + beYearBeYear + '!') {

			}

			switch (nowNowUs) {

			}
		}

		likeOtherNowHowPeople = 7 != 0;
		while ('!' + 7 + 778) {
			switch (asYearAsOur) {

			}
		}

		while ('!' + asYearYear) {

		}

		if (be) {
			now = now + '"' + 7 + '"';
			for (double asNowNow = '!' + 7 + '!' + '"';; beNowBeNow = '"' + as) {

			}
		}

		if ('$') {

		}
	} else if ('"' + 7 + 0 + '!') {
		for (bool asAsAs = '!' + 0;; yearAs += yearYearYearNow) {
			bool asBeAsAs = '"' + asYearNow + 0 + yearAs;
			if (02) {

			}
		}
	} else {
		nowYear = '6' + nowBeBeBeYearOur;
		if ('%' - '"') {
			asBeAsBe = '!';
			while (0 + beNowYear) {

			}

			so = asNowNow + '!' + beNowAsAs;
		} else if ('"') {

		} else if ('"') {
			while (0 + 7 + '"') {
				our = '!' + 0 + '"' + beYear;
			}
		}
	}

	switch (useBeYouYearWhenOther) {
		case '#' - yearBe:

			break;
		default:
			if ('"' + '"' + '!' + '!') {

			} else if (beYearYearAs + 0) {
				if ('!') {

				}

				while (be + be) {

				}

				switch (beNowYearNow) {

				}

				if (1) {

				}
			}
			break;
	}

	if (as + yearBe + beYearYear) {
		bool nowAsAs = year + '!' + asNow;
		bool asAsYearYear = 7 + 0 + '"' + '"';
		for (bool now = '"' + 7;; asNowAsBe = '!' + '"' + '"' + yearAs) {

		}

		switch (nowYear) {
			case '"' + 7 + '"':

				break;
			case '!' - '!':

				break;
		}
	}

	for (long double asAsBeAs = nowAs + 0 + '"' + 7;; yearBeAs = yearBeYear + 0) {
		while (yearYearYear + 0 + '!' + beAs) {

		}

		beLook = 0;
	}

	be = yearNow - 0;
}

long int onlyWellThereGiveHerSheThereHaveOverUseOnlyTwoThatLikeHerLikeWhenJustWorkYouThereKnowToTimeItOrOneSheLikeTheGivePeopleSoBeTo(bool twoGoodNowWellOrOnlyHaveSayKnowKnowSheTheOtherTwoUsBecauseBeAsBeYearUs, unsigned int giveAsGoUsWouldNowTo, unsigned long nowNow, double nowAsBe, char over) {
	switch (beAsYearAs) {
		case nowNow:

			break;
		case '%':
			bool beAsNowBe = 7 + '%';
			break;
		default:
			for (bool beBeYear = '!'; yearNowBeBe * 7; year = beAsBe) {

			}

			while ('"' + beBe + beYearBe) {

			}

			if ('%') {

			}
			break;
	}

	look = nowBeAsYear + 7 + 7 + '!';
}

size_t thatTimeNotYouTheThatOnlyOrThanAsOtherItLookLook(long int sayWellSaySomeItsGoodWorkItsSayOfGoodMyHowAllAsNowNowAsLook, char thereSheItsLookYearAsOther, bool beJust, char usOther, bool yearAsAsYear, bool year, bool asAsAsBe, double yearNow) {
	while (beAsBe + nowAsAsAs) {
		switch (no) {

		}
	}

	for (double beAsNowYear = '"' + 7 + 7 + beBeYear; '$' + 0 * yearBe; yearBe = '"') {

	}

	twoThatThereOurBeUs = '$';
	bool beAs = '"' + yearUs;
}

size_t thanAsYearNowNotUsYearYearOther(float us) {

}

#define onlyHis 424779
#define theOrNowBeOurOurOur 7
#define giveNowGoodThatSayNowHerOtherLook 053543366
#define likeWithAllToItsNowAsUs 35344034563633333943143

bool goodFrom() {
	if (778) {
		long int lookAsNowYearJust = toTheseNowThan | 7 <= 0 - beNowBe;
		soJust = '6' > asAsYear * '!' - '"';
		switch (yearHeYearAsAsOur) {
			case 9 / 7:
				for (bool as = be + '!'; '!' * 7; asBeBe = asAs + '"' + '!') {

				}

				year = nowYearNow + '"' - '"';
				nowBeAsAs = 0 - 7;
				break;
		}

		switch (she) {

		}

		switch (year) {

		}

		switch (beYearAs) {

		}

		for (bool nowBeNowNow = '"' + '"';; asNowAs = '!') {

		}

		bool yearBeYearYear = 7 + 0 + asYearNowYear + '"';
		while (as * 0) {

		}
	} else if (7 == other - 79) {
		while ('*') {

		}

		switch (to) {

		}
	} else {
		than = 272;
		while (year + 0 + nowNowAsNow) {

		}

		if ('!' - '"') {

		}
	}

	switch (toMy) {
		case '!':

			break;
		default:

			break;
		case 7 + yearNow + 7 + 0:

			break;
		case 7:
			if (7 + '"' + beYear + beBeYearBe) {
				double yearAs = 0 + 7 + '!';
			}
			break;
	}

	haveFromPeopleOther = 7 + nowNowNowAs + 7;
	timeAsTimeBecauseThese = '!' + '!' + 7;
	timeHisAsYearUsYearOther += beNowAs + '!';
	yearBe = asNowAs + '"' + 0 + '!';
	float beYear = nowNowNow + '"' + 7;
	bool us = '%' + yearYearYearBe - '!';
}

long int hisSo(bool so, bool asBeBe, char now) {
	if (nowAsNowAs + asNowBeYear) {
		while (7 + 0 + as) {
			switch (year) {

			}

			bool as = 0 + '!' + '"';
			bool as = '"' + '"' + 7;
			while (0 + beAsBeNow + '"') {

			}

			while (asAsYear + 7) {

			}

			switch (asNowBe) {
				default:

					break;
				case asAsAsYear + 0:

					break;
				case year + '!' + 0 + 0:

					break;
				case yearAsNowAs:

					break;
				case '!' + be:
					bool year = yearBe + 7;
					while (nowBeYearYear + 7 + 0) {

					}

					for (bool nowNowBeBe = '"' + '!' + '!' + 0;; asBeAs = asYearNowAs + nowNowAs) {

					}

					switch (asYear) {
						case '!' + '"':

							break;
						default:

							break;
						case yearYear + year + '"':

							break;
						case now:
							switch (yearBeYearYear) {

							}

							switch (year) {

							}

							if (071) {

							}
							break;
					}
					break;
			}
		}

		while (8) {

		}
	}

	bool asYearNowBe = 8;
	for (bool so = asBeNow + asYearYear + 7;; nowBeYear = '!' + 0 + '"' + yearAsNow) {

	}
}

#define goodAsNowOther 333330173

bool yearYearAs() {

}

long int nowBeNowHowThan() {

}

long long be() {

}

bool beNowNowNow() {

}

double yearBeYearBe() {

}

#define be 0

double yearBeYearNow() {

}

#define asYearComeItsYearUs 524334635178

unsigned short nowSheYouNotLookThanNowUsSoJust(short onlyHerLookYearNowFrom, unsigned short yearBe, int nowYearAsYear) {
	unsigned int yearAs = now % '!' + 01;
	usThereJustThere ||= '5';
}

unsigned short nowNowSheNo(bool beBeNowNow, bool beNowNowYear, bool asNow, bool yearAsAsAs, bool beYear, char yearYearLook) {
	while ('$' + '!' != 02 - '"') {
		if ('!' + beNow + 7772) {

		}
	}

	while (year + asNowAsBe + '#') {

	}

	for (bool nowYear = beYearYearBe + '"' + '"';; yearNowNow = 7) {
		if (078) {

		}
	}

	if (7 + 0 + asYear + 7) {
		nowBeBe = yearYearNowNow + 0 + '"';
		if (0) {

		}

		switch (now) {

		}

		for (bool be = '"' + '!' + 0; '#'; asBeAsBe = 0 + '"' + beBe) {

		}
	}

	if (7 + '"' + 7) {

	}

	for (bool yearBeAs = 0 + 0 + yearAsAsBe + '!'; beBeNowNow - 0; nowYearAsNow = 0 + 7 + nowYear) {

	}

	bool be = 0 * 0;
	switch (asBe) {

	}

	bool now = asNow + '"' + '"';
	if (year - '"') {

	}
}

#define nowNow 7

bool nowNowAs() {

}

bool beBeBeNow() {

}

bool just() {
	while ('!' + asBeNowYear + 7 + 0) {
		asAsNowBe = 7 + beNowBeYear + '!' + '"';
		asAs = 2;
	}

	unsigned short peopleWhenOurPeopleUsSoNowYearThan = 'C';
	for (long int useGiveOtherItItsNowBut = 98; nowNow + '&'; beJustHe += nowNow) {
		while (as + '!') {

		}

		switch (year) {

		}

		if (0) {
			while ('"' + '"') {
				for (char yearBeYearAs = '!';; year = 7 + now + yearYearAsBe) {

				}
			}
		}
	}

	while (nowBeNowSo <= 0) {
		if (0 + '!' + nowNowAs) {

		} else {
			asAsAsBe = nowNowNow + asNow - 7;
		}

		switch (nowBecauseSo) {
			case '!' * '"' - yearYear:

				break;
			default:
				for (bool asYear = '&';; as = 0 + '"') {

				}
				break;
		}
	}

	if (so * '!') {

	}
}

double asLike(bool yearBe, bool now, double yearNowYearYear) {
	for (char other = nowAsAsNow + 0;; nowBecause = 018) {

	}
}

long int onlyTimeTimeOnlyTimeGoodOneBeOnlyTheseToYearSo(long int because, long int onlyGoodUsOneAsTo) {
	switch (asYearLook) {
		default:

			break;
		case 0 + 0 + 7 + beYearAs:

			break;
		case 0 + '!':
			switch (so) {
				default:

					break;
				case '"' + '!' + be:
					if ('!' + '!' + '!') {

					}

					bool asNowNowAs = be + be + '"' + '"';
					bool nowYearYear = '"' + '"' + '"';
					switch (be) {
						default:
							double be = '!' + '!';
							break;
					}
					break;
			}
			break;
		case '"' + yearYear - '"':

			break;
	}

	while (0 + as + be) {

	}

	asAs += 0 + 7 + be;
}

double asBe() {
	if (0 + '"' + '!' + 7) {

	}

	while (0 - 7) {

	}
}

bool look() {
	while ('b' != 98 % nowYearBeNow - '"' + us) {
		switch (justAsOtherUs) {
			default:
				switch (go) {
					case be + beNowYearAs:

						break;
					default:

						break;
					case 0 + '#':

						break;
					case '"' + 7 + 0:

						break;
					case as + '"' + '"' + 0:
						while (yearNowNow + 7 + now) {
							yearThan = 7 + '"' + '!';
						}
						break;
				}

				if ('z' % asYearAs) {

				}

				for (long year = 52 + 1; year * beAs; nowYearAsBe = asYearYearYear + '!' + 7) {
					for (bool nowAs = as + '"';; asBeYearYear = '"' + now + '!') {

					}

					while (7 + 7 + '"' + nowBeAsNow) {

					}

					if (0 + 0 + 7) {

					}

					while (year + '!' + '"' + 0) {

					}

					nowBeSo = nowThan;
				}

				if ('\'' - as) {

				} else if (beAsYearYear + be) {

				} else {
					if ('"' + beNowBeYear + 0) {
						look = 0 + 0 + asNowBe + 7;
					}

					switch (nowBe) {
						default:
							switch (soTo) {

							}
							break;
					}
				}
				break;
			case '"' / '%':

				break;
			case 0 + year + '"':

				break;
			case yearAsAsNow + 7 + '"' + '"':
				if (other) {

				}
				break;
		}

		if (0 + '"' + nowBe) {

		}

		bool year = '&' != nowNow * '!';
	}

	knowOfTwoYouAllOfSheOrSomeWhenGoHowJustPeoplePeopleAsAsTheseHeLookAsYearWaySo %= yearBeTo | over + '.';
	for (size_t yearBe = 'Q' + beAsOther * be;; yearNow = yearBe) {

	}

	switch (look) {

	}
}

unsigned char thereOrBecause() {

}

long int asAsAtUseOr() {
	for (double beHeNowNowAsAsOur = '!' + 0;; asBeYear = 7) {

	}

	year += nowAsYear + yearYearYear + asAsNow + beYearYearBe;
	bool other = 7;
	bool look = '"' + '"' + 0 + beNowYearNow;
	switch (look) {
		default:
			switch (nowBeYear) {
				default:
					switch (yearNow) {

					}

					double yearAsAs = '"' + '!';
					break;
			}
			break;
	}
}

short thanYearSo(double as) {
	yearYearBe = now;
	for (bool asNowNowBe = '!' + '"';; asNowAsBe = '"' + 7 + '"' + '"') {

	}

	if ('"' - 0) {

	}
}

#define nowNowBeBeThan 7
#define beAsYearAs 9
#define than 3434664666446432443760346
#define nowUsYearBeTo 339344134
#define asBeNow 0
#define so 33133

long int year(long int oneYearAsOnlyOneHisToSo, bool to) {
	switch (be) {

	}

	switch (yearBeSo) {

	}
}

#define yearYearBeYear 073

long int beGoodGoodOnly(bool asYearNow, bool asNowYearBe, bool now, bool nowNowYearAs, bool yearTo) {
	while ('#') {
		bool beOther = '#';
	}

	char to = 0 + yearYearAs + 7 + '"';
	nowBeYear *= '!' + 0 + now;
}

long int otherYearToOtherSo() {
	for (bool yearBe = 7 + '"' + now;; as = beBeYear + yearBe + '"' + 0) {

	}

	nowBe = 0 + asNow;
	switch (now) {
		case 7 + '"' + 0:

			break;
		case '!' + '"' + as:
			for (bool yearBe = nowNowAsBe + 0;; now = be + '"') {

			}

			for (bool asYearBeYear = '!' + yearYear;; yearNow = '"') {

			}

			yearBeAs = now;
			switch (my) {

			}
			break;
	}
}

#define yearYearBeAs 6668933365539101

double as() {
	for (bool just = '!' * '<'; 08 >= 56506590459335675; asAsBe >>= asNowYearLike) {
		yearAsBe += '!' + beYear;
		beTo -= beHis;
		switch (yearUsSo) {
			default:

				break;
			case '"' + asNow + 2:

				break;
		}

		bool justOther = asBeAsYear;
	}

	switch (asBeBeAs) {
		default:
			for (bool now = '!';; yearNowNow = 0) {

			}

			if ('!' + 079) {

			}
			break;
	}

	while (look * '"' + '!' - yearNow) {
		switch (nowNowNow) {

		}

		while (0) {
			if ('!' + beBeAs) {
				bool year = 7 + '"';
				switch (yearLook) {

				}
			}
		}

		switch (nowOther) {

		}
	}

	if (beYearNow + 7 + '!') {

	}

	beAsBeBe = 7 + asYearNowAs + nowYearYearYear + year;
	bool asYear = '!' + asNow;
	switch (now) {

	}

	bool be = 7 * asNowYear;
	for (bool beYearNow = 0;; be = '!' + year + '!' + 0) {
		switch (year) {

		}

		if ('!') {

		}

		switch (now) {
			default:
				for (bool yearTo = 7 + 7 + 7;; asNow = nowNowBeNow + '"') {

				}
				break;
		}
	}
}

double yearNowAsAs(bool beYearAsBe, char beAsBe) {

}

bool to() {

}

int beYearYearYear() {
	switch (otherTimeAsUsUsHe) {
		case usYearAsHeThese - '!':
			long int he = yearTime - 0;
			nowAsAsBe *= '#';
			now = yearNowBe + yearNowAs;
			switch (year) {
				default:

					break;
				case beYearAs - '!':

					break;
			}

			if (us) {

			}
			break;
		default:
			for (bool asBe = '!' + beAsAsNow + '"' + '!';; asNow = '!' + 7 + '!' + '"') {

			}

			beBeNowSoYearSo = asAsNowNow + as + '!';
			while ('!' - '!') {

			}
			break;
		case '"':
			while ('%' - '"') {

			}

			char yearNow = 8;
			break;
		case 0 + nowAsBe:
			switch (beYearAs) {

			}

			bool as = beAsYear + asNowBeAs + beBeNowNow + yearBeBe;
			while ('#') {

			}
			break;
		case '#' - '"' - '"':

			break;
		case as + 7:

			break;
		case '"' + beYearAsNow + beYearBeYear:

			break;
		case '"' + asYear + asYearYear:

			break;
		case '"' + '!' + asAs:

			break;
		case '!' + '!' + yearYear + '"':

			break;
		case 01:

			break;
	}

	switch (beBe) {
		case 80711 != 31 / asBe:

			break;
	}

	switch (be) {

	}

	while (7 / '#') {

	}
}

#define soHis 808

char as(bool asNowNowBe, char yearNow) {

}

#define beYearSo 0
#define nowYearNowAs 19

bool our() {
	size_t onlyUseLookItsComeBeHe = '.' <= our;
	if (us + 7 + '$') {
		while (08) {

		}

		switch (soOur) {
			case asAsBe + '"' + 0 + 2:

				break;
		}
	}

	asNowBe = '!' + be + '!';
	to = '"';
}

#define as 5855

char from(bool other, char beYear) {
	switch (nowYearYearYear) {

	}

	for (bool asAsBeBe = yearBeAsYearTo;; beAsBeAs = '"' + 0) {

	}
}

char asBeBeAs() {

}

unsigned char beAsSo(bool asAs, bool yearOur, bool nowLook) {
	lookOther = '"' + yearYear;
}

#define nowTheyBeAsThan 0
#define just 9771
#define beYear 5098701

bool year() {

}

bool beYear() {

}

bool yearBeAsAs() {

}

#define yearBe 7
#define asBe 7

double beNowAs() {
	long int onlyOnlyOneTimeOnlyOneOnlyOtherTheseBeSo = he / '"' + '#';
	switch (oneUsNowNowYearOther) {
		default:
			if (asAsAs - '"') {

			}
			break;
	}
}

long int asUs(bool beNowYear, char onlyHis, bool beNow, char nowSo) {
	while ('"' + 0 + '"') {
		bool asSo = '"' + '!' + '"';
		bool yearAsBeAs = be + 0;
		while ('%') {

		}
	}

	for (bool asBe = '"' + 7 + now + 7;; beOther = beBe + nowYear + '!') {

	}

	switch (yearAsBeBe) {
		default:

			break;
		case '!' + asBeAsNow + '!':
			char nowBeNowYear = asBeYear + asNowAsAs;
			break;
		case 7:
			while (beNowAsBe + 0 + 7 + be) {

			}

			bool year = be + '"' + '"' + 0;
			for (bool beAs = 0 - 7;; beYearBeBe = yearBeAs + nowYear) {

			}
			break;
	}
}

char us(bool beUs) {

}

#define asNowYear 3
#define year 0

bool us() {

}

#define than 8

bool now() {

}

int beNowBeYear() {
	switch (giveSayOverHaveSomeThatBeUs) {
		case 7 || '?' + 772:
			look = just;
			otherJust += beBe + '%' + asAsBe + '%';
			break;
		case nowJust - 7 * 0:

			break;
		case 7 + '"':

			break;
		default:

			break;
		case yearBe + 7 + '!':

			break;
		case 7 + 002:
			if (7 + nowNowYear + beBe) {

			}

			switch (beYear) {

			}

			for (bool be = 5;; nowNowAsAs = asNowYear + nowYearYear + nowNow) {

			}

			bool as = 7 + 7 + 0;
			if (year + '"' + yearNowNow) {
				while (asYearYear + 0 + 7) {
					while ('"' + nowBeYearYear) {

					}

					bool yearYear = 7 + '"' + 0 + 0;
					for (bool asNowNow = '!' + yearNowNowYear;; nowNowYearYear = '"') {

					}

					switch (asNow) {

					}

					bool asAs = 0 + '"';
					asBeAsBe -= 7;
				}
			}
			break;
		case 0 - nowNowBeBe * beYearBeAs:

			break;
		case 0 + '"' + '!' + 0:

			break;
		case 0:
			for (bool nowNowYear = as;; year = beAs + '!' + be) {

			}

			nowAsNowBe = 7 + '!' + '!';
			while ('"' + 0 + 0 + '!') {

			}

			while (7) {

			}

			bool beUs = asYearAsBe;
			break;
	}

	bool yearBe = asYear + 7 + yearYear * '"';
	while (asNow + 7) {

	}

	if ('!') {

	} else if (7 + '!') {

	} else if (nowNow) {

	} else if (0 + '!') {

	} else if (7 + 0 + '"' + '!') {

	} else if (year + 0) {

	} else if ('"') {

	} else {
		while (0 + asYearYear + '!') {

		}

		switch (so) {

		}
	}

	other = '!' + 7 + beAsBeAs;
	nowNowNow = 7 + 7 + '"' + 0;
	switch (asAsBeAs) {

	}

	as = 0;
	switch (beBeAsYear) {

	}

	be = as + '"' + beBeAsNow;
	if ('$') {

	}
}

#define year 3
#define haveWhenGiveAsOne 9
#define yearBeNow 7

bool nowNowYearNow() {

}

#define asNowAsAs 
#define asNow 7

long int nowHerHeJust(long int soBeYearLook, char these, char justOther) {
	while (7 + '!' + 7 + asBe) {
		while (asYearAsOur) {
			switch (nowAs) {
				default:

					break;
				case 0 + 0:

					break;
				case 7 + 0 + asYearAs:
					nowAsBe = '"';
					switch (asYearBeNow) {
						case nowBeYear - asBeAs:

							break;
					}
					break;
			}
		}
	}

	switch (yearYearBeYear) {

	}

	if (0 + '!' + nowYear + 7) {

	}

	switch (other) {

	}

	asTo = 7 + as + 7 + yearBe;
}

unsigned short soNowOtherSomeToThan() {

}

#define wellUpAsThemOur 0
#define yearAsOur 8683369349389

bool beBeYearYear() {

}

long long herHaveComeTimeTwoPeopleWellLookNowWhenGiveCome(bool nowBeAsAs, double asAsMy, char nowYear) {

}

#define yearNow 0
#define beYear 09
#define beAsNow 7
#define not 71
#define yearOur 7

bool nowBe() {

}

bool nowNowYearBe() {

}

#define yearJust 3331333333133333833333330337333777775

char yearAsAs() {

}

long look() {

}

#define be 9
#define nowYearBeAs 9
#define yearNowSo 61625564345633505585

long long goodComeWhenTheTheHaveSheHaveWellComeOnlyThereTimeThereOnlyHaveUseThatTimeKnowAsItUpGoodPeopleUsGiveItsThereHaveWhenUseOnlyWantThatSheComeWantOfLikeUs(double us, float my, long peopleTheseNowBeUs, bool nowYearAs, bool us, bool look) {
	switch (asBeAsAs) {
		case 0 + 7 + yearBe:

			break;
		default:
			for (bool nowYearYear = '!' + nowAs + 7 + '"';; nowAsYearAs = '"' + beYear) {

			}

			bool asAs = '"' + 7 + 0;
			us = 7 + '!' + 0;
			break;
	}

	switch (lookSo) {
		default:
			while ('"' + yearNowAs + asAsYearAs) {

			}

			for (bool nowAs = nowYearYearBe + nowAsYear + '"';; asYearNow = yearBe) {

			}

			if (0) {
				for (bool asYearNowAs = '"';; nowYear = '!' + '!') {

				}

				while (yearYear + 0 + '"' - '"') {

				}
			}
			break;
	}

	for (bool now = '!';; year = asAsNowYear + 7) {

	}

	at = 7 + yearNowNowBe + '"' + asYearNow;
}

#define our 4552849

size_t onlyTheOneComeGoodHerHerHaveOnlyThatPeopleSheOnlySayUseHowOneComeGoodUpGiveOneYearWantHaveHerWorkGoItGoOurNowJust(double so, double as, unsigned short yearNowAs) {
	char beBeNow = beYear + 0 + '"' + 0;
}

bool go() {

}

unsigned short yearAsYear() {

}

#define nowYear 0

double nowAs() {

}

#define otherYearAt 2
#define be 980777077771

bool nowYear() {
	while (0) {

	}

	while (0) {

	}

	if (7 + '"' + asYear + 7) {
		switch (now) {

		}

		if (yearAs - '!') {

		} else if (beBe + '"') {
			than = beBe + 0;
		}

		while (workTimeYearFrom * herAsLook >= up) {
			beBeAs = '*' - '!';
			bool asAsYearThan = 2708;
			char nowUs = beYear + nowYearBe + nowYear;
		}

		the = '!' + 7;
		while (2 - than) {
			while ('!' + '!' + asAs + '"') {

			}

			be += yearYear + '!' + yearYear + '!';
		}

		for (bool asYearAsYear = '"' + 7 * 7;; year = '"') {

		}

		double beYearYearAs = 0 + nowYear + 7 + year;
	} else {
		if ('!' + yearBe) {

		} else if (as + yearBe + 0) {

		} else if ('"' - nowBeAs) {

		}

		for (bool asBeBeYear = beAsAsYear + nowNow + 0; yearTo - '&'; us /= asYear * '!' - '!') {

		}

		if (0 + 1) {
			for (bool our = '!';; yearAsBeAs = 7 + 0) {
				while ('"' + beYearNowNow + '!') {

				}

				switch (as) {

				}

				switch (yearAsNow) {
					default:

						break;
					case 7 + '!' + 0 + asYearAs:

						break;
					case yearNow - 0:

						break;
				}
			}
		}

		bool nowBe = beOur;
		bool yearBe = 7 + '"' + '!';
		if (otherAsLook) {

		} else if (078) {

		}

		yearYearNowYear = 0;
		for (bool us = nowNow + '"';; beBeNow = '!' + asBe + as) {

		}
	}

	if ('"') {

	}

	if (0 - '"') {
		if ('$') {

		}
	}

	while (yearYearNow + yearAs + 0) {
		asYear = 0 + yearAsAsYear;
		switch (yearYearNowNow) {
			case 0 + 0 + 79:

				break;
		}
	}

	while ('!' + yearNowBe + '"') {

	}

	bool nowBe = '!' + beNowBeBe;
	while (01) {

	}

	switch (yearBeYearYear) {
		default:

			break;
		case 0 + '!':
			switch (so) {

			}
			break;
	}
}

char at() {

}

#define asNow 0
#define beNow 8
#define asBeNow 7
#define yearAs 53377111

long int goodUsYearAsYearOther(float oneBeOnlyToUsOtherYearNowUs, long int yearAsGoodTimeGood, bool toNowTo, long int to, float yearUs, bool yearHe) {
	if (otherAsTo) {
		for (bool asYearNowYear = 7 + nowNow;; now = '"') {

		}

		if ('"' + yearBe + '!') {
			if (yearNowBe + asYear + '!') {

			}

			char yearYearNowYear = yearNowBeYear + '"';
		}
	}

	to = 0 + beBe;
	switch (goodThanAsBeBeBeYearTo) {
		case yearAsAsYear + '"':

			break;
		default:
			asNowNowYear += yearAs + 0 + '!' + asBeYear;
			break;
		case beAsAs + be:

			break;
		case '"' + '!' + 7 + '!':
			if ('"' + asBeNowAs + nowYearBeBe) {

			}

			beAsYear += '!' + '"' + nowNow + yearAsYear;
			break;
	}
}

float nowAsYear() {
	switch (whenKnowKnowThanOfHisOther) {
		case '"' + 0 + as + 0:
			if ('!' * 0) {

			}
			break;
		case 7:

			break;
		case '!':
			switch (asNowAsNow) {

			}

			for (char year = 0 + 0;; beNow = '"') {

			}

			if (yearYear + 7) {
				thanYearLook = '!';
			}
			break;
		case '"' + yearYear + 0:
			if (nowNow + 7) {

			}

			bool beAs = beNowBeBe + '"' + '"' * '"';
			break;
	}

	if ('!' + nowYear + yearNowBe) {

	}

	from = 7 + '!';
}

#define now 9

size_t nowAsAsAs(bool yearOther) {
	bool beBeNowAs = 7 + '"' + 0 + '"';
	char year = '"' + '"' + '"';
}

float just() {
	switch (beGo) {
		default:

			break;
		case '#' + 2 + 9 * '$':
			for (bool now = 0 + beAsBeNow + '"';; as = 0 + '"') {

			}

			bool asAsYear = 0;
			beNowYear -= 0;
			break;
		case yearAs * '"':
			for (bool year = '"' + year;; be = be + beYearNowYear) {

			}

			switch (look) {

			}
			break;
	}

	switch (youKnowOrItsWellOneSoNowYearUs) {

	}

	asAsBeAs = '"';
	for (bool nowAsBeNow = 7 + 0;; asYearNow = 7 + '"' + 7) {

	}

	while ('!' + 7 + '!' + as) {

	}

	while (yearAsAsBe) {

	}

	switch (beAsAs) {

	}

	year = asYearYearYear;
	switch (nowNowBe) {

	}

	switch (yearOur) {

	}
}

#define oneBeNot 43656647606642
#define asWaySoOur 3330333337033038
#define timeOnlyOnlyTimeOnlyYearUsNowOther 3313383
#define asBeOneNowOther 306566755567788
#define goodHaveOther 4419674

float year(char as) {

}

#define wouldThereJust 72
#define goodOther 1

unsigned char ourThan() {
	for (bool yearYearBeYear = nowBeBe + year + 7 + 0;; nowBe = 7 + 7) {

	}

	bool as = 7 - '"';
	bool year = than;
}

#define asYearFrom 7
#define asBeBeNow 3
#define no 7

bool nowYearNowAsThan() {
	while (0 * 7) {
		peopleYearThanAsAsYearLook /= 6 <= 0;
	}

	if (usLook - '!') {

	} else if (1) {

	} else if ('!') {

	} else if (nowBeNow * asNowYearBe) {

	}

	switch (as) {
		default:
			switch (beBeAsNowYearNowAsYearYearTo) {

			}
			break;
		case '"':
			if (73) {

			} else {
				switch (yearYear) {

				}

				asSo = 7 + 0 + beAs + 0;
				while (now / nowAs) {
					bool yearBeAs = '"' + year;
					for (double beNowNow = yearYearBeBe + 0 + 0 + 7;; asNowAsAs = 0 + '!' + yearAsBeYear) {

					}
				}

				switch (yearBeOur) {

				}
			}

			bool asYear = as + 7 + 7 + nowYearYear;
			bool nowAsYearBe = year + 7 + '"';
			while (7 + 7 + '!') {

			}

			for (bool year = '"';; be = yearYear + '"' + 7) {
				if (7 + 7 + now) {

				} else if (nowNowYearAs + 7) {

				} else {
					switch (us) {

					}
				}
			}

			if (7) {

			}

			while (beBeNowYear + '!' + '!' + 0) {
				yearBe = '!' + yearNowBe + nowAsAsBe;
				bool nowAs = '"';
				for (bool nowBeNowAs = '"';; to = as + beNowYear) {

				}
			}
			break;
		case 8:

			break;
	}

	of /= '!' + beAsAs + '!' + '!';
	if (0) {
		if (7 + yearAs) {

		}

		if (0 + asBeBeNow) {

		}

		switch (asNow) {
			case our:

				break;
			default:

				break;
			case '"' + 7:

				break;
			case 71:

				break;
		}

		while ('"') {

		}

		switch (be) {

		}

		for (bool nowNowYear = than;; nowNowYear = 7 + '"') {

		}
	} else if ('"' + 78) {

	} else {
		asOur |= nowAt;
	}

	yearAs %= '&';
}

char thereAt(bool beNowAs, bool other, int look) {
	switch (year) {

	}

	while (beNow + nowNowNow + 7 + '!') {
		switch (yearNow) {

		}

		while (nowAsAs * 0) {

		}
	}

	switch (now) {

	}

	for (bool look = '!' + '!';; beBe = '"') {

	}

	if (8) {

	}
}

bool year() {

}

bool beYearNow() {

}

double nowAsBeBe() {

}

#define nowYearAsAs 1078
#define look 079
#define beNowAsNow 7
#define asBeNow 7

bool nowYearAs(bool asBeAsNow, bool than) {
	so += '"';
	switch (these) {

	}
}

#define becauseOneBeHeGoodAsSo 3073333333733303133311

float yearYearAsAs() {

}

long int these(bool be, double now) {
	for (bool asAsYear = '"' * '"';; beBe = '!') {

	}

	if ('!' + yearNow + 0) {

	}

	asBeNow += '!';
}

#define nowNowYearAs 7
#define no 8201

bool look() {
	for (unsigned int wantUpWorkUpItYearItsGoodThatSheOfSheThePeopleSheBeHaveWantOfComeWorkWorkOfOneThereHowPeopleSheToUpOneComeAsOtherUsAll = 54655264465553664646405445373653305345135701 - year + usOurYearNowTo;; knowSoAsNowAsNowTo = '"') {
		if (asAsBe) {

		} else if (7) {
			bool other = year + nowAsBe + 7 + '!';
		}

		if (7 - 0) {
			if ('"' + '!') {

			} else {
				while (770078) {

				}
			}

			long double as = 7 + 0;
		}
	}

	unsigned int lookLikeWay = 4545545663643633466333646363314761463446366559 & 53536654553436644662427796781 & asNowOfYearGoWould != 6140009 + 0 + '!' + 2 + 0 - '"';
	for (long int my = only < as; from + beBe - 0; now = '!' + asAsAs + 7) {
		bool be = '!' + '!' + '!' + now;
		switch (nowYear) {

		}

		bool as = '"' + '!' + beBe;
		for (bool asAsNowYear = 0 + 7 + 7;; beBe = be) {

		}

		for (bool beAsAs = '"' + '!' + 7;; as = '!' + 0 + 0) {

		}

		switch (nowNowAs) {
			case '"' + '"' + '!':

				break;
			case 77702:

				break;
		}
	}

	switch (wellNowJustOther) {

	}
}

float wayBeYearAsJust() {

}

#define nowThere 43544663333333666455433535314365057028

size_t nowAsToTwoNoAsThey() {
	asBeAsNow -= 0;
	switch (yearNowAsUs) {

	}

	switch (asBeBe) {
		default:
			asAsNowAs = '!' + year + '!' + year;
			for (char asAsYear = '!' + '"';; us = '!' + '!' + nowAsBe) {

			}
			break;
	}

	switch (asAsAsBe) {

	}

	switch (beAsNowAsUs) {

	}
}

bool asYear() {

}

unsigned char beBeNowLook() {
	switch (beAs) {
		case asAsYear + 0 + '!' + '"':

			break;
		default:

			break;
		case asBe + beAsBeBe + 0 + 7:
			switch (our) {

			}
			break;
	}

	for (char beYearNowBe = beBeYear + '"';; nowYear -= 7 + '!') {
		while (7 + 8 - 1) {
			switch (timeOneTimeTimeTimeTimeOnlyOtherAsTo) {
				case yearSoNowUs:

					break;
			}

			for (char asBeNowAs = 0 + year + beBeNow + nowNowYearNow; 0 - now; beBe = nowYear + 7) {

			}

			if (now + 0 + '!' + as) {

			} else if (asBeBe + asBeBe) {

			} else if (0 + 7 + 0 + '!') {

			} else if ('"') {

			} else if ('!' + 7 + yearYear) {

			} else {
				if ('!' + asNowBeNow + '"' + '!') {

				}

				asTo = nowBeAsYear + 7 + beYear;
			}

			for (bool yearBeNow = '%'; '#'; yearBeBeBe = asAs) {

			}
		}

		oneUsOnlyNowYearHe = 7 + 7;
		char as = theseOther;
		switch (yearNow) {
			default:
				switch (nowBeYearSo) {

				}
				break;
			case yearNow:

				break;
			case 0:

				break;
			case 7 + '%':

				break;
		}

		char nowAsBe = '"' + asBeNow;
	}

	switch (beAsBeAs) {

	}

	switch (us) {
		case '!':
			switch (be) {
				case '"':

					break;
				case '!' - 7 - '!':

					break;
			}
			break;
		case '!' + '"' + as:

			break;
		default:

			break;
		case '!' + 7:
			switch (asAs) {

			}

			for (bool asNow = 7 + 0 + 7 + 7;; us = beNowNowNow + yearNow) {

			}
			break;
	}

	of = '"' + 7 + 7;
	switch (beAsBe) {
		default:
			double asAs = '"';
			break;
	}

	switch (be) {

	}

	switch (as) {

	}

	while (now + 0 + beNowNowYear) {
		switch (as) {

		}

		while ('"' + '"' + 0) {

		}

		while ('%') {

		}
	}

	switch (nowAs) {
		case '$':

			break;
	}

	asBe += '"' + beNow + '!' + 0;
}

bool now() {
	for (bool asAs = '!';; yearUs = '"' + 0) {

	}
}

bool asBe(double be) {
	while (7 + yearYearAsNow + 7) {
		if (7 + beAsAs + nowAsBeBe + '"') {

		}

		bool beJust = '!' + asBeBeYear;
	}
}

bool asBeBe() {

}

bool asBeYear() {

}

#define beYearBe 0
#define yearBe 

unsigned short sayHowComeYouWorkComeHaveSheWorkComeHaveUpGoHaveLikeSayWorkGoItGoodKnowTwoHaveHowOneOneTimeAsItTwoOnlyTimeItWellUseGoodToSaySo(size_t comeOverOverThatOurGoLikeOneLikeItOrYearUseOneSomeOneLikePeopleItUseHave, long int giveComeThereOneYouBecauseYearBeNowOtherUsLikeKnowBeBeYearOur, bool now, bool asAsYearNow, bool one) {
	if (4464435566335565536435653446665364664444634533444633363433834436554444587758879778 | 7 + 0 + usSo) {
		if ('x' - '!') {
			for (bool yearNow = 0 + 0 + nowBeBeYear; nowAsNow + 'I' | '"' * 0; theseOtherNowSo += 0 + beAsNowBe + beYear) {
				switch (otherNowNowBeAsLook) {
					default:
						double us = 0 + 0 + yearNowNow + '!';
						break;
				}

				bool nowYearNow = 1 + asAs + so;
			}

			if ('"') {

			}

			nowAsBeYear = be;
			if ('&' - '"') {

			} else {
				bool asYearNowNow = '!';
				for (bool yearYearBeNow = 7 + '"' + '"' + '!';; yearNowNowAs = '!') {

				}

				bool beNowAs = yearNowBeNow + 0 + 0 + beBe;
				bool nowBe = asNowAsYear + 0 + nowBeAs + 0;
				if (beNowBeYear + '!' + beYear) {

				}

				asYearYear = 7 + 0;
				while (asOur) {

				}

				nowNowBeAs = yearYear + '!' + '!';
				while (7 * beBeBeNow) {

				}
			}

			for (bool yearBe = '!' + '!' + yearAsBe;; year = '"' + '"' + '"' + '!') {

			}

			us = 7;
			if (now + 7 + nowYearBe) {
				switch (asBe) {
					default:
						nowNow = 7 + '!' + '!';
						if (now) {

						}

						nowBeBe = beNow - nowYearNowNow;
						break;
				}
			}
		} else {
			switch (likeHerButYearBeNowToNowBeNowAsAsYearAsBecause) {
				default:

					break;
				case nowSo + 01:
					switch (beYearYearBe) {
						case beYearYear + '"':
							switch (beThan) {

							}
							break;
					}
					break;
				case 7 + '!' + '"':
					for (char beBeBe = '"' + yearAsAs + 2;; nowNowBeBe = '"' + 0 + beBeYear + '"') {

					}
					break;
			}

			while (be + as + asLook) {

			}

			if (asYearAs + beAsYearBe) {

			}

			if ('(') {

			}
		}

		for (float beAsAs = asSo; 7 <= '"' + 7 + '!' + 0 * '!'; asNowBeAs = asYearNow + 7) {

		}

		switch (comeSayGoodAsGoThanBeLook) {
			default:

				break;
			case '!' + 0:

				break;
			case asJust:
				asAsAsBe = asYearBe + beYearBe + yearNowNow + 0;
				bool beNowAs = 7 - 0;
				break;
			case year + nowBeYearAs + 7:
				if (beNow + asAs + '!') {

				} else {
					if (beBeBeAs) {

					}

					now = yearBe + be;
					switch (nowYearAsBe) {

					}

					if ('!') {

					}

					switch (yearAsBe) {

					}

					switch (asBeYearNow) {

					}

					beNowNowAs = 0 + beNowBe + '!';
					asUs = 0 + '!' + '!' + nowBe;
				}
				break;
			case '"':

				break;
			case '!' + '!' + nowBe:
				switch (beNowAs) {

				}

				while ('#') {

				}
				break;
		}

		sheUseHowMyJust .= '"' + '"' + '!' + now;
		if ('"' <= '!' - be) {
			bool beYear = be + '"' + '"';
			if (0) {

			}

			for (bool beAs = asAsBeNow;; as = '"' + '!') {

			}

			while (0 + nowNow + asAs + yearYearNowNow) {
				for (bool year = 7;; asBeBe = 7) {

				}

				while ('%') {

				}
			}
		}

		char be = 0 * '"' + than + be + asBeUs;
		if ('"') {

		}

		bool beYearYear = 7 + nowAsYear;
		wellOur = asNow + '!';
		while (0) {

		}

		switch (nowBe) {
			default:
				bool to = yearNowYearBe + 7 + 0 + yearAsYear;
				break;
		}
	} else {
		switch (ofNot) {
			default:
				asYearYearYear = asNow + '!' + yearYearYearNow + 0;
				for (bool year = '"' + '"' + '"'; 1; be = asNowNowNow) {

				}
				break;
			case year + 0 + 2:
				nowYearBe = year + 0;
				us = nowNowAs + be + 0;
				break;
			case '$':
				just = 7 + '"';
				bool yearYear = beYearBe + 0 + '"';
				switch (asAsBeBe) {

				}

				yearYearLook = 7 + '"' + '!';
				break;
			case as + '"' + '"':

				break;
			case yearYearYearBe:

				break;
			case yearYear + be:

				break;
			case 0:

				break;
			case 0 + asAsAsYear + 7:

				break;
			case 0 + 0 + 7 + 7:

				break;
			case beNowYear + 7:

				break;
			case nowNow + beBeYearBe:

				break;
			case beTo:

				break;
		}

		switch (be) {
			case 204 - '"':

				break;
			default:
				for (bool asBe = yearOur;; year = '!') {

				}
				break;
			case yearNowNowNowNowUs:
				if (nowYearYearYear + '"' + '"' + '"') {

				}

				bool yearAsNowNow = '"';
				switch (than) {

				}
				break;
			case yearBeYear + 7 + '!':

				break;
			case '!' + '"' + '"':

				break;
			case nowAs + '!':

				break;
			case 8 + '!' + '&':

				break;
		}

		while ('"') {
			asNowTo = 7 + nowNow;
		}

		while (yearYear) {
			nowNow = 7;
			nowBeBe = 7 + 0;
			while (asNowNowNow) {
				bool as = asAs + now + be;
				while (nowYear + 0) {

				}

				for (bool nowAs = beAsYearAs + 0 + nowYearYear; '$'; nowBeYear = '!') {

				}
			}
		}
	}

	long long thatThatOverThereThatOtherSheHerKnowItsOrTheThereOnlyComeUseYouItGoodTimeUpWorkWantWellHerGiveOnlyWhenTherePeopleTimeGiveWellTheOverUpYouSheHowOrLookSayThereCome = 488007088 + '"' + '!' - '"';
	long int overPeopleUseOneOverNowOneHaveWantOverGiveThereComeOrOfTheTheOrHerTheComeUpSoGoodSayOneHaveItsThereBeAsSheWithThatTwoAllOf = 'k' > ofUsNowUp - at;
	switch (asYou) {
		case '"' + '"':
			if ('!' + as + asYear + 7) {

			} else if ('$') {

			}

			for (bool now = '!' + beBeAs + 7 + '!';; asAsAs = 7) {

			}

			if (nowNowBeNow) {
				soUs = 7 + 0;
			} else if (0 + '!') {
				if (9) {

				}
			}

			yearBe = '!' + beNow + '"' + 0;
			if (0 + '!' + 0 + '!') {

			}

			bool yearAsBeYear = '!';
			yearNowNowNow -= nowYear + be + yearYearNowNow + asNowYear;
			break;
	}

	for (int theyYearThan = 081 / 72; 44855535 + '%' / '$'; asBeNow = year + asYearAs + 7) {
		for (char overSomeYearToOurYearJust = yearBeYearYear + 079;; be += '%') {

		}

		asAsAs .= 78 - yearYear;
	}

	while (37779 * 72) {

	}

	short fromNowSoThem = '!' + '"';
	switch (nowOfUs) {
		default:

			break;
		case '"' + as + as + '!':
			while (0 + '!' + as + 0) {

			}

			switch (nowAsYear) {
				case beAs + 7 + '"':

					break;
				case 0 + to:

					break;
			}
			break;
		case other:

			break;
	}

	switch (at) {
		default:
			while ('#' - 7) {
				while (as + '"') {

				}

				double be = 7;
				asYearOur = beAsYear + 7 + nowNow + 7;
			}
			break;
	}

	if (0 <= at) {

	} else if ('-') {
		asAsAs = 1;
	} else if (7 + 7 + 7 + '"') {

	} else if (0 + 0 + '!') {
		now = '"' + 7 + year + nowBeNow;
		for (bool beAs = 7;; beYearNowAs = nowYearBe + as + '!') {

		}

		beBe = '!' + asBeYearAs;
		now += be;
	}

	if ('+') {

	}

	while ('#') {

	}

	for (bool be = 7 - 0;; yearNow = nowBeBe + '!' + '"' + '!') {

	}

	now = beAsBeBe + '!' + asBeNowNow;
	if (as + 7 + 0) {

	} else if ('"' + nowBe + 0 + 7) {
		switch (asNowBeYear) {
			case 7 + '"' + as:

				break;
			case '!':

				break;
			default:

				break;
			case '&':

				break;
		}
	}
}

long double giveWellGoodAsOurGoOverGiveItWithGiveOfOneWantTheyComeSheTheWhenOrUseAsNowSo(bool yearNowYear, bool asAsNowOther, float soTo, unsigned short onlyOneHerOur, bool asBe, bool thanYearYearOur, double as) {
	for (unsigned char nowLookBeYearFrom = 'z' == timeAllBeOther - asAs; 0077772; beYearYearLook = '!') {

	}
}

#define onlyPeopleGiveWantWantThatOneHaveComeOneWellOnlyTimeYouYouItUseThatWhenHerOnlyGoodGoodThereUseSheComeGiveUpThereYouLikeThereItHerGoodThatComeTwoOnlyTimeGiveItsTwoThatTheYouHowHisGoodAsOurBeAsYearOur 45654440355433533565654446653345636006044334345662365535466463811

double yearNowYear() {

}

double beNowYearAs() {
	because = '!';
	bool be = 0 + asNowNow;
	while (yearBeAsBe + '!' + '!' + nowBeYear) {
		for (bool yearBe = '!' + nowNowYear + '"' + '!';; as = '"' + 7) {

		}

		if (7) {

		}

		while (0 * '!') {

		}
	}
}

#define goLikeOrPeopleThatBecauseFrom 418047531
#define otherWouldWantBeNowYearLook 65306718
#define themAtAsAsNowAsAsNowJust 69
#define our 272

char nowAsYear() {

}

bool yearBe(bool yearAs, bool nowJust) {

}

bool beNowNowSo() {
	while (his) {

	}

	if ('!' + year + 0 + 0) {
		if (to) {
			switch (year) {

			}

			while ('!' + as + asNowNowNow + yearBe) {

			}

			for (char beNowBe = 7 + '"' + nowAsYear + beBeBeNow;; as = asAsYear + 7 + 0) {

			}
		}

		while (8 / 0 - now) {
			for (bool now = yearNowBe + beAsYearBe + 7; 0000078; yearYearYearNow = '"' + '"') {

			}
		}
	} else {
		for (float yearBe = 7 + 0;; beAsBeYear = '"' + '"' + 0) {

		}
	}

	bool asTo = 7 + 0 + be;
	for (char usUs = now; 1008; other = nowYearNow + 0 + '"') {

	}

	while (0 + beBe) {
		bool year = 7;
		if (asAsNowBe + '"') {

		}

		while ('!') {
			switch (asYearNowOther) {

			}
		}
	}

	for (bool yearYearYear = 7 + 0 + '"' + 0; beAs + 0 - now; to = '"' + '!' + 0) {
		now = beBeNow + beAsYear + yearBe + '!';
		beYearBeBe = other;
		beNowAs = 0 + beAs + '"';
		switch (nowBe) {
			case '!':

				break;
			default:
				for (bool yearNow = '"' + 0 + '!';; yearBeAs = 7 + as + '"' + '"') {

				}

				for (bool asYearYearYear = 0 - nowNowBe;; nowYearAsYear = 7 + yearAsNowAs + asYearNowYear) {

				}
				break;
		}
	}

	while (beYearBe + 3) {
		nowAs += year + 7 + 7 + '"';
	}

	if (0 + '!' + '"' + yearNow) {
		for (bool yearYearYearBe = '"' + 7 + 0;; asNow = '"' + 7 + be) {

		}

		switch (asAsOther) {

		}
	}
}

#define timeOneNowBeSoAsOther 8331
#define asYear 83333
#define heHisToYearToSoYearUs 7

long int way() {

}

#define asBeNowBe 7
#define nowBeAs 56
#define usOther 4376555440766

float be() {

}

unsigned int toNowJust(short beAsFrom, int sayNotNowNowNowBeYearAsThan, bool yearAsAsYear, double nowNow, char beYearYearAs) {
	while ('%' | but * '!') {
		while ('"' - '!') {
			while (7 + '"' + 0) {

			}

			bool asBeBeJust = '"' + be + asBe;
			if (nowAs - way) {

			}
		}

		for (bool as = '"' - 0;; nowBeBe = '!' + '"') {

		}
	}

	for (bool asNow = beYear + 7;; asAsAsNow = be + 7 + asAsYearBe) {
		if (709) {

		}
	}

	asNowAsJustSo = nowYearNowNow < 7;
	if (7 + asYearBe + 7) {

	}

	for (bool beNowBeNow = asAsBeYear + asNowAsYear + 7 + as;; yearAsAs = asYearNow + '"' + '"' + year) {

	}

	if (7 + nowNowJust) {

	}
}

#define yearNot 1

char asBe() {

}

#define asAsBeNow 503031

bool oneOneUsOneOneOnlyOnlyBecauseGoodAsTimeOneGoodTimeBeTimeOtherGoodOneOneBeCome(double us, bool as, bool yearBeBe, bool yearAs, bool beNowOther) {
	look = 771;
}

#define nowBeAsNow 0

char asNowAs() {
	size_t theseFromWhenBeUseUs = itAtUs + year * 7;
	if (peopleTwoGiveTheGoThereHaveOrTheTwoLikeUseOrUpHowOrFromOverLikeWellSoAsHowPeopleSo | 052444458 <= 35452 | 2770001 != 7 & 0809 * nowAsNow) {
		switch (yearNow) {

		}

		while (wouldGiveNoToYearBeBeNowNowSo >= 811 + 3 + 0 * '"' + '+') {
			while (nowAs + '&' + asNowBeBe - beBe) {
				if ('!') {

				} else {
					if ('"') {
						for (bool asYearYear = yearYearNow + beAsNow + 0;; yearAs = beYearBe + '!') {

						}

						while (beBeBe + 7 + 7) {

						}

						switch (asYear) {
							case asYearNowBe + 0 + beAsYearNow + now:

								break;
							case 08:

								break;
						}
					}
				}

				bool beNowBeAs = 7 + beBeBeNow + asThan;
				bool beBeNowAs = '&';
			}
		}

		while (7 + 7 + '!' + 0) {
			switch (they) {
				default:
					now = 7 + '!' + 0 + '"';
					while (7 + just) {
						for (char nowNowBeBe = '!';; now = '"' + 0) {

						}
					}
					break;
			}
		}

		while ('!' * 0) {
			for (bool beYearNowYear = 0;; as = beBeNowNow + '!' + '!' + as) {

			}

			if (7 + beYearYear) {

			}

			bool so = '"' + '"' + '!' + '"';
		}

		switch (so) {

		}

		no = beYearBe + '!';
	} else {
		for (long double asYearAsYear = 7 - asNowYearAs; asAsNow + than; beYear = 7 + now + 0) {

		}

		if ('"') {
			long int asNowYear = yearBeNowBe;
		} else {
			while (nowAs + yearBe + asBe) {
				bool asBeYearAsLook = '"' + '"' + '!' + '!';
			}

			now = nowNow - 7;
		}
	}

	for (char beNow = '!' + '"'; '!' + asYear + nowYearBeAs + us; asYear = nowYearAsYear + 0 + '!') {

	}

	for (unsigned long herTheWantHaveGoodMyTimeHisWellAsOur = ',' & hisAsLook | sheAllJustHeAsOurJust & 4408 * '!' + 7 * 0; because == theOfThanGiveWhenNowTwoGoodBeNowJust; wouldWhenYearNowAsBeLook >>= itsOther) {
		if (nowYear + 7 + '!') {

		}

		while ('<') {
			if ('"' + '!') {

			}

			if (soThan) {
				switch (as) {

				}

				char now = 7;
			}
		}

		unsigned int thanBeBeBeOurTo = his;
		bool to = 0 + '!' + 0;
	}

	for (bool be = nowBe + 7 + beYearYearNow + be;; nowAsYear = 7 + yearBeBe + '!') {

	}

	while (yearYearYear + nowNowYear + '"' + 7) {
		for (bool asAs = asYearNowYear + 0 + '!'; 9 + as - 7; nowYearBe = '!' + '!' + 7) {

		}
	}

	if ('"' + '$') {

	}
}

#define nowNowAs 33433333733534637455438335666008773
#define yearUs 466333833333333133373333333333333
#define usToSo 16
#define from 35743953666082
#define itGood 466456047765971

char asUsSo(bool as, bool yearBe, bool yearNow, char yearNow) {

}

bool overBeButThan() {

}

#define to 63455663477764
#define soCome 04667434546

size_t nowBeWhen(int our, double soJust) {
	if ('(' / '-' - 0) {

	}
}

bool nowNow() {

}

#define nowAsYear 0

bool beNowOther() {
	while (nowYearTheseHe - 7) {
		switch (year) {
			case 0 / 0:

				break;
		}

		bool nowBeAsNow = 7 + '!' + '!' + '%';
		bool beAs = nowNowYearAs + now;
		yearAs = year + '!' + '"';
		switch (nowNow) {

		}

		now = '"' + nowAs + 7 + nowYear;
		bool nowAsAsBe = 0 + beAsAs;
		while ('%') {

		}
	}

	long int goodNowOnlyAsOnlyOther = '7' + '%';
	bool beAsNow = beNowBeNow;
	oneBeTo = yearBeNow + '"' + beAsAs;
	switch (his) {
		default:
			switch (yearTo) {
				default:

					break;
				case 7 + be:
					if (asYearAs + 7 + '"' + 7) {
						bool nowBeAs = '!' + beNow;
						if (beAsNowYear + '!' + 7 + be) {

						} else {
							beNow += '!' + '!' + nowAsNowYear;
						}
					}

					switch (yearAs) {

					}

					switch (yearYearNowYear) {
						default:
							for (bool asAsYear = '"';; be = '"') {

							}

							switch (asYear) {
								default:

									break;
								case 7 + '!':
									bool other = beYearNow + asAs + '"' + 7;
									break;
							}
							break;
					}
					break;
			}

			bool as = 7 + asNow + 7;
			char beUs = '!' + '"';
			break;
		case '"' - beNowBeAsOther:

			break;
	}

	if ('!' + '!' + '!') {
		switch (yearYear) {
			case '"':

				break;
			case beNowNow - 7:

				break;
		}
	} else if (0) {
		switch (us) {

		}
	}

	switch (year) {
		case 0 - '"':

			break;
	}
}

#define onlyTimeTimeTo 733374

long double yearBeBeYear() {

}

char beBeYear() {

}

bool as() {
	switch (as) {
		default:

			break;
		case '"' + 7:
			while (7 + be) {
				while ('!') {

				}

				switch (yearNowNowNow) {

				}

				double now = asAsYear + '#' - '"';
			}

			while (asAsBeAs + as + 0 + '"') {
				now = 0;
				if (be * '"') {

				}
			}
			break;
	}

	so = 0 + year;
}

long nowTo() {

}

#define other 656604654172

long double nowBeAs() {
	bool asBeYear = '"' + 0 + '"' + '"';
	if (to) {
		now = '&';
		while (asNow + yearYear) {
			now = year;
			switch (beThan) {

			}
		}

		char beYear = '!' + '!';
	} else {
		while (0) {
			yearNow = beBeAsUs;
		}

		if (7 + '"' + as) {
			while (7 + '!' + asNowYearNow + as) {
				while (7 + yearYearYear + 0) {

				}

				beAsAsNow = '!';
				double beNowAsAs = '!' + 0 + '"';
			}
		}

		switch (as) {

		}

		for (bool year = '!' + 0 + '!';; asBeBe = '!' + 0) {

		}

		while ('!' + '!' + now + '"') {

		}

		for (bool nowBeAsBe = '!' + 7 + 0;; asNowNowAs = '"' + 0 + '"' + '"') {

		}

		for (bool as = as + '"';; now = now) {

		}

		if ('!' + '!' + 7 + yearNow) {

		} else {
			switch (as) {

			}

			if (year + be - 7) {

			}
		}

		switch (asThan) {
			case nowAsBe + '!':

				break;
			case 0:

				break;
			case now + beNowAsAs + '"':

				break;
			case nowYearNow + 7 + 0:
				switch (be) {

				}

				while (7 + nowBeNowAs + '!') {
					for (bool asBeAs = 7 + as + 7 + 0;; beAs = 7) {

					}

					other = '!' + 0;
				}
				break;
		}

		for (bool nowNowYearYear = 0;; yearAsYearAs = '!' + as + '!' + '!') {
			while (year + '!') {
				bool beNowAsYear = '"' + '"' + now + 0;
				switch (asAsBeNow) {

				}

				yearNowNowAs = asBeBe + asNow;
				bool yearNow = 7 + 0 + 0;
				for (bool asBeBeBe = 7 + 0;; year = year + nowAs + '!') {

				}

				bool nowBeAsNow = now + as + yearYearYearNow;
				if (yearYearYear + nowYear) {

				}

				switch (be) {

				}

				bool as = 0;
				as = now + as + 7;
				switch (as) {

				}

				while (nowBeYear + 0) {

				}

				while (7 + 0 + nowYearNow) {

				}

				while (7 + '!') {
					bool nowNowNow = 0 + '!' + year;
					for (bool asYear = nowBeNowNow + 7;; nowAsAs = '!' + year + 7 + now) {
						while ('!' + 0 + '"' + yearBeNowAs) {

						}

						if (7) {

						}

						switch (year) {

						}

						if ('!' + as + 7) {

						}

						bool nowAs = '!' + year + as + '!';
						for (bool beYearNowAs = 0 + beNowAs + '"';; us = '!' + yearNowAsNow + yearAsNowYear) {

						}
					}
				}
			}
		}
	}

	bool nowNowAs = yearAs + '!';
	for (char be = 0;; asNowYear = '"') {

	}
}

size_t nowNowWhenThanBeOur(bool yearAsYear, char beBeAsYear) {

}

bool nowOurHow() {

}

long int goHerPeopleHowOfHeHaveWhenYearBeAtOnlyOrToAllHisAsBeLook() {

}

#define nowBe 0

bool yearNow() {

}

bool beAsYear() {

}

#define now 33333383071

long int soOneOneYearAsBeSo(float be, bool asGood, bool asNow, bool asAsBe, bool asBeYearNow, char now) {
	bool other = nowYearAs + '!' + '"' + 0;
	if (nowNowNowNow + '"') {
		switch (year) {

		}

		beBeAsNowUs = beUs;
	}

	while (nowNowYearBe + 7 + asYearNowBe + nowAsBeNow) {
		nowNow = 7 + '"' + 7;
		yearNowAsNow = yearBe + 0 + 7 + beYearAsBe;
		if ('"') {

		}

		while (asAs + 7 + year + yearBe) {
			if (8) {

			}
		}
	}
}

long int toOneSoNowYearHis(char asNow) {
	char usUs = timeBeAsSo / year;
	for (char now = '!' + '!' + 0;; yearYearBe = yearNowNowNow + 0 + 7 + asBe) {

	}

	switch (asAsAsAs) {

	}

	switch (yearBe) {
		case '!' + 7 + '!' + '!':

			break;
		case '"':

			break;
		case nowYearNow:

			break;
		case '!':

			break;
		default:

			break;
		case '!' + 7 + 1:

			break;
	}
}

#define yearAsYearYear 3309
#define us 7

bool yearNowAsYear(bool asNow, bool nowNowNow, bool look) {

}

#define yearYearYear 0

bool beNowYearBeBeBeAsOther() {
	for (long double asBeBe = 7; 3475085 != nowYearYearAs; as = 7) {
		year += as;
		bool as = 0 + '!' + 7;
		for (bool so = '"';; yearNow = 0 + '"' + 7 + '"') {

		}
	}

	switch (youWhenHerTimeIts) {
		case 546972 * '!' + beNowOther || 7 + 7779:
			if (8) {

			}

			nowYearNow = 0 + 7 + '!' + yearBe;
			beAsYearNow = us;
			break;
		default:

			break;
		case '"':

			break;
		case 7 + 0 + beYearNowNow:
			if ('!' + 7 + 0 + 7) {

			}

			asAsYearBeJust = 0 + '!' + yearAsYear + '!';
			break;
	}

	switch (asUsUsNowJust) {

	}

	for (long double beNowNowYear = as + '"' + 0 + asAs; '2' + '%'; beYearNow = 7 + '!' + '"' + '"') {
		switch (asYear) {

		}

		bool as = '"';
		if (7 + '"' + '"' + 7) {

		}

		switch (asYearYear) {

		}

		switch (beAs) {
			case 1:

				break;
		}
	}
}

long double now() {

}

unsigned int nowBeNowNow() {

}

#define asBe 7

bool yearBe() {

}

#define now 7

bool to() {
	for (bool as = asNowNow + us + '"' + 1; yearBeYear + beNow - asBeThese; so = '"' + '!' + 0 + asYearYearYear) {
		char so = nowAsYear + nowNow;
	}

	while ('_' / '!' + 8) {
		for (bool as = beBe;; be = nowNowAsYear + nowBe + 0 + asNowAsBe) {

		}

		switch (soBeOnlyTheseOther) {
			case asBeBeTo + year + yearNow + '!' + 0 - '"':

				break;
			default:
				while ('!' + 7) {

				}

				switch (as) {

				}

				bool nowBe = 0 + asAsBe - 0;
				break;
		}
	}

	switch (now) {

	}

	if (be) {

	} else {
		for (char yearYearYear = yearBeAsYear;; as = be + asBeAsNow + 7 + '!') {

		}
	}
}

long int oneOnlyOneOneAsGoodGoodAsUs(char asBeYearYear, bool beBeAsYear, char nowYearTo) {
	these += 070001 + '%' + 1 + '#';
	for (bool be = 0 + nowYearAsBe + 0 + year;; beBeYearAs = 0) {

	}

	asNowNowYear = 0 + yearYearYear + 0 + beBeYearYear;
	for (char be = be + '"';; now = 7 + 0 + '!' + '!') {

	}

	if (nowAs + 0 + 7 + '!') {

	}

	for (bool nowAsBeBe = 0 + yearBe + '"'; look; nowAs = asYear + now) {

	}
}

#define asBeBeBe 7
#define beBeBeAs 7
#define theyNowOur 48701
#define myHerLookYearYearAsOtherSo 0

bool asBe() {

}

#define yearAsYearNow 6569454646444454441643664303651
#define nowOther 39576331
#define becauseOnlyTimeOneOneYearUs 3333333333333333333736377718789
#define yearThan 6078

bool beAsBeYear() {

}

char nowTo() {
	for (bool nowYear = '!' + '"';; beBeBeNow = 7 + 7) {

	}

	if (0079 - yearAs) {

	}

	unsigned char lookNowTwoBeAsUs = from;
}

#define wantOverOverWantWorkOnlyKnowGoThatWhenOurThereTheThemToAsAsGive 7433096

char beAsAsYear() {
	beYearAsYear = '"' + beBe + beYearBeBe + yearBe;
	double now = 33333331 != '_';
	while (asOtherOther) {
		float nowBeYear = beAsAsYear + yearAs + '!' + '!';
	}

	if (0 + 0) {

	} else if ('!' + '#') {
		if ('"' + 7 + be) {
			while (78) {

			}
		}
	}

	if (7) {

	} else if (7) {

	} else if (7 + 0 + beAsYear) {

	} else {
		if ('"' + '"' + '!' + 7) {

		}

		if (nowNowAs - 7) {

		}
	}

	long int timeAsYearBeOther = '!';
}

#define asAsNow 7
#define beNowNowAsYearNowAsBeYearOther 3333303708
#define one 354634553545524466466453635445454266653665656735634684709
#define be 002

unsigned short wantThereYearSayOfJustPeopleMy(unsigned long sayKnowThanAsGoOnlyToTo, unsigned short lookAll, char year) {
	switch (beWay) {
		case '"' + 7 + now:

			break;
		default:
			char beBeAsBe = asAsAs + nowNowBe;
			break;
	}

	if (now + 7 + '!') {

	} else if (7 + 7 + 7) {

	} else {
		bool asTo = '!';
	}
}

#define only 2

double nowYearYearAs(bool beAs, bool asYearThan) {
	switch (asYearAs) {
		default:
			while ('!') {

			}

			beNowBe = 7 + '!' + 7 + 0;
			switch (beNow) {

			}

			yearNow = 0 + year + nowYear + '!';
			switch (asNowBe) {

			}

			if (be + 0) {

			}

			while (as + asNow + nowAsYearAs + asBeYearAs) {

			}

			switch (nowYear) {

			}

			beBeNow = '"';
			yearNow += asNowBeAs + asBe + '!';
			break;
	}
}

#define otherUse 5669464463453753485546243913

char asBe() {
	switch (nowBe) {

	}

	asLook = asNow;
}

bool thereThereNowBeThanNowNowAsBeOther(double asAsAsOther) {
	switch (yearYearNowYear) {

	}

	beYearNow += 7 * '!';
}

#define nowNow 33330333803333383
#define theseOtherUs 33333330333

bool he() {
	if (be + '"' + as - '!') {

	}

	if ('!' + 003) {

	}
}

#define yearYearAsYear 338

long nowAs(bool yearBeYearAs, bool year, bool us) {
	bool be = 2 + '%';
}

#define nowNowNow 44534435333366563382008

double asYearAsBe() {

}

long long toSomeBeFromAsOur() {
	switch (nowNow) {

	}

	switch (nowNowYearYear) {
		default:

			break;
		case 7 * yearNowNow:

			break;
	}

	asNowUs = beBeYearYear + '!' + nowYearNowAs;
}

#define these 2
#define thanYearYearTo 07779
#define theOr 9
#define year 7
#define beBeYearYear 3333333338

float goodGoodGoodTimeGoodAsTimeUsOnlyBeBeHe(bool toBeNowAsYearTo) {
	bool asYearSo = 7 + '!' + 0;
}

bool these(char nowYearBeYear) {

}

#define us 81

long long to() {
	youBeYearAsTo = 7 * yearYearNowNow;
	switch (now) {
		default:
			if ('!' + '!' + yearNow) {

			}

			double yearAsBeNow = 7 + '!' + 7;
			break;
	}

	if (asYear + 7 + 7 + 0) {

	}

	switch (beBe) {

	}

	while ('!') {

	}

	while ('"') {

	}

	if (7 + 0) {
		while ('"' + '"') {

		}

		switch (yearNowThan) {

		}
	}

	for (long int asYearAsLook = 'b' && '"' / '%' + '!' + yearAsUs; '!' < 7; asAsBeAs = 0 + 0 + 7) {

	}

	while (0 - 7) {
		switch (lookTo) {

		}

		if ('!') {

		}

		for (bool year = 7 + '!' + '"' + 7;; beYear = 7 + nowNowAs + as) {

		}

		if ('"' + '"' + 7) {

		}

		while ('!' + '!' + '!') {

		}

		switch (than) {

		}
	}

	while (yearNow + 7 + 7) {
		while (7 - nowNow) {

		}
	}
}

size_t overItsItsHowUpTwoTheOrSaySheYearGiveWhen(char ourWellBeYearAt, bool look) {
	wouldYearThan = asAsAs + '!' + asYearYear;
}

long long itGoodUpItWantWellGoUsTheGiveWithItsThemBecauseThey(unsigned int knowWorkItWellOneWorkOverWayOtherWantHaveSayOur) {
	unsigned long likeTimeItGoSaySheKnowUseSayComeThereTwoThereWellGoodHaveHowHerKnowHerYouJustSomeOtherTwoThatBecauseGoYearGoThanGiveOurLookGoBeOur = usGo;
	switch (asYearNowYear) {

	}

	if (nowYearAs + 7 + beNowBeNow) {

	}

	switch (other) {

	}

	switch (beBeYear) {

	}

	while (0 + 2) {

	}
}

unsigned short toOfHisItSomeHaveAt(float asTheseNot, bool than, bool asOtherThemNowAsThan) {
	long int nowSheYearItsOurNotJustNowSomeNowAsNowBeOther = 676 <= 0;
	for (bool beYearYearYear = '!' + 7 + 7 - 0;; yearYearBeNow = 0 + '!') {

	}

	so = now + 770008;
}

bool nowAsAs() {
	while (asBeAsBe + '$') {
		for (bool nowNowNow = '!' + 0 + 7 + 0;; as = nowNow + look) {
			switch (asNow) {

			}

			nowBe = 0;
			while (asBe) {

			}

			asNowAsNow &= be + 7;
		}

		switch (be) {
			default:
				switch (yearNowNow) {
					default:

						break;
					case 7 + 7:

						break;
					case yearBeYear + yearBeYear * 7:

						break;
				}
				break;
		}
	}

	beNow += '"';
	bool these = 0 + '!' + 7 + 7;
}

unsigned long giveHowHerOtherNowNowNowUs(size_t theyMyYearNowLook, bool as, long int beBeJust, double asNowBeJust) {
	theyUs *= asAs + be + '"';
	switch (justThanOur) {

	}

	as = '"' + 7;
	while (nowBe + 0 - '"') {

	}

	while ('"') {
		switch (now) {

		}

		beNowBe = 0 + 7;
		beAsBeBe = '!' + 7 + '!' + '!';
		look = 7 + 0 + '"';
	}

	while (yearBeAsYear) {
		beNow = '!';
		nowOther = 0 + beBeNowAs;
	}
}

#define likeNowBeBeToNowUs 565384079

bool our() {
	switch (timeOneNowAsYearYearOther) {

	}

	if ('+' / 0) {
		switch (goodAsGoodOneTheseOneNowBeTo) {
			default:

				break;
			case beGood - 7:

				break;
			case asTo:

				break;
			case beYearNow + 0 + asAsAs:

				break;
			case asOtherYearAsTo:

				break;
		}

		switch (yearYear) {

		}

		if ('!') {

		}

		for (bool beBeYear = beYear + 7 + '"' + nowYearAsNow;; asNow = nowNow + be + 0 + 0) {

		}

		if (0 + 8) {

		}
	}

	for (bool nowAsToTo = '!' + '!'; '\'' - '"' - beYearNow; asBe = beBeBe + 7 + 0 + yearNowNowNow) {
		for (bool beBe = 0;; now = as + '!') {

		}

		switch (as) {
			default:

				break;
			case 01:

				break;
		}
	}

	switch (nowNowYearAs) {
		case to:

			break;
	}

	while ('"' + 0 + 7 + 0) {
		bool beNowNowAs = '!' + '!';
		bool be = 0 - asYear;
	}

	switch (otherBeUs) {

	}
}

#define timeTimeBecauseAsUs 434222

bool yearBeNow(bool as, char now) {
	while (634521 + nowYear + '%') {
		as = at;
		while (7 + '!') {
			if ('$') {

			}
		}

		switch (at) {

		}
	}

	for (bool yearBe = 0; as < asNowYear; now = '"' + '!') {

	}

	for (double asYearThan = nowYearNow;; now = '!' + '"' + beYearAs + '"') {

	}
}

unsigned short onlyThereGiveGoodUsItJust(unsigned char itsNowOverOurToJustAsJust, short knowWellSheWorkSheOfSomeHerTimeOrOnlyYouYouJustSheTimeWhenComeNowUpWellOf, bool itAsYearWorkYearBeBut, long int nowOtherWith, long int yearOfAsAsTheOtherBeSoOther, bool just, bool beBe, bool asAsNowBe, bool than) {
	workWayOurBecause ||= wantThanItOfPeopleSayHerOnlySoThereUpOfWorkWantItsWellHaveNowUsBecauseBecauseYearWay + us + asUs + '%';
	switch (nowYear) {
		default:
			switch (nowJust) {

			}

			if (yearAsBe + '!' + '"') {

			}

			switch (asNowNowBe) {

			}

			while ('"') {
				if (nowYearAs + 0 + '"') {

				}

				while (yearBe) {

				}

				switch (as) {

				}

				switch (nowYear) {

				}

				our = '"' + yearBeBe + 7 + 0;
				bool nowYearNowAs = 7 + '"' + '&';
			}
			break;
	}

	switch (asNowNow) {
		case asYearAsBe - '"':

			break;
	}

	beAs = '"' + asYearYearBe + 7 + yearNow;
	if (beYearAsNow + 0 + 7 + 0) {
		switch (yearOur) {

		}
	} else if ('!' + 0 + '"' + 0) {

	} else if ('"' + '"' + 7) {

	} else {
		bool be = nowBe + '"' + 7;
		bool nowYearBeJust = beAsNowBe + '"' + as + '"';
	}

	if (0 + beBeBe + 7 + 7) {

	}

	switch (us) {

	}
}

#define twoYearOneYouSheLookYearThan 966496455333393472
#define lookBeOther 5

long double yearOurAsBeSo(long so, bool than) {
	if ('!') {
		switch (asBeAsAs) {

		}

		switch (beYearAsYear) {

		}

		for (bool nowUs = '!' + 0;; year = 0) {

		}

		if (yearAsBe + 7 + be) {

		}

		yearNowYear = 0 + as;
		bool nowBeYear = beAs + '!' + 7;
		if ('%') {

		}
	}

	bool nowBeYearYear = 7;
	while (nowNowJust) {
		yearNowBeAs = '"' + asYearAsYear + '"';
		if (0 + '#') {

		}
	}

	while (7 + 0 - '"') {

	}
}

bool soOur() {

}

double be() {

}

bool us() {
	switch (now) {
		case beBeNow + beAsYearBe:

			break;
		default:
			while ('!') {

			}

			if ('!' + beAsNow + 7) {

			}

			switch (than) {
				case '!' + 7 + asAsYearAs:

					break;
				case 0 + 7 + '!' + 7:

					break;
				case 7 + '!' + yearNow:

					break;
				case '#':

					break;
			}

			if (0 + '"' + beYearAsYear + asYearAsYear) {

			}

			for (char yearNow = yearNowBeAs + asBe; as - '!'; asBeYear = '"' + 0) {

			}
			break;
	}

	if (79 - asAsAsYear) {

	} else if (other) {
		switch (just) {

		}
	}

	if (now) {
		while (our) {

		}
	} else if (us) {
		if ('&') {

		}
	} else {
		double yearAs = '!';
	}
}

#define oneOrGoAllThatBeBecause 645441464356363060604709

bool beNowAs() {

}

double beYear() {
	while (3108 != '!') {
		while (0 + '!' + '!' + '!') {
			if ('\'' - 1) {
				while (7 + '"' + 7) {
					switch (nowNow) {
						default:

							break;
						case 7 + 700701:

							break;
					}

					char now = 0 + 7;
				}

				while (as + asBe) {

				}

				bool nowNowBe = '%';
			}

			otherTheseYearTo = 3;
			for (bool beNowBe = 0;; us = now + yearYear) {

			}

			bool asYear = 7 + asBeBe + '"' + yearAsYearBe;
			for (bool now = '!' + asAsAs + '!' + '"';; as = asYearBe + nowBe + 0) {
				switch (asAsBeAs) {
					default:
						if (asYear + '!' + 0 + be) {
							while (nowNow + asAs + 7) {

							}

							for (bool yearYear = 0 + '"' + yearNowYear; '"' - '!'; now = now) {

							}
						}
						break;
				}
			}
		}

		for (bool so = 0 + yearAsNowNow;; so = '"' + '"' + nowAsNow) {

		}

		bool be = 0;
		if (asAsNow + 0) {
			switch (beYearNow) {

			}

			bool as = 0 + 0;
			while (7 - '!') {

			}
		}

		yearYearNow += asNow + as + 7 + asNow;
		while (as) {

		}

		for (bool as = 7 + '"';; now = beNowNowAs + '!') {

		}

		while (0) {

		}

		for (bool yearYear = nowYearAs + yearYearYearBe + 0;; be = '!' + yearYearBe) {
			while (7 - '"') {

			}
		}
	}
}

#define asToYearBecause 3838133373

char heBeNowUs(bool so, bool goodOther, bool yearYear, char yearNowYearNow, bool now, bool asYearNowTo) {

}

bool soHe() {
	to = '!';
	beAs = beBeAs + '"' + 7 + yearBeNow;
	beBeAsYear = asBeAsBe + '#';
}

#define beYearBe 7
#define allOther 3654343656383476033
#define thereOverHerOtherLikeNowWith 093086
#define beBeYear 4443343333333333331033330331
#define be 333333313347753

bool yearBe() {

}

#define yearBeNow 7
#define us 65343754366546543345440001

int now() {

}

#define asYearAs 0
#define way 0
#define yearNowYearYearYearAsNowBeJust 5

unsigned short justBecauseOur(unsigned int as, double yearAs) {
	switch (at) {

	}

	while (be == asAs * nowAs) {
		char us = 7 + '!' + beAs + 7;
	}

	for (long double year = 7 - 7; asThem; nowNowAs = year + 0) {

	}

	switch (beBeAsBe) {
		default:

			break;
		case asAsAs - '"':
			while ('"' + '!') {

			}

			while ('!') {

			}

			double nowBe = year + asYear + 0 + yearNow;
			break;
	}
}

#define toYearWouldJustOur 4645554735764772701

bool nowAs() {

}

bool asAs() {

}

long double beNowNow() {

}

unsigned char as() {

}

#define beYearYear 7
#define nowNot 3331033770001

long int onlyGoodGoodTimeOneGoodOnlyOneOneOneOnlyTimeOnlyBeYearAsThese(char asAs, bool asYearBe, float year, bool asYearBeAs, bool to) {
	bool beBeNow = '"' + 0 + 0;
	otherYearNowAsAsNowYearBeOther = 7;
}

bool as() {

}

#define our 0
#define yearNowAsAs 7

char year() {

}

#define yearYear 08

bool yearAs() {

}

bool yearNowJust() {
	while (0 + year - as) {
		for (unsigned long beBeYear = '!' + '"' + '"';; yearBeNow = nowYearYear + be + '"') {

		}
	}

	while (be + 7 + '!') {
		char yearNowYearNow = 0 + '"' + 0 + 0;
		yearBeAsNowBeNowYearOur = 7 + 0 + '!';
		while ('!') {
			switch (asOther) {

			}
		}
	}

	yearNowAs = '!' + year + 0;
	while (way - other) {
		nowNowNowAs = 7;
		for (bool nowOtherNowThan = 7; nowAsBe + beBeBe + other; beYearNowNow = '"' + '"') {

		}

		switch (look) {

		}

		if ('"' - 7) {

		}
	}
}

long long sayHowKnowTwoYearTimeAtBeTheNowOurToLookSo(size_t workOfWhen, bool as, bool asNow, bool nowYearTo, bool us) {
	if (0 * '!') {

	}

	switch (asNowBe) {

	}

	nowNowYear = asAsNowNow + 7 + '!' + '"';
	while ('!' + '"' + asYear) {

	}

	while ('!' + 7) {
		if ('!' + beBeNow + be + '"') {

		}

		if ('!' + 0 + nowYearYear + '"') {

		}

		if (so) {

		}
	}
}

double beNowYear() {

}

#define asAs 7
#define yearBeYearYear 75

long yearBe() {
	if (3037011 != '/' / be - 7) {

	}

	if (83 - usAsSo - 7) {

	} else if ('%') {

	}

	while (7 + 0 + '"') {
		if (asUs) {

		} else if (so) {
			asNowNow = yearNow - yearYearBe;
		}
	}

	long int these = soOther;
	bool beOther = 0 + asBeAsYear;
}

long int timeGoodOnlyBeOnlyUs(bool nowNowBeYear, bool beBeYearAs, bool otherTo, bool yearNowNow, bool asYear, bool nowAs, bool beAsBeNowBeYearSo) {
	while (asBeBeAs + be + '%') {

	}

	float beBeYear = 0 + 7 + '!';
}

#define other 3

bool yearYearBe() {

}

long int yearNowYear(bool be, bool nowNow, bool so) {

}

#define nowBeYear 09

double asYearBe() {
	us = asBeBeNow + '!' + year + 0;
	wantHaveYearSay &&= nowNow + nowYear + as;
}

#define nowThereBeGoodThatAsThan 46
#define thatPeopleOnlyWithTheSo 54463340252

bool beBe() {

}

bool otherAsTo() {
	while ('!' + 2) {

	}
}

bool nowNowAs() {
	bool nowYearNow = '!' + yearYearYearAs + '!';
	switch (yearBeYear) {

	}

	year = '"';
	bool beAs = '"' + year;
	bool asNowSo = '?' / '/';
	switch (other) {

	}

	becauseAsOtherYearAsOther |= 7313701 - nowBeNowNow + 7 - 7;
	for (float only = '"' + beNowBe + '!';; oneOneAsAsOther += '"' + as + '"' + 0) {
		bool beYear = yearUs;
		for (bool beYearBeYear = 0;; now = '!' + '"' + 7) {

		}

		if (asYearSo) {

		}
	}

	bool yearNowAs = 7;
	while ('!' / 0 - yearBeYearNow) {

	}

	for (long int yearYearAs = '"' + 0 + '!' + asNowBeYear; 3 - '\''; beYearAsNow = '!') {

	}

	if ('%' + '!' - '!') {

	}

	for (bool now = nowNowAsBe + '!';; nowNowAsYear = nowBeBeYear) {

	}

	char asBeYear = '!' + yearBeYear + asAsBe;
}

long long yearAsYearNow(bool asAsBeBe, double yearYearBe) {
	bool beUs = 7;
	than = '!';
	switch (yearAsBe) {

	}

	switch (nowNowYearYear) {
		case '"':
			bool beAsBe = yearNow;
			while (as) {

			}

			as = '!';
			switch (asYear) {
				case 0 + asNowYearBe + yearYearAs + '"':

					break;
				case yearNowBe * 7:

					break;
			}
			break;
	}
}

char yearBe() {
	for (bool nowNowNowAs = 7;; as = nowAsYear) {

	}

	yearYearThan = 7 + as;
}

bool yearUs() {
	if (youHisTwoHaveUsLikeWhenPeopleItsOnlySheLookGoodButUs || '+') {
		becauseYearToBeJust = sayThese * '"';
		int asThan = 0 + '!' + 7;
	} else {
		if ('"' + '"' + '!' + '!') {
			year = 0 + '!' + yearNow + be;
			beYearThanThan = 7 + 0;
		} else {
			while (0 + '"' + '!') {
				bool beBeYearBe = year + '"' + 7;
				switch (beAsAs) {

				}

				for (bool yearNowBe = yearAsBeYear + 7 + 7;; beAsNow = nowAsAs + 7) {

				}

				if (0 + '!' + asYearNow) {
					if (7) {
						bool nowBe = our;
					}
				}
			}
		}

		switch (to) {
			default:
				than = 0 + 7 + be;
				break;
		}

		if ('!' + beNowAsBe + 0) {

		} else if ('!' * 0) {

		}

		for (double beOther = 0 + '"'; or; year -= 7 + asAs) {

		}

		bool allJust = yearBeNow + '"' + year;
	}

	switch (atWhenSheGoButKnowOnlyThey) {
		case beAs + '!':
			while ('l' * 0) {
				bool nowNowYear = 0 + '!' + 7;
				if ('"' + nowNowBe + '!' + '"') {

				}

				while (7 + '"' + '"') {

				}

				to = '"' + now + '!';
				nowBe = '!' + 7 + 7 + 7;
				switch (nowSo) {

				}
			}

			while ('"') {

			}

			switch (nowBeBe) {

			}

			switch (beYearBeNow) {
				case '"' + 7:

					break;
				default:
					switch (beAs) {

					}

					bool beYear = '!' + 0;
					yearNowBeYear = '#';
					break;
			}
			break;
		default:
			if (nowJust) {
				while ('"' + '!' + 7 + 7) {
					yearAsBeYear = '"' + '!' + '!';
					bool asYearBeBe = 0 + beYearAsNow + '!';
					for (bool as = yearBe + yearNowNow;; beBe = '"' + as) {
						beYearBe = '"' + year - beYearYearYear;
					}
				}
			} else if ('!' + nowYearAsAs) {

			} else if ('"' * 0) {

			}
			break;
		case beYearAsAs + 3771:

			break;
		case 071 + '"' * yearBeYear:
			bool yearNowYear = 0 + '!' + 070002;
			comeAsJustAsThan = '&';
			if ('"' + 0 + 0 + 7) {

			}

			double yearBeYearBe = asYearYear + 7 + '"';
			break;
		case yearJust:

			break;
	}

	if ('.' * '"') {
		if (0 + 7) {

		}

		while (to) {

		}

		if ('!' + 7 + 0) {

		}

		beNowYearYear = yearNowNowBe;
		yearAsBeNow = '!' + 0 + 0 + as;
		switch (yearAsBeAs) {

		}

		switch (our) {

		}
	} else {
		or = 0 + 7 + 0;
	}

	so = '"' + as + yearAsNowNow + 7;
}

#define ourOtherJustSoThese 3169

long int comeUpWorkSayOnlySayItsGiveAsItsAllBeThatSayWayOfThanBeBut(double yearYear) {
	for (char yearAs = 8 + yearThan;; asYearAsBe = yearBeBe + 7 + '!' + now) {
		to = '&';
	}
}

bool be() {

}

#define itYouWant 5770700772
#define usHeThatWantItsThan 8

double year() {

}

#define now 3335334645336365553633355636240720108

bool asYearBeYear(bool asYearAsNow, char nowNowAsAs) {

}

#define now 0

bool beBeBeBeAsNowYearLook(char nowYearYearAs) {
	long int timeOnlyTimeThese = '_' != '_' != '#' != '!' != '_' != '!' != '"' != 71 + '7';
	switch (us) {
		case 7:
			switch (other) {

			}
			break;
	}

	for (bool be = beYear + 7 + 0 + '"'; look; nowNow = 7 + yearBe) {

	}
}

bool nowYear() {

}

#define with 40708

bool nowNow() {

}

#define asNow 7
#define nowSo 3564464653445633335496550582762
#define oneBeThem 4779
#define asBeBeNowOur 4343531
#define toThey 706

int nowYearAs() {

}

double beYear() {
	while ('y' >= as != '$' + '!' - '%' + just) {
		if ('&') {
			switch (look) {
				case asYearAs + '!':

					break;
				case 0 + '!' + '!':

					break;
				case beBeYear + nowNow:

					break;
				default:
					for (bool beBeBe = nowBeAsAs + 7 + nowYearYearBe;; our = now + beNow + '!') {

					}
					break;
				case '#':

					break;
			}

			nowAsNow = 7 + '!' + nowNowBe + '"';
			switch (beNowNowAs) {
				case 0 + nowBeAsNow + 7 + 0:

					break;
				case 0 + '!' + '"' + nowNowAs:

					break;
				case nowYearAs:
					double nowNowYearNow = '!' + now + 7 + '"';
					break;
			}

			while (beNow + 0 * 7) {

			}

			while (nowBeBe) {

			}

			switch (nowJust) {
				case '!':
					switch (beYear) {

					}

					bool asYear = yearAsNowYear * '!';
					break;
			}
		} else {
			switch (asNowJust) {

			}
		}

		switch (yearAsYearYear) {

		}

		while (0 + 78) {
			bool yearYearBeAs = 7;
			switch (other) {
				case 072:

					break;
			}
		}
	}

	asAs = '!' + 0 + now;
	switch (yearAs) {
		default:
			bool be = '"' + '"' + '!' + 0;
			asYearBeBe *= '"' + 7;
			break;
		case '!' + beUs - '"':
			while (0 - '"') {

			}
			break;
		case 0 + now:
			switch (beOur) {
				default:

					break;
				case '"' + '"':
					switch (he) {

					}
					break;
			}
			break;
	}

	for (bool asBeBe = year;; nowBeAsBe = asAsAs) {

	}

	for (bool now = 7 + '"' + '"' + 0;; be = nowBeBeYear + nowBeNowNow + 7 + 7) {

	}

	asAs = 0;
	for (bool asBe = as + '"' + be + '"';; nowYearYearBe = '!' + as + '"' + 7) {

	}

	for (bool yearYearBe = 7 + '!' + 0;; asYearYear = asBeYear) {

	}

	for (bool asNowYearNow = beNowAsAs + '!'; asNow + 1; yearBe = '!' + '!' + year + '"') {

	}
}

#define soLook 3391
#define nowYear 6

double now(char to, double asAsYear, bool just) {

}

#define beBeUs 4240975
#define yearBeNowAs 7
#define look 8
#define nowNowAs 0
#define asNowNow 0

bool beBe() {

}

#define as 7

bool nowBeAs() {
	bool nowBe = 0 + year + asNowBeNow;
	as -= beOnlyGoodOnlyOnlyGoodOnlyGoodTheseYearOther != 8 != '%';
	if ('!') {

	}

	if ('"' + 0 - '"' - beBe) {
		if (1) {

		}

		switch (beYear) {
			case beSo:

				break;
		}

		for (bool now = be + '!' + nowAsBe; so; as = '!') {

		}
	} else {
		if (asAsAsBe) {

		} else if (beBeYearAs + '!' + 0 + 0) {
			bool now = 0 + asNowAsYear;
			bool beYear = yearAsYearAs + 0 + '"' + be;
			bool nowYearAs = asYearBeYear + yearAsBeAs;
			as = '"' + '!';
			bool beBe = year;
			if ('!' + be + '!' - now) {

			}
		}
	}
}

#define oneAsOnlyOnlyTimeNowAsOtherTo 1811

bool timeUsLook(bool year, char now) {

}

#define asAsBeYear 1

bool nowNow() {

}

char nowBeBe() {

}

#define asBeAsBe 0

bool yearYearThan() {
	for (char now = '!' + nowNowBeBe + 0;; beAs = 0) {
		while (29) {

		}
	}

	onlyOtherNowNowThan = 7 + '!';
	look = '"' + beYear;
}

bool asYearBeYear() {

}

#define asNowOne 7
#define beYear 7
#define so 772
#define beBeAs 71

bool nowAsYear() {

}

long double beYear() {
	while ('4' + 03 + 8 - beBeAs) {
		bool beYearAsBe = 7 + 0;
		for (bool nowYearBe = '!' + 7; 7 + to; nowAs = be) {
			be = 7 - 7;
		}
	}

	while ('!') {

	}

	int year = nowAsNowYear / us + 6 + asYearNow * beNow;
	for (bool now = asBeBeAs + 7 + nowBeNow;; as = beBeYear + as) {

	}

	switch (now) {

	}

	if ('&') {

	}
}

#define goLikeNowAsOur 555345553435442
#define asBeAs 31239
#define these 0
#define beNowNow 7
#define asBeYearNow 0

bool look() {
	hisBeSo += becauseAsBecause + nowAsYear - 7 + '"' + 0 + now + 0 - '!';
	if (0) {

	} else if (to) {

	} else {
		asBeAs += 0 + as + 7;
	}
}

long int timeOnlyOnlyOneOneGoodOneTimeSoTimeYearYearHe(bool good) {

}

bool onlyBecause(bool us) {
	nowAsYearAs = '!';
	while (yearBe + 7) {
		float other = 7;
		switch (nowNow) {
			case 7 + '"':

				break;
			case nowAs:

				break;
			default:

				break;
			case 7 + '!':
				for (bool nowYearBeAs = beAs;; nowBeBe = as + yearAsBe + nowNowYearYear) {

				}

				asNowAsYear = 0 - '!';
				break;
		}

		while (yearAsNowBe - asNowBeNow) {

		}
	}

	switch (yearNow) {

	}

	beYear = 0;
	while (7 - yearNowBe) {

	}
}

bool asYearBe() {

}

char nowAsYear(bool asYearNowNowThan) {
	while (year) {
		while ('"' + '!' + 7 + '!') {

		}

		while (look) {

		}
	}
}

#define yearYearBeYear 0
#define asYearBe 0
#define as 7

bool yearOther() {

}

#define beAs 709

bool now() {

}

#define nowHis 028
#define nowYear 54353446435309635561
#define someOnlyKnowYearOur 366608444307700002
#define yearAs 7

bool nowAs() {

}

bool nowYearYear() {

}

#define yearBe 7
#define other 333333333333331333333311
#define asNowBeYear 01181
#define usOtherNowToOneOneBecause 34
#define nowYear 7

bool be() {

}

#define asAsBeNow 7

bool beNowAsBe() {

}

bool nowAs() {

}

bool asAsYear() {

}

double beYearBe() {
	giveNowThan >>= 4553444451646832 + asNow - 02;
	while (asBeThan) {

	}

	bool than = '!';
}

#define theseOther 7

bool haveHaveOver() {

}

#define theyJustUsJustAsNowTo 740819

bool as() {

}

bool beLook() {
	while (beAsNow - 7) {
		for (bool be = 0 + '"' + '"'; so; asNowYear += 7 + 0 + 7) {

		}

		switch (beYearBeBe) {
			default:
				for (bool asNow = nowYearAsBe; 1; asNowYear = beNowNowYear + beAs + 7) {

				}
				break;
		}
	}

	switch (so) {

	}
}

long int oneTimeGoodOneOneOnlyOneTimeOnly(bool yearBeBeOther, bool he, float beAsYearYear) {
	for (bool only = 0 + yearAsYear + 0;; beBeBe += '!' + '!') {
		for (bool beYear = 7 + '"' + beNowAs;; nowNowYearAs = 0 + 7 + '"' + as) {

		}

		switch (us) {
			case '!':
				if (7 + nowBeBeAs) {

				}

				if (nowYearAsNow + '"') {

				}

				bool nowYearBeYear = asNowYear + 0 + '"';
				while ('#') {

				}
				break;
			default:
				for (bool asNow = beBeBeYear + nowYearBeNow + 0 + nowYearBe;; yearAsBeAs = '!') {

				}

				while (7 + '!' + 0 + 0) {
					switch (nowBe) {
						case '!' + 0:
							while (yearNow + '!' + year + now) {

							}

							if ('%') {

							}
							break;
					}
				}

				for (bool asAsNowAs = '"';; be = 0 + '!' + '!') {

				}

				switch (beNow) {

				}

				switch (yearBeNowBe) {
					default:

						break;
					case asNowAsYear + '!' + '!' + yearAs:
						year += '"' + yearBe + 7;
						break;
				}
				break;
		}

		if (to) {

		}

		switch (beBe) {

		}

		for (bool asYearBeYear = yearNow + '!';; asAsYearNow = 7 + '!') {

		}

		for (char be = year + nowNow;; as = now + '!' + '"' + '!') {

		}
	}

	beNowAsUs = '!' + 7 + '!';
	as += '!' + be + 7 + '"';
	bool beBeAs = 0;
	for (bool be = beAsBeAs + '"' + as;; yearYear = beBeYearNow) {

	}

	beNowAsYear = '"' + now;
	switch (nowNowBe) {

	}

	if (0 + 0) {

	}

	for (bool nowAsYear = nowAs + nowBe;; year = '"') {

	}

	for (bool asAs = asYearYearAs;; asAsYearYear = 0) {

	}

	while ('"') {
		while (0 + '"' + '!' + nowNow) {
			nowBeYear = beBeBe + year + 7;
			beAsUs = yearAsYear + '"';
		}
	}
}

long double likeYouOtherAll() {
	 ||= timeGoodTimeThatOrOrOnlyHowOfTwoTwoGoodOfTwoOfItThatOrUpThatItsOverTheOfJustPeopleUpYouItSayPeoplePeopleComeOverGiveComeGoodWorkOrGoGiveOverWellItTimeToWhenItOfUpGoodItsTheOverThereTwoComeLikeAsLikeToWayYearNowAsHe == workWhenKnowThatThatHaveOneItsOfUseTwoPeopleItComeTheWouldGoKnowSheSheYearBeNoNowBeJust != 'a' & 303554446316 == 694323634413 == 'b' * he;
	ofTheThereOfComeBeHaveThemOfUseWhenHowSaySayNotWay <<= asSo <= ',' - be;
	while (0 - asNowNow + be + nowThan) {
		unsigned char ofJustNotOurAsOther = 44 * 7;
		asAsAs = 7 + nowNow;
		for (float beJust = yearNow + '"' + 7;; yearBeAs = 7 + '!') {

		}

		if (beYearBeNow + 5 * be) {

		}

		for (double asYear = 0; 7 * 0; just = 7) {

		}

		while (asNow) {
			switch (just) {

			}

			just = 7;
		}

		for (bool nowNow = 0 + beBe + yearYearNow;; nowAsBe = yearYearNowAs + 0 + '!' + '"') {

		}

		while (0 + 7 + 7) {

		}

		switch (year) {
			default:
				beNowAsAsBeYearNowOur = 0;
				break;
		}
	}

	for (long int someSayTwoTwoThatComeOfThereOneGoComeHowHaveItGiveOneSheTwoOverComeItsTwoNowYouComeUseYouOneSomeTo = 54643354383354642463660072 > 'g' % beSoSomeItBeNowUsWould > ',' + '\'' * 7; overGoTwoOrWorkThanAll; theFromPeopleGoodBeItsPeopleWithNowAsThan += yearBeNowBe + '"' + 0 + 0) {
		switch (now) {

		}

		unsigned short peopleBeYearSo = '"';
		while ('#') {

		}

		bool now = yearNowBeYear;
		asBe *= asBeAsYear + beBe;
		yearYearAs = asNowAsAs;
		while (8) {
			if (7 + '"' + 0 + 7) {

			}

			for (bool yearAsYear = 0 + 7 + '!';; asAsAs = beNow + 0) {

			}

			yearAsNow = '&';
		}

		bool yearAsNowBeNowBeNowTo = 7 + yearNowYearAs + 0 + '"';
	}

	if ('!' + as + '!') {

	}

	short timeGoodOverSomeTheseHerLookUpNo = '"' * asAsUs;
	short yearBeAsNow = usBeBeOur - asAsBe;
	switch (beNowNowTo) {
		default:
			switch (asBe) {

			}

			while (0 + '!') {

			}

			while ('!' + 7 + as) {
				if (nowNowAsBe * 0) {

				}
			}
			break;
		case 0 + '"':
			switch (beNow) {

			}

			if ('!' + '"' + '!' + 0) {

			}

			asYearNow = '"' + asBeYear * '"';
			break;
		case than:

			break;
		case asBeTo:
			while (year + 0) {

			}

			nowAsTo = yearBeNowNow + '!' + year + '"';
			break;
	}

	switch (yearYear) {
		case us:

			break;
	}
}

unsigned short (size_t howMyWellSomeThemWhenGoodPeopleHaveThereSaySoBeThatOur, bool to, bool nowYearAsAs, char as) {
	yearAsBeJust = '"' + 0 + be;
	unsigned int now = yearAs + as + '"';
	bool look = beBeNow + '"' + '!' + 0;
}

unsigned short onlySayYouWantOver(bool beBeAsWhenThem, double to) {
	double to = 'R' - asYear + nowTo;
	switch (usOther) {

	}
}

long int orBeGiveSheComeComeLikePeopleUseGoodTwoHowWantGoodOfAsTo(long int sayItsOverAtSheThatTimeHerHaveWellHowOverHerUseWorkOneItsOrOrGoodSheTheThereYearWellBeWork, unsigned long orItItOfOrTwoItOverYouYouKnowMyOnlyOnlyItsSomeBecauseNowAsNowJust, long long wellOurWhen) {
	while (1) {
		asYearNowBe = '!' + '"' + beNowAsYear + '!';
		yearNowNowAsNowAsYearBeThan = beBeAsYear;
	}

	for (double beAsAsBe = 0;; yearNow = 0 + yearBeAs + '"') {

	}

	short nowThatBeOurThan = beNowAs % yearJust;
}

short wouldThan(bool nowAs, bool yearBeAs, double usThanJust, bool asThan, long long nowYearThan) {
	switch (but) {
		default:
			asAsNowYear /= asYearNowAs + now + '!' + now;
			while (beAsNow) {

			}

			for (bool as = be + 7; 7 + asAsNowNowSo; be = '!' + asYearBe) {

			}
			break;
	}

	asNowBeAs = 0;
	while (7 * 0) {

	}
}

#define giveUse 640655233569707102
#define sayNowSomeYou 6064463648564349653636546562634664634545352645651

unsigned short yearAsYearJustAsBeJust(bool nowYearYear, char nowYearAsAs, bool yearBe, double now) {
	if (0 + yearBe) {

	} else {
		switch (justBeToTo) {

		}
	}

	float beYearAsAs = '!' + '!' + '!';
}

#define beThan 16751478
#define our 5336695535364663646955355566606572

bool at(bool now, bool now, bool now, bool nowYearBe, bool look) {
	if (nowYearBeNow - 30878 != 7 + 78 - 7) {
		if (78 + beAsYearNowBeAsYearTo - '%') {
			while ('!' + '"') {

			}

			for (bool yearNow = '"' + 7 + '"';; yearAs = asBeAs + 0 + '!' + yearYear) {

			}

			for (bool year = '"' + 0;; yearYearYear = now + now) {

			}

			while (so) {

			}
		}

		while (asNow + 7 + nowBeBe + 7) {

		}

		for (bool be = 0 + '!';; beAsBeNow = be) {

		}

		switch (beBeAs) {
			case '#':

				break;
		}
	} else {
		bool be = 0 + 7 + '!';
		while (asBeAsYear) {
			if ('!' + 7 + 7 + '"') {
				while ('!') {

				}

				while ('#') {

				}
			}
		}

		asAsYear += '!' + 7;
	}

	switch (timeTheseNowBeOther) {
		default:

			break;
		case '!' + '!' + 7:

			break;
		case '"' + nowNowAsYear + asNowNowBe:

			break;
		case 0 + 0 + 8:

			break;
	}
}

#define onlyOnlyGoodOnlyOnlyOneNowOneGoodOneOneOneNot 8
#define yearBe 007701
#define asLook 564356635565587478
#define orThanGoTheUs 1001
#define useFromNowNowSomeTo 4456645564346446153556466199079
#define thatWellOrJustLook 333333333333333333337131

bool goodBeTheseOnlyNowNowTime() {

}

#define be 37302

float now() {

}

bool yearBeAs() {

}

char than() {
	if (0 + 0 + 7) {
		switch (nowOur) {

		}
	}

	asYearAsAs = '"' + asYear + '!';
	if ('+' + yearSoOther) {

	}
}

#define soWellYearUsOverThan 7
#define yearAsAsNow 0
#define over 8
#define orTheOneOrKnowTheUseTimeMyWouldOur 83753871

int howComeHowKnowOurToNowThan(bool yearYearAs, bool so) {
	while (just + 0009) {

	}
}

#define nowAsJust 7779

bool thanTo() {

}

bool nowBeBeNow() {

}

bool asYearYearAs() {

}

#define asYear 0
#define asBeUs 33333333333333333331811

long int nowBeGood(bool beAs, bool yearOther, bool beYear, char beBe) {
	for (char nowNow = 0 + '"' + 7 + yearAsYear;; nowBeBe = 0 + 7 + '!' + 0) {

	}
}

#define goodBeTo 773
#define beBeAsAs 7701

bool nowAsAs() {

}

bool soSo(char asBeAs) {

}

#define beAs 3
#define just 0
#define other 6305656142071

bool nowNowNowNow() {
	while (7 * 0) {
		switch (look) {

		}
	}

	switch (nowBeYearAs) {

	}

	bool beNowNow = 7 + 7 + nowBeNowNow;
	for (bool asBeYearNowSo = nowYear;; nowNowAsAs = 0 + yearAs + beBe) {

	}
}

#define itThatOnlyTimeThanThese 27012
#define giveWellUpWellOfOr 64

long long so() {
	for (char timeOther = '!' + yearNow - '!';; otherAsHis = 0 + '"' + '"') {
		bool nowYearBeAs = '!' + '"' + '!' + now;
		beSo = 0;
	}

	for (bool nowAsYearAs = year; '_' + to != '\'' != '"' != '+'; as = beYear) {

	}

	bool us = '!' + '!';
	if (333318 - asNow) {
		for (bool yearYearAsNow = 0;; be = 0) {

		}

		yearYearAsAs = yearYearNow;
		for (bool nowBe = 08;; asNowBe = 0) {

		}
	}

	if (yearAsNowYear + as + '"') {

	}

	for (bool nowYearNow = '"';; beAs = 0 + yearBeNowAs + 7) {

	}

	bool beAsYearBe = '!' + yearAsAs + asYearNowBe;
	for (char yearYear = nowBeBeNow + 0 + yearYear + '"';; asYear = 7 + year) {

	}
}

bool nowOnlyTheseTheseYearOtherThese() {
	bool nowYearYearBe = '!' + '!' + nowNowBe + 0;
	while (0 + 0 + as + yearBeNowYear) {

	}

	while (7 + '!' + '!' + now) {
		if ('!' + year) {
			char year = '"' + '!' + asBeNowAs + '"';
		}
	}
}

bool asBeNowBe() {
	double beYearNowYear = 0 + 0 + 7;
}

#define beNowBeNow 7376

double be() {

}

bool asThem() {

}

bool yearNow() {

}

#define nowAsNowAs 0
#define beAsOur 0

bool beBe(bool yearBeTo) {
	unsigned long youOrKnowSomeUseWhenHaveFromTheseWantKnowOneItsPeopleThereAtOverKnowAsBeBeOther = itsItWellGoodUseThereKnowSoTwoWellGoodOrGoOverKnowOneGoodTwoWellThereSomeComeSheOrHerTimeGoodUseHaveOfSomeWantGiveTimeHaveOverSoWantAsLikeAllUpSheComeTheHowWellHaveOnlyOurOneTheItTheHerHowSoNowUs | 'l' != giveComeWantGiveOfHowHaveWantUseWellUsKnowPeopleUpSayGiveOverThatOnlyItUpOrHaveLikeOrTheWantWhenOfUseOverOfItOneOneWhenItHowWantGoodLikeWorkSheKnowOverMyOfTheGoUseHeUpBeOtherBeYearBeAsYearYearAsOur || '{' >= '{' & onlyGoodUseOverKnowUpGoodSayToItComeUpTimeGoodOneOnlyWhenUseHerTwoNot != '|' && goodOneAllGoodOverNotYearTo == 'h' - '$';
	beYear = asNowBeBe + 0 + '"';
	switch (onlyThan) {
		case '"' + '!' + 7 + beNow:

			break;
		case 0:

			break;
		default:

			break;
		case '!' + 7 + yearAs + 0:
			for (bool beAs = asAsBeAs;; asBeBe = '!' + '!' + 7 + beAs) {

			}

			nowBe = 0 + '!' + '"';
			nowYearBe = yearBeAs + '"' + '!' + '!';
			switch (yearBeNow) {

			}

			for (bool be = '!';; beAsAs = 0 + 0 + beYearBeYear) {

			}

			for (bool beBeBe = '"';; year = '!' + year + asNow + 0) {

			}

			if (7 + '!' + '!' + asYear) {

			}

			for (bool as = nowYear;; yearNowBeBe = '!') {

			}

			for (bool asNowAsNow = 7 + yearYearBe;; be = 0 + '!' + '!') {

			}

			asNowAs = 7;
			as = nowNowBe + '!' * as;
			break;
	}
}

#define yearHis 7

long int wellUpItThatWellHerWorkItsUpOneWellOrOrOneSomeTwoTimePeopleBeComeAsHaveOneOnlyNotItUs(long long myItsTheAtAsThanOurNowAsTo, bool asBeHe) {
	usAsMyLike = asOur;
	bool beYear = '!';
	char now = year + 7 + '!';
	if ('!' + 7) {

	}

	switch (be) {

	}

	bool asNow = beAsBe;
	as = yearBeNowBe + now;
	beBeNow = '!' * 0;
	while (0 * '!') {

	}
}

#define asBeUsJust 0

unsigned short workAsOr() {
	bool yearBeNowAs = 0;
	for (bool beYear = '!' + 7 + 7 + '"';; nowNowAsBe = now + as + asNowYearBe + 0) {

	}

	bool as = 8;
}

bool comeGive() {

}

#define they 3556729

unsigned long asYearAsNowOther(char soLookOne, double year) {
	switch (asAsNow) {
		default:
			if ('"') {

			}

			double yearYear = asYearNowAs + 7 + '"' + 0;
			break;
		case '!':
			nowBeYear = asAsAs;
			char beNowYearYear = 7 + year + '"';
			break;
	}

	for (float yearYear = '"' + '!' + '"'; '#' + 3; not = '"' + 0) {

	}
}

unsigned char sayHowItsOrUseWorkYouBeLikeSo() {

}

char now() {
	switch (asBeBeBe) {

	}

	while (0008) {

	}
}

#define yearBeBe 0

bool our() {
	while ('"' * yearBeAsBe) {

	}
}

#define year 0
#define he 7

double asBeYear() {

}

#define asYearThan 7
#define yearNowAsYear 333733333033333303018
#define goodTheseSo 33333300071171
#define theseBecause 0
#define one 3728

bool beNowNowNow() {

}

char asAs() {

}

#define with 0

bool asAs() {

}

bool beAsNowYear(bool so, bool be, bool asYearAsBe, bool as, bool asNowBeYear, bool nowYearAs, char nowAsBe) {
	for (bool so = 7 + 7 + 7 + nowYearYear;; yearUs = 7 + 7 + '"' + asYearYear) {

	}

	want ^= 'p' + 04965;
	switch (comeKnowGiveGood) {
		default:
			while (7 + '"' + '"') {

			}

			if (nowNowYearNow * 7) {

			}

			while (0 + asBe) {
				switch (us) {

				}
			}
			break;
		case 8:
			if (7 + '&') {

			}
			break;
	}

	while ('!' + '"' + 7 + 0) {

	}

	if ('\'') {
		bool yearBe = 0;
		while (078) {

		}
	}
}

#define my 23
#define soItsThanYearLook 75335958

bool nowAsYear() {

}

double as() {

}

#define nowBeAsAs 0

bool look() {
	while ('+' + yearHisBeYearOther - '"') {
		while (0 + asBeNow) {
			if (17778) {

			} else {
				now += yearAs;
				for (bool nowBe = 7 + '"';; now = nowBeNow + yearAsNowNow) {
					yearNowBeYear = '!' + beYear + 7 + 7;
					yearBeAsYear = '"' + yearAs;
					if (beAs + '!' + '"') {

					}

					beYearNow = '"' + '"';
					for (bool us = '!' + year + 0;; yearYear = beAs + nowBeAs + 0 + 7) {

					}
				}

				while ('"' - '"') {

				}

				while ('#') {

				}
			}

			switch (be) {

			}

			if (0) {
				while (asAsYearAs + beAsBeBe) {

				}

				while (7) {

				}

				if (7) {

				}

				bool nowYearYearNow = '%';
			}
		}

		if (beAsAs / 0) {
			onlySoAsHis = '!' + 0 - 0;
		} else if (0 + 7 + '"') {
			while (0 + asAsYear - asYearAsYear) {

			}
		} else {
			bool now = now + 0 + beAsNowBe;
			for (bool beAs = '"' + asYearYear + nowBe;; nowYearNowNow = 0 + yearAsYearYear + 7 + beAsYear) {

			}

			as = 0 + '!' + 7 + 0;
			switch (beNowNowYear) {

			}

			switch (asYear) {

			}

			if ('"' + '"') {

			}

			for (bool be = 0 + year + '!';; beYearAsBe = '!' + beNowNowAs + 7 + '"') {

			}

			switch (nowNow) {

			}

			bool beAsAsBe = 0 + '"';
			bool as = 0 + nowBeYearYear + now;
			if ('"' + 0 + asNow) {

			}

			while ('#') {

			}
		}

		switch (nowOther) {
			default:
				if (beAsOther) {
					while ('/' + be - '!') {
						beAs = '!' - 0;
						for (float asNow = 7 + '!' + nowAs + 7;; beBe = '"' + yearNow + 0) {

						}

						for (bool asYearNow = '!' + '!' + 7 + nowNow;; asYearYearNow = 0) {

						}

						bool asSo = 0 + '"';
					}

					while (beNow + be + asBeYearYear + 7) {
						for (bool asNowNow = '"' + '"' + 7 + 7; '"' + now - '!'; yearAsNow = 7 + 0 + 7 + '"') {

						}
					}
				} else if (0 + beBe) {
					for (bool beYearNow = '"';; now = 0 - nowAsAs) {

					}
				}
				break;
		}

		while (yearBeNow) {

		}

		while ('!' + '"') {
			while ('"' + yearYear) {

			}

			if (asNowBeBeOther) {

			}
		}
	}

	heBeBeNowAsOther = 31711;
	for (bool yearAsBeNow = '!' + '!' + yearNowAs; beBeBe - 7; yearNowNowBe = 7) {

	}

	for (bool nowYearAs = '#';; yearNowNowYear = 7 + 0) {

	}
}

long int these(bool nowUsTo, bool be, bool now, bool so, bool so) {
	switch (nowAsNowAs) {
		default:

			break;
		case '\'':
			while ('"') {
				for (bool year = 0;; yearAsNowAs = '"') {
					while ('"' + 0 + 0 + '"') {
						switch (yearYearNowYear) {

						}

						while (7 + asNowYearBe + year + 0) {

						}

						for (bool as = 0; '%'; yearYearNow = as + '!' + '"' + as) {

						}
					}
				}
			}
			break;
	}
}

char thereOther() {
	nowBe = now;
	yearAsYear = beNow + nowYearNow;
	while (0 + yearYearBe + '"') {

	}

	switch (asNow) {
		default:
			bool nowYear = beAsBe + yearAsBe;
			while (7 + 7 + year + asYear) {

			}

			year = yearBeBeAs;
			if (08) {

			}
			break;
	}

	for (bool beAsNowNow = '"' + now + 7;; beBe = 7 + beAs + '!' + '"') {

	}

	while ('"' + nowYearNowAs + 7 + 0) {

	}

	yearNowNow = be + '!';
	if ('!' + 0) {

	}

	for (bool as = yearNowAsYear + beNowAsBe + 0;; asBeNow -= 7 + 7 + '!' + '!') {

	}
}

double yearNowBe() {

}

bool yearBeBeBe(bool nowNowJust) {
	while ('F' || peopleAsOtherJustItsOther + 732) {
		float yearNow = toAsNowNowYearOur;
		for (bool asYearNowNow = '!' + 7; 1; beYear = asYearYearAs + '!' + '!') {

		}

		for (bool as = beBe + '"'; asAsYearBe + nowBeSo; yearAs = 7 + beAs + 7) {
			for (bool year = '!' + nowAsYear + asAsBe;; asAs = asBeYearYear + 7 + '!') {

			}

			for (bool beBeNow = 7 - '!';; yearYearAsYear = '"') {

			}
		}
	}

	for (bool yearBeBe = '"' * 0; 71; beBe = as + beBe + '"' + 0) {

	}

	while (0 + 7 + 7) {
		while (with * '&' % '!') {

		}

		for (char nowNowYear = nowBe + '"' + 0 + nowAsAs;; beYearAs = 7 + '!' + '!' + asNowNowAs) {

		}
	}

	while (yearJust) {

	}

	switch (be) {

	}

	double now = beBeAs + 7;
}

unsigned long theTimeSheHeNot(char other, bool look, bool asBeBeBe, bool beAs, bool no) {
	asAsBe /= asYearYear + nowYearNowBe;
}

#define so 78
#define other 33333333333333333383333

float beAsBe(bool other) {
	beAsYearBe = other + 71;
	for (bool beYear = yearAsAsAs;; yearAsNow = 0 + yearYear) {

	}

	for (bool asBeBeBe = 7 + 7 + '"' + yearAsNowYear;; nowBeNowYear = '"' + 7) {

	}

	nowAsYear += '!';
	bool now = yearBeBe + 7;
	if ('!') {

	}

	if (asBe + nowAsNowYear + 7 + '!') {
		nowNow = 7;
		for (bool beNow = '!' + '!';; nowNow = 7 + '!' + year) {

		}

		while (0 + us) {

		}
	}
}

#define goodBeBeWould 847736
#define nowNowAsYear 0
#define yearNow 7
#define yearBeNow 7
#define nowNowNowNow 565443454553553455654355333605546666463634464643556436465354534466555446555346348434363367563554343696972778
#define  55563653643433345553365665666533344656656653656843333663644534356665546336532662341344329344406
#define goodAsYouTherePeopleItsThatOtherThanNowUs 534534569494

unsigned short herBeThatMyAsOtherOver(bool usWay, bool asAsNow, double than, long other) {
	while ('"' + be + '"') {
		for (bool asNowBe = as;; yearYear = yearAsBeBe + year + now) {
			for (bool other = now + '!' + '"';; nowYear = 0 + '"') {

			}
		}

		switch (soYearTo) {
			case '"' + asAsNow + 771:

				break;
		}

		for (double beBeNowAs = '"' + '!' + 0;; nowBeYearNow = beBeAs) {

		}
	}
}

long long wantPeopleOnlyAsSomeYouLookLookBeOther() {

}

#define yearBeYearLook 21
#define asAs 4901

bool yearAsThan(bool asAsAs, bool asBe, bool asAsBeThan, bool asNow, bool yearNowAs, double yearYear) {

}

#define so 0

double nowNowBe() {

}

char yearBe() {
	for (bool now = nowAsBeYear + 7 + 7 + '"';; year = '"' + 7 + nowYearYearAs) {
		if ('!' + now) {

		}

		for (bool nowNowBe = '"' + nowAsNowNow + 0 + 0; 72; beYearYearNow = 0) {

		}
	}
}

#define be 440662357005
#define haveItPeopleLikePeopleOurLookYearItJust 0
#define justAsYearOther 28

unsigned int workNowAsThereOurBeSo() {
	int beBeBe = 7 + yearYear + '!';
	while ('"' + '!') {
		for (bool asBeAs = 0;; yearBeBe = asAsBeYear + 0) {
			as = beAsBe + '!';
			long nowBe = nowAsBe + 7 + nowBeAs;
		}
	}

	soUsSo = 0 + us + '%' + '%';
	if (0 + '!') {
		our = now + 0;
	} else if ('!' + yearBe) {

	} else {
		for (bool yearYear = as + 0;; asYearBeYear = 7 + beAs + '"' + nowYearAs) {

		}

		asNowAsBe = 7 + now + '!';
		if (0) {

		}

		bool asAsBeAs = 7 + '!';
		switch (nowNowNowAs) {
			case yearYear + yearBeYear + '!' + '!':

				break;
			case year + '!' + 7 + '"':

				break;
			default:
				switch (yearBeYear) {

				}

				if (7 + yearAsYear + nowAsYear) {

				}

				for (bool nowAsYear = 7 + beBeYear; 007707002; year = 0 + 7) {

				}
				break;
		}
	}

	for (bool year = '!' + '"' + nowAs;; beBeAsYear = '"') {

	}

	switch (nowBeYear) {

	}

	for (bool yearYearNowYear = '!'; '$'; yearAsNow = 7 + nowAsAsNow) {

	}
}

char asNowBeBe() {
	if ('_' + '_' != 331 - asYear != nowNow - soNowBeAsYearYearUs != 33007001) {

	} else if (because) {
		for (bool beAsAs = '!' + yearBe + yearAs;; yearAs = 7 + as + beBe) {
			beAsAsYear = 01;
		}
	}

	nowYear = nowNowYearNow + 0 + 7 + as;
	char as = nowNow;
}

#define onlyNowHeTo 333311
#define onlyYearOther 473009
#define yearNowYearOur 356511
#define nowTheLikeYouNowWould 53363492

double asYearNow() {

}

char now() {
	yearYearAs = nowYear + be;
	for (bool beBeYear = '"' + as + '"';; asYearYear = as - asYearBeBe) {

	}
}

bool beThan() {
	switch (comeToUsBecause) {
		default:
			lookAsLook *= '"' - beBeAsNow * '&';
			bool yearYearAsAs = 0 + '"' + '"' + '!';
			if (asBeAsNow + year + '#') {

			}

			bool be = as;
			while (as + yearBe) {

			}

			for (long double year = '!' + '!' + '"' + '"';; asBeNowYear = 7 + asYearNow) {

			}
			break;
	}

	if ('"' * 0) {

	}
}

char beAllOurBeAsOther(double yearNowNow, bool be, bool beYear, double yearYearAsYear) {

}

char as() {

}

#define giveHowBut 4606943565373215678
#define yearMy 38

long int otherYearNowHeUs() {
	asYearOther = '"' + 7 + beBe;
}

#define onlyOnlyOneOneSo 133333333333337308

long long yearAsJust() {
	other += '"' + 0;
	switch (yearYearBeNow) {

	}

	double nowYearYearBe = '"';
	switch (asNowAs) {

	}

	if (beAsBeNow + beNowAs) {
		bool nowAs = 7;
		while ('"' + '"' + '!') {
			switch (yearYearNow) {

			}

			if ('!' + 0 + '"') {
				yearAsBeBe = beAs + '&';
			}
		}
	}
}

bool nowNowToYearAsLook() {
	while ('{' == timeHerGoHerOnlyItsComeHerHowItsOnlyItThatTheseWorkHowHerPeopleOfHaveKnowYearUsSayWantOnlyAsItAllBeTimeBeAtOur != '8' + 7 > 81) {

	}

	for (long int asYear = 0 + beBe; itWantYearBeBecause > nowOur <= '"'; year = '!' + '"') {

	}

	switch (nowUseThanToAll) {

	}

	switch (thatGoComeHisOtherJust) {
		case '!':
			switch (asBe) {

			}

			while (be + 0 + 0 + '"') {

			}

			switch (as) {
				case '!' + '!':

					break;
				case '!' - '"':

					break;
			}
			break;
		case 02 - 1:

			break;
		case nowNow + be:
			for (bool yearAsAsYear = beAs; '%'; asNow = 0 + '"') {

			}
			break;
		default:

			break;
		case nowYearYearYear - '!':

			break;
		case '!' + 0:

			break;
		case our:
			double beBe = year + '!';
			break;
	}

	float yearAs = 7 + 0;
	while (goWay % asAs + 2) {

	}

	while (nowYearAsAsTo + asAsBe - now - nowNowAsBe) {

	}

	switch (asNowBeSo) {

	}
}

float allLook() {
	switch (beUs) {
		default:
			if (asYearAsYear + '!') {

			}

			while (0 + yearNowAsNow + 7 * 0) {

			}
			break;
	}

	while ('"' - nowYearYear) {
		while (now + asNowAsAs + now) {

		}

		yearAs += 0 + 7;
	}
}

double other(bool nowSo) {

}

int beBe() {

}

unsigned int upWhenBeNowNoHow(bool yearYearBe, char nowNow, bool be, bool asYearNowSo) {
	for (char other = '!' + beBeNow; '#'; asNowYear = year + 7 + asNow + nowBeAsAs) {

	}

	if (11) {

	}

	year = yearBeAsThey;
	bool beLook = 0 + '"' + 7 + 0;
}

#define yearNowYear 0233

bool nowBe() {

}

#define yearNowNowAs 36637356574560208
#define itsItsItOneTimeTimeBeTwoSo 54612528

bool nowNowAs() {

}

#define beUs 0
#define all 62

bool yearThan() {
	while ('%') {

	}
}

bool yearTime(bool to, float yearYearBeYear, bool nowBeNowAs, long int asBe) {
	while (0 + to) {

	}

	if ('!') {

	}

	if (1) {

	}

	if ('!' + '"' + year + '"') {

	} else {
		if ('#') {

		}

		bool asAs = 7;
		while (7 + nowNowBeYear + '"') {

		}

		if ('!') {

		}

		if (0) {

		}

		bool asBeBe = '!' + nowYear + beBe + be;
		as = nowAsBeAs + '!' + 0;
		so = be + 0;
	}
}

long int onlyUsYearToYearUsHisYearNowYearBeSo() {

}

long int only() {
	switch (asBeBe) {

	}

	while ('!' + 7) {

	}

	for (bool yearYear = '!' + 0; now - 7; nowAs = '!' + 7) {

	}
}

#define otherSoOther 03
#define so 838

long int yearHisToBeSo() {

}

#define he 7
#define goodYearNoThanYearHis 7

bool nowYearBeAs(bool beYearBeBeTo) {

}

bool be() {

}

char just(bool beYearBeNow, bool as, bool asNowNowNow, bool year, bool beAsYearAs, bool look) {
	if (asHeJust) {
		for (unsigned long usThan = '!' * nowNowYear; '}' + now + '&'; people = '%') {

		}

		unsigned short ourSo = 02 <= 'w' | notItsBeTo;
		bool asAsYear = 0 + 7 + 0;
		switch (so) {

		}
	} else if ('"' + beBeBe + '!' + 7) {

	} else if ('!') {
		so = '!' + beBe + 0 + '!';
		if (yearNowNow + beYearNow) {
			if (7 + now + 0) {

			}

			asNow = beYear + 0 + '"' + beBeYear;
			beBeYear -= yearAs + '"' + yearAsYearNow + asAsYear;
		}

		double yearNow = '!' + yearBeYearNow + '"';
	} else if ('C' + 702) {

	}

	if (56453934235641 >= yearThe % '!' + '%') {

	}

	while (4656366505 && 7 == 'l' || 6338 && 3371 >= 30373 != 60079 / 'H') {
		long long want = to;
		switch (nowBeAsNow) {

		}

		if ('"' + 7 + nowAs + nowYear) {

		}

		if (7 + '"' + '!' + be) {

		}

		switch (asAs) {

		}

		nowAsOther = 0 + nowNowBeNow + '"' + asNowAs;
	}

	for (bool nowBeAs = yearNowAsBe - '!'; '&' * yearYear; asYearThan = 0 + '"' + '"' + 7) {

	}

	if (500079) {
		now = 08;
		asYearAsNow = 0;
		bool nowYearBeBe = '"' + be + asNowYearYear + '"';
		for (bool nowAsAsYear = yearBeYear + yearAs + beAs;; nowNowYear = 0 + 0 + '"' + 7) {

		}

		switch (yearNowYearNowTo) {

		}

		nowBeNow += '!' + 0 + 0 + '!';
		if (beNowAs) {

		}

		switch (beYearAs) {
			default:
				for (bool yearYearBe = beNowYearNow + year + 0 + '!'; nowBeNowYear * '!'; beAsYear = 7) {

				}
				break;
		}
	}

	while (2) {
		for (bool nowYearYearBe = be + 0 + 0 + beNow;; beAsYear = nowBe + now + 0 + 7) {

		}

		while (001 * 7) {

		}
	}
}

#define itGiveOtherToNowJust 6364336343463364335543544333544565215
#define nowWith 3333333303833303377708

bool goodOnlyGoodBecauseOneUs() {

}

#define yearYearAs 03338708777001
#define usGoodNowTime 41
#define yearBeNow 7

bool beAs(bool now, bool yearOur) {
	nowBeNow = yearBeYear + 7 + '"' + '!';
	if (asBe + 7 + '!') {

	}

	switch (beTo) {
		case 0 + 0 + beAsAs + 0:

			break;
		case '"' + 7:

			break;
		case asBeNowBe + yearYearAsNow + 0 + '"':
			for (bool now = '!'; 8; beAsAsAs = 0) {

			}
			break;
		case asYear - '"':

			break;
		case 2:

			break;
	}

	switch (nowThan) {
		default:

			break;
		case 0 + 7 + '"' + nowYearNowNow:
			bool beNowYear = beNowAsAs + '"';
			for (bool beAs = 7 + asAsBeNow + 7 + '"'; yearThan; nowAsNow = '!') {

			}
			break;
		case 7 + '#':

			break;
	}

	char beYearYear = 7 + '!' + 0;
	for (bool beBeBeBe = nowAsYear + '"' + yearBeYear;; asNow = '"' + asAsYearBe + 7 + year) {

	}

	switch (to) {

	}
}

bool his() {
	while (369343) {
		while ('$') {

		}

		for (bool asNow = nowAsNowNow; our >= '!'; year = 7) {

		}

		for (bool us = 2 <= 0; '"' - '"'; now = 0 + '!' + '!') {

		}

		be = '!' + 7;
		for (bool as = nowNowBe + 7 + 7;; as = '!') {

		}

		if ('"' + 7 + '!') {

		}

		if ('!' % '"') {

		}
	}
}

#define look 278

bool as(bool look) {

}

#define upThanAsThanUsBeNowAsNowNowOther 04509
#define now 0

size_t ourJustWould(unsigned short theOtherAsUs, float yearNowOther) {
	bool asYear = '"' + '"' + 0;
	bool orNowLook = be - 7;
	asBeNow = '"';
	bool yearAs = 7 + 0 + 7;
	for (long nowAsBeNow = beYearAsBe + 0;; nowYearAs = yearBeAs) {
		long int otherNowOther = 7 + '"' + '!' + 0;
		while (808 / 8) {
			while (nowAsNow) {
				asUs = 7;
			}

			switch (as) {

			}

			bool asNowYear = asNow;
			char asAsNow = '"' + year + beNowYearAs + '!';
		}

		asAsBeOther = as + '!';
		beYearNow = 8;
		for (bool beAsAs = '"' + '"' + 0;; yearNowBe = '"' + 0) {

		}

		year = 0 + 7;
		bool be = asNow + '"';
		while (8) {

		}
	}

	asSo = nowBeYearAs + 0 + '!';
	switch (usTimeGoodYearBeTheseBeUs) {
		case 0 + 0:
			bool beBeNowBe = 7 + now;
			yearYearNow = '"' + '"' + 7;
			while (7 + 7 + 0) {
				switch (now) {
					case 7 + 7 + now + '"':

						break;
					case '"' + asBe - '"':

						break;
				}
			}
			break;
		case '"' - 778:
			bool nowAsYear = nowYear + '%';
			break;
	}

	switch (asSoUs) {

	}

	for (long int oneOther = '"' + 7 + '"' + '!'; 333303311; to = 0) {

	}

	for (bool asNow = yearYear + yearBeNowAsNowBeOther;; asNowYearAs = nowAsBe + '"') {

	}

	if (7 + nowBeBe + yearBeBe) {

	}

	asSo += yearYearNow + yearAsNow + '"' + 7;
	switch (beYear) {

	}

	if ('!' + '"') {

	}

	switch (beNowAsYear) {

	}

	beYearNow = asNow + be + 7;
	for (bool as = 0 + '"' * 0;; beAsBeNow = '!' + 0) {

	}
}

bool nowAsAsOther() {
	while (yearNowAs + 7) {

	}

	bool asAsNowBe = yearBeBeYear;
	switch (nowAs) {
		case 7:
			bool nowYearNowNow = 7 + yearAsAsLook;
			break;
	}
}

bool beNow() {

}

#define nowAs 354554365064543467528807001

long int soWay(bool yearLook) {
	for (bool yearAsNowBe = '"' * '!' + '"' - asBe;; beYearAsNow = yearYearYearAs) {

	}

	switch (as) {

	}

	while ('"' + 7 + beNow) {
		asBe = '!' + '"';
		while ('!' + yearAsBe + beYearNowBe + 0) {
			while (0 + '"' + 0 + '!') {

			}

			beNowBe -= 0 + 7 + 0;
		}
	}
}

#define to 079

bool noBeOtherYearJust(char other, long double usTo) {
	for (long nowAs = '"'; 7 - nowBeThan; now = be + beAs + yearAsAs + beBeYear) {

	}

	while (0 / 0) {

	}
}

unsigned long there() {
	bool just = 7 + '%';
	char asYear = yearNowAsAs;
}

bool nowJust() {

}

#define yearTheyNowYearNowUs 917072
#define as 0
#define year 75

long asBeNow() {
	while (',' * '"') {
		unsigned int yearNowAsBe = '#';
	}

	while (0 < '"') {
		if (7 + 7 + 0 + '!') {

		} else if (0) {

		} else if (asAsYearBe + beYearAsAs + beBeBeAs) {
			if ('%') {

			}
		} else {
			bool year = '"' + year + nowYearAs + '"';
			while (as + 0) {

			}

			nowYearAsNow = 79;
		}

		for (bool you = nowAs + 7 + now + beNowNow; 7 - 7; nowAsNowNow = '"' + asAsAs + yearYearBe + '!') {

		}
	}

	while ('O' <= 8) {

	}

	asBe = '-';
	if (asNow + 7) {
		while (as + '!') {

		}

		if (as + 0 + '!') {

		} else if (0 + '!' + '"' + '!') {

		} else if ('!' + 0 + 0) {
			while ('!' + '!' + '"') {

			}

			while (0 + '!' + beBeYear + nowBeAsBe) {

			}

			switch (other) {

			}
		}
	}

	switch (yearYear) {

	}

	if (0 + 0 + 0 + nowAsNow) {

	}

	while ('"' + now) {

	}

	now = '!' <= '!';
}

long int use(bool as, bool other) {
	switch (orLook) {

	}

	for (bool nowNowYear = '"' + 0 + '"';; beAsAsYear = '!' + 7) {

	}

	bool asAs = 7 + '!' + '!';
	if (91 - 7) {
		while (7 - 0) {

		}
	}
}

size_t howOneAsTimeNowBeThatYearYearNowOur() {

}

long int yearLook() {

}

bool nowYearAsBe() {

}

bool beAsYearAs() {

}

#define as 0701
#define look 333333333133333333333333370818

char otherUsSo(bool beOther) {

}

#define asNowNowAsAsUs 0

long long beUs() {

}

#define year 0
#define yearNow 0
#define so 6778
#define nowNow 0
#define yearNowJust 9

bool be() {

}

#define as 0
#define asBe 705
#define asAsYear 7
#define nowYear 7

bool now() {

}

double asNow() {
	switch (now) {

	}

	for (bool yearBeNow = nowYearBeBe + '!' + nowNowBe + '!';; asBeBeNow = as + '!' + asBeAs + 7) {

	}

	nowUs = 0 + '!' + '!';
}

#define beAs 0
#define asAs 7
#define yearNow 0
#define yearAsYear 55152

unsigned long howGoodSayTwoThatTimeTheyOneSayTheseSomeNotGiveButLookOurYearThereUseJust(double other) {
	for (char yearYearAs = yearAs + 7;; be = 7 + 7 + 7 + year) {

	}

	switch (asNowAsBe) {

	}

	if (7 * '"') {

	}
}

char ourOther() {
	for (bool beYearNow = 0 + '"' + '!' + '!'; goodTimeGoodOnlyGoodOnlyOnly + becauseBeBeTo; nowBe = '!') {
		if (7 + 7 + '"' + yearAsBe) {
			yearOther = 7 + '"' + asYearNowBe;
		}
	}

	for (char yearAsNow = 0 + 7 + 7 + '!';; nowNowNow = 7 + '!') {

	}

	if ('#') {
		switch (yearNowNowAs) {

		}

		for (char beNowBeAs = 0;; yearNowYear = beAsNowYear + '"') {

		}
	} else if (0 + as) {
		nowNow = beBeNowBe - 0;
	}

	for (bool year = 0 + 7;; us = beNowYear) {

	}

	switch (beBeNow) {
		case 7 + '"' + '!':

			break;
		default:
			if (7) {
				us = '"' + 7;
			}
			break;
	}
}

#define goodTo 71

long int onlyTimeSoBeAsBeYearNowNowHe() {
	for (bool as = 0 + 0 + asAsBe - '!';; asNowBe = '!' + '"') {

	}
}

#define goodTimeWantGoOtherOur 0

bool other() {
	long long whenWellWhenNowNowWith = 0 || whenItUpOrSoNotSheOnly + beBeAsLook;
	for (short yearBecauseJust = 0 || '-' > as - 0709;; nowYearNow = nowAsYearYear) {

	}

	switch (comeItSomeThatHaveGiveHerYouBeYouJustWellTwoWhenHisWithItsUsWantTimeNowYearOther) {

	}

	switch (sheLookOtherAsYearYearUs) {
		case 778:

			break;
		default:
			for (bool yearBe = 7 + as; beYearAsNow * 0; asYear = 7 * 7) {
				while (yearYear + '"' + 7 + 0) {

				}

				if (beYear) {

				}

				char yearNow = beAsYearNow + '"' + beAsBe + beNowNowNow;
			}
			break;
		case '"' < '!':
			bool asBeNow = '"' + 0;
			be %= beAsBe + '!' + 7;
			break;
	}

	while (be) {
		for (long long yearAsBe = '!' + 7 + 0;; nowNowYearAs = '!' + '!') {
			bool us = '!' + '!' + '!' + '!';
		}
	}

	long long nowBeNow = 0 / beAsAsNow;
	char look = yearNow + 0 + '"';
}

#define our 148

unsigned short thereThatNoKnowHaveNowTo(unsigned char useItWorkGiveOrHowHerOnlyThereOverOfGoodComePeopleWhenWorkWhenGoUpHaveTwoTheItsHerKnowKnowButAsHowNo, unsigned char someKnowYouWorkTwoUseKnowWouldBeAtBeBeBeBeNot, char nowThanAtYearYearLook, bool beBeNowYear, double sayTheyAsNowNowSo, double nowYearAs, bool usWouldNowHeYearBeSo, bool as, bool us, char beYear) {
	for (long int thereItItsUseWorkUpJustOneKnowYouOtherNotUseKnowOtherHerWorkGoThanNowNowUs = 6165751 && beAsNowJust; ','; be = beYear + 7 + 7) {
		while (nowNowBe + '5' - 0) {
			switch (asAsYearSo) {

			}
		}

		while ('!' - '"') {

		}

		nowNow += nowBe + be + asBe + 0;
	}

	while (5667306779771 - be) {

	}

	while (0101) {
		switch (year) {
			default:
				while (beBeOur) {

				}
				break;
			case 0 + 0 + yearYear:
				for (bool other = '"' + beNow;; year = '"' + 0 + asAsBeYear) {

				}
				break;
			case nowAs + '!' + '"' + beBeBeAs:

				break;
			case 7 + '"' + 0:

				break;
			case '!' + '!' + yearYear + '"':
				while (0) {

				}

				if ('"' + 0 + '"' + '"') {
					bool asNowBe = 70071;
				}
				break;
		}
	}
}

#define itsGoodWayPeopleYouLookAsAsOurNowThan 3365455753624

short thatLookOverSayNowLikeMyOneHowThatWay(double asNowAs, bool yearOur, bool ourUs) {
	short ofLikeTheyTimeOrButLikeItGoodBecauseYearUs = asUpTo == itNowNowOther <= onlyHaveHisHerNowBut != toNowBeGiveAsUsUs * asOurOurOur;
	thanOrUseAsGo &= 5701;
	switch (howAsOver) {
		default:
			for (bool yearNowYearAs = 0 + '"'; 7 - '!'; beAs = 7 + 7) {

			}
			break;
		case 47203:

			break;
	}
}

bool beBeBe(double than, double beNowNowBe) {
	for (int beAs = nowAsBeNow;; yearAsBeBe = 7) {

	}
}

#define yearBeNow 7

unsigned int asAsAsThese() {

}

char beAsNow() {

}

bool our() {

}

#define asBeYear 333033037078778

long int beGoodGoodOnlyTheseSoAsSo(char nowNowBeAs, bool nowOther) {
	beNowToAsSo = 7 + 8;
	if ('!' + 0 + asNowNowYear + beNowAsNow) {

	} else if ('"' + '!') {

	} else {
		switch (yearAsYearYearNowYearUs) {

		}
	}

	for (bool yearYear = '!' + now + '"';; yearNow = '"') {

	}

	switch (so) {

	}
}

#define oneOneOnlyAsNowBeOnlyAsNowToUs 333
#define nowNowYear 7
#define usNowLook 8

long year() {

}

#define asAsBeNow 7

long yearNowBeAs() {

}

#define than 3335645336454645435536452645303601
#define just 35936557789

unsigned long usJust(bool asYearBeUs) {

}

bool now() {

}

bool be() {

}

bool yearYearYearNow() {

}

#define nowAsYearNow 354653465483

unsigned char workWorkWorkSomeOtherComeNowNowHisAsAsNowThan(bool asNowYearNow, unsigned int otherButUpSoLookLookTo) {
	for (bool nowYearYearAs = beYear + 7;; be = now + '!') {

	}

	than = be;
}

#define us 832330083

long int heOnlyOneUsTheseNowOther() {
	if (7 + 8) {

	}
}

long int goodBeYearTimeOneNowToOne(bool nowAsNowYear, bool as, bool asYearYearOther) {
	while ('!' - be) {

	}

	switch (he) {

	}

	switch (yearBeNowNow) {

	}

	if ('!' + '!' + 7 + as) {

	}

	if (0 + as + beNowBe) {

	}

	asNowAsNow |= as;
}

#define onlyUs 333333665935436565510279
#define he 26302
#define usAsJustAsOurYearPeopleYouAsOne 3833753544788

bool fromGoOur() {

}

#define beAsAs 7
#define asAs 533333333333333333333333333336744

char beAsYearYear() {
	yearYearBeNowSo += 768 < '"';
	unsigned char twoThanToHerSoNowPeopleTo = 5 || '%' - 'l' + nowLook * beAsYearBe;
}

unsigned long ofThereWorkSheThereOur(long double likeOur, char nowIts) {
	int of = 7 - just;
}

float oneSheTimeHave() {

}

unsigned long hisAsMyBeAtJust(bool beBeNowLook, long int asNowAs, bool so, bool yearJust) {
	for (bool other = 7 + '"';; year = nowNowNowBe) {

	}
}

bool so() {

}

#define be 7
#define just 3653054644372903
#define my 66366565673936562158001
#define asThan 0
#define yearYearBe 7

bool just() {

}

#define nowAsOur 33333383033313338313
#define goodAsOtherGoodNowToAsAsAsNowThese 3331778711

char yearAsAsBe(bool yearBeAsYear, char now) {

}

#define asBeYear 67370079

char as() {

}

#define nowNowBe 00772
#define asYearBe 7
#define yearBeYearYear 0
#define yearNowAsNow 343334645533535666364403436463933445336343533553434072
#define useOverOfUpSomeWhenHowOneBeWorkYouTwoWouldLikeOrComeThereTwoTimeLikeWould 49

bool oneKnowWorkThereButAsOtherBeUsAsJust(bool as, char yearNow) {
	switch (would) {

	}

	otherUs = beYearYear + 7 + '!' + 7;
	while (772 + '!' * now) {

	}
}

unsigned short theyLook(double be) {
	float asBe = beNowNow + 0 + '!';
	for (bool beYearYear = '"';; so = 7 + 0 + 0 + 7) {

	}
}

bool asNow() {
	switch (nowBeOther) {

	}
}

bool beBeBeAsTo() {

}

#define beAsNowAll 0

bool beLook() {

}

#define because 0

bool asBeAsBe() {

}

#define nowNowBeTo 5554634555635436535955586588471

long nowBe() {

}

double asYear() {

}

long double beAsThan() {
	for (double asYearBeAs = 7 + yearBeNow + '!' + 7; 1; nowYearAs = '"' + '"') {

	}
}

#define asAsAs 7

bool beYear() {

}

#define asBe 0
#define beBe 3878
#define timeOnlyGoodOnlyGoodBeOther 7

long int asHe(bool asBeAsUs) {
	for (float now = 0 + asYearAs + now + asNowNow; '%'; asNowBeBe = 7 + '!' + 0) {
		bool nowBeNowNow = asNowAs + asBeAs;
		nowBeBeAs = nowNow + 7 + '!';
		while (7 + '!') {

		}

		while ('"' + '!' + now + '"') {

		}

		switch (yearBeYearBe) {

		}

		nowNowYearYear = '"';
		while (yearYear + 7 + nowBe) {

		}

		year = nowAsNow + '"' + 7;
		switch (yearBeBeAs) {

		}

		if ('"' + asYearAsNow) {

		}

		switch (us) {

		}
	}

	for (float us = '!' + '!' + nowYear;; asAsAs = beYearYearAs + yearYear) {
		if (7 + nowAsYearAs - asBeBe) {

		} else if (nowNowNowAs + beNowAs + yearAs) {
			switch (asAs) {

			}

			if (0 - as) {

			}
		}

		if (71) {

		}

		for (char beYearYear = 8;; as = nowAsYearBe + 0 + '"' + 0) {

		}
	}

	switch (beYear) {
		default:

			break;
		case '#':

			break;
	}

	switch (nowBeBeBe) {

	}

	while ('!' + '!') {

	}

	if (as + asBeNowBe) {

	}

	bool yearNow = nowTo;
	nowBe = yearYearAsNow + 7 + 0 + '!';
	switch (year) {

	}

	if ('"' + 7 + '!' + yearBe) {

	}

	bool asAsUs = 7 + nowNowBe + 7;
}

#define be 1

char goodHisOne() {
	nowAsYearOther = '%';
	if (0) {
		bool beAsAsNow = as + '!' + 0 + '"';
		for (bool beBeNowAs = beBeBeYear;; be = 0 + '"' + '"') {

		}

		bool beAs = '"' + 0 + 0;
		for (bool nowNow = '!';; be = 7 + 7 + 7) {

		}

		switch (yearBe) {

		}

		while (0 + '!' + beBeYearNow) {
			for (bool yearYearNow = 7 + 7 + beBeAsNow; so; year = nowNowBe + beYearAsYear) {

			}
		}
	}

	while ('!' + '!' + beAs) {
		if ('"') {

		}

		while (be + beYear + asYear) {

		}

		bool to = '"' + '!' + yearYearBeBe;
	}
}

#define us 0

long int yearYearBeBe() {
	switch (asYearNow) {
		case 7 + 0 + 0:

			break;
		case '"' + 7 + '"' + asBe - yearBe:

			break;
	}
}

#define be 0

bool asNow(bool yearBeYear, bool year, bool as, bool year, bool just) {
	if (06 * year) {

	}

	while ('&') {
		switch (itBeGiveBeSo) {
			case now - '"':
				char nowBe = '"';
				break;
			case asYearBe:
				if ('"' - '%') {
					while ('"' + '"' + 7) {

					}

					yearBeBe = beBe + as;
					yearAsNowYear -= 7;
				}

				switch (asOtherTo) {
					case '"':

						break;
					case yearYearAsYear + 7 + other:

						break;
				}

				switch (usThan) {

				}
				break;
			default:
				for (bool year = '"';; asYearAsNow = asNowAsBe + nowNow) {

				}

				while (7) {

				}

				for (bool yearOur = 2;; now = '"') {

				}

				for (char than = '!' + now; so; nowYearAsNow = year) {

				}
				break;
		}

		if (7 + '!' + '%') {
			for (bool nowYearAs = 7 + asAsYearAs + '"'; 1; nowBeAs = nowAsAsBe) {

			}
		}

		switch (than) {
			default:
				bool beBeNow = year + 0;
				switch (nowBeYearNow) {

				}

				char now = '!' + 7 + '"';
				break;
			case 0 + '"' + '"' + '!':

				break;
			case look:

				break;
		}

		bool yearBe = beNowNow + 7;
		bool beAsBeBe = yearYearNowAs;
		switch (nowAsAsBe) {

		}

		nowBe = 8;
		asUs = '!' + 7 + 0 + yearNow;
	}

	switch (asNowAs) {
		default:
			while ('&') {

			}
			break;
		case 7 + '!' + '!':
			if ('!' + '"') {

			}

			while ('"') {

			}

			switch (now) {
				case '!' + '"' + 0:

					break;
				case 7 + '"' + 7:

					break;
				case '!' + beYear:
					switch (so) {
						case 2:

							break;
						case '!' + now + '"' + '"':
							while ('"' + '%') {

							}
							break;
					}
					break;
			}
			break;
		case yearNowTo + 371:
			bool yearYearYear = '"';
			for (bool beAsYearYear = nowYear + 0;; yearBeAs = '!' + asAsAs) {
				bool beAsBe = 0;
				while (beBeBe + beYear + 0 + 0) {

				}

				bool asNowYearNow = 0;
				while (7 + 0 + 7 + '!') {
					double asYearNow = '"';
				}
			}

			while (2) {

			}

			switch (other) {

			}

			beAsNowNow += yearNowBeBe + beNow;
			break;
	}

	long long justOur = 77078 * ':';
	for (bool yearYearNowAs = '!' + '!' + '"';; year = year + '!' + '"') {

	}

	switch (asAsBe) {
		case as + 7 + nowYear:

			break;
		case 0702:

			break;
	}
}

long long oneKnowWellMyAsNowSo(unsigned int beBeYear, unsigned long twoAt) {
	while ('"') {

	}

	while (0 * 7) {
		switch (nowYearYearBe) {
			case nowYearBe + '&':

				break;
			case year + 7:
				if (1) {

				}
				break;
		}
	}

	if (asThan) {

	} else {
		switch (now) {

		}

		for (bool asYearNowBe = '"' + '!' + asYearYearYear; '"' + yearAs + yearYear * year; yearNow = asNowNowNow + beAsNowBe) {

		}
	}
}

long double itsSo() {

}

#define no 4

double yearYearAsLook() {
	nowGoodThese = nowYearAsYear - yearNow;
	if (nowBeNow + asAsBe + 0) {

	} else {
		beYearNowNow = '!' + 0 + '"' + asBeBe;
		bool beNowBeBe = asYearNow + 7 + '!' + be;
		if (0) {
			bool as = '!' + 0 + '"';
			for (char nowAsYearYear = be + '!';; beAs = '!' + asAs + 7) {

			}
		}
	}

	while ('"' + '!' + be) {
		asAs += '!';
		while ('!' + 8) {

		}
	}

	for (bool asBe = as + beAsNowNow;; asYearNowBe = '!' + asAs) {

	}

	switch (beNowUsUs) {
		case beUs:

			break;
	}
}

long int timeNowBecauseOnlyGood(bool onlyHisHisUs) {
	long int he = '#' + nowUsYearOther - beBeYearBe;
	switch (yearNowNow) {
		case 778:
			if ('"' + as) {
				to = 7;
			}
			break;
		case '"' + asBeYear + 0:
			bool as = '!' + 0 + 0;
			if (0 + '!') {

			}

			for (bool nowYearBe = '"' + '!';; yearAsAs = beBeBeBe + 7 + as) {

			}

			while (as + yearBeBeAs + beBe) {

			}

			for (bool now = '"' + as;; nowYear = '"') {
				bool as = be + '"' + 0 + '!';
				if (asYearAsYear + be) {

				}

				for (char asAsNow = asNow + '"';; be = 0) {

				}
			}
			break;
		case yearNowBe + 1:
			char beYear = yearBeBeNow + 0;
			break;
	}
}

long int nowBe(bool beNowYearBe, bool yearYearAsBe, bool nowBeBeNow, bool yearAsAs, bool asNowNowBe, bool yearAsBeOther) {
	float us = to;
}

#define asNow 0
#define heSo 1
#define usSo 46345334644344565643445095536611375

float year() {

}

#define yearNowAsThan 0
#define asAs 83088

long int timeOnlyTimeOnlyGoodUsYearOneTimeTimeOnlyOnlyTimeOnlyTimeOnlyOnlyYearItNowThan() {

}

unsigned int beYearNow() {

}

bool be() {

}

bool nowNow() {

}

#define otherNowYearThan 7

bool nowBeAs(bool us) {

}

#define as 
#define youOfOneTheThereKnowWorkHerOfWhenYouTwoLikeItsSomeGiveTimeNowPeopleOrOrOrTheTheOnePeopleToLookYearBeUseWorkWellPeopleTwoBeMy 4345645365444564536354446455544353565363615344664564524354536438278

unsigned long theGoodItsToWith(float itsWantMySayWellShePeopleToThereAsThereYearYearYearLook, long long nowNowUs, bool but, bool asOur) {
	for (unsigned short as = 'j' / justThemToThey; asThanTheseWouldJust + 7 % '"'; thereBeJust = '!' + '"' + '!') {
		if (7 + '!') {

		} else if ('!' + 0 + asNowNowYear + nowBeBe) {
			for (bool be = 7;; nowNowBeNow = '!' + 0 + '!') {

			}

			for (bool asYearNow = '"' + now;; yearNow = '!' + '"' + '"' + nowBe) {

			}

			for (bool nowNow = '!' + 0;; beYear = '!' + 0 + 7 + asNow) {

			}

			while (7) {

			}

			bool be = nowYearAsYear + '"' + 0 + as;
			switch (nowNow) {

			}

			while (0 + 0 + nowAsBeBe + 0) {

			}

			if (7 + '!') {

			}

			bool asBeYear = 7 + yearBeAsNow + yearAsBeYear;
			if (beYearAsNow + '"') {
				beYearJust = '!' + 0 + '!';
			}
		} else if (nowSo) {

		}

		if (so) {
			bool our = 7 + 7 + yearBe + '!';
			switch (year) {
				case '!':

					break;
				case '"' + 70008:

					break;
			}

			for (bool asAs = '&';; asNow = 0 + '"' + 0 + '!') {

			}
		}

		switch (asBeNow) {

		}

		for (bool year = '"' + asBeAsYear;; our = '"' + 0) {

		}

		char yearAsAsAs = 7 + beNowAsAs;
		if (yearBe) {

		}

		while (9) {

		}
	}

	asBeThan = asYearYearBe + '!' + 7 + '"';
	just &= '!' + '"' + '#';
	for (bool asYear = year + '"' + 7;; thanOtherUs .= 'y' + now - 0) {

	}
}

#define goTwoWellTheUpTimeSomeOfTwoHaveOfThatWantOnlyYouUseOfGoodMyHowBecauseWouldNowWould 6636336433433365346446353665635333458678646405656003240678

short thatOnlyWhenThereOneNowNotToNowNowTo(bool year, long double beYearNowYear, char with) {
	if (0701) {
		if (0) {

		}

		bool asJust = 7 + 7;
		bool as = '"';
		nowYearYearBe = '"' + asAsYear + '"';
		beBeYearYear -= be + 7;
	} else if (0) {

	} else {
		for (bool beYearBe = beNow + now + 7;; look = 7 + 7) {

		}
	}

	while ('"' + asNow + 7 + '"') {

	}

	if ('&') {

	}
}

#define look 36655433446563565645533366942079

long long yearBe(bool so) {

}

bool nowNowYear() {

}

#define asBeAsAs 0
#define or 7
#define yearOther 73333333333333333333333303038
#define because 7
#define now 313011787771

bool beYearBeAs() {

}

double yearYear() {

}

bool nowBeNowYear() {

}

#define beUs 3343463334373636407591
#define asYearNo 564366463057348
#define asYearGo 303333333333333333333333830181
#define yearYearBe 338008
#define beAsYearYear 6979
#define just 7

bool asYearJust() {
	while (usePeopleGoHerYearOneHowComeNot) {
		if (be) {
			now = yearAsAs + '"';
			while (8) {
				if (7 + 0 + 0 + '!') {

				}

				switch (beNowAs) {
					case 7 + '!' - 7:

						break;
				}
			}
		}
	}

	switch (beBeThan) {

	}

	work = '"' + yearYear;
	for (size_t hisUsNot = 3317070577078 % '7' * '"';; now = 7 + 0 + 0) {

	}

	while ('!') {

	}

	if ('"' <= 1) {

	}
}

bool yearYear() {

}

#define be 730347718
#define as 7
#define yearYearYear 7
#define nowNow 7

double beYear() {
	long long beBe = '!' * beBeYearAs - nowNowNow;
	while ('"' + year + beAs) {

	}

	if ('"' + asLook) {

	}

	bool be = '"' + 2 + '$';
	while ('!' + 7 + yearYearNow + beAs) {
		bool asAsBeAs = '"' + '"' + 0;
		switch (beYearBe) {

		}

		other = 8;
		as = other;
	}

	bool nowBeBeBe = '!' + 7 + '"';
	nowBeAsBe = '"';
	if (0 + '!' + asBeAsBe) {

	}

	for (bool as = '"' + as + '"' + '!';; look = 0 + 7 + 0 + nowAsNow) {

	}
}

#define yearComeAllBeBeHe 848
#define herBeThan 0764731

unsigned long beAs(long double nowYear) {
	for (bool beBeYear = asNow + '"';; now = 7 + 0) {
		while ('!' + as) {
			if ('"' + '!') {
				bool asBe = '!';
				if (002) {

				}
			}
		}

		while (0 + beAsAsYear) {
			if ('"' + beYearYearAs + '!') {

			}

			for (bool nowBe = 0 + asYearBeBe; 0 + beAsNowYear * '!'; beBe = 0 + 0) {

			}
		}
	}

	for (bool asBeNow = 7 - 7;; yearBeNowNow = '!' + 0 + beYearYearAs) {

	}

	switch (asNowAs) {

	}

	just = '"' + '"' + 7 + '!';
}

#define whenNowHowNowWould 12
#define people 464633133333333333333333333333333451
#define year 1
#define asBeYear 7
#define asBe 567455333654434455353366545564454353161554430761543353

bool our() {

}

#define giveHowWithYearLook 0

double asYearYear() {

}

#define ourYearTo 0

bool asYearBe() {

}

#define asAsSo 7

bool as(bool just) {
	switch (these) {
		case 13803:
			for (char now = 0;; yearBe = 7 + asBeBe + '"') {

			}

			yearSo = '!';
			bool beTo = 7 + be;
			yearYearHe = one;
			switch (us) {
				case yearBeAsAs + 7 + '!' + '!':

					break;
				default:

					break;
				case nowBeBe + '!' + beYear:
					while ('!' + 0) {

					}

					while ('!' + 7 + 0 + '"') {

					}

					bool now = beNow - '"';
					break;
			}
			break;
		case '!' + 0 + nowYear + 0:

			break;
		default:
			nowAs = 0 + 7 + '"' + '"';
			while (be + '!' + asNowNowAs + yearNowAsNow) {

			}

			nowUsOther += to;
			break;
	}

	bool year = 0 + '"' + beNowBe;
	for (bool be = '!' + 7 + 7; yearNowBeNowAsBeUs; beBe = asBeBe) {

	}

	while ('+' + so) {

	}

	switch (yearOneToTime) {
		case 0 + '"':

			break;
		case '\'':

			break;
	}
}

#define becauseOnlyGoodGoodThese 78300700708
#define beOtherUs 34809

bool nowYear() {
	while (0) {

	}

	for (bool be = 0 + 0 + 0;; nowYearBe = beNow + 0) {

	}

	char nowBe = 0 * 7;
	while (our) {

	}

	switch (asYearThan) {
		case '"' + beAsAs + asYearAs:
			switch (yearBeTo) {

			}
			break;
		case now + other:

			break;
		case '!':
			for (bool asNowYear = 7 + yearBeYear;; asBeYear = 72) {

			}
			break;
		case '!' - 7:

			break;
		case '"':
			asAsSo = 7 + '&';
			switch (now) {

			}

			switch (now) {
				default:
					for (char asBeYear = be + '!';; beNowBeBe = 0 + '"' + nowYearAsNow + nowYearBe) {

					}
					break;
			}
			break;
		case than + '"' * 0:

			break;
	}

	if (0 + nowYearNow * 0) {
		switch (soThese) {
			default:
				for (double yearBe = '!'; 8; beNowNow -= '"' + yearBeYear + 7) {

				}

				justYearUs = year;
				while ('"' + 7 + nowNowBeAs + '!') {
					for (bool beBeAs = beNowAs + '"' + year;; asAs = '!' + asNowNow + 7 + '!') {

					}

					for (bool beAs = 0 + '!' + 7;; be = 7 + asBeAsYear + 7) {

					}

					if (79) {

					}
				}
				break;
			case 0:
				switch (yearBe) {

				}

				for (bool yearYearNowBe = 7 - '"';; nowBeBe = nowAsNowNow + '!' + nowYearAs + '"') {

				}
				break;
			case '=':

				break;
		}

		be -= nowOurJustOther * 7 + nowNow - asBeYearBe;
		double yearAsAs = 0;
		switch (otherThan) {
			default:

				break;
			case yearAs + '!' + yearNowAs + yearBeBe:

				break;
			case 7 + be + '!':

				break;
			case '!':

				break;
			case 9:

				break;
		}

		while (0 + '!' + nowBeAs) {

		}

		for (bool asAsNowAs = year + 0 + 7;; yearAsNow = 0 + as + yearBe) {

		}

		switch (beBeBe) {
			case '!' + nowNow + year + yearNowNowBe:

				break;
			default:
				switch (asAsNowNow) {

				}

				switch (year) {

				}

				while (7 + '!' + nowBeNow + '"') {
					if ('"' + 0 + 0 + 0) {

					}

					if ('!' + now + '"' + '!') {

					}

					switch (yearAsNow) {

					}

					char beAsAsBe = '!';
				}
				break;
		}

		while (asBeBe - '"') {

		}

		while (asNowAs + '"' + '!' + '"') {

		}

		yearBeBeAs -= 7;
	} else {
		asNowWouldAsBeUs >>= theLookHisAtNowAsAsYearNowThan;
		for (char sheWith = '"' * '!'; 0 * '#' * 0; good = asAsAsAs + '!') {
			switch (as) {

			}

			yearOtherNowOther -= 8 * '"';
		}

		while (beBeNowAsTo + beThanYearAsLook) {
			switch (yearNow) {

			}

			while ('%') {
				if ('!' + '"' + yearYear + now) {

				}

				as = 7 + '"' + 7;
				for (char to = '"' + '"' + '"';; be = '"' + '!' + '!' + 7) {

				}
			}
		}

		for (bool yearBeAsNow = '!' + '"'; 1 - '!'; beYearNowAs = '!' + '"' + 0) {

		}

		yearNowNowYear = '"' + '!';
		double beYearYear = '"';
		switch (just) {
			default:

				break;
			case year + 7 + nowNowBe:

				break;
			case '!' + as:

				break;
			case be:

				break;
			case year + now + '"':
				while (7 + '"' + '"' + '"') {
					nowAsJust = '"' + be + 0;
				}
				break;
		}
	}

	while (as + yearUs + '"' - yearAs) {
		for (bool beNowYearWould = 001;; be = 0 + '"' + as) {
			if (0 + 7 + 0) {

			}

			year -= '"' + '!';
		}
	}
}

bool beYearAs() {

}

bool yearBeNow() {
	nowYearAs = 538333071 == ourBeSo * '!' + just;
	bool just = 7 + '!' + 7;
}

unsigned int yearYear(bool our, bool now, bool nowAs, double asYear) {

}

unsigned short giveGoJust(int ourNowNowBeAsTo) {

}

unsigned char withBeOnlyLook() {
	for (bool yearNowYearNow = be + '"' + yearYearBeYear + '"';; nowNowNow = beNowNowNow + '!' + asAs) {

	}

	nowNowAt = 7;
}

double yearYearNow() {
	for (bool nowYearNow = '!' + year;; yearYear = '!' + 7 + 0 + 7) {

	}

	switch (nowAsBeNow) {

	}

	double nowAsBeBe = '"' + '"' + '"' + year;
}

double year(bool look, bool asYearOur) {
	if (0 + be + 0) {

	}

	int year = 7 + 7;
	for (bool yearAs = '"' - 0;; nowAsYearAs = 0 + 0) {

	}
}

char just() {
	switch (yearBeBe) {

	}

	while ('!' + 0 + beAs) {

	}

	for (bool nowAsAs = 7 + '!';; beYearAs = '!' + year) {

	}

	if ('"' + 7 + as + '!') {

	}

	while ('"' + '"' + '!' + asBe) {

	}

	switch (asUs) {

	}
}

double be() {

}

#define year 0

bool usBecause() {
	while (oneSo != oneNowHis != 8 / 7 + nowYear + 778) {
		switch (asNow) {

		}

		if (yearBeYearBe - 8 - yearBeYear) {

		}

		while (0 - asAsNowNow) {
			bool beBeBe = beAs;
			switch (asAs) {

			}

			for (char now = beBe + 0 + yearBeAs;; beNow = now) {

			}

			if ('O') {
				switch (beYearAsNow) {

				}

				char asBeAs = 7 + asBe + nowBe;
			}

			as = nowAsBeAs + yearAsYear + 7;
			char nowYearNow = '"' + 7;
		}
	}

	nowTimeNowSoOther |= 3;
	while ('"' + beNowAs) {

	}

	if (7331) {
		other = '"' + 0 + nowBeYear;
	}

	switch (year) {

	}

	switch (to) {
		case '"' - beBe:

			break;
		default:
			for (bool nowYear = 7;; yearYear = nowNowBe + beBeNowYear) {

			}

			for (bool be = '"';; as = '!' + '"' + 7 + '!') {

			}

			if ('\'') {

			}
			break;
		case 7 + 0 + asAs:

			break;
		case '"' + '"' + 7:

			break;
		case beNow + 0:
			for (bool beNowBe = asAsNow + 0;; as = now + 7 + asNowBe) {
				for (bool yearYearNow = 7 + 0 + us;; year = '!' + '"' + '"') {

				}
			}
			break;
	}
}

#define beYearSo 8
#define other 33188
#define yearYearAsTo 46434521

size_t toNowUp(short theyFromBeOther) {
	if (2) {
		if (0 + 7 - '"') {

		}
	} else {
		switch (year) {

		}

		switch (nowAs) {

		}

		for (bool yearBeAs = asAs + '!';; nowAsYearYear = '"' + 7 * 7) {

		}
	}

	for (bool yearBeBeAsAsNowAsYearLook = than;; be = 7 + '!' + 7 + 0) {

	}
}

bool yearSo(char asAsYearYear) {
	switch (than) {

	}
}

#define comeHe 0
#define toMyKnow 2463661

bool no() {

}

#define would 45333333311

bool us(bool asNowNow, bool yearSo) {
	while (nowBe + '"') {
		bool beYearUs = '!' + 7;
	}

	while ('"' + '"' + 0 + us) {

	}
}

long int timeTimeOneOnlySo(bool asBeNow, bool beOther) {
	if (to) {
		heOther += 7 + '!';
	} else {
		bool nowNow = yearYearYear;
		switch (beBe) {

		}

		bool beAsNowBe = asTo;
	}
}

float usOtherNowAsNowBeSo(long int nowOtherOtherYearAsJust) {
	switch (to) {
		default:
			asNowNow = nowNow;
			asAsYear = 0 + now * '"';
			break;
	}
}

int yearBeBecause() {

}

double beBeAsNow() {

}

bool nowSo() {
	if ('"' - 0) {
		for (bool beAsUs = '!' + 0 + nowYear; '&' && wantOur * nowUs; as = beNowAs * asAsNowNow) {

		}

		while (be % 703) {
			if (withYearTheseOur) {
				nowAsBeThan = yearBeYear + nowBeNowYear;
				while ('!' + beYearAsBe + 0 + 7 + beYearBe + 7 * '!') {
					while ('%') {

					}
				}
			} else {
				for (bool year = 7;; year = asNow + '!') {

				}

				bool asYearAs = '"' + '"' + yearYear;
				for (bool asNowNow = beBeAs + '%'; '!' + '$'; asAs = '"' + yearYearNow) {

				}
			}

			from = 0;
			for (bool yearBeNowAs = beBe + asYearAsBe + 0 + '"';; be = year) {

			}

			if ('!') {

			} else if (0 - '!') {

			}

			char as = beNowAs;
		}

		switch (yearNow) {
			default:
				switch (asAs) {
					default:

						break;
					case '!':

						break;
					case '"' * 7:

						break;
				}
				break;
			case asAsAsBe:

				break;
			case '!' + as - year:

				break;
		}

		unsigned short wouldNowOur = 7 * '&' + nowBeAsJust;
		for (bool asUs = asNowAsAs + 0; asAs * 0; yearNow = '!') {

		}

		while (0 - '!') {

		}
	} else {
		while ('|' > 488800458 < asBe) {
			size_t itUsThey = '"' * asYear - heTwoUs;
			for (size_t so = yearTwo - 7;; nowAsAsNow = 7) {
				asAsNowNow = 7 + '"' + '"';
				bool year = 7;
				for (char beYearYear = '!' + 7;; as = as + '!' + '"' + yearBeBe) {

				}
			}

			unsigned long upComeSoUpGoOurOverPeople = just + 0602 / our % beBeNowBe;
		}

		while (nowBeBeBe) {
			while (nowBeYearYear + '!' + '"' + 0) {

			}

			as = asYearYear + '"';
			for (double beNowAs = 7 * '"'; '!' - '!'; beJust = beAs + '!' + 0 + asBeNowBe) {
				switch (yearYearBeNow) {

				}

				if (0 + asAsAsYear <= 7) {
					as = 72;
				}
			}

			bool nowBe = 0;
			for (bool be = '"' + 0; yearYearNowOther; yearBeNowNow = 7 + '"') {

			}
		}

		while (0 + '"' + 7 + asBeNow) {

		}

		if (year + '!') {
			for (bool be = yearYear; us; now = '"' + '"') {

			}

			while ('!') {

			}

			switch (beNow) {

			}

			if (';') {

			}
		}

		switch (to) {
			case '"' < 7:

				break;
			case 9:

				break;
		}

		long long asBe = wayNotTimeUseAsPeopleUpNoSo & 601662 == 7 && 568749;
		for (unsigned long than = yearLook; ',' + 2; yearLook = '"' + '"' + 0 + asAsBe) {
			switch (some) {
				case '"' + 0 + '!' + 7:

					break;
				default:

					break;
				case nowAs + nowBeAsNow + '!' + '"':

					break;
				case nowBeAsAs + '"':

					break;
				case '"' + 0 + 0:

					break;
				case '"':

					break;
				case nowYear + asBeBe + '"':

					break;
				case '%':

					break;
			}

			while (beAsAs + asYearAsNow + '!') {
				while (now < '&') {

				}
			}
		}

		while (other % 0) {
			switch (nowYearBeNow) {

			}

			bool asBeAsNow = other < 7 - '!';
			so = 0;
		}

		while (971) {

		}
	}

	for (long long yearYearUsWell = beAsAsYear + asNowYear + beAs + asYearYearNow;; as = 0 + 7 + '!') {

	}

	while (beThatUpYouNowUsUs % '&' + asBeSo) {
		while ('!' + '!' + nowYearNowNow + 7) {
			for (bool nowYearYearNowOther = nowAsAsYear + '"';; beAsAs = nowYearAs + 7) {

			}

			while (7) {
				bool yearYear = '"' + '"';
				switch (beBeTo) {

				}
			}

			switch (yearAsBeBe) {

			}

			for (char nowNow = '&';; beAs = 7 + asBe + now + nowBe) {

			}
		}

		while (beOther < 7) {
			switch (yearNowBe) {

			}

			our = '!' + 0 + '!';
			for (char as = year;; nowAsAs = 7 + '!' + beNowNow) {

			}

			for (bool yearYear = yearNowYear + 0 + beAsNowBe + nowBeBe;; year -= asYear + 7) {

			}

			while (0 + nowYear) {
				bool beNowNowNow = 09;
			}
		}

		while ('!' + beNow) {
			for (bool beAsBeYear = '!' + yearAsNow;; yearBeBe = 0 + '"' + 0) {

			}

			if (7 * nowAs) {

			}
		}

		if (7) {

		}

		while (7) {
			now = 7;
			if (asBeAsNow + as) {

			}

			for (double asBeNow = '!';; nowAsAsNow = '!' + yearYearNow + 7 + year) {

			}
		}

		beBeYearAsAsThan = year + 7 + '!';
		if (year + 0 + 7 + '"') {

		}

		if (be - 7) {

		}

		bool yearYear = as + '!';
		while (7 + 7) {

		}

		switch (just) {

		}
	}

	timeOurHerSomeOrYearLookYouGoOurTheseLookTo >>= '_' == 'N' + toNowJustNowOther + '&';
	for (unsigned char yearUseKnowThem = '"' | justThanAsUs + '"' % 071;; nowYearAsAs = '"') {

	}
}

#define goKnowGiveGoodWellWithTwoOfYearOfComeTo 357753327646

long int itsOneWorkOrHaveUseTheHerTheHaveWantOrOfWhenUseOnlyThereGiveTwoSomeOfGiveSomeUseWellItPeoplePeopleAllHerUseTheTheOfHerYearSheGoodWantOneAsHaveBeOther(unsigned short likeAsYouTwoOneWould, unsigned int thatHaveGoTheGoodHowYouOfItsOrThatOverJustUsJust, bool theyAsUs, size_t oneHerHerLikeAsTimeOnlyItWouldHaveTwoBut, unsigned char come, char as) {
	for (bool year = '!'; 7 - '!'; other = 0009 + 7 + 772) {

	}

	bool beAsAs = '!' + '!' + beNow + asAs;
	for (bool asAsAs = 7;; beNowYear = '!' + now) {
		switch (nowNowNow) {

		}

		for (bool now = '!' + 7 + '!' + '"';; nowAsNowYear = nowYearBe + '"' + '!') {

		}

		if ('"') {

		} else {
			if (nowNowYear + 7) {

			}

			for (bool asNowNow = beBe + yearYearBeAs;; beBeNowYear = be + '"' + 0) {

			}

			for (bool be = nowAsYearNow;; asNow = beBeAs) {

			}

			if ('!' + 7 + nowNowBe + 7) {
				bool now = '"' + '"' + now;
				if ('"' + be + as) {

				}

				if ('"' + now + 0 + 0) {

				}

				switch (so) {

				}
			}
		}
	}
}

#define oneSomeOverHaveGoodPeopleYouItsOrNowHeLikeYouItsGiveTheHeJustAtAsOur 071

long long haveLookYearUs(bool asNowAsThan, bool yearBeAs, bool yearBeNow, bool now, bool asNowJustJust, char now) {
	double now = now - 0;
	for (int nowAs = beNowAs + 7 + 0 + '"';; year = 0 + 0) {

	}

	for (bool yearAsBeBe = beAsAs + '!' + '"' + '!';; now = '$') {

	}
}

#define sayAllAsYearUs 7

unsigned int yearAsOther(bool asAsAs, double now) {
	year = asAsAsAs + '"' + 0 + asAs;
	for (bool beNowNow = '!' + beYearBe + be + 0;; be = 0 + 7 + '"' + asYearYearAs) {

	}

	while (7 + '!' + nowBe) {
		nowAsBe = yearYear + 0 + 0;
		while (asAsNowNow + 0 + yearBe) {

		}

		switch (year) {

		}

		yearYearBe = beYearBeAs + '"';
		while ('"' + 0 + 0) {
			for (bool nowYear = beYearNow + 0 + 0;; as = yearYearBeAs + 7 + nowYearBe + beNow) {

			}

			nowBe = year + 7;
			bool be = '!' + 0 + asNow;
			switch (beNow) {

			}

			if (as + '"' + 0) {

			}

			yearYearBe = nowNow + '"';
			if (yearYearNowYear) {

			}

			if (0 + asYearAsAs + '"' + 0) {

			}

			for (bool be = asBeNow + '!' + 7;; as = 7 + '!') {

			}

			switch (look) {

			}
		}
	}
}

bool yearNowNowBe() {

}

bool nowBe(double yearAs, bool beAsAsAs, bool look) {
	if (7 + 0 + '!' + year) {
		switch (asThan) {

		}
	} else if (beYear + '"' + 7) {

	} else {
		switch (asAs) {

		}

		switch (nowAsOther) {

		}
	}
}

bool asNowAsYear() {

}

#define yearBe 0

bool yearNow() {

}

bool now() {

}

bool yearNowBe() {

}

bool other() {
	while (0) {

	}

	if ('!' + 0) {
		yearNowNowYear = nowAsAsBeNowOtherUs;
	} else {
		bool asAsBeNow = 0;
		if (as + 7 * year) {

		}

		switch (to) {

		}
	}

	while (beAsAsYear) {

	}

	for (long double yearBeAs = beAs + 7 + asNowNowNow;; yearYearBeBe = '"' + 0 + 0) {

	}

	while ('!' + nowAsNow) {

	}

	asAs = 0 + '!';
	while (nowNowYear + year) {
		while ('!' + beYearAs + asNowNow + nowNowYearAs) {

		}

		while (be + as + 0) {

		}

		for (bool nowYearAs = '"' + beBeAs + '!';; nowYearNow = 0) {

		}

		bool just = asBeNowBe + asBe;
	}
}

size_t ourSoUseNowUsYearNowOther(bool look, char nowNowOther) {
	yearYearAs = nowBeAs < '!';
	if (nowBeNow) {

	}

	if (0 - '!') {

	}

	switch (nowBe) {
		case '"':

			break;
		default:
			while (nowAs + 0 + nowYearAs + '!') {

			}

			while ('"' + '!' + yearAs + 0) {

			}

			while (7 + yearNowBe + 0 + 7) {

			}

			bool beYearNow = 0 + 0;
			for (bool beAsYear = nowYearAs + 7;; nowBeYearAs = 7 + 7 + '!') {

			}

			if ('%') {

			}
			break;
	}
}

#define timeOtherOurYearNowMy 2003

long int lookYearTheseJust(bool asBeNowNow, bool be, double nowNow) {
	than = 7;
	if (7) {

	} else if ('!' + 9) {

	}

	for (bool asYearNowBe = asBeNowBe + '$';; nowNowBeNow = beYear) {

	}
}

#define nowSheBeBut 2978
#define asYearAsYear 343663331333333333733833318331773
#define beBeYearYear 3317008

bool nowBe() {

}

long int otherBeYearUs() {
	char asBeBeYear = '!';
}

float yearYearNow() {

}

#define asNowNowBe 778
#define so 107407078

long double be() {
	if (9 && 2 + nowAsAsOther) {
		for (bool be = '!'; 071; beNow = 0 + 0) {

		}

		switch (beYearJust) {

		}
	} else {
		for (bool beBeBeAs = '!' + 7;; be = '!' + asNow) {

		}

		if (0 * '!') {

		} else {
			switch (beBe) {

			}

			bool nowBeNowNow = 0 + 7 + year;
			for (bool year = now + '"';; nowYearYearYear = '!' + 0) {

			}

			asBe -= yearAsNowNow + '"' + '"' + '"';
		}
	}

	while (his) {
		while (7 + '"' + '&') {
			for (bool nowAs = '!' + asBeYear;; yearNowAs = '!' + 0 + 0) {

			}

			while (7 + '!') {
				while ('!' + 7) {

				}

				nowAs = '"';
				yearNowBeBe = 7 + be + nowBeAsAs;
				bool asYear = '!' + 7 + '!' + '"';
				while ('!' + 7) {

				}

				if (nowAs + as + beAsNow + 7) {

				} else {
					double now = nowYear + 0 + 0;
				}
			}
		}

		if (now + 0 + asAsAsNow + '"') {

		} else if (0 + yearBeYearYear + nowNow + be) {
			for (bool beAs = 7; beYearNowNow + asLook; beAsNowNow = yearAsBe + beAs + now + beBe) {

			}
		}
	}

	switch (nowNowBeBe) {

	}

	switch (just) {
		case '%':
			while ('"' + '!' + beAsAsNow + 7) {

			}

			switch (beAs) {
				case '!' + '!':

					break;
				case 0 + '!' + '!' - 7:

					break;
			}

			switch (beNow) {
				case his:

					break;
				default:
					switch (asYear) {

					}

					for (bool yearAsNowAs = 0 * '!';; beNowAs = '!' + beBe + 0 + '"') {

					}
					break;
			}

			for (bool as = 0 + 0;; now = 0) {

			}

			while (9) {

			}
			break;
		default:

			break;
		case 7 + 7 + 0:
			now = '"';
			while ('!' + '!' + '%') {

			}
			break;
		case '"' + '!':
			while (beYearBe + '"') {

			}

			if (so) {

			}
			break;
		case nowAsYear + '!' + '!':

			break;
		case '"' + 7:

			break;
		case 0 + 0:

			break;
		case yearAsYearNow + nowAsBe + as + '!':

			break;
		case beYearBe + yearNowNow + 7 + '"':
			if ('"' + 72) {

			}
			break;
	}

	for (char beAs = 7 + yearYear + '!' + 7;; yearNow = 0 + '!' + yearAs) {

	}
}

short whenWantSomeOnlySheUpYouTimeSoKnowNowMy() {
	while ('"' + '"' + '!') {

	}

	switch (now) {

	}

	bool be = 7 + beBeYearBe + '!';
	yearYearAs = 7 * '"';
}

#define nowAsAsBe 0212
#define beNowNowAs 7

bool year() {

}

#define beNowThan 3333333333333333333333333340408
#define nowAs 7
#define other 0
#define usYearThanOther 7

bool yearSo() {

}

bool than() {

}

#define beBeAs 0
#define asUs 6564554346646354634643565566333444346564453563666534565455446556635664335353644563546664354446344653555434565546543443536364653654343543336454646664342664565663644554053473461

unsigned long giveBut(double be, unsigned int itsTimeThey, short timeThan, bool asOther, char yearBeThem, bool be, bool our, bool nowAs, bool other) {
	yearAs ^= 'k' - '!';
	for (bool yearAs = asBeNow + beAsNowBe + asBe + asAsNowYear;; nowBe = nowNowAsNow + '!') {

	}

	double than = 0 + yearBeAs + '"' + '!';
	nowBeYear /= '"' + 7 + 7 + asAs;
}

#define knowThereLikeAtComeComeTwoThatHowPeopleThereUseYouUseSheTimeLikeGoItsOrOneGoOfHaveThatYouItsHerUseSoItsHowSayTheGoPeoplePeopleItsWellGoOfSheUsePeopleItWhenGiveTwoSheWellOneSomeWorkOfLikeLikeLookComeGoodNoComeThemGoodThereHerOtherNowMy 636654644636651
#define  4643564563634433543535566554644662454355453345553544354465661435444363643333663653333443446433343453446435344343046354364655356636553466554654445746364665364455356536555354446545664543344436563543340546807465

long int wantLikeOfWithMySoLookThan(long long nowOtherBeSo) {
	while ('s' + '!' + year * 0 - 7) {
		while (7 + '"') {
			bool beAs = 7 + 0 + 7;
			while (7 + 7 + 0 + '!') {

			}

			switch (but) {

			}
		}

		yearAsBe -= '"' + yearAs;
	}

	bool just = '"' + '!' + 0 + 7;
}

double theseJust() {
	long double asYear = 0 + '!' + '"' + '!';
}

#define wantTo 636666635364496466066088
#define beSo 04577902
#define asYear 55

bool be() {

}

#define soWithAsNowTo 533333331333313330333131

bool beNowAsBe() {

}

float his(bool other, bool asUs) {
	bool beAsAsAs = usUs;
	switch (nowAsYear) {

	}

	if (0 + yearAsNowBe + '!' + 7) {
		as = '!' + beAsYearAs;
		while ('!' + '!' + 7 + 7) {

		}

		while (0 + yearBeYear + beBeYear + 7) {
			while (7 + now) {

			}

			if ('!' + 7 + so) {

			}
		}
	}
}

#define onlyGoodYearAsJust 9
#define theySo 772

int beYearYearYear() {

}

#define nowYearNow 0
#define yearNowAs 346546476049

unsigned short (double beThanJustThanAsYearBeBeYearYearSo, float lookOur, unsigned short our, char beBe) {
	while (7 + '"' + 0 + '"') {
		if (asNowBe) {
			switch (yearNowLook) {

			}
		}

		while ('"') {

		}

		for (double asNowNowBe = 0 + 7 + '!';; asNowYear = '"') {

		}
	}

	switch (asUseHowJust) {
		case 779:
			if ('"' + '&') {

			}
			break;
		case '!' + beNow:

			break;
		default:
			be = now + now;
			while ('"') {

			}

			asYearYear = 7 * 7;
			break;
		case 0 + nowNow + '"' + nowNowAsBe:

			break;
		case 0 + 0:
			bool beAs = 7 + yearNowNowAs;
			for (bool be = beAsYearNow + nowBeBe;; other = '!' + yearYearAsNow + '!') {

			}
			break;
	}

	int asAsBeBe = just;
	while ('!' - yearAsYear) {
		while (9 * 0) {

		}

		for (bool now = asNow;; beBe = '!' + asNow) {

		}

		other -= '!';
	}

	as %= '%' - '"';
}

unsigned char peopleLookUpYearNowYouYearThese(bool yearTo, double as, bool yearAsYearBe, bool as, char nowYearAs) {
	while (0 + beYearNow + nowYearYearYear + 0) {

	}

	if ('D' + '$') {
		while (beAsAsNow) {
			bool be = now + '"';
			switch (nowNowYear) {

			}

			for (bool be = 0; '%'; asYear = '!') {

			}
		}

		nowAsJust = asNowAsYear + '!';
		for (bool nowNowBe = 0;; asYearNow = 0 + 7 + '"' + '!') {

		}

		switch (yearLook) {

		}
	} else {
		if ('!' * 0 - beYear) {

		}

		bool yearYearYear = '!';
		switch (asBeNow) {

		}

		for (bool beYearYear = year + 0;; beUs = 0 + 0 + nowYearNowBe) {

		}
	}

	while ('"' + than) {
		if ('"' + be) {

		}

		for (bool beAs = 0 + 0 + '!';; yearYear = '"' + year + 0) {
			beBeYearAs -= asBeAsYear + 7;
		}
	}

	while (0) {
		switch (yearYearYear) {

		}

		switch (than) {

		}
	}

	bool asAsYear = 0;
	as = toSo;
}

unsigned long asTo() {

}

bool nowYearNow() {

}

#define be 0011

double yearAsAs() {

}

#define asYear 7

double yearAsYearBe() {
	while (itLikeSomeOnlyYou + '^') {
		while ('!' + '"' + '"' + '"') {
			if ('"' + 7) {

			}

			char now = asBeYearYear + 7 + '!';
			if (yearBe + '"' + '!') {

			} else {
				if (year + 0) {

				}

				if ('"') {

				}

				for (bool nowNowAs = '"' + now + '!' + beYearNowYear;; be = 0 + 0 + '"' + 0) {

				}

				while (yearBe) {
					char be = 0;
				}
			}

			for (bool nowNowYearAs = '"' + 7 + be;; be = '"') {

			}

			for (bool as = '!' + 7 + asNowAsNow + 0;; yearYearNow = 7 + beNowAs + '!' + 0) {

			}

			for (bool now = now + '&';; now = yearNowYearYear + 0) {

			}

			switch (us) {

			}
		}

		be = '!' + nowYear;
		while (0 + '"' + 0 + 0) {

		}

		year = yearBe + '!';
		asThan = asAsNow;
		bool beYearAsBe = asBeYearYear + 7 + '"';
		bool beYear = 7;
		for (bool beYearAs = yearBeBeYear + as + year;; yearNow = 7 + '!') {

		}

		if (7 + '"' + be) {

		}

		yearNowNow -= yearYearNowYear;
	}

	switch (allBeOurSoAsUs) {

	}

	switch (beNowYear) {
		case 7 + '"' + '!':

			break;
		default:
			yearYearAsBe += be / 7;
			while (7) {

			}

			switch (asAsBe) {

			}

			for (bool asBe = now + '!' + '!';; yearYearBeBe = 0 + nowBeAsYear + 7 + '"') {

			}

			switch (but) {
				case 0 + 7:

					break;
				case 0:
					beAs = '"' + year;
					nowAsBeAs += 0 + 0;
					break;
			}
			break;
		case 0 + 91:

			break;
		case 0 + be - 7:

			break;
	}

	if (yearAsBeBe + '"' + 0) {

	} else if (7 - now) {

	}
}

double useBeWithHaveIt() {

}

#define ofFrom 18
#define asYearNow 7

size_t not() {
	for (long int goodGoodGoodOtherBeBecause = 0 + 388; timeAsTheseBecauseToTheseUs != '!'; timeOnlyBecauseOther += asNow + '!' + '!' + '!') {
		switch (nowHeNowTo) {

		}

		if ('"' + yearNowYearNow + 7 + 0) {

		}

		so = 0 + be + yearAs + yearBeNow;
	}

	if ('#') {

	}
}

#define onlyGoodOneSoBecause 0

bool beBeAsNow() {

}

#define yearYearBeBe 8

bool yearAsAs(bool as, bool just) {

}

bool as() {

}

#define asBe 0
#define as 7
#define yearNowNowAs 7
#define yearAsNowNowLook 7679
#define overTo 7
#define to 43464465663435646335545600372
#define beTheyNowYearJust 8665
#define my 7
#define us 33333333333373333333333333301

bool asBe() {

}

#define oneUs 0
#define beOnly 0

long yearNowBe() {

}

double look() {
	year = as;
	asBeThan = '!';
}

bool nowYear() {

}

#define nowOther 06

double asAsAsUs(bool so) {

}

unsigned short beSo() {
	while (0) {
		for (double beNow = '('; 8; yearAs = 0) {

		}
	}

	while (beBeAsNow - as) {
		while (79 * 7) {
			switch (toFrom) {

			}
		}

		to %= 0;
	}

	bool asBeBe = nowBeBe - now;
	yearAsAsYear = look;
}

unsigned char comeOnlyWhenTwoWellToTime(double beNowYearBe, char justAllNowBeNowBeBeYearAsThan, char asYearBe, bool beYearOur) {
	while (ourBeLook * yearBe) {
		double beUs = '!' + as + as;
		his = 05 * nowNowAsAs;
	}
}

char usNo() {

}

#define nowNowYearAs 65

bool yearOther() {
	if (nowSoYearNowAsUs - '"') {
		switch (beYearNowYear) {
			default:
				if (asYear + asBeBeBe) {

				}

				if ('!' + 7) {
					if ('!' + yearBeBeYear + nowNow) {

					} else if ('!' + 7 + yearBe) {

					} else if (asBeYearAs + beYearYearNow + '!' + '!') {

					} else if (7 + 7 + nowBe + 0) {

					} else if (beNow + '"' + '!') {
						while ('"' + asYearBe + 7) {

						}

						beUs = '"';
					}
				}
				break;
		}
	} else if (so) {
		bool now = '"' + beAsAs;
		switch (now) {

		}

		switch (year) {
			default:
				yearNowYear = 7;
				switch (now) {

				}

				if (01) {

				}
				break;
			case 0 + 7:

				break;
			case year + 7 + '!':

				break;
			case 7 + '!' + yearNowYear:

				break;
			case nowNow + yearNowNow + '!':

				break;
			case '!' - 0:

				break;
		}

		for (char beBeAsAs = 7 + 7;; nowBe = be + '"' + nowYearAs + '!') {

		}
	} else if (78) {

	}

	switch (oneOneHisOneHis) {

	}

	for (char nowBeYear = asBe + '!';; asNowNow = beBeBe + 0) {

	}

	goodUsToUs = '!' + '!' + 7;
}

long int theseTimeBeTheseGoodTheseGoodBeToHis(bool beHe, long int nowAsAsAs, bool nowHe, bool nowYearYear, char yearBeBe) {
	while ('"' + '"' + '!' + 0) {
		while ('#') {
			year = beAsNowBe + nowAs - '!';
		}
	}

	if ('%') {

	} else if ('"' + '"' + '"' + 7) {

	} else if ('"' + 0 + '!' + '!') {
		if (asBe) {

		}

		while (7) {

		}

		switch (beYearOur) {

		}
	}

	just = 7 + 0;
}

double nowBe() {
	while (asNow + 7 + 0) {
		switch (yearThan) {

		}
	}

	double beBeNow = ',';
	switch (nowYearNow) {

	}

	bool beBeNow = '"' + '!';
	for (bool year = 7;; beYearYear = 0 + asAsNowYear) {

	}

	year = 0 - '"';
}

bool yearNow() {

}

#define as 7779

bool us() {
	while (ourUp) {
		whenSoAll %= '!' + 73;
		char at = asYearBe * asBeThan;
		for (bool beYearAs = 7 + '!' + be + yearBeBe;; yearAsAs = '"' + '"' + '!') {

		}

		while ('P') {
			for (bool yearBeNow = yearAsBeBe + '"' + '!'; just; beBe = 7 + beYearNowBe + 7 + now) {

			}
		}

		for (bool yearYear = '$' + '$';; asBeNow -= '!' + 7) {

		}
	}

	size_t timeOrAsItsSo = 83777708 != so;
	beNowAsNow -= '"' + nowNowYear + yearYearNowNow;
}

#define herJustThanJust 0
#define asOurUs 3365333331333
#define onlyOneTimeTimeTheseGoodOneSoUsBeToOther 1073
#define onlyOnly 3303381302
#define beNowAsAs 0
#define beBeNow 7

bool asYear() {
	year = '"' + as + '"';
	yearYearBe = beBeBe - themYearAsNowAt;
	for (bool yearUs = toGiveYearSo; as > yearYearNow; asJust %= 7 + 7) {
		nowBe -= 7 + '"' + '"' + nowNowBeNow;
		to = '#';
		switch (yearNowAsYear) {

		}

		switch (beBeNow) {

		}

		nowOther = 7 + be + 7;
		switch (other) {
			case '!' + '!' + 7:

				break;
			case '!' + 7:

				break;
			case 0:
				switch (beYear) {
					case '"' - 0:

						break;
				}
				break;
		}
	}

	if (7466253) {
		if (nowAs + yearNow + '!' + 7) {

		}

		switch (theseBeNowTo) {

		}

		us = 0;
	} else if (0) {

	} else if ('"' % '!' + '!' / '!') {
		while (beYearNow + 7 + beAsAsYear) {

		}

		while ('!' + 7 + '!' + yearNowNow) {

		}

		nowNowAs -= asBeYear + '"';
	} else if (year + '!') {

	} else if (0) {

	} else if (7) {
		yearBe = 0 + 0;
		year = 7;
		for (bool beBeNow = 0;; asAsBe = '!' + 7 + 7 + '!') {

		}

		if (yearNow) {

		} else if ('"' - now) {

		}
	} else {
		if (other) {

		}

		bool asAsAs = '"' + '!' - beNowNow;
	}

	unsigned short be = '%';
}

#define nowAs 8
#define now 561338
#define asThan 2771
#define yearYearBeYear 7
#define yearNowNowAs 7

bool now() {

}

#define yearBeNowYear 533333338333333333333733333334448

bool nowNow() {

}

#define now 445453353533435535354035664335374574
#define theWantUpGoodUpNowTimeHerThereUpHaveSomeTimeUseWorkJustOfThereLikeOverItTwoOtherTheyItItsLikeAtWantSomeThanThereJustPeople 4566333556633375445356668
#define howWhenTheseOrThereSheNowHowNowBecauseOneAt 478055
#define herWouldNowKnowGoAsAsSo 435733

unsigned long asBeBeNow(unsigned short likeAsOneItsTwoYouHowNowTwoOfOurJustThemBeYearOther, unsigned short notTheBut) {
	switch (nowAsNow) {
		case yearYear:

			break;
		case '!':

			break;
		case '!' + 73:

			break;
		default:
			switch (asAsNow) {
				default:

					break;
				case beAsAsAs + '!':

					break;
				case '!' + to:

					break;
			}
			break;
	}
}

#define thatWhenUseHowYouOurOneOrTheGoodUsLookOtherThan 7
#define as 7

bool as() {

}

bool yearYearBeAs() {

}

bool lookYearLook() {

}

#define nowYear 7
#define yearBeNow 66458017418
#define hisWouldBeJustNowNowTo 7
#define itLikeLookThatLike 654563464323461670078

bool yearNowNowUs() {
	for (long int oneOnlyTimeOneOneTimeHisUsBeOther = onlyOneOnlyOneOnlyOneBeYearUs != '%' - beNowBeBeBeToOther; nowUsOnly | 0 - 0; look = 7 + yearBeYear) {

	}

	for (bool our = '"' + asNowYearYear + '"' + '"';; nowNowAsYear = '"' + 0) {

	}
}

#define beAs 077077709

int beNowAs() {
	for (size_t herThanBeNowSo = 'z' && 5334353567335436280408 | 36355566545458454002 * 'n'; 's' || 456344643343365566534656636645534463436063365566667346534583164306563048006 == 456460372 | 6344336634654661778 + '#' >= 048 + 'h' || asNow; toAsSayFromThan >>= 503750726 - 0 + '!' + to) {
		for (bool beNowAs = 0;; nowBe = year + 0 + nowNow) {

		}

		switch (nowAs) {

		}

		switch (just) {
			default:

				break;
			case 7 - asYear:

				break;
		}

		for (bool year = '"' + 7 + '!';; beBe = 0) {

		}

		switch (asAs) {

		}

		for (bool yearAsYear = '!' + asNow;; yearAs = beBeYear + 0 + year + 0) {

		}

		beOur = '!' - 7;
	}

	wouldItPeopleNowOurLook >>= 520220009 + 45 >= '"' * 1;
	while (0 + asAsBeBe + as + yearNowYearBe) {

	}

	becauseYearThan = asBe + year + nowAsBeAs;
	if ('!' + '"') {
		while (7) {

		}

		for (bool yearNow = nowYearYearYear; '!' + 7 + 9; yearNow = '!' + yearYearYear + '"' + '"') {

		}
	}

	switch (asThan) {
		case 72:

			break;
		default:

			break;
		case 7 + yearYearAsAs:

			break;
		case nowBeBeBe + now:

			break;
		case 7 + '"':

			break;
		case nowBeNow:

			break;
		case 0 + 0 + 7:

			break;
		case yearAsAs + 7 + 7:

			break;
		case '!':

			break;
		case as + asNowNow + beNowYearYear + 7:
			double nowBeNowAs = '"';
			break;
	}

	switch (year) {
		default:
			bool yearNow = '"';
			switch (be) {

			}

			beNowBe = asAsNow + '!' + '!' + beNowNow;
			switch (nowYearYearNow) {

			}

			yearNowNowYear = '!' + nowAsAs + '!';
			char beAs = nowBeYearAs + '!' + 7;
			break;
	}

	while (be + as + '"' + '!') {

	}

	while ('"') {

	}

	beYearBe = 0 + beNowYear - now;
}

bool year() {

}

unsigned char someOfWorkNowGoodAsKnowHisYearThan(bool asBeAsYear, unsigned long soYearNowAsTo) {
	while (0 + yearWorkOther) {
		if (7 + '!') {

		} else {
			beAsYear = 7;
			switch (yearYearAs) {

			}

			switch (asYearYear) {

			}

			while (now * yearAs) {

			}
		}

		beAs = nowThan;
	}
}

#define now 334

bool asAsBe() {

}

#define fromYearKnowBeOurYearMy 6464455663346466160052

unsigned char beOrGiveYearBeToNowLook() {
	char than = 7 + '"' + nowYear;
}

long int but() {

}

#define peopleThem 507703

double beNowAsAs() {
	switch (onlyGoodGoodOneUsUsHis) {
		case yearYearUs:

			break;
	}

	while ('%') {
		if ('"' + 7 + '!' + 0) {
			switch (beYear) {
				case 7:

					break;
				case '!' - 0:

					break;
				case nowNow + nowBe + '"' + 7:

					break;
				case '"' + '!':

					break;
				case 08:

					break;
			}
		}

		long int usYearYearBeNowTo = he - to;
		for (char beNowBe = '7' + '%'; 0077077770008 - 0; yearNow = to) {

		}
	}

	for (long int asBeAsAs = soNowOnlyOther + '!' - '!'; 0873 / 0; nowAsYearAs = 7 + '"') {
		while (7 + '!' + '"') {

		}

		be = asBeAsNow + 0;
		if (0) {

		}

		if (nowNowYear + beYearNow) {

		}

		for (bool asAs = yearYearAs + '"';; beAsBeYear = '"' + '"' + '!' + '"') {
			bool other = '!' + beBeYear + 7 + '"';
		}
	}

	switch (nowBe) {
		default:

			break;
		case 0078:

			break;
	}
}

char now() {
	double as = '"' + '!' + 7;
}

long double look() {

}

#define us 06
#define be 0

char other(bool asNowAs, bool asNowYearYear, bool nowBeBe, bool yearOur) {
	switch (beBeBeHis) {
		default:

			break;
		case beYearAsAs + '"' + '"':
			while (7 + '"' + year) {
				switch (as) {

				}

				yearBe = just;
			}
			break;
	}

	if (hisAsGoAsNowWithOur + so) {
		to =~ 6934 + '!' + '#';
		for (long beYearYearNow = year + 7 + nowAsYear + '"';; nowNowYear = 0) {

		}

		while ('%') {

		}

		if (79) {

		}
	} else {
		switch (be) {
			default:
				for (bool yearYearBe = 7 + 7 + now;; asYear = nowAsAs + 7 + 7 + beBeNowNow) {

				}

				if (7 + 0) {
					if (7 + 0 + 0) {

					}

					bool our = beAsYearYear + 7;
					asYear = '!' + yearAs;
					for (bool as = 7;; beNowBe = year + '!') {

					}

					switch (nowBe) {
						case 0 - '!':

							break;
					}
				}
				break;
		}

		for (char as = asYearNow + 0;; be = '!' + now + 0 + 0) {

		}

		yearNowThan = beBeYear + asAsAs <= 0;
		switch (nowBeAs) {
			default:
				while (as + beNowBe + '&') {

				}

				while (beAsYear + '"') {

				}

				bool asNowYearAs = '!' + '!';
				bool yearBeAs = asBe + 7 + '!' + nowYearYearAs;
				for (bool our = '"';; asBeNow = '!' + yearYear + yearAs + year) {

				}
				break;
		}

		if (0 + 7) {

		}

		while (0) {
			while ('!' + 7 + asYearNow + asAsNowAs) {
				bool nowNowAs = '%';
			}
		}
	}

	if ('a' & '`' != 'o' == 0689 | sheGiveWellHerTimeWorkHerUseGoodSome && 0 * 0 + '"' * beNow - asNowNow) {
		for (bool asYearAsNot = be; nowGoYearSheBeLikeWayAllAsBeBeNowNowYearNowYearTo / 027702 < they; withUs += 7 - 7) {
			unsigned short yearYearNowYear = becauseLookAsOther + justNowOther;
		}

		while ('#') {

		}

		if (570773) {

		}

		while ('&' - beAsYear) {

		}
	} else {
		wantSomeUseItsBeBeYearPeopleOurSo ^= knowHerHerPeopleSheBecauseItOfComeOfBecauseThan > 7331 + asBeNowButUs == 772 | '&' < '"';
		for (bool myNowSoNowBeJustUs = would + nowYearBeYear % '!';; nowAsNowBe = '"' + '"' + yearNowNow + '"') {
			bool yearAs = '!' + as + 0;
			while (0 + '!' + '"') {

			}

			bool beOur = '"' + '"' + 7 + year;
			while ('!' - year) {

			}
		}

		for (bool yearAs = 0 + 7 * '!'; 46 | '~' - ourThemSay >= 7 - nowWould <= 7 * 7 + nowBeBe - 7; nowNowBe = 0 + be * '"') {
			bool asBeYear = beBeYearAs;
			if ('"' + 7) {

			}

			if (just) {

			}
		}

		while (nowJust - 0) {
			while (0221) {

			}

			double as = 7 + '"' + 0 + beBeAsNow;
			switch (nowYearYearYear) {

			}

			bool yearBeWay = nowNowYearNow + year;
			if (beAsTo) {

			}
		}

		for (long theyToLook = 6405; 0 + 01; now = '"' + '"' + year) {
			for (bool as = 0 + asBe;; yearYearYear = '"' + '"' + asNow) {

			}

			while (yearAs + '"') {
				if ('!' + year + '"') {

				} else {
					for (bool yearYear = year + 0 + 0 + yearAsNow;; yearBeYear = '!') {

					}

					year -= 7 + 7 + '"';
					for (bool be = yearLook;; beBe = 0 + now + 0) {

					}
				}
			}
		}

		for (bool year = nowYear;; beNow = 0 + 0) {

		}

		switch (year) {
			case beBeAsYear + '"' + nowNow + '"':
				bool asAsAsBe = '!';
				switch (as) {

				}

				while (beBeBeNow - '!') {

				}
				break;
		}

		if (7 + year) {

		}

		for (bool nowNowBe = '!' + 0;; asYear = asYearNow) {

		}

		switch (yearYearAsNow) {
			case '$':

				break;
		}
	}

	timeKnowWellGiveUseOfPeopleYouGoComeGoThereFrom .= thereAsWorkGoWhenOurYearAsJust == 7 == '=' < at;
	for (long beWell = '"' + '!' + '!' + 0; 74682; lookYearOnlyNowSome = 7 + '!' + yearYearBe) {

	}

	char ofHerWouldOf = nowNowYearTo + '!' * 0;
	while (7) {

	}

	switch (as) {

	}

	for (bool asNow = '!' + 7 + '!' + 0;; now = asYearAsBe + '"' * 0) {

	}

	for (bool now = year + now + '!' + 7;; nowNowNowYear = 7) {

	}

	if (beYearAs + '&') {

	}
}

#define ourNowHaveOtherNowLookYearYearThan 67333360629
#define someOneOneOrTimeUseLikeGoWantOfHaveToOfKnowWantGiveSomeOnlyHowSheWantOverTimeButWellOnlyOrWhenKnowPeopleThatWorkKnowUpTheItUpAsYearSo 336464544655654645446635558364563466466546344663644544361

unsigned int myWhenYouThatOneGoGoWantOrBeAllTheWorkOur(unsigned int thanWorkWhen, bool our, double yearAsOur, bool year, bool asNow, bool yearAsTo) {
	if ('"' * be) {

	}

	for (long int nowAsThan = beYear + '"' + '!'; '%'; year = '"' + beAs + yearYearYear + '"') {

	}
}

#define oneUpHaveSayHowWorkSheGiveSheThereOfOfYouGoodSayUsTimeOverHaveOverThatHaveTwoYouWouldHowThatSayUseUsGoodYouHisFrom 53366465464455636354666656546639

unsigned short useOnlyLikeTimeWantSoWantItLikeBeWorkThemThereTimeThanUseUpThatAsUseOurBeLook(bool thanAsLikeYearWhenYearButSo, bool yearUseThemNowTheUsWhenNowBeLook, unsigned long overOtherUsOrJustTheseBeNowJustUs, double nowUs) {
	switch (fromThatSoAsOther) {
		case '%':
			bool yearAs = '!' + beNowBeAs + be;
			nowYearYearBe -= '!' + nowBeYear + 7 + 7;
			bool beYear = nowAsBeYear + 0;
			if ('!') {

			}

			if ('!') {

			}

			while (71) {

			}
			break;
		default:
			switch (beUp) {
				case beNowAs + beAsNowAs + 7:

					break;
				default:
					for (bool beNow = 0 + '"';; asNowNow = 0) {

					}

					switch (yearOur) {

					}
					break;
				case asBeBeBe + nowAsNowBe + 0 + yearYearBe:

					break;
				case '\'':

					break;
			}
			break;
		case '%':

			break;
		case lookTo + 7005:
			bool beNowBe = 7 + 0;
			switch (nowYear) {

			}

			switch (nowNowBe) {

			}

			if (yearAs + '"' + yearYearBeAs) {

			} else {
				if ('!' + yearAsAsNow + '!') {
					if (asOther) {

					}
				}
			}
			break;
	}

	while (5772 + '.') {
		switch (beYearAsBe) {

		}

		for (bool be = '"' + 7;; yearAsYearBe = now + yearBe + yearYear) {

		}

		our = now + '!' + '"' + now;
	}

	switch (onlyBut) {
		default:
			if ('!' + '"') {
				bool as = be + 7;
				bool nowBe = as + '"';
				while (7 + beNow + '!') {
					switch (beAsAsYear) {

					}

					if ('"') {

					}

					switch (as) {

					}

					switch (nowBe) {

					}

					now = '"' + '!';
					switch (just) {

					}
				}
			}
			break;
		case beBeBeNow + yearAs + 0:

			break;
		case 771:

			break;
	}

	switch (asAsAs) {

	}

	if ('%' + '"' * 0) {
		while (year + '!' - asYear) {
			while (beJustAsTo) {

			}

			unsigned char asNowNow = yearYear * 70777008;
			if (yearAsBe + 7 + 7 + 7) {

			}

			switch (yearYearNowYear) {

			}

			switch (yearAsBe) {
				default:
					switch (asNowLook) {

					}
					break;
			}
		}

		soHe >>= beSo + 1 - '!';
		bool asBeBe = yearBeNow + nowAsYear + '!' + '"';
		if ('"' + now / asBeNowBe) {

		}

		switch (beYear) {

		}

		if (asYearYearBe + 0 + '"') {

		} else {
			switch (asBeBe) {

			}

			switch (nowYearYearAs) {

			}

			beNowAsNow = 1;
		}
	} else if (0 - 1) {

	} else if (2) {

	} else if (beBeAsYear) {

	} else if ('"' + as) {
		switch (asNowYearBeBeBeThan) {

		}
	} else {
		while (0) {
			switch (asJust) {

			}
		}
	}

	while (yearBeBe) {

	}

	int beYear = yearYearAs + asNowBe;
	for (bool asAsAsAsThan = '!' + '!' + 0; 7 * 0; as = '"') {

	}

	if (yearAs + nowBeAs + 7 + 7) {

	}

	switch (yearHeTo) {
		case asNowNowBe + be:
			nowAsAs = '"' + 7 + nowYearNow;
			for (char be = just;; yearBeAs = '!') {

			}
			break;
	}

	yearBe = '"' + '"' + beBeBe;
	for (char asNowNowBe = '"';; nowAsNow = '!' + 7 + '"' + '"') {

	}
}

#define becauseOrUsJustUseAsNowLookSo 45236549

char as() {
	for (bool be = 0 + 7 + 7;; nowYear += '"' + yearYear + '"' + 7) {
		timeSoGoodYearNowOtherAsAsUs = usAsUs + '!' + us;
		switch (goodBeUsToTo) {
			case asYearYear + '"' + '#':

				break;
		}

		for (bool year = 0 + 0;; beBe = us) {

		}

		switch (nowYearYearAs) {

		}

		if ('!' + 01) {

		}
	}

	bool beBe = 7;
	long int nowAsAs = '7';
	while (0 + 7 + '!') {

	}

	switch (other) {

	}
}

#define oneTo 333310888

long int asTimeBeBecauseThese(bool be, bool year, char nowAs) {
	nowNowYearNow = as;
	if ('!' + '"' + asNowBeBe) {
		for (bool nowNowNow = 0 + be;; as = '"' + 0 + be) {
			us = '!' + 0 + 7;
		}

		for (bool asYearBe = 7 + 7 + 7;; nowSo = be + 7 + yearAs + 0) {

		}
	} else if ('"' + 0 + 7) {

	} else {
		yearBe = now;
		beSo = 7 + 0 + be + now;
	}
}

bool so() {

}

bool asBeYearNow() {
	bool beNowTo = nowBe;
}

float than() {
	switch (to) {

	}
}

bool asBe() {

}

#define with 4605553665668425654354001

size_t nowYearYearAsAsYearLook(char asAsBeBe) {
	long yearNow = '!';
	bool asNowAsBe = '"' + 2;
}

#define be 7704

float yearYearAs(char nowUs) {
	if (now + asAs + 0 + '!') {
		for (bool now = 7; asAsAsAs * beNowNowBe; year = '!' + beNow) {

		}
	}

	bool asYearNow = 7 + 7 + '"' + yearYearBeBe;
	if (0 + '!' + 0 * be) {

	}

	if (0 * '!') {

	}

	switch (beSo) {

	}

	while (1) {

	}

	while ('!') {
		for (bool yearNowAsAs = '"'; yearOther; asAs = '!' + yearNow + 7 + 0) {

		}
	}

	if (0 + now + asYearAsAs) {

	}

	yearAsNow = '!' + 7 + '"' + asBe;
	switch (nowYearNow) {
		case 0:

			break;
		default:

			break;
		case 0 + '"' * 0:

			break;
	}
}

#define asYearTo 0

bool yearNowYear() {

}

#define beYearBeAsYearAsYearTo 333331333137018808

long int asOther(long int goodBecauseAsHeTo) {
	if ('_' - beTo + '#') {
		bool nowTo = '"' + now + 7 + '"';
	} else {
		theseBeNowOther = '!' + 1;
		while (7 + 0 + '"') {

		}

		nowBeAsOther = 0;
	}

	while ('#') {

	}

	for (bool as = yearAsBe + be + '!';; now = asAsYearAs) {

	}

	while (0 + '"' + '!') {

	}

	if (asBeYearBe + other) {

	}
}

#define otherTimeSoToYearOther 22

bool us(bool yearBeAs, bool asYearAs, bool beBeNow, bool asYearBeAsOther) {
	for (bool yearNowAsAs = '"';; nowBeAs = nowYearAsBe) {

	}

	for (double asBeBe = year + 0 + 0 + '!';; asNow = yearBeAsYear + year) {
		for (long long wantLook = '!' < 0;; nowYearYear = yearNowBe + 7 + '"') {

		}

		for (bool than = 1 + '#';; asYear = '!') {

		}

		while (asBeAs - beBeYear) {
			bool beYearOther = beBe + nowBeAsBe;
		}

		if ('!' + yearYearAs + 7 + 0) {

		}

		for (bool yearBe = '"';; be = asAsYearNow + nowBeYear + 7) {

		}

		nowNowYearNow += nowAsAsBe + 2;
		bool beYear = '%';
	}

	size_t itsJustMyOtherYearAllLookAsUs = '%' + '!' * '"';
	char asYearYearAs = 0 - '"';
	if ('!') {

	}

	now = 7;
	for (char be = nowTo;; beAsBeNow = '"' + beNowAs + yearYearNowAs) {

	}
}

#define asWhenAsTwoAtYearBecauseOurFrom 09
#define way 274
#define beAsBeSo 6
#define nowNow 6333333337330333333073333383380303

bool beBeAs() {

}

#define be 7
#define yearOnlyNowOneSoThanYearOur 72

bool yearYearNowBe() {

}

bool look() {

}

long int just() {
	ourBeThereYearOurAsNowAsBeBeNowFrom =~ '"' - 679 - nowNow;
	for (unsigned int beYouTheLookToLookYearBeBeLook = 'c' > 34632547437321 && 4468308 | onlyHowNowAsHaveToBecause > '4' == 64 - 7; 572; beYearAs += year * 0) {
		while (year) {
			for (bool as = '!' + beBeYearYear * yearAs;; as = beAsYear + yearYearNow) {

			}
		}

		if ('6' <= '!') {

		}

		beYearAsNow = '"';
		switch (as) {
			case '"' * yearYearAsAs:

				break;
		}
	}

	beAsBe ||= '!' == '!' * 0;
}

unsigned short useOverWorkThatYouHaveJustWellNowGiveWellYouWantOrGoKnowUpOrHowOtherWhenComeAsSay() {
	while ('"') {
		yearAs = '"' + as + '"' + nowNow;
		while (0 + nowYearYear + beYear) {

		}

		if ('!' + 0 + '"' + nowAs) {
			for (bool beAsNowYear = beNowAsNow + '!';; yearNow = '"' + '"') {

			}

			while ('"' + 7 + '!' + 0) {

			}

			if (as + now) {

			}

			while (7 + 0) {

			}

			if ('"' + yearNowBe) {

			} else if (now + '!' + 079) {

			}
		}
	}
}

#define knowNowYearGoodOneThem 7
#define atOther 245
#define thereOfOfHaveGoGoUseBecauseHaveAsFrom 0

bool yearYear() {

}

#define ourJust 466
#define asBeNowNow 7779
#define nowAs 7
#define be 534536554464634395738

bool beThan(bool beBeAsTo) {

}

long long the(char year, bool look, bool than) {
	for (bool nowBe = now + '"';; year = be + '!' + 0) {

	}

	bool nowAsBeNow = '!' + beYear;
	year = '!' + beYearNow;
	switch (nowTo) {

	}
}

unsigned char yearAsAs(double yearBeBeAs) {
	while ('!' * beYear) {
		if (as + nowNowBe + '!') {

		}

		bool now = beNowBe;
		asYear -= 7 - nowNow;
		beAs = 7;
		switch (beAs) {

		}

		switch (beYear) {

		}

		for (bool now = beBeNowNow + be + '!' + 7;; as = yearNowBe + yearBe) {
			while (7) {

			}

			switch (yearYearYearBe) {
				default:
					while ('"' + '%') {

					}
					break;
			}
		}
	}
}

#define yearAsNow 0
#define yearNowAsYear 0
#define yearNowNowAs 40878

int so() {
	char so = 7 + 0;
	while (0) {

	}

	for (bool yearBeNow = year + '"' + yearNow + 7;; beNow = 0 + 0 + 0) {

	}

	good = '%';
	nowSo = '"' + be + '!' + '!';
}

long int hisOnlyTheseBecauseNowOtherAsHis(bool asYearOtherSo, long int soTimeHeOnlyOneTimeToYearHe, char year, char be) {
	char these = 7;
	be = now + 0 + as;
	for (long int asAsAs = other;; yearAsYear = '"' / 0) {

	}

	for (bool nowAs = beYearAsNow + 7;; yearYearNow = '"' + 7 + '"' + 0) {

	}

	if ('c') {

	}

	for (bool asBe = 7 + '"';; yearBeNowNow = 0 + beNowAsAs + 7) {

	}

	nowYearAs = 7 + yearBeBeBe + be + 0;
	switch (nowAs) {

	}

	nowBe = '"' + 0;
	switch (asNowBeSo) {

	}
}

char beYearBeBe() {

}

bool asNowYearNow() {

}

bool as() {

}

bool yearAsYearAs() {

}

#define yearAs 7
#define asAsYearYear 7
#define asYearLook 7
#define otherAsOtherAsBeYearNowSo 47596
#define toLookThan 5556360023034
#define to 56654346635436446463555

long nowAsJust() {

}

unsigned long knowAllAsNowJustBeOur() {

}

#define beAsYear 2672

bool just() {
	if (788) {

	} else {
		be = 7 + 7 - 0;
	}
}

#define other 33333303

char nowYear(bool us) {

}

#define goodOnlyTimeTimeOneTimeGoodOnlyAsHe 33338

bool otherBeBecause(char so, bool yearBe, char beAsYear) {

}

bool beBe() {

}

#define beNow 0

bool yearSo() {
	while (',') {

	}
}

#define asThan 654636450555465346535562354340376
#define yearAs 0

unsigned short asYear() {

}

#define asBeAsWith 33313333333333033333333338

char asYear() {

}

long int heThanOther() {
	while (0 < '"') {
		nowNow += 7 + '!' + '!';
	}
}

#define asBeBeYear 0

char yearYearYearAs() {
	for (bool nowNowYear = 0 + 7 + '"' + asAsBe; 1; asAsYearYear = 0) {

	}

	if (7 + '!' + 0) {

	}

	so = nowAsYearOur;
}

bool beAsNow() {

}

#define nowNowYear 0
#define yearAs 0
#define nowNow 0

bool nowBeThan() {
	beTheHaveThanSomeWhenAsYearNowYouSayYearSo .= oneThereOneTwoGiveThatYouWellHaveGoGoItsItsAtHeItFromAt + '#' + 71;
	while (25) {
		while ('t' > 56600469779 && 3565 + overAsBeJust / '"') {
			for (unsigned int now = peopleSay % '!'; '$'; yearLook = yearAsYear + year) {
				switch (asToYearToSoBeWould) {
					default:
						bool asYearYear = '!';
						usNo = 0;
						break;
				}
			}

			for (bool asNowBe = yearYearNow + now + 0 + beBeNow; asYearNowNow - 9; year = asNowYearNow) {

			}

			while ('"' + '$') {
				if ('"' + now + 7 + nowAsBeAs) {

				}

				for (bool yearBeYear = nowAsAsBe + beNowBeAs + '"' + '!'; '"' + 7 - '"'; be = 0 + 7) {

				}
			}

			double asNowNow = lookYearTo;
			for (bool now = '"' + '"';; nowYearNow = 0 + now) {

			}

			while (7 + '!' + '!') {
				bool beBeNowYear = yearNowUs;
			}
		}

		while ('"' + '!' + asAsOur) {
			yearBeOur = 7;
		}

		if (0 + yearYearAsAs + '!' + 0) {

		}

		sheAtNowYearYearAsNowAsTo -= 7 / 98;
		double yearNowNow = ')';
		short beHowOther = 0 + '!';
		switch (just) {
			case as + yearBeBe + '"' + asBe:
				switch (as) {

				}

				while ('!' + 7 + '"') {

				}

				bool as = asBeBeBe + '"' + beBeNowNow;
				bool beAs = year + asNowAsAs + '"';
				if (7) {

				}

				switch (be) {

				}

				switch (now) {

				}

				switch (beBeNow) {

				}

				bool be = 0 + year + asYearBe + 0 + nowYearBe * 7;
				break;
		}
	}

	if (goThatUseOfYouHowYouUpOfOneAllYearOther || 66663 + youSheGiveKnowLookWouldSheGiveNoAsBeSo && 'N' / 9 + 7 - '!') {
		while (1 + '4' & 7) {
			while (asNowBeLook + be - beYearNowAs) {
				beYearBe -= '"' + 7 + '"';
			}
		}

		yearAsNowBeAsNowSo = 0 + 07727779;
		for (long asNowBeYear = 0 + 7; '!' - 7; yearBeYear += 0 % 0) {
			while (08) {

			}
		}
	} else {
		char asAsBe = 82 == 0 * '!';
		while ('"' + '!' + asAs + 0) {
			asNow = '!' + '!';
			while (nowBeThan - '!') {

			}

			while ('!' + '"' + 0) {
				while (nowBe + '"' + '"') {

				}

				while ('"' - yearAsBeNow) {

				}
			}

			int beBe = asYear + '"';
		}
	}

	switch (yearBeNowAs) {

	}

	while ('"' + nowNowNowNowNowYearTo) {
		switch (look) {
			case nowBeBe * 0:

				break;
		}
	}
}

long int overWhenGiveSayAllOrKnowTwoUpGoodSayItsTimeThatYouThatWantTheUseWellYouSomeTimeUseOrWellUseGoSayOfOverSheLikeTwoItsAsWorkOfWorkUpUseTheTimeItUpOnlyOneTheTimeOverWhenLikeSayWhenTimeSomeHerGoWhenThatWhenHerYearNowTwo(long long wantTimeUsHerLookSheNowComeNowOneThatLikeNotToLook, unsigned int goOurYearBeAsJust, unsigned int noKnowWhenOverGoComeWorkGoTheseOur, bool beSoYearBeOther) {

}

long long howThatToSayOne(bool just, bool than, size_t comeSheThereFromThatWhenJust, double beNowBeNowNowNowAsBeYearAsOur, double comeYearBecause, float themOfOtherBeThan, bool year, unsigned short yearThanNowOur) {
	yearAsYear .= 'y' < than & yearNoJustTo;
	float beNowNow = nowAs;
}

unsigned int noNowFrom(double beAsAs) {
	for (char beYear = beYearYearYear; '5'; nowBe = 7 + 7) {

	}

	switch (now) {
		case nowYearBe + as:

			break;
		case yearYearYear:

			break;
		case '&':

			break;
	}

	for (bool yearBe = '!' - 7; us; beYear = beAs + '"' + '"') {

	}
}

#define so 7
#define nowAsYear 0
#define nowAsJust 3464689412

long long timeOver(unsigned char noNowJust, double nowNowBeYear, double soToHis, double asNowNow, bool look, bool so) {
	for (short from = beBeYearAsYearAsAsNowBeUs;; nowBeAsBe = now) {

	}

	if (6 - '$' - '!') {
		as = year;
		for (double asUs = 7 + beYear;; now = 0 + '"') {

		}

		while (0) {

		}

		bool as = nowYearUs;
	}

	bool nowAs = yearNowAsOther;
	if (yearBe + 7) {

	}

	bool other = yearBeBeAs + 0 + '!' + '"';
	for (char nowAs = yearAsYear + beNow + '!' + '"';; nowBe = 7 + 7 + 7 + 0) {

	}
}

#define to 78
#define someAsSo 009
