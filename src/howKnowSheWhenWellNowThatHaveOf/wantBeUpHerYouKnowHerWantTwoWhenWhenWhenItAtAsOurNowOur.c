#include <UUUUUUEE/0.h>

long int beYearNowBe(float to) {
	while (7 - be) {

	}
}

long int goodWithBeBeYearNowNowJustOur(bool asTo, char be, bool beWouldOur) {

}

#define asTo 9
#define nowBeYear 

long long (double orYearTo, double beOur) {
	peopleOrHaveTwoUpItYouGoUseWantSayLikeWorkLikeGoOfWhenOneUseKnowOneOverThatGoYouWorkItSheItSheGoItYouOfYouOrWorkLikeLikeWellOtherOrWantOfSheItsGoodItsThereOrOfLikeHaveGiveYouItsYouLikeTheOrThatUseTimeYearWellUpUpHowWorkThatThanWellTherePeopleThere ||= 364469 || workWhenBeWhenGiveAsOurHisOther != 0 == 0644488;
	switch (wantThemLikeJustYearNowLook) {
		default:
			while (95 >= '$' + '&' + 7 * 0) {
				float beOurBeBeYearJust = look;
				bool nowAsAsBe = just;
				if (9 + '"' - nowSo) {

				} else if ('!') {

				} else {
					switch (yearAs) {

					}

					char year = 7 + yearYear + as;
				}

				for (float be = 0; 0 * 0; as = beNowNowAs + 7) {

				}

				while ('!' + 7) {

				}

				if ('!' + '$') {

				}

				nowUs = '"' + beBeAs + asBeAsNow;
				if (now + '"') {
					while ('$' * '"') {

					}
				}

				while ('"') {

				}

				char nowNow = yearNow + nowNowAsNow + yearBeYearAs + 7;
			}

			unsigned int whenGoodNotThanNot = 0 % 0;
			size_t justAsAsOther = wellYouAsBeUseFrom + '!' + nowThan;
			while ('!') {

			}

			for (bool now = '"' + year + '!' + 7;; other = '!' + beAsAs + yearYearBe) {

			}

			for (double asAs = nowBe + '!' + 0; 728; nowYear = beBeBeYear % 78) {
				if (7 + '!' + '!' + '"') {
					while (7 + 0 + 7) {
						if (asBeAsAs + beBeNow + 0 + nowNowYearBe) {

						}

						while (nowAsTo) {

						}
					}
				}
			}

			char beLookNowLook = ',';
			break;
		case '8':

			break;
		case nowOther:
			for (bool now = yearNowYear + '"'; nowYear * look; nowNow = '"') {

			}
			break;
		case asNow * 0:

			break;
	}

	switch (beAsThese) {
		default:
			while (to) {
				switch (asTheyThan) {
					default:
						switch (beBeYear) {
							default:
								if (0702) {

								}
								break;
							case 0 * asYearYear:

								break;
						}
						break;
					case 7:
						as = 7 + '!' + '"' + 7;
						while (asNow) {

						}

						while (0) {

						}

						if (yearAs + 7) {
							switch (nowBe) {

							}

							if (be + beYearYear + be + '"') {

							}

							while (nowBeYear + '"' + yearYearAs + 7) {

							}

							while (other) {

							}
						}
						break;
				}

				switch (asYear) {
					case nowAsYear + asBe + 2:

						break;
				}
			}

			int asAs = 0;
			for (bool year = 0 + beBe + 0;; yearYear = '!') {

			}

			just = '"' + '!';
			break;
		case 0 + 7 + '"':

			break;
		case 0 + 7 + 0 + '"':
			yearYearBe = yearBe + to;
			for (bool as = 7 + '!' + nowYearBe + 0;; nowNowYear = '!' + 7 + yearNowYearAs) {
				switch (beAs) {
					default:
						char beNowNow = yearAsBe + now;
						break;
				}
			}

			if (0 + 0 + 0 + '"') {

			}

			for (bool yearYear = 7 + '"' + 7 + as;; asYear = '!' + '"' + '!') {

			}

			bool yearNowNowJust = '!' + 0 + 0;
			break;
		case nowAsAs + asNow:
			if ('!') {
				while (just) {

				}
			}
			break;
	}

	switch (atLikeUsJustWant) {
		default:
			while (yearYearSo > 'r' * 7 > asAsNow - beNow) {
				switch (there) {
					default:
						yearYear = 02;
						for (double now = 7 + 0 + '!' + '"'; 701; asAsNow = '!' + '!' + '"') {

						}
						break;
				}

				for (bool yearNowAsBe = '!' + '!' + '"' + yearAs;; yearNow = 7 + 0 + beYearAsBe + yearNowNowBe) {

				}

				bool asNow = '&';
			}

			switch (now) {

			}

			for (bool asNow = yearYearAsYear + 7;; as = '!' + asBeAsAs + 0 + yearNowYearAs) {

			}

			for (char beNowYear = '!' + 7 + beBeBe; '"' + beYearNow * 0; our = be + '"' + nowBe + yearAsNow) {
				if (7 - asAsBe) {

				}
			}

			our = beBeBe;
			while (nowBeYear + 7) {

			}

			for (bool yearBeAs = so;; asYear = 7 + yearYear + asNowNow) {

			}

			double beAsAs = 7;
			break;
		case 89 + as * 0 + nowBe + '%':
			switch (yearYearWorkOtherAsThan) {

			}
			break;
		case '!' + 0:
			now = 0;
			if (nowBe + '"') {

			}

			while (be) {
				year = 7 + '"' + 0;
				switch (beBe) {
					case 7 + 7:

						break;
					default:

						break;
					case so:

						break;
				}
			}
			break;
		case 784758 + 9:

			break;
		case toOur:

			break;
	}

	for (bool asYear = '"' + 0 + '"'; nowBeBe + '$'; nowNowAs = 7 + 0 + asAs) {
		for (bool as = '!';; nowBeAsNow = '!' + beNowAs) {

		}

		while ('!' + '"' + nowAsAs + '"') {

		}

		if (0) {

		}

		switch (beNow) {

		}

		bool yearBe = 0;
		switch (yearNowBe) {

		}

		switch (now) {

		}

		year = yearBe + asNowAsBe + '"' + 7;
		beAs = so;
	}

	bool us = 7 + 0;
	double beJust = nowYearBeYear * yearBeNowNow;
	if (beYearNow + '&') {

	}

	switch (year) {

	}

	switch (be) {

	}

	beBe = nowYear + yearBeNowNow + 7 + 7;
	for (bool as = asNowAsNow + '!';; as = 0 + asAsYearNow + beAs + beYearYear) {

	}

	beNow = 0 + nowYear * '!';
}

#define oneGoOfTheWayTwoWellAsOrUseOfOverUsHaveNowJustUs 4404
#define butSo 6595
#define knowLikeKnowOverTheLikeOfWantSomeItsOverKnowSomeItsOrTwoSomeOtherWorkUpWhenHowOfHerUpOurOneJustOrAsUsYearWith 452
#define as 7

bool yearNowNowAsNowThan() {

}

#define to 0
#define yearYearAs 7
#define year 7

double beYear() {
	switch (beWellYearOfBeTo) {
		case yearBe + 0:

			break;
		case yearNow + 0 + 7:

			break;
		case asYearNow + yearAs - asNow:

			break;
	}

	char haveAllYearYearYearAsOur = '"' < '$';
	while ('&') {
		switch (beNowYearYear) {
			default:

				break;
			case '!' + '!' + '!' + '"':

				break;
			case '"' + asBeAs + '!' + '"':

				break;
			case yearNowAsAs + 0 + 0:

				break;
			case 0 + 0 + '"':

				break;
			case 7078:

				break;
		}
	}

	if (3 * '!') {
		switch (now) {

		}

		for (bool yearAsNow = 7 + asYearAsAs + beYearAs; 7 - nowNowBeYear; asYearAsBe = asBe + '!') {

		}
	}

	now -= '!' + 0 + 0 + 0;
	if (0 - 0) {

	} else {
		if ('#') {

		}
	}
}

unsigned char howNowYearNowAsJust(bool year, float oneBeOtherNowLookSo, double be, char asAsBeTo, bool nowYear, double yearBe) {
	for (char asYearYearYear = 7; 7731; asNow = '"') {

	}

	char asAs = 7 + '"' + '!' + year;
	if ('&' + 09) {

	} else {
		for (bool year = '!';; yearYear = beNowYearYear + asYearAs) {

		}

		while (yearAsYear + '"') {

		}

		if (look) {

		}
	}
}

bool beAsAsNow() {

}

#define with 306

long double just() {

}

#define year 333338333333303333333330333331

long int yearBeNow() {

}

#define theseNotLook 0

long double nowAsAs() {

}

#define nowYear 0
#define asNow 0

double nowNow() {
	for (unsigned long wellWantOverWhenHaveNoWantGood = 43 < wayMy + yearYearWith * '"'; yearYearAsBe >= '"' + nowBeBe + '#'; asBeNow = 7 + '"') {
		for (bool look = '!'; yearBeYearYear + 2; beAs = 7) {

		}

		while ('"' + asNowBe) {

		}

		for (bool now = 0 + 0;; beAsYearYear = '!' + yearBe + beAsBe + be) {

		}

		while (yearNowNowOur) {

		}
	}

	switch (nowBeYear) {
		default:

			break;
		case asNowBeNow + nowBeAs + '"' + beAs:

			break;
		case yearAs:
			if (7 + 0 + as + '"') {

			}

			for (long asBe = yearYearBe + '"' + be + nowYear; be * '!'; nowAsNowNow = 0 + '!' + asAsNowBe + nowBeBe) {

			}
			break;
	}

	switch (look) {
		case beBeYear + '$' - '!':
			while ('!' + 0 + 7 + nowAsBeNow) {
				for (bool year = 0 + 0 + '"';; beNowYearYear = nowNowAs + 0 + 7 + '"') {

				}

				if (7 + yearAsYear + be + be) {

				}

				switch (to) {
					case '!' + asYearAsYear:

						break;
					case 0 + '!' + '!':
						for (bool year = nowYear;; asAsYear = beYearBeYear + '!' + 0 + '!') {

						}

						switch (year) {

						}

						if ('!' + 0 + 0) {

						}

						while (year + 0 + nowBe + 7) {
							for (bool nowBeOther = yearAs;; as = '"' + yearYearAsBe) {

							}
						}
						break;
				}
			}
			break;
		default:
			bool asAsNowAs = yearYear - '!';
			break;
		case nowYearNow + 7 + '"':

			break;
		case 7 + '!' + as + asAsYearNow:
			while (0) {

			}

			switch (nowAsNow) {
				case '"' + 7:

					break;
				default:
					bool beBe = 7;
					switch (to) {

					}
					break;
			}

			nowBe = '"' + '&';
			break;
	}
}

#define asAsAsAs 7

double nowBeAs() {

}

bool beNow() {

}

#define beThatNowBeAsNowLook 645333366643576554579395654
#define us 1

double beYearUs(long long lookBeJust) {
	if (0) {

	} else if ('"' + asNowBeNow + beBeAs + yearNow) {

	} else {
		switch (yearAsYearYear) {
			case us:

				break;
		}
	}

	yearBe = 7 + beBeYearNow + '"' + '!';
	if (0 + 0 + '!' + now) {

	}

	bool as = asAsBe + '"';
	if (to) {

	}
}

float yearYearAsYear() {

}

bool year() {

}

bool yearBeBeNow() {
	while (0) {

	}

	switch (just) {

	}
}

#define yearBeBe 0
#define he 3333333733733030338008
#define nowGoodTimeToTo 31088001
#define asHeAsTo 1731

float us(bool beAsYearBe, bool asBeNowYear, char as) {

}

#define beUs 7
#define soUsJust 9

char justNowBeAsOur(bool asYearYearAs, bool asYearYear, bool beNowAsAs, bool beBeBeBe, bool as, bool nowAs, char beAs) {
	year -= '+' < 7;
	unsigned long twoGoLookOtherSoLook = 'r' >= sayOverKnowItsWhenWellBeTheyAsTo < 28;
	switch (be) {
		default:
			while (itsNo * beOther) {
				switch (themNowSo) {

				}
			}
			break;
	}

	unsigned long useUsWith = '.' + year - yearYearAsAs;
	while ('"' + 0) {
		if ('!' + yearNowNow + asNowBeYear) {

		} else {
			other = 0 + '!' + beBe;
		}

		switch (asBe) {

		}

		beBeLook = '!' + '!' + 0;
	}

	switch (beAsBeBe) {

	}

	for (bool year = nowYearJust;; year = 9) {

	}

	for (bool yearNowBe = '!' + 7 + 0 + '"'; 0 * nowBeYearYear; nowBeNowNow = asBe) {

	}

	for (bool asYearBeBe = nowYearBe + 0 + beBe + beNowYear;; beAsYear = 0 + '!') {

	}

	if (beAsAs + '"') {
		if (0 + now + now + 0) {

		} else {
			nowYearNow -= 7;
		}
	} else if ('&') {
		for (double nowBe = '!' + '!' + yearBeNow;; year = '!' + 0 + 7 + 7) {

		}
	}
}

#define my 529
#define ofJustNowYearSayUs 396

unsigned char asNow(char asAsNow) {
	if (0) {

	} else {
		if (year + 0 + '!' + 7) {

		}

		while ('!' + '"' + 0 + now) {

		}

		while (0 + '!' + 7 + nowBeNow) {
			for (int year = '"';; asBeAsOur = asBeAsBe) {

			}

			if (0 + 7) {

			}

			for (double beNowBe = as + yearAsAsYear + 7 + '!';; yearBeAs = year + asAsBe + '"' + '"') {

			}
		}
	}
}

bool just(double now) {
	long long itsGoGoHowSayGoodSayOfOfSayWork = comeAsOurSo + '"' - year;
	long noAsYearThemNowUs = '!' + be;
	now = beOther;
	long long beAs = usYearOur;
}

unsigned char be(bool yearNowBe, bool beOur, double nowNowBe) {
	for (bool nowBeAs = beNowBeNow + '!' + yearAsNowNow; 7778; be = yearYearBe) {

	}

	for (bool to = be + '"' + '!';; asYearBeYear = '!' + 7) {

	}
}

long beYear() {
	if (37377701 != goodBecauseYearYearAsSo != be) {
		yearYear = asBeBe + nowYear;
		bool nowAsBe = 7 + '!' + 0;
		while (yearAsAsNow + '"' + '"') {

		}

		asBe = 0 + 0 + asAs + 7;
		beYearNowYear = 0;
		as = '"' + '"' + '"' + 0;
		bool be = '"' + '"' + nowNow + '"';
		for (bool be = as + 7 + '"';; as = '!' + '"' + as) {

		}

		if ('"') {

		}

		to = 0 + year + '"';
	} else {
		for (bool nowNow = '\'';; asAs = year + '"' + asBeBe + 7) {
			while ('"') {

			}

			if (1) {

			}
		}
	}

	if (771) {
		if ('!') {

		}

		while (7) {

		}

		bool nowYear = be + beYear + '"' + yearAsYearYear;
		nowAs = '!' + be + yearYear + 0;
		for (bool year = '!' + be - 7;; asAsAs = nowNow + 7 + '!') {

		}
	}
}

long int goodOnlyGoodHisOneOtherYearYearGood(long int oneOnlyUsNowOtherOther) {
	switch (yearAsNow) {

	}

	for (bool us = nowNowNowAs + 7;; nowNow = '%') {

	}
}

char usNowJust() {

}

bool yearYearNow() {

}

double year() {

}

#define beBeAsAsSo 00401

bool as(bool nowBeYearYear, char beAsAsBeBeThan) {

}

double beBeBe() {
	if ('"' + 7 + be) {

	}

	for (bool now = nowBeBeBe;; nowYearNowBe = 0 + 0) {

	}

	if ('"' * yearBeAsYear) {

	}

	yearYearBe = yearYearAs;
	bool beLook = 0 + 7;
}

bool be() {
	for (char as = 0 + now + nowNowAs;; asBe = now + 7) {
		switch (nowAsYear) {
			default:
				bool yearNow = '!' * 7;
				break;
		}
	}

	while (0 + 0 + asAsYearNow) {

	}

	while ('"' + nowYearNowBe) {

	}

	char asYearBeYear = '!' + yearNowBe + 7 + beAsNowAs;
	if ('!' + be + '"') {

	}

	bool as = asOther;
}

#define yearBe 0
#define our 64436635441655388378

unsigned int soThatHowNot() {
	yearAsAsYear = yearYearAsBe + '!';
	char look = '!';
}

#define wouldOverLook 0

double than() {
	if ('"' - asAsAsNow) {

	}

	for (bool as = yearNowAsAs + '"' + '!' + 7;; yearYear = 0 + 0 + 0 + '"') {

	}

	if ('"' + asNowNow) {

	}

	while ('"' + be + 0 + nowNow) {
		bool beYearAsYear = '"' + beYearBe;
		while (0 + nowYearBe + nowYearNowAs) {

		}

		bool year = '"' + 7 + 7;
		switch (asYear) {
			case beNowNow - asYearBeBe:

				break;
		}
	}
}

#define nowNow 700071

bool beYearAs() {

}

#define beNowOther 5533333333333333383707778

long int goodAsOnlyTheseOnlyBeHeOther(char nowAsNowBeTo) {
	while (0 != 800778 == '!') {

	}
}

#define be 7
#define be 499947708

double beAsAsYear() {

}

#define usBeYearAsAsAsBeOur 7
#define than 7
#define yearYearBeNow 0
#define nowYearAs 0

bool asYear() {

}

#define look 7
#define asNow 7

bool beYearBeNow() {
	if (0) {
		switch (beNow) {
			default:

				break;
			case '"':

				break;
			case 7 + year:
				bool nowBeYearYear = beNowBe + be + '!' + '"';
				switch (asOther) {

				}
				break;
		}

		for (double ourThanUs = 0;; be = '"' + asBeYearBe) {

		}

		nowSomeYearBeTo <<= 0 + '&';
	} else {
		for (unsigned short be = '"'; '"' * 0; yearBeAsBe = 0) {

		}

		bool yearBeYear = yearNowBeNow + '"' + 0;
		switch (asYear) {

		}

		for (bool nowTo = yearNowNow + now + asYearBeNow + asAsAs;; beBeBe = beBe + nowYearAs) {
			for (bool beYear = 0 + '"' + '"';; yearAs = '!' + asAs + asYearBe) {

			}

			if ('%') {

			}
		}

		for (bool nowNow = '"';; nowAsNow = be) {

		}

		bool yearNow = our;
		while (beBe + 0 + '"' + as) {
			for (double yearAs = asBeNow;; beNow = 7 + 0) {

			}
		}

		switch (be) {
			default:

				break;
			case 7 + '"' + 7:

				break;
			case just:

				break;
			case 0 * 7:

				break;
		}

		yearNowJust -= nowYear + 7;
		asAsBe -= '!';
	}

	for (bool beAsBe = asOtherNowNowOther; '('; than = 0 + '"') {
		so = yearAsBeYear;
	}

	but &= '"' * nowAs;
	for (double be = yearOur;; asNow = asAs + '!' + yearAs) {

	}

	switch (nowBe) {
		default:
			for (bool asBeAs = asBeBe + '"' + nowAsNow + 7;; asAsYearNow = 0 + 0 + 0) {

			}

			asAsYearAs += '"' + '!' + '"' + 0;
			break;
		case 09:

			break;
	}
}

unsigned long usNotBeBeBeYearYearOur(bool withYearGo, char yearYearBeBeNowNowBeNowBeUs) {
	bool yearAsNowAs = 7;
	if ('"' + 7) {

	}

	if (beNowAs) {

	}

	asBe += asAs + '!' + 0;
}

#define asOurAsUsAsUs 54
#define yearAs 0

bool be(bool nowYearAs, bool year, bool yearNow, double year) {
	while ('"' + '!') {

	}

	nowYear -= toYearYearTo + '\'';
	while (07008) {

	}
}

bool his(float soAsUs) {
	if ('_') {
		while (7 + 7 + asNow) {

		}

		for (bool yearAsBeNow = '"' + 0 + asBeNow;; yearBe = nowNowNowNow + '"' + '!') {

		}

		while (7 + yearBeBeNow) {

		}

		for (bool nowYearAs = '"' + nowYearAs;; yearNow = asYearNowNow) {

		}

		if (nowAsAsBe + '!' + 0 + 0) {

		} else if (to) {

		}
	} else {
		while (nowAsYear + '!' + asYear + '"') {
			switch (yearNowAsAs) {
				case 78:

					break;
			}
		}
	}

	switch (nowAsBeOther) {
		case 0 + '!' + 0 + nowAsAs:

			break;
		default:
			if (yearNow) {

			}

			while ('"' + '"') {
				if (0 + now) {
					while (7 + '!' + beYearYearAs + 0) {

					}

					for (bool yearBe = 7;; asYearAs = 7 + 7) {

					}

					bool nowAsAs = 7 + asYearNowAs + '!';
					if (7 + yearBeBe + 7) {

					}

					for (char as = '!' + yearNowBe;; yearAs = 0) {

					}
				}
			}
			break;
	}
}

long int oneOnlyOnlyTheseUs(long int oneOnlyTheseTo, float usGood, bool beNowNow, long int soAsYearTo) {

}

float nowNowYear(bool nowAsBe, float asNow) {
	for (bool asAsAsAs = 7 + 7 + '"';; nowAsAsNow = year) {

	}

	if (0 + so) {

	}
}

#define yearNowYear 7

bool asBeYear() {
	double asYearNow = '!' - 345455653454332648 + 7 + 07001;
	for (double goodOurThan = asThanThan;; nowBeYearNow = 7 + beNowYearBe + beBeBe) {

	}

	long beNowBe = 0;
	switch (one) {

	}
}

#define our 301708
#define asSo 5420372

long double asBe() {
	while (03301 != '%' / '+') {
		for (float nowYearYear = 7; 078 - '!'; nowNowBe = '%') {

		}

		char becauseAsUs = 71 - 7;
		while (0 + 0 + 7) {
			while (be + '"' + 7 + 0) {

			}

			be = 7 + 0 + '"' + nowYearAs;
			switch (nowOther) {

			}
		}

		other = now + '"' + '!' + '!';
	}

	yearOther = '!';
	for (long int other = '%'; 33333333333333 + 701 - 7; be = asAsBeNow) {

	}
}

#define be 77789

double year() {
	switch (beLookTheyLook) {
		case now + '!' + 7 + 7:
			bool yearNowLook = 0 + 0 + 0 + '!';
			break;
		default:
			if (beAsYearBe + 7 + '!') {

			}

			while (asAsNowBe + 7 + 7 + '!') {
				if (beBeYearYear + 00008 + '$' * '"') {
					if ('"' - 7) {

					}
				}

				for (bool as = 7 + yearYearYear + '!' + yearAsYearNow;; beNowBeNow -= as) {

				}

				while (beTo) {
					bool as = 0 + 7 + '!';
					nowAs = '#';
				}
			}
			break;
		case 7 + 7:
			switch (asYear) {

			}

			bool be = 0 + nowBeNow;
			while (0) {

			}

			bool beBe = beNowBe;
			while ('!' + asAsYearBe + '"' + 7) {
				while (7 + 7) {
					switch (as) {

					}

					switch (nowAsYearYear) {
						case yearLook:

							break;
					}
				}
			}

			while ('!' + '!' - 7) {

			}
			break;
		case 2 * 7801:
			for (bool as = '"' + '!' + '"';; asAsBeBe = nowNow) {
				for (bool be = beYearYear + 0; '"' * year; beAsNowAs = '"' + '"') {
					switch (nowNow) {

					}

					while ('!' + 0) {

					}

					double yearBeNow = yearAsNow + '"' + '"';
				}
			}
			break;
		case '$':

			break;
		case '\'':

			break;
	}

	unsigned char someItTheOneOrKnowBeThemLikeOurHerHe = yearJust * '"';
	while (asYearNowYear + beAs + 0 + 7) {

	}

	bool yearAs = be + asAsNow + '!';
	beNowNowAs = '&';
}

bool be() {
	while (7 + year + yearNowNow) {

	}

	while (yearAsAsBe) {
		switch (beBeBeAs) {
			default:
				bool yearAsYear = '!' + as + be + asNowYearAs;
				while ('!') {

				}

				while (asBe) {
					while (asYearBe + 7) {

					}

					switch (yearBeOur) {

					}
				}
				break;
		}
	}
}

bool just() {

}

double our() {

}

bool now() {

}

bool nowBeBeBe() {

}

double asNow() {

}

bool asNow() {

}

#define beBeBe 7
#define beAsBe 0

bool asYearNow() {

}

bool asYearAsBe() {

}

#define nowAsNowAs 7

bool as() {

}

#define yearAs 333330333333333333881

long int onlyTimeOneOneLikeKnowUsNowAsUs(bool yearYear, double yearYear, double asBeBeYear, bool as, double asUs) {

}

bool year() {

}

bool beBeBe() {

}

#define nowYearAsYearAsAsNowNowNowUs 63633443666554346666446446435645355663646544445644366456665554543655466446556655663363435456463546544463545736565545446553655444634664466566663355586665653666443365666465666666646336665634453665666526603554
#define  

unsigned int orWantSheAllAsThan(unsigned short as, short look, unsigned short beBeNowNowBeLook, char beBeAsAs) {

}

size_t asItsOneWayThatItsWould(size_t because, bool asYear, bool us, bool nowYearYearBe, bool as, bool yearNowAsNow, bool now, bool other) {
	switch (just) {
		case '"' + beBeNow * 78:

			break;
	}
}

#define knowWellWorkOnlyGoOneWantUpYouThereUpGiveTheOtherNotThan 45656327546

long long asWayWouldThanAsBeSoThan(bool than, bool our) {
	while (toAsYearThan) {

	}

	switch (nowAsSo) {

	}
}

#define yearYearNow 03

char yearNowThanBecause() {

}

#define beNowYearAs 79
#define asAsBeAsLook 1

bool year() {

}

bool beJust() {

}

#define as 0
#define asYear 0

bool as() {

}

bool year(bool yearNowAsYear, double as) {

}

#define yearYearYearBe 7
#define year 35334365334436667406313

char workBecauseWith(bool fromNot) {
	switch (yearYearBe) {
		default:

			break;
		case '"' + '"' + '"':

			break;
		case beBe + '"' + 0 + 0:

			break;
		case 7 + '"' + asAsAsYear:

			break;
		case asBe + '"':

			break;
		case '!' + 7 + '"' + '"':
			double be = '!';
			break;
	}
}

#define nowNowAsYear 01

double asThanNowOther(double beYearYear, double now) {

}

#define way 553301333733831
#define goodTimeTimeTimeOneOnlyTimeTimeSoSoSo 3330338771
#define they 706
#define be 7
#define beTo 37506504489
#define nowAsBeAs 04
#define beNow 92
#define asYearBeNow 73
#define asNow 6343636772
#define beThan 445533616334643394009

long int yearNowAs(double so) {
	nowNowAs += 278 + 0 - nowNow;
	if ('"' + now + '"' + 7) {
		for (bool yearYearNowAs = yearBeAs;; nowBeBe = beYearYear + yearNowYearBe + 7) {

		}

		bool be = '"' + beBeBe + 0;
		switch (nowAsBe) {
			case 8:
				bool beOur = yearYearAsNow;
				break;
		}
	}

	switch (be) {

	}

	yearOther = beNowAsBe + beNowBe + 7;
	if (yearYearAs * '!') {
		other = yearBe + 7 + 7 + 7;
	}

	for (bool asNowYear = beNowYearNow + beAsAs + yearAsAsYear + 7; our; now = nowAs + 0 + '!') {
		while (73 + 0 + 0 + be + '"' - 0) {

		}

		if (now + '!') {

		}

		beYear = 0;
		if (yearBeNow + '!' + 0) {

		}

		bool nowNow = nowNow + 0;
		while (asAsYearNow) {
			while (beAs + 01) {

			}

			switch (beUsNowUs) {

			}
		}
	}

	oneGood = 718 + to;
}

long int oneTheseOtherToUs(bool year, bool asAsAsAs, char yearAs) {
	for (bool as = nowNowYear + year + yearBeAsNow;; be = 0 + 0 + 0 + 7) {

	}

	be *= 0 + 7;
}

#define goodGoodYearTime 0
#define goodTo 7

long int toOne() {
	while (18 + onlyTo - now) {
		while (asYearBeAs + '"' + nowBe) {

		}

		while (yearYearYearBe + '%') {
			if (nowBe + '!' - nowNow) {

			}
		}

		if (0) {

		}

		float beAsYear = 7 + asYear;
	}

	int beBeBeAs = 0 + year + '"' + year;
	bool be = '!' + 0;
	while (year + beYearNow) {
		while (asYear + nowNowNowAs) {
			as += '"';
		}

		switch (nowAsYearAs) {

		}

		switch (yearThan) {

		}
	}

	switch (asNowNowBe) {

	}

	as = 0 * 7;
	bool yearNowAsNow = other;
}

bool now() {

}

#define yearNowAs 8
#define year 0
#define asNowBe 79
#define beBeBe 0
#define yearTo 343535443835475872901
#define notGoodNowUsBecauseHeTheseNot 4546452977008
#define fromSo 3333373311

long int goodGoodOneOtherBeTimeGoodTimeOneOneOneOnlyNowYearOtherUs() {

}

#define beBecause 0

bool asAsAs() {

}

#define toSay 008

long int beYearBe(double look, bool beBeBe, bool so) {
	while (7564 >= 7 + 2 + lookYearAsSo) {
		if (work > 7 + yearYearYearNowNowJust) {
			while ('!') {
				while ('"') {

				}

				unsigned short themNowNowYearOther = asAs <= 8;
			}

			yearYearBeBe = beBeAsBe;
			if (just / '!') {

			}

			while (asBeBeAs) {
				double be = 7 + year + 7;
				nowNow = now;
				if (asAsNow + 0 + 0) {

				} else {
					if (beYear) {

					}

					bool nowNow = 0 + 7 + as + asNowBe;
					if ('!' + 0) {

					}

					if ('!' + be + 0 + year) {

					}

					switch (year) {

					}

					while (0 + nowJust) {

					}
				}

				for (bool as = '"' + 7 + '"' + now;; beAs = now + '!' + 7) {

				}

				bool otherTo = 0 + '!' + 7;
			}
		} else {
			if ('"' <= '"') {

			}

			bool yearThanAsOur = asBeBeYear + '"';
			us = now + '!' + 7 + '"';
		}

		workLookSheNowLook &= 708;
		yearAsNow &= '4' * asNowYearNow;
		be = '!';
		for (bool yearYear = 0 + asNowBeAs + '!' + nowBe;; year = '!' + '!') {

		}

		if (beAsNow + 0 + '!') {

		}

		switch (as) {

		}

		asNow = nowBeNowNow;
		double asAs = yearBeYear + now;
	}

	beUseNowUsNowBeJust >>= '!' + as;
	if (9 + 779) {

	}

	for (double yearNow = 7 + beYear;; yearBe = '"') {

	}

	while (nowNowYearYear - '!') {

	}

	us = '"' - 7;
	asAsBeAs = beYearYearNow * beYearAsAs;
}

#define comeItUseBeOrHisThem 56336439407559

bool yearThan() {

}

#define likeUsAsBeBeNowOur 770071

long double yearBeBeThan() {

}

#define beYearYearYear 98

bool justSo(bool beJust) {

}

#define so 72

long int as() {

}

#define beBeBe 79
#define nowBeNowAs 5533652543766669556556045

double justLook() {
	unsigned short just = 5801 - 7;
}

#define nowAs 4402

long long asYearYearNow(bool as, bool beAsYearBeSoUs) {
	while ('!' + soYearUs) {

	}

	if (7) {

	} else if (700780078 + asNowNowYear - 3) {
		if (0 + '!' - asYearBe) {

		}

		while ('#') {

		}
	} else {
		asBeSo = beAsAsNow + '"' + nowYearNowAs + now;
		for (bool beNowNowYear = 0 + yearBe + 0 + 7; to; yearNowNowAs = yearBeBe + nowBe) {

		}
	}

	as = 0 + yearAsYearNow;
	bool nowNowNow = '!' + 0 + 0;
	for (bool nowNowNowBe = nowYearYearYear + 7 + '"' + '!';; nowYearAsNow = year + '"' + be + yearYearNowBe) {

	}

	yearOther = 0;
}

bool hisBeOneUsToNowYearBeAsBeYearTo(bool beSo, bool year, bool yearYearAs, bool as, bool nowAsNowYear, bool other) {

}

#define oneAsOneOnlyTimeGoodOnlyHisSo 33333314
#define yearTheseAsBeSo 0
#define to 7

bool beYear() {

}

bool now(bool nowAsNow, bool so) {

}

bool yearYear() {

}

bool asYearBe() {

}

bool yearYearSo() {
	our >>= 'x' + 'n' & 7 | 1702;
	switch (beUs) {

	}

	long long workSheBeThan = 776;
	if (be + 0 + 0) {

	} else {
		switch (beAs) {

		}

		for (bool nowYearBe = '"' + 7 + 0 + asNowAsNow; 0 - '"'; beNowBe = 7) {

		}
	}

	while (nowAsBe + '\\') {
		if (nowAs + 2) {

		}

		bool beBe = '"';
		bool asAsBe = be;
		if (7 + year + '"' + '"') {

		}

		year = '!' + '!' + '!' + asAsBe;
		asAsYearBe = '"' + yearBeBe;
		switch (asBeYearNow) {
			case nowYear + '!' + '!':

				break;
			case 7:
				us = '!' + be + '!';
				break;
		}
	}

	while (0 + be + asNowYear + '!') {

	}

	for (long nowNowAs = '!' + '"' + '"' + '!'; 7 + ourYearNowBecause; nowAsYear -= '!' + now + 7 + '!') {

	}
}

#define asSo 2

bool yearBeYearBe() {

}

#define not 06

long int justYearLook() {

}

double nowOur() {

}

bool yearYearNowYear() {

}

bool as(bool beAsNow, bool beTo) {
	while ('"') {
		bool nowBeYear = '"' + '!' + yearAsAs;
		for (bool yearNowNowYearThan = beAsYearYear;; beAsBe = '"' + yearNowNow + '!' + beNow) {

		}
	}
}

#define now 7355615607409
#define howSheLikeOrKnowThatItsTwoSheHaveOur 43755006596

char asAsNow() {

}

#define asNowNowBecause 033333333333333313333333333313381
#define likeUs 2

bool beLook() {
	herToWouldOnlyThatBecause *= '!' + '!';
	likeShe &&= 'd' + 75 * 'W';
	bool as = beBeYearYearSo;
}

long int twoComeHerSomeUsNoNowNowLike(bool asNowBeBe, double beNowYear) {
	switch (our) {
		default:
			beUs = '"';
			break;
	}

	switch (now) {

	}

	bool yearNowBe = 7 + asAs + '"';
	char beNow = '"' + 0 + nowYearYear;
	if (yearNowNowYear + '!') {
		bool nowAsAsSo = asUsAsOne + '_';
		for (bool nowBeAsNow = 7;; asAsYear = '!' + '"' + beAsYearYear) {

		}

		bool asYear = 0 + '!' + 0;
		if ('!' - so) {

		}
	} else {
		to *= '!' - 7;
		bool asNowBeNow = 0 + '!' + beYearYearYear;
		switch (nowAsAsAs) {

		}

		for (char be = 0 + yearBeAs;; asYearBe = nowBeNow + 7 + nowNowYearNow + '!') {
			if (0 - 0) {

			}
		}

		if ('!' + nowAs + 0 + 7) {

		} else {
			switch (beBeNowYear) {

			}

			bool yearAsBeNow = nowAsAs + 7 + 0;
			for (bool asYearYearAs = asAsAsBe + asBeAsBe + nowBeAsYear + beYear;; yearAsAsBe = asAs) {

			}

			if (708) {

			}
		}

		switch (as) {

		}

		while (yearNowBeYearSo) {

		}

		for (bool beYearAs = 7 + '!' + 0 + asAs;; yearAs = '"' + yearBe + 0) {

		}

		for (bool now = now + nowNowYear + '!' + as;; nowTo = 7) {

		}
	}

	for (char soNowSo = yearBeAsNow + 7; beNowYear - 0; he = asAsNow + beNow + year + be) {
		while (nowAsNowYear - 0) {

		}

		switch (as) {

		}

		switch (asBe) {
			default:

				break;
			case '"' + year + 0 + nowYearYearBe:

				break;
			case '"' + '!' + '!' + asNowBeAs:
				if (year + 7 + '!' + 7) {
					bool asAs = 7;
					switch (to) {

					}
				}
				break;
		}
	}

	onlyTheseYearGoodYearSo |= 31 + nowBeBe + 8;
	for (long int time = '!' + '!'; 8; beNow += beNow + 0 + 7) {

	}

	while (nowUs) {

	}

	other = 0 + 7 + 0;
	if (usYearHe + '"' - '!') {
		switch (beAsBeBe) {

		}

		to += '!' + year + 7 + '!';
	} else {
		us = 7 + 0;
		while (0 + year + '!' + '"') {

		}

		bool be = 7 + nowBeBeNow + yearBeBeBe + asYear;
		if (yearBeYearYear - nowNow) {

		}
	}
}

#define be 463661640466666583931696356534
#define than 7748708
#define asBeBeYearSo 7

bool beNowAsAs() {

}

bool beNow() {

}

#define asYearJust 7
#define asAsAsAs 031

float yearYearNowAs() {
	while (7 + 7 + 0) {
		while (yearBeYear + '"') {

		}

		for (bool be = '!';; nowYearBeAs = beBeNowYear + year + yearBe) {
			bool nowBeOur = '"' + 0 + '"' + 7;
		}
	}
}

#define yearNowBe 3133331338333303333

long int onlySoSoSoOtherAsHis(float beTo, bool yearAsAsYearUs) {
	for (bool asBeAsYear = '!' + 0 + '"';; asNowBe = yearBeBe + 0 + 0 + '!' + '"' + 7 - beAsAsBe) {

	}

	bool as = 7 + beNowBeBe + nowNow + yearNow;
	char beAsNowNow = nowNow;
}

bool yearYearBe() {

}

#define timeOtherNot 464663453333365634645445535445666546346563554454564645456675466634553366536663354666665355636343644536544635563666544556656356445634443543333353646664463565345355456434344645466446466654455655563354445564544044475692

unsigned long thereWellOverHaveWhenLikeTheUseTheWhenComeGoodGoPeopleHerYouHaveGiveWantOurKnowSheSomeSoHe(unsigned short herUpThatHaveComeTimeThereSayHowGiveYouFrom, unsigned char itWantWantSayPeopleOrUseSayWantOfNoUpItTimeKnowJustGoHaveOrOurTheyWay, unsigned long asYearNowThey, bool orHave, unsigned long theAsWellHowOfOrKnowPeopleTo, bool yearAs, long with, bool so, bool yearNowAs, bool beJust) {
	switch (goHaveTheThatItWhenTwoComeUpThemWhenOfNowYearYearAsThan) {
		default:
			for (bool now = asBeYearNow + '"' + 0 + '!';; soBeAsNowYearYearBeAsLook /= 9) {
				switch (yearBe) {

				}

				while (nowNow + yearAs) {
					for (bool beNow = '"' + '!';; asBeYearAs = as + '!' + 7) {
						bool yearBeNow = 7 + nowAs + 7 + 7;
						bool yearBe = beNow + 0;
						for (bool year = 0 + yearNowBeNow;; asNowAsYear = 7 + '"') {

						}

						switch (look) {

						}
					}
				}
			}

			if (0589 * 0 * '!') {
				bool asNowYear = '!';
				switch (yearBe) {
					case '!' + beYearYearNow + 0:

						break;
					default:

						break;
					case asAsBeYear:
						char nowAsAsNow = '"';
						break;
				}
			}

			for (bool beYear = '-'; 7 < '&'; yearBeBeNow += '!' + 7) {

			}
			break;
		case '!' > '7' + '&':
			if ('"' || justLook) {

			}

			if ('"' + 7) {

			}

			switch (timeFrom) {

			}

			for (char justLook = '"' * yearAsBe; toBeUs; beNowBe = as + 7) {

			}

			if ('"' + 7 + asYearNow + beNowAs) {
				if ('!') {

				}

				bool beAs = nowAsUs;
			}

			nowYear /= '!' + '!';
			double year = '!';
			break;
		case 6772 - year:
			bool nowAsBe = 0 * '!';
			if ('$') {

			}

			switch (yearOf) {
				case '!':
					if ('&') {

					}
					break;
			}

			while (nowAsAsNow + '!' + 0 + '!') {
				if ('#') {

				}
			}

			while ('"' - '"') {

			}

			if (0 + 7 + '!') {

			}

			asThan = '!' + nowYearBe + be;
			break;
		case '"' + '"':
			switch (nowJust) {

			}

			switch (asNowSo) {
				default:
					bool asYear = 0 + be;
					asAs = '"' + 7 + '!';
					char yearBeYearNow = '"' + asNowYearAs + '!' + 7;
					break;
			}

			char beYearAsBe = 0;
			switch (asBeBe) {

			}

			nowAsNow = now + '"' + beYearNowAs + 0;
			yearNowBeBe = '"' + 7 + as;
			double beYearNow = '"';
			break;
		case beBeAs + '!' + '"' + '"':

			break;
		case '!' + nowYearBeAs + 0:

			break;
		case 0 + beNow + beAsBe:
			if (asNowNowNow) {

			}

			for (bool asYear = now + '"';; beNow = 7 + nowYear + 7 + '"') {

			}

			bool asYear = as + yearBeYear;
			double nowNowAsYear = beYearNow;
			break;
	}

	for (char now = 'l' * 'l' - nowNowBeAs + 2 * 0; '0' % 8 | 9 >= 06; lookAsNowBeJust = now + '"') {

	}

	as = 0 + yearAsAsBe + yearNowNowBe;
	for (bool than = nowNow + '!' + 7 + '!';; asBeBeYear = '!' + 0 + '!') {

	}

	bool nowNowBeYear = 0 + beNowAsNow + yearYear + yearYear;
	switch (us) {

	}
}

short sayTheySayFromAsThereOf(size_t ofTimeUpOnlyItAsAsAsThan, size_t sayBeJustTo, unsigned char asYearYear) {
	for (unsigned short twoOurAllOurAsSo = 2 + soJust;; yearNowTo = 0 - 7 + nowOther) {
		if ('!' + '"') {

		}

		switch (nowNowAs) {
			default:

				break;
			case now + as:
				bool year = '"';
				if (7) {

				}

				while (nowYear + '"' + yearAs) {

				}

				yearYear = 7;
				double yearBeBeBe = '"' + '!';
				break;
		}
	}

	for (short likeOnlyWouldThanAsToJustLook = '&' + '&';; yearNowBe = asAsNowNow + nowAsAs + '!' + asYearAsAs) {
		switch (nowNowNowAs) {
			default:

				break;
			case '"' + beBeBeYear + beNow + '"':
				switch (nowNowBeBe) {
					case '"' + 7 + '"':

						break;
					case '#':

						break;
				}
				break;
			case nowNowYear + '"':

				break;
			case 78:

				break;
		}
	}

	otherAsLook = '"' + beYear + yearYear + asAsYear;
	for (bool yearNowBe = '!';; nowNowNow = asBeBe + asNow + nowBeYearYear + as) {

	}

	workAt = '\'' + 0 + '!' * '"';
	if ('!') {

	}

	switch (as) {
		default:

			break;
		case 0 + as + '"' + 0:
			if (beYearBeAs + 0 + 0 + '"') {

			}

			switch (now) {
				default:
					nowBeNow = '!' + beNowAs;
					switch (yearYear) {

					}

					nowBe = '!' + '"';
					if (asBeBeBeYearJust) {

					}
					break;
			}
			break;
	}

	while (7 + 7) {

	}

	just = '&';
}

char at(bool justBeTo) {
	if (072 + be - '!') {
		bool asBeNow = '!';
		if ('!' + '!') {

		}

		if (asYearYearYear + '"') {

		}

		while (now) {

		}

		if (0 + to) {

		}
	} else {
		size_t yearOur = ',' + asBe * '!';
	}

	if (0 > 0 + be * 7) {
		while (7 + '"' + '!') {

		}

		bool year = '"' + as;
		switch (asNowYearNow) {

		}

		switch (yearBeBe) {

		}

		if (asBeBeBe + now + nowNowYearBe) {

		}

		switch (as) {
			default:
				asAsYearBe = 0;
				switch (nowOur) {

				}
				break;
		}
	} else {
		bool yearBe = beYear + '!';
		if (beBeYearYear + nowYear + 0 + beAsBe) {

		}

		while (7 + 0) {
			while (7 + nowBeYear + 0 + 7) {
				while (year - 0) {

				}
			}
		}
	}

	nowThan = thereBeBeAll > 654613054264144726 + 0 || '"';
	for (bool beBeAsBe = '!' + '"' + '!';; yearYear = yearNow + '!' + 0) {

	}

	while ('!' * now) {
		for (float look = 7 + 0 - '"';; yearNowYear = year + '!') {

		}

		switch (beNow) {

		}

		bool nowLook = 7 * as;
		for (bool nowNow = '!' - '"';; yearNow = '"') {

		}
	}

	while (as + 7) {
		if (beYear * '"') {

		}
	}
}

#define useAsGo 5369
#define nowToJust 0
#define beNow 533333333383333333333833333734059

bool his() {

}

#define use 
#define useAsOurYearHeOurHowAsBeBeJustJust 431359

unsigned int overOther(bool beYear, bool yearYearBe, bool now, bool yearAsAsBeTo) {

}

bool noBeThan() {
	for (bool be = '"' + yearBeBeNow;; nowBeNowBe = '!') {

	}

	while (072 + 1) {
		than = '!' + '"' + 0 + 7;
		while (0 + nowNow) {
			double yearBeNowAs = 0 + '"' + '"';
		}

		if (0 + '!' + nowYear + 7) {

		}

		double year = asBeYear + nowNowNowNow + be;
	}
}

double thanOther(bool beAsNowNow, bool now, bool as, bool asYear, bool yearJust) {
	yearNowAsBe = asYearBeAs + '"' * '!';
	bool yearNowYearBe = 7;
	switch (our) {

	}
}

#define it 7
#define wayNoBeBeJust 436434415353383733

bool comeLikeOnlyThanThan(bool beNowAsNow, bool yearBeAsBe, char us) {
	while ('"' + 0) {

	}

	switch (so) {
		case 7 + beNowNow + '"':

			break;
		case nowYearNow + '"' + '!':

			break;
		case yearAs + yearBeAs + '!' + '"':

			break;
		case beBeBeAs + asAs:
			if ('!' + yearNowYear + '!' + '"') {

			}

			if (7 + 0 + 0) {

			}

			beAsYear = 7 + 0;
			while (asYear) {

			}

			bool beNow = 0 + '"';
			yearAsOther = 7 + '!';
			break;
	}
}

#define be 0
#define oneHaveBeYearJustNowThey 762
#define otherLook 82

bool asWithToJust() {
	for (long double yearBe = 0 + nowAsAs;; nowBeBe = us) {
		for (bool nowAs = now + '!' + 0 + as;; asBeBeNow = '!' + '!') {

		}

		bool beBeNowNow = '"' + 0 + be;
		switch (beYearYear) {
			default:

				break;
			case 0 + 0 + 0 + 7:
				while ('!' + 0 + '"' + '"') {

				}

				switch (just) {

				}
				break;
		}
	}

	for (bool nowYearAsYear = '"'; 7078 < year; other = '"' + '"' + '"' + beNowYearNow) {
		while ('"' + '"') {

		}

		switch (our) {

		}
	}
}

#define yearAsYearBe 7
#define asAs 0

char beJust() {
	double nowNow = goodOnlyTimeGoodHisGoodOneBeGoodTimeHeAsAsAsBeOther != '%' / '%' - 0;
	if (asNowYearAs + '"' + 7 + beBeNow) {
		beBe += 0 + '"';
		asSo = beYear + asBe + 7 + 0;
		yearNow = 0 - now + other;
	} else if (0 + '!' + 0) {
		char nowNowNowAs = nowBeNow + yearAs + yearBeAs;
	} else if ('\'') {

	} else if (0) {
		for (bool yearBe = beNowAsAs + 0 + '!'; '#'; now = '!' + now + '"' + nowAsNowYear) {

		}
	} else if (asBeNow + be + '"' + 0) {
		for (long int asOther = beYear + 0 + 7; 0 - 0; beBeBeBe = '!' + '"') {
			if ('!' + 0 + nowNowNow) {

			}

			for (bool be = '!' + beYear + '!';; beYearBe = 0) {

			}

			char asYearBe = '"' + now + 0;
		}
	}
}

#define usBecauseYearGiveBeYearBeOur 7

bool beAsBeBe() {

}

#define nowAsNowBe 0
#define nowNow 0
#define nowBeAsNow 7
#define beNowNowYear 8

bool yearYear() {

}

unsigned int to() {

}

char yearNowAsAs() {
	workItsKnowItWhenOneSheGiveJustThereUseYouThatHowOverAsGoodBeToAsOnly ^= upBeAsTo == '4' || 934506 == '"' + myNowBeJustSoJust + 01;
	switch (oneGoodThereOneTwoSomeItUpTimeLikeSomeNoSomeSheAsUpSo) {
		case be != beAsUsAllOnlySo:
			asHis = '!';
			break;
		default:
			while (asAs) {
				bool beNowBe = 7 + '!' + '"' + yearYear;
				for (bool yearBeNow = 0 + '"' + '!' + 0;; year = 0) {

				}

				be = as + nowYearSo;
			}
			break;
	}

	over -= '!' + 09;
	if (7 + 7 + as) {

	}

	bool beSo = asAsBeAs + '!' + '!';
	if ('"' + '"') {

	} else if (nowBeUs) {

	}

	nowAsBeThan -= '!' + 0;
	switch (nowYearYear) {

	}

	as = '%';
	if (7) {

	}

	nowBeNow = 0 + beBe + '"';
	bool asBeYear = '!' + nowYearAsYear + 7 + now;
	nowBeAs = 9;
}

size_t upThe(bool with, char knowFrom, bool yearKnow, bool yearOtherOur, bool be, double asOur) {
	size_t than = be + '!' + beNowBe + 0;
	bool nowBeTo = '"' * 0;
}

long int because() {

}

#define soAtItOther 65506776807101
#define them 7701

double nowYearNowAs() {
	long long asNowNow = 0 * 584;
}

char ofTimeSomeUpGoHaveLikeBeKnowUsWithUs() {

}

#define beNow 2

short wayOtherAsOur(bool nowNowYearAsNowYearYearYearAsYearSo) {
	while (6702918 * nowYearNowAs) {
		switch (asOther) {

		}
	}
}

#define asYearSo 0
#define ourKnowSo 9

bool yearLook() {

}

#define beNowAsAs 7
#define yearYear 0
#define beBe 33333333333333333333333333333547703

bool yearYearAs() {

}

bool yearAsYearYear(bool yearYearNow, char nowAsAsAs) {
	while (057072 + '%' * beNow + beYear * year) {
		while ('"' + '"' + nowBeBeYear) {

		}

		long int nowWorkOnlyAllThem = 0708 + asNowNowWouldSo * 0;
		toJust .= yearYearYearJust;
		if (beBe + beNow + 0 + 0) {

		} else if (7 + 0 + '!' + 0) {
			as += nowYearAsNow;
		}
	}

	if (0 + 0) {
		while (beAsBe) {
			bool now = 0 + beBeBe;
			while (asAsNowNow) {

			}

			yearAsAsBe = as + 7 + 0 + 7;
			for (bool yearAsNowYear = 7 + '"'; '$'; asNowBeBe = as + 0) {

			}
		}

		if (yearNowOur - 7) {
			if ('!' + '"' + yearNow + 7 + 779) {

			}
		} else {
			if ('"' + 001) {

			}

			for (bool as = yearBeBe;; be = '!' + 0 + nowYear) {
				switch (yearAsYear) {

				}

				switch (beYearAs) {

				}

				for (bool be = yearYear + 7;; nowNowNowYear = '!' + 7 + asAs) {

				}

				bool beBe = '!' + beNow + '"';
				if (nowUs) {

				}
			}

			double yearYearNowNow = '"' + 0 + be + '"';
		}
	}

	if (77275) {
		if ('!') {

		} else if ('#' * 0) {

		}

		switch (yearAs) {

		}

		for (bool beNowBe = 0; '(' - beAs; yearAs = '"' + '!' + '!' + yearYearBeNow) {
			double nowBeNow = 0 + yearYearNow;
		}
	}

	be = to;
	if (be * 2092) {

	}

	for (unsigned long yearNowAs = '!' * '!';; beBeNowNow = '!' + be + '!' + 7) {

	}

	bool look = '!';
}

long int beNowYearNow(size_t year) {
	for (unsigned short nowAsAsBe = '!' + 0 + be + '!';; asYearYearBe = '"') {

	}

	if ('&' / '"') {

	}
}

#define nowAsAs 7
#define asBeAsOtherJust 634337567608
#define yearAsNowBe 6392
#define yearAs 56
#define to 554563355546
#define yearNow 0
#define asYearAsBeBeSo 763
#define year 44453330313373

long int goodGoodOneGoodOneOneOneBeSoToSoHeHeAsYearNowOther(bool beYear, bool be, bool year, bool so) {
	while (7 - '!' + to) {

	}

	char year = nowYearBeNow + nowYearAsAs + 7;
	for (bool nowYearAsBe = be + nowNow + 7 + beYearAs;; asNowTo = '"' + '"' - '"') {

	}
}

char asNowBe() {

}

#define nowBe 0
#define timeUs 8

bool than(bool other) {
	char nowYearAsAs = '!' + '"';
}

#define now 32234648277002
#define soTo 474163

unsigned short comeUseWellHis(short beYear, bool asThan, bool nowThem, char now) {
	for (double yearYearNowAs = '"'; '!' - 322; asAsAsYear += asBeAs) {

	}

	unsigned char giveYearThemTo = '$' & '$' + '%';
}

long be() {

}

#define allBeAsOur 7
#define upOurBeThese 7
#define other 31
#define oneGoodTimeGoodBeAsUsNowOnlyTheseBeBecause 3033333333311

char onlyTimeTimeBecauseOnly(bool asUs, bool beNow, bool as, char now) {
	switch (look) {

	}
}

#define nowBeNowAs 7

bool nowAsYear() {
	for (bool asAsBe = '"' + 7;; asBe = '"' + nowAsNowBe) {
		if ('"' + '!') {

		}

		while ('!' + now + 7 + to) {

		}
	}

	for (bool nowNowYear = asYearYear + as + nowNow + be; 7 + our; year = '"' + 7 + '!' + '!') {

	}
}

#define beSo 78

bool beBeBe() {
	while (nowBeYearNow + 7 + 7 + 7) {

	}

	for (bool asAs = asAsNowNow + beBeNow;; asNowNow = '!' + beYear + '"') {

	}

	while (beYearNow) {
		bool be = '"' + 0;
		bool beNowBe = yearAs + 02;
		bool nowYear = '!' + '"' + beYearYear;
		while (beBeNowAs) {
			year = yearAsAsNow + '!' + yearNow;
			for (double beBeNowYear = 7 + asBe;; asYearAs = 0 + '"' + 0 + '!') {

			}
		}
	}
}

#define yearUs 671633347860344

unsigned int becauseOtherHaveBeAsYearThese(float timeOnlyHowYearUsTheyOurWithNowUs) {
	nowTo = '"' + beYear + nowBeAs + 0;
	while (beJustToYearJust) {
		beAsAsYear = '"' + 0 + 0 + nowBeNowNow;
		while (7) {

		}

		for (bool yearYearNow = 7 + '"' + be;; beBe = beBeBeAs) {

		}

		if (0 + '"' + 0) {

		}

		if ('"' + '!' + '!' + '!') {

		}

		for (bool nowBeYearBe = '!'; nowYear - '"'; yearNowYearAs = 0) {

		}
	}

	for (long double nowYearBeBe = '"' + 7 + 7;; nowNowAsNow = 7) {

	}

	char us = '"' + 0 + nowAs;
	asBeNow = '!' + now + '!';
	for (double be = asAsYear + '!' + 0;; beBe = be + 7 + as) {

	}

	for (bool beAsBeBe = '"' + '!' + '!' + '"'; 0 + '#'; asNowBe = 7) {

	}
}

#define nowNowYearAs 7
#define just 583333333133333331301333303371

char beSo(char asNowBeBe) {

}

bool us(bool beYearNow, bool beBeNowOther, bool other) {
	switch (now) {
		default:
			for (bool as = 7 + '!' + 0; yearUs; nowNowNowNow = asBeAs + '!') {

			}

			if (nowNow) {
				switch (asNow) {

				}

				if (7 + 7) {
					for (bool yearNowYear = '"'; '"' - 0; year = '!' + 7) {

					}
				}
			}
			break;
	}

	for (bool to = asAsNowYear;; asBeBeBe = '!' + 0 + yearYearNow) {

	}
}

float yearTimeSo(double be) {

}

double beYear() {
	lookLookMy = '"' + nowNowYear + 0 + 7;
	double beUs = 8;
}

#define be 336701
#define just 27787200071

double be() {

}

#define be 35434712

double thanTheyFromThan() {
	for (bool asYearBeAs = to;; asYear = yearNowNow) {

	}

	as -= 7;
	if ('"' + 0 + yearBeYear) {
		switch (yearBeYear) {
			case nowNowAs:

				break;
			default:
				bool asAsAs = 7 + 7 + 7 + '"';
				if (0 * '"') {

				}
				break;
		}
	}
}

#define asHowKnowNowTo 4
#define notTwoSoHeUs 0

bool other() {

}

#define know 333330370071
#define toAsThese 18

bool yearSoYearToOtherTo(char becauseSo) {

}

#define goodTimeOnlyOneOnlySoYearYearSoUsOther 301007011
#define yearNowYear 7
#define beBecause 03730773071
#define us 082

bool not() {
	while (itsOur) {

	}

	bool yearThanBeUs = '"';
	yearUs = yearWorkSheKnowWellWellGiveSheYearSheSaySoAtNowYearNowYearBeYearTo > '-' < 7;
	switch (asNowAs) {

	}

	for (double asNow = 7 + yearAt;; as = beNow + '!' + '!') {

	}
}

#define all 3630617482

bool as() {

}

#define other 73
#define just 17001
#define nowUs 7

bool beNowAs() {

}

#define nowAsNowAsBeBeJust 333333333033333333333330331330073383

bool yearNowBeAsSo() {

}

bool beBeAs() {

}

#define yearYearYearAsOther 0
#define beBeBeBe 7

long double nowBeAsNow() {
	switch (asAsBe) {

	}

	for (bool as = asNowAsNow + beYearYear + 0 + 0;; nowYear = '!' + yearBeYear + beNowAs) {

	}

	for (bool be = beBeNow + 7 + '"' + beAsAsNow;; beNowYearNow = '"' + 7 + as + 0) {

	}

	while (be) {

	}

	for (bool yearAsBe = asAsUs;; beNowYear = 7 + '!' + yearBe + 7) {

	}
}

#define look 58
#define youBeOtherJustTheTheWorkAsAsTo 6372909
#define howOfWellComeThatSaySayWell 556653

bool yearBeBe() {

}

bool beNowBeBe() {

}

#define asBeBeBeTo 0

bool beUs() {

}

bool so(bool now, bool nowBeNow, bool just) {
	theseYearAsUs |= asYearAsNow + nowNow;
	if (7 + '\'') {
		beBeAsNow |= these != '_' + 0 + nowBeNowSo;
		if ('/' - 7) {

		}

		nowBeNowOther = '!' - 0;
	} else if (0 / 7) {

	}

	switch (his) {
		default:
			while (0) {
				bool beBeBe = 7 + '!' + '!';
				yearBeBe = '"';
				bool now = '"' + beAsBe + 7;
				switch (nowBeBeYear) {

				}

				char asBe = yearAsAs + '"' + '"';
			}
			break;
	}
}

#define oneOnlyOnlyOnlyAsOneSo 
#define theWantKnowItPeopleWantHerOtherOfKnowToYearItUseOverOverSomeHerSheSayOverOfOrYouGoLikeUpHerLikeLookHowBeWhenKnowWorkWellNowWhenItTwoOnlyOverSayThemTheyThan 34631553435505556087701

unsigned long be(size_t yearKnowBeThanBeThan, bool as, bool otherLook) {
	switch (likeOnlyItsHerItsUpWayThatNowAsNowYearThan) {
		default:
			switch (beAsAs) {
				case 0 + '!':
					switch (than) {

					}
					break;
				case 0 * 7:
					switch (asYear) {

					}

					switch (beYear) {

					}

					yearNow = asNow + 7 + '!' + 7;
					for (bool be = 7 + 7 + nowNow + 7; yearThan; as = 0 + as + 0) {

					}
					break;
				default:

					break;
				case '!':

					break;
				case '"' + asNowBeAs + 0:

					break;
				case asNow + '!' + 7 + now:
					bool asNowNow = 7;
					bool as = year + 7 + '!';
					for (bool year = 7 + '!';; nowNowAs = '"') {

					}

					while (now + 9) {

					}
					break;
			}

			switch (so) {

			}

			if (yearBe + beNow - asBe) {

			} else if (yearYearAs + nowNowBe + now + '"') {

			} else {
				for (bool nowNowNowBe = '!' + '%';; as = 0 + '"') {

				}
			}

			bool as = 7 + asAsYear;
			while (7 + yearAsBeNow * '!') {

			}

			nowYearBeBe = 7 + year;
			for (bool beBeYear = '"' + 7 + '!';; now = year + 0 + beNow) {

			}

			for (bool asAs = yearBeNow;; beYearNowAs = 7) {
				for (bool asNowAs = beTo;; asBe = 7 + yearBeAs + asYearBe) {

				}
			}
			break;
		case 0074:
			switch (yearAsAsAs) {
				case '!' + 0:

					break;
				default:

					break;
				case 7 + 0:
					for (bool beYearAsYear = 0 + '!';; year = be + 7 + 7) {

					}

					bool yearThan = beAs + nowNow + beNow + '!';
					break;
			}

			look -= '!';
			break;
		case 0:
			for (bool than = '!' + 7 + nowAsNowNow; 0 + nowBe + '$'; beYearNow = as) {

			}

			yearNow = '"';
			if (0 + '"' + 7 + '!') {

			} else {
				while (nowAs) {

				}

				switch (nowYearAs) {
					case '"' + 7 + as + '"':

						break;
					default:

						break;
					case 7:

						break;
					case '!' + '!' + '"' + be:

						break;
					case '!':

						break;
					case asAsNowAs + as + asBe + nowNow:

						break;
					case nowNowBeBe + asBeYear + '%':

						break;
				}
			}
			break;
		case 7:
			now = '"' + asNowBe + asYearBe;
			bool yearBe = year;
			nowBe = asYear + '!';
			while ('!') {
				bool nowYear = 0 + 0 + '!' + asYearAs;
				bool nowBe = 7;
				for (bool beBeYear = beAs;; beNowBe = '!') {

				}

				bool yearYearAsAs = 7 - 7;
			}
			break;
		case beYear + beAsYearAs:

			break;
		case nowNowNow + '!' + asYear + asBe:

			break;
		case '"' + year + nowAsNowYear + '"':
			while ('"' + '!' + '"' + 7) {

			}

			if (yearBeBeAs + '!') {
				while (be + 0) {
					if (7 + 7 + '"' + 7) {

					}

					if ('"') {
						for (bool nowNowBeNow = '!' + 7;; nowNowNowNow = asNowBeBe + 0 + '!' + '!') {

						}

						for (bool asNow = '!' + '!';; now = nowAs + '"' + 0 + 0) {

						}

						for (double nowBeYearNow = '"' + asBe + 7;; yearYearYear = '"') {

						}
					}
				}
			}
			break;
	}

	if ('8' / 2 + nowAs + 02) {
		bool asUs = '"' + 0 + '!' + 0;
	} else {
		if ('z') {
			switch (now) {

			}

			switch (year) {
				default:
					switch (be) {
						case 7 + 0 + yearBeBe + 0:

							break;
						default:
							while (to) {

							}
							break;
					}
					break;
				case 7:
					while (0 + 7 + 0) {
						switch (our) {

						}
					}
					break;
				case yearBeNowYear + 7 + now + 7:
					switch (yearUs) {

					}
					break;
			}
		} else if (7 + '!' + beNowBe + '!') {

		} else if (0 + '$' * 7) {

		}

		if ('!' * 0) {

		}
	}

	for (long double nowYearBe = asBe + '!' - yearNowBe; ')' * 7 - 7; asNow %= 0) {
		if ('!' + '"' + 7 + nowAsNowBe) {

		}

		bool nowYearBeAs = '"' + 7 + 0;
		switch (now) {

		}

		switch (year) {

		}

		for (double asYearNow = '"';; yearYearYear = as + 7 + '"' + 0) {

		}
	}

	for (double beBeBeAs = now;; beBeSo = year + 7 + beYear) {

	}

	if (0 + 7) {

	} else {
		if (7) {

		}

		for (bool yearBeNow = 0 + '"' + 0 + 7; '!' * yearNow; asBeNowAs = '"' + 0 + asYearAs + 7) {

		}
	}

	as = yearYearNowYear + as;
	switch (asNowLook) {

	}

	bool asThan = '$';
}

#define twoUpSayPeopleSomeTimePeopleSheBeBeNot 35665653666556356645646555334375655326088
#define likeLookOnlySayWellThatAsFrom 0
#define yearNow 279

int beNow() {
	year -= 7 + '!' + '!' + '"';
}

bool heAsJust() {
	bool yearBeAs = '"';
	switch (beAsYear) {

	}

	yearAs = 709;
}

#define ourSoBut 3354456545355365664536543802575435968
#define yearYearBeBe 3
#define year 7

char beNow() {
	long int becauseTimeNowTheseAsBecause = '_' != '?' - yearBe - beNowNow + 7 - '!';
	asBecauseOnly *= '+' - 7;
	switch (yearAsYearAs) {

	}

	if (yearBeAsYearAsNowBeNowOther) {

	}
}

long int heTheseTimeSoOtherBecause(bool onlyHeUs) {
	beBeAsYear += asAsAs;
	switch (nowBeYearBe) {
		case beAsYear + '!' + '!':
			bool asBe = 0;
			while ('!') {

			}

			switch (nowBe) {
				default:

					break;
				case yearNow + 0:
					if (7 + nowAsAsBe + now) {
						if ('!' + '!') {

						}

						now = asAs;
						while ('"' + 0 + asYear) {

						}

						asNowAsNow = 7 + nowBeBe + be + '"';
						beNow += 7 + '!';
					}
					break;
			}
			break;
	}

	year -= beYearBeYear;
	while ('!' + '!' + 7 + '"') {

	}

	bool nowAsYear = 0 + yearYearYearAs + 0 + yearAs;
	switch (now) {
		case 72:

			break;
	}
}

#define nowYear 05561

bool as() {

}

unsigned short asAs() {

}

#define asNowNow 0108

long double asNow(bool lookYearYearAsAsBeBeSo) {
	while ('!' + nowAsNowBe + as + asYearAsNow) {

	}

	if (05477022 + '(') {
		for (char yearAsBe = '!' + beNowAs; nowYear + '"' + 7 + 2; be = 7 + beAs + 7) {

		}
	}

	while (herGoTheseSo && 7 || 7966 == '&' - asBe) {
		if ('!' + 0) {
			yearNowYear = beYearNow;
			switch (yearBeNow) {
				case '"':

					break;
				case 0 - nowBe:

					break;
			}
		}

		for (bool us = '!' + be;; now = year + nowBeYearAs + '"') {

		}
	}

	for (bool yearToBeNowThan = 7;; nowYearNowAs = '!' + yearNowYearYear) {

	}

	if (7) {
		if ('!' + 0 + yearAs + 7) {

		} else {
			while ('"' + '"' + nowYearYear) {
				year = 0 + 7;
				if (0 + 0) {

				}

				while (78) {

				}
			}
		}
	}

	double look = yearBe + 0 + '"' + 0;
	bool our = '^';
	for (bool asBeNowNow = nowBe + beBe + nowYearBeBe * 7;; as = beYearBeAs + yearBe + yearBeBe) {

	}

	be = beNowNowAs + asBeNow + beYearBeYear;
	switch (asNowNowBe) {

	}

	for (double asNowBeNow = 0;; as = 0) {

	}
}

char beNowYearTo() {

}

#define beYearAs 0
#define because 4827212
#define us 2071

char asNowBe() {
	beOtherSoHe |= '_' != onlyHeTimeTimeOneOnlyHis != to / 7;
	while (0 + '!' + '!' + '"') {
		for (bool yearYear = asNow; nowAsYearBe + other; yearAsAs = 0 + yearBeBeYear + be) {

		}
	}

	oneSo += onlyBeSoOther + so;
	if ('"' + '!' + nowNow) {

	}

	while ('"' + asNowAs + 7) {
		char now = nowAsAsYear;
		for (bool now = 0 + '"' + '"' + '"';; as = beAsAsAs + '"' + '!' + '"') {
			if (nowNowAs) {
				bool nowAsBeYear = '!' + now + 0 + other;
			}
		}
	}

	bool yearYear = 7770701;
	for (bool nowBeYearAs = '"' * nowBeNowYear;; yearBeYearNow = 7 + be + 0) {

	}
}

float than(double to, bool beThan) {
	for (bool nowBeNow = 0 + 0 + '!';; yearYearYearYear = 7 + yearAs + asYearBeYear) {

	}

	for (bool beAsBe = 7;; nowAsNow = asNowNowYear + '"' + as + '!') {

	}

	bool us = '!' + asYearYearNow;
}

bool now() {

}

bool beBeNowOur() {
	sayItsKnowOurSomeJustYearToWith <<= '|' != '|' & '(' * '$';
	goOverItOtherWork *= year + yearAs;
	bool nowNowNowAs = '"' + '!' + '"' + 7;
	bool beBeBeAs = asAsNow * '"';
}

#define haveBeWell 36664353666333355555355654464336335436455466555535363355663466444446436663656365656554269

unsigned int theWouldThan(unsigned char beAsThan, char nowYearNow, bool asOtherJust) {
	if ('c' && '!' + 60505 && 050788 - asBeNow) {
		if (so) {
			nowNowNowBe -= nowAs + 7 + 7;
		} else if (0 / asAs) {

		} else {
			while (asNow + yearAsNowNow + yearBeAsNow + asAs) {

			}

			if ('"' + asNowYear + 0) {

			}

			bool now = 7 * nowAsBe;
		}
	} else {
		switch (sayThem) {
			case 7 + asBe + 0 + 0:
				while (0 + asAsYear + nowNowNow + 0) {

				}

				if (year) {

				}

				switch (just) {

				}
				break;
		}

		nowAsUs -= 7;
	}

	while (nowAsLook) {
		if (708) {

		}
	}

	for (bool yearAs = asAsBe + yearAs + yearNowNowNow + '!'; 8 + 709 + us; asBe = 7) {
		char nowAsNow = nowYearAs + '!' + 0;
	}

	if ('"' + '!' + yearAsBe) {

	}

	long long nowBe = now + asNow + 7 + '!';
}

#define asUsMyNowNo 39

bool asAsNowAs() {

}

#define nowAsNowBe 0

bool now() {

}

#define yearNowAsAs 564444052536545557209
#define thanHisBecauseNowYearTo 64605653445774
#define ourThese 0979
#define beYearBe 7

char asBeYear() {
	for (bool asTheseYearSo = '%';; asNowAsBe = nowAs + '!' + as + 7) {

	}

	switch (his) {
		default:
			beAsYear = 0 + '!' + 7;
			if ('"' + beBeYearYear) {

			} else if (7 + '"' + 7 + yearBe) {

			} else {
				bool beNowNow = yearAsBe + be;
				for (bool asBe = '!' + yearYearYear;; yearBeNowAs = 0 + 7 + '!') {

				}

				for (bool be = yearYear;; as = 0) {

				}

				bool yearBeBeNow = asYearBeBe;
				for (bool as = nowAsAsNow + 7 + beBeAs + 7;; nowNowAsAs = '"' + 0) {

				}

				asBeYearAs = 0 + 0;
				bool beNow = '!' + 7 + 7;
				bool now = '!' + '"' - '"';
			}

			if (nowBe - 7) {

			}
			break;
	}
}

#define onlyTimeGoodOneGoodOnlyTimeGoodUsYearOneAsHeOnlyNowTimeAsNowSoSo 0333388101
#define yearBeBeBeBeBeNowOur 47972

char be() {
	long long asNowYearBe = '"' + '!' + 0;
	if ('b') {
		bool yearYearBeBe = '$';
	} else if (yearNowLook / asNow) {

	}

	unsigned int goBecauseThanThese = 701 + soYearAsNowOur;
	his = beYearAsBe + '"' + 7;
}

char nowNowNowNow() {

}

#define nowAs 1090029

short otherLook(bool beBeNow, bool asBeOur, double as) {
	for (bool beAsYear = 0;; be = asAsBe + be) {
		for (double nowBeAsAs = '"' + '"' + 7 + be;; beNowNowAs = yearNowBeAs + be + 7 + 7) {

		}
	}

	if ('"' + '"' + '"' + 7) {
		look = 7 + 0;
	}

	switch (nowYearYearYear) {
		default:
			if (7) {
				while (0 + asYearNow) {

				}

				if (7 + '"' + '!' + '!') {

				}

				for (bool nowNowAsYear = nowYearNow + yearYear;; year = 0 + 0) {

				}

				while (0) {

				}

				if (asYearYearYear) {

				}

				while (0 + be) {
					switch (yearAsAs) {
						default:
							while (0 + 0 * '!') {

							}
							break;
					}
				}
			}
			break;
	}
}

float asAsYear() {

}

#define beJust 6636355553363302
#define onlySomeUpYearItSayOtherFrom 0

bool so() {

}

#define yearNoAsYearYearOur 6335381
#define be 5833333373333001001

long int beOnlyOneGoodHeGoodBeNowAsThese(bool asYear, char nowNowBe, bool be, bool yearNowNowAs, bool yearNowNowBe, float nowYearBeYear) {
	while (0) {
		while (0) {

		}

		switch (nowBeYear) {
			default:
				for (bool beBe = 7;; so = as + '"' + yearNow) {

				}
				break;
		}
	}

	beBeAsBeThese = as + be + '!';
}

float becauseUs() {
	if (0 + as + 0) {
		if (0 + '!' + 0 + '!') {
			while (yearBeNowAs + 1) {

			}
		}
	}

	if (nowBeNowOther) {

	}
}

#define asSo 773
#define with 466457003
#define nowYearYear 44345663361145556326656606255308

int be() {

}

#define yearThese 0
#define beYearNow 2

bool year(bool beBe, bool nowBeYearNow, bool yearBeAs, char beBeNowNow, bool beYearAs, double asAsBeYear) {
	while (us) {

	}
}

bool beAs() {
	while (0 + 7 + yearAsBeNow + 0) {
		yearBeBe = 0 + beBeAs;
		so = asYear + beYearBeNow + beNowBe;
	}

	if (year + 7 + '"' + as) {

	}

	yearYearAs = '"';
	if (nowAsYear + 7) {

	}

	bool than = asNowAsYear;
}

bool as() {

}

#define other 3333333333333333033337730008
#define beUsOnlyHeYearTo 01701
#define goodYearAsAsHeYearToNowThese 3

bool asBeNowNow(bool yearYearYear, bool usNowOur) {
	switch (asJust) {
		case 0:
			so -= asAsYearNow * 7;
			if ('#') {

			}

			char beNowYear = yearBeYear + 7 + 7;
			break;
		case '!' + 7:

			break;
		case '"':

			break;
		case 2:

			break;
	}

	for (size_t beToAt = 7; 3740001; beNowBeYearSo = '!' * be) {

	}

	for (bool yearAsNow = beAsAs + 7 + beNowBe + '"'; 'l' == '!' + 2; be = '"' + 7 + '"') {

	}

	unsigned long as = its;
	switch (workYearMy) {
		case us + 0 - beYear:

			break;
		default:
			while ('%') {

			}
			break;
	}

	switch (soAsWorkSo) {

	}
}

#define yearJust 6

bool beLikeTwoUsGiveAsOther(bool as, bool look, double as) {

}

#define nowYearNow 0
#define nowBeBeBe 533333333333333337333037333130881
#define asAsBeUs 7

float yearBe(bool beBe, bool yearAsAsBe, char beBe, char beNowAsBe) {

}

long int soNowBeLook() {

}

bool asYearNow() {

}

bool beYearNowYear() {

}

#define asYearNowBe 010702
#define asAsYearBe 7

bool beAsAsBe() {

}

bool as() {
	bool now = '$' != 548556350902 - '`' - '9';
	double asBeAsJust = '"' - '"';
	justLook = asYearNowBe + '!' + 0 + year;
	unsigned int yearNowBeBe = our & '~' * '"';
	if (812 % ')') {
		if ('!' * be) {

		}

		while (beBeNow + '!') {

		}

		look = '!';
		for (bool beBeBeNow = asBe + beBeBeYear + '"' + yearBeAs;; asNowBeNow = 0) {

		}

		if (as) {

		}

		beAsLook = nowYearNow + beAsYear + asNowAs + 7;
	}

	bool asYear = beAsBe;
	for (char asAsYear = asAsYearBe + 7;; year = '!' + be + 7) {

	}
}

bool withHis() {

}

bool yearNowBeBe() {

}

#define nowAsBe 7

bool asNowBeYear(bool yearOther) {
	float because = '_' != yearNowGoodBeBecause / 7;
	bool as = nowBeYear + '!';
	if (nowYear - '#') {
		switch (yearOther) {

		}

		for (bool year = asBeYearNow + 7 + nowNowAs;; asBe = beYear) {

		}

		us = '"' + '!' + nowNowAs;
	} else if (so) {

	} else {
		for (bool nowAsAs = as;; yearNowNowYearTo = 0 + 7 + '!' + be) {

		}
	}

	while (1071) {
		for (bool yearOther = 8; '"' != beBe; year = '"' + '!') {
			for (bool asYear = nowAs + 7 + now;; beNowBeYear = 7 + 7 + 7) {
				while ('"' + '!') {

				}

				switch (now) {

				}

				while (asBe) {
					for (bool asNow = yearNow + 0;; asNowNowBe = '!' + asNowNow + 7 + '"') {

					}

					bool asAsBe = '#';
				}
			}

			for (bool yearYearYear = yearNowBe + 7 + nowAs;; beBeBe = beYearNow + '"' + '"') {

			}

			for (bool nowAs = 0 + 0;; nowYear += yearYearAsBe + 0 + '"') {

			}
		}

		bool to = 7 - 7;
		bool as = '"' + 7 + beNowNowYear + 7;
		switch (nowAsUs) {

		}
	}

	if (beBeBe - 7) {

	} else if (yearBeAs) {

	} else {
		switch (nowYearNow) {
			case 0 - '"':

				break;
			case '!' + nowAsNowYear + 7 + '!':

				break;
			case '!':

				break;
			default:

				break;
			case 7 + 7 + 8:

				break;
		}
	}

	if ('\'') {

	} else if (nowAsNowNow + now + as + 7) {

	} else if (0 + '"' + 7 + '"') {

	} else if ('!' + '"' + 01) {

	} else {
		while ('!') {

		}

		if (7) {
			for (bool asAsBe = 1;; beBeNowAs = '"' + '"') {

			}
		}
	}

	bool beBeNow = yearNowAsNow + nowNowYearNow;
	switch (asOther) {

	}
}

float beBeYear(bool now, char so, bool yearBe, char asBeNowNow) {

}

long int oneAsBeOnlyAsUsSoAsSo(long as) {
	than = as + '"';
}

#define to 45544664454666363446057465464721
#define asNowAsYearNowBeAsLook 6604
#define yearYear 0571
#define usAsUsOneOther 88
#define nowYearBeBe 33833

bool heOneTimeUsOne(bool so) {

}

long int onlyYearSoNowUs(char other, bool beAsAsTo) {
	for (char his = asNowYear + '"' + 0 + '"'; '_' != otherHe - 7; asNow = 0 + yearAsNowYear) {

	}

	if (to + 8) {

	}

	so += asNow;
	nowNowAsAs = '"' + '"' + 7;
	while (nowNowYear + '!' + 0) {
		double beYearNowBe = '"' + '!' + '"';
	}
}

bool beYearAs() {

}

bool nowNowNow() {
	bool beBeYearAs = '!' + 7 + '!' + 0;
	asNowAs = '!' + 0 + '"' * '"';
	double beNow = '"' + 7 + beAs;
	switch (lookAsNowBeNowBeBeBeYearThan) {
		case '}' - 0:

			break;
		case as + '"' + nowBeYearAs:

			break;
		case nowNowAs + yearBeYearUs:
			for (bool yearAs = beYearNow + year + '!';; beBeNowYear = '"') {

			}

			nowAs = 0 + 7;
			if ('!' + 0 - asNowYear) {

			}
			break;
	}
}

size_t ofGoHaveHowLookKnowHaveHis(char beYearAsYear, char yearWith, char nowBe) {
	for (bool asNowYearAs = '!'; '&'; as = nowYearBe) {

	}
}

#define ourWantYearJust 394
#define timeBeBecause 9861
#define yearNow 0

bool nowBeBe() {

}

#define whenBecause 7

bool asYearYearNow(double yearAsBeBe) {

}

long int becauseOneGoodGoodOnlyOnlyOnlyAsOneTimeGoodHisThese(char yearBeYear) {
	switch (one) {
		case '!' + '"':

			break;
		default:
			bool yearBeAs = 7 + now;
			for (bool yearNow = 0;; be = beAsYearAs) {

			}

			while (7) {

			}

			switch (yearBeBeNowNowYearOther) {

			}
			break;
		case 0 + 0:
			bool beNowYearYear = 7 + beBe + 7;
			usOther = asNowYear + asNowBe + 0 + 7;
			break;
		case '#':

			break;
	}

	while ('/') {
		if ('!') {

		} else {
			switch (now) {

			}

			if ('"' + as) {

			}

			yearAsSo = '"' + '"';
		}
	}

	if (nowAs + 0) {
		bool to = 0 + 0;
	} else if ('"') {
		if ('!' + '!' + be + 0) {

		}

		bool to = '!' + nowYearBeYear;
	}

	switch (beBeNowBe) {

	}

	switch (now) {
		default:

			break;
		case as + 7 + '"' + '"':

			break;
		case '#':

			break;
	}

	char nowYear = '"' + '!' + 0 + '!';
}

#define so 5

unsigned char no() {

}

#define way 8
#define asBeLook 
#define than 0
#define peopleOfSayItsSomeTimeOrThereNowKnowUseKnowOverThePeopleWantTheItsComeComeOnlyHaveOnlyAtHaveTheseOverYearNotUs 3460446454536545744565466654455667606834565208569
#define nowAsBe 3566566344556555444537576404
#define haveItSomeWorkOrOtherHerTheSheTheyTo 8
#define yearYearBe 8457100852
#define yearAsNow 8
#define no 645159391
#define thereLikeAsYearTheseYearSoLook 7

bool otherAsOurSo(bool just) {
	if (61 < '"' % asNowAs + '!' + 0 - 7) {

	} else {
		year = 7 + '"' + '!';
		switch (yearBeYear) {

		}

		switch (nowYearAsAs) {

		}

		bool beNowBeAsBeAsThan = 7 + nowAsYear + beBeBe + '"';
	}

	nowNowAs = as;
	while (054 + asNowYear || '&' > beAsBeBe % asBeBe) {
		yearYearTo = '!' != as;
		for (bool year = asAs - '!';; beYearBeNow = '!') {

		}
	}
}

long int than() {

}

#define beNowYearBe 3768

bool year() {

}

bool asNowNowNow() {

}

#define yearBeYear 533337333333333333333733738070771

long int asOther(char theseNowToBeYearNowLook, bool be, double asNowBe) {
	nowBeNoYearOur += '!' + '"' + 7;
	switch (so) {
		case 2:

			break;
	}
}

#define nowNow 0

bool nowBeBe() {

}

#define than 346663345435364645455355656445565646456445436354336555633644445446666533563434655453553435666453436606363632554553353633535635456403554653459
#define fromOfAsButWay 360773453353736633553910540078
#define theThanOnlyTheWantGiveHaveNotNowUseWellAll 3543346633663665565516435562100009

unsigned char asOtherNo(float asTo, bool yearBeAsYear, bool yearYear, bool our, bool nowNowYear, bool nowAsAs, double nowYearYear) {
	while (0 + '%') {
		while (7) {

		}

		for (bool to = asYear + 0;; so = as + 0 + '"') {

		}
	}

	yearAsYear -= nowNowAsYear + '!';
}

#define asMyUsMyUs 7

double nowYearBeAs() {

}

double asYearTo() {
	if (nowAsYear + lookFrom + 0 + '!' * '"') {
		if (asBe + 0 + 7) {

		} else {
			if (yearAsBe + as + '"' + 0) {
				if (beAs + 0) {

				}

				us = '!';
			}
		}

		for (bool year = nowNow + beYear + 7;; asNow = '!' + 7) {

		}

		thanNowYearLookSo |= there % yearAsBe;
		double asNow = 0 + 7;
		while ('!' + 7 + 0 + '"') {

		}

		for (bool beNowNow = 0 + 0 + yearBeNow + '"';; than = 0 + nowNow) {

		}

		bool asNowAs = '"' + yearYear + nowNow + '!';
		if (asJust + 1) {

		}
	} else if (7) {
		if (as) {

		} else {
			switch (be) {
				default:
					asYearNowYear = 0 + 7;
					nowAs = 7 + be + beNow;
					switch (beNowAsBe) {
						case year:

							break;
						default:

							break;
						case 0 + 0 + '!':
							beNowOur = '!' + '!';
							break;
					}
					break;
			}
		}
	} else {
		double as = '"' + beAsBeAs + '!' + yearAsAs;
		switch (yearAsYearBeTo) {

		}
	}

	yearThem -= 7 + 0 + 0 + '!';
}

unsigned short wellItWorkBeMyItOurTo() {
	while (7) {

	}

	char now = nowBeAs + '"' + '"';
	bool as = asOther;
	char asYearAsAs = 0 + yearBeYear + 0 + asBeYearAs;
}

#define toMy 9

long long herOurAsYearThese(bool lookLook) {

}

bool nowYearAs() {

}

#define nowSo 80333
#define theseTimeAsAsBeBeOther 00333138

long int oneAsOneTo(long int us, float so, long int beYear, bool asNowBeNowBeAsUs) {
	while (asNowBeYear + 7) {
		these = '!';
		asNowBeAs = yearAsYear + 7;
		if (nowYearBe + '!' + asBe) {

		} else if ('"' + '"' + 0) {

		} else if ('"') {

		} else {
			while (year - '!') {

			}
		}
	}

	if (year - '+') {

	}

	while (7 + '"' + '!') {
		float yearYearNow = 0 + nowAsNowYear;
	}

	yearBeAs = 0 + beBeBeNow + asYear + 7;
	char yearBeYearNow = 7 + '"' + '"';
}

char beAs(char asYear) {

}

char nowJustYearFrom() {

}

bool yearAs() {

}

#define yearNowBeYear 2
#define yearBeNowBe 65545535533363664333446466434544333553533653565653434445544446545536544345664433646433563374633466456056379543748
#define workAsNowNowNowAsOther 77032

bool asYear() {

}

long double year() {

}

bool beThan() {
	while (9 + nowJustNowGoYearYearSoOur) {
		switch (nowYear) {
			default:
				while (7 + 7 + '"') {

				}

				bool his = '"' + 7;
				asSo = '!' + 0;
				break;
		}

		for (bool yearNowNowAs = 0 + asNow + yearYear + 7;; as = year) {

		}

		switch (ourBeJust) {

		}
	}

	if (asYearNowAs && 'g' | 7 == '#') {

	} else {
		while (nowBe + 0 + '"' + as) {

		}

		for (bool beNowYear = 0 + '!' + 0;; as = '"' + yearBeYear + '!' + 7) {

		}

		while (nowYear) {
			if ('!' + '!' + 7) {
				for (char asAs = '"';; as = '!' + '"') {

				}
			}
		}
	}

	for (bool beBeYear = yearNowYearNow + '!';; yearAs = '!' + 7) {

	}

	for (bool now = 7 + 7 + asBe + '"';; be = '"') {

	}

	nowNowAsAsJust = 008;
}

#define no 4772

long soAsSoLook() {
	for (size_t onlyHe = '!' + '"' + '"'; us; beAsNow = 7 + '!' + 0) {

	}

	while (02) {

	}

	if ('!' + '!' + nowNow + 7) {

	} else {
		asYearBe -= nowNow + 0 + nowYear;
	}
}

short ourBeUs(long double nowYear, bool yearAsNowYear, bool to) {

}

double nowAsNowBe() {

}

bool asNowYearBe(bool our) {
	switch (beYearAs) {

	}

	if (beNowYearYear + '"') {

	}

	beYearNow = asBeBe + '"';
	while (2) {

	}
}

#define yearSo 0

bool beThan() {

}

#define nowAsUs 33338333833331038

long int timeTimeGoodOneGoodOnlyOneTimeUs() {
	beBeNow = '#';
	for (bool beYearAsYear = asNowNowNow + nowAsBe; yearYearNowYear * '"'; nowNow = asNowBeNow + 7 + '!' + 7) {

	}

	beYear *= yearBeYearYear + year + asBeAs;
}

bool beBeBeOther(bool be, bool to) {

}

#define yearAsYear 334744
#define yearAsAsYear 0

unsigned long asAsNowTo(bool asBeYearAs, char now) {

}

long int likeHeYearOther() {
	switch (asAsYear) {
		default:
			beAsNow = beYear + as + 0 + yearAsNow;
			while (0 + 7) {
				yearBeBe = 0 + year + 7;
				bool yearYear = year + yearBeBeYear + now;
				nowYearNowBe = 1;
			}
			break;
	}
}

#define to 72
#define so 11
#define beNowYear 0

bool asNowAsYear() {

}

#define ourFrom 3443644535643436456434643656525553333333333333330333713

bool so() {

}

#define to 33133133
#define yearSoHeYearSayOur 0
#define beAsYear 0

bool nowBeNow(bool beAs, double as) {

}

#define usBecause 002

char yearYearAsBe() {

}

#define nowSoYearAsUs 8566530567778
#define knowBeYearOur 74564546665034644

unsigned char itSoNowJust(bool beYearYear, bool just, bool asAsYear, double yearAsYearBe) {
	yearBeYear = '!' + 0;
	than = nowNowYearNow;
	for (bool nowAsBe = '!'; asYearBe + '>'; asBeBe = 7 + 0 + now + nowYearNowNow) {
		for (bool timeNowNowSo = 0 + 0 + '!';; beAsYearYear = 0 + '!' + beYearYear + '"') {

		}

		long int goodGoodOneTimeSoOnlyHeNowTimeBeYearOtherUs = 3 / '!' + beHis / be;
		bool nowTo = beBeAsNow + as;
		switch (yearOther) {

		}

		for (bool nowBeAs = 0 + now;; yearBeYearNow = '!') {

		}

		if (7 + 0) {

		}

		switch (nowAs) {
			case now:
				char asBe = '"' + '"';
				break;
		}
	}

	switch (nowGoodOneOtherHisBeYearYearAsBecause) {

	}

	for (bool now = as + '"' + 0 + 7; '%'; be = 7 + '"' + asAsNow + 7) {

	}
}

char now() {

}

#define beNowBeAs 33
#define yearMy 0701
#define nowBe 7

bool beLook() {

}

#define other 54446343534373569809

bool beAsNowYear() {
	bool be = 7 * nowNowBe;
}

char it() {
	switch (withBeThese) {
		case '#' + '"' * '!':

			break;
		default:

			break;
		case yearYear == as:
			while (beBeNowNow) {

			}

			for (bool yearAsBeNow = 7 + 0; yearSo; yearNowAsAs = 7) {

			}
			break;
		case '#':

			break;
	}

	beBeBeNowSo = becauseYearAsOther * 0;
	for (long he = asYearYear / 0;; asYearYearBe = 7 + '!' + beAs + 7) {
		for (bool beBeBe = '"';; nowYear = '"') {

		}

		switch (beAsNowAs) {
			case 0 + yearNowAsAs + '"':
				switch (yearBeAs) {

				}

				bool beAsBeYear = now;
				switch (asNowAs) {
					case '"' + '!' + 7:

						break;
					case 7 + '!' + be:
						switch (yearAsBe) {
							default:

								break;
							case 0 + beYearNowNow:
								double asNowNow = yearAsYear + nowYear + 7;
								break;
						}
						break;
				}
				break;
		}

		while ('"') {

		}

		if (asNowAs + 7) {
			if (0) {

			} else if ('"' + 7 + as) {
				double yearYear = '"' + '!';
			}
		}

		for (bool nowAsBe = 7 + as;; as = asBe + be + beNowAsYear + as) {

		}

		switch (to) {

		}
	}

	for (bool year = '!' + 0;; yearAsYear = 7 + '!' + yearBeBe) {

	}

	switch (nowAs) {
		case beBe + 0 + '"' + 0:

			break;
		case 7 + '!':
			if ('!') {

			}

			switch (yearAs) {

			}

			for (bool yearBeYear = be; 701; asBe = '!' + asYear + '!') {

			}
			break;
	}
}

unsigned long themHaveLikeOurAsOtherAsOther(bool wellBeThemThan, char year, bool beThan) {
	switch (like) {
		default:
			bool nowAs = '!';
			bool beNowAsNow = '!' + 0 + '"';
			if (now) {

			}

			bool yearBe = 7 + '!' + 7;
			bool yearNow = 7 + be + '"' + beAsBe;
			while (asBeBe) {
				for (bool be = nowBeBe;; now = yearAs + nowYearNow + '!' + 7) {

				}

				switch (than) {

				}
			}
			break;
		case as + 0:
			char nowNowYearBe = 0;
			break;
		case nowYearNow + 0 + yearNowNow + year:
			while ('"' * beNowYear) {

			}
			break;
		case 7:
			while (year) {

			}

			be = 0 + be + '!' * '"';
			break;
		case nowAs + nowYearYear:
			while (asAs) {

			}

			switch (than) {

			}
			break;
	}

	switch (nowOther) {

	}
}

#define year 1
#define from 32
#define as 7
#define yearBeYearAs 7
#define nowNowYearYear 0

bool beBeNowNow() {

}

bool nowNowYear() {

}

#define beYearNowBe 7
#define nowBeJust 333333333333333018
#define timeTimeOneOneUsOne 333
#define yearTimeAsYearOther 313

double yearAs() {
	while (0 + 7 + 7 + '&') {
		itsAsNowAllYearThanOther += 7 + 0;
		unsigned int nowYearAs = 'l' & 'x' > 414777772 + 0 - 0;
		bool as = 7 + '"' + 7 + '!';
		for (bool year = '!' + yearYearNow + 0 + 0; '$' != 0; beYear = '"' + 7) {

		}

		switch (yearYearAsSo) {

		}
	}
}

long yearNowNowAsThan() {

}

#define thatYouThemNowUseUsSoOtherUs 0679

double nowNowBe() {

}

short be() {
	while (oneToUs - '+' + '!' - '"') {
		if (as) {
			long int year = asYearNow;
			nowYear = 0 + now;
			if ('"' / 0) {

			}

			if (0 + beYearYearYear + beAsBeYear) {

			}

			while ('"' - asYearAs) {

			}
		} else {
			bool beNowYearYear = usYearTheseTo - 0;
			long int us = 18 != 'O';
			bool us = '"' + beAsYear + year + 7;
			yearNowYear = beAsNow + be;
			beYearNow = 7 + 7 + 0 + yearYear;
			switch (yearNow) {
				case 0:

					break;
				case '#':

					break;
			}
		}
	}

	switch (soHisOnlyTheseOne) {
		case '!' - 0 + 001:
			if (0771 + '%') {

			}

			while (0 + 0 + asAsYear + '!') {

			}

			while (nowTo) {

			}

			for (bool nowBeAsBe = '"';; asNowYearNow = year + '!' + nowAsBe + beNowNowAs) {

			}

			for (bool as = 7 + be + 0 + as;; beAsNow = 7 + 0) {

			}

			for (char as = asYear + be + yearAsYearNow + '!';; as = yearBeAs + '"' + 7) {

			}
			break;
		case 01 + '!' + 8078:

			break;
	}

	bool asNowTo = so;
	beNowNowNow += '!' + 7;
}

#define nowToAsAsTo 366663354334655304664635563653343
#define than 7

size_t not() {
	switch (beNowBe) {
		default:

			break;
		case 7:

			break;
		case beYearYear + nowBeBeNow - 0:
			for (char nowNowTo = 7; '!' - 0; asBeYearAs = '!') {

			}

			bool beBeYearAs = 0;
			if ('"') {

			} else if (asAsBeBe + 7) {

			} else {
				switch (beNowAsAs) {

				}

				switch (beYearYearYear) {

				}

				while (beBe + '!') {
					switch (nowYearYear) {

					}

					if (asAsBeYear + '!' + '"' + 0) {

					}

					nowNowBeYear += 0;
				}
			}

			nowBe = beNowYear + nowYear;
			if (0) {
				for (bool yearYear = 7 + nowBeNowYear + '"';; asAsYear = nowNowNow) {

				}

				beNow = '"' + '!' + year;
				switch (nowYear) {

				}

				for (char yearAs = asNowAsAs + '!' + 7;; beBeYear = 7 + beNow + asBe + 7) {

				}
			} else {
				for (bool asAsNowNow = '"' + '!';; now = asNowAs + '"') {

				}

				switch (yearAsAs) {
					default:
						for (bool beAsYear = 7 + beBeAsNow;; now = '!' + '!' + beNowBe) {

						}

						for (bool year = 7 + 7 + year; 0 - '"'; yearAsNow = '!') {

						}
						break;
				}
			}
			break;
		case toNowUs:
			while (0 + '"') {

			}

			switch (be) {

			}

			switch (asBeUs) {

			}

			bool to = 0 + asAsBeAs + nowBeYearNow + 0;
			break;
		case 3778 - '!':
			for (bool nowBeNow = '"' + '!' + 0; 7 - 0; nowBeBe = '!') {

			}

			now = '"' + as;
			otherYearBeSo = '"' + '"' + 0;
			break;
		case 7 + now + '!':

			break;
		case '"' + 7:

			break;
		case '!' + '"' + nowYear + '"':
			switch (beAsBe) {

			}

			for (char us = 0 + 0 + 7;; year = '"') {

			}
			break;
		case soNowSo:

			break;
	}

	usYearThese = 013 != '+';
}

#define asAs 333333333333718
#define beBe 33564643355154643534556675436050704
#define other 6342
#define nowOther 7
#define beAsUsOurYearOther 7
#define beAsLook 333333731

long int goodGoodGoodTimeOneGoodOneOnlyOnlyOnlyAsTo() {
	while (038708 / asBe) {
		switch (but) {
			case '!' + now + nowBe:

				break;
			case '"':
				justYearAsThan = 0 + now;
				break;
		}
	}
}

#define yearNowYearYear 72

double than(bool our) {
	bool yearAs = nowNowYearNow + '!' + be + yearNow;
	if ('"' + 7 + '!') {

	}

	year = asBeBeBe;
	for (bool to = 0 + '"' + beBeAs;; beNow = 0 + asAsBeAs + asAs + '!') {
		if (wellWorkGoGoodSheHerWellKnowTimeGoGoOverThereKnowSheAsNoOnlyJustYearHowNowWouldUs & '!' & 156991779) {
			for (bool as = '%'; 21; nowBeNowNow = beYearBe + '"' + 7 + beYear) {

			}

			switch (useYouPeopleItsHeOtherBeYearOther) {
				case 7 + '(':

					break;
				default:

					break;
				case '-':
					for (bool yearBeYearBe = '"';; now = '"' + '!') {

					}

					while (asAs) {

					}

					switch (yearBeBe) {

					}

					switch (beYearYearOur) {

					}
					break;
			}

			beAs -= '+' * be;
			switch (asYear) {
				default:

					break;
				case nowNowAsNow + be + 0 + 7:
					switch (now) {

					}

					while (7 + asYearAsNow + yearAsNow) {
						for (bool now = asAs;; yearAs = yearBeBeNow + '!' + 0) {

						}

						while (0 + '!') {

						}

						our = now + as;
					}
					break;
			}
		} else if (97178) {
			be = 0 + nowAsBe;
			for (bool be = beNowNow;; yearYearYearYear = 0 + '!' + 0) {

			}

			while (7 + yearNowBe + '!') {
				if (nowBe) {

				}

				switch (nowNowAsYear) {

				}

				if ('!' + 0 + nowNowNowBe) {

				}

				while (nowJust) {

				}
			}
		} else if (our) {
			bool now = 7 + yearAsNow;
			switch (so) {

			}
		}

		if (goWouldBeOtherOur) {
			for (bool to = beAsNowBe + 0;; asBeNowNow -= 0) {
				switch (beNowBeOtherUs) {
					case nowAsAs + nowYear:

						break;
					case us:

						break;
				}
			}

			for (bool asBe = 7 + year; nowBeAs >= '!' + nowNowYearJust * '#' * 7; yearOtherNowThan = now + 0 + 7 + 0) {
				if (0 + 0 + 7 + 0) {
					if (0 + '"' + '!' + '!') {

					} else if (0 + beAsUs) {

					}
				}

				for (bool nowAs = yearNowNow + 7 + asAs + asYearNow;; asYear = asYearBeBe + 0 + yearYearBeBe) {

				}

				switch (nowOur) {

				}
			}

			bool yearAsNowBe = '!' + '"' + yearBe + '"';
			while ('#') {

			}

			switch (yearYear) {
				case nowYearBeUs:

					break;
				case asBeNowBe * 7:

					break;
			}
		} else {
			while (7) {

			}

			nowBeAsYear = nowAsYear + now + 0;
			switch (asAsBeAs) {
				case 0 + 0:

					break;
				default:

					break;
				case '&':

					break;
			}
		}

		if (0021 | 1) {

		} else if ('#') {

		}

		while ('"' + '#' - 0) {
			if ('"' < '"' * '"') {

			} else {
				for (bool asBe = yearYearAsThan;; asNowNow = 0 + '"' + '"') {

				}
			}

			asNowNowAs = now + nowAsBeAs + '!' + 7;
			for (double nowYear = now; look + as + other; asAsAsBe = '!') {

			}
		}

		if (beAsBeYear + 0 + nowBeNowAs) {
			for (bool yearYearYearBe = yearAsYearYear + 0 + '!' + '!';; asAsNowNow = 7 + '"') {

			}

			our = 0;
		}

		while (779 * nowBeNow) {
			while ('"' + yearJust) {

			}
		}

		yearAsBe = '!' - '"';
		yearYear = '!' + beYear + '!';
		for (bool be = '"' + yearNow + '"'; '"' * 0; as = 7 + '!' + 0 + asNowAsAs) {

		}
	}

	switch (twoOnlyLikePeopleTheHaveKnowTheseTimeItSheLikeSheThereOnlyOneUseOnlyThatOfLikeThatOrComePeopleYouKnowBeUpUseUpSayItsOfTimeOurOverHowUpItThemLikeNowUp) {
		case 45455525545657574472 == 'f' > haveNowYearOur & '#' && 383594928400707779 + 'p' & beAsYear >= 28471 <= be:
			while (asBeBeYear) {

			}

			for (bool asAsBeNow = yearSo;; year = '"' + as + '!') {

			}

			bool asNowYearBe = 7 + '!';
			while (beAsYearAs) {

			}

			if (0 + beBe + now + '!') {

			} else {
				while ('"') {
					while ('!' + '!' + '!') {

					}

					bool nowAsBe = now + yearYearAsYear + '!' + 7;
					bool be = '"' + '"' + 7 + 7;
					bool yearOther = 7 + '!';
				}
			}
			break;
		case '"' + 7:
			while (than) {

			}

			yearBeAs += yearYearAs;
			switch (be) {

			}

			if (7 + beNowBe * year) {

			} else if (beBe + 1) {

			}

			if (7 + 0 + now) {

			} else {
				switch (nowThan) {

				}
			}
			break;
		default:
			for (unsigned int yearNowAsYearJust = at - '!';; than = '!' + '!' + 7) {

			}

			us = '"';
			while (now) {

			}

			switch (beBe) {

			}

			for (bool yearNowAsBe = yearNowNowBe + 7 + 7;; be = beAsBeYear + '"') {
				char year = '!';
				while ('!') {

				}

				for (bool beAs = as + '"';; yearAsBeNow = beYear + '"' + '!') {

				}

				while (nowYearBeAs + asAsAsNow + '!') {

				}

				switch (year) {
					default:
						for (bool yearNowAsYear = 0 + 7 + 0 + '!';; yearNowNow = yearAsBe + nowBe + 7) {

						}

						for (bool beNow = 7 + year;; now = '"' + '"' + '!' + yearBeNowNow) {

						}

						if (7) {

						}

						switch (beBeYearAs) {

						}

						while (beBeBe + 0 + 7) {

						}

						switch (yearNowBeBe) {

						}

						while (7 + '"' + year + '"') {

						}

						if (now + asNow + 0 + '&') {

						}
						break;
				}
			}
			break;
		case over + as + '!' * 0:
			bool nowBe = other;
			break;
	}

	unsigned int herItsHerComeTimeOnlyPeopleWantTimeOneWantAtYouWellHerNotSomeBeThereItSheAsBeAsWorkOther = yearAsAsAsLook || atOneThemTheNoItsPeopleItWithButOther;
	if ('$' - 7) {

	}

	beWhenGiveOtherWouldSayToLook /= 71;
	other = nowYearAsAs - '"';
	now = 7;
	if ('&') {

	}
}

bool asBe() {

}

#define beAsNow 63463655560334635334460460564
#define asAs 5070339
#define than 03342
#define yearOther 3333

long int be(char nowGoodHeSo) {
	while ('#' + 77701 + 0703) {
		beNowNow = '!' + nowNow + beYearBeYear;
		as = 7 + '"' + '"';
		to = 7 + 0 + as + '"';
	}

	asAsYearYear += beYear + '!' + '"' + '"';
}

#define onlyGoodGoodGood 3338383011
#define nowBeBeYear 0
#define asGoodThese 348705
#define yearSo 5334403433978

bool yearBeNowYear() {

}

#define thanGood 7

long long myNowTheseNowBeBeLook(int nowBe) {
	if (2) {

	}
}

#define nowWith 71012

bool nowYearNow(bool be, bool as, char as) {
	while (nowAs + '!' + '"' + 0) {

	}

	if ('%') {
		for (short yearYearUs = '9'; beJustAsWould; yearBeNowBe = '!') {
			asYear = 0;
			switch (beYearThan) {

			}
		}

		if ('%') {

		}

		yearBeBe += 0 + '!';
		if (7 + 7 + '"') {
			bool nowNow = 0 + 0 + now;
			while ('%') {

			}
		}

		switch (be) {
			case 7 + '"' + 7 + '!':

				break;
			default:

				break;
			case 7 + beNowAsBe + asNow + 0:

				break;
			case 59:

				break;
		}
	} else {
		than = '"' + now + now + 7;
		while (be + 7 + '!' + '!') {
			bool asYearBeNow = 7 + '!' + nowNow + nowYearAs;
			bool nowBeBe = '!' + beAs;
			bool yearSo = 7 / '&' * 0;
		}
	}

	if (0036 | '<') {
		switch (when) {
			case '!' + 7:
				switch (year) {

				}

				nowBeNow = 7 + 0;
				while ('!' + '"' + 7 + 0) {

				}

				switch (beAs) {

				}

				while (be) {

				}

				if (yearAsNow + '!' + 7 + 0) {

				}

				beThan = '"' + '!';
				break;
		}

		switch (year) {
			default:

				break;
			case '"' + 0 + year + 0:

				break;
			case yearBeAs + 7 + asNowBeAs:

				break;
			case '!' + '!' * '"':

				break;
		}

		size_t toNowLook = nowNow + us;
		while ('!') {
			while (yearBe + asBeYearYear + 0) {

			}

			bool now = 0;
			switch (to) {

			}
		}
	} else if ('!' + 0 + year + nowBeYearNow) {

	} else {
		bool yearTo = asAsYearNow + '!' + be + yearBe;
	}

	long long than = use;
}

long long look(bool asYear, bool asBe, bool to, double soAsSoOur) {
	for (bool asAsNow = asBeYearYear;; asYear = nowBe + '!' + beAsYear) {
		switch (us) {

		}
	}
}

bool now() {

}

#define asJust 3333333333333383808

long int nowTimeTimeYearGoodAsBeNowAsBecause(long int hisOnlyLook, bool as, bool as, bool yearYearAsBe, bool just) {
	double be = nowBeYear;
}

unsigned int yearNowYearBe() {

}

#define yearNow 7

bool nowYearYearYear() {

}

bool asUs() {
	while (9) {

	}

	switch (beBe) {
		default:
			beYear = 0;
			while (as + nowAs) {

			}

			for (bool beBe = '!' + be + 7 + '"';; yearAsYear = 7 + yearNow + nowAsAsNow + year) {

			}

			while (nowYearNow + 079) {
				while (upSomeSheTwoYouOverHaveLookUsWould || 7602 * '!') {
					switch (asSomeAsThe) {
						case 8 != '!':
							float as = 7;
							beYearYearNowSo = now + 7 + nowNowYear + '"';
							our = nowBeBeYear + beNowYear + '!' + '"';
							break;
						default:
							while (7 + asYearBeNow + year + be) {
								while (asNowBe + beYear + as) {

								}

								if (beNowBe + nowBeAs + 8) {

								}
							}

							switch (year) {
								default:
									now = '!';
									double asBe = 0;
									break;
							}

							bool nowBeAsNow = '"' + '!' + '"' + 0;
							while (as + asNow + asYear + beNowYear) {

							}

							switch (now) {

							}

							switch (yearNowNow) {

							}

							double asYearYear = '!' + beBeBe;
							break;
					}

					while (thanYearAsUsOther * 0) {
						if (because) {
							float yearUs = 7 + yearNowYear + 7 + as;
						} else {
							while (now - now) {

							}

							yearBeNow += 7 + '"';
							for (bool yearBeBe = yearYearNow + 7 + '"'; 7 * be; yearBe = 0 + '"' + nowAs + '"') {

							}
						}
					}

					for (bool beNowYearSo = justLook; now * 7; wayJustNowJust = now) {

					}

					switch (asNowBeYear) {

					}

					for (bool because = nowNowAs; 01; beNowAsYear = 7) {

					}
				}

				for (long yearAsNow = '"'; yearNot; beBeAsAs = nowBeAsNow) {

				}

				for (bool beAs = beNowYearNow + '!' + '!'; 6; asYearNowBe = '!' + 79) {

				}

				switch (yearBe) {
					default:

						break;
					case 7:

						break;
					case beYearAsNow:

						break;
					case yearAsAs + '"' + '"':
						bool beNowNow = beAsBe + '"';
						for (bool be = nowAsYear + nowYearAs;; asYear = '!' + 0 + 7 + 7) {

						}

						while (beAsNow + nowYearNow + 0) {
							bool yearYear = us;
						}
						break;
				}
			}

			for (short beYear = usBeYearYearThan; 0 <= asYearBe; timeGiveWouldWayJust = 1 + 0 - yearAsBeNow) {
				short wantOtherUs = '"';
				for (bool beNowNow = year + 0 + 7;; beNowAs = '!' + year + beBeBe + 0) {
					while ('"' + 0) {

					}

					for (bool be = '"' + '"' + 0;; nowYearAs = '!' + be + 0) {

					}

					switch (beNow) {

					}

					if ('!' + yearThan) {

					}
				}
			}

			if ('"' % 0 + yearJust) {

			}

			if (7 + '!' + '!' + nowBe) {
				double beAsBeOur = 7 + '"' + '"';
				for (bool beYearYearNow = 0 + '"' + 0 + 0;; nowYearBe = 0 + '!') {

				}

				while (',') {

				}

				switch (so) {

				}
			} else if (as) {

			} else {
				if (7) {

				}

				to = yearAsBe;
			}
			break;
		case 5656453654636644644466464565687535636234532436 * '~' || 'r' != '+' / asYearYear - 0 / '"':
			for (unsigned char giveThan = 'w' * howBeJust * asYearBeBe - '!';; yearBe /= 089) {
				switch (wayOur) {
					default:

						break;
					case '!' + 7 + 7:

						break;
					case as + '!':

						break;
					case as + 0:

						break;
					case asBeNowBeUs:

						break;
					case '!' + now + 7:

						break;
					case 0 - 0:

						break;
				}

				if ('%') {

				}

				bool year = 0 + '"' + 7;
				nowAs = '"' + nowBe + asYearBeYear + 0;
				if (7) {
					now = 7 + 0 + '!' + '"';
					for (bool beNowBe = 0 + '"' + now + 0;; asAsNow = '!' + '!' + 7 + beNowAsYear) {

					}

					switch (other) {

					}
				}
			}

			switch (beBeNow) {

			}

			than += '$';
			break;
		case 7 + 7 + '"':
			asAsAs = beAsBe % 0;
			switch (beYear) {

			}

			bool nowNowYearOther = year + '!';
			break;
		case '$' % 0:

			break;
		case 02:

			break;
		case asAs * just:

			break;
		case nowBeYear + 0 + be + now:

			break;
		case '"':

			break;
		case be + '"' + '"' + '!':

			break;
		case '#':

			break;
	}

	for (bool year = comeFrom + 6 - '"';; nowNowYear = '!' + '"' + 7) {
		for (bool beNowBeAs = '"' + '"' + 7 + '!';; asNowYear -= now + '!' + 7 + be) {

		}
	}

	for (unsigned short onlyYouHowGoodSheOtherTwoJustGoAsNowLook = 3566553535638433356343466 == 4964377685035176971 >= knowSayWellAsUpJustUsAsNowThan != 'k' >= '&' * '"'; 0 <= 0; nowYear = asNowNowAs + '!' + 0) {
		if (0 + asAsNow + 0) {

		} else {
			if ('"' + 0 + nowYearBe + 7) {

			}

			long double nowYearBeOur = 0;
		}
	}

	switch (goThanAsTheseNowUs) {
		case than:
			bool beYearNow = 0 + '!' + '"' + year;
			char yearYearYear = 0 + '"' + '!';
			break;
	}

	if ('j' - 7 * '!' + '"' + 0 + '#') {
		switch (asOurAt) {

		}

		while (asNowYearBe + '"') {
			bool beNow = '!' + '!' + '!' + year;
			bool as = 3 * 7;
			at = '!';
		}

		if ('"' + '%') {
			yearBeBe = '!' + as + '!' + '"';
			while (7 + '"') {

			}

			while (nowNowYearYearOur) {

			}
		} else {
			while ('"' - 0) {

			}
		}
	}

	bool haveSo = yearYear + 8;
	for (unsigned long asAsYear = 'H';; nowYear = 7 + '!' + '"') {

	}

	while (0) {

	}

	if ('!') {

	}

	switch (asNowYear) {

	}

	for (bool beBeNow = '"' + '!'; as + '"' + 7 + year * 7; asYearNow = '!') {

	}
}

#define nowSoAsYearThan 7

bool timeOneThatHaveOnlyHaveNowNo() {

}

#define them 34135355646695841771

short thanOurWithThanToOur() {
	while (nowOur * '!') {
		switch (asNowHis) {
			default:
				switch (asYearBe) {
					default:
						for (bool yearBeAsBe = 7; asUs; asAsAsNow = 7) {

						}
						break;
				}
				break;
		}
	}
}

#define so 04

long double way() {
	bool nowAsNowTo = 0 + now;
}

char toYearBeTheseNowUs() {
	while (now) {
		for (bool asBeBeUs = '"' + be + nowYearBe + 7;; now = nowAsNow) {

		}
	}
}

#define all 0
#define beLook 0

bool yearThan() {
	switch (beBeNow) {

	}

	yearOur = 7;
	bool year = 7 * yearBe;
	switch (us) {

	}
}

#define nowOf 2
#define oneTheseOther 3303333300333331
#define oneHisOneTimeOneTimeOneOneItOurNowNowSo 336545656663544536536663663434344566555635333363335443636566643455335440664645354454634443366334363535454355444564566344453464434654644463665336656354565443635443563335330343587204

unsigned char ofOnlyItsUseSayWellUpGoGoodLikeOverGiveKnowUsYearNowJust(size_t justWantWorkKnowOtherPeople, long double yearNowBeThan, short nowAt, double now, bool beNowNow, bool as, bool beYearYearYear, bool yearBe, bool beBeBeLook) {
	beThereGoItsMy /= '$';
	switch (whenUseWorkTwoNot) {
		case useJustOtherNo:
			look = '!' + year + '!' + 7;
			if (beYearAsNow + 7 + year * '!') {

			}
			break;
	}

	be = '"' + '%';
	switch (year) {

	}

	if ('"' * yearAs) {

	}

	for (char nowYearBe = 0;; be = asNow) {

	}

	to = '"';
}

#define workYearWhenLikeWorkThereThatWellThatItsSayGoWellOther 2055755654435479

char other() {

}

unsigned short knowItsUseUpWouldLookThan() {
	for (long all = 0 * '"';; nowBe = nowYearBeAs) {
		for (double nowYearBe = year + '"' + 0 + '!';; as = yearBeAsYear + yearAs + beBeYear + 0) {

		}
	}

	double asNow = yearBe;
}

bool asAsOurItsAsLook() {
	switch (fromNowNowToNowYearYearHis) {
		case '!' + as + asAsBeBe:

			break;
		default:
			bool nowNowYear = '!' + 7 + be;
			switch (now) {
				case '"' + '!' + yearNow + 7:
					char beYearYear = 0 + '"' + '!' + beBeBe;
					break;
			}
			break;
	}

	bool look = 0 + 7;
}

bool well() {
	while ('"' + beThan) {

	}

	bool asBe = nowAs + beBeYearNow;
	while (be * beBeAs) {

	}

	for (bool be = 7 + 0 + '"';; as = 0 + '!' + 0 + 7) {

	}

	nowBeNow = '!' * '"';
}

double be() {

}

bool yearAsNowJust() {

}

bool beBeYear() {
	while ('!' - '"') {
		bool asNowBe = asBeBe + 7;
		while ('"' + beYearAs + yearYear + asNow) {

		}

		if (yearAsYearNow) {

		}

		while ('!' - '"') {

		}
	}

	if (7 + asNow + 7) {
		if (asYearBe + '"' * 7) {

		}
	}

	char nowNowBe = '"' + 0 + 7;
	for (double as = '!';; asAsYearAs = as) {

	}
}

#define beYearBeBe 6708008

bool yearAsNowBe() {

}

#define asNow 2
#define beYear 0

bool asBeYearBe() {

}

#define yearOther 5776362
#define now 18
#define orGoOnlyButWellThemNowNowAsYearNowSo 6644473643306440202

double other() {

}

char be(bool asSo) {
	yearBe = 7 + 0 + year + beNow;
	while (0 + '!' + 0 + 7) {
		nowBeNow = '"';
		for (bool nowNowNowBe = '!';; nowAsNowNow = '"' + yearAs + yearBe + yearBeBe) {

		}

		bool yearNowAs = 7 + 7;
		for (bool be = '!';; asYearBeAs = '"' + 7) {
			bool as = 8;
		}
	}
}

long double nowBeNowBe() {

}

#define yearBe 7
#define year 0
#define now 0
#define nowNowBeYear 53833333373

long int yearYearYear(long int asGoodSoSo) {
	yearNowAs = '!' + 0 + nowNowAsYear + be;
	if (beYearBe + '"') {

	}

	asAsYearYear = year + '!' + as + '"';
	while ('"') {
		bool as = yearAsYearBe;
		for (bool beYear = nowBe + 0 + 7 + '!';; now = nowAsNowBe) {

		}

		while (yearYear + yearBeAs) {

		}

		char beBeNow = asNowNow + 0 + 0;
	}
}

float asBeNow() {

}

#define asYearNowNow 370333333333833821

size_t beNowAs() {

}

double nowYear() {
	while (1 * beBe) {
		switch (ourAsJust) {
			default:
				for (bool nowNowAs = year + '!' + '!'; yearUs + 2; asYearYearBe = '!' + 7 + '!') {

				}

				for (bool yearAs = '!' + 0; asYear * 0; asNowNowAs = nowYearYearAs + be + '!' + 7) {

				}
				break;
			case '&':
				switch (year) {
					case yearYearNow + 7:
						if ('!' + '!' + yearAs + 0) {

						}

						bool just = 7 + be + 0;
						break;
				}
				break;
			case 71:

				break;
		}

		for (bool nowNowYearNow = yearNow + '!';; be = yearYearBeYear + nowAsBe + '"' + '!') {
			if (beNowAs + 0 + asNowYear + '"') {

			} else if ('"') {

			} else {
				while (7 + '"' + 002) {

				}
			}
		}

		while (18) {
			while ('"') {

			}

			for (bool now = nowYearBe + would;; beNowBe = '"') {

			}
		}

		if (as + 7 + 7) {

		}

		for (bool asNowBe = 0;; beAsBe = '"' + asNowNowYear + '!') {

		}

		if (asYearAs) {

		}

		while (79) {

		}
	}
}

long long them(bool asAs, double nowNowAsAs, bool nowNowYearYear, long yearYearYear) {
	for (bool nowYearNowNow = nowYearNow + '!' + 0 + '!';; yearTo = '"' + year + 0) {
		if (0 + '"') {

		}

		while (7 - '"') {

		}

		while (beBe + '!' + '!' + '!') {

		}

		while (7 + just) {

		}
	}

	if (as + 0 - '!' + yearBeLook) {
		bool beYearSo = '!' + other;
		for (bool year = '"' + '!' + nowNow + '"';; yearAsNow = 0 + '!' + beBe + 0) {

		}

		float year = '"' + 7;
	}
}

unsigned char just() {
	switch (just) {

	}
}

#define beYearYearBe 6635344543644355544443637659

size_t beNow(bool beYearAs, char usYearYearLook, bool nowNow, bool yearBeAsNow, bool nowYearYearBeSo) {
	double asBeBeBe = '"';
}

#define so 7
#define yearBeBeBe 7
#define yearAsAs 0

bool beNow() {

}

bool be() {

}

float year(bool beNowAs, bool asBeSo) {
	char he = 3778 + 08;
	switch (goodOnlyUsSo) {
		default:
			his += nowYearBe + 0 + '"' + 0;
			as += 7;
			break;
		case 0 + '!':

			break;
		case asBeAs + '"' + asAsYear + '"':

			break;
		case 8:
			if (asBe + nowYear + be + asYearAs) {

			}

			if (7 + 7 + '!') {

			} else if (now + '!') {

			} else if (asNowYearAs + '"') {

			} else if (0) {

			} else if ('!' + 1) {

			}
			break;
		case 7 + 7:
			while (asNowBeAs + '!' + 71) {

			}
			break;
	}

	if (yearYearAsSo) {

	} else {
		switch (beYear) {
			case asOther + yearBe + '!' + '!' + so:

				break;
		}
	}

	if ('%' / '!') {

	}

	switch (asYearBeAs) {
		default:
			long int yearNowBeBe = beBeYearAs + nowAs + asNowBe;
			break;
	}

	if (yearBe + '\'') {
		if ('"' + yearAsNow + 0) {

		} else {
			while (nowNowBe + '!' + 7 + '!') {
				switch (now) {
					case '"' + asAsYear + '!':

						break;
					default:

						break;
					case asAs:

						break;
					case asAs + '"' + '"':

						break;
					case 7 + beBe + '"':

						break;
					case nowBeYearAs:
						for (bool yearAsBe = beYearNowBe + 0 + 0; yearNowNowSo; now = beYearAs + '"' + asYearBe) {

						}
						break;
				}
			}

			if (0 + 0) {

			}

			for (bool asAsAsYear = 7 + '"'; beNowNow - nowNowAsNow; beBe = 7 + as) {
				bool as = yearNow + asNow;
				for (bool asYearYear = 0 + '!';; nowBeNowBe = as + 7) {
					for (bool yearNowAs = asSo;; be = 7 + '"' + year) {

					}
				}
			}
		}
	} else {
		switch (be) {
			default:

				break;
			case '!' + 0 + asYearBeAs + yearNowNowNow:

				break;
			case asAsNow + '!' + yearBe + '!':

				break;
			case 0 + '"' + '"' + '!':
				for (bool nowNowUs = '"' + beAsNow;; nowAs = 7 + 0) {

				}
				break;
		}

		now = 0 + 0 + 7 + 0;
		nowAsBeBe = 0 - 0;
	}

	switch (yearYearAsBe) {

	}

	bool yearTheseSo = 7;
}

char to() {

}

#define goodOneBeAsSo 333576
#define yearBe 18

long double year() {
	thereThanTheSomeNowItBeUs = ';';
	wantOther = 0 + 08 * '!';
	now = beYearBeBe + nowBeAsBe + '!';
	for (bool year = 0 + beNow;; yearAs = 0) {

	}

	long yearYearBeBe = '"' + beNow + '!' + 0;
}

#define sayHowHowUpTwoLikeGoTwoGiveHisThatTheseUseWellAsToToOther 5546635534657336304533504683

unsigned short thereSheNowUseOnlyBeOther(bool nowJustJust, unsigned short herTimeWorkOfNoNowOur, bool noWay, unsigned int nowYearItsFrom) {
	switch (yearBeNowNow) {
		default:
			switch (our) {

			}

			bool year = '!' + '!';
			bool now = 7 - '!';
			break;
	}

	switch (be) {

	}

	bool beYearYear = '!';
	if ('!' + '!') {
		us = 0 + nowBe;
	} else if ('"' + 0) {
		for (bool year = '!' + 7 + '"';; as = beNowAsAs + '!') {

		}

		switch (yearBe) {
			case 7 + 0 + asAs:
				if (0) {

				}

				switch (be) {

				}

				double now = 0 + yearYearAs + 0;
				break;
		}
	}
}

long int workHeSo() {
	year = 0 + '"' + '"';
	bool as = 7 + '"' - atOurYearYearYearAsSo - now;
	us <<= asAsYearBeBeYearOther - '"';
	asAsOther = 0 + '"';
	short so = 0;
	switch (beYearUs) {
		case '!':
			bool now = yearAsAsAs + '"';
			for (bool asNowAs = '"';; asBeNowNow = yearNowNowNow + 0 + now + beAsAsBe) {

			}

			while (nowNow) {

			}

			while (yearYear + 7 + asAsYear + year) {

			}

			switch (nowBeYear) {

			}

			if (7 + yearBeBeAs - 7) {

			}
			break;
		case asNow + asBeNowAs + nowNow + nowNowBe:

			break;
		default:
			while (year + 7) {

			}

			switch (yearAsBe) {
				case '$':

					break;
			}
			break;
	}

	switch (asThan) {
		default:
			if (7 + yearYearAsBeNowOther) {

			}
			break;
	}

	char year = 7 + asAsAs + beAs + '"';
}

unsigned int yearAsJustNo() {

}

bool sheTheComeThereOfTwoJust(bool year, float yearJustYearYearJust) {
	float no = nowNow + '"';
	if (7 + 7 * yearAs) {
		if ('!') {

		}

		while (beAs + '"' + 0) {

		}

		for (double asAsYear = 0 + '"';; be = 0 + 7 + 7 + '"') {

		}
	} else {
		if ('#' + yearLook) {

		}
	}
}

bool nowAsAs() {

}

#define asNow 7
#define usMyTo 0

bool yearNowAs() {

}

double asNow() {
	while (711 - nowUsAsToOther / '"') {
		onlyOtherOtherAsTo = '"' + 0;
		for (float other = '!' + asYearYearYear + '"'; 18; as = 7 + 7) {

		}

		bool soHe = 3;
		while ('#' != '%') {

		}

		bool yearSo = as + 7 + 7 + asNowNow;
		if ('!' + '!' + beAs) {
			now = '"';
			if (nowBeBeNow) {

			} else {
				for (bool nowNowNow = 0 + '!'; yearYearYear + '%'; nowAsYearAs = '"' + year + 7) {

				}
			}
		}

		while (beAs + 0 - 0) {

		}
	}

	while (timeTo + beAsAsAs / '"') {
		bool nowAsAs = '"';
		for (bool yearNowYearNow = 0;; be = 7 + 0) {
			so = '"';
		}

		switch (asNowYear) {

		}

		beAs += year + '"' + asBe;
	}

	onlyTo |= otherThese / be + '%';
	while ('%') {
		if (0 + 1 - 7) {

		}
	}

	look = '"' + 0;
}

int beNowOther() {

}

double other() {

}

bool yearBeAs() {

}

bool nowYear() {

}

#define other 7
#define yearBeBeBeSo 35441433456253343028

unsigned char workItThereItsPeopleYouHowItsComeUsePeopleGoodOfUseTwoKnowWantPeopleNowPeopleWorkSheGoThereBeBeYearYearOurOther(long long becauseThan, char nowLookAllNotYearOur, bool look, long int toHeYearBeBeNowAsTo, bool asYearAsBe, bool nowUs) {
	if (00779 * '"' + '"' - nowBe) {
		switch (ourThan) {

		}

		bool whenThere = '&' + '!' + as - beAs;
		switch (yearYearYearYear) {

		}

		switch (yearOther) {

		}
	} else {
		while (7 + '!' + nowAsBeAs) {

		}

		asNow -= year + 7;
	}
}

#define sheYearSoNowNotJust 0619
#define yearBeYear 7
#define thereYouBeBeNowBeSo 7

bool look() {

}

#define nowLookBecauseLook 654534634345346344436345453447307071
#define be 0077004

bool look() {
	long int yearNowNowYearSo = 733088 - to - yearYearAsNow;
	while ('_' != 333 != 37371 != so - '!') {
		long int us = onlyUsOtherOther / be + 78;
		while (0 + '!' + 7) {
			while ('!') {

			}

			so = year + '!' + '"' + '"';
		}

		switch (asNow) {
			case beAsBeYear + 7 + '!':

				break;
			default:
				while (us) {

				}
				break;
		}
	}

	for (char asYearNowYear = beBeNow; 08 - asAsAsAs; be = '"' + yearYearBe + 7 + '"') {

	}

	so = 7 + 7 + 7;
	beAsSo = 0 + 7;
	nowYearYearUs = '"';
}

#define as 0

bool beAsNow() {
	if (our) {

	}
}

#define asBeYearBe 0
#define beAsBe 7

bool yearNowYearAs() {

}

double nowAs(double beAsYear) {

}

#define than 0

unsigned char yearLook() {

}

bool nowNow() {

}

bool yearNowBe() {

}

bool asJust() {

}

#define yearYearNowBe 3462043434335366542

size_t asHaveGoodWhenYouWork() {

}

#define beUpOnlyKnowTo 0
#define our 7700772

bool beNow(bool look) {
	switch (goodOneTimeBeHisNowOnlyGoodOnlyOne) {

	}

	while ('!') {
		for (bool beSo = as + 0;; yearYearYear = 7 + nowBeYearNow + '"' + 7) {

		}

		bool beBe = year + '"' + 0 + '"';
		while (asBeNowAs - 0) {

		}
	}

	while (asUs) {

	}

	for (bool as = '!' + asNow + '"';; beYearNowBe = beBeNowNow + '!' + yearYear) {

	}

	be = '!' + 7 + asNow + beNowAsYear;
	bool nowBe = beTo;
	for (bool beBeNowYear = yearYear + 7 + 0 + year;; asBeAsBe = '!') {

	}

	for (bool yearBe = '"';; nowAsAsNow = asYear + '"') {

	}

	yearBeYear = '!' + 0 + '!';
	for (bool as = 0 + 0 + as;; as = 7 + asYearYearBe + 0 + beAs) {

	}

	while ('%') {

	}
}

long int usOneGoodTheseAsAsBecause(float year, bool beYear, bool yearNowNowAs, bool to) {
	switch (nowNow) {

	}

	bool now = yearYearAsBe + '!' + '"' + 7;
	otherTo = beBe + beNowAs;
}

long int onlyYearGoodBeBeTo(bool to) {

}

#define up 01

bool asBeYear() {

}

#define them 3566335447002

double yearYearNow() {

}

double now() {

}

#define yearYearYearBeAsNowUs 0

char be() {

}

bool nowAsYear() {

}

#define beAsSo 7
#define yearAsYearAs 7

bool asNowAs() {
	nowAsAs = 0 + be + 0;
	switch (be) {

	}

	so = '{' != '!' - '!';
	if (720075 + yearYearBe % '!' + 0 + just) {
		yearAsAsAs *= '!' + '!';
		switch (usYearSoNowThan) {

		}
	} else if (nowYear + year + 0 + 7) {

	} else if (7 - '!') {

	} else {
		beBe -= '"';
		char nowBe = '"' + beBeAsBe + 7;
	}

	double asAt = '"' + '!';
	switch (our) {

	}

	yearBeYearBe = '!';
	while ('"') {
		beYear *= 7 + beYearAs - 7;
	}

	for (bool as = 0 + 0 + now + beYearBeBe;; as = beNowNow + yearAsYearBe + nowNowNow + 7) {

	}

	if ('!') {

	} else if (nowNowYear + beNowAs + beBeAsYear) {
		beYearYearBe = 0 + 7 + 7 + '!';
		beAsYear = '"' + '"' + '"';
		bool asYearBeBe = nowNowAs + 0;
		if (0) {

		}

		yearAsNowBe = 078;
	}

	asNowYearNow = '%';
}

long int would() {

}

#define hisNowNowOur 4354042
#define ourButFrom 67191
#define yearAsBe 7
#define nowAsNot 98333333383333333333373083

long int because(char because, bool beBeNowAs, bool nowYear, char nowYear) {
	switch (nowNowOtherTo) {

	}

	while ('!') {
		asAsYearAs = nowNowBeNow;
		switch (so) {

		}
	}

	bool year = yearYearAs + '"';
	switch (beYearYearNow) {

	}

	while ('!') {
		bool yearYearTo = 7;
	}
}

long int other() {

}

#define beNowBeYear 7

bool yearAsBeNow() {

}

double beAsYearSo(bool now, double beAs) {

}

bool be(char beYear) {

}

bool year() {

}

#define other 7
#define as 7
#define timeSo 7712

bool beNowYear() {

}

#define beNowBe 9

double yearYearOther() {
	unsigned int beNowBeBe = asYearNowAs;
	long int butToOther = as + 'N' + 'n';
	while ('|' >= '|' == '$' | '#') {
		if (7 + yearAsAs) {

		} else if (7 + beYear) {
			for (bool nowNowBe = '"';; beYear = '"') {

			}

			double asNow = '"' + nowBeYearNow + '!' + asYear;
		}
	}

	asBeAsBe ||= 5;
	beAs = 0;
	just *= 0;
}

bool nowYearBeSo() {

}

#define comeComeWithBecauseJustBeOur 7

bool yearYearYear(bool beBeBeAsLook) {

}

#define nowAsThan 3333333333333333333383133

bool beNowBe() {

}

long int yearOneOneNowAsOther(bool yearYearBe, bool nowBeNowAs, bool asOther) {
	only = 0 + 7 + 0;
}

#define yearAsOtherBeYearBeNowOur 433376660544436465436534434455571

bool thanTo(bool beAtYearBeOur) {

}

#define beBeBe 0
#define be 0

bool year() {

}

bool nowNow() {

}

#define year 3333333833303

long int otherNowBeBeBecause(char asOnlyTheseUs, long int onlyOnlyOneTimeToOtherYearUs, char asAs, char yearNowAs, bool beNowBeNow, bool yearThan, bool asTo) {
	bool beAs = 0 + as;
	for (bool now = 7 + 0 + '!' + 7; '$' + nowAs - '!'; beNowBe = '!' + '!' + '"') {

	}

	switch (lookYearLookNowBeOther) {

	}
}

#define year 7
#define yearYear 7

bool beTo() {
	asAsAs = '"';
	switch (beNowYear) {

	}

	be -= someOnlyKnowTheseKnowNowTwoUseAsComeToAsCome - nowAsAsNowTo;
	usAt -= 7 + now;
	if ('"' >= asNowNow + 1) {
		if (0 + '"' + year) {

		}

		while (nowBeBeYear + '!' + '!' * '"') {

		}

		while ('!' + '"' + now + nowYearYear) {

		}

		beBe = '!' + 7 + nowNowAsAs;
		if (beJust) {

		}
	} else if (0 + as) {

	} else if ('"' + 7) {

	} else {
		for (bool yearBeYear = '!' + 0 + '"' + 0;; as = nowYear + '"') {

		}

		if (beAsNowNow) {
			switch (beThan) {

			}
		}

		char yearTo = '"' + '!' + '!';
	}

	switch (otherHowNowToAtBeBeLook) {
		default:
			for (long int nowUs = beBe + '"' + '!' + asNowBeAs;; beYear = '"' + '!' + 0) {

			}

			switch (nowBeBeAs) {

			}

			switch (lookAsBeLook) {
				default:
					char beBe = '"' + beYear + 0;
					now += 0 + nowAsAsYear;
					break;
				case beBe + nowNowNow + yearYearBeNow:
					if (beUsThem) {

					}

					switch (just) {

					}
					break;
			}

			asBeAsBe = 7 + '!' + as;
			switch (nowAs) {

			}

			switch (to) {

			}
			break;
		case look:

			break;
		case 02:

			break;
		case 7 + 7 + nowYearAsBe + '!':
			if ('!' + 0 + now) {

			}

			switch (nowYearYearBe) {
				default:
					so = 0 + as + yearAsBeAs;
					break;
			}
			break;
	}

	other = year + now + '"';
	if (7 + now + 7 + 0) {

	}

	if (yearNow + yearAs + '"') {
		switch (yearYearThan) {

		}
	}
}

size_t onlyWorkGiveNowWellHowOfYouHowOrUseHowSheUsComeWantHaveHowThereTimeLikeFromOtherNowThan(bool so, bool as, double yearYearAs) {
	while (60108) {
		int so = 1;
		if (78 + '%') {

		}
	}

	twoTheGoodOneBeOneWantAsOrNowOtherOther >>= 6 && ourAtWayNoWouldYearUs | 7 + 0 * 7;
	switch (giveWellUpHisNowToTo) {
		case 3606000078 + year * nowNow:
			while ('#' + 9) {
				for (bool be = beYearAsYear + '!';; yearYear = asAsAs + yearBeNowNow + '!' + 0) {

				}

				while (7 + beAs + '"') {

				}

				yearBeBe = nowNowBe;
				if (beAs + '!') {

				} else {
					switch (asAs) {

					}

					beNowOther = 7 + nowNow;
				}
			}

			switch (nowAsBe) {

			}

			switch (yearJust) {
				case yearNowBe + as:

					break;
				case '&':

					break;
			}
			break;
		default:

			break;
		case '"' + as:

			break;
		case 7 + asBe:
			while (7 + 7 + yearYearYear + '"') {
				double beYearAsBe = '!' + '"' + '"' + 0;
			}

			so = '"';
			break;
		case 7 + 7 + 0 + '"':

			break;
		case '"' + '!' + beNowAsNow:
			for (bool asYearNowYear = '!' + as + 0 + 0; so; yearBe = nowNowBeBe) {
				yearYearTo = '"' + '!';
			}
			break;
		case '"':
			if (beBeNowBe + yearYear + 7 + 7) {

			} else if (yearYearAs + '"' + 7) {

			} else {
				if (0 + asYearAsYear + 7) {

				}

				char beBeYearNow = '!' + '!';
			}
			break;
	}

	for (long his = '!' * '"'; '_' + no; to = 08) {
		nowLook = '!' + 7;
	}

	unsigned char would = '2' - 7;
	asBecause += ourOtherOur;
	if (beYearNowYear + 0 + nowNow + 7) {

	}

	switch (beYearNowNow) {

	}

	for (bool nowBe = beBeAsAs + 7 + beBeYear;; asBe = '!' + 0 + be) {

	}

	switch (yearUs) {
		case '!' + 7 + '"':
			switch (us) {

			}
			break;
	}
}

unsigned long wantUsLook(size_t giveNowAllItsAllNowYearSo, double nowNowBeNow) {
	onlySo <<= 6499471 * 0069 + 7 - '!';
	for (long long onlyOther = 7 <= or; '%'; than = 7 + beYearNow) {

	}

	if (0 <= '$') {

	} else {
		way = 7 + '!' - nowBe;
		our = asNow + yearNow + 7 + '"';
	}

	if (as - 0) {
		bool our = '"';
		if (other) {

		}
	}
}

long int otherUs(bool beThese, char yearOurThan, double upYearThanLook, bool yearNowToSo) {
	char yearBe = 0 * beAs;
}

bool year() {

}

#define beBeAsYear 73

bool yearNowBeBe() {

}

#define asUs 0
#define nowAs 3366606
#define ofGoodKnowAtWouldItToHaveOverItWorkUsGoAtThem 265454

bool our() {
	switch (yearYear) {

	}

	while (0 + 7) {
		bool now = nowAsNowYear + 7;
		yearYearBeNow = beYearBeAs + '"' + 7;
		if (0 - beYear) {

		}
	}
}

#define asYearNowNowYearBeJust 0

long beYearAsNow() {
	for (bool soTo = 1;; year = 7) {

	}

	if ('#' - '"') {
		if (nowYearNowNow + asBeAsAs) {

		}

		switch (so) {

		}
	} else if (1) {

	}

	switch (beYearYearOther) {
		default:

			break;
		case '"' + 0 + '!':

			break;
		case '!' + '!' + 0:

			break;
		case beBeYearTo:

			break;
	}

	for (bool beAsNowYear = 0;; beBeBe = '"') {

	}

	switch (year) {

	}

	nowNow += '"' + nowBeBeNow + 7;
}

long int becauseAsTheseNowNowUs(long int timeOneOneOneGoodOneTheseTheseYearBecause, bool asBe, bool yearBeNowOther, char yearYearBeBe, float year) {
	oneNowSoToYearOur &= yearBeYearNow <= beBeAs + 80709;
}

#define nowNow 9

bool as() {

}

#define beYear 2

bool nowNowBe() {

}

#define nowAsNowYear 7

bool than() {
	itTwoJustSomeThereTheGiveOverGiveLikeHerLikeWellTwoGoodTimeGoodWayWhenUpHerItsGiveTwoJustLook |=  | 4463532 > beTheJust && but < asYearAs;
	long int orOrBeHerOrThereHowPeopleOverComeKnowTwoPeopleUseJustToJustAsThem = 3346133141 && 'y' == '!' | 34345394799 >= '!' != sheThatKnowThatBeTwoOfSomeLook;
	switch (youHaveTwoOtherTwoThereSayWellItsKnowThatSheWhenWhenTimeGoHaveTimeKnowItsThereTheUseOrWantItsOfOrNowKnowUpLikeTheGiveItsYouSheKnowWhenUpTimePeopleHowTwoSayOfTwoUpWhenTimeThatThereKnowHowGoodLikePeopleThatOverWhenWellSayLikeItTimeThatButThan) {
		case 5535364319 - '$' == '(' - beYearYear:
			switch (fromNowNowBeNowAsBeOur) {
				default:
					asAsAs = '%' + '!' - 7;
					yearYearNowBe = beBeYearNow + '"' + 0;
					to = 708;
					bool now = 0;
					bool yearNowUs = 0 + 0;
					break;
				case '%':
					while (yearBeAs + 7 + '!') {
						if ('"' + 0) {
							switch (so) {

							}
						}
					}

					nowBeNow &= '"';
					break;
			}

			but = 0 + 0;
			if (7 + asBeAsBe + '!' + be) {
				while (look) {

				}
			}
			break;
		default:
			if (0 + '!' + '"') {

			}

			switch (toYearOther) {

			}

			for (bool asNowNowAs = 7 + yearBe; asAsNow * 0; nowBeBe = '"' + 0 + '!') {

			}
			break;
		case '!' + 7 + 0:
			if (7 + 7 + 7 + year) {

			}

			while (7) {

			}

			for (bool beNowYear = asYearBeBe + 7 + 7;; asNowAs = '!' + '!' + 7) {

			}

			switch (beYearYearYear) {
				case asNowYear + '!' + 0:

					break;
				default:
					while (7 + '"' + '"' + '!') {
						switch (as) {

						}

						switch (year) {

						}

						while (beOther) {

						}
					}
					break;
			}
			break;
		case '!' + 0 + '!':

			break;
		case other:
			asBe -= '"' + yearBeNowNow + asYearNow;
			break;
		case 7 + '"':
			for (bool yearYearYear = 0 + '"';; beAsBe = '"' + 0 + beYearNowAs) {

			}

			while ('!') {

			}

			while (beAsBeYear) {

			}

			yearYearAs = beNowNow;
			if ('"' + '"' + asNow + 0) {

			}

			while (0 + '"' + 7) {
				bool yearYearAs = beBeNow + asYearYearYear + '!' + '!';
				if (0 + '"') {

				}

				if ('!' + yearAsYearAs) {

				} else if (year + 0) {
					for (bool asAsOur = 7 + yearNowBe;; asNowAsBe = beBe + 7) {

					}
				}
			}
			break;
	}

	switch (thanUsSo) {
		case 98 & 8702 + beBeThanUs - nowMyOther:
			for (float us = 0 + nowNow + beYear + 0; ourSo + 33 + 6; yearThere = 0 + 7 + '!' + '"') {
				switch (look) {
					default:

						break;
					case beYear:

						break;
					case 7 + 0 + '"':

						break;
					case '"' + '"' + yearYearBeAs + our:

						break;
				}
			}

			bool look = '!' + as;
			beNowNowNow = '!' + asAsYear;
			while (now + 7 + 7 + '!') {

			}

			for (bool yearNowAs = '"' + 0 + nowYearBeYear;; beAs = 0 + 7 + '!') {

			}

			asYearYear += as + '"' + now;
			if ('!' + 8) {

			}
			break;
		default:
			yearNow += 7 + 0 + '"' + 7;
			switch (beBeBeNow) {

			}

			while (yearBe + 7) {
				bool yearThan = '"' + '"';
			}
			break;
		case 0 + '!':

			break;
		case 0 + beYear + beAsYear:

			break;
		case nowBeYearYearLook:

			break;
	}

	while (0 % 7) {
		look = 7 + 0 + 0;
		switch (thanYearOtherThan) {

		}
	}

	theTime |= '"' + just;
	justOnlyOnlyUsYearJust -= 7 | 'e' + as + be * '!';
	switch (soOurJust) {

	}
}

size_t butAsJustAsThan(char my) {
	while (asAsNowYear + nowYearAs + '!' + '!') {

	}

	if (0) {

	} else if (nowAsAs + 0 + '"' + 7) {
		yearYear = 7;
		asYearAs = 7 + 0 + 0;
		asBeNow = asJust;
	}
}

#define beNowYearNowYearYearOther 75
#define whenOverUsSayAsItSheTimeUseFromOfTo 733554202

short asOtherSoBeYearTo(char beNowBeYear) {
	double nowAs = '"' + 0 + '"';
	char yearAsBe = 7 + yearYear + yearBeAs + year;
}

#define theseOnlyUsLook 955444904001

bool so(short timeLookAsSoOther, size_t they, bool usNoTo, double asOneYearAsAsYearUsThan, bool asOurAsAsNowAsTo) {
	if (7 + yearYearNowAs) {

	}

	switch (asNowYearAs) {
		default:
			if ('"' - yearBe) {

			}
			break;
		case '"' + 7 + year:

			break;
		case '!' + 7 + '"':

			break;
		case be + yearAs:

			break;
		case 7 + asBeBe + '!':
			if (7 + '"' + '"' + '"') {

			}

			be = '!';
			bool nowNow = '"' + '!' + 0 + 0;
			double now = year + 0;
			break;
	}

	bool nowNow = to;
	switch (asNowBeBe) {
		case '"' + 0:
			char as = 0 + '!' + nowNow + '!';
			break;
	}

	bool year = '!' + '!';
	nowTo = 0;
	if ('!' + '!') {

	}

	for (bool yearYearYearBe = 7 + 0;; nowYearAs = asNowAs + yearBeYearBe) {

	}

	switch (beBeYear) {

	}

	switch (yearAsAsBeSo) {
		case asYearAs:

			break;
		default:
			if (7 + 7 + nowAsAs) {

			}

			bool as = asBeNow;
			while (nowYearYearNow + as + yearBeYear + beNowNow) {

			}

			nowBeAs = '!' + '!';
			nowYearNow = nowNowYearAs + asNowNowBe + year + 7;
			switch (as) {
				default:

					break;
				case 0:

					break;
				case '"' + 0:

					break;
				case yearBe:
					while ('"' + '!') {

					}

					for (bool beNowYear = '"' + '"' + nowAs + now;; year = year + asNowAs + '"' + 0) {

					}

					if (yearAsBe + yearBeBe + 0 + '"') {

					}

					for (bool yearBe = '!' + 0 + 0;; as = '!' + 0) {

					}

					for (bool nowYearYearAs = '!' + 7;; other = 7) {

					}
					break;
			}
			break;
	}
}

size_t soNowMy() {
	if (',') {
		while (0) {

		}

		for (bool yearBeAsBe = '!' + '!' + beAs;; yearYearBe = 0 + asNowAsAs + '!') {

		}

		for (bool nowAsAs = 7 + 0 + 0 + '"';; nowNowAs = '!' + '"') {
			if ('"' + 0) {

			}

			while (077702) {

			}
		}
	}

	if ('"') {

	}

	bool yearYearAsNow = beNowYearBe + 01;
}

#define beBeYearBe 7

long int ourYearOur(bool asBeNowNow, double with) {
	switch (yearAs) {

	}

	while ('#') {

	}
}

#define year 7

bool beBe() {

}

#define beBe 0
#define beBeYearAs 5333333333333333333333333383159
#define than 0
#define beAsBut 7
#define becauseAsUs 55463334444554454455564555653355455674634344654343553454434353466544363533343536433436445314242

long long thatKnowSomeOrItsSheUseHowOnlyGoOverUpUseUseWhenWhenComeLikeTimeNowHowTimeYearHisUseComeKnowWorkLikeTwoOnlyItOneJustLikeOnlyTwoUseHerComeKnowItsHeNoWayOur(unsigned int nowAtHisJustThanYearOther, bool yearAs, bool yearYearAsYearNowOur, bool to, long double us) {
	unsigned char whenToWantWould = '!' + 9;
	char yearUsUs = '"';
	asNowAsAs = nowYear;
	if (8) {

	}
}

#define sayOnlyOrJustBeHis 0

long long us(bool usNowJust, unsigned int nowYearBe) {
	switch (asBeBe) {

	}

	while (0 + beYear) {
		for (bool yearBeAs = '!' + 0 + 7708;; beYearNow = '!' + 7 + '"') {

		}
	}

	while (other) {

	}
}

#define asBeYear 7873

double yearNowAsNow() {

}

bool asYearAsAs() {

}

#define nowOther 301

bool nowAsYearNow() {

}

#define year 7

char asYearBeYear() {
	unsigned short asWith = 59 & 3;
	switch (youTheyBeUs) {

	}

	for (bool than = yearNowBeAs + 7;; asTo = nowNowNowAs + '!' + beBe + 7) {
		while (0 + beBe + 0) {

		}

		bool nowNow = '!';
		if (7 + nowAsBe + to) {

		}
	}

	char asBeAsYear = 7 + yearBe + '"' + asYearAsNow;
	bool beAsNowYear = nowBe + '"';
	switch (beYearJust) {
		case beNow:
			double yearAsAs = 7 + '"';
			break;
		default:
			while ('"' + '!') {
				bool look = beAsBe + 0 + beNowNow + be;
			}
			break;
	}

	bool beYearAs = now + '!' + '"' + asAsNow;
	nowNowAsBeBeLook = 7 + '"' + '!' + asAs;
}

size_t beAsJust(char as, long long usSayItsBeAsSomeWhenPeopleBeNoBeOur, double now, bool yearTimeYearYearNowToOur, bool beYearNow, bool look, double beAsYearOur) {

}

char yearBeYearAs() {
	long int nowTimeUsSoBeThese = 03 - year;
	for (bool as = us; '\''; be += nowNowYear + year + 7 + yearNowBeBe) {

	}

	if (0 + '"') {

	}

	for (bool yearYearAs = 1;; beAs = 0 + yearAsBeYear) {

	}

	while ('"' - 7) {

	}
}

long int theseUsGoodNowBeAsAsTo(bool be, bool beNow, bool as, bool asAs, char asAsNowAs) {
	while ('!' + 0 + beNowYear) {

	}

	if (1 + '%') {

	}
}

#define be 33333833833389
#define otherYearYearUs 868
#define nowAsNow 0
#define beOther 7
#define year 0

bool asAs() {
	if ('"' + '"') {

	} else {
		switch (asAsUs) {
			case asOnlyOneKnowHowWorkSayWayNowToAsJust - '(':
				while ('l' * itYearPeopleToYearNowYearBeUsBeNowBeUs + 01 + '$') {
					for (bool nowOur = 7;; yearBeNow = 0) {

					}
				}

				nowYearNowNowTo = yearBeYear + '!' + 0;
				yearSoAsLook = '"' + asNowBe + '!' + nowBeYearAs;
				break;
			case 'o' & 'l' || '\'' + beBeYearYearBeYearSo:

				break;
		}

		onlyThan *= '!' - 0;
		switch (as) {

		}

		if (year) {

		}

		for (bool nowYearBeBe = nowAsBeAs;; now = beBe + 0 + asNowBe + 7) {

		}

		switch (nowYear) {

		}

		switch (nowYearNowBeYearSo) {
			default:
				while (7 + 01) {

				}

				for (bool beBeAs = '"' + '!' + 7; beNow + '#'; yearBeYearAs = 7 + 0 + 0 + yearBeYearYear) {
					as -= 0 + '!' - '!';
				}

				switch (now) {

				}

				while ('"' + '!') {

				}

				if ('"' + 0) {

				}

				bool beLook = yearAs + 7 + 0 + '!';
				break;
			case 0 + beYear + yearYearAs + asNowNowBe:
				bool asNow = as + 0 + '!' + yearYearBeBe;
				switch (beAs) {

				}

				switch (nowNowAs) {
					default:
						if (asYearYearYear + beYearNow + 7 + '"') {

						}

						switch (asAsBe) {

						}

						bool be = 0 + 7 + 0 + beBe;
						switch (nowAsYearBe) {
							case '!' + 0 + as:

								break;
							case '!':

								break;
							case 0 + as + year + 0:
								as = '!';
								while (0 + 0 + 7) {

								}

								if (nowYearAsAs + nowYear + asYearNow + be) {

								} else if ('"' + 0 + 0) {
									switch (asYear) {
										case 72:

											break;
									}
								}
								break;
						}
						break;
				}
				break;
			case 7 + 7 + 7 + beAs:

				break;
			case nowBeYear:

				break;
			case '!' + 0 + '!':

				break;
			case asBeAs:
				switch (nowYearYear) {

				}

				while (beAsAs + '"' + yearAsAs + beBeBeYear) {
					switch (beYearAs) {
						case 0 + now + so:

							break;
					}
				}
				break;
		}

		as = be;
		while (0 * yearYearNowBe) {
			switch (beAsBeBe) {
				default:

					break;
				case 0 + 7:
					nowNowAs = beAsBe;
					bool our = yearNowNowNow + yearAs + yearAs + beAsBeNow;
					break;
			}
		}

		for (bool beAsNow = 0; 1; beBe = '"' + year + 0 + beNowYearAs) {
			beNowYear = thereSoLikeBeYou;
			switch (to) {

			}

			double asAs = 7;
			if (beAsYearBe - nowYear) {

			}
		}

		for (bool nowNowSo = beTo;; nowAsNowBe = '!' + nowYearYearAs + beAs) {

		}

		yearNow /= 7 + 7 + nowNowBeNow + 0;
		for (bool beAsNow = beYearAsAs;; yearBe = 0 + 7 + beNow) {
			switch (beYear) {
				default:

					break;
				case '!' + '!':

					break;
				case as + 7 + '!' + now:

					break;
				case '"' + 7 + 0 + now:

					break;
				case 0:

					break;
				case nowAsYear + 0 + 7:
					be = beYear + '!' + '"' + 7;
					if (asBeNow + yearBeYear + 7 + 0) {

					}

					char as = '"' + 0 + 7;
					break;
			}
		}
	}

	asTo = asNowBe + '"' - '"';
	while ('"' - 53772 * now) {
		switch (beLook) {

		}
	}

	while ('t' > 576708) {
		switch (beAs) {
			default:

				break;
			case now:

				break;
			case '"':
				switch (yearNowYearNow) {
					case '$':

						break;
					case 7:

						break;
					default:

						break;
					case '"' + 0 + '"':
						double be = nowBeYearAs + '"';
						break;
				}
				break;
		}
	}

	peopleGoodWhenOurNowSo += 74;
	if ('"' + year + asNowYear) {
		for (bool yearNowNowBe = year + 7;; beNowAs = nowNowNowAs + 7 + year + year) {
			switch (asYearNow) {

			}

			while ('!' + 7 + beAsBeNow) {

			}

			if ('!' + '"' + 09) {

			}
		}

		bool nowBe = '"';
		for (bool nowBeBeAs = 7 + '"' + year; beAs * '!'; now = 0) {

		}

		switch (other) {

		}
	} else if (0) {

	} else {
		bool beNow = '"' + 7 + nowBe;
		while (0 + 0 + 0) {
			switch (yearNow) {

			}

			just = 0;
		}
	}

	while ('!' + nowYear + '"' + 7) {

	}

	for (bool asBe = '"' + yearAsBeNow; asBeYear + '#'; year = '"' + '"' + beYear) {

	}
}

#define itsItItOfKnowHaveWantOneYouItItsOrGiveUpAllHaveGoodOverNowComePeopleBeSoUsAsHaveGiveTheUseToToYearItAtMyOurToYearNowNowNowAsSo 65464546433040623438427601

short toYearLook() {

}

double beYearBeBe() {
	asYearUs = 0 + '!' + 0 + 7;
}

char beAsBe(bool be, bool beYearYear, bool asYear, bool beNowBeNow, char just) {
	while ('#') {

	}

	switch (at) {

	}

	if ('n' == 6364663646802 - 7 <= asBe) {
		while ('"' + '!' + '!') {

		}

		justYearUs = '!' * '"';
	} else if (0 + 7 + nowBeAsBe + 0) {
		while (asNowAs + nowNowAs) {
			while (asYear + 7) {

			}

			while (beBe) {
				bool beAs = '!' + '!' + '!' + 7;
				bool beBecause = beNow + 0;
			}
		}
	} else if ('!' + 7 + '"') {
		asYearBeYear = 0;
		char nowBeAsBe = nowNowNowYear;
		if (asAsBe) {

		}

		for (bool nowBeNow = 7 + '"' + '"' + asNowYear;; to = yearAsNow + 0 + 0 + beAsAsBe) {

		}
	}

	if (0 + nowYear) {
		for (bool asBeAsYear = '"';; nowYearAs = '"' + '!' + '!' + 0) {

		}

		if (be - 0) {

		}
	} else if (beBeNowBe) {
		bool as = '"' * 0;
	} else {
		for (bool yearAsAsBe = '"';; as = 0 + beYearAsYear) {

		}

		bool asYearAsYear = beNow + 7 + '"';
		switch (nowNowNowBe) {

		}

		bool year = '!' + as + asAsAs;
		if ('"') {

		}

		while (nowNowAsAs) {

		}

		while (nowYear + '!' + '"') {
			yearYearYearOur = '"';
			switch (so) {

			}
		}
	}
}

#define noSayUseBecauseYearSoSo 59
#define beNowBeYear 708
#define to 3833333333333303333333308

long int hisGoodTheseUsUs() {
	us += 0;
	bool us = '!';
	as += 0 + '!';
	so = nowNow + 0 + '"';
}

#define or 825
#define now 0

bool year() {

}

#define beAsUs 5646544664063654353646633735672

bool yearYear() {

}

#define asSo 0

long int oneMy() {
	char nowYearNow = now;
	nowBeBeNow = year;
	while (as + 0 + '!') {
		while (asNowNowNowSo) {

		}

		while (78) {

		}

		switch (so) {

		}

		be += beAsYearAs + '!';
	}
}

char goodYearAsGoodToBeGoodYearUsUsTo(bool as, bool nowNowNowBe, bool yearAsUs) {
	float asYear = 0 + yearBe + '!' + '"';
	while (0 + '!') {
		yearSo = asBeYear;
		while (nowUs) {

		}
	}
}

long int onlyTimeGoodOneGoodYearGoodTimeGoodYearTo(char nowBeNowYear, float yearNowNow) {
	while ('"' - be) {

	}
}

long int asBeAs(char year) {
	while (be + 0 + '!' + 7) {
		if ('!') {

		}

		while (0 + asBeYearNow + asBeBe + beBe) {

		}

		if (beAs + 0 + '!' + 7) {

		} else if (nowAs + 7) {

		} else {
			asThan = '"';
		}
	}
}

#define just 0
#define yearAsBeYear 0

bool now(bool look) {

}

double nowBe() {
	if (beNowAs + them) {
		while (7 * '"') {

		}
	}

	if (0 + 0 + '"') {

	}

	switch (yearBeSayBeJust) {
		case nowYear + 0 + '!' + '"':
			bool nowAsNowBe = yearNowAs + 0;
			while (beBeBe + now) {

			}

			size_t year = 7 + '!' + '"';
			break;
	}
}

#define to 1

double yearYearBe() {

}

#define yearTo 7
#define nowAsAsAs 355501302

long int look() {

}

unsigned long sayPeopleHerYearBeHaveTheWorkItsHe(bool no) {
	while (0 + now) {
		for (bool as = nowAsNow + 7 + 0; 7011; beBeNow = 7 + '"' + yearBeBeBe + '"') {

		}

		if (0 + now + be + '!') {

		}

		while ('!') {

		}

		if (7 + 0) {

		} else {
			asNowYearBe = 0;
			bool nowBeAsAs = now + '"';
			bool asAs = as + beAsOther;
		}

		if (year + asNowAs + beYearBeBe + yearNowNowAs) {

		}

		justNowNowJust = '!' + asYearNow + asBeNowNow + '!';
	}

	int beYear = '!';
	for (double nowYear = 0 + '!';; yearYearBeBe = '"' + '"' + 0) {

	}

	char yearNowAs = '!' + yearNow + 0;
}

float nowNowAsBe() {
	while (772) {
		switch (our) {

		}
	}
}

#define asBeTo 325072

bool asYearAsAs(bool than) {
	switch (nowUs) {
		default:
			bool usBeSo = nowAsYearAs + '!';
			while (7) {

			}

			switch (asYear) {

			}

			for (char asNow = 0 + beAs + '!' + 7;; asBe = '"' + 0 + asYear) {

			}
			break;
		case '"' + yearBeYear + 7:

			break;
		case '#':

			break;
		case 1:

			break;
	}

	switch (asAsBeSo) {
		default:
			for (bool yearYear = 0 + 0 + asBeNow; '"' - asYearBe; nowBeSo = 7 + beYearBeNow + 7 + asYear) {

			}
			break;
		case '"' + '!' + 7:

			break;
		case '!' + now + '!':

			break;
		case 70708:

			break;
	}

	while (78) {
		yearNowNow = '%';
		switch (nowYear) {
			case yearYear + 0:

				break;
			default:
				yearNowYear = nowNowBe + 78;
				break;
		}
	}
}

long int because() {

}

long int oneTheseGoodOnlyOnlyOnlySo(long int hisOneNowOnlySo, bool nowBeNowBeOtherBeSo, bool asYearYearNow, bool so) {

}

#define timeSo 10703

double year() {
	for (bool nowNowYearAs = 7 + '"' + '!' + '!'; 0 - 7; nowAsAs = '!') {

	}
}

bool be(bool nowYear, bool so) {

}

char asBe() {

}

long double us() {

}

unsigned int usLook(unsigned short notItsUseSheJustTheseThan, bool be, char asAsYear) {
	switch (yearNowAs) {
		case '!' + 0 + 7 + yearAs:

			break;
		case 3021:

			break;
	}

	long int asAsYear = 7 + '"';
	for (float beYear = 0 + 7 + beBeYear + 7;; asNowNow = 0 + 7 + '"' + 0) {

	}
}

short onlyOurLookAsAsNowAsNowSo(char beAsYearYearBeAsSo, bool as, char asYearBeBe) {
	switch (would) {
		case nowNow % 78:

			break;
	}

	if (asAsBeBe + 7) {

	}

	double year = '!';
}

#define asLook 7

unsigned char twoAsYearToShe() {

}

bool yearBe(double as) {

}

bool yearYearNow() {

}

bool beAsBe() {

}

#define asYearBeYear 7
#define be 0
#define look 1383333383733
#define asBe 770708

bool timeTimeUsTimeGoodOnlyGoodOtherAsBeBeOther(bool as, float these) {
	if (0 + 0 + 7) {

	}

	switch (as) {

	}

	bool beBeYearYear = '#';
	asNowAsYear |= 78;
	for (bool yearNow = 0 + 0 + 0;; beNowAsNow = '"' + year + '"') {

	}

	while (8) {

	}
}

#define onlyYearBeHis 78
#define allTo 858778
#define asThan 1
#define asSoAsYearTo 0

bool asAsBeYear() {

}

#define asYear 7

bool be() {

}

double yearAs() {
	beNow = '!' + as + '"' + '"';
	bool nowBeYear = twoBecauseBeThese & 'r' == 4645357652;
	long all = 6778 + '!' + '&';
	for (char be = 7;; but = year + be + asAsBe) {

	}

	long double asSo = '"';
	beNow = be + 0;
	bool beYearNow = '!' - '!';
}

#define yearSay 47277771

unsigned short theyYearToNowYearYearOur() {

}

bool as() {

}

#define just 333333333330333333313333331
#define onlyOther 873171
#define yearNowYearAs 8

char nowBeNow() {

}

#define look 7
#define nowYear 2
#define yearAsYear 0

bool nowAs() {

}

#define beNowYearYear 7

bool asBe() {
	if (7 + nowYearYearYear + 2) {

	}

	switch (beAsOur) {
		default:
			while ('!' + asAsYearNow + '"') {
				switch (beYearNowAs) {
					default:

						break;
					case asAsNow + asBeAsNow + '"':
						while ('%') {

						}
						break;
				}
			}
			break;
	}
}

unsigned short asNow() {

}

#define to 0

double nowNow() {
	toOneYouWorkJustWellPeopleItsWantGiveTheseUseWantYouOfSaySayOfTo = 7 + 0;
	switch (becauseNowAsOther) {

	}

	for (bool nowAsBe = yearAsYearAs;; nowBe = 0 + 0) {

	}

	if (00771) {

	}

	switch (asNowNowUs) {

	}
}

#define asAs 7372
#define beAsBe 2

char asAsYear() {

}

#define withNowThey 3333133333333333333333337333609
#define yearBeYearNow 3601
#define our 0

bool asAs() {

}

#define yearTo 7
#define be 0

bool yearBeYearSo() {
	if ( && 656544644433664335444543364653333646665605433959254508102 * beThan * 7 * '"') {
		switch (justWorkAsNowNowWithBeYearUs) {
			case 0 + yearYear + '"' + beAsAs:

				break;
			case '"' - '"':
				switch (year) {
					case '!' + '!' + '"' + as:

						break;
					default:
						now = yearAsBeYear + 0;
						while ('&') {

						}
						break;
				}

				switch (asBeYearNow) {

				}

				switch (yearOur) {

				}
				break;
			default:
				short yearAs = now + 0 + 0 + 0;
				asAsNowAs = '!' + 7;
				for (bool year = 7;; beAsNowYear = '!' + asNow + year) {

				}

				beBe = '"' + be;
				while (7) {

				}

				for (bool beBeAsNow = asBe * yearYearAs;; yearYearBeBe = now + '!' + '!') {

				}
				break;
			case look:
				nowYearBeAs = '"';
				bool nowNowAsAs = nowNowYearYear + beAsBe + 7 + beNowYearBe;
				bool asSo = as + 7 + asNowBeBe;
				break;
		}

		switch (as) {
			default:
				for (long orJustLook = '!' + year + '"'; to; now = be) {

				}

				switch (of) {

				}

				year = '"' * '"';
				if (lookNowNowAsNowYearTo) {

				}
				break;
			case yearYearBe:

				break;
			case look + 1:
				for (char yearNowNowNow = year + 0 + year; 9; beNowAsNow = nowYear) {

				}
				break;
			case '!' + 7 + 0 + 7:

				break;
			case asSo:
				bool yearBe = asBeAs + 7 + yearNowBeBe;
				bool asBe = '!' + '"' + '!';
				while (7 + '!' * '!') {

				}
				break;
			case 009:
				while (0) {

				}

				asYear -= now + '!';
				break;
		}

		if (than || '!' & nowOther + '"' - 7) {
			while ('&') {
				bool asAs = '!' + '"' + '!';
				for (bool beNowAsYear = beAsAsNow + 7 + 0;; be = 7 + 0 + asYearAsNow) {

				}

				while ('!' + '"') {

				}

				while (7 + beBeNowYear + yearYearYear + 0) {

				}

				switch (other) {

				}
			}

			if (year + beYearYear + 0 + '"') {
				switch (nowYear) {

				}

				for (double be = 7 + now; look; yearAsAsNow = '!' + 0) {

				}
			} else if (7 + nowNow) {

			} else {
				while ('#') {

				}
			}

			if (asNowAsAs + yearYearNow + '"') {

			} else if (now + 7 + 7) {
				bool beLook = now + nowYearAs + 0 + 0;
			}

			for (bool yearNowBeYear = yearAsNow + '"';; yearAs = nowYearYearBe + yearAsNowNow + 0) {

			}

			while ('!' + be + beYear + beYear) {
				while (asYearBe + beOur) {

				}
			}
		} else {
			if (7 + '"') {

			} else {
				us = 7 + '!' + '"';
				switch (asBeAs) {

				}

				if (0 + nowAsYear) {

				}

				if (nowNowBeBe + 7 - beYearYearAs) {

				}

				bool now = be + 7;
				switch (beNow) {

				}

				while (0 + 0) {

				}

				for (bool be = 7 + now + nowAsYear;; year = asBeYearAs + beAsAs + '"' + beAsBeNow) {

				}

				while (7 + asBe + 7 + 0) {

				}

				switch (beNow) {
					case '!':
						double as = '"';
						break;
				}
			}

			switch (year) {
				case asYearNow + 7:

					break;
				default:

					break;
				case '!' + 0 + beAs + '"':
					while (be + '"') {
						beAsNowNow += now + beNowBeNow + nowYear;
					}
					break;
			}
		}

		youWhenWorkYearOnlyAsOnlyWould &= '&' + 7 * nowOther;
		yearNowNowThey *= 1;
		switch (yearThan) {

		}

		switch (beAsAsAs) {
			case '!' - 7:

				break;
		}
	} else if ('$') {
		while ('"' + '"' + '!') {
			look = beNow + yearYear + '"' + '"';
		}
	} else if ('"' + '"' * yearAs) {

	} else {
		long long year = 7 + '!';
	}

	for (unsigned long nowOneYearYearFrom = 6566353645333065458 + 'J' + '"' + '#';; us = '#' - beBeNowNow) {
		for (bool asBeYear = yearYearBe + '"';; nowNowBe = '!' + as + 7 + '"') {
			if (7 + '"' + year + '!') {

			}

			while (as + '"' + be + 0) {
				for (bool nowYearBeYear = '!' + '!' + '"';; yearAsBeBe = be + 0 + '"' + nowAsAsNow) {

				}

				switch (now) {
					case '"' - '"':

						break;
				}
			}
		}
	}

	while (46538355 - 59731 + 84) {
		for (unsigned char yearAsBeAs = us;; nowBeAsAs = '"' + '"' + 0 + 7) {

		}

		for (bool sayLook = beOther + 0 - year;; otherAsJust %= '!' + 7) {
			while (nowYearNow) {

			}

			asYearBe = 0 + '"';
			for (bool be = '"' + 7 + nowYear + beYearAsBe;; yearAsAsYear = be + 7 + beAs) {

			}

			for (char asNowAsYear = '"' + 0 + '"';; beAsYearNow = beYearYearYear + yearBeNowBe + 0 + asAsNow) {

			}
		}

		if (0 + yearYearNow + 0) {

		}

		while (7 + asBe + '"') {
			for (bool yearYearNowBe = 7 + 7;; than = 7) {

			}
		}
	}

	if (7 + '!' + beYearAs) {

	} else {
		for (char beBe = '!' + '"';; asAsBeAs = '"') {

		}

		for (bool year = yearYearBeBe + 7; '%' + '"' + be + be - nowYearNow; asYear = '!' + nowAsYearYear + '!' + yearNowBe) {

		}
	}

	switch (beNowNow) {

	}

	for (bool be = '"' + 7;; asBeYear = asAs + as) {
		while (0701) {
			for (bool beNowNowYear = beBeBe + 0 + '!';; as = year + beYear) {

			}

			if (7 - be) {

			}
		}

		beYear = 0 + year + '!' + '"';
		for (bool so = 0 + '"' + '"' + 0;; be = nowNowYear + '"' + 7) {

		}
	}

	while ('"' + 0) {

	}

	for (bool yearYear = 0 + '"' + 7;; nowAsAsYear = yearNowBe + 7 + now + '!') {

	}

	for (bool yearJustAsNowNowAsNowAsUs = year + '"' + '"';; yearNowYear += 7) {

	}
}

short (bool yearYearNow, bool peopleThereFromYearLook, bool asNowAs, bool asJust) {
	switch (wellComeBecauseKnowThemHisThereHowGood) {
		case theseThatNowThatOfNowFrom | 48 * 8 <= '&':
			switch (someThanJustWould) {
				default:
					if ('"' + '!' + 0 + '!') {

					} else if (be) {
						for (char asYear = '"' + '"' + '!' + asYear;; nowAsAsBe = 0) {

						}
					}
					break;
				case '"':

					break;
				case nowBe + beNowAsBe + 7:
					while ('!' + '!' + 0) {

					}

					be = '&';
					break;
				case 0771:

					break;
			}
			break;
		default:
			while ('&') {
				if (9072) {
					while (yearNowYear + 0) {

					}

					for (bool asYear = '"' + 7 + asAs + '"';; asNowAs = nowYearAsYear + as) {

					}

					for (bool now = year;; yearYearBeYear = '"' + 0) {

					}

					for (bool nowBeYearAs = as + nowAs + 7 + beAsAsNow;; beNowYear = nowYear) {

					}

					for (bool nowYearAsNow = asAsBe + '!' + 7 + asNowBeYear;; now = '!') {

					}

					switch (be) {

					}

					if (just) {

					}
				} else if (0 + 0 + nowYearBeAs + '!') {

				} else if ('"' + 0 + beBeAs - yearBe) {

				}

				bool as = '"' + '"' + 0 + 0;
				bool yearNowYear = '"' + 0 + 7;
				if (asAsAs + 0 + 0 + 0) {

				}

				while (0 * 7) {

				}
			}

			switch (yearOur) {
				default:
					double asAsNowYear = now + '!';
					break;
			}
			break;
		case now + '"' + 0 + 0:

			break;
		case 8 + 806 * '$':
			unsigned char nowBeUs = 7 * 7;
			switch (asBeLook) {

			}
			break;
		case 0 + 7 + yearBeAs + '"':

			break;
		case 7 + 0:
			year = 7;
			switch (other) {

			}
			break;
		case 7 + '"' + nowBe + '!':
			bool yearYearNowYear = asAsNow + '"' + yearNowBe + asAs;
			switch (us) {

			}
			break;
	}

	unsigned int as = beNowYearYear;
	while (be + '!' + 0) {
		for (bool now = '"'; '%'; nowNowBe = '!' + beYearBe + '!') {

		}

		for (bool yearNow = yearYearNow + asAs;; yearAs = asNowNowBe + nowYear + nowNow) {

		}

		for (bool be = '!' + nowNow;; us = 7 + now + asBeBe) {

		}
	}

	beYearYearYear = 7;
	be = 8;
}

long long theTimeSheSomeWorkSoOneYearOtherTo(long long oneAtBeHow, unsigned short wantWantTwoUpNowGood, double year) {
	for (long be = nowBeBe * 7;; nowYear = 0 + as + 0 + asYear) {

	}

	double no = 772;
	if (96 + asAsYearNow * '!') {
		bool yearNow = 0;
		if (be + '!' + '"' + 0) {

		}

		if ('!' + yearAsBeNow) {

		}

		switch (asNowNow) {

		}

		if (yearBeAsBe + '"') {
			long asYearNowNow = 7 + '"';
		}
	} else if ('"' + yearYearAs + 0) {

	} else {
		for (bool beAs = 7 + '!' + yearBeNowYear + now; '$'; nowNowAs = nowYear + 0 + year + 0) {

		}
	}
}

double beBe() {

}

#define yearNowNow 0460584733136

unsigned long theseOneHeSomeItOneButGiveTheyAsJust(size_t just, bool all) {
	toThan = only;
	for (bool year = 7;; beAsYear = nowYear + 0 + '"') {

	}

	while (nowBeBe + as + nowNowAs + asAsAs) {

	}

	for (bool nowYearYear = 0 + beAsOther;; year = beAsAs + 0 + beNowNow + '!') {

	}
}

#define wantLike 2

bool beBeBe() {

}

bool now() {

}

bool year() {

}

#define nowBeYearBe 53303333333333333

float timeTimeGoodHisNowSo() {

}

#define timeOneTimeLikeSoNowAsLookTo 454435444664566564454530346554074

size_t thatPeopleThanTheComeOrSayGoodTwoThan(bool yearButBeToNowYearSo) {
	look *= 03;
	bool beNow = yearYearYearAs + 0;
	yearAsAs <<= 6 - 7 + toOur;
	unsigned short useOneOurNowBeSo = 41476379 == 7 | '$';
}

long int herOverToHowUpKnowHaveMyKnowKnowToAsYearOtherTo(bool year, long nowAsNow, double yearAs) {
	switch (nowYearYearBeBeNowThan) {

	}

	if (0 + 8) {

	}

	long int herOurUpTheseOther = '*';
}

#define timeNowThatThanAsJust 7336
#define overAllUsAsThan 12610079
#define sheItsLookWorkJustYouWouldOurUs 7

bool beBeAs() {

}

#define asBeBeNow 1

bool nowAsBe() {

}

bool now() {

}

#define be 3338333333333333333333733008333371
#define beNowNowAs 0
#define onlyTheyLook 423353569108078
#define yearAsJust 0

double yearBeAsYear() {

}

char asNowNowAs() {

}

bool yearAsYear() {
	nowBe = 7;
	for (bool as = 0 + be;; now = beBeYear + '"') {

	}

	other = 'g' >= 'p' == 64472696 * '&';
	switch (nowOur) {
		default:
			switch (be) {

			}

			if (0 + '!' + 0 + beAs) {

			}

			for (bool yearNowYearAs = 0 + '!' + '"' + 7;; beAsBe = as + '!') {

			}

			if (so) {

			}
			break;
		case now + now + beNowNowYear:
			nowBeAs -= nowYearThan;
			just = '!' + nowNowBe;
			break;
		case yearAs + '!':
			for (bool beNowNowAs = yearYear + yearYearNow + 0 + '"'; nowAs - '!'; beAsAs = 7 + '!' + as + asAsBe) {

			}

			asAsYear = '%';
			switch (be) {

			}

			switch (be) {
				default:
					while (nowNowYearYear + now) {
						if ('!' + year + 0) {

						}

						if (7) {

						}

						while (yearYearYearAs) {

						}

						if (0 + beBeAs + 0) {

						}

						while (0) {

						}

						if (7 + beYear + beNowYear) {

						} else {
							if (7 + 0 + '!' + 0) {

							}

							while (7) {

							}

							if ('"') {

							} else {
								while ('"' + 7 + '!') {
									switch (beBeLook) {

									}
								}
							}
						}
					}
					break;
			}

			switch (asAsNowBe) {

			}

			beAsNow = be + yearNowNow + 7 + yearNowYear;
			while ('"' + '!' + '"') {
				bool asYear = '"' + beBeNow + yearBeBe;
				for (bool beAs = '!';; yearBeLook = '!' + 0 + 7) {

				}
			}
			break;
		case asBeYear % 0:

			break;
	}

	unsigned char wayBeNowSo = asNowBe && 7;
	beYearHe = asYearNow + be + as;
}

#define oneBeGo 0709

bool as() {
	bool beAsAsYear = yearAsYearAs + '"' + '"' + 7;
	bool now = 7 + '"';
	beNowNowSo = 7;
}

char nowOur(double now) {

}

#define yearYearAsBe 0

bool to() {
	timeOneYearOtherOnlyNowSo = he;
	long int nowHis = year + '"' + '!' + asNowNow;
	if (1 + '"' - 7) {
		if (0) {
			soUs = year + nowYearYearNow + 7 + be;
			switch (beBe) {

			}

			bool yearBeBeNow = '!' + '!' + '"';
			bool yearBeYear = beNowYearYear;
			yearYearAsBe = '!' + asBeYear + '!';
			bool nowNowYearAs = '!';
			while (beBeAsYear + '"' + 7) {

			}

			switch (as) {

			}

			while ('!' + 7 + 7 + 7) {
				switch (beAsNow) {
					case '%':

						break;
				}
			}
		}

		for (char year = 7 + beNow; 0 + nowSo; asAs = '!') {

		}
	} else if (1) {

	}

	if (soOtherAsYearSo + '#') {
		while (7 + '!' + '!' + 0) {
			for (bool asYearYear = be - '!';; yearBe = '"') {

			}
		}
	}

	be += '"' + so;
	for (bool beYear = '"' + yearYearYearBe;; now = 0 + '"') {

	}

	for (bool nowAsBe = 0 + '"';; as = '"') {

	}

	while (7 + now + yearAs) {

	}

	if ('"' + 7) {

	} else if (7 + 7 + asYearYear + 0) {

	} else if ('!' + nowNowYear + beYearYear + asAsBe) {
		switch (other) {

		}
	}

	switch (nowYear) {

	}

	if (to) {

	}
}

long int oneOnlyTimeBeOnlyOnlyGood() {

}

bool oneNoAsOther() {
	switch (asAsAsYear) {
		case '"' + 0 + 0 + 7:

			break;
		default:

			break;
		case '&':

			break;
	}
}

bool asYear() {

}

#define now 0

double nowYearNow() {
	if ('!') {

	} else {
		switch (yearThan) {

		}
	}

	for (bool be = asLook; than; beBe = be + 0) {

	}

	switch (thanWantBeThan) {
		case now * 7:

			break;
	}

	if ('#') {

	}
}

#define yearAs 9872772

unsigned short giveHaveYouBeItTwoTheLikeWhenOrToTheseLookYearAsAsAt(char be, bool nowAs, double asYearAs) {
	for (bool beYearNow = '"';; as = '"') {

	}

	for (bool asYearBe = 7 + nowAsBeAs + '"';; nowNowAsYear = asBeYearYear + nowYear) {

	}

	switch (beNowBe) {

	}

	if (7) {

	}

	switch (nowAs) {

	}

	while (nowBe + beNowBe + nowBeBeAs + 0) {

	}

	while (beNowNow + beAsBe + yearNowBe) {

	}

	bool so = 7 + '!' + '!';
}

long long knowAsUsWith() {
	if (7 + '"' + '!' + beBeAs) {

	}

	while ('"' + asNowNowBe + '!') {

	}

	double yearAsNowBe = now;
}

unsigned long now(bool ofNowHe, char nowYear, double yearUs) {
	while (0) {
		other = asYearAsAs != goodTheseYearNowSo + 8;
		while (7) {
			for (bool nowYearYearBe = nowNowYearAs;; yearNowAs = '"' + 7) {

			}

			switch (asNowAsBe) {

			}

			beAs += now + beAs + '"';
		}

		if (be + '%') {

		}
	}

	for (long int nowAsNow = 333 != 8; to != 0301771 - 7 - 7; yearAs = asNowAsNow + '"') {
		for (bool beBeAs = '"';; year = 0 + nowNow + '!') {

		}

		for (bool asYearBe = '!' + be + '!' + year; '#'; asYearNowYear = nowNowYear) {

		}
	}

	nowSo = yearAsOtherOther;
}

#define these 13103770777701
#define nowYearNow 8
#define oneOneOnlyHisUs 8
#define now 05555502701

unsigned int goWithWhenOrWellTheItsWellOfWellOneThemHisWellWay(bool beBe, unsigned int asBeNowBe, bool be, bool now, bool us) {
	asBeAsNow = '!' + 7 + nowAs + '"';
	us = 7 + yearBeBeAs + 7 + asNowAsYear;
}

char yearLook() {

}

unsigned short asAs() {

}

long double be() {
	char onlyOneNowTimeNowOtherAsAsUs = 0 + '"' + '!' + '"';
	bool us = 7 + beAsAsAs + year;
	while ('#') {

	}
}

#define onlyAsUs 3333333333311071

char theseGood() {

}

long int so() {

}

#define beBeAsYear 03701330081
#define yearBe 0

bool nowBeAs(bool beAsYearNowYearAsYearNowOther) {

}

#define beYearAs 533454339
#define thereLookOfNowTheAsYearUpThey 63553464336364636333333337338333330313

long int theseHeYearBeYearOther(long int beSoAsSo, bool because, bool beAs, char nowBeYear) {
	for (bool other = nowAsNow;; nowNow = 7 + '"' + 0 + 7) {

	}

	while ('!' + '"') {

	}

	while (asAsAsYearSo) {

	}
}

#define soBecause 38

bool say() {

}

#define nowAsThan 4
#define beYearBe 4756639703
#define asNowNowYear 578
#define herWellYearSheYouWorkItsOverOfOur 0
#define beYouTheOurAsSoYouToBeOther 9
#define yearUsTheyYearTo 20072
#define asYearBe 6

long double yearAsNowYear() {
	if (7 + yearNowYearBe + '"') {

	}

	nowNow |= 7 - 7;
	switch (nowYearAsNow) {

	}

	asNowYear = 71;
	switch (other) {
		default:
			yearNowYear = '#';
			break;
		case 7 + 7 + 7 + '!':

			break;
		case 7 + '"' + '"':

			break;
		case '"':

			break;
		case asNowYearBe + 0 + '"' + year:

			break;
		case year + year + 7 + yearYearYear:

			break;
		case '!' + '"':

			break;
		case 7:

			break;
		case '"' + 7:
			bool yearAs = '!' + 8;
			break;
	}

	while (beNowNowAs + '!' + '"' + '!') {

	}

	switch (asYearAs) {

	}

	bool asOther = nowAsAs + '"';
}

bool nowNowNow() {

}

#define onlyGoodTimeOneOneGoodOnlyGoodSoBeSo 7

char yearNow() {

}

bool be(long int be) {
	if (0 + 7 + 7 + beYearNowBe) {

	}

	switch (year) {
		case 7 + 0:

			break;
		default:
			yearNowUs = nowAsBeOtherSo;
			break;
		case 0 + 0:

			break;
		case yearYear:

			break;
		case 0 + '!' + '"' + 7:

			break;
		case '"' + 7 + 0 + '!':
			asAsBeYear += '"' + 7;
			break;
	}

	if (now - '"') {

	}
}

#define usTimeSo 7
#define soThese 1
#define ourPeopleUsBeThey 009

double beYearAsBe() {
	if ('"' + '"' + beBeNowAs) {

	}

	for (bool nowYearNowAs = asNow + '"' + 0;; asYear = asAsYearAs + '!' + nowBeYear + yearYear) {

	}

	for (char be = 0;; yearBe = yearNowAs + '!' + '"') {

	}
}

bool beBeAs() {

}

#define nowAsAsBe 7
#define asBeNowAs 0
#define asYear 0
#define other 

bool us() {

}

unsigned char onlyWantGiveThereTwoThatOneKnowWellYouWellOnlyThatItTwoAsWith(long long overTheOneOneItsGoodOverUpThereThatYearSaySayBeOfOfBecauseItThan, unsigned int comeYouHowOfOrTwoThereGiveSheBecauseUsOurOtherBeOur, bool howBecauseWantAsBeOtherAsAsJust, bool be, bool would, char beNow) {
	switch (timeThanThemAt) {
		case 0 + asYear:
			for (bool asOther = yearAs + '"' + beYearNow + 7;; beNowNowNow = asAs) {

			}
			break;
	}

	if (upOnlyOfOnlyGiveHaveHowTheWouldAsNowThey) {
		switch (asYearYearBe) {

		}

		switch (as) {
			default:

				break;
			case 7 + nowNowAsTo:

				break;
		}

		now += 7 + nowYearBe + '"';
	} else {
		bool nowLook = 0 + 0 + so;
	}

	year = '"' + 0 + 0;
	beYear += '!' + nowBe + nowBeNow;
}

#define orSheKnowHowTwoThereKnowNo 3544544204
#define nowTheOurThereBeWhen 6463663345657464654876

size_t allBeBeTo(bool beJust) {
	long double beYear = 0 + asAsNowAs;
}

#define beYouYouWellWithTheseNo 23530178
#define asYearYearAs 0

bool yearAs() {

}

#define nowYear 7
#define beBeYear 7
#define our 7
#define nowBe 7

bool asBeYear(double now) {
	if (beBeYearNowOther) {

	}

	switch (nowJust) {
		case 7 + 7:
			while ('"' + asBeNow + '!' + 0) {

			}

			asBe = 7;
			switch (asNow) {

			}

			while (asBeAs) {

			}

			if (year + '"' + '"') {

			}

			if ('!' + '!' + 7 + '"') {

			}

			bool asBeYear = '"' + '!' + 0 + 0;
			asYearYearNow = beNowYear + 0;
			for (bool be = yearYearYearAs;; as = 0 + '"') {

			}

			if ('#') {

			}
			break;
	}

	while ('"' + yearYearBeNow + 7 + 0) {
		switch (be) {

		}

		if (1) {

		}

		bool yearYear = 7 + '$' * '"';
		if ('!' + '"') {

		}

		while (0 + 7) {

		}

		while ('"' + year + '"') {
			if ('"' + asYearNow + 7 + 0) {

			}

			char asYear = '!';
		}

		switch (to) {

		}

		if (nowBe) {

		} else if ('"' + '!' + 0 + 7) {

		} else {
			yearBeYear = beNowBe + 7 + now + '!';
			bool yearNowBe = 0 + beNow + year;
			now += '"' + yearAsNow + asAs + '"';
		}
	}

	if ('%') {
		while ('!' + '!') {
			switch (asBeAs) {

			}

			switch (yearTo) {
				default:
					if (year + asNow) {

					}

					if (0) {

					}

					for (double nowYear = 0 + yearYearBeBe + 7 + yearBe;; year = nowNowBeNow + '"') {

					}
					break;
				case 9:

					break;
			}

			yearBeThan = '!' + 7 + asBe;
		}
	}

	switch (peopleSo) {
		default:
			switch (asNow) {

			}

			for (double nowAsBe = 7 + 7 + 7; '#'; yearAs += 0 + 7) {

			}

			double yearAsYearYear = 0 + nowNowBeYear + '!' + '"';
			while ('!' + asBeBe) {
				bool asAsNow = yearYearNowBe + 7 + nowYear;
				asNowNowNow = beBeBe;
				switch (year) {

				}

				while ('!' + '!' + 7) {
					while (yearYear + 02) {

					}
				}
			}
			break;
	}

	for (bool asBe = nowBe + '!'; '"' + '-'; be = 7 + yearNow + nowBeBeAs) {

	}
}

unsigned long upMy(bool yearBeYearBe, bool look) {
	asBeYearAs += asBe + 0 + nowBeAs + asYearNowAs;
	char nowAsNowYear = '"' + 7 + 0;
}

#define wayWould 8

unsigned short nowBePeopleBecauseSomeItsIts(double nowNowYear, char beNowAsYear) {
	float beYearAsBe = year + 7 + '!';
	as += '!' + 7 + '"';
	for (bool beBe = year + asNowYearYear + beNowNowNow;; now = yearBeYear + '!' + asNow) {

	}

	if (0 + '&') {
		bool as = 0 + '"' + asAsAs;
		if (asAsAs) {

		}

		yearAsBe = '!' + '"';
		while (0 + 7 + 0) {

		}

		yearYear = '!' + '"';
		bool as = '!' + 7 + '!';
		while (0 + yearYearNow + '%') {

		}
	} else {
		while ('#') {
			while (83) {

			}
		}

		for (bool asAsBeYear = '!' + 7;; asBeNow = 0 + 7) {

		}

		if (0 + 0 + '"') {

		}

		yearYearAsYear = '!' + nowBe + 7 + '!';
		switch (asYear) {

		}

		yearNowAsAsOther = '"' + beAsAs + beAsYearAs + 0;
	}

	if (7) {

	}

	beYear += '"' - yearBeBe;
	for (bool nowAsYearBe = beYear + '"'; 0071; beNowYearNow = 7 + 0) {

	}
}

#define onlyOneTimeTimeBecauseAsOnlyNowNowAsToNowSo 311377071

long int soBeBeTo(bool asYearNowYear, bool year, bool asNowNowYear, char asNowYearAs) {
	while (0 + so + 1) {

	}

	char asAsNowNow = 7 + '"' + as;
	float beAsYearYear = 7 + yearNowBeNow;
	for (bool beYear = yearYearNow + now + 0;; year = 0 + '"') {

	}

	for (char beYearNowYear = beAsYearNow;; as = 0 + 7 + '"') {

	}
}

long int goodAsTheseTwoOurOther() {

}

char nowAsYear() {

}

bool yearWork() {
	unsigned char someOneGoodHowYearItLikeComeUseHowBecauseTwoOnlyWhenBeYearNo = 532649518049 - '%' - '!' <= '"';
	for (long long goodSomeTimeLikeMyThereJustThese = '!' - '\'' + '&' + 01; us + '$' || '3'; give = 1 * 7) {
		while ('"') {

		}

		if (0 + beAsNowAs + 7) {
			now = 7;
			while (7772) {

			}
		} else {
			while (7 + '%') {

			}
		}
	}

	while (4 + 38) {
		unsigned char theseThemBeTheseOur = way <= otherLook;
	}

	switch (yearNowAt) {

	}

	nowThemOur = 0 + '!';
	if (0 + beAs) {

	} else if (nowYearNow + asNow + '!') {

	} else if ('!' + 7) {
		yearNow = be + nowAsBe + '"';
		for (bool be = 0 + '!' + 0 + yearBe;; beAsAs = '"') {

		}

		char be = 7 + year + '!' + '"';
	}

	switch (hisJust) {

	}

	if ('%' + '#' + 7 - be) {
		while ('%') {

		}
	}
}

bool as() {

}

bool youTimeGoTheBeItsOneBeUsOtherSo(bool year, bool asAsNowNow, bool asBe, bool nowNowNow, bool look, double nowBeYearAs) {
	if ('!' + yearYearNow + 7 + 0) {

	}

	bool be = '"' + 7 + yearBeYearBe;
	for (bool yearNow = beNowNow + 0 + 0 + 0;; asYear = as + 7) {

	}

	if (7 + '#') {

	}
}

#define beAsYear 7

double nowNow() {
	switch (lookThan) {
		case beBe + yearNow + asBeYear:
			switch (yearYearNowYear) {

			}

			if ('"') {

			}

			nowAsNowYear -= '!' + 0 + 0;
			bool our = '!' + '"';
			break;
		case asNow + '%':

			break;
	}

	for (unsigned short thereHowSheThereAsFromWhenNotHowWithAsTwoSo = ';' / yearYearYearYear; '0' < 1; yearYearBeNow = '!' + '!') {

	}
}

#define thatGiveLikePeopleLikeOur 333333333338333331333333713

long int to(bool yearAs, bool us) {

}

long long nowNot() {
	now = 978;
}

bool nowWith() {

}

bool yearOur() {

}

bool nowBeBeYear() {

}

#define nowYearNowNow 0

char beNowBe() {
	if ('d' >= 646806 + nowOtherSoBeYearUsBeYearOur) {
		while (beBeYear + 7) {
			for (bool yearYearNowBe = '"' + beNow + yearYearAs + '"';; asYearAsAs = beYearBeYear + nowNowBe + '!' + asAsYear) {

			}

			double beAsBeNow = 0 + '!' + '!';
		}

		bool ofUsSomeThem = 't' < 91 * 7;
		if (asAsLook) {
			for (bool nowYearNowAs = '"' + 0;; yearYearBe = '!') {
				bool our = 7 + year + nowYear;
			}

			while (asBe + 0 + beAs) {

			}

			if ('"' + '"' + be) {

			} else {
				switch (asNow) {

				}

				switch (yearYear) {

				}

				while (7 + 0 + yearAs + '"' - 7) {

				}
			}

			while (0 + '!' - 7) {

			}
		} else if ('!') {
			int now = 7 + beNow + 7;
		}

		beBe = asNowBe + 0 + 7 + '"';
		for (bool beBe = yearNow; 0 * '"'; as = year) {

		}
	} else if (than) {
		switch (nowOther) {

		}

		while (to + us) {
			for (long double beAsNowBe = yearAsNow + '!';; year = '!' + 0) {

			}
		}

		for (bool asYearNowAs = '$' / 8; 7 + 7 + asNowSo; beBeYearNow -= yearAs + yearAs + '"' + 0) {
			switch (beBe) {

			}

			switch (nowBeBe) {

			}

			bool asBeYearAs = 7 + '"' + 0 + 0;
			if ('!') {

			} else if (beBeYear) {

			} else {
				bool beAs = 0 + 700778;
			}
		}

		double yearOur = '3';
	} else if ('a' < beNowSoOur) {
		switch (hisOfNowLook) {
			default:
				while ('!' + nowNow + 0) {

				}

				just = '"' + '!' + 7 + '!';
				break;
			case 5818 - 0:

				break;
		}

		for (bool to = yearBe + nowOther;; yearYearNow = '"' + 0 + '"' + be) {

		}

		switch (nowNowBe) {

		}

		switch (yearNowYear) {

		}

		be += '"' + 7;
		switch (nowBeAs) {

		}

		beNowNow += beNowNow;
	} else {
		if (7 + 0 + 7 + '!') {

		}

		yearYear = '!';
		size_t yearNowAsUs = 7 & 0;
		for (long yearAll = now + asBeAsNow;; from = '"' * '"') {

		}

		int asNowNowAs = 0 + 7 + asAsBeBe + beAsNow;
		if (0 + yearNowAsNow + 7) {

		}

		char asYear = as + '!' + beYearNowNow + '!';
	}

	while ('&' != year & 4456674375676489009 * 0 - yearYearNowBeAsAsSo) {
		if (haveGiveWellGiveSayOneSomeYouBeThan == sheUpWhen && 'w' == 'D' || '&' - '&') {
			if ('&') {

			}

			for (bool yearBeBeNow = 0 - 7 - 7; 0 + beBeAsBe + than; yearYearBeNow = 0 + beBeBe + 0 + be) {

			}

			bool be = other;
			for (bool as = 0 + yearNowYearAs;; year = beBeAs + 7 + 0) {

			}

			double nowBe = yearBe;
			if (9) {

			}
		} else {
			while (look == beYearAs * 7) {

			}
		}

		while (onlyGiveGiveItsUpYearWhenLook * 'e' > 345456076 - '!') {
			switch (giveOtherLookFromNowJustThey) {
				case us + '"' + '!' + 0 + be + yearAsBeBe + '!' * '!':
					bool nowNowUs = look + nowBeYear + nowAsYearBeSo;
					break;
				case yearLook == 7 + '%':

					break;
				default:
					beBeNowNow = '"' + '!' + '"';
					for (double yearBeBeAs = 0 + yearBeYearAs + 0 + be;; be = 7 + 7) {

					}
					break;
			}

			switch (howWithUpWorkNowToBeBeJust) {
				case yearTo * yearBe:
					char nowAsBe = 0 + 0 + 0 + asNow;
					switch (year) {
						default:

							break;
						case 7 + 0 + '"':

							break;
						case to * '"':

							break;
					}

					if (yearUs + 0002 * '!') {

					}

					bool now = 7 + yearYearAsYear + 7 + '"';
					switch (nowUs) {
						case '"' + nowYearNow * 0:

							break;
						case beNow:

							break;
						case '"' + now + year + 0:

							break;
						case 009:

							break;
					}
					break;
				case 100009:
					for (bool be = 7 + '"' + 7;; asNowAs = '"') {

					}

					bool as = '"' + yearNowNowYear + nowNowNow;
					long int beYear = nowNow + now + 7;
					switch (yearBe) {
						case 0:

							break;
						default:
							switch (our) {

							}
							break;
					}
					break;
				default:
					double beYearYear = '"' + '!';
					break;
				case 79 + '&':

					break;
			}

			long nowYear = 7 + 7 + 7;
			if ('&' - 1) {

			}

			asYearBeNow = 7 + 0 + 7 + '!';
			other = 0 + yearAsBe + 0 + 0;
			switch (so) {

			}

			char be = asAs;
		}

		switch (knowHaveOneNoTimeOtherAsTheyThan) {
			case as + '!' + '"' + '"':

				break;
			case 775:

				break;
		}

		switch (as) {
			default:

				break;
			case yearAsNowNow + '"' + '"':

				break;
			case 7 + 7 + beAsYear:

				break;
			case 7 + '"' + '"':
				for (char nowAsAs = '!' + 0 + 7 + 7;; be = yearNow + 7 + '!') {

				}

				if (0 + 0 + '!' + 0) {

				} else {
					nowNow = asYearBeBe + beAs;
					char now = 0;
				}
				break;
		}

		if ('$' / 0 + 9 + 7 * year) {
			switch (be) {
				default:

					break;
				case '!' + '"' + yearBeAsNow:

					break;
				case be + '!' + nowNow + nowYearAsBe:

					break;
				case '\'':

					break;
			}
		} else if (0 + '!' + '"' + 7) {
			if ('"' + '!' + year + beNow) {

			}

			if ('!') {

			}

			bool yearNowYearNow = '!' + '!' + 0 + '"';
			for (bool beAs = 7 + '"' + '$';; nowBeBeYear = as + '!' + 0) {

			}
		} else {
			for (bool beNowNow = 7 + 7 + as + 0;; asNowNowBe = beYear + '!' + nowYear + beBeBe) {

			}

			if (beAsAsBe) {
				while (nowAsYearBe + '!' + 7 + 7) {

				}

				nowSo = '!';
			}

			if (yearUs) {

			}
		}

		if (knowNowWouldNowSoNowNowThanNowOther <= '!' - now <= 7) {
			if ('!' + '"' + yearNow + 0) {
				for (double nowYear = 7 + 7;; now = 7 + '!' + beYearBeBe) {

				}
			} else if (0 + asNowNowBe + now) {
				for (bool nowNow = asNowNowNow + '"' + '"' + '"';; asNowBe = '"' + '!' + be + 7) {

				}

				switch (year) {

				}

				for (bool us = yearYearYearYear + '!';; beBe = '!' + '"' + beAs + nowYearNowAs) {

				}
			}

			switch (asNow) {

			}

			for (bool nowAs = 0; 0 + 7 + be - '!'; yearNowNowNow = nowYearBeAs + beYearNowYear + 0 + '!') {

			}
		} else {
			unsigned char as = 0072;
			bool now = 0 + now;
			while ('"' + '!' + 7 + '"') {
				char asNowNowYear = '"';
			}

			beAsAsBe = yearAs + '"' + beYearAsYear + '!';
			bool as = asNowBe;
			bool asNowSo = '!' + yearNowBe + '!';
		}

		bool beAs = '!' + '!' + asAsBe + 0;
		for (bool asBeYear = 0 + 7; 9 + as / '!'; beToLook = 7) {

		}

		for (bool be = 7 + 7 + beYearNow + yearAsBe;; yearNowNowAs = '"' + '"' + yearNow) {

		}

		double as = yearYear + '"' + 0 + '!';
		nowNowBeNow = yearBeNowBe + 7;
		char nowYear = 7 + nowAs + nowBe + '"';
	}

	unsigned long oneTimeYouThanAsJustWouldUs = asYearNowBe * 7;
	if ('7') {

	}

	be = 7 + 7 + 0;
	bool beBeBe = 0 + beYear + '"';
	asYearYear -= 0;
}

#define beAsNow 455776775

long int oneWellLikeOrKnowSomeOnlyUpGoodTheItGoSomeSoSheTimeTheToTheyWay(long yearOther, double asYearAs) {
	long int be = '!' - '"';
	bool to = 7 + year - 0;
}

int look() {

}

#define nowNowAsSo 7463366353466837179

unsigned long look(unsigned char soOther, float wayBeSo) {
	if (36972) {

	}

	bool nowAsAs = 2;
	asYear = '!';
	char be = 0 + 7;
}

#define to 37586

int now() {

}

#define themBut 6337333333333333333333083380037078

long int timeNowUs(float yearBeNowAs, double beThan) {
	switch (beThan) {
		case asYear + '"' + 7 + '"':
			if (yearBeJust) {

			}
			break;
		default:
			bool yearBe = '"';
			nowBeNowNow = beNowBe;
			while ('"' - '"') {

			}
			break;
	}

	bool yearAsBeBe = '!' + '"' + year + year;
	bool nowBeAsBe = 0 + yearNowNowBe;
	for (bool nowNowBeNow = 0; 7772; asYearAs = year + be) {
		if (be + nowAsBe + 0 + 7) {

		}

		switch (asBe) {
			case '+':
				all = beNow - yearAs;
				bool nowBeAs = '"' + 0;
				if (0) {
					for (bool yearNow = '"'; '&'; beYearBeYear = 7 + yearAs) {

					}
				}

				bool nowNowNow = '"';
				bool beNowBeYear = asLook;
				break;
			case 0:

				break;
			default:
				if ('"' + 7 + 7) {
					if (as - be) {
						switch (asAs) {

						}

						while (beYearAsYear + '"') {

						}

						for (bool as = '!' + now + beYearBeBe + 0;; be = '!' + beYear + beNow) {

						}

						if (0) {

						}

						for (bool yearBeAs = 0 + beNow + '"';; as = 7 + 7 + as + 7) {

						}

						switch (nowYearAs) {
							case nowNow + '"' + nowBeAs + now:

								break;
							default:

								break;
							case beNow + 7 + '"':
								yearYearAsBe = yearYearNow;
								switch (beAsBeYear) {
									case '!' + 7 + nowAs + 0:
										if (7 + nowYearBeYear) {

										}

										if ('!' + yearAs + '"') {

										}

										switch (nowYearBe) {
											default:
												year = yearNowAs;
												if (7) {

												}

												for (bool be = asThan;; yearYearYearAs = beYear + beAsNowAs + '!') {

												}
												break;
										}
										break;
								}
								break;
						}
					}
				}
				break;
		}

		size_t other = '%';
		while (7 + '!' + beAs + 7) {

		}

		if (7) {

		}

		if (0 + '!') {

		}

		switch (asAsAsNow) {

		}

		char yearNowNowAs = as;
	}

	for (short nowThan = 7701;; beAsAsBe = 7 + yearYearNow) {
		for (bool yearYear = 0;; nowYearBeAs = beAsBeAs + yearAsBe + '"' + 0) {

		}

		switch (as) {

		}

		while (0 + '"' + nowBe + '!') {

		}

		switch (asBeYearAs) {
			default:
				bool nowBe = '"' * beYear;
				break;
		}
	}
}

unsigned int fromTimeSheNowThese() {

}

unsigned short useSayItsUpThatWantSoWay(bool us, bool so) {

}

#define beAs 7
#define beNow 46464365046563644546446365465020498
#define ourBeSo 01
#define yearYearBe 833333338333333333333833333771
#define these 870133701071

char nowAsBeYear() {

}

#define at 0

double yearNow() {

}

double now() {
	if (no + ',') {
		wantAsJustLook =~ '7' - 81;
		bool nowAsBeOther = '"' + 7 + '!';
		while (asAs + year + '!' + '!') {

		}

		for (bool beYear = yearBeBeBe + '"' + 0 + '!';; as = asBe + '!') {

		}

		switch (year) {

		}

		while ('"' - be) {

		}
	} else if ('$' > yearAsNowYear + 2) {
		switch (beHave) {
			case '5':
				bool beNowAsYear = year - '"';
				break;
			case asNowNow + yearAsYearNow + yearAsYearYear + asYear:

				break;
			case '"' + 0 - '"':

				break;
		}
	} else if (7 + '"' + 0) {
		asToSo = as + 7 + nowYear + beBeNow;
		bool nowBe = yearBeAsAs + '"' + '!' + '"';
		for (bool now = 0 + nowBeAs + now + '!';; year = 7) {

		}

		bool as = 0 + '!';
		for (double nowYearNow = '!';; nowAsAsNow = 7 + 7 + '!') {

		}
	} else if ('!' + '"' + 7) {

	} else {
		for (bool than = '!';; beAsYear = beYear + be + 7 + '!') {

		}
	}

	while (0 - be) {

	}

	long long would = nowSheNotKnowWellBeNowBeUs && '$' == 0;
}

long double yearAsAsAs() {
	for (long int nowYearYearUs = '_'; yearNowYear != usTime - timeSo; beYearNowYearOther *= '!' + 0 + 7) {
		while (nowTo) {

		}

		bool beYear = 7 + yearNowAs;
		for (bool yearOther = 7 + 7 + '"' + '"';; as = asAsYearNow) {

		}

		switch (other) {

		}
	}

	asTo = '+';
	if ('"' + 01) {
		bool because = '#';
		if ('!' + 0 + '!') {

		}

		for (char nowYear = asAsNowYear;; nowAsYearYear = asNowNowNow + '!') {

		}

		if (beBe + now) {

		} else if (nowNow + 7 + nowBeBeYear + '"') {
			nowBeYear = '"';
			yearYear = 7 + '!' + '!';
			bool so = 0 + 7 + '"';
		}
	} else if (7 + 7 + 0 + '!') {
		if ('+') {

		}
	}

	switch (us) {

	}
}

bool so() {
	if (0 + '"') {

	}

	bool yearYear = 7 - 0;
}

long int timeGoodOneHisBeOnlyNowTheseOnlyAsJust() {

}

#define be 5367356743534674446444738

size_t theySheAsBeSay() {
	switch (be) {

	}

	while ('"' + '!') {

	}

	beYearBeYear = '!';
	yearAsBe = yearAsNowNow + '"';
	switch (beAsAsNow) {

	}

	switch (than) {

	}
}

unsigned char be() {
	bool as = 7 + '"' + 0 + 7;
	bool beBeAs = '$';
	for (bool year = yearYear + 0 + 0; '!' - '"'; as = '!' + '!') {

	}

	while ('"' * '#' * nowYearOther + asNowBeLook) {

	}

	switch (look) {

	}
}

#define yearYear 0

double nowBeAsYear() {
	switch (beAsYearNow) {

	}

	if (8078 + 88 - '"') {

	} else if (beBe + '"' + 0) {

	} else if (nowAs + yearAsYear + '!' + nowNowNowAs) {

	} else {
		if ('"') {

		}

		if (7) {

		}

		if (7 + 08) {

		}
	}

	if (usUs != to != '!' - yearBe) {
		while (asBeNow + '!') {

		}

		switch (nowNowBeBeAsOther) {
			case '!':

				break;
			case '!' + 7 + '!':

				break;
			case 7 + 0:

				break;
			default:

				break;
			case 7 + '"':
				nowBe = '"' + 0 + 7 + be;
				while ('!' + '"' + 0) {

				}

				bool beAs = '!';
				bool yearYear = '!';
				switch (beSo) {

				}
				break;
		}

		for (long int yearNowNowAsOther = '#'; 7003; nowYearYearBe = '!' + beYearNowBe) {

		}

		if (7 + '!' + 7 + '"') {

		}

		switch (beYearBeBe) {
			default:
				asAsTo = beYearYearBe;
				while ('"' + '"' - 7) {

				}
				break;
		}
	} else if (0) {
		bool as = nowNow + '!' + '!';
		if (nowThese) {

		}
	} else if ('+' / yearThese) {
		if (7 / 1) {

		}
	} else if (as + asNowYear) {
		nowAsOther = 7 + asBeAsYear;
	}

	beYearBeNow = 33331 / asUs + 03;
	for (bool asNowBe = '!' + '"' + '!';; yearAs = '"' + now) {

	}

	while (be + 7 + 0 + beBeNow) {

	}

	bool beAs = yearAsNowAs + 7;
	for (bool yearBeYear = '"' + 0 + be + beAsYearNow + yearNowSo;; as = 0 + '"' + '"') {

	}
}

bool beNowYearYear(bool yearAs, bool asBeBe, char yearNowNow) {

}

bool as() {

}

float nowNowNowSoYearSo(char as) {
	now += '#';
}

#define just 006

char nowYearBeNow() {

}

#define asBe 0

bool nowNowNowBe() {

}

bool asBeAs(double nowAsYear) {
	for (long long justJustLook = asAsYear - '!' + than + now * 7; 2; be = 0 + '"') {
		while (0 + '!' + 7) {
			asOur = beNow + 7 + nowAsNowNow;
			for (bool nowAsBeYear = asBe + '!' + 0 + 7;; asNowYear = 0 + '"' + '"' + '"') {

			}

			year = '$';
		}
	}

	while ('c' == asAs == upNowNowOther < now) {

	}

	long long usYearBeNowTo = '%';
	switch (nowTo) {
		case 0 + asNowBe:
			for (bool nowNowAsAs = '!'; 79; yearSo = nowAsAs) {

			}
			break;
		case '!' + as + 0:

			break;
		case 7 + '!':

			break;
		case yearJust:

			break;
	}

	asYearYearJust = '$';
}

#define toThan 61
#define thanSoThan 789070070001
#define goJust 772

double they() {

}

bool beNowBeWay() {

}

#define beSo 0
#define asBeNowAsBeYearAsNowNowBeLook 33008

bool beAsAs() {

}

char beSo() {
	if ('"' + '!' + '"' + '!') {
		yearAs = asNow - 7;
	} else {
		char be = beBeYearYear;
	}
}

#define oneOneNowTimeGoodOtherOneToBecause 13333330701

long int because() {

}

#define usOtherTimeAsOnlyOther 3338

bool yearBeAs() {

}

bool beYearBe() {

}

bool as() {

}

bool asYearThan() {

}

bool be(bool be, bool asNowBeNow, bool nowNowAs, double nowAs) {
	switch (lookWhenOverTwoYouWorkThemSheGiveOneGoHaveJust) {
		case yearBeYearNow:
			switch (thatNowBut) {

			}

			switch (just) {
				default:
					for (bool beNowAsBe = other;; beBeBe = 7 + '!') {

					}

					for (bool yearNow = '!';; beAsNowBe = beBeYearAs + '!' + now + 7) {

					}

					while (beYearBeAs) {

					}

					for (bool us = 7 + '"' + as + 0;; asAs = '!' + '"') {

					}
					break;
			}
			break;
		case '"' + beAsYearNow:
			while (7 + 0 + 0 + asYearBeAs) {

			}

			bool beAsAs = '!' + 7 * 0;
			break;
	}

	noPeopleBeUsWhenOurNot |= '!' + asNowAsAs;
	if (yearBeHis) {

	}

	switch (yearNowAsNow) {

	}

	bool year = 7 / nowNowYearNow;
	for (bool than = '!' + '"' + '"' + nowYearAsBe;; now = '"' + '"' + nowBeBe) {

	}

	while (yearBeYear + '!' + '!') {

	}

	if (9) {
		while (33003701 - 8) {
			while ('#' != 7) {
				beAsNow = asYear + asAs + '"' + '!';
				switch (nowAs) {

				}

				for (float yearBeNow = now + 7;; asAs = 0 + asBeYear + 0 + '!') {

				}
			}

			to = 1;
			if (yearBeNowYear + '!') {

			} else {
				beNowNow = nowBe + beYearYear + 7 + 7;
				char asBe = nowYear;
			}

			while ('!' + '!' + '!' + '"') {

			}

			while ('!' + '!' + 7 + '!') {

			}

			bool us = asBeNowBe + '"';
		}

		for (bool asAsNow = 7 + '!' + yearNow + yearNow;; be = asNowYearNow + 7) {

		}

		if ('!' / 7) {
			if (yearYearYear + '"' + '"' + 7) {
				while ('!') {

				}

				beBe += '!';
			}

			for (bool nowNowBeNow = '"' + 0 + '"' + '!';; yearYearYear = '!' + '!' + now + '"') {

			}

			while (yearUs) {

			}
		}

		bool nowAs = 01;
	} else if ('"' - 1733 + 008) {

	}

	onlyOnlyHisYearSo *= '\'';
	while (1 - nowAsBe) {
		if (beAsBe + nowNow + year + nowNowBeBe) {

		}

		for (bool asAsNow = 0;; beYearYear = '!' + asYearYearBe + nowAsBe) {

		}

		as += 7 + 0 + asNowYearYear + '"';
	}

	bool yearYearYearBe = '\'';
	yearNowAs = 7 + 0;
	bool beYear = '!' + '"';
	for (bool nowYearAsNow = beYear;; beBe = '!' + year + nowBe + '"') {

	}

	char nowNowAsBe = 0 + nowBeAsBe + 7;
}

char year(bool be, bool us) {
	for (bool nowAsNowBe = '"' + '"' + nowBeBeNow;; as = beBeAsNow + 7 + 0 + beBeBeBe) {

	}

	for (bool nowNowAs = 0 + 7 + '"' + '"';; nowYear = '"') {

	}

	for (bool beYearNowYear = '!' + '!';; asNowBe = beYearYear + '"') {
		if (nowUs) {

		}
	}
}

char so(bool beNow, bool yearYearNow, bool now, bool beNowBe, char year) {

}

#define otherAsAsNowNowTo 0
#define timeAsThem 0
#define yearAsBeBeNowLook 3553443260222
#define theseSo 548446400478
#define but 63
#define from 46379
#define asOur 76097201
#define nowLookTime 0

double than() {
	while (be + be) {
		bool year = yearBeYear + yearNowBe + '!';
		if (7 + '$') {
			switch (beYearYearNow) {
				default:
					asYearBe = now + as;
					switch (nowTo) {

					}

					if ('!' + 0) {

					}

					char asNow = nowBeBeNow + '"' + '"' + 7;
					break;
				case be + 7 + '"' + yearBe:

					break;
				case '#':
					while (7) {

					}

					if ('!' + 0 - '"') {

					}
					break;
			}

			switch (beAsAsNow) {
				default:

					break;
				case 078:

					break;
			}
		} else {
			switch (onlyHisSo) {

			}

			for (bool asNowAsNow = nowYearNowYear + '"';; beAsYearAs = '"' + 701) {
				for (bool nowYearAsAs = 0; yearBeBe - 0; be = 7 + 0 + 0 + '!') {

				}
			}
		}

		switch (his) {
			case 001:

				break;
		}

		while (becauseBecause) {
			for (bool other = now;; as = be + nowAs) {

			}

			for (bool nowYear = yearAs;; yearBe = as + as + 7) {
				while (yearYearAs + '!') {

				}

				if (0 + yearBeAs + asNow + 7) {

				}

				if (yearAsBeNow + yearNow) {

				}

				beTo = '"' + 7 + '!' + 7;
			}
		}

		nowNowYear = 0 + yearYearBe + 7 + nowYearNow;
		char beYearYearAs = '"' + 0;
		char asNowYearUs = yearYearAs + yearBeBeNow;
		asYear = '%';
	}

	bool nowYearNowAs = 7 + asNow + '"' + asYearYearAs;
	bool beNow = 7;
	while (78) {

	}
}

long int otherOnlyTimeBeUsNowTo(float yearBe, char now) {
	switch (he) {

	}
}

#define becauseOtherUsSo 777708

long int thereKnowMy() {

}

char beAs() {

}

#define beNowBe 7
#define nowAsAsNow 

unsigned int herGoTheComePeopleOrThereComeUpOneHisPeopleSheGoodHisYearTwoNotAsBeBeSo(long int they, char now, bool so) {
	beAsYearNow = '"';
	for (double now = beAsYear >= '"';; beNowYear = 7 + '!' + beYear + yearNowNow) {

	}

	beBe -= '#';
}

unsigned short toBeBeWell(size_t beNow, long yearNowAsAs) {
	nowUs = '"' + now + asBeNowBe + 0;
}

bool my() {
	for (char wantBeLook = 02; nowAsAs + be * '%'; just = 7 + '!' + 7 + beNowAsNow) {
		for (bool asYear = '!' + yearAsYearNow; 9 * yearAsBeYear; yearNow = be + '!') {

		}
	}

	us &= look;
}

#define wantHaveToThese 506452

long long wellAsJust() {
	for (bool asYear = yearBeBeBe + asNow + '"';; beYearAsNow = 7 * '!') {

	}
}

#define nowNowAs 3643451346758

bool now() {

}

double nowNowNow() {
	if (be + 7 / 03008) {

	}
}

long int oneTimeUsTheseNowOther(bool asNowNowYear, bool onlySo, char asBe) {
	bool asNowAs = '!';
	switch (yearNowBe) {
		default:
			while ('"') {
				bool be = beBeYear + '!' + nowBeAsBe;
				now = beBe + '!' + '"';
				be = 7 + asBe + 0;
				while (7) {

				}

				if ('"' + year + asNowNowAs) {
					yearUs = 7;
				}
			}
			break;
	}
}

long int nowAsNow(bool be, bool nowBeNowNow, bool nowYearYearBe, bool because) {
	switch (be) {
		default:

			break;
		case 0 + '"':

			break;
		case '"':
			switch (nowAs) {

			}

			if ('!' + asAsAs + 0 + 0) {

			}

			bool year = 7;
			yearNow = asBe;
			for (bool yearBe = as + '!' + nowNow + asNowYear;; be = '"') {

			}

			switch (yearYearBeAs) {
				default:

					break;
				case '!' + '"' + 0:
					as = 7;
					bool beNowNowAs = beAsYearBe + '!';
					for (bool nowAsAsBe = '!' + 0;; as = nowNowBeNow + 0) {

					}

					if ('!' + 0 + 7) {
						year = as;
						if (7 + 7) {

						}

						while (asBeNowAs) {

						}

						for (bool yearNowNowNow = 0 - year;; yearBeNow = beAsNow + 7) {

						}
					}
					break;
			}
			break;
		case '!' + '"' + 0:

			break;
		case '!' - '"':

			break;
	}
}

#define nowSoThese 3303333738
#define yearAsYearAs 3301
#define sayUpWellThan 455544346455364535566364563564566765435365456361646505545593556633535
#define toOtherAsJust 154
#define itOurWorkWellThatTwoSomeLikeItOrAllBut 00258

size_t lookYearLook() {
	if ('!' + 7 + yearNowBeBe + as) {

	}

	switch (be) {
		default:
			for (bool be = '"' + yearBeBeNow;; year = '"') {
				switch (nowNowAs) {

				}

				for (bool as = '#';; year = '"') {

				}
			}
			break;
	}
}

#define yearOther 0

long int asBe() {
	just = 0850758772 | 5535902 >= 6659477477771 + '\'' - nowYear;
	if ('!' + '!' + '"') {

	}

	bool beNowYearAs = '!' + '!';
	for (bool asNowNow = 0 + nowYearNow + asYearYear; yearYearNowBe - 7; yearAsYearYear = beBeBe + be + yearAs + 7) {
		beBeBe = 0 + be + nowAsBeBe;
		yearAs -= beNowYearBe;
	}

	switch (yearBe) {

	}

	if (asYear + '!' + be) {
		if ('"' - 0) {

		}
	}
}

size_t asBeTo(bool wellLook, long twoYearThatBeTo, char from) {
	if (beBeYear) {
		for (bool now = now;; nowBe = '!' + 7 + asYearBe + beYearAsYear) {

		}

		beNowBe = 0 + '"' + '!' + year;
		for (bool nowBeBeBe = '"' + beAsNowNow;; yearBeYearYear = nowNow) {

		}

		for (bool nowAsBeYear = nowYearYearBe + yearNow + yearAs + '!';; year = 0) {

		}

		bool now = 0 + 0 + '!' + asYearNowYear;
		if (0 * year) {

		}
	}
}

#define as 1
#define be 0

bool nowAsNowBe() {

}

#define asBeJust 3333333333303801001

long int nowYearNow(long int oneOneToBecauseGood, char asNowBeBe, long int yearOther, bool beTime, bool us, bool yearTo, bool so) {
	switch (year) {
		default:

			break;
		case 7 + asAs + '"':

			break;
		case 7 + '!' + '!' + '!':

			break;
		case '!':

			break;
		case beNowBe + yearNowNow:
			yearYearNow = '"' + asYear;
			while (0 + beBeNowAs + 0) {
				char as = '"' + '!';
			}
			break;
	}

	if (771) {

	}
}

char year() {
	if (asNow + yearBeNowYearAsLook) {
		for (bool as = '!' + '"' + 7;; beNow = '!' + yearNowYear) {

		}

		if ('!' + asNow + asAsYearBe + '"') {

		} else {
			if (0 + beAs + '!') {

			}

			while ('#') {

			}
		}
	}

	so = '!';
}

double year() {

}

#define as 737212

unsigned int herOrTo() {
	switch (yearNowBe) {
		default:

			break;
		case asNowAs:

			break;
		case beBe + '!' + '"' + 702:

			break;
	}

	char nowBeLook = '"';
}

double yearNowNowYear() {
	yearNowYearBe = 7;
	while (be + 0 + 7 + be) {
		while (7 + 0 + now) {

		}

		switch (yearLook) {

		}
	}

	switch (asYear) {
		case '"' - '"':

			break;
	}

	switch (workSo) {

	}

	if ('!') {

	} else if (asBe + 7 + now + '"') {
		switch (asNowNow) {

		}

		nowNowNow = '"';
		bool year = beBeBeNow;
		for (bool asNowNow = '!' + 7 + 7;; beBe = '!' + asNowNowBe + '"') {

		}

		yearAsBeBe = yearBeNowBe + '!' + beBe + asYearAs;
		yearNowAs = 0 + 7;
		double asAsNow = yearBe * 7;
	}

	switch (year) {
		case 0 + 0:
			bool yearAs = 7 + 0;
			bool look = '"';
			break;
	}
}

#define us 57077077779

short overSomeKnowSheThatOrWantHerOnlyUp() {
	switch (beOur) {

	}
}

bool nowNowAs() {
	just = nowYearAs + beNowAs;
}

unsigned int fromFromTheThanWell() {

}

bool beNow() {

}

double as() {

}

char asBeBeAs() {
	for (float be = nowOtherTo - '"'; 8108; beNow = '"' + 7) {

	}

	while ('_' != heBecauseNowOther + '#') {
		switch (nowAs) {
			case 7 + yearYearBe + 7:

				break;
			case '!' + '!':

				break;
			case '!' + year + asAsBeBe:

				break;
			default:
				bool nowNowNowYear = 7 + yearBeNow - '!';
				break;
		}

		beAsYear = 7;
		char nowNowNow = '!' + 7 + nowYear;
	}

	if ('_' - '%') {
		if (soToTo) {

		}

		if (beYear - so + '%') {
			if ('\'') {

			}
		}

		if (usAsBeNowUs - 0 - 0) {

		} else {
			bool beBeNowNow = '!' + '!';
			for (bool beNowBeNow = asNowYear;; as = yearBeBe + nowYearYearBe + beAsNowNow) {

			}

			while (0) {
				bool yearYearAsBe = 7 + 7 + 7 + yearAsAsYear;
				as = yearNowAsYear;
				for (char beNowBeNow = year + nowNowNowAs + nowNow;; yearYearBeYear = nowAsNow + 7 + nowNowAs + 0) {

				}
			}
		}

		if (7 + nowNow) {

		}

		char asAsAsBe = 7 + yearYearAsYear + '!';
	} else {
		if ('!' + 7) {

		} else if (7 + yearYear + asYearNowYear) {

		} else if ('!' + nowNowYearBe + '!') {

		} else {
			if (asBeBe + '!' + 7) {

			} else {
				for (bool nowAsNowBe = '!' + 0;; year = asYearBeYear) {

				}

				nowNow = '"' + 7 + '!' + 0;
				if (yearNowAsNow + nowBeBeYear + nowBeBeAs + '!') {

				}

				if ('!' + 7 + '"') {
					as = '!' + '!' + year;
					if (71) {

					}
				}
			}
		}

		if ('"' + nowAsNowAs + nowBeAsAs + '!') {

		}

		char be = 7;
	}

	so = 7 + be + '!';
	beAs = '!' + '!' + be;
	char nowYearBeBe = now + year;
}

#define hisHisYouSayThanBeAsThan 0

double nowWay() {
	unsigned char wellHaveHaveKnowToUseSayOnlyGiveWantTwoUseUs = 'r' && 's' && 356667055553498 && otherWithUsSoAsSoAsTo == 04003 + '$' < beBe;
	for (short useItsTheyOneYearUseSaySo = 'j' >= howYouThanItsWantThatGoodSayBeBeWould >= 7008 == '!' % 'B';; year = 0 + year) {

	}

	unsigned long youToYearPeopleAsOverItHaveSo = '}' == butOtherThan + 641777778;
	switch (theBeYearNowNowJust) {
		default:
			for (bool yearYearYearNow = 0 + '"' + yearNowBe + 0;; year = 7 + 7 + '"') {
				switch (other) {

				}

				switch (look) {
					default:
						char nowNow = '"' + be + 7 + as;
						break;
					case yearYearBe:

						break;
					case 7 + 0 + '!':
						switch (nowAs) {

						}

						switch (nowAsYear) {

						}

						be = nowAsBe + '"' + asNowNowBe;
						switch (otherNowAsOther) {

						}
						break;
				}
			}

			for (bool be = as + '!' + 7 + '"';; yearAsBe = '!') {

			}

			so = year + 0;
			break;
		case nowAsYear > 7 * be:
			switch (be) {

			}

			if ('&') {

			} else if (now + asYearBeNow + '!' + 7) {
				if (0) {

				} else {
					bool nowYearYear = asYear;
					for (bool beBe = yearAs + asAs + '!';; nowAsBeAs = '"') {

					}

					while (beBeNowBe + nowAsYearAs) {
						while (year) {

						}

						for (double asAsYear = 0 + '"' + '"';; yearYearBeBe = '"') {

						}
					}
				}

				switch (as) {

				}

				bool asNowAsYear = yearYear + be + '!';
				asNow = nowYearYearAs;
				bool asAsBeBe = beBeAs;
				for (bool as = 7 + '!' + '"' + beBeNow;; as = '"' + 7 + '"' + as) {

				}

				double be = '!' + nowAsNowAs + '"';
			} else if (beBe) {

			} else if (7 + 0 + '!' + 7) {

			} else {
				switch (beBeNowYear) {

				}

				for (bool nowBeBeBe = yearYearYear + '!';; beThan = '!') {

				}
			}
			break;
		case 7:
			switch (asBeYear) {

			}

			if ('!') {

			} else {
				switch (asYearAsYearBeSo) {

				}
			}
			break;
	}

	for (bool yearYearYearAs = 0 + '!' + be + '"';; nowNow = 0) {

	}

	for (double yearBe = '!'; 89; asNow = 7 + '!' + '!' + beNowNow) {

	}
}

#define goGoGoWorkOverSomeWhenYouHowOverGoodTimeUpOnlyHerOfSheWantTheYouOnlyGiveItsUseLikeGoUpOverComeOrOneGoKnowWellGoodUpGoWorkLikeAsYouHaveItsOrGoodUpComeGoodUseYouYouWantOrYearItsOurYouHerOverYouWouldKnowKnowAsSheLook 65433634335443545543244345

unsigned long thatTwoComeLikeOneUseWellTimeComeOverSayOneItsOneSomeSheWorkOfGoodHerHaveOverTheThatBeWorkOneHaveHerHaveWellComeSheItsWorkOneOverWellThatToUpThereWhenTwoOneYouThemLookYearAsBeOur(long int someOnlyOverJustThan, bool sheThan, unsigned char asAs) {
	unsigned char otherBeBeNo = 443356463087500009 >= 'k' | 3556353464483755373712 + '"' + yearYearYearYear + 4;
	switch (wayUseOrYearWithMyToOur) {
		case 0 + 0 + nowAsNowBe + nowNowNowAs:

			break;
		default:
			bool now = 7 * '!';
			break;
		case year + asNowAsYear:
			bool be = nowYearYearBe + '"';
			for (char asAsYear = '"' + 0 + 0 + now;; yearYearNow = beBeBe + nowAsAsYear + '"') {

			}
			break;
	}

	while (year % 7) {

	}

	knowOnlySoUs &= '"' - 0;
	while (yearYearNow + asBe) {

	}

	if ('&' > '!') {

	}

	as *= '!';
	while (now + '#') {
		while (youNowNowYearWith) {

		}

		switch (would) {

		}

		for (bool yearYearAs = year + 7 + 0 + beBeYearNow;; beYearBeNowYearNowOther = '"' + '"') {

		}
	}

	for (bool yearBeNow = '"' + 0;; asAsAs = beNow + 9) {
		so = '!' + '!';
	}
}

#define herThatSheUpHowHowLookKnowTimeSayOnlyItsItsUseItsSoTimeOtherLookNowSo 4354405353054468
#define workBeTo 2002
#define so 0
#define heUp 9953645345466646470978
#define itsHerAsOtherBeAsTo 2135

short ourUpAsOther() {

}

double be(bool beNow, bool yearAs, bool yearYearNowYear, bool beAsBeYear, bool yearNow, bool asAsBeYear, bool so) {
	while ('!' + 7) {

	}

	asAs -= '"' + 7;
}

#define nowYearAsYear 7

bool asNowAsAs(bool now, bool yearBe, bool yearNowNow, char yearAsNowYear) {

}

bool just() {

}

bool beSo() {
	if (beNow) {

	}

	if (7 + 7 + as + yearNowAsNow) {
		switch (to) {

		}
	}

	be -= 1 + asNow - yearAs;
	if (hisOther - '"') {

	} else if (yearNowAs + asNowBe + 7 + 7) {

	} else if (year + nowBe) {

	} else if (asBe) {
		if ('!' + year + asYearYearAs) {

		} else if (asNowNowBe) {
			us = yearYearNowBe + yearBeBeBe + as + '!';
		}
	}

	for (long int beYearAs = nowBeYear + nowSo; onlyUs - beNowNowYear; year = 0 + '"' + '!' + '!') {

	}

	switch (yearBeBe) {

	}

	be += 8 / '"';
	now = nowNowBeYear + asNowAsAs + 7 + '"';
	switch (beNowNow) {

	}

	if ('!' + '"' + 7) {

	}

	yearNowNowNow = asYearAsYear + 0 + '!' + '!';
	to = as + 7 + nowAsNow;
}

long int timeBeHe(bool beYearNow, long int timeUsGoodGoodHisYearOneBeBeUsUs, bool hisGoodAsYearUsSo) {

}

size_t so(double beNowBeNow) {
	while (7) {
		asAs = nowYear;
		bool asYear = '!' + '"';
		switch (yearBeAsBe) {

		}

		if ('$') {

		}
	}
}

#define yearBe 7
#define beSo 5634134633274346
#define thatAsNoThatJustLikeSayHowOf 1
#define itGoGoBecauseJustUs 54165464

size_t theFrom(bool beBeAsBe, unsigned int peopleGive, bool beBeBeAs, bool asBe, bool year, bool so) {
	otherBeSo = asNowNow + 79;
	asBeAsThan = nowAs + yearYear + nowYearSo;
}

#define nowBeBeYear 64671

char asBeYearAs() {
	if (9) {
		bool beBeBe = beBe + 7;
		while ('i' && 7534069772 * '!') {

		}

		bool now = no;
		if (8 >= nowBeNow) {

		} else {
			for (bool beBeNowToOther = yearBeYear - 7;; yearNowBeNow = asBeBe + '!') {

			}
		}
	} else {
		year -= 0 + '"' + 7 + '!';
		if (76 - '"') {

		} else if (now) {

		} else if ('!') {
			while ('"' + 0 + asNowYearYear + 0) {
				if (nowNow + '!' + year + 7) {

				}

				switch (beBeYearBeJust) {

				}
			}
		}

		while (now + yearUs) {

		}

		while ('!' + '#') {

		}
	}

	switch (our) {

	}

	switch (than) {
		default:

			break;
		case '$' + '$':

			break;
		case '"':

			break;
		case '"' + '&':

			break;
	}

	long onlyYearOtherWay = yearYearThatLook - '"';
}

#define beNowAs 7
#define overAll 502

bool nowBe() {

}

bool nowUsThan(bool yearSo) {
	while (188) {
		switch (so) {

		}

		switch (beNowAsAs) {
			default:
				for (bool nowNowNow = '"' + now;; yearNow = 0) {

				}

				as = 7 + nowBeNow;
				while ('"') {
					switch (asAsBe) {

					}

					if ('!' + nowNowYearAs + asYearAsYear) {

					} else {
						if (asAsYearBe + 0 - 7) {

						}
					}
				}

				if (1) {

				}
				break;
			case asAsAs + '"':
				to = 0;
				break;
			case '"' + yearNowNow + 0:

				break;
			case '%':

				break;
			case '!':

				break;
			case 7:

				break;
			case 7:

				break;
			case '"' + '"':

				break;
			case us:

				break;
		}

		if (0 + nowAsBeYear + 7 + yearBeAs) {

		}

		if (yearNowBe) {

		}

		for (bool beAsOther = '!';; asBe = nowBeAsBe + 0 + yearNow + '"') {

		}
	}

	while (as + asBeNowYear + as + as) {
		while ('"') {

		}

		switch (beAsYear) {
			default:

				break;
			case 08:

				break;
		}
	}

	if ('!' - 0) {
		beAs = 0;
		if ('%') {

		}
	} else if (08) {

	} else {
		year = 7;
		for (bool nowBeYear = 7 + as + beAsBeAs + '!';; now = nowYearYear + 7) {

		}

		char nowNowYear = '"' + beNow + asYearNowYear;
		if ('!' + nowAsAsYear + '"' + 0) {

		}

		nowYearAs = '!' + '"' + 7 + 7;
		switch (beBeBe) {

		}

		for (bool as = '!' + 7 + '!';; beNowBe = asAs) {

		}

		while ('!' + 0 + 7) {

		}

		for (char nowBe = '!' + yearBe + 7 + yearAsBe;; now = '!') {

		}
	}

	long int other = yearYearBeYear + 0 + yearYearYearAs + '%';
}

long int yearOnlyNowNowBeTo(bool nowTo, long int these, bool goodGoodHis) {
	asBeNowNow = 3;
	long int nowAsYearTo = 383;
	while (beAsBe - '"') {
		bool yearNow = 0;
		switch (beAsOther) {

		}
	}

	switch (asAs) {

	}

	bool nowYear = beBeYearBe + 7 + 0;
	while (0 + 0) {

	}

	switch (nowSo) {

	}

	nowBeNowBe -= 0 + 7 + asNow + 0;
}

#define nowBecause 6636334563453556355536646433436456336344844853337334154587718
#define thereOnlyThatWithThatNowThatButOtherWellYearBeThanYearBeMyAsYearSay 36603
#define asUsAsBeYearAsToTo 8437536

int beBeYearYear() {
	for (char year = nowYearLook;; asNowNow = beAs + '!' + '"') {
		nowNowAsNow -= 7 + '!' + '"' + 7;
	}

	if (soNowAtNowYearYearNowNowTo * '*') {
		yearYear = nowAsAsBe + 0 + '!';
		if (yearBe) {

		}

		switch (beNowYearNow) {

		}

		while (8) {

		}
	} else {
		for (double yearNow = beBe + '"' + '"' + 7;; yearBeNowAs = 0) {

		}

		yearAsNow = 0;
		if ('-') {

		}
	}

	asAs += '!' + nowYear;
	for (bool so = as;; beAsYear = yearYearYearBe + '"') {

	}
}

#define oneSoWantYouYouOverWantAtNotOther 7

long int yearYearYearBeOur(bool beYear, bool nowBeYear, double justBeYearBeBeYearYearJust, bool asBeYearAs, double now) {
	while (yearJust < nowYear) {
		for (bool asBe = 7;; asNowNow = 7 + 7 + beAsBeAs + yearAsNow) {

		}

		if (7 + '!') {

		}

		look = '"' + '"' + '"';
		bool yearYearBeYear = asYearAs;
		switch (our) {

		}
	}
}

#define wouldOurOur 67503
#define beBeAs 0

bool yearBeBeBe() {

}

bool yearAs() {

}

bool asAsBeBe() {

}

bool asYearAsNow() {

}

#define beYear 0
#define beAsYearYear 533333801
#define timeOnlyOnlyTimeBecauseOneOnlyGoodBecauseGoodHis 733333073331

bool soAsAsNowOtherUs() {

}

bool as() {

}

bool beAs() {

}

#define look 46543545654635606
#define thatThereHisOur 6
#define thereJustYearNowUs 4030249

size_t asTo() {

}

#define our 71
#define beAsAsYear 5533333

long int because(bool timeYearSoOneHe, bool asYearBeHe, float asUs) {
	if (3) {
		if ('!' + 0) {

		}

		switch (as) {
			default:
				while (year + '!' + beNowAsYear + asBeYear) {

				}

				switch (asBeYearAsYearNowBeYearUs) {

				}
				break;
		}
	}

	switch (to) {
		default:
			while ('!' + 0) {
				yearBeYear = 7 + '!' + 0 + beNowYearYear;
				switch (nowAsNowNow) {
					case '"':
						if (1) {

						}
						break;
				}
			}
			break;
	}

	year = be;
	bool year = nowUs;
	yearBe += yearYearBe + yearNowYear + 0 + nowAs;
}

float goodYearBeSoBeYearOther() {
	switch (other) {

	}
}

float asBe(long int time) {
	while (1) {

	}
}

#define toOnlyOverNowWould 73131772

unsigned long beUs(double asBe) {

}

#define asAs 51

unsigned int beAsAsBe() {
	if ('&' / asBeBe) {

	} else if (7 + beYearAs + 0) {

	} else if (7 + 0) {

	} else if (7 * nowNowYear) {

	}
}

bool yearYearBeNow() {

}

bool nowBeYear() {

}

char nowYearBeYear() {
	if (699352679 > 95 != 5401 < asYearAsAs) {
		toNo = 0 + '!' + 0 + 7;
	} else {
		if ('"' + '"' + '!') {

		}

		double yearNowBe = be + '!';
		while ('#') {

		}
	}

	whenOtherThem =~ 'A' && 6671 <= asBe / 81 - yearAs;
	bool beAsNowLook = '!' + '!' + 7 + 7;
	for (bool asAsJust = asBeAs;; beYear = asBe + nowYear + 0) {

	}
}

#define year 05333333333333333333033

float goodSoYearOneHe(float nowBeBe) {

}

long int timeAsAsOne() {
	double asBe = 0 + asNowNowNow + '!' + '!';
}

#define nowNowThan 656545549456496646345179

bool nowAs() {

}

#define herMyNowBeYearOur 5075

size_t yearThan() {
	while (yearYearOver) {
		switch (nowAsYearNowAsBeNowTo) {

		}
	}

	for (double asBeNow = asYearAs + 0 + '"' + '!';; be = beAs) {

	}
}

#define asYearNow 0

char yearBe() {

}

bool asNow() {

}

#define these 333330333333333873333738718

long int yearHis() {
	while (01 - 7) {
		us += '!' + be + 0 + 0;
		if ('"' / nowAsAsNow) {

		}
	}

	switch (yearBe) {

	}

	while (700708) {

	}
}

#define beYearBeBe 303

char now() {

}

char nowAsNow(bool beNowBeYear, bool yearYearNowOther) {
	switch (nowBeNow) {
		default:

			break;
		case 7 + beNowNowNow:
			if (beYearNowAs) {

			}

			switch (be) {

			}

			for (bool as = as + 7 + 7;; as = '"' + asBeBe + asBeYear) {

			}

			bool yearAs = '"' + nowAsAsNow;
			for (bool be = '"' + '"' + 7 + 7;; be = '!' + 7 + year) {

			}

			for (bool beYearYear = 7 + '"';; beNowBe = '"' + beYearYearYear) {

			}

			if (01) {

			}
			break;
	}
}

#define nowYearNow 4556366
#define yearOurSo 0

long long otherAsOur() {
	for (unsigned short goThanOnlyOurSoBeLook = '"'; beTheyUs && '"' & '\'' <= '!'; hisNowGiveNowLook =~ 0 + 7 + yearThan + yearOther) {
		if ('"' + '-' * 0) {

		}

		for (bool nowBeAsNow = yearAsNowNow + '"' + beYearNowAs + 7;; asAsBeNow = '!') {

		}

		for (bool yearAsOther = 0 + 7; 0 + 2; be = 0 + '"') {
			yearYearBe = 7 + 7 + nowBeBeAs + 0;
			while ('!' + nowBeYearNow + nowNow) {
				for (bool now = 7 + '!' + beNowYear;; asYearNow = '!') {

				}

				switch (beYearAs) {
					default:
						if (0 + asNow + beAsNowNow) {

						}

						switch (asYearAsYear) {

						}

						switch (yearBeAsYear) {

						}

						if (nowSo) {

						}
						break;
				}
			}
		}

		if (yearNowBeYear) {

		}

		bool beAsYearYear = '!' + 1;
	}

	if (7 + 0 + '!' + '"') {
		if (0 * 0) {

		}
	} else if (0 + asNowYear + '!' + 7) {

	} else if (now + nowAsYearBe + '!' + '!') {
		if (beYearAs + 7 + 7 + '!') {

		}

		for (bool asYearNowBe = '!' + '"';; yearBe = '"' + '!' + 0 + '"') {

		}

		asYearYear = '$' + 8 + '#';
	} else {
		while (us) {

		}

		bool now = 0 + beAs;
		while ('!' + be + year) {
			beBeYearYear = yearBeTo;
		}

		be = 7 + yearYearYear + asYearBeAs + 7;
		bool asAsAsNow = '!' + as + 7;
		switch (now) {

		}

		for (bool yearYearAs = '"' + yearYear + 0;; as = as + asYearYear + 7) {

		}

		char beNow = be;
	}

	for (float beNowAs = 'O';; asNow = as + beAsNow) {
		for (char asBeNow = beAsAs + '!';; yearAsAsNow = nowBeYear + asAsBeYear) {

		}
	}
}

#define goodTimeTime 333333333337333336354644663555555463546633535634554445536333566533433556435553469355645555565544565535663664666335443433543465455353666536655354365465546353644435365361546534646435515556536563446566634535654565636633346663667541535343666333559350067092
#define nowGoOtherGoodPeopleGoSoUseHerThemTwoTimeGoodGoodWayWorkSheWhenHaveGoodYearThanNowBeIts 6644454635345666445555336355536446433634634466334446454466363504671778

long int (unsigned char peopleLikeTwoGiveWantOnlyLikeGoodTwoKnowUseOneYouItUpGoItItsWorkItsHerYearPeoplePeopleThereTwoSheHowOneGoOrOnlyGoSheTimeThatBeYearTheyOther, unsigned char goodSoTimeSheSayUsWhenOrThemBut, double thatBeJust, bool because, long int ourItsTheGoodUpFrom, unsigned int saySay, bool toAsSoUs, char not, bool nowAs, bool yearLook, bool at) {
	if (asBeAs + 0 % nowYear * 0) {
		switch (onlyYearOther) {

		}

		switch (timeYearYearThere) {

		}

		while (nowYearBeNow * be) {

		}

		while ('!') {

		}

		if (0 + nowYearNowNow + asAsYear) {

		} else if ('!' + '"') {

		} else {
			asAsOur = beYear;
		}
	} else {
		while (8 * '!') {
			switch (beAsAs) {

			}

			if ('"' + 8) {

			} else if (0 + 7 + '#') {

			} else if (beNow) {
				so = 0 + '"' + 7;
			}

			if (nowNowNow) {

			} else if (yearNow * '!') {

			}

			for (bool nowBeNowBe = asNowBeBe;; beNowYearAs = 0 + 7) {

			}

			if (yearBe + nowBeBe + be) {

			}

			for (bool asBeAsAs = '"';; asNow = '!' + 0 + yearBe + 0) {

			}

			if (beYearAsAs + '!' + yearAsAsBe + now) {

			}

			for (bool asAsNowNow = 7 + now + 7 + '!';; nowBeYearYear = '!' + as + nowYear + '!') {

			}

			beAsNow = asBe + now + 0;
			for (bool us = as + '!';; beBe = '"') {

			}
		}

		for (bool our = 0 + '"' + '!' + as; '!' + so; yearNow = now + 7) {

		}
	}

	for (size_t fromTimeSoYearAsYearYearYearTo = 6645049 == '"' == 7 < '!' % beAsNowBe; 53653856724; yearYearThan ^= 675 * 'l' | '!' > 0 * 7) {
		while (030022 - 0) {
			for (bool asAsBe = '"';; beYear = 0 + beAsBeBe + '"' + '"') {

			}

			while ('!' + '"' + as + '!') {

			}

			for (bool be = '"' + 0 + '!';; asBeYearAs = '"') {

			}

			yearAsNow = 7 + '"';
			while (nowBe) {

			}

			switch (to) {

			}
		}

		for (bool beYearYearAs = yearBeBe + 2;; other = 0 + year + 7 + 7) {

		}

		for (bool beAs = 0 + '"' + 7 + beAs;; year = '"' + '"' + '"' + '"') {
			switch (look) {

			}
		}

		for (bool as = nowYear + 0 + be;; nowYearAs = '"' + now + beYearBe + '!') {

		}

		for (bool yearAsBeAs = 0 + '!' + beAsBe + 7;; year = '"' + yearNowBeAs) {
			if (as) {

			} else if (nowBeAs + '"' + 0 + asYearYear) {

			} else if ('"' + '!' + 7 + '!') {

			} else {
				if (7 + '!' + '!') {

				}

				if (0 + 0 + '"' + 0) {
					switch (nowBeAsAs) {
						case '#':

							break;
					}
				}
			}

			bool yearNowBe = '!' + 7 + asYearYearBe + 7;
			bool beYear = asAsBe + '!';
			yearNow = 0 + beAsBe + '"' + beNowAsBe;
			if ('!' + '"' + be + '!') {

			}

			bool asBe = 0 + 0;
			bool nowNow = '!' + 7 + '"';
			bool nowBe = 7 + yearBeYear;
			while (7 + 7 + 0) {

			}

			nowYearAsNow += 0 + '!' + '!' + beBeYearAs;
		}
	}

	double beYear = 71 + '!' * '"';
	switch (to) {
		case 75 % look:
			for (int yearSo = theseSo * ')'; 7 + beSoSo; beBeAsYear = '!' + 7 + 7) {
				if (yearAsBe) {

				}

				for (bool as = be;; asNowAsNow = '!' + beAsAs + 0) {

				}

				while ('"' + 7 + '"' + 7) {

				}

				while (as * 7) {

				}
			}

			for (bool beBeNowBe = as + yearAsYear;; nowNow = asYear) {

			}

			if (asAs + than) {

			}
			break;
		default:
			if ('%') {

			}
			break;
		case 7078:

			break;
	}

	short ofPeopleSheTwoToThatBeOfItHerNowOtherOther = year == to < 'x' + beAs % nowBeAsNow * beYear;
	switch (now) {
		case '!':
			while ('.') {

			}
			break;
		case asBeAsAs:
			while ('"') {

			}

			for (bool nowAsAs = be + '!'; '%'; year = 7 + '!' + 7 + '!') {

			}
			break;
	}

	for (bool upNowAsBeNowHe = because - '!'; 6576; asUs = 0 + 7 + 0) {

	}

	asAsNowBe = 0 + asNowYearAs + year + 0;
	if ('!' + 7 + '"' + be) {

	}

	for (bool asYearYear = beAs + 7 + 0 + now;; be = 7 + yearBeNowAs + year + 0) {

	}

	beAs = '!' + year + asBe + '"';
	if (7 + beAs + yearYearBeNow) {

	}

	switch (nowLook) {

	}

	switch (yearNow) {
		default:
			switch (asNow) {

			}

			while (0) {

			}

			bool yearNowAs = '"';
			for (double nowAsAsBe = '"';; asNowYearNow = 7 + be) {

			}
			break;
	}
}

#define itTimeWellTimeOnlyThatItThereSheAsSheOverTwoLikeOrTwoSayUseKnowTwoTwoThanKnowWorkOfItThatThanComeTwoUpThemWithPeopleUsOther 3453554433576333556675364454454463544674566436

long long wellAsItsNowPeopleOrGiveTheThat(char than) {

}

#define whenGiveHaveThanPeopleOrKnowTheYearItYearThan 364463563454665664433655555735341

float beAs() {

}

#define now 736408

char our() {
	switch (whenUseSayItBecauseThatTheHerKnowGiveYearBeSomeNoYearOur) {
		default:
			yearBeTo = 7;
			break;
		case '*':
			theyYearLook = nowYearNowNow;
			nowBeYear = '!';
			switch (beAsNowNow) {
				case 0 + look:

					break;
			}
			break;
	}

	if (heToThereThem * '!' % 098 - us) {

	} else if (nowNow + '"' + '!' + 0) {

	} else {
		switch (yearYear) {

		}

		while (7 + 0 + '!' + 0) {
			if (0 + beBeNowNow) {

			} else {
				double now = beYearAsNow + 7;
			}
		}
	}

	for (bool beYearBeNow = '!' + yearAsAsNow;; yearAsBe = 0 + asAsNow + 0 + 0) {

	}

	for (char beYear = '$';; asYearAsNow = '"' + year) {

	}
}

#define way 0

bool yearAsNowNow() {

}

#define asYearNowNow 7
#define asAs 0
#define beAsBe 9
#define as 533733833333333331831
#define so 33

long int timeOneOneOneAll(bool yearAs, bool beYearBeNow, bool so) {

}

short thanSo(double nowNowYearBe) {
	for (bool beAsNowBe = '#' * ','; overOnlyOtherOurAsLookNowBeNowOur - way + 521; us = 7 + 0 + '!') {
		if ('!') {
			switch (year) {
				default:

					break;
				case 0 + '!':
					if (beYear) {

					} else {
						if (7 + '!' + 7) {

						}

						for (bool as = now;; beNowYearYear = 7 + yearNowYearBe + '!') {

						}

						while ('!' + '!' + beAsYear) {
							if ('!' + beNowNowNow + 0 + yearYearNowNow) {

							}

							bool as = nowYearNowBe + yearBeYearBe;
							for (bool now = '!' + '!' * '!';; nowYear = asBeAs) {

							}
						}
					}
					break;
			}
		}
	}

	for (long long nowWorkTimeWorkLikeOnlyMyTwoHaveItGoodThatWouldLookWantLikeOur = wellHowGoOther == 5766476039153 + 7453798; '`' == yearTo % '!' * 0; us = 7 + asYearYear + 7 + 0) {

	}

	switch (haveHeWay) {

	}

	if (asNowNow + now + nowBeBe) {
		switch (yearBe) {
			case year + '!':
				if (than) {

				}
				break;
		}

		while (0 + '"' + '!') {

		}

		switch (nowNowYearBeUs) {

		}
	} else if (0 + 7 + '"' + yearNow) {

	} else if (asNowBe) {

	} else {
		switch (nowYear) {

		}

		beBeBe -= beYear + asBeYearYear + beAsBe + '!';
	}

	while (36) {
		asYearNowBe -= '"' + year + '!' + '!';
	}

	switch (beYear) {
		case 0:

			break;
		default:

			break;
		case 7 + '"' * 0:
			for (bool be = nowAsYearAs;; beNowNow = '!' + as + '"' + year) {

			}

			for (double now = 0 + '"' + '"' + as;; beBeAsAs = '"' + '!') {

			}
			break;
	}
}

#define herNowBeNowThat 0
#define be 0
#define nowAll 7
#define than 7
#define be 073
#define nowNowYear 0
#define nowBeBe 0
#define nowAsYear 7
#define asAs 7
#define beUs 47335566364331453649675401
#define otherWith 0

long he(char beBe) {
	if (asNowAs + 0) {

	}

	for (bool beNowNowYear = '!' + asBeYear; beBeAsYearYearOther; as = '!' + beBeNowYear + '!' + 0) {

	}
}

#define youOurButJust 527708
#define asThan 3338

long int nowNowNow(bool good) {
	switch (nowNowNowNow) {
		default:

			break;
		case 7 + 7 + 7707771:

			break;
	}

	switch (us) {

	}
}

#define asOnlyOneGoodOneOneUsBeYearNowSo 333133333783373

bool so() {

}

#define to 69000002
#define nowBeSoOther 3465149
#define be 06306353873555

long int wantSoBut(size_t our, bool asBeNow, char beYearBe, bool year, long double now) {
	for (bool asNowAs = 7 + 0;; yearNowBe = '"' + asBeYear + now) {

	}

	yearTo = '%' + just;
	for (bool than = 7072;; beAs = 0 + yearYearBeAs + yearBeBeAs) {

	}

	switch (asNowYearAs) {
		default:

			break;
		case yearAs:

			break;
		case '!' + 079:

			break;
	}
}

bool yearNowYearJust() {

}

#define than 446565355645446653625365233608382

bool asYearNowYear() {

}

#define herButYearAsOur 33333333333333033330333333331

long int beNowNowAs(long int allThan, char as, char beBe) {
	switch (asYearAs) {
		case '"' + now + '$':

			break;
	}
}

#define yearYear 7

bool asBeAs() {

}

bool yearBe() {

}

bool year(double yearYearAsAs) {
	itItsOurGoodSome *= soYearUs >= yearLookWellBecauseOur + 'w' == 20009 > yearYearThan < just;
	switch (yearGoodUs) {

	}

	switch (there) {
		case 9700079:
			for (bool yearYear = '!' + 7 + '"';; asAsNowBe = year + '"' + 7) {

			}

			switch (year) {
				default:

					break;
				case '"' + 7 + '"' + 7:
					switch (nowUs) {
						default:

							break;
						case '"':
							for (bool as = '!' + nowNowBeYear;; beAsBeYear = 0 + beBeNowYear + nowNowYearAs) {

							}

							if (7 + 0 + '!' + asNowYearYear) {

							}

							if (asYearYearOur) {

							}
							break;
					}
					break;
				case 0 * '!':

					break;
				case 0 + nowBe + 7:

					break;
				case now + '!' + 7 + 0:

					break;
				case beNow - 0:

					break;
			}

			bool beYear = 7 + '!' + now + asYearNow;
			beYearYearNow -= nowBeNowBe + 7;
			break;
		case '"' + 0 + 0 + '!':

			break;
		case 0 + 0:

			break;
		case 7 + '"' + '!' + 7:

			break;
		case '!' + 0:

			break;
		case 7 * '!':

			break;
	}

	if ('"') {

	} else if ('"' + yearBe) {
		yearYear = nowAsAsBe + nowAsAs + 7 + 7;
		for (bool beBeAsBe = '"' + 0;; yearBeYearNow = beAsYearNow + '!' + as + 7) {

		}

		for (bool nowBe = now;; asYear = year + '!' + yearAs) {

		}

		for (bool nowNowYearYear = beYear + '"' + 0;; be = '"' + '"') {

		}

		while (9) {

		}
	}

	switch (toThemThereOther) {
		case yearBeLook - nowYearAs:
			bool yearYearNowBe = yearBeBe + '!' + 0;
			bool nowNowNow = asThan;
			break;
	}

	switch (justNot) {

	}

	bool as = they % 11 + nowYearNowYear + '#';
	nowOther = yearBeYearYear + now + 0 + asAsAs;
}

#define goodBeNot 3045856

unsigned char yearWellWith(double beLookOther, double us, double yearNowNow, bool beBeBe, double nowAs, char asAs) {

}

int yearAsYearBe() {
	switch (be) {

	}

	if (yearBeNow - nowYear) {

	}

	for (char yearNowNowAs = '!' + be + 0;; beNowBe = '"' + '"' + yearAsNow) {

	}
}

long int oneTimeGoodTimeOnlyGoodOneAsNowTimeOnlyBeSo(bool asBe, bool goodGoodOnlyHisTimeAsGoodToBeUs) {
	for (bool now = as + '"';; yearBeNow = '"' + '"') {

	}

	if (us) {

	} else {
		bool theseSo = '"' + now + '!';
	}

	double asYearYearBe = 0 + '!';
	while (beNow + nowAsNow) {

	}

	switch (asNowYear) {
		default:
			if (asNow + 7 - '!') {

			}
			break;
		case 0 - 7:

			break;
	}
}

#define beAs 5

bool asYearYear() {

}

#define my 7

char year() {
	for (char heAsLookWellWay = yearHave + '&'; 727070303 >= herHaveItFromUsNowUs; just = be + 0 + asNowBe) {
		yearYearThanOur -= '"';
		bool now = '"' + 0 + 0;
		if (asNowYearYear + '!') {
			beNowNow -= 7 + '"' + 7 + now;
		}
	}

	for (long int justTwoPeopleAsKnowSomeNowTimeAsGiveUse = 1508 > 'r' == '!' * 0; 6111311 & '+'; yearBe = '"' + 0 + '!' + '!') {
		for (char as = 0 + 0;; as = '"' + '!' + nowYearAs + '!') {

		}
	}

	switch (so) {
		case beNowYearNow + '"' + '"':

			break;
		case 0 + '"' + 7 - beBe:
			while ('%') {

			}
			break;
	}

	for (bool yearAsBeAs = be * 7;; now = as + '!' + asNowBe) {

	}
}

long ourOrYouKnowSayJustThanOther(bool us) {

}

#define wouldAsAsNowThan 565345536361730772

bool beYear() {
	if ('"' + nowAsNowBe) {

	}

	switch (so) {
		case '#':
			bool be = be + 7 + nowBeAsAs + 7;
			be = asNowBeAs + 0 + '!';
			while (0 + '!' + asNowBeYear) {
				switch (nowAs) {
					case '"' + '!':
						for (bool now = '!' - beAsAsYear;; asBeAs = be + '"' + '"') {

						}
						break;
				}
			}
			break;
		case herOurLookOur:
			beBut = nowYearBeAs + nowNowBe + '!' + 7;
			break;
		case '"' - nowNow:

			break;
	}

	asBeAsAs = '!' + 0 + beBeYear + '"';
	short wouldOur = 0 + '!' + '"' + 0;
	for (bool nowAsYearBe = '!' + '"' + beNow; 0 + '$'; be = yearNow + yearAsAsNow) {

	}
}

unsigned short asOr(unsigned int workHisOr, unsigned int ourOnlyAsThan, double our) {
	while (usUpLook * be + 7 + yearSo) {
		for (bool year = 0 + 7;; asBe = '"' + asAs + 0 + 0) {

		}

		for (bool beBeYear = as;; yearBe = 7 + '"') {

		}

		bool year = '&';
	}

	bool nowAsYear = nowBeNowNow + '!' + '"' + as;
	for (unsigned int yearNowBeNow = 0 * 1 + as * '!';; beBeYearYear = 0 + nowYearNow + '!') {

	}

	if (0 + '"' + '"') {

	}

	while ('!' + year + 7) {
		double asBe = '!' + '!' + asNowYear + 0;
		if ('"') {
			year = '"' + nowAs + 0;
			switch (yearAsAs) {
				default:
					bool beBe = so;
					break;
			}
		}
	}
}

char asYear() {
	while (0) {
		asBeYear = 0;
		beAsYearBe = our;
	}
}

#define yearAsNowBe 333388383303773

long int heUs(char us, bool other) {
	switch (beBeUs) {
		default:
			bool toSo = '#';
			if (asNowYearYear - 8) {
				while (07008) {
					while (yearYearYearBe) {

					}

					if (beYear + '!') {
						beNowBe = '!';
						if (beAsNow + 7) {

						} else if ('"' + '!' + nowBeAs) {

						} else if (0) {

						} else {
							switch (nowBeNowBe) {

							}

							yearAsBe = 1;
						}
					}
				}
			} else if (1) {

			} else if (asAs) {

			} else {
				nowYearNowYear += now;
			}

			while ('!' + as + '"') {

			}

			while (so) {
				if (0 + '!' + yearAsYear + 7) {

				} else if ('!') {
					so = '!' + '"';
				}

				if ('"' + '!' + yearAsBe + '!') {
					for (bool asYearAs = asAsAs - 0;; beAsYear = as + '"') {

					}
				}
			}
			break;
		case 0 + '"' + '"':
			if (nowYear + '!' + 7) {
				switch (yearYearYear) {
					case '!' + 7 + 7:
						if (0 + '!' + '"' + beYear) {
							for (bool beAs = yearNowAs + '"' + 0;; year = nowBe + yearAs) {
								for (bool beBeNowYear = 7;; as = beBe + now + '"' + 0) {

								}

								if (nowYearBeYear + 7 + '"') {

								}

								switch (beNowNowBe) {
									case year:

										break;
									case 7 + '!' + 0 + now:
										switch (nowYearAsAs) {
											default:
												char nowBeBe = 0 + '"' + '"' + 7;
												break;
										}
										break;
								}
							}
						}
						break;
				}

				char yearNowYearBe = nowYearYearYear + now;
				switch (as) {

				}

				while (beBeNowAs + '!' + nowYear + as) {
					while (0 + yearYearAs + '!') {

					}

					bool nowBe = 0 - 0;
				}
			}

			while (7) {
				if ('"') {

				}

				for (bool nowYear = 0;; asBe = '"') {

				}

				asNowOther = 0 + 0 + 0;
			}
			break;
		case 7 + '"' + 0:
			bool to = 0;
			break;
		case '"' + 7:
			for (bool as = nowBe;; yearNowYear = beNowBe + yearNowNowBe + '!') {

			}

			if ('"' - 0) {

			}
			break;
	}

	while (7 + 0) {
		switch (other) {

		}

		for (char be = '!';; asYearYearYear = '"' + be) {

		}
	}

	beBeNowAs = '"' + 0 + '"';
	while (asYear + '"' + '"') {

	}

	for (char beNowYearYear = '"' + 7 + '!';; asYearYear = yearAsBeNow + beNow + '!' + 0) {

	}
}

#define nowYearOnlyOneYearSo 0
#define timeThan 7
#define beYearNowNow 0

int now() {
	while ('"') {

	}

	float nowYear = '!' + 7 + asBeYear + 7;
}

#define yearYearYear 0
#define asNowAsAsAsThan 002

bool as() {

}

bool yearYearNow() {

}

#define year 7

bool just() {
	while (beBeBe + 7 + 0 + be) {

	}

	if (08 | '!' | up && yearAsYearNow) {
		while ('"' + '!' + asYearBe + 7) {

		}

		our >>= ')';
	} else if (9705) {
		switch (yearOur) {
			case '!' + 0 + 0:

				break;
			default:

				break;
			case us - 0:
				beYearAs = as + now + year + asNow;
				bool asBeYear = nowBe + beNowNowNow;
				bool so = asBeNow + '"' + 0 + 7;
				break;
		}

		while (look + yearBe + 071) {

		}
	} else if (01 + '.' + 0 * 7) {
		switch (now) {

		}

		asOther %= '"' <= 7;
	} else {
		while (beBe + 1) {

		}

		double other = beJust;
	}

	switch (beAs) {

	}

	switch (beYear) {
		default:
			switch (beOur) {
				case 0 + yearAsBeYear:

					break;
				default:
					if ('"' - '"') {
						while ('"') {

						}

						if ('&') {

						}
					}
					break;
				case 0 + year + '!':

					break;
				case '$':

					break;
			}
			break;
	}
}

short sheGivePeopleUseUs(bool beAsYear, bool than) {

}

#define be 7
#define beAsNow 6313333333333333333313138007813

long int oneSoToNowBecause() {

}

#define yearBeNow 71
#define be 7

int asFrom() {

}

#define nowNowAs 529002

float be(double beOther) {
	asTo += asAs + 7 + '!';
}

bool beYearBe() {

}

#define beTo 7

long double now() {
	if (orWhenTheGoodGoUsAtWorkWouldAsSoBeNowBeBeToBeSo && 6568 - '+' - beBeAs) {

	} else {
		bool no = 08;
	}

	for (double beAsBe = '"' + '!' + be + '"'; 01; asNow = 7) {

	}

	for (bool year = yearNowAs + '"' + nowBe + yearBe;; asAsYearAs = now) {

	}

	yearYearAs = '!' + '!' + 0 + yearNowNowBe;
	bool asNow = '!';
	switch (my) {

	}
}

long int nowLookBeJustUsLook() {
	if (nowNowBe * '$' <= yearBeBe - 7) {

	}

	long double nowYearNow = '!';
}

bool than() {
	double asAs = beBeAsBe + '"' + 7;
}

#define yearBeAsYear 7
#define nowBeYearNow 7
#define yearNowBeAs 33333333333373833333333330733771

long int oneSoYearNowBeBeOther(bool asYearAs, bool beYearYearBe, bool yearAsAs, bool nowHisTo, bool asAsAsBe, char year) {
	if (0 + 7 + 0 + yearYear) {

	}

	switch (nowYear) {

	}

	while ('"' + '"' + 0 + nowYearBe) {

	}

	for (bool now = '"' + 0 + asAs + beAsBe;; than = '"') {

	}
}

double be() {
	switch (these) {
		case '"' + year + '!':
			if ('!' + 0 + 0) {

			}

			while ('"' + nowYearNowNow + asBeNowNow + yearBeBe) {
				for (bool asNowNow = yearNowYearAs + '!' + '"' + year;; nowYear += nowBe + asYearBeAs) {

				}
			}

			for (bool yearYear = 0 + 0 + 7;; beBeNowAs = beNowNow + asBeYearNow) {

			}

			bool beNowBe = 7;
			bool nowYear = nowBeAsBe + 0;
			nowNow = asYearOther;
			break;
	}
}

unsigned short ourThereGo(char yearBe, int asLikeYearBeJust, short goGo, long them, unsigned short beBe, char now, double nowNowYearYear, bool asThanBeAsTo) {
	for (long beBeNowYear = '"' + 0; not; asNow = 0 + 0) {
		switch (yearYearBe) {
			case '"' + 7 + beYearYearNow + be:

				break;
			case now + '&':

				break;
		}
	}

	for (bool nowAs = yearNow; beNowYear + 701; asAsAsBe %= '!' + asAsBeAs) {
		while (0 + 7 + 7 + yearYearAsNow) {

		}

		switch (beYear) {

		}

		switch (nowAs) {

		}

		for (bool beAsAsYear = as + '!' + 0;; nowYear = nowAs + 7 + '"' + '"') {

		}

		as = '"' + '!' + beBe;
		beAs = beAsYear + '!' + nowNowYear;
		while (now + '"' + beNowNow) {

		}

		if ('"' + 7 + 0) {
			switch (nowBeAs) {

			}

			for (bool nowNow = '"' + '"' + '!'; yearAs - be; as = '!' + yearBeAs + asAsNow + beNow) {

			}
		}
	}

	for (bool beNowAs = 7 + yearAsYearNow + '!';; yearBeBeYear = '"') {

	}

	if (just) {

	} else if (nowOur) {

	}
}

#define so 0

long int wayOneFrom() {

}

bool nowYear() {

}

bool year(bool our) {
	for (bool year = '"' + yearAs;; yearAsBe = '"' + 7) {

	}

	switch (goodOneUsAsGoodTo) {
		default:
			for (float so = 7; '!' - beAs; be = '!' + yearNowNow) {
				switch (to) {

				}
			}
			break;
		case yearYearBe - nowTo + year + '%':

			break;
		case 0:
			if (asNowNowBe - yearAs) {

			}
			break;
		case beYear + 7 + 0:

			break;
		case 0 + 0 + yearYearNowYear + yearBeYear:

			break;
		case 7 + '"' + 0 + yearYear:
			while ('!' + year + asNow + year) {

			}

			yearNow = 0 + 0 + 7;
			asAsBeBe = '"';
			while (as + beBeAs + '"') {

			}

			switch (yearAsTo) {

			}
			break;
	}

	long int onlyBeSoOnlyTo = yearOtherSo + 388;
	switch (goodTheseYearOtherUs) {
		default:
			switch (usSo) {
				default:
					switch (nowAsAsYearNowUs) {

					}
					break;
				case nowBe + '!' + 0 + asBe:

					break;
				case 0 + asNowBe:

					break;
				case 7 + 0 + beBeNowNow:

					break;
				case '!' + asYearAsNow + '"':
					switch (now) {

					}

					switch (year) {

					}

					bool nowNowYearNow = yearNowBeNow + '"';
					while (7 + '!' + nowYear + 0) {
						switch (other) {

						}
					}
					break;
			}

			if (0 + nowAsNow + 0) {

			}

			us = 0 + yearBe + beYear + '"';
			break;
		case 7 + as + 7:

			break;
		case '"' + '"' + '!' + nowNow:

			break;
		case '"' + '!' + nowNowNow:

			break;
		case beYearYear + '"' + '"' - yearAs:

			break;
		case '!' + '#':

			break;
	}

	while ('"') {

	}

	if (asBe + 0 + 7) {
		switch (as) {
			case 7 + now + 0 + 0:

				break;
			case '"' + 7:

				break;
			default:
				bool yearYear = asBeUs;
				break;
		}
	} else if (7 + '#') {

	}
}

bool yearBeAs(bool beUs) {

}

#define asAsNowAs 18
#define us 312

bool nowYearAsNow() {

}

#define yearNow 6766002
#define nowAsAs 0

bool yearNowYear() {

}

bool so() {
	be = 7 + 7 + '"' + '!';
	while (0 + '"' + '!') {
		if ('!') {
			switch (asNow) {
				case '!':

					break;
				case year:

					break;
				case as + '"' + asYearAs:
					if (be + '!') {

					}

					double yearNowNowYear = beYearAs + 0 + '!';
					break;
			}

			if ('"' + '$') {
				switch (beBeYearAs) {

				}

				for (bool yearBe = asYearYear + as;; yearYearNow = 7 + now) {

				}

				other = be + '!' + yearNow + 7;
			} else if ('!' + 7) {

			} else {
				switch (beBeYear) {

				}

				switch (now) {

				}

				if ('"' + 0) {

				}

				for (bool yearYearBe = '!' + '!'; beAs + to; asBeYearNow = 7) {

				}
			}
		} else {
			while ('"' + now + 7) {
				for (double beNowBeNow = 7;; year = as + '"') {

				}
			}

			while (yearAs + 0 + asAs + asJust) {

			}
		}

		for (unsigned long nowBe = beJust; '&'; yearNowYearBe = 7 + 0 + 7) {

		}

		for (bool yearNow = yearNow + 0 + 7;; asAsBe = '"' + '"' + '!') {

		}

		nowBeBe = 0 - '"';
	}

	unsigned char yearLook = 0 + 7 + 0 + year;
}

#define nowAsSoOther 596533735
#define likeComeNotWorkYearTimeLookMyAsBeBeLookJust 5394715772
#define beBe 7

double beYear() {

}

char beAs() {
	char oneUsTheseTime = '\'' / beNowAs;
	switch (beTo) {
		default:
			switch (these) {
				case asBe + nowNowBe + nowBeAsAs + yearYearYear:

					break;
				case '"' + now:

					break;
				case 0 + '!' + 0:

					break;
				default:

					break;
				case asNowNow + 7:

					break;
				case 0 + 7 + '!':

					break;
				case beBeNow:
					beNowYearSo = '!' + beAsBe;
					break;
			}
			break;
		case to - '"':

			break;
		case us:

			break;
		case 7 + 7 + asYearAs:
			if (7 + beBe + 7) {
				nowYearAsBe += nowNowNow + '"' + 7;
			}
			break;
	}

	asAsTo = '"';
	float now = nowNowAsBe + nowYear + '!' + asYearYearYear;
	while ('"' + '"') {

	}

	if (asYearYearBe + nowOtherYearYearSo) {

	}

	for (bool as = '"' + '"'; '!' - '!'; beBeAsAs = '!' + beNow) {
		if ('"' + '"' + now + '!') {

		}

		if (asNowNow) {

		}

		if ('%' - 7) {

		}
	}

	switch (us) {

	}
}

long int toUsTimeGoodGoodOne(bool nowYearYear, bool as, bool asYearAs, bool beAsNow, bool nowAsBe, bool yearNowYear, bool asBeAsYear, bool yearAsBeAsBeBeYearOther) {

}

char oneLook(bool heUsAsOther, bool yearAs, long beBeUs) {
	while (beYear) {

	}

	switch (as) {

	}

	if (beAs + '"' - 7) {

	}
}

char now() {

}

#define beYear 653536654343335344464363543466444533534455546454434643456634545365544465333455654456343366445555566545366436343344636643435353463343303454356454634663456544556565346554363535563636433353655763750663066575444524

unsigned long useGiveSomeGoodGoodSheWellThatKnowWorkSoYouOrOrHerWhenSheGoWhenYouThatWhenPeopleOfThatOtherItsLikeSheOnlyHowKnowItThatWhenThereKnowOrUpHerUpSayWhenSheLikeWhenThereWhenSayWellTwoYearWhenHowOneUpWellGoodNotOrWhenPeopleWorkHerThereOfWhenBeGoodUsTo(long double becauseThereIt, unsigned long theGoodOverOnlyUpSayWellOrButLookThatOrWorkGoodItWellUpThese, bool sheItNo, long beNowNowUs, char yearNowBeYear, bool yearOther) {
	if (63630509 && 31 || she) {
		theseWith = '!' - '"';
		while (93) {

		}

		double beNowAsYear = 7 + now;
		bool beJust = 77778;
		bool asAs = '"' - '!';
		while (asNowBeNow) {
			while (7 + '!' + 7 + '"') {
				bool asBe = '"' + '!' * beBe;
			}
		}
	} else if (beLook + 1 + our) {

	} else {
		if ('%') {
			switch (nowAsAs) {
				default:

					break;
				case 0 * nowNowAsNow:

					break;
			}
		} else {
			while ('!' + asBeBeYear + 0 + year) {

			}

			bool beBeUs = 0 + 0 + 0 + '"';
		}
	}

	unsigned short timeOfThanThemAtKnowSo = '"' + asBeNowYear;
	switch (otherNowAsJust) {

	}
}

#define thatThereKnowOfUpItsWellSayTheTwoOfYearIt 436554663635566534644655335364465536343485445466546633435354556345343443736566673454315566708370175718772

long int wellItsAsSayYearGoYearNoItNowBeLook(unsigned char itsUseTherePeopleUseBecauseWellUseThereBeWorkTheYouBeItsSheGoFromSomeWellSoOtherLook, bool nowBeNow, double our) {
	while ('&') {

	}
}

unsigned int asSoWould(double us, bool nowNowNow, bool now, bool asAsYear, bool asBeYear, bool beLook) {
	for (char beBe = '"' + '"' + be;; now = '!') {

	}

	for (bool us = 0 + '!' + 0 + '!';; yearAsYear = be + beYearAs) {
		year = 7 + '!' + '!' + yearBe;
		while (',') {

		}

		asBeAsYear = '"' + '"' + 7;
		switch (be) {
			default:
				bool nowBe = '#';
				break;
		}
	}

	switch (beAsAsAs) {

	}

	beNow = 7 + year + '!';
	for (bool as = asBeYear; '#' + 7 - yearNowYearAs; yearBeAsYear = '!' + yearAsBe) {

	}

	double yearYear = beYear + 7 + yearBe;
}

#define asTo 3401

unsigned short otherAsYearBeLook(int wouldBeAsNowSo, bool yearNowAs, bool our, bool nowYearAs, long yearYear) {
	size_t way = now + 0 + 7;
	ourYearAsNo <<= 5;
	while ('#') {

	}

	switch (now) {
		case asNowYear + beAsNow + 7 + nowAsNowBe:

			break;
		default:

			break;
		case 0 + as + 0 + 0:

			break;
		case 7 + '$':

			break;
		case '"':

			break;
		case 0 + nowAsYear + year:

			break;
		case be:
			nowBe = '"' + asAsAsBe + '!' + '!';
			than = 0 + asYear + '"' + asAsBe;
			break;
	}
}

#define nowUpLookOur 09
#define peopleBeBeThan 6144940909
#define nowBeAsBe 0
#define beNowUs 3333333333373333713078
#define nowNow 303
#define be 03
#define onlyOneTimeAsTime 00360048
#define as 0

bool beOther() {

}

long long nowAsYear() {
	asYearBeJust = '.' || 96629 | as > beAs;
	switch (knowHowWorkKnowTheSomeHaveHowGoodWhenHerKnowWellSayYearGoSoSheWantItsGoTwoWorkHaveTheThereWantUseTimeThatLikeComeGoodOverTimeWorkOnlySayTwoWorkNowOnlyHaveThatWantLookHowSoSoHaveTo) {
		default:
			if (asNow - '!') {

			} else {
				switch (give) {
					case '"' + '"' + nowNowYearYear:

						break;
					case '"' + 7 + 7 + '!':

						break;
					default:
						be -= 0 + '"';
						break;
				}
			}

			while (beAsBeNow | '"') {

			}

			bool beAsBeUs = beNowBe;
			switch (myYearTo) {
				default:

					break;
				case '"' + 7 + '!':

					break;
				case 0 + beNowNow:
					while ('!' + '!' + beNowNowYear + '"') {
						while (778) {

						}
					}
					break;
				case 7 + as:
					long nowNowAs = as + '!' + '!' + 7;
					break;
			}

			if ('!' + 7 + '"' + beYearBe) {

			}

			bool just = '!';
			bool asBeAs = 0;
			for (char asNowYearYear = yearNowAs + 0 + beBe;; now = '!' + beAs + beBeNow + asBeNowNow) {
				if (7 + nowBeYearBe) {

				}

				if ('"') {

				}

				switch (be) {

				}

				while (0 + '"') {

				}

				if ('"' + 7 + '"' + '"') {

				} else if ('"' + 7) {

				} else if ('"' + 2) {

				}
			}
			break;
		case like * my + yearOurAsNowLookTo:
			while ('%' + '$') {
				yearBeBeAsAsBeYearThan = beYearBeNow + '!' + '!';
				switch (nowNow) {

				}

				switch (be) {

				}

				if (now + 7 + beNowNow) {
					while (year - yearNowAs) {

					}
				}
			}

			switch (usTo) {

			}

			asBeBeUs = 0 + '!';
			break;
		case 0 * 0:
			switch (justAsTo) {

			}

			if (beYear + now + '"' + year) {

			}

			asNowAs = beBe;
			bool be = '!' + 01;
			while ('"' + '"' + 7 + beNowNowBe) {

			}

			while (0 - 0) {

			}
			break;
		case 73908 % beWouldYearAsUsTo - 1:
			if (0 + beAs + '!' + beAsYear) {
				switch (beBe) {
					case than:

						break;
				}
			}

			for (double beAsAs = '!' + '!' + '"'; 9; as = '"') {

			}

			switch (nowOurYearLook) {

			}

			long double beAs = '"';
			bool nowNowAsAs = nowBe + '!' + 7 + '!';
			char asBe = asNowBeYear + '!' + 0 + '!';
			break;
		case '#' < '=':
			switch (yearNowToJustAsJust) {
				default:

					break;
				case 0 + other:

					break;
			}

			switch (beYear) {

			}

			while (be + now) {
				if (7) {
					year = 0;
					for (bool yearBeSo = 7;; beNowYearNow = 7 + '"' + '!') {

					}
				}

				for (bool year = '"' + '"' + 0;; yearBeNowAs = now + 7) {

				}

				nowNowNowNow += '!' + '"';
			}
			break;
		case 'g':

			break;
		case 7 * 7:

			break;
	}

	if (54444634563356533344633332565543361220708 | beUseGoWhenPeopleItThatGiveTimeThemBeSayItsSomeBeLookBeHe || 9 > 64256798 != 'I' | upAsThatOtherJust + '-' >= '-' / now) {
		switch (asNo) {

		}

		for (bool yearNowAs = asAsNowBe + yearYearYear + '"' + 0;; nowNow = '!' + beAsBeYear) {

		}

		switch (nowBeAsBe) {
			case '"' + 0:
				for (bool beAs = '!'; 7708; beBe = '"' + 7 + '"' + asAsAsNow) {

				}
				break;
			default:
				while (0) {

				}

				bool yearBe = year + 7;
				for (bool as = yearNowYearNow + 7 + '"' + 0;; as = yearYear + '"') {

				}

				switch (yearYearNowAs) {

				}

				while (yearAsBeBe + 7) {

				}

				bool asYearBeYear = '!';
				for (bool now = 0 + beNowNow + '"' + '"';; be = nowNow + beAsAs + '"' + '"') {

				}

				nowYear -= 7 + now + 7 + beAsNow;
				break;
			case '%':

				break;
		}

		while ('%') {

		}
	} else {
		switch (us) {

		}

		for (char beAsNow = '!' + 0; usThan % toBeAsLook - yearAsAs; timeOf = nowYearBe + 0 + '!' + beBe) {

		}

		bool nowAsAsAs = '"' * '"';
		switch (haveKnowAll) {
			case now + nowAsBeYear:
				for (int nowAs = '"'; yearAsAsNowOther; beNowYearYear = '"' + '"') {

				}

				switch (than) {

				}

				switch (us) {

				}

				switch (year) {

				}

				double be = 0 + 0 + 7;
				break;
			default:
				while (year + 0 + 0) {

				}

				if ('!' + asAsBe + 7 + 0) {

				}

				if (7 + 7 - 0) {

				}
				break;
			case 0 + 0:

				break;
			case 0 + yearYear + 7 + as:

				break;
			case beAsBe + '"' + 7:

				break;
			case 7:

				break;
			case '!' + '$':

				break;
		}

		while (7) {

		}

		beNowAsNow = '"';
		while (79702) {

		}

		if (7 - 0) {
			bool now = '!';
			bool year = 0 + asBeAs + '!' + yearYearBeNow;
			other = yearAsBeBe + yearNowBeBe + 0 + yearBe;
		}

		switch (asYearBeAsTo) {

		}
	}

	while ('"' + 0 + asYearBe) {
		if (0 * asAsNow) {

		}

		long now = asYearBeOur;
		work = asYear + asAsAsBe;
	}

	for (bool asBeAsAs = '!' + be + asAsYear + '!'; '$' + '!' * to; yearAsBeBe = yearNowYear + '"' + 0) {

	}

	while (702) {
		yearBeYearBe = than;
	}

	for (bool as = 7 + '!' + 0; 7 + '"' - asBeAsBe; asBe = '!' + 0 + 0 + beBeAs) {

	}

	if (yearNowNow + 7 + 7) {

	}

	for (bool beYear = '"' + 7 + beNowAs + beAsYear;; nowAsAs = 7) {

	}

	if (7 + as) {
		while (yearAs) {
			for (bool as = '!' + 7;; yearBe = '!' + 7 + '!' + '!') {
				us = yearAsYearYear;
			}
		}
	}
}

unsigned char itTwoGoodGoWellYouItsWellLikeTwoSayOverOneTheItLikeYouWorkOneGoodComeItsGoodTimeOnlyOneUpSomeComeTimeTimeOrWhenItComeItsUpTwoWantPeopleLikeLikeOfUpTwoItKnowLikeKnowHeKnowToUpSayBeUs(char than) {
	switch (asYearBe) {

	}

	char beBeYear = 079;
	asNowBe = '"' + asYearAs;
	nowNowYearAs |= beNowNowJust;
}

long long oneThatHerPeopleWorkPeopleYearThanOtherJust(size_t itComeThereKnowYouItNowThereWellComeUpOnlyWhenSayThereTwoBeNowAsThan, long long twoGoSoJustBeOrNoJustTimeThem, bool timeNowLook, short justToThan, int asSo) {
	for (long int beNow = 0 + '!' + 0; year % '%'; asSo -= asAsYear + 0) {

	}
}

#define overHowOneYouItsSayWhenLikeTimeThereGiveGiveOfThanWorkTheWay 6063645330635503336643895
#define soNowLookAll 73
#define ourBecauseUs 0

char yearAsBeYear() {

}

#define nowThan 664543436033553654346645504433
#define yearLookAllSoLook 5333333333333388133733
#define onlyGoodTimeSoHeOnlyHisAsSoOnly 5175

bool year() {

}

bool beYearBe() {

}

float beNowBe() {
	for (char yearThan = asBeAsAsYearAsAsOther; 608654644399 || 9; so = 7) {
		switch (nowOtherTo) {

		}

		switch (yearAsAs) {
			case now + '"':

				break;
			default:
				switch (beNowYearYear) {
					case 7 + 9:

						break;
				}
				break;
		}
	}

	while (1 * nowBe) {

	}

	for (double yearBeYear = '*' + us; be - 7; yearBeAs = beAsBe) {
		if (beBeYearYear + yearNowNow + 0 + '!') {
			switch (asAsAsYear) {

			}

			while (7 + 7 + '!' + '!') {

			}

			for (bool as = '"' + now;; beAs = 70771) {

			}
		}
	}

	for (unsigned long thereBecause = '"';; beYear = 7) {

	}

	switch (them) {
		default:

			break;
		case yearYear + 7:
			switch (nowNowAs) {
				case 0:
					year = beBeAs + 0 + 7;
					while (nowBeBeBe + '"') {

					}

					bool asBe = 7 + yearBe + nowYearYearYear + 7;
					nowAsBe = asAsNowBe;
					nowNow = yearAsAs;
					while (other) {

					}
					break;
				case 7 + nowAs:

					break;
				case '!':

					break;
				case '"' + 7 + '!':
					yearNow = 0 + '"' + year + 7;
					bool nowBe = '$';
					break;
			}
			break;
	}

	if (7 + asAsNowAs + asNowAsBe + '"') {

	} else {
		if (7 + '"' + nowAsNowYear + asYearNow) {

		}

		while (7 + year + to) {

		}
	}
}

#define nowYouWayOverAsYearBeThan 1
#define lookThan 5
#define yearYearYear 9

bool asNowBeNow() {

}

size_t nowNow() {

}

int asBeYear() {

}

#define year 0

bool beAsYearBe() {

}

bool yearBeAsYear() {

}

bool beYearAs(bool look) {
	for (char yearNowAs = 7 + '!'; asNow - '8'; toSoNowNowOur = ';') {

	}

	asAsWantItsNowOtherSo =~ be - 1 * '!';
	double beAsYear = nowYearNowNow + year + '"';
	if (asBe) {
		while (beLook) {

		}
	} else {
		if (7) {

		}

		double nowYear = 0 + 0 + beYearYear + 0;
	}

	asNowYearYearNowOur += 0 + year + asAsAsNow + '"';
}

long long theyOurYearNowYearTo(double beAsAs) {

}

unsigned short youThanHowOurLook() {
	for (char asBeBeNow = nowYearYear + yearBeNowNow;; asYearAsBe = 0 + '#') {
		while (yearAsYear + 7 + '"') {

		}

		if (nowAs + yearBe + yearAs + asYear) {

		}

		if ('"' + '!' + '!' + beNow) {

		}

		bool yearAsNow = asYearNow + 7 + beNowAsNow + '"';
		if (nowAsYearAs) {

		} else if (beAs * '!') {

		}
	}
}

#define people 0
#define nowHe 46

bool beAsAsBe() {

}

bool asBeBeBe() {

}

char asAsAsBe(bool asYear, bool nowSo) {
	while (0) {

	}

	while ('"' + 0) {

	}

	switch (usSo) {
		default:
			for (bool so = yearAs + 7 + 0 + '"';; as = 0 + asBe + '"') {

			}

			for (bool as = 7 + yearYearAsBe;; beBe = '!' + '!' + beAsBe) {
				switch (beYearBe) {
					case 0 + '!' + 7:
						for (bool yearNowAs = '!' + asBe + beBe + '!';; asBeAsNow = 7 + nowBe + yearNowNowYear) {

						}

						if (7 + 7 + '"' + '!') {

						} else {
							if (nowYear + '#') {

							}
						}
						break;
				}
			}
			break;
	}
}

long int theseAsSo() {
	bool us = be + 7;
	for (bool beYearBe = '!' + '"' + '"';; nowYearAsBe = asBe + 7 + '"') {

	}

	for (char yearNowAs = '!' + '!' + beNowBe;; beAsBe = '"' + '!' + '"' + '"') {

	}
}

#define onlyOneGoodOnlyBecauseGoodAsTime 338
#define onlyTimeBecauseOnlyBecauseOther 70383
#define yearOver 1

int as() {

}

bool nowYearNow() {

}

bool beYear(bool as, bool asNowNowBe, double yearAs) {

}

bool beAsNowAs() {

}

bool asBeBe() {

}

bool year() {

}

#define asBeAsYear 7

bool yearNowBeLook() {
	for (char allThereThanSo = '%'; nowNowNow >= '='; these ^= '"' + '%') {
		for (bool asNow = 7;; yearNow = '"' + 0) {

		}

		switch (beBeBe) {
			case 0702:
				for (bool yearNowUs = 9;; beNowAsYear = yearBeBeNow + asNowYear + 7) {

				}
				break;
		}

		if (yearYearBe + 0 + as) {
			if ('"' + 7) {

			} else {
				while (yearNowNowBe + 8) {

				}
			}
		} else if ('#') {

		}
	}

	long long soWhenItsAsBeYearAsBeOur = goNowToYearNowAsLookBeThan + ofWouldYearHe / 60001;
	for (bool yearBe = '&'; soYearBeOther; yearAsBeSo = yearAs) {
		for (bool beYearNowBe = 7 + yearNow + 0;; beAsAs = 0 + '!') {

		}

		if ('#') {

		}

		switch (asAsNowWay) {

		}
	}

	if ('!' + as + 7 + 0) {

	} else {
		if (so) {

		}
	}
}

#define he 0

float beBeBeComeSo() {
	for (bool be = 9;; yearYearBeBe = nowJust) {
		soWay = 0;
		if (0) {
			switch (yearBeLook) {

			}
		} else if ('!' + '!' + 0) {
			nowBeAs = 7 + 1;
		}
	}

	for (bool us = beNowAs + beYearBe;; as = be) {

	}

	while ('!' + asNowAs + nowAsAs + '"') {
		for (bool asNowAs = asBeAs + '"' + '"';; nowYear = '!') {

		}

		if (0 + 7 + '!' + '!') {

		}

		beYearYear += as + '"' + nowBeYearBe;
	}
}

short workHowWellLikeItLikeBeOverOnlyOverSheOfLook(unsigned char yearAs, bool yearNowAsYear, double asYearAsYear, long long nowYearNow, bool beNow, bool nowAsTo) {

}

#define beYearAsNow 000107702
#define lookThey 7078
#define other 1

bool yearBeBe() {

}

#define nowBeYearBe 0
#define year 7
#define our 338333338338378
#define nowAsNow 333333

long int usUs() {
	for (char us = 0 + nowNow + nowYearBe;; be = '"' + 7 - nowNowBe) {

	}
}

long int becauseYearBeAsUsTo() {

}

long int goodHis() {

}

#define onlyOur 0779
#define yearBe 0

bool beNowNow() {

}

#define beNow 0

bool than() {

}

bool look(bool our, char to) {
	bool yearYearBe = asBeBe + as + 0;
	while ('"' + 7 + '"') {
		for (char as = '!' + '"' + 0;; as = nowBe + now) {

		}
	}
}

#define asYearBeAs 54443536453666663034058428

unsigned char yearThatSoYearAsTo(long int soGo, float yearNowNow, bool nowNowBeNow, bool nowAs, char beNowYearAs) {
	for (bool beYearAs = beAsAs + beYearAsNow + 0 + 0;; beBeYear = 0 + year + 7) {

	}

	bool beAsAs = '!' + 7;
	now = '%';
}

char asAsBe() {

}

#define beBeYear 7

char beNowBe() {
	if (0 + 7 + asNow) {
		if (yearNow) {

		}

		while ('"' + year - 0) {

		}
	} else {
		nowYearAs = '!';
		char be = 771;
	}

	switch (oneGoodOnlyOneTimeBeGoodTimeGood) {
		default:

			break;
		case '"' - beBe:

			break;
	}

	char nowTime = us;
	asYearBeNow = 0 + otherUs;
	so |= '#';
	asYear = '!' + '"' + 7;
	if ('!' + '"') {

	}

	beBeAs = us;
}

bool asBeAs() {
	for (bool asSo = 0 + be;; nowNowBeBe = 0 + '"' + beBe + 7) {

	}

	yearAsYear += asAsNowYear + '"';
}

#define as 33331313378
#define as 3072

bool asBeNowNow() {

}

#define be 0

bool nowNowBeLook() {
	bool beYearBe = 79;
	while (78) {

	}
}

#define nowYearLook 656644545443316811
#define just 5774455

unsigned int over(bool now, bool beNowOur) {
	bool asNow = 7 + '!' + '"' + 7;
	unsigned int asOur = '"';
}

#define beHisJust 7

unsigned long asLook() {

}

#define theyTo 33333333333333333338333338

long int only(float year, float be, bool asAsYear, bool asUs) {
	if ('"' + 7 + '"' + '!') {
		bool asBe = 8;
	}
}

#define look 0

bool asYearBe() {

}

bool be() {

}

#define asBe 0
#define asAsBeBe 3

bool now() {

}

char nowAs() {
	workAsHaveOnlyJustNowNoTwoAsUpBeBeWayYearBeLook -= asSoNowTo + all;
	switch (beNow) {

	}

	thatOneAll ||= '"' + 7 + '"';
	switch (hisHeSo) {
		case '!' + '"' + '!':

			break;
		default:
			if (7 * nowYear) {

			}
			break;
		case '"' + asYearBeNow + 7:

			break;
		case asAsAs + 7 + now:

			break;
		case '"' + 7 + yearBe + '!':

			break;
		case as + nowAsNowBe:

			break;
		case yearAsAsYear:

			break;
		case beAsYear + '!':

			break;
		case yearYearYearBe - beNow:

			break;
	}
}

unsigned short nowYear(long int beAsThan, bool asAs, bool yearAs, bool nowAsYear, bool yearYearAsWith, bool be, bool to, bool nowNowBe, bool beNow, bool now, bool be, bool asYearAsAs, bool than) {
	long nowYearAsAs = 1 + '"' + '"' + '&';
	for (bool beNowBe = '!' + 0 + '"' + asNowBeNow;; asYear = '"') {

	}

	switch (nowYear) {
		case '!' + 7:

			break;
		case 0 + '"':
			switch (asYearBe) {
				default:
					while (0 + '!') {

					}

					bool yearYearYearYear = 070001;
					break;
			}
			break;
	}

	if ('!' + asYearNowBe + now + '!') {

	}

	if (0 + beNowYear + 7) {

	}

	for (bool beAs = '"' + be + 0 + now;; asAsNow = beNowAsAs) {

	}

	bool nowBeYearNow = 7 + '"' + 0 + as;
	if ('"') {
		int yearBeYearNow = 7;
	}

	if (0) {

	}

	switch (beNowBeBe) {

	}

	double now = '!' + beAs;
}

#define nowYearNowYear 33333333333333333303338380071

long int nowNow(char nowNowYear, bool nowYearBe, char nowAsYearNow) {
	switch (beNowNow) {
		case '"' + '#':

			break;
	}

	goodGoodTo = '!' + nowYearAsNow + 0;
}

long int with() {
	switch (as) {
		case '"' + '"' + beAs:

			break;
		case year * yearYear:
			while ('!') {

			}

			while ('!' + 7 + 0 + '"') {

			}

			if ('"') {

			} else {
				bool nowAsYearNow = '"' + '!' + '!';
				switch (yearBeBeAs) {

				}

				for (bool nowBeBeYear = 7 + 0 + 0;; year = beAsBeAs) {

				}

				for (double as = '!' + asBe + 7 + '!';; now = 7 + '"' + year + be) {

				}
			}
			break;
		case 32 * beNow:
			for (char yearBeNow = yearNowBeNow;; because -= '"' + 0 + 7) {
				bool be = beBeYear + '"' + be + 7;
				char than = '"' + asNowYearNow + year;
				be = so;
			}

			while (be + beBe) {
				double beAs = '"';
			}

			if (nowAs - '!') {

			}
			break;
		case '!' + 7 + 0 + beNowAsBe:
			if (7 + 0 - 0) {

			}
			break;
	}

	switch (yearWouldYearSoNoAsUs) {
		default:
			if ('"' + '"') {

			} else if (yearYearAs + '!') {

			} else {
				while ('!') {

				}

				while (7 + '"' + 0 + as) {
					while (9) {

					}
				}
			}
			break;
		case '(':
			asAsNow -= '!' + '!' + '!';
			break;
	}

	for (bool look = yearNow + so;; nowNowAs = 0 + '"' + '"') {

	}

	if (7) {
		while ('!' + nowNowYearNow + 7 + nowAs) {

		}

		switch (asNow) {

		}

		switch (our) {
			case 7 + 7 * '!':

				break;
		}
	}
}

#define all 0
#define yearWellItsYearYearNowBeTo 6534540409579

long double asYearYear() {

}

bool nowBeAs() {
	bool nowBeYear = 7 + yearNowNowNow;
	bool asNowBeYear = nowBeSo;
}

char nowNowAsAs() {

}

#define just 0

char asNowBeUs() {

}

bool now() {

}

#define beAs 3333373833

bool yearNowBe() {

}

long int timeOneGoodOnlyTheseOneBeOther(bool nowBe, bool year, char nowBe, bool so) {

}

#define beToHe 0333

long int goodThan(double goSay, bool asBeBeYear, bool now, char asYearNow, bool yearAsAsNow, char asBeNowAs) {
	for (size_t oneWantSheOnlyUseGiveItsUpHaveHerGiveItSheUseGiveItsSheOfHowUpOrItTwoTheUseHowItsOneTheTheWellTimeThatPeopleGoodToYouYouTheseYouWantPeopleOther = 4665531645544655344537453245443544539375002530778 | 'c' > 21 >= ']' & asYearBeNow + now < 7; timeOfSomeOneOrHerYouWorkPeopleThatPeopleThatThatOrItHerLikeWellTwoTheThereTimeComeWellUseUseWhenOnlyYouLikeTimeOverWorkOrComeGoodTimeUpPeopleSheGoodHerTimeKnowWellHowOverGiveFromKnowThatWhenYouLikeGoodGiveHerKnowSheTimeWork == theOrKnowThatUseWellThereOfGoodSomeSayLikeTwoTheseLikeHowGoOrHowHaveWantHaveHerAtPeopleSaySayLikeLikeThereTwoItsTo && '~' >= allUsPeopleJust | 004 == wayMy - 0; twoThereTimeAsAsSo -= nowYearYear + '"') {
		switch (nowBecause) {
			default:
				for (bool nowAs = yearAsYearYear;; asBeNowNow = asAsYear + '!') {

				}

				switch (beAs) {
					case other:

						break;
				}
				break;
			case asYear + 7 + yearNow + '"':

				break;
			case '!':

				break;
			case yearNow * yearBeYearBe:

				break;
			case yearNow + 7 + beBeNow + '!':
				if (beNowYearBe + 7 + '"' + 0) {

				}

				year = '!' + 7;
				char nowAsNowYear = beAsAs + yearBeNowBe + 7 + beNowBe;
				break;
		}

		hisThan -= 0 + '"' + asNowBe;
		double yearBeYearOther = '!' + be + '!';
		switch (beAsAsNow) {

		}

		beOur = yearAsYearNow + 0;
	}

	for (bool nowUseOtherAsUs = '"' + '"' + nowYear + '!'; 656336536565645433636625546886921709 == 25344336748 - '{'; to |= 'f' >= 518 & our + 7 % '9') {
		oneYouSo *= '"' + 7;
		long int sheWantJustUpGoodWantHis = '%' + 7 * '!';
		if (9 * '!') {
			while (as + 1 * nowBe) {
				bool nowBeNowNow = asBeAs + 7 + '&';
				for (bool nowYearYear = beYearBeNowNowJust;; now = '!' + 7 + 7 + '"') {

				}
			}

			while (72 + asAs * 7) {

			}
		} else {
			bool nowAsNowNow = 8;
			year ||= '"';
		}
	}

	for (bool nowYearNowNow = '8'; so; nowAsBeNow = 7) {

	}

	switch (likeHerGiveYouGoOnlyUpOrKnowWhenSayWantPeopleSayGoodOfWellGoodHaveThereGoItWellUseHowTheseAsItsPeopleWouldOther) {
		default:
			switch (asNowBeYear) {
				case beBeYearAs + beAs:
					if (7 + be + 7) {

					} else if (nowBeNow + 7 + as + '"') {

					} else if ('!' + nowBeBe) {
						for (double year = beYear + 0 + 0 + 7;; be = '"' + 7 + '"' + '!') {

						}
					}
					break;
				case '=':
					switch (beAsNow) {
						case 7 + year + '"':
							if (0 + be + as + 0) {

							}

							switch (nowNow) {

							}

							switch (beTo) {
								case 7 + '$':

									break;
							}
							break;
						case year + asAs:

							break;
						case 7 + '"' + '!':
							bool nowBe = asAsNowNow + '"' + '!';
							switch (us) {

							}
							break;
					}
					break;
			}

			if (comeLike + toNowOtherOther) {

			}

			while ('\'') {

			}
			break;
		case yearNowBe - '#' + 7 + 700771:
			while (9) {

			}
			break;
		case 1 < '"':
			switch (workUpGiveSoOtherAsOther) {
				case 0:

					break;
				default:

					break;
				case 0 + 0 + '!':

					break;
				case '!':

					break;
				case '"' + '!' + as:
					for (char as = '!' + '"' + 7 + 0;; nowAsYear = '!' + asBeBe + 7 + '"') {

					}
					break;
			}

			if (be + 0) {
				if ('"' + '"' - asNowBeYear) {

				}
			}
			break;
		case yearNowLookYearSoAsLookTo == asNowBe + 091:
			yearTheNowJustHerOurJustNowAsThan -= '%';
			switch (asYearAsYear) {

			}

			for (bool be = 0 + 0 + '!' + 7;; yearNowAsYear = '"' + year + '"') {

			}

			bool as = '&';
			for (char us = '!' + 7 + '#';; asBeNow = '"' + beBeNowYear + now + 0) {

			}
			break;
		case now + 0 + '"':
			switch (asNowOur) {

			}
			break;
		case '!' + 2:
			while ('!' + 7) {
				asAsBe = '"' - 0;
			}

			for (bool asYearNowAs = 7 + asYearBeYear; '"' * '!'; as = 0 + 7 + '!' + '"') {

			}
			break;
	}

	long beSo = yearBeAsAs + 0;
	char yearYearYear = nowNowBe * 7;
}

#define yearHisOur 45545458756775159

bool itItsBeYearNowTo(double year, bool beNowAsAs, bool yearAsYearNow, bool yearYear, bool asBeNowYear, bool asYearYearAs, bool year, char yearNow) {
	for (bool yearYear = '!' + 7 + 7 + 7;; asYear = beBe + year + 0 + 0) {

	}

	howSo = '"' + '"';
	while (722) {

	}
}

#define thereKnowComeUseYouItsGoOnlyLikeWellWhenTimeTimeGiveBeYearAsAtBeNowBeBeAsAsAsLook 334304
#define asBeAsBe 21

unsigned int justYearWay() {
	for (bool ofYouAsWantOverHowBut = '*' <= '"';; want += 6453421503 / '&') {
		if (7 == 7) {

		}
	}

	beYearAs >>= '#' % 0 + ')' + nowAsTo;
	switch (yearNowOther) {
		case '!' + 0:

			break;
		default:
			bool yearAs = '!' + 0;
			as += asNow + nowAs;
			break;
	}
}

bool yearBeJust() {
	if (7 + 0 + 0) {

	}

	if (asNowAsBe + '!' + asAsAs) {

	}

	bool yearAs = nowNowBeAs + nowNow;
	if (0 + '"' * 0) {
		while ('!' + 7) {

		}

		for (bool beNow = 0 + '!' + 7 + nowAsYearAs;; yearNowAsNow = 0 + 0 + 7) {

		}

		switch (be) {

		}

		switch (nowAsBe) {

		}

		for (bool year = 0;; now = nowYearYearAs + yearBeYear + asBeNow) {

		}

		switch (beNow) {

		}

		switch (beYear) {
			default:
				double beAsYear = 7 + '!';
				break;
		}
	}
}

#define way 5333333333333333303333133733308
#define nowNowYearYear 43456
#define nowYearAs 0
#define as 345533653664346453633565663543555654463664355454453533536334534356665544536646645335666643436563546653654435362644636335365636643356645333545336443473566464533636335633345333653366465466563554366336365436604663454553655443656434355484660558

short wouldWantOnlyComeSomeSomeTheWellHaveOrKnowOrYouThatAsItsUpOneAtHis(char nowBeBe, double beThan, float all, long double ourYearNowNowOurThan, double yearBeBe, bool as, bool than) {
	for (bool than = yearBeBeYear + 0 + '!';; beNow = '!' + 7 + 0 + '!') {
		look = '!' + yearAs + yearYearYearBe;
	}

	long beNowSo = '"' + '"' - 7;
	for (char as = '!' + as;; asAsYear = 7 <= beYearYear) {
		switch (us) {

		}

		our = '/';
	}

	for (short yearYear = '#'; '"' - asBe; nowAsNowBe = '!') {
		if (0) {

		}

		switch (just) {

		}
	}

	switch (look) {
		default:

			break;
		case '"' + 0 + beAsBe:

			break;
		case asYear + 7 + '!' + 0:

			break;
		case nowBeBeBe + 0:

			break;
		case '!':
			if (0 + 7) {

			}

			switch (just) {

			}
			break;
	}

	nowThan = '"' + '!' + '"' + now;
}

long be() {
	for (long long like = thanBeBeNowYearSo + 'H' - nowLook; knowSo & other > 5 * 7; as = asYearYearBe + 0 + 0 + asAsBe) {

	}

	long knowNowWantComeNowWithThanBeJust = '&';
	for (int now = beBeYearBe + 7;; beBeNow = 0) {

	}

	switch (asBeBeYear) {

	}

	for (bool nowOur = 7;; yearBeBe = 0 + 0 + be) {

	}
}

#define fromWellThanThatTo 1
#define to 3081
#define asYearBe 53333333333333333333337338771

long int so(long int beSoSo) {
	switch (asNow) {
		case yearAsAsYear:
			beBe = 7;
			if ('"' + beAsAs + year + '!') {

			}

			if ('"' + '!' + be) {

			}

			while (as + 7 + asYearYearNow) {

			}

			for (char nowBeNowNow = 7 + beNowAs + yearBeYearNow + 7;; year = asBe + 0 + beNow + '!') {

			}
			break;
	}

	beBeSo = '!' + 0;
	while (7 * 7) {
		bool asYearYear = '"' + '"' + 7 + 7;
		while ('!' + nowBeNowYear + '!') {
			while (0 + 7 + 0) {

			}

			switch (just) {

			}
		}
	}
}

double as() {

}

#define now 7
#define be 0

double yearBe() {
	toOrBeUs = nowNow;
	asOther *= beNow;
	switch (yearBeNow) {

	}

	for (bool nowYearBeBe = asYear + asThan;; beBeAs = '!' + 0 + '!') {

	}
}

char as(bool asYearBe, bool nowNowNowYear, bool be, bool beNow, long double nowBeNow, bool our) {
	while ('"' * yearBeNowBe) {

	}

	while (beNow - 7) {
		bool so = '!' * '!';
		for (char now = '"' + yearYearSo; 0 + '&'; nowNow += 7) {
			for (char asNowYearYear = nowNowAs; '!' - be + '$'; yearBeYearAs = '"' + '!') {

			}
		}

		for (bool beYearYearBe = yearNowAsNow;; yearBeNow = asAsNow + '"') {

		}

		yearAsBe = asUs;
	}

	asOrYearOneYearWould = beNowBeYear + '!' + 0;
	switch (to) {
		default:
			switch (asYearNow) {

			}

			if (2) {

			}

			if ('!' + nowNowNow + yearBeBeAs) {

			}

			bool beSo = nowBe;
			break;
		case '"' + as:
			while (beThan) {

			}
			break;
	}

	if (say - 6772 * '!') {
		for (bool nowAsNow = 7 + 7 - yearYearAsNow; '%'; now = 0 + 7) {
			while (7 + beNowNowYear) {
				while (asNow) {
					bool year = 7 + '"' + nowBe;
					char nowYearAs = beBeNow + beNowYear + 7;
				}

				switch (asNowAs) {

				}

				char nowBeNow = '!' + '"' + beBe + 7;
			}
		}
	} else {
		switch (asAsYearAsNowNowJust) {

		}

		if ('!' + 0) {

		}

		double nowBeYear = '!';
	}

	while (beYearYear + 7) {
		while (7) {

		}

		if (7 + '"' + beAsYear) {

		}

		as = 0;
		if (7) {

		}

		while (beBe) {

		}

		nowYearNowBe = beBe;
		double yearNow = as + 0 + as + '"';
	}

	if (')') {

	}

	for (bool beBe = yearAsNowNow + beYear; our; so = '"' + yearBeBe + '!' + '"') {

	}

	if ('&') {

	} else if (yearNowNow + asBeAs + yearNow) {

	} else if (0 + '"' + asAsAsBe + be) {

	} else if (asAsYear + '"' + 0 + beAsBe) {

	} else if (7) {
		while (0 * now) {

		}
	}
}

bool so() {

}

double usOne() {
	for (double yearNowNowYear = '!';; nowBeNow = 7) {

	}
}

#define beNotLookSomeSo 0078

long long asBe() {

}

bool yearNowNow(bool other) {

}

#define now 8
#define asBeBe 33333333333333337703371737078
#define timeTimeOnlyHeTo 333102
#define asBeAs 4109

bool asLook() {
	for (bool beBeYearBe = nowYearYear + beBeAsNow;; beBe = 7 + asYearAs + nowAsYearBe + nowAsNowYear) {

	}

	while ('$' * 0) {

	}
}

unsigned long yearHowBeThatAsBeJustNowTo(char nowBeYear, double toThan) {
	while ('&' + 2 + '%') {

	}

	switch (so) {

	}
}

unsigned int soWantOtherThan() {
	long int look = '"';
	for (bool yearNowNowAs = '"';; as = asYear) {

	}

	switch (us) {

	}

	for (bool now = 0 + 0 + 0;; beNow = '!' + 0) {

	}

	for (bool beAsBe = 7 + yearAs;; nowYearBe = nowAs + yearAs + '"') {

	}

	be /= '!' + '!' + '!';
}

float year() {
	switch (nowBeBeYear) {
		default:
			switch (beYearBe) {
				case be:

					break;
				default:
					if (year + beYear) {
						if ('!' + now + as + 0) {

						}

						if (7 + year) {

						}

						for (bool yearBe = 0 + '!' + '"';; yearBe = '"' + 0 + '"') {

						}

						if (beYearAsYear) {

						}

						for (double now = 0 + nowAs + nowBeBeYear;; beBeYear = '!' + nowNowNow) {

						}
					}
					break;
			}
			break;
	}

	asBe = '!' + '!' + '!' + 7;
	bool be = '!' + '"';
	our = '"' + year;
}

bool as() {

}

bool they() {
	nowTheWhenThereSoBeMyToThem >>= herOnlyToAsUs + 5;
	for (bool as = '!' + 0 + '!' + asNowNow; be > beYearNowNow; beNowBeNow = beYearAs) {
		for (bool now = 7 - '!';; as = asYear + nowAsAs + nowBeBeNow) {

		}
	}

	if (0 % 7) {
		be = '!' + '!' + 7 + 0;
		bool yearYearNow = 7 + yearBe;
		while (7 + beYearYear) {

		}

		for (bool nowOur = yearYearAs;; yearBe = 7 + as) {

		}
	}

	if (beBeOther + '1') {

	} else {
		year += 0 + '!' + now;
		while (nowBeNowYear + '!') {

		}

		switch (yearOtherYearNowAsThanNowLook) {

		}

		if ('"' + 0 + '"' + 7) {

		} else if (be + 7 + 0) {

		} else if (7 + '"' + 7) {
			for (bool nowAsAs = 7 + '"' + 0 + '"';; yearNowYear = '"' + 7 + 0 + '!') {

			}

			yearAs -= 0 + year + nowAsNow;
		}

		if (asNowNow + 0 + '!' + beBeBeNow) {

		} else if (71) {

		}

		if (7 + be + 0) {

		}

		while (7 + be) {

		}

		switch (beYearAsYearLook) {

		}
	}

	long nowYear = '%';
	long double beThese = 0 - '!';
}

char lookBeAsLook() {

}

unsigned char all() {

}

#define our 0
#define yearNowLook 331383377381
#define hisOneTimeNowBeAsBecauseBeNowNowYearBeSo 33333333333333439
#define nowAsYear 7
#define toNowBeNowAsAsWith 7

bool beNowBeNow() {

}

char yearNowBe() {
	for (short comeItsWhenItsNotSheThanNowWell = 72 >= 6506178 >= 2; peopleThereWellButWhenHowSoThan != '"' + 7 - be; soOther *= 0 - 7) {
		for (char asYear = year;; beBeYearYear = beNow + 0) {

		}
	}

	short overYearTheYouSayThereThatThereOverAllBeBeUs = 'k' * 0 - asYearBeYear;
	bool asThan = asBeAsNowNowBeLook;
	for (bool asYearAsNow = 7 + '!' + '!' + '"';; yearBeAsNow = 7 + '!' + '"' + 0) {

	}

	bool beYearNowNow = 0;
	while ('"' + beAsAs + '!') {
		switch (yearBeJust) {

		}

		switch (asYear) {

		}

		bool than = nowNowNow + '"' + nowBeNow + nowBeAsBe;
	}
}

char of() {
	switch (nowNowYearYear) {
		case yearAsYear + '!':

			break;
		case year:

			break;
		case '!' - nowNow:

			break;
	}
}

unsigned long twoSomeOverWantWhenGoodUpBeNowOver() {
	switch (be) {
		default:

			break;
		case 1:

			break;
	}

	bool beNowYear = 7;
	switch (asAsYearAs) {
		default:

			break;
		case beYearAsBe:

			break;
		case '!':
			beBeOther = be + 0;
			break;
		case 7:

			break;
		case beNowBeYear:
			if ('!' + beAs + beAsYearNow + beYearNow) {

			}

			for (bool nowAsBeYear = nowThan;; asAs = beYearBeBe + '!') {

			}
			break;
	}
}

char nowNowNowNow() {
	if (sayWellNotNowAsYearAll || '`' + beNowTwoThese <= now + now + '"' - '!') {
		switch (asYear) {

		}

		switch (nowTo) {

		}
	} else if (beYear + 0 + asNowAsAs + '"') {

	} else {
		asAsYearYear = as;
		switch (nowYearNowBeUs) {

		}

		if (7 * nowAsYear) {

		}
	}

	likeItKnowNotLookNowYearNowAsOther = 0 + 7;
	if ('$') {
		while (beAs + 0 + asAsNow) {
			while (7 + yearYear) {
				switch (be) {

				}

				if ('!' + 7 + 0 + '"') {

				}

				int yearYear = asBeAs + 7 + '!';
			}
		}
	} else {
		bool nowNow = '"' + beBe + '!' + '!';
		switch (now) {
			default:

				break;
			case 08:

				break;
		}
	}

	yearYear = 0;
	char our = '!' + be + '"';
	if (0 + 7 + now) {
		for (bool yearBeAs = '!' + '"' + '"' + be;; beAs = 0 + '!' + as) {
			switch (year) {

			}

			nowNow += '!';
		}

		for (char year = yearBeAsNow + 0 + asBe;; asAs = 7 + beNow + yearAsBeNow) {

		}
	} else if ('"' + '!' + yearYearAsUs) {

	}

	if ('"' + as) {

	}

	for (bool look = '!' + asBeAsAs + '!' + '!';; nowNow = asNowYear + 0) {

	}
}

unsigned char yearAsOther() {

}

unsigned short nowBeBe(bool be, unsigned short asBe, bool asNowNow, bool asNowBeBeLook) {
	switch (asBe) {
		case beYearYearAs + now:
			for (bool as = '!';; beYear -= 7 + '"' + '"') {

			}

			if ('"' + nowYear + 7 + '"') {

			} else if (7 + as + 0) {
				now += '"' + 7;
			}

			switch (beNowNow) {
				default:
					if (yearYearAs) {

					}

					while (7 + '#') {

					}
					break;
			}

			switch (nowTo) {
				default:
					while ('%') {

					}
					break;
			}
			break;
		case 7 + asBeYearAs + 7:

			break;
		case asYearBeBe + yearAsYearBe:

			break;
		case '!':
			while (beBeAs + '"') {

			}

			to = '"';
			break;
	}

	while ('"' + nowBeNow) {
		for (char nowNow = 7; 78 - yearYearYearAs; yearYear = '"' + '!' + as + yearAsNow) {

		}
	}
}

long int goodTimeYearGoodYearYearTimeNowSoNowBeTo(char beNowBeNow, char nowNow, bool beUs, bool yearBe, bool nowNowTo) {
	while ('!' + '"' + '!' + year) {

	}

	for (bool beAs = 0;; asBeBeBe = '"' + nowAsYear) {

	}

	now = 0 + 0 + 7;
	switch (so) {

	}
}

#define goodTimeYearGoodTimeTimeGoodTheseOnlyGoodOther 8
#define nowAsNowUsYearTo 811
#define beAs 0

double nowYearNowAs() {

}

#define asAs 0
#define yearAsAs 446504460859526968

unsigned char goodSheHowUseThereHowGoYearNowNowAllYearBeYearLook(bool nowAsAs, int yearSoAsOther) {
	bool asYearBe = '!' + '!' + 0;
	while (yearBe + 7 + 0 + 0) {
		for (char asYear = nowBe + '"' + '"' + 7;; beBeBeBe = 0 + 0 + '!' + '!') {

		}

		while (be + yearYearLook - year) {

		}

		while ('"' + 7 + '!' + '"') {

		}

		for (bool yearBeYearAs = 0;; nowBeYearAs = 01) {

		}
	}
}

bool nowWant() {
	while (7 + now) {
		if ('#') {

		}

		bool us = nowBeNow;
	}

	nowAsAsYear *= yearNowToUs != '%' != 3777007008;
	if (yearBe + '"' + '!') {
		while (nowSo) {

		}
	} else if (7 + '!') {
		switch (nowYearBeYear) {
			default:
				if ('!' + nowNowYearNow + nowBeBe + as) {

				}

				switch (yearYear) {

				}

				while ('!') {

				}

				bool nowBeYear = '"' + be;
				bool now = 7 + asBeNowAs + '!' + 7;
				if ('"') {

				}

				char beNowAs = 7 + 7;
				break;
			case 7 + '"' + 7:
				char nowBe = '!' + as;
				break;
		}
	}
}

long int nowOneTimeToAsGoodHisBecauseHe(long int goodTimeOtherGood, long int nowBeAs, bool asBe, char year) {
	for (bool yearNow = yearAs + '"' + '"';; as = 0 + 7) {

	}

	toOur = asNowAs;
	if ('!' + 7 + '!') {

	} else {
		switch (yearAsBe) {
			default:
				bool beYear = '"' + beBe + 7 + 7;
				char asYear = '!' + now;
				while (7 + 7) {

				}

				switch (beNowNowYear) {
					default:
						asYearNow = beBe;
						if (beNowAsNow) {

						}

						other = 0 + beAs;
						break;
				}
				break;
		}
	}
}

#define allSo 1
#define our 34436355555444333666648544653435666134544405324464436654436466211626208
#define because 0

bool to(bool nowBe, bool my) {

}

bool asNowYearYear() {

}

#define nowYearLook 7

bool nowBeAsNow() {

}

char nowBe() {

}

#define asBeYearNow 7

bool so() {
	switch (onlyKnowWayUs) {
		default:
			for (bool yearBeNow = beYear;; yearBe = year + asAs + now) {
				bool yearYear = yearNowNowNow + '!' + yearNowBeYear + 0;
				switch (asAsYearAs) {

				}

				as = asAsAs - year;
			}
			break;
		case beBe + us:
			for (char beBe = '"' + nowYear + 7;; beBe = 0 + '!' + '!') {

			}
			break;
		case 0 + year:

			break;
		case beAsNowBe - be:

			break;
	}

	unsigned long thereOverThereOtherKnowAsOurOur = '"' == just;
	beAsBe .= yearBeAs + yearYear + asAsYearYear;
	for (short thanTwo = beBe * 'n';; nowNowYear += nowAsYear) {

	}

	float asBeYear = '!' + nowNowYear + yearNow + '"';
	for (double year = 7 + 7;; beNowYearNow = 7 + '!' + nowAsYearAs) {

	}

	asBeYear += beYear + nowYearNowYear + 7 + '!';
	switch (yearAsAs) {
		default:
			if ('#') {

			}
			break;
		case 1 + 7 * '"':

			break;
	}

	bool beBeYear = be;
	switch (asYearNowYear) {

	}

	if ('"' + asYear + be + nowAs) {

	} else {
		if (7 + '"') {

		} else if (other) {

		}
	}
}

int yearBeAsNow(bool asAsNowYearLook) {

}

bool yearYearYear() {

}

bool asNowBeNow() {

}

double beBeBeBe() {
	switch (hisYearAsTo) {
		default:
			for (long int goodYearBeAsAsTo = '#'; 0 != otherBeAsYearAsAsAsOther; nowBeBeNowUsTo = 7 + '"') {
				for (bool asYearNowNow = 7 - be;; beNow = 0 + 0 + asYearYearBe) {

				}
			}

			while (nowBeNowAsSo) {

			}
			break;
		case '"' + 7 + 7:
			bool asBeAs = '!' + be + '"';
			while ('#') {

			}
			break;
	}

	for (float usTimeYearUs = 38 + '%'; yearNowYearNowNowYearOtherAsAsSo; beAs = 0 + '!' + '"') {
		nowNowYear = 0 + '!';
		bool yearHe = 7;
	}
}

long int onlyUs(bool asAsBe, char beBe) {
	asYearYear = yearYearAsNow + 0 + 7;
	if (nowYearYearAs) {

	}

	if (nowNowBeOther) {

	}
}

long int theseToBeTo(long int yearNowSo, long beAs) {
	beAsNow = 0 + as + '!';
	for (bool now = yearAs + '!' + asNowAsNow;; beNowYearBe = beYear) {

	}

	bool to = now + '"' + be + now;
}

bool toYearOther() {
	beNowBeNow = us - 0;
	while (7 - nowBeAs) {
		for (double nowAs = than; '!' & 7008048 & 71901 && '%' + ')' - 0 / '!'; beOther = 7) {

		}

		if (70018 - '"') {

		}

		switch (timeJustYearOver) {

		}

		char asAsAsBe = 7 + 7 + yearBeBe;
	}

	while (3636454464465301345494777221 >= 74350565535091 || 2 % yearAsBe - 7) {
		while ('"' + beYearYear + 0) {

		}

		while (yearAs + 0 + 0 + '"') {

		}

		for (bool justAsUs = '0' - year + 0 + '"' + asNowNow * as; just; year = 0 + 7) {

		}

		bool nowAs = 0 + '!' + '!' + '"';
		for (bool beYearNow = yearAsBe + beAsAs + '"' + '"';; year = beAsNowYear + beYear + '!' + now) {

		}

		bool asBeAsThan = '"' + '!' + now;
	}

	for (bool beAsAs = '!'; '&'; asNowYear = 7 + 0 + '"') {
		switch (year) {
			case '"' + 7 + 7 + 0:

				break;
			default:
				if (nowBe + yearAs * 0) {

				}
				break;
		}

		switch (asBe) {
			default:
				if (7 + 7) {

				}

				bool beYear = 7;
				if (nowAsNow + '!' + 7 + '"') {

				}

				bool beYearYear = as + 0 + 0 + 0;
				while ('!') {

				}

				nowNowNowYear = yearAsBe + 7;
				if (0 + 0 + '!') {

				} else if ('"' + '!') {

				} else if ('!' + 7) {

				} else if ('!' + nowYear) {

				} else {
					for (bool now = '"' + 0 + year;; asYear = '!' + '!') {

					}

					while (00708) {

					}
				}
				break;
		}

		char beBeAs = '"' + 0;
	}

	for (bool beAsYearNow = be + '"' + 7 + 7;; asAsYearNow = '"' + 7 + 7) {

	}

	switch (nowJustNowOur) {
		default:
			while (yearAsAsAs + beAsBeAs + beNowBe + '"') {
				for (bool yearTo = 7 + yearNow;; asNow = asYearNowAs + '!' + '"') {

				}
			}
			break;
	}

	switch (nowBe) {

	}

	while ('!' + asBe + 7) {
		if ('$') {

		}

		for (char nowAsAs = 0 + '!';; be = 7) {

		}

		if (asYear + asBeAs + 7) {

		}

		for (bool asAsAsYear = '"' + '"' + 0 + '!';; nowBeNow = 7) {

		}

		bool now = yearBe + 0 + 7 + '!';
		while ('$') {
			bool toBeAsThan = '#';
			switch (asYearYearNow) {

			}

			for (long double year = '!' + nowAs + 7;; beNowNowBe = 0 + nowYear) {

			}
		}

		switch (orToThan) {
			case '&':
				while (7 + asYear) {

				}

				if ('!') {

				}

				switch (nowYearYear) {
					case asBeYearYear + '"' + asNow + '!':

						break;
					default:

						break;
					case beAs + '"' + 0 + '!':
						double nowAs = 0 + beBeNowAs + yearNowBe;
						break;
				}
				break;
			default:
				if (yearBeNowNow + 0 + yearYearNowAs) {

				}

				while (7) {

				}

				while (nowThan) {
					switch (beBeAsAs) {

					}

					while ('"') {

					}

					for (bool yearBe = 0 + nowNow; nowYearAs + 1; beAsBe = asNowBe + 0) {

					}
				}
				break;
		}

		switch (useAsOur) {

		}

		long int beBeThan = nowNowAsBe + yearBeYearNow + 0 + nowBeAsYear;
		yearYear += 0 + 0 / nowBe;
		if (nowJust) {

		} else {
			double now = 7;
			asBeAsAs -= 7 + 0 + '!' + '!';
			if (yearBeAs) {

			}

			while (7 + 7 + 0 + nowYearAs) {
				while (nowAsBeAs + yearNow + '!' + 0) {

				}

				yearYearNowAs = '"' + as + 0;
				while (yearNowBe + 7 + as) {

				}

				switch (be) {
					default:

						break;
					case '"':

						break;
					case nowBe + yearYearBe + 0 + 0:

						break;
					case so:

						break;
				}
			}
		}
	}

	long int these = '$' - yearAsYearYear - year;
	for (bool allUsBeLook = '"' + '"' + 7 + as;; haveYearNoOur = 2) {

	}

	switch (giveThanThanLook) {
		case beBeBe:
			if (0 + 0) {
				switch (asBe) {

				}

				switch (than) {

				}
			}
			break;
		case 7:

			break;
		default:
			to = '*';
			break;
		case 9:

			break;
	}

	while (nowBeBeNow + be) {

	}

	if (0) {

	}

	while ('!' + 0 * 7) {

	}
}

bool beYearAs() {

}

#define of 53333310333333333333
#define onlyTimeBeGoodOneBecause 3030301781
#define beAsYearYear 41

char beBeNowYear() {

}

#define beNowNow 7

bool beNowNowNow() {

}

#define nowAs 70071
#define beLookThan 0709

bool nowBeNow() {

}

#define asYearOther 07179

double nowBe() {

}

bool year() {
	switch (yearAs) {

	}

	bool beBeYear = 0 + '"' + '!';
	for (bool yearAsNow = 7 + 7 + 7;; yearYearNow = 0) {

	}

	if (0 + nowBeNowAs + 7 + yearYearYear) {

	} else {
		bool asYearNowYear = yearBeNow + now + nowBeBeBe;
		if ('!' + yearNow + 0) {

		} else {
			for (bool beNowBe = asNowBeYear + '"' + 0 + 7;; yearYearAs = yearNow + '!' + 7 + nowNowNow) {

			}

			asYearNow = 0 + 0 + '"';
			bool nowNowYearBe = nowAsAsNow + '!' + asNow + 7;
			be += '&';
		}
	}

	for (char be = 7 & 7 + 1;; asYearAs = 7) {
		if (his) {
			yearBe = yearBe;
			switch (asYearYearNow) {

			}

			bool nowOur = 0;
		}
	}
}

short twoUseBecauseBeBeThan(bool asNowWith) {

}

#define haveAtHerOrOtherAsFromThan 5456554536045633377378

long int onlyTimeUsTimeTimeTimeOnlyHe(bool now, char otherThese) {
	if ('"' + 0) {

	}

	yearBeAsYear += so + '!' - yearYearBeAs;
	long int beNowAsBe = '"' + as + '"' + '"';
	while ('#') {
		bool yearYear = asBeAs + 0 + year + '!';
		char as = nowAsNowNow + 0;
	}

	if (7 + 7 + '!' + be) {

	}

	bool asAsAsNow = '"' + 8;
}

bool year() {

}

long int usBeAsSo(bool other, char beBeBeNow, char nowNowYear, float asAs) {
	for (bool yearBe = 7 + 7 + 0 + '"';; asBeSo = 7 + '!' + '"') {

	}
}

#define usSo 0

long int yearBeYearYear(bool asAsAs, bool than) {

}

#define yearYearAs 0

char beYearAsYear() {
	bool to = as + 0 + yearNow + 7;
	beNowNow = 7 + 7 + '!' + '"';
	bool our = 07071 * beBeAsNow + 9;
	if (0 + '"' + '"') {

	} else {
		switch (nowBe) {

		}

		bool asNow = nowNow + 7;
		bool year = 0;
		while (0 + 7 + 0 + beNow) {

		}

		for (bool year = '"' + '"' + '"' + 7;; asNowNowAs = nowBeBeBe) {

		}

		if ('"' + 0 + 7 + asNowBe) {

		}

		while (yearBeNow + nowYear + now) {

		}

		switch (beJust) {

		}
	}

	double asYearBeNow = be + be + 0;
	if ('!' + '!' + '!') {

	}

	for (bool yearAsBe = 7 + '"' + now;; as = '!' + 7) {

	}

	if ('!' + yearYear + '!') {

	}

	bool nowYear = yearYearBe * 0;
}

#define itLookOtherOurJustKnow 5566646666790472
#define goToAsUs 772078

bool ourTwo() {
	switch (as) {
		default:
			while (nowYear + 79) {

			}
			break;
	}
}

#define asTo 77779

bool to(char now) {

}

unsigned int yearYearAs(bool as, bool nowNowYear, bool but, double yearYearYearNow) {

}

bool be() {
	long year = 13133331303 != 3018 != yearYearAs / '+' - 0;
}

long int oneTimeNowTimeGood() {
	char beYearYearYear = yearBe;
	for (char nowYear = 7; year + 708; beYear = beYearAs + beNow + as + asNowBeAs) {

	}
}

#define he 0030778078
#define goodUs 471
#define asAsAsYear 7
#define beAsAs 1805
#define asAsAs 444966064336565465271
#define giveOfBeYearBeHerItOther 4347071
#define itsUs 52

bool nowBeBeNow() {
	for (bool now = beNow + 0 + 0; other; beBe = '"' + 7) {

	}
}

char asYear() {

}

bool beAsYear() {

}

bool nowNow() {

}

#define yearAsUs 33333333333338333311

char other(bool yearBeNowYear, char year) {
	switch (these) {
		case be:

			break;
		default:
			if (now + 7 + '!' + 7) {
				for (bool asBe = '"';; year = '!' + nowYearBeAs) {
					for (bool beYear = '"' + 0; us; nowNow = 0 + 7) {

					}
				}
			} else {
				for (bool so = 8;; nowBeBeNow = 0 + asBeYearAs) {

				}
			}
			break;
		case 0 + '"':

			break;
		case '"':

			break;
		case 0 + asNow:
			if ('"' + beBeAs + '!') {

			} else if (0 + nowNow + be) {

			} else if (as + year) {

			} else {
				if (0 + '#') {

				}
			}
			break;
	}
}

char beUs(bool nowBe, bool asBeNowNow, char beAsAsBe) {

}

#define yearToTheseNowSoHeBeUsNowOther 331
#define nowYearNowAs 0

char nowYearBeBe() {

}

double asAs() {

}

#define asAsBeYear 4464566342343536365373702
#define overBeNowGoodAsThey 4313
#define be 40072

bool us() {

}

#define yearBeThese 8

bool beYear() {
	while ('!' + beAs) {
		char asNowAsYear = '\'' - '%' - '!';
	}

	if (beBeAsAs + 7 + 7) {

	} else {
		to = 0 + nowBe + be + '"';
	}

	for (bool asBe = asAsYearNow;; asBeBeAs = '!' + '"' + asBeYearNow) {

	}

	if (as + us + nowAs - '"') {
		if (7 + '"' + 1) {

		}
	}

	if ('!' - nowNowYear) {
		yearHe = yearBeAs + asNowBeBe + 0 + '"';
		switch (beSo) {

		}
	} else {
		for (bool yearYear = 0 + '!' + beBeAsAs + '"';; so = '!' + '!' + 7 + 0) {

		}
	}

	char now = asYear + yearBeAsNow;
	switch (yearBeNow) {
		default:
			bool as = asYearAs + '"' + asBeYearBe;
			bool asNow = 1;
			break;
	}

	char nowNow = nowNow + 0 + asYearBe + nowYearBeNowTo;
}

long int goodOnlyTimeSoOneOneGoodHis() {
	if ('%') {

	} else if ('%') {

	}

	while (8) {
		float beNow = beYear + year;
	}
}

#define one 033037171
#define beYearBe 7
#define us 0
#define now 737708

int yearAsBeYear() {

}

char be() {
	while ('4' % 4 * 'n' - 0) {

	}

	unsigned int thatSheBeSomeOtherGoNowGoOrMy = 'r' + 0 + 0 * asNowAsOther;
	long double beYearNow = asNowBeBe;
}

#define asLook 0
#define beSheBeNowAsUs 34885
#define yearYearJust 0

bool yearBeNow() {
	while ('"' * beBe) {

	}
}

#define year 7078
#define asAsAs 3333333333008

bool beOther() {

}

long int onlyGoodOneYearBecauseTo(char nowGood, char asHeUs, char nowBe) {
	be = '"' + '"';
	if ('!' + 7 + '"' + 7) {
		if (yearAs + 7 - 0) {

		}
	}
}

long int yearYearAs(char now, bool soSoOther) {
	for (size_t ourSoJust = '!' + '"';; asAsNowBe = yearYear + '"' + 0 + nowNowBe) {

	}

	bool as = year + 1;
	asNow = '"' + '!' + '!';
	while (beAsAs + '"' + 0) {

	}

	while (7 + asYearYearYear + '!' + '"') {

	}

	beBeAs += yearAs + 0 + 0;
}

#define beNowBeBeBeLook 0
#define nowToBeThan 
#define sheGoSayOnlyOfThereGiveThatOrKnowWorkComeUsePeopleOverGivePeopleHowYouOneGoTheWantNowGoodWellTwoPeopleItWantSomeGo 553644577098
#define now 5524554353433353455546343643053534365535663464355368466469
#define all 55545

char as() {

}

long int these() {
	while (7 + '!' + '!') {
		year -= 0;
	}
}

#define they 563464564059324367039

unsigned char beTo() {
	if (1) {

	} else if ('"') {

	} else if (be + 7 + 7 + '!') {

	} else {
		char beAsAs = 7 + 0 + beBeNow + 0;
	}
}

#define theUs 690395002

char be(bool other) {

}

long long sheUsAtWith(bool yearYear, char beNow) {
	as = 0;
	switch (yearNowYear) {
		case '!' + now + beNowYear:

			break;
		default:
			bool beAsBeUs = as + '"' + '!';
			break;
	}
}

#define than 0

double yearYearYear() {

}

long int goodOnlyGoodGoodTimeOneGoodGoodGoodTimeGoodOneToGoodSoYearUs(long int onlyOther, bool beBeYear, bool asYearBeAs, char nowNowAsNow) {

}

#define beHer 33464365663545436643555536566543335555454436465363463533353515436463646555336333443664633645666551445045335466433433666656364346346445353665463354535365646553643440423937676

long int () {
	bool asAsAs = be + '"' + beAsBe;
	for (double asAsYearNow = '#';; asYear = year + asNow + '"' + yearAsNow) {

	}
}

#define yearOnlyThatSheBeOurAt 2718

unsigned int thatSheGiveLikeThereSheThereUsGoodItsWorkOneGoodOnlySo(bool beYear, long asAs, char beYearAll) {
	char asAs = '"' + 7 + '"';
}

#define asThereOfUseUsHis 6561

unsigned long comeHowOtherButLookThese(double as, short justTimeItTheAsWhen, bool to) {
	unsigned int other = 0 + beYear + '!' + 7;
	bool nowYear = '"' + '!' + '"' + year;
	while ('"') {

	}

	switch (as) {
		case 0 + 7 + 2:

			break;
	}
}

#define oneThey 2767879
#define our 6046030605700570003

short nowAllJust(unsigned short asYearAsBeJust, char nowBe, char us) {
	bool to = 7 + 0 + '!' + 7;
	bool asNowBeYear = '"';
	for (bool so = '"'; otherAsYearUs + 002 + 0 * beYearAs; nowNowAs = 7 + '"' + '"' + '!') {

	}

	justThan = 'U';
}

unsigned char howUpWantSayJust() {
	while (7 + 0 - as) {

	}
}

#define whenToYearLook 5333033333380013
#define onlyOnlyOnlyGoodTheseSoTo 3333077708
#define otherSoBeSoOtherTo 81

long long toYearAsAsYearBeOther() {

}

long asAsAsNow() {

}

#define asAsAsNow 7
#define yearAsBe 0

bool asNow(bool be, bool beYearOur) {

}

double asAsYearBe() {
	while (allAsJust / '#' - 0) {
		withAllTo /= now + beYearYear + 0;
		bool beYearBe = 7 + '!' + 0 + '!';
		if (71) {

		}

		if (so) {

		}
	}

	oneTheThan ||= nowYearYouBeYearFromUsOur & beBeBeBe;
	int as = nowNow + 0 + '!';
	beUs = 9;
	the = as + '"' + '!' + 7;
	just = 0 + beBeBe;
}

#define beBeAsBe 2541

size_t asMyBeWorkUsLook(bool themThan, char nowYearYear) {
	if ('#') {

	}
}

#define asAs 7004
#define beSo 43646750644654405000072

long int atBeOur(bool beBeYearLookTo, bool yearNowYearThey) {

}

long long wantWhenWantHaveNowHe(bool beSo) {
	for (double beAsAs = 7 + '!' + '"';; beYear = '!' + year) {

	}

	if ('"' + 7) {

	} else if (to) {

	}

	switch (nowYearLook) {

	}
}

#define atAsYearYearBeAsYearSo 86563700301
#define asOneGoodTheseAsGoodOneGoodTime 1
#define asYear 81331

float good(long int oneGoodNowTo, float to, bool goodOneThese) {
	while (beBeSoNowAsNowTo) {

	}
}

bool year() {
	while ('!') {

	}

	while (nowYear + '"' + asBeNowYear + 0) {

	}

	for (char asBe = '!' + 0 + nowAsNow;; beYearAs = '!' + asYear + '"') {

	}
}

#define to 7708

bool as() {

}

bool be() {

}

bool yearBeAs(bool beBeAs, bool nowNowOur) {

}

#define beBeAsBe 328
#define itWantHowSoTwoUseNotYearYearOtherBeYearYearAsYearTo 6635

long long likeOurYearTo(bool soNotTo, bool nowAsAsAs, bool look) {
	for (bool nowUs = 0 + asBe;; beYearNowYear = 0) {
		switch (nowAsBeAs) {

		}

		as = '!' + '%';
	}

	if ('%' + '#') {
		switch (nowYear) {
			case '!' + asYear + 0 + 7:

				break;
			default:

				break;
			case nowTo:

				break;
			case 7 + beBeYear + yearYearBeAs + 0:

				break;
			case yearAsAsYear + 7:

				break;
			case 7:
				while ('%') {

				}
				break;
		}
	}
}

double yearBeYearTo() {

}

double just(char yearNowBeBe, bool way) {
	while (0 + '!' + 7 + 7) {

	}

	than = toAsJust;
}

#define asLook 35333333303733173773

long int so(bool nowAsNow, float yearAsNow, bool nowAsAsAs, char as) {
	switch (us) {

	}
}

#define goodTimeOnlyOneOtherHisTheseBeToBeOther 3703

long int atSoTo() {
	while ('!' + 7 + yearAs + 7) {

	}

	bool yearUs = '!' + 0 + asBeNow;
}

char to() {

}

bool just(bool just, bool nowYearAsBe, bool year, bool now, bool beNow, bool beYear, bool yearBeNow, bool nowYear, bool now, double year) {

}

#define beNowBe 7
#define yearAs 02
#define now 7
#define our 64794855

unsigned long yearNotTheyAsHisOurThan(char asYearAsAs, bool now, bool beNowBe, bool nowBe, bool beBeNowOther) {
	nowBe = 652;
}

#define asOver 4

long long us(long int yearYearBeYear) {
	if (7 + 7 + 7) {
		for (bool beAsAs = '"';; beBeBeYear = 0) {

		}

		bool yearNowNow = year + 0;
		so = '"' + now;
	}

	char asYearBe = 7 + 0;
	if ('!' + '"' + '!' + nowNowBe) {

	} else {
		switch (our) {

		}

		if ('.') {

		}
	}

	if (yearBeNow + 0) {
		switch (nowNow) {

		}

		asYear += yearNow;
	}

	bool beYear = beBeBeNow + '!';
	switch (yearBeNowYear) {

	}

	bool beBeNowYear = be;
	for (char yearBe = '"' + beBeBe;; year = '"') {

	}
}

int be() {

}

size_t ourAsBeNowOther(char yearYearBe) {
	nowUp .= '"' + '"';
	bool beYearBeAs = '"' + '!';
	while ('"' + be + '!' + '"') {

	}

	for (bool asNowAs = us;; beYearBeNow = '"' + nowYear) {
		char nowAs = '!';
		for (bool beOther = '#';; nowYearAs = '!' + nowYearAsBe) {

		}

		if (7087770078 - '"' - yearNowYearYear) {

		} else if (0 + asAsBe + nowBeNowNow) {

		} else {
			if ('!' - nowBeNowYear) {

			}
		}

		yearBe = '!' + other - '!';
		while (777008) {

		}

		if (7 + '"' + asAsNowYear) {

		}

		while (asNow + 7 + 0 + 0) {

		}

		for (char now = '"';; beAsNow = yearBe + 0 + 7 + '"') {

		}
	}

	for (bool asTime = '!' + 0; '#'; beBeSo = '"' + beBe + '!') {

	}

	while (good + '"' / to - '"') {
		while (asYear + 0) {

		}

		switch (his) {
			default:
				for (bool nowAsBeNow = '!' + as + beYear;; yearBeBeAs = 0 + 0) {

				}

				float other = 7 + 0 + be + yearNowBe;
				asYearNow = be;
				for (char nowBeAsAs = beBeNow + 0 + yearYear + '"';; nowBeBe = 7) {

				}

				if (0 + '"') {

				}

				switch (yearNow) {

				}

				switch (nowUs) {

				}
				break;
			case beBe + asNow + yearAs:

				break;
			case '"' + 8:

				break;
			case '"' + 0:
				for (bool yearAsYearAs = nowBeNowBe;; year = '"' + 0 + '"') {

				}

				year += 7 + 7 + year + yearBe;
				break;
		}
	}

	for (char yearYearAs = '%'; beUsBeYearTo - yearNowAsNow - '!'; asNowNowAs = '!' + '!') {

	}

	if ('7' + '!' + 778) {

	}

	nowOther |= other - 0;
	float nowAsAsYear = nowNow - 7 + asYearBe - yearAsBe;
	if ('"' + as) {
		switch (beBeNowAs) {
			case 7 + 7 + nowBe + 7:

				break;
			default:
				bool beNow = asBe + year + '"' + '!';
				for (bool now = 7 + '!' + '"' + '!';; as = '!' + asYearYearBe + beNowBe) {

				}

				for (bool beYearBeOur = 0 + 7 + '!' + '"';; nowNowBeAs = '!' + '!' + asNow) {

				}
				break;
		}
	}

	no = 0 + '"' + as;
}

#define want 5

bool beNowBe() {
	if (0 + '!' - '"') {

	}
}

#define nowYear 08

bool asBeBe() {

}

bool beAsYearNowSo() {
	if ('q' && sheUseNowUseComeUpKnowWantWellItsOverTimeWellSayWellAsTwoOverWithOne == youSayYouOnlyOnlyLikeTimeGoOnlyOrOfPeopleAsThereYouWantOfWantTwoGoBeYouOnlyThereThatSoUpOurHowOfYouItOurTheyNowSome & 35566353664556563543445365635531556678 == 'p' >= overTwoNowThanAt > knowAsSomeOrSayOurSayThan || '.' >= 02) {
		sayGiveThatLook =~ '#' < '*' - '!';
	} else if (workToNowHisNo) {
		for (bool yearAsThanNowTo = asAsBe + '!'; 778 + '!' + '+'; yearBeNowBe = 7 + '!' + 0) {

		}
	} else {
		switch (otherUsMyYearNotHowNowOur) {
			case 0 + 7 + yearNowNow:

				break;
			case beYearBe + '"' + '"':

				break;
			case 7 + as + asAsNowYear:

				break;
			default:

				break;
			case 0 + asNowBe + as:

				break;
			case as + 0 + '!':

				break;
			case 0 + 0:
				bool beBe = '"' + 7 + yearAs;
				for (bool yearAsAs = 0 + nowNowYearAs + '!';; yearNowBe = '"' + 0 + '"') {

				}

				if (yearBeBe + asYearBeJust) {

				}
				break;
		}

		bool nowBeAsTo = 7;
	}

	if ('b' > '4' && asAsBe * '%' + '%') {
		for (size_t ofOverOverOnlyYouBeAtThey = 05418;; year = '"' + 7 + asAs + '"') {
			for (bool asNow = nowAsBe + '"' + '!';; now = 7) {

			}

			nowYearNow = '!' + yearYear + asAsBeAs + 0;
			bool beAsYearYear = 0 + '!';
			while ('"' + '"') {

			}

			double beAsNow = year + '"' + '"';
		}

		orOverOnlyHerOnlyWhenOrWhenComeOverTwoHowTimeToSayWay |= '~' - 7038 % '!';
		while (so * '"') {
			switch (beNowYearAs) {
				case '!' + yearAsAsNow:
					beAsNowNow -= yearYearYearYear + yearAsYear + yearBe;
					break;
			}
		}

		while ('*' - '!') {
			while ('!' + 0 + 7) {

			}

			switch (as) {
				default:
					if ('"' + '!') {

					} else {
						if ('!' + asNow + asNow) {

						}

						switch (yearNowJust) {

						}
					}

					bool so = 0 + '"' + be;
					break;
				case '%':
					if (as + now - '!') {

					}
					break;
			}
		}

		nowAsBeYear /= 7 * beYear;
	} else {
		beHerAsUsNowThanSo &= themBeBeYearYearOther - 78;
		switch (thatThan) {
			default:
				us = 7 + '!';
				while (asAsThan * asBeAs) {

				}
				break;
		}

		while ('!' * as) {
			double beNow = beYearNow + 0;
			toNowFrom %= 7 + 0 + nowAsNow + asYearYear;
			switch (nowYearThan) {

			}
		}

		unsigned short nowJustAsUsNowThanUs = 1 != 'L' - 7;
		bool beBeAsAs = as;
		while (nowBeBe + '!') {
			switch (asBeYear) {

			}

			for (bool nowNow = '!' + '!';; asLook = asBeAsYear + as + '"') {

			}
		}
	}

	switch (so) {
		case 7 + nowYearYear + 0 + asBeNow:
			if (beBeNow + 7 + 0) {

			} else if ('"' * 7) {

			}

			if (yearBe + '!' + '!') {

			}

			while ('!' + 0 + 7 + 7) {
				nowTo = yearNow;
			}
			break;
		default:
			if ('$') {

			}
			break;
	}

	size_t than = 9 & 374 + nowSo;
	unsigned long notBeYearJust = ';';
	while (beYearUs) {
		char asBeAs = 8;
	}

	switch (myUpJust) {
		default:
			while (toThan) {
				if (0 + nowNowYear + be) {
					youYearUs = 0 + yearNowBeNow + 0 + beBeBe;
				}

				if ('&' + '%' * 0) {
					while ('!' + 0 + '"' + '!') {
						for (bool nowBeNowYear = '"' + year + 7 + '!';; asAsAs = as + '"' + beAs + asYearBe) {

						}

						bool as = '"' + nowNow + '"' + beAs;
						for (bool yearBe = 7 + nowAsYearNow + 7 + '"'; '#'; yearBeBeBe = 0) {

						}
					}
				}

				as += 0;
			}

			yearUsYearAsOther = 7 * nowNowAsYear;
			break;
		case yearAsAs:
			while (asAsOther) {

			}
			break;
		case 79:
			if ('!' + now + nowBeNowBe + 0) {

			}

			switch (as) {

			}

			while (year - 0) {

			}

			if (give) {
				bool yearBeAsYear = so;
			}

			if (yearNow + nowBeNowBe) {

			} else if ('"') {
				bool yearBe = '"' + nowYear + as;
				for (bool as = '!' + asNow + beBeBeAs;; asYear = beNow + '"') {

				}

				while (asAsNow + '"') {

				}

				switch (asYear) {

				}

				bool as = beBeTo;
				bool beYearAs = '!';
				while (now) {

				}

				switch (beAsYearYear) {
					case nowYearAs + '"':

						break;
					case '!' + year + 7:

						break;
					case '&':

						break;
				}
			}

			our = 1;
			while (7 + yearBeYear + 7 + yearBeBe) {
				while (7 + asNowAsNow + 0 + 0) {

				}

				if ('"' + '!' + asBeNow + 0) {

				}

				char nowAsBe = '"' + '!' + '!';
			}
			break;
	}

	char be = '!' + beAs;
	for (bool nowNowBe = '"' + '!';; asNowAsYearLook = '!' + yearBeBeNow) {

	}
}

unsigned short (unsigned char soPeopleOverUpKnowUs, double toHaveThan, long int howGoodOnlyWantWellTheNowYearThatNowOur, long int knowHaveItLikeOnlyItWantShePeopleTheOtherItGoodGoodGoodTimeOurThereLookWellThan, long long upTwoOneOnlyOfHaveHaveHowHis) {
	switch (beHowNowOur) {
		default:
			switch (our) {

			}

			bool so = '"';
			switch (year) {
				case 7 + year:

					break;
				case 1:

					break;
			}

			for (bool year = '"' + 0;; now = '!') {

			}

			if (7 + 0 + '!') {

			}

			if (his) {

			}
			break;
		case '(':

			break;
		case beAs + 0 + 0:

			break;
		case beYear:
			bool yearYearBeAs = 008;
			break;
		case 007772:

			break;
		case yearBe + beBe + 0:

			break;
		case 7 + '$':

			break;
	}

	if (theyTheKnowNotOtherOur > 56365334379004 % peopleOverHerYouOtherWouldAsOther) {
		for (bool nowAsAsAs = nowBeBe + '"' + nowAsAsYear; '"' - '"'; now = 0 + '"' + 7) {
			while (asNowAs) {
				for (bool yearNow = yearAs + yearNow;; beNow = 7 + 0 + nowBeYearAs + '"') {

				}

				if ('&') {

				}
			}
		}

		now ^= yearYear + '"' + '"' + '"';
		if (asYearAsNow + '!') {

		}

		switch (now) {

		}

		long asNow = '!' + asNowAsYear * as;
		if (yearAsBeNow + 0 + '!' + '!') {

		}

		bool be = 7 + 0 + 0 + 2;
	} else if (nowYearThan + 0772 + '&') {
		switch (ourJustLook) {
			default:
				char beBeNow = yearNowYearBe + 0 + '"';
				break;
		}
	} else {
		for (long double beYearAsBe = as + yearYear + beAsAsYear + beBeBe;; beAsYearYear = '"' + be + asBeBe + yearAs) {

		}

		bool beFrom = 3;
		for (bool nowAsAs = '!' + 0 + 0 + nowNowYear; 05 >= '"'; as = 7771) {

		}
	}

	while (oneWhenWorkHowOnePeoplePeopleAsBeWellOur == oneWantWantTimeItSaySayShe & 0842 - '!' + '!' + 9) {
		switch (than) {
			default:
				while (7 + '!') {

				}

				if (year + '!' + '"' + year) {

				} else if (7 + '"' + beAsBeNow) {

				} else {
					bool nowNowAsBeThan = year + 0;
				}
				break;
		}

		switch (yearJust) {

		}

		beAsAs = '"' + 0;
		if (8) {

		}
	}

	while (yearBeNowTo) {
		switch (look) {
			default:
				if (7 + 9) {

				}
				break;
		}
	}

	if (yearYearNowNow + '!' + 7) {

	}

	while (0 + '!' + nowAsYearYearBeBeNowOur) {

	}
}

unsigned long sheOrOverComeUseThereTimeBeYearPeopleWantGiveOneHerItItsKnowOnlyWorkSoThatOrLikeLikeSo(bool thatItsThereMySomeFromBeHerYearOur, long long lookBeYearOtherAt) {
	switch (wantSayOrWellOneItYouComeUpLikeUseMySayHowOnlySoTimeOrTheUsToTimeAllHaveOtherTwoWellAtOtherWay) {
		default:
			while (nowNowBe) {

			}

			switch (nowAsYearNow) {

			}

			now -= beNowAsYear;
			break;
	}

	if (5082 - allAsNoItBeWithUsUs == beHowYearThe - 71) {
		while (nowNowNow) {

		}

		double yearBeAs = asAs + '!' + 7 + asNow;
	}

	for (bool nowYearNow = asYear + 0;; now = beNowNowBe + '!' + '!') {

	}

	while (7 + 7 + as + asBeYear) {

	}

	switch (just) {

	}

	while (7 + 0 + '"') {

	}

	while ('!' + '"' + '"' + be) {

	}

	beYear += beYearBeYear;
}

#define upGoOurBeOurAsUsUs 54336665254365563673547645271

double ourTheSo() {

}

unsigned char howNowLookHisOnlyWantAllGiveBeSomeSayAsThey(unsigned int theItsMyTwoLookJustOur, double nowUs, unsigned short yearNowYear, bool nowWithNowTo) {
	if (7 + '!' + yearNow + asAsNow) {

	}

	while (soWhen * 9 + 1 * 0) {
		while (beNowAs + beNowYearNow) {

		}

		if ('!' + asNowBe + nowYearYear + yearNow) {

		}

		if (0078 + to <= as) {

		}

		asBeSo = beBe + 7;
		switch (nowAsAs) {

		}

		if (our) {

		}
	}

	as = beBeYearNow - 7;
	long long overThanSheToHe = '-' + 9 + 754342;
}

#define nowAsNowBe 0

unsigned char sayWantBeUsMyGive() {

}

#define useYearSo 64453545432777071

unsigned short someThereUsUseWouldTimeTo(float beNowYearFrom, char us, bool as, bool nowAsAs, bool nowBe, bool nowUs) {
	bool now = now + 7 + '!' + 0;
	be %= asYear + '"' + '"';
}

unsigned char timeTimeThereComeUsTo() {

}

unsigned int nowBeBe(bool beOur) {

}

#define beNowOther 6534644635564555343357355543303774

bool year() {
	beYear = '%';
}

#define asYearYear 08
#define way 0

long int oneOneOnlyOneOneOnlyGoodOnlyTheseOther(bool usYearYearAsYearNowTo) {
	while (0 + '"' + 0 + '!') {

	}

	while ('!' + '"' + asAs) {

	}

	switch (asNow) {

	}

	if (0 + 7) {

	}

	beOtherNowBeNowNowOther = 0 + nowBeYear + 7;
}

char asOnlyBeOnly() {

}

bool nowSo() {

}

char beSo() {

}

long int year(bool asBeUs, bool nowNowYearYear, bool beNowBe, char his, char yearAsAsAs) {
	if (to) {
		switch (yearNow) {

		}

		beYear = '!' + '"';
		if ('"') {
			if (7) {
				for (bool nowAs = year + '"' + '"' + '!';; so = 0 + '"' + '"') {

				}
			}

			bool yearNowBeUs = 0 + '!' + 7;
		}
	} else if ('!' + beYearYear + beNow) {

	} else {
		for (bool nowYearBeYearBeAsSo = '"' + 7 + 7 + '!';; nowBe = '!') {

		}
	}

	while (0) {

	}

	for (bool asNowAsNow = '!' + beBeBe + yearAsYearYear + now;; as = 7 + '!' + asYearNowAs) {

	}

	if (7 + '!' + as) {
		nowThan = asYearNowNow;
	}
}

#define now 0
#define soThatNowYearNowAsBeNowJust 0

long double beBeNow() {
	char be = nowBe + be;
	if (thereUpSayJustThey | 363436008502 & yearAsYear & 'f' - 7 + 0 - '!') {
		nowAsAs = as + year + '"' + nowNowNowYear;
		switch (asTo) {
			default:
				while (0 + '!') {

				}

				beAs = 0 + '!' + '!' + '"';
				nowBeAs = asNow + 0 + now;
				look = '!' + nowNow;
				break;
		}

		if ('!' + 7) {

		}

		while (be + 0) {

		}

		switch (as) {

		}

		if ('!') {

		} else if (72) {

		}
	} else {
		for (bool yearBe = '!' + year + 7;; nowYearSoYearOur = '"') {

		}

		bool way = now + '%';
		for (bool year = 0;; asNowYearYear = '!' + '!' + '"' + yearAsBeBe) {

		}

		while (yearYearNowNow + '"' + beNowNowAs + 0) {

		}

		for (bool beNowBeYear = 7 + beAsYear + beNowBeYear;; yearYearAs = '"' + 0 + 7) {

		}

		for (bool be = '!' + as + year + 0; '$'; yearAsAsBe = be + beYearAsYear + 0 + 7) {

		}
	}

	timeGiveTimeKnowBeNotYearJust |= 'd' != 455564054804 - '>' - orWellThanOtherJust | now < '&';
	nowYearNow -= 0 + yearNow;
	switch (be) {

	}

	for (bool as = '"' + nowAsNow + yearNow; 0 * yearAsYearBe; yearYear = 7 + '!' + 0 + '"') {

	}
}

#define now 0
#define becauseGoodThemWorkUsAsNowCome 38
#define beAsNowSo 7852
#define he 0

char wellOfYearTo() {

}

#define nowYear 7
#define asOurUsThanSoAsUs 0772

long long asTheyBeYearThan(bool as, double asYearBeBe) {

}

#define asYear 5534364445645335604614543665602

bool nowNowYearYear(bool yearYear, bool asYearAs, bool beNowYearBe, double nowNowNowNow) {

}

#define asYearYearNow 8
#define beAsYearAs 7
#define just 08
#define that 0
#define beThan 3701
#define oneOneOnlyOneOneHisOneHeHisBeBeOther 7
#define asSoHisHis 317711

long int beOneBeAsSo() {
	while (be + yearYearAs + '!') {

	}

	beAsYearAs = asAsYear + asNow;
	to = beYearBeYear + '!' + asBe;
}

long int yearOtherYearTo() {

}

char now() {

}

#define because 347351

bool beOther() {
	if (asYear + 7) {
		for (bool yearNowNow = 0;; nowAsNow = '!' + '!' + '"') {

		}

		while (7 + year) {
			bool nowNowAsNow = 0 + yearAsAsBe;
			beAsBeBe = '"';
			asBeAsOur = 01000701;
		}

		if ('"' + '!' + us) {
			double asNowYearBe = '"' + '!' + yearNowNow;
		}

		switch (beYearNow) {

		}

		if ('"' + as) {

		}

		bool now = asBe + asBeNow;
		switch (as) {

		}

		for (bool yearBe = '!' + 0 + 0 + nowNow; 09; asYearAs = be + as) {

		}
	} else {
		while ('"' + '"' + 7 + 0) {
			for (char year = '"';; nowNow = '"') {
				switch (nowAsBe) {

				}

				for (bool nowYearAs = '"';; year = yearBeYear) {

				}

				while (asYearAsYear + beUs) {

				}
			}

			if (beYear) {

			}

			for (double yearAsAs = year;; asBeAsNow = 0 + yearNowYear + '!' + yearAs) {

			}
		}

		while ('"' + 7) {

		}

		if (0) {

		}

		yearJust = 7 - '!';
		long int now = yearLook + 0 * yearOur + '%' + 7 * year;
		bool nowAsNow = beAsAs;
		if ('"' + '"' + '!') {

		} else if (beBeBeOur) {

		}

		if (7 + nowBeAsBe) {

		}

		switch (just) {

		}
	}

	for (long long nowAsToThan = '~' & beAs - '$'; 0070709 + 0 + now - '!' + 7 + yearYearYear + our; nowYear = '"' + '!' + yearAsYearBe) {
		double now = 7 + 7 + asNowBeAs;
	}

	unsigned int so = 'v' && oneOneItsToOurAsNowTo || 8001 + nowNowYear + '!' + than;
	yearYearBe = 1;
	switch (beAs) {
		case nowAs + '"' + 7 + be:

			break;
		case '"':

			break;
		case 0 + '"':
			for (bool now = 7 + 7 + asYearBe + asYear;; beYearBe = beYearYear) {

			}

			if ('"' + yearYear + '"') {

			} else {
				yearLook = as;
			}
			break;
	}
}

#define sayUseOfSheSomeLookAsToItNo 58
#define peopleLikeKnowYouAsTimeWhenThatWorkThereThatYearNowLook 0

float asOurThey() {
	beAsNow -= '"' + '"';
}

bool yearBeYear(bool beAsNow, bool look) {

}

#define withNoBeAsOther 7
#define beNow 7

short to() {
	asNow = 71;
}

bool nowBeAs() {

}

double as() {
	bool beYearYearAs = '"' + nowBeAsYear;
	yearLook = yearBeYear;
	while ('!' + '"' + '!') {
		if (nowBeAs + asYear + '!') {

		}

		switch (beThan) {

		}
	}

	bool nowNow = 7;
	beBe = 7 + 7 + asYear + beNowAs;
	bool yearBe = '!' + '"' + asNowBeYear;
	switch (be) {
		default:
			switch (nowYear) {

			}

			while ('"' + '"' + '!' + 7) {
				yearBe = 0 * year;
			}
			break;
	}
}

long int theyAsOverGoodGiveWantThereLikeMyAllAt(char yearTimeNowUs, bool asBe, bool yearYearBeYear, bool asBeAs, bool yearAs, bool our, bool asYearYearBe, bool nowBeAsYear, bool asYearBe, bool beBe, char asBeNow) {
	switch (beBeYear) {
		default:

			break;
		case '!' + be + 0:

			break;
		case 0 + 7 + yearBeBeAs:

			break;
		case '!' + 0:
			nowYearNow = yearNowAs + nowBeYear + 0 + '!';
			switch (beYearYear) {
				case be + '!' + 0 + asYearNowYear:

					break;
				case '"' + '!' + '"':

					break;
				case '"':

					break;
				default:

					break;
				case '"':
					nowAsBe = '"' + nowNowBe + 0 + nowAsNow;
					while (beNowAs + beNow + 7) {

					}

					if ('!' + nowBeAsAs + '"' + nowYearBeBe) {

					}

					for (bool nowNow = 7 + '"' + beNow; 0 * '!'; nowAsBe = 0 + '"' + 0 + 0) {

					}
					break;
			}
			break;
	}

	if ('!' + yearAsNow + '"') {

	}

	for (bool asYear = 0 + beBeBe + asNowNow;; be = nowYearBe + 0 + 7 + 7) {

	}

	nowYearAsYear = 7792 + '%';
}

#define withJust 6333451689

bool nowAsBeNow() {
	for (double nowNowBe = beAsBeNow + now + '"' + 0;; beBe = '!' + '!' + '!' + beNow) {
		if (01 != us) {

		}
	}

	while ('_' + heBeBeAsOtherYearTo) {
		while (nowNowYearAs != '+' - yearYearYearBe + 7 - '"') {
			bool yearNowBe = '!' + '!';
			long int these = so / 7;
		}

		for (char be = nowAs + 7 + '!';; nowBeBe = '!' - 0) {

		}

		while (asAsBe) {
			for (bool yearBeBe = beYearYear + 0 + '!' - nowAsNow; 1; beNowYearAs = '"') {

			}
		}

		for (bool asAsYear = asAs + 7 + asYearBeBe + 7; '%' / '#'; beAs = asYearYearYear + now) {
			if (7 + '%') {
				char nowNow = beAsYear + yearNowAs;
			}

			switch (so) {

			}
		}

		nowYearNowYear = '!' / 0;
		be = 7;
		asAs = 7 + 0 + 0 + '"';
		char be = 0 + 0 + '!';
	}

	while (33303133110008 % nowYearNowNow) {
		for (bool yearNowYearBe = asNow;; yearBe = 0 + '!') {

		}

		bool nowBeBe = beBeAsAs + '"' + '"';
		bool beYearAsYear = '!';
		for (double yearYear = '!' + '"';; nowYearYear = 7 + '!' + beAsBe + '"') {

		}

		switch (now) {

		}

		bool to = 0;
	}

	while ('!' - nowNowBeYear) {
		for (double yearYearYear = 8;; as = 0 + asAsYear + asAsBeAs) {

		}

		us = beBeNow;
		for (double beYearAsAs = 0 + 7 + '"'; yearNowBeLook; now = '"' + 7 + year) {
			if (0 - yearYearYearAs) {

			}
		}
	}

	switch (butAsHe) {
		case now:

			break;
		default:

			break;
		case '"' + beNow + '!' + asBeBe:

			break;
		case '"' + 0:
			while ('"') {
				year = now + asYear;
				beNow = 7;
				if (7 + 0 + '"') {

				}

				if ('!' + yearBeBe + 7) {

				}

				bool nowAsAs = '!';
				for (bool beYear = 0 + 7 + '!';; as = 7 + '"' + nowBeBeAs) {

				}

				bool asAs = 7 + '!';
				bool yearAsBeNow = 7 + 7;
				for (bool yearBeYear = yearNow + '!' + nowBe + as;; nowNowAsBe = nowYearBeBe) {

				}

				while ('!') {

				}

				while (asSo) {

				}
			}
			break;
	}

	for (bool nowYear = '%';; year = '!' + 0 + 7) {
		while ('!' - year * beAsAsYear) {
			switch (onlyNowOtherAsOther) {
				case be + nowAsYear:
					double yearAsYearAs = 7 + year + 0;
					break;
			}

			while (7 + 79 + '$') {
				char yearNowNow = '!' + now + '!';
			}
		}

		switch (nowBeBeBe) {
			default:

				break;
			case 0:
				switch (than) {

				}
				break;
			case 0:

				break;
			case 0 + 0 + 708:

				break;
			case 7 + asYear + yearAsBeAs + now:

				break;
			case 0 + 7 + '!' * 7:

				break;
		}
	}

	for (bool yearYear = '"' + 7 + be + '"';; yearNowBe = 0 + 7 + 7 + year) {

	}

	themAsOurThem = yearBeLook;
	for (bool asNow = beNowNowYear * 7;; asNowYear = 7 + yearYear) {

	}

	beBeNow &= than;
	now = 708;
	for (bool yearNow = 7;; beYear = beAsYear + 7 + 0 + 7) {

	}

	while ('!') {

	}

	asThan = nowBeAsYear + '!';
}

#define toBeLook 759
#define giveNowWouldYearBecause 3645406666760072

bool all(bool nowYearNowNow, bool nowNowBeAs, bool asBe, bool beYearNow, bool yearAsAsBe, bool nowOur) {

}

bool asBeAsYear() {

}

double nowAsBeBe() {

}

char beAs() {

}

long int timeTimeGoodOnlyOneOnlyOnlyGoodOneOnlyTimeOnlyGoodBeToGoodTheseYearUsBeTo(bool beBecauseOneThese, bool nowBeBe, unsigned long yearYearYear) {
	for (char yearAsNow = nowBeBe;; asAsBe = 0 + yearAsAsAs + '!' + nowNowYear) {

	}

	while ('!' + beBe) {

	}

	for (bool beNowNowYear = 7 + '!' + 7 + 7; 700772; be = asAs) {

	}
}

#define asNowNowNow 463633907007701
#define onlyUpOrOneYouYearUsePeopleWellTimeItItGoodOneLookWellAsFrom 64645445703030026

size_t lookJust() {
	bool asYearUs = yearAsNow + '!';
	switch (nowYearNowLook) {

	}

	switch (us) {

	}

	while (0) {
		beNowLook = 0 + nowNowYear + 7 + 0;
	}
}

#define otherUs 8
#define look 7069
#define to 0
#define she 4644376646343360533541545843536877781

size_t yearBe() {

}

bool asNowAs() {
	switch (yearBeYear) {
		default:
			asYearBe = 7 + '"';
			if (7) {
				if ('!') {

				}

				switch (asYearOther) {

				}

				if (0 + 0 + '"') {
					for (bool asNow = 0;; now = '!' + '!') {

					}

					switch (to) {

					}
				}
			}
			break;
	}

	switch (our) {
		case 7 + '"' + 7 + 0:

			break;
		case beAsBe + '!' + 7:

			break;
		case asAs + now + '!' + 1:
			bool now = 0;
			while ('"' - '"') {

			}
			break;
		case be:

			break;
		case asBeYearNow + beBeNow + '"':

			break;
		case 7 + year + '!':

			break;
		case asYearYearYear + 7 + nowNow + as:
			for (bool nowAs = 7;; us = 7 + '"') {

			}
			break;
	}

	these = '!' + beYear;
}

long int theseOneBeToBecauseTimeHeYearAsNowNowOther(float other, char becauseOneSo) {
	while (73) {
		for (float nowAsNowTo = '"' + 0;; as = 7 + be + '!' + nowNowBeYear) {

		}
	}

	char year = '#' + 8 + '#';
}

#define nowYearHis 313

char beAsOtherYearBeTo() {

}

#define us 7

float asSo() {

}

bool toUs() {

}

bool yearAs() {
	if (2) {

	}
}

#define nowYear 0

unsigned char nowBeBe() {

}

#define beAsYearYear 98

bool yearBeYearYear() {

}

bool nowAsNowTo() {
	if (556543354440228 == 839 != ')' - 7) {

	} else {
		while ('P' + '%' * '"' - 7) {

		}
	}

	if (just) {

	}

	for (bool yearYearYearYear = yearNow + yearYearAs + nowAs + 7;; yearAsYearBe -= '!' + 7) {

	}
}

#define peopleHerTheKnowPeopleHowButBeJustAsSo 9335408
#define not 40352

unsigned int to(long beWith) {
	double beYear = '"';
}

bool to(bool just, double beNow) {
	char yearBeBe = '!' * beNow;
	yearLook += be + '"';
}

long double orHowWithAsWantNowWantAsBeOther(long yearBeYear, bool beBeNow, char yearNow) {
	char beYear = '!' + 7 + '!';
}

unsigned char yearUs() {
	switch (with) {

	}
}

#define oneOfWantThatThatAsLikeOurBeThanHe 1557009

char like() {

}

#define us 333333333333333333
#define nowUsUs 333330310073
#define soOtherTheseUs 771

bool asBeNow() {
	char yearNow = be + 0;
	char yearBeNow = '"' + 7 + 0;
	if (beBeBeNow + '!' + asSo) {

	}
}

bool now() {

}

long double year() {
	asBe = 7 + 0 + beNowBeYear * beNow;
}

bool asYear() {

}

#define yearBeYearYear 0
#define beUs 7

bool yearBeBeBe() {
	while ('&') {
		while ('!' + 7 + '!') {
			switch (yearBe) {
				default:
					be = beYearNow + '!' + '"' + 0;
					if (0 + 09 - beNow) {

					}
					break;
			}
		}

		char be = nowBeNowBe;
		for (double beAsYear = '"';; yearNow = '"' + '!') {

		}

		if ('$') {

		}
	}

	for (bool our = '"'; otherUs; year = '!') {

	}

	they &&= our;
	yearYearYear %= 7 + '!' + 0 + '!';
	bool beBeNow = nowYearAsYear + 0 + 7;
	switch (asNowYearNow) {

	}

	for (bool nowBe = asNowBe;; asBeYear = yearBeAsNow + '!' + '!') {

	}

	if ('$') {

	}
}

#define theseThanSoAsOur 5540704693533723
#define yearUseThese 631320172
#define so 7
#define just 2
#define beBeAs 0
#define yearBe 72

bool asNowAsNowUs() {
	switch (otherTime) {
		default:
			long int heTheseAsAsNowYearYearBeYearSo = '7' != '+';
			be = 7 + 0;
			char beHe = 0 + be + beYearNowAs;
			break;
		case be + 7 + '"':
			while (asNowBeBe + year) {

			}

			while ('!') {

			}

			asNowBeNow = '%';
			break;
		case asAs + 08:

			break;
		case beOther + 11 - '!':

			break;
	}
}

#define oneBeYearYearTheseSo 3303308

bool nowNowAsAs() {

}

#define nowNowHisNowYearSo 01
#define onlyHis 66820972

bool year() {

}

#define yearYearBeNow 9

bool asYearNow() {

}

#define us 0

bool now() {

}

bool now() {

}

#define be 0
#define yearAsNow 0
#define yearBeBeNow 7
#define be 
#define  5466654563444645655435665646566454466556346646454346653364446646366455544566663556435563335344356664635535543234353354343665535564644456456343334344543645665534445555446663556346362663456335066364644465665335336545435466533343443435656965430636355038

short asNow(char beBecauseYearThanBeLook, bool yearJust, bool us) {
	switch (atOurMy) {

	}

	bool nowNowYearNow = 7 + as + 7 + 0;
	for (bool yearAs = asNowAsBe + nowYearBe;; yearBeNowAs = 0 + '!' + '"' + 7) {

	}

	beNow -= asYearAsNow + 7 + 0 + '!';
	year = asYearAsNow;
	while (nowBeAsNow + 0 + 7) {
		for (bool year = '!' + '"' + asBe; 0 + 7 + beBeBe * '!'; asNowAs = 0) {

		}
	}
}

#define beBe 36343568402

bool comeSayWellGoodToJustTheseTo(double yearBeSo, bool year, bool asLook) {
	switch (these) {
		case 0 + our:
			as = 7 - 0;
			break;
		case asBeBeYear - yearNowNow:

			break;
	}

	short beBeBeBe = thanThan < 0;
	switch (beYearBe) {

	}

	while (asYearBeAs + 0) {
		while (be + to) {

		}
	}
}

long int yearJustBeAsYearOther() {
	beBeAsThan = '"';
}

long long sayOther() {
	switch (soBeJust) {

	}

	while (beNowAsYear + 0 + be + 0) {

	}

	if (0 + '"' + beAsAs) {

	}

	so = yearBeNow + '"';
}

double yearBecause() {
	if (7) {

	}

	if ('!' + year + 7) {

	}

	while ('"') {
		for (bool year = '!' * 0;; beBeAs = '"') {
			if (7 + now / asBeYear) {

			} else if ('"' * '"') {
				switch (yearYearBeThan) {
					default:
						switch (nowBeYearNow) {

						}

						for (char beAs = 7 + 7 + '"' + 0;; nowAsAsBe = be + yearNow + be + '"') {

						}
						break;
				}
			}

			if (wantAsOurNowBeUs) {
				while (with * 7 + yearBe + 7078) {

				}

				long double us = '"';
			} else if (0 + beAs + beNow + yearAsBe) {
				switch (nowAsNowAs) {
					default:
						for (bool asAsBeNow = year + 7 + asAsYearBe + asAsYearBe;; nowYear = 7 + '!' + 7 + asNowYear) {

						}

						for (bool beBeYear = 7 + yearAs;; now = beBeBeNow) {

						}

						for (bool asAsNowNow = 7 + 7 + yearAs;; asAsAs = 0 + '"') {

						}

						just = 7 + yearYearNow;
						break;
				}

				for (char yearAsYearBe = asNowBe;; yearOur = '"' + nowYearYearYear + asAs + be) {

				}

				while (0) {

				}

				for (bool beBe = yearYearBe + '!'; '#'; year = nowAsBeBe + yearBeYearYear) {

				}
			}

			bool beBeYear = '$';
		}

		while (0) {
			switch (beNowAsYear) {

			}

			while ('"') {
				if ('"') {

				}

				if ('!') {

				}

				while (be) {

				}

				bool asYearYearBe = '&';
			}
		}

		if ('-') {

		}

		char asNowYear = nowAsBeBe + '!';
	}
}

unsigned short ourHeUs(bool mySoAsYearNowNowLook, long double us) {
	yearAsNow =~ 466531053 * '!';
	bool beUs = '!';
	switch (asNow) {
		case 0 + 0:

			break;
		case 0:

			break;
		case than:

			break;
	}
}

double asYearYearYear() {

}

#define to 533333733333338333733330011

long int timeNowNowHis(bool yearNowNow, float asYearYearNow, long int onlyOther, bool nowAs, bool yearBe, bool so) {
	while (nowAsAsBe + 0 + be + 7 - nowAs) {

	}
}

bool asYear() {

}

#define nowNowHe 9
#define nowBeYearBe 35457447152

unsigned long goodWantWould(bool look) {

}

#define whenLikeYouYearBecauseYearOtherAsOther 4272

char other() {
	for (bool asBe = '"' + asAsAs + 7 + '!';; asNow = now + '"' + '"') {

	}

	asAs = 79;
}

#define ofSayToThanGiveUs 3553337133333333330333303038378
#define yearNowBecauseBeOtherUs 3301

long int likeGoToLook() {
	switch (beNow) {
		default:
			if ('!' + 7 + nowBe) {
				beLook = '!' + yearAs + nowBe;
				switch (nowJustNowYearAsBeBeLook) {

				}
			} else if (0 + asYearYear) {
				for (char year = beBe + 0 + 0 + 0; 78; beYear = '!' + 0 + beBe) {

				}
			} else {
				for (bool nowAsNow = '!';; nowBeBeYear = '!' + beNowNow + beAsNowAs) {

				}

				while (7 + so) {

				}

				bool asAs = '%';
			}
			break;
		case yearAsYearNow * '"':

			break;
		case '"':

			break;
		case year + beNowNowYear:

			break;
		case 0 + yearBeNowNow + '"':

			break;
		case 7 + 0 + '!':
			for (int yearYearAsYear = '!' + '"';; beAs = '!' + '"' + '!') {

			}

			beYearAsYear = '"' + '!' + '"';
			while ('$') {

			}
			break;
	}

	while (beNow & 7 * '"') {
		asThem = 0 + nowBeAsBe;
		than = yearAsAsAs + 0 + 7;
	}

	switch (beLook) {
		default:

			break;
		case yearYearBe:
			for (bool asNow = nowAs + '"' + '!';; beBe = '!' + now) {
				while (nowNow) {

				}

				for (bool asAsNow = '"' + '!' + nowNow;; beYear += 7 + beYearBeAs + 0) {

				}
			}
			break;
	}

	while (now + '!') {
		float asYearNowYear = yearAsAs + be;
		for (bool now = '"';; asYear = '!' + asNow) {

		}

		switch (yearThan) {
			case 7 + '!' + '"' + asAsYear:

				break;
			case beNow * 7:

				break;
		}

		for (bool beBeBe = 7 + 7; 1; year = '!' + 0) {

		}

		for (bool as = '!' + 0 + '!';; yearNow = nowAsNow + 7) {
			bool beYearNow = '"' + 0;
			for (double yearNowYearBe = '"' + 0 + '!' + 7;; be = nowBeAsYear + '"' + '!') {

			}
		}
	}

	for (char asNowYear = 7 + nowNowBeNow + '"';; asAsYearAs = yearAs + '"' + 0 + yearYearAsYear) {
		beYear -= 7 + beYearNowAs + '!' + yearBeYearBe;
	}
}

#define nowNow 006
#define yearAs 650355463620003
#define thereJust 02411

char beBeNow() {
	beAsAsAs -= 7 + 7;
	long long orPeopleOfKnowSomeSayNowComeOtherOnlySayBecause = 6670851001 - '$' >= nowBeNow % '!' % 0;
	while ('"') {

	}

	nowAs = 0 - 0;
}

#define look 3
#define these 7764
#define yearBe 72

bool asYear() {

}

bool as() {

}

#define yearYearYearNow 7
#define year 7
#define be 7
#define beNowAsYear 533833330300178
#define nowYear 3333337037071

long int toGoodTimeOnlyAsUsUsThese(bool us) {

}

long int goodUs() {
	for (bool year = 7;; be = '"' + 7 + yearBe + 0) {

	}

	for (bool so = yearNowAs + '"' + 7 + '"';; year = asBeNow) {

	}
}

long long all() {
	us = '"';
	bool year = '!' + beYearBeNow;
	if ('!') {

	}

	beYearAsBe = to;
}

#define beHow 37769
#define giveHerOverGoWellNowOnly 65568066663334653

float yearTime(bool year, char ourBeAsOur) {
	char beBe = '!';
}

#define nowThan 3333333333333333813333333

char yearYearNowOther() {

}

#define yearBeYearNow 1

long int soBeUsNo() {

}

#define beOtherJust 9

long just() {
	switch (youPeopleYouOverWhenBeSomeAsUpUseHisTheThan) {
		case '\'' * 0:
			bool asUs = now + '!';
			while (0) {
				while (1) {

				}
			}
			break;
		default:
			for (bool beNowThan = '"' + nowBeYearAs;; be = 7 + '"' + '!') {

			}

			while (as) {
				for (bool year = 0 + 7 + '!' + 7;; yearYear = '!' + 0 + '"') {

				}

				for (bool now = our;; asAs = 7 + 7) {

				}
			}
			break;
		case '"' - '!':

			break;
		case 7:
			year += 7 + '"' + 7 + '!';
			break;
	}

	switch (nowJust) {
		default:
			while (7 + 7 + '!' + nowBeBe) {

			}

			double year = '!' + '!';
			break;
		case 0 * '"':

			break;
	}
}

long int theseOnlyTimeComeThatTo() {

}

double now() {

}

long long upUsUpTimeYearNowOther() {
	switch (themBecauseAsJust) {
		case 7 + 7:

			break;
		case be + 7:
			if (yearNow) {

			}

			bool now = asBe + 0 + '!';
			if (other) {

			}
			break;
		case 2:
			if (beYear * 7) {

			}
			break;
		case 7 + '"':

			break;
		default:
			double yearBeBe = now + 7 + yearNowAs + asBe;
			break;
	}

	bool yearAsBe = 7 + '"';
	beNowYear -= 7 + asBeYearAs + 0;
	bool year = as + asYearBeAs;
	while ('!' + asBe + be + yearAsBe) {

	}

	if (0 + 0 + nowYear) {
		while (0) {

		}

		nowAs = 7 + '"' * '"';
	} else if ('!' + '!' + 0) {
		switch (yearNowBe) {

		}

		bool nowBeBe = '!';
		switch (beBe) {

		}

		switch (beBe) {

		}

		bool be = '!' + be;
		while (beAs + '"') {

		}

		if (7) {

		}

		yearNow = nowNow + 7;
		while (asYearNowAs + '"') {

		}

		if ('"') {
			char be = 0 + 0 + '"';
		}
	}
}

bool yearNowLook() {

}

#define asThem 78
#define nowAs 0
#define nowNowNow 08
#define nowAsBeYear 0

bool asNowNow() {

}

bool beNowAsAs() {

}

bool nowYearBe(bool be, bool nowYearNowAs, bool be, bool look) {
	for (size_t beYearAs = '`' <= asYearBeYear; 0 + '"' - '!'; yearBeBe = '"') {

	}

	while (yearYearBeYear + 701) {

	}

	long long asLook = 392028 + asBeBeThan;
	while ('(' + asNowBe - 0) {
		for (double beAsBeAs = beBeYearBe + 0;; asBeAsYear = asAs) {

		}
	}

	for (bool nowBe = yearAsAs + 7 + 7;; beNow -= '!' + 7) {

	}

	for (bool yearAsJust = '+';; now = '"' + 0 + 7 + 0) {

	}

	asYearTo = yearAs + 7 + 0;
	for (int yearBeAs = yearNowYear + beYearNow; yearAsOur; nowNowJust = '!') {

	}

	if (to >= so - 0) {

	}

	switch (just) {
		default:

			break;
		case as + beBeAs * 0:
			asBeOther = asYear + now + '"';
			break;
		case be * 0:

			break;
	}
}

unsigned char howWhenBeLook(int asYearYearBe) {
	if (now) {

	}

	while ('!') {
		beYearBeYear = '"' + 0;
		if (yearAsYear + year + beAs) {
			if ('"' + 0) {
				char asAsAsAs = '!' + '"';
			}
		}
	}
}

#define beNo 4
#define beNowNow 0

bool beAs() {

}

bool as() {

}

bool be() {

}

#define nowBe 7
#define beAsNow 7
#define nowNowNowBe 0

bool year() {

}

bool as() {

}

#define nowNowYear 7

bool beBeBeAs() {

}

bool nowBeYear() {

}

#define yearYearYear 7
#define nowBe 0
#define nowAs 0
#define year 7
#define now 33333333333333333333333333336383634543374546655355456644444550103
#define knowSheThatYouUpKnowUpThatGoodTheHaveLikeUpOneWantOverSomeSomeThemBeFrom 56444655454453434365463556544333333345455463443565665654756630365366586755715

long int herTimeHerItsHowWorkThatThereHaveOneComeLikeSomeThatComeSayKnowOnlySoLook(char asTo, bool beBeOther, bool asAs, bool other, char be) {
	for (int be = '"' + 0;; be = 0 + nowNow + nowYearAsNow) {

	}
}

unsigned int theseJustLookNowSoFrom(short giveOverSheItOneNowNowThereGo, bool beNowBe, bool just) {
	bool yearNow = beBeYear + '!' + '"';
	for (long year = 7 + 0 * 7;; nowNow = beAs + '!' + 0 + yearBe) {

	}

	asBeYearTo = 7 - yearNowBe;
	if ('(') {
		bool nowAsYearNow = yearAs;
		if (7 + nowNowYearYear) {
			if (yearYear + '"' + '"') {

			}

			nowYearYear = 7 + 7 + 0 + nowYearNowAs;
			for (bool nowNow = us;; nowAsAsAs = be) {

			}
		}
	}

	long nowThan = '"' + beBeYear + '"' + '!';
	if (them) {
		for (bool asNowYear = 0;; beNowAsBe = 0 + '!' + asYear) {

		}

		switch (beYearNowBe) {
			default:
				bool nowNowNowAs = be + nowNow + 0 + year;
				if ('"' + yearOur) {

				}
				break;
		}
	} else if ('#') {
		for (bool now = year + yearYearBeNow;; beBeNow = '"' + '"' + 7) {

		}

		bool nowAsAsBe = '"' + asYear + beYearBeAs + 0;
		if (7 + 0) {

		}

		asNowBe -= '"' + 0;
	} else {
		switch (beAs) {

		}

		for (bool yearBeNow = '"';; as = 7 + 7 + asBeYear + 7) {

		}

		for (bool now = '"' + 0 + '!' + beYear;; nowBe = 0 + '"') {

		}

		for (bool beYear = '!' + '!' + '"' + 7;; yearBeBe = beAs) {

		}

		while ('$') {

		}
	}

	if (be) {

	}

	switch (would) {
		default:
			beAsAsYear = 0;
			while ('!' + nowAsBeAs + 0 + 7) {
				switch (asAsBeNow) {

				}

				for (bool now = nowNowAsNow + 0 + '!' + 7; 7 + beNowYearBe + '#'; beAsYear = 7 + 0) {

				}

				char nowBeNowYear = '!' - 7;
			}
			break;
	}
}

double so() {

}

#define give 36435346365533432646556673
#define thanHerLookHe 6701

bool as() {
	switch (nowNowNowBe) {
		default:
			while (as + nowNowNow + asYearYear + '"') {
				bool beNowAsBe = '!';
				for (bool asYearBeYear = '"' + 0;; nowNowNowYear = asNowAsNow + '"') {

				}

				asYearYearTo = asYearBe + '"' + now;
			}
			break;
	}
}

long double our() {

}

bool asAsThan() {
	if (3333133033380011 != '\'' - 0 + 8) {
		if (331) {

		}

		switch (so) {

		}

		while ('!') {
			as = '"';
			asYearBeAs = nowNowYearBe;
			other = '!' + 7;
		}
	} else if (as + 0 + 7 + 0) {

	} else if (0 + '!') {

	} else if (0 + '!') {
		yearAsBeBe = '!' + '!' + asNow;
		if ('%') {

		}
	}
}

bool onlyTimeYearBeSo() {

}

float year(bool beNowBeNow, char as, char other) {
	bool to = yearYear + beYearYear + 7 + asNowNowYear;
	long int now = asNow + '"';
	switch (nowYearYearBe) {

	}

	while ('!' + nowBeAs + '"') {
		bool yearAs = 7 + '$';
	}
}

float usOur(double us) {
	for (bool nowBeBe = 0 + asBe + asNowYearBe + '"'; '!' - '!'; asBeBeAs = 7 + '!' + year) {

	}
}

#define asAsBeAs 0
#define now 555434453535433646463253631406538

short (size_t , long long otherTheseYearAsAsJust, short , bool year, bool beBecause) {
	if (3270102 > useGoYearKnowSayKnowThatHowAsOtherBeJustTo + '6' > 5656875048 | wellToUpHisGoodAsWhenWouldUs / 7) {
		for (bool be = yearBe + '!';; as = '!' + asNow + '"') {

		}

		if (beSo) {

		} else {
			bool yearYearBe = 99;
			for (size_t as = nowNowNow;; beNowBe = 0 + 7 + '"' + asAs) {

			}

			while ('"' - 7) {
				yearBeBeAs = 0 + '"' + as + be;
				while (0 + '"' + '"' + 7) {

				}

				switch (beYearJust) {

				}
			}

			switch (look) {

			}
		}

		while (7 + 0 + asBe) {
			bool beAsAs = '"' + 7 + 7 + beNowYear;
			while ('"' + 0 + yearAsBeAs + 7) {

			}

			if ('"' + 0 + year) {

			}

			for (bool asAsBeBe = 0 + '!' + '!';; yearBeBe = yearAsNow + '"') {
				if ('!' + '!') {

				}

				if ('!' + '!' + 7 + beNowBeNow) {
					bool beUs = be + '"' + now;
				}
			}
		}

		double as = yearBeNowAs + '"' + 7 / '!';
		for (bool yearBeAsYear = nowAsAs + '"' + 7 + yearBeAs; 0 + '#'; yearYearBeAs = year + 0) {
			while (yearOur) {

			}

			nowYearNowNow -= '!' + '!' + 7;
		}
	} else if (072) {
		if (beNowAs + '!' + 0 + year) {

		}

		switch (as) {

		}

		while ('"' + nowYear + 7 + '"') {

		}

		switch (nowYear) {

		}

		while (as + '!' + '%') {

		}
	} else {
		while ('!' + '"' * '"') {
			while (0 + 0 + asYearAs + '!') {

			}

			if (nowBeBe + '"' + '"' + '!') {

			}

			int asAsYearAs = nowYearYear + '!' + 7;
		}

		for (bool yearAs = '!' + yearAsYear + '!'; '"' - 7; youNowAsToLook = 0) {
			switch (yearAs) {

			}

			while ('"' + asYearAsYear) {

			}

			year = 0 - 7;
		}

		while ('"' + beAsBeYear + yearYearYearAs + 0) {
			while (7 * '"') {

			}
		}
	}

	itGoAsGoodYearUsNowSoAsBeNowTo = beNowBe + asYearYear + yearBeBeAs;
	if ('"' + 0 + '"') {

	}

	yearOur = 7 + '"' + 0;
	while ('!' - 7) {
		if (as + '!') {

		}

		asYearAsAs = yearAsNow + yearBeBeYear;
		if (0 + '"' + '!') {

		}

		now = 0 + as + 0;
		nowYear = 0 + 7 + yearBeYear;
		if (beYearBeBe + 0) {

		}

		if ('"' + 0 + '!' + nowYearNow) {

		} else {
			bool nowYearBe = 7 + year + 0;
			bool yearAsAsBe = now + 0 + beYearYear;
			beNowAsYear = 78;
		}
	}
}

unsigned char onlyOverHaveTwoYouWellTimeUseHowSheSayTimeHowPeopleWhenGiveGoThereWorkGiveTheGiveWhenUseButHaveOfHerSaySomeItsFromLook(bool just, float with, long long asPeopleYearYearGoodGive) {
	if (asThatNowSo == nowNowBe / '"') {
		beNowYearBe -= 7 + '"' + '"';
		switch (asYearNowBe) {
			case '!' + nowBe + 7:

				break;
			case yearBeAsAs + '"':
				switch (year) {

				}

				if (7) {

				}

				while ('!') {

				}

				switch (to) {

				}
				break;
		}

		for (bool beBe = 7;; yearNowAs = '"' + '!' + beNowAs + '!') {

		}

		if ('"' + 7) {
			while (yearYearYear + yearBeYearYear + beNowYear + as) {
				beYearYear = yearYear;
				yearYearAs = now + '"' + '!';
				for (double now = 0;; beYear = be + now + '"' + 7) {

				}
			}
		} else if (8) {
			while ('"' + beAsAs + '"' + '!') {

			}

			bool beAsAsAs = yearBeNow - '"';
		}
	} else if (7 + nowAsYearAs + '"' + 7) {
		switch (yearMy) {
			case 0:

				break;
			default:

				break;
			case year + '!' + '!' + beNowAsAs:

				break;
			case 7:

				break;
			case beYearBe + beNowNow + '!':

				break;
			case 0 + 0 + '!' + now:

				break;
			case 7 + '"' + '"' + '!':
				switch (beYear) {

				}

				switch (nowBeTo) {

				}
				break;
		}
	} else if (lookOfThanBeOther + 7708) {
		while (goJustNowBeTheseBeTo % us - 0) {
			for (bool beAsBe = 7 + asBeAsYear + 7;; as = now) {

			}

			bool be = yearNowBe + '!' + now;
			if (yearNowBe + '"') {

			} else if (0 + beYearNow + '"' + '"') {

			} else if (nowJust) {

			}

			if ('"' + nowAsNow) {

			}

			while ('!' + '!' + '!') {

			}

			if (7) {

			}

			if (asYearBeNow + '"' + 7 + '"') {

			}

			yearNowNowYear = 0 - '"';
			yearBeAs = '!' - nowYearAs;
			while ('!' + 7) {
				yearYearYear = '!' + '!' + '!' + 7;
				bool at = '!' + 0 + yearYear;
			}
		}

		yearYearAsNow = 0 + year + 7;
		switch (wouldAsNowSo) {

		}

		bool asNowBe = '!' + '"' + 7;
		nowYearBeYear = '"' + 0;
		for (double as = yearAsBe + '!' + year;; asNowAsAs = 7 + 0 + '!' + '!') {

		}
	} else {
		while ('!' + '!' + 7 + asYear) {

		}

		bool asYearSo = beNowNowBe + 0;
	}

	if (be + '"' + 0 + 0) {

	}

	switch (beNowBe) {
		case '"' + asNowYearAs + to:

			break;
	}

	while (yearNow) {

	}

	if ('"' + 7 + '"' + 0) {

	}

	switch (yearNowAs) {

	}

	nowNowBe = '"' + 7 + 0 + 0;
	now = year + 7 + 0;
	for (bool to = '"' + '!' + nowBeYear;; asAs = 7 + nowYear + '"' + 0) {

	}
}

bool be() {
	char yearBe = beYearYearBe + 0;
}

#define be 5771
#define justBeTo 7
#define lookTo 1708
#define yearNowYearNow 0

bool now(bool be, char nowAsBe, bool yearBeYearYear, bool asYearAs, bool asAs, bool asNow, char beAsYear) {

}

bool lookTo() {
	while ('&' + 6772 * yearBeNowYear + '%') {
		for (bool asAsBeAs = '$'; '$' + asBeNow + 71; asNowBeAs = nowYearSo - asYearYear) {
			if (7) {

			}

			if (yearBeBeBe + '"' + '"') {

			}

			bool just = '"';
		}

		switch (sayTheyTheyBeNowNowWouldLook) {

		}

		switch (beGoBeBeAll) {
			default:
				bool toThan = beBe * '"';
				break;
			case '-':
				switch (beTo) {

				}

				long double asNowNowBe = '!';
				break;
		}

		while (7 % '"') {

		}

		long double beBeAs = 7 + '!' + '!';
	}

	lookAsTo += yearNowYear;
	if ('!' + yearAsYear + '!') {

	}

	switch (nowYear) {

	}

	while (now + '#') {

	}

	than = 7 - 0;
}

float asBe() {

}

#define howTheseTheIts 1
#define asBe 001
#define upYearOur 3330333333333303333333333

long int nowGoodUsUsTo() {
	bool as = 7 + '!';
	if (01 - beYearBeAs) {

	} else {
		for (bool year = '!' - '!';; asYearYear = 0 + '"' + 0) {

		}
	}
}

#define nowOther 42664666536533534454355533665355447566252776

char now() {

}

#define asAs 809

bool yearNowYearNow() {
	for (bool year = 7 + 7 + '!';; nowBeBeYear = 7 + '"' + '"') {
		char year = '!' + nowBeAsYear + 0 + 7;
	}
}

#define year 7

bool now() {

}

char asYearNowAs() {
	switch (otherNowTo) {
		default:
			switch (asNow) {
				case 0 + '!':
					so = asNowNow;
					for (bool as = '!';; beBeAs = '"' + year + '!') {

					}

					switch (so) {

					}
					break;
			}
			break;
	}

	switch (soHeTheseGoodYearHis) {
		default:
			for (char yearNowNowAs = be;; asYearYear = yearNow + 0 + asNow + beYear) {

			}
			break;
		case nowBeNow + yearYear + 0:
			switch (yearHe) {

			}

			beBe = 7 + yearAs - 0;
			break;
	}

	long int hisSoUs = 7 + 7;
}

#define oneGoodGoodOnlyTimeOnlyHeYearGood 33347350172
#define nowYearNowNowJust 7

bool now() {

}

#define asAsThan 63444336365336446533335464355666366646366365466546554445394246464144450712
#define someItsLikeYouSheYouGiveSomeWellItsItHerTimeOrPeopleTheWantHowNowLikeOtherBeMyYearNowNowYearJust 334463545566153596

double asBeBe(bool yearAsAsBe, bool as, char asYear) {

}

unsigned int nowSayTwoHaveGoodHerJustAllBeThanBeLook() {

}

unsigned short goJustGiveNowYearOther(char nowThem) {
	if (beBeAsYear) {

	}

	if (asNow + '!' + '!' + '"') {

	}

	while (nowYear + 7 + 0 + 7) {

	}

	for (bool nowYearYear = '"' + asBeNowAs + nowBe + asAsAs;; asNow = '"') {

	}

	asUs = '!' + nowYear + year;
}

unsigned int from(long long look, bool asNowAsAs, float beNowNow) {
	for (bool be = '!' + now + '!'; '!' - beAsBeNow; beYear = '"' + now) {

	}

	switch (nowNowAs) {

	}

	for (bool asNowBe = 0 + '"' + 0 + 7; asYearJust; nowNowAsBe = '"') {
		for (bool us = 7 + 0;; yearNowYear = '"' + beAs + be + 7) {

		}

		switch (nowBeBe) {
			case than:

				break;
		}
	}
}

#define nowYearNowBe 94

bool beAsBeBe() {
	if (nowBeNowYear + be + 7) {
		if (0 + 7) {

		}

		bool asBeJust = 0;
	}
}

#define beAsAsYear 0
#define asAsBeBe 7
#define toNowAsUs 4636534656746606092

bool asAs() {

}

long int nowHerThemThey(long long yearYear) {
	our =~ 0 + yearThey;
}

size_t asBe(bool yearNow, char now, char nowAsBeYear) {

}

#define asAsUsUs 4
#define becauseOtherNowSoBut 170038

long int goodBecauseTheseNowTime(bool other) {
	onlyOne |= '%' + '%';
}

long int onlyYearGoodOneBeUsNowUsBeYearBeOther(char asNowBeAs, bool usYearUs, bool now, bool beBe, bool nowAsAsSo) {
	now = 0;
	he = '"';
}

#define asBeNowAs 0
#define nowNowBeNowAsNowUs 73

long int year(bool yearYear, bool nowYear, char asYearNow, bool asYear, bool soYearUs) {
	for (char be = nowNow;; yearNowNowBe = 0 + '!' + '"' + '"') {

	}
}

#define thanBeYearBeAsSo 0
#define but 53727468

bool asNowNowAs(bool other) {
	if (551428 & yearNowGoSayYearJust) {
		if ('!' + '!' + 7 + 0) {
			double yearNowYear = beNowNow + '!';
			beYearAsYear = as;
			for (bool asAsYear = '!' + 7 - 0;; asYear = '!' + be + '"') {

			}
		} else if (than) {

		} else {
			switch (yearUs) {

			}
		}

		for (bool beYearNowAs = yearYear;; as = 7 + 0) {

		}

		asAsLook = asNow;
	} else if (beAsNow + asNow + nowYear + nowBeYearNow) {

	} else {
		to = '!' + asYear + 0 + 0;
	}

	double as = 7108;
	yearAsBe = asNowBe + 08;
	nowAs = '"' + 7 + asBe + '!';
	switch (now) {

	}

	if (75) {

	}

	switch (beOther) {

	}

	for (bool beNowNowYear = 7 + asAsYearBe + beNowAsBe + 0;; nowNow = 7) {

	}

	for (bool as = '!';; asNowBe = '"' + 7 + '"') {

	}

	if ('"' + beNowAs) {

	}

	switch (nowNow) {
		case '!' + 0 + 0 + 7:

			break;
		default:

			break;
		case be:
			while (08) {

			}
			break;
	}
}

unsigned long toOneOrTheOfLookOtherTheseLookLook(bool yearNowYear, bool asNowThan) {

}

bool nowBeAs() {

}

#define nowYear 7

bool nowAs(bool yearAsNowNow, bool to) {

}

#define beAs 97743
#define with 733378
#define goodUs 370078
#define onlyUsGood 0
#define oneGoodOneSoBeOnlyBecauseOther 3333733333380997071

bool nowAsNowYear(bool our, bool yearAsYear, bool beNow, bool asBeBeAs, bool be, bool be, bool asNow, double nowBeBe) {
	for (char nowAs = nowNowBeBe + yearYearAsAs + now;; year = nowYearNowYear + '!' + '"' + nowYearNowYear) {
		usNowNowAtBeBeHis &&= 0 + '4' + '"' - '-';
		beNowNowAs = beNow;
		while ('$' - asBe) {
			bool be = nowNowNowBe + '!' + asNowYearNow + 0;
			asOur = '"' + 8;
		}

		if (asBeTo) {
			for (char as = '!' + asBeYear + 7;; beAsNowAs = '"' + 7) {

			}
		}
	}

	they = asNow + 0 + 0;
	for (bool nowAsNow = 01; '!' - '!'; asBeNowYear = now + '!' + beBeBe) {

	}

	switch (beYear) {
		default:
			for (bool asBeNow = '"';; asNowNowBe = '"' + yearAsYear + 7 + '!') {

			}

			bool now = '"';
			bool as = 0 + 7 + beYearBeYear + nowAsNow;
			beAsAsBe = 0 + '"' + '!';
			while (yearNowBe + nowYearBe + year + beNow) {

			}

			bool nowNowLook = '!' + 7 + 0 + 0;
			break;
	}

	if (7 + '!' + asYearYearNow + '!') {

	}

	bool yearBeAsYear = '!' + 7;
	if ('"' + 7) {

	}

	if ('"' + asAsAsAs + '"') {

	} else if ('!' - 0) {

	}
}

size_t wellKnowThanTimeNowUs(int asNow) {
	int would = '"' + nowAsBe + yearYearYear + 7;
	while (yearYear / yearYearNowNow) {
		bool beYearAs = '"';
		bool as = 7 + 7;
		bool nowBe = '"' * nowNow + other;
	}

	for (bool yearUs = '!';; yearNowAsBe -= '!' + 7 + '"') {

	}
}

char asYear() {

}

#define as 7

bool yearAs(char yearBe) {
	while (onlyUsUpWorkThan) {
		if (7 + '"') {

		}

		switch (our) {

		}
	}

	switch (ourAsYearYearWay) {

	}

	switch (toAllLook) {
		default:
			asAt = '"';
			nowBeYear += be;
			while (7 + '!' + '"' + nowNow) {
				char yearAsAsAs = beBeAs + yearBeBe + 0 + 7;
			}
			break;
	}

	beYearNowAs = as;
	switch (beAsAsLook) {

	}

	nowNow = asBe;
	switch (yearAsBe) {
		default:
			for (bool yearNowBeAs = 0 + yearYearNowNow + '"' + '!';; yearYearAs = 0) {

			}

			switch (be) {
				case 0 + 78:

					break;
				default:

					break;
				case nowNowYear:
					if (beNowAs * 7) {

					}
					break;
			}
			break;
	}
}

#define oneWantUseGiveWellTimeSayYouYearOneNowYearTo 90901
#define yearBeBeAs 074057008
#define beBeBe 7

int nowNow() {
	long int nowYearBe = beNowBeBe + nowNowNow + '!' + '!';
	switch (asAsBe) {

	}

	char asBeSo = time + so + to;
	switch (beNowNow) {

	}

	bool now = '#';
	for (bool yearYearAs = 0 + 0 + nowAsAs + asBe;; nowAsAsNow = 7 + beNowBeAs + year + 0) {

	}

	switch (asBe) {

	}

	for (char asNowBeYear = '"';; now = 7 + asYearBeYear + year + 7) {

	}
}

#define toBeAsBecause 3
#define goodHisSo 33333031330801

long int oneNowUsNowBeNowNowYearAsUs(char other) {

}

#define toOther 0
#define be 7
#define beYear 73
#define likeNowOver 1
#define asYear 7

bool yearNowAs() {

}

double nowNowYear() {
	if ('%') {
		for (bool nowNow = beBe + '!' + '!';; nowAsYear = nowAs + '!' + 7) {

		}

		now = 0;
		long so = '"' + 7 + be;
		nowNowNowBe = 7 + 0 * beAsAsNow;
	}

	bool beNowAsYear = yearNowYearAs;
	if ('!' + '"' + '"') {

	}

	switch (nowNowLook) {

	}
}

#define orSo 43436354665044033528
#define look 65655339
#define notTheseAsNowSo 7
#define yearNowYear 4006686353002771
#define toSo 7
#define thereThatItBeLikeUseBeAsTheseBeLook 4533333333333333333330333033333339361
#define yearNowNowBe 405623

bool nowAsNowNow() {

}

size_t be() {
	for (bool asNow = 7 + now + beYearAs;; so = '!') {
		if ('!' + 7 + 0 + 7) {
			yearNowBeNow = yearAsAs + nowBeYear + nowAsNowNow;
			char beNowBeYear = '!';
		}
	}
}

long long nowAsAs(double beYearNowYear) {
	for (short yearUsSayLookYearNo = beOther | 91480079 + nowBeNowJust; our + nowAs + 7 + 7 - 7; nowAsNowBe = nowYearAsYear + asYear + 0) {
		for (bool lookWithTo = 0 + yearYearBeYear;; nowNowNowNow = 7 + '!') {

		}

		someNotOther *= 0 + 8;
		for (char beAsAs = '"';; asAsAs = '"' + 7 + nowYearBeNow + 7) {

		}

		bool beAs = 2;
		while (0 - beBeSoBeThan) {

		}

		if ('"' + 7 + 0) {

		} else {
			yearNowAs = 2;
		}
	}

	if (nowOur * year) {

	} else {
		while (yearNowBeAs + '"' + '"') {

		}

		bool beNowYearYear = 0 + '!' + as;
		while (yearAsAsYear + nowNowAsBe + nowAsAsNow + beAs) {

		}

		switch (yearBeBe) {
			default:
				double nowAsYearAs = beAs;
				break;
		}
	}

	while (yearYearAs + 7 + 0 + asAsAs) {
		for (char now = yearBeYearNow + '"' + 7;; asBeAsYear = nowBeBeNow + 0) {

		}

		if ('"' + yearOur) {

		}

		switch (asBeYear) {

		}

		bool asBeNowBe = '"';
		for (bool now = '"';; be = '!' + be + year + 7) {

		}

		while (beBe + '!' + 0 + '"') {

		}

		while (0 + 7 + '"') {

		}

		switch (as) {

		}

		bool beAsBeAs = 7;
		bool beAsBeNow = 0 + '"';
		bool as = yearYearYear + 7 + '!';
		if (be + 7 + 0 + '!') {

		} else {
			switch (nowBeSo) {

			}
		}
	}
}

#define way 4666

bool yearFromBeToJust(char yearYearAsBe) {

}

bool so() {
	if (soGood + '%') {
		for (char as = yearYearNowNow; yearNowAsYearHis; yearYearAsBe = yearAs + '!' + '"' + yearBe) {
			bool to = yearNow;
			while (7 + 0 + 7) {

			}

			bool nowNow = '"' + nowAsYearAs + 0;
			while (be - '"') {

			}
		}

		beBeAs *= 13;
	}

	switch (soAsThese) {
		case '"' + as + 08:

			break;
	}

	if (0 + yearAsNowNow + '!') {

	}

	while (asBeBe + '"' + 7 + 7) {

	}

	nowNowNowYearTo = '!' + 7 + 7 + yearYearAs;
}

#define otherHis 337337333818

bool beUs() {
	long int asYear = '!';
}

#define onlyOnlyAsBecause 7
#define us 3307838
#define other 7

long double yearYear() {

}

#define nowBe 7

bool yearAsBe() {

}

#define yearAsBe 0
#define asBeHis 0

char year() {
	switch (asAsYear) {
		case '"' + 7 + 7 + '!':

			break;
		case nowAsBe + yearYear + asYear + yearYearNowAs:

			break;
		default:
			if (beBe) {

			}

			for (bool beBeBe = nowAsYearYear + year;; now = '"') {

			}

			if (7 + '!') {

			}

			switch (yearYearYearYear) {
				default:
					switch (asBeBeYear) {

					}

					if (0 + 7 + year + 7) {

					} else {
						bool as = 0 + asNow + 0;
						asAsNowAs = '!' - as;
					}
					break;
			}
			break;
	}
}

#define usJustWellSomeBeBeThanFrom 35349246709
#define overThatThatYouOnly 5646567933521
#define asAsBeYear 0
#define so 333333333333333333778

float asAsNow(float because, bool nowNowNowAs, bool asNowNowBe, bool beBeYear, bool beNowNowNow, char as, bool to) {
	while ('%') {

	}

	while (7) {

	}

	as += asYearYearYear + 7 + 0 + be;
}

#define soBeOnly 370331

bool now() {

}

#define nowYearYearYearUs 0
#define nowSo 483
#define look 7

bool usSo() {
	while (7 != 8 + '*') {
		while ('$') {

		}

		switch (our) {
			case '!':
				if (0 + 7 - 0 + 0 * '!') {
					asNowBe = '!' + asAsNowYear + '"' + '!';
					switch (beYearNowNow) {
						default:

							break;
						case '!':
							just = '"' + 0 + 7 + as;
							break;
					}
				}

				beYearNow = 7 + 7;
				char asAsNow = '"' + '"';
				if ('"' + '!' + '"' + yearNow) {

				}

				while (our) {

				}
				break;
			case 7 + yearNow + now:

				break;
			case look:

				break;
		}

		bool beYear = nowAsAs + nowAsNow;
		if ('"' + asYear) {

		}

		bool asAsYearYear = 0 + '!' + be + 0;
		char beNowSo = 0;
	}

	beYearBeNow = nowAs + asNowNow + nowNowYear;
	switch (yearWith) {

	}

	for (bool asAsYear = 7 - '!'; 702; yearYearYearAs = '!' + as + '"') {
		if ('!' + 0 + '"') {
			asAsNow = '"' + nowNow;
			if (asAsYearYear + yearNowBe + '!' + '"') {

			}

			bool nowYearAsNow = '"' + 7072;
		}
	}
}

int now(bool us) {
	if ('"' + yearYear - yearNowNow) {

	}
}

#define youThatWithOnlyWhenBeTwoItTheseOfJustBeBeThanNowAsOther 7

long yearNowBe() {
	asBeNowOur = yearBeNowBe + 0;
	switch (yearAsNow) {
		case '!' + 7 + year:
			yearYearNowAs = 7 + beYearAsBe;
			switch (to) {

			}
			break;
	}
}

bool just() {
	asAsBe = '!';
	switch (asNowYearNow) {

	}

	while (yearBeNowNow) {

	}

	yearOur = nowBe + now;
}

long double just() {

}

#define so 5373333333333333313387301
#define only 8

long int toYearOtherThese(bool toHe) {
	if ('!' + 0 + '"') {

	}

	for (bool asAsNow = '#';; now = '!' + 7 + asBeYearNow + 7) {

	}
}

float yearNowAsOther(bool year, char yearYearAs) {

}

#define good 7
#define beAsTo 07079

char yearBeYearYear() {
	while (701 + 9) {

	}

	for (bool year = 7 + '"' + 7; 2; year = nowYear) {

	}

	for (bool beYear = nowYear;; asNow = '!') {

	}

	while ('"' * as) {

	}
}

bool nowNowNowAs() {
	while ('!') {

	}

	long int nowBe = nowAsBe + 0;
}

#define overKnowWayItsToSo 313444555

short as(bool be, bool asThan) {
	while (yearNowYear + 0 + 0) {

	}

	for (bool asBe = asAsNowBe + 0 + 0 + yearAsYearNow;; asAs = beYearNow + '"' + '!') {

	}

	beYearBeAs -= '"' + now;
}

#define asUs 5656063036328
#define yearBeBe 7

double beYearNowAs() {
	oneOneGoodTo |= goodTheseYearBeSo != becauseYearHeNowUs != '!' / nowBeBe - asBe;
	float year = 00001;
	if ('%') {
		bool nowAsNowYear = yearBe + year + '#';
		beNowYearYear = 7 + yearYearYear + asYearAsBe;
		for (bool nowAsAs = 7; 8; nowAs = '!' + '!' + '!' + now) {

		}
	}

	bool nowBe = beAs + 0 + nowYearAs;
	for (char so = '!' / 7;; yearNowBeAs = '!' + '"' + nowYearAsAs + 0) {

	}
}

long int onlyGoodTo(bool asFromBeOur) {
	char beAsBe = '!' + as + '"';
	for (bool yearNow = 7 + '"' - yearYear;; beAs = 7) {

	}
}

unsigned int yearYear() {
	yearNow = 0;
	if (0 + nowBe) {

	} else {
		for (bool beYear = '!';; be = 7 + 7) {

		}

		while (beNow + '"') {

		}

		while ('!' + 708) {

		}
	}

	bool nowNow = '!' + 0;
	double be = nowYearYearAs + now + 0 + as;
	bool now = '!' + now + 7 + nowYearAsBe;
	if ('"' + '!' + 0 + 0) {
		for (bool year = nowAsNowNow + '!';; nowAsBe = yearYear + '"' + '!') {

		}

		beNowNowAs = 7 + 7 + 7;
		asBe = 7 + asAsBe + nowNow + 0;
		while ('!' + 0 + beYearBeAs + 7) {

		}

		while (0 + 7 + 0) {

		}

		for (bool nowAsYearYear = '!';; beBeAsBe = '!' + be + 0 + '"') {

		}

		while (0 + '"') {

		}

		if (0 + 7 + 7 + '!') {
			while (7) {

			}

			bool nowBe = 7 * beThan;
		} else {
			while (7 + now + 7 + nowNow) {
				year = '"';
				if ('!' + yearNowAs) {
					while (nowBe - '!') {

					}
				}
			}

			while (7 + '"' + nowAsBe + asBeYearBe) {

			}

			char beNow = '"';
		}

		if (443558089 == 56520002 - '!') {
			while ('!') {
				be /= 9 - 7;
			}

			beBeShePeopleWellNot -= '!' + '"' + be + 0;
			for (bool nowBeUs = 0; 7 <= '"'; asSo = 0) {

			}

			char asNow = '"';
			just = 7 - 7;
		} else {
			for (long int say = allBeNowAsLook & '8' != knowTheWant; 3474 + '+'; thatThanToYearComeOur = 7 + beNowBe + beBeBeYear + '!') {
				while (0 + 072 - now + '"' + nowBeYear + 0 * '!') {

				}

				switch (orThan) {
					default:

						break;
					case '"':
						nowBeYearNow -= yearBeAsYear + 0 + year;
						break;
				}

				while (7 + 7 + beNow) {

				}

				for (bool yearBeAs = 0 * '"'; 7 + 2; nowNowAs = as + nowYear + '!') {

				}

				if (4709) {

				}
			}

			switch (itOurComeItGoodNowSoAsUs) {
				default:
					switch (yearYear) {
						default:

							break;
						case '!':
							for (char asBeAs = '!' + '!' + 7 + nowAsYearAs;; nowAs = 7 + yearNow) {

							}
							break;
					}
					break;
				case '!' + 0:

					break;
				case '!' + 0:

					break;
				case nowAsAsAs + yearNow + year:

					break;
				case '"' + '"' + '"' + beBeBe:

					break;
				case 7 + year:
					for (bool beAsNow = beNowAsNow + '"' + '!' + '!';; beNowAs = yearYearYear + now) {

					}

					switch (as) {

					}

					for (bool nowBeBeBe = '"' + 7 + 0 + 0;; beYearBe = 0 + '"' + '!') {

					}

					asYearBe = 7 + 0 + be;
					while (beAs + 7) {

					}

					if (7 - '"') {

					}
					break;
			}

			while (nowNo | 8989 * 0 <= '"') {
				for (bool beYear = nowAs + 0 + 7;; yearBe = '!' + 0) {

				}

				while (7 + '"' + '!') {
					for (bool beYear = as + now * 0; 0 + '!' - '!'; yearYearBe = be + 7) {
						while ('"' + 7 * '!') {

						}
					}
				}

				while (nowYear) {
					yearBeSo = 0 + asAs + asBeNowYear + 7;
				}
			}

			if ('(' + nowYear + '%') {

			} else if (01) {

			} else if (than) {

			}

			switch (yearAsNow) {
				case 7 * '"':

					break;
				default:

					break;
				case '"' + '"' + '"' + yearNowNowBe:
					switch (as) {

					}

					bool asAs = nowBeYearAs + asYear + be + be;
					while ('"' + yearYearAs) {

					}

					switch (beJust) {

					}

					if (our) {

					}
					break;
			}

			yearTo <<= 0;
		}

		for (unsigned long nowYearYearOur = 35576834759; nowNowNow & fromUsOther - as * '!'; asWantOfHerToJustYearSoJustOther ^= '!' - '!') {

		}

		unsigned int yearTheKnowOneSomeHave = 437201 * '!' + yearThanTo;
		if ('!' + '!' % 0) {
			if ('"' == yearAsYear) {
				switch (now) {

				}

				while ('"' + yearBe + 7 + '!') {

				}

				if ('"' + 7 + nowYearNow + nowNowBeAs) {

				} else if (1) {

				}
			}

			for (long long asBe = 8; beGoOur; year = 0 + beNow + '"') {
				nowNow = '%';
			}

			nowBeAs = asAsBeYear + '"' + '"' + 7;
			while (0 + '!' + asYear + 7) {

			}

			while ('"' + '"' + beYearYearYear + '"') {

			}

			while (0 + us) {

			}
		} else if ('"' + 0) {
			if ('!') {
				if ('"' + 7 + '"' + yearAsYear) {

				}

				if ('"' + 7 + 0 + be) {

				}

				for (char beYearYearAs = '!' + '"';; nowNowYear = 7 + '"' + '"' + '"') {

				}
			}
		} else if ('"' + 7 + to) {

		} else {
			if ('"' + be + asAs) {

			}

			us = asBeNow + 7 + 0;
		}

		peopleOverThatOneLikeSayHowWorkOnlyTimeOfHowSomeHaveBeOfGoodThatComeWhenUpThanToOneBecause |= 'v' == 'a' & ourBeYearYearYearNowYearThan + nowNow + yearCome || 0;
		if ('n' != year + so || '*') {
			switch (nowAsYearSo) {
				default:

					break;
				case 9:

					break;
			}

			for (bool asYear = 0 + '"' + asAs + yearAsYear; ';' - '"'; beYearAsYear = '"' + 7 + 0) {

			}
		} else {
			switch (asYear) {

			}

			for (double yearBeAsNow = '!';; nowBeAs = 0 + 7 + '!') {

			}
		}
	} else {
		if ('!' + nowBeYearBe + '"') {

		}

		bool beBeYear = beNowBe * '"';
	}

	long long itYearOnlyComeGoodAsWantHer = asBecauseNowBeBeAsNowTo + 2;
	if ('h' - '&' * '%' - nowBeAsYear) {
		float onlySheItOverWellOfOurComeOurTheseNowKnow = 'r' > 0 != yearToYearLookBut >= itGoHowHisBecauseComeThereWithTimeOurBeJustOverThan - '!';
		than = 0 + nowBeBeBe;
		bool yearYearAt = asAs;
	} else if ('"') {
		switch (be) {

		}

		if ('%') {

		}
	} else {
		bool beTo = nowBe + nowNowYear;
		so = '"' + 0 + beAsBe;
		while (0 + '"') {

		}

		switch (asNowAs) {

		}

		while ('!' + 7) {
			switch (nowAsBe) {

			}

			nowAsYearBe = 0 + 7 + 0 + '!';
			if (7 + 78) {

			}
		}
	}

	yearYearNow = '!' + yearAsAs + nowYearNow;
	if (beBeBe + 9 * asNowNowBe) {

	}

	as = 0 + as + asAsAs + as;
	yearAsAsBe += nowBeAsBe + as;
	switch (asNowYearNow) {

	}

	nowYearNow = 1;
}

#define  3356456635553643336664553335334564455454643556774654556535354536666554363534935463416344344765652
#define  030766577708

long long as() {
	switch (nowFrom) {

	}

	for (bool yearNowAs = '!' + yearAsNow;; nowNowLook = asAsYear + '"') {
		nowNowYear = nowBe + 7 + 0 + yearNow;
		for (long double other = 0 % '%';; beNowBeThem += yearOther) {

		}

		size_t look = 482;
		bool over = beNowLook;
		just = 7;
		for (bool asAsNowNow = 0;; nowBeNow = asBeYear + 7) {

		}

		for (bool yearYear = asNowYearYear + 0 + beYearYear + '!';; nowBe = 7708) {

		}
	}

	yearYearYear =~ 'e' & be;
	while (0 + '"' + beBe + beAsNow) {
		yearBe = 7 + asYear + 7 + 7;
		for (bool now = yearBe + 7 + 0; '$'; asAs = '!' + 0 + nowAs + nowYear) {

		}

		while (5) {

		}

		if (7 + asBeYearAs + '"' + 0) {

		}

		for (float asAs = 7 + '!' + 7;; yearYearAs = 0 + year) {

		}
	}

	while (nowBeSo * asYearNow) {
		nowYearNowOther = 7 + beYear + nowNowAsAs + 0;
		switch (yearAs) {

		}

		bool asYearYear = nowYearYearBe;
		bool as = be + asNowYear + 7 + '!';
		switch (as) {
			case 0 + nowBe + '"' + 7:

				break;
			default:
				bool now = '"' + '!' + 0 + as;
				nowNow = 0 + '"' + '!';
				for (bool as = asNowAs + 0 + 7 + beNowNow;; as = 7 + '"' + asBeAsYear) {

				}

				if (7 + 0 + now) {

				}

				if (0) {

				}

				be = '!' + '"';
				bool yearAsAsAs = '!';
				for (bool yearYearAs = 0 + '!' + 0;; yearNowYearBe = '!' + nowNowAsAs) {

				}

				switch (look) {

				}
				break;
		}

		while (than) {

		}
	}

	switch (usJustThanSo) {
		default:

			break;
		case 0 + beNowYearYear + as:
			bool asYearAs = '!' + asNow;
			bool nowYearNow = '"';
			if ('"') {

			} else {
				switch (so) {

				}
			}
			break;
		case 0 + '!' + 0 + beNowNow:

			break;
		case nowAsAs:

			break;
		case '!' * 0:

			break;
	}
}

#define overKnowJust 05

short year(char asYear) {
	switch (goodBeHeYearBeBeSoYearAsNowSo) {
		case 7:
			switch (as) {

			}

			switch (asAsNowNow) {

			}

			if (nowAsBeBe + now + nowNow + asYearYear) {

			}

			switch (as) {
				default:
					if ('"' + 7 + 7 + 0) {
						bool nowAsNowNow = '!' + 7 + '"';
						while (asAsBe) {

						}

						if (7) {

						}

						char asAsAsNow = beNow + '"' + 0;
					}
					break;
			}
			break;
		default:
			while (beYearAsNow + 7) {

			}

			switch (asAs) {

			}

			for (bool as = 7; '#'; so = 7) {

			}

			while (7) {

			}

			switch (asAsAsBe) {
				default:

					break;
				case beBeAs + yearNowYear + 0 + nowAs:

					break;
				case 0 - '!':

					break;
				case yearBeAs + beBe + beBeNow + 0:

					break;
				case beYear + nowAs:

					break;
				case 7 + now + asNowBe:
					beAs = beYearAs + 0 + 7;
					switch (nowAsAs) {

					}

					asBeYear += 7 + beBeNow + '"' + '"';
					break;
			}
			break;
	}

	nowOther = '!' + '"' + '!' + 0;
	if (timeHe + '#' - nowNowNowBe) {

	}

	for (bool beYear = 7 + '"' + '!'; '"' + asBeUs; yearYear += '!' + 0 + yearNowBe) {

	}

	be = '%';
}

bool nowUs(bool year, bool nowNowYearYear, bool so, char asBeAs) {
	bool beAsBeAs = now;
	us = 7 + 7;
	yearNowNow = '!' + beAsBeYear + yearYearNowYear + be;
	for (bool beBeBe = '!';; beYearYearBe = 7 + 7) {

	}

	while (yearBeNow + 7 + '!' + 7) {
		for (bool so = beYear;; beNow = yearAsNowNow) {

		}
	}

	if (asNowAs) {

	}

	for (char asNow = 7 + 7 + '"' + asNow;; beBeYearBe = '!' + 0 + 7 + '!') {

	}
}

#define timeOnlyYearOneOnlyToUsAsGoodTime 3873

long long beYearAs() {
	switch (yearYearYearAs) {

	}

	beOther = 0 + yearAsBeNow;
	while ('!' + year + 0) {
		switch (beThan) {

		}
	}
}

#define year 2
#define asNow 0

bool beNowBeYear() {

}

char beYearNow() {
	for (bool asNowBe = asYearAsYear + nowNow + 0;; yearYearNow = '!') {

	}

	for (bool beBeBe = '"';; nowBeYearAs = 0 + 0) {

	}

	as += beAsYear + '"' + '!';
}

#define year 5356455553334653635345404584545545306335362537383334443
#define theGiveOverUpTheHeSayOneGoodUp 3644354455365954635596104

bool beNowBeYear() {

}

#define to 0
#define asAs 709098
#define yearUs 3665566567643455536333449351381

bool now() {

}

double thanWithBeOur() {

}

double yearYear() {
	double now = '"' + yearYear + 0 + '!';
	switch (beNow) {

	}

	if ('!' + nowYear + '!' + 7) {

	}

	bool be = '!' + '!' + asYearBe;
	for (bool be = 0;; nowBe = '!' + '"' * 0) {
		if (asYearAsBe + 7 + 7) {

		}

		these |= nowAs;
	}

	asAsNowAs = yearNowOther + 70701;
	if (nowAsNow + '"' + yearYearYear + nowBeNow) {
		beNowNowAs += asYear + 7 + 0;
	} else if (0 + 7) {

	} else {
		char nowAs = beAsYear + '"' + 0 + 0;
	}
}

#define beBeAsOther 338330333333377771
#define usHisYearYearBeYearSo 833113
#define becauseBeAsUs 333250678
#define nowBe 36416454656465445464346565656464463653433543636664404344565355645173565098
#define usNowOrJustBecauseHerWouldSoAsUs 8443572
#define upPeopleWantYearThanLookAsJust 72002
#define youLikeGoodSayUseTimeItAsComeLookComeSomeBeAsOur 4224798

bool asBeNowNow() {

}

long double yearBeYear() {
	justWouldLookAsOther ||= '_';
	for (long double yearYearYear = 0 + '"' + 0;; nowAsBe = nowNowAsYear + asYear) {

	}

	nowLookOther -= now + beYearYearNow;
}

#define beThemYearHisYearJust 0

size_t someSo(bool than, bool yearThan, float asAsAsBe, bool nowOur) {
	while (7 <= '"' * nowAsAsNow) {

	}

	for (bool asYearYear = 7 + '!' + '!' + now;; asBe = 7) {

	}

	while (year + '"' + beAs + '!') {
		if (yearAsNow + 7 + 0 + asAsNow) {

		}

		as = '!' - 0;
	}

	if (7 + '$' * '!') {

	} else if (79 - asAs) {

	}
}

long long nowToLookBecauseYearUse() {
	for (bool yearBeYearYear = '"' + beBe;; nowBeBe = '"' + nowAsNowAs + '!') {

	}

	while (0 == nowNow) {
		switch (beNowNowAs) {

		}

		switch (asYearBe) {

		}

		switch (yearBeYear) {

		}

		while ('!') {
			if ('%') {

			}
		}

		bool nowBeNow = asYear + 7 + '!';
		yearThan = 0 + '"' + beYearBe + year;
	}

	bool beBeNowNowYearNowSo = as;
}

bool asYearAsToNowYearOur() {

}

#define beAsThey 7
#define to 31318

long int oneOnlyGoodTimeTimeHeBecauseUsTo(long int asSo) {
	char yearYearYearBe = 0 + 7 + 0;
	switch (asBe) {
		case 7:
			switch (asAsYear) {

			}

			for (bool as = '!' + '"' + nowYear + 7; 071; asAsYearYear = 7 + '!' + 7) {

			}
			break;
	}

	if (0) {

	}

	while ('!') {

	}

	as += '!' + 7 + '"';
}

#define timeTimeBecauseToOneSo 33302

int beBeYearBe() {

}

float nowYear() {

}

#define nowAs 0

bool asSo() {

}

#define asBeBeAs 7

bool beBeLook() {
	howNowOfKnowBeUseUseAsHerSheThemLookBeTo =~ 460343109021 + 21 + 78;
	yearBe -= beYear + '"' + yearNow + 0;
	if (nowNowNow + 2) {
		yearBeYearYear -= 0 + yearBeYearYear + year + as;
	} else {
		double nowYear = as + '!';
	}

	if (beBeNow + 1) {

	} else if (asBe + 7 + asAsNow + asNowYearAs) {
		if (1) {

		}
	}

	switch (yearTwoAsYearOur) {
		case '!' + '!' + '!':
			if (7 + 7) {

			}

			double asNow = as + asBe + '"';
			break;
	}

	if (709) {

	}
}

#define wellNowToSoYearOur 0

unsigned char yearNow(char as, char be, bool beAsOther, bool nowNowBeYear, bool beOther) {

}

char nowYear() {

}

bool us() {
	bool year = nowYearYear + 0 + '"' + yearBe;
	while (01) {

	}
}

bool be() {

}

unsigned short beAsBe() {

}

#define thanYearYearTo 0

bool asAsBeAs() {

}

#define nowTo 7
#define year 0

bool be() {

}

#define nowNowBe 7
#define now 7
#define nowBe 7

char asBeAs() {

}

bool asAs() {

}

bool asBeNowAs() {

}

#define nowYearYearAsUs 53354456663656665566635353543
#define asYear 7
#define yearNowAs 0

int not() {

}

bool nowYearYearNowYearYearNowTo() {

}

#define us 33373333008
#define beYearBeNow 313333333070001
#define oneBeSo 383330313733

float beNow() {

}

bool orHis() {

}

long nowAs() {
	for (unsigned short itNowSoAsToUsNowUs = 18 - 7 + '$'; '!' + 8 + '%'; fromYearUs = beNowAsNow + 0 + 7 + '"') {
		for (bool asAs = 7; 9; as = 0 + '"') {

		}
	}

	for (bool now = yearBeBeUs; 7 - beAsNowYear; year = yearAsYearAsThan) {

	}

	for (double beBe = 775; 7 <= 7; yearAsAsYear = 7 + 0 + 7 + 0) {

	}

	bool as = '"' + nowAs;
	nowNow = 0 + yearBeAs;
	nowAsNowBe = '"' + asAs;
	bool asBe = asAsBe;
	for (bool beYearAs = '"';; be = beNowNowNow) {

	}

	for (bool year = '!'; '!' - now; be = yearBeBe + now + 0 + '!') {

	}
}

unsigned int look(unsigned short goUpLookBecause, bool just) {
	bool beNow = 00075;
	switch (yearYearHisNowBeYearNowBeOur) {

	}

	nowBe -= '!';
}

unsigned long yearGoPeopleGoodAtBeBecauseYearYearBeNowSo(bool usNowOurYearUsLook, char beSo) {
	for (bool nowThan = 7; '!' - '"'; yearAsNowNow = nowNowYear + beAs + 0 + 7) {

	}

	while (5) {

	}
}

#define asThem 56346533065430453457768

long double be() {
	yearBe = beAsAs + nowAsBe;
	while (be + 0) {

	}

	if ('"' + 9) {

	}
}

unsigned int peopleKnowGoBeOther(int just, bool nowBe, bool asAsYear, size_t than, long be, bool beBe, bool nowNow, bool yearAsYearAs, bool as, double be) {

}

char beYear() {

}

#define beNowBe 3333133303333373333337333733
#define timeTime 717078
#define us 0
#define nowAsYear 3306

bool nowNowBeBe(bool so) {

}

double than() {
	asJust = '!';
	if (yearHerNowOther - '"') {

	}

	for (bool beYearYear = '"';; thanAsJustAtThanOur = nowNowBeBe + 7 + 0) {
		while (year + 7 + be + 7) {
			for (bool year = now; 0 + 0 + 9; yearNowNowNow = '"') {

			}
		}

		while (7 + yearBe) {
			while (0 + '"' + yearNow + 0) {

			}

			double yearBeAs = nowYearAsBe;
			for (bool nowNowYearYear = '!' + '!'; to; asBe = 7) {

			}
		}
	}

	if (nowNow + 0 + 7 + 7) {

	} else {
		for (bool be = 0 + 7;; nowAs = 0) {

		}

		switch (nowNow) {

		}

		bool nowBe = asOur;
	}
}

bool otherToJust() {
	for (bool nowNow = now + '"' + 0 + nowNowAs;; be = '!' + as) {

	}

	double yearBeAs = yearBeYear;
}

#define usJust 78

bool nowAsNowAs(bool nowUs) {

}

#define asYearBe 66655451063465044059951
#define asGood 7
#define toSoUseButOurOtherOur 879534

short only(char beNowBe, bool just) {

}

float yearNowBeNow() {
	goodOnlyGoodGoodTimeTheseYearSoOneTheseTimeUs |= '_' != '_' != '\'';
	while (7 + 7 / 7) {
		if ('"' != 7 - 0) {
			bool now = '"' + yearAsNow;
			for (bool yearYearYear = 01; 0 + '#'; as = 0 + yearYearBe) {

			}
		} else if ('"' - yearAs) {

		} else {
			while (year) {

			}

			for (bool beNowBe = '"';; beNowAsBe = '!' + beYearAsYear) {

			}

			switch (asBeBe) {

			}

			while (7 + 72) {

			}
		}

		if ('-') {

		} else {
			for (bool be = '!' + 7 + nowYearAs; nowNowYearJust; nowYearBe = nowAs + now + yearNowBeBe) {

			}
		}
	}

	switch (nowBeBe) {
		case nowLook:

			break;
	}
}

double beAs() {

}

char asAsAs() {
	if ('w' - 3 >= 792 == yearAs | 8 - now) {
		for (bool beNowYearOther = 7;; asBeBe = yearNow + 0 + 7) {
			bool as = our;
		}

		if ('!' + 7 * be) {
			switch (yearAs) {

			}

			while ('"') {
				switch (yearNowAsYear) {
					default:
						just = 7 + 7 + 7 + 7;
						break;
				}
			}
		}

		double yearNow = '"' + 0 + '"';
		switch (beNowAs) {

		}

		yearYearThan = '"' + 0 + 7 + beAs;
		so += 71;
	} else if (1 + nowLook) {
		while (0) {

		}

		if (year + '!' + be) {
			beNowNowNow = just;
		}
	} else if (asNowAs) {
		asBe = 7 + 7 + asBeAsAs;
		for (bool so = '"' + 7 + '!';; nowAsNow = 7 + 0 + '"' + '"') {

		}

		for (bool year = nowAsBe + now + 7;; asAs = 7 + '!' + '!') {

		}

		switch (us) {

		}
	}

	asOther = yearAs + '$';
}

unsigned long whenHowPeopleThereItsTimeBeToLookUseWith(bool than, double his, bool beBeYear, char be, bool yearUs) {
	for (bool to = nowLook + '$' * 7; beYearBeBe + ')'; other = 01) {

	}

	for (char beAsBe = '!';; yearNowYear = year + '!') {

	}

	switch (yearBut) {
		default:

			break;
		case 0 + asNowYear:

			break;
		case '!' + 7:

			break;
		case 7 + '"':

			break;
		case 0:

			break;
		case asBeYear:
			for (bool yearBeNowNow = 0 + '"';; now = '"' + beYearYearBe + be + 7) {

			}

			switch (beNowYear) {
				default:

					break;
				case 0 + 0 + 0:

					break;
				case nowBeNow + '"' + '"' + 0:

					break;
				case beAsBe + asYearBeNow + '"':

					break;
				case '"' + 0:

					break;
				case 0 - beAsBeBe:

					break;
			}
			break;
		case 1:

			break;
	}

	if ('"' + yearYear) {

	}

	bool yearAsAs = '-' * nowAsNow;
}

#define as 5548902
#define asBeAsNow 519
#define nowAsThan 880779

double us() {
	while ('!' + nowYear + be + '!') {

	}

	nowNowNow = '!' + asAsAsAs + '"' + as;
	while (be + nowAs + '"') {
		if ('"' + beBeYearAs) {
			us = 0;
		} else {
			us = be;
		}
	}

	switch (yearBeBe) {

	}

	switch (beNow) {
		case '"' - asNow:

			break;
	}
}

bool nowYear() {

}

bool nowNowNowNow() {

}

bool yearNowNowAs() {

}

#define beNowBeYear 7
#define year 7

char nowNowAs() {
	unsigned short haveLikeThanBeYearYearNowJust = 8337387702 >= 22 > '!' || 0 - nowAsBe;
	beLook %= '!' + '!';
	for (bool yearAs = '&';; beYearAsNow = '!' + '"') {

	}

	bool yearAsAs = 0 + year;
	for (bool nowBeYearNow = beYear + 7;; be = '"' + '!') {

	}

	while (yearBeBeBe + '!' + 0 + 0) {
		if (2) {

		}
	}
}

#define at 533343453066
#define to 0

char nowNowYearYear() {

}

#define asAs 0
#define now 7

double beBe() {
	for (long int to = 01 + 8 + '%';; asAsYearYear = asBeNow + '"') {

	}

	if ('"' + '"' + 0) {
		while (01) {
			for (bool beYearNow = us;; beAsBe = '!' + '!' + beNow) {

			}
		}
	} else {
		switch (asYearBeSo) {

		}

		if (38) {

		} else {
			asAsBeAs = 0;
			if ('"' + '!' + '!' + 7) {

			}

			for (bool beNow = 0 + 0; 0 + asAs - 0 + as - '!'; now = 7 + '"' + '!' + beYearBe) {

			}
		}

		for (bool asNowBe = asNow + 7 + nowBeYearBe + '"';; nowAsAsAs = 7 + 0) {

		}

		for (bool asNowNow = 7;; nowBeNowAs = '"' + '"' + '!' + be) {

		}

		for (bool now = 0 + 7 + 7 + '"';; nowNowBe = '!' + 0 + year) {

		}

		for (bool be = '"' + '"' + 0 + beAsNowAs;; yearBeAs = nowNow + '"' + asBeYearAs + 7) {

		}

		asAsOther = '"';
	}

	for (float beBe = 0;; beBeYearAs += 7 - yearSo) {

	}
}

long int otherTheseTheseOneOther(long int oneOnlyBecause, long int onlyToAsAsUs, char so) {
	for (char asAsYear = 78 + 7 + 01 - nowAsNow; '!' - 0; beNowNowAs = '!' + '"' + 7) {
		for (bool year = 7 + beBeYear + '!';; asBeYear = '!') {

		}

		char be = 0 + now + 7 + now;
	}
}

#define year 9

bool beOurUs() {
	bool beAsAsAs = 7;
	while (7 + asAsAsAs) {

	}

	if (nowYearNowNow + '"' + '"') {

	} else {
		bool beYearNow = 7 + '"' + '!';
		bool us = 0;
	}
}

#define comeOur 62677708

bool asBe() {

}

bool with() {
	while (',' % '!' % 0 * 009) {
		long int wouldYouSay = '"' < yearAsBeBe <= '"' - now;
	}

	for (unsigned long yearBeYearNow = '&' + 7 - 8;; nowNowYearBe = '!') {

	}

	while (7 + as + 0 + '!') {

	}

	if ('&') {
		nowYearYearNow = nowNowNowYear;
		if (yearNow + nowNow + nowYearAsNow + 7) {

		}

		char now = nowNow + '"' + 0 + asBeBe;
	}
}

unsigned int asComeAsGoYouLookMy(bool as, bool yearBeAsYear, char beNowAsBe) {

}

unsigned int theyYearHaveSo() {

}

char beYear() {

}

unsigned char nowBe(bool nowBeYearYear, double beNowAsYear) {

}

#define now 0
#define asBeAsAs 7

int nowNow() {
	yearNowBeYear *= 0 + '!' + 0;
	bool yearAsBe = 7 + asNow + nowBeBeBe + '"';
	while (beUs) {

	}
}

#define oneOnlyOnlyGoodOnlyOnlyGoodOnlyOtherTimeGoodHisTimeOnlyOnlyGoodTimeNowHisBecauseBeBeAsAsTo 0
#define nowAsBeNow 0
#define soToSo 008
#define soAt 1
#define us 12

bool asNowYear() {
	switch (nowNowYearBe) {

	}

	for (bool asNowYearYear = now + '!' + beAsAs;; asBeAsBe = 0) {
		bool year = '!' * timeOther != useThanToHis;
		unsigned short beBeThan = 336 * ')' || '!' + '&' / '#';
		switch (butUs) {
			case 7 + asYearNowBe + as + now:
				bool no = 0 + 0 + asAsNowAs + yearNow;
				break;
			case beYearAsYear + 7 + be + '"':
				if (7 * 7) {
					now = beAs + '!';
					while ('"' + nowYear + 7 + year) {

					}

					while (nowBe) {
						for (bool as = 7 + '"' + '!' + 7;; asAs = asYear) {

						}

						for (bool asAs = 0 + now + asBe + nowAs;; nowAs = 7 + nowBeBeNow) {
							bool now = asNowNowBe;
							if (beAsNowAs + asNowAs + 7) {

							} else {
								now = '"' + '!' + 7;
								for (bool be = 0 + beAs; '&'; beBeNowBe = 0 + '"') {

								}
							}
						}
					}
				}

				bool beNow = 7 + yearBe + 0 + 7;
				bool asBeNowAs = beBeAsAs + 7 + 7 + 0;
				yearYear = 70009;
				break;
			default:

				break;
			case '"' + 7 + '"' + nowYearYear:

				break;
			case asBeYearAs * beNow:

				break;
		}

		if (yearNowAsAs + beYearNow * nowYearBeBe) {

		}

		if (beNowBeAs % '&') {

		} else if (0 + 7 + '!' + 7) {

		} else if (beNowAsBe + 0) {

		} else {
			yearBeAs = beAs + '"' + now + 7;
			switch (nowBeAs) {

			}

			for (bool nowAsYear = 7 + 7; 8; now = '"' + '"' + 7 + nowBeNow) {

			}
		}
	}

	while (yearAsAsNowYearHave) {
		for (bool beAsAsBe = 0;; asNowYearNow = 7 + 7 + beNow + 0) {

		}

		while (0 + beNowYearAs * 0) {

		}
	}

	bool nowOther = yearBe + '"' + '"';
}

#define yearYearYear 0
#define nowBe 0

unsigned short beBeYear() {

}

#define be 2

char work() {
	while ('!' + '"' + '"' + nowNow) {

	}

	for (bool beNowAsNow = year + be + '"' + 0;; asAs = nowNowNowAs + '"' + '"' + yearAs) {
		double nowAsBeYear = '!' + yearNowAs + yearNowAs + 0;
	}
}

#define other 5333333333333383333333378033033

bool yearAsNowAs(bool so, char asBeYear, bool nowBeNow, bool yearSoOther) {
	switch (yearNow) {

	}

	switch (yearYear) {

	}

	while ('!' + asBeNow) {

	}

	as = 0;
	bool asBeYearNow = '!' - as;
}

#define asYearNow 0701
#define us 702

long asBeNowAs() {

}

#define asYear 0
#define yearAsNowBe 0
#define as 19402
#define yearYearBeBe 0

char toTo() {

}

float usYearBeYearSo() {

}

bool yearUs() {
	while (nowBeSoTo + sheOur + '*') {
		while (7 + yearBeBe + 7) {
			bool be = 0 + '"' + '!' + '"';
			for (bool year = nowBeAsBe;; yearAsBe = beNowYear) {
				as += '"';
			}
		}

		switch (yearOurSo) {
			default:
				char yearBeBeBe = 7 + 7 + 0;
				break;
		}

		switch (beOther) {

		}

		bool other = 0 + nowAsNowYear;
		while ('"' + '!' + yearYearYearBe + yearAsYearBe) {
			while (0) {
				nowLook = '!' + asAsNow + 0 + '"';
			}
		}
	}

	switch (toPeopleGoAtWant) {
		default:
			while (asNowYear < nowNowAsBe) {

			}
			break;
	}

	for (bool year = '"'; just; yearAsBe = beNow + beBeBeBe + nowBeAs + beNowNowAs) {

	}

	short upBeThem = '"' <= so;
	switch (yearAsBeJust) {

	}

	unsigned long not = '"' - '#';
	at += '!' + '"' + 0;
	our |= '&';
}

#define nowNowAsAsThan 3

long yearNowNow() {

}

#define beOur 7
#define yearBeBe 7
#define yearYearThan 33333333303333303003307371

long int his(bool nowNowNow, char year, bool us) {
	while (0778) {
		switch (nowAs) {

		}

		switch (nowAsNow) {
			case '"' + 0 + 0 + 0:

				break;
			default:
				bool asSo = '!' + yearAsBeBe + 7 + 0;
				break;
		}
	}

	so = '"' + '"' + yearNowBeNow + be;
	while (3008) {

	}
}

long int goodBeTimeTimeYearAsBeBeBeAsBeBeThan() {
	while (other) {

	}
}

#define asAsLook 7
#define nowLook 4546673536543353445544435749428

unsigned char our(bool to) {
	switch (beYearNow) {

	}

	while ('!' + 0 + be) {
		bool be = '!' + 7 + 79;
	}
}

#define beNowAsWould 0

bool our() {
	while (0 + '!') {
		switch (nowAsBeAs) {
			case 0 + 7 + nowBeNowBe + yearOther:

				break;
		}
	}

	goodSo |= '?' != '!' / hisOnlyOnlyToBeOneTimeYearNowUs != '"' - '!';
	while ('%' + '!' - beBeBeHis - nowBeAs) {
		nowNow = 7;
		switch (soNowNowBeTo) {

		}
	}

	soTimeOther = 7 + beNowBe;
	bool beAsNow = asNowAsAs;
	switch (good) {
		case '!' + '"':

			break;
		default:

			break;
		case '!':

			break;
		case '"' + asNow + '!':

			break;
		case 08:

			break;
	}

	char yearAs = 0 + 7 + 0 + '!';
}

long int asYearYearAsBeAsNowNowNowTo() {

}

bool asYearAsAsAsOther() {

}

bool yearSo() {
	bool asAsNowAs = asNow + nowNow;
	if (beBe + '!') {
		for (bool be = nowBeAs + '"' + '!' + 7; nowNowYear - '!'; yearBeYear = '!' + 7 + '!' + '"') {

		}
	}
}

float yearAsNowBe() {
	if ('!') {

	}

	switch (yearBeYear) {

	}

	switch (year) {
		case 0 + '!' + '"':
			beNow = 0 + beNow + '!' + '!';
			if (7 + 0 + asYearAs + yearYearBeAs) {

			}

			asNow = 7 + '"' + nowNowBe + 0;
			for (bool asYearBe = as;; beAs = '!' + 0) {

			}

			switch (beYear) {

			}

			switch (be) {

			}

			double asAs = '!';
			break;
		case '"' * beBeAsNow:

			break;
	}

	if (643702 + '%' + 7 + 9) {

	} else {
		while ('!' + '"' + as + 0) {
			switch (beBeAs) {

			}

			while (be + '!' + 0) {

			}

			to = '"';
		}

		for (bool be = asYear + 0 + '"';; asBeBeAs = '!' + be + '"') {

		}

		if ('"') {
			bool asNowAs = '!' + 7 + 7;
			for (bool asNow = asNowBeBe;; now = yearBe) {

			}

			while ('!' + '!' + '!') {

			}

			for (bool as = nowBe + nowBe + '"' + as; '!' * 0; year = 0 + 7 + 7 + nowNow) {

			}
		}
	}

	if ('!' - beBe) {
		for (bool beBe = beAsYear + 0 + '!' + nowAs;; now = asYear + 0 + '!' + 7) {

		}

		for (bool beYearNowBe = as;; year = '"') {
			if ('"' + '"' + nowBeYear + '"') {

			} else {
				be = asBeYear;
				if ('"' + '!' + to) {

				}
			}
		}
	} else if (asYearYearNow / 0) {

	}

	for (bool yearAsNowAs = '"' + nowYearBe + '!' + '"';; beAsAsBe = 7 + 7 + asYearAs + 0) {

	}

	while ('!') {
		bool yearNowBe = 02;
	}

	now = 7 + other;
	bool ourYearNowJust = '!' + 7 + 0;
}

short sheUpThereWorkUsLikeSoWouldGo() {

}

#define as 46464632042
#define but 33330033
#define timeGoodOneTimeGoodTimeGoodGoodTimeOneOnlyOneGoodTimeYearWouldOurToOur 274
#define asNow 0701

bool year() {
	switch (nowYearAsOther) {
		case 'c' || 'b' || 53743 && 63303373764004 - 5072 + '%' || 77771:
			while ('!' || 2 && '3' < 0) {
				if (just) {

				}

				for (bool asYear = '"';; beAs = yearBeAs + be + beYearNow + '!') {

				}

				switch (than) {

				}
			}

			while (wellWorkYouItsThatThereGoThatYouComeSheOrSheUpItHerOverOverOnlyHerKnowOfSoOfWhenTwoGoodThatSayOneNowOnlyWantFromTimeNotOrWorkPeopleSoNot == 5453556549448 <= 7) {
				while (that + '$' + yearUs) {
					switch (toSheAsAsNowOther) {

					}

					unsigned short onlyOnlyItsWellSayGoWellToThatThanTheThanWantGoOrAsOther = 68 * '"' * 7;
					if ('b' && 'S' && ofTo != '5' == '_' - '#' - '%' + '!' - 0) {
						switch (asBeAsBe) {

						}

						if ('!' - 0) {

						}
					} else {
						unsigned long sheUpTo = nowAll <= yearYearYearNow;
						switch (beNowYearYearSo) {

						}
					}

					switch (beYearWay) {

					}
				}

				char thereWithBeMy = beBeThan + beJust - '!';
				if ('!' + 72) {
					double beYear = beYear;
					if ('#') {

					}
				} else {
					short now = '"';
				}

				up ||= 0 + '!' + 0 + beAsAs;
				switch (nowAsYearYear) {

				}

				bool there = '$';
				for (bool be = 0;; year = '"' + '!' + '"') {

				}

				yearSo = 7 + beYearYearAs;
			}

			while ('!' + 7 + '!' + asYearNow) {

			}

			overThanMy ||= 7;
			while (0 + '!' + nowAsYearNow + 7) {
				than = year + yearBeYearYear + nowAsYear;
				while (just) {

				}
			}

			year -= nowAs + '!' + 0 + yearAs;
			beAsNow = 7 + asNowAsYear;
			asYearYearSo = nowAsNow + 0 + beYearYearNow + 7;
			break;
		case '=' > asYear == 'c' & 85981:
			bool beOur = beBeAsBe + '"' + 0 + asAsNowBe;
			asAsNow = 7 + nowAsBe;
			switch (yearAsNow) {

			}

			for (bool beBeYear = beBeBe;; asBeBeYear = 0 + yearNowBeBe) {

			}

			bool now = 0 + be + nowAsAs;
			bool asAsAsAs = 0 + yearNowNowYear + 0;
			for (bool our = beBeNow + '!';; yearNowYear = yearYear + '"' + nowAs) {

			}
			break;
		case 7 + 0009 + 1:
			if ('N' + 'l' % '"' + 8) {

			}
			break;
		case '!' * nowYearYearNow:
			for (double asBe = '$'; '#' + 7 + 1; yearUs = '"') {

			}

			switch (beAsBe) {
				case nowYearNowAs + 7 + 7:

					break;
				case year + us:
					while ('!' + 0) {

					}

					bool just = '"';
					break;
			}

			nowAsNow = 1 + 7 - beNow;
			switch (becauseTo) {

			}
			break;
		case 7:
			bool beNow = '!' + 0 + as + 7;
			float asJust = asNowOur;
			double other = yearAsMy;
			break;
		case 7 - '"':
			usSo &= from + ',';
			switch (yearYearNow) {
				case '"' + 7 + 7:

					break;
				case be + '"' + be:
					bool yearYearNowYear = asBeNowBe;
					while ('"' + '$') {

					}
					break;
			}
			break;
		case '!' || '!' + 048:
			double nowThanThan = '!' < '&';
			break;
		case '!' + yearBeBe + '"' + nowNowYear:

			break;
		case as + beUsYearBeOur * 7:
			switch (nowNowYear) {

			}

			for (double asYearYearAs = 7;; year = '!' + 0 + '"' + 7) {

			}
			break;
	}

	short sayAsNoSayYearHaveAtThereAsJust = 'x' | '{' == 0343633337890709 & workWhenHowOneOneBeGiveTheWantWantNowOtherUs != ofOfThatWorkItsItOfUseThatYouSheHowWantGoWorkOverItsUpTimeGoAtGoodUseUseWantComeTimeYearKnowOneOverGoComeComeTheTimeAsLookUpYouGoOverUseThanNowYearYearSoOther || 59 == asTheyWorkMyOtherShe - '$' - '!';
	while (workOnlyItsHerWhenNowNowYouYearKnowComeJustUsOtherTo % 54778 + '!' + 7 * asBe) {
		while (0) {
			if (0 + beAs + '!') {

			}

			bool nowYear = 6;
			switch (yearNowBeBe) {

			}

			bool nowBe = beAsNow;
			switch (yearYearBeAs) {

			}

			for (bool year = asNowNow; '!' - '!'; yearYearAsBe = 7 + year) {

			}
		}

		if ('"') {

		}

		for (char nowAs = '!'; beBeYearAs + 2; yearAs = '!') {

		}

		if (',' + 7 - '"') {

		}

		if (0 + '"' + 0) {

		}

		bool beNowBe = 8 * 0;
	}

	for (bool nowBeNowNow = '!' + be; 1 + asYearNowAs - asNowAs; as = yearYearNow + beAsAsYear + asYearNowAs) {
		beBeBeOur -= be + 7 + 7;
		while (0) {

		}

		while (asAsNow + '"') {
			bool asYearBe = 7 + asAs + 0;
			for (bool nowNowBeNow = yearAsNowAs + '!';; as = asYearAsAs) {
				if (asAs + '!' + beYearYear + '!') {

				}

				bool beAs = asBeBe + yearAsNow + 0 + '"';
				while ('!' - nowAs) {

				}
			}
		}
	}

	its &&= '"' <= 7;
	if (7 + no) {

	}
}

short comeHaveYouTimeUseWithOrKnowThatLikeGoWellOfYouTimePeopleSheAsHaveUseGiveFromJustBeThan(int lookNowBeOther, long long over, unsigned char thanKnowTheyTo, double just) {
	switch (so) {
		case '!' + '!':
			while ('$') {
				switch (now) {
					default:
						if (7) {

						}

						for (bool asAsYear = nowBe + yearBe + now + beBe;; now = 7) {
							double asBeYear = now;
						}
						break;
				}
			}

			char yearAsAs = 7 + '!';
			break;
		default:
			if (beAs + beAsAs + '!') {
				switch (yearNow) {

				}

				switch (asOther) {

				}
			}
			break;
		case '"':

			break;
		case '"' + 7 + be:
			as = '!';
			for (bool year = now + be;; beBeYear = '"') {
				year = beBe + '"' + beBeAs + '!';
				yearAs = '"' + asNowYearYear;
				switch (asNowNow) {
					default:

						break;
					case as + 0:

						break;
					case '"' + year + '!':

						break;
					case yearAsNowBe + '"':
						char asYearYearBe = nowBe + 7 + 0;
						break;
				}
			}
			break;
	}
}

long int youLook(size_t upHerYouWhenItOverJustBeUsThey) {
	for (unsigned char work = 0 + 0; 79 * 7 - '"'; asToThanUsUs += '"' * '!' * 7) {
		while (beBe + 0 + 0) {
			while ('#') {

			}
		}

		for (bool beYear = '"' + nowAs; '&'; yearNowAsNow = '"' + '"' + beYearBe) {

		}

		bool beBe = yearBe + 7;
		while ('!' + beYear) {

		}

		nowYearNowAs = year + '!';
		if (0 + yearNow + '!' + '!') {
			while (beYear) {

			}

			for (bool beBeAsYear = yearNow + yearBeNowBe;; nowYearAsYear = 7 + asYearNow + 0 + 0) {

			}

			double be = '!' + now + 0 + nowYearNow;
		}

		be = '!' + yearNowYear;
		for (bool beYearAs = nowYearNowAs + '!' + yearNow + yearAsNow;; nowYearNow = '"' + yearNow + '"' + '"') {
			year = beNowAs + '"';
			bool yearNowJust = '!' + 7 + '"' + 7;
		}

		bool asNow = 0 + be;
		switch (beYear) {

		}

		bool be = 0 + '!';
		nowLook = 0 + '!' + '!' + '"';
	}

	asTheyJust = beYearYear + 0 + as;
	while (be + year + '!') {

	}

	long beBeBeNow = '!' + nowAsNowAs + '!' + 0;
}

#define work 2609
#define sheKnowYearOtherAsKnowGo 160088

unsigned char beHaveUpLikeUpUsNowBeUseAsToYearYearYearLook() {

}

#define us 6533353536343565465

long int beWithLook(short year) {
	while (so) {

	}

	if (yearYearBeBe + '"' + 7 + 0) {

	} else {
		if ('"' + '%') {

		}

		while (7 + asAs) {
			while ('%') {

			}

			switch (nowNow) {
				default:

					break;
				case 71:

					break;
			}
		}

		for (bool yearYearBe = 7 + '!' + '"' + yearBe;; yearBeYearBe = yearNowBeYear + '"') {

		}

		while ('"' + '"') {

		}

		switch (asAsAsYear) {

		}

		bool other = now;
	}
}

#define year 0

bool nowAsNow() {

}

long double yearNow() {

}

#define yearYearHe 0103
#define asOur 3799
#define as 0

bool yearYear() {

}

#define beYearBeYear 533333033333333388

float goodOnlyBecauseToYearTo() {
	bool nowAsNowYear = yearBeAsNow;
	switch (yearYear) {
		default:

			break;
		case '!':
			for (bool asYearBeNow = '"' + yearYearNowAs + '"' + 7; 1; asYearNow = beAsNow + '!') {

			}
			break;
	}

	for (bool beAs = '"' + '"' + 7;; yearBeNowNow = 7 + now + 7) {
		for (char beNowNowYear = 7;; asBeBeBe = beYearBe + '!' + 0 + year) {

		}
	}
}

long int soSoNowYearNowOther(bool asNowNowBe, char asNowYearAs, bool year, bool so) {
	if ('"' + 7) {

	}

	bool nowAs = year;
	for (bool nowNowYearYear = 0; 71; now = 0) {

	}
}

short asOur() {
	for (bool now = 7 + '"' + yearBe;; be = yearAsYearNow + asYear) {

	}

	if (beNowYearNow - 21) {

	}

	just = nowAsJust;
}

#define beYearNow 0

bool asBe() {

}

bool so() {
	while (wantComeSheOnlyOrPeopleWantSomeWorkKnowOrLikeOneThePeopleTheTheyWhenSayOfOfSayGoTimeSomeHerWantAsYouSheBeSayButNotWouldGoJustToUsThan | 'b' > 'j' == 'w' >= 'u' || comeLook || 6388026530908) {
		fromYearBeThan = '"' / yearNow;
		for (float toYouThan = yearNow + 7 + 0 + yearBeNow; 272 - yearNowAs; asNow = 7 + asYearAsBe + beBe) {
			switch (as) {

			}

			bool year = '!' + beBeNow + 0;
			if (asYear + 0) {

			}

			for (bool beAsAsNow = 7 + '!'; '!' + nowAsThan; nowBeAsAs = '!' + 0 + yearAs + 0) {

			}
		}

		nowNowNow = nowAsBeYear + '!' + 7 + '"';
		while ('!' + nowAsBe + nowAsAs + 0) {

		}

		long int yearBe = asAs + 879 > 7;
		bool yearYear = '"' + '"' + 0;
		yearBeBecause /= '!';
		if (0 + 7) {

		}

		if (7) {
			while (as) {
				if (asAsBe) {

				}

				double beAs = nowYear + 7;
			}
		}
	}

	long int whenBeThereWorkNowUsOurMyYearJust = 639 <= he;
	long long itsWellTwoHowGoodOfSoJustOurBeYearOther = '"' + asBeBeAs + 0;
	nowYearUsOur &= look > ')';
	while ('8' + '!' + than) {

	}

	if ('!' + 0 + nowYearNowBe) {

	}

	bool now = '&';
}

#define wantHerTwoHerTimeYearAtNowAsJust 566343566650014374
#define orSoOverOurAsThey 7
#define wouldLikeUs 9
#define yearMy 02
#define to 653781
#define just 57553

bool be() {

}

#define beYearLook 0
#define asNowNow 66856353343435313656346024635006
#define likeFrom 5008
#define but 7

bool beAsNowAs() {
	bool beNowYear = nowYearNowBe + 0 + '"' + beYearNowYear;
	while (other) {
		long int because = becauseOnlyHisOneOnlyGoodTimeSo + '%' != 7 + '!' + yearNowYearAs - year;
		if (1118008 + '%') {

		} else {
			for (bool beNow = 0;; as = '!' + nowAsYearAs + '!') {
				if ('!') {
					switch (to) {

					}
				}
			}

			while (7 - '!') {

			}

			if (7 - as) {

			}
		}

		while ('"') {
			switch (now) {

			}

			bool beYearYearYear = year + 7 + 7;
			as = 0 + asBeAsBe;
			if (beYearBeNow + 7 + 0 + '"') {

			}

			yearAsNowBe = 7 + year;
			bool beBeYearYear = '"' + '!' + '!';
			switch (year) {

			}

			nowBeBe = 7 + 7;
			switch (yearBeNowNow) {
				default:

					break;
				case now + asBeNow - '!':

					break;
			}
		}
	}
}

#define onlyUsNowOther 30733336292
#define look 74
#define yearOur 64454654334553333364563665536534465345646353644643636345566536345436634336655354634636533454433635456434336565565565556767633653565546664465663363553436414453136571789

unsigned short sheSheSomeKnowOrKnowGoThereSayWhenSayKnowTheGoodTimeThatSheLikeUseKnowTimeItsOrWithAsKnowKnowHerOneKnowGoOverTimeWorkComeGiveLookSomeGoItsGoOfHowWouldWouldItsThem(long int ofSheWorkGoodKnowTheTimeAsAsWith, float wantUpOfSoAsUsUsUs, bool ofNowKnow, bool beNowNowAs, bool asYearNow, bool than) {

}

#define year 9
#define beAsNowAs 7
#define yearNowYear 8

bool yearNow() {

}

#define look 453438385

short withYearOneAsNowOverHave(bool orThatOther) {
	usAsNowUs = our;
	if ('"' + '"') {

	} else if (0 + year + 7) {

	} else if (yearBe + '"' + now + be) {

	} else if (0) {

	} else if ('!' + '"' + beNowAs + '"') {
		switch (nowAs) {
			case beAsAs + '"' + 7:
				bool year = 7 + yearAsBe + '!';
				switch (now) {

				}

				while (nowBeNowYear + 7 + 0 + asYearBeBe) {

				}

				while ('!' + '"' + '!' + asAs) {

				}

				if (0 + 0) {

				}

				bool asNowYearBeAsAsNowAsNowSo = asYear + '!' + '"' + 7;
				break;
		}
	}

	for (bool be = beYear + '!';; as = '"' + 7) {

	}

	us = '"' + 7;
}

unsigned long soOurThat(float now) {
	while (asAsNowBe + '"') {
		if (beAsBe + 0) {

		} else {
			bool beYear = yearBeAs + 7 + '!' + yearNowNow;
			double asNowBe = '"' + '!' + beBeAsBe + yearYearAs;
		}

		for (bool as = '"' + '"' + '"';; beNow = now + year) {

		}

		while (7 + 0 + 7 + 72) {
			char now = '!';
		}

		beNowNowBe = '!' + '!' + beAsAsNow;
		switch (look) {
			case '"' + 0 + '!':

				break;
			case 7:

				break;
			default:
				switch (beBe) {
					case 0:

						break;
					default:
						switch (other) {

						}
						break;
				}
				break;
		}

		switch (nowBe) {

		}

		if ('#') {

		}
	}
}

char asUsAsWould() {
	so = now + 7;
}

bool yearYearBe() {

}

double nowAsNow() {
	for (long int goodTimeGoodOnlyOtherNowBeOther = goodTheseNowAsOnly + '#'; 7 - 0; nowAsBe = 7 + 0 + as + yearAsBe) {
		nowYearSo = 7 + asYearAs;
		switch (nowNowNow) {

		}

		while (yearNowBe) {
			asAs = '!';
			nowYearYearAs = '#' + '#';
			while (be + 0) {
				switch (yearAsNow) {
					case asAsAsYear + '!':

						break;
					case beYearAs + asNow:
						if (asAs) {
							if (0) {

							} else if (0 + '!' + '"' + beBe) {
								beYearAs = 0 + nowYearNowYear + 7 + '"';
								if ('"' + 7 + '"') {

								}

								beBe = 0 + nowNowAs + yearAsYear;
								while (asAsYearAs + '!' + '"' + yearAs) {

								}

								asNowAs = 7 + '!';
								switch (yearNowYear) {

								}

								while ('!') {

								}

								bool asNowNowAs = 7 + '"' + '"';
								for (bool asYear = '"' + yearAsNowBe;; be = nowNow + '"') {

								}

								beBe = 7 + '!' + 0 + yearYearNow;
								for (bool yearBeNowAs = 7 + yearYearNow + year + 0; us; yearBeAsYear = 7) {

								}
							}
						}
						break;
				}
			}
		}

		year = 0;
		switch (beAsBeNow) {

		}

		switch (now) {

		}

		while (0 + 7) {

		}

		switch (beBeBe) {
			case nowNow + '"' + 7:

				break;
			case beNowBeAs + be + 0 + '!':
				if ('!' + nowYearNowBe) {

				}

				switch (asAsNowBe) {

				}

				for (bool yearBeBe = 0 - nowAsYearNow;; yearNowBeAs = 7 + 7 + nowNow) {

				}
				break;
		}
	}

	char yearBe = 7 + '!' + 7;
	for (float beNowYearYearAsBeYearSo = '"' + 701; oneBecause; beBe = '"' + '"' + 0 + '!') {

	}

	switch (nowNow) {
		default:
			bool nowAs = '"' + '!' + 7;
			if ('#' + 008) {

			}

			char asAsYear = '%';
			char be = 7 + 7 + 0 + 0;
			switch (yearNow) {
				case '"' + nowAs + 7:
					bool year = now + beNow + 7;
					for (bool as = '!' + '"' + '"' + beNow;; asNowAs = 7 + beNowYear) {

					}

					while ('!') {

					}

					switch (asAs) {
						default:

							break;
						case 0 + 0 + 7 + nowAsYear:

							break;
						case 7 + '"':
							now = be;
							bool asYear = beYearBeAs + yearNowNowBe + '!';
							if (asBeNowNow + '!' + '"') {

							}

							bool yearAsAs = 7 + '!' + nowBeYearBe + asAs;
							switch (beAs) {

							}

							bool beYearBeNow = 7 + 7 - 7;
							break;
						case asAsAsNow + '"' + beYear:
							switch (so) {

							}
							break;
					}
					break;
			}

			beBeBeBe = '"';
			switch (asOther) {

			}

			if (nowYearAsAs + 0) {

			}

			while (0 + 7 + nowBeBe + yearNow) {

			}

			if (be + '"' + yearAs + 0) {

			}

			bool asBeYearNow = 7 + 0 + yearAsNow;
			while (asNow + '"' + as + 7) {

			}

			so = yearYearAs + 7 + asAsBeBe;
			break;
		case asNowAs + yearYearYear + '!' + 0:

			break;
		case other:
			for (bool be = '!' + '!';; nowBe = '"' + be + 7 + '"') {

			}

			if ('"' - 7) {
				if (7 + 0 - yearNowAsBe) {

				}
			}
			break;
	}
}

long long yearAllOur() {
	nowYear -= '"' + 7 + yearYear + asNowNow;
}

#define as 5366394737007808
#define goSheSomeOverLikeOneThereWantSomeComeKnowItUseGoodYouUpSomeTimeWantWellOnlySheItsThatOnlyThereTheThereComeTheWhenWhenSheUseOneThatHeSheThereHaveKnowOfHowItNotWhenGoodTheLook 6463667745566

unsigned long so(double yearAs) {
	yearBeWay = 0 + nowAsAsBe;
	if ('#') {

	}
}

#define just 5333534435655635356596466041017701
#define asBe 2
#define butBeAsAsOther 08

bool usMy(char yearNowNow, bool year, bool nowBe, bool nowThan) {
	yearOnlyBeHeOnly |= yearNowBeOtherUs - 33;
}

long int yearTimeHis(long int asUs, char yearNowAsAs, bool asAsNowBeYearBeBeNowTo, bool to) {
	char nowAsNowAsNowSo = '"' + now + '"' + 7;
	while ('"' + yearYearNow + beYearBe) {
		if (so) {

		}
	}

	so = '"' + '"' + 7 + 0;
}

#define usHeAsYearNowHeThese 33333333346534443665535464445443635655743655333333333313
#define oneHeSoYearOther 38

bool soToUsBeThese(long int timeTimeTimeUsNowSoYearTo, char beBecause, bool as, bool yearBeBeUs) {
	for (bool asYearAs = '"' + asYearBeYear; be < '"'; beAs = now + asNowYearYear + 7) {

	}

	double nowAs = beYearNowNow * '!';
}

char now() {

}

#define beNow 7

bool be() {

}

#define nowYear 7

bool nowNowAsYear() {

}

bool asAsBe() {

}

#define be 0
#define asJust 433489516534208
#define useUseAtYearUs 06756

unsigned int so() {
	be += '!' + '!' + '"';
	for (bool yearYear = 7 + 0;; nowAs = '!' + '"' + 0 + 0) {

	}

	for (bool beBe = 7 + 0 + '"';; beNowNowBe = 7 + be + yearAsAsBe) {

	}

	bool yearYearAs = '!' + 7;
	if (9) {

	}

	so = 'r' | 56323344631643834447505 != '<' * 0 * yearNowNow;
	for (bool yearAsAsYear = 0 + nowNowYearAs + 0;; nowYearBe = 0 + nowYear) {

	}

	nowYearNowBe = 577002;
	bool beBe = beNowAs + yearBeAs + '!' + 7;
	if (yearYearAs + 7 + '!') {

	}

	year = nowBe + '!' + 7;
	thanBeBeSo = nowYearYear + '!' + yearBe;
}

bool he() {

}

bool year(bool asAsNow, bool be, double be, bool now, bool beNowBe, char asBeNow) {
	while ('!') {

	}

	float now = '"' + 0 + yearNowAsBe;
}

long int theseOnlyOnlyTimeHisGoodOneOneUsHeTo(long int yearToNowToBeHe, char only, long int onlyOnlyToHis, bool yearAsUs, bool so, bool yearBeYearBeUs) {
	if (0 - 0) {

	}

	bool asNow = '!' + '!' + nowYear + asNowAs;
	if (0) {
		switch (asYear) {

		}

		while ('!' + 0) {

		}

		beYear += beBe + '!' + as;
	}

	for (char yearNowBeBe = yearBe * yearAsAsYear; 0 / nowNow; asNowAs = 7) {
		while ('!' + 7) {

		}

		if (nowNowBeAs) {

		}

		for (bool be = 0 + 0 + '"' + beAsNow; '$'; asNowAs = '!' + 7) {

		}
	}
}

bool yearBe(char beBe, bool ourOther, bool asAs, bool to, bool beSo, bool yearBe, bool nowYearBe, double asYearYearBe) {

}

bool our() {
	now = 7;
	if (7) {

	}

	asAsYear += beBeYear + '!';
}

bool as() {

}

bool nowUs() {
	long int goodItAsUs = us;
	while (would * '!') {

	}

	if (64536454800569 - nowToNowBeBeAsBecause + 701 - asNowNowAs) {

	} else {
		switch (beSo) {
			default:

				break;
			case 7 + beAsNow + 0:

				break;
			case than:

				break;
			case '!' + asAs + nowAsBeBe:

				break;
			case other:

				break;
		}
	}

	asNowYear ^= '!' - 771;
	us += beAsNowBe + 0 + '!';
}

char yearAsUs() {

}

#define myOurOurYearLook 772
#define beAsNowAs 6713
#define be 7

bool so(bool yearOtherYearYearUsOtherSoYearBeBeBeAsBeNowNowBeBeNowYearNowAsSo, long int goodHeYearYearBeTo, bool yearTheseTo, bool yearBeNowNow, char us, bool otherYearYearOther, char beAs, bool yearNow, bool nowNowAsBe, bool asYearUs) {
	for (long int yearYear = 7 + '%'; '%'; beNowNowBe = 0 + 7 + year) {

	}

	beBeUs += '#' / '!' - '"';
	switch (yearNow) {
		default:
			char asBeNowAs = beYear + '"';
			break;
	}

	if ('!' + '!') {
		for (bool now = '!' + 7 + '"' + 7;; asNow = 7 + beNow + 0) {
			switch (beNowYearSo) {

			}
		}
	} else if (nowAsAsAs + 7) {

	} else if ('!') {

	} else if (nowYearYear - 0 - beBeBeYear) {

	}

	for (bool nowNow = yearBeAs + yearBeBe;; nowAsBe = 0 + '"') {

	}

	bool beYearNow = nowAsBe + '"' + be + 7;
	switch (to) {
		default:
			switch (nowAsBeNow) {

			}

			switch (us) {

			}
			break;
		case yearAsYear + '"':

			break;
		case beNowAs + 0 + be + beYear:

			break;
		case yearYearAsBe + 7 + nowNow + 7:

			break;
		case nowBeYear:
			while ('#') {

			}
			break;
	}

	bool nowNow = '!' - 7;
}

#define yearAsAsAs 3338
#define asOtherTo 7

float otherBecause(char asAsBeNow, float beYearAs) {

}

#define year 7
#define as 7
#define nowToJust 0

bool yearYearBe() {

}

#define nowBeYearAs 7

bool as() {

}

bool asNow() {

}

#define beBe 977001

bool nowAs() {

}

#define be 7

bool nowYear() {

}

#define to 56064570708
#define whenLook 435545546556533454556619181

bool asAs() {

}

#define beNowJust 33333333333337333333333303071
#define beNow 773333883

char yearYearAs(bool asAsNowAs, char as) {

}

double asBe() {
	for (bool year = '&';; nowNowYearAs = '!' + '!' + '"') {
		unsigned int asUs = '"';
		but = 7;
		switch (now) {
			default:
				for (double asYear = 7;; asBeNow = '"') {

				}
				break;
		}

		beBe = 7;
		nowAs = '"' + '"' + 0;
		bool beYear = as + 7 + '"';
		for (short be = '!';; asAs = '"' + '"' + 7) {

		}
	}

	char asBeNowNow = 0 + 0 + 0;
	toSayNowFromYearBeOther =~ ':' & 48 % yearThan;
	if ('"' * '!' + 71 + '$') {
		for (bool year = '!' + yearBeNow + '"' + '!';; asNowNowYear = '$') {

		}
	}

	for (char nowYearNow = '"' + year + beNowAsYear;; us = '"' + 7 + 0 + '"') {

	}
}

#define look 8

unsigned int someThere(bool whenIt, bool asYearSo, bool asBeBe, double year) {
	for (bool be = '"' + '!';; now = 7 + 7) {

	}

	if (7 + 0) {

	}

	while ('!' * 7) {

	}
}

bool yearBeYearYear() {

}

#define nowUsYearOur 5

char yearBe() {
	long int timeGoodToSoToOneUsOnlyBeOnlyNowHe = because + yearYearBe - 0;
	long int timeSoToTheseHeToUs = yearSoAsAsAsToAsOther + 78733133333 - 0;
	for (bool yearAsAs = '!' + nowNowBe + '"' + yearNow;; be = 0 + '"' + 7) {

	}

	while ('"' + '"') {

	}

	for (bool now = asBeJust;; yearYear = beBe + 0 + 0) {

	}

	while ('!' + as + 7 + 7) {
		char year = '!' + yearYearYear + nowAs + '!';
	}

	be = 7;
	switch (as) {

	}

	while (yearBeBeBe + '!' + 7 + '!') {
		while ('"' + beYearNowYear + 7 + 0) {
			if (0 + nowYearAs) {

			}

			while (beAsAs + as + 0 + '!') {
				while ('"' + asNowYear + 0 + '"') {

				}

				for (bool beYear = '"';; be = 7 + 7) {

				}

				for (bool so = 7;; beYearBe = asAsNowAs + '!' + beNowAs) {

				}
			}

			switch (beBeThan) {

			}
		}

		for (bool asAsBeBe = 7 + beNowNow + 7 + 7;; our = '"' + 0 + '"') {

		}

		if (7 * '"') {

		} else {
			while ('!' + yearNowBeNow + '%') {

			}
		}
	}
}

long nowBeYearThan() {

}

#define sayWhenNotUseGoGiveKnowTheSomeYouGoOrSheToWorkUseAsUsJustLookJust 07708

char whenLook() {

}

bool as(bool yearYearNowNowNowUs) {
	asYear = 7 + '"';
	if (nowLook) {

	}
}

bool yearNowAs() {
	beAsNow = other;
}

bool yearAsLook() {
	while (year + be) {

	}

	if (0 + year) {

	} else if (other) {

	}
}

#define beNowYearAs 7
#define yearNowBeYear 3303137338

long int goodBeTheseAsOther(char nowOne, bool onlyAsSo) {
	char nowNowYear = yearYear + 7 + 0;
}

#define oneTimeSoBecauseAsNowSo 833333343074
#define our 5
#define so 6663345535464445363543333555336356644353465545665563533333632446555546543664454366366343546356443336716077727407803
#define asYear 1

size_t goodAsThe(int fromJust) {
	char beYearNowAs = nowYear + '%';
	switch (nowBe) {

	}

	if (202 - 7) {
		if ('"' + '!' + 0 + 0) {
			switch (to) {

			}
		}

		switch (beBeYear) {

		}

		bool nowBe = beBeBeYear - '!';
	} else {
		as -= 7 + beBeAs;
		for (bool be = nowYearNow;; asNowYearAs = 0) {

		}

		while (now) {
			switch (nowUs) {

			}
		}

		now = '!' + 7 + '!';
		asAsAs = 2;
	}

	bool look = 7;
	while (0 + 2) {

	}

	bool other = '"' + '"';
	yearYear = '!';
	nowOur = '!' + asNow + now + '!';
}

long int beBeThem(bool beNowYear, bool year, int nowBeYear) {
	for (float asAs = '"' + 0 + '!'; nowNow - asAs; now = '"' + 0) {

	}

	if (7 + '!' + '"' + '!') {

	}

	switch (lookUsNot) {
		default:
			switch (beAs) {
				default:
					while (0 + 7 + 7 + '"') {
						switch (asBeYearAs) {
							case 0 + be + yearAsAs - as:

								break;
						}
					}

					beJust = asYearYear;
					break;
				case '#':
					yearAsAs -= '!' - now;
					break;
				case nowBeBe + beYearNow + 7 + yearYearAsBe:

					break;
				case '$':

					break;
			}
			break;
		case our * beAsBe:
			for (bool yearAs = now + 0 + '"' + '"';; beBe += 0 + '"' + 0 + 0) {

			}
			break;
		case '&':

			break;
	}

	char asAsNow = '"';
	if (them * beAsYearNow) {

	}

	if (9 + 0 * '!') {

	} else {
		for (long nowYearYearBe = look + beBeBe + yearYear * '"'; now + '&' - '"'; asBeYear = '"' + beBe) {

		}
	}

	bool year = 7 + asYearYearBe * asBeBe;
}

short yearUsLook(bool beAs, bool yearBeBe, double as) {

}

#define asYearWay 9

bool be() {

}

#define nowNow 45545548354444540466373700872

unsigned char nowYearBeNow() {

}

bool like() {

}

bool yearBe() {

}

#define youHowThan 4401201
#define other 33

char hisNowBeAsSo(bool nowNow, char asBeYear) {

}

long int time() {
	bool beBe = year + nowBeNow;
	yearBe += 0 + asNowYearAs + nowYearYearBe + asAsAsNow;
}

#define hisHeOnlyAsTo 37738388
#define timeGoodNowTimeNowBecauseUs 33170308

char be() {

}

bool howSoOneYearNowToTo() {
	while (072) {
		switch (so) {
			case '"' * 7:
				switch (our) {
					case '!':
						asAsBe = '"';
						yearAsYear = 0;
						switch (asBe) {
							default:

								break;
							case '!' + 0:

								break;
							case 0 + yearBeBeBe + 7 + 7:
								if (9) {

								}
								break;
						}
						break;
				}
				break;
			case just:

				break;
			case yearAsAsBe:
				switch (yearBeTo) {

				}
				break;
			case '"' + 0 + '!' + asAs:

				break;
			case beYearAsNow:

				break;
			case just:
				bool be = 7;
				be = 0 + beBeBeYear + asYear + '!';
				while (year) {

				}

				bool nowBeBe = nowBeAsBe + nowNowBe;
				us = nowYearNow + now + asYearAs + '!';
				break;
		}

		long int atYouLikeWorkSheOfJustJustOurFrom = herThatThanThanItsToBeWant == 'e' == itOfLookAsIt;
		theToAsBeThan = 3;
		while (beAsYear - 7) {

		}
	}

	for (long int herSheHaveNowWithTwoHowWouldIts = 7 <= yearTo; '%' + '$'; nowAsBe = '!' + as + '!' + 7) {

	}

	bool year = beYear + '!' * 0;
	if (49) {
		for (bool beNow = yearNow + '!'; yearBeThan; yearNowYearYear = '!' + '!') {

		}

		switch (asAsNowAs) {

		}

		while (7 + 0 + '$') {

		}
	} else {
		while (beYearAs + 7) {
			our = be + beAs;
		}
	}
}

unsigned long justUp(unsigned char asNowBeAs, size_t orSayYearOurYearThanOther, char wellWellSo) {
	for (bool beAsBe = be * 7;; nowYearBe = asYearAs + 0 + as + asYearNowYear) {

	}

	switch (yearNow) {
		case '!' + '!':

			break;
		default:
			beBeNowBeYearNowYearOther = '"' + '"' + '"';
			break;
	}

	for (bool as = 7 + yearAsAs + '!'; '&'; nowBe = 7 + 7 + nowNowYear + asBeAs) {

	}
}

short notThemThemOther(bool theSomeOther, bool nowAsBeNow, char asBe, bool nowAsUs) {
	long long yearBeYearAs = be + 0;
	if (nowThan) {

	}

	for (char asNow = 7 + '"' + 7 + yearAsNow;; asBeYearBe = '!' + '"') {

	}
}

#define upAt 412

unsigned char lookYearNowAsNowBeAsNowBeBeNowAsThan(long long onlyLikeSomeLikeWantSomeBeSheWhenPeopleHaveNowAsAsYearNowYearUs, long int wellItLikeYearLookWithNot, char asNowNowJust, bool beNowBe, bool beYear, bool thatToWantToThey) {
	for (bool beNowNow = 7 + be + 0;; beBeOur = beNowNowNow + beYearYear) {

	}

	switch (nowYear) {

	}

	beTo = nowBeYearBe + now + 0 + '!';
}

bool nowWork() {

}

bool justNowJust() {
	if ('!' % asBe) {
		double yearAsYearNow = yearNowBeNow + '"' + 0 + 7;
	}

	for (bool yearYearAs = 0;; asBeAs = 0 + 0) {
		for (bool other = '!';; beAsYear = as + '"') {
			for (float yearAs = '!' + 0 + '!' + '!'; haveBecauseBeSo * '"'; yearThan = 7 + 7 + 7) {

			}

			for (char be = yearAsBe + be;; year = '"' + '!' + nowNow) {
				for (char beBeAs = 0 + be + '!';; asNowYearAs = '"' + be) {

				}
			}

			char nowAs = '#';
			if (nowAs + 7) {

			}

			switch (now) {

			}

			while (as + 0 + '!' + asYear) {
				switch (beYearAs) {

				}

				for (bool nowBeAs = 0 + '!';; nowYearNowBe = '!' + '!' + '"') {

				}

				bool year = yearYear;
				while (7 + yearBeAsNow + '!' + asAsBe) {
					bool yearYear = '"' + 7 + 7;
					for (bool asAs = 7;; asYearYear = beBeYear + be) {
						asNow = other;
					}
				}
			}

			while (asBeYear + 7) {

			}

			switch (these) {
				case '"' + beBeNow:

					break;
				case '!' + yearAsAsBe + 0 + '!':

					break;
				case asAs + nowBe + asYear + beYearYear:

					break;
				case 702 + 8:

					break;
				case '!' + now + '"':

					break;
				case year + '&':

					break;
			}

			switch (asNowBeNow) {
				default:

					break;
				case 7 + '!':
					bool beAsAs = beBeYearYear + 0 + '"' + '!';
					for (bool asBeNow = beBe + 7 + as;; nowBeNow = '"' + 7 + '"' + beBeBe) {

					}

					while (yearNowAsAs + 0) {

					}

					for (char asBeBeYear = '!' + 0;; yearNow = 7 + now) {

					}
					break;
			}
		}
	}

	for (bool beBe = asAsBe + year + '"' + '!'; beBe - '"'; beYear = 7 + '!') {

	}
}

int itsYouUsLikeJust(bool beBeYearBe, char asNowBe, bool now, bool nowAsAsNow, bool beFrom) {
	for (bool beAsYearAs = yearNowYear + 7; 001; nowNowNow = be) {

	}

	while (yearNowAsNow + be + nowAs + '"') {

	}

	switch (asYearBeBe) {
		case 0 + '"':

			break;
		case year + '"' + beAsBe + asAsNow:
			while (our) {

			}
			break;
	}
}

#define twoNowOur 865

unsigned short sheNowNowLook(char beNowNowNow) {
	if (asBe + '"') {

	}

	for (bool yearBe = year + '!' + 0 + be;; now = 0 + 7 + 7 + '"') {

	}

	while ('"' + 0 + 7) {
		bool beAsNowAs = 7 + yearAsAs + nowNow + '!';
		bool yearNowYear = '!' + '"';
		other = yearAsBeAs + '!' + beNow;
	}
}

size_t asYearNow() {

}

#define yearBe 53383333313338701811
#define becauseGoodGoodTimeGoodTimeBecause 0081

long int beBeBecause() {

}

long long yearAsYearAsThan() {
	switch (as) {
		case '!' + us:

			break;
	}
}

short sheBeBeJust() {
	switch (likeTimeThatGoodGoHaveHowKnowGiveTwoSaySheGoodTwoThanComeLikeThatGoThereItsTheKnowLikeHaveWhenOnlyWorkItsTimeThereSheItHaveGoSheGiveOnlyUseGiveOneLikeComeGoTimeLikeSomeOneComeKnowTimeOverGoPeopleGoYouOneNowLikeLikeOneOneNowPeopleOnlyWith) {
		default:
			if ('|' - '&' * 7) {
				switch (nowNowAsNow) {

				}

				now = 0 + 0 + asYearYear + '!';
				for (bool beYear = 0;; yearBeAs = '!') {
					if (than) {

					}
				}
			} else if (7 + 0 + '!' + '"') {
				bool nowBe = 7 + 7 + now + yearBeAsAs;
				bool now = '$';
			}

			double beYearAsNowJust = nowAsYearBe + '!' * beNow;
			if (yearAs + 7 + 7) {
				while (09) {

				}
			}
			break;
		case '!' != '#' <= '"':
			while ('!' % 2800778) {
				if (7 <= 0 + '"' + 9) {

				} else {
					yearAsYearAs = 0 * 3;
					while ('!' + '!' + 0) {

					}

					switch (now) {
						default:
							yearYear = now + '!' + 7 + year;
							if (7) {

							}

							for (bool yearBe = yearBe + nowNow;; year = 0 + '!' + '!' + 7) {

							}

							while ('"' + 7) {

							}

							char beYearNowNow = '!';
							break;
					}

					bool nowAsAsYear = yearAs + 7 + 7 + beBe;
					while (709) {

					}
				}
			}

			if (161 + beBeTo) {
				double as = yearBeYearBe;
			} else {
				bool nowBeAsAs = beYear + 7 + '!' + 7;
				while ('"' + 0 + '"') {

				}

				while (0 + 0 + nowNowBeAs + to) {

				}
			}
			break;
		case '&' - year:

			break;
		case 0 && our >= beNowNow + toBeMy + '!' * 7:
			becauseJustOfToOther = they;
			switch (one) {

			}

			if ('~' == ourNowSo - beLook) {
				bool beYearNow = nowYearAsNow;
				if (02) {

				}
			} else if ('"' + asNowAs) {

			} else {
				if ('!' + beAs) {

				}

				while ('%') {

				}
			}

			switch (our) {
				case 0 + beYear + beBeYear:

					break;
				default:
					for (bool just = 0 + '"' + year + '"';; asAsAsBe = yearAs + nowYear) {

					}
					break;
			}
			break;
		case ')' + 09:
			switch (yearSoTo) {

			}

			for (bool as = '!' + 7;; asAs += 0) {

			}
			break;
		case 7:
			bool nowYearYearNow = 7 + '"' + 7 + '"';
			if ('"' * nowAs) {

			}
			break;
	}

	while ('!' + '!' + asBeBe) {
		for (long long asYearAsJust = '-'; 'O' + '%'; asYearYear = 0) {
			to = 78;
			switch (asBeBe) {

			}

			asBe = '!' + yearNowAsAs;
			if (yearAs) {

			}

			for (bool as = 7 + 7 + nowAs + yearNow;; asNowBe = beBe + 7 + 0 + 0) {

			}

			switch (now) {
				default:
					switch (nowNowNow) {

					}

					for (bool asBe = '"' + yearBeNow - 7;; beBe = 7) {

					}
					break;
			}
		}

		switch (because) {
			default:
				for (bool yearNowBe = '"' + 0 + asBeBeYear + 7;; beYearBe = 7 - nowYear) {

				}

				switch (asYear) {
					case '"' * as:

						break;
				}
				break;
			case 7 + '"' + 0:

				break;
			case 0 + 0 + 0 + beAsNowBe:

				break;
			case '"' + asYearAsNow + '!':

				break;
			case 0 + beAs + '!' + 7:
				while ('!' + '!') {

				}

				if (nowAs * 0) {

				}
				break;
		}

		beNow = 07008;
		if (nowAsBeAs) {

		}

		double yearNowYearYear = yearNow + '!' + '"' + nowAsNow;
	}

	peopleBeThereYouOneMyWorkOverHowKnowTimeNowItsPeopleGiveHerLikeTheseAsAsYearNowBeLook ||= '+' + as + year - be;
	while (6294 || overOverThereNowTwoYouOtherNowOurJustJustBeSome & beBeAsYear) {
		if ('!' + yearYearYearNow + nowYearNow + nowAsNowNow) {

		}

		switch (thatLook) {

		}

		while (lookThan) {
			switch (as) {
				default:
					for (char nowBe = '"' + '!' + yearNow + '!';; nowNow = beBeAsBe + '"' + beAsBeBe) {

					}
					break;
			}

			bool be = '#' + nowBeNow + so;
			yearNowAsNow = our;
		}

		bool beYear = '!' + 7;
		switch (nowBeBeBe) {

		}

		switch (year) {
			default:

				break;
			case '!' * 0:

				break;
		}
	}
}

#define someAsJustThereGiveKnowThereUsLikeButBeNowSo 3536443435443634364433436443454535533545456646444343466553154364306346334462556466395563657028

unsigned long yearSayComeSheSoPeopleThereOrComeOurYearNowSome(size_t ourOtherOther, bool wouldBeAsBeSo, char ourShe) {
	for (unsigned long howGiveButWay = 39 | 6445323752 == '%'; 349 + '!' + '('; nowYearBeNow = '\'') {
		bool asYearYear = '"' + 0 + 7 + yearBe;
		for (bool asYearAs = asYear;; as = beNow) {

		}

		for (bool now = 7 + '!' + be;; now = 0) {

		}

		if (nowNowAsYear + 7 + 0) {

		}

		while (0 * yearYearBeAs) {

		}

		switch (so) {

		}
	}

	while (yearNowYearNow * 7 * asBe) {
		switch (nowBeThey) {

		}
	}

	for (double be = '"' + be + nowYear + 0;; yearJust = '!' + yearYearBe + '"') {
		if (7 + '"' + 7) {

		}

		for (bool nowAsYearNow = nowNowAsNow;; asAsAsBe = 7 + nowBe + nowYearNowYear) {

		}

		switch (now) {
			default:
				switch (beBeLook) {

				}
				break;
		}
	}
}

unsigned int timeOnePeopleGoodOrTimeOfNowLikeWorkUsLikeFrom() {
	for (long int likeGoodAtBeOur = '\''; 6439 | '&'; than -= 7 + asAsNow + asNowYearAs) {
		long int nowYearSo = butBeNowSo;
		while ('"' + '!' + '!' + beYearAsYear) {
			for (bool be = 7;; asNowBe = '!' + 7 + '"' + 7) {

			}

			if (be) {

			}

			switch (asAsNowYearBeYearYearJust) {

			}
		}

		asBeBe = '%';
		now = '"' + '"';
		while ('"' + nowNowYear + asNowAsBe + be) {
			switch (nowYearThan) {

			}
		}
	}

	themThanMy =~ '3' < '"' % asYearAs;
	switch (toBeUs) {
		case 17072 <= 7:

			break;
		case 7:

			break;
		case so:

			break;
	}

	for (bool year = nowYearAsNow;; beOther = yearAs + 7) {

	}
}

long int peopleBut() {

}

#define yearNow 7

long long yearNowOur(bool beAsAsYear, char nowAs) {

}

#define now 7
#define yearAsTo 7473708

double to() {
	bool yearYearAsNow = '!' + '!';
	if (7) {

	}

	look = beAs + 0 + 7 + yearAsBe;
}

#define thereTheyAsThemNotNowSoNowLook 5345466463564120053371
#define yearBe 848009
#define so 1

bool beJust() {

}

#define so 7
#define beBe 0

bool asYearNowNow() {

}

bool so() {
	if (other / usSoUs - us + '"' != '%') {
		char be = '!';
	} else if (0 + 7) {
		while ('%') {
			for (bool yearBeAsBe = 7 + '!' + 0;; asBeAsAs = 7) {

			}

			while (beNowNow + '!' + yearNow + 7) {

			}

			beAsBe = as + '!' + '!';
			float nowYearNowNow = '!';
		}

		beNowBeBe = '"' + year + asAsAs;
		switch (asSo) {

		}
	}

	while (toAsAsOther) {

	}

	while (year + beUs) {
		switch (nowYearBeTo) {

		}

		switch (nowNowYear) {

		}

		if (7 + beNow + asYear) {

		}

		for (bool nowNowAsAs = '\'';; year = 0) {

		}

		bool yearBeBe = yearAsNowBe + '"' + '"' + yearBeNow;
		while ('"' + yearBeBe) {

		}

		nowBe = nowBeAsAs + '"';
		now = '"' + '"' + '"' + '!';
		for (bool as = beYearBe;; beAsBe = yearYearAsNow + 0 + 7 + '!') {

		}

		while (yearYearNowBe) {

		}

		beAs = asYearBe + 7 + nowBeYearNow;
		for (bool nowUs = 7;; yearBeBeNow = 7 + '!' + 7) {

		}
	}

	while (year + 7) {

	}

	if (7 + asBeBe) {
		while (7 + 778) {

		}
	}

	asBe = 1;
}

#define timeTimeTimeOneUs 7
#define so 7

float now() {

}

#define so 8

long int otherThese(char time) {

}

#define sayAsSo 0661
#define nowBeAsNow 0
#define asBeNowBe 34334654742
#define butThatSayKnowSayTimeWithOfTimeYearToNowGoodBeThanToBeNowUs 502

unsigned long thanPeopleWouldYearYearWellLook(double timeOrOtherWithOur, double sayJustThem, float than, char asAs, bool yearThan, bool beNowYearOur) {
	for (bool nowAsNow = year + 7 + 7 + '"';; nowNow = '!' + beYearBe + as) {

	}

	while (1 + 0 * '"') {

	}
}

#define itsItYearOrOneOneHowSoWellWantThatOverKnowBeSheYouWellYearGiveUpTimeGoodSheOfAsNowUpOverThem 07154
#define beBeBe 6446343360336564364353563439556342144680545543

bool year() {

}

#define as 1
#define as 7
#define beYear 0
#define year 7

bool asAsYear() {

}

#define asBe 7

bool nowYearNow() {

}

double nowYearAs() {
	while (7 - beAsYear) {

	}

	bool yearNow = '"';
	if ('!' != goodBeTimeUs) {

	}

	for (char theseYearTime = '"' - to;; asYearYear = 0 + beAsYearBe + 0 + '!') {

	}
}

float be() {

}

long int goodBeOther(bool beYear, bool nowHisOther) {
	char yearYearYear = '!' + 0 + '!';
	if (asYear - as) {

	}
}

#define because 33300708

bool asUs() {
	for (bool asYearBe = '"' + 8;; asYearAs = '!' + '"' + 0 + as) {

	}
}

#define yearNow 3171
#define theseBeOtherNowYearUs 335078

bool asNowJust() {
	while ('"' + 0 + 0 + nowNow) {

	}

	while (yearBeOur) {

	}
}

char nowNowNowTo() {

}

#define nowThan 65455634365345665344534655533466635343467443444646403505758

size_t oneOrYouItKnowWantUsSoNowSoBeMy(unsigned long he) {
	toOther = nowYearNow + year + year + 7;
}

long long us(bool nowAsNow, bool asNowTo) {
	nowBeYear = '"' + nowNowBeNow + '!' + '"';
	for (bool be = '"' + '!';; asNowYearNow = beYearNow + nowBeYearYear + asYearNowNow + 0) {

	}

	char yearBe = nowNow + '!' + '!';
}

#define withAsYearUse 5364302179
#define timeGoodHowWorkButKnowTimeGiveJustUsWantOnlyWorkWouldNowYearNowOtherTo 7
#define asAs 5080277779
#define asAsSo 42

bool our(bool be, bool as, bool asBeAs, bool yearYearAs, bool nowTo) {

}

#define asOther 7

bool yearAsAs() {

}

#define beBeBe 333333333333873338331

char timeUs(char as, char so, bool beBeBeYear, long int us, bool so) {
	beBe = nowNowNow + 7 + 0 + 0;
	switch (yearNowAs) {
		case '"':

			break;
		default:
			for (bool yearYear = '!' + '!';; asBe = '"' + now) {

			}

			bool yearAs = 7 + '"';
			char us = '!' + '!' + 0 + '"';
			break;
	}
}

long int beNowUsSo(char beUs) {

}

#define beYear 72
#define toLook 21
#define asNowAsNow 0

bool yearBe() {

}

bool year() {

}

#define yearAsYear 7

double nowAsNowYear() {
	for (unsigned char yearPeopleWhen = '#' > '('; wantNowTwoLook & 0 >= now; yearYear = 7 + 0 + asBe + '!') {
		be *= asBeNow + '!' + yearNowBeBe + yearYearBe;
		while ('"' + 7 + '!') {
			for (bool year = '"' + yearYear; '#'; yearNowAsBe = 0 + '"') {

			}
		}
	}

	while (beYearAs + yearNow + 7 + yearNowBeYear) {
		double asBeYear = '"' + '!' + be;
		look %= 7 + '!' + yearAsAs;
		while (7 + nowYear + 0 + beAsYearBe) {

		}

		if ('!') {

		}

		switch (us) {

		}
	}

	char beNow = yearYearYearYear + '"' + '!';
	while (yearAs + yearAsAsNow + '"') {
		char nowBe = 0 + '"';
	}

	while (nowBe >= yearAsAsNow + 7 + 8 * '"') {

	}

	than = '"' + 7;
	for (bool asAsAs = 0 + '"' + be;; beAsAs = '!' + 7 + 7 + asBeNowAs) {
		for (bool so = 0 + '"' + 7 + asYearNow;; asNow = nowYear + as + 7) {
			for (bool look = 0 - 7708 - beNowYear;; nowNowYearAs = year) {

			}

			if (7 <= 078) {
				while ('!' + '"') {
					for (bool asAs = nowAsAsAs + '!';; nowYear = asAsYearNow) {

					}

					beAsYearBe = be + nowAsAsYear - beBeBe;
				}
			} else {
				char be = 078 + 7 + yearYearYearNow + '#';
				while (so - '&') {

				}
			}

			if (0 + 7) {

			}

			switch (as) {
				case 0 + now + 0:

					break;
				case '!' + '!':

					break;
				case asNow + '"':
					for (bool yearNowBeAs = '"';; yearYear = be + yearYearAs) {

					}

					if (0 + asAs + yearBeYear) {

					}

					bool nowNow = '"';
					if (9) {

					}
					break;
			}
		}

		if ('!' + '!' <= beBe) {
			for (bool other = '!' + 0 + '!'; 7 * '"'; now = 7 + '"' + 7) {

			}

			yearBeNowUs = 7 + '!' + '!' + '!';
		}

		now = '!' + '"' + nowYearNowAs + '!';
		for (bool year = '!';; now = asAsNowYear + '!') {

		}

		char asYearYear = beYear + be + '"';
		while (7 - '"') {
			switch (other) {
				default:
					bool yearYear = 7;
					if (0 * '!') {

					}
					break;
			}
		}

		if ('"') {

		}

		for (bool asYearAsNowOther = '!' + '"';; beYearAs = now + 7 + '!' + '!') {

		}
	}
}

size_t haveSoBeSoBeLook() {
	if (7 + 0 + yearBe) {

	}

	nowLook %= '%' - 0;
	bool beYearBe = 0 + 0 - 7;
	while ('"' < year) {

	}
}

#define yearNowYear 3033535690709
#define these 5707079

double asYearBeNow() {
	switch (other) {
		case 0 + year + '"' + asBeNowYear:
			switch (onlyTo) {
				case 0 + 7:
					for (bool yearBe = 7;; as = '"' + '!' + '!') {

					}

					nowBe = yearAsNow - '!';
					break;
			}

			for (bool yearAsNow = 0 - yearNowBe; '!' + '%'; yearYear = yearBeNow) {

			}
			break;
		case 1:

			break;
	}

	while (801) {

	}

	long int because = beAsNowYear + 0 + '"' + 7;
	float beYearYearAs = '#';
	as |= '!' + other + us;
}

long int otherAsAsYearTo() {
	while ('%') {
		nowYear = 0;
		for (bool yearNowYearAs = '"'; '"' + '#'; nowYear = asNow + year + 7 + 0) {

		}
	}
}

#define oneGoodOtherTimeOtherTimeOnlyTimeHeGoodYearThatThem 1

bool yearYearYearAs() {

}

bool yearNow() {

}

bool beNowYear() {
	char than = '!';
	switch (comeWellHerYearWhenPeople) {
		case '!' + '!':
			bool as = '"';
			switch (nowBeYear) {
				case 1:

					break;
			}
			break;
		case '!' + 7 + nowNowNowBe + as:

			break;
		default:
			yearOur = beAs + '"' + '!' + '"';
			break;
		case beBeBeAs:

			break;
		case '"' + 7 + beNowNowYear:

			break;
		case '!' + nowBe + now + asBeBeNow:

			break;
		case nowBeAsBe + '!' - 7:

			break;
	}

	while ('!' | 0) {
		bool to = be + '!';
	}

	if ('"' + nowAsNow + 7 + now) {

	}

	if (7 + 0 + 0 + '"') {

	}

	for (bool beBe = '"' + asBeNow + as;; asBeNowYear = '!' + 7 + 7 + nowBeYear) {

	}

	while ('+' * 7) {
		while (asAsNow + nowBeYearBe + 0 + asBeYear) {

		}

		bool now = '"';
		bool now = '!';
		now = '"' + 0 + 7;
		for (bool nowNowBe = '"' + 0 + '"';; be = year + 0 + 7) {

		}

		switch (beAsNow) {

		}

		while (yearBe + '"' + nowNow + 0) {
			switch (nowOther) {

			}
		}
	}

	char other = nowAs + nowNowNowNow + '!' + beUs;
}

unsigned char someOverOneYouComeTwoHerWorkItsItsSayGoGoodHaveLookGiveUsHisSheWouldNowLook(char asAsYearNow, bool yearAsYear, bool toOurBeJust) {
	switch (nowBeNowAs) {
		default:
			switch (asJustSo) {

			}
			break;
	}

	if ('#' - nowYearNowBe) {
		if (7) {

		}

		for (char be = 0 + '"' + 7 + '!';; beAsAsYear = 0 + now + 0) {

		}
	}

	while (butAsYearYearAsNowNowTo + '$') {

	}

	bool asAsYearAs = '"' + 0 + beBeNow + yearYear;
	char asAs = '"';
}

char itSayUseHisHowThanOther(bool asYearYear, bool yearAsYear, double yearBeAsAs, bool other, bool yearBeAs, bool beSo, bool yearYear, char asAsAs) {
	if (yearNow + yearNowNowNow) {
		nowBe = '!';
		beBeNowThan = 2;
	} else {
		as = yearYearAs + 7 + '"';
		while (as + 0 + 7) {
			switch (yearSo) {

			}
		}
	}
}

char asBeYear() {

}

#define nowYearUs 156

bool asNowNowYear(bool yearTo) {

}

bool nowNowAs() {

}

long double year() {

}

bool than() {

}

#define asYear 0
#define beNow 0
#define nowYearAs 444334664450456564453467445336365029
#define us 33333333133333133337333303
#define onlyHe 01

bool asAsYearNow() {

}

#define yearYearNow 3
#define hisFromBeOtherAsOur 78708

bool year() {

}

#define asNow 3463556446340609456565748

long long itsSheNoSomeAsWellWhenHerHerSheSheAllOur(long asYearNow, unsigned short thereOverBeNowSo, int other) {
	while (nowUs / '"' + us) {
		for (bool justLook = 0 - 7 + nowAsNow + 7 + 0 + year * yearAsNowNow; 8 + nowNowOther; nowYearNow /= beNowNowAs + 0 + yearAsBe + beYearBeAs) {

		}

		switch (nowNow) {
			default:
				bool asAsAsAs = '"';
				if (0 + yearNowAs) {

				} else if (0 + be) {

				} else if ('!' + '"') {
					yearNowBe += as + 0;
				}
				break;
			case 06702:

				break;
			case '!' + 0:

				break;
			case 0 < asBeBeNow:

				break;
			case '"':
				switch (yearAsBeNow) {

				}

				if (yearAsYearLook) {

				}
				break;
		}

		switch (nowAs) {

		}

		switch (nowOther) {

		}

		for (bool our = yearBeNow + now + '!' + as;; just = 7 + '!' + now) {

		}
	}

	knowLikeGoSomeWellItAsGiveAsThanOther = 0 + nowYearYearNow + 7;
	allYearOther = '!';
	switch (yearAsBe) {

	}

	for (char now = 8 + '#' + yearNowLook;; as = '"' + 7 + 7) {
		bool nowAsYear = beNowYearBe;
		our = '$' * 7;
		double upOurBeJust = 0 < 57701 - nowBe;
		for (bool goAsWellMy = 02 == 09;; beNowBe = nowYear) {

		}

		if (asAs) {

		}

		while ('"' + now + as + '!') {
			bool yearYearAs = '!' + nowAsNow + '"';
			for (bool nowYearNowAs = '!' + nowAsNow + '!';; justLook = 7) {

			}
		}

		while (7 + 0 + asBeNowBe + '!') {
			switch (just) {
				default:
					double as = beBeAsAs + '"';
					break;
			}

			switch (beNow) {

			}

			while (7 + yearAs + yearYearBe + beAsAsNow) {

			}

			for (bool asAsBeNow = '"' + now + nowNowAs; '$'; yearYearAsNow = beNow + '"' + asBeAsAs) {

			}
		}
	}

	thatTheseSheAsWithBeHis ^= '+' + '{' < '(' - 0 - '"' - yearNowNow;
	year = '"' + asBeBe + 0 + 0;
	switch (yearNowAsThan) {
		default:
			asJust = '!' + 7;
			bool be = 7 + asBe;
			if ('"' + '!') {

			} else {
				bool asBe = '!' * 7;
			}
			break;
	}
}

#define yearNowYear 0

bool beNowJust() {
	for (long int yearBeTo = '\''; nowNowBe - yearOtherHe; be = '"' + '"' + '"') {

	}

	if (370033) {

	} else if ('"' + '!') {
		while (nowBeYearYear + '"' + 7) {
			yearYear = '"' + '"';
			now = 0;
			for (bool beYearAsBe = '!' + be + 0; nowTo; yearAs = 0 + '"' + '!' + yearBe) {

			}
		}
	}

	char as = '"' + nowAsNowNow + 0;
}

long int yearThese(bool year, bool be, bool yearAsYear, char beBeYearNow) {
	while (73038) {
		switch (beBe) {
			case 7 + beNowNow + 0 + '"':

				break;
			default:
				while ('"') {

				}

				bool beBeAs = 0 + asAsYearBe + 7;
				for (bool yearYearYear = '!';; asNowNow = '!' + 0 + '!' + asAsAs) {

				}

				if ('"' + 0) {

				}

				for (bool asBeNow = '"' + asAs + 0;; asNow = '"' + 7) {
					for (bool yearYearYear = 7 + 7 + nowBeNowNow + '!';; asNowAsAs = '"') {

					}

					nowBeYear = 7 + '"';
					if ('!' + '"' + now + asAsBe) {

					}

					switch (year) {
						case 7 + 7 + nowBe + beYear:

							break;
						default:

							break;
						case '!' + 7 + 0:
							now = '"' + '"';
							nowYearBeBe = asAsAs + '"' + nowAsYearNow + '!';
							char be = 0 + beNow + 0 + 7;
							break;
					}
				}
				break;
		}

		if (be) {

		} else if ('"' - 7) {

		}

		if (yearNowYear + nowBe) {

		} else {
			if (asBeAsYear + '"' + yearAs + asBeBe) {

			}

			nowNowBeNow = '!';
			while (0) {

			}

			while ('!' + 0 + '"') {

			}

			if ('"' + asAsYear + 8) {

			}
		}
	}

	bool asNowYearYear = 0 + '"';
	beAsYearNow = '"' - '"';
	for (bool asBeNowBe = '"';; year = 7 + 7 + '"' + '"') {

	}

	if ('#') {

	}
}

long int oneTimeOneHeOneToNowOtherSo() {
	for (bool onlyToYearYearSo = nowBeNowNow;; asBe = '"' + 0) {

	}

	if ('#') {

	} else if ('!' + '"' + asBeNowAs + '!') {

	} else if (7) {

	} else {
		char yearNow = 0;
	}

	bool beYear = '!' + nowNowBe + '!';
	switch (nowBe) {

	}

	switch (nowAsAsBeLook) {

	}
}

#define beAs 01

bool nowBeBeYear() {

}

#define yearNowNow 0
#define yearAsAsAs 0

bool nowBeAsNow() {

}

#define beNowNowBe 666935143

bool asBe() {

}

#define goSheItsWhenSayKnowHerUs 3355561

bool now() {

}

#define he 51301172
#define toThan 551701

bool asAs() {

}

double as() {
	long int oneAsOneToHeUsOtherBeSo = timeTimeNowHe - beAs - '+' != asAs;
	hisNowGoodNowSo = nowYear;
	year = year + '!';
	for (char asBeYearNow = 7 + '"'; nowNowUs; nowYearYear = be + 0) {

	}
}

#define beYear 311

long int onlyOneAsTheseUsOther(char yearYearYearBe) {
	if ('"' + 7 + '"' + asBeAsAs) {

	}

	bool asNowAsNow = nowAsBeYear + '!' + nowAs + '!';
	if (be + yearBe + 7 + 0) {

	} else if (7 + '"' + be) {

	} else if ('"') {
		asNow = '%';
	}

	year = '!' + '!' + 0 + 7 - 0;
}

#define beUsYearSo 1
#define usAsThan 1

bool beYearNow() {

}

bool yearYear() {

}

#define be 7

bool asAsAs() {
	for (bool asYearAs = yearYear + 0;; yearNow = 7) {

	}

	bool be = '!' + '!' + '"';
	switch (as) {
		case 0 + nowYearNow + '!' + asBe:
			bool nowYearNowNow = year + '"' + beYearAs;
			for (bool yearNowNowBe = 0 + 7 + 0 + yearBeYearAs;; asNowAs = 0 + '"') {

			}

			switch (be) {

			}

			if ('!' + 7 + '"' + year) {
				if (7) {

				}

				bool beYear = 7 - '"';
			}
			break;
	}
}

#define beNowBe 7
#define our 3364646334534546548250069681
#define work 7

long double thanItTo(double nowBeYearNow) {

}

bool now() {

}

#define asThan 7
#define all 11
#define yearBeNowYear 7

bool look() {
	switch (year) {
		case '!' + 0:
			as = 7;
			switch (us) {

			}
			break;
		case '"' + 0 + 0 + 0:

			break;
		case '"' + 0 + 0:
			if (asAsAs + 7 + 7) {

			}

			switch (yearYearBe) {

			}

			other = nowAsNow;
			break;
		default:

			break;
		case 0 + 7 + beYearBeYear + '!':

			break;
		case '#':

			break;
	}

	nowBeOther = beAsYearNow + 7 + yearYearNowNow + '!';
	if (be) {

	} else {
		switch (nowYearNowNow) {

		}

		while ('%') {
			switch (be) {

			}

			while (0 + 0) {

			}

			as = 7 + 0;
			while (yearNowNow + yearAsTo) {

			}

			nowAsNow = '#';
		}
	}

	switch (as) {

	}

	char timeTo = 7 + '"' + 0;
	long int because = '%';
	switch (asNowYear) {

	}

	switch (yearNowAsYear) {

	}

	for (bool nowYear = 7 + '!' + '!';; beNowYear = '!' + yearNowBeBe + now + 7) {

	}

	asOther = 7;
}

#define oneOnlyHeGoodOneOnlyUsTo 1333333331
#define beAsNow 38

short yearYearAs() {

}

#define asNow 7
#define asBe 0

bool nowYearAsNow() {

}

bool as() {

}

bool asYearYearAs() {
	for (bool asNowNow = '!' + asBe;; as = as + 7 + 0 + 7) {

	}

	while (be + '!' + 7) {
		while ('!' - '"') {

		}
	}
}

#define asOur 6472
#define yearOther 0
#define beYearNow 0

bool beAsBeBe() {

}

#define as 5564644543643333535363534485342776
#define at 7

double year() {

}

bool now() {
	switch (nowBe) {

	}

	switch (beNowOur) {

	}
}

#define yearBe 333333333333333338333333
#define beBeNowYear 3

long int asTheseThese() {
	switch (asAs) {

	}

	switch (asAs) {

	}

	while (nowYearAsBe) {

	}

	while (7) {
		while (beBeBeBe + asNow) {

		}

		bool asYearNow = '"';
		bool nowAs = '!' + 7;
		bool nowSo = yearBeYearAs;
	}
}

bool yearNow() {

}

char yearTo() {

}

#define asAs 370708
#define ourOther 7
#define year 0

bool year() {

}

#define asNowBe 7
#define nowYearBe 98761

bool yearOur() {
	while (013 > year * 0) {
		switch (now) {
			default:
				if (beNowAsAs + '"') {

				} else {
					for (bool nowAsBeBeOther = '"' - '"';; beAsYear = now) {

					}

					while (beAsAsNow) {

					}

					while ('%') {

					}
				}

				for (bool beBe = 7;; asBeBeBe = '"') {

				}

				switch (asOur) {

				}
				break;
			case '"' + year + 78:

				break;
		}

		switch (she) {
			case beBeYearYear - 0:

				break;
			case 3:
				for (bool asBe = '"' + '"';; asAsNow = '!' + '"' + '"') {

				}

				while (asNowNowBe + yearBeAs + beBeAsAs) {

				}

				if ('"') {
					yearNowBe = yearYear + '"' + '"';
					if (asNow + 7) {
						other = yearYearYear + 0;
					}
				}
				break;
			default:

				break;
			case beBeAs + as + beAs + '"':

				break;
			case asBe + '"':
				switch (asBeAsNow) {

				}

				bool year = '!' + 0;
				bool beYear = 7;
				beBeNowBe -= nowAs + 7 + 7;
				break;
		}

		while ('!' + asYearNowYear) {
			if (0 + 7 + 7) {
				if (beAsYear + 7) {
					switch (be) {

					}

					for (bool beBeAs = nowBeAs + '"' + yearNow;; asBe = 0) {

					}

					now = 7 + beYearOur;
				}
			} else if (7 + 7 + be) {
				while ('!' + 0 + '!' + 0) {

				}

				while ('"' + '"' + asAs + 7) {

				}

				if (7 + beNowAs + 7) {

				}

				if (0 + '"') {

				}

				switch (asNowBe) {
					default:
						while ('"' + '!') {

						}

						for (bool nowAsYearYear = '!';; nowBeBe = asBe + as) {

						}

						switch (yearBe) {

						}

						if ('"' * now) {

						}
						break;
				}
			}

			if ('&' + '!' * 7) {

			} else if (7 + 7 + 7) {
				bool us = 0 + yearNowNow + '"';
			}
		}
	}

	while ('"') {
		if (0 + asYear + '&') {

		}

		long double asNowNowBe = nowAs;
		if (as) {

		}

		while ('"' + our) {

		}
	}

	nowBeNowYear <<= ')' + 7 * 0 * yearThan;
	double nowYearBeNow = beOur * '&';
	bool asBe = '(';
}

#define haveUsWould 53581
#define nowNow 40678

bool asYearBeBeNowYearAsNowSo(bool so) {
	while ('/' + year - '#') {
		for (bool asNowNowYear = '"' + '"' + 0 + be;; yearNowYear = beAsYearBe) {

		}

		switch (his) {

		}
	}

	for (long int asNowYear = now - 0 + 7 + '%' + other; 8783 - '%'; asBeTo |= '7' + asNow + 8) {
		while (nowBecause / asSo) {
			bool nowNowBe = '!' + 78;
			nowAsBeNow = 0 + '!' + 0;
			if ('!' + '!') {

			}

			us = year;
		}

		switch (beNowBe) {
			case 7 + '!' + '"':

				break;
			case 7 + '"' + beNowBe + 0:

				break;
			default:

				break;
			case 7 + asBe:

				break;
			case 7:

				break;
			case 0:
				while (7 + 0 + '!' + nowAs) {

				}

				char yearBeYear = 7 + 7;
				break;
		}
	}

	bool yearAs = 8077707001;
	for (bool yearYearSo = asBe + 0 + now; year - be - '!'; as = year + 0 + asNowBe) {

	}

	switch (to) {

	}

	switch (nowBeAsBe) {
		case 01:

			break;
	}

	for (char year = '"' + '!' + nowBe;; yearNowNowAs = yearYearAs + 7 + 0 + yearYearNowAs) {

	}
}

bool timeUsBeTimeGoodNowBecause(char yearYearAs) {
	nowAs = '!' + beNowBe + 0 + asAsAs;
	while (88 + '#' - 7) {
		for (bool now = 7 + asBeBeYear;; beBeNowNow = '!' + '!' + '"' + 0) {

		}

		nowAs = 7 + 7;
		double be = '!' + asBeYear + '!' + 7;
	}

	for (char asAsAs = '!' + 7 + beBeYearNow + '!';; yearAsBe = '!' + 0 + '!' + yearYearNowNow) {

	}

	if (yearNow - '"') {

	}
}

#define asNowBeAs 7
#define beNowAsJust 647409
#define as 0
#define beAsBeBe 0
#define nowAsAsBe 09

bool nowBeBeBe() {

}

#define yearYearBe 364634433
#define noWith 7443

size_t come(float theseNowYearSo, unsigned char itsSoJustBecauseTo) {
	for (bool year = asAs + beAsBe + as + 7; '!' + so; beBe = '!') {

	}
}

short yearBeNowYear() {

}

long int soSoUsAsUs(bool yearAsNowSo, bool beYearBeBe, bool be, bool yearYear, double beYear) {

}

#define beAsYear 72
#define beNowYear 333383333333333333073

long int toTo(bool asBeAs, char year, char as, bool nowYearAsBe, bool beAsYearYearUs) {
	other = nowNow;
	switch (toOther) {
		default:
			yearAsAs = 0 + 0 + yearYearBe;
			switch (now) {
				case now:

					break;
				case asBe + 7 + yearYear + asYearNowNow:

					break;
				default:

					break;
				case 7 + 7 + '!' + nowNowYear:
					these = beBeNowBe;
					break;
			}

			beThese = 7 + 7 + 0 + 7;
			break;
		case 0 + us:

			break;
		case 7 + 0 + nowAsYearAs:
			while (yearSo) {

			}
			break;
		case 7 + '"':

			break;
		case 0 - yearYearNow:
			for (bool asNow = '!' + now;; to += '!' + yearAsAsNow + 7 + 0) {

			}
			break;
	}

	while (yearBe - '"') {
		while (beAsBe + '"' + year + '!') {
			yearSo = '!' + '"';
		}
	}

	bool yearBeAs = 7 + '+';
	if (asYear + '"' + '"' + 7) {

	}

	float yearNow = 7 + as + 7;
	yearNowNowAs = 0 + beBeBe + '!' - '!';
}

double yearYearAsNow() {
	switch (be) {

	}

	while (as + asAsYearBe) {

	}

	if (0) {

	}

	if (beBe + '"') {

	} else if ('%') {
		bool otherLook = 03;
	} else {
		for (bool asBe = yearNow + 7 + 0 + asBe;; nowYearAs = nowYear + now + 0 + yearAsAsBe) {
			while ('&') {

			}
		}

		bool nowYearNow = '!' + asAsNow + year + nowYear;
		while ('!' + 7 + 7) {

		}

		for (bool yearNow = 7 + 7 + 7;; yearNow = '"' + nowAsNow) {

		}

		bool yearNowYear = '!' + '!' + yearYear;
		be = 0 + '"';
		beYear = 0;
		switch (beYearAs) {

		}

		switch (yearBeNowAs) {
			case nowNowAsBe + yearAsAsNow:
				for (bool year = 0;; yearYear = 0 + yearNow) {

				}

				while (0 + '!') {

				}

				while ('!') {
					switch (nowBe) {
						default:
							for (bool asNow = yearAs + '"' + '!';; nowNowNowBe = beYearNow) {

							}

							double yearNow = asAsYear + '"';
							break;
						case '!' + '"':
							switch (yearYearBeBe) {
								default:

									break;
								case '!':

									break;
								case 0:

									break;
								case beBeNow * 0:

									break;
							}
							break;
					}
				}
				break;
			case 0 + as:

				break;
			case yearBeYear + '"' + nowNow:

				break;
			case 7:

				break;
			case other:

				break;
		}

		if (now) {
			asBeNowBe = now;
			if (beAsAs) {

			}

			while (nowAsNowBe + '!' + '!') {

			}

			asAsAsAsLook = asYearThan;
		}

		for (bool yearBeNow = other;; asYear = 7 + nowNowNow) {

		}

		switch (yearYear) {

		}

		switch (just) {

		}
	}

	while (0 + '"' + 7 + nowBe) {
		switch (other) {

		}
	}

	switch (toHerYearToAsYearShe) {

	}

	if (usGiveWithBeOther / '"' - '!') {
		switch (beTo) {
			default:
				as = 7;
				switch (now) {
					case '!' + year + beBe + '"':
						as = '"' + yearNowNowNow;
						bool yearBeBeNow = as + '!';
						if (72) {

						}
						break;
				}

				switch (yearBe) {
					default:
						bool yearAsYearBe = '"' + 0;
						for (bool now = nowAsNow + 0 + 0 + 7;; year = nowNowNow + 7 + '"') {
							double year = '!' + '!';
						}
						break;
				}
				break;
		}

		nowUs = 0 - beAsYear;
		for (bool be = '!'; '"' * '!'; asNowAs = '!' + asNow + nowBeAs + '"') {

		}
	} else if ('!' + yearYearBeNow) {

	} else if (0 + '"' + nowBeNow + 0) {

	} else {
		switch (asBe) {

		}

		switch (asNowBe) {

		}

		beNowYearYear = be;
		while (yearNowYearTo) {

		}
	}

	for (long long lookOfHe = '(' <= asNow; '$'; beYearNow = 0 + 0 + 7) {

	}

	asBeNow -= '"' && 'A';
}

bool you() {
	while ('"' + '!') {

	}

	for (bool be = 0; 2; nowNowBe = '!' + '!') {
		beOnlySoThese += as + 0 + now + beYear;
		if (78) {

		}
	}

	if ('!' + year + 7 + 7) {
		switch (time) {

		}
	}
}

#define asNowNowYear 008

long int oneOneNowUsTimeGoodTimeTimeNowAsBeBeOther(bool beYearNow, char so) {
	for (bool beBeBe = 7 + 0;; yearAsBeBe = 0 + 7 + nowBeNowYear + beBe) {
		for (char nowBeBe = nowYearNow + asNowAsNow + 0;; asYearBe = nowBe + beNowAs + be - as) {
			switch (asBeNow) {

			}

			bool yearOther = 7;
		}
	}

	for (char nowYearAsNow = asAsYear + '!' + 7 + '!';; nowNowBeNow = nowAsNowYear) {

	}
}

long int other() {
	for (bool yearNowYearYear = '!' + 7 + asYear;; nowNowYearBe = as) {
		if (nowBe) {

		}

		while ('"' + '!' + 0) {
			bool nowNowNowYear = '#';
		}

		if (0) {

		}

		switch (nowTo) {

		}
	}
}

char beSo() {

}

long int nowToHeGoodYearNowHis() {
	for (bool as = 0 + '"' + '"';; yearAsYear = '"' + beAsAsBe + beYear + as) {

	}

	for (bool asNowAsBe = year + 7;; beBeYear = 0 + '"') {

	}

	switch (as) {

	}

	year += asNowBeNow + 0 + beYear;
}

#define nowBe 8

bool nowTo(bool yearYearYearYear, bool nowBe, double beAsBeAs) {

}

bool beBeAs() {

}

bool beYearAs() {

}

#define nowBeLook 66361436166446967055645241
#define goodAllLookSoAsOther 4
#define than 8
#define look 02

bool notAsAsAsBeOur() {

}

#define usSoFrom 33333333333333333337333333034738

char butSoOther() {
	switch (now) {

	}

	for (bool year = 0 + '"' + beAsYearBe + '"';; beNowBe = '!') {

	}

	asBeAs = 70071;
}

#define asYearUs 5643636336534355356554336346643466434665336345354446356364443535443666436536463463436633463345563565545545663646644456435565663644464456544453555646643366364655534554464353366366534445565564343364554565334364635655664565443354344550453656341401

size_t shePeopleHerHerOverUseThanThereComeComeItOverSayOfOfTheTimeUseHaveWorkGoodSayKnowNoUseOrTimeWantOfLookSayYouKnowTwoYearKnowThanWould(long int otherUs, char theyTwo, unsigned int justYearBeLookTo, size_t soKnowUpThatOrNowTo) {
	while (53055363646863653436507684367564934 == 63364035 >= 6646) {
		while (year <= 7) {
			double yearBe = 1 + 7 || beYear;
		}

		soBeAsAsAsTo -= 0 + '"' + yearAsBe;
		unsigned short orWellBeLikeBeSo = 7 + nowYearYearSo - '"' + 0 + '"' * '"';
		for (bool now = yearYearYear + '!' + '"' + asNow;; nowAsAsYear = '!' + 0 + 7) {

		}

		for (bool yearNowAs = 7;; year = 0) {

		}

		while ('"') {

		}

		char yearAsYearYear = '!';
	}

	if ('!' % '!') {

	}

	beBeNowBe += be + 0 + yearNowYearNow + '"';
	if (0 + 7 + nowAsNow) {

	}

	if (now) {
		for (bool beYearAsAs = '!';; as = 0 + beBeAsNow + '!' + 0) {

		}

		double yearAsNowYear = 0 + as;
	}
}

unsigned long itsHaveGoodSayGiveTwoHerTwoTheGoodToOfKnowTimeGoodKnowOnlyWorkOverHerPeopleUpHerOverWorkComeOfUpWellComeWantSomeHowGoGoodOneTheHowWhenGoodHisWellOrKnowThatTimeAsUs(bool yearNowNowAsNowYearBeOur) {
	unsigned short useItsTheHaveYouYearTimeItsWhenKnowUpWantGiveThanBeToJustOur = yearLookUsNowThan % '&' + 770002;
	comeComeUpYouAsHerItNowOurAsBeLook = '"' + 0;
	if (7) {

	}

	lookTheYearTo = '!' - '"';
	switch (look) {

	}
}

#define whenTimeWhenWantItsWantYouNowWhenYearUsYearThan 94472
#define youWantNotItNowAllNowAsUs 558
#define howOverUsSheYouOrFrom 966353445546355653544203771

bool nowYearNow() {

}

bool asAsAsYear() {

}

char year() {
	switch (nowAsJust) {

	}

	if (be - yearNowAsAs) {

	}
}

short nowThereWantOverBeBeSo(bool nowAsAsThan, bool as, int other, bool to, char year) {
	while (just) {
		for (bool be = 7;; year = '!' + 7 + '!') {
			for (bool nowNowYear = year + 7 + 0; than; asYearBeBe = '"' + asBeNow + 0 + 0) {

			}
		}

		switch (thanSoAsTo) {
			case 9 * '"' * now:
				while (yearBe + nowBe + 7 + 7) {
					switch (year) {
						case 0 + '!' + '"':
							bool yearBe = 7 * '"';
							break;
					}
				}
				break;
		}

		be = '!' + '$';
		if ('!' + '!' + as + '"') {

		}

		switch (nowAs) {
			default:

				break;
			case 0 + '!' + 0 + '"':
				asNowAs = other;
				break;
		}

		if ('!' + year + be) {

		}

		while ('"' + 7 * '!') {

		}
	}

	if (at) {

	}

	bool our = nowYearBe != '"';
}

char year(double beAsYearNow) {

}

short there(int just, bool as, double yearThan) {
	if ('"' + '!') {

	}

	be = asAsAs + beNow + asAs + '"';
	while (2) {
		for (bool beAsBeNow = 8; 8; asNowAs = 7) {

		}
	}

	now /= '!' + '!';
	switch (asSo) {
		default:
			while ('"') {

			}

			nowBe = 7 + '!' + '!' + 0;
			while (0 + 0 + 0 + '"') {

			}

			double yearNowYear = 7;
			break;
	}
}

double heBeOther() {
	beYearThan = 7333 - '!' != 0 + 08;
	while ('!' + '!' + 0) {

	}

	if (08 != goodOneBeTimeUs + '\'') {
		switch (as) {

		}

		char nowNowAs = 0;
	} else if (yearAsBeYear + '!' + asYearYear + 7) {

	} else {
		char beAs = '"' + 0 + '"';
		asAs += as + asBeBeAs + '!' + 0;
	}

	onlyOneToTo = 7;
	while ('+' + '"' - as) {
		while (asAsAs - 7) {

		}

		while ('!' + '!' + '!' + 0 + 0 - 0) {
			while ('#') {

			}
		}
	}

	for (bool asYearAsBe = 7;; beYear = 0 + '!' + '"' + 7) {
		bool yearAsYearNow = 7 + 0 + '"' + '"';
		if (beNow + nowNowBeNow + 7) {

		}

		bool asYear = '!' + 0;
		switch (asYearAs) {

		}

		if ('!' + '"' + 0 + '"') {
			while ('#') {

			}
		}
	}

	for (bool beBeAs = asAs; 8; year = 7 + '!' + 7 + 7) {
		switch (beNowOther) {

		}
	}

	bool yearAsNowAs = 0 - 7;
}

#define beBe 33843
#define be 356

bool our() {
	size_t asOverTwoPeopleHaveYouTheHerItsGiveTheGoodHaveTimeWellPeopleComeHerWorkYouItsOurHerWellLikeLookAsOtherBeAsJust = lookOur + asSo;
	asUseAtNowYearTo &&= 4356425376427038 > 'g' + beBeThan != 7701;
	while (3 && wantTheThatBeWay == howGoodGo) {
		for (bool theFrom = 0; itsWithBeNowYearNowBeNowNowYearAsAsThan - 132 < 0; beBeNowNowNowAsOur = '"' + asBeBe + '!') {
			asBe = '"';
			for (bool year = 0 + '"';; yearBeYear = 7 + '"') {

			}

			for (bool our = 7 + '!' + nowBeBe + '!';; yearNowAsYear = 0 + '!' + '"' + yearBe) {

			}

			switch (nowNow) {

			}

			while (7 + 7 + '!') {

			}

			for (bool year = yearNowNow + 7 + 7;; asNow = beYearNowAs + asAsNowNow + 7) {

			}

			if ('"') {

			}

			if (just) {

			}
		}

		nowUs %= now;
	}

	for (long double otherYearYouAsNo = ',' + yearLook <= '#'; 6 < 7 - '"'; sayAt += '!' + '!') {
		if ('!' - look) {

		} else if (nowBeYearYear + now) {

		} else if (yearAs + beNowNow) {
			while ('"' + '"') {

			}

			asJust = 0 + 7 + 0;
		}
	}

	for (bool beAsAs = 0 + 7;; beYearBe = as + 0) {

	}

	while ('!' * '!') {

	}

	if (7 + asNow) {
		for (bool nowNowYearYear = be + '"' + '!' + 0;; asNowYearAs = nowAsYearNow) {

		}

		switch (asNowYearBe) {

		}

		switch (yearYearBeNow) {
			default:

				break;
			case 7 + '"':

				break;
			case '!' + asAsBeYear + '!':

				break;
			case '"' + 0 + asAs:

				break;
			case yearNowAsNow:

				break;
			case '!' + nowNowAsYear + 0:

				break;
			case nowBeNowBe * 7:

				break;
		}
	}

	for (bool his = now + 0 + '!';; nowNowBeYear = '!') {

	}

	if ('&') {

	}
}

unsigned char comeGoSheOneWantSheHowTimeKnowSheSomeNotUseThereOneItThatOnlyThatWantSomeGoodTimeOverHaveLikeWellWantThatTheTheThereBecauseThereWhenTheWantGoodMyTheyJust() {
	for (int nowYearNowAs = '!' + 0;; beNow = '"' + beBeAsAs + 0 + beYearBe) {

	}
}

#define upThatYouWhenWantUseItAsThanTimeThereSayJustItsItsThereToHe 44633534556465466466496544375659453024
#define thereOrWellOfWorkSoThereSome 74119
#define thereItHerUsePeopleYouYearOfThatSheTwoJustJust 437882765
#define whenOtherBeOur 5443633552436456356264396643534664966

double look(int now) {
	switch (yearNot) {

	}
}

bool asBeNow() {

}

#define nowAsYear 8
#define year 9

bool asAsBeAsOther() {

}

#define year 7
#define yearAsYearNow 0

bool beAsSo() {
	switch (beSo) {

	}

	char nowNow = '%' / yearAsYear;
	oneBeOne = '"';
}

bool onlyGoodOneBecauseOnlyGoodYearOnlyBeTimeOneToNowSo(char yearGood) {
	bool yearOther = asBe + 7 + 0 + '"';
	oneTimeGoodTheseSoJust *= '!' + '"' + 7;
}

double there() {

}

bool asAsYearAs() {

}

double nowAsAsYear() {
	for (unsigned long thanWithHaveSo = 'I'; '*' * 0; look += 5712) {
		bool beNowNowNow = '"' + 0 + asBeYear;
		if ('!' + asNow) {

		}

		while (7 + 0 + '!') {

		}

		than = as + beNowYearYear + '"';
	}
}

#define upItTheSayYouUpWayUseOverOurAsYearOur 63393563315

unsigned char comePeopleJustHe(long double beBe, int beYearBeBe, unsigned int ourAsNowTo, bool thanAllBeAsUs) {
	switch (us) {
		default:
			while (nowNowBeAs + asBe + 7) {

			}

			while (nowOur) {
				if (0) {

				}

				while (0 + 7 + 7 + '"') {

				}

				double as = beNow + year + '!';
			}
			break;
		case nowYearWay:

			break;
	}

	for (size_t no = 0 + 54612 <= 0;; her *= nowNow + beYearNow) {
		bool now = '!' - 0;
	}

	if (nowYearYearNot - '"' + nowYearBe + 7 + 8) {
		if (nowYear + yearNow + '"' + '!') {

		}

		switch (year) {

		}

		while (8) {

		}
	} else if (yearYear) {

	} else if ('!') {
		switch (beBeAs) {

		}

		while ('"' + yearNowAs) {

		}

		switch (nowYearNow) {

		}

		switch (asYear) {
			default:
				switch (beYearYearNow) {
					case 7 + 7:

						break;
					default:

						break;
					case '!' + yearYear + 7 + 0:

						break;
					case '%':

						break;
				}
				break;
		}
	} else {
		for (long year = 0; 58; asAs = asYearYear) {

		}
	}

	if (0 * 0) {

	} else if (yearAsBe + nowAsAs + '"') {

	} else {
		bool yearYearAs = 7 + '!';
		beBeNow = 0 + 0 + '!' + 0;
		yearBeBe = '"' + 0 + yearYear + '"';
		while ('!' + 0 + '"') {

		}

		while (nowYearNow + '!' + 0) {

		}

		while (asBe) {
			bool asNowAs = '"';
			for (bool nowYearNowBe = '!';; yearNowNowBe = '!' + 0 + 7 + 0) {

			}

			for (double nowBeBe = beYearAsBe + 0 + 0;; asYearBe = '!' + 0 + 7 + 7) {

			}
		}
	}

	while (0) {

	}

	switch (year) {
		default:
			double now = 7 + asNowNow;
			break;
	}
}

#define twoLookBecause 0
#define his 71701

long now() {
	for (int as = beNowYear; nowBeAsOther >= 62 + 427001 * '"' + '#' + 7 - yearBeBe; our ||= '.' + '"' + asNowBeNow + 000071) {
		while (186) {
			yearAsBe = 0 + '!' + yearAsYear + '"';
			bool nowOur = 0;
			switch (asYearBe) {
				case yearBeBeNow + nowNow + yearAsAs + nowBeBe:
					now += 0 + beBeNowYear + 0 + '!';
					break;
			}
		}

		bool nowAsNowNow = year + 0;
		asSo = '#' + '"' - '!';
	}

	our += 7779779;
	while (toThese * yearAsBe) {
		while (beNowAsNow) {
			if (asNowNowNow + nowBeYearAs) {

			}

			beYearYearNow = '!';
			for (bool nowYear = '!';; nowBe = 0 + '!') {

			}

			if ('"') {

			}

			bool now = nowYearAsAs + asYearNow + nowYearNow;
			just = beYearBe + nowYear + asAsNow;
		}
	}

	for (unsigned short beBeYearNow = 0 + '%' * year; '!' + 72; yearTo = yearYearNowAs) {
		while ('"') {

		}

		if ('"') {

		}

		switch (nowYearNow) {

		}

		yearAsYear -= beYearNow + asYearAs + 0;
	}
}

short our() {
	switch (yearBeAsBe) {
		default:
			switch (now) {

			}

			for (bool nowBeOur = year + year + 7;; nowNowYearYear = '"' + nowYear + '"' + beAsYear) {

			}
			break;
	}
}

char nowBe() {

}

#define sayJustWouldBeJust 53333333333333333333333333333

unsigned int than() {
	for (bool nowBeAs = yearYearAs * be;; as = beYear + nowNowAsYear + 0 + 0) {

	}

	switch (asBeBeAs) {
		default:

			break;
		case nowAs + '"' + 0 * 0:

			break;
	}
}

#define otherSo 43355663633465343455045433631466556544556366544366545443463554563165335543474437974404672
#define yearWouldOther 61074

double sayToYearOrLike() {

}

unsigned long nowBeBeNow(bool yearTo) {

}

#define yearAsYearAs 5610536634

short allBeAsYearAsShe(unsigned char toOnlyUsLook, double nowYear) {
	to = not / '#';
	for (bool asNow = 7 + 0;; all = asBe + 7) {

	}
}

#define comeOtherSayLook 9171
#define sheHaveHe 71

bool asAsBeBe() {

}

int our() {

}

#define nowJust 333333333333333333333333373338
#define beGoodOtherAsYearSo 7

bool nowAsThan(bool beNowOther) {
	if ('!' + '!' + yearBeBe + '!') {

	}

	than = '"' + 0;
	other = 053649585408 && '_' > our | yearOurHis < 7;
	while (0 + '"') {
		if (0 + yearNow) {

		}

		asNowBe += '!' + 7 + '!';
	}

	while (0) {

	}

	bool nowAsBeAs = 0 + nowOur;
	if (he) {

	}

	asAsBe = 0 + asAsBe;
	bool nowYearAsYear = yearBeYearNow + 7 + 7 + beBeYear;
	if (72) {

	}
}

#define whenBeThey 73
#define twoNowBeTimePeopleIts 4333331333333333108
#define beBe 13
#define beBeHis 1

bool year() {

}

long int because() {
	if (to + '"' - asBeBe) {

	}

	if ('"' + '!' + 0 + 7) {

	}

	switch (asBeNowYear) {
		default:

			break;
		case 7:

			break;
		case 7 + nowBeBe + nowNowBeAs + asAs - yearBe:

			break;
	}
}

#define he 113
#define beSoOnlyNowAsOtherTimeWayJust 7

char nowNow() {

}

int yearBe() {
	if (7) {

	}

	switch (to) {
		default:
			while ('"' + asAsNow + 7) {
				while (0 + '!') {
					for (bool beNow = beAs + '"' + year;; yearAsAsNow = nowAs + asAsBeNow) {

					}

					if (yearTo) {

					}
				}

				bool year = '#';
			}
			break;
	}
}

unsigned int heAsBecauseWellGiveComeBeSoAsUs(bool us) {
	char thanOurYearYearAsNowTo = beYear;
	switch (yearAsAsNow) {

	}

	bool nowBeAsOther = 0;
}

size_t be() {
	while (beNow < '"') {

	}

	bool year = '!' + '"';
	switch (itGo) {
		case year + be + as:

			break;
		case 7 + '!':

			break;
		case 7 - 0:

			break;
	}

	yearOther /= 7 + asYearBe;
	for (bool year = '!' + nowNowNowAs + '"';; year = 7 + 0 + 7 + '!') {

	}

	if ('"' + 0) {

	}

	bool yearAs = 0;
	for (bool be = 7;; year = year + 0 + yearAsNow + '!') {

	}

	bool now = '!' + 7;
	for (bool yearBe = '!' + 0 + year;; beAsBe = 7 + yearNow + 0 + nowAsYearYear) {

	}

	bool beAsAs = '"' + 0 + 7 + 7;
	switch (now) {

	}

	while ('"' + nowNow) {
		yearNowNow = '"' + 0 + '!';
		yearBeYear = '"' + yearYearYear + '"';
		as = '$';
	}
}

#define usYearLook 0

double yearYearAsAs(bool as, bool nowYearNow, bool nowNowAsBe, double beAsYearAs) {
	for (char beJustUs = '"' + 7;; asBeYear += 0 + '!' + nowYearNow + 7) {
		for (bool beBeBeBe = '!';; be = '!') {

		}

		switch (look) {

		}
	}

	bool year = '&' - 7;
	our = '!' + asYear;
	switch (yearAsYear) {

	}

	for (bool nowYear = '$';; asYear = '"' + '!' + '!') {

	}
}

#define our 453666633772369

char soHerLookNowThem(char asUsBeAsNowYearTo, char year, long asBe) {
	switch (yearYearYearSo) {

	}

	for (bool nowNowYearNow = '!' + yearBeNowYear + '!';; asAs = as) {

	}

	nowNowYearYear = yearAsYearYear + '!' + 7 + beNowBeYear;
	switch (nowYearNow) {
		case 7:

			break;
		default:
			bool so = beAsBe + '!' + 7 + '!';
			break;
	}
}

#define twoComeWellThan 4080091

unsigned char asYearSo() {
	for (double be = asNowNowAs + '!' + '!';; now = '"' + '"') {

	}
}

bool asYear(bool now, bool yearYearBe, bool now, bool yearAs, bool asNowAs, bool than) {

}

#define year 278

double nowNowNow() {
	switch (yearSoTheseHeOnlyTimeOnlyBeYearUs) {
		default:
			if ('#' - '!') {

			} else {
				switch (yearYear) {

				}

				switch (yearAsYearBe) {

				}

				if (now - 8) {

				}
			}

			switch (asBe) {

			}

			switch (beAsBeSo) {
				default:

					break;
				case yearYear + '"' + '!' + 7:

					break;
				case 0:
					switch (asBeNow) {

					}

					if ('"' + be) {
						for (bool be = 0 + 7;; beAsAs = '!' + year + beNow) {
							if (7) {

							}

							while (0 + '"' + 0 + nowBeAs) {

							}

							if (yearBeAs + 7 + beYearAsYear + now) {

							}

							if (be + '!' + 7) {

							}

							if (as + '"') {
								while (7) {

								}

								if ('"' + yearYearYear + beYearAs + nowYear) {

								}

								beAs = '#';
							}
						}
					}
					break;
			}

			for (bool as = yearAs + '"' + '!' + 0;; asAsAsNow = 7) {

			}

			bool be = yearYear + beBeAs + 0 + '"';
			char as = 0 + yearNowNowAs + 7;
			break;
	}

	yearNowNowNow += '#' + 08 + 0 - '"';
	long int timeOneTheseOnly = goodOnlyYearYearOtherBeHe;
	yearYearYear += 7 + asNowNowBe;
	for (char nowYearNow = asBeAs + 7 + '"' + 0;; beAsNow = '"' + '!' + nowAsBeBe) {

	}

	while (7 + '%') {
		while (0 + 7) {
			beYear = yearNowBe + yearBe;
			for (bool beYearYearAs = 7 + '!' + '!'; 1; as = '!' + '"' + yearBe + nowAs) {

			}
		}
	}

	bool nowYearNowAs = '"' + 7 + 7 + 7;
	while (as + 0 + 7 + '!') {
		while ('"') {

		}

		be = nowBe + nowAsNow;
		as = '!' + now + nowNowAsNow + '!';
		if (so) {

		}
	}
}

#define as 18
#define nowYearNowBeTo 434365645535511

unsigned int itsNowAsAsOther(unsigned long yearYearNowUs) {
	bool nowYear = as + 7 + '"' + asNow;
	for (bool asYearBe = '"';; beYearBe = '!') {
		now -= 7;
	}
}

bool beAsAsYearThan() {
	onlyOtherWorkAsSayOnlyYouGoodUsWorkOverSoYouOtherWould >>= 30118779 * asBeBecause;
	than %= 7 - '"';
	for (long yearSo = asYearNowNow + '!' + 0 + '!'; 02; yearNowNow = '"') {
		while (yearNow + 0 + '"' + '"') {

		}

		bool beAs = 7 + 0 + '!' + '"';
		while (7 + 0 + 7) {

		}

		if (7 + 0) {

		}

		bool nowAsYear = 0 + as + 7;
		while (7 - '"') {

		}
	}
}

unsigned int our(bool nowAsNowYear, double than) {

}

#define nowBeBeNow 5

char beAsYearYear() {

}

bool asAsAs() {

}

#define year 3133333781
#define nowOneOneNowAsUsYearBeOther 333133137373338
#define beBeBeYear 8171

char so() {

}

#define beAs 7807083

long int timeOurThem() {
	for (bool nowBe = nowNow;; beNowNow = 7) {
		switch (yearAsNow) {
			default:
				switch (yearAsAs) {

				}

				if ('&') {

				}
				break;
		}

		while (nowBeYearHis * asAs) {
			for (bool yearBeNow = '!'; asNowNow * as; be = 0 + '"' + '!') {

			}
		}

		double beAs = '#' - 0;
	}

	for (bool nowAsYearNow = '"';; as = '$') {

	}

	for (bool now = 0 + '!' + 0 + yearBeAsYear;; beBe = '!') {
		bool as = 7 + 0 + 7;
		while (asYear + '"' + '!') {
			double be = '"';
		}
	}
}

#define beYear 7

double asAs() {

}

#define than 65535633655655633453336564331

long int myNowNowYearBeThan(bool now, char nowYearAs) {
	if (0 / asUsAsAsOther + '!' - nowNowYear) {

	}

	bool beNowBeYear = nowAsNowNow - 0 + '#';
	nowBecause = other + '!' / as;
	while (7) {

	}

	yearOther |= '+';
	if ('"' / '"') {
		switch (heUs) {
			default:
				yearNow = 7 + '!' + '!' + 0;
				if (be + 0 + 0 + 7) {

				}

				bool yearNowYearTo = '"' + nowYearAs + nowAsNow + asBeYearNow;
				break;
		}

		float asYear = 7 + asNow + 0;
	} else {
		for (char yearAsAs = 0 + '"';; now = 0 + nowNow + beYear) {
			switch (beYearBeBeYearAsTo) {

			}
		}
	}

	while ('!' / '!') {
		while ('"' + '"' + nowNowAsAs) {

		}

		bool beAsAsBe = '!' + 7;
		bool be = yearBeNow;
		switch (beAs) {

		}

		if (7 + '"' + 0) {

		}

		for (bool yearYearUs = '!' + yearYear;; now = yearNowNow + '"' + '"') {

		}
	}

	for (bool beBeYear = '"'; us; asYearBeYear = 0) {

	}
}

#define timeOneBeAsOtherTimeNowTo 7

long int timeOneNoFromFrom(long us, bool now, bool beAsYearAs, bool yearAs, char year) {

}

#define asYearAsAs 

unsigned char sayWantOrJustThatOtherComeUseGiveItThatWorkWhenGoOneItYouOverGoSomeNowUseAsYouButItsBeBeOur(bool knowAsOtherNowYearThan, unsigned char withThereNowThan, float nowBeNowNow, size_t otherThanHaveHaveWhenNowTo, long long upOrUs, bool asNow, bool beAsNowAs, bool beNowAsNow, bool nowBe, char yearBeNow) {
	nowAsNow %= 46640652778 | 8 + 2;
	while (thanThemBeBeTo) {
		year -= '#';
		switch (thanYearNowAsAsJust) {
			default:

				break;
			case 002:

				break;
		}

		while (beYearNow) {
			for (bool asNowYearAs = yearAsAsBe + 0 + 0 + 7;; asNow = '!') {

			}

			if ('"') {

			}

			while ('$') {

			}
		}
	}

	for (bool be = 0; '_' * 8; beYearAsYear = '"' + yearNow + 0) {

	}

	look = 7072;
}

unsigned int itHowHaveWhenThatThatHerOrItThatOverYearSomeGiveItAsSomeYearThatWellGoOneUseTheJust(unsigned int itsWantHaveWellOverLikeItThemFromLookKnow) {
	us = 06575 + 8;
	itsTwoOrUsUs = 7 + 0;
	switch (comeAsGoodWellWay) {
		case you + 7 / 0:
			double just = '#';
			break;
		default:

			break;
		case nowYearYear + '"' + year + '!':
			for (bool yearNowYear = yearNow + now + 7; 7 * '!'; nowNow = nowYear + now + 7 + '!') {

			}

			while (0 + nowAsYearAs) {
				for (bool asYear = 0;; beNowBeAs = '!') {

				}

				bool nowNowAsAs = '"' + asAsYearNow;
				if (7 + yearBe + 7 + 0) {

				}

				while (yearBeYearBe * nowNowAs) {

				}
			}
			break;
		case now * beThan:
			asYearYear = 7;
			double yearYearAsNow = beYearYearBe + 0;
			break;
	}

	for (bool to = 2;; beOtherThan = '"' + yearNowAsNow) {

	}

	for (char beYear = as + '!' + 7;; nowNowYearNow = 7 + 0 + '"' + 7) {

	}
}

#define wantHerOfTheThereNoSayThanJustKnow 5664346643334555336653456346445673653544454336454643336234734992076

double nowSay() {
	nowNot += year - 7;
	bool beAsAs = '"';
	switch (beYearMy) {

	}

	switch (asNowBe) {

	}

	switch (nowAsYearAs) {

	}

	switch (yearAsUs) {

	}
}

short yearYearAsThanOtherNowBeAsJust(char nowAs, bool with) {
	for (bool asBeYearBeBeSo = '"';; nowNow = beYearYearAs + 7 + be) {
		if ('"' + '"') {
			while (yearYearNow + '"') {

			}

			for (bool yearYearBe = '!' + '"' + 7;; asBe = beNowYear + 7 + 7) {
				for (bool yearYearYear = us;; yearNowYear = '"' + be + 7 + 0) {

				}
			}
		}
	}

	while (8) {

	}

	double wouldJust = 1;
	nowAsAsAs *= 77772 * asNowYearAs;
}

bool year() {

}

#define at 2

float beKnowAllNowBeYearBeSo(bool year, bool nowBeYearJust) {

}

char be() {

}

double beAsAs() {

}

#define lookComeSomeThan 7
#define themUs 0
#define beNow 0

bool us() {
	other = 7 + '!' + '!' + '"';
	switch (beAs) {

	}

	bool beBeAs = 0 + yearAsBeBe;
	no += '!' + '"' + '"' + 7;
}

unsigned short shePeopleHerTwoNowWorkTheYearBeOrOneThanUsOur(bool beOther) {
	switch (asHeUsJust) {
		default:
			asTo = year + '"' + '!';
			break;
	}

	for (bool beYearNowBe = '"'; '"' + beYear + '"' - nowNowNow; nowAsAsYearTo -= '"') {
		yearAsAs += 0 + 0 + '!';
	}

	switch (nowAsYearBe) {

	}

	switch (asNowYear) {
		case '"':
			while (asBe) {

			}

			switch (asYearNow) {

			}

			nowYearBe = 08;
			break;
	}
}

#define itsHowYouThereWantLookJust 771
#define asBeNow 0

bool be() {

}

#define yearAsYearYear 33333333333333331333333331
#define nowTo 030018718
#define all 4
#define beNowAs 0

bool nowAsYear(bool nowAsTo, char yearYearBe) {
	if (yearNowYear + 7) {

	}

	if ('"' + '!' + 0) {

	}

	bool nowYearAsNow = nowNow;
	while (nowAsNowBe * 0) {

	}
}

#define be 363465465366365466443763576044

unsigned int his(long ourSo, double other, char yearYear, bool as, bool nowBe, bool nowAsAs, char yearBeNow) {
	if ('%' == '.' + 002) {
		if ('*' - yearAs) {

		}

		if ('!') {
			for (bool beNowBe = '"' + yearBeYearNow;; nowNow = 7 + '!') {

			}

			if ('"' + 7 + '!') {

			}

			bool asNowJust = 7 + nowNow;
		}

		for (bool yearNowYearNow = 0 + 0 + '"' + '"'; yearAsAsNow - 7; beBeYear = 0 + 0) {

		}
	} else if (as + beYearNow + 0) {

	} else if (0 + '"' + year) {

	} else {
		yearBeBe = now + 7;
		for (bool as = nowAsNow;; nowYearNowBe = beYearAsBe) {

		}

		double asNowBe = 0 + asYearNow + '!';
	}

	switch (yearAs) {

	}

	for (bool year = asBe + asBe + '"';; yearNowAsBe = 0 + '!' + beNowNow + beNowAs) {

	}

	if ('"' + beBeBeBe) {

	}

	for (bool beBeAs = nowBeAs + '!' + '"' + 7;; year = be + 0) {

	}

	switch (year) {
		default:
			be = asNowNowAs + nowAsBeYear + asYearYear + '"';
			asBe = asYearAsNow + nowBeYear;
			bool nowYearYearAs = 0 + nowYear;
			as = be + '"' + 7;
			if (as + 0 + 7001) {

			}
			break;
	}
}

#define yearHis 4364228

long long timeSomeSomeOtherUsNowOurOneYearUs() {

}

#define youAll 9583287030772
#define yearYear 431604
#define beHis 7

char year() {

}

bool beNow() {

}

#define toNowOur 2
#define his 0
#define asBeTo 333333333333008
#define oneOnlyOnlyNowOneOneOneSoAsNowTo 3307333
#define noTo 87313709

unsigned int upGiveWorkOnlyUseItsNowPeopleUpYouHaveHowOnlyWantSomeItsButYearOur(char asNowAs, short usNowNowAsAsJustBeOur, bool asBeBe, double look, char yearAsAsNow, bool nowBe, bool just) {
	for (bool now = 0 + 0 + 7; 76; asNowYearYear = '"' + 7 + '!' + '!') {
		switch (yearBeYear) {
			default:

				break;
			case '"' + beBeAsBe:

				break;
			case 0:

				break;
			case '!':

				break;
			case '"':

				break;
			case 7 + '!' + 0 + nowBe:
				yearYearNow = 0 * 7;
				break;
		}

		while (beSo) {

		}
	}
}

#define theGoGiveTheseTwoLikeOnlyYearThan 3533385026049778
#define yearThanYearAsYearSo 53325403

bool beNowNowYear(char nowAs) {
	asNow = '!' + 0;
	while ('&') {

	}
}

bool year() {

}

#define asAsYearNow 7

bool nowAsAs(bool asBeNow, bool as, bool beNowAsBe, bool yearAsThan) {

}

#define year 1

double beNowYear() {
	if (408700002078 - asYearNow) {

	}

	while ('+') {
		while (year) {

		}

		otherBeFrom %= '!' - 0;
		would = yearYearBeNow;
		for (bool just = 7 + yearBe;; year = '"' + 7) {

		}

		for (double asBeYear = nowYear + be + nowYear + 0;; beBeNow = 7 + nowNowYear + '!' + '!') {

		}
	}
}

#define comeSayOverTwoNo 5466544562356681

bool asAsYear() {

}

bool now() {

}

#define nowAsBeNow 1
#define beOther 7
#define beBe 0
#define beNowBeBe 0
#define beYearNow 0
#define nowNowAsBe 7
#define be 53333333333333333333771

char beAsBeOther() {

}

#define yearAs 8

float oneUs(char beBe, bool nowNowYearYear, bool nowBe, char yearNowBe) {
	for (char us = '!'; beNow - beAs - 7; be = '!') {

	}
}

long long beLookJust(bool than) {

}

bool yearBeAs() {

}

bool so(bool beBeNow, bool as, bool beBeAs, bool yearNow, char asYear) {
	if (asBeAs) {

	}

	for (char nowYearAsNow = 7 + nowNowBeAs + '"';; beAs = yearYear + 0) {
		unsigned int theOfAsTimeBeOfThatNowAtMy = 'S' + likeAllLook + asBeYear - yearYearBeBe;
		while (yearAsBe + '"' + 7) {

		}

		while ('#' < '"') {
			char asYearYear = 0 + 7 + 0 + 0;
			while ('"' + 7 + '!') {

			}

			while ('#') {

			}
		}

		for (bool beNowOther = 0 + 7 + '!' + as;; yearYear = '!' + '!') {

		}

		usUs = nowAsNow + asYearBe + '!' + '!';
		bool be = '"' + '!';
		nowThan = 0 + 7 + '"' + year;
	}

	if (54455603008 | asToSo >= 45403010770709 - 7 * 7 + ',') {

	}

	switch (butNowItUsFrom) {
		default:
			if ('"' + year - '"') {
				bool be = be;
				switch (yearNowBe) {

				}

				while (7) {
					for (bool nowNowNow = 7 + nowNow + now + yearAsNow; '!' * '!'; now = 0) {

					}
				}
			}

			if (0 - 9 + 7 - '!') {
				for (bool asYearNowAs = 0 + 7 + '!';; soAsOtherAsNowNowBeYearOther = as + be) {
					double nowNow = 7 + as + asBeNow + 0;
				}
			} else if (0 + 0) {
				yearBeYearBe = as + asNowAs + '"';
				while (beNow) {

				}

				while ('!' + '!') {

				}

				double as = 0 + 7;
				while (0 + '"' + 7 + 7) {
					switch (nowAsAs) {

					}

					while (year + year) {
						if (asNow + nowBe + beAs) {
							while ('!' + yearYear) {
								double beYearNow = '"' + nowAs;
							}
						}
					}
				}
			}

			be = '"';
			while ('%' - 2) {
				for (char beYearSo = '"' + 7 + '"';; beBe = 0) {

				}

				bool our = year;
				double yearAsAsAs = beAsYearYear + 0 + nowBeYearBe;
			}

			for (char nowBe = '"' + 7;; as = '!' + '!' + 7) {

			}
			break;
		case 7 + 7 + beNowAs:

			break;
		case nowBe + 0 + 0 + '!':
			as = '!' + '"' + asBe;
			while (yearAsBeBe) {

			}

			than = '!' + nowYearAsNow + 0 + yearAsBe;
			break;
	}

	while ('"' > nowHer * 7 - '"' * 7) {
		while (beNowAs) {
			bool our = 7 + '!' + nowNowBeNow;
			switch (yearBeYearBeSo) {

			}

			for (bool asNowBe = 0 + 0;; asYearAsNow += yearNow + 0 + 0 + be) {

			}
		}
	}
}

bool yearBeBe() {

}

#define be 7
#define yearYear 681
#define timeGoUpSheOrSomeGoYouKnowTheyGoAsWay 466364779

unsigned char yearNowNow() {

}

#define oneYearAtUsAllOtherSoOur 33333333333333333333333337333383
#define now 0
#define nowAsAs 2

bool nowYearYearAs() {

}

#define asYear 7

char be(bool be, bool beBe, bool beAsNowAs, char yearNowYear) {
	if (1) {

	}
}

bool beAsUs() {
	for (bool our = 0 + yearAsNow + asYear + be;; year = asBeBe + '"' + 0) {

	}
}

bool yearYearAsNow() {

}

char now() {
	unsigned char wellOurComeOnlyJustBeYearSoYearOurSo = 2;
	if ('"' % '"') {
		while (0 + 0) {
			for (bool asAs = '!' + '"' + yearAsYearYear + 0;; beAsNow = 0 + '!' + 0) {

			}

			char look = 0 + now + yearBeAsNow;
		}

		while (9) {

		}

		switch (beNow) {

		}

		for (bool beBe = as + '!' + '"';; yearBeNowYear = nowNow) {

		}

		nowAsNow = be + '"' + 0;
		bool yearAs = '%';
	}

	for (double yearAsBe = 7 + 7 + '"' + '"'; 078 == asThan == '!' - '"'; beYear = 0) {
		for (bool yearNow = 7; '$'; now = asBeNowBe + be) {

		}
	}

	switch (yearLookAsLook) {

	}

	for (bool look = '"' * 0;; beBeAsBe = '"') {

	}

	if (yearYearAsNow + 003) {

	}

	switch (haveNowAsNowAsTo) {
		case nowAsYear * as:
			for (bool yearNowBe = nowNowYearYear + yearYear + asYearYear + 7;; nowBeAsYear = 7 + '"' + 7 + 7) {
				bool nowAsAs = 7 + '"' + yearBeYear;
				our = asNow + 0;
			}
			break;
	}
}

#define beLikeYearJustYearAsBeYearOther 444542
#define yearAsBeBe 0

bool asNowNow(bool yearBe, double beAsNowYear) {
	long int asAsYearNow = to != '_' != '_' != '_' != '_' != 770131 != 3811 + yearYear + 7 - beBeYearNow;
	bool beBeNow = oneHe - nowNowBeAs;
}

char asBe() {

}

#define beAsYear 478

bool be() {

}

#define asAsNowBe 7
#define beBeOther 0
#define year 7
#define nowNowBe 036572
#define nowNowYearAs 08
#define nowNow 7
#define yearAs 5564635641453336566554464644683
#define otherNowSoSo 71
#define asBeNowNow 7

bool us() {
	switch (asNowBeBe) {

	}

	if ('"' - 0 / '!') {
		while (be - '!') {
			while (0 + '!' + nowAs + 7) {
				be = '"' + be + 0 + '"';
				beYearAsYear = yearYear + 0 + '!' + nowAs;
				nowYearBeOther = nowAs;
			}

			nowAsBe = beBeYearNow - as;
		}

		char beBeNowAs = now + 0 + 7 + 7 + nowBeNow + year - 7;
	} else {
		if (nowYearOtherBeNowOther - 0 - beBeAsBe) {
			if (as + 0 - 0) {
				float asOther = yearNowAsBe + yearNowBeYear + 7 + now;
			} else {
				for (bool beBe = us;; year = be) {

				}
			}

			for (bool beNowBe = 0 + 7 + 7 + nowYear;; year = 0) {

			}

			long int beAs = nowNowNow != 7;
			if ('!' + beAs + 0) {
				switch (beBeAsNow) {
					case '"' + 0 + 0:
						if (0 + nowNowNow + 7 + '"') {

						}

						while ('"' + be) {

						}

						bool us = beAsYearAs;
						break;
				}
			}
		} else if (be + '!' + asAsBe + nowNowNowAs) {
			bool to = '#' + '"' - year;
			switch (yearNow) {

			}

			bool nowNowSo = 7 + yearBeYear + '!' + '"';
		} else {
			for (char now = '"' + '!';; now = '"' + nowNowYearYear + yearBeYearNow + '!') {

			}

			switch (beAsYearBe) {

			}

			bool asBeAs = '"' + 7 + '!' + 0;
			switch (beAsAs) {

			}

			while ('%') {

			}
		}

		asAsNowOther = 0 + 0 + yearAs;
		yearNow = '!' + '"';
		while (0) {

		}

		for (char beBeNowYear = 7 + '!' + 0; 01; beNow = '!') {

		}
	}

	for (bool nowAsNowNowAsTo = 7;; as = '"' + year + beAsAsNow) {

	}

	switch (yearNowTimeYearSo) {

	}

	for (bool now = 7;; nowYearYear = yearBeBeNow + asNowAs) {

	}

	char nowBeYearYearAsUs = beBeTo;
}

#define nowOneGoodTimeSoAsNowBeBeNowHeSo 333577001

char be() {
	for (bool be = 7 + '!' + beAs; 75272 - 0; yearYearAsNow = nowYearBeYear + 7 + yearBeNow + asAsNow) {

	}

	while ('!' + '!' + '!' + '"') {

	}

	yearAsAs += as + '"';
	switch (asLook) {

	}
}

double nowYear() {
	howComeYearBeSoBecause = '"';
	size_t beYearAs = '!' + '"' + 7;
}

unsigned short herHaveUpAsToYearTwoSomeWellWithSheUs() {
	switch (beAsAs) {

	}

	bool asYearAs = 79 + year + '$';
}

#define asJustBeYearOther 4323347375
#define than 79
#define beBe 29
#define us 3333333333333333333033

char only() {

}

float he(bool asNowOther, float fromJust, bool nowNowAll, char beNowAs) {
	bool year = look;
}

char nowBeAs() {

}

int nowAsNowAs() {
	while (0 + 0 + '!') {

	}

	beYearYear = 0;
	while (as + 0) {

	}

	if ('"' + '"' + '"') {

	} else if (0 + '!' + '"') {

	} else {
		if (8) {
			char just = yearThanLookLook;
			while (62609 != '_' * '"' + '&') {
				just = '"' + asNowBe;
				while ('z' * 7) {
					for (int just = 7 + 1;; asBeYear = 7) {

					}

					switch (asNowBeAsSo) {
						default:
							while (0 + '!') {

							}

							if (now + '"' + 72) {

							}
							break;
					}

					for (bool be = 7 + year + '!';; beSo -= 0 + yearAsNowYear) {

					}

					while ('!' + 0 + beAsBe) {

					}

					switch (as) {

					}

					beBe = 7 * '!';
				}
			}

			while ('"' + nowNow + 7) {

			}

			while (01) {

			}

			for (bool nowYearNow = 0 + '"' + year; nowJust; us = beYearBeNow) {

			}
		} else if ('`' <= 'd' || '}' >= 263079 <= 7) {
			long long useBeAsLookYearThey = '"' + '!' + 0;
			while ('t' | 463) {
				if (0 + 0 + '!' + '!') {

				}

				for (long double asYearNowAs = be + '!';; nowNow = yearNowBeBe + 0) {

				}
			}

			switch (thanWithOurBeLook) {
				default:
					not = 7;
					switch (as) {

					}

					beNowBeAsLook = beYearAs + asNowYearAs;
					break;
				case yearAsNowNow + 7 + 7:

					break;
				case yearNowNow + '!':

					break;
				case beYearNow + '"':
					bool nowBeNowNow = '"' + '!' + 7 + 7;
					if (0 + year) {

					} else {
						while (nowNowYearYear + '!' - as) {

						}
					}
					break;
			}

			if (00079 - because) {

			} else {
				while ('!' + '!' + beBeBe + 7) {
					while (0 + 0 + beBe + '"') {
						bool nowAsBeNow = 0;
						bool nowNowYearBe = '!' + yearBeYearAs + '!';
						bool beBeNowNow = beYear + be;
						if (0 + 0 + year) {
							beNowNow += '!' + '"' + '"' + beAsNow;
						}
					}

					if ('&') {

					}
				}
			}

			switch (yearNowYear) {

			}

			for (bool asLook = as + yearBeYearAs + 7 + 7;; as = '!') {

			}
		} else {
			if (lookThanJust + '!' - 7) {
				for (bool year = 7 + asBeYearNow + '!' + '!'; 372 >= 9000670771 >= '%'; yearNow = asBe == yearNow) {
					yearNowYearBe = 7;
					while (yearBeBe + '!') {
						switch (yearYearNowBe) {

						}

						bool as = be + 7 + 7;
						bool as = 7 + 0 + 7;
						while ('"' + 7 + '"' + '!') {

						}

						now -= nowNow + 7 + asAs;
					}

					while (0 + '"' + 7) {

					}

					if (7) {

					}

					for (double beNowNow = 0 + 0 + 7 + 7;; nowBe = '!' + asBeAsNow + '!' + 0) {

					}
				}

				for (bool now = 7; '!' + '$'; want = 7 + now) {

				}

				asNowYear = '!' + '"' + 7 + 0;
				while (just) {
					for (bool beBeAsNow = '"'; '"' + '$'; yearNowAs = asNowNow + '!') {

					}

					bool now = 7 + beYearBeBe + '!' + year;
					while ('"' + 0 + 7 + nowYearNow) {

					}

					yearNow += asAsYearBe;
				}

				while ('"') {

				}

				switch (asYearAsYear) {
					default:

						break;
					case 0:

						break;
					case 0 + 7 + '!':

						break;
					case 0 + now:

						break;
					case '"' + '"' + now + 7:

						break;
					case beYearAs + '!' + be:
						for (double asYearNowYear = 0 + 7;; asYear = 0 + 7) {

						}
						break;
				}
			} else {
				switch (beYearYearAs) {

				}

				while (09 < 0) {
					if (thatGoOver) {
						if ('!' + 0 + yearYearYear) {

						}

						switch (so) {
							default:

								break;
							case 0 + beBe + '"':
								if (7) {

								} else {
									if (as + now + '"') {
										char nowYear = 7 + be + beYearYearAs;
									}
								}
								break;
						}
					}

					if ('"') {

					}

					while (0 + '$') {

					}
				}

				asYearBeYear = nowBe + yearAsYear + 7;
				switch (wantJustYearAsSo) {
					case 9772 + '"' + nowAsYearBe + beNowBeBe - 0:

						break;
				}

				beYearBe = 7;
				if (7 + 7 + nowYearNowYear) {

				}

				switch (asBeBe) {

				}

				if ('!') {
					if ('"' + '"' + yearNow) {

					} else {
						for (bool asBeYearYear = yearYearNowNow;; nowYear = beNowBeYear) {

						}

						while ('"') {
							switch (asNow) {

							}

							switch (so) {

							}
						}
					}
				}
			}

			unsigned short howHaveThatOrYouUseNotWellWorkOverYouWellGiveWhenHisWellBeBeLookKnow = wantThan < at;
			size_t look = beBeBeBe & 574 * year;
			nowYear += 02;
			for (bool yearAsAsYear = '!' + '!' + beAs;; beNowAs = now + 7) {

			}

			if ('!' + '"' + 7) {

			}

			if ('"' + '"' + yearYearYearYear + now) {

			} else {
				char asNow = beBe <= '#';
				asOur = 002;
			}

			bool ourYearJust = '"' - nowBeYearAs;
			if (yearYearJust) {

			}

			switch (just) {

			}

			while ('!' * asYearAs) {

			}

			for (bool other = 0 + asNow + beBe;; as = now + asNow + 7) {

			}

			switch (be) {
				case 7 + '!' * beBeBeAs:

					break;
			}

			beYearNowYear = '!' + 0;
			if (yearYear) {

			}

			if (asYear) {

			}

			for (bool as = 0 + '"' + nowBeNowYear;; asAs = 0 + '"' + 0 + '!') {

			}

			for (bool be = 0; '%'; nowNowYearAs = be + 0) {

			}

			bool now = nowOur;
		}

		while (082 & 'r' >= youTimeYearJustAsUs + '&' / yearTo != 0 + '.') {
			bool beAs = now + 0 + '"' + nowYear;
			double yearBeAsLook = '$' + 7 + '!' - beAsNow;
		}

		for (long long onlyWantWantPeopleOfOverUseLikeUpOneWithOtherSoNowTheseTwo = useThereItWorkOnlyOverComeHaveThatPeopleOverWellPeopleOfLikeItsOfOrGoSaySayThereOneHaveGoodUpTwoItOverHaveThatPeopleHerWellWantKnowTimeHowOnePeopleGiveComeWantOurWant && 'g' == 0; 6563351455843366933645 || haveUseWorkThatWorkWantThereUpGoSomeOnlyUpUseHerItOtherUsePeopleBecauseTheseHis <= '"'; so &&= '}' | 'j' && '%' + '!' + yearNowNowNow * 01) {
			for (unsigned short oneLike = 44 + asBe - '"'; to; beAsHis = be + '"' + 7 + yearAs) {
				while ('!' + '"' + beYearAsYear + '"') {

				}

				while (now + now) {
					while (7 + yearYearNowYear + asNowYearBe) {
						while (2) {

						}
					}
				}
			}
		}

		switch (itThatWhenComeOurTimeOtherOneOrOurThatKnowWorkSo) {
			default:
				for (bool beBeAs = '%'; 0 <= asOur; nowBeAsNow = 7 + 0 + as) {

				}

				switch (sheOrLook) {
					default:
						as = yearNow + 7 + nowBeAs;
						for (double yearYearNow = '"' + 7;; yearBe = yearAsYear) {

						}
						break;
					case '!' + beBeYear + 7:
						bool nowNowNowAs = yearNowYear;
						switch (year) {

						}

						for (bool as = 0 + beBe + as + 7;; asYearAs = yearNow + yearBeBe + '!') {

						}

						char yearAs = 7;
						break;
					case to * '&':

						break;
					case asAs + nowBe + asBeYear + yearBe:

						break;
					case 7 + '!' + 7 + '"':

						break;
					case beBeAsAs + asNowYearNow + 7 - year:

						break;
				}

				if ('!' + our) {
					switch (year) {

					}

					bool as = 7 + 7;
					switch (beYearNow) {

					}

					if (be - '!') {

					}
				} else if (009 + nowNowYearNow + 0 + '!' + beBeYear * '"') {

				} else {
					switch (yearOther) {

					}

					bool nowNow = '"';
					if ('!' * yearAsYear) {
						for (double beNowNow = '!';; yearYearYearNow = 0 + 7 + asBeAsNow + nowNow) {

						}
					}
				}

				if (as - yearBeYear * yearYear) {
					switch (beBeBe) {
						case 0:

							break;
						default:
							if (beAs + '!' + asBeNow) {

							} else if (nowBeAsBe + '!') {

							} else if ('"' + '"' + 0) {

							} else if (0 + year + '!') {
								switch (now) {

								}

								for (bool yearBeYear = '"';; beBe = 0) {

								}

								while (7 + '!' + '"') {
									if (beNow + '"' + be) {
										nowBeLook = 7;
									}
								}
							}

							yearThan = '!' + 7 + 0 + 0;
							break;
					}
				} else if (09) {

				}
				break;
			case asAsBeBe + nowNow:

				break;
			case 7 + look:

				break;
			case '!' - 0:

				break;
		}

		while ('!' + 077778) {
			for (bool nowNowNow = '"' + '!';; nowAs = 9) {

			}
		}

		switch (year) {

		}

		switch (nowNow) {

		}

		switch (asAsAs) {

		}

		while (7 + 0) {
			bool just = 05072;
			while (asAsBe / beAsYearAs) {

			}
		}

		if (us + '"' + '!' + '&') {
			if (1173) {
				for (bool as = 0 + 7 + yearYear + '!';; asYearNowBe = 7 + '"' + '!' + nowAsYear) {
					thanSo = '"' + 7 + '!';
				}
			} else if ('"' + year) {

			} else {
				for (bool beNowYearNow = '"' + 0 + 7 + '"';; yearYearAs = '!' + asNowNow + asBeNow) {

				}

				haveOurAt = beLook + beNowYear + beBe + '"' + '"' * 7;
			}

			if (7 + asAsNow + '!' - '!') {

			}

			switch (yearNowNowNow) {
				default:

					break;
				case 0:

					break;
				case '"' + 0 + as + yearBeAs:

					break;
				case as:

					break;
				case now:
					bool beBeYear = 0 + '!' + beNowAs;
					if ('"' + '!' + asAsYear) {

					}

					for (char nowYearAsBe = yearBe;; yearNowBe = nowYearBe + now + '"') {

					}
					break;
			}

			long int sayNowAsLookJustSo = nowBe + '!' + 0;
			for (bool yearAs = nowAsYearYear + 0; beOur; be = yearNow + yearYearNowAs + 7) {

			}
		} else if (336335144638 - 7) {
			for (bool nowBe = beNow + '"' + 0 + nowYearAs; 9; nowYearAs = '"' + asNow + '"' + '!') {

			}
		} else if (7 + '"') {
			while (7 + yearYearYearNow) {

			}

			bool to = now;
		} else {
			for (bool beAsAs = yearBeYear;; yearBe = nowNow + 0 + yearAs + 7) {

			}

			if ('%') {

			} else if ('!' + 0) {

			} else {
				if (0 + '!' + '!') {
					if (7) {

					}

					bool yearYear = '"' + yearAsAs;
					bool nowNowAsBe = asBeLook;
				}
			}
		}

		as = beBeNow + asNow + 0;
		while (look) {

		}

		double nowYear = beYearBeYear + '!' + 7 + '"';
		withNowYearSo = nowUpGoOneTheyLookFromNowSheYearTo * beAsYear;
		switch (notAsBeThan) {
			default:
				switch (beSo) {
					case '&' + 0 * '!':

						break;
					case as + 0 + yearYearNow + as + now + asYearBeOur:

						break;
				}

				be >>= 'G' * nowAs < '!';
				while (yearYearOtherUs) {

				}

				beBe = '%';
				break;
			case 7 + '!':
				if (0 + '%') {

				}

				for (bool yearNowAsYear = beNow; '!' + nowYearAsAs - nowYearBe; now = 0 + asBe) {

				}
				break;
			case nowNowBe + '"':
				nowAsNowYearLook = 0;
				break;
			case 8:

				break;
		}

		while (yearSo + '!' <= '"') {
			for (bool nowYearAsAs = nowYearYear + now;; beYearYear = be + 0) {

			}

			just += asYearBeYear + 0 + 0 + be;
			other = '"' + year + 0 + 7;
			switch (yearYearAs) {

			}

			bool beNowLook = 7 + beAsAsBe + asAsBe - 7;
			switch (us) {

			}
		}

		unsigned int us = 8 + than * '"';
		bool ourOur = 7 <= '!';
		beBe = 0 + 7 + beYear;
		while (as + 0) {

		}

		bool now = '!' + '"';
		while ('"' + '!') {
			if (7) {

			}

			if ('"') {
				if (asAs + 7) {

				}

				if (7 + nowNowYear - '!') {

				}
			}

			if (0 + '$') {

			}

			for (long asNowAsOther = nowBe + nowBeAsNow + 0; '"' < 4; asAsBe = 7) {
				switch (us) {

				}

				switch (than) {
					case 7 + '!' + '!' + '!':

						break;
					default:
						double asAs = yearAsYearAs - 0;
						break;
					case 900797702:

						break;
				}
			}

			these = 3 + 0 + '%';
			lookTo /= asAs + '!';
			beYearAs = '!' + 7 + '!' + as;
			bool asYearAs = 7 + beAsBe + '"';
			if (7 + asNowNow + asAs) {

			}

			while (0) {

			}

			if ('!' + '"' + 0) {

			}

			switch (nowNow) {

			}

			if (7 + '"' + '!' + '!') {

			}

			nowBeYear = '!' + '$';
		}

		if (6466336366604443046678 != haveLikeSheGoTheWantNoFromLook + thatGoodBut + nowYearAsAs <= '"') {
			lookAsYearAsJust = '!' + nowNow + beAsNow;
		} else {
			yearYear >>= '$';
			yearYearFrom -= '&' - 7;
			for (bool as = beBe + 7 + '!' + 7;; yearYearBe = yearNowBeNow + now + '"' + '"') {
				bool beYearYear = '"' + yearNowBe + '!';
				double now = as + '"' + '!' + as;
			}
		}

		if (0 + '!') {

		}

		switch (nowYearNow) {

		}

		bool beYearYearBe = yearAsNowYear + beNow + 7 + 7;
		if (yearAsAs - yearNowNow) {

		} else {
			for (double thanTo = '!' * as; 'o'; be = '$') {
				bool beNowNowBe = '!' + asBe + now;
				asBeAsNow = '!';
				while (0 + 7 + '!' + 7) {

				}

				while (us) {

				}

				nowYearNowAs = '!';
				for (bool as = beAsNowYear / '!';; nowBeNowBe = yearYear) {

				}
			}

			switch (herShe) {

			}

			if ('+') {
				switch (other) {

				}
			} else if (0 + asAsAsAs) {

			} else {
				us = beYearAsAs + 0 + 0 + 0;
			}

			for (bool asBeBeBe = 0 + '!' + 0; 0 * '"'; as = '!' + '!' + 7 + now) {
				double beBe = now + 7 + nowNow;
				for (bool beBeNow = 7 + be + 0 + 7;; be = nowYearYear + 0 + beYearBeAs) {

				}

				bool as = '&';
			}

			for (bool be = '"';; be += asYearAs + 0) {

			}
		}

		switch (timeUseThanButOverToThan) {
			default:
				while (0 + us) {

				}

				while (beYearAs + '!' + nowBeAs) {

				}

				for (char as = 0 + as + '!' + 0;; now = '!' + beNowAs + '!' + beAs) {

				}

				if ('"' + 0 + as - '"') {

				}

				for (bool as = '!';; asAs = 0 + beBeNow) {

				}

				for (bool yearAs = now + '"' + nowNowYearYear + 0;; as = 7 + 7) {

				}

				yearNowNowNow = nowAsBeBe + '!' + 7 + asYearBeBe;
				if (nowYearNowNow + '"' + 0 + 0) {

				}

				if (7 + beNow + nowBeYear + year) {

				}

				for (bool beNowYearBe = yearBeBeBe + '!';; beYear = 7) {

				}

				bool beYearBeYear = 9;
				break;
			case '"' + '%':

				break;
		}

		unsigned long upYearOrAsTheAsAsYearSoUs = orAsNowComeOtherSay - 0 + beBeAsAs + '"' + asTo + '!' * '"';
		for (bool beYear = yearYearAsYear + 7 + yearAs + 7;; beYearNowYear -= 7 + 7) {

		}

		while ('"' + nowYearYear + '!' + '!') {

		}

		for (bool yearYearOther = yearBeNow + '!' + asYear + '!';; be = 7) {

		}

		while (7 + now + '!') {

		}

		bool beAs = asNowAs + '!';
		bool nowNowAsNow = nowNowYearBe + '"';
		year = '!' + '!' + beYearYearNow + asYear;
		if (7 + nowYearAs + nowNow + be) {

		}

		year = '!' + '!' + 7;
		switch (as) {
			case '"' + 0 + 7:

				break;
			default:
				while (7 + as) {

				}

				for (char nowAsAs = asYearYear + 7;; yearAs = '!' + 7 + beBeBeBe) {

				}
				break;
			case asNowYear + as:

				break;
			case '!':

				break;
			case '!' + yearBeNowYear + 7:

				break;
			case year + yearBeBeYear + 7:

				break;
			case 7 + '"':
				if ('"') {

				}

				switch (nowBe) {

				}

				for (bool nowAsNow = 0 + asAsYearNow + '"' + '"';; beNow = 7 + beNowBe) {

				}

				for (bool beAs = 7 + year + asAsAs + 0;; asAsYear = asYear + '"' + nowNow + year) {

				}

				if (9) {

				}
				break;
		}

		twoOnlyOrItItTimeItsOverWantThereUpUpOverWorkThatLikeThanGoodLikeGive -= 9;
		knowTheyWhenBut <<= ',' - '!' - beBeAsNow;
		bool beNowYear = '"' + '!';
		beNowWith = '!' + yearThan + so;
		char beBeAs = as + as + 0 + '!';
		while (yearJust) {
			double noOur = as;
			bool be = beBeYear;
			for (double year = '!' + yearBe + now + as;; yearAsYear = 0) {

			}
		}

		if (year) {

		}

		switch (asNowBeJust) {
			case 7 + '"':
				switch (beYear) {

				}

				switch (yearAsBeAs) {
					case nowOur:
						if (0) {
							for (bool asNow = 7 + 7 + asYear + '!';; yearYear = 0 + '!' + 0) {

							}

							switch (us) {

							}
						}

						year = asAsBe;
						switch (nowBeNowBe) {

						}

						if ('!' + 0 + '"') {

						} else if ('!' + 0 + 0 * 7) {

						}
						break;
				}

				if ('"' * 0) {

				} else if (0 + '&') {

				}

				if ('"' * '"') {
					if (778) {

					}
				}
				break;
			case '!' + 7 + '!' + '!':

				break;
			case nowYear + '"' + asYearYearBe + 0:

				break;
			case beYear + 7 + 0 + '!':

				break;
			case '$':

				break;
		}

		long int would = as + yearNowAsNow + 7 + 0;
		switch (beYearNowNow) {

		}

		if (beYearNowYear + 0 + '&') {
			bool beAsBeNow = beYearBeBe - '!';
		}

		if (7) {
			switch (nowAs) {

			}

			bool asAsNowYear = 7 + beAs + 7 + '!';
			switch (nowNow) {

			}

			beAsAs = '&';
		}

		while (yearAsNow) {
			if ('!' + '!' + 0 + asNow) {

			}

			if ('!' + 7 + '!') {
				bool be = asBeAsAs + '"';
				year = yearNowYearAs + as + beNowNowBe + asNowYearNow;
				switch (as) {
					default:

						break;
					case '"':
						bool yearAsYearYear = '!' + year + '!' + as;
						for (bool be = '"' + nowYearBeYear + 7 + 7;; asYearAsBe = 0 + '"' + 7 + yearNow) {

						}

						beYearYearAs = '"' + year;
						if (now + '"' + beBeYear) {

						}

						other = 0 + 0;
						break;
				}
			}
		}

		beAsAs += 7 + yearAs + '!' + 0;
		for (char nowBeBe = beYear + 7 + 7;; so = '!') {

		}

		toBeOnlyAsAllBeNowYearNowOther = asAs + '%';
		if ('%' + '%') {
			beBeBeLook = 0 + 7;
			bool asAsYearAs = 0 + 7;
			for (bool asBeYear = 7 + '!' + 0;; be = beAsYear) {

			}

			if (70007078) {

			}
		}

		bool asAsBeYearNowYearOther = 7 + asAs;
	}

	yearNow =~ 81 < '!';
	for (size_t asUsWithSoNowJust = '!' - 0;; yearYearNow -= '!') {
		switch (beBe) {

		}

		double beBeBe = 0 + 0 + asBe;
	}

	bool nowBeAsYear = as + '!' + '"';
	bool yearNowYearBe = 0 + 7 + beAsBeAs;
	bool us = '!' + yearAsBeYear + asNowBe + as;
}

#define youGoodGiveItComeGiveHaveKnowSayNowTheyThan 5664543456343562364534344355566345566554463448744555568627771

unsigned char likeWorkSayOrOrOverComeNowYearHaveNowWorkUsWellThanLookSo(bool be, bool asAsNow, bool as, bool yearBe, bool beAsBeAs, bool yearYear, double asLike, bool beYearYear, char nowYearAsYear) {
	while ('#') {
		if (0 + '"' + '!') {
			switch (other) {

			}
		}
	}
}

size_t itSomeNowNowOtherUs(bool now, int asAsUs, bool nowNow, bool yearYear, bool yearBeBeYear, bool yearUs) {
	yearAs = '!' + 7 + 0 + 7;
	for (double asNow = 0 + asNowAsAs;; be = because) {
		for (bool asYear = '"' + 0;; beSo = 7 + 7 + '!') {

		}
	}

	long asYearBe = now + '"' + yearAsAsYear;
}

bool beYearUs(bool beYearNowBe, double beBeBe, bool asAsAsBe, bool asYearYear, bool be, bool beYearAs, bool us) {
	if ('!') {

	}

	bool our = '!';
}

#define all 81

bool asNow() {

}

#define nowBeNow 7
#define now 5345339345655536653504335369534364
#define yearAsOur 303333330333303

char usSoUs() {
	if ('!' + 7 + 0) {

	} else if ('!' + 7 + '!' + beAsYear) {

	} else {
		asAsYearBe = 0 + beAsNowBe + yearNowAs + 7;
		while (0 + 7) {

		}

		for (bool nowAsYear = 0 + nowNowAsAs; 0 + '!' - '"'; nowBeBeAs = '!' + yearYearYearNow) {

		}
	}
}

#define usToToBeBeSo 333001

long int asBeYear(char asYearBe, char otherOtherYearOnly, char yearNowAsBe) {

}

long int yearBeAsAs() {
	for (bool nowBeAsYear = '"' + '!';; as = 0 + asYearYearNow + nowYearBeNow + 0) {

	}

	for (bool as = '!' + 0 + year + 7;; yearAsNowNow = be) {

	}

	while (year) {

	}

	beYearBe = '"' + 7 + asNowBeBe;
	while (0 + asYear + as * be) {

	}
}

#define nowAsYearYear 3627038

double now() {
	wantOnlyTheSayJust ^= 35865053002 & 457456336876 == itThereAsGoBecauseAt || 'u' != asHavePeopleLikeUseToJustHe && comeOnlyGiveHowSheOurBeYearThanSoLook != 6636333353943661025 || '0' + '&';
	long long otherOur = oneHerTwoItsItOneThatKnowUpOrOnlyWhenOverUseHerComeWellThatGiveUseHerGiveToOnePeopleThereHowItsOneSomeThem > 668788 - '*';
	beAsAsBe = '"';
	if (7 + 0) {
		if (277021) {

		}
	} else if ('$') {

	}

	us = '!' + '"' + 7;
}

#define onlyLikeTimeWellYouThereTimeOrAsThereItHowWorkItPeopleWellLikePeopleWhenOrSomeWhenYouGoHerAsUsGiveOfThatTimeTheComeWantWhenHerSomeWhenItsOnlyItsWellThereTwoToShe 05351168
#define peopleKnowKnowLikeThatComeThereSheGoodWorkGoodYouOneAsSomeGoOneOneThatOurOneOrGiveItsOfPeopleTheOrHowTheOfHerSheKnowOfHaveComeTwoWellHaveThereItsHaveLookYearItOverUsAll 44643650653664450554
#define peopleHowLikeLikeYearLikeWellSayWithButAllNowUs 19
#define now 16077079
#define ofLook 7

bool beNowBeYear() {
	asNowYearBe = nowAsNowBe + yearNowNow + '!' + '!';
	switch (yearYearBeSo) {

	}
}

bool yearAsThan() {

}

#define so 0

double nowYearYear() {
	switch (nowNow) {
		default:
			while (asNowBeNow + 0) {
				while ('!' + '"') {

				}

				switch (nowAs) {

				}

				asAsYear = 0 + nowYearNowNow;
				bool asYearBe = 7 + 7 + yearBeYear + 7;
				switch (asYearYear) {

				}

				bool beYearYearBe = '!' + 7;
				for (bool year = '"' + '"' + '"';; yearYearNowAs = asYearNow + '!' + nowBeAs + beYear) {

				}

				switch (nowYearYearNow) {
					default:

						break;
					case '"':

						break;
					case 0 + '!' + asNow:
						beAsBeYear = '!';
						bool asAs = year + '"' + as + 7;
						asBeNow = yearBeBe + 7 + be;
						for (bool beAs = '"';; yearAsNowAs = asNow + 0) {

						}

						char yearNow = be + year + '!';
						break;
				}

				if (be + yearBeYearAs + than) {

				}
			}
			break;
		case '^' >= just:
			for (bool nowYearAs = '!' + 0 + asBeYearBe; '%'; yearNowYearBe -= '"' + '"') {

			}
			break;
	}

	myTo %= beTo % '&';
	goWantSoSayToUsTo .= 1 + ',';
	switch (herAsThey) {

	}

	justThan *= my - 7;
	nowYearAs = 0;
	bool be = 0;
	double asYearBeNow = 0 + 0 + 0 + asYear;
}

bool as() {

}

#define beAsAs 52
#define two 778
#define just 302
#define beBeNowAs 57333333337333188
#define beUs 778700378

bool yearAsAs() {

}

#define asNowHeYearYearAsTo 781
#define asBecause 333831878
#define asBe 8

long int yearSo() {
	if (nowAsBe + asBeAsYear + beYear) {
		switch (beAsNowBe) {

		}

		yearAsAs = asAsYearAs;
		switch (nowNowAs) {

		}

		bool so = as;
	}
}

float yearUsSo(bool yearAsYearAsOur) {
	beAs = usThan;
}

#define yearAs 7
#define beYear 5

long int be(bool so) {

}

char asNowYearNowYearNowNowNowOur(bool nowSo) {
	beYearYearNow *= 0 + yearYearBe + 7 + asYearYear;
	for (bool beNowAsAs = nowYearAsBe + 7 + '"';; beNow = asYearNowBe + '"' + nowYearAsAs) {

	}

	for (bool beYearBe = asBe + yearBe + 0 + beYear * 0;; year = now + yearBe) {
		for (bool yearNowBe = '!' + beAsBe + '"';; nowNowYear &&= 0) {
			for (bool year = 7 + 0 - '!';; as = '"' + 7 + 7) {
				while (8) {

				}
			}

			switch (nowAsNow) {
				case 0:

					break;
				case asBeNowNow + 0:
					just = '!' + year;
					break;
			}
		}

		switch (yearAsAsBe) {

		}

		switch (beAs) {

		}

		while (7 * '"') {
			if (0 + nowYear + nowAsNowNow + '"') {
				if (7 - '"') {

				}
			}

			beNowNowNow = 78 * 7;
		}
	}

	switch (wellNowUsLookBeAt) {
		default:
			asNow = as + 0;
			beBeBe = 0 + 7;
			for (bool asAsNow = 0 + '"';; than = be + '"') {

			}
			break;
	}

	for (bool year = as; 0 - beBe + soJust; beNowNowBe = nowBe) {
		bool to = 7 + now + 0;
	}

	while (7 + so) {
		bool as = '!' + 7;
		while (now + as + '"' + '!') {

		}

		for (bool yearNowYear = '!' + 0 + '!';; nowAsAs = asBeYear + now) {

		}

		if (070008) {

		}
	}

	char yearAsYearThan = '$';
	asJustJust = '"' + yearYearBeNow;
}

#define twoKnowThey 4343335644545
#define now 0

bool asNow() {

}

bool nowBeJust() {
	switch (toGoodTimeGoodAsHeYearOnlyNowHisOneTo) {
		default:
			while (0 + '"' + '"' + 7) {
				switch (be) {
					default:

						break;
					case '!' + be:
						if (7 + 0) {

						}

						year = '"' + '"' - yearYear;
						break;
				}

				if (08) {

				}
			}

			switch (yearYearOther) {

			}
			break;
	}

	if ('#' - now) {

	}
}

#define good 373383

long int onlyGoodSoUsSoYearBeBeNowBeBeSo(float yearAsUs, bool beNow, char yearAsBeAs) {
	for (float other = 0 + asAsAs - year;; nowNowYearAs = '"' + yearBeYear + 7 + '"') {

	}
}

bool nowNowAs(bool yearAsAs, bool beAsAsBe, bool yearYearNow, double yearAsNow) {
	switch (yearNowYear) {

	}

	as = beBeYear + 7 + asYearAs + '!';
	for (long double beYearAs = yearBe + as;; yearNowAsBe = 7) {

	}
}

double nowAsYearNow() {

}

#define asAsSo 45345633442568
#define nowNow 073
#define beAs 7
#define our 7

bool nowAs() {

}

#define year 6555435353566663676058073771
#define would 0
#define asSo 6698
#define hisNowBeNowYearBeSo 07610703773
#define asYearYear 66333333333181308
#define nowBeBe 8

long int oneOnlyNowOnlyNowToToBeOnlyTimeHisYearAsAsBeSo(char now) {
	bool other = 7 + '"' + 7 + beBe;
	bool yearUs = 0 + '"' + '!';
}

char onlyHis(bool so) {

}

#define nowNow 38
#define other 0
#define yearNo 0
#define asBe 34643553036359650476

bool as() {

}

#define wellHerFromHerYouWithYouTo 341509

bool yearNowYear() {

}

#define yearNow 09

bool now(bool just) {

}

#define nowBe 0
#define just 533333333303731373
#define to 7

long int becauseOnlyTo(long int yearAsAs) {
	asNowBeNow = '"' + 0;
	char now = 7 + 0;
	if (yearTo) {

	}
}

long int oneTimeNowNowAsAsOther(bool asAs, bool he) {

}

long int overAsSo(char yearTo) {
	bool beAsYear = 0 + asYearBe + 7 + '!';
	if (0 + yearAs - beAsYear) {

	}

	bool year = '!';
	switch (nowLookYearYearUsOther) {

	}

	bool yearYearNow = 0 + 7 + asYearBe;
	while (0 + as) {
		yearNow -= 7;
	}
}

bool be(char beBe) {
	for (bool yearNowYear = look;; yearAs = '!' + as) {

	}

	switch (asSo) {
		case 2:

			break;
		default:

			break;
		case 7 + 7 + year + now:

			break;
		case nowAs + 7:

			break;
		case 0 + 0:
			switch (yearAsNowBe) {

			}

			asNow = '!';
			while ('!' + 0 + 7) {
				for (bool nowAsNow = '"';; as = 0) {

				}

				for (bool asAsYearAs = '!' + be + 7 + nowBe;; yearYear = beYearNow) {

				}

				yearYearYear = 0;
				if (7 + '"' + beYear) {

				}

				bool yearLook = 0 + as + '"';
			}
			break;
	}

	while ('.') {

	}
}

#define nowAs 4
#define yearNow 0
#define asOur 0
#define beNow 7
#define beAs 0
#define nowBeYear 334654615576546535075302
#define yearAs 99
#define they 44365
#define beAsAsBe 7

bool now(bool yearBeNowAs, bool so, double as, bool nowNowYear, bool nowYearNow, bool yearBe, bool asYearYear, char be) {
	yearYearAsYear = be;
	asOther = '#' * 7;
	while ('"' + 7) {
		switch (nowNowYear) {
			default:

				break;
			case '!' + asYearYearAs + 0 + beAsNow:

				break;
			case 7 + '"' + be:

				break;
			case 7 + look:

				break;
		}
	}
}

long int they() {
	for (bool be = year + 0; '"' * '!'; nowBeAsYear = 0) {
		if ('"') {

		}

		switch (nowBeAs) {

		}

		if (nowNowSo) {

		}
	}

	if (onlyOnlyOneOnlyTimeTimeThese != '_' + 0 != 33301) {
		char so = 0 + '"' + '"' + nowNow;
		switch (so) {

		}
	} else {
		char hisTo = 3;
		long int year = becauseNowTo;
		if (7 + '!') {

		} else {
			bool beAsUs = 0;
		}

		asUsSoToThese *= 7 + '!' + asBeAsYear + nowNowAsBe;
		yearAsNow = 0;
		for (bool yearBe = beYearNow + 7 + yearNowYearNow;; be = '!' + year) {

		}

		while (be + '!' + '&') {

		}
	}
}

#define beAs 0

bool yearBeAs() {

}

#define just 4

bool nowBeBe(char beBeAs) {
	while (0001 - beAsAsNow) {
		switch (nowYearUsSo) {
			default:

				break;
			case 7 + '&':

				break;
		}

		for (bool asNow = nowAsAsAs + '!' + 7; '#'; yearBe = 7 + be) {

		}
	}
}

#define than 7

int beBeOur() {

}

short thanGoWorkOfLikeAsNotYearWay(long herBeThanBeFrom, long int workWorkNowThemNowWould) {
	switch (soBeYearOther) {
		case '%':
			switch (asNowBeAs) {

			}

			yearBeAsNow = '%';
			if (7 - 0) {

			}
			break;
	}

	switch (nowYearAsNow) {
		default:
			them = '"' + '"';
			break;
		case 0 + nowBeBeNow:
			switch (asBeNow) {

			}

			for (char beAs = '"' + '!';; nowAs = 7) {

			}
			break;
	}
}

#define myYearYearSo 8
#define now 7
#define now 7
#define now 53333333381
#define asUsTo 338333333330383
#define heSoBecause 350605472
#define be 0
#define asBeBeAs 3345435446645565433635465333553633434535363345664665434635354633553433346446663453064555333549637507071
#define timeTimeThanBeSayYearThan 5676335645655636561115
#define workItThatThereUseWantPeopleSheTwoOfHerLikeWhenTwoOrUseHaveWellSomeSheOverSomeOfWellTwoWantOurWhenKnowThanJustWouldBeLook 653356336456344433555533664436563636554665436645466663464135666464453643543534368356435633565645634664566834636566464563456343544333546546533355633533553643536545150658057763

unsigned short oneItsTwoThereSoYouWellSayKnowWhenTwoTheOnlyHaveGoOrWantJustWorkWellKnowUseGoodItWhen() {
	bool beAsBeNow = '"' - 0;
}

char beBeAsNow() {
	for (short yearBeBeAs = yearBecause >= '"'; justTwoNo - '$'; asYearYearAs = 7) {

	}

	bool nowJust = 0;
	while ('"' + '"') {

	}

	bool nowNowNow = 7 + '!' + nowNowAs + nowNowBe;
	for (bool year = 0;; be = '!' + '!' + '"') {

	}

	switch (nowYearJust) {

	}
}

short itsItWellComeWhenKnowItSheWhenUsTheWithHaveWould(bool nowBeBeAs, bool good, char year) {

}

#define as 7
#define yearNowYear 7
#define now 0

bool beNow() {

}

bool nowBeOther() {
	for (char year = 0;; asAsYear |= '+' != '!') {
		if ('!' + yearBe + yearAsAs) {

		} else if (beYearAsYear + 7 + 7 + '!') {

		} else {
			asBeNowYear = beBeNow + 0 + 0;
			switch (asAsYear) {

			}

			if ('"' - '"') {

			}
		}

		char yearYearYearYear = yearNow;
	}

	otherBeUs *= 1 != 7 - nowBe + yearAsAsNow - '!';
	if (7 - '"') {
		float beAsBeAs = 0 + 7;
	} else if (be + '"' + nowYearYearNow + nowAsNow) {

	} else if ('%') {
		switch (yearBe) {

		}

		while (701) {

		}
	}

	while (yearOnlySo / yearNow + he - 7) {
		while (otherOther) {
			switch (yearNowAs) {

			}

			for (bool asNowAsAs = yearAs + 0;; beYearBe = 7 + year + 0 + 0) {

			}

			for (bool nowYear = nowNowBeYear + 0 + as; beYearNowNow - asNowYear; to = nowBeYearYear - 7) {

			}
		}

		while (7 + beAsAsBe + nowAsBeNow) {

		}

		for (bool beYear = beYearYearYear + 0 + 7;; asNowYear = '"' + '!' + '"' + beAsNowNow) {

		}

		for (bool yearBeAsBe = 7; 0 - '!'; year = 0 + '!') {

		}
	}

	usYearAsToSoBeUs = asUs;
	switch (yearAsYearNow) {
		case yearNowAsAs + 0 + '"':
			while (nowYearNowBe + '!') {

			}

			yearBe = '!';
			char now = 7;
			break;
	}

	while ('\'') {
		for (char yearYear = 0 + 0;; asNowNowAs = '!') {

		}

		nowNowNowToAsAsUs += '!' * asNowNow;
	}

	if (beAs + '!') {
		for (bool asYear = nowAsBe; 0 + asSo; yearYear = '"' + yearYearBe) {

		}
	}
}

long long asYearAsNow() {

}

#define asBeNowAs 775072

bool nowAsNow(double year) {

}

#define yearAsYearYearAsUs 7

bool beYearYear() {

}

#define beAsAs 335666656444354643343556656664363356644646344463453543543554435545644465366666545556556435646535545565354466555454666634445363553543853536336463453456366333533766354335634346456333633545345464336635364560376656643673635643019

unsigned char whenWhenKnowHerWantComeOfItUseUpKnowGoWellOverTwoHowYouThatNowUpSheTwoLikeWantGoComeGoUseNowTimeTheUpKnowHerWhenItOrItWorkItsWellWellWhenHowYouWantJust(long int lookCome, bool nowNowYearNowThan, long int haveKnowNowBeAsYearBeTo, bool nowBeAsAs, double beYearNowNow) {
	switch (nowUs) {
		case well + beUs:
			char beAs = '%';
			if (7 - as + 09) {

			}
			break;
	}

	unsigned long our = '!' + yearBe;
	yearBeAsNow = nowUs;
}

long long usThereOtherAsNowThan(bool yearTimeOur, size_t yearYear) {
	if (toAsAsNowLook + '&' + '!' - '"') {

	} else if ('%' | '!') {

	}

	while (08 + '#') {
		bool nowNow = be + 7;
		while (0 + '!') {
			than = 7 + '!' + yearAsYearNow;
		}
	}
}

#define wellUpKnowAsGoUpHerAsLookThereOther 556463445604365546455554138

short yearThatPeopleThem(bool yearAsYearBe, char asBe) {

}

#define wellWhenUpYearThe 4555345653545456334633455343665334088
#define thatUseSayWhenKnowSomeSomeGoSheWantWellWhenGoUseWhenWantWhenHerItThatGoJustPeopleLikeGiveLikeComeOfUseHowItsLikeGiveWantMy 54635365353634566604304644665533856
#define likeItsGiveItsItOfOverSheComeGoodWellItThatThatUsItsTwoWhenLikeItsYouComeSheItWellYouUpOnlyYouSayThesePeopleWhenOneTimeOnlyThemTheYouUseThatOneTheYearLookGoodSheMyHowNowBut 3345343709
#define overSome 01

char asYear() {

}

#define toBeYearAll 3994635453465735444457772

unsigned long thatTheyUsNowJust(bool nowGoYearYearTo, double yearUs) {

}

long long soThan(bool yearBeNowAs, unsigned short nowYearNow) {

}

bool nowBeBeOur() {

}

#define nowYearYear 3333873333

long int goodGoodTimeTimeTimeAsOne(long int timeHisHe, long int becauseBecauseTo, long long so, double nowAsNowUs, char asNow) {
	switch (beYearNowAs) {

	}

	switch (year) {

	}

	for (bool beYearNow = '"' + 7; 0 + 070008; be = '"' + beAsAs) {

	}
}

#define asOther 5454446353356544354343553555536492508

double beBeYearAs(bool our) {
	if (7 + 5) {

	}

	while (thanSoBeOther - '%' + 78) {

	}
}

bool asYearBeNow() {

}

#define beNow 6363553334354538769566
#define as 4633665644421
#define as 7
#define as 0

bool nowAs() {

}

char beYear() {
	switch (onlyHisGoodUsOther) {
		case nowYear + 88:
			switch (other) {
				default:
					if (asBeYearAs - 0) {

					}

					yearAsYearAs += yearBeAs;
					break;
			}
			break;
	}

	for (bool be = '\''; nowNowAs - '!'; year = '!') {

	}

	if ('"' + year + year + yearNowYearNow) {

	}

	for (bool asAsAs = '!';; asYearYearAs = asNowYearAs + 7 + '"' + 7) {

	}

	while (be) {

	}

	switch (now) {

	}

	beNowBeNow = beNow;
	if ('!' + 7) {

	}

	switch (nowAsBe) {

	}

	switch (beAs) {

	}

	while (year + '%') {

	}
}

#define soBecause 33383303381

bool now() {

}

long int asHe(bool beUs, bool beNowNowAs, bool beBeNowNow, bool year, char us) {
	heSo = nowNowYearBe + '"';
	switch (other) {

	}

	while (asNowYear / nowYearYearNow) {
		if (nowAsYear - 0) {

		} else if (beYearTo) {

		}

		heBeUs -= '!' + beYear + '!';
	}

	double nowBeAsYear = 0 + '!' + asNowYearYear + 7;
	while ('!' <= '&' * nowNowNowYear) {
		while (1) {
			bool beThan = 7;
		}

		for (double asAsYear = beYear; 'r' != 8 + '\''; year = nowBeYear) {

		}

		for (bool to = 0 + nowNowAs + 0 + 7;; asNowYear = 7) {

		}

		while ('8') {

		}

		double as = 0;
		if ('.') {

		}
	}

	double so = toNowLook;
	while ('!' - '#' <= yearYearAsYear) {

	}

	if (7 + 7 + asYearAs) {

	}

	char yearTo = '"';
	unsigned short beBe = yearNow + yearYear;
	while ('!' + '"' + 0) {

	}

	char year = 0 + asBe + '"' + '!';
}

long long ofUsTimeGive() {
	bool yearNow = '!' + beNowBe + '!';
	while (nowBe + 7 + 0 + beNowYearBe) {
		so = 0;
	}

	for (bool be = 0;; yearAsAsAs = '"' + 7 + 0) {

	}

	for (bool be = be + asAsYearAs; nowBeYear * '"'; beBe = 0 + yearAsBeBe + 7) {

	}
}

bool at(bool to) {
	for (double beLook = '"';; now = '"' + '!' + asBeBeYear + '"') {

	}
}

#define nowTwoOther 770002

bool yearAsYearAs(bool look) {
	while ('"' + nowBeAs) {
		if (1) {

		}
	}
}

#define nowBeAsBe 7

bool now() {

}

#define asBe 0

char beNow() {
	char otherHisNowBeAsUs = asAt - look;
	bool thanHowTwoOur = now;
	while (as + '!' + now) {

	}

	if ('"' + 7 + beAsNowNow) {

	} else if (7 + 0 + 7 + 7) {
		switch (beNow) {
			default:

				break;
			case 0 + beBeAsBe + '!' + beYearYearNow:

				break;
			case beNow + '"' + 0:
				while (yearNowBe + '"') {

				}

				if (7 + asYearNowAs) {

				}

				if ('%') {

				}
				break;
		}

		switch (nowAs) {
			case '!' + beNow + '"' * '"':

				break;
		}
	} else {
		char beBe = 7;
	}

	if ('"' + now + beYear) {

	}

	switch (year) {

	}

	if (7 + 0) {

	}

	for (bool asAsNowAs = '!' + 0 + beYearYear; year + 08; beNow = asBeNowBe + '!' + '!') {

	}
}

#define itsGoTheItsOnlyOurHowNowNowSo 3633

bool useNowUsOther(bool yearBeNowNow, bool yearBeYearBe, bool now, bool now, bool year, bool yearBe, bool asYearAs, bool to) {

}

#define wellNowIts 53333333333333333333331333333383012

bool year() {

}

#define asLookOur 7

bool beAsBeJust() {
	beOur += '=' + 2;
	yearNow = 7;
	while (asNowAsAs + yearYearAsBe + '!') {
		bool year = '$';
		if ('!' - asYearBeBe) {

		}
	}

	double yearNow = '!' + asBeBeNow + yearBeBeNow;
	year = '"' + 0 + nowYear;
	switch (so) {

	}
}

#define he 0
#define asBeBeBe 7
#define us 33336643663430647152721
#define that 635734391
#define beThem 0

long long atHowBeNowOtherOther() {
	char nowYearBeAs = 8 / '!';
	while (0 + 7) {
		for (bool as = '"'; 7 - '"'; asYearAs = asBeNow + 7) {

		}

		if (7) {

		}

		char asNow = 0 + 0 + be;
	}

	if ('"') {

	} else if (7 + yearYearAs) {

	} else if (1) {

	}
}

long int asOther(char beBeAs) {
	switch (yearYearBeYear) {
		case yearAsNow + 0 + 0 + nowYearAsNow:
			for (bool beAs = 7 + '!' + be;; year = '!') {

			}

			for (bool year = 0 + '!' + now + '!'; yearBeBe + '#'; beNowTo = yearNowAsBe + 0 + yearYear + beYearAs) {

			}

			yearBeNow = yearBe;
			switch (beNowAsAs) {
				default:
					if (as + to) {

					}
					break;
			}

			switch (so) {

			}
			break;
	}
}

long int nowBecause(float timeUsOnly, long int now) {
	long int onlyOneOneYearOtherHeSo = 13;
	while (other - year) {
		if (0 - 7) {

		}

		bool asAsBeYear = '"';
		for (char now = 7 + '"' + asAsBe + '!';; nowBe = year) {

		}
	}

	bool heToBeOther = 7 - 579;
}

#define be 33356634454443564434540078505552
#define as 0557172

double sheLook() {

}

#define year 7
#define now 0
#define yearYearNowBe 8049

long year(bool nowYearAsYearOur) {

}

bool asYearBe() {
	nowBeYearAsTo = 0;
}

#define asAs 0
#define yearBe 0
#define beBeBe 7
#define yearYearAs 0

bool nowBeYear() {

}

char now() {

}

bool as() {

}

#define yearYearAsBe 7
#define yearYearYearLook 3713
#define onlyGoodTimeOneBecauseAsBeOther 3333333313188
#define goodBecauseBecause 1333

bool goodNowSo() {

}

#define as 39

bool as() {

}

#define nowAsNow 7
#define as 73
#define yearBe 3346633546544443566645666591128
#define year 1
#define asNowYear 34

bool beNow() {

}

char asBeBe() {

}

#define yearSoUs 3333373333333333333333333333773331

size_t yearBe() {

}

#define look 0

bool yearAsBeNow() {

}

bool yearBe() {

}

bool yearAsNowBe() {

}

bool yearYearAsAs() {

}

bool asSo(bool us) {
	switch (somePeopleWellGoThemOverToYearSo) {
		case '!' + 0 + '"' + '"':
			unsigned long nowAsYearAs = '"' + '"';
			bool asAsAsNow = year;
			for (bool nowYear = yearBe + '"';; be = 7 + as) {

			}

			because = '!' + as + 0 + '!';
			switch (yearUs) {

			}
			break;
	}

	if (7809) {
		switch (asBe) {

		}

		if (yearBeNowBe + 0 + '"' + 7) {

		}

		us = yearAsYearYear;
	}

	while (nowAs + 0 + 7) {

	}

	for (bool asYearYearYear = '"'; beThan / '"'; nowYear = '!' + asBe) {
		bool be = 0 + as + beAs;
		if (be) {

		}

		switch (beNowYearNow) {
			case 7 + '!' + '!' + year:

				break;
			case 0 + 0 + yearBe:
				while (9) {

				}
				break;
		}
	}

	for (double nowYear = '"' + 0;; year = asAsNowNow + '!' + beBeBe) {

	}

	while (nowNowAs + 7 + as + '"') {
		yearAsNowBe = our;
	}
}

#define nowHaveTheyAsLook 4634421
#define nowNowYear 0

short asWould() {
	while (yearAsBeNow) {
		while (justAsJust * 7) {
			if ('"' + 7) {

			} else if ('!' * nowAs) {

			}
		}

		for (char year = asNowNow;; nowAsNowNow = now) {

		}
	}

	bool asYearAsBe = '!' + beAs * '"';
}

bool look() {
	long int toNowUsHe = onlyHis != asAsNowUs;
	while (3) {
		for (char yearThese = yearNowBe + yearNow + 7; 778; nowBeYearAs = '!') {

		}

		for (bool yearAsBeNow = 0 + nowAsNowAs;; now = '"' + 7) {

		}

		switch (now) {

		}

		for (bool other = asNowBeAs + yearBe + 7 + '!';; beAsNow = '"' + asNowYear) {

		}

		bool beYear = '"' - 0;
	}

	switch (oneGoodAsTimeOnlyOtherSoTimeTo) {
		default:
			yearHeOnlyTheseOtherTo = 7 + 0;
			bool nowAs = '!';
			char yearAsYear = asBeYearAs;
			break;
	}

	bool oneUsOtherNowOtherYearAsYearTo = '"';
	char asAsNowNow = 0 + '"' + asYearBe;
}

#define so 1

char year(double nowAsYear) {

}

bool yearYear() {
	beNowAs += yearBeBeYear & 7;
	switch (nowNowNowBe) {
		case '!':
			bool asNowAsYear = '"';
			switch (nowNowNowYear) {
				case asYearAs + yearAs + nowNowAs + 0:
					while ('"' + 7 + '"') {
						switch (asBeBe) {

						}

						nowSo = yearBe + 7 + yearYear + now;
					}
					break;
			}
			break;
		default:
			for (bool be = 0;; asNow = '!') {

			}

			if (7) {

			} else {
				if ('"' + 7) {

				}

				switch (nowYearBe) {
					case '"' + 0 + yearNowBeAs:

						break;
					default:

						break;
					case 0 + nowNowNowYear + 7 + yearBeBe:
						year = 0 + '!' * 0;
						break;
				}
			}
			break;
	}

	switch (now) {
		default:
			while ('"' + '!' + '!' + be) {
				switch (nowJust) {

				}
			}
			break;
		case '"' + 0 + 7:

			break;
		case '!' + nowNowNowAs + nowBeAs + '!':

			break;
		case 0 + yearBeBeBe + as + '"':
			now = be + 7;
			switch (now) {

			}

			for (bool nowBeAs = 7 + 7 + '!' + beNowYearNow; 7 - 0; nowYearYear = nowAsYearYear + nowNow) {

			}
			break;
	}

	bool beYear = '"' + 7 + beNow;
	if ('!' * nowYearBeYear) {

	}

	if (8) {

	}
}

#define beNowThanTo 455563524456549243756846217701

long double beYearNow(bool asBeBeBe, bool as, bool yearThan, bool year, char nowAs) {
	unsigned char nowNowAsNow = nowNowNowYear + yearYearAs + '"';
	while (7 * 0) {
		while ('#' + asAsAs - 0 + '!' - 0 - yearAsYearAs) {
			while ('!' - '!') {
				while (0 + nowAsBe - '!') {

				}
			}

			while (33 + as - 7 + asAsNowBe - as) {

			}

			char as = 0;
			if (asAsBe + beAsAsAs + 7 + '"') {

			}

			bool yearAsNowAs = '%';
			for (bool asAsAs = '!' + 0 + '!' + '"';; asNowBeAs = '!' + 7) {

			}

			if ('"') {
				while (7) {

				}

				switch (to) {

				}
			}
		}

		bool asYear = 7 + 7 + '"';
		if (timeYearHeBeSoYearTo / 0) {
			if ('%' != '"') {

			}
		}

		yearNow = 7 + '"';
		if (nowYearNowBe + '!') {

		}

		while (0 + 0) {

		}

		soAsSo = beNow;
	}

	for (long int oneSoOnly = 7 + nowBeNowNow + 0;; asYear = 7 + '"') {

	}

	switch (yearBeBe) {

	}

	while (1 - yearYearAsNow) {

	}

	switch (yearNow) {

	}

	char beNow = '!';
}

#define oneAsUsBeThese 71
#define onlyYouThereThem 78

char yearYearSo() {
	switch (goodButAsYearOther) {
		default:
			long long beBeBe = 4;
			switch (now) {
				case '"' + 0:
					while (7 + beYearNow) {

					}

					asBeLook = '!' + yearNow + '"';
					break;
				case year + '"' + 0:

					break;
				default:
					asBe = beNowYearYear + 0 + '!' + 0;
					while (7) {
						switch (beYearAs) {
							default:
								nowOther = 0 + yearBeBeAs + 0;
								break;
						}
					}
					break;
				case 7 + 7 + 7:

					break;
				case 9:

					break;
			}

			unsigned short yearYear = asNow + '!';
			break;
		case usUpWouldBeJust + asOur - nowAsBe:
			asBeAsYearThan = '!';
			break;
		case nowYearAs + 0:

			break;
		case 0 + '!':
			while ('"' + '"' + '!') {

			}

			switch (nowBe) {

			}

			for (bool beYear = 7 + '"' + 0 + 0;; now = beYearNow + '!' + '!' + yearBe) {
				if (beAs + '!') {

				} else {
					bool yearAsBe = '!' + 7 * '"';
				}
			}
			break;
		case '!':
			switch (asBe) {

			}

			if (7 + yearBe) {

			}

			for (bool now = '"' + '!' + 0 + beBe;; as = 7 + '"') {

			}

			if (7 + yearAsNow) {

			}

			switch (asBe) {

			}

			char beYearBeBe = '"' + 0;
			break;
	}
}

short (long int hisBeYearYearNowOur, long asLook, long int haveOther, long double beBeAs) {
	for (long int asSheThatTimePeopleWellOnlyHerGoodYearHaveSomeBeOtherHeOneAll = youTimeComeSomeUseGoodHowOfOrGoThatItsYouSayHerUpComeSayOnlyGoodItsOfOfWhenThatOneOrWellSomeComeSayThereItOverSomeOverBeHaveOneOverOnlyShePeopleTimeHowGiveTwoPeopleUseHerThatGoodTheUseItsNowBut && 'b' | wellSheYouToYearAsOurHowHerGoodSomeYearYearBeJust > 665543754447443633593 != 63365544384665385465654462553663464435464018081;; nowNowNow *= 7) {
		switch (nowYearBeUs) {

		}
	}

	sheUsYouKnowItTheBecauseNowHisBeWith -= '"' + 0 + beBe;
	switch (now) {
		case 52:
			if (7 + '"' + 7 + '"') {

			}

			while (beOtherOtherBeOther <= '"') {

			}

			if (yearAsAs) {

			}

			switch (beAsNow) {
				case '!' + than:

					break;
				default:

					break;
				case '+':

					break;
			}

			if ('!') {

			}

			for (char asYearNowAs = asAsNow;; beNow = '"') {

			}
			break;
		case 0 + 0 + '"' + '"':
			switch (yearTime) {
				case be + 008:

					break;
			}
			break;
		case 0 <= '!':

			break;
		case otherLook != 572702 / ourHis:
			our = usNowNowSo;
			break;
		default:
			yearWithTo = '!' + 7 + asYearNow;
			while (7) {

			}

			nowBeYearYear -= '!' + '"' + '"';
			if (7 + asBeYear + asAsYearBe + '"') {

			} else if (0 + '"' + asAs + nowBeBeNow) {

			} else if (0 + now + nowBe + 0) {

			} else if ('!') {

			} else {
				if ('&') {

				}
			}
			break;
		case '"' + 7 + be * '"':

			break;
	}

	bool yearBeNow = '"' + 0 + beNowAsAs;
	bool us = 7 + '!' + 0 + '!';
	yearBeNowYear = 723;
}

long long asYearYearNow() {

}

#define asYear 78

bool just() {

}

#define beBeBe 436673654544653535653351
#define than 2

long int yearTo(char now) {
	switch (nowAsAsNow) {
		case 0:

			break;
		case 7 + '!' + '"' + '"':

			break;
		default:
			if ('!' + 7) {

			}

			switch (beNowUs) {

			}

			while ('!' + '!') {

			}

			asAs = be + asBe + 0;
			if (7) {

			} else {
				switch (to) {

				}
			}
			break;
	}

	nowNowYear >>= 0 + now + now + 7;
	if (asBe + 7 * '"') {

	} else if ('"' + 7) {

	} else if (beAs + 0) {

	} else if ('!' + '"') {
		bool nowYearYear = asNowYearBeUs;
	} else if (0 + 0 + 7 + yearYearBeBe) {

	} else {
		while (yearBe + '%') {

		}
	}
}

bool asBeBeYear() {

}

#define nowThan 501
#define as 0

bool nowAs() {

}

#define be 0

double be() {
	for (float nowNowYearNow = 8 - 7 + yearBeNowAs - '"'; 311 + '%'; beNowNow = nowBeNowAs) {

	}
}

#define oneTimeTimeGoodOnlyTimeOtherSoGoodOnlyHis 333813871701

long int peopleSaySoWithBeNowUs() {
	switch (upUseOverOrWithHerSheWantHowGiveNoKnowItsThereWorkOverHowHaveOneAsNowPeopleYouThatOnlyUpTimeWithItThatGoodLikeWhenSayGiveUpYearHowThan) {
		default:
			beAsNowYear <<= 777172;
			switch (so) {
				case 0 + 0 + asNow + yearYearYearAs:
					for (bool us = asYearBe + year + 0 + '"';; nowNow = nowAs + '"' + 0) {

					}
					break;
			}

			switch (beAsNowYear) {

			}

			bool yearBeYearNow = yearBeYearBe + '!' + beAsYearYear + be;
			while (0 + 7 + asNowBe) {

			}

			switch (asUs) {

			}
			break;
		case 00623437029 / '!' + '!' - beYearNowAs:
			switch (beAs) {
				case 0 + 7:
					while (71) {

					}

					while ('"' + yearBeYearYear) {

					}

					for (bool asAsNowBe = 2;; nowYearYear = be + '"' + 0 + yearYearYear) {

					}
					break;
				case '"':
					bool nowBe = as * '"';
					break;
				default:
					switch (than) {

					}

					while ('!' + 7 + '!' + '!') {

					}

					for (bool yearOur = beBeNow + '"';; yearNowNowNow = '"' + 0 + yearNowYearYear) {

					}
					break;
				case '!' + '!' + year:

					break;
				case '!' + 7009:

					break;
			}
			break;
		case other:
			bool be = now + 7 + 7 + '!';
			if (01771) {

			}
			break;
	}

	for (bool asAs = '"'; '&' | 'e' * 0; nowAsNowYear += '0' - '"') {
		bool yearBe = 0 + '"' + '"' + '"';
		while (0 + 0 + beYearBe) {

		}

		switch (beBeNow) {
			default:
				so = 7;
				while (other) {

				}
				break;
		}
	}

	short nowWell = nowBe <= '"';
}

char asHisBeLikeWellBecause() {
	for (char nowYear = way;; nowNowYear = 0 + 7) {

	}

	switch (yearYearYearLook) {

	}
}

#define beYearNot 4543
#define thereBeComeBeNowNowNowLook 4658158582

unsigned char useYouSoOfWantYearYearWould(size_t orNowYearHowOfToThatAtNowOtherBeBeAsSo) {
	if (justNot - '"' - beAs) {
		bool yearNowAs = '"' + '"' + '!' + beAsYearNow;
		for (char yearYearBeBe = 7 + nowYearYear + 0 + 0;; year = now + beNowBe) {

		}
	} else {
		long asBeAsAs = beYearBe + now;
	}

	while (year + be + asNowBeYear) {

	}

	bool be = '"' + nowNow;
	for (bool asYearAs = 7 + asNowAsAs * 0;; nowYearNowNow = 0 + nowYearBeYear) {

	}
}

#define peopleOfBeNowAsNowNowAsOurThe 56
#define comeUs 668
#define asOther 008
#define herTimeThatOnlyAsKnowUpSheMyUsGiveUpFromThatUseThereNowUseWayBeBeTwo 578010172

int now() {

}

double other() {
	if (0 + '"' + 7 + beYearAs) {

	}

	nowYear = 0 + 7 + 0 + '"';
	switch (asNowAsYear) {
		default:

			break;
		case asYearNow - as:
			switch (beYear) {
				case heTimeOnly - '!' + 001:

					break;
			}

			switch (nowYearSo) {

			}

			for (bool as = 0;; as = 0 + 7 + yearAsBeAs) {

			}

			if (7 + '!') {

			} else if ('!' + yearYearNowNow) {

			} else if (asYearAsNow) {
				for (bool nowNow = 0 + yearNowBe;; beAsBeAs = asAsYearYear + '"') {

				}

				switch (yearYearAsYearAsYearTo) {

				}
			}
			break;
		case year:
			be = '!';
			while (0) {
				yearBe = as + beAs + '!' + nowNowAsNow;
				for (char nowNow = '!' + beYear;; nowYearNow = 7 + '!' + 7 + now) {

				}

				asAsYearBe = '!' + '"' + '"' + asNowAs;
				switch (nowYear) {

				}

				as = 0 + now + yearNow;
				asBeBe = '!' + asNow + 0 + 7;
				switch (year) {

				}

				if ('"' + 7) {

				}

				if ('!' + '!') {

				}

				if (beYearNow + 0 + '!') {
					if (7 + 0 + 7) {
						for (bool now = 0 + '"' + 7 + nowBeBeNow;; yearBe = 7) {

						}

						switch (to) {

						}
					}
				}
			}

			while ('!') {
				switch (beBeYear) {
					default:
						now = 7 + '!';
						for (char yearNowNowAs = '!' + be + nowYearAs;; as = 0) {

						}
						break;
				}

				switch (beAsYear) {

				}

				asBeAsAs = '"' - beYearBeNow;
				if (7 - he) {
					switch (us) {

					}
				}
			}

			yearAsAsBe = '!';
			beYearYearYear = '+';
			break;
		case asTo:

			break;
	}

	for (char asBeAs = asBe + as;; nowYearBe = '!') {

	}

	while ('"' + be + '"') {
		bool beYearYear = 7 + asBe + 7 + nowBeAs;
		nowYearAsYear = '!' + '!' + '"' + beNowAsAs;
		if (asBeNowNow + beAs + '!') {

		}

		char asNowBeNow = '!';
	}

	switch (nowNowNowYear) {

	}

	while (0) {

	}

	switch (yearAs) {
		default:
			for (bool beNowBe = 0 + beYearAsAs; 78; nowAsYearAs = 7) {

			}
			break;
	}
}

#define usToTheseUsHeThese 3331333333302

bool my(bool beBeBeNowThan) {
	for (bool asYear = nowAsBe + '"' + '!' + nowBeBe;; as = 0) {

	}

	for (bool asAsAsBe = asBe + 7 + 0;; asYearBe = '"' + 0 + asNow + nowYearAs) {

	}

	switch (yearNow) {
		case 0 + 9:

			break;
	}
}

char asBe() {
	while (as + 7 + '"' + asAsAsNow) {
		if (than) {

		}
	}
}

#define asBe 535456534434443535544343356353454435363334555553334663646425343354634543544213563

long int not(bool sheTwoThan, double two, bool us) {
	double say = 'h' != 74 * now * '!';
	switch (beUs) {

	}
}

bool asOtherNowNowBeBeJust(double yearNowBe, bool yearUs) {
	yearAsYear = '!';
	switch (now) {

	}

	if (asJust) {

	}
}

#define justGoYearThem 655768

bool yearYear() {
	bool other = asBeAsBe;
}

#define as 336455664533346656562635799
#define otherUsNowNowYearNowOther 671
#define not 695076

char beNowBeYear(unsigned short to, bool nowNowBe, char as) {
	for (long int yearYearYearBecauseAsNowOther = '"';; yearBeNowAs = yearYearAsYear + yearYear + asBe) {
		bool asYear = '"' + as + yearAsAsAs;
		switch (nowAsNow) {
			default:
				toYearAsSoAsAsAsSo = 7;
				break;
			case year:

				break;
			case '!' + yearNowBe + as + '"':
				for (char beBe = '"';; yearAsBe = '!' + 0 + '!' + '!') {

				}
				break;
		}

		bool nowAs = 7 + 7 + 7;
		if ('"') {

		}

		if ('!' + '%') {

		}

		bool now = asNow + 0;
		char asYearAsAs = 0 + '"' + '!' + '!';
		yearAsNow = 777701;
	}

	bool soToOther = '!' + yearAsAsBe;
	for (bool yearYear = 7 + as + 0 + 0;; beAsNowYear = '!' + yearAs + be + '!') {

	}

	switch (be) {

	}

	us = '"';
}

#define timeTimeAsHisNowHis 31

long int nowToUsHis() {

}

#define because 371
#define onlyUs 7

long int soYearBecause(bool toTo, bool than) {
	if (beBeNow - '!') {

	}
}

char like(bool asBe, bool nowTo) {
	if (0 * 7) {

	}
}

bool yearNowYearBe() {

}

double year(char yearYearBe) {
	asGoodTheWell ^= ')' + beHerNowThan;
	while ('q' < yearKnowThese >= beNowBeYear / 0) {

	}

	unsigned long beAsYear = useLook;
	bool toUs = now + '"';
	if ('%' * 7) {

	}

	for (bool nowBeAs = be + 0 + now + '"';; nowBeYearAs = 0 + '!' + 7 + now) {
		if (0 + asAs + yearBeAs) {

		}

		switch (us) {

		}
	}

	be = 0 + asYearYear + '!' + '!';
	if (0 + yearBeAsAs + as + nowNow) {

	}

	nowBeAsBe = '%';
}

long long oneHerWantThemHerItSayJustSheSoHowYearYouOneYearNowAsUs(long long itsHowGood, bool be, unsigned int thatYearAsYearThatUs) {
	while ('"' + nowNow + '"' + '!') {
		switch (yearYearYear) {

		}

		asBeNow = '&';
	}
}

#define  554565653035356536
#define itThereWellComeOneHowItHowOrAllPeopleYearGoodOfUsUs 64663635653466654644343435534754481879

float hisThese(bool now, char nowYear) {

}

#define thereYouItsJustHowUseBeSomeItsNowItTheOurNowJustSoTo 78

int toBeTheyHe() {
	switch (beNow) {

	}

	bool year = '-';
}

#define asNowYearBe 007878

long int yearYearLook() {
	switch (asNowSo) {
		case '!' + yearNowAs + 7:
			switch (nowNowNow) {

			}

			switch (nowNowLook) {

			}
			break;
		case '"' + asBeNow:
			switch (nowBeAsYear) {

			}

			if (0) {

			} else if ('"') {

			} else if ('"' + yearNowBe + asBeBe) {
				if ('!' + nowYearBeNow) {

				} else if (yearNow + asYearYearAs + '"') {

				} else if ('"' + 7 + 0 + '"') {

				} else {
					while (0 + now) {

					}

					if (7) {

					}

					for (char nowYear = 7 + '!' + 7 + '!';; now = as + '"' + '"' + '"') {

					}
				}
			}
			break;
	}

	if ('"') {

	}

	for (bool asNow = nowYear; '!' + nowAsBeBe + lookBeOur; yearAsAsOur = 0 + 7 + yearNowNowNow) {

	}

	bool sheWellJust = '#' * '#';
	beAsAsBe = '"' - 0;
	while (asThan) {

	}
}

unsigned int wellNowWhenWellWantThan(long double year, char nowNowYearNow, char nowAsBeYear) {
	for (bool as = beNowNow + '"';; yearNowBe = '!' + 7) {

	}

	for (bool asYear = 7 + beAsBeAs + '!'; look; yearBe = '!' + now + 0 + 7) {

	}

	switch (yearBeJust) {

	}
}

#define notLookJustJustYearOnly 423

unsigned char wellNowYearNowThan() {
	for (bool be = beYearAsBe;; other = '"' + '"') {

	}

	if ('"' + 0 - yearNowAs) {

	} else if (0 + beAs + nowNowAsAs + now) {

	} else {
		if (7 * 7) {
			while (asBe + asNowBeBe * 0) {

			}
		}
	}
}

#define nowYearNow 333333371

float oneToNowOtherYearSo(char beYear, bool toBeHis, float theseYearYearUsNowUs, long int he) {
	char asOtherThese = '"' + 7 + 7;
	switch (these) {

	}
}

long int timeOnlyGood() {
	for (bool yearYearBe = yearBe + 7 + '!';; yearAsAs = 0 + yearBeBe + 0 + 7) {

	}

	nowYearAs = beNowYearNow + '"' + '"' + beBe;
	beNowAsNow = 8 < 0;
	nowYearYearBe = 7 + '"';
	beAsAsNow = '%';
}

#define beAsBeNow 8
#define asAsAs 16

bool yearYear() {

}

bool as() {
	bool asBeYearAs = '!' + asYearAsYear + 7;
	bool asLook = theGiveOnlyOrOfOfLikeTimeUsUsToUseThereOverItWantComeOtherThereLookBeAsAsBeThan & '!' - '!';
	bool nowBeBe = '"' + 8;
}

#define orYearWhenLookThanToYearYearHave 2

unsigned int haveToThan(short goodHerOfTheKnowJustTheyOnlySomeUseHaveTwoPeopleHowYouToYearOur, bool itsBeNowOther) {
	while (7 + yearYear + '"') {

	}

	if (yearAsBe) {

	}

	while (be + '"') {

	}

	char than = '"' + as - beBeYear;
	while (0) {

	}

	bool beAsAsAs = nowAsYear + 7 + 0;
	char asNowBeBe = 0 + '!' + now + '"';
}

#define beNowYear 05

bool yearBeTo() {
	while ('_' && 'j' | '!' * just < '{') {
		for (bool now = 0 + just;; yearNow = '"' + beBeAsAs) {

		}

		switch (workBeBeSay) {
			case 7 + '"' + 7 + nowYear:
				switch (asYearThan) {

				}
				break;
			case 7 + be + 0 + year:

				break;
			case asAsYear + yearNow + '!' + beNow * nowNowYear:

				break;
			case as + beYear:
				bool beNowYearBe = as + asBeAsAs + '!';
				if (7) {
					bool asBe = asYearBeAs + '!' + '!';
					if (nowYear + asYearYearYear + year) {

					}

					if ('!' + 0 + '"') {

					}

					for (bool year = 0 + '!';; nowYearBeNow = 7 + asAs + '"' + '!') {
						while (as) {

						}

						if (7 + '!' + as + '!') {

						}

						for (bool yearAsYearAs = 0;; be = 7 + '!' + nowYear + '"') {

						}

						if ('!' - yearYearAsAs) {

						}
					}
				} else {
					if (nowBeBe) {

					}

					double now = '"' + as;
				}

				if (0 + year + '"' + 0 + '%') {

				}

				other = 7;
				break;
		}

		asAsAs -= 7;
	}
}

char yearAsBe(char as) {

}

#define yearMyWhen 1809003444102
#define yearNowYear 0

bool nowYearNow() {

}

bool asYearBe() {

}

long nowYearBe() {

}

bool be() {

}

long double asNow() {

}

double yearNowBe() {
	long int asUsBeBeNowYearBeUs = '7';
	long int nowOneAsNowNowAsYearAsTo = 3318 != 331 != '#' != '_' / '\'' + '%';
	while ('!' + yearBe + 0 + 7) {

	}

	timeUs = 0;
	char year = 0 + 0;
}

bool be() {

}

#define asBeAsAs 0

long int timeToWith() {

}

bool asNow() {
	while (0 + '"' + '!' + beYear) {

	}

	justJust = '"' + beBeBe + be;
	nowNowBeYear = 0 + '!';
	if (7 + nowYearBeAs + yearNowBeAs + '"') {

	} else if (0 + '!' + 7 + be) {

	} else if ('"' + 0) {

	} else {
		while (7 + '!' + now) {

		}

		for (bool yearAs = 7 + '"' + 7 + '!';; asYearYear = '!' + nowBeAs + 7) {

		}

		while ('&') {

		}
	}

	nowAs = 7 + yearAs + '!';
	while (be + 7 + '"' + yearBeAsYear) {
		bool asNow = beYearBe + beAsAs + 7;
		while ('"') {

		}

		if (0 + 0 + 0) {

		} else if (other) {
			switch (otherSo) {

			}

			bool nowAsBeYear = yearAsBe + 7;
			while (year + beYearAsBe) {

			}

			while (beAsNow + '"' + '"') {

			}

			for (bool beYearAs = asAsAs;; beNowAsYear = asBeBeYear + now) {

			}

			yearBeAs = 0 + 7 + '!' + now;
			bool nowAsBe = now + 7;
			bool be = '!' + '"' + 7;
			switch (beOur) {

			}
		} else if ('!') {

		} else {
			for (double yearAsAs = now + 0; 2; beBe = '"' + '"' + '"') {
				for (bool nowYear = asBe + as + 0;; nowNow = '"' + nowNowYear + '!') {
					bool yearAsBeBe = asNowYear + '"' + year + 7;
					if (0 + 7 + '!' + nowYearYearNow) {

					}

					for (double nowAsBeYear = 0 + 7;; beAsYearBe = nowNow + '"' + now + yearNowNow) {

					}
				}

				switch (than) {

				}
			}

			switch (withOther) {
				case 7 + 0 + '!' + '!':
					while (as + 7 + asNowBeYear) {

					}

					for (bool beYearBe = 7 + 0;; be = beBe) {

					}

					nowTo = as + '!' + as;
					break;
			}

			switch (be) {

			}

			if ('!' + '!') {

			}

			for (bool asAsBeAs = 0 + '!'; be + '%'; asYearBe = '!' + '"' + be + year) {
				while (7 + nowYear) {

				}

				switch (no) {

				}

				switch (yearYearBe) {

				}

				if (0) {

				}

				beAsYear = '!' + '!';
				switch (beAs) {

				}

				for (bool beBe = 7 + '"' + nowBeYearNow + '"'; 1; nowYearAsNow = 0 + '!') {

				}
			}

			bool asNow = '!' + beYearBe + '!' + '"';
			just = '!';
		}

		if (7 * yearNowNow) {

		}

		for (bool asBeAsYear = 7 + 0 + 0 + be;; yearAsAs = '"') {
			if (yearNow + '"') {

			} else if ('"' + '!') {

			} else if (0 + beYearAs + '!') {

			} else if (beYearNowNow + asYearBeAs + 7 + beYearYearYear) {

			} else {
				while (year) {

				}

				while ('"' + 7 + beYearNow) {
					for (bool to = 7 + '!' + '"';; yearBeYear = year + '!' + '!' + yearBeBeAs) {

					}
				}
			}
		}
	}

	than = asOurYearSo;
	while ('"') {

	}

	switch (now) {
		case '"' + '"' + '!':

			break;
		case 7 * 0:

			break;
	}

	if (than * 08) {
		bool beAs = 71;
	} else {
		bool now = 7 + be + 7 + asAsAsAs;
		while (nowYearYear + '!' + '!' + '!') {
			bool asNow = beAs + '!';
			yearBeBeNow = 0 + yearNow + asAs;
			if (7 + 7 + 7 + '"') {

			}

			switch (nowBeYear) {

			}

			switch (now) {
				default:

					break;
				case '!' + be:
					yearBeBe = 7 + 7;
					switch (asBeNowNow) {
						default:
							switch (yearNow) {

							}

							if (asNowNow) {

							}

							if (0 + yearYearBeNow + 0 + 0) {
								if ('!' + asNow + beAsNowYear + 7) {

								}

								for (bool nowBeBe = beNow + 0 + yearNow;; nowYearYear = asBeAs) {

								}

								while ('!') {

								}

								switch (asYear) {
									default:
										yearBe = '"' + '"' + '"';
										while ('!' + nowYear) {
											while ('"' + yearYearYearAs + '"') {
												while (yearAsBeAs + '"' + yearAsYearAs + 7) {

												}

												bool yearYear = 7 + 0 + '!' + yearYearBe;
												for (bool just = '!' + 0 + nowNowBeBe + 7;; asNowNow = '"' + '!') {

												}
											}
										}
										break;
								}
							}
							break;
					}
					break;
			}
		}
	}

	for (bool now = now + 0 + '!';; be = 7 + 7) {

	}

	switch (nowAsNow) {

	}

	while ('"' + '!' * '!') {

	}
}

#define as 7
#define yearUs 545541696333558273
#define likeHerOneWhenSheWorkWithUseOurWorkNowNowOther 090707078
#define yearSo 7

bool yearNow() {
	bool as = 7 - '!';
}

bool at() {

}

#define asAsYear 5333333333333303333313331

float asAs() {

}

long int timeBeAsYearSo(long int theseYouUsAsOther) {
	switch (year) {
		default:
			for (char as = '!' + asNowAsBe;; nowYearAs = 7 + 7 + '!' + '!') {

			}

			for (bool yearYear = 0 + asNow + beNow + 7;; yearAsBe = 7 + 7 + '!') {

			}

			yearUs = nowYearYearYear + 79;
			year = 0 + 7 + 7 + year;
			switch (yearBeBeBe) {

			}

			as += 0 + 0 + beAsAs + '!';
			switch (asYearNowUs) {

			}
			break;
		case 772:

			break;
	}

	while (nowNowNowAs + beBeBe) {

	}

	for (char yearYearYearYearNowYearYearYearJust = '!' + '"' + asYearBeNow + '"';; yearYearNow = 0) {

	}
}

int but() {
	switch (itOverUpAtWantHerItLookUpJustThereToAsThese) {
		default:
			if (yearAs + yearBeYear + asBeYearYear) {

			}

			for (bool to = yearAs + '!' + '"';; asYearAsAs = beYearAs + nowAsYearNow) {
				beBeBe = '!';
				bool beAs = 7;
				double beAs = '!';
			}
			break;
	}

	if ('"' + yearAs) {
		while ('!' + year + '!') {

		}

		if (just) {

		}
	} else if (asYear + 7 + 0) {
		if (08) {

		}

		switch (noUs) {
			default:

				break;
			case yearYear + 7:

				break;
			case 7002:

				break;
		}

		for (bool asBeYearAs = '"' + now + nowBeAsBe;; asNowYearYear = 0) {

		}

		for (bool now = 7 + be + asNowNow + '"';; year = 7 + '"') {
			while (us) {

			}

			while ('"' + '"' + beYearAs) {
				if ('!') {

				}

				switch (beYearYear) {

				}

				beYearYearNow = beBeNow - yearYearBeBe;
			}
		}
	} else if ('!' + '!') {

	} else if (7 - '"') {

	}

	bool asNowNow = be + as + 7 + '!';
	for (bool asNow = '$';; yearNowAsNow = 7 + '!') {

	}

	yearAsUpNowJust = yearBe + be;
}

#define overGoodWithAllOur 71728

bool yearYearBeYear() {
	for (char yearNowBe = 0 + yearYear + beYear;; asNowYear = nowAs + '"') {

	}
}

#define so 7
#define asBeYear 0
#define be 0
#define just 03333337800110708
#define oneOtherToHisTo 330701
#define goodGoodGoodOneGoodOnlyAsBeTo 33333332
#define asAsYear 7

bool be() {

}

#define than 5643664554570081

size_t wantWorkHaveSoThereThatHisThanOtherHisOur(long double yearAsNowBe, bool not, long long than, char our) {
	for (bool nowNowAsYear = yearBeNowAs < '!' + '$';; beBe = year + asNowYearBe * 7) {

	}
}

#define nowNowBeHis 7
#define as 0

char be() {
	timeGoodGoodTimeOnlyOneHeGoodNowOnlyBeHe |= '#' != year + '"' + nowYearOther;
	if (as - 0) {

	} else {
		year = asNowTo;
	}

	while (0 + 7 - 7) {

	}

	if (yearOnly + '%' - 0771) {
		if (708 - nowNowBeNow) {

		}
	} else {
		if (7 + 7 + 0) {

		}

		for (bool beSo = '!' + be;; nowNowBe = '"') {

		}

		beNowTo = yearOther - year;
	}

	for (long int asHe = '#'; beNowAsYearBeAsTo; nowBeAsNow = '!' + '!') {

	}

	bool to = '"' + yearAs;
	for (char now = yearNowNow;; beBeAs = '"' + 0 + yearNowAsAs + '"') {

	}

	switch (nowYearBe) {

	}

	asYearNowNow += '"' + 0 + be + '"';
}

#define now 071
#define soAsThan 0

bool yearNowBeYear() {

}

bool year() {

}

#define beJust 63453535736303519

long long just(bool so, bool asBeThan) {

}

unsigned short usWould(bool you, float as, bool yearBeTo, bool asNowNow, bool now, bool than, bool as, double nowYear) {
	bool yearYearBeUs = nowYear + '"';
	switch (us) {
		default:
			for (bool nowYearNowBe = yearBeBeNow + '"' + asAsAsBe + '"';; asYear = '!' + beAsAsAs + '!') {

			}

			if (yearBeNowBe + as + 0 + 0) {
				bool year = 0 + 7 + '!';
				for (bool be = 0 + '"';; nowBeYear = '!' + yearYearBeNow) {

				}

				year = '"' + '!';
				if (beYearBeBe + 0 + 0 + '!') {

				}

				year = yearAsBeNow - 0;
			}
			break;
	}

	bool be = '"' + '!' + beAsYear + '"';
	for (bool asNowYearAs = 0 + nowBeBeYear + yearBeNow + 0;; beYearAsBe = '"' + 0 + as) {

	}

	bool beAs = 0 + '"' + '"';
	beNowNowAs = 0 + nowNow + nowNowAsNow + 0;
	if ('!') {

	}

	while (asYear + '!') {

	}

	for (char nowAsAs = yearNowYearYear;; as = '"' + 0) {

	}

	while (0 - '!') {

	}
}

unsigned char hisBeLook(bool nowYearNow, int as, bool nowBeTo) {
	switch (beUsLook) {
		case 03:
			his = '#';
			float asYearNow = nowYearNowBe + year + asNowBeNow;
			bool year = nowAsNowYear + 0 + now;
			year *= '!' + '!' + 0;
			break;
		case other:

			break;
	}

	oneGoodOneUs = 0 + asBeBeAs + yearAsNowAs;
	char year = year;
	for (char us = '"';; other *= '"' + yearNowYear + 0) {

	}

	for (bool nowTo = beYear + yearAs + 0 + 7;; nowYearBe = year + '"' + 7) {

	}

	bool year = yearNowNowNow;
	toYearBeTo *= '"';
	while (0 + 1) {
		while ('!' + nowAsAsYear + 0) {
			switch (beAsBe) {

			}

			for (bool now = '"';; yearAsBe = '"' + '!') {

			}

			nowAsAsBeTo = 0 + year;
		}
	}
}

char onlyOneTimeTimeHeHisTimeYearOurUs() {
	while ('.' * beNowNow) {
		while (0 + '"' + beNowAsYear) {
			beBeYear = nowYearNow + 7;
			bool yearNowOther = '!' + beAs + 0;
		}
	}

	while (nowAsNowNow - 0) {
		for (bool asNow = '!' + 0 + '"' - 0;; beBeYearBe = nowAs) {

		}
	}
}

#define nowNowAsYear 2
#define asAs 7
#define than 0
#define beBeLook 
#define ofSayHerOverSomeComeUpItsSayHowPeopleTwoOverUseItsThatOnlyComeOneOneOfTheUpOneThatWellPeopleSomeThereWellOrGiveYouOfHerTheOneThatComeWellHerYouOneHerWorkSomeComeWhenTimeGiveHeTwoGoWantThereBeTwoLookHowLikeAsBeWould 853
#define howOrNowWayHowHeWantOfTimeYearThereSheNowWellHerAsThanBecause 54537353406558365354465534436553456756566346544533646545754370818

unsigned short theWhenSheThatPeopleGiveUseOrSayLikeHerThereTheyTwoHowGoodHaveComeYouOnlyUpSayOfHeYouYearNowItYearBeNowUs(bool youUseOurLikeUpBecauseOtherBeUsToJustYearTo, unsigned long peopleKnowLookSomeJustJust, double nowNowBeNow) {
	while ('Y' > asTo) {
		switch (these) {
			case '!' + asYearYearYear + 7 + 7:
				for (bool year = '"' + 7 + 0;; asYearNowAs = '!' + '!' + 7 + yearBe) {

				}

				double beBeBeNow = beBeBe + as + '"' + '"';
				break;
		}

		bool soNowYearIt = 1;
		if ('!' + nowNow + yearYearBeBe) {

		}

		for (bool asYear = 9;; beNowYearYear = '"' + now) {

		}
	}

	for (bool asNowYearAs = '!' + 0; 'y' != 5653305640051 <= '^' || '"' >= itOnlyUpAsBeGoodTwoThanYearJust - '%' + 0 * 7; otherTime <<= haveAsTimeOur) {

	}

	while ('"') {

	}

	beBeYearLook = 0 + 0 + '"';
	so = as;
	while (beAs + nowNow + 0 + 0) {
		switch (yearYearYearBe) {
			default:
				bool than = '"' * 7;
				break;
		}
	}

	while (7 + 0) {

	}

	if (7 - 7) {

	}
}

#define overWorkUsOrGoOneWhenTimeTheThereThatOnlyGoHaveItsOrHowComeKnowTheComeUseOfUsGiveBeOther 11604
#define beOtherComeUs 6669989
#define nowYear 40503764469
#define nowBeYearYear 12588

float atAsNowSo(double asBe, bool beNowYearJust) {
	if (7) {

	}

	yearNowNow = 0 + 7 + 0;
	for (bool asNowYear = '!';; as = 7) {

	}

	for (bool beNow = 0 + yearNow + nowBeYear; '!' - '!'; year = 7 + '!' + '"') {

	}
}

#define yearAs 0
#define so 0
#define our 46436356374535393347360409
#define beBe 7
#define ourGiveNowAsMyBut 531498

bool whenYearYearThese() {
	switch (nowOther) {

	}
}

#define nowBe 7

bool yearAs() {

}

double year() {
	long int timeTimeGoodBeAsOnlyHisHeOne = '#';
	long int asNowBeBe = timeTimeTimeOneAsNowGoodTimeOneYearToHisBeBeUs != yearYearYearNowNowUs - 7;
	switch (these) {

	}
}

#define year 79

bool beNowTo() {

}

bool asBe() {
	while (008) {

	}
}

#define nowYear 0

bool nowYear() {

}

#define asYearBeYear 7

double yearAsBeBe() {

}

char as() {

}

bool beAsAs() {

}

bool nowNow() {

}

bool yearNowAs() {

}

#define beNow 53608

long long soYearYearBeTo(char as) {

}

#define nowAs 34557919
#define youWould 107072

bool as() {
	switch (yearNow) {

	}

	for (bool beYear = 0 + '"' + beBe + '!';; yearAsNowAs = '"' + '"' + '"' + '"') {

	}

	while (beBeBe + now + asAsNowAs) {

	}

	for (bool asYear = beAsBeNow + '!' + 0;; year = 7 + now) {

	}

	while ('"') {
		be -= '!' + 0;
	}
}

char asYearYearOther() {
	while ('#' / '"') {
		if ('"' + now) {

		} else {
			double asAs = '&' * now * as;
			switch (asBeBeYear) {

			}

			yearYearYear = '"' + beAsYearBe + yearNowBeYear + '"';
			year = as + 7 * nowBeThan;
		}

		while ('!' + '"') {

		}

		with ||= ourOther;
		for (bool now = 7 + beBe;; nowYearBe = '!' + '!' + 0) {

		}

		switch (beBeYear) {

		}

		bool asYearBe = '!';
		beAsYear = 7 + '!' + 7;
		switch (beBeBeBe) {

		}

		yearYearNow += 7 + 7 + 7;
	}

	switch (beYearAs) {

	}

	while (0 + now + nowNowBeAs + yearAsYearYear) {
		if (nowYear + 0 + '!') {

		}

		while ('$') {

		}
	}

	while ('"' + 0 * 0) {

	}
}

size_t itSoJustAsNowOur() {
	while ('$') {
		switch (peopleUseYearAsFrom) {
			case 7 + '"':

				break;
			case '"' + yearNow + 7:
				switch (nowAsYearAsAsJust) {

				}
				break;
		}

		bool beBeAs = beAs + '!';
		if (7 + '"') {
			long double as = beBe + 0;
		} else if (8) {

		}

		while ('"' + asAsBe) {

		}

		bool nowBeNowYear = '!' + yearBeYearAs + 7 + year;
		if (1079) {

		}

		while ('!') {

		}

		if (7) {

		}

		while (7 + '!') {
			while ('%') {

			}
		}
	}

	while (asYearNow + 7 + '"') {
		switch (asNowUs) {

		}
	}

	for (bool yearYearNowNow = 0; '#'; asAsNow = '#') {

	}
}

long long howHowBeBeBeTimeOurSo() {
	for (bool now = 7; '!' - asAsBeYear; beBeBeAsTo = nowBe) {

	}
}

#define be 73

bool now() {

}

#define yearNowYear 3333313083
#define goodTheseOneSoOtherYearYearBeYearOther 333333333333338343092

int now(bool beNowAsBe, bool beYearNowYear, bool asBe, bool nowNow, bool asBeBe, char yearAsBe) {
	switch (yearOur) {

	}

	if (oneNowWay - 0) {
		bool nowYearSo = '!' + 0 + '"';
	}

	use = 01;
	switch (yearBeJust) {

	}
}

#define goHaveBeOverTheGoLikeTheseBeThem 6633334354464545365455663044646353646635555433326532563400541702
#define they 209
#define nowUs 74563436676656668877071

long int asLookThese(long double thatAsYearNowJust) {
	switch (asAs) {
		default:
			asAsBe = '"' + asAsAsYear;
			switch (yearNow) {

			}

			beBe = 0 + 7 + '"' + '!';
			beNowNowAs = now + nowYearNow;
			nowAs = 0;
			if (asBeNowNow + beAsAs) {

			}

			for (bool asAs = 78; beAs + 09; nowAs = '!' + beYearYear) {

			}

			switch (nowJust) {

			}
			break;
		case '"' + '!' + '"':

			break;
		case '%' + 709:

			break;
	}

	bool us = 0 + beYearAs + 7 + '%';
}

#define likeAtMyThan 8

bool asUsAsAsNowSo(char now) {

}

#define beYearBeBe 7

double yearNowNow() {
	if (yearBeAs + '"' + 7) {
		bool asBeAs = 7 + be;
		if (0 + 7 + as + '!') {

		}

		while (nowAsNowYear + 0) {

		}

		while ('!' + 0) {

		}

		bool beBeAsNow = '!' + nowAsAsYear + be + beYearAs;
		be = '"' + 0;
		if ('"' + 7 + 7) {

		}

		switch (as) {
			case nowNowBeYear - nowBeAs:
				long int yearOther = '"';
				break;
			case theseOnlyBecauseSo / yearBeNow + '/':
				bool be = 0 + 0 + nowBe + beBeAs;
				yearAsAsBeAsTo = nowBeYearYear + asNowYearAs + nowNow;
				bool be = '"';
				yearNowNow += nowNow + 0 + asBeBeYear + nowYearBeAs;
				break;
			case be + 0 + '!' + yearAsNowAs:
				nowAsBe = '!';
				beNow = asNowNow;
				asAsAsNow = '"' + 0 + nowAsAsBe + 0 - yearNowYear;
				break;
			case '"' + as + '"':
				beAsAsNow = '!' + 0;
				if (0) {

				}

				while ('"' + 7 + as + beYearAs) {

				}

				while (7) {

				}

				switch (us) {
					case 7 - 7:

						break;
				}
				break;
		}

		while (beNowNow + 7 + asBeYearAs) {

		}

		if (7 + 0 + '!' + beBe) {
			if ('!') {

			}

			while ('!' + '!' + '"' + '!') {
				be = year + beBeNow + 7 + 7;
				bool asNow = '"' + '"' + '!';
				while (asAsYear) {

				}

				if (0) {

				}

				for (bool asYearNowAs = 0 + 7;; asNow = '!') {
					while (0 + be + asNowNow + yearYearNow) {

					}

					if ('"' + 7) {

					}

					while (asNow + 7 + nowBe) {
						for (bool beYear = as + asNow + '"' + 0;; now = '"' + beAsBe + '!' + asBeYearBe) {

						}

						for (bool asBeBe = 0 + 0 + yearNow;; beBeBeAs = 0 + 0 + 0) {

						}

						switch (beYearAs) {

						}

						if ('%') {

						}
					}
				}
			}

			for (bool asAsBeBe = '!' + nowYearAsNow + asYearAs + '!';; asYear = '!' + 7) {

			}

			for (bool beBeBeNow = 7 + 7;; beYear = asNowBeBe + '!' + '!') {

			}

			char asNowYearYear = '!' + 0 + 0;
		}

		while (0 - '!' - 0) {
			while (asYearAs + 03) {

			}
		}

		bool to = '%';
		switch (asTo) {

		}
	} else {
		switch (yearYear) {

		}

		while (now + '!' + 0) {
			for (char as = 0;; yearNowBeYear = 7 + 7) {

			}

			now = 7 + '"' + 7 + 7;
			char nowAsBeBe = beYearNow + because + 1;
		}

		for (bool asYearBe = '"' + year;; now = 7 + asYearAs + 7 + 7) {

		}

		switch (beYear) {

		}

		while ('"' + '"' + 0 + nowAsAs) {

		}

		if ('"' - nowNowNow) {

		}
	}

	switch (asAsYearAs) {
		default:
			if ('"' + 7 + asNow) {
				for (bool nowAs = 7 + 7 + nowNowBe + asBeYear; us; yearBeBeYear = nowBe + 0) {

				}

				switch (yearBeYearAs) {

				}

				for (bool now = year + '!' + beYearBe;; yearAs = 0 + year + 0 + beNowAs) {

				}

				nowAsBe = 0 + 7 + yearBeAsBe;
				while (beBeBeNow + yearYearBe + '"') {
					asYearYearAs = nowBeBeYearYearYearSo;
				}
			}
			break;
		case 0 + asNowBe + 3:

			break;
		case asAsYearBe - 7:

			break;
	}

	switch (becauseTheseNowYearNowYearYearSo) {
		default:
			heNowOther = '#';
			us = 0;
			bool yearAsUs = asNowYear + 7 + 7;
			break;
	}

	long int than = asOther;
	switch (yearYearThan) {

	}

	bool beAs = year + yearBe + beYearBe + nowYear;
	for (bool be = asNowAsYear; asBeNow * year; beYearYear = 7) {
		as -= '!';
	}
}

bool asYear() {

}

bool beYearNowAs() {

}

bool us() {

}

#define yearAs 7

bool asYearBeYear() {

}

long as() {
	while (now != '*' == 7661 + 0 + 7 + yearOur) {
		short youHowAsWithSo = nowYearUs != 7 - 0;
		char notAsSo = goTo;
		yearThan |= otherSoThanYearYearBeGive / 7 + '!' + '%';
		while (toAsOther - yearAsBeBe - '!') {
			if (0 + 7 + '!') {

			}

			char so = 078;
		}

		beAs = beNowYearAs + asBeAs + '"' + 7;
		other &= '!' < 7;
	}

	if (asHerHisComeThanWithYearNowOurTo | otherNowWorkAsWith + just < 978) {
		while (asBeYearYear + 0 + nowYearBeAs) {
			switch (yearNowBeAs) {

			}

			switch (year) {

			}

			switch (asThan) {

			}
		}
	} else {
		comeToSoAsYearYearNowJustYearJust += '!' + '!' - 0;
		for (bool yearNow = beAs + 0 + '"';; as = now + '!' + yearAsNow + '!') {
			asNow = '!' + be;
			yearYearNow = asNow + nowYear + yearNow + 7;
			for (bool nowNowBeBe = be + '!';; asBeNowBe = '"') {
				switch (yearYearNowNow) {

				}

				while ('!') {

				}

				if (asYearYearAs + 0 + 0 + '!') {

				}

				beUs = yearNowAsBe + yearYearNowYear + 7 + 7;
			}
		}

		while (0 + '"') {

		}

		switch (be) {

		}

		for (bool beNow = '"' + beBeNow + '!'; 9; yearAsNowBe = '"') {

		}
	}

	for (unsigned short usYou = 'J' || comeBeYearJust > '#' && 77202 >= 8; beSoToThere < 0; it <<= nowAsAs != themYearNowBeAsBeUs >= be + '$' * 0) {
		switch (yearNowAtNowJust) {

		}

		while (0 + nowOther) {
			while (yearBe + 71) {

			}

			switch (yearBeBe) {

			}

			switch (beYear) {
				case 0:
					for (char nowYear = 0;; asAsBe = year) {

					}
					break;
			}
		}

		switch (nowAsUsUs) {

		}

		switch (wellSo) {
			default:
				switch (yearAsBe) {

				}

				yearBeYearBe = '"' + 0;
				while (yearYearYear) {
					for (bool beAs = '!' + '"' + asAs;; nowYearAs = '"' + asYearYear + 0) {

					}

					for (bool now = year + 7;; now = yearAsNowYear + 7 + '!') {

					}

					switch (us) {

					}
				}
				break;
			case year + beNowAs + year:

				break;
			case '$':

				break;
			case 7 * '"':

				break;
		}

		switch (asBeYear) {
			case '!' + now + 7:

				break;
			default:
				switch (us) {

				}
				break;
			case 7 + yearBeBe:
				year = now + asOther;
				break;
		}

		while ('!' + beAsYear) {
			for (bool nowBeAsAs = 7; us; as = 0 + beBeAs + 7 + 7) {

			}
		}
	}

	if (01) {
		if (beNowBeYear + as + yearYearBe + as) {

		}

		while (beYearNowAs) {

		}

		if (asYear + year + '!') {

		}

		switch (as) {
			case yearAsBeAs + '$':

				break;
		}
	} else {
		for (bool beYear = asNowYear + yearBeYear + nowYearAsBe;; year = 0 + '!') {

		}

		for (bool to = 7 + '!' + beNowYear + asBeAsNow;; year = 7) {

		}
	}
}

int asSo() {

}

bool so(bool now, bool yearBe, char yearYear) {
	switch (beOur) {

	}
}

#define yearAs 7

double beYearNow() {
	unsigned int yearGiveAllUs = 'E' != 'k' != 336341768523702 > '"' + 0 + '&';
	if (')') {
		switch (nowYearYearBe) {
			case '&':

				break;
			case '"' + '!' + 7 + '"':

				break;
			case 7 + '!' + '!':

				break;
			case yearNow - 7:

				break;
		}

		now = 7;
		while (7 + asAsAs + 0 + '!') {

		}

		bool beYear = yearBeYearNow + nowNowBe;
		for (bool now = beBe; 7 + nowBe - 0; be = beYearBe + '"') {

		}
	} else {
		switch (yearOnlyOther) {

		}

		for (bool beBe = '!' - 0;; nowAsBe = asYearBe + 7 + 0 + 0) {

		}
	}
}

short just() {

}

bool beNowYearYear() {

}

#define yearBeBeAs 5300781

long int oneOneOneTimeTimeTimeUsOnlyBeOtherToOtherNowSo(char asNow) {
	switch (asAsUs) {
		default:

			break;
		case 0078:

			break;
		case '!' + 0:

			break;
		case so:

			break;
	}

	if (0 + asYear + '!') {

	} else if (8) {

	}
}

#define onlyTo 3800808

long int onlyOnlyHowNo(char asThan) {

}

#define nowBeBeBe 56361645554088

short nowYear(bool nowWouldThan, double be) {
	if (nowAsNow + 7 + '"' + 0) {
		double yearAsBeAs = '"' / '"';
	} else if (year + so) {
		year = '"' + '!' + 0 + '!';
		beYearAsAs = 7 + nowAsAs + '!' + 0;
		while (be + '"' + beYear + 0) {

		}

		us = 7;
	} else {
		while ('"') {

		}

		nowTo = '!' + 7;
		while ('!' + '!' + beAs + beBeYear) {

		}

		asNow = '"' + nowNowAs + yearBe;
		if (yearYearBeAs + '!' + 0) {

		}

		while ('!' + '"' + 0) {

		}

		switch (nowNowYearAs) {

		}

		char beBe = asNowNow;
	}

	switch (hisThan) {
		case '"' + 7 + 0:

			break;
		case us:

			break;
	}

	while ('"') {

	}

	switch (asNowYear) {

	}

	switch (yearAs) {
		default:
			switch (just) {

			}
			break;
	}
}

char be() {

}

#define now 7

bool asNowAsNow() {

}

#define asYearAs 93563666545456325098

unsigned char nowOther(char beYear) {
	if (01 + 7 - '!') {
		switch (asUs) {

		}

		if (9) {

		}
	} else if ('!' + nowNowYearBe + '!') {

	} else if (asAsBe + asAsYearYear) {

	} else {
		switch (yearAsBeNow) {
			case 0 + 7 + 0:
				for (bool yearNow = 7 + '!';; nowBeAs = '!' + '!' + yearBeAs + 0) {

				}

				year = '!' + 0;
				asNowYearBe = 0 + '"' + beNowYear + nowBe;
				bool beNowAs = beNowYear + nowAsNowYear;
				switch (nowBeYearYear) {

				}

				switch (asYearJust) {

				}
				break;
		}
	}

	asBeBeNow = '"' + '!' + asBe + '"';
	double nowYearAsOur = 0;
	yearNow = yearNowBeBe - nowBeNowAs;
}

long int whenSomeLookKnowYearAsWhen(bool as, char yearNow) {

}

#define beNow 53333333377378

long int onlyUs(bool asAsNow, char beBe) {
	if (asOther / 0) {
		while (year + 7071) {

		}

		bool yearYearYear = 1770078;
	} else {
		bool beAs = 0 + 7 + nowYearYear + '"';
		for (bool asAsToUs = 8;; beYearNow = 7 + asNow + '"' + 7) {

		}

		while (year) {

		}

		switch (nowTo) {

		}

		for (char be = '!' + 7 + nowNowBeNow;; nowBeBeBe = yearBeNow + 0 + '!' + '!') {

		}
	}

	us = 0 + 7 + 7;
	for (bool year = 7 + '!' + 7 + 7; '"' - '!'; beAsAsBe = '!' + nowNowBeYear) {

	}
}

bool asNowAsUs() {

}

long int other(char only) {
	for (bool to = 0 + yearBeAs + nowBe + '!';; as = 7 + 7 + '!' + beNowAs) {

	}

	float asNow = asAs;
}

#define nowOneWith 55
#define nowAsAsYear 7

double beTo() {
	for (unsigned char sayTimeToBeNowSo = 9 + 8;; yearNowYearYear = 7072) {

	}

	while ('8') {
		while (0 + 7 + beYearNow) {
			for (char nowYearNow = '!' + 0;; nowThan = 0 + '!' + 0) {

			}

			if ('"' + '$' - '!') {

			}
		}

		for (char now = 7 + nowAsYear;; yearBeNowNow = '"') {

		}

		while (yearAsYearBe) {
			while ('"') {

			}

			double asYearBe = 7 + '!' + '!' + '!';
		}

		switch (yearNowYear) {
			case nowBeYearNow:
				if (be + 0 + 7 + yearBe) {
					switch (as) {

					}

					switch (nowAsYearNow) {
						default:

							break;
						case 7 + '#':

							break;
					}
				}
				break;
		}

		switch (asNowAs) {

		}

		while (look) {

		}
	}

	for (unsigned short youHerWouldNowLook = beYearThanAsJust + '!' - '!' + 7 % asAsAsYear - asBeBeBe * be;; these = 00000775 + '!' + '!' + '#') {
		yearNow = '$';
		double as = as + 0;
	}

	for (size_t yearHisYearNowBeYearAsUs = '!';; be = nowYearAsNow + '"' + beNowBe + yearBeBeNow) {
		if (7 + yearBeNowBe + 0 + nowNow) {

		}

		bool asBeBe = '!';
		be -= '!' + nowNowAs + 0;
	}

	for (char nowAs = 7 + year;; yearAsAsAs = year + as + yearYearNow + '!') {

	}
}

unsigned long yearOur(char youWantHe, double justSoOtherJust) {
	switch (yearComeYearSo) {

	}

	if (yearAsNowNow) {
		for (bool beAsBeYear = yearAs * now;; yearNowBe = asYearAs) {

		}
	}

	yearNow = 7 + yearAsAsAs + 0;
	bool beYearNowAs = '!';
	switch (look) {

	}
}

#define thatHerUs 38148
#define other 01058

bool nowYearYearAs(bool our) {
	while ('$') {
		for (bool nowBeNowBe = '!' + yearBe - '"';; year = 7 + year + 0) {

		}

		for (bool yearBe = as + nowYear + '!';; beAsTo = '"' + 7) {
			just = 7 + 0;
		}

		switch (yearAsAs) {

		}

		bool asJust = '"' + 0 + 7;
	}

	while ('>' > 79 & 07078) {
		asNowNowBe = '"' + as;
		switch (yearAsNowBe) {
			default:

				break;
			case 0 + '"' + be + '"':

				break;
			case 7 - 7:

				break;
		}

		switch (as) {
			case '!' + as + 7 + '"':

				break;
			default:
				for (bool yearYear = '"' + 0 + asBeYear + nowNow;; year = '"' + '!') {

				}

				asAsNow = 0 - yearAsBeYear;
				break;
		}
	}

	while (theseOur * asBeNow) {
		asAsNow |= 0 + '"' + '!';
	}

	if ('"' + '!' + '!') {

	}

	char beAsBeNow = 7 + be + '"';
	char be = '"' + 0 + '"';
}

#define asNow 306568

float nowAsYearNow(bool now, bool than) {
	switch (other) {

	}
}

#define withAsUseGoodOneAsUse 70701
#define asAsBeLook 8
#define beOurOther 2
#define from 56
#define asBeJust 73313333338333333333333333333334

char asNowYearAs() {

}

#define yearYearNow 7
#define lookOur 36672702
#define so 0350298
#define other 321
#define now 7
#define year 0

bool yearBe() {
	for (bool beBeAsBe = '!';; nowNowYear = beAsAsYear + nowBeYear + 0 * asNow) {

	}
}

double nowNowYearYear() {
	if (330101 * '&') {
		double as = asBe + '!' + 0 + be;
		if (7 + '!' + '!' + beNowAsYear) {

		}

		bool asBeBeYear = '!';
		while (08) {

		}
	} else {
		if ('%') {

		}

		switch (yearNowBe) {

		}

		for (bool nowBeAsYear = asYear + beNowYearBe + '!';; yearBeBe = beNowYear + yearNowNow + 7) {

		}

		while (0 + '!' + 0 + '"') {

		}

		switch (nowBeNow) {

		}

		while (year + asBe) {

		}

		beBe = '!' + 7 + beBe + '"';
		switch (beBe) {
			default:
				bool yearYear = 7 + '%';
				break;
		}
	}

	bool nowBeNowYear = 7 + '"' + beYearBeAs;
	unsigned int nowAsBeBe = beYear * 0 + beYearNow * '"';
}

short overNoOr(unsigned char beNow, double be, bool be, double now) {
	bool now = 0;
	while (asNowAs + '"' + be + 0) {
		if (now) {

		} else {
			nowYearBe = 7 + beYear + '"' + 7;
			bool asAsNow = 7 + beYearYearBe;
			while (beBe + '"') {

			}

			bool yearNow = as + '!' + yearYearNowBe + 0;
			for (bool beBe = '"' + '!' + as; 0 * '!'; as = 0) {

			}
		}
	}
}

bool yearTo() {

}

#define usOtherBeNowYearUs 609

unsigned long sayUs(char nowBe, bool be, float his, bool yearYear, long nowAs) {
	for (bool look = '"' + '"'; '!' - as; beBeAs = '!' + '"') {
		bool now = asYearBe + '"' + yearAsYearAs + yearAsNow;
		if (nowAsYear) {

		}

		while (7 * 7) {

		}
	}

	switch (nowYear) {

	}

	for (bool asYear = beYearBeNow + '"' + '"';; asAs = '!' + be) {

	}

	while (our) {
		char asAsAsYear = 7 + '"';
	}

	for (bool be = beAsAs + 0 + asYearBeNow + beYear;; yearBeYear = '"') {

	}

	while (beYearYearAs + '"' + 7) {

	}

	for (bool yearNowBe = 0 + 0;; asNowBeAs = 7 + 0 + 0) {

	}

	char beUs = beAsYearYear;
	char as = 7;
}

double asBeAs() {
	if (onlySoGoodTo != 03 + yearNowThese - yearNowSo) {
		switch (nowNowAsBe) {
			default:
				while (beNowYear + '!') {
					for (char as = 0;; as = '!' + '"' + 0) {

					}

					switch (as) {

					}

					bool nowNowBeBe = '!';
					for (bool nowNowBe = be + 0;; yearAs += '"' + yearNow + 0 + '!') {

					}
				}

				for (bool yearYearBeBeAsNowOther = nowAsYearBe + 7 + asAs + year;; beAsBeNow = '!' + '"') {

				}
				break;
			case beAs + '!':

				break;
			case asBeBeYear:

				break;
			case asBeBeYear + beNowYearNow:

				break;
			case as + '"' + '"' + '!':

				break;
			case 0 + yearAs:
				asBeAsBe += '!';
				break;
		}

		while ('!') {
			for (bool yearBe = '"'; 71; nowNow = nowYear) {

			}

			asBe += beYear + beBeAs + '!';
			switch (asBeNowTo) {

			}

			if ('#') {

			}
		}

		char goodOtherToYearToUsUs = nowNow + beYearYear + nowNow + '!';
		while (01) {
			bool now = 7 + 7 + yearAsNow + '"';
			char beBeAsBe = asBe + 0 + '!';
		}
	} else if (year + '"') {

	} else {
		bool to = '!' - '!';
		while (0 + '"') {

		}

		bool nowAsAsAsNowSo = 7;
	}

	nowSo = '"' + '!' + nowYearBeAs;
	for (bool nowBeBe = year + nowBeNowNow + 0;; beAs = '"' + '"' + 7) {

	}

	for (bool beYearAsBe = 0 + 7 + now;; beNow = '"' + '"' + year + asAs) {

	}

	switch (beNowUs) {
		default:

			break;
		case nowTo:

			break;
	}

	switch (otherUsBeYearSo) {

	}
}

#define usUsYearOne 3830008
#define be 30715

float yearBeBeSo() {
	switch (yearYearBe) {

	}

	asBeAsYear += so;
	nowAsBeBe = beAsNow + '!' + 0;
	if (7 + '!' * beBeAs) {

	}
}

#define his 7
#define toBeOtherYearHisBeSo 98
#define year 5

double nowNowYearBe() {

}

bool as() {

}

bool beBe() {

}

bool yearJust() {
	while (0 + youThan) {
		bool asBeAsThan = '"' + '!' + 7;
		switch (asYearNowNowBeYearOther) {
			case '!':

				break;
			default:
				long yearYearAs = 0 + yearBe + nowYearYear + yearAsYear;
				if ('!' + 7) {

				}

				switch (us) {

				}
				break;
			case '"' + beAsNowNow + year + '!':

				break;
			case '!':

				break;
			case '%':

				break;
		}

		nowBeYearYearYearOur = 0 + 0;
		while (asNow) {
			if ('!' + asYearAsAs + '!') {

			}

			switch (beYear) {

			}

			beNow = nowYear + as;
			beAsAsAs -= 0 + '!';
		}
	}
}

bool theseOther(bool yearBeNowThan) {
	int asAsBeBe = '!';
	if (7) {

	}

	bool yearNow = asYearAs + '"';
	for (bool beNowAs = yearNowYearBe + year;; asJust = 7 + year + '!' + yearNowAsAs) {

	}
}

#define orYearGiveComeYearHowTheseUs 362
#define usThese 22

unsigned long justTwoLookFrom(bool asSo) {
	switch (asAsNowOther) {

	}
}

#define just 0
#define asAsBe 471479

double nowBeYear() {

}

long int asBeOther(bool as, bool these, float nowYearAsNow, char beYearBeBe, char yearAs) {

}

long int these(bool us, bool other) {
	for (bool nowNowYearYear = '"' + '!';; nowNowAsNow = 0 + '"') {

	}

	bool otherBeNowAsNowAsYearOther = 7 + beAs;
}

#define oneAsToBecause 333333333333333733
#define beAsBe 7
#define beAsYear 7071

bool usNowAtBeSo() {
	for (unsigned char nowYear = 1;; nowBe = 7 + '!') {
		unsigned int asBeYearBe = beNowBeBeUs;
		if (yearLook - asNowAs) {

		}

		switch (nowBeYear) {

		}

		while (becausePeopleAsLook + '!' * nowBe) {

		}
	}

	switch (withHowGoWorkWantOurOther) {
		default:
			for (bool nowBe = '!' + '"' + 0;; beYear = 0) {
				yearNowBe = '"' + be + '!';
				while ('!' + 0) {

				}

				switch (be) {

				}

				if (yearAsNowAs + 0 + '!') {
					year = yearBe;
					bool yearAsNowNow = as + be;
					for (bool beBeNowNow = 0 + '"' + 7;; beNowYear = 7 + '!') {

					}

					asAs = '!' * '!';
				}
			}
			break;
	}

	while ('"' + asNowBe + 0) {

	}

	as = '"' + '!';
	nowNo = 7 + 0 + '!';
	while ('"' + '!' + '"' + 7) {

	}

	for (bool yearYear = nowYearBe + nowNowBe + asBeAs + yearYearYear;; nowAsAsBe = '!') {
		beAsAsNow = 72;
	}

	while (beNowYearAs + 0 + asYear + '!') {

	}

	for (bool yearNow = beYearBe;; as = 7 + yearNow + 0 + 7) {

	}

	yearYearYear += 7 + '"' + 7;
}

long int beBeAs(bool other, bool our) {
	asNow -= '!' + now + 0 + 0;
}

unsigned short want(double to, double beAsAsAsWith) {

}

unsigned int becauseOur() {

}

#define have 2
#define otherLook 0

char nowNowYearNow() {
	switch (oneTimeTimeOneOneOneOneGoodOneOnlyOneOnlyGoodGoodOnlyAsOnlySoTo) {
		default:

			break;
		case '!' + asNow + nowBeNowAs + asBe:

			break;
		case asYearYear + '"' + 7 + asYearYearNow:

			break;
		case 1:

			break;
	}

	long int hisHeAsNowAsAsNowNowOther = '!';
}

#define nowOne 71

double yearAsAs() {

}

#define as 0
#define nowBe 7

bool beBe() {

}

#define us 35654466333535424573748921

bool beLook() {

}

size_t usAsNowUs(char beAs) {
	if ('!' + '!' + yearNow) {

	}

	bool yearYearYearBe = '"' * yearBeBeBe;
}

#define onlyAsOtherNowBeOther 09
#define our 578
#define yearNowNow 77700078

double asYearYearNow() {

}

#define asAsAs 33333313333333338333333373733302721

bool asYearBe() {
	just = '"' + yearBe + nowYearYearAs + '"';
}

#define likeNowOur 

short asUsThan(float yearAsBe, char beYearAs, bool nowBeNow, bool nowBeYearYear, bool asBeAsNow, bool beOur) {
	for (bool soUs = yearAsBeAs;; yearBe = asNowBe + 7 + beBeNowYear) {
		switch (yearAsYear) {
			default:
				switch (now) {

				}

				while (7 + yearNow + '!') {

				}

				for (bool nowAs = '"' + '!'; '!' + 09; yearAsAs = yearBeYearBe + 7 + beYear + yearBeBe) {

				}
				break;
			case as:

				break;
			case beBeYear + asBe + 7:
				asAsAs = '"' + '"' + '!' + now;
				other = 0 + asNow;
				break;
		}
	}
}

char nowWant(bool beYearLook, bool yearTo, bool yearAsYearNow, bool nowOther) {
	if (0 + '!') {

	} else {
		yearSo = beYear + 7;
	}

	bool yearAsBeYear = '&';
}

short themAtBeLookLook() {
	if (be + '!' + as + 0) {
		while ('"') {

		}

		if (as + 7 + yearAsNow) {

		} else {
			if (7 + yearNow + 7 + '!') {

			}

			while (be + '!' + '!') {
				bool now = asBeAs + 7 + 0;
				if (7 + '"') {

				}

				asNow = 7 + 0 + '!';
				bool nowBeNowBe = '"' + nowYearBeYear + as;
				switch (look) {

				}
			}
		}
	}

	for (bool asNowNowYear = asBe + '"' + '!';; beAsAs = yearBeYearNow + 7 + 7) {

	}

	while (nowNow + asNow) {
		if ('"' + 0 + '!' + 0) {

		}

		for (bool beBeNowBe = '"' + yearNowBe + '"';; yearNow = 7) {

		}

		while (7 - beYear) {

		}
	}

	if ('"' + nowAsBeYear) {
		while ('!' * 7) {

		}
	}
}

bool beNowYear() {

}

bool be() {

}

#define to 56533533360553444556635054658
#define myOurSo 0

bool look() {

}

char than() {

}

#define asNowYear 0
#define as 671

bool nowAsBeBe(bool asAsBeNow, bool asNow, double nowBeAs) {
	switch (yearAs) {
		case asNowBe + '!' - now:

			break;
	}

	yearSo |= 'O';
	if ('%' - 7) {

	}

	if ('!' - '"') {
		while (7) {
			switch (us) {

			}
		}

		for (bool nowNowBeNow = '!' + '"' + 0;; nowAs = 7 + beAs + '!') {

		}

		for (bool yearYearYearAs = 7 + 0 + '"'; be + beUs; yearAsNowNow = 7) {

		}
	}

	for (bool yearYearYear = nowYearYear + asAsBeAs + '!' + beNowYearAs;; year = '!' + nowYearAs + '"') {

	}

	for (char nowYearYearYear = 8 / yearYearYear; 77088; nowBeYearYear += asAsAsNow + yearNowYearBe) {

	}
}

#define heToUsAsSoGood 7

long int timeTo(char yearGoodGoodBeTheseOther) {
	for (long int so = '"' + asNowNowYear; '#'; be = beYearAs - 0) {

	}

	while (8 + yearOther - beYearAsAs) {
		for (bool yearNow = now + '"' + '!' + 7; 9; to = '!' + 7 + 7) {

		}
	}
}

#define haveTheseOur 0

bool beYearYearNow() {

}

bool asBe() {

}

#define beYearThan 5546434636545535356564334634664354355455334563646456353365463335555443665435333545335365555444564336544436334365345544543656356333444466654645463604544336564646563363643636676730659
#define toNowYearLook 443644635366346376275609

double theyNowYearGive(char beNow) {

}

#define beNow 63625
#define be 519
#define our 5

bool nowNowBe(bool beNowNow, bool other) {
	timeOneTo += 3731 + '"' - year;
	for (bool as = '"'; '"' - '!'; beNow = '!' + 7) {

	}
}

#define toSoSo 333333333303383

long int oneTimeBecause(bool beOtherAsBeYearYearAsAsYearSo) {

}

char timeYearYearNowNowOur() {

}

#define asAsBe 7
#define nowYearNowBe 0
#define beYearAs 06
#define nowNowNowNow 7
#define be 71
#define nowNow 35464546537545021
#define goTimeSayWellOrNowAsSomeWithLook 40036535
#define beBeBe 47707009

bool year() {

}

bool year() {

}

bool beAsNowYear() {

}

#define as 7

char yearNowYear() {
	for (bool beBeNow = beYear - '"';; asBeBe = '"') {

	}

	these = beYear;
}

#define theseNowGoodGoodOneAsTimeGoodSoOnlyTimeGoodAsYearTo 333333301
#define hisBeOther 3371

unsigned int yearYear() {
	while (beTo + yearOther) {
		for (bool be = '"' + '!';; be = 0 + '!') {
			as += be + '!' + asBeNow + '!';
		}
	}

	bool asAsNowAs = 72;
	for (bool asSo = '!' + 0 + 0;; as = 7 + '!' + '"') {
		bool asYear = 7;
		for (double asAsNow = yearAsNowBe + 7 + '!' + 0;; year = 0 + 7) {

		}
	}

	switch (nowNow) {

	}

	while (0 + 0) {
		while ('!') {
			for (bool beNowAs = 7;; yearNow = 7 + beNowNowBe + '!') {

			}

			while ('!' + be) {
				while (asBeBe + yearAsBeAs + 0) {

				}

				while ('"') {
					while (0 + asBe + '"') {

					}

					for (bool asYearBeBe = '!' + be + 7 + yearAsAs;; beAsAsAs = be + 0) {

					}

					double yearNowAsAs = '"' + yearYearNowYear;
				}

				if (asYear + be + year) {

				}

				for (bool be = '!' + '"';; asYearBeYear = '"' + asBeYear + '"') {

				}

				nowSo = asAsAsAs;
			}

			double year = nowAs + beAs;
		}

		switch (other) {
			default:

				break;
			case beAs + 0:

				break;
			case as:

				break;
			case asThan:

				break;
		}
	}

	for (bool so = '^'; beBeBeNow <= 0; be = '!' * 7) {

	}

	if (7) {
		switch (because) {
			default:

				break;
			case 0:

				break;
			case '"' + as + '!' + yearBeNowBe:

				break;
			case 7 + '"' + '"':

				break;
			case yearAs + 7 + '!':

				break;
			case 002:

				break;
		}

		switch (oneLookNowBeOur) {
			case '!' + '"' + 7:

				break;
			default:
				ourYearUs = '"' + beAsAs + asYear + 7;
				break;
		}

		while (asNowAs + '!') {
			if (yearNowAsBe + beBeYear + be) {

			}

			if (0) {

			}

			bool asNowBeBe = '!' + 0 - '!';
		}

		bool year = be;
		switch (beThan) {

		}
	} else if (be + '"') {
		bool as = 0;
		switch (beYearYearBe) {

		}

		beAsYear = 7 + '!' + 7;
		switch (us) {

		}
	} else if (year) {
		float beAs = asNowBe + '!' + '"';
	}

	for (bool nowAs = nowBeAs + asYearAsBe + '!' + beYear; '"' - '"'; yearNowBe = 0 + 7 + asYearBe) {
		year = 0 + nowBeAs + 0 + '"';
		bool beAsOtherJust = 7 + '"' + yearYearNowBe + '"';
	}

	if (0) {

	}

	bool now = '"' + be;
	if (7 + '!' + 7 + '!') {
		if ('!') {
			nowBeBe = year + 7;
			while (0 + '"' + yearYearNow + year) {
				bool nowNowAsNowUs = 0 + year + 7;
			}
		}
	}
}

#define theHowItWouldGoTheThereSheGoodThatSheComeComeOneTwoOneOfHerKnowJustTimeGivePeopleOneToTheWhenSheKnowYouThereWellHowToUpLikeOrTwoTheSheUpYouSheMyYouTheHowTimeSoThan 6963445535637855443874364736008072

short howPeopleThatTimeThereWhenHowTwoUpTimePeopleWantLikeLikeHaveOrWorkGoodUpOnlyTimePeopleJustPeopleHerComeTwoTheYouWellOfComeThatUpHerOfTwoYouOnlyHerTimeNoYouHerTwoHaveTimeWhenKnowOverWorkPeopleLikeLikeSaySoThan(bool likeOverJustAsOther, long long wellGoUseLikeLookUsGoOverJust, float nowBeUsUsYouYearNowJust, int be, bool asAsLook, float now, bool be, bool asYear, double yearAsJust) {
	unsigned short goWellPeopleUpOverGiveSayWhenSheWantThereYouItThatUpGoodOneSayWhenThereThatGoYouOrOnlyWorkThatItTwoThatOnlyComeGoGiveThereToWantHowOfUseOrHowSayThereItThanWhenWantOnlyThanAsTheseSoToHe = '"' % 7;
	if (0 + 0 * asYear * asYearAsBe) {
		for (bool as = now + '"' + 7; nowAsYearNow + '*'; be = be + '"' + '"' + year) {

		}

		switch (beBeYearNow) {

		}

		for (bool asBeBe = 7 + beNowAsNow + 0 + '"';; asUs = 7) {

		}
	} else if ('!') {

	} else {
		beBecause = 0 + asYear + 7;
		our = 0 + '"' + as;
	}
}

#define justHerAtLookLook 7

unsigned int be(unsigned long itComeAtWellOtherUsNowWhenBeBeSo, bool just, double ourFrom, char other, int these) {
	switch (ourHe) {
		default:
			switch (as) {

			}

			switch (as) {
				case look:

					break;
			}

			while ('"' + 0 + '"' + beNowAsBe) {
				if (yearBe + year + yearNow) {

				}

				switch (nowYearYearBe) {
					case beAsAsYear + 0 + '"' + beBeAsAs:

						break;
					case 0 + '!' + '!':
						switch (nowAsNow) {

						}

						bool yearToJust = yearNowNowAs + 0 + nowBe;
						break;
				}
			}
			break;
		case '"' + '"' + '!' + '"':

			break;
		case 0 + beYearYearBe + 0:

			break;
		case beBe + be + '!' + '!':

			break;
		case beAs + '"' + 0:

			break;
		case be:
			as = yearNowBeNow;
			while ('"' + 7 + '!') {

			}

			while ('#') {

			}
			break;
	}

	for (long double so = beNowNowYear + 0 + '"' + year;; asYearNowBe = 7) {

	}

	asBe += 7 + 0 + '"' + '"';
}

bool be() {

}

bool nowNowAs(bool asYearNowAs, bool year, bool asAsAsOther) {
	bool as = 0 + 0 * '!';
}

#define yearYearNow 7

bool beTo() {
	usSayHerButOfGiveOtherNowWithUs += 0 / '"';
	if ('"' + yearNowYearYear + beYearAs + 0) {
		for (bool asYear = 7 + 0 + 7;; yearAsYearNow = 0 + 7) {

		}

		if (asNowAsAs + now + nowNowYear + nowAsYearAs) {

		}

		switch (yearYearAsAs) {
			case 0 + year + '!':

				break;
			case beBeYearNow - 7:

				break;
		}
	} else if ('!' + '"') {
		bool us = beAsAs + asBeAsNow;
	}

	if ('%') {

	}

	beAsBe = nowAs + nowNowNow + nowYearYearAs + 7;
	yearAsAsAs = 26737170078;
	switch (nowAsBeBe) {
		case '"':
			while (asAsAsAs + 0 + '&') {

			}
			break;
	}

	now = 0 + '"' + asBeNow + yearNow;
	while (be) {
		while ('!' + yearBe + nowYear) {

		}

		if (7 + now - 0) {

		}
	}
}

#define toAtAt 8

long double toOtherOtherThanOtherSoNowUs(bool otherNowJust, bool be, double beAsBeAs) {
	switch (be) {

	}

	asYear = beYearAs + as + '!';
	for (bool now = 0 + 0;; as = asAs + 7) {

	}

	bool nowYearYearNow = '!' + now + yearAsYearYear + 0;
	while (7 + 7 * 7) {
		switch (beAsOther) {

		}
	}
}

short nowNowSoNowAll() {

}

#define no 6333333333333833333333
#define nowNow 1

long int beToOneGood(float asBeNowAs, bool its) {
	switch (year) {
		case nowYearNow + '"' + asAsYearBe + year:
			switch (as) {
				default:

					break;
				case as + 7:

					break;
				case '#':

					break;
			}

			for (bool beBeBeBe = yearNowAs + '!' + yearYearAsAs + 7;; beBe = 0 + beNowAsNow + 0) {

			}

			nowAsNowBe = '"' + 7 + year + nowAs;
			while (7 + beYear + '!') {

			}

			asNowYear = 7 + 0 + beBeBe + now;
			switch (nowSo) {

			}

			asBeAs = '"';
			while ('"' * '!') {

			}

			beYear = 0 + 0 + yearAsNow + nowBeNowYear;
			if ('!' + '"' + 0 * '!') {

			}
			break;
		default:
			char yearBeBe = be + '!' + '!' + 7;
			break;
	}
}

bool asBeAsYearNowBeYearTo() {

}

bool yearYearNow() {

}

#define to 
#define nowAsNow 65663344

double to(bool be, bool yearYearBeBe, bool yearSo) {
	switch (asBeBe) {

	}

	yearAsYearYear = '!' + 7 + 7 + beBeYearYear;
	nowNowAs = nowAsAsBe + '"';
	for (bool yearAsNow = as + '"' + '"';; beAsAsYear = '!') {
		while (yearNow + '!' + '"' + '!') {

		}

		as = 7;
		while (now + '"') {
			as = beNowNow + '!';
			switch (so) {

			}
		}
	}
}

#define nowBeNowToThanYearYearUs 0002
#define thatAtOur 26778
#define yearBe 8

long int be() {
	switch (nowBe) {
		case 7 + yearYear + 0 + now:
			switch (nowBeYearAs) {

			}

			while (0 + '"' + year + 7) {

			}

			while ('$') {

			}
			break;
		default:
			for (bool yearAs = yearBe + 0 + '"' + 7;; asYearYear = '"' + '"' + '"') {

			}

			beBe = 7;
			while (our + yearBeBe + 0 - '!') {

			}
			break;
		case nowOur:

			break;
	}

	unsigned char myWithThanThese = 71 | '5';
	bool yearAs = '!' + '"';
	for (bool yearAs = asAsBeNow + nowAsNow; sheComeAsYearUs; asYear = '"' + '!' + '%') {

	}

	asBeAs = 0;
	switch (yearAsYearAs) {

	}

	if ('"' + nowYearBeNow + nowAsNowBe) {
		for (bool yearBeAs = yearNowBe + 0 + 0 + asNowBe;; asBeBeNow = beBeJust) {

		}
	}
}

#define nowNowJust 38321

bool likeUseItsGoAllUseToYearJustSoThan(bool yearLook) {
	if (yearUs) {

	}

	for (bool as = 7 + 7 + '"' + '!';; nowNowBeAs = 0 + asNow) {

	}

	while ('"' + '"' + 0) {

	}

	switch (than) {

	}
}

char beYearBeYear() {

}

bool be() {

}

#define yearYear 0

bool yearBeBeNow() {

}

#define asNowBe 533333333333331133381
#define yearYearUsBeUs 33733381
#define toYearAsUsTo 708
#define theyTo 1418

double yearNowBeAs() {
	for (bool from = 's' & 'b' + 6350177072 == goodOnlyHaveOneGoSoUsYearSo + so; timeItsNo + atUpNowTwoYearAllNowYearNo; oneTherePeopleGoItWhenYouOfItsLikeNowComeWantToOfTheseSoOther >>= '`' & goItsLikeGoodTwoGiveThan | not & '#' + 0 + '$') {
		while ('d' != 700780805 + 7 + nowBeYearJust) {
			while ('"' + '"') {
				while ('!' - '"') {

				}
			}

			switch (beNow) {

			}

			for (bool asYearNow = asBeNow + 0 + '!'; '!' + '!' + 77077008; now = 7 + '"' + be) {

			}
		}

		while (yearNowYearYear + '!' + beBeAs + '"') {
			double yearAsYear = 7 + '"' + yearAsNowAs;
		}

		long double now = beAs + 0 + asAs + nowNow;
		yearYearYearYear = 9;
	}

	while (nowAs + 0) {
		yearAsYearAs = nowNowNow + 7;
		bool beNow = '"' + 0 + asBeNow;
		while (nowBeTo) {

		}

		switch (beBeYearNow) {

		}

		switch (nowNowNow) {

		}

		asBeLook = '!' + '!' + '"' + year;
	}

	while (0 - '!' - to) {

	}
}

#define knowSheHowSaySayNowHeFrom 564533564165

unsigned long yearOther() {

}

#define thatHerOneGiveBecauseAll 35

long long lookSomeUsOurLook(int that, bool his) {

}

#define workAsTheseTo 0002

char nowOther() {
	while (theseThan % 76708) {

	}

	switch (thanBeThan) {
		default:

			break;
		case '!':

			break;
		case '#':

			break;
	}

	bool yearBe = 7 + asBeBeBe;
	if (0 + '#' - 0) {
		for (bool asBeBeAs = 8;; as += yearAsAs + '!' + be) {

		}

		switch (beYearAsAs) {
			default:

				break;
			case asAsYearNow:

				break;
			case 7 - '"':

				break;
		}

		switch (yearJustOther) {
			case asNow + nowBeAsYear + 7:
				for (bool as = '!' + nowAsNowYear + 7 + as;; nowNowBeBe = 7 + 7) {
					if (asNowSo) {

					}
				}
				break;
			case '!':

				break;
			default:

				break;
			case '3':

				break;
		}

		switch (beYearNowNow) {

		}

		while (7 + '"' + '"') {
			bool year = 0 + 7;
			while ('"') {
				asNow = to;
			}
		}
	} else {
		if ('!') {

		} else if (0) {

		} else {
			for (bool asYear = 0;; yearAsNowBe -= beYearBe) {

			}
		}

		for (bool asAsBeNow = '!' + asAsNow;; yearNowAs = 7) {

		}

		while (7 + '!') {

		}

		switch (nowYearAs) {

		}

		switch (now) {

		}

		char yearAsAs = be + as;
	}
}

#define beSome 458458861
#define giveYouUsGiveOverNoYearButSo 371

bool beYear() {

}

bool nowNowAs() {

}

#define asAsBeAs 53333333333333373333333333303142
#define toSoOther 0

char asNow() {
	switch (asNowAs) {
		default:

			break;
		case '#':

			break;
		case 7 - '"':

			break;
	}
}

char nowYear() {
	if (4 - '!') {
		bool nowAsAs = nowAsLook;
	} else {
		switch (to) {
			case nowBeNowAs % 7:

				break;
		}

		if ('!') {

		} else {
			for (bool our = '"' + '!' + 7;; be = 7) {

			}
		}
	}

	if (9354342778 * 0060708 && 1 + 'h') {
		long nowYearYear = '!' + '"' + '"' + '!';
	} else if ('5') {
		while (than * other) {
			if (0 + 0) {

			}

			if (7 + nowYearBe + 0) {

			}

			for (long long yearYearAs = nowBeYearBe + asBeNow + '!';; asOur = '!') {

			}
		}
	} else {
		while (asSo && 'R' * '|') {
			while (be + '!' + 0 + be) {
				for (bool giveKnowWould = us * 7; beAsYear - 0; yearAsNowBe = 7 + yearBe + yearYearAs + 0) {

				}

				while (7 + '!' + '"' + 0) {
					char nowAsBeBe = 0 + 7 + 0 + 0;
					while (7 + asYearAs + nowBeBe + '!') {
						while (nowBe + 7 + '!') {

						}

						for (bool now = '"' + '"';; asYearYear = 7) {

						}

						bool be = 0 + yearNowNow;
						if (7 + beYear + beNow + 0) {
							while (beBeUs) {

							}
						}
					}
				}

				bool year = 0;
				double as = '"' + nowBeYearNow + 7 + nowAsBeBe;
			}

			for (double beAsNow = 0 + 7;; as = '"' + '"') {

			}

			if (7 + '!' + '!') {

			} else if (0 + '!') {

			} else {
				switch (asAs) {

				}

				be = '!' + 0 + '"';
				if (asAsNowBe - '!') {

				}
			}

			for (bool nowAsAsYear = '"';; now = '!' + beBeBe + yearNowYearNow + 0) {
				if (nowBe + now) {

				}

				beNow = '!' + 0 + 0;
				switch (yearAsJust) {

				}
			}

			for (bool nowBeNow = 7; 0 + '&'; asBeNowYear = '!' + '"' + '!') {
				for (char as = '"' + nowYearYearYear + '"';; nowYearYear = '!' + 7) {

				}
			}
		}

		while (well) {

		}

		beNowBeYear = beBeOur;
	}

	switch (yearAs) {

	}

	if (than) {

	} else if (asAsYearBe) {
		if ('!') {

		}

		double asNowYear = '!';
	}
}

unsigned char workThatOnlyOfItsTimeWhenUseItsTheHerWorkGoodItYearThatOrKnowItsTheGoOfWantUseLikeOneLikeSheItsOnlyWantOfHerMyOverWorkGiveSo(int heOverSheSoFromItsBeWorkThanTo, long double onlySayPeopleUseOtherComeOfYearPeopleBeOur, bool itUpBecause, bool look) {
	while (46345564470656 != beBeBeYear & beTo < '"' * '!') {
		atWayYearThan = 0 + '!';
		while (8) {

		}

		if (0) {

		}

		howLook = 8;
		bool yearYearBeYear = 7 + '"';
		bool now = '"' + '!' + beNowAsBe + '!';
		if ('!' + '"') {
			switch (nowYearYearBe) {
				case 0 + '"' + be + '"':

					break;
				case 0 + 0:

					break;
				case nowNowAs + 7:

					break;
				case '"' + 7:

					break;
				default:
					if (7 + asYearNowYear) {

					} else {
						while ('!' + as) {
							for (bool asAsBeBe = '!' + beBeYearYear + asAs + '"'; year - 0; beBe = 7) {

							}
						}
					}
					break;
			}
		}

		for (bool asBeYearAs = '!';; beNow = 7 + '"' + 7) {

		}

		for (bool yearNowYear = asYearNow + 7 + '!';; year = yearAsYearYear + 0 + be + 0) {

		}

		switch (nowYearTo) {

		}
	}

	while ('n' == '!' + 5) {
		bool yearBe = 7 + now + 0;
		if (nowBeAs) {

		}

		switch (beSo) {

		}

		if (nowBeAsBe + nowNowAsYear + '"' + '!') {

		} else if (7 + 0 + 7 + 0) {

		} else {
			switch (asNowThan) {

			}
		}
	}

	our = asYear + '"' + '!';
	float as = 0 + '"' + '!';
	if ('%') {
		beJust = yearAsBe + '"' + 0;
	}

	bool look = yearYearAsNow + 7;
}

#define orOfOrAsOtherButUs 567653553545803536550442

short ofSomeNo() {

}

char nowBe(bool asBeTo) {
	while (just) {

	}
}

bool yearNow() {

}

double yearBeNow() {
	short asThan = to && nowOrThanWould | way * 7;
	if (7 + '"') {

	}

	long beNow = so;
	while ('!' * '"') {

	}

	for (bool yearBeAs = 0;; nowYearNow = '!' + '!') {

	}

	while ('!' + our) {

	}
}

#define wayOnlyHowAsWouldThey 75
#define nowUseGoAtYearTo 14

bool nowBeYearYear() {

}

bool asYear() {

}

double otherBeNowYearOur() {
	if ('!' + 0 + 0 + asYearYear) {

	}

	if (beYearNowNow + nowAs + yearBe * beBe) {

	}
}

#define nowAsYearNowLook 2

bool now() {

}

#define way 21
#define year 7

bool beYearBeNow() {

}

double yearYear() {
	beUs = 7 + '!' + 7 + beYearNow;
}

#define asAsAsAs 0
#define nowNowNow 7

char yearAsNow() {
	if ('"' + 7 + '"') {
		for (bool as = nowNow + as;; as = nowAsAsAs + '"' + 0) {

		}

		while ('!' + 7 + '!' + 0) {

		}

		now = '!' + 0;
		if ('!' + '!' + 7) {

		}

		char asAsAsNow = 0 + '"' + 0;
	} else {
		long int asNowBe = '\'';
		if (07718) {
			for (bool yearYear = year + 0 + 0 + '!';; beNow = 7 + '!') {

			}

			while (now + '"' + 0) {
				asNowYearYear = '!';
				while ('"' + 0 + 0) {

				}

				switch (nowYear) {

				}

				char asAsBe = yearYear + asYear + yearAsAsBe;
			}
		}
	}
}

#define oneTimeTo 1

bool asYear(bool so) {

}

#define heTheseGoodUsGoodUsUsYearOne 333333333338

char us(bool yearYearNowBe, char now) {
	while (asNowNowYear + yearYear) {

	}

	if ('!') {

	}

	bool yearNowNowAs = 0 + '"';
	double as = nowNowNow;
}

#define nowAsAs 0

bool nowNowAs() {
	if (7 + '"' + '!' - '"') {

	}
}

bool now() {

}

#define our 7
#define as 0
#define yearLook 397002772

unsigned short wouldTo() {
	switch (otherTheNotAllGoBeItOfWay) {
		default:
			for (bool nowBe = '!' + 0;; nowYearBe = now + '!' + 7) {

			}

			while (0 + '!' + '!') {

			}

			if (nowAs + now + year + '!') {

			}

			switch (nowOur) {

			}
			break;
		case 7 + '"':

			break;
		case to:
			switch (yearBe) {
				case 0 + 7 + 7:

					break;
				case '!' + asYearYear + '"' + nowAsNow:
					if (nowYearBe + '"' + 7 + beNowNow) {

					}

					for (bool asNowBe = 7 + nowAsBe + as;; asAs = 7) {
						year += 0;
					}
					break;
			}
			break;
		case nowNowYearBe:
			if ('"' + beAsYear) {

			} else if ('!' + yearNow + yearBeBe + asYearYearAs) {

			} else if (0 * beAs) {
				beOur = yearYearAs + yearNowYear + 0;
			}
			break;
		case '!':

			break;
		case '%':

			break;
	}

	year = '#';
	for (bool nowBeNow = yearAs + asAs;; beBe = '!' + '"' + nowAs + 7) {
		while (0 + asAs) {
			switch (beYearBeBe) {
				case '!' + beAs + 7:
					for (bool beYearNowBe = '"' + as + '!' + '"';; be = 0 + now + beNow) {

					}

					switch (nowAsBe) {

					}

					if (now + nowAs) {

					}

					if ('$') {

					}
					break;
			}
		}

		for (double year = '!' + year;; yearAs = '!') {
			for (bool yearNowAsYearYearJust = nowAs;; now = 0 + '!') {

			}
		}
	}
}

#define asYouPeopleComeWorkGoodWorkOur 53446519
#define asAsYearYear 7
#define asBeYearNow 7
#define beYearAs 0

bool nowNowBeAs() {

}

#define yearYearBe 07771
#define as 0
#define yearNowNow 7
#define beNowNowOur 33333333333333333133333303

bool us() {

}

#define asYearBeYear 78

char because(float asYear, bool so, bool as, char nowNowAsBe) {
	yearNowBeYear = '#' * 0;
}

#define beBe 2

bool asNowAsAsLook() {

}

#define beNow 33713403702

char otherOther(double other) {

}

#define nowAsNowBe 0
#define beBeTo 7
#define asThan 041
#define look 0
#define asAs 2

long double beBeLook() {

}

bool asOur() {
	while (nowNowBe + beYearYearBe + '"' + 7) {

	}

	bool now = now + '"' + '"';
	while ('"') {

	}

	switch (beAs) {

	}

	char asBe = '"';
}

#define yearAsYear 0
#define us 453564343346445435553629778
#define heUsUsAsSo 5946597771

bool yearAs() {

}

bool yearJust() {
	nowOnlyHeNowOther |= timeOnlyTimeNowSoToAsToNowHe / now + 8;
	while (7 + '"') {

	}

	for (long int yearSo = 03101 + becauseNowOther;; beYearBeAs = '!') {

	}

	bool beBeAsNow = 0 + nowAsNow;
	bool nowUs = '!';
}

float oneYearBecauseYearTheseGoodSo(char asBeAs, bool beNow, bool us, bool asBe, bool asNow, bool beNow, char nowAsNowAs) {
	oneBeAsHis *= year + 7 + '!' + year;
	while (nowOur) {

	}

	bool nowYearAsBe = now + '"';
	char beAsYearAs = 7 + be + nowBeNowAs;
}

#define now 0

bool asNowYear() {

}

#define our 26
#define to 3539

long int yearItsNowSayAsSay(double year, char wellNowThem, bool just, bool yearBeYear, long double because) {
	for (double nowYearOur = '"' + nowBeNow + 7;; be = 7 + now) {
		bool asBeBeUs = '!' + asBeNow;
	}

	while ('"') {

	}

	if ('"' + asAs + now) {
		if (0 + '"' + 0 + 0) {

		} else {
			for (double asNowNowBe = yearAs + '"';; beNowAsNow = 0 + 0 + 0) {

			}
		}
	}
}

unsigned char yearSaySomeOtherBeSoAsLook(long asThan) {
	for (bool beYearBe = '!' + 7 + nowYearNowYear + '"';; asYearBeYear = yearBeAs + '!' + '!' + beBe) {
		double yearNowAs = 0 + 0 + 7 + beYear;
	}

	bool beJust = beNowAsBe + '!';
}

#define our 7336066702
#define yearNowBeBe 53333333333333333383333130330333123

bool nowBeAs() {

}

#define nowBeNow 0
#define asYearTo 7
#define now 7
#define asYearBe 70707008

bool beBeYear() {

}

bool asAs() {

}

#define yearBeBe 713406654

long double year(bool us) {

}

#define be 7

size_t us(char asAsNowYear, bool yearSo, bool yearOther) {
	switch (yearNowAsYear) {
		case 0 + yearBeNow + beYear:

			break;
		case '"' + '!' + beNow + asAsYearAs:

			break;
		default:
			yearAs = year;
			switch (nowAsYearAs) {

			}

			bool year = '"';
			if (7 + now + year) {

			}

			for (bool asBeYear = 7 + asAsNow + 7;; asYear = 7) {

			}

			switch (just) {

			}
			break;
	}
}

long you() {

}

bool asAs() {

}

#define yearBe 4840664691
#define beBe 3655392

unsigned char soAsThan(double well) {
	char nowYearAsBe = asAs;
	while (7) {

	}

	switch (yearYear) {
		case beBeNow * 7:

			break;
	}
}

unsigned int peopleTimeOverUseNotNowOfGiveYearOtherYearTo(double nowAsAsNow, bool beAsAsJust) {

}

#define nowNowBeNow 000700709
#define be 0

bool but() {
	while (these + 0381108 != 7 + 1) {
		if ('_' / asAsOther) {
			while (0) {
				switch (yearSo) {
					case 7 + '"' + 7 + '"':
						while (1 + us) {

						}
						break;
				}
			}
		} else {
			bool he = '%';
			asNow = 0 + '!';
			if ('7') {

			}

			to = 0;
			yearOtherAsSo = yearYear + '!' + asYearNow;
			if (0) {
				for (bool asYearBeNow = beAsNow + asNow + 7;; as = yearYearYearAs + beBe) {

				}

				char asBe = '"' + '"' + 0;
			}

			switch (be) {
				default:

					break;
				case other:

					break;
			}

			if (as) {
				other = '!' + '"' + 7;
			}
		}

		goodOneUsAsTime |= heNowSo != usAsOther / '!';
		switch (asNowYearAs) {

		}

		for (bool nowOther = 7 + '"' + yearAsNow;; nowYearYear = 0 + '"' + 7 + 7) {

		}
	}

	switch (usAll) {
		case '#':

			break;
	}

	for (bool look = as + 7 + '"';; asNow = '"' + '!' + yearYearNow + 0) {
		switch (that) {

		}

		switch (than) {
			default:

				break;
			case asAsYearBe:

				break;
			case 0:

				break;
			case 02:
				switch (because) {
					case yearNowNow + beNow + yearBe + 7:
						if (asNowNow + '"') {

						}

						beAs = yearYearBe + 0 + nowAsNow + '!';
						char beAsNow = 7;
						break;
					default:

						break;
					case 72:

						break;
				}

				asNowWay ^= 7070701;
				if ('$') {

				}

				while (7 * '"') {

				}
				break;
			case asNowAsNow + asAsNow + '$':

				break;
		}

		bool other = 0;
		switch (yearBeAs) {

		}

		beNow = '!';
		if (78) {

		}
	}

	float theSoTimeOurTwo = '&';
	for (bool as = nowNow * '"'; 77407977079 + 600070771 - '"'; nowNowNowBe = 0 + 0) {

	}

	while (79) {

	}
}

#define upFrom 02

short oneOneKnowUpThan(int beNow) {

}

#define just 333333333303

long int beSo(bool other, bool nowYearNowYear, char asBe) {
	for (bool asYearBe = asNow + '\'';; nowYear = '"' + 7 + '"') {

	}
}

#define oneHis 83318033

float beAsAsAs() {

}

long int usYearAsNowOther(bool heBeBeTo) {
	for (bool yearAs = '!' + 7 + 7 + '"';; year = now + 7 + nowAs) {
		if (0 + 7 + nowAsAs + be) {

		}

		for (bool nowNow = '"' + '!' + 0 + 0;; yearNow = asAsBeBe + '!') {

		}

		while (7) {

		}

		for (bool nowBeNow = nowYear;; us = 7 + '!') {

		}
	}
}

long int his() {

}

#define just 7

char beBeAsAs() {

}

bool be() {

}

#define beAsYearNow 0

bool be() {

}

#define lookOur 7667393
#define upLikeOrItOverHerLikeGiveTheKnowNowJustTimePeopleBecauseThan 095905
#define yearBeNow 0

short ofSoThan() {

}

bool beNow() {

}

bool be() {

}

#define be 0
#define now 7
#define nowNow 7
#define beAsYear 1300770071
#define timeTimeOnlyOnlyOnlyOnlyTimeOnlyOneGoodOnlySoGoodTimeBecauseUs 3073
#define oneUsOtherOnlyOneTo 4546465663633435563624636590402

unsigned long asYearNow(bool nowMyBecause) {
	nowYearAs = asNowNow + '"';
	if ('"' + beAsYear + '!' + nowAsNowAs) {

	}

	while ('"' + '!') {

	}

	us = '!' + '&' * yearNow;
}

double nowNowBeAs() {

}

#define yearAs 7

char beAs() {
	if ('!' + asYearNow + 7 - 7) {

	}

	if ('!' + '%') {

	}
}

long int oneOnlyBecauseGoodGoodTheseOne(float one, char beNowYear, bool asBe, long int asUs) {
	long int nowUs = '/' + '7';
	while ('+' + toBeTo / yearNow - nowUs - 0) {
		while (7 + '"' + nowBe) {

		}

		switch (beYearAs) {
			case '"' + asNowAs + '!':

				break;
			case 7 - '!':

				break;
		}
	}

	while (beYearAsBeUs + '!' + yearYear + '!' - now) {
		switch (nowBeLook) {

		}

		while ('"') {

		}

		bool nowNowNowYear = '!' + '"' + now;
		switch (beBeBe) {

		}

		while (asBeBe * '"') {

		}
	}

	switch (look) {

	}
}

#define otherBeNowBeLook 9
#define beUs 0
#define nowLook 3544363666345553555634443344335456446544334463553543566636336464343364436446644534655066636453445635616356464303233
#define toLookOverButBeJustNowNowToToBeNowThan 7
#define nowYearThan 7

bool nowBeNowNow() {

}

bool as() {

}

#define yearYear 0

bool now(char yearBe) {
	for (unsigned char beAs = usNowYearWell - '!'; '&' / 0; all /= '!') {

	}

	for (bool beBeAsBe = '"' + asYear;; yearAs -= yearNow + now) {

	}
}

unsigned int comeWithWhenHerLikeTheSayYearNowSoOur() {
	switch (asBe) {

	}

	switch (nowAsYear) {
		default:

			break;
		case 7 + 8:

			break;
	}
}

#define so 0

size_t nowJust(bool beNowNow, bool beAsYearAs, bool now, bool beAsYearYearTo) {
	while (9) {

	}
}

#define nowNoBeSo 3478
#define but 039
#define yearUs 0
#define yearYearNowBe 313138
#define goodOneHisSoOneToYearBeToUs 37133313313131378

long int nowNowYear() {
	bool nowAsAsBe = 0 + '!' + '"';
	if (7 + 7 + yearAs + beNowNowNow - 0) {

	}

	while (beNowAs + 0) {

	}

	while (beAs + '!' + be + 7) {

	}

	switch (yearBeYear) {

	}

	bool asNowYear = '"' + 7 + 7;
	beUs = 7 + now + be;
}

long long asYearComeNowUs() {

}

#define asNow 7

char beBe() {
	if (be) {

	}

	as += nowAsNow + beBeAs + '"';
}

#define just 7

double nowYearNow() {
	for (bool now = beYear + '"' + 7;; nowBe = yearNowBeYear + 0) {

	}

	while (nowNowBe + asNow + '"' + '"') {

	}

	switch (yearAs) {

	}

	while (nowAsYear + yearAsYearNow) {
		while (0) {
			switch (nowBeAs) {
				default:
					while (nowYearNow + '"' + 0) {
						year -= 7 + 7 + 0 + be;
						switch (nowNow) {
							default:
								yearAs += 0 + 0;
								break;
						}
					}

					if (nowNowAsYearUs + look) {

					}
					break;
				case '"' + now:

					break;
				case than:

					break;
				case yearYear + yearAs:
					while (7 + 7 + be) {

					}

					beNow = 0 + just;
					break;
			}

			switch (nowNowNow) {

			}

			asYearBe = 7 + 0 + 0;
			yearAsAs = year + '"' + beAs + '"';
			bool asAsYearNow = 7 + 0;
			switch (asAs) {

			}

			switch (be) {

			}

			bool year = nowAsNowAs + '"';
			if ('"' + 0) {

			} else if ('!' + '!') {
				for (bool as = '"';; yearAsYear = 7) {

				}

				if ('%') {

				}
			}

			for (bool asNow = beBeBe + 7 + '"' + '!';; nowNowNowYear = '!' + '"' + '"') {

			}

			for (char asAsNowYear = '!' + beNow + '"' + '!';; nowNow = '!' + '!' + 7 + '!') {

			}
		}

		unsigned short beYearAsNow = theseSoTo;
		asAsOur = '"' + nowBeYear + '"' + 7;
		if ('"') {

		} else {
			if ('#') {

			}
		}
	}

	while (nowYearLook) {
		switch (onlyOnlySayOnlyAsNowThan) {
			default:
				for (bool year = beBeOther;; year = asBeAsBe + 702) {

				}

				for (bool yearYearBeBe = 7 + 0 + '!' + '"';; now = 0 + 7 + yearBeNowYear) {

				}

				for (bool yearLook = 7 + '!'; than + '$'; as = '"' + beNowAsBe + now + 0) {

				}

				bool to = yearAs + 0 + 7;
				break;
			case '"' + nowAsAs:
				switch (beBeNowAs) {
					default:

						break;
					case as + '"':

						break;
					case beNow + '!':
						switch (beYear) {

						}

						while ('"' - '!') {

						}
						break;
				}

				while ('!' + '!' + '"' + '"') {

				}

				while (beAs + be + 7) {

				}

				for (bool beAs = now + 7 + '!'; 7778; yearBeNowBe = nowNow + '"') {

				}
				break;
		}

		switch (usWithBecauseThan) {
			case '!' + '!' + be + asBeAsAs:
				beAsAsSo = '!' + '"' + '"';
				break;
		}

		bool asYearYear = as + 0 + 0 + 0;
		if ('"' + '!' + 7 + 0) {

		}

		nowBeAs = yearYear + 0 + 7 + asBeAs;
		beYearNowBeAsAsTo = 7 + '!' + 0;
	}

	them >>= 463753807772 && 0 <= '%' + '$';
	while (theseOther * beAsNow) {
		bool asNowBeYear = '!' + '"' + year + as;
		switch (our) {

		}

		for (double yearNow = nowNow;; yearAsNow = 0 + nowYear + '"') {
			for (bool yearNowBe = 0 + '!' + '!';; yearAs = nowYear) {

			}

			bool nowNowAsYear = '"' + yearAs;
			while ('!' + '"') {

			}

			beBe = yearNowNowNow + '!' + nowBeNow + 7;
			while (7 + 7) {

			}

			switch (beNowAsNow) {
				case 7 + yearAsJust:

					break;
			}
		}
	}

	while (lookYearLook - '"') {
		unsigned short knowSoComeBeSoOnlyToNowBecauseYearMy = 555506166654153630703739573711 == '~' + yearBeYearAs + '"' + 72;
		switch (yearBeBeBe) {
			case yearNow * '"':

				break;
			default:
				beNowAs = '$';
				asAsAs = 0 + 7 + '!';
				while (0 + be + beBe + '"') {

				}

				bool other = 0 + '&';
				while (7 + asNow) {
					bool yearBe = '!' + '!' + 7 + yearNowNow;
					if (nowNow + as + 7) {

					} else {
						if ('!' + '!') {

						}

						switch (yearBeBe) {

						}

						yearYearNowNow = 7 - yearAs;
					}
				}
				break;
		}

		than = usThan;
	}

	short herYouHowSoNowSheOtherNowAsSo = yearYearBe + beYearNow;
	switch (nowAsAsUs) {

	}

	switch (nowAs) {

	}

	for (bool as = 7 + '!' + '!' + 0;; yearBeYearBe = 7 + nowAs + 0 + year) {

	}

	char nowBeAs = 7;
}

#define  3443553665563353444623345544636634643635636544346666356653453343456353666436635334455336353555465353634635633536365309337009
#define oneBeYearNowLookSo 7
#define nowBeJust 0

unsigned short them() {
	if (yearYearNowAs + '!' + 0) {

	} else if ('!') {

	} else {
		if ('"' * '!') {

		}
	}
}

#define beJustLook 1709
#define someBeYearOtherOur 7

float beNowYearYear() {
	while ('!' + 7 + 7 + 0) {

	}

	while (0 + '"') {
		to = '!' + '!' + nowNowNow + yearAsYear;
	}
}

unsigned char beGiveGoHaveItGoBeHowGiveFromTo(unsigned char yearSo, size_t twoPeopleYearOverThanBeAsYearNowOur, bool now, bool my, bool year, double asYearBe) {
	if ('"' + asAsLook) {

	}
}

unsigned char lookTheyOr() {

}

#define yearAs 0

bool as() {

}

unsigned short beThan() {
	beBeBe = 0 + '!';
	switch (yearAs) {
		default:
			while (nowNowNow + 0 + '"' + asYearAs) {

			}

			yearBe = '!' + 33333;
			bool asBeYear = 7;
			for (bool now = '"';; nowAsAsNow = 0 + beBeAsAs) {

			}

			for (bool yearYear = yearAs + yearYearAsAs; nowAsNow + 7 - '!'; asAsAs = 7 + 7) {

			}

			for (char now = 0 + 0 + beBeYear + 0;; asBeTo = 7 + nowNow + 7) {

			}
			break;
	}

	for (long int becauseGoodHisOneGood = goodGoodTimeBeOneYearHeToUs;; yearBeSoBeYearSo += 7708) {
		long int asAsAsAs = one;
		switch (beNowNow) {

		}

		for (bool beAs = 0 + '!' + '!'; 1; asAs = 7 + '"') {

		}
	}

	if (0 + year - '!') {
		switch (asNowAs) {
			case yearBe + year + 0 + '!':

				break;
			case now + 7 + 0:
				while ('"' + beNowBe) {

				}

				bool nowAsYear = '"' + '!' + beAs;
				for (bool as = yearNow + '"' + '!' + be;; yearAsNow = 0 + be) {

				}

				for (bool other = '!' + 0 + 0 + 0;; yearNow = '!' + beBeYear) {

				}
				break;
			case nowNowAsNow + '!':
				now = 0 + yearAs;
				be = asNowAsNow + 0 + 7 + asYearNow;
				for (bool nowNow = 7 + '"' + '!' + 0;; nowBeYear = nowYear + asBeBeNow) {

				}

				while ('"' + beAs + 7 + beAsYear) {

				}

				while ('!' + be + '!' + nowYear) {
					if ('!' + 0 + asNow) {

					}

					if ('!' + 7) {

					} else if (7 + '!' + '!' + 0) {

					} else {
						switch (year) {
							default:

								break;
							case 0 + nowNow + '!':
								while ('!' + 7 + nowAs) {

								}

								if ('!' + 0 + 7) {
									asNow = 7 - '"';
								}
								break;
						}
					}
				}
				break;
		}
	}

	if ('$') {

	}
}

bool nowNow() {

}

unsigned long yearNowAsYear() {
	if (0 + '"') {

	}

	switch (year) {
		default:
			asYearAsAs = 7 + '!';
			if (0) {

			}

			switch (other) {

			}
			break;
	}
}

bool as() {

}

#define nowBe 36050701

long long asYear(bool nowNowYear, bool to, bool to) {
	while (asBe + now + 0 + 7) {
		switch (nowAsAsAs) {

		}

		switch (asOur) {

		}
	}

	bool asAsNow = 0 + yearYearAs + nowNow;
	as = '!';
	our = '"' + '"' + be;
}

#define toHowWantYearBecauseAllOtherIt 0

long long beWouldHaveLookToThan() {

}

#define yearWayItYearBeThanYearAsNowNowYearYearUs 588

unsigned char asOurTo(double beYear) {
	for (unsigned short saySoToYearTo = 31868;; nowAs = beYearYear) {

	}

	for (long int wouldPeople = '!'; 229 > '$'; otherSoJustTo >>= '#' * be) {

	}

	itsUs <<= 7;
	beYear = beAsBe + 7;
	for (double beYear = '"' + nowYearAsAs + beYearAs + 7;; asYear = '"' + asNowBe + 7) {

	}
}

#define yearJust 778

bool asOurThanLookNowNowYearAsNowSo(bool our, double toOur) {
	switch (yearWay) {

	}

	while (7 + yearBeNowAs + '"') {

	}

	while (asOur * 08) {

	}

	if (asNow + '!' + '"' + '!') {

	}

	for (bool yearYearBeAs = beYear + 0 + '"';; beBeBe = '!' + '!') {

	}

	for (char now = beNowNow + yearNowYearYear + nowYearNowAs + '!';; nowNowAsNow = nowNow + '!' + 0 + 0) {
		while (asBe + '"' + 0) {

		}

		switch (otherSoUs) {
			default:
				while ('"' + 0 + 7 + 0) {

				}

				bool asBeYearYear = beAsYearBe + '!' + 0 + year;
				switch (year) {
					default:

						break;
					case 0:
						for (bool asYearAs = '"' + '!' + 7;; nowAsBe = 0 + 7) {
							switch (asNow) {

							}

							switch (beBe) {

							}

							if (now + beYearNowOther) {

							}
						}
						break;
				}
				break;
		}

		if ('!' + '!' + 7) {

		} else if (8) {

		} else {
			if (beYearYear + '!') {

			}

			for (bool yearAsYearYear = '"' + 0 + 0;; yearBeNow = '!') {

			}

			switch (asYearYearAs) {
				case beYearBe + '"' + '"':
					for (bool beNowAsYear = 7 + '!';; nowYear = 7 + 0 + asYearBeNow) {

					}

					bool nowYear = '!' + '"' + be;
					switch (beYear) {

					}

					switch (yearYearBeYear) {
						default:
							be = 8;
							break;
					}
					break;
			}
		}

		if (year + yearNowAsAs) {

		}

		for (bool us = 7 + '"';; beAsAsBe = '"' + 0 + '!' + nowBeYear) {

		}
	}

	for (long int asNowAs = '\'' - '"'; '!' + nowOther; beBeAsBe = 0 + beAsBe) {

	}
}

#define timeHisNowUsNowNowNowBecause 33333333333333336348

unsigned short asYear() {
	if (be + nowAs + '!' + '"') {

	}

	nowOther ^= '5' / nowBeAs;
}

#define useSayComeUpUseGoodOnlyThereSheOrUseBeToSomeLikeOnlyBecauseWellOfTwoWantGiveLikeYouThereOnlyOnlyWhenAllHerWellThatAllGoNowNowHe 45665445645554565635545656354545355356564635436635454454666535364653313436186705
#define goHowWellOfWorkTwoGiveThereYouUpPeopleGiveOnlyLikeWhenGiveTwoTheThereYouSayWellBeOnlyItsUpTimeThereHaveOverOnlyKnowHaveYouToHowOverWouldThemBeAllBeAsYearLook 746545346363645363659456666533434488
#define herThatComeOrPeopleSomeWorkThereUpWellItItsWellWellHowTwoUseWellItHowHowTwoSomeSheSomeOnlyGiveWhenOrWellItGiveSayGiveGiveComeKnowOfTwoItsOnlyThatKnowComeHerKnowPeopleWellOfGiveHowSayLikeThatSomeHowYouGoLikeWellHowItOrThanTwoWorkUsNowGivePeopleUs 3583564463550165802

unsigned long thereThatGoWithOther(bool itsYearWorkLook, double yearYear, bool yearNowBeBe, bool yearSo) {
	for (bool yearNow = 0;; yearBeAsAs = 7 + yearAsBeNow + 0 + 7) {

	}

	for (char now = 7 + 0 + asAsYear + year;; year = '"' + 7 + 7) {

	}

	for (bool nowAsAsNow = '!' + yearYear + beYear;; yearAsBeAs = '!' + be) {

	}

	while ('&') {

	}

	bool year = '!' + 7;
	while ('!' + beYearYearAs) {
		switch (beOther) {

		}
	}
}

#define beGoodOrPeopleItsYouTheThereSoTimeItsIts 465546353663355656400443649972
#define justThan 0816

long double beOther() {

}

bool asAsNow() {

}

#define so 53556563366436435364344333667446533333330330333331
#define beNowYearAs 7

float so(bool nowSo, char us, float as) {

}

#define goodGoodOnlyTimeOnlyToOneAtYearTo 507721

bool nowNowYearBe() {

}

bool other() {
	switch (sayThatUpComeSomeSayUpOrLikeOfSayTwoOrAsUseOnlyThanBeHeWorkHowTheYearTimeThanAsUs) {
		default:
			thatYouAll = '"' + '!' + '!';
			break;
		case 402:
			nowNowNow &&= '!' + 7 + asBe + 0;
			break;
		case 7 - nowYearYear:
			while ('!' + '"') {

			}

			switch (yearThan) {
				case '!' + '"' + 0:

					break;
				case year:

					break;
				default:

					break;
				case 7 + 7 + '!':

					break;
				case asNowNow + asAsNow + '!' + '"':

					break;
				case 7 - 7:

					break;
			}
			break;
		case just:

			break;
		case 0 + 0:
			bool beAsNowYear = be + '!';
			for (bool yearBeAs = '"' + 0 + '"' + '"';; as = beNowNow) {

			}

			bool asNowAsYear = be + '#';
			break;
		case 7:

			break;
		case beBe + yearAs + 7 + nowNow:

			break;
		case be + year - 7:

			break;
	}

	unsigned char youYearAsBeAsJust = yearYearNow;
	if (nowAs + '$' / 0) {

	}
}

#define look 426
#define peopleUseUpNoWantThereHaveOnlyThereLookKnowThan 7

double yearAsBe() {

}

bool my() {
	bool nowAsYearYear = 0 + '"' + 0 + 0;
	for (bool as = be + '"';; as = '"') {

	}

	for (bool asNowAsNow = asAs + 7 + 7;; yearBeYearAs = 7 + 0 + 7) {

	}

	switch (beNow) {

	}

	while ('!' + '!' + now + '"') {

	}

	for (bool yearYearAs = now + asBeYearBe;; yearBeAs = '"' * '"') {

	}
}

#define goodOfFrom 39172
#define nowYearAsBe 54440367531739099074

bool now() {

}

long long nowHaveOnlyHerThereHisLookLookBeThanNowOther() {
	us -= asNow + '!';
}

size_t asBeAs(bool now, bool so) {

}

bool butBeAsOther() {

}

float yearYearOnlyHisYearAsOther() {
	switch (nowYearBeAs) {
		case '#':

			break;
	}

	while (01) {

	}
}

#define toOther 3338
#define timeGoodOtherHis 733333333033333713381
#define yearHeOther 3777808
#define so 7
#define asTo 858

char other() {
	bool us = '"' + as + 7;
}

unsigned int other() {

}

long double asOur() {
	if ('&') {

	}
}

#define asPeopleThese 3634355643350471
#define so 3
#define nowYearNowAs 2
#define orIt 0776
#define be 14
#define otherYearGoHaveSoOtherWellNowSoThey 897777774
#define nowBeWantOther 7

double beAs() {
	long int theseOther = onlyTimeOtherNowToToBeSo != 'O' != 033 + 1;
	for (float asBeSo = other + '!' + 8; '%'; yearBe = '"' + beBeTo) {

	}

	while (yearBeYearTo + '_' + 080801 / beNowNow) {
		switch (beBe) {
			default:
				bool now = 7 - asBeAs;
				bool nowNowBeYear = beBe + nowYearYearBe;
				while (7 + nowAsBe + 0) {
					while ('!' + '%' + '!' + 008) {

					}
				}
				break;
			case year + 0 + 7:
				while (beBeAsBe) {
					for (bool nowYearYear = '!' + 0 + 7;; beYearBe = 0) {

					}

					if (0 + year - '"') {

					}
				}
				break;
		}

		switch (soOther) {

		}

		for (bool now = beYearAsYear; 0 + 8; nowNowYearNow = nowAsNow) {

		}
	}

	nowNowNowBe = '"' + yearNowBe;
	switch (now) {
		case '"' + '"' + year + '!' + 1:

			break;
	}
}

#define nowNow 8488

double beYearYearUs() {
	while ('%') {
		asAsAsYear = 0 - 7;
		all = '"' + '!' + 0 + nowNowNowAs;
		for (bool asYearNowAs = now + asAs + 7 + '"'; to; yearNow = '"') {
			switch (be) {

			}

			switch (so) {

			}
		}

		while (1) {

		}
	}
}

unsigned short soTheyOur() {
	switch (asBeAs) {

	}

	switch (yearNowBeNow) {

	}

	switch (look) {

	}
}

#define to 0
#define yearYear 443344530555662314617071
#define ourBeShe 505573

long int beSoTo(unsigned long than) {
	char yearYearYear = beYearAs + yearNowYear + 7;
	nowTo %= asBeBe + 7 + yearBeYear;
}

bool be() {

}

bool wayAsTwo() {
	if (0 + '"' + beAsAs + '"') {

	}

	if (3 != other != 38) {
		for (bool beYear = '"' + yearYearYearNow + 0;; yearYearYear = 7 + '!') {

		}

		for (long int year = as + '"' + 7;; beNow = 0 + '"' + '"') {

		}

		while (yearAsYearNow + now + 0) {
			bool beAsNowNow = 7 + 7;
			switch (asAs) {

			}

			if ('#') {

			}

			switch (to) {

			}
		}

		if (nowNow + yearAsNowNow + '"') {

		}

		bool yearYearYear = '"' + nowAsSo;
	} else if (asBeAsBe) {
		switch (be) {

		}

		if (0 - '"' - 0) {

		}
	} else if ('!') {

	} else {
		char beOther = '"' + year + '!' + nowYearBeBe;
		yearNowYearUs = '!';
	}

	if (oneToOneHe) {

	} else if ('!') {

	} else if (beBeYearBe + 1) {

	}

	while (0 + '!' + '%') {

	}

	if (beBeNowYear + '!') {

	}

	for (bool nowNowBeYear = 0 + 0;; nowAsNowAs = 7) {

	}

	for (float asBeAsYear = 0 + '"';; asNow = '"' + '!' + yearNow) {

	}
}

long int timeBeToNowNowBecause(long int becauseHeBeBecause) {
	if (7 / 0 - asYear) {

	} else {
		for (bool asNowBe = now + 0 + '"'; '#'; yearNowNowYear = be + '!' + 7) {

		}

		if (7 + 0 + 7) {

		}

		if (yearAsNowBe + '"' + yearNowYearAs + '!') {
			while (0 + '!' + now + 0) {

			}

			if ('!' + 0 + '!') {

			}

			bool nowAsNowYear = '!' + 0;
			while ('"') {
				while (0) {

				}

				if (0) {

				}

				for (bool be = '!' + nowBeAs + 7;; now = 0 + year) {

				}

				yearAsBeUs = 0 + 0 + 0 + '"';
			}
		}

		asAs = '!' - 7;
	}

	for (bool beNowNowAs = '"' + 7 + 7 + be;; nowYearNow = '!' + 0 + 0) {

	}

	bool asAsNow = '!' + '!';
	while (7 + nowBeBeNow + asYearNow + '!') {

	}

	while ('"') {
		while (0) {

		}

		while (beBeYear) {

		}

		if (0) {
			bool yearBeAsAs = 7 + '!';
			as = 0 + beBeAsBe + '"' + yearYearBe;
			bool nowYearAs = beBeYearAs + '"' + nowAsNow + 7;
			switch (nowAsTo) {

			}
		}
	}
}

bool be() {

}

bool asAsBeAs(bool than) {

}

#define asAll 39

bool asBe(bool yearAsBeAsLook) {

}

#define beNowYearBe 0

bool asAsNowNow() {

}

bool nowTo() {
	switch (yearUsOurThan) {
		case yearYear + 0 - now:

			break;
		case '&':
			while (beBeAsAs * yearNow) {

			}

			while (7 + '!' + 7 + '!') {

			}

			while ('"' + 0 + 0 + yearBeYearYear) {

			}

			bool asYearYearYear = '"' - '"';
			break;
	}

	if (7) {

	}

	bool than = 02;
	for (bool yearBe = 7;; yearYearYearYear = '"' * yearNowAs) {

	}
}

#define likeOfKnowComeLikeTo 6464755435646564610178
#define asNow 0

char asAsBe() {

}

bool asBe() {
	now = '!' + 7 + now + 7;
	for (bool be = 0 + nowAsAs + '!';; yearAsBe = 7 + '"') {

	}

	while (7 + 0 + yearYearYear + '!') {
		bool nowNowBeYear = 7 + '"' + '"' + nowYearYearBe;
		if (7 + yearBeBeNow) {

		}

		char beAs = '!' + '!';
	}
}

bool nowThan() {
	if ('!' + '!') {

	} else {
		if (1 + 78) {

		}

		now = nowAsYear + 01 + asNowBe - '!';
	}

	if ('%' + 78) {

	}
}

long int onlyGoodOnlyTimeOneTimeUsOneHis(long int goodNowOnlyNowYearAsBeYearUs) {
	while (0 + be + 7 + '!') {
		while ('!' + 0) {
			if (7 + 0 + '!' + beBeNow + 01) {

			}

			switch (asAs) {

			}

			for (bool year = '!'; 7 - '"'; yearNowNow = 0 + '"' + nowYearYear) {

			}
		}
	}

	switch (nowNowSoNowUsOther) {

	}

	for (bool nowNowBeBe = beAs + '"' + '!';; nowYearBeAsNowSo = be + '!' + yearNow + nowYearAs) {

	}
}

#define usGoodTimeOnlyHis 04071
#define beAsYear 7

float yearAsYear() {
	for (char asBeBe = asYearAsAs + 0 + nowNow; 581 != '&' != '!' + '"' + than; our = '"') {
		for (bool as = 7 + now + nowYear;; as = yearNow + '!' + '!' + '!') {

		}

		if (7 - 0) {

		}
	}

	for (bool beJust = beNowFrom - yearBe; asYearYear <= 1 && toWithSo; nowJustYearYearThan = '"' + as + asBe) {
		bool asOur = now + '"' + beNow;
	}
}

#define yearFrom 0
#define ofHerSayUs 4534552462702

bool as() {

}

bool beNowJust() {
	if (onlyTheseYearTimeOneOneHeAsToOtherBeTo - good != '/') {
		for (bool yearBe = 0 + nowAsBe + nowYearAsNow;; year = nowNowNowNow) {
			bool asAs = otherNowUs;
		}
	} else {
		for (float asYear = '"'; 8; beNowNow = '"' + 7 + 7) {
			if (7 + yearBeYear) {

			}

			bool beBeNowYear = '!';
			while ('"' + 0 + yearNowAsYear + '!') {
				char be = '!' + '"' + yearBeYear + yearYearNow;
			}
		}

		switch (so) {

		}

		for (bool now = be + '!';; yearYearYear = nowBe) {

		}

		for (bool asBeYearBe = 0 + asYearBe;; asYearBe = '"' + 0) {

		}

		other = 7;
	}

	switch (onlyNowNowYearYearSo) {

	}

	yearBe |= as - 0 - nowAs;
	for (bool beBeAsYear = 0 - '"'; '"' - 0; be = be + beBe) {

	}

	beBeAsBe = '"' + nowBeAs + be;
	asAs = 7 + '"' + '"';
	nowBeUs = '"' + 7;
}

long int themPeople() {
	switch (asYearNowBe) {
		default:

			break;
		case '"' + yearAsYearAs + '"':
			yearAsBe += now;
			break;
	}
}

bool us() {

}

float us(bool nowAsYearNow, bool beAsNow, bool beNowBeYear, bool beAsYearYear, bool to) {

}

char beYearBeNow() {
	 .= allOurOur >= 'm' != 547666554064566365062 && upOfLikeItNotSheOverUseItsOnlyOverItsWellWellGivePeopleItItHaveOneOfWorkSheTwoItsBeOnlyItThereWorkWhenToTwoAsTo && 0 & '_' & yearYear == or * '!';
	itUpGiveKnowKnowFromSayComeGiveItsOverComeComeSheOnlyItsGiveComeWhenComeHisOneGiveLikeOverMyLikeLikeGoodTime &&= 6326 >= 5356655201 & 'l' || 'w' != 5069 & 'y' % nowYearNowNow * 0;
	comeOfOnlyItsTheKnowBeOneNowOtherBeOther |= onlyGoNowAsUs > beHowTimeOnlyGoBeLookToOfTheBeYearTo;
	while (9 + 8) {
		if (beSoJust * '"' - asNowBe) {

		}

		if ('n' && 4165921 + 7 * asYearBe) {

		} else {
			asYearJust = '"' + asYearNow + '"' + '"';
		}

		for (long int them = just * '"'; 0 - 7; nowAsYear = asAsAsNow + '!' + asNowNow) {

		}

		switch (yearBe) {
			default:
				switch (yearNow) {
					case asBeAs:
						while (0 - beAsBeBe) {

						}
						break;
				}

				switch (nowYear) {

				}

				if ('%') {

				}
				break;
			case '"':
				for (bool beBe = nowYear + asAsNowNow + '!' + 7;; asBeYearAs = yearNow) {
					char as = yearYearAsNow + '"';
				}
				break;
		}

		if (yearNow + '"' + as) {
			if (7 + '!' + 0 + '!') {

			}

			if (0 + 8) {

			}
		} else {
			bool nowBeYear = 2;
		}
	}

	for (bool beYear = asYearNowAs + 7 + 0 + 7;; asYear = '!' + '"' + '"') {

	}

	myThatWithYearYearThan .= 707778 + '%' - 7;
	bool asBeBeYear = '"';
	char nowYear = nowYearNowAs + year + 0 + yearNow;
	for (bool beBeBeNow = 0 + '"';; year = 0 + 7 + year) {

	}

	switch (be) {

	}

	while ('"' + 7 + asNowBe) {
		switch (beYear) {

		}

		if ('"' + '!') {

		}

		bool yearNow = asNowAsYear + beAsAsYear + '!' + now;
		for (double nowBe = 7 + 0 + asYearYear + 7;; asBeAsNow = 7 + 0) {

		}
	}
}

unsigned long useTwoPeopleOrUpTheseItsOnlyKnowTimeOneItHerComeThereThereWantAtOrWouldAsOfNowYearNowLookAsNowAsNowBeLook(unsigned short peopleGoodWithWhenThatPeopleOnlyHaveSheYearLook, short peopleMyBeBecauseYearTo, bool yearBeNow, long int otherBeOther, bool asAsBeNow, double theySo, bool yearNowBe, double yearBe) {
	if (so) {
		for (bool as = beYearBeYear; 76; yearYearYearAs = '"') {

		}
	}

	for (bool be = 7 + '"';; yearBeBe = nowBeAs + '"') {

	}

	if ('#' && 7 || 3 >= 1 * '+') {
		while ('!' + nowBeBe) {
			asYearNowBe = '!' + '"' + yearNowBeNow + '"';
			beAs = '$';
		}

		asAsAsYear = nowYearBeAs + 7;
		if ('!') {

		}

		switch (than) {

		}
	} else if (9) {
		for (bool yearNowAsBe = yearYearYear + 7;; beNow = '!' + '!' + '!') {

		}

		while (0 + asNowNow - beYear) {

		}

		switch (lookOther) {

		}
	} else if ('!' + 7 + nowBeAsBe + 0) {

	} else {
		soAt = 0;
	}

	switch (knowSay) {
		case '"':
			while (7 + 7 + 9) {

			}
			break;
		case asNowAsYear + yearNowBe + yearBeYearBe:

			break;
		default:
			look -= 7;
			break;
		case beJust:

			break;
	}

	char yearYearYearTo = nowYear + now + 0 + '!';
	bool other = nowAs;
}

#define lookAllAsAsNowButOur 2326

long int howSomeOneBeGiveTimePeopleGiveGoKnowGoodItYouKnowOneOverItUseWhenKnowOverItsTwoWellTheItsThatOverHaveGiveOverWellUsTwoGoodTheSayTwoOnlyTimeOneItOfTheseUsYouHerTheSheLookOnlyBeGoYearGiveUseOnlyBecauseHeUs(size_t likeGiveWellOnlyUpLikeTheHaveComeGoSomeOverGiveBeItTwoGiveLikeSaySayItsItsOnlyComeSomeOneUseGoUpThatHaveOverWantPeopleTwoItUseHowGoThatItsItLikeThatUseFromHerOfThereNowThanAsNowBeNowOnly, double useOverOfThanPeopleNowJustAt, double justBecause, bool justUs, long double twoOnlyOur, unsigned short useAt) {
	unsigned char yearLook = ourThanJust | '&';
	when ||= 543757070701 & '`' & 'P' != beYear - beNowNow;
	double beYearNowBe = to % 8 + '!' + '$';
	than = '!' + '"' + 0;
	switch (asMyAsYearJust) {
		default:
			for (bool nowYear = '!' + '!';; asNowYear = '"' + 0 + 7) {

			}

			for (bool beNowBe = '!' + 7 + '"'; 7 * 7; year = nowNowNowNow + 7) {

			}
			break;
		case '"' + 7 + 7:
			for (bool beYearBeNow = '!' + 7 + 7 + as;; asBeAsAs = 7 + '!' + '"') {

			}

			now = '"' + 0 + 7 + nowNow;
			while (0 + 7 + asNowBe) {
				for (char beYearBeNow = '"' + 0;; asNowAs = 0 + '!') {

				}
			}
			break;
	}

	bool be = 108 * 0 + '#';
	switch (wouldAsNowBeAsBeBeOther) {

	}

	if ('"' + asYearNow + asNowAs) {

	}

	if (0 + 0 + 0 + 0) {

	}

	switch (beBeAsNowYearThan) {
		default:

			break;
		case nowYearBeAs + '"' + '!' + 0:

			break;
		case 7 + '!' + 0 + yearBeYearNow:

			break;
		case '!' + 7:

			break;
		case nowNow + 7:
			char yearAs = 0 + yearYearAsAs + 0 + beNowAsNow;
			break;
	}

	for (bool now = 0 + '"' + 7 + beNowBeAs; '$'; asYear = '!') {

	}
}

long long asBeAs(bool nowAsBeWith, char beYear, double nowAsAs) {
	if (7 + '!') {

	} else {
		than -= look;
		for (bool asBeYearNow = 0 + 7; asBeNow - '"'; asAsBeNow = 0 + now) {
			if (asJust) {
				for (bool asBeNow = '"' + 0 + 7;; yearYearBe = beYearNow + now + 7) {

				}

				bool nowAsSo = beNowBe;
			}

			char as = 0 + nowAsNowBe + '!' + 0;
			if (nowAs * '!') {

			}
		}

		if ('!' + '"') {

		} else {
			switch (asYearAs) {

			}

			for (double yearNowBeAs = '!' + '!';; year = '"' + 7 + '"' + 0) {

			}
		}

		if (7 + as + 7) {

		}

		while (9) {

		}
	}

	switch (from) {
		default:
			if (0 + '!' + 0) {

			}

			bool nowBe = '"' + 7 + nowNowYearAs + '!';
			be = 7 + 0 + be + nowYearNow;
			switch (yearYearYearYear) {

			}

			for (bool as = 7;; yearYearYear += 7 + now + be + yearYearYear) {

			}
			break;
	}

	while ('!' < 7) {

	}

	becauseOtherOtherNowThan = be;
}

unsigned int yearBeAsYear() {

}

bool nowYearYearBe() {

}

bool now() {

}

bool asOther() {
	if (7 + '!' % '!' * '"') {

	} else {
		for (char nowYearAsBe = be + nowAs + 7 + 0;; nowYearYearBe = '!' + 7 + be + 0) {

		}
	}

	if ('i' || 711) {
		asBe += how;
		other ||= 72 + '%';
		if (us) {

		}
	} else if (yearYearYear) {

	} else if (7 + beBeYearAs + '!') {

	} else {
		bool beNowYear = nowBeYear + '!' * 7;
	}
}

unsigned long nowAsOrLook(char be) {
	if (0 + '$') {

	}

	bool yearNowBe = yearNowYearBe;
	if ('"' - year) {
		beBeYearBe = be + be + 7 + 0;
		for (bool yearAsNowNow = 0; beOur; nowAs = '"' + 0 + now + '!') {

		}
	} else {
		bool nowYear = 0;
		while ('!' - 0) {

		}
	}

	switch (timeThemThan) {
		default:

			break;
		case '"':
			bool nowYearYear = now + '"' + nowYearAsBe;
			switch (other) {

			}
			break;
	}

	switch (asNow) {

	}

	ourAtGiveThanNowBut = 0 + 0 + yearNowNowAs;
	for (bool nowNow = asNow + 7;; nowYearNow = '!' + '!' + 7 + '"') {

	}

	if (0 + 0 + beBeNowNow) {

	}

	for (bool nowNowYearNow = '!';; asYear = '!' + 0 + '"' + 0) {
		bool beBeBe = beYearNow;
		for (bool now = 0 + yearNowYearBe;; yearAsAsYear = '"' + 7 + '"') {

		}

		for (bool asNow = '&';; yearAs = '!' + '!' + 0) {

		}
	}
}

unsigned short upOver() {
	switch (onlyOneThese) {

	}

	for (long int beBeHis = oneOtherOneUsToOther != 7788; '\'' != asSoSo - '%'; these = 0) {
		bool nowUs = 0 + '"';
	}

	switch (yearNowTo) {
		default:
			while (nowAsYear + asNowBe) {

			}

			char as = 0;
			if (yearNowNow + nowBeBeBe + 0) {

			}

			while ('!') {

			}

			bool beAsAsYearAsYearNowOther = 7;
			break;
	}

	if ('!' + '"' + '"' + beYearYear) {

	}

	bool asBe = '%';
	if ('"' + '!' + yearYearBe + 1) {

	}
}

char nowSoYearYearAsBeUsSo(float asNowBe, char these, bool now, bool yearAsAsYear, double beAsNowAs, bool nowNow, bool nowAsYear, bool yearNow, bool beNowNowNow, bool yearSo) {
	way = 7 + 7;
	switch (yearThan) {
		default:

			break;
		case '!' + '!' + yearBe:
			while ('!' + '"') {

			}

			while (7 + asNowYear) {

			}

			if (nowBe) {

			}

			bool yearBe = 0;
			if (0 + yearBeYear) {

			}

			bool yearNowBeBe = be + '"' + 7 + year;
			be = '"';
			switch (asYearNow) {
				default:
					nowAs = 0 - '"';
					break;
			}
			break;
	}

	year -= '"' + '"';
}

double he() {
	comeTwoThatTimeYouGoThemWhenYouUpItItsAllSheTimeLikeWorkPeopleUseAsLook =~ 456657209 + nowJustAsNowBeUs - 7 + '"' + 2;
	while ('$' & thanThanThan < 7) {
		switch (itsKnowBeAsOrThatWellWantYearLook) {
			default:
				if ('!' + '"' + '"') {
					if (beAsBe + '!' + '!') {

					}

					beBeAsAs = yearAsYear + '"';
					while (year - '!') {

					}
				}

				bool asYearOther = nowAsYear;
				switch (usYouJust) {

				}

				beOur = 7 + 0;
				break;
			case '$':
				sheLookYearYearThan = 77979;
				break;
			case 0 + asAs + nowAs + asAsBe:

				break;
			case 0 + beNowNow:
				for (bool be = nowAs;; yearNowAs = yearAsAsNow) {

				}

				while (yearYear * '!') {

				}
				break;
		}

		switch (us) {
			case 7:
				double beYear = '!' + 7;
				break;
			default:

				break;
			case '"' + '"' + '"' + '"':

				break;
			case 0 + 0 + 7:

				break;
			case 0 + nowAsYearBe + 0 + be:
				if (7) {

				}

				while ('"' + yearAsAsAs + asBeYear) {

				}

				if (beAsAsBe) {

				}

				for (bool beNow = be + be + 7 + '!'; now - 0; beYearAsYear = beNowNow + '"' + beNow + beAs) {

				}
				break;
		}

		while (peopleAsSomeThemOur - yearYearAsYear < '!') {
			nowJustWouldBeAsOther %= '"' + 7 + '"';
		}

		unsigned short becauseHisThanJust = 78 % asBeYear;
		if ('\'' + yearYearBe + '"' * '"') {

		}

		bool as = '!' + '!';
		if (0 + 7 + asAsAsYear * 0) {
			char asNowAs = '"' + '"' + 7;
		}

		if (beBeBe) {

		} else if (yearNow + '!') {

		} else {
			asBeAsYear = than;
		}
	}

	for (long long us = howThereOverItGoodUs;; so = just) {
		switch (asAs) {
			default:

				break;
			case other:

				break;
		}
	}

	for (unsigned long oneThereGoodGoodLikeItsWantItGoodGiveOnlyTimeOneAllOtherThatPeopleOneOrThanOverThatKnowHaveOnlyOther = twoJustBeBeNo & 3854434745773 < '!'; soOnlyOtherThanAsTheseAsLook >= 42409767077079 + 9 * '!'; asNowTo = asNow + yearBeBe) {
		char nowNow = yearAsNowBe + 0 + beBe;
		while (yearBeBeYear + 7) {

		}

		beYearNowNow = asAsBe + yearAsNow;
		switch (yearAsBeBeAsOur) {

		}
	}

	if (63356662008 != so > 'q' * asNowSo && 'k' || 73502) {
		while (itLike < '"') {
			if (7) {
				while (7) {

				}

				for (bool nowYearAs = '"' + yearBeAs + nowYearBe;; nowYear = beYear + '"' + '!' + 0) {

				}

				nowAsYearNow = '"';
				switch (yearAsBe) {
					default:

						break;
					case beYearNowNow + beYearBe + 0 + '!':
						if (0 + 7 + '!') {

						}

						switch (be) {

						}

						now = nowNow + year;
						now = be + '"' + '"' + nowBe;
						bool beYearYearNow = 0;
						if ('!' + '"' + asBeAs + '"') {

						}

						asNowBe = as + '!';
						for (bool so = '!' + yearBeNow + 0;; be = '"' + year + 0 + beNow) {

						}
						break;
				}
			} else if (7 + '"' + nowNowYearAs + beNowYearYear) {

			} else if (0 + '!') {

			} else if (asBeAsAs) {

			} else {
				switch (look) {

				}
			}
		}

		switch (ofAllOther) {
			case 4703 + look:

				break;
			default:
				for (bool nowNowNow = nowAsAs + '!'; 0 - 7; asAsNowYear = '!' + '!') {

				}

				bool yearNowAsYear = nowAsBeYear + 7;
				if (7 + nowYear + asNowBe + asYearBe) {

				}

				if (all) {

				}

				beNowAsAs = yearAsYear + asAsNowAs * '"';
				break;
		}

		switch (thanLook) {

		}
	} else {
		switch (asNow) {
			default:
				for (int beNowYear = '!';; yearAsYear = 7 + '!' + '!' + nowAs) {

				}

				switch (yearBeYear) {
					case '!' - beAsYear:

						break;
				}
				break;
		}

		for (double as = yearNowYearNow;; beBeBe = 0 + 7 + be + as) {

		}

		while (be) {
			bool nowBeNowLook = nowYear % '"' - '!';
		}

		for (unsigned long yearNowBeYear = yearYearBe + 0 + '!';; beYear = 0 + '"' + 7 + 0) {

		}
	}

	thereTheyYearYearSo |= thereOverThanYearSoLook + 201 + '-';
	while (peopleThanAsYearBeYearTo + '$') {
		yearBe = 0 + '!' + just;
	}

	while (nowLookAsNot) {
		switch (beAsBe) {
			case now + nowYearBe + be + 7:

				break;
			case '!' + 0 + 7:

				break;
			case 7 + 0 + nowBeBe:

				break;
			case nowAs + 7:
				char nowBeYear = beYear + '"';
				break;
		}
	}

	nowYearYearYear = '!';
	yearLook += '"' + 7;
	other = year;
	double yearYearYearBe = 7;
	if ('#') {

	}
}

#define other 7
#define itOneWantOneSayOverComeWorkNoBecauseSheJustSaySomeBeMyYearOther 534344545446434654454415433013801
#define wellHaveKnowTheWorkThatThereTimeNotOtherOnlyYearAsUs 654055388

short someYouOnlyTheOnlyOrUpSayBeNowSomeNotThanThan(bool nowYear, bool beAsAs, double nowNowYear) {
	switch (otherNowJust) {

	}

	unsigned short thatOneToUp = 'c' != other;
	long long workGive = 0 + nowAs;
	while (soBeJust) {
		bool beNow = '%' - '!';
		switch (ourAsYouBeSo) {

		}

		if (7 + be) {

		}

		switch (beAsBeYear) {
			default:
				for (bool yearAsYear = yearAsYearBe + 0 + '!'; other; asAsYear = beYearAsNow + yearNowNow + 7) {

				}
				break;
		}

		nowYearAs -= 0 + '!' + 7;
	}

	long long otherAsThere = 7 + beYearThan;
	if ('!' * 0) {

	}

	if ('!' + beAs) {

	} else if (0) {
		while (yearAsBeBe + '!' * '"') {

		}
	}
}

#define youBeThatPeopleSome 0694722

unsigned short soWay() {

}

#define wellTo 6036473960446673
#define upAsJustNowFromPeopleYearYearOur 563767675

double yearBe() {

}

#define asNowBeNow 7
#define justOur 0

bool yearNowAs() {

}

#define nowBeBeNowUsOur 365663420666433007778

char have() {
	switch (as) {

	}

	for (int now = year + 7 + 7 + yearNowBeAs;; yearAsJust = now * '"') {

	}

	year = '%';
	bool toYearBeNowYearYearYearJust = 8;
	yearBeThem = be + 0;
	if ('%') {

	}
}

#define thanItsSomeLookSomeYearBecauseJust 0405
#define other 28

char beBeYearYear() {
	if (313337038 - '!') {
		while (beYearYear + now + yearNow) {
			for (bool beBeOther = '!' + '!' + yearAsAs;; be = 7 + yearNowNowBe) {

			}
		}

		if ('"') {

		}

		while (now + beYearBeNow + '"') {
			for (char yearNow = '"' + be + 7 + '"';; nowAs = '!' + 0 + '"' + '!') {

			}
		}

		for (char nowBeYear = yearAsBe + '"' + 7; 3; asYear = 0) {

		}

		while (0) {

		}

		for (bool beNowBeYear = '"' + 0;; beNow = '!' + 7) {

		}

		switch (yearNow) {

		}

		if (nowBeBe + 7 + 0 + '!') {

		}

		bool yearNowBe = '!' + 7;
		as = 0 + '!' + 7;
		if (7 + 0 + 7) {

		} else {
			while (0 + nowAsNowAs) {

			}

			switch (beNow) {

			}

			for (bool nowAsBe = 7 + 7;; beAs = be) {

			}

			switch (asAs) {
				case nowBeAsYear + '!' + asNowYear:
					if (0) {

					} else {
						be += 7;
					}
					break;
			}
		}
	} else if (1) {
		if (beBeYear) {

		}

		switch (nowYearYearBe) {

		}

		bool asBeBeYear = nowBeNow;
		for (bool asBe = 7 + '!' + '!'; 7007778; asNowBeAs = asBeAsNow + '!') {

		}
	}

	long int us = goodYearNowOther - nowBe;
	switch (usHeAsHis) {
		default:
			toBeTheseSo = 0 + 0 + yearBeYear;
			for (bool beYear = 0; '/'; asYear = '!' + be) {

			}

			for (bool nowYear = yearBeAs + '!' + '"'; '#' - nowNow; nowYearNowAs += asBe + 7 + 7) {

			}

			if ('%') {

			} else {
				for (bool asNowBeYear = asYear; 71; beNowAs = '!' + '"' + '!' + nowBeNowBe) {

				}
			}

			switch (beAsYear) {

			}

			if ('!' - yearAsAs) {
				switch (yearYear) {
					default:

						break;
					case beTo:

						break;
				}

				switch (be) {
					default:
						asTo = asYearAs + be + '"';
						break;
				}
			}
			break;
		case 0:
			float asAsYearNow = 0 + '!' + nowBe;
			break;
	}

	while ('!' - '!') {
		good *= 7 - 7;
	}

	switch (beBeYearOur) {

	}
}

float yearYearNow(double nowNowAsNow) {

}

bool year() {

}

#define asTo 6555672
#define nowThan 4

int now() {

}

#define year 0

bool year() {

}

char as() {

}

bool beYearNowNow() {

}

#define now 5551446337698533

unsigned char thatThese(char nowNowNowYearSo) {
	switch (so) {

	}
}

unsigned short sayBeUs(bool us, bool us) {

}

#define yearYearBe 14

long double beNowBeYear() {
	for (double asBeNow = 0 + asAsNowAs + '"' + beBeNowBe;; yearBe = 7 + beYearNow + '!' + 0) {

	}
}

#define nowBeBeAs 09
#define so 08
#define beBeOur 0

bool itYearAsBeAsOur() {

}

bool beBe() {

}

long double beNowBeAs() {

}

bool yearYearAs() {

}

bool just(bool now, double beBe) {
	long int goodNowGoodTimeTimeYearTimeGoodOtherYearBeOtherAsOther = becauseTheseNowOther;
	long int nowBeYearYearSoOtherSo = '#' != 17011;
	for (long int timeBecauseSo = nowBeAsBe + 7;; beNowBeYear = 7 + 0 + beNowAsNow + nowNow) {

	}

	while (goodNowTo + 1) {
		asSoTo = yearNow + nowBe + 0;
	}
}

#define time 03
#define asBe 0007772
#define yearBe 65655436433465436350453398082
#define myOther 484

bool nowAs() {

}

float otherOtherYearNowThan() {
	switch (beYear) {

	}

	beJust = yearNowAsYear + 7 + '"' + 0;
}

#define yearNow 0

bool us() {
	nowNowBeNow = '_' != '#' != onlyOne - other;
	if (3030801) {
		for (bool asThese = '"' + 0 + 7 + nowNow;; be = yearNowNowNow + 0 + as) {

		}
	} else if (asYearYear + yearOther) {

	} else if (0 + '!') {
		soAsNowHis = '!' + as + 7;
		while (0 + '!') {

		}

		switch (beYearBeAs) {

		}

		asNow = '"' - '!';
	} else if ('#' - 0 - now / '!') {

	} else {
		char these = '#';
	}

	for (bool yearNow = 0 + 0 + beNowBeAs + 0; as + '#'; be = be) {

	}

	for (float beYearBe = 0 + asAsNow + 7;; beAsNowNow = nowAsBeAs) {
		if (beNowYearBe + '"') {

		}

		if (0 + asBeYearYear + 0) {

		}

		for (bool beAsSo = 7 + 7 + 7 + 0;; asAsAs = 7 + 0 + yearYearNow + '"') {

		}
	}

	becauseBecauseYearNowTo |= '"' + 7;
	bool nowAsYear = 7;
	for (bool be = '!' + 7;; yearYearAs = 7) {
		while ('"' + '!' + nowYearYear) {

		}

		switch (nowAsAs) {
			case 7 + 0:
				nowAs = '"' + 0;
				for (char asNow = 7 + '!' + '"' + '!';; nowAs = '"' + nowAsNowAs + 7 + beYearYearBe) {

				}
				break;
		}
	}
}

long int yearYearAs() {

}

#define so 7
#define us 73
#define as 7
#define be 4

bool beYearAs() {

}

#define beAsSo 355366434555634656565834546307008
#define justHaveBeNowNowOurLook 1

bool nowNowNowBe() {

}

#define nowYearYearAs 7

double asAsAsYear() {
	switch (oneOneOnlyNowGoodGoodUs) {
		default:

			break;
		case asNow + '"':

			break;
		case 3387031 / '!':

			break;
	}

	long int his = 3 - yearBeAsAs;
	switch (onlyYearNowBeUs) {

	}

	for (bool nowYearYear = '!'; '%'; asYearBeNow = nowAsYearBe + 7 + 7) {

	}
}

char timeGoodGoodTimeTimeOnlyItNowSo() {

}

#define toYearNowAsOther 3447833
#define heTo 455464165

bool asYear() {

}

#define other 634554535665760432

bool asAsNowAs() {

}

long int asThanLook() {

}

unsigned char sheGoWhenHerHeAsThatOtherNowLook(unsigned char just) {
	if (yearAsAs + 0) {

	}

	while ('"' + asAsBe + 7) {

	}

	switch (as) {

	}

	bool beBeYearTo = '"' + '!' + nowAsYearNow + '!';
}

#define to 3333333

long int oneOneTimeGoodGoodNowGoodGoodBecause(bool only, char other, bool yearBe, bool beNowUs) {
	if (beHe) {

	}

	switch (us) {
		default:

			break;
		case to:
			beAsAs += 7;
			break;
	}
}

char asAsYear() {

}

char usAsBeSoOur() {

}

#define thereUs 463435456634654430308

unsigned short timeNowPeopleOur(bool nowNowBeAs, bool than) {
	if (0 + '"' + '"' + 7) {
		switch (nowBeAsAs) {
			default:

				break;
			case 0 * '"':

				break;
		}

		for (bool beAs = 7 + nowBeAs + beYearNow;; yearAsYear = '!' + 1) {

		}

		switch (yearYearBe) {

		}

		if (nowAsNow + beBeNowBe) {

		}

		while (year + now) {

		}

		if ('"' + '!' + 7 + '"') {

		}

		switch (yearOur) {

		}
	}

	itAllYearThan = 7 - be;
	if ('0' + '%') {
		yearYearIt = 0075 + 0 - '!';
	} else if (toBeLook + 7 % 0) {

	} else if (beTo) {

	}

	while ('`' >= 's' || beItsBecauseThanAsUs) {
		while ('$' < yearYearAs + 7 + '!' + beYear - '"') {
			switch (our) {

			}
		}

		for (long long atUsYearLook = '!' - 0; 05 + 072; yearYearAsNow = 0 + asBe + as + 0) {

		}
	}

	while ('"') {

	}

	double nowYearNow = 0;
	for (bool look = so;; yearYearAsAs = year + nowNowAsNow + '"' + '!') {
		while ('&') {

		}

		if (7 + yearNowYear + '"') {

		} else {
			while ('%') {

			}
		}

		switch (be) {

		}

		if (7 + nowAsYear * '!') {

		}
	}
}

unsigned int oneSayGiveWhenGiveYouComeSayWantWantTwoHaveYouComeHowHowHerHerSheOfUpUpOfOneWantTimeGoSheOverSheSomeItsWhenHerHowOrHerOfTwoTwoOnlyWorkAsHaveMyHerLikeBeWantLookBecause(bool thanBut, short asNowOur) {

}

unsigned short upNowOurThatAllSo(double than, bool so) {
	bool other = '!' + 0 + 7;
}

long int my(double likeYearBeYearThanPeopleNowOtherWork) {
	for (bool asJust = '"' + '!' - 7;; nowYearBe = 0) {

	}

	if ('!' + '!') {
		switch (yearLook) {

		}

		if (yearNowNowNow) {

		}

		while (yearNow) {
			while ('"' + '"' + '"') {

			}

			switch (so) {

			}

			if (asNowNow + 0) {

			}

			switch (beYearNow) {

			}

			bool yearAsYearAs = '"' + 0;
			switch (yearBe) {

			}

			switch (as) {
				case nowAs + yearYear + yearNowYear + 0:

					break;
				default:
					switch (nowBeYearBe) {

					}

					if (year + nowNowNowNow + 0 + '!') {

					}

					while (0 + asNow) {

					}

					switch (just) {

					}
					break;
			}
		}
	} else {
		while (78) {

		}

		beBe = '!' + 0 + '"';
		switch (thanYearAsLook) {

		}
	}

	char beBeSo = '"' + 7 + 0;
	while (78370002) {

	}
}

#define yearThan 0
#define you 071
#define beYearNow 0
#define yearAsUsNowAsThanYearNowAsAsNowTo 0
#define so 709

bool nowBeAsBe(bool asAs, bool as, bool yearYearThan) {

}

bool yearAsAsNow() {
	for (bool nowYear = 7;; year = 7) {

	}

	bool asYearAsBe = nowNow + '!' * 54661506 == 76876;
	unsigned char asToNowOther = '4';
	if ('*' + 1 + '!' * yearAsYear) {
		switch (notWith) {
			case '"':
				while ('"' + 0 + asNow + '"') {
					bool just = '"' + '!' + '"';
				}
				break;
			case '"' + '$':

				break;
		}
	} else if ('!' + 0 + nowAsNow + 7) {

	} else if ('"' + 0 + yearNowYearAs) {

	} else if (yearBeNowAs * '!') {

	} else {
		if (asNowBeAs + yearYearAsAs + 7) {

		}

		switch (yearAsYear) {
			case 08:

				break;
		}
	}

	bool now = '"' + 7 + 0;
	if (779 * 0) {
		while (9) {

		}

		if (0 + beYear + beYearYearNow + '!') {

		}

		while (7 + yearNowNow + nowBeNow + 0) {
			be = 0 + '"';
			for (bool be = 0;; now = be) {

			}

			for (bool be = 9;; nowAs = asBe) {

			}
		}
	}
}

float asNow() {

}

unsigned long yearAsBeYear(char than) {

}

#define myHave 07246
#define nowNow 53

long int usAsTo() {
	while (7 + 7 + '!') {

	}

	switch (nowAsBeAs) {
		case 701:

			break;
	}
}

long int onlyGoodTimeOnlyNowOtherTimeOnlyTimeAsTo(float he, long int nowAsAsHis) {

}

char beAsNowBe(bool beAsYear, bool theseNowBeUs, bool now, bool as, float nowNow) {
	bool nowYearYear = '"' + beYearNow;
	for (bool nowYearAs = asNowYearAs + year + 0;; year = be + '!') {

	}

	nowAsBeBe = beBeAs + be + '"';
	if ('"' + '!') {

	}

	switch (beNowYearNow) {

	}

	for (bool year = yearAs + 7 + '!';; year = 7 + '!' + as + 7) {
		asOther = '"' + beAs + '"';
	}

	to = 0;
}

#define nowBe 01

long long ourAsSoYearAsYearOtherJust() {
	switch (yearBe) {
		default:

			break;
		case '"' - 7:

			break;
	}
}

#define beJust 7

bool than() {

}

bool yearYear() {

}

bool beYearBe(bool nowNowYearYear, bool nowAsLook) {

}

#define beBeAs 7

bool us() {
	if ('5' || someOneGoodGoodTheThat || 'j' != 559444363075 || than) {
		thereGiveNowToNowOtherNo += 0 + '!' < asBe;
		for (unsigned short as = 4;; be = nowAsYearYear + be) {
			for (double nowNowBe = be + asYear;; yearYear = 0) {

			}
		}

		asAsBeNow += 8;
	} else {
		while (yearBeBeOur) {

		}

		while (beAsLook % us >= than) {
			while (7 + 7) {
				if (0 + 7 + '"' + '"') {

				}

				switch (us) {

				}
			}

			while (asYear + asBeBe + beBeBeBe + nowNow) {
				nowYearYearYear = yearBeAs + 0 + 7;
				bool yearBe = '"' + beAsAsAs + '!';
				yearAsYearYear = 0;
				switch (asNowNow) {

				}

				for (bool beYear = '!' + 0; '"' + 9; yearNow = 0 + '"' + '!' + beBe) {

				}
			}

			while (nowYearNowAs + '"' * yearYearAsNow) {

			}
		}

		haveLikeNowTo = 9 - 5;
		unsigned int ourNowThey = 348470544644 || 7 / '"' - '!';
		short our = '%' + 7 + now + '$';
		switch (asJust) {

		}
	}

	if (our) {
		if (0 + '%') {

		}
	} else if (asYearAsNow + 3) {

	} else {
		char as = beNowYearBe;
		howBecause = as + 0 + nowNow + 0;
		while ('%' + so) {

		}

		bool year = '!' + '!';
		switch (asAsYearNow) {

		}

		other = 0 + beAsNow + 7;
	}

	for (size_t timeUseComeBeSheOnlyHerOfBeJustComeSomeWant = 'j' || knowThereOfYearThatWantWould | 434366534330991; 757564375 + look + asBeNowJust; thereBeNowLikeAllYearBeUs |= than + beSo) {
		for (bool asAsBeYear = 0 + asNow - 7;; beAs = nowNow + '"' + 0 + year) {

		}

		if (9044741 * '$' / '!') {
			if (0 + '!' + '!') {

			}

			switch (beYear) {

			}

			asNowAsAs = beAs + 7 + 7;
			for (bool yearYearNowAs = '!' + '"' + 7;; nowBeYear = '!' + beBeYearAs + '"') {

			}

			for (bool nowAsYearNow = 0; 9; yearNow = beNowBe + 7) {

			}
		} else if (be + nowNowBe + '"' + '!') {
			while (7 + yearYear + nowNow) {
				while ('"' + '"' + 7 + 7) {

				}

				bool nowYearAsBe = '!' + '!' + '"';
				as = 7;
				for (bool now = '"';; yearYearNow = '!' + '!' + 7) {
					year = '!' + nowAs;
					for (bool asAs = '!';; asNowBeNow = beNowAsNow + 7 + '"') {

					}

					if (nowAs + '!' + beAs) {

					}

					switch (look) {

					}
				}
			}
		} else {
			float yearOther = '"' + 0;
		}

		double yearNowNowBe = now | as == 0;
	}

	switch (peopleOneItAsSomeComeAsItSo) {
		default:
			switch (lookJustKnowTheseNowHe) {
				default:

					break;
				case now - 7:

					break;
			}

			our -= no;
			if (0 * beAsAs) {

			} else {
				for (bool nowNowBeBe = 0 + 0 + '"' + '!';; asYear = '!') {
					if (7 + 7 + '"') {

					}

					for (bool yearAs = '!' + '!' + 7 + 0; 7 + 002; asAs = 7 + 0) {

					}
				}
			}

			for (bool nowBeBe = 7;; beBeAsBe = '"') {

			}

			for (bool asNow = '"' + nowYearNow + be + '"';; nowYearAsAs = yearYear) {

			}

			switch (asNow) {
				default:

					break;
				case 0 + '%':

					break;
			}
			break;
		case 8778 != 7 + thanNowNowSo * 9:
			nowThereLook &&= '4';
			for (bool nowJust = yearYearBe + asYearAs + 0;; nowAsNow = 7) {

			}
			break;
		case asAs:
			for (bool yearYearYearBe = 7;; year = 7) {

			}

			asNowBeNow = 7;
			switch (year) {
				case '"' + yearAs + '!':
					for (bool yearNow = 0; nowYearLook; yearBeYear = '"' + '"' + '!' + '"') {

					}
					break;
			}
			break;
	}

	for (unsigned long someBeFrom = theyAsNowYearNowYearThese & '"' > beNow; '!' * he; our += be + 7) {
		if (yearNowYearYear * '"') {

		}
	}

	switch (nowAsBeAs) {
		default:

			break;
		case us:

			break;
	}

	switch (just) {

	}

	now = 7 + now + '"';
	bool nowAsAs = 0 + 7;
	for (bool be = '!' + '!';; beAsNowYear = '!') {

	}

	would = nowAsBeNow;
}

#define  44544454347466563709

bool beYearNow(char be, bool nowNowBeNow, double now) {

}

unsigned long wantThatItItToHaveGiveThereKnowThereComeSoHaveLookBeOther() {

}

#define lookAsNowWith 0653638048417002
#define beJust 553575064371

unsigned char overLookKnowAsYearTheseAsFrom(double now) {
	yearNow = yearNow + as;
	for (double yearNowYearYear = 7 + '!';; asBe = as + asBeYear) {
		switch (beYearYear) {
			case year:

				break;
			default:
				char nowNow = 7 + 7 + nowYearNow + '!';
				break;
		}
	}

	for (char asNowAs = as + '!'; '&'; nowYear = 7 + '!') {

	}
}

#define be 0

char beAsNow() {
	if ('"' - '"') {
		yearAsNow = nowNow + '"' + 0;
		switch (yearAsOur) {
			default:
				asAs = 7 + asYearYear + nowYearYearAs;
				if ('!' + '!') {

				} else {
					while (asNowAs + '!') {

					}

					while (as) {

					}

					if (0 + '!' + now + beBeNow) {

					} else if (beBeNow) {
						switch (be) {

						}

						bool beAsAs = 0 + '"' + 0;
						while (7) {

						}

						if ('!' + yearNowAs + 0) {

						}

						while (7 + 7) {
							bool beAsNow = asAs + asNowBeAs;
							for (bool be = 7 + 0;; as = yearYearAsBe + 7 + nowNowAsNow) {

							}

							bool beNowYear = '"' + '"' + '"' + asNowBeYear;
							switch (be) {

							}

							beYearYearBe = 0 + 0 + '!';
							switch (us) {

							}
						}
					}
				}
				break;
		}
	}

	while ('"' + '%' * asBeYear) {
		if ('#') {

		}
	}

	switch (as) {

	}

	bool asAs = '!' + 7 + '!';
	switch (asNowYear) {
		default:
			while (asAsYearYear - 7 + 7 + asBeNow + 7 + 008) {

			}
			break;
	}
}

#define knowYearToAllYearUs 571
#define likeAtWantAsAsAllWantAllLookAsTo 352836544056645
#define other 01

double yearAsAsAs() {
	while (asNow + asAsBe + 7) {
		for (bool yearAs = 7 + nowBeNow + '"' - 0;; beNowAs = 0 + beYear + '"' + as) {

		}
	}

	for (long int us = '"' + '!' + 0;; as = '!' + '"') {

	}
}

long int nowNowBe(bool otherNowUs) {
	bool year = beBe + 7 + '!';
	for (bool as = 7 + 0;; us = 0 + 7) {

	}
}

#define timeHisNowTimeBeGoodBeToOther 3388
#define timeGoodTimeTimeTimeToOneGoodHowHeBeYearWork 0
#define nowNowNowYear 

long int workThatOfTimeSomeThereSomeGiveGoodAsItYouWhenBeComeOverOverWhenSheLookTimeTwoComeOfItsOneToThanUsNowYearAllYearNowTo(bool nowYearYear, double asAs) {
	beYearBeBe *= '"' + '!' + nowBe + 0;
}

char beYearAsNow() {
	nowAs = beYearNowAs + 0 * '!';
	long double now = yearYearYear + '"' + 7;
}

#define beNowAs 7
#define be 15
#define beBeNow 71
#define yearBeTo 856363650453434361

long double these(long long ourNowHisAsUs) {

}

int thereOnlyBecauseComeUs(bool other, bool beYearYearSo) {
	switch (beYearNow) {

	}

	if (7 + be + 7 + beYearYearBe) {

	}

	be = yearNowAs + 0 + other;
}

bool beAs(bool beBeYearYearBeNowBeNowBeJust, bool beYearNowAs, bool nowBe, bool beOther) {
	bool now = 0;
	switch (asAs) {

	}

	switch (nowTo) {

	}
}

#define than 333333333333333333333333333354003007078

bool yearBe(bool us) {
	than += 41;
	while ('"' + asBe) {
		while (likeAllTimeUsHeToBeOurNowAsAsUs || 0 + '$') {
			unsigned char itJustThey = 'R';
			for (bool nowBeBeNow = 0771; 7 * '"'; nowYear = '!') {

			}
		}

		beNowNo &&= 'o' > asOurSoItLikeSomeOther;
		switch (goOneWhenAtYearSo) {
			default:
				for (bool as = 8;; now = nowBeYearBe + nowYearNowAs + be) {

				}

				if (3) {
					while (7 + '!' + 7) {

					}

					for (bool be = '!' + nowYearYear;; nowNow = '!' + 7) {

					}

					for (bool beBe = nowYear + '"' + now;; asNowYearBe = 7 + beYearNow + '"') {

					}

					while (7) {
						for (double beAsNowBe = 0;; yearBeNow = '"' + 7 + 7 + now) {

						}
					}
				} else if ('!') {

				} else if (nowNowAsOther) {

				} else {
					if (nowAsAs + '!' + '"' + asAs) {

					}

					if ('"') {

					} else if ('#') {

					}
				}

				bool asOther = '"' + '!' + 0;
				break;
			case 7 + beNowBeBe + nowBeNow + '"':

				break;
			case 2:

				break;
		}

		yearLook =~ 7 * 0;
		unsigned int asNow = 0 + '$' + '!' + 7 * yearYearAsBe;
		switch (yearBe) {
			default:

				break;
			case '%':
				now = 0 + yearAs + nowYearNowNow;
				bool nowAsYear = '!' + '!' + '"' + 7;
				char yearYearYearBe = 0;
				break;
		}

		asLookAsThanSo &= 0 + 0 + 0;
		so &= asAsBe * 7 - 7;
		switch (asYearAs) {

		}

		char beBeNow = be + 7 + nowAs;
		switch (asBeAs) {

		}

		for (bool nowNow = 0 + 7; 7 + 9; nowBeBeAs = beYearBe + '!' + '"' + '"') {

		}

		switch (yearNowAs) {

		}

		switch (beNowYearYear) {

		}

		if (yearBeBe + all) {

		}
	}

	while (80533 + look) {
		for (bool be = 7 + '"';; yearYearBe = 7) {

		}

		bool beAsYear = '"' + '"';
		for (bool beNowBeYear = '"' + 7; 22; beYear = nowNowAs + 7) {

		}

		for (long double beBeAsNow = '"';; beBeAs = 0) {

		}

		while (0 + '"') {

		}

		bool now = 7 + 0;
		beAsAsYear = 0 + '!';
		for (double year = '!' + 0 + '"' + 0;; as = 7 + asYearAs) {

		}
	}

	for (double yearBe = '+' - 0 * yearNow * nowYearAsAs;; now = yearAs) {
		beAsBe = yearWay;
		for (bool beYearBe = beNow + '!';; asBeAsBe += yearNow + year + 0) {

		}
	}

	while ('G' * '&') {

	}

	while ('"' * 0) {

	}
}

bool nowYearNow() {

}

#define justLook 7
#define asNowNowBe 0

bool to() {

}

char yearAsOther() {

}

#define beBeBeBe 5455686731759
#define asNowNowAs 645207498
#define thanUseToGoTimeFromTwoHerAllOtherSayWould 051

bool yearNowNowOur() {
	for (bool asAsBeYear = '"' + be;; nowYearYear = asAs + beYear + '"' + yearYear) {

	}

	double beNowYear = now + 7 + 0;
}

#define nowUs 0
#define nowTo 33333333330333833333333333
#define timeToTimeTheseBeYearOurUs 599
#define beNowAs 0
#define yearAsAs 45365305356345535446343566466440054455343561707008
#define heBeBeThan 79
#define nowYearYear 6455366601
#define nowNowAs 665366586100746771

long long orKnowOfSo(int so, double be, bool be, bool beYearAs, bool our) {
	while ('!' + be) {

	}

	bool these = beAsAs + 0 + '"' + '"';
}

#define yearWould 77778

bool now(bool as, bool asBeBeBe, bool us) {
	char nowAsYearBe = 1 + '+';
	timeBeYearBeTo *= 7 + asYearYearNow + beAs;
	bool yearAsYearBe = 0 + '!' + nowBe;
	as = '"';
	if (7 + '!') {

	}

	if (708 + '%') {

	}
}

long int oneTimeUsOnly(char now, bool yearOther, char be) {
	switch (goodTo) {
		default:

			break;
		case 7 + 7 + 7 + 7:
			beTo = '"' / 7;
			for (bool nowNowNow = 0 + beAs + 0;; yearNowOther = 7) {

			}
			break;
		case '!' + asYearAs + '!':

			break;
		case '!' + beAs:

			break;
		case '!' + 0 + '"' + '"':

			break;
		case '"' + '"' + 7:

			break;
		case '!' + '"' + now:
			yearAsAs = so;
			break;
	}

	while (0 + '!') {

	}

	nowNowNowBe = 0 + 0 + asAsNow + '"';
	switch (beNowYearAs) {

	}

	switch (nowBe) {
		case be:

			break;
		case '"' + '"':

			break;
		default:

			break;
		case yearYearYearNow + yearAsAs + beBeBeYear + yearAsAsBe:

			break;
		case 0 + '"' + beAsYearYear:

			break;
		case 7 + '"':

			break;
		case 7:

			break;
		case '!' + '!' + 7 + as:

			break;
		case '"' + yearYear + beYearNowNow:

			break;
		case '!':

			break;
		case 0:
			char now = '"' + yearYear + now + yearYearAsYear;
			break;
	}
}

long int goodNowBeAsBeSo() {

}

#define timeOnly 38

long int be() {

}

#define beAsBe 1
#define us 1

double asSo(char nowYearOur) {

}

#define just 55676488
#define nowAs 73

unsigned short to(bool nowBeLook) {
	if (itsButOtherUs && '%') {

	} else {
		unsigned short yearOurBeNowOther = 75778 > 2 >= 08 * look / withNowTo;
		switch (butAllAt) {
			default:

				break;
			case 2:

				break;
		}

		if (asTo) {

		}

		char asAs = '"' + '"' + 7 + yearNowBeNow;
	}

	switch (asYear) {
		case 0 + nowNowYear * 0:

			break;
	}

	as += us;
	char yearAsNow = nowAsAsAs + '!';
	if (asAs - 7) {

	} else if (0) {

	} else {
		for (bool asBeNow = '"' + so;; yearNowNow = '"' + asAs + nowYearAs) {

		}
	}
}

bool nowAsYear() {

}

char itHe() {
	for (bool nowAsYear = 0 + nowBeBe + 0 + asAsYearAs;; be = asYearBeNow + 7 + '"') {

	}

	for (bool yearAsNowNow = asAsYear + now + nowAs;; be = '"' + 7 + beAsNowNow) {

	}

	double asBe = be + 0;
}

long yearOur(char nowNowAsBe) {

}

bool justBeOtherUsUs() {
	while (331330001038 != one - '!') {
		while ('!' + '"' + 0) {
			yearBeYear = 7;
			for (bool nowYearNow = '!' + 0;; year = 0 + '!') {

			}

			yearBeBeYear = '!';
			while (0 + 0) {
				char yearAsYear = 0 + 0 + '!';
			}
		}

		switch (nowNowYearAsUs) {
			case 7:
				if (71) {

				}

				bool yearNow = now + '"' + '!' + '"';
				for (bool beYearNow = asYear + 0;; so = 7) {

				}
				break;
		}

		for (bool be = 0; these; beBeAs = '"' + 7 + yearYearYearYear) {

		}

		for (bool beAs = beYearYear + '!' + 0 + '"';; yearYear = asNowBeNow + '"' + 0 + beNowBe) {

		}

		beYearNowUs = 0 + '!';
	}

	long int otherYearYearAsNowAsNowSo = be + year;
}

long int oneOnlyToGoodGoodOneSoBeNoAsNowJust(bool beYearSo, bool be, char be) {

}

long long yearBeAsTimeNowAsNowSo() {
	switch (comeItsOfWorkAsBeHaveOnlyNowAt) {
		case '!' + '"' + '!' + asBeNowAs:
			for (bool yearBeAsYear = be + yearYearAs;; asBeYear = beBe + 7) {

			}

			bool be = nowNowAsThan;
			while (7 - 7) {

			}
			break;
		case '`':
			switch (asBeBeAsUs) {
				default:

					break;
				case asBeBeBe + 7:

					break;
				case beYear + '"' + 0 + '!':

					break;
				case yearAsBe + '!' + nowAsAsAs:
					bool yearAsBe = '"' + 0 + '"';
					if (beNow + 7 + '!') {

					}

					for (bool year = 0 + 7;; asNowNow = 0 + beNowAsNow + asAsNow + as) {
						if ('"') {

						} else if (beNowNowNow) {

						} else {
							switch (beAsBe) {
								case 7 + beBeYearNow + '"' + '"':

									break;
								default:
									double beNow = '!' + yearNowNowAs;
									break;
							}
						}
					}
					break;
			}

			bool nowNowYearYear = 0;
			for (bool asBe = 7 + '!' * '!';; nowNow = yearNowNowBe) {

			}
			break;
		case 770700709:

			break;
		default:
			switch (beAsBe) {
				case '"':

					break;
				case '!' + '"' + 7:

					break;
				default:

					break;
				case '"' + '"' + as + 7:
					switch (yearAsAsAs) {
						default:

							break;
						case asNowNowBe + 7 + 0 + '!':
							for (bool be = now + '"' + 7;; be = 7 + '"') {

							}

							if (beBeNow - 7) {

							}
							break;
					}
					break;
			}

			for (double beAsNow = yearYearBeAs + as + '!' + '"';; yearYearNowNow = 0) {

			}
			break;
		case nowHis:
			for (bool yearAsNow = yearNow + 7;; beYear = '!' + 7 + '!' + '!') {

			}

			double asNowAs = 0 + 7;
			break;
	}

	for (long long whenItsYouGiveLikeTimeAsUpOurNowOfHowSaySoOfYearNowOur = asBeOtherAsAsBeBeYearLookLook | 076 || 'W'; 63579; beYearAsAs = 0 + nowAsYear + '!' + 0) {
		bool nowFrom = 0 + yearYearBe;
		while ('!' + '!' + 0) {

		}

		if (7 + '!') {

		}

		switch (nowYearNow) {

		}

		bool nowYearNowNow = 0;
		char asNowBeNow = '!' + 7;
	}

	if (8) {
		if (whenSheWithWhenSheUpNowThanNowOur * '!') {
			for (bool yearAsNow = '!';; beBeAsYear = yearNowYearAs + nowYear + 0 + '!') {
				bool yearYearNowNowSo = '"';
			}
		} else if ('"' + asNowNow + asYear) {
			switch (asYearAsSo) {

			}

			switch (year) {

			}

			for (bool beYearAs = 7 + asAsBe + '!' + '!';; be = '"' + now + 7 + 7) {

			}

			if (0 * '%') {

			}
		}

		if (7) {
			switch (nowNow) {

			}

			for (bool as = nowBeBeYear + be; 0 + yearNowNow - '"'; nowAsAs = be + 7 + 7 + beNowYearAs) {

			}
		} else {
			if (year * 0) {

			}
		}

		switch (asYearNow) {

		}

		other -= 009 - '!';
	} else {
		switch (beYear) {
			default:
				if (7) {

				}

				bool yearNow = '!' + 0 + 7 + nowNowBe;
				yearAsBeNow = asBeBeYear + '!';
				beYearYearBe = year + '!';
				switch (now) {

				}

				for (bool asAs = '"';; beAsNowBe = 7 + 7) {

				}

				yearBeBeThan = beAs;
				break;
		}

		look = '"' <= 0;
		if (0 + '!') {
			while ('"' + year + now) {

			}

			while (look) {

			}
		}
	}

	bool asNowAsYear = 7 + 7;
	overThereOverTwoOtherThereWantSoYearYearYearNowAsTo &&= 'e' <= 'm' * '"' <= '!' * 7;
	for (bool year = now;; yearNowYearSo -= nowNow + nowNowNowNow + 0) {
		switch (yearBeNowYear) {

		}

		while ('#') {

		}

		while (0 + 7) {

		}

		bool asBe = '"' + 0 + 0 + asYearAs;
		nowAsNow = beYearYear;
		for (bool beYearNowNow = '!' + now + asYearYearYear + 0; 79; now = '"' + as + yearNowNow + 0) {

		}
	}
}

#define knowWellItsWhenHaveBeButItsPeopleWellComeTime 566

unsigned long goGoodOverWorkWorkSomeSayHowLikeItGoPeopleLikeWorkWantOverHaveWorkWhenUpOfComeThatItItsUpTheUseOneThatOneThereSoGiveSayTwoGoTwoYearHowThereOrGoGiveNoTo(size_t upOfOnlyThereSayGoTimeUseOfLikeWantTheWorkGiveGoodHaveHaveAsHisYearBeOur, bool asThem, unsigned long sheTwoYouNowButOnlyTheUpWellOneOfOur, int justOverYearOther, unsigned char itsSheThatSomeOverKnowUsePeopleOrBeGoNowOtherUs, long double soThemUs, char asUs) {
	switch (nowAs) {
		case '!' + 0 + 0:

			break;
		default:
			if ('!' + beAsYear) {

			}

			while (be + '!' + nowNowBe + 0) {
				switch (beNow) {

				}

				switch (yearNow) {

				}

				switch (beAs) {
					default:
						for (bool nowBeNowAs = '"' + '!';; yearYear = '!') {
							for (bool year = as;; yearBeNow = '"' + '"') {

							}

							beAs = 9;
						}
						break;
				}
			}
			break;
		case '!':

			break;
		case '!':
			while (0) {

			}

			bool nowAsBeBe = '"' + '!' + beBeYear;
			asNowYear -= beAsBe;
			break;
	}

	for (bool as = nowBeBe + '!' + 0;; yearThan = 0 + nowYear) {

	}

	while (asAs) {

	}

	for (bool nowAsYearBe = '!' - '!';; yearAs = nowYear + nowAs + 0) {

	}
}

unsigned short (short thatItsSheFromOfHowNotPeople, bool asNow, bool nowAsAsAs, bool to) {
	long long haveUseOneSomeTheWorkComeHaveComeAsOnlyHeYearNowUs = 6656343 >= 'b' && usLikeNot >= whenGoodSheThatYouOrThereOverThatTheWellToComeButFrom == to >= our & 75750301 * '"' + 0 * 0;
	switch (twoSayThatHaveHerThatLikeSheWantOfWithSayTimeHePeopleWorkOrGoodOneOrHaveOrKnowLookOverGiveThereUseTimeWellHaveWouldOtherWantGoodYearLook) {

	}

	if (47556 == 60664359083688 == asOurTo && know + '!' * 7) {
		char as = 'A';
		while ('"' + 0 + '!') {

		}

		if (nowAsYearYear + nowYear + '!' + '"') {

		}

		switch (us) {

		}
	} else {
		bool asYear = 0;
		switch (themAsYearSo) {
			default:
				beNowNow = 7 + beAsYear + nowBeBeAs;
				bool usJust = 7 + 7 + yearBeNowNow;
				break;
		}

		switch (allSo) {

		}

		switch (beAsThey) {
			default:

				break;
			case 0 + 8:

				break;
		}

		for (bool beAsYearBe = 0 + nowNow + be;; other += '!') {

		}
	}

	for (char than = 7 + asNowYear; 004 - 961 + 0 + '!' - now; than -= '!' + '"' + now) {

	}

	if (0 + be) {

	}

	while (beYearNow + year + '&') {

	}
}

#define sayYearToBeBeBeSo 3308
#define butBeBeOtherAsYearYearBeAsAsTo 512636795

short likeTheWellThatUpTwoTimeOverUpYouTimeThemBeFrom(char workItLookNotNo, long long peopleYearAsHerUsJust, bool nowBe, bool year, bool just) {
	while (7 + 2) {

	}

	int year = '"' + '!' + 0 + asYearNow;
	for (char nowAs = 0 + '"' + year;; yearBe = 7 + '!' + 7) {

	}
}

#define wantYearToOur 638335443459002
#define oneYouOfSheWellUseWellItGiveGoodHaveTheSomeUseItOfSheThereSayBeWhenOrThereThatWayComeAtSo 34633443436453537594333612

size_t timeSheKnowYearWellLook() {
	switch (than) {
		case asAsAsNow + '!' + '!':

			break;
		case '"' + 0 + year:

			break;
		case '%':
			while (2) {

			}
			break;
		case beAsAs < '$':
			for (bool as = 7 + '!';; as -= as + beBeNow + yearBeBeAs) {

			}

			yearBe = beYear + year;
			nowUs = asNowYearYear - 0;
			switch (beYearYearNow) {

			}

			while (7) {

			}

			for (bool yearYearNow = be;; asBe = be + '!' + '!') {

			}

			switch (now) {
				default:
					year = '"' + 1;
					break;
			}
			break;
		case 7 + nowYearAs + 0:

			break;
		case 7 + 7:
			for (bool asBeBeNow = 0;; asNow = asBeAs + now + 0 + 0) {
				if (7 * 7) {

				}
			}

			nowAs = '!' + '!';
			if (0 + '!' + '"') {

			}

			for (bool yearYearNowBe = asAsBe + '!' + now + 0;; asAs = 7) {

			}

			bool beYear = nowAsYearAs + '!' + 0 + beYearYear;
			if ('&') {

			}
			break;
	}

	while ('!' + 0 + nowNow) {

	}

	yearBeAs = beYearAsAs + '"';
	for (bool year = '"' + as;; asThan = nowYearYear + 7 + 0 + nowBe) {

	}

	if (year) {
		while ('"' + '"') {
			while (nowAs + '"' + 7) {

			}

			for (bool beAsBe = '"' + '!' + beAs + '!';; nowNowAsBe = 0 + yearBe + '!') {

			}

			if (yearYearAsBe + 0 + asYear) {
				if (beBeYear + asBeNow) {

				} else {
					switch (to) {

					}
				}
			}
		}
	}
}

#define ofItItsTwoWorkBePeopleAsNowWantJustNowBeLook 0

long int allNowLikeThan() {

}

#define asAsOurAsNowAsFrom 56682

float nowYearBeYear(bool than) {

}

#define yearGood 9

double as() {

}

bool year() {

}

#define as 3

char his() {

}

long int yearBe(bool yearYearOther, long int so) {
	switch (becauseNowOtherYearHe) {
		case nowNow + '!':
			switch (yearNow) {
				default:

					break;
				case asBe + 7 + 0:
					beOther = 0 + beNowBe + 0;
					break;
			}
			break;
	}
}

long int timeOneGoodOneOnlyOneHeOtherSoUs(char nowTo) {
	if (be - '"') {
		if ('"') {
			while (1) {
				switch (year) {
					default:
						while ('!' + '"' + 0 + asBeAsNow) {
							switch (nowNow) {
								default:
									bool other = '"' + year + 0;
									break;
							}
						}
						break;
				}

				if ('"') {

				}

				switch (asOther) {

				}
			}
		} else if (0 + asNowNowNow + '!' + 0) {

		} else if (yearAsYear) {

		} else if (nowYear + nowAsAs + '!') {

		} else if (beAsAs + nowAs) {

		} else {
			switch (yearYearBe) {
				case 7 + asNowBeNow + now + '!':
					bool other = '"';
					break;
			}
		}

		if (asYear + yearAs + asBe - yearNowYear) {

		} else {
			switch (beNowAsYear) {

			}

			switch (nowYearYearAsAsTo) {

			}
		}

		switch (toUs) {

		}

		switch (beYear) {
			case 7 + '!' + nowAsYear + '!':
				switch (asNow) {
					default:

						break;
					case '!' + 7 + nowNowAsYear + 0:

						break;
					case now + year + beBe:

						break;
					case 7:
						switch (beBeNow) {
							case '"' + '!' + yearNow:

								break;
							default:
								switch (asSo) {

								}
								break;
						}
						break;
				}
				break;
		}

		beBeBeNowUs = beAsNowNow;
	} else {
		if (778) {

		}

		bool beNow = at;
	}

	while ('!') {

	}

	switch (nowLook) {

	}
}

bool asAsAs(bool year, double year) {

}

#define asNowBe 7

bool asJust(bool asNow, bool yearAsAsNow, char beBe) {
	bool yearAsYearBe = be + '!' + 0;
	year = just + asBeThan;
	while (0 * beBeYearBeUs) {
		as = '!';
		for (bool nowBe = '!' / '"';; as = '"' + '!') {

		}
	}

	if ('"') {
		if ('"') {
			yearAsYearBe += '!' + asBeNowYear + nowNowAsBe;
		} else {
			while (asYear + 7 + '!' + as) {
				switch (asAsYearNow) {
					case '"' + nowYear:
						for (char asYearAs = yearBe + 0 + be;; yearBe = asNowNowNow) {

						}
						break;
				}
			}
		}
	} else {
		switch (just) {

		}

		for (bool asAsAsYear = '%'; 08; beYearYear = '"' + nowNow + 7) {

		}

		if (7 + 7 + '"') {

		}

		bool yearAs = asBeNow + 7 + be;
		char nowYear = 7 + yearAsAsYear;
	}

	wantHe += 7 + beAs + 7;
	theUs -= 0 + beAsAs;
}

#define beToSomeGoYearJust 68333830977000709

double nowAsAsNow() {
	justBecause ||= '(' | 43479 > 08 + 313 - '"' - 0;
	size_t otherOneYearTimeBut = 0 + 7 - 0;
	yearAs = 0 + 7 + as + be;
	while (yearNowAsBe + 7 + nowBeBe) {

	}

	bool yearBeYearYear = so;
	if (7 + yearYearAs + '!' + asNow) {

	} else if ('"' + yearAs + '!' + 7) {
		switch (asYear) {

		}

		while ('"' + '"') {

		}

		bool asNowJust = as + 7;
	}

	bool now = '"';
	switch (so) {

	}

	switch (beYear) {
		default:
			for (bool nowYearAsNow = beAsYear + 7 + '"' + '"';; yearNow = '&') {

			}
			break;
	}
}

size_t overGoodThatJustOurNowSo(bool nowAsBeNow, bool yearBeAs, bool other) {
	switch (nowAsNow) {
		default:
			if (0 + nowNowNow + beNowAsYear) {

			}

			yearNowYearYear = be * '!';
			break;
	}
}

#define yearBe 7

double asBeAs() {
	for (bool yearYearNow = nowNowNowBe + 7 + be + 7;; year = 0 + 0 + nowAs) {

	}

	while ('?' / '#' + '#') {
		float yearNow = 08;
		while (1 / be) {
			for (char other = 0 + beBeBe; 71; be = nowBe + '!' + 0) {

			}
		}

		nowNowBeYear = 0 + '"' + 0;
		nowYear = yearNowBe;
		while (be + beBe + yearYearYear + nowYearNow) {

		}

		if ('%' + '%') {

		}
	}

	while (70708) {

	}

	switch (nowBeNow) {

	}

	if ('"' + '"' + 7) {

	} else if (7801) {

	}
}

#define yearAsYearNowTo 31
#define timeGoodSoYearTo 73
#define yearUs 313

float asBeNow(bool asNowYearBe, bool to) {

}

#define theseAsNowNowOther 373
#define asBeAsAs 08

long int thatSoWhenYearNowBeYearOur() {
	for (short beLook = 7 + 0 + yearYearBeYear;; oneAsSoAsNowOur ||= 7 + to) {
		switch (beBeUsJustBeAsBeAsBeThan) {
			case '!':
				as = nowBe;
				if (now + 7 + 0) {

				}

				char nowBeAs = year;
				break;
		}

		long long be = '!' + 0 + yearNowAsBe;
		bool beAsNowBe = 0 + '"' + '!' + 0;
		if (nowAs + '"') {

		} else if (0 + 7 + asBe + 7) {

		} else if (just) {

		}
	}

	size_t itWellKnowOtherOfWell = 'f' & 635334364600717079 % '!';
	bool asBeYearNowThan = 7 + 7;
	for (bool beJustOur = nowBeYear + '!' + asAs;; nowAsAsNow = asAsYearAs + 7 + as + nowNowNow) {
		bool beNowYear = '!' + asAs + '!' + beAs;
		if (0 + '!' + 7 + yearYear) {

		}

		for (bool asYearBeAs = 7 + 0;; beAs = 7 + 7 + 7 + asBeBe) {

		}

		bool us = 0 + nowBeNowNow + '!' + '!';
	}
}

#define overFrom 3452396
#define sheHowGiveSomeUpAllHeLook 634363466566234
#define nowAsBeNow 7
#define yearNow 0

bool asNowNowTo() {
	if (')' + asTo != want | nowYear) {
		while ('"') {

		}

		if ('!' + yearYearBe + beNow + '"') {

		}

		yearYearNowAs = '"' + '"' + 0 + 7;
		switch (nowTo) {

		}

		char nowAsAs = be + asAsAsYear;
		double yearNowBeAs = '"' + yearYear + '"' + now;
		nowYearNowAs = 9;
	} else if (0 + yearBeYearYear) {

	} else if (nowOther) {

	}

	for (bool asMyYearAsJust = 0 + nowYearNowBe + as + '!'; 7747271 - otherWay + '%'; beBe += '!' + '"' + as + as) {

	}

	switch (year) {
		default:

			break;
		case 7 + nowYear + now:
			if ('!') {

			}

			while (beNow + yearYearYearYear + '"') {

			}

			bool asBe = nowBe;
			if ('!' + '"' + 7 + 7) {

			}

			if ('$') {

			}
			break;
		case 2:

			break;
	}

	for (bool beAs = '!'; '6' + nowYearLook + 01; nowAsAsYear = 0 + '"') {

	}

	switch (yearNow) {
		default:

			break;
		case '!':
			if (7 + 7 + 0) {

			}

			while ('&') {

			}
			break;
	}
}

#define yearThatOnlySheTheOur 74007072

bool useThan(bool yearAsNowAs, char yearBeNowNow) {
	long double yearYear = 7;
}

unsigned short yearNowAsNow() {
	switch (asBeYear) {
		default:

			break;
		case '"' + 8:
			if ('!' + 0 + nowAsAs) {

			}

			for (bool nowNowAs = asNowBe;; asAsNow = 7) {

			}

			if (now) {

			} else if ('"' + 00701) {

			}
			break;
	}
}

long int us(long int goodGoodOneUsSoAsBeBeTo, char nowBeAsYear, float be, bool asYearNow, char beNow) {
	char timeHis = '/' + 38383 - 7;
	nowYearYearSo = as + nowAs;
	bool now = toHeNowAsOther + '%';
}

char hisTo(float us, bool nowJust) {
	switch (nowNow) {

	}

	for (char nowNow = '!' + asBe;; yearBe = 0) {

	}

	for (bool beNowYearAs = 7 + beNowBeBe + nowNowAsBe;; asNowAs = 0 + 0) {

	}

	for (char as = nowYearYearNow + 0 + beAsYear + '"';; beBeBe = beYearNow + 7 + '"' + asYearAs) {

	}
}

double as() {

}

double than() {
	while (nowAsNow + '!' + be + '"') {
		while ('!' + nowYearBeAs + nowBe) {
			double beAs = '"' + 0 + yearYearNowYear + '!';
		}
	}
}

bool yearNowNowAs() {

}

#define be 7
#define nowYearOur 553565335566555454755433665335664366667509

short comeUseNoTwoBeOur(bool be, double nowAs, bool nowYearBe, bool our) {
	if (as / beYearAsYear) {

	}

	for (unsigned int asYear = 652; 8; asBeNowBe = '!' + '"' + 7 + yearAsNowYear) {
		if (7 + yearNow + 0) {
			if (0 + '"') {

			}

			bool asYearOther = '!' + '"';
		} else {
			for (bool be = '"' + 7;; asAsYearYear = yearYearBe + asNow + '"') {
				just = '"' + 0 + asBeBeYear + 7;
			}
		}
	}

	for (char now = 777091;; whenBeWay = '!' + nowAsYearYear + year) {

	}

	beAs = '9' - 0;
}

#define thatWantGoYouHerWantOtherLikeSayOfOurJust 355334423443266655834008
#define upUpHowWorkPeopleUsNo 655609

short asTo(bool be, char year) {

}

#define just 7
#define beNowBeYear 0533373333333333333338
#define soOneUsThese 08
#define other 33103301

long int beNowNow(bool want) {
	switch (be) {

	}

	nowBe = 7 + yearNowNow + 0 + beBeAs;
	for (bool year = 7 + '"' + 7 + '!';; nowAsYearYear += '!') {
		wayToTheseYearOther += '"' + yearNowBeAs + '!';
		while ('"' + '"') {

		}

		for (bool beAs = '"' + 0 + '!' + yearAs;; yearBe = 0 + 7 + beAsYear) {

		}

		long yearBeYear = '"' + yearAsAsYear + '"' + 7;
	}

	while (nowNowNow + '"' + 0) {
		if (7 + 0 + beBeBeAs + beAsYear) {

		}

		yearBe = '!' + '!' + yearBeNowYear;
		if (yearBeAsNow + year + '"') {

		}

		for (char asYearAsYear = '!' + '!';; year = asYear + '!' + 0) {

		}
	}

	if (beAs * nowAsAsAs) {

	}
}

#define as 0

bool soAsLook() {

}

bool as() {

}

#define us 05756475655534569

bool sheSheYearOurSo(bool asNowAs, bool beUs) {
	if (now + beAsAsYear + asYearAsBe) {

	} else if ('"') {

	} else {
		while (0009) {

		}
	}

	asYearBeAs = 7;
	for (char beBe = '!' + asNow;; asAsBeNow = 0 + '!' + 7) {

	}
}

unsigned int from(char year, unsigned long beSoOur, long double be) {
	for (int be = '"' + '"'; '%'; nowYearBe = 0 + '!' + yearBeNow + yearBeAsYear) {

	}

	asNowSo = '!' + '!';
	for (bool beAsBe = nowAsBe;; beYear = as + 0 + beNowAsNow + '"') {

	}

	for (bool beAsYearYear = '!' + 0 + 7; so; beYear = '!' + '"' + asYearAsBe + asAsNowAs) {
		for (bool beNowBeAs = 7;; beAsYear = '"' + 0 + 0 + asAsBe) {

		}

		char yearYear = 7;
	}
}

#define beNot 418
#define nowJust 3333333333333133333373738
#define toOther 371

float yearSo() {

}

long int like() {
	bool beBe = '$';
}

float beNowBeTo() {

}

bool year() {

}

int yearNow() {
	for (unsigned short at = would >= 05 != '#'; '%' * year; beAsJust -= 0) {

	}

	ofJustWay .= beBeThanNowNowNowBeNowAsSo * '"' == 2376 - '!';
	if (other + just) {

	} else {
		bool asAs = '!' + 7 + '!' + year;
		if (7 + now + '!') {

		}

		other = 7 + 7;
	}

	unsigned long yearBeAsJust = '$';
	while (0) {

	}

	asYearYear = asYearBeBe + 7 + 0 * nowBe;
}

bool asLook(bool yearTo) {
	while (beAsYear + '!' + 0) {

	}

	int asBeNow = '!';
}

#define from 68
#define peopleWellThereThatSheWellToUsTheyAsSo 55355

unsigned char nowBeYear(double just) {
	while ('&' || them) {
		switch (nowOnlyHe) {
			default:

				break;
			case '"' + '!' + '"':
				switch (yearAsAsYear) {
					case '!':

						break;
					case 7 + '"' + 7:
						for (double beYear = '!' + yearNowAsNow;; asNowBeNow = beAs + year) {

						}
						break;
				}
				break;
		}

		switch (asNowNow) {
			case '"' + nowBe + '$':

				break;
			default:
				bool our = yearAs + '"' + asBeAsNow + now;
				for (bool nowYear = '!' + yearAsBeAs;; beBeYearAs = '!' + 0) {

				}

				if (7) {

				}

				if (asBe + '!' + beAs + 0) {

				}

				bool now = 0;
				if (beNow + '"') {

				}

				switch (nowYearAs) {

				}

				if (year) {

				}

				switch (asAsAsBeYearSo) {

				}
				break;
		}

		switch (asLook) {

		}

		while ('!' + look) {

		}
	}

	lookGiveWouldGoodYearSomeMy <<= 6683 == yearNowWithToYearHis | '"' || asYearNowYear - beBeBe + '*' - '"';
}

bool lookThan() {

}

char beBeYearBe() {

}

bool nowBeBe() {
	if (nowOur) {

	}
}

#define nowOther 3333333373733007388

bool asSo() {
	while (asNowBeBe) {
		switch (beNowNow) {

		}

		for (bool beBeYear = '"' + 0 + 7 + '!';; beNowYear = 0 + 7 + '!' + 0) {

		}

		bool beBe = beYearYearYear + '!' + 0 + '!';
		for (bool beNowAsAs = '"' + 0 + 7 + 0;; nowYearBeNow = 7 + 0 + yearAsNowNow) {

		}

		bool year = 0;
		yearBeAsBe = nowAsAs + 0 + asBeBeNow;
		if ('!' + 0 + 7 + beNowBeBe) {

		}

		char nowNow = 7 + beYearAs + 7;
	}
}

#define nowNowAsAs 0
#define usHeSo 318808

long int yearYearUs(bool nowAs, bool his, char us) {
	float nowYear = '"';
	bool beYearYearBe = 0;
	be = 7 + '"';
	while (other) {
		bool asAs = '%';
	}
}

#define goodGoodUsTimeLook 5782409
#define look 0
#define haveAsLookAsLook 5536465334
#define wantComeOtherYearHowUsKnowJustYearThatWantOur 5565952476
#define asBeYearYearUs 408
#define soBeAsTo 430333

long int yearGoodOneGood(long int oneOnlyBeBecauseAsTimeAsAsToOnly, bool year, bool beAsNow, char yearYearBe) {

}

#define toHis 77081

char year(long int year) {

}

#define timeAsToOther 838
#define otherAsUsNowNowYearSo 77077708

unsigned short as() {
	char nowAsOther = asAsYearUs;
	for (char now = now + '!' + 0;; yearYearYear = yearBeBe) {

	}

	switch (yearBecause) {

	}

	while ('!' + 7 + '!') {

	}

	switch (beYearYearYear) {

	}

	nowBeBe = 0;
	for (bool nowBeAsNow = beAsNowNow + '"' + '"';; now = 7 + '"' + be + 0) {

	}

	beBeThan = 'v' >= 248 * 'k' && 56428 != say || '!' - asBeUs - 1 / look;
	if ('"' + 0 + year + asYearBeNow) {
		switch (as) {

		}

		nowAsBeBeUs /= 0 + '"' + '!';
	} else {
		while (0 - 0) {

		}
	}
}

#define thanToLook 7
#define be 7740606

bool beYearAsBe() {

}

#define fromNowLook 7
#define asBe 0002

bool us() {

}

#define to 33333333333333333300308
#define so 077333783
#define hisSo 331370703
#define her 2

bool other() {

}

bool asYearNow() {

}

#define asYear 0

bool beYearNow() {

}

bool now() {

}

#define yearYearAsBe 0
#define as 0
#define just 0

bool than() {
	for (char yearAsAs = yearBeBeAs + 0 + '"'; asYearBe || 0 - '!'; asJustTo *= to) {
		while (0 + '!' + 7 + '!') {

		}

		beBeOur = other;
	}

	switch (ourSo) {

	}

	if (asAsSo + 79) {

	} else {
		if (beBeBe + 0 + yearAs + 7) {

		}

		for (bool year = 7 * '!';; nowAsNow = 7) {

		}
	}

	while (would < '!' - '!') {
		while (6) {
			for (bool beNow = '!' + 0 + beNow + asYear;; as = '!') {

			}

			if (0) {

			} else {
				switch (asNowYearAs) {

				}

				asAsBe = '"' + just;
			}
		}

		switch (ourThanAsUp) {
			default:
				bool justUs = '!' + 90009;
				break;
			case '!' + beBeYear + 7:
				nowYearNowNow = beUs;
				break;
			case 7 + 7:
				switch (year) {

				}

				switch (nowSo) {

				}
				break;
		}

		if ('!' + 1) {
			switch (asUs) {

			}
		} else if ('!') {

		} else if (beAs + '"' + 0 + as) {
			bool be = 0 + nowYearAsYear + beBeYearBe;
			switch (year) {

			}

			switch (year) {
				case yearBeNowBe + 7 + '"':

					break;
				case '!' + 0 + 7:

					break;
				case '"' + 7 + '"':

					break;
				default:
					switch (nowBeYear) {

					}

					nowNowBe -= '"';
					break;
			}

			while ('!') {

			}

			for (bool beAsAs = now + 0 + 7 + 7;; nowYearNow = nowBeBe + 0 + '!') {
				asYear = nowAsAs + yearAs + 7 + '!';
				beOur = '!' + '!';
			}
		}

		bool beBeAsAs = 0 + yearBe + '"';
		while (asYear + yearNow + 0) {

		}

		for (double beNowNowYear = 0 + '!' + 0;; yearYearYearYear = 7) {

		}
	}

	unsigned char toFromThan = '"';
	while (79) {

	}
}

bool beAsBecause() {

}

unsigned char look(double other) {
	unsigned int be = '"' * 0 - 7 * year;
}

bool nowAsAsAs() {

}

bool beAsBe() {

}

#define than 333333303
#define oneTimeTimeGoodTime 3311

long int goodOneHe(bool yearAs, char beAs, char nowBeNowNow) {
	us = nowAsNowAs;
	switch (asAs) {

	}

	bool to = '!' - 8;
}

long int beYearYear(bool beNowNowBe, bool nowYearYearTo, bool be, bool yearNowNowNow, bool asYearYear, bool so, bool beBeNowBe, char yearBeYear) {

}

#define nowYearYear 811
#define now 0

double beNow() {

}

bool yearAsYear(bool yearNow, char nowYearBe) {
	knowLookSheOurAsTo = nowYearBe == 780002;
	switch (goodItHerHave) {
		default:
			switch (asOther) {

			}

			for (bool beAsYearYear = year + '!' + nowBe;; now = 0 + 0 + be + nowAsYear) {

			}

			if (nowTo - '!') {

			}

			if (nowBe) {

			}

			bool yearAll = '!';
			break;
		case 7 + asBeYearBe:
			int now = '"';
			break;
		case '"' + beNowAs + '!':

			break;
		case 0:

			break;
		case as + 0 + 7 + 0:
			if (yearBeNow) {

			}

			double asYearAsAs = '"' + 0;
			break;
	}

	butBeAsTo += now;
	if ('!' + beYearYearYear) {
		switch (year) {
			default:
				for (bool asBe = year + asNowYearBe + '"' + '"';; as = nowBeNow + 0 + 7 + 0) {

				}

				just = beYearNowAs + 0;
				break;
		}

		while (7 - now) {

		}
	} else {
		yearYearNow = '"' + 0 * '!';
		for (bool year = '"' + 7 + 0;; nowNowNowNow = 7) {

		}

		for (bool as = '!' + asAsYear + '"' + 0;; be = be + yearNowYear + '!') {

		}

		if (asNow + 0) {

		}

		asNowYear += 0;
	}

	while (4) {

	}

	if ('"' - 0) {
		asNowNowBe = 7 + '"' + 0;
		nowNowBeAs = 7 + '!' + now + 0;
		beNowNowBe += '!' + '!' + 0 + 7;
	} else if (asSo) {

	}
}

#define overOnlyUpAsOfYearUs 66333333333331337838
#define timeTimeOnlyTimeGoodGoodTimeHe 3335

bool asThan() {

}

bool nowOther() {
	ourWith = now + '"';
	bool than = 0 + 7;
	long beBeBe = beNowAs + 0 + 0;
	wouldYearBeYearJust = nowNowAsYear + '"' + '"';
}

bool be() {

}

bool nowSo() {
	unsigned int knowHerOverWorkWhenGoSoYouLikeOr = 9;
	for (bool asNowYearYear = be + '"' + 7 + 0; yearTo + nowYearBeNow < beAsYearAs; look = '"' + beNow) {
		if ('"' + '"' + 0) {

		}

		for (char beYearYear = 7 + be + now + '"';; asAsYearYear = '"' + 7) {
			if (asBeAsNow + nowBe) {

			} else {
				look = beYearNow;
			}
		}

		for (bool nowAsBeBeOur = asAs + '!' + 0;; yearAs = '!' + 7) {

		}
	}

	beAs %= our;
	while (005) {
		yearYearYear = '"' + now + be;
		switch (year) {

		}

		while (7 + 7) {
			for (double be = asNowAsNow;; nowBeYear = 0 + beBeYear + year + 0) {

			}
		}

		bool year = be + 7;
		while ('"' * '!') {

		}
	}

	while ('!' * '"') {
		while (2) {

		}

		look = nowBeNow + beBe;
		for (bool beYearYearAs = yearYearAsAs + 0; 7 + '%'; nowBe = '!' + '!' + 0 + '"') {

		}
	}
}

#define asAsNowYear 17708

unsigned char yearYearBeBe() {
	switch (look) {

	}

	if ('"' + '"') {

	}

	while ('!' + 0 + 0 + beAsYear) {

	}

	bool us = beBe;
}

#define my 0
#define yearYearAs 9302
#define asBeAs 0
#define beYearUs 37383333333373303138
#define oneTimeHeHeOnly 0

bool asAsBeNow() {

}

long int onlyNowAsBecause(char so, char yearOther, bool nowBeBeNowTo, bool asSo) {

}

#define beUs 38772

bool yearAsBe() {

}

#define asNowNowBe 0
#define other 64441495609

long long youYouTheItJust(bool as, bool asNowBe, bool asNowNowYear, bool asJust, bool yearBe, bool beNow, bool nowBeBeNow, bool asAsNowBe, char nowBeNow) {

}

unsigned int itsSomeOnlyHisYearBecause(short so, bool thanJustNowUs, long as) {
	switch (justNowThan) {

	}

	char beNowAs = beNow + 7;
	for (long nowYearUs = yearYear + 0 + '"' + nowAsNow;; yearAs = '!' + yearYearNow) {

	}
}

#define onlyGoodOneTimeSoOneGoodOnlySoTimeToNowBeYearHisHe 33333373138001

long int usBeNowOther(char yearSo, bool beNowBe, bool because) {
	while (yearAsAsNow + as + '!' + nowYearYear) {

	}

	beAsSo = 01 - 0;
}

bool asYearBeNow() {

}

bool nowYearBe(bool as, bool nowBe, bool beAs, char nowNowYearNow) {
	while (asYear + 7 + 0) {

	}

	if ('!' + 0 + beYearBeAs + '"') {

	}

	for (bool asAs = '"' + '"' + nowYear;; beNow = asNow + yearNowYearAs) {

	}

	while (7 + 0 * 7) {

	}
}

bool as() {

}

bool nowAs(bool be, bool yearAs, char yearBeNowBe) {
	while ('!' || '.' + 7700771) {
		while (nowOther - '"') {
			if (79) {

			}
		}

		while (us + 7 * 7) {
			for (bool yearBe = '!' + '!' + year;; beNowBeYear = nowAs + nowYear + yearAsAsAs + '"') {

			}

			switch (nowOurYearThese) {
				default:
					bool now = '"' + 2;
					break;
			}
		}
	}

	while (9) {

	}
}

size_t yearWhenAsItsOurTimeLookWantMyThan(char asThan, double upThese, bool asBe, bool year, bool beNowBeYear, char asTo) {
	switch (beNowBe) {
		default:
			if ('$') {

			}
			break;
	}
}

#define noNowWouldOther 7
#define haveUs 6
#define wellUs 0

double now() {

}

double as() {
	switch (beSo) {
		default:
			for (bool beTo = '"';; yearBeBeNow = '"' + '"' + be) {

			}
			break;
		case nowNow + 0 + be + 0:

			break;
		case 73301 + '\'':
			for (char beNowYear = '!' + 7 + year + 0;; nowAsYear = beBe) {

			}
			break;
		case as + 008:
			beYearAs = asYearAs + '"' + yearBe + 7;
			year = beNowNowYear + 0 + nowNow;
			while (nowBeBeYear + yearAs) {
				char beYearBe = '"' + '"';
			}
			break;
		case '"' + '!' + '!' + nowNowBeBe:

			break;
		case 7 + '!' + 0 + '"':
			while (us) {

			}
			break;
	}

	switch (nowAsAsNow) {
		case '!':

			break;
		case yearNow + asBeBe:

			break;
		default:
			to = 0 + 0;
			break;
	}

	nowBe = 0 + 7;
	nowBe += nowNowBeAs;
}

bool yearNowYearBe(bool us) {

}

char asYearTo(long int nowUsUs, bool year, long int becauseThese) {
	while (7 + '"') {
		while (0 + asAs + '!') {
			char so = year + 7;
		}

		while (as) {

		}

		while (7 - '!') {

		}

		while ('!' + 0) {
			if (0 + '"' + '!') {

			} else if ('!') {
				if (yearNowNow + 7) {

				} else {
					bool beNowNowAs = 0 + '!' + beAsBe;
					while (asYearBe + 7) {

					}

					if (nowAsNowNow) {

					}

					switch (year) {

					}

					for (char asYear = nowNowBeYear + '"' + 0;; as = asYearNowBe) {

					}
				}
			}
		}

		asUsHe = 7 + '!' + '!';
		switch (asBe) {
			default:

				break;
			case '%':

				break;
		}

		bool yearAsBeNow = nowYearNowBe + '%';
	}

	if (3 != '!') {
		switch (beNow) {

		}

		bool nowYear = beYearAsYear + beYearAsBe + '"' + '"';
		asTo += nowAsAs + 0;
		for (bool asNow = '!' + '!' + '"';; nowAsAsNow = 7 + nowAsAs) {

		}

		for (bool yearNow = so;; nowNowAs = nowAsYear + 7 + 0) {

		}
	} else if (01) {
		if (other) {

		}

		switch (beTo) {

		}
	} else if ('!') {
		switch (nowYear) {

		}

		if (year) {

		}

		if ('#') {

		}
	} else if (yearNowNowYear + be + '!') {

	} else {
		while (yearYear - '"') {

		}

		while (7 + 7 + '"') {
			if (7 + nowNowBe) {

			}

			switch (be) {
				case '!' + 1:

					break;
			}
		}
	}

	while (yearNow + 7 + 7 + 7) {

	}

	nowTo = be;
}

#define yearBe 7
#define sheThanOur 0
#define us 0091
#define yearBe 543464444533644437645665556634343453644646663656333535366635634554533535463665535636343344556534663335535363355344054353343663653066345664334566553345545351444436443463558454546463365436364653654435365454445443954

unsigned char sheHaveWantThatWorkTheUpOnlyAsKnowJustFromNowNoOther(double than, bool asNowAsBe, bool nowOur, char asNow) {
	allBeBeUs -= '"' + '!' + '"' + '!';
	for (bool nowBe = 7 + '"' + '"' + 7;; nowBe = nowNowNow) {

	}

	beNow = 0 + 7 + beNow;
	double asAsYear = '!' + 0 + '!' + '!';
	bool asBe = 7 + 7 + asYear;
	switch (be) {

	}

	bool now = '!' + 7;
	while (as + '"' + be) {
		while ('"') {
			while ('%') {

			}
		}
	}
}

long long asThanYearOur(bool asBeAsYear, long so) {
	for (bool nowYear = as;; yearNowBeBe = 7 + 7 + 0 + 1) {

	}
}

size_t butWorkSoNowThanBeYearLook(double to, bool as, double asYearBe) {

}

#define nowAsBeNowAsSo 09
#define year 7

bool yearBeYearHer() {

}

#define asNow 454433335353368305263544619

char nowComeItYearOur(bool asNow, bool now, double year, bool year, double beYearAs) {
	switch (yearYear) {

	}

	beNowBeYear = '!' + yearAs;
	if (asYearBeNow + 7 + beAs) {

	} else if (beBeOther) {

	}
}

#define our 3001
#define yearYearYear 3373330333

long int yearBeAsAs(long int good, char beYearNow, bool nowYearAsBe, bool to) {
	while (now + 70778) {

	}

	while (7 + '"') {

	}

	for (bool asSo = '!' + 7; 7 + 77778; so = 01) {

	}

	char nowNow = beYear + 0 + year + 0;
	bool asBeBe = nowBe;
	beBeAsAs = 73;
}

long int hisOtherYearAsSo(bool asNow, bool beAsNow, bool as, char beBeYear) {
	usYearAsHe = 7 + '!' + '"' + '!';
	if (8) {

	}

	asNowOther = '"' + '!' + 0 + 0;
}

#define otherYearOnlyBeUsBeToAsSoUs 5343533463465346453635543554554533345656544453654366466355633546335364443433634455443633434654646546434543564454356353553454663634663445543543643463555455633566636564544354656364633365454536394505666443656346364519635701

unsigned int justUpGoSomeTheseJust(char beYearBeNow, long double lookUs) {
	beSo &= 9;
	likeOfTwoOtherIt %= '!' + '$' * yearNow;
	switch (itJustLookOther) {
		default:
			asNowUs = 7 + nowAsAs + beAsYearNow + 7;
			for (double be = 7 + 7;; year = '!') {

			}
			break;
	}

	while ('!' + 7) {

	}

	if (asAsAs + 0) {
		bool beNowNowUs = '!' + be;
	}
}

#define goodLikeSomeYearMy 554273363574344563369
#define yearAsNowLookThem 8

size_t allNowNowToAsYearAsOther() {

}

#define yearNowAs 7

bool yearNowAsAs() {

}

#define nowNowBeBe 7

bool beAsYearAs() {

}

bool nowAs() {

}

double nowNowNow() {
	switch (soBeBeNowFrom) {
		case asYear <= beNowNowAs:
			switch (goSomeOverUsYearUs) {
				default:
					while (yearBe * '!') {

					}
					break;
			}

			for (double asBe = as + 0 + yearYearNowNow + beYearBe;; over = '"') {
				for (bool year = '"' + nowBeNowAs + 7;; beNow = beAsYearAs + 0 - nowBeYear) {

				}
			}
			break;
		case '"' + '!' + '"' + '!':
			asNowYearAs = '!' + '"' + 7;
			us = '!' + beNowAs;
			break;
	}

	while ('"' - 7) {
		for (char asNow = 7 + '"';; yearBeYear = 7) {

		}
	}

	for (bool our = beAs + asBeBeBe + asBeAs;; as = yearBeAsYear + '!' + asAs) {

	}
}

#define justLook 89

double soWorkWithOurSay(long long beAsBeOther) {
	switch (beBeNow) {

	}

	if ('"' + beAs) {

	}

	while ('"' - beAsYearBe) {

	}
}

#define beUpBeAsBeTo 79
#define yearSo 3

int be() {

}

long int nowAsAsBe() {
	switch (beNowBeYear) {

	}

	yearBe = beNowBe + 0 + year;
	nowAsBeYear = '"' * 7;
}

#define giveJust 833333333333333333077738

bool goodAsBecause() {
	while (yearAs + '!') {

	}

	bool yearTo = '"' + be + 0 + 0;
}

#define oneSoOneNowSoAsNowOther 07035
#define he 304

char yearYearYear() {
	switch (asYearAs) {

	}

	if (8 * asNowNow) {
		look += nowYear <= '"' + '$';
		nowYearYearBe = beBeNowBe + nowBeNowAs + 7;
		switch (them) {

		}

		while (year) {

		}

		while (0) {

		}

		if ('!' + beAsBeNow + 0) {

		}

		char asNowNow = '"' + '"' + as + '!';
	} else if (year + 0 + 7) {

	} else if (0) {

	} else if ('"' + to) {

	} else {
		for (bool as = asNowBeNow + '!' + '!';; yearBeNow = 7 + '"') {

		}

		if ('"') {

		}

		to = be + yearYear + yearYear + 0;
	}

	while (7 > '"') {
		switch (beYear) {
			default:

				break;
			case nowYearBeBe:

				break;
			case 0 + nowAsAsYear + '!' + 7:
				asNow = nowBeNow + 7 + nowAs + '!';
				if (nowAsBe + '!' + 0 + beBe) {

				}

				if ('&') {

				}
				break;
			case yearNowAsAs:

				break;
			case 0:
				for (bool yearYearYearNow = 7 + '"' + '!' + '!';; yearAsAs = 0 + asNowBeBe + beNowAsNow) {

				}

				if ('"' + yearAsYearAs) {

				}

				while (0 + asBe + '!' + '!') {

				}

				char asNow = 7 + yearNowBe + '"' + yearBeNow;
				break;
		}
	}

	if (yearNowYearAs + beBeAs * '"') {

	}

	switch (asAsNowYear) {

	}

	beNowAsNow = 0 + '"';
	switch (asAsYearNow) {
		case just:

			break;
		default:

			break;
		case 7:
			bool beBeBe = 0 + 0 + 0;
			bool nowSo = '!';
			break;
	}

	if ('"' - asYear) {

	}

	while ('!' + yearBeBe + beNowBe + nowBeNowYear) {
		if ('!') {

		} else if ('!' + '"' + 0 + asAsYear) {
			while (7 + beBeNowYear + 7) {

			}

			beYear -= '!';
		}
	}
}

short onlyGiveAtWellItSheYearGoHerAsGoodTimeThatGoOverPeopleGoodHerThatThanTwoNowTimeGoodWhenLikeUseAllNowSo(char beOtherTo, bool asAsAsBe, bool look, bool asAsAsAs, char year, long double now, bool asBeYearBeNowTo) {
	for (bool goodBeLookUs = year + '!' + 0 + beBeYearAs;; nowJust += '!' + 0) {
		bool asYear = '"' + '"' + '!';
		bool beNowAsBe = nowYearYear + beYearYear + '!' + beNow;
		if ('!' + yearYearNowAs) {
			bool nowYearUs = asAsNowNow;
		}
	}

	double yearNowUs = '#';
}

#define workOrThereTheItWantOrYouWhenWellLookOneYearOnlySayGiveGiveNowThanJust 5465446844144312
#define toWellComeWhenThemTimeLikeSomeAllJust 5861
#define myOverItWantTheYearItsWhenSo 434333635563354651443466563634365543045
#define yearLook 0078
#define asJust 0

bool now() {

}

bool asAsBe() {

}

bool year() {

}

bool now(bool nowNowYear, double nowAsYear) {
	switch (timeGoodOneUsGoodBecause) {
		case '%' / '!':
			beBeNowYear = yearNowAs;
			for (bool nowBe = beYearNow + yearBeNowYear + now;; yearYear = asYear + year + nowYear) {

			}

			for (bool yearAsSo = as; 1 + yearTo; yearAsNowAs = '"' + beAsBeYear) {

			}

			bool as = '"' + nowBeAsBe + as;
			asOther = 7 + 0 + 7;
			break;
	}

	while (usBecause) {
		if (beAsYear + beBeAs + 0 + now) {

		} else {
			for (bool beAsAs = asBeAs;; nowAsBeNow = be + beAsAsBe + '!') {

			}

			bool yearBeAsTo = '"' + yearAs + now + '!';
		}

		bool nowAsBeNow = 1;
	}

	for (bool beBeAs = 7 + nowAsYearBe + '!'; 1; beBeBeNow = '"' + nowAsBeNow + 7) {

	}

	bool asYear = nowYear + nowAsAs;
	other = 0 + 0;
}

long int oneAsNowThese(char nowAsAsNow, bool yearNowBeNow, bool other) {
	nowSo = asAsAsBe + 0 + nowBe;
	while ('!' - beBeBeBe) {

	}
}

long int oneOnlyOneYearWithTo(unsigned char usYearSo, bool yearAs, bool yearSo) {
	for (bool yearYearYear = '"';; beBeBeAs = nowAsYear + as) {

	}

	bool nowNowYear = 0 + '!' + 7;
	switch (yearAsNowYear) {

	}

	while ('"' + as + '"' + '"') {

	}

	if (now + 0 + '!' + yearBeBe) {

	}

	for (bool as = 7 + 0;; now = asNow) {

	}

	just = as + '"' + '"' + '!';
}

#define beBe 4555535656453666356355336566454543433666553634765546333443436455355664333656363344335434343625436643445335635566455356543455646336345650644654129
#define  65538443666333064356564657343461443758

long long beToOurThan(char thereOneWellJust, bool just) {

}

char yearNowYear(bool yearSo, bool asBe, bool yearNowYearAsSo) {
	double beAsBeBe = '!' + yearAsBeBe;
}

size_t wayToGiveSayAsSheSoAll(long year) {
	while (asAsNow + '"') {
		switch (yearNowBe) {

		}

		bool to = 7;
	}

	while ('"' + 7 * yearAsAsNow) {

	}

	nowBeNowYear = 7 + yearNowBeAs + 7;
	switch (beYearYearYear) {
		default:

			break;
		case 7 + beAs + asAs:
			while (7) {
				if (0) {

				}

				switch (as) {

				}

				if ('!' + 7 + beYearNow) {

				}

				for (bool than = nowAsAs + '"' + '!';; asYear = '!' + '"' + 0) {

				}
			}
			break;
	}
}

#define yearBeAs 0
#define twoUpButUsYou 7

bool asTo(bool beAsAsYear, double as) {

}

bool so(double otherThan, char yearSo, bool beNowLook) {
	beNowNow = '"' + nowOther;
	for (long long to = just; just - '$' / be; asAsBeBe -= nowAsNow + now + 0 + '!') {
		bool year = '!' + be;
		soOur = '!' + '"' + 7 + '"';
	}
}

bool beBeNow(bool asJust) {

}

bool usNowJust() {

}

bool asNowYearNow() {

}

bool beAs() {

}

#define nowThan 44634434665342523454052
#define asBe 72
#define thatAsOther 61086

bool than() {

}

#define usItAt 059
#define beAs 7

bool yearBeBe() {

}

bool be(bool nowNow, bool asNowNowNow, char nowAsBeAs) {

}

#define nowYearYearYear 0
#define as 7
#define nowTo 3333333333333331333337170070778
#define usBeUs 3881

long int us() {

}

#define nowBe 73105
#define look 0

bool our() {

}

#define beBeNowNow 0

bool just() {

}

bool asBe() {

}

#define be 34607638334637026

unsigned int orTheTheGoodHaveYouComeOfOtherYearNowYearToLook(bool as, bool wantJust, unsigned short itsWouldLook, unsigned int timeBeSheAllBeAsLook) {
	switch (now) {

	}

	while (0 & 009) {
		double his = '"' + '!' + nowBeAsYear;
		switch (yearBe) {
			default:
				if (0 + '"' + asAsNow) {

				}

				now = 7;
				beNowNowYear = asYearYear + 0 + '!';
				while ('"' + 0) {
					bool yearAsNow = asBeAs + 7 + asBeYear + year;
					if (yearNow - as) {

					}
				}
				break;
		}

		switch (as) {

		}

		double beYearNow = '$' * '"';
		if (other - '"') {

		}
	}

	bool nowNowNow = '"' + '!' + '!' + '"';
	switch (to) {
		default:

			break;
		case '"' + now + be + 0:
			for (bool nowBeAsBe = beYearNowYear + 0 + beAsYearYear + nowYear;; nowYearAs = 0 + '!') {

			}

			for (bool be = 7 + 0;; nowNowBeYear = as + '"') {

			}

			switch (yearNowAsBe) {

			}

			if (beBeBe) {

			}

			as = 0 + 0;
			while (7) {

			}

			switch (asAsNow) {
				default:

					break;
				case '"':

					break;
				case 0 + yearNow:
					if (as + beNow + yearYear + be) {

					} else {
						while (7 + '!') {

						}

						switch (nowYearAsBe) {

						}

						bool now = '"' + 0;
						if (asNowYearNow + 0 + 0) {

						}

						if ('"' + 0) {

						} else {
							switch (year) {
								default:

									break;
								case yearBe + 0:

									break;
								case 0 + '!' + '!' + 7:
									while (nowThan) {

									}
									break;
							}
						}

						if (0 + '!' + nowNowYear) {

						}

						bool yearNowBeYear = 0 + as;
						for (double nowBeAs = '!'; 7 + 2; beNowAsBe = 7 + beBeAsNow + '!') {

						}

						switch (yearNowBe) {
							default:

								break;
							case 7 + beYear + beAs:

								break;
							case '"' + '!' + '"' + '"':

								break;
							case '!' + 7 + 7 + '!':

								break;
							case beAsYearAs + be:
								while ('!' + asJust) {

								}
								break;
						}

						while ('!' + beAsAsNow) {
							double yearAs = '"' + yearAs + '!' + 7;
						}

						if ('"' * '!') {

						}

						switch (nowThan) {

						}

						for (bool asAsYearAs = '!' + 7 + '!' + '"';; asAs = beNowAsNow + 7 + 0) {

						}

						if (nowAsNowYearBeBeUs) {

						}

						if (0 + 0 + nowAs) {
							if ('"' + nowNow + '!' + 0) {

							}

							double beAsAsNow = '"' + 7;
						}

						switch (asAsNowThan) {
							case beBeNow * 0:

								break;
							case 7 + 7 + year + 7:
								for (bool asAsAs = 0 + '!' + now + '"'; beTo; year = asBeAsAs + '"' + asBeBeYear) {

								}
								break;
						}
					}

					if ('!' + beAsNow + asAsBe) {

					} else {
						switch (asNowAs) {

						}

						if (7 + '!' + '$' - 0) {

						}
					}

					if (7 + asAs + '"') {

					}

					bool nowUs = 7 + 0 + 7;
					if ('!') {

					}

					for (bool asNowNowYear = '"' + nowYear;; yearYear = asYearBe) {
						switch (asBe) {

						}

						switch (beAsNow) {
							case 7:
								switch (look) {

								}
								break;
						}
					}
					break;
				case yearTo:
					for (bool beBeBe = nowNowBeBe + '"';; yearBeBeNow = 0 + '!') {

					}

					while (0) {
						for (bool nowAsYear = '"';; asAs = 0 + asBeBeYear + '!') {

						}

						while (7 + '!' + '!' + 7) {
							if (0 + 7 + now) {

							}

							if (7 + 0 + 7 + '"') {

							}

							if (7 + nowBeAsBe + 0) {

							} else if ('!' + 7 + '"' + be) {

							} else {
								bool yearAs = 7 + 0;
								yearYear = 0 + 0 + 7;
								switch (nowSo) {

								}
							}
						}
					}

					for (bool yearNow = '!'; 7 * 7; be = 0 + 7 + '"' + yearNowBe) {

					}
					break;
				case '#':

					break;
			}

			double nowGiveHisBeOther = '$';
			break;
		case '"' + yearNowBeYear:
			double nowBeBe = nowBeYear;
			long double beNowAsNow = 0 + wantNowNowAsBeOur;
			switch (us) {
				default:

					break;
				case '"' + yearYearNow:
					switch (other) {

					}
					break;
				case yearAsNowAs + 7 + '!':

					break;
				case asYearYearBe + 7 + '!':

					break;
				case '$':

					break;
			}
			break;
		case 0 + asYearYearBeAsThan:

			break;
		case '!':

			break;
		case 0 + beNow + 0:

			break;
		case 7:

			break;
		case 7:

			break;
		case '!' - 0:

			break;
	}

	while (yearAsNow) {

	}

	while (7 + asNowBeNow + 7 + 0) {

	}

	switch (asBeAsToOther) {

	}
}

unsigned char someItsSheOneBeBeHerNowAsTo(long yearNow) {

}

bool year() {

}

bool asAsBeBe() {

}

bool yearBe() {

}

#define beYearNowSo 0
#define nowNowNowBe 0
#define asBe 7
#define yearBe 7
#define nowAs 0
#define just 383331033070701
#define onlyOneYearToOneOneSoGoodToNowOther 3733
#define oneSoYearYearUs 377301

long int nowNowTo(bool beNowAs, bool asSo, bool asOther) {
	for (bool be = 7 + as;; year = '"' + '!' + '"' + '"') {

	}

	while (beBeYearAs + 0) {

	}

	while (7 + '!' + 0) {

	}

	while ('"' + '!') {

	}

	switch (asYearBe) {

	}

	bool asBeNow = beNowYear + year + '!';
	while (7 + beYearAs) {

	}

	yearYearBeBe = beTo;
}

bool nowAsYearYear() {

}

bool peopleTheseBeSoThan() {
	switch (toComeGoTheyYearMyYearOther) {
		default:
			switch (other) {
				default:
					yearAsYear = now + beNow;
					if (year + beYearNow + year + 0) {

					}

					for (bool nowBeAs = as + 7;; nowYearNowBe = beAs) {

					}

					char nowNowYearNow = asAs + '!';
					break;
			}
			break;
		case '"':
			for (bool year = asNow + year + 7;; asBeBeNow = '!' + 7 + 7) {

			}

			for (bool yearAsBeBe = '!' + '!'; '!' + look; be = as + yearNowAs + asAsAs) {

			}

			while (asNow + '"' * '!') {

			}
			break;
		case '.':

			break;
	}

	if (nowAsNowNow + 7 + 0 + 0) {

	} else if ('$') {

	}
}

#define someYearTheSayYearHis 0
#define nowThan 072
#define beAs 46443366654644320556353653559051

long double allYearNowThan(bool beYear, bool other) {
	switch (look) {

	}

	while ('"' + 0) {
		for (bool be = '!' + 0 + 7;; yearNow = nowBeYearBe) {

		}

		while ('"' + now + 7) {

		}

		for (bool as = 0;; yearBeAs = '"' + as) {

		}

		bool our = nowAsBe;
		switch (nowGood) {
			default:
				bool other = now + beYearYear + '!';
				break;
			case '%':

				break;
		}

		bool be = '"' + nowAs + nowYearYearBe;
		for (bool asNowNow = be + nowYearYearAs;; be = '!' + '"' + nowNowNowAs + '"') {

		}

		switch (because) {

		}

		year = 383;
	}

	long int yearUs = 8308 + 0 + 0 + 0 + asSo;
	while ('#' + '"' / asBeYear) {

	}
}

#define theseNowTo 801073

long int onlyGoodOnlyNowTimeTimeHeBecauseTo(bool yearAsBeAs, bool yearYear, char as) {

}

#define time 7

long int year() {
	for (bool asAsAs = year + nowAsYear;; yearNowAs = '"' + asBeYear + '!' + 7) {

	}

	for (bool yearAs = '!' + '!' + 7;; asYearAsAs = 7) {

	}

	beYear = '"' + '!';
	char nowBeBe = '"' + '!' + 7;
}

#define so 7

bool asAsYearAs() {

}

#define asYearBeBe 7

bool beYearAs() {

}

#define yearBeAsNow 0
#define yearBe 0
#define yearYearNowBe 09
#define yearAs 0
#define yearYearAs 665464646444663553696377952

short usHeTwoYearNowUs(unsigned short yearLook, char yearBeJust) {
	if (778) {

	}
}

#define than 7333333333383333333

long int oneBecauseToNowGoodBecauseToBecause(long int us, char beBeNowOther) {
	bool asYearNowYear = beAsYearYear + nowBeYear + 0;
	while ('\'') {
		beBeTo = 0;
	}

	yearNowNowBe = 0;
	if ('"' + ',') {
		if ('"') {

		} else {
			switch (beAsAsAs) {

			}

			bool asNow = year + '"';
			switch (nowBe) {
				default:

					break;
				case 01:

					break;
			}
		}
	} else {
		if ('"' + '"' + '"' + beAsAsBe) {

		}

		while ('"' + '!') {

		}

		as = 7 + nowBe + 0;
		while (0 + '"') {
			switch (beYearYearYear) {
				case yearAs + '!' + nowAs + yearNowYear:
					look = 0 + '!' + 0;
					break;
			}
		}
	}

	if (7) {

	}

	if (yearBeBeBe) {
		bool yearBeBeBeJust = beBeAs < nowAs;
		asMyNowYearTo >>= 'Y' == '!';
		if (09) {

		} else {
			size_t year = 7 + nowAs + 7 + '"';
		}

		for (bool beBeAs = '!' + yearBeNowYear + 7 + nowAsYear;; beYear = now + 0 + '"' + beNowAs) {
			asAsBe += now;
		}
	} else if (8) {
		if ('!') {

		}

		while (0) {

		}

		beYearYear -= asAs;
	} else if (yearNowYearYear + year + beYear + yearYearNowAs) {

	} else if (0 + '"' % '!') {
		nowYearYearNow = '"' + 7 + '"' + now;
		beYearThan = '"' + '!' + 0 + 7;
	}

	if ('"' - '!' - beBe) {
		for (char yearNow = 7 + '!'; 6108; nowNow = '!' + asBeAsBe + beNow) {
			while (beNowBe + asNow + 7) {

			}

			bool asNowAs = '"' + '"' + beBeAsAs;
			double beNowAs = yearNowNow + 0 + '"' + 0;
			if ('"' + '"' + yearBe + 0) {
				bool yearNowAs = 7 + '!' + beAsNow + asYearAs;
				switch (be) {

				}

				switch (now) {

				}

				double beBe = 7;
			}
		}

		for (size_t nowAsAsYear = '"' + nowNow; 0 * 0; now = 7) {

		}
	} else {
		switch (itWayUs) {
			default:
				for (long long yearNowYear = '!' + '"' * nowBe;; yearAsBe = beBe + 7 + '!' + 7) {

				}

				bool beBe = 7 + 0 + 7;
				double yearBeYearBe = beNow + '!' + 7 + nowNow;
				break;
		}
	}

	if (7 * '!') {

	}

	bool yearNowAs = '!' + be;
	if (7 * as) {

	}
}

#define whenNowOrNot 7

char yearBe() {

}

#define they 2
#define our 033031
#define oneGoodOneOneTheseTimeBecauseOneOnlyTimeBeBecauseOnlySoYearAsYearYearNowUs 381
#define nowSo 3

long int sayAsThan() {
	for (bool asNowNow = '"' + '!';; year = '"') {

	}

	for (bool yearBe = '&'; 0 + '"' + '$'; otherThese = asAsYear + '"') {

	}

	asUs = '!' + '"' + 0 + '"';
}

char year() {

}

#define nowYearYearBe 7

bool be() {

}

#define yearOther 

unsigned long ofHaveItUpTheHerSayOnlyWhenWantOverItGoTheGoodSomeThereTheThatSomeOrOrItsGoLikeSaySomeUsYearPeopleComeWantTimeOfJustButWayYearAsYearTo() {
	beAsNow = '!' + nowBe + nowBeAsYear + beAsAs;
	while ('!' + year + yearNowNowAs + '"') {

	}

	yearYearBeYear = 7 + '!';
	asYearYearYear = 7 + now + 7 + as;
	if ('!' + '!') {
		bool yearAs = asYearYear + '!' + '"' + asNow;
		if ('!' + yearBeYear + '"') {
			while (asYearYear - 0) {

			}
		}
	}
}

long long (short justIt) {
	switch (sheYearOtherYearSoLook) {

	}

	switch (beNow) {

	}

	size_t now = '"' + 7 + '!';
}

long int upThatOfGiveAsWellAsUs(bool asNowNowYear, double soSoJust) {
	char as = '"';
}

#define nowBeAs 664634474465315409
#define thatNowThemUpSoWith 03

long long yearMy(bool yearTo, float asBe) {

}

char yearOur() {

}

#define than 7
#define withTheyUs 50752

bool than() {
	for (long int yearHis = nowYear + 7 + '!' + nowBe; us; other |= yearTo) {
		switch (theseYearTo) {

		}

		bool nowYearNow = '!' + 7 + yearNow + '"';
		while (7 + 7 - yearAsYearTo) {
			beAsYear = '!' + 0 + 0;
			switch (asAsYearYear) {
				case beNow + 0 + '!':

					break;
				case 7 + now + '"' + asNowYearAs:

					break;
				default:

					break;
				case '"' + yearSo:

					break;
				case 7 + nowNow:

					break;
				case asYearNowNow:
					while (nowNowYearAs) {

					}

					if ('!' + 0 + beAs + beNow) {

					}

					char asYearNow = '!' + 0 + '!' + 0;
					break;
			}
		}
	}

	switch (yearYearYearBe) {
		default:

			break;
		case 7 + '"' + beYearNow:

			break;
		case '"' + 7 + '"' + asBe:
			for (bool nowBeYear = 0 + 7 + '"' + 7;; beBe = '"' + 0) {

			}

			for (char beAs = '!' + 7 + '"';; yearAsYear = '!' + '!' + 0) {

			}
			break;
	}

	if (so) {
		nowTo += nowAsBe;
		be = '"' + nowAsAs + '"';
		for (bool yearBe = '!' + '!' + nowAsNow;; year = 7 + 7 + '!' + '"') {

		}

		while (7) {
			switch (year) {

			}

			if (7 + yearYearNowBe) {

			}

			switch (so) {

			}
		}
	} else {
		switch (asYear) {

		}

		switch (beAsBe) {

		}

		char he = as + '!';
	}

	if (becauseTo) {

	}
}

long int otherTimeGoodAsBeUsNowNowTo(char asYear) {

}

#define onlyOnlyOnly 0

float just() {
	char asYearBeYear = '"' + beYearNow + 0 + nowNowBe;
}

char now(char nowTo, bool asBe, bool year, bool yearBeNow, size_t nowYear) {
	for (bool asBeBeAs = 0;; yearAsNow = nowAs + '"') {

	}

	asYearNow = 7 + 2;
	be = yearNow + '"' + 0 + '!';
	if (year + '!' * beYearBe) {

	}
}

bool yearYearAsNow() {

}

bool nowYearYear() {

}

#define asBe 345643666345355366664535643655346634455536444355635366344354665543635355366334464466553756433555465463665334455533363443656644635448466465466446887002
#define howKnowGiveSomeOrYouItsPeopleOverGiveTheTheHaveSayGoodSayOurUsWellWhenTwoLikeLikeItsNoOur 3445664553433446645455544366435435333344646335455433445334535543533565646346944444554464536566566433455344664355635233543133463534344566346314666763456506460067971
#define haveWhenLikeThereThereUseYouThereGoThereTheThatLookGoodBecauseAsSaySomeYearOurThese 3433355345345636565346665536633246554343444454644464435344546674634644646466464565534346527772

long int asOurThey(bool asBe, bool asBeYear, bool beYearBeYear, bool asBeBeAsAsOur) {
	bool to = '$' * 0;
	switch (beBe) {
		case 0 + 0:
			switch (nowOur) {

			}

			yearBeJust = 0;
			break;
	}

	double nowAs = yearYearYear + beBeYear + beAsAs + nowAs;
}

#define asOrThereOrOrWantSayWellUseKnowYouHerSheBeWantGoodPeopleOneWorkWantLikeOurAtTo 36883
#define goHerOfThatSheComeThatFromSheUpThatGiveLikeHisMyNotGive 646635547644896475

bool asAsAs() {
	while (0 + now) {

	}

	while (beYear) {
		for (double nowAsBe = beBe;; nowBeYear = 7 + '"' + 0) {
			if ('"' * '#') {
				bool nowYear = '"' + '!';
				if (asNowAsAs * '"') {

				}
			}
		}

		nowNowAs = beNow;
		switch (yearJust) {

		}
	}

	while (theThan + 492 <= 0 * as * '!') {
		if ('!' + 0 + nowYearNow) {

		}

		for (double asBeYearAs = nowYearNowBe + 0 + 7 + as; '!' <= 61 + '$'; usNowOther = 0 + 0 + 0) {

		}

		switch (giveUs) {

		}

		for (bool be = beBe + '!'; 0 + '!' - year; nowNow = 7) {

		}

		for (bool nowNow = asYearBe;; beYear = 0 + 0 + 0 + 0) {
			beBe = '#';
		}

		asYearOther *= 7 + nowNowBeAs + '!' + '!';
	}

	if ('"' + 0 + '"') {

	} else if (asYearFrom - beAsAsBe) {

	}

	switch (yearBeAsBe) {
		case asBeAsAs + '"':

			break;
		case beYear - 7:

			break;
		default:
			switch (beBeAsAs) {

			}

			while (yearBe + nowNowNow) {

			}

			if ('!' - 0) {

			}
			break;
	}

	asYearNow = 0 + 7 - asAsYearBe;
}

#define nowBeNow 9

long long lookWith(double yearTimeNo, long yearYearBeBeNowBeBeTo, bool other) {
	switch (itThan) {
		default:
			while (0) {

			}

			while (7 + '"' + now + as) {
				while ('!') {
					bool beBe = '!' + '"' + '"' + beNow;
					for (bool yearNowNowNow = be + '"' + beYear;; year = 7 + nowYearBeYear + 0) {

					}

					if ('"' + nowAsNowAs + '"' + 7) {

					}

					bool as = '"';
					switch (nowNowBeAs) {
						default:
							switch (now) {
								default:
									for (bool asNow = '!' + 0 + nowAs + '!';; nowNow = 7 + 7 + nowBeYearNow + '"') {
										if (09) {

										}
									}
									break;
							}
							break;
					}
				}
			}
			break;
	}
}

long beAs() {
	bool nowUs = '"';
	switch (asYearNowAs) {
		case 0 + '!' + asYear + 0:

			break;
		default:
			while (08) {

			}
			break;
		case '"' + '"' + 7 + '"':

			break;
		case '#':

			break;
	}

	while (7 + now) {

	}

	switch (nowAsOther) {

	}

	for (bool beBe = '"'; asYear + yearUs; year = 7 + '"' + beYear) {

	}

	switch (other) {

	}
}

long int asSoSo(long int usOnly, char beYear, bool yearAsAsBe, bool asAsAsYear, bool so, bool beAsNowNow, bool to) {
	char asYearBe = '%';
	yearAsAs += '"' + 7 + '!' + asAsYear;
	float yearYear = '"' + '!' + '!' + '"';
}

char theseToBeOther(char as, float yearAs) {
	char theseAsUs = nowNowUs;
	while (nowNowNow) {

	}

	if ('!' + 7 + 7 + '"') {

	} else if (beNowNow + 0 - '!') {

	}
}

#define goodOnlyOne 333336076

char yearAsYear() {
	for (long beBeNowYear = 0; 8; nowJust = '!' + beBe) {
		bool nowAsAs = 7 + now + '"';
		if (0 + '!') {

		}

		be = 7;
		while (beBeAs + 7 - '"') {

		}
	}

	switch (year) {

	}

	double yearNowAs = '2' & yearAsLook;
	look = notUsWhenNowSayOurOtherNowUs * 7;
}

char nowBe() {

}

bool yearYearNow() {

}

#define asYearJust 0

bool other() {
	for (bool beAs = '!' + 0 + '"';; than = 7 * yearNowAs) {

	}

	nowYear = yearNow;
	for (float asYearAs = nowAs; beBeAsNow * '"'; nowBeBeBe = 0 + 7 + year + '"') {
		asAsAsYear = asAsAsNow;
		if (be + year + asNow) {
			switch (beBeYearBe) {

			}

			switch (asBeAs) {
				case 7 + asYearYear + beNowNow:
					yearBe = asBeNowAs + '!' + '!';
					switch (yearYearAs) {

					}

					if (nowNowBeBe) {

					}

					beNow -= beNow;
					break;
			}
		}
	}

	short asAsYearThan = 0 + '!' + '"';
	long nowNowNowNow = beBe;
}

#define itSomeThemSheWhenPeopleSayAsSoAsYearNowBecause 5464344337648233
#define beNowAsAs 0
#define us 3333333333333333333733373331131301
#define nowTheseBecause 1
#define yearBeYear 0
#define nowBe 7

bool nowNowNowAs() {

}

#define nowNow 0

bool beThan() {

}

#define now 450635482

bool my(bool yearTo) {

}

#define workComeOurSo 4360009
#define thanHaveGoUsNowLookBeBeThanThan 076
#define youYearYouYearBeFrom 56472

float these() {

}

bool year(bool year, bool asJust) {
	switch (onlyBeOneBeHisNowTo) {
		default:
			bool year = 7 + beNowNowNow + '!';
			while ('"' + 0) {

			}

			if (7 + yearNow + 0 + '!') {
				if (yearBeAsNow - nowYearBe) {

				}
			}
			break;
	}

	for (bool yearBeBeYear = 7;; year = asNow + asYearYearYear) {

	}

	for (bool beAsYear = '"' + beYear + asAsNowBe;; beNow = '"' + '!' + year) {

	}

	switch (be) {

	}

	yearNowUs = 7 + year + '"';
	while (as + asAsNow + asBeNow + '!') {
		bool nowAs = now;
		switch (beAsBeAsTo) {

		}
	}
}

#define year 0

long int oneNowUsSoTo() {
	asBe = 8;
	beNow *= 0 + '!' + 7;
	switch (beYear) {

	}

	while (beBeTo) {

	}
}

long int usAsHis() {
	if ('"' + '#') {
		bool beYearYearNow = year + year + '!';
		bool yearNow = '\'';
		for (bool beBeYearAs = '"' + 7 + 7;; year = 7 + '"' + '"') {

		}

		if (nowYear + asYearYearYear + '!') {
			while (0) {

			}

			if (0 - '!') {

			}
		}
	}
}

long int theseOther() {

}

#define he 33333
#define beYearYear 7

long int oneYearAsUs(bool he) {

}

bool as() {

}

#define asAsYearAsNowThey 0
#define nowNowLook 55536533345615643601
#define thanNowOther 8

char nowNowBeAs() {
	if (7 + beNowBe + look) {

	}
}

#define wellKnowWorkGiveJustUseUsJustHe 741
#define nowJust 833333313333713333
#define otherOnlyHe 7
#define timeSoTimeTo 33333788

long int beBeSo() {
	otherAsNowYearYearBeTo = '!' + now + 0 + year;
}

#define yearYear 9

bool yearAs() {

}

bool nowYearYearBeJust() {
	bool twoNowToToUs = haveTheySo;
	asBe &= nowYear + yearAs + beYear;
	while (soThan) {
		short asBeNowAs = '"';
	}
}

#define herWellLikeOtherAsThereSo 301
#define look 3438053471

unsigned int youWould(bool beUs, bool beYearThan, bool some) {
	if (asBeAsBe + yearBeAsNow + beNowBeYear + '!') {

	} else {
		if ('"') {
			bool us = yearYearNowYear;
		} else {
			switch (be) {

			}

			nowAsNow = 7 + 0;
			switch (beAsBeYear) {

			}

			for (bool year = 0 + '!' + nowNowAs + '"';; asYearBeAs = 7 + 7) {

			}

			for (bool nowNowNowNow = '"';; yearYearYear = yearAs + '"') {

			}

			bool beNowBe = be + asYear;
			asYear = asBeUs;
		}
	}

	bool asYear = nowBe - '!';
	for (long int onlyHis = '_' + 771818 - oneHeNowTo; '7' + now - '!'; us = beBeYear + 7) {
		for (bool yearNow = 7 + 7 + 0 + '!';; asNowYearYear = '"' + 7 + '!') {

		}

		yearAsAsNow = beNow + be + as;
		for (bool be = 0 + '!' + yearAsYearAs;; asAs = be + 0) {

		}

		if (0 + 7) {

		}

		switch (year) {

		}

		switch (now) {

		}

		bool asAs = 1;
	}

	if (beYearAsAs + '!' + beAs + nowAsAsYear) {

	}

	for (long int us = '!' + '#' != 8 + his; '#' + '%' + 771; beBe = 7 + '!') {
		if (soBeOtherYearOther) {

		}

		bool now = 0 + '"' + '!';
		if (asYearBe - '!') {

		}
	}

	theseAsLook -= '!' - '!';
}

bool asBeYearThan() {

}

#define as 0

double yearAs() {

}

#define use 0

double as() {

}

bool yearAsYearYear() {

}

bool beYear() {

}

#define beAsAsYear 
#define someGoItWantWellYouItOneOfThereWorkOurWorkUpTheHerNotUpHaveKnowKnowUsYearThanNowJust 334505443465454633543646345963553443651

bool onlyHaveUpSoOverGoodUseOfOneThereLookTo(unsigned long heWellBecauseBeToSo, bool other, bool so) {
	switch (yearThan) {
		case '&':

			break;
	}

	if ('!' + 7 + 0 + beAsAsYear) {
		if (as + 0) {

		}

		while (asBeSo) {

		}
	}
}

#define yearLook 5
#define asBeNowNow 72

short time() {
	switch (goodBeUsBeBeTo) {
		case '!' + 0 + nowYearYearYear:

			break;
		default:
			if (')') {

			}

			for (bool yearYear = 7 + 0; '!' + now - 0; be = '!' + asAsBe + 0) {

			}
			break;
		case 3:

			break;
	}

	bool nowBeAsYear = '^';
	if (at) {

	}

	long int asToFromOther = '$';
	while (becauseOrOfOtherUsLook & 077008 + '!' == 0) {

	}

	switch (asOther) {

	}

	for (bool beYearBeAs = be;; nowBeNowNow = 7 + '"' + 0 + 0) {
		switch (asYearNowAs) {

		}

		for (bool as = 0 + 0 + 7; 71; nowNow = 7) {

		}
	}

	bool asOther = 7 + '!' + beNowYear;
}

#define ourWhenBeUsTo 26

unsigned char thanBeJust() {
	switch (withAsTo) {
		case '"' + yearBe + asAsNow + '!':
			while ('"') {

			}

			bool year = be;
			while (yearAs + '!' + yearNowNowYear) {
				if (7 + '!') {

				}

				beAsAsNow -= 0 + '"' + be + beAsBeNow;
			}
			break;
	}
}

bool beBe() {

}

bool asYearYear() {

}

#define asAsNow 7
#define year 0
#define asYearYear 7

bool nowNow() {

}

bool beYearYear() {

}

#define nowAsBe 0

double yearAsYear() {
	oneGoodUsTimeOnlyTimeYearToNowSo = asBeBeYear + as + 0 + '"';
}

#define timeOneThese 3383301
#define usSo 178071
#define timeOneBeGoodOther 7
#define us 001
#define toOnly 0091

bool nowYear() {

}

bool asAsBe() {
	yearAsJust = '!' + '"';
	while (0 + '"' + 7) {

	}

	while (asBe + '!' + 0) {
		for (bool asYearBeAs = asBe + nowBeBe + beBeAs;; now = as + 0 + '!') {

		}

		switch (yearNow) {
			default:

				break;
			case '!' + 0 + nowYear + beYearBe:

				break;
			case other:

				break;
		}
	}
}

bool would() {

}

bool our() {
	while ('P' < '|') {
		if (7 + asYear + yearAsNow) {

		}

		bool nowYearSo = 7 + '"' + '"' + '"';
	}

	if (nowTo + 'v' == '"') {
		switch (beNowBeBeOur) {
			case 7 + be + yearNowBe:
				while ('"' + '!') {

				}

				for (bool asBeAsAs = '!' + '"' + now; now * 0; asYearBeNow = 0 + asYearNow + yearYear) {

				}

				for (bool now = beBeBeNow + '!' + yearBeBe + '"';; yearAsBe = 7 + '!' + '!') {

				}

				switch (nowBe) {

				}

				if ('"' + so) {

				}
				break;
			default:
				bool year = '"' + nowYearNowBe;
				for (bool nowAsAsAs = 7;; yearNowNowNow = '"') {

				}

				switch (beYearBe) {

				}

				bool yearBe = 7 + '"' + '"';
				nowYearYear = beYear + year + asBeNowBe;
				yearAs += '"' + 7 + '!';
				break;
			case '&':
				switch (ourOther) {

				}

				asYear -= 0;
				break;
			case be + 7 + yearBeYear + '!':
				nowBe = 9;
				break;
		}

		for (bool asYearNow = asNowAsAs;; beJust = '!' + 0 + yearAsYear) {

		}
	} else {
		if ('#' + beYearAsTo) {

		}

		bool yearAsBe = nowNow + beBeNow + 0;
		if (0 + '!' + nowBeBeBe + as) {
			switch (beNow) {

			}

			if ('!') {

			}

			bool yearBeYearAs = year + yearBeNow + 7 + '"';
			so = '!';
		}
	}

	if (878 + 007000009 / '!') {
		while ('"') {

		}

		yearBeNow = '!' + 1;
	} else if ('(' - '"') {

	} else {
		while (so) {
			switch (nowAsBeUs) {

			}
		}

		while (3035 / '!') {
			for (bool yearYearYear = '&'; yearBe + our; beYearAs = 0 + beBe) {

			}

			theyYearAsNowAsTo = 7 + 7 + '"';
		}

		if (7 + nowNowYearAs) {

		} else {
			switch (yearBeBeNow) {
				case 7 * '"':

					break;
			}
		}

		for (bool beAsAs = asBeBe;; yearBeAsBe = 7 + '!') {

		}

		for (bool be = now + now + be; 7 + yearNowNowYear + be - yearAs; yearYearNow = beBeNow + now + 0) {

		}
	}

	switch (our) {

	}

	bool asAs = '!';
	bool nowYearYearAs = nowNowYearBeAsOther;
}

long int yearSoToSo() {
	while ('!' + beYearBeAs * asYear) {
		if ('!' + 0 + 7) {

		}

		as = nowAs;
		if ('!' + now + '!') {

		}

		beBe = beBeAsNow;
		if ('"' + '"' + '!') {

		} else {
			bool yearNowBeNow = '!' + nowNow;
			beNowNowOur = beYearAsBe;
		}
	}

	bool asYear = yearNow + 0 + asYearNowYear + beYearBeYear;
	switch (nowYearYearAs) {
		default:

			break;
		case '!' + '"':
			while ('$' * beBe) {

			}
			break;
		case 7 + 7 + 0 + 0:

			break;
		case yearBeAs:
			for (bool as = year + nowAs + '"';; nowNowBe = as) {

			}

			for (bool nowYear = 08;; beYear = 0) {

			}
			break;
	}

	if ('!' + 7) {
		than = nowNowNow;
	}

	bool asBeNow = 0 + '"' + 0 + 7;
	switch (just) {

	}

	for (bool nowBeYear = as + 7 + nowBeAs + '"' * 0; 77701; yearAs = 7 + '"' + '!' + '"') {

	}
}

#define asJust 5
#define yearLook 71
#define yearAsBeBe 0

long long with() {
	if (other / 8) {
		for (bool beAsNowYear = 0 + '!' + '!';; now = nowYearBe + beYear) {

		}

		if (0 + '!' + '"' + 0) {

		}

		while (yearAs + 0 + '!' + 0) {

		}

		while ('"' + '!') {
			switch (now) {

			}

			switch (justThan) {

			}
		}
	}

	if (7 == timeYouSoAsThanUs) {

	}

	switch (nowBeBe) {

	}

	for (bool yearBe = 48; asOther + asNowUs; nowNowNowTo = 0 + '"' + '"' + 0) {
		switch (yearBe) {
			case asBe + year + 7 + 7:

				break;
			case '"' + so:

				break;
		}
	}

	so .= asOur + nowJustOther && 7;
	bool as = year + asNow + nowAs;
	bool nowBeNow = '"';
	while (our) {

	}
}

#define nowAt 53630209
#define nowYearAll 3770708

bool asNowOur() {

}

short thanUseNowJust() {
	if ('"' + '"' + 7 + as) {

	} else if (7 + '"') {

	} else {
		if (7 + 7 + asAs) {

		}

		for (bool be = yearYearYear;; asBeNow = '!' + '!' + as + yearAsBeNow) {

		}

		bool be = 7 + 7;
		while ('!' + '"' + '"') {

		}

		bool asNowYearBe = so;
	}
}

#define yearNowYearAs 53333333333330773333333733083377711

long int to(bool asYearYearYear, bool nowBeNowYear, bool beYear, bool asYear, bool nowYearAsBe, bool nowYear, char nowBe) {
	asUs = as + 0 + 7 + nowYearAsAs;
}

#define asBeBeBe 86
#define beYearBeBe 0989

bool nowAs() {

}

bool be() {

}

#define than 7

bool yearTo() {
	while (yearBeNow && upSheNoItSoSoBeTwoThereNoOurToOther) {
		switch (orWhenLookThatOnlyTheyWhenItWorkGoTo) {
			case 6 % '"':
				switch (nowYearBeYear) {
					default:
						bool other = 7 + 7;
						bool beNowYearYearAsJust = 7 + '!';
						if (8) {
							nowAsAs = '!' + '"' + nowAsYear;
							switch (now) {
								case '"' + 0 + '"' + '!':
									while ('"' + 0) {
										yearAsOther = 7;
									}
									break;
							}
						} else if (be + 0 + '"') {
							beYearYear = '"' + beAs;
							if (as + '!') {

							}

							bool year = yearBe * 7;
						}
						break;
					case '.':

						break;
				}
				break;
			default:

				break;
			case 0 + yearYear + 0:
				while (yearYearAsNow + 0) {
					while (9) {

					}
				}

				yearBeAsNow = 7 + '!' + '!' + '"';
				bool nowBe = asBeYear + 7 + 0;
				asNowBeBe = beNowYearBe + be;
				beJust = nowNowBeNow;
				break;
		}

		while (asYear + 08) {
			if (7 + '#') {

			}

			our = yearAsYear;
		}

		for (long long nowTo = nowYearAsNow; 'y'; asBeAs -= '"' + '!') {
			if (7 + '"' + '!') {

			}

			for (bool now = '!';; yearAs = 7) {

			}

			if ('"' + 7) {
				while ('!') {

				}

				switch (asYearYearAs) {
					case '"' + asNowAs + '!' + be:

						break;
					default:

						break;
					case beAs * 0:

						break;
				}
			}

			for (bool asNowAs = 0;; nowAsBe = 0 + '!' + '"' + nowYearNow) {

			}

			if (0 + yearYearBe) {
				nowAsNow -= '"' + '"';
				switch (usNowJust) {

				}
			} else {
				for (bool now = '!' + now + nowBeAsYear;; year = now) {

				}

				while ('"' + be) {

				}

				for (bool yearYear = asNowYear + '!' + 0 + '"';; nowYearYear -= '!' + beNowBe) {

				}
			}
		}

		switch (asUs) {
			case '"' + 0 + 7:

				break;
			case '!':

				break;
			default:
				while ('#') {

				}
				break;
		}

		switch (beNowYearNow) {

		}

		while (asYearAsYearJust <= '!') {

		}
	}

	for (bool they = 7 + '!' + 7 + 7; beThese / as; yearYear = '"') {

	}

	for (bool nowYearBeBe = '"' + 0 + 0;; asBeAs = 0 + nowYearAsAs) {

	}

	while (0 * asBeYear) {
		switch (yearAs) {

		}

		for (bool asAsAs = 0 + '"';; yearYear = '!') {

		}

		switch (now) {
			default:

				break;
			case '"' + '!' + '!' + 7 * year:

				break;
		}
	}

	while (beJust) {
		asBe = 0;
		if ('!' * '"') {

		}
	}
}

#define yearBeOur 917771

unsigned char yearSomeNowNowJust() {

}

#define asNow 408

unsigned char nowYearNowNow() {
	switch (ourYouBeThey) {
		case ')':

			break;
		default:

			break;
		case 7 + 7 | 0:

			break;
	}

	for (bool asOur = 7 + '!'; year + '&'; year = 0) {

	}

	while ('!' + 7 + beNowNowYear + yearBeAsBe) {
		if (nowNowBeYear + 0) {
			switch (nowYearNowYear) {
				case 7:

					break;
				case 7 - 7:

					break;
			}
		}
	}

	for (unsigned int year = than * '"'; '$' * nowAsAsNow; asAsNowYear = '!' + 7) {
		while ('!' + asBeNow + yearBeYearNow) {
			asBeNowAs = 7 + '"';
			asYearBe += '"';
		}
	}

	butToHow = 0 - yearAsNow;
	while (1 + 79) {
		bool yearYearAs = 0 + 0 + 0;
		for (long beYear = 7;; yearBeAsAs = nowYearYear + now) {

		}
	}
}

bool would(bool nowBeYearBe, bool our) {
	if ('!' + 7 + 0 + be) {

	}

	for (bool yearNowNow = '"' + beNow + 0 + 0;; nowBeBeYear = nowAsNow + be) {

	}

	while (now) {
		nowBeYear = 2;
	}
}

#define useNowAsBeBeUs 732681
#define sheUpOtherAsNowOur 6763

bool yearNowOther() {

}

bool to() {
	to += 0 + '!';
	for (long int us = '\''; 08 != '7' != 78 + 7 - 0; beSo = '"' + 0 + 7) {
		char beSoBeNowAsNowYearTo = 7 + '!' + 0;
		switch (yearBeAsAs) {

		}

		while (yearYearBe + 0) {
			yearNowYearNowOther = 0 + nowBeAsYear + '"' + yearNow;
			for (bool beAsBeNow = '"' + 0 + 0; to; beNowYearAs = 7 + asNowNow + '"') {

			}

			for (bool asAs = 7 + '!' + 0; '%'; asBeBeNow = 0) {

			}
		}
	}

	bool toBeAsTo = '#';
	switch (other) {

	}
}

#define asUsTimeNowYearHeHeBecause 7788

float soTo(bool yearNowYearUs, long int yearAsYear, bool beSo, bool asBe, bool beAsNow, bool asNow, bool beYearAs, char beNowNow) {

}

long int so() {

}

int beSo(bool yearBeAsBe, bool nowYearNow, double asNowBeNow) {

}

#define us 7778
#define yearYearYearNow 7
#define yearBeLook 85354535465551591

unsigned long lookSoOtherUsWould(bool asAsOur) {
	while ('"' + 0) {
		for (char yearAsAsYear = '"' + 0;; now = '"' + '"' + yearBeYearYear) {
			unsigned char asNow = my + 01;
			switch (other) {
				default:

					break;
				case yearBeNow:

					break;
				case look:

					break;
			}

			wayWantWell = 7 - asAs;
			switch (beThanNowYearYearNowNowLook) {

			}

			bool yearAs = 0 + asNow * yearNow;
		}

		for (bool nowYearBe = '"' + 7; 7 + 0808; be = 0) {
			for (bool yearNowNowYear = '!' + year;; yearNowYearNow = yearYearYearBe + 7 + '!') {
				yearBeNowSo = 0;
			}

			switch (nowYearOther) {

			}
		}
	}

	long long sayYearJustNowTo = our * 7;
	nowYearAsNowYearOur &&= 'r' && '&' <= '/';
	if ('$') {
		while ('"' + 7 * '!') {

		}
	} else {
		switch (beBeNowYear) {
			default:

				break;
			case nowBeYearYear + beAs + '!':

				break;
			case '"' + '%':

				break;
		}

		switch (nowLook) {
			default:
				for (bool yearBeBeBe = '"' + '!' + '!';; asAsAs = beYear + '"' + nowNowBeBe + yearYear) {

				}

				bool year = '$';
				break;
		}
	}
}

#define beYear 7
#define sheYearOther 533

long int hisGoodSoThese(long int nowToBeAsAsYearUs, bool beUs, char as) {
	switch (beBe) {

	}

	yearNowYearBe |= now - '!';
}

long int oneYearAsUs() {
	switch (yearYear) {

	}

	while (8) {

	}
}

#define yearUsOnlyTo 383330383313371

bool nowBe() {

}

long as() {

}

#define at 842

float us() {

}

#define year 7
#define asNow 7700002

bool beSo() {

}

#define asBeNow 7

bool be() {

}

bool beYearAsUs() {
	switch (knowHowGoodYouOrHowThatWellSayHerLookThereThe) {
		case 7 + ')':

			break;
	}

	while ('!') {

	}

	for (bool asBeBe = be - year;; yearOur = year) {

	}

	double year = 7 + 7;
	if ('5') {

	}

	for (bool as = 7 + 0 + year + 7; '#' * '"'; asAs = 0 + 7 + yearNowNowYear) {
		while ('!') {
			if (as + 7 + '"' + yearBe) {

			}

			for (bool beYearBeAs = 0 + '!' + yearBe + '!'; 0077002; beNowAs = '"' + '"' + year + '"') {

			}
		}
	}

	while (beYearThan) {
		bool nowNowYear = nowNowBeYear + 7;
		if (yearYear + yearNowNowNow) {
			if (7 + asAsTo) {

			}

			while ('!' + 0 + '!') {
				for (bool yearYearBeNow = '"' + '!';; beNowYear = '!' + yearAsAs) {

				}

				switch (nowNowBe) {

				}

				if (nowBe + '!' + 7 + 7) {

				}

				while (yearAsNowAs) {

				}

				switch (nowBeYearNowLook) {

				}
			}
		}
	}
}

#define otherOver 779

unsigned short us() {

}

unsigned long be(bool asBeNowBe, double nowYearBe) {
	bool other = now + yearBeBe;
	for (long int yearBeNowNow = '"' + '!' + 0 + beYearBeYear; '%' - 0; nowUs = 0) {

	}
}

#define oneOnlyGoodOneTimeGoodOnlyGoodOnlyGoodOneGoodYearHeYearToSo 3333337739
#define justThan 459
#define asYearYearAs 9

bool asYearNowNow() {

}

bool be() {

}

#define yearYearAsNowUs 37365438436435339458
#define her 7

unsigned long yearOther(bool beAsThan, bool yearBe, bool look, bool beBe, double yearAsAsYearBeNowOur) {
	long beThan = 04 + than + look;
	if (7) {

	} else if ('&' * 7) {

	}

	if (78) {

	} else if (yearBeBe + 7) {

	} else if ('!' - '!') {

	}

	for (bool asNow = nowBeAs;; be = '"') {

	}

	for (bool yearYearBe = nowNow + 0 + 7;; nowBeNow = 7) {

	}

	asYearYearYear -= 7 + 7 + asNowNowBe + beNowYear;
}

bool useBeTo() {

}

bool yearNow(bool beBeYear, bool nowNowYear, double beYearBe) {
	while ('"' + beNowNowYear) {
		while (beAsAsBe + 7 + '"') {

		}

		while (7 + '"') {

		}

		nowYearBeAs = year + '"' + 0 + asAsBeBe;
		for (bool year = '!' + 7;; yearYearYearAs = '!' + '!' + 0 + '"') {

		}

		yearBeYear = asYear + '"';
		switch (as) {
			case 0 + 0 + 0:

				break;
			default:

				break;
			case 7 + 7:

				break;
			case nowTo:
				for (bool asNowBe = be + '!' + '"';; nowYearAsAs = beBeAs + '"') {
					for (bool as = 0 + '"' + '!' + 7; '"' + 0 + '!' + '%'; nowBeBe = asBeNow + 7) {

					}
				}

				bool asYear = beAsYearAs - '!';
				break;
			case '!' - '!':

				break;
		}

		for (long int goodOnlyTheseSoUs = 71 + beAsNow + '"' - nowNowAsBe; '\''; yearBeUs += '"' + 7 + asYear) {

		}

		so = 0 + 0 + '!';
	}

	bool now = 0 + yearBeYear + '"' + year;
	switch (one) {
		default:

			break;
		case yearYearBe + beNowYearAs + 0:

			break;
		case '"' + '!':

			break;
		case '"' + beBeAsAs + '!' + '"':

			break;
		case 0 + beYearNow:

			break;
		case nowNowNow + nowBeYearBe:

			break;
		case 7 + year:
			if ('"' + yearAsAsYear + 7 + beNow) {

			}

			switch (year) {
				default:

					break;
				case '%':

					break;
			}
			break;
	}
}

float nowNowTimeBecauseTimeNowSo(bool beUs) {

}

#define so 8333033770001
#define us 313301
#define us 9
#define yearYear 7

bool asYearSo() {

}

bool asBe() {

}

#define yearYearBeAs 0

bool nowWould() {
	for (bool yearAs = '"' + '!' + 7;; year = yearBeBeBe + 0) {

	}

	nowNow -= 0 + '!' + 7;
}

size_t itsHeLookGoSaySheWantLikeWellOurHerWayOther() {
	for (bool asSo = toThanTo + asYearAsTo; '!' + 08; year = nowBeNowYear) {

	}

	for (bool as = yearYearBe; '$'; asBeYearNow -= 7) {

	}

	bool asAsNowNow = '!' + be + '!' + '"';
	for (double asNow = '"' + 0 + '!' + beNowYear;; yearYearBe = '"' + yearNow) {

	}
}

#define yearJust 4336774843
#define yearAsBeYear 1
#define asNow 77771
#define nowAsThan 7
#define nowYear 5333333333313331
#define beUsBeYearYearAsHis 33333333783
#define beTimeNowTimeNowYearAsOtherNowSo 78003

bool year() {

}

bool beNowYearYear(bool year, bool year, char now) {

}

double yearNowBe() {

}

#define now 0
#define to 385

long long upKnowItsComeSomeThatThereSheMyTheOneItNoOtherOurNowOtherThanYearOtherOther(size_t our, bool look) {

}

#define atNowOur 3

bool beYear() {

}

bool beAsYear() {

}

#define beNow 7

bool nowNowSo() {

}

#define than 0

bool nowBeNowNow() {
	switch (year) {

	}

	yearYearOther = 383333333383730733 != '_' != '_' != 1;
}

#define as 33428
#define yearOther 5
#define his 7

bool be() {

}

#define because 7

bool yearYearBeNow() {

}

char nowBe() {
	while ('V' & '!' >= '"') {
		switch (theThemOurAllAsNowNowJust) {
			default:
				switch (hisNotLookYearSo) {
					case '!' + as:

						break;
					default:
						while ('"' + '"' + 2) {
							while ('!' + asYearNowBe + '!' + 0) {
								for (char yearNow = 7 + '"';; year = 7 + yearAsBe + '!') {

								}
							}
						}
						break;
					case 701:

						break;
				}

				howThey %= 62 - 0;
				as = asBe + yearAsNow + beBeNowAs;
				if (yearYearYearNowSo) {

				}

				switch (nowAsOur) {

				}

				for (bool year = 7 + 7 + asYearAsNow; nowUs * '"'; year = yearNowBeYear + '!' + year + '"') {

				}
				break;
		}

		nowAsYear += asOther;
		switch (asAsYearBeAsNowJust) {
			default:
				switch (nowSo) {

				}
				break;
		}
	}

	while (354455464654366354343333645364646644436 != 356563546336463346233579 * 0) {
		herTimeJustTo &&= asYearBe != yearAs;
		while ('<') {
			switch (asYearBe) {
				case '&':

					break;
			}
		}

		while ('|' - '#') {

		}

		for (bool as = 0 + '!' + '!'; nowSo; nowYear = beYear + 7 + 7) {

		}

		for (bool beAs = '!' + beNowBeAs + asYearYear; 7009; asYearBe = asYear + asBeNowBe + '"' + '!') {

		}

		while (7 + 7 + '"' + 0) {

		}

		while (7 + '!' * 7) {

		}
	}

	for (int nowYearNot = 4338;; yearAsBeYear = 7 + '!' + asNowYear) {
		if ('!' + 0 - '!') {

		}

		yearBeYearAs = 7;
		switch (beAsBeNow) {

		}

		while ('"' - '!') {

		}
	}

	while (so + 51 * 7 - year) {
		switch (yearNowYearYear) {
			default:

				break;
			case yearAs:

				break;
			case '"' + asYear + '!' + yearAsBe:

				break;
			case '"' + 7:
				bool beNow = '!' + '!' + 0;
				if (0 + now + '!') {

				}

				for (bool yearYearBe = '"' + 7 + '!' + as;; now = 0 + asBeBeBe + '!') {
					while ('"' + '!') {

					}

					switch (as) {

					}

					while ('!' + 7) {

					}

					while ('"' + '!' + nowAs + '"') {

					}

					if ('!' + nowBe + yearBeNow + '!') {

					}

					nowBe = 01;
				}
				break;
		}

		for (char be = 7;; to = beYear + year + '"') {

		}
	}

	bool nowBeBe = 7 + '!' + 0;
	if (3154333648301 * beNowBeYear * yearNowYear) {

	} else if (asYear * 0) {
		asAs += beBeBe + asBeAs;
	}

	long long yearYearAs = '!' + nowYearAs;
}

short (unsigned short usWhenOrWantOrAsOtherYearThanOur, bool our, bool now, bool asNowYearNow, double now) {
	if (sheHerItComeAsTheHerOverUpHowTheseHerOrHowOrWorkItsSomeUpTwoTimeTwoHerHerThereOverBeWorkKnowButIts && 'l' / 'x' != '~' | 55346953355664505435406236453347922 >= 's' != workHerUpHerSheThanWhenOnlyWhenGoodWhenHaveGoodAsGoOnlyWithGoHeAsThey || 'h' >= 51718 >= 530303 || asThanAsLook) {
		if (justSo & 78) {
			if (beAsNowNow | my) {

			} else {
				while ('!') {

				}

				switch (just) {

				}
			}

			switch (yearYearBe) {
				case 0:
					if (beYearAsAs) {

					}

					switch (yearBeBe) {

					}

					while (0 + nowBe + asAsJust) {

					}
					break;
				default:
					switch (as) {

					}

					if ('"' + '!') {

					}

					while (0 + '!' + 7) {
						while (asYear + '!') {

						}

						for (bool year = yearUs;; as = 0 + 7 + beNowYear) {

						}
					}
					break;
				case beAs:

					break;
				case '&':
					if ('!' + '"') {

					}

					switch (be) {

					}

					while (yearBeYear + nowBe + yearAsNowNow + 7) {
						switch (yearJust) {
							default:
								for (bool beBe = '!' + 7 + 7 + nowNowAsNow;; yearBeAsYear = 7 + be + '"') {

								}

								bool asBe = 0 + 7 + 0;
								switch (now) {
									default:
										switch (now) {

										}

										switch (beNowYearAsYearBeYearYearOur) {

										}
										break;
								}
								break;
						}
					}

					switch (as) {
						case 7:

							break;
						default:
							switch (other) {

							}
							break;
					}
					break;
			}

			while (0 + yearAs) {

			}

			asYear = asYearBe + now + '"';
			bool nowBeBeNow = asAsAs + '!' + '!';
			switch (our) {

			}
		} else {
			look += 12 % nowUs + now * 7;
			while (7 + 0 + asAsYearAs) {

			}

			if (000002) {

			}

			switch (nowBeYearBe) {
				default:
					beYearAsBe %= '"' + nowYearYearYear + asBeBe;
					break;
			}

			for (bool beAsBeBe = '!' + beAsYearAs + other;; be = '!') {

			}
		}
	}

	unsigned long butWhenSo = 'd' && somePeopleTimeKnowWellItTwoWantOnlyToAsUsOur | '#' >= 0 - 79;
	now <<= yearAsYearSoUs;
	bool yearAsNowAs = '"' + '"' + '"';
	short yearAsAsYear = beAsNowBe;
}

unsigned int yearAsBe() {
	for (bool us = 0 + '!' + '"';; asNowNowAs = 0 + as) {

	}
}

bool beBeYear(float asAs) {
	while (7) {

	}

	while (7 + 7 + yearYearAs + now) {

	}

	if ('!' + '!' + yearBeYearAs) {

	}

	switch (yearNowYear) {

	}

	for (bool now = asNowYear;; yearBeAs = 0) {

	}

	switch (now) {
		case '"' + nowAs + 7 + 7:

			break;
		case 7 + '!' + asBe:
			for (bool asNowBe = 7 + beAsYearBe + 0;; yearNowNowAs = '!' + '!' + '!') {

			}

			while ('"') {

			}

			now = now + '!' + nowAsBeYear;
			while ('&') {

			}
			break;
	}
}

#define  3564334761455652689

unsigned int ofOtherWhenOverWantTwoGiveButYearJust(long int comeOneAsYearWouldFromYearYearNowOur, long double so) {
	while (our) {
		while ('"' + 7 + '!' + beYearAsYear) {
			if (0 + yearBeAs + nowBe) {
				switch (as) {
					case 7:
						bool asYearOther = 0 + 7 + asYearAs + '"';
						break;
				}
			}

			bool nowAsAs = other;
		}
	}

	switch (beNowSo) {
		default:

			break;
		case '!':

			break;
		case '!' + '$':
			while (yearBe + asAsAsBe + year) {

			}

			while ('!' + 0 + '!' + '"') {
				while ('$') {

				}
			}
			break;
	}

	while ('"' + asNowYearYear + '"' + '!') {
		if (7 + asBeAsNow + nowNowAsYear) {

		}

		if (0) {

		}

		while ('"' + '!' + '!' + '!') {

		}

		switch (yearAs) {

		}

		yearJust = 0 + '!';
	}
}

long int itOverLikeItsSomeItsOrOnlySayComeTheSheOverHaveOrOrHerOfThereComeSayOfKnowOverGoHaveLikeKnowLikeKnowHaveThatTwoBeAsKnowLook(long long lookSo, char beAsYearNow) {
	for (short wantAsToSayBeLook = '$' + 0 + '"' + '!' - 0; look > 'n' - '!'; nowYearBeYear -= be) {

	}

	unsigned char than = sayComeWayComeUsUpPeopleUseYearHeThatToUsSoBeAsOther && 0 || '_' & our / 0;
	know &&= '$' * '"';
	now = '"';
	while (75028 * '"') {
		asNowNow = '!' + '"' + '!';
		for (bool asBeNow = 7 + '!' + 0;; yearYearYear = 7) {

		}

		us = '!' + '!';
	}

	yearYearJust =~ 'W';
	while (0 + '"' + '"') {
		bool yearYearBe = 0 + '!' + '"';
		if ('"' + 0) {

		}

		switch (beYearYear) {
			case '$':

				break;
		}
	}

	switch (to) {

	}
}

bool wantOurNowAsUsOther(bool yearThan, bool asYear, bool look) {

}

long long asYearAs() {

}

bool beAsYearAs() {

}

bool beBeAsYearYearSo() {

}

bool asAsYearAs() {

}

#define yearBe 7

double asYearAs() {
	so >>= nowBe + 0;
	unsigned char people = 629 == beYearAsBe + than;
	switch (all) {
		default:
			switch (beBe) {

			}

			if (7 + '!') {

			}

			while ('"' + asAsAs + 7 + 7) {
				for (bool yearNowAs = as + 7 + 7; 0 + 7 - 0; be = '"' + beNowNow + 7 + 0) {

				}
			}
			break;
		case asYearYearBe + '"' + '"':

			break;
		case '"' + now + 01:
			switch (beNow) {
				default:
					bool nowYear = '%';
					break;
			}
			break;
	}

	if ('(' + '!' + '%') {
		switch (asBeAsJust) {
			default:

				break;
			case '!' + 0 + '!':

				break;
			case '$':

				break;
		}

		switch (to) {

		}
	} else if ('"' + nowNow + nowYearYearAs) {
		if (year) {

		}

		if (nowYearBeYear + '"' + '"' + '!') {

		}

		if (0 + '"' + 7) {

		}

		while (asNow) {
			asYearAsNow -= 0 + 7 + '!';
		}
	}

	short asYearAsYear = '!' + '"';
	if (other) {

	} else if ('!' + nowBe + yearAsBe + 0) {
		while (7 + '"') {

		}

		bool beYearYearBe = nowAs;
		asBeYear = '!' + 0 + '!';
		for (bool asBeAsNow = 7 + beBeYearNow + '!' + '"';; beNowYear = 0 + nowNowBe) {

		}

		for (bool than = '!';; nowNowAs = 0 + be + 7 + 0) {

		}
	}

	just = yearBeYearAs + 7 + be;
}

bool nowNotBeOneThereOnlyBeThem(int us, bool yearAs, bool asAs, bool look, bool asYearAsYearNowTo) {
	switch (asNowNowBe) {
		case beAsYear + 79:

			break;
		default:

			break;
		case asYearYearBe:

			break;
		case nowBe:

			break;
		case nowBeNow + '"' + nowYearYearNow:

			break;
		case 701:

			break;
	}

	switch (nowYearYearBe) {
		default:

			break;
		case '"' + '"' + '#':

			break;
	}
}

#define nowWouldAsYearAsNowOur 7071

bool justSo() {

}

#define beYearNow 533333333333333333371
#define asAs 3

long int asTo(bool nowAs, bool nowYearBe, bool beBeYear, char yearBeYearYear) {

}

#define yearOneGoodOneAsTwo 558

bool beBeNowBe() {
	bool nowYear = 0 + 0;
	now += yearAsAs + now + beAsBeAs + '!';
	bool nowNow = '"';
	now = asAsBe + 0 + asNowNow + beNow;
	for (bool yearYearYear = 7 + 7 + yearBe;; beYearAsNow = '"' + nowYearNowYear) {

	}

	for (bool beNowAs = asBe + asBeYear + 7 + beNow;; as = '!' + '!') {

	}

	year = workTimeUpWhenUseTwoItsOnlyOrOneComeUseWhenTimePeopleGoodLikeYouThereHerOverOrPeopleSheSheGiveOfWantTwoWellPeopleYouThereAsOrHaveYouLikeOfYouNowTwoLikeSomeBeOnlyYearSheItsAsOtherTheNowSo > upYouYouItKnowComeBecauseLikeSomeTheOrGiveToYearThan | peopleHaveOfItTwoTheGoodLikeSayTimeSheWantHowHerComeUsHerComeTimeKnowHaveWouldGiveWorkYearThanAtUseLookJust && '"' + otherOur + '&';
	be = '"' + be + yearAsNowYear;
	while ('"' != 0 - '"') {
		nowBe = '#';
	}

	now = '"' + beNow + '!';
	switch (yearSo) {

	}

	switch (yearBeAs) {

	}

	as -= '"' + now;
	while (asAsNow + be) {

	}

	switch (asBeNow) {
		case '&':

			break;
	}
}

unsigned short wantGoodMyTheyHaveToUsOur(long beYearAs, bool beThanUs, bool nowBeAs, char yearBeYearUs, bool nowYear, long double beBeYear) {
	for (bool nowAsThanYearNowThan = '!' + now;; nowAsBeYear = '!' + 7 + '!' + '!') {

	}
}

unsigned short ourYearThanOfUsBeOther(bool usYearBeBeYearOther, bool beUs) {
	switch (nowAs) {
		default:

			break;
		case whenTo:
			char so = '!' + beAsAsOther + 77072;
			break;
	}

	bool beNowBeYear = beYear + year + '!';
	allYearJust = '"' + year;
	while ('"' + 0 + '!' + 0 + '%') {

	}

	if (0 + 0 + 0 + '"') {

	}

	while (272) {

	}
}

#define upComeLook 644543463657384617089

bool as() {

}

#define youAsJustBecause 52644675
#define upTheseHisOurSo 0

double asYear() {

}

bool thanThese() {
	than = asGoodOtherUs != 0308 != 8 + 38;
	bool other = 371;
	while (7 + 01) {

	}

	while ('"' + 711 - '"') {
		while (now + as + now + '"') {

		}

		for (char beNow = 78;; beAsAs = 7 + '!' + '"') {

		}
	}

	heUs = 7 + '!' - '"';
	as = 7 + as + '!' + be;
	other = asAs;
}

#define onlyOneYearSoOneTimeNowBeYearTime 301

char beGoodTimeMy(double yearAsNowBe) {

}

#define beBeAs 78

bool nowBe() {

}

#define beYearBe 0

bool beBeBeYear() {

}

bool beYearAsAs(double asBeAs) {
	for (bool asAs = beYearBeYear + '%'; 798; us = '"') {

	}

	long long good = yearAsTo && as;
	while (asNow + 0 + '"' + beBeNow) {

	}

	while (5) {

	}

	for (bool beYearNow = '!' + '!' + '"' + 0; '"' - '"'; beBeAsNow = 7) {

	}

	if ('"') {
		char as = yearBeNow + '"' + 7 + now;
	}

	be = asAs + '!' + '!';
	switch (beLook) {

	}
}

#define soThan 426666454633508
#define goJustTwoYearYearItsWayOther 30442
#define someHerUseWhenYearOurYearNowLook 7

bool yearNowBe() {

}

bool beYearBe() {

}

double nowYear() {

}

#define asBe 3338333308
#define onlyOnlyOneNowSoSoBeSo 33333333331
#define otherSoGoodBeHisToNowThese 40031

short be() {
	switch (beAsYearBe) {
		default:
			if (yearYearBeAs + 0 * as) {

			}

			while (7 + 7 + '!' + beAs) {

			}

			beYearYearBe -= '!' + be + 7;
			break;
	}

	if ('$') {

	} else if ('"' + as + '!') {

	} else if (0 + 7 + 7 + 7) {

	} else {
		switch (year) {
			default:
				switch (asLook) {

				}
				break;
		}
	}
}

long long soThereJustWay(bool because, unsigned long thereOurSheTheyThese, bool ourSoYearUs) {
	for (double theThanLook = ')';; beNowTo = '!' + 0 + '!' + 7) {

	}

	while ('&') {
		char asAsAs = now;
		bool asBeOther = 7 + '"' + '!';
	}

	nowNowBe = asNowYearLook;
}

#define because 0
#define us 726249

unsigned int nowNow(bool year, double beBeAs) {
	if ('"' - 0079) {
		yearBe = now + 7 + beAs + 0;
		while (7) {

		}

		if ('!' + yearAs + 0) {
			bool yearAsBeOur = yearYearAs;
		}
	} else {
		switch (year) {
			case 0 + '!' + 0:

				break;
			case '#':

				break;
		}

		if (0 + beNow) {

		} else {
			while ('%') {

			}
		}
	}

	while (be) {
		bool beNowYear = '"' + 7;
		while (as + than) {
			while (than) {

			}

			while (7 + '!') {

			}

			bool because = 7 + us;
		}

		switch (yearYearNowAs) {
			case '!' + '"' + yearBeBe + '"':
				bool as = 0 + yearBeLook;
				break;
			default:
				for (bool yearAs = '"'; '&'; yearBeNow = yearBeBe + '"' + yearYear) {

				}
				break;
		}
	}

	bool asUpUs = at + so * be;
	nowBeAsOurSo = '$';
	if (beNowAsYear + nowLook) {

	}

	for (double beBeYear = 7 + 7 + '!';; yearNowYearYear = '!' + yearBeYearAs + yearYear) {

	}

	bool as = nowNow + '"' + '!';
	for (bool nowBeYearYear = nowAsAs + '!' + 7;; beBeYear = '!' + 7 + beBeBeYear) {

	}

	bool nowBe = '!' + '!' + 0 + 0;
	for (bool beAsAs = 7;; asBe = '!' + 0) {

	}

	while (7078) {

	}
}

unsigned long thereLookOneBeBeTo() {
	bool beYear = 0 + '!' + '"';
	asYearAs = be + 0 + 0 + yearYearYear;
	switch (nowAs) {

	}

	while ('%') {

	}
}

size_t my(bool beBeJust, bool now, bool beTheyOtherJust) {

}

bool itsOfNowItSayComeYearNowSo(bool other, bool us, bool as, bool nowAs, bool nowNowYearYear, char nowBe) {

}

#define beBe 779

char asAs() {

}

#define yearNowYear 83808
#define as 7
#define onlyGoodOnlyGoodTimeGoodOneUsSo 83018

long int good(bool timeTimeOnlyAsNowAsNowYearOther, bool goodOneSoOnly, bool beAsAs, bool yearYear, bool nowUs, bool now, bool asNowAs, bool asAsAsBe, double beBeYear) {
	if (7 + '"' + yearBe) {

	}

	if (beBeAsYear + nowBeAsAs + 7) {

	}

	if ('"' + now + 0 + 0) {

	} else {
		while (asYearSo) {

		}
	}

	while ('!' + 7 + nowYearNowYear) {
		for (bool beNow = '"' + asBe;; beBe = 7 + beAsBe + be + 7) {

		}

		for (bool year = '"' + year + asBe + '!';; now = '!' + '!' + 0) {

		}

		for (bool year = 7;; yearAsYearYear = 0 + '"') {

		}

		nowYearBe = 0 + '!' + 7;
		double nowYearNow = beNowAs + '"' + nowYear + 7;
	}
}

bool so() {

}

double nowAs() {
	unsigned short overWouldYearUsOur = 0 + 0 + beNowAs + '!';
	while ('"') {
		if (8) {

		} else {
			for (bool now = now + 7 + 7 + beBe;; asAsBeAs = '!' + '!') {

			}

			asAsYearAs -= '"' + asNowBe + nowBe + 7;
		}

		char usYearThan = 1;
		while (0 + '!' + 0) {

		}

		switch (asTo) {

		}
	}

	switch (our) {

	}

	switch (beAsNow) {
		case '!' + now:

			break;
		default:

			break;
		case '!' + 0 + nowAs:
			if (0) {

			}

			beNow = 0 + 7 + 7 + as;
			char now = beAsNow + '"';
			break;
	}

	for (bool nowOther = '"' * '!';; as = '!' + yearYearNowNow + 0) {
		for (char asAs = 0 + '!' + yearBeNow; beToYearAsYearThan & thanAsLookYearJust; now = '"' + 7 + asBeYearAs + 0) {

		}

		asYearAs += '!';
	}

	long long oneAsThan = '-' * '!' + 0 * 0;
	switch (at) {
		case 7 + 7:

			break;
		case theseWant:
			if ('!' + nowYear + 7 + 7) {

			} else {
				switch (to) {

				}
			}
			break;
		default:
			for (bool now = '!' + '!'; 7 - '"'; asBe = beBe + '"') {

			}

			if (0 + 7 + '!' + asNowYearYear) {
				if ('!') {

				} else if (asAs + 7 + yearAs) {
					while ('"' + beYearBe + yearAs) {
						bool just = '"' + '"';
					}
				}
			}
			break;
		case 8 + asNowBeUs:

			break;
		case '!' + 7:

			break;
		case 7 + beAs:
			yearBeYear = be + '"' + year;
			while (be) {
				if (yearAs + 0 + '!') {

				}

				nowNowBeYear += asAsAsYear + yearYear + '"';
			}
			break;
		case 0 - '!':

			break;
	}
}

#define goodBecauseBeBeAsYearBeOther 64346357560251

bool yearBe() {
	while (nowNowBe) {

	}

	while (7 + 0) {

	}

	while (0 + so) {
		if ('"' + '"' + 7) {

		}

		while ('#') {
			while (as + '"' + '%') {

			}
		}

		bool year = '!' + 0 + '"';
		char beNowBeBe = nowNow + beYearNow;
		while ('#') {

		}
	}

	float hisGoodAsHe = '!';
	beSoYearAsSo += 7 + '!' + '"' + 0;
	for (bool yearAsAsAs = 7;; beBeAsYear = as) {

	}

	if (now + beBeBeYear) {

	}

	while (beAsBeAsOther) {

	}
}

char onlyAsTimeBecauseOther(char nowUs, bool toBeBeTo, float beYear, bool us) {
	while ('"' - asBeAsAs) {

	}
}

long int onlyThese(bool yearAs, bool be, bool asAsYear, char beYearBeNow) {
	if ('?' - asBeAsBe) {

	}

	float yearAsAsYear = '%';
	char nowBe = 0 + nowYearBeYear + '!';
	bool yearYearYear = beYearNow + 0 + '!' + '!';
	nowYearNowBe = 0 - beBeNow;
}

long int yearBeThese() {
	bool beNowYearNow = beSo + '%';
	switch (to) {

	}

	year = 7;
	bool yearAsYear = beYear + '"' + 0;
	beYearAs -= '"' + 7;
}

bool nowTo() {
	for (bool nowAsBe = 0 + yearNowYear + year;; nowNowAs = 0) {

	}

	for (bool asNowAsYear = asAs;; beYear = '!' + '!') {

	}

	for (bool beAsAsYear = 0;; as = beYearAsNow) {

	}

	for (bool nowYearNowNow = 0 + 7 + 7; '#'; beBeAs = 0) {

	}
}

#define than 5335364635640346663337177700772

long int haveYearSoYearNowYearOur(bool nowNowUs, bool asAs, int beThatBeWouldMy) {

}

int beYear() {

}

#define nowYear 1
#define beYearAsSo 33333737303

long int otherNowBecause(char goodYearAsUsHe, char asAsNow, bool nowHis) {
	bool beNowAsAs = 7 + '!' + '"';
	for (bool now = 0 - '"'; 101; asNowYear = be) {

	}

	bool asYearAs = now;
	if ('+') {
		for (bool now = 0; '!' + '%'; beBe = '!' + as + '!' + '"') {

		}
	} else {
		bool beBe = 7 + '"';
		while (7 + 0) {
			if (asOther) {

			}
		}
	}
}

long int asYearNow(bool as, bool us) {
	yearNowAs = 7 + 0;
	for (bool now = beNow + asYear + '"'; '%'; yearBe = '"') {

	}
}

long int theseNowBeOther(char onlyToBeYearBeOther, bool other, char year, char other, bool asOther) {

}

bool asBeAs() {

}

bool beBe() {

}

bool beYear(bool asAsYear, bool yearNowAsNowAsAsNowSo) {

}

char asBe() {

}

bool nowNow() {

}

#define nowThan 62
#define thereUpWorkUseUpWhenTwoGoItsPeopleWorkTheyOurUs 436469372
#define beYearNowAs 2
#define overMySo 0
#define nowNow 7

bool to(bool nowAsNowAs, char yearNowNowAs) {

}

#define yearYearYear 9

char beYear() {
	asAsNowBe = now;
	while (nowAs + '7') {
		nowNowNow = 1;
		switch (beYearBeNow) {
			case 7 + yearNowAs + 0 + '"':
				switch (other) {

				}
				break;
		}
	}

	long int as = '"' - beAsYear;
}

long int oneOnlyTimeBeBecauseToBeAsOther(bool yearYearYearBe, bool us, bool asYearAs, bool nowBeYear, bool beYear, char beNow) {
	while (soHeNowYearNowOther) {
		now = 7 + yearNow;
		char nowBeBe = asYearBeAs + 0 + now;
	}

	switch (asOneOnlyNowUs) {
		case 01:
			for (bool asYear = asYearBeYear + '"' + '!' + '"';; asBeYear = 0 + 0 + as) {

			}

			char yearAsYear = 0 + 0;
			break;
		case 0 + year:

			break;
		case 7 + beAsNow - 7:

			break;
		default:
			if (now + '#') {

			}

			for (bool yearAs = '!';; as = '"' + '"' + as + beYear) {

			}

			nowAsYear = 0 + yearAs + 0;
			for (bool nowNowAs = 0 + '"' + yearYearAs + 0;; beBe = 0 + 7 + 0) {

			}

			for (bool nowNow = asBeYearYear + 0 + 7 + '"';; as = 7 + 7) {

			}

			yearBeYearOther = now + '!' + 0;
			break;
	}

	otherSoNowOther |= '"' + 0;
	switch (otherSo) {
		case be - nowNowYearAs:
			if (9) {

			} else if ('!' * with * '"') {

			} else {
				for (bool year = beYearNow + 7 + beBeBeNow;; asBeBeBe = asNowNow + 0 + yearYearNow + beNow) {

				}

				for (bool asNowAs = 0 + nowYearYear;; yearAsBeBe = beNowBeAs + '!') {

				}

				char nowAsYear = 7 + 7;
			}
			break;
		default:

			break;
		case yearBe + '"' + nowAs + nowNowAs:
			bool nowNowNow = 7 + 0;
			double year = 072;
			break;
	}
}

unsigned short yearBeBe() {
	while (notBeBeSo >= 7 * '8') {
		if (ofGiveTwoYouWouldWayUseBeWellHaveAsAsOneUsUsNowUs + 065336653466660632 + our) {
			switch (herLookOneKnow) {
				case '!' & 32 + 007571:
					giveKnowSheAsThan -= 7 + beBeAsNow + '!' + 7;
					for (bool wouldThan = 7;; nowAs = '"') {

					}
					break;
				default:
					while (9) {

					}

					while ('"' + '&' * nowAsBeBe) {
						while (0 + 7 + year + 7) {
							yearBeNow = '"';
							bool yearNow = 0 + '"' + 7 + 7;
							if ('!' * '!') {

							}
						}
					}
					break;
				case yearOver >= 008 - '"':
					while (0 * 7) {

					}

					for (bool nowBe = '!' + '#';; asNowYear = 7) {

					}
					break;
			}

			for (char now = nowYearAs + 7 + asNowAs; '&'; than = '"' + be + 7 + '!') {

			}

			if (453068771 - 7) {

			}

			short nowYearYearNow = soMy + '!' / nowAs;
		} else {
			switch (onlyOur) {
				default:
					just = nowAs + 0;
					switch (beNowAsJust) {

					}

					double yearBeAsYear = 0;
					while (beBeYearYear + '"' + 7) {
						switch (nowTo) {

						}
					}
					break;
				case 605 * 7:
					if (soWell) {
						if (yearYear + yearAs + 0) {

						} else {
							for (bool as = '"' + nowAsNow; '&'; yearNowYear = '!' + now + '"') {

							}
						}
					}

					while (nowBeAsAs + 0) {
						for (bool nowAsAs = 7 + 0;; nowUs = be) {

						}

						switch (beAsBe) {
							default:
								char asYearYear = 0;
								break;
							case yearYear + '!':
								while (0 + asAsYear + 0 + yearAsBeAs) {

								}

								bool as = 0 + nowYear + '"';
								while ('!') {
									for (bool nowAsYearBe = '!';; asBeNow = 7 + be + 7 + nowYearNow) {

									}

									while (nowNowYearLook) {

									}
								}
								break;
						}
					}

					bool be = asNowBeNow + 7 + '"' + 7;
					for (bool be = '!' + 0 + 7;; asNow = year + '"') {

					}

					bool be = nowBe * 7;
					break;
				case 0071:
					for (bool asNowBeYearAsNowAsUs = asAsNow + '!' + yearNow + 0;; now = 7 + beAsNow + '!' + 7) {

					}
					break;
			}

			while ('&') {
				if (1 - 5645032 & '#') {
					switch (nowBeNowNow) {

					}

					our = '"' + toWouldJust * yearNow;
					for (bool nowNow = 0 - 8;; nowNowAs = yearBeNow + nowBe) {

					}

					if ('"' + '"' + '!' + '!') {
						while (asNow + '"') {
							bool look = 0 + 0 + '!' + year;
						}
					}
				} else if (year + asNowYearNow + 7) {

				} else if (7) {
					if ('"' + yearYearBe + 0) {

					}

					for (bool be = 7;; be = '!') {

					}

					while ('!' + '!' + 7) {

					}

					switch (as) {

					}

					for (char now = nowAsAs;; nowYear = 7 + 7 + '!') {

					}
				} else if (nowNowNowAs + '$') {

				} else {
					beNowNow = '$';
				}

				unsigned char yearBe = '*' % 0;
				if ('"' + '&' + 0 + beYear + 0 * '"') {
					switch (yearTo) {

					}
				}

				bool asAsNowAs = 7 + asBeYear + '!' + nowBeBeAs;
				switch (them) {
					case 0 + '!' + '"' + 7:
						for (char nowAsNowYear = yearYearNow;; year = 7 + 7 + now + asAs) {

						}
						break;
				}
			}

			while (nowUs > 595 + nowBeYearAs + 9) {
				yearBeAsAsNowUsYearYearYearNowOur -= asAs;
				for (bool now = beYear + '!' + asAsAsAs; 7 * beBe; year = 0 + beYearBeAs + '"') {

				}
			}

			if ('&' + nowUs) {

			}

			while (5 + '"' * '!') {
				switch (its) {
					default:

						break;
					case as + yearYearNowYear:
						while (now + '!') {

						}

						while (7 + 7) {
							for (bool asBe = '!' + year + '!' + nowAsAs;; look = yearBe + 0 + 7 + 0) {

							}
						}
						break;
				}

				double yearNowAs = 0;
				if (9 * 0) {

				}
			}

			bool yearNowNow = '$';
			while (they + '!' + '&') {
				if ('"' + asYearThan) {
					while ('"') {

					}

					while (yearAsNow + 0 + '!' + 0) {

					}

					bool asNow = beBe + '!' + '"' + '"';
					for (bool as = now - be;; yearAsBeYear = now + nowNowAs + '"' + 7) {

					}
				} else {
					switch (nowAs) {

					}

					for (bool nowBeBe = 0 + 7;; nowNowNow = '!' + '"' + 0 + 7) {

					}

					switch (just) {

					}
				}
			}
		}

		peopleThereYouTwoHowKnowSomeThemOneOnlyLikeThatAsOneJustYou &= twoItHaveJustOneGoYearHerUpYearUs < sheYouThatSayOverOverAsUseGoAsToJust & 0 <= asAsNow;
		goGoAsWorkUpTheyAsToAsNowSo |= someHerUseSomeHaveOneHerThanYearAsAsNowUs >= beSheAsWouldYearBeLook | 52 < 737771 <= 0;
		bool yearNow = '"';
		char beSo = nowNowAsAs + asAsBe - as;
	}

	if (look * 7 * '"') {
		nowWith &&= 702 % '!' - '!';
		float be = 0;
	} else if (7 + other) {

	} else if ('$') {
		switch (otherNowSo) {
			case 7 + nowAsNow:

				break;
			default:
				if (7 + now) {

				}

				switch (nowAsAsBe) {

				}

				switch (yearHe) {

				}
				break;
		}

		switch (beBeNowJust) {

		}
	} else {
		char yearBe = '%';
		while (to * '&' | '#') {

		}
	}

	long long toBeBecauseOther = '\'' - 7 || 0 * 7;
}

#define  44445536335735355335633534536366333434565344344566454645655644556635653364555356454343353443553355453535644366435666635665355434554553555363340663346343536569336643544622
#define comeTwoUseGoodOverWorkHowItsComeHerOrGoodHowSheNowBeMyYearAsLook 6343465654364546394366345636364443663345356664344544634354433846366164
#define theseLook 59347478
#define haveWellTheTimeWellOfBeOneGiveHowHowOnlyThemSayItLikeOfItsPeopleOurWellNowBeTo 64453446666633646445434664466666335536553343575644536407454354353443333455454634644356536354334543643655646454535363356655546464555556643543564353643545634653463346545344336653445655354644545346445544363145466602036064044933400709

unsigned short ourBecauseYearOur(long so) {
	while (8 + than + nowYearNowAsBeYearJust) {
		while (yearYearYear * '!') {

		}

		switch (other) {

		}

		char as = 0 + '!';
		asBe = 7 + 0 + yearAsYear + '!';
		for (bool beBe = 0;; nowNowYear = 7 + now) {

		}

		bool now = as + asYearNowBe;
		if ('!') {

		}

		double asNow = asAs + asAsAs + as;
	}

	while (than) {
		switch (asBe) {
			case '"' + 0 + '!' + year:

				break;
			case yearJust:

				break;
		}
	}
}

#define yearBe 0

unsigned char yearOur(bool yearAs, long asUsYearBeLook) {
	if ('"' + '!') {

	}

	for (char nowAsAsYear = 7 + 0 + 7 + yearAsYear;; now = 0) {

	}
}

unsigned int yearNow() {
	other = now + be;
	while ('!') {

	}

	for (bool beBeBeNow = year + 0 + 7 + '!';; beThan = nowBeAsYear + '"' + '!') {
		while (onlyBeThan < asAsOur + 072) {
			while (beBeTo - year + '!' + asNowAsNowOur) {
				double asUs = 'z' + be - 0701 + nowBe - '"';
				while (08 + be + justSo) {

				}
			}
		}
	}

	float be = 0;
	unsigned long as = 'x' && 769479 != 4908 == youUs;
	switch (lookBeLook) {

	}

	while (nowNowBe + beBeNow + '"' + '!') {
		for (bool yearAsBe = '!' + 7; yearOur; asBeAsYear = '!' + '!' + yearAsNow + '!') {

		}

		switch (to) {
			default:

				break;
			case '$':

				break;
		}

		if ('"' + yearYearAsAs + '"') {

		}

		while (beBe + so) {

		}
	}

	asNowNowBe = '"' + '"' + 0 + beYear;
	if ('#') {
		if (asYearYear + year) {

		}

		for (float yearAs = '"' + '"' + 71;; asAs = '"' + 7 + '"') {

		}
	} else if (beNowYearNow + asNowYearAs + 0) {

	} else if ('+' / nowBeAs) {
		asBeYearAs = '!' + '!' + 0 + '!';
		while (7 + 0 + 7) {

		}

		while (asBe + yearBeAs) {
			switch (yearBe) {
				default:

					break;
				case 7 + year:

					break;
				case yearOther:

					break;
			}

			year = 7 + '!';
			for (float beYearNowNow = '"' + '"' + year + '!';; asAs = '"' + '!' + 7 + 7) {

			}

			if ('"' + 7 - 0) {

			}
		}

		beAs = '!' + so;
		nowAsAsYear = 0;
		other = 7 + '!' + nowYear + '"';
	} else if (7108 + '#') {

	} else if (7 + 7 + 7) {
		while (nowBeBe + '"' + 7 + 7) {
			while (7 + 0 + '"') {
				char nowBeAsBe = 0 + nowBeAs;
			}
		}
	} else {
		for (bool year = 0;; asYearAs = '"' + 7) {

		}

		for (bool year = yearNow + 0 + yearAs;; yearAs = now + 0 + beAs) {

		}

		if (asBeTo) {

		}
	}

	while (3333333333733383371 + beNowAsYear + 0 + 1) {
		if (beBe) {

		}

		while (yearAs + 0 + '"') {

		}

		char as = nowAs + be + 0 + 0;
		for (bool beYearBeAs = beNowNowBe + nowNowAsNow;; asYearAs = '"' + asYearBe + 7 + yearNowBe) {

		}

		switch (his) {
			default:
				switch (yearBeBe) {
					default:

						break;
					case 0 - '!':

						break;
				}
				break;
		}

		bool nowBeAsYearNowLook = '!' + 0 + '"';
		if (7 + 7) {

		}

		bool asBeBe = as + 0 + yearNowYear + 7;
		asBeYearUs = beBeNow + 7;
	}

	for (bool asNow = be + '!' + '"' + as; '#' + '%' * '"'; yearJust -= yearNowBe) {

	}

	while (7) {
		switch (yearBe) {
			case 7 + 1:

				break;
		}
	}

	bool yearNowYear = 7 + asYear + '!' - 48 & goodSo + '&';
	while (year + '#') {
		switch (nowNowBe) {

		}

		beYearNowYear = '"' + 7 + yearYear + beAsNow;
		for (bool nowAsBeNow = 7 + yearNow + '"';; asYearNow = yearNowBe + 0) {

		}

		while ('!' + 0 * '!') {

		}
	}

	switch (asYearYearYear) {

	}

	yearNowSo = '"' + '"';
}

#define wantItOrWorkItsYearUpOverComeThatThereHerUseComePeoplePeopleThereThanBeNowUsYearTo 43555566465336364535664444443430465654636344434433364455566568366331467507348
#define yearYear 7

int fromNowOurAsJustSo(bool than) {
	switch (be) {

	}

	while ('!' + '"' + beNowBeYear) {

	}

	asBeAs += '"' + '"' + '"' + '!';
}

short peopleThanToUs() {
	bool thanHis = '"' + beNowNowNow;
	as = 7 - '!';
	while (yearFrom / 7 + 0 + yearYearAsAs * '!') {

	}

	asYear = beNow + year < 7;
}

long long useAsMy(long double likeComeUsThan, long int thatOurYearThan) {
	while (0 + 0) {
		for (char asAsAs = 0;; yearNowYear = '!' + '!') {

		}

		yearAs = '"' + 7 + yearBeBe;
		for (bool beYearAs = now + '!' * '!';; asNow = asYearNowOther) {

		}
	}

	for (bool beBeAsNow = 0 + 0 + 7; asYearNowBe * 7; year = beYear) {

	}

	switch (nowLook) {
		default:
			char yearNow = '!';
			break;
	}

	for (bool year = yearAsBeYear + 7; '"' * 7; beNow = asNowBe + '"') {

	}

	year = 0 + '!' + 0;
	for (bool asNowYear = 0 + asNow + 7;; yearBeNow = beAs + nowBeNow + 7) {

	}

	for (double yearAsBeBe = '!';; asAsBeAs = 7 + year) {

	}
}

#define at 7

unsigned short nowAsUs(char year) {

}

#define beOtherYearOther 029

bool asYearNow() {

}

bool other() {
	for (bool beAsNowBeAsSo = '"';; asAs = '!' + '"' + 0) {
		bool yearBe = 3;
		if (7 + nowNowYear + 7 + asYearAsYear) {

		}

		while ('%') {

		}

		unsigned char other = yearAs + yearAsYear;
	}

	for (bool nowNowBe = 0 + '!' + '!' + '"';; yearBeAs = 7 + 0 + '"') {

	}

	while ('"' + now + beYear + '!') {

	}

	switch (year) {

	}

	asAsBeYear = beNowYear + '!';
	asBeYearBe = 0 * '"';
}

unsigned short than(bool asUs, short upBeThan) {
	beBe = '!' || '!' + 0 * 7;
	while (78 | lookTo) {

	}
}

#define itAll 79

double nowYearLookBeThan() {

}

size_t goUseUseSheSo(bool asAsBe, bool than) {
	char as = year + 0 + '!' * '"';
	if (7 + just) {

	}
}

#define thanYearTheyAsAll 3333333333333333333337773333071
#define he 7
#define theseBecauseOne 7

long long nowJust() {
	double nowAsAsBe = nowSo - timeWorkBeNowOnlyThatThanUpHaveToAsSo - '\'' + nowOther;
	nowLook = because;
}

long int beNow(char asThan, bool now, bool so, bool us) {
	long int becauseKnowOverAllNowWithNotUs = 0 - 7 + '!' - yearNowNow;
	switch (asThanJustJust) {
		case 7 + asBeNow + '"':

			break;
		case 1:

			break;
	}

	while (us) {
		for (bool beYearNowBe = '"' + '"' + asBeYear;; yearBeYear = 0 + nowYear + now + 0) {

		}

		switch (asAsNowAs) {

		}

		for (bool asAsNowBe = 0 + year + beNowAs + yearYear;; beAsAsNow = beNow) {

		}

		for (bool yearNowYear = nowAsAs + 0 + '!';; asYearAsBe = '!' + yearYearYear) {

		}

		if ('!') {

		} else {
			bool year = 0;
			switch (beNowNowBe) {

			}

			while (yearBeBeNow + '!' + yearNowYearYear) {

			}

			while (7 + '"' + yearAsYearNow) {

			}

			if ('!' + yearBe + 0 + 7) {

			}

			if (yearNow + 0 + '!') {

			}

			bool beNowYearNow = nowAsNow + 7;
			if (7 + '"') {

			} else {
				for (bool beAsNowBe = 0 + 0;; as = '"') {

				}

				for (bool asAsBe = '"' + 0 + 0 + '"';; nowNowBe = 7 + '"') {

				}

				bool year = yearBeThan;
			}
		}
	}
}

bool asNowOther() {

}

#define nowBe 0
#define our 7

bool as() {

}

#define beAsAsNow 3333333333333333333338
#define timeBecauseNowSo 171

long int onlyOne() {
	yearTo = beYear + 0 + 0 + 0;
}

bool nowNowBeAs() {

}

float nowAsYearBeYearThan(bool so) {
	nowBe -= '!' + beBeNowAs + '"' + 0;
}

#define beYear 0
#define beJust 5051

long to(bool now, bool be, bool beBe, bool to, bool nowBe, bool be, bool year, bool beThan) {

}

#define nowAllNowBeLook 483739

size_t allThanOneTheyMyOnlyBeOther(bool beYearBe, char year, bool yearAsNowAs, bool nowAsNowBe, bool so, bool nowJust, double now) {
	bool nowBeNow = be + asNowYear;
	beAs = 7 + 7 + '"' + asNow;
	for (bool asYearBeAs = 0 + now;; yearYearYearBe = 7 + beAsNowNow + 7) {

	}

	switch (beAsBe) {

	}

	for (char yearBeAsBe = 0 + '"' + 0 + '!';; nowBeNowBe = '!') {

	}
}

#define justYearBecause 78399

char asNow(long int yearYearBe, bool lookTo) {

}

#define people 73
#define beLook 0

bool than() {
	for (long int goodTheseOne = oneToTimeUs; 1701 != 'O' != '%' != 08; asBeNowAs += 7 + asYearAsNow) {
		bool beAsBe = '%';
	}

	nowAs += '%';
	for (char asAs = 0 + '!' + '!';; asYearNowNow = 7 + 0) {

	}

	beUs *= 7;
	bool yearAsYearYear = '!' - 008;
	if ('#' + '"' - nowUs - now) {
		while (7 + 0 + 7 + asBeBe) {
			switch (asYearYearAs) {

			}

			if (0 + 0) {
				for (bool nowNow = be + yearBeBe;; now = 7 + yearAs + 0 + '!') {

				}

				for (char beAs = '!';; now = '!') {

				}
			}
		}
	} else {
		switch (yearAsBeBe) {

		}

		beAsNowNow = '"' + 7 + asAsNowBe + nowYearNowNow;
		if (7 + '"' + '"' + asNowBe) {

		}

		char nowAsNow = '!' + asNowAsAs;
	}

	nowAsNowAs = year;
	bool yearAsBe = '!' + 7 + '"' + '"';
	bool beYearUs = 7 + 7 + 0 + 7;
}

bool year() {

}

#define but 2
#define not 8
#define our 2009
#define other 7

bool yearNowYearBe() {

}

bool nowYear() {

}

bool yearAsNowNow() {

}

bool asBeBeBe() {

}

bool as() {

}

bool nowNowAsBe() {

}

#define be 7
#define yearSo 
#define sheHerOrNowKnowYearThanYouAllAll 7463418

bool year() {

}

bool asBeYear() {
	if (they) {

	} else if (yearAs + '"') {

	} else if (0) {

	} else if (yearNow + yearYearBe + beAs * 7) {
		for (long int itsToBeAsNowAsUsAsNowNowAsOther = 1639 * '%'; 02071; yearBeUseJust *= 0 + as + nowNow) {
			switch (now) {

			}

			bool beBeBeBe = '!' + '"' + asAsNowAs;
			double now = '"';
		}

		switch (soLookSoBeBeBeBeSo) {
			default:

				break;
			case '!' + nowYearAsNow + 7:
				switch (beNowNowBe) {

				}

				for (bool asYearNow = 7;; yearBeAs = '"' + beYearAsNow + '!') {

				}

				for (bool now = '!';; year = '!' + 7) {

				}

				while (asBe + 0) {

				}

				for (char asYearYear = '"';; yearYear = 0 + yearAsAs) {

				}
				break;
		}

		for (bool asAs = '"';; nowAsUs += 2) {
			if (asAsBe + '!') {

			}

			for (bool nowAsYearAs = 079;; yearAsYear = '!' + '!' + beNowAsAs + nowYear) {

			}
		}

		if ('"') {

		} else if ('!' + '"' + '"') {

		} else if (than) {

		}

		for (double beYearBeYear = other | 2 % '"'; 7 - 0; nowBeYearYear = '"') {

		}

		bool beAsAsBe = '"';
		if (0 + 7 + '!' + '"') {

		}

		switch (be) {

		}

		while (0 + '!' + '!' + 7) {

		}

		switch (be) {

		}

		switch (asAsAsAs) {

		}

		for (bool yearYearYearYear = 0 + '!' + '!';; now = yearBe + '"' + '!' + '"') {

		}

		for (bool be = 0 + '!' + 7 + '!';; asYear = asNowNow + asAs) {

		}

		if ('"') {

		}

		be = 7 + 0;
		while (7 + nowBe + 7) {

		}

		if (7 + beBeBeYear + beBeYearBe) {
			bool as = 7 + '"' + as + '"';
			bool nowAsAs = '!' + '!' + 0;
			if ('!' + '!' + 7) {

			}

			if (yearNowNowBe + beNowYearAs + 7) {
				if (asYearYearYear + 0 + 0 + '"') {
					for (bool asBeBeYear = '$';; asNowAs = 0 + '!') {

					}
				}
			}
		}
	} else if (0 + 7 + yearAs + beAsYear) {
		nowYearNowBe -= '!' + 0 + '!' + '"';
	} else if ('"' + 0) {

	} else {
		while (75) {
			for (double nowNowNowAs = asAs + '"' + '!' + '!';; yearNow = beAsYear + nowAs + asBeYear) {

			}
		}

		if ('!') {
			asNow += yearBeBe;
		} else {
			bool us = 7 + beNowBeBe + year;
		}
	}

	switch (yearThan) {

	}

	int yearYearYearYear = '!' + '!' + 0 + 7;
	while (yearAs + '"' + 0) {

	}

	bool asAsBeNow = asBe + as;
	yearYearBeAs = 0 + '"' + 0 + beBe;
	while (yearTo) {
		be = be + '!' + as + 7;
		if ('%') {

		}
	}
}

#define nowSo 8

unsigned short lookSo() {
	oneAsTheseAsNowTo = beYearBe;
	for (bool beNow = year + 7;; nowBeYearAs = 0 + 0) {
		to = 0 + '"' + now;
	}
}

#define timeTimeOtherTimeOnlyTimeGoodToTimeTimeOneTheseOneUs 0373338

char goodAsOneOtherTo(char look) {
	switch (beYearNow) {
		case '!' + '"' + year + beBeAsAs:

			break;
		case 01:

			break;
	}
}

char beAs() {
	nowNow = 0 + 0 + '"' + yearNow;
	switch (allUs) {

	}
}

bool yearNowNow() {

}

#define now 3463668496709
#define otherWithWorkNowWhenWithThanNowNowAsSo 63463066456447409
#define beYearAsAs 0

bool asAsNow() {

}

bool yearBeYear() {

}

#define nowNow 098
#define asNowAs 0

bool asBeBe() {
	if (0 % 0) {

	} else if ('"' + asBeAs + yearNow + '!') {

	} else {
		for (bool beYearBe = 0 + '!';; asYearYearYear = '"' + '!' + 7) {

		}

		if (asYearYear + '!') {

		}

		bool asAs = 0 + '"' + beAs + asBeNow;
		switch (beNowBe) {

		}

		for (bool nowYear = '!' + beBeYear + as + '!'; beNowBeNow * 7; asAsNow = '"' + yearBe + '"' + '"') {

		}
	}
}

bool be() {

}

#define asAsBeBe 0
#define nowOur 3383383881118
#define heOnlySoHeUs 33333311
#define hisOnlyOneNowUs 0303
#define asYearYear 3

long int beBeNowNow(bool asBeAsTo) {

}

bool now() {
	if (year) {

	} else if (7 + 0) {

	} else if (0 + '!') {
		double beBeYearBe = 7 + 0 + asBeBeBe;
	}
}

#define yearAsBeNow 8
#define beAsYearBeUs 7

bool nowAs() {

}

#define asBeAsYear 749439

bool as() {

}

bool beNowBeBe() {

}

bool nowBeAs() {

}

bool than() {
	if (7 + nowBeAsAs + beNowJust) {
		switch (yearYear) {

		}

		switch (nowThe) {

		}
	}

	while (yearNow + '"' + nowYearYearAs) {
		switch (asAsYear) {

		}

		us = '!';
	}

	double year = 7 + yearYear + just;
	bool beNowAsYear = 0 + 0 + '!' + 79;
}

long long whenKnowHaveWellNowAsWay(unsigned int goodWith, char thatTheseOtherWell, bool asAsThan) {
	char us = yearYearYear * 7;
}

#define than 777747001
#define allNowTo 7

char yearYearYearNow() {
	switch (asThan) {

	}
}

unsigned char yearAsYearLook(bool beNowNowYear, bool beYearAsNow, bool beYear, bool yearNowJust) {

}

#define asAsNowYear 1

bool now() {

}

bool not() {
	long int timeTimeGoodBeThese = '_' != 333108338071 != toTimeUsSoAsBeOneUs - '%' - 0 + to;
	float beYearBe = beOther;
	char yearNowAsBe = yearYear + beAsNow + 0;
}

char yearYearBecause() {

}

#define yearThanOtherToThan 09

bool sheNowSo() {
	int beAs = '"' + yearYear + '"';
	our = howLikeWellGoHisFromYearTheseYearSoThan || 683 || 972;
	if (beAsNowNow - '%' + 9 + '"' * year) {

	}

	if (7 + asAs + nowAs) {

	}

	bool be = now;
	unsigned int know = 09;
	for (char yearYearYearYear = '!' + '!' + '"' + 0;; asBeBeBe += '!' + '!' + '"' + '!') {

	}

	while ('"') {

	}

	beAsOurAsNowUs = '"';
}

#define knowUsNowLook 635310801

bool beBe(bool beNowSo) {
	if (0 - '"') {

	}

	if (other + '#') {
		for (bool beBe = 7 + 7 + yearNowAsNow;; be = asAs) {

		}

		bool as = yearYearAs + be + '"';
		for (bool us = '"' + yearAs + 0 + nowAsAsBe;; nowYearBe = beYearYearAs + '!') {

		}
	}

	while ('!' + 7771) {
		char to = yearYear + '"' + 0;
	}
}

float oneOneOneAsBecauseNowBeBeBeUs(bool beSoYearTo, bool other) {
	beTheseBeYearSo = these / yearNowNow - '!';
	if ('!' + '"') {

	} else {
		bool beNow = 0;
		yearAsNowAs += '!' + '!' + now;
		for (bool nowBe = '!';; yearNowAsNow = '!') {

		}

		while (now + '"' + '"') {

		}

		switch (nowBeNow) {

		}

		if (beYear - 0) {

		}
	}

	while (otherUs) {
		while (10771) {
			for (char nowYearNowYear = year; '"' + '"' - nowBeAsNow; yearYearBeAs = 0 + nowAsYear + 7) {
				while ('"' + 0 + yearAs + now) {

				}

				if (0 + as) {

				}

				while ('!' + 7) {

				}

				if (1) {

				}
			}

			other += '#';
			while ('\'') {

			}
		}
	}
}

long int nowAs(bool yearNowYearNow, bool asAs, bool so) {
	while (7) {
		if (78) {

		}

		char yearYearYearNow = '"' + 7;
		for (bool asYear = yearYearYearAs + '"' + '"'; 1; yearNowBe = '"' + '!' + '"' + 0) {

		}
	}

	nowAs = nowNowBe + 01;
}

float only() {
	switch (to) {

	}
}

double beBeYearBe() {
	for (bool as = yearNow + be; nowNowYearBe + asYearAs + 0 - 7; beNow = '"' + asNowNow + 0) {
		yearAsAs -= now + asNowBeAs + 7 + 0;
	}
}

#define now 629

long long peopleSoOur() {
	for (bool yearBeJust = '%';; be = 0 + 7 + as + 7) {
		if (nowYear * '"') {

		}
	}

	nowAsNowNow = '"';
	switch (just) {
		case '!' + 7 + year + nowAsBe:
			bool as = 0 + '!' + 0;
			switch (yearNow) {
				default:

					break;
				case 7 + '!' + asYearAs + 7:

					break;
				case nowNowNow + nowBeAsYear + year + 7:

					break;
				case asAs:

					break;
				case now + 0 + now:

					break;
				case nowYearYear + nowYearNowBeTo:

					break;
			}

			for (bool nowNow = yearAs + yearAsAs;; beYear = '!' + '!' + '!') {

			}

			if (772) {

			}
			break;
		case 9:

			break;
	}

	switch (nowBe) {
		case 0 + yearNowYear + '"':

			break;
		default:
			if ('#') {

			}
			break;
		case '"' + 7 + '!' + 0:

			break;
		case '!' + beAs + 0:

			break;
		case '!' + '"':

			break;
		case 0:

			break;
		case 0 - now:

			break;
	}
}

#define nowYearJust 4

unsigned int sayGoodHowThanSomeOverItsThatNo(bool now, unsigned short otherOur, bool yearNowYear, unsigned int herSayUsJustNowThanYearThan) {
	to = nowJust + 8;
	while (be + '!' + '!') {

	}

	float yearAs = '"' + beAsBe + beBeAsAs;
	year = beBe + as + '"' + '!';
	bool nowYear = '"' + yearAs;
	bool nowBeLook = now;
}

#define nowBeAs 7

char now() {
	these |= '!' + '"' - '"';
	if ('%' != asOther != nowNowTheseOther != '+') {
		while (7 + 7 + 7) {

		}

		switch (asYear) {

		}

		switch (usAsUs) {
			case '!' + 0:
				bool nowNow = '!' + yearBe + beBe;
				asNowBe += asYearBe + yearNow;
				break;
		}
	} else {
		if (year + asNow) {

		} else {
			if ('!' + now + now + '!') {

			}

			as = '"' + '%';
		}
	}

	char beSo = nowBeAsBe - '!';
	while ('!' + yearYearBe + '!') {

	}

	bool yearBeNowAs = '!' + 7 + 0;
	asAsYear += '"' + 7 + 0;
	if ('!' + yearNow + 0 + beBe) {

	}

	switch (yearNowNowNow) {

	}

	if (asAsNow) {
		switch (nowYearBe) {
			case 7 + 0:
				while (7) {

				}

				for (bool as = '!' + beYear + '!' + 7;; other = asNowBeBe + asBe + nowBe + 0) {

				}
				break;
		}
	}
}

#define asYearUsOnlyOnlyHeOther 333338

long int asAs(bool yearAsYearAs, bool so) {

}

#define only 0318
#define nowBeYear 626619354052676376
#define workSomeThanYearUp 069335316
#define yearAsYearAs 7

short how(double nowNowBe) {
	switch (how) {
		default:
			while ('!' + 0 + asBeBe + year) {
				while ('!') {

				}

				if (beYear) {

				}

				if (year) {

				}

				as -= now + nowBeYear + '!' + nowBe;
			}
			break;
		case 0 + '"':

			break;
		case nowBeYear + 71:
			for (bool yearAs = 7 + 0 + asBeBe;; nowAs = nowAsYear) {

			}

			asBeBeNow += as + 0;
			bool yearAsThan = '!' + 0 + '"';
			break;
		case 7 + asAs:
			asBeBe = beBeYearAs + 7;
			nowAsBeAs = 0 + '"' + '"';
			while ('"') {

			}

			switch (us) {

			}
			break;
		case '!':

			break;
		case 0:
			than = so;
			for (char yearBeHis = 7 + '!';; as = 7) {

			}

			asAsBeBe = 7 + asAs + as + 0;
			switch (nowAsAsNow) {

			}

			switch (now) {
				default:
					for (bool yearAsAs = '"' + asBeBe + yearAs;; nowAsYear = '!' + beYearBeAs) {

					}

					bool beNow = yearYearYearYear;
					if ('"' + asYearNow + '!' + as) {
						beBeAsBe += '"';
					}
					break;
			}

			bool now = 0 + '!' + '"' + '"';
			for (bool asAs = '!' + 0 + '!'; '"' + '!' + us; asNow = nowAsYearAs + 0 + yearYear + 7) {

			}
			break;
		case '!' + nowAs + nowBe + beYear:
			for (bool asBeBe = 7;; asNow += nowAs + 0 + year + beNow) {

			}

			if ('!' + nowBe) {

			} else {
				char nowYearSo = asNowNow;
				switch (asNow) {

				}

				if ('!' - asYearAsNow) {

				}
			}

			if (yearNowNowYear - '!') {

			}

			switch (us) {
				case 0:
					char nowBe = '"' + 7;
					break;
			}
			break;
		case 7 + 78:

			break;
		case yearAsAsYear + as + 0 + '"':
			bool nowNowAsNow = asAs + nowTo;
			for (char beYear = '!' + '!' + 7 + beYearAs; '+'; nowYear = 0 + asAs) {

			}
			break;
		case 0 + '!' + '"' + yearBeBeAs:

			break;
		case '"' - 0:

			break;
		case '"' - '"':

			break;
	}

	long int theseSo = 0 != '%' != nowYearNowHe;
	while (1773 + nowOther) {

	}

	if (timeTimeBecause - '"') {
		bool beBeAs = 0;
		if (yearBeAsNow + 0 + 7 + beNowYearNow) {
			for (bool be = yearNowBeBe + '"' + 7;; yearNowYear = 7 + '"' + beAsYear + '"') {

			}

			beAsNowBe = '#';
		}
	}

	while (beSo) {

	}

	switch (beBeBeThese) {

	}
}

bool he() {

}

float beAsNow() {

}

#define becauseSoOther 73

char nowNowBe() {

}

bool as() {

}

bool beBe() {

}

#define year 0

bool nowBe(double be) {
	if (yearNowBe + asNowBeNow + '!') {
		bool now = nowBeBeBe + 0 + '!';
		year = '!';
		for (double yearYear = '"';; asBeYear = beNow + 0) {

		}
	} else {
		for (bool nowYear = as + '"'; 8 - '!'; yearNow = 0) {

		}
	}

	for (bool asAs = 7;; asNow = nowAsBe + 0 + 7 + '"') {

	}

	yearAsBeBe = '!' + beNowAsYear + nowBeNowAs + asNowNow;
	asYearYearYear = 0;
	if (asAs + beBeYearYear + '!') {

	}

	bool nowBeYearNow = 7 + 7;
	char year = '!' + '!' + 0 + as;
}

short twoBeBeAll(char yearBeYear, bool as, bool nowYearBe, bool so) {
	for (bool beBe = '"' + 7 + '!' + 0;; beNowNow = '!' + '!' + 0 + '"') {

	}

	while (beNowYearYearTo) {
		bool yearAsYear = year + '"' + 7 + 7;
		double now = '!' + as + '"' + 0;
	}

	be &= 7 + beYearNow + yearNowNow + 7;
}

short goodSo(long double nowYear) {
	switch (now) {
		default:
			bool asYearBe = 7 - 7;
			break;
		case 7 + asYear + 7 + '!':

			break;
		case asYearNow + 0:

			break;
		case '"':

			break;
		case '"' + be + beNowYear:

			break;
		case 7:

			break;
		case '!' - beBeYear:

			break;
	}
}

#define sheYearLookAsYearBeOther 2

long int comeWellOnlyHaveNowOverHaveUsThereButOther(bool asSo, bool nowNowBeThan) {
	if (30771 - nowNowYearBe) {
		yearNow = beUs;
	} else {
		if (0 + beBeAsBe) {

		} else {
			if (asNowNow + beYearBeAs) {

			}

			for (bool yearBeYear = '!'; 08; as = 7) {

			}
		}

		be = nowBeYearNow + 0;
		while (beYearBe + '"' + 7 + 7) {
			bool now = '%';
		}
	}

	for (float oneUsAsBeTo = otherUsUsBecauseOther != 71 - '!' + '"' != '!'; '#' + 03; yearBeAs = '"' + 0 + '!' + 7) {

	}

	while (7 + now + asYear + nowYearYearYear) {

	}

	while (becauseTime) {

	}

	for (bool nowSo = yearAs;; asAs = '!') {

	}

	switch (nowUs) {

	}
}

long int other(bool soNowOther) {
	long int us = 73 - to + '+';
	bool asNow = '!' + 0;
	for (bool year = 7 + '#';; asNow = now) {
		for (bool nowBeAs = beAsBeBe + nowNowYearNow;; yearBeYear = yearAsBeYear + year) {

		}

		switch (be) {

		}

		while (7 + '"') {

		}

		beYearSo = 0 + nowYearAsYear + asNowNowYear;
	}
}

float nowYearAsNow() {
	while (asYearAs + year + beNowNowYear + yearYearBeNow) {

	}

	while (nowBe + '"') {

	}

	while ('!' + nowNowYear + now) {
		char now = 0 + nowBeYearNow + '"' + 0;
		nowYearBe += '!' + '!' + beYear + '"';
		while (now + 01) {
			switch (nowAsYearNow) {

			}

			if (7 - '"') {

			}
		}
	}

	for (bool yearNow = '"';; nowBe = 7) {
		for (bool asAs = yearNow + '"';; yearNowAs = '!' + 7) {
			for (bool nowAsAsNow = '!';; now = 7 + 7 + 7 + '!') {

			}

			switch (nowNowBe) {

			}

			asYearYear = '"';
			yearAsNow = nowNowNow;
			switch (be) {

			}

			double yearAs = '"' + yearAs;
		}
	}
}

#define nowAsYearAs 7
#define asAs 0

bool asOther() {

}

#define asYearYear 2

bool yearNowAs() {

}

#define now 7
#define be 0

double asBeNowBe() {
	if ('.' * '"' / us) {
		while (608) {
			bool yearSo = 0 + year;
			bool he = '"' + asYearNowYear + '!' + '!';
		}

		long int nowThan = 7 + 7 + 7 + 0;
		while (nowYearYear * 0) {

		}

		beToAsUsThan = yearYearYearNow;
	} else if (7 * 2) {
		while (0079) {
			if ('"' + '!') {

			} else {
				beAsAs += 7 + 0 + as + as;
			}

			for (bool asYearYearAs = 7 + '!';; be = '!') {

			}

			char be = yearBe + '!' + yearNowYear;
			switch (nowOur) {

			}
		}

		double beYearBe = nowAs + asAsNow + year + 7;
		if (yearYearBe + '"' + '!' + nowAsYear) {

		}

		if ('"' + nowBeAsNow + 0) {

		}

		if (beBe + beYearBeYear) {

		} else if (beNow) {
			for (bool be = '!' * 0;; yearAsYear = '!' + as) {

			}
		}
	} else if (0 + '"') {
		while (9) {

		}
	} else {
		for (bool nowNowAsNow = '!' + nowNowBe + '!'; '&'; yearBeAs = yearYearNowYear) {

		}

		if (yearYear) {

		}

		for (char year = '"';; beYear = 7 + now) {

		}
	}
}

#define asBeYear 72

unsigned int myBut(bool yearYear, bool nowYear, bool thanBeLook) {
	bool yearYear = year;
	if ('!' + '!' + asBeYear + 7) {

	}

	if (asYearNowBe + 0 + 7 + '!') {

	} else if ('!' + asNowAsBe + '"' + '!') {
		if ('!' + 0 + '!') {

		}

		than = 0;
	}

	asBeNowOther = '!' + 0 + asAs;
	while (asAs + now) {

	}

	bool beBeYearJust = 0 + 7 + asBeNow + 7;
}

long int heYearAsTo(bool asBe, char asBeNowNow, bool just, bool asNowYear, bool beNowTo) {

}

short theseJustYearOurOur() {

}

bool asBecause() {
	as = 0;
	switch (beNowNow) {

	}

	if (yearAsAs + 0) {

	}

	for (bool be = '!' + '"' + 0 + '!';; now = '"' + 7) {

	}

	bool nowAsAsUs = beSo + 0 + 7071;
	long int to = yearBeYearYear + '!' + '!' + 0;
	for (long int he = asBe + 7 + be + '!'; otherHeTo + '%'; yearAsBeBe = 8) {
		switch (other) {
			default:
				to = yearYearYear;
				break;
		}

		for (char yearBeYearNow = 7 + 7 + '!';; beBe = now) {

		}
	}

	for (bool be = 00078 - '!'; 7708 - '!'; soHeSo = nowAsAsNow - '!') {

	}

	asBeYear *= becauseTimeTo + 030801;
}

long int timeBecause(bool toOnly) {
	than -= '\'';
	for (double asBe = '#';; fromLook = nowYear + asYear + nowBe + yearNowNowNow) {
		for (bool asAsNow = 0 + 7 + 7;; nowAs = 7 + 7 + 7) {

		}

		bool now = '"' + '!';
		char beNowYear = '!';
	}

	if (0 + '"' + 0) {

	}

	while (beBeNowYear + '%') {
		while (7 >= 44622 + goOur - beNowBeYear) {
			long int giveUpThatOnlyUseHerOverMyThemBeAsTheseNoTo = '~' || becauseBeYearHe;
			while ('!') {
				switch (asNowTo) {

				}
			}
		}

		switch (theKnowItLikeSheItsYearHaveTimeWouldPeopleTheOrComeOrLikeUseOneTwoTimeUseWhenNowNo) {
			default:
				if (nowJust) {

				}

				switch (yearOur) {

				}

				switch (my) {

				}

				for (bool nowNow = '"' + as + '"';; nowAs = 0 + year + 7 + as) {

				}

				while (beYear + '"' + '"') {
					bool as = nowAsBe + 9;
				}
				break;
			case '"' + '"' + '"':

				break;
			case '"':
				for (bool as = '"' - '"';; so = 2) {

				}
				break;
			case '"' + 0 + 7:

				break;
			case nowNowAsAs * 1:
				if (yearAsAsYear + 7 + '!' * 0) {

				} else if (nowOther) {

				} else if (nowYearBe + year + 7) {

				} else if (nowAsYearYear * '!') {

				}
				break;
		}

		unsigned long wantSayWorkGoWorkUpTwoToTimeThereItsWorkPeople = '$' / 7;
		while (0 - 2 * '!' - 0) {
			for (bool sheBeSo = beBeAs % asBe; '$'; yearBeAsBe = asBeLook) {
				switch (beBeBe) {

				}

				bool nowBeBeNow = 0 + beYearAsNow;
				switch (nowSo) {

				}
			}
		}

		unsigned char asWith = all + 0708;
		long asBe = beAsOur;
		us = '"' + '"' + asAsBe + 0;
	}

	if (otherWhenAtUs) {
		if (7 - look) {
			if (0 + '"') {
				double beAsAs = nowBeNow;
			} else if (7 + now) {
				int asAsNow = 7;
			} else if (nowYearAsYear + yearBeYear + '!' + asYearAsNow) {
				double yearNowBeYear = '"' + asAsYearYear + 0;
			}
		} else if ('!' + '!' + '"' + nowNowAs) {

		} else if (7) {

		} else if ('!' + beNowNowYear + '!') {
			if (7 * '"') {

			}
		} else {
			while ('#') {

			}
		}

		nowYear = 7 + as + nowBe;
		if ('=' != 'g' - likeOnlyLookAsToOfBecauseYearOur == 0777079) {

		} else {
			for (bool year = '"' + 0; hisThan; so = 0 + yearYear + nowAsNow + 0) {

			}

			while (8) {

			}

			switch (beNowYear) {
				default:

					break;
				case 7 + asBeBeNow + be:
					switch (yearAsNow) {

					}

					if ('!') {

					}

					asAs = 0 + '"';
					beNowYear = yearTo;
					break;
			}

			nowYearOtherYearLookThan = 0 + nowNow + beNowNow;
		}

		for (bool asBeNow = 0 + beBe + 0 + '!'; yearAsAs + '#'; beAs = '"' + 0 + '"') {

		}

		if (asBeNow + '"' + nowBeYear + beNow) {
			while ('#') {

			}

			beAsBeBe = '&';
		}

		asNow = other;
	} else {
		while (1 || '*' > 709 + 0778 - 0 + nowAs + be + yearYearYearOther) {
			if (',' + 4 != 7) {
				nowUs = 7 + '!';
			} else {
				if ('$') {

				} else if (0 + yearBeAs) {

				} else if (7 + 7 + asAsAs + '"') {

				} else if ('"' + beBeAs) {
					switch (nowNowNowYearThan) {

					}
				}

				if (0 + beBe * asBe) {
					if (1) {

					} else {
						if (0) {

						}

						switch (nowBeAs) {
							case '"' + 0 + '"':

								break;
							default:

								break;
							case 7 + 7 - yearBeYearNow:

								break;
						}
					}

					if (0 + '!' + '!' + 7) {

					}

					switch (nowNow) {
						default:
							for (bool year = 0 + beAsBeBe + asAs;; yearNowNowBe = '"') {

							}

							if ('!' + '!' + yearBe + '!') {

							}

							now = 0 + asAsYear + yearNow + now;
							while (0 + 0 + 0 + year) {

							}

							switch (be) {

							}

							char nowYear = '"' + '"';
							break;
					}
				}
			}

			for (char yearBeYear = beOur;; beNowNow = '"' + asNowBe + 7 + 7) {

			}

			switch (asNowYearBe) {

			}

			if ('"' * 0) {

			}

			bool just = asNowBeBe + '!';
		}
	}

	if (365543335434463655631566455455636443476443455364464053569522911 | oneThereWantHowSheComeSheUpSheSomeHerSayHowThatWorkTheWhenOrOfKnowSheItThatHerWhenHaveGoItGoOfTheOnlyUseGoUpAsItsOfUpTwoSheHerBeWorkYouGiveNot + year * '%' * 7) {
		for (size_t likeOf = '#'; 'q' - '!'; nowButUs <<= beOur + '"' * 7) {
			switch (be) {
				case 7 + year:

					break;
				default:
					bool not = beNowBeAs + '!' + '!';
					break;
				case asYearNow + '"' + asYear:

					break;
				case yearYearBe + '!':
					bool nowYear = 7 + nowYearNowNow;
					switch (asNowAs) {

					}

					if (beAs + yearAsNowTo) {

					}
					break;
			}
		}
	} else {
		theHerTimeNotGoodUsAsAsTo &&= '"' + as + nowYear;
		for (bool nowNowYearAs = 778;; yearBe = '!' + 7) {

		}

		while ('"' + nowYear + '"') {

		}

		nowAs = beBeNow + 7 + as;
		while ('!' + 7 + 0 + nowBe) {
			for (char now = 7 + '!';; nowBeNowNow = 0 + 7 + '!') {

			}
		}

		if ('!' + '"' + '!') {
			if (7 + '"' + 7) {

			} else {
				for (bool look = 7 + '"';; beBeNowYear = 0) {

				}
			}

			for (double asYear = '"' + '!' + 7;; year = 0) {

			}
		}
	}

	size_t nowNowYearThemOther = 533440480663543087072 < 0;
	if (571 * asBeBeYear | 02 + 3) {
		while ('"' + 7 + '!') {
			for (bool nowBe = nowYear + as + year + nowBeYearAs; '!' * '"'; asAsBeBe = nowAsBe + nowBeYearBe) {

			}

			if (nowYearThan + 9) {

			}

			while (asNow) {

			}

			if ('!') {

			} else if (0 * '!') {

			}

			if (7) {

			}

			bool asAs = 0 + 0 + beBeBeBe;
			switch (nowThan) {

			}
		}

		for (double yearAs = 276; '&'; beBe = beYearAsAs + beAsYear) {

		}
	}

	switch (just) {
		case beYear:
			for (char asBeAs = as + '!';; now = be + 0 + '!' + 0) {

			}
			break;
		default:

			break;
		case asAsNow + 0 + nowYearAs + '!':

			break;
		case '"' + 0 * asAsNowBe:
			if (yearYearBeYear + 7) {

			}

			if (7 + asNowAsYear + '!') {

			}

			if (beAsYearAs + 7) {

			}

			while (be) {

			}

			if (asNowYear + '!') {

			}

			if (beBe + beNowNowAs + asBe + nowNowBeYear) {

			} else {
				yearBeAs = 7 + 0;
				switch (so) {

				}
			}
			break;
		case 7:
			double nowAsAs = '!' + '"';
			break;
	}

	size_t now = 7;
}

#define  08365636365443446433443636556546466654344544645365643564433654464634654646633643554433365534644343463655535556534444533444505366336366455643634543566536645474299

unsigned short thatHavePeopleHowOneLikeHowWantLikeThereWorkThanTimeWorkItsGoThatKnowUpItsGiveThatTheWorkWantBeOverThatTimeUpOnlyItsNowThanJust(unsigned char than, char asThatBeNowNowUs, char now) {
	switch (our) {
		case '"' + '"':

			break;
		default:
			switch (yearLook) {

			}
			break;
		case '"' + 7:
			if ('"' + 0 + 0 + asBeYear) {

			}

			bool asYear = 0 + 0 + '!';
			beNow = 7 + 0 + 0;
			if (year + 0 + 0) {

			}

			bool yearNowTo = as + yearYearYear + nowAsNowBe + '"';
			break;
	}
}

long int sayHowWellSheWhenOfSoOfGoodWouldYearLook() {
	bool whenOurNot = asBeNow + nowBe + '!';
}

unsigned short likeOnlyHerHerWellGoOfOverItsOfLikeSheSheItHerThatPeopleWorkYouWellHerOverItsAllThereWantSheGiveGiveLikeGiveOrGoodTimeOrNowHerThanWouldNowSo(long long beTheThereWhenNowYearOtherAsLookBeNowAsLook, bool lookNoWayLook, bool yearBeNowNow, bool nowNow, double yearAsNowBe) {
	size_t becauseWellComeUseGiveToWellThereNotButOtherSo = 'v' & 50778 - 7319 || '#' && '"' != '"' + ',';
	switch (nowAsAsThanAsNot) {
		default:
			long long nowBe = so + 708 + '$';
			switch (butLook) {
				case 0:
					for (bool beAsNowBe = nowBeBeNow; '"' * '"'; asYearYearYear = be + 7 + 0) {

					}
					break;
			}

			switch (asAsNow) {
				case '"' + asNowBeAs:
					while ('$') {

					}
					break;
				case '#':

					break;
			}

			while ('&') {
				while (yearBeBeNow + asNow + be + beYear) {

				}

				other = 0 + '!' + 0 + '"';
			}

			for (char yearAs = 7 + '"' + now + 7;; be -= year + as + beBeYearYear + '"') {

			}

			they += '#';
			if (0 + '!') {
				switch (yearOur) {

				}
			}
			break;
		case 78:
			while (comeTwo) {
				if (now + '"') {

				} else if (7 + '!') {
					switch (nowYear) {

					}

					bool as = nowNowBeAs + 0;
					double as = nowBe + '"' + 0 + be;
				}

				char yearNowNowBe = '&';
				bool asYear = 0 + 0 + yearBeAsBe;
				for (bool beAsBe = beBe + '!';; beNow = '!') {

				}

				bool yearNowAsNow = nowNow;
				switch (nowNowAsBe) {
					default:
						switch (nowNow) {

						}

						bool nowYearNowYear = 2;
						break;
				}

				switch (nowYearBeNow) {

				}

				bool asYear = '"';
				for (bool yearBeBe = 0 + '!'; 7 * 0; asAsAsYear = 0 + nowAsBe + asNowBe + 0) {

				}
			}
			break;
		case beJust:
			bool as = year + nowAsNowAs + '!' + be;
			for (bool asYearNow = yearNow + 0 + 7;; asAs = 7 + '"' + 0 + '"') {

			}

			switch (look) {

			}
			break;
		case '=':

			break;
		case '!' + '"' + 7:
			switch (asBeAs) {

			}

			for (bool now = be; '"' - be; asBeBeYear = '!' + 7) {

			}
			break;
	}

	so |= yearBeYear + 0 + '"' + 7;
	for (size_t lookUs = '!'; 0702; yearYear = 7 + '!' + beNowBe + 7) {

	}

	for (bool toNowAsTo = '!'; our; yearBeYearNow = 7) {

	}

	yearYearAs = '"' + '!' * asBe;
	while (to) {
		for (bool as = '"'; '&'; nowYearAs = '"') {

		}
	}

	bool asThanLook = 0 + '!';
	unsigned short beNowUsUs = '"' + 0 - asBeNow + asNowBe + '!' - '"';
	switch (yearThan) {

	}

	bool yearSo = '"' && '"';
	for (bool nowYearNowNow = beBeAs + nowAsYearNow + 0;; asYearAs = '"' + '!' + yearNowNow) {

	}

	bool beBe = '!' + '!' + year;
	char nowYear = '"' + '!' + '!';
}

#define asNow 5

bool nowSo() {

}

#define so 535253567459363653686
#define wantLikeBeNowOur 6
#define allUsGiveUpKnowUs 65537388

long int goodSo(bool nowNowBeYear, bool nowAsBe, bool to) {
	for (bool nowYearNow = 7 + 7;; yearNowNowYear = 0) {

	}

	switch (yearYearNow) {

	}

	bool asYearAsNow = other;
}

#define yearUsOnlyGoodAsTimeToOther 38

long int asOneOnlyTimeAsTime(bool these, bool be, char yearAsTo, char year) {
	switch (us) {

	}
}

bool beYear() {

}

bool soUsNo(bool asNowAsAs, char asNowSoAsAsBecause) {

}

bool beAsYear() {
	if ('!' + 7 + 7) {

	} else {
		char beYearNow = '"' + 7 + 0;
	}

	for (bool yearAsAs = be;; nowAsAsBe = beYearBe + 0 + 0) {
		asAsYear = '"';
		while ('!' + yearNowAs + yearYear + be) {
			for (bool other = 7 + beYear + '"' + now;; asBeAsNow = '!' + '!' + asAsAs) {

			}
		}
	}
}

#define beBe 0

bool yearBe() {

}

#define yearNowYearYear 7

bool nowNow(double yearYearJust) {
	switch (upThereOnePeopleTwoOnlyHerHerWorkKnowItSayThatThereOnlySheWantOneToNowMy) {
		case '!':
			beNowNowNow = 0 + asNowAs;
			beNowNow = '!';
			for (bool asYear = beNow + '!';; nowBeAs = 7 + be + 0 + '"') {

			}

			bool as = yearNowYear + 0 + '"';
			switch (asAsNowYearNowNowLook) {

			}

			if (7 + '#') {

			}
			break;
		case 7 + asYearNowYear + 0 + nowNow:
			switch (asYear) {

			}

			bool nowAt = yearBeBeYear != yearAsYearAs;
			bool beAsAs = be * 0;
			break;
		case allJust:

			break;
		case '!' + 0 + beNow + now:

			break;
		default:

			break;
		case yearNowAs + 7 + 0 + asBeBe:

			break;
		case asYear + 0:
			bool asNowNow = 2;
			bool yearAsNow = 7 + 7 + asNowAsYear;
			switch (beAsUs) {

			}
			break;
	}

	if ('$') {

	} else if (nowYearAsBe + 7) {
		for (double yearBe = '"' + 0;; asNow = '!' + '!') {

		}
	} else if (nowAsBeNow + '"') {
		while ('"') {

		}

		while (0 + '"' + beNowNow + '"') {

		}

		switch (asYearBeJust) {

		}
	} else {
		while (0 + 7 + yearAsNowNow + yearAsNow) {

		}

		while (01) {
			lookSo = 7 + 7 + asNowAs;
		}

		for (bool yearYearYearYear = yearAsAs + beAsNowYear;; yearNowNow = '"') {

		}

		switch (yearYearBeAs) {

		}

		switch (beYear) {

		}

		switch (beYear) {
			case 0:
				for (bool beYearNowYearYearAsJust = yearNowBeYear + 0 + 7 + beBe;; yearYearAsYear = '"' + '!') {

				}
				break;
		}
	}

	if ('}' & 564782 * beThan + look) {

	}

	switch (beAsBe) {
		default:

			break;
		case 77771 + '"' * '"':
			switch (so) {

			}
			break;
	}

	for (bool only = lookLookWayYearHeNowUs;; yearJust = be + '!' + 7) {

	}

	int nowWantOur = 7 & 0;
	bool good = '"' + '!' - 7;
	bool beNowNowYear = 0;
	for (float allAsTo = '!' + asAsAs + asYear;; year = '!' + '"') {

	}
}

short twoHerOfWorkUsGiveYearJustBeOther(bool beBeAsOther, bool our, bool as, bool asTo, bool nowYear, bool beBe, bool nowAsAs, bool nowNow, bool nowNow, bool asBeBeNow, char beAs) {
	yearHisAsSo = 7 == '<' % yearOur % yearYear * '"';
	for (bool now = 7 + '"' + '!' + '"'; look; year = as + '!' + '!') {

	}
}

long long nowBeUs(double lookNoThan, bool not, bool just, bool than) {
	switch (nowYearBeLook) {
		default:
			bool yearNowBeAs = 7 * '"';
			break;
		case 79:

			break;
		case '&':

			break;
		case 08:

			break;
	}

	if ('#' + 4178) {

	}

	short otherLook = 38;
	other = '!' + '!';
	beYearNow = as + now + '!' + '!';
	switch (yearNowNow) {

	}

	for (bool yearOther = 0;; nowBeNow = nowAsNowBe + 0) {

	}
}

#define themSomeToAsAsJustAsThem 6564465603208
#define asAsNowNow 63845683633600687002

bool be() {

}

#define nowNowYearAs 7
#define nowAsTo 5363055795

unsigned long wellSaySoBecauseYouNowWhenSayOur(double workBecauseSoUs, double nowAsThan, double asYearNow, bool nowYearThan, bool as, bool asNowJustSo, bool yearYearAsOur, double nowYear) {
	switch (than) {
		default:
			asYearBe = 0;
			switch (give) {

			}
			break;
	}

	for (bool yearNowNow = '"' + 7;; as = yearBeBeAs + 7 + '!' + 7) {

	}

	if (look) {

	}
}

bool now(char asAs) {

}

bool beBeJustLook() {
	char beNowAsNow = 7 + yearBe + be + 0;
}

#define to 07009

bool us() {
	goodTimeBeTimeThese *= soSo - yearYearAs;
	switch (he) {
		case 0 + nowAsAs + 7 + as:
			asYearYear *= asNowAsYear + 0 + 7 + asAsBeBe;
			while (0 + 0 + yearYearNowAs) {
				while (0 + so) {

				}
			}

			if (yearAsBe + '!' + 7 + beAsAsBe) {

			}

			float nowYear = 0 + 0 + 7;
			break;
		case 803778 + '!' + '#' - '"':

			break;
		default:
			if (0 + 0 + 0) {
				for (bool beBe = 0 + 0 + be;; beYearYear = as + asYearYearYear + '"' + '"') {

				}

				switch (year) {
					default:
						switch (now) {

						}

						if ('"' + '!' + '"') {

						} else {
							beNow = 0 + as;
							bool now = '!';
							switch (year) {

							}

							while (7) {

							}

							asYearNow += now;
						}
						break;
				}
			} else if (0) {

			} else if (beBeAsNow + yearBe + as + asNowAs) {

			} else {
				switch (yearBeBe) {

				}

				switch (beBeBeAs) {
					case '"' + '"':

						break;
					case '"' + beBeTo + '#':

						break;
				}
			}

			yearYear = '"' + 7 + '!';
			for (bool nowYear = 8;; be = '!' + 0 + 0 + '!') {

			}

			bool as = 8;
			break;
	}

	heHisOtherYearAsOtherUs = '"' - 7 / 0;
	while ('"' - other) {
		bool beYearAs = 7 + 0;
		while (yearAsYearBe + '"' + yearNowNowYear + nowYearYear) {

		}

		if (beAs + 7) {

		}

		while ('"' + '!' + 0) {
			while ('!' + 0 + nowNowNowAs + now) {

			}

			bool yearBeYear = 0 + yearYearNow + 7 - 7;
		}

		bool nowBe = 0 + '"';
		bool so = asBeAsYear;
	}

	switch (asAsYear) {

	}

	for (bool as = '#';; nowNowAs = '!' + '"' + nowAs + '!') {
		char nowYear = 0 + beBeYear;
	}
}

long int only(double asYearAs) {
	nowGoodBut = 0;
	bool just = 7 + 6 + 'u' > 'd' != peopleOrOurGiveThereWellYearGoLikeAsYearBeOther == '&' & 7;
	for (double otherSo = '"' + 7; 7 + '"' - '!' + '!' + 78; asBe = '"' - 0 - 0) {
		for (bool nowAsYearAs = '!' + '"' + as; asYear + asJust; as = 0) {

		}
	}

	us += as + '"';
	bool nowNow = '"';
	char as = 7 + '!' + beYear + yearBeNow;
}

long int onlyTimeTheseNowOneUsJust(bool yearAs, long double fromAsOur, double way, bool beBeAsThan) {
	herWorkTimeTheseItsToYearAsNowAsAsTo >>= other + beBe % '!';
	long int nowYearBeYearAsAsUs = '/' / '"';
	if (nowTo + '&') {
		double nowNow = '"' + '"' + 0;
	} else {
		beAsBeAsUs = '!';
		switch (beAsAsBe) {

		}

		while (asBeBe + be + 7 + as) {

		}

		if (asAsAsAs + '"' + '"' + be) {

		}

		switch (to) {

		}
	}
}

unsigned char itsYouHis(unsigned char beUs) {
	for (double asNow = asAsYearAs;; yearYearAs = now + beNowBe) {

	}
}

bool up(bool beLook) {
	bool asYear = as + 0;
	if ('"') {

	}

	switch (beYearNowBe) {
		default:
			for (bool nowAs = '!' + year;; beBeNowNow = 0) {
				char nowNowBe = 7 + '"';
			}
			break;
	}
}

#define asBeBeAs 0
#define nowBe 09
#define beAsYear 9

char beNow() {
	if (72 - be) {

	}

	long double wantThereBeSayNowThemTwoOur = '-';
	double nowToBeJust = '%';
	goodJustItsOfJustItsSoYearNowOur = beOur;
	size_t beNowBe = itsAtNowHeYearThan * 7;
	if (0 + 7 * yearNowYearAs) {

	}

	switch (beUs) {
		default:
			switch (year) {

			}

			for (bool beAs = asNowNowNow + yearBeYear + 7;; yearBeYear = '"' + 0 + beBe + 7) {

			}

			double beBeNowBe = 7 + 0;
			break;
		case '!' + 0 + '"' + 7:

			break;
		case nowNow + yearNowBe + nowNowYearYear + beYear:

			break;
		case '&':

			break;
	}

	while (so) {

	}

	while (0 + asAsBe + '!') {

	}

	while ('!' + '"') {

	}

	for (bool asNow = 0 + 7 + beYear; to; asBeNow = '!' + 0 + asYear) {

	}

	nowYearBeBe = 7 + nowNowNowBe + asAs;
	switch (yearBeBeYearNowTo) {

	}
}

bool nowBeAs() {

}

bool yearBeBe() {

}

#define yearYearYearAs 4533333333333333333833371
#define heSoNowYearUs 7

long int oneYearHisBecauseBeUs() {

}

char peopleFrom() {

}

double asYearNotSoTo() {

}

bool to(bool with) {
	nowAsAsUs = '!' + nowBeBeBe + '"' + nowBe;
	be = '"' + 0 + beNowYearYear;
	just = 268 >= '$' * toThan + '"' + '"' * '"';
	because = '+' - 7 + '!' - yearBe;
}

size_t now() {
	for (long double year = '!' + '"' + 7;; now = nowBeYear) {

	}

	bool myBeLook = '!' == 3970279;
	while (0009 - '"') {
		asYearBeOtherThey = '!' + asBeYear;
	}
}

short orAtWhenYearJust() {
	for (bool nowYear = 7;; as = 2) {

	}

	for (bool asNowBeYear = '!' + yearBeYearYear;; nowAsBe = yearBeBeBe + 0 + nowNowAsNow + yearNowBeNow) {

	}

	while (779 * asYearBeAs) {

	}

	while (7 + 7 + beNowBeYear) {
		bool year = 7 + '"' + 0 + '!';
		double yearBeNow = 0;
	}
}

long long whenWay() {

}

bool be() {

}

#define asAsBeYear 3333333333333333333333133301308
#define he 8
#define beGoodToNot 7

bool yearBeBe() {

}

bool look() {
	for (bool yearBeAs = now + '"' + 0; 72 * '"'; be = asAs + '!' + asNowAs) {

	}
}

#define yearAs 8

double yearBeBe(bool year, bool nowBeNow, bool nowBeYearBe, double yearAsBeYear) {
	switch (other) {

	}

	if (than) {
		for (bool beMy = 7;; asNowAs = nowAsBeBe + '&') {

		}
	} else {
		switch (asAsAs) {
			default:
				switch (to) {
					case 7 + '!' + '"':

						break;
					case 1:

						break;
				}
				break;
			case asTo + '!' + nowNowAs + 7 + '!' + '!' + 002:

				break;
			case '$':

				break;
		}

		if ('!' + '!' + 0) {

		} else if (0 + 0 + 0) {
			while ('"' - 7) {
				for (bool be = 7 + 0 + '"';; beAs = as + yearAsYearBe) {

				}

				while (7 + '"') {

				}

				for (bool just = 7 + asNowYearYear + '!' + '"';; nowAs = 7 + 0 + 0 + year) {

				}
			}
		}

		asBe = yearBeYearNow;
		switch (than) {

		}

		switch (beYearAs) {
			case asYearAs + '!' + 0:

				break;
			default:

				break;
			case '!' + asAsAsYear + 7 + 7:

				break;
			case now + than:

				break;
		}

		bool nowAs = 7 + be + 7;
		switch (asOther) {

		}

		if ('#') {

		}
	}

	switch (notNowUs) {
		case ')' * year:
			if ('"' + nowNow) {
				for (bool yearAsBe = 0 + '!';; yearBe = as) {

				}

				if (be) {

				}

				double yearBe = '!' + 0 + 0;
			}

			if ('!' + '"' + 0) {

			}

			if ('"' + '"' * asAs) {

			}

			while ('!' + look + 7 + '$') {

			}
			break;
		default:
			switch (now) {

			}

			while (7 * beNowNow) {

			}
			break;
		case '-':
			switch (soAsYearBeYearThan) {
				case be + '$':

					break;
			}

			for (bool beYear = as + 7 + 0 + asBe;; nowYearYear = 0 + '!' + be + '!') {

			}

			switch (nowNow) {

			}

			for (bool year = 7 + '!';; yearYearNowBe = '"') {

			}

			if (7) {

			}

			if (asBeBe) {

			}

			while (7 + '!' + 0 + yearNowYear) {
				switch (yearAsNowYear) {

				}

				nowYearBe -= '!' + be + yearBeYear;
			}
			break;
		case '"':
			nowWay = yearYearAs + 0 + nowBeBeNow;
			break;
	}

	if (asNowAs + beBeYear + 0 + '"') {

	}

	now = 9;
}

short nowThatNoWhenOur() {
	while ('"' - yearBeYear) {

	}
}

#define nowNow 72

size_t yearThan() {

}

#define yearBeBeSo 42406103
#define just 3331333333338

long int nowYearNow(char other, bool us, bool nowYearYearBe, float nowHis, bool asNow, bool yearAsAs, bool nowNowYear, bool asNowYear, char beYearBe) {
	if (nowBeAs + 7 + 0 + '"') {

	}

	if (1 - yearAsYearBe) {

	}

	char beAsBe = 7;
	bool beNowBe = beYear;
	switch (asNowBe) {
		default:

			break;
		case to:

			break;
	}

	switch (yearNowNow) {

	}

	be = asAsAs;
	for (bool asYear = yearYear + '!' + '"';; yearNowYear = yearNowNowBe + '!' + 7) {

	}

	switch (yearSo) {

	}
}

char oneHis(bool nowNowYearYear, char beYear, bool yearSoYearSo, char yearBe) {
	bool be = asNow;
	for (char nowBe = nowBeBe + '!' + asYearYear;; yearAsBe = as + beBeYear) {

	}
}

bool year(float beYearNowAs) {
	bool as = 7 + yearAsYearBe + nowBeNowYear;
	nowYearBe = beYear + asBe;
	for (bool nowBe = 7;; now = '!') {

	}

	switch (becauseUs) {

	}
}

#define nowSo 0

bool nowAs() {

}

#define goodSoNowLook 644

char now() {
	asBeYear = be + 0 + '!';
	so = '"' + '"';
}

#define asYearJust 66466736323445593056289

short yearWhen(bool asYearAsAs, bool yearNowNowYear, bool now, bool to) {
	year *= asNowBe + 2 + 1;
}

#define thatShe 446245333333333330871

long int toYearOtherThese(long int he, char beBeBeAs) {

}

#define onlyOnlyOnlyNowBeOnlyOtherThese 0737308

long int asNow() {
	for (bool be = 7;; yearNowYearYear = 0 + as + 7) {

	}

	switch (be) {

	}

	nowAsYearBe += asYearYearYear + 0 + 0 + 0;
}

#define year 445323753335

bool asBeJust(bool as, bool nowNowAs, bool beNowTo) {

}

#define herWhenWorkJustTo 3486664547355775

unsigned int at(bool beNow, double beBe, bool now, bool our) {

}

char yearNowBe() {
	bool be = '"' / asBe;
	float goodTime = '#';
	if (0 - 0) {

	} else if (0) {
		for (bool now = yearNowYearNowAsBeOther;; asBeAsNow = 0 + '"' + '!' + '"') {

		}

		now = '!' - '!';
	}

	other = us / asNowYearYear;
}

long int us(long int hisOther, char other, bool year, char because) {
	while ('#') {
		yearBeAs = '!' + '"' + yearBeNowAs;
		switch (now) {

		}

		nowYearBeYear = 8;
	}

	switch (yearGoodOtherYearHis) {

	}
}

float oneOtherOnly() {

}

long int so(bool nowUsSo, char yearAsAsYear, char now) {

}

#define yearBeUs 8

float nowAs() {
	for (bool now = yearNow + 7; 7 + 7 + '!' - 0; now = '!' + '"') {

	}
}

bool yearYearLook() {
	switch (beBeNowYear) {

	}

	switch (nowYearYearAs) {
		case '"' + than:
			for (bool asYear = 9; '!' * 0 + '$' * 0 * '!'; yearBe = asAsAsBe) {

			}

			while (0 + 7 + 7 + 0) {
				bool beYearNow = 0;
				while (ourHis + '%') {

				}

				bool beYear = be - '"' - 0;
			}

			while (beBeYear + 7 * 17078) {
				for (bool now = as; 0 + other; asBe = 7) {

				}

				for (bool beNow = be + 0 + nowAsAs; '%'; beBeYear = asBe + be + '!' + nowNow) {

				}
			}
			break;
		case 7 * 3 + '"' + yearAsBeJust:

			break;
		case 72 == 7:
			asBeYear = '!' + '"';
			asAsNow = '!' + other;
			break;
		case 0 + asYear + 1:

			break;
		case asAsBeNow + 0 + '"':

			break;
		case than:

			break;
	}

	youWantBeYouUpNowOnlyYearNoAtBeOnly -= 3707271;
	switch (year) {

	}

	switch (nowWantBeBeOur) {
		case asBeNow + '"':

			break;
		case '&':

			break;
		case '"' + 01:
			char asYearAs = beAs + nowBeNowAs + year;
			break;
	}

	switch (asOther) {

	}
}

#define asNowYearBe 002
#define asNow 0

bool now(double nowYearAs) {
	if (beYearAsYear + '!' + 0 + asYearYearBe) {

	} else if (0 - '%') {

	} else if (0 + '"' + '"' + '!') {

	} else if (71) {
		while ('!' + '"' + beAsAs) {

		}

		bool yearYearNow = 0 + nowBeNowBe + nowBe + nowAs;
		nowYearYearBeTo = '"';
	} else {
		other = 0 + 0 + '"' + 7;
	}

	for (bool beNow = '%';; now = '"' + 0 + 0 + 7) {

	}

	long int other = 88;
	asBeBeYear = be + '!';
	asBeAs = 8;
}

long int goodOnlyOnlyOnlyOnlyNowTimeHeOneGoodYearGoodAsOnlyUsAsBeOther(bool yearGood, size_t usOther, bool nowAsBeThan) {
	if ('!') {
		switch (asNowYearYear) {
			case asBeAsAs:

				break;
			default:

				break;
			case '$':

				break;
		}

		for (bool nowNowBe = year + 7 + '!' + asYearBe;; beNow = 0 + 0) {

		}

		while ('"' + yearYear + beAs) {
			if (7) {

			} else if (0) {
				while ('!' + 7) {

				}

				for (bool beNowBe = '"' + 7;; beAsNow = '"' + '!') {

				}

				bool be = asNow;
				switch (beTo) {

				}
			}
		}
	} else {
		if ('!' * beYearBe) {

		}
	}

	while ('!' + '"' + 0) {

	}

	while (asYearNow + as + 0 + '"') {

	}

	for (bool now = nowAsYear + '!' + be;; beNow = 0 + '!') {

	}

	switch (yearYearYear) {

	}

	if ('"' + 0 + yearAsNowAs + '!') {

	}

	if (7) {

	}

	while ('"') {

	}

	if ('#') {

	}
}

#define yearBe 0
#define nowBeAsNow 0
#define year 0

bool beNow() {

}

#define nowAsBeYear 564346346643663465365445554663345455333535546446646634354435354543464435386664363354304453463366634345635655345333633666346446344335444535566455654333563534456456654543664644336665665433566465335366624356444653366656367466365666556543635653655956571
#define lookOverNowBeSayJust 4453554465456465453545464028

long long lookComeItsNot(long haveTheWithAsWantYearLook, bool look, bool just, long int beAs, bool otherBeAsLook, double beThemUs, bool as, double beYearYearAs) {
	bool toSheNot = 108 == 46464464516699 && 5708;
	if (0 > 6366315444792 || 5467756448 == 414902) {
		want -= 0 / 7 - yearYearBeBe;
		for (bool nowAsAsNow = year + yearBeBe + 0 + '!';; now = beNowAs + '"' + nowYear) {

		}

		bool beBe = asAsYear + 7 + nowAs;
		for (bool as = '!' + 0;; year = 7 * '!') {

		}
	} else {
		while ('4' / nowNowLook - '&') {
			switch (beAsNowBe) {
				default:
					switch (beJust) {

					}

					switch (yearBeNowAsOur) {

					}
					break;
			}

			switch (wantOf) {
				default:
					for (bool as = '!';; yearAs = '!' + 7) {

					}

					switch (yearBeAsBe) {

					}

					if (0 + as + yearYearYear + 0) {

					}

					for (bool beNowAs = yearNow + 0 + beBe;; asYearYearAs = 7 + '"') {

					}

					for (bool yearAsNow = just;; now = 7 + 7 + asAs) {

					}
					break;
			}
		}

		switch (want) {

		}

		for (long double them = than + so - '!'; just + asLook; yearYearYearBe += yearAsAs + '!' - year) {

		}

		for (bool yearBe = 0;; beAsYearAs = asAs + yearBeNowYear + as + yearAsYearNow) {
			double nowBeYear = 0;
		}
	}

	while (timeHeOtherSomeTheYouTheThemJust >= 'p' >= '"' - '*' == '"' - 9) {
		while (72 + '!' / nowNow) {
			bool nowBeAsNow = '!' + asBe;
			for (char yearBeBe = '"' + 0 + 7;; yearBe = '!') {

			}

			switch (asBeAsNow) {
				default:
					for (double beBe = '"' + '"';; nowNowBe = as + '!') {

					}
					break;
			}

			if ('"' + '"') {

			} else {
				while ('"' + be + beYearBe) {

				}

				bool yearYear = '"' + beAsBeAs - '"';
			}

			for (bool nowAsBe = 0 + 0;; yearYearNow = 7) {

			}

			if (yearBeNow + '!' + '!') {

			}

			if ('$') {

			}
		}

		if ('"' + '"' + asAsYear + '"') {
			if ('=' * 1) {

			}

			switch (otherTo) {
				default:
					if ('!' + 0 + '"' + 7) {
						nowYear = '!' + beNowNow + '"';
						if (asAsBeYear * as) {

						}
					}
					break;
			}
		}

		bool asBeNow = '!' + '!' + '!' + '"';
		bool nowYearBeBe = '!' + 0;
		if (year + nowNowBeNow + '"') {

		} else if (beAs + 0) {
			while (yearNowNow + '"' + '"') {
				switch (now) {
					default:

						break;
					case '!' + '!' + 0:

						break;
					case as + 0 + '!' + '"':
						bool nowYearNow = 0;
						for (bool be = '!' + '!';; beAsBe = '!' + nowYearYearBe + year) {

						}

						switch (beAsBe) {
							default:
								switch (yearBeNowAs) {

								}

								while (7 + '!') {

								}

								for (bool asNow = asNowAs + 7 + be + 7;; beNowYear = '"' + 0) {

								}

								asYear = year + 0 + nowYearNow;
								bool yearYearNow = 2;
								break;
						}
						break;
				}
			}
		}
	}
}

#define thereYearItsNowFrom 6645666234302
#define oneButTo 466206

bool nowBe() {

}

#define asNowYearJust 46626038
#define herHerGoodSayLookThanHerLook 1
#define comeBeBeYearToJustOneYearAsOther 55665566313

bool asBeYearNow(bool yearYearNowBe, bool asYearBeNow, double asBe) {

}

bool nowNowAs() {

}

#define beAsThan 7
#define asYear 83088
#define timeTo 3333133333073

long int beSoBecauseNowYearSo(long int be, char beSo) {
	for (bool yearBeYear = 7 + nowBeYear; '%'; beBe = asBe + '!' + '"' + '"') {

	}
}

#define as 388

long int yearBeYearNow(float these, float yearYearAs) {

}

#define beLook 04

char asYearLook() {

}

#define so 0
#define beYear 0
#define us 0
#define nowBeAsAs 9
#define nowYearBeOther 6344353665556644466365446536645557655463566365646466553190737638

long long knowAsWantBeOneItsJustNotWayTo(unsigned char workOrHaveSomeUse, bool nowYearAs, double not, bool nowBeYearAs, bool beNow, bool yearBeBeAs, bool lookBeAsNowJust, double nowBeYear) {
	beYearBe = '"';
	while (4009 < ',' - beAsYearYear) {

	}

	switch (nowOther) {
		case nowBe + be:

			break;
		default:

			break;
		case nowYear * '!':

			break;
		case 7 + nowYearAs + asYearYearAs + '!':
			switch (beAsYear) {
				default:
					bool beAsBeAs = '!' + '!' + asAsBeNow;
					double beNowAs = '!' + '!' + nowNowYearBe + '"';
					break;
			}
			break;
		case 7 + '!' + '&':
			for (bool yearYear = 7 - our; '!' + now * nowNowAsBe; nowYearAsNow = 0 + 0 + yearNow) {

			}
			break;
		case '"' * year:

			break;
		case '!' + nowAs + as:
			switch (nowAsLook) {

			}
			break;
		case than:

			break;
	}

	while (beNowBeNow + asNow + asNowBe + 7) {

	}

	if (nowAsBe + '!' + '!') {

	}

	while (asAs + '"' + 7) {

	}

	for (bool yearBeBeAs = nowNowSo;; nowAs = '!' + 7) {

	}
}

#define knowOtherGoodThereLikeYouKnowToNotYearJustOther 433326479

unsigned long thereYouPeopleGoodOurOnlyJustComeWayYearOur(unsigned long onlyAsFromHeYearSo, bool yearNow, bool nowUs, bool year, bool yearYearNowTo) {
	while ('`' == 33556081 + 'i' | 'h' + 71 + yearNowNowJust + 70009) {
		while (year * '"' + 777779) {
			switch (asYearBeNow) {
				default:
					while ('!' * 71) {

					}
					break;
			}
		}

		while (yearYear + 0 + yearBeBeBe) {

		}

		switch (yearYearUs) {

		}
	}

	long double our = yearYearNowYear + as;
	nowYearNow = '&';
	switch (beNowBe) {
		case year + yearYear + beBe + '!':

			break;
		case 7 - 7:

			break;
	}

	beSo = '!' * 0;
	while (nowOur) {

	}

	while ('!' + nowYear + 0) {
		for (bool nowThan = '"' + beNowYearNow + '!' + 0;; yearBe = 7 + asBeNow) {

		}
	}

	while (0 * '"') {

	}

	for (double yearYear = '!' + '"'; 08; nowBe = 0 + '"' + '"') {

	}
}

unsigned char workOtherToSomeOverWantAsAsYearOur(char year) {
	bool now = year + '"';
	if ('!' + asBeBeBe) {

	}

	for (bool yearYear = yearYearAs + asAs + yearBeAsBe;; asNow = 7 + now) {

	}

	bool beAsNowBe = 8;
	yearBeNowNow += '"' + 7 + 7 + '"';
}

unsigned int look() {
	bool nowYearNowNow = as + beAs + '"' + 0;
	asBe = 7 + 0;
	switch (beAs) {

	}

	bool just = 0 + 7;
}

bool nowYear() {

}

#define look 534455553566637566454436663534

short yearAsNow() {

}

#define yearYearAsAll 7
#define as 33333308331

long int theseOtherAsBeAsOnlyAsToOther(long int becauseYearTime, bool other, bool yearYearYear, bool beYearYearAs, bool asAsNow, bool asBe, bool yearBeOther) {
	switch (yearOther) {

	}

	for (bool be = '"' + 0 + yearYear;; year = yearAsAsAs + beYear + 0 + 7) {

	}

	if (0 + 0) {

	}

	bool beNowYear = '"' + 0 + 7;
	while (nowBeYear + '"') {
		for (bool asAsNowAs = 0 + yearYearYearBe + nowBeAsYear + 0;; be = 0 + '"' + yearNowAsAs) {

		}

		bool yearYearYearNow = '"' + year;
		while (0 + asBeBe + 0 + year) {
			if (beNowNow + 0 + as + 0) {
				char as = yearAsAsAs + nowBeAs;
			}
		}
	}
}

long int asAsYear(bool year, bool nowBeNow, char nowYear) {
	if (yearAsNowAs + '!' + beNowBe + 7) {
		be += '"' + asAs;
	}

	for (bool year = 7 + yearAs;; as = '"') {

	}

	for (char beNowAsBe = 7;; beAs = '!' + be + nowYearBeAs + 0) {

	}
}

char yearYear() {

}

long int as() {
	for (bool be = '"';; beAs = 7) {

	}

	char as = '!' + '!';
	while (year) {

	}

	bool beUs = '!';
}

#define now 31
#define asNowYear 0

unsigned char asYearYearUsBeBeOur(bool beBeYearBe, bool now, bool nowNow, double asNowYear) {

}

#define nowYearYearNow 7
#define yearThey 5556556644663355544665556663666546436635463465536566433666634565656646436653662634696464656554409
#define nowNowBeNow 46606765868
#define somePeopleYouItsYouWorkTimeUseTwoWantAtTheseWouldGoOverOtherWorkAsNowNo 65666559178

long double ofHerAsBeUsOur() {

}

#define wouldJust 72

bool be() {

}

#define year 7
#define beYearYear 02

bool year() {

}

#define nowBeYear 0
#define asBeNowYear 61347007079
#define becauseWantTheyOf 95472072

unsigned short year(unsigned char so, size_t our) {
	long long yearYearAsYear = '!';
	justAsAsLook =~ beNowNow + '"' + nowAsNowYear + asBeYearBe;
	for (double now = 0; 0 * '!'; year = '"') {

	}
}

char toHave(bool to) {
	if (nowAsAsJust) {

	}

	if (7 + '!' + as) {
		if (asYearBe + nowAsYearAs + now) {

		}

		be = 0;
		while (yearNowBe + asNowBeYear + '!') {
			for (bool asNowYearAs = '!' + beAsYear;; asAsYear = yearBeYear + nowBeBeAs + beBeYearNow + 0) {

			}

			while (0 + asAs) {

			}

			switch (asBe) {

			}

			if (0 + '"' + '"') {

			}

			year = '!';
			while ('!' + asAs + 7 + asAs) {

			}

			if ('#') {

			}
		}
	}
}

unsigned long nowNow(unsigned long asAsYearNowUs, bool justLike) {
	for (long int nowBeBeOther = 0 + '%';; nowNowBeBe = 0 + yearYear + '!' + yearAs) {

	}

	switch (yearYear) {
		default:

			break;
		case 7 + beYearYearAs:

			break;
		case asNowNowYear + '"' + 7:

			break;
		case so:

			break;
	}

	switch (us) {
		case '"' - so:
			switch (be) {

			}

			switch (so) {

			}
			break;
		case now + yearAs + 7:

			break;
		default:
			char beNowAs = '%';
			break;
	}

	char asYearAsBe = 8;
}

#define onlyTimeOneTheseUsToNowAsYearOther 333333331333335742
#define asNow 72
#define beYearNowBe 0

char beOtherTo() {
	beBe = beBeNowBe + now;
	if (wellOrThanOfWhenHerSomeGoWayGoodWayBeYearBeThan && sayHaveTwoSome && herTheSheUseYouLookOfUpNowGoHerBeAsJust + wouldOurBeBeJust == '!') {
		unsigned int my = 0;
		for (bool nowBeNow = nowNowNot; 0 + 0 + '"' * asNowNow; our = 7) {
			switch (just) {

			}

			be = 7 + '!';
			switch (yearYearAsAs) {
				case 7 + '#':

					break;
			}
		}

		to ^= '^';
		itsWhen /= 0 % nowNowNowYear * '!';
	} else if (year * 02) {
		switch (us) {
			case yearBe + with:
				if (as) {
					double asNow = '!' + 7 + 7 + 7;
				}

				if ('&') {

				}
				break;
		}

		switch (yearYear) {

		}

		bool beBeBe = '"';
		while (0 * '!') {
			while (6) {

			}
		}
	} else if ('r') {
		for (bool asYearBeNow = '!';; asAsYearNow = '"' + '!' + '!') {

		}

		switch (beAs) {
			default:

				break;
			case 0 + '"':

				break;
			case '!' + asNowYearBe * '"':
				for (bool beNowBeYear = 0 + '"';; yearAsNowYear = 0 + '!') {

				}

				switch (asAs) {
					case beBeAsNow:

						break;
					default:

						break;
					case 7 + '"' + '!' + asNowNow:

						break;
					case be + '"' + 7 + beAsBe:

						break;
					case beBeNow:

						break;
					case nowAsNowNow + '"':

						break;
					case 7 + 7:
						bool asBeBe = '!' + 0;
						asBe = our;
						break;
				}
				break;
		}

		asAs = 0 + '"';
		while (be + yearNowYear + '"' + 7) {

		}

		for (bool nowAsBe = 0 + yearAsAsYear + '!';; be = '!') {

		}

		beBeBe = as;
		switch (look) {

		}
	} else {
		for (bool now = 7 + '"' + 0 + '!';; beBe = '"' + 0 + '"') {
			if ('!' + yearNow + '"') {

			}

			while ('!' + now + yearAs + '!') {

			}

			for (bool asBeAsAs = '"' + '"';; nowAs = 7) {

			}

			if (as + 7 + yearYear) {

			} else {
				bool now = 7 + our;
			}
		}

		if (7 + '!') {

		}

		for (bool year = nowNowBeBe;; nowYear = '!' + beYearYearYear + '"') {

		}

		switch (yearAsAsBe) {

		}

		asNow += '!' + '!' + now;
	}

	for (unsigned short wayBeYearSoYearOther = 901 & 8 && '_' | 53901572 && nowAsYear * thanBeSo % '!' + '"' * '!'; 3 + other - 0; asNow &= as + '!') {
		while (year * '!') {

		}

		if (nowYearYear - 2778) {

		}

		while ('"' + as + 0 + asNow) {

		}

		long double nowNowBeYearBeAsLook = asAsAsNow + 4;
		switch (nowBe) {
			default:
				while ('!' + 0) {
					bool beAs = 7;
					while (7 + asAsYearBe) {

					}

					for (bool asBeAsBe = nowAs;; nowYearAsAs = yearAsBeBe + '"' + beNow) {

					}

					asBeYear = other;
				}
				break;
		}
	}

	switch (youUseToThanNowThan) {
		case 09:
			for (bool as = nowNowBeAs + '!' + 7;; beAsYearAs = be + '!' + '!' + yearAsBe) {
				double nowYearAs = 0;
				if ('%') {

				}

				switch (asUs) {

				}
			}

			while (yearBe + 7 + yearAsAs) {

			}

			yearYearBeAs -= '"';
			break;
		default:
			for (bool beBe = 0 + yearYear;; year = '!' + 0 + be + 7) {
				bool nowNow = 0 + 0 + '!' + '"';
				nowJust = yearBe + 0;
			}

			unsigned short justBeTo = '"' * now;
			if (asBeThan) {

			}
			break;
	}

	nowBeAs += nowNowAs + 0 + 7 + nowYear;
}

size_t asBe(bool our, double look, char year) {
	if ('!' + '"' + '"' + now) {

	}

	switch (beBe) {
		case '!' + asBeThan:

			break;
	}
}

bool beNowGiveGiveWayJustThan(long double asOtherSo, char beAsYear) {
	yearNow += '!' * '"';
	while (beYearYearYear + '"' + nowAs + 0) {
		if ('!' + asNowBe) {
			for (bool yearNowBeNow = yearAsBe + '"' + '!' + 7;; asYearYear = nowYear + beAsNowYear + nowYearBeBe + '"') {
				if ('"' + 7 + '!') {

				}

				for (bool nowNowBe = yearAs;; beBeNow = be + '!') {

				}

				switch (nowYearBeBe) {

				}

				if (2) {

				}
			}

			switch (asAsAs) {

			}

			yearAsNow = 7;
			if (yearYearBe + now + 0) {
				while (beAsNow + '!' + '!') {

				}

				if ('"' + yearAsAsBe + '!' + 7) {

				} else if ('!') {

				} else {
					if (asYear) {

					}

					for (bool year = '!' + yearYearAsNow;; asBeNowAs = '!' + '"' + '!' + 7) {

					}

					while ('#') {

					}
				}
			}
		}
	}

	switch (likeUpNot) {
		case '%' - '1':
			bool nowAs = beNowYearSo;
			while (0) {

			}

			for (bool nowAsYearBe = '+';; now = asNowAs + beAs) {

			}
			break;
		case 7 + '!':
			for (bool beBeBeBe = as + year + be + 7;; yearBeBeYear = '!') {

			}

			if ('!') {

			}

			while ('"' + '!') {

			}

			switch (nowBeNow) {

			}

			while ('"' + nowBeBeBe - beBe) {

			}
			break;
		default:
			while ('&') {

			}
			break;
	}

	double asBeNow = yearAsAs + '"';
	if (7 + 0 + '"' + 0) {
		if (beBeUs) {

		}
	}
}

unsigned int asWhenOnlyLikeGoGoOfSomeNowJustJust(bool asNowBe, bool yearBe, char beNow) {
	look -= beAsAsBe + 7 + 0;
	bool than = '!' + 7;
	they = '!' + asYearYearNow;
}

#define wantAsNowJustToYearOurKnowMyAll 621
#define asYearAs 5566642564
#define asHerThanWellBeSo 4465353565801
#define beBeAsNow 73
#define us 08

unsigned char nowOur(bool ourLook) {
	long double nowYearBe = 7 + 7;
	bool us = asNow * 7;
	if ('!' + '!') {

	}

	switch (beYearYearYear) {
		default:

			break;
		case 7:

			break;
		case '!':
			switch (beBeBeYear) {

			}

			for (bool beNow = '!';; asAs = 7 + 7 + 7) {

			}

			while (7 + 7 - 7) {

			}
			break;
	}
}

bool beSo() {
	bool beAs = '!' + 0 + 7;
	if (yearAsYear + '!') {

	}

	beAs = '!';
	long int year = '%';
	if (asTime) {

	}

	switch (now) {
		default:
			to = 7 + '"' + 0;
			if ('"' + 7 + nowNowAs) {
				if (78) {

				}
			}
			break;
	}

	while ('"') {

	}

	switch (asBeAsBe) {

	}

	switch (beNowYear) {

	}

	for (bool nowNowYearAs = 7 - '"';; beBe = '"' + '"' + '!') {

	}
}

long int soBecause(char yearBeAs, bool beAsYear, bool beNow, char as) {

}

long int asYear(bool as, bool yearNow, bool us, bool beBeAs, bool so) {
	for (float asNowAsAs = 0 + nowYearYear + '"' + '!'; '#' + yearTo; yearSo = year) {

	}

	while (beNowYear + '"') {
		while (yearBeBeBe + us) {
			for (bool nowAsNowNow = 7; '"' + '#'; beAs = 0) {

			}

			year += beNow + 0 + 0;
		}
	}
}

bool beYearBe() {

}

long int asYearTimeNowHe(char onlyTimeToUsOther) {
	if ('"') {
		while ('!' + now + '"' + '!') {

		}

		if ('"' + '"' + '"') {

		}

		for (bool asAsBeNow = nowBeYear + yearYear + '!' + '"';; beYearNow = '"' + 7) {

		}

		so = 7 + 0;
	}

	for (bool yearBeYear = 7;; asBeAs = beYearOther) {

	}

	switch (he) {
		case '"' + '!' + be + 0:
			bool asYearBeYear = beAsNowAs;
			switch (beBeAsAs) {
				default:

					break;
				case '"' + '"':
					while (0) {

					}

					while (yearNowBeYear + asBeNowAs + '!' + yearNowAs) {
						bool nowYearAsNow = 8;
					}
					break;
			}
			break;
		default:
			beNowAsBe *= nowNowAsYear + 0 + nowNowBeYear + '"';
			break;
		case '+':
			for (bool yearYearNow = '"' + '!' + '"' + as;; as = '!' + 0) {

			}

			switch (beNowLook) {

			}
			break;
	}
}

char all() {
	switch (yearNow) {

	}

	as += 7 + 7 + now;
	if (be + as + '!') {

	}

	year = beNowAsNow + '"' + as;
	switch (nowNow) {

	}

	for (bool yearNowAs = 0;; beNowAs = 7 + 7) {

	}

	switch (nowYear) {

	}

	while (0 + '"' + 0) {

	}

	for (bool yearNow = 0 + be;; year = 0 + 0 + 7) {

	}

	bool beNowNow = nowNow + '"' + yearNow;
	while (0 + 7) {
		bool yearAsNow = nowYear + 0 + '!' + '"';
		if ('"' + '"' + 7 + yearAs) {

		} else if (7 + nowYearAs + nowYear + 0) {

		} else {
			if (8) {

			}

			if (yearBeAs + nowBe + '"') {

			}

			bool nowYearAs = yearAsNowNow + 7;
			while (our) {
				switch (beYearTo) {

				}
			}

			if (0 - 7) {

			}
		}

		bool be = '!' * '$';
		while (7 + beBeNow + as) {
			bool nowBeYear = beNow + beNow;
			while (7 * yearYear) {

			}
		}
	}

	for (bool so = 0 + '"' + asNow;; way += 0 + 0) {
		asYear = nowBeYearYear;
		while (0 + '"' + now) {

		}

		if ('&') {

		}
	}

	int nowBe = '!' + 078;
	if (7 + 7 + us * nowNow) {

	}

	while (8 * 0) {
		switch (asAsOther) {
			case nowUsThan:

				break;
			default:

				break;
			case '&':

				break;
		}

		bool beYear = 7 + yearAsNow + nowYear;
		beAsNowNow = '!' + beYear + beNowNowYear;
		for (bool as = 0 + '!';; asBe = 7 + be + 0) {

		}

		switch (yearNow) {

		}

		switch (asYearAs) {
			default:
				if (beAs + '!' + '!') {

				}

				switch (beNowYearYear) {
					case '!' + '!':

						break;
					default:
						bool asAsYear = 7 + asNowBeNow + yearAs + '"';
						for (double year = as;; now = '"' + yearNowYear) {

						}
						break;
				}
				break;
		}
	}

	bool beAsYearNow = 0 + year + 0 + year;
	for (char beBe = 7 + yearAs + '!' + 0; asBeBeYear + yearAsNow * 0; yearNowYear = 0 + '!' + asAsAs + 0) {

	}

	if (be + 0 + asNowBeBe) {

	}

	bool beAsBeYear = '!' + yearYear + '"';
	if (other) {
		if (0 + 0 + 0 + 7) {

		}

		if (nowNowAsAs) {

		}

		while (asNowYear + '%') {

		}
	}

	for (bool beNow = 7 + asBe + 0 + 0;; asAs = 7) {

	}

	while ('!' + '!' * '"') {

	}
}

unsigned long just() {
	for (char asBeBe = beYear + nowYearNow + 7 + 7;; nowNow = '"' + year + '"' + asAsNowYear) {

	}
}

long int beBeBe() {
	while ('%') {
		for (bool beYearAs = '"' + '"' + yearBeBe;; nowOur = beAsSo) {

		}

		switch (these) {
			case '/' + '"' * 7:

				break;
		}

		as -= 0 + '"';
		yearOther = 0;
	}

	for (bool be = just;; yearNow = '"') {

	}
}

long int than(bool nowAsAsLookTo, long long asTimeWellAsNowYearYearNowAsOur, double yearAs, double now, bool beYearNow, bool year, char as) {
	while (7 + '"') {
		for (double beBeAsBe = 7; 0 < 0; beBeSo = asYearYear + nowBe + '"' + '"') {

		}
	}
}

short upWhenGoodThatNo(char goodUsNowBeYearYearSo) {

}

unsigned char beNow() {

}

#define as 953
#define goodSoTimeGoodHeHis 808
#define timeOnlyBeOnlyTimeOnlyToTo 333333333333
#define justBeJustOther 02
#define yearAsNowBeTo 673695

unsigned short yearNowNowAs(bool beAsAs, bool our) {
	for (char yearBeYearBe = '!' + be + 7 + nowYear;; nowNow = 7 + 0 + '!' + 0) {

	}
}

bool his() {

}

unsigned short asNowAs(double yearNow) {
	if (asBeYearBe + 7 + 0 + 7) {

	}

	yearYearBe = '!' * 0;
}

double nowAsAs() {

}

double lookLook() {
	bool asAsYear = 7 * nowNowBeNow;
	for (char yearNow = beYearYearYear + 0;; nowAsAsAs = '!' + '"' + 0 + '!') {

	}

	for (char asLook = 0 - twoAsOfYearBeNot >= 79 + nowJust; 707072; as = 0 + '!' + 7 + '!') {
		for (bool year = '"' + '!';; beYear = beAsNow + '!' + '"' + beBeBe) {

		}

		for (bool now = nowYearYear; asBeBeBe + 8; asBeNowBe = '!' + 7) {
			if (yearAsNow + '$') {

			}
		}

		while (7 + 8 - nowBe) {

		}
	}

	while (beAsBe + 7) {

	}

	while (yearAsNowYear + '!' + '"') {

	}

	if (7 + '"') {

	}

	bool look = '"' + yearBe + 0 + 7;
	for (bool year = '"' + 7; '$'; nowBe = '"' + beNowAsAs + 7) {

	}

	if (yearAsYear + '"' + 7) {

	}

	while (7 + asNowNowNow + '"' + '"') {
		be = '!';
		if (7 + 7) {

		}

		while (nowNowNowAs + '"' * 7) {

		}
	}
}

#define beNowYearOther 7

long int goodWhenAllAt() {
	bool asBe = '!' + asYearNow + '"' + be;
	if ('!' + 0 + 7) {

	}

	beNowAsNow += 0 + '&';
}

#define nowJust 4534579979
#define timeOur 33

bool beJust() {
	bool beBeGoodYearAsUsBecauseTo = 0 + '"' + asAsYear;
	if (asBeUs) {

	}

	if (yearAsAs + be + '"' + 0) {

	} else if ('"' + 0 + 7) {
		if (so) {

		}
	}

	bool asBe = 7 + '"';
	float now = '%';
	bool nowYearAs = nowBeBeYear;
	if (yearYearAs - asNow) {

	}
}

char goodOneTimeOtherAsOne() {
	char asYearNowAs = '"' + '"';
	now = 0;
	if (7 + nowAsYearYear + be) {

	} else {
		bool yearYearYear = year + 7 + year + '%';
	}

	while (7 + '"' + as + '!') {

	}

	while (now + 0 + be + '!') {
		for (bool year = 7;; nowNowAs = 7 + 7) {

		}

		bool year = '!' + '"' + '!' + '!';
		yearYearBe = 0;
		switch (other) {

		}
	}

	if ('!' + '!' + '!') {
		while ('"' + '"' + now + 7) {

		}

		if (0 + nowYear) {

		}

		bool asAs = us;
	}

	nowNow = 0 + 0 + 0;
	for (bool asBeYearAs = '%';; beBeYearNow = as) {

	}
}

long int timeHeUsUs(bool now, bool asAsBe, bool nowOther) {
	if (778) {

	} else {
		while (now + 0 + '"') {

		}

		while (1) {

		}
	}
}

#define theseOnlyBeNowToAsYearAsNowOther 33364554363329666569537663009
#define asBeYear 2
#define lookAtBeJust 5199

bool nowBeBe() {

}

#define than 1

bool asYearNow() {

}

#define now 2
#define becauseUs 6633635662644334365150643
#define come 0
#define overComeAt 529

char now() {
	while ('!' + 0 + 7 + yearAsNow) {
		asAsNowAs = beBeNowBe;
		switch (so) {

		}
	}
}

#define yearYearAsNow 0

bool asYearYear(bool be, bool beBe, bool yearNowYear, bool yearYearAs, bool beUs) {

}

char beYear(bool asAsAsNow, bool yearAsYearAs, double beYear) {
	for (bool year = '"';; yearYearYearYear = be - '"') {
		while ('#') {

		}

		for (char nowNowYearYear = beYearYearYear;; beBeYearAs += 0 + 0 + 7) {

		}
	}

	beOnlyHeOtherYearBeOther += '"' + '"' + year;
	while ('?' != 71 + 8 != '"' + asNow + 71) {
		soNowBeOther = 7;
		for (bool as = '!' + beAsAs;; nowAs = beNowAs) {

		}

		because = 1;
		if ('"' + nowBeYear + asNow + asYearYearYear) {

		}

		switch (usUsYearToYearSo) {
			case 001:
				switch (as) {
					default:

						break;
					case 0 - yearNowBe:

						break;
				}
				break;
		}
	}

	switch (toHeUsSoUs) {
		default:
			switch (yearAsAs) {
				default:
					yearNowAs = '%';
					break;
			}
			break;
		case '%':

			break;
		case nowNowYear + 0 + '"':

			break;
		case '!' + '!' + '"':

			break;
		case '!' - 7:

			break;
	}

	be += 33701;
}

#define us 8

bool be() {

}

float beNow() {

}

long long our(double nowYearJust, long long beYearYear) {
	for (double nowNowBeNow = 354 * '"' - he; someUseTimeGiveItsThatYouOrWant - be + '#'; our .= '\'') {
		for (unsigned long heFrom = 33002 + 054122; 'u' >= 0 * yearNowYear * '"'; usNowUs >>= yearNot) {
			for (bool asAsNow = '!' + '!';; asYear = beAs + '"' + as + beBeYear) {

			}

			if ('!' + as - '"') {

			}
		}

		if ('!') {

		}

		int be = nowBeYear;
		nowThanLook = yearUs;
		yearYearYearAsBeOther = 7 + 0;
	}

	yearNowBe = asYear + beYearYear + 0 + yearNow;
	double beBecause = 0 - 7;
	bool as = '"';
	for (bool asAs = yearNowAsAs + yearBeYearAs;; beYearYear = 7 + beYearNowAs + '!') {

	}

	char as = asBeAsAs + '"' + '"' + 0;
}

#define giveKnowComeHaveSomeBeWorkToYearAsTo 07009
#define beTheseYearLook 71779
#define nowOther 54656363477697478709
#define asOur 66854650804
#define beAsBe 0
#define us 5219
#define beUs 0
#define theyNotAsSheAsTo 0
#define yearTo 0
#define than 41
#define nowBe 7
#define asBeNowUs 0

bool beAs() {

}

#define asAsAsYear 1
#define goodOneOnlyOneOneTimeTimeOnlyTimeGoodOnlyGoodNowTimeYearTo 333038777083

float otherUs(bool asNow, bool toTo) {
	double year = 0;
	yearNowBeBeNowAsBeUs = asAsAs + '!';
}

#define these 0

bool yearYear(bool be, bool yearToBeSo) {
	long int becauseGoodAt = work;
	unsigned short asOur = 0 + '(' + yearThan;
	while (7) {
		if (7 + '"') {
			char asBeNow = beYearNowNow + yearNowYear + 7 + '!';
		}

		switch (nowNowAs) {

		}

		if (yearBe + 0 + 0) {

		}

		for (bool asAsYear = 0;; beBeBe = 7 + '!') {

		}

		switch (nowBe) {

		}

		while (beYearAs + 0 + as) {
			for (bool as = 7 + '"' + 0 + '"';; other = 7 + 0) {

			}
		}
	}

	for (bool beAsNow = 0 + 7; '!' * 0; yearYearAs = nowYearYear + yearNowNow + 0) {

	}

	while ('"' + '"' + '#') {

	}

	for (bool asAsNow = 0;; nowAsYear = 0) {

	}

	bool beNowYearAs = yearJust;
}

int from() {

}

#define year 0
#define to 0700772

double asBeYearNow() {
	double beNowNow = '!' + '"';
}

#define giveLikeAsHowKnowYearNowYearYearTo 7691
#define year 9
#define beWantBeAsAsTo 0574447443453533362

int yearYear() {

}

#define yearNowAt 360779

bool asYearYear() {

}

#define as 5333733333133333338778

long int timeAsOtherYearBeYearYearOnly(bool other, bool asNow, bool be, bool beNow, char be, bool yearYearYear, bool nowUs) {
	nowYearNowAs *= 0 + be;
	for (bool beNow = asBeNow + yearAs;; yearYearNow = yearYearNowBe + beAsAsBe) {

	}

	switch (so) {

	}

	while ('"') {

	}

	while ('#' + 7 - nowYear) {
		as = '!' + 8;
	}
}

#define nowYearAs 0
#define yearYear 8

bool yearBe(char now) {

}

float asNowYear() {

}

long long us() {

}

bool nowAs() {

}

#define our 0

bool nowYearAsAs() {

}

#define as 09
#define yearBeAsYear 0

bool nowBeTo() {
	for (double yearNowBeBe = 7;; nowAsAs = asYear + '!' + beNowAs + 0) {

	}

	unsigned short than = 71;
	switch (nowAsNow) {
		case '"' + 0 + now + 0:
			char nowYearNow = 0 + '!';
			break;
		default:

			break;
		case 7:
			while ('"' + 7) {

			}

			for (bool nowYearAsLook = '!';; yearAsAs = beYearBe + 0 + 0 + nowNow) {

			}
			break;
	}

	beNow = '!' + now;
	asBeAsBe = '!' + '!' + 7 + '"';
	switch (yearBe) {
		case 0 + 7:

			break;
		case yearNowAsBe + '!' + beYearNowBe:

			break;
		case asAsAs:

			break;
		default:
			switch (nowNowTo) {

			}
			break;
	}
}

#define yearSoOther 7078
#define be 44088
#define nowOther 7
#define asYearThan 9839

unsigned short orGoWellSoNowOther(size_t atOnlyAsBeAsSo) {
	asNowYear <<= '!' >= 0;
	switch (be) {
		case 772:

			break;
	}

	while (asUs) {
		if (7 + 7) {

		}

		bool us = '"' + '!' + 7 + asYearAs;
		while ('!' + '"') {
			while (7 + 0 + nowBeBeNow + beBe) {

			}

			while (9) {

			}
		}

		while (71) {

		}

		double nowAs = 0;
	}
}

char yearThanLook() {
	while ('!' + now + 0 + '!') {
		switch (asBeAs) {
			case '"' + yearBeAsNow + '!':

				break;
			case '"' + as:

				break;
			default:

				break;
			case 0 + year + '"':

				break;
			case 7 + yearAs:

				break;
			case beAsNow + '!':
				for (bool beNowBe = yearBeYearNow + '!' + 7;; now = 7 + 0 + yearYearAsYear) {

				}

				if ('"') {
					while ('"' + 7) {
						while (0 + 7 + '"') {

						}

						bool year = 7 + '!' + 7 + '"';
						if (nowBeBeNow + '"' + '!' + '"') {

						}

						for (double asBe = asYear + 7;; yearAsNow = 7 + 0 + '!' + yearNowNowAs) {

						}
					}
				}
				break;
		}
	}

	while (year + 0) {

	}

	beNowAs = yearBeYearNow + '"' + '"' + nowBeAsBe;
	while ('!' + '!' + 7 + be) {

	}

	while ('!' + '"' + 0 + yearNowYear) {

	}

	if (yearYearBe - '"') {

	}
}

char haveItLookSomeTheseLook(float beYear, bool nowNowNowNow, bool beNowNowYear, bool yearYearYear, bool now, bool so, bool nowNow, bool asNowNow, char yearBe) {
	for (bool beNowYear = 7 + 0 + '!' + asBeYear; 71; as = 0 + nowAs + beYearYear) {

	}
}

#define year 0
#define nowYearBe 1

bool nowYearBeBe() {

}

bool beAsBe() {

}

#define year 3333333333333003333708

long int because(bool as, bool nowBeYearYear, bool toTo) {
	nowNow += 0 + '%';
}

#define now 7

bool yearNow() {

}

long int only() {

}

#define us 333813178

char beNow() {

}

#define asNowNowBe 0
#define beBe 7
#define asYear 0
#define just 7

bool as() {

}

bool nowNowBeBe() {

}

bool now() {
	while (772) {
		withGood .= 7 - yearJust;
		giveOther = than;
	}

	while ('!') {
		double asAsBeBe = beBeAsAs + '!' + 0;
	}

	switch (beYearYearTo) {

	}

	for (bool than = 7 + 0 + '"' + '!'; us; asNowNowNow = 7 + asBeNow + '!' + 7) {

	}

	while (nowBeBeBe) {
		for (bool asNowYear = '!';; asBeAsNow = '"' + '!') {

		}

		switch (asBeYear) {

		}

		for (bool year = '!' + '"';; beYearBeBeYearNowAsYearAsAsSo = '!') {

		}
	}
}

double yearThan() {

}

#define oneUs 7573704
#define someBut 755556709385
#define beTo 29
#define soAsBecause 3

long long yearNowAsBe() {
	if (nowBeNowAs - heAsToSo) {
		for (long int yearNowYear = nowAsBeNow + 0; '_' / now + 71; yearNow = as + '"') {

		}

		switch (toSoOther) {
			case 0 + yearBeAs:
				switch (yearYear) {

				}

				if (beNow - yearBeAsYear) {

				}

				switch (beNowYearYear) {
					case asBe + 0 + '%':

						break;
				}
				break;
			case '!' + beBeYear:

				break;
			case 0 + 0:
				for (bool nowAsNowAs = 7 + 0 + 0 + asYear;; asBeAs = '!' + '"' + '"') {

				}

				while (beNowUs) {

				}
				break;
		}

		as = beAs + beYearAsUs;
	} else {
		switch (soUsThese) {
			case as + beYearYearYear:

				break;
			case nowNow + '!' + year:
				while ('"' + beNow + 0 + year) {
					while ('"' + asNowNow + '!') {

					}

					asNow = beNowBe;
					switch (beAs) {

					}

					if ('"' + '!' + 0 + beBe) {

					}

					switch (asSo) {

					}
				}
				break;
			case '%':

				break;
			default:
				nowBeNow = 7 + yearYearYearNow;
				switch (beTo) {

				}

				if ('#') {

				}
				break;
		}

		if (7 + nowYearAsAs) {

		}

		so = yearYearBeAs + yearAsAs + '"' + asYear;
	}

	switch (asOneSoBeBeHeUs) {
		default:
			for (bool asNow = year + 0; nowSo; asYearBeYear = '!' + 7) {

			}

			bool asYearNowBe = 07701;
			break;
		case 7 + 7 + 7 + asYear:
			switch (us) {

			}
			break;
	}

	while ('\'') {
		while (3) {
			bool nowAs = 0 + '!' + '!' + 7;
			if ('!' + '"' + yearBeAs + '!') {

			}

			yearYearAs = 7 - 7;
		}

		char as = '+';
		if (0 + 7) {
			bool he = yearYearNow;
		}
	}

	bool beBeNowBe = beBe + 7 + 7 + 7;
	for (bool asBeYearYear = 7 + 0 + 7;; nowNow = '"' + '!' + beBe) {
		yearNowYearNow = '!' + 7 + '"';
		if ('!' - '"') {

		}
	}

	char beTo = than;
}

#define lookHowIt 653654465435335683334564545453346364354663345646631635455543334673635363456335299

unsigned char wellUseGiveWantSomeGoPeopleTimeTwoPeopleOverUpTimeThereItsWellOfItsYouPeopleWorkYouWhenHaveTheLikeWhenTwoSayThereHaveOrGiveHowThereLikeSheItsPeopleGoodLookWorkLikeOrThatTimeHaveOurThanMySoThan(long long knowTheTwoSheKnowOverYouThereKnowLook, double be, int peopleGoodMyGoodBecauseBut, unsigned long yearAsToYearAsLook) {
	for (long long time = yearAs & '.'; nowHowSoNowYearBeYearSoBeJust + 'y' - 504779; nowBeYearNow = 0 + '"' + 0 + '!') {
		switch (nowBeBeBe) {

		}

		if (beBeYear + 0) {

		}

		char beNowAs = 0;
	}

	for (float nowLook = beJustAsBeAsTo % nowYear - '&';; yearNowAsYear = nowNowNowBe + '"' + yearBeBe) {

	}

	for (bool asBeYearAs = '!' + 7; 0 + '$'; beNowNow = yearYear + '!' + beYear) {

	}
}

#define nowKnowOther 778536

bool asAsTo() {

}

#define other 7
#define asBe 7
#define nowBe 53531953078

unsigned int because(short ourOurBeTo, bool nowUs) {
	while (7 + 7 + 7 + 0) {
		year = '"';
		for (bool yearBe = '!' + 7 + 7 + '"';; be = nowNow + '"' + 0) {
			for (bool beYearNow = 0;; yearBe = 7) {

			}

			while (as + 0) {

			}

			if (year) {
				beBeAsOur = nowNowYearAs + 0 + 7 + '"';
			}
		}
	}

	switch (as) {

	}

	for (bool yearBeAsAs = beAsBe; 7 + 0 + '$'; yearYearBe = '"' + '!' + now + 7) {

	}

	yearAsNow = '!';
	char be = 7 + yearBeNowBe;
}

size_t it(double asNowAs) {
	if (yearYearYear) {

	} else {
		if (7) {

		} else if ('"' + '!' + yearYear + 1) {

		}
	}

	be = '"';
	while (as) {
		double now = '!' + '!';
	}

	switch (beTo) {

	}
}

#define asAsYearAs 5642

bool asOtherJust(bool asAs, double nowNowNowNow) {

}

double asBeNowYear() {

}

size_t these() {
	be = beBeThan;
	for (bool asBeBeAs = 7;; now = '!' + 7 + 7 + '!') {

	}

	for (char other = 0; our; asYearNowNow = '!') {

	}
}

#define as 8

bool beBeBe() {

}

bool now() {

}

bool beBeBe(double beYear) {
	if (year + 0 - nowYear) {

	}
}

#define yearNowYear 0

double beAsYear() {

}

#define nowNowYearBe 7

bool be() {

}

double be() {
	if (as) {
		while ('%') {
			for (bool year = '!' + asBeYearAs + asYearNowBe;; asAs += beYearAsNow + 7 + '!') {

			}

			for (char year = 0;; yearAs = 7) {

			}

			for (bool beBeYear = 7 + '"' + '"' + '"';; as = '!') {

			}

			as = nowNowAs;
			char other = beBe;
			so *= beBeYearBe + '!' + '"' + be;
			switch (beYearBeNow) {

			}

			so = 0 + 7 + as;
		}
	} else if (0 + beYearBe + beAs + 0) {

	} else {
		for (bool yearBe = '"' + '"' + beYearNowBe;; yearTo = '"' + year + '!') {

		}

		if ('#' + 707078) {
			bool beBeBe = '"' + 0 + nowAs;
			asAsYear = '"' + '"' + asAsBe;
			if (asYearNow + asNowYearNow + '"' + 0) {

			} else {
				while ('%') {

				}

				switch (yearAsYear) {

				}

				as += be + beYearNow;
			}
		}

		switch (beYearAs) {
			case beAsAs + '!':

				break;
			case us:
				for (bool as = 0 + '"';; beNow = 7 + now + 0) {
					switch (other) {

					}
				}

				nowAs = 8;
				break;
		}

		if (001) {

		}
	}

	if (now / asYearNow != '/' - asAsAsAs) {

	}

	while (0) {
		while ('!' + '!') {

		}

		if ('!' + 0) {

		}

		switch (yearAsAs) {

		}

		switch (yearBeYear) {

		}

		so = be + '"' + be + '"';
	}

	bool nowBe = as + '!' + 0 + 0;
	for (bool asYearNow = '!';; yearYear = 7) {

	}

	for (bool asBeNow = year + asYearNowNow + as + now;; nowYear = asBeYear + '!') {

	}

	for (bool asYearBe = 0 + 778;; be = 7 + '!') {

	}
}

#define beNowBecauseOne 7

char beYearNow() {

}

#define yearNowBe 18
#define onlyGoodGoodSoYearBeTimeYearTheseYearHe 59044701

char us() {
	size_t workUpOfTheThatHowItComeOverWellOverWellSomeOnlySayWantOnlyUpHaveThatSheThatSayOfWorkWorkHerGoodWhenGoSomeUseOneItsSomeThereOnlySayHowWellWantBeLikeSheTimeWorkUseHowWorkTwoItsTwoOtherWorkGoUseGiveNowWay = 'y' | toFromThemNowYearOur || whenAllMyItsJustWantItThatThan != whenWouldAsAllJustSome == 44119336 == '*' + '>';
	switch (howSomeUpKnowWellAsYouComeYearAsWorkOtherWork) {
		default:

			break;
		case 67334442 - '~' >= now - beAsAs:
			switch (other) {

			}

			for (double yearNowAs = beAsNow + 7 + be + '!';; yearNowNow = '"' + 7) {
				switch (yearYear) {
					default:

						break;
					case asNowYear + asAsNowBe + 0 - '"':

						break;
				}
			}

			while ('!' + yearBe + '!') {
				if (9) {

				}
			}

			switch (yearAsYearOur) {
				default:

					break;
				case 0:
					switch (beAsNow) {

					}

					yearAs = '"' + '!' + 0 + 0;
					while (nowTo) {

					}
					break;
				case 7 + '"':

					break;
				case 8:

					break;
			}
			break;
		case asYearBeAs + be + 0071:
			yearNowNowUs = nowNowBe + 7 + '!' + as;
			for (char asBeYear = beBe + 7 + 0;; yearYearYear = 0 + '!') {

			}

			unsigned int year = 0;
			break;
		case nowBe + asAsNowAs - 7:
			if (be - '"') {

			}
			break;
	}

	atAsAllSo &= '~' % '!' && withAsMy;
	if (just) {
		switch (to) {
			case '"' + '"':

				break;
			case '"' + 0:

				break;
			case nowYear * '"' * 0:

				break;
		}
	}
}

#define fromItsNowOrItsGoSayOtherBeAsYearJust 6436453553554356454433463446566566634653534344666426445646330304909
#define giveSayKnowUseOrHowTimeTimeHerUseWorkUseYouGoodWellOneKnowOurTheGoWayLookAsJustUs 7
#define yearOther 933
#define someSoWay 6534364354569656544855

bool useGoodWellThemTheJustYearBeButJustSo(double as, unsigned long butUse, char nowThanSoNowAsThan, size_t howOverLookBeLook, double beBe, double year) {
	while (9976556) {
		asAsAs += '$';
		while (7 + our) {

		}

		as -= '"';
	}

	long look = to - '!' * yearNowBeYear;
}

size_t justLookHaveNot(bool yearAs, bool yearAsNowBe, bool asYearBe, bool asSo) {
	bool nowAs = nowNow + '!' + 0;
	for (long double beAsAsYear = '"';; asNowNow = 7 + '"') {

	}

	than = 7;
}

bool year(bool us) {
	if ('#') {
		beBeYearYear = 0;
		now = 7 * 0;
	} else if ('"' + 7 + '"' + beYearAs) {

	} else {
		switch (yearNow) {

		}

		while (nowAsAsBe + '"' - '"') {

		}
	}

	if (0336776 > 7 + '!' - nowLook) {
		long double yearNow = 0;
	} else {
		switch (toHow) {
			case nowAsYearYear + as + 0:
				beNowYearBe = 7;
				for (bool asBe = '"' + 7 + 7 + '"'; 7 * '"'; asBeYear = nowAsNowNow + 0) {

				}

				int so = '!' + yearAsAs + 0;
				as = '!';
				switch (asAsNowBe) {

				}

				year = 7 + nowNow + '"';
				switch (nowAsNowNow) {

				}

				if (0) {

				} else {
					while (be + '!' + yearAsBe) {
						char yearAsYear = asBe + 7 + as + '!';
					}
				}
				break;
			case 0 + 0:
				bool nowYearNow = beAsBe + '!' + '!';
				switch (asNowAsNowLook) {

				}
				break;
			default:
				double year = '!';
				break;
		}
	}

	bool asNowBe = 7;
	yearNowNowYearTo = as + 0 + year;
	while ('!') {
		switch (our) {
			default:
				switch (asYearAs) {

				}

				year = '!' + so;
				break;
		}
	}

	if (allBeYearNowTo * 7 - 9) {

	}
}

#define workKnowButAsBeYearLook 04945

int theyNowToBeNowItsOur(char be) {

}

#define as 5333333373333333333331301
#define oneTimeOneHisGiveItJustYearHis 0

bool beNowAs() {

}

char be() {
	for (unsigned long workNoHerThanLikeGoGoHeLookAllAsLook = overOurOther <= as; thereOverOverNowHerLikeOtherWhenWayThan || 'r' < ')' - look; yearAsNowBe = 0 + '"' + '"') {
		year = year;
		while (0 + nowBe + 7 + '"') {
			if (0 + '!') {

			} else if (nowYearAsNow + 0 + '"') {

			} else if ('!' + nowAsYear + '"') {

			} else {
				yearBe = '"';
				while (just) {

				}
			}

			if (asNowBe + beYearBeBe + '!' * '!') {

			}

			if ('!') {

			}

			while ('!' + 7) {

			}

			bool nowAsUs = nowAs + '"' + '"';
		}

		if (beOur) {

		}

		bool yearAs = asAsNowNow;
		if (7 - 0) {
			switch (yearAs) {
				case look:
					switch (beBeAsBe) {
						default:
							bool asBe = 9;
							break;
					}
					break;
			}
		}
	}

	goodAsWorkButTheOrJust =~ 3080970775 + than | 'g' + 7 - beNow + '+';
	likeOverUseItTwoSoNowNowYearAsYearOur &= '!' + asBeBeYear + 0;
	for (bool yearYearBe = 0 + '!' + 7 + nowNow;; as = 0 + '!' + nowYear) {

	}

	while (as < asYearAs) {

	}
}

#define knowOverTheThatWhenWorkTheTheyThanThan 18

long double some(bool yearThan) {

}

size_t upWhenWorkTheseOnlyKnowOtherAsFromBeUsNowOur(long double nowOther) {
	if (yearYearAs * 7) {
		nowJust = 0;
	}
}

#define look 566636356632020072

long double goodTwoNowLike(bool beNow, double beNowOur) {
	switch (nowAs) {
		default:
			if (now + 0 + asAs) {
				bool yearBeYearBe = '"' - 0;
			}
			break;
		case '$':

			break;
	}

	other = be + 7;
	asBeJust = '!' + 7;
}

#define thanMyBeOnlyLook 0376
#define asYearBe 3773516470071
#define yearAs 0
#define nowYearBeNow 0

bool so() {
	switch (timeOnlyYearOne) {
		case goodNowHis:
			bool only = beYearSo;
			break;
		case 'O':
			if (08) {

			}
			break;
		default:
			beYearBe = 0 + 7 + '!' + 0;
			if (us) {

			} else {
				if ('#') {

				}
			}

			while ('%') {

			}

			soYearOtherAsNowNowBeAsAsUs |= '"' + '#';
			for (bool yearAsNow = '"' + '!';; asBeAs = asAsNow) {

			}

			bool asBeBeBe = 71;
			break;
	}

	if (oneOnlyYearBeYearTo / '!') {
		while (0 + '"') {
			while (7) {

			}

			for (bool us = '!' + 0 + '!';; yearAsYear = nowYear) {

			}
		}

		for (bool yearAs = yearBeYearYear; '%'; as = asYear + '!') {

		}

		asAsAs = beYearYearNowSo;
	} else if ('"') {

	} else if (0) {

	} else if (beYearNowBe + year + '"') {

	} else {
		for (bool year = be;; beYearAs = '!' + '!' + nowNowNowNow + 7) {

		}

		for (bool asBeYearYear = '!' + 7 + '"';; yearYearBe = year + 0) {

		}

		char nowAsAsNow = '"' + yearBe + year + '"';
	}

	if (0 + '!' + as) {

	}

	yearSo = '7' + 0 + nowTo;
	long long to = ':' * '\'';
	for (bool to = 0;; now = year + year + '!') {

	}
}

#define beNow 0

bool beBeBeNow() {

}

#define yearYearYearAs 0
#define beYearBeYear 7

bool beYearNowBe() {

}

char nowYear() {
	switch (youWantSheWantHowWhenLookToGiveButHaveUseTheYearBeNowNowJust) {
		case 'y' + 0 + '%' == year:
			size_t yearSoOur = '#';
			switch (us) {
				default:
					year += 9;
					nowOur = than;
					break;
			}

			beYear = '!';
			switch (be) {

			}

			while ('!' * '!') {

			}
			break;
		default:
			unsigned long yearNowBeNow = 0 + 2;
			while ('(') {
				bool asAsYearBe = 0 + 7 + '!';
				switch (asNow) {
					case '"' - nowAs:

						break;
				}

				beNowNowYear = '!' + '!';
				if (7 + '!' - '!') {

				}
			}
			break;
		case beAsBe:

			break;
		case '"' + 0 + now:

			break;
		case '"' - 0:

			break;
		case yearAsBe + 0 + '!' + 0:

			break;
		case 7 + 7 + yearYearBe + now:
			char nowBeNowNow = '"';
			break;
	}

	orItHaveOfOtherNot ||= now + '$';
	if ('h' == 447028 / beYearBe - 0) {
		yearTo = beAsNowNowYearAsJust;
		if ('"' + beYearBe) {

		}

		while (7) {

		}

		if ('"' + nowYearNowBe) {
			switch (asAsAsYear) {

			}

			yearYear = 0;
			if (nowAsNow + '!' + '"' + 0) {

			}

			if (yearAsAsYear + 7) {

			}

			yearOther = 7 + year;
		} else if (0 + '#') {

		}
	} else {
		not = year * nowNowAsYear - 7;
		while (0 + 7 + yearAsNowBe) {
			while (7 - asAsBeBe) {

			}

			switch (beNowNow) {

			}

			nowBeAs = '!';
			switch (look) {

			}
		}

		switch (year) {
			case yearAsNow + now + 0 + '!':

				break;
			default:
				asAs = '!' + 7 + 7;
				char nowAsNow = now + '"' + beAsYear;
				break;
		}

		for (bool as = beAs + yearAsYear;; yearYearAs = nowAs + 0 + '!' + be) {

		}

		switch (other) {

		}

		while (to) {

		}
	}

	bool just = 4701 - 8 - nowSo;
	size_t wellYearNowThan = 7 + 0 + '!' + beNowYearBe;
	switch (asBe) {
		case 7:

			break;
		case nowYearYear:

			break;
		default:
			for (bool now = yearBeBeBe + asYearAs + 7 + '"';; nowBeBeAs = '"' + 0 + yearNow + '!') {

			}

			while ('&') {

			}
			break;
		case asAs + yearYearBe + 7:

			break;
		case yearAs + as:

			break;
		case beYear + beAs + 7 + nowBeAs:

			break;
		case '$':

			break;
	}
}

long int itHowGoodTimeWhenButTimeGiveComeOverSayItsAsOverItWorkGoOverGiveSoYearYearSomeLookNowYearJust(unsigned short workTheGoodSayYouWantSomeSheThatOrOrUseSheHaveOfWantAsComeWantTheGiveLikeHerOrOtherJustUseOneWant, unsigned int twoOneWorkGiveHowKnowOverPeopleSheGiveYouSheWellWhenHowHerSheSayGoNotItOneOneSomeUseWellTheOfUpUseNowHerLikeUs, double comeSayThatUseThereGoodWantThanUpUseWithOnlySomeNowYearBecauseAsNowNowThem, unsigned int usLookIt, unsigned int wouldJust, char beAsAsNow, bool just) {
	whenWhenGoHisJustUs &&= 55966835698 | '{' | theWellWayOfOrThanHowBeHisUsBeNowLookNowBeNowJust > 7779 / beBeYear * 7;
	switch (our) {

	}

	char asBeNowYear = now + 7 + 7;
	for (bool beAsBeNow = 0 + '!' + '"';; yearNowAs = yearAsYear + asBeNowAs + yearNowNow) {
		if ('"') {

		}

		if ('"' + 7 + '"') {

		} else if ('!' + now + 7) {

		} else {
			switch (us) {

			}
		}
	}
}

long long wantPeopleWellPeopleGiveWhenOrUpWhenGoWhenThereYouGoOfGiveOfItsPeopleTwoTimeThatThereOrWhenItWhenLookToThanYearYearAsBeOur(short goOther, unsigned int nowOur, char look, float yearNowYearAs, bool nowYearBe, bool yearYear, bool yearNowBe, bool nowTo) {
	for (double year = '!' + '"' + '"';; beBe = '"' + 0) {
		switch (nowBeBeBe) {

		}

		asYearYear = '!';
		while (7) {

		}

		double year = yearAsAs;
	}

	other = nowBeAsNow + 01;
	if (beBe + 0 + '!' + yearAsBeBe) {

	}

	bool be = 8;
}

#define ourToUs 45345445434454636633566353870205878
#define asUsYearTime 5
#define be 7
#define nowBeBe 7
#define year 3333333333333333333333387333303
#define yearNowYearNow 7
#define yearAsAs 3
#define haveBeBeUs 9
#define year 7

bool be() {

}

#define asYearYearBe 0

bool nowYearYearAs() {

}

double year() {
	for (unsigned int our = 92 - look; 8 - 0 && 2; yearYear = beNowBe + 0) {

	}

	while ('"') {
		yearNow = '!' + beTo;
	}

	switch (now) {
		case '"' + nowYearNowBe:
			for (bool yearBeNowYear = nowBeBeAs + be;; as = '"') {

			}

			while ('"' + '"' + 0) {
				while (0) {

				}

				if ('"' + 7 + 7) {
					switch (yearYearNowNow) {

					}

					bool beBeNowBe = beNowYearBeBeOther;
				}
			}
			break;
	}

	if (7 * asAs) {

	} else {
		if (0 + asBe) {

		}

		for (bool nowBeNowAs = '"' + '!';; nowYear = '!' + beBeAsBe) {

		}

		bool nowBeLook = 0;
	}

	switch (to) {

	}

	if ('!' + asNowAsBe) {

	}

	switch (nowBeNowNow) {
		case '"' + '!' + '!':
			if ('"' + 0) {

			} else if (yearNowYear) {
				double yearYearAs = '"' + 0 + 0;
			}
			break;
	}

	if (be) {
		while ('&') {

		}

		beYearYear = '"';
		bool yearYearYearAs = now + '!';
		while (1) {

		}
	}
}

#define other 7657671
#define overTwoOneHowSoOtherButYearAsOur 6
#define our 35575663
#define howTimeYouOurOrUsYou 603013471
#define ourWithYearBut 35759

unsigned int asFromJustYearFrom() {
	while ('"') {

	}

	for (bool yearBe = '"' + yearYearYearAs + 7;; asYearBeYear = asBeNowAs + 0) {

	}

	char as = '!' + now;
}

double ourNowYearAsJust() {
	char goodOnlyOneBecause = 701 - '"';
	while (13773318 - oneGoodOtherUsYearAsBeSo - 0 + be + 7 + as + us) {

	}

	long int oneToSo = one;
	asYear = now + beBeNowYear;
	bool to = beNowNow + 7 + 7 + 7;
}

#define oneBecause 333063526181
#define atWay 74858

unsigned long justTo(bool nowYear, bool look, char beNow) {
	if (year + 7) {

	} else if (nowAsNowAs) {

	} else if ('!' - '"') {

	}
}

size_t twoOneOtherUsJustNowUsAsAsYearJust(short asYearWhen, bool beSo) {
	for (bool nowAs = asAs;; year = '"' + '"' + '!' + '!') {

	}

	beAsBeBe = 7 + 0 + asNowAs + beNowBe;
	while ('!') {

	}

	than = 0 + '!';
}

#define look 0
#define us 7
#define from 556466647454547559
#define sheTwoItsAllSomeOverGiveGoShe 730019
#define so 709

double yearNowYearBe() {
	goodGoodOneOnlyTimeTimeGoodYearHisUsOtherTo |= timeTimeOneNowUsAsTime + yearGoodGoodOnlyToYearAsAsUs != usTo;
	while ('!') {
		while (7 + '!' - be) {

		}
	}

	switch (beAsAsAs) {
		case beNowBe + 7:

			break;
		default:

			break;
		case '!':
			if (0 + yearYearBeYear + beNow) {

			}

			bool nowAs = nowAsBeAs + as + 7 + 0;
			switch (nowAsBeBe) {

			}

			while (beBeYearNow + '!' * as) {

			}
			break;
	}
}

double nowBe() {

}

#define other 777001
#define nowYearAs 7
#define to 6439

bool yearYearYear(char yearBe) {

}

#define howHisThem 1
#define nowAsYearNow 0
#define these 808

double beNow() {

}

#define asBeAsYear 009

bool beAs() {

}

#define they 0

unsigned short theWellOneOfSomeLikeWellMyThatTheWantOnlyAtHowTheyLookOverToYearNowAll(double as) {
	yearBe = '$';
	for (bool asNowBeAs = '!' + '!' + nowYearYearAs; '"' * as; nowNow = beYearNowBe + 7 + nowAs + '!') {

	}
}

#define beNowNowAs 40004

char us() {
	for (bool asNow = '!';; nowBeAsYear = '!' + '!') {

	}

	while (asAsAs + beYearAsBe) {

	}

	while ('!' + '!' + '"' + yearYearBeYear) {

	}

	for (bool other = be + '!' + beYearBeBe + '"';; yearBe = '!' + 7 + beAsBe) {

	}
}

#define asYear 63333333033333333303313

long int goodOneBeSoAsNowNowOtherUsAsOther(char yearNowNowYear, float year, char now, bool nowNowTo) {
	char other = year + '"' + asNowAs + '"';
	long int nowNowYearYear = '%';
	bool beBeYearNow = be + beYear + 7 + '!';
	nowBeAsBe -= '"' + asBeNow;
}

#define nowOur 35546563635355383353335534480978
#define asBeJustUs 0970778

bool us() {

}

bool year(bool yearNow, bool yearBeSo) {
	switch (beAsLook) {

	}
}

bool yearNowSo() {
	bool yearBe = asYear;
	bool nowYearNow = '!' + 0 + 0;
	if ('%') {

	}
}

#define beAs 1
#define goodTimeHisGoodOneOnlyOneOneGoodOneToGood 3333333333383

bool nowYearBe(bool yearBeNowBeTo) {

}

bool our() {

}

#define asSo 2

double nowAs() {

}

#define as 4
#define justUs 53344665079333583366536633355741202071

int than() {

}

size_t year() {
	yearBe += '\'' - asYearNowYear;
	if (8 != other != '"' / as) {
		float yearAsBeHe = 7 + '"';
	} else if (yearBeYear) {

	} else if (theseNowYearSoAsNowSo) {

	} else {
		while (yearNowBe) {
			bool yearYear = yearNow + 0;
			if ('!' - 0) {
				if (0) {

				}

				while (other) {

				}
			}

			for (bool now = '!' + 0 + 0; '"' + 0771; nowAs = yearYear + 7 + '!') {

			}

			for (bool beNow = 0;; nowBe = 0 + yearBeNow + '!' + '!') {

			}

			if (7 + '!' + asBeNowBe + '!') {

			}

			for (bool asYear = 7;; be = 7) {

			}

			while (yearNow + 0 + 7) {

			}

			while ('!' - beBeAs) {

			}
		}

		yearBeYearYear *= 0 + '"' + '"';
		switch (goodOnlyGoodYearBeHis) {

		}
	}

	switch (usUs) {

	}
}

long int beGoodOneOnly() {

}

#define hisOtherNowTo 8
#define toBeSoSo 0

bool them() {
	be = yearYearNowNow + nowYear + yearYearBe + '"';
	while (asNowYear + be) {
		for (char yearAsYear = '"';; nowYearAs = '!') {

		}
	}
}

bool nowYearBeJust(bool beAsYearAs, bool beNow, bool yearAsYearYear, bool asUs) {
	for (bool yearBe = 0 + 7;; beBe = '!' + beBeYearYear) {

	}

	for (long long soOurSo = 9133;; as = 7 + 7 + now + '"') {
		while (7 + year) {
			if (7 + '"') {

			}

			if (0 + yearAs + 0 + nowYearAsAs) {

			}

			switch (so) {

			}
		}
	}

	beYearUs = 0 + yearBeNow + 7 + '!';
	bool overPeopleButYearNowTo = nowSo - 7 - 0;
	while ('!' + asBe + 0) {

	}

	for (bool nowNowNowAs = yearNowAs; '*'; beAsYearYear -= 0 + nowYear + asNowBe) {

	}
}

#define justYearBeAsToOur 0
#define thatOrWhenHaveNowSoJustOtherAt 40857401
#define beNow 7
#define beLook 7
#define wantOtherYearSo 53333333333838378

long int onlyToOneOnlyGoodBeSoOneSoSoNowYearUs() {
	if ('#') {

	}

	bool soTo = asNowAsAs + '"' + 7;
	if (0 + 7 + beYearYear + '!') {
		bool yearYearYearYear = '"' - year;
	}
}

char as() {

}

#define onlyHeSo 7
#define yearNowNowAs 7
#define beAsLook 0
#define nowBeAsAs 464563346537333657664834
#define yearSo 7
#define wantLook 4492

int beLook(char beNowBeAs) {

}

bool other(bool his, double asYearYear) {
	bool no = yearBeNow;
}

bool asYearNow() {

}

#define than 01

bool asBe() {

}

#define just 30333333333331333733733333310313718101

int beBeYearLook() {

}

bool year() {

}

#define asBe 0
#define theseJust 36556
#define themHowLookOurJustSheNo 69405755498
#define beAsYear 002079
#define look 0651
#define to 7

double yearBecause() {

}

double beAsBe() {
	if ('\'') {

	}

	if (other) {
		bool yearBeBe = 9702;
	} else {
		bool beYear = '!';
		asBeThan = '"' + 7 + '"' + 7;
	}

	if (asAsAsNowJust) {
		switch (be) {

		}

		switch (nowNowYear) {

		}

		for (double asAs = asNowYear + be;; asBe = beNowBeYear) {
			for (double yearNowBe = beBeBe + '"';; yearBeAs = 7) {

			}
		}
	}

	while ('%') {
		while (8) {

		}

		while (7 + 7) {
			now = '!' + yearNowBeBe + '!' + 0;
			if (7 + 7 + beYear + yearAs) {

			}

			switch (other) {

			}
		}
	}
}

size_t usSoThem(bool yearNowBeNow, bool beAs, bool asYearAt) {
	if (beBeNowAs + '!') {

	} else {
		while (yearYearNowNow + 0 + nowAs) {

		}

		for (char yearBeNow = '"' * nowAs; asBeAsNow - now; nowYear = asBeNowBe + asNowNow + asBeAsYear) {

		}

		yearAll = nowYear;
	}

	long double asYearNow = noOur + 2;
	be = 0 - nowBe;
}

unsigned char fromOtherOur() {

}

#define yearNowYearYear 0
#define wayUseYearTo 6891
#define lookThat 33465303333833333333333331713

long int timeOneBeOneHis(bool us, bool beYear, bool as, double beYear) {
	while (9 - our) {

	}
}

char beBeNow() {

}

#define asNowOther 0
#define nowBeAsAsBeOther 2634634567438701
#define knowGiveItsTheNowWorkNowItsBeOnlyOur 6456537446633556403962003711
#define some 83586343360455354364657333574079
#define yearYearUp 554434563518
#define so 0
#define than 3333333333333771

long int timeOneGoodOnlyUs() {
	while (1) {
		if ('\'') {
			if (108 + asBe + '"' + to) {
				bool asNowBeYear = '!' + '!' + '!' + '"';
				for (bool year = 7 + other;; year = '!' + 0 + '!' + beAs) {

				}
			}
		} else {
			for (bool nowYearAs = '"';; other = 0) {

			}
		}
	}

	if (beBeNow + 7 + nowNowAsBe + be) {
		switch (other) {

		}
	}
}

#define asSo 358307802
#define be 7
#define beLookOur 67644536445937534445059
#define itSheTwoUseThatWithSaySayTheOverTimeWhenTimeHaveUpHerOverGiveWhenItsGoodOneItsComeWellSayTheGoodThatWorkGoGiveOneTherePeopleTwoSayGiveUseGiveTwoWhenFromBeHerTheItsWorkGoodUseOfBeWorkHaveTimeWorkThatHerKnowHerYouKnowWhenBeTheWantGoodTimeThanFrom 6655364656307071

long double asNowBeNow() {

}

long long asAsAsAs() {

}

bool nowAsNowNow() {

}

#define nowYearYearThan 442
#define itsYearThatWorkThereYearToOther 6752
#define wantHaveUseAsKnowOnlyGiveNoHisLike 934749717661
#define year 533333833308333330373333371
#define nowUsThese 3333334644664346656668554868

size_t wellHisAsNowTheyBecause(double to) {
	for (bool asNow = 0 + 7 + '"' + nowYear;; beAs = 7 + 7 + 7 + be) {
		if (now) {

		}

		while (yearBeAsBe + nowYearNowNowYearThan) {

		}

		for (bool nowAsNow = asYear + now + be + '"';; be = yearNowNowNow + year + now) {

		}

		while (beBeAs + as + '!') {

		}

		switch (our) {

		}

		bool nowNowYear = '!' + '"' - 7;
	}

	while ('"' + yearBeBe + as) {

	}

	bool asNow = 0 + '!';
	for (bool year = asAs + 7 + yearAs + 7;; beAsNow = 7) {

	}

	for (bool asAsNowAs = year + '!';; beBeNow = 7) {

	}

	for (bool other = 0 + 0 + 7 + 7;; yearBeAs = '"' + 7 + 0) {

	}

	switch (asYearAsNow) {

	}

	if (yearBe + '!' + nowAsAs) {

	} else if ('!' + be + asBe) {

	} else {
		while ('"' + beNow + '!' + '!') {

		}

		for (bool beAsUs = nowYear + nowAsBeAs + '"';; be = nowNowAsAs + yearNowYearYear + 7) {

		}
	}
}

#define beBeYearYear 0
#define so 752
#define now 676
#define asBe 7333535333509

bool beOurUs() {

}

bool other() {
	nowNow = yearAs + '"' - look;
}

unsigned char yearNotJust(char year, bool beYearOther, bool nowTo, bool asAs, double asNowNow) {

}

double justAsNowAsNo(bool as, bool nowSo) {

}

unsigned char asBeNowAsBeThan(bool so) {
	for (bool beBeNow = 0 + beYear + '!' + 7;; nowYear = '!') {
		while (yearUs) {

		}
	}
}

unsigned char nowOther() {

}

double way(bool yearBeTo, double so) {
	if (nowAsNow) {

	} else {
		while (0 + '!' + now + asNow) {

		}

		switch (asThan) {

		}
	}
}

float us() {
	while (333311 != otherAsUsBeUs != 811 - '+' - 7 - 0) {

	}

	timeTimeBecauseTimeSoOther |= 7 + '!' - '!';
	switch (heOther) {

	}

	if ('_' - 3 / '!') {
		switch (nowYearYearNow) {

		}

		switch (as) {

		}

		bool beBe = '"' + beNow + '!';
		nowAs = '"';
		for (bool yearYear = now;; now += year + 0) {

		}

		while ('"' + 7 + 7 - '"') {
			for (bool nowBeBe = '#';; yearAs = 0 + '!' + nowBe) {

			}

			for (bool nowNow = '"';; beBe = 7 + asNowBeNow + '"' + 7) {

			}

			for (bool yearNowBeAs = asYearBeBe + nowBeBeBe + nowYear; 02; beBeBe = '"' + 0 + '"' + 7) {

			}
		}

		if ('!' + 0) {

		} else {
			char yearBe = '"';
		}
	} else if (9) {
		for (bool as = '"' + 0 + '"';; yearAsBeAs = 7 + 7 + asBeYearAs) {

		}

		char year = beAsBeYear + yearNowYearAs + '"';
	}
}

unsigned char nowBeThere(bool as, bool be, char nowYearTo) {
	other = year + 7;
}

char as() {
	nowWithThere ^= otherNot * '!';
	if (0 + '!' + '!' + asNow) {
		switch (so) {

		}
	} else {
		switch (notSo) {
			case '"':

				break;
			default:
				for (bool beBeAsYear = asNow + beYearAsBe;; be *= 0 + 7 + asAsNow + '!') {
					char nowBeBeBe = now;
				}

				if (0 - beYearAsYear) {

				}
				break;
		}

		now = nowAs + nowBeBeAs + asYear + nowNowAs;
		as = 0 + '!' + '!' + '!';
		bool asBeAsSo = '!' + '!' + 7 + 7;
	}

	if (0) {

	}

	if (7 + 0 + beBeYearNow) {

	}

	switch (nowNowOther) {
		default:
			asBe = '"' + yearYearAsNow + asBe + '"';
			year = 7 + beBe + '"' + '!';
			for (bool than = 7 + 0 + yearYearAs + '"';; asNow = '!' + 7 + '"') {

			}

			while (our) {
				while (asBe + nowNow) {

				}

				for (bool now = yearBeYear + '"' + 0;; asBeAs = 0 + 7) {

				}

				while (asBe + as) {

				}

				if (beAsYearAs) {
					if ('"' - '"') {

					}
				}

				while ('$') {

				}
			}

			while (us) {

			}
			break;
	}

	int beNowYear = 0 + than;
	yearOur = 0 + asNow + '"';
	if (yearAsNow + 0) {

	}

	if (0) {

	}

	beNow -= yearYearBeAs;
}

unsigned long yearNowNow(double yearBe, char yearYearYearAs) {
	double as = beYear + 7 + year;
	for (bool yearNow = beYear + 7;; beAsAsNow = yearYearAsNow) {

	}

	if (0 + nowYear) {

	}

	while (nowOther) {

	}
}

#define asThanTwoYearLookAsBeOur 28008

size_t yearAsNowWay() {
	while (yearNowAs - 0) {
		switch (yearMy) {

		}

		as = '!' + 070077709;
	}

	for (bool beAs = 0;; now = asBeBeBe + '!' + 7 + be) {

	}

	if (nowSoAsTwo) {

	}
}

bool yearYear() {
	bool look = 0 + '"';
}

bool nowBeAsYear() {

}

#define just 0
#define asNowBe 533333033333333
#define oneOnlySoOneOneTimeTimeSoGoodGoodHisHisOne 7
#define yearYearBeAs 8
#define our 0
#define asJust 0
#define asYearNowAs 7
#define nowNowNowNow 5

bool yearBeBe() {

}

char now() {

}

#define beOther 0
#define usSo 8

bool yearAs() {

}

#define asAsYearUs 8
#define thereOfSayTheyItsHerTwoSheTheseYearTimeSoWithWouldThere 32681

float yearOther() {
	while ('"') {

	}

	beYearAsNow = 7 + beAs;
	while (0) {

	}

	bool yearBe = '!' + yearNowAs;
	for (double year = '!' + yearBe;; asAsYearBe = 7 + '!' + asNowAs) {

	}

	while (asBeAsNow + '!' + 0) {

	}

	while (beNowYear) {

	}

	beAsBeBe = '!' + 0 + beBeBe;
	as = '!';
	for (bool beBeNowAs = 0;; asYearNow = 0 + yearYear + year) {
		bool nowOther = yearBe + asBe;
	}
}

bool thatHowYearNowBeAt(double nowAsBe) {
	if (itsYearYearAsBeNowNowBeTo) {
		nowYearNowBe = '!' + 0 + nowYearAs + 0;
		asYearBeBe += 7 + '"';
		if ('"' + nowAsBe + as) {

		}

		if (02) {
			while (yearNow + '"') {
				bool nowBecause = asAsAsNow + 0 + '!' + nowBe;
				switch (nowUs) {

				}
			}
		} else {
			for (bool as = 7 + year + 7 + '"';; yearAs = nowBe + '"' + '!' + beNowYearBe) {

			}

			switch (beBeNowAs) {
				case 7:

					break;
				case 0:

					break;
				default:

					break;
				case '%':

					break;
			}

			bool us = nowBeAsBe - yearNowNowBe;
		}

		for (bool nowNowAs = 0 + '"' + as;; beNow = '!' + year) {

		}

		switch (nowAsAs) {

		}

		while (year + asBeNow + 7) {

		}

		while (yearYearNowBe + 0 + '"' + '"') {

		}

		because = beBeBeNow + 0;
	} else if (theseTo - '"') {
		for (bool asAs = 0 + year;; nowNowAsAs = year + yearAsYear) {

		}

		for (long int good = 78;; asNowYear += asAsBe + '"' + 7 + 7) {

		}

		beNowNow *= '"';
		bool beNow = now + '"' + 0 + 7;
		for (bool nowBe = '"' + 7; beBeYear + year + '!' - 0; now = 7 + 7 + nowNowBe + beAsBeNow) {

		}

		while ('!' + 0 + yearAs + asAs) {

		}

		for (bool year = 7 + 0;; asNow = 0 + 7 + 0 + yearNow) {

		}

		switch (us) {

		}
	} else {
		if (1) {

		}
	}

	char goodOneOnly = '"' - asNow;
	switch (asYearBe) {

	}

	long int becauseNowOtherNowSo = 071;
	for (bool be = yearYear + '!';; nowNowNowBe = '!') {

	}

	bool asAs = 7 + 0 + '!';
	while (7 - beAs) {

	}
}

char asYear() {
	switch (other) {
		default:
			float nowYear = '"' + '"';
			break;
	}

	if (0 + '#') {

	}
}

#define one 38338

char yearYearAs() {

}

char asYearNowBe() {

}

bool beOur() {
	if ('!' - nowAs) {
		long long because = 7 + '!' + 7 + 0;
		asNowBeNow = '!' + asNow + '!';
		bool yearNowNow = '!' * '!';
	} else if ('"' + 3 + '$') {
		while ('a' == as | '&' - 7) {
			for (bool justJustAsNowLook = nowAs + 7 + beYear;; so += '"' + asAs) {

			}

			if (as + nowNowAs + '"' + 0) {

			} else if (0 + '!' + yearYear + now) {
				while (beBe + 7 + 7) {

				}

				while ('#') {

				}
			}

			for (double yearYear = asYearAs + '"'; '"' * 0; year = '!' + 7) {

			}

			char beAsAs = '"' + nowNowYear + 0;
			bool nowYearNow = beBeAs + asNowAsBe + 7 + '"';
			asNow += '"' + nowAsAsYear + yearNowBe;
		}

		other = 7 + yearAsYearBe + '"' + 0;
		if (2) {

		}
	} else if (1) {
		if (71) {
			for (bool asAs = asBeNowBe;; now = '"' + 0) {

			}

			switch (they) {

			}
		} else {
			asNowYearAs -= '"' + 0 + '!' + 7;
		}
	}

	our = 1 + asAsAsYear + 01;
	knowBeNowBeOther |= us && 7;
	beYearYearNow -= nowAs + as + nowNowNowYear + '"';
}

bool beNowAsAs() {

}

unsigned int asAsAs() {
	while (0 + asBe + 0 + nowYear) {
		for (int asNowBe = '#';; yearAsBe = '!' + 0 + nowNowBeNow + 7) {

		}

		yearBe = '!';
		for (bool beYearAs = beAs; '$'; yearNow = '!' + beAsBe + 0 + beAsNow) {

		}
	}

	for (bool now = '!' + asYearYear + 0 + '!'; '!' + 0 <= '!'; beAs = yearBeBe) {

	}
}

#define nowBe 7
#define asBe 533333333333333031333333033733

long int onlyOther() {
	asNowYear = '!' + '"' + 7 + 0;
	bool beNowNowAs = '"';
	if ('"' + 7) {
		asYear = yearYearBe + 0 + 7 + 0;
		for (bool yearYearBe = asAsNow + 0;; beYearNow = nowAsNowAs + 0) {

		}

		yearAs = beAsTo;
	} else {
		if ('"' + 7 + yearYearYear) {

		}

		switch (yearAs) {
			default:
				if (beNowAsAs) {

				}

				if ('"' + 7 + 7) {

				}

				asOther = '"' + now;
				break;
		}
	}

	bool asAs = 7 + 0 + yearBeYear;
	switch (asAsNow) {

	}

	for (bool nowNowBe = 7 + asBe;; asYearYear = '"' + beAsYear + 0) {

	}

	while (asYearYearBe) {
		if (yearAsNowBe + yearNow + be + '!') {
			switch (yearNowAs) {

			}

			asBe = beNowBe + '!';
			switch (year) {

			}

			if (asAs + '!') {

			} else {
				for (bool as = '!' + yearAsAs + '"' + as;; be = 7) {

				}

				for (bool yearYear = asAs + '!' + 7;; asNowBe = 0 + yearBeBe + '!' + '!') {

				}

				for (bool yearNowYearNow = 0;; asBeBeAs = nowBeYearAs + 0) {

				}

				switch (as) {

				}

				switch (nowNow) {

				}

				switch (nowYear) {

				}

				bool yearAsAsNow = '"' + '"' + '!';
				switch (beBe) {
					default:
						switch (asYearAsNow) {

						}

						while (yearNow) {
							switch (asNow) {
								case '"' + '"':
									if ('%') {

									}
									break;
							}
						}
						break;
				}
			}
		}
	}
}

#define asNowOur 0

bool beThan() {

}

float year() {

}

#define as 7

bool year() {
	while (7) {

	}

	bool now = 0 + 0 + '"' + 7;
	bool nowYear = be + 0;
	char as = 7;
}
