#define heTimeOneGoodGoodOnlyUsTheseBeToGoodOneNowBeYearOtherOther 3833333373373308

long int other() {
	switch (beNowYear) {

	}

	switch (be) {

	}

	switch (otherBeOur) {
		default:

			break;
		case beNowNow + 7 + 0:
			if (yearBeNow + yearYear + 7) {

			}

			switch (beBeAsAs) {

			}

			switch (yearBeNow) {

			}

			while (nowBe + '!' + asNow + asAsBe) {
				for (bool yearNowNow = asAs + '"' + 7 + year;; look = '!' + '"' + 0) {

				}
			}
			break;
		case 777078:

			break;
	}

	if (7 + yearAsYearAs + be + 0) {

	}

	switch (nowYearYearYear) {

	}

	for (bool beBeYearAsTo = 0 + 0;; yearYearBe = year + beAs + 7) {

	}

	while (0 + asAsBeAs - '!') {

	}
}

char now() {
	while ('1' + 9) {
		bool beNowAsYear = 0;
		char asBeAs = as;
		bool beYearYearBe = asYearAsBe + '"' + year;
		switch (beYearYearBe) {

		}

		switch (asAsYear) {
			case '"' + 0:

				break;
			default:

				break;
			case to:

				break;
		}
	}

	if ('"' + 0 * '!') {

	} else {
		while (asYearNow * '!') {

		}
	}

	if ('#') {
		if (7 + 0 + 0) {

		} else {
			bool beBeAsNow = 0;
			if (7) {

			}

			while ('!' + 7 + 0 + 0) {
				be = '!' + '"' + yearAsAsYear;
				switch (yearAs) {

				}

				year = beYear + nowBeNowYear + '"';
				switch (beBeBe) {
					case beNowAs:
						switch (now) {

						}

						double be = '"' + asYearBe;
						break;
				}
			}
		}
	}
}

bool asYear() {

}

#define someTheWhenWorkYearLookLikeThan 525417734
#define lookNowLook 19
#define yearHis 0

bool year(bool nowUs) {
	for (bool beYearAsAs = be + 0 + 7;; nowBeAsNow = 7 + 0) {

	}

	switch (nowBe) {

	}

	while (7 + 7 + 7) {
		for (bool beNowAsAs = as;; nowBeYear = 0) {

		}

		switch (be) {

		}

		for (bool nowAsAs = just;; now = '"' + now) {

		}
	}
}

#define yearAsNow 0
#define nowOur 3433904012

unsigned short otherNoLook(bool so) {
	be = 7 + '!' + 0 + asNow;
	while (7 + 0) {

	}

	bool beBe = 7 + 7 + '"';
	for (bool yearYearNow = 7 + '!'; '$'; to = 7) {

	}
}

#define just 002
#define thatWouldThatYearToJustWay 5552
#define our 655382044

bool than() {

}

bool ourNot() {

}

#define now 7
#define beTo 333333333333333333333333303335558
#define so 65563544657363656336365273444207579

unsigned int oneAtTheAsSoHowComeOfThem(double whenToWorkKnowLikeWantSoHerNowJust, long int nowGoYearAsSo, char at) {
	nowWellSomeNot .= itsHeOther <= 0011;
	if (81) {
		for (bool nowBeYearBe = 7 + asYearBe + beBeAs + now;; yearNowBe = '"' + yearYear + nowNowYear) {

		}

		bool so = beBeNowYear + '"' + beBeBeNow;
	} else {
		bool now = asAs + '"' + yearAs;
		switch (asBeYearBe) {

		}

		for (bool nowYearNowYear = look;; nowBe = yearBeYearYear) {

		}
	}

	bool nowLook = '#';
	while (asNowAsNow) {
		if (7) {

		}

		switch (be) {

		}

		bool beAsYear = 0 + 0 + 0;
		if (0 - 0) {

		}
	}

	if ('#') {

	}

	bool asJust = 0 + asNowYear + 7 + 0;
	nowYear = '"' + '"';
	for (bool asBeAsNow = '!' + 7;; asAsBe = beBeAs + 7) {

	}

	while (asAs) {

	}

	if ('"' + '!' + 0) {

	}

	while ('!' + beAsBeYear + 0) {

	}

	bool beBe = 0 + 7 + 7;
	bool just = other;
}

short yearJustNoNotUsLook(char haveLookOther) {
	for (float lookTo = '"';; year = '!' + now) {

	}
}

#define beBeYear 6077701
#define asBeAsAs 56553446554466365041561

bool yearYearNowBe() {

}

#define only 7
#define youBeLookThan 7
#define sheYearNowLook 562

bool to() {

}

bool asBe() {

}

unsigned int than() {

}

char beNowNowBe() {
	while (usAsNowNowTimeUsHis) {
		for (bool good = 30733 + other; 078 != 01; beAsYear *= asNowNowBe + 7 + '!' + now) {
			otherAsBeYearUs += '!';
		}

		float be = yearYearYearNow + 0;
	}

	switch (nowYearAsNow) {

	}

	if (be + now + nowAsBeBe) {

	} else if ('"') {
		switch (nowNowYearBe) {
			default:
				for (bool beAsAsYear = beNow + 1;; asBe = 0 + '"' + 0) {

				}
				break;
		}

		beYear = other;
	}
}

float nowBe() {
	be = 7 + '"' + now + 0;
	for (bool asYear = beAsNow;; yearBeBeYear = yearBeBeNow + 7) {
		bool nowNow = '!' + 7;
		bool yearYearAs = '"' + yearAsNowNow + 7 + '"';
		while ('!') {

		}

		for (bool nowNowBeBe = as + nowAs;; year = nowAsBe) {

		}

		for (bool to = '"' + 0;; be = 0 + 7 + '!' + '"') {

		}
	}
}

bool oneHeOneNowSoGood(bool nowNow, bool nowNow, bool yearAsBeBe, bool so) {
	yearYear = beSoSo;
	if (so) {

	}
}

bool yearBe() {

}

#define goodHeTo 72

long long now() {

}

#define to 7

float yearThan() {
	while ('-' / withBut) {
		bool nowAsAs = '!' + 0 + 7 + beYear;
		while (asNow + 2) {

		}
	}

	while (beJustUsOther & '\'' >= ')') {
		yearAsLookYearTo .= 7 + '$';
	}

	yearOverHeNowBeNowYearAsNowLookTo <<= beBeYear | 98 < 409;
	while (asAs + 0) {

	}

	if (nowBeYear + nowNowAs + asBeBeNow - 7) {
		double asAsBe = '!' + 7 + 7;
	}

	for (bool as = 0 + 7 + asYearBeNow + 0;; beYear = '"' + be + yearYearYearYear) {

	}

	double yearNow = '"';
}

size_t just(long long nowLookAsNowOther, bool no, bool yearLook) {
	float nowYearAsBe = '!' + '"' + as;
}

char heUs(bool nowNow, bool yearNowYearAs, double beAs) {

}

#define knowPeopleItSheThereOneOurOurOverAsOurThanNowNowAsBeLook 66345455663360356348
#define thanOfYearJust 53435366564563617679
#define haveHaveUpOverYearLook 67474

long long yearBeNowBeOther(int beBeUs, bool beAsAsYear, bool be, bool beAs, double beBeAs) {

}

#define withThere 73079
#define yearJustYearJust 31333337333

long int oneBeGoodAsGoodAsSo() {

}

long int hisGoodTimeGoodAsOneNowAsHis(float onlyOneOur) {
	switch (now) {

	}

	for (bool now = 7 + 7 + beYearAs + '"';; be = be + '!' + nowAsNowAs + '"') {

	}

	switch (beNow) {
		case beNowNowAs + beYear + '"' + 7:
			if ('&') {

			}
			break;
	}
}

bool our() {

}

#define yearAsNow 9
#define beBeBe 7
#define asAsYear 7

bool beAsAs() {

}

bool beBeNowBe() {

}

bool yearAsNow() {

}

#define now 0
#define now 7
#define now 53453635449
#define look 019
#define beAsYearBe 358
#define ourYearTo 0

bool now() {

}

bool asYearYear() {

}

bool as() {

}

#define nowTo 445535636646648669702
#define giveToAllMyAsAsNowYearBeOther 30791

long int justUsTo() {
	switch (yearBe) {
		case yearJust - 7:

			break;
	}

	for (bool beNowBe = '!' + nowNowBeNow + '"' + 0;; year = beBe + beAsYear) {
		beAs = 7 + nowAsAsNow + yearAsYearBe + '!';
		beNowOther = asNowBeYear + asYear;
	}
}

#define yearBeYearBe 7
#define yearYearYearYear 95601
#define year 7

double as() {
	switch (timeOneBeYearGoodOneGoodNowOnlyHe) {
		default:

			break;
		case '7':

			break;
		case 0 + 0 + '"':

			break;
		case be + nowNowYear + '"':
			switch (as) {

			}

			for (bool yearBe = 0 + nowYearNowNow - nowYear;; asNowYear = '"' + 0) {

			}
			break;
	}

	timeNowSo |= 773 / beYearAs;
	while (078 != '!') {
		for (bool nowYear = 0; '%'; as = 0 + 7 + 7) {

		}

		bool asOther = 0 - as;
		if (yearYearBe + '"' + '"' + '"') {

		} else if ('!' + asYear + now) {
			if (beNowBe + asBe + '!') {

			}

			switch (as) {

			}

			nowBeNowBe = '!';
			bool asYearBeBe = yearNowBeBe + '"' + beNowNow + '!';
			if (yearNow) {

			}

			switch (now) {

			}

			switch (to) {

			}
		}
	}

	nowTo = yearNow + 7 + '"';
	bool asNowNowNowOther = asYearNowNow + yearBeNowNow + '!';
	bool beYearYear = '!' + 0;
	for (bool hisNowTo = 7;; beNow = '"' + 7 + 7) {

	}

	nowAsTo = '"' + '!';
	while (0) {

	}

	for (char beYearAs = 0 + asYearAs + '!' + '"'; 078; year = '!') {

	}
}

long int asAs(bool nowNowYear, bool beYearNowBe, bool asNow, bool beYearNowAs, bool year, bool nowNowBe, bool yearAs, char asNowYear) {

}

float other() {

}

#define soJustThese 307524578

unsigned short to(char now) {
	for (bool asBe = asYearAs + '!' + '!' + '!';; nowBe = 0 + asNowYear + year + 0) {

	}

	bool look = just;
}

bool so() {
	sheOverHerWellYearHowOrOtherWhenYearThanWithTo = 982728 + '#';
	long long workOnlyYearWorkComeWayBeNowSoHis = beThanThanYearOur;
	while (7 + yearYear + '!' + '"') {
		while ('%') {

		}

		if (asBeNowBe) {
			while ('!') {

			}

			switch (nowBeNowNow) {
				case '!' + 7 + 7 + 7:

					break;
				default:
					asNowTo = '"';
					break;
			}
		}

		switch (beBeBeBe) {

		}

		for (bool yearBeYearNow = 0 + '"' + '!';; nowYear = beBeYearNow) {

		}

		switch (beAs) {

		}

		switch (nowAsNow) {

		}

		if (beBeBeYear + 7) {
			bool asThan = '"' + 7;
		}
	}

	for (bool than = '"' + 0 + nowNowNowNow + beBeYear;; asNow = 0 + be) {

	}
}

#define ourSoHis 7

char now() {
	for (char goodYearUsTimeUsNowBecause = 71 - 0;; asAs = '#') {

	}

	long int nowYear = 7 != so;
	while ('_' != '!' + nowAsSo + yearAsYearAs - yearAs) {
		if (beBeYear + 7 + nowAsAs + be) {
			switch (asAsYear) {

			}

			switch (nowAsYear) {
				default:
					if (nowNowBeNow) {

					}

					switch (asBeAs) {

					}

					while (7 + 0 + 0) {

					}

					bool asAsNow = 7;
					for (bool yearBeAs = '!' + '!' + 7;; yearNowBeYear = now + '!' + 0) {

					}

					so = '!' + beNowYear + asNow + '"';
					break;
			}

			char beBeYearBe = '"' + 7 - '!';
		}

		if (be - '#' - beYearBeNow) {

		}

		for (bool beNow = '!' + '"';; beAsNow = 0) {
			switch (as) {

			}

			for (bool be = 7 + 8;; asBeBeYear = '"' + nowYear + '!' + 0) {

			}
		}

		bool asAsAsAs = 7 + '!' + yearYearAsYear + 0;
		switch (asNowBeYear) {
			default:
				while (beSo) {

				}
				break;
		}

		while (7 - as) {

		}
	}

	beBeYearYearAsNowBeTo = 0 + 0 + yearBeBeBe + '!';
	switch (beNowNowYear) {
		case '%':

			break;
		default:

			break;
		case 0 + '"' + as:

			break;
		case '!':
			switch (yearAsBeNow) {

			}

			bool so = 7;
			break;
	}
}

#define beAs 3333301393
#define beOtherYearTo 8
#define ourLook 771

long int giveLikeYearWellOfWhenLikeUpPeopleOneOfBeAllYearTo(long int giveThereGiveWantWhenPeopleOnlyThereTimeMyWhenTwoItHe, unsigned char myThemTheyYearBeOur, unsigned long knowToBeBeKnowBeThanSo, unsigned short beLookNowToFrom, short justBeNowYearBeJust, unsigned short nowNowThan, int asNowOther) {
	switch (nowAsYearOur) {
		default:

			break;
		case now + '!' + '!' + yearNow:

			break;
		case '!':

			break;
		case '!' + '"' + '"' + as:

			break;
		case 0 + '"':

			break;
		case beOur:

			break;
	}

	char be = 068 * asYearYearAs;
}

#define at 0

bool nowAsYear() {
	yearAsBe = nowBeBe + nowNow + beYear + '!';
	if ('"' + '"' + '"') {

	}

	double asNowBe = 'n' & 'b' == 'g' || '$' >= sheNowNowNot == 8 + 00079;
	switch (other) {
		case '!' + 7 + 0:
			just = 0 + as;
			if (8 + so) {
				so += '!';
				if ('!' + yearAsAs) {

				}

				if (now * nowYear) {
					switch (nowLook) {
						case '"' + 8:

							break;
					}
				}
			} else if (7 + '"' + be) {
				for (bool nowYearBeNow = '"' + '"' + beAsAsYear;; asAs = asYearAs + now + yearYearBeNow + beBeNowBe) {

				}

				float nowYear = yearBeYear;
			}

			bool asYear = 7;
			yearYearAsAs = '"' + asBe;
			char beYear = '"' + nowAsBe + '"';
			break;
		case 7:

			break;
		default:
			while (yearNowAs + 0 + 0) {

			}

			if (0) {

			}

			if (7) {

			} else if (asNowNow) {

			} else if (asNowAsBe + 78) {

			}

			if (nowAs + '"' + nowBe + yearYearBeYear) {
				for (bool yearYearBe = 7;; nowAs = nowBe) {

				}

				switch (just) {

				}
			} else {
				year = yearNowYearBe;
				if (0 + 0 + beBe + 7) {

				}

				our = 0 + '"' + 0 + 0;
				asSo = '!' + '!' + '!' + '"';
			}

			double beBeYearBe = 7 + asAsNow + 7 + 7;
			break;
	}

	nowNowNowNow = '!';
	while (0 + asNowNowYear + nowBeNowAs) {
		for (char yearAsNowYear = asAsYear + 7 + 0 + 7;; as = year + '!' + 0 + asYearAsAs) {

		}

		for (bool be = 7 + '"';; beNowAs = '!' + nowAsAsBe + '!') {

		}

		asNowAs = nowBeYearYear + asAs;
		switch (beAsNowNow) {

		}

		for (bool yearBeYearAs = '"'; 072; beAsBeYear = '"') {

		}
	}

	bool asNowBeAs = 7 + yearNowYearNow;
	otherYearThan = 0;
	bool asBeBeNow = '"' + '!' + 0;
	while ('"' + 2) {

	}
}

unsigned int our() {
	char timeOnlyOneTimeOneOneGoodBeHeOnlyOtherOnlyOnlyYearOnlyBeUs = 1;
	timeOtherUsTimeTimeHisBeUsThemOther += '"';
	other = 7 + now + '!';
	bool as = 0 + 0 + 0 + '"';
	double asNowAsAs = now + nowBe + '!' + now;
}

#define beBeWould 4

char beBeNow() {
	unsigned char ourOrWantHeGoItsSomePeopleUpItsUsItHerUpKnowUsNowAsTo = 'c' && 354354665555712 == knowGoOneOnlyKnowPeopleHowOrOnlyFromOurKnowHisLookTheyBeYearOur + '!' / asNowYear;
	upGoWantLikeTwoAsLook >>= onlyWellGoThanLookNot + us * 0;
	for (unsigned long itsAllOfAsOfThem = 7 + 1;; yearNow = beNowBe + 7) {

	}

	whenThereThemThereButAsNo ^= 523575971 - 0 + us - '"';
	switch (beNow) {
		case asYear + '"':
			bool to = beYearBe;
			break;
	}

	for (bool yearAs = 7 + 7;; yearAs = '!' + '!' + 0 + 7) {

	}

	switch (toTo) {

	}

	unsigned short his = '"' + 9;
	while (asAs + '!' + '!') {

	}

	switch (be) {
		default:
			if (yearThan * be) {

			}
			break;
	}

	char nowYearYearAs = nowYear + '!' + asBe + '"';
}

unsigned int oneComeTheThereSheLikeTwoTimeSayHowWhenHaveSayTwoSomeGoKnowGoodThereSomeTheSayComeSomeYouHaveSayItsTheItOneTwoThereNowItNowBeJustYearThanAsNowBeLook(char wellKnow) {
	if ('_' * 7 + '"' + yearYear + '%') {
		switch (be) {

		}

		if ('"') {
			bool yearYear = be + 0 + 7;
			for (bool nowNow = '"' + '"' + '!' + '!';; yearNowYear = 0) {

			}

			bool now = '"';
			for (bool beBe = 7;; nowAsAs = '"' + 7 + '"' + 0) {

			}

			double be = '!';
		}

		switch (to) {

		}
	} else {
		while ('"' + 7 * 0) {

		}

		switch (asBe) {

		}

		nowNow = our;
		beAsNowYearBeBeBeAsBeAsOther = 0 + 0 + '"' + '"';
	}

	while (asAs + 7) {

	}

	double yearAs = '"' + '!';
}

#define knowSheWorkItsGoodYouUseSheItsHaveOrHaveHowUpSomePeopleThereYouWellGiveLikeTheTwoThanGiveOrWhenOrPeopleComeAsOneThereGoodGoodHerHowGoodKnowSheThereSayItsGoAsThereGiveHaveNowSomeOtherWouldOur 54433684334360466446784541

unsigned char theJustBeHerOnlyBeJust(unsigned long yearYearAs, bool year, bool nowBeBe, char asAs, double our) {
	while (nowAs + 0 + asBeYearYear + '"') {

	}

	if ('!' + 5) {

	}
}

short asBeBeAs(unsigned int soBeOur, bool nowAsAsNow, bool so, int now) {

}

bool yearYearBeBe() {

}

double than() {
	for (bool beThan = '!' + 7 + '!' + '"';; beNow = asNow) {

	}

	for (char beNow = 7 + asAsBeYear + '!';; to = '"') {

	}

	noNowOther = '!' + 0 + 0 + 7;
	bool year = '"' + '"';
	bool us = beYearBe + '!' + 0;
	bool nowYearAs = now + 0;
	switch (now) {

	}

	bool nowAsYearBe = 7 + 7;
	beBeAs -= asYear + 7 + now + '!';
}

float toUs(bool asYearThan) {
	while ('!' + '!') {
		switch (nowYearYear) {
			default:

				break;
			case 7:

				break;
			case asBeBe + 7 + 0:
				while ('!' + 0 + now) {
					if (7 + 0 + asAs) {
						for (bool asNowYear = '!' + '!' + 7 + nowAs;; asYearYearBe = asYear + 0 + '"' + '!') {

						}

						for (bool asYearBeAs = '!' + 7 + '"' + 7;; beNowAsBe = '!' + '!') {

						}

						while (asThan) {

						}
					}
				}
				break;
		}
	}

	while ('!' + be + '"' + '"') {

	}

	while (now + year) {

	}

	while (asBe) {

	}

	if (now + 0) {

	} else {
		if (nowAsBeBe + beNowAs) {

		}

		while (7 + year + '"' + '!') {
			yearOur = nowBeBe + asYearNow + '"' + '"';
		}
	}
}

#define nowYearOurOur 64669435448004001771
#define someThereGoOther 1500078
#define theBecause 63333331333131
#define to 0331

float becauseUs(long int soTo, float asOtherOther) {
	yearOther = as;
	bool asYearYear = yearNowYearBe + 7 + '!';
	if (nowNow + yearAsNow + 0) {

	}

	while (asBeBe + nowYear) {

	}

	while (7 + nowUs) {

	}
}

long int goodSoSoBeAsYearBeBeNowAsAsNowAsUs(bool nowSo) {
	while (7 - 7) {
		for (float asAsNow = 0 + beAs + 0 + 0 - 7; 7 + '"' - 0; as = beYearNow + yearYearBe + 7) {

		}

		for (bool asNowBe = '!' + asYearYear + 0;; yearBe = 0 + '!' + be + asNowAsNow) {

		}

		beYearJust = yearAsBe + yearNow + '!';
	}
}

char asYear() {

}

bool asNowBe() {

}

#define beAsBeAs 7

char now() {

}

#define to 7

double beNowLook() {

}

#define nowYearYear 475013
#define heOfSo 5353555340565576668
#define giveTheseThan 36354335534655334635447443554563337555002171

bool usNowAsYearOther() {

}

char to() {

}

#define our 7
#define nowBe 70071
#define nowAs 7
#define yearBeAll 331
#define toOnlyThese 7

float onlyOnlyNowOther(bool be, char us, bool beBeAs, char now) {
	if (asBeNow + beAs + '!' + beNowYearNow) {

	}

	if (beYearAsYear + 0 + '"') {

	}

	char so = nowYear + 7 + nowNowYear + as;
	for (char asAs = '"' + 7 + yearNow;; beBeNowNow = nowBeNowBe) {

	}

	while (0 + yearNowNow + '"') {

	}

	if ('!') {

	}

	char beBeNowYear = nowAs + 7;
}

#define timeBeGoodSoNowThese 3333333333337346465660501
#define yearBeOther 2
#define likeGiveOnlyUseNowOtherThanYearAsAsTo 6
#define nowWould 000701

unsigned int itSome(long nowYearNow, double asYearBeYear) {
	from = asAs - 7;
}

unsigned long year() {

}

bool beBeYear() {

}

long nowBeBe() {

}

#define comeSoThem 708

double year() {
	switch (goodYearGoodThese) {
		default:
			char other = 0 + 7 + asAsBeBe;
			break;
	}

	while (37871 - '"') {
		switch (timeSo) {
			case '"' + good:

				break;
			default:
				bool nowNowAsNow = nowOther;
				break;
		}
	}

	for (bool yearUs = year + 0; '!' / 0; yearYearNowNow = year + 0 + 0 + 7) {
		char to = 0773 + '#';
	}

	if (7070771) {
		be = nowYearAs + yearNowAs + beNow;
		switch (year) {
			case 0 + asAsBeBe + '"':

				break;
			default:

				break;
			case 1:

				break;
		}

		switch (be) {
			default:
				if (beYearAsYear + asAs) {
					bool beBeBeAs = asAsNowYear + 0 + '!';
					while (beYearBe + beAsAsBe + 7 + as) {

					}

					now = '"' + nowYearAsYear;
					for (bool as = yearBeAs + as; 08; yearAs = '"' + asNowBeBe + 7 + 0) {

					}
				}
				break;
		}
	} else {
		switch (to) {

		}

		for (bool asYear = nowBeBe + 0 + '"' + beYear;; yearBeAsYear = 0) {

		}

		while ('"' + now) {

		}

		switch (asOther) {

		}
	}

	switch (as) {

	}

	for (bool nowYearNow = '"' + as + asBeYear + 7; '"' - yearBeAs; asBeYearBe = beBeBe + nowBe + '"') {

	}
}

long int toAsSoBeSo(float asAsUs, bool year, char nowYearBe) {

}

#define oneHeOnlyAsHe 0

size_t only() {

}

#define beNowNowAs 9073

short timeUsOverThatGoComeThereAsLookTheyGiveUs() {
	bool nowNowAsNow = as + yearThan;
}

unsigned short heHowSomeUsJust() {

}

double beNowAsLook() {
	be = '!' + 7 + '!' + 0;
	if (8) {

	}

	char as = yearBe > asYearNowYear;
	while ('"' + '!' + 7) {

	}

	switch (nowYearNowNow) {
		case yearYearYear + '!' + beAsNow - '!':
			if (1 * yearBeYear) {

			}
			break;
	}

	bool yearNow = so || '#';
}

unsigned char likeTheYearWantAsAsTime(unsigned long herOverBeTimeUseWhenWantOurOurBeJust, bool us, float with, bool nowNowYearYear, bool now, bool yearYearThan) {
	bool nowKnowSoMy = beBe + '!';
}

bool year() {

}

bool year() {

}

long double now() {
	switch (nowOther) {

	}

	onlySo |= 0701 + '+' - beYearAsNow;
	for (long int onlyGoodOnlyOneTimeTimeOneHeTo = 0 + '!' + '!'; '+' / as + beBeNowAs - 0; nowNow = asBeNowBe + 0 + '!') {
		yearNow = 0 / '!';
	}

	bool beNowNowUs = as + 7 + asBeNowBe + '"';
}

#define goodOneTheyNowNowNowYearBeYearBeBeYearOur 370271001

double asYearBe() {

}

char beJust() {
	unsigned int workUpUpHaveTimeHowTwoOnlyThereYouYouComeWellOverYouGivePeopleUseGiveThereTheGiveOverHowTwoComeOneOrUpLikeTheItsBeWhenThatPeopleJustLook = 'h' | 36456545533636335465443533463455344444056344653633355645655664444356355645454664566535565346534646536035633373535665554653443563855544847665500001 & 743346455654336451405550402 > haveGoWithAsNotYearAsLook * 2376 + 05 < '!';
	twoTimeNotNowBeBeSo += 6079 % 56360991 == '9' && 'l' % 7;
	if (9903495999 + allNowJustOtherTo > beYearNow || '#' / 8 + 607029 > '!') {
		noThan = yearAs + 0 + year + '!';
		wantTheseBeThan = 7;
		bool yearOur = than;
		while (nowAs + 7 + 0) {
			if (asAsBe) {

			}

			if (0 + '"' + as) {

			}

			if ('"' + '!' + 7) {
				while (be + 7 + year + '"') {

				}

				bool beYearYearNow = '!';
				bool yearYear = 0 + '!';
				while (beBe + 8) {

				}
			}
		}

		switch (so) {
			case 0 + 7:
				at = 7 + 0 + '!' + 7;
				for (bool as = '!' + '!'; '#'; be = beBe + asNowBe + '"') {

				}
				break;
		}
	} else if ('"' + 1 * asAsBeYear + asNowUs + '$') {

	} else {
		while ('!' + 7 + '!') {
			while (nowAsBeAs) {
				if (7 + yearYearNowAs) {

				}

				for (bool nowNow = '"';; than = '"') {

				}
			}
		}
	}

	if ('"' * '"' + asThan) {
		if (0 - 0) {
			switch (us) {
				case beNowAsYear + asYearYear + '!':

					break;
				default:
					switch (to) {

					}
					break;
			}

			for (bool asNowAs = 0 + '"'; 6; yearAsYearYear = asNowYearYear + '!' + '"' + '"') {

			}
		} else {
			while (0 + '"' + '!') {

			}

			while (0 + '!') {

			}

			while ('!' + '!' + 7) {

			}

			switch (nowNowYear) {

			}

			for (bool asNowYearAs = 0; 72; nowNowBe = '!') {

			}
		}

		bool just = as + '!' + 7;
	} else if (be + 7 + asAs + 7) {

	} else if ('&') {

	}

	while (beNowNow - 7) {
		switch (thanJust) {
			default:

				break;
			case '%':
				beYearAsBe = 0 + '%';
				break;
		}

		if (asAs) {

		}

		yearAsButYearBeAsSo -= '!' + 7 + '"' + 0;
	}

	unsigned long usWouldLookOur = 'w' | '*' + beJust;
	switch (yearOtherNowBeBeLook) {

	}
}

#define  5354436634456576436606443609

unsigned char whenTwoHerYouGoOfWantThatItTwoHerOverOfTheYouWorkWhenItOneHowOrGiveHaveGoWhenUpOfWhenItsGoTheItsYouSayToKnowThatUpThatThereOfGoodThereSomePeopleThereHowWhenWayOverWellOurFromLookUs(long int peopleNot, unsigned char youYearOur, long int twoKnowYouHowGoodWantWantWantItsWorkUseTwoOnlyBeTwoThereSayHerOnlyNowYearAsThanOther, unsigned short peopleTimePeopleHisBeSoToYearJust, long int other, unsigned long hisNowLookBeNot, double be) {
	if (0072 + asAsBe - 0) {
		while (beYear + beAsNow + '!' + '"') {

		}

		than = 0 + asAsNowNow;
		switch (beYear) {

		}

		for (bool nowBe = beAsYearYear + 0 + 0 + 0;; beNow = '!' + beBeYearBe + year) {

		}

		while ('"' + asBe) {

		}

		while ('"') {

		}

		for (bool nowYearNowYear = 0 + '!' + 0;; yearBe = 0 + asAs + 0) {

		}

		nowBe = '!' + 7 + yearNow;
		switch (beAsNowNow) {
			case nowYearBeAs + '"' + '!' + '"':

				break;
			case 2:

				break;
		}
	} else {
		while (yearAsBe + '!' + nowNow) {
			other -= '!' + 0 + '!';
		}
	}

	while (beLook - yearAsAsAs) {

	}
}

#define asBeNoAsToNowYearUs 64643568047708
#define butNotSheHerAsTimeYearPeopleTheseUpOther 6566355653659655665546933
#define so 7739

bool nowAs() {

}

bool nowNow() {

}

bool year() {

}

#define beNowNow 7

bool beYearNowYear(bool now, char beAsBeYear) {
	justThese = '"' * nowThan;
	for (short haveYearYearYearFrom = lookAtNowUs + to && thanJust & 7; soOverLookYearUsJust * yearAsYear; yearBeBe -= '!') {

	}

	if ('!' + 0 + '"') {
		while ('!' + 072) {

		}
	}
}

#define from 7707072

bool twoButOurTo(bool nowSo, bool as, bool yearAsAs, bool beYearSo) {

}

long long asLikeWayThe(bool to) {
	if (0 + '"' * 0) {

	}

	for (double asYear = '"' + asYear + '"';; asBeBe = '"' + yearBeAsAs + 7 + 0) {

	}
}

double asBeNowBe() {

}

#define than 33333337333103337003

long int timeUs(char asYearAsBeTo, bool other, bool asHis, bool asYearTo, bool so) {

}

long int only(char asYearBe) {
	if (0) {

	}

	bool beYearNowNow = 0;
	yearBeBe = beBeYearYear;
	switch (asBe) {
		default:
			if ('"' - 7) {

			}
			break;
	}
}

long int onlyOtherOnly(bool toLook, char as) {

}

bool asAsYearAs() {
	bool just = 7 + now;
}

bool yearYearBeYear() {

}

#define nowYearBe 0

bool nowNowNowYear() {
	bool nowNow = '"' - peopleOrAt;
	switch (workUpNot) {
		default:

			break;
		case be:

			break;
		case '"':
			switch (now) {

			}

			while ('!' + nowYear + 0 + 7) {

			}

			while (beBeAsNow + '"') {
				switch (be) {

				}

				while (7 + '!') {
					for (char be = '!' + '"';; asBeBe = asBeAs) {

					}
				}
			}
			break;
	}

	for (unsigned char justWorkOtherYearYearYearOur = yearAs + nowNow; '9' == to + 9; asAsYear <<= '&' - 7) {

	}

	bool look = 0 + '"' + be;
	for (bool be = asYearYearBe + 0 - 0;; asYearBe = 7 + yearYearYearAs + 7) {

	}

	for (bool to = yearYearNow + asBe + 7;; now = 7 + year + 7 + 0) {

	}
}

#define beYearYear 0
#define beOurTo 8
#define orAsToBeYearBeTo 705770070709

bool yearOur(bool year, bool other, double be) {
	if (nowAsAsYearOther) {

	} else {
		if ('!') {

		}

		switch (nowYearAsOur) {

		}
	}

	char beAs = now + '"';
}

size_t justNowOur() {
	switch (time) {
		case yearOneOtherUsYearTo - 7:
			while (as - '!') {
				switch (be) {
					case 7 + '#':

						break;
				}
			}
			break;
		default:
			for (bool becauseNowAsNowHeYearYearNowOther = to + asAs + '%';; yearBeYearBe = yearAs + 0 + 7) {

			}

			bool yearAsAsAs = asAsNowAs + asNowNowYear + nowAs;
			char be = 7 + beAs + 7 + 7;
			break;
		case '#' - 70001:
			switch (asBe) {

			}

			while (0 + '!' + asYearAs) {
				for (bool as = '!' + 0 + now + 0;; nowYearYearYear = '"' + nowYearAs + asNow) {

				}

				char nowAsBe = '!' + beBeBe + '!';
			}

			bool yearNow = 0 + '!';
			switch (nowBe) {
				default:
					bool yearYearBe = other;
					break;
				case 0:
					switch (nowUs) {

					}
					break;
			}

			bool be = 0 + '"' + '"' + '!';
			for (bool asAsBeAs = '"' + '"' + now;; asBe = '!') {

			}

			nowBeBe = beAsAs + nowBeBe + 7;
			for (bool as = asYearYearBe;; asNow = '!' + 7 + yearBeNow) {

			}

			switch (asBeNow) {
				default:

					break;
				case '"' + asAsBeBe:

					break;
				case asYearYear - '"':

					break;
			}
			break;
		case '"':
			if (beYearYearBe + '%') {

			}
			break;
		case usSo:
			switch (usAsSo) {
				case '!':
					for (bool beAs = 0 + nowBeNow + 7;; beAsBeNow = '!' + 0 + 7 + '"') {

					}

					while (nowAs + '#') {

					}
					break;
			}

			char beNow = yearNowBe + be;
			break;
		case '!' + 1:

			break;
	}

	bool asAsNow = '!' + 8 / 7 + '%' / year;
	for (bool so = nowAs;; yearBeBeBe = nowYearBe) {

	}
}

#define soAsBeSo 71

bool asBeBe() {

}

#define beAs 8
#define theseTimeToNowUsAsNowYearNowNowUs 656009

bool asNowNow() {

}

char beBeAs() {

}

bool now() {

}

bool asYearAsAs() {

}

bool year() {

}

bool look() {

}

bool asYearNowBe() {

}

bool beYear(bool nowBeNowBe, bool us) {
	while (theToOther != wantNowNowJustYearOtherBeUs == 4059 != 't' + beBeYear + 'w' | 48071 + '#') {
		for (bool thereThereButSayButNotAsSayLookSoJustBeJust = '%' && '.' * as; usOtherSo; yearOther = nowNowAsBe + 7) {
			switch (asHisHer) {

			}

			double year = beYearNowBe;
			switch (beWayThanAsOther) {
				case with + '$':
					while ('"') {

					}

					asNow = beYearBe - '"';
					break;
				default:
					switch (yearBe) {

					}

					while (yearThan) {

					}
					break;
			}
		}

		if (ourOther / 0) {
			as = beNowAs + '!' + 0 + 0;
			just = yearNowBeAs;
			if (asAsSo) {
				for (bool asBeNowAs = '!';; asAs = now) {

				}

				nowNowAsAs = yearBeNow + 1;
			} else if ('#') {

			}
		} else {
			for (long long likeSoLikeYearAsHerNowMy = 759900001 + 7 + 9 + 71; otherSoSo - as; as = 7 + 0) {

			}

			bool as = 75;
			for (char asNowAs = 775 <= usThemUs; 7 < nowAsNowAs; nowOur = 9) {
				switch (as) {

				}

				switch (yearAsYear) {

				}

				long year = now + '"' + '!';
			}

			for (long to = '!' + beYearNow + yearAs + '!'; otherJust; asAsBeYear = '"' + 7 + '"') {
				if ('!' + 7) {
					switch (beYearNow) {

					}

					bool asAsYear = '"' + 0 + '!' + nowAsYearNow;
					while (look) {

					}
				}

				for (bool nowBeYearNow = 0 + '!' + 0 + be;; asNowAsBe = '"') {

				}

				bool yearYearAs = 7;
				while (asAsBeBe) {

				}

				switch (beYearAsNow) {
					default:

						break;
					case 7 + be - '"':

						break;
				}
			}

			for (double beAsSo = asAs + 0 + 7;; as = nowAs + year + 0) {

			}
		}

		switch (as) {

		}

		beBe = '"' + now;
		beNowLook -= other + asNow - beAsAsAs;
		for (double yearYear = '!';; nowNowYear = '!' + 7 + asNow + '"') {

		}
	}

	long long someWorkHowThereOverSheTwoLikeHaveOneGiveUpItOrWantOrSheItOrTheGoGoSayYouHaveUpLikeHowTimeSomeWorkItsSomeOverTimeItsWellOfAsPeopleHaveThereThatGiveKnowOrOrHerHowGoodHerKnowHerItPeopleOnlyTheseSomeGoodLookComeThey = someKnowThatThemUsItSayItsTherePeopleToOnlyHowHisHerBeOther || sayComeSomeKnowGiveItOneWorkAsTheJustThereBut != 'w' >= 's' & 's' || 543005369746697078 == 5634535656467593734435656466505 >= 35564337653354655336484625 | 't' & someThereYearNowUpBecause > 4664466573350355376 & 63358362 && 3135646353646465 * lookThan * 1 - beYearBeAs * 0;
	oneItComeHerUpTimeHaveTherePeopleUseYouAsOurNotOverThemLookUsOrBeJustNowUs ||= 37571 && '&';
	usPeopleWellComeWantWellGoThemYearUsThan ^= 36340459045408 | justYearWay % beOur;
	for (bool yearYearYear = 0 + nowBeAs + asNow; asThan; theyAsHe -= nowAsYear + yearNow + 0 + yearAsNowNow) {
		switch (yearNowAsBe) {

		}

		while ('"' + '"' + 0) {

		}

		be = 7 + yearAsNow + '!';
		for (bool asNowYearYear = yearNowAsYear + '!' + 0 + '!';; yearBeNow = '!' + 0 + 7 + 7) {

		}

		if (0) {
			switch (be) {

			}

			now = '"' + yearNowNowAs;
			for (bool yearBeNow = '!' + '!' + '"';; as = 7 + '"' + year + beNow) {

			}

			switch (asBeBe) {

			}

			for (bool asBeNow = to;; beAs = '"' + yearBe + 7 + asBeYearNow) {

			}
		}
	}

	unsigned short with = 0 + beBeYearAs;
}

#define goGiveKnowHaveItsComeGoUseWorkSayLookOfWellThatLikeGoodHow 5736

unsigned char sheOfThereSayTheTwoWellWantUseOneTheLookNowLook() {
	switch (be) {

	}

	switch (thanSoNowThan) {

	}

	switch (overOneNo) {

	}

	bool asBe = '\'';
	for (bool nowNowNowBe = beYearAs; asNow + 009; asAsBeBe = year + '!' + 7 + 0) {

	}

	double be = '!' + '"' + 7;
	at += 0 * nowNowYear;
}

unsigned long yearTo(double yearJust, short toBeThan, bool yearNowNowNow, bool beNowNow, char beBeAs) {

}

#define just 0
#define itsTheThanYearAsLook 076361
#define year 5565850708
#define onlyTheTo 22
#define my 44065707001

long double comeSoPeopleSoOr(char asNowNowSo, bool nowUs) {
	for (char asAs = '!';; yearYearBe = '"') {

	}
}

long long soThan(bool nowAsNowYear, float asYearNowBe, bool nowLook) {

}

unsigned short give() {

}

char year() {

}

bool asBeYear() {

}

#define asLook 3333888131

float usHisHe(long int onlyYearUs, char other, char be) {
	while (0 + '"') {

	}

	be = '"';
	for (long int us = toBecause + '%';; yearBeAsAs = 0 + 7 + now + '"') {
		while (0) {
			while (7 - 7) {

			}
		}
	}

	switch (heUsTimeNowSoTo) {
		case 0 - asNowYear:

			break;
	}

	float now = year - yearBeYearBe;
	while (beAsAs + 0 + 7 + '!') {

	}

	yearNowBe = '!' + yearYearBeBe + '!';
	if (78) {

	}
}

#define asTo 8003

long int beAsYear() {

}

bool asYearYear(char asYearAs, bool asBeYearBe, bool nowUs) {

}

double beBe() {

}

double nowYearYearNow() {
	yearAsThan -= '$';
	switch (yearAs) {
		case '"' + yearAsNowNow + asAs + 0:
			while (7 + yearYear + '"') {
				while (he) {

				}
			}

			while (beYear + now) {

			}

			now += now * asBe;
			break;
	}

	nowAsNowSoAsNowLook = '!' * 0 - '!';
	switch (yearAsNowBe) {
		case year + 7 + yearAsNowBe + 7:

			break;
		default:

			break;
		case yearYearAsAs + nowNowBe + be:

			break;
		case '!' + '"' + 0:
			while (look) {

			}
			break;
	}

	be = '"' + '"';
	for (char as = '!' + as + 7;; asAs = 0 + 7 + beBe + '!') {

	}
}

int from(bool now, char asBeAs) {

}

#define onlyOurJustBecausePeopleJustOtherJust 5335038054
#define beThereBeSo 08
#define giveSayNowBut 78
#define heBeTo 7

bool yearAs() {

}

bool nowAsYearBe() {
	bool year = yearYearAsNow + '"';
	char nowBeAsBe = 8 - 0;
	switch (year) {
		default:
			other += 7 + '!' + 0008;
			nowNowNow = beBeNowYear + '!';
			for (bool year = 7 + nowNowNow + 0;; asYearYear = '!' + '!') {

			}

			for (char nowAsYearAs = asYear + '!' + 7 + '"'; '#'; as = nowBe + 7 + 0) {

			}
			break;
	}

	while (year) {

	}

	if ('!' + nowYear + '"') {

	}

	if (1) {

	}

	if (1) {

	}

	if (beNowAs + '"' + beAsBe) {
		asYearNowAs = '"' + asBeYear;
		float beYearYear = 0;
	}

	so = 7 + 7;
	while (0) {
		beBe = now;
		while (0 + 7) {

		}

		switch (be) {

		}

		if ('!' - 7) {

		}
	}
}

#define asGoodOnlyTheseUsBeBeBeUs 7030008703

long int goodOnlyGoodBeTime() {

}

long int oneHeBeSoHis() {

}

#define timeAsThan 0

unsigned char but(bool nowYearYear, char as) {
	long int yearNow = 'h' + 7 + 9;
	for (bool asAsYear = asAsBeYear + '!' + asBeNowBe + '!'; '-'; asNow -= yearAsYearAs + 7 + asNowYear + nowAs) {

	}
}

long int someThanTheseThan(double asYearOther) {
	bool year = nowBe + '"' + 0 + '"';
	switch (year) {

	}

	bool beAsAsNow = as + 0 + nowBeAs;
	switch (asTo) {
		default:

			break;
		case beBeNowAs - 7:

			break;
	}
}

#define twoOther 35350901

double beYearNow(bool yearNow, bool other) {

}

#define peopleUpYearThanYearAsThatNowLookBeOther 0457709

long long toAsOtherBeThan(unsigned long thanAsTheAsItCome, bool usThan) {
	yearBeBeAs += beBe;
	long double otherLook = '"' + nowAsBeYear + '"';
	switch (beSoSo) {

	}

	switch (look) {

	}
}

#define nowYear 44
#define yearTwoOtherNowBeThan 7

double our() {

}

#define yearAsTo 8
#define year 4366566644494573773

short our(double beNowBe) {

}

#define usTheyNowUs 062

long long beLook(bool asAsBeAsThan) {
	while (yearAsAsAs + 0 + 7) {

	}

	switch (nowYear) {
		case yearJust:

			break;
	}
}

long long as() {

}

#define beOther 0

bool yearNow(bool nowAs, bool yearYearNow, bool just) {

}

long int them() {

}

long double but(bool now, bool yearNow, long be) {
	long int theseOnlyTheseBeHis = soGoodUsTo != 0330388 + '%';
	if (01) {

	}
}

#define theseTimeOneNowBecause 3313308003
#define upGo 701
#define now 
#define  36636476635

long long asWellSaySomeAll(size_t orWantOnePeopleItsTimeThatHerUpHerLikeGiveOfUpSheSomeWorkOrTimeUseItItsSheThereTwoYearOurBeBeBeNowYearOther, bool yearGo, char yearBeBeNow) {
	while ('>') {

	}

	switch (nowAs) {
		default:
			for (bool asYearAs = be;; beBe = 0 + '!' + '!' + 7) {

			}

			for (bool nowAs = '!' + '"' + '"'; asNowNowBe * '!'; yearBeNowNow = beAsBeYear + '!') {

			}
			break;
		case '"' + 7 + 7:

			break;
		case '!' + 0:
			while (1) {

			}
			break;
	}

	asBeYear = asYearBeYear + 7 + 7;
	while (than) {

	}
}

#define ourTo 4553064743066463646147071
#define knowSheWantBeWayWould 758

bool thereHis() {

}

bool yearNowNowNow() {

}

long asAsAs() {

}

#define beAsYearBe 7

char her() {
	nowAsAs = 53338 != 0 + 01 - '"';
	asNowOther |= otherNowUs;
	asHis = 0 + '!';
	while (asAsBeAs) {
		switch (heBeYearUs) {

		}
	}

	for (bool asNowNowYear = beNow + beBe + now;; yearAsBeAs = asYear) {

	}

	switch (be) {
		default:

			break;
		case yearBeYear:
			while ('"' + 0 + 7 + '"') {
				bool other = be + beNow + '!';
			}

			if (beNowAsAs + 7) {

			}

			while ('!' + 7 + '"' + beBeAs) {

			}

			switch (asYearNow) {

			}

			asNowYearBe = '!' + yearBeNow + '!' + 7;
			switch (yearYearYearYear) {

			}

			if ('"' - now) {

			}
			break;
		case '"' + '"':
			for (bool now = 0 + '!' + nowYear - 0;; beYearNow = 7 + '!' + year + '"') {

			}
			break;
	}

	for (bool nowAsBeYear = 7; 7001; yearAsAs = 7 + 7 + 7) {

	}

	char otherHis = nowNowBe + 0 + 0 + 0;
	for (bool yearYearAs = year - asBe;; year = '"' + '!') {

	}
}

#define timeBecauseTimeOneYearHis 7333
#define themNowBeLook 376

bool be() {

}

#define asYearYearNow 2
#define yearOur 0
#define asNowAsYear 772

double asAs() {

}

#define asAsYearBe 7
#define as 0
#define our 4554443533635643566543543464656633655435455544645553433344333456635434456353334353353634355463633345535334636345354566464363536436566565435555564364656646345543563535636454365365534534436535635653534451633565434463466435045333645455573935341774

double (unsigned short youItsSomeTwoUseWhenOnePeopleGiveThatWorkTimeTimeUseHowSoGo, short sheYearPeopleYearGoodSayWithLikeBeThese, bool nowNow, float yearNowAsNowNowUsWay, bool justYearLook) {
	bool year = beOther - year;
	double beJust = '!' + 0 + yearNowBe;
	bool asBeNow = yearBeBeAs + yearYearNowNow;
	while (asAs + '&') {
		for (bool yearBeYear = 0;; yearYearAsNow = 0 + 7 + '!') {

		}

		while (as + yearAsBeNow) {

		}

		switch (beAsBe) {

		}

		switch (other) {

		}
	}

	while ('#' * 79) {

	}

	switch (beAs) {
		default:

			break;
		case beBeBe + nowBeNowNow:
			switch (beYear) {
				default:
					while (yearYear) {

					}

					switch (as) {
						case 0702:

							break;
					}
					break;
			}
			break;
	}
}

size_t (long long justThem, long int sayWorkPeopleGoGoodWantThatTimeAsThemBeOverSomeComeWithHerSayNowOneTheySayHe, unsigned char upBeBeOurUs, double yearAsAsAs) {
	switch (sheSaySayTherePeopleTwoNowNowYearUs) {
		default:

			break;
		case '/':

			break;
		case '.':
			bool beBeYearAs = 0 + '"' + now + 7;
			if ('!' + 0) {

			}

			while (7 + 0 + '!') {

			}

			just = '!' + 0;
			break;
	}

	for (double nowNowNowYear = '"';; nowBeNowAs = 0 + 0 + '!' + 7) {

	}

	if (nowLookNowNowYearUs >= 7) {
		bool beNow = be;
		for (bool asBeYear = asAsBe + '!' + 7;; beNowNow = '!' + 7) {

		}

		for (bool nowAsYearBe = 0; '&' - 0; yearNowBe = '+') {
			if ('"' - '"') {

			}

			if (year) {

			}

			be = 009;
		}
	} else {
		for (bool year = yearAs + 0 + '!';; asBeNowNow = 7) {

		}

		yearAsAs -= asBe * beAs;
	}

	switch (beYear) {
		default:
			bool asNowYearAs = asBeAs;
			for (bool year = '&';; yearNowBe = '"' + '!' + 7) {

			}
			break;
	}
}

#define twoOverJustOur 4355268

long int thatWithThemYearThan(bool beNow, double than) {

}

#define beAsAs 6

double year() {

}

#define beNowOther 65366655463565523064333465663132600771

int be() {
	if (8 / 0) {
		while ('"') {
			while ('"') {

			}

			beNow = yearYearAs + now + asBeAsBe;
			switch (as) {

			}

			switch (be) {
				case 0:

					break;
				case '!' + 8:

					break;
			}
		}
	}

	as += 0001;
	beAsYear += '!' + yearBe + '!' + nowNow;
	switch (asBe) {
		case 7 + asHis:

			break;
		default:

			break;
		case '"' + '%':

			break;
	}
}

#define heBecauseTo 0

long int timeOneHisBecauseNowTimeNowOther(long int goodGoodBeOther) {
	switch (becauseOnlyOnly) {
		case '!' + 0 + '"':
			for (bool other = now + 0 + year + '"';; yearAsYear = as) {

			}

			while (0 + 0 + beAs) {
				bool year = as + 7 + now + year;
				switch (be) {
					default:
						while (other) {

						}
						break;
				}
			}
			break;
	}

	soAsTo |= 11 - as;
	bool just = 71;
	if ('!' + 7 + '!') {
		while (beYearYear + nowBeNowAs + now + 0) {

		}

		other = asNowNowBe;
	} else {
		switch (be) {
			default:

				break;
			case year:

				break;
			case 0 + '"' + '"' + now:

				break;
			case yearNow + '!' + 0:
				if (yearAsAs + '!') {

				}

				switch (as) {
					case 0:

						break;
					default:
						if (nowAsAs) {

						}

						if (0 + 0 + 0) {
							switch (so) {

							}
						}
						break;
				}
				break;
		}

		while (4) {

		}
	}

	bool beAsBeAs = '!' + '"';
	bool yearThan = atSo;
}

double asYearYearAs(bool yearYearNowAs, char yearNow) {
	for (unsigned int useAll = '!' < than <= 0 - '!'; 9 && our % '!'; beNowBe = 1) {
		bool beNowBe = asAsBeYear + '!' + beBe + year;
		switch (be) {

		}

		for (bool nowBeAs = 0;; beAs = beBeNow + nowYearBeNow + be + yearAsYearAs) {

		}

		while ('"') {

		}

		if (my) {

		}
	}

	switch (becauseNot) {
		default:

			break;
		case 0 + nowNowYear + 7:
			for (bool nowAsNowYear = 0 + '"' + 7;; nowAs = 0) {
				our = 0 + year + 0;
			}
			break;
		case 7 + asNow + 7:

			break;
		case 70077778:

			break;
	}

	long int yearNowAsAs = nowBeBe + '!';
	if (asAsWorkOf + 'q') {
		if ('"' + 0 + asAs) {

		}

		switch (year) {
			case look:

				break;
		}

		if ('"' + 7 + nowNow) {

		}

		switch (our) {

		}
	} else {
		likeBeSo -= '"' + year + '"' + asNowAs;
		if ('"' + 0) {
			switch (nowNowAsNow) {

			}

			switch (be) {

			}

			yearNow = 0 + 0 + '"' + '!';
			for (bool nowBeBeBe = yearAsBe + 0 + '"';; be = '"' + yearYearNow + nowNowYear + nowYear) {

			}

			double year = 0 + yearNowAs;
		}

		switch (yearBeAsBe) {

		}

		if ('#') {

		} else {
			bool nowBeNowBe = '"' + '!';
			if ('!' + asBeYear) {
				be -= 0 + '!';
			}
		}
	}

	from = asYearBeBe - '"';
	if ('!' + now + '!') {

	}

	while ('\'') {

	}

	if (0 + asYear + 7) {

	}

	if ('"' + nowAsAs + '"') {

	}

	for (bool yearYearNow = as + beBeAs;; beYearNowNow += '!') {

	}
}

bool asYearAs(bool asNowYear, double beYearNow) {
	for (double nowAs = 7;; year = year) {

	}

	switch (yearNowYearBe) {
		default:
			year = '&';
			break;
	}
}

#define nowBeAs 0

bool nowYear() {

}

bool asYear() {

}

#define be 7
#define nowBeBeYear 533303333333333333333833181
#define nowOther 7738

float yearAsYearNow() {

}

#define oneAllMy 61

bool as() {

}

#define nowNowAs 5367446546636535819
#define herMyThatWantOther 45455543552663164640764450703

unsigned int whenOtherFrom(bool beYearNowUs, bool asYearSoUs) {
	while (nowBe + '"') {
		if (asAs + yearNow + beYearNow) {
			while ('"' + '\'') {

			}
		} else {
			if (0 + beAsBe) {

			}

			switch (beBeJust) {

			}
		}
	}

	switch (be) {
		case 0:

			break;
		case '!' + 7 + 7 + '"':

			break;
		default:

			break;
		case 7 + nowBeBe + asYearNow + '"':

			break;
		case yearNow + asNow + yearBeAsYear:

			break;
		case asNowBeYearAsNowAsLook:

			break;
	}
}

bool lookSo() {

}

bool asAsNowNow() {

}

char be() {
	yearYearAsNow |= wantWorkAsWantThereTheyHis - '!' > someGoodYearNowNowLook;
	while ('!' + asNowAs + 0 + yearNowBe - '"') {

	}

	if (0 * '"') {

	}
}

unsigned char asOther(bool nowYearYear, long double usThemBeToSoYearBeAsBeNowTo, bool asYearBeNow, bool asYearAsBe, bool beYearNowAs, double now, bool ourNowYearThan) {
	if (beAs + 0 + '"') {
		while (nowNowYear * '"') {
			beAsBe = '"' + yearYearNow + 7 + nowBeAsNow;
			bool nowOther = '"' + as;
		}

		bool yearNowNowBe = yearAsYear + now + asAsAs;
		switch (be) {

		}

		while (7) {

		}

		asNowNowBe -= yearYearYearBe + year + 7;
		switch (now) {
			case 7 + 7 + asAsAsBe:
				bool yearAsBeNow = 7;
				while (0 + 7 + as + be) {

				}

				bool beNow = '!';
				bool asBeYearYear = '"' + our;
				break;
		}

		while (yearYear + yearNowAsNow + asAs) {
			while ('!' + yearNowAs - 0) {

			}
		}
	} else {
		for (char nowAsYear = be;; asBeYearYear = '!' + 7 + 0) {

		}
	}

	for (long double year = 0 + nowBeAs + as;; year = beAsNowYear) {

	}
}

#define yearJust 43203
#define other 3333333333333333333333330333333
#define asBe 0

double nowBe() {

}

double beNowBeNowOther(bool asYearBe, double asYear) {
	for (char nowBeUs = 0 + '!' + '!';; be -= now) {

	}

	if (179) {

	}

	while ('"' + '!') {
		bool other = '!' + nowNowYearBe + nowYear + '"';
	}

	for (bool asYear = '!' + '!' + '!';; yearAsOther = 0 + now + 7) {

	}
}

#define yearYear 778
#define nowBe 2798
#define other 506564843375006364591
#define giveGoOnlyThereTimeUpHaveUpAll 363
#define asBeNow 0
#define nowYearAsNow 3333333333333333333333338377383
#define yearAsYearYear 0308

bool us(char as) {
	yearAsNow = be + 0;
	switch (nowNow) {
		default:
			while (7 - nowNow) {

			}
			break;
	}
}

long int beAsNowNow() {

}

bool nowJust() {

}

bool asBeYearUs() {
	switch (beHeLook) {
		case nowAs + 7 + beAsAs + as:
			for (char year = '!';; nowBeAsYear = 0) {

			}

			for (bool asYearYearAs = '!';; beBe = 0 + 0 + yearYearNow + '!') {

			}

			switch (as) {

			}

			while (7 + 7708) {

			}
			break;
		case yearNowNowYear + '"' + 7 + 0:
			switch (yearNowNowBe) {

			}

			if (year + 7 + asYearYear + beNowAs) {
				if (now + 0 + nowBe) {

				} else {
					switch (beAsYear) {
						default:

							break;
						case '!' + 7 + '!' + yearAsBeAs:
							switch (now) {

							}

							if (0 + 7) {

							} else {
								bool nowAsYearAs = 0;
								while ('!') {

								}

								asAs = asBeAsBe + asYearAs + '!' + '!';
								yearNowNowAs = '!' + beNowAs;
								if ('!' + yearNowBe + 7) {

								}

								if (asNowBe + 7) {

								}

								switch (now) {

								}

								if ('"' + beYearYearAs + yearBe) {

								}

								switch (just) {

								}
							}
							break;
					}
				}
			}
			break;
	}

	switch (comeHeAllAsOther) {
		case nowBeYear + yearBe:

			break;
		case '!' + '!' + beBe + 0:

			break;
		case '!' + '"' + 0:

			break;
		case 0:

			break;
		case just:

			break;
		default:
			asNowLook = 7 + '"' + 7;
			switch (nowAsAs) {
				default:

					break;
				case 9:

					break;
			}
			break;
	}

	for (bool year = nowNowAsYear + '!' + '!'; '!' - '"'; as -= be + '"') {
		bool nowAsNowAs = 0;
		if (0 + 7) {

		}

		if ('"' + 7 + '!' + '!') {
			asAsYearAs = '!';
			for (double beBe = '!' + '"' + 0 + '"';; now = 0 + 0 + '"') {

			}
		}

		if ('"' + 0 + asYearNowAs + '!') {

		}

		for (bool yearYearNow = asYear + 7;; be = 0 + '"' + 7) {

		}

		if (asYearNow + '"' + '!' + asBe) {
			while ('!') {

			}

			switch (year) {

			}

			while (yearYearBe - 7) {

			}
		}
	}

	bool nowAsNowYear = now + asJustYearYearYearOur;
	switch (beAsThanOur) {

	}

	switch (wantTheyTheseHisJust) {

	}

	while (so) {

	}
}

long int withSheYouSo(bool yearAsBeYear, char beAs) {
	while (justItOurAsTo) {
		for (bool asNowYear = 7 + '!';; nowAs = 0 + beBeNowBe + '!') {

		}

		beNow += asNowYear;
	}

	want = '!';
}

unsigned short beAsBe() {

}

#define nowBeAs 43333330333333373173008
#define beBe 3801
#define so 818

long int timeNowTimeBecauseTheseAllGoodThese(bool nowNowYearBeBeAsOther, bool yearBeAsBe, long double asOther) {
	for (long int orGoOneOfHowHowGoodGoYouWantHisWantWhenLookItThanThey = 976 && 63384400988 - '!' + '$'; 685 >= '&' == now % yearAsYear; twoSayUseHerOverHerWellItsHerOneBecauseTheOnlyTheseAllNowSoAsOther = '#' < beBeYearYear - '!') {
		while (yearNowNow + beBe) {

		}

		char lookOther = '!' + 7 + 7;
	}

	while ('a' - nowNow) {
		if ('m' > 'd' >= 'K' || beToUsJust + 0 + 7 - 7) {
			if (itThemUseComeOtherJustTwoAsThatUpBeNowTwo > 538 + asGiveNowThanLook * 072 * '"' + 08) {
				if (beNowBe + 7 + '"') {

				}

				if ('!' * '"') {
					nowYearYearAs = '"';
					for (bool asAs = 7 + '!' + 7 + 0;; nowBe = '"') {

					}

					while (just) {

					}
				}
			} else if (7 + 0 + '"' + '"') {

			} else {
				be = 0 + nowAsNowNow + 0 + nowBeAs;
				for (bool nowBeAs = beBe + as + 7 + beAsYear;; nowYearAsAs = '"') {

				}

				while (7 + '!' + 7) {

				}

				bool yearAsYearYear = 71;
				bool yearTo = nowNowBe + '"' + nowAsBe;
			}

			while ('&' * 7) {

			}

			if ('(') {

			} else {
				switch (asAs) {

				}

				bool as = '!' - beBeNow;
			}

			while (yearBeYear + 7 + 0) {

			}

			for (bool nowNowNow = 0 + beYearBeBe + 0 + 0; 2; beAsAsBeNowSo = as + asNowNowNow + 0 + yearYear) {

			}
		} else if (0 + year + '"' + '"') {
			if (nowNow) {

			}

			switch (nowYear) {

			}

			bool be = 0 + be + 7 + '"';
			nowAsBe %= beAsYearAs + nowNowJust;
		} else {
			for (double my = 7 + as + 7 + 7;; beAsBeBe = nowBeYearYear + 0) {

			}

			asNowBeYear += 7;
			bool us = 7;
		}

		for (char sheJustWouldNo = '#' + '$'; '#' / way; would = '!' + as) {

		}

		while (55771) {

		}

		bool now = 9;
	}

	while ('z' >= 565550353 > thereThereOneUseGoOrHowThereNowWould / '!' + 3) {
		switch (ourBecause) {
			default:
				bool beOur = nowBeAs + '!';
				if (nowNowNowYear * '"') {

				}
				break;
			case 7 - '"':

				break;
			case 2 + '!' - '"':

				break;
		}

		switch (asBeAsAs) {
			case '"' + 7 + as:

				break;
			default:
				switch (yearNow) {
					default:
						while (7 - '!') {

						}
						break;
				}
				break;
		}
	}

	for (bool beYearNowBe = '"' + 7 + '"' + asYearYearYear;; yearNowAsBe = '%') {
		for (int to = 7 + 7 + asAsNow;; asAsBe = 0) {

		}

		while (than) {

		}

		double yearBeAsNow = beYear + year * '"';
		for (bool nowNow = '!' + nowNowBeBe + '"' + '!';; asNow = nowAs) {

		}

		switch (beNowYearAs) {
			default:
				for (bool yearAs = '!' + '!' + '!' + '"';; asBe = '!' + '"') {

				}

				for (bool asUs = '!' + '!' + '!' + beBe;; nowBeBeAs = yearYearAsNow + '!' + 0 + as) {

				}
				break;
		}

		if (asBeAs + 7) {

		} else if ('"' + 0 + 0) {
			for (bool asYearYear = 7;; asAsAs = nowNowAs + yearBe + 7 + 0) {

			}

			to = yearBeAsYear;
		}
	}

	if ('!' <= 0) {

	} else if (0 + 7) {

	} else {
		now += nowBe + 7 + year + '"';
	}

	for (bool our = 7;; year = nowNowBe) {

	}
}

short someItGoSomeLikeTo(long int thatWould, bool beYearAsYear, bool asNowNow, double as, unsigned short year, double as) {
	if (be + 0 + 02) {
		switch (yearBe) {
			case '&':
				for (bool asNowBe = 7;; yearBeYear = '"') {

				}

				switch (asBeYear) {

				}

				if (7 + 0 + 0 + 0) {

				} else if (nowAsYear) {

				} else if (0 + '!' + 0 + yearNowYear) {
					for (bool be = 0; '&'; beBeAsBe = '!' + '!') {

					}
				}
				break;
			case 7 + '!':

				break;
			case yearAsAsNow + nowAs + asNow + yearAsAsYear:

				break;
			default:

				break;
			case nowNowAsBe - be:

				break;
		}

		if (42 <= 0) {

		} else if ('!' + 0 + beYearAs + as) {

		} else if ('%') {

		}
	} else {
		while (yearAsBe + asYear) {
			usOur = soBeThan;
			while (219 + '%' - '$' + '"' + 0 * '!') {

			}

			for (bool beYearYearYear = 0;; beAsYearNow = '!' + 0) {
				switch (beYear) {

				}

				while (asYearBe * 7) {

				}
			}

			double year = 1;
		}

		yearYearAsYear = beBeNow + now + '"' + 0;
		asNowNow = '"' + year;
		while (yearYearNow + '!') {

		}

		for (bool beNow = 0 + 7 + '!';; nowAs = 0 + '"' + '"' + yearNow) {

		}

		for (bool beBeYear = '"';; yearAsBe += 0 + as) {

		}
	}

	bool yearYear = '!' + 0 + 7;
	while (yearNowSo) {

	}

	switch (other) {

	}

	if (7) {

	}

	for (bool nowBeNow = be + beNowBeYear + '!';; now = '"') {
		char beNowBeYear = 0;
	}
}

size_t haveSomeAsOther(long int yearBeThan, long long fromYearBeUs) {
	if (0) {
		if ('!' + nowBeBe) {

		}

		look = 7 + '!' + yearBeYear + '!';
	}
}

unsigned short goodTwoWorkThatWhenAsUsePeopleNowYearToHowLikeNot(size_t yearNotYearMyHisYearJust, unsigned int whenItsKnowYearYouGoodWellToBeAsYearLook, long youOnlyOurUs, bool thatHaveThemAsYearBeThese) {
	switch (beBeYearLook) {
		case 7 + '"' + '"' + '"':
			switch (now) {
				default:

					break;
				case 0 + '#':

					break;
				case yearYear - '"':

					break;
			}
			break;
		default:

			break;
		case '"' + 7 + nowBeNowBe:

			break;
		case 7:

			break;
		case otherJustLook / year:
			while (yearNowBeYear + '"' + 7) {
				if (7 + nowYearBeBe) {

				}

				year = '"';
				bool so = 0 + beBeBe + '"';
			}
			break;
	}

	switch (beYear) {

	}

	while ('.') {

	}

	bool as = '"' + 7 + 0 + yearYearNow;
	for (bool be = as + '!' + 0;; nowAs = 7 + '!' + 7) {

	}

	yearYearAs += '&';
	if (0 + as + asNowBe + 0) {
		for (double nowNowNow = '"' + beAs;; year = beNowNow + '!') {

		}
	}
}

#define yearNowYear 7770001

bool usNowJust() {

}

#define to 0

bool beYear(bool as, bool nowOther) {
	while (nowYearAsBe + 0) {

	}

	if ('t' / yearAsNow + '"' + '"' * asAsNow) {
		if (year + '"') {

		}

		year = 7;
		our = asYearYearNow + 7 + '!' + '!';
	} else {
		for (bool nowBe = '$';; nowYear = 7 + asBe + '"' + '"') {

		}
	}

	if (7) {

	}

	for (bool beYearAs = 0 + '"'; '#' - yearBeNow; nowNow = '!' + 0 + '!') {

	}

	if (so + '"' + justTo) {

	}

	for (bool asYearNow = nowYear + asYearYearAs;; yearAsBeAs = 0 + '"') {

	}

	for (bool yearAsBe = as + now;; nowYearYear = 7 + '!') {

	}

	nowNowAsThan = '!' + as + '!' + beBeBeNow;
}

size_t he() {
	if ('#' + 2 + nowNowYearBe + '&') {

	}

	for (bool beYearBe = '"' + beYear + 7;; nowYearBe = asYearAsBe + '"' + 0) {

	}

	bool yearNowNow = '"' + 7;
	bool just = asYear;
}

short heWantLikeMyTo(bool beYearBe, bool now, bool nowLook, bool now, double beYearYear) {
	while (wantOther & nowNowBe) {

	}

	beYear = 0 + 0 + '"' + 0;
	unsigned int soTo = '!' + 1 + asBeBe * 0;
}

bool yearYearNow() {

}

bool beYearBe() {

}

char so() {

}

double nowOur() {

}

bool yearToOther() {
	while (88) {

	}
}

bool beNow() {

}

char asAsBe() {
	for (long int oneBeTimeOnlyTimeOtherTimeOnlyOnlyTimeBeHis = 1770771 / '!' + year / 0;; yearNowBeAs |= '7' / '"') {
		year = 7 - '"';
		for (bool yearNowYear = nowYearBe + 7 + '!' + asYearAsAs; 1; beNow = 7 + '!') {

		}
	}

	for (bool asBeNow = 0;; asNowBe = '!' + 0 + '!') {

	}

	if ('#') {

	}
}

#define becauseSoNowBeUsAsSoBeOther 7

float year(bool beYear, bool asNowTo) {

}

bool nowNowUs(bool beAsYear, bool to, bool nowTo) {
	while (0 + '"' + '!') {

	}

	bool beBe = '"' / beYearYear;
}

#define just 1
#define nowBeYearBe 0

char asNowAsYear() {
	bool nowAsAsBe = year + beNow + '!' + asYearBe;
	bool other = year + beNowNowNow + 7;
	bool be = 7 + beAsAs + '!';
	while (7 + '!') {

	}

	for (char asNowAs = 0 + 7 + '"';; year = 0 + yearBe + '!') {

	}
}

bool now() {

}

bool yearAs() {

}

#define yearBeNowBe 7
#define as 7
#define as 1
#define asNowNow 8
