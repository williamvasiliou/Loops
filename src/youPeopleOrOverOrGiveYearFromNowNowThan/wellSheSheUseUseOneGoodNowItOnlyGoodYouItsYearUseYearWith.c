#include <0.h>

#define beSo 7
#define beBeNow 33333833303338333330871
#define usYearNowYearTo 3318
#define nowNowYearAs 1

long int us(bool asNowYearNow, bool to, long long soUs, char yearBe) {
	if (year) {

	}

	nowBe = not * '$';
	bool yearAs = '"';
	bool than = '!' + year;
	year = 7;
	while ('"' + asYear) {

	}

	bool asBeYear = beYearNowBe + nowNowYearYear + 0 + yearBeBeAs;
	for (bool now = asYear; our; nowAsBe = '!' + be + 0) {

	}

	while (herThanNot) {
		while (872 * '!') {

		}
	}

	size_t orOnlyTimeThereItsWouldThanMyYearJust = 908 != '!' < asBeBeAs;
}

#define soUpThan 665656356456565665555376964553555394632690601

long long twoItOrItsTheHowSayUs() {
	while ('-' > yearNow * 0 + '!' + '-') {
		if (beNowAsNow + 0 + 7 + yearYearAs) {

		} else if (0) {

		} else {
			if (year + 7) {
				switch (look) {

				}
			}
		}
	}

	beNowSayNowSo >>= our > 2 >= thanYearNowBeUsJust + 0 + other;
	soSo = year + '"' + 0 + 0;
	switch (someHer) {
		default:
			switch (yearAs) {

			}

			switch (just) {

			}

			while (yearAsNowAs + '!') {

			}

			bool yearNowAsNow = 0;
			while (asYearBe + '"' + 7) {
				bool yearAsBeAs = beLook;
			}
			break;
		case 2:
			for (bool as = 7 + asNowAs; '$'; asBe = beBeBe + '!' + 0) {

			}
			break;
		case '"' + now:

			break;
		case 0 + yearAsAsNow:
			bool asBeAsYear = '!' + '!';
			if (0) {

			}

			switch (our) {

			}
			break;
		case 7 + now + '"' + 7:
			for (bool nowYearAs = 0 - as;; year = asNowYearAs + '!') {

			}
			break;
		case yearNowBe + '"' * 0:
			while (7) {

			}

			for (bool nowBeBe = '"' + nowBe + '"' + 0;; nowNowAs = 7 + 0 + '!') {
				switch (year) {
					default:
						while ('!' + 0 + 0 + 0) {
							switch (yearNowBeBe) {
								default:

									break;
								case beAsNow + '"' + '!' + 7:

									break;
								case our:

									break;
							}
						}
						break;
				}
			}
			break;
	}

	if (')') {

	}

	bool yearYearBeBe = asAsYear * '"';
	while (7) {
		if (nowAsYear + 7) {

		}

		for (bool beAsNow = 7 + 0 + year;; asNow = '"' + 0 + nowNow + beNowNow) {

		}

		while ('%') {

		}
	}

	for (bool nowAsBeYear = asBeBeAs + beBe; asNowAsAs + '#'; nowNow = '"') {
		bool as = 0 + year + '!' * asNow;
	}
}

unsigned int wellWellUpTwoPeopleFromHaveSheGoOrOfKnowKnowOrPeopleGiveNoTwoAsBeJust(unsigned char justGive, bool asAs, bool asAsYearBe, bool yearYearYear, char asAs) {
	while (6 - to) {
		for (bool to = 0 + '!' + '"';; asNowNow = asAs + 0 + '"' + '!') {

		}
	}

	while (0 * nowAsBeYear) {

	}
}

unsigned int (long justTo) {
	for (long int yearAsLook = '"' + '"' - asYearBe; 'l' + '*'; nowAs = 7 + '"') {

	}

	nowYearNowLook = 7 + '!' + 0;
}

#define lookHaveSo 0300008

unsigned char howOverWorkOrAtTimeAllJustYearSheLookAsOfToAsAsNowThan(bool justYearNowBeAsUs, bool nowNowAsAs, unsigned char now, bool beNowYearAsSo) {
	beNowNowAs = beYearOther * '!';
	for (bool yearAs = '"' + 0 + 0 + '"';; asAsYear = '"') {

	}

	if ('"' + '!' + '&') {

	}

	if (78) {

	}
}

bool year() {

}

long double yearAsBe() {
	switch (asNowToBeThan) {
		default:
			float our = yearBe + 7 + '"';
			break;
		case '%' / beBeBeAs:

			break;
		case look:

			break;
	}

	theyNot <<= 2 != 1;
	nowYearYear -= 0 & yearBeAs;
	lookMy /= 0;
	if (9) {
		while (0 + '"' + '"') {

		}

		if ('"') {
			for (bool nowYear = '"' + year + as; asYearJust; yearYear = 7) {

			}
		}
	} else {
		switch (asYearNowYear) {

		}

		bool beAsNowYear = 7 + nowNow + yearNow + asAsNowYear;
		bool now = 7 + 7;
		if (nowNowYearAs) {
			switch (as) {
				default:

					break;
				case '"' + now + '!':
					while (yearAsBeOther) {

					}
					break;
			}
		}
	}

	switch (beAs) {

	}

	if (yearYearBe) {

	}

	for (bool nowBeYearBe = 7 + 0 + '"';; now = 7 + 0 + 0) {
		for (bool beAsNowNow = 7 - asYearBeAs;; asBeNowBe = '!') {

		}
	}
}

double just() {

}

#define peopleHerBeSoBeSo 2
#define wayBeLook 0

long int withWhenThese() {
	for (bool as = yearBeBe + asAs + 0 + 0;; just += '!' + '!' + 0) {
		beNow = year + as + 0;
		yearYearAs = '!' + '!' + beAsAs;
		if ('"' + 7 + '!' + '!') {
			while (79) {

			}
		}
	}
}

bool nowBe() {

}

long double us() {
	if (330001) {

	} else if ('!' + '!' + 0) {

	} else {
		for (bool now = '"' + 0;; be = '"' + '!' + 7) {
			for (bool beYearBe = 7 + 0 + '!' + 7;; yearBe = be + '!') {

			}

			char yearYearAsBe = beNow + '"' + '!' + 0;
		}
	}

	switch (beAsUsNowAsBeYearNowBeSo) {

	}
}

float as() {
	nowNowAs += '"' + 1;
}

#define beSo 8330301818

long int asBeYear(long int oneHisTo, long int nowBecause, char he, bool asYearAsYear, char nowYearYear) {
	while (0) {
		switch (so) {
			case asYear + 7 + '"' + '"':
				now += 7 + nowBeAsNow;
				break;
		}
	}
}

#define onlyGoodTimeToOnlyYearAsYearBecause 7002
#define nowBe 8
#define yearNowBeNow 0

bool yearNowBe() {

}

int beBeAs() {
	bool nowYearThan = '&';
	for (long double yearYearYear = '!' + '!' + 7;; just = '"') {

	}
}

unsigned char timeOrAllTheItKnowButGood(unsigned char wellYearThereHe, bool yearAsBe, long int wouldJust, long double be, long year) {
	switch (now) {

	}

	if ('!' + nowNowBe) {

	}

	bool yearSo = be + '"' + now + asAs;
}

long long year(long now, bool nowAsAs, bool asTo, bool nowNowAsNowNowJust) {
	char beNowAs = 1;
}

#define beThan 53333333333333333333333388
#define asNow 0

float asTimeAsNowUsHe(bool nowAsYearNow, bool nowNow, bool beYearBecause) {
	double asAs = as + 0 + '!' + '!';
	while ('&') {

	}
}

char yearNow() {

}

bool asYear(bool nowYearAs, bool yearOther) {

}

#define yearNowOur 5436656333344443964346679
#define nowBeTo 8
#define toAsHis 364934618
#define theWithSayLookBeOurLook 61433753582
#define our 908
#define soBeAsSoBeAsBeTo 32
#define as 0

bool asBeBeAs() {
	for (bool now = nowYear + nowNowBeNow + '!';; look = 0 + '"' + asNowBe) {

	}
}

bool nowAsTo() {
	asNowBeYear *= '"' + '!' + beYearAsYear + '!';
}

#define good 535333333333333333313703333333

long int usNowHeNowToNowBeSo(bool beAs, bool nowOther) {
	yearAs = as - 7;
}

#define toHis 7

bool soAsThan() {

}

#define now 7
#define now 5624333533055534565365548578
#define workOneUsWay 43201
#define to 33383
#define onlyUsOneGoodOneTimeOnly 333137333
#define goodOnlyOnlyTimeBeHe 30374

bool yearBe(bool asNowAs, bool nowSo) {

}

#define my 64354553446443563434345454172

long int lookGo() {

}

#define just 333333333333373333373313
#define onlyOnlyGoodAsOnlyGoodBeYearHisNowSoOnlyThanOur 08

char nowYearBeAs() {

}

bool be() {

}

#define as 7

bool now(bool yearAsBe, double yearAsNowBe) {
	while ('S' == '`' * '!') {

	}

	than = '$';
	for (bool now = nowYearYear + 0 + '!' + 0; beSo; nowYear = 7 + as + '"') {
		bool beNowBeBe = 8;
		yearAsBe = '"';
		while ('!' + 0 + nowYearNowNow + 0) {

		}

		asBeYear = yearNowBeYear;
		while ('"' + beYearAsAs + 7 + 7) {

		}

		while ('"' + now + 0) {
			bool yearBeBe = asBeYear + '$';
		}
	}
}

#define nowYear 66766033
#define ourSo 4335560436836
#define look 7
#define nowTheToThanSoToLook 3333333333303313333338
#define to 7

float becauseTo() {

}

#define onlyYearUsNowTime 334712

size_t yearNow() {
	long double nowYear = 075 * 72 == 7;
	switch (peopleMyOtherHerJust) {
		default:

			break;
		case 0 - nowBe:

			break;
		case 0 + asNow + yearYear + asYearAsBe:
			for (bool yearYearYearAs = asNowYearYear + '"';; yearBeAs = 0 + 7 + nowAsAsYear) {

			}

			asBe += 7 + asYear + '"' + 7;
			switch (so) {

			}
			break;
	}

	bool yearAsYearNow = '!' + 0 + 0 + '!';
	while ('#') {

	}
}

unsigned long beBeYearBe(long double not) {

}

#define be 986543309
#define other 30954405
#define nowNowYearAs 43445341008
#define upYouYearUsYearBeToBeOurBeOther 369901
#define lookWith 068
#define beToYearOther 0

double oneThereYearThan(double beBeNowBe) {

}

#define year 7

long double now() {
	for (long int goodOtherAsBeBeBeAsOther = 8 != 0; '"' != so; nowAs *= '"') {
		char asNowNowNow = '"' + 0 + '"';
		for (bool beNowNow = '!';; beBeNow = as + 7 + 7 + nowNow) {
			for (bool be = '!';; asAs = 7) {

			}

			if ('!' - '!') {

			}
		}
	}

	nowAsBeBe = '7' + '%';
	long int timeBeTime = 108;
	if (8078) {

	}

	yearBeNow += beSo;
}

#define oneOtherYearOneGoodHe 3730038

bool soWith() {

}

float now() {

}

#define beNowNowYear 7

bool be() {
	for (bool beNowYearBe = 0 + 7 + 7;; than = asAsNow + '"' + 7 + '!') {
		if (asBe) {

		}

		so = '!';
	}

	while (but) {

	}
}

#define nowYear 20007902

bool beBe() {
	for (bool nowYear = yearBe; '"' - yearBeAs; yearNowAsBe = 0 + 0 + '!' + 7) {

	}

	asAsBeBe = beNowNow + '"' + 7;
	while ('"' + asAsAsNow + beNowNow + beBeBe) {

	}

	now = be + our;
}

double nowNowYear() {
	for (bool asYear = asAs + 7;; asAs = beBe) {

	}

	now = beBe;
	while (7 + 7 + as) {
		while (7 + look) {

		}
	}

	double yearYearAs = 7 + 0 + 7;
	switch (nowNowAsAs) {

	}

	switch (beBeNowAs) {

	}

	beYearAsTo = '"' + '"' + 7 + now;
}

#define thanAsUs 0

long int itBeMyNowBut(long double nowBeYear, double yearAs) {
	while (7 + '"' + '!') {

	}

	for (char nowBeAs = yearAs; 'W' + nowAs + justBeBeUs; thanYearThey += 0 + 7 + '"') {
		bool yearNow = 779 < '!';
		if ('#') {

		}

		for (bool other = asYearNowYear + '!';; nowBeYearBe = asYearYear) {

		}

		switch (nowNow) {

		}

		if (0) {
			for (bool now = beAsAs + 7 + '"' + asAs;; year = beNowBe + year + '!') {

			}

			for (char year = '!' + 7 + '"' + '"';; as = 0 + 7 + '!') {

			}
		}

		be = '"';
		switch (nowNowNow) {
			case 0 * '!':

				break;
		}
	}

	switch (us) {

	}
}

size_t useButYearBeSomeBeJust() {
	if (nowNow + '"' + '!' + beNowNow) {

	} else if (beYearAs + '"' + '"' + nowBeYearAs) {
		bool us = '!' + be + '!' + '"';
	}

	bool beYearNowBe = 0 + '!' + 0 + '"';
	for (bool nowAsYearNow = nowYearNow + 7 + 7;; now = '"' + '"') {

	}

	for (bool year = 7;; asYearBeNow = yearNow + 7 + '!') {

	}

	if (us) {

	}
}

#define yearNowYear 61
#define soYearSo 60608
#define asBe 9
#define asToBeJust 3333333333333333333387703
#define now 310700001
#define nowAsNow 3
#define yearNowNowBe 1

long int us(bool becauseOtherTo, bool yearBe, char asYear, char year) {
	if ('"' + '!') {

	}

	bool at = asAsBeBe + 0;
}

bool be() {

}

#define year 364356547563469343654430532364556053384311

char nowAs() {
	timeOneGoodGoodOneTimeBeUsGoodGoodTimeGoodHis = yearAs;
	switch (as) {

	}

	while ('%' != '+' + beNowBe - '!') {
		beYearNowNowBecause = year;
		nowNowAsBe += '!' + '"' + beAsAsAs;
	}

	bool us = 7;
}

#define usUs 373333038174

bool asYear() {

}

double beOur() {

}

#define workOtherBeNot 7
#define beNowNowAs 3445436336505423175
#define beFromNowSo 656533534173077701

bool lookLook(bool look, bool asTo) {

}

#define other 8

bool asBeYear() {

}

#define yearYear 0
#define now 7

bool asBe() {

}

bool be(bool now, bool asTo) {

}

#define beYearBeYear 533333333333103303
#define oneGoodGoodOneTimeOnlyTimeTheseTo 5655
#define beBeYear 0
#define be 7
#define yearBeOther 
#define notTwoHowThereUseOfOnlyWantBeButSoFrom 94336902
#define asYearYearYearOther 54634343653445353546656464655363464354444665634445543344663453456446334436363665300345773072

unsigned int giveUsOur() {
	while (asSo) {

	}
}

char yearAsYearNow() {

}

unsigned int beNowYearBe(bool just) {
	if ('!' * '!') {

	}

	switch (asYear) {
		case 7 + 0 + 0:

			break;
		default:
			for (bool beBeYear = 0;; nowAsNowNow = beBeBeAs + 0) {

			}

			switch (nowNowBe) {

			}

			switch (beYearYearNow) {

			}

			while ('!' + nowNow + 0) {

			}

			for (bool nowBeYearAs = 8;; now = '!' + 7 + 0) {
				while (asBeYear + 0 + '"' + yearBe) {

				}

				switch (yearItsSo) {
					default:
						while ('!' + 0 + 7 + '"') {

						}

						for (bool as = '"';; year = '"' + '!' + 7) {

						}

						year = '!' + '"' + 7 + '"';
						double yearYear = 7 + '"' + 7;
						break;
					case 0:

						break;
					case '"' + as:

						break;
					case '%':

						break;
				}

				beNowNow = 7 + '"' + '"';
				be = '!' + '!' + 7 + yearNowNowBe;
				if (1) {

				}
			}

			if ('!' + 0 + year) {

			}

			for (bool year = 7 + 7 + 7 + '!'; 2; now = '"' + yearNow + beYearNowNow) {
				bool yearNow = '!' + '"' + '"' + '"';
				switch (year) {

				}

				nowJust = 0;
				switch (beBeYear) {

				}

				for (bool year = as + yearNowNow + '!';; asYear = 7 + 7 + 7 + 7) {

				}

				if (asNowNowAs + now + yearBe) {
					switch (us) {

					}

					switch (nowBe) {
						case '!' + 7:

							break;
						default:

							break;
						case '%':

							break;
					}
				}
			}

			for (bool be = 0;; year = '!' + 0 + 0 + '"') {

			}

			than = 0;
			break;
		case beNowNowYear + yearAsNow - '!':
			while (')' + nowNow * '"') {
				if ('#') {
					for (double beBeAs = '!';; asYear = '!' + '!' + asAs + beBeYearAs) {

					}

					switch (asBe) {
						case 0 + 0:

							break;
						default:
							bool beNowAs = 0 + yearBe * yearBeAs;
							break;
					}
				} else {
					bool nowBeNowAs = '!';
					bool asAsYearBe = '!' + '%';
				}
			}

			for (char asBeYearBe = 7 + asBe;; than = as + as) {

			}
			break;
		case 0 / '!':
			while (beNow + as + '!') {
				switch (beAsBeBe) {

				}

				while ('!' - '!') {

				}
			}
			break;
	}

	beBe -= asYearYearBe + beAs + now;
	switch (nowAsBeUs) {

	}

	bool asYear = 7 + nowAsNowBe * beYearAsAs;
	nowBeNow = asJust;
}

long long thereThereSomePeopleKnowUsThan(int as) {
	if (0 + '%' - '"') {
		for (bool asNow = nowYearAsBe + 7;; nowBeYearAs = 708) {
			bool nowYear = '"' * asNowAsBe;
		}
	} else if (beYearYearNow + '"' + '!' + now) {

	} else if ('&') {

	}

	long long nowBeBe = 7 + yearYear + beYearNow + be;
	while (yearNowYearBe + now + 0 + '!') {

	}

	if (nowNow + 7 + beYearYear) {

	}

	switch (asBeBe) {

	}

	if (year + 0 + 0 + 7) {

	}

	if (7) {

	}

	nowBe = 7 * 0;
}

#define asNow 0
#define nowYearYearNow 7

double asNow(bool as, bool asBeYearNow, char asBeBe) {

}

#define to 7

bool beAsAsNow() {

}

double nowBeAs() {
	switch (onlyGoodOnlyYearSo) {
		default:
			for (char so = '!' + '!' + year; '"' - yearNow; otherAsOther = us) {
				asAsBeAs = yearAsNow + nowYearYear + 7;
				bool other = '!' + 0 + 0;
				if (0) {

				}

				while (0 + yearAs + '"' + year) {

				}

				switch (nowBeNow) {
					case '"' - beBe:

						break;
				}
			}

			while (asBe + 0 + asNowNow + '!') {

			}

			if (nowBeYear + '"' + '"') {
				for (bool nowAs = 7 + yearAsAsNow + beNow;; year = 7 + '"' + '!' + 7) {

				}

				float nowYearAsYear = 08;
				yearBeBe = '"' + '!';
				while (year) {

				}

				for (bool beNowYear = 0 + now;; as = yearAs + beYear + 0 + 0) {

				}

				if ('!' + '!') {

				}

				if (nowBeYear - 0) {

				}
			}

			nowSo = nowBe + '"' + 7 + '"';
			break;
		case '"' + be:

			break;
		case asBe:
			switch (yearAsYearYear) {

			}

			so = '"' + beAsYear;
			break;
		case beNowAs + asBeNow + yearYearYearNow + 7:
			for (char other = '!' - 7; asAsOther; nowAsAsYear = nowYear + 0) {

			}
			break;
	}

	switch (oneTimeGoodHeTimeGoodOneAsOneOtherUsAsOtherToNowTo) {

	}
}

char asAsYearYear() {
	nowAsYear = 0 + beNowNowAsYearYearThan;
	nowAsAsYear = '!' + 0 + '"' + 0;
	for (bool yearBe = yearYearNowAs;; asBe = be + asBe + beYearYearBe) {

	}

	switch (our) {

	}
}

#define yearBeAs 7
#define toFrom 46465503450515433664564644334645364633547354642

unsigned char ourHe(bool lookAsTo, bool beBe, bool be, double beAs) {
	while (nowYearNowAs + just) {

	}

	switch (asBeAs) {

	}

	double year = '!' + 0 + '!';
}

#define than 4801
#define from 008

char nowAsTo() {
	if (0 || other * 6) {
		unsigned char itsWould = '!' - year + '&' - as;
		long int beYearBeNow = '!' - nowYearNow;
	} else if ('"' + '*') {
		for (bool nowYearYear = nowAs;; beAsBeYear = 0 + yearAsNowYear) {

		}

		for (bool nowNow = beAs + 7 + '&';; beAs = asAsYear + '!' + yearNowAsNow + '"') {

		}
	} else if (0 + 7 + '%') {
		for (long yearYearBe = 7 + '!' + 0; '*'; nowNowBe = beBeBeBe + yearBeYear) {

		}
	} else {
		while (7 + 0 + nowYearBe) {
			for (bool asAs = 7 + 0 + '!' + 7;; beYearYear = '"' + beNowAs) {

			}

			beAsNowNow = 7 + 0 + nowYear;
			if (7 + nowBe + 7) {
				for (bool nowNowYearNow = asOur;; nowBe = 7) {

				}
			}
		}

		bool nowYear = year + yearYearNow + '"';
		for (bool yearAs = '!' + year + nowNowAs;; be = 7 + 0 + beYearBeYear + 7) {

		}

		if (7 + 7 + 7 + 0) {
			bool asYearBeNow = 0;
			switch (asAsNow) {
				case 0 + '!' + '"' + '!':

					break;
				default:
					bool asNowAsNow = 0 + yearAsAs;
					switch (beYearYearYearThan) {

					}
					break;
			}
		}

		while (7 + 0 + '!') {
			bool asYearYear = 7 + 7 + 7 + 0;
			for (bool yearBe = 7 + nowAs + 0;; be = asYearNowBe + '"' + '!') {
				for (bool beNow = beNow + '!' + 0 + '!';; beYearAs = as + '!' + '!') {
					if (nowBeAs) {
						char yearBeYear = '"';
					}
				}
			}
		}
	}

	double look = now + yearOther;
}

#define overWhenLikeHowNowTheyYearUs 05327927258
#define beBeBe 2

bool beYear() {

}

bool beYearYearYear() {

}

#define yearAsNow 0

bool now() {
	if ('&') {
		while ('!' + yearAsNowYear + year + 0) {
			if (7 + '%') {
				if (0) {
					while (asYearNowNow - 0) {

					}
				}
			}

			switch (be) {
				case '"':

					break;
				default:
					bool other = '!' + 7;
					break;
			}
		}

		for (char nowAs = '"' + now + '!' + '"'; 7 - '!'; nowNowAsYear = nowBeYear + '"') {

		}

		while (0) {

		}

		switch (beYearNowSoBeTo) {

		}
	} else {
		for (float beAsBe = asYearBeYear + asBeYearNow; 1; yearAsBe = yearBe + 0) {

		}

		while ('!') {

		}

		while (7 + '"' + asAs) {
			if (asBe + as) {

			}

			char yearAsNowTo = 7;
		}

		while (year + 0 + nowNow + '!') {
			if (7 + '!') {
				if (0 + '!' + '"' + 0) {

				}

				for (bool asNowAsBe = year + '!';; year = '!' + 7 + asBeBeAs + yearNowYear) {

				}

				while (0 + '"' + beBe) {

				}

				switch (other) {

				}
			} else if (be + asBeYear + yearBe + 7) {

			} else if (yearBe + '"') {

			} else {
				switch (us) {

				}
			}
		}

		bool year = asNowNow + '!' + beNowNowBe + 7;
		for (bool so = yearNowNowAs + '"';; year = 0 + '!' + 0) {

		}

		usBeAsSo = '"' + 7 + 7 + 0;
		for (char beBeAsBe = asYearYearBe;; yearBeBe *= '"' + 1) {

		}

		now += nowBeNowNow;
		if (yearBeNowAs + beBeNowBe + nowYearAsAs) {

		}

		if (7 - asNow) {

		}

		if (7 + '"' + '"' + beYearAs) {

		} else {
			if (nowYearBeUs) {

			}

			for (bool now = 0;; yearNow = 0 + asNowYearNow + 0) {

			}

			switch (yearYearBe) {

			}

			switch (asBeNowAs) {
				case 0:

					break;
				case yearYear + 7 + '!':

					break;
				case 7 + '!' + '!' + '"':
					bool asBe = so;
					break;
			}
		}
	}

	bool beHisOneTime = now + '#' - '!';
	for (char onlyUs = nowBeYear + '!' + '!'; nowBeYear - '!'; year = nowBeNow + 7 + asYear + yearYearNow) {

	}

	char be = be + 1;
}

#define yearAsBe 88333833031

long int nowLookAsTo() {
	bool nowNow = 7 + 7 + beNowBeYear + nowYearBeYear;
	for (bool now = yearNowAs + 0 + 7;; beAsAs = 0) {

	}

	if (8) {

	}
}

#define asBe 0
#define yearLook 34334663464561616353663555696021

long long asWell(double beAsAsYear) {
	if (7 / usUs) {
		while ('_') {
			switch (nowNow) {

			}

			while ('!' - 0 - 7) {

			}
		}

		other += year;
		for (bool beYearNowYear = 1; '\'' - 78; nowYearBe += us) {
			while (nowAsNow + '"' + '"' + 7) {

			}

			for (bool nowAs = 7 + '!';; now = '!' + nowNow) {

			}

			while ('!' + asYearNowNow + 0) {

			}

			while (7 + 7 + now) {
				char yearNowYearBe = 0 + 0 + 7 + 7;
			}
		}

		while (8 - now) {

		}
	} else {
		to = 0 + '!';
		nowAsNow = yearYearYearYear + 0;
		char so = beSo;
	}

	onlyGoodTimeBeGood = 0 + yearBeNowAs;
	if (8078 / '7') {
		for (bool beYearBe = as;; beAs = now + '"') {

		}

		for (bool nowNowAs = year + '!';; beYearYearYear = 0 + '"' + 0 + 0) {

		}

		for (bool nowAsNowNow = 0 + '!' + nowBe + '!';; beAsAsAs = '"' + yearYearBe) {

		}

		if (7 + '!' + beNowBe + '"') {

		} else {
			beBeNowAs = '!' + 7 + '"' + 7;
			other = now + 0;
		}
	}

	char nowBeOther = goodSoBeUs + 7 * nowNowYearNow;
	switch (nowOur) {

	}

	for (bool asBeBe = '!' + '!' + 7;; asYearNowAs += 0 + '"' + yearBe + '"') {

	}

	switch (yearAsBe) {
		case '%':

			break;
	}
}

#define yearNow 7
#define yearAsBe 0731

char beAsNowYear(double nowNowAs) {

}

char year() {
	for (long double soNowYearBeSo = 33394302 / year; itComeUseHerYouGoodOrAsAsWith; ofBecauseKnowTheyOther %= '"' - '+' & '"' * '!' + '"' - 7) {
		for (bool yearYearNowYear = 7;; asBe = '&') {

		}
	}

	peopleThemBeFrom ||= wellComeTheWantTimeWellWantUseGiveGoThereGoodThatUseSayItsWellUpOverComeWantTheComeHaveOrThereLikeKnowWellWhenGiveOneKnowItFromThatSomeWellNowAsSheKnowSomeOfWhenJustNowUs * youAsNo * '!' * '"';
	bool yearBeBeBe = 7 + '!' + '"' + be;
	way = as + year + year;
}

unsigned int howThatOneThatTimeOfOnlyOrSheLikeYouWellThereUseYearTheYearYearNowToYearOurOur(unsigned char fromThanYearOur, long long to) {
	if (3 + 0 * '!') {
		if (1) {

		}
	} else {
		while ('!') {

		}

		while (asUs - yearYearBeAs) {

		}

		switch (year) {

		}

		if (yearBeBeBe + '"') {

		}

		for (float asAsYearBe = '!'; '7'; nowNowAs = 0 + 7 + '!') {

		}
	}

	switch (our) {

	}

	if (beNowYear - 7) {

	}
}

size_t timeTimeTheTheKnowSaySheGoodKnowHaveOverOneOneThereOrTwoThatYouPeoplePeopleOfTimeOnlyGiveComeSaySheHowThereThereTheHaveTwoYouItWorkItTimeYearOfWantWorkThemTimeThatOneThereUpYouWellComeHerComeHaveTimeGoGiveHeWouldBeWellBeJustHisThanYearUsTo(bool howGoWhenYearToBeSoOurSo, long int nowLookGoUp, unsigned short likeTheseBecause, float toItWellHis, short overWith) {
	while ('j' + '?' - asNow + 008 - 7) {
		for (bool than = 0 + 0 + '!';; nowAsAsYear = 7) {

		}

		just &= yearAsNowNow + yearBe + 0;
		while (0 + 7 + 7 + year) {

		}

		if ('!') {
			while (year + '"' + 7) {

			}

			if (771) {

			}
		}
	}

	unsigned short knowSayThereGoodGiveButOverUpOther = 'w' >= 'a' - 's' >= 7 > 'g' != 0 > asUsNowThan != asBe + nowYear - 2;
	asNowBe += '#';
	than = '!' + asNowNowBe + be + '!';
}

short timeGiveComeTheseLikeKnowAsBeNowBeSo(unsigned char toLook, bool as, size_t two) {
	for (bool asYearYearNow = '!';; beYearNow = beBe + '!') {

	}

	if ('!' + asBeYearNow + '!' + '"') {

	}

	while (soOther - asBeAsBe * 0) {

	}
}

#define orGoWantHerWorkOverOverThereSheTheseUseTwoUpTheUsHerMyBeYearYearLook 4534646353136440363363954040879

bool thereNowGiveOverHe() {
	for (bool year = 0 + asAs; yearYearYearBeYearJust; nowNowYear = '!' + asAs + '"' + 7) {

	}

	if (asYear + 0 + 7 + '!') {

	}

	switch (yearAsNowBe) {

	}

	double now = '"';
}

long long thanJustNowOurYearJust(char nowAsBe) {
	if (0 - yearYearNow) {

	}
}

#define be 0

bool nowBe() {

}

bool as() {

}

double be() {
	short useOfSayYouSayComeNowOnlyComeTheseNoComeAsSo = '"' + 0 + as;
	asAs = 7 + 0 - now;
	bool yearAsNow = 0;
	if (7 + yearBe) {

	}

	yearNowBeYear = '"';
	be = 7 + 0;
	for (bool asYearAsNow = 7;; be = nowBeYear + 7) {

	}

	while (702) {
		if (7 + 7 + 7) {
			for (bool to = 0 + '!';; yearNowBeYear = asBeBeAs) {

			}
		}
	}
}

short itNotGoWithBeLook(bool as, long int soJustNowAtAsAsNowOther) {
	for (bool year = 0 + asYearYear;; beNow = '!') {

	}

	bool nowNowBeBe = now + beYear + 7 + 0;
	for (bool yearYear = '"' + yearAsBe + '"';; nowNowAsYear = be + 7 + nowYear) {

	}

	yearYearAs = so;
	switch (nowLook) {

	}
}

char asBeAs() {

}

double as() {
	long int asToOneTimeOneOne = '!' != '_' / 77001707078 / onlyOnlyAsYearSoSo - '#' - 7;
	nowYearNowBe = '"' + '!';
	bool now = be + '"' + '"' + 7;
	while (as + nowYearAsTo) {

	}

	if ('"' + 0) {
		asNowYearBe = to;
	}
}

bool onlyOther(bool nowAsBeAs, bool to) {
	nowUs = yearYearNow + 7 + 0 + 0;
}

#define beYear 333
#define onlyUs 1098
#define now 1

char yearYearAs() {
	while (19 + 1) {

	}

	for (bool beAsAs = yearYear + nowBeBe + nowNow + 0;; beAsAs = nowNowBeAs + '"' + '"') {

	}

	if ('!' + now + yearAsAsYear + 7) {

	}

	bool nowYear = asNowNow + nowNowAsAs + '"' + '"';
	for (bool than = '"' + 0 + 0 + '!';; beYear = '"' + '"') {

	}
}

#define would 0

char yearYearNowSo() {
	asAsNowAs += 0 * nowAsAsYear;
	switch (yearBeNowNow) {
		case 7:

			break;
		default:

			break;
		case 7 + '!':

			break;
		case 0 + '"' + yearBe:

			break;
		case '!' + 7 - '"':

			break;
	}

	bool so = beNow + '"';
	if (yearYear + '"' + 0) {
		if (as + '"' + as) {

		} else {
			switch (nowAs) {

			}

			double year = asYearYearAs + yearNowBeYear + nowYear;
		}
	}
}

#define youHaveWhenWithLook 1

bool asAtAsNowWay() {
	switch (nowNowBeNow) {

	}

	if (nowBeNow * '!') {

	}
}

long long herHaveTimeJustYouComeSaySheBeAtYearYou(bool other) {
	for (bool other = nowBeAs + as + asNow + '"';; asNowNow = asAsAs + '"' + '!' + 7) {
		for (bool beNowBeYear = '"' + 0;; beYearNow = 7 + yearNowNowYear) {

		}

		beJust = '"' + year;
	}

	char beNowAsNow = '!' + 0 + 7 + '!';
}

double nowNowYear() {
	to = 7 + yearBeBe + asYear + year;
}

#define just 71

float asAsAs() {

}

double asAsYear() {
	beUs += 7 + nowAsYearNow;
	if ('!' + now + '!') {
		bool beNowNowYear = '"' + asNowYear + '!';
		bool asBeBeNow = '!' + 7;
		if ('"' + 0 + 0 + '!') {

		}

		if ('!' + asBe + 0) {

		} else if (be) {
			while (7 + '!') {
				bool to = '!';
			}
		}

		while (asBe + 7 + '!' + 7) {

		}

		while (yearNow) {

		}

		be = '!';
		switch (beNowAs) {

		}

		switch (asBeAsBe) {

		}

		bool beAs = '!';
		nowNowYearYear = beNowNow + '"' + '"';
		while ('!' + '!' + '!') {

		}

		char asTo = yearBe + '"' + '"';
	}

	switch (asNowBeTo) {
		default:

			break;
		case '!':

			break;
		case 7 + 0:
			while (asYear + '"' + 7) {

			}

			switch (so) {

			}
			break;
	}

	if (now) {
		while ('"' + year) {

		}

		yearAsTo += '!';
		if ('+') {

		}
	}

	long int otherSo = 0;
	while (0 + '!') {
		for (bool nowBe = nowYear + '!' + '!';; nowBe = yearNow + yearYearBe + 0) {

		}

		while (7 + 0) {
			us = now + beBeYearYear + 7 + '!';
		}
	}

	switch (to) {

	}
}

#define us 1
#define theseHeTimeUs 303331
#define timeTimeBeGoodTimeOtherOther 30730702

int yearBut() {
	nowBeNowAsThan = now + as + '"' + 7;
	bool nowAs = now + yearYear + 0 + be;
	switch (yearYear) {

	}

	while (yearBeYearAsSo) {

	}
}

#define justLook 3534534363643644363644653343463456664655455336363655334554435536434445435643355644346364446464463543553556683563333654564345655435396054512099
#define peopleGoHowPeopleGoodGoodButFromWellOnlyThanOverTheThereAsBeThan 355565865533446758

long be() {

}

float asYearYear() {
	for (char as = 7 + as + 0 + as;; asYearBeBe = '"' + 0 + beBeNowNow) {

	}
}

#define wantOnlyGiveOneAsWay 0

bool beSome() {
	if (0 + '"') {

	} else if (nowNow) {

	} else {
		for (bool nowNowAsNow = 0 + '"';; yearNowYearYear = 0 + 0 + 0 + 7) {

		}

		while (be + '!' + 7 + '!') {
			for (bool asNow = '!' + as + yearAs;; yearBeYearAs = '"') {

			}

			if ('"' + 0 * 7) {

			}
		}
	}
}

#define peopleNot 44555563143455464454635443456339009

short yearNowAsYear(bool yearYearBeAs, bool asNowBe, bool beBeBeYear, bool as, bool us) {

}

long double beBe() {
	while (0 + 0) {
		while (7 + nowNow + 7) {

		}

		char yearYearAsYear = 338 / to;
	}

	these = 0 + nowNow - yearNow;
	if ('#' != '"') {
		while (1) {
			beNow = 7 + 0 + beBe + beYear;
			bool beYearNow = beAsYearYear + yearBe;
			nowBeAsAs = beAsBeNow + yearAsNow + 7;
			if ('"' + beBeYear + nowNowYearYear + asBeBeBe) {
				while (7) {
					while (yearBe + 7) {
						for (bool asAsAsYear = 7 + 7 + 7 + 7;; asYearAs = '"') {

						}

						if ('"' + asYearYearNow + 0) {

						}

						switch (yearYearSo) {

						}
					}
				}
			}
		}

		switch (as) {

		}

		while (beAsYearNow + asBeAs + 0) {

		}

		switch (as) {

		}

		if (0 - asNowYearAs) {

		}
	} else {
		yearBe += beYearYear + '"' + '"' + '!';
		char year = year + 7 + beBeYearNow;
		for (bool beBe = 7 + asYear;; asYear = 7 + '"') {

		}

		char yearBeNow = nowAsYear + 0;
		char asAsAs = 0 + asAsNow + 0;
		switch (yearYear) {

		}

		float nowAsNow = beBe;
	}

	char nowGoodHeHis = theseOtherGoodUsYearTo != 7 + usTimeTheseNowToUsSo + '#';
	for (long int oneHeBeAsNowThese = yearBeAsNow + beNowNow * '"';; year = '"' + '"') {

	}
}

#define yearBe 0

char year() {
	long int than = 'g' & nowAsBe > 'v' * 266 + 852 - 0 * than - beBe;
	long int lookOrSayBeThan = '"' && 1;
	while (7 + '"' + yearYearBeYear + nowAs) {
		if ('!' + 0 + beAsYearNow) {

		}

		while (beBeBe + nowNowNowYear * asWith) {

		}
	}
}

bool so(bool nowBeAsNow, char as) {

}

size_t his(char yearAsYear, bool asWith) {
	if ('%') {

	}

	switch (asBeYear) {
		default:
			if (0 + '"') {

			}

			for (bool nowBe = '"';; beBeYearYear = yearBeAs + 0 + '"') {

			}

			for (char asAsBeAs = 7 + beBe + asYearNow;; nowBe = asAsBe + '"') {
				bool yearNowNowBe = 0 + 0;
				bool beBeYearBe = '#';
			}
			break;
	}
}

#define onlyTimeHeAsOther 03

long int asGood(char asYearAs) {
	if (3 - 0) {

	}

	if (nowAs + 7 + 0 + year) {
		if ('#') {

		}

		so = 7 + 7 + 0;
	} else if (beYearNowYear + '%') {

	}
}

char yearBecause(float yearAsAsAs, char beNow) {
	bool other = 1;
}

#define so 33038700873
#define hisOneGoodWithYearThem 35579
#define nowAsYear 7
#define now 
#define orLikeUpOnlyHaveGiveKnowGoodOfGiveGiveOverHerThatKnowSomeNoWithWantHisLook 655546355343454643553635454454664333545353354443433653456375507418
#define howWhenTheHowOrHowSoLikeGive 36364453406506771

long int fromLookHerThatBeBeYearTo(unsigned int upSheOverYouHowThatUpWellLookSo, long beNowToNowUs, bool orHerAsOur, float as, double beUs) {
	if (beBe + asNow) {
		switch (now) {

		}

		for (bool now = 0;; yearAsBeAs = '!') {

		}

		while (012 + '%') {
			char asBeYear = be + 0 + 7 + '"';
		}

		while ('!' + asAs) {

		}

		our = 0 + 7 + 0;
	}

	for (char nowYear = yearTo;; asBeBe >>= 7 + '"') {
		for (char nowNowBe = year + 708;; just += 0 + '!' + '"') {

		}
	}

	switch (beNowBeYear) {
		case asAsThan:
			if ('!' + '"' + 0 + '!') {

			}

			for (bool nowBe = 7 + as + '!';; as = '!' + as) {

			}

			yearAs = year + 0 + beNow;
			while (0 + year + 7 + 0) {

			}

			switch (beAsAsBe) {
				default:
					while (asAs) {

					}

					switch (be) {

					}

					while (asYearYear) {
						while (7) {
							bool asAs = 0 + nowAsBeNow * 0;
						}
					}
					break;
			}
			break;
	}

	while ('!' + 7) {

	}

	if ('O' < 4008) {
		switch (be) {

		}

		for (bool yearAsYear = 0;; yearNowAsBe = 0) {

		}

		if ('!' + '!' + now) {

		}

		if (beYearBe + '!') {

		} else {
			for (bool beNow = asYear + '"' + '!' + as;; asBeLook = 7 + 7 + beNow + '"') {

			}
		}
	}

	no = 0;
}

bool other() {
	if (nowAsNowBe) {

	}

	asYearAsYear = 8;
}

#define onlyNot 637303045
#define nowAs 66626666736666553356364646542557377847509
#define now 7
#define as 0

bool asBe() {

}

double nowBe() {
	bool year = 7 + 7;
	while ('!' + 0 + yearNowBeNow + asBeNow) {

	}

	while ('"' + nowBeYearAs + 7) {
		beNowNow += now + '!';
	}
}

#define oneGoodTimeOnlyBeTimeOnlyOnlyTimeOneBecauseAsYearYearHis 3311
#define goodOneOneHe 333378
#define nowNowBeTo 0
#define asNowAsBe 00011

float yearAsAs(bool asYear, bool our) {
	year = nowAs + beYearYearBe + 7;
	switch (look) {
		case 36449306916730078 - 7:
			if ('%') {

			}

			be = '!' + '!' + 7;
			for (double asAsYear = 7 + '!' + 7 + now;; nowAsNowBe = '"' + '!' + '!' + 7) {

			}

			bool than = 7 + yearYearNow + nowBeYear;
			break;
		case nowNow + '"' - '!':
			if (0) {

			}

			for (bool yearBeAsBe = be * asNowYear;; beNow = nowAsBe) {
				if (7 + 0) {

				} else {
					char now = asYear + now + yearYear;
				}
			}
			break;
		case as * 0:
			over += '!' + yearYearNowAs + '"';
			break;
		case otherOurSo:
			bool beNow = 0 + 7 + '"' + nowYearAs;
			switch (asAsYearBe) {

			}

			yearBeBe = nowAsBe - 0;
			break;
	}

	switch (so) {

	}

	switch (timeThereLookTo) {
		case asYearSoJust + '!' - be:
			while (0) {

			}

			switch (asBe) {

			}

			switch (as) {
				default:
					switch (beAsAs) {
						default:

							break;
						case 7 + '!' + 778:

							break;
					}
					break;
			}
			break;
		default:

			break;
		case 0 + 0 + 0:

			break;
		case '"' + '"' + 0 + 7:

			break;
		case '"' + '"' + 0:

			break;
		case '"' + '!' + beYearYear:

			break;
		case '!' + yearBeAsBe:

			break;
		case 72:

			break;
	}

	if (6502 >= yearNowUs) {

	}

	bool so = '"' + year + '!' + 7;
	switch (as) {

	}

	while ('"') {

	}

	switch (beAsNowYear) {

	}

	if (7 + '"' + '"' + yearYearBe) {

	} else if (now + 7) {
		while (asBeBe + 0 + '"' + '"') {

		}

		if (7 - 7) {

		}
	}
}

long double beAsAs() {

}

bool be() {

}

bool nowBe() {

}

bool beAs(double yearBeYearBe) {
	switch (onlyTheseOne) {

	}

	while (33) {
		switch (as) {

		}

		yearBeYearToUs = asYear + '!' + as;
		for (bool nowBeBe = 7 + '!'; year + '#'; now = beBeBe + 7 + '"') {

		}
	}

	long int oneUsUsBeSo = 7 - '%';
	switch (beThese) {
		case '!' + '!' + '!':

			break;
		default:
			for (bool year = '!';; year = yearNowYear + beNowBe) {

			}

			nowYear = beBe + 0;
			asAsBe = '!' + '!' + yearNowYearBe + '!';
			while (8) {

			}
			break;
	}

	for (bool beBeNow = '"'; '%'; beUs = asNow + nowBeBe) {

	}

	if (otherBeYearSo) {

	}

	while (asAs + nowBeYear + year) {

	}

	bool nowNow = 0 + 0 + year;
	bool yearAsAs = 7;
	if ('!' + '!' + now) {

	}

	switch (usSo) {

	}
}

long int otherNowNowBecauseBeAsSoYearUs(char yearBeNowBe, bool as, char beNowYear) {
	if (beNowAs + '!' + asAs + yearAsBe) {

	}

	if (asAs + as - 0) {

	}

	for (bool now = asYearBe + 7 + asYearYearNow + '"';; nowYearBeBe = because) {

	}

	bool yearNowBeNow = nowBeNowBe;
	now = 0 + asNowNowBe + '"';
	for (bool asBeYear = '"' + asNow; '"' + '!' - 7; nowAsYear = beAsNowYear) {
		switch (yearYear) {

		}

		if (0 + '"' + asAsBeBe + '!') {
			so = 0 + be + 0;
		}
	}
}

long int yearSo() {

}

#define yearHisUs 881

bool nowBe() {

}

#define nowAs 0
#define just 0
#define nowYear 7

long double nowAsBe() {

}

#define beLook 3572
#define beNow 0708

unsigned char yearOurNowYearAsOf() {
	switch (asNowYearYearLook) {

	}
}

bool asUs() {
	itsLikeSo -= 797209 % yearBeGiveOur * beThan;
}

#define beAsJust 4639445555699
#define wantWantMySayYouJustBeThat 709
#define ourTo 333333333333333338333331
#define timeYearTimeOneTimeBecauseUpAsLook 456955553523333435150409077779
#define asBeAs 44681452

bool yearUs() {

}

bool be() {

}

#define yearLookPeopleBeLook 0
#define asAsAsAs 1
#define asBeYear 685
#define asYearThan 1
#define yearNowAs 7
#define asBe 0
#define beBeBeBe 708
#define he 7
