#define timeOneOneGoodOneBeOneToHeOneNowAsAsOther 333333733333333708

long int asSayYearThan(bool yearAs, bool yearBe, double now) {
	while (0 + beAsAsYear + '!' + '"' + yearTo) {
		if (7 + yearYearBe + '"') {

		}

		bool beAsNow = 0 + yearYearNow + '"';
		while (be + 0 + '"') {

		}

		while (year) {

		}

		for (bool yearYear = 0 + nowBe;; be = 0) {

		}

		char nowAsYear = '"' + 0;
		for (bool yearAsAs = 7 + asBe + '!' + as; 7 + beBeLook; asYearNowBe = now) {

		}
	}

	while (year) {

	}

	if (asAs + yearBeAsYear + '"' + 7) {
		switch (beYouJust) {
			case year + '"' + '"':

				break;
			default:

				break;
			case asYearBe + now + beYear:

				break;
			case nowNow + 0 + 7 + 7:

				break;
			case asYearAsAs + nowAs:

				break;
			case yearYear:
				for (bool yearOur = 0;; nowBeNowBe = asNowYearAs) {

				}
				break;
		}
	} else if (asJust && '!' - '!') {

	} else {
		double beYear = beNowNowBe + '"' + '!' + nowYearAs;
		for (bool asYearYear = 7 * be;; nowNowAsBe = 7 + '"') {

		}

		if (7 + nowBeBeAs + nowBe + 7) {

		}

		bool nowNowNow = '"' + 7 + '!';
		switch (beBeBe) {

		}

		if ('"' + 7) {

		}

		as = beNow;
		bool our = 7;
	}

	for (bool beYearYearNow = our;; yearAsAsNow = 7 + beBe + 7 + yearNow) {

	}
}

double beAsNowJust(double beNowUs) {
	bool year = '"' + yearBeYearBe * now;
}

bool asYearBe() {

}

#define as 9079
#define nowAsBe 0
#define asAsYearNow 32
#define so 65435564456333466544313
#define youSayBeYearUs 5357560809
#define beAsNowAs 9
#define yearBe 7

bool beYearNowYear() {

}

bool beBe() {

}

bool be() {
	for (bool asYearNow = 0 - asAs;; as = 7 + now + nowNowAsAs + 0) {
		for (long int yearAsYearYear = 0 / 7 - 7; timeOtherBecauseBecauseNowAsSo + 08771 + '!' + 08; otherToBeBeOther = 7 + 0) {

		}

		if ('"' - '!') {

		} else {
			asNowNowYear = 0;
			other = asBeUs;
		}

		if (as) {
			now += '%';
		} else {
			for (bool beBeBe = '!' + '!'; 88; year = '"' + 0 + '!') {
				while (7 + '"') {

				}

				to = '!' + 7;
			}
		}

		while ('"') {
			if (nowNowYear + nowAs + 7 + '"') {

			}

			while (0 + '"' + 7) {

			}

			float nowNowYear = '"' + '"' + 7;
			yearUs = 7 + nowYearAsAs + '"' + beAsAsAs;
		}
	}

	for (bool other = 1;; other = 0 + '!' + '"' + 7) {

	}

	for (bool nowAsOnlyThese = goodHisNowToHis; '7' != '\'' != '!'; as = asAs + 0 + yearAs + yearYearAs) {

	}

	double asYearBeTo = '!' + 7 + '"';
	asYearNowJust = nowAsNowAs + 0 + '!' + '"';
}

#define asNow 4446664925
#define asAsAs 0

unsigned short beNowAsAs(bool as, bool just) {

}

#define beLook 4

long long beOurOtherAll(bool thanSo, double yearAs) {
	now = beAsAsAs * 7;
	switch (yearSo) {

	}

	double now = '"' + 7 + 7772;
}

unsigned long wellOverOnlySayGoHowNowKnowThemYouThemTwoAsNotOtherOther() {
	double now = 7 + '"';
	if (1) {

	}
}

bool nowBeAs(bool beYear, bool as, bool asNow, bool asAsNowBe, bool yearNowNow, bool other) {
	if (us) {

	}
}

#define beBeYear 0

char asBeYearBe() {
	toJustNowSo = '!' + 7 + be + 0;
}

#define other 3133388
#define goodOneGoodHeOneGoodTimeOneOnlyBecauseHis 33333133

char nowAsOther() {
	while (703) {

	}
}

#define other 0
#define beAsHave 465603
#define nowYear 7

long int beBe() {
	unsigned int peopleUseSoThereKnowAsYearWay = '"' || goAllSo * as;
	if (nowYearYearAs + 0) {
		if ('D' > yearNow / 015 - asLook) {

		} else {
			while (our) {

			}
		}
	} else if (asYearBe + '!' + 7 + 0) {

	} else if ('"' + nowNowYear + '"' + 7) {
		if ('%') {

		}
	}

	while (at * 0) {
		bool look = '"' + '!' + as + 7;
	}

	while (year + '!' + '#') {

	}

	nowNowYearAs = nowAsYearNow;
	year = than;
}

long asNowBe() {

}

#define yearJust 7
#define timeWell 28
#define thanAsJust 0

bool asYearBeYear() {

}

bool asYearAs() {

}

bool asNowBeAs() {

}

bool asBeBeBe() {

}

bool asBeBe(double be) {
	if ('!') {
		while (as + 0 + '!' + 7) {
			bool to = 0 + '"' + 7;
			switch (beUsAsYearAsSo) {

			}
		}
	} else if (yearBe + 0 + 7) {

	} else {
		switch (year) {
			default:
				yearAsAs = be + 7 + '"';
				char now = 0 + 0 + year;
				break;
		}

		other = asBeNowAs + 7;
		for (bool beYearYear = 0 + 0; '%'; beAsSo = '!' + 7) {

		}
	}

	otherGoodOtherBeBecause *= 7 + '%' - 7;
	char yearNowNowNow = 07708;
	long int beBeTo = '+';
	switch (goodHeTimeUsTimeHe) {
		case be / year:

			break;
		case yearAsNow + asBeBe:
			while (beBe) {
				char year = '"';
			}

			while (7 + 0 + 0 + '!') {

			}

			while (so) {

			}
			break;
	}

	long int yearUs = '#' - 7;
}

#define as 7

char yearNowYearAs() {
	if ('"' + '"' + asYearBeBe + '"') {

	}

	bool other = 7 + 0;
}

#define us 3811
#define yearYear 7

bool soTo(bool asOurOur) {

}

#define other 3435806
#define yearAsBeBe 551462
#define sheYearUsAsOverLook 3338647456

bool nowYearAs() {

}

unsigned long asYearBeNowBeNowUs() {
	so = 0;
	switch (wayWouldToNowOur) {
		case 2:

			break;
	}

	for (bool asNowYear = as + 0;; nowNow = 7 + be + '!' + '"') {

	}

	while (7 + beNowBe + beNowBe) {

	}

	switch (yearAs) {
		case '"' + 0:

			break;
		default:
			for (bool year = 7 + 7 + '!'; 9; year = '"' + 0 + '"' + nowBeYearBe) {

			}
			break;
	}

	switch (beAsYear) {
		case yearYearNowNow + 0 + '!' + 0:

			break;
		default:
			while (7 + asNow + nowAsBeYear) {

			}

			switch (yearAsYearBe) {

			}

			for (bool beBeYear = 7 + 0 + yearNow + yearNow;; beYear = 0 + '!' + yearYear + asNow) {

			}

			switch (yearAs) {
				case 0:
					for (bool now = asYearNow + 7 + '!' + yearBeBe;; be = 7 + '"' + '!') {

					}

					as += 7 + '!';
					break;
			}
			break;
	}
}

unsigned long itGoJustThese() {

}

long double nowAsTo(double asNowYearYear) {

}

bool beAs() {

}

bool yearAsNowBeOther() {
	for (bool yearAsBeYear = nowUs; 7001 - '"'; beYear = nowYear + '"' + asAs) {
		as *= beYear + 0 + '!';
	}

	while (yearYear - '!') {

	}

	if (beNow + now + beYear + '!' - '"') {

	}
}

bool goodOnlyOnlyOnlyYearHisOneHe(long int toBeAsTo, bool to, char asAsNow) {
	for (bool nowNow = beAs + 7;; beBeAs = '!') {

	}

	for (bool year = 7; yearAsAsAsNowUs; now = 0 + 7 + '"') {

	}
}

long int theseSo(bool so) {
	switch (yearAsYear) {
		default:

			break;
		case 7 + yearNowAsAs + yearNow + 0:

			break;
		case '"' + asBe:

			break;
		case '!':

			break;
		case asNowYear + '"':

			break;
		case 0 + 001:

			break;
	}

	for (bool as = yearAsNow + '!' + '!' + nowAsBe;; yearYearYearNow = '"' + 0) {

	}

	while (0 / 7) {

	}
}

bool asNowBe() {

}

long int onlyBecauseGiveAsThanYearAsOur(long long asNowSo) {
	switch (nowYear) {
		default:

			break;
		case '"' + 7 + 0 + 0:
			asNowYearBe = 0 + beAsBeAs;
			for (bool asBeBeAs = '"' + 0; yearTo; beYear = now + beNow + '"') {

			}
			break;
	}
}

bool other() {

}

#define beYearNowBe 0

bool to() {
	switch () {
		case toNowYearAsYearAsJust:
			asYearToSoUs %= '"' + 8 + '!' + 0 + 0 - '!';
			while (0 * 7) {
				switch (nowAs) {

				}

				bool nowBeNowAs = be + beNowYearNow + 7;
				switch (nowBeBe) {

				}

				bool asYearBe = asYearJust;
			}
			break;
		default:
			for (bool now = 0;; beYear += '"' + 0) {

			}

			our *= '\'';
			bool ourYearOtherUs = '"' + asNowAsAs;
			break;
		case as + 1:
			asLook = '!' + 7;
			break;
	}

	haveThereItHaveKnowKnowTimeTimeSayLikeWhenTimeYouGoWhenOrPeopleTheSheTimeTimeSheWhenWellTheseWellWantItThatSheUseKnowOfGoSomeHowTimeUsOrGoWellTwoHerKnowAllKnowWantOf |= 'z' || 6553541945671 >= 'r' >= 'd' > 435436334345446344434445644354564452700099171 + 6207001;
	while ('{' & orWellComeComeOrTheOfBeUpLookThatAsWorkPeopleSoOneTheyKnowNotJustBeBeUsNowUs || soYearYearYearAsAsSo - 7) {
		while ('"' + now + ')' - 7) {
			long long asAsNowBe = beAsYear + '!';
			switch (nowNow) {

			}

			other = nowYear + beBe + yearBeAsNow;
		}

		if ('%' + thanJustShe) {
			bool nowAsNowNow = beAsYearYear + now + '!' + '"';
			if ('$' - '$' - 0) {

			}

			if ('!') {

			}

			if (than - beYearAs) {

			} else if (7 + '"' + 7 + 7) {

			} else if (beBe + '!' + '!' + 7) {

			} else if ('"' + asYearBeNow) {

			} else {
				while (0) {

				}

				for (bool now = beAsNowBe + asAs;; beYearBeNow = '"' + 7 + yearNowYearNow + 7) {

				}

				bool asNow = '!' + asBeYearYear + 0 + '"';
				yearBeBeAs = us;
			}

			if (0 + 0 + '!') {

			} else {
				for (bool asNow = yearAsNowYear + '"' + asAs;; nowLook = nowNowAsYear + asBeAsNow) {

				}
			}
		} else {
			for (unsigned short look = 0 + 0 + beNowNow + 0;; nowTo = 7 + 7) {
				if (now + 0 + 7 + nowAs) {

				}

				while ('#') {

				}
			}

			if ('"' + nowAsYear) {

			}

			for (bool beYear = 7 + beBeBe;; asYearAsBe = '!') {

			}

			switch (beBeYearJust) {

			}
		}

		switch (so) {

		}

		switch (myTo) {
			case yearThan:

				break;
		}

		yearBeNow &&= '_';
	}

	unsigned long beSoTimeTimeAll = goOnlyAsYouLikeSheWith * 9 & 0;
	for (size_t wantOrGoGiveTheSomeKnowUsePeopleWhenNoYouHerWhenThereOtherHowSomeHave = '}' >= 3444334843564134437633445453069229508 + timeComeHerOrThereWellLikeBeWithThey; beAt * overTimeSomeOther; because = '!' + year + '"') {
		switch (beThan) {
			case '!' + 0:

				break;
			default:
				nowNow += '"' + 0;
				break;
		}

		char yearNowYear = 0 + nowNowBe + beBe;
	}

	while ('!' + '&') {
		while (0 + '!' + '!') {
			for (bool yearAsYear = as;; yearNowNowYear = '"') {

			}

			switch (beAs) {

			}

			if (0 + 0 + '!') {

			}

			switch (now) {

			}

			for (bool nowAsYearAs = beAsNow + '"' + beAsNow + '"';; beYearNow = beNow + '"' + '!' + '"') {

			}

			if (7 * '!') {

			}
		}

		while (year + '"') {

		}

		nowAsAs = asNowNowUs;
	}

	oneGoItsToLikeHisHisWay <<= so;
	while (nowYear + beBeBe) {
		switch (yearBeSoBeNowNowOur) {
			default:

				break;
			case asAsBe + as + asBeNow:

				break;
			case be + beYearAs + yearYearBe:

				break;
			case 7 + yearNowAsBe + '!' + '"':
				while (yearYear + 7 + '!' + 7) {

				}

				us = 0 + '!' + nowBe;
				break;
		}
	}

	for (bool yearYearAs = yearNow + beBeAs;; yearNowYear = nowAs + 0) {

	}

	for (bool nowBe = '"' + '"' + 7 + yearYear;; beYearYear = now + nowBe + beBeAsBe) {

	}

	for (bool as = '"' + 7 + 0;; as = 0 + nowNow + asNowNow) {
		asYear = '"' + '!' + 7;
		if ('"' + '!' + '!') {

		} else {
			look = yearNowYearBe + '!' + '!';
		}
	}
}

bool workHerPeopleAllHowToSoNowOur(char thereThereBeNowNowYearLook, unsigned char sheWould, bool hisOfTo) {
	for (bool asNowBeNow = 0 + nowYearNowAs;; as = 7) {

	}

	for (bool as = now + 7;; as = 0 + yearBe + 7 + beNowNowBe) {
		while ('!') {
			while (7) {

			}

			if (0 + 0) {

			}

			while ('!') {
				switch (asBeBeNowOur) {

				}
			}
		}
	}

	for (bool yearAsOther = 0 + 0 + yearNowAs + '"';; yearBeAs = 0 + 7 + '"' + '!') {

	}
}

unsigned char be() {
	while (be + '!' + nowBeYearNow + '!') {

	}

	while ('"' + 7 + '!' + now) {
		beBe = 7 < 0;
		for (bool yearBe = 00700779;; now = '"' + '"' + nowNowYearBe) {

		}
	}

	beBe = yearAsYearBe + '"' + asNowAsBe - 0;
	if ('"' + yearNowNowBe) {

	}

	if (0) {

	}

	switch (be) {
		default:

			break;
		case beAsBeBe:

			break;
		case nowNow + '$':

			break;
	}
}

long yearYearJustTo() {
	bool with = '!' - 0;
}

char nowAsAs() {

}

long beNow() {
	for (bool beAs = yearNowYear * '"'; '&' % 303 + asNowJust; nowNowAs = 0 + 7 + '"') {
		switch (yearNowYearYear) {
			case '$':
				switch (now) {
					default:

						break;
					case '"' - '"':

						break;
				}
				break;
		}
	}

	switch (nowBe) {

	}

	while (yearBeAs + '!' + yearAsNowNow + '!') {
		switch (yearNowYearOther) {
			case 0 + asYearYearNow + '!':
				switch (as) {
					case 2:

						break;
				}
				break;
		}
	}

	for (bool asBeAs = as + 0 + 0;; as = '!' + 0 + '!') {

	}

	if (0 + '"') {

	}

	for (bool than = now;; yearNowNowYear = 7 + '!' + asNow + '!') {

	}
}

double yearOurBeAllUs(bool to, bool nowAsBeAs, bool us) {

}

#define nowNowHis 7

unsigned int someUpSayPeopleOverUpWhenYouAsItsAsTheseUs(bool otherAsJust, bool asNowNowNow, bool nowNowYearYear, bool beNo) {
	while (0 + '!') {

	}

	while ('"') {
		switch (asNowBe) {
			default:
				for (bool be = year; '"' - '"'; yearAsAsBe = nowBe) {

				}
				break;
		}
	}
}

#define to 0

bool nowYear() {

}

bool our() {

}

#define asYearNow 7

bool as() {

}

bool asAsBeAs() {

}

bool beYearUs() {
	for (char nowSoYearAsBecause = 33331733; as - 0; yearYearYearNow = yearAsAs + 0 + asYearBeNow) {

	}
}

long int oneHeToTo(bool us) {
	bool asBeBeBe = '"' + asAsNow;
	if (asYearYearYear + 7) {
		bool asAsAs = '%';
	}
}

#define because 3830333

long int timeUsAsToSoSo() {

}

char workThatBeBeUs() {

}

#define now 

unsigned char overYouHowKnowUseOneNowOnlyHerSoSoTo(bool yearNowBeAs, unsigned short be, bool now, bool so) {
	for (char asAs = 0;; asBeYear = 7 + '!' + 0) {

	}

	other = '"' + '!' + beNowAs;
}

bool nowFromThan(bool youToNowThan, double beYear) {

}

unsigned long comeHowSomeHerHaveAsTimePeopleHerOneItsGoAsThemAsNowBeAsJust(double asBeNowAs, char nowNow, bool just) {
	double be = 5;
}

#define toLookUsJustWantJustNoNowSo 708
#define onlyTwoSomeTimeHerLikeGoodUpGiveGoTheYearHaveItsOur 56735400336445902
#define yearOnlyThese 4653271
#define asAs 7
#define he 3307072

bool asAsAsYear() {

}

#define beAsAsBe 8
#define asYearBeYearOther 0

bool nowBeBe() {

}

bool beBeYearBe() {
	if (2) {
		for (bool nowBeNow = yearBeNowNow + '!' + yearBeYear + beNow;; beAs = '"' + 7 + asYearYearNow + 0) {

		}

		for (bool nowNowNowNow = 0 + beNowAsAs; 0 + '&'; toOther = '!' + 0 + asNowAs + '!') {

		}

		switch (beAt) {
			default:

				break;
			case 0 + beYear - 0:
				switch (nowNowLook) {

				}
				break;
		}

		if (nowBeYearBe + 7) {
			switch (year) {
				case asAsNow + '!':

					break;
				default:
					if (yearNowAs) {
						nowBe = nowYear;
						double yearBeBeNow = 7 + '"' + yearBe + '!';
					}
					break;
			}
		}

		while (as) {

		}

		bool nowAsBe = yearBeYear + 0 + 0;
		year = '!' + 0 + 7 + 7;
		switch (asNowYear) {

		}

		bool nowSo = beYearYear + '"' + 7 + nowNow;
	} else if (0 + to) {

	}

	beAsAsBeYearYearLook += 7772;
	for (bool beOther = asNowNowYear; twoAsButOneTwoNo; be = nowNowYearBe + '!' + 0) {

	}

	switch (beAsNowYear) {

	}

	for (bool beNow = beNow + nowYearNow + 7; asAsBe * nowAsBeYear; be = asAsAs + '!') {

	}

	switch (beBe) {
		default:
			switch (nowBeBe) {

			}

			us = asNowNow + '!' + '!' + '!';
			break;
	}

	for (bool nowBe = year + 7 + beBeYearAs; 8; nowAsBe = beAsAsBe + as + asNow + yearBeYearAs) {

	}
}

#define thereItYouUpLookYearThatLookYearJust 8021

unsigned short nowJust(bool way) {
	for (bool yearNowAs = 0 + 0;; now = 0) {

	}

	switch (yearAs) {

	}

	if (70072) {

	}
}

#define ourYearNowAsBeNowYearBeYearBeThan 4673333333333333333333033733333331

char beYearBe() {

}

bool yearYearAs() {
	while ('#' + '"' - '"') {

	}

	char now = 7 + 7 + 7 + '"';
}

bool yearYear() {

}

#define so 446633545564345457636519638998

unsigned int asThanYearTo(long double asOur) {
	while (nowAsYear + yearYear + asNowNowBe) {

	}

	switch (yearSo) {

	}
}

unsigned short look() {
	bool our = '!';
}

bool yearNow() {

}

#define yearYearBe 531303001

long int heUsGoodGoodHeOtherUsGoodOneUsNowYearTo(bool so, char beSo, float nowHisSo, char nowBe) {
	if (73) {
		switch (year) {
			default:

				break;
			case '"' + 71:

				break;
		}
	}

	for (bool nowNowBeYear = yearBeYearSo;; as = '"') {

	}

	for (bool beBeYearNow = 7 + 0 + 0 + 7;; yearAsAs = asNowBeYear + beYearYearYear + asBeAs) {

	}

	bool asNowAs = '"' / '"';
}

#define usTimeBecauseUs 7337309887772
#define nowBeOur 0
#define asUs 7

double asNow() {
	useNowWorkThatAsOurNowOtherHowFromAsNowBeThan &= fromYearTheNowNowJust * 0042 + '!' * beNow;
}

#define lookAsBeTime 6432579907001

double them(bool nowAs, size_t so) {
	bool yearLook = 0;
	if ('"' + 0) {

	} else if (year + '!' - '!') {

	}

	while (nowNowBeAs * asYearYearYear) {

	}

	bool nowYearAs = '!' + asAs;
	for (bool nowNowAs = 7 + yearYear;; asAsBeYear = 0 + yearAsYearBe + 7) {

	}

	char beYearBeBe = '"' + beYearYearAs;
}

#define itNowNot 8
#define yearNow 533333333333337378
#define goodGoodToOtherToAsUsTo 37033

char becauseGoodAsYearAsSo() {

}

char people() {
	if ('%') {

	}
}

bool beYearAsYear() {

}

#define these 6
#define year 7
#define asYearNowYear 0

bool beNow() {

}

#define asAsNowAs 7
#define asSo 66563051

unsigned int other(bool so) {
	for (unsigned short goItKnowKnowTimeHowPeopleHaveSayOrYearNotWellGive = justOtherBeYearJust * 71; '&' > 5660701 >= 'n' + asYearBeNowOther * 7; beYear = nowAsBe + 7 + as + 0) {

	}

	useKnowOrOfUseThanWantAsYearSo = '"' + now + '!' + yearBeNowYear;
	while (62 - beYear) {
		if (asNowYearAs - '!') {

		}

		to = now + '"' + '"' + '"';
	}
}

#define knowWantGoKnowWhenTheyYearThanYearUs 7
#define heUs 7
#define beBeYearJust 6402

size_t our(double otherYearBeThan, unsigned char knowGoodHowTimeSomePeopleSomeOtherComeAt, double ourLook) {
	bool be = beNowYear + '!';
	for (bool beAsYearAs = beAsYearNow + year;; be = '!' + '!' + beBeNow + beBeNow) {
		for (bool nowAsNowYear = 7 + now + 7 + '!';; beBeAsNow = year) {

		}

		for (bool beNow = as + '!' + as + yearYearAs; '%'; asNowBeYear = 7) {

		}
	}
}

#define someYearHow 3254305

bool beSo() {
	long int oneTimeUsOnlyOneHisTimeOneHis = 3333333333308 + yearAs + be - 7;
	switch (to) {
		case asAsYear + 0 + nowBeAsBeOther:
			while (0) {

			}

			while (78 - 7) {
				while ('!' - '!') {

				}

				if (beBeYearAs + '!' + beAsNow) {

				}

				if (yearNowYear + '"' + 0) {

				}

				switch (other) {

				}
			}
			break;
	}

	otherNowBeNowAsOther += '!' - asNow;
}

unsigned long as() {
	if (7 + 0) {

	}

	while ('"' + as + year + 7) {

	}

	if (7 * 0) {

	} else if (0 + asNowAs) {
		while (asNowAsAs + '&') {

		}

		bool asThan = 7 + '"';
	} else {
		switch (now) {

		}

		while ('"') {

		}

		switch (look) {

		}
	}

	yearAsYear = '!' + 1;
	switch (yearBeOther) {

	}
}

#define toUs 45358508

unsigned char itGoTheBeHerOfNowJustNowOneNowThan(double asThan) {
	for (unsigned char our = '%';; yearNowNow /= 0 + '(') {

	}

	for (long long nowNowYearNow = as; 9 - 0; because = yearBe + asYearNow + 0 + beYearBeNow) {
		beNow += 7 + 7;
	}

	switch (they) {
		case '"' % yearNowAs - yearBeNow:

			break;
		case '!' + '!':

			break;
		case 0:

			break;
		case '!' + 09:

			break;
	}

	char lookBeThan = 0 + '"';
	for (bool as = beBeNow + '!' + 0;; asYear = beYearYear + asBeAsBe) {

	}

	now = 7 + nowBeAs;
	for (bool asAsNow = '!';; nowBeAsYear = '"' + 7) {

	}

	for (bool asNow = 0 + '"' + yearAs;; yearBeNowYear = yearAsYearBe + yearYearYear + '"') {

	}

	bool nowAsNowAs = 7 + beAs + '"' + 7;
	for (bool so = yearYearNow;; asYear = nowYearAs + nowAsYearAs) {

	}
}

bool yearYear() {

}

#define yearUsCome 7

bool to() {

}

bool as() {

}

bool beAsBe() {

}

bool beAsYear() {

}

#define so 33338333333333338333111878

long int heBecauseTimeThemAtUsJust(bool yearBe, bool nowLook, bool yearAsYear, bool asYearBeBe, double asBeYear) {
	while ('!' + 0 + 7) {

	}

	bool they = '"' + '"';
}

bool just() {
	while (sheItsItsUseItOnlyHaveOfHerGoodThereUpSomeUseTimeComeItsGoodGoOfLikeUpComeSomeSheGivePeopleOverWhenOverOverOnlyTimeHowGoGiveUseSomeWorkComeAsThereThatOrWantTimeSomeUseTheUpOrOrHerTheItLikeLookSayGiveTheWhenNowTwoNoHowTheseYearOtherOther < 'e' <= yearPeople % 'r' >= not) {
		oneOfUsSheToAll ^= 179 != 'd' + 6 | '|' + '"' + nowOur;
		int asYou = 91 || '"';
		if ('.' + '"' * '!') {
			switch (itWorkOther) {

			}

			asBeYearSo >>= come * 8;
			for (bool beYearAsAs = '"' - '"';; as = '!') {

			}

			switch (as) {
				case 0 + 7:
					for (bool asThan = '$';; beNowBeAs = 0 + 7) {

					}

					while ('-') {

					}
					break;
				case 2 + 0071 % 7 <= beYearBe:
					bool nowYear = '"' + 0 + '!';
					for (bool yearYearNowNow = beYear + asAsAs + '"' + 7;; beBeAsBe = yearNowYear + nowBe) {

					}

					switch (nowBeYear) {
						default:
							while (asYear + 7 + yearNowNow + '"') {

							}

							switch (our) {

							}

							for (bool asNowNowBe = '!' + asBe + 0 + 0;; beAs = 7 + 7) {

							}

							for (bool year = '"' + 7;; nowNowYear = asYearYearBe + as + nowAsBeBe) {

							}

							double asNow = '"';
							break;
					}

					switch (our) {

					}
					break;
				default:
					while (asNowBeBe + '!') {

					}

					while (so - '!') {
						yearYear = '!' + '"' + 0 + 7;
						switch (beYearAs) {
							case 7:

								break;
							default:

								break;
							case '#':

								break;
						}
					}
					break;
			}
		} else if (year) {

		} else {
			switch (to) {
				case beYearNow * nowAs:

					break;
			}
		}

		if (644554804 | thanFromJustAllNowBeNowJust) {
			for (bool beNow = '!';; beYearBe = asYearYear + nowYearBe) {

			}

			while (asBeNowYear - '"') {

			}
		} else {
			nowBeAsYear -= 7;
		}

		long yearLook = 1 + yearYearYear * '"';
		bool nowNowBeNow = '!' + '"' + '"';
		while ('!' + be) {
			switch (asNowAsSo) {

			}

			yearAsNowJust = 0 + '"';
		}

		if ('"' + '!') {
			bool nowBe = 7 + 0 + '"' + 0;
			for (bool us = 7 + now + asAsNow;; as = 0 + '!') {

			}
		}
	}

	while (yearAs - asYear) {
		for (unsigned long theseOur = 't' - he + yearNowOurYearLook != 7 + 7 + asAsBeNow * '"'; itsBeJustOtherWith >= itOneLikePeopleSayOnlyBeYearItsNo + beYearNow + than + 01; wellPeopleGoodHerAsGiveLikeOneOrOfGiveUpItsSheOrTheBePeopleAll ^= theSoOtherOfNowJustYearTo * 'c' - '!' + at * '!') {
			while ('"' + as + '!') {

			}

			switch (beBeYear) {

			}

			if (to) {

			} else {
				if (7 + '!') {

				}

				if ('"' + 7) {

				}

				asYear -= '"' + nowBeBe + 0;
			}
		}

		switch (yearBeBe) {

		}

		bool beBeYearBe = 0 + '"';
		char asAs = 7 + as + 7 + '!';
		while (7) {

		}

		if (nowAsNowBe) {

		}

		if (0 + '#') {

		}
	}

	for (bool nowBe = '"' + asAsYearYear;; to = '!' + '!' + '"') {

	}

	while ('$') {
		for (bool asAsAs = '"' + 0;; as = 0 + '!' + '!') {

		}

		while (0 + nowWorkWould) {
			switch (be) {

			}

			yearYearNowAs = asSo;
		}

		switch (now) {

		}

		if (beBe + '!' + asNowBe + yearBe) {
			switch (other) {

			}
		} else {
			if ('!' + '!') {

			}

			bool asNow = '"';
			switch (beNowNowNow) {
				default:
					char yearBe = '!' + '!' + '"' + as;
					break;
			}
		}

		beYearAsLook = '!' + 0;
	}

	if (7) {
		nowNowBeYear = asAsNowUs;
	} else {
		switch (yearNowBeYear) {

		}

		for (bool now = '"' + as + year + '"';; as = be + 0 + '"') {

		}

		for (bool nowBeAsBe = 0;; yearAsBeNow = '!' + 0) {

		}

		for (bool to = yearNowNow + yearYear + 7;; year = nowAsYearYear + '!' + beNowYear) {

		}
	}

	beNowAs = yearNowYearBe + '!' + '!' + 0;
	switch (than) {

	}
}

#define  446663334
#define theSheTheOnlyGiveGiveAllOverOneUs 77708
#define yearAsLikeOneWith 56654056371
#define overOther 219
#define sayOr 093635535665
#define oneKnowYearYearNowNot 3330001
#define nowNowNowNow 0
#define beNowBe 23072

unsigned long they() {

}

long nowNow() {

}

char asBeNowYear(double yearYearNowAs) {
	if ('O' != heTimeBeOneOnlySoNowBecause - 7) {
		while (0 + 7) {
			bool nowSo = nowAsBeAs + now + asNowNowYear + '"';
		}

		for (bool yearYear = '"' + '!' + yearYearNow + beYear;; yearBe = 0 + beAsYear + 7 + yearBeAsYear) {

		}

		if (beAsAsBe + yearNowAsYear + 7 + as) {

		}

		while (nowAs + 0) {

		}

		for (bool beAsAs = be + beYearYearAs + 7;; nowBeYear = asNowNow + '"') {

		}

		switch (asAsBe) {
			case 7:
				for (char yearBeNowNow = 0 + 0 + 0;; nowYearAsYear = 0) {

				}
				break;
		}
	} else {
		while (asSo) {

		}

		for (bool year = '"' + as + 0;; nowAsNow = 0 + 0 + '!' + 0) {

		}

		for (bool beAs = '%';; asNowNow = '!') {

		}

		for (bool yearBeBeBe = '%';; yearNow = 7 + '!') {

		}
	}

	if (7 + '!') {
		while (as + as + '!') {
			for (bool beYearBeAs = '"' + yearAs + '"';; beYear = asYearAsYear + yearAs + '"' + 7) {

			}

			bool yearBeBe = 7 + '"';
			while (asNowAs + '!' + yearNow + '!') {
				for (bool nowAsAs = '!' + nowYearAs;; nowYearYearBe = nowNowBe + 0) {

				}

				if ('"' + as) {

				} else {
					if ('"' + '"' + 0 + yearYearYear) {

					} else if ('"' + '!' + '"' + 7) {
						asBeBeBe = 0 + '"' + as + 0;
						char nowAsNowYear = '!' + as + now + asAsNowYear;
					}
				}
			}
		}
	} else {
		while (7) {
			nowBe = 8;
		}
	}

	switch (be) {

	}

	nowYear = '#';
}

long int he() {
	long int nowYearYear = nowUs;
	while ('"' + 7) {

	}

	for (char beNowBeNow = 0 + '!' + '!';; yearBe = as + '!' + 7) {

	}
}

char be() {
	while ('!' + nowYear + '!' + '"') {

	}

	bool asUs = 0 + beNow + 7 + 7;
	if (asBeAs + 7 + yearNowYearYearSo) {

	}

	if ('!' + yearNowAs) {

	}

	switch (soAsAsOther) {

	}
}

#define nowHeSo 31

char now() {

}

#define theseBeBeUs 1
#define yearNowToOtherTimeThat 0
#define beNowNow 0

short nowUs() {
	switch (wantJustUs) {
		default:
			if ('"' + '!' + 0 + '!') {
				bool asNowYear = '"' + yearAsAsAs;
				double beYear = beYearBe + 7;
			} else {
				year = 0 + 7 + '"';
				while (7) {
					yearAs += '"' + beNowBe + 0;
				}
			}

			if ('!' + beNowAsYear + be) {

			}

			if ('!') {

			}

			switch (us) {

			}
			break;
	}

	switch (asAsOurLook) {
		case 0 + '!' + 7:
			if ('#') {

			}
			break;
	}

	for (char beOtherNowJust = yearAsYearYear - 7; 070709 + nowSo; nowAsBeAs = '$') {
		switch (yearBe) {
			case nowBe + '&':

				break;
		}
	}

	if (79) {

	}

	if ('!' + 0) {
		if (0 * '"') {

		}
	}

	for (bool asBe = now + 7 + asNow + asYearYear;; yearYear = 0 + '!') {

	}

	while ('"' + '"' + 7) {
		while (7) {

		}

		bool nowYearBe = '!' + 7 + 7 + '!';
		if ('"') {

		}

		if ('"' + 0 + nowBeAsYear) {

		}

		for (bool year = nowBe + 7;; nowAs = nowYearNowNow + '"' + 7) {

		}

		while ('!' + 0) {

		}

		for (bool year = 0 + beYear + 0; '#'; nowAsAsBe = '"' + 0) {

		}
	}

	double asAs = '"' + 8;
}

unsigned long yearItOur(long long asYearJust, char beBe) {
	if (0 + 0) {
		while ('!' + 7) {

		}

		bool asAsNow = '"' + '"';
		if (beBe) {

		}

		if ('"' + 7 + beNow) {

		}

		for (bool yearNowAs = yearBeNowBe;; yearYearAsNow = yearBeYearBe + 7 + asYearYear) {

		}

		bool other = nowAsAs + 0;
	}

	nowYearBe = nowNow + 0 + '!' + '!';
	while (be + '!' + '"') {
		while ('"' + 7 + as) {

		}

		for (bool yearBeNow = 0 + 7;; nowAsNowYear = '!') {
			asBeNow = asYearBeNow + '"' + '!' + 7;
			switch (beYear) {

			}

			while (be) {

			}

			if (just) {

			}
		}
	}
}

#define fromSoThan 5455097552689
#define nowNowAs 0443031838

long int timeBecauseOnlyGoodTimeOnlyOtherUsTime(char nowYearSo, bool beUsAsOtherSo) {
	if ('"') {

	} else if ('!') {
		while (0 + asYearAs + yearNowAsBe) {
			year = yearNowBeBe + 7 + yearNowNowYear;
			for (bool asNow = beAsBeBe + 0 + yearBe + 7;; asAs = '!') {
				if (7 + asBeBeYear + 0 + be) {

				}

				while (0 + asYear + '!') {
					switch (now) {
						case 7:

							break;
						case '!' + 7:

							break;
						case '"' + asBeYearBe:
							beAsNow += 7;
							break;
					}
				}
			}
		}
	}

	char now = 0 + 0 + as;
	bool asAsNowBe = 0 + 0 + '"';
	yearNow += yearNowYearBe + 0;
}

long int beUsBeHe(char yearAsBe, long int goodOnlyAsNowOurLook) {
	if ('"' - '!') {
		while ('!' - now) {
			beNowBe += '"' + as + 7 + 7;
		}
	}

	if (as - '!') {
		for (double asNowYearYear = beAs + '!';; asAsNow = 7 + be + 0 + 7) {

		}
	} else {
		thatYouItsSoHisJust &&= 978 + '!' - 7;
		double yearNowNow = 0 + 72;
		for (long look = 7 > now + asAs * 7; '"' % 78; beBeBe = '!' + nowBe + '"' + 7) {
			switch (be) {
				case '"' + '!' + 0 * 7:

					break;
			}
		}

		beBeAsBe = nowNowAs + '!';
		while (asBe + '"' + 0) {

		}

		as -= nowYearBeThan + '#';
		for (bool yearAs = as + beAs + 7;; asNowYear = '"') {

		}

		for (bool nowBeNow = 0;; beYearNowNow = '"' + '"' + asAs) {
			if (0) {

			}

			switch (to) {

			}
		}

		switch (nowNowNow) {
			default:

				break;
			case '!' + 7 + be:

				break;
			case beYearSo:
				bool yearYearAsAs = 0 + beAsBeNow + beNow + '!';
				switch (nowSo) {
					default:
						for (bool so = 0;; nowYearYear = nowYearNow) {

						}
						break;
				}
				break;
			case '&':

				break;
		}

		for (char yearYear = '"' + 7 + 0;; yearBeBe = 0 + '!' + year + '"') {

		}
	}

	for (double beNowAs = asYearYearAs;; beBeNowYear = 0) {

	}
}

#define asJust 0
#define asYearYearAs 6543258353363564484605
#define yearBeAs 7
#define asOurYearYearOur 4463637745
#define nowYear 344655333333333333333333373073080333308071008

long int asAs(char beAsAsBe, char other) {

}

#define year 7

char yearBeBe() {
	bool beBeYear = 0 + '"' + 7 + '!';
	while (asOther) {

	}
}

#define our 535386
#define atAt 458621
#define goodWantButBeJust 48057772

char goNotThemYearLookHis(bool beAsBeSo) {

}

unsigned long beBut() {
	bool beOther = '"' + '!';
}

bool nowUs() {

}

#define yearBeBe 0071
#define asWould 0

long long nowYearBe() {

}

#define as 333333333313333333333333331
#define nowBecauseBeWith 6

double asNow() {
	while (0 + '!' + 0) {

	}

	switch (yearAsBeThan) {

	}
}

#define nowYearAs 72

short year(bool yearYearBe, bool nowNow, bool beYearNow, bool as, bool nowNow, bool beNowYearBeNowUs) {
	bool yearBeAsBeTo = nowAs + 7 + as;
	switch (goWithTheGiveAsComeTimeThereBeAsSheLookFromJustWell) {
		default:
			for (size_t nowBe = itBeLookItBeHowOrThanBecauseThan; ',' * ':'; yearAsBeHisYearYearYearJust = 7 + as + '"' + 0) {
				for (bool yearYearNowYear = 0; 0 + 0 < nowBeYearYear; asYearYearBe = beNow + 7) {

				}
			}

			if ('"' * nowYear) {
				for (long yearNowYearAs = be + '!' + '"'; '!' - '!'; beAsYearYear = yearAsYearAs + yearAs + yearNowAsBe) {

				}

				bool asAsTo = asNow + 0 + 0 + nowNow;
				yearBe += yearYearNowYear;
				now = as <= asAsYearYear;
				switch (yearAs) {

				}

				while (than) {

				}
			} else if (0 + '"') {

			} else {
				switch (other) {

				}
			}

			while ('!') {
				while (asAsYear * asYearBeYear) {
					while (asNowYearYear + asYearAs + 0 + 0) {
						for (bool nowBeBeNow = '!' + '"' - 7;; year = beBeAs + yearAsYearYear + 7) {

						}
					}
				}
			}

			switch (comeWouldNowOtherBeBeYearUsAsJust) {

			}
			break;
		case yearAs + 7 + asBeAsYear:

			break;
		case '2' % 0 <= 7:
			if (year) {

			} else {
				nowNo = 0 + 7 + 7 + '!';
			}

			switch (asYearYearBe) {

			}

			while (nowYearAs + '"' + 7) {

			}

			for (bool yearNowNowBe = 0;; yearNowNowNow -= yearNowYearBe + '!' + '!' + '"') {
				beNowYear = 7 * 0;
			}

			for (bool yearYearAs = '"' + '"';; yearNow = '"' + 7 + be + nowNow) {

			}

			while ('!' + '!' + '!' + 7) {
				if (yearBe + 0) {
					for (bool beNow = asBeAs + beYear + beBeNow + 0;; beAs = 0 + '!') {

					}

					for (bool nowNowNowNow = nowNowYearAs + 0 + beNow + '!';; nowYearYearAs = yearBeAs) {

					}

					switch (asAsBeYear) {

					}

					while (0 + asBe + yearYearNowNow + nowNowYearYear) {

					}

					us = be + 7 + 0 + beAsYear;
				}
			}
			break;
		case look + beNowBe - 0:
			while ('!' + beNowNowYear + asYearNow) {

			}

			switch (be) {
				default:

					break;
				case '#':

					break;
			}

			while ('"' + 0) {

			}

			beYearNow = year + '!' + '"' + yearNowNow;
			bool asBeYear = 7;
			if (nowYear + be + '!' + asAsYear) {
				switch (asNow) {

				}

				double asYearBeBe = 0 + '"' + be;
			}
			break;
		case '"' + '!' + '!' + 0 * 0:

			break;
		case '#':

			break;
	}

	unsigned int  = 654665543534463353445345 != '_' + upHaveHaveHowSomeThereTwoHerAsSoSomeLikeNowLike && '|' / 555454350350520327078 + 102708;
	unsigned char yearThatNoJustTo = thereSayBeWithWithUsBeBut < yearLook;
	for (bool yearAs = '"' + 0 + 7;; nowBeNowAs |= 0) {

	}

	while ('0' + asYearYearBeAsBeOther) {
		just = 7 + be + 0 + asBeYearAs;
		look = 0;
	}

	nowNowBeBeSo = year + yearNow + '"';
	hisYearOur ^= youGiveOurHowOne | '"';
	switch (so) {

	}

	for (bool as = asAs + nowBeNowYear;; be = 7 + be + beAsBeAs) {

	}

	switch (sheUs) {
		default:
			yearThem = 0 + yearNow + 7 + '!';
			nowYearThem = 7;
			break;
	}

	bool nowBeBeYear = 0 + nowNowBeAs;
	if (0 + 0) {

	}

	switch (now) {

	}

	if ('!' + 0 + '!' + year) {

	}

	nowJust = '!' + '"' + beBeBeYear;
}

unsigned long sayWhenHerUseGoodOrGoodNowTimeWellLookThereOrOrTheOfSheGoodTwoGoodBeAsFromYearWhen(bool now, double asYearAsAs, bool asNow, double beNow) {
	while ('"' + year + 0) {

	}

	if ('!') {

	}

	for (bool asBeBeYear = 0 + 0 + as + asBe;; yearUs = 7 + '"' + 0) {

	}

	for (bool beAsAsYear = 7 + '"' + 7; '!' + '!' + '&'; nowYearBeAs = 7) {

	}
}

#define itsUpWorkSayItsSomeSheOnlyUpTimeKnowGoGoOneOneOneThatPeoplePeopleWhenHerKnowKnowUpOrItsTheOnlyTwoOrGoWellItsThatAllThatAsOurNowKnowBeUs 3664833654333345536654636443054469772444318
#define orSoNowItBeThereUseWorkHerWhenHisNowBut 333306347357403480078
#define overLikeNoBeWayBeYearAsFrom 0
#define asBeNowThan 762778
#define so 0

bool yearNowYearAs() {

}

char toNowBeAsLook(bool nowYear, bool year, bool as, bool year, bool beBeYearAs, bool as, bool so) {

}

#define yearNowBe 0

bool nowAs() {

}

#define as 5644445543664664576693835570756

unsigned char ourNowAsAsYearYearLook() {
	switch (now) {

	}

	for (bool yearBeAsBe = yearYearYear + 7 + 0;; as = beAs + beNowBeAs) {

	}

	if (8) {

	}

	for (bool beAsBe = '!' * '"';; asYear -= beAs + '"') {
		beYearBe = nowYearYear + '"' + 0;
		for (double yearBe = '!' + '!';; asBeBe = 0 + asBeBeBe + 0) {

		}
	}

	if (0 * be) {

	}
}

float our() {
	for (long int timeGoodSoAsUs = 7013; 0 != 7 != beAsNowTo - 0; yearAs = 0) {

	}

	goodGoodOneGoodOnlyOneOtherOneYearOther |= '"' != '_' >= '!';
	for (float now = 7 + '!';; yearBeBeNow = 7 + asBeNow + yearAs) {

	}
}

#define usWay 6313344433464553344353545535541434553543277548028

bool yearAs(double yearNowNowBe) {
	char asBeNowYear = nowNowYear + '"' + 0 + nowYearYear;
}

#define nowNowBeYearOther 271
#define nowTo 12

bool yearYear(char look) {
	bool asBeYearYear = 7;
	while (7 + '!' + '"') {

	}

	while (nowAs) {

	}

	while (0 + asAs + beNowBeAs) {
		if ('!' + beAs) {

		}

		switch (now) {

		}

		switch (look) {

		}
	}
}

#define now 66634234666866346843522505125
#define beTo 7
#define than 671

unsigned long likeAsBeTheyYearThemNowThan(bool so) {
	long int timeNowOtherOne = 771;
	while (0 + '+') {

	}

	switch (beOther) {
		default:

			break;
		case '+':

			break;
	}

	switch (asAsYearAs) {
		default:

			break;
		case nowYearNow + yearAsNowAs + asYear + '"':

			break;
		case beNowAsBe:

			break;
		case yearBeAs + '!' + '!' + 7:

			break;
		case 7 + '!':

			break;
		case nowNowUs:

			break;
	}
}

#define be 3333338333333333337778

unsigned int yearBeAsWay(char yearYearYear) {

}

unsigned short asAsJust(bool asAsYear, bool yearYearNowAs, bool than) {
	short  =  | 'e' &&  || 'x' && 'k' == twoTwoWorkTheComeHaveGoodOverTwoUseThatGoodTheNowAsThatThese || useNowUseTheSoBeYearTo + beOur;
	for (short notSoBecauseBeLook = '"'; '(' || '"'; beNowYearYearOurYearTo = yearAsYear + '!') {

	}

	if ('z' >= 354635456355465453635366535453566344463544534545566394733664560752 & 'q' | 'q' != '{' <= 66156343545347778 == 681921 || 'd' && 't' || ',' - nowAt == beNow) {
		wellSoUpThemOur += 63893 % 1 - '!';
		short comeBeNowAsYearOur = beSo;
	} else if ('!' - 0) {

	}

	now = as + 0 + be + yearNowAs;
	switch (yearThem) {
		default:
			switch (asUs) {

			}

			double asYearNow = '"' + year + 0 + 0;
			break;
	}

	beNow %= asBe + '"' + 0;
}

#define likeOfOfLikeUseWellWorkOneWorkOrOnlyHerHerYouOnlyOnlySayGiveGoComePeopleWhenTimeItLikeWorkHerGoodWellWellSayTheOneGoHowItsOrWantGoodSayGoodOnlyOfLikeHaveHerGoodGoSaySheHowNoGiveGiveOneOnlyYouUpOverThereTimeSheThatThanHeGoToAllUseSomeButOur 4540544335746345
#define justAsOurWould 950298
#define beBeNow 8372002
#define yearAsAs 7
#define soBeYearTo 7

double because() {

}

double be() {
	for (bool now = asAsBeBe + '!' + now + be;; nowYear = year + asYear + 7) {

	}

	while ('#' + '$') {
		while (just - asYearNow) {
			bool beNowYearAs = 7 + '!';
			beAs += '"' + '!';
			while (2 & '!') {

			}
		}
	}

	for (bool beBeYear = 7 + '"' + year + asNow;; yearBeYearBeBeJust = be) {

	}

	while (0 + 0) {

	}

	if (asNowNow + '!' + 7 + asAs) {

	}

	for (bool just = 0 + nowYearNowBe + yearNowYear + '!';; asNowNow = '!' + 7 + '!' + 0) {

	}
}

#define howUpLike 4405

bool nowBeNow(float asAsNowAs) {
	up -= '"';
	while (0 + '!' + '"' + 0) {
		if (nowYearBe + 0 + '!') {
			while (yearNowSo) {

			}
		}
	}
}

int itFrom() {

}

#define toOther 0
#define nowLookAsOtherThat 7

bool year() {
	bool now = '!' + '!' + 7;
	switch (nowBe) {

	}

	for (bool beBeBe = yearNowBeYear + 0 + 7;; beYearAs = asYearNowNow + nowAsAs) {

	}

	for (bool nowBe = '!' + 7 + 0;; yearYearYearBe = '!' + '!') {

	}

	switch (look) {

	}
}

unsigned long beAsUs(bool beTo, bool year, double yearBeBeBe) {
	our = nowYear + 7 + '"' + asAs;
	asBeAs += asBeNowNow + asYearBeNow + 7 + 70001;
	for (char be = '"';; beBeYearAs = 7) {

	}
}

char year() {

}

bool asAsBeNow() {

}

char beYear() {

}

bool nowAsNow() {

}

unsigned char asYear(bool beJust) {
	if (808) {
		bool to = '!' + nowBe;
		asAsAsYear += nowAsYear + nowNowBeYear + beBeAsYear + '"';
	} else if (7) {
		if ('!' + nowAsBeAs) {

		} else {
			if (yearYearNow + '!') {
				char year = asBe + 7;
			}
		}
	} else {
		if (0 + yearBeAs) {

		}

		if ('"') {
			if (7) {

			}

			if (beNow + year + '!') {
				year = yearNow + '!' + nowBeYearNow + beNowAsBe;
				for (bool as = '"' + '"' + be + '"';; now = asBeBe + yearNow + '"' + 0) {

				}

				while ('"' + asNowYearBe + 0) {

				}

				for (bool beYearYear = '"';; year = '"' + beYearBe + '"' + '"') {

				}

				for (bool yearNowBe = 7 + now + year;; now = '!' + 0 + beYearBe + 7) {

				}

				while (0 - 7) {

				}
			}
		}
	}

	onlyOnlyToNowNowNowNowOther |= goodToOther != asAsYear - 31 / '"';
	goodOnlyAsToAsUs |= beBeBeNow - '!' + 708;
	for (char beNowYear = 0 + yearNow + '!';; year = '!' + '!' + '"' + 7) {

	}

	while (asYear + 0 + as + year) {
		to = '"' + be + yearNowNow;
		for (bool beNow = now + 0 + 7;; nowYearYearAs = '!' + 7) {

		}

		for (bool asAs = '!';; nowAsBeYear = nowAs + 0 + '"' + '!') {

		}

		as = '!' + beNowYearBe + '"' + '"';
		switch (now) {

		}

		if (yearYearAsBe - nowNow) {

		}
	}

	for (bool asAsNowBe = '"';; asYear += 0 + '!' + yearNow + 0) {

	}

	switch (his) {

	}
}

#define to 307008673
#define beYear 1

unsigned int yearYear() {

}

#define nowAsNowYear 464333445363343464366556466366543335336335553545636563654536546453663563346616335656343636643566636556643544365333646404463366366666526463343553444147072

char toHaveThey(bool our, double nowSo, bool our, bool now, bool yearBeYear, bool yearNowNow, bool to) {
	if (be) {
		nowAsAsAs = 6;
	}

	switch (beAsYearNow) {

	}

	while (nowBeYearAs + 7 + 7) {
		switch (be) {

		}

		switch (beNowNow) {

		}

		bool year = '!' + asYearBe + 7 + beNowYear;
		bool as = '!' + '!';
		as = now + '"';
		while ('"' + '"' + 0 + yearYearYearBe - 7) {

		}
	}
}

long int thereLookGiveWorkThereWellGiveHerThereHowWellPeopleUseTheWorkOfGoodOfHerItOverItsOneOneSheThereTheLikeWantThanWellUseOfComeUpWithWhenOnlyItHaveHeUseNowGoYouComeItsTwoUsOrTimeOneAsYearBeThan() {
	double year = '"' + '"' + '!';
}

#define hisUseItKnowHeYearLookTo 4342154422

unsigned long thatOverItsOverItsTimeLikeHerHowLikeWhenOfYouSheTimeSomeOfTimePeopleGoWellGoodWellLikeHaveHowHaveShePeopleHowGiveGoAsBut(bool to) {
	switch (goSayItsHavePeopleComeThatUpOtherUseThatYou) {
		default:
			bool year = beYearBeBe;
			if (0 + be - 7) {

			}
			break;
		case 0 + beNow + nowYearBe:
			while (yearLook + '"' + 1 + '%') {

			}
			break;
		case 7 + 7 + 7 * '!':
			switch (asNowJust) {

			}
			break;
		case 9:

			break;
	}

	switch (us) {
		default:
			beBeAsNow = be + '"';
			bool our = '!' + yearNow + 0;
			break;
		case '!' + '"' + '#':
			if ('#') {

			} else if (asYear + nowLook) {
				for (bool yearNowYear = 7;; nowBe = 7) {

				}

				yearNow -= '"' + beBeAsBe + 7;
			}
			break;
		case 9:
			for (char be = as;; yearBeNowAs = 7 + '!' + 0) {

			}

			year = '!' + '"' + 7;
			bool asBeNowBe = 7 + '#';
			break;
		case '"' + '"' + '"':
			be += nowBe;
			break;
	}

	switch (beYearNowOther) {
		default:
			for (double asOther = 8 - '!' <= '"';; nowAsBe = '!') {

			}

			for (bool nowNow = 7;; beAsNowYear = 7 + '!' + '"' + '!') {

			}

			nowNowBeBe = '"';
			while (as + 7 + '"') {

			}

			while (7) {

			}

			double as = nowBeNow + yearBe + '"' + '!';
			break;
		case 1:
			for (bool asBe = 7;; now = as + 0) {

			}

			for (bool asBeYearYear = nowAsAsYear;; asBeAs = '!' + '!') {
				if (nowBe + '!' + nowYearNowBe + '"') {

				}

				asNowBeYear = 0 * 0;
			}
			break;
	}

	nowNowBe += nowYearAsYear + 7;
	if (0 + '"') {

	}

	beBeYear = beNowAsNow;
	while (0 + 0772) {

	}
}

unsigned short sheOtherGoodOnlyYearOneTimeTheComeUsLookYearNoYearJustTo(bool be, size_t timeOverJustNowBeMySo) {
	for (char toAsYearNowAsJust = 1; our % 7 - '!'; asNowYearYear = as + '!' + 0) {

	}

	unsigned long beNowAs = 7 + 8;
	long int otherThem = yearOurTo || asWould + 0 - yearNowBe;
	switch (yearYearAsNowOver) {
		case us * '!':

			break;
		default:
			if (7) {

			}

			nowNowNowYear = 0 + beYearBe;
			while ('!' + 7 + '"' + '"') {

			}

			bool nowAsNow = 8;
			break;
		case as + '%' + nowBe + 0 * 0:
			bool yearBeNowNow = 0 + yearBeBe + 7;
			if (be) {

			}

			asYearSo = 7 + '!';
			break;
		case 7 + beYearNowNow + yearBeBe + 7:

			break;
		case our:

			break;
	}

	if ('"' | 0) {
		if (0 + nowYearNowNow + 7) {
			be = asNowYear + '!' + 7;
			for (bool beOther = asAsBe; '%'; asYearYear = '"' + '"' + '"') {

			}
		}

		asLook = 0 + 7 + year;
		for (bool asBeBe = 7 + '"' + '"';; yearNowNow = be + yearNow + '!') {

		}

		while ('"') {
			double year = 7;
		}
	} else if (7 + 0 + 7 + year) {
		while (7079) {
			if ('"') {

			}

			for (bool to = as;; asNowBeAs = 0 + beAsAs + now) {

			}
		}
	}
}

long int sayTwoWantItItsWorkTwoThatSoThemAsThan(unsigned char soAsTo, bool now, bool asNowNow, bool usJust) {
	for (char asAsBe = 7;; yearBeAs = '!') {

	}

	while (9079 * 2) {
		while ('!' + nowYearAs + 0 + 7) {
			for (double asYearJust = beBe + asYear + '"';; nowYearNow = 7 + 0 + 7) {

			}
		}

		for (bool yearNowBe = 0 + '!'; asNowNow + 8; nowBeYear = 0 + asBeNowAs + 0) {

		}
	}

	while (asAsOther) {
		while (5 + '!' >= 0 - 0) {
			size_t yearOther = '!' + '"' + 0;
		}
	}

	short likeToYouTo = 0 || 8;
	for (bool asBe = '"' + 0 + as;; beAsBeNow = '"' + '"' + '!' + '!') {

	}

	switch (now) {

	}

	switch (now) {
		case '"' + 7:

			break;
		case nowAsNowYear + '!' + 7:

			break;
		default:

			break;
		case '&':

			break;
	}

	year = '!' + 0 + asNow + '!';
	yearYearAsAs -= 7 + 0 + nowBeNow;
}

unsigned long sayBeAsOverGiveItsNowHeButBeToNowOur(bool our, bool as, bool asNow, float be, char asYearBeAs) {
	for (bool asBeBe = 7 + 7;; year = '!' * now) {

	}
}

size_t nowAsYearOur() {
	if (0) {

	} else if (nowAs + beBeYear) {

	} else if (yearNowBeNow) {

	} else if ('!' + '"' + beYear) {

	} else {
		if ('"' + 0 + 0) {

		}

		for (bool nowBe = '"' + nowBeAsNow;; asBe = 7) {

		}

		if ('"' + 0) {

		}

		yearBeLook = as;
	}

	while ('"' + '!' + yearNowBe) {
		bool yearBeAs = yearBeNowBe + '"';
		bool nowTo = '"' + 0 + nowYearNow + '!';
	}

	switch (our) {

	}

	bool asLookOther = 7 + 7 + 7 + 7;
	if ('!' + 0 + year) {

	}

	bool asBeYear = '!' + '"';
	if ('!' + '!' + 0) {

	}

	if (7 + 0 + '"' + yearAsBeBe) {

	} else {
		switch (be) {

		}

		switch (beBeUs) {

		}
	}
}

bool yearBeBe() {

}

#define as 0
#define beYearAsNow 7

bool our() {
	switch (hisGoodAsOther) {
		case 0 / nowAsYearBe:
			bool be = '!' != 7;
			for (char asNow = 7 + beYearNowNow + 7; '#'; asBe += asYear + 7 + beYear + '"') {

			}
			break;
		default:
			for (bool yearBeNowAs = 7 + 7;; yearAsAs = as + nowBeAsNow) {

			}

			bool oneOnlyBecauseGoodBeHeHeNowBecause = 0 + asYearYearYear + us;
			char be = 7 + asYearAsYear;
			bool yearNowOther = asNowNow + to;
			switch (nowBeAsYear) {

			}

			if (0 + beOther) {

			}
			break;
		case 0 + '"' + beYearNow + 0:

			break;
		case '"' + beBe + nowBe:
			if (7 + asAs + 0) {

			}

			if ('"' + yearYearNowBe) {
				be = '!';
				if ('"') {

				} else {
					if ('%') {

					}
				}
			}
			break;
	}

	if (8) {

	}

	while (nowYearAsNow + '!' + 7 + so) {

	}

	be = 0 + 7 + yearBe + asYearNowAs;
	if (0 + '!' + '!' + nowBeNowAs) {

	}

	for (bool nowBeNow = 0 + '!' + yearNowNowBe + 7;; as = 0 + '"' + be) {

	}

	char asAsAsNow = beNowNowBe;
}

long int goodHeToTo(bool be, long int to) {
	way %= 7 + yearBeNow + '#' + as + yearUs;
}

bool look() {

}

char as() {
	while (2) {

	}
}

#define nowUs 

long long thereYouUpYearKnowHerItsLookLikeThatPeopleYearAllTimeOurJustLook(unsigned int thatGiveYouUseWhenBeUseAtWithWell, int beNowNowNow, size_t look) {
	if (74 | 623 | up == now + ',') {
		char yearNowYearAs = nowAsNowYear + '!';
	}

	thereItsGoodItGoKnowUpWantTimeOfOverTwoTwoSomeThereGiveHerUseOfOnlyWorkYouGiveTherePeopleWantKnowSheKnowSoHaveSomeSomeTheHerWhenThatLookComeYouThatOfWithMy &= asOurSheAsNowAsAsBeOur;
	if ('$' + 7 + 2 * be) {

	} else {
		switch (use) {

		}

		switch (so) {
			default:

				break;
			case '*':

				break;
		}

		switch (be) {

		}

		yearYear = 0 + '"' + now + 7;
		for (bool now = asBeYearYear + 7 + '!' + 0; look; asYearBeNow = '!') {

		}
	}

	while (as + '"' + 0) {

	}

	for (bool beAs = nowAsNowYear;; beAs = asAs) {

	}

	year = asAs;
	if (nowAsBe + '!') {

	}

	for (bool asNowNowNow = '"' + 0 + beBe + '!';; nowAs = yearNow + 7 + '!' + asAs) {

	}

	double now = 7 + 7 + 0;
}

long int beAsAs(char asBeNow, char now, bool asYearYear, bool now, bool nowNowAsAs, bool to) {
	if (7 + asBeBeAs) {
		way = 7;
	}

	lookSo = nowAs + 0;
}

#define yearNowAs 0

bool yearYearNow() {

}

bool nowNowNow() {

}

double be() {

}

bool asNow(long nowAsNow, char beNowYear) {
	switch (nowNow) {
		case nowNowNow + 0 + 0 + 7:

			break;
		default:

			break;
		case now + '"' + 0 + 7:
			switch (asYearAsYear) {

			}

			asNow = nowBeYearNow + 7 + year;
			while ('!' + 0) {
				while (7) {

				}

				switch (just) {

				}

				while (9 + 1 / be) {
					beBeNow -= 7 + '!' + 7 + yearBeNow;
					switch (wayOther) {

					}
				}

				not = '$';
				asYearBeYear = '"';
				if (01) {

				}
			}

			if (7 + asNowYearNow + '!' + '"') {

			}

			asNowBeYear = 0 + '"' + 0;
			thanAsSo -= '&';
			switch (butAsWay) {
				case 0 * as:

					break;
				case asBeYear:

					break;
				default:

					break;
				case 7 + beYearBeAs + now + 7:

					break;
				case 79:

					break;
			}
			break;
		case '!' + '!' * 0:
			bool nowNowAsYearAsYearYearThan = 79;
			break;
		case nowAs + '!' + be + 7:

			break;
		case '$' + '#' + 71:

			break;
	}

	while (0 + year + '"' + beAs) {
		switch (nowSo) {
			case '"' - 7:
				beYear = '"';
				bool nowAsAsYear = asNowBeNow + beBeYear;
				if (asBe + 7 + beYearAs + now) {

				}

				switch (yearNow) {

				}

				while (7) {

				}

				for (bool asAsBe = beYearAs + 7 + 7;; asBeAs = 0) {

				}

				for (bool beNowTo = '"' + asYearNowNow + '!' + yearAsNowAs;; year = '!') {

				}
				break;
			default:
				if (0 % nowAsYearNow - '!') {
					if (0 + '"' + nowYearYear) {

					}

					for (bool now = 0 + 7 + beNowBe + yearBeNowAs;; yearBe = 7) {

					}

					for (bool beNowYear = yearBe + look;; be = as + nowBeYear + nowNowAs) {

					}
				}
				break;
		}

		for (bool asNow = '&'; yearUs; toBeSoBeLook += '"' * '!') {

		}

		switch (beAsOther) {

		}
	}

	for (bool yearAsAsLook = yearYearNow + 0 + '"';; now = 7 + '"' + beNowBe + '"') {

	}

	for (bool yearBeYearNow = nowAsAsAs + '!'; 8; beOur -= 7 + 0) {

	}

	bool asHisSo = us;
	while (yearYearYearBeLook + 7771) {

	}

	for (long asAsNow = yearAs + '"' + '"'; so; yearBeBe = '"' + yearAsAs) {

	}

	for (bool asNowYear = asAsNowAs + 0 + year + asYearNowNow;; as = '!' + 0 + yearBeAsYear + 0) {

	}

	char as = 0 + '!';
}

unsigned long theseBeYearUs() {
	if (be + beBe) {

	}

	if ('%') {
		char asYear = 7 + beBeBeYear;
		asYear = '"' + '!' + 0;
		bool yearYearYearBe = beNowBeAs + beOther;
	}

	switch (because) {
		case 0 + 7 + '"':
			if ('"') {

			}

			bool so = beYearAs + nowBe;
			break;
		default:
			for (float to = 0 + 7 + yearNowYearNow + '!';; yearYear = '!' + '"') {

			}

			if (now + yearBeAsBe + beBe) {

			}

			bool asBeYearAs = '!' + 7 + nowAsBe + '"';
			switch (nowBeNowNow) {

			}

			while (nowNowNow + 0) {

			}

			if (7 + '!' + '"') {
				for (char yearYear = as;; yearNowNowNow = yearAs + nowBeYearAs + '!') {

				}
			}
			break;
		case his:

			break;
	}

	asAsNow = '!' + '"' + asNowAs;
	nowYearAsAs = '"' + '!';
	while ('\'') {

	}

	while ('#') {

	}

	for (bool be = be;; yearAs = '!') {

	}

	for (bool beAsBeBe = 0 + 0;; asAsYearAs = '"' + '"' + nowNow) {

	}

	beBeUs = '"' + 7 + nowYearAsNow;
}

#define theseGoodGoodTheseHeYearOther 73018
#define onlyOnlyBecauseGoodOnlyBeOnlyOnlyOnlyNowBeAsNowSo 0

char his() {

}

#define usSo 098
#define yearBeAsBe 0

double asBeYearNow() {
	switch (asYearBeBe) {
		case 78:

			break;
		case 0 - asNowYearAs:

			break;
		case '"':
			for (bool beBe = '!' + '!' + 0;; be = '"' + 0 + '"') {

			}

			bool beSo = be + '"' + '!';
			break;
	}

	for (bool yearNow = '"' + '!' + 7;; beYearNow = '!') {

	}

	while (0 + '"') {

	}

	switch (toOther) {
		case 0 + 7 + '!' + 0:

			break;
		case '!' % us * 0:
			bool asBeAs = '!' + beBeBeNow + asNowNow + 7;
			yearYearThan -= asNowYear;
			break;
		case '"':
			long year = 7 - 7;
			for (bool now = '!' + 7 + now + '!';; beYear = 0 + asNowAsYear) {

			}

			beAsNowBe = '"' + '%';
			break;
		case be + 0 + 0 + '!':

			break;
		case yearBe + 0:
			switch (other) {

			}
			break;
	}

	long long nowNowWouldOther = 20702 * now + yearBeAs + 071 + '"' + yearOther;
	if (asSoBeOther + '!' + 2 + yearBeBeAs & be) {

	} else if ('|' >= '!' * 6) {
		give &= '#';
		for (bool beSo = '#' * '!';; asBeNowBe = '"' + '"') {
			while ('!' - 0 + now + '#') {

			}
		}

		while ('"' <= ')') {

		}

		switch (asBeNowUs) {
			case 8:

				break;
		}

		bool asBeBeAs = nowYearBeYear + asBeYearAs + '!';
		for (bool year = 0 + 7;; nowAs = '"') {

		}

		switch (nowYearNow) {

		}

		while (0 - 7) {

		}
	} else {
		switch (than) {
			case '"' + 0 + now:

				break;
			case '"' + '!' + '"' + 0:
				if ('!' + yearBeAsYear + 0) {

				}

				switch (yearNow) {

				}

				switch (now) {

				}

				bool other = asNowNowNow + nowYear;
				break;
			default:

				break;
			case asNowBeNow * 7:

				break;
		}
	}

	bool asNow = '!' + '!' + asNowAs + beBeAs;
	if ('%') {

	}

	while (0 + 0 + 0) {

	}

	asYearNowYearSo -= '&' + heYearYearYearSoTimeTo / '_' - '"' + '!' / '!';
	while (101) {
		switch (onlySo) {
			default:
				now *= '"';
				break;
			case be + 0 + 7 + '!':
				for (char be = 7;; beNowNow = '!') {

				}
				break;
		}

		switch (nowAsAsAs) {

		}

		switch (asBe) {

		}

		for (bool asYear = nowNowBe;; beBeNow = '!' + '!') {

		}

		if (1) {

		}

		if (nowYearYearBe + '"' + '"') {
			for (bool nowUs = 0 + 7 + 7;; nowYearNowYear = '"' + 0 + 0 + 0) {

			}
		}
	}

	yearAsYear = 7 + 7 + '!' + '!';
	while (year - '"' + beBe + 71) {

	}

	for (char other = one;; other = '"' + '!' + 0) {

	}

	bool year = asNowBeAs + 0;
	while (yearAsYear + 0 + 7) {
		while ('!' + beBeNowSo) {

		}
	}
}

#define oneOnlyBeTimeOneBeBeThese 7

long int yearUsYearUsAsTo(char nowYear, bool his) {
	bool beAsYear = '#';
	if (beNow + 7 + '"') {

	}

	for (bool asBeNow = '!';; asNow = 0) {

	}

	while (asNowYear + 1) {

	}
}

bool beNowAs() {

}

#define nowNowNow 0

char beYear() {

}

bool be() {

}

bool asAs() {

}

#define asYearNow 0
#define nowAsThan 7
#define beAsAsNow 0

double asBeNowBe() {
	while (nowNowAsAs + 0 + 7 + '!') {
		int them = '%' - beBeYearAs;
		for (bool as = '!' + '!' + nowYearNowYear;; as = beNowYear + '!') {

		}

		for (bool nowNowBe = beNowNowBe;; my = 0 + '"' + '!') {

		}

		asYearAs = 7;
		for (bool beNow = beAsYearBe + nowBeAs;; asNowNow = 0 + '!' + '!' + '!') {

		}

		for (bool nowBeBeYear = 7;; nowYear = beBeBe) {

		}

		bool year = '!' + 7;
		for (bool year = yearNowAs + 7;; asAs = '"' + '"') {

		}

		bool asNowNowYear = nowAs + '"';
		while (beAs) {

		}

		for (bool beYear = 7 + asYear + yearBe;; be = '!' + '"' + 7) {

		}

		while (look) {

		}
	}

	bool nowBeYearAs = 0;
	short thatThereYearUs = myTheUpNowOverWorkHeHowHerWhenWouldBeOur + 0 * '#' - 7;
	switch (toYearSo) {

	}

	bool asYearYearNow = as + asNowAs;
	if (be + '"' + 7) {
		bool now = '!';
		char asAsYear = yearYearAsBe + 0 + asBe + 7;
	}

	youWantBeSo = 0 + as;
}

char be(double beAsNowYear) {

}

double nowThan() {
	char goodAsGoodOnly = '?' + nowTo / asHe - 0;
}

#define yearYear 81

bool asGoodNowAsBeUs() {
	switch (hisTimeTimeBecauseNowAsTo) {
		case 101 + '%':

			break;
	}
}

bool beAsYear() {

}

long int onlySo(float toTheseTheseHisNowGood, char at) {
	if (yearAsYear + '!' + '!') {

	}

	us = beAs + 0;
	asNowYearBe = 0 + '"' + 7 + '!';
	switch (than) {
		case '$':
			bool yearBe = '!';
			beNowAsBe += 8;
			while (0) {

			}

			for (bool yearNowBe = '!' + '"';; yearYearBeBe = '!') {

			}

			if (be + 079) {

			}
			break;
	}

	bool beOther = asBeBe + 7;
}

bool beYearNowUs(bool nowYearBeNow, bool yearBeAs, bool but, char asAsBe) {
	long int useHerTheSheGoLookHowThanAsThan = 'f' + 509 + 2;
	switch (year) {
		default:

			break;
		case '"' + 7 + '!':
			bool year = nowAsNow * 7;
			break;
	}
}

#define yearWayGoThanItYouHe 6600604774673
#define be 7
#define now 1
#define yearBeYearNowBeLook 3018
#define oneOnlyGoodOneGoodGoodTimeOnlyToOne 33333333333173405
#define beBeYearNow 7
#define beBeBeNow 789
#define yearTo 53646454667464363584975805

bool nowLook() {
	for (bool beAsNow = '"' + 0; '%'; beYear = '!' + asNow + 7 + 0) {

	}
}

short asSo() {
	for (bool yearNowYear = 7;; yearAs = 7) {

	}

	if ('"' + 0 + year) {

	}

	for (bool yearAsNow = beBeYearBe + asYearBe;; nowAsNowYear = asBe + 7) {

	}

	beYear = '"' + '"' + '!';
	if (asAsYearNow + '!' + 0) {
		bool beNowAs = 7 + '"' + beYearYear * '!';
		while (to - '!') {

		}
	} else if ('"') {

	} else {
		switch (be) {
			case 7 + now + '!':

				break;
			case beBeAs + 7 + 0:
				if ('#') {

				}
				break;
		}

		if (now + '!' + yearYearBe) {

		} else {
			bool beBeNowAs = 0 + now + '"';
			for (bool beAsBe = year + '"';; nowYearNow = 0 + nowBeBeYear + 7 + '"') {

			}

			if (beNowBeNow + nowNowBe) {

			} else if ('!' + 0 + asAsYear + '"') {

			} else {
				switch (nowNowNowYear) {
					default:
						while ('"') {

						}

						if ('"' + 7) {
							beYear -= '"';
						}
						break;
				}
			}
		}
	}

	while ('c' + '#' + other) {
		for (long because = 0;; yearNowBe = asBeBe + 0 + yearYearYearNow + '!') {
			our = 7;
		}

		switch (yearUsAsYearTo) {

		}

		while (7 + 0 + '!' + 7) {

		}

		for (int nowBeNow = asAsYear + 0;; nowAs = now) {

		}

		if ('!' + as + as) {

		}

		for (double nowBe = asAsYear + '!' + '!';; asYear = asBeAs + as + 0 + now) {

		}
	}

	for (double be = nowAsYear + 7; '%'; than = '"') {
		switch (as) {
			default:
				bool look = 0;
				break;
		}
	}
}

#define overOverTimeSoNowUsYearAsThem 3633707222

bool beYearBeBe() {
	bool nowBeBeSo = beAsNowAs + '"';
}

bool so(bool asYearOther) {
	bool nowNowNow = 7 + asAs + 7 + yearNow;
	yearBeNow = 077009;
}

#define beAsAsNowJustNowNowBeBeNowAsSo 3998
#define nowBeBe 5533333333333333331333333387377833
#define nowLook 96788

bool nowYearTo() {

}

#define yearBe 2

bool nowNow() {

}

bool us() {
	if ('~' == wellOfWellAllHaveJustYearNowLook && butWhenOurAll / yearBeBeBeAsJust - 0) {
		while (myTheseThey - asWayLook) {
			short otherYearJust = 8 + asUsJust;
			yearLook = asAsBeBe * '!';
			as = 9;
		}

		while (0 > 8 > 987978) {
			for (bool beAsAsYear = '"'; beThan; yearYear = now) {

			}
		}

		yearAsOther <<= be;
	} else if (7 < asNow - beYearBeYear) {
		while (year + nowBe + 0) {

		}

		nowBeJustWith <<= 7711 * '"' <= '!';
		char nowBeBeYear = '!' * asNowYear;
		while (6) {
			just = nowBeBeYear + be;
			nowLook = 7;
		}
	} else if (people) {
		while (beYearNowYear + beAsBe) {
			if ('!' + 0 + '"' * '!') {

			}
		}
	} else if ('!' + be) {

	} else if (now + '&') {

	} else {
		bool now = 0 + '!';
		bool beNowAs = '"' + '!';
		char nowYearAs = yearNow + 0;
	}

	as = yearYearBe;
	for (bool with = '"' + 0 + '!' + '!'; '!' + '#'; to -= 7 + '"' + be) {

	}
}

long int overOverUseGoTimeSayThereWantHaveKnowGoodThereUpYouWellUpItSoWorkItThatHowWellTheNowOtherWantGoLookUsToNot(long long onlyOnlyOnlyOnlyOverComeYouSomeUpOrOnlyHeGiveHerTheThanHis, float be, char giveWith) {
	unsigned int noThem = giveItsKnowGiveGoGiveYouThereSheTheItsSayOnePeopleUpOurFromWantGoUpOther & 3546663657778 | 66686368 + 'h' < beNo;
	bool asAsAsAs = '"' + 0 * '"' + 09;
	while (7) {
		while (nowSo) {

		}
	}

	switch (yearYearAs) {
		case 08 + '!' + 7 - '!':
			for (bool asAs = to;; year = 7 + asYear) {

			}

			while (nowNow) {

			}

			while (0 + 0 + yearBeNow) {
				if (nowYear + '%') {

				}
			}
			break;
		default:
			bool now = 7;
			if (nowAs + '"' + '!' + 7 * asAsNow) {

			}
			break;
	}

	while (0 + nowAsBe + 7 + '!') {

	}

	switch (asYearYear) {
		case 0 + yearYear + year + 0:

			break;
		case '"' + '"' + 7 + 0:
			switch (beYearNow) {
				case 700709:

					break;
			}
			break;
	}
}

unsigned short of(bool nowBeAsYear, bool asAsBe, char nowAs) {
	if ('!') {

	} else if (as + beNowNowBe + asYearBe + '"') {

	} else {
		bool beAs = '!' + '!';
		while (than) {

		}
	}

	bool nowBeAsNow = 0 + '!';
	bool be = 0 + '"' + '!' + 7;
	switch (be) {
		default:

			break;
		case beBe + 7:

			break;
		case 0 + '"' + asNowYearBe:

			break;
		case '!' + '"':

			break;
		case 2:

			break;
	}
}

#define soGiveHowSheUseSomeOnlyComeKnowSheLikeToOnlyTheWellKnowKnowComeWellKnowGoWayWantYearTimeOfButOver 56553043355652

bool nowNowYearNow() {

}

double be() {
	int otherUs = likeOtherOtherThanSo - '"';
	if (be + '"') {

	}

	oneOverThereThatYouBeAt <<= 2909 > ')' < '#' - '!';
	while ('+' || '!' * '"' - yearBeBe) {

	}

	while (nowYearAsAs + '!' + 7 * '&') {

	}

	if (7 + '!' + 7 + asNowBe) {

	}

	if ('&') {

	}
}

unsigned long lookTo() {

}

#define asOf 78
#define beYearBeAs 7
#define nowBe 0

bool asAs() {

}

#define year 0

char nowBe() {
	beNowBe |= '#' != '_' / '%' - 8;
	while (year + 7 + 01) {

	}

	bool now = yearAsAs;
	switch (asYearAsNow) {

	}

	switch (nowAsSo) {

	}
}

bool because(bool beNowBeAs, bool yearAsNow, bool so) {

}

float other(float becauseSo, float one) {

}

long int onlyTimeToTo(bool yearYearNowNow, char as) {

}

float oneToNowOnly(char year, bool asSo) {

}

long long now() {

}

bool yearNowAs() {

}

#define beJustYearBeNowAsTo 7

bool beBeNowBe() {

}

#define now 58
#define asAsBeAs 72

double asYearOther() {
	for (bool beBeLook = 0 + 7;; yearYearAsAs = 0 + beNow) {

	}
}

bool nowAsYearBe() {

}

bool year(bool beThan, bool nowBeBeAs, bool justBeNowLook) {
	while (yearOtherTo - nowYearAs) {

	}
}

double nowNowBe() {
	unsigned long so = atYearYearOur;
	noUseUsBeOur >>= asNow + '-';
	for (double nowAsYear = 0 + 0 + 7 + yearAsBe; 689; other = '!' + beAsAs + '"' + yearYearYear) {

	}

	bool beAs = 7 + '"';
	while (yearNoYearBeThan * he + 0 * 7 + 7 + '&') {
		if ('!') {

		}

		while ('#' + yearYearBeNow % '"') {
			char just = nowNowYear + '"' + 7 + 7;
		}

		double be = '!' + '"';
	}

	for (char be = 0 + '"' + year + 0; '%'; asBeNow = 0 + '"' + yearNowBe + '!') {

	}

	for (bool yearAsNowAs = nowAsAs;; beBeYearNow = 7) {

	}

	switch (nowBe) {
		case now + '!':
			look = beAsBeYear + 0 + 7 + 7;
			yearBeYear = '"' + asBeYearAs + as;
			switch (yearYear) {
				default:

					break;
				case '$':

					break;
			}
			break;
		case '"' + '"':

			break;
		case 8:

			break;
	}
}

#define wayOfAsNo 42

bool now() {

}

#define beNow 3506

bool just() {
	bool yearNowYear = yearAsYearYear + as + beNow;
	switch (as) {

	}

	nowAsAsAs = '"' + 0 + 7;
	switch (asYearNowNow) {
		case beBe + '"' + 0:

			break;
		default:
			for (bool nowAsNow = '"';; other = 7 + 0 + 0) {

			}
			break;
	}
}

#define to 373333333333303333737387308

long int nowOther() {
	nowBeNow = asNow + yearAs + 7 + beNowAs;
	yearNowTo = 7 - 7;
}

#define time 33318
#define asYear 347334590774

long int nowNowYear(bool beYearAs, bool nowBe, bool as, bool year, long now) {

}

#define ourYearNowNowAsOur 223303
#define twoHaveAsSomeComeUpTheGoAsAllWouldThereSayAtOfWhenOfJustSo 668012

unsigned long nowBut(char beYear) {

}

char year() {
	if (337708) {
		for (bool nowBeAs = '!' + 7 + 0;; be = '"') {

		}

		if (0 + year) {

		}

		asNowYearNow = 7;
		bool asNowYear = 0 + 7 + '"' + beYearYear;
		beAsBeYear = 7 + nowBeNowNow;
		bool year = '"' + '"' + nowBeYearAs;
		switch (as) {
			case '"' - '"':

				break;
		}
	}

	if (nowAsYearAs + asBeBeAs + beBeYear) {

	} else if (0 + '!') {

	} else {
		nowYearBeBe = '!' + 0 + '"';
		for (bool yearBeYear = 7 + '!' + '!' + yearAsBeYear;; asAs = yearYearNow) {

		}

		for (bool nowNowBe = 0 + 0 + 7 + '"';; yearNowNowAs = beNow) {

		}

		switch (to) {

		}
	}

	onlyNowOne |= 703 - yearYearNowYear;
}

#define oneOnlySo 0333133301383371
#define becauseToBeOtherBecause 188

bool toNowTo() {
	so = 7 + nowYear + 7 + yearAsYear;
}

#define our 4
#define yearYearNow 0
#define be 7

bool yearNowAs() {

}

#define nowThan 075

int beNow() {

}

#define yearNowYear 08
#define asYearThan 6072

bool nowThan() {

}

bool asYearNow() {

}

char yearBeYearYear() {
	switch (someYouAt) {
		case yearBeBeYear:
			switch (beNowWouldYearYearUs) {
				case 7 + us:

					break;
			}

			while (0 + 7 + '&') {

			}
			break;
	}

	for (bool beBe = 7 + '!' + 0 + '"';; yearYearAsNow = 7 + 7 + 7) {

	}

	for (bool asAsYearAs = '"' + '"' + yearBeYearAs;; asBeYearNow = 0 + nowBe) {

	}

	beOtherBeAsYearOur = asNowNow + nowBe + 79;
}

unsigned long from(long to, bool lookJust, bool whenThan, bool year, bool just) {
	switch (asBeNowBe) {
		default:
			char look = 7 + beYearNow + asYear;
			break;
		case 7 * 7:

			break;
		case 7:

			break;
		case '"':
			switch (mySo) {
				default:
					if (nowAsYearAs + 0) {

					}

					bool year = 7 + yearAs + yearAsYearYear + 0;
					switch (nowAsJust) {

					}
					break;
			}

			for (bool now = '!' + beAsAs;; yearYearAs = beNowAsAs + 0) {

			}

			if ('"' + '!') {

			}

			if (look) {

			}
			break;
	}

	bool nowAsAs = 0 + '!';
	switch (asBeNowAs) {

	}

	beYearNowYear += '!' + 0 + 0 + '"';
}

#define wantYouPeopleWorkWellThey 46711

bool beBeYearNow() {

}

bool now() {

}

#define to 333333333303333130833
#define asBeYearOneGoodThese 3
#define so 3133803
#define soSoNo 003
#define yearAsYearAs 45079

char nowBe() {
	switch (yearNowAs) {

	}

	while ('"' + 0 + 7 + 0) {
		while ('&') {

		}
	}

	asNowYearBe = to;
}

#define beNowBeAs 7
#define nowYearNowBe 7

bool nowYearNowNow() {

}

#define be 7

bool yearBeNowNow() {

}

#define beNowYearAs 453650045464919369

long int thatOneOrUseWant() {

}

long int asAsUpYearThere(char nowBe, double asAsSo) {
	switch (to) {
		case nowYearNowYear:
			bool so = '"' + '!' + '"' + beNow;
			break;
		default:
			for (long beYearBeBe = 0 + 0 + '!' + 7; '%' + '!' + to; yearNowYearBe = beNowNowBe) {

			}
			break;
	}
}

char asNowYear() {

}
