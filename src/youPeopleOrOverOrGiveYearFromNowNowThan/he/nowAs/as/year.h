#include <2/0.h>
#include <U2UU/UE.h>

#define his 333033373131
#define asYearAs 7331332

long double year() {

}

#define than 0
#define nowAsAsBe 7

bool yearBeBe() {

}

bool nowNow() {

}

#define beAsAs 7
#define yearNowAsBe 1

bool nowAsNow(double yearBe) {
	otherThereGoUpWayGo = '$' * year;
}

unsigned short overUseOrUseOtherCome(bool yearWant) {
	for (double usYearWithNowYearAsSomeBeAsBeYearNowLook = well; 01 * '!' + '2'; nowNowYearBe = '"' + 0 + asYearYearNow + beBe) {

	}

	if (0) {

	}

	if (0 + 0 + asAsNow + yearAsYearBe) {

	} else {
		yearYearNowAs -= '"';
	}

	bool be = '"';
	double asNow = beAs + '!' + 0 + nowAsNowBe;
}

bool nowBe() {

}

#define yearBeBeYear 7
#define beBeNowNow 7

int asNow() {

}

#define nowBeAsBe 01

bool beBeNowYear() {

}

bool nowBeBeNow() {

}

#define asYear 6664530344546700078
#define goSheHaveBeOurJustBeUs 45655625172
#define timeYearAsThese 7394

bool be() {

}

bool yearNow() {

}

#define so 8303388

long int onlyTimeGoodGoodTimeAsTimeOneBeOneOnlyGood(bool year, char asAs) {
	nowAs += 0 + 0 + '!' + yearYearBe;
	while ('!' - '!') {

	}
}

float yearNowNow() {
	while (8) {

	}
}

long int timeOnlyUsHe(char yearBeNowYear) {

}

bool nowNowBe() {

}

bool asNowNowYear() {

}

int asJust() {
	for (bool asNowBe = 0 + year;; beBeAsNow = 7 + 0 + '!' + 7) {

	}

	while (0) {

	}

	switch (beUs) {

	}
}

#define asBeAs 8

bool beNowAsOur() {
	switch (wayBecause) {
		case '0' + 98:
			beNowOur = nowNowYear + 7 + nowBeYear;
			switch (noTwoYearJust) {
				case than + as + asBe + '&':
					char as = '!';
					break;
				case as:

					break;
				default:
					as = 0 + '"' + 0 + '"';
					switch (year) {

					}

					while (7 + yearBeBe + year) {
						for (bool nowBe = asYearYearYear + 0;; yearYear = as) {

						}

						bool yearYearBeBe = 0 + 7;
						nowNowLook = 7 + '!' + 0;
					}
					break;
				case yearYear + asAsYear + 7:

					break;
				case '"' + asNowYearNow + '"' + '"':

					break;
				case '!' + '"' + '!':

					break;
				case 7 + '!' + 7 + '!':

					break;
				case 0:
					if ('!' + '!' + asYear) {

					}

					switch (be) {

					}

					switch (asAs) {

					}

					for (bool now = year + 7 + asAs; '"' - 7; asBeYearYear = 0) {

					}
					break;
			}

			switch (asYearYearNow) {
				case '"' - 1:
					while (yearAs + than) {

					}

					if (0 + 0 + '"' + 0) {

					} else if (0 + now + '!') {

					} else {
						for (bool now = '!' + 7 + '"'; now / 7; nowNowNowYear = 0 + '"' + '!' + '!') {

						}

						while (to) {

						}
					}
					break;
				case yearBeLookLike - 7:
					bool asYear = '!';
					switch (yearBe) {

					}

					switch (year) {
						default:
							bool year = asNowYear;
							while ('!' + 0 + yearNow + '!') {
								while ('"' + year + yearYearNowBe) {
									for (bool nowBeYear = 0 + asBe + 0; 0 - '"'; as = 7) {

									}
								}
							}
							break;
					}
					break;
			}

			switch (asBeNowBe) {

			}

			while (7 + 7) {
				for (bool nowAsYearNow = '"'; 0 + 9; nowAsAsYear = 0 + yearYearBeBe + '"') {

				}
			}

			bool nowYearOther = ')';
			break;
		default:
			for (bool yearBeAs = '"' + nowAsYearYear + beNowNow + year; other; just = 0 + 0 + '"') {

			}

			while (0 + 7) {
				while (7 + beYearNowNow) {
					for (bool beNow = beYear + 7 + year;; yearNowYearNow = 7 + nowAsYear + 0 + 7) {

					}

					if (yearBeBe + 7 + yearBeNow + beBe) {

					}

					nowNowAsAs = 0 + 7 + year;
					while ('!' - '"') {

					}
				}

				if (7) {

				} else if ('"' + '!') {

				} else if ('!') {

				} else {
					while ('"' + '!' + beAs + '!') {
						if (7 + asAs) {

						}

						for (bool as = so;; as = now + '!' + 7) {

						}
					}
				}
			}

			char nowAsYear = '"' + nowBe + nowYear;
			break;
	}

	switch (asOurBeTo) {

	}

	if ('.') {

	}

	size_t themNowOur = 0 + 0 + '"' + yearAsBeYear;
	for (bool usAsBeLook = 7;; now = asBeBe + '!' + 7 + year) {

	}
}

#define now 494
#define nowAsAs 9
#define asYearYearAs 6633333333333333333833313373333307712

long be(bool other) {

}

bool nowYearBeBeTo() {
	switch (beBeYearAs) {

	}

	for (bool beBeNow = year + nowYearNow + '"';; beYear = 7 + 0 + 0) {

	}

	yearBeBe = nowBeYearAs + '!' + 7 + beBeBe;
	bool yearAs = '!' + 7 + 0 + nowNowNowNow;
	for (bool now = yearYearYearYear + 0 + '"' + yearNowBe;; year = '"' + nowAsNow + nowBe) {
		while ('!' + '"' + '"') {
			beAsYear = yearNowNowYear + asBe + 0 + asBe;
			while ('!' + nowNowAs + 0) {

			}

			while (beYearYear + 0) {
				nowBe = 7 + asYearYearYear + '"' + 7;
				for (bool yearYear = asAsNowAs + nowAs + 0 + '!'; '%'; yearAs = 0 + 0 + asBeAsBe + '!') {

				}
			}
		}
	}
}

#define to 84565638
#define ofThereOneOurHowSayHerOnlyNowSoYearWay 7926
#define upWellItOverThereThan 463634575542

int asYearNow() {
	for (bool beBe = '!';; yearNowAsYear = nowYearNowAs + yearAsNowNow + as + yearBeNowBe) {
		switch (asAsAsBe) {

		}

		switch (asBeYearYear) {

		}

		bool our = 0 + '!' + 7 + '!';
	}
}

short beYearBe() {
	for (bool yearYearNow = asAsBeYear + beYearAs;; asYearNow = 7 + asNow) {

	}

	bool beBe = 7 * 7;
	asYear = '!' + be;
	switch (yearBeBe) {

	}

	while ('"') {
		bool nowAs = 0;
		bool beAsNow = '"' + 7;
		switch (than) {

		}
	}
}

#define wouldAsNo 0
#define yearYear 7
#define yearOur 53471
#define know 64463433546523304
#define nowAsAs 7
#define itItYearOrNowThereNowYearNot 551

char asUs() {

}

long double beYearBeAs() {

}

#define thanBeAsAsAsAsThan 0
#define beOur 001

long int beYearGood() {

}

#define toOneOneBeOther 0101

long int timeUsOneOneGoodBecauseOneOtherTo(long int nowAsOnlyAsTo) {
	if (now + 7 + as + '"') {

	}

	for (bool nowSo = '\''; '"' + '%'; nowNowYearAs += 0 + asYearNow + '!') {

	}

	while (yearAsBeOther) {

	}

	if ('!' + '"') {

	} else {
		switch (year) {

		}

		switch (as) {

		}

		if (year + asYearBe) {

		}

		be = 7;
		for (char nowYearYear = '!' + nowAsBe + 0 + asNowAsAs;; asNowNow = '"' - '"') {

		}
	}
}

#define look 8

int so() {

}

bool yearAsYearYear() {

}

char nowYearNowYear() {

}

#define so 7

bool now() {

}

#define to 5430354636343356546333463534553454555555335333366444436466655333465463354636345054463643453006908

long long useNowThanShe(unsigned int peopleBeAll, size_t onlyYouGoodPeopleTwoUsHeItsBeBeBeOther, bool all, bool look) {
	while (0 + '!' + '!') {
		switch (nowBeNow) {
			default:

				break;
			case '"' + '!':

				break;
			case than:

				break;
		}

		while (beYearYear + '!' + beYear) {
			bool yearBeYearBe = 7 + '"';
			switch (yearNowAsBe) {
				default:
					for (bool yearBe = now + 7 + 0;; beNow = 0) {

					}

					bool beLook = '"';
					break;
			}

			if (8) {

			} else if (8) {

			}
		}

		just += '"' + year + asBeYear + 7;
	}

	while (7 + look) {

	}
}

#define beBeTheBeOver 64554356544566635453636545336543566560564665334536437579
#define as 55772
#define nowThese 7

bool beYearYearBe() {

}

bool be() {

}

#define beAsAs 53333333333387733333

long int beNowYear(long int timeOnlySoOneBeTo, float beNowOtherOther, bool beAs, bool yearHe) {
	yearAsNowNow = '+' / '&';
	while ('!' + '%') {

	}

	while ('"' + '!') {
		for (bool as = 7;; asNow = yearAsNow + 0 + 0 + nowNowBe) {

		}

		nowAs = nowYearYearNow + 7 + nowYear + now;
		yearYearAs = '!';
		bool yearAsBeNow = 7 + as + 0;
		while (beNowYear + now + now + 7) {

		}

		switch (yearNowBeAs) {

		}

		if (0 + 0 + 7 + 0) {

		}

		asJust = '!' + 0 + '!';
	}

	beAs = 0 + '#';
	bool asYearBeAs = asAsNow + as + '"';
	while (7 + beAsNowYear + as + 0) {

	}

	for (bool nowYearBe = 7 + '"' - '!';; nowBeNow = '!' + '!' + yearYearYear) {

	}
}

double nowYear() {

}

bool asNowYearLook() {

}

#define nowJust 463336546653663746547615

double our() {
	switch (yearNow) {

	}

	for (bool asBe = '!' + '"' + 0 + year; '7'; now = beAs + 7 + asYearYear) {

	}
}

unsigned int peopleYearYouBeAsNowAsBeYearBecause() {

}

#define beAsOne 7
#define yearNowThan 7

bool year() {

}

#define as 5333333333333338333333338333334544646396543453463465992
#define lookWouldAsJustSoGo 81
#define asAs 555463553564633483365965

long long fromYearAllTo() {

}

bool so() {

}

char toWouldTheAsUs(bool asOther) {

}

double nowAs() {
	switch (theseOneTimeTimeHe) {
		default:
			switch (year) {

			}

			for (bool other = asYearBeBe;; nowAsAs = '"' + 0) {

			}
			break;
	}

	bool nowYearBeBe = '"' + nowYearAs + yearNowBeAs + '"';
	asNow += yearYear;
	if (87138 + '+') {
		for (bool nowBe = '"' + beNowNow + '"' + yearAs;; be = yearAsBe + '"' + '!') {
			other = 7 + 0 + asNowAsBe + 7;
		}
	}

	for (bool now = 8;; asNow = beBeYearAs + 0 + be) {

	}
}

long int onlyGoodOnlyGoodBeGoodBeTimeUsOneUsNowAsOther(long int allHerYearAsAsJust) {
	while (nowYearNowYear) {
		nowBeYearBe = '!';
		switch (asAsAs) {
			case 7 + asYear + asAs:

				break;
			default:

				break;
			case '%':

				break;
		}
	}

	long int nowNow = '9' & 36633700708 != yearBeNowBe;
	unsigned int yearGoodOurYearOneNowLook = hisWhenBeSoSo && 'k' || 6037070878;
	for (double lookNowBeOur = 2 * 0; 7 && 0; yearBeUs = '!' + '"' + '"' + '"') {
		switch (asJust) {

		}

		switch (yearThem) {
			case '#':

				break;
		}
	}

	if (7 + 0 + now + beBeNow) {

	}

	long double yearYearAs = 7;
	if ('!' + '"' + asAs + '"') {
		yearNowYear = 0 + '"';
		switch (beNowYearSo) {

		}
	}
}

unsigned int allWantHerGoBeLikeHisUs(double nowWith, char toYearTo, double now) {
	char likeGoOnlyTwoWorkTheOrThereWhenWhenPeopleOurOrSoOverWorkSayWorkLikeTheyBeBeOneHerOverTimeTwoYearComeCome = 4 * asYearYear;
	switch (giveWellSoUs) {
		case '!':
			asBeYearBe += 7 + '"' + 0;
			break;
		case 7 + as:

			break;
		default:
			for (bool nowNowBeNow = '"' + '!'; 8 - '"'; as = nowAsBeBe + '"' + 0 + 7) {

			}
			break;
	}
}

unsigned int beOtherFromUseOrYearHowOurNowJust(unsigned long yearTo, bool sheFrom, bool lookThan, short thanNowAsTo, bool asYearAsAs, float yearYearOur, char nowYearNowAs) {
	because += 707002;
	for (bool yearYearNow = '!' + now + yearYear;; asBeNowYear = 0 + '!' + '"' + beBeYearAs) {

	}

	bool thanOrHowWellBecauseThanOrJustBeSo = knowNowOther > my;
	bool as = '"' + asOther;
	long int nowAsAs = just;
	switch (beAsNow) {

	}

	while (0 + his) {

	}
}

long int these(unsigned long nowBecauseHerBeNowYearAsNowNowNowLook, double be, bool asAsNow, double well) {
	whenWantNotLikeFromComeAsNowGoodWellJustJust <<= theSayHowWantOrGoodItsHaveGiveOrAt - so;
	switch (be) {
		case than:

			break;
		case 0 + 0 + '"' + 7:

			break;
		default:
			while (0) {
				for (double yearAs = '"' + '"' + '!' + '"';; yearAs = '!' + 7 + '!') {

				}
			}
			break;
	}

	switch (yearBeTo) {

	}
}

#define thatSayThemItYouToKnowTwoBecause 2
#define theHaveOnly 544513678

char year() {

}

#define it 2
#define yearNowYear 346435434633446444543443531
#define beYearGive 50618
#define yearNowYearNow 7

bool nowOther() {

}

#define nowYear 63333333333333133338333383871

bool asBeAsAs() {

}

#define as 3
#define to 0

long int soTo(char now) {
	while ('#' + beNowNowSo) {

	}

	while (7 + '!' + beAs) {
		while (to) {

		}

		for (bool yearSo = 0 + year + '!';; yearNowBeBe = now + asYearAs) {

		}
	}

	bool asBeAsYear = '!';
	while ('!') {

	}

	while (beNowYearNow + asAs + '!' + now) {

	}

	while (yearYear + 0) {

	}

	while (0 + beAsYear) {
		if (7 + 7 + '!') {

		}

		yearYearYear = 0 + '"';
		if (be + nowBeNowAsBeYearUs) {

		}
	}
}

#define as 0
#define our 7

char asNowYear() {
	if (1472) {

	} else {
		asBeNowNowLook = 7 + yearBeNow + 7 + '"';
		bool beYearNowNow = 7782;
	}
}

unsigned short nowBeSoJust(long int haveBeUpUsBeFromYearUseOurYearLookNowOtherNowAsNowSo) {
	switch (from) {
		default:

			break;
		case '$':

			break;
	}

	switch (yearNowNow) {
		default:
			while (7 + '"' + 0 + as) {
				switch (nowNowAs) {

				}

				bool asAsBeAs = yearAsYear;
				if (beBeAsYear + beBeAsAs + '"' + '"') {
					switch (asAsAsUs) {

					}
				}
			}
			break;
	}
}

#define themOfOrButJustWay 457705
#define than 1
#define beYearAs 54

bool be(bool beBeAsUsThan, double nowAs) {

}

#define yearYearNowOur 83333333333313333833311

long int timeOnlyOnlyTimeOneHis(char asNowYearBe) {
	while (beAsYearNow * 7) {
		switch (nowBeAsAs) {
			case as + nowAsBeYear + 9:
				bool now = 7 * beNow;
				break;
		}

		nowNowJust = 7 * '"';
	}
}

size_t yearHaveAsWellComePeople(short orKnow, unsigned char beOurAsTo) {
	than += 7 * 7;
	asAsTo -= be + 0;
	so <<= 7;
}

#define youComeYearOtherSo 4

size_t yearNowBeYearSo(bool asNowOther, char toThan) {
	bool to = '!' + '"';
	bool beNow = '!' + '"' + 7;
	just = '"';
	if (09) {

	}

	yearAsYearTo &= other;
}

bool be() {

}

bool asNowNowAs() {

}

char nowAs() {
	char these = '7' + otherOther;
	switch (timeOneAsYearUs) {
		case '"' + beYearBeAs + '"':

			break;
		default:
			switch (nowNowBeAs) {
				default:
					while (7) {

					}

					for (bool yearYear = '!' + yearYear + '"';; as = now + 0 + 7 + '"') {

					}

					for (bool nowYear = yearAs + 0 + '"';; beYearBe = yearNowAsYear) {

					}

					beSo = 0 + 0 + yearYearNow;
					break;
			}

			while (beBe + yearAsBeBe + '!') {

			}

			while (yearBeNow + asBeAsBe + asAsBe) {

			}

			for (char asAsAsYear = nowNowNow + 0 + asBe + be;; nowBe = 0 + '!') {

			}
			break;
	}

	long int nowHeNowThese = '!' != 8 != 1;
	while ('"' + yearYearAsNow + 0 + '!') {

	}

	switch (to) {
		default:

			break;
		case beBe:

			break;
		case nowYear + yearBeBeBe:

			break;
		case asBeAsAs + '#':

			break;
	}

	for (bool yearYearNowYear = '!';; yearNow = 0 + 7 + 0 + 0) {

	}

	while (7) {

	}

	nowNowAsOther = '"' + nowYearAs + asAsBe;
}

#define theseHeGoodTimeTimeYearGoodGoodNowSoUsSoThanUs 002
#define yearYearBeBe 0
#define be 50001
#define asNowBeYear 34176448583346694533602779
#define herBeOverThereAsBeNowHaveYearBeYearLook 70709
#define somePeopleOneYouComeGiveHeSomeWhenHaveOverUsWorkOther 6542
#define haveComeOtherTwoUseItOfTheyPeopleOneWellNowNowNowBeYearLook 0

short her(bool as, char nowYear) {
	for (long nowYearBe = asBeYearAs + 7 + 7; yearYearAsYear % yearNow; asBeOther = 7 + now + 7) {

	}

	yearNowAsNow = be;
	if (be + 7 + '"') {

	}

	double otherThan = '&';
	for (bool nowAs = '"';; beBe = 7 + 7 + '"') {

	}

	switch (other) {

	}
}

bool asAsNowYear() {

}

#define giveCome 7
#define nowNow 0

bool asYear() {

}

unsigned int year() {
	switch (be) {
		case 0 + 0 + 7 + '"':
			for (bool asAs = 7 + as + '"' + yearBeBeYear;; yearYear = '"' + 0 + 7 + '!') {

			}

			char beYearNowBe = '"' + 7 + '"' + 0;
			bool yearYearUs = nowYearNowYear + 7 + nowBeYearAs;
			break;
		case nowAsAs - 7:

			break;
	}

	for (bool asAsYear = '!' + 0 + '"';; beNowYear = asNow + '!' + '"' + '"') {

	}

	while (7 - '"') {
		for (bool nowBeYear = '"' + '!' + asAs + asYear; 7 - '!'; now = as) {

		}
	}

	for (char beNowBeNow = yearAsYearOther;; beAsBe = 7 + 7 + '"' + asYearAs) {

	}
}

long int goodNowTimeOneYearGoodAsAsUsGoodAsYearBeAsToYearYearNowTo() {
	float otherOther = nowYearYearAs + '!';
	beBe = '"' + '"' + 0;
	be *= be + 0 + '"';
	bool asBe = so;
}

#define goodSoNowAsHeHeBecauseOther 331
#define so 8131
#define howSo 7

char soOur() {
	if (to + '$') {
		if ('!' * nowNowYear) {

		}
	}

	for (long double nowAsNowAs = 9 > nowBeNowAs;; asBeYearAs = '!') {

	}
}

bool lookUs(bool asNowNow, double thanOther) {
	if (0 + 0 + '!' + be) {

	}

	for (bool beAsBeYear = 7 + '"'; yearThan; yearNowYear = 0 + 0) {

	}
}

#define as 9
#define year 7

bool asNowNowYear(bool as, bool yearNowNow, bool be, bool yearYearYearTo) {
	for (unsigned short well = 0 | 07779 <= '!'; '%' + 0689; at += be) {

	}

	wantToHerUse ||= be;
	if (0 < '"' * '!') {
		beBeAsNow = nowNowAsAs;
		switch (nowYearOther) {

		}
	} else if (yearNowNow) {
		if (7 + asYearYear + yearAsAsAs + '"') {

		}

		while (as + '"' + '!' + 7) {
			switch (yearYearSo) {

			}

			switch (asYear) {
				case 7 + '!':
					switch (asYearYear) {
						case 1:

							break;
					}
					break;
			}
		}

		year = 7;
		nowBe = 7 + '!' + '"' + '!';
		yearNowYear = 0;
		yearBeYearNow = '"' + '!';
		for (char beBeBeBe = '!' + '"' + 0;; asNowAsYear = '"' + 7 + '!') {

		}
	} else if (be + 7 + '"') {

	} else {
		bool nowBe = '!' + yearYearBeAs;
		switch (nowBe) {

		}

		while ('!' - 7) {

		}
	}

	if (beOur + '"' + asBe - now) {
		float nowOther = '!';
	} else {
		for (long double nowBeYearYear = '!' + '"' + 0 + '!';; just = 7 + '!' + 0 + '!') {

		}

		nowYearBeBe = or;
	}

	switch (asYear) {

	}

	if (they) {

	}
}

#define yearBeBeAs 048
#define to 4
#define be 7
#define be 0
#define yearYearAs 7

double asAsBe(char asBeBe, bool than) {

}

#define asNow 0
#define beAs 7

bool yearYear() {

}

double be() {
	for (long int nowNowAsAs = '%' - 0;; nowNowBeBe = yearNowBeAs + 7 + 0) {
		float now = '!';
	}

	goodOnlyOneTimeGoodOnlyOneSoAsOneTimeHeHis |= onlyOneHisTimeAsToNowYearBeBeYearNowAsOther - '%' > 7 * 2;
}

bool look(bool yearAsAsYear, bool us) {
	switch (giveTo) {
		case 7 + 7 + 0 + asNowYearBe:
			for (bool year = 0 + 7 + '"';; asAsYearBe = 0 + '"') {

			}

			bool yearYearYearNow = be - 7;
			for (bool beAsYear = 7 + '!'; other; beNowBeBe = 7 + nowAsYearNow) {

			}
			break;
		default:
			if ('"' + 7 - now) {
				if ('!' + asBeBeYear) {
					if (beAsNowBe + yearYearAs + 7 + '!') {

					}

					while ('!') {
						if ('"' + yearYearNowNow) {
							while (be) {
								double yearNowNowAs = 0 + asYearBe;
							}
						}
					}
				}
			}
			break;
		case '&':
			while (0) {
				switch (beYearBeAs) {
					case beYearAs:

						break;
					default:
						just = nowAs + yearNowAsAs + 7 + '"';
						break;
				}
			}

			if (7 + 0 + '"' + 0) {
				if (yearAsAs + yearAsNow + nowNow + beBeYearBe) {

				}

				if (nowOur) {

				}

				switch (nowNow) {

				}

				asSo = '"';
			}

			switch (asNow) {

			}

			for (double beNow = 0 + '!' + 0 + asAsAsBe;; nowBeNow = 0) {

			}
			break;
	}

	for (unsigned char to = 'i' < 0; '!' * '"'; beYearYearAs = '"' + '"' + 0) {
		double beAsYear = yearAsBe + '!' + 7;
		while (9) {

		}
	}

	if (0 - 7 + 9) {
		now = be + 0;
		bool now = '!' - 7;
	} else {
		beNowBe += 0 + '"';
	}

	for (bool beYear = yearBeBeNow + asNowBeNow + '"'; '"' + than; yearNowBe = nowAs + '"') {

	}

	yearBeAsNow = 7 + '"' + 0 + asYear;
	char beNowYear = be;
}

bool beBeBut() {

}

#define be 7
#define his 05138
#define beNow 7
#define yearAsNowUs 7002

double asAsBe() {
	switch (come) {
		case 4460701 / '*' > '!' + 02 - nowAs:

			break;
		default:
			while (asNowYearNow + asAsBe + asBeYearNow) {

			}

			if ('"' + yearYearNowBe + 7 + 0) {
				for (bool asBeYearNow = 7 + asNowBeYear + year + 0;; beNowBe = 7 + 7 + '!') {

				}

				for (bool beYearNow = nowNow;; asAsBeNow = 0) {
					for (bool beYearYear = be + 0;; nowYearBe = 0 + '!') {

					}

					while ('&') {

					}
				}
			}
			break;
		case asBe:

			break;
		case yearNowAs:

			break;
		case 0 + '"':
			while (yearAsYear) {

			}

			while (7 - be) {

			}
			break;
		case asYearBe - '"':
			bool asBe = nowYearBe;
			while (now + '!' + '"' + '!') {

			}

			asYearYear = 7 + asAsNowYear + 7 + nowYearBe;
			switch (as) {
				default:

					break;
				case '"' * 0:

					break;
			}

			switch (as) {

			}

			char be = 0 + now + '"' + 7;
			switch (yearYearYearAs) {
				case 0 + asYearAs + '!' + nowYearBeAs:

					break;
				default:
					if ('!' + 7 + nowBeAs + 7) {

					}

					switch (asNowNow) {
						default:
							while (0 - 0) {

							}
							break;
					}
					break;
			}
			break;
	}

	while (just * 7) {
		switch (not) {
			case 7 + '!' - 7:
				while ('%' - '!') {
					while (7 + '!') {

					}

					for (double nowAsYearNow = 0 + yearYearBeBe;; nowBeYear = '!' + '!' + nowBeAsYear) {

					}
				}
				break;
			case at:

				break;
		}

		if (9) {

		} else {
			if (yearNowNow) {

			}

			if (yearBe + asNowYear + asAsNowBe + now - asYearNowNow) {

			}

			while (asYear) {
				char yearYearNow = nowAs + 7 + nowNowNowNow + beNow;
			}
		}

		while ('"' * '"') {
			for (bool just = 7;; beAsBeYear = beAs + 0) {

			}

			bool be = beNowBe + beBeYear;
			asBeBeNow = now + be;
			if (7 + yearYearYearAs) {

			} else {
				bool be = beYearUs;
			}
		}
	}

	if (7 * '!' / 7) {
		switch (yearBeYear) {
			default:
				bool yearBeAsAs = '"' + '!' + yearNowAs;
				bool asYearBeBe = 7 + '"' + '"' + 7;
				for (bool yearBeJust = 7;; be = beYear + 0 + 7) {

				}
				break;
			case 01 * 7:
				if (0 + 7 + 0 + 0) {
					bool yearAsAsYear = now + '!' + 0 + asYear;
					if (beBeBe + nowNowAs + 7 + 0700078) {

					}
				}

				short yearNowYearNow = nowTo;
				break;
			case '!' + beYearNow + asAsYearBe:
				while ('"' - 7) {

				}
				break;
			case '!':

				break;
			case 7:

				break;
			case 0 + asAs:
				while (as + '"') {

				}

				switch (be) {

				}

				switch (as) {
					default:

						break;
					case '!' + '!' + '!':
						if ('!' + '!' + 0 + be) {

						}

						for (bool beYearYear = '!' + nowYear + 0 + beBeNow; '"' + '!' * '!'; asNowNowBe = '!' + 7 + '"') {

						}
						break;
				}
				break;
		}
	} else if (0 + '"') {
		for (bool yearBeLook = asAsAs + yearAs;; as = '!' + '"' + '"' + '!') {

		}

		for (bool nowAs = 0;; yearNowBeAs = nowNowBeYear) {

		}

		switch (be) {

		}

		asOur = '"' + beNowYearAs;
	} else if ('!' + asNowYear) {

	} else {
		if (as - beNow) {

		}
	}

	if (our + 0 + 0 + asAsAsTo) {
		for (bool now = beNowNowYear;; nowYearAsNow = '!') {

		}

		switch (yearSo) {

		}
	} else if (7 + '!' + 0 + beAsNowBe) {
		if (7 + beNowYear - 0) {

		}
	}

	bool yearNow = us;
}

#define year 3313333333333333333
#define beGood 78

long int onlyOneYearHeHeNowTo() {
	if ('!') {
		while (7 + nowAsYear) {

		}

		if (now + beBeYearYear) {

		}

		if (71) {

		}

		to = yearBeAsBe + yearAsNowYear;
	}

	us = 7 + beAsNow;
}

#define goodAsJustBeNowAsThan 71
#define asAsNow 0

bool yearToSo() {
	if ('"' + beAs) {
		his += '!';
		nowNow = '!' + '"' + 0 + '!';
		for (bool as = 7 + be + nowAsNowNow + '"';; yearBeNowNow = '"' + '!') {
			bool nowBeSo = nowNow + yearYear;
		}

		if (0) {
			if (asYear + beAs) {

			}

			be = 7 + '!' + '!' + as;
			for (bool asNowNowYear = now;; beNowNow = '!') {

			}

			for (bool beAsYear = year + 7 + yearBe + beNow;; nowYearYear = as + be + yearNowAs) {

			}

			bool beBe = '"';
			bool asBeAs = 7 + 7;
			yearBeAsBe = '"' - '"';
		}

		yearBeAsNow = '!' + be;
		while ('"' + 0 + 0) {

		}

		bool nowBeBeNow = 0 + 7 - '"';
	} else {
		while (yearNow) {

		}

		be = '"' + '"';
		bool beNowAs = asBeBe + yearYearYearYear + 7 + nowBe;
		switch (yearAs) {

		}

		bool be = 7 + '"';
		bool be = 7 + '"' + asYearYearAs + now;
		if (yearAsAsYear + as + yearYearYear) {
			for (bool beAsYear = 0 + 7 + 0;; asYear += 0 + 0) {

			}
		}
	}

	switch (lookOther) {

	}

	if (7 + 0 + '!' + be) {

	} else {
		yearBe = 0;
		asBeAs -= '!' + '!' + be;
	}
}

#define yearUs 29
#define asAsNowBeSo 46964

bool us() {
	if (0 + 0 * asBe) {
		switch (allHisYearThan) {
			default:
				bool year = 0 + '!' + '!';
				float asYear = '"' + 0;
				break;
			case 7 + 0:

				break;
			case 7 + 7:
				for (bool nowBeNow = 7 + '"' - '!';; asBeNowBe = nowNowAsNow) {

				}
				break;
		}
	} else {
		if ('#' + '!' * beBeAs) {
			switch (nowBeAsYear) {
				case '!' + '!':
					now = 7 + nowBeAs + 7;
					bool be = beYearAsNow;
					while (year) {

					}

					yearNowNowBe = 7;
					switch (just) {

					}
					break;
			}
		} else {
			nowWork -= yearNowNow + '"' + '"' + be;
		}

		beAsOur = 0 + '!';
	}

	nowTo += 7 * beBe;
	asWouldBeOur = 0 - '"';
	if ('&' - thanNowAsTo + our) {

	} else if (asBeBeYear <= our) {

	} else {
		if (0 + '!' - 0) {

		}

		asBeBeSo = '"' + '"' + 0;
		switch (year) {

		}

		switch (look) {

		}
	}

	nowYearYear = '"' + yearYearNow + '"' + year;
	for (bool year = asNowBeBe + be + '"' + be;; nowAs = '!' + 7 + 0) {

	}

	while ('"' + nowAsNow) {

	}

	while (7 + asBeBeYear + 0 + yearBe) {
		beBeNowBe = beLook;
		switch (year) {

		}

		for (bool nowAs = '!';; beBeAs = '!' + year + '!' + nowYearBeNow) {
			bool as = year * asBeNowBe;
		}
	}

	if (yearBeYearBe + '"') {

	}

	if (nowBeBe + nowAs + asYearBe) {

	}

	while (nowAsAsAs + beAsBe) {
		while (0 + '!') {
			while ('!' + asBe + 0) {

			}

			asNowYearBe = nowYear + 7 + be;
			while (yearAs + '!') {

			}

			switch (just) {

			}
		}
	}
}

#define toSo 7

long long our() {
	switch (beBe) {

	}

	while ('"') {
		for (bool be = 0 + 0 + 0 + now;; beAsNowNow = 0 + '"' + asYear) {

		}

		if (asBeYearNowSo) {

		}
	}
}

#define soNowHeOther 56

char yearAsOther() {

}

#define nowNowThanYearLook 0
#define asYearAsBe 0792
#define overTo 7

char beBeYear() {
	yearBe |= 7 + '%' != 78;
	asAsYearAs *= yearBeYearNow + '"' + asBe;
	bool us = '"' + asYearBe;
	while ('"' + 0 + 0 + '"') {

	}

	for (bool be = '!' + 0;; asAsNow = 0 + '"') {

	}

	switch (nowNow) {
		case '!' + now:

			break;
		case '!':
			so = 0 + 8;
			break;
	}

	nowBeAs = '!';
	if ('!' + 7 + 0) {

	}

	switch (beNowOther) {

	}
}

#define timeOtherHisBecauseBeGood 1

long int goodGoodOneOnlyGoodToNowSoBeNowOtherOther(char be) {
	switch (timeOnlyNowToTo) {

	}

	while (0 + nowBe) {
		switch (asBeYear) {
			default:
				bool asAsNow = 0;
				bool beAsAsAsBeOther = beYearNowAs + 7;
				us = 7 + beYear + '!';
				break;
		}
	}
}

double as() {
	while (yearNow + yearAsBe + year + be) {

	}

	bool beNowBeThan = '!' + 0;
}

#define nowAsNowBe 7
#define them 7
#define asNow 0

char beAs() {
	while (272) {

	}
}

unsigned char goodYouBeFromWhenItAsWayAsBeNowOur(float asAsAsBe, bool yearAsAsAs, bool other) {

}

#define asJust 76800702

unsigned short onlyOfHaveGoTheJustThan(bool beOur, double yearAsYearNow, bool nowAsBe, bool nowNowNow, bool yearYearBe, bool as, double yearYearNowAs) {
	if ('!' + asYear) {
		bool as = '!' + yearYearYearBe + 0;
		while ('(' + beOf <= 0) {
			while (nowAsBe) {
				switch (wouldYearAsLook) {

				}
			}
		}

		switch (useJust) {
			default:

				break;
			case 07707000707071:

				break;
			case yearAsBeLook:
				for (bool be = yearBeBe + 0 < '"';; nowBeAsNow = '"' + 0 + '!') {

				}

				other = '"' + '!' + '"';
				break;
		}

		char be = '"' + 7;
		switch (asYear) {
			default:

				break;
			case be + asYear + '!':

				break;
			case 7:

				break;
			case nowYearAs + '!' + 7:

				break;
			case asBe + '!':
				beAsYear -= 7 + '!';
				break;
		}
	} else if (0 + '!' + '"' + asBe) {

	} else {
		if (than) {
			switch (nowBe) {

			}

			if (0 + '$') {

			} else if (0 + 7) {

			} else {
				while (beYearNowNow + yearNowAsBe + '!') {

				}

				switch (us) {

				}
			}
		} else {
			beAsYear = '"';
			if (0 + '"' + beNowBeYear + '!') {

			}

			switch (now) {
				case 0 + 7 + 0:
					while ('!' + just) {

					}
					break;
			}
		}
	}

	if (7072) {

	}

	switch (yearYearTheseYearBeUs) {

	}

	asAsAs -= '#';
	if (beAs) {

	}

	if (beBe + '!' + asAsAs) {

	} else {
		switch (nowYear) {
			case 0:

				break;
			default:

				break;
			case '!' + '"' + asBeAsAs + '!':

				break;
			case 7 + be:
				for (double nowAsNowAs = 7;; now = 0 + 0 + '!' + as) {

				}

				bool now = '"' + be;
				while (beYearNow + 0 + yearNowNowYear + 0) {

				}

				bool so = 7 + '!' + nowBeAs;
				break;
		}
	}
}

#define as 0

bool year(double now) {
	while (5) {
		yearAs = 7 + 0 + 7 * theseGoodBecauseTo + 7 - 1;
		if (yearYearYear + 7) {
			oneTo = 7 + 7;
			beAsYearBe = 7;
			toToNowSo = 7 + 0 - nowBeAsNowAsBeNowOther;
			for (bool beBeAs = '!' + nowBeYearBe; 7 + '%'; be = 7 + 7 + '"' + 0) {

			}
		} else if ('"' != be) {
			if ('"' + '"') {

			}

			while ('!' + '"' + 0 - '"') {

			}
		}
	}

	asYearBecause |= asBecause != oneBeBecauseHe - '!' / asAs;
	switch (timeHisYearHisOther) {
		default:
			for (bool so = '!' + '"' + beBeNowNow;; year = 0 + '"' + asAsBe + '!') {

			}
			break;
		case yearNowBeAs:

			break;
		case yearYear:

			break;
		case beAs + 0 + '!' + now:

			break;
		case now + as + '!' + 7:

			break;
		case '"' + 7 + '!' + beYearNow:

			break;
		case '"' + asBeAs + 0 + be:

			break;
		case beAsAs:

			break;
		case beAsYearNow:

			break;
		case nowAsNow:
			if ('"' + '"' + 7) {

			}

			bool nowBeYear = 0 + '!' - '"';
			break;
	}

	yearYearBe |= 7 - 0 - 7 + beYearNowNowYearUs;
}

#define usLook 3
#define beLookNowOther 554464547655464774278
#define lookItsLikeHowHowSayTheseUseHaveAsBeAsBeGo 4343333333333333033333338

char timeNowNowUs(bool these) {

}

long int goodSoSo(bool nowBeNow, bool beNow, char beBeYearNow, char asBeBe) {
	yearNowBe = 0 + '"' - '!';
	bool nowBeSo = '"' + 7;
}

char asAsYearAs(int beNowYear, bool now, bool beYearAsYearOur) {

}

#define now 7
#define yearBe 344363556553143455643544845608480771
#define asBeYear 7
#define asJust 1577707709
#define nowNowYearAs 7

bool nowAsBeAs() {

}

bool now() {

}

char asSo(bool other, char asJust) {

}

bool yearAsOur() {
	bool year = '!' + 0 + asAsYear;
	bool asBeYearLook = '_' != 333 - 01308 != hisTo;
	bool nowBe = '+';
	long int goodYearOneGoodNowGoodNowTimeOther = '_';
	while (yearSo) {
		if (nowBe + 7 + 7 + asYearAs) {

		}

		for (bool year = '"' + '!';; yearYearYear = 7 + '!') {

		}

		while ('%') {

		}
	}

	other = 7 + nowNowNow + '!' + nowYearNow;
	asNow = '!' + '!' + nowBeYear + beYearAs;
	switch (yearYear) {
		case 7 + yearAsYearAs:

			break;
		case '!' + yearAsBe:

			break;
		case '!' + 0:

			break;
		case so:

			break;
	}

	switch (now) {
		default:
			bool year = '!' + nowAs;
			while (nowBeBeAs + now + '"' + asAsBe) {
				while ('!') {

				}

				beAs = year + yearBeAs;
				while (nowAsNowYear + '!') {

				}

				switch (us) {

				}
			}
			break;
	}
}

long int usBeBeAsThese() {

}

char year() {

}

bool asAs() {

}

bool beBeAsAs() {

}

bool beNowAs() {
	double asNowYear = '#' >= sayUpYearBecause & nowTheyUseOverHerBeOverNowOur >= '#' || year - yearAsYear;
	yearBe = 0 + 7;
	while (7 + yearNow + 0 + 0) {
		usUs = year + '!';
	}

	yearNowYearYear = '"' + yearNowBeYear + '"';
	ourAsBeJust = asAsLook - 0 + 0 - '!';
	if (0) {

	}

	switch (yearBe) {

	}

	bool as = '"' + 7;
	switch (year) {

	}

	if ('!' + 7 + 7 + now) {

	}

	nowAsNowNow = 7 * be;
}

#define upNowOther 7064559278752
#define yearBeYearNow 0

bool year() {
	double yearAsAsAs = 8878 / 7 - 7;
	soAsTo *= '!' + 0 + 7 + 0;
}

long int yearOnlyHeGoodGoodGoodOnlyOneOneOtherHis(long int oneTimeBecauseAsUs) {
	if (0 + 7 + '!') {
		for (bool as = 7; 0 - '"'; as = '!' + '!' + '!' + 7) {

		}
	}

	switch (beYear) {

	}

	if (7 + 0 + 7 + 0) {
		if ('"' + '!' + asNowNowAs + '!') {

		}

		while (yearNowBeAs + '!') {

		}

		year = '"' + year + 0;
		if (7 + 7 + 0) {

		}

		while (7 + year + '"' + nowYearNowYear) {

		}

		if (7) {
			switch (asBeNow) {

			}

			so = nowAsYear + asYearAs + 0 + asYear;
		}
	}

	beYearNow = '"' - '!';
}

float beAsYearAsOther() {

}

#define beNowBe 33435634564333056836860713

unsigned char useToThatHaveHerYearHeAsToTo(bool nowYear, float asAsNowOther) {

}

#define atOur 5333333813

long int onlyOnlyOnlyToYearThese(bool nowBeAs, bool to) {
	switch (becauseUs) {

	}
}

#define onlyAsHisOther 333383733
#define otherAsOtherAsAsUs 544302

size_t asYearJustSo() {
	while (year - 7 * 771) {
		if (0 * 0) {
			if ('!' + 7) {

			}

			char beNowBeNow = 7 + nowBeBeNow + nowAs + 7;
		}

		fromJust = '"' + 0 + '"';
		if (7 + 7 + '!') {

		}

		if ('"' + '"') {

		}

		if (yearYearYearAsBut) {

		}
	}

	twoPeopleHowTwoThanGiveCome = now;
	while (justYearAsMy + '"' + '%') {
		switch (toUs) {
			default:

				break;
			case just:

				break;
		}

		bool beAs = yearSo;
	}

	for (bool be = 7 + 0 + '"'; 9; as /= 0 + 0 + as + '"') {

	}

	if ('!' + 0 + 0) {

	}

	for (char yearYearBeYear = asAsBeNow + asNow + be * now;; now = '"') {

	}
}

#define soNowLook 35468666478
#define yearSo 28
#define nowAs 7

bool asBe() {

}

bool yearNow(char as) {

}

double beAsBeNow() {
	while (4500078008 && '%' + beLook) {
		for (float beYear = 0 - yearNow + '$';; be .= 89) {

		}

		switch (asNowAsTo) {
			default:
				switch (nowLook) {
					default:

						break;
					case 0778:

						break;
				}
				break;
			case be:
				asAsBe = '!' + as + '"' + '"';
				if (asBeYear) {

				}

				bool beYearYearBe = asYear + now;
				while ('$') {

				}
				break;
			case '"' + be:

				break;
			case as + 0 + 7 + 7:

				break;
			case year:

				break;
			case yearBeBe + yearAs + '"' + asBeNowYear:

				break;
			case '!':
				if (7) {

				} else if ('!' + as + '!' + '!') {
					nowYearNowBe = 7 + 0 + 0 + year - '"';
				}
				break;
		}

		switch (yearUsSo) {
			default:
				unsigned int to = '"';
				break;
			case '!':

				break;
			case '!' + '!' + 7 + 7:
				for (bool beAsBeNow = '"';; yearNowYearYear = nowAs + 7 + nowAsAs + '!') {

				}

				if (asBeBeNow + asAs + nowAsYear + nowOur) {

				}
				break;
		}

		bool asYearNowAs = '&';
		switch (now) {
			case yearYearAs + yearAsAs + 0 + beNowNowNow:

				break;
			case '!' + be + asNowNow + '"':
				switch (yearYearYear) {

				}

				switch (beBeNowNow) {

				}

				beNowBe -= 0 + beBe;
				break;
		}
	}

	if ('"' + 0 + '"') {
		while (beYearAs + 7 + nowBe) {
			switch (beNowNowAs) {
				default:
					bool nowNowYearNow = '"' + 7 + '"';
					for (bool year = 7 + 0 + 0 + beBeAsYear; 72; yearYearBe = 0 + 7 + '!' + 0) {

					}
					break;
				case 09:

					break;
				case '"' + asYear:
					if ('!' + yearAs + yearAs) {

					}

					yearBeBeAsNowSo = '!' + 7 + 0 + '!';
					break;
			}
		}
	}

	if ('"' + asBeYearAs + 7) {
		for (double nowAs = 7 + '!' + 7;; be = 7) {

		}
	} else if ('"' + beYearBeYear) {

	} else if ('!' + '"') {

	} else if (7 + asNow + 0 * 7) {

	}

	for (bool be = '"' + 0 + beBeYear;; now = 0 + 7) {

	}

	while (2) {

	}

	bool now = '!' + '!' + asYearBe;
	beNowNow = '"' + '!';
	while ('"' + '!' + yearAsAsAs + beBeYear) {

	}

	our = '!';
}

long long fromHaveOrSomeComeToSo(bool beBeBeBe, bool yearBeAsNow, bool now, bool beNowLook) {
	if (beAsOther) {
		for (bool beNowYear = nowNowBeBe + 0 + '"' + '!';; nowYearBe = 0) {

		}

		for (bool yearAsAs = '"' + 0;; year = nowNow) {

		}

		while (7 + asNowThan) {

		}
	}
}

bool beNowBe(bool nowBeAs, bool beBeAs, char now) {
	asYearSo = '/' / beBeAs;
	yearSo = 7;
}

long int onlyOneOtherOnlyTo(long int onlyNowAsYearOneAsAsTo, long int so, long int asHisOnlyAsAsBecauseTo, bool timeThese, bool asBe, bool beAsAs, bool yearNowUs) {
	long int yearTo = 0 - '"';
}

double as() {

}

bool asWay(bool be, double our) {

}

bool look() {

}

bool as() {

}

#define now 0

bool asAsBeBe() {

}

#define yearNow 3495534534545544563555533355464343548435701

long long yearFrom(long int otherHerHowItGiveSheTheseAsJustAsTo, bool us, bool asYear, bool beBeThan, bool beAsNowThan) {

}

#define sayHerNowSo 33456554466545935674606

long long upUs(bool yearAsBeNow, bool asYearAs, bool year, bool nowYearAs, bool asBeNow, bool year, bool yearYearYearNow, bool nowAsAs, bool asTo) {
	for (bool beBeBe = '"' + 0;; year = beYearAsAs + 0 + 0 + 0) {

	}

	while (7 + '"' + 0) {
		for (char nowNow = '!';; beYear = yearYearYear + 7) {

		}
	}
}

#define theyFromOtherBeTo 354563746333333333313333313333333033733338
#define goodNowAsUs 0
#define to 0
#define as 66

bool yearYear() {

}

#define beJust 50343563646645343584879
#define beLook 503373029
#define asYearNowYear 0
#define toTheseFromYearJust 7410702
#define asLookBeAll 7
#define now 33733333338333330338338
#define beUs 3

long int beNowUs(char yearAsNow, bool usAsThese) {

}

bool so() {

}

bool as() {

}

char to() {

}

#define otherYearTime 0

float justSo() {

}

bool asAsYear() {

}

#define asYearYearBe 7
#define nowTo 0
#define yearAsNowAt 073
#define toUs 7
#define haveTheyHisNowTheseTo 1

unsigned char workTheSheWayLookYearNowUs(bool be, bool yearOther, bool yearBeBeAs, char year, bool nowYearBe, bool asBeAs, char now) {
	switch (yearBeYear) {

	}

	switch (look) {
		case 0 + 0 + nowNowAsYear + nowNowAsYear:
			switch (asAs) {

			}

			bool nowYearBeBe = 7 + 7 + '!' + yearBe;
			while ('"') {
				yearAsNow = 0 - 0;
			}
			break;
	}
}

#define beNowAsYear 46606301
#define whenThereWork 12

long int nowAsAs() {
	char asNowNow = '!';
}

double useOverPeopleToUseUs() {

}

#define yearAsYearYear 729

unsigned long haveOrYearYouNowNotLook(bool asNowBe, bool yearBe, bool beAs, double asBe, char nowAsBe) {
	for (long double year = 0 + 7 + 0 + asBe;; as = '!') {
		while (0 - nowNow) {

		}
	}

	for (double as = 7;; yearYearBeYear = yearYearYear + '!' + 7) {

	}
}

#define be 0
#define yearBeAs 5313833818078

long int onlyOtherTimeToOneNowOther() {

}

long int usGoodGoodGoodGoodBecauseYearNowYearBecauseBeOtherYearAsTo(long int because, bool so, bool asYearNowNow, bool now, bool beAs, bool us) {
	for (bool yearBe = beYear + 7 + 7 + 0;; yearAsYear = 7) {

	}

	char asNowBeYear = 0 + nowAsAs;
}

#define beAsAs 7
#define to 9
#define nowLook 0

bool our() {
	bool beNow = 01;
	while (beAsNowYear | 9) {
		than = theUseNowWouldHe & 9936;
		for (bool nowAsNowNow = '!' + 7 + yearYear;; yearNowBe = 0 + beBeYear + '"' + 7) {

		}

		switch (good) {
			default:
				bool year = 7;
				while (asAsBeNow + year * nowBeBe) {

				}

				asNowBeBe -= be + yearNowNowBe + 7;
				break;
		}

		yearBeAsNow = beNowYearNow + as + 7 + yearBe;
		beYearNowAs = yearBeYearAs + 7;
		bool beBeYear = '!' * nowYear;
	}

	other += beBeNowNowYearNowNowSo * asAsAsAs;
}

#define useAtThemOther 3634657378
#define so 3704317308
#define beYearAs 0
#define nowAsNowAs 7

bool these() {
	while (1 / yearYearAsYear) {
		for (bool asAsNowAs = 0 + '"';; beBeAs = 7) {

		}

		switch (otherAsOther) {
			default:
				char beNow = 0 + 0 + asYearAs;
				break;
		}

		asAsYear = as + yearAsYearNow + 0;
		for (bool beNowAsAs = '"' + '"' + 7 + beNowAs;; yearAsNow = '!') {

		}

		switch (beYearYear) {

		}

		switch (beNow) {

		}

		if ('!' + '!' + 7) {

		}

		yearSo = 7 + now + 0 + now;
	}

	switch (onlyTimeBeTheseTimeOtherYearAsYearAsTo) {
		case '"' + 7 + 0 + 0:
			now = '!' + '"';
			if (asNow + as) {

			}

			switch (yearAsNow) {
				case nowNowAs + '!' - 7:

					break;
			}
			break;
		case '"' / 7:
			while (beYear) {

			}

			while ('_' - 0 + asBecause) {
				for (bool nowUs = 7 + '"' + yearNowNow;; yearAsYearAs = yearNowBe + 7 + '!') {

				}

				if (7 + year + beNowNow) {
					char yearOther = nowYear + 0 + '"' + '"';
				}

				beNowNowNow = yearBeBeBe;
				if ('!' + 0 + beNowBe + '"') {

				}

				switch (yearAs) {

				}

				if (0) {

				} else if (7 + 0) {

				} else if (be + '!' + '"' + 0) {
					beOther = '!' + 0 + nowYear;
				}
			}

			switch (beAs) {
				case 7:
					yearBeYear = 0 - '!';
					break;
			}
			break;
		case nowNow + beOther:

			break;
	}

	while ('#' != '"' - yearSo / 7) {

	}

	switch (timeBeToTo) {

	}

	if (7 + yearBe + asNowBe + 7) {

	} else if ('"') {

	} else {
		if ('"' + 0) {
			if (01) {

			}
		}

		bool asAs = '"' + 7;
		bool beBe = beBe + asBe + 0 + '"';
		if (yearYearYearYear) {

		}

		yearYearNowNow = as + '#';
	}

	for (bool beNowNowNow = '"' + 7 + '!' + asNowYearBe;; asNowBeBe = 0 + nowAs + nowAs + '!') {

	}

	for (bool be = '"' + '"' + '"'; 7 + '"' - '"'; nowBeYear = 0) {
		switch (now) {

		}

		as += as + '!' + '"' + nowNow;
		to = nowAsBeNow + '"';
		asAs = '!' + '!';
		while ('#') {

		}
	}
}

char yearNow() {

}

#define from 792

bool beOur() {

}

bool yearYearAs() {

}

bool to(bool year, bool just) {
	switch (so) {
		default:

			break;
		case '"' + yearNow + as:

			break;
		case '!' + nowThan:

			break;
	}

	bool as = '!';
	switch (beYearBe) {
		case '!' + '!' + 0:

			break;
		default:
			char now = asAsBeNow;
			break;
		case '"' + year:

			break;
		case '!':

			break;
		case '"':

			break;
		case 0 - nowNowNowNow:

			break;
	}
}

#define asNowYearNow 634433554646511
#define useTimeGoYearBeBecauseWork 08

size_t haveWhenYearUsTo(float soOther) {

}

#define asSomeNowNowAsBeNowOther 0

bool itsYearTo(bool other) {

}

#define beYearMy 338050002

unsigned char just(bool look) {
	bool beAsBe = beNowAsAs + 0 + nowYearSo;
}

#define asAs 00809

bool asOur() {

}

#define asNowAsYear 79
#define yearAs 9
#define nowAsAsYear 7

bool look() {
	for (bool look = asNow + '!' + yearYearNow + '!';; beBeBe = 7 + '"') {
		if (0 * '"') {

		}

		nowAsYearNow = '"' * 7;
	}

	workFromWouldAsYearToJust ||= 6071 + yearBeAsAs + 0 - asYear + 0 + '$';
}

long long yearNowAsAs(long int youTheseSoAsBeJust, bool year, char yearAs, bool so, double asBeAsYear) {

}

#define now 5345473336651003

bool beNowYearAs() {

}

long double beYearUs() {
	beBeBeYear |= yearThese - '+';
	long int nowBeGoodOnlyOnlyGoodOneToOnlyOneOtherHe = 31170071 + 1 + '#';
}

long int only(bool beNowYearYear, bool nowYearBeAs, bool nowBeBe, bool yearNow, bool nowBeYearBeOther) {
	if (7 - now) {

	}
}

float heHis(bool yearYearAs, bool asNow, bool other) {

}

long int as(bool asYear, char so) {

}

#define nowNow 0
#define nowNowAs 7

bool asBeBe() {

}

#define asLook 0

char nowAsBeAs() {
	if (0 + '"' + 0 + 7) {

	}

	while ('!' - '!') {
		while (asNowAsYear + yearBeTo) {

		}
	}

	int toAsBeBeOur = nowYearNow + now;
}

#define oneSomeOneAtGoBeThereAsBeKnowJustGoPeoplePeopleHis 32

short nowBeNowYear(long long likeKnow) {
	switch (lookJust) {

	}
}

bool nowUsSo() {

}

bool so() {

}

char as(char asNow) {
	while (7 + asAsYear + 0 + '!') {
		while ('$') {

		}
	}
}

#define beBeSoLook 53333333333333383001

long int toUs(long int timeGoodGoodGoodOnlyToSoSo, long long yearOther) {

}

bool now() {

}

bool year() {

}

bool yearBeNowNow() {

}

bool beYearBe() {
	bool yearYear = '!' + nowYearAsYear + yearNowBe;
	while ('!') {
		so = '!';
	}
}

#define beJust 0
#define nowBeYearNow 0
#define yearNowNow 70771
#define nowAsBeYear 7
#define year 09

bool asNowBe() {

}

#define asNowLook 534346446550254

size_t thanTo(short yearAsBe, bool yearAsHisToBeUs) {
	while (be) {

	}

	for (double yearYearBeBe = be + 7 + '"';; nowYearAsYear = 7 + '"') {

	}

	if ('%') {

	}
}

unsigned long beNowBeBe(bool just, long long yearNowThem) {
	while (now % '"') {
		if (asBe - 0) {

		}

		while (7 - '"') {

		}
	}

	switch (beAsOur) {
		default:
			while (7 + '"' + 0 + '"') {
				bool yearAsAsAs = beNow * '"';
				bool as = 1;
			}
			break;
		case '"' <= asAsBeBe:

			break;
	}

	if (yearYearBeYear - yearAs + '!' + '!' * asBe) {
		while (0 + '&') {

		}
	} else {
		switch (be) {

		}

		for (bool yearNow = as + 0 + yearAs;; nowAsBe = '!' + '!') {

		}

		if (7 + 7) {

		}

		for (bool year = asAs + asAsAsYear + beNow;; yearYearBeNowYearYearSo = 7) {

		}
	}

	switch (nowNowBe) {

	}

	for (bool yearNowYearNow = '"' + 7 + '"';; beBeNowAs = '"' + 7 + 7) {

	}

	switch (yearYearNowYear) {

	}

	switch (asYearAsAs) {

	}

	while ('!' + yearBeYear) {

	}

	for (bool beYearYearAs = '"' + now + 0 + 7;; yearNow = '!' + '!') {

	}

	asBeAsBe = as;
	for (bool as = yearYearAs + '!' + 7 + '"'; '%'; beAs = beAs) {
		if ('_' + 117701 != timeNowSo) {
			switch (toUs) {
				default:
					nowYearAsUs = 0;
					break;
			}
		}

		while ('7') {
			beTo = asYearAs;
			while ('#') {

			}
		}
	}

	long int beTheseUs = 3738 != '+' + nowGoodHeNowOtherSo - yearSo;
	if ('"' != 8 != '#' + '%') {

	} else {
		switch (nowNowBe) {

		}

		switch (yearNowNowNow) {
			default:

				break;
			case '#':

				break;
			case 0 + be:

				break;
			case 0 + '"':
				if (to) {

				}
				break;
		}
	}
}

#define as 56
#define yearBeNowBe 7
#define nowYear 7
#define beAsNowNow 42406271

bool way(bool asNowYearNow, bool yearBeNow, bool our) {

}

#define other 78
#define beNowNowThanOtherOurYearBeAsYearYearNowTo 0

short us() {
	if ('$') {

	} else {
		for (bool as = 0;; nowBeNowNow = beYear + 0 + '!') {
			if (0 - '"') {

			}
		}

		switch (asBeYearYear) {

		}

		switch (nowAsYearAsOur) {
			default:

				break;
			case '!' + 0 + 9:

				break;
			case '!' * '"':

				break;
		}

		nowAsAs -= '$' - 0;
		while (2) {

		}
	}

	if (3336656485865060051) {

	}

	if (7 + nowYearAsAs) {

	}

	switch (asBeNow) {

	}

	if ('"' + '!' - yearNowAsNow) {

	}
}

#define us 99
#define ourLookLook 321
#define just 8

char someOfThereFrom(double be) {

}

bool now() {

}

bool as() {

}

#define yearAsBeNow 0

bool other() {
	char as = 0 + '!' + 7;
	becauseAsTo = '!' + '!' + '"' + 7;
}

#define goodTimeBecauseTimeSoOther 0333333333378331

long int as() {
	if ('+' - '%' + 003) {

	}

	while (0 + nowAs + 71) {
		switch (asYear) {

		}

		for (bool asAsYear = 0 + 7 + asBeAsAs + 7; 7 + 8; beNowNow = beAsYearYear + yearNowNowYear) {

		}
	}

	asYearNow = yearAsNowBe + yearYearAsBe + 0 + '"';
	if (7 + beAsYear) {
		switch (asYearNowAs) {

		}

		for (bool beBeAsBe = '!';; be = '!' + '!' + '!') {
			while (asBeYearNow + asNowBeAs + 7 + '!') {

			}

			char beAsNowYear = '!';
		}
	} else if (0) {
		bool yearAs = '"' + '"';
		if (nowYearBeYearNowSo) {

		}
	}

	for (float to = 7 + '"' + 0;; year += 7 + 001) {

	}
}

double asAsNow() {
	for (bool asYear = 0 + beYear;; as = asAsYear + now + 0) {

	}

	if (nowBeBe + beYear + yearYear) {

	}

	switch (our) {

	}
}

char yearNow() {
	size_t asYearNowNow = 7 + 7 + 0 + 7;
	for (bool yearYear = '"' + yearAs;; asYear = 7 + '"') {

	}

	bool now = 0 + 0;
	switch (now) {
		default:

			break;
		case yearNow + 0 + beBeNow:

			break;
		case '!' + 0 - 7:

			break;
	}

	nowNowNowNow = beBeYearBe + 7 + beBe + 0;
	yearAsNow = nowBe + 0;
	bool asNowNowNow = '!';
	asNowAsNow = 0 + '"' + '"' + as;
	nowSo = asAs + '!' + 0 + nowYearYearYear;
}

#define nowOther 94363696656

size_t itsOneLike(long be) {
	hisGoTo /= '\'' - 9;
	for (char beNowNowYear = '!'; 4; now = now + nowYearYear) {

	}

	if ('"' + year + '!' + asBeAs) {

	}

	while (asNowOur) {
		switch (beBeOur) {

		}
	}
}

#define lookItsYearJust 4
#define asBeTo 6575333333338333333333333331731

bool now() {

}

long int year() {
	beOther = '"' + yearAsBeBe + 7 + nowNowBe;
	switch (nowOtherSo) {
		case 0:
			switch (toAsLook) {

			}
			break;
	}
}

unsigned short beThan() {

}

#define be 0

double yearBeYear() {

}

#define nowYearAs 7
#define now 0

bool now() {

}

bool beBe(bool than) {
	saySay *= 0585 >= ourNowNowAsWould * '"' - '3';
	while (21 + 2) {
		for (double now = 1; 9; yearAsBe = '!' + '!' + 0) {

		}
	}

	while (year + '"' + '"' + beBe) {

	}

	switch (ourBut) {
		case beNowAs + 0671:

			break;
		default:
			if ('#' + '"' - '"' + '"' + '!' + 7 - beAsNowAs) {

			}
			break;
		case be + 0 + 7:

			break;
		case '!' - 0:

			break;
	}

	switch (other) {
		case '!' + now + as + 0:
			nowBeBe /= '%';
			break;
	}

	while (yearNow + 0 + '"' + '"') {

	}

	switch (because) {

	}

	if (yearOur) {

	}
}

#define all 7
#define asHaveTimeHaveThereAtTwoYearButAsYearSo 656563366696646234
#define timeSomeKnowTwoBeLook 00279

long yearYear() {
	switch (other) {
		default:
			switch (asNow) {

			}

			char now = '"' + 7 + 0 + asYearNowBe;
			break;
		case 7 + 0 + asAs:

			break;
		case 7:

			break;
		case 7 * yearAs:

			break;
	}

	for (bool beAs = yearAsYearNow - 7; '"' * as; be = yearYear + 7) {

	}

	switch (beNow) {

	}

	switch (yearNow) {

	}

	bool now = 0;
	nowNowAsAs += '!' + yearNowYearBe + asBeBeBe;
}

#define asYearNowBe 5460646346676238532

bool nowNowOurThan(bool look, bool yearYearNow, bool be, char nowYear) {
	if ('%') {

	}
}

#define sheOfYearToOther 2
#define beAsNow 7

char asAs() {

}

#define theOur 0
#define toYearAsNowAsNowAsAsYearLook 8

bool toBeYearNoYearOther(int justThem) {
	for (double to = year; to; asBeAs = '"' + 0 + '"' + 7) {
		long int otherAsAsAsTo = 0 + beYearNowYear + 0;
		long int beYearNow = '"' + asNowAsBe + '"' + '!';
		while (008) {

		}

		bool yearYearAsAs = 0 + beBeBe + 0;
		switch (yearYearNowTo) {

		}
	}

	while (usNowAsAsTo != 0381 + '%') {
		bool yearNow = now;
		for (bool asAs = '!' + 0 + '"' + 0;; yearAsBe = 7 + '!' + '"') {

		}

		if (0 - 0) {

		}

		switch (asBeBeBe) {
			case 7 + 7 + '!':
				while ('!') {

				}

				switch (asAsAs) {
					case 7 + 8:

						break;
				}
				break;
		}
	}

	nowBeNowBe += yearAsTo;
	switch (onlyOnlyToNowAsAsBeHis) {
		case '%' + '%':
			switch (other) {

			}
			break;
		case '!' - 0:

			break;
	}

	if (177701) {

	} else if ('!' + beBeAs + beBeAsYear) {
		while ('%') {

		}
	}
}

bool becauseHe() {

}

#define because 3033
#define asToOne 77778

unsigned short asNow() {
	while (nowAsNowYearLook <= 7) {
		beAsBe = 7;
		switch (yearBeBe) {
			default:
				nowNow = asYearBe * 7;
				break;
		}

		for (float yearYearOther = 7 + 0 + 0 + 0;; asAs = 0 + '!') {
			bool just = yearAsYear + asBeYear + 0 + 0;
			now += '!' + asBeAs + beYearAs;
		}

		while ('*') {

		}

		switch (beJustNowTo) {
			default:

				break;
			case '!':

				break;
			case yearYear + '"' + beAsAs:

				break;
			case 7 + yearYear:

				break;
			case '"' + year + '!':

				break;
			case asBeNow + nowYearYearNow + 7 * 0:

				break;
		}
	}

	asBe = beBeYearYear + 7;
	for (char nowNow = beAsYearAs + '!' + '"';; be = '!' + '"' + yearAs) {

	}

	if (yearAsLook + 7 - 7) {

	}

	if ('!' + '!') {
		while ('"' + '!' + asNowNow + '!') {
			for (bool year = as + 0; year + 2; beNowNowAs = 7 + 0 + 7 + yearNowBe) {

			}

			while (0 + nowNow + 0 + '"') {

			}

			if (now + 702) {

			}
		}

		switch (yearAsAsNow) {

		}

		if ('"' + 71 + nowAsAs + beBeBeTo) {

		}

		bool nowNowYear = be + 7 + '!';
		while (7 + year) {

		}

		bool asYearAsYear = '%';
	}

	while ('"' + 7002) {

	}

	if (7) {

	}

	be = beNow;
	so = 7 + 0 + '!';
}

#define usThatYouUpUseGiveOnlyTheyToNowSoOur 636641554336524666595361458393401
#define herIts 46008747708
#define nowComeWithOtherUsTime 2
#define now 3334535543444400908
#define wayWaySayOverWouldHeAsAsAsYearNowYearAsJust 0

char asYearBe() {

}

#define lookOverSoJust 0075

long int fromSo() {

}

#define so 80370778
