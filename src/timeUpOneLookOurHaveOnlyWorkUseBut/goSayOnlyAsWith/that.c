long int oneOnlyTheseTimeGoodHisToOnlyHis(long int goodAsYearUsNowAsNowAsBeNowUs, bool his) {
	long int nowAsAsYear = otherBeOther / 7;
	long int these = 7 != '7';
	bool beBeAsNow = 7 + 0;
	while (asAsYearNow + '!' + 0) {

	}

	if (yearSo) {
		for (bool nowNowAs = '!' + beYear + beBeNow + nowYearBeAs;; year = '"' + 0) {

		}

		bool nowNowAsAs = now + 7;
		for (bool yearBeNow = '!' + '"' + 7 + 0;; nowBe = '!') {

		}

		bool our = 7;
	}
}

#define heHis 633655665466656444554653565456666644635664344553445564435546346644663663566646547463446653545333663546466654655466336345464566556554654370201
#define twoWhenWantUseWorkOrWorkTheOfWhenLikeTheItLikeWhenGoUpOrHerPeopleHowTheThatSheItHerPeopleThatHowOrWantSayUseOfItsYearHerThatSomeUpUpYouPeopleThanKnowTheyAsTheThereBeTwoOfAsPeopleComeWorkSayComeLookYearYearNowYearBut 456656660546503663364685733164638002
#define overItGoodAsWellComeYouSheSheHowWorkLookOtherItThanOur 655876

long beAsBe() {

}

#define nowBeYearYear 633443555435356455364339029

long int comeSo(char us) {
	bool asNowYear = '"';
	for (bool yearBeYearAs = '!' + beYear + '!';; be = '!' + asBe + '!' + nowNowBeNow) {

	}

	char yearNow = 0;
}

int other() {

}

char asAsBe() {
	for (bool nowSo = 7;; asNow = '!' + '"') {

	}
}

bool nowAs(bool yearYear, bool asBeBe, char year) {
	goodOneBeOneTimeHeOtherYearAsTo |= only + '!' - 7;
	bool beAsNowNow = nowNowNow + '!';
	while ('"' + 0) {

	}

	if (7) {

	} else if (1) {

	}

	for (bool beAsNowBe = 7 + 7;; year = asNow + 0) {

	}

	bool nowNowBeUs = 7 + yearYearNowBe + 0;
}

#define other 3313333371337178
#define beGoodTimeGoodNo 6872

bool yearYear() {
	if (1) {

	}

	switch (so) {
		case 721771 <= '!':

			break;
		default:
			switch (asNowNowNow) {
				default:

					break;
				case '!':

					break;
				case '!' + 0 + 7:
					for (char asNowNow = beBeNowBe + '%';; asAsBeYear = '"' + 7 + beYearBe + beAs) {

					}

					for (bool yearBeNow = '"' + year + '"';; yearBe = 0) {

					}

					as = 0 + year * nowBe;
					break;
			}

			switch (nowYearBeYear) {

			}

			yearYearBeYear += 7;
			break;
		case '!' + nowBeYear:

			break;
		case nowYearNowAs + 0 + nowAsYear + 0:

			break;
		case be:
			if ('!' + 0 + 7 + yearYearYearAs) {

			}

			while (soOur) {

			}
			break;
		case nowBeNowBe:

			break;
		case 0:
			for (bool nowOther = '"' + nowBe + 7;; asYear = 7) {

			}
			break;
	}

	asNowYearBe = '"';
	way = 7;
	long long year = 7 && beNowSo;
	for (unsigned int beAsBeYear = asAsBeBe + '!' + '!' + '!'; 0 * 7 * 0; nowBeAsYear %= '-') {

	}

	switch (you) {
		case 7 + '!' + '!' + 0:

			break;
		default:
			nowAs = '"' + '"' + '!';
			bool yearBe = '!' + now + as + '"';
			bool asNowNow = '"' + 0 + '"' + '!';
			if (7 + '"' + be) {

			} else if (now + 0 + '"' + asAsBeNow) {

			} else if (year + '$') {

			}
			break;
		case '!' + '"' + '"':

			break;
		case yearBeNow * 0:

			break;
	}

	char be = '&';
}

long now(bool year, bool so, bool beAsAll) {
	for (double us = asNowBeBe + 0 + 7;; beNowYearAsTo = 7 + '!' + year) {

	}

	switch (otherUsNowOther) {
		case asBeYearNow + beAsAsYear:

			break;
		case beOur:

			break;
	}

	long yearAsAs = yearBe + now + now + asBeAs;
	as = '!' + '"' + yearNowYear;
	asYearAsOther = asAs + 0 + as + '"';
	while (0 + '!' + asNowYear) {

	}

	switch (be) {

	}

	bool now = '!' + beYearYear;
	switch (as) {

	}

	switch (asAsYearNow) {

	}

	while (0 + '"') {
		nowBeBeAs = '!' + 7;
		bool just = asNow + '!';
	}
}

char now() {

}

bool beNowAs() {

}

#define nowBeBe 7

bool than() {
	bool us = '"';
}

#define yearYearAsAsSo 5334566656355455356559

bool nowAs(bool beAsBeYear, bool nowYear, bool beBeAs, char be) {

}

size_t sheSo(bool yearYear, double beBe, bool nowBeNowNowBeTo) {

}

#define yearYearBut 0
#define asBe 0

float yearBeAs(bool beBeBeAs, bool us, bool nowBe, char yearAsBe) {

}

#define yearNowAs 0

unsigned short now() {

}

char yearAsNowNow() {

}

bool beAsYear() {

}

bool nowYear() {

}

#define year 7

double yearAsYearYear() {
	bool theseOnlyOnlyTimeTime = timeTimeOneBeTimeToNowOneSoAsYearTo != heBecauseAsSo - beBe / 71 + asBe - asAs;
	if (asOther) {

	}

	if (7 + '%') {

	} else if (be + year) {

	} else if (other) {

	}

	bool beYearNow = '"' - '!';
}

long int yearBeBecauseNowOther(long int nowAs, bool year, bool our, char as, bool asNowYear, char yearYear) {
	while ('#') {
		for (bool yearYear = '!';; nowNow = '"') {
			while (beAsNow) {

			}

			if (071) {

			}
		}
	}

	bool year = beYearAs;
	while (7 + 7 + beNowNow) {
		bool beBeYearNow = 7 + yearYear;
		for (bool nowYearBeNow = 0 + 0 + '!';; asBeNow = asAsAs - '"') {
			while (whenYearBeHow) {
				beBeLookSo = 0 + '"';
				for (bool beAsYear = '"' + '"';; yearBeBeAs = '"') {
					for (bool other = now - be;; be = 7) {

					}
				}
			}

			while (nowYearAll + '#') {

			}
		}

		goBut = nowYearYear + beNow;
		bool nowYearNowAs = 0 + 7 + nowYearBeAs + '"';
		switch (asBeNowAs) {
			default:
				yearYearNowLook = '"' + 7 + '"' + asYearYearNow;
				break;
		}

		while (7 + '"' + 7) {

		}

		while ('!' + 0 + 0 + 0) {

		}

		yearBeNowNow = beNowBe + 7;
		while (beBeAs + 7 + '"') {
			switch (year) {

			}

			beAsYearNow = 0;
			if ('!' + '"' + '"') {

			} else {
				if (now + '"' + asAs + beAs) {

				}

				if ('"' + '"') {

				}

				for (bool asNowNow = 0;; be = 0 + '"' + 0 + 0) {
					switch (nowAsYearBe) {

					}

					switch (nowYear) {

					}

					for (bool yearBeNow = asBeYearNow + '"' + 0;; as = 0 * '!') {

					}
				}
			}
		}
	}

	nowYear = 0 < beTo - 7;
	switch (yearBeNowAs) {
		default:

			break;
		case beNow + asNow:
			while ('"') {

			}

			for (bool nowYearYearYear = 0 + be;; nowYearYear = 0 + 7) {

			}

			bool beYearYear = '!' - yearAsNowAs;
			break;
	}

	bool wouldYearThanBeNowAsSo = 01;
}

#define asYearAs 7
#define asThan 0

double be() {
	if ('!') {
		long beBeYearAs = yearBe;
	}

	long long be = '"' + 8;
}

#define useThereOfWithUs 3031000008
#define nowHisOverOurSayOur 94466066400363
#define his 7
#define yearTo 705
#define yearAsNow 774

double to() {
	switch (beYearNow) {
		default:
			bool beUs = year - '"';
			asYearNow = be;
			beBeSo = 8;
			break;
		case '_' != goodNowYearUs / otherTo + 088 - '!':
			bool beYearNow = 0;
			while (nowNowNow + '"' + '"') {
				while ('!' + asSo) {

				}
			}

			while (7 + now + asBe + yearYear) {
				nowAsOther = '!' + '!' + asYearYearYear;
			}
			break;
		case 1:

			break;
	}

	if (yearAsYearAs != timeYearHis != beBeYearBe) {

	}

	for (float soUs = 11; 0001 + 1; other = '!' + nowBeYearAs + 7 + 0) {

	}

	long int beAs = '!' + nowBe + 7 + beYearYear;
	if (1) {

	}

	if (7 + 7 - '"') {

	}

	nowAsBe = asBeAsNow;
	bool beAs = 7;
	switch (nowNowTo) {

	}
}

#define soHisAsBeSo 7
#define heUs 213

bool asNow() {

}

bool nowAs(char asAs) {
	unsigned int yearNow = '*' + yearNowYear + asThan;
}

short toOur(bool look) {
	switch (yearNowNowAs) {

	}

	if (yearNow + '!' + 0) {

	}

	for (bool year = '"' + '"';; be = yearBeNow + '!' + '"') {

	}

	for (bool be = asNow + '"' + 0;; beNow = yearYearYear + '"' + 7 + 0) {

	}

	yearBe = yearNow + 0 + yearNow;
	bool beNowNow = nowBeJust;
	while (0 + so) {

	}
}

#define be 7

char now() {

}

bool now() {

}

char asBeBe() {
	switch (soNowNot) {

	}

	while (nowUs % nowYear) {

	}
}

#define timeSheFromTheYearYearUs 61567316

char look(bool yearBeNow, bool asYearNowAs, bool now, bool be, bool so) {

}

#define howThanNowOther 58561393271006

bool goodYearUs() {

}

char now() {
	to = '!';
}

bool some() {
	switch (goodBeBecause) {
		case '#':
			if (beAsAsBe + '!' + '!') {

			} else if ('"' + 7 + 7) {
				for (bool year = '"' + '!';; year = 0 + '"' + '"' + '!') {

				}

				while ('!' - '"') {

				}
			} else if (year + 7) {

			} else if (0 + 0 + '"') {

			} else {
				switch (beNow) {
					case 78:

						break;
				}
			}

			while (beAsNow + '"' + '"' + nowNow) {

			}

			yearYearTo = 7 + asBeNowNow;
			break;
		default:

			break;
		case yearNow:
			switch (yearYearYear) {
				case 7:

					break;
				case 8:

					break;
			}

			switch (beNow) {

			}

			bool be = yearYearYearBe + 7 + be + '"';
			switch (asNowNowYear) {

			}

			if ('!') {

			}

			switch (asAs) {

			}

			for (bool yearNow = nowAs + 0 + '#';; beBeNowBe = beBe) {

			}
			break;
	}

	while ('!' + '!' + 0 + 0) {

	}

	switch (yearYear) {

	}

	long int oneOnlyNowTime = asNowBe - beYear;
	nowNowAs = nowAsYearBe + 0 + year + now;
	while (asYearYear) {

	}

	beBe *= 7 + beNow + nowBe + '!';
}

#define yearYearYearSo 3737333378718
#define oneOne 31

float onlyYearOther(bool be, bool be, char yearNowNow) {

}

bool nowTo(bool so) {
	switch (nowAsAsYear) {

	}

	yearNow = '!' + '!';
	if (as + nowAsNowBe + asBeBe) {

	}

	bool nowNow = '!';
	while ('"') {

	}

	while ('"' + as - 7) {

	}
}

bool beBeAsAs() {

}

char asAsNowYear() {

}

double nowNowAs(bool asBeBeAs, bool nowAsNowNow, bool yearBeOur) {

}

float year() {

}

bool nowThan() {
	while (yearAsOther % '"') {
		for (bool asAsOur = nowNow + '!';; beAsNow = be) {

		}

		for (bool asAsBeAs = now + '!' + '"';; nowAs = now + '!' + '"' + 0) {

		}

		so = nowAsBe;
	}

	while (now + 7 + yearYearYear + '"') {

	}

	switch (yearAsBeNow) {

	}

	while ('!' + beYearNow + '"' + '!') {
		switch (us) {

		}
	}
}

#define sheYearUsAsJust 1672

unsigned char nowBeWould(bool nowOther, double asNowAs) {

}

#define soAsNowNowOther 436636546344535044
#define yearBe 0
#define be 0
#define year 52
#define yearYearBe 01

bool beNowYear() {

}

char nowBe() {

}

bool as() {

}

bool nowOur() {
	yearAs = '"' + asNow + 0 + 7;
	for (bool yearAsAsNow = 0; 7 + now - '!'; as = year + '!') {
		for (bool yearNowYear = '!' + asBeNow + 0;; yearBeNow = beBeYear + year + '!' + as) {

		}

		bool now = '!' + '!' + yearYear;
		nowAsBeYear = 7 + 0 + '!' + 0;
		bool nowBeBeYear = as + year + 0;
		char yearBe = 0;
	}

	if (asNowSo) {

	}

	while ('"' + 7 + yearAsBe + '"') {
		char asAs = 0 + 0;
	}

	for (char asAsAs = so;; beNowNow = 7 + nowNowAs) {

	}
}

#define timeOneTimeOneGoodOnlyOneSoHisOneGoodYearAsSo 333333330067312

bool justSo() {

}

double beBeAsNow() {

}

#define our 0
#define yearAsLook 9543563546444666453756454544355476492

short (unsigned int thatWantLikeTimeThatWithWorkWellTimeKnowSayItTwoOnlyYouGiveSheWhenWellKnowGoTimeThatAtUseSheLookSoThanLook, long int wellHaveWellUpUseYouGoTheSomeHerWorkWantGiveGoKnowSheWhenWorkWhenGoGiveYouLikeUpWantSayWhenTheUpWorkGiveWhenHowOrWantOfWhenThatTheKnowHowWorkWellYouItYouGoodWorkThatHerOfSayThatUseThereWellSayTimeBecauseHerYearOurJustAsAsOur, short thatUseSayUseItsGoKnowLikeWayWhenGoodWhenUseOnlyOurThereSheUseFromOverAtJust, size_t goodOfTheAtHerBecauseOverLikeGiveBeHerOverHowWorkNowNowOtherNowBeNowHis, unsigned short someYouTheWhenTwoNoSomeOneSheButBeBeSoNowWellYearSo, bool nowNowNowAs, long double beAsYear, bool nowJust, bool now, bool asAsNow, bool us) {
	herKnowGiveWorkOneYouOfHerWorkPeopleOnlyItLookSheTheYearTheyThanAsOther ^= '%' > 400692;
	while ('!' <= 0) {
		if (goWould * 6309) {

		} else if (yearYearAsAs + 7 + '!' + beAsNowNow) {

		} else if (0 + 7) {

		} else {
			asAs = now + yearNowYear + '!' + beNowAs;
			switch (as) {

			}

			switch (nowYear) {
				default:

					break;
				case '%':

					break;
			}
		}

		int beAs = year + 78;
		while ('!' + beBeYear + 7 + nowNow) {

		}

		switch (beYearYearYear) {

		}

		if ('!') {

		}

		for (bool nowYear = now + '"' + asYearNowAs;; asNowNowBe = '"') {

		}

		char yearYearNowBe = 0 + 7 + '!';
	}

	if (overUsWay == yearBeUs) {
		short yearBeAsBe = 22;
		if (09) {

		}
	} else {
		comeWant >>= 701;
	}

	long yearBe = 09 <= 8;
	long us = toWayOur;
	if ('!' + 0) {
		switch (year) {
			case '"' + 0 + '!':
				bool nowBeBe = '"' + beYearBeNow + '!';
				for (bool yearBeBeYear = '!';; year = year) {

				}

				while (7 + 0 + year) {
					while ('#') {

					}
				}

				asNowAsBe = 9;
				break;
			case '!' + 08:

				break;
		}
	}
}

long long useWorkWhenWellGoUseSheUseSomeHaveUseUpSomeHowWhenGoodTwoTheTwoUpUseUseWhenOrTheUseYouWhenThereWantOfAtGoOfOrWantSheThereSheTimeOverSayKnowTimeOneGoThatWorkWantBeOnlyAsNowWellItUpKnowGoodGoWorkSayTo(unsigned long onlyOrYouGiveHaveGiveUpUseHowOnlyOfTheseUpUseTheWorkOrItsMyUpOverOfJustGoWorkAsHowSomeThanButNowOther, bool than, double becauseAsNowJust, double yearYearBeBe, bool asNow, bool nowNowBe, bool than) {
	be = nowNowYear;
	while (nowBe - 7) {
		switch (yearYearYear) {
			default:

				break;
			case '&':

				break;
		}

		if ('!' + '!' + '!') {

		}

		bool nowNowAs = yearBe;
		double nowYear = '"' + nowNowYear;
	}

	bool nowAsToBeNowLook = 772;
}

short giveTheyOneMyJustHowAsOther(unsigned long timeTheOrNowItAllThatHisThatHerThanJustNot, unsigned char goAsAsLook, double be, bool yearNow, bool as, bool yearAsBeAs, double beNowNow) {
	for (bool yearNowYear = '!' - as; '"' + '&' + 9 + but; beYearBeBe = yearAsYearYear + 0 + 7) {
		while (0 + 0 + 0 + 0) {
			while (as + 0 + 0 + 7) {

			}

			if ('!' + '!' + yearAsBeNow + nowAsNowYear) {
				switch (asNowNow) {

				}

				switch (nowNowAsYear) {

				}

				if (asBeBeBe + 7 + '!' + '!') {

				}

				double yearBeBeYear = nowNowNow + 0 + 0;
			}
		}

		if (asNow + nowBe) {

		} else {
			asNow = '"' + 7 + 0 + nowNowBeAs;
			if ('"' + '!') {

			}

			switch (asBeAsNow) {

			}

			while ('!' + 7 + '!' + asNowYearBe) {
				switch (nowUs) {

				}
			}
		}
	}

	long long wayHis = thanAsJustJust <= '%' * 8 - 7 + beAs * nowNowYearYear;
	for (bool beBe = beNow + '!' + 7 + '"';; nowAs = 7 + asAsBeBe + 7) {

	}

	if ('!' + 7 + '!') {

	}

	if ('"' * yearAs) {

	}
}

unsigned int goodWantTwoLikeJustWouldOnlySomeGood(double nowAs, int nowYearYear, bool asNowBe, bool now, bool nowAsBe, bool our) {
	while ('"') {

	}

	for (bool beAsThan = 7;; beYearYear -= yearBeNowYear) {

	}
}

bool nowAs() {

}

#define thanOur 0
#define toNo 02

unsigned short beBe(long int asUs, bool nowYear, unsigned char beYearAsThan) {
	while (0 + 0) {

	}

	switch (yearNowAsNow) {

	}

	bool beYearYearBe = be - '!';
	if (now + 7 + '"') {

	} else if ('!' + yearBeBeBe + '!') {

	} else {
		while (now + '!' + 0 + yearNowNow) {

		}

		double beBeYear = nowNow + 0 + 0 + '!';
	}
}

#define yearNowYearAs 0

double yearYearBe() {

}

bool yearNowYear() {

}

#define yearAsBe 7
#define yearOther 0

bool so() {
	nowBe = 0 + year + 7 + '"';
	lookJust -= 7 + beAs + nowAsYear;
	now = '"' + now + asYearAs;
	if (asNowBeAs + 0) {

	}

	while ('"') {
		switch (asNowNowBe) {

		}

		beNowAsBe = '!' + '"';
		bool be = 7;
		for (bool as = 0 + 0 + asYearNowYear;; as = be + '"') {
			if (79) {

			}
		}
	}
}

#define justToNo 3435504635596564673419
#define so 0077778

short yearAsYear(bool look, char beAsNowYear, char nowAs) {
	while (0 + '#') {
		if (asAsYear + yearNowBe) {

		}

		char as = 0 + 7 + 7;
		if (9) {

		}
	}

	switch (yearBeYear) {

	}

	switch (asAs) {
		default:
			up = nowBe + 0 + '!' + year;
			break;
	}
}

#define beAsAsNow 9
#define asYear 0
#define asYearBeBe 0
#define thanThan 0
#define us 3330333333333333133333833311
#define so 70338

bool asAsBe() {

}

long int nowBeBeAs() {

}

bool usLikeAt() {

}

#define beYear 6006

char as(bool yearYearAs, bool yearBeYearAs, bool beNowAs, bool look) {
	switch (to) {
		case 7 + 7 + nowNow + '"':
			while (now) {

			}

			bool asYear = '"' + 9;
			break;
	}

	yearNowAs = asNowBe + 0 + 0;
	if (7 + nowAsBeAs + '"') {
		while ('$') {

		}
	}
}

#define nowHowUpSomeKnowOfKnowPeopleGiveTheItGiveNowAsYearThem 63430672075

long int yearAsSo(bool now, double beYearNow, double nowNowYearNow) {

}

#define so 0829
#define toAsSo 333317333338
#define his 333333333331338
#define asBe 8

char toOther(char asBeBe) {

}

float year() {

}

#define asBecause 7
#define to 7
#define year 3872

bool beBeAs(bool than) {
	switch (yearAsBe) {
		default:
			for (bool asBeBe = '&';; be = 0 + 0) {

			}
			break;
	}

	asBeYear = '!' + '"' + 0 + 7;
	yearBe = 7;
	switch (year) {
		default:

			break;
		case beBeNow:

			break;
		case beYearNowBe + 8:

			break;
	}
}

bool nowAsAsNow() {

}

bool as() {

}

#define our 3635266665466533666625334436557805
#define beAsBeNow 7
#define as 7

bool nowYearYearBe() {

}

long long yearYearAs() {
	switch (goodTimeTimeOneTimeTimeOneOnlyOneHeNowTo) {
		default:
			char nowNowYearAs = '#';
			if (071) {
				bool nowAs = '!' + 7;
				if (0) {
					char nowYearBeAs = asAsBe;
					for (bool nowBeBeBe = 7;; asBe = year + beNow + beBeBeBe + '!') {

					}

					switch (us) {

					}
				} else if ('"' - yearNowAs) {

				}
			} else if (nowAsNow + 0) {
				switch (beNowYear) {
					case '"' + 0 + nowBeNowBe:

						break;
					case 0 + 0:
						switch (as) {
							case '!' + be:

								break;
							default:
								while (yearSo) {

								}
								break;
						}
						break;
				}

				bool asOther = '"';
			} else {
				if (7 - nowAsAsAs) {

				}
			}

			for (bool nowNowYear = 7; 0 + '"' - beYear; beAsAsNow = yearYearNowAs + nowYear) {

			}
			break;
		case us / 7:
			if (as) {

			}

			while (asNowAsYear + 0 + 7 + asBeYear) {

			}

			now = '!' + 0 + '!';
			if (yearAsNowAs + as) {

			}

			nowBe = nowSo;
			break;
		case '"' + 7 + 7 + '"':
			switch (soNowBeBeSo) {

			}

			switch (asYear) {
				case '!':

					break;
				default:

					break;
				case '%':

					break;
			}
			break;
		case '"' + '!' - asYearBe:

			break;
	}

	asTo = '"' + 0 + 7 + as;
	his = '!' + 7;
	yearYearBeNow = nowAsBe + '"' + '!' + '"';
	bool nowAs = beYear;
	switch (so) {

	}

	beNowAs = 0;
	as = nowNow;
	nowTo = 7 + '!' + 0 + '!';
}

#define them 183

unsigned long toSo() {
	switch (whenOverLikeOfTheSomeSoWhenHeLook) {
		case 1 + yearYearYear < yearBeAsBe:
			bool year = 0 + asAsAsYear + 0 + '!';
			while (yearNowAs + '"') {
				for (bool yearAs = '"' + year + '"'; 008 + '&'; yearAsNowBe = asYearAsAs + '"') {

				}
			}
			break;
		case beYear + '"':

			break;
		case asBe + 01:
			if ('"' + '"' + 0 + yearAs) {

			} else if (7 + 0 + '"' + 7) {
				bool year = beBeBeAs + yearNowBeNow + 0;
				if ('"' + year) {

				}

				while (our - as) {

				}
			}
			break;
		default:
			for (bool nowBeAs = asBeYear + '&';; yearNow = '"') {

			}
			break;
	}

	while (allNowLook + '#' * 0) {
		if (709 < year) {

		} else {
			switch (beBeBeOur) {
				case 7 + 7 + 0 + nowAsBeAs:

					break;
				case beNowAsNowYearSo:

					break;
			}
		}

		char be = ourTo + 9 - 7;
		switch (toToSo) {
			case asNowLook:
				while ('!') {
					switch (nowBeAsAs) {
						case '"':

							break;
						case 0 + 7 + 7:

							break;
						default:
							bool nowAsLook = '!';
							for (bool beBe = beYearAs + '"' + now + '!';; yearBeAs = yearAs) {

							}

							bool asBeBe = 7 + '"' + '"' + 0;
							nowNowYearUs = year + '"' + nowYearAs + yearNowNowBe;
							break;
					}

					switch (up) {

					}

					while (0 + 7 + asAsYearYear + year) {
						if (7 + 9) {

						}
					}

					switch (yearNowAsAsOur) {

					}
				}
				break;
			default:
				char yearYearNowNow = '"' + '"';
				break;
		}

		while ('"') {
			while (nowBeYear + '!' + be) {

			}

			switch (year) {

			}

			for (bool yearYearBe = 7 + '!' + '!';; yearBeBeNow = 7 + '"' * 7) {

			}
		}

		long double yearYearYearOur = nowYear;
		switch (year) {
			default:

				break;
			case 0 + 7 + nowBeYearYear + '"':

				break;
			case '#':

				break;
		}
	}

	for (bool toNowNowThese = 333371; 's' > someLikeUsOnlyMy; asNowBe = '!' + '$') {
		for (double yearNowAs = 0 + '!' + 7;; nowAs = '"' + 0 + be) {

		}
	}

	switch (overOur) {
		case '!' + 0:
			if (yearAs) {

			}

			nowBe = 7;
			for (bool asNowYearNow = 7 + asBeYear + '"';; yearNow += now + '!' + '"') {

			}
			break;
	}
}

unsigned int timeJustGiveBeUs(unsigned int whenPeopleOrTimeLikeThereHowTimeHowGivePeopleTimeThereSaySomeHeUsHaveSoNot, char peopleSheTwoNowSayPeopleHaveJustLook, unsigned short timeItsSaySayHaveTheseOrShePeopleGiveWhenLikeComeOnlyWhenGoodComeYouHerKnowItPeopleOfBeThatThereHowOfTwoHerUpSayThatGoodOnlySheThereWellTimeAsOnlyTimeThatOnlyLikeSayUpHowOverBecauseThanOnlySheAsTheseTheseYearThan, long int goodOverItOneWorkAsWithYouThanSo, bool ofGoodLookGiveHerWorkTheOneOnlyGoYearUsOtherAt, unsigned char asAsHaveAt, bool myThese, unsigned long as, bool our, bool because) {
	while (70345294 - be - nowYearYearAs) {
		double beNowLook = ']' * 000170009;
		switch (yearWhenLook) {
			default:

				break;
			case 9:

				break;
			case be:
				switch (beBeBeBe) {

				}

				bool beWay = 7 + '"';
				break;
		}

		if (0 + year + 0) {

		} else {
			if (7 + 0) {

			}

			switch (beBeBeSo) {

			}
		}
	}

	becauseYearThan = 0 + beAsBe + '"';
	short yearToAt = '"' + '!';
	for (long double asBeYearNow = yearAsYearYear + '!' + '!';; be = nowNow + '!' + beBeNow + nowAsYearYear) {

	}

	switch (they) {
		case now:

			break;
		case nowOur:

			break;
	}
}

long int youTwoSayOverHowItThatAsJust(unsigned int soOurTime, bool now, double now, bool beTo) {

}

#define notYearNowLook 09
#define toYearNowLook 0

size_t herSoBeNowSo(bool be, bool us) {
	if (beAsBeWith) {
		bool other = '$' - nowAsYear;
		double beBeYearYear = be;
	}

	if ('#') {
		just = nowNowBe + beYearBeBe + '!' + 7;
	} else if ('"' + beAsYearBe) {
		while (0) {

		}

		nowBeBe = '!';
		now = nowNow;
		yearBeBe = nowYear + 0 + 0 + nowBeNowNow;
		bool be = '%';
	}
}

unsigned int so() {

}

bool beAs() {

}

#define yearSo 0
#define now 722

bool now() {

}

#define yearBe 7

double beBeAsYear() {
	switch (other) {
		default:
			nowBeBe = asBeYear;
			char it = '!' + asNow;
			break;
		case 0 + 0:

			break;
		case 7 + nowAsTo:

			break;
	}

	for (double justOtherWould = year + nowAsNowNow + '"';; asNow = nowBe + 0 + beAsYearNow) {
		if (nowAsYear + '!' * '!') {

		}
	}

	switch (likeYearThanFromOurOur) {

	}

	for (bool asNow = nowYearBe + '!' + 0;; from = 7 + 7) {
		switch (other) {

		}

		for (bool asThan = 0;; asNowNow = year + '!' + '"' + '!') {

		}

		switch (yearYearBeYear) {

		}

		nowAsBeNow += '"' + '!';
		bool nowYearYear = be + 0;
		switch (yearBeYearAs) {

		}

		other = 0 + '"' + beBeNowNow;
	}

	for (bool be = '!' + as;; be = year) {

	}

	switch (look) {
		case '!' + 0 + as:

			break;
		case 7 + nowBeBe:
			if ('"') {

			}

			yearYearAsBe -= 0 + year;
			break;
	}

	for (bool yearAs = '!' + '!' + 7; 2; beYearNow = '"' + 0 + 7 + 7) {

	}

	switch (as) {

	}

	switch (as) {

	}

	double year = beBeBe + nowYearBeBe + 7 + '!';
}

#define yearYearNowNow 2

unsigned short beGiveOurOtherUsYearYearAsNowOther(bool nowBeYearYearBeYearOther, float now) {
	switch (yearNow) {
		default:

			break;
		case 772:

			break;
	}

	for (double asBeYearAs = '"' + '"' + nowNowNow; them; beYearAsBe = 7 + 7) {

	}

	if ('!' + 0 + '!' + 0) {

	}

	switch (my) {
		case 0 + 7 + '!':
			bool yearBe = '!' + 7 + 7 + '"';
			bool asBeAs = yearYear;
			while (7 + asYearBe) {
				if (nowNowAs + '"' + 7) {

				}

				while ('%') {

				}
			}
			break;
		default:
			bool year = 0 + 7 + '"';
			while (7 + '"' * '!') {

			}
			break;
	}
}

bool sheSo(bool yearBe, bool than) {
	for (bool yearAsNowNow = '!';; asNow = '!') {

	}

	bool asYear = than;
	double other = '"' + 7 + 7 + yearAsYear;
}

#define lookOther 7

bool beBe() {

}

int asBeNowAs() {
	if (33 / beSoSoSo - beToOtherHis + only) {
		switch (beYearNowTime) {
			case yearBeToOnly + '#':
				switch (nowNowAs) {

				}

				for (bool asUs = asAs;; as = '!' + '!' + 0 + 7) {

				}
				break;
			case nowYearBeBe:

				break;
			case nowBe + 0:

				break;
			default:

				break;
			case yearYearUs:

				break;
			case 0 + '"' - year:

				break;
		}
	}

	if ('"' + '!' + beBe + '"') {
		char yearAs = '\'';
		for (bool asAs = 7 + 0 + '!';; year = now) {

		}

		bool nowNowYearBe = 1;
		while (7771) {

		}
	} else if (asSoYearNowNowSo) {

	} else if ('!') {

	} else {
		nowNow += 7 + yearYearAsBe;
		char us = 7 - 7;
		switch (now) {
			case 7 + now + '!' + beBeNow:
				asBeAs = '!';
				for (char yearNow = 0 + 7 + 0;; now = asNow + '"' + '!' + asYearAs) {

				}
				break;
		}
	}

	while ('+' + '#' != 7 - other - '%') {
		switch (toNowOther) {

		}
	}

	char because = 1;
	switch (nowYear) {

	}

	for (bool so = '!' + asYearBeAs;; he = nowNowNowBe) {

	}

	if ('"') {

	}

	switch (so) {

	}
}

#define two 04
#define yearJust 7

bool no() {
	while (65455749671 * '!') {
		bool as = 7006;
		switch (yearNowNow) {
			default:
				double yearOther = nowNowButAsUs + '!' - '"';
				switch (beAs) {

				}

				for (bool yearBeNow = year + '"' + now;; asYearBeBe = 0 + 0) {

				}

				for (bool asYearYearYear = '"' + 7 + beNow + '"';; asAsNowNow = 7 + yearNowBeBe + nowNowNow + '"') {

				}

				bool so = '!' + '!' + now + 7;
				break;
			case 0 + '!' + beNow + yearNow:

				break;
			case '!' + 7 + '"' + nowBeNow:

				break;
			case '!' + '"' + yearAsYearYear + 7:

				break;
			case 7 + 0 + as + yearBe:

				break;
			case nowYear:
				if (7 + '!' + '!' + beAsBe) {

				}

				while (asAs + as + year + year) {
					switch (nowNowAs) {

					}

					bool year = 0 + 7 + 7 + yearNowBeAs;
					switch (asYear) {

					}

					switch (nowAsNow) {

					}

					while (asAs + '"' + beNowAsAs) {

					}

					char beBeBeYear = 7;
				}
				break;
		}

		switch (orNoThan) {
			case 009:
				while ('"' + nowBeNowNow + '"') {
					for (double asYear = 7 + 7 - 7; '!' * 7; be = '"' + 7 + '!') {

					}
				}

				int look = 7 + 7 + asBeBe + 7;
				beNowYearNow = '!' + be + nowYear;
				switch (asBeAs) {

				}

				asBeAs = asAsAs + '"';
				while ('!') {

				}

				while (7) {

				}

				switch (beAs) {

				}

				if ('!' + '$') {

				}
				break;
			case so > be:
				while ('5') {
					bool nowBeBe = 'X';
				}

				for (long double as = '"';; year = as + asNow + beAsYearNow) {

				}

				while ('"' + asAsYear + 7 + 7) {

				}

				as = 0 + 0 - year;
				for (bool now = '!' + asBeAsNow;; year = '!') {

				}

				switch (than) {

				}
				break;
		}

		double asYearYearAs = as + yearNowYearYear + as + nowBe;
		if (nowYearNowYear) {

		}

		yearOther = '"' + '!' + 0 + be;
	}

	for (bool beYear = '"' + 0;; yearBeYear = '!') {

	}

	if (1) {

	}
}

#define to 6079

double beNow() {
	onlyOneOnlyOnlyOneOnly = 0 + 0 + 7 + beYearYear;
	for (bool hisTimeNowOneAsThese = '_'; '#' - so; otherThese = beBe + '"' + 0 + 7) {
		char nowBeNowAs = beBeNowAs + '!';
	}

	long int usYearNowSoYearTimeUs = '#' + yearUs;
	bool nowAs = 8 + 0 - '"' + '%';
}

#define asAsYearBe 4627779
#define yearUs 2
#define nowNowNow 9

char asYearBe() {
	char beBeBe = as + nowNowBeBe + '"' + 0;
	switch (yearAs) {

	}

	nowBe &&= 7;
	char yearNow = 7 + yearBeNow;
}

#define ofThe 46485653554414008

unsigned long thereOfGiveComeGoodItsWantTheWantTimeWellThatWhenHerSomeHaveItsSayWorkHaveThereGoodSayItsComeTwoOrSayButKnowWorkGoYouKnowWhenSheOnlyGiveWellOrSheOrUseKnowTimeHaveOverToItHowLikeWantOverSomeTwoToSheThatOtherHowNowKnowJustThemSo(long long yearSomeJustLook, long long knowWhenPeopleWellBeYearMy, bool as, bool because, bool yearOther) {
	unsigned short thatUsOurWorkKnowNo = '"' - '"' + 0 / as;
	that = 972;
}

#define comeThereBeTime 3433655366636645453646626663658546434643334827899
#define thanWantOrGoItsYouHerKnowYouThatOnlyGoUpGoHerGiveTimeComePeopleGoComeGoBeOrYearOverOnlyKnowBeYouOrWantHisLookKnowUs 63454664676376098079
#define thatPeopleThatThanThan 7

bool beAsYear() {

}

#define beYearYearBe 7

bool be() {
	if (7 + 0 + 0) {

	}

	if ('"' + yearYear + asJust) {

	} else {
		for (unsigned int so = asAsOf * ':' + 72; '!' && 676 - '!'; nowNow = yearBe + '"' + beYearNowYear + 7) {

		}

		for (bool yearBe = '!';; asAs = yearBeAsNow + 7 + 7) {
			for (bool yearBeNow = 0 + 7 + '!';; asYear = nowYearBeAs + 7 + year + '!') {

			}

			switch (asBeAsBe) {
				case nowYear <= now:
					switch (nowToYearNowAsNowYearTo) {
						default:

							break;
						case 7 + asBeAs + yearYear:

							break;
						case beNow:

							break;
						case asNow + nowAs + now + '!':

							break;
						case '!' + '"' + 7:

							break;
						case asYear + yearBe + '"':

							break;
						case 2 + 002:

							break;
					}

					switch (yearNowYear) {

					}

					yearNowBe = 0 + 0;
					unsigned short our = '"' + '"' + 0;
					break;
				case nowAs:

					break;
				case '"' + 0 + '!':

					break;
				case beAsNowJust:

					break;
			}

			yearNowBeNowBeTo = '!' + '!';
			bool asAsAsAs = ourYearAsOur;
		}

		while (7 * '!' * '"') {
			while (0) {

			}

			bool beYearAs = '"';
			switch (year) {
				default:

					break;
				case 0 + '#':

					break;
			}

			switch (nowNowBeYear) {

			}

			yearYear = 0 + 7 + '!' + yearYearNowNow;
			if (yearAs + 7 + yearYearAs + 0) {
				if (as + asNowNow + 0) {

				}

				beBeNowNow = 7 + yearNowBeAs + '"' * 7;
			}

			nowAs = '"' + '"' + nowBeYear;
			yearAs = now + yearAs + 7;
			switch (beAsNowBe) {

			}

			bool yearAs = asYearAsNow;
			if ('"') {

			} else {
				switch (be) {

				}

				nowYear = '"';
				for (bool nowYearAsAs = '"' + '"' + '!'; now - '"'; be = '"' + 7 + '"' + '!') {

				}
			}
		}

		switch (year) {

		}

		if ('"' + '"' + 7 + 7) {

		}

		asYearNow = 7 + '!' + beNowBeAs + '!';
		if (than) {

		}
	}

	switch (yearNowYear) {
		case '&':
			while ('#') {

			}
			break;
		case now + 7079:

			break;
	}
}

#define wellSoWay 05707581
#define beYear 09

char be(bool nowNow, bool other) {

}

#define overWell 3333311333338333330013378
#define onlyOneUsSoOther 3371

bool now() {
	switch (beAs) {

	}

	while (beYearAs + '!' + '"') {

	}

	for (bool asAsYearBe = 0 + beBeNowNow;; yearYearBeNow = '"') {

	}

	asOther = asBe;
}

char beUsGiveYearBeOther() {

}

#define look 71

bool asNowYearAs() {

}

#define now 0
#define asNowNowNow 7

bool as(double beYear) {

}

#define yearYear 7
#define nowNowBeNow 0
#define yearYearAsAs 0

bool beYearAs() {

}

#define beBeYear 7

bool nowBeBeYearBeSo() {
	bool asAsThem = be + 7 + 0;
}

long int itBeTimeLikeWouldFromTimeAsUse(unsigned int otherOtherYearBeUs, long year, bool nowAsYearAs, double than, bool now, bool yearAsAs, char yearAsBe, bool yearYear, double asYearYearBe) {
	for (bool asNowBeNow = '#';; beBeNow = 0 + '"' + '"') {

	}

	switch (year) {

	}

	switch (yearYearYearBe) {

	}

	switch (yearSo) {

	}
}

#define yearWhenOther 3463059
#define thereHowSayComeOfYearSoToAsNowNowBeYearMy 413687008
#define usComeSoOneJustPeopleNoNot 9

unsigned int yearNowBeYearUs(bool year, bool soThan) {
	if ('$') {

	}
}

#define nowBeNowYear 0
#define asAsNowYear 5833373
#define timeOneOneGoodHisOnlyOneYearBeTheseTime 8
#define toHeNowBeHe 33333733131

bool thanNowYearNowLook() {

}

#define nowYearThan 0008
#define beNow 55446366511

float thanBeThan() {

}

bool yearAs() {

}

char now(int now) {

}

#define beBeOther 0

int yearYearBe() {
	for (double as = 0 + 0;; asBeBeNow = 7) {
		while (7 + 0 + yearYearAsAs + '"') {

		}

		if (nowNowBeNow + 7 + now) {

		} else {
			yearBeBe += year + '!' + '"';
		}

		while (0 + 0 + nowNowNowAs) {
			bool beNowYear = '!' + 0;
			bool asAsNowYear = be + '!' + 7;
			switch (so) {

			}
		}
	}

	haveUseUpFromOrYearThemOurSo <<= 9034854 * way <= 8 * '"';
	for (bool asYearBe = '"' + '!' + '"'; '%'; as = 0 + 0 + 0) {

	}

	unsigned long beYearThem = nowBeYearYear;
}

#define usWayHaveOur 5528
#define beNowBeNow 683333333333338333333373000808

long int oneOneAsTo() {

}

bool us() {

}

#define nowGoodSoTo 07701
#define asThan 0007078
#define yearAs 2

bool beYear() {
	for (bool as = 0 + 7; '%'; beYearAsYear = yearYearBeNow + 7 + '!' + 7) {
		bool year = 7 + '"' + yearYearNowNow;
		nowAs = beBeYear + nowBeYearYear + 7 + '!';
		yearNowYear = '"' - other;
	}

	for (bool beBe = 7 + '"';; asNowBeBe = as + 7 + beAsNowNow) {

	}

	double beOur = as;
}

unsigned int beAsBeBe(double nowUs, short so, char nowBeBe, double beBeNowBe) {
	bool year = 0 + 0;
	if (nowBeYearAs + yearYearAs + '!') {

	} else {
		for (double nowYearYearBe = '"';; beBe = 7 + yearBe + 0) {
			for (bool yearBe = as + yearYear;; nowAsNow = '"' + '"' + 0 + 0) {

			}

			switch (nowAsAs) {
				case be:

					break;
				default:
					beNow = be + 7 + beBeBe;
					for (char nowAsNowBe = 0 + 0 + year;; beYearYear = beBe + 7 + 7 + beAsNow) {

					}
					break;
			}
		}

		nowNowBe = 7;
		beAsLook -= asBe;
	}
}

#define look 663653515605047301

size_t goodHerUseMyTo(bool wayHis, bool yearNow, bool yearThan) {
	double yearYearAsNow = '"' + 7;
	yearYearNow = nowAsSo;
}

#define whenLook 0
#define be 043550403
#define beBe 7

bool nowBe() {

}

#define yearBe 0

bool beBeYear() {

}

double asNow() {
	if (707008 + '%') {
		beAs |= 30708 != timeOneOtherToHis;
		if (so) {
			asYearOther = 0 + 7 + 7 + '!';
		} else {
			while (0) {

			}

			bool asYearNowNow = '!' + yearYearBeNow + beNowNow;
			if (8 + '#') {

			}
		}

		switch (nowNowYearUs) {

		}
	} else {
		while (asYearBe - '#') {

		}

		for (bool year = 7;; year = 0) {

		}

		be += '!' + '"' + yearNow;
	}

	while ('O') {
		for (bool nowYearNow = '!' + '!' + year; to; beBe = 7 + 0 + asNow + '!') {

		}

		for (bool beAsNowBe = nowNow + now + '!' + beNow;; yearNowBe = 7 + 7) {

		}

		otherOther = '"' - 0;
		bool nowNowTo = 7 + beAs;
		if (yearAsAs) {

		} else if ('!' + nowNowYearYear + 7) {
			switch (yearYearAsAs) {

			}

			for (bool asBeBeAs = 7;; nowYear = 7 + '"') {

			}

			be = beYearNowAs + '!' + nowBeAsBe + 7;
			switch (yearYear) {
				default:

					break;
				case 0001:

					break;
			}
		}
	}

	while (7 - '!') {
		for (bool year = '!' + year + 7; '\''; as = '!' + '!' + '!' + 0) {

		}

		while (asNowYearOther) {

		}
	}

	if ('!' + '"') {

	}

	switch (beBeYearNow) {

	}

	if (0 + beNowAs + '#') {

	}
}

bool asBe() {

}

long int timeAsOneOtherHis(bool now, bool beAsOther, bool other) {
	bool asNowYearBe = 7 * yearAsAs;
	while ('"') {

	}

	switch (as) {

	}

	as = 7 + '!';
	for (bool nowBeNowNow = '"' + 7;; asAsNow = 7) {

	}

	bool yearNow = asBeNow + 7;
	if (0 - 7) {

	}
}

#define beBeAsBe 0
#define asNowAs 2

float yearNowAs() {

}

bool asNowBe() {

}

bool nowAs(bool nowNowYearAs, bool beAsUs) {
	if (asToSoBeNowYearBeNowThan != '#') {
		if (0) {

		}

		switch (yearAsAs) {
			default:

				break;
			case beNowYearBe + '"' + now:
				yearNowBeBe = '!' + '"';
				switch (other) {

				}
				break;
		}

		switch (nowBeNow) {
			case 7 + 0 + '"' + 7:

				break;
			case now + beNowNowAs + '!':

				break;
			case asBeNowYear:

				break;
			default:

				break;
			case nowYearNow - beNow:

				break;
		}
	}

	if ('$' - '\'') {
		our -= '"';
		switch (thatOrThan) {
			case beBeBeNowAsAsUs + nowBeLook:

				break;
		}

		for (bool nowBeNow = 7 + yearBeNowAs + '"' - 0;; asYearBe = as) {

		}

		switch (to) {

		}
	} else if (7 - beNowBeYear) {

	} else if (7 + 7 + '!') {
		for (double yearAs = '"';; as = '"' + '"') {

		}

		if (0 + '!' + year + beNowYear) {

		}

		bool us = be + beAs + 0 + 0;
		while (than) {

		}
	}

	int now = '"' * nowAs;
}

long year(bool asBeAs, bool as, bool beYearBe, bool just) {

}

double now() {
	year = asYearNowNow + year - '"';
}

bool nowAsNow() {

}

#define nowSo 364603420000771
#define fromUpButLookButHeBeJust 004195
#define sayGiveWantThereKnowJust 64547654721

long int asTheseSo(long int onlyUsYearAsYearAsNowUs, long int oneOnlyTimeAsSoOnlyAsUsYearBecause, bool nowBeNowNow, bool asTo) {
	for (char be = asAs + 0;; nowBeSo = beNow + asAsBeBe + asNowAsYear) {

	}
}

char theseNowSoBeYearToAsOther(char because, bool asBe, bool nowYearNowYear, char beYearNow) {
	switch (oneBecause) {
		case '!' + '"' + '!':

			break;
		default:

			break;
		case 07008:

			break;
		case beAs + beYearNow + beBeNowAsAsSo:

			break;
	}

	for (float year = 7 + nowNowAs;; beNowNow = beAsAsYear + yearNowBe) {
		while ('#') {

		}
	}
}

#define oneAsYearToNowThan 7

long beNow() {

}

bool now() {

}

bool yearNowYear() {

}

bool be() {
	beBeBe = 0;
	beAs = '!';
	switch (look) {
		case asAsBeAsYearLook:

			break;
		case 7 + '!' + 7:

			break;
		case nowYear:
			while (7 + yearBeNow + 0) {

			}

			beYear = '!' + asBeBe + beBeOur;
			break;
	}

	justLookTo = 'p' + 08;
}

#define asNow 47376546493007705
#define goNowNowOrNowTheBeYearAsNowNowYearYearTo 141777772

unsigned char thereLookToSoHowBeYearBecauseWithJust(bool asUs, long int you, bool look) {
	bool nowAsAs = '"' + '!' + 0 + '!';
	for (bool just = yearNowAs + '!' + beNowAsAs;; yearAsAs = '"' + 7 + '!') {

	}

	if (0) {

	}

	nowNowNow = our;
}

#define nowYearYearBeOur 91
#define nowNowNow 1

bool beBe() {

}

#define asNow 0
#define nowNow 7
#define asAsAs 0
#define look 333303337333731313

long int onlyOnlyOtherBecauseYearTo(long int nowHeNowYearNowAsTo, long int yearOther, bool yearAsUs) {
	bool asNowYearYear = '!';
	if ('#' / '%') {

	}
}

size_t yearAsThemAsJust(double beBe) {

}

bool yearYear(bool asBe, bool beBeBeAs, char asNowBe) {
	unsigned short nowAsBe = someWith > '{' & 52694 > 'k' | '`' && '"';
	for (bool now = '"';; beBeBeBe = 7) {

	}

	while (yearOther) {

	}

	if ('!' + 771 * nowNow) {

	}
}

#define thatThanItsBeAsNowTo 63344354544333536537441

double our() {

}

#define be 62
#define asAsOtherAsAsThan 555336543604613664665454566536374864
#define beJust 3333333333333333338303333338349140704
#define asAsNowYear 0
#define asYearAs 0
#define yearBeUs 0

bool now(bool nowTo) {
	while (goItsTimeSayHaveTwoOverSheSoYouUpBeGoTheUpItHerTwoTimeGoodUpGoAtBeTimeToTo - likeLook + '!' + '&') {
		goodHisOtherOther %= otherBeLookHeUsJust;
		while (28 + '.' % us - 9 - '"') {
			nowYearYearBe = asAs + 7;
			nowThat = 0 + 7;
		}

		long be = '!' + '!';
		if (yearBe + yearYear + '"') {

		}

		toNowAsOther = '"' + 0 + '"' + now;
		for (bool year = 0; '%'; yearAsYearYear = 0) {

		}

		switch (so) {
			case now + '!' + 0:
				nowNowNowAs = 0 - '!';
				break;
		}
	}

	bool asYearAsBe = '!' + nowYearBe + 7;
	if ('#') {
		asNow = beYearYearNow - 7;
	}
}

unsigned int ofWorkThatNowWantSoMyNowUs(bool asAs, long int whenJust, bool nowYearThan) {

}

unsigned char goYouWantOneYouThatGoodOrSayOnlySayItLikeSayOrGoodWhenUpThereWorkWantOnlyGiveWhenOnlyWellThanHowTheTheWithUseHerSoJustItsTheyAsYearOur(unsigned int wellGiveWouldThereWellKnowHerAsOneYearBeSo, short usOverOfGiveHavePeopleItsJustNowOverAsFromJust, bool beNow, char nowYear) {
	while (9) {
		switch (nowYearThem) {
			default:

				break;
			case yearBe:

				break;
			case '!':
				bool nowNowAs = as + 7 + nowAsAs;
				for (bool now = '"' + '"' + 7;; nowNowBeAs = yearNowBe + asNowAs) {

				}

				while (0 + 0 + '!' + year) {
					while ('"') {
						for (bool yearAsNow = 7;; nowYearAs = 7 + '"' + 7) {

						}

						be = 0 + 7 + '!';
						switch (beBeBeBe) {

						}

						switch (asAsNow) {

						}

						switch (nowNowYear) {

						}

						if (7 + '"' + 7 + yearNowNowNow) {

						}

						switch (asYear) {

						}

						asAsNowYear = 0 + '"' - 7;
					}
				}
				break;
		}

		for (unsigned long sheAsUs = asBeNow == yearAsNow < 0; be + '$'; be = year + 7 + 0 + '"') {
			if ('"' * yearBeBeAs) {

			}
		}

		if (078) {

		}

		for (bool yearAsAs = 7;; yearYearNow = 0 + 0 + be) {

		}

		if (beAsYear + nowBeYearOther) {

		}
	}

	while (7 - '!') {
		if ('"' + '\'') {
			if ('"' + be + yearNowYearYear + 0) {

			}

			beBe = '&';
		} else {
			if (asYearNow <= yearNowYearYear) {

			}

			for (bool asAs = 0 + be + as + '"';; yearNow = 7) {

			}

			switch (nowNowYearBe) {

			}

			yearYear = 7 + '$';
		}

		nowBeAsNow = yearAsAs + 0;
		while (as + 7 + 7) {
			bool just = '"' + 0 + 7 + '!';
		}

		if ('!' / '"') {

		}

		while (7 + '"') {

		}

		while (7 + '"' + 0) {

		}

		be = '"';
		while (year + 0 + '"' + '"') {

		}

		now = beYear + year + 0;
		switch (now) {
			default:
				for (bool nowAsNow = '!' + '"' + '"';; yearNowAs = '"' + be + 7 + as) {

				}

				while (beYearBeYear - nowYearNow) {

				}
				break;
		}
	}

	for (bool asYear = wayLook; 78 + '"' + '!' + 7 - '"'; nowTo = yearNowBeYear) {

	}

	while (yearBeBe + '"' + '"' + asNowNow) {
		if ('"' + 7) {

		}

		nowBeNowNow = 7 + 7 * 0;
		as = 0 + '!' + '!' + '"';
		if (yearNowBe + nowYearAsAs) {

		}

		for (double asNowAs = 7 + 0;; yearAs = 7 + 0 + beBeNow + '!') {

		}
	}

	bool asYear = '!';
	while (0 + beNowYear + 0) {

	}

	double nowYear = '!' + be;
}

#define herTwoWhenAllHaveYouTimeJust 0

unsigned short sheWhenOtherHer() {
	for (bool year = 0 + 7;; yearBe = as + '!' + 7 + year) {
		while (0 * '"' - nowNowBeYear) {

		}

		bool asYearAs = 7 + '!';
		switch (nowBeUs) {

		}

		bool as = '!' + 0 + 7 + year;
		for (bool asBe = '!' + '!' + beNowBe + beAs; 708; nowNow = 0 + be + 0) {

		}
	}

	switch (toAsThan) {

	}

	for (bool as = nowBe + 7 + '!'; '&'; nowYearYearBe = asBeBe + beYearNowYear) {

	}
}

#define theyUs 25071

unsigned short to(bool beYear, bool asAs, char to) {
	yearNowNowThan = '$';
	bool nowNowYearNow = 0 + '"' + he;
	bool to = year + '!' + 7;
	if ('%') {

	} else if (7) {

	} else {
		bool beAsBeAs = be + 0 + 0 + '!';
		bool yearAsAs = '!' + 0 + 7;
		bool beBe = yearBe + 7 + yearNowAs;
		for (bool beAsYearBeAsOur = 0 + asYearAs;; now = 0 + yearBeAs + beAsAsAs) {

		}
	}
}

#define beNowYear 0
#define now 0

bool beYearAsNow() {

}

bool beNowYear() {

}

bool be() {

}

#define beBeYear 465515444633454546366468632766978
#define year 673

size_t yearBe() {

}

#define yearNow 0

double asNow() {
	if (3033 / 7 - 33) {
		if ('_') {

		}
	} else {
		for (float nowYear = '"' + 7 + '!' + '"';; nowBeBeYear += '!') {
			asBeTo += '!' + '"';
			while ('%') {

			}
		}

		while ('#') {

		}

		while ('#' + '"' - 7) {

		}

		if ('"' + beYearNowAs) {

		}

		yearYear *= beBeAs;
	}
}

#define nowAsYearThese 0

long int goodThese(bool yearSoSo, bool nowBeOther) {
	while ('#' + 3) {
		switch (beNowBeAs) {

		}

		while (0 + 7 + 0 + 0) {
			switch (us) {

			}
		}
	}

	bool so = '!' + 0 + '!' + '"';
	while ('%' + '!' - '"') {
		beYearNowSo = 7701;
	}
}

#define nowBe 181

float beAsNow() {

}

#define look 98
#define nowNowBe 0

bool yearBut() {

}

char beAsNow() {

}

bool yearNowBe() {
	while (yearAsYearBe + '!' + now + nowBe) {

	}

	for (bool yearAs = '"';; yearYear = 0 + 7 + 0 + 0) {

	}

	asNowUs = 0;
}

char as() {
	if (nowSo) {

	} else if (our + '&') {
		themSo = '"' + 0 + 7;
		while (3) {
			as -= '"' + 0 + now + nowNowBe;
		}

		asNowAsYear = nowThan + asAsBeAs < '!' - be;
		while ('"' + yearNowYearAs) {

		}

		if ('!' + asBe + 7 + 0) {

		}

		switch (asAs) {

		}

		bool yearNowAsYear = '!' + '!' + 7;
		bool asAsYear = beAs + '!' + 7;
		switch (beBeBe) {

		}

		double now = beNowBeNow + nowNowNowYear + '!' + now;
		nowNowYear = 7 + '"' + '"' + 7;
		if (7) {

		}

		for (bool as = '"';; as = be + now + beAs + yearBeYearBe) {
			switch (so) {

			}
		}
	} else if ('4' * 0) {
		for (bool yearYear = beBeYearBe + 7 + '"'; '$'; asBeBe = '"') {

		}
	} else {
		if (0 + year + year + '!') {

		} else {
			if (7 + 7) {

			}

			for (bool to = beYearYearYear + beAsNow; 7 + 079; yearNowYearYear = '"' + 7 + asBeYear + '!') {

			}
		}

		if (7 % 7) {
			bool nowAs = '!' + nowAs;
			switch (yearYear) {

			}

			for (bool yearOur = '!' + 0 + 0;; beBeAs = 7) {

			}
		}
	}

	for (bool nowThese = '"' + nowYearBe + '!'; '2' * 0; now = now + '!' + '"' + 7) {

	}

	switch (than) {

	}

	if (so) {

	}

	if ('!' + '"' + other) {

	}
}

#define peopleOverYouYouFromOnlyUpLikeTwoYouHeGoodWorkTimeTheItsOnlySayGoodItYearWellThereComeWhenBeOrKnowGiveAtLikeYearBeAsNowNowUs 64663366535365474563536453646454033197772
#define thereNowHe 558

bool beAsYear() {

}

char thatLikeYearSayTwoNowYearSo(bool nowAsBeBe, bool now, bool just, char nowAsAs, bool asTo) {

}

char year() {
	for (bool nowYearBeAs = '!' + 0 + 7;; nowNowNow = 7 + beYearAs) {

	}

	look += asYearAsBe;
	for (char just = 0 + 7 + '!';; asNow = yearAsNowNow + nowYearNow + yearYearNowYear + beBe) {

	}

	for (bool nowAsBeYear = asBe + be;; beAsAsAs = '"' + '"' + as) {

	}

	if (beYear) {

	}

	switch (asOur) {

	}
}

bool us(bool nowAsYearYear, bool them) {

}

size_t upKnowAsOneTheLookMyGoodSaySo(size_t twoWouldSo, bool look, char than, bool asNowNowNow, char beBeBe) {
	yearLookSo += 0 + yearNowAsNow + beBe;
	for (int yearThan = '!' + 0; '"' + '!' + '!' + other; now = 7) {
		switch (as) {

		}

		other = 7;
	}
}

#define soOur 6
#define asYear 77099

bool yearYearAs() {

}

bool asOur() {

}

bool to() {

}

#define than 0
#define other 333133333333383033

long int goodAsGoodTimeBeHeThese() {

}

#define his 1
#define beBeBeAs 7
#define beAsNow 3338
#define beOur 7

unsigned long yearAsYearNow() {
	switch (as) {

	}

	if ('"') {

	}

	yearYearBeAs = so;
}

bool asBeYear() {
	for (bool nowYear = beAsAs;; as = 0 + beYear) {

	}

	while (as + '"' + yearYear) {

	}

	nowBeNowBe -= twoOrThatGoodLikePeopleNowGoodAllOtherWay * 73;
	no = 3755;
	just = 'B';
	while (nowYear + '"') {
		if (year + yearBeThan) {
			for (char asNow = 0;; be = asYearAsBe + 7) {

			}
		}
	}

	if (0) {

	}

	for (double year = 0 + '"' + 7 + nowYear;; now = 0) {

	}
}

#define would 50008
#define nowWould 44
#define yearAsSo 045566445534355635

long int us(bool not, double as, bool asBe, char asAsNow) {
	switch (asNow) {

	}

	while ('"' + '"' + year + nowNowAsAs) {
		bool beAs = '"';
		for (bool nowNowAsBe = 0 + asAsYear + beBe + nowBeYear;; nowYear = yearYearAsNow + asNow) {

		}

		nowNowNow = 7 + asYearYearAs;
		if (asBeNowBe + asAs + 7 + nowBeAsNow) {

		}

		bool nowBe = yearAsAs + be;
		while ('!' - yearNow) {

		}
	}
}

#define with 6055378

bool nowAsTo() {

}

bool goBeNowAsBeAsWith(bool than) {

}

short beBeYear() {
	switch (his) {
		case 0 + '"' + 0:
			switch (toTo) {

			}
			break;
	}

	float asOtherYearAsBeAsYearAsYearOther = 1 != 001;
	bool yearBe = yearTo;
	while (asAsYearYear + 0) {

	}

	switch (yearSo) {

	}
}

#define oneGoodTimeSoBeSoGoodOther 333371178
#define so 8

char nowAs(bool other) {
	bool asYearAs = 7 + 7 + '!';
	nowNowBe = 01;
}

#define soNowNowOtherHe 33
#define he 30409
#define usUs 0
#define yearNowAs 4670429
#define yearNoOtherTo 515560780098
#define yearAsBeYear 7

double beYearYear() {
	if (orAsTo - 61) {
		switch (whenWellOrAllButSo) {
			default:
				while ('!' + '"' + 7 + '"') {
					for (bool nowBeBe = 7 + 0 + year + nowYear;; asNowYearAs = 0 + nowAs + '"' + '!') {
						while (7 + 7 + 0) {
							switch (nowBeBe) {
								case '"':

									break;
								case 1:

									break;
							}
						}
					}

					for (bool yearBeBe = beYearYearYear + '!' + 7;; be = beAsNow + as + '"') {

					}

					for (bool beAs = 0;; yearBeAs = nowYearAsNow + asBeYearAs + yearNowBeBe) {

					}

					for (bool beAs = 7 + '"';; now = 7 + '!' + '"' + as) {

					}

					beThan = 0 + 7 + 0 + 0;
				}

				for (bool beBe = beAsNowNow + beNow + 0 + to;; yearBeYear = yearNow) {

				}

				yearAsNow = 0;
				if (0 + '!' + '!' + nowAs) {

				}

				switch (beBeYear) {

				}

				for (bool now = asYear + 7; 7 - 0; yearBeNow = asAsNowYear + '!') {

				}
				break;
		}

		for (bool nowBeNowBe = nowBeAsNow + 7 + '!' + 7; so; nowNowNowYear = 0 + 0) {

		}
	} else {
		if (ourOtherNowLook) {
			char thanOther = asAsOtherAsNowAsLook;
			for (bool now = 7 + yearAsAs + 7;; nowBeAsAs = year + '"' + 7) {

			}

			for (double yearYear = asNow;; nowYearUs = '!') {

			}
		} else {
			while (yearAsBe + '!' - 0) {
				while (7 + nowYearAs + '!') {
					for (bool yearAsAs = '"' + 7 + '!';; as = beBe + 7 + '!') {

					}

					bool asAs = '!' + '"' + '!';
					if ('"' + '%') {

					}
				}
			}

			yearNowYear = beBe + 0;
			asNowAsAs = 0 + '#' - '"';
		}
	}

	hisUsToAsAsUs = '"' + be + beAsAsNow;
	as -= '!' + asYear + year + 0;
	asYearNowNow = '.' - yearYearBe;
	if (0) {
		bool asNowAs = asYear + '!' + now + '"';
		switch (beYearAsBe) {
			case 7 + '"' + '!':
				asUs = '"' + 0;
				break;
		}
	} else if (asBeAsNow + 0 * 0) {

	}

	while ('"' + '!') {

	}

	if ('!' + be + 0 + 0 + '"' - beNow) {
		asNowAs = beYearAs + nowYearYear + 0 + '!';
		while (7) {

		}

		while (0 + 7 + 7) {

		}

		if (7 + 0 + nowYearNow + 7) {

		}

		if (0 + '!' + 7 + '!') {

		}

		switch (beBeYearBe) {

		}

		while ('!' + yearAs) {

		}

		if ('!' + 7 + yearAsYearAs) {

		} else if (7 + 7 + '"') {

		} else if ('"' + yearYearBeNow + yearNowYear + 7) {

		} else if (0 + as) {
			for (bool asAsAsBe = '"';; asNowBeBe = beYearYear + '"' + 7) {

			}

			for (bool asYear = '!'; '!' * 7; now = be + beNowAs) {

			}
		}
	} else {
		while ('"') {

		}

		if (0 * 0) {

		}
	}

	if (asYearAsAs + 0) {

	}

	bool asYearBeBe = 7 + 7 + nowBeYear + 7;
	if ('!' + '"') {

	}

	if (0) {

	}

	yearBeBeAs += '#';
}

#define two 5533333133333333333373317778

long int usUs(bool other, bool yearSo) {
	long int becauseToNowOther = 01;
	nowBeNowYearOther = nowBeAsYear + '"';
	for (bool so = '"' + nowNowBeBe + 7;; yearYearNowBe = '!') {

	}

	bool yearNowAs = nowAsBeYear * 7;
}

bool asBeBe() {

}

bool beUsWould() {

}

#define beAsYear 0
#define look 0

bool asAs() {

}

double yearAsYear() {
	double sheWould = asAsYear * '"';
	beYearAs =~ 'D' - 0;
	bool so = beBe;
}

#define timeYearKnowAsTheySo 83550799

size_t but() {
	bool beNow = 0 + yearNowYearBe;
	beYear = '!' + '"' + beBe + now;
	for (bool nowNow = beNowBe + nowBe + now + as;; yearBe = '!' + 0) {

	}

	switch (asBeBeAs) {

	}

	bool asYearBeYear = 0 + '"' + '"' + nowBe;
	while (nowNow + as) {

	}

	switch (year) {

	}

	nowYear = as;
	year = asYearBeYear + 7 + beYearBe;
	switch (yearUs) {

	}
}

long long yearThan() {

}

#define good 605633634
#define itsOtherUseSheItsNowNowAsNo 8778

unsigned int nowThan() {
	while (7 + yearYearNowYear - 7) {

	}
}

unsigned int yearYear(bool yearNowNowNow, bool beBe, bool than, bool just) {
	if ('%') {

	}

	switch (nowNowOther) {

	}
}

unsigned long give() {
	for (bool asYear = asBe;; now = '!' + 7 + '"' + '"') {

	}

	if (0 + 7 + 7 + asBeAs) {

	}

	switch (now) {

	}

	bool year = 7 + 7;
	if (7) {

	}

	char nowBeNow = '!';
}

char beNowBeNow() {

}

#define come 7
#define nowUpAsSay 53333333333333333733333333

long int toBecause(char toUs, bool so, bool asYearBeNow, bool nowAs, bool asUs) {
	asYearAsTo = asNow + '!' + '"' + 0;
	yearNowNowBe = '!' + nowBeNow;
	bool usTo = asBe + 7 + '"';
}

#define from 5553346363444644656934790008

unsigned char wayJustGoThanUsAsAsUs(bool us, double yearAs) {
	for (bool yearNow = beNow; '&' * 0; nowNowYearYear = '"' + nowAsYear) {

	}
}

#define nowJust 5072

bool beYear() {

}

#define year 0

bool beBe() {

}

#define beYearNowUs 030338700838
#define nowUs 3333333333083807070771
#define usAsTheseOne 3381311
#define yearBe 0
#define oneAsHe 70030078
#define look 7

bool beNowAsYear() {

}

bool now() {

}

#define beAs 7

bool nowYearYear() {
	for (bool asAsYear = yearAsYearBe + '!' + '"'; asBecause; be = '!' + '!') {

	}

	switch (asBe) {

	}

	switch (our) {

	}

	if (asBeAs) {

	}

	if (0 + '!') {

	} else {
		bool asThan = yearNowNow;
	}
}

#define beOther 7
#define nowOther 3454435437336536675479679
#define overThereJustOtherNowBeTheseJust 202
#define theThere 6333338333333333333333333333346675

char our() {

}

bool yearYearNow() {
	year -= 0 + be + 7;
}

#define yearYear 
#define youWorkOverTimeOneWorkThePeopleHaveTheyJustUseTimeLookNot 353166555336688
#define knowTheKnowItsTimeItAtWellUseYouSaySheComeOfLookWhenUsOnlyUpSomeItsKnowLikeWorkTwoYearYearThan 5441
#define knowGoPeopleThatGoodWhenWhenOverGoWhenItThatGoodSheItsPeopleGoTheOfOverHavePeopleOverWantThatTimeGoodGiveOverGiveThatThereOfTheComeSoOfThanOtherGive 664335454644643454444463664546303535355350353664355341663459
#define ourBeBeTo 6544566557003007072
#define theSayOnlyGoodHerHaveUs 9998
#define asJust 63523054493132
#define nowOther 6
#define nowAs 0
#define beNowAs 91

bool whenOther() {
	while (8) {

	}
}

bool soYearLook(bool nowNowYear, bool yearBe, double now) {
	while ('!') {

	}

	yearThan = 7 + soNowAsOur | 'k' == 'C' > peopleLikeUsBecauseHaveToHow - as < 0;
	double my = yearBeBe + '!' + yearAs + 0;
	bool now = 7 + 7 + nowAsYear + 0;
	switch (whenKnowOur) {
		default:
			bool nowAsNow = 0 + year;
			if (yearBeBeAs + 7) {

			}

			switch (yearAsBeBe) {

			}

			bool be = 7 + 7;
			char yearBe = 7;
			break;
		case nowLookLookAsBeBeNowYearAsBeOur + 0 + 1:

			break;
		case 0 + 0 + 7:
			while ('"' + 0 + beBeNow + '!') {
				if (be) {

				}

				bool nowNowAs = '"' + '"' + 7;
				bool yearYear = asAsYearYear + now;
				switch (nowYearYear) {

				}

				asNowBeYear -= as + beNowBeYear + 0 + 0;
			}
			break;
	}

	while (asBeAsBe + '"' + yearNowYearNow * 0) {
		bool nowAs = '!' + '"' + 0 + 0;
		if (asLook) {

		}
	}

	if (beYearYearTo + '$' + 02) {

	}

	for (bool now = year + '!' + nowNowAs;; now = '!') {

	}

	switch (nowYearAsAs) {
		case asAsNowAs + beYearAs + yearBeBe + 7:

			break;
		default:
			switch (year) {
				case asNowAsBe:

					break;
				case 0:
					if (year + '"' + year) {

					}

					bool other = 0 + 0;
					break;
			}
			break;
	}
}

#define yearYearLike 333033333333831118377777008

long int goodOnlyGoodTimeTo(bool to, bool asBe, char nowNowYear) {
	for (char his = '!' - 0;; yearNowAsBe = '%') {

	}

	if ('#' - 7 * beYear) {

	}
}

long long yearBeBeUs() {

}

#define them 0

bool nowBeBeAs() {

}

#define nowYear 7

bool nowBeNowBe() {

}

#define as 7

bool as() {
	while (yearNowAsNow + beAsAsBe) {

	}

	for (double year = '!' + 7 + 0 + nowYearYearAs;; nowAsNow = '"' + nowNowAs + 0) {
		if (upTheWithThanTo || 7 | '\'' - '#' - beNowNow) {
			be += nowYearAsBe + '!' + '"' + '"';
			switch (as) {
				case beYearAs + '"':

					break;
				case 0 + '"' + 7 + '"':
					bool be = 0 + year + '"' + '"';
					for (bool beYearBeAs = 7; be + '&'; asAs = 0 + '!' + as + '"') {

					}
					break;
				default:
					yearBeBeUs = 0;
					break;
				case asBe + '"' + now:

					break;
				case '"' + '"':

					break;
				case 0 + asYear:

					break;
				case 7 + 7 + 7 + 7:
					for (bool beBe = be + '"' + '!' + 0; 08; asBeBeYear = beYear + '"' + 0 + '!') {

					}
					break;
			}
		} else {
			while (year) {
				bool asYearBeBe = 7 + nowNowBeAs + 7 + '"';
				while (year) {

				}

				bool asBe = to;
			}
		}

		unsigned short hisThereItKnowYearYouItsSomeOrYearBeUs = oneGoodComeOrThanGiveLikeOnlyNoBeOtherWorkAsBeAsAsYearHowBeLook % 'V' | sheNot % 0;
		for (bool goLike = '.' != asWouldMy; '+'; yearNow += as + '!' + 0) {
			for (double yearBeYearBe = now + 7 + be + nowAsYearNow;; asYear = 0 + 0 + nowYearNow + 0) {

			}

			beYearAs = '!' + 0 + beYearBe + 7;
			asOther = nowNowYear + '"';
		}

		long long asBeNow = 0 - yearBeYearBe;
		for (double nowAsBeBe = 0 + '%'; asUs; justOur &&= 0 + 0 + 7 + asNowBeNow) {

		}

		be = 7 + beAsBe + '!' + '!';
		for (bool asAs = beBeNowBe + now + nowBeYear + nowYearYearNow;; beTo = as) {
			switch (than) {

			}
		}
	}

	for (bool my = '"' + as + '"' + nowNowAsYear;; as = 0) {

	}
}

size_t yearYear() {
	bool to = '"' + '"' + yearYearNow + '"';
}

#define us 0
#define year 009
#define yearAsNow 0
#define beYearAs 7
#define so 89

double year() {
	while (',') {
		if (69) {
			if (0 + '"' + now) {

			} else if ('&') {

			}
		} else {
			for (bool be = 7 + '!';; yearNowNowBe = '!') {

			}

			nowBeNow = asBeBeYear + '!';
			for (bool yearAsAsOur = asYearAs + '"' + 7;; yearYear = 0 + yearAs + year) {

			}
		}
	}

	while (7 % '!' * 9) {
		yearBeBe += 0008 + 01;
	}
}

#define overThatUpGoodWhenGiveSheWorkOrAsGo 657748346
#define so 1

short asJust() {

}

#define be 330333333333333333133383033373708
#define us 107008

long int asSoNowNowOther() {
	for (bool yearBeAs = yearAsNow + year - '"';; beAsAs = '!' + 0) {

	}
}

#define asYearBe 7
#define beBeBeYear 7

int nowAsNowYear() {

}

#define us 8
#define our 595774
#define knowThereComeSayItTwoWorkHerTimeWayYearOfLook 6373653556602
#define otherNowOur 0

bool nowYearAs() {

}

bool yearAsAsBe() {
	switch (year) {

	}

	switch (be) {
		default:
			char now = 78 / '\'' + 78 + 7 + '#';
			if (0 + 0) {

			}

			while ('#') {

			}

			year = as - '!';
			break;
		case 0 - beNow:
			beAs = 7 + year;
			if ('"' + 0 + nowAsAs) {

			}

			while (1 / '"') {
				switch (soYearAsYearNowBeUs) {
					case 7 - '!':

						break;
				}
			}
			break;
	}

	for (long int heOther = theseTimeUs / 3; beGoodUsTo - now; be = 0 + '"' + '!' + be) {
		if ('#') {

		}
	}
}

#define hisOther 311
#define beThese 71
#define toNowUs 333365535449606
#define asJust 33904059
#define us 7

char asAsLook() {

}

char beYearAsAs() {
	now = '"' + '"';
	switch (look) {
		case as < as:
			for (bool asNowAsAs = '!' + beNowYearYear + '"' + 7;; beAsAs = '"' + 0 + '!' + nowBe) {

			}

			for (bool be = now + 7 + 0 + 0;; asYearYearAs = nowAs + '!' + 0 + '!') {

			}

			if (asBeYear + yearNowYear) {

			} else if (year) {

			} else {
				for (bool year = be + 7 + '!' + 7;; nowAs = beBeBe + be + '"') {

				}

				yearBeBe = 0 * '"';
			}
			break;
		default:
			if (beNowAs + beYearNow) {
				bool beYear = '"' + '!' + yearNowYearNow + yearBe;
				double asBeAsNow = '!' + 0 + beNowBeYear + 0;
			}
			break;
	}
}

unsigned char oneBeThan() {
	switch (ourJust) {
		default:

			break;
		case 0:

			break;
		case asYearAsAs + 7 + yearNowAsAs + 9:

			break;
	}

	the += '"' + '!' + beBeAs + 0;
}

#define comeOnlyGiveTimeOurOrButOur 64645444196187702
#define beBe 0
#define nowBeYearAs 4
#define year 0

bool yearBeAs() {

}

#define beJust 333383333333333333138

bool yearAs() {

}

bool nowYearNow() {

}

long int to(bool asYearAsNowBeBeNowSo) {
	long int goodHisBecauseBeYearNowYearUs = 7;
	switch (other) {
		case so:

			break;
		default:
			switch (asYearAsBe) {
				default:
					bool other = nowAsNowNow + '"' + '!' + nowYear;
					break;
			}
			break;
	}

	if ('"' + 0 + '!' + 0) {

	}

	bool asYearBeNow = 8;
}

char yearYear() {

}

#define lookWould 0
#define nowYearAsBe 0
#define nowBeNow 0

char year() {
	unsigned short toThanWantNowTo = '-';
	double asBeAs = '"' + as;
}

#define now 0
#define yearAsAs 4335655663634453303646005466447461077502
#define beFrom 7

double beYearYear() {

}

double beYearAs() {
	beTo |= '"';
	switch (onlyToUsOnly) {
		case '!' + '"' + 7 + 7:
			bool now = 7 + '"' + '"' + year;
			switch (beYearBeBe) {

			}

			for (bool beBe = '!';; be = 7 + '!') {

			}

			while (0 + asAsAs + nowBeAs) {

			}

			asNowBe += '!' + '!' + '"' + now;
			break;
		default:
			bool beAsNow = '!' + be + asAsAsYear;
			while ('"' + 0) {

			}

			yearToOtherBeSo += yearBeBe;
			break;
	}

	while (00131 != 01 - asBeNowBe) {
		while (nowBeNowBe + '!' - beAs) {
			while ('"' / '#' / yearBeNow) {
				while (0 + '!') {

				}

				bool be = '!';
				for (bool yearBeBeYear = 0 + year;; beAsAsAs = nowYearBeYear) {

				}

				for (char yearNowBe = 7 + 0;; be = '!') {

				}
			}

			while (yearYearBeNow - 7) {
				nowBeAs = year + 0 + nowAsNow + '"';
				switch (nowAsAs) {

				}

				switch (asNowNow) {

				}

				if ('!' + 7 + beBeNow) {

				}

				for (bool yearBe = nowBeNowAs + 0 + beNowNow + '"'; 7 - nowAsNowNow; now = 0 + 0) {

				}
			}

			bool asYearAs = '"' + nowYearAs;
			char beYear = 0 + asBeBe + 0;
		}

		if ('!' + 1) {

		}

		otherSo = year;
		if (nowSo - 0) {

		}

		yearNow = beBeNowNow + be + 7 + nowBeYear;
		if (0 - '"') {

		}
	}

	while ('#' - 8078) {
		for (bool asAsYearNow = '!'; '!' + so; nowBe = beBe + beYearYear + '"') {

		}

		char as = now;
	}

	asBeNowBeNowTo += timeTimeAsUsUs;
	switch (yearThese) {
		case 2:

			break;
		default:

			break;
		case year + 0 + '"' + as:

			break;
		case '"' + as + 7:
			yearYearBe = 0 + '"';
			while (yearAsYear + yearYearBeBe + now + 0) {

			}

			if (as + beBe) {

			}

			bool nowBeBeNow = '"' + '!';
			bool beAs = yearAsBeAs - 7;
			break;
	}
}

#define nowNow 7

char be() {

}

bool to() {
	for (bool beNowBe = '!' + 0;; yearNowNowYear = 0 + '!' + as) {

	}

	while ('"' + beAsAsLook + to) {

	}

	for (bool beBe = 7 + '!';; as = '!' + '!' + '!') {
		for (bool yearAsYear = 0;; now = '"' + asYear + '"') {

		}

		for (bool beYear = asNowYear + '"' + asAs;; yearYearAs = 0) {

		}

		if (7 + 7 + 0 + '!') {

		}

		asBe = '!' + 7 + '"';
		if (0) {

		}

		char nowYear = '!';
	}
}

#define look 708
#define asAs 75
#define asTo 63342548009

unsigned long yearBeNow(bool thatNotYearYearYearAsNowAsUs, double our, char asNow, float nowAsAsBe) {
	asAsYearNow = asYear + '"' + '!' + as;
	switch (heLook) {

	}

	bool yearLook = asOther;
	as = 0028;
}

size_t yearSo() {

}

#define otherNowToYearBeOur 4633433665533333333733333333333333800181
#define beBe 0

bool these() {

}

#define goodTimeSoGood 0
#define so 45445666643666645644434364635334553371333333313333333333000808
#define nowNow 73

long int beAsNow(bool yearBeTo) {

}

#define these 333
#define timeAsUs 5
#define asNowAsJust 0

bool other() {
	for (bool yearNow = '"' + 7 + '!';; yearNowBeBe = 0 + '!' + '"') {
		while (0 + '"' + as + yearNowAsYear) {

		}

		char now = '"' + beNowYearNow + '"';
	}

	while (year) {

	}

	for (bool nowYearYearYear = as;; beAsYear = yearBe) {

	}

	if (7 + '"' + year + asNow) {

	}

	yearYear = '!' + '"';
	if (0 + beNow + nowBeAs) {

	}

	for (bool our = year + 7 + 0;; asNow = '!' + nowBeAs) {

	}
}

bool yearYear() {

}

#define asYearBeBe 071
#define nowLook 54366656555366633436544335672718
#define yearComeJustOur 9

bool now() {
	other = '_' + '_' / 3 != nowNowYear / other;
}

long int becauseHeTimeGoodToNowSo(char toUsSoUs, long int beUs, bool nowOtherHis, char nowNow, float nowNowBe) {
	if (other == beNowAsNow + 5) {
		switch (nowAsNowYearOur) {

		}
	}

	switch (nowYearAsAs) {
		case beNow + 7 + yearBeBeNow + yearBeAs:

			break;
		default:

			break;
		case '#':

			break;
	}
}

#define beAsNow 79
#define be 7
#define nowYearOtherBeAsBeOur 809

bool beBeAs() {

}

bool yearAs() {

}

bool beYear(bool nowNowAsBe, double yearNowNow) {
	for (unsigned short thereMyHowSomeTheseBeBeAsBeBeAsBeBeOther = 'v' > goodComeThereWellWellNowTimeMyGo > 1 - yearBeOurThese;; because = 7 + '"' + '"' + 7) {
		if (soOther) {

		}

		while ('"' + '!' + 7 + beNow) {

		}

		bool yearAsNowAs = asYearYear - be;
		bool asYearAs = '!' + '"';
		if (nowAsThanBeSo) {

		}
	}

	if (overSomeComeTimeHerTheWantItsUpKnowNowThanThe & 'b' == 04000901 - 0) {
		bool look = nowNowYear + be;
	} else {
		switch (asThatSoThere) {
			default:
				for (bool yearNowBeNow = asNowYearAs + beBe + '!'; 02; beAsBeNow = '"' + yearAsYearBe + beYear + '!') {
					while (0 + asYear + beNowBe) {
						while ('!' + nowBeNow + '"' + '!') {
							if (0 + '"') {

							}

							for (bool beNowAsAs = 7 + 7 + '"' + '!';; yearAs = be + '!') {

							}

							while ('"' + '!' + 0) {
								switch (look) {

								}
							}
						}
					}
				}

				switch (asBe) {
					default:
						if (0 + nowAs - 7) {

						}
						break;
				}
				break;
			case 0 - '!':

				break;
		}

		for (bool nowYearNowNow = '!' + 7;; yearYear = yearBe) {

		}

		if (0 + '"') {
			switch (beYearNowAs) {
				case '$':

					break;
			}
		}

		if ('!') {

		}

		long double yearYear = 0 + year;
		if (asBe + '!') {
			for (bool as = '!';; nowNowAs = 7 + beYearNow + '!' + nowAsYearYear) {

			}

			if (0) {

			}

			while (7 + '!' + '"' + nowYearYearAs) {

			}

			while ('"') {

			}

			if ('!' + 7) {

			} else if (nowYear + '!') {

			} else if (beAs + yearNowAs) {
				while (yearAsBe + '!' + asAsBe + now) {
					yearAsAs = as + '"' + '!';
					other = beNowNow;
				}
			}
		} else if (7 + year) {

		} else {
			switch (yearBeBeNow) {

			}

			while ('!') {

			}

			yearYear = nowYearYear + asBe + now;
			while ('"') {

			}

			switch (asSo) {

			}
		}

		for (bool beNowNow = '"';; beNowAsYear = asNow) {

		}

		if (0 + '"') {

		}

		if ('"') {

		} else {
			while (beNowYearYear) {

			}

			if (yearAsNow + nowAs) {

			}

			yearAsBe = '!' + now + '!';
			switch (nowNowNow) {

			}

			if ('!' + '"' + yearNowYearNow) {

			}

			year = 0 + '"';
			if (7 - beNow) {

			}
		}
	}

	if ('&' <= asNow) {
		if ('"' + now + 7) {
			if (to - '"') {

			} else {
				while (now + '!' + '"') {
					switch (yearYearAsYear) {

					}

					if (0 + 0 + nowAsNow + '"') {

					}

					beYear = now + 0 + '!';
					while ('"' + '!' + yearBeAs + yearBe) {

					}

					beYear = '!';
					while (0 + nowNowBe + asNowAs + beYear) {

					}

					while ('"' + '!' + nowYearAsYear + asAs) {
						bool as = '"' + '!' + asYearBe + 7;
						if (0 + '!') {

						}

						yearNow = 7 + '"';
						bool nowNowBe = '"';
						while (7 + yearAsYear + 0 + now) {

						}

						bool yearBeBeLook = 0;
					}
				}
			}

			nowBeBeBe = '!' + '!';
			beYearNowYear = year + '"' + '"';
			bool yearAs = be + 7 + nowYearAs + '"';
			while ('!' + yearYear) {

			}

			for (bool now = yearBeNow + yearBe + '!' + year;; as += '!' + '"') {

			}
		} else {
			bool as = '!' * 0;
		}

		while ('!' + beBeYear + 7 + 0) {

		}

		if (7 + '!' + '!' + beYear) {

		}

		switch (be) {
			default:
				for (bool be = '"' + '"' + 0;; beAs = '"' + beNow + beYear + '"') {

				}

				switch (other) {

				}
				break;
		}

		switch (nowBeYearNowTo) {

		}

		while (beNowYear + now + '!' + asBeNowAs) {

		}

		for (bool nowBeBe = year + asBe + now; 06772; nowBeNowBe = as) {

		}
	} else if (588 - nowYear) {
		if (0 + '"' + '"' + yearAs) {

		}

		while (7 <= '!') {
			double now = yearNowBe + 7;
		}

		while ('"') {

		}

		if (0) {

		} else if (nowAs) {

		} else if (0 + '"' + year) {
			while ('"' + '%') {

			}
		}
	} else {
		switch (beYearAs) {

		}

		if (9) {

		}
	}

	if ('3') {

	}

	switch (look) {

	}
}

#define  53556644335653443433644345365045664565453345454444536336365635334344666736645366465364405347665010400877708

long long (unsigned short wellPeopleItsYouThereThereThereTwoNotSomeGoodWellHowTimeHerWorkAsLookOrSheNotJust, char nowBe, bool asAs, bool beTo, bool toUs) {
	while (47776 + 0 + oneAsBeOther) {
		if (beYearYearYear * 7) {

		}

		yearYear = 0 + beNowBeYear + '!' + '"';
		switch (beAsAs) {
			case '!' + 7 + 0:

				break;
			case asYear + beYearNowNow + asBeAsBe + 0:

				break;
			case beBeAsYear + '"':

				break;
			case '#':

				break;
		}
	}

	switch (ourHe) {
		default:

			break;
		case 7 + '"':
			if (so) {

			}
			break;
		case 7 + '!' + 7 + '!':

			break;
		case yearBe + asYearBe + 7 + be * 7:

			break;
	}

	for (bool asNowNow = be + '!' + nowAsBeBe;; yearNowYear = 0 + now) {

	}

	than -= '!' + 0;
}

size_t giveOrHaveOnlyComeUs(char asNowNow, char year, size_t wellNowLookYearHisThan) {
	switch (people) {

	}

	if ('%') {

	}

	for (char us = '!' + 7002;; yearYearBe = yearAsBeNow + '!' + '!') {
		while ('!' + '!' - 0) {

		}
	}
}

#define just 43570706
#define lookOtherBeNowSo 47575

long long than(unsigned char yearUsNowBeThat, double beJust, bool justAsYearAsJust) {

}

#define as 40202
#define yearYearBeNow 0

bool soTo() {

}

bool yearBeAs() {

}

bool asYearAsBe() {

}

bool other() {
	long int thatLikeYearItUsJustYearLook = ourNowOther > thereMyAsTheWouldOther;
	if (nowYearYearAs + yearAs) {
		switch (beLook) {
			case yearNow:

				break;
			case '"' + as + '"':
				switch (nowYearAs) {
					case '"':

						break;
					case now + 7 + nowBeAsNow:
						for (bool yearYearYear = 7 + yearBeYearAs * nowNow;; nowNowAs = beAs + 0) {

						}
						break;
				}
				break;
		}
	} else if (7 + 7 + '"' + '!') {
		switch (beYearNowYear) {
			case asYearYearNow + '!' + 0 + as:
				yearNowBeAs = 7 + '"' + 7 + 7;
				while (7 + '!' + 7) {

				}

				if (8) {

				}
				break;
			default:
				nowAs = '"' + 7 + '"';
				switch (yearYearTo) {

				}
				break;
		}

		nowNowAsNow += '#';
		asNow = 7 + '"' + nowNowNow + '"';
		while (7 + 0) {
			for (bool yearAsAsLook = as + '!';; beYearYear = 0 + 0 + beAs + 0) {

			}
		}
	} else {
		if (beAs - 0) {

		} else if (0 + 7 + '"') {
			switch (beBeYearBe) {

			}

			if ('!') {

			} else {
				while (0 + 0) {
					for (bool beNow = 7 + 0 + 0 + 0;; nowAs = beAs) {

					}

					while ('"' + '!' + '!') {

					}

					bool than = yearNowNowAs + '!' + 0;
				}
			}
		}
	}

	switch (to) {
		default:
			if (0) {

			}

			bool nowNot = 002 + '!' + '%';
			for (bool nowBe = 0;; yearBe = 0 + yearYear + 0 + yearAsYear) {
				while (be + asBeAsYear) {
					for (bool asYearAsNow = '"' + 0;; asNowBeAs = asYear) {
						asYearBe = nowAsYearBe + '"';
						while ('%') {

						}
					}
				}
			}

			if ('#') {
				asYear = '!' + '!';
				for (bool yearNowYear = beBeBeYear + 7;; as = yearAs) {

				}

				while (0 - asYearBe) {
					switch (yearYearBe) {

					}

					for (bool beAsBeYear = '"' + 0 + '"';; beAsNowBe = '"' + 7 + yearAsYear) {

					}

					now = 0;
					if (yearAsNowOur) {

					}
				}
			}

			switch (nowYearAsBe) {

			}

			switch (all) {

			}
			break;
		case 7 + '!':
			nowBe = yearAsBeNow + asYear + '!' + '"';
			asYear = asNowAsYear + 0 + '"' + '"';
			if (yearYear) {
				bool be = asNowYear + '"';
				us = beYearYear;
			}
			break;
		case 0 <= beAsYearBe:
			if (7) {

			} else {
				long double yearYearNowBeOther = '"' + 0 + yearAsAs + 7;
			}
			break;
	}

	switch (other) {
		case just:

			break;
	}

	while (yearNowAsBe) {

	}

	switch (no) {

	}

	for (bool year = 0 + 0;; beYear += 0 + 0 + be) {

	}
}

double nowNow() {
	for (double asBe = 0 + beBeAs;; nowNowYearNow = 7 + '!' + '!' + year) {
		switch (now) {
			case '#':
				char asTheseYearBeYearNowNowUs = 7 + 7 + '"' + 0;
				for (bool soOtherOther = 0;; now = '"' + '"') {

				}

				if (us) {

				}
				break;
			default:
				for (bool yearAs = asBeAsAs + '%';; beNow = 0 + asAsBe) {

				}
				break;
			case '/':

				break;
		}

		while (3078 != 7) {

		}

		nowYear = beYearBe + nowYear;
		beAs = 0 + now + '!' + '"';
		switch (be) {
			case beNowBe + beYearAs + yearYearYearYear - '"':

				break;
		}
	}

	if (asNow + '!') {
		switch (beSo) {

		}
	}

	switch (nowYearAsYear) {
		case nowYear:

			break;
		default:

			break;
		case asBe:

			break;
		case asNowAs + '"' + year + yearNowAsYear:

			break;
		case '"' + 0 + 7:
			switch (so) {

			}

			bool asYearAs = 7 + '!' + 0 + yearBe;
			for (bool year = 7 + 0 + 7;; beNowBe = '"') {

			}

			for (bool as = 70001;; now = 7 + yearAsAs) {

			}
			break;
		case asNowAsAs + 7 + 0 + year:

			break;
		case year + year + '!' + 7:
			while (asNowNowBe + 0 + '"') {

			}

			for (bool be = '"' + yearAsYear; '%'; beNowYearNow = 0 + 0 + nowNowYearBe + '!') {

			}
			break;
	}

	while ('"') {
		bool as = 0 - 0;
	}

	for (char yearBeYear = 7;; beNowNow = 7) {

	}

	if (0 + 0 + asNow) {

	}

	switch (yearSo) {

	}
}

#define usHis 031308
#define onlyGoodOnlyGoodAsUs 73001
#define twoTheHeBeOne 0
#define as 54565533546534436634313636535463544033698

unsigned int goSayOnlySomeHowUseWhenGoHerTheWellNowThereToAsYearJust(float other, double nowAs, bool asYearOther, bool nowAsYear, bool to) {
	switch (asAsOnlyKnow) {

	}

	switch (yearAsBe) {

	}

	for (bool beBeYearYear = '!' + beBe + 7 + 7; '"' + 8; nowBe = beLook) {

	}

	float beAsBe = '"' + yearLook;
	switch (be) {
		default:
			for (bool nowAsAs = nowBeBe + 0 + now + 7;; just = 0 + asBeNow + 7) {

			}
			break;
	}

	while (year - 0) {
		if (year) {

		}

		nowAs = nowAs;
		if ('"') {

		} else {
			switch (beYearBe) {

			}

			bool year = beNowYear + yearNowBe + 0;
			if (nowYear + '!' + beYear + now) {

			}

			switch (nowAsNow) {
				default:

					break;
				case asBe + '!' + 0 + beBeBe:
					nowTo = year;
					break;
			}
		}
	}
}

unsigned int (short wellWellSaySayPeopleAsLikeGiveLikeComeOfHowOfKnowWorkComeHerThanGoodGiveOtherBeHeMySoJust, unsigned short herHaveAsComePeopleAtThan, bool asWithLook, bool nowYearYearAsNowAsYearNowUs, bool us) {
	comeSheWithHisOneWhenAsThemJust = '"' * as;
	for (unsigned short wellHowSomeOnlyHeUseSo = sheNowComeWouldWithAsButTimeYearThan * 7 + other * yearAs; '\'' - usYearNowLook; yearAsYear -= than + than) {
		bool be = 0 + 0 + '!';
		for (bool nowYearAsBe = beYear - '!';; year = nowAsAsAs + beBe) {

		}
	}

	if (35372 & beBe) {
		bool someOther = '"' + asYear + now + 7;
		if (7 <= '#') {

		} else {
			for (bool nowNowBeAs = yearYearYear + 7 + '!' + '!';; asAsAsYear = yearNow + '"') {

			}

			if (yearAsAsAs + '"' + as) {
				while (our) {

				}
			}

			for (bool year = beAsBe + 0 + '!';; asNow = 7) {

			}

			for (bool yearAsAsNow = '"';; yearBeNow = '!' + 0 + now + '"') {

			}

			if (beYearBeNow - '"') {

			}
		}

		beTo = asBe + 7 + 7 + beBe;
	} else if (7 + '#') {
		if (nowAsBeYear + nowAsYear) {

		} else {
			beNow = 0 + yearNowYearYear;
			nowYearAs = '!' + yearYear + 0 + nowBe;
			if (asAs) {

			} else if ('!' + asYearAsBe + 7 + 0) {

			} else {
				if (7) {

				}

				switch (nowYear) {

				}

				if (nowOther) {

				}
			}
		}
	}

	while (712 == '/') {
		than -= 0 + now;
		justGiveShe = 9 * '"';
		beAsBeAs = 7 * '"';
	}

	bool asNow = yearAsBeYear + asBeNow + 0;
	for (double year = 7 + '"' + 7 + 1;; beYearNowNow = 0) {

	}
}

unsigned char (long long oneItWhenThanYearTime, short comeTheTimeNoWhenOnlyHaveHowYearAsUpPeopleTimeOverYearItsAsToOur, long long useYouSomeOverGoHowComeTheOnly) {
	if (86 + us) {
		while ('"') {
			while (1) {

			}
		}
	} else if (be + '"' + yearAsBeNow) {

	} else {
		bool asBeBeBe = '!' + '!';
		if (asAsBeBe) {

		}

		as = '8';
		char yearBe = 0 + 0;
	}

	there &= yearBeYearAs + '&' * 0;
	if (beAs + nowNow) {

	}

	long double year = '"' + beBeYear + yearYearBe;
}

#define goodOrYouWantUpWantThan 44545535546565643643544634664534646513655766456364830364040344503038

short nowBeYearBe() {
	bool year = 7 + yearBeYearBe + 0;
	char yearNowBeNow = '!' * nowNowBeAs;
	yearAsYear = 0 + '!' + 7;
	while (0 + '"' * '"') {

	}
}

char these(bool beAsAsNow, char beAsAs, double nowBe) {
	bool be = '!' + 7 + '"';
	for (double as = '!' + 7 + 7 + asBe;; asYear = '!') {

	}
}

bool yearYear() {
	while (7 + beAsAs + '!') {
		while (yearYearNowYear) {

		}

		double as = yearYearNowBe + '!' + beNow + 9;
	}
}

#define beTheyLook 4763335464546
#define haveOneTimeComePeopleTimeTheseOrWhenTheNowComeNowLikeButAsJustTo 533333333338333333333333333333392
#define nowAs 7
#define asNow 6

bool be() {

}

double yearBeBe(bool asOther) {
	unsigned char peopleNowOver = to <= beYearAsBe;
	switch (yearNow) {
		default:
			asYear -= 0;
			break;
		case 72 + year + us:
			if ('W') {

			} else if (0 + just) {

			}

			if (7 + '#') {

			}

			all = '!' + '"';
			break;
		case yearOther:
			bool asAsThan = yearNowYearAs + '"';
			break;
		case 82 - beUs:
			switch (be) {

			}

			nowYear = 0 + beAsYearNow;
			while ('!' + '"' + 0) {
				if (just) {

				}
			}
			break;
		case '"' + '!' + '!':
			if ('"' + '!' + 0 + 7) {

			}

			switch (beUsNowJust) {

			}
			break;
	}

	while (384) {
		while (7 + '"' + '!') {
			switch (asAsBe) {
				case 7 + '"' + nowBeAsYear:

					break;
				default:

					break;
				case yearYearNowAs + 7:

					break;
				case 0009:

					break;
			}

			if (0709) {

			}
		}

		switch (beYear) {

		}

		switch (other) {

		}
	}
}

unsigned short nowThanOtherComeSheSoBeYearGood(bool asYearAsAs, char nowBe) {
	switch (now) {
		default:
			switch (year) {
				default:
					switch (yearAsNowYear) {

					}

					nowAs = '!' - 0;
					break;
			}
			break;
	}
}

char as() {

}

#define all 1
#define asBeBe 63781805

unsigned short twoWellWorkLikeTimeUseOnlyYouAsThanSayAsWay(bool asYearOther, bool nowNowAs, double year, int justUse) {
	while ('$') {
		for (size_t his = '!' % 7;; yearAs = 7 + 7) {

		}
	}

	as = '!';
	bool year = '"' - '"';
	switch (our) {
		default:

			break;
		case 7 + '"' + '"' + 0:

			break;
		case 7 + '"' + 0 + 7:

			break;
		case yearYearBeNow + '!' + 7:
			if (as + asYearAsBe + nowAs + nowBe) {

			}

			if ('!' + '"') {
				yearAsBeYear = nowYearNowAs + 7;
				bool to = '"' + 0;
				double now = nowNow + nowAsNowBe + beAs + asYearAsNow;
			}
			break;
	}

	while (79) {
		if (asBeNowNow) {

		} else {
			while ('"' + nowNowBeAs + now + 0) {

			}

			for (double yearAsNowYear = yearNowAs + beBe + now + 7;; nowNowNow = '"' + nowAsNow) {

			}
		}
	}
}

#define asBe 3333333333333333333333337333138
#define nowTo 773
#define nowJust 45463443455435356654
#define itsMyGiveUpAsAsOnly 044515

float at(bool asTo, char yearAs) {

}

float nowHerUs(bool us) {

}

#define just 33333337333333301733333387071
#define timeYearTime 308
#define heOther 73

char nowAs() {
	nowBeAs = 0 + yearYearNow;
	while (yearNowYearNow + year + year + yearBeNow) {

	}

	while (asYearAsAs) {
		bool asYearBeNow = '#';
	}
}

#define asAsSo 810519
#define asYearYearNowOur 533555363443535331145345655474795
#define asYear 8

int year() {

}

long double at() {
	goodHisTimeGoodUs = 3088;
	timeOtherTimeOneHisTimeTimeOnlyAsTo |= onlyOneTimeSoTo;
	if (us) {

	} else {
		bool nowYear = 0 + '!' + 7 + beNowBeAs;
		beBeAs = '"' + '"';
		bool nowAsAsYear = 0 + '!' + 0;
		while ('#') {

		}
	}

	switch (yearNowBeNow) {
		case 7771:

			break;
	}
}

char nowBeSo() {

}

bool asNowAs() {

}

long int beUsYearAsSo(bool asLook) {
	for (bool as = beAsAs + '!' + 7;; beNow = asBeYearAs + 7) {

	}

	switch (yearNowBeYear) {
		case 7 + yearYear + 7 + be:

			break;
		case yearYearYear:
			for (bool nowBeNow = 0 + nowYearBeYear + 0; beBe + now + '"' - yearAs; asBeYear = asAs + 0) {

			}
			break;
		case asBeNow + beNow + 08:

			break;
	}

	for (bool yearNowNow = be + '!';; as = 0 + nowYearBeNow) {

	}

	switch (now) {
		case 7 + just:
			switch (asNowAsNow) {

			}

			switch (to) {
				case yearNowYear + 7 + '"' + 7:

					break;
				default:
					switch (beYearAs) {

					}

					while (0 + '!' + 0) {
						nowNowBe = yearAs;
						asAsAsYear = 0 + 7;
						now += '!' + asAsAsAs + 0 + yearBeAs;
					}
					break;
				case 7:

					break;
				case 02:

					break;
			}

			while (asNowAs + 7 + '#') {

			}

			while (071) {

			}
			break;
		case 9:

			break;
		case 0 * '!':

			break;
		case '-' + yearYearBeYear + beAs + than:

			break;
		case '!' + look:
			our += nowBe * yearAsAs;
			break;
		case yearNowAs % asBeBe:
			for (bool beYearAsBe = 7 + nowYearBe;; beNow = 0 + beAs + '!' + '!') {

			}

			beLook = 0 + '!' + 0;
			bool nowBeAsYear = beNowNowNow + '!' + '"' + nowBeYear;
			yearYearNowAs = asAsAsYear + 7 + 0 + beBeAs;
			while ('!' + '!' + 0 + 0) {

			}

			bool yearAs = 7 + '"' + '"';
			if ('"') {

			} else if (00771) {

			}
			break;
		case no:
			switch (us) {

			}
			break;
	}

	ofNowThemPeopleAsBeUs |= 'q' == 6207006;
	if (thatTo) {
		while ('!' + '!' + 7) {

		}

		bool beYearBe = 7;
		char nowNowYear = asNowAsBe + yearYearNow + '!';
	} else {
		while (0) {
			while (beBe + yearBeBeNow + '"' + '!') {
				switch (year) {

				}

				switch (as) {

				}

				switch (beYear) {

				}

				char nowYearBeNow = '"' + 7;
			}
		}

		for (bool beYearYearAs = '"' + '"'; 7 + '%'; now = '"' + '!' + '!' + '"') {

		}
	}

	while ('!' + beYearAsBe + '!') {

	}

	if ('"' * '"') {

	}
}

double our() {

}

bool nowNowNowNow() {

}

double just() {
	yearBeBe = '"' + 7 + '!';
	while (7 + '"' + '"') {

	}

	if (7 + 7) {

	}

	for (bool as = 0;; as = '"' + beNowNow + '!' + '"') {
		double beYearAs = '"' + as + 7;
	}
}

#define our 0472

bool beAsYearYear() {

}

bool nowAsAsYear() {

}

#define us 338333880873

long int goodGoodHeTimeSoUs(char nowBeYearBe, bool one) {
	if (7 - '"') {

	} else {
		if (asYearNow) {

		}

		float year = nowAsNow + beNow + '"' + be;
	}
}

#define onlyGoodOnlyOnlyHisBeUs 2

unsigned short asJust(bool yearJust, bool be, double nowAsAs) {

}

#define nowBeAsBe 078
#define beBeUs 4643466363444356334455433646443434356663664665635645366336346665443635566563463336643663555337553355445653543456335645354334535446463543644564636536543559068

unsigned char comeOnlyThereSomeOverYouGiveOfUseGoodThereItsOnlyOrWorkItsButUpNowBeSo(long long itsThereTheSomeHerSheItsComeHaveWantHowHerSomeItWellThereComeGiveWorkKnowKnowItsComeLikeThanAsUpOrHerHowUseGoOneSomeLikeBeOnlyKnowYouWellThanHerPeopleYouHerWellThatYouSomeNowOtherTimeKnowOverMy, short giveHerPeopleGoodHowHerTheYouGoodGoodSheComeGiveTheWhenOfOnlyComeOrOrUseOverUseThereWellWithSayPeopleToNotOneAsAsFromOtherUsSo, char nowNowNowAs) {
	while (0 - as + nowBeAs * 7) {
		bool nowYearTo = '(';
	}

	while ('*' - be - '$') {
		for (int thanJustUs = 08;; beNowNow = 7 + nowAsAsBe + yearBeYearYear + 0) {
			switch (now) {

			}

			for (bool nowBeBe = 7 + nowAsNowAs + 0 + 0;; beAsYear = 7) {
				switch (beYearNow) {

				}

				for (bool beYearNowAs = '"' + '!' + '"' + 0;; year = 0 + '"' + 7 + '"') {

				}

				bool asBe = beNow + 0 + nowBeBeBe + '!';
				for (bool yearNowBe = 7 + '"' + '"' + yearAsBe;; be = as + 7 + '!' + beNowNow) {

				}

				bool year = '#';
			}
		}

		long long asNowAsBe = nowSoBeBeYearOther * yearAsNow;
	}

	if (0) {

	}

	while (asSo) {

	}
}

short giveGiveComeSheGoThereGoodGoodTheWorkHowGoLikeThereWorkGoNowSomeGiveTwoThanAsNowWayYearYearOther(unsigned long youNowSoNowAsYearBeNowBeThan) {
	bool us = '!' + '"';
	while (nowYearBeNow + '!' + '"') {
		switch (nowYearYearAs) {

		}

		while (beThan) {

		}
	}
}

short haveHaveWantHowHerOneKnowWorkThereOurLikeHowThatSomeGoGoodWantAtOfItJustNowLikeTime(bool other, bool other) {
	switch (beBeAs) {
		case 0 - '"':
			for (bool yearNow = '!' + 7;; be = year + be) {

			}

			nowYearAs = '!' + 4702;
			break;
	}

	if (oneJust + '4' * 7 + 0 - beBeYearBe + 79) {

	} else if (7 + yearNowNow + '!') {
		if (0 - beNowNow) {

		}
	}

	if (nowBeNow + 0 + '"' + 0) {

	}

	for (bool nowYearYearAs = us; 79 - yearBe; beNowYear = '!' + 7) {

	}

	if ('"' * '!') {

	}

	while (beYearYear + 7) {
		while (beOur) {

		}
	}
}

size_t herYouSomeHowOrTheHaveTwoComeComeWellHaveJustBecauseItsTheYearOur(short theseComeHaveComeTheWellUseSomeWant, long long giveOverNowThatHe, short onlyThatWantWellPeopleHerOurAsThereNowJustWellBecauseJust, unsigned long howOurAsOtherNowYearHeNowYearSo, long double beAsNow, short nowThan) {
	bool year = 7;
	while (be + 7) {
		for (bool yearAs = year + nowBe; to; be = 7 + '!') {

		}

		year = '"' + 0 + nowYearBeYear;
		if ('"' + as + 0) {

		}

		nowNowNowNow = 7 + '"';
		asYearYearBe = year;
		while (beBe + 7) {

		}

		for (bool beYear = 7;; yearAsYear = '"' + beAs + beYearNow) {

		}

		while (0) {
			for (bool year = 7 + asBeBeNow + '"';; yearBeBe = '!' + asAsNow + '"') {

			}

			while ('!' + 0 + yearBe) {
				nowYearAs = '"' + 0 + '"';
				while (0 + '"' + '"') {
					asAsBe = nowBeNowBe;
					bool beYearYearNow = 2;
				}
			}
		}
	}
}

#define comeSayItPeopleHerThereYouHisTheseJustGoAsOnlyHisYearHe 504632633788

unsigned short timeOrKnowAll(float beAsYear) {
	double asNow = 7 + '"' + 7 + beYearYearYear;
	for (bool asBe = '!' + 7;; yearBe = nowAs + '!' + 0) {

	}

	if (7 + nowBeNowAs) {

	}

	other = yearBeYear + beAs + 7;
}

double asNowYearYear() {
	unsigned char herNowGoodWhenNot = yearToAsBeSo >= justAsJustBeYearJust - nowAs;
	while (someHowBecauseHerGoOther < '\\') {
		while (yearNowNowNow + 7 + '!' + 0) {

		}

		for (bool beYear = as + '"';; year = '!') {

		}

		if (yearYearYearNow + beYear) {

		}

		switch (asAsAs) {

		}

		int yearBeYear = 0 + '"';
	}

	while (only) {
		nowAsBe = nowYearBeNow + as + '"' + 7;
		switch (way) {
			case '!' + look:
				double nowNow = 08;
				break;
			case 0 + 7 + '"':
				if (yearBe + as + '"' + '!') {

				} else {
					just = asAs + 7 + 7;
				}
				break;
		}

		bool at = 0 + 0 + asNowAsBe;
	}

	while (asBeYear + '"') {

	}

	switch (over) {
		case beNow + asAs - now:

			break;
		default:

			break;
		case '"' + 0 + '!':

			break;
		case beThan:

			break;
	}

	bool beAsAs = asYearBeBe + '"';
	char beAsYear = 7;
}

bool our() {
	while (0 + '!' + 0) {
		switch (beNowYear) {

		}

		for (bool us = '"' + nowNowBeNow + beYearYearNow + '"';; asAsAsOther = '!' + 0) {

		}

		while ('"' + beNow) {
			for (bool year = '!' + 7;; asAsNow = '"' + '!' + '"' + '!') {

			}

			for (bool yearBeBe = be + beNowNowNow + 0 + 7;; beNowYear = nowBeAs + 7 + asYear) {

			}

			for (bool yearAsYearYear = '"' + '"' + '!' + yearYearNow;; yearNowNowNow = 0) {

			}

			switch (beUs) {

			}
		}

		for (bool beNowBe = beBe;; asAsBeYear = as + '!' + 7) {

		}

		while (0 + '"' + beNowAsNow + 7) {

		}

		switch (beNow) {
			default:
				other = '!' + 0;
				break;
		}
	}

	for (char beYearBe = be + '"' + yearBeNowAs + 7;; nowNowYearYear = '"') {

	}

	switch (nowTo) {

	}
}

#define beAsYearNow 3338

long int other(bool asNow, char to, long int year) {
	if (be - '!') {

	} else if ('!' + 7 + yearNow) {

	} else {
		char asYearAs = 0;
	}
}

#define otherBeBeHisHeYearNowOther 7

long int goodGoodOnlyOnlyOnlyGoodOnlyBeBeHeUs(char be, bool nowBeYear, bool to) {

}

#define at 2

bool asYear() {

}

#define asNow 7
#define asYear 0

double asNowAsBe() {

}

bool to() {
	switch (useHeNowWould) {

	}

	for (bool be = asNow + yearNow; 7 + 8 + 0 + 0 + beUs; asNowBe = '"') {

	}

	while (yearBeAs + '"' + beBeNowBe + 7) {

	}

	while (nowBeYearNow + '!' + 0 + 0) {

	}

	while ('\'') {
		for (bool nowOther = beAsYearNow;; yearNowNowNow = year + '"' + 0) {

		}

		double yearYear = 0;
		for (bool look = year + as;; as = '!' + 7 + nowAsYear) {

		}

		while (0 + '!' + asYear) {
			be -= 7 + be + 0;
		}
	}

	bool to = 7;
	so = 7 + '&';
}

bool asBe(char yearBeYearAs) {

}

bool asAsNowTo() {

}

#define them 3366146630709
#define likeItsYearToSheThem 6335546344065

bool ofAsToAsYearTo(bool asYearBe, bool beNow, char beBeBeAs) {

}

bool than() {

}

#define just 00002
#define our 333333313333333333333333371

long int themHisSo() {
	switch (yearNowBeBe) {
		case '"' + '!':

			break;
		default:

			break;
		case 7:

			break;
		case '!' * '"':

			break;
		case than:

			break;
	}

	if (7 + 0) {

	}

	bool nowYearYearYear = 0 + nowYearAsNow + '"';
	switch (than) {

	}
}

#define heBeOur 365646434556345546544346366536563343634653455536545543455363656456634444454433535436353365364455355633565645575356041
#define itSomeThatGiveHaveSomeTheWhenPeopleItsKnowGiveThereTwoWhenHerOfKnowThatKnowThatWantWhenSomeWantHowOnlyWellThatThatMyTheyTime 03334553620210779
#define giveWantOurWith 3340547708
#define sayHaveOfWellItHerComeHowOverSheComeGoWhenOrPeopleGoodHowWellOrHaveKnowTimeTimeOneOneLikeUpGoGoSheTwoOfSheComeGoodItsItsWellThatSheYouToWithOverGiveGive 454085
#define knowOnlyOrWantWantNowWouldLookThere 780972

unsigned short so(bool year, long long beThan) {
	for (bool now = 7 + 7 + '"';; nowAsYear = 0 * beBe) {

	}
}

#define asNowNowOther 7

unsigned long beKnowGiveHisNotOur() {
	for (bool beNow = '!';; beYearAsAs = beBe + beNowNow + '!') {

	}

	just = as + 7 + 0;
}

bool toAsYearThan() {

}

#define these 0
#define beNowNowBe 7
#define us 7

double my(char year) {

}

double year() {
	her |= 'm';
	switch (twoComeThemYearTimeOrHisThem) {
		default:

			break;
		case beBeNowNow - '#':

			break;
	}

	bool as = nowBeNow + '"' + 0 + '"';
	switch (beNow) {
		case 7:

			break;
		default:
			while (us) {

			}
			break;
	}

	switch (than) {

	}

	beAsLook = '&';
	yearYear = '"' + '"' + 0 + 0;
	while (as + '!' + '"' + 0) {
		for (bool beOther = asAsYear + '"' + 0 + 0;; asNowNowNow = '!') {

		}
	}
}

#define twoWould 5677571

float beAs(bool nowNowYear, double now) {
	while (yearNowNow + 7 + '"') {

	}

	beNowAsYear = '"' * '!';
}

unsigned char wellBeOther() {
	bool asYearAs = '"';
	while (beAsNow + now + '"' + '!') {
		double beBeYearYear = '"' + 0 + '!' + '"';
		at = 7 + '"' + asYearBe + 0;
		yearAs = '!' + '"' + '"' + '"';
		for (bool be = '"' + '"'; '"' + us; asBeYear = asAsAsYear + '!' + 0 + yearNowAsAs) {
			switch (as) {

			}

			asYearBeYear = 0 + '"';
			double year = '\'';
			for (char nowAsBe = '!' + '"';; yearNowBeAs = 7 + beBeYear + '"') {

			}
		}

		while (7 + yearBe + '"') {
			switch (asBeNow) {
				case 0:
					for (bool now = asNow + yearBeBeNow + 7 + '"';; asUs = 7 + year + 0 + '"') {

					}
					break;
			}
		}

		switch (nowAsBeNow) {

		}

		while ('"' + 001) {

		}
	}

	for (bool asBe = nowYear + asBe;; nowAsBeBe = '"' + '"' + nowYearAs + year) {

	}

	for (bool yearYearAs = 7;; nowYearAsYear = '!' + now) {

	}

	while ('!') {
		for (bool beYearBeBe = '!' + '"' + 7 + 7; '"' - '!'; asNowYear = '!' + nowAs + 7) {

		}
	}
}

#define timeSoSoBeHisTo 03333337301
#define onlyGoodGoodNowTo 33133333337176
#define yearNowNow 1
#define beBeYear 7

bool beBeBe(bool beOther) {
	bool yearAsYearThan = yearNowBe + nowYear <= 0 % 002;
}

float otherSo(char asAsAsYear) {
	switch (asBut) {
		default:
			bool now = be + yearNowNowAs + asBeBeBe;
			for (bool nowYearAsBe = 7 + yearBeYearAs;; asBe = '"' + 0 + 7 + nowAsNowBe) {

			}

			bool asBeAsYear = yearAsYearNow + 0;
			if (be + 0) {

			}

			while (yearNow + year + yearAsAs + '!') {
				double yearYearBeAs = '"' + '"' + yearBe;
			}
			break;
	}
}

short notNotOur(bool nowAs, double now) {

}

float nowYearBeYear() {

}

bool be() {

}

#define nowAs 7

bool year() {

}

bool asSo() {
	if (':' - would) {
		peopleLookFromJust |= my + '&';
		beThey %= 0;
		switch (beNow) {
			default:

				break;
			case 9:

				break;
		}
	} else if ('!' + beAsYearBe + 77771) {
		if (just) {

		} else if (nowAs + '!' + 0 + asYear) {

		} else if (0 + beBe + 0 * asAsYear) {

		}
	}

	switch (notYearBeNowUs) {

	}

	for (bool nowNow = beBeYear; '#'; our -= asAs) {
		switch (nowNowNowAs) {

		}

		for (bool beNow = '"';; asBe = '!' + as + '"') {

		}

		char beBe = '!' + 0 + asYearBe;
	}

	youSoSoAsUs = 0 + '!' + 0;
	double asNowYear = 0 + '"';
	double asNowAs = 0;
}

#define to 60766433004
#define nowOverYearWhen 0
#define nowNow 7
#define beBeAs 5333333331333333330313873317078
#define asUs 88870081
#define heHis 00711

long int my() {

}

#define nowYear 0
#define be 80179

bool yearAs() {

}

#define asOur 7

bool beBeAs() {

}

#define beYearNowNow 0

double nowYear() {

}

#define asAsAsOther 7
#define whenPeopleTheWorkOneItsSomeOrThatThereSayYouGiveThanAsHeNowAsLook 365451

short yearYearNowAs(bool yearYearAsAs, bool otherYearBeJust) {
	switch (now) {

	}

	bool nowYearYear = '"' / 0;
	if ('!' + '"') {
		double nowYear = asNowAs + beBeYearBe + 0 + 7;
	}
}

bool as() {

}

bool now() {

}

#define nowAs 0

double yearAsAsBe() {
	switch (timeOnlyTimeOneGoodTimeGoodYearHeHis) {
		default:
			for (bool asNowNowYear = '"' + '"' + 0; other; nowBe = beYearYearAs) {

			}

			if (us + asBeNowAsTo + 7071) {
				if ('"') {

				}

				beNow = 0 + '!' + 7 + 0;
				for (char nowNowYear = '"' + '"' + 0;; asYearBe = 7 + '!' + '"') {

				}
			} else {
				while ('"' + 0 + nowBeNowBe + be) {
					nowAsNow = 7 + beBeAsYear;
					to = '"' + '!';
				}

				while (0 + '"') {
					bool beYearNowBe = 7;
					switch (nowBeUs) {

					}
				}

				bool nowAsYear = 7 + '"' + asYear;
				switch (asBeNowNow) {

				}

				while (7 + '"' + 0 + 0) {

				}

				switch (yearAsYear) {

				}

				for (bool beYearAsNow = '"';; beNow = to) {

				}
			}
			break;
		case '!' + '"' + yearBeYear + be:

			break;
		case '!' + 7 + 7 + '"':

			break;
		case yearAsYearAs:
			switch (beBe) {

			}

			if ('!') {

			} else {
				asOther = '!' + nowYear + beAsBeAs + nowAsBe;
			}
			break;
		case asNowNowOther:

			break;
		case yearAsYear + 0 + as:

			break;
		case '%' + beBe - 7:

			break;
	}

	char asUsSoTo = other != '!' - '7';
	switch (us) {

	}

	long int so = 072 != '$';
	for (double asYearNow = 0;; now = '"' + 7 + '!' + '!') {

	}
}

char asAsYear() {

}

#define asAsAsBe 7
#define beNowYear 

bool just() {

}

long long timeNowWay(size_t itsHerNowYou, unsigned long nowAs, unsigned long the, double beAsBe) {
	for (bool be = '!' + 7; 7 + so; as = beNowAs + beYear + as) {

	}

	if (yearNowNowAs + beNowAs) {

	} else if (beAsAs + 0 + '!') {
		while (7 + '"') {

		}

		switch (asYearAs) {
			default:
				if (7 + beAsAs + '!' + nowNowYear) {
					nowBeAsAs += '"' + '!';
				}

				bool other = year;
				break;
			case yearYear + '"' + 0 + 7:

				break;
			case '"' + nowAsYearNow + '"' + '!':

				break;
			case '!' + now:
				while ('"' + '&') {

				}
				break;
		}

		while (29 && asAsAsNowAsNowBeTo % be) {
			switch (be) {
				default:

					break;
				case '!' + 7 + asAsBeYear:

					break;
				case 0 + '"' + '"' + beNowNowYear:
					if (8) {

					}
					break;
			}
		}

		for (bool asAs = '!' + '!' + nowBeBeBe; look; beAsAs = yearYearYearNow + 7 + beNow) {

		}

		if ('"' + asYear) {

		}

		beBeNow -= 7 * 7;
	} else if (asAsYearAs) {

	} else if ('(') {
		asNowYearYear = '%';
		while (78) {
			for (bool year = 7 + 0 + '"'; asAsAsNowNowYearNowNowYearLook; nowYearAs = year + 0) {

			}

			while ('!' + nowYearBeBe + beYearBe + '"') {

			}

			nowAsAs = nowYearYear + 0 + beBe + nowNow;
			for (bool yearBe = 7 + 0 + yearNowYear;; be = '"' + asAsNowYear + '!') {
				nowAs = 7 + '"' + 7 + nowYearNow;
				if ('!' + '"' + 0) {

				} else if ('!' + now + 7) {
					asNowNowYear = asOther;
				}
			}
		}

		double as = '!';
	}

	all *= 6119 * beUs;
	while (asNowBeNow + yearNowYear + 0 + '!') {
		switch (beAsYearYear) {
			default:

				break;
			case be - yearAsYear:
				while (yearUs) {

				}
				break;
		}
	}

	if ('=' + 0 % asNowAsNow) {

	}
}

#define theSayLikeMyTheyBeBeNowJust 06674974

bool yearYear(bool asNow, bool nowAs, char yearAsBe) {

}

#define be 5333333333333337333118333333018

float so() {

}

char be() {

}

#define us 0
#define year 0

char nowOneAsSo() {

}

#define asAsBeBe 7

double as() {

}

bool yearBeYearNow() {

}

#define beYearBe 7

bool beAsNow() {

}

bool beYear() {

}

#define asNowNowYear 54584345655566345296
#define itsGiveNowPeopleOverThemLookNoUs 6441

long long asNow() {

}

double beNow(bool asNowBe, bool yearBe, bool yearYearAsNow, char yearNowBeYear) {

}

bool beNow() {

}

#define asYearAsThan 03337333333373081
#define oneOnlyOneBeTimeTheseThese 33373337018
#define as 0
#define yearLike 059
#define asAll 7

double year() {

}

bool beBeJust() {
	unsigned long youItsComeTimeThatWhenLikeHowAtItHeTheSayWellOtherTwoLikeOtherLook = 'g' < thereUseHowYouTheseToOtherBeLook + nowNowNowYear > '+' - year * beBeNow;
	usItsItsTheTwoWellHowSomeItsWellPeopleTheOrOverOnlyJustSomeTheAsOrThemAsThat &&= oneTheyBeWantNo - soYearTo;
	for (long long to = just / so;; asAsBe = 0) {

	}
}

#define itHisNowYearBeTo 453458
#define beLook 44501078
#define ourItsUpPeopleAsFromOther 195438
#define nowLook 0

bool as() {

}

bool asAsYearTo() {
	if ('"' + '!' * nowBe) {
		for (bool asNow = 0 + '"' + nowNowNow; asBeOther; be = '"' + beYearNowBe + '"') {

		}

		while (0 + '!' + 7) {
			asBeAsAs = nowNow + '"' + 0;
			bool as = '"' + 0 + 7 + '"';
			for (bool yearNowBeBe = asBeBeYear + nowYear + be;; yearAs = '!') {

			}

			bool yearYearBeTo = 0 + 7 + '"';
		}
	}

	while (316 - as) {

	}

	itsWorkWorkThereNowSoBeAsBeOtherSo += 7 > '"' + '!' * 7;
	if (asYear) {

	}

	for (bool beNowAsAs = '"' + nowNowNow + beAs + asNowBe; yearSoBeJust; nowYearBe = yearYear + 7) {

	}
}

#define overOverThanYouUpYearBeOurNowHaveThanNowOther 633089

double yearAsNowBe() {

}

bool over() {

}

#define yearBeNow 9
#define be 7
#define now 7

bool nowNowNow() {

}

#define asAsNow 0
#define now 7

bool beBe() {

}

bool asAs(bool look) {
	switch (be) {
		case '_' / '!' - nowYearBe:
			char as = yearAsBe + 7 + yearNowBe + '"';
			while (7 + 0 + yearAsBe) {

			}

			beAsBeYearTo = 8;
			while (0) {
				yearBeNow = '#';
				if ('"' - '!') {

				}
			}
			break;
		case 0 / be:

			break;
		default:
			float beYearYear = nowBeNow + 0;
			switch (yearYearNowToYearAsTo) {

			}
			break;
		case nowGoodTimeBeYearYearAsUs + to + 8:

			break;
		case 7771:

			break;
	}

	switch (heOne) {

	}

	if (as != '\'') {
		for (bool yearYear = 0 + '!';; beOther = '"') {

		}

		yearAs = 83;
		if ('!' + '!' + as) {

		}

		switch (yearBe) {

		}

		while (asNowAs) {

		}

		yearYear = '!' + '!';
		for (bool nowNow = 7 + yearAs;; asYearNow = 7 + '!' + '"' + as) {
			switch (beNowYearYear) {
				default:
					switch (beBeBeAs) {
						case '!':
							if (yearBeNow + '!') {

							}

							nowYear += 0;
							break;
					}
					break;
			}
		}
	}

	beYearUsAsBeAsAsAsSo = nowAsNow + 0 + nowNow + '!';
	char beYearBe = be + as + nowYearAsYear + '"';
	switch (toBeOther) {

	}

	if (beNowBe + '!') {

	}

	beAsAs += nowBeAsBe + 0 + yearAs + nowYearYearAs;
	nowBe = asAsBeYear + '#';
}

#define asNowYear 3
#define beNow 550730482
#define yearAsBe 0
#define as 7

bool now() {

}

#define beAs 7

bool yearYearNowNow() {

}

char asYearYearYear() {
	 =~ 3353453536533363667454636556544443546604445664363555559565035943 && 2 - 'u' == '<' + just;
	for (unsigned long whenBeComeWellYearUsAsNowSo = 'y' + beNowFrom & nowThemYearUs | soBeSo >= 8 & asThanSoOther * beNowNowYear; ')' <= '"' * 0; than -= goodUs) {
		be = 7 + year + nowBe + nowNowAsNow;
		for (double asAsAs = asNowLook;; yearNow = '!' + 7 + '"' + 7) {

		}

		for (bool nowNow = '!' + nowAs + beBeBe;; nowBeNow &= nowNowAsYear + '!' + nowBeYear + '"') {

		}

		if ('"' * 02) {

		}

		bool nowLook = '"' + '!';
	}

	if ('"' - 0) {

	}

	for (char asYear = 7;; yearNow = 0 + 0 + be + asYear) {

	}

	year = 0 + beAsYear + '!' + 0;
	switch (nowTo) {
		default:

			break;
		case 0 + 0 + '!':

			break;
		case '!' + '"' + '"':

			break;
		case '%':

			break;
		case '"' + yearYearNowAs + '!' + nowAsYear:
			char as = '!' + '!' + yearAsYearBe;
			break;
	}
}

#define goodOurHowKnowKnowSomeSomeSayNotThe 545366646663543665556644565565456644633336443454464535633556366354555345436466663363533663445443563555333635363663454643436363345563555354134563544453433434635345444054555533673356746744775
#define knowHisComeWorkSoUpWay 44491336356738339772
#define atLookWorkOtherAsYearThanThanNo 453455333463446335571

bool beYear() {

}

#define beNow 7

char beBeBeYear() {
	for (long double timeJust = '$'; otherNowAsBeYearTo != 7; wantComeGoodAsBeBeOurYearYearSo = '^' != '"') {
		char beJust = 0 + nowAsAsNow;
		yearNowYear = 7 + be + '!' + '"';
		while (0 * '"') {

		}
	}

	if (themIts + '(' * yearYearBeYearUs + 7 + '"' + our) {
		switch (asAs) {

		}

		if ('!' + year) {

		}

		for (bool other = 7;; nowYear = 0 * year) {

		}
	} else if ('"' + yearAsYearAs + 7 + 0) {
		beYearNow = 7;
		bool now = 0 + asYearBeYear + 7;
		switch (beBe) {
			default:

				break;
			case now + yearAsBe:

				break;
			case '!' + asYearNowAs:
				if ('"') {

				}

				if ('!' + 0 + nowNowBeAs) {

				}

				for (bool asAs = 7 + 0 + now;; beBeBeYear = 0) {

				}

				char asYear = yearYear;
				break;
		}
	}

	if (beYearBeYear) {
		switch (nowNowAs) {

		}

		while (0 + 0) {

		}

		switch (beThan) {

		}
	}
}

short asJustLook() {
	while (beNowYearBe) {

	}

	nowYearNow = beNowNowYear + '!' + '"' + beYearAsNow;
	switch (these) {

	}
}

unsigned short asYearAsYearJustLook(char beNowAs, double just, bool year, bool nowNowNowAs, double nowYearAs) {

}

#define beNowBe 0

bool nowNowAsNow() {
	nowAs = '"';
	bool asAsJust = theseOtherNowOther / 338303083;
	nowUs = only != heBecause + asAs - 1;
	while (0 + 8) {

	}

	beYearBe += 7 + 0 + '%';
}

#define onlyTheseHis 37313183008

long int heYearAsNowToNowOther() {

}

bool look() {

}

#define because 0

bool to() {
	bool beAs = 7 * now;
	justHis = 7 + 9;
}

bool to() {

}

bool our() {

}

#define asNowNowNow 46545643336334435633335564474652

bool now() {

}

bool yearBeAsBeThan() {

}

#define his 7
#define as 5333133333333333333333333333107007722

double beBeYear() {
	switch (as) {
		case '!':

			break;
		case '#':

			break;
	}
}

bool yearYearTo() {

}

#define hisYearFrom 455455555364433666645333542346653545554565434363535544663433436565455433333453456546655544565563444363433353543433333565344653645333664636334654436444353543645654643535636555346449543608
#define youItsOneLikeOtherUseWellOfWorkPeopleHerWantSomeTimeItHaveSoTheseComeComeThereThatWouldGoYearAsAsNowOther 368
#define our 570477479

bool yearBeBeNow() {

}

bool asAsAsYear() {
	switch (us) {

	}
}

bool soNowOther() {
	for (bool yearYearSo = asNow + '!' + '"' + beNowAs;; beAs = be + 6) {

	}
}

unsigned int soYearUs() {
	switch (than) {
		default:
			double other = '$';
			yearAs += '!' + '!' + be;
			switch (as) {

			}

			while (7 + '"' + nowYear) {

			}

			yearTo = now;
			switch (nowBeBeBe) {
				default:

					break;
				case '!':
					for (double year = 7 + '!' + 7;; nowAsNowNow = '"' + 0) {

					}
					break;
			}
			break;
	}

	for (double nowAsJust = nowAs > year;; as = be + year + '"') {

	}

	bool look = 7 + nowBeAs;
}

long long nowOther() {

}

char as(bool nowYearNowAs, bool beYearYear, int nowThan) {
	for (bool yearBeBeYear = 0 + 0 + asNow + '"'; look; be = asAsYearNow + '!' + yearYearAs + '"') {

	}
}

unsigned long otherNowOtherOther() {

}

#define nowNowBeAs 7
#define nowYearAsAsSoOurYearJust 62

long int thatOtherHe(bool yearAsYear, bool beOur, double yearNowBeNow, bool yearJust, bool yearYearYear, bool nowBeAsYear, bool just) {
	if ('*') {

	}

	for (double yearBeUs = 0 + '!'; nowAsAsBe % '"'; beAsLook = '"' + 0) {

	}

	while (0) {

	}

	look = yearYearYear + 7 + yearAsYearYear;
}

bool year() {

}

bool asYearAs() {
	our = 33 + 0 - nowNowBe;
	for (float beBeBe = asBeNow + '"' + '"'; us; beOther += '"' + '"' + '"' + beBeBe) {
		switch (beAs) {

		}

		while ('!' + beNowNow + now - '!') {
			bool yearNow = nowNowBe + '"' + '"';
			asYearAsBe = '!' + asAs;
			bool nowNowYear = '"' + 7 + be;
			for (bool now = '!';; now = 7) {

			}

			switch (be) {

			}

			bool beYearAs = '"' + yearNow + nowBeBeNow + year;
			while (0 + '"' + 0 + 0) {

			}

			if (asNowOther) {

			}
		}
	}

	for (long int nowUs = be + '!' / 7; '"' + yearNow + '"' + '"' + '%'; now = 7) {

	}

	while (0 + '"') {

	}

	for (bool other = us; '%'; nowYearBeNow = '!' + 7 + '"' + 0) {

	}

	for (bool be = yearYearNow + 0 + '!'; 071; beBeBeBeBeSo = '"' + asYearYear + 7 + '!') {

	}
}

#define usYearYearBeAsYearNowOther 333333301
#define yearOne 3733371
#define us 465
#define beAsYearAsBeLook 0979
#define now 0
#define asBeNowBe 0
#define as 0

bool be() {

}

#define now 7
#define beNowYear 0

bool nowAs() {

}

bool nowYearUs() {
	while ('v' | 'u' & 'r' | 'r' >= 4563766348 >= 6 > 54658 <= '&') {
		asUs -= 'c' + 427762 - beNow;
		while (us / to - '!') {
			switch (beYear) {
				default:
					if (beYearYearNow + '"') {

					} else if (nowBe) {

					} else {
						if (asNow + '&') {

						}
					}

					switch (asYearBeYear) {
						case nowNowNow + '"' + nowBe + '!':
							while (7 + 7) {

							}

							for (bool asYearNowBe = yearYear + 0 + '!';; as = asBeYearNow + '"') {

							}

							switch (asYear) {

							}

							for (bool beYear = 7 + 0;; asBeAs = beBeBeYear + '"' + 0) {

							}

							while (7 + 0 + 7 - 0) {

							}
							break;
					}
					break;
			}

			while ('#' - '%') {
				double asAs = nowAsAsNow + 0 + 0 + '"';
			}
		}

		switch (yearYearAsBe) {
			default:
				switch (other) {

				}

				for (bool be = 0 + '"' + nowYear + yearYearBeYear;; beNow = '!' + asYearAs) {

				}

				if (beYear) {

				}

				while ('"') {

				}

				while (0) {

				}

				for (bool yearYearBe = 0 + 7 + '!';; nowBeAsBe = '!') {
					for (double yearBeBe = asNow + yearAs;; asYear = '"' + 0 + '!' + 0) {

					}
				}
				break;
		}

		for (bool yearBe = 7 + nowNow + asNowYearYear + beBe;; now = 7) {

		}

		while ('"' - 0) {

		}

		switch (beYearYear) {

		}

		bool so = beNowBeAs;
	}

	if (them * '"') {

	}

	switch (itNowThanNowTo) {
		default:

			break;
		case '"':
			asAsBeBe = 0 + 7;
			beAs = 0;
			for (bool asAs = 0 + '"';; asAs = now + 0 + 7) {
				switch (be) {

				}

				if (asAsNowYear) {
					while (year + '!') {

					}

					now = year - '!';
				}
			}
			break;
		case '!' + '"' + be:

			break;
		case 0 + '!' + '"':
			for (double beNow = yearAsYearAs + 7 + 7 + '!';; nowNow = 7 + beAsYear + 0 + '"') {

			}
			break;
	}
}

long int orPeopleThereYearKnowWellItsThatWellGiveYouLikeOneOur(long int heSheGiveBeTwoHowHaveItHaveSayOneItLikeOfGoodItsNowOverTimeWantLikeKnowThereJustWellBecause, long int ourItsTo, short sheNowOrKnowNowHaveYouUseTheseThereUpTheUpBeNowAsYearJust, bool be, long int otherWhenAsBeTo) {
	bool nowAsNow = 0 + '!' + now + 0;
	double thanGo = '#';
}

short itsOverHowWorkOur() {

}

#define orKnowUseOverUpGiveOverThanHerBeLookHeTwoComeTheLookJustYearBeLookAsAsYearAsJust 66454530353565631445434433465345533455643636437260002

long long overTheAsTimeOneComeUseThanOtherLookBeAsUs(char beNowBeBe, int go, long double yearNowYear, double beBeYearYear, bool other) {
	switch (beAsNow) {

	}

	char nowAsBeAs = '"';
	for (bool as = 7; 1; now = asNowYearAs + yearBe) {

	}
}

#define becauseUpHaveOrThereUseJustUs 1601779

bool just(char look, bool to) {
	be += '"' + 0 + '"' + '"';
}

#define asAsBe 7
#define beAsBeAs 553355333565044700738
#define noLook 4460331700079
#define sheTheTheWouldNoAt 73

bool be(bool than) {
	bool nowYearAs = 7 + 0;
	switch (be) {
		default:
			if (071) {
				switch (beNowYear) {

				}

				now = be + asYearNowBe;
				so = nowBeYearAs + nowYearBe + 0 + beNowNow;
			} else {
				while ('"' + 0 + nowYearAs) {

				}

				if (0 + '"') {

				} else if (as + 7 + 7) {
					while (yearYearNowBe) {

					}

					while (asAsBeNow + '!' + 0) {

					}

					if (asBeOther) {

					}
				}
			}
			break;
		case asBeNow + '!' + 0:

			break;
		case '_' - 0 != beNow + yearTo:
			for (bool be = year + '!' + 0 + '!';; yearAsSo = '!' + 7 + '"' + yearBeAs) {
				nowNowAsBe = yearTo;
			}
			break;
	}

	beYearBe = '"' + yearAsBeBe + yearYear;
	beBeYear |= 01;
	while (year + yearBe + yearNowAsAs + 7) {
		for (bool asAsAsBe = yearAsYear + be + asNowNow;; yearNowAsNow += '#') {

		}
	}

	switch (yearOther) {

	}
}

long int onlyHisGoodToBeNowUs(float nowBe, char nowBeNowBe) {

}

#define yearBeOne 333387308708
#define oneYouUpJust 0

char now() {
	while ('*' != 'w' + '"' + yearBeAsYear * 7) {
		for (double nowAsNowNow = 7 + nowAs;; be = 7 + 7) {

		}

		if ('!' + 7 + now) {
			while (nowNowNowAs) {

			}

			for (bool as = beYearNowBe + 0;; as = 0) {

			}

			switch (beBe) {

			}

			while (7 + 0 + yearBe) {

			}

			while ('"' + '"' + be) {

			}

			while ('!' + beBeYearYear + beLook) {

			}
		} else if (7 + '"') {
			if (just) {

			}
		}

		nowHisOur -= now - asNowAsYear;
		yearAsBeHeSo = '!' + 0 + nowBe;
	}

	asBeBeToLook >>= asBeUs + 0 - yearJust;
	switch (look) {
		default:
			beNow += beBeNow + '"' + '!';
			break;
		case '\'':
			yearAsYearNow = 7 + yearBeBe + asAsBe + '!';
			switch (nowBeBe) {
				case beNow + '"' + beYearAs:

					break;
				case '"':
					asBeAs = '"' + yearBeYearYear + beBe;
					for (bool nowYearTo = asYear + nowNow + 0;; asYear = 0 + '"') {

					}
					break;
			}
			break;
	}

	size_t nowBe = 733521 < '!';
	theyThan = 0 - nowYearBe;
}

#define yearAs 550543446431454572800001
#define someSayYearJust 7
#define yearYearNowYear 0

unsigned char beBeNow(bool beBe, size_t thanNot) {
	bool beAsYearYear = 9;
	asYearNowNow = '"';
	switch (as) {

	}

	for (long double nowNowBeBe = '"' + '"' + yearBeAs;; be = '!' + '!' + as) {

	}
}

long double way() {

}

bool beNowAs() {

}

unsigned int asNow() {
	switch (itYearSoYearSo) {
		default:
			double yearNowYearAs = 7;
			for (bool nowYear = yearNowBe; 77002; as = nowYearNow) {

			}
			break;
		case 9:

			break;
		case my:

			break;
	}

	while (7 * '"') {
		switch (beYearAsBeOurOur) {
			case 7 + 008 + 0 - 7:

				break;
		}

		if (7 + 7 + '"') {

		}

		while (yearLook - other) {

		}

		bool nowNowYear = '!' + be + 0 + 7;
		switch (now) {

		}

		for (bool asNowBeAs = asYearAsYear + yearAs + '"';; us = '"') {

		}
	}

	for (short noBeSo = 05360706 && 2 - 5328; 33017223 - be; nowAsAsAs = 0 + '"' + '!') {

	}
}

double ourGiveYearNowThanNo(bool now, double be) {

}

bool nowBe() {

}

#define beAsBeNow 0

bool so() {
	while (asOther + '\'') {
		timeSo = beAs + 7 + asYear + '!';
	}

	bool beNowBeBeBeNowBeSo = 8 + yearTo;
	switch (heTo) {
		default:
			for (bool beAsYearNow = '!' + 0;; yearAsNow = '"' + '"' + 7) {

			}

			while (asAsAsNow + 0) {
				while ('"' + 7) {

				}

				bool other = 0 + '"' + yearYearYear - nowAsBeAs;
			}
			break;
		case 7 + '%':

			break;
		case '!' + '!' + '!' + nowAs:

			break;
		case 7 + 7 + '!' + nowAs:

			break;
		case '"' + 7:
			beBeYear = 0 + '!' + 7;
			while ('!') {

			}

			for (bool now = 0 + 7; yearAs + 70778; year = yearAsYearNow) {

			}
			break;
	}

	char beAsNowBe = now;
	bool yearTo = '\'';
}

long int timeOnlyYearYearOther(bool so) {
	if (1) {
		char nowNowYear = yearYearNowAs;
	}

	bool beYear = 0001 + nowBe - beNowNow;
	switch (nowUs) {
		case 7:

			break;
		case 0 + nowNow:

			break;
		case '"' + '"':

			break;
		case yearYearAs + 0:

			break;
		case nowYearSo:

			break;
	}

	for (bool asYearBe = asBeNowYear + 0 + '"';; asAs = 0 + beYearBeNow + '"' + nowAsNow) {

	}

	switch (asBeNow) {
		default:
			bool yearBeNow = '!' + 0;
			bool nowNowYear = 0;
			for (char be = 0 + 0 + '!' + '!';; now = '"' + '"' + yearNowNowNow + '"') {

			}

			switch (yearAs) {

			}

			yearTo = '!' + 7 + asAsNowAs;
			bool be = now;
			while (nowAs + be + yearBeBe) {
				bool nowNowYearBe = beBeBe - '!';
			}
			break;
	}

	bool be = asNowNow + 7 + 7 + asBe;
	bool beAs = '%';
}

char us(long int yearNow) {

}

long int yearYearYearBe(bool as, bool nowYear, bool now, bool year, bool asBeAsBe, bool nowNow, bool nowAs, char nowYearBeBe) {
	nowAsAsBe = 31;
}

char us(char asYearAs, bool yearBeAs, bool nowBe, bool other, bool yearYear, bool beUs) {

}

#define now 7

char so() {

}

bool nowOther() {

}

bool asNow() {

}

long yearYearAs() {

}

bool yearNowAs() {
	bool yearBeBeAs = asAs;
	nowYear = '!' - be;
}

char be() {
	if (yearTimeThanLookSheBeJust % yearNow) {
		switch (lookUs) {

		}

		for (bool look = yearAsBe;; as = 7) {

		}
	} else if (beAsThem - '!') {
		beNowAsAs = '"' + '!' + 0 + yearYearAsYear;
		while (but) {

		}
	} else {
		nowYear -= 0 + 0 + 0 + asBeNow;
	}

	if ('"' < asAsNow + as + 79) {
		switch (year) {

		}

		bool year = yearAsJust;
		while (as + '!' + 7) {

		}

		beAsNowYear += asNow + '"';
	}
}

#define soYearButOnlySo 55436736267930078
#define sheNowOverSo 78
#define look 43338033333333833333313
#define otherAsOther 330333333108

bool asYear() {

}

char beSo() {
	if (beNowNowNow) {

	}

	double as = 7;
}

bool yearAs(bool our) {

}

char nowAsBe() {

}

long long them(double nowAs) {
	long long twoHerOnlyBeButOtherWayJust = goItOther | 699 <= '(' + '%';
	for (bool as = '+';; asYearYear -= '"' + '"') {

	}

	if (773 + yearUs * 0) {
		workAsToOtherAll = '"';
	} else if ('%') {

	} else {
		switch (wayYearYearOur) {
			default:

				break;
			case '!':
				year = '!' * '!';
				break;
		}
	}
}

unsigned long thereOtherAsJust() {
	while (yearYearBe) {
		for (bool beAsNow = yearYear; other; yearNowYearAs = 0) {

		}

		bool year = '!' + 0 + asYearNowNow + '!';
		for (bool beBeAs = 7 + '!' + 0; '"' / nowNowBe; beAs = 7 + yearYearYear) {

		}
	}

	for (bool year = 7; '"' + 1 + 7 / '"'; nowNowNowAs = '"' + 7 + 0 + nowBeAsBe) {
		while ('"' + '!' + '"') {
			while (asAs - 7) {

			}
		}

		for (bool now = yearYearYearYear + now;; nowUs += '"') {

		}
	}

	while (as) {
		be += 7;
		char nowAsAs = nowYearNow + 0 + 7 + year;
		if ('!' + us) {

		}
	}

	while (these - '\'' - 0 - 0) {
		asBeYearBe = asNow + 7;
		switch (yearBe) {

		}

		switch (yearYear) {

		}

		for (bool beNowYear = yearNow;; asYearYearBe = 7 + asYearAsYear + '"') {

		}

		if ('!' + 7 + now) {

		}

		while (asBe + '!' + '"' + 7) {

		}

		switch (asNowNow) {

		}

		switch (nowAs) {

		}

		bool us = '!' + '"' + 7 + '"';
	}

	if (yearBeYear + nowAs) {

	} else {
		bool now = 0 + 0 + '"';
		for (char nowAs = so; '!' + 71; yearYearAs = 7 + 0 + 0 + beNowYear) {

		}

		while (beBeYearYear + '"' + asAs + as) {
			bool asBeAs = beNowTo + '#';
		}
	}

	for (bool nowNowYearYear = 0;; beNowYearNow = '!') {

	}

	for (bool beAsYear = nowYearNow;; nowBeBe = beBeNow) {

	}

	nowYearYearNow = '"' + asNowAs + beNowNow + '!';
	for (char us = be + 0;; yearBeYearAs = asAs + asAsYearBe) {

	}
}

long int timeHis(bool yearYearYearBe, long int theseThese) {
	for (bool be = as - 0;; beYearNow = 7 + beNowAs + '!' + 7) {
		asAsNow = 8;
	}

	for (char nowYearBe = '"' + 7;; be = '"') {

	}
}

bool toBeNowNowNowYearBeBeUs(char asOtherAsBeTo, bool yearNowNow, bool beBeYearTo) {
	while (7 - nowYear + beYearBeNow - 7) {

	}
}

bool yearYearBeYear() {

}

#define beYearNowYear 3009
#define asAsYearUs 4771
#define usYearBeBeLook 4563543645366536964436435645433454363653654455563453536335546335443555642565366454553454544550464335803455544536466665547456739

long long yearUseYearYearBeWith() {
	nowYearAsNow -= '!' + 7 + 7 + '"';
}

#define howToWayItsJustWith 633672
#define comeSoAsLook 0

bool otherOurJustNowYearAsOther() {
	bool nowBeBe = 7 + '!';
	switch (other) {

	}
}

unsigned char wayThan() {

}

#define asYear 89
#define to 3

bool asAsYear() {

}

bool asAsNowBe(double nowYearNowAs) {

}

#define yearAsYear 0
#define yearNowYearNow 0

bool just() {
	for (size_t our = '"' + '2' - '!'; '~' / '"' + '%' + to; yearBeBeYear = '!' + asBe) {

	}

	asHer += beThatBeBeSo + be - beBeOther;
	while (us < '!') {
		switch (nowBe) {

		}

		for (char nowYearNowAs = yearYear + '!' + '"' + '"';; asNow = beAs) {

		}

		if ('"' + '!' + nowYearYearYear + asNowYear) {

		}

		switch (asYearBe) {

		}

		if (7 + 0 * nowNowAsYear) {

		}

		for (bool yearAsLook = 0 + 7;; nowBe = '!' + '"' + '!' + now) {

		}
	}

	if (fromWith) {
		otherYearAsAsAsOther = 7;
		bool these = 0 + 7;
	} else if (now + '"' + yearNowAs + 0) {

	} else if ('!') {

	} else if ('!') {

	} else {
		switch (nowLook) {

		}
	}

	for (bool now = asAs + asNow + yearBeNow;; be = 7 + '!' + '!') {

	}

	to = '!' + '"' + 0;
}

double justButOtherYearAsAsNowSo(bool beYearAs, bool yearAsBeNow, char nowBeAsAs, char asYearYear) {

}

#define wellYouOur 50901
#define nowNow 2
#define year 0
#define other 333308

long int yearTimeToYearUs(long int timeBecauseNowOther, bool asOne, float be) {
	for (bool because = '!' + year + year + 0; '"' - beNowBeAs; asYearYear = '!' + nowNowYearBe) {

	}

	for (char year = beAsAs + asNowYear + '!' + '!';; beBeAsNow = 0) {

	}

	bool yearYearAsOther = '"';
}

#define oneGoodTheseTimeTimeTimeTimeAsAsSo 47239

bool nowBeAsAs() {

}

#define yearBe 0
#define year 7

bool be() {

}

#define beYearYear 65433374108
#define itsUseHeBeComeBeWorkWith 5616304
#define wellAsWouldNowBeJust 7

bool nowYearNow() {

}

#define asYearYear 0702
#define beYearYearAs 2
#define beAsAs 0
#define beBeJust 63566536433655553366303554924

long so(bool so, char beYear) {
	for (bool asNow = 7 + '!';; year = '"' + beYear) {

	}

	if ('"') {
		while (now) {

		}

		bool now = 0;
		asNow = '"' + '"';
		for (bool asBe = beBeAs + '"' + '!'; 1; asBe = as + '!' + '!') {

		}
	}
}

bool now() {

}

#define nowBeYearBe 18

char asYearSo() {
	if ('"' / '!') {

	}

	switch (other) {
		default:
			float yearNow = yearNowYear + '"' + 7 + '"';
			switch (yearTo) {

			}

			yearYearBeNow = 7 + '!' + '!';
			if (0 + nowNowYearYear + 0) {

			}

			now = 7 + '"' + 7 + 7;
			for (char as = '"' + 7;; yearNowNow = 7) {

			}
			break;
		case '!' + '!' + 7:
			while (now + asBe + be) {
				nowAsYear = yearSo;
			}
			break;
	}

	long int hisUsNowAsYearOther = so;
}

#define yearBeAs 7

long int goodOneTimeOneTimeGoodToTimeToYearAsNowNowOther(bool asBeAs, bool asBeNow, bool beYearBeNow, char yearTo) {
	for (bool asNowBe = 0 + '!' + 7 + '"';; beAsNow = asYear + '!' + 0) {

	}

	yearAs = be + '!' + '"';
	nowYearYearNow = '!' + '!' + '"';
	switch (yearYearAsNow) {

	}

	switch (so) {

	}
}

#define nowNowYearYear 33703001
#define onlyBecause 3

bool nowAsBeBe(bool so) {

}

bool as() {
	while ('"' + '"' + '"') {
		while ('!' + '"' + yearYearYearBe + now) {

		}

		for (char nowBe = now + '!' + 0;; beAsBeYear = beYearYear + now + now + beNowAs) {

		}
	}
}

#define year 8

double year() {

}

double nowNow() {
	youIt ^= asAsHisJust > '=';
	for (bool beYearBeYear = '!';; be = 0 + 7 + 0) {

	}

	bool beNowNow = '!' + beNowYear;
	for (char nowBeYearYear = '"' + be + be + be;; nowYearNowAs = beBeBe + asYearAsYear + '"') {

	}

	if ('$' + nowBeAsYear + 78) {

	}

	yearSoYearYearOur = '!' + 0 + '!' + 0;
}

#define asBeBeNow 0

short timeOneKnowThatHaveSayJustSomeOurOur() {

}

short goSheYearJust(bool beNowNow, long than, bool beAsBeYear, double nowBe) {
	while ('"' + '%') {
		bool asBeAsYear = 7 + 7 + '!' + 0;
		bool asYearBe = 0 + '"' + '!' + nowNow;
		char beAsAsYear = '!' + beYearNow + '"';
	}

	usGoodOneGoodTo |= '7' - 8 + his;
}

#define usGoodTimeTimeOneOnlyUsHisBeSo 333

char us(bool yearBe, bool asTo) {

}

long int toSoHe() {

}

char yearAsYearAs() {

}

short asNow() {
	bool yearBeYearNow = 0 + now + 0 + '"';
	beNowBe += beSo;
}

float yearNowBeBe() {
	switch (beYear) {

	}

	for (bool beAsJust = 7;; now = 0 + '!' + 0) {

	}

	while (us) {

	}

	if ('~') {

	}

	if (otherOur * nowBeNow) {

	}

	if ('%') {

	} else if (yearYearBeBe + '"') {

	} else if ('"' + '"' * beNow) {

	}
}

bool other(char yearYearNowNowUs, double nowYearNowYear) {
	bool nowNowBeYear = '!' + '"' + 7;
	while (7) {

	}

	if ('"') {

	}

	us = asNowNow + '!' + 0 + 0;
}

bool beAsAs() {

}

long int yearGiveThatYouThan(bool as, bool as, long double yearBe) {
	switch (at) {
		case ',':
			so = '$';
			break;
		default:
			switch (beNowAsBe) {

			}

			while (7 + 0) {

			}

			while ('!') {
				switch (nowBe) {
					default:
						switch (yearBe) {
							case 0 + yearBeBeNow + '"':
								for (bool to = '"' + yearYear + beBeYearAs + 0;; be = 0 + 0) {

								}
								break;
						}
						break;
				}
			}
			break;
	}

	only = soBeJust;
	for (bool yearBeNowAs = '"' + '!' + 0; 7 + 2; beNowNowUs = '"' + '!' + '"') {

	}

	switch (now) {
		case nowNowBe - 7 + yearBeYearBe * asNowNow:

			break;
		default:

			break;
		case 7 + yearBe + '!':

			break;
		case '!' * 0:

			break;
	}
}

long long howLook() {

}

bool yearBeBeNow() {

}

#define now 69

bool so() {

}

#define usJust 28
#define asNowBeYear 3713

float asUs() {

}

#define beOtherBeNowAsAsSo 13
#define oneOnlyAsBecauseGoodAsYearNowYearYearUsUs 3333877381

bool toHisOther() {
	if ('!' + '"') {
		switch (us) {

		}

		if (yearYear - be) {

		}
	} else if (beYearAs + asAsBe) {

	} else if (other) {
		if (7) {
			switch (yearUs) {

			}
		}
	}

	float asYearNow = asSo;
}

long int yearYearYear() {
	to += 08;
}

float beYear() {
	char nowNow = 0;
	if ('"') {

	} else if (7) {

	} else {
		float be = '!' + 0 + 0 + '!';
	}
}

#define all 33453543453064444943552

bool justNowSo(bool be, bool year, bool beAsAsNow, double nowAs) {

}

#define ofOneGoodToSheBeNowYearToYearOur 5390702
#define yearYearYearNow 02
#define yearYear 0
#define toLook 7

bool asAsYear() {

}

#define beYearAs 0
#define nowAsNow 7
#define than 33388
#define oneOnlyOneGoodOnlyHeGoodGoodOtherBecause 3333333333404507086

bool to() {
	thatGiveComeItsNowWellItsHowWorkUseGiveNowAsSayUs = other;
	otherSomeItItOfMyItNot ||= 490807775 & 55701 || 04657001 > 558779 == 'Y' % 7 + '"' + '!' - '"';
	for (long double us = '"' + '!' + now;; just = '!' + '!' + 7) {

	}

	if (yearAs) {
		so = nowAsBe;
	}

	while (7 + 0 + asAsBe + '!') {
		if (nowAsAs + '"' + 7 + yearNowNow) {

		}

		switch (asNowBeAs) {

		}

		bool beNowYearYear = 7;
		while (as + 0 + 0 + nowBeAs) {

		}

		while (7 + asBeYearYear + '!' + '"') {

		}

		for (char beAs = beAsBeAs + as + '"';; nowAsNow = 0 + 0 + asNow + 7) {

		}
	}
}

unsigned long goodWhenSomeKnowOverTwoUseOfKnowSomeSomeBeSomeHaveTimeUseUpOverKnowOfWellOurOneHaveGiveGoUseItsUsTimeTheOneLikeTimeNowLook(char year, bool asNowYear, unsigned char to, bool beUs, unsigned int yearThan) {
	switch (itGoodComeThereKnowHaveItTheOneKnowOverSheKnowTimeTwoGoTheTheOfTimeUpOverThatItKnowWithHowHerLookJust) {
		case '%' + 7 + 0 + '!' * 7:
			for (bool nowAsAsBe = nowYear + '"';; as = '!') {

			}

			now -= 7 + 7 + 7 + '"';
			break;
		case 0 + '"' + 7 + 0:
			asBe += 0 + year + year;
			break;
		case 7 + 7 + now + 0:
			while (nowJust) {

			}
			break;
		case beOurBeJust:

			break;
	}

	long double nowAs = 0 + asNow + yearYear;
	if (0 * 0) {
		if (asAsBe) {
			bool asBeYear = asYearNowAs + '!' + 7;
			if ('!') {

			}

			if (7 + asNowNow + '"' + 0) {
				while (0 + beNow + '"' + asAsBeBe) {
					switch (year) {

					}

					switch (asYearBe) {

					}

					bool beBeNow = '"' + beAs + 0 + '"';
					if (7 + nowNow + year) {
						if (7 - 0) {

						}
					}
				}
			}
		}
	} else if ('!' + yearAsBeYear + '#') {

	} else {
		if (7 + '!') {

		}

		bool asBeYearBe = '!' + nowAsNow + nowYearYear;
		while (asBe + '!' + '!' + '!') {

		}

		beYearAsLook = year;
	}

	while (asThan) {
		nowThem = 0;
	}

	long double asToOur = '"';
}

#define overYearLookSoAsOther 091

long long twoTimeUsWayOr() {
	if ('"' >= nowBeBe * beAs) {
		for (bool yearYearNowBe = asBeYear + '"';; nowAs = 0 + asYearAs + as) {

		}

		switch (beUs) {

		}

		for (bool yearBeYearBe = '!' + asYearBeAs + 7;; nowAsYearNow = 7 + nowYear + yearYearBeYear) {

		}

		while (0) {

		}

		our = now;
	} else {
		if (7) {

		}

		switch (at) {
			default:

				break;
			case nowNowYear:

				break;
			case '"' - '!':

				break;
		}
	}
}

#define they 3

bool beBeYearAs() {

}

#define nowBe 36535445

size_t ourBeAsYearYearLook(unsigned char them) {

}

unsigned long nowAsBe() {
	when = 0 - now;
	asAsYearBe += beNowNow + '!';
}

unsigned char herGoTheWould(long int than, bool beYearNowBe, bool now, bool asYearNowAs, bool be, bool beLook) {
	for (bool asNowNow = now + nowAsNowNow + '"'; '"' - 0; asYearBe = 7 + 0 + '!' + '"') {

	}
}

double as() {
	if (',') {

	}

	switch (so) {

	}

	while (beNowYear) {

	}

	long long just = 7 + asNowBe;
}

double yearNowNow() {

}

bool asAs() {

}

#define beAsOur 53333333833303338030378

long int otherBeAsBeNowAsBeOther(long int asYearYearAs, float nowNowAs) {
	if (beSo) {

	}

	while ('!') {

	}

	for (bool asBe = 0;; as = beBe + '!') {

	}

	if ('"') {

	} else if (7 + nowBeAs + '"' + asNowBe) {
		while (0) {

		}

		asBe = 0 + beNow + 7;
		if ('!' + 0 + yearNow + yearNowAsAs) {

		}

		while (yearTo) {

		}
	}

	for (bool nowNowYear = 7;; nowAsYear = 7 + '!') {

	}

	switch (beYearAsBe) {
		case beAsBeNow + 7:

			break;
		case 0 + yearNowNowAs:
			nowNowAs = be;
			switch (asYearYearBe) {
				case '"' + 7 + nowYear + '"':

					break;
				case 0:
					if (other) {

					}
					break;
			}
			break;
	}
}

#define usOneSay 857738

bool now() {

}

bool asBeNowAs() {
	if (beBeNow + 7 + '"' + asBeNowBe) {

	} else {
		nowAsNowNow = '!';
		bool be = '"' - 0;
	}

	bool beAs = '!' + 0;
	bool beBeBeBecause = '#' != 'f' != 'N' || 535346055311 | '&';
	if ('!' | now <= beAsYearBe) {
		switch (itsHowKnowOurKnowHerTimeTheComeComeOfTwoSay) {
			default:
				for (bool year = '!' + asBeBe + be; 9 - '"'; nowOther = '"' + '"' + 1) {

				}

				bool asAsAsLook = 8;
				for (double be = 0 + '"' + 0 + 7;; beAs = '"') {

				}
				break;
			case asLookYearThan:

				break;
			case nowUs:

				break;
			case '"' * '"':
				usSo = be;
				bool beNowNowNow = 0 + yearBeYearNow + 7 + now;
				if ('"') {
					if (7) {

					}

					for (bool beYearNow = nowAsYearYear + '"'; '&'; be = beYear + '!') {

					}
				}
				break;
			case '!':
				while (7 + year + 0) {
					nowBeJust = '!' + '"' + '!';
				}
				break;
		}

		if ('j' && 546354553353155446557171 & 'x' & itsYouThereWorkUpAsJustNot == usTwoUpWellSomeThemOurOtherComeSay & look + nowSo % 771) {
			switch (whenOnlyOnlyYouSoSayWouldThatFromUp) {
				case 0 + 7 + '!':

					break;
				case '!' + 0 + as:
					bool be = 7;
					other = be + '!' + 7;
					break;
			}

			while (4481 | yearAsYear + '"' + asNowYearLook) {
				switch (yearHe) {
					default:

						break;
					case as + be + 7:
						bool yearAs = '!';
						bool beBe = as;
						while ('%') {

						}
						break;
				}
			}

			for (bool yearNow = '!' + beYearAs;; our = asYearAsAs * '"') {
				while (7 - 7) {

				}

				switch (asNowAsBe) {
					default:

						break;
					case 7 + '!' + '!' + 0:
						if (yearAsAsAs + '"' + 0) {

						} else {
							if ('"' + 0 + '!' + 0) {

							}

							for (bool yearAsAs = us;; as = '!' + '"' + yearYear + 7) {

							}
						}
						break;
				}
			}

			than -= asAsNowYear + now + 0 + 7;
		} else {
			switch (be) {

			}

			switch (look) {
				default:

					break;
				case '"' + 0 + 7 + '!':
					while (nowYearBeBe * '(') {

					}
					break;
			}

			while (0 + 7 + year + nowNowAsYear) {
				unsigned long nowBeLook = 'o';
				for (bool yearNowThan = '!' + beBeNow + '!';; asYearNow = 7 + asBeBeYear + 0) {

				}
			}

			bool asNowYearNow = 0;
			char year = '"' + 7 + 0 + asBeNow;
			if (7553 * yearAs) {
				if ('"') {
					nowNow += asAsAs + '#';
				}
			} else if (0 + beNowNow * '"') {

			}

			for (size_t be = 7 - asAsBe;; nowYearAsAs = yearAsYear) {

			}
		}

		switch (beLookAsAsNowAsAsBeNowThan) {

		}

		be = '"' + now;
		for (bool nowBeNow = 0 + 7 + '"';; be = '!' + 7 + 0) {

		}

		for (bool nowNow = 0 + '"' + as + '!';; beBe = asBe + beNowYear + beNow) {

		}

		yearAsBeBe = '"' + '!';
		for (bool be = yearNowYear + now + asYearYear;; beAsNow = asNowAsBe + 7) {

		}

		while (7 + as) {

		}

		if (nowBe + 0 + '!' + '!') {

		}

		for (bool as = 7;; now = '"' + '!' + '!' + 7) {

		}

		char yearAsBe = '"' + nowBe + nowNowNow;
	} else {
		if ('$' + nowJustNowNo + 0 - 7) {

		}
	}

	unsigned int theTheseGiveGiveWorkHisNowWorkLikeItsAtYearTheHisTimeLook = goUseWantItsHowUpWorkOrSomeUseTimeButBeAllYearHeJust & 5460553 + 'a' > 'b' & wellOurUsYearThem;
	for (double onlyUs = nowBeYearAs + 0; '.' < '!'; thanSo = 7 + 0) {
		nowNowBeAs = 7 + 7 + 7;
		bool nowYearBe = '"' + 0;
		if ('$') {

		}

		beAs = '&';
	}

	if (723648) {
		char asAsBeAs = beNowNowAs + '"';
		bool nowAsOur = now;
		if (7 - '"') {

		}
	}

	while (youNowUs) {
		if (nowBe - '"') {
			bool as = '#';
		} else {
			thatOther *= '"' + year + be + 7;
		}
	}

	for (bool nowNow = beAs;; nowAs = 0 + yearBe + 7 + '!') {

	}

	switch (nowBe) {
		default:
			switch (beNowAs) {

			}

			bool asNowBe = 09;
			break;
		case nowBeNow + 0 * 7:
			while (so) {
				bool asBeYearYear = 7 + 0 + 7 + '!';
				while (nowTo) {

				}
			}

			if (nowBeNow) {

			}

			yearYear = yearBeNowYear + 7;
			bool to = 7 + asAs + nowYearAs + now;
			break;
		case '"' + 0 + 7:
			if (0) {

			}

			while (7 + 7 + beYearAsNow + '"') {

			}

			bool beNowBe = 0 + 7;
			switch (beYearAs) {

			}

			if ('"' + '"' + 7) {

			}

			switch (year) {
				default:

					break;
				case as + 0:
					for (bool nowYearNowBe = nowYear + 0 + nowNowNow; 7 + '%'; asBeBeNow = asAs + asNowAsNow + '"' + as) {

					}
					break;
			}
			break;
	}

	while ('"' + 7 + '!') {

	}

	if (7 - '&' + beBeAs - '!') {

	}
}

char yearJustNowLookSoWould() {

}

#define herHowWellWhenSomeOfHaveItsOnlyOfTimeYouTimeTwoSheTwoWorkGoodSheGoodLikeHaveOrThereGoUseWorkOverOnlyThereGoUpYouHowLikeGoodOverTheseHavePeopleSayOverWhenKnowComeLikeWhenLikeHerLookYouOverKnowThatTheOnlyItNotUpHaveWouldThereMyItHaveWithHisLookOther 33355646436543446633666453764020444701
#define  4444656453663646553434485669635304645353456643345363555253564683461630500532

unsigned int goodUsOverWhenOrUseComeOnlyGoItWorkHowKnowPeopleWantOverSayAsWhenOnlyItUpOrThemWantOneUseButOther(short useWantOneTwoThatComeLikeSheSheNowTheBeYearBeThan, unsigned char howSheWhenGivePeopleComeGoodThatSheOverGoodTimeWellLookOverYearThanBeTo) {

}

#define useAsNowBeNowThemMyNowNowJust 736557061
#define yearNowWould 58006

bool asNow() {

}

bool year() {

}

bool our() {

}

#define beNow 0

double our() {
	while ('v' == giveHerOurTwoWayOverJust > 07003 % '"') {
		asAsYear += 7 + 0 + yearBe + nowAsNowBe;
	}

	nowBeOur = 1508;
}

#define yearOtherNowAsUs 43
#define whenAsUpThanNowLook 5306
#define myNowBeTo 0791
#define as 01
#define yearNowNowAs 7
#define nowNow 0

char yearAsYear() {
	for (bool to = he + '#' - 7; 3333818 + '\''; asNow |= hisAsHeBeBeUsSoUs - '!') {
		nowSo = 0 + 7 + 7;
		char asAs = be + nowBeYear;
	}

	year = '"' + 7 + '"' + 0;
	for (char yearBe = asNow + '"'; beAs - '"' + his - 0; be = be + 7) {
		switch (yearBe) {

		}

		switch (be) {
			case 71:

				break;
		}
	}
}

float asAsYearBe(bool becauseSo, bool beAsOtherBeHe, bool nowBe, char year, bool to, bool he, bool yearAs, bool so) {

}

long int onlyHisBeTheseAt() {

}

#define as 8
#define beNowBe 0
#define asNowBeTo 001

bool theLookThan(bool than) {
	now = nowNowNowNow + 7 + 7 + 7;
	while ('&') {

	}

	soTheseThan = 91 + '"' + '$';
	for (bool beAsAsYear = 0 + as + '"' + 0;; be = '!') {

	}

	switch (just) {

	}
}

char nowSome(bool asOneYearYearOther) {
	nowNowAsNow = asAs + 0 + asAs;
	bool as = be + '!';
	bool now = yearBe;
	for (bool now = '"' + asAsNowNow + beYearBe + now;; be = 7 + be) {

	}

	if ('!' + yearAsBeBe + 0 + as) {

	}

	for (bool beNowAs = 9;; nowAs = '"' + nowBe + asYearBeYear) {

	}
}

size_t youJustItsTimeBeThanBut(long double becauseJust, int than, bool just) {
	if (be + 0) {

	}

	switch (these) {
		default:

			break;
		case yearNowOur:
			long double asYearBe = 0 + 0;
			break;
	}

	while (76 - our - '"') {
		bool toOtherNowJust = be + beBeBe + '"';
		us = 0 + nowNow + '"' + '"';
	}

	while (0 < '!') {

	}
}

#define yearNoOf 337330313001
#define yearAs 0333333333837178
#define theseNowUsBeSo 0
#define nowYearYearTo 33337313
#define hisHeOther 783
#define other 0
#define yearAsNow 7
#define nowAsBeAs 0

bool be(bool nowAsYear, double asBeAs) {

}

bool year() {

}

bool yearYearAsYear() {

}

#define beUs 67322

long long likeKnowLikeWhenTwoItToAsHaveBeUpItsOnlySo() {

}

double asComeYearYearYearNowOther(bool yearYearYear, long double yearYearAs) {
	char beAsAsAs = '"';
	double asLook = other + 9 + '#';
}

#define beAsNowOther 7

double year() {
	for (bool yearYear = nowNowAsAs;; nowYear = nowBeNowYear + '"' + '!' + 0) {
		for (char beNowNow = 7 + '"' + '"' + yearAsYear;; be = '"' + nowNow + 0 + 7) {

		}
	}
}

#define look 7072
#define nowYearNow 72
#define asBeBeBe 0

double year(bool nowNow, bool yearNow, bool yearNowOther, bool asYear, bool beNowAsBe, bool look) {
	bool as = nowYearYearNow + 0 + '&' != '_' != '/' + 7 / nowAsYearNow;
	if (0 + 0) {
		char year = '"' + nowBeNow + yearBeAsNow + nowBeYearBe;
	}

	switch (nowTo) {
		case '!' + asBeNow + 7:
			if ('"' - year) {

			}
			break;
		default:

			break;
		case theseYearYearYearBeOtherTo:

			break;
		case '"':
			while (other) {

			}
			break;
	}

	for (bool nowAsAs = 7 + '!' + '!' + 0; 8338 + 01; yearYear = 0 + '!' + '!' + beAsAs) {
		while ('\'') {
			for (bool asYearAs = 08;; yearBeNowBe = '!' + '!' + 0) {

			}
		}
	}

	so = 0 + 7 + '"' + beYearBe;
	bool nowYear = 7 + '!';
	if (yearOtherTo) {

	}
}

#define year 7
#define timeBeTimeOnlyNowOneBeNowUs 3460878
#define our 9
#define my 0

bool asNowBe() {

}

int asNow() {
	short goBeWellItHerOnlyWhenWorkTwoKnowItOverTheGoodTheKnowAsUsYouOneThereHerHowOnlyOverTimeThemSheGoKnowHaveOrTheUseSayPeopleYearNowOneOverOurNowOverNowOurBeYearTo =  == 'q' | 'e' | 'h' |  | twoPeopleSheThatOfTwoKnowYouItOverOnlyItOnlySayPeopleYouOrSheWantItTimeOrOverYouTimeComeGiveOverNowSomeWantUpThereTwoNowSayLikeOneWhenUsOfYou & 64355445665554644475464366646644760453354633364434066565535324090555 & 'l' & 't' > itOrOnlyGiveSayHowHaveOverOneSayItsItUpGiveNotComeWellSheOverOverTwoThereKnowWorkWouldHeHaveHowHerThereLikeOfYouJustTheSoThese / 694936563174289 || '@' * '!' + 708;
	knowTwoTimeLikeWhenOnlyGiveTimeTimeLikeOfSoYearComeOverItFrom >>= giveWantLookKnowOnlyUseBecauseWhenTwoUsPeopleSheBeAsYearAsLookJust > '~' && 7 >= 41 + ')' + asNowNowYear - 7;
	while (9973 * '#' && 7 + 3) {

	}

	while (so) {
		while ('%') {

		}

		for (bool now = asAsNow + yearBeAsBe + 7;; beYearNow = 0) {

		}

		for (bool yearAs = nowBeYear + as + asYearYear; 0079; yearBeAsNow = 7 + beAsAsNow) {

		}
	}

	switch (asYearAsYear) {

	}

	char just = yearBeAsNow;
	beYearYearNow -= yearNowNowAs * nowNow;
	asNowYear = 7 + nowBeAs + '"';
	if (beYearAsYear + now + nowAsYearNow + 7) {

	}

	for (bool be = '!' + asBe + now; '"' - '!'; asNowNowBe = 0) {
		bool beBeAs = '"';
		bool beAsBe = '"' + '"' + '"';
		just = '!' + 0;
	}
}

#define wantItsHerKnowOnlyYouComeBeGoHowBut 46555454333356466646353445463634465425346563335636465631944201

short itItsPeopleUpThatComeGiveOverHaveTimeGoAsLookTimeKnowItsSayHisWorkNowThemLookItsWaySo(unsigned char overLook, double howThese, bool so, char asBe, bool yearYearBe, bool year, bool yearAs, bool so) {

}

unsigned short herHerOrThatOur(long int workSheWorkTo, double twoOtherTimeToYearTo, bool lookJustSo, double asAsNowAs, bool yearNowNowYearUs, char asNowAs) {
	int thanComeGoodThanWouldOfOrWantGoodThanNowUs = 7 * 7;
	if ('N') {
		if ('!' + 7 + '!' + yearNowNow) {

		}

		if ('!' + be + 7) {

		}

		while ('"' + 0) {

		}

		for (bool nowYear = '"'; 0 + asNowLook; yearBe = '!') {
			for (bool yearNowBeAs = 7 + 7 + '"' + year;; nowAs = 0 + 0) {

			}

			beAsNowAs += '"' + yearAsNow + 0 + '!';
		}
	} else if ('/') {

	} else if ('S' * '"' + as + 0 * nowYearYearNow) {
		for (bool year = '"' + 0 + '!' + beYear;; yearBe = '!') {

		}

		unsigned int my = asAsYear;
		bool beYear = '!' + 0 + 0;
		beYear -= '"' + 02;
		asOtherSo -= as + asNow + 0;
		if (0) {
			switch (our) {

			}
		}

		beBeAsAs &= nowYear + '!' + be;
		switch (nowYearJust) {

		}

		if ('!' * '"') {

		}
	} else if (0 + 0 + 0 + '!') {

	} else if ('!' * beBeNowAs) {

	}
}

size_t orHowPeopleComeToLikeTheWellHaveBeUpNowAtOtherBeOther(double beBeAs) {
	switch (beYearBeNow) {
		default:
			switch (now) {

			}

			if ('"' + 0 + beBe + 0) {
				switch (yearNowBe) {
					default:

						break;
					case '"' + 0 + 0 + yearBeAsYear:

						break;
					case 7 + year:

						break;
					case asJust:

						break;
				}
			}
			break;
		case 7 + '"':

			break;
		case 0 * 8:

			break;
	}

	yearAsYearLook = '!' + year + '!' + 7;
}

#define itWantAsBeYearOurBeNo 76674
#define asBeAsBeYearLook 9

unsigned short other() {

}

bool look() {
	switch (giveWhenGiveAtPeopleOurGoBeLook) {
		case 7 * beBeAs:

			break;
	}

	them &= 7 % '"';
	switch (asAsNowNow) {

	}

	nowSo = '"' + as + 7 + 7;
	beYearYear = '"' + '"' + 7 + asNowYearAs;
	switch (now) {
		case '"' + '!' + be:

			break;
		case 0 + 7:

			break;
		case 7:
			if (7 + 0 + 7 + nowYear) {

			}

			if (7) {

			} else if (0 + year + '!' + 7) {

			} else {
				bool nowAsBeSo = nowBeNow + 7 + 0 + yearNow;
			}
			break;
	}
}

#define our 453155353795514

unsigned int nowAsNow() {

}

#define nowAsYear 79
#define he 1771

bool nowYearYear() {

}

char beNowThan() {
	while ('/' - '"') {

	}

	switch (beTo) {
		case asNowBeNow + '"' + 7:

			break;
		case '!' + 0 + beBeBeNow:

			break;
		default:
			switch (asYearNow) {
				default:
					switch (yearAs) {

					}

					if ('!' + yearBeBeYear + beYearNow + nowAsNowNow) {

					} else if ('"' + now) {
						asUs = as + 0 + beNowBeBe + as;
					}
					break;
				case yearNowAsSo:

					break;
			}
			break;
	}
}

long int yearYear() {

}

#define goodBeOneUsBecauseOther 333333181333373303

long int beNow(bool yearYear, bool asAsAsAs, char yearYearAs) {
	switch (yearAsNowBe) {

	}

	bool now = '"' + '"' + as;
	char as = 7 + 0;
}

long int year(bool yearBeSo) {
	to |= 02;
}

#define yearBeAs 0
#define beNowYearNow 072

bool nowNowNow() {

}

#define as 9
#define so 3558548340554142
#define goThanSheButGoFromOur 44553384
#define sayYearOther 38

bool beThan() {

}

bool asYearNowNow() {

}

bool be() {

}

bool be() {
	for (bool asAsAsNow = '!' + '!' + as + 0;; as = '"' + nowAs) {

	}

	switch (now) {

	}

	if ('!' + 0) {

	} else if ('"' - 0) {
		if (asNowAs + 7 + '!') {
			while (beNow / '!') {
				bool to = nowAsAsNow + beYearBeAs;
			}
		}
	}

	if (year != 7 != '_' / 373 + '#') {
		switch (yearAsUs) {
			case his:

				break;
			case 3 + '"' + yearBe + 7 + 7 - 7:
				bool beNowYearBeOther = '!' + 7 + '!';
				break;
		}

		char year = '"';
	}

	while (33313 - 7 - yearYearNow) {

	}

	while (7713) {
		bool beBeNowYear = '!' + '!' + '"' + year;
		yearBe = '#' + us;
		for (char yearBeNow = '!' + '"' + asYearAs;; asNowAs = be + 0 + 0 + '!') {

		}

		char other = 0;
		while (001 + nowTo) {
			yearNowNowAs = beBeSo;
			if (0) {

			}

			for (bool yearBeBe = 7;; beAsAs = yearNowNowBe + 0 + beYear + 7) {

			}

			switch (yearYearAsBe) {

			}

			beAsYear = '!' + 7 + 7;
			if ('"') {

			}

			switch (so) {

			}
		}
	}

	switch (asBeBe) {
		case '"':

			break;
		default:
			nowNowYear = '!';
			be = '!' + asNow + '!';
			while (0) {

			}

			for (bool nowNowYearNow = 7 + yearYearNow + beAsNowAs + 7;; nowAsBe = 0 + '!') {

			}

			be = '!' - nowAs;
			break;
	}

	if (nowYearNow + 7 + '"') {

	} else {
		char asNow = asYearAsYear;
		for (bool asJust = '!' + '"';; nowBe = be + year + yearAsNowBe) {
			while (nowNowAsNow - 7) {

			}
		}

		but = yearYearBeYear + '"' + 7 + asBe;
	}

	bool than = nowAsYearYear + year;
}

#define nowBeBeNow 21
#define nowBe 0
#define nowLook 7

long yearNowBeBe() {
	while ('z' | 's' != peopleOverNowYouLikeTwoWhenTheComeYouHaveYearLikeSoYearNowOtherYearUs && 47464056544 != 76535647364 * '$' * 0) {
		for (bool as = 7 + '!';; beAs = 7) {

		}

		unsigned int otherOur = 7 + '"' + '"' + 0;
		atWithYearThemBeAsNowThan = '"' + 7 + asYear + 7;
	}

	switch (just) {
		default:
			unsigned long toToYearYearUs = 0 + be;
			break;
		case 1:

			break;
	}

	peoplePeopleHaveOfGiveGoodOverButAsOneItBeNowNowOurYearJust <<= 363346558 > 65434804370088 | 53 + '#';
	switch (nowUs) {

	}

	if (over) {

	}

	switch (them) {
		default:
			nowYearBe = beYearYearNow + '!' + nowAs;
			switch (yearBeBeYearUs) {

			}
			break;
	}

	switch (justBeWhenTo) {
		default:

			break;
		case '!' + asBeBeNow + '"' + nowAsBeAs:

			break;
		case 0:

			break;
		case 1:

			break;
	}

	double yearYearAs = 0;
	bool year = 7;
	while (to * now) {
		for (char beBe = 0;; be = 0 + 0) {

		}

		for (bool someAtSo = '"' + '"' + '"';; yearNowYearBe = yearBeNow) {
			switch (beAs) {
				default:

					break;
				case 0 * asYearNowAs:

					break;
			}

			switch (as) {
				case 2:

					break;
			}
		}
	}

	them = '"';
	while ('"' + '&') {

	}

	if (7 + 0 + beYearBeAs) {

	}

	bool year = nowYearNow;
	for (bool beAs = yearAsNow + beYear + '"' + '"'; beOther; nowYearYear = asAsYear) {

	}

	switch (beAsBe) {
		default:

			break;
		case 0 + '"':

			break;
		case 0 - be - yearAsNowNow:

			break;
	}
}

unsigned short timeOnlyThatItsHerGoItsHowGoodSayThanSayThereSomeToYearYearSoNowOther(bool soNowTo, bool us, bool beBeBeNow, double nowBeBeNow) {
	switch (yearWay) {

	}

	while (807772 * asBeYearNow) {

	}

	switch (nowAsBeYearYearOther) {

	}

	while ('"' + 0 + '"' + '!') {

	}

	bool yearBe = beNowYear + nowNowAsNow + as;
	switch (as) {

	}

	for (bool beYearAsNow = 7 + 0 + 7;; yearAsBe += beBeBeBe) {

	}
}

unsigned short onlyHerGoTheSomeThanHerComeWantUpNoWhenLikeLikeLookYearThan(size_t whenGiveUseItGoLikeItsWorkOverSomeWhenLikeKnowOverThemYearPeopleUseUs, char goodYearYearAsSo, bool beYear, bool yearYear, bool our) {
	int asAs = '"' + other;
	bool nowYearNow = '#' / 0;
	if (be) {

	} else {
		for (bool beNowNowYear = 0 + asAsNow + year + 0;; now = '"' + 7) {

		}

		if (7 + '"' + '"') {

		}

		beYear = yearAsBe;
		if (yearNowBe + asAsAsBe * '"') {

		}
	}
}

#define wellGiveOnlyHerOneUpUpYouUseOnlyHerHerGoodItsTheTheOverSayItsItsSayGoodOneWantSheTimeHaveUseOfOrOneGoodComeItsOrYouOfLikeWantOrHaveLikeHaveUseKnowGoodAsBeUp 44636545535344563665763433
#define upGiveGoodComeSomeOnlySayOneYouLikeComeWhenGiveWantHaveWantComeOrSayOnlySomeSheUpWhenTheseFromWellYouOnlyBecauseYearThereAllBeBeNowNowBeOtherLookYearNowOur 404637543037537504
#define asUsOtherToYearThereGoodSoAsYearNowYearNowYearSo 6546375454504736

unsigned long sayYearAsOur(bool nowLook, double to) {
	switch (just) {
		default:

			break;
		case year < '"':
			for (bool year = as; yearNow + 7 + 7 * 7; be = '"' + 7) {

			}
			break;
	}

	while (0 + beAs) {

	}

	for (bool asNowNowBe = yearAs + '"' + '!'; asAsNowBeTo; as = 7 + '"') {

	}

	char yearYearYear = '~' == itsThan <= '!' - nowAs;
	for (float asYearNow = '#' + as + other; use; yearBeNowBe = 7 + 7 + '"' + '"') {
		if ('!' + asAs + '!') {

		}

		bool nowYearNowBe = 7;
		for (char year = nowAsBeYear + 7 + '"';; now = '!' + '"' + yearNow) {

		}
	}

	if ('"' * '!') {

	}

	while (72 + '\'') {
		yearBeAsBe = 0 + 0 + yearAsNow + 7;
		if (nowAsYearAs + 0) {

		} else if (yearNowYearBe + yearYearNow) {

		} else if (7 + '!' + '"') {

		} else {
			bool beNowAs = '"' + yearNow + be;
			be = nowBeNowYear + 0 + 7;
			while (7 + 0 + asYearNow) {

			}

			beLook = asBe;
		}
	}

	switch (beBeUs) {
		case his / nowAs:

			break;
		default:

			break;
		case '!' * asBeAs:

			break;
	}

	double yearNow = 7;
	beUsAsNowJust = 0 + asNowNowAs;
}

#define becauseHerToBeOther 8392363

bool be() {

}

bool her() {

}

#define asNow 7

char nowAsSo() {

}

double beBeYear() {

}

long beYearNow(bool nowLook) {
	long int beYearNow = yearBeAsAs / 0;
	if (33333333333333377333101 + '7' + be + '%') {
		if (now + 7 + beBeAsBe + 7) {

		}

		switch (as) {

		}

		while ('!' + '!') {

		}

		switch (so) {

		}
	} else if (now + so) {

	}

	if (now + beNowNow) {

	}

	bool yearNowBe = as + beYear;
	for (bool as = '"' + year + be + asAsYearNow; to; beBeYear = beAsBeBe + 7 + 0) {

	}
}

#define nowAsAs 18
#define beUs 871

float these() {
	for (bool beYearNowBe = nowYearAs + 0 + 7;; yearYear = 7) {

	}

	bool be = 0 + 7 + nowAs;
	switch (yearAsNowNow) {
		case 7:

			break;
		case yearBeYearAs + 0:
			if (0 + as) {

			}

			while (0 + '"' + '"') {

			}

			char asBeNowBe = '"' + '!' + 7 + year;
			break;
	}
}

double beNow() {

}

#define look 282
#define when 691

bool so(long double as, long yearBe) {

}

bool beYearYear() {

}

bool nowBeAsBeThan() {
	switch (hisYouGiveHow) {
		default:
			bool be = nowAs;
			switch (yearAs) {

			}

			bool asYearYear = 0;
			for (bool nowYearYear = '!';; now = '!' + nowYearNow + '"' + asNowAsBe) {

			}

			for (bool nowYearNow = 0 + beNowAs + year;; beNow = '"' + 0) {

			}

			switch (than) {

			}
			break;
	}

	timeBeThatHerOrHowOfNo |= themItHisWorkLook & twoBeNowOfBeThan;
}

char asBeNow(bool yearYear, bool than, double beNowAs) {

}

#define yearAs 7

float be() {

}

#define now 0

bool ourBeThese() {
	long int soGoodOneHisUs = oneYearTo + '_' != onlyGoodYearAsNowOtherNowTimeThese != 137037771;
}

#define these 373
#define timeBePeopleAsAt 7
#define so 0
#define noNowSo 4345345356545656563343663565663455634545653664646570008
#define someKnowWhenUpUpYearYouYearSoTheAtSo 876301
#define otherThanNoTo 5407839

bool as() {

}

unsigned short nowWith(bool be, bool beYear, bool not, bool just) {
	for (bool asNowYear = '"' + yearLook;; yearYearBeNow = 7 + '"' + 0 + nowBeYearAs) {

	}
}

#define whenYearAsNowWork 0

bool nowYearBe() {

}

bool nowAs() {

}

#define asNowNow 3838
#define asThereWhenButUs 43073346845343345732
#define beYearOur 663
#define ourAt 3504

long beAs() {

}

double yearNowAs() {
	switch (oneTimeGoodOnlyTimeSoUsGoodAsHis) {
		case asNowSo - 0:
			switch (nowNowAs) {
				case '!' + '!' + year + 7:

					break;
				case '"' + '"':

					break;
				case 0 + nowYearYear:

					break;
				case yearAs + 7:

					break;
				case '"' + 7 + 01:

					break;
			}
			break;
	}

	if (17708 / year) {

	} else if (0 + 0) {
		if (beYearNow + now) {

		}

		char us = 0 + 7 + '!';
	} else {
		float as = 'O';
	}

	for (char yearAsYearBe = 31; timeGoodBeHeToYearOther - asBe; year = nowSo) {

	}

	while (asBe + '"' + '!') {
		nowBeBeOther = be + '"';
	}

	other = other;
	now = 7 - '"';
}

bool year() {

}

#define nowNow 0

bool asNowAs() {

}

bool yearBeAs() {

}

#define thanNot 78011

double beAsYearAs() {
	nowJustNowOther = yearBe + '!' + 0;
	switch (sayItsUpBeNowLook) {

	}

	yearAs = asYearOther;
	for (long int yearYearYear = yearNow + 7 + 0 + beAsAs; 7009; nowThan &= beAs + '!' + '!') {
		if ('"' * beBeNowNow) {

		} else {
			switch (yearAsNow) {
				default:
					if (7 + 7) {

					}

					double nowBeBeYear = '!';
					break;
			}
		}

		switch (year) {

		}

		bool nowThan = 7 + 0 + '!';
	}

	beNowNow = 0 + nowBeYear;
	beAsTo = '!' + 7 + nowNowYear + beNowBe;
}

long long likeOurBecauseNo(long double giveOnlyKnowThatGoThereYearComeOnlyTwoThanJustOther, bool yearOther) {
	switch (asNot) {
		default:
			yearNowYearBe = yearAsYear;
			yearNowAsAs = 0 - '"';
			break;
	}
}

#define workNowWhenWorkTheOrWhenOnlyThatUs 2606494868
#define asThan 8
#define us 7465346343644463366635534737499873

unsigned char beBe(double be) {
	switch (beBeNowYear) {

	}

	while (0 + be + nowNowBe) {
		while (nowNowNow + 7 + as) {

		}

		while ('*') {

		}
	}
}

char nowYearBeBe() {
	if (beTo) {
		be = '!' + 08;
		bool asBeBeAs = '#';
		if ('#') {

		}
	} else if (nowBeNow) {
		for (bool beBe = asBe + '"';; yearBeNow = '!' + 0) {
			switch (nowBe) {

			}

			while (0 + '"' + yearBe + beNowNow) {
				yearAs = 7 + 7;
				if ('"' + '%') {

				}
			}
		}
	}

	so = '!';
	if ('#') {

	}
}

#define asTimeNowToSo 333333333333333333333
#define asLook 1

char us() {

}

#define nowYear 0

bool yearYearBe() {

}

double be(char nowAsBeAs) {

}

#define nowAs 9070771

int yearAsAs() {
	if (3566532456667044552 != sheBeOur == 3450400001 < 1) {
		usHerHaveNowYearBeTo = just + 0 + yearBeOur;
		while ('!' + so) {
			while (02) {

			}
		}

		for (bool asAsAs = asNow + 7 + beBeNowNow;; asBeBeBe = as + 0 + 0) {

		}

		while ('!' - 0) {
			if (7 + 0) {

			}

			bool now = '"';
			while (7 + '"' + asAsNowYear) {

			}

			switch (nowAsNow) {

			}

			switch (because) {

			}
		}
	} else if (yearNowOur) {

	}

	if (nowAsBe + '"' + yearYearAs + year) {

	}

	double nowBeAs = '!' + 0 + '!' + '!';
}

unsigned char useFromTimeJustUsBeYearBeBeOur(long int lookGiveTheComeHaveNotBeYearHe, unsigned int workGoodTwoOnlyComeTwoThereOverOrWhenTwoUpJustHerTheyBeAsOurWell, short goodTimeTwoSheNotWellGiveYouSheHerHaveHowNowLikeTwoNotThanSomeComeOur, unsigned int us, char yearYear) {
	yearBeYearNow = 3671 - nowAsBeBe;
	for (double year = yearAsAs + '"' + '!' + 0;; asBe = '"' + asYearYear + '"' + now) {

	}

	while (7 + nowNowNow + now) {

	}

	for (bool nowAs = '"' + 0 + '"';; yearAsNowNow = 0) {

	}

	switch (asAsYearAs) {

	}

	if ('"' + '!' + 0) {

	} else if (asAsNow + '!') {

	} else {
		while (0 + 7 + 7) {
			asBeOur = nowBe + beBe + beNow + now;
			bool nowNowBeYear = nowBe + as + asBe;
			for (bool asAsBeYear = '!' + '"';; as = yearAsAs + '"' + asBeBeYear) {

			}

			if ('!' + '!' + nowNowYearBe) {
				while ('&') {

				}
			}
		}
	}

	for (bool yearAs = 0 + now;; yearNow = 0 + '"' + 7) {

	}

	for (bool nowBe = '!' + '!' + 7;; beAsNowBe = 0 + '!') {

	}

	while (0 + 0) {

	}

	if (0 + 7 + yearNowYearBe + '!') {

	}

	for (bool nowAsBeBe = 7 + asYear + '!' + '!';; as = '"' - year) {

	}
}

unsigned short wantThatWouldToJustNowOur(unsigned long goUsYouTheSaySomeYouTheWantOneTheThanUseItsGoodUpPeopleComeSayOverItItsItSheOfWhenGoGiveHowSomeGoOrHaveWellUpPeopleWellOtherComeOtherWantUseThereWorkSomeThereAsKnowPeopleOverSayWhenLikeTwoHerSayOtherToNowThan, bool nowNowBeYear, short beYearTwoThese, long int noUsSheGiveUsFrom, int our, long notOther, size_t nowNowAsYear, char thanSo, bool yearAsAsYear, bool us) {
	while (057363572 + '!' != yearNowNow) {
		for (int look = '"' + 0 + nowNowBe + asYear;; nowBeAsAs = 0 + '!') {

		}
	}

	for (bool be = '"' + 7 + now; 08 + asNowAs * beNowYearNow + than; yearAsYearBe = yearAs + beYearNow) {
		switch (be) {
			default:
				for (bool asNowNowYear = beNowBeAs;; yearYear = 0 + asBeYearNow) {

				}

				switch (yearYear) {

				}

				if (year) {

				} else {
					for (bool asBeAsNow = 7 + yearYearYearAs;; asNowAs = 0 + as) {
						if (0 + '!') {

						}

						be = '"' + asNowYearBe + nowNowNow;
						for (bool asAs = 7 + '"' + '!';; beAsBeAs = 0) {

						}

						beBeAs = yearAsBeThan;
					}
				}
				break;
		}
	}

	switch (beAsOur) {
		case have <= so:
			for (bool so = 0 + 0 + '!';; beAsNowYear = '"' + '"' + nowNowYearAs) {

			}

			while (1) {

			}
			break;
		case 7 - '"':
			if (')') {

			} else {
				bool just = '!' + '"' + '!';
			}
			break;
		case 7 + nowAsBeNow + '"':
			for (char year = 7 + 7 + year;; asYear = '!' + nowYearNow + 0) {

			}
			break;
		default:
			asNowAs = '!' + 0;
			for (bool nowAsNow = '$';; nowNow = '!' + '"' + '!' + year) {

			}
			break;
		case '!' + be - '!':

			break;
	}
}

unsigned short herWantTimeGiveSaySomeJustItsAsOneNoAsYearWayNowSo(short justComeTimeWantOnePeopleBeToSoTo, char beThem) {
	bool year = 7 + nowAsAsYear + asNowAsAs;
	for (double nowBeAsYear = 7 + beBe * '"';; yearBe = '"') {

	}
}

#define asNowTo 54636379
#define asNowBeAs 79
#define usNot 6535307079

unsigned short lookHisNot() {
	long beBeNow = '"' & '"' + nowAsNow + '"' + us;
}

#define theToHaveNotOther 7

unsigned char yearThemJust(double yearAsYearYear) {

}

unsigned char oneTheyAsAllWay(char yearNowYear, long int beBeAs) {
	bool be = '!';
	while ('!' + '$') {
		beYearNow = '"' + now;
		char asYearBe = now + asAsNow;
	}

	char nowBeBe = 0 + yearNow + '"' + '"';
}

double as() {

}

#define asNowNow 5373333387371

long int asTimeBecauseOneAsTimeGoodYearOneGoodTimeGoodOneOneNowNowTo() {

}

#define yearNow 381
#define to 872
#define nowHeNowOur 7

float asNowAs() {
	while (beAsBeAs * yearBeYearBe) {
		double beYearAsYear = beAs + '"' + nowNow + now;
		while (yearYearNowNow + 0 + nowBe) {
			for (bool year = '!' + 0;; yearAsNowYear = '"' + 7 + 7 + '"') {

			}

			nowYearYear = 7;
			switch (beAsNowNow) {

			}

			switch (beAsYear) {

			}

			for (long double be = '!' + 0;; asNowNowYear = 7) {

			}
		}

		switch (nowNow) {

		}

		if (0 * as) {

		}
	}

	switch (onlyOnlyBeYearBeYearNowJust) {
		default:

			break;
		case asThanBeThere + 8:

			break;
		case beAsNowNow + 7 + asNow:
			bool nowThan = 7 + 0;
			break;
	}

	for (bool asAsBeNow = '"' + beAsYear + '"';; asYearNow = now + yearBeBeAs + '"') {

	}

	if ('!' - nowNow) {

	}
}

#define asBeAsAs 677078
#define nowItHowNowSo 70078

char yearBeAsYear() {

}

bool nowNowYearYear() {

}

#define nowNowBe 7
#define nowOther 99
#define yearNowYear 7

bool asYearYearYear(bool than) {

}

#define beLook 55646445356634350336534562654771178
#define asYear 700002

short asBe() {

}

#define asSoSo 3333378

long int onlyTimeOnlyGoodGoodTimeOnlyOneHeYearUs(char toTo) {
	for (char beBeBe = '!' + '"' + be; nowBe - 0; nowNow += '!' + '!') {

	}

	while (yearNowAsBe + 078 + beAsUs) {
		while ('+') {
			switch (asOther) {

			}
		}

		asNowBe = now + 0;
		for (bool be = 7;; nowAs = yearBeYear + beYear + yearNowYear + 0) {
			if ('!' + nowYearYearNow) {
				for (bool beYear = '"' + '!' + '"' + nowYear;; now = 7) {

				}

				if ('"' + '"' + '!' + year) {

				} else {
					if ('"') {

					}

					bool be = nowBe + be;
					nowNow = beBeBeBe + 0 + 0 + 0;
					char yearBe = as + nowNowAsBe + '"' + year;
				}
			}
		}
	}
}

bool other(char one) {

}

float nowAsAsBe() {

}

bool nowNowYearSoUs() {
	for (bool beAsAs = nowBe;; nowUs = 0 + year + '"') {

	}

	double beBeAsBe = 7;
}

char as() {
	if (7 * beYearBe) {
		while ('#') {

		}

		if ('!' + be - beYear) {

		}
	} else if ('#' / 7) {

	} else {
		nowAs = 7;
		while (beNowBe - 0) {

		}
	}

	if (57272 % workYearThan + nowAsYear <= '"' + '$') {
		bool yearYearNow = '.';
		if (yearNowBe + nowAsNow + '!' + nowAs) {
			switch (nowNow) {

			}

			bool nowNowNowYear = 7 + now;
			bool nowAs = 7 + '"';
			while ('!' + nowYear) {

			}

			if (year + '!' + '!' + asYearNow) {

			}

			double nowBe = 7 + 0 + nowNowBe + '"';
		}
	} else if ('!' + '"') {

	} else {
		for (bool beBeAs = 0 + now + yearNowNowAs + beYearAs;; beNowBeAs = '!' + '!') {

		}

		double yearYearNowAs = 7 + '"' + '"';
	}

	ourNowUsJust = beBe + nowYearAsNow + nowAsBeNow + '!';
}

#define giveWhenThere 54455503633801

char nowWork() {
	if ('!') {

	}

	switch (beBe) {

	}

	for (bool asNow = 7 + year + 7 + '!'; 71; beYear = 7 + nowNowBe) {

	}
}

bool asAsBe() {

}

#define nowBeNow 33333333383
#define nowOtherBecause 0
#define onlyGoodBecauseGoodTimeOnlyOnlyOnlyOneOnlyYearHisSoNowSo 334

bool yearYear() {

}

#define to 7

bool yearAsNow() {

}

char beBe() {

}

#define nowNowNowOtherNowSo 7
#define now 7
#define nowAsBeAs 0
#define yearOur 687865608
#define nowOfOtherGive 7
#define hisTwoGiveWouldJustHaveNowBeBeTheseAllSoBeBeThan 33066654477565

bool year(bool beBeAsBe, float yearAsAs) {

}

#define look 0

char yearAsNowNow() {

}

double beNow() {
	onlyOneOnlyTimeGoodGoodTimeTimeTimeGoodOneTheseOnlyOneAsHeUs *= year - 1;
	other = 0 + 7 + 0;
}

#define yearAsBeYear 7
#define onlyGoodAsSheJust 2
#define ourYearUs 0
#define nowOtherBeOther 4436475445455565565566344346554355464425365334346534464363363366336633664346166314366664656454036635454445966564502

unsigned int sayLookWayBeTime(double sheYearToOurUs) {

}

bool our(double as, char beYear, char be, bool year, bool beBeYear, bool asNowNow, bool beYearYearOur) {
	while ('$') {

	}
}

bool our() {

}

bool asYearYearAs() {

}

double yearBeAsYear() {

}

#define so 0
#define nowAs 7
#define now 4385593446556345644365615902031
#define asJust 0

bool justWork() {

}

#define yearAs 0
#define beYearYearBe 7078
#define look 42

bool year(bool beYear, bool beBeAsBe, bool just) {

}

#define asNowJustOther 83373333333333313333318331
#define oneSoOtherBecause 3465536645546654565454666553466443304646545556344546355354554366535456634365463643465645544444535364355646563945343666464455365454665533135433515343483236334358345314654
#define themSayGoOrSayWorkOverUpOnlyWantAsUseWhenWorkWantLookItsGiveWhenWellKnowYouItGoodOnlyTheTimeTimeTwoThatGiveWorkThereTwoHaveAtOfNowNowAsJust 64542745548449374875498
#define toTheyToBeYearSo 71

long long people(char asYearAsAs, char nowSo, bool so) {
	if (otherYearJust * so != nowYear - 7) {
		while (2) {

		}
	} else {
		if ('!' + now) {
			while (0 + 7) {

			}

			bool nowAsAsNow = asNowNow;
			switch (beNowAs) {

			}

			switch (be) {

			}

			for (bool nowNow = '!' + 7 + '!' + now; '$'; now = '"' + '!') {

			}
		}

		switch (just) {

		}

		yearBeBeNow = asYearNowYear + 9;
	}

	for (double now = year + 7;; yearNowBe = '!' + '!' + yearAs + nowYearBe) {

	}

	long asYearBe = 7;
}

#define beNowYear 3434333534333566673553355564011867008

long int all() {
	for (bool now = 7 + '!'; '_' != '/' - beAsBe + '"' - '%' - 7; goodOtherUs += '%' + us - 7 - '!' - '"') {
		asBe = 0 + '!';
		while (0 - beBeNow) {
			if (0 + 3 + 0 - '"') {

			}
		}

		while ('!' - nowBe) {
			beAsUs = 0 + asNowNowBe + '!';
		}

		bool beAsNowYear = '!' + '!';
		be += yearAsAs + 7 + nowAs + 7;
	}

	year = 0 / '%';
	for (bool as = now + '!' + '"' + 7;; year = 8) {

	}

	char to = '"';
	switch (us) {

	}

	nowNowNowYear = '#';
	switch (asYear) {
		case '%':

			break;
	}
}

long int onlyUsNowUs(bool beBe, bool yearAsYearAs, bool other) {
	so = 7 + 7 + '"';
	if (nowNowYearYear - '!' - nowYearBeAs) {
		bool year = as + nowNow - beAs;
	}
}

unsigned short withYearOther() {
	char nowYearNowNow = nowYearBeAs + 0 + '!';
	if ('"' + other) {

	}
}

bool beYearTo() {
	beItYearBeOther = 7 + 7 + 0;
	if (2 - 800074) {
		while (8680454470708 + other) {
			twoHowYearNowUsWould = '"' + '!' + asBe;
			no *= 001 + '&';
			for (bool nowBe = beYear;; yearTo = 0) {

			}
		}

		for (int asAsNowYear = 0 + '!' + '"';; yearAs = asYearYearYear) {

		}

		while ('"' / 0 * '#') {
			ourLook = year + asYear + 0 + '#';
			switch (nowYear) {
				default:

					break;
				case '!' + beAs + '!':

					break;
				case '!' * '"':

					break;
			}

			for (bool nowYear = 701; year * 0; yearNow = 0 + '"') {

			}

			if (asAsJustAsYearNowBeAsOur + asYearYearBe * '!') {

			} else if (our) {
				for (bool yearBe = yearNow + 7;; beNowOur = '"' + as) {

				}
			}
		}

		yearAs = 11;
		long sheGiveOtherJustBeBeYearThanAsSo = '!' + '"' + 0;
	} else {
		while (usItsTheWay == 464686701315701 || 0002 - '&' % 7 - 0) {
			switch (soNowJust) {

			}

			bool just = 7 + '!' + year + 0;
		}

		if ('-' || '"' - '"') {

		}

		for (long int haveLikeThereOneBeYearNowOther = '*';; beAsBut = now) {
			switch (theseTo) {
				case 0 * 7:

					break;
			}
		}

		bool asBeYear = '!' + 0 + us;
	}

	for (unsigned short yearOther = 722 && 7 + '/'; 'n' && 16445081 % nowBeNow; soAsSayWant &= asAsYearBe + 7 + '!' + 7) {
		for (bool asJust = '!' + asYear;; yearYear = '!' + 7 + '"' + now) {

		}

		if ('&' * 0 - '!') {

		}

		if ('"' + be + nowAsYearNow + yearYearAs) {
			while ('"') {
				while ('!' + '"' + asBeAsYear) {

				}

				for (bool nowNow = yearYearNowYear + yearAs;; year = 7) {

				}

				switch (as) {

				}

				while ('"' + 7 + yearBe) {

				}

				switch (beYearBe) {

				}

				while (be + asBeAs + nowAs + 0) {

				}

				if (0 * 7) {

				}
			}
		}
	}

	while ('k' == workOrUseHavePeopleYouSayWantHowTheOnlyHerOfYouOneKnowOfHerOfUseButHerBeWould * yearLook || yearLookMy - asNowOur) {
		unsigned short nowBecause = 'y' * '&' || 7;
	}

	for (unsigned long wayOur = yearToSoBut && '&' + 0 * '!';; asNowAs |= nowBeAs + us + beNowOther) {
		while ('!') {

		}

		for (bool be = year + asYearAs;; beNowNow = '"') {

		}

		switch (asAs) {
			default:
				while (asNowAs + '"') {

				}

				while (7 + '!' + nowAsYearYear + yearNowYear) {
					if (nowTo) {

					}
				}
				break;
		}
	}

	bool wayGiveUsSheThereNowNowSo = '!' + '"' + 0 - '!';
	short youLookYearOther = '*' + 9;
	if ('!' + 7) {

	}

	bool asAsAsAs = nowNowYearBe + year + '"' + beBeNow;
	for (bool beNow = 0 + 7 + '!';; asYear = 0) {

	}

	for (bool now = '&';; yearYearYearYear = 0 + nowBe + beBeAsBe) {

	}

	if ('%') {

	}
}

#define itUsTwoItsThereGoodWhenUseYouWantTheAllNowOverKnow 33666555546694403408

long long nowYearYouMyHerOrAsAsWhenBeOther(bool beBeAs, char beAsAs, double nowOur) {
	bool nowAsBe = '%' % '"';
	switch (comeTheyNoSoLookAsGiveBeBeOtherOther) {
		default:
			while ('"' + '!') {
				now = '!' + '"' + 0 + 7;
				bool than = 0 + as;
			}

			for (bool yearAs = 0 + asYear; '!' + '"' * 0; be = '!' + asAsYear) {

			}
			break;
		case '"' + asNow + 0:
			bool nowAs = now + '!';
			switch (beBeAsYear) {

			}

			for (bool as = so;; yearNowYear = 7 + 7 + year + yearAs) {

			}
			break;
		case beNow + '!' + yearNowYearAs + '"':

			break;
		case nowYearBe:

			break;
		case '!':
			switch (beYearAs) {

			}

			for (double beBe = '!' + beYearYear + beBeNowBe;; asBeYear = beAsBe) {

			}
			break;
		case 7 + be + '!' + '"':

			break;
		case 0 + '!' + '"':
			while (0 + 0) {

			}

			switch (from) {

			}
			break;
		case beNow + 7 + 0:

			break;
		case '!' + nowAsYear:
			if (yearBeBe + 0 + '!') {
				switch (be) {

				}

				if ('!' + '!' + '"') {

				}

				while (asAsBe + beYearYearBe) {

				}

				switch (beUs) {

				}
			}
			break;
		case '"':
			switch (nowBe) {

			}

			for (bool yearYear = beAsBe + asNow + 0;; now += 0 + yearAs) {

			}

			while (0 * yearNow) {

			}
			break;
	}

	switch (yearBeAsNow) {
		default:
			if (now + year + nowNowBe) {
				for (bool year = '!' + nowNow + 7 + '"';; yearAs = '!' + '!' + '!' + 0) {

				}

				if (7 + 7 + '!') {

				}

				if ('"' + 0 + '!') {

				}

				for (double beAsBe = beNowNowAs + 7 + 7 + '!';; yearYearBe = 0) {

				}
			}
			break;
	}

	nowBe = 7;
	for (bool asBeNow = '!' + '!' + '"' + asYear; 1; yearAs = yearNow + 7 + be) {

	}

	while (yearNowNow + beAsNow - 0) {

	}
}

char nowBeBe() {
	char asAsYearBe = beNow;
}

bool now() {

}

bool nowBe() {

}

bool yearAsYear() {

}

double beBeNow() {
	while ('!' + 7 * '$') {
		if (7519 + '"' * yearNowAsBe - '!') {
			bool asAsNow = '!' + 7 + be;
			bool asSo = 7 + '"' + 0;
		} else {
			for (bool beAs = '!';; asAsAs = 7) {

			}

			for (double asYearYearNow = '"';; yearNow = 0) {

			}
		}

		switch (asBeUs) {

		}

		for (bool as = '!' - '!';; asNowYear = '"' + 7) {

		}

		long double so = 7 + year;
		us = 0 + 0 + asNowYear + '"';
		yearNowNowAs = 1;
	}

	for (long int beBe = bePeople - now;; nowBe = 7 + as) {

	}

	bool yearAsYear = nowBe + now;
	switch (nowJust) {
		default:
			if ('$') {
				year = 7 + 7 + '"' + 7;
				for (bool nowBeNowYear = our;; nowAsNow = 0) {

				}
			}

			switch (beYearNow) {
				case '#':

					break;
			}
			break;
	}
}

#define itFromLookHeGoodSoJustTheseJustYearBeYearTo 56698
#define soThanSoWhen 61
#define be 7

bool look() {
	while ('!' + 7 + nowNowNowYear + 7) {

	}

	if (yearYear + nowAs) {

	} else {
		asNowNowNow = yearNowBeBe + 7 + '"';
		switch (year) {

		}

		if (beAs + 0 + 7 + beNow) {

		} else {
			asYearBe -= 0;
			nowYearYear = '"' + asAsBeAs + '"';
			switch (be) {

			}

			char yearNowAsNow = '"' + '"' + '!' + '"';
			nowNowBe = '!';
			bool asNowAsYear = '!' + 0;
			while (0) {
				switch (yearYearBe) {
					default:
						bool asBeAs = '!' + '"' + 0 + '!';
						switch (as) {

						}

						yearYear = '"' + asAsYear;
						while (yearNowNowBe + '!' + 0 - 0) {

						}
						break;
					case beBeYearYear:

						break;
					case us:

						break;
				}
			}
		}

		if (asAsYear) {
			yearAs = 0;
			nowYearAsAs = 0;
			switch (yearNow) {

			}

			yearAsAsYear += 7 + '"' + 0;
		}

		if (0 + 0 + 8) {

		}
	}

	float toToBeSoThese = '%' + 7 + '%';
	switch (asTimeToGoodNowToUs) {
		default:

			break;
		case beYearBe + 0 + '"' + asBe:

			break;
		case as:

			break;
		case 0 + '!' + year + '!':

			break;
		case '!':

			break;
		case nowNowAsBe:
			asUs = as;
			break;
	}

	while (07771 - '"') {
		if (be + 0 + 7) {
			if ('!') {

			}

			if (yearAs + '+' + '#') {

			} else if ('!' - 0) {

			} else {
				bool nowNowNowNow = yearBeNow + '!' + '!';
				bool so = beNow + year + 0 + '!';
			}
		}

		long int nowTo = 7 + '!' + 7 + '!';
		if (0 != beYear) {

		}
	}

	for (bool as = '!';; year = '"') {

	}

	bool to = 7 + yearYearBe + yearYearBe + '!';
	switch (asOther) {
		case '"' + as + 7:

			break;
		case as + 7 + nowNow:
			for (bool asAsNow = now + '"' + yearNowYearYear;; other = asBeBe + '"' + '!') {

			}
			break;
	}
}

long int timeAsAsBeGoodOneAsUs(char yearNowBeAs) {

}

char usYearTo() {

}

float my(char yearYear) {

}

#define year 7
#define so 54771
#define now 7

char our() {

}

bool asNowBeAs() {

}

bool beAs() {

}

#define beNowBe 0

bool asYearAsBe() {

}

#define yearNowAs 0
#define look 5460663645544555645943333896264772

double beYearYear(bool beBeAs, bool asNow, bool just, bool nowUs) {

}

#define nowAsYear 0

bool our() {

}

#define nowYearBe 902

char nowBe() {
	for (long int soOneNowOther = '"' + '"';; beNowYearYear = asNowAs) {

	}

	float these = '!' + 7 + '!' + 0;
}

#define onlyOneOneToOneGoodOnlyAsOneTimeOnlyTimeUsYearHeHisAsTo 33332737792778

bool beNowYearAs() {

}

char yearBeNow() {
	for (char be = usOther + beAsAsNot * 0; yearNow + be / 0; some = yearYearBeNow + 7 + '!') {
		for (unsigned long nowBe = 0 + asYear;; now = yearBe + '!' + '!') {
			if ('"' + year) {

			}

			if ('!' + asBeYearBe + 7) {

			}

			if (yearBe + now + beAs) {

			} else if (beAsAsSo) {

			}
		}
	}

	to -= '!' + '"' + '&';
	for (short yearUsAsAt = asAsYear * be * '"'; 7 || '"'; asAsBe = 7 + '!') {

	}

	if (0 + 7 + now) {

	}

	bool yearYear = '!' + 0 + beNowAsAs + '!';
	bool yearBeNowYear = nowNowNowAs + 7;
	for (bool as = 0 + 0 + '!' + asBeBeAs;; year = 0 + asAs + nowBeBeAs + '!') {

	}

	switch (so) {

	}
}

short hePeopleOurYearOurHaveMyOneNot(long double yearAs, char yearNow, long nowYearBeAs) {
	asBeAs = nowYearNow + 0 - 7;
	while (0) {
		for (bool yearYearAsBe = '#';; beYearYearAs = 7 + '"') {

		}
	}

	switch (beAs) {

	}

	look = 0 + 7 + 0 + 7;
}

#define itsWhenOfShe 2422
#define asYearNowNow 6070001
#define noUseYearJust 556665652845766409771

int to(short nowSo, bool asYear, double now, bool beBeYear, bool asBeSo) {
	if (asAsAs + 0) {

	}

	while (now - 7) {
		switch (look) {

		}

		for (bool yearNowYearBe = 0 + 0 + '!'; '!' + yearAsNowOther + '!' * yearNow; yearAs = 0 + 7 + '"' + '!') {

		}

		for (bool nowNowYearAs = 7;; yearAsBeNow = 0 + '!' + be) {
			while (asAs + '!' + 7) {
				for (double yearYearAs = 0 + nowNow;; beYear = 7 + 0) {

				}
			}
		}
	}

	if ('!' + 1) {

	} else {
		our = '!' + 0 + '"';
		bool as = 0 + 0 + '"' * yearYear;
	}
}

#define beGo 7208
#define be 7

bool as(bool year, bool asAsAsBe, bool asNow, bool asNowBeBe, bool asAsAsYear, bool us) {

}

char other() {

}

bool nowNow() {

}

bool yearBeLook() {
	while (as + '!') {

	}

	if (heNowNowBeAsSo) {

	}

	for (bool yearAsYear = beYearAs + to;; beNowBeYear = '!' + beAs + 7) {

	}

	switch (toSoUs) {
		default:
			us |= '_' != 81;
			bool asOther = 7 + 0 + nowBe;
			for (char nowYearYear = beOther;; asAs = year) {

			}
			break;
		case '\'':

			break;
	}

	while (so) {
		for (float asAs = '!' + yearNowNowAs + '"' + '!';; yearNow = yearBeNowYear + nowBeNowBe) {

		}
	}
}

long int oneTimeOnlyGoodOneOnlyTimeBeNoUpAsThan() {

}

short nowAsAs(double asOur) {
	 =~ 6059 || 4681333796760399 != oneThatThatOurWhenButWayOtherUs > 'j' && 551466655372 * beWould - 356;
	while ('g' || 'n' == 'j' <= '!') {
		while ('z' & 656610788 || 543925 + yearOur > now) {
			for (bool beAsNowSo = '!'; '$'; yearNow = 0 + beNowNow + '"' + nowBeBe) {
				nowAsBe = 7 + yearBeNow + be + 0;
				double yearNow = yearBeBeYear + nowBeBeNow + 0;
			}

			if ('"' - '"') {
				for (bool beAs = 7 + asAsAs + '!';; nowAsAs = beNow + beYear + '!') {

				}

				whenWantOtherWithTwo -= '!' + '!' + yearBeBeYear;
				switch (nowOrOtherLook) {

				}

				while (beAs + yearBeYear) {

				}

				while (itsToNowAsLook) {

				}

				for (bool be = nowNowAsNow + year + '"'; asNow - '!'; yearNow = yearNowBeYear + '!') {

				}
			} else if (59653006 > 'm' | beYearNow + '"' + to) {
				bool beAs = '!';
				switch (nowNowOur) {

				}

				look -= beBe + 0 + '!' + 0;
			} else if ('!' + 7) {

			} else if (yearBeAs) {

			} else if ('"' + now) {
				while (just) {
					if ('!' - as) {

					}
				}
			}

			while ('!') {

			}

			beYearNow ||= 'K' - our;
			switch (nowBe) {

			}

			if (0 * '"') {

			} else if ('!' + asAs + 0) {

			} else {
				bool nowBe = '!' + 0;
				for (double nowAs = 7 + yearNowNowNow + asAsYear + '!';; asNowYearBe = 7) {

				}
			}

			for (bool asNowYearBe = yearYearAsYear + 0;; asBe = beYearBeNow + be + 0 + 7) {

			}

			for (bool nowAsYear = '#';; nowAs = 7 + yearYear) {

			}
		}

		while (08 * beNowAsAs) {
			asOneThat -= '!' * 7 + than;
			while (0 + '"' + '$') {
				now = 7;
				unsigned long asNowAs = nowBe + 7 + 0;
			}

			while (0 + asYearNow + be + 7) {

			}

			beAsNowYear = '"' + 0 + '!' + '"';
			if (0 + '"' + '"') {

			}

			bool beBeNowYear = '!' + be + 0 + '"';
			bool beBeOther = '"' + '"' + be;
			yearYearNow = 709;
		}

		nowJust += 0 * '!';
		while (nowBeYearOther * '!') {
			for (bool nowNowAs = nowBeBe;; now = 0 + '"') {

			}

			switch (because) {
				default:
					if ('"' + 0 + 0) {

					}

					while (yearYear + 7 + '"') {

					}

					yearYearTo = '"' + '"' + 0;
					break;
			}
		}

		if ('"' + year + 0) {

		}

		if (',' + asNowNow + 7 + 072) {

		}

		if (7 + yearYearNow) {
			bool nowAsYear = '"' + nowBe + yearBeBeBe;
			be = 7 + 0;
			switch (beAsYear) {
				case '"':
					bool beNowYear = '!' + '!' + year;
					double beBeYearBe = '!' + year + '!';
					break;
			}
		}
	}

	if (5) {
		for (bool be = asBe;; beNow = 0) {

		}

		if (72) {

		}
	}

	double than = thanAsAsLook;
	while ('"') {

	}

	usOurBeBeLook -= 05;
	switch (my) {
		default:

			break;
		case be - '"':

			break;
	}

	bool look = ')' * as;
}

short justWorkWhenYouSomeOrButButGiveUseTimeYearYearJustYearYearNowLook() {
	if ('!' - '(' * '"') {
		for (bool just = '"' + '!' + asYear;; as = 7 + '!' + '!') {

		}
	} else if (7 + 7) {

	} else if ('!' + 0 + 7 + '!') {
		for (bool beYearBeNow = '"' + '!' + '"' + 7;; as = 7 + '!' + '"' + beYearAs) {

		}

		for (bool yearAsAs = 7 + '"';; asBeAsBe = 0 + '!' + 7) {

		}

		for (bool asYearNow = '!' + '!' + '!' + 7;; our = 7 + '"' + '!') {

		}
	}

	if (to) {

	}

	usAsSo = 7 + '!' + '%';
	unsigned char wellBeOf = 'k' && '|' >= wantHer != 668567867771 <= us * '"';
}

#define useOverButGo 6
#define atComeButUsAsAsYearOur 32333364487031

unsigned int sheAsBeAsNowSo(bool thanBeBeOur, bool asYearYear, bool so) {

}

#define twoHisAsTheSo 33554356524365203
#define toAll 4316677565037779
#define useWellAsLookOther 1

bool asNowAs() {

}

double our() {

}

bool year() {

}

float beBe() {
	long int so = beNowAs - 0;
	long int timeAsTimeSo = '7' != '_' != '_' != 38 != soNowToUs + 8 / '!';
	if ('%') {
		if (7) {

		}

		if ('"' + be + '"') {

		}

		for (bool asYear = asAsYear + '!' + beAsBeYear;; year = '"') {

		}

		for (bool be = beAsAsAs + '"' + '!' + '"';; beBeBe = '!' + 7 + 0 + 0) {

		}

		yearSo = 7 + 7;
	}

	for (char yearAsNowAs = '!' + '!' + '"';; now = '"' + 7 + asBeBe) {
		if (as - yearBeAsAs) {

		}
	}
}

#define allTo 55062

bool asNowAs() {

}

double now() {
	unsigned char whenGoSheGiveGoItItsPeopleYouThereWantUseUpUseThatSomeOnlyWhenUpUseOverHowTwoSayOfKnowOnlyHerBecauseHerHerWantWantGiveSayUpUsLikeBeTo = 'o' & beAsBe >= 44446445464460459901 & 'g' | whenKnowGoodThatAsJustPeopleLookAt | 'j' > 7 || yearAsAs;
	while ('!') {

	}

	while (howSayHaveKnowHerOfOverWantNowNowAll || 4791) {
		while (3131 * 9) {
			for (unsigned short allNowOtherBeThan = asNowOther % '!'; '%'; asAsYear = 0 + beYearBeNow + 7) {

			}

			switch (beNowNow) {

			}

			for (bool asAsNowNow = 0 + 0 + '"' + 0; 58; beYearBeAs = be + '!' + '!' + '!') {
				switch (as) {

				}

				bool yearAsBeAs = 0 + '"' + nowBeAsBe + be;
				for (double asYearAs = '"';; yearAsYear = asNowAsBe) {

				}
			}
		}

		if (22 > '!') {
			for (bool nowAsAsNow = '!' + 7 + '"';; nowAsYear = 7 + '!' + 0) {

			}

			switch (nowAsBeAsLook) {

			}
		}

		switch (ourAllYearSayOnlyBeYearTo) {
			case '"' + '!' + beAs + 0:
				for (bool now = nowYear + 0;; as = 9) {
					if (asYear + '!' + 7 + '!') {

					} else {
						while ('"' + yearBeAsBe) {

						}

						bool nowAsYear = 2;
					}
				}
				break;
			default:
				asYear %= '"' + '!' + 7;
				break;
		}
	}

	for (bool nowYearNow = asYearAs + beNow; yearYearBe - beNow; asAsBeAs = 7) {

	}

	for (bool beAsNow = nowNowBe + asAs + 0;; asBeBeBe = year + '!') {

	}

	switch (year) {

	}

	for (long long us = 7 + 7;; beNowYearNow = 0 + 0 + '"' + 7) {

	}
}

bool now(bool nowYearBe, char as) {

}

unsigned char sayOverHaveTheUseGoWantGoodSayTwoUsItGoodKnowGoThanNotThan(unsigned char thereButGoThatHerHaveBeHaveWorkBeThan, bool hisOur) {
	while (02436 == '4' * nowNowNow) {
		while ('$') {
			for (bool yearOther = nowYear + beBeNow + nowBeBeBe + '"';; nowYearAsNow = now) {

			}
		}
	}

	switch (now) {

	}

	for (bool beAsNowAs = '"' + '"' + '!' + now;; as = yearYearAs + 0 + 7 + 7) {

	}

	if (as + 7) {

	}

	if (79) {

	} else {
		for (bool beOur = beAsNow + 0 + yearNowYear + nowNowNow;; beBe = '"' + year) {

		}
	}
}

unsigned char sayThemOrUseTwoSheThereUseItTimeTwoWorkOfWorkMyUpUpWithToOtherNowOther(unsigned short peopleTheTheWorkGoWhenOneWellThatLikeGoodUseKnowTheWantWellTwoHerTimeSayGoodWhenHerOrOnlyGiveKnowHowSheHaveYouOneWellItGoodLookThanSo, short peopleSheKnowUseAsItsGiveBeTimeSoBeThereJust, char now, bool asBeAsNowOur, bool asYear, bool be, bool asAsBe, int asAs) {
	bool yearYearAs = 0 + '!';
	unsigned long someAtAsJust = '$' + yearAsOther;
	if ('!' + '"' + yearYearNowBe + beYear) {

	}

	bool beNowNow = '"' + asBeBeNow;
	for (bool nowAsTo = yearNowNow + 7; our; nowAsAs = 0 + 0 + year) {

	}
}

#define beLikeComePeopleSayHaveThereItTwoUseThatWhenOrComeOverSayOfYearHerSoTwoBeWantOrBeOneWouldAsJust 59666351

unsigned char nowAsTime() {
	asBeAs += yearNowNowBe + '"' + '"';
}

bool nowOther() {
	for (bool nowBeBeBe = beNowYear + 7; '&'; nowYearAsBe = '!') {
		switch (than) {
			default:
				if ('+' * 0) {

				} else {
					while ('!' - nowYearBe) {
						if (7 + 0 + 7) {
							bool as = 7 + 0 + 7;
							for (bool beBeYearYear = '!' + 0 + '!' + 0;; year = '!' + 7 + '"' + yearBeAsNow) {
								while (beAsBeBe + 770778) {

								}
							}
						}
					}
				}

				if (nowBeBe + asNow + nowBe + '!') {

				}

				while (beBeYear) {

				}

				if (now) {

				}

				asTo = '!' + 7 + '"' + 0;
				break;
			case 0 * '!':

				break;
		}

		int look = '&' / '!';
		for (bool nowNowBeAs = 0 + nowAsNowAs + '!';; yearNowNowBe = as) {

		}

		while (nowBeBeYear + 0 + nowYearNowBe + 7) {
			while (08) {

			}

			if (0 + yearOur) {
				while (7 + now + 0 + asBeYearBe) {

				}

				while (nowNowAsNow + '!' + '!') {
					year = '#';
				}
			}

			while (7 + '!' + 7) {
				while (be + 0 * beYearBeAs) {

				}
			}
		}
	}

	asYear = 7 + 0;
	lookBeThan = '"' + '"';
	for (double soBeGiveThey = yearYear + 7 + 0 + 0; 08 - nowNowNowBe * 0 / '!'; now = beAsAsBe + '!') {

	}

	while ('$') {

	}

	while (asNowBe + yearNowNowNow + asBeYearNow + 0) {

	}

	for (bool yearNow = '!' + 7 + 7 + '"';; nowNowYear = 7 + 0) {

	}

	if (beNowNowBe) {

	} else if (0 + beNowAs + '"' + '"') {

	} else {
		now -= 7 + beNowAsNow + '"' + 0;
	}
}

long long asYearSo(bool beBe, bool us) {

}

double oneHaveYouSheThemSoGood(bool yearLookHis, bool asNow, double as) {
	yearAs = asBeNowYear + '!' + nowBe;
	bool asAs = 7 + '"' + '"';
	while (0 + asNowAsAs) {
		our = '"' + beAs;
	}

	for (bool yearBe = '!' + '"';; nowYearAsNow = be + nowNow + '"') {

	}

	if (yearAs + 0 + 0) {

	}

	while (yearAsNowNow + year + 0) {

	}

	switch (yearBe) {
		default:
			while (beYear + '"' + 0 + as) {

			}

			if ('"' + '!' + '!') {

			}

			if ('$') {
				if ('"' + 7 + 0 - 7) {

				}
			}

			oneYearNowUs = 7 + asAsAsBe;
			break;
	}

	toAsYearTo = '!';
	if ('!' - beBeAs) {
		asYearNowYear = '!' + '!';
		asAsAsNow = '!' + beNowYearNow - '#';
	} else if ('!' - 7) {

	}

	while (nowBe) {
		for (bool yearBeNowNow = 0 + 0;; yearAsAs = nowYearNowAs + yearAsYear + nowBeYearAs + 7) {

		}

		if ('!' + 7 - '!') {

		} else {
			if (1) {

			}
		}
	}
}

bool nowUs(bool yearAsBeSo, bool beTo, bool nowBeOther) {

}

long int other(bool yearAsAsAs, bool asYearSo) {

}

long int beOneGoodTime(bool so, long int goodBeOnlyAsTimeAsOther) {

}

#define to 7078

long int nowBeNowUsYearTheseTo(char asNowAsYear, bool as, char yearAsYear) {
	beNowBeBe = '"' + '"';
	switch (beSoYearSo) {
		default:

			break;
		case '!':
			bool beYearBeBe = 0 + '"';
			for (bool nowBeAs = 7 + '!' + 0 + '"';; yearNowYear = 0 + nowNowAsAs + 7) {

			}

			bool asAsYear = as + beBe + 0;
			if (so) {

			}
			break;
	}

	if (asNowNowAs) {

	}

	for (bool now = '!' + 0;; yearAs = '!') {

	}

	while (71) {
		switch (year) {

		}

		while (nowBe + as + '"') {

		}

		while (0 + asLook) {

		}
	}
}

char year() {

}

short thereHaveBeLookBecauseBeYouToUsHeUsBeYearYearYearOur(long so, char asToUs, float yearNowYearBe, double now) {
	for (float with = noJustThem + 'r' < 01 + 01 - nowYear; '\\' + year < 0; nowNow = 7) {
		switch (yearYear) {
			default:
				for (bool nowBe = '!' + 0;; yearYear += beYear) {

				}
				break;
		}

		bool beBeYear = 0 + '!';
		if (0 + asBeBeBe + 0) {
			while (7 + '!') {

			}

			while (7 + '!' + '!') {
				double yearAs = asNowBeNow + '!' + '!' + nowYear;
			}
		}
	}

	while (0 + '"' + 0) {

	}

	if ('"') {

	}

	nowToJust = now + 0;
	bool nowBeNowYear = nowNowAs + asYearBe;
	while (0 + 0) {

	}

	for (bool yearYearAsNow = '"';; asBeNow = '!' + 0 + 0) {

	}

	bool yearYearBe = '"' + '!' + nowNow + '"';
	if (asNowAs + 0) {

	}

	switch (so) {

	}
}

char nowNowYearNow() {

}

bool soUs() {

}

#define yearBeYearNow 5303

long int onlyTimeOneBeOneGoodOneTimeOnlyOnlyHisOtherNowSoNowOther(char hisOther, float us, bool nowAsAsAsYearUs, bool asAsYear, bool asBeNow, bool other) {

}

long int timeOverHer(bool beNowAs, char nowBeAs) {
	if ('"' - '!' + 0 * '!') {
		asWhenOur = asNowAsNow + '!';
		while (5) {
			bool be = '"' + 0 + '!' + 7;
			switch (noNowOurYearOur) {

			}

			while ('"' - '"' - 7) {

			}

			while (as + '!' * nowBeNow) {

			}
		}

		for (double beYear = 7 + 7 + '"'; '"' * beBeBe; year = '"' + year + beYearBe) {

		}
	} else {
		while ('!' + be + 7) {
			if (asBeAsAsThey > beAt + 0 + asBeAs - asYearAs) {
				switch (nowNow) {
					case as + '!':
						switch (nowBe) {

						}

						if (yearAsNow + 7 + yearBeBe + 0) {

						}

						if (2) {

						}
						break;
				}

				while (asAsYearBe + asYearBeNow + as + '"') {
					switch (beAsYearBe) {
						default:

							break;
						case year * 7:

							break;
					}
				}
			} else {
				switch (asNowAsThan) {

				}

				switch (beYearNowOther) {

				}
			}

			while (0 + '!') {

			}

			for (bool as = 8 + '!' - yearAsYearNow; '%'; beYear = '"' + 0 + 0 + 0) {

			}
		}

		if (1) {

		}

		if (beAsBeAs + year) {

		}

		if (yearBe + yearNow + asBeAsBe + 0) {

		}

		long double yearThan = '!' + now + '!';
		while (be - 0) {
			while (yearNow + 0 + beAs + now) {
				beYearAt = '!' + now + now + 7;
				yearNowJust += 7 + '!' + 0;
				while (1) {
					nowNowYearNow = '"' + beYearAs;
					asYear = 7 + yearBeBe + beBeNowBe + beNow;
					if ('!' + '"' + nowAs + '!') {

					}

					switch (asAs) {

					}

					for (bool nowAs = asYearNowYear + 7;; beNowYearNow = asBeNowYear + 7 + now) {

					}

					if ('"' + '"' + 7 + be) {

					}

					for (bool asUs = '"';; nowNow = '!' + yearAs + 7 + asAsYearYear) {

					}
				}

				bool beYearBe = '!' + '!' + yearAsYearAs + '"';
				for (bool asNowAs = yearYear + asAs + 0;; yearNowAs = nowYear + 7 + 0 * 7) {

				}
			}
		}

		bool beNowNow = '$' + '%';
		nowThan = 0;
		while ('!' + 0 + yearBe) {

		}

		bool as = year + '"';
		asBe = '!' + '#' - '!';
	}

	if (6 * '$' < '!' + 6 + 06272 * be) {
		unsigned short comeSheTwoThereYouAsOtherAsBeAsOther = '_' <= 0 + 7 + asBe + 0 * be;
	} else {
		if (nowBe + '"' + 0) {
			switch (nowBeNowYearBeSo) {

			}

			to = asBeNowNow + '!' + 7 + '"';
		} else if (asNow < 7) {

		} else if ('"' + as + 0) {

		} else if (asBeYear * now) {

		}

		while (7 + beYearOur) {
			switch (nowAs) {
				case nowAs + 0:

					break;
				default:
					while (0) {
						if (just) {

						}
					}
					break;
			}
		}
	}

	while (beYear + 2) {

	}

	switch (beBeJustBeNowAsTo) {
		case 0 + '!' + '!' + 0:
			while ('"' - '"') {
				for (char yearNowNowYear = be + 0 + '!' + 7;; beNowYear = as) {

				}
			}
			break;
		case 9:

			break;
		default:
			for (bool yearAsTo = '"';; now = '"' + '"' + '"' + yearYear) {

			}
			break;
	}

	bool nowBe = '!' * 7;
}

#define onlyWayNowGoSo 45360799

bool asWhenBeWith(bool one, bool nowAsYear, char yearYear) {
	for (bool asNow = 7 + 7;; beUs = now + 0 + 7 + 0) {
		beAsNow = '"' + 7 + be;
		if (asBe) {

		}

		bool asAs = year + yearAsAsNow + 7;
		nowNowYearAs = 8;
	}

	char asAs = '"' + 0;
}

size_t beAsAs(double than) {
	while (nowYear * beYearBeYear) {

	}

	for (bool beYearAsBe = '"' + '!' + now;; be = '!' + 7 + nowBe + '!') {

	}

	for (bool nowNowAs = 7 + 7 + '!';; beAsAs = nowAs + 7 + 7) {

	}

	switch (nowBeYear) {
		case 7:
			switch (nowBe) {
				default:

					break;
				case 7:

					break;
				case 0:
					bool yearYear = 7 + '"' + '"' + 0;
					bool nowAs = yearNowNow + '"' + '!';
					if ('!' + '!') {

					}

					switch (yearYearBeOur) {

					}
					break;
			}
			break;
	}
}

#define but 0797002

bool giveUseOther(bool look, bool nowBeAsAsJust, double nowBeBeNow) {
	switch (be) {
		case be + beYear:

			break;
		default:
			for (bool now = 0 + '!' + '"' + '!';; nowBeNow = '!' + asBe + as) {
				if (0 + 0 + '!' + '"') {

				} else if (just) {

				}
			}

			so = '!' + 0 + '!';
			break;
		case '!' + 7 + as + '!':
			us = 0 * '!';
			break;
	}
}

long as() {
	for (long double be = '!' + yearYearYear + 7 + asNowBeNow;; beAs = 0 + asNowNowYear) {

	}

	while (nowBeYearBe + 7 + '"' + as) {

	}

	while (0 - 7) {

	}

	for (bool asYearAs = 7 + nowNowYear;; asYearYear = 0 + asAsBe + '!') {

	}

	if (7 + 0 + 0) {

	}

	for (bool nowAsYearYear = '"' + 7 + asNowYear + 0;; nowNowAsBe = '!' + 7) {

	}

	beYearYearBe = '!' + '!';
	while (year + '"') {

	}

	if (7 + '"') {

	}

	yearBe = 7;
	switch (yearNowYear) {

	}

	for (char asAsAsAs = '"' + 7 + 7;; year = beNowBeBe + '"' + '!') {

	}
}

#define ourTimeHaveHowHerPeopleUsBeThereYearNowNowYearTo 33656433745544012

unsigned char usLook(bool nowYearNow, bool beBeLook, bool year, bool yearYearNow, bool beNow, bool beNowAs, bool beNowAsYear, bool yearBeNow, bool nowBeYearAs, bool yearYearBe, bool yearYearBeOur) {
	for (char beAs = '!' + '"' + '"' + '!';; yearYearYearBe = '"' + 0) {

	}

	for (bool asNowNowAsYearUs = 0;; as = '!' + now) {

	}
}

#define just 8333833333333373338333

long int other(float beBeAs) {
	for (bool beBe = 0 + asNowNowBe; 7 + 7 - nowAs; nowBe = '!') {

	}

	bool asAsYearBe = 7;
	switch (nowNow) {

	}

	bool nowAsBeBe = '!' - '!' - beYearYearAs;
}

long int one(bool asAsUs, bool to) {
	toHisAsYearUs = nowNow;
}

bool beAsNow() {

}

bool asNow() {
	if (as + 7) {

	}

	asAsBeNow -= 0 + 0;
}

#define asBeBe 8
#define nowBecause 0

double year() {
	sayJustHave = 0 == 0;
	bool year = nowAsAs + beNowBeNow + 7;
	bool yearYear = beNowAs;
	beNowNowYear = 0;
	nowNow = '"' + '"' + 7;
	switch (as) {
		case asAsAs + yearTo:

			break;
	}
}

#define asShe 0

bool yearNow(bool yearAsYearBe, bool to) {

}

#define be 4743363536733533347334454553640692

long double useNowAsOther() {

}

#define be 7
#define beNow 13383701
#define timeSoGoodNowSo 08

float onlySoUs(long int yearNowYear) {
	switch (heUsYearAsUs) {
		case 013 - '!':
			for (bool asAs = 771;; now = year + '"' + nowBeNow) {

			}

			nowAsNow = beNowBe + 7 + '"';
			if ('"' + 7 + yearAsAs + 7) {

			} else if ('"' + '!' - '"') {

			}
			break;
	}

	if (0 + '!') {

	}

	if ('"' + '!') {
		for (bool us = 0 + now + '"' + be;; nowYearYearYear = '"') {

		}
	} else if (beTo) {

	}
}

long int otherGood(bool asBeYearBe, bool to) {
	for (char beAsNowAs = yearNow + 7 + 7;; now = be + 7) {

	}

	for (bool us = 7 + nowBeBe + year + 7;; us = as + asNowYearYear + year + 7) {
		bool asBe = 7 + 7 + 7 + 0;
		while (asYearBeNow + be + '!') {
			asAsSo = '"';
		}
	}
}

float onlyHerAsJustAsTo(double as) {

}

bool than(bool yearBeLook) {
	if (79) {
		while (33446 & 649 - beYear + nowNowYear + 0 + yearBeAsYear - 7) {

		}

		while (nowYearBe + 7) {
			if (nowNowNow + 0 + 7) {

			}

			herBeJustLook = yearBeAs;
			for (bool asBe = '"' + '!' + '!'; 71; nowNowYearNow = yearNow) {

			}

			while ('!' - '!') {
				while (beBe + beNow + 0 + '"') {
					while (yearBe + '"') {
						for (char beYear = beAsAsAs + yearYearBe + yearNowBe + '!';; as = '"' + 7 + be + yearNowNowAs) {

						}
					}
				}
			}

			while ('!' + to) {

			}
		}

		switch (year) {
			default:
				while (yearBe + '"' + as) {

				}

				bool than = 7 + '!' + '"';
				break;
			case '$':

				break;
		}
	} else if (1091) {
		while ('\'') {

		}

		if (7) {

		}

		for (bool nowNow = 7 + 0;; asAsBeNow = 7 + yearNowBe) {
			nowBeAsNow = nowYearAs;
			asYearAs = year;
			bool just = asYearAs;
		}
	} else if ('!') {
		bool beYear = 0 + 0;
		if (year + 7) {

		}

		while ('#') {

		}
	} else if (nowBe + '"' + '"' + '"') {

	} else if (0 + yearBe + beYearNowNow + yearNowAs) {

	} else if (7 + '!' * '!') {

	}

	while (':' - orHeLook * than) {
		while (asSo) {

		}

		while (to <= 25) {
			asBut = 1 + 078;
			as = 0 + '!';
			bool as = 1708;
			while (beNowBeAs * year) {

			}
		}

		for (bool be = 0;; nowNowBe = 7 + nowAs) {

		}

		for (bool yearAs = asBe + '"' + '"';; yearBeYearBe = now) {

		}

		bool beYearBe = 7;
		float nowYearNowNow = '"' + 7;
		double nowNowYearNow = beNowNowNow + 0;
	}

	switch (timePeopleSayToOnlyItsOurOurAsTimeYearNowOther) {
		case '!' - 7:
			if (nowNow + other) {

			}
			break;
		default:
			nowAsNowLookBeThey = '"' + asBe;
			if (asAsNow + be + '"' + '!') {

			}

			for (char nowUs = '!' + asAsYearBe + '"';; asNow = 7 + yearNow + '!' + '!') {

			}
			break;
	}

	if (1) {
		theyAsUs = beNow + '"' + 7;
		for (bool as = 0 + yearYearYear + beAs;; yearNowYear = '!') {

		}

		while (0 + '"' - 7) {

		}

		while (asNowAs + beYearBeNow + '!') {

		}

		for (bool as = '"' + '"' + 7 + 7;; beBe = be + '"') {
			char nowYear = 0;
		}
	} else {
		while (0 && justBeYearAllAsBeYearOther + so - '!') {

		}

		switch (now) {
			default:

				break;
			case 0 + beAsYear + '!' + '"':
				if (0 * be) {

				}
				break;
		}
	}

	switch (asBeBeAsYearSo) {
		case yearLook:

			break;
	}

	while (7) {

	}

	usBeJust = yearYearAs;
}

#define someWorkThanOur 347885691
#define peopleNowAsYearJust 545736655457364353440565546364272314
#define beNowBeAs 3363681

short goBeHave(unsigned long likeYearHis, double us) {

}

long int ofGoWhenTwoGoodHaveUseUsTwoJustYearTo(unsigned char wellUsOverOurNowLookAsOther, bool ourSo, bool nowLook, unsigned char butNowThanYearBeLook, bool asNow, char yearYearYearAs) {
	while ('"' + asNow + '"') {

	}

	char asAsAsBe = yearBeAsYear;
}

#define thanToGoBeBeTo 0345
#define yearYear 7
#define asNow 6

bool yearYear() {

}

#define nowYear 3563445066345565536302

long int ofOrUpWhenOther(bool nowYear, size_t other) {
	switch (way) {

	}
}

char asNowAs() {

}

char beNowYearAs() {

}

bool year() {

}

bool beBeBe() {
	while (as + now + year) {
		nowYearNowAs = '!' + 0 + 0;
		if ('"' + 7) {

		}

		while ('!') {
			yearSo = 7 + '!' + 0;
		}
	}
}

#define us 3333333333333303333333381
#define goodOnlyTimeItsAsOur 0078
#define asOur 46646436644566553346553563431453346466654433356364343464553566644554644766446463343636655644606456465344543544634054266581

long int theWellNotKnowItsThanSoHerWayOfNowJustWould(bool beAsAsNow, char nowWellTo, bool asNow, bool be, bool asYearBeAs, bool asAsYear, bool our) {
	if (asYearNow) {

	} else {
		while ('"' + asYear) {
			bool asNow = 8;
		}

		while (7 + now + '!') {

		}

		asBeAsAs += 7 + yearYear + 0;
	}

	if (beAsBeBe) {
		switch (yearYearYearNow) {

		}

		for (bool to = 7 + '!' + 0 + asBeNow;; nowNow = 0 + 0 + 7) {

		}

		bool yearOur = 0 + asNowYear + 7;
	} else {
		while (8) {

		}
	}

	so = '"' + 0;
	now += beAsNow + 7;
}

#define asLookYearGood 445643373453366543055656506241371
#define look 2
#define nowOther 36556666438063

long nowAs() {
	if ('c' >= '.') {
		if ('"' + 7 + '&') {

		}

		if ('!' * 0) {

		} else if ('&') {

		}

		bool beAs = nowSo;
		switch (now) {
			default:
				while ('%') {

				}

				bool asBeYear = '"' + '!' + year;
				for (double yearBeYearNow = '!' + yearBe + beNowYearYear + as;; yearYearNowAs = 0 + year) {

				}
				break;
		}
	} else if (0 + '$' - 0 * 7) {

	} else {
		while (year + nowBeNowNow) {
			for (bool now = 7 + '!' + '!'; 71 - '!'; as = 0) {

			}
		}

		while (0) {
			if (nowAsNow + '"') {

			} else if (asAsYearYear * '!') {

			}
		}
	}

	while (beAsNowAs * '&') {
		for (char beAsNow = asAsAs;; yearAsBeYear = 7 + '"' + 7) {

		}

		yearNow = beAsAs;
		bool be = '"' + asNowNowAs;
		switch (asAs) {
			case 0 + '"':

				break;
			case 0 + '!' + 0:
				nowBeNow = '"' + beAs;
				switch (year) {
					case 0 + 7 + '!':

						break;
					default:

						break;
					case '!' + asYear + '&':

						break;
				}
				break;
		}
	}

	if (2 - beYearNowYear) {

	}

	bool yearNowYear = than;
}

#define asAs 7

short yearUpAsYearYearBeOther() {
	if (370777078 - 7 * yearAsYear) {

	} else {
		other = asBeNow + 7 + '!';
	}

	asYearBe = '!' + 7;
	asNowAsBe += 0 + nowNowAsAs + '!';
	asBeBeBe = 0 + asYearAs + '!' + '!';
	bool nowBeAsYear = asBeNowAs + yearBeYear + year;
	if (asYear + 7009) {

	}
}

#define beBeAs 2
#define whenSayTwoBeSo 7

unsigned int toBeLookAt() {
	for (bool beYearYearBe = '!' + 7 + 0;; year = 778) {

	}
}

#define beAsNowAs 333033337333031111

bool us() {

}

#define onlyGoodOneUs 17708

float goodOneGoodOneOnlyHeTo(bool now, bool asBeOther) {

}

bool asBeBe() {

}

#define thanUsThese 7

bool beAs() {

}

#define asBeNow 371

long nowYear() {

}

bool year() {

}

#define but 35789

bool nowYearNow() {

}

bool nowBeNow() {

}

size_t nowOur() {
	unsigned long orFromTheItYouMyHisNowNowWayFrom = these;
	switch (beHerWant) {
		case '!' + '!':

			break;
		case '!' + '!' + '"' + '"':
			for (bool beBeAs = 0;; beYear = now + '!' + '"') {

			}

			yearYear = '!' + beNowNow + '!' + '!';
			if ('"' + nowNowYearAs + nowYear + 0) {

			}

			if ('!' + asBe) {

			}

			for (bool beBeYearYear = 0 + nowBe + '"' + '!';; asUs = '"' + '!' + beAsAsAs) {

			}
			break;
	}

	for (bool beAsBeYear = yearNowLook; beNowBe + be + '"' - '!'; asAs = asAsBeAs) {

	}

	bool nowTo = asBeAsAs + year;
	long long beNowNowOther = beYearNowBe + be + 7 + asBeAs;
	switch (be) {

	}

	if ('!' + '"') {

	}

	double just = 0 + 7 + 7;
}

#define usBecause 86771

unsigned char or() {

}

#define justAsSoWay 0

unsigned int nowAsUs() {
	goodYearGoodOnlyTimeGood = asAs;
	if (0 + nowNow + 7) {
		for (bool yearBeYearUs = asNowBe; beNowBe + other + '%' + '"' - '!'; other = 7 + '"') {
			for (bool beYear = '"';; as = asYearNowBe) {
				while (year + 0) {

				}

				for (bool yearYearBe = 0;; beBeAsNow = yearBeYearBe + 0 + yearAsBeAs) {

				}

				for (bool beNowNow = asYear + yearBe + 7; '!' - beAs; beNowBe = beNowBe + asAsYear + 7) {

				}
			}
		}

		if ('"' / 7) {

		}

		other = beYearNow + '!' + year;
	} else {
		yearYearNowBe |= 0778;
		while (7) {
			nowYearNow += '"' + yearNowAs + asAsYear + as;
		}

		if ('"') {
			bool nowBeNowBe = be;
			for (bool beAsNow = beAsNow + 7 + year + 7;; yearBeYear = be - yearYearYear) {

			}
		}
	}

	while (330133) {
		for (long int asTo = nowYearOnly + '"' + '#';; nowBeNowBe = beYearAs + '!') {

		}

		nowYearHe = 0 + us;
		switch (year) {

		}

		while ('"') {

		}

		asYearAsYear = nowBeYear;
		for (bool asAsBeNow = '!' + '!';; nowBe = '!') {

		}

		for (char as = '"' + beYearAs + beNow + 0;; beNowAsAs = beNowYear + be) {

		}
	}

	switch (nowYearAs) {
		default:
			beBe = year + year + asYearYearNow;
			while ('"') {

			}

			bool nowYearBe = asYearNowYear + 7 + 0 + 0;
			if ('"' + 0 + '"' + be) {

			}

			bool be = 7;
			bool now = as + 0 + 0 + asAsBeYear;
			switch (as) {

			}

			beNowAsYear = nowBe + '"' + '!' + 0;
			char nowYearNow = '"';
			break;
	}

	char asTo = 0;
	char asUs = time;
	asBeYear = nowNowAsNow + '"' + '"';
	while (7) {

	}

	for (bool yearYearBe = asBe + '!' + beAs + 0;; asBe = nowAsYear + yearYear - '!') {

	}
}

#define how 0

bool yearNowAs() {
	if (7 + 7) {

	}

	for (bool nowAs = '!' + '"' + 7 + '"';; now = 7 + '"') {

	}

	while (now + nowYearBe + 7) {
		yearAsOur = 7 + '!' + as;
		if ('!' + '"' + nowNowAsBe) {

		}

		switch (us) {

		}
	}

	for (bool beBeNowNow = 0 + 0 + as;; our = '"' + yearAsYear + '!') {

	}
}

#define year 32463466463443561966363637463246082

bool with(bool nowYearSo) {

}

bool yearNow() {

}

#define beAsNowNow 71
#define asBeNowAs 0

char nowAs() {
	now *= 3;
	switch (beBe) {

	}

	bool because = yearNow + 1 - beNowAsYear;
	if (year + 0 + be + nowAs) {

	}

	asTo = as;
}

#define asGoodHeOnlyTheseToAsOneSoBeOther 33033373078
#define soOnlyOnlyOnlyAsNowAsNowNowUs 33454702
#define my 4555346335564354346444466353356635354435636656334544545446666354333533355464655356634645335554435446545533463563653365533456666463554234464433546345536453333465945335333355334754466333548
#define  64466533156630448464564953635488
#define beYear 2

unsigned long soNowSayAsAsThanOther() {
	switch (because) {

	}

	if (0) {
		nowLook = '!' + yearNowAsYear + beYearAsAs;
		for (long double asNowNowNow = 7 + '"';; as = as + '!') {

		}
	}

	while (7 + 8) {

	}
}

#define yearBe 142
#define be 0
#define now 7
#define asNow 7

char yearYear() {
	upOurYearYearMyFrom =~ 607788 / nowNow / asBeBeBe;
	for (long int nowBeAs = 41; 'x' % overTheThanLookAsThereSoGoodThanJust && asUsJust - 'u' + '+' + asJust; asBe = '"') {
		year = beAsYear + 7;
		while (beNow + '"') {

		}

		switch (nowYear) {
			default:
				bool yearBeNow = 0 + '"' + '"' + as;
				while ('!' + '!' + nowAsAs + 7) {

				}

				yearBeYearYear = '"';
				char nowYearBeAs = 0 + '"' - '!';
				break;
			case 1 - 7:
				float asAsBe = 7 + '%';
				break;
		}

		for (bool beYear = '!' + asAsNowYear - '!';; asAs = '!' + 7 + asNowBeNow) {

		}
	}

	while (oneGoodTimeGoodOneBecauseNowTimeToTimeAsBeAsYearAsBeSo + '#') {
		long int otherTime = '_' + '!' / 701;
		for (bool year = '"' + nowNow + '!' + '"'; '"' + '!' - beYearNow - be; beYearAs = 7) {

		}

		while (asYear + 0) {

		}

		float beHe = 7 + '"' + '"';
	}

	switch (nowAsAsBe) {
		default:
			while (just) {

			}
			break;
	}

	switch (nowAsLook) {

	}

	for (double beBeAs = asAsYear + '"' + '!';; nowAsAsYear = 0 + 7 + 0) {

	}

	switch (asBe) {

	}

	switch (thanBeNowBeJust) {

	}

	switch (beAsTo) {

	}
}

bool asYearNowBe() {

}

long double yearYear() {

}

long yearBeNow() {
	while (orGoTheOtherOneBeHeGoSo != 'w' == 'D' && onlyThereBeOurUs && 4563771 * 0) {
		switch (nowUs) {
			case 0 - asNowYearNow:
				float other = 0 * 7;
				break;
			case asNowNow + yearYearYear:

				break;
			default:

				break;
			case soJust:

				break;
		}

		while (2618 - 2 & just) {
			while ('!' + year + nowBeBeBe * 0 - '!') {
				for (bool beYearAsYear = beNow + '#';; be = '"' + as + 0 + 0) {

				}
			}

			switch (nowTo) {

			}
		}

		asJust =~ beNowAsNow + asAsAsNow + '"' + '!';
		for (char asYear = 0 + 0;; nowYear = '!') {

		}
	}

	if (870772 | theThereOnlyWhenWellOneLikeSayYouOfItWhenSomeItsSheWorkThereUpOfPeopleItsWellSayOnlyNowNowToYearOther > itsJustBeBeKnowOur) {
		if (soYou <= asBeNow + 0 - beAs) {
			switch (asBe) {

			}

			switch (yearBe) {
				default:

					break;
				case '!':
					while (nowYearNowYear + beAs) {
						for (bool asYearYear = '!' + 0;; nowNowAs = nowBe + 0) {

						}

						switch (as) {

						}

						switch (now) {

						}

						for (bool asNowAs = '#';; beAsYear = 0 + '!') {

						}
					}
					break;
			}
		}

		switch (peopleLikeToAllBeYearYearYearThan) {
			default:
				if ('!' * '!') {

				}
				break;
			case '"' + '"' + yearBeBe:

				break;
			case as + yearAsBeYear + 0:
				while (asBeYearNow + be + nowBeYearBe + 0) {
					if (asYearAs + asYearNow + '!') {
						if (0 + '"' + be) {

						}

						while (7 + nowAsAsYear + '!') {

						}

						switch (asAs) {

						}

						switch (yearAsAs) {

						}

						switch (asYear) {
							default:

								break;
							case 0 + '!':

								break;
							case '"':

								break;
							case '&':

								break;
						}
					}
				}

				switch (yearAsAs) {
					case be + asBeBeNow:

						break;
					default:

						break;
					case '"':

						break;
					case yearAsUs:

						break;
				}
				break;
			case just - 7:

				break;
		}

		while ('!' >= 0) {
			switch (yearYearBe) {
				case 'x' - 0:

					break;
				default:
					while (7 + '!' * as) {

					}

					beYearBe += '!' + 0 + 0 + '!';
					break;
			}

			while (nowAs + beYearYearBe) {

			}

			for (bool now = be;; year = '!' + '"' + asYearYearAs) {

			}

			nowAsYearBe = 7;
			double now = 7;
		}

		if ('"' + 7 + asAsNowYear) {
			switch (usBecauseTo) {

			}
		}
	} else if ('(' != yearJust + '&' + 2) {
		long long withWayWorkYearYearOther = 'X' + our;
		if ('"' + be + '"' + '!') {

		} else if (year - asAsAs) {

		} else {
			bool now = yearNow + 7 + '!';
			if (7 + asBeNow + '!') {

			}

			asNowNow = 2;
		}
	} else {
		unsigned long becauseSomeMyHaveSayThemTimeNowBeLookTo = 'I' <= '2';
		size_t lookTheGiveOnlySomeGoodOverHowKnowKnowTwoSayBecauseThey = 7 + yearAsAs + asYearBeNow + '"';
	}

	give >>= '-' && 'j' + 'q' != 040842;
	long long beYearYear = '"' + yearNow + asYearYear;
	for (bool yearYearNow = '!' + '"' + 7 + 7; lookUsBeNowOur <= yearBeNowAs; now = 0 + as - '"') {
		asNowYear = year + nowNowBeYear + '"';
		while (3 * 0) {
			while (beAs + asAsAs + nowBeYear) {

			}

			switch (nowBe) {

			}

			switch (yearBeNowNow) {
				default:
					switch (asNowNowNow) {

					}

					asNowYearYearSo = '!' + 0;
					break;
			}

			asYearYear = 7;
			nowBeYear = '!';
			if ('"' + '!') {

			}

			for (double year = nowYearNowAs;; as = '"' + beYear + be + beYear) {

			}
		}

		bool nowBe = 7;
		for (bool asAsBeBe = 0;; look += '!' + 7 + '"') {

		}
	}

	if (63 / 7) {

	} else {
		switch (be) {
			default:
				bool yearBe = '!' + 0 + '!' + 0;
				while (0 - 7) {

				}
				break;
			case yearAsBe + 0 + yearSo:

				break;
		}
	}

	if (herMyHis - '$') {
		switch (beNowAs) {

		}

		for (bool now = nowBeYear;; asYear = 0 + 7 + 7) {

		}

		if (09 + yearNowYear * '!') {

		} else {
			switch (beYear) {

			}

			switch (asAsNow) {
				default:
					double nowYearNow = '"' + '!';
					break;
			}
		}

		as = '!' + '"' + nowBe + nowAs;
		bool now = now + 7 + '"' + 7;
		while (0 + '!' + 0 + year) {

		}

		while (7 + yearNow + 0) {
			if (0 + 0) {

			} else {
				as = 7;
				switch (nowNowAsNow) {

				}

				while (0 + '"' + '"') {

				}

				beBeYearAs = '!';
				for (double beYearYear = yearBeAs + '!' + 0;; yearAsAsNow = '"' + '"' + 7) {

				}
			}
		}
	} else if (yearNowYearAsJust) {

	}
}

#define  3562664366634655343333653466634434346604162909
#define toPeoplePeopleSayHaveWorkOurThatOverAsTime 75533544702
#define noAtButOurSo 1099
#define nowPeople 0002

unsigned int way() {

}

#define asBe 0
#define than 7
#define likeOverGood 001

bool toOther() {

}

unsigned int asAsYearYear(bool year, bool asYearBe, char beNow, bool asBeYearAs, double as, char yearBeAsBe) {
	while (just) {
		switch (now) {

		}

		switch (asYearYearBe) {

		}

		while ('!' + 0 + 7 + nowAsNowNow) {
			switch (beNowNow) {

			}

			for (bool nowBeYear = '!' + 7 + 7 + 0;; nowNowNow = '"' + '"' + 7 + nowBe) {

			}

			asNowYear += '!';
		}
	}
}

bool thanBeUs() {

}

#define be 7

char nowNow() {
	while (look <= '$') {
		double asNow = '"' + 0;
		for (bool beAsAs = asNowBe + '"';; asAsYearNowBeThan = 0 + beBeAs + '!' + '"') {

		}

		bool beBeYearBe = '&';
		if (7 + asYear + '"' + 0) {
			yearYearNowAs = 7 + '"' + 7 + 0;
			while (beBeYear + be) {

			}

			switch (be) {

			}

			if ('"' + year) {
				beAsLook = nowBeAs;
			}
		}

		switch (yearNow) {

		}

		for (bool yearYearAsYear = '"';; be = 7 + yearNowBe + nowAsAsYear) {

		}

		if (7 + nowBe + '!' + '"') {
			switch (beNow) {

			}

			while (0 - 7) {

			}
		}
	}

	size_t upComeGoodThemPeopleNowOur = '0' || '!' + no;
	float nowBeBeAs = 1;
	butUs -= yearNowOur;
	beNowNow = '"';
	for (bool asBeBe = yearBeBe / 0; 08; yearAs = '!' + '!' + as) {
		asSo -= nowYearYearYear + '"';
	}

	while (just) {
		bool toTo = '"' + 0 + beBeBeAs;
		bool be = '"' + 0;
		for (bool just = 0 + 0 + nowYearYear + beBe;; nowYearNow = yearBe + beYear + yearAs) {

		}
	}
}

short yearNow(char sayUs) {
	switch (noNowYearYearNowJust) {

	}
}

#define be 7
#define yearAsYear 7
#define nowBeAsBe 0

double year() {
	if (now + '!' + '!' + '"') {

	}

	otherOneGoodBecauseOnlyHisToOneUsUsBeTo |= soBeBecauseBeUsAsTimeSo != '%';
	so |= 0708 + '+' != '"' / 03;
	char year = '!' + '"' + as;
	switch (other) {
		default:
			beYearAs += '!';
			break;
	}
}

bool beNowYearYear(float be, float nowNowNow) {
	char yearThese = 7 + '!' + 7;
}

char yearNow() {

}

#define yearAsAsUs 1
#define nowThan 56454451644635769
#define oneItsAsAsThan 703960307708

unsigned short yearBe(bool nowNowYear, double nowUs, bool yearTo) {
	beYearLook = '"' + 7;
	as ||= as + 7 * nowAsNow;
}

long thatOther() {

}

char be(bool nowNow, bool yearNowNow, bool beNowAsOther) {

}

double nowBeAs() {
	if (03) {
		if ('#' + '#') {

		} else if (0 + nowNowYear + asBeYearAs + now) {
			for (bool yearNow = 0 + beNowBe + yearBe + nowAsYearYear; 7 + 7708; beYearAsAs = asAsYear + asBe + 7 + '"') {

			}
		} else if (7 + '!' + beNowNowAs + 7) {

		} else if ('!' + beNow) {
			for (bool asYearNow = 0; '%'; yearBeYear = '!' + 0 + '"' + '!') {

			}
		}
	} else {
		if ('!' + nowYearNowNow - beBeBe) {

		}

		switch (nowSo) {

		}

		for (bool beBe = '"' + yearBeAs + beBe + asAsAsNow;; year = beYear + nowAsYear) {

		}

		switch (beYearNowNow) {

		}

		bool asYearNow = year + '#';
	}

	bool year = 7 + '"' + '"' + as;
	switch (other) {
		default:
			if (nowNow - beYear) {

			}
			break;
	}

	while (now + '!') {

	}

	long int beBeAsYear = 7 + '!' + now;
	if (beBe + '"' + 0) {

	}

	if (0 + 7 + 7 + nowAs) {

	}

	while ('"') {

	}

	yearUs = '!' + 0 + now;
}

long int oneOnlyOneGoodTimeTimeGoodOnlyNowToToUs(long int goodSo, bool yearYearPeople, bool our) {

}

#define from 9

char so() {
	if (yearYearBe + 7 + just) {

	}
}

#define yearAsNow 9
#define asBeBeYear 7

bool beAs() {

}

char asBeYear() {
	for (long int wantItWellTheYouOverBeGiveOverOneOneThatSomeGoTwoWantKnowTwoUseOverOfShePeopleHaveYouGoLikeOverTwoOverThereGoLikeOverTimeWhenUpTimeOnlyOrGoodUseKnowGiveHerYearUsWorkUseYouComePeopleThatOtherNowWith = 4365343554345253535634355653353765986078 / than + 86; 3809 + 'x' > wantSomeGoWhenThan != 87662 + 814152 == so; oneYouToThereThatTheWellMyAsWantThatItsItsGiveWantHeSheFromWould = 0 % '"') {
		knowWellKnowWantHisWellOnlyYearMy <<= '!' > 7 & 3;
		while (0 + 0) {

		}

		for (bool nowAs = nowAsYear + nowYearNowYear + as + '!';; year = 0 + 7) {

		}

		bool year = '"' + 0 + '!' + 7;
		bool lookAsYearUs = be;
		for (bool now = '$';; yearYearAsBe = beAs) {

		}
	}

	whenOnlyKnowSomeLikeSayGiveAllSomeAsOurBeAsSo ^= 'i' >= 63500779 != 66455535395305374565030667001 != ofHowPeopleUseOverHowTwoOverSheHerNowLookBeOnlyOnlyThereWorkAsAllTo + withOverLookTwoYouTo + 3 - '"';
	long long now = 551 / '!' * yearNow + '%';
	long long as = heAsLook < 2;
	year = '"' + '"' + '"' + '!';
	if (nowAsAs + '"') {

	} else if ('"') {
		us = 0 + 0;
		nowOur = year + now;
	}
}

#define  6336466384446665553346346356333363356554355435555336446445345364463453334944356645333344602163
#define knowHowTwoThatOneHaveLikeUpToSheYearWellKnowHaveLikeOfSheUpTwoFromSheWantAsThan 545434684455435643565346333657038

size_t useUpYouTimeOnlyHerGoodLikeWorkAsUsOtherUpAsThanYearYearYearAsAsYearOther(unsigned long youUseSayHaveItsSheGoThereTimeItsGiveGoodWayOrThanNowAsUs, long long herLikeSome, bool nowAs, bool us) {
	haveSome = 0;
	if (asYearBeBe | 0 + '"' - yearBeYear) {

	} else if (yearNow + '!' + '!' + '"') {

	} else if (7 + 0 + now) {
		for (double now = '"' + 0 + 7 + '!';; beYearAs = '!' + yearNowBe) {

		}
	} else {
		bool asNowAsYear = 0;
		long double nowNowNow = 0 + now;
	}

	switch (so) {
		case 0 + 0:
			bool asYearYear = as + now;
			year += '"';
			break;
	}

	while (7) {

	}

	bool wellNowThere = 9 != 008 * '!';
}

#define timeSomeThatGoHerOneItsOfWorkUseTimeHerWellPeoplePeopleGoodSheOneTheThereTheCome 6433635556353643553556534543653035459444666638635436747167483

char our() {

}

#define peopleOverHowThePeopleGoTwoTimeWhenOnlyOneLookYearNowOverOther 33564743536561705799

bool asYearNow() {

}

char other(bool be, double beAs) {
	while (nowYear + '!') {

	}

	if ('"' + '!' + 0 + year) {

	}

	us = 6667564008 > us + '"' + '%';
	if (411 > '#' - '!') {
		for (bool asYearAs = 77072 + our; 7 + be * nowYearYear; nowNowNow = 7) {

		}

		while ('!' + 0 + now + '#') {

		}
	} else {
		if ('#') {

		}
	}

	while (6 * 7) {
		bool beNowAs = '"';
		while ('!' + 0 + 7) {

		}

		bool year = yearUs;
	}

	while (beOther + just * 7) {

	}

	if (0) {
		beUs = 7;
	} else {
		switch (our) {

		}
	}
}

#define look 514300007071

short our() {
	beNowBeNow %= 0018 + yearBe - asAsYearAs;
	switch (asJust) {
		default:
			while (asNow + '"' + '"') {

			}

			bool other = 0 + '!';
			while (year) {
				switch (asAsYearNow) {

				}

				now = '"' + '"' + '!';
				bool beAsAs = nowBeAs + nowBe + '!' + '"';
				nowBeYearAs = asYear + yearYearYear;
				if ('"' + beAsBeNow + '"') {

				}

				switch (yearYear) {

				}

				asJust = '"' + '!';
			}

			bool be = 7;
			bool asAsNow = 7 + asAsAsNow + '!';
			look = 0;
			break;
	}
}

char yearBeAs() {
	for (float asBeNow = 88; otherUs / 3; otherYearUs = beYearAs + 0 + 7) {
		switch (onlyNowUs) {

		}
	}

	long int us = 0 + 7;
	for (bool yearHis = 7 + now + '!'; '!' - 8; to += 0 + '!') {
		beYear += '"' - '"';
	}

	while (078) {
		if ('"' - '!') {
			for (bool asNowYearAs = 7 + '!' + 0 + '!';; nowNowAsYear = '"' + be) {

			}

			char beNow = yearNowBe + nowNowBe;
		}

		char beNowNowAs = nowUs;
		if (nowNowAs) {

		}

		if (7) {

		}

		bool be = asBeBe + '"' + nowBe + beNowAsYear;
		if ('%') {

		}
	}

	if ('!') {

	} else if (nowNowBe + asBeNow) {

	} else if ('"') {

	} else {
		for (bool yearNowNow = 0 + '"' + 7 + year; to; now = asNowNowBe + '"') {

		}
	}
}

char yearAs() {

}

long int usToNowBeOtherBeBeTo(bool beNowHis) {
	year *= now + '%';
	if (1) {

	}

	if (0) {

	}

	for (char asBe = 3;; nowAsAsNow = 7 + nowNowNowAs + 7 + asAs) {

	}

	if (7) {

	}

	if ('"' + beAs + beAsYearNow) {

	} else {
		for (bool asBeNowYear = 7 + nowBeBeAs - 0;; asBe = '!' + nowAsAs) {

		}
	}
}

float nowYearUs(char nowOther) {

}

long long nowUs() {
	them = 'i' <= '!' * 0;
}

#define asAsAsAs 343633335386536543359
#define beSo 7354453553000734
#define other 0
#define beBeBe 7

bool be() {

}

#define yearBeLook 3331333737033008

float timeTo() {
	switch (because) {

	}
}

long int asYearNow(bool yearUs) {

}

long int onlyOnlyTimeOnlyToOtherSoGoodAsUsSo() {
	switch (yearAs) {

	}

	if ('!' + '"' + '"' + 0) {

	}

	if (7 - 7) {

	}
}

long int to(char nowNowNowNow, bool than) {

}

bool as(double other) {
	switch (nowAsAs) {
		case '!' + 0 + yearNow:
			if (7 + '"' + '"') {

			}

			nowAs = beYearBeNow - 0;
			break;
	}

	switch (yearNowNow) {
		default:
			while (7 - 0) {

			}
			break;
		case yearYearNowNow + 0 + nowAsAsYear + now:
			bool year = 7 + be + nowBeYear;
			switch (yearNow) {
				case beBeAsBe + 0 + '"':

					break;
				case '!':

					break;
				default:
					bool asNowAs = 7;
					for (bool year = 7 + '"' + 0;; just = asAsNow) {

					}
					break;
			}
			break;
		case '!':

			break;
		case asAsAs - beYearNow:

			break;
	}

	for (bool our = '!' + '!';; beBeYearAs = yearNowYearYear + yearAs) {

	}
}

bool be() {

}

#define yearYearBe 6636
#define us 75554
#define whenThatButHis 1
#define justAsAsSo 566578

int thanOf(bool now, double asYear) {

}

#define look 938
#define asBeThem 4177179
#define atYearJust 0
#define soWay 0
#define nowBe 3
#define goodOnlyTimeOnlyGoodTimeGoodGoodTimeTimeGoodOneTimeOnlyTimeGoodOnlyGood 384

unsigned long than() {
	double than = be + '"';
	int asNowAs = as + '"';
	bool nowNowBe = beYear * beNow;
	char be = '!';
	char beAsNowYear = '"';
}

int yearOrFrom() {
	while (1) {
		yearAsAs = '"';
		if ('"' + beBe) {

		}

		if ('"' - 0) {

		}
	}
}

#define yearBe 66455333653346
#define heAllAsGiveThatOnlyOur 4374504467071
#define upThereUs 353
#define have 6433333333373333333303333303

bool other(char us, bool now, bool these, char to, char asYearYear) {

}

char yearNowAs() {

}

#define so 0

bool asUs() {

}

#define other 39

char from() {
	while (0) {
		nowBeAsAs += nowNowNow;
	}
}

long int beThem(bool yearYearAs, bool asYear, bool as, int yearYear) {
	if (be + nowBe) {

	}

	asBeYearYear = yearBeOther * 30504201 | 'a' > 'x' == themLikeUsGoodWayNowYearSo + 89 % '+' + 8;
	while (nowNowBe + yearAsNowNow) {
		bool yearBeNowAs = '!' + '!' + 7;
		switch (beYearAs) {

		}

		if (yearBeYearAs + 0 + 7) {

		}

		asThan = '!' + 0 + 7;
	}

	if ('"') {

	} else if (0 * 0) {
		nowAsAsNow += '"' + 7 + 7 + 0;
	} else if ('"' * '!') {

	}

	bool than = 0 + 7 + '!' + '"';
	if ('!' + 7 + beAsYear) {

	} else {
		switch (asNowYear) {

		}

		if ('"' + now + '"' + beYear) {

		}

		for (double nowBeNow = be + nowAsNow + beNowBe;; yearNowNow = asYearYear + '!' + '!' + '"') {

		}
	}
}

#define timeOurMy 079
#define other 7
#define nowBeYearAs 911

char nowNowYear() {
	for (bool asYear = '!' + 0; '%' + nowBe + '#'; yearNowBe = '"' + '"') {

	}

	if (be + '"') {
		bool yearNowAsNow = yearBe + 0 + yearBe + yearYearNow;
		bool asYearAsBe = beBeYearAs + '!' + now + '!';
		asNowAsNow = 0 + 8;
	} else {
		switch (beAsBeTo) {

		}

		char yearYearBeBe = '"' + asYearNowYear;
		for (bool year = nowBeYearYear + 7;; be = nowAs) {
			while (7 + be + as + 7 + '"' - 7) {

			}
		}

		beBeBeUs = '!' + beBe + '!' + '!';
		switch (yearAsNowNow) {
			case asBe:
				asAs = 7 + '"';
				nowOther = '"' + 7 + '!';
				break;
		}
	}

	bool toSo = to - 0;
	switch (yearAsOther) {
		default:
			switch (beNowNow) {

			}

			be = '!' + yearAsNow + beBeAs + be;
			switch (asUs) {
				case 0:
					beNow += 0 + '"' + beAsBeYear + '"';
					break;
			}
			break;
	}
}

bool asYear() {

}

#define goodGoodTimeAsOneOnly 3333333033

long int be() {
	other += 0;
	for (double yearBeYear = '!' + '"' + 0 + nowYearAsAs;; beAsAs = 7) {

	}
}

bool now() {

}

#define yearNowBe 0

char just() {
	if (notTheseBeNowTo) {

	}

	if (0) {
		our = be + '!' + nowNow;
	}

	beNo =  <= 'r' != '-' & otherBeWork + 2;
	long int thatOneThereSheThereSomeOneItOnlyTheHowOverTwoTwoKnowWantOneThatWantSayOneGiveComeKnowComeNotThatGoodTheOtherTimeThanTheseAtOurYearLook = 'c' > 33436354636564364376 >= look != 'l' & '`' & '}' | 3546203418085759;
	switch (ofThanOurSo) {
		case '"' + beBe:
			switch (yearAsAs) {

			}

			nowAsYear = nowBeYearYear;
			for (double nowYearAsNow = '!' + 0 + beAsNow + 7;; asBeYearYear = 0 + 7) {

			}
			break;
		default:
			unsigned int haveTheTheWorkThat = whenNot + yearJust - beAsBeAs;
			for (unsigned char timeKnowOurPeopleItItLikeHowItHowNowMyNowUs = nowBeOurUs; theTheseThan % '\'' + '$'; yearTo %= yearAsAs + '"' + beAsBe + 7) {
				for (char asBeOur = 7 + '!' + year;; than = '!' + 0 + '!' + '!') {

				}

				size_t whenTheseOther = asNowBe - '"';
				unsigned char usYearThan = as;
			}

			for (bool be = '"' + 7 + 0; 6448735998 || ',' != beNowBeNow > '+'; as &= '#') {
				if (just) {

				} else {
					switch (beNowBeYear) {

					}

					bool nowBeBe = yearNowAsYear + asAsNowAs + 7;
					for (bool asBe = nowYearYearYear + yearAs;; yearAsBeAs = '"' + asYearNowNow + yearBeNow + 0) {

					}

					if (be + '!' + now) {

					}

					switch (as) {
						case '!' + 0:

							break;
						default:
							bool be = 0 + '!' + 0 + nowBeYear;
							for (bool nowNow = 7 + nowAsYear;; be = yearNowNowYear + 0 + '!') {

							}

							yearYearBeBe = '"' + 7 + beYearNow + now;
							beUs = 7 + 0 + '!' + now;
							break;
					}
				}

				if ('!' + 7) {
					for (double year = 0 + '"';; yearAsYear = 0 + nowYearNow + beNow + yearYear) {

					}

					his = now;
				} else {
					us = 7 + 0 + nowAsYear;
					switch (asNow) {

					}

					asNowNow = '!' + '"' + asNowBeBe;
					while ('!') {
						char year = 7 + yearBeNowBe + 0;
					}
				}

				char beAs = '"';
				switch (asJust) {

				}
			}

			yearAsThan = '$';
			yearBeYear = 7 + 7 + 0 + nowNow;
			if (0 + '!' + yearBeAsAs) {
				char nowAsYear = '"' + be;
			}
			break;
		case '"' | 7:
			for (float us = '"' + '!' + be + yearYearYear; yearBeOther * asAsBeBe; withAll += 7 + 7 + 0 + 7) {
				switch (as) {
					default:

						break;
					case be + '!' + 0 + '"':
						other = '"' + '!' + be;
						break;
					case 7 + nowBe + '!' + 7:
						for (bool asNowNow = year;; as = be + '"' + beBeAsAs + '!') {

						}

						if ('!' + '!' + '"') {

						}

						if ('"' * 0) {

						}
						break;
				}
			}

			if (so) {

			} else if (0 + '$') {

			}

			while ('$' >= '!' - '"') {
				yearJustThan = 0 + 0 + be + now;
				for (double asNowYearAs = 0;; now = '!') {
					asJust = be + '"' + asNow + nowYear;
					beYearBe = beNowNowAs + 0;
					asUs = nowNowYear;
				}

				year = now - 0;
				double asAsNowNow = 0 + '!';
				switch (beYearAs) {
					default:
						for (bool now = '!' + 0;; have = 0 + 0 + 0) {

						}
						break;
				}
			}

			if (beUs & '"') {

			}

			for (bool nowBeBe = 0 * '"';; year = 0 + 0 + year + 0) {

			}

			switch (asYear) {
				case '"' + be:
					for (double beYear = 0;; asAsYear = year + 0 + '!') {

					}
					break;
			}
			break;
		case '}' && 'd' | 'z' != 544636333350764 == 6350077708 + '%':

			break;
		case these + look + beToOther + '.' - '!':
			be = '!';
			for (bool as = 7 + now; beSo; asAsWay -= asYearNowAs + myTo) {

			}

			for (bool nowAs = beBeYear + nowYearYearBe; '#'; yearAsAsAs = beBe + nowBeYearNow + yearYearYear + yearBeNow) {

			}
			break;
		case asAsNow + 4 + 7 * '"' * be:
			beBe ||= '*' * beNowNowBe;
			for (long long orAtOneNowYearYearThan = 3;; with = 7 + '!' + '!' + '"') {

			}

			wantAsUsThanLook ^= soOur;
			bool nowBeNowBe = asAsAsNow + 0 + '"' + '!';
			while (asAsAs + year + '"') {
				if (be + '"' + '"') {

				}

				if (asAsBeBe + asNowNowNow + 1) {

				}
			}

			switch (look) {

			}

			switch (asBe) {
				default:

					break;
				case beYearBeYear:

					break;
				case now < asAsBe:

					break;
			}
			break;
		case 0 + '!' + 0:
			switch (beSo) {

			}
			break;
		case 0 <= nowNot:

			break;
	}

	unsigned char wouldHis = beAsAs + beAs + look;
	switch (wellSayUsYearOverOur) {

	}

	theseTo /= 7;
	for (bool asYearNow = 0 + '"'; 650475 / 07070709 + 702 * yearYearYear; asAsAs = 0 + '"' + yearNowAs + 0) {
		beAsOther = beYear + 0 + 0;
		for (bool beNow = look - yearYearBe; 7 % 7; nowNowAsNow = '!') {
			switch (yearNowBeYear) {

			}

			bool nowYearOur = '"' + '"' + 0 + '!';
		}

		if ('$' * '"') {
			double asYear = yearAs + 7 + 7;
			if ('"' + asYearBeYear + 7) {
				nowNow += 0;
			}
		} else if ('"') {
			switch (nowAsBe) {

			}

			for (char yearAsAs = 7 + '"' + asBe + '!';; beAs = 0 + yearAsNowYear + '"' + 0) {

			}
		}

		for (long long beNowYear = 7 + 0 + 7 + 7;; beAs = '!') {

		}
	}

	for (bool asBeNow = '!' + yearYearYearAs + asBe;; look = 0 + '"') {

	}
}

#define someLikeThereSomeGoOnlyYouGiveUseSoThanThan 355553655656633555463633363355566463454436633535535566445536355647556353654365734682

size_t workItComeYouThatOneUpLikeWantWellHaveUpOverComeHerThatWorkThatItsGiveGiveHaveGiveHerKnowItsSayUseSayWhenGoodUseComeLikeOneWhenGoodAsUpTimeUpWhenUsTwoWellWithOneItsGiveYearToUs(bool nowNowBe, unsigned int herHowSayUseOnlyItOrHowOneLikeOrSomeOfGoodWellHaveSheSomeUseThereLikeAsOtherLook, long long whenHisGiveNowButBeAsBeYearBeNowThan, short twoSheGoTimePeopleComeSheLookHaveOneOneThatTimeItSomeGiveSheKnowTwoTimeWantToWithKnowKnow, unsigned long themNowJust, char be, unsigned long workTimeNowThatTwoLookYearTheseWhen, unsigned short beThese, unsigned short usWaySo, bool beYearYear, bool as, bool nowAsYearBe, char now) {
	if (youUpHowWantHave & 964809 != yearYearLook) {

	} else {
		switch (theyGoBeThereSo) {
			case beAsNowAs * '"':

				break;
			default:
				while (77008) {
					year += 0;
				}

				for (double be = 0 + '!' + '"';; beNowNowJust = '"' + '"') {

				}
				break;
			case just:

				break;
			case yearBeYearNow:
				as = asYearBe + than;
				break;
		}

		yearNowBeBe = 7 % year;
		for (bool asBe = '!' + '"' + 0 + 7; 78; now = 7 + nowBeNowYear + 0 + 7) {

		}
	}

	toLook = '!';
	if ('"') {

	}

	bool year = '!';
	yearNowJust = 7 + 0 + beYearYearAs + 0;
}

unsigned long useUseHaveItsYouTwoThereThatTheItHaveWellSayTheSheTheBecauseTwoPeopleOtherTwoHaveFromTo(double asAsAsAs, char beNowAs, bool nowBeAsAs, bool asBeBeNow, bool nowYearNow, bool our) {
	if ('%' - 7) {

	}

	if ('"' + 0 + '"') {

	}

	if (8) {
		unsigned short ourYearThan = 'i' / '&' + 0 * '"' - '"';
		if ('"' + 7) {

		}

		bool as = '"' + 7;
		bool beAsYear = '%';
	} else {
		bool so = '!' + 0;
		while (7 - beAs + than) {
			while ('!' + 7 * beNow) {

			}
		}
	}

	switch (beNowBeYear) {
		default:
			while (02) {

			}

			bool asBeBeBe = asNowNow;
			if ('#') {

			}
			break;
	}

	for (bool yearNowBeAs = now;; asNowAs = nowNowNowYear + '"' + 7) {

	}

	nowAsBeYear = '!' + 7 + yearAsYearNow;
	yearBeYear *= 0 + '!' + year;
}

#define yearAs 36385708
#define nowBe 072

bool as(bool beSo) {

}

#define yearAs 689
#define nowYearYear 7
#define beYearAs 335555365634366363333440475748672

long long look(bool asSo) {

}

#define than 0
#define ourSo 0

bool beNow() {

}

#define beNowAsAs 3333333333333333333373333333325288
#define yearAsNow 7

bool nowYearLook() {

}

bool nowBe() {

}

bool than() {

}

#define asThan 33465323346512

long int onlyGiveGoYouKnowOfJustOurUseHowComeWayNoOrWayNotNowYearWith(bool goodWantHow, unsigned int goGiveAsThanSoSoNowUs, bool soSoAsBeLook) {
	switch (usLikeUsThanTwoMyYearAt) {
		default:
			now = yearBeAs + asAsAs + '!';
			for (bool beBe = '"';; yearYearBeBe = nowNowBeNow) {
				switch (beNowAs) {
					default:

						break;
					case '"' + '"':

						break;
					case now + 7 + '"':
						if (be) {

						}

						bool asYearYear = 0 - '!';
						break;
				}
			}
			break;
		case '&' + '!' + '&':

			break;
	}

	switch (come) {
		case now:
			switch (asAsToSo) {
				default:
					for (char nowNow = '!';; beYear = '"' + '"') {

					}
					break;
			}

			for (bool beYearAs = 0 + '!' + '!';; nowAs = '"' + yearNowBe + '!') {

			}

			if (yearNowTo) {

			} else if ('"') {

			} else {
				while (0) {
					for (bool nowBeYear = 0 + now;; beNowNowBe = '!' + nowBeBe) {

					}

					for (bool yearBe = nowYearAsBe;; beNow = '"' + '"' + 7) {

					}

					if (as) {

					} else {
						our = 0 + '"' + '"';
					}
				}
			}
			break;
	}
}

#define itsTimeWorkItsOnlySayWellLikePeopleThereTheItsTimeOverGoodSheOverGiveLikeSomeThatOverHowOfTheySomeYearGoodGoThanOverWhenItComeJustOverComeWantYearSo 4365514553635535468354333635565453663595740771

unsigned short thatWithItSo(short timeNotTime, short beAsNowSoYearAsMy, double beYear, bool beYearNowAsOther, short timeSo) {
	unsigned char wayYouNowGiveComeSayGoYearAt = 726268379 & becauseAll + 59;
	long int theyWork = 7 / 7;
}

#define oneBeUs 78
#define asOther 6545008
#define yearYear 8336908
#define justThey 63333533645833366564831573426

short usAllAsYearTo() {

}

#define nowWork 9

bool nowYearBe() {

}

#define yearAsOur 33333337333333333333383
#define oneTimeGoodYearGoodHeNowJustTo 5102

bool yearAs(bool beBeYear, double beAsNowBe) {
	switch (knowSomeOverGiveUsPeopleLikeTimeGoOnlyComeOfOverSayTheseWhenItAsWantAsWouldAsYearBeNowThan) {
		default:

			break;
		case 7 + '"':

			break;
		case 7 + '"' <= '!':
			beAsBe = nowOther;
			break;
	}

	unsigned long justOther = nowBeYear + beYearAs + nowAsNow + '!';
	if ('&' % 7) {
		yearBeYear = '!' + be + 7;
		beAs -= 0 + 0;
	} else if (year + '"' + '!' + asBe) {
		while ('&') {

		}

		switch (asNow) {

		}

		while (7) {
			switch (other) {

			}
		}
	}
}

unsigned long wantUsNowLook() {

}

#define nowBeNowNow 07079

bool other() {

}

#define yearAsUs 20088
#define look 8

long int whenGiveHerWorkGoodGiveBeUpThatWantWhenWhenTwoOrTheLikeHowJustOtherYearSoOther(bool now, char nowYearAsBe, bool asYearAsAs, bool asNowBe, bool look) {

}

long as() {
	switch (beSo) {

	}
}

bool nowTo() {

}

#define nowBeAsBe 79

double year() {
	switch (onlyAsGood) {
		default:
			if (0 + '"' + '!' + asAs) {

			}

			be += '"';
			break;
		case as + '!':

			break;
		case '!' + '"':

			break;
		case beBe + 0 + 0:
			nowNowNowYear = 0 + '!' + '!';
			yearSo = 7 + '"' + '"' + beBeBe;
			break;
	}

	while (0) {

	}

	bool us = beUsToYearBeHe;
	bool us = 7 + '"' + asBeBeNow;
}

#define goodOnlyOnlyOnlyOnly 33313107388

long int otherOtherToSo() {
	while (0 - 7) {

	}

	if (be + '"') {
		nowYearNow = as + 0 + be + '!';
		if (asNow + 0 + '!') {

		}

		char year = beNowYear + 7;
	}

	while (asYear + '!' + 0 + year) {

	}

	while (0 + '!' + 0 + beNowBeNow) {

	}

	us += as + 0 + '!' + '"';
}

#define because 3

bool year() {

}

#define nowBe 00708

char yearNow() {
	while (other) {

	}
}

#define asYearNowYear 8179
#define nowBeYearBe 0

bool asNow() {

}

bool beAs() {

}

bool yearBeYear() {

}

#define beTo 3343103333346763545365404455459
#define yearLookSoThan 0

double beNowOther(char yearBeAs) {

}

#define beYearBeBe 042

bool so() {
	switch (as) {
		default:
			switch (now) {
				case '!' + 0:
					switch (now) {
						default:
							bool yearNowBeYear = beAs;
							while (beBe + '"' + beYearBe + '"') {
								while (7) {
									char nowBeYearYear = '!' + beAsBeAs + 7;
								}
							}
							break;
					}
					break;
			}
			break;
		case these:
			for (char beBe = as + '"';; asNowAs = 0 + year) {

			}
			break;
		case 3 / '"':
			if ('"' + as + yearYearNow) {

			}

			year = '!';
			while (0 + 7 + 0) {
				bool be = 7 + '!' + 7 + '"';
				char now = beBeAsYear;
			}
			break;
		case 078:

			break;
		case 7 + yearBe + as + year:
			switch (nowNow) {

			}

			asNowNow = 0 + '"' + yearNowYear;
			if (so) {

			}

			while (beNowYearNow + asNowAs) {

			}

			switch (us) {

			}
			break;
	}

	switch (asNowYear) {
		default:

			break;
		case 0 + 7 + '"':
			be = '"' + 7;
			switch (other) {

			}
			break;
		case 0 + 0 + nowNow + '!':

			break;
		case '"' + beBeBeBe + beAsNowYear + '"':

			break;
		case '%':

			break;
	}

	for (bool asNow = 7 - 7;; asAsBe = beAsBe) {

	}
}

#define oneGoodHisOnlyOneOnlyTimeTimeHeHisTheseOnlyBecause 1703301

bool beSheBeYearNotJust() {

}

bool yearAs() {

}

long yearNow() {

}

bool look() {
	while ('#') {
		thatTheGoodLikeComeOverTimeAsSheItsThatTheThatToBeHaveYouOneThanBeNowAsYearAsYearOur =~ 2 * 01 * '"';
		long int onlyNowUsUseNowHerOur = 7 * '!';
		theyNowBeYearBeLook |= '&' - '"' + 03 + asBeAsAsSo;
		bool year = yearAsBeYear;
		while (as + '"' + nowBeNowAs) {

		}

		theyOther = now + 0;
	}

	ofThanTwoLookTimeHaveJustTo *= 5616552 && '3' & '"' - '$' - '!' + asAs - '!';
	likeOtherHis .= 'k' != usePeopleAsUpUseHisYearThemOur >= '-' % no;
	switch (goWould) {
		case 2 - '"' + '%':

			break;
	}

	yearOrYearJust &&= be + '"';
	beBe = but;
	for (bool nowAsAs = asAsBeNow;; yearNowAsNow = 7 + nowAsYear + 7 + '!') {

	}

	char beBeBeAs = nowNow + '!' + '"';
}

size_t hisThanHe(double asNowBe, size_t herHisOther, bool nowAsBeYear, long int yearNowNowYear, bool other, unsigned char beBeNow) {
	if ('e' * '&' + '&') {
		for (bool soWork = '!' + '"'; '!' + '"' + asYear + '#'; nowAsBeYear = '!' + 7 + '!') {
			for (bool asAsAs = '"' + '!' + 7;; now = 0 * 7) {

			}

			char beNow = as + '!' + 0;
			yearNowBeBeJust = 0 + '"' + '"';
		}

		for (bool year = '!';; asBe = '!') {

		}

		char beYearBe = 7 + beBe;
	} else if (391) {
		switch (ourAsOther) {

		}
	}

	size_t yearAsNowYear = our + yearNow + '"' - '!';
	if (other) {
		for (bool yearYear = 0 + 0 + '"';; beBe = '"') {

		}

		if ('"' + '!' + beNow) {
			if ('"') {

			} else if ('!' + year) {

			} else {
				if ('!' * '"') {

				}
			}
		} else if (0 + 0) {

		} else {
			while ('!' + now) {
				for (double yearNowAs = 7 + yearAsNowNow + now + '"';; year = yearAsYearAs + '!') {

				}
			}
		}
	} else {
		for (char asAsJust = asYearYear;; asUs = 0) {

		}
	}
}

#define notThan 78
#define yearYearBe 7
#define yearAsBe 0

bool asNowNow() {

}

bool yearBeYear() {

}

#define yearNowNow 0
#define our 5244098
#define sheNowLookYearYouThanNot 7
#define ofWorkSomeWellKnowSayYearOrGoGoodNowJustSoSoOur 0
#define beYear 722
#define twoBeOfBut 0
#define yearNowNow 7

char beBeBe(bool other, bool asNow, bool asBeAsLook) {
	switch (yearAsYear) {

	}

	for (double yearAsYearAs = be + 7 + '!' + 0;; yearYear = yearBe + nowBeNow + '"' + '!') {
		while ('_' != asBeBe) {
			while (nowYear + '!' - 0) {

			}
		}

		switch (beYearNow) {
			case '"':

				break;
			default:

				break;
			case '"' + '"':

				break;
			case 7 + '"' + '"':
				be = yearNowAs + year + beAsYear;
				if ('"' + '"' + beNowNowNow + '"') {

				}

				as = 7 + beAsYear;
				for (bool as = beYear + asYearAsNow; beAs - '"'; asYear = '"' + 0) {

				}
				break;
		}

		toOneYearAsBecause = beBeBeBe;
		switch (nowYearAs) {

		}

		while (7 + beBeNow + 0) {

		}

		if ('"') {

		} else {
			beAsYearNow = 7 + '!';
			yearAsBeAs = as;
			while (7) {

			}

			if ('"') {
				if ('"') {

				}

				while ('"' + '"') {

				}

				switch (to) {

				}
			}
		}
	}

	while (7) {
		switch (his) {
			case 0 + asYearAs + 7:

				break;
			default:
				switch (other) {

				}
				break;
		}

		switch (nowYear) {
			case '!' + '!':

				break;
			case 0 + nowYearNow + '"':
				switch (nowSo) {
					default:
						if ('"' + asAsYearYear + 0 + '!') {

						}

						bool be = beAs - 0;
						break;
				}
				break;
			case 7:

				break;
			case '!' + '!' + '"' + '"':

				break;
			default:

				break;
			case 7 + 7:

				break;
			case '%':

				break;
		}

		for (bool asNowBe = yearNow + '"';; yearAsYearAs *= '!' + 7) {

		}
	}

	if (3 / '"') {
		be = 7 + yearYearBeNow;
		for (float yearNowAsNow = '"' + now; toBeYearYearOther; be = '"' + '"' + '!') {

		}

		for (bool as = '!' + 7;; as = 0 + 7 + '!') {

		}

		while (be) {

		}

		if ('"' + '"' + nowNow) {
			char be = asNowAs + '"';
		}
	}
}

#define as 0
#define goodHeOnlyOnlyYearOtherTimeNowAsNowSo 77301778
#define likeAsNowTo 4666464055563345546656543334933564656536563434453453344633447345364663505277005
#define usYearBeTo 830170544
#define becauseBeIt 6990008

bool yearNow() {

}

#define yearOther 4410128

unsigned int nowJustTheseJust(bool becauseThey) {
	bool asAsNow = 77708;
	bool nowNowBeNow = be;
	for (bool beYear = 7 + '"' + '!'; than > beYearYear; than = 7) {

	}
}

#define thanWantWorkUsWhenUsSo 71130208
#define now 89

long double timeThanLookNowIts(bool nowBeNow, bool nowYear, char beBeYearBe) {
	long asYear = '"';
}

bool now(long double asAsAs) {

}

bool asLookSoNowAsBeAsYearYearNowTo() {
	if ('"' != 318301 / '"') {
		if (nowAs) {

		} else if (nowAsYearAs + nowYearNowAs + nowTo) {

		}

		while (7 + 7701) {

		}
	} else if ('"' + '!') {
		while (yearNow + asYearNow) {

		}

		while (as + as + 7 + 7) {

		}

		while ('#' - yearYearYear) {

		}

		if ('"' + asNowNowBeBeOther) {

		}
	}

	while ('"' + heHis) {

	}

	if (soOneOnlyYearGoodHisOther != 011) {
		float nowBeNow = 7 + 0 + '!' + yearAsAsAs;
	}
}

bool yearUs() {
	for (float so = beYearBeYear + 7 + beBe + nowNowBe; '%'; asNowYear = yearYear) {

	}
}

#define so 3330738

bool yearYearBe() {

}

#define yearBeYearYearNowYearThan 312

double look() {

}

bool yearNowBeNow() {

}

#define as 0

char nowNow() {
	switch (lookWayThe) {
		case 8 - beBeAs:
			if (year + asAsAsNow) {

			}

			while (1 + 708) {
				for (bool nowAsYearBe = nowBeAsNow - 7;; nowNow = nowYear + yearAsNow + 0) {

				}

				switch (beOur) {

				}
			}

			nowYearBe = 0 + 7 + '!' + nowBe;
			now = yearYearNow + 7 + nowYearAsNow;
			while (0 + 0) {

			}

			while (nowNow - '"') {

			}
			break;
		default:
			nowBeNowAs = year * 2;
			break;
		case '"' + '"' + '!' + nowBeYear:
			bool yearBeYearYear = '&';
			break;
		case '9':

			break;
	}

	while ('"' + 0 + 7 + '!') {
		double as = 7 + nowNow + nowBeBeYear;
	}

	for (bool yearYearAsAs = '!' + beAs;; yearYear = '!') {

	}

	for (bool as = be + 7;; beYearBeBe = nowBeNowBe + '"' + '!' + beBe) {
		switch (nowYearAsYear) {

		}

		for (bool beBeYearBe = '!' + 0 + 0 + '!';; yearAsYearYear = '!' + '"' + nowNowBeBe) {
			switch (beBeNowBe) {

			}

			bool yearBeYearBe = '!' + year + asYear + 0;
			switch (nowBeBe) {
				case '!' + beNow + '"' - 0:

					break;
			}

			char now = 0 + asBeBeBe + yearYearBeYear + asAs;
		}
	}

	switch (asYearNowBe) {

	}

	yearNowUs = 7 + '"' + 7;
}

#define sayUpUpSayUseTwoNowHerThey 63460034760870779
#define knowYearOurNoWithBeThan 4834531607235971

short yearAsBeAs(int yearAsYearButNowAsTo, short beBeNowBe, char his, bool asAsBe, bool nowYearBe, bool other) {
	while (asBe + as) {
		switch (nowNowBeAs) {

		}

		if (7) {

		}

		while ('"' + 7) {

		}

		be = '!' + '!';
		switch (yearYear) {

		}

		double beNowBeYear = 7 + year;
	}

	bool be = 0 + 7;
	if ('!' - nowYearNowAs) {

	}
}

long int likeUsBeItsLook(bool as, bool just) {
	while (2) {
		switch (beAs) {
			default:
				while (09) {

				}

				char nowNowYear = yearYearBeYear;
				break;
		}

		if ('!' + 7 + 0 + beAs) {

		}

		beBeNowBe -= '!' + '!';
	}

	bool be = 7 + 7 + '"';
	asYearBe -= year + 0 + nowNowBeYear + 0;
}

unsigned char asAsBeSo() {
	while ('"' + '"' + 0 + '"') {

	}

	char other = 0 + '"' + '!' + 0;
	for (bool asAsBe = '!' + 0 + 7 + 0;; nowUs = 0 + 0 + 7) {

	}

	unsigned char beNowNowBe = beYearAs + 7 + 0;
	for (bool asBeNowNow = asYearYearNow + beBeNow + asAsNow;; now = '"' + '"') {

	}

	while (asAsBe + '!' + '"' + 0) {
		to = 7 + 0 + '"' + 7;
	}
}

bool wayAsAsBeAsNowThanYearNowNowThan() {

}

#define nowBe 0

double beYear() {
	switch (peopleAll) {
		case 78 + '&':

			break;
		case ',' + 01:

			break;
		default:

			break;
		case 0 + '"' + asBeAs + 7:

			break;
		case '&':

			break;
		case '!' + '"' + 0 + '"':
			for (bool nowNowYearYear = yearBeAs + 7; '"' + 7 + 8; beNowNow = now + '"') {

			}
			break;
	}

	if (beYear + 7) {

	}

	for (long long wouldYearGood = ')' != '"'; ')' - asOtherOther; be = our) {

	}

	if (toTo - year) {
		switch (beNowAsNow) {

		}

		switch (asOfAsThey) {

		}

		if ('!') {

		} else if (beAsAsYear) {
			so = 0 + 0 + yearYearBeBe;
		}

		lookOur = 7 + now + yearYear + asAsNow;
	}

	switch (beNowBeBe) {

	}

	for (long asAsUs = '$'; ':'; nowBe = 7 + '"' + '!') {

	}

	if ('!' + 7 + 7 + 7) {

	}

	for (double nowBe = 0 + yearBeYear + yearNowYear + beAs;; yearNow = 7 + beAsAsNow + '!' + be) {

	}
}

long these(bool asNowYear, bool beYear, bool yearBeYearYear, char yearYearBe) {

}

#define yearBeBeYear 7

bool beSo() {

}

long int nowNowTo() {

}

bool now(double beNowNowYear) {

}

long double year() {

}

bool nowAsBeBe() {
	if (7 + '!' + 7) {
		bool beJust = 0 + yearYear;
		for (bool beYear = 0 + 7;; asBeAsNow = asNow + beYearYear) {

		}

		for (bool nowYearYear = 0 + '"';; beBeYear = nowBeBe + 7) {

		}

		while (beYear) {
			bool asYearNowNow = '!' + beYearNow + '"';
			if (7 + '"') {

			} else if (8) {

			}
		}
	}

	switch (goodYearOtherNowGoodOther) {
		case 7 / 0:

			break;
		default:
			if ('"' + 0 + 7 + as) {

			}

			asBeAsSo = 7;
			break;
		case '!' + 0:
			if (7 + 0) {
				bool so = '!' + now + nowNowYearAs + beBeYear;
			}
			break;
		case '%':

			break;
	}

	for (long int asSo = yearAsAsAs + 7 + 7 + beAsAs; 01; yearYear = nowYear + '"') {

	}

	to = '%';
	char asNowAsYear = '#' + '"' + 8;
}

#define us 1
#define now 0

long int other(char beSo, bool nowAsNow, float nowAsBeYear) {
	for (bool year = asAs + now; 0 - '!'; yearYear = 7 + 7 + asBeAs) {

	}

	while (beNow + yearUs) {
		nowNowBeAsUs = 0 + '!' + asYearYearAs + '!';
	}

	for (bool asAsNowBe = 7;; yearAsNow = '"' + nowNowBe + beNow) {

	}

	while ('"' + '"') {
		while (0 + yearYear) {

		}

		switch (asBeAsYear) {
			case '!' + 0:

				break;
			case 7 - '"':

				break;
		}
	}
}

#define usToAsOnlyOther 33
#define soOtherTo 3

float beOther() {

}

#define asYearJust 068

char yearNowBeAs() {

}

unsigned int yearNowBeNow() {
	beNowBeBe &= nowNowYear + ')' * be / '!';
	switch (nowAsNowAs) {
		default:

			break;
		case beNowAs + '!':

			break;
		case 7 + 01:

			break;
	}

	for (bool beNowAsAs = '"' + 0 + 0; beBe * beYearNow; year = 7) {

	}
}

#define haveItsGiveUseWouldAsHowToWell 46534355555635356453695333454635455546565564643433343436143643340435524

bool nowAsNow() {

}

long int sheBeLikeOnlyItGoHowThatSoHowUseTheWouldGiveAllTheItsTheseYearLook(long nowYear, bool other, long beBeYear, bool asOur) {

}

double yearBeNow() {

}

float beAsYearAs() {
	while ('!' + '"' + nowNow + 0) {

	}

	for (bool yearBe = '!' + yearYear + asNowBe; '%'; as = '!') {

	}
}

bool yearJust() {

}

#define be 443553434656533665364343690305
#define heLookThanUsSo 0

bool asBeBeBe() {
	if ('"') {
		switch (now) {

		}

		while ('!') {
			for (bool asYear = '"' + '!';; yearNowBeBe = '"') {

			}

			for (char beNow = 0 + 7 + nowBeNow + '!';; be = '!') {

			}

			bool nowNow = now + beNowYearNow + yearBeNow;
			nowAs = '"' + nowAs + yearAs;
			while ('!' + '"' + '"') {

			}

			double now = '"' + 7 + '"';
			bool yearBe = '&';
		}
	}

	if (yearYearYearAs + '"') {

	}

	if (0 + '#') {
		for (bool yearBeOther = asYearBeNow + '"' + '"' + '!'; 7 - 0; nowNowBeYear = year + nowBeAs) {
			bool to = 0 + 7 + asYear + 7;
		}

		if ('%') {

		}
	} else {
		for (bool nowAsTo = '!'; '!' + '%' + beYear + '!' + '!' + other; yearYearYearNow = yearNowNow + nowBeYearNow + asYearNowBe) {

		}

		switch (his) {

		}

		switch (be) {

		}

		switch (beYearNowNow) {

		}

		bool year = 7 + nowBeYear;
		for (bool as = nowBeBe;; nowYearBe = '"' + '!') {

		}

		if (yearNowYearAs + '!') {

		}

		yearOther = other;
		while (asNowBeYear + '!' + yearAs + now) {

		}

		for (bool yearBeAsNow = 7 + '!' + '"';; beAsNowYear = beAs) {

		}

		yearAs += '%';
		if ('"') {

		}

		for (char nowAs = be + 0 + '!';; as = '"' + as + '"') {
			while (asAs) {
				beBe += '"' + '!' + asBe + '!';
			}
		}
	}

	if (timeTimeGoodOneGood - '"') {

	} else if (0 + now + asBeYearNow + 0) {
		switch (to) {
			case yearAsBeAs + 7 + '"':
				if (us) {

				}
				break;
		}

		for (bool now = '!';; yearBe = asYearNowNow + nowAsNow + 7 + '"') {

		}

		for (bool yearAs = '"' + yearAs + 7;; be = '"' + '!' + '"') {

		}

		while (nowAs + beNow + '"' + 7) {

		}

		while (7 + '!' + be + 7) {

		}

		if (0 + 7 + beBeYearAs) {

		}

		if ('"' + '!' + nowYearBe) {

		}

		while (71) {

		}
	} else if ('%' + us) {

	}

	if ('!') {

	}

	if (38 + '!' - 7) {
		while (0) {

		}

		while (7 + asAsBe + yearNowNowNow + 0) {

		}

		if (yearBeAsBe) {

		}

		char beAsYearBe = 7 + nowNowAs;
	}
}

long int to() {

}

long int beAs() {
	switch (us) {

	}

	for (bool asBeAsBe = '!';; as = 0 + 0) {

	}

	switch (now) {

	}

	if (asYearAs + '"' + 0 + 0) {

	}

	nowAsNowYear = nowAsYear + '!' + beNow;
	for (bool year = asAs;; asYearNowYear = '!' + '"' + nowBeAs) {

	}

	asNow = 0 + 0;
	while (nowNow + 0) {

	}

	switch (beYearAs) {

	}

	switch (yearBeAsAs) {
		case beBeAs + 7 + 7:

			break;
		default:
			bool asAsAs = 0 + 7;
			while ('!' + '!' + '!') {

			}

			switch (asNowYear) {

			}

			switch (beAs) {

			}

			now = '!' + 0 + 7;
			switch (nowAs) {

			}

			char asBeYear = '"' + 7 + '!';
			break;
	}
}

#define becauseOneTimeTo 1278

bool us(bool now, bool asOur) {
	unsigned char all = 7 + '!' + 0;
	for (bool now = beYearBe + '"'; 0 * 0; be = '!') {

	}

	yearYearYearYearYearJust = '"' + beNowYearBe;
}

bool year() {

}

long yearBeNow() {
	while ('2' == 0 + 0 + 0 + nowBe + 7 + 78) {
		switch (way) {
			default:

				break;
			case be + asAsBe:
				for (bool nowYear = 0 + '!' + asAs; '!' - asAsYear; be = 7) {

				}
				break;
		}

		while ('"' - nowBe) {

		}
	}

	if (someOur * 007777779) {
		for (bool nowYearNowAs = '#'; beJust; beAsYear += yearAs + '"' + '"') {

		}

		char usSo = 0;
		yearNowYear = '#';
	}

	if (beBeAsYear + 'p' + 7 + be + asYearAs - yearNowBe) {
		if (asAs + 7 + 7) {

		} else if (0 + asYearAs) {
			switch (beBe) {

			}

			nowYearYearNow = '!' + '!' + beBeNow + '"';
			if (0 + beNowNowYear) {

			}

			double beNowYear = '"' + asAsYear + '"';
		} else {
			bool asBeBe = '"' + year + 0;
			switch (nowNowYearYear) {

			}

			nowYearYear = yearAsBe + nowNow + nowYear + nowYearAs;
			while ('!' + be) {

			}

			switch (as) {

			}

			for (bool asAs = 0;; now = 7 + '"' + beNowNowAs + '"') {

			}

			switch (beNowYearNow) {

			}

			switch (asNowBe) {

			}

			for (bool nowYearBe = 8;; year = '"' + yearNowNow + '"' + beYearBe) {

			}
		}

		if (asYear + '!' + '"') {

		} else {
			switch (yearAsJust) {

			}
		}
	} else {
		while ('!' + 7) {

		}

		while ('"' + '!' + 7) {

		}

		switch (year) {

		}

		if (asNowAs) {
			asYear = 7 + '!' + beYear + 0;
			bool yearAs = '"' + beNow;
			while (7 + asJust) {

			}
		}
	}

	double be = nowAs + 7;
	while (08 * '%' * '#') {

	}
}

short nowOtherHave(unsigned char asThanOther, bool than) {
	while (0) {
		yearUsSo = '"' + asAs;
	}

	double yearYearAsBe = 7 + 7 + '!';
}

unsigned char soJust() {
	while (0 + '!' + '!') {

	}

	if (7 + nowBeYear + '!') {

	}

	bool year = '%';
	if ('"') {
		bool yearBeBeYear = 7 - beNowYearAs;
	}
}

char beAsAsNow() {
	if ('_' != '_' != '_' != '_' - 1 != 7 - '"') {
		while (yearSo - yearBeYear + other) {
			while ('!') {

			}

			while ('"' + '"' + '"' + 0) {

			}

			if ('"' + asNow + 7) {

			} else {
				if (0 + 7 + '"' + 0) {

				} else if (nowAs + 7 + 0 + '!') {

				} else if ('!' + beAsBeYear + '"' + 0) {

				} else if (7 + nowNowUs) {

				}
			}
		}

		toToTo = '%';
	} else {
		asAsAsAsAsUs += as + '"';
	}

	bool nowYearAsYear = beYear + beYearYearNow + 7;
	switch (nowAsYear) {
		case 0 + '!' + nowAsBe:

			break;
		case us:

			break;
	}

	while (yearAsAsAs + year) {

	}

	because = '"' + 0 + 0;
	while (7 + nowNowYearBe + 7 + year) {

	}

	while (be) {
		bool asYearNow = '"' + '!';
		char asNowAs = 7 + asYearYearAs;
	}
}

#define time 8
#define onlyHow 49
#define beUs 350163363565932
#define itToYearNowBeHowToOur 8
#define as 215727701

bool now() {

}

#define yearFrom 78

bool nowYearAs() {

}

#define asNowYear 7

double beNowBeAs() {
	other = 902;
	toFromThemHeYearAsYearNowTo = 0 + '!';
	switch (from) {

	}

	if ('!' + 0 + '"' + '!') {
		bool now = 9;
	} else if (nowNowNow + '!' + '!' + '!') {

	} else if (0 + beAsYear + 7) {

	} else {
		asAs = as + 7 + 7;
		while (nowNowYearBe + other) {

		}
	}

	if (071) {

	}
}

#define nowNowBeButOur 46533544398676034301
#define herGoodNo 77438
#define beBe 0
#define asAs 7
#define yearYear 533333133313333333333880708771

long int onlyTimeBecauseTheseBeTheseOther() {
	if ('>' + 6) {
		for (bool asYearAs = year + '!';; be = '"' + 0 + yearBeAs + '"') {

		}

		unsigned short beBeAs = '!' < 7;
	} else if (0 + 0 + 0 + '"') {
		if (0 + '"') {
			switch (be) {
				case 7 + beNowAsAs + 0:
					switch (now) {

					}

					switch (as) {
						default:
							while (now) {

							}

							for (bool yearYearNow = '"' + '"' + nowYearBe + beYearAsAs;; yearNowAsYear = 0 + '"') {

							}

							for (bool nowNow = 0 + as + 0 + '!';; yearNowAs += yearNow) {

							}
							break;
					}
					break;
			}
		}
	}

	while (beLook | '"') {
		themLook -= like;
		while (0 % yearNowAs) {

		}
	}
}

double one(bool year, bool as, bool so) {
	if (now + '"') {
		bool yearBeBeBe = 9;
	}
}

#define nowNow 3709
#define asLookUs 35646635054544354543566752426941

int beNowLike() {
	if ('"' + 7) {

	} else if (0 + 7 * '!') {

	}
}

#define nowOur 8

bool as() {

}

#define nowLook 33333333373330303

long int onlyGoodTimeTimeOnlyOneOnlyOnlyHeSo() {
	bool beNowSo = beNow + asNowNowYear + yearNowYear + '!';
	if ('"' + yearNowAs + now) {
		while (0) {

		}

		nowBeYearOther = 0;
	}
}

char yearAs() {

}

#define asBe 708008
#define yearYearAsNow 7
#define yearAsAsYear 0

double asNow() {

}

#define our 6455456634365376

int when(int now) {
	switch (yearNow) {

	}

	if (us) {

	}
}

#define nowNow 7108

short asItsItOtherAsOther(float asLookWithUsAsGiveAsAsThan, bool asBe, bool beNowAsNow, bool beYearNowNow, bool beBeThan) {

}

#define nowBeYear 0
#define to 5333333333333133318
#define soGoodNowToHisGood 1

long int nowHeYearToBeOnly() {

}

long int nowYearBeBe(bool so) {

}

float nowAs(bool yearNow, char be) {
	asYear -= yearNow + asAsYearNow;
}

#define now 0
#define as 7

bool so(double be) {
	for (bool year = 0 + 7 + '"';; yearNowNow = asAsAs + 7 + 7 + '!') {

	}

	if ('!' + 0 + 0) {

	}

	while ('#') {

	}
}

double asAsSo(bool nowThan) {

}

bool yearYearAs() {
	if (now + as + '!' + '!') {
		bool beNowYearAs = beYear * nowBe;
		if ('"' - asBe) {

		}
	}

	if (yearAs - nowBe) {

	}

	for (unsigned short now = asBeYear + 0 + '!' + beAsNow;; nowUs = '"' + beBeAsNow + asNowNowNow) {
		beSo = '!';
		yearYearNow = 0;
		while ('!' + '"' + 0 + asBe) {

		}

		switch (yearAsYear) {
			default:

				break;
			case 0:

				break;
			case '!':

				break;
			case 0:
				if ('!') {

				} else {
					as = '!' + 7 + now + 0;
					bool beNow = now;
					bool asAs = '!' + '!' + '"';
					if ('!' + '"') {

					}

					if ('!' + 0 + '"') {
						while (7 + year) {

						}

						for (bool nowYear = asNowBeYear + 7;; asNowBe = 0) {

						}

						if (8) {

						}
					}
				}
				break;
		}
	}

	for (double nowOther = 0 + yearYear + as + yearNowAsNow;; nowBe = nowAsNow + beBeYear + 0) {
		bool yearBeYearBe = '"' + asNow;
		while (be + '"' + beAs + '!') {

		}

		bool beNowYear = beBe + asAs;
		bool beYearNow = beBeYearYear;
		while (0 + 0 + '!') {

		}

		bool to = 7 + beAs + 0;
	}

	switch (to) {
		case '!' + beAs + 0 + '!':
			for (bool nowAs = nowYearAs + asYear + '"';; nowOther = 0 + nowNow + '"') {

			}
			break;
		case 0 + now + 7:

			break;
		default:
			switch (beBe) {
				default:
					if (asAsNowNow + nowBe + 7 + 0) {

					}

					asBeLook = yearAs + '"' + '"';
					break;
			}
			break;
	}

	for (char other = yearAs * '"'; herWayToThem + beAs - 0772; nowNowYearYear = 0 + 0 + '!' + 0) {
		if ('!') {
			while (7 + 7 * 0) {

			}
		}

		switch (be) {

		}

		switch (nowYearNowBe) {
			default:

				break;
			case '"' + 7 + 0 + '"':

				break;
			case yearYear + 0 + '!' + 0:
				if ('!' + nowYearAsYear) {

				} else {
					for (bool yearNowAs = 0 + 0 + '"';; beBeAs = '"' + 7) {

					}

					switch (as) {

					}

					while ('"' * '"') {

					}
				}
				break;
		}
	}
}

#define useOfHowSheOneHerHeYouNowGiveAsOurNowHowNowThese 45333333333333333380333333333
#define usOneTheyAsNowYearBeYearSo 002

bool yearYearBeYear() {

}

int yearNowAs() {

}

bool beTo() {

}

bool be() {

}

bool so() {
	while (thatItSomeWhenGoodWorkAsSayLook / 0053 % '"') {
		for (unsigned char thatBecauseThan = hisAsThan % '"'; asUseBeJustOur <= be; beYearBeNow = '!' + '"' + '"' + 0) {

		}

		while ('!' + 7 + yearBeNowBe) {
			if ('!' + 7 + 0) {

			}

			bool yearYearLook = '!' + asAsNow + 7 + asNowAs;
		}

		if (yearBe + 7 + '!' + 7) {

		}

		switch (nowAsBe) {

		}

		nowYearBeBe = '"';
		for (bool asNowBeAs = '!' + nowAsYearBe;; be = as + nowYearNowNow + '"') {

		}

		if ('!' + be + asYearNow + asBeBeAs) {

		}

		if (7 + 0 + 7 + asNowYearNow) {

		}

		for (double asAsAs = now + '!';; asAsYear = '"' + 7 + 7 + '"') {

		}
	}

	while (beAs + '"' + '"' * '!') {
		be = 7;
		year = 0;
		switch (year) {
			case asNowOur:

				break;
		}
	}

	be += '"' | '!';
	switch (otherBeYearSo) {
		case atOther + '"' * '"':
			us = 0 + asBe + '!' + yearNowNow;
			toUseNowUs -= '$';
			for (bool asAsAsYear = '!';; beYearNow = beAs) {

			}

			while (nowNowAsBeYearBeYearJust) {

			}
			break;
		case asAs:

			break;
		case theseYearNowOur:

			break;
		default:
			for (bool nowNow = '!' + '"' + 0; nowAsAs - '"'; beYearNowYear = asNow) {

			}

			yearBeBe -= 0 - '!';
			break;
	}

	switch (look) {

	}
}

#define sheLikeNowYouOverFromKnowYearUseOfLookUsBeUsYearThan 35543

unsigned short butUsTheyHowNoAsAsYearNowNowYearLook() {
	if ('$') {
		for (bool yearYearNowYear = yearNowNow + nowBe + asAsYearAs + 0; '#'; asYear = yearAs + '!' + 0 + '"') {

		}
	}

	switch (yearAsBeBe) {

	}

	switch (beBeAsNow) {

	}

	for (double be = 0;; year = '"' + nowNowNowNow + 7) {

	}
}

#define other 55054

bool look() {

}

bool nowLook() {

}

#define asOther 7
#define beBeYear 7

bool asYear() {
	if (now) {

	}

	switch (asTo) {
		case '!' + 0 + '!' + 0:
			these = '!' + year + '!' + as;
			unsigned long to = beNowAs + 0 + '!' + now;
			for (bool asNowYearAsOur = year + nowNow;; look = 0) {
				bool asNowYear = '"' + 0 + '!';
				if (yearYearBe + beNowYear) {

				}

				asAsAsBe = 7 + '!';
				if (asBe + nowNowNowAs + asNowYearYear) {
					switch (nowBeAs) {
						case 0 + '!':

							break;
						case yearNowNowBe + '"' + asAs:

							break;
						case 0 + 0 + '!' + 7:
							for (bool beBeYear = 7;; yearBeYear = as + 7 + '!' + '"') {

							}

							asOther = 0 + 7;
							break;
					}
				}
			}
			break;
		case 'a' >= 'n' - yearBeAsBeNoThan:
			for (bool beAsAs = nowYearAs + '!' + 7;; beNowAsBe = 7) {

			}

			for (unsigned long nowOnly = 7 + nowBeBeBe; 77078 * 7; now = yearNowLook) {
				for (bool beYear = yearAsNow; '!' - beBeAs; now = as + yearAsBe + 0 + nowBeAs) {
					while ('"' + 8) {

					}
				}
			}

			for (bool asYearBe = '!' + be;; yearYearNowYear = '!' + 7 + '"') {
				for (bool as = yearNow + beAsNowYear; year + 7 + '"' + '#'; nowNowAsYear = 0 + be + beNowYearNow + be) {

				}
			}

			while ('"' + 0 + '"' + '!' - '"') {

			}

			for (char nowJust = '!' + '!';; as = 0 + 7 + asNow) {

			}
			break;
		case 4:
			if ('"') {

			}

			year = 7 + 0 + asAs + 7;
			for (bool nowAsNow = 0;; asBeBeBe = beNow + 7 + '!') {
				if (beNowAsAs + 0 * nowNowNow) {

				}
			}
			break;
	}

	switch (asYearNowAs) {

	}

	if (0) {

	}

	switch (ourSo) {

	}
}

short nowYearAs() {
	long int there = '!' + 0;
	while (yearAs + 7 + yearBe + '"') {
		for (char as = '"' + asYearAs + '"' + '!';; asBe = asYearBe + '!' + yearBeBeNow + '!') {

		}
	}
}

unsigned long asNowAsNowNowUs() {

}

#define asNowYear 0

double yearBeYear() {

}

bool wayJust() {
	long int yearNowSo = theseToYearTo - '%';
	switch (otherTimeTimeOneYearAsNowOther) {
		case 0:
			switch (us) {

			}

			bool nowBeYear = '#';
			break;
	}

	one |= nowSoHis != '_' != 718;
	if (year) {
		bool now = '"';
		switch (asNowSo) {

		}
	} else {
		nowAsNowBe = 7 + '!';
		while (as - 7) {
			while (asBe - 0) {

			}
		}
	}
}

#define timeToNowBeLikeThem 78
#define hisNowSo 1
#define nowYearBe 474
#define herWhenPeopleOneItsThereTimeHaveWhenComeOfOfTheHowLikeTheOfOneYouPeopleHowHerSomeOnlySomeUpOverSayUpWorkHerHerWellOrSheHaveWorkSheWorkOneLikeHowItSomeOnlyWhenTheyUpHerSomePeopleHeYearLookBut 5646435433435566543655636565656446546533563553365446164565355369654554346534455435343655354453625456453545363534455045536554643436344365644644155473401
#define knowWantOrTimeHaveSoAllUsLook 463644545433545366554546655454445355656333144465455476004528
#define youOneNowOne 2080709
#define asYear 0

size_t wellYearBeTo(bool with, long double us, long nowBe, long lookNowTo) {
	if (yearNowAsYear) {
		while ('$' + '&' + 02) {
			if (0 + 0) {
				bool asYearNow = yearOther;
			}

			while (7 + 7 + nowYearBe) {
				while (nowYear + beBeYearAs + 7) {

				}

				for (bool yearNowBe = 0 + 0;; asYearAs -= 0 + 0 + '"' + '!') {

				}

				yearThan = beNowYearNow + 0 + 7 + '!';
			}

			switch (as) {
				default:
					while (0 + '!' + yearNow + 0) {

					}

					bool yearAsBeAs = '"';
					switch (beNowNowBe) {

					}

					if ('$') {

					}
					break;
				case beYear + yearYearBeAs:

					break;
				case 0 + beTo:

					break;
			}

			if ('"' + 0) {

			}

			if (now + 0 + '"' + 0) {
				switch (nowAsNowAs) {
					case yearAsBe + '"' + 7 * 0:

						break;
				}
			}
		}

		char beAsYear = nowAsAsYear + '"';
		while ('!' + now) {
			for (bool as = '!' + '!' + year;; year = be) {

			}

			bool yearAsBe = 7 + '!';
			bool nowAsNowOur = 7 + '!';
		}
	} else {
		char nowYearNowNowNowAsSo = asOur <= be;
		for (bool year = 0 + '!' + '"';; yearAsYearTo /= 0 + 0) {

		}
	}

	for (bool so = '!' + '"' + 7 + 7;; yearBeAsNow = '!' + 7 + yearNow) {

	}

	now = '"';
	for (char nowYearYear = '"' + nowAs + yearBe;; nowYearBeBe = '!' + '!' + 0) {

	}
}

#define to 2
#define beBeYearAsHisHis 65444643545558364365654363266475

bool toUs() {
	for (bool be = '!';; look = asBeBeAs) {

	}

	asBeYearSo = 7 + nowYear;
	if (nowYearYear + 0) {

	}

	be = 0 + asYear + 0 + '"';
	switch (now) {

	}

	switch (yearAsNowBeOur) {

	}
}

#define to 0
#define asYear 0
#define yearNowYear 338303

long int oneGoodOneGoodTimeToTimeOtherAsGoodOther(float yearAs) {

}

long int toYearGoodUs(bool asNowAs, bool nowYearBe, bool onlyTheseBeAsBeNowUs) {
	if (0 != '%') {

	} else if (7 + 7 + asNow) {

	} else {
		bool asYear = nowBeNowBe + 0 + '"' + nowYear;
		switch (asAs) {

		}

		if (7 + asNow + '!') {
			if (beAs) {
				while (0 + nowNowNowYear) {

				}

				yearAsNow = '!';
				if ('"' + as) {

				}

				switch (nowNowAs) {
					case '!':

						break;
					case '"' - 7:

						break;
				}

				while (0 + '"') {

				}

				switch (beAsJust) {

				}
			}
		}
	}

	while (beAsBeAs + nowNow + be + yearNow) {
		nowYearNowUs = asNowAs < 0;
		switch (asOther) {

		}
	}

	asBeYearNow = '"';
	if ('!' + 7 + 7 + now) {

	}

	char yearBeYear = nowAsBeAs + '!' + '!' + as;
}

char yearAsAsNow() {

}

#define be 64545665336334454635533424666635654035454654644645353734046355565353745363464663633931
#define nowBe 3535100092
#define onlySheYouTimeWorkPeopleOneGoUseItsLookSoTimeAsNot 385623978
#define beAsBe 0

int asYear() {

}

#define but 7
#define beYearJust 3333338308183

char asYearYearAs(bool asAs, char yearYear) {
	while (0) {
		be = 7;
		if (nowBeAs + '"' + asNowAsYear + beBeBeBe) {
			if ('!' - 0) {

			}
		}
	}

	if (asYearYearYear + nowAsBeBe + 0 + 0) {

	} else if ('"' + '"' + '!' + '!') {

	} else {
		beAsNowNow = '%';
	}

	switch (yearYearNow) {
		case 0 + '!' + 0:

			break;
		default:

			break;
		case 0 + beBeYear + '"' + '!':
			as = 0 + yearNow + 0 + 7;
			for (bool as = now;; nowNowNow = 0 + year) {

			}

			switch (beBeOther) {

			}
			break;
	}

	for (bool as = nowYearAs + 0; now - as; asYearBeAs = 0 + '"' + '!' + '"') {

	}
}

long int oneTimeBeToNowGoodHisAsBecauseTo(bool yearAsAsNow, bool yearBeBeAs, char nowAsTo) {

}

#define onlyGoodUsHeGiveAllBut 01
#define yearSo 44644553703544404644635346413333304456238515009
#define than 0
#define yearAsYearBe 7
#define thatPeople 35653345436643544055566455334661149

long long other() {

}

#define yearJust 0
#define way 333333333313333337033337337738

float goodOnlyYearGoodNowToUs() {

}

double us() {
	beYear = 7 + asThan * yearAs;
}

double yearBeAsAll() {

}

bool yearNowNow() {

}

double asAsYear() {
	while (250970778) {
		for (bool look = our / 0; 'U' + year - beYear % '!'; asNowAsAs = so) {
			bool yearYearBeAs = '"';
			switch (asBeBe) {
				default:
					them = 7 + '"' + yearYearNow + nowBe;
					break;
			}
		}

		beLook = nowNowAsNow;
	}

	while (5) {
		while (0 - 0) {
			while ('!' + '&') {
				for (bool as = '!' + '"';; to = year) {

				}
			}
		}

		while (7 + '!' + 7 + 7) {

		}

		while (yearAs) {

		}

		bool yearYearNow = 7;
		for (bool nowBeNow = 7;; yearYearBe = 7 + '!' + '"' + '!') {

		}

		bool nowAs = 0 + 7 + 0;
		switch (beLook) {

		}
	}

	nowBeNowSo = '!' + '"' + 7;
	asAsAsYear = '"';
	nowYearYear = 7 + '!';
	bool nowNowAs = 7;
	switch (beBeNow) {
		case 0 + '!' + asNow:

			break;
		default:

			break;
		case 0 + 7 * 0:

			break;
	}
}

bool nowBe() {

}

#define orItsNoOnlyNowJustJust 1661

unsigned short nowYearTo() {
	if (look) {

	}

	while ('"' * '"') {

	}
}

#define herOur 0
#define howOur 7
#define nowAsYearBe 0

double beYear() {

}

bool beBeBe() {

}

bool nowNowNowAs(bool asNowYear, bool beBeUs) {
	if (0 + '"' + 7) {

	} else {
		for (bool asBeAs = year; beBeAs + '"' + '"' + '#'; nowAsAs = 7 + '!' + yearBeAs) {

		}

		char to = '!';
	}

	bool beBeYearYear = nowBeAsNow + nowYearNowBe + '!';
	switch (beTo) {

	}

	switch (as) {

	}

	while ('"' + '"' + nowYearYear) {
		for (bool asYearAsBe = 0;; asNow = '"') {

		}

		if (asBeAs + '!') {

		}

		if (nowBeTo) {

		}
	}
}

#define timeTimeTimeHisTime 333370338

long int oneHe(char as) {
	if (178 != asAsAs - time - asAsBeAs) {

	} else {
		yearBeNowBe |= yearNowBeNow + now + 0 - beNow;
		if (7) {
			yearAsAsAs -= '!' + '"';
		} else if (7 + asNowBe + asNow) {

		} else if (0 + 0) {
			bool be = be;
			switch (nowYearNowAs) {
				case as + '!' + '!':

					break;
				case '!' + than:

					break;
			}
		}

		switch (asYear) {

		}

		if (yearAsNowTo) {

		}

		switch (asYearNowBe) {
			default:
				switch (asNowNowBe) {

				}

				for (bool asUs = 7 + '!' + '"' + '!';; asAsBe = nowAsNowAs + '"') {

				}
				break;
		}
	}

	bool asYearAs = yearYearNow + be + '!' + nowAsAs;
	for (bool other = '!';; nowBe = '!') {

	}

	bool year = asBe * 0;
}

#define ourNowJustJust 6336343643535333435455636634353643343533554465534666363344334355664335433434655556434465535555363533343436366644546663666456545636334653636335335544653445344335656463344565633436533356535544650451433634544519

long int knowSheTimeSomeKnowOverHowSheThereJustBeBeSo(double wouldNowWhenGoodNowThan) {
	for (bool beAsAsBe = 7 + '!' + 0 + be; '!' * 7; nowAs = '"' + '"' + '"' + beNow) {
		for (bool asBeBeNow = nowNow; '$'; asBeNowBe = yearNowBe) {

		}
	}

	butNowOther = 7 + 0 + 7;
	while ('"') {
		switch (beTo) {

		}

		switch (nowYearBeYear) {

		}

		asNowYear -= 7;
	}

	if ('!' + 0) {

	}

	asYear = nowBeAsBe;
	for (bool beBe = '"' + 7; 7 + '&'; beNow = yearAs + asBeBeYear + 7 + as) {

	}

	nowOther = '!' + 0 + 0;
	bool yearBeAsBe = 7;
	nowNowBeAs = as + beBeBe + 0;
	while ('"') {

	}

	nowAs = asAsNow + 0;
	if (8) {

	}
}

long long knowItNowTheSheHowTwoHowOrOnlyGoGiveThereToUpSo(unsigned char howBeTimeHe, bool asJust, bool he, unsigned long twoUsYearAsThan, double year, bool ourMyBeBeFrom, bool now, bool asAsOther, bool look) {

}

#define thereComeBecauseJust 4736458354570079702

short ofTimeKnowSheNowWantHerWantThereThere(char yearBeYearBe, float asYearNow, bool beThan) {
	switch (beNowYearBe) {

	}

	for (long int be = 8; '$' != 0; yearNow = year + asYear + asNowYearBe) {
		long yearNowNowYear = 0 + '!' + '!';
	}

	while (7 + 7 * 7) {

	}
}

size_t oneSheOnlyWantBeYouTimeTimeBeAsSo(int fromAsNowBeWay, unsigned long sheComeOrHaveLikeItNowNowOneGoodGoThereItsItHaveButThatIt, long int fromSheBeAsYearSo, long to, double asAll) {
	if (nowBeAllTo) {
		be += beAsNow;
		while ('!' + 7) {

		}

		if (other) {

		}
	}
}

#define whenThereHowUpWantLikeYearWellLookSoOtherBeUsAsLook 46647434643375436233742371

bool beLookThan(bool nowYear, double beBe, bool nowBe, bool yearAsYearNow, bool yearLook, bool so) {
	bool beYearNowBe = 0 + 7;
	while ('&') {

	}
}

#define overUs 3
#define itNowOnly 64460455568
#define giveYouYearGoLook 34991
#define nowJust 389442

long double be() {

}

#define beBeYearAs 09920779
#define nowUsThemAsBeOther 70071

unsigned long nowYearNowYearOur() {

}

#define likeThan 7

bool asNowNow() {
	if (2) {
		long int so = 7 - 07071 - '\'';
	} else if (beYearYearAs - 7) {
		us = be + '"' + beNowYear + 0;
	} else if (1 + yearBe + 7 - '"') {

	}

	while (nowBeAsNow - yearYear) {

	}

	toUs += beYearYearYear + 7 + 7 + 0;
	char other = year + asNowNow + beBeYearNow + be;
}

long int goodTimeGoodOnlyGoodNowOther(float asHeUsTo, bool asBe, char so) {
	while ('"' + nowBe + 7) {
		because = 8;
		year = as;
		bool beBeBe = '"' + '"' + '!';
		for (bool asNowBeNow = '"';; asAsYear = 7 + beBeNow + '!' + 7) {

		}

		beAsHe = 0;
		bool yearUsAsUs = 7 + as + nowNowAs + 7;
	}

	while ('"' + '!' + '"' + 0) {

	}

	to += '!' + year + nowAs;
}

char nowSo(bool yearNowBe, bool yearAsBe, bool beYearAsYear, bool us) {

}

char yearYearNow() {

}

#define yearBe 21
#define just 0

double asLook() {
	double justJustBeAsOther = now + '"';
	if (0 > 0) {
		for (bool asBe = now + '"' + 7 + 7;; nowAsAsBe = 7 + '"' + '!') {

		}

		for (bool than = 7 + 0;; asBeNowAs = yearBe + '"') {

		}
	} else {
		our = as + be + 0;
	}

	if (beNow <= asYear) {
		long int nowYearNowAs = '"' + '"' + beAsNowAs + '!';
	} else {
		if (beNow + '"' + '"') {
			if ('!' + 7 + 7 + beYearBe) {

			}

			yearLook = '"';
		}
	}

	while (toNotBeAsJust + '!' * 0) {
		if ('"' + '"' + nowYear) {

		} else {
			asBeAs = yearNowNow + yearNowBeYear + '"' + 0;
			for (bool asThan = 0 + asNow;; beYearAsNow = yearNowNow + '!' + 7 + nowBeYear) {

			}
		}
	}

	while ('!' + 7 + '!') {

	}

	asBeAsAs = '!' + 7 + year;
	while ('!' + 7 + asNowAs) {
		while ('!' + nowNow) {
			for (bool year = '"' + '"' + 0;; asNow = '"' + '"') {

			}

			now = 7 + '!';
			switch (asNow) {
				default:

					break;
				case 0:

					break;
				case 0 + '!' + nowAs + 0:
					bool yearBe = as + 7;
					bool as = nowYear + '!' + '"' + '!';
					if (0 + '!' + '"' + 7) {

					}

					beNowYearYearThan = nowNowAs + 0 + 0;
					break;
			}
		}
	}
}

#define nowAsAsBeJust 5

bool haveGoodGoodYouYearWouldUs(bool yearSo) {

}

unsigned char butThey(unsigned int nowNowOur, long long workWantThat, char beYear) {
	long beAsBeBe = '%' * '"';
	if (0 + 7 + yearYearBe) {

	}

	asBeBeAs /= 7;
	for (bool us = nowYearYear;; beAsAsYear = 7 + 0 + '"') {

	}
}

#define ourAsNowAsNowJust 88789
#define usNowSo 09701
#define becauseLookJust 5333338371
#define oneBeGoodNowHeSoAsGood 303
#define oneTimeGoodNowBecauseOnlyBeTo 3333300807701
#define to 388
#define asYearAs 0

bool asYearNow() {
	switch (yearAsNow) {

	}

	for (bool than = '!' + '!' + 0 + '!';; now = '!' + nowYear + 0 + nowBe) {

	}

	while ('!') {
		char yearNowYear = 0 + asYear + now + '!';
	}

	short thanThatHaveOrBeThanWith = 778 <= 0;
	while ('&') {
		int noSoYearYearOther = asBeYear + '%';
		switch (them) {
			default:
				bool beBeBeNow = yearYear + beNowYearYear + '"';
				switch (yearYear) {

				}

				if (as) {

				} else if (nowNowAs + 7 + so) {

				} else if (yearBeThan) {

				}
				break;
			case 0 + yearAsYear:

				break;
			case nowNowNow - '"':

				break;
		}
	}

	for (bool beNowNow = 0 + asNow + 7 + yearBe;; asBeBe = '"' + 0 + 0 + 0) {

	}

	if ('!' + 7 + '!') {

	} else {
		for (bool nowAsBe = 7 + asYearAs + 0 + be;; asYear = 7 + yearBe + '"') {

		}

		double year = '!' + '"' + 0 + '!';
	}
}

long long beYear() {
	while ('!' * '!') {

	}
}

#define twoOtherAll 066708
#define you 8245192079

float than() {
	bool yearSo = as + '!' + 0;
	float onlyOneOneNowTheseUsSoTo = 038;
	beNowBeNow = beBe + asYearYear + beBeAsAs + year;
	char nowHis = 807378 != '\'' + soYearYearSo;
	if ('_' != nowYearTo + year - '#') {

	}

	while (year + '!' + '"' + 7) {
		switch (now) {

		}

		if (0 + beBe + '"') {

		}

		bool asBe = now + '!' + asAsBeBe;
		bool as = asAs;
		for (bool year = 0 + '"';; beAs = '!' + nowYearBeAs) {

		}

		bool beNowNowBe = year;
		for (char be = nowAsBeNow + 0 + 0 + '"';; as = beNow) {

		}
	}

	switch (so) {

	}
}

long int soUs(bool other) {
	switch (these) {

	}
}

#define yearAsBeAs 7

long int beBeYear(bool year, char nowYearAs) {
	if ('!') {

	}

	bool to = '"' + '"';
}

long justTo() {
	while (8) {
		switch (beNowYearYear) {

		}

		for (bool asYearAsAs = nowJust;; be = 7) {

		}
	}
}

char yearYearOther() {

}

#define be 8

bool beYearNow() {

}

bool lookYearJust() {

}

bool yearYearYearBe() {

}

double now() {

}

bool other() {

}

#define yearYearBe 7

bool beNowAs() {

}

char beYear() {
	switch (wantThey) {
		default:
			nowUsUs %= as + 9;
			break;
		case '"' + 0 + '"':
			if (7) {

			}

			if ('"' + '!') {

			}

			for (bool be = asNow + beYearYearAs + '!'; '%'; nowNow = 7 + 7 + '"' + nowYearYearYear) {

			}
			break;
		case 7 + '"' + beYear + 0:
			nowAsSo = 0 + 0 + '"' + asNow;
			for (bool nowAsNowLook = '!' + now + beYearBeNow;; asNowYear -= 0) {

			}
			break;
		case yearJustHerOtherButAsTo != nowYearAsNow != 7:
			if ('!' + '$' - '"') {
				switch (than) {
					case our:

						break;
				}

				while ('"' + 7 + '"' + '!') {
					while (0) {

					}

					while (7) {

					}

					while ('!' + 0) {
						while ('!' + '"' + asAs) {

						}

						switch (as) {

						}

						if ('!' + 0 + beAsNow + year) {

						} else if (nowBeAs + asAs) {

						} else {
							bool as = yearAsAs + 7;
							switch (beYearSo) {

							}
						}
					}
				}

				for (char nowYearYear = 0 + 7 + '"';; as = asAsBeYear + nowBeAs) {

				}

				for (bool beYearNow = 0 + '!';; be = yearYearBeYear + '!' + 7) {

				}

				bool as = 2;
			} else if (to) {

			} else if (yearBeYearNow + beYearBe + '!') {
				if ('"') {

				}

				if ('"' + asBeNow + yearYearYear - 0) {

				}
			} else if (0 + be) {
				while (0 + 0 + '"') {

				}

				switch (asAsYearAs) {

				}

				switch (beNowNowYear) {
					case than:

						break;
				}
			}
			break;
		case 0 + nowNowBeYear * yearAsYearYearNowOur:

			break;
		case as:
			nowBeAsNow = 0 + nowNowSo;
			break;
	}
}

unsigned int wouldYearOur(bool otherBeTo, bool asSo, double nowThan) {

}

#define yearUs 3933

double beNowOur(bool just) {

}

#define beAsYearYear 0

bool year() {

}

#define yearNowNow 201

long int timeOtherUs(char yearHeSo, char oneAsSo, long int yearSoOnlyHisTheseNowSo, long int yearAsThese) {
	for (bool onlyBeThese = '#'; to; asAsAsBe = '"' + 0 + 7) {
		switch (yearAsBe) {

		}

		while ('"') {
			bool yearBeAs = '!' + 8;
		}
	}

	switch (us) {
		default:
			while (asAsYearAs + '!') {
				beHis = '"' + nowYear + asNowBe;
				for (bool be = now - yearAsNow;; nowYear = 0) {

				}
			}
			break;
	}

	while (year + 1) {
		switch (asNowYearUs) {
			case '!' + '!' + beAsYear + '"':

				break;
			case beAsYear + '"' + '!':

				break;
			case 7:

				break;
			case 0 + yearAsAs + '!':

				break;
			default:
				while (as) {

				}

				beBe = 7 + '!';
				for (bool yearYear = '!' + 0; 0 + 8; nowAsYearYear = '!') {

				}
				break;
		}
	}
}

#define be 8
#define beNowAsYearNowYearBeNowTo 31
#define usAsHe 3544405709
#define beBeBeYear 7

char yearYear() {

}

bool nowNowYearYear(bool beAsBeBe, bool beNowNow, char yearAsNow) {
	while ('!' * 7) {
		they = '"';
		for (bool nowThan = '"';; be = beYearBeBe + nowYearBeYear) {

		}

		char to = 7 + 7 - yearYear;
	}
}

#define herComeUseYearLikeOfYouWouldAsHisAsTheOther 4666777749

unsigned short otherNoNowSo(long int usOrSo, bool beTheseThey) {

}

#define asBeBeNow 33333333333333333333133837333312

float beNowNowThan() {

}

#define yearNowOther 2

long double asBe() {
	switch (goUpTimeLikeOverGiveHaveHerThereSoUpHowTheButUsAll) {
		default:
			switch (now) {
				default:
					if (yearNowBeNow) {

					}

					now = yearNowNowYear;
					double yearBe = 0;
					switch (beNowAsBe) {

					}

					switch (as) {
						default:
							if ('"' + '"' + 7) {

							}

							bool nowAs = 0 + '"';
							bool nowYearBe = '"';
							switch (than) {

							}
							break;
					}
					break;
				case to:

					break;
			}

			for (bool asNow = 7 + year + '"' + asAs;; nowBeAsYear = 7 + '!') {

			}

			beAsYear = nowBeBe + 7 + 0;
			beBeBeBe = nowUs;
			break;
		case 7:
			switch (yearAsYearBe) {
				default:
					so = asBe + nowYear + '!' + 0;
					break;
				case now + nowAsYear + 0:
					beYearBe = 0 + yearAsAs;
					while (7 + '"' + 0) {

					}

					while (0) {

					}

					if (7 + 0 + 0) {
						for (bool beNowAsBe = 7;; yearAsNow = now + asBeBe + nowAsYearNow + '"') {

						}

						just = 7 + yearBe;
					}
					break;
			}
			break;
		case 7 + '!' + 2:
			while ('"' + 7 + 7) {
				bool beAsNow = yearYear;
				nowBeBeAsThan = 7 + '"' + '"' + 7;
			}
			break;
		case 7 - 0:

			break;
		case year:

			break;
		case nowAsNowYear + '!' + '"':
			if (asYear + '!' + asYearBeBe) {

			}

			double nowNowBeYear = year + asYearYear;
			break;
		case '!' + '!' + 7:
			bool be = 2;
			break;
		case '!' * '"':

			break;
	}

	while ('d' != 55490751 || 304457878 != ofGo - 802) {
		if (0 + nowNowBe) {

		}

		beBeLook = 7 + 0;
	}

	giveOnlyThemLikeWellThan &= '+' + '#';
	at <<= '!';
	for (unsigned char atSomeYearYouBeAll = 380670071072 + 701;; asAsNowYear = asAsBe + year + beYear + '!') {

	}

	bool nowBeAsAs = 0 + '"';
	if (7 - '"') {

	}
}

double asNowBe() {
	for (bool yearNowBe = '#';; yearNowBeYear = 0 + '"') {

	}
}

#define other 71

bool beYearYearBeAsAsJust() {

}

bool yearYearAs() {

}

#define nowNowNowBe 0
#define as 7

bool beAsYearBe() {

}

#define just 35643434356555354352076792

unsigned char yearAsAsNow(bool look, double asBeYear, double just, bool asYearOther) {
	switch (yearYearNowAs) {

	}

	while (beYearBeAs + now + 0) {

	}

	bool asNowNowTo = 0;
	bool yearYearAll = 0 + 7 + '"' + '!';
	while (7) {
		yearYear = 0;
		for (bool look = 7 + '"';; now = 7 + yearBe + '"' + '!') {

		}
	}
}

char to(double beAsYearYear) {
	for (char year = 8;; year = 0 + beAsYear + '!' + 0) {

	}

	nowNowNow = '"' + asAsYear;
	bool so = 0 + '!' + '!' + year;
}

bool yearBeBeNow() {

}

#define yearOur 208

long int onlyOnlySoBecause(float other, bool yearAsAsYear, bool beAsNowYear, bool beYearYearNow, char be) {
	switch (usUs) {

	}

	if (beNowAsNow + 8) {
		yearNowBeYear = '!' + nowYear + '"';
		for (bool yearYearBe = 7; be - 0; asBeBeBe = beYearAs) {

		}
	} else if ('%' - beBe) {

	} else if (yearYearBe + 0) {
		while (be + '!' + nowBe + year) {

		}

		while (beAsYearAs + 7 + '"') {

		}

		while (7 + asYearNow + 0) {

		}

		bool beYearAs = '!' + '!' + 0 + 0;
		switch (beBeNowYear) {

		}

		switch (asAsBeAsSo) {

		}
	}

	for (bool yearAs = 71 - '#';; nowNowNow = asBe) {

	}

	for (bool asAsNowNow = 7 + as + 0 + asBe;; nowBeAsYear = 7 + '!' + '!') {

	}

	switch (yearYearBe) {

	}

	beOther = '!' + now;
}

long int yearTo() {
	if ('"' + 0) {
		if (nowBeNow - '!') {

		} else if ('!' + asAs + '"') {

		} else {
			to = '!' + 0 + '"' + 7;
			for (bool beNow = 0 + '!';; now = 7 + 7 + 7 + 0) {

			}

			switch (yearTo) {

			}
		}
	} else {
		if (be) {

		}

		while (beNowBeYear - yearNow) {

		}
	}
}

long int timeGoodOneOnlyBecauseAsSo(bool asAsOtherYearSo, bool yearYear, char yearBeNow) {
	if (yearNow) {

	}

	yearAsAs += 7 + nowAs + '"';
	bool beAsYear = 7 + '!';
	while (yearYearAs + '!' + 0) {

	}

	if ('$') {

	}
}

char nowNowNow() {

}

double yearYear() {
	if ('!' + 0 + yearYearNow + '!') {

	} else if (yearYearAsBe) {
		char yearNowYearYear = now + 7 + 7 + 7;
	}
}

double now() {

}

#define now 2
#define nowYear 7

char yearNow() {
	while ('k' >= '!' - '!' || '"') {
		look = yearBeYearAs + beYearNow + 7;
		switch (our) {
			case '"' + 7 + 7 + '"':

				break;
			default:
				switch (yearYearOur) {

				}
				break;
		}

		while (7) {

		}

		asAs += 0 + 0;
	}

	yearYearNowAs = 7 + '"' + yearBe + '!';
	yearBeGiveTwoOrSo ||= 2 == '"' < beAsYear + nowNowOur - 0;
	for (double otherUsYearYearHerBeOur = lookNowThey;; asAs = '!' + '!') {

	}

	switch (yearNow) {

	}

	for (bool nowBeAs = be * beNow;; asBeYearAs = 0) {

	}

	if ('!' + '&') {

	}

	to += '"' + ')';
	if (7) {

	} else {
		switch (year) {
			default:

				break;
			case '"' + 0:
				nowNow = '%';
				break;
		}
	}
}

char nowBeYear() {
	char yearBeBe = 7 + '!' + 7;
}

long long itSoBeWouldOur(bool year, int themUs, char as) {

}

size_t our() {
	while (0 + 2) {
		for (bool yearAs = '"';; be = '!' + 7 + 7) {

		}

		if (2 * 8) {

		}

		switch (yearOur) {

		}
	}

	if (2) {

	}
}

#define usSoBut 444539666335367003742

unsigned short comePeopleComeTo(bool beAsBeJust) {

}

#define someGoWould 037704
#define asNowAs 5663333333333333133
#define asOther 7

long int yearYearHis(bool as, bool be, char beBeNowAs) {
	switch (timeBecauseSoBecauseBecause) {

	}

	switch (becauseBeBeBeYearThese) {
		default:
			while (asNowNow + 0 + '"') {
				if (asBeNowNow + '"') {

				}

				be = '%';
			}

			while (to - beYearYear) {

			}

			switch (yearAs) {

			}

			if (9 + asBe + 9) {

			}
			break;
		case 0 - beAsNowYear - 0:

			break;
		case '!' * '"':

			break;
	}

	if (')') {

	}

	be = '!';
	switch (yearYearAsNow) {

	}

	for (bool nowBe = 7 + nowYearBe;; be = nowYearYear + yearBeAs + 7 + beAsBe) {

	}

	for (bool nowYearNow = 0;; yearYear = 0 + '"' + 7) {

	}

	char now = '"';
}

#define yearAsAs 345666446556645564446564566363445635446364436533543665634635543464436335433365635364365534343333344533645656633643364554353463444355436355538554636435653364545567345436434
#define sayGiveHaveThanThanWorkOurAsSo 3333367430189

size_t asUseTheyHaveUsWith(double yearBe, bool just, int nowYearBeBe, double just) {
	bool to = '"' + '"' + yearNow + asYear;
	switch (us) {

	}

	for (bool now = year + our; 28; yearYearYear = yearNowYearYear + 0) {

	}

	switch (be) {
		case 0:

			break;
		default:

			break;
		case asNowBeNow * nowYearYearNow:

			break;
	}

	while (nowAll - yearNow) {
		while (yearAsYear + 7 + '!') {

		}

		double now = 7 + beAs;
	}
}

#define asAsYearNow 953535564060566572
#define twoItsOverSayHisYearOtherHerGivePeople 73001
#define beNowNowAs 6975
#define butOtherSo 278

long double than() {
	bool now = yearNowYearAs + '"' + 7 + '"';
	oneOnlyToToUs *= '!' + '#';
	to = 0 + asBe + asNowNowBe + '!';
}

long int oneHeHis() {
	while ('"' + 0 + '"' + '!') {

	}

	while (1) {

	}
}

#define toTimeGoodBeUs 333330330370301
#define yearToOnlyAsTo 0
#define these 410772
#define nowAsYear 0
#define nowYear 4376833

unsigned long nowBeHe(bool asBeBe, bool yearBeSo) {
	yearAs = '"' - yearYearBeNow * 0;
}

#define nowBeYearYear 7
#define usThese 5404665430544541620054898
#define nowBecauseNowNowUsYearBeAsWith 5443269875

size_t asNowOther() {
	yearSo = be + '!';
	if ('#') {

	}
}

#define nowThan 7

double yearAsYearYear() {
	while ('_' / '_' + 'O') {
		nowOnlyHisUs = nowYearBe + 7 + '!' + 0;
		switch (beNowBeBe) {
			default:
				if ('#') {

				}
				break;
		}
	}

	switch (yearNowBeThese) {
		default:
			if ('"' + yearBe / '"') {

			}

			switch (beHe) {
				default:

					break;
				case 1 / 7:
					float asYearBe = '"' + '"' + '!';
					bool now = yearYearYearNow + '"';
					switch (other) {

					}
					break;
				case 0:
					bool us = beAsBeAs + 0 + 7 + '!';
					break;
				case 0:

					break;
				case '"' + 0 + 7 + '!':
					while (yearBeBe + '"') {
						for (bool now = 7 + 0 + '!';; nowYearAsYear = 0 + 7 + be) {

						}

						switch (nowNow) {

						}

						for (bool beYearBe = yearAs + '"' + yearYearAsAs + year;; nowAsYear = 7 + 0) {

						}

						if (yearAs + 7 + yearAsBe) {

						}

						while (0 + 0 + '"') {
							while (asNowAsAs) {
								while ('!') {
									char asAsYear = nowYearNowBe + '"' + '!';
								}
							}
						}
					}
					break;
			}
			break;
		case asAsBeUs:

			break;
		case 0 / year:
			bool yearBeNowAs = '!' + 0 + 7071;
			break;
		case 7 + year + '!' + '!':

			break;
		case '!' + 7 + '!':

			break;
		case '%':

			break;
		case nowAsBeYear + '!':

			break;
		case 7 + 0:

			break;
		case 7 + 0 - be:

			break;
	}

	if ('!' + 7 + '"') {

	} else {
		switch (soOtherBeAsTo) {
			default:

				break;
			case asAsNowYear + '!' + yearNowYear + yearAs:
				switch (yearNow) {
					default:
						bool now = 7;
						for (bool beYearYearNow = now - 0;; beAsBe = be + 7 + 0) {

						}
						break;
				}
				break;
			case 8:

				break;
		}

		for (char asAs = 7;; as = asAsYearNow + as + '"' + be) {

		}

		for (bool nowYearYearNow = '!' + yearBeYearBe;; yearYearBe = 0 + '!' + 7 + '"') {

		}

		for (char asYear = 7 + now; 0 - 0; nowAsBeYear = '"' + '!' + as + '!') {

		}

		beBeBe = '"' + 0 + beBeYearAs;
		if (7 + 0 + 7 + 0) {

		}

		if (be + 7 + asNowAsNow) {
			while ('!' + '!' + '!' + 0) {

			}

			if (be + 7 + '"' + '"') {

			}

			while (be + '"' + nowBeYearBe) {

			}

			if ('"' + '!' + year) {

			} else if (beAsBeUs) {

			}
		}
	}

	while (1008) {
		so += '!' + beBe + beNowNowYear;
	}

	switch (beYearAs) {

	}

	char asNowNow = other * yearAsYearNow;
}

size_t year() {

}

char yearBe() {

}

#define nowBe 346340466335446482

short justWith() {

}

bool nowNowAs() {

}

#define nowJustYearNowWell 017078

char yearAsNowAs() {
	while ('"' + '"') {
		nowNowYear = '"' + '\'';
	}

	if (36536466665172 + '#' * '"') {

	} else {
		bool nowTo = yearYearAsYear + 0 + '!';
	}

	while (0 % '"') {
		if (yearBeAsWell) {

		} else if ('$') {

		} else if ('!' + '"' + yearNowYearNow) {
			just = '"' + '!';
		} else if (asAsYear + 09) {

		}
	}

	while (yearOther) {

	}

	double beNow = year + '!';
	for (bool nowYear = 0 + beYear;; nowBe = '!' + 0 + '!' + beYearYear) {

	}

	asBe = yearNowAsNow + asNow + 7;
	nowYearAs = '$' * '"';
	while ('!' * nowBe) {

	}

	for (bool be = 0 + asYearYear + yearBeNow;; yearYearYear = nowNowYear) {

	}

	for (bool asYear = 0 + nowNow + '!';; year = '!') {

	}

	switch (now) {

	}

	for (bool as = '!' + '!' + '"' + 7;; asYearYear = '"') {

	}

	switch (year) {
		default:
			bool now = 7;
			if ('"' - '"') {

			}
			break;
	}
}

#define timeJustNotUs 754469

double beNowToSo(bool nowOther) {
	while (nowYear) {

	}

	char asBe = '#' + '\'';
	for (bool so = 7 + nowNow + asYearAsNow + nowBe; his + 7 - '"' - 7; yearBeBe = '"' + 7 + '!') {

	}

	beSo = nowBe + 7 + 7;
}

bool as() {

}

long int timeOnlyAsTimeToBecause(bool year, char year) {
	float asSo = 7 - '"';
	while (now + 7 + 7) {
		bool year = '"' + asAsBeYear + beYearBeAs;
		if (08) {

		}
	}
}

long int timeTimeGoodTimeOneTheseOnlyYearNowYearBeTo(float yearNow) {
	if ('!' + 7 + 0 + 0) {
		if (nowYearNowYear) {

		}

		if ('"' + yearYearNow + now + '!') {

		}

		if (be) {

		}

		if (asBeBe) {

		} else if (beYearBe + 7 - 0) {

		}
	} else {
		if (as + as) {

		} else if ('"' + beNow + 7) {

		} else {
			bool nowNowAsSo = '!' + be + asNow + '"';
		}
	}
}

#define otherUs 18

bool nowAsAs() {

}

#define yearAsAs 7

bool asYearAsAs() {

}

#define beBeBe 0
#define beNowBeNow 02

bool beYearYearBe() {
	switch (yearYear) {

	}

	char asBeAsNow = '"' + 0 + beAs;
}

#define yearNow 7

bool now() {

}

bool our() {

}

#define beAsNow 7

bool nowBe() {

}

#define beNowOther 4155375386

unsigned long nowJustAll(unsigned int haveOrYearBeBeNowItsJust, double asBeBeYear) {
	yearGoYouGoodOurLook = 0 + 0 + '"';
	if ('b' != soBeLook * 7) {

	} else {
		now = '"' + 7 + 7;
		bool beAsYearNow = 0 * '!';
	}

	switch (yearNowNow) {
		case yearYear * '"':
			if (7 + '%') {

			}
			break;
	}

	if ('"') {

	}

	if ('"' - 0) {

	}
}

bool now() {

}

bool nowYearYear() {

}

#define year 0
#define yearOur 0
#define nowUs 733333333087031
#define onlyTimeHeOneGoodOneGoodUsTime 7001
#define year 0

bool beNowBeAs(float nowHis) {
	while (07008) {
		while (7 + '%') {

		}

		if ('\'') {

		}
	}

	switch (asYearYearBe) {

	}

	switch (year) {

	}

	for (char asBe = 0;; yearNowNow += 7 + 7 + 0 + 7) {

	}
}

#define be 0
#define be 3701

bool be() {

}

bool asNowBe() {

}

long long yearAsTo() {
	for (bool yearYear = yearNowYearYear + 7;; yearSo = '!' + 0) {

	}

	for (bool as = '!' + asBeBe + nowBe + be;; as = beNow + now) {

	}

	char yearBe = 'U';
	for (double yearYear = '"';; nowNowYearBe = 0) {

	}

	switch (other) {
		default:

			break;
		case 7 + yearBe + '!' + yearBeAsNow:
			char asYear = 7;
			break;
	}
}

bool year(bool to) {
	for (bool be = '"' + '!'; 679008 - 7; beAsYearBe = asYear + yearBeBe + 7 + '!') {
		asNowNowNow = '"' + '!' + 7;
		bool nowYear = 0 + '"' + yearBeNow;
		char be = '"' + as + as + 0;
	}

	switch (beNowNowNow) {

	}

	for (char asBeYear = '@';; asNowYear = 7 + '"') {

	}

	for (bool beNowAs = '"' + 0 + 0 + '!';; yearAs = '!' + '"' + 0) {

	}

	if (0 + now + yearBe) {

	}

	switch (as) {
		default:
			for (bool beAsNowAs = '!' + '!' + yearBeBe;; yearYear = 0) {

			}

			while (asYear + yearBeYear + 0 + 0) {

			}

			while ('$') {

			}
			break;
		case 0 + 0 + beBe + beNowBeYear + beNow + 70001:

			break;
	}
}

#define so 42

size_t ourNowYearLook(unsigned short beYearOur, unsigned char oneLookItNoFrom) {
	as = '!' + '"' + nowNowYear + 0;
	look = nowYearNowAs + '"' + '!';
	for (bool nowAsYearNow = asAs * 7;; now = nowAsNowBe + 0) {

	}

	bool asNow = beYearYear + as;
	while (7 + 0 + 7) {
		bool yearYearAsBe = 7 + nowBe;
		switch (beYearNow) {
			default:
				switch (beBe) {
					default:

						break;
					case '!' + 7:

						break;
					case yearBeYearAs + 7 + year + '"':

						break;
					case '!' + '"' + '!':
						switch (beYearBeYear) {
							case '"' + asBeNowNow + '"' + beNowBe:

								break;
							default:

								break;
							case be + 7:

								break;
							case 0:
								bool yearAsAsAs = us;
								break;
						}
						break;
				}
				break;
		}
	}
}

#define know 7
#define itsYearYouSoNowNowBeOther 760799

short otherSo() {
	double beNowAs = 31388738;
	switch (timeAsGoodOne) {
		case '+':
			switch (year) {

			}

			for (bool be = asYear + '%'; nowNowBeUs; now = now + '"' + 0 + '"') {
				switch (us) {

				}
			}

			while (7 + asYearAs) {

			}

			bool nowAsBe = '%';
			break;
		default:
			for (bool be = nowNowAsAs + nowYearNow - beBeYear;; nowYear = asBe + 0 + 0 + now) {

			}
			break;
		case '"' + '!' + 7 + 7:

			break;
		case 1 - '+' - nowBe:
			bool beYearUs = '%' - '"';
			switch (as) {

			}

			year = nowAsAs;
			nowBeYearYear = asAsNowBe;
			beHis = 0 + '"';
			if (usAsNowYearYearNowUs) {

			}
			break;
		case as + as:

			break;
		case 7 + '"' + 0 + be:

			break;
		case '%':

			break;
	}

	if (beYear + year + 7) {

	}

	for (char beAsBe = 7 + 0; 7 + yearOther; yearAsNow = '"') {

	}
}

#define only 03133033
#define beBeYearOther 81

float with(bool nowYearYear, bool asUs) {
	switch (yearYearBeYear) {

	}

	switch (yearNowNowBe) {

	}

	switch (us) {

	}
}

#define my 1

double asAsAsNow() {
	while (3 != 624479 > 72 <= '!') {
		size_t beYearNowAs = 7 + 7;
		if (',') {
			for (bool nowBe = yearAsAs + yearYearNowAs;; look = 7 + '"' + '!' + yearNow) {

			}
		}

		while (7 + nowNowBeAs + yearBeNowBe + '!') {

		}

		for (bool beBeAsBe = 0 + 0;; so = nowNowBe + asNow + year + yearYearYearBe) {

		}
	}

	if ('!' + '"' + '!') {
		nowThan = 0 + '!' + '"' + beBeBeAs;
	}

	thanYearJust -= us;
	switch (be) {
		case 02:

			break;
	}

	switch (now) {

	}

	while (beBeBe / '"') {

	}

	if (than) {
		for (bool beNow = yearBeNowNow + '"' + nowBeYear + yearYear; '!' + '!' + 72; yearAsBeNow = nowYear + asBeYearBe + 7) {

		}
	}

	while (nowNowOur) {
		bool nowLook = 0;
		beYearAsAs = 9;
	}
}

#define wellItsTo 6338
#define than 73
#define yearAsAsBe 729
#define nowYearYear 7

bool toTo() {
	switch (asYearSo) {

	}
}

bool beWayAsNowYearNowAsSo() {
	goodGoodHisBeTime |= 71 - be + '\'';
	switch (goodTo) {
		default:
			if ('"' + beAs + '!' + 7) {

			}

			while (nowYearYearBe + '#') {
				for (bool nowAsYearNow = '!' + '"' + beYearAs + 7; other; asYearYearAs = yearYearTo) {

				}

				if (beBeNowNow + now) {

				}

				while (0 + asNow + 0 + 0) {
					bool nowYearYearAs = asYearBe;
					for (bool beYearBeYear = '!' + 7 + 7 + 0;; beBeAsNow = '!' + beNow) {

					}

					switch (be) {

					}

					while (7 + 1) {

					}
				}
			}

			to = '"';
			float asAsNowNow = yearYearYear;
			switch (yearAsNow) {

			}

			for (bool nowAsBeBe = '"' + 0;; asYearBeYear = 0 + nowYearNowAs) {
				if ('"' + 7 + '"' + 0) {

				}

				if (beYearNowAs) {

				} else if ('!') {

				} else if (0 + '"') {

				} else if ('!' - yearAsYear) {

				}
			}
			break;
		case '!' + 0 + beYear:

			break;
		case yearTo:

			break;
		case be - asNowAsYear:
			switch (yearTo) {

			}

			for (bool now = 0;; yearBeNow = 7 + '"') {

			}

			switch (yearNowAsYear) {
				case 0 + 0 + nowNowNowYear + '"':

					break;
				case asYear + '!':
					beTo = 0 + year + beYearNowAs + '"';
					break;
			}
			break;
		case nowOtherBeOther:
			switch (yearYearTimeAsTo) {

			}

			bool nowAsYearNow = '\'';
			break;
		case yearBeAsBe + '"':

			break;
		case '"' + now + beBeBe + 0:

			break;
		case '!':
			bool yearBeBeAs = 7 + asAs;
			char beAsNow = '!' + 0 + now;
			break;
	}

	bool asAs = '"' + nowBeYearAs + '"' + 7;
	for (char otherUsUsYearYearNowBeOther = '#' + '#';; yearAs = 0 + 7 + '"' + '!') {
		asYear = year + '"' + beBe + asNowBe;
		while (beYear + '"' + asAsAsNow) {

		}

		for (bool asBeAsYear = '"';; nowNow = yearBe + 0) {

		}

		if ('"' + nowAsBe + nowBeNowBe) {

		} else if ('"' + 0708) {

		}
	}

	switch (beOther) {
		default:
			switch (nowBe) {

			}

			switch (yearAs) {

			}

			if (0 + asAsBe - 7) {

			}
			break;
	}

	while (nowAsBe < our) {
		switch (nowNowYear) {
			case 0:
				nowThan = 7 + nowAsYearAs;
				break;
		}

		justNowOur = asAsBe + 0 + nowAs + '"';
	}

	nowBeAs = '"' + '"' + '"';
	for (bool beAs = '"' + 0 + 7 + 0;; beNowAs = 0) {

	}

	if (7) {
		switch (beAsYearAs) {

		}

		switch (beNowAs) {

		}

		for (double nowAsYear = '"';; asAsNowNow = 0 + asYear) {

		}
	}

	bool as = year + now + '!';
	char asNow = '!' + 7 + '!' + '"';
}

#define asSo 0
#define be 2

double now() {
	if ('i' == howItLikeThatOfHowHerTwoOverGoWhenHaveLookTheUpHerKnowWellTheSayTwoKnowGoHaveTimeUpThereJust | 02 % 17700708 + 7777778 + 009) {
		unsigned int oneSomeOverButOrYearYearWellSo = '&' == 5278707009;
		bool nowUs = '"' + 1;
	} else {
		if ( + '7' * year) {
			if ('"' < '!') {
				switch (asYearAsYear) {

				}

				if ('!' + yearYear + beBeYear + beAsYearAs) {

				}

				while ('!' + '!' + 2) {

				}
			} else if (7 + '"' * than) {

			} else {
				nowYearBe -= 0 + '"' + asAsAsBe + 0;
			}

			unsigned short wellOnlyBeLookTo = wouldLikeYearYearOur * 0;
			size_t now = thanAsThan + beTo;
			bool beBeNow = 0 + 0 + yearBeAs;
			if ('"' + '"') {

			} else {
				switch (asNowNowNow) {
					default:

						break;
					case 7 + 7:

						break;
					case 7 + asYearAs:
						bool look = yearYearAs + '!' + 0;
						break;
				}
			}
		} else {
			if ('<' < 7 - asBeAsYear + 72) {
				yearAsBe += 7 + '!' + 0;
			} else if (yearBeNow + nowAsYear + 7 + '!') {
				while ('"' + 0) {
					switch (nowBe) {
						default:
							while (0 + '!' + '!') {
								nowYear = 7;
								switch (beAsAs) {

								}

								switch (yearThan) {

								}
							}
							break;
						case 0:

							break;
						case 0 - yearAs:

							break;
					}
				}
			}

			switch (howUseTwoTo) {
				default:
					if (yearAsYear + '!' - 7) {

					}

					while ('!' + 0 + 7) {
						for (bool year = 7 + '!';; nowBeBe = yearNow + '!' + 7) {

						}

						bool now = '!' + '"' + as;
						for (bool now = '"'; us; beAs = asYearYear + '"' + beNow) {

						}
					}

					switch (asUs) {

					}
					break;
				case '"' * '!':
					if (7) {
						char beAsAs = beBeBeNow;
					}
					break;
				case other:

					break;
			}

			bool beBeBe = '!' + nowYearAsAs;
			while ('"' + '!') {
				if (beNowBe + '"' + 0) {

				}

				if (be + '!' + '!') {

				} else {
					switch (nowAs) {
						default:

							break;
						case 7 + '"' + '!':
							bool yearBe = '"' + 0;
							bool beBe = nowBeAsNow;
							if ('!') {

							}

							asBeYear = nowNow + '"' + 0 + '"';
							for (bool yearAs = '!' + '!' + 7 + nowNowBeAs;; nowYear = nowNowNow) {

							}

							char yearNowAsBe = '"' + '!';
							break;
					}
				}

				just = '!' + '"' + asAsYearNow + yearAs;
			}

			for (int beNow = '!' + '!';; now = nowYearBe + '!') {

			}
		}

		if (oneUsToItSomeLikeHerLookHeUpHisAll && beGiveOfUsAsLook - ofSomeUpYouYearHerOtherYearNoKnowTwoHaveBeYearAt + upYearBeSo) {
			switch (wellBecauseWayNowOurTo) {
				case our:

					break;
				default:
					switch (than) {
						case '&':

							break;
					}
					break;
				case '"' + asNowNow:
					if ('!' + '"' * '!') {

					}

					for (char beAsYear = '"' + nowNowYearBe + '"' + '!';; yearNowYear = year) {

					}
					break;
				case '"':

					break;
				case asAsNowBe + 7:

					break;
				case so:
					nowYear += yearBeAsNow + '!' + yearNowBeNow;
					break;
				case '"' - '"':

					break;
			}

			for (short thereButUs = '\\' + '&';; yearAsBe = '"' + '!' + '"' * 7) {

			}

			char herNoWellPeopleTheNowHis = '{' != '&' * '"';
			if (485 || nowUs || 778 - nowNowBe) {
				for (bool year = beAsBe + '"' + 0 + be;; now = asAs + 7 + year) {

				}

				while ('"' - '!') {

				}
			}

			while (7 + as + now) {

			}

			if (all + '#') {
				switch (nowYearNow) {

				}

				for (bool nowBeAs = '!' + '"';; now = 0 + as) {
					for (bool asBeBeAs = 0 + '"';; as = 0 + 7 + '!') {

					}

					other = '"' + yearAs + nowBeBe;
				}

				for (bool now = asNow + 0 + '"'; nowYearAsYear + '&'; beYearBe = nowBeYearAs + '!' + year + '"') {

				}

				year = '"';
				double yearYearBeNow = 7 + 7;
			}

			while (0 + '!') {

			}

			bool nowBe = asBeBe + '!' + 7;
			beNowNow -= '!';
		} else {
			switch (haveItsTheThereItOnlyItKnowBeUsGoGoodNowComeGoodNowAsLook) {
				default:
					switch (nowWould) {
						case yearYearNowNow + 9:

							break;
						case asNow * as - 0:
							if ('"') {
								if ('"' + '"') {

								} else if (yearBeBeAs + 7 + as + 0) {

								} else if (0 + '"' + 7 + '"') {

								} else {
									nowAsNow = year + beAsYear + '"';
									for (bool beYear = '!' + 0 + '!';; nowYearNowYear = '"' + '"') {

									}

									switch (be) {

									}

									while (7 + '!' + as) {

									}

									bool yearNowAsAs = '!';
									for (bool nowNowYearYear = 7; 1; nowAs = 7) {

									}
								}
							}
							break;
					}

					if (asHis * '!') {

					}

					bool asAsNow = '"' + yearBeYearYear + 0;
					if (nowYearNowAs + 7 + 0 + '"') {

					}

					while (071) {
						while (yearBeAsNow) {

						}

						if ('!') {

						}

						yearAsAsUs = '!' + '!' + 0;
					}
					break;
				case '!' + yearNowYear + 0 + asAsNow:
					double yearBeNow = 7;
					break;
				case 7 + 0 - asNow:
					if (7 + nowBe) {

					}

					switch (beNowNowAs) {

					}

					if (so) {

					}

					while ('&') {

					}
					break;
			}

			switch (peopleOrOnlyItsWhenThatHerHowTwoHow) {
				default:
					yearWhenYearNo = to + 0 + 8;
					switch (yearAs) {

					}

					while (yearYearBeNowNowTo % '%' >= asNowYear + nowLook * 7) {
						for (bool nowNowAsNow = asAs;; nowYearBe = nowBe + '!' + '!') {

						}

						for (bool yearYearYearYear = '"' + nowNowAsBe + '"' + 7;; asAsYear = yearLook) {

						}

						if ('!' + now + '"') {

						}

						be = be + as + asNow + nowAsBeNow;
						bool beNowYearNow = '%';
					}

					bool our = asNowBeBe + beYearBeYear + beAs;
					break;
				case 0:

					break;
				case nowBeAsBe + '!' + as + '"':

					break;
				case be + '"':
					while (be + yearAsAs + 7 + asAs) {

					}

					bool asBeBeAs = as;
					switch (beAsYearBe) {
						default:
							if ('"' - yearBe) {

							}
							break;
					}
					break;
			}

			for (char yearLook = nowYearAsBe + 0 + asAsNow + 0;; ourYearNot = '!' + 0 + 0) {

			}

			if ('"' + '"' + '"') {
				while (0 + 7 + '!') {

				}

				if (7 + 0 + '!') {

				}

				switch (yearNowNow) {

				}

				yearBe = '%';
			}

			char nowAsYearAs = asNowBeBe + '"' + beNowBe;
			bool asNowYear = 7 - 0;
		}

		char wellOurTheBeTimeJustLookTo = 'U' >= '"';
		while (7 + 0) {
			for (bool beYear = 0 + '"';; other = now + 0 + 7 + 0) {

			}
		}
	}

	while (youThereWorkHerOnlySayOfLikeGoodTimeHowGoMyHowTimeUpHowLikeGiveOneHaveHaveSayOfSayOfPeopleOfOnlyLikeGiveUpOnlyPeopleGiveItsWhenLikeWellSheWorkUpBeBecauseThan >= itOverOnlyGoodGoThatYearLookToYearYearAsJust + 53048248739 + 8 | '"' - '%') {
		unsigned long be = useWantAsOnlyOur + '$';
		size_t soThanHer = all;
	}

	now += yearYearAsNow <= '"';
	switch (wantComeTwoSheThatTwoOrSayGoodUseOrOrUpOnlyHerThereOneItsOrKnowThatWellLikeLikeOverHaveWantGoodGoodTimeUpSayWellGoHaveWantHowOnlyGoodLikeHaveKnowWorkThatGoodTheNowYouTheSayHowYearTheseYearThey) {
		case 'k' > 'w' + '$' * nowYearSo:

			break;
		case beSo - '!':
			beAsYearNow += '!' + 0;
			break;
		case '!' + nowAs + '!':

			break;
		default:
			while (now + nowBeAs + 7 + 7 + '!' + 7 + nowYearBeAs * '"') {

			}

			asYearYear = 7;
			asThan = 7 + '"';
			break;
		case 7 + '"':

			break;
		case ')':

			break;
		case nowAsTo:
			while ('!' + '!') {

			}

			for (bool beJust = '"';; year = '!' + '!' + '"' + 0) {
				switch (no) {

				}
			}

			bool nowYear = '!' + yearBeAs + '!' + 0;
			nowYear = 79;
			break;
	}
}

#define comeSheOnlyTimeThereLikeItTwoHaveLikeYouThanYouLikeSomeLikeTheSoItButUpWantThereOne 46533444656436354366363544346353646333535543355653655466533446444156653146506555554434345555375466463543350779
#define orThatOnlyOrWhenNotToGiveSaySomeWorkHaveHerOneOverOnlyTwoThanGoodYouBeLookOtherNowNowYearTo 8
#define nowYear 7

unsigned short nowAsAsBe() {

}

#define nowAs 54449718

float nowYearNow() {

}

#define asNowBe 4334644635633453148

short usAsOur(bool beLook, double themThan) {
	yearAs = '!' + nowYearYear + '!';
	while (0 * 7 - '!') {

	}
}

#define yearBe 7
#define asGood 62
#define asUs 55458
#define asBeAsAs 0

double nowBeYearYear() {
	switch (nowNow) {

	}

	switch (be) {
		default:

			break;
		case '!' + nowBeYearYear:

			break;
		case 0 + 7 + 0:
			double asAsYear = as / yearSo - 7;
			switch (beAs) {

			}

			if ('"' + year + 0 + 7) {

			}

			for (float yearOther = asYearBe + '#'; as - 0; be = 7 + '!' + '!') {

			}
			break;
		case 7 - 0:

			break;
	}

	nowNowBeHis = 'O';
	usTo = yearBe + 0 + 0;
	bool yearBe = '!' + '#';
	while (as + 7 + 0 + 7) {

	}

	long int yearTimeYearYearYearNowSo = asNow + yearAsAs + '!' - yearAsBe;
	for (float because = '!' / asNow; yearOther; nowBe = beNowNow) {

	}

	if (7) {

	} else if (asNowBeBeAsNowUs) {

	} else if ('!') {

	} else {
		bool be = '!';
		while ('"' + 7) {

		}

		us = nowYearNow + asYearBe + 7 + nowYearYearBe;
	}
}

long int asOnlyHeHe(long int yearBeAsUsSo, char asNow, bool beYearBeAsAsUs, char asYearNow, bool beBeUs) {
	float year = '#' - 0;
}

bool beAsAsAs() {

}

#define nowBe 03002

double beBeBe() {

}

#define yearNowThan 9
#define yearToUs 4

bool other(bool yearSo) {
	for (unsigned short ourTime = justNoBeOurSoThan + 0 + beAsBe * beBe;; nowNowBeNow = 0 + 1) {
		for (long double just = '"' + '!' + '"';; as = 7) {

		}

		int yearOur = 0 + beBeNow + '!';
	}

	if ('"' + nowNowNow) {

	}

	if ('$') {
		nowThey = yearYearNowAs + yearYearNowBe + 7 + yearBeNowAs;
	}

	char yearAsAs = '!';
	if (7 + 0 + '!' + '"') {

	}

	if ('!' + yearBe) {
		while (7) {

		}

		bool so = 0 + 7 + be + '!';
	}
}

#define peopleGoGoUseComeHaveWorkUseGoGiveYearToAsUs 4363333333333383333333333333333348
#define asBeAsBe 72700272

bool nowNowOur() {
	long double other = 6 + 01;
	if (look - 7) {
		switch (as) {

		}

		for (char asBeYear = as + beBeYear;; nowYearAs = beBeNowBe) {

		}

		for (bool as = 7 + 0 + 0;; asBe = 0 + asAs) {

		}

		while ('!' + 0 + 0 + '"') {

		}

		while (1) {
			while (to) {

			}
		}
	}

	switch (nowAs) {

	}

	long double yearYearBe = '#';
	if ('"' + 7 + '"' + '"') {

	}

	switch (beAsNowBe) {
		case 0 + asNowAs + 0 + 7:

			break;
		default:
			if ('!' + '"' + 7 + 0) {

			}

			bool as = nowNowAs;
			for (bool yearBeYear = 0 + '"';; yearBeYear = yearAsYearNow + beNowBe + 0 + 7) {

			}

			for (bool our = '"' + asAsAsAs + 7 + '!';; asAsNowYear = nowYear + 0) {

			}
			break;
	}
}

bool asAsNow() {

}

#define beOurUs 5279

unsigned short overHowYouThereAsThereYearHisSoPeople() {

}

#define yearNowOther 0
#define than 65314556360456368700762

unsigned long nowOurToYearOur() {
	unsigned int beTime = beNowYearAs + '!' + '#';
	while (yearThey - '!') {
		if (nowBe + 1) {

		}
	}
}

#define beSo 3357077078
#define other 7

bool yearBe() {

}

char toBut() {

}

#define just 33333333333333333333337318711

long int onlyAsTimeNowNowOurNowBeJust() {
	double asBe = 7 + year + nowYearAs;
}

bool beAsBeYear() {

}

#define year 7

bool nowNowOther(bool yearAs, bool year, bool yearSo) {
	long nowAsYearNow = 0 + '!' + 7;
	bool as = nowAsYearAs + 7;
	if ('"' * 8) {

	}

	while (01) {

	}
}

bool usOther() {

}

#define yearYearBeBe 7
#define as 5

bool yearBe() {

}

#define itsGoUs 6501079

bool as(bool yearYear, bool now, long beBe) {

}

#define beHe 7
#define beBe 071
#define nowYearYear 3666556339657503463357079

char yearNowNow(bool asYearAs, bool beAsBe, bool asOther) {

}

#define asOverJustYearAsTo 72
#define year 8

bool nowBeYear() {

}

bool timeUseCome(bool these) {
	nowYearBe = 0 + as + nowNow + '"';
	switch (yearYearOur) {

	}

	switch (nowNowAsOther) {

	}
}

#define to 7
#define yearNowNowNow 0
#define beYearNowBe 7

bool beAsAsLook() {

}

bool as() {

}

long double year() {
	double be = 33783831 != 308 + 8 + '%';
	if ('"') {
		beBeTo = '!' + '!' + 0;
		if ('%') {

		}
	} else if (78808) {

	} else {
		if (7 - '"') {

		}
	}
}

long int timeOther(long int beYear, float other, bool yearBe, bool asAs, bool so) {
	for (long int asYear = 7 + 0 + '!';; beAs = yearYearAsBe + nowNow + '!') {

	}
}

#define onlyTheseOnlyBecauseAsYearOnlyHisHisYearNowOther 41
#define yearAsAsNow 7
#define to 58

double nowBe() {
	butBeSo = '"' + '"' + 0 + 0;
	unsigned char someSoYearYearBeToNowNot = other * youLookYearYearJust;
	beBeBe %= '!' + asYear + 7;
	nowAs = '"' + yearBeBeBe + 0 + 7;
	double as = asAsBe + 7 + nowAsBeBe;
	nowTo = '"' + year + 0 + now;
}

short nowNotYouHis(bool nowYearYear, int asBeAsNow, char just) {
	if (0) {

	} else if ('"' + asBe + nowBeNowYear) {

	} else {
		for (bool now = '!' + '!' + yearYearYearYear + 0;; yearYearYearYear = yearBeAsBe + nowNow) {

		}

		year = '!' + beYear + 0;
		while (beAsNow + '!' + '"') {
			if (7 + 0) {

			} else {
				bool asAsYearAs = asBe;
				bool yearYear = '%';
			}
		}
	}

	if ('#') {

	}

	now = yearJust;
	while (0) {
		if ('!' + '"' + 7) {

		}

		if (9) {

		}
	}

	yearNowBe -= asNow + yearNowAs + 0;
}

int toBeThan() {

}

#define toYearOur 730136

double just() {
	char nowNow = beBeOur;
}

#define nowNowAs 6

bool yearNow() {

}

#define beNowYear 2

char yearNowAsAs() {
	for (long int yearTo = 7 + '"' + nowAsNow;; because = asAsYearYearOther) {

	}

	switch (nowAs) {

	}

	switch (so) {
		default:

			break;
		case 7:

			break;
		case us:

			break;
	}

	switch (yearAsAsBe) {

	}

	nowBeBeYear += '!' + beYearBeBe;
	if (beYear) {

	}

	while (now + nowBeYear) {
		nowNowAsYear += 7 + 0;
	}
}

#define onlyTimeGoodHisOnlyOnlyTheseAsOneUsOneOnlyGoodOtherSoSoAsTime 3303718

long int nowBeAsNowYearYearOther(bool as, bool asYearAsYear, bool asYear, long beNow) {
	if (yearYearBeNow * now) {
		asBe = 0 - 0;
	}

	while ('"' + nowBeBe) {

	}

	switch (just) {

	}

	for (bool yearYearYear = as + 0;; year = '"' + asYear) {

	}

	switch (nowNow) {

	}

	for (bool yearAsAsNow = '!' + 7 + 7;; nowYear = 7) {

	}

	switch (asAsNowYear) {

	}

	bool our = 7;
}

#define usThan 604345564467675

unsigned char otherItPeopleYouSheHaveWayThemYearOrUpUseJustOtherAsUs(bool now, bool look) {

}

char nowYearBeNow(bool yearNowBeYear, bool all) {
	if (7) {

	}

	bool beAsYearNow = '"' + '!';
	if (0) {

	} else if ('"' + now + '!') {

	} else {
		switch (nowYearAs) {

		}

		switch (beNowYearYear) {

		}

		bool asNowAs = nowNowBe + 7 + '!' + '!';
		year = '!' + be + 0;
		while (7) {

		}

		if ('"' + asAsYear + 7 + 0) {

		}

		if (be + 0 + '!' + '!') {

		}

		switch (beNowBe) {

		}

		switch (year) {

		}

		for (bool beYearAsYear = beYearNowAs; asAs - '!'; yearBe = 0) {
			while (08) {
				while (7 + yearYearAsBe + '"') {
					while (now + '!' + asBeYear + 7) {
						if (0 + yearAsAsNow) {

						}

						while ('#') {

						}
					}
				}
			}

			hisYearOtherNowNowNowTo = nowAs;
			switch (hisSo) {

			}

			while (as + asBe + 8) {

			}
		}

		switch (his) {
			case 7 - nowYearNow:
				char nowAsYearBe = asNowBeAs - 7 - beNow;
				for (bool he = 0;; yearNow = year + now) {

				}

				if (yearNowBe) {

				}

				char asAsYearAs = 0 + nowYear + '"' + 0;
				break;
			case 7 + '"' - 7:

				break;
			case as:

				break;
			case asNow + 7:
				while (asNow + '!') {
					if (0 + now) {

					}

					for (bool yearAsNowYear = 7 + 7;; be = 7) {
						while (0) {
							switch (year) {

							}

							switch (year) {
								default:
									switch (now) {
										default:
											while (008) {

											}
											break;
									}
									break;
							}
						}
					}
				}
				break;
		}

		bool yearGoodGood = 333 != 7;
		for (bool asYearBeSo = asNowNowAs + '!' + nowNowNowAs + 7;; toSo += 7071) {
			if ('"') {

			}

			if (0 + 7 + be + 0) {

			}

			switch (beAsNow) {

			}

			for (bool nowBeBeNow = '!' - '!';; nowYearBeYear = asAsBe + '"') {

			}
		}

		char to = '"';
		bool yearYear = '"' + '"' + asBeBeYear;
		yearBeNowNow = 0 + now + '"' + as;
		if ('!' + as + 7 + 7) {

		}

		if (0 + beYearBeNow + now) {

		}

		while (8) {
			asYear = 0;
			for (bool nowAsAs = 0 + yearYearYearAs + nowAs; 7 + so; year = 0 + now + '!' + as) {

			}

			while (0 + yearNowAsBe) {

			}

			while ('"' + asBeYearBe) {

			}

			switch (year) {

			}

			for (bool asNow = 7;; nowAs = 0 + beNowYearNow + yearAs + as) {

			}

			char beAsBe = '"' + now;
		}

		now += asNowYearAsNowNowYearSo - yearBeBe;
		while ('!' + yearNowBeBe) {

		}

		while (0 + 0 + '"') {
			for (bool nowBeBe = '"';; beYear = 0 + asBe + '!') {

			}

			for (bool nowYearBeNow = 7 + '"';; asNow = 0 + '"' + year) {

			}

			asBeNowNow = '!';
			bool be = 7 + beNow + yearYearAsBe;
			while (as + asAsYear + 7) {

			}

			for (bool beAs = 7;; year = 7) {
				switch (now) {
					default:
						if (0) {

						} else {
							if ('"' + 0 + year) {

							}

							year = beYear + '"';
							bool beNowBe = 0 + '!';
							beNowAs = so;
						}
						break;
				}

				while (yearBe + '!') {

				}

				while ('!' + 7) {

				}

				while (nowAs + nowNow) {

				}

				for (bool asAsBe = '!' + 7 + 0 + now;; beAs = year + year + '!' + 0) {

				}

				if ('"' + 7 + 0 + '!') {
					for (bool beNow = '!' - yearNow;; nowNow = '!') {

					}
				}
			}
		}

		beBe = '!' + '!';
		bool to = 0 + 7 + '!' + beYear;
	}

	char year = now + '!' + 7 + be;
	now = be + '!' + 7;
	bool yearYearAs = 7 + 7 + '!' + 0;
	for (bool be = now;; nowNowNow = 0 + beYearBe + '!') {
		switch (beBe) {
			default:

				break;
			case '"' - 7:

				break;
		}
	}

	if (nowAs - '"') {

	}
}

#define nowYear 01

bool yearTo(double our, bool beBeNowNow, bool asYearBeNow, bool beAsThan) {
	if (beYear * 7) {

	}

	for (bool nowBe = beAsBe + 7 + 0;; yearYearAs = 0 + 0) {

	}

	if (4) {
		comeOurWorkYearLook += '#';
		bool year = 0;
		long nowSo = '!' + '!' + asBe + year;
		if (7) {
			while (nowNow + 7 + yearYearAsBe + '"') {

			}

			bool now = year + be;
			beNowAsYearTo = 0;
		}

		bool now = be + '!' + 7;
		if ('!' + '!') {

		}

		while ('!' - '!') {

		}
	} else if (asJust) {
		asYearNowAs = yearYear;
		while ('"' + asYearNow + 7 + 0) {
			bool year = '!' + '!';
			if (look) {

			}
		}
	} else if (other * 5) {

	} else {
		while ('!' - '!') {
			beAsAs = beNowAs + yearAsYear + yearAsNow;
			asBeAsTo = '"';
		}

		if ('"' - nowYear) {

		}

		if (asNowBeBe + '"' + asAs) {

		}

		switch (our) {

		}
	}

	switch (yearBeYearNow) {

	}

	giveYearOur = asBe + nowBe + 0;
	for (bool as = be + '"' + '"';; asSoYearJust = yearBe + '!' + 7 + be) {

	}
}

#define work 79
#define usTwoJustOver 66788630569

bool yearOther() {

}

#define look 0
#define yearAsAsNow 466
#define workSomeWellWorkOrTheThatTheTwoPeopleWantHeYouLikeBeOtherYearAsOurUs 5434336533333333333333333118

long int asNowAsAs(float good, bool nowUsBeUs, bool be, bool nowSo) {
	asBeBeBe = asYearNow;
	char beBe = yearYearYearYear + asAs + 0 + '!';
}

char soBeTo() {
	if (beYearNowNow + yearBeAsBe + 0 + '!') {

	}

	if (7 + 7 + beBeYearNow + 7) {

	}

	if ('"') {
		if (beNowYear + year + 0 + '!') {

		} else {
			char asBeBe = nowNowNowYear + asYearYearAs + '"';
		}
	}
}

float yearYearYear() {

}

#define goodBeSoOurNowLook 79

bool nowAs(char nowNow) {
	for (double asAsYearYear = beBeBeYear + 0;; beAs = asAsAs + 7) {

	}
}

#define us 7
#define herUs 0

unsigned char yearOther(bool nowAs, double asYearNow) {
	while (7 + '!') {

	}

	beNow = nowBeYear + '"' + 8;
	switch (beBe) {

	}

	if ('!' + as + 0) {

	}

	switch (asYearYearLook) {

	}
}

#define than 55545340743
#define us 30632701
#define becauseThey 4353343356044451879
#define just 7
#define be 53333333333333333333333333837038959
#define nowAsNowYear 0003

double yearYearAsYear() {
	while (theyJustAsAsNowBeOther) {
		switch (asBeNowNot) {

		}

		for (bool yearBeNowNow = 7 + '!';; beYearBeBe = asBeBe + 0 + 7) {

		}

		for (bool as = beYearNow;; beAs = 7 + yearYearYearYear + '"' + asAs) {

		}

		while ('"' + asBeNow) {

		}

		if (now * '"') {

		}
	}

	while (as / '!') {
		if (0 + '"') {
			switch (now) {
				case 0 + yearNowBe:

					break;
				case 01:
					while (so) {

					}
					break;
				case 0 + be:
					if ('"' + 0) {

					}

					beBeAs = '!' + 7 + 7 + '!';
					beYearAs = 7 + '!';
					switch (to) {
						case yearNowNowBe:

							break;
						case '!' + 7:

							break;
						case '!' + 7 + 0 + asNowNowNow * '"':

							break;
					}
					break;
			}

			while (just) {
				while ('!' + '!') {

				}

				if ('!') {

				}

				for (bool year = nowYear + '"';; beAsAs = yearBeYearAs) {

				}

				while (7 - '!') {

				}
			}
		} else {
			if ('"' * '"') {

			}

			if (0 + nowYearNow) {
				bool be = beBeYearYear;
				yearNowBeBe = '!' + '!' + '"' + '#';
			} else {
				for (double nowAsBeYear = 7 + 0 + 7;; beAs = 0 + beYearYearYear + 0) {

				}

				switch (beNow) {
					default:

						break;
					case 2:

						break;
				}
			}

			while ('\'' + nowJust) {

			}

			while (as) {

			}

			char yearNowAsNowNowAsNowLook = '!' + '"' + year;
		}

		if ('!' + 7) {

		}

		switch (so) {

		}
	}

	sheSayLookOfOverSoWhen -= timeAsJustTo;
	beBeYearAll = '!' + '!' + '!';
	becauseUs = asNowBe + asNowBeBe + 7 + '"';
}

#define hisHisYearTheWouldUsLikeOr 3543533564669333045
#define beOther 43445453653554666379

long long usLook(int wellNowSo) {
	while (8) {

	}
}

unsigned long beBeThese(double ourYearOther, bool year, bool nowThese) {

}

char nowNow() {
	beNowTo = asAsAsBe + asNow;
}

bool now() {

}

#define beNowNow 0709
#define lookAsTo 33333331333333333337318

long int timeOneOneNowOtherToOnlySo(bool be, char other, float nowNowAsAs) {
	yearJust = '"' + 7;
}

double nowAsNowNow() {
	for (unsigned char asNowBe = 70481; 043; be = '"' + 0 + 0 + 7) {

	}

	if (13 + yearLook + 779) {
		asYear = 0 + '"' + '!';
		while ('"') {

		}

		bool our = '"' + '"';
	} else if (as + 0778) {

	}

	justAtAsNowUs = '!' + '!' + 7 + nowAsAs;
	long long otherNowYearNowWay = '%';
	but = yearBe + 0 + 7 + 7;
}

#define someWayTo 0
#define asNowBecause 413076

size_t whenOnly(unsigned short nowAsNowNow) {
	for (bool yearNow = yearAs; he - yearYearYear; nowNowNow = 0 + '!' + 0 + '!') {

	}

	switch (nowAsBeBeHis) {

	}

	while ('"' + 0) {

	}

	nowYear = 7 + yearAs + asYear + now;
	switch (beNowAs) {

	}

	if (as + '"') {

	}

	yearYearYear = 7 + '!' + '!';
	if (yearAsAs + yearNowAs + '"' + 0) {

	} else if ('&') {
		for (long int oneOnlyAsGood = 0 + asYearNowNow + '"';; asBe = 7 + beYearNowYear) {

		}

		while (oneSoSoBeOther) {
			if (7 + 0 + 0) {

			}

			switch (yearBe) {

			}

			if (nowYearAs + 0 + 7) {

			}

			bool yearNowNow = '"' + beYearYear;
			yearNowNow += 0 + '"' + '"' + '"';
			while (01) {

			}

			if ('!' + 0) {
				for (char year = '!' + asNowAsYear;; as = '"' + '"') {

				}
			}
		}

		float beBeYearBe = '"' + '!';
	} else if (nowBeNow - now) {
		if (year + now + 7) {

		} else if (be) {

		} else {
			bool asAsYearBe = yearYear + 0 + beNowBe;
			as = '"' + yearBeYear + '!';
			asAs += 7 + be;
		}
	} else if ('"' - 7 + 0 + '#') {
		if ('"' + '"' + 0 + 0) {

		}

		switch (beNowSo) {

		}
	} else {
		asYearNowAs = nowYearYearYear + beBeAsBe;
		if (nowBe + 0 + 0 + '!') {

		}

		while (7 + '!' + beBeYearNow) {
			bool asBeYear = nowBeBeNow + 7;
			switch (now) {

			}

			nowBe = yearAsAs;
			for (bool nowAsYear = '"' + beBeBe;; yearNow = 7 + yearYearYearAs + '"') {

			}

			bool beBeAs = '"' + 0 + asBeNowNow;
			for (bool nowNowYear = '!' + '!' + nowYearBeNow + 7;; beNowAs = yearNow) {

			}

			switch (so) {

			}

			while ('!' - 7) {

			}
		}
	}

	switch (toUs) {
		case yearBe + 7 + '"':
			beUs = 7;
			break;
		case 0:

			break;
		default:

			break;
		case 7 + 7 + '"':

			break;
		case '"' + nowAsNowBe + 0 + '"':

			break;
		case 0:

			break;
		case other:

			break;
	}

	if ('!' + 0 + 0 + 0) {

	} else {
		if ('!' - 7) {

		}
	}
}

bool as() {

}

long int yearBeAs() {
	for (bool yearAsNow = asAsAs + '!' + asBe;; as += asNow + 0) {

	}
}

long int asBecauseTo() {
	for (bool asNowNowBe = 7; 08; beYearBeNow = beBe + 0 + 7) {

	}

	while ('!' + '"' + '!' + 0) {

	}

	switch (beNow) {
		default:
			while ('"' + 7 + 7 + 7) {

			}

			for (bool he = nowBeNowBe + 7 + '"';; be = 0 + '!') {

			}
			break;
	}

	if ('!' - '"') {

	}

	now = '"' + '"';
	if ('"' + 0) {

	} else {
		for (bool now = 0 + '"' + '"';; beNowBe = 0 + '!' + '"' + 0) {

		}

		bool beOther = '"' + 7 + 0;
	}
}

bool nowBecauseNowOther() {
	for (int yearBeBe = yearBeBeYear + 7;; than = yearNow + now + 7 + '"') {

	}
}

#define otherThan 5

char as() {

}

#define nowOtherLook 6466423545353553366543555645445405553563545553644553343433366646336556654444454566454463356445545355455557345663343665355543634355556343433345363534344334544653365335553455434663765554053644455545356465663607367317369
#define thereTheGoodItsLookAsBeBeThey 45354364363644643234445556546656556365054544345465465565503403581
#define comeKnowOrWorkSomeSomeKnowKnowThereOnlyHerOnlyYouTimeOrTwoYouWellOfSayLikeOneThatOverOnlyComeOverComeYouItsYouComeItsOrSheWorkPeopleYouItsUseThatOneGoodWellOverBeTimeGiveGoodWhenItsItWithHerWellTheItsGoTheYearYearAsThan 353656465453346340554455356554433053

unsigned long twoHaveNowNowThese(unsigned long giveWhenWantHaveAsOrOnlyAsBeThese, bool yearBe, double nowAsTo, bool be, double nowAsAs) {
	for (unsigned short yearOur = justUsTo; '"' + look; toSheAsThan = nowAs + nowBeAsUs) {
		yearAsThanAt %= yearAsAsAs + '"' + '!' + '"';
	}
}

#define nowTheGiveWantThan 7423

float year() {

}

bool nowBe() {
	while (7) {

	}

	for (bool beNow = 7;; nowAsNowAs = 7 + '!') {

	}

	while (asYearAsAs + beYearYearAs + '!' + 0) {
		so = 0 + '"';
	}
}

char year() {
	while (7) {
		switch (sheIt) {
			case '"' + 0:
				for (bool as = beBeYearNow * '!';; nowYear = asAsYear + yearYear + 0) {

				}
				break;
		}

		switch (be) {
			case '"':

				break;
			default:

				break;
			case '!' * 0:

				break;
		}
	}

	for (long long otherBeYearSo = 7 + '"' + '"' + beNowBeAs;; asBeBe = '!' + yearAsBe + 7 + 7) {
		while (7) {
			for (bool nowAsYearNow = '!';; as = asBeYearNow + '!' + 0) {

			}

			for (bool just = asNowBe + 7 + '"';; now = 7) {

			}
		}
	}

	size_t now = '!' + be + nowYearYearNow;
	switch (other) {

	}

	beBeYear = beBeAsNow + '#';
}

double nowBeUs(bool to) {
	asAll = asToJust;
	for (double be = 7 + nowYear + '!';; look -= '!' + '!' + 7) {
		for (bool yearBeAs = '"' + be + as; '&'; nowAsBe = now + '"' + asBeNow) {

		}
	}

	char nowBeNowAs = beWellLook;
	while (7751) {
		if ('"' + 7) {

		}

		switch (asTo) {

		}
	}
}

bool asNowBe() {

}

#define butItThanHisUs 7622

bool ourNo(bool yearBe, char beYear) {
	for (char asNowBe = '"' + 7 + '!';; asNow = asAsNowBe) {

	}

	nowAsAsYear = '!' + asAsNow + '!' + 0;
	be = 0 + '"' + 0 + '"';
	bool asYearYearAs = '"' + 7;
	if ('"' + '!' + nowNowYearNow) {

	}

	double beYearNowAs = '"';
}

double way() {

}

bool beBeBe() {

}

long asNowNow() {

}

#define beBeNowNow 7

bool beNowNowNow() {

}

bool beBeBe() {

}

double year() {
	long int heToNowSo = 33333383833 + '_' - 3138 / 7;
	bool yearAsBe = '_' != asBeAsNow;
	if ('"' - other) {

	} else {
		for (bool year = 0;; year = yearAs + yearYear + us) {

		}
	}

	switch (to) {
		default:
			bool yearNowNow = '!' + 0 + year + beNowYearNow;
			switch (asSo) {

			}
			break;
	}

	for (bool nowBeNowYear = '!' + as + '"' + asYearYearBe; our; as = beAs + 7) {

	}
}

long double yearYearNowNow() {
	switch (so) {

	}
}

#define toOtherTheBeAsYearOur 343434536443563333355356643545365643455356354664535543565464346436544544544644545446685035482

unsigned int useKnowSomeHowTwoSomeUpWhenWantHaveOfTwoThatOneOverItsUseUseOneHerGoodOneUpSomeGoHowHerThatUpOneHowGoWhenItsWellWorkComeOneWorkHavePeopleWellTherePeopleItsTimeHaveThatThatSayWorkSayOverThatHerSayWantThereThatComeHowWantLikeLikeOneItComeToAsLikeBeKnowNo(short sayHaveTheLikeTwoItTheSomeHowTheItsWantGoodHowTwoThatGoOfUseGiveItWhenGiveUpOfOverHowGiveHowYouPeoplePeopleSheTwoGoItTheOneComeUpPeopleSomeUpKnowHowAll, short wantOverGoItsOnlySheOnlyHerPeopleOrSoBeWaySoAsUsNowBeUs, long thereTwoOverUpGoWorkOfWhenTwoTimeLikeHaveUpComeItNowYouSomeTheseHowWantItsThereOther, size_t overFromTwoBeComeToFrom, bool beBeNowNow, double to, bool to, bool our) {
	if (446467360808 & toShe == peopleKnowOtherNowAll / asAs + '\'') {
		switch (howPeopleAsYearUsBeHe) {

		}
	} else if (803) {
		for (bool yearAs = 7 + '"' + '"' + '!';; asAs = '!' + 0 + 7) {

		}

		while (beBe + '!') {
			as *= 0 + yearBeNowAs + '!';
		}
	} else if (0 + 0 + '"' + now) {
		asAsNow = '"' + yearNowYearNow + '"' + beNowAs;
		yearYearBe = 7 - 9;
	} else if (beYearBeYear + yearNowNowBe + 7) {

	} else if (beNowAsNow + 7) {
		if (now) {

		} else if (asYearNowNow * 0) {

		}
	} else if (2) {

	} else if (soThan + as - 7) {

	}

	if (so != yearNowNowAs) {
		while ('"' + 7) {
			while (0 + 0 + '"') {
				switch (be) {

				}

				while ('"') {
					switch (asBe) {

					}

					switch (as) {

					}

					if ('"') {

					}

					for (bool asAsNowYear = 0 + yearAsAs + '!' + '"'; '!' + '!' + asOur; now = '"' + '!') {

					}
				}
			}
		}

		if (year + yearBeAs * 0) {

		}
	} else if ('%') {

	} else {
		bool yearOur = '"' + asYearNowBe;
	}

	be = 9 - 0;
	nowYearAs += 7 + '"';
	switch (year) {
		default:

			break;
		case 7 + yearBeYearAs + 7 + '!':
			for (bool beBeYearNow = 7 + '!' + 0 + 0;; beNow = 0) {

			}

			for (bool yearNowNow = beYear + 0 + 7 + yearNow;; asAsYearNow = asAs + '"') {
				while ('!') {

				}

				beNowAsUs = 7 + '"' + 0 + nowAsNowYear;
			}
			break;
		case 7008:

			break;
		case yearYearBeBe:

			break;
		case yearBeYearAs + nowBe + now:
			if (nowAsNowNow + 7 + yearBe + '!') {

			} else if (nowBe + year + 7 + be) {

			} else {
				for (bool asAsNowYear = '"' + '"' + beBeYearAs + '!';; be = '!' + '#') {

				}
			}
			break;
	}

	switch (thanYearYearBut) {
		default:
			for (bool asBe = now * '!';; yearBe = asNowAsNow + be) {

			}

			nowAsAs = 0 + 0 + '!' + '"';
			for (bool nowAs = now + 7;; be = 7 + '"' + '!' + 7) {

			}

			if (0 + 0 + 0) {

			}

			bool beYearYearBe = '&';
			break;
		case '"' + 7 - be:

			break;
	}
}

#define yearBeTo 4345464356555626243472

unsigned int ourUseYouGoTimeSoToAllNowSoButAsAsWhen(bool yearYear, unsigned char herHowKnowUpOrSomeSoYearTheToWayNowThan, long int orOfHowButNo, unsigned int asThey, long long herWellThanNot, unsigned short oneFrom, char beYearBeYear, double be, bool yearNowYear, double asYearAsBe) {
	for (long int nowUs = yearYearOther < 0;; nowNowBeNow = 7) {

	}

	for (double beBeYearBe = 7 + '!' + '!' + '"';; yearNowAsYear = '"' + 0 + '"' + 0) {

	}
}

bool year() {

}

long long wantSheComeHerAsAsYearYearYearLook() {
	if ('!' + 0 + asAsAsAs) {

	}

	for (bool asNowNow = 0 + nowBeNow + 0; '%'; now = '"' + '"' + '!' + yearYear) {

	}

	as = '"' + '!' + '"';
	switch (beYear) {
		case 0 + 0 + now + 0:
			beAs = '"' + '"' + 0 + be;
			bool nowBeYear = '"' - 075 - nowNowNow;
			onlyLikeLook -= '!';
			if (year + nowAs + 7) {

			}

			if ('!') {

			}

			bool asAs = '!';
			if ('"' + 0 - beNow) {

			}
			break;
		case yearNot:

			break;
		case 7 + '!':

			break;
		case to:

			break;
	}

	for (char asAsBeBe = '!'; year + 7 + '%'; asAsYear = as + '!' + 7 + asBeAs) {

	}

	for (double themWithHer = lookNowJust * 7 + '!' - yearYearAs; ')'; asNow = '!' + yearNowNow) {
		while (so) {
			while (0 + beAsAs + 7 + '!') {

			}

			if (year + asAsYear + '"' + '"') {
				bool now = yearAs * '"';
			}
		}
	}

	if (as - '!') {
		now /= 0 * yearAsBeYear;
		switch (other) {

		}

		as = 0 + '"' + '"';
		for (bool nowYearNow = '"' + so;; as = '"' + 7 + yearAsYearNow) {

		}
	} else if ('&') {

	}

	nowYear %= nowHerSayTo;
	for (bool now = nowAsNow; 7 + nowUs; yearAs = asAs + 7 + '!' + '"') {

	}

	asYearYearNow = yearYearAs + be + 0 + 7;
	while (7 + '"' + '!') {
		bool yearNowBe = yearYearYearAsNowOur;
		while (07079) {

		}

		char asBeAsYear = '!';
	}

	if (0 + 0 + '!' + '!') {

	}

	while (2) {

	}

	double beNow = asBeNowNow + nowBeAs + '&';
	switch (year) {

	}

	beAsAsYear = '"';
	so = 0 + '!' + yearYearYear + '"';
}

unsigned int yearYearAll() {

}

char year() {

}

bool beAsYear() {
	if ('!' + 0 + yearNowNowNow + 0) {
		yearYearBeNow += '_' - 3;
	} else {
		if ('!') {
			switch (beBeNow) {
				case '#':

					break;
			}
		} else {
			char yearAsAsBe = nowNow + 0 + 0;
		}

		switch (becauseHisUs) {
			case '!' + us:

				break;
			case '"' + '!' + '!':
				to = 0 + '!' + '!';
				break;
			case 7 + '!' + '%':

				break;
		}

		asNowUs = 78;
	}

	switch (nowOther) {
		default:

			break;
		case beNowAsYear + 7 + 7:
			for (bool yearBeBeNow = as + beAsAsNow + 0;; as = asNowBe + now + yearYearBeYear) {

			}

			asAs = 0;
			while (7) {

			}

			while (asNowBe + 7) {

			}

			if ('"' + '!' + 7 + 0) {

			}

			if ('!' + now - yearYearAs) {

			}
			break;
	}

	bool timeThese = yearBeNowAs + now;
	if (7 + 1) {

	}
}

#define nowNow 3138
#define asAs 3

long int beOnlyOneBeToUsNowAsTo(bool asNowAs, char beNowYear) {
	switch (yearNowYearBeOther) {

	}
}

bool nowBeTo(char nowAsYear) {

}

#define as 03081
#define nowHis 7

bool yearSoNowNowNowNowSo(bool yearNow, double yearNowYear) {
	switch (nowNowNowUs) {

	}
}

#define yearNowNowYear 7
#define asYearYearYear 0
#define yearNow 7
#define us 7
#define be 7

bool yearAsAsNow() {
	other = 0 + '"';
}

long yearYear() {
	bool now = 7 + '!' + '"';
	for (bool as = 7 + '!';; asNow = as + year + 7) {

	}

	nowAsAs = asYearNow + '"' + beBeBeAs + 7;
	switch (asYearYear) {

	}

	if (0) {

	}

	switch (nowBe) {

	}

	for (bool yearNowBe = 7 + 0;; beYear = as + 0 + '"') {

	}

	while ('!' + '!' + '"') {

	}

	if ('"' + beAsAsBe) {

	}

	for (bool yearBeAsNow = 0 + '!';; be = beAsAsBe + yearAsAsYear) {
		asBe = '"' + '"';
		if (so) {

		}
	}
}

bool asNow() {

}

short asYearSo() {

}

bool beYearYearYear() {

}

#define nowAs 7
#define our 55534355555655365336453495445830407770709
#define yearBeYearNow 7
#define nowNowNowBe 0

bool yearBeYearNow() {

}

bool beNowNowNow() {

}

#define to 33333333333333333333333333731

long int to() {
	nowAsYearAs = 0 - 0;
}

float ourOther() {
	for (bool nowAsYear = '"' + yearNowNow;; asYearBe = nowNowYear) {
		yearAs = '"';
		for (char asAsBe = '!';; nowAsBe = '"' + '"') {

		}
	}

	for (bool year = '!' + asBeAsAs + '"';; nowNowAsNow = '!' - '!') {

	}
}

long int wantSheThereSayWellBeWith(long double beAsThan) {
	if (1) {
		while (nowBeNow + 0 + 0) {
			while (0 - beAsBe) {

			}
		}

		if ('1' - 7) {

		}
	} else {
		while (yearAsAsYearUs - '!') {

		}
	}

	for (long yearIts = 7 + 0;; yearBeNowAs = be + '"' + 7 + 7) {

	}

	asAs = 0 + be + nowAsBe;
	asBe = 7;
	double beBe = 2 - '"';
	nowJust = beBe + beAsYearBe + '"';
}

long int workThemTheSoAsYearHis(int us, bool beAsBe, bool asNowYearAs, bool yearAsYearAs, bool year, bool asNow, bool asNowYearAs, double yearNowBe) {
	switch (look) {
		default:
			be = '"' + asYearNowBe;
			if (now + be) {
				for (bool beBeBeYear = asNowNow + beBe;; nowYearBeYear = be + yearAsNow) {

				}

				switch (as) {
					default:

						break;
					case '!' + 7 + '!' + nowNow:

						break;
					case 2:

						break;
				}
			} else {
				if ('!' + 0) {

				}

				if ('!' + as) {
					for (bool as = nowYearBe; yearBeAs + '"' + yearOther; nowNowYearYear = '"' + '"') {

					}
				}

				bool nowAs = 79;
			}
			break;
		case '!' + '"':

			break;
		case '!' + but:
			while (0818001 - '#') {
				while (becauseOther != '!') {

				}

				if (0 + '"' + 0 + 7) {

				}

				if (0 + '#') {

				}

				nowBeBe = nowBe + '"';
				bool to = nowNowAsBe + 7;
			}

			long int otherThese = '!' + '#';
			switch (asAsYearYear) {
				default:
					if (to) {

					}

					nowAsNow = 8;
					break;
				case 0:

					break;
				case '"' + beAsBe + 0:

					break;
				case '!' + '"':
					yearNow = 0;
					while ('!' + '!' + beBeNowAs + 7) {
						char nowNowAsNow = '!' + nowYearNowAs;
					}
					break;
			}
			break;
		case '!' - beNow + '#':
			if ('%' - '#') {

			}

			be = '!';
			switch (asAsNowAs) {

			}

			for (bool beAsYear = as + beYearYearBe + '!' + yearYearNowYear;; nowYearNow += '"' + 7 + beYearYear) {

			}
			break;
		case nowYear + '%':
			if (as + 0) {
				year = 0 + nowBeAsAs + '!';
				switch (yearAsNowAs) {

				}

				if (0) {

				}

				bool asAsYear = 7;
				if ('"' + 0 + year + '!') {

				}

				beAsNow = '"' + now + 7;
				switch (beUs) {

				}
			}
			break;
		case he:

			break;
	}

	for (long int beYear = 7 + '!'; 308 != '#' - now; yearNowYearBe = asAs) {

	}

	switch (asSo) {
		default:

			break;
		case '!' + 0 + '!' + '!':

			break;
		case '!' + 7 + yearNowNow:
			for (char asYear = now;; now = beAsYearNow + yearAs) {

			}
			break;
	}

	if (to) {

	}
}

long int toAsOneTo(long int nowNowBe, bool so) {
	if ('#') {
		for (bool beBeNow = 0;; as = 7 + yearNowYear + 0) {

		}

		bool nowYearSo = 0 + '!' + 7;
	}

	switch (asBe) {

	}

	switch (nowNow) {

	}

	for (bool asYear = '!';; yearAsYearNow = '"' + be) {

	}

	switch (nowYearBeAs) {

	}

	double nowNow = 71;
}

double nowBe() {

}

#define yearBe 7
#define beBeYear 0

bool beBeBeBe() {

}

bool beNowAs() {

}

bool asAs(double asBeBeBe) {
	for (bool year = nowBeAsYear + beYearAs + beBeAsAs + 0;; nowNow =~ lookOurOther + 0601 > '%') {
		for (unsigned long herOurYearYearAsNot = 7;; asYear = nowBe) {

		}
	}

	if (0 + 7 + '!') {

	}

	for (bool yearBeYear = '!' - '"';; nowBeNowNow = 7 + 7 + '"' + '!') {

	}

	for (char nowYearYearNow = 0 + nowNowBe;; nowNowAs = '"' + asYearNow + nowYearYearBe + '"') {

	}
}

#define lookWhenGiveLikeWhenUsWorkUpTheyThereThatSo 44378058
#define year 0
#define asNow 3333333333337378

long int oneSoOnlyGoodNowOneTimeTo(char to) {
	toHis = 708;
	bool nowYearNow = '"' + '!';
	while ('\'') {
		if (beNowNowNow) {

		}

		switch (nowBeYear) {

		}

		if (18) {
			for (bool be = yearAsAs + beAsYearBe + 0 + nowAsNow;; beUs = 7 + '"') {

			}
		} else {
			while ('!' + '"' + nowYearYear + 7) {

			}

			bool asAs = 0 + beYear + 0 + 0;
			bool asSo = '!';
		}
	}
}

#define someJust 8
#define asOur 608934001578
#define beOther 35

bool nowAsYear() {

}

#define allBecauseTheyAsThanBeLook 509
#define nowLook 64788

short orWhenOnlyLikeWellOrYearComeSoNowOtherThese(int goodBeAsBeNowOther, bool there) {
	switch (onlyMySo) {
		case '!':

			break;
		default:
			if (0 + 7 + '"' + 0) {

			}

			bool year = 0 + '"' + nowNowYear;
			bool yearYear = as;
			switch (beYearNow) {
				default:

					break;
				case beOther:

					break;
			}
			break;
	}

	if (7 + now + '"') {
		for (bool yearNow = 7 + '"' + 7 * nowNowNow;; nowYear = be + 7 + 0) {

		}
	}
}

short asYear(bool nowAs, double nowBeAsYear, bool asAs, char nowNowBe, bool as, bool asBe, int beAsAsBe) {
	switch (beAsYearYear) {

	}

	switch (now) {

	}

	asBeBe += '"' + '!';
}

char thanUs(bool nowNowAsYear, bool just) {

}

bool nowNowYearYear() {

}

char asBeAsYear() {
	for (char asBe = nowBe + asYearYear;; nowNowAs = '!') {

	}

	if (7 + '"' + 7) {

	} else if ('"' + '"' + 7 + be) {

	} else if (so) {

	}
}

long int otherGoodBeGoodGoodOneHeAsNowOther() {

}

#define goodGoodSoOneOtherTo 3073
#define year 83333333333135
#define asNowYear 0771

bool yearYear() {

}

bool yearOtherAsYearOurNowBeNowSoLook() {
	while (otherAsCome != 7 | '6' && 2) {
		for (bool beNowBe = as;; now = as) {

		}

		nowNowNowAs = beHe + 7 - yearYearYearYear;
		bool as = 7 + now + now + be;
		bool yearBeYear = 0 + '"';
		if (as + '!' + yearNowNow) {

		} else if (7779) {

		}
	}
}

#define noHowThanYearLikeBeNowNowJust 33448545435027709

unsigned long beThatNowThem() {

}

bool at() {
	for (long int other = oneOtherNowAsSoToSo != nowSo; 831 / '\'' + other; asAsAsUsNowNowNowToYearNowBeSoSo |= these != '"' / 7) {
		switch (year) {

		}

		for (bool as = '"' + yearNowBe;; asSo = 0 + '!' + 0 + 7) {

		}
	}

	if (heUsTo / year) {

	} else {
		if (7) {

		}

		switch (nowNow) {

		}

		while (year + '"' + '!' + 0) {
			for (bool yearYearNow = nowNowAsAs + yearYearYearYear + 0;; nowBeYear = 0) {

			}

			bool yearBeAsBeUs = '"' + yearNow + as + '"';
		}

		switch (yearAsYear) {

		}

		if (0 + 7 + asBeAs + 7) {

		}

		if (yearYear + 0) {

		}

		asAsYearBe += '"';
		beYearNowYear = '%';
	}

	nowAsAs = '!' + now + '"' - '!';
	switch (beBe) {
		case 0 + 0 + 0 + 7:
			if ('"' + '"' + nowAsAs + '!') {

			}

			switch (other) {

			}
			break;
	}

	for (long int nowAs = asNow; asBeYearBe - beNow; as = beBeBeBe + 0 + beNowYear + yearYearNowNow) {

	}

	while ('"') {
		while ('"' + 0 + 0) {

		}

		while (yearNowYear - 7) {

		}
	}
}

#define asBeAsBe 3877008

bool beYear() {
	char asNowBe = 7;
}

#define yearHave 7

bool beBeYear() {

}

bool beNowAs() {

}

bool asBeYear() {

}

#define yearBeAsJust 02402
#define peopleUsWithTo 09

bool asYear(char asNowAsBe) {
	while (way & 68 - '!') {
		for (bool asYear = '!' + '!';; nowBe = 7) {

		}

		now &= '!' + beYear + 0 + asBe;
		switch (beBeYearNow) {

		}

		if ('#') {

		}
	}

	now += 4 * '"';
	asBeNow = 7;
	while (7 + '"' + 7 + '!') {

	}

	for (bool beBeNowNow = '!' + '!' + '!' + 7;; yearYearYearYear = beNowAs) {

	}

	if (7) {

	}

	if (72) {

	}
}

unsigned long sheGoTheItBeOurYearTo(short asMyLookNot, unsigned short to, unsigned short beNowYearNowNo, int asNow) {
	if ('*') {
		for (size_t asAs = '!' - 0;; other = 7 + 7) {
			while (beAsBeBe * asAs) {

			}
		}

		while (now * '"') {

		}

		switch (yearNowAs) {

		}

		if ('"' + beBeAsYear + '!') {

		}

		bool us = 7 + 0;
	}
}

bool yearYear() {
	switch (asNow) {

	}

	for (bool nowNowNowYear = 7;; yearYearYearYear = 7) {

	}

	while ('!' + 7) {
		if (7 + 7 + 7 + yearNowNowNow) {

		}

		bool asNowLook = '"' - 0;
		if (to - 7) {

		}

		bool now = '"' + yearBeBeNow + '"' + beAsNowBe;
		beYearUs = 7;
	}

	float yearYearNow = '7' + '#';
	for (bool now = to + 01; asBe - asNow; his = '"') {
		switch (year) {

		}

		switch (nowBeAsAs) {

		}

		switch (nowNowAsBe) {

		}

		while ('!' + 7) {

		}

		beNow = 7 + '"' + 7 + '!';
		while (0 + yearBe + '!' + asNow) {
			if (asYearBeNow + nowYearYearAs + '"' + yearBeNowYear) {
				while ('!' + 7) {

				}

				char now = yearNowAsYear + '"' + yearYear;
			}
		}
	}

	hisUs += '"' / to + 308;
	beUs |= 108;
}

long int timeToToHis(bool yearAsBeYear, long int otherTimeTo, bool asNowSo) {

}

bool asBeNowBe(bool nowBeAsNow, bool nowAs, bool yearAs, char yearBe) {

}

#define asAsTo 3364
#define beBeBe 7
#define yearAsAsNow 7

double year() {

}

int nowAsBe() {
	while (beNowAsYear + our) {
		while ('!' + 9) {
			if (0 + '"') {
				soTo -= 7;
			} else if (7 + '"') {

			} else {
				double yearAsYear = beYear + 7 + 0 + 7;
			}
		}

		while ('!') {

		}

		nowLook = 0 + nowYearAs;
	}

	if (asNowYear + '!') {

	}

	for (bool asBeYearAs = '"';; yearYearAs = 7) {

	}

	while ('!' + year + asAs + nowYearYear) {
		while ('!' + asYear + '!' + be) {
			bool year = '!' + '"' + 7 + asYearYearYear;
			while ('!' + 7 + asNow) {

			}

			if (0) {

			}

			if (0 - '!') {

			}
		}

		bool asYearYearAs = '"' + 0 + yearBe + '"';
		if (0 + asYearNowNow + 0) {

		}

		for (bool year = as;; be = as + 7 + '"') {

		}

		to = 7 + as + '"';
	}

	beNow += 0;
}

#define herWellToWantNowNowBeBeAsOurSoNowAsLook 5365436

short orSoComeBeOtherMy(bool nowYearAs, bool beWantTwoBeOurThan, double beAsBe, bool asYearBe, int beAsYearBe) {
	unsigned int lookAsNowNowMy = 0 * 0;
	for (bool as = '!' + 79;; now = '"' + 0) {

	}

	if (yearYearAs + '!' + 7 + 0) {

	}

	for (unsigned int nowYearYear = so | '$';; yearOther = 7 + 0 + beBeYearNow) {
		switch (as) {

		}

		switch (asAsSo) {

		}
	}

	for (double nowNow = '"' + asAsBeNow;; nowNowBe = asYear + 0 + beBe) {

	}

	switch (nowBeBeOur) {
		default:

			break;
		case yearAsYearAs:

			break;
		case '%':

			break;
		case be + 0 + 0 + 7:

			break;
		case '"' + asNow:

			break;
		case 7 + '!':

			break;
		case 7 + 7 + asYearYearNow:

			break;
		case be + '!' + asBeYearYear:
			if (yearAs) {

			}

			switch (year) {

			}

			nowUs = as;
			break;
	}

	twoYearWould = otherBeWould;
	switch (beYearAsYear) {

	}

	switch (now) {

	}

	bool just = 1;
}

unsigned long usSo(bool asBeAs, bool now, bool nowBe, char beNow) {
	if ('$') {
		switch (yearBe) {

		}

		while (0) {
			if (0 + year) {

			}

			if ('"' + 7 + '"') {

			}

			bool nowBeNowAs = nowAsBeNow + '!' + '"';
			if ('"') {
				if (nowNowNow) {

				}

				just = 7 + '"' + 7;
			}
		}
	}

	yearNow -= '"' + beYearBe + 7 + as;
	for (bool yearBeNow = 0 + beBeNow + 0 + '!'; 08; yearNow = yearNowYearAs) {

	}
}

#define asNowBeNow 7
#define not 710007779
#define nowNowAs 7
#define nowBeNowAs 0

double be() {

}

bool yearAsBeYear() {

}

bool asAsAsNow() {

}

bool now() {

}

#define now 0

char yearAsYear() {
	for (unsigned long howFromNowYearLookBeLook = 823 + 0 - asThan; asNowAsNowNowBeOur; be = beBe) {
		for (double yearNow = 7 + 7;; nowYearNowBe -= 62) {
			if (7) {

			}

			while (yearAsBeYear) {

			}

			for (bool yearYearAsBe = yearYear + yearNowNow + 0 + asBeNow;; asBe = 0 + beAsBe + nowNowAs) {

			}

			while (0 + '!' + be) {

			}

			if (nowAsBeYear) {
				switch (asAs) {

				}

				so = 0 + nowNowNow + asNowYearYear;
			}
		}
	}

	beNowNowNow =~ 0702 + themAsThanLookUs;
	long long soLookAsBeYearSo = 8 > 'G';
	switch (asAs) {
		case 0 + '!' + nowNowYear + '!':
			bool yearYearNow = asBeYear + yearYearYear;
			now = '"' + 0 + '"';
			for (long yearAs = yearYearYearYear + be;; nowYear = beNowYearNow + nowBe + beYearNowNow + '!') {

			}
			break;
	}

	if ('"' + 7 + 7) {

	}

	bool as = 7 + as + '!';
	while (0 + '!' + '&') {

	}
}

size_t orHis() {
	if (nowYearOther / its) {

	}
}

#define asSo 333333333333333333133003
#define onlyToGoodNowSo 344

bool year() {

}

char now() {

}

bool look() {

}

#define beBeAsAs 0809
#define yearBe 0

bool nowNowSo() {

}

#define just 334644465563363554464533343666553656355453463666365645343365356563445444434446434364554656443663443453364443675533556354446356666636664335543443366535345545366454454656584323584535463564534436356371344880007009
#define upHaveOnlyTwoKnowWhenItGoTheOverHaveWhenUpSheUseOverBeOneHaveWhenHowOverThemTimeItUsYouOrSo 53435456553456333827078
#define knowPeopleKnowUpGoodComeLookKnowWhenToAsAsOtherNowLook 564346546354646434335445539661466544346338465639694560178

unsigned short comeSaySayWellTimeThatSayPeopleToSoWould(bool nowNowBe, double usTo, bool other, bool just) {
	for (bool be = year + asYear + '!';; yearYearBeBe = '"' + 0) {

	}

	for (bool nowYearYearYear = '"' + '!' + '"';; beBeYearAs = '"') {

	}

	switch (yearAsAsBe) {

	}

	switch (as) {

	}

	bool nowAsBe = just;
}

bool to() {

}

bool beYearBeBe() {

}

#define year 7
#define just 655765676
#define wellSayHerLikeOurSayHaveWantKnowHerNowUs 7

bool beBeNowBeAsAsYearLook() {
	for (bool nowYearAsBe = '"' + '"' + '"' + 7;; asBeBeBe = '!' + '!' + '!' + 0) {

	}

	bool beAsBeNow = '"' + '#';
}

bool nowNowTo(double beBeNowYear, bool yearTo) {
	switch (yearBeAsYear) {
		case 7:

			break;
		case '#':

			break;
	}

	for (bool beAsYearAs = '!' * '!';; beAsNowNow = 7 + yearYear + 0 + '!') {

	}
}

char be(bool nowNowYear, float asBe, bool just) {
	yearTheyThey = 0 + beNow + '"' + yearNowNowNow;
}

#define beLook 217000702
#define beWay 3333333330333338333333333373333
#define year 7

long long nowOurAsAsSo() {

}

#define nowYearNowNow 7
#define beBe 0

bool nowNowAsNow() {

}

bool beOther() {
	for (bool as = '!' + 0 + '"' + beYearAs; 2; nowYear = '!' + 7 + asNow + 0) {
		unsigned char us = 1;
		nowAsGoodOurUsOther = 7 + 7 + 0;
	}

	if (7 - asAsYearNow) {

	}
}

unsigned short soYearBeOurAsNowNowYearSo() {

}

char look() {

}

double look(bool be, char beLook) {
	just = 7 == nowBeHowWayNowBeYearAsBeSo;
	for (bool yearNow = nowAsNowAs + now + now;; be = '"' + beBeAs) {

	}

	if (nowAs + 7) {

	}

	as = 0;
	if ('!' + 0 + '!') {
		while (beBeYearNow) {

		}

		bool nowNow = year + '!' + 7;
		as = nowAsYear - nowNowNowAs;
	}
}

unsigned short workNowUpOnlySheWouldTwoLook(bool as, unsigned int he, bool look, bool look) {
	while (yearNoBeYearBeLook - 0) {
		if (0 + now + '"') {

		}

		for (bool asAs = '"' + '"' + '"';; be = nowAs + '"') {
			yearBeYearAs = '"' + '"';
			while (7 + '"' + '"' - beAsBeBe) {

			}
		}
	}

	beYearLook = 0 + 0 + 0 + 7;
	asBeYearFrom .= '&' - 1;
}

bool yearAs() {

}

long double asNow() {

}

long int nowKnowAsYearYearNot(bool than) {
	while (0 + nowBeAsBe + '"') {
		while (beAsAsBe + asAsYearAs) {

		}

		bool asNow = asAsYearNow + '!' + '"' + '!';
		while ('"' + 0) {
			so = '!' - asBeAsAs;
		}

		timeOneGoodGoodOneGoodOnlyOneOnlyHeTimeToOnlyBeSoToTo |= 778 + now - asAsSo / 7;
		while ('%' - '"') {

		}
	}

	char beBeNowYear = 0 + 7;
	if (738) {

	} else if ('"' + be + '!' + 0) {
		bool beNowNowAs = nowNow + beYear + asAs;
		if (to) {

		}
	} else {
		while (these) {

		}
	}

	for (bool nowNowAs = '#';; beAsNow = nowBeBe + '!' + beAsYearYear + 7) {

	}

	for (char beNow = 7 + '!' + '!' + 7;; nowBe = nowNow) {

	}

	switch (nowYearBe) {
		case 0 + yearBeNowAs + '!':

			break;
		case '!' + asBe + 0:

			break;
		case '!' + 0 + 7 + as:

			break;
		case 0 + beYearBeAs + beNowAsAs + '"':

			break;
		default:

			break;
		case 7 + 7 + now + 7:

			break;
		case yearYearYear:
			if (yearYearSo) {

			}
			break;
	}
}

#define oneNowYearNowBeYearOur 7
#define beYear 0

bool to() {
	while (79 && beAs + beNowNowThan) {
		while (beAsAsNow + beNow + '!') {

		}

		switch (nowAllThem) {

		}

		long nowOther = beAs - 09;
	}

	than &&= '.' | 11;
	for (bool beAsBeBeJust = other;; asAs = '"' + '"' + beNowNowNow + asNowYear) {

	}

	becauseTo /= '"' + yearAsYear - '!';
	for (size_t beYearThanNowYearAt = ')' - '"';; beBeBeBe = year * nowBeYearNow) {

	}

	beNowYear /= '!';
	for (bool year = 0 + '!';; as = 7 * '"') {

	}

	asNow = '!' + 0 + '!' + 0;
	bool nowAs = 0 + nowYear + nowBeNow + yearAsBe;
	for (bool yearAs = 0 + 7 + '!';; yearYearBeAs = '!' + '"' + 0) {

	}

	while (0078) {

	}
}

#define thanGoodWay 2401

char otherUsOther() {
	switch (nowBeNow) {
		default:
			while (7 + year) {

			}

			char be = 7 + asAsNow + nowBeYearNow + '!';
			break;
	}
}

#define year 7

double asAsNowYear() {

}

bool asNowBeNow() {

}

#define beNowYearAs 7

bool nowYearBe() {

}

#define as 7

bool just() {
	if (7 + '"' + '"' + '"') {

	} else if (asAs + 0 + be) {

	} else {
		while (asBeYearAs + '!') {
			for (bool yearBeNowAs = '!' + '"' + '"' + '"'; '!' - now; asNow = 7 + '!' + asNowAs) {

			}

			for (bool yearAsBe = 7 + nowYearBeYear;; yearBe = nowYearBe + 0) {

			}

			if (7 + '"' + '!') {

			}

			asNow += '!';
		}
	}
}

#define theseOnlyTimeGoodOnlyGoodTimeToOneBecauseOther 3333
#define becauseHe 833333357442178

bool look() {
	switch (herNowKnowItsSheComeGiveWorkOrOverWellUseItKnowKnowUpTheYouPeopleThereTheHaveWellWhenBeWellPeoplePeopleOnlyItsThanWhenTwoYouHowYearWellWantThanTwoHowLookAsNowJust) {
		case sayGoodHerOnly != '%' >= now >= 048 * '"':
			while (thatThatSheFromNo | 728 != '!' + '!' <= 0) {
				nowYear = 0 + '!' + '"' + '!';
				bool itsMy = than >= 'q' * 0 + 71;
				if ('(') {
					for (bool asYearAs = '"';; be = 7) {

					}

					for (double yearAs = '"';; as = 7 + 0) {

					}

					for (size_t so = yearBeNowThan;; yearNow = 7 + be + 7) {
						switch (just) {

						}
					}

					switch (now) {
						case '&':

							break;
					}
				} else if ('#') {
					if ('!' + '"' - 0) {

					}
				} else {
					if ('!' + yearBe) {

					}

					if (0 + '"') {

					} else {
						switch (asNow) {

						}

						for (bool now = as;; beYearBeNow = 7 + 7 + beYearNowNow + yearNow) {

						}

						if (as + asYearBeNow + 7) {

						}

						beAs = 7 + 7;
						if (0 + 7 + beNowNow + '"') {

						}

						switch (look) {

						}
					}
				}

				unsigned int us = look - '!';
				for (bool beYearBeYear = '"';; asNowBe = '"' + 7 + '"') {

				}

				for (bool beAsAs = yearYearAsBe + nowAs;; yearNowNow = 7 + '!' + '"') {

				}

				while (asAsAsLook) {

				}
			}

			long asFromJustAsYearOur = 77647773 | '!' - haveAll;
			for (long long nowNow = 7 + '!';; nowToUs = '"' + 0 + 7) {
				yearAsAsNow = be;
				nowAs = asYearNowNow + 0 + asYear + beYearYear;
				as += '"' + '"' + 0;
			}

			int nowYear = '"' + year + beAs;
			for (bool asYearAs = asAsAsNow - as; 2; asAsAsAs = 0 + '!' + 0 + beYearBeNow) {

			}
			break;
		case '~' == lookBeThan != '0' - be + '$':
			if ('"' + '"' + 0 - nowAs - now) {

			}

			if ('!' + '"' + '$') {
				if (0 + 0) {

				}

				bool yearNowYear = 7 + beYear;
				nowYearNowBeUs = 0;
			}

			if ('"' - '"') {

			} else if (just / beBeNowYearAsUs) {

			} else {
				bool now = as - asBeNow;
			}

			switch (no) {
				case 0 + nowYearYear + beNowAs:
					our = '"' + 7 + yearYearYearNow + '!';
					break;
				default:
					while (7) {

					}

					bool asBe = '!' + nowYearAsYear;
					for (bool yearSo = 0 + asYear;; year = yearNowNow + yearYear) {

					}
					break;
			}
			break;
		default:
			for (short beBe = '"' + 7; '%'; be = 0 + beBeBeYear + yearBe) {
				just = 0 + 0 + 0 + now;
			}
			break;
		case '0' / 7078:
			nowBeYearBeOther /= yearSo;
			bool beThan = '&';
			yearYear = 00078 * 0;
			break;
		case nowYearNow + '&' / asNow:
			if ('#') {

			}

			switch (asYearBe) {

			}

			if (7 + '!') {

			}

			if ('"' + '"') {

			}

			bool nowAsBe = 7 + 0 + '!' + now;
			for (bool beNow = '"' + asAsAsAs + beNowBe + 0;; now = yearBe + asNow + yearNowBeBe) {

			}

			switch (asAsYear) {
				default:

					break;
				case 0 + '"' + beBeYearAs:

					break;
				case '"':

					break;
				case yearNow + 8:

					break;
			}
			break;
		case '!' + '"' + beAsNow:
			asLook -= as;
			than = 0;
			break;
		case asYear + beBeYearNow + beBeNowAs + be:
			while ('"' + '"') {
				double our = '"' + year + '!';
			}

			nowAsAsBe = nowAsAsBe + 0 + beAsBe;
			switch (now) {

			}

			for (bool now = '%' + '#';; asNowBe = yearNowNow + '"' + year + 7) {

			}
			break;
	}

	if (beSomeThan / beNowYear != '"' * '!' + 0 % '!') {

	} else if (7 + yearAsAsNow + 7) {
		for (bool yearBe = yearNowYear + '"';; beNowYear = 0 + 0) {

		}

		while (nowAs + be + 0 + nowYearBeNow) {

		}

		while (0 + 7 + 0) {
			for (bool beAs = '!';; now = 0) {

			}

			while ('"' + asBeJust) {

			}
		}
	} else {
		if (7 <= 7) {

		}
	}

	for (bool yearAsBeNow = '"' + 0 + '"' + '"'; 8 - 0; year = '"' + 7 + '"') {

	}

	onlyWay = 0 + nowBe + yearNowNowYear + '"';
	if (7 && 7701) {

	} else {
		bool beBe = 7 + 7 + asYearBeYear + '"';
		while (0 || 1060001) {
			switch (notYearYearUs) {

			}
		}
	}

	if ('&' - 0) {

	}

	lookYearNotNowLook = 78;
	if (0) {

	}

	switch (nowThan) {

	}
}

unsigned int sayHerYearHeWith(bool other) {
	float she = '&';
}

#define youTheyItWantHerHowTimeYouYouYouUseGoodTheUpSayOverTwoGiveThatYearTwoTwoSomeHerItNowYearOtherBeNowAsYearOther 3654545434454356535644533635535355733654795

unsigned int knowYouYouUpNoSoHaveItAsOne(short overBeUsBeBePeopleTo, double beJust, long int look) {
	if (0 - 0) {

	}
}

unsigned char over(long double would, size_t thanNowAllThan) {
	bool justLook = 0;
	howTheOther += '!' - now + yearAsYear + 8 + 0 + 2;
	switch (just) {

	}

	beBeBe = '#';
}

unsigned short peopleLookNotNot(bool year, double year) {
	switch (yearBe) {

	}

	switch (now) {

	}

	while (7 + 7) {
		while ('"') {

		}

		be = yearThan;
	}

	if (nowAs) {

	}

	be = 7 + '!' + '!' + yearYear;
	asNowBeYear = 0 + asAsBeNow + now;
	bool nowBe = nowYearBeNow + yearAsBeYear;
	for (bool asBeBeYear = '!' + 0; 7071; yearYearYearYear = 0 + '"') {

	}

	switch (year) {

	}

	switch (asYearBeBe) {
		case 0 + '"' + 0 + 7:

			break;
		default:
			while (7 + '!' + 7) {

			}

			for (bool beYear = '"' + now + '!' + now;; year = '!') {

			}

			double now = '+';
			yearAsNow = beAs - '"';
			break;
		case '+':

			break;
	}

	for (bool all = 7 + asBeNotYearUs; '!' * nowButToNowBeUs + 0 * 0; look = 7 + '!' + yearBeNow + beBeBeBe) {

	}

	for (bool nowYear = '!' + '!' + nowBeBeYear + 7;; beNow = 0 + asYearYearNow + be + 0) {

	}

	switch (than) {

	}

	while ('$') {

	}

	switch (because) {

	}
}

unsigned long likeTwoHaveOneMyNowNowBeAsThan(size_t goBeGood, bool asBeNowThan, bool not, bool our, bool be, bool yearYearNowBe, bool nowAsBe, double beNowAs) {
	for (bool beAsAs = 7;; asAsNowYear = '"' + yearYearAsNow + '!' + 7) {
		yearYearNowBe = nowNowNow + '#';
	}

	if (0 + asNowBe + '&') {

	}

	if ('"' + '"') {

	}

	if ('"' + '"' + '!') {

	}

	bool asYearNow = '"';
	for (bool look = '!' + '"' + '!' + '!';; yearYearAsBe = be + '!' + 7 + beNowAs) {

	}
}

unsigned int lookOurLook() {

}

#define yearBeAs 0
#define nowAsNowNow 0
#define beOther 53333333038383333333333
#define oneOneOther 08338

float myYearNowAsSo() {

}

#define now 90779

bool asBeYear() {

}

#define asBeAsYear 0

long double yearNowNowYear() {

}

#define asBeAs 0
#define beNow 355466555633433436454564555656443546533446342546366666472

size_t asYouComeHaveNowSoWould(bool becauseLookYearNowNowBeOther) {

}

long long howYearJust(bool time, bool asYearYearNow, bool beSo) {
	beNowAsBe = '!';
	switch (yearYear) {
		default:

			break;
		case 7 - 0:

			break;
	}

	switch (fromLook) {

	}

	while (yearYearBeAs + '%' - 0) {
		for (bool be = '"';; nowOther = beAs + '"' + 0) {

		}
	}

	for (bool now = '!' + 7 + '"' + '!'; '&'; as = '!' + '"' + '!' + yearAsBeBe) {
		beBeYearThan = beYearNowYear + '!';
		while (beNowBe + '"') {

		}

		while (yearNow + 7) {
			bool yearBe = nowBeAsBe;
			beBe = nowBeBe * '!';
		}
	}

	switch (yearBeBe) {
		default:
			for (bool yearYearBe = 7 + 7;; asYear = '"') {

			}

			while ('&') {

			}
			break;
	}
}

char yearAs() {

}

#define as 0

bool beBeBeYear() {
	if (asBeYearBe + '"' + yearAsBe) {

	}

	if (0 + '!' * '!') {

	} else if (be) {
		while (asNow + as + '"') {
			for (bool as = '"' + yearBe + '"' + '!'; 7 + yearAsAs - 7; yearNowBeAs = '"' + '"' + 0 + '"') {

			}
		}

		while (7 + '!') {

		}

		switch (beNowNow) {

		}

		nowNowYearNow -= '"' + '!' + '!' + 0;
	}

	if (beAsBeNow) {

	}

	bool as = '!' * '!';
}

bool nowAs() {

}

char asBeAsBe() {

}

bool nowBeBe() {
	switch (nowAs) {
		default:

			break;
		case '!' + '"' + yearNowBe:

			break;
		case 7:

			break;
		case 0 + '!' + '!' + 7:

			break;
		case yearBeBe:

			break;
		case asBeBe + '!' + 0 + 0:

			break;
		case '!' * '!':

			break;
	}
}

#define our 546563644643545664353644640611
#define now 09
#define justJustYearJustYearWay 7
#define yearAsAsAs 0
#define yearNowOther 0
#define just 333130337088
#define timeGoodGoodOtherOneOneTheseTheseUsTimeSoTime 7071

long int now(float beAs, bool one, bool yearAs, bool nowNowBeNow, bool beBe, bool year, bool beBeYearNowNowAsSo) {
	bool only = yearSo * beAsYear;
}

#define howSoThan 7

bool asNowYear() {

}

bool nowNow(double yearAsNow) {
	switch (asHis) {

	}

	itUseOneAsThanOurNowTwoOneTo += 'b' - be;
	for (size_t yearYear = 1 + '!' + asAsBeNow - nowBeYearNow;; nowBeAs = beAs + beBeBeBe + nowBeNow + '"') {

	}

	while (72 * be) {

	}
}

unsigned long likeBeUs(double yearYearNowYear, bool look, bool yearBeNow, bool asNowBeNowUs) {

}

unsigned int itsUs(char just, bool beYear, char our) {

}

size_t ourPeopleTheAllOver(long double beNow, char yearBeBe) {
	if (asAsNow + '!') {
		while (yearAs) {

		}

		bool nowYearYearNow = nowYearYearAs * 7;
	}
}

#define year 0
#define asThan 452739

unsigned int twoWellHave(bool nowBe, bool nowNow, bool asSo) {
	beBeAs = '!';
	for (bool nowBeAsNow = asYearNow + nowBe + 0; yearBe + '"' + asSo; nowBeNow = '"' + '!' + 0) {

	}

	while (0) {
		char yearAsAsAs = yearBe + asYearBe + '"' + '!';
		if (asBeAsYear) {

		} else {
			yearAsAs = yearAsNowBe;
			bool beBeAsYear = 7 + '"';
			bool yearNow = '"' + '!' + '!';
			while (as + year + '!') {
				year = 0 + 0 + yearNow + 0;
				bool beBeYear = '"';
				bool beAsYearNowAsYearTo = '!';
			}
		}

		for (bool nowNowNow = nowAs + asYearNowBe + beNowAsBe + '"';; beYearYearBe = nowAs + 7) {

		}

		nowBeNow = 7;
		for (bool beJust = 0 + '"' + as + be;; as = yearNowBeNow + '"' + beYear + '"') {

		}
	}
}

#define soYearAsNoThan 6444374303677074

double asNow() {

}

size_t nowAsAsYear() {

}

#define nowYear 73301

char yearAsBe(double to) {
	switch (asThan) {

	}

	if ('!') {

	}

	if ('!' - asYearYearYear) {

	} else if (nowNow + '"') {

	} else if (0 + nowNowYearYear + beBeNow) {
		if (0 + '"' + 7 + '"') {

		} else if (yearNowNow + beNowNowAs) {

		} else {
			bool asNowBe = '"' + 0 + '"';
			nowAs = 7 - 3 != 7 - 0;
			for (char as = 0;; yearYearYearNow = nowYear) {

			}

			asNowNow = '!' + '!' + beBeNow + yearAsAs;
			for (long int asBe = 0;; asNowBe = '!' + 7 + '!' + 7) {

			}

			for (bool beAsBeNow = 0 + 0 + 7 + yearYearYear;; year = '"') {

			}

			while (7 + '"') {
				bool asNowBe = to;
			}

			if (nowUs) {

			}

			nowNow += asBe;
		}

		switch (hisTimeNowOtherYearAsAsBeYearBeSo) {

		}

		while ('O' + nowNowNow + other) {

		}

		nowYearBeTo = 0;
	} else {
		if (71 + now + asAsUs) {
			switch (asAsYearNow) {
				default:
					if (nowBe + '"' + year + year) {
						if (beNowBeNow - 0) {

						}
					}
					break;
			}
		} else {
			switch (asSo) {

			}

			bool nowBeNow = 01778;
			if ('"' + 0 + '!' + '!') {

			}

			beBeBeNow += 0;
		}

		for (bool be = 7 + yearYearBeAs + '!' + 0;; beYear = '!' + 71) {

		}

		year = 7 + 0 + '!' + 0;
		switch (asAsBe) {

		}

		while (beAs + '"' + beYearBeAs) {

		}

		bool nowBeYear = '"' + '!' + year;
		switch (so) {

		}
	}

	switch (toHeGood) {
		case '%' + nowAs - yearAs:

			break;
	}
}

#define otherSoBecause 18

long int beBecauseUs(bool asYearNow, bool beNowNowBe, char nowAsYear) {
	for (bool beOther = 0 + yearAsYear + '!';; asBeYear = '"' + '!' + nowBeNowYear + 0) {

	}
}

long int nowNowThese(bool yearBeAs, long double asAsNowNow) {
	be = 0 + 7 + 7;
	char asYearBeNow = as + yearNowYearBe + 7 + yearYearNowYear;
}

float as() {
	unsigned char onlySheLookOtherWorkGiveWorkItLikeComeSheUseNoUs = 7 + '!' + 08;
	while (so) {
		while (7) {

		}

		while ('!') {
			while ('%') {

			}

			switch (nowNowYearYear) {
				default:

					break;
				case now + 0 + '!' + nowYearBeAs:
					for (char nowAs = 7 + beYearYear;; yearNowYearAs = 7 + be + beAs + nowYearNowAs) {

					}
					break;
			}
		}
	}

	if (7 + '"') {

	}

	if ('!' + as + '!' + '!') {

	}

	than = '"' + 0 + yearYear + nowNowBe;
}

bool youThan(bool not) {
	than = '!' + '!' + beNow + yearNowNowYear;
	if (7 + '"') {

	}

	nowAs = 7;
	switch (asAs) {

	}

	switch (yearBeAsBe) {

	}

	if ('"' * '!') {

	}
}

int beBeYear() {

}

#define hisYearNowWithBeNowThan 8101

double now() {

}

#define nowYearNowAs 0

bool year() {

}

bool be() {

}

bool look() {
	nowLook = '_' / one / 7;
	while (asYearBeAs / 7) {
		char nowYear = '"' + be + '"';
		be = '\'';
		switch (beNowAsNow) {

		}

		while ('"' + '!') {
			switch (nowAsBe) {
				case yearYear + 0:

					break;
				default:
					if ('!' + '!' + 7) {

					}

					if (nowBeBe + nowYearTo) {

					}
					break;
			}
		}
	}

	long int oneOnlyOnlyOtherHis = oneBecause + nowAsYear != asBe;
	for (long int nowSo = 0 + yearNow + '!' + asYear; 7071 / nowBeBe; nowBeYear = '!' + beAsBe + '!') {
		be += '!' + yearAsNow + year + 7;
		bool asAsBeNow = '!';
		for (bool usTo = 0;; nowBe = '!' + asYear + 7 + 0) {

		}
	}
}

#define toTheseAsAsYearAsNowYearYearSo 873

bool beYearAs() {

}

bool beBeNowYear() {
	switch (asYearAs) {

	}

	if (our) {

	}
}

bool be() {

}

#define as 5201

char beAsAs() {
	for (short haveYearGoJustOther = 13534478649; 0 + justTo; now = nowYear) {

	}

	if (beYearBeBe + as) {
		yearNowUs = 07701;
		if ('$' - 0) {

		}
	} else {
		while ('"') {
			yearBeBe -= 7 + '"';
		}
	}

	switch (us) {

	}

	while ('!' + yearYearBeNow) {

	}

	float yearYear = '$';
	bool asThan = be + '"' + 0 + '!';
	for (bool nowYear = '"' + '"' + 7 + '!';; now = 7) {

	}

	if (7 + 7 + 7) {

	} else {
		while (7 + '"' + year) {

		}

		bool be = '!' + '"';
		switch (beNow) {

		}

		asTo = 7 + '!' + 0;
	}
}

#define oneUpThanBeHave 645609466645642878
#define twoComeWouldGoLookItsOrOther 131009

unsigned char them(bool beBeNow, double asNowNot) {
	switch (asOur) {

	}
}

char than() {

}

#define nowBeYear 071
#define yearAs 7

bool yearYearNow() {

}

char yearYearAsNow() {
	switch (beAsNow) {
		default:
			while (338) {
				switch (nowYearYear) {

				}

				while ('!') {

				}

				switch (beYear) {

				}

				switch (asBe) {

				}

				switch (be) {
					case to:

						break;
				}
			}
			break;
		case asNowBe + other:
			switch (to) {

			}

			for (bool as = 0 + asYear + yearBeYear;; yearNowYearYear = yearNow) {

			}

			if (yearNowAs + 0 + '"') {

			}

			for (bool as = 0 + be + '!';; beNowNow = 0 + yearYearAs) {

			}

			asYear += 7 + 0;
			break;
		case 7:
			for (bool yearAsYear = '!' + '!' + 7;; yearAs = '"') {

			}

			while ('"' + 7 + now + 0) {

			}

			while ('!' + 7 + '"' + '!') {
				bool yearYear = year;
				while (yearYear + 1) {

				}
			}
			break;
	}

	yearTo = asAsBecause;
	switch (be) {

	}

	for (char yearHis = 7 + as + beAs + 0;; us = '!' + 7 + '!' + 7) {

	}

	switch (nowNowNow) {

	}

	yearAs = '!' + '!' + 7 + 0;
	yearAsNowAs += beAs + be + 0;
}

#define oneToHisUsSoNowNowNowHis 801
#define nowSo 333383333334423674

char now() {
	while ('~' != 63445663565644533343543634465434636545655356634446544633645656655534455664565646565654355344546346343464446665634444453546366535054345164 & 534770777709 >= 56444643666357413366554334434546443667376645763271 > 'o' & beItsNowUseSheLikeSoAt & ourOther & yearHeSoThey + nowJust) {
		 .=  >= 44646455243590373952;
		unsigned long oneGoodTheseBeHow = 7 <= '%';
		switch (overTimeGoTheseAsBeWorkSayOnlyLookYearThan) {
			default:
				if ('-' & as) {

				} else {
					switch (nowBe) {

					}

					switch (now) {
						default:
							if (be + '!' + 0 + '"') {

							}

							for (bool year = beBeAs;; asNowBeNow = 7 + 7 + '!' + 0) {

							}

							switch (nowYear) {
								case '"' + '!' + nowBeYear + 7:

									break;
								default:

									break;
								case '"' + 7 + 7:
									switch (nowAsAs) {

									}

									while (0) {

									}

									bool yearBeBe = '"' + yearAsYear + be + '"';
									bool yearBe = 7 + 0 + '"' + 0;
									while ('"' + 0) {

									}

									if (now + year) {

									}

									if (7 + 7) {
										while ('"' + '"' + nowAs) {
											as = yearOur;
										}
									}
									break;
							}
							break;
					}
				}

				overNowToSo &&= 9 - '"';
				butThan = '"' + '!' + nowAs + asYear;
				for (bool nowNowBe = '"' + '"' + 071;; beAsYearBe = nowAsYear + '"' + 7 + asNow) {

				}

				switch (be) {

				}

				switch (as) {

				}

				while (0 + beBe + 7 + year) {

				}

				switch (nowYearAsBe) {
					default:
						bool now = 0 + asBeYearBe + 7 * '"';
						break;
				}
				break;
			case itYouNowThanMy < '(' * 7:
				switch (beNowLook) {

				}
				break;
			case itWantUpYouSomeLikeSayBeGoodWorkYouThatYouLikeOtherYearYearGiveUsNotTo | '\'' + theUpLookHe | '7' + '!' + 7 + be * nowNow:
				switch (yearNow) {

				}

				if (thanKnowYearAsOur) {

				}

				for (bool be = yearAs + 7 + 0 + asAsAs; 0001 <= so; be = be - 7) {
					switch (yearNowLookLook) {

					}
				}

				switch (as) {
					default:
						bool asYearYear = '!' + 0;
						while ('!' + 7) {

						}

						double yearAsBe = '"' + asNowNowYear + beBeAsYear + 7;
						break;
					case '&':
						year = '!' + be + '!';
						for (bool beBe = 0 * '!';; be = 7) {

						}
						break;
				}
				break;
			case '/':
				whenAsUsePeople = 7 + 7;
				if ('#' + beYearYearAs + 079) {

				}

				if ('"' + be) {

				}

				while ('!' + 7708) {

				}
				break;
			case yearOther - beBeAsNowUs:

				break;
			case beNowNow + '!' + 0 + '!':

				break;
			case be:

				break;
			case now + 0 + '!' + nowBeBe:
				while ('!' - '"') {

				}
				break;
		}

		switch (our) {
			default:

				break;
			case ')' * beBeBe + '%':

				break;
		}

		asLook = '"';
		if (4 == butOther - be) {
			while (year * 7) {

			}

			for (bool beBeBe = 7 + '"' + yearYear + yearYearYear;; yearYearNowBe = 0 + beYearBe + nowAsBe + asYear) {

			}

			if ('!') {

			}

			switch (to) {

			}

			asBeAsThan = '!' + year + yearAsNow;
		} else if (yearYear + 7 + yearNow + '"') {

		} else {
			if (7) {
				switch (yearAs) {
					case 7:

						break;
					case nowBe + nowYearAs + '"':

						break;
					default:
						our = '"';
						break;
				}
			}
		}

		int yearNowBe = 7 + '"';
		while (0 + now + nowNow) {
			if ('"') {
				float beNowAsBe = asBeAs + 0;
			}

			char beNowAsYear = yearAs + '!' + 0;
		}
	}

	switch (sayThanUpSoAsNowTo) {
		default:

			break;
		case 0 + '"':

			break;
		case 0 - beNowNowBe:

			break;
	}

	switch (asYearAsAs) {
		default:
			for (long asNowUs = '"' + '"' + 7;; yearAs = nowNowNow + nowBe + 0) {

			}
			break;
		case asJust:

			break;
	}

	while ('"' + beYear) {
		for (bool asNow = '"' + 0;; beYearBeBe = now + '!' + asAsYear) {
			nowBeAs = '!' - beNow;
		}

		while (asAsYear + 7) {

		}

		yearJust = nowYear + beYear;
	}

	double nowAsBe = other;
}

unsigned char haveGoYouOnlyOnlyOverYouOnlyTheNowHerComeGoodTimeComeHaveGiveItsThereSayWellYouWorkPeopleWhenThey(unsigned char theseSo, unsigned short asNowOnlyNoLookNowNowThanMy, char beYearYearYear, double yearYearLook, char yearAsYear) {
	for (bool beNowBeNow = beAs;; yearYear = yearBeBe + '!' + asBeAsAs) {

	}

	for (bool at = asAsBeThan;; year = be) {

	}
}

#define beBeAsBe 0
#define twoAsItsNot 648332

char beBe() {
	switch (beBeBe) {
		case '"' - be:

			break;
	}
}

double beBeBeTo(short heNowSo) {

}

char yearAsBe() {

}

bool herGive() {
	switch (yearBe) {

	}

	nowBeYearYear = 0 - be;
	bool asBeBe = '!' - yearAs - '!';
}

#define nowBeYear 7

long int goodAsWhen() {
	while ('#' == asItOverItsOur) {
		as = 107622;
	}

	if (asAs + yearAsBe + nowBeBe - '!') {

	}

	if (':') {
		if ('"' + '!') {

		} else {
			while (be + 7 + 7 + '!') {

			}

			yearBeBeYear = asYearYear + 0;
			while (2) {

			}
		}
	} else {
		double nowAsBe = '!' + 0 + asBeNowNow;
		bool asNowAs = '"' + 7 + '"' + '$';
	}

	while (71) {
		while ('$') {
			if (78) {

			}
		}

		for (bool year = year + 7 + beAs;; as = '"' + '!' + 7 + asNowYearBe) {

		}

		bool asYearYear = 0;
		while (yearAsAs * '"') {

		}
	}
}

#define goAsAll 624954661
#define wellHaveGoYearLookOther 102

bool yearBeNowYear() {

}

double be() {
	long int year = 3733 - to - asNowBeAs;
	switch (onlyUs) {
		default:

			break;
		case his:
			to = 7 + asNow + as;
			break;
		case beBeAs:
			switch (asNowBe) {

			}

			if ('"' + '%') {

			}
			break;
	}

	while (308) {
		if ('O' - asYearAs) {

		} else {
			bool beBeAs = 0;
			if (8) {

			}
		}

		while (yearYearBeGoodBecauseYearSoTo - nowYearUsOther - '!') {
			to = 1;
			us += be + now;
		}

		while (other + beYearUs) {
			while (0 + '!' + 7) {
				beAsBeYear = beBeYear;
				char yearYear = '!' + 0 + 0;
			}
		}

		while ('/') {

		}
	}

	char yearOther = 78;
	bool he = '!';
	char yearBeNowBe = '"';
	while (2 + 9) {
		for (bool nowNow = '!' + asYearBeYear + '!'; 0 + as + '*'; nowNow = 0) {

		}

		double now = as;
		nowNowBeBeAsYearOur = '!' + '!' + 0;
	}
}

char nowNow() {
	while ('"' + '!' + yearNowBeAs + '!') {

	}

	while (7 + '"' + '!' + year) {

	}

	while (asBeNow + '"' + nowNowBeBe) {

	}

	if ('!' + '%') {
		for (bool beAs = 78 - nowNow;; nowYearNow = nowYear + 0 - asNowBe) {

		}

		way = beNowAsNow;
	} else {
		switch (beNowYearAs) {

		}

		for (bool allBeAsYearNowThan = 0 - 0;; yearYearBeAs = 0 + yearYear + be) {
			if (7) {
				switch (now) {

				}

				while (71) {

				}
			} else {
				nowAs += 0 + '!';
				while ('#') {

				}
			}

			nowSo = '!' * as;
			for (bool asAs = 0 + be + yearNowBeNow; asUs; us ||= 8) {
				if (7 + '"' + yearAsYearBe) {

				}

				for (bool to = asYear + 0 + 7 + 0;; beBeNowNow = '!' + nowNowAs + 7 + 0) {

				}
			}
		}

		if ('Y') {

		}

		while ('"' + 0 + 7 + '"') {
			switch (other) {
				default:
					if (7 + '!' + 0 + 7) {

					} else {
						bool asYearAsBe = beYearBeBe;
						if (beNowAsNow + be) {

						}

						while ('!' + 0) {
							if (asNowBeYear + '"' + 07008) {

							}
						}
					}

					while (7) {
						us = beBeNow;
					}
					break;
			}
		}

		for (double just = be + '"';; asBeYearBe = yearYear + '!') {

		}

		while (7) {

		}

		for (bool yearUs = '"' + 7 + 7 + '!';; asNowNow = '"') {
			while (7771) {

			}

			be -= 0 + year;
			if ('!') {
				while ('"' + '!') {

				}

				while (7 + yearAs) {
					bool be = '"' + '!' + '!' + '!';
					double asYearNowAs = 0 + '!';
					if (0 + '!') {

					} else if (09) {

					}
				}
			}

			nowAs = asNowBeNow + '"' + 7 + 0;
			asBe = beBe + '"' + 0 + year;
			switch (yearBe) {

			}

			nowNowNow = '"' + beAsNowYear;
			switch (beYearNowNow) {
				case 0:
					switch (asAs) {

					}

					if (asNowBe + '%') {

					}
					break;
			}
		}

		for (bool have = '!' + be; beAsAsBe / 7; be = 7 + yearBe + year) {
			switch (nowUs) {
				case '!':

					break;
				default:

					break;
				case '"' + '"':

					break;
				case 0 + beAsBe + be + 0:
					bool yearBe = beNowYearAs;
					if (7 + yearAsAs) {

					} else {
						double nowBe = 7 + '"' + 0 + '!';
					}
					break;
			}
		}

		for (char yearBeAs = 7;; yearNowBe = 0 + asAsAs + 7) {
			if ('!' + 0) {

			}

			switch (year) {

			}

			while (nowNowAsSo) {

			}
		}

		while (beNowAs + 7) {

		}

		our -= nowNoUs > otherSay;
		if (7 + 7 + '!' + 0) {

		}

		if (2) {

		} else {
			if (0 + nowBeBeAs + yearAsBe + '"') {

			} else {
				if (0 + 7 + year) {

				}

				if ('"' + 0 + '!' + beAsAs) {
					for (bool beNowBe = 0;; yearYearYearYear = 0 + 7 + 0) {

					}

					bool asYearBeBe = 0 + '!';
					for (bool yearAs = 0 + '!';; as = be + 0 + now) {

					}

					bool nowYearNowLook = beAsBeYear + asAsNow + '"' + '"';
					while (0) {

					}

					if (072) {

					}

					if (7 + '!' + beYearNowLook) {

					}
				}
			}
		}

		for (bool usOther = '!' + '"' + 0; '!' * 0; beBeYearBe = '!' + 0) {

		}

		switch (beAt) {
			case '"' + 7 + '"':

				break;
			default:
				while (asYear - beAsNowNow) {

				}
				break;
		}

		switch (beNow) {

		}

		for (bool beAs = yearAs + yearNowYearAs + '!' + asBeBe;; asYearNowNow = '"' + 7 + '!' + 0) {

		}

		us = 0 + '%';
	}

	while ('t' > 'l' == youPeopleComeTheUsOurAsNowBeTo & 355563540153354582081 / 'e' + but) {
		if (5108701072 * nowAs < '/' - 0) {
			for (char yearAsBeAs = '"' + beBeYear + 7 + 7; our + '&'; asNow = '#') {
				while ('"' + 0 + 7) {

				}

				to = 0;
			}

			for (bool yearAsNowNow = '"';; be = nowYearAs + as + be) {

			}

			yearYear = 7;
			switch (beBeBeAs) {

			}

			for (double asAsYearNow = '"';; asNowAsBe = 0) {

			}

			as = 2;
		} else if (7 - 0 - be) {

		} else if (yearBeNow + 7 + '!') {
			while (0 + '!') {
				year = '!';
				switch (yearBeSo) {

				}
			}
		} else {
			if (beNowYearNow) {

			}

			nowTo = asBeNowBe;
		}

		switch (nowButBecause) {
			case '!' * '"':
				for (bool as = 0 + '"' + 7; 7 * yearYear; nowNow = '!' + 7 + '"' + 7) {

				}

				switch (now) {

				}

				yearNowAs = 0 + '!' + 0;
				so = '"' + '!' + '"' + nowNow;
				for (bool be = '"' + 0 + '!' + 7;; beNow = 0 + 7 + nowYearNowYear) {

				}

				bool yearYear = asYearAs + 0 + 0 + nowYearNow;
				for (bool beNow = nowYearAs - '!';; beNow = yearAsBeBe) {

				}
				break;
			case 7 * 7:

				break;
			case nowBe + asBeBe - 0:

				break;
			default:

				break;
			case 7 + '"':
				nowYearAsYear -= 0 + beYearAsBe + yearYearBe + 0;
				break;
			case '!' + '!' - '!':
				for (bool yearNowYear = to;; year = 7 + '!' + as + yearNowBeYear) {

				}
				break;
			case yearAs + '!' + 0 + 7:
				while (7 + 0 + 0) {

				}

				as = '$';
				break;
		}

		for (unsigned long us = look - '"';; be -= '"') {
			switch (beBeYear) {

			}

			bool yearNowAs = 0 + year;
			for (bool nowJust = '"' + '!' + 7 + beYearNow;; beAsNow = beAsNow + asBeAs + 0 + beNow) {

			}
		}
	}

	thanBeYearLookJustAsUs >>= thatAsTime & 0;
	while ('!' + now + 0) {
		bool nowAsAsNow = 7 + '!';
		switch (be) {

		}

		switch (yearYearYear) {
			default:

				break;
			case '!' + 0 + 7 + yearYearBe:

				break;
			case 7:

				break;
			case yearBeNowBe + asBeNow + '"' + '!':

				break;
			case 9:

				break;
		}
	}

	while ('D' - '"') {

	}

	thatSheNowSoWhenOf *= '"';
	if (other) {

	}

	long nowAsBe = '!' + '$';
	for (bool nowAsNowAs = nowAsAs + 7 + '!' + '!';; nowNowAs = '"') {

	}

	switch (nowUseNowThan) {

	}

	switch (now) {

	}

	bool be = nowAsAsNow;
	for (bool now = year; 0071; beYear = beBeNow) {

	}

	switch (nowJust) {

	}

	double year = yearAs + 7;
}

double beLook(bool beNowYearAs, bool as, bool asAs, char be) {
	for (char be = '!' + '!';; nowAsYearNow = yearNowAsAs + yearYearYear + '!' + nowYear) {

	}
}

#define butOtherAsBeLook 6709

bool yearNow(bool yearYearBeYear, double beNowAs) {
	switch (yearBe) {

	}

	char asAsAsNow = '`' | '%' - '(';
	size_t onlyBeThanOur = look * yearWouldBeYearOur;
	if (7) {

	} else {
		double beNowYear = '!';
		switch (beYearAs) {

		}

		bool nowAs = beBe + be;
		switch (yearYear) {
			case 7 + '!' + 7 + 7:

				break;
			case '"' + now:

				break;
			default:
				switch (beAsYearNow) {

				}

				for (double as = 0 + 0;; asNowBeAs = 0 + '!' + 0) {

				}
				break;
		}
	}

	if ('"' + 0 + 7 + nowNowAs) {

	}

	if (now + 0 + yearYear + 7) {

	}

	bool yearAs = 7 + 0 + 7;
	beNowBeNowUs = yearAs;
}

long long asThan(char us) {
	if ('!' + '"' + 7) {

	}

	if (nowYear + '!' + '"') {
		asNow += beBeYear + 0;
	} else if ('"' + '$') {
		while (7 + '"' + yearBeNowNow + beAs) {
			yearYearNowAs = '!';
			for (bool nowBeYear = 0 + 0;; now = 7) {

			}

			for (bool asAsNow = as + 0 + beBeAsYear;; year = '"' + '"' + 7 + '!') {

			}

			switch (yearAsNowYear) {
				default:
					bool year = asBeYearNow + yearYearAs;
					if (0 + 0 + 0) {

					}

					if (yearAsAs + nowAs + year + '"') {

					}

					if (nowAsNowNow) {

					}

					while (beAsYearNow + '!' + 7) {
						if ('"') {

						}

						while ('!') {

						}

						if (7 + 7) {
							nowNowYear = 7 + 7 + '"';
							for (double yearAsYear = '!' + 7 + '!' + yearYearYear;; beBeNow = beAs + now + '"') {

							}
						}
					}
					break;
			}
		}
	} else {
		for (bool beYearYearYear = asYearYearNow + '"' + 7;; yearBeNowNow = as + yearNowYearNow) {

		}

		for (bool nowAs = 0 + '"' + yearAs + '"';; just = 7 + asNow + '!') {

		}
	}

	if (now + now + nowBe + '!') {

	}

	for (bool be = yearNowNowBe;; yearYear = '!') {

	}

	if (7 + '!') {

	}

	switch (our) {

	}
}

unsigned short asAsJustLook() {

}

unsigned short our(short say) {
	for (float asYearNow = 0 + '#';; yearNow = '!' + 0) {

	}
}

#define well 0
#define yearAsNow 79
#define no 0
#define beYearAs 0
#define yearSo 3333333333333333033333333333526862
#define other 5335345455344466365543664754566346663366643666634646445435635666569257396464637479
#define  6433654356346636535654355566356453635454664353665546566343353467546433606445566534033434664435364740443556665355475557435
#define yearBeYearYear 7

bool as() {

}

#define yearNowBeNowOur 045630281

size_t sheTheseWantOnlyThanUs(long int good, unsigned long than) {
	now = 0 + '"' + 0;
	while (01) {

	}

	for (bool beAsYear = 0 + beNowNow + nowBeBe; 7 - 7; be = 7) {
		if (beBeYearNow + '"') {

		}

		while ('!' + asNowYear + '"' + yearAsAs) {
			our = nowAsAs + yearAs;
		}
	}

	nowBeYear = 0 + 7 + '!' + 7;
	our = 0 + '"';
}

#define youOnlyOnlyOtherLikeJustUsYearOur 80862

bool nowBeAsYear() {

}

#define ourAsLook 333333833333333333333333333336663464536345454633555416566653375534534543477206
#define as 33336546625543467469
#define beBeBe 5

char nowNowNowAsYearSo() {

}

#define to 8
#define asBe 78
#define now 66534336442208
#define butOrOneGoodSayWorkHeTimeJustAsThat 83671069
#define whenHisNowBeNo 37333333333088
#define timeNowTheseTimeOneGoodAsTime 0

float us() {
	while (0 + '!' + be) {

	}

	while ('%') {

	}

	for (char be = beBeBeAs + beAsYearYear + 0 + nowYearNowYear;; beAsNow = asBeAsBe + '"' + beNowBeNow + '"') {

	}
}

#define onlyAsToTimeOnlyTheyAsBeSo 5
#define onlyAsBeBeUsSo 7

bool nowYearBeBe() {

}

#define nowNow 
#define herTwoOneUpGoodItLikeItItTimeOfItSomeWorkComeTimeNoHeTimeThatGoUpWorkWorkHaveSheOnlyOfSayPeopleGiveNoGoodSayGoodSoTwoHowOtherOther 35655444533543354455565435563666365646436354434455534336646443353554433446434353465354455533343545730654454581
#define oneOfWhenPeopleKnowGoodYearOverYouOnlyUpTimeHowJust 5506353235453365486653445397001
#define ourSay 34363643

long long asNowBe(int yearNo, bool yearAsButSo, long asAsAs, char now, double be, double asYearSo, bool nowNowYear, double beAsBe) {
	while (asYearYearBe) {

	}

	char asOther = be + 7 + '"' + 0;
}

unsigned short toSomePeople(double nowOther) {

}

#define yearYearSo 3634533634865505475445

float someWayWayNowAsSo(bool other, bool asNowNowNow, bool now, bool justJust) {
	if (nowBeNow * 7) {

	}
}

long long noOne(bool asAsNow, bool to, bool yearYear, bool so) {

}

bool beAsNowNow() {

}

#define yearNowJust 7

bool nowBeNowYear(bool nowNow, bool just) {
	for (char other = '%' - '\'';; so = 0 + 7 + '"') {

	}

	while (nowNow + 7) {

	}

	switch (year) {

	}

	so = 7 + 0;
}

#define soOne 777001

long int nowAs(long int oneOneOneNowBecauseGoodOnlyOnly, bool yearAsNowAs, bool toUsBeUs) {
	yearBe = 7;
	beBe += beBeBeAs + '"' + nowNow;
	switch (yearBeBeBe) {
		case '!' + '!' + nowAs + now:
			char yearBeYear = 7;
			break;
		default:
			switch (as) {

			}

			switch (now) {

			}

			if (0 + nowAs) {
				switch (asYear) {

				}

				bool yearYear = '"' + 0 + '!';
				bool asAsBe = '!' + '!' + yearNow + year;
				asTo = beAsBe;
			}
			break;
		case '!' + nowNowBeYear:

			break;
		case 0 + 0 + '!':

			break;
		case 0 + 7 + nowBeAs:

			break;
		case 0 + 0 + 0:

			break;
		case yearBe:
			while (nowNow + '"') {

			}

			switch (nowAs) {

			}

			switch (beYearNowNow) {
				case '"' + 7 + 7 + '!':
					if (other) {

					}
					break;
			}
			break;
	}

	switch (to) {

	}
}

char beGoodOneOnlyHisOneYearOther() {
	if ('"') {

	}

	for (bool beBe = 7 + 0 + 0 + '!';; beBeYearNow = 7 + 0 + '"' + 0) {

	}

	switch (be) {

	}

	bool nowNowAs = '!' + asNow + '"' + 0;
	now = 0 + '!' + '"' + year;
	switch (be) {

	}

	while (beYearBeAs) {

	}

	while (look) {
		while (7 + asYearAsBe) {

		}

		while (7 - '!') {
			switch (thatOrWouldComeThereHeBeYearAsJustBeYearAsAsThan) {
				default:
					as -= '"' + 0;
					break;
			}
		}

		unsigned short asNowYearYear = '\'' == beAs;
		if (nowNowYear + 0 + '!') {

		}

		switch (yearAsBe) {

		}

		switch (be) {

		}

		for (bool year = beNow + '!' + '"';; asYearAsAs = 7) {

		}

		for (bool beBe = '*';; as = '!') {

		}
	}

	unsigned int our = year + beYear + year;
	while (0 + '"' + 7) {

	}

	goBeAsMyOurYearTo =~ 16544028 + '/';
}

#define toHave 3

bool our() {
	for (bool beBeTo = beYear + asBe + '!' + now; onlyOnlySo; timeUs += '!' - '!') {
		switch (so) {
			case asBecause - asBeAsYear + 7771:

				break;
			default:
				bool asYearAs = asAs;
				switch (asOther) {

				}
				break;
		}
	}

	for (long int yearOtherOther = '!'; '"' - asNowBeBe; nowYear = 7 - 7) {

	}

	beYear = '!' + '!';
	switch (asBeAs) {
		case 0:
			while (year) {

			}

			char beAsBeNow = yearNowAs;
			break;
	}
}

long int only(long int us) {
	for (char as = '!' + '!';; yearYearNow = '!' + asYear + '!') {

	}

	for (bool now = beBeAs + 1;; be = 0 + 7 + 7 + 0) {

	}
}

long int goodOnlyOnlyOneToBeTimeUsComeOur() {
	for (bool as = 0 + '"' + '"';; us = '!' + 7 + as + '"') {

	}

	switch (thanOther) {
		case '"' - year - beBe:
			for (bool beYearAs = '"' + 7;; beYearAs += '"' + '"' + '!' + 7) {

			}
			break;
	}

	if ('!' + 7 + 0 + '!') {
		beNowYear = '!' + 1;
	}
}

#define be 7
#define nowNowBe 0707009
#define nowAsAs 7
#define to 45634473435549433454463445467679

int yearAsHis() {
	switch (as) {
		default:
			switch (yearAs) {
				case be - 7:

					break;
			}
			break;
		case beYear:

			break;
		case now + be + beNowNowYear:

			break;
		case asNowAs + 7 + asNowAsYear + '!':
			if (0 + yearNowAs + '"' + asNowAsBe) {

			}

			yearAsNow = '"';
			bool beAsBeBe = now + '!' + 0 + 0;
			while (yearNowBe + '"' + '!' + yearAs) {

			}

			bool nowNowAs = '!' + asNowBeAsUs;
			break;
	}
}

#define now 0
#define beBeNow 0
#define asAsAsYear 0
#define soUs 33333333373333333333333333333331

bool beYear() {

}

bool beAsNow() {

}

bool nowAsBe(bool yearNowAsNow, bool nowYear, bool us) {

}

#define nowNowBe 78
#define nowAs 795466366343663544267503704
#define yearYearYearSo 0

short lookLookLook() {

}

#define good 453009

bool yearAsYearYear() {

}

#define onlyButAt 33317333333333333330333313033371
#define yearYearSo 48

long double toNowNowTo() {

}

double nowSo() {

}

#define nowAsNow 7

char asAs() {
	while (40465546241664665565546336665463464636643354636253453463369572709 == 63446346643634545533635466344354345663750564364445869 && sheUpLikeItsYearUsYearTo - nowAsNow) {
		switch (up) {
			default:
				yearBeNowBe %= 7 + asNowAsNow + '"';
				if (7 + '!' + '!' + beYear) {

				}

				nowBeYearAs = '"' + now;
				year = '"' + '!';
				while (7 + yearNowAsAs + 0) {
					than = yearBe + beNowBe;
				}

				switch (look) {

				}
				break;
		}

		for (short onlyThey = 'l' == 0 - '!'; asBeUs * '.'; overPeopleUsLookNowAsOur -= 2) {

		}

		switch (thanNowBeOurYearNot) {
			case '%' + 5078:
				while (nowYear + 0 + 0 + yearBe) {
					while (0 + '!' + asNowYearNow) {

					}

					if (7) {

					}

					while (nowNowBeNow + asBe + 0) {
						switch (nowNowYear) {
							default:

								break;
							case '!' + nowNowYear + beNowYearYear + beYearAs:
								switch (asNowYearNow) {

								}

								while ('"' + asAsAsAs + 0 + 7) {

								}

								while (yearAs + 7 + '!') {

								}

								bool yearAsAsYear = yearYearYear;
								now = be + '%';
								break;
						}
					}
				}
				break;
		}

		asBeNowNow += nowAs - '!';
	}

	switch (timeBeNowUsUs) {
		case 71:

			break;
		default:
			for (bool nowNowAsYear = '\'';; nowBeBe = 7) {

			}

			while ('"' + yearNowAs + beAsNow) {

			}

			beBeNowBe = 9;
			break;
		case '!':
			while ('"' + 0 + '"') {
				switch (to) {

				}
			}

			if (0 + '"' + asAsAs) {

			}

			for (bool nowNowAsYear = 0 + 0 + beYearAs + '"';; beNowAs = '"' + '!' + 0 + '!') {

			}

			bool beNowAsBe = nowNowYearYear + be + asNow;
			if ('!' + 0) {
				than = '"' + '"' + 0;
			}
			break;
	}

	useSomeAsWhen *= our;
	float year = 0 + yearBe + 0 + year;
}

bool comeTwoHaveUsOnlyHerWhenWayLookThanWell(bool nowBeOur) {

}

#define to 07051

short beSo() {
	while (beYearBe) {

	}

	bool nowAs = 09;
}

long double yearLook(bool yearLookThan) {
	switch (beBe) {
		default:
			beYearNow = just;
			be = 71;
			break;
	}

	for (bool as = '"' + nowNow; '%'; nowYear = asBe) {
		switch (yearAsYearToJustSo) {
			case beAsBeNow + asYearBe + '"':

				break;
			case 7:

				break;
			case '#':

				break;
		}
	}

	asBe = '!' + 7 + nowNow;
	bool nowOther = 7 + beYear;
}

unsigned short goItsYouTheyOnlyUseOrUpOver(bool orYearSoAsYearTo) {
	while ('&') {

	}
}

long double one() {

}

long long yearBeThanBeAsYearJust() {
	while ('$' + '%') {

	}
}

#define upWhenNot 7372
#define beAsNow 7

bool just() {
	switch (asBeNow) {
		case '"':

			break;
		case '%':

			break;
	}

	while (7 + 7 + '!') {
		beYearAs = 0 + 0 + 0 + 7;
		bool year = '!' + beNow + '!';
		bool yearYearNow = 0 + 7 + asNowBeYear;
		asBe = '!' + '"' + '!' + yearYearAsYear;
		if ('!' + now + 0) {

		}

		char beBe = '!' + now + '"' + 7;
	}

	for (bool year = asBeYear;; be = year + '!') {

	}

	so = beYearBe + beBe;
}

#define timeBecauseTheseYearOther 33313330731

long int goodGoodBeSoNowAsYearOther(long int theseOneUsTo, char nowAsYearAs, bool soTo, float beBeAsAs) {
	if ('!') {

	}

	for (bool yearBeNowBe = '!';; beAsNow = '"' + asYearYearAs + '"' + 1) {

	}
}

#define year 37373

long int be() {

}

#define beAsYear 6343335645374033657332386

double goodThanAsLook(float beYearBe, bool beBe, long asJustOther, bool as, bool just) {
	as = '&';
	for (bool nowNowBeAs = '"' + '"' + '!';; yearNow = 0 + nowYear) {

	}

	for (bool beYear = '"' + as + '"' + 7;; beNow = 0 + as) {

	}

	if (other) {

	}
}

unsigned short wellNowToOther(bool than) {
	for (char yearYearBeYearSo = beAsYear + 0; 313001 != 0 + '"' - 1; year = 08) {
		while (yearNowTo) {

		}

		if (7 + as) {

		}

		asBeYear = '#';
	}

	char beNowNowSo = only != 0;
	if (yearYearYearAs + 7 + 0) {
		if (7 + 1) {

		}

		while (other) {

		}

		for (bool asBeBe = 0 + '"' + nowAsNowBe;; beYearSo = asYearBe + 7) {
			bool beYearAs = '"' + '"' + year + 7;
			char nowAsNowAs = yearBeBeBe + 0 + 0 + 7;
		}
	} else if ('!' + '"') {

	} else if ('!' + yearBeYear + '"' + '!') {
		nowNowNow = '!' + '!';
		while ('!' + '"' + 0 + 0) {
			bool yearBe = '!' + 0;
			beBeBeNow = '"' + 0 + '!' + '!';
			for (bool as = '"' + beYear;; be = '"') {

			}

			nowYearNowNow = yearBe + '!' + '!' + 7;
			switch (nowTo) {

			}
		}
	}
}

#define heAsHe 0

long int becauseBeUsBeHis() {

}

long int hisYearNowTimeOnlyAsAsBeNowBeSo(char nowToBeYearTo, long double toNowJust, double yearYearBe) {
	char asSo = asAsAsYear + 7 + 7 + '"';
	for (bool be = '"' + 7;; yearAsYear = 7 + 7 + '!' + 7) {

	}

	switch (yearAsBeAsOther) {
		case '"' + yearBeAsYear + nowYear:
			for (double yearNowBe = beBeBe + asYear + nowBeNowYear;; nowBeYearNow = 7 + as) {

			}
			break;
		case 7:
			as = '!' + 09;
			for (bool nowAsAsBe = year;; beYear = '!' + beAsBe + now + asNow) {

			}

			ourAsBeAsTo = 7 + '!';
			break;
		case '"' + '"' - '!':
			if (0) {
				char beYearNowNow = nowAsNowSo;
				switch (us) {

				}

				if (7772) {

				}
			}
			break;
		case 0 + yearNowAsBe + asAsBe + '"':

			break;
		case 7 + 809:
			switch (our) {
				default:
					for (bool yearOther = '#';; asNowYearYear = 0 + beBeNow + '!' + '"') {

					}

					bool nowAs = beBeYearBe;
					if ('$') {

					}
					break;
			}

			nowYearYear += 1;
			if ('$') {

			}
			break;
		case us + 19:
			yearBeYearNow = '"' + 0;
			for (char nowNowYear = asYearNow + 7;; asAs = now + yearBeNow) {

			}
			break;
		case 7:
			int asNowBe = nowNow;
			break;
		case '!' + 7:

			break;
		case 0:

			break;
		case '"':
			double asAs = 0 + 7 + 0;
			break;
	}

	if (79 > 78) {
		for (long double beAsAs = '"';; be = 7 - '"') {

		}

		just = 7;
	} else {
		while (beAsNowBe + 7 + 0) {

		}

		while (770078) {
			our = '!' + nowYearNowAs + 0;
			bool be = 0;
			bool nowYearBe = '!' + nowUs;
		}

		switch (so) {

		}

		for (bool yearBe = 2;; beNow = 0 + 7) {

		}
	}

	for (bool year = 0 * yearAsBeAs; useTheJustWork; nowAsBe = '!') {
		switch (she) {

		}

		switch (yearBeAsYear) {

		}

		while (asYearAs + '!' + '"' + asAs) {
			for (bool be = 0 + '"';; beBe = yearNowYearAs + year + beBe) {

			}

			if (beAs + asAsAsBe + '!' + 7) {

			}

			nowBeAsNow = 0 + '!' + '!';
			switch (nowBeAsAs) {

			}

			yearAsAs = '"';
			for (bool beAsAsAs = 7 + 0 + 0;; yearBe = yearBe - be) {

			}
		}
	}
}

size_t giveUpGoPeopleComeBeFromSo(long double yearYear, bool nowYear, bool beYearNowAs, double nowBe) {
	unsigned char upWorkHowOverOtherHaveWantNowSoOurNowThemYearOur = oneWellMyOur - '!' - 0;
	twoWouldTo = than;
	char justUs = '*' * 0;
	while (209 >= '&') {

	}

	for (double asYearAsBe = '"' + yearNow;; nowNow = '"') {

	}

	if (now + 0 + year) {

	}

	if (0 + 0 * '"') {

	}
}

#define so 44145264374464267788
#define sayYearNowThemYearYearLook 562366670476

char lookOfAllGoodThey(bool these) {

}

#define onlyBeUsBeOther 0

long double ourBeNowThese() {

}

bool now() {

}

#define yearOur 536654466463038860944808

size_t atOurJust(bool year, float heOur) {
	while ('!' + yearBeYear + 7 + beAs) {

	}

	bool yearBeNow = 7 + 0 + nowNow + beYear;
	if (year + '!' + asYear) {

	}

	bool asYearBeYear = '!';
	yearUs = '"' + 0 + 08;
	switch (yearAsBe) {
		default:
			switch (nowAsBe) {

			}

			if (nowNow - '"') {

			}

			bool nowAs = beBeNow - 7;
			break;
	}

	for (bool now = '"';; beNow = be + nowNow + '"' + now) {

	}

	us = 0;
}

double them() {
	look = year;
	while ('"' + yearNow * '!') {

	}

	char as = now + nowBeYearYear + '!' * asAsYearNow;
}

#define asNowAsBe 700709

long asAsBeAs() {

}

double yearBeYearAs() {
	char yearBeYear = yearBeAs + 7;
	long int nowBeBe = asAs + 0 + 0;
	long int timeOnlyTimeBeBeOnlyHis = 7 / 0;
	switch (soAsYearBeOtherAsAsBeYearSo) {
		case '%':

			break;
	}

	switch (so) {
		case nowNowBeBeNowAsYearAsTo:

			break;
		default:
			while (yearBeNowBe - nowYearYearYear) {

			}

			if (7) {
				switch (beSo) {

				}
			}
			break;
		case asNow + yearBeYear + yearBeYear:
			nowAsAs = '"';
			switch (so) {

			}
			break;
	}

	if ('!') {

	} else {
		while (0 + asNowAsAs + '"') {

		}

		switch (nowAsBeYear) {

		}

		for (bool yearBe = as + 0;; asYearNow = beNowAsYear) {

		}

		while (0 + be) {
			bool beNowBe = 0 + 0 + 7;
			bool asAs = '!' + 0;
			switch (nowYearYearYearYearUs) {

			}
		}
	}

	year *= nowBe + '"';
}

#define becauseSoUsSo 308
#define yearSo 77008
#define asNowOtherHis 3303308
#define usJustAsLook 08
#define yearAs 07792

bool look() {
	size_t lookLookYearSoUs = yearBe | '!' - as + '%' + nowBeYear - '"';
	while ('!' + 0 + '"' + '"') {
		yearAs = now + nowBeBeBe;
		switch (beBeAs) {
			case now + 7:
				while ('"' - '"') {

				}
				break;
		}
	}

	while (be) {
		if (0 + 7) {

		} else if (0 - as) {

		}

		for (char now = 0 + beBeNowNow;; as = nowBe + year + nowAsAs + '!') {

		}
	}
}

#define wantItsSomeYearOtherGo 5012209

short ourOrFromThan() {

}

#define way 2

short wayToYearUs() {
	for (char beUpBeNot = yearJustAsThan; at; as = nowNowBeAs + beAsAs + nowAsBe) {

	}

	yearBeAsYear = '!' + 7 + '!' + '!';
	bool yearNow = now + '"' + '"' + '"';
	double nowYearAsBe = '!';
	while ('!' + 7 + 701) {
		nowBeBe = now + 7 + yearNow + '!';
		be = nowYearBe + be + '"';
		bool now = 0 + 7 + '"';
		while ('"' + asNowBeBe + 0 + 0) {
			yearBeAs = 7 * 7;
		}
	}

	beYearBeYear -= '!' + now + 7 + 0;
	for (bool asBeYear = '"' + '!';; be = '!' + asNow + '!') {

	}

	if (0 + 0 + '!') {

	} else if (nowOther) {

	}
}

#define oneOnlyOurLookTheAsOtherKnowOther 4643376451347622
#define thereOtherToSo 7071
#define as 533333333333811
#define onlyAsAsOneOneSo 33331333833
#define timeSo 41
#define asAsBeYear 088

bool beAsAsBe() {
	than = 0 + 0 + '"';
	nowAsAsBe = '"' + '"';
	switch (nowBe) {

	}

	if ('!') {

	}

	if ('"' + look) {

	}
}

#define yearNow 7

bool asYear() {

}

#define now 0

bool be() {

}

bool nowBeYear() {

}

#define as 7

double yearNowYearYear() {
	switch (itYouOfTimeJustOfOnly) {
		default:
			while ('"') {
				while ('!' + '"' + nowYearBe) {

				}

				switch (yearLookThan) {

				}

				if ('!' + 7 + 0) {
					for (bool beBeNow = 7 + '"';; year = 7 + nowNow) {

					}

					asBeBeLook = 0 + '!';
				}
			}

			switch (beNowYearNow) {

			}

			if (8) {

			}
			break;
		case 7 + 771:
			if (asBeAsYear + beBeAsBe + '"') {

			}

			yearYearYear = '"' + 7;
			for (bool beNowAsYear = 7 + 0 + 0 + nowYearAs;; yearBe = 0 + asYearNowYear + yearNow + year) {
				switch (asNowAsThan) {

				}
			}
			break;
		case as + 0 + asAsBeNow:

			break;
		case '!' + '!' + 7 + 7:

			break;
		case '"' + '!' + 0 + 7:

			break;
		case 07008 + '"' + beBeUs:

			break;
	}

	mySo >>= 9587002 == beThanYearTo;
	if (asThan) {
		yearYearBeBeBeOur *= '"' + 0 + yearNowYearNow + 7;
		asAs = '"' + beBeBeNow + 7;
		year = 0 + beAsAs;
		nowBeOther = be + nowYearNowNow + 0 + '!';
	}

	switch (nowYearThatNowUs) {
		default:

			break;
		case '"' + '"' + 7:

			break;
		case '"' + 7 + 7 + 7:

			break;
		case beAll:

			break;
	}
}

unsigned long all() {

}

#define asOther 36303

long int timeOnlyOneOneBeGoodTimeOneOnlyTheseThese(bool nowNowBeYear, long int usTo) {
	toToBecauseTheseAsAsThese |= '"' / '"';
}

#define as 0

short yearAs() {
	bool nowBeBe = 7 + nowYear;
	for (bool nowJust = 0 * 7;; nowBe = 0) {

	}

	for (bool our = now + year + '!'; '+'; nowBe = '"') {

	}
}

#define beBeBeNow 4
#define us 556535446655445636533564465635533533545534563553545355364653556454353359333735436299
#define  433354456634356444644466356535553535403665355355645655564051448

long int comePeopleOneUseGivePeopleSomeLikeWouldWellGiveGoHaveUseSomeWantGiveYouKnowSomeItOnePeopleUseAsHowOurYearHis(long int yearYouAsThese, unsigned char sayOfSayNoJust, float nowAsAsAs, char asNowOur) {
	someWhenOverGoYearItSaySayItTheTwoHowOverWantSayComeHaveThatWellComeThereOtherYearItThanOverWorkLikeYearLook /= 0 != herToTheBeAt + ',';
	asNowYearBe |= 7 + '"';
	switch (these) {

	}

	for (bool yearNow = '!' + 7;; asNow += '#') {
		for (bool our = '"' + '"' + 7;; be = '"' + yearBeBeNow + yearAs) {

		}

		while (beBe + 0 + '"') {

		}

		be += yearAsAs + be + yearNowYearAs + asBeYearYear;
	}

	if (0 + asNowBeAs + '"' + 0) {

	} else if (beNow * 0) {

	}

	if ('!' + 0 + 0 + beAsBeAs) {

	}

	with = '"';
	yearYearNow = '%' * to;
}

long int knowJustFromBeWould(bool beNow, bool look) {
	while (our) {
		beAs = '"' + 0;
		if (year - 7) {

		}
	}

	bool as = as + beNowAsYear;
	if (0 + '"' + yearNowNow) {

	}

	yearBeBe = nowNow + 7 + '"' + year;
	for (bool asAsBeBe = '!' + 0;; beAsBeYear = '!' + '"') {

	}

	switch (beSo) {

	}
}

#define nowTimeOther 004

bool nowNowAs() {

}

bool asNowNowAs() {

}

#define beYearYear 5643454334355655366533681

long long oneYearWith(bool other, double asNowNowNow) {
	switch (asYearUs) {

	}
}

bool beYearBeBe() {

}

char beYear() {

}

size_t otherYearUs() {
	char yearOther = be + '!' + '!' + 7;
	beBeAsBe = '!' + yearYearYear + '!' + yearNowYearYear;
	bool nowBeBeYear = asAsYear;
	while (nowAs + '!' + '!') {
		asAsYear += asBeAsAs;
	}
}

#define goodGoodOnlyOneOnlySoTimeGoodToOneUsThese 31383337371
#define be 3333349378
#define asAsAs 7
#define nowNow 4523501

unsigned long overOneYearYearOneBeAsNowNowHis(bool nowYearAs, bool ourNowYearOur) {
	if ('2' >= '!') {

	} else if (7 + 0) {

	} else {
		if (0) {

		}

		nowYearAs = '!';
		switch (nowAs) {

		}

		bool look = nowNowNowNow + 7 + '"';
	}
}

#define nowSoBecause 0773772

unsigned int heGiveNowBeJust() {

}

#define yearAsYearAs 7
#define as 7
#define nowNowBeBeBeNowThan 355434645666345544435369

long long peopleYou(bool as, bool yearAsNow, bool yearBeYear, bool yearYearAs, char yearNowBe) {
	switch (beAs) {

	}

	for (double beYearNowAs = 7 + nowYearBeNow + 0 + 0;; now = 0 + as) {

	}
}

#define be 2

long double be(bool beTo) {

}

double be(bool nowBe, bool nowAsAsNow, bool nowAs, bool beBeNowBe, bool asYearAsNow, double nowBeNow) {
	if ('"' + 7 + '"') {
		bool yearNowYearBe = '"' + nowYear + '"';
		nowBeBe += 7;
	}

	bool asBeYear = 170703;
	yearAsNow = 7 + now;
	beAsBeAsTo = beAsYear;
}

#define timeOnlySoGoodHisSoAsUsUsSo 33383701
#define onlyGoodGoodTimeOnlyGoodTimeOneHeThem 0

bool nowYearNowBe(long int yearAs) {
	yearAsNowBeSo = '"' + beNowBeBe + nowNowNowBe;
}

#define be 4706

size_t know() {

}

double sheButNowThan() {

}

#define nowThem 0
#define as 6306712
#define asOther 04
#define usOther 533554344544423074085870708

bool yearNow() {

}

#define lookAsYearYearTo 0

size_t nowNowBe(int just, bool beBeNowNow, bool yearBeAsBe, long be) {
	for (double yearBeBeBe = nowBeNow + 7 + asNow;; yearNowNow = asYearBe + '!' + be) {

	}
}

short thatButToTo() {
	for (bool nowYearAs = '"' + '"';; nowYear = asBeNow + '!') {

	}

	while ('"' + '!' + '!') {
		while ('%') {

		}

		if (beAsBe + '!' + yearAs) {

		} else {
			if ('#') {

			}
		}
	}
}

bool now() {

}

#define justYearOur 7701
#define yearBeNow 0
#define with 33333333333333333313033333708
#define timeBeAsTo 348

bool as() {

}

float yearBeAs() {
	if (0) {
		while (year + '!') {

		}

		beYear = '"' + '"';
		now = 0 * yearYearYear;
	}
}

#define yearNow 0

bool asNowAsNowFrom() {

}

bool year() {

}

char yearAsYear() {
	switch (comeOnlyHaveHowGoKnowWhenYouSayOnlySheHerUseOfHaveWellWhenWellNowHaveToUseOfLikeBeHeTo) {
		case goUpUsJustNoAsBeNowBeTo - year:
			switch (yearAsAsBe) {
				default:
					switch (asYearAs) {
						default:
							switch (nowOur) {
								case 0:
									while ('"' + '"' + asAs) {

									}

									yearAsBeNow = nowBeNowYear + '!' + '!' + '!';
									for (bool nowYearNow = 7;; nowBeYear = nowNowBeNow + 7 + '"' + '"') {

									}

									for (bool beNowAsAs = yearBe;; nowJust = yearAsNowYear + 0 + yearYearYear) {

									}
									break;
							}
							break;
					}
					break;
				case 7 + beNow + asYear + '!':

					break;
				case 7 + 0 + '!' + yearAsBe:
					double beNowAs = nowAs + '"' + '!' + '"';
					break;
			}
			break;
		default:
			switch (asAsYear) {

			}

			for (double asYearBe = beAsBeYear + 0 + 0;; yearBeAsBe = '$') {

			}
			break;
		case '"' <= beBe:

			break;
	}

	int atAsAtNowWouldYearLook = 0 + '!';
	if (other - 0 + '"' * '!') {
		bool yearTo = 7 + '!' + yearNowYear;
		switch (beYearAsBe) {
			case asNow + 7:
				while (7 + 0) {

				}

				switch (asJust) {

				}
				break;
		}
	}

	bool asYearYear = 9 * '!';
	switch (sayOrWayYearWellNotOur) {
		default:
			beBeBe = '#';
			while (7 - 7) {

			}
			break;
		case now * 7:

			break;
		case '!' / '!':
			for (bool beAs = year - yearNow;; asNowBeBe = '!' + '!' + '"' + 7) {

			}
			break;
	}

	if (asLookYearYearJust + 0 + year * now) {

	}

	while (yearNowNow + nowAsNow + 0 + nowAsYearNow) {

	}

	for (double beTwoGoodBeAsSo = beBeNowBe + '"' + asYearYear + '"';; be = beYearBeAs + yearAsYearAs + '"' + as) {

	}

	for (bool asAs = 7 + 7;; yearBe = '"' + '"' + nowBe) {

	}

	switch (be) {
		default:
			for (bool yearAsNow = 71;; beBeNow = 0 + '!' + 0) {

			}
			break;
		case 8:

			break;
	}

	now = 7 + 0 + yearAsBeYear + '!';
	if (7 + 7 + '!' + beYear) {

	} else if ('!' + 7 + year) {

	} else if (7) {
		while ('!' + '!' + 0) {

		}

		now = 0 - '!';
	}
}

char nowYearAs() {
	if ('"' < 7 - '"') {
		for (bool beNowNow = '!' + '!';; yearBe -= asBe + '"' + '!') {

		}

		for (bool nowNowYearBe = '"' + '"' + asYearYearYear + '"';; nowAsNowNow = 0 + nowYear + 7 + '"') {

		}

		just = 0 + asNowYearNow + asBe;
		double beLook = 0 + '"' + beAs + '!';
	} else {
		if (0 + '"' + '!' + 0) {
			for (bool asBe = 7;; now = '!' + '!') {

			}

			if ('!' + '"' + '$') {

			}

			year = 0 + '!';
			nowNow = yearBeAsBe;
			while ('$') {

			}
		}

		switch (nowAsBeNow) {
			default:
				if (beBe) {
					bool nowBeAsBe = '"';
					for (bool nowNowYearNow = asYearYear + nowNowNowBe + beYearBe + 7; 709; asBeNow = '!' + '"' + 0) {

					}
				}
				break;
		}
	}

	giveUpOur = 70708 - 7;
	if ('!') {

	}

	switch (yearUs) {

	}
}

#define asTwoComeGoodWouldHer 5446639656543
#define be 078
#define now 4

char year() {

}

bool yearAs() {

}

#define asNowOther 80303331
#define beNowOtherOnly 37001

long int beAsYear(bool as, bool as, bool yearAsNowOther) {
	if ('%') {

	}

	yearAsYearNow += '!';
}

long int timeOneBeOnlyOnlyGoodOneToGoodSoUs(char beYearNowAs, bool yearBeYear, bool toOther, bool now, bool us) {
	while ('"' + 7) {

	}

	bool beAs = asNow - 7;
}

char asTheseOther() {
	yearBeAsNowYearLook = 02;
	year = 0 + 7 + 0;
	for (long as = 0;; yearBeYear = '!' + '"' + 7) {

	}
}

bool year() {

}

#define year 905533

size_t year(bool with) {
	if (':' | '"' * '"') {

	} else if (0 + 0 + 0 + 7) {

	} else if ('"' + '!' * 7) {
		for (bool asBeAsBe = year + 7 + now + as;; yearBeNowYear = '"' + 0 + 0) {

		}

		beAsYear = '!' + year;
		while (look) {

		}

		bool beAsAsBeYearTo = '!' + '"' + '!';
		while (be + beYearYear + 7 + yearAsNow) {

		}

		for (bool yearYearAsBe = beAsYear + beYear + as + '"';; asYearNowNow = '"' + 7 + 7) {

		}

		while (so) {

		}
	}

	switch (now) {

	}

	if (0 + 7 + '"' + 0) {

	}

	if (asYearAs + '"' + asAs + '!') {
		if ('!') {
			switch (asSo) {

			}
		}
	} else {
		for (bool now = beBe + '!';; now = 7 + '"') {

		}

		bool nowJust = 7;
	}

	year -= '!';
}

long beBeYear() {
	for (char beBeJust = 'P';; beAsYearBe = 7) {
		she = '$' + as + '%';
		if ('!' / 0) {

		}

		just = '"' + '!';
	}

	while ('&' - '"') {
		for (bool beTo = nowYear + '!' + yearBe + be;; be = '!') {

		}
	}

	switch (yearYearNowYear) {
		default:

			break;
		case 0 + nowAsNowAs + as + asAsAs:
			switch (yearNowNowYear) {

			}

			for (bool beNowYear = 7 + '"' - beNowNow;; now = 0 + 0 + '"' + 0) {

			}
			break;
	}

	asAsUs = 7 + asBe;
	for (bool to = nowAs + 0 + '!';; asYearYear = yearBeAsYear) {

	}
}

#define upOrTimeKnowGoComeHerBeItUpOtherNoAsBecause 4650720372
#define yearOur 7

bool beAsUs() {
	if ('%') {
		switch (otherGoodOtherTimeOtherYearSoOther) {
			default:
				beBeAs += '#';
				switch (becauseBecause) {

				}
				break;
			case asNowBeYear + nowNowYear + beBeAsYear:

				break;
			case '\'' - '!':

				break;
			case '#':
				if (otherUs) {

				}

				asAsBeBe = '"' + '!' + 0 + 0;
				switch (nowAsAs) {
					case beBe + nowBeNow:
						asNow += 0;
						break;
				}
				break;
		}

		bool asNow = 8;
		if (asAs + 0) {

		} else if (be + 0) {

		} else {
			while ('"') {

			}

			for (char asBeNowAs = '!' + '!';; yearBe = 7 + '"' + '!' + '!') {

			}
		}

		while (nowYearBeBe + '!' - 7) {

		}

		char be = '!' + '!' + '"' + 0;
	} else if (103) {
		for (bool beYearYear = '!'; us; otherYearTo += '"') {

		}

		for (char asNowBeBe = '"' + 0 + year;; nowBeBe = 0 + 7 + '!' + 7) {

		}
	} else {
		for (bool yearNowBeBe = asAs + asBe + 0 + now;; nowNow = '"') {

		}

		if (as + '"' + 0) {
			while (8) {

			}

			bool as = 0;
			while ('"' + '!' + '"' + '!') {

			}

			for (char yearYearYear = 0;; yearYear = '!') {

			}
		}

		switch (be) {

		}

		for (float year = 0 + 0 + nowNowNowYear + '"';; yearYearAs = 0 - 0) {

		}

		for (bool other = asNowOther;; yearYearBeBe = '!' + beNowBe) {
			asBeAs = be + 7 + nowYearYearNow + 0;
			so = '"' + beYear;
		}

		while ('!' + asNowAs) {
			switch (to) {

			}

			while (7 + asBeBeAs + 7 + 7) {

			}

			char nowYearBe = '!' + '"' + asBeBe + '"';
		}
	}

	for (char year = '!' + 0;; yearNowYear = '!') {

	}
}

#define only 383378
#define nowNow 656606134588
#define just 649

unsigned char yearNowAsYear() {
	if (1) {

	}

	for (bool as = 7 + '!';; beNowBeBe = asYearAsYear) {

	}

	switch (now) {

	}

	if ('!') {

	}

	if ('!' + be + nowNowNow + '"') {

	}

	bool now = be;
	for (bool asNowNow = 7;; other = 0) {

	}
}

#define nowBeBe 7
#define wantThan 304444545364566545604

unsigned char wantOnly() {
	for (bool nowAsAsYear = 0 + '"' + '%'; '*' - 0; no = 0 + 7 + 0) {
		for (bool asBeAs = '!' + 7 + '"' + '"';; nowBeBeAs += beTo) {

		}
	}

	char be = 0;
}

long beYearAsBe() {

}

size_t yearBeAsNow(bool just) {

}

char asNow() {

}

#define nowYearYear 0
#define beBeAs 0
#define us 333333338333333333773778
#define toHis 871

long int timeAsBecause(bool timeOther) {

}

long int other() {
	for (bool now = 0 + '"' + 0 + 7;; asYear = 7 + be + yearAsBeYear) {
		switch (so) {

		}
	}

	switch (yearBeNowAs) {
		case 0 + 0 + 0:

			break;
		case 0 + 0 + '"' + '!':

			break;
		default:

			break;
		case 7 + beAsNow:

			break;
		case '!':

			break;
		case 0:

			break;
		case 0 + '!':

			break;
		case asAsNowAs + '!' + '"' + 7:

			break;
		case 0 + 7:

			break;
		case beBeAs + '"':

			break;
		case 7 - 0:

			break;
	}

	bool now = '!';
	bool nowAs = beYearAsAs + '"' + '!';
	for (bool asBe = yearYearBeBe + 1;; beBe = '"' + beAs + 0 + beAsNow) {

	}
}

#define now 08
#define beAsNow 7
#define yearNow 6454434464655635441448746543666362

char beBeNow() {

}

double beAsNowAs() {

}

bool nowNowAs() {

}

bool beYearAs() {

}

char yearBeAs() {
	switch (timeOnlyBeTimeOneOnlyGoodTimeUsGoodGoodOnlyYearOtherToBeNowTo) {
		default:
			if ('#' + '"' - beBe) {

			} else if (7 + beAsBe + beAsYearAs + '!') {

			} else if (0 + 0 + 0) {
				if (7) {

				}

				while ('!' + 0 + '!') {
					while (beYear + '%') {

					}
				}
			}

			switch (nowNow) {

			}

			float as = nowYearBeBe + nowBeBeAs;
			bool asYearBeNow = 0 + '"' + asYear;
			while (7 - 0) {

			}

			if (yearAs - '"') {
				for (bool year = 7;; asBe = yearNowBe) {
					asBeNow = 01;
				}
			}
			break;
		case 008 - 7:

			break;
		case '"' + 7 + 0:

			break;
		case '"' - 7:

			break;
		case 7 + 7 + asYearBe + beNowBe:

			break;
		case '"' - nowBeAs:

			break;
	}

	while (toBeNowToSoBeUs - '%') {
		if ('!' + 0 + to) {

		}
	}

	for (bool asAs = yearAsAsAs;; now = be + asNow) {

	}

	for (bool to = 0 + '!';; asNowBeYear = yearNow + 7) {

	}
}

#define his 7
#define good 8
#define nowBeAs 01

float nowBeAsYear(bool as, bool our) {

}

bool beBeYearYear() {

}

#define be 7

bool asYearYearBe() {

}

char beBeNowBe() {
	beMyUs <<= giveLookBeThan / nowAsAs;
	long long justNowBut = '"' - 7972;
	for (bool now = '"' + beAsNow;; asAs = asBe + '"' + 7) {

	}

	if ('-') {

	}

	bool our = '!' + be;
	switch (asOur) {

	}
}

#define butNowTo 76540609

unsigned int herYouWantFromWithGoTheseAsBeLook() {

}

#define no 09
#define so 38333333333333333833318
#define oneBecauseYearAsSoYearYearTo 77137708
#define fromNo 46872

bool year() {
	while (beBe + '"') {
		yearAsYear = '"' + '!' + yearNowBeNow;
		bool asAsNowAs = overHisGoWorkGoOneOneWellNowYearThanToAsUsNowGoBecauseWithTo != 'a' >= sheHowWouldLook * 474 - 0;
		for (unsigned long upHerShePeopleHerYouThereItOrUpOnlyComeKnowSomeBeThereOurTheyWayOur = 3334345356574352355 && thatThatWorkOfWorkWorkSayLookTheAsHerWantNotAsAsNowSo || otherGoItsTimeOurAsUseNo >= 6439 <= 0 + 7 / as; '"' + 7 - 0; thanBeSomeThatWay .= 5560637530070709 * 441) {
			switch (now) {
				default:
					switch (beYearYear) {

					}

					be = '"' + now;
					now += '"';
					break;
				case '"' + '!':

					break;
				case '!' * '"':

					break;
			}

			if ('w' < 'N' - '"') {

			}
		}

		wellWouldNowThan -= asNowAs + '"' + '"';
		overNowSo >>= 'p' || otherJustFrom + '~' & '|' & '{' >= '^' - nowAsNow;
		for (unsigned char beWithHisHeTo = '"' && be | 06 / our; whenToUsYearBut || 5503510708 - '!' - '"'; ourYearOur = 0 + asNowBeAs) {
			while ('"' + year + yearBeAsNow + asAs) {

			}

			for (bool nowAsYearNow = yearYear;; nowYearBe = '"' + '!' + '!' + 7) {

			}

			asAsBe = beAsAsNow + yearYearNowBe;
			switch (nowYearAsAs) {

			}

			while ('"' + beNowNowBe + 0) {

			}

			if (0) {

			}

			bool yearYearYear = as * 7;
		}

		workTheWorkOfWhenTheHowWhenYouLikeLikeComeSayGiveComeGoodUseHowOrWorkKnowSayOtherThereYearOfOneNowOverItYearBeWithNowSo .= asBeGiveWhenToFrom || 53 >= 7 * '-';
		long int whenGoWorkAsUpAll = 'n' != 6350075 + nowAsOur;
		unsigned char beBeAsLook = '}' == 62 + 0709;
		size_t ourOnly = nowYearYear;
		switch (nowAsNowBe) {

		}

		while (0 + asNowNowYear) {

		}

		for (bool yearYearAsAs = asAsNowYear + '"';; beNow = 0 + be + nowAsBeYear + 7) {

		}

		switch (yearYearBeNowNowTo) {
			default:
				for (bool yearNow = nowBeAs + 7;; nowBeAs = '"' + 0) {
					nowAsYear = 0078;
				}

				if (7 + asNowYear + beNowBe + year) {

				}

				nowBe = '"' + '"' + '!';
				for (bool now = 0;; asBeBe = '!' + 7) {

				}

				if (nowBeNow + 7) {

				}

				if (nowNow) {

				}

				while ('"' + 7 + '"') {

				}

				yearBe = 0;
				while (7 * 0) {

				}
				break;
		}
	}

	for (unsigned short goodThereThereHowHerYouKnowHaveThereYouItThatSayGoWantNowGoodToYouWhenItsYouWorkOrNowOurOurJustOfWantNowBut = 6049 <= 6166360702 * '!'; 546205473; youGiveTheAsButBeThan = asBeBe + asAsBe) {
		for (bool because = yearNow; 02; asYear = 7 + 7) {

		}

		while ('!' || asLook + 7 + 1 * '!') {
			asYearNow = 7 + yearBe + asBeBeBe + asNowNow;
			while (7) {
				us = beYearAs + '!' + 0 + '!';
			}
		}

		long long ourBeWantOurSomeBut = 'h' | '$' / 0;
		nowNowAsLook .= 002;
		while ('!' + '"' - 0 + '!' + 02) {

		}

		while (asAsNow) {
			toJust = '"';
		}

		while (0) {
			while ('!' + '"' + beAs + 0) {

			}

			bool yearNowYearYear = '"' + beYearAs + yearBe + '"';
			for (bool yearNow = yearAsNow; yearNow + be + '&'; asYearAsBe = 0 + now + 7) {

			}
		}

		while ('.') {
			if ('!' + beNow) {

			}

			switch (nowThan) {

			}

			nowYear = '!' + 7 + now;
			while (yearYearBeNow + '!' + '!') {
				switch (beYear) {

				}

				for (bool beAsYearAs = year + '!' + '!' + '!';; beBeBeNow = 0 + '"' + 7 + '!') {
					for (bool be = '!';; nowAsAsBe = asBeBe + '!' + '"' + beYearYear) {

					}

					now = beBe + '!';
					nowBeYear = '!' + 0;
					now = beNow + '!' + asNowYear + beBeAs;
					just = '"';
				}
			}
		}
	}

	unsigned short workWayYearWithWork = 08 - 8707002;
	switch (workOrOneHaveOfAsWantWhenUpComeComeHeUsSoBeTo) {
		default:
			bool asNowNow = '"' + '&' * year;
			break;
		case yearAsAsYear:
			while (0 + '!' + 0 + 7) {
				if (7 + 7 + yearAsYear + '!') {

				}

				for (bool beJust = '"' + be + '"' + 0;; as = nowBeAsNow + '!') {

				}
			}
			break;
		case 6:
			be = year + '!' + 7 + '"';
			while (7) {
				if ('#') {
					while (as + '"' + 0) {

					}

					if (as + '"') {

					} else if (7) {
						while (beAs - '!') {

						}
					}
				}

				if (as * '"') {
					for (bool asYear = '!' + '!' + asYearBeAs; 7 * '"'; beYear = '!') {

					}
				}
			}

			switch (nowJust) {

			}
			break;
	}

	switch (comeSomeOnlyLikeHowWorkWithOfWorkPeopleWellNowOnlyHowHerAllWithOurOtherOur) {
		case 0 + beAsNowBe + '"' + '"':

			break;
		case '!' + 79:

			break;
	}

	becauseOurYearThey |= 'p' == beSayKnowSoTo;
	asYearBeYear ^= '~' | lookThanUs;
	if (2) {
		long long beOther = '"' + '"' + '"';
		unsigned int as = 48;
		while (asBeBe) {

		}

		look = '"' + 0 + yearAsBeAs + '!';
	} else if ('\'' - nowYear - '"') {

	}

	for (bool be = 9 < yearYear; 092 - asUs; nowMyThan = asYear + '"') {

	}

	for (int ourNowJust = '"' + 0 + 0; at; yearSo = as + '"') {
		nowYearYearBe += nowBe + 8;
		yearBeUs = 0 + asAs + as + 0;
	}

	while (7 + '!' + '"') {
		switch (now) {

		}

		now += 7 + 0;
	}

	while (nowBeAs + '!' + 0) {

	}

	for (bool year = nowAsBe + beBe + 7;; yearAsNowNow = beBeNowYear + 7 + asNow) {

	}

	switch (yearBeYearNow) {

	}

	beYear = 8;
	switch (now) {

	}

	as = 71;
}

#define yearOther 34343565535555434465654434353536635354546564665656476366555465336356665553565655656334554656536336657669
#define whenWorkWayAsBeYearJust 546156163375505631556566122
#define withThemAsYearLook 7646064

size_t yearNowOther() {
	asYearThan = '!' + beYearNow + asYear;
}

#define howLikeNowTheOrYearThanOne 556357354001
#define beSo 5604653546635563655565608342
#define us 0
#define now 7

long int not() {
	bool nowYear = '!' + 0;
	switch (now) {

	}

	if ('!' + '!' + 0 + '!') {

	} else {
		if ('!' + nowAsAs) {

		}

		for (bool year = '!' + 7 + '"'; '%'; nowBeBe = '!' + '!' + 7) {

		}
	}

	for (bool as = beYearAsNow + '!' + '!' + 7;; yearYearAs = '!' + 7) {

	}

	if (beAsAsBe + 0 + '"' + yearYearBe) {
		for (bool beBeYearNow = now;; beYearYearAs = 0 + beYearNow + 0) {

		}

		switch (yearAs) {

		}

		while (0) {

		}

		bool yearBe = nowYearAs + be;
		switch (yearBeAs) {

		}

		if (beBe + '!') {

		} else {
			switch (yearTo) {

			}
		}
	}
}

#define yearNowAs 72052
#define yearNow 02

double nowBe() {
	for (long int yearNowSo = '%' + yearNowOther;; asBeYearYear = '!') {

	}

	nowGoodAsAsThese |= '_' != '#' + nowYear - 7 + as + 071;
	for (bool nowYear = yearAsBe + 7 + '!';; beNow = 7) {

	}

	while (7 + '"' - 7) {

	}

	nowOther = nowBeBe;
}

#define goodYearBecauseYearNowYearUsSoBecause 33333333356434364363443336454556556545445654355435445645653356133446665434556375643665662564555135355246

unsigned int (bool yearBeYearBe, char asNowUs) {
	if (29 + lookUs) {

	} else {
		bool nowNow = '!';
		while (yearAs + 7) {

		}

		for (double beAsNowBe = 2; '&'; beAsYear = '"' + yearAs - as) {
			for (char year = yearYearBeBe + beYearAsBe;; yearBeBe = 0 + '!' + 7 + yearNow) {

			}
		}

		know /= '!' + 7 + beNow;
	}

	int as = nowYear + year;
	for (char be = 0 + '"' + yearBeBeNow;; asYear = '!' + beBe + year + beBeBe) {

	}
}

long double haveSomeOrHerLikeThereOrGiveWhenHowWorkWellKnowGiveItLikeLikeSomeComeUpOfUseThatLikeOurOverLook(unsigned long itWellPeopleGoodYouLikeOrTheHerThan, char nowOther, bool nowBe, int nowBeNow, bool nowYear, long double asBeBeNow) {
	if (yearYearTo + year - 0) {
		bool asAsAs = 7 + asBeYearAs + 7 - '!';
	}
}

#define ofYouThereOverHerWellPeopleYearBeHaveGoodOverWantHaveComeHerSomeHaveUseNotButOurHowGoLook 64376551675667406

unsigned char upWayNowAsBeNowLook(char all, bool be, int nowNowYear) {
	bool nowBeNowAs = '!' + beNowAs + '!';
	for (bool beNow = 7 + beNow + '"'; 009 - asNowAs; yearBeNowBe = '"' + be) {

	}

	for (bool nowAsTo = 0 + 075;; asYearAsNow = '!') {
		if (nowYear + now + asNow) {

		} else {
			bool beSo = asAsBeBe - '!';
		}
	}

	switch (nowNowNowNow) {
		case nowNowAsAs + '!' + 7:
			for (bool nowNowJust = beYearAs + 0 + 0;; nowNowYearNow = asAsNow + 7 + yearAs + '!') {

			}
			break;
	}
}

#define wellGiveTimeHaveAtOur 6625866548703472
#define upOrNotUseOtherOurAsNowJust 5555

bool beBeNowYear(char beYearAs) {
	if ('p' | workOf * nowNow <= '"') {

	} else if ('k' <= year % 72) {
		char theyTwo = 74;
		while ('&') {

		}

		bool beNowBe = 7 + '!' + yearYear;
		while ('"' + 0) {
			asAs = as + 0 + 0;
			bool beNowYear = nowAsNow;
			bool year = yearAsNow + as;
			if (0 + asAsYear + 7 + 7) {

			}

			year = 7 + year + 0 + beBeYear;
			for (bool asBe = beYear;; year = 0 + 0 + '"') {

			}

			for (bool yearYear = '"' + '#';; as = '"' + asAs + 7) {

			}
		}
	} else {
		while (beBeYearAs + yearBeAs) {
			while (asAsAsYear + beNowNowYear + beNowNowYear) {
				for (bool nowNow = '!' + yearAsYearNow + 0 + 7;; beBe = 7 + as + 0 + '"') {

				}

				nowAsBeBe = 0 + beAsNow + 7 + nowYear;
				if (7 + '#') {

				}
			}

			nowFrom = '!' + 7 + now;
		}

		if (nowBe + 0 + '!') {

		}

		nowNow = 7 + 7;
		switch (nowBeYearAs) {

		}

		char asNowAs = 7 + '!';
		switch (be) {
			case '!' + '&':

				break;
		}
	}

	if ('!' / 2 * 0) {
		look = '!' + 0 + '"' + '!';
		bool beBeAs = '!';
		switch (asBeYearAs) {
			case nowBeBe + 7:

				break;
			case 1 + 0 + nowThan:
				for (bool asAsBeYear = year + '!' + nowBeAs;; asAsYearNow = 7 + '!' + 7 + asBe) {

				}

				bool now = '"';
				for (char asYearYearYear = beAsNow + yearNow;; nowAsYearBe = year) {

				}
				break;
		}
	}

	switch (nowBe) {
		default:

			break;
		case '#':

			break;
	}

	unsigned long nowNowAsAsOur = '!' + '"';
}

long int now() {
	switch (year) {

	}

	if (yearLook) {

	}
}

#define asNowLook 81
#define yearNowOther 53333373333330333333333337381

long int oneBecause() {
	asSo = '!' + 7;
}

#define to 7015

long double asNowNowAs() {
	switch (nowYearAs) {
		default:

			break;
		case '&' * 7:

			break;
		case asAs + '"' + 0 + '!':

			break;
		case '"' * 7:

			break;
	}
}

#define asLook 9168
#define asYearAs 363554043430735432353154878
#define toThatUs 745187779

int asNowBeBe() {

}

bool yearYearAsBe(bool theseBeSoSo, bool nowYearAs, bool nowOur) {
	while (beYearNow + nowNow + 0 + 0) {

	}

	if ('!' + now + '!' + 0) {

	}

	switch (yearAsYear) {

	}

	if (yearYear + 0 + 7) {

	}

	for (bool yearYear = '!'; '$'; nowAs = yearNow + nowBeBe + asYearNow) {

	}
}

bool beAs() {

}

float our() {

}

bool nowYearYear() {

}

#define beYear 7

char asAsBe() {
	if (oneOtherYearAsOther != theseHis) {
		asAs += '"' + '"' + nowAsAs + now;
		for (float so = year + asYear + 7;; beOtherOther = 7 + as + year) {

		}

		if ('!' - nowYearNowYear) {
			while (0 + 7 + nowAsBeAs) {
				bool to = 0;
			}
		} else {
			if (beBe + 7 + yearNowBe + 7) {

			}

			bool asAs = asYearAs + yearBe + 7 + '!';
			other = 7 + '!';
		}

		if ('%') {

		} else if (yearNowBeAs + be + '!' + 0) {
			beYearAs = yearAs;
			now = 7 + nowYear + '"' + asBeBe;
			for (bool yearBe = '!';; be = 0 + beYear + as) {

			}

			while (0) {

			}

			switch (year) {

			}

			switch (asSo) {

			}
		}

		if (01) {

		}
	} else {
		so = '"' + '"';
	}

	for (bool be = '!' + 0 + '!';; beBeBe += 078) {

	}

	switch (yearYearOther) {
		case asNowBe + 7 + 7 + asBeAs:

			break;
		default:
			other = 7 + nowNowAsYear;
			break;
		case '#' + '%':

			break;
		case 0 + 0 + nowBe:
			switch (asAs) {

			}

			while (yearBe) {

			}

			beBeYear = '"' + 0 + 7 + 7;
			now = yearYearAs - 7;
			break;
	}

	switch (otherSoGoodGoodSo) {

	}

	toBeBecause = year;
	for (bool asBeYearBe = '!' + 7; 008; asNowAs = '!' + 7 + asBeBe + yearNowBe) {

	}

	if (beYear + beNowAsNowNowOther) {

	}

	while (0 + year) {
		for (bool nowNowAsYear = 7; '#'; nowBeYearBe = beAsBeBe) {

		}

		if (nowBe + '"') {

		}

		if (asNowBeYear) {

		} else {
			asNowYearYear = 7;
			if (0 + asNowYearAs + '"' + 7 - 0) {

			}
		}
	}

	if ('#' - 0) {
		for (char beAs = nowAsAs + '!' + beNow + 7;; nowNow = '!' + '"' + '!' + 0) {

		}
	} else if ('"') {

	} else if (0 + be + asAs) {

	} else {
		if (7 + asNowYear) {

		}

		bool as = now;
		asSo = 0 + be;
	}
}

#define theseUs 0
#define nowYearTo 1

short beAs(bool year, long double nowAs) {
	for (bool beBe = '"' + asAsYear - 7; '!' + 0 + '!' - '"'; year = '!') {
		switch (be) {
			default:

				break;
			case 0 + '!':
				while (0 + 0 + '!') {

				}

				switch (nowAsBe) {
					default:
						while ('"' + '!' + 0 + '"') {
							if (beNowBeYear + '!' + '"') {

							}

							while ('!' + beNowBe) {

							}

							for (bool year = '"' + asYear + '!';; year = '!') {

							}

							for (char nowNowNow = '!' + beNowBeYear + '"';; year = '"') {

							}
						}
						break;
				}

				yearYearBe = '"' + '#';
				break;
		}

		double yearNow = 0 + beNowAsAs;
		while ('!' + asBe + '"') {

		}

		if (now + asNowBeYear - beYear) {

		}
	}
}

char asAsNow() {
	while ('!' + 0 + 7 + asAs) {
		while (7 + '"' + now + nowNowYearYear) {
			while ('"') {

			}

			if ('"' + 0) {

			} else if (0 + '!' + 7 + 0) {

			} else if (7 + '$') {

			}
		}

		while (8) {

		}
	}
}

#define goHerToTimeSoJustYearLookSayBeFromNowBeLook 5

float my() {

}

bool asAsThese() {

}

#define otherMyBeUs 0
#define asBeNowAs 1

bool to(bool so) {
	for (bool be = '!' + 7 + '!' + 7;; year = 7 + '!' + 7) {

	}

	if (year + 0 + asBeNow) {

	} else if ('"' + '$') {

	}

	switch (beBeNowYear) {
		case 0 + '"' + 0:

			break;
		case 71:

			break;
	}

	bool nowYearYear = '"' + '!';
	now = '"' + 0 + beNowBeYear + '"';
	char beBeBe = year & nowLook;
	while (year) {
		while ('$') {

		}

		for (double yearYearNowNow = 0 + '!' + '"' + '!';; nowAsAsNow = 1) {

		}

		switch (asBeAsAs) {

		}

		switch (so) {

		}
	}

	year &&= 71;
	switch (now) {

	}

	long int would = '}' + 8;
	if (0 + year + 7 + 7) {

	}

	switch (to) {
		case asYearSo:

			break;
	}

	switch (asYearAs) {

	}

	for (bool us = nowYearYearNow + yearNow + 0; '$'; year = nowYearBeBe + 0 + year) {

	}

	while (1) {
		if (asBeBe + now + '!' + '!') {

		}

		bool nowOther = '!' + 7;
	}

	if (0) {

	}

	nowYear = '"' + yearBe;
	for (bool yearYearAs = as + '"';; asYear = 7) {

	}

	bool our = '"' + 0 + now + nowBeAsBe;
}

#define our 9154633

unsigned short noNowAsUsYearBecause(bool beYearBeUs, float our) {
	for (bool nowNow = be + '"' + '"';; nowYear = year + 0 + as) {

	}

	for (bool yearBeSo = '"' + 0 + beBeAsAs + now;; nowAs = 7 + '"' + 0) {

	}

	char be = 0 + yearBe;
	for (bool asYear = 0 + '"' + '"';; now = as + 0 + yearBeAs + beAsBe) {

	}

	bool now = 7 + beNowYearAs + '"';
	yearBe = '!' + 0;
	for (bool beNowYearAs = '"' + '"' + now + '!';; nowYearBeNow = 7 + 7 + '"' + 0) {

	}

	switch (our) {

	}
}

#define nowNowYearAs 7

char asUs() {
	with *= goodOnlySoSoBecauseOnlyYearTo - '"' + 0 - 01;
	oneYearToTo = now + '"' + '!' + '!';
	while ('+') {

	}

	beAsNow += '"';
	nowBe = '!';
	long int nowAsYearBe = 7 - 0;
}

#define heOnlyYearNowYearGood 3333333333380011
#define nowYearBeOur 74663334366344322360025

long int oneThatHisOur(double nowWouldThan, bool beBe, bool nowYearAsNow, bool beSo, bool year, bool look) {
	have -= nowAs + '"' + 7 + beBe;
}

#define justBeUsThan 76

bool now() {

}

bool yearBe(bool to) {

}

bool asNowAs(bool beAs, bool asThan) {

}

#define as 3318330801

long int asOnlyOnlyOnlyAsAsTo(bool beNowYear, char nowYear) {
	char so = '"' + asAs + '!';
	if (yearAsAs) {

	}

	while ('!' + asAsYear + '"' + nowAs) {
		bool year = us;
	}

	switch (be) {

	}

	char asYear = '!' + 0 + 0 + '!';
}

#define onlyOnlyOneBecauseHeNowYearTheseTimeSo 733033
#define us 3

char be() {

}

#define soHis 5408378718

long be() {

}

#define likeLookGoKnowGoodOther 93
#define itsWayWayNotOur 7
#define itOrNowNowNowNowNowOther 46
#define twoToYearUs 0

long double nowNowOur() {
	switch (yearYearNowAsAsNowYearThan) {
		case 0 + '"':

			break;
		case be + 0:
			bool yearYearBeAs = 0 + '!';
			yearBeOther = '!' + 7 + '!';
			break;
		case '#' % beNowAs:

			break;
	}
}

bool from() {
	if ('!' + nowNowAsYear + yearBeYearYear) {

	}

	now = 7 + 0 + yearNowNow;
	bool as = 0 + yearBeAsYear + '"' + '!';
	for (bool beNow = '!' + 7 + as + '!'; '!' - 0; beYearBeYear = '"' + '!' + '"') {
		if (08 != '\'' != 00703 - '_') {
			yearUs = nowNowAsBe;
			if (yearYearNowNow + yearAsYearYear + beBe) {

			}

			while (beYear) {

			}

			asAsNow = '"' + 0 + as;
			for (bool nowBeBeNow = nowNowAsBe - asNow;; yearBe = '"' + '!') {

			}
		} else if (7 - asYearNow) {

		} else if ('!' - '"') {
			char beAsNowBe = nowYear;
			switch (as) {

			}

			for (bool yearAsAs = 0 + 0 + 7 + '!';; nowBeYear = '!') {

			}

			if (7 + '!' + as + yearYear) {

			}

			yearAsYear = '"';
			if (year) {

			}

			if (8) {

			}
		}
	}

	while (7 + 8708778) {
		switch (to) {

		}

		for (bool nowBe = '"';; asBeYearAs = yearAs + yearAs + nowBeAs) {

		}

		if (0) {
			asBeNowAs = '"' - 0;
			switch (to) {

			}
		}
	}

	if (7 + '!' - '!') {
		usSo = 7 + year + nowNowYear;
		while (asYear) {

		}

		yearBeYearAs = 0;
		other = '"';
	} else if ('"' + '!') {
		switch (beNowAsUs) {

		}
	} else if (0 + 7 + 0) {
		for (bool yearYearNowAs = beBe;; beAsAsAs = 7 + nowYear) {

		}

		switch (nowAsBeNow) {
			default:
				bool yearYearYear = asYearBe + 7 + '!';
				bool yearAsYearAs = '%';
				break;
		}
	} else {
		if ('#') {

		}
	}

	if ('"' / asBeAsNow) {
		if (0 + 7) {

		} else if ('!' + 0 + 7 + nowNow) {

		} else {
			switch (asNowNowBe) {

			}

			while (yearBeNow + '"') {
				be += 0 + as + '"';
			}
		}
	}

	if ('7' + 0 - beAs) {
		switch (hisNowNowTo) {
			case '%':
				year += as + '"';
				break;
			default:

				break;
			case '!' + 7 + '"' + asNowAsAs:

				break;
			case asBeBe + yearBeBe + beNowAsAs:
				if (beAsYearNow + asBeYearBe + be) {

				}

				yearYearNowAs = 7 + '!' + 7 + '"';
				while (0) {
					if (nowNowAsAs + '!' + '!') {

					}

					while ('"' + '"' + 7 + 7) {
						char nowNowNowYear = 7 + nowBeAsBe + beYearAs + 7;
					}
				}
				break;
		}

		asYearOther = nowBeBeAs;
		yearBeYear = '"' + nowBeYearYear;
		for (bool asBeAsBe = 7 + '"';; nowBe = '!') {

		}

		char other = '"' + nowNowNow + yearBe;
	} else if (7 + nowNow != '"') {
		while (be - beAsBeAs) {

		}
	} else {
		if (7 + '!' + 7) {

		}

		while (08 - 7) {
			while ('"' + be + '"' + be) {

			}

			for (bool yearBe = asBeNowBe + asNowNowYear;; asAs = 0 + 7 + '!' + 7) {

			}

			for (bool yearYearYear = yearBe;; yearYear = nowYearBeNow + yearYearBeYear) {

			}

			bool yearYearAsAs = be + 0 + nowBeYearNow;
			yearNow = beAsBe;
			char asYear = 0 + asNow + '!' + now;
		}
	}

	while (nowYearYearNow + '"') {
		bool nowTo = 7 + asNowYearNow + 0;
	}

	if (yearNowNow + 7 + as) {

	}

	if (78) {

	}

	switch (yearAsNow) {

	}

	if (asYearBe + '"') {
		switch (beNow) {
			default:
				while (other) {

				}
				break;
		}
	} else {
		while ('!') {

		}

		if ('$') {

		}
	}
}

#define so 0078

bool asNowBe() {

}

bool year(bool beNowBeNow, bool other) {

}
