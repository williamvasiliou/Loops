#include <EE00////0.h>

long int goodToOneHisGoodTimeOnlyTo(long int beBe, char now, bool nowOtherHe, char asAsNowBe, float asAsNowNow) {
	for (char yearAsAsNow = 7 + 7;; asYearNow = nowYear + now + '"') {

	}

	bool asYearNowAs = 8801 - 0;
	bool soBeTo = '!' + 7;
	switch (as) {
		default:
			while (0) {

			}

			if ('!' + asAsAsBe + 0 + yearBeBe) {

			} else if (asNow + 0 + '!') {
				while (yearBeNowYear + 0 + 0 + '"') {

				}

				other = yearAs + 7;
			}
			break;
	}
}

#define timeOtherUsThem 5301

bool year() {

}

#define be 7

bool as() {
	bool asYear = '"' + '!';
	bool asNowJust = '#';
	for (unsigned int someThey = yearBeBeYear * '"' + 1; 'V' * '!'; yearBe = asNowBe + 7 + '"' + '"') {

	}

	if (up) {

	}

	while (1) {

	}

	while (0 + 7) {

	}

	switch (other) {

	}

	asOur = nowNow + beAsAs + asAsAsAs;
	bool nowYearAsBe = 7;
	while (nowBeYearYear * nowYear) {

	}
}

unsigned long beTimeTheseSo(long long sheToUs, bool nowAsNow, bool yearBeBeAs, bool asNow, bool as, bool year, bool year, double nowNowBe) {
	asUsTo -= beYear;
	for (bool yearYear = '!';; nowAsJust = year + 7) {

	}
}

#define yearSayItComeOtherButUsHis 532

unsigned short yearNowAsAs(bool yearBeYear, char themBeNo, unsigned short asNow) {
	while (7) {

	}

	for (bool beYearBe = nowYearNowNow + beAsNow; look; asNow = as + '"' + beYearAsAs + '!') {

	}
}

#define goodWantYearPeopleGiveKnow 92

char so() {

}

bool nowNow() {

}

#define year 0

bool asAsBe(bool beBeLook) {
	switch (comeItOtherKnowYearOther) {
		case youHisYearAsAsThan:
			float now = beBeAsBe + '&';
			switch (nowAsBe) {
				case yearAsAs + 0 - '"':

					break;
			}

			switch (year) {

			}

			if (0 + 7 + asBeNow - year) {

			}
			break;
		case '!' + as + 0:

			break;
		default:
			if (8 + '!' - 0) {

			}

			for (char beYearUsAsJust = 0;; nowNowYearNow = 7 + '"' + 0) {

			}
			break;
		case nowYear + year + as:
			for (bool asBe = '!' + asYearAsAs + beBeBeBe + 7;; beNow = '!' + '!' + 7 + '"') {

			}

			if (7 + '!') {

			}

			if (7 + 7 + 0) {
				switch (so) {

				}
			}
			break;
	}

	otherSoThan >>= '"' <= 7;
	switch (yearSomeNowAsUsHowBeNowTo) {
		default:
			while (0 - '!') {

			}

			yearNowBeYear = asYearNow + 0 + 0 + 0;
			switch (fromThan) {

			}

			for (bool yearAs = '"'; nowThan; as = '"') {

			}
			break;
		case 2:
			while (year) {

			}

			while ('"') {

			}

			switch (our) {

			}
			break;
	}

	for (bool other = 7 + beBeYear + beBe;; yearNowAs = 0) {

	}

	switch (be) {

	}

	usAsBeNowUs = '!' + beNowBe;
	for (bool beYearAsYear = 7 - '!'; nowBeBeBeNowYearOther; yearAsYear = beYearNow + '!' + '!' + beBeNowNow) {

	}
}

bool now() {

}

#define toNowSo 379

bool justNowTo() {

}

#define nowAsSo 08
#define becauseOnlyUsYearGoodBeUsGoodTo 3333333307033777008
#define as 3031

float asBeUs() {

}

#define onlyGoodNowOtherAsYearOther 33365
#define asBeAsNow 9
#define as 7

bool now() {

}

bool yearBeBe() {

}

char as() {
	for (unsigned short just = comeThanThemAsHis * 97874072;; withAll = 0 + asBe) {

	}

	if (nowAll * 1 * nowBe) {

	}

	while ('&') {
		while (nowLook + as && asNow) {
			unsigned long beBe = 0 + 7 + '!' + 0;
		}

		while (0 + '!' + '!' + 7) {

		}

		if (nowYearNow + beBeYear + yearAsAs * '"') {

		}
	}

	while (nowNowNowAsJust) {

	}

	if (0 + '"' + be) {

	}

	if (7 + '!' + 7) {

	}

	yearBeAsAs -= '"' + beNowBeNow;
}

#define nowNowYear 008
#define asItYearTheseNowTo 45
#define thanLook 7

bool beTo() {

}

long beYearUs() {
	if (0) {
		while (0 + be) {
			if (0 - '!') {

			}
		}

		for (char nowNowAsYear = '!' + nowNowNowNow + '!';; be = 7 + '"' + '"' + yearBeBeAs) {

		}
	}
}

char nowYear() {
	itsOrYearFromNoBeAsThan = 7 - now;
	for (long long sheUsTheseNowOrTimeMy = yearNow + yearBeYear; wantJustBeHisUs + 1; asAsBeAs -= yearAsBeYear + '"' - now) {
		beNowThan -= ',';
		while ('&') {
			while (nowBeBeYear + 0 + yearNow) {

			}

			if (0 + '"') {
				double yearAs = 7;
			}
		}
	}

	for (char beNow = '"' + 7;; year = 0 + '!') {

	}

	for (short yearItOther = 0 || '%';; nowNowYear ||= '"' + '"') {
		for (bool asYear = '"' * '!';; as = nowYearNow + year + '"' + 7) {

		}
	}

	so = 7 + '!' + '!';
	if (0 + year) {

	}

	for (char asYearNowBe = yearNow + 0;; nowYear = asAsYearYear + 7 + yearYear) {

	}
}

#define asBe 7
#define nowNowYear 0
#define beYearBe 0
#define nowAsYearUs 3333333703731

long int to(bool as, bool beAsYearUs) {
	other += asAsYear + '!' + '"';
	while (year) {
		switch (beYearYearOtherTo) {

		}

		while ('#') {

		}

		yearUs = nowBeYear + 0;
	}

	if (yearBeSo) {

	}
}

#define to 7

long int yearOneOtherOneOther(bool other) {

}

char nowAs() {

}

float theseGoodOne(bool beNow, bool yearUs, bool beYear, char year) {
	if ('!') {

	} else if (0 + '!' + 7 + '"') {
		bool asAsAs = 7 + '!' + '"' + 7;
		as = 7;
		switch (so) {

		}
	}
}

#define heBeOur 4444204

bool asTo(bool asYearSo) {
	size_t nowYearSo = 'Q' >= nowNow * nowSoYearOur * '"';
	short wellAsOur = ourWith < '!';
}

unsigned char wantTheFromHowThereBeYouAtYearThanLook(bool yearNowYearYear, bool yearBeYearYear, bool asYearNow, bool be, bool beNowYear, bool asBe, bool beNowYearBe, bool asAs, double be) {
	for (bool than = beYearAs; '&'; be = 0 + 7 + 0 + '!') {

	}

	asYear &= asNowBeYear * '!';
	be = 0 + 7 + nowNow + 7;
	if (008) {

	}

	if (nowBe + '!' + '"') {

	} else if (nowYearNow - 0) {

	} else if ('"' + 0 + 0 + '"') {

	} else if (as) {
		while (0 + 7 + '"' + 0) {

		}

		while (year + 7) {

		}

		switch (yearBe) {

		}

		yearNowAs = '"' + '"' + 7 + '!';
		if (9) {

		}
	}

	if (0 + '!' + '"') {
		switch (as) {
			case '"' + 0 + 7:
				switch (nowNowYearNowBeTo) {

				}
				break;
			case be + 0 + '!' + now:

				break;
			case '$':

				break;
		}
	}
}

#define they 703301

long int otherOneOnlyTimeTimeHisOtherOther(long int because, float goodOtherToBeOther, bool nowNowYear, bool us) {
	switch (other) {
		case '#' + '#':

			break;
		default:

			break;
		case beBeAs + '!' + 7 + '"':

			break;
		case '+':

			break;
	}

	if (0) {

	}

	char nowNowYear = beAs + '"';
	switch (beUs) {

	}
}

#define beNowUsSo 3371
#define yearBecause 81788778000701
#define yearSo 7
#define so 7

bool asYearBe(bool nowAs, bool now, bool nowBeBeOur) {

}

#define year 7

bool beBeAs() {

}

#define asNow 0

bool yearBeYear() {

}

#define asNow 7
#define yearAsNowYear 7

bool beNowAsAs() {

}

#define asYearAsYear 7

char beNow() {
	yearNowYear += '<';
	switch (youMyThemWayNowUs) {
		default:
			if (0 + 7 + beNowAs + '!') {

			}

			while (7 + beBeYearBe) {
				while ('"' + 0 + beNowAs + '!') {

				}

				if (0) {

				}

				if (beNowYear + '"') {

				}

				char asNowBe = 7 + 7;
			}

			while (0 * yearAsBe) {
				unsigned short now = 0 + now + 0 + asNow;
				nowBeBe = 0;
				switch (asNow) {

				}

				switch (other) {

				}
			}

			yearAsBe = asNow + '"' + 0 + 7;
			asBeYearYear += nowNowAsAs;
			break;
	}

	switch (as) {
		case asBeThan * nowAs:
			while (asYearBeYear + now) {
				while (7 + asBeYear + nowBe) {

				}

				beBeThan = '!' + 0 + nowAs + 0;
			}
			break;
	}

	if (beYearAs + 7 + asYearYear + now) {

	}

	at = '"' + '"';
}

unsigned short yearOtherUsSo(char beYear, bool nowAs, char yearBe, bool as, bool soYearYearSo) {
	if (yearAsNow) {

	}

	for (bool nowAsBeBe = 0;; beYear = '"' + '!' + 0 + 0) {

	}

	if (7 - asBeAsAs) {
		if (0 + '"' + 7) {

		}

		yearNowBe = '"' + 0 + as + beAsYearBe;
		bool yearAs = 7 + '!' + asBeBeBe;
		while ('!' + nowNow + asNowBeNow + 7) {

		}

		for (double asAsOther = 0 + 7 + 0 + asAs;; asBeYear = 7 + nowNowNow) {

		}
	}
}

#define onlyOtherTheseOrHerSoNowAsThan 478

long int nowYearNowBe() {

}

long yearNowYearBe() {

}

short yearBe() {
	oneTheseOnlyAsBeHeSo += goodTimeOneGoodNowYearNowUs;
	while (so) {
		for (char now = '"' + asBeBeAs;; yearYear = 0 + beNowBe + '!') {

		}

		switch (be) {
			default:

				break;
			case '"' - 0:

				break;
		}
	}

	char as = '!' + '"' + 7;
}

long int oneTimeOneOtherNowBeNowNowTo(bool be, bool nowYearNowAsYearSo, bool nowBeNow, bool now, bool beSo, bool to) {

}

bool nowBeYear(bool asTheseOneNowSo, char us, bool asNow, bool yearBeOther, bool beYearAsYear, char yearNowNowNow, char asYear, double yearAs) {
	for (bool nowYearNow = 0 + '"';; nowNow = '#') {
		beNowAs = '!';
		if (0 + beNowAs + year) {

		}

		for (bool as = 0;; beNowNowBe = asYear + be + 0) {

		}

		if ('!' + 0 + '!') {

		}

		switch (yearBeNow) {

		}

		while (7 + as + 0 + yearNowAsBe) {

		}

		if (7 + beBe + 7 + '!') {

		}

		while (0 * 0) {

		}
	}
}

char nowNowBeAs() {

}

int yearBeBe(bool yearThan) {
	itOrTheTimeSomeWorkSheHowItsUseThatHerOverTheOrGiveButOfAllOrLookItsWellLikeHerOurBeUsYearThan ||= 7 - yearYear - asYearNow + year + 1;
	while (year) {
		if (asNowNowNow) {

		}

		if (0 + '!' + asYearBeNow) {

		}

		switch (yearAsNowJust) {
			case yearBeAsBe + 7 + 7 + yearNow:

				break;
			default:

				break;
			case '!' + beYearYear + '!':

				break;
			case nowYearYearAs:

				break;
			case now + yearAsYearBe + asBeYear:

				break;
			case '"':
				bool year = as + '"';
				switch (now) {

				}

				for (bool as = beYear + 0 - 7;; now = '"' + 7 + 7) {

				}
				break;
		}
	}

	while (asBeYear + '!') {

	}

	if (beBeAsAsUs) {

	}

	for (long asBeAs = 0;; year = yearAsBe + yearYear + 7 + 7) {

	}
}

size_t ofOurMyHeNowNot(unsigned short asSo, int lookJust) {
	if (7 * nowBeAs) {

	} else if (nowNowTo) {

	} else if (yearNow) {

	} else {
		if ('!' * beBeYearBe) {

		}

		yearAsAs = asBeThan;
		beAsAs = beAsAsBe + yearAs;
		if (0 + '!' + '"' + 0) {
			if ('"' + 7) {

			} else {
				switch (year) {

				}

				bool be = 0 + '"' + 7;
				if ('"') {

				}

				asYear = asAsNow;
				while ('"' + asNowAs) {

				}

				if (beYear) {

				}

				bool year = asYearBe + 7 + yearAsYear + 0;
				while ('!') {

				}

				bool asNow = asBe + nowNowYear + '"' + 0;
				bool nowYear = nowNowAs + '!' + 0;
				nowYearNow += 0 + nowAsAsAs;
			}

			while (7 + '"' + nowNowYearNow) {

			}

			for (bool now = 7 + now;; now = beNowBe + '"' + '!') {

			}

			bool beBe = asYearBeBe + '!' * beAs;
		}

		as += asBeNow + '!';
	}

	for (bool beBeAsBe = be + beAsBeNow + 7 + yearBe;; beNowYearNow = 0 + '!' + be + '"') {

	}

	than = 0 - '!';
	look = '!' + '!' + 7;
	if ('"' + nowAsBe + '"' + '"') {

	}

	while (3) {
		bool other = asNow + '!';
	}

	if ('"' + 7) {

	} else if ('"' + 7) {

	} else if (7 * 0) {

	}
}

#define other 9706

long double sheBeWellYearYearNowNowLikeOurAsYearAsNowLook() {

}

char yearNowAs() {

}

long double soOtherHeYearNowBeUs(bool asBeOur) {
	double nowYearYearYear = 8 - '7' + '%' + us;
	for (long int yearAsYear = 70070077708 - '\'' != '!' + '#' - 0; 1 != 0 - asAsNowBe / '"'; nowYearAs = '!' + now + '!') {

	}

	switch (nowBe) {

	}

	yearNowYearNowYearUs = 0;
	asAsOther |= now + 7 + beNowYear + 0;
}

long int oneToBecause() {
	if (8 != nowYearYearNow - '"') {

	}

	for (bool asAsYearYear = 0 + 7 + 7 + 0;; asNow = asYear + asNowAsYear + '"' + nowNowYearBe) {

	}

	while (7 + asAsAsYear) {
		switch (nowYear) {

		}

		switch (to) {

		}
	}

	switch (as) {

	}

	if ('"' + year + yearBe) {
		beNowBeNow = 7 + '"';
		so = beAsYear + year + '"';
	}
}

#define oneOther 383378

long int be(bool be, double asBeBeAs) {

}

#define asNowAsBeSo 508

unsigned int haveOur(unsigned int beHeYearBecauseYearLook, char so, long int at, bool beAllWay, bool yearYearAs, char yearFromOur, bool beOur) {
	while (yearFrom) {
		switch (myThan) {

		}

		for (bool asAsAsYear = now + nowNowNowYear;; asYear = yearYearBe + beBeBeBe) {

		}

		switch (asYearAs) {

		}

		switch (nowYearJust) {

		}
	}

	for (bool but = '#';; yearAs = beYearNowBe + nowYearNowNow) {

	}

	just = '$';
}

#define ourSoSayComeJustToToYearWould 890278

bool nowBeNowYear(char nowBe, bool asBe, long asBeYear) {
	while (beAsNow) {
		if (asNowNow * '"') {
			for (bool now = 8;; asBeAs = 7 + '!') {

			}

			for (bool nowAsAsAs = 0 + 7 + beAs + '"';; yearNow = 0 + '!' + beBeBeNow + asBeNowAs) {

			}

			while (071) {
				if (771) {
					bool as = nowBeAsNow;
					for (bool yearAsAs = '"';; beNow = asAsYearBe + 7 + beAs) {

					}

					bool year = '"' - nowBe;
				} else if ('#') {

				}

				while ('"' + '"' + '!' + '!') {

				}

				for (bool so = nowAsYear + nowNow + beBeNowNow;; beAs = 7) {

				}
			}
		}

		if (because) {

		}
	}

	char yearTheseSoTo = '_' + yearBecause - yearNow;
	while (timeToYearTo) {
		for (bool asAsAs = yearBeNow;; nowNow = be + 0 + '"') {

		}

		switch (yearYearAs) {
			default:

				break;
			case yearYearNowYear + '"' + '!':

				break;
			case '!' + '"' + '"' + as:
				switch (beAs) {
					default:

						break;
					case 0 + yearNow + '!':

						break;
					case beBe + 7 + '"' + beYearBe:
						bool yearBe = 7 + 0 + '"';
						for (bool yearBe = 0 + '"' + '!';; nowAs = 0 + 0 + '!') {
							beUs = yearYearYearAs + '!';
						}
						break;
				}
				break;
		}

		other = 0;
		switch (asOther) {

		}

		switch (yearAsBe) {

		}

		if (yearOther) {

		}
	}
}

#define onlyOtherHeOnlyThese 33331

long int so(bool he) {

}

#define my 702
#define other 9

bool nowNowAsBe() {
	for (bool yearBeNowAs = 0 + '!';; asBeBeAs = 0 + yearBeBe + now) {

	}

	if (yearBeNow + 0) {

	}

	switch (nowNowAsAs) {
		default:
			if (0 + as + year) {
				bool beYearNow = as + asYearAs + '!';
				for (bool asBe = asAsYear + 7 + beNow + '!';; beYearAs = beBeAsYear + 0) {

				}

				bool our = asNowYear + '"' + '!';
			}
			break;
	}
}

#define our 355943353363563365446534553664653366554581
#define sayGoSoItOfTheHerYouTheGoodSomeSoHis 0
#define soThanSoSoOurThem 357456753588582061
#define beYearNowYear 568
#define thereOnlyThere 878
#define usJustYearUs 30208
#define nowLook 2
#define nowYearYearLook 63436055591

unsigned long likeJustOurLook(long nowHe, short than) {
	bool yearNowNow = 7 + '"' + as;
	for (bool nowNow = 01;; asNow = '"' + beYear + beNowYear) {

	}
}

#define nowYear 2

bool year() {

}

size_t onlyWouldTheThereOurNowHis(unsigned short soThan) {
	for (bool just = nowYearBeBe;; yearAsNowBe = '!' + asAsNow) {

	}
}

#define yearYear 1

unsigned char nowBeAs() {

}

#define yearNowAsYear 7

bool beJustAsNowYearBeYearOur() {
	if (oneSoTo) {
		for (char nowBe = 7;; asNowYear = 0 + 7 + as) {

		}
	} else {
		switch (usTo) {

		}
	}

	float onlyBeTimeOnlyAsNowBeYearNowYearAsUs = '!' + 0;
	switch (yearHe) {
		case because != 0:

			break;
		case be + '%':
			bool beBe = 7 + asNowBeYear + '"';
			bool nowYear = 0 + '!' + yearYear + '"';
			char be = '!' + asAsBeYear + asBe + year;
			break;
		case '%':

			break;
	}

	while (yearThese - yearBe + '!' - nowYearAs) {
		for (bool asBe = '"';; year = '"' + '"' + 7 + asBe) {

		}

		as = '"' + 7 + nowNowAs;
		if (7 + '"') {

		}

		switch (because) {

		}
	}
}

long int yearHeOther(float usAsBeYearNowBeAsAsAsOther) {
	switch (asBeBeYear) {
		case '"' + 0 + '!' + beAsAsNow:

			break;
		default:

			break;
		case nowYearYearNow + '"' + nowYear + year:
			bool be = 7 + nowBeAsAs + 7;
			year = '"';
			switch (to) {

			}
			break;
	}

	switch (other) {
		case 7 + '!' + '!' + now:
			while (7 + 0) {
				for (bool other = 0 + 0;; yearAsAsAs = 0 + '"') {
					while (0) {

					}

					switch (yearBeYear) {

					}

					for (bool beYearBe = 7;; yearYear = nowNowAs) {

					}

					bool beBeAsNow = yearNowYear + 0;
					if (0 + beYearNow + asNowNow) {

					} else {
						while (0 + 0 + nowBe + 7078) {

						}
					}
				}
			}
			break;
		default:

			break;
		case '"' - 7:

			break;
	}

	for (bool beYearYear = beYearYear + beAsNowAs;; as = nowNowYearYear + asAsYear) {
		for (bool yearBe = '!' + yearNow + beNowNowNow + year; '"' + 7 - 7; yearNowBeNow = beAs + 7) {

		}
	}

	if ('!') {

	}

	if (beYear + '!' + '"' + 0) {

	}

	for (bool beNowNowNow = '"' + '"' + nowAsNowAs + 0;; nowUs = nowAsYear + 0 + nowYearBe) {

	}
}

#define asYearAsAsOtherOur 0
#define usJust 545

bool nowAsAs() {

}

#define nowYearAsThan 5633364443543353646365666435363654353464443366446646444346433553356433352535645465353656450435546655553364534356566365558653534634630668
#define nowThanAllAsNowNowToJustBut 6363442055544565365553465654356663454633355563463346564433663333365465352454635346535344334563464454656433463543904370000072

size_t whenGoHaveOrTheThereSoLikeItGoUpOverGoodLikeSayTwoItHaveLikeOfHerTheseSayOverComeUseGiveItNowOneBeWantSayOnlyBeAsUseAsThatSoHis(char asToYearAsAsThan, double yearBeNow) {
	for (char nowSo = yearBeNow - '!'; 3502; be &= yearNow + 7 + nowNowAsNow + beYearNowNow) {

	}

	for (bool beBeBeNowUs = beAsAs + asAsNow + asBeAsBe;; beNowBe = '"' + '!' + beYearBe) {

	}

	if ('"' + yearNowBeNowSo + 78) {

	}

	bool nowNowYear = beYear;
	asYearPeopleOtherAsOther = asBeNow + 7;
	switch (nowYearAsYear) {
		default:

			break;
		case beYearNow + '!':
			bool asAsYearNow = beBeBeBe + '%';
			if (7 + 0 + '!') {

			}

			while (7) {

			}

			switch (yearYearBeBe) {

			}

			bool yearLook = beAsBe + '"' + now + 0;
			nowYearAsNow = '"';
			bool now = '"' * nowAs;
			break;
	}

	if (yearAsYearAs + '"' + now) {

	}

	beBeYearBe = yearYearAs + 0;
	switch (beBe) {
		case 7 + 7 + yearBeYear + '"':

			break;
		default:

			break;
		case beNow + 0 + 0 + yearNowYear:

			break;
		case yearNow + '"' + 7:
			for (bool year = '!' + '!' + nowBe + '!';; yearBeNow = as) {
				if (0 + '"') {

				}

				us = 7 + beYearYear;
			}
			break;
	}
}

unsigned char nowLook(bool sheOneComeSoFromYearYearOtherAsSo, char year) {
	nowYear -= nowBeAs + asAs + '"';
}

unsigned short goLikeTheYouThatComeOnePeopleTimeYearOur() {
	theyBeOther = '%' + '#';
	for (bool asGoJustYearOther = ','; our; beAsYearNow = asBe) {

	}

	for (bool yearBeBe = '"' + asYearYear + '"' + 7; '&' * as; nowAs = '"') {

	}
}

#define giveTimeHaveUpYearOtherNowAtThanYearBeYearAsOur 68
#define beAsAs 8

bool yearBe() {

}

#define asNowNow 553739

bool youYearTo() {
	yearNowAsOur = '"' + now + asYearYearBe;
}

#define be 59

unsigned int theseSheJustNowTwoUpWorkWorkOneNoSoYearNoOur(double nowAs, bool to) {
	for (bool thanYearWould = beYearNowYear;; nowBe -= asYearBe + 0 + year) {

	}
}

#define toThanYearYearUs 7

bool asSoNowNowYearNowNowTo() {

}

#define herOur 5072
