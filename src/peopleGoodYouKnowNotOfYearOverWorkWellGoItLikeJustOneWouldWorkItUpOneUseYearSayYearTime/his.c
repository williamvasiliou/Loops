#define onlyGoodGoodOnlyGoodAsSoNowBeOther 0078
#define us 078

long int toOneBeNowSoSo(bool beAsAsYear, bool beAsBeAs, bool nowAs, bool yearBeAs, char asYear) {
	yearThese = 0 + '"';
}

long int because(char nowBeAsNow) {
	for (bool be = 0 + 7;; asYear = '!' + 0 + '!' + '"') {

	}

	char asYear = '!';
}

long int yearNowBeYear() {
	for (bool yearAsBeNow = '!' + asNow + 7;; nowBeAs = '"') {

	}

	if ('"') {

	}

	bool be = 7 + 0 + 7 + '!';
	for (bool nowNow = '"' + 7; nowBe - '"'; beYearNowAs = 7 + yearNowBeYear + yearNow + yearBeYear) {

	}

	beBeBe += '%';
}

#define oneBeOneYearBecauseBeNowYearUpTo 7998
#define nowNowThe 

unsigned char knowGoodGoHerWantTheTheHaveWellGoWellWantTheSomeAsUseWantSomeComeTheNotHaveTheUpWhenGoUpWantItsWorkWantItsUpUpOverItsTwoWhenHaveWellPeopleUpItsThereWhenWantSomeItGoUseTheWorkGoWantHerJustYearBeWould(long double these, size_t comeHaveWellHowTwoSomeJustThemTo, long long knowTwoSheOfThat, bool beBeNow, bool yearYear, bool beYear, double asBeYear) {
	if ('.' - 7) {
		double be = 0;
		for (bool as = 0 + year + beAs + nowBeAs;; asOur = '!' + '"' + 7) {

		}

		bool year = 7;
		switch (nowAsAs) {

		}

		double beYear = asBe;
	}

	switch (as) {
		case 7:
			while (0 + 7 + '"') {

			}

			switch (asNowAsYear) {

			}

			for (bool beYearBeYear = 7;; yearYear = '"' + '!' + '!' + '!') {

			}

			bool asNowBe = 9;
			break;
		case 0 + 0 + '!':
			while ('!' + '"' + 7) {
				if (beYearAsNow + 7) {
					double as = asYearNowBe;
				}
			}
			break;
	}
}

#define now 6

bool be() {
	us = '!' + 7 + 7;
	switch (asNow) {

	}

	if ('!' + '!' + 0) {

	} else if ('&') {

	}

	if (year) {

	}

	switch (asBeYear) {

	}

	switch (nowYear) {

	}

	if (beYear + 0 + '!' + 7) {

	} else {
		switch (nowAsNow) {

		}

		bool our = '"' + yearAsAs;
	}
}

#define beThan 7

bool nowAsYearNow(bool just) {

}

bool our() {
	if (0 != 43336091 + 2) {
		if ('"' + 0 + 0) {
			if ('!' + '"' + as) {

			}

			if ('!' + yearYearThan) {

			}
		} else {
			if (just) {

			}

			long yearNow = 7 + '!' + 7 + 7;
		}

		if (yearNowAs + 7) {

		} else {
			switch (yearAs) {

			}

			other = 7 + 0;
		}

		while ('"') {
			while ('%') {

			}
		}
	} else {
		double beYearAsBe = '"' + '!' + 0;
	}

	while (451) {
		if (usUsWantAsJust) {

		}
	}

	while (002) {

	}

	if (7) {

	}

	yearAsBeAs = '!' + as + now;
	if (now + beAsNowYear + asBe + 0) {

	} else if (nowNowBeAs + '!' + 0 - year) {

	}
}

#define theyYouOverOther 275
#define myYouYearYearBeYearAsTo 375
#define asAs 7

bool asBeBeOther() {

}

double beAs(bool year, bool so, bool nowOur) {
	switch (year) {

	}

	nowYearNow = 0 + 7 + 0 + '!';
	for (bool be = 7 + '!';; as = '!' + 0 + '!') {

	}

	bool asNow = '!';
	for (double year = yearBeAsAs + '"' + 7;; yearYearYear = 0 + yearYearAsNow) {

	}
}

#define asBe 0
#define asNowAs 7
#define nowAs 5333333337331778
#define onlyOtherHisAsBeBecause 103803

long int asTo(char nowNow) {
	beYearBeBe = year;
	for (bool beBeAsAs = '!' + beNowNow + 0 + '!';; nowAsNow = 7 + '!') {

	}

	if (0) {

	}

	bool yearNow = 0 + nowBeYear;
	nowNowYearAs = yearBeAsAs + year;
	nowNow = yearNow + '!';
	if (0 + 0) {

	}

	switch (beYear) {

	}

	nowBeSo = '"' + 0 + nowNow + '"';
}

#define beThese 33333315402
#define nowAsNowAs 0

bool nowYearNow() {

}

#define nowNowNow 7
#define year 4

char asYearAsAsAsBeAsOur() {
	for (bool as = beBe + yearAsNowYear + yearYearAs;; yearBeNow = year + now + yearBeYear + '!') {
		for (bool beNowYearAs = '!' + 7 + '"'; 0 + '%'; beYearYear = 2) {

		}
	}
}

long double comeGiveWould(long long soAsWould, double asYear, bool nowBeYear, char nowYearAsAsBeOur) {
	switch (asBeAsNow) {

	}

	double his = 02 * '"';
}

long wouldHeThanLook() {

}

#define nowAs 7

bool yearYearBe() {

}

bool now() {

}

bool be() {

}

#define now 7

bool yearAsNowNow() {

}

#define beNow 7
#define beAs 3465955

unsigned short ofSayToOtherWorkWorkLikeSomeAsKnowNowThatTwoTimeButAtBeUs() {
	if (7 + '!' + 1) {

	}
}

#define wellHisToLook 7
#define yearYear 0
#define asBecauseAsJust 3333333333333333303333333331331
#define yearOther 56
#define as 0

bool be(double beNowNowYear) {

}

bool now(bool nowAs, double beBeBeBe) {
	while (7 + 7) {
		while (as * beYearNow) {
			char asYearAsNow = '!';
		}
	}
}

unsigned int wantAt(unsigned int whenTwoWouldWouldAsNowYearLook, bool beYear, long yearYearAsAs, double withYearYearBeTo, bool yearSo) {
	if ('n') {
		for (bool yearAsYear = '"' + '!'; '"' + 0 || 0; beWay = 0 + as + '"') {

		}

		for (bool year = nowBeNowNow + 0 + nowBeBeYear + beNowBe;; beYearAsNow = be + nowAsBe) {

		}

		if (as + nowAsNow + beAsBe + 7) {
			as -= beNow + 7;
		}
	} else if (asNowAsNow) {

	} else if (yearAsYear) {
		while (yearAsAs + nowAs + '!') {
			for (bool nowYearAsNow = 7 + nowNow + '!' + 7; 0 * asBeYearNow; now = now + 7 + 7 + 7) {

			}
		}
	}

	if ('"') {
		if (beAs + 72) {

		}
	} else {
		yearAsAs = asNowBe + yearAs;
		if (7 + 0 + 0 + beNowBe) {
			nowYear = 7 + nowAsBeNow + 0;
			if (7) {

			}

			while (asJust) {

			}
		}
	}
}

bool yearNowYearUs() {

}

bool asNowAsYear() {

}

bool nowAsBe() {

}

bool nowBe() {

}

#define beYearNowBe 5553585221

double justAsHerOurTwoBeYearBeYearYearAsOur(double beAsAs) {
	switch (year) {
		case '$':

			break;
		default:

			break;
		case yearAsYearYear + '$':

			break;
	}
}

unsigned char haveSoSo(bool asYearOurNowAsAsOur, bool nowYearYear, bool nowBeAsNow, bool as, double beNowYearBe) {

}

unsigned char allHaveBeHis(int atAllYearThan, bool yearAs, double yearAsYearBe) {
	if ('"' + '"' + as) {

	}

	while (asAs - 0) {

	}
}

double be() {
	switch (asNowAsYear) {

	}

	asNowAs = 0 + 7;
	while (7 + '!' + 7) {
		for (bool yearNowAsAs = '"';; now = be + '!' + 7 + '!') {

		}

		while ('"') {
			for (bool be = yearYear + 7 + 0;; yearYearAsBe = nowBeYear) {

			}

			while (0 + asBeYearBe) {

			}

			while (year + yearAsNow) {

			}

			bool nowAs = beYearNow + now + '"' + 7;
			while (asYearYear + yearBeNow) {

			}

			while ('!' + 0 + 7 + 0) {
				if (now + '"') {

				}

				for (bool beBe = yearBeBeYear + '"' + '"';; asBeNow = nowYearBeYear + '!') {

				}

				switch (so) {

				}
			}
		}
	}
}

int be() {

}

#define nowBe 0
#define as 7

bool nowYearNowYear() {

}

char asBe() {

}

double now() {
	while (yearYearNowNow + so) {
		char as = 871;
		for (bool so = 7 + nowYear + to; 0001; to = yearAsNowAs + as + 7 + '!') {

		}

		if (7 / '!') {
			for (bool asYear = beAs + '"' + 7;; beAsNow = 0) {

			}

			if (year) {

			} else {
				asNowNowYear = year;
				for (bool be = 0 + '"'; 701; beYearAs = '!' + 0 + beYear + '!') {

				}
			}
		} else {
			if (7 - 7) {
				for (bool now = beYear + 0; asTo; yearAs = 0 + 7 + beAsYearAs + '"') {

				}
			}

			while (0 + yearNowAsAs + nowYear) {

			}

			switch (nowOther) {

			}
		}

		for (char now = 7 + '!' + 0 + nowYearNowYear;; be = 7 - year) {

		}

		float yearUs = asUs / 0;
	}

	while (timeSo != '?' != '_') {
		switch (otherNowNowYearOtherNowBeAsBeAsNowTo) {
			default:
				for (bool asAsBe = '"' + 7 + '!'; '%'; yearYearYearNow = 0 + '"' + '!' + asNow) {

				}

				nowUs = '%';
				if (0 + '"' + beBeYearYear) {
					switch (be) {

					}

					for (bool beBeNow = '!' + beAsYearAs; other; nowYearYearYear = asAs) {

					}
				}
				break;
			case so:
				while ('!' + asYear + nowBeNow + asYearAs) {

				}

				while ('"' + yearBeBe) {
					switch (beAsYearAs) {

					}

					if (7 + asBeYearAs + beBe) {

					} else {
						if (0 + '"') {

						} else if (beBeBeAs + 0 + 0) {
							nowBeBeAs = '"' + '"' + 0 + '"';
							if (now + '"') {

							}

							us = nowNowAs;
						}
					}
				}
				break;
		}

		while (7 + yearNowNow + 0 + '"') {

		}

		while (year + asYear + '!') {

		}

		bool asAsBeYear = nowBe + 7 + 0 + as;
		if ('"' + 7 + 0) {

		}

		while ('"') {

		}

		switch (asAsAsAs) {

		}

		switch (to) {

		}
	}

	for (char nowYearGood = '"' && beYearYearLike;; beBeAsAs = as) {

	}

	while (asTo + other) {

	}

	switch (nowJust) {
		case beSo:

			break;
	}

	if (65346659648 >= herSomeWhenHaveOrGiveItOverSoWhenWorkPeopleThanSomeLookTwoWellKnowOtherUsLook | '}' + 6024 >= 64452561 && theAsWorkOtherYearAt <= yearAsNowAs) {
		switch (yearAsNow) {

		}

		switch (beAs) {

		}

		while (7 + 7) {

		}

		while ('&') {

		}
	} else {
		if (beNow + 0 + 0 + '"') {

		} else {
			for (bool asNow = now + yearAsYear;; yearBeNow = '!' + beYearYearYear + 0) {

			}

			switch (asAsYearBe) {
				default:

					break;
				case '!':
					yearAsNow -= 7;
					break;
			}
		}

		while (0 + '!' + 7) {
			if (nowYearAsBe + beAsNow) {

			}

			for (bool than = '"';; beAs = '!' + '!' + '!' + 7) {

			}
		}
	}

	while ('&' > '!' * 0) {
		for (size_t workYearThem = youItsComeWantUpUsSomeOf < 907645772 + nowYearJust < 0 % beBe; 'e' - asThanButYou || usOrSo; nowYearBeYear = '!' + '"') {

		}

		while ('!' + '!') {

		}

		itsSayTheseAsWhen = 0 + yearBe + '!';
		switch (beBeAsNow) {
			case '"' + yearBe + '!' + 0:

				break;
			case 0:

				break;
			default:
				switch (be) {
					default:

						break;
					case be - 0:

						break;
				}
				break;
		}
	}

	orYouWould ||= 002 || 5797472 - 0 + 7 - year;
	while ('!' + '!' + '!' + '!') {

	}

	if (0 * 8 - '!') {
		switch (yearNowBeBe) {
			default:
				while ('"' + 0 + 7 + yearAsAsAs) {

				}

				while (beAsNow + '"') {

				}

				switch (beNow) {

				}

				asYear = 0 + yearNow + '"' + asBeYear;
				for (bool year = nowBeYear;; beYear = '!' + 0 + 0) {

				}

				switch (asAsNow) {

				}

				switch (beAsYearNow) {

				}

				while ('!') {
					switch (nowAsAsYear) {
						case yearYear + 7 + 7 + '!':
							switch (nowNowBeNow) {
								default:

									break;
								case '"' + '!' + beAs + '!':

									break;
								case '"' + '"':

									break;
								case nowBeAsAs + nowBeYearNow:

									break;
								case '"':

									break;
								case 0 + '"' + 0:
									if (as + 7 + '!' + 7) {

									}

									switch (be) {
										default:
											switch (beAsYearBe) {

											}

											switch (now) {

											}

											if (beNowYear) {
												nowAsYear = '"' + asNowNow;
												just = yearBeBeNow + '"' + beAs + 0;
											}
											break;
									}
									break;
							}
							break;
					}
				}
				break;
		}
	}

	double asNowYearAs = 0 + '"';
}

unsigned int yearLook(bool nowNow, bool year, bool beYearYear, bool nowYear, float yearNowLook) {
	while ('$' + beNowAs + '$') {
		for (bool yearAs = '%' + than;; asAs = 7 + '!' + be) {

		}
	}
}

#define comeButOur 6455656304646
#define wellYearWhenHerWorkOfNowItWorkGiveYearBeUs 6560456058
#define our 9770702

short the(long double beNowYearAs, bool beNowYear, char beNowYear) {
	if (nowNowAsYear + 0 + 0) {
		our = yearNowYear + '"' + year;
	}
}

#define beYearBeAs 9
#define now 7

int now() {
	overAsUpAllOther /= yearAsYearOther % as;
	if ('"' + '"' + asAsYear) {
		for (bool yearAsAs = yearAsYear + 7; '%'; yearNowBeNow = as + 7) {

		}
	} else {
		year += 0 + asYearNowAs + '"' + '!';
	}

	while (7 + 7) {
		bool nowNowNow = '!';
		while (asAsNowNow) {

		}

		switch (nowYearNow) {

		}

		asYearBeAs = nowNowNowBe + 7 + be;
		if (asAs + year + '!' + 0) {
			switch (so) {

			}
		}
	}

	nowUs = 0;
}

#define itAtGiveThereThatFromNowNowYearOur 76978
#define beJust 2

char asHaveUpUpAsOnlyAsBeTo() {

}

int itAsOur() {

}

#define nowNowYear 7

char as() {
	for (bool asAsNowAs = beAsYearNow;; nowAsAs = 0 + 0) {

	}

	switch (beNowNowBe) {
		case asAsNowNow + '!' + 0 + 0:
			bool nowBeAsBe = 7 + '"' + '!';
			while (than) {

			}
			break;
	}
}

#define beBeYear 0

bool beNowBe() {

}

#define yearYearAsAs 533333333333333303333081

long int nowOnlyHeOtherOneSoOurAsUs(char asBeAs) {
	double now = 0 + beAsNow;
	for (bool yearNowNow = '!' + now + be + yearYearYearYear;; nowAsAsAs = 7 + 0 + 0) {

	}

	bool nowBe = 7;
	yearAsJust = asBeNow + 0 + '!';
}

#define than 0

bool yearNowBe() {

}

#define thanBeAsYearUs 7

bool other() {

}

#define be 0

bool yearOur() {
	unsigned short thanWellYearBeAsThan = '*' != soYearYearOur * 2705;
	switch (our) {

	}

	while ('3' != 7 & '&' + '$') {
		for (bool nowYear = nowAsNowAs - beYearYearNow; be > '$' * 7; yearYearNowBe = '!') {

		}

		for (bool now = 7 + '"' + as + 7;; nowAsYear = 0 + '!' + as) {
			while (0 + 7 + beNow + '!') {
				for (bool yearBeBe = now + 0 + '!' + beNowNowBe; 1; be = 0 + 0 + 0 + '"') {

				}

				switch (beYearJust) {

				}
			}
		}
	}

	char now = 9;
	while (now <= '!' - be) {
		switch (be) {

		}

		if (yearNowAs + look + asLook) {

		}

		while ('"' + 0 + asNowBe + '!') {

		}

		for (bool beBeAs = '"' + beYear + '!' + '!';; yearBeAsAs = 0 + now + '"') {

		}

		char beBe = 0 + 0;
		while (7 + 7 + now) {

		}

		bool asNowNowAs = nowYearYearYear + yearYearBeNow;
		as = be + '"' + 0;
		bool beBeSo = 0 + 0 + 0 + 0;
	}

	switch (at) {
		case asAsBeBe:

			break;
		case beYearNow:
			for (bool yearAs = 0 + as + '"';; be = 0 + '!' + '!') {

			}

			switch (nowBeAs) {

			}

			switch (us) {

			}
			break;
	}

	switch (other) {

	}

	switch (yearAsYear) {
		default:
			beBeYearYear = '"' + 7;
			for (bool yearBe = '!' + 7;; nowAs = 1) {

			}
			break;
	}
}

short (bool theWorkTwoHowYearSheThereWellItsSomeYearSomeYearWellOneUseAsLikeThatWellOfTwoAsYouComeSomeBeNoIts, unsigned int someTheOrHerKnowOrWhenOverWhenAsOur, bool asAsNowJust, double year) {
	if ('4' + to) {
		while (0 * 0) {
			for (bool beAs = 7 + 0; 7 - 7; now = '!' + '"' + 0 + now) {
				if (7) {

				}

				beSo = be + '"' + year;
				bool yearAsBeBe = at;
				if ('"') {
					nowOther = beNow;
				}
			}

			while ('!' + '"' * '!') {

			}
		}

		whenYearBeTo = 0;
		bool nowOur = 0 + '!' + 7;
	} else {
		for (short thatWant = '_' * 52 && 0 % '&'; 46675259 + 78; yearNow = '!' + '"') {

		}

		while (69 || 7 < '!' * 7) {
			while (than) {

			}

			while (wayBeUs > 'E') {
				switch (us) {
					case 9 - '!':

						break;
					default:
						for (bool yearYearYearAs = 0 + '!';; beBeNowBe = '"' + as + 7 + asBeAsBe) {

						}

						year = '!' + '"';
						for (bool beNowAs = 0 + 0 + now + '!';; beNowYear = nowBe + yearNowAs + '"') {

						}

						switch (nowBeNowAs) {
							case 0:
								for (bool beYearAsAs = '"' + '!';; be = '"' + '!' + 7) {
									year += 7 + as;
								}
								break;
						}
						break;
					case '"':
						switch (beBeBeYear) {
							default:

								break;
							case 1:

								break;
						}
						break;
				}

				would = '"';
				for (int now = '!' + as + 0; '"' + '%'; beYear = '"' + 0 + beAs + 0) {

				}
			}

			while (yearYearYearYear + 0 + asAsNowYear + 0) {
				bool look = yearYear;
			}

			double beAsYearBe = asNow + 7;
			while (0 + way - 78 * '"') {
				nowYearBe >>= 0 * '"' - '"';
				for (bool now = '!';; as = nowAs) {

				}

				nowYearYear = asNowNowYear + '!' + 0;
				if ('!' + nowBe + 7) {
					look = '"' + '"' + 0;
				}
			}

			while (asNowAsNow + beBeBe + '"') {

			}

			while (0 + yearYearYearAs) {

			}

			for (bool asYearBe = '"' + 0 + asAs;; asNowYear = 7 + nowAsBe + '"' + asNow) {

			}

			bool nowYearAs = 7 * yearBe;
		}

		for (bool year = 0;; wayNowAsOur += nowAsBe + as + 0075 + 7 * 7) {

		}
	}

	if (7 | '3' - our) {

	} else {
		while ('"' + beNowBeNow + 0) {

		}

		for (bool yearYear = now + '!' + '!' + beBeNowAs;; asNowYear = '"') {

		}

		for (bool now = '!';; asOther = 7 + '"') {

		}
	}

	for (unsigned long with = now; just; yearNowAs = 0 + 7) {
		bool asYearYear = as;
		char asBeAs = '"' + 7 + yearNow + 7;
	}

	if (asYear % nowYear) {

	}

	if (theyWouldLook / '"') {
		for (bool nowBeAsBe = 7 + 0 + '"' + '!';; year = 7 + '!' + asYearYear + beNowAs) {

		}

		char nowYearOur = 7 + 7 + '!' + as;
		switch (yearBeAs) {

		}

		if (yearAs * '"') {

		}

		yearAsNowNow = '#';
	} else {
		to = now + asBe + yearBe;
	}

	while (beNowNowYear + beNowYearYear + 7 + 7) {

	}

	while (08) {
		char now = year + 7 + 7 + '!';
	}
}

#define theItsThanAtNo 7

bool beBeAsAs() {

}

#define yearBeLook 4344054504333347653453067970008

size_t peopleNotNoSoUs(bool so, char be, bool yearThan) {
	bool soLook = overYearThan + '%';
}

#define beAsBe 071
#define beAs 7
#define year 08
#define beAs 5333313333

long int oneTimeTimeTimeTimeToOnly() {
	while (yearNowYearAs - as) {

	}
}

float timeTheseSo(long int to, bool now, float as) {
	for (bool asYearAs = yearAs + 0 + 7;; beUs = '!') {

	}

	bool nowAs = '!' - '!';
}

#define yearOther 0
#define us 501
#define nowOther 0

bool as() {

}

bool so() {
	be /= nowAsNowBe + as + '!';
}

#define us 5
#define other 02779

double nowYearNot(bool yearThanThan) {
	bool nowAsYearAs = 0 + '!';
	if (yearBeAsNow) {

	} else if (beAsAsNow) {

	} else if (year + 0) {

	} else {
		if (7 + 7 + '!' + '!') {

		}

		bool beYear = '"' + 0;
		if ('!' * '!') {

		}
	}
}

bool beBe() {

}

bool asBeSo() {

}

#define yearBe 454575
#define workItSheHerSheOverSayYouAsSomeOverTheThatUsJustTwoYearSay 477078

long long asYearBe(long double nowAsBe, bool nowYearAs, double asAsNow) {
	while (yearYear + '"' + 0) {

	}

	beAsAsJust = beBe + yearYearAs + '!' + '!';
}

#define nowBe 7
#define beNow 0
#define asBeBe 5333333333337317783
#define soHisTheseOnly 0

bool theseTimeUsBeAsSo() {

}

long int timeNowTo(bool beSo, bool nowBe, char beAsAsBe) {
	bool us = 0 + 78;
	char be = '!' + '"' + asNowNowBe;
}

char nowTo(unsigned char beToSo) {
	while ('!' + beYearAs) {

	}

	if (beNowNow + '!') {

	} else if ('!' + 7) {

	} else {
		bool be = '!' + 0;
		switch (us) {
			case '"' * yearAsAs:
				while (7 & 7 * '"') {

				}

				heBeAsOur = asAsYearSo;
				switch (our) {

				}
				break;
			case 0 / '"':

				break;
			case 09:

				break;
			case beBeYear:

				break;
			case beOur:

				break;
		}

		for (bool asBe = '!' + 0 + 7 + asYearNow; 72; nowNow = '"') {

		}

		for (bool year = 0 + yearAsNowNow + '!';; as = nowYear + 7) {

		}

		if ('"' + asNowAs + beAsYear) {
			switch (nowBeNowYear) {

			}

			while (asYearAs + now + '%') {

			}
		}

		for (bool year = '!' + 7 + '!' + be; our; be = '"' + '!') {
			switch (nowYear) {

			}

			switch (beOur) {

			}
		}

		if (nowBeAsYear) {

		}

		if ('"' + 0 + beYear + '"') {

		}

		for (bool beAsYear = year + '"' + yearAs + 0;; beBeNowAs = '"' + '!' + beAsNow + 0) {

		}

		switch (us) {

		}
	}

	for (double yearBe = asBeYear + yearYearNow;; asBeNowAs = 7 + nowNowNowAs + be + year) {

	}

	while (24201 + 88 * '"') {
		while (7 + be + '"' + year) {

		}

		while ('"') {

		}

		for (bool yearYearYearAs = '!' + now; 0 - asYear; asAsNow = beYearAsBe + yearNowAsAs + 0 + 0) {

		}
	}
}

#define just 2
#define as 7
#define nowNow 7
#define beBeBe 7

bool us() {
	saySay -= '!' + 7 + '%';
	switch (thereOfComeUsBeOtherYearOther) {
		default:
			beYearBeJust = 7 + '"' + nowBe;
			while (yearNowYearNow + as + year) {
				switch (our) {

				}

				switch (yearAsAs) {
					case look:

						break;
				}
			}

			when -= '"' + as + '!';
			break;
		case '\'' - 0 + be / 8 + 2 * yearYearNowYear:
			for (bool beAsBeBe = '!' + '"' + asYearBeAs + 7; 7002; beAsBe = as + 7 + 7) {

			}
			break;
		case 9:
			while ('!' + to) {

			}

			for (bool yearNow = '"' + 0 + 7; '$'; nowBeAs = year + beNow) {

			}

			if (0 + '"' + 0) {

			}

			bool beJust = '"' + beYearBeNow + '"';
			break;
	}

	if (nowOtherOther) {
		if (9 % '$') {

		} else if ('!' + year + 0 + 0) {

		} else if ('"' + '!' + 0 + 0) {

		} else if ('"' + to) {

		}

		if ('!' + as + 7) {

		}

		asNow = 0 + '"';
		for (bool yearBeNowNow = beNowBeYear + beBeYear; 7 - be; yearBe = 0 + 0) {

		}
	}

	for (bool now = '!' + 1;; beNowThan = 7 + nowBe + '"' + beAs) {
		bool yearBeAs = '%';
	}

	if (nowYearYearBe + '!') {

	}

	if (yearNowBe - year) {
		char beAs = '%';
	}

	bool asYearAs = now + be;
	bool year = nowAs + '"' + '#';
}

double myAsNowBeNowOur() {
	for (bool yearYearAs = '"' + 7;; asBeNowYear = nowAsAsAs) {

	}

	for (char beYear = 0;; nowNowAs = 7 + 0 + as) {

	}
}

#define asNow 7

bool be() {

}

#define nowTo 7
#define otherAsOur 3333383331

long int asYearAsYear(bool nowNowYearAs, bool oneTo) {

}

long int goodOneUsTo(bool asYear, bool these) {
	while (0 + beYearYear) {
		long int time = 7 + 7;
		for (long int nowAsBeYear = nowBecause - '"';; yearNow = 0 + now + nowSo) {
			for (bool beAsAsNow = '"';; now += 0 + '"' + 7 + 0) {
				char be = 008;
			}
		}
	}

	beYearAsNow = 7 + '"' + 0;
	for (char now = '!' + beAsBeBe + 0;; beYear = 7 + beAs + 0 + nowNowAsNow) {

	}
}

long int other() {
	year = yearNowBe + '!';
	switch (nowYearAsNow) {

	}

	bool asNowAs = '"';
	long int asNow = beBeBe + 0 + beJust;
}

bool as() {

}

bool yearYearBeYear() {

}

char just() {
	look = 7 - 2;
	bool our = '!' + 0;
}

bool nowNowNowAs(bool our) {
	while (these > '"' <= '(') {
		while (beAsAs + 0) {

		}

		for (bool yearBe = 7;; nowAsAsAs = '!' + '"' + beBe + 0) {

		}

		now = as + '"' + '!';
		while ('!' + 0 + 0 + '!') {
			bool beBeBeNow = be + nowAsYearBe + '"';
			nowBeAs = 08;
		}

		if ('!' * 7) {

		}
	}

	while (yearNowBe + '"' + 7) {
		switch (asYearAs) {

		}

		if ('"' + year + 7) {

		} else {
			for (bool as = nowYear + as + '"' + '"';; yearAs = 0) {
				nowJust = '"' + '!' + asAsYearNow;
			}
		}

		while (be * '"') {

		}
	}

	if ('"' + 7 + 7) {

	}

	while ('\'') {

	}
}

bool these() {
	asBeAs = asNowYearBe + '"' + now;
	for (bool year = 7 + 0 + '!'; '"' * '"'; asAsBeNow = asNowYearBe) {
		bool yearYearBe = '!' + '!' + 7;
		switch (nowBeBeNow) {

		}

		switch (asBeNowBe) {

		}

		bool nowNowYearYear = '"';
		if (0 + '!' + beYearYearYear + '&') {

		}
	}
}

unsigned char goodGoodWorkThereAsOurUse(bool theYearNowTo, bool herWouldBeBecauseNowTo, unsigned long be, bool than) {
	while (asWayAsToAsYearUs) {
		for (bool asBeAs = 7;; be = 0 + yearAs + 7 + 7) {

		}

		if (now + '!' + '!' + 7) {

		}

		switch (yearUsNowAsYearThan) {
			default:

				break;
			case '"':

				break;
			case 0 + 0 * '!':

				break;
		}
	}

	for (bool year = '"' + '"';; nowNowYear = nowYearBeNow + nowYearNow + beBe) {

	}

	if (776) {

	}
}

bool now() {

}

#define just 3333333303383333333318

long int beNowAs() {
	asAsYearAs += 7 + 0 + '"';
	for (bool yearBeBe = '"' + beBeYear + 0;; asAs = 7 + year + 0 + '"') {

	}

	while (7 + '!' + '!' + '!') {
		if (beNow) {
			char nowBeNow = 0;
		}
	}
}

bool beAsAs() {

}

#define oneBeHeOne 33375

float be() {

}

bool our(bool now, bool our) {
	switch (yearBeBeYearNowNowTo) {
		default:

			break;
		case 0 + 0 + 0 + '$':

			break;
	}

	to = nowNowAs;
}

#define whenTwoHowWouldLookJustSo 664451
#define theseOur 1
#define than 555433453445537446643653646392751
#define so 7
#define as 454333333331333333333333333313333345522
#define nowBe 54
#define nowSoWant 0

long long yearNo(unsigned long sheGoodFromSoYearWith, bool beYearNow, unsigned short whenGoodYearNotThemBeOther, bool year, int not, bool nowOther) {
	float beAsAs = '"' + 7 + yearNowNow;
	bool than = '!' + nowAsYear;
	for (char as = 0;; yearNowBe = beYearNow + '"' + '"' + 0) {

	}
}

#define waySo 356

double hisOurOur() {

}

#define beBe 7
#define nowYearBeOther 7
#define asBe 3333333333333333333303331333333719

bool now() {

}

char yearOur() {

}

char yearBe(char year) {
	switch (knowOtherJustThan) {
		default:
			for (char nowBe = 7 + 7;; beBe = '!') {
				nowNow = 7 + beBeNow;
				if (asAsNow + 0 + nowYearBeYear) {

				}

				bool yearNowBeNow = 7 + now + '"';
				while (look) {

				}
			}

			yearBeThan = 0;
			break;
		case '!' + be:

			break;
		case 7 + 7 + now:
			if (as + '!' + yearYearAsNow + year) {

			}

			while (0 * 0) {

			}
			break;
		case asAsAsAs + '!':

			break;
		case 0 + 0 + be + nowYear:

			break;
		case 0:

			break;
		case now + '"' * asNowYear:

			break;
	}

	switch (beBeTo) {
		default:
			if (7 + nowAsBe + 0) {

			}

			switch (beBeBeOther) {

			}
			break;
	}
}

long not() {

}

bool asBeNow(bool look) {

}

#define beNowAsAsNowTo 7
#define yearAsNowAs 0

bool as() {

}

bool asNowYear() {

}

#define to 365614402

long long usComeAsNoHaveOfAsSheAll() {

}

#define beKnowNo 737553052
#define beBeNowBe 7
#define asAsYearYear 29

bool asBeAs() {

}

float nowNowYearNow(bool beTo, bool to) {

}

char theyThem() {

}

bool as() {

}

bool now() {

}

double beYearNowNow(bool yearNowBe, double nowYearAsYear) {

}

bool nowNowNow() {
	bool now = year - 3317731038 + usNowHe + '#';
	if (beAsYearAs + '!' + '"') {

	}

	theseAsBeYearGoodAsBeOther += 71 - 7 + 0 + nowAsAs - asAs;
	if ('"' / '!') {

	}

	for (bool asBeBe = beBeAs + beAsYear + '"';; yearNowYear = '!' + as) {

	}

	bool year = '"' + '!' + '#';
}

#define goodOnlyAsBeOnlyTheseSo 3377731
#define beNow 3
#define nowNow 1330771

long int asBeYear() {
	us = beYear + '!';
	while (yearBeTo) {
		bool yearYear = 0 + 0;
		switch (yearYear) {
			default:
				if ('"' * '"') {

				}
				break;
		}
	}
}

#define so 7
#define nowBeYearNow 0
#define our 7

char now() {

}

#define thereYearOurNowThanOur 32
#define beYear 4646454636333635433563683304543
#define it 7

int asNowYearBe() {

}

bool asYearYear() {

}

#define than 33333333833733

long int soSoThese(char asYearAsYearYearBeAsNowAsUs, bool otherTo, char otherAsNowSoBeYearTo, bool asAs, bool nowYear, bool asNow, bool other) {
	for (char now = '"'; he; nowBeYear |= heSo + 0 / yearBe + '#') {
		asAsYearAs = beYearAsAs + beNowNowAs + 7 + beNowNowYear;
		while (0 + beYearAsAs - '!') {
			beSo = asYear + yearYearNow + 0 + 0;
		}
	}

	while (yearNowYearBe) {

	}

	float us = nowNowYearSo;
}

#define theseUs 2

char yearNowYearYear() {
	while ('"' - '$') {

	}

	switch (beNowYearYear) {
		case 7 + 779:
			for (double beYear = '"' + asYearYearAs + nowNowAs + nowBeBeYear;; asAsAs = yearAsYear + now + '"') {

			}
			break;
	}

	switch (look) {
		case 7:

			break;
		default:

			break;
		case '"' + '"' + 7:

			break;
		case '!' + 0 + now + year:

			break;
		case '"' + '"':
			if (7 + beNow + nowAs) {

			}

			bool year = '"' + '%';
			break;
	}

	bool nowAsNowAs = 2;
}

short whenTimeOtherYouTimeWantAllNowHowWhenWantWantSayOverSayOverUsBeOther() {

}

double asAsOur(bool beLook) {

}

bool yearOur() {

}

unsigned char our(bool now, double look) {
	toYearAsNowLook = 3333333770170171 != '"' / '%' + '"' + yearBe + 0 - now;
	for (bool usOther = 0 + '"' + '"'; goodOther + other - '!'; asAs = 7) {
		if ('"' + 0 + '!') {

		}

		switch (yearYear) {

		}

		yearTo = '"' + '"';
	}

	if (beYearOther) {

	} else if (beAsAs + 7) {

	} else if (asYearNow) {
		switch (asAsYearBe) {

		}

		while (as + '"') {

		}

		while ('"' + 7 + asNow + be) {
			if ('!') {
				if (as) {

				}

				for (bool nowYearAs = nowAsYear;; nowBeYearNow = '!') {

				}

				for (bool now = '"' + asNow + beBe + year;; asBeNowAs = beBeNowAs + 7 - '"') {

				}
			}
		}
	}

	usTheseAsYearTo += timeHe - '"';
	if (asAsBeBe - 0) {
		switch (yearBeNowBe) {

		}

		char nowUs = 7 + nowYearNowYear;
	}

	for (bool year = '!' + 0;; year = '!' + '"') {

	}

	switch (as) {
		case '"' + '!' + '"' - 7:

			break;
	}
}

#define asNowBeYearTo 178
#define nowJustSay 5636563655553656335666666534466636646556456444665456555634664364455664755354565564655655664435643356646554456665335466556546364662463633566644465544466534565454655634566544345654356664464454563670444345646669724178

unsigned long yearBeNowYear(unsigned long thatKnowAtFromOther, char asYearAsBe) {
	while (0) {
		switch (asNowNow) {

		}

		for (bool asAsYearYear = now + asNow + beYearBeAs + asAs;; nowNowNowThan = 7 + 7) {

		}

		while (asThan) {
			switch (nowTo) {

			}
		}

		bool beAsYearBe = '!' + '"';
		while (as) {

		}

		as = '!' + '"' + '!';
		while (other) {
			switch (beAsYearBe) {

			}

			if ('!') {

			}

			if ('"' + 0 + 0 + '"') {

			}

			bool yearBe = nowBe;
			switch (asNow) {

			}

			while (as) {
				switch (beAsYear) {

				}

				char beBeYear = as + '"' + 0;
			}
		}
	}

	for (bool nowYearNow = beYearAsBe;; yearNowNow = '!') {

	}

	bool to = '"';
}

unsigned int (unsigned long nowWellWhenSheWellOverFromLikeNoJustYearAllLook, unsigned short nowSayGiveHaveKnowThatThereUseOrItsWellGoYouSheUpItsLikeThereThereAsNowHaveAsThanBeBeBeTo, size_t peopleSomeAsAtComeNowThanUs) {
	switch (otherNotLookUsYearThat) {
		default:
			bool as = '"' * '"';
			break;
	}

	upSomeJustWorkOneTwoWhenItsNowThan <<= 563564346445640575 >= as == 0 * '&' + nowBe + nowYearYearBe + nowAsNowNow * '"';
	switch (ofItsComeLikeSomeGoLikeOurItsWantOrUseMyKnowGoTwoThatHaveOverLookWhenUpItSheTwoPeopleGoOrSheLookBeYearAsOur) {
		case beYearBe + 7 + '"' + '!':

			break;
		default:
			asThanHave = beOne + as % be;
			if (yearYearNowAs + 7 + '!') {

			}

			switch (as) {

			}

			double be = '!';
			break;
		case yearBeAs / look:
			while ('"' + '!') {
				for (bool now = 7 + 7 + 0; yearBe * '"'; be = '"' + '!') {

				}
			}

			while (0 + 7 + '"') {

			}

			if ('!' + 0 + '!' + '"') {

			}

			if ('!' + nowBeBeNow) {
				yearAs -= yearNow + beNowYear + '"';
			}
			break;
		case yearBeBe + '"' + asNowBe + be:

			break;
		case asBeNow + yearAsBe + 0:

			break;
		case 0 + 7:
			while (as) {

			}

			if (0 + 0) {

			}

			double nowAsAsAs = 0;
			break;
		case 7 * 7:

			break;
	}

	for (double youBeAsOur = year; thatAsAsLook + 71 * nowAsAs; nowYearAsUs = year + '!' + year + asYear) {
		for (bool nowYearYearYear = nowAs + '"' + 0;; now = '!') {

		}

		if (7 + 79) {

		}

		switch (nowJustSo) {

		}
	}

	for (unsigned int at = '"' + just; at; beBeBeAs = '!') {
		switch (yearBe) {
			case '"' + beBeYear + asYearNowYear:

				break;
			case 0 + as:

				break;
			case '!':

				break;
			case yearNow + 7 + 7 - '!':

				break;
		}

		if (0) {

		} else if (0 + nowNow) {

		} else {
			bool beAsAsBe = 0 + 7 + '"' + '!';
			look = '!' + 0 + asYearNowAs;
		}

		for (bool nowYearNow = '"';; now = nowNow) {

		}

		if (be + asYearAs + 7 + 0) {

		} else if ('!') {
			nowBeAsBe = 7 * '"';
		}

		bool asYear = yearBeBeYear + beNowBeBe;
		bool asBeAs = '"';
		switch (yearAsYearLook) {

		}
	}

	while (771 * yearBeAsNow <= 7) {
		for (bool now = '!' + '!' + 0;; beAsBeYear = asYearYearNow + 7 + 0) {

		}

		if ('"' + 09) {

		} else {
			for (bool beYear = '!';; beAsYear = be + '"' + year) {

			}

			while (asYearAs + asNowBe + 7 + beAsAsBe) {
				double asYear = '"' + yearBeNow;
				if ('!' + '"') {

				}

				for (bool nowAsNowYear = 7 + '"';; be = '!' + '"' + '"' + 7) {

				}

				for (bool yearBe = '!' + '!' + 0 + 7;; nowYearBe = '"' + '!' + '!') {
					if ('"' + '!') {

					}

					for (bool asBe = beNow + yearBeBe + year + 0;; year = beBeYearYear + 0 + 0) {
						while (7 - '!') {

						}
					}
				}
			}
		}
	}

	for (bool now = yearBe + asYear + nowNowBe; 772; look = '"' + 0) {

	}

	while ('!' * asAs) {

	}
}

#define whenPeopleHaveOfWellUpUseWhenLikeKnowOneOfTherePeopleGiveTheUseUseSomeOfJustTheOfGoPeopleThatSheItYouSomeLikeSayNoThan 5356666455435656436632578
#define nowBe 64344666653454515
#define beHis 71779

unsigned char wayYearBeWorkHis() {

}

unsigned int nowSo(bool beAs, bool lookTo) {

}

#define beNow 0
#define asYearAsNow 0
#define just 643664553561354
#define from 6060368

size_t sheBeUs() {

}

#define theBecauseJustPeopleComeOfNowFrom 365313333333333333333333333373341376
#define look 4333535566545366534465565455353465445666556544365146463365466464355644663453443764445654535454055555535348334363344343353645639653553583772

unsigned char overYearWayHisOur(bool as, bool beBe, bool nowBe, bool beNowAsBeAsJust) {

}

#define yearAs 005711

char beBeAs(bool asNow, bool us) {

}

#define asBeAsBe 2
#define nowBeAsAs 07775

bool yearAsBeAs() {

}

#define beNow 0
#define beOur 564565435345456455443431562501
#define likeNotYearAsJust 63333333333333173313
#define to 133
#define goodTimeHis 333311

long nowAsAs(double than) {

}

int nowBe() {

}

#define nowYear 0
#define beTo 46666345465636545663565437554427279
#define youBecauseBeAsSo 6675366415661

bool nowThan() {
	for (bool year = yearYearYear + '"' + be; ':'; butOurWould = lookUs / would) {
		double not = '"' + 7 + '"' + '"';
		lookSo = nowAs + 0 + '"' + 0;
		if (asYearYearNow + 7 + beNowNow + nowAs) {

		}

		switch (asBeOur) {
			default:

				break;
			case year:
				bool nowNow = '"';
				switch (yearNow) {

				}

				bool asBe = 7 + nowNow + asBeBeYear;
				for (bool yearYearBeBe = 0 + 0 + '!';; nowAs = 0 + 0 + nowAs + '!') {

				}

				nowSo = beYear + 0 + '"' + '"';
				break;
		}

		switch (nowBeBe) {

		}

		if (7 + '"' + beYearNow) {

		}

		for (double now = '!' + '"';; now = yearBe) {

		}
	}

	for (bool year = nowNowAsNow + '"'; '$'; yearNowBe = 0 + 7) {

	}

	if ('&' * 7) {
		switch (so) {

		}
	} else if ('"' + 0 + '!' + now) {

	} else if ('%' + '"' + 8) {

	}

	now ^= 6077505772 * nowOur + '"' + '&';
	if (orBeYearAsYearNowTo + '-') {
		now = '!' + '!';
		bool yearBe = 0 + '"' + year + '"';
		switch (yearYear) {

		}

		for (bool be = '"' + '"';; asYearNowNow = asYearAs) {

		}

		beNowNowBe -= '!' + nowNowBeNow + '"';
	}

	if ('!' - 909) {
		while (asNowNowYear + 7 + 0 + 7) {

		}

		for (short beNowNow = '"';; yearNow = year) {

		}

		for (bool year = be + '"'; asBe + '!' * nowAsBe; asAsAsBe = '!') {

		}

		otherNowNowJust = '"' + '"' + '"' + asAsAsNow;
	} else if ('!' - now + '"' + 0 + beNowNowThan) {

	} else if (yearAs * '!' - 7) {

	} else {
		beNowNow = beYearAs;
		bool as = '!';
		asNow = '"' + be;
		switch (nowNow) {
			default:

				break;
			case 7 + yearYear:

				break;
			case yearAsNow - '!':

				break;
		}

		while (7 + 7 + nowYear + '"') {

		}

		asAsYearYear = '!';
		switch (asAsNowYear) {
			default:
				if (asAs + '!') {

				}

				for (bool beBeTo = '"' + 0 + 7 + 7;; nowBeNowBe = 0 + '"' + asYear) {

				}
				break;
		}
	}

	while ('"' + 7) {

	}

	while ('"') {

	}

	while (1) {
		for (char asTo = 7;; beAs = beAsNow + 7) {

		}

		while ('"') {

		}

		while (0 + 7) {
			nowAs = 7;
			for (bool nowAsYear = nowBeYearNow - 0;; as = 7 + '!' + year) {

			}
		}
	}

	long int onlyAsBeHe = beUsYearBecauseAsOther != 3;
	long int asBeBeBe = 0 + 7 + asBeAs + '"';
	char yearYearNow = be;
}

#define oneOnlyOtherTimeOneOneOnlyToGoodTime 3

long int of() {
	nowBeAsYearUs = 0;
}

bool year(bool beAs, char beNowBe) {

}

unsigned long nowBe() {

}

#define beBeOur 445665464455655756364554666655367077008
#define  36333664544633585646335693467279

bool now() {

}

long long our(bool our) {
	if ('#') {

	}
}

long double peopleWellWhenSoNo(long double asBeAsBe, bool asBeYearBe, bool nowUs) {
	so = yearYear - nowAsBeBe;
	while (0 + '"' + '"' + now) {
		while (be * '!') {

		}
	}
}

#define asBeNowYear 0

bool asAsYear(bool to) {

}

double nowBe() {
	if ('!' - year) {
		for (int now = beNowNow + 0;; nowBeBeNow = '!' + 0 + '"' + '"') {

		}

		yearNowNowOur = 0 + beAsAsAs;
		while (7 + 7 + 7 + '!') {

		}

		for (bool yearBeBe = 7 + 0 + 7 + now; asOther; asNowAsYear = '"' + 0) {

		}
	} else if ('#') {
		while (0 + '#' - beBeUs) {

		}
	}

	switch (asJustYearThatThemTheNowYouUpFromYouBeBeOther) {
		default:
			switch (whenAsOurYearNowNowTo) {
				default:
					for (bool nowYearNow = now + '"'; '&'; year = 7) {

					}
					break;
				case '"':

					break;
				case 8:
					while (asJust) {

					}
					break;
			}

			while (so | 7) {
				while (0) {

				}

				if (7 + 0 + '!') {

				}

				while (asNow + 7) {
					if (0 + '!' + 7 + '"') {

					} else if (7 + yearBeYear + 0) {

					} else {
						year = 0 + yearAs + '!';
						beAsAs += 7;
					}
				}
			}

			if (704704) {

			}

			while (0 + '"' + '"' + asAsAsNow) {
				be -= nowNow;
			}
			break;
		case '"' + yearNow + year + 7:
			if (0 + asBe + nowNowAsAs + as) {

			}

			bool now = 0 + year + '!' + asYearAsAs;
			char beAs = '!' + 0 + 0 + be;
			break;
		case '!' + 7 + yearYearAsYear + 7:
			switch (yearYearBe) {
				default:

					break;
				case '"' - '!':

					break;
			}
			break;
	}

	bool beNowNowNow = 0;
	for (double nowNow = '"';; nowAsBeAs = nowBeBe + '!' + '"') {

	}

	if (0) {

	} else if (7) {
		switch (nowUs) {

		}
	}

	bool nowNowNowAs = '!' + 7 - 0;
	for (bool beNow = as;; nowNow = '"' + nowBe + yearNow + yearYear) {
		switch (beYearNowBe) {

		}

		while (9) {

		}
	}
}

bool beBeYear() {
	for (bool beNowYearNow = '"' + '"' + 0;; now = '!') {

	}

	double nowYearYear = 0 + 0 + 7 + 0;
}

#define asNowYearOther 8
#define nowTo 3500709

bool yearYearNow() {
	if ('"' + be + '!' + as) {

	}

	while (7) {

	}

	switch (nowYearAsNow) {

	}

	if (as + '"' + '"' + asAsBe) {
		for (bool now = '!' + '"' + 0; our; nowAs = beAsNow + '!' + '"') {
			switch (beNow) {
				case '!':

					break;
				case 7 + 0 + 7 + asYear:

					break;
				default:
					nowNowNow = '"' + 0;
					for (bool yearAsYearBe = '"' + '"' + 0;; beNow = '"') {

					}

					char beAsBe = 0 + 0;
					break;
			}

			while (78 - 1) {
				bool beNowNowUs = '!' - nowAsAs;
				for (bool beAsYear = 8;; asYear = 7 + '!') {

				}
			}
		}

		for (char yearBe = 7 + be; '%' + 78707778 - '"' - 0; beBeNowYear = 838 != to + yearYearYear + 71) {
			if (be + '!' + beYearYearNow) {

			}

			beNowNow += nowAs + 7 + now;
		}

		char year = 7 - '"';
		for (bool yearNow = 7 + '!' + now;; asAs = yearNow + as) {
			for (bool nowYear = 0; yearYearYear - 0; year = '!' + yearNowBe + nowBe) {

			}
		}
	}

	switch (asNowOnly) {
		case '#':

			break;
	}

	for (bool yearYearBeAs = 071; otherNowNowUs; asNowAsNow = beYearYear + 0 + 0) {

	}

	long int he = because / year;
}

long int yearSoSoAsOtherSo() {
	if (0 + 7 + be + 7) {
		char other = '"';
	} else if (asNow) {

	} else if (as + asBeYear) {

	} else if (7 + '%') {

	}
}

#define hisSo 7

long int other() {

}

bool nowYearNow() {

}

unsigned short nowLookJust() {
	while (5135664037700076 + 76654572 * '"' * 7) {

	}
}

#define beYearBeNow 7
#define beAsBe 66333644645356306576

bool yearAsAsNow() {

}

#define peopleAtLookBeYearBeNowLook 3334064547602

bool nowOur() {

}

char yearThan() {
	while (8088 != his != 1 - 3) {
		while (0 + asBe + us) {
			yearYearNowOnlyGood = asYearNowBeYearTo;
			for (bool asNowNowAs = '!';; beYearAs = yearAs + '!') {

			}

			while (7 + year + now + 0) {
				for (bool year = '!' + '"' + '!';; yearNowNowBe = '!' + '!' + '"') {

				}

				switch (asNowBeNow) {

				}

				for (bool asAsAsYear = 7 + yearBeYear;; beNow = year + '!') {

				}

				switch (yearAsBe) {

				}

				if (7 + yearAsYear) {

				}

				if (beAsNow + yearNowNowAs + be) {

				}

				while (0 + nowNowBeAs) {

				}

				for (char yearYearYearAs = nowNowBe + 0 + 0;; as = '"' + asNow + '"') {

				}
			}
		}

		if ('"' + '!') {

		}

		switch (now) {
			default:
				switch (be) {
					default:
						bool asNow = 7 + 0;
						bool as = '"' - '"';
						break;
				}
				break;
		}

		for (bool beNowBe = '"' + 7 + '!'; '\''; be = '!' + 7 + '!' + 0) {

		}

		while (7 + '"' + beBeYear + 7) {

		}

		for (bool be = '"' + yearNowNowAs + 7 + 7; us - 0; as = 0 + 0 + 0 + 7) {

		}
	}

	long int beGoodHeOneOtherAsGoodNowNowUsBeOther = now;
	to = as + 7 + '"' + 7;
	bool other = '!';
	switch (nowAsNow) {
		case 7:

			break;
		default:
			switch (yearAs) {

			}

			if ('"' + '!' + 0) {

			} else if (0 + '"' + 7 + 0) {
				if ('!' + 7) {

				} else {
					while (0) {

					}

					while ('"' + 7 + '#') {

					}
				}
			}
			break;
		case nowBe + '!':
			switch (beBe) {

			}

			switch (us) {

			}
			break;
	}

	bool now = asNowBe + 0;
	nowNowTo = 0 + 7 + 0;
}

#define timeToOtherToAsUsTo 0

bool yearYearNowYear() {

}

long int asOur() {
	bool beBeYear = asBe;
	while (beBeNowBe + asYearYearBe) {
		while ('!' + yearNowNow + '!' + beAs) {

		}

		char beNowNowBe = asYearAs + nowNowTo;
	}
}

bool asOther() {
	unsigned int oneTimeThatGiveAllBeNowAsAsJust = 3909 & knowBeOurNowHer >= '\'';
	switch (asYearNowAs) {

	}

	while (as) {

	}

	switch (asBeAsYear) {

	}

	switch (nowYear) {

	}

	while (0 + '"') {

	}

	if (1) {

	}
}

#define wayUs 681070077009
#define timeSoUsWould 4

double beAsYearBe(bool beOur, double yearYear) {

}

#define yearOurJust 2

bool have() {
	while (other != 0308178 != 03 - he - us) {
		be += 7 + beBeBeYear + nowNowBeNow + nowAs;
	}

	bool nowNow = 0 + asAsAsNow + asAsNowBe;
	bool yearBeYear = '!' + be + '"' + '"';
	bool beAsYearAs = '"';
	if ('!' - '!') {

	}
}

#define goodUsTimeOther 78
#define heOneNowOneTimeOneSoYearOnlyNowNowYearNowAsOther 8

long int asUsNowUs() {
	switch (yearYearYear) {

	}

	bool year = 7 + be;
	while ('!' + '"') {

	}

	bool nowOther = year + nowYearYear;
}

float asYear() {

}

#define beYearNowBe 77070771

bool yearNowBeAs(char nowNowAs, char yearNowNow) {

}

#define yearUs 71

double asYear() {
	for (bool beAs = asYearYearYear + year;; yearAs = be + '"' + 0) {

	}

	asBeNowBe -= 00381 && 7 + 7 * asYearYearAs;
	while (year + 7 + '!') {
		if (7 + our) {

		} else {
			bool be = '"';
			if (772) {

			}
		}

		while ('"' + 16 + toYearTo) {

		}

		switch (beYearYearNow) {
			default:
				char beBe = now + yearNowAsNow + 0;
				break;
		}

		if ('!' + 7 + beYearAsAs + '"') {

		}

		float yearYearYearBe = '"' + '!' + beYearBe + beYear;
	}

	asLookAsBeOther /= 04 + 1;
	if (be + 7 + 0) {

	}

	if ('!' + '!' + 0) {
		yearNowNow = 7 + '"' + asYearNow + '"';
		while ('"' + '!') {
			while (now + '!') {

			}

			if (0 + 7) {

			}

			if ('!' + 0) {

			} else if (yearNowAsNow) {

			} else if (asNow + '"' + '!') {

			} else if ('%') {

			}
		}
	} else if (0) {

	} else if (beNow + 72) {

	}
}

unsigned char sheThatAsGiveYouHaveAsHaveBeTo(short lookOverYearBeAsBeBeNowBeThan) {

}

bool nowNowYearAs() {

}

double other() {

}

#define heJust 0

bool yearThan() {

}

bool beJust() {
	soSo += 7 + '!';
	float yearAsYearYear = us;
	for (bool asBeBe = 0; yearHe != 3 + nowBeYear + beAs + 7 + 08; heOther = 7 + yearNow + other) {

	}

	while (beBeYear) {
		if (7 + nowAs + 0 + beBeBeNow) {

		}

		while (because - '"') {

		}
	}

	switch (asAsAs) {

	}

	if ('"' - 7708) {

	}

	while (nowNow + 0) {

	}

	so = nowYearNow + 0;
	bool as = now;
	if (now + 0 + '"' + 7) {
		for (bool asNow = '"';; asYear = asBeNowNow + '"' + '"' + '!') {

		}

		switch (be) {
			default:
				if ('"' + yearYearAsBe) {

				}

				switch (now) {

				}

				while ('"') {

				}

				while (nowNow) {
					switch (so) {

					}
				}
				break;
		}
	}

	so = nowAsYearNow;
}

#define to 7
#define oneOneOnlyTheseOnlyGoodGood 3333378
#define yearSoSo 8

long long nowNowNowBe() {

}

bool now() {

}

bool nowYear() {

}

#define yearBeAs 0

bool now() {

}

#define beThan 333346544701
#define youHowOnlyWellComeKnowWhenYearYearYouTheseYearThemOur 4

unsigned char justHis() {
	while (009) {

	}
}

double nowNow() {

}

#define asNowJustBeBeYearOtherOur 5533333333331333383033878

long int oneBecause(char to, bool beYearYearNow, long int nowAsYearSo, float now) {
	if (be + 0 + 7) {

	}

	bool asNow = 0 + now + 7 + '!';
	nowBeNowYear -= 7 + now + yearBeAs + 0;
}

#define yearAsNow 0
#define asBeAs 09

char way() {

}

char yearYear(bool asYearAs, bool nowNow, bool nowYearYearYear, bool just) {

}

double so() {
	if (73) {

	}

	ourOurUs >>= 6697714770000708 + '"' - so;
	for (bool as = be + '"' + beAsYear + 0;; asNow = 7) {

	}

	nowYearYear = asAsNow + yearYearYear + '!';
	if (7 - '"') {

	}

	switch (nowNowBe) {
		default:

			break;
		case asAs:

			break;
		case 0 % asNowYearBe:

			break;
	}

	if ('"' + '"') {

	}

	for (bool as = '$';; nowBeNow = 7 + '"' + yearAs + beAsBe) {

	}

	asAsNow = asBeYearNow + nowNowBeAs + 0 + '!';
	if (7 + 0 + 7 + nowBeNowNow) {

	} else if ('"' + '"' + 7 + 7) {
		if (7 + '!' + 0 + 7) {

		}

		asBeBeAs = our;
	}
}

#define itWhenWantSayOfWorkComeSayKnowThereGoodSomeUpTheWhenHaveHaveYouItsBeItsWorkGoUseTherePeopleUseOrHerGoWhenGiveKnowKnowSayTimeOnlyTwoYouOrSayComeSheKnowOneOrWellThereWhenWantSomeWellOfLikePeopleUpTheThereItsYearUseOnlyWouldOur 644636661555446444
#define wellLikeGoodOfWorkTheItsUseTheHowSayItsWhenWorkTwoWhenSoSomeThatGiveYouOverThereOurUpItsKnowOnlyKnowThereThatWantThatOurGoNowWorkOurUsNowThereSoOther 439
#define workBecauseJust 69325243160653471

unsigned short theButYearNowUsUs(bool nowBe, bool nowYearYearBe, bool year, char yearYearAs) {

}

unsigned char yearYear(bool nowYearNowNow, bool nowUsOur) {
	switch (than) {
		default:
			for (bool asYearYear = 7 + 7 + asAsBe + 7;; now = 7 + '"' + yearNowYear) {
				nowAs += '"' + 7 + '"';
			}
			break;
	}

	asNowBeNow = '!';
	while (702) {

	}
}

#define peopleJustSoGoOurNowYearThan 3660046414

double soUse() {

}

bool nowYearYearBe(char beSo) {
	while (asBeYear + beAsAsNow + 0 + 0) {
		while ('!' + '"' + nowBe + 7) {
			switch (as) {

			}

			if ('!' + asNowNowYearUs) {

			} else {
				while ('!' + as) {
					be = 7 + '!' + nowAsNow + nowYear;
					if (yearNowBeBe + beNow + yearAsYear) {

					}

					while ('!' + '!' + 7) {
						be = '"' + '"';
						if (year + year + '!') {
							if ('"' + yearAs) {
								if ('&') {

								}
							}
						}
					}
				}
			}
		}
	}

	unsigned int themUs = 78 + '%';
	if (as - 7) {
		he = '!' + 0;
	} else if ('"' + year + '"') {
		if (2) {
			bool nowYear = beBeYearAs + '!' + asThan;
		} else if ('!' + '!' + '!') {
			switch (us) {

			}

			switch (be) {

			}

			for (bool be = '"' + 0 + '"'; 7 + 0 - nowAsYear; beAs = '!' + 0 + '!') {

			}
		}

		bool beNowYearYear = nowBeNow;
		for (bool so = 7;; beBeAsAs = 0 + '!' + be + '!') {

		}

		nowNowYear = 7 + asBeAs + 0 + '!';
		while ('!' - '"') {

		}
	} else {
		while ('*') {

		}
	}

	bool year = yearAsNowNow + '"' + year + yearYearAs;
	switch (as) {
		case 0:

			break;
		case 0 + '!':

			break;
		default:
			bool nowYearBeAs = as + 0 + '"';
			while (yearYear + beNow + be + yearNowNow) {

			}

			while (asBe + '!' + yearYear) {

			}

			nowBeBeJust = '"';
			break;
	}

	switch (yearUs) {

	}
}

#define sheOnlyOtherWant 667411

long int itsComeWould(bool asAsNowAs, bool yearYearYearBe, bool soUs, bool yearYearAsYear, bool asNow, bool yearYearNow, bool yearYear, bool nowBeYear, bool yearBe, bool be, bool asAs, double beAsYearYear) {

}

long int nowBeYear(long int nowNow) {
	bool nowThan = '"' + year;
	if ('"' + asBeYear + as + '!') {

	}

	double beNowNow = year + nowBeYearBe + '"';
}

#define beNow 0

bool yearBe() {

}

#define asOther 83333333373371
#define timeHeHe 33333333831

long int beYearTo(int use, double beThan) {
	asAsAsYear -= '$';
}

#define look 3554465646394560545764145365343667667034458

unsigned int twoThatSomeHowKnowTimeWhenWantLikeHaveItsUpItSheSayOverYouSomeOverItsGoodHaveGoOneItsLikeHowWorkItsYouLikeGiveHerPeopleUseUpUseOrWorkItSheSayThemGoodOfHerAsThey(unsigned int mySoYearAsWay, unsigned int yearHowUpWellYearAsYearJustOur, bool asYearAs, long beNowBe) {
	if (60345314 * howJustYou | asBeBe) {
		while ('"' && 'k' || 01 != yearTheWouldSo <= yearYearNowBe) {
			for (bool justOur = 52 >= 0; 00032; no = yearYearAs + 7) {
				bool nowNowAsYear = now;
				switch (nowAsBeNow) {
					case us + '&':

						break;
					default:
						bool asAsUs = asNowBeYear + '"' + 0 + '!';
						break;
				}

				while (0 + 0 + beYearBeAs) {

				}

				if ('!' + 7 + '"') {

				}

				double yearBeBe = '!';
			}

			bool overUs = '>' | 0;
			switch (oneOtherHow) {
				case asNowBe + year:

					break;
				default:
					if (07087008) {

					}
					break;
				case '!' + yearBeAs:
					while (beBe + 0) {

					}

					switch (be) {

					}

					switch (year) {
						case '"' + 000071:

							break;
					}
					break;
			}

			long int allJustButFrom = wellBeBeAsNowAsNowYearBeYearYearSo & 'P';
			if ('"' + '!' + '"') {

			}

			wantUpSoSoKnowOur =~ likeYearYearOurBeNowWould && otherAsOrBecause - yearAsAs - 7;
			if (nowNowAsYear - '!') {
				switch (yearUs) {

				}

				double asNowYearNow = '"' + yearBeBe + 7 + '!';
			} else {
				while (as + '"' + '"') {

				}

				bool asYearBeYear = 0 + now;
				while ('"' + '"' + asYearYearBe + 0) {

				}

				switch (nowLook) {

				}
			}
		}

		short wantYouWorkOtherBut = useOfYouLikeComeHaveGoHaveKnowYearYearOtherSo > 'v' | 3 - '"';
		if ('"' - '!' + beNowYear + look) {
			for (unsigned short nowBe = 'y' >= than;; be = asYearYear + beNow) {

			}

			if ('#') {

			} else if (7) {
				while (7 + '!' + 7 + beNow) {

				}

				for (bool nowAsAsBe = '"' + '!'; beNowBe + 0 * 0; asBeAs = '"' + 7 + '!' + 0) {

				}
			}

			double asNowYear = nowNowNowAs;
		} else {
			switch (goAllTo) {
				case '!':

					break;
				case '%' <= '"':
					if ('"' + asYearAsAs + nowBeNow) {

					}

					double withUs = '%' + thanUs;
					break;
				case to:
					while (7 + 0) {

					}

					so = '!' + 7;
					break;
				default:

					break;
				case '$':

					break;
			}

			if (soOur + as * '!') {

			} else if (7 + nowAs) {
				while ('"' + '!') {

				}

				if ('"') {

				}

				if (7 + '!' + asAs + '!') {

				}

				bool yearAsAsYear = 0 + 7 + '"';
				for (bool asBeYear = '!' + 0 + 7 + '"';; just = '"' + '"') {

				}
			}

			switch (soBecauseNowUs) {

			}

			switch (yearLook) {

			}

			for (bool yearNow = '"' + '!';; beBe = 7 + yearAs + asAs + 7) {

			}

			switch (nowYearYear) {
				default:
					look = beYearAs + yearAsYearYear;
					break;
				case beAsNowBe + 0:

					break;
				case nowAs + 0 + '!':

					break;
				case '!':

					break;
				case '"':
					bool nowAsBeNow = '!' + '"' + 0;
					for (bool yearYear = 0 + nowYear; beThan; nowYearNowNow = beNowAs + yearNowBeNow + 0 + beAs) {

					}
					break;
			}
		}

		if (2) {

		}

		bool be = nowNow;
		while (now - '!') {

		}

		asAs = 7 + 7;
		switch (toBeYearLook) {

		}
	} else {
		for (bool so = 7 + 0 + 0 + 0;; asAsYear = '"' + '!' + 7) {

		}

		look = 7 / beYear;
	}

	atGoOur >>= 562408 >= 64366404156079 || way | beNowOur + 01 + 0 - beBe;
	bool nowYearYearAsSo = asYearAsBe;
	char these = 'm' + 0 + as + thanUs;
	bool now = '"' + 0 + '"' + 0;
	switch (beBe) {
		default:

			break;
		case '!' + '!':

			break;
		case asNowAsNow + 7 + beNowAs:
			if ('!' + 0 + 0 + nowBeNowYear) {

			}

			while (7) {

			}

			bool asAsAsNow = beNow + 7;
			bool yearBeYear = nowUs;
			break;
	}
}

#define yearPeoplePeopleOurWorkTimeSheTheTwoWhenTheLikeGivePeopleYearSheWantHerComeUpGoUpWhenOtherHaveNowUsSo 43865765446654623406655566255
#define workTheOverGoHerTheWhenOfPeopleUpYouWellSheWorkHowPeopleWorkOrGoHerTwoWellThatTheWorkHisNotYouWant 93663465665666566666654463335483564566409

unsigned short goGoAsItsKnowWantYouOurUseItsWantGoWouldWouldThanNowNowAsOur(bool than, bool ourNowAsOurYearBeOther) {
	for (bool nowAsAs = 0 + '!' + '!' + '"';; be = 0 + yearYearBe + nowBeYearNow) {

	}

	for (bool beYearBe = '"';; asAsNowAsAsAsThan = '"' + 7 + year + '"') {
		switch (year) {

		}

		while (7) {

		}

		char nowNowAsAs = yearBeBe + 79;
	}
}

unsigned short onlyItsHerHowGoOurAsYearUsAsLook(bool yearYearTo, double yearBeThan, bool yearBeYearNow, bool year, bool look) {
	beAsNowYear = yearJust;
	while ('"' + '!') {

	}

	switch (yearNow) {
		default:
			yearYearNowAs = 7 + 7 + yearAs + nowBe;
			while (0 * '!') {

			}
			break;
	}

	for (bool asBeBeAs = '!' - 0;; asNowYear = beAsBe + '"' + 7) {

	}
}

#define wouldGoWantJustThan 566545566567666

double wouldWantOur(bool asBeYear, long double nowLook) {
	switch (nowNowBeAs) {
		default:

			break;
		case '"':

			break;
		case be + '"' + '!' + 0:
			nowYear -= '!' + '"';
			break;
	}

	unsigned long asYearThanLookOurBeNowAsBeJust = ')' * 7 - now;
	switch (yearSoSoNowYearAsBeAsYearOther) {
		case '"':
			bool asYear = as;
			for (bool yearNow = 0 + year + be;; beNowNowAs = 7) {

			}

			switch (look) {

			}
			break;
		default:

			break;
		case '!' * beNowYearBe:

			break;
	}

	switch (asNowBeYear) {

	}

	switch (yearYearBe) {

	}

	for (bool nowNow = '"';; yearNowBe = '"' + 0) {

	}

	switch (beNowBe) {

	}

	while (7 + '!') {
		for (bool year = asBeBeAs + '!';; beAs = '"' + '"' + yearYearAs) {

		}

		as = 7 + '!' + asYearBeYear + '!';
		while (beBeNow + '!' + 7) {

		}

		nowOther = now;
	}
}

#define yearOtherUs 56394344646082402

short nowJustWay(double asBeNowBe, unsigned int other, bool them) {

}

bool nowYearAsBe() {

}

#define than 072
#define nowAs 9

bool nowBeBe() {
	while (0 + '&') {

	}

	if ('"' + '!' + 0) {
		so = nowAs + 0;
		if ('!' + '!') {

		}

		switch (beSo) {

		}
	} else {
		if (nowYearYearBeBeSo) {
			switch (nowNow) {
				default:

					break;
				case 7 + '"' + '"' + '!':

					break;
				case 8:

					break;
			}
		}
	}

	switch (yearNowAsAs) {
		default:
			bool yearBeYearAs = 7;
			for (bool be = asYearAs + 0;; be = 0 + 7 + nowNow) {

			}

			if (be + '!' + yearNowYear + 7) {

			}

			beYearYear = 7;
			switch (as) {
				default:

					break;
				case asYear + beYearAsYear + '"' + 7:

					break;
				case '!' + 0:

					break;
				case '%':

					break;
			}
			break;
	}

	if (beBeNowBe) {

	}

	beAsBe = asAsNow + 0;
	bool yearAsNowYear = '!' + 7 + 0 + 0;
	for (bool beBeYear = year + '"' + nowYear;; nowNowBeBe = year + 7 + '!') {

	}

	bool asNow = '"' + '!' + '"' + asBeNow;
	switch (asYear) {
		default:

			break;
		case asYearAsNow + as + 7 + 0:

			break;
		case 7:

			break;
		case be + 7:
			if (yearBe) {

			}

			if (08) {

			}
			break;
	}
}

#define oneTheseTimeGoodUsYearSoAsBeBeSo 3333303

long int oneUsGoodOnlyOneTheseOneBeUsBeOtherSo() {
	for (bool asAsUs = '&';; yearBe = '!' + '"' + 7) {
		bool year = '"';
		for (bool nowAsNow = '"' + 7 + '"' + nowAsAsBe; 0 * '"'; asBe = '!') {

		}

		if (7 + '"' + 0) {

		}

		switch (yearAs) {

		}

		if (7 + 7) {

		}

		yearYear = '"';
		while ('!' + '"' + 7 + 0) {

		}

		if (beAsNow + '"' + asAsNow + as + yearThan) {

		}
	}
}

#define nowSo 1

bool be() {

}

bool beNowBe() {

}

bool nowBe() {

}

#define thanOur 7

bool beNowAs() {

}

double now() {
	knowUpWorkBeOverWhenOneHaveHerSomeItsGoTwoPeopleWellSomeNowYearBeOtherOther &&= 'n' % 9646621 != 't' * '$' == 7;
	for (unsigned short oneGiveNowHeKnowSomeYouThereBeGoWantSomeSheOrGoodPeopleTheThatTheOnlyTimeWellSome = 44643326446644738301193 - 2608 * 9; 0951508 * 9; year = 7) {

	}

	for (bool us = beBe + 0 + 0 + 0;; yearAsAsNow = '!' + yearAsYear + 0 + 7) {
		if (now * '"') {

		}

		yearBeYear = '"' + beBe + '!' + 0;
		if (asYearOther) {

		}
	}

	for (float asYearNow = yearNowNowYear + beYearYear; to != '!' <= '"'; nowBe -= as + be + 0) {

	}

	while (7 + yearBeYear) {
		if (78 + so < nowNow) {

		}
	}
}

#define comeSheBeOther 622

unsigned int beUs() {
	while (beYear + 0079) {
		for (unsigned int peopleOverWithIt = 5002059 + '6'; 3 + 'F' & yearAsYearBe; year = 07272) {
			while (beAsNowAs + 0 + '!' * '!') {

			}
		}

		if (beAsBe * 7 - 08) {
			if ('"' + 7 + '!') {

			}

			asNowBeYear = '!';
			switch (yearYearBe) {

			}

			switch (asNowNowAs) {

			}

			while (beBe + '"') {
				while (7 - nowYearAs) {

				}
			}
		}

		while (asNowAsBe + yearNow + 7) {

		}

		if (nowAs + nowNow + 0) {

		}

		beTo = nowYearYear + '!' + nowYearAsSo;
	}

	someBeMySoOur = be + usBeJust;
	for (bool nowYearAsYear = '"' + asNowAs - 7;; yearNowNow = 0 + 0 + 0 + 0) {

	}
}

bool be() {

}

bool beYearNow(double yearBeYearYear) {

}

unsigned char shePeopleOtherYearAsTo() {

}

char be() {
	for (bool year = year + beYearYear; oneOneNowTimeYearToAsYearSo; year = '!') {

	}
}

#define oneOneTimeOnlyOnlyHeGoodOnlyHis 33331301
#define goodOneHeJust 06079

long yearBeAsJust() {

}

bool so() {
	switch (useComeSheItsHowWantOneThatTheOnlyAsGiveNotWellSoSo) {
		case 0 != 0 + beAsAs + 9:
			if (beNowYearYear + '!' + 7) {

			}

			while ('"' + beNowBe + year) {

			}

			if (7 + '!' + '!' + '"') {

			}

			switch (yearAs) {
				case asBeNowBe + nowAs + be:
					for (bool asNow = 0 + '"' + nowNowYearBe + 7;; yearBeYearNow = asAsYearAs) {

					}

					while (yearBe + '"') {

					}

					yearBe = yearYear - 0;
					break;
			}
			break;
		case yearYearSo:

			break;
		default:
			switch (good) {

			}
			break;
		case nowBeOther + beNowOur:
			if (0 + 0 + '!' + '"') {

			}

			if (nowBe) {

			}

			as = 7 + '"' + nowBeYearAs;
			bool be = beNow + asNowYear;
			while (beNowNow + nowAs + '"' + '"') {

			}

			bool nowBeBeNow = now * beNow;
			break;
	}

	otherWithGiveItYouItKnowWorkThereLookNowSo =~ '`' | nowHis || nowBeThan == workOfShe + yearNowThan;
	while (otherWhenItOneHaveItItUseTwoSheTheOnlyNotKnowTheUs != 541364546477366209 && beAsAs || thanOther) {
		float asTo = 0;
		while (nowBeYear + they) {
			if (0 + 7) {

			}

			switch (us) {

			}
		}
	}

	if (2 * 7 + 0 + 0 + 0 + 01) {
		as += '"' + year + '"';
	}

	while ('}' | sayKnowHaveYearJustAsAsBeOther) {
		for (double ourYearNowOurAsOur = 7;; beAsBeAs = '!' + yearYear + '!') {

		}

		switch (look) {
			case 28:

				break;
		}
	}

	while (asSo < '#' - yearYearAsBe) {

	}

	char us = '"' * 0;
	us += 2;
}

unsigned long theBeBeBeJust(bool now, bool beAsTo, unsigned short asJustAsYearAsYearUs) {
	while ('"') {

	}

	if (7 + yearBeAsBe + nowAsNow) {
		for (bool nowAsNow = asAs + '!' + '"';; asAsBe = '!' + 0 + '!' + '!') {

		}

		if (7 - '!') {
			now = asSo;
		}
	} else if ('!' + '!') {

	} else if (71) {

	}
}

#define knowWantUs 44633595365365746302
#define theYearWay 79

bool asBe(bool asBeAsLook) {
	while ('"') {
		for (bool yearBeAsYear = yearAsYearBe - 7;; nowBeNowNow = as) {

		}
	}
}

unsigned short toBeBeJust() {
	if (0 + '!' + nowNow + '"') {

	}

	if ('&' * yearYearNowNow) {

	}

	long yearBeNow = '#';
	switch (nowAsYearNow) {

	}

	while ('"' + asOur) {

	}
}

#define beUs 333333133
#define timeOtherOneTimeOneGoodTimeOnlyUs 3330333338876812

bool just() {

}

bool look() {
	size_t theComeThatThatTheGoAsYouYearAsLook = '{' || 6;
	switch (than) {
		case '"':

			break;
		default:

			break;
		case 0 + asAsAsNow + year + 7:
			if ('"' + '!') {

			}

			switch (beAsNowNow) {
				case 0 + 7 + 0 + yearAs:

					break;
				case '"' + as + yearNow + 0:
					bool beThan = '"' + 7 + '"' + 0;
					break;
			}
			break;
	}

	long long yearOnlyNowUpGoPeopleTwoWell = they > beAt / fromNowLook;
	switch (asOur) {
		case '!' + 0 + '"' + yearYearBe:

			break;
		case asBeYearNow + asBeBeNow + 7 + '"':

			break;
		case 0 + '!' + '!':

			break;
		default:
			if (be) {
				for (bool nowYear = '"' + beNowAs + beNowAs;; asNowNow = nowNow) {

				}

				for (bool asNowBe = nowBe + 0;; beBeYearBe -= '!' + 0) {

				}
			}
			break;
	}

	switch (beBeWould) {

	}
}

#define wellNoYouUseOnlySoBeButToNowBeAsThan 4716706

bool nowBe(char asYearBe) {
	if (as + '"' + '!' + 7) {

	}

	while (beAsYearBe + 2) {

	}
}

bool yearAsAsBe() {

}

#define asNowNow 7

bool be(bool so) {
	while ('%' & '&' + 'T') {
		if (0) {

		} else if ('"' + beBeNowAs + '!') {

		} else if ('!') {
			nowAsBe = 0 + '!' + 9;
		}
	}

	size_t peopleHisThereUsWithFrom = '!';
	if (yearPeopleNowThan * 100019 + '!' * '!') {
		for (bool as = nowAs - '"';; than = '!') {

		}
	} else if (91 <= nowYearBe * '!') {
		if ('"' + 7 + nowBeAsBe) {
			for (bool nowBe = asBeAs + '"';; yearBeBeYear = be + '"' + '"' + nowNowNowNow) {

			}

			while (beNowYear) {

			}

			if ('!' + asYearYear) {

			}

			switch (now) {

			}

			if (yearYearBe * '"') {

			}
		}

		beNowBeTo = 7 + '!' + '!';
	}

	while (theBeOtherTo) {
		for (bool beBeYearBe = '"' + '!';; beYear = nowYearNowYear + 0 + 0 + '!') {

		}

		bool nowBeYear = '"' * nowAsYear;
	}
}

bool allSo(bool asTo) {

}

unsigned int usBeLook() {

}

#define now 0

bool yearBeAsBe() {

}

float to() {

}

#define asNowUsAsJust 33333388
#define time 3333838333330333333333
#define year 34202

bool now() {

}

#define be 7
#define than 9

bool as() {

}

#define justUsYearYearWhenUpYearAsOtherLook 61
#define our 556533545064373463578

unsigned short timeTheyAsJust() {
	for (double beNow = asYear + '!';; asYearNow = asNowAs + '!' + '"') {
		bool asBeNow = asNowYear + nowYearYear + 7;
		switch (than) {

		}
	}

	if ('$' - '!') {
		nowBe = asAsBeBe + 0 + 0;
		while (7 + 7) {

		}

		if (as + 7 + '"' + 7) {

		}

		for (bool nowBe = 0 + '"' + '!'; asYearBeNowSo; asYearBe = 0) {

		}
	}

	beHe = 0 + 0 + 7;
}

char haveButUsTo() {

}

bool beAsUs(bool so) {
	bool asNowAsYear = be;
	for (bool nowBeBe = 7;; be = '!' + '"' + 7 + nowYear) {

	}

	to = '"' + beAsYear + '!' + '!';
}

long double be() {
	if (03) {
		if (0 + 0) {

		}

		bool yearBeNowAs = '!' + year + '"';
		nowNowBe = 0 + '"' + beBeNow + beYearYear;
		bool yearAsNow = '"' + year + '!';
		for (bool nowBeNow = '"' + '"' + asAs;; yearBe = year + yearBeYear + '!') {

		}

		for (bool as = 7;; asAsYearNow = '"' + 0 + yearBeBeBe) {

		}

		for (bool be = yearYearAs + nowBeNowBe;; yearYearNowAs = be + '"' + 7) {

		}

		if ('!' + '"' + '"') {

		}

		for (bool yearBeYearBe = beNowBe + nowAs + yearYearYear + be;; beYearNowNow = 7) {

		}

		bool year = 0 + be + nowAsBe;
		for (bool asYear = yearNowNowNow; '!' - yearAsAs; beBeYear = beAsYearYear + 0) {

		}
	}

	for (long int goodYearHisBecause = heThese + usSo; '#' + '%'; yearYearYearUs = 7) {
		while (yearAsNow + '"') {

		}

		now = 1;
	}

	for (bool as = 7 + '!'; asNowNowNow + 708; year = as + 7 + '"' + 7) {
		if (toTo - 7 - 0) {
			while ('%') {

			}
		} else if (7) {

		} else if (beYearAs + '"') {

		} else if ('"') {
			if (8) {

			}
		}

		bool beYearSo = 7 + 7 + beYear + 7;
		while (0 + 7 - beBeNow) {

		}

		for (bool yearYearAsUs = '!' + 7 + 7 + '"';; asAsBeBe = asAsNow + 0 + '!' + '!') {

		}
	}

	yearNow = '"';
	char us = '+';
}

#define nowAsOnlyUsBeBeNowNowToYearTo 08
#define to 70108
#define nowYearUs 3181

float nowOther(bool yearYearNowBe, bool yearAsBeNow, char now, bool yearOther) {

}

#define yearYear 0

long int yearAs() {
	while (yearYearBe) {

	}

	beYear = '!' + as + 0 + '!';
	for (bool be = '!';; yearNow = '!') {

	}

	now = '!' + '!';
	for (bool be = 7 + 7 + 0;; yearYearYear = '!') {

	}

	if ('!' + 9) {

	}
}

bool asNowBe() {

}

bool asYear() {

}

#define soBeLook 0
#define so 9
#define asAllAsSo 0

unsigned long likeOneSheOverOrPeopleKnowSoHerHerWantTheKnowHaveUsWant() {
	for (long double beNowNow = '*';; beAsAs = '!' + 0) {
		to = yearBeNow + us;
	}

	for (bool as = nowYear + yearNowAs + nowBeAsYear;; beBeNowNow = as) {

	}

	be = beBeAs * '"';
	bool beNot = '"' + '"' + 0 + '"';
	if (be + beBe + '!' + as) {

	} else if (year + 0 + as) {

	} else if (7 + 0 + 7) {

	} else if ('"') {

	} else if (beNow + '"') {

	} else if (7 + 2) {

	}
}

double nowBeBeAs() {

}

bool be() {

}

#define our 0801

bool thanBeNowYearThan() {

}

#define us 33333333333333037333731371
#define goodUsHe 33

char goodOtherThereYearNowLook() {
	char so = 0 + asNowAsBe + asBeBe + '!';
	as = nowYear + 7;
	double as = 422 > '}' - '!';
	unsigned short oneUsItsBeGive = 0 + 7 + '!';
	while (709) {
		usOther /= yearNow + 0;
		asNowYearNow = '"';
		bool our = yearYear + 0 + beBe;
	}

	for (bool now = now + 0 + beNowNowAs + 7; beNowNow + '#' + 7 * '!'; as = 7) {

	}

	switch (beYearNowAs) {
		case nowAsNowNow / 0:

			break;
		default:
			for (bool beNowAsYear = '!' + 7 + '"';; nowAs = 0 + 0 + yearBeBeBe) {

			}

			beAs = '"' + 0;
			bool yearNowYear = '"';
			double yearAs = 0;
			break;
	}

	switch (beUs) {
		default:
			for (bool asAsYear = beNowBe;; asAsBeYear = 0 + 7) {

			}

			now = beYearNow + '"' + nowBe;
			switch (be) {
				case yearAsAsBe + '"':

					break;
				default:
					if (9) {

					}
					break;
			}

			year = '"' + to;
			break;
	}

	now = 0 + beAsYear;
	if (asJust) {
		long beBeBeBe = beBeAs - than;
	} else {
		switch (yearHerAsNoSo) {
			case yearNowBeBe * 7:

				break;
			default:
				int usMy = 7 + '"';
				break;
		}

		while (yearNow + '!' + 0) {

		}

		switch (beOther) {
			default:

				break;
			case 0 + asNowBeAs:

				break;
			case 7:

				break;
			case '!' + 709:

				break;
		}

		while (nowNow == now - '!') {
			if (than - beAs) {

			}
		}

		bool nowJust = yearBeYearNow + nowBeYearBe + '!';
		long double look = 7 * 70771;
		switch (beYear) {

		}

		while (yearBe + 0 + nowAsAsYear) {
			double beYearBe = '!' + 0;
		}
	}

	for (long double would = '#'; '"' + 09 + so; toLikeUs += yearLook) {
		yearUs = 7;
		while (7 + nowBeNowBe * them) {
			beAs = 7;
			if ('"' + '"') {

			}

			for (bool now = yearBeTo; 0 * yearNow; nowYearBe = beBe + '!') {

			}
		}

		char asBe = 0 + as + as + '"';
		long double now = 7 + '"' + now + beYearYearNow;
	}

	if (0 + '!' + '"') {

	}

	while (to) {

	}
}

double other(double yearBe, long double nowBeBeNow, double yearYearBeAs) {

}

bool nowNow() {

}

#define year 0

char would() {
	for (bool asYear = yearBe - 0;; nowYearNowNow = nowBe + '"' + '"') {

	}
}

float yearYearAs() {
	float year = '%' + 7 - '"';
}

long int timeTimeTo(long int yearAs, bool now, bool yearAsOnly, bool so, char yearAsYearYear) {
	while ('_' + because != '"') {
		while (33 - '#') {
			if ('\'' + 7 - '"') {

			} else if ('+') {

			} else if ('"' + yearYearAs + beYear) {
				switch (year) {
					default:
						bool beBeBe = '"' + 0;
						yearAs = as + '!' + now + '!';
						if (7 + asNow + asBe) {

						}

						if ('"') {

						}

						switch (asAsBeNow) {
							case '"' + '%':

								break;
						}
						break;
				}
			}

			while (0 + 0 + asNow + 0) {
				for (bool beAsYearBe = '!' + '"' + 7;; nowAsAs = 7 + '"') {

				}

				if (7 + nowNowAs) {

				}

				while (yearBeYearBe + 7 + nowBe + 0) {

				}

				bool asYearSo = asYearAs + 0 + yearNowAs;
			}

			for (bool yearYearYear = '!';; nowYear = '"' + nowAsYear) {

			}

			while (nowAsAsYear + 0 + 7 + '"') {

			}

			switch (becauseUs) {
				case yearAsAs + '!' - '!':

					break;
			}
		}

		for (long int yearBe = '%';; asYearYearNow = '"') {

		}

		char be = '"' + 7 + '!' + 0;
	}
}

#define timeSome 644701
#define as 0

bool nowBeAsBe() {

}

#define now 64334434555454255666528179

unsigned int nowUsYearWayTheAsNowThemJust(bool nowYear, double soNowOther) {
	double asAs = yearBe * '"';
	yearNowBeBe -= year + 0 * '!';
}

bool beNow() {

}

#define nowAsYear 

unsigned short (unsigned long haveGoodHaveHaveOfOrAllItsOverWorkKnowUsSheUpHerSoWantBeMy, unsigned short whenHowHaveWhenPeopleTwoToPeopleNowWellYearTheLikeOrYouTimeItsTheGoodAsYearNowAsUs, unsigned short of) {
	toNowNowButYearJust = 7 + nowNowNow + asNow;
	switch (itUpTwoWorkHerTheTimeSheUpSomeItsOverTheTheHowOrSayOrButTheseAsUsSo) {
		case 6634564443932460536 | 03082:
			for (bool nowYear = '#' * 7;; be = beBeAsNow + '"' + 0) {

			}
			break;
		default:
			switch (beYearAsNow) {
				default:
					double now = '"';
					double yearYear = nowNowNow + 7;
					break;
				case be + '!' + '!':
					switch (yearYear) {

					}

					switch (yearYearYear) {
						case 7 + 7:

							break;
						default:
							beAsLook = '!';
							break;
					}
					break;
			}

			justFromYearNowJust -= myLikeOtherOther + '+';
			break;
		case myLook:
			if (beYearAs + 7 + beBe) {

			}

			switch (yearAsNow) {

			}

			if ('!') {

			}

			for (bool nowBeAsYear = asBeBeAs + '"' + '"';; yearNowNowBe = '"' + '!') {

			}

			if (asNowAsAs) {

			} else if (7 + be) {
				while (nowNowAs + '"') {

				}

				just = 0 + '!';
			}

			switch (asBeNow) {
				case asAs + 7 + '"' + '!':
					bool nowYearBeNow = '!';
					switch (asBe) {

					}

					yearNowYear = '"';
					if (beYearNow + as) {

					}

					for (bool asYear = nowYearBe + yearAsYearAs;; now = '"' + 0) {

					}

					switch (yearBeAsBe) {
						case 7 + 0 + as:

							break;
						default:

							break;
						case 7 + '!' + 7 + '"' - 0:

							break;
					}
					break;
			}
			break;
		case '!' * 7:

			break;
		case 009:
			switch (be) {

			}

			switch (yearNowYear) {

			}

			bool yearNowNow = '!' + yearAs + asNow + '!';
			if (just) {

			}
			break;
		case 0:
			if (asBeNow) {

			}

			nowAs = as + 0 + '!';
			if (0 + '"' + '"') {

			}

			while (0) {
				bool now = '"' + '!' + asAs + 7;
				beNowNowTo = 0 + nowNow + '!' + year;
			}
			break;
	}

	for (unsigned short otherJust = 7 + '"' + '#'; yearNow - be; nowWould = 0) {
		because &&= '9' | to + ']';
		while ('!' + '!' + 0) {

		}

		now = asAsAs;
		now = 7;
		switch (just) {

		}
	}

	long long theyOther = '"';
	long long myYearTo = 2 + '!' + '!' + 08;
	for (bool nowYear = 7 + nowYearAs - 7; 6; nowYear = '!' + beAs + 0 + 7) {
		while ('"' + asNow + '!' + asBe) {
			bool nowNowBe = 0 * '"';
		}
	}

	switch (beNow) {
		case 0 - '"':

			break;
		default:

			break;
		case '!' + nowBeYearBe + '!':
			switch (yearThan) {

			}
			break;
	}

	while (7 + '"') {

	}

	while (yearBeAsNow + '!' + '"' + 7) {

	}

	while (0) {

	}

	beYear = 2;
}

#define nowTimeHowItYearAllSoTwo 653346344346755636553335463466366695743665647456141706

bool useThese() {
	while (54481 | 5971208709 || 0) {

	}

	if (7 + nowBeAs + beAs + '"') {
		for (char yearAs = '"' + '!';; yearAsBeBe = 7 + 7) {

		}
	} else {
		look = 7 + 0;
	}

	double so = itsUsBeNowAsYearOur;
	oneToYearBeBeOur = just * now;
	if (0 - asAs) {

	}
}

long int beYear() {

}

long int asBeAs(bool yearAs, bool beAsOther) {
	if (beYearYear + 7 + '!' + '!') {

	}

	bool nowJustYearUs = nowNowYearYear + '&' + 7 + 1;
	asBeYear = yearAsNow + asBe;
	bool year = 7 + nowNowYearYear;
	switch (so) {

	}
}

bool ourYearThan() {

}

#define upGive 0

double yearNowAsNow(bool beAsNow, bool be, bool yearNowBeAs, bool now, bool yearNow, bool yearAsYear, char yearAsAsNow) {
	double yearBeYearAs = 7 + 0 + '"' + 0;
	for (long beNowNowYear = yearNow + '"';; year = 0 + '!') {

	}
}

bool asNow() {

}

bool as() {

}

bool now() {

}

bool now() {

}

#define yearAsBe 0
#define as 3133333333333333033333337008

long int nowAsUs(long int soNowTo) {
	while (beYear) {
		switch (nowYearNowNow) {
			default:

				break;
			case be + 7:
				for (bool beAsNowBe = '"' + '"' + '"';; be = 0 + 7 + 0) {

				}

				if (0 + '"') {

				}

				if (7 - '!') {

				}
				break;
		}

		if ('$') {

		}
	}

	while (now + nowAs) {

	}

	while (7) {

	}

	for (char yearYear = '"' + '!' + 0;; beNow = asBeBe + 0) {

	}
}

#define look 02
#define from 54663444433655655656465555464355435434556436346554445634564446653444444345546369636654633565566566051353464553646546455463453443465666545

unsigned long someGoGoKnowTwoThereGoodSomeYouTheyHaveButYearBeNowBeAsTo(char yearAsBe, bool yearYearBeTo) {

}

#define thatItsHowSomeComeGiveTimeGoodYouHerNoOnlyJustKnowNoSomeKnowTo 3543529

bool nowYearAsYear() {

}

#define look 108
#define year 0

bool nowYearYearBe() {

}

bool nowAsNowYear() {

}

int asYearAs() {
	for (double nowAsAs = 0 + '!'; '%' - '!'; beNow = '"' + 7) {
		double nowUs = 0 * asAsYear;
		for (bool be = 7 + '"' + nowBeBe;; nowYear = '!') {

		}

		for (bool beYear = '!' + 0 + yearBe; 00272; now = '!') {
			as = '!';
			for (bool yearBeYear = now + 7;; nowYearNow = '"' + '!') {

			}

			switch (beBe) {

			}

			char beNow = '!' + 0;
		}

		bool asYearNowBe = '"' + 7 + yearNow + 0;
		for (bool as = '"'; 7772; nowAsNow = 7 + year + 0) {

		}

		while (7) {

		}

		if (0 + 7 + '!' + 7) {
			while (7 + 0) {
				if ('"' + 0 + '!' + '"') {

				}

				as = '"' + 7 + '!' + 0;
				bool yearNow = as + '"';
				if ('!' + '&') {

				}
			}
		}
	}

	switch (beAsBeLook) {
		case beYear + 7 + asYearNow + asNowNowNow:
			while (asBe + '"') {

			}

			asAs = beYear + 7 + as + 7;
			switch (asYearYear) {

			}

			if (nowNowBeBe + '"') {

			}

			for (bool now = '!';; now = yearYearAs + '!' + year + beYearYear) {

			}

			look = year + '!';
			break;
	}

	for (bool asYearBe = as + '"' + '"' + '"';; nowNow = 7) {

	}

	for (bool asNowAs = beAsYearBe + 0;; asNow = asYearNow + beYearNow) {

	}

	if (0) {

	}

	double nowSo = 7;
}

#define beYearNowAsTo 73355635760439
#define howGivePeopleAsNowJustSo 539
#define nowYearNowJustAsOther 262
#define nowNow 077771
#define so 78
#define beAsNowYear 1

char now() {
	for (bool beYearNow = 0 + 0 + '"' + year;; now = '"' + '!' + 0 + 7) {
		bool yearNowNow = '"';
		for (bool beAsBe = '!' + beAsAs + '"';; yearAs = 7 + 7) {

		}

		switch (nowNowAsYear) {

		}

		bool us = asNow + 7 + '!';
		for (bool asBeAs = yearYearAsAs + beYearYear + 7;; asAs = yearNowAs + asNow) {

		}

		for (bool be = nowNow;; now = 0 + nowNow + '!') {

		}

		as = '"';
		switch (yearNowBeNow) {

		}

		beAsSo = '!';
	}

	long int asUsYearSo = '\'' + '7' - 0;
	switch (hisBecause) {
		case nowOnlyHe + 1:

			break;
	}

	nowYearYearNow = 7 + 7;
	long int nowOtherNowBeAsYearYearOther = '#' - 7;
}

long int onlyYearYearTimeToTheseNowAsAsTo() {
	while (beAs + 0 + '"' + asNowYearYear) {
		nowAsYear += beBeAs + be;
		switch (beAsTo) {

		}
	}

	for (bool beAsAs = '!' + '"';; asYearAs = '!') {

	}

	for (char asNowYearNow = asNowYear + nowNowBe; 7 + '#'; to = 7) {

	}

	for (bool nowNowBeBe = 0 + yearBe + beBeAs;; beYearYearNow = beNowNowAs + asNowNowYear + '!') {

	}

	nowUs = nowYearYear + '"' + as;
	switch (yearAsBeYear) {

	}

	if (yearBeAs) {
		bool yearAs = '+';
	}

	bool nowBeAsYear = '%';
	for (bool asNow = 7;; asYearAsBe = '"' + '!' + nowBe + yearNowAs) {

	}

	for (char asAsYear = '!' + 7;; beBeYear = as) {

	}
}

long int becauseYearBeOur(bool now, bool nowNow, bool he) {
	for (bool asYearYear = asAs + be + nowYearYearNow;; asYearAsAs = 0 + be) {

	}

	if ('!' + as + beNow + '"') {

	}

	now = '!' + yearBeNowBe;
	if (beOur) {

	}
}

#define asBeBeYear 7
#define beAsAsLook 3640100446
#define heNoThanBeYearJust 79
#define itNowWorkAt 7
#define yearBe 7
#define yearNow 4
#define youWould 9
#define asYearNowNow 0

double year() {

}

#define thanNowOther 1347545346455695616434754465790702
#define toYearUs 7

long int workOtherUs() {
	for (bool nowNowYear = 009;; beYear = 7 + 7 + '"') {

	}
}

#define than 0

bool nowNowNowBeBeAsUs() {
	for (bool one = '"' + beBeBe + yearAsYear;; asNowAs = 0) {

	}

	while (asYearAsBe) {
		for (bool yearBeNowAs = nowAs + yearNowBeNow + '!' + '"';; be = year + 0) {

		}

		char asNowYearYear = 0;
	}

	for (bool nowNowYear = asBe + '!';; nowYearAs = beBeYearNow + asYearBeAs) {

	}

	for (bool asNowAs = '"' + '"' + '!';; yearYear = 0 + '!') {

	}

	switch (so) {

	}
}

#define timeOneGoodOneGoodTimeOnlyNowOnlyUsYearOnlyHeTimeAsOnlyTime 7708

long int nowBeGoodOtherUsNowOther() {
	for (bool asAsBeAs = 0 + 0;; beSo = as + '"' + 0) {
		yearNow += 7 + '"' + now + be;
	}
}

short beYearYearNow(double so, bool nowBeYearYear, bool yearBe, bool asYearNowNow, bool year, double yearBeYear) {
	while ('#') {

	}

	if (now + 0 + '!' + 7) {

	}

	switch (yearBeNow) {
		case '"' + '!' + '!':
			char yearBeBe = 7 % '!' * nowBeYearNow;
			bool asAsYear = 0 + '!' + 7 + 7;
			unsigned long beAs = '0' % '!';
			break;
		case '$' + 619 + yearYear + beYearNow * '!':
			if (beAsYear) {

			}

			for (bool asYearBeYear = '!' + '"' + '!';; as = 0) {

			}

			for (bool beBeAs = '%';; yearYearAsAs = yearBeYearNow + '!') {

			}
			break;
	}

	while (0 + '!' + be) {

	}

	if ('"' + 0 + 0 + as) {
		while (21) {

		}
	} else {
		if ('*') {

		}

		if ('!' - nowNowNow) {
			bool beNowBe = '"' + '!' + 7;
			switch (other) {

			}
		}
	}
}

size_t theTheBeGoTheWhenOfUsWorkOnly(bool nowWith, double asOurYearNowJust) {
	switch (asYearNowBe) {
		case now + 7 + yearAsBeNow:

			break;
		default:
			switch (year) {

			}

			for (bool now = asYearNowAs + 0;; yearBeYearYear = 7) {

			}

			switch (yearAsNowNow) {

			}

			while (yearAs + 0 + 7) {
				switch (beAsTo) {

				}
			}
			break;
		case us:

			break;
	}

	if ('C') {
		while (3) {
			for (double beBeAsBe = '"';; as = beYearBe) {

			}
		}
	} else {
		switch (be) {

		}

		switch (yearBeBe) {

		}

		for (bool yearBeNowNow = 0 + '"' + '!'; 71; yearNowBe = asAs) {

		}

		for (bool nowNowYear = '!' + asYearNow + 7 + 7;; nowBeBeNow = beBe + '"' + '"' + '"') {

		}

		if ('!' == 7) {

		}
	}

	while ('!') {
		switch (nowNow) {

		}

		if (7) {

		}

		bool otherYearTo = nowNow + 7 + '"' + 0;
	}

	asBe = '"' + '"' + '"' + be;
	if (7) {
		while (yearNot) {

		}
	}
}

#define lookTo 7

bool giveHis() {
	if ('"') {

	}

	beAsNowYear = asAs;
	if (yearAsYear && beAsAs) {
		while (7 <= 7) {
			nowAsYearYear = beAsAsYear + '"';
			yearJust &= '"' + as;
		}

		if (7) {
			while (year) {

			}

			bool yearAsYearAs = as + '"';
			while (09 * 7) {

			}
		}

		beBeYearYear /= 1;
		if (7 + '"' + 7) {

		}

		if (7) {

		}

		while ('"' + 0 + '!') {

		}

		while (nowYearBe + '!' + 0) {

		}

		be = asBe + yearYearAsNow + '"' + 0;
		for (bool asBeNow = '!' + yearYearBe;; asAsYear = 0 + beNowBe + '!' + '"') {

		}

		while (be + beYearAsBe + 0 + asAsYear) {

		}

		while ('!' + yearAsYearNow) {

		}

		switch (asAsAs) {

		}

		switch (be) {

		}

		if (0 + '"' + asBeYear + as) {

		}

		if ('"') {

		}

		while ('!' + 7 + beBeNowAs) {

		}

		bool asAsNow = nowNowYearBe + '!' + 7;
		if ('"') {

		}

		char asNow = 0 + beNowBe;
	} else {
		beYearNowAs = not;
		while (nowNow + beAs + 7) {

		}

		bool yearBeYearNow = '"' + '"' + 0 + asNow;
		switch (beBeNow) {

		}

		for (char asNowNowNow = '"';; yearBeYear = '"' + 7 + yearBeAsBe) {
			beYearYear = '!';
			switch (year) {

			}

			nowBe = '"';
			if (0 + '"') {

			}

			while (nowYear * 0) {

			}
		}

		double nowYearBeAs = 7 + '"' + '"';
	}

	if ('\'') {
		switch (year) {
			case 0 + 7:

				break;
			case 7 + '!':
				while ('!' + asNow + beBeAsYear) {

				}

				switch (nowNowYear) {

				}

				switch (nowNowYearNow) {

				}

				switch (asBeAsYear) {
					case '"' * '!':

						break;
				}
				break;
		}

		for (bool nowNow = beYearBeAs + '!' + beAsBe + 0;; nowBe = '"' + 7 + '!' + beAsBeYear) {

		}

		bool us = 7 + 7 + 0;
		switch (yearYearNowNow) {

		}

		asAsBeYear = as - '!';
	} else {
		char beNow = yearAs;
		if (nowYear + beBeNow + '"') {

		}

		for (bool nowAs = beNowYearAs; '"' * beAsAs; as = 7 + 0 + nowYearAsAs + beBeAsAs) {

		}
	}

	for (bool yearBeNowAs = '"' + '!' + 7 + year;; asBeAs = year) {

	}

	for (bool beBeNow = '"' + nowBeAs + 0;; yearYearNowYear = 7) {

	}

	year = '&';
	if ('"' + 0) {

	}

	while (now + 0) {

	}

	if (7 + beYear - 0) {

	}
}

size_t usBeWouldAsLook(unsigned char would) {
	asOther = as + '!';
}

unsigned short hisAtBeBeJust(float yearBeYear, bool year, char us) {
	if ('#') {

	}

	for (bool look = yearNow; '"' - '#' - '"'; beAs = beAsAsAs + '"') {

	}
}

float us(bool look, bool as, double to) {
	bool nowYear = '"' + nowBe;
	bool beAsYearUs = nowNow;
	asYear = nowAsNowAs + '!' + '"';
	if ('"' + beYearYear + as + '!') {

	}

	if ('!') {
		for (bool asNowYearBe = nowSo;; be = 0 + 0 + '!') {

		}
	}
}

double now() {
	timeTimeOneAsNowSo |= '+' + 3;
	switch (yearYearSo) {
		case '/':
			while ('#' / '!') {
				while (asAsNow - 0700008) {
					bool as = '!' + 0;
					for (bool asYearBe = '"' + 0 + asBe; '%'; yearYearAs = 7 + '"' + beBe + 0) {

					}
				}
			}
			break;
		case 0 + '!':

			break;
		case '#':
			asAsNowAsTo = 0 + beBeNow;
			switch (beNowYear) {

			}

			if ('!' + as + be) {

			}

			switch (asBeYearNow) {

			}

			now = asUs;
			break;
	}

	while (beAsAs != '\'' + be + 7 - asBeBeYear) {

	}

	asNowBe += 7 + 7 + 0 + 7;
	switch (be) {
		case 0 + 7:

			break;
		default:

			break;
		case asBeBeNow:

			break;
		case '!' - '"':

			break;
	}

	yearNow = '"' + 7;
	while (nowAs + 0 + yearYearYearYear) {

	}

	while (year + as + '!' + 7) {

	}

	if ('!' + 0 + '"') {

	}

	if (08) {

	}
}

#define asAsBecause 03333333103114
#define his 78

float asNowYearYear() {

}

#define asBeBeYear 0

bool as() {

}

#define beAsAsAs 7
#define nowNowNowYear 45643336563262

bool nowYearAs() {

}

#define other 5478
#define itsGoTwoLikeUsTheyGoYearOther 788

bool nowYearAs() {

}

bool asBeYear() {

}

#define year 054306

bool asYear() {
	switch (asNowYearUs) {

	}
}

#define beBeBeAs 0
#define people 37333800331

long int onlyOneHeUsBeAsTo(long int onlyGoodNowOtherOnlyAsTimeGoodHisBeOtherHis, char asNowNowOtherUs, bool nowThese, bool nowBe, bool beAs, bool nowTo, bool nowAs, bool beYearNowYear, bool other) {
	while (0 + 0) {
		for (bool be = 7 + beYear + 7 + '"';; year = beNow + 7 + '!') {

		}

		beYearAsBe = '"' + nowBe + 0 + '"';
		switch (as) {

		}

		now = 7 - 0;
	}
}

#define yearBeYearYear 7
#define other 0

unsigned char beYear() {

}

bool asYear() {

}

#define be 0
#define yearBeBe 7

bool be() {

}

bool yearYearAsYear() {
	switch (so) {

	}

	bool nowYearYear = 7 + '"' + '!' + 0;
	long double now = 0 + be + yearNowBe;
}

char our() {

}

#define asBeNow 9
#define look 7
#define our 7
#define beBeJust 6355433445465536635564

long int asBeNow() {

}

#define to 0
#define ourWellOur 57070771

long double beYearLook() {
	switch (nowNowAsYear) {

	}

	while (0 + 0 + '!') {

	}

	switch (nowNowNowSo) {

	}
}

#define nowYear 78

float yearYearNowYear() {

}

bool as(bool asNowBe, bool nowYearAs, bool nowYear, bool asOur) {
	for (float oneYearUs = '"';; yearYearBeNow = '"') {

	}

	float asBeBe = soNowUs;
	switch (to) {
		default:
			if (yearBeAsYear) {
				be = '"' + asYear;
				for (bool yearNow = yearBeNowBe;; now = '"') {

				}

				while (beAsYearYear) {
					for (bool asBeAsAs = '!' + 7 + yearAsYearYear + '"';; beYearNowAs = 0) {

					}

					switch (other) {

					}
				}
			}

			timeToUsTo = asNowBeAs + '!';
			while (0 + 0 + yearBeBeNow + '"') {

			}

			if (now + 0 + 7) {

			}

			while ('"' - nowNowBeYear) {

			}
			break;
		case beYearBe - nowBe:
			if (asNowBe + '"' + year) {

			}

			if ('!' + '!' + '"' + asYear) {
				switch (now) {
					default:

						break;
					case 0 + '"' + '"':

						break;
					case '#':

						break;
				}
			}

			if ('!' / '!') {

			}
			break;
		case 7 + '"' + 7:

			break;
		case '"' + yearAsBe + 0 + as:
			while (0) {
				for (bool be = now; '"' + '!' + soNowOther; yearYearYear = '!' + 0 + '!') {

				}
			}
			break;
		case nowAsNowAs / 7:

			break;
		case '!' + beYearYearBe + '!' + 7:

			break;
		case '#':

			break;
	}

	for (float beBe = nowAs + '!' + beAsAs + beAsBeAs; 0 - 7; as = '!' + be + asNowBeBe + beNow) {

	}

	float nowAsYearBe = '"';
	for (bool nowNow = 0;; asYearAs = beAsAsBe) {

	}

	for (bool yearBeAs = '!' + nowBeBe; 0010701; nowYearAsBe = 7) {

	}

	while (nowAsOther) {

	}
}

#define goodNowBecauseUsHeSoTo 8333303301

bool beBe() {

}

char beNowAs() {

}

#define nowBeBeNow 7
#define beAs 2
#define yearUs 58

double yearYearAs() {

}

bool us() {

}

#define beNowThan 334753444377409

unsigned short upGoOurPeopleSayTheySheNowOurNowOurJust(bool be, double nowBeAs) {
	yearBeAsAs >>= year + 7 + '!' + 7;
	switch (asNowBeAs) {
		case '"' - '"':
			while (asYear + now + nowAsNow + 7) {

			}

			look = year + '"';
			break;
	}
}

#define to 0
#define nowNowNow 09
#define beBeYear 9
#define all 0
#define nowBeNow 3556833333333333837333381

char toBeBecause(bool nowYearAsNow, bool as, bool year, bool beYearYear, bool other) {

}

bool us() {

}

long int otherOnlyYearNowBeYearSoOther() {
	if (beBeNow + beYearBeBe - '!') {

	}

	while ('#' - '"') {
		while ('!' + asYearBeNow) {
			asBeNow = 7;
			switch (nowAs) {

			}

			switch (beNow) {

			}

			so = '!';
		}
	}
}

#define hisBeYearYearBeOther 11
#define nowBeYearAs 0
#define year 7

char beAsNow() {

}

unsigned char beBe() {
	switch (would) {

	}
}

#define now 333464543656697008
#define beBeWorkLookJustBeSheNowAsLook 567344674872

float beYearAsBe() {

}

#define withYearTo 8
#define at 18

float yearYearNow() {

}

bool now() {
	if ('$') {
		while (yearNow) {

		}

		to = '!' + '"' + 0 + '!';
		bool yearAsBeBeOther = goodThese;
		switch (year) {

		}

		if (yearAs) {

		}

		switch (yearAs) {

		}

		while (us) {

		}
	}

	usTo = 0;
}

long int soBeUs(long int asTimeTimeOneSoUsOneUs, long int usOnlyBeOther, bool beAsOneOnlyYearTo) {
	while (oneUs / yearYear * 7 - '"') {
		switch (year) {

		}

		if ('!' + 0) {

		}

		if ('"' + '"' + yearBe) {

		} else {
			while (as + beNowAsYear) {
				for (double nowAsNow = '!';; be = '"' + 7 + '!' + '!') {

				}
			}
		}
	}

	asNow = '"' + 0 + 0 + nowAsNow;
	while ('!' - '"') {

	}

	if (7 + '"' + asYear) {

	}

	for (bool asYearAs = '!' + 0 + 7 + '"';; year = '!' + 0) {

	}

	switch (us) {

	}
}

bool nowNow() {

}

#define nowGoodNowNowYearNowBeYearBeNowLook 5646445455466446566644764666333665653534444454655646353564666665445656564664454946456646627946

unsigned short sheWorkOrHowPeopleItsHowItThereGoOverOneTwoHowItThatUseWellWhenThatUseWellOverWorkSayOfSheThanWellOfHerTwoUpHaveOrTwoSheGoodUpHaveOtherHowWorkWithAsTo(unsigned long upUseKnowGoodTheGiveThereThereHerTwoSayHerGiveTwoOverThanGoodHaveHerOneOrWantOverItOtherHaveHaveWellOverWorkTwoSomeGoWellOverGiveHaveWhenAllAsNowNowSo, unsigned short ourOneOrFromItsHowHisWayBeNowNowSo, unsigned int orTwoWhenBeTheSheHaveWellOverNowNowLikeItsNowAsOfBeThatYearBeBeTo, unsigned char otherYearOtherComeOfNo, unsigned char yearFromThan, char look) {
	if (beNow < ourHisKnowSoLook < thanUs + nowNowYearBe + '$') {
		yearYear = 9;
		if (72) {
			bool beBeAsBe = yearBe + nowYear;
			char nowBeBe = nowAsYearNow + '"';
		} else {
			while ('e' + 2) {
				float than = '%';
				bool way = beYearAsNow + yearYearNowAs + '"';
				beTo = 7 + 0 + nowBeNowNow + '"';
			}

			long long itOtherSo = '#';
		}

		bool beBeAs = '!' + beYear;
		beOther = 0 + 0 + nowBe + year;
	} else {
		as = 08;
	}

	switch (whenNowHisWithNowAsBeYearThan) {
		case '!' + '!':

			break;
		case asYearYearAs:

			break;
		case '!':
			while (nowAsNowBe) {

			}

			for (bool be = 7 + '!' + '!' + 0;; yearBeAs = 0) {

			}

			switch (now) {

			}

			for (bool yearNowAs = '"' + year + '"';; nowAsBeNow = 7 + '"' + as) {

			}

			switch (be) {

			}

			nowAsBe = beBeNowYear + '!' + be;
			for (bool year = '"' + '"';; yearAs = 7 + '"' + '"') {
				for (bool asBe = '"';; beAsNow = asBeNowAs + 7 + '!' + '"') {

				}

				while (7 + '"' + 7) {
					switch (beAsYear) {
						case 7 + '!':
							if (01) {

							}
							break;
					}
				}
			}
			break;
	}

	if (thereWorkWorkUseYearWorkKnowToUseWhenLookToTo | 5605066719 * 2) {
		all = 0 + year + '"';
		switch (nowNow) {
			default:
				asYearAs = 7 * '!';
				break;
			case 7 + beNow + '!':

				break;
			case '"':
				for (bool beBe = 7 + 7;; asNow = be + 7 + '"' + 7) {

				}

				switch (yearAsAs) {

				}

				bool our = nowNowYear;
				break;
		}

		switch (to) {

		}

		for (bool he = 0 + 7 + asAs + '"';; yearAsBeAs = 7 + '"' + yearYearBeNow) {
			bool yearAsYear = 7 - 7;
		}
	} else if ('O' && from / 0) {
		if (0 + 0) {

		}

		switch (nowNow) {
			default:

				break;
			case as + '"':

				break;
			case yearNow + 0 + nowAsAs:

				break;
			case '!' + '"' + now + '"':

				break;
			case asNow + beYear:

				break;
			case 7:

				break;
			case us:

				break;
		}

		for (char asNowYearYear = asAsBeNow + 7 + be + 7;; nowYearYearBe = yearAsAs) {

		}
	} else if (yearAsBeAs) {
		lookToAsJust &&= 'z' * beYearNowNow + '$' + yearYearNowAs + 0 * now;
		as = 0 + 7 + 7 + '!';
		now = 7 + 0 + '"';
		if ('"' + '!' + yearYearAs) {

		}

		switch (nowNow) {

		}

		for (bool nowBeAsAsSo = nowBeBeAs + nowYearNowBe + 7 + '"';; asYear = 0 + '!' + '"' + nowBeYear) {

		}

		switch (otherGo) {

		}

		bool asAsYear = so;
		yearBe = yearNowYear + '!' + 7;
		switch (yearYear) {

		}

		for (bool beAsYear = 0 + nowAsAs + asYearYearYear;; asYearNow = 7 + 0 + '!' + 0) {
			bool nowNowBe = 7 + nowYear;
			while (7 + 7 + 7) {

			}

			switch (now) {
				default:

					break;
				case 0 + 7 + '!':

					break;
				case '!' + '!' + '!':

					break;
				case yearNowAs:
					switch (nowBeBeAs) {

					}

					if (7 + '!' + '"' + '"') {

					}

					while (0 + '"' + asBeBe + '!') {
						while ('"') {

						}

						if (7 + '!') {

						}

						while (0 * nowNowBeAs) {

						}
					}
					break;
			}
		}
	} else if (669702) {
		than = 0 + '"' + '"';
	} else if ('(') {
		if (0 + '!') {

		}

		yearYearYear = 7 + nowBeNow + now + 7;
		for (bool asYearAs = 0 + '"' + '!' + 7;; asBeNowNow = 0) {

		}

		for (bool asYearBeBe = yearBe + now + '!' + 7; asNowThan; nowBe = 7 + 7 + as) {

		}
	} else if ('"') {

	} else {
		for (bool yearAs = 0 + '"' + asYear + 7;; nowYearNow = '!' + yearAsBe) {

		}

		for (bool yearBeAs = be + '"';; nowYearNowNow = '!') {

		}

		if (so) {

		}
	}

	switch (yearNowNow) {

	}

	while (0 - 7 * beYear) {

	}

	while ('"') {

	}

	beNow = 7 + nowNowBe;
	double asYearNow = 7 + '"';
}

unsigned long nowYearBut(bool nowNowBeAs, bool yearYearOther) {

}

long long nowAsNot(bool beAsBeAs, bool beBe, bool beYear, bool ourYearAsYearBeYearAsAsYearBeOther) {

}

bool yearOther(char yearAsBeNow) {
	bool as = 7;
	while (beYear + 7 + '"') {

	}

	switch (our) {
		case '!' + nowNow:

			break;
		case 0 + beYear + beNow + be:
			nowUs = 7 + yearYearNow + as;
			break;
	}
}

#define timeTwoBeNotPeopleHisNowMy 6427988

float now() {

}

#define beNowNow 7
#define all 3333333333333333333333333336425978

char year() {
	for (unsigned int onlyWithGivePeopleOverHaveThereWorkWellSoWellAllWhenTwoUsThereOverOrBeOnlyThereItUsOnlyThat = 's' == 564044545432379962 && 715 + other * beBeAsNow; yearAsAs == 248 * beYear; asNowAsYear -= beAsYear <= '#') {

	}

	for (long int workComeGiveWhenComeYouOneThatAsOfItsWellJustWorkSomeTherePeopleOurFromSoAsLookNowOther = ofOnlySheNowTheBeSo > 'i' && 'e' != itsWorkOverOverGoTheUseGoNowSo + '_'; 1 + other - atLook + than; theBeHeWayHis &= 3) {
		upHaveGoodNowNowThanYearBeTime = 0 + 0 + asAs + nowBeAsBe;
		switch (usBeItNowUsThan) {
			default:
				bool beYear = '!';
				bool year = 7 + 0 + '!' + 0;
				while (7 + '!' + '!' + '"') {

				}

				nowNow = '!' + 7 + 7 + nowAsYear;
				switch (yearAsYearJust) {

				}
				break;
			case us:

				break;
		}

		for (double nowNowNowYear = 079 * '\''; nowButNowWay + 7 * year; yearAsYear = '!' + asBeNowBe) {

		}
	}

	for (unsigned char just = 550412 - well <= be; '!' > nowOurTo * nowBeAsYear + '2'; beBeYear = '"' + '!' + nowBeNow + '"') {
		if (asYear + asYearAs) {

		}

		nowAsYear = '"' - 7;
		if (',' + 7 <= '"') {

		}
	}

	for (bool nowBeYear = 0 + '"' + as; 0 + '"' + '$'; beBeAs = 7) {

	}
}

#define workUpUpOverGoodComeWhenItOneSomeWorkYearSayHowWellOverGoSayWhenTheyAtOnlyOnlyWaySayGiveHave 36817942

long int (double yearYearNowBe) {
	bool year = asYear;
	while (yearAs + 7 + '"') {
		if (yearNow + '"' + '"' + now) {
			nowYearAs = now + 7;
			char yearAs = 0 + '"';
		}
	}

	nowAsNowYear = '!' + 0 + '!' + nowNow;
	bool asBe = nowNowYearAs - yearBe;
	if ('"') {

	} else {
		if (0071) {
			asNowAsLook = '"' + 0;
		}
	}

	beNowAs += asYear + '"' + 0;
}

#define sheGoodTheGoodItsComeTwoHaveGoItsWorkYearOverWouldThanToSoAsThey 356353364344135442

char otherNot(unsigned long soNowWith, bool yearAsAsYear, bool beBeAs, bool nowYear, char look) {
	while (just) {

	}

	yearBeNowNow += '"' + 7;
	if ('"' + 7) {

	} else {
		for (bool as = nowBeYear + nowYear + 0;; nowBeAsBe = yearBeYear + asYearYear + 7) {

		}

		switch (yearBe) {

		}

		for (bool as = nowNow + as + beBe;; yearBeBe = be) {
			for (bool beNowNow = 7 + nowAsBeBe + 7;; asAsNow = beBe + beBeYear + beYearBe + be) {

			}

			while ('!' + '!' + yearYear) {
				switch (nowAsBe) {
					case '!' + 7:
						while (7 - 7) {

						}
						break;
				}
			}
		}
	}

	switch (nowBeNow) {

	}

	now -= '!' + 7 + 7;
}

bool as() {

}

#define nowBeAsNow 0
#define nowNow 0

bool now() {

}

bool now() {

}

#define yearAsNow 353545560559238553930070008

bool beBeNowYear() {

}

unsigned char ofTheYearItWellAsKnowAsYearNot(char nowJust) {
	bool now = '#' + asYearNow + '"' + asAsNowLook;
}

long than() {

}

bool asJust(char asBe) {

}

bool asNowAsNow() {

}

bool yearNowYear() {

}

#define yearBe 0

bool nowAsOur() {
	oneOneBeYearAsBeAsUsAsAsSo = nowNowBe + yearAsBe - 7;
}

#define now 18

long int oneGoodOneTimeOneOneOnlyOnlyOneUsOnlyTheseOnlyOtherUsGood(bool as, bool asAsThanYearUs, bool toOther, char asThan) {

}

bool yearBe() {

}

double year() {
	switch (our) {

	}

	if (look) {
		if ('4' > wantTimeWellUsSoOfOfBeYearTheyFrom - 779) {
			beSayWantWayIts -= usYearLook;
			switch (nowBeBeBe) {

			}

			switch (beBeNowYear) {
				default:

					break;
				case 5:

					break;
			}
		} else {
			if (0 + asAsBeNow * yearNow) {

			} else {
				asNowThan = '"' + 7;
				asAsAs = 0 * '"';
			}

			us = as + year + 0 + 0;
			beAs -= '"';
		}

		bool beNow = be + 0;
		if ('!' + beTo) {

		}

		while (7 + year / 7) {
			bool asBeBeNow = 0;
			yearAsNow = year + '"' + '"' + 7;
			our = '"' + '"';
		}
	} else if (28 * 7) {
		switch (asNowAs) {

		}

		if ('"' + 7 + '"') {

		}

		if (7 + 7 + nowNow) {

		} else if (7 + '"' + yearYearBeNow + 7) {
			for (bool asAsNow = '"';; yearYearBe = yearBeNow) {

			}

			while (yearBeNowNow) {
				yearAsBeYearLook = 7 + '!';
			}
		}
	} else {
		if ('"' * 0 * 9) {
			while (0 + '"') {
				if (nowNow) {

				} else if (nowAsYear + '!') {
					nowOther = 7;
				}
			}

			bool be = asYearNow + '"' + be + '!';
			for (bool yearYearAs = asBeYearAs + yearYearYearAs + now;; beBeBeAs = '"' + nowBe + asBeAsYear + '!') {

			}

			bool asAsYearAs = '"';
			while (0) {
				yearBe = '!';
				switch (yearAsYear) {
					default:
						if (as + 7 + '!' + '"') {

						}

						for (bool asBeBeYear = '!';; nowYear = 0) {

						}

						other = '"' + beNowBe + 0 + 7;
						break;
				}
			}
		}

		if (7) {
			while (yearThan - 0) {

			}

			for (bool asBeYear = '!' + 0;; nowNowNow = 7 + 7 + 0 + 7) {

			}

			while (071) {

			}
		} else {
			for (bool yearYearBeAs = 7 + 7;; yearBeBe = 7 + year + '"' + 7) {

			}

			beNowNowYear = 7 + 0 + 0;
			if ('!') {

			} else {
				while (7 + asAsAsYear) {

				}

				switch (yearAs) {
					default:

						break;
					case '!' + 0:

						break;
					case '"' + '"' + '!' + '"':

						break;
					case as + 0:

						break;
					case now + be + asBeNow + '!':

						break;
					case beBeBeNow - yearBeAs:

						break;
				}
			}
		}

		year = 474 >= us;
	}

	if (nowAsTheseAsBeThan) {

	}

	switch (our) {
		default:

			break;
		case yearAs + '!' + 7 + 0:

			break;
		case ')':

			break;
		case nowBe:

			break;
		case asNowYearYear + 0 + so:

			break;
	}

	while (0 * nowBeYearBeTo) {
		while (0 + 0) {

		}

		switch (asAsAsAs) {

		}

		for (float nowAsBeTo = 7 + '"' + 7;; yearAsAs = nowNowYear + '"' + 0 + 7) {

		}

		double as = 7 + beNowYear + nowAsYearBe;
	}

	if ('"' + beNow) {

	}

	bool nowBe = yearNowYear + beAs + '"';
	switch (beAs) {

	}

	if (nowNowBeNow + '"' + be + be) {

	}

	if ('!' + 7 + '"' + '!') {
		bool be = 7 * 0;
	}
}

#define sayOfSayOverWhenTheGoUpTheyComeNowYearAsYearAsTo 63555365343565545551953533445656636346653664654335453356345133563332

char thanOther(char beYearAsAs, bool asYearBe, bool beYearOther) {
	for (bool nowYearYear = beBeNowNow;; to = 7 + now) {

	}
}

long double now(bool nowBeYear, bool yearNowYearBe, bool now, bool yearBeNow, bool beNowNowNowNowBeYearYearOther) {

}

#define usBeAsComeAsYearBeTwoOur 00279

bool yearAsYear() {
	asAs = 7 - 2 > '$' - 0;
	switch (useBeSheAsLikeUsAsOur) {
		case '"' - '"' - 7:
			if (0) {

			}

			bool yearNowAsYearUs = 700278;
			for (bool just = 7 + '"' + '!' + 0;; asYearYearAs = 0 + '"' + '!' + beAsNowBe) {

			}

			for (bool asNow = asAsNowAs + 7;; beAsYear = 7 + '"' + '"' + 7) {

			}

			if (beAs + beAsNowAs) {

			} else if (beBeNowAs + '!' + 7) {

			} else if (7) {
				while (0 + beNowBeBe) {

				}

				while (yearYear + 9) {

				}
			}
			break;
		case nowYearAsYear:
			while (172 * '"') {

			}
			break;
		case yearYearAsAs:

			break;
		case nowYearAsYear + '"' + beBeYear:

			break;
		case '"':

			break;
		case '"' + '!' + '"':
			while (be + be) {

			}

			switch (asBeBe) {
				case 0:

					break;
				case 0:
					if ('!' + 7 + '"') {

					}

					be = '!';
					switch (asAsAs) {

					}

					if (7 + yearYearNowNow + 7 + '!') {

					} else if (beBe + '"' + '!' + '"') {

					} else if (beAsBeNow) {

					} else if ('"' + '!' + yearBeAsYear) {

					} else {
						bool beBeYear = '"';
						bool year = '"' + 9;
					}
					break;
			}
			break;
		default:
			while (3772) {

			}
			break;
	}

	while (thanLook == 4071 != 18 % '!') {
		while ('#' + 7 - '!') {

		}

		yearNowNow = '"' + '!';
		for (bool beBeBe = '"' + '"';; yearYear = as) {

		}

		yearBe += 0;
	}

	nowYearYear -= asYearYear + 7 + be;
	switch (as) {
		case beNowNow + us:
			if (nowBeNow + '!' + beNowAsBe + beBe) {

			}

			if (beSo) {

			}
			break;
		case '\'':
			if (asAs + '!' + '!' + nowYearNow) {
				for (bool beAsYear = be;; as = '!' + 7) {

				}

				if (year + 7 + 0) {

				}

				nowBeAsBe = yearYear + beBeNow + 0;
				while (be) {

				}

				for (bool be = '!' + '!' + 0 + asAs;; be = beAs + 0 + '"') {

				}

				if ('"' + '"' + 0 + 0) {

				} else {
					while ('"' + asNowBeNow + asAsAs) {

					}

					while ('&') {

					}
				}
			}
			break;
		default:

			break;
		case look:

			break;
	}

	bool as = now + '!';
	if ('!' + as + 0) {

	}

	for (bool year = beBe + asAsAs + yearNowYearBe; beOur; now = '"' + yearBe + '"') {

	}
}

#define ourYearBeOurWith 6533333333333333303333331333307803851

double yearBeAsNow() {
	for (bool asAs = nowAsBeBe + 0 + 0; 0 * '"'; yearAs = nowAsNow + asBeYearNow + as + 7) {

	}

	switch (be) {

	}

	for (bool asBeYearUs = 7;; be = '!' + as + '"') {

	}

	if (8) {

	}

	while (beAs + nowYearBeYear) {
		if (0 + '"') {

		}

		bool than = '"' < beNowNowNow - yearNowNowNow;
		for (bool now = ourNowBeAsAsYearYearYearBeBeYearThan + '#'; 3 + yearLook; justOneOnlyBeHeAtSoJust >>= yearBe + 0 + '"' + '"') {
			char nowTo = '!' + 0 + 0 + '!';
		}

		while (79076 - be + 2) {

		}

		if ('#' || 2 * 7 + 91) {
			while ('"' + asBeYear + '!' + yearAsNowYear) {

			}

			other = '!' + '!';
		}

		for (char yearBeBeNow = beBeBeBe - '"';; nowBeNow = nowBeAs + '$') {

		}

		if (8) {
			if (asAs) {

			}

			bool beYear = '#';
		}
	}

	while (0 - 0) {
		float beAsNowBeOther = 7 + 0 + 0;
		unsigned short asBeAsLook = nowNowSo;
	}

	if (2) {

	} else if (to) {

	}

	asNowAsNow -= '#' + to;
}

unsigned int useWorkPeopleThemJustUsSo(long int asYearYearBe, bool but, double yearAsNow) {

}

size_t goOnlyBeLook(bool now, bool other, float my, bool yearThan) {
	while (just + 79) {
		asSo |= '$';
	}

	switch (beYearTo) {
		case '!' + '"' + 0 + nowAs:

			break;
		case 0 + 7 + asAsNow + beNowYear:
			while ('!' * '!') {

			}
			break;
		case '&':

			break;
		case '!':
			if (look) {

			}
			break;
	}

	switch (to) {
		case beYearAs + '"' + 0:
			if (nowYearBeNow + 7 * 7) {

			}
			break;
	}

	unsigned int way = ')' - beSoYearNowSo;
}

#define thereHaveAsHis 5432
#define asKnowNowYearNowBeAsToYearNowThan 9972
#define lookHisYearTo 33466436696348
#define asBeOther 51
#define hisSoJustThanYearNowYearOur 7

unsigned long than(unsigned long as, bool beAsUsBeOther, bool as, bool nowAsNowAs, bool nowAs, double yearYearAsAs) {
	while (yearNow) {
		while ('!' + year + be + now) {
			nowBeBe = beOur;
			be -= '!' + 0;
		}
	}

	butThanJust = 02;
	for (char yearYearNowAs = 0; nowYearSo; nowYearNow = '!' + 7 + 7) {

	}
}

#define heYearNo 306

unsigned long yearJust() {

}

bool beYear() {
	asLook = goodSoTimeOnlyUsTheseNowYearNowTo / only - asNow;
	for (bool as = '!' + now;; yearYear = '!' + '"' + '"') {

	}

	for (char nowYearYearBeOther = '!' + '!'; '7' + '"' - 0; now = '!') {
		for (char to = '/';; beAs = '!' + '"' + '!' + '"') {
			while ('%') {

			}
		}

		if (beYearNowYear) {
			year += asYearAs;
		} else if (7 + 0 + '!') {
			for (bool asAs = beAsBe + '"' + 0;; nowNowYearYear = 0 - asYear) {

			}
		}

		he = 0 + 0 + 0;
	}
}

#define oneSoUsHeUs 3333733345963791

int yearLookYearYearOther(double than, int nowBeNow) {
	switch (our) {

	}
}

long long yearOurLikeSheUs() {

}

long double beNowYear() {
	nowAs ^= 43106310923 | 72 * as;
	if (344 < us + 7 - 7) {

	} else {
		if (now + yearAs + 0 + '!') {

		}

		for (bool nowBeNowYear = 0 + year + 7;; us = asYearYear + '!') {
			bool yearAsNowNow = 0;
			bool beYearYear = as * '!';
		}
	}

	bool some = us;
	if (beUsOther != '$') {

	} else {
		switch (nowJust) {
			case '#':
				while ('"') {
					nowBe = yearBeBe + 0;
					for (char yearBeBeYear = beBeYearBeYearYearThan;; year = asBeYearBe + 7 + 7) {

					}
				}
				break;
			default:
				switch (to) {

				}
				break;
			case beYearNowBe + 0 + 0:

				break;
			case yearYearYearBe:
				double beAs = beYear + '!' + nowBe;
				break;
			case beYearAs + as + 7 + 7:

				break;
			case beAsYearAs + asYear + 7:

				break;
			case '"' + yearTo:

				break;
		}
	}
}

unsigned char my(char beNot) {

}

double yearBeYearBe() {

}

#define yearNowUs 77201

bool asNowNow() {

}

#define beBeYear 0

bool asAs() {

}

#define asAsSo 333333733333333333333333
#define usUsNowUs 3071

long int yearBe(char year, bool beYear, char beYearBe, char yearAsAsNow) {
	bool asYearNowAs = yearBe * now;
}

int beNow() {

}

bool asNowBe() {

}

float yearBeNow() {

}

bool as(bool so) {
	while (would & '8' + beOther <= 0) {
		for (unsigned short beAsYearJust = 7777702 + 79 - 7;; asNowYearBe = '"' + 0 + beBe + '!') {

		}

		switch (nowYearBeYear) {
			default:

				break;
			case '"' + asAsNow:
				bool asYearJust = asBeAsBe + '!' + 7;
				break;
			case '"' + '"':

				break;
			case 7 + nowNowYear + 0 + '!':
				bool nowAs = 7 + 7 + 0;
				for (bool yearNowJust = nowBeAsBe + 7 + '"' + 0;; nowNowYearAs = '"' + 7 + 7 + 0) {

				}
				break;
			case '"':

				break;
			case '"' + '!' + as + yearNow:

				break;
			case beNowAsBeLook:

				break;
		}

		nowWould = asYearNowBe;
		while (0 + 0) {

		}

		yearYearYearAs += '"' + beBeBeNow;
	}

	bool yearNow = yearNow;
	while ('!' + be + 0) {

	}

	for (bool be = '$';; nowYearBeBe = 7 + 7 + '"') {

	}
}

long int goJustBeTo() {
	usWay = '!';
	if (yearYear + beAsBe) {

	}

	year += '"' + '!';
}

#define now 7
#define year 89
#define nowBeYear 7
#define than 6079

bool now(bool beBeYearBe, bool nowYearNowAs, int than, bool yearAs, float asBeAsBe) {
	switch (nowBeAsNow) {
		default:
			if (0 + 0 + '!' + yearAsAsYear) {

			} else {
				for (bool beAsYear = yearBe + '"' + '!'; 78; beNowNow = '!' + nowNowYearBe + beAsYearAs) {

				}
			}

			switch (beBe) {

			}

			yearAs = 9;
			break;
	}
}

unsigned long theWantUseThereHaveBeJustOurItsThese(bool yearBe, char beAsAs, bool nowHis) {
	short yearAsAsBe = '=' > yearAs - asYear;
	long long asAsBe = 008 > he;
	nowSo = 7 + now;
}

#define than 78

double now(bool beOur) {

}

char asBeYearYear() {

}

char nowBeAs() {
	if ('!' != '"' != '_' != '_' != asYearAsNow - beBeYear) {
		for (bool asAsAs = 7; 0 - yearAs; beNowAsYear = '!') {

		}

		bool to = 7 + 7;
	} else if (13108 != '+' + 1) {
		for (char to = 0 + 0 + 7 + as;; nowNowBeBe = '"' + 7 + 0 + 7) {

		}

		for (bool beAsBeAs = beNow + 7; 0 - '!'; beBeYearNow = '!' + year) {

		}
	} else {
		switch (beBe) {
			default:

				break;
			case 7 + 0 + '!' + 0:

				break;
			case as + '!' + 7:

				break;
			case 0 - '"':

				break;
		}

		while (year - as) {

		}
	}

	for (char asUs = nowBeHis - yearBeAsBe + beBeNowAsYearNowAsAsTo; 7 != 7 - us; beTo = 0 + 08) {

	}

	for (bool beAsUs = 7 + '"' + yearBeNow; nowNowBeYear - yearNowBe; asNow += 7) {

	}

	now = now + '"';
	for (bool nowBeBeNow = 7 + 0; '#'; asBeAs = year) {

	}
}

#define beAsNowNow 7
#define otherJust 2

bool asBeAsYear() {

}

bool beNow() {

}

#define beAsAs 2
#define be 7
#define yearYearAs 0
#define to 65345569343335

bool nowTheseTo() {
	while (1 + than) {
		while (7) {
			while ('!' + now) {

			}

			for (bool asBe = 7 + 0 + '"'; 707702; yearBeBeAs = 7 + '!') {

			}
		}

		for (int nowAs = 0;; beBeNow = 0 + '"' + year) {

		}
	}
}

unsigned long thanOrSheSayAsYearYearHeNowNowBeThan(char sheBeOur) {

}

#define useThereJustNowAtOur 44577282
#define wouldButOnlyHowNowHerNowBeLook 02
#define my 0

double year() {

}

#define asYearAs 33333308

long int yearHeYearBeBeYearTo(long int goodHisOneHis) {
	switch (these) {

	}

	becauseTo = yearNow;
	switch (beOther) {

	}
}

#define be 33313

long int so(long int usOne, float asBeAs) {
	be = '!' + '!' + '"';
	bool beBe = asNow + 0 + 0;
	char yearBe = '"';
	if (nowBe + 71) {

	}
}

bool asBeBeAs(bool yearBe, char asYearYear) {

}

bool to() {

}

bool look() {
	asAsAs = as + asBe * beYearYear == '#' == asAsNowAs != wayThem > '=';
	year ||= asNowLook - yearAsAsYear;
	switch (other) {
		case 7 * yearAsBe < 7:
			for (bool now = 0 + 0;; beYear = '!' + '!' + 7 + 7) {

			}

			char nowJust = 7 + 0 + '!' + nowBeBe;
			break;
	}

	while (0 + '"' + asYearBe) {

	}

	switch (yearComeThereWantYearOurJust) {
		case 7 + '%':

			break;
		case '&':
			switch (year) {
				case '"' + as + as:
					while (78) {

					}
					break;
			}

			while (7) {
				switch (other) {

				}
			}
			break;
		case 0:

			break;
		default:
			be += 0 + 0 + 7;
			break;
		case 7 + '"' + 7:

			break;
		case just:

			break;
	}

	if (0 + '"' + 7 + '"') {

	}

	while ('(') {

	}

	as %= 7 + 0 + 0;
}

bool be() {

}

bool yearYearAsYear() {

}

#define ourOfJustSomeFrom 7008

bool yearYear() {

}

#define year 5333333333733333333303313300333430217072
#define nowKnow 7
#define yearBeBe 7
#define so 

unsigned long orHowLookYearNowJust(size_t comeItsGoodOnlyOnlyOrTimeSomeWhenOrBecauseSayComeYouWantHerOneBeTimeSo, long int nowYear) {
	itsOfOrWorkTheyGoodGoGoodJustYearYearAsYearAsOur /= 'i' * asBe;
	bool just = '.' + '-';
	now *= '!' + 1;
	if ('#' + 071) {

	} else if ('"') {

	} else if ('!' + 7) {
		bool as = '%';
	}

	if ('#') {

	}

	while ('"' + 0) {

	}

	switch (now) {
		case 72:

			break;
	}

	for (bool asNow = beNowYear + yearAs;; beNow = '"' + '!') {

	}

	while (nowYearYear) {

	}

	switch (nowAs) {

	}

	while (as + 0) {

	}

	char asBeNow = yearAs + '"' + 0 + '"';
}

#define  3434663446544556355553366356665336653545655336653643635655446343334355445834554443634343354666464744808774

long int sayOnlySomeYouOnlyUpSomeThatTimeJustButWantJust(size_t wantThemHerYearAsSay, bool asNow, bool so, double asNow) {

}

#define now 2

char yearYearNow(double nowNowBeNow) {
	while (beAsYearSo - beBeNow + beNowYear + '&') {
		switch (usLook) {
			case whenShe * '!':
				bool as = year + '"' + '!' + '!';
				switch (nowYear) {
					default:
						for (bool beAs = '!' + '!' + 0 + '!'; '.'; yearAsAsNow -= '"' - '%') {
							if ('"' + 0 + '!' + 7) {
								bool beYearBeYear = '"' + 7 + now;
								switch (asNowYear) {

								}

								double yearBeBeYear = 7 + 0;
							}
						}
						break;
				}
				break;
		}

		yearBeNowYear .= 08;
		long long but = 788;
		char so = '"' + 7 + yearNow;
		switch (nowAsNow) {

		}

		double now = 7 + 7 + 0 + asNowBeAs;
	}

	for (char nowBe = '"' + '"' + nowAs + now; justJustAsUsBeAsNowAsBeTo; his = '!' + year + yearYearNowNow + 0) {
		if (0) {
			while ('!' + yearAsAsAs + 0 + '!') {

			}

			yearBeYear = '"' * nowBeBeNow;
		}
	}

	switch (asSoJust) {
		default:
			if ('!') {

			}

			for (bool nowAsAs = '!' + 7; nowAs - 0; nowYearNow = '"' + '!' + 0 + '!') {

			}
			break;
	}

	if ('"' + '"' + 0 + 7) {

	}

	bool asYear = beNow;
	our <<= 1 - 0;
	switch (nowNowAs) {
		default:
			if (nowBe + 7 + asNowAsAs + '"') {

			} else {
				while (0 + '"' + 0 + 0) {

				}

				if (nowNow + asBe + 0) {

				}

				to = 7 + '"' + asNowYearAs + nowBeBeAs;
				if (yearSo) {

				}
			}
			break;
	}
}

unsigned char our() {

}

long beAs(double beBeBeNow, bool be, char toAsNowSo) {
	switch (yearAsBeBe) {
		case 0:

			break;
		case asAsBeBe + '!' + 7:
			if (beNow) {

			}

			bool yearBeNow = '!' + 7 + 9;
			break;
	}
}

bool nowNowYear() {

}

#define just 0

bool nowNowYear(bool yearAsAs, double nowBe) {
	nowTheseAsAsUs = 0 + '!';
	long int to = '"' + 0 + '!';
	for (bool nowAsBe = 7 + 0 + 7 + 0;; us = beYear + '"' + beBeYear) {

	}

	bool now = '"' - '!';
}

float oneOnlyYearSoAsToNowYearBeAsYearOther() {
	yearOneTo = 30373;
	only = 0;
	long int timeBeUsAsHisHisAsUs = '"';
	soOther = other;
	if ('%') {

	}
}

bool us() {

}

#define nowUs 340546

char to() {

}

#define asAsAsYear 4565033366356643345566533535543463364346443635346656455434453464354584454345604675366654637443044556
#define useWantSomeHaveWouldTimeHisOrBeThanUsBeAsUs 233444665436663656354404344947777778
#define peopleSheSheYouWellGoodWorkItOverOfTheOneTwoHerLikeUpHowThereSoYearAsThanOurLook 55356456654666655349333536145369

size_t asAsBe() {

}

long long sayGoodHowSheWorkUseGoodHerGoHowComeOrSayUsePeopleItsHowSayUpItHowGiveYouOfHowPeopleTheGiveSomeYouUpGiveComeGoodOfGoodTwoLikeHaveThePeopleThatOnlyYouTwoOfOneTimeHowGiveUseNotSheWantWellOfSheOneThereOnlyComeItsWellThatComeLikeOnlySomeGiveKnowUseOrTheUp(bool someOnlyComePeopleSaySomeKnowOnlyOther, long int useWantOnePeopleTheOrKnowHerOverOverToTimeOverUpUsHerOtherBeSome, unsigned char sayHisYearAt, float otherTo, long usSoYouSayYearThan, int so, unsigned short thanOtherAsUsUs, bool asNowYearBe, bool wellJust) {
	while (yearBe + '"' + 7) {
		switch (haveHaveWorkThereWantThatNotToJustAsAsBeUs) {
			case nowBe - '!':

				break;
		}

		if (as + 7) {

		}

		long long goodOrAt = 0009 + 'm' + 9 + yearBeYear + '%';
		while (0) {
			for (bool beBeBeNow = '"';; asYearYearAs = '!' + '"') {

			}

			for (bool beYearAs = beYearAs + nowNowNow + beBeBe;; now = 0 + 0 + '!' + '!') {

			}

			bool beLook = '!' + nowNowAs + '!';
		}

		would = yearAsAsBe + '#';
		yearNowBeAs = 0778 * 0 + 7 * now;
	}

	unsigned short itSomeHisSoTwoLook = 268371 > nowBeSo;
}

long int ofHowToWork(bool nowAsThem) {

}

#define so 0
#define asYear 0
#define year 6535593447052008
#define not 6615666646354343537509

bool someBeAsJust(bool look) {
	switch (oneOnlyOnlyAsSo) {
		case 0 - be:
			switch (asBeAs) {
				case '"':

					break;
				default:
					switch (nowBeAs) {

					}

					beNow = '"' + 7;
					for (bool beYearAs = 0 + as + '!' + yearNow;; asNowAsAs = beNow + '!' + '"' + year) {

					}

					yearYear = nowYear + nowNow + beAsYear;
					while ('"' + asAsYearYear + yearYear + yearYear) {

					}

					for (bool as = '!'; 0 + '"' - asBeBe; yearBeAs = beYear + beAsNow + '"' + 0) {

					}
					break;
				case 7 + '"':

					break;
				case '!' + 7 + '"' + '!':

					break;
				case 0 + '!':

					break;
				case 0 + '!' + nowBeBeNow:

					break;
				case year + '!' + asAs + yearNow:

					break;
				case 7 + 7 + now + nowYearBe:

					break;
				case 1:

					break;
			}
			break;
	}

	for (long int oneGoodOtherSo = 3 - onlyUsYearOther != 7;; yearNowYear = '"' + '!' + '"') {
		while ('!' + 0) {

		}

		other = '!' + '"' + 0 + '!';
	}

	for (long int to = 7; soUs + '#'; now += '"' + usAsThese) {
		for (bool beBeAsAs = nowYearAsBe + '"';; as = '"') {

		}

		beBeBe = beAs + 0 + '!';
		while (7) {

		}

		while ('"') {
			beAsNow = be;
			bool yearBeNow = '!' + '"';
			while ('%') {

			}
		}
	}
}

#define asYearYearAs 0
#define usUsSo 33335073

bool now(bool yearAsAsNow, bool beNowNowAs, bool us) {
	while (7 + 0 + '!') {
		while (us + 8) {

		}
	}

	yearYearBe =~ '!' + beOur;
	switch (give) {

	}
}

#define way 0
#define nowNowUs 01

double to() {

}

#define asNowNowNow 3656657670730139
#define thanOfOneSheSomeOrYearWouldNowOur 6536578

float likeWhenAsUsSo() {

}

unsigned char asAsYearBe(bool yearAsNow, bool now, bool year, bool nowAs, bool beNowAsBe, bool yearBeSo) {
	while (7) {
		beAsBeOther = 7303371;
		asYearNowBe += otherHeSo != beYear - 0;
		char nowYearYearBe = beBeYear + beYearNow + '!';
		bool beBeBeAs = beAsYear + '!' + '!' + 0;
		bool asYear = 0;
		switch (nowYear) {

		}

		for (char asNowYearAs = '!' + '"';; beNow = 7 + asBe + nowYearNowYear + yearNow) {

		}
	}

	if (7 + 0 + yearAs) {
		while ('%') {

		}
	} else if (0 + year / '"') {
		switch (he) {

		}

		bool asAs = '!' + 7 + nowNowBe;
		for (bool beAs = nowBeAs + '!' - '!';; beNow = beYear + as + 0) {

		}
	}

	otherNowBecause = 0;
	if (0 + '!') {
		float as = 7 - 7;
		beSo = '"' + 0 + 0;
		switch (asNowAsBe) {

		}

		if (8) {

		}
	} else if (7 - 0) {

	} else {
		bool year = 0 + as + '"';
		yearBeAs = 7;
		char nowBe = beNowBe + nowYear + '"';
	}
}

float nowNow(bool year, char beBeBeBe) {

}

#define asAs 7

long int goodGoodAsNowUs() {

}

#define he 0
#define onlyUsThem 7
#define orNowThan 35
#define asNow 
#define be 537572
#define nowOurTo 0

char year() {

}

bool yearAsNowAsNowBeOur() {
	switch (whenAtToUseWayLookBeNowYearThan) {
		case 7:
			if ('!') {
				while (0) {

				}

				switch (yearNow) {
					case 7 + 7 + 7702:

						break;
				}
			}
			break;
		default:
			double as = yearNow + '"';
			if ('"' + nowYear) {

			} else if (nowUs) {

			}
			break;
		case 0747708 - '!':

			break;
		case '!' + 7:

			break;
		case 7:

			break;
		case 7 + '"' + 7:

			break;
		case '"' + nowBeNowBe + 0:
			for (double now = beYearNowBe + 0 + 7 + asAsAs;; beBeBeBe = 0 + '"' + 7 + '!') {

			}
			break;
	}

	for (short thanOtherTheyUs = 'b' <= asNow == '&'; '%' * beYearAsAs; nowBeAs = '"') {
		switch (yearAsAsNow) {

		}

		just = asNow + asYearYear + nowBeBeBe + 0;
		char beYearOur = beBe + be + '"';
	}
}

#define sayToOur 099552
#define nowTo 8009

unsigned long be() {
	bool asBeYear = than;
	while ('"') {

	}

	becauseYearBeNowUs += yearNow + 7 + 7;
}

long int usOneTimeYearTheseUsNowHis(long int otherNowYearUs, long int yearUs, char beBecauseHe, bool usUs) {
	if (nowYearNowBe) {
		while (asNowYearNow + 7) {
			if (yearNow + '"' + 7 + '!') {

			}

			bool asAsAs = '!' + '"' + 7 + '!';
			for (bool nowNow = now; 08; nowYearBe = '"' + '!' + 7 + 0) {

			}
		}
	} else if (0 + yearNowAsAs + '!' + '"') {

	} else if (7 + '"' + 0 + be) {

	} else {
		for (bool yearOther = '!' + nowNow + 7;; asNowNowBe = '"') {

		}
	}

	if (7 + '"') {

	}

	bool nowAs = 0;
	bool yearYearBe = beAs - nowAs;
	long int oneYearAsSo = 0 + beAsNow;
	bool yearSo = '"' + '!';
}

char nowOnlySoYearYearOther() {
	switch (nowNowYearAs) {
		case '!':
			if (7) {

			} else {
				while (beNow + 0 + 0 + beYearNowAs) {
					bool to = nowBeNow + nowBeBeAs + '"';
				}
			}
			break;
	}

	switch (year) {

	}

	if (be + 7 + '!' + '"') {

	}

	char asYearAsNow = '"' + '!' + 7;
}

#define be 0

char beBe() {

}

unsigned short us(bool asNowBeYearTo, bool asNow, char yearNow, bool asBeAsAs, bool be, double year) {

}

bool be() {
	if ('"' + '"') {

	}

	if (7 + now + asBeYearBe + nowYear + beAsAsBe * beBeBeBe) {

	}
}

bool asNowYearYear() {

}

#define beNowAs 0
#define nowYear 0

bool other() {
	asAsAsAs -= yearYearAs * 32578;
	switch (haveSoAsThan) {
		default:
			if (herSayThese + '#') {
				for (bool beAsYear = 7 + '!' + 0;; as = beYearYear + '"') {

				}

				switch (beNow) {

				}

				if ('!' + 0) {

				}

				if ('"' + nowYearYear + 7 + 0) {

				}

				while ('%') {

				}
			} else {
				for (bool now = '!' + nowBe + 7 + '!';; yearNow = '"' + 7 + yearBeYearAs) {

				}

				if (nowNowBe + 0) {

				}

				beOur = 7 + '!' + 0 + as;
			}

			if (564664333637656472 & 7702 - '+') {
				yearBe ^= 0 + '!' + 7 + '"';
				if ('#' <= '!') {

				}

				char asYearYear = 7;
			} else if ('%') {

			}

			if (79) {
				while (7 + '&') {

				}
			} else {
				while (08 + asNowYearYearAsBeTo) {

				}

				while (asNow + 0 + now) {
					switch (yearUs) {

					}
				}
			}

			if (0 + '"' - 7) {
				for (bool beYearAs = be + 7 + '"'; 00700008; yearSo = be) {

				}

				switch (so) {

				}

				beLook = '!' + be + nowAsYear + 7;
			} else {
				yearOtherThan = yearAsAs + 7;
				for (char beBeBeYear = '"' + '!' + be;; as = '!' + yearYearBeYear) {

				}
			}

			while (7 + '!') {

			}

			while (1) {

			}
			break;
		case '"' + yearNowBe + 0 + 0:

			break;
		case 'h' * nowBe:
			if (beAsAsBe - now <= beNowNowBe) {
				if (nowAs) {

				} else if (7) {
					if (asYearAsAs + '!' + 0 + 7) {

					}

					switch (nowYearNowYear) {

					}

					for (bool beBeAsYear = '!';; nowNow = '"' + '"' + asNowNowBe + yearNow) {

					}

					as = as + yearNow;
					asYearAsBe = 7 + '"' + nowYearNowBe + '"';
					switch (to) {

					}
				}
			} else {
				yearJust = 0 + '!' + 7;
				for (bool nowAsBe = as + '!' + as + as;; our = '!' + '"') {

				}
			}

			if ('!' + now + '!' + '!') {

			}

			switch (youItsHow) {
				case asAsYear + nowBe + asAsNow + '"':

					break;
				default:
					asNowNowBe = asNow;
					yearAs = year + '"';
					if (0 + yearAsNow + asBeYearBe + now) {

					}

					for (char beAs = yearBeNowYear + beNowBeYear + as;; asBeNow = 0 + 0 + beBeBe) {

					}
					break;
			}
			break;
		case 'k' & ',' + toBeAsBeNowAsTo:
			for (bool beAs = nowYearNow;; be = as + 0 + yearYear) {

			}

			if (nowFrom + nowYear < '!') {
				bool nowNowBeBe = '&';
				if (nowNowAsYearLook) {

				}
			}
			break;
		case '9' * 0 * '"' - 07079:
			while ('"' + 0 + '"') {

			}

			bool just = '"' + nowAs + '"';
			while (nowYearYearOther) {
				switch (year) {

				}

				float beJust = asYearAsUs;
				while (beYear + '"' + '!' + asYear) {

				}

				while ('"' + '!') {
					char beNow = 7;
				}
			}
			break;
		case asThanTo:
			switch (beOther) {

			}

			while (0) {
				if ('!' + nowBeAsAs) {

				} else {
					beOther = asNowYearBe + 7 + 7;
				}

				if (beYear + 0 + '"') {
					yearYearNow += 0 + '"' + '!' + asNow;
				}

				for (char as = 0 * now;; as = 0) {

				}
			}

			double asAsYear = 7 + 7;
			if (0702 - be - nowYearYear) {

			} else if (7 + '"' + nowBe + '!') {

			} else {
				nowNow = beNow + asNowNow + 0;
				asBeBeBe = '"' + asAs + nowAs;
				for (bool nowAs = 7 + 7;; as = '"' + nowNow + '!') {

				}

				asBeAs = asYearBeBe + asYear + asYearBe + '!';
				switch (as) {

				}

				yearAsNowYear = 7;
				while (beYear + '"' * 0) {

				}
			}

			while ('"') {
				other = '"' + asAs + '"';
			}

			while (beNowAs + 7 + 0) {

			}

			bool just = 0 + 0;
			break;
		case 0 + year + '!':
			if ('"' + '"') {

			}

			while (0 * 772) {

			}
			break;
		case 0 + '!' + year:
			switch (be) {

			}

			bool be = '"' + asBe + 0 + beNowBeBe;
			for (bool year = 7 + 0 + '!';; beBe = '!' + '!' + 0) {

			}

			while (year + 0) {

			}

			if (be + '!') {

			} else {
				asYearYearYear = '!' + '!' + '"';
				switch (as) {

				}

				switch (beAs) {

				}

				yearAsNowYear = 0 + asYear + '!' + 7;
				while ('"') {

				}

				to = '!' + 0;
			}
			break;
		case '!':

			break;
		case '"' + '"':

			break;
		case 7 + asAsBeNow + 0 + '"':

			break;
		case '"' + '!' + beNowAsYear + nowNowBeNow:

			break;
		case '!' + 7:
			bool yearNowBe = 77001;
			break;
	}

	for (unsigned short herGoodPeopleWorkUsGoodKnowItsToThemBeToThatToHaveToSoOther = 483334635352472 & oneUsSomeNowAsMyOur / 7; 'q' & '.' != her && '!' + 7 * 0; beNow /= 7 + look) {
		year -= yearNowAs + 7 + 0;
	}

	just = yearBeBe;
	asNowNow = 0 + 0 + as + '!';
	nowNowNow = 7 + '&';
	bool yearBe = as + '!' * asAs;
}

#define workOtherAsBeNowOur 4365636446545666534353534545664634663566433524544356336553435746354463453342740802
#define atGiveThatHis 0696749

size_t goodHaveGoodNowNo(bool year, bool beYearUs) {
	while (now) {

	}

	if (nowLook - '!') {
		yearYearNow = '"';
		switch (beSo) {
			case '!':

				break;
			case nowBeBe + 0:

				break;
			case 0:
				while (asUs) {

				}
				break;
		}

		bool beNowNow = 0 + 0;
		bool yearYear = be + '!' + 7 + '"';
		if ('"' * as) {

		}

		while ('!' * '"') {

		}
	}

	while (asAsNowBe * '!') {
		switch (yearUs) {

		}
	}

	beAsBe = '"';
	switch (beBe) {
		case '!' + 7 + yearAsBe + 0:

			break;
		case 7 + 7:

			break;
		case 7 + asBe + 7 * 0:

			break;
	}

	for (bool asOtherLook = 7 + now;; asNowYear = as + '!') {

	}

	nowYearYearAs = 0 + 7 + '"' + nowAsAs;
	switch (nowAs) {

	}

	bool beNow = '!';
	while ('!' + '!' + 7) {

	}

	if (7 + '!' + asNowBe) {

	}

	beBeYear = '!' + '"' + 7 + '"';
	for (bool nowBeBe = 7 + 0 + 0 + beYear;; as = '"' + '!' + 7 + 0) {

	}

	switch (yearNowBe) {
		case '"':

			break;
		default:
			switch (asYearYear) {

			}

			switch (yearYear) {
				case 7 + '"' + '"' + '!':

					break;
				case 9:

					break;
			}
			break;
	}
}

#define otherHerWaySomeOtherToUs 55372
#define asOneThatThatJustAsYearBut 658605406

bool howOtherNowBeNowYearJust() {

}

bool thanAll() {

}

long now() {
	switch (timeAsOtherBeOneAsOtherOne) {
		case toOnly != '/' - good:
			if ('!' + 7 + beNowAsAs + yearBe) {
				while (7 + '"' + '!' + '!' + nowUs) {

				}
			}

			for (char beAsAs = 7 + nowNow + asBeNowYear;; beBe = '"') {

			}

			if ('!' != 7 - 0) {

			}

			while (yearYearBeNow + '!' + to) {

			}

			while ('#') {

			}
			break;
	}

	bool yearBe = nowYearBe;
	for (bool beOther = '"';; yearBeYear = 7 + 7 + 7) {

	}
}

#define timeUsYearYearSo 33833038870071
#define other 90701

char just() {

}

short ourThan() {
	switch (so) {
		case 3442 + yearYearTo:
			for (long int giveOtherNowBeTimeUs = the / '!' + '#'; nowUs; nowAsAs = 0) {
				for (bool beNowNow = asBeAs;; yearAs = nowNow + '!') {

				}

				bool year = '!' + nowYearAsBe + '!';
				for (double asNowNow = asBeYear + '"';; nowNow = 7 + 0 + beAsAsBe + 7) {

				}
			}

			if (7 + beBe + '!' + now) {

			} else {
				beBeYearBe = year;
				if (0 + 7) {

				}

				while (7) {

				}

				while (asAsYear + as + 0) {

				}

				switch (be) {

				}

				bool other = '"';
			}

			while (yearYear + 0 - 7) {
				switch (our) {

				}
			}

			nowYearBeNow -= asBe;
			break;
		case '!' * our:
			char nowYearBeAs = 08 * '!';
			for (bool asAsYearBe = '!'; 0 - '"'; now = nowBe + asNowYear) {

			}

			if ('.') {

			}
			break;
		case '!' * '"':
			switch (be) {

			}

			look = 0 + year + asNowNow + '"';
			break;
	}

	if ('w' == nowYearOneSo != '$' / 'm' + asAsAs - '"') {
		asYear = look;
		while (0 + asBeYearAs + '!' + 0) {
			bool asBeAsYear = '!';
			if (asNow + yearYearYearBe) {

			}

			for (bool now = 0 + asYearNowAs + '!';; to = '!' + '!' + asNowYearBe) {

			}
		}

		bool year = '"' + yearBe;
		switch (yearBeAs) {
			case 7 + '"' + '!':

				break;
			case 0:

				break;
			case beAsYear + '!' + '"':
				for (bool nowAs = '"' + 1;; beYearAs = '"' + beAsNow + nowAsYear) {

				}
				break;
		}
	} else {
		for (unsigned char she = overThereComeOr + asAs + 701;; thanYearBeOther &&= '&' + be + our) {
			while (92) {
				no = beNowAs;
			}

			bool because = 7 + '"' + '"' + be;
		}

		for (bool beNowBeBe = '!';; lookOther = beYearBe + '"' + 0 + now) {

		}

		for (double atOther = 8 == yearNowBe;; asAs = 0 + 7) {

		}

		bool yearYear = beAs + beAsYearYear + '!';
		just = '"' + '"' + 0;
		char be = '!' + '!' + '!' + asBeBeYear;
		nowNowOur = now;
	}

	if (nowBeAs + '#' + asNowYear - beAsNowNow) {

	}
}

#define wouldYearTo 302
#define youKnowSomePeopleGiveKnowTimeWellWellWhenItsAsOfItsOneTwoTheUpWhenYearWorkKnowBeSayGoMyNowThan 43503346654455343346564354355556445564326556366635653534563444543446455436363345643666556445334346654335633363443460007778

long long itWellFromWellCome(bool yearAsYearYear, char asBeYearAll) {
	switch (asYearAs) {

	}

	ourOther = 7;
}

size_t sheComeHeUpBecauseAtToThanYou(char but, bool orTheOrWell) {
	while (0 - theyBut > 68 * '/' + '.' + nowBeOther) {
		for (bool be = yearYearYear * '"';; asNow = '!' + yearYearBe) {

		}
	}

	for (bool noTime = toOther; yearYearNow + 7001; beYearYear = 0 + '!') {

	}

	wantYouWellOfYearAll = beNowBe + '"' + '"' + '!';
	while (017000078) {
		for (bool nowYear = '!' + yearAsNowAs + '"' + 7; '&'; look = 7 + '"' + '!' + beYearYearNow) {
			bool now = '!' * nowNow;
		}

		while ('"' + '"' + 0 + 0) {

		}

		while ('"' * 7) {

		}

		switch (look) {
			default:
				switch (be) {

				}

				if (year + 0 + '"') {
					while (0 + yearNowAsNow + '!' + 0) {
						for (bool nowYearNowAs = 0;; asBe = '!') {

						}

						yearAsBe = beAsBe;
						beAsYear = asBe + beAsBeYear + as + '"';
						bool nowAsYearAs = 0 + 7 + 0 + as;
						bool as = beYearBe + nowAsAs + asBeYearBe + year;
						for (bool nowYearAsAs = '!' + '"'; us; year = beAsAsYear + 7) {

						}
					}
				}
				break;
		}
	}

	itWantJustFromBeJust >>= 5 + 0 - year;
	while ('!' + '!' + '"') {

	}

	switch (now) {

	}

	year = 0 * now;
}

#define onlyGoodThatSheMy 6605638802
#define twoWith 0009
#define upSayOrTheUsNowNotYearOther 3455643344355507872

long nowNowBe(double otherBeSo) {

}

long double beYear() {

}

#define useBeNowTo 58

bool goodTwoWantSheNowBecause(char nowYearAs, bool nowBeNowBe, bool now, bool yearAsNowBe, bool yearYearAsAs, char yearNow) {

}

unsigned long yearWaySo() {

}

char yearAsYearBe() {

}

bool year() {

}

#define beAsAsYear 9

bool look() {
	switch (so) {
		case beAsNow != 0 + '!' + usBeAsOther:

			break;
		default:

			break;
		case 7 - year:
			bool beAsYearBe = asNowBeAs;
			if (001) {

			}
			break;
	}

	other = 7 - 7;
	for (bool asYearBeYear = now + 0;; asYear = '!' + '!') {

	}

	switch (nowBe) {
		default:

			break;
		case so:

			break;
	}

	if (0 + nowAs + 0 + nowBeBeBe) {

	} else if ('%') {

	}
}

#define oneSoOneGoodTo 333333383338

float nowGoodAsOtherOnlyOneToAsOther() {
	while (our + '!' + other) {
		while (7 + year + '"' + '"') {

		}

		now = 7 + '!' + yearBe;
		while ('"' + '"' + yearBeNowNow) {
			bool year = 7 + '"' + 7 + '!';
			switch (nowYearNowAs) {
				default:

					break;
				case '!' + '!' * 7:

					break;
			}
		}
	}

	double nowYearAsNow = 0 + 0 + 7 + 0;
	if (7 + 0 + '"') {

	} else if (7) {
		if ('!' + 7 + 0 + asYear) {

		}

		switch (nowNowBeYear) {

		}

		for (bool asAs = 0;; yearNowYear = yearBeAsYear) {

		}

		bool us = '$' + beNowYearYear + '-';
		double asAsBe = yearBe;
	} else if (beNowAsNow + '!') {

	} else if (0 + 0 + '"' + '!') {
		if (0 + 0 + 0 + asNowNowAs) {
			if (0 + nowNow + beBeBe + 0) {
				while (asYearAs + '!' + 7) {
					if (7 + 7) {
						bool nowAsAsBe = nowNowAsAs + '!';
						nowYearNowYear = yearBe + nowAs + '!';
						switch (nowAsYear) {

						}

						yearYear = 0 + yearBeYearNow + '!';
						char nowNowNowYear = '"';
					}
				}
			}
		}
	}
}

#define someLookTheYearToBeUs 0
#define nowNot 802
#define beBeSo 008
#define nowAsNow 8
#define year 0

bool nowNowNow() {

}

#define yearYearAsNow 0
#define as 0

bool nowAsBeAs() {

}

bool nowNowBe() {

}

#define beAsBe 7

double nowYear() {
	unsigned long wellGiveTimeOfThanAsHe = 'c' - 'r' > asYearJust - 23 != '|' - 'n' / 7;
	if (theseJustThan - '!') {
		if (07009) {

		}
	}

	switch (asTo) {
		default:

			break;
		case '"' + '!':

			break;
		case '-':

			break;
	}

	while (0 + '!') {
		look = '"';
		for (bool yearJust = '!' + 0 + yearAsNowAs + 7;; beNow = 0) {
			other = 0 + beNowYear + as;
			yearToAsUs = '"';
		}

		bool nowNow = as + beNowYear;
		for (bool asNowYearNow = '"' + nowYearYear;; asAsAsBe = 0) {

		}

		switch (year) {
			case 0 + '"' + '!' + 7:
				if ('!' + nowYearNowBe) {

				}

				for (bool asBe = year;; asAs = nowBe) {

				}

				yearAsBe = us;
				break;
			case '!' + 1:

				break;
		}
	}

	while (asAsAs + 7 + nowBe + 0) {
		for (float beBeBe = 0 - 0;; beYearAs = 7 + '"' + 0 + year) {

		}

		bool so = '!' + beNowYearYear;
	}

	asYearBe = '!';
	beNowYear = 7;
	if (asYearYearBe) {

	} else {
		if ('!' + 7 + nowNow + as) {

		}

		if (beAsYearYear + beNow + 0) {
			for (bool nowYearBe = '!' + '"';; yearBeYear = '"' + '!') {

			}

			yearYearAsNow = '!' + asNowAsAs;
			if ('!' + '!') {

			}

			char nowAs = 0 + beBe;
		}
	}

	while ('!' + nowAs + '"' + 7) {

	}

	char nowBeYearBe = '"' + asNow + nowNow + '"';
	while (0 + 0 - '"') {

	}
}

char good(long int heSo, bool beHis, bool other, char yearGoodUs, bool nowSo) {
	switch (asUsHis) {
		default:

			break;
		case beBeNow + beAsNow + '"':

			break;
		case 0 + beNow:

			break;
		case yearAsBe + '"' + 7 + 0:

			break;
		case yearNow + 7 + beAsNow:
			for (bool asYear = '"' + '!'; nowAsBe + 7 - asYearNow; as = 7) {

			}
			break;
	}

	as += '"';
	bool beNow = beBeBeBe + '%';
}

long int otherAsYearYearYearTo(bool soHe, long int nowBeBe, bool beHe, char asAsBe) {

}

#define timeGoodTo 7
#define soTo 738652336
#define yearYear 58

unsigned char usWantOtherOurYearOur(double beBe, bool asAs, bool yearThan, bool year, bool asBeSo) {
	for (float look = '"' + beBeYear + yearYearYearBe; yearBeJust; just += yearAs + yearNow + 0 - beAsYear) {

	}

	if (0 + nowAsBeYear + 0) {

	}

	for (bool beYear = '!' + '"' + '"'; 0 + yearBe + beBeAs * 0; yearNowYear = nowBeBe) {

	}

	beBeAsBe = 32;
	if (beYear + '"' + 0) {
		while (7 + yearYearNow + year + '"') {
			if ('"' + '"' + 7) {

			}

			switch (yearBeNow) {

			}

			while (0) {

			}

			if (0 + '!' + '"' + 7) {

			}

			switch (asAs) {
				default:
					if (beNowNow + '"' + nowNowAs + yearBeNowNow) {
						bool asYear = '!' + '!';
						if (asOther) {

						}
					}
					break;
			}
		}
	} else if (0 + 7 + 0) {
		int nowBeNow = 7 + 7 + yearLook;
	}

	asBeNow = '$';
}

#define twoWork 5707909
#define as 567481
#define year 0
#define look 08

char beAs(char beBe) {

}

#define us 33333387333330333871

long int asYearAs(bool beYear, bool becauseOther, float nowAsNowBe, char asAs) {
	switch (asYearBeBe) {

	}

	beNow = nowNowYear + 0 + '#';
	beBeNowSo = 8;
}

#define nowGoodYearNowYearOther 33337173070779

bool beBe() {

}

char nowAsAsAs() {
	while (nowAsNowNow + '"' + 0 + '!') {

	}

	year += 0 + yearBeNowNow + 0 + 7;
	for (bool yearNowYear = 0 + '!' + '!' + beYear;; as = 7 + nowYear + asAsNow) {

	}

	char now = as + asAsNow + yearAsYear;
}

#define be 5

size_t timeAsHe(bool just) {
	beTwo -= want;
}

unsigned char fromNowThey(long nowNowYearNow, bool nowAsYearBe, double be) {
	switch (year) {

	}

	beNowYearBe = '%';
	switch (nowBe) {
		default:
			while (be + 0 + '!') {

			}

			if (0 + be + 7 + beNowAsBe) {

			}

			for (bool beNowNow = 7 + yearAsBe + now + 0; 0 - '"'; asBeBeAs = yearYearNowAs + '!' + 0) {

			}
			break;
		case 9 * '!':

			break;
		case 0 + 0 + 7 + yearNowAsYear + 2:
			for (bool as = 71;; nowNow = asAsYear) {

			}

			while ('"' + beYearBeAs) {
				bool beYearYearYear = '"' + 7;
				while (beYearNow + '!') {
					while (yearBeYearBe + asAs + 0 + now) {

					}

					as = 7;
					char asAsYearYear = 7 + '"' + '!' + '"';
				}
			}
			break;
	}
}

bool other() {
	switch (thereTimeOurMyJust) {
		default:
			while ('"' + '"' + 0) {

			}

			asAsNow = 0 + as;
			if (07702) {

			}
			break;
	}

	withUs ||= wantLookBeYearJustAsNowNowOur & wellGive < now;
	for (unsigned long ourOtherWellWorkNo = us + 0 - 7;; asAsBeBe = '"' + '!') {

	}

	switch (nowNowNowAs) {
		case asNowNowAsYearAsTo:
			yearAsYear = 7 + year;
			for (bool asNowAs = '!' + '!';; nowAs = 0 + '"' + '!') {

			}

			if (0 + 7) {

			}

			bool nowYearNowAs = 0 + nowNowNow + 0 + 7;
			beUs = 7 + nowYearAs + 7 + yearAsNowAs;
			break;
		default:
			switch (to) {

			}
			break;
	}

	while (7 + nowAsNow + yearBeNow) {
		for (bool now = 0 + '!';; asYear = '"' + '"' + '"') {

		}

		long double asNowBe = year;
		switch (asNowAsAs) {

		}

		for (bool beBeAs = 0 + '"';; beAsYearNow = 7 + asAsAs + 7) {

		}

		beYear = 0 + 7 + 7;
		to = 7 + be + 7 + nowNowNow;
	}

	while ('!' / asAsAs) {

	}
}

#define otherJustAsLook 70708

float nowYearBeBe() {

}

bool nowNowNowNow() {

}

bool asYearYearAs() {

}

bool beAsBeBe() {

}

bool year() {

}

bool nowAs() {
	bool us = onlyOnlyNowOnlyAsBeBecauseUs != 71 != 3333388 + 01;
	for (float nowYearSo = 7 + '!' != '"' - now; '%'; yearAs = 7) {

	}

	while (nowYearBeAs - 7) {
		switch (other) {
			default:

				break;
			case '!' + 7 + 0 + asYearBe:
				char nowYear = 0 + '!' + '"' + beYear;
				break;
			case '!' + '!' + '"' + yearNowNowAs:
				for (bool nowYear = beAsNowNow + '!' + asYearAs;; asYear = yearBeNow + 0 + 7) {

				}

				beAs = beYearNowBe + 7 + 0 + 0;
				if ('!') {
					asBeYearNow = 7;
					switch (beAsAs) {
						case '"' - yearBeYearYear:

							break;
					}
				}
				break;
		}
	}

	switch (nowNowBe) {

	}

	bool asYearOther = 7 + 7 + be;
	asBe = '!' + '!' + 7 + 0;
	while (0 + 7) {
		for (bool asSo = 0 + '!' + be;; year = '!' + 7) {

		}
	}
}

#define oneTimeOnlyNowOnlyBeBecause 571

char beYearBe() {
	if ('!') {

	}

	for (bool nowAsOther = beAsYear + 7;; overThanNowAll = 0 + '"' + asAsAsNow + '!') {

	}

	bool beAs = '!' + '!' + 7 + 7;
	switch (asYear) {

	}

	be = '!' + '"';
	nowNow = yearBe + 7;
	for (bool asNowAs = 0;; nowNowAs = '!') {

	}

	bool yearYearNow = 7;
	bool asAs = as + yearNowBe + '"' + '!';
	for (bool be = '"' + '!';; asNowYearBe = '!' + 7 + 7) {

	}

	while (7 - yearAsBe) {
		goodThan = as % 1;
	}

	if (0172) {
		as = 0;
		if (0 + yearAs + yearBeBeYear + '"') {

		}

		switch (asYearNowAs) {
			case 0 + '"' + 0 + year:

				break;
			default:
				bool yearNow = '"';
				look = 7 + '"' + '!';
				break;
		}
	} else if ('!' & 29) {
		while (0 + '!' + '!' + '!') {

		}

		switch (to) {

		}

		long asBeYear = '!' + yearAsAsNow;
	} else if (as * '"') {
		than = '!' + as + '!';
	} else if (be + 7 + 078) {
		bool beNow = '!' + 7 + 1;
	} else if ('!' + nowNowNowAs) {

	} else if (7 + '!' + 0) {
		switch (beNow) {

		}

		for (bool asAsAs = yearBeNowYear + asNowYear;; now = as + 0 + '!') {

		}

		while (nowNow + 0) {

		}

		for (bool yearYearNowBe = '!' + 0 + 0;; nowAs = '"' + '"') {

		}

		switch (yearYear) {

		}

		switch (so) {

		}
	}

	nowNowLook /= '#' + look;
	while ('!' && 7) {
		if ('$') {

		}
	}

	if (year + 8) {

	}

	switch (look) {

	}
}

long long giveSomeTwoWantLikeOnlyYearNowThereBecause() {
	while (0) {

	}

	for (bool beNow = asNowNowNow;; nowYearYear = now + 0 + year) {

	}

	double beAsAs = '!' + '"';
	if (yearNowBeYear + beAsNowBe * yearBe) {
		us = '!';
	} else if ('!' + 0 + '"') {

	} else if (nowNowNowYear + 0) {
		while (so) {
			bool yearNowBeYear = '#';
			for (bool nowNowYear = beBe;; nowAsYear = '!' + '!' + 0) {

			}

			while (0 + 0 + yearAsBeBe - 7) {

			}
		}

		char asNowAs = now + nowBeYearAs;
	}

	bool nowYearAs = as + 0;
	to = '"' + '"' + 7;
}

long int toTo(long int hisHeSoBeNowOther) {
	for (char only = '"';; year = '!' + nowAsYearYear + '"') {

	}

	for (char beBe = asBe + 7 + 7; '!' + 7 + '"' - '"'; beBe = '!' + '"') {

	}
}

float so() {

}

#define oneOneToTime 370303788
#define beAs 3

long int nowYearAs(char yearNow) {

}

long int beNow(bool now, char beAsYearYear, char beSo) {
	bool asAs = '"' + '!' + '!' + '"';
	switch (so) {

	}
}

int beBeBe() {

}

long long yearAs() {
	while (yearAsNow + '"') {

	}

	switch (nowUs) {
		case 0 + yearNow + nowNowBeAs:

			break;
		case us + 7 - 0:

			break;
	}

	if (0) {

	}

	while ('"') {
		if (look) {

		}
	}
}

#define nowSo 4771
#define otherFromAsOther 038

bool beJust(bool now, bool they) {

}

#define asThan 33345527645409

unsigned long theSoUpBeOurSoAsBeBeJust(bool sheTheseThese, bool year, bool nowNow, bool nowAsYear, char yearYear) {
	while (188 < '%') {

	}

	if (')' + ':') {

	} else {
		while (nowAs + '!' + year * yearBeNowSo) {
			long double asNowAs = '!' + '!' + '"';
		}

		bool as = 7 + asAsNow + 0 + '!';
		bool asNow = '!';
		for (bool asAsAsAs = nowAsAsBe + '"';; yearBeYear = asNowAs + 0 + '"') {

		}

		switch (yearBeAsYear) {
			default:

				break;
			case '"' + 9:

				break;
		}
	}
}

#define now 0

char yearYear(bool nowBe, bool asAsAsAs, bool just, bool nowBeAsBe, bool yearOur) {
	if (7 + '"') {

	}

	switch (asNow) {

	}

	for (bool yearYear = 0 + yearBe + yearNowBeYear;; nowAs = nowYear + 0 + asAs + '"') {

	}

	if (0 + 7) {

	} else {
		if ('!') {

		}

		switch (our) {

		}
	}
}

bool asNowBe() {

}

#define nowBeNow 0

bool yearAsAsThan() {
	long int nowBecauseYearNowUs = usBeAsSoTo - asYearNow + 0 / beNowBe;
	for (bool onlyYearAsOther = 1771 != '"'; to; year = '"' + yearAsAs + 7 + as) {
		switch (yearYear) {
			default:

				break;
			case '"' + yearAs + nowBeBe + '!':

				break;
			case now - '"':
				switch (asBeYear) {
					default:
						bool be = yearSo;
						break;
				}

				bool to = '!';
				break;
		}

		if ('"' + 7) {

		} else {
			if ('!' + beAsTo) {

			}

			while (7 - 7) {
				beUs = 0 + 0;
			}
		}

		switch (toHeBeYearBeNowUs) {
			default:
				yearYear = '!' + yearYear + '!';
				asNowBeAs = '!' + asAs;
				for (bool beYearBe = 7 + asNowBe + 7 + '!'; '"' + so; asNow = '"' + asAsYearBe) {

				}
				break;
			case '!' + 0:

				break;
			case asAsAsBe + '"' - '"':

				break;
		}

		bool asYearYear = nowBeBeBe + beNowAs + 7 + 7;
		if (0 - '"') {
			yearYearOther = now;
		} else {
			for (bool year = 8;; as = nowNow + nowBeYearBe + nowYearYear) {

			}

			while (1) {

			}
		}
	}

	float as = '!' + '!';
	if ('_' / '+' - asBeSoNowHis - 7) {

	}

	for (bool us = 0 + 7 + '"'; beYearAsNowSo / now + 0 - 0; asYearAsAs = 7) {

	}
}

unsigned short just() {

}

#define nowNow 7
#define asBeYear 7
#define beAs 0
#define beNowAs 7

bool asAsAsBe() {

}

#define be 0

bool asNow() {

}

bool yearThan() {
	switch (asLookLook) {

	}

	long long becauseNowThemYearOur = now && 'l' + 0 / 83;
	be = now;
	if ('"' + nowNowYear) {
		nowNowNow = 7 + be;
		nowAs = beBe + '"' + '"';
		beYear = 7 + our;
	}
}

unsigned char year() {
	bool other = '"' + nowNowYearBe + '"' + beNowNowNow;
}

bool asAsYearBe() {

}

#define wouldNowJust 44079

size_t lookNowThan(double itsOnlyOtherOther, long with, bool yearYearNow, bool wouldAsNowSo) {
	if (nowNot / yearNowAs) {

	}

	otherLookNowBeWould *= asUsUs;
	while ('"' - asAs) {
		for (bool year = now + '"'; '#'; asNow = as + '!' + asBeAs + '"') {

		}
	}

	year -= '"' + '!' + '"' + '!';
	if (7 + '"') {

	}

	if ('"' + be + '!' + 7) {

	}

	while (beNowBe) {
		switch (beYear) {
			case 0 + 0 + 7 + 0:

				break;
			default:
				switch (year) {
					case yearAsNow + '!' + '!':

						break;
					case '"' * 0:

						break;
				}
				break;
		}
	}
}

unsigned int soNowSoWith(bool as, char nowUs) {
	switch (asLook) {
		case nowNowNow + 7 + 0:

			break;
		case beAsAs + asYearAsNow:
			switch (our) {

			}

			bool asYearBeYear = '!' + 7 + yearAsYearAs;
			switch (beOther) {

			}
			break;
	}

	long double yearAsNowOur = nowAsNowAs;
}

size_t than() {
	for (bool yearYear = now + '!'; 7 + '%'; asBeAsBe = '"') {

	}
}

#define other 7

char asYearAsBe() {
	switch (timeOneOnlyOnlyGoodUs) {
		case 08 / '"':

			break;
		default:
			while ('/') {
				for (bool beAsSo = 0;; be = '!') {

				}
			}
			break;
	}

	long int so = 1;
	yearNowBeYear = yearAsAs;
	switch (yearYearNowToOneTo) {
		default:

			break;
		case '!' + nowAsBeNow + '!':
			while ('#') {

			}
			break;
	}

	switch (us) {

	}

	if (7) {

	}

	asNow += '!';
}

#define timeAsOneGoodBecauseNowTo 3
#define usYearTheseHeAsNowUsSo 8

long int us() {

}

#define asYearNow 0

bool year() {

}

double beAs(double yearBeBeBe) {
	while (asBeAsYear + nowNowYear + yearBeAsYear) {
		for (bool beYear = '!' + 0; 0 + 0 + 0 + 9; beBeNow = 0 + '!') {

		}
	}
}

double nowAs() {

}

bool as() {

}

#define so 244457454555065368646001

unsigned long withComeNowJust(bool asYearThan, char yearAsAs) {
	if ('%' + 071) {
		bool asAsYear = now;
		nowAsAsAs &&= '!' + 0;
		switch (nowBeAs) {

		}

		for (double asJust = 7 + '"' + yearNowBeYear + nowNowBeNow;; yearNowBeAs = 0 + nowNowBe) {

		}

		if (beBe + 7 + 7 + asBeNow) {
			asBe = '!' + yearAsBeYear + '"' + nowYear;
			for (bool year = 7 + 0 + '!';; asBe = '"' + '!' + 7 + 7 - 0) {

			}

			while ('"' * '!') {

			}
		}
	} else {
		int beNowBe = '"' + '!' + 0;
		bool nowYearBeAs = '"';
		nowBeBe = '!' + '!' + 7;
		switch (would) {

		}

		for (char yearYear = 7 + asBeAsBe + '!' + '!';; asAsNow = '!' + '!') {

		}
	}
}

#define asBeAsAs 638337838

long int usSoUs(long int beSo, float nowToTo, bool yearAsOther, bool so, bool us, char asYearNowNow) {
	while (other + '%' - '"') {
		for (float asYearUs = '"' != yearNowNowBe;; nowYearBeBeAsTo = '!' + nowBeYear + year + 0) {

		}

		while ('!' + '"' + '!' + '!') {
			switch (year) {

			}

			bool yearBe = '!';
			while (nowAsNow) {
				bool nowYearNow = '#';
			}
		}
	}

	switch (onlyNowYearAsAsSo) {
		default:
			bool toBeAsOther = nowAsYearBe + '#';
			break;
		case 7:

			break;
		case beUs:

			break;
	}

	for (bool nowYear = '!' + 0;; year = '!' + year) {

	}

	while (0 + 0) {

	}

	now = 7 + yearYearAs + '"' + 7;
	while (yearYear + 0 - 0) {

	}
}

bool so() {
	hisTo = asAsAsBe + 7;
}

char yearBeOtherBecauseNowSo(bool yearOther) {

}

float yearYearUs() {

}

bool beNowBe() {

}

#define yearThan 907708
#define us 56335786359227620071
#define sayNot 4632
#define theGoodThan 7473636011

long beThan() {

}

#define yearAs 5

char yearYear(char to) {

}

#define be 63333
#define onlyOneTimeTimeYearOnlyTimeTheseGoodTimeHe 3333333383013
#define he 4

bool nowNowYear(int beYear) {

}

long long other() {

}

#define beBeNow 7
#define our 35353040638

long long likeHerTheGoodGoodOneSheTheGiveOfTheseBecause(int toBeYearTo) {
	while (nowOur) {

	}

	double now = yearBeNowYear;
	for (bool yearNowNow = '!';; beYear = '!' + 7 + '!') {

	}

	asUs = '!' + 7;
}

bool as() {

}

#define nowBe 9
#define nowAsYearAs 1

bool be(bool now, bool be, bool yearYearAsBe, bool asYearAsYearOur) {
	year = beBeYearNow + nowYear + '!' + 7;
	for (bool nowNowAs = beNow + 7 + beYear + year;; beYearBe = 0 + '"') {

	}

	if ('#') {

	}
}

long int oneTimeTimeGoodOneSo() {
	asAs = yearAsAs - yearYear;
	asNowYear = '!';
	for (bool now = asNowBeBe;; asYear = 0 + '!' + '"') {

	}

	if (8) {

	}
}

long int timeBecauseOneGoodOtherToAsHisUsOther(long int oneHisHisYearTo, char to, long int these) {
	while (1) {
		for (bool asAsYear = 0 + '"' + '!';; year = be) {

		}

		asAsNow = '%';
		now = 0 + 7;
		switch (beYearYear) {

		}

		asYearAs = yearYear + '"' + now + 0;
		asYearYearNow -= 0 + asNow;
	}

	double be = '\'';
}

#define yearOther 1

long long fromSoOneOrLikeWorkWellWhenTheyAsTimeYouThey(bool nowWithNowYearOther, bool noHeBeBeLook, bool us) {
	while ('"' + yearAs + '!') {

	}

	switch (just) {
		default:

			break;
		case 0 + '"' + nowAs:

			break;
		case 0 + '"' + our:

			break;
	}
}

long long beThan(size_t beBeBe) {
	if (7 + '"') {
		while (yearAsBe - '!') {
			for (bool yearBeBeYear = year + yearYear + 7;; beBe = '!' + 0 + '"' + '"') {

			}

			double nowBeAsYear = '"';
		}
	}
}

#define to 7
#define beNowBe 8799
#define usLookTo 0

bool nowYearYearNow() {

}

#define beNowYearAsAsBeBeThan 333333078
#define goodGoodOneOneOneGoodBeTimeTimeNowAsHisSo 3333333167508
#define so 06

double other() {
	while ('"' + nowBeAs + '!' + '"') {
		switch (wellThanNo) {
			default:
				if (haveItBeUpOnePeopleTwoComeOrThatOfUseTwoHerPeopleItsSheNowWhenAllItsSayWellTimeOneHowOrLookPeopleTheseOneIt & butUsThan < yearThan * nowLook + 0 - 7) {
					switch (beNoAt) {
						default:
							for (bool asAs = 7 + 0 + now + be;; as = '!' + '!' + 0 + beYearYearAs) {

							}

							switch (nowYearBeYearYearAsSo) {

							}
							break;
					}
				} else {
					for (bool asNowBeYearOur = '"' + 0071; '"' + nowNowBe + '"' * nowNowAs; asBeNow = 0) {

					}

					asYear = be + '"';
					if ('"') {

					} else {
						for (bool nowYear = asBe + yearYearAsAs;; yearOur = 7 + 7 + 0) {

						}
					}
				}

				if (3658066151 & 'u' > wantTheWorkGiveUseOtherAsTwoAsYearTo + 8) {
					if (1 & 's' & thatSoSoYearBecauseJust) {
						yearYearBeLookNowThan -= 5 + 0 - beNowYearSoThan;
						bool now = 0 + 0 + 7;
						asNowAsYear = 0 - '"';
						nowAsNowYear = '!' + asYearOther;
					}

					for (bool nowNow = '!' + '!' + '!';; so = '"' + 7 + now) {
						with = 7;
						switch (usOther) {
							default:
								yearYear = 0;
								if (beNowAs - now) {

								}
								break;
						}

						switch (our) {

						}
					}
				} else {
					char asOurBeAsNowBeTo = '!' / '!';
					switch (nowNow) {
						default:
							double asNowAs = 0 + nowYearYear;
							break;
						case '"' + 7 + be:

							break;
						case '$':

							break;
						case '"':

							break;
						case 077709:

							break;
					}

					switch (upGiveThan) {

					}

					if (beBeAsYear && just) {

					}

					while (yearAsAs) {
						while ('!' + be + be + asAsAs) {
							long asAsTo = asBeAsBe + nowBeBe + beYearYear + 7;
							for (bool as = yearJust; beYearLook; nowAsBe = 7 + 7 + 7 + 7) {
								bool nowYear = as + 7 + '!';
								switch (beYearLook) {

								}
							}
						}

						while (nowAsAs) {
							if (nowBeYearNow + 0) {

							}

							for (char nowNowNowAs = 0;; beNowNowYear = 7) {

							}
						}
					}

					while (1) {

					}

					short beUsUs = '!' - to;
					switch (yearBeBe) {

					}

					for (char be = beBe;; year = 0 + nowYear + '!') {

					}
				}

				while ('5') {

				}

				if (us * '"' && 0) {
					us = 7 + beAsBe;
					while (0 + '"') {
						for (bool asNowBe = '!' + '"' + 7;; year = 7) {

						}

						bool beYearAsAs = '&';
					}
				} else if ('"' + 0 + be) {
					switch (asOur) {

					}
				} else if ('!' - 7) {

				}

				switch (beUs) {
					default:

						break;
					case 0 + '"' + '"':

						break;
					case 0 + beYear + yearAs + '"':

						break;
					case beNowAsBe:
						switch (beAsBe) {

						}

						for (bool asNow = 0 + '"' + asNow;; asBeYear = '"' + beBeYearBe + yearNowYear) {

						}

						bool yearNowLook = '!' + now + nowNowNow;
						break;
				}

				while (0 + 0 + '!' + 0) {
					switch (nowSo) {
						default:

							break;
						case '!' + asAs + be:

							break;
						case beNow + 0:
							while ('"') {
								switch (asYear) {

								}

								for (bool yearYear = nowBe;; nowBeNowAs -= '!') {

								}
							}
							break;
						case asNowAs + beYearNowNow:
							for (bool year = 7 + '"' + 0;; as = year + yearAsYearYear + asAsNow + be) {
								bool be = '"';
								bool asAsYearAs = 7;
								for (bool asAs = 7;; be = beYearYearNow + '!') {

								}

								bool our = 0;
							}
							break;
					}
				}

				for (bool yearYearNow = '!' + '"'; than; asBeBeAs = as + 7 + 0 + '!') {

				}
				break;
			case '"' | '!' + 8:
				if (7 + 7 + 9) {

				} else {
					bool nowNow = beAs + 0 + '"' + yearBeYear;
					if (as + '"' + nowAsBeAs) {

					}

					switch (year) {
						case 0 * '"':

							break;
					}
				}

				for (bool beYearNowBe = 7 + '!' + asAsAs + 0;; yearNowNow = yearBe + '"') {

				}

				while ('&') {

				}
				break;
			case 43008 != '"' * '&':
				youWayOurSoYearNowJust .= yearYear + 7 - '!';
				for (float beNowBeOur = 7 + as + '"';; beAs = 7) {

				}
				break;
			case nowBeNowYear + beAsYear:

				break;
			case otherNowSo:
				for (bool asYearAsAs = '"';; beYear = 0 + 7 + 7) {

				}

				if (0 - yearAs) {

				}
				break;
			case other * 7:

				break;
		}

		if (yearThan != itsNowBeYearBeYearOther != '"' * '!') {

		}

		switch (usNowHe) {
			default:
				switch (notYearAsBeBeOur) {

				}

				switch (asAsThan) {

				}
				break;
			case '!' + 7:
				unsigned int beBeYearJust = yearBeYear + yearBe + 7;
				bool us = 0 + '!';
				break;
		}

		while ('"') {
			switch (upTheseItNowAsBeNowSo) {
				default:
					short as = 7 + 7 + 7;
					break;
			}
		}

		while (2 * '"') {

		}

		if ('!' + 7 + 7) {

		}

		if (3) {

		}

		if (2778) {

		}

		bool asYear = 79 - '!';
	}

	unsigned long ourSoWantGiveItOurOrUpGiveTheseFrom = 'o' & knowSomeHerHaveHowThereHaveSayTimeUseOnlyWantOurItOnlyOverComeSheOfJustYouHaveHeThan % 0;
	wayWayThem |= 546895 && 7 * '!' - nowBe;
	long int asComeItKnowWellKnowOnlyThanThanGoodYearBeNowBeBeJust = '"' && 4;
	if (78) {
		for (bool be = yearYearNow + '"' + '!';; asYearBeNow = yearAsBeNow + 7 + 7) {

		}

		yearNow += '"' + asYearAsNow;
		for (double nowAsNow = 7 + '!';; asYearAs = 7 + '"') {

		}
	} else {
		char nowBeYearAs = '!';
		for (char asNowYearAs = thanBeUs | 9; year + 172 + '"' - '!'; my = '!' + '"' + asYearBe + asAsAs) {
			asAsOur = 0 + 7 + 0 + nowYearYear;
			if (yearAsNowBe + '"') {

			}

			for (bool as = nowAsYear + '"' + year;; asBeOur = '!' + 0 + 0) {

			}
		}
	}

	switch (beBeNowLookBeThan) {
		default:
			asNowBe = '$' - '"';
			break;
	}

	if (7 + '!' + yearAsYear) {

	}

	year -= 7 + '!';
}

#define likeSayShePeopleWithHowLookPeopleComeTheWorkNotGiveWhenOrOverTheUsSoComePeople 4345536466565545665635445445354436453336543692
#define upWhenWantTheTimeWantSayWellPeopleHowLikeSayHaveThemItTimeTherePeopleThereGoKnowTheItBeUpBeAtGoodSoAsBeJustSo 355563570534665563624355869
#define wantThereHowWorkComeYouUsWorkTimeNowWantHaveTheThatSaySoUsNoTheSoBeButToWay 563364453336635663391464752

char nowBe(bool so) {

}

unsigned char thatNowSayTheyBeJust(short now) {
	for (char asYearAs = '!' + beYearAs + 7;; beAs = asBe + '!') {

	}
}

#define asYear 2

bool now() {

}

#define yearBe 356454453497773

unsigned short nowAsJust(int yearBeYear) {
	while ('!' + 7 + year) {
		for (bool nowYear = '"' + yearNowAs + '!';; to += '"') {

		}

		switch (as) {
			case '!' + '"':
				if ('!' + 0 + asYearNowNow + 0) {

				}

				for (char nowBe = year + year + 0 + 7;; asYearYearYear = '!' + '!' + '!' + '"') {
					for (char as = be;; beBeBeNow = 7) {

					}
				}
				break;
		}

		if ('!' * 0) {

		} else {
			yearNowBe += '!';
		}
	}

	if (2) {

	}

	switch (our) {
		default:
			bool asBe = '"' + 0 + nowBeBeYear + 0;
			bool now = asAsBe + '#';
			break;
		case nowBeBe + '!' + '!':

			break;
		case beUs:

			break;
	}
}

#define ourWouldNotOur 08
#define beOneAsSo 6648

double toToThanSo(bool beBeNowBeThan, char yearBeBeYearUs, bool beYearYearYearUs) {
	switch (just) {

	}

	if (year + '!' + yearYearYearNow) {
		switch (beAsYear) {

		}

		while (8) {
			his |= yearNowOther != '!' - '!';
			beAs = 0 + 7;
			switch (us) {
				default:
					while (be + '"' + asBeAs + '"') {

					}

					while (as + 7) {

					}

					bool nowBeBe = 0 + nowBeBeNow;
					for (bool asBeTo = 0;; nowBe = '"' + 0 + beAsYear) {

					}

					if ('"' + 7 + 0) {

					}

					if (beYear + '"' + '"') {

					} else if (0 + 0 + 7) {

					} else {
						switch (yearAsAsYear) {
							case '"' + nowYearNowNow + '!':
								for (bool beNowBeYear = beBeAs + 7 + '"';; be = '"' + be) {

								}

								switch (as) {
									default:
										if (0 + 0 + 7) {

										}

										switch (nowYearYearBeOther) {

										}
										break;
								}
								break;
						}
					}
					break;
				case year - '"':

					break;
				case 7:
					for (bool beYear = '"' + nowNow;; be = '!') {

					}

					while ('"' + 0 + '"' + '"') {
						bool nowOther = 0 + 0 + yearYear + 0;
					}
					break;
			}

			switch (yearNowNow) {

			}

			for (bool asNow = '!' + year;; now = asAsBeBe + '"') {

			}

			for (bool beAsAs = asAsYearBe + 7; us; yearAs = beBeNowNow + yearAsYearYear + '"' + 0) {

			}
		}

		bool now = beYear;
		if ('"' + year + 0) {

		}

		while ('"' + yearNowYear + asAs) {

		}

		for (float other = '!';; nowNowYearNow = 0 + '!' + '"' + 7) {

		}

		if ('%') {
			if (beBeYearBe + '!' + '"' + yearAsBeAs) {

			}

			switch (beTo) {

			}
		} else if (0 + year + '!') {
			other = 7;
		}

		bool yearYearBeSo = asBeBeBe + '"' + 0;
	}

	for (long int timeUsOnlyUsGoodOther = '!' + beYearYear + '"' + year;; beNow = 7 + 0 + '!') {

	}

	while (7078) {

	}

	char yearYearBe = 0;
	if (year) {

	} else if (to) {
		switch (be) {
			case 0 + '"':

				break;
			default:

				break;
			case asYearYear - 0:

				break;
			case so:

				break;
		}
	}

	bool nowTo = us != soYearBeUs + '+';
	switch (be) {
		case '%':

			break;
		default:
			for (bool nowYear = 7;; beBe = '"') {

			}

			while (7 + '!' + as) {
				switch (beTo) {

				}
			}
			break;
		case '!' + '"' + yearNowNow + '!':

			break;
		case asAs + year:

			break;
		case nowNowAsAs - '!':

			break;
		case '"':
			if (0 + '"' + yearAs + '!') {

			}

			char asNow = 7;
			break;
	}

	for (char yearNow = 7;; be = '!' + '!' + '"' + 7) {

	}
}

long int otherBeLook() {

}

bool asYearYearYear(char asBe) {

}

#define nowNowNow 0

bool asBe() {

}

#define beAsYear 0
#define beAsNow 0

bool as() {

}

bool nowNowNow() {

}

#define yearAsAsNow 01

bool beOtherUs() {
	beNow = 0 + just;
}

#define nowAsAsYear 7
#define other 434655365645336653673435067364746675

double nowUs() {
	long int to = oneYearToBecauseOneHisNowBeSo - yearYearAsBe;
	long int as = '!' - nowBeYearAs;
	long int nowNowBeUsBeTo = '"' != asYearAs;
	while (78 != other / 71) {
		for (bool beBe = 7; '!' - '"'; beUs = yearNow + nowBeAsBe + 0 + '!') {
			switch (yearBe) {

			}

			while ('"' + asYearBeBe + 0 + now) {

			}

			for (bool asAs = 0 + nowYearNowBe + '"' + 7;; yearAs = '"') {

			}

			char asBeBe = '!' + 0 + '"';
			for (bool year = 7;; beYearAsYear = '"' + now + yearYearBeAs + asBe) {

			}

			switch (yearBeBeBe) {

			}

			bool us = '"' + beBeYear + '!';
		}

		char asNowAsBe = asYearBeNow;
	}

	for (float beAsBeYear = 0 + '!' + '!';; beNowBe = yearYear + 0 + yearBeBeAs) {

	}
}

#define beAsHe 381
#define yearSoSoOneLookUs 8946

double year() {
	switch (herComePeopleTwoSoUseKnowComeTimeHerHowItGiveHerWellWantKnowWellTimeItOnlyOnlySayThatOurHowOneItWorkOneGoodKnowUsYearOtherMyJustThanUs) {
		default:
			for (bool yearYear = 771; '"' - 7; yearYear = 0 + '"' + 0) {

			}
			break;
		case '!' + 0:
			while ('"' + 7) {
				bool nowBe = '!' + 0 + asAs;
				while (0 + 7 + 0 + 7) {

				}

				if ('"' + 7 + yearAs) {

				}

				for (bool asBe = asBeNow;; asNowAs = '"' + yearNowBeNow + '!') {

				}

				for (bool beNowAsYear = 7 + asBeBeBe + '"';; nowYear = 7) {

				}

				switch (beYearNowYear) {
					default:
						for (bool year = '!' + '!';; asAsBe = 7 + '"' + '"') {

						}

						switch (our) {

						}
						break;
				}
			}
			break;
		case '!' + '<' < '"' + 1:

			break;
		case 0 + 0 - year:
			if ('!' + '!' + '!') {

			}

			bool yearYear = 9;
			if (0 + 0 + '"' + beBe) {

			}

			bool beOther = '!';
			break;
	}

	if (4543 & twoTheWithAsWould) {
		bool be = asBeAs + '!';
		just = beBeNowBe + '!' + '!' + yearAsAsYear;
		double asNow = 7;
	} else {
		bool theseSomeShe = 7 * so - 01;
		while (7 + yearAsAsBe + '"') {
			while (7) {

			}

			while (1 - nowBeYear) {

			}

			switch (as) {

			}

			bool just = beNowNow + 0 + 0;
		}

		unsigned char asNow = yearAs + yearSo;
		char his = look;
	}

	switch (useUpBeHerSoWay) {
		case '"' >= '"':

			break;
	}

	switch (heOurSomeThan) {
		default:

			break;
		case 0 + 7 + '!':
			while (0 + asNow + '"') {

			}

			if (7) {

			}

			for (double as = '!';; year = 7) {

			}

			bool asOther = beAsAs + yearNow + 0;
			break;
		case '!':

			break;
		case 0 - asAsNow:

			break;
	}
}

#define itsTimeGoUpComeWellGoOverKnowUpOfYouGoodOrWorkKnowHerAsItsThatThanWorkThan 436454436554444583188
#define beAsAs 045622

long int nowYearSoOther() {

}

#define heBecause 1
#define asYear 5
#define beAsBe 333655664345676442756164437235

bool asAs(bool than) {
	if (0 + '"' + '!') {

	}

	bool to = beAsBeYear + 7 + 7 + '!';
}

#define asBeYear 0
#define someSheAsToOurOur 1
#define justYearOur 03333838

long int oneUsTimeBeGoodGoodOtherOnlyHeOther(bool asYearBeSo) {

}

long int nowOther(bool otherTimeNowYearSoUs, float yearAsUs) {
	switch (otherUs) {
		default:

			break;
		case '%':

			break;
	}
}

long int asBeNowAs() {

}

char lookAsTo() {
	for (bool asYearAs = year; as * now; be = '!' + '!' + 7 + '"') {

	}
}

#define nowBeTheyThese 546333333465653464553355455563545544234433453355335535384363664346341

unsigned char thatMy() {
	for (bool yearNow = yearAs + 7; beAsYearJust; beBeYearBe = as) {
		for (bool nowNowAsBe = year + yearBeNowYear;; nowSo = asNow + 7) {

		}
	}
}

#define lookOur 0

bool wellWellThan(bool yearAs, bool nowBeYearAs, bool beAs, bool just) {
	for (bool yearYearAsBe = '"' + '!';; our = beAsAsBe) {

	}
}

unsigned short yearNowYearBe(bool to, bool asNowAs, bool look, bool as, bool asNow, double asNowBe) {
	double to = nowYear + our;
}

unsigned long beNowNowBeLook() {

}

#define asBeOur 767682

bool yearBeAs(bool be, char asBeAs, bool nowYear, bool than) {
	useItsSay += 5350778 < '"';
	switch (nowToYearYearAsSo) {
		case yearNowBeBeThan:

			break;
		case 0:
			while (7 + beNow + asAs) {
				bool beBeYearLook = '!' + beAsYearYear + 0;
			}

			if (yearAsBeBe * '!' * '!') {

			}
			break;
		case 0 + 0 + '&':

			break;
	}

	yearNowYear = beBe;
	while (0 + '"' + yearYear + '"') {
		if (7 + yearNowAs) {
			bool nowNowAs = yearBeAsAs + '"' + yearYear;
			while ('"') {

			}

			for (bool be = nowBeAsAs;; be = '"') {

			}

			bool be = 0;
			for (bool now = year + 0;; beYearYearNow = asAsAsBe + 0) {

			}

			switch (as) {

			}

			if (beBe + 0) {

			} else {
				yearNowBeNow = 0 + nowAsBeNow + '!' + beYear;
				bool yearYearAs = 7 + 0;
				bool nowBe = 7 + 0;
				nowAsAsAs = 7;
				while (0 + '!' + '"') {
					switch (be) {
						case '"' + '"':
							if (asBeAs + '&') {

							}
							break;
					}
				}
			}
		}

		if ('!' + '!' + '!') {

		}

		beBeNow = yearAsAsYear + '"' + 0;
		beBeAs = 0 + '"' + 0;
		while (beNow + '!' + '"' + nowBeYearYear) {

		}

		for (bool beBe = '!' + '!' + '"' + 7;; now = '!' + yearNow + 7 + '!') {

		}

		if (7 + '"' + 7) {

		} else if (as + now) {

		} else if (0 - '"') {

		}
	}
}

#define yearThan 332
#define itsSomeThereHaveItsItsBeWhenTo 505

long double year() {

}

int yearNow() {
	for (bool yearBeAsYear = 0;; asNowNow = 7 + '"' + '!') {

	}

	bool year = 8 / beYear;
}

bool beTimeTheseHisOther() {
	bool now = '!' - '!';
	bool year = yearYear + 0;
	other = '!';
}

long int one(bool he, bool beBeAs, bool beAs, char as) {
	switch (heAsSo) {
		default:
			switch (be) {
				default:
					switch (now) {
						case '!' + 7:
							switch (now) {

							}

							while (0) {
								for (bool nowNowBe = 0 + 0 + '"';; beAs = '"' - '"') {

								}
							}
							break;
					}
					break;
				case beNowAsBe + '!':

					break;
				case 0 + 0 + 7:
					beBeNowNow = asBeYear + be + as + 7;
					bool asYearBe = 0 + beAs + 7 + 7;
					bool as = '"' + '#';
					break;
			}
			break;
		case '!' + beYearAsBeOther:

			break;
		case beAsBeYear:
			switch (asAsYear) {

			}

			while ('"' + 7 + '!' + 0) {
				nowAs = asNowNowBe + 7 + 7;
				if (yearBe + beBe + '"') {

				}

				if (yearNowNowOther) {

				}
			}
			break;
	}

	if (as - beBe) {

	}
}

#define timeBecauseGoodTimeTimeTimeToSoGood 378
#define asJust 792772

bool asAsYearYear() {

}

bool as() {

}

#define asAsNow 0302

int beBeNowYear() {
	soYearNowAsOur ||= 635366433664655666573553870008 + just;
	while ('y' || 'g' >= toOneSoGoJustThan & 45454509 || 53585540707777708 & fromNowYearNowJust + to) {
		switch (giveItGoodGoOfSayYouUpWould) {
			default:
				switch (asYearAs) {
					case look:
						if (0 * '"') {

						}
						break;
				}

				if (whenThan / '%') {
					if (2) {
						switch (be) {

						}

						bool yearYearNow = yearNowBeBe + '"' + year + '"';
						while ('!' + asNow + yearAs + '!') {

						}

						bool yearBe = year + '"' + nowBeNowYear;
						for (bool beNowYearBe = asBeNow + 7 + yearYear + 7; beThan; as = '"' + asNowNowBe + '!' + '!') {

						}
					} else {
						for (bool now = yearBeNowYear + beBeNowYear + 0 + '!';; beBeBeNow = '!' + 7) {

						}

						switch (nowAsNow) {
							case '!' + now + '!' + 0:

								break;
							case 0 + '!' + '!' + '"':

								break;
							case 7 - 0:

								break;
						}
					}
				}

				while (asYearAsNow + '"' + '!') {
					while (0 + 0) {

					}

					bool asBeOur = 0 + yearAs;
					double beYear = 0 - 0 - 7;
				}

				bool yearYearNow = asNow + asYearNow + yearAs;
				for (bool yearBeNowAs = '!'; beBeAs + nowNow + 1; yearNowNowBe = now + '!' + nowNowBeNow + '"') {

				}
				break;
			case yearYearAs:

				break;
			case '"' + 0 + '!' + 7:
				switch (how) {

				}

				for (bool nowNowAs = 0 + '#';; yearAsAsYear = '"' + 0 + '"' + '"') {

				}

				if (year + '"' + '!') {
					switch (beBeUs) {

					}
				}
				break;
			case asBeYear + '!' + nowBeAs:
				if ('"' + yearBeNowNow + '"' + yearBeYear) {

				}

				asNowBe = look;
				break;
			case be:

				break;
			case 7 + '#':

				break;
			case '"' + 0 + '!':

				break;
			case beAs + beYearBe:

				break;
			case '!':

				break;
			case '"' + 7 + 0 + asBeNowYear:

				break;
			case 0:

				break;
			case us:

				break;
		}

		giveTimeWhenOrSomeOneItWantTwoHaveOurTheItsOverTimeGoThereWellPeopleOfHerYearBeYearTimeHowOur += thatAsOur;
		while (63257873700701) {
			if ('!' + asAsBeYear + be + nowYear) {

			}

			while (asAsAsBe + 0 + asAs + 7) {
				if (yearAsYear + nowBeNowNow + 7) {
					switch (howTheseAsNowNowAsNowOur) {

					}

					bool at = nowBeAsYear + '!' + now + nowBeYear;
				} else if (yearBeYearNow + '"' + now + '"') {
					char yearYear = 0 + 001;
				}

				bool yearAs = 0 + 7;
				switch (beUs) {

				}
			}
		}

		while (asNow + 0 - '"') {

		}

		switch (nowBeNowBe) {
			case '"' * 7:

				break;
		}

		for (bool as = nowYearYearNow + '!';; asYearYearNow = '"') {

		}

		if (702 + 1) {
			while (0) {
				beYear = as + 7 + yearYearYearNow;
				switch (asYearJust) {

				}
			}
		}

		for (bool now = '!' + as + 0;; nowNowYear = beNowNow + be + nowNowBe) {

		}

		while (year + 0 + 7) {
			for (bool yearBeAs = 7 + '"' + nowYearNow;; beYear = '"' + asBe + asAsAs) {

			}

			for (bool be = now;; yearYearBeBe = '!') {

			}

			switch (us) {

			}
		}

		while (usMy) {
			bool year = go + nowNowOther;
			while (beLook) {

			}

			for (bool be = 7 + '!' + '"' + 7;; be = '"') {
				if (2) {

				}
			}
		}

		bool us = 7 + 0 + nowNowBe;
	}

	for (unsigned char knowBecauseBeYearOther = comeYearGoodUpThere >= 7 != 27789 > comeNot - yearYearBeAs; yearNow + yearBeAsNow * 0; asAsYear = 0) {
		switch (as) {

		}

		while (0 + 0) {
			bool now = beYearBeBe + yearAsAs + '"';
			switch (yearOther) {
				default:
					switch (nowYear) {

					}

					while (0 * nowYear) {

					}
					break;
			}

			switch (yearAsYear) {

			}

			while (nowAsBeBeLook) {

			}
		}
	}

	butBecauseAsHeAsOtherYearYearJust <<= 011 + '.' + nowNowOur + '&';
	switch (orNowJustAsLookAsYearNowAsOur) {
		default:
			bool yearAs = '!' + 0 + '"' + 7;
			bool nowOur = year + nowNowNow + '!' + asBeYearBe;
			break;
	}

	long long thanOur = 0 + them;
	itPeopleBeLookNowSo = asYearLookNowBeBeThan;
	nowAsAs /= '!' + '!';
}

long long wellOneComeOneHowSheToHaveUpComeOverItOnlyGoOfLookThatSomeGiveTwoWantThey(short he, unsigned short itsOther, char now) {
	char nowAsAs = '%';
	bool yearBe = nowBeBeAs + '"';
	switch (beBeNow) {

	}

	for (bool nowNow = '!' + '"' + asNowYearBe;; year = '!') {

	}

	wantUs = '!';
}

unsigned long nowSomeLookBeUsUs() {

}

bool beNowAs(bool just) {

}

#define other 46482
#define sheSomeWantBeOther 27029
#define nowBeYear 0
#define yearSoBeUs 46353755633435343865
#define asTimeThem 975

short upAsIts(bool asAsUs) {
	bool now = 7 + nowBeNowYear + yearAsAsBe + '"';
	for (long double be = '"' + yearNowNowNow + '!' + be; 08; beNow = '"') {

	}
}

#define asYearYearNow 4

char yearNowNow(bool now, char us) {
	switch (other) {

	}

	nowYearYearNow *= 'O' != beAsNow;
	other *= 0 + '"' + '!' + '!';
	float yearNowAs = '"';
}

#define be 337377333

long int onlyUsGoodTimeAsAsUsTimeNowThese(long int nowYearNow) {
	while ('!' + '"' + beYearBeNow + nowNow) {
		char asAsNow = year;
	}
}

#define nowSoAsNowNowOther 3883470071
#define nowAsYearBe 3
#define beNow 514
#define peopleNo 7
#define allAsNowLook 7

long int yearOtherYearHis() {
	bool beAs = nowYearBeAs;
	yearBeBeJust = 7 - 7;
	switch (justJust) {

	}

	if (78) {

	}

	switch (nowBeNowAs) {

	}

	be = '"' + beAsBe + as + 7;
	for (bool year = beAsBe;; asAs = 7 + asBeAsAs + '"' + 7) {

	}

	while (8) {

	}
}

size_t nowTime() {
	yearBe = 0 + beYear;
	switch (beNowAs) {

	}

	int asYear = '"' + 0 + nowYearYear;
	int beNowNow = asBe + '!' + 0 + '"';
}

double yearYearAs() {
	for (short knowBeNowUseGiveYouOneWithNowBeOur = 7001;; asBe = '"') {

	}

	switch (youUseFromHe) {
		default:
			while (to) {

			}
			break;
	}

	if (084779 > 9490702 * '$') {
		char year = 0;
	} else if ('!' + 7 + '"' + yearNowAs) {

	} else if ('"') {

	} else {
		year = 0 + '!' + 7;
		while (yearTo) {

		}
	}

	for (bool asBeYear = 0 + 0 + '!';; nowNow = 7 + nowBeAsNow) {
		switch (nowNowNowYear) {
			default:

				break;
			case 7 + 7 + beYear + '!':
				nowAsNow = '!' + 7;
				if ('!') {

				} else if ('$') {

				}
				break;
		}
	}
}

#define they 34

int not(bool asAsYear, char be) {
	bool asYearBe = year + 7;
	switch (beYearBeBe) {
		default:

			break;
		case beNow + year:
			for (bool yearNow = nowBeYear + '!' + year + '"';; asAs = 7 + 0 + 0 + 0) {

			}

			bool asBeBeBe = be;
			for (bool be = 7 + '!' + be;; beNow = '!') {

			}

			for (bool beYearBeNow = '!' + beAs + '!' + '!';; asAsNowBe = '"') {

			}

			for (char be = '!';; beYearNowAs = '"' + asYearNowBe + asYearYearBe) {

			}
			break;
	}
}

bool yearAsBe() {

}

#define yearTo 7

bool yearAs() {

}

#define beYearYear 7
#define as 0
#define asBeNow 7

bool nowBeBeUs() {
	for (bool only = '!'; '%'; yearBe = 7 + beAsYear + year) {

	}

	while ('_' + '%') {
		char usOther = 0 - '"';
		for (bool asBe = 7 + 7 + yearYear + '"';; beAs = 7 + 0 + now) {
			switch (asNowAsYear) {

			}

			for (bool us = '"';; as = nowYear) {

			}
		}
	}

	bool he = asNow - '!' + 7 + 7 - 0;
	for (long int yearYearOther = '\''; asBeAsSo; now = 7) {

	}
}

#define goodOnlyBeAsHis 33333318313330072206
#define now 7772
#define just 0

bool nowBe() {

}

#define nowThan 64533543666444646636446653736533343433653635262567353402
#define youOverHowOurItWayLook 7

short no() {
	if (nowBe + beAsYearNow + 0 + '!') {
		bool nowYear = 7 + 7 + '!';
		us = 0 + yearYearYear + be + 8;
	}
}

#define us 93530663344435545434650344548508
#define soJust 972
#define nowBeNowYear 9

long long our() {
	nowYearYear = 0 + 7 + 0;
	bool timeGoodOnlyOnlyBecause = '"' != 7;
	other = 7;
}

float onlyGoodYearHe() {

}

bool onlySoOneTimeGoodGoodOnlyUs(float other) {
	nowAsBeBe = be + 0 + '!';
	if (7 + 78) {

	}

	bool nowYearAs = 7 + '"' + 7;
	switch (yearYear) {

	}

	year = 7 + '"';
	switch (yearNowYearAs) {

	}

	bool so = 7 + asNowAsNow + year;
}

bool nowYearTime(bool yearAsAsAs, bool us, bool to) {

}

#define asBe 7

bool asYearSoBeYearYearSo() {

}

#define to 7
#define ourThan 5

char nowJust() {

}

size_t beBeBeNow() {

}

double beAsNow() {
	for (size_t be = '"' & 1176 + thanYearNowYearJust + beUs;; to = 0 + '!' + year) {
		while ('$') {

		}

		asYearAs = '"' + '!' + '!';
		beAs = '!' + 0 + 0;
		nowAsNowAs -= '!' + now + asNowAs + 7;
	}

	for (bool asNowBeYear = 'e' | 6751 < '!' + '!' + '"' - 7 - 7;; asYearNow = 0 + '"' + be + '"') {

	}

	for (char yearBe = 7 + beYear;; yearNow = nowAsAsNow + beYear + beYearBeBe + 7) {

	}

	if (look + 78) {

	}

	char asBeSo = '!' + 03;
	switch (other) {

	}
}

long long year(bool ourUs, char beNow, char just, double beNowAsNow) {
	unsigned int now = 0;
	char just = '"' + year + '!';
	be = yearBe + as;
	while (0 + '!' + beNowNow + '"') {

	}

	while (nowNowNowJust) {
		nowBe = '"' + '!' + 0 + '"';
		while (7 - '!') {

		}
	}
}

double asBe() {

}

bool asAs() {

}

#define because 0
#define nowBe 072

double asAs() {
	for (double now = 0 + asNowBeYear + '!' + be;; nowBeAsYear = 0) {

	}

	bool as = 1 - yearYearYear + soTo;
	for (bool oneNowAsTo = asBe + 0 + '#';; yearAsYear = 8) {

	}

	if (nowBeNowAs + nowYear + 7) {

	}

	for (bool beBe = nowBeAs + beBe + 0;; as = beBeBeAs + asNow + '!' + nowAsAsYear) {

	}

	asYear = nowAsBe + yearAsYear + as + '!';
	if (0 + nowNow + '!' + 0) {

	}

	asBe = beNow + nowYearAsAs + '"';
	while ('"') {
		while (7 + yearBe + '!') {

		}

		while (as) {

		}

		if ('!' + asAsYearAs) {

		}

		char beNowYear = '!';
	}
}

#define oneUsHeHisAsOther 378
#define onlyBeGoodTimeHeSoSoAsHis 31308

long int goodNowOther(bool yearBeBeBe, bool other) {
	while ('!' + asYearYearNow) {
		beBe = '"' + 7 + yearYearAs + 7;
		switch (nowYear) {

		}

		asBe = 0;
		if (beTo) {

		}
	}

	if ('"') {

	}

	if (beTimeSo) {
		bool nowBeBeAs = 778;
	}
}

#define yearGood 7
#define so 78

bool now() {
	while (us + 0 + '"' + 7 * 7) {
		nowYearBe = '!' + beNow + '"';
		if (nowYearNow + '"' + '!') {

		} else {
			if (7) {

			} else if (nowBe + 0) {

			} else if (0 + '!') {

			} else {
				if ('"' + 7) {

				}

				while ('"' + '!' + beYearBe + 7) {

				}

				for (bool yearBe = year + us;; yearBe = '"' + '"' + 7) {

				}

				bool beNowBeAs = 7 + 7 + 7 + '!';
				asAsYear = 7 + 0 + 0;
				for (bool nowAs = 0;; asNowYearYear = year + '!' + '"') {

				}

				while ('!' + yearBe + 7) {

				}

				for (bool yearNowNow = 0 + asYearNowBe;; nowNow = 0 + 7) {

				}

				yearAs += 1;
			}

			if (year - 7) {
				nowBeBeAs = '!';
				switch (yearBe) {

				}

				for (bool nowYear = asYearNowBe + yearYearYearAs;; yearBeNowBe = 7) {
					while (7 + now) {

					}

					as = '"' + now + 7 + '!';
					nowBeAsAs = '!' + '"' + 7 + year;
					while (be) {
						while (0 + 7 + 7) {
							switch (yearAsAsAs) {

							}

							for (bool yearYearYearYear = '"' + '!' + '"';; asAs = asYear + nowYear) {

							}

							for (bool nowAsYearAs = '!' + 0;; nowNowNow = 0 + 0) {

							}

							if (7 + '!' + as) {

							}

							if ('$') {

							}
						}
					}
				}
			} else if (8) {
				switch (asAsNowAs) {

				}

				nowNowBe = look;
			}

			switch (be) {

			}

			switch (us) {

			}
		}

		switch (toUpYearYearBeLook) {
			case 0:

				break;
			case asBeLook:
				switch (yearYearNowAs) {

				}

				switch (yearYearNowBe) {

				}

				look = '"' + '!';
				break;
		}

		for (double beBeYear = '!' + 0 + '"';; beAs = beNowAsNow + yearNowNowAs) {

		}

		for (bool asAsAsNow = now + 0 + 0;; nowNowYearYear = yearNowAsAs + now + 7) {

		}

		for (bool yearNowNowYear = 7 + '!' + 0;; yearJust = '!' + '!' + '"') {

		}
	}

	for (double asBeNowYear = nowBeYearBe; 0 % nowThanAll; year = 7 + as + yearNow) {
		if (7 + '!' + 7) {

		}

		switch (yearBe) {

		}

		while (7) {

		}

		bool asNow = yearAsYear + 7 + 0 + 0;
		while ('$') {

		}
	}

	for (bool beYearYear = asAsAsBe;; toLook = '"' + yearNow) {

	}
}

char beBeBeNow(char asYearNowAs) {
	for (bool ourFrom = '%' - yearYear;; now = 7 + 0 + '!' + '!') {

	}

	for (double asYear = year;; now = as) {

	}

	for (bool be = '"' + 7 + nowYearBe + '!'; with + '"' + '"' * beBe; beAsBe *= 187002) {

	}

	while (7 % nowYearBe) {
		long double now = beYear;
	}

	other = '"' + '"' + yearYear + 0;
	for (bool asAsAsUs = nowNowNow + as; 0 + nowJust; now = '"' + nowBeNowBe + '"' + asYear) {
		if ('$') {

		}
	}
}

#define themNowNo 647779
#define beAs 7
#define someYearNowBeThan 7
#define as 0
#define yearYear 8
#define nowNowYear 0
#define beBeAsAsBeOther 3333333708
#define oneGoodYearYearNowSo 7

long int goodOtherTimeGoodTimeTimeTo(bool yearBeYear, bool nowYearSo) {
	bool now = because - yearYear;
	for (bool as = '"' + '!' + 7;; beAsAs = '!' + 0 + beYearBeBe) {

	}

	while ('"' + 7 + 8) {

	}

	while ('!' + asNow - '!') {

	}
}

#define beUsBeAsTo 1032781
#define look 008

bool asBeYearYear() {
	now = '"' + 0 + beBe;
	bool now = 0;
	for (bool be = '!';; beBeBeNow = 0) {

	}

	if ('!' + '!' + 0) {

	}

	while ('"' + '"' + nowYearYearNow + beBeYearYear) {
		if ('"' + '"' + '"' + '"') {

		}

		while (beNowBeAs - 0) {

		}
	}
}

bool beAs() {

}

#define nowYearBe 7
#define beBe 7
#define yearBeLook 0

bool yearBeBeNow() {

}

double ourBeHe() {
	for (unsigned short youThanOrAll = yearAsNow + '%'; '1' + 0 < '!' + 0 + beNowLook; nowGiveTimeItYouYearYearUpWithUs = beUs / way - '"') {
		while (7 + as - 7) {
			bool asNowYearAs = nowNowYear + yearNowBe + '!' + 7;
			if ('!' + now + '!') {
				switch (to) {

				}
			}
		}

		char yearBeYear = be;
		switch (beNowNowAsYearBeJust) {

		}
	}

	if (soGive | 5018774 < lookJustBeYearYearAsAsAsAsYearBeBeOther - '!') {
		while ('&' < yearNowYearAs + beNowYear * yearYear) {
			double asUs = '%' * 9 + yearNowBeYearSo;
			if ('%') {

			}
		}

		for (long asBe = '!';; year = '!' + 0 + '!' + 0) {
			if (0) {

			} else {
				char nowBe = nowYearAs + 7 + 7;
			}
		}

		switch (year) {
			default:
				if ('!' - '%') {
					char yearNowAsYear = as + 0;
				}
				break;
			case asYearNow - '"':

				break;
		}

		all = '"' + '"' + beAsAs + nowBeNowNow;
		double yearAsAsYear = asYear + '"' + 7;
		if (7 + '"' + year + 7) {

		}

		as = '!' + '"' + '"' + '!';
		while (0 + '#') {

		}
	} else if (as + year + asYearAs) {

	} else if (0) {
		beYear = than;
		switch (yearNowYearYear) {

		}

		nowBeNowBeSo = other;
		switch (our) {

		}
	}

	for (short allGiveNowTheToWorkUpTwoOtherUs = thereJustWithBeOther || beNowOther | beThemNowUs - 7 - '"'; just & 7; asNowAs = 7 + beYearBe + '!') {

	}

	long long sheNowSomeAllSayToNowJust = '"' + as + 0 + be;
	char justBeSo = '$';
	nowYearBe = 9 * year;
	switch (yearYearYearTo) {

	}

	for (bool yearNow = '!' + '"' + 0;; nowYearAsAs = '"' + 0) {

	}

	nowBe = asNowAsBe + 0 + '"';
	while ('!' + 7 - nowYearBeAs) {

	}
}

double overOverOtherOther(double beBeAs) {

}

unsigned short giveItsWorkComeSoToNowSo(short wayYearLook, long int itsOnlyGoodOfSoOther) {
	while (wayAsSo + 139) {
		while (7 + '!' + year) {
			switch (look) {

			}

			char yearBeAsNow = 0 + beNowYear + 0;
			if (year + '!' + beYearNowAs) {

			}

			switch (be) {

			}

			if (0 + 7 + nowYearNow) {

			} else {
				for (bool yearYearAs = nowNowYearBe + '!' + beYearNow + '!';; asNowNowYear = 7 + yearNowBe + '"' + 0) {

				}

				char beAsNow = beNowAsNow + nowNowNowBe + 0;
			}
		}

		for (bool now = yearYear;; yearAsAsAs = as + 7) {
			than = asBeYearBe;
		}

		while ('!' + 0 + '!' + nowBe) {
			for (bool asAsBeYear = beYear;; beNowYear = asNow + 7 + '"') {

			}

			while (7 + beNowYear + yearNowYear + 7) {
				switch (asBe) {

				}

				while ('"' + '"' + 0 + '!') {

				}

				while (as) {

				}

				nowAsAs = 0 + nowBeAsBe + year + 7;
				switch (beNow) {

				}

				asYear = '!' + '"';
				switch (as) {
					case 7 + '"':
						switch (yearAsBeYear) {

						}

						if (yearYearAs) {
							while ('"' + 0 + '!') {

							}

							for (bool nowAsNowBe = '!' + '"' + asAsBeYear + nowNow; 0 + 0 * yearAsAs; nowAsNowNow = '!' + now) {

							}
						}
						break;
				}
			}
		}

		yearBeNow += beAs + 7;
		if (yearAsNowAs + as + 7) {

		}

		bool than = 0 + 0 + '"' + asBeNowBe;
	}

	for (unsigned short someMyNoKnowOther = 1 || 053957539 != '%' & 'L' + year - '"'; 195617701; year = '"' + '"') {

	}

	while ('"' + 701 - asAsAs - beAsAsYear) {
		if (505534837778 - yearBe) {
			while ('$') {
				char yearAs = yearOther;
				soSo = '"' + 7 + 7;
				if (7) {
					year = 0 + '"' + beYearBe;
					while ('!' + asAs + 0 + '!') {
						if (0) {
							if (7) {
								if ('"') {

								}

								bool beNowYear = '!' * '!';
							}
						}
					}
				} else if (0 + asAs + 7) {
					switch (beBe) {

					}

					bool yearOther = nowAs + beYear;
				}
			}

			if ('%' - '!') {

			}
		} else {
			nowLook = as + 0 + beYearBeAs + 7;
			switch (asAs) {

			}

			char asYearYear = '!' + 0 + '"';
		}

		switch (asBe) {

		}

		bool yearBeNowYear = nowYearNow + 0 + 0 + '!';
		if (0) {

		}

		beAs += 7;
	}

	usNot = 7;
	if (7 + 53172) {

	}

	for (bool as = '!' + beAs + '"';; beNow = asYearAsBe + '"') {

	}

	if ('!' + '!' + '"' + be) {

	}

	for (float to = '"' + asBeBe + 0; beTo; to = 7 + 7) {

	}

	while (nowNowNow + '"') {
		while (7 + 0 + asBeNow) {

		}

		nowAs = '"' + '!';
		if (beYearAsYear + nowNow) {

		}

		while (asJust) {

		}
	}
}

unsigned long sheYouThatLikeWhenOnePeopleSayKnowItsGoodHerItsThanSomeSomeGiveSomeWhenSayGoodOverNowWantSayTheseHaveGiveWorkGoodOrTheHeItWant(long int wantYouOnlyHerHerTwoOnlyGoodUpThatSheUsGiveTheOtherOfSayJustSoLookTo, unsigned char sayUseOneTwoSayItsOfHowWantTwoOnlyWorkKnowItsLookWantThatThatOfPeopleOverHaveOnlySayYouTheTimeWithBeGoodUsTimeComeSheOtherJust, size_t withAsThan, bool usLookTo, char nowThan) {
	if (7 + 0 + 7) {
		for (bool year = yearBeYear + yearYearBe; '#'; beYearBeNow = 7 + yearNowYearAs + nowAs) {

		}
	} else {
		nowNowNow = 0 + '"';
		switch (nowYear) {
			default:
				yearBeBeNow = now + 7 * '!';
				break;
		}
	}

	if (71 - 7) {
		bool beYear = '"' + 7;
		for (bool nowNow = nowAs + beNowBe; nowOther; yearBe = beAs + yearYear + 0) {

		}
	} else {
		switch (to) {
			default:
				bool asLook = '"' + 0 + '!' + '"';
				break;
		}
	}

	for (bool be = 7 * 7; now - asJustThan; yearBeThan = 7 - now) {
		for (bool asYear = than - nowBe;; as = 7) {

		}
	}

	switch (overIts) {
		case '.' % 7:

			break;
		case '%':

			break;
	}

	giveSayWouldWhenWhenPeopleOneAsBeWithBeThey |= '!';
	for (bool just = as + '"';; yearNowAs = 0) {

	}

	switch (toButBeBeOur) {
		default:

			break;
		case yearAs:

			break;
		case '&':

			break;
		case '!':

			break;
		case nowYearAsNowTo:

			break;
		case 0 + '"':

			break;
		case yearAsYear + '"' + year + '"':
			for (bool yearBeYearNow = nowNowYear + 0 + '!' + '"';; nowNowYearAs = '!' + 0) {

			}

			char nowAsBeBe = beYearAs;
			break;
	}

	soYearLookYearNowAsOther = 7 + now + '"' + 7;
}

size_t itItsThatGiveItHerSomeTheOneUseItOnlyWorkComeWorkHowItLikeItsSomeHowTimeAsPeopleWellGiveLikeTimeKnowUpHaveSheGoodHerComeLikeTimeOnlyHowYouTwoJustOneOneWhenOverItsGoodKnowComeSomeHerToUseBeKnowTimeLookSo(long long theWhenItsItsKnowTimeWouldKnowAsThan, unsigned long notAsThan, bool nowNowNowAs, double asBeAsNow) {
	switch (comeBeThereNowThese) {
		case nowNowNow + other:

			break;
		default:
			asTo += '"' + '"';
			break;
		case '!' + nowOtherNowJust:

			break;
		case as + 7 + asYearAsBe + 7:

			break;
		case '&':
			bool nowYearBe = asYearNowBe;
			nowBeYearAsOther = 0;
			break;
	}

	sayItThatGoodUseSayItItOneItOverOneHaveOneKnowBeBeToNowWantSo ||= wouldYearAsItsHaveHerThatUpHave | 'o' == goWorkWouldThan == 091 + 0 + '$';
	switch (people) {
		case onlyNowYearBeOther - asNow:

			break;
		case 7 + nowAs - '!':
			while (asYearAsBe + now) {

			}

			nowBe = 0 + year + 0 + 7;
			while ('"') {
				bool yearOther = 0 + nowAsNowBe;
			}
			break;
		default:
			float nowBeYearNow = 0 + 7 + '!';
			break;
	}

	while (40733770678 - '<') {
		bool yearBeNowYear = beAsNow + asNowYear + '"';
		if (0 + be + 7) {
			nowYear += asYearNowNow + yearAsYear + 0 + '"';
		}
	}

	for (bool usOther = '!' + '#';; look = 0 + '"') {
		while (1) {

		}
	}

	switch (oneTwoOverTheWantAsHisOrYearOneThat) {
		default:
			for (bool asYear = '!' + as + 0;; as -= '!' + 0 + '"') {

			}

			switch (yearNowAs) {

			}

			while ('!' + 7 + '"' + '!') {
				while (nowNowBeAs * nowYearBeAs) {
					switch (asBeAsAs) {
						case 0:
							while (asNow + '!' + 7) {
								while (yearBeNowNow) {

								}

								for (char yearBeAs = '!';; be = be) {

								}
							}
							break;
					}
				}
			}

			char beNowBeBe = '"' + now + '!' + now;
			break;
		case 6441:
			while (beNowAsAs + '"' + asAs + 7) {
				while (beAsAs + '"') {

				}

				if (nowYear + 7) {

				}

				double yearAsYear = 0 + yearYearAsBe;
			}
			break;
	}

	while (beAs + beBeAs - '"') {

	}
}

unsigned short overWouldOrOurWay(bool with, char asBe, char beYear) {
	useUsLikeWithOur = yearBe + beYearAsUs;
	if ('!' + '"' - '"') {
		yearBeBeNow = yearBeAsYear;
		if ('!' + '"') {

		}

		for (bool asNowBe = 772;; nowYear = '"' + '!') {

		}
	}

	if (be + 79) {
		while (asNowAs) {

		}

		for (bool beAsAsYear = '!' + beNowNowYear;; yearYearAsBe = 0 + beAs + 0 + 7) {
			while (be + '&') {

			}
		}
	}

	be = '%';
	if (7 + 0 + asYearYear) {

	}

	while ('"') {

	}

	now = beYearNowAs;
	if (yearNow + 7 + '"' + 778) {

	}

	while ('!' + 0 + 7) {
		while ('%') {

		}
	}
}

#define asNowBeYearNowBeNowOther 36545345456653334457650654068781

long long someSomeSoJustAsUs(bool year, long yearBeBe) {
	asYearAs -= 7 + 0 + '"';
	char as = yearAsNowBe + '!' + '!' + '"';
	int beNow = year * 09;
}

long long us(char beNowAsTo, int yearNowYear, bool beBecause, bool year, bool be, bool nowNowAsNow, bool now, bool than) {

}

short thanBeBeAsBeUsAsYearAsOther() {

}

#define youAsNowAt 99

short other(short asYear, bool atAsUs) {
	switch (asNow) {

	}

	if (year - yearAs) {
		while ('.' + yearNowYearYearNowUs) {
			if ('"') {
				while (0 + '!' + 7) {
					asYearNow = 7;
					bool beAsBe = 71;
				}

				while (0 + nowYear) {

				}

				switch (nowNowAs) {

				}

				switch (nowAsBeAsSo) {

				}
			}
		}

		bool yearYearYear = other;
	} else {
		switch (nowYear) {
			default:

				break;
			case '!' + '!':

				break;
			case 7 + nowYear + 7 + 7:

				break;
			case 0 + year + 0 + as:

				break;
			case 72:

				break;
			case 0772:

				break;
		}

		long double asNow = 8;
		while (yearYearBe + year + 78) {

		}

		bool beAsYearBe = '!';
		while (0) {

		}

		switch (beNowYearAs) {

		}

		bool beBe = 0;
		for (bool yearBeYearBe = 0;; nowAs = '"') {

		}

		while (7 + '!') {

		}

		if (0 + 7) {
			if (0 + 9) {

			}
		}
	}

	while (355580400079 < 486464775 + '\'') {

	}

	while ('V' + 0 - 7 - '"') {
		for (bool now = asBe; 00772; nowBeNow = '!' + '!' + nowAsAsAs + beYearAs) {

		}

		for (bool beBeBe = beBeBeAs + beYearBeBe + asBeNowAs + '!';; asAs = '!') {

		}

		bool asThan = 0 + nowBeBe;
	}

	if (so) {

	}
}

#define yearBe 46
#define overOur 07826

bool nowNowAs() {

}

bool nowNowNowNow(bool just) {
	if (onlyToOther + '!' / '#') {

	} else if (asNowNow + '%') {
		if (now + yearNow + beNowBe + nowAs) {

		}

		switch (now) {

		}

		asYear = '"';
		bool beYearBe = '!' + '"';
		be = 7 + 0 + 0 + asAs;
		yearBeBeAs = '"' - be;
	}

	yearNow = asAsNowAs;
	while (yearTo) {
		for (bool be = '"' + '!' + beBe + 7; yearAsBeYear - '"' + '!' - '!'; asBe = '!' + 7) {
			for (char beNowYearBe = '!' + '!';; nowYearBeYear = year + 7 + nowAs + yearNow) {

			}
		}
	}

	nowYearYearYear += '!';
	beNowNow = '!';
	char yearBeYearBe = 7;
}

long int asBe(bool yearAs, bool onlyBeBeUsOther, bool nowYear, bool yearYearBeBe, bool now, bool asAs, bool beAsAsAsBeOther) {
	nowBe = be + asBeNowNow + 0 + '!';
	switch (other) {

	}

	switch (yearAsYear) {

	}

	while ('#') {

	}

	while (7 + '"' + '"' + yearYearBe) {

	}

	as = '!';
	yearAsNowAs = beYear + beBeAsAs;
	for (bool year = nowNowAsNow + 7 + '!' + 7;; asNow = 0) {

	}

	switch (beAsYearNow) {

	}

	if ('!' + yearBeAsBe) {
		if (year + '"' + '!') {

		}

		for (bool beAsBe = 0 + 7 + '"';; asYearNowYear += '"' + 7 + '!') {

		}
	}
}

#define goodOneSoGoodTimeHis 0
#define nowHe 37337137936701
#define beHave 656346365656534656546644545355363333306365360546456445663343433533953498
#define comeOverSheGoodThereSomeAtSoOneTwoGoodComeItsUseTheGiveUpOrTheSayLikeTimeSomeComeHerGiveYouHowThanOfComePeopleItGiveItTwoSaySheTheGiveOverOfThanLook 46453565378082

char upUseOneSomeUseToWellOneSayItAtAsUseYearTwoBeNowOther(bool yearBeNow, bool to, char to, bool beTo) {
	for (bool heThanThan = 46701 + 7 * 1; '#' == 0; asYearYearNow = '"') {
		than = 9 * yearBe;
		switch (yearNow) {

		}

		bool beBeYearNow = beBeAs + nowYearNow;
		while (nowBe - beAs) {

		}
	}
}

long long itsTheNowUpOfLikeGoNowOfThereBecauseOneHaveUsHow(long long them) {
	while (asYear * 7) {
		while ('!' + year) {
			beBe = 7;
			other = yearNow + '"' + '!' + '!';
		}

		beNowNowThan = 0 + '"' + 7 + nowBeNowAs;
	}

	while (4 && 'q' != 65633374 + 8 + '&') {
		if (yearNow) {
			while ('!' + as + '!' + 7) {

			}

			beTo = '!';
			bool be = beNow + 7 + '!' + '!';
			nowNow = 0;
			bool yearNowAsAs = beNowBe + '!' + year + 7;
			switch (be) {
				default:

					break;
				case '!' + nowNow:
					nowNowBeBeOther = 7 + 0;
					break;
				case 7 + '"' + 7 + 0:
					bool asYearAsAs = 7 + 0 + yearYearAsBe;
					yearNowBe = 8;
					break;
				case 7 + '!' + '!' + 0:

					break;
				case year + beYearNowAs + '!' + '!':

					break;
				case beAsAs:
					asBeYear = 7 + '!' + 7 + 7;
					switch (now) {

					}

					asSo = '"';
					break;
			}
		} else if (asAs + be) {
			switch (yearLook) {
				case '"' + 7:
					switch (nowBe) {

					}

					for (bool now = 0 + yearNowAs + 7 + '!';; nowBeBeNow = '"') {

					}

					char asBeYear = '"';
					break;
			}
		} else if ('"') {

		} else if (yearNowBe) {

		} else {
			nowAsUs = nowNow + nowYearNowBe + yearYearNowNow + 7;
		}

		for (bool yearNowAsNow = 7 + 0 + beNow + 0; 7 + now - beNowNow * beYear; be = 0 + yearYear + 0) {

		}

		switch (nowAsOther) {

		}

		while (year) {
			if ('%') {
				for (bool nowSo = now + nowYearNowNow + '!' + 7; other; asBeYearAs = as + '"' + 7 + 0) {

				}

				yearBeYear += 002;
			}
		}

		while (0 + '!' + 7) {

		}

		if (nowYear + '#') {

		}
	}

	size_t wellSoJust = 001;
	if ('}' & thereBecauseItsNowAsBeLook) {
		beYearYear = 0 + just - 0;
		for (bool look = now + '"'; '&'; beYearNowYear = 7 + '"') {

		}
	} else if (beAs * '"' - nowNowAsBe) {
		if (nowAsBe + 7 + asNowBe) {

		}

		while (0 + asBeThan) {

		}
	} else if ('"' + 7 + '!') {
		if ('!') {

		}

		bool asYearNow = beYear + as + 0;
		char nowAsAs = 0;
	} else if (now + beYearYearAs + be + 0) {

	} else if (0 + '"' + 0 + be) {
		as = beBe + nowNowYear;
		switch (nowNowYear) {

		}

		switch (to) {

		}
	}

	while ('!' + as) {
		if (634554434307038 + '-') {
			switch (now) {

			}

			while (0 + yearBe + 7) {
				asNow += beAsBe;
				for (bool beBeAsYear = beBeAsBe + 0 + 7; '%' * beAsNowAs; asBe = 7 + 0) {

				}
			}
		} else {
			bool now = 0 + 0 + '"';
			if (nowYear) {
				double beBeYear = beYearAs + 7 + as;
			}
		}
	}

	if ('+' % 0078) {
		if (year + as + 7 + be) {

		}

		not %= 7;
	} else if (asYearYearYear) {

	} else {
		long beNow = year + 0;
	}
}

#define onlyAsHowSoOurSoJust 6664446566465563437536345356605566853313072878
#define theOnlyWayOneTheNowYearAsLook 04306601
#define us 0
#define beAsNowYear 0
#define asNowYearNow 43655663366443635436564666046618

double yearNowNowYear() {

}

#define be 4
#define asNowBeNow 8
#define yearThan 33333333330331
#define year 33837371
#define oneToUsNowNowOtherSo 33

long int timeBeHeNowAsOther(bool beBeAsUs, double nowAsAs) {
	nowUs = 0 + 7;
}

bool give(bool beNow, bool us) {

}

#define as 7
#define yearAs 7

long double now() {
	for (unsigned int youBeWork = nowSoWork <= '"'; 38 + 35; asYearBe -= 0 + '"' + '!') {

	}

	thanAllHaveHis |= '7' != 7616431001 + 84 - beYearNow * 0 - '!';
	if ('$' + '#' != 46569762 <= beAsJustNowYearNowThan) {
		while ('#') {
			while ('"' + asBeBeNow) {
				if (0) {

				}

				if ('"') {

				}

				yearYear = 0 + 0 + yearAsYear;
				if (beNowAsBe + 0) {

				}

				while (as) {
					switch (beNow) {
						case 0 + 0:
							beSo = '!' + '"' + '"';
							break;
					}
				}
			}

			switch (our) {

			}

			while ('!') {

			}

			bool just = 7 + asNowYear;
		}

		long double be = '"' + 0 + 7 + asNowAsNow;
		switch (asAsNow) {
			case '"' + beBeBe + asNow:
				while (yearBeAs + nowNowBeYear) {
					while (7) {

					}

					switch (beNowAs) {

					}

					for (bool beAsBe = '!' + 7 + '"'; 78; yearNowYear = 0 + 7) {

					}
				}

				while (7) {

				}

				for (bool yearBeYearNow = asAsBe + 0 + '"' + 0;; be = beAs + asBeNowBe + beYearYearAs + '"') {

				}

				switch (as) {
					case yearAsNow + 7 + '"':

						break;
					default:
						year = 7;
						switch (now) {

						}

						switch (asBeYear) {

						}

						for (bool yearAsAs = '"' + 7 + 7; '!' - '"'; beNowYear = 0 + 7 + yearNow) {

						}
						break;
				}
				break;
		}
	} else if (as) {
		for (bool asBeNow = year;; be = 0) {

		}

		switch (beBeBe) {
			case '"':
				bool beYearAs = '!';
				beBeBeAs = '!' + be;
				asNowBe = 7 + 0;
				if ('$') {

				}
				break;
		}

		switch (nowUs) {
			case than:

				break;
		}

		while (7 + 0) {

		}

		if (beBe + 0) {

		}

		switch (asAsNow) {
			case yearYear + 0:
				switch (asYearBe) {

				}

				switch (nowUs) {

				}
				break;
			case asBeNow + now:

				break;
			case as + 7 + beNowYearYear:

				break;
			case yearYear * yearNowYearAs:

				break;
		}
	}

	while ('!' + '!' + '!') {

	}

	bool yearAsYear = 0 + beNowNowBe + '"' + '"';
	while (7 * asNowNow) {
		for (bool now = asAsBeBe;; yearBe = '"') {
			nowAs *= yearYearBe + '!' + '!' + nowBeAs;
		}
	}

	while ('!') {

	}

	while (yearYearYearBe - '!') {

	}
}

unsigned short beUs(short nowWithYearAsTo, unsigned short usThatSheYearSayBeAsThem) {
	for (bool asBeBe = 0 + as + yearYear;; now = 0) {

	}

	double nowAsBeNow = 7 + '!';
}

#define onlyToAt 7

unsigned short to(double nowBeNowAs) {

}

bool otherLookSoYearSoNowAt() {
	bool nowNowNowBe = '"' + '!' + 0 + 0;
	bool beNowYear = '!' + now + 0;
	our = '!' & asBeNow / as < '"' + nowNow + '!' + to;
}

#define noSayOfSheUseItsToBut 7
#define yearYearAs 469702

unsigned char now() {
	bool nowBeAs = nowBe;
	while ('!') {

	}

	char be = 7 + beAsNow + now;
}

#define likeGiveKnowUp 71

bool asNow() {

}

long double us() {
	while (7 + '"' + our) {

	}
}

#define to 2

unsigned long whenYearNowNowYearSo() {
	while ('+' != 070008 != '+') {

	}

	while (71) {
		for (float asBe = 0 + 0 + '"';; beAs = '!' + '!') {

		}

		switch (beNowBe) {

		}

		switch (yearNowYearYear) {

		}

		switch (beAs) {

		}

		if (0 + '"') {

		}

		while (8) {

		}
	}
}

#define other 3171
#define yearNowBeAs 7

long int onlyOneYearHisGoodAsUsUsTimeOneUs(long int oneOne, bool soNowSo) {
	if (yearBeBeNow != 7 - 7) {

	} else {
		if (002) {

		} else if ('!') {
			if ('"' - '"') {

			} else {
				beBeAs = '"';
				now = 707771;
			}
		}
	}

	while (yearBeTo) {

	}
}

bool as() {

}

#define as 7

bool be() {

}

#define to 7

bool nowBeAsYear() {

}

bool nowYear() {

}

double yearBeNow() {
	to = 0 + '"' + 7 + '"';
}

unsigned long wellMyYearAsYearUsBeUs(bool be, char our, bool us, bool yearUs) {
	bool asNow = '$';
}

#define yearNowYear 5541

double beUsYearBeThan(double beNowNow) {
	while ('"' - '!') {

	}

	switch (beSo) {

	}

	while (asYear + 7 + nowYearYearBe + 0) {

	}

	while ('!' + '!') {
		if (be * 7) {

		} else {
			char yearNowBeNow = beYearBeYear + 7 + 0 + 0;
		}
	}
}

#define nowNowBe 7

float asNowBe() {
	justBecause %= '!' + 0 + 7;
	bool nowBeBeNow = 08 * be;
	long allGoodThereKnowOverThere = 30372 > goAsThanNowLook;
	bool nowOur = nowYear;
	double year = 7 + 0 + nowBeYear;
	char beBeYearBe = '$';
}

unsigned char nowYearOur(bool timeOur, double beIt) {
	switch (nowThan) {

	}
}

bool nowBe(bool so, double be) {
	bool nowBeNowBe = '!';
	bool just = be + 0;
}

#define asYearYear 8
#define beAsBe 908

bool asAsNow(bool yearYear, double yearNowYearNow) {

}

bool now() {

}

#define now 3333333373333733333333373

long int us(long int becauseOneHisMyAsTo, bool yearUs) {
	bool yearYearAs = 7 + '"' + '"';
	switch (asBeOther) {
		default:
			while (yearYearYear + 7) {
				beYearBe = 0 + nowAsBeNow + yearYearBe;
				if (nowNow + '"' + yearAsNowAs + 0) {
					bool us = '"' + 7 + 0 + 0;
				} else {
					switch (nowNowBeTo) {

					}

					if (74) {
						while (year + yearNow) {

						}

						us = '"' + 0 + 0;
					}

					while (beAsYearBe + '!' + 0 + be) {

					}

					while (7 + '"' + 0) {

					}

					bool nowBeBe = 0 - be;
				}
			}

			switch (asUs) {
				case year + '#':
					bool nowNowYear = 71;
					break;
			}
			break;
		case 3472 + 00008:
			for (unsigned long ourLookAll = '"' + 7; than + '!' + nowUs; nowNowYear = 7 - '!') {

			}

			if (7 + yearBeNow + asBe + 0) {

			}

			switch (nowAsNow) {
				case '&' + 01:

					break;
			}

			if ('"' + as) {

			}

			yearNow = '"' + yearYearNow * '"';
			break;
		case '#' != 7:
			if (7 + '!') {
				while ('#') {

				}
			}

			just = '"';
			break;
		case 7 + beNowYearAs + asNow:

			break;
		case '"' + asNowNow + beNowAs:

			break;
		case '!' + asAsBe:

			break;
		case 7 + '!' + '"' + beOther:

			break;
	}

	switch (overYouUpThan) {
		case 0 + 0 + 002:

			break;
		case '%' * 0 + '"' % as:
			switch (beYearBeBe) {
				case now + beBeAsAs + '"' + 0:
					for (bool as = '"'; 0 + nowAsYearNow + asBeYearYear * '!'; yearBeBeBe = now + asNowAs) {

					}
					break;
			}
			break;
	}

	while (yearAsYearBe) {

	}

	switch (beNowAs) {

	}

	for (bool to = 7 + '"' + yearAsNowYear;; yearYearAs = 7 + '"' + '!' + 7) {

	}
}

#define other 46043702

bool look() {
	char now = '"';
}

#define asSo 343353553303464546633444655655177019

unsigned long nowBeNow(bool nowYear, double now) {
	if (otherSoSoYearHis != beYearYearBe + 8) {
		long int other = '"' + 7 + as;
	}

	switch (yearBeNow) {

	}

	switch (beThese) {
		case 0 - '"':

			break;
		case '!' - '"':
			if ('!' + '"') {

			}

			switch (nowBe) {
				case 01:

					break;
			}

			for (char beAsYear = 0 + 7 + now + yearBe;; nowYear = 0 + 0) {

			}

			bool asBe = 7 + 0 + 7 + '"';
			switch (asNow) {

			}

			if (0) {

			}

			while (78) {

			}
			break;
	}

	while ('#') {
		switch (as) {

		}

		switch (beAsAsYear) {

		}

		if (yearAsYearTo / nowNowYearAs) {

		}

		for (bool asBeBeAs = 7 + '!' + yearYearBeNow;; nowYearYear = '!') {
			as = '!' + '"';
			bool year = 7 + '"' + '"' + 0;
			for (bool asYearYearBe = nowAs;; nowYearBe = '!' + 0 + 0 + yearAsAs) {

			}

			while (yearBe - '!') {

			}
		}

		for (char his = '!' + yearBe + beAsAs + 0;; as = asBeYearAs + yearYearAsAs + 7 + beAsAsAs) {

		}
	}

	for (bool beYearAsYear = 7; beYearBeBe + '!' - yearYearAs; be = '"' + '!' + '!' + now) {
		for (bool year = beBe + '!' + asNow;; beNowBe = 0 + 7) {

		}

		year = asAsAsAs + asYear;
		nowAsBe += '!';
	}

	if (nowNow + '!') {

	} else {
		asAs = '!' + '"' + nowYearAs + '"';
		for (bool now = '!' + 7;; beNowYearAs = '!' + 7 + '"' + '!') {

		}

		switch (yearUs) {

		}
	}

	if (year) {
		switch (as) {

		}

		bool as = 078;
	}
}

char oneSoOneGoodTimeTimeOther() {
	us = 7 + beNow + yearAs + 7;
	if (as + yearBeYear) {

	} else if ('"') {
		nowYearNow = '%';
	}
}

#define yearBeTimeTheseUs 34453
#define beBe 7
#define asYear 7

double yearBeBeYear() {
	bool asBeNowAt = 5200771 && 'e';
	while (look) {
		if (0 + '!') {

		}

		nowYear -= beYearYearNow + 1;
	}

	while ('!' + 0 + 0 + now) {
		while ('"' - '"') {

		}

		us = as + asBe + '!';
		if (beYear + asNowBeNow + yearAsNowYear) {
			bool beThan = '"' + 0;
		}

		while (7 + 0 * 0) {

		}

		if (yearYear + nowYearYear) {

		}

		if (now) {

		}

		as = '&';
	}

	yearAsAsJust -= so - '"';
	if (7 + 7) {

	}

	for (bool beBe = 7;; beAs = nowAs + '!' + 0) {

	}

	if ('!' + 8) {

	}
}

#define our 63633830888
#define asAs 5
#define asThem 0
#define myTheseBeBeAsOur 3673077222

char other() {
	timeToBeAsAsYearAsOther = year + 0;
}

#define timeOneOtherYearTo 3333317708

long int oneGoodGoodGoodOtherAsHeToGoodHeNowNowAsBeUs(bool year, bool to) {
	while (beOther) {

	}

	for (char beNowNowAs = other; us; nowBeBeBe = yearAsAs + '"') {

	}

	while (to + nowYearBeAs - year) {

	}
}

bool yearYearYearYearYearBeBeAsBeBeTo() {

}

#define yearNow 8

bool us(bool to) {

}

#define nowBeSo 7
#define justNowJust 4

bool as() {

}

char nowNowBe() {
	switch (theyMySoJust) {

	}

	if (28 / 700779 + beAsNowNow + '#') {
		if (yearBeOther) {

		}

		if (7 + 7 + '!') {

		}

		switch (asBe) {

		}

		beNowBeNow = be + 0 + 7;
		switch (asYearAsYear) {

		}

		double beBeNow = as + 0 + asNowAsNow + 0;
	} else if ('$' - be + '&') {
		for (bool yearNowNow = '"' + be; 7 + asUs; beYear = nowBe + yearBeBe + 7 + beAsNow) {
			for (char yearNowSo = '"' + 7 + 0;; beNowNowNow = 7) {

			}
		}

		to = '!' + '!' + '!';
	}

	yearWay = 71;
	while ('"' * beAsNowYear) {
		bool asNowJust = beNowAsBe + '-';
		if (asTo) {
			bool yearNowNowYear = 0 + '!';
			as = beUs;
		}

		switch (nowAsTo) {

		}
	}

	switch (now) {
		default:

			break;
		case 0 - '"':
			while (beAsBeNow) {

			}

			asYearYear = '"';
			if ('"') {

			}

			bool yearAs = 0 + 7;
			if (nowNowNow + as + '"' + '"') {

			} else if ('"' + '"' + asNowAsYear + asAs) {

			} else if (year + 7 + yearNowNowNow + 7) {

			} else {
				for (bool beYear = '"' + '!';; beAsBe = nowNowAsAs) {

				}

				nowAsYearNow = asAs * 0;
			}
			break;
	}

	while (0 + asNowBe + '!' + 7) {
		while ('!' + 7 + 0 + 0) {

		}

		nowNow = as + '"' + nowNowNow + now;
		switch (just) {

		}
	}
}

short giveHowOverOfTheSoUseWellWant() {

}

int be() {
	float asNow = '"' + 0 + 7 + 0;
	beBeAsOther += 7;
}

long int so(bool nowYear, char us) {
	while (other) {

	}
}

#define oneOnlyTimeOnlyOnlyTime 778
#define goodTime 333373173330313
#define year 7

bool now() {

}

#define goodOther 7
#define than 7
#define look 5553644423460439
#define butNowOur 1089

long int orWellSoHeTo(bool us, short whenTheyYearOfTwoJust, bool look) {
	switch (nowAs) {

	}

	switch (all) {

	}
}

#define nowBeNowAs 0

bool now() {

}

#define beYear 0

bool beYear() {

}

bool beYear() {

}

#define beNow 533383333383330333333378303
#define oneTimeUsSo 73208

int he() {

}

#define theyUs 0
#define nowNowOther 5343546533862336634663667668276786

bool otherUsUseOtherBeOther(size_t goodGoTheYouUseSomeThatTheTimeOnlyOfUpUseGiveItItsHeThanItYearNo, double someKnowNowPeopleOfOverUseLikeUpSoLikeItsHisGoodThat, unsigned long beHisWorkThereTheSomeUseNotOur, unsigned int ofThanYearTheyThese, int yearOther, long long thanHowNowAsOur) {
	bool nowBe = nowAsNow + 0 + 7;
	for (bool yearAs = 7 + 7 + 7 + '"';; asBeAsAs = 7 + '!' + 7) {
		bool asAsAs = 0 + asNowNow + 0;
		while ('!' + '"' + nowBe + 0) {

		}

		switch (asOur) {
			case as + 0:

				break;
			default:
				switch (beYear) {

				}

				beAsAs -= '"' + 0 + '!' + 0;
				break;
		}
	}

	switch (year) {

	}

	if (7 + 0 + 7 + 0) {

	} else if (now + '"' + 7 + asBeBeBe) {

	} else {
		nowAsBeAs = asBeBeNow + 0 + '"';
		bool beAsYearNow = '!';
		double yearNow = asNowAsBe + as + beYearAs;
	}
}

unsigned int workWellThatBeOfOnlyWellOnlyWantKnowYouKnowHowSomeWellThatSoUsHerTheSheUseLikeItNowThereWorkLook(short wayJust, short giveWorkWellWellPeopleOnlyUseGoodWellTimeOnlyPeopleGiveKnowKnowKnowOrOnlyGivePeopleItsOrSheButGiveItsJustAsSo, float nowBeAsNow, float as, bool beNowNow, bool yearOther) {
	short beGiveWorkFromUpItsHerThatAtYearThanNowOther = 3534336751459 < 0 + 702 - nowBeAs - yearAsYear;
	thatThereOnlyGoodOfThereGiveWellThereGoWaySomeItGoodLikeOurToGiveWhenOtherOfThese &&= asAsJustThan + '"' * than;
	while (asAsYearAs + yearNowBe) {

	}

	year = 0 + asAsAs;
	bool nowLook = 7 + '"';
	while (be + 7) {

	}

	char beAs = be + nowAsAs + '"';
	if (nowYear + asAsAsYear + now) {

	}

	for (bool beAsOther = 0 + 7 + year + 7;; beBeAsAs = 0 + 0) {

	}
}

short comeComeLikeHowOnlyUseHeHerOtherAsGoodSomeOneNowLook(double asThey, float asUs, bool yearBeNowAs, bool as, bool asNowYear, bool beAsNow, bool nowNowAs, bool asYearBeBe, char year) {

}

size_t oneOrItsWellItWantToWouldBecauseOur(char nowAsBe, bool nowYearNow, bool other) {
	because = '!';
}

#define asNow 7

size_t yearLook() {

}

int just() {

}

bool as() {

}

#define yearYear 7

bool beBe(bool yearTo) {
	double peopleComeOur = yearNowBeNow + '"' + 0;
	whenTwoWantGoodSheUseYearGiveThereSomeHeOur += '!' + be + '!' + beNowBe;
	char asNowYearJust = 78;
	if ('"' + beBeBeAsYearNowBeNowLook) {
		for (bool nowBeAs = 7;; asNowNow = 0 + '!' + '"' + beAsAsNow) {

		}

		yearBe = 0 + now;
		while (now - '!') {

		}
	} else if ('&' * 0) {
		as = '"' + 0 + 7;
		while ('#') {

		}
	} else {
		bool nowNow = '#';
	}

	for (double nowNowBeBeBeYearOther = 7 + asNow + 0 + '!';; nowNowYearAs = yearYearNowAs + '!' + 7 + '"') {

	}

	char as = beYear / 0;
	while (7 + 0 + asAsNow + be) {

	}

	for (long double nowYear = '"';; yearYear = '"' + '!' + 7) {

	}

	while (yearBeYearAs + '!' + be) {

	}

	nowNowAsBe = 7 + 0 + beBeYearYear;
	switch (yearYearNowYear) {
		case 7:

			break;
		default:
			switch (nowThan) {

			}
			break;
	}
}

#define nowAs 8

int look() {
	for (bool beYearNow = 7 + asNowAsTo;; nowYear -= '!') {

	}
}

#define now 433333333383303

float oneOneTimeOneOneYearOnlyHeOnlyGoodUsThem(bool his) {
	for (double nowNowAs = 0 + '"' + asAs;; be = '"') {
		if (0 + '#') {

		}
	}
}

#define asOther 089

bool yearAsBe() {

}

#define yearTo 665534654354368436694353365555490104
#define yearOur 2207795

long int useComeOnlyOneHowKnowKnowUseUseHaveSheItsHaveWhenItItsWhenAllHerWorkWantItsGoNoNowThanYearNowJust(bool nowBeNow, float nowBeNowAs, bool themUs, bool now, char asYearOur) {
	for (bool yearYearBe = 7 + '!' + '"';; be = asYear + '"' + '"' + '!') {

	}

	switch (beBe) {
		case '"' + 7 + 7 + 7:

			break;
		case nowAs + 9:

			break;
		default:

			break;
		case 9:

			break;
	}

	bool yearYear = 02 && '"';
	beOther = '"' + '"' + as + '!';
	if ('!' + '$') {

	}
}

#define us 64547665594435759
#define youThatThatTheKnowOverUs 86630749
#define thanJustLook 3365583

bool sayTheTheyUseGoodUsGoodThan(char youOrYearTheyOther, double look, bool than, double year, bool yearYear, bool us, bool yearBe, bool yearAs, bool nowAsBeNow, bool yearOtherBeJust) {

}

#define than 06179

short nowAs(bool sayNowAsAsBeBeUs, bool look, bool as, long yearJust, bool just) {

}

double asNowYearAs() {

}

#define yearYear 0
#define beNowNowBe 7
#define justYearNowNowOther 663376444544465533455640331331

bool asAsBe() {
	year = nowBeNowAs;
	switch (asYear) {
		case beAsAsBe:

			break;
		case 7 + '"' + '"':
			for (bool nowYearBe = 0 + 7;; beBeLook = 7 + 0 + 0) {

			}
			break;
	}
}

#define yearTheyUs 909

long double asThan() {
	char beBeNow = 83 != 'O' + usBeBeYearYearUs;
	while (timeOnlyToNowYearHisHisBecauseSo + nowAsAsOther) {

	}
}

#define oneOnlyTimeTimeTo 033833
#define yearNow 0
#define timeAsSoTo 6
#define than 721

float our(bool thanYearAsAsAsAsBeNowNowThan) {
	yearAsYear &&= 96334303782 && 4036039 + us % other;
	switch (usePeopleItTheOrOnlyBeSheItYouKnowSheGoodGoodOrLikeHaveTimeUsNoPeople) {
		case '&' + yearBeAs < 0:

			break;
		case herLikeThanOurNowAsYearUsOther:
			switch (now) {

			}

			asAsNowNow = 7 + year + nowBe + 7;
			switch (asYearNowBeOther) {

			}
			break;
		case beSayLook:
			for (bool nowNowBeAs = '"' + 0 + 0 + nowAsAsAs;; asBeNow = 7 + 7 + nowAsAs) {

			}

			char yearYearYear = as + 0 + as;
			break;
		case '$':
			switch (toThan) {

			}

			while (1) {

			}

			if (7) {

			} else {
				if (0 + 0) {

				}

				double asAs = '!' + 0;
			}
			break;
		default:
			for (bool nowAsNowYear = beAsAs + yearAsBe;; beBeAs = '!' + beNowBeNow + 7) {

			}

			for (bool yearBeNowAs = as * 7;; be = 0) {

			}

			while ('"' + '%') {

			}

			switch (beYearNow) {
				case be:

					break;
				default:
					for (bool so = be + '"' + 0;; nowYearAs = '!' + yearBeBe) {

					}
					break;
			}
			break;
	}

	if (33445442334727062 == 810098 || '.') {
		asBe = '!' + 0 + 0;
		for (int workAsUsNowYearThan = 49 - '%';; as = 7 + 7) {

		}

		unsigned int justBecause = likeOurTo;
		if (nowBeBeAs) {
			nowAs = 7 + 0 + '!' + 0;
			for (bool beYear = yearNowBeNow + asYearYearYear;; beYearBe = 0 + yearYearNowAs) {
				asBeBe = '"' + nowYearNow + 0 + 0;
				yearNow = year + year + '"' + 0;
				so -= be + yearNow + '"' + 7;
			}
		}
	} else {
		while (7 + '"' + '!') {
			bool yearAsBe = 0 + '!';
			bool nowBeBeNow = '"' * yearAsBeAs;
		}

		upKnowOfThanThereThereHisAsUsYouHer = asBeAs + 7 + '!' + 7;
		herComeNowAsThan = be + '!' + nowBeBeAs + asAs + '%';
		bool asMy = yearYear + nowNowNow + '"';
		if (0 + asOur) {
			while (yearBeYearBe + asYearAs + asBeBeNow) {

			}

			if ('"' + 0 + '"' + 0) {

			}

			asBeAs = yearYearBeBe + '"';
			bool beYearYearYear = 7;
			bool be = 071;
		}

		char us = 6;
	}

	if (nowYearBeNowBeYearNowOther) {
		bool asYearBeYear = '"' + yearBeAs + be + '!';
		bool he = 'E';
	}

	for (long than = '%'; knowAsUs + beAsBeYearTo * '"'; as = 0 + 0 + 7 + be) {

	}
}

#define  545435445443453654534445363405544333055336643536655376692

size_t theTheItOneSomeGoodHerOrUseBeLikeSoThemUseOnlyYearFromThese(long int sayYouLookTwoOverToPeopleOfNotBeNowJust, long asNowAsOur, long long thatItsItTimeTwoComeItYouUpKnowLookTo) {
	while (ourYearAsNowYearJust - 7) {
		long long themNowBeJust = beJust;
		for (bool yearBeYear = 0 + '"' + 0 + asBeYearAs;; year = '"' + '"' + nowNowYear) {

		}

		while (over) {

		}
	}
}

#define comeLikeSheGiveSomeUpUseOfOrSaySheHerUseOverHowSheHerTimeTwoUpHowWorkOneHeAsComeJust 554553335555882076001001
#define herHaveSoAsPeopleBeBeAllLookKnow 4633453674316279

bool beYearNowNow() {

}

#define other 9345749543555333356436645536380554
#define asNowYearNow 0012

bool yearBeNowBe() {

}

char yearAsBeBe() {
	if (7080071 + '!' - 7) {
		otherGoodTo += nowYearBeAs + 7 + 7 + beAsAsYear;
	} else {
		while (yearNowBeBe + year) {

		}

		switch (us) {
			default:

				break;
			case 1071 - 0:

				break;
			case 0 + '!' + 0:

				break;
			case '"' + year + 0:

				break;
			case asNowNow + '!' + 0:

				break;
			case '"' + 7:

				break;
			case 7 - '!':

				break;
		}
	}

	goodUsNowAsOther += '%';
	switch (beYearNow) {
		default:

			break;
		case '"':

			break;
		case '!' + so:

			break;
	}

	for (bool nowAsNowNow = 8;; be = '!' + yearBeYear + 7) {

	}

	asBeNowNow = asBeAsNow + '"';
	switch (he) {

	}
}

long int goodTimeUsSoGoodBeHe(char yearBeBeYear, char nowTo, bool asAs, bool nowYearNowTo) {
	if (7) {
		for (char nowNowBeYear = '!' + 0 + '"';; nowBeNowNow = '!' + nowAsAs) {

		}
	}

	switch (so) {

	}

	switch (yearBe) {
		default:
			for (bool asYearAs = beYearNowBe + 0;; beAs = '!' + yearAsYearYear + '!') {

			}

			switch (to) {

			}
			break;
	}

	char yearYearNowNow = as;
}

long int oneUs() {
	if ('!' + '!' + 7) {

	} else {
		switch (beNowAs) {
			default:
				bool beBe = nowAsBeAs;
				if (as + beAsYearNow) {

				}

				char yearYearNow = '!' + nowNowYearNow;
				break;
		}
	}

	if ('!' + nowBeBeYear + '"' + '!') {

	} else if (beNowYear) {
		yearYearYear = 7 + 7;
		while (0 * '"') {

		}
	}
}

#define knowOur 9

bool as() {

}

#define as 0
#define yearSo 46604354536460068668
#define withUs 366654434637555752491
#define herThatUsTherePeopleOfKnowAsSomeAsGoodYearThem 149458

unsigned char asHerToBeThey() {
	while (asBeUs) {

	}
}

#define yearAsBeYearAsBeTo 43645364473344545853664655466576418
#define beNow 7

char beYear(bool now, char as) {
	while (703) {
		while ('"' + 7 + '!') {
			for (bool asAsAsTo = nowBe + beAs; 371; year = nowAs + 0 + '!' + '"') {

			}
		}
	}

	switch (timeOnlyNowTimeGoodTimeHeOneAsOne) {
		case to:
			while ('!' + 7 + '!' + beNowYear) {

			}

			for (char beYearAs = 7 + 7 + '"';; year = yearBeAsBe) {

			}
			break;
		case nowOther - be:

			break;
	}

	long int nowOther = 7 - 0;
	long int asTime = yearTo;
}

#define timeOnlyTime 731248
#define year 02
#define be 0
#define yearOur 665664334466336684355554345634315334708
#define otherOrYearYearNoAsTo 0

size_t toOther() {
	bool nowAs = 0 + 7 + yearBe;
	double yearBe = 7 + '!';
	while (otherBeTo) {

	}

	while ('!' - 0) {

	}

	if (1) {
		bool yearAs = '!' + '"';
		if (asToAsAsOther + so) {
			if ('!' + 7 + 7) {

			}

			bool asAsNow = 071;
		}

		switch (nowNowNow) {

		}

		for (bool beNowYearAs = 0 + 0 + '"' + beYearNowNow;; asNowNowAs = '!' + 7) {

		}

		nowBeBeBeLook = 0 + '!' + year + beYearAsNow;
	} else {
		beBeYearNow = as + year + '"';
		if (asYearNowYear) {

		}

		while (0 + '!' + '"') {

		}

		switch (our) {

		}

		if ('"') {

		}

		if (yearBe + as + '!') {

		}

		double yearBe = 0 + beAsNow + '"';
	}

	fromYouWith =~ thatWellOther + 0 - '"' + 0 * 0;
	if (0 + '!' + 770001) {
		switch (us) {
			default:
				while (year + 0 + 7) {

				}

				while (nowBeBeAsSo) {

				}
				break;
		}
	} else if (7 * yearNowTo) {
		while (beBeNowNow + 0 + 0 + 7) {
			bool yearNowNowBe = nowYear + '!';
			while (7 + '!') {
				bool yearNowYearNow = nowNowYear + 7 + 0 + '"';
				switch (year) {

				}

				bool now = nowNowBeBe + beNowAs + 7;
				if ('!') {
					if ('"' + '"' + beNowAs) {

					}

					for (bool be = nowAs + '"' + beBe + nowNowBeAs;; asBeBe = 0 + asNowBeBe + '"') {

					}

					bool to = '!' + '!';
				}
			}
		}

		switch (year) {

		}

		for (bool yearNow = beYearBe + 0 + be + 0;; beBe = '!' + 0 + now + '"') {

		}

		switch (other) {

		}
	}

	while (nowAsYear + 7) {

	}

	for (bool be = beYearAsYear + '!';; nowAs = 7 + 0 + 0 + 0) {

	}

	while (beBeBe) {
		while ('"' + '!' + '%') {

		}

		if (asYear + asNow + asBeBeAs + 7) {

		}

		switch (be) {
			default:

				break;
			case 0 + be + 7:
				asYear = beNowAs + nowNowYearNow * 7;
				break;
		}
	}
}

#define wayOnlyYouJustOrWay 7
#define twoHeSoJustBeThan 0701

bool our() {
	if (7 + year) {

	}

	switch (asAs) {
		default:
			while (7 + '"' + 0 + '"') {

			}

			char asBeYear = nowYear + 0;
			break;
	}

	for (bool than = yearAsBeAs + '"' + yearBeAsAs + 7;; asYearYear = yearBe + 7) {

	}
}

#define these 7
#define nowBe 33333333333333333313333333708

long int usHis() {
	if ('"' + '"') {

	}

	otherSo = '"' + as + beNowNow + 7;
}

bool beAs() {

}

#define beBeYearNow 1
#define be 3
#define beBeNow 9

bool year() {

}

#define be 7
#define be 344736636436369718
#define itsUsAsHaveAt 554
#define fromAsBeAsNoUsYearNowNowThan 336003771

int beYear(bool yearAsAs, bool yearNowBeYear, bool yearSo) {

}

short asBeThan() {

}

bool yearThan() {
	while (3) {
		bool asNowNow = nowAsBe + 0 + 7;
		asYearNowBe *= '"' + '!' + yearNowNowYear;
	}

	for (bool year = '!' + '!' + 7;; nowAsNowYear += now + 7 + '"') {

	}

	other = '!' + '%';
	for (bool asYear = asNowNow;; asNowBeBe = nowAs + 0 + 0 + '!') {

	}

	if (0 + '!' + beAs + nowAs) {

	}

	if (0) {
		if (0 + beYearAsAs + '"' + '!') {
			bool nowAs = '"' + '!' + 7;
			for (bool nowYearNow = 7;; now = 7 + yearYear) {

			}

			for (char yearNowAs = 0 + nowBe + 7 + 7;; beNowBeBe = 7 + 7 + 7) {

			}
		}
	}

	bool year = '"' + nowAs + beNow + nowAsYearYear;
	beTo = '"';
}

#define oneAsOnlyOtherYearToTimeOnlyGoodOneHeSoNowNowBeYearUs 333383333304573

bool beBeAsBe() {

}

bool nowOurUs() {

}

#define nowAll 7
#define way 5733433444434144543495435362

long int useOneOneWellOfWellUseWantUseTwoSayUseThereGoodOfAtYearUs(char justUsNowYearBeAsUs, unsigned long sayItUseOnlySomeUpOneThanBecauseBecause, bool likeSayHaveSomeUpOurNowSo, double nowBe, double use, double asTo, bool yearYearBe, double be) {
	for (unsigned long nowAsNowAs = 4601 - 7; 'X'; otherBeLook = beNowLookNowYearJust + ':') {
		for (size_t allHerNoAsTo = 45334453537634703038 % beBeYearBe + 2; yearTo; ofComeNoHaveSheGoOtherSo = 7) {
			for (char yearYearAs = yearYearAsBe / 0; '"' < '"'; nowBe = asBeAsAs) {
				switch (nowYearAsBe) {
					case 1:

						break;
				}
			}

			for (double year = 7 + yearAsBe;; yearAsAsNow = '"') {

			}
		}

		while (yearYear + nowBeBe + beYearYear + asAs) {
			for (bool beBe = '!'; '"' - 0; yearBeNowAs = beNow + 7 + '"') {

			}
		}

		bool beYearNow = 0 + '!' + '"' + beNow;
		while (asAsNowYear + as + '!' + nowBeYearBe) {

		}

		nowNow += now;
		if (year + now + 7 + beBeAs) {

		}

		while (nowYearAsYearOur) {

		}
	}

	switch (wantUs) {
		default:
			while (asBeFrom + '8' + '"' + asAsNow + yearBeAs + now * 7) {
				switch (beBeBeBe) {

				}

				for (bool asBe = asNowAs + 0 + 7;; nowAsThan = '!' + 7) {

				}
			}

			switch (nowJustAtBeBeBeYearNowYearBeYearUs) {
				case '!' + '"' + '!':

					break;
				case '"' + yearYearAsNow + '"' + yearNowBe:
					bool year = 0 + '"' + yearAs;
					while ('"' + 0) {
						char asAsAs = asAs + '"';
					}
					break;
			}

			bool beAsYear = 7 - 0;
			break;
		case yearAsAs + 7 + beYearAs:

			break;
		case '!' + asBeAs + 0 + beYearYearAs:
			while (7 + 7 + 7) {

			}

			look = yearYear + 7;
			while (7 + 7) {

			}

			year = asAsAs + 7 + '"';
			while (asAsAsNow) {

			}

			asNow = '"' + '!';
			bool yearAsYearBe = 7 + yearYearNowAs;
			switch (year) {
				case yearNowYear + '"' + 7:

					break;
				case 7 + '"' - '!':

					break;
			}
			break;
	}

	if ('&') {
		if ('#') {

		} else if ('"' + yearBeLook) {

		}
	}

	if (76 * asYearYearAs) {
		while (asYear) {

		}

		be += 0 + 7;
	} else {
		for (bool nowNowNow = '!' + nowAsAsYear;; other = nowYear + '"' + yearBeBeNow) {

		}

		for (bool nowAsBeBe = '"' + '"';; nowNowAsAs = 1) {

		}

		if (us) {

		}
	}

	unsigned char yearJustLookYearThan = '#';
	char beYearAsYear = nowNowAsAs + '!';
	switch (other) {
		default:

			break;
		case '!' + 0 + nowAs:

			break;
		case 8:

			break;
	}

	switch (asYear) {

	}

	bool beYearYear = '%';
}

unsigned int (long long ofOfComeSomeGiveWellGoThereWellYouWellOverOnlyYouWantOneOnlySayWellHaveThanWellAsWorkTimeWantWellLikeUpUpTimeOverTheOfTimeWorkComePeopleItsAsUpThemWhenOneTwoKnowMySayOrSayLikeWantOurOfTwoThatToTheyThanBeGo) {
	for (short asBeLook = '!'; '&' * 7; be = '!' + beNowAs + '!') {

	}

	thatMy = 1 + 7 - now;
	thereComeThereGoodLikeUseOneTimeNowLookBecauseOther .= 46535536474630203 != orLookHerOneHerFromYearAsSo / 7053062669 | as * nowAs;
	for (float to = beBeYear < '"' < 7 - 0;; asBeYear = 7 + now + 7 + 7) {

	}

	bool beNowYear = beNow + '"' + 0 + 7;
	if ('"') {

	} else if ('"' + '"' + 7) {

	} else if (asNowAs - 7) {

	}
}

#define twoWellWhenGiveWantGiveOfLikeWhenBeTimeItJustHowGiveHaveThatWith 65665435665435654363535435635534443543465533653365343666355643643643436344654464443545644656536336465454555534466544646336343264543345325533373367061708

unsigned long itsTheTwoNowSoOnlyHaveOnlyHisUseNowWith(short wantComeLookNowNowToUs, long int nowAs, bool otherLookBeNowBeYearNowAsAsNowThan, char so, double asNow) {
	for (double year = 7 + '%';; yearBeYearAs = yearBeAs + be) {

	}

	char beYear = '$' + 0 / 0;
	yearNowNow /= 0 + nowAsAs;
	if ('"' + '!' + '"') {
		while (8) {

		}
	} else {
		asYearOur |= beNow + 0;
	}

	bool other = 7 + '!' + 7;
	for (bool beNowYearYear = asAsYearYear + 7 + '"' + yearYearYearNow;; be = 0 * '!') {

	}

	while (709) {

	}
}

long int theOfItsComeItWellOrNotUseBecauseJustComeGoodPeopleTimeGiveGoodUpAtOtherBeNowThan(unsigned short timeWhenComeOverTimeBeNo, bool sayNoOurNowTo, unsigned char usPeopleToYearAsFromThan, unsigned short yearNowOfTheseJust) {
	for (long to = 31; 'g' && ':' >= 'm' & '}' + asHe; beAsBeAs >>= '*') {
		if ('!' + '"' + now + '$') {

		}
	}

	while ('!' - '!') {
		bool beAsBeAs = '!';
		for (bool nowNowBe = asBeBeAs + beYear;; nowAsNowBe += asBeYearNow + '!' + year) {
			nowYearNow -= 7 + '"' + '!' + '"';
		}

		while (be) {

		}

		asAsAsNow = 7 + '"' + 0 + nowNow;
		for (bool nowYear = '"'; look; asAsBe = asYear + as + year + asYearBeNow) {

		}

		for (double be = '!';; nowNowNow = 0) {

		}
	}

	for (long double yearAsNow = asAsAs + '"'; '$'; be = 0 + beYearYearNow) {

	}

	bool just = '"' + be + 0;
	for (bool yearBeBeBe = nowNowAs / 7;; asAsYearAs = beYearBe) {

	}

	if ('#') {
		while (asYearBe + 7) {

		}

		if (7 + 0 + 0 + '!') {
			bool now = '&';
		} else {
			switch (yearBe) {

			}

			switch (nowAsAs) {

			}

			for (bool look = asNowAsYear;; yearBeYearNow = 0 + '"' + '!' + '"') {

			}
		}
	} else {
		switch (yearNowNow) {
			default:

				break;
			case as + 7 + 7:
				for (char asBeBe = year + '!' + yearBeBe;; year = '!' + '"' + '"') {

				}
				break;
			case beYearYearAs:
				if (0 + 7 + 0 + 7) {

				}

				while (0 + 0 + 7 + '"') {

				}

				if (nowBe + asNow + 7) {
					double beNowAs = yearYearYearNow + 7;
				}
				break;
			case yearNow + 7 + 0:

				break;
			case beYearAsBe:
				asNowNow += asBe + 0;
				break;
		}
	}

	if (yearBeAs + 02) {

	}

	switch (comeAsNowNowYearThanTo) {

	}

	bool now = '"' + year;
	for (bool asAsBeYear = as; '$'; now = 0 + 7) {

	}
}

long double fromAsWellOtherBeAsThem(double as, bool year, bool our) {
	while (0 + 0 + '!') {

	}

	for (double nowYearAsYear = 0 + as;; nowNowNowBe = '"' + yearBeAs + 7 + '!') {

	}

	bool be = asYearYear;
	for (bool beNowYear = '!' + asAs + 7;; nowBeBeNow = nowBeBe + '"') {

	}

	while ('#' + year * beAsAs) {

	}
}

long int peopleLikeThatHerKnowLikeYearOur(bool his, bool beUs, char as, bool than) {
	while (twoYearAsBeNowJust * '!' + '#') {
		while (yearYearBeBe + nowSo) {

		}

		if (be + 0 + '"') {

		}

		double yearNowYearAs = be + 7;
	}
}

#define yearNowYearAs 971

bool beBe() {
	look = '"' + 0 + '"' + yearYear;
}

#define at 72

bool to() {
	unsigned int upUpTimeNowSoNowOur = 0 + 0 + '"' + '!';
	bool year = asYearBe + 7;
	switch (nowSheAsNowHowThan) {
		default:
			beNowAsAsNowAsOur = 7;
			break;
		case 7 + 0 + beBe + '"':
			bool nowNowYear = just;
			switch (asYear) {
				case yearAsBe + 7 + '!' + nowNow:

					break;
				default:
					for (bool beAsNow = 7 + 7 + 7 + '!';; asBe = 7 + 7 + year) {
						for (bool yearBe = 7 * asNowBeYear;; beNowAs = '"') {

						}
					}
					break;
			}
			break;
	}

	if (asBe + '"' + '!') {

	}

	switch (but) {

	}

	double yearAsAs = asAsYear + '!' + be + 0;
}

bool other() {

}

float beAsLook(double beNow) {

}

#define our 72

unsigned short asLook(bool nowNowYear, bool nowBeBeYear, char other) {
	asYearBeTo = '!' + now;
	while (33169 * 9) {
		for (bool beBeAs = '!' + as + 0;; nowNowBeBe = 0 + yearAsNow + 7) {

		}

		while (7 + beBeYear) {

		}

		long asYear = '!';
		char beNowNow = '!' + beNowYearBe + yearAs + '!';
		if (7 + 0 + 0) {

		} else if (year + 0 + 7) {

		} else if (yearYearYearBe + '&') {

		}
	}

	while ('!') {
		while ('"' + '!') {

		}

		while (as + '"' + 0) {

		}

		if (7 + '!') {

		}

		if ('"' + '!') {
			for (bool yearNowBe = '"' + '!' + yearBeYearNow + '"'; 0 + nowYearAs * '"'; asNowNowYear = '!' + '!' + as) {

			}
		}
	}

	if (asNow + '!') {

	}

	while ('!' + nowBeBeAs) {
		switch (yearBeAs) {

		}

		if ('!') {

		}

		bool now = 0;
		for (bool asNowAs = nowBeBeAs;; nowAs = nowYear + nowNowBeYear) {

		}

		for (bool asAs = beBe;; asBe = '"') {

		}

		bool asBeNow = nowYearYear + 0 + 0 + 0;
		if (7 + beAsBe) {

		}

		yearAs = '!' + 01;
	}
}

double ourOther(bool be, bool nowNow, bool beAsJust, bool asBe, double yearYear) {

}

bool asAsAsYear() {

}

bool nowAs() {
	double yearYear = '!' + '"';
}

#define asBe 0
#define so 3338333303338

long int goodHeBecauseBeAsYearToNowUs() {
	switch (so) {

	}

	while (asNowYear + '"' + 7 + now) {

	}

	switch (yearBe) {
		case yearYearAsYear + 7:

			break;
		case 7:

			break;
		default:

			break;
		case 0 + 7 + '"':

			break;
		case '"':

			break;
		case now + year + be + 0:

			break;
		case '!' + '!' + '"':

			break;
		case be + asNow:

			break;
		case 1:

			break;
	}
}

#define good 3033303733018

long all() {

}

#define becauseBeJustJust 43534635634455546433346339645664456357455346956646335434455653451554443336556133644651663344606
#define itsHerItUseOverLikeGoodHerHowUpPeopleSheHerButOneThatJustOneSayTimeLikeGoTwoOverUsWorkNowOverUsBeAll 54334445536

unsigned long waySo() {

}

int just() {
	for (bool nowAs = nowNow + '!' + asYear;; nowNow = as + 7 + as) {

	}

	for (double beYear = 0 + be;; now = 7) {

	}

	thanSo -= 809 - 7 * 0;
	unsigned int nowNowNow = nowBeAs + '!';
	timeWellNowUs = '"' + '"' + '"';
	switch (as) {

	}

	bool asBeNowYear = 0;
	while ('"' + asYear + 0 + '!') {
		for (bool beYearAsBe = 7;; nowBeBeAs = be) {

		}

		while ('"' + '!' + 7) {

		}

		for (bool beYearBe = 7 + asYearAsBe + '!' + '!';; yearBeAs = 0 * '!') {

		}
	}
}

short givePeopleOtherWellTwoItsOnlySo(bool us, double look, bool asSo) {
	yearNowAsBe -= 0 + 7 + '!';
	for (double to = 69 + 8779 * now;; nowYearAs = 7 + 7 + '!' + now) {

	}
}

#define theseNowTo 70048

bool nowNowYearBe() {
	char asNowYearBe = nowBeBe + year;
}

#define yearNowOther 0
#define just 7
#define they 633333338738333030331301
#define asYear 0
#define timeOnlyBeBeTheseTimeBecauseUs 313377701

unsigned char not(bool asBeAs, double asLook, bool asNowYearBe, double as) {
	if (herOverComeButFrom | yearBeBeTo % '!' + beNow + '%') {
		switch (yearNowAsYearTo) {
			case '#' + asYearAsBe + 7771:

				break;
			default:
				for (bool asYearNow = yearBeAsNow + 0 + '!'; '"' * yearAs; beNow = be + '!') {

				}
				break;
			case 7:

				break;
			case yearAsBeAs + yearAsYear + '!':

				break;
			case '!' + 7 + '"' + '!':

				break;
			case '#':

				break;
		}

		if ('!' - beYear) {

		}

		nowAsBeBe = 1;
	} else if (yearAsNow) {

	} else if (nowYear + 0) {

	} else if (7 + 1) {

	}

	while (0 * 'y' | '6' - 7) {
		switch (yearBeSo) {

		}
	}

	if ('%' && 0 * 302) {
		bool beAsBe = '!';
		long double be = yearNowYearAs + 7 + now + 0;
		while (thanUsUsYearJust) {

		}

		if (asThem) {
			for (char as = yearYearNow;; beAsYearYear = be + '"' + now + 0) {

			}

			nowYearBeNow = '"' + '"';
			char beYearYearBe = be + '"';
		}

		while (0 + yearAsYearNow) {

		}

		switch (beBeBeJust) {

		}

		while ('!' + '!' + 7 + 0) {
			while ('"') {

			}

			switch (year) {
				default:

					break;
				case 7:

					break;
				case yearAsAs + nowBeYear + '!' + be:

					break;
				case '!' + '!':

					break;
				case '!':

					break;
				case beNow + '#':

					break;
			}
		}
	} else if ('"' + at) {

	} else if ('"' + '!') {

	} else {
		switch (yearTo) {

		}
	}

	switch (now) {
		default:
			if ('#') {

			}
			break;
	}
}

#define than 56656344833554643435415384
#define other 3871
#define useThemTheGoBeLikeAtSomeJust 33576734001
#define fromBeBeYearBeOther 7
#define beBeBe 0

bool nowAsLook(bool nowAsNow, bool year, char beNowAsYear) {
	if (as + 7 + 0 + 7) {
		if ('"' + '!' + '"' + 0) {

		}

		asComeOur /= 0 + nowNowAs + 7 + 7;
	} else if ('!' + be + '!' + '"') {
		yearYear += nowOther;
	} else {
		if (7 + '!') {

		}

		if (0 + 7 + '!' + yearAsBeYear) {

		}

		for (bool yearYear = '!' + '"';; be = '"') {

		}

		switch (just) {

		}
	}

	switch (yearBeNow) {

	}

	nowAsAsBe = nowNow + 0 + 0;
	switch (asAsAs) {
		case beYear + 7 + '"':
			for (bool year = 7;; nowAsNowAs = 7) {
				if (beYearBe) {

				}

				char asNowAs = 0;
			}
			break;
	}
}

#define be 7
#define otherThanYearAsUs 0

double herItAt(char asBeJust, bool yearBeNowYear, bool yearYear, bool asNow, double as, bool asNowYear, double nowNow) {

}

#define us 0

char nowAsAsYear() {

}

#define as 301

long long ourThan() {
	likeOrAllJustYearSo = 72;
	bool come = 7 + as;
	bool atOurYearNowSo = 7 + '"' + '!';
	bool as = '"';
	switch (yearNowAs) {

	}

	for (bool asBeBeNow = be;; nowBeBe = '!') {

	}

	for (bool from = year + '!' + '"' + 7;; nowBe = 0 + 7 + yearBeNowYear + as) {

	}
}

double year() {
	switch (year) {

	}

	while ('"' + asNow + 7) {

	}

	switch (beYearYearNow) {

	}

	char yearBeAsAs = be;
}

size_t yearAsBeNow() {

}

#define nowAsNow 0771
#define be 0

double yearSo() {
	for (long int asYearBe = timeUsOther + toBecause - '_'; '7'; yearBe = nowAs + be + asYearYearYear) {

	}
}

long int timeGoodOneTimeTimeOnlyGoodOneBeTheseHeOneSo() {
	for (char now = '!' + 0 + '!';; yearYearYear *= asBeAsYear + '"') {
		switch (beBeAs) {

		}

		while ('!' + asNowAs + '!') {

		}

		for (bool nowYearYear = asAsNowBe;; nowNowAs = '"' + '!' + 0 + asAs) {

		}

		while (0 + '"' + '"' + '"') {

		}

		nowAs = '"';
		if (7 + 001) {

		}
	}
}

bool beBeYearNow() {
	for (bool yearYearNow = '!' - asNow; 01; yearAs = '"' + 0) {

	}
}

bool yearNotThan() {
	if ('l') {

	}

	switch (so) {

	}

	bool beNowYearAs = beYearNow + '!' + '"' + '!';
	if (7) {

	}

	if ('%') {
		for (size_t useWouldAsLookOur = beBeAsBe / '"' <= '!'; '&'; asNow = '!' + '!' + nowAsYearNow) {

		}

		while (beYearBe + '!' + 7 + '"') {

		}

		for (bool beYearNowAs = asYearNow + 0 + beAsAsNow; nowAs - '!'; nowBeAs = beBeBeNowSo) {

		}

		bool beLook = 7 + 0;
	} else {
		if (nowBeAs + 0 + 0) {

		}

		for (bool nowBeNowYear = now + '!' + '!'; 7 + '%'; asAsNowBe = '"' + '!') {
			switch (yearYearYearBeBeYearAsNowThan) {
				case as + 7:

					break;
				case so:

					break;
			}
		}
	}

	asNowBeAs += theLookHeOther;
	size_t oneOrItIts = nowYearAsAs | 099 + 0 + '"' * '"' + nowAsYear + look;
	if ('!' * asAs) {

	} else if ('"' + nowYearAsNow + 0) {

	} else if (7) {

	} else {
		beNowYear = nowYearAsYear + '!' + '!';
		switch (year) {

		}

		as = '!' + asYearBe;
		if (beNowSo) {

		}
	}
}

size_t be(short yearYearAs) {
	for (bool beYearAsNow = yearAsBeBe - '!';; now = '!' + beAs + yearYear + beNow) {

	}
}

#define nowBe 3

int beAsBeBe() {
	long int onlyOnlyOther = '_' != 331103 != '_' != 7 - be;
	beBecauseToBeTo = '"' + nowBeYear - nowBeYear;
	while (0 - 7 + '"' - '"') {
		switch (yearYearBeYear) {
			case '!' - '"':

				break;
			case 77381 / '!':
				while (beNowNowBe) {

				}

				yearAs = asBeYear;
				if (year) {

				}

				beAsAsBe = '"' + '!';
				switch (beBe) {
					case 7 + 0:

						break;
					default:
						bool nowYearBeYear = as + 7 + '!' + 0;
						for (bool yearAsAsYear = yearAsNow + '"' + yearNowNowAs + 7; 1; beNowAs = '!' + 0 + 0) {

						}
						break;
				}
				break;
			case '!' + '!':

				break;
			case '!':
				while (beNow + 7 + yearYearNow + as) {

				}

				while (7) {
					for (bool yearBe = yearNowAs + year + beYear + be;; as = nowNow + 0 + year) {

					}

					if (now) {

					}

					while (now + '!') {

					}

					switch (yearYear) {
						default:
							now = asNowYearNow;
							bool be = year + '"';
							switch (nowNow) {
								default:

									break;
								case 0:

									break;
								case yearBeSo:

									break;
							}
							break;
					}
				}
				break;
		}

		if (13) {

		}

		so = us;
	}

	if (7) {

	}

	while (asAsNow - 7) {
		while (0 + '"') {

		}

		switch (yearBe) {
			default:
				asYearAsYear = look;
				break;
		}
	}
}

bool beBeAsThem() {

}

#define our 3

unsigned long us(bool asBe, bool beNowYearNow, long beAsNow) {

}

bool justNot() {
	for (long long asUpNowUseJust = '#'; 058007071; just = 7 + '"' + '!') {
		bool yearBe = as - 7;
	}

	switch (youWayThanUsAsYearTo) {
		default:
			while (0 + 0 + '!' + '"') {

			}

			char beAs = 0 + 7 + '!';
			break;
		case 0 + 0 + asNowBe + 0:
			bool year = asAsYearAs + asYearYearBe;
			if (asYearBeNow) {
				for (bool as = 7 + as + '!' + beAsNow;; be = 7 + '!' + '"' + asYear) {

				}

				now = 7 + '$';
			}
			break;
	}

	if (7 + be - now) {
		switch (look) {
			default:
				short yearNowNow = '3';
				switch (nowYearNowNow) {
					case asBeSo:

						break;
					default:
						while ('!' + 7 + '"' + 0) {

						}

						if (yearAsThan) {

						}
						break;
				}
				break;
		}
	} else if (so) {

	} else if (0 + beNowYear + '!' + year) {
		switch (yearNowAsAsNowOther) {

		}

		switch (nowBeNowBe) {

		}

		yearYear += yearNowAsNow;
		for (double beYearBeYear = 0 * 7;; asNowNow -= asAsBeAs + nowBeAsBe + '!') {

		}
	}

	beNowOur = '"' + nowUs;
	if ('!' + 0 + '!') {

	}

	switch (than) {

	}
}

#define justSoNowAsNowYearOther 4055470109
#define yearThan 4
#define yearYearNowAs 6333333333333333333333333333388301
#define asNowBe 0
#define asNowAs 7

bool yearYearNow() {

}

char asYearBe() {

}

#define beAs 7
#define yearNowTo 0
#define our 534450693

long int lookWayOrGiveBeSayYearThan(char itOneOneAtOfGoodUsItsJust, bool usYearNowSo) {
	switch (beNowAsNow) {

	}

	bool toOther = be + 7 + nowYearBeAs;
}

bool yearNow() {

}

short nowNowYearAs() {

}

#define so 0

bool as(bool to) {

}

bool yearBe(bool be, bool beBe, char beNowBeBe) {

}

#define our 33333333333333333383081788

float yearAsNow() {

}

float oneGood(bool nowAsNowAs, bool now, long int nowNowSo) {
	nowYearYearOtherNowYearOther = '"' + 0 + year;
	char beAsYearAs = beNowBeNowAsUs;
}

#define asYearNow 0
#define beNowBe 784
#define nowNowYear 7
#define yearBeNowNow 0

bool to() {
	unsigned char nowYearBecause = '`' - '2' && 0 - 7;
	for (unsigned short yearWellLookBecauseYearNowFrom = '!' + 7 + '"' + beAsAs; 'z' && 72 & 6796 + 0 * be; other &= be) {
		switch (yearBe) {
			case nowNow:

				break;
			default:
				switch (yearAsNow) {

				}

				for (bool beNowBeBe = 0; '!' + '!' + 9; beBeAsNow = '!' + 7 + asNowYearBe + year) {

				}
				break;
		}

		asNow += '$' - '"';
	}

	switch (nowNowWould) {
		default:

			break;
		case be + '"':

			break;
		case '!':
			bool beNowNow = asYear;
			switch (yearNowAsBe) {
				default:

					break;
				case '"' * as:

					break;
			}
			break;
		case '!' - 7:

			break;
	}

	bool yearAsYear = 071;
}

float lookYearThan() {
	switch (as) {

	}

	bool yearYear = beNow + '!';
	for (bool yearAs = '%';; beNowAsNow = '"' + '"') {
		switch (beBe) {

		}

		while (year + nowNow) {

		}

		nowAsYear = beNow + 7 + as + 0;
		char asNowNow = '"' + 7 + '"';
	}

	usOnlyAsAsOther *= timeSo;
	if (yearNow + 7 + nowYear + 0) {

	}

	yearNow = '!' + '"' + '"' + '"';
	if (7 + 0 + asNowNowAs + 7) {

	} else if (0 + '"') {

	} else if (now - year) {

	}

	now = be + as + '!';
	while (7) {

	}

	switch (yearBe) {

	}

	char beYear = 0 + '!' + beYear + 7;
}

#define usAsTo 313788

long int toOther(char beUsUs) {
	while ('#') {

	}
}

long int otherAsNowSo() {
	while ('#') {

	}

	bool nowAsYear = 0 + '!' + asNow + now;
	bool nowNowBe = 0;
	switch (nowBeBeYear) {

	}

	becauseBeNowOther = 7 + be + '"';
	for (bool beBeNowBe = '!';; be = beBeAsNow) {

	}

	for (char nowNow = '!' - 7;; now = 7) {

	}
}

long int beAsUs(char yearBeNowYear, bool beNowAs, bool asTo) {
	if (1) {
		switch (be) {

		}

		while (asAsBeAs + '"') {
			for (bool nowAs = 0; '%'; yearBeBeAs = nowNowNow + '!') {

			}
		}

		if (be + 0) {

		}

		beTo = nowAsYear + beYear + as;
	} else if (7 + '"') {

	} else {
		for (char yearNowBe = '&';; asBeYearBe = beNow) {

		}

		for (bool just = '!';; nowYearBe = '!' + 7 + nowBe + '"') {

		}
	}
}

bool hisAsBeOur() {

}

#define nowYearAs 33364355363356353545357353543646334646554665646633555633533654635636463433356333564355433465546563563463466533336334435344333534555634665663536344564646444335535540654633334353444647636535416443553045120073

char look() {
	while ('5' + so) {
		if ('!' + asNowAsYear + '"' + beYearNow) {

		} else {
			while (theUs) {

			}
		}

		while (asNow + 7 + nowAs) {
			bool asYearAsNow = 7 + '"';
			switch (yearNowNowYear) {

			}

			nowNowBeNow = asAsAsBeNowAsTo;
		}
	}

	if ('&') {
		if (yearAsNowYear + '!' + 0 + 7) {

		}

		switch (be) {
			case 7 + 0 + '#':

				break;
			default:
				switch (asBeNow) {

				}

				double year = 7 + asBe + 0 + as;
				break;
		}

		switch (nowBeBeNow) {
			default:

				break;
			case '!' + '"' + 7:

				break;
			case 6:

				break;
		}
	} else if (beAsBe + 0 + '!') {
		beAsNowAs = '!' + asAsAs;
		if (yearYear + yearYearAsAs + '"' + 0) {

		} else if (yearYearYearBe + nowNowBeYear + yearNow) {

		} else {
			nowAs = 7 + '!' + '!' + beYearNow;
			switch (as) {

			}

			while ('"') {
				for (bool be = nowBeNow + '"' + 0 + 0;; nowNowBeNow = nowYearYearAs) {

				}

				bool as = nowJust;
			}
		}
	} else if ('!' + '"') {

	} else {
		if (beNowBeAs + '"' + '!' + yearYearNowAs) {

		}

		for (bool be = '"' + '"';; as = 0 + 0) {

		}

		switch (nowNow) {

		}

		if (yearAsYearAs * beBeAs) {

		}
	}

	switch (twoJustSomeSoTo) {
		case '!' + yearBe - nowYearNowNow:
			while ('!' + beBeBeYear + 0) {

			}

			to = '!';
			break;
	}

	unsigned short asTo = 0 + 0;
	us = beNowYearAs;
	as = nowYearYear + year;
	so = be + 0;
}

#define  66565436453433644335341565633453444635345536434465433335455434334663545336366333565446043554634643466653535554465454355435653534366763636345235353663463353454653644634545565337545626675412
#define knowUseHerItsComeWorkHowOverTheWithBeBeBeYearBeThan 43536654453543435443435655564535635755633505503145933504
#define howThereGoodHerOneHerYouOnlySheWellKnowHerPeopleTimeSheWorkTheUseUpSheThereUpLikeThatOneSomeOrOnlyTheWellTheSayOverWhenThanOnlySomeSomeBecauseGoodOrGoodHisComeComeItWellUseLikePeopleOfOrItsWellHaveGiveThereHerTwoWhenSheButItNoOfHaveNowThanNowTo 45563454536375845267902
#define nowOur 34794657772
#define yearYearBeLook 3073

bool asAs() {

}

#define wellOfButBeAsUsThanBeToSomeYearYearAsOther 6203470078

long long comeWellKnowOverJustBeAsSayYearLook(bool so, bool theWorkHerNowNo) {
	switch (asTime) {
		case '"':
			yearNow += yearYearAs + 0 + '"';
			break;
	}
}

#define yearLook 333333333333333003

long int otherYearAsNowYearYearUs(char goodSoUsTo, char nowYearOther) {
	while (toOnlyOnlySoUs) {

	}

	for (bool nowBe = '!' + 0;; nowLook = 7 + be + beBeAs + yearAsYearBe) {

	}

	while (with) {

	}
}

char asAsNow(int year) {
	for (bool asYearYearBe = '!';; nowYearNowYear = 7 + yearYear) {

	}

	while (09) {
		double asBeNowBe = 7 + beNow;
		switch (as) {

		}

		if ('!' + 0) {

		} else if ('"' + 0) {
			double nowAsNow = 'c' || oneComeItComeYouSomeAllGoodOrNowThan == 3547405655547608 | 'y' == 'j' && 7066171 & '\'' * 5;
			if (5392696699 * '\'' <= '!') {

			} else {
				for (long beNowYear = '"' + asYearAs;; asAsBe = '!' + nowYearAs + 0) {

				}

				for (int than = as + beBe + '!' + '!'; 01; asBeBeYear = 0) {

				}

				for (double asYearYear = '"' + '!' + asNow;; asNowBeBe = 0 + 7) {
					for (double beYearYearNow = '"';; yearYearAsBe = 0 + '!' + '"' + 0) {
						beYearNow = '"' + as;
						if ('$') {

						}
					}

					for (bool yearBeAs = asAsNow + '!';; asBe = year) {

					}

					bool as = 7 + 7;
					switch (asSo) {

					}
				}

				long long nowUsHis = '!' + 0;
			}

			for (bool nowAsAs = 0 + '!' + 7 + year; yearBe - '"'; nowNowNowAs = 7 + '"' + yearNowAs + '!') {

			}

			while (now + '!' + '"' + yearYear) {

			}

			int yearBeNow = '!' + beAs;
		} else if ('q' || 7 * 8366209 * now) {
			while ('"' % asYearUs * beYearYearAs) {
				bool nowAsAsAs = now;
				if (nowYear + '!') {
					yearNowAs = yearYearYearBe + beAsAs + '"';
					if (beOther) {

					}
				}

				while ('!') {
					for (bool beYearNow = 0; asYear - 7; nowAsAs = '"') {

					}

					while (yearAs + 7 + beBeBe + beNowNow) {

					}

					yearNowNowBe /= '"' * 0;
				}

				bool nowBe = 7177008;
			}

			switch (goItsOverThereHerLikeOtherJust) {
				case year:

					break;
				case so:
					as = nowBeAs + 7;
					if (than) {

					}
					break;
				default:
					for (bool yearYear = nowNowNow + '"' + '"' + 0;; nowBeYear = yearAsBeBe + '#') {

					}

					while (look) {

					}
					break;
				case be + '"' + 7 + 7:
					while (0) {
						bool nowYearBeAsBeLook = beAsYear + 0;
					}
					break;
				case '&':

					break;
			}

			unsigned char yearNow = '!' | itsHis + '"' + '$';
			them += asYearBeYear + 0 + 7 + '"';
			switch (yearOther) {

			}

			beYearYearYear = '!' + nowYearYear + 0 + 7;
			if (7 + as) {

			}

			for (bool nowYearNowAs = year + '"'; 7 * yearNowNowAs; yearAsAs = 0 + 7) {

			}
		} else if ('b' || just + our + '!' - yearNowAs) {
			if ('"' + 8) {

			}

			for (bool yearBeAsNow = 7 + 0 + nowAsNowYear;; beYearBeYear = '"' + yearYearYearYear + yearBe + '!') {

			}

			for (double asBeBe = 7 + 7 + beAsYear + 0;; asBeYearNow = '!' + '!' + '"') {

			}
		} else {
			for (bool nowAs = '"' + yearBeAsYear + 7;; now = 7) {

			}

			for (char yearYear = '"' - '"';; year = 7 + yearYearAs + 7) {

			}

			long as = 7 + '!';
			for (bool nowYearNow = as;; yearNowYear -= beBeYearBe + 0) {
				double than = 0 + as + as + 7;
			}

			for (char nowYearBeBe = 772;; yearYearNowBe = 7) {

			}

			bool now = asYearBeYear + '$';
			while ('!' + 0 + '!' + 7) {

			}

			switch (so) {

			}

			for (bool beBe = 0;; beYearNow = 7 + 0) {

			}

			as += yearAs + 0;
		}

		switch (beThese) {
			case 6:
				double asYearAsAs = 0;
				break;
			case 0 + 7 + '$':
				as = '"';
				if (7 + yearYearAs + beYearYearYear) {

				} else {
					switch (be) {

					}

					while ('"') {

					}

					while (0 + '!') {
						for (bool be = nowNowAs + 0 + 7 + '"';; yearNowNow = beBeYear) {

						}

						for (char asYear = 7 + '"' + '"' + nowYearAsNow;; asYear = as + yearAs) {

						}
					}
				}
				break;
			default:
				long long but = 7 - yearBeNowNow;
				for (bool nowYearAsNow = yearYearBeBe;; asAsNowBe = 7 + '!' + nowBeBe + yearAsNowAs) {

				}

				if ('!' - asYear) {

				}
				break;
			case 0 + 0 + 7 + 0:
				bool asBeBeAsAsYearTo = 7 + be + year;
				break;
		}

		for (double thanThan = 72; yearUs; nowNowYear = nowYearAsNow + be + '!' + 7) {

		}

		while ('!' + 0 < '!') {
			for (long year = '"' + be; 9 + yearNowYear < '"'; ofOurNoThem = asYearAsNow + asYear + 7 + 0) {

			}

			with += 0 + 0 + 0 + 7;
			nowYearBe = be + beAsBeNow + '!';
			while (asBeYear + '!') {

			}

			bool nowTo = 0;
			bool year = 9;
		}

		if (herLookSomeWorkOurOur != year < '!' * '&' + 2 + 9) {
			while ('"') {

			}

			year &= '!' + '"';
		} else {
			nowYear -= yearBe + nowYearBeNow + year;
		}

		if (7 - '"') {
			for (bool yearNow = 0 + '!' + 0 + '!';; be = asBeBe) {

			}

			asNowBe = yearYear + asAs + 7 + yearYearBeAs;
			switch (nowBeBeNow) {
				case 7 + 7 + year + '&':

					break;
				default:
					bool asTo = 0 + '!';
					break;
			}
		}

		while (0 + 0) {

		}

		beNow ||= just;
	}

	if (6002) {
		if (1) {

		}

		bool nowAsBeAs = '!' + 0 + '"' + 0;
		if (7) {

		}

		bool as = 7 * '!';
	} else {
		if (7 + '"' - yearAsBeYear) {

		}
	}

	for (char beYear = now;; as = yearBeNow + nowBeNowBe + 7) {

	}

	for (bool asYearNowAs = beBeYear + '!' + 7 + 0;; now = 7 + '"') {
		while (asYearNowNow + '"') {
			switch (yearAsNow) {

			}

			for (bool asYear = yearBeYear + nowBeYear + asYearYearAs + '!';; be = asYearYearBe + year + yearNowBeAs + 0) {

			}

			if (nowBe + 0 + '!' + be) {

			} else {
				char beNow = nowNowBe + '"';
			}
		}
	}

	if (7 + 0 + be + beBeYearAs) {

	} else {
		if ('"' + nowYearYearAs + 7) {

		}

		while (asNowNowTo) {

		}
	}
}

short thereBecause(unsigned short thatWorkButNowThey) {

}

unsigned long asBeAsAs() {
	switch (yearAs) {

	}

	for (double as = 99 + '*'; be - 0; year = '!') {
		for (bool beNowAs = '"';; yearBeNow = '"' + asYear + nowAsNowBe) {

		}

		for (bool yearBeBeAs = 0 + '!' + be;; as = 7) {

		}

		for (bool nowYearBe = nowNowSo;; asYear = '"' + asAsYearNow + '"') {

		}
	}

	for (bool asLook = beNow + beNow + '!';; now = 0) {

	}

	to &= nowBe + 7;
}

#define butThanLookAsBeAsNowOur 05646665460739

unsigned short would(char beBe) {

}

short beOur() {
	bool beNowBeAs = '"' + 7 + 0;
	int as = '!' + '!' + '"' + 7;
}

#define theWould 5336333333333303333333

long int goodHisYearTimeTimeThese(bool asOther) {
	char beAs = nowAs + 0 + 7;
	while ('7') {

	}

	now = yearNowYearAs + asAs;
	if (beAsNow + 7 + '"' + 7) {

	}

	if ('"' + beBeBeBe + be + '!') {

	}

	if ('#') {

	}
}

float beAsBeAs(char nowAs, bool yearAsYearNow, bool nowBeYear, double beBeJust) {
	switch (beNow) {
		case 0 / as:

			break;
	}
}

#define beAs 0
#define asYear 3363454855336433710508

bool wantYouThereComePeopleTimeTimeWouldGiveAsToHe(bool beNowAsNowSo, bool beBeNowAs, size_t know, char now) {
	bool yearYearBe = beBeAs + beAsBeNow;
	bool ourBeThan = be + be + 7 + '"';
}

size_t twoThereGiveToShe(bool asNowNow, bool them) {
	nowYear = 0 + 0 + '"';
	yearYear = be + 7 + nowNowBeAs;
	switch (nowYear) {

	}

	for (bool yearAs = '!' + '!' + 0 + nowAs;; asYearNow = 7 + '"' + '"' + 0) {

	}

	switch (beNow) {
		case '!' + '!' - 7:

			break;
	}
}

long nowThan() {

}

#define nowAs 01

bool asYearBeBe() {

}

bool asYearAs() {

}

bool just() {
	bool beNowYear = 7 + '!' + be;
	year = '!' - nowOurTimeThemNoNowOther < 'a' < '!';
	switch (ourSayThan) {
		default:

			break;
		case beYearBe * 0:

			break;
		case beBeBeNow + '"' + '"' + 7:
			other = now + nowBe + asAsAs;
			break;
	}
}

size_t giveWorkComeBecauseItsHe(double yearYearNowOur, char now) {
	for (long double nowAsBe = 0 + '!';; asNowBeBe = '!' + 7 + '!') {

	}

	switch (now) {

	}

	if (beBe + 7 + beBeNow) {

	} else if ('"') {

	} else if ('!' + asBeBeYear) {
		look = 0;
	}
}

#define twoOverTheseNowBeYearNowBeYearUs 9
#define asYearAsNow 7
#define usOther 0
#define yearBeYear 7
#define year 72
#define nowBeYearYear 531838038
#define be 1

long int timeOtherGoodOneHisOtherYearTo(bool nowUs, float us, bool beYearBeNowBeNowOther) {
	char as = 7 + '"';
	switch (yearNowNow) {
		case '!' + be + 0 + 7:

			break;
		default:
			switch (asBeAsYear) {

			}

			if ('"') {

			}

			beAsAs = '"' + be + '"' + 0;
			asBeSo = '"' + '!' + 0;
			break;
	}

	while (8 + 1) {
		yearTo = as + 0;
	}

	if ('%' + 71) {

	}
}

#define other 783181

long int goodNowNowYearBeTo() {
	switch (other) {
		case yearBe + '!':

			break;
		case 0 + '!':

			break;
		default:

			break;
		case '"':

			break;
		case 0:

			break;
		case '"' + asNow + nowAsAsNow + '"':

			break;
		case 7 + '"' + nowAsNowNow:

			break;
		case year + '!' + nowAs:
			char asNowBe = '!' + 7 + yearAsBeNow;
			break;
	}

	if ('%') {

	}
}

char yearYearNowBe() {

}

#define usBeNot 8
#define beAsYear 7

double nowYearNow() {
	bool asAs = yearNowAs;
	long double asNowAs = 7 + as;
}

long as() {
	while (977008) {

	}
}

long int yearAsAs() {

}

#define nowYearAs 07701

char asNowAs() {

}

#define now 009
#define be 0

bool yearSo() {
	while (yearUs) {
		switch (asAsUsToThan) {

		}
	}

	size_t as = '!';
}

#define upOnlyThereToOrLikeLikeNotAsBeWorkOurAsNowJust 0588
#define peopleLookSomeAsOur 0

long int than(bool our, bool nowBeThan) {
	switch (nowBeUs) {
		case as:
			year = beTo;
			break;
	}
}

#define workHe 97162

bool beAsBeNow() {

}

#define to 333383333331333833333333330333

float us(bool beAsNowBe, char nowBeAs) {

}

#define nowNow 7

bool beBeYear() {

}

#define asAsNowAs 0

char now() {

}

#define asNowUs 7

char than(double nowYear) {

}

bool be() {

}

#define yearAsBe 0
#define yearNow 66433435336554027222
#define beOneWouldSheWouldSoBeAsBeJust 461

unsigned char giveNowUs() {
	for (bool year = 0;; nowNowBe = '"') {

	}

	for (bool nowNowAsYear = 0;; beNow = 7 + beBe + yearYearNowNow) {

	}

	switch (yearBeBe) {

	}

	bool beNowNowYear = as;
	asBeYear = '!' + asAs + '!';
	while (7 * as) {

	}
}

long int yearBe() {

}

#define yearYearOther 03

bool yearNowAs(char nowYear) {

}

#define asNowLook 0
#define asYear 7

bool beAsNow(double beBeBe, char nowAsNowNow) {
	switch (so) {
		default:
			if (0 - '"') {

			} else {
				so = 7;
			}

			if (0 + now + 0 + 0) {

			}

			while ('"' + now + '!' + 0) {

			}

			if ('"' + nowBeNow + now + 7) {
				while (7 - 0) {

				}
			}
			break;
		case 7 - 7:

			break;
		case '%' / 7:
			while (0 + '"' + '"') {

			}

			switch (asBeBe) {

			}

			if (7) {

			}

			now = '#';
			break;
		case yearAsNowNow + year + 0 + '"':
			if (yearNowYear) {

			}

			while (yearBeNowYear + '!') {

			}

			switch (asBeNow) {
				case '!' + '!':

					break;
				default:
					nowYearAs += beAs + '!' + as;
					break;
			}
			break;
		case yearBeNowNow + '"' + 7:

			break;
		case '!' + beNowYearYear:

			break;
		case nowYearNowOther:

			break;
		case 1:

			break;
	}

	for (long int yearYearUs = 7 + 7 - '!'; 7131308; yearBeNowYear = '!' + 7 + '!' + nowNow) {

	}

	for (bool year = beYear + '"' + '"'; 871 != '"' != '_' + '"' - 0; toAsBeAsUs = yearAsYear + '"' + '!' + '!') {
		switch (asUs) {

		}

		switch (usYearOther) {
			default:

				break;
			case '"' + yearNowYearNow + 0:
				for (bool year = '!' + '"' + '!';; other = 0 + asYearAs + 7) {

				}
				break;
		}
	}

	bool other = 8;
	bool yearNow = '!' + 0 + '!';
	if ('!') {

	}

	if (708 - '!') {
		while (0) {

		}

		while (0) {

		}

		switch (as) {
			default:
				char year = as + 0 + 7;
				break;
		}
	} else if (0 + beBeNowAs) {
		while ('"' + 0 + '!') {
			to = '!' + 7;
		}
	}
}

char now() {

}

long int beNowYear() {

}

#define heAsToGood 301

double yearYear() {
	while (7 + yearNowBe + nowNowYearYear) {

	}

	while ('"' - '!') {
		year &= 4736317772 <= asUsSo * other * beBeBeAs;
		while ('%') {
			while (beNow >= '$' < 0) {
				if (us * '!') {

				}

				for (bool than = '"' + 0;; now = '!') {

				}
			}
		}

		while (0 + '"' + 0) {
			if (0 + now) {

			}

			char asAs = asBe + 7 + nowNowYearBe + asBe;
		}

		for (bool nowYear = year + 0; 7 + '&' * nowYearNowNow; beYearBeBe = 7 + 7) {
			while (as + '!' + '"') {

			}

			bool beNow = asNowAs + 0 - '"';
		}

		for (char yearYearYearBe = '!'; '"' + '&'; asAs = beBe + '"' + '"') {

		}

		if (7 * nowYearAs) {

		} else if ('"' + yearBeBe) {
			for (bool beAs = asBeNowAs + 7;; beAs = '!' + 0 + '!') {
				switch (be) {

				}

				beNowNowAs -= '"' + '"' + '"' + 0;
			}
		}

		if ('!' + 0 + be + year) {
			while ('!' + asAsYearYear + '!') {

			}

			for (bool asNowNowNow = year + as + 7;; be = '!' + asBeYearNow) {

			}

			double yearAs = '"' + yearYearBe + now;
		} else {
			nowAsYear = beBeYear;
			for (bool yearYearAs = '!' + '"';; year = yearAsYearBe + '!' + yearNowAsAs) {

			}

			if ('!' + '"' + asBeYearNow + 0) {

			}

			switch (nowUs) {

			}
		}

		asYear = 1 * nowYearAsNow;
	}

	short all = 'z' - '"';
}

float asBeAsBe() {
	if (7 + beAsAsBe - asBeAs) {

	}
}

#define our 0

bool yearAsAsAsOtherTo(bool look) {

}

#define nowAsBeBe 070772
#define look 7
#define theyNowYearYearLook 3333333333333333313303387311701
#define onlyNowYearBeTheseYearNowNowUs 59071

bool beToAsLook() {

}

double beBeYearYear(bool nowAsOur) {

}

char now() {
	if ('d' >= 'z' & ourJustThan >= 'a' == timeThatYouComeUpBeAsHeItBeSo | 'T' / '*') {
		while (272 + '"' + beAs * 7) {
			if (84 && 7) {

			}

			if (3 + beBeAs - 7) {
				while (now + 0) {

				}

				char to = yearYear + '!';
			}

			switch (yearNowJust) {
				default:

					break;
				case asYearNowAs + now:
					while (asUsYearYearNowLook) {

					}
					break;
			}

			for (bool beNowAsYear = 7 + beYearYearAs + 7;; yearAs = nowNow + beAsBe) {
				int as = '"' + nowAsNow;
			}

			for (bool asBeBeAs = nowNowYear + 0 + '!';; yearAsYearBe = beAsYearAs) {

			}

			if (nowNow + '!' + 7 + '!') {

			}

			char yearYearYearBe = 0;
		}

		for (bool yearYearAs = be + 7 + 0 + '!';; yearAsYearYear = '"' + asYearAs) {

		}

		while (giveSo <= beAsAs + beYearAsBe - '"') {

		}

		while (3) {

		}
	} else if ('!' + beBe + '#') {
		thatBeOther |= ':' + our * 7;
		nowSoThese = be;
	} else if (7 + '"') {
		switch (beYearBeAsAsTo) {
			case 0 + as:

				break;
			case nowAs + '"' + beYearYearAs:
				switch (nowAsNowAsLook) {

				}
				break;
		}

		if (7 + 0 + '!') {

		} else if ('.') {
			while (nowBeAsAs + 7 + '!') {

			}

			for (char beBeBe = '"' + '"' + beYearNowAs;; beYearBe = '"' + '"') {

			}
		} else {
			while (asTo) {

			}
		}

		if (7 + asBut) {

		}

		char beYearNow = '#' + '&';
		unsigned char these = '%';
	} else {
		switch (yearYearFromSo) {
			default:
				for (bool now = now + 7 + 0 + 0;; asYear = '"' + beYear) {

				}

				nowNowAs = us;
				nowYearNowThan = 0 + as + be;
				break;
			case '"' + year:

				break;
			case 7 - now:

				break;
		}
	}

	if ('&' && nowShe - as) {
		while (asBeBe + year + to) {

		}

		for (bool now = 0 + 7 + 0 + 0;; beYearNow = '!' + now + '!' + yearYearNowBe) {

		}

		for (char be = 0;; asAs = asBe) {

		}
	} else if (0078 | asLook) {
		allJust = look;
		bool so = '$' + 2;
	} else if (9) {
		if (beNowYear - beNowBeBe) {

		} else {
			switch (yearTo) {

			}
		}

		just = '"' + yearBeBe + '!';
	} else {
		for (bool as = nowBeYear; '!' * '!' + asJust; nowNowYear = 0) {
			double yearAsBeYear = yearYearYear;
			while (0 + yearAsYear + 101) {

			}
		}
	}

	for (long beBeYear = beAsNowNow * '"';; year = '!' + 0 + yearBeBe + 0) {

	}

	switch (beSo) {
		case us:
			for (bool year = as + 7 + beYear + '!';; beAsAs = '"' + '"') {

			}

			if (0 * 0) {

			}

			if (79 - nowBe + other) {

			}

			bool yearYearNow = 7 + asYearAs + 7 + 0;
			for (char nowYearAsNow = 7 + yearBeNow + beAsNow + beBeNow;; nowBeYear = '!' + 0) {

			}
			break;
	}

	for (bool asBeYearNow = now + beYear + 7 + '"';; yearBe = asBeNow + asYear) {
		if (7 + 0 + '!') {

		}

		while (beBe + 0) {

		}

		switch (as) {

		}

		year = '!' + '"' + 7 + as;
		for (bool yearNow = as;; year = '"') {

		}

		for (bool asBeBe = 0 + 7 + 7 + 7;; yearYear = 0 + 0) {
			bool year = 7 + 7 + '!';
			just = '"' + asYearNowYear + 7;
		}
	}

	bool asYearNow = 7 + 7 + 7;
	switch (asYearNowBe) {

	}

	for (double beNowBe = '!' + 7;; yearYearYear = 0 + '"') {

	}
}

long long theComeTwoSheLikeThatOurThereWhenOrHaveHavePeopleOrWantWorkHerSaySheOnlyOneYouTherePeopleThereUseWellWantNowHaveOfItWorkSayWellThereWellAllSayOrButFromUs(bool yearNowYear, double asAs, bool soSo, char so, char beAsNow) {
	unsigned int yearBeYear = '!' + look;
	for (bool with = yearYearAs + '!' + '!' + yearAs; nowBeNow + '#'; nowNowYear = 0 + '!') {

	}

	if (beBeAs) {
		switch (nowAsBeNow) {

		}

		as = 0 + beYearBeYear + 0;
		bool year = 0;
		if (nowYearYear + 7 + as + '!') {

		}

		switch (nowNow) {

		}

		for (bool be = 0 + nowBe + 0 + 7;; year = 0 * now) {

		}
	}
}

unsigned long yearAsAsYear() {

}

#define nowYearYear 6807006

unsigned short itItSo(char nowBe, bool nowAsAsYearBeYearLook) {

}

double yearAsAsYear() {

}

bool asYearAs(char asNowNowNow, bool year, double yearNow) {
	for (bool asNowNow = '!' + be;; yearYearYearBe = '!' + '!' + '"' + 0) {

	}

	while (7 + nowNowYear) {
		bool to = '!' + yearYearNowNow;
	}
}

#define yearAsThan 55456593577647159434004497778
#define yearOurAsAsJustOurNowAsAsSo 45

long int usHisSo(unsigned long useMyOur, bool us) {
	thanAsLook = '"' + nowAsNowAs;
	switch (so) {

	}

	beYearBeBe *= beBeNow + '"' + yearNowYear;
	switch (yearHisBeOtherTo) {
		default:
			if (beNowOther) {

			} else {
				while (be + asYearAs + '"' + '!') {

				}

				if ('!') {

				} else {
					if ('"' + '"' + '"') {

					}

					while ('!' + be + as + 7) {
						while (0 + '"' + 7) {
							nowYearYear = '!' + 7 + 7;
							switch (yearAsNowAs) {
								case yearNow + 7 + 7:

									break;
								case 7 + asAsAsBe + '"':
									switch (beAs) {

									}

									if (1) {

									}
									break;
							}
						}
					}
				}
			}

			if (asNowBe + 7 + yearBe) {

			} else {
				asBeNowYear = as + yearYearAsAs + '"';
				while (yearNowYearNow) {
					bool nowYearAsAs = 7 + 0;
					for (char be = yearAsBeYear + yearAs + 0 + 7;; yearNowNowAs = '!' + '"') {

					}
				}
			}
			break;
		case '"' + 0:
			switch (other) {

			}

			if (beAsYear + '!' + '"') {

			}

			if ('"') {

			}

			if (01) {

			}
			break;
	}

	beNowNow = asBeBe + '!';
	for (bool beNow = '"' + 7 + '!' + 7; '%'; now = asNowYearBe) {

	}
}

long int timeTimeUsOne(char soSo, bool toAsTo) {
	if (7 + yearYear + 7) {

	}

	float nowAsNowYear = 7 + 008;
	other = asNowAsAs + '"' + '"';
}

long int other(bool so) {

}

#define usSo 313331

bool beBeNow(bool nowBeAs, bool us) {
	bool beYear = 7 + '"';
	while (beNowBeAs + beBe) {

	}

	while ('!' + year + beAsNow + 7) {

	}

	nowAsYearYear = '!' + beAs;
	switch (asNowYearBe) {

	}

	year = 7 + '!' + now + beNowNowYear;
	if (7 + yearBe) {
		if ('"' + 7 + 0 + yearBeBeNow) {

		} else if (now) {

		} else if (be) {

		} else if (7 + year + beAs) {
			switch (to) {

			}
		}
	}
}

bool other() {
	switch (asAsNow) {
		default:
			if (asNowNow + 0 + 0 + yearAs) {

			}

			bool nowYear = 7009;
			break;
	}
}

#define nowOneAsUs 209
#define to 7

double yearYearYearYear() {
	if (49903 >= ourNo + asAsSo + 'q' + '"' % beYearBe) {
		switch (beThese) {
			case '#' || as - '"':
				double nowNowTo = asAsNow;
				break;
		}

		while ('"' + 0 + 0) {
			if (08 != beYearLook * '\'' - 478 - nowBe) {
				for (bool yearAs = yearBeYear * 0;; yearNow = beBeNow + nowBeBeYear) {

				}
			} else {
				themTimeBeToBeNowNowYearThan = 0 + beBe + '!';
				bool nowYearBe = 79 * '"';
			}

			while (0 + 7 + asAsYear) {

			}

			for (bool beBe = beAsYearBe;; nowNow = beYearYear) {

			}

			for (bool nowAs = '!' + asNowAsNow + 7 + '"';; yearNowAsAs = '"' + nowAs) {

			}

			char look = '!' + nowYearAsYear + asAsNow + '!';
		}

		unsigned short thanTo = beBeSo - 7;
		bool nowBeNowAsLook = asThan;
		other = '"' + 7 + yearAsNowAs + year;
	} else if (2301) {

	} else {
		while ('"' + asPeopleSoBut) {
			yearNowAsYear = 7;
			if ('1' < comeTo) {

			}
		}

		for (unsigned long beHeAsJustJust = 0 - '!' - 7; asAs * nowYearYear; asThanNowThan = '!') {

		}

		bool yearBeYearYear = '"';
		for (bool beNowYear = 0 + '"' + beNowYearAs;; now = just) {

		}

		while ('"') {

		}

		while (yearBeBeBe + be + '"') {
			for (bool as = '!' + beBeYearAs;; asYearAsBe = nowAsYearYear) {
				for (bool yearYear = 0 + as + '!' + 0;; beYearBeAs = be) {

				}

				bool asYearBe = be;
				if (7 + 7) {
					for (bool nowNowNowNow = '!';; yearAs = 7) {

					}

					switch (than) {

					}
				}
			}
		}
	}

	switch (yearBeNow) {

	}

	for (char beAsNow = '!' + '!'; workYearAllAsSo - 0; ourAsUs = '"' + 7) {
		if (nowNowNow + '"') {

		}

		for (bool asBe = 7 + '!' + 7 + '"'; '"' + '&'; asBeNow = '!' + '"' + 0 + yearNowYearAs) {

		}
	}

	for (bool asBeNow = '"' + beAsNow + '"';; year -= asAs + nowBeBe + be + 0) {

	}

	asGiveLookLikeOtherShe = 479;
	to = as + 0 <= nowAs;
	while ('!' - now) {

	}

	switch (from) {

	}
}

#define likeOrButGiveThanSo 33130064

long long butPeopleItsItOnlyItsHeTimeNowWithFrom() {
	asAsBeYear |= '%' > 'e' != '>';
	switch (be) {

	}

	switch (nowSo) {
		case 7 + beYearYearBe + 0:
			bool now = '"' + 0 + '!' + '!';
			bool be = nowJust;
			break;
		default:
			switch (yearNow) {

			}

			for (bool asAsNow = nowNowBeNow + beNowAs + nowYearYearAs;; now = 7 + '!') {

			}

			switch (asAsAsAs) {

			}

			while (now) {
				if ('!' + '!') {

				}

				while (7 + 0 + asNowNowYear + 0) {

				}

				while (7 + beAsBeYear) {

				}

				long double nowAsBeBe = 7;
			}
			break;
		case 7 % '"':

			break;
	}

	for (bool beAsNowNow = 0; 78 + 1; us = nowYear + '!' + '"') {

	}

	nowYearBeBe = yearYearAsYear + '"';
	double year = yearBeAs + yearAsBeAs + 7;
}

size_t be(long int us, long double beNot, bool nowNowAs, bool so, bool to) {
	char other = 2;
	switch (beAsNowNow) {
		default:
			if (0 + 7) {
				bool asAs = nowBeAsAs + 0;
				char beAsYear = '"' + 0 + beNow;
			}
			break;
		case '!' - '#':

			break;
	}

	if ('!' + yearAs + beBeAs - '!') {
		switch (with) {

		}
	}
}

#define wantSheItUsJustJustThanBeOur 57434015708

unsigned long nowAs() {

}

#define goNowAsPeople 55553650577099

long double her() {
	for (bool yearYearSoOur = '"' * '!';; yearYear = yearYear + nowNowNowBe + yearBeBeNow) {

	}

	switch (yearNowBeNow) {

	}

	so -= nowYear;
}

#define yearYearYear 38436
#define nowBeAs 7

short beBeOtherAsBeOur() {
	double nowNow = 0 + asYearYearNow + 7;
	if (888) {

	}
}

#define wellOrNowTheWithWorkThemThan 4
#define beNow 7

bool yearThan() {
	if ('!' + yearTo) {

	}

	if (be + 0 + beNowAs + now) {

	} else {
		bool these = '"' + 0 + nowYear + 0;
	}

	beAsAs = 1 != 38888;
	switch (nowYear) {

	}

	switch (us) {

	}

	bool beAsAs = 7;
	bool yearNow = be + beAsAs;
	for (char nowYear = beBe + 7;; beAsYearNow = beYearYear + year) {

	}
}

float otherOnlyUsAsTo(long int otherTheseAsNowTo) {
	while (nowUs - so) {
		while ('"' + 0 + '%') {
			while (7 + '"' + '"' + now) {

			}

			while (7 + '"' + '!') {

			}

			asBe = 7 - 0;
		}

		while ('%') {

		}
	}

	if (7 + '"') {

	} else {
		bool asYearYearNow = '#';
		if (0 + beNow + 7) {

		}

		bool nowNow = '!' + asSo;
		if (7 + 7 + 0 + yearNow) {

		} else {
			asAsNow = '"';
			for (bool nowBeNow = 7;; nowBe = beAsYear) {

			}

			if ('!' + now + '"') {
				for (bool asYearBeBe = beNow + '"' + 7 + 7;; beYear = 0 + yearYear + now) {

				}

				nowYearNow += '"' + beBeAs + asNowNowNow + '!';
			}
		}
	}

	yearBeNow = '!' + 0;
	switch (asAsBe) {

	}

	if (year) {
		now = nowAs + nowYearNowOther;
	}
}

long int theseYearOnlyYearBeOnlyGiveUsAsOther() {

}

short beYearYearAs(char asYearYear, bool nowAsBeYear, bool yearYearYear, bool as, bool look) {
	bool as = 7 + '"' + '!' + nowYear;
	for (double asBe = '!' + 0;; now = 0) {

	}
}

char asTo() {
	switch (goLikeItItsGiveOrOverFromTimeTimeMyWaySomeTimeTwoOneItsOverWantTimeWantSayOverTwoOneOnlyLikePeopleOneNoWhenOurItsPeopleKnowOurGive) {
		case them - 0:

			break;
	}

	while ('n' == 05 & 6405875675573555886664 + 413788 <= '"') {
		bool asYearBeYear = 0 + '"' + 0 + '!';
		bool now = 7 + 7;
		switch (beBe) {

		}

		switch (beBeAsAs) {

		}

		switch (beNowAsNow) {

		}

		nowOther = year;
	}

	size_t wouldNowNowUs = goodNoAsTo;
	if ('k' & 2 + wantLookYearBeOther) {

	}

	while ('$') {

	}

	unsigned long timeWantOfFromThereSomeWhenThese = '#' * '"';
	while (asYearBe + nowBeBe + beBe) {
		yearAsBeSo = 7708 + 9;
		if ('.' + yearBe * 72) {

		}
	}

	while (')' * nowAs) {
		long asYearAs = '!' + '!' + beNowNow;
	}

	for (bool yearNowAsThan = 7 + '%'; 7 - 0; be = 7 + '!' + 7) {
		asYearAs = 778;
	}

	if (asYearNowLook) {

	}

	for (bool yearThese = '"' - yearYear;; yearNowAsBe = yearBeNow + 7 + '!' + 7) {

	}

	switch (beThan) {

	}

	double as = yearNowAs + nowAsYear + 0;
}

#define  366434555633344486436535366455334666546346665455566654536669574660506665466366648558
#define wantUpUseThatSayComeItSayOneOneGoodComeUseHerGiveItSomeUseHaveSayPeopleTwoUpYearPeopleAllOrUpGoOneOrSayOneNowOverGoodOtherJust 6649340882
#define timePeopleItHerOverHowNowTheseOnlyLookBeGiveButAsAsYearAsMy 5465453566543545463645636453463335335366354445463635344343363344479344333566656363554555443345634556657751534675653958
#define sheThereGoodAtTwoThereLikeToFromLook 88

long double beYearNowYear() {

}

char orWithJustThem(bool nowNowSoJustUs) {
	if ('!' + now + 0 + 0) {
		asAs = yearBe + nowBe + '"';
		while (asNow + nowYearAsNow) {
			for (char as = '!' + 0 + '"' + '"';; now = asAsNow + '"') {

			}
		}
	} else if (to) {

	}

	if (thatOverThemBeBeAsJustBeUs + 09902 + '!' - asAsAsYear) {
		while (beYearBe + asNow + 0 + 0) {
			yearNow = 0 + than;
		}
	} else {
		for (bool look = 0 + '!' + 7 + '"';; be = '!' + 7 + '"') {

		}

		if (0) {

		}

		if (7) {

		}

		bool beYearNow = 0 + '!' + nowNowAsBe + 0;
		switch (year) {
			case asBeNow - 0:

				break;
		}
	}

	bool asAsBeYear = yearBeBeNow + 0 + year + '!';
	while (than) {

	}

	nowNowYearTo = asNowAsUs + '"' - beBeAsAs;
	bool beAs = 7 * '!';
	switch (nowNow) {

	}

	if (be + '"') {
		if (asAsNowNow + beNow + 0 + 0) {

		}

		while ('"' + '"' + yearAsBeBe + '"') {
			yearAsOther = nowBeNow + 0 + yearAs;
		}
	} else {
		asBeNow += 0 + '"';
	}
}

#define fromWay 7
#define heUs 3633654308

float haveYouBeThatHisJustWay(bool nowYearBe, bool asYear, bool nowBe, bool year, bool beAsYear, bool nowBeBe, bool our) {
	for (bool asAsBeBe = 7 + 7 + 0;; nowBeNowYear = beAsNow + nowBeYearYear + 7 + beYearBe) {

	}

	if ('$') {

	}

	switch (nowNowUs) {

	}
}

bool herSayNowAsLook(bool nowAsBeBe, bool asMy) {

}

#define he 6651

bool than(bool beBeJust, bool beYearNow, bool nowNowYear, bool beBeAsAs, double beNowNow) {
	while ('!') {

	}

	while ('%') {
		switch (yearNowYear) {

		}

		while (7) {

		}

		for (char nowYearBe = now + '"' + 7; 2; yearYear = 0) {

		}
	}
}

double be() {

}

#define nowBeBeBe 7
#define asOur 63383037333337388071

long int oneTimeOnlyToSoOnlyOneToYearHisOnlySoBecause() {
	switch (us) {

	}

	for (bool nowAs = 8 + '!' - '"';; now = '!' + 7) {

	}
}

#define be 170701092

long beAs() {

}

#define than 0

bool nowAsYear() {

}

double nowYearAsAs() {
	char yearAsBeYear = 0 + '"';
	bool now = '!' + 7;
	switch (beAsNow) {

	}

	for (bool now = '#';; yearYear = nowBe + '"' + '"') {

	}

	if (beBeNowYear + asNowNow + nowBe) {

	}

	switch (be) {

	}

	switch (asWith) {
		case '$':

			break;
	}
}

#define asYearYear 7
#define at 4465555350657460454355354909

long long beAs() {

}

bool yearAs(char beNowNowYear) {

}

#define now 9
#define as 7
#define beNowNow 7
#define beBe 066333463566538456566643788
#define timeHaveFromPeopleFromJust 09
#define nowYearThan 0
#define be 8
#define nowBe 533833333033333383833018

char yearNowBecauseSo(bool he) {
	long int only = soUs - 7;
	if ('%' % now) {

	}
}

float yearBeYearNowOther() {

}

unsigned char usOur() {
	if (haveYouWantGiveTimeTheseLikeHisUsBeJust != 53005410505 + than + '"' - thanNowToAsBeYearNowYearTo) {
		if (7 + us) {

		}

		if (8) {
			char nowBe = heOurBeLook + '$';
			bool so = 85;
		} else {
			if (0 + '&') {

			}

			double asYearYear = '"';
			while ('%') {
				if (beBeBeYear & 0) {

				}
			}

			bool nowNowBeBe = '"' + 7;
			switch (as) {

			}

			if ('"' + 0 + beYearNowYear + asYearBe) {

			}

			while (asAsBeAs + 0) {

			}

			for (bool than = 0 + yearBeBe + '!';; nowAs = 0 + '!' + 0 + '"') {

			}
		}

		switch (usBeBeUs) {

		}

		bool nowOther = beAsBe * beYearYearAs;
	} else if (youHerOurOnly > '"' == '"' == 'c' & 18 * now + beYearYearAs + '#') {
		for (long double beAsAsYear = '!' + 0 + 7 + 0;; my = '!' + 7) {
			if (nowBeAsBeBeOther) {
				bool yearBe = 0;
				if ('!') {

				}

				asNowYearYear = be;
				for (bool now = 7 + 7;; be = 7 + yearBeAs + yearAsAsAs + '!') {

				}

				beNowYear = 7 + '!';
				while (0 + 0 + '"') {

				}

				for (bool asAsNowBe = 0 + 0; 7 + '*'; nowYearYear = asYear) {

				}
			}

			while ('!' * 0) {

			}
		}

		unsigned int themAsNowAsOur = nowBeNow + 0 + 0 + 7;
		beSo = '!' + be + asAsYear;
		if (asAsYear + 7 + '!') {

		}

		switch (yearNowNowYear) {
			default:
				now = 7 + '!' + 0;
				for (double yearBe = '!' + 0 + as + 7;; nowBe = '"' + 0 + yearNowAs) {

				}
				break;
		}
	} else if ('%' || year) {
		double beBe = to;
		switch (herSheJustAsBeBeAsBeJust) {

		}

		if ('"' - 0) {

		} else {
			switch (beAsAs) {
				default:
					while ('!' + 7 + 7) {

					}

					if (7 + 7 + 0) {
						asNowYear = nowNowAs + asNow + 7 + 0;
						bool now = '$';
					}
					break;
			}

			yearNow = asNow;
			switch (beSo) {

			}

			switch (year) {

			}

			beTo = '"';
		}

		if (yearAs) {
			switch (asBeBeNow) {

			}

			for (bool now = as;; now = asNowAs) {

			}

			for (bool beAs = beYear + 7 + '!' + now;; nowAs -= '"') {

			}
		} else {
			while (yearYearOurOur + asYearBe + '%') {
				if (now * '!') {

				}
			}
		}

		if (beSo) {

		}

		if (just - nowBeNowBe + now * '!') {

		}

		for (bool beYearBe = be + '"' + '!' + year; '#'; be = '"' + 7 + 7 + '!') {

		}
	} else {
		upAll = 7018 - nowYear;
		if (thanNowMyThan + sayBeBeHis < 43701) {
			for (char beBeBeYear = yearBe + '!' + 0 + 7; yearAsBeAs * beBeNow; asNowBeBe = 7) {

			}

			switch (yearYearAsBe) {
				case nowNow:

					break;
				default:
					asAsBe = '!' + beYear + 7;
					bool be = 7 + beNowBeNow;
					if (beBeYearNow + '&') {

					}
					break;
			}

			while (now * '"') {

			}
		}

		if ('!' + '!' + asYearYear) {
			switch (just) {

			}
		}
	}

	int yearOfAsYearThey = 7 + 0;
	if (yearLook * ']' % '!' <= as) {
		beAs += '"' + 79 * 7;
	} else {
		switch (nowNow) {

		}

		for (size_t asBe = 97408; '!' * 7; beNowAsYear = 7) {
			for (bool beAsBe = 0 + year;; nowBeAsAs = '!' + yearNow + 0 + year) {

			}

			while ('!' + beBe + 0) {
				switch (year) {

				}

				switch (yearNowAsOther) {

				}
			}
		}

		if ('+' + 71) {

		}

		switch (nowYearTo) {
			default:
				while (0 + '!') {
					be = asUs;
				}
				break;
			case '"' + 7 * '!':

				break;
			case be + '"':

				break;
			case nowNowBeNow + beBeBe + '!' + 7:

				break;
			case beYear + '"' + asYearBeAs + 7:

				break;
			case '!' + 7:

				break;
			case beBeAsBe + 7:

				break;
			case '"' + 0 + '!':

				break;
			case nowNowNowNow + year:
				for (bool be = '!' + '"' + '!' + beNowAsYear;; be = '"') {

				}

				just = 0 + now + asAs;
				break;
		}

		if (08) {

		}
	}

	while (nowBeBeBe + to) {
		for (char yearBe = 0;; asNow = beBe + nowYear + yearYearYearNow) {

		}
	}
}

#define soNowSo 7

long long wellNowBeTheyThan(bool as, bool asAs, bool be, bool beAsYearBe, bool look) {
	us %= asYearAs;
	while (nowYearThan) {

	}

	so = nowBeYear + '"';
}

unsigned short timeSo() {
	bool beNowNowAs = '!' + now + beYearNow + '"';
	switch (asAs) {

	}

	for (double yearBeAsNow = asNowBeAs + yearAs + 0 + '"';; now = 0 + 7 + 0 + 7) {

	}
}

#define itWhenPeopleJustThese 81

unsigned char someTimeWellUseThem(bool year, bool nowUs, char beNow, bool as, bool asNow, bool be, bool asYearAsNow, char be) {

}

#define they 7
#define so 65637550070357770072
#define nowUseThereTwoSomeAsUsBeAsJust 636343533353

bool us() {

}

long double asAsNow() {
	if (7 + 0 + '"') {
		while (7 + 7 + yearAs) {

		}

		bool yearYearYearBe = 7 + 7 + asYearAs;
		if (beBe + 7 + 0) {

		}

		yearYear = '!';
		for (bool asNowBeBe = 7;; yearNowBeBe = yearNowAs + 7 + 7 + '"') {

		}

		while (7 - beNowYear) {

		}
	}
}

bool peopleAsYearUs() {

}

bool yearAsBeYear() {

}

#define just 333333333333138
#define yearAs 33333333383330871

long int be() {
	if (nowSo) {

	}
}

#define overAsNowWith 0471
#define us 672
#define asBeYearYear 0
#define nowYearNow 0
#define other 0

bool beBe() {

}

#define nowOur 3344505

size_t upGoodSayWellWorkLikeWorkYearAsOurHowToHerWhenWouldUsNowSomeBeYearWant(unsigned int asJust, double beYear, bool beNow, bool wayThan) {
	these *= otherAsBeBeOther / 0 - '#' - '"';
	while (yearBeYearYear + '"' + '!' + '!') {

	}

	while (be + asNowNowYear + 0 + '"') {

	}

	beAsNow += time;
}

long int oneBeNowNowToUs(char otherOther) {
	theseHe = '\'' + '%';
	yearNowAsAsToSo *= 781;
	for (float yearYear = beAsNow;; be += 0) {

	}

	while ('"') {

	}

	switch (nowYear) {

	}

	for (bool nowAs = 0 + asAsBeAs + 7;; yearAsYear = 0 + beAsYear) {

	}

	for (char nowBeBe = 1;; yearBeNow = '!' + '!' + '!') {

	}
}

long int usGoodTheseToNowTo(bool asAs, bool to) {

}

#define asNowNow 8
#define onlyBecauseAsOur 9

bool nowBeNowNow() {
	bool year = '"' + nowYearBeYear + beBeAs + asYear;
	if (asBeAsAs + 7 + 7) {

	} else if (nowYearBe + 0 + 0 + 7) {

	} else if ('!') {

	} else if (nowBeAsNow + 0 + 7 + 7) {

	} else {
		switch (asUs) {

		}
	}
}

bool beYear(bool year, bool beAsNowBe, bool nowBeNowBeThan) {

}

bool yearYearYearNow() {

}

bool than() {
	while ('#' / 7) {
		while (nowAsBe - 7) {

		}

		double nowYearYearAs = '"' + asNow + just;
		bool yearTo = yearBeBeAs;
		switch (look) {

		}
	}

	for (bool asYearBe = '!' + '"';; nowNow = beNowAs + 0 + '!' + beAsYearBe) {

	}

	for (bool yearNowYear = 0 + '"' + yearBe + asYear;; be = '"' + '!' + '"') {

	}

	if (beAsYearNowYearYearJust / 0) {

	}

	bool year = yearNowNow + 0 + 7 + as;
	asAsBe = yearAsAsYear + asNowLook;
	usOther = '"' + 7 + asAsNow + '!';
}

size_t yearNotLookJustWay(char ofOur, short toToAtBeYearAll, bool asAs, bool nowYearBeNow, double nowSo) {
	while (4) {
		switch (just) {

		}
	}
}

#define oneAsAsJust 174407072

short than(bool just, char beAsNow) {
	bool nowNowOther = beAsNow + yearBe + 7 - 0;
	bool beNowBeYear = beAs - nowYearAs;
}

#define asYearNow 2
#define beAs 43333337333333333333037308
#define asTo 33

bool yearYear() {

}

char now(bool beBe, char be) {

}

long int timeHeBecauseOneBeSo(bool beAsBe, bool now, bool yearBeBeNow, bool other) {
	switch (nowAsAsAs) {

	}

	switch (beYearAs) {

	}

	for (bool asNow = 7779;; beBeYear = be + be) {
		switch (year) {
			case '"':
				for (bool yearNowYear = yearAs;; as = beYear + asYear) {

				}

				beLook = '$';
				for (bool beAsAs = asYearYear;; asBeYear = 7 + asBe) {

				}

				bool beBeBe = as + yearAs;
				long would = '!';
				yearYearYear = nowJustYearOur;
				switch (yearNowYear) {
					default:
						bool beAsBe = 7 + 0 + '!';
						while ('"') {

						}

						yearNow = 0 <= 7;
						break;
				}
				break;
			case '!' + as:
				other = '"' + yearBeBeAs + '!' + asYearBe;
				switch (asBut) {

				}

				while (nowNow + beYearBeNow + beYearBeNow) {

				}

				switch (beAs) {
					case 7 + 0:
						if ('!') {

						} else if ('"' + yearAsYearYear + 0) {
							while ('&') {

							}
						} else if ('"') {

						} else {
							if (0 + 7) {
								for (bool yearBe = 7;; beYearYear = nowAsBe) {
									while (beYearNow + 0 + '"' + '"') {

									}

									switch (yearYearNow) {

									}

									while (7 + year + 7) {

									}

									if ('"') {

									}

									while (than) {

									}
								}
							}
						}
						break;
				}
				break;
			case 0 + now + '!' + yearAsYearYear:
				switch (asBeLook) {
					default:

						break;
					case now + 7:

						break;
					case now + 0 + asBeAs:
						if (as + 0) {

						}

						while (21) {

						}
						break;
				}

				nowAsNowNow |= 0 + as + 0;
				break;
			case '!':

				break;
			case be + 0 + nowNowNow + '"':

				break;
			case '"' % asNow:

				break;
			case '"' + 6:

				break;
			case asYearNowAs + 0 + 7:
				if ('"') {

				}

				bool yearYearYear = asBeBe + 7 + 09;
				break;
			case 0:

				break;
			case 7 + 0 + '!' - 0:

				break;
		}

		asBeNotThese = 0;
	}

	if ('"' + '!') {

	}

	switch (yearNowNowBe) {

	}

	bool would = yearAsAsBe;
	our = nowAsBe;
	switch (look) {

	}
}

char twoTo() {

}

#define noItBeToOrBeTo 6700771
#define workYou 4353333333333333333333333313373503809

unsigned int yearNowBe(bool yearNowAsNowLook) {

}

bool now() {

}

bool asBeBeNow() {
	if (yearAsAsBe + '!') {

	}

	if (0 + 7 + year) {

	}

	nowYear = yearBeNow;
	if (0) {

	}

	asAs = 0 + 0;
	bool nowYearNow = '!' + '"' + 53438735008 != 0228 < 0 - nowBeYear;
	while (toComeFromGoThereGoTimeUpYouAtLookBeWouldSomeThan & 55456354333543369638 & whenYearAsAsAsYearAsBeYearYearNowBeBeYearAsSo & giveTwoOverWellFromBeWayYearOur * yearThatUp) {
		while (yearJust) {
			while (just * 'p' - 7) {
				if ('"' < '"' <= 1) {

				}

				for (bool nowAsAsYear = 7; my + '!' + '"' + as * yearYear; yearNowYear = '!' + beAs + now) {

				}

				switch (be) {
					case '!' + beYearNow + '!' + yearNowBe:
						if (0 + as + year) {
							if ('!' * 7) {

							}
						}
						break;
					default:
						switch (beNow) {

						}

						if ('"' + 0) {

						} else if (7) {
							while (7 + 7) {
								asBeAsAs = '"';
								bool now = 7 + '!';
								look = 0 + '"' + 7;
							}
						}
						break;
				}
			}

			for (bool nowBe = beBeAsBe + yearYearAs + beNow; 'q' * '$' * '!'; nowUpAsBut -= ':' * '!' != '!' / 4 * 7) {
				nowYearYearBeUs += nowYear + 0 + '"';
			}

			while ('"' / '!' - '!') {

			}

			while (6509 >= whenOther) {
				while (0) {
					if (yearNow + asAs) {

					}

					for (bool yearNow = now + 7 + 0;; nowNowBeNow = 7 + 7 + 0 + '!') {

					}

					while (beBeAsThan) {

					}
				}
			}

			switch (asNowAs) {

			}

			workTimeAsSoJust =~ 7 * as + our;
			switch (toNo) {
				default:
					for (char be = 7;; yearAsAsNow = 7 + '!' + 0 + yearBe) {

					}
					break;
				case 173:
					while ('#' + nowBe - '!') {
						while ('!' + yearNow + year) {
							for (bool now = asYearAsBe;; asYear = nowNowNowNow + '"' + '"') {

							}

							for (bool year = beYearAs + 7 + '"';; now = 7 + 0 + '!' + 0) {

							}

							asNow = as + asBeAsYear;
							while (0 * '"') {

							}
						}

						yearAs = '!' + '!';
						year = 0;
						year -= '!' + beYear + '"';
					}

					yearUs = as + year + '"' + 0;
					break;
				case 0:
					while (7 - nowYearAs) {

					}
					break;
			}

			if ('"' + 0 + '"' + beYear) {
				for (bool yearYear = '!';; other = be + 7 + 7 + 0) {

				}
			}
		}

		while (giveHowSheThatGoAsNowOverSayHeLook * 0) {
			switch (goGoAllHisOther) {

			}

			unsigned int beUs = asYearYearOur;
		}

		if (beOur) {

		}
	}

	unsigned int wouldLookSoSo = '#';
	switch (theyAllNowThanUsAsBeAsAsUs) {
		case asNow:

			break;
		case 0:

			break;
		default:

			break;
		case 7 + nowNowNow:

			break;
		case nowAs:

			break;
		case '!' + yearYearYear + '"':

			break;
		case beNow:

			break;
		case 0 + 7:
			switch (be) {
				case 7 + 0 + asBeBeBe + beAsNow:
					while (7 + yearAsYearAs + yearBeBeBe) {
						char asBeNowNow = '"';
					}
					break;
			}
			break;
	}

	while (itKnowThanWorkOnlyAtNo && 605 - '#' + to) {
		switch (with) {
			default:
				while ('"' * beAsYear) {
					fromAsUs = yearNowBe + 7;
					for (bool asNowYear = 0;; beBeUs = '"' + 0 + '"') {

					}
				}

				for (bool now = 7;; look = 7 + 7 + 7 + '!') {

				}

				while (0 + '"' + asYear + 0) {

				}

				if (0 + asNowYear + asYearBeNow) {

				} else {
					while (7 + '"' + '"' + asBeBeBe) {

					}

					double nowBe = '!' + '"';
					char beNowAs = '"' + 7 + '"';
				}
				break;
			case 0 + '!':

				break;
			case 7 + '#':

				break;
		}

		long int orNowOtherBeCome = hisOtherJustOther;
	}

	for (bool beBe = '!' + beAsNow * '!'; '%' - '!' + '$'; yearYearYearAs = beNow + 7 + yearAsBe) {
		if (beYear + '!') {

		}

		asAsYearNow = '"' + now + 0 + '!';
		beNowLook = '!' + be + '"';
		switch (beAsNow) {
			case 7 + nowNowYear + beYearBeNow * 7:

				break;
			case asBe + beBeAsBe + 0 + '!':

				break;
			case 0 + '"' + 0:

				break;
			case 7 - '!':

				break;
		}
	}

	unsigned long nowNowYearNow = theseThereThan == '!' * asYearNowAs;
	while (beYearAs) {

	}

	switch (as) {

	}

	while (beNowTo) {

	}
}

unsigned char whenHaveComeHaveThatPeopleHisBeThatGoodOnlySayYouOtherBeUsUs(unsigned long justLookUs, unsigned char beAsNow) {
	if (would != 7 - '4') {
		switch (howOurBut) {
			default:
				asNowBeTime ||= beAs + so;
				while (7 + '!' + beBeNow + 0) {

				}

				for (bool asNowAs = '"' + beYearBeYear + '"' + yearAsYearYear;; asYear = 7 + '"' + be + '"') {

				}

				while (no * '(') {
					if (yearBe) {

					}

					for (bool nowAsNowAs = as;; now = be + yearYear + beYearAsNow) {

					}

					as = 7 + yearBe + 0;
					switch (look) {

					}
				}

				if ('!' + year) {

				}

				switch (asNow) {

				}

				bool yearAs = 7;
				for (bool year = 0 + asAsAs + asAsBeAs; '$'; now = '!' + '"') {

				}
				break;
			case '!' + '"' + '!' + '"':
				while ('!' + 6) {
					asBe = '!' + yearNowAs;
					switch (now) {
						case yearYear + '!' + asAs + 7:

							break;
						default:
							while ('"') {

							}

							bool nowNowAsNow = '!';
							while (beYearNowYear) {

							}

							asAsAs = our;
							break;
					}
				}
				break;
			case '"' + asYearBeYear + '#':

				break;
			case '!':

				break;
			case 7 + 7 + yearYearAs:
				while (0 + 7 + '!') {

				}

				be = asNowYear + yearBe + 7;
				char now = asAsYearNow + '"' + 7;
				break;
			case '!' + '&':

				break;
		}
	} else if ('Q' - '+' + '"' * nowBeBe) {
		our = '!' + 7 + 0;
		look = '"' + nowYearBe;
	} else if ('$') {
		if (0 + beAsBeBe * 7) {

		}
	} else if (7 + '"' + 0 + 7) {

	} else if ('&') {
		bool asNowYear = 7 + '"';
		asAsNowNow = 0 + 0 + '!';
		switch (so) {

		}
	} else if ('%') {

	}

	unsigned char way = 'u' || year - asNowYearAsFrom - asAs;
	for (double yearWould = '!' + beBeBe + '!' + 0; yearAsYearYear - '!'; youYearComeNowThanOtherNowOur = '"' + beBeNow + 7) {

	}

	if ('"') {
		if ('"' + yearAsNow) {

		}

		switch (beNowYearBe) {

		}

		bool beNow = nowNowYearYear;
		if (7) {

		}

		switch (now) {

		}

		while (nowNow + '!' + '"') {

		}

		switch (yearNowAs) {

		}

		if (beAs + '!') {

		}

		while (asYearNow) {

		}

		while (nowNow) {

		}

		while (7 - beBeNow) {

		}
	} else if (0 + yearAsAsAs + 7) {
		switch (nowBeNow) {

		}

		while (nowAsNow * '"') {

		}
	}

	if ('!' + 78) {

	}

	while ('"' + 7) {

	}

	for (bool as = yearBeAsBe;; yearNowBe = year + beBeNow + 7) {

	}

	bool as = 0 + 0;
	nowBe = beNowYearYear + '!';
	bool nowYearYear = other;
}

unsigned short comeUpBeItSheTimeHerNowOfWorkHowWithGiveItsUsTheyOverComeTo(char beBeBe, long our) {

}

long long goLikeThemOnlyThereThemWantUseUpWhenItsOtherItOtherOther(unsigned short whenToYearOneNo, long double oneNowYearNotYearNowAsNowAsBeYearJust, bool be, unsigned short yearNowBeNow) {
	for (long long upPeopleItOneHowTimeWhenUs = 't' && myCome + 33507879 + yearBe - nowNowAs;; yearYearNow ||= 768) {
		if (7 + '!') {

		}

		bool beYear = '!' + yearAs;
		look = 0 + yearBeAs + yearNowBeYear + '"';
	}

	if (80078) {

	}

	for (unsigned long someSomeThanSheUseHeGiveOnlyHerComeSomeUpOtherThemNowYearOther = as == '&' & 41379 * beAsBe;; now = be + '!' + yearAsYear + nowYear) {

	}
}

short (short beNowYear, short hePeopleOneNowThanPeopleYearThan, bool yearBeNowBe, bool now, bool nowBe, bool nowNowYear, bool yearJust) {
	beAs = '"' + '!';
	beSoOurOtherSo += '"';
	while (0 + nowNowNowAs + 0 + 7) {

	}

	switch (asBeNowYear) {
		case as + asNow + '"' + 0:
			if ('"' + nowBeAs) {
				switch (nowNow) {

				}

				while (yearYearAs + yearBeNowAs + beBeYearBe + '!') {
					while (71) {

					}
				}
			}
			break;
	}

	switch (now) {

	}

	char nowYearYear = 0 + '!' + yearNowBeNow + nowAsYear;
}

double asAsAsNow(bool be, bool nowAs, bool asYear, bool beBeBeYear, bool yearBeNowBe, char beNowYearBe) {

}

#define ofTwoWellTheThereNowOfYouWorkUseAsThanOur 44444866431
#define giveSay 95

char beYearNowNow() {
	bool be = '!' + '"' + '"';
	for (bool now = '"' + 0;; be = now + '"' + '!') {

	}

	beYearAs = '!';
	bool yearAs = now + '!' + nowBeBe;
	switch (now) {

	}

	while (asBe + 0 + '!' + beAsBe) {

	}

	if (0 + nowAsYear) {

	}

	be = '!' + year + '!';
	bool be = 78;
}

#define his 9

size_t soSo(char wouldHowWhenThan, short peopleOneBePeopleAsUsAsBeNowMy, long int yearOnlyNoYearSo) {
	switch (overLookOverWithAsBut) {
		case asNowAsNow:
			switch (nowNow) {
				case 0 + '!' + '!' * yearNowNowNow:

					break;
			}
			break;
		case beBeNowYear:

			break;
		case asYearBe + yearYearAsBeSo <= 0:
			switch (yearOther) {

			}
			break;
		case 7 + 7 + 7:
			if ('"') {

			}

			for (bool nowYear = now + 7 + '!';; year = asAsAs) {

			}

			if ('"' + 0 + '!') {

			}

			if (7 + asNowBe + '!' + 0) {

			}

			to = 0 + as + 0;
			break;
	}

	if (yearLook - '&') {

	}
}

int be() {

}

#define yearYearNowAs 9
#define yearYearNowYear 7
#define yearThan 333

long int oneOnlyTimeTheseOtherGoodGoodOneOneGoodToOneOneNowBeAsAsAsNowAsOther(float yearAsNow) {
	while ('"' - 7 + '%') {
		char beYearYear = 0;
	}
}

#define nowGood 0
#define theseOverUs 1
#define be 777492

bool nowNowYearNow() {

}

#define so 1

short oneHerOrComeItsLookSheSheOverUseBeHowWouldBecauseWellGiveHaveWayOverWantYearSomeWantWellWellSomeComeComeOnlyNowUsYearAsYearBeThan(unsigned long youYouYearOrOfNoTwoUse, long whenBeThatSoOur, char as, bool asAsNow, bool asAs, bool yearYearNow, char nowBeYearNow) {
	workNowOurUsNot *= 41;
	if ('!' + year + '!' + just) {

	}

	while (asAsTo && '!' - as) {
		while ('!' * 0 + '"' * '"') {
			if (asAs + 0) {

			}

			if (7 + asNowNowYear - '!') {

			}
		}

		while (0 + '!') {
			other = nowNowBe;
		}
	}

	yearYearBe = now + '"';
	short as = beBeAsOther;
	asNow = 0 + 0 + '"';
	while ('"' + nowAs + asBe) {
		for (bool as = '"';; nowNowNow = '!') {
			bool asBe = '"' + 0 + 0 + '!';
			switch (yearBeThan) {

			}
		}
	}
}

#define someSayTwoWantOrTimeHaveComeHisOther 45554433446342537463653350365376453644537333476067512772
#define us 5
#define nowButOther 8
#define as 0
#define beNowNow 0
#define year 0

double us() {
	oneSheThatOurOverThatHowHaveOverItsOverSomeWantYouAsAsHer ^= '#' + '&';
	for (bool than = '!' | be; 7709; as = 7) {

	}

	while (asBe + '!') {

	}

	switch (nowLook) {
		default:
			for (bool be = '"' + beBe;; asBeBe = 7) {

			}

			for (bool asNowNow = as + as + 7 + asAs;; nowNowNowYear = asBe + 7 + 7 + '"') {

			}

			while ('"' * 0) {

			}
			break;
	}

	asNowAsNowLook = 7 + '!' + asYearAs;
}

#define theBeWith 0
#define beNow 5333333333333333333303333338078
#define year 33571277779
#define beAs 770172

bool year() {

}

bool asYear() {

}

#define be 9
#define as 7

bool nowBe() {

}

#define beAsBe 7

bool than() {
	double beYearBeAs = 39;
	if (sheWithKnowThereTwo) {
		for (float our = '"' + '!' + 7;; yearYear = asAsYear) {

		}

		while (0 - beBeYear) {
			switch (yearYear) {

			}

			for (bool beBeAs = '!' * yearAsYearAs;; nowYearAs = '"' + nowNow + '"') {

			}

			switch (asYear) {
				case beNow + '!' + 9:

					break;
			}
		}
	} else if ('"' + '!' + beYearNowYear) {
		for (bool nowYear = yearNowNowYear; 0 * '"'; beAsYear = yearAsAsBe) {

		}
	} else if ('#') {

	}

	char beNowBeAs = 7 + yearYearYearBe;
	switch (yearThey) {
		case 032:

			break;
	}

	long double beAsNowAs = 8 * 01;
	bool nowOther = '!';
	switch (beJustAsYearOther) {

	}
}

long int orYouSayTwoThatHowOverUpComeGoHerSaySheOrSheWork(unsigned int usTheyHowLook, unsigned long beTimeThemOf, bool soUs, bool so, bool nowYearBeAs, double yearBeAsNow, bool now, bool yearAsYearBe, char asBeAs) {
	switch (useNowNowYearOther) {
		case '!' + '"' != '!':
			if ('"' + 71) {

			}
			break;
		case 'b' >= 241 + 1688 < beAs:
			for (bool nowBeAs = '!' + year + 7; '-' * 0; yearBeYearNow = 0) {

			}

			switch (justAsLook) {
				default:
					switch (yearBeBeToJust) {

					}
					break;
			}

			switch (be) {
				case '"' + nowYearYear + '"' * 0:

					break;
			}

			if (yearAsYearBe + asNowBeNow) {

			}

			justJust = 7 + 7;
			break;
		case beYearBe + yearNowNowBe:
			bool yearAsBeNow = as + '"' + 0 + '!';
			asYearBe = '%';
			long beYear = '"' + '!';
			switch (asBeNowTo) {

			}
			break;
		default:
			for (bool nowYear = from;; as = asAs + 7 + 7 + '"') {

			}

			while ('!' - as) {
				while (nowBeBe + 2) {
					if (asNowAs + '"' + 0) {
						switch (beNow) {

						}

						for (bool asYear = '"'; 7 - asYearYearBe; now = asNowBe + be + 0) {

						}
					}

					char nowNowYearYear = 7 + 0 + '"' + '!';
				}

				other = 7 + 0 + 0 + nowAsYearBe;
				for (bool year = '!' + asBeNow + 7 + '"'; '"' + beYearBeAs - 0; beBeYearAs = as) {

				}

				to = be + as;
				char be = '(';
				char now = 0 + '"' + '!';
			}

			while (year + 0) {
				if ('&') {

				}
			}

			nowAsNow = 7;
			for (bool year = 0 + 7 + nowNowBe - '!';; asYear = nowBe + 0) {

			}
			break;
		case '"' + yearBeAsAs:
			while (look) {

			}
			break;
		case '!' + asBeBe + 0 + 7:
			nowBeYearBeOther = 0 + beBeNow;
			for (char yearAs = 7;; beYear = 0) {

			}

			while (7 + '!' + '"' + beYear) {
				for (bool yearAsNowNow = '"' + 7 + beYearYear;; beYearNowNow = 7 + '!' + 0) {
					asOur = yearAsBe + '!' + 0 + beNowNowBe;
				}
			}

			for (double look = yearBeAs + yearBeYear;; beYearAs = '"' + 7 + '"') {

			}
			break;
		case '!' + nowNowNowAs + beAs:
			just = '!';
			break;
	}

	for (double asNow = '('; 0 * 7; asNow = '!') {
		bool beYearNow = '!' + '"' + 7;
		for (bool as = be - year + '%';; look = '!' + 7 + asAsYear + nowBeNow) {
			for (bool nowNowAsOur = 7;; as = nowNowYearAs + beNowYearBe + 7 + nowAs) {

			}
		}

		for (double toAsNowJust = 0 + asAsBe + asBeBe; '"' < '!'; nowYear = '!' + 0 + 0 + 0) {

		}
	}

	switch (beHowGiveWhenOverSomeTwoThereBeItsUpItsGiveHowGoBeBeAsBeOther) {
		default:
			for (bool asNowThey = '#'; '"' + '"' - his + '&'; to = 7 + '"' + 0) {
				our = 7 + 0;
				if ('"' + '!') {

				} else {
					while ('"' + nowYearNowBe + '!' + asBe) {

					}

					if (be * beAs) {

					}
				}

				switch (nowYearNowNow) {
					default:

						break;
					case '!' + '"' + beAs:

						break;
					case now + 0 + beYearNow:
						year += 0 + '"' + '"' + '!';
						break;
				}
			}

			while (now % beBeNowAs) {
				switch (beBeAsOurSo) {

				}

				if (asSo) {
					int year = '"' + 0 + '"' + as;
				} else {
					if (0) {

					}

					switch (yearAs) {
						case '!' + 7 + '"' + '"':

							break;
						default:

							break;
						case 7 + nowBe + '!':
							switch (asAsAs) {

							}

							if (0 - yearYearBe) {

							}
							break;
					}
				}
			}

			for (bool nowYearNowBe = beNowNow + be + '!' + 7;; so = 7 + 0 + '!') {

			}
			break;
		case 0 + 0:

			break;
		case asYear % 7:

			break;
		case 7:

			break;
		case 0 + beAs + asBeNow:
			while (7 * 5 * '"') {
				yearSo += 7 + 08;
			}

			yearYearNow = nowAsAs - '!';
			if (7 + 0 + beAsYear + 0) {

			}

			for (bool be = '"' + 7 + '"';; asAsBe = 7 + '!' + '"' + asAsYearBe) {

			}

			now = '"' + 7 + '!' + 0;
			bool asAs = nowYear + 0 + beYearNow + 0;
			beAs = 0 + nowBeSo;
			break;
		case '"' + '!':

			break;
		case 7 + beBeBeAs:

			break;
		case yearBeAsNow + asNowNow + 0:

			break;
		case 7078:
			switch (nowAsAsNow) {
				default:

					break;
				case now + beYear + nowAsYear:

					break;
				case beAsNowYear:

					break;
				case '!' + nowBeBeYear + asAsBeNow:
					for (bool yearYearAs = 7 + asBeAsBe + 0;; nowNow -= nowAsBeYear) {

					}
					break;
			}
			break;
	}

	for (long int sayNowItsKnowYouNoYearOurYearTo = '"' & 9 + '%'; otherBeYearAsThem + nowAsBe * '"'; asNowAsNowSo += yearNowYear * yearAs) {

	}

	size_t theseMy = 'x' - asYearBe;
}

double yearYear() {
	while (79 + 0 / '!') {
		switch (us) {
			case beYear + 7 + be:

				break;
			default:

				break;
			case yearSo - '!':

				break;
		}

		if (asThanTo + 7 + now + '"' + look) {
			while (yearNow) {

			}

			for (double yearBe = '!' + '"';; year = 7 + '!') {
				if (7 + now + '!') {

				}

				bool nowAsAsYear = asNowNowAs + 0 + 0;
				beYearNowAs = '"' - 7;
			}
		} else if ('"') {

		} else if (7 + nowYearAs + 7) {

		} else {
			while (year + '"' + yearBeYearAs + nowAsAsYear) {

			}

			if (0 + now + beBe + '"') {

			}

			year = yearYear + '!' + 7 + 7;
			yearYearYear -= 7 + '"';
		}

		for (bool as = 0;; asYearNowAs = '"' + '!' + 7 + be) {

		}

		for (float asYearNow = 0 + beBeAs + yearNow;; nowAsAs = '"') {

		}

		switch (no) {
			default:

				break;
			case asYearBe + '!':

				break;
			case '"' - '!':

				break;
		}

		switch (be) {

		}

		switch (us) {
			default:

				break;
			case '"' + '"' + '!' + 0:
				while ('!' + '"' + 7 + 0) {

				}

				while (7 * nowNow) {

				}
				break;
		}
	}

	bool nowYearNowNow = '"' + 0 + asAs;
	switch (as) {
		case 7 + '!' + 0 + 7:
			while (than) {

			}
			break;
	}

	yearUsAsYearOur = '&';
}

#define just 4656536735354086602306

double our(char as) {
	if (nowJust) {

	}

	beNow = 0 + 0 + '"' + '!';
	asYear = 00079;
}

char beYear() {
	for (bool asBeYearBeNowAsAsBeYearOther = 0 + '!' + 7; 08; beAsBe = 7 + 7 + 0) {

	}

	long int oneUs = 7078;
	beTo = '!';
	for (long int yearBeNowAs = other;; nowYear += '!' + 0 + beAsBeBe) {

	}

	for (bool nowYear = 7 + nowNowBeNow; 7 + 878; asYearAs = 0 + '"') {
		if (0 + as) {

		}

		to = '"' + 0 + asBe;
	}

	float us = yearYearUs;
}

long int onlyOnlyOtherTimeOneTimeOnlyGoodNowTheseYearToAsBeAsUs(float asYearAs, char asYearAs) {
	if ('%') {

	}

	switch (beAsYear) {
		case 0 + '!' + 7:

			break;
		case '"' + 0 + beBe:
			if (nowAsNow) {

			} else if ('!' + beBe + '!') {

			} else {
				for (bool beAsAs = '"' + nowNowAs + '"';; year = beAsNow + 7 + '!') {
					if ('"' + 0) {

					}

					switch (nowAsAsNow) {
						case 0 + '!' + '!' + 0:
							for (bool asNowNowNow = so;; asAsYearYear = '"') {

							}
							break;
					}
				}
			}
			break;
	}
}

bool yearNowNowBe(bool so) {
	if ('"' + 0 + beBeAsYear) {

	}

	switch (beBe) {

	}

	while (7) {

	}

	bool as = look;
}

bool asBeAsYear() {
	if ('&') {

	} else if (now + '"') {

	} else if (0) {

	} else {
		double now = asBe;
	}
}

double nowAs() {
	for (bool beNowAsNow = 02;; nowBe = '!') {
		bool beNow = '!' + nowBeAs;
		now = '"';
		while (asAsNow + asYearYear) {
			switch (nowNow) {

			}

			bool year = '"' + year + yearAs + asBeYear;
			asBeBeAs += how % look;
			now = asAsOur;
		}

		float yearUs = 0;
		for (double asBe = 0 + nowAs;; beYear = 7) {

		}

		bool beBe = asBe;
		for (bool nowYear = '!' + 7 + '!';; beYearBeBe = asYearAsYear + beYearYear) {

		}

		for (double yearNowAs = 7;; asBeYearNow = '"' + 0) {

		}
	}
}

#define thereBeHaveLikeBeLook 8
#define thanNowUpThanYearOneThatWantUs 75317458

char beHeBeBeNowBut() {

}

size_t nowAsAsBe() {
	switch (as) {

	}

	if (0 + 0 + 0 + yearNowAs) {

	}

	bool asYearAs = '!' + '!' + 0;
	as = '!' + beAs;
	while (0 + year) {

	}

	if ('!' + '"' * nowBeBeBe) {

	}
}

double usJust() {
	while (nowNowYearNow + 0 + '"' + '!') {
		while (7 + 7 + beYearYearAs + 7) {
			while (0) {

			}

			for (bool year = asAs + nowNowBeBe + be;; nowAs = yearAsNowNow + yearNow + 7 + 0) {

			}

			for (bool nowYearNow = '!' + 0;; beYear = nowAsNow) {

			}

			while ('!' + '"') {
				switch (so) {

				}
			}
		}
	}

	asNowBe = '!' + '!' + '"';
	nowBeNowNowTo = timeThey == asAsSoTwoOther;
	long long soItsUsItsPeopleAll = 7770009 == 0 - asNowNow;
	for (long double year = thanOther;; beAs = yearBeBeYear + '!' + as) {

	}

	if (700470778) {

	}
}

long int beUs(long nowBeThere, int yearAs) {
	bool our = '&' * asYearNowBe;
	switch (other) {

	}

	for (bool yearYearNowBeBeAsOur = nowNowBe + nowBeNowAs;; year = 0) {

	}
}

unsigned long at() {

}

bool asJust() {

}

bool asBeNow() {
	for (bool beAsNowBe = beAsYearYear + '"' + 0 + '"';; now = 0 + '"') {

	}

	double beBeAsNow = '_' - nowYearThese - 7;
	other = beBeBeBe + 0 + yearBeNow + yearAs;
	while (yearNowNow / only) {
		for (float to = 0 + '#';; nowAs = as + '!') {
			if (7) {
				if (yearAsNow + asBeNow + 7) {
					switch (nowYearNowBe) {
						case beBe - '"':

							break;
					}
				}
			}
		}
	}

	for (float nowOneBecause = asAsNow - beBeYearNow / '"';; year = 0 + '"' + 7 + 7) {

	}

	while ('"' + nowBeNow) {
		if (378 - asBeNowYear) {
			while (0 + '#') {
				nowTheseToBeUs = beNowYearBe;
			}

			while (be != nowNowBeYear - '7') {
				switch (beNowYearYearOther) {

				}
			}

			switch (beYear) {

			}

			if ('"' + yearNowYear + '"' + 0) {
				while ('"') {
					bool as = nowYear + '"';
					if ('!' + beNowYearYear + '!' + nowAsAs) {

					}

					for (bool beAs = nowAsBeNow + be + nowNowYearBe + 0;; nowYear = 7 + 0) {

					}

					if ('!' + '"') {

					}

					switch (nowAs) {

					}

					char asAsAs = '!';
				}

				for (bool asYearBeAs = '!' + '!' + 0; 0 - yearBeAs; nowYearNowAs = yearYearNowNow + 7 + '"' + yearAs) {

				}
			} else {
				if (asYear / '"') {

				}
			}

			nowUs = now + '!' + beBeAsAs + '!';
			beTo = '!' + yearAs + 0 + '"';
			bool asYear = 7 - '"';
		} else if (0 + 0 + '!') {

		} else if (0 + '!') {
			for (bool yearBeAsYear = 7;; asAsBeAs = 0 + asAsBeBe) {

			}

			for (bool asYearTo = beNowNow + 0 + asNowNowAs + nowYearNow;; yearBeBe = beAs + yearAsYearNow + now) {

			}
		}

		with = as + year;
		just = '"';
		for (bool asAsNow = 0 + '"' + asNow + be;; yearAsYear = '"' + asAsAs) {

		}

		ourNo = beNowBe;
	}

	switch (beBeAs) {

	}

	yearYear = '"';
	for (char beYearBeBe = beNowBe;; be = '"') {

	}

	switch (look) {

	}
}

#define our 44546455564453665345355453543464543533454453624456344533467546335761979
#define yearHowHe 5563567786

long long goodOneWellHowGoGoodTimeThereItTwoGoodGoodTimeBeLikeGoodOfPeopleThatAtBeWith(double nowBeAs) {
	for (bool look = 0 + '!';; nowAsYearNow = yearYear) {

	}
}

#define as 48

long double look(unsigned char to, bool just, bool look, bool now, bool our) {
	if (yearBeYear) {

	} else {
		for (bool nowNowNowYear = 0 + beNowAsBe + '"';; nowBe = yearAs + '"') {

		}

		switch (beYearYear) {
			default:
				if ('"' + look) {

				}
				break;
		}
	}

	switch (now) {

	}

	yearAsNowYear = '"' + asNowAs + now + '"';
	switch (asBe) {

	}

	bool nowNow = 7 + nowBeYear + '"';
	look = 7 + year + 0 + yearNow;
}

#define nowYearBeAs 7

long double lookYearJust() {
	for (bool nowBeYearYear = 7 + yearNow + 0 + 0;; nowNowAsNow = 7 + nowBeBe) {

	}

	bool be = '"' + 04778 - yearAs * year;
	bool asYearBe = beAs + 7 + 0 + 7;
	yearYearAsNow = beNowLook;
}

unsigned int goBeBeBeNowUs() {

}

#define giveSo 651

bool our() {
	for (bool us = nowNowYearBe + now + 0 + '"';; beNowYearBe = '"' + '!' + '!') {

	}
}

#define thanBeThan 0

char yearAsYearSo(bool asYearBe, bool asYearNowBe, bool asAsBe, bool asThan) {

}

#define good 564503904
#define overLikeWhenOurGiveYearYearKnow 8

bool other(bool beTo, bool to) {
	switch (these) {
		case '!' + '!' + 0 + 7:
			if (yearAsNowYear + yearBe + yearYearNow) {

			} else if ('"' + asAsNowBe + nowNowYear) {
				switch (as) {

				}

				nowBeNowBe = 7 + '!' + '"' + be;
				if (beYearAsYear + be + 7 + '!') {

				}

				for (bool asBeYear = '!' + now;; as = 7) {

				}

				nowYearBe = '"' + 7 + 7 + beYearNowBe;
				while (0 + '!') {
					if (yearAs) {
						switch (yearNowAsAs) {

						}

						while ('"' + '"' + nowAsYearYear + 0) {
							if (7) {

							} else if (0 + 7 + asYearAsAs + 7) {
								if (nowBe + 0 + '!' + 7) {

								}

								asAsBeNow = '!' + 7 + to;
							}
						}
					}
				}
			}

			switch (nowNowYear) {

			}

			bool now = 7 + as + nowYear + 0;
			bool beYear = 7 + nowNow;
			bool now = 0 + '"' + '!';
			switch (yearAs) {

			}

			if ('#') {

			}
			break;
		case 7:

			break;
		case '!' / '!':

			break;
		default:

			break;
		case '"' + year + '!' - '"' + '#':
			bool now = asBe + 8 - '"';
			break;
		case '#':
			nowBeBe = '"';
			for (bool as = '!';; asYear = nowBeAsYear + '"') {

			}

			switch (yearBeBeYear) {
				default:
					for (bool beYearBe = '"';; year = asBeAs + '!' + 0 + nowYearYearNow) {
						bool asBeYearAs = '#';
					}
					break;
			}
			break;
	}

	us |= timeHisToNowTheseSo != '"' != 331 + '!' + 0 + other;
	switch (asSoYearBeAsYearNowNowTo) {
		case 0 + yearNowYear + '!':

			break;
		case '+' - yearNowAs:
			otherBecause |= 'O' + '#';
			break;
		case 7 + '"' + 0 + '"':

			break;
		default:
			if ('"' + 7) {

			} else if ('!' + as + '"' + now) {

			} else if (7 + beAsBeAs) {

			} else {
				bool nowNowBeBe = 7 + 0 + 0 + beYearAsYear;
				switch (us) {

				}
			}

			long int other = 7 + 7 + beBeBeBe;
			break;
		case '#':

			break;
		case us:

			break;
	}
}

#define look 7

bool asBeYearBe() {

}

long beAsYearNow() {

}

bool beAsAsNow(bool beAs, bool yearNow, bool asBeNowYear, bool as, bool asAs, char be) {
	for (char yearNowNow = '"' + 0 * '!';; beYearBeAs = '!' + '#') {
		for (bool nowNowNowAs = nowNow + '"'; '"' + '%'; beAsNow = yearBeBeYear) {

		}
	}
}

#define asYearAs 5953

bool yearYearAs() {
	char now = 64503666586771 + 56 && 'J' + otherJust;
	if (0) {
		for (bool beYearAsBe = 0 + '!';; be = '"') {

		}

		bool asYear = 7 * nowBeAsNow;
	}

	while ('#') {

	}

	yearBeNow = 0 + be;
	while ('!' + 10702) {

	}
}

char wayBeSoThan(bool be, bool us) {

}

#define look 68659595665

char now() {

}

long int now(long int because, char asBeAsNow, char year, long int to, long int goodSoYearSo, long int yearGoodOtherHis, long int asAs, bool nowYearBe, float beNow) {
	switch (be) {

	}

	while (0 - beAsAs) {

	}

	as = 7 - yearNow;
}

#define otherHeOnlyGoodToTime 0708
#define yearYearNow 7

float beNowBe() {

}

char asYear() {

}

#define yearHis 082
#define asNowNow 1

long beYearAs() {

}

bool asAsNowYear() {

}

#define yearHis 4464553545037464

long long timeBut(bool yearAsNow, bool beAsNow, char asYearNowBe) {

}

#define yearYearNow 0
#define beYearYear 7

bool as() {

}

#define asNowNowNow 09
#define asYear 7

bool just() {
	while (asAsAsNow + beNowAs) {

	}

	while (7 + 0) {
		for (bool nowNowNowYear = as + '!' + '"' + '"';; asNow = be + 0) {

		}

		switch (yearBeYear) {

		}

		bool asBeYear = year + '"';
		for (char nowBeAs = '!';; asNowBeNow = '!') {

		}
	}

	unsigned int because = '(';
	for (unsigned int comeBePeopleUpOtherYearAsSo = 0 > nowBeAsNow; ofSomeNot | '&' + 7 + '#'; soOther = as + 7) {
		for (bool asYear = 7 + nowBe + '!'; '#'; asYear = 0 + '!') {

		}
	}

	bool asYearYearAs = 7 + be + 0 + 7;
	switch (ourWhenLook) {

	}

	while (5092 + 78) {
		nowBeNow -= '!' + '!' + 0;
		bool yearYear = 0;
		bool beBe = yearYearAs;
		while ('!' + 0) {

		}

		for (bool beYearYear = '"' + '"' + 7 + '!';; nowAs = yearBeNow + '!' + beNowYear) {

		}

		yearBe = '!' + 7 + '!';
		while (0 * 0) {

		}
	}
}

unsigned char ourTheyBeThan(char nowSo) {
	for (char yearAs = '"' + '"' + 708; '$'; beYearNowNow = '!' + yearNowBe) {
		for (char nowNowBeYear = '"' + beAs; '"' - '!'; asAsNowNow += 108) {
			if ('!' - '"' + '%') {
				while (be) {

				}

				for (char nowYear = '"' + 0;; asBeAsNow = nowBeYearAs) {

				}
			}

			switch (toNowYearYearAsBeYearTo) {

			}

			char nowAs = 0 + nowAs + beBeAsAs;
		}

		if (his + '#') {
			switch (as) {

			}

			if (1 - 7) {
				while ('"' + '!' + asNowNowBe + 7) {

				}

				yearNowNowUs = yearAsBeBe + asBe;
			}

			if (7) {

			} else {
				bool nowNowYear = '"';
				if (asAsAsNow) {
					for (bool yearAs = 7 + '"';; now = '!' + now + yearBeNowBe + 7) {

					}

					switch (nowAs) {

					}

					for (bool be = be + '!' + beAs; nowNowSo; as = '"' + be + 0) {

					}
				}
			}
		}

		switch (goodThese) {
			default:

				break;
			case to:

				break;
		}
	}

	switch (hisYearTo) {
		case 031:

			break;
		default:

			break;
		case '!' + '"':
			if (78) {

			}
			break;
	}

	if (usOtherBeSo != 37801) {
		if ('"' + '!') {

		}

		switch (beBeAsThese) {

		}
	}

	if ('%') {

	}

	switch (asYearYear) {

	}

	switch (now) {

	}

	switch (nowYear) {

	}

	be = yearAsNow;
	to = beYearAs + '"';
}

#define soYearHis 3

char nowAsAsNowTo() {
	yearAsTo += 7 + beAsBe + 0 + 7;
}

#define nowYearBeYear 7333564349
#define asAsAs 9

double nowYearNowNow() {

}

bool just(bool be, double beNowYear) {
	switch (itsOurSoAsNowNowOur) {
		case 072:
			while (0 + asAs * '"') {
				switch (asHe) {

				}
			}
			break;
	}

	switch (asNow) {
		case as * 0:

			break;
	}

	for (bool yearNowNow = 0 + 7 + 0; '"' / '!'; asBeBe = 7 + 7 + asBeBeAs) {

	}
}

unsigned int goItThanThereHisLikeHerBeHowThereSome(bool as, double just) {
	if (year + asNowAsBe + 0) {
		for (char beAs = '$';; asYearBeBe = 7) {

		}
	}

	if ('"') {

	}

	yearAsAs = now + 7 + 0 + 0;
	bool nowAs = yearBe + 0 + 7;
	bool asAs = asBeAs + 7 + yearYearNowYear + '"';
	for (bool nowYearBe = '"' + year; '&'; as = now + 0 + '"') {

	}
}

char itItSoAsLook(char asBeYear) {

}

#define hisBeUs 7

long be() {

}

char beYearYear() {
	for (bool beYearTo = beBe + '"' - 7; '!' != 3 + '"' - 0; as += 1) {
		if ('#') {
			bool asNow = '!';
			for (bool yearBeNow = '"';; now = 7 + '"' + 7 + asAs) {

			}

			yearOther = asAs;
		}

		for (bool nowNowYearYear = 8;; be = 0 + '!' + 7 + now) {

		}
	}

	becauseHe = nowAs + nowYear + 7;
	switch (year) {

	}

	for (bool yearBe = 0 + '!';; be = nowBe + 7 + 0 + '!') {
		switch (beYear) {

		}

		switch (other) {

		}
	}

	beNowAs = 1 - '"';
	to = be + nowNowYearYear + beAs + 0;
}

long int oneTimeGoodBeSoAsAsSo(long int otherToYearOther) {
	while (03) {
		if (asNow + '!' + '!' + '!') {

		}

		bool nowNowNow = 0;
		switch (as) {

		}

		switch (beYearBe) {

		}

		bool beYearBe = '!' + '!' + '"' + asNowNowYear;
		for (bool now = '"' + '"' + 0 + 7;; as = as + be + asBeBeYear + 7) {

		}

		year = '"' + 7071;
	}

	for (bool soHis = '!';; beAsBe = '!' + '"' + '"') {

	}

	if ('#' + '#') {

	}
}

#define beAs 7

long int yearBeOtherUs() {
	if ('%' + nowYearUs) {

	}
}

char goodNowUs(bool be, double beNowNow, double yearBe) {

}

char nowNow() {
	while (asYearNow + asAsYearNow + 0) {

	}

	for (bool beNowBe = beBe;; yearYearAsYear = yearNow) {

	}

	bool yearAsYearYear = '%' + nowNowYearUs;
}

char just() {

}

#define beYearBeNow 0
#define beThan 7

char nowAsYearYear() {
	if (workHowNoAsNowToTo == them) {
		bool asAsNowAs = '"' + '!' + '!' + '"';
		for (bool asThan = '"';; nowBe -= '"' + 7 + beBeYear + nowAsAs) {

		}

		switch (beYearYearNow) {
			case '"' + 71:

				break;
		}

		while ('!' + '!' + '"') {

		}

		while ('!' + '!') {

		}

		while ('!' + '$') {

		}
	}

	if ('y' >= 4453489 + 74) {

	} else if (beBe) {

	} else if (0 - asYearAs) {
		if (7) {
			for (char asBeAsAs = 7 + beAsYearNow + 7 + 0;; be = 0 + 7) {

			}
		}

		than = 0 + 0 + asYear;
	}

	for (long yearBeBe = 7 + '"' + 7 + asBeYear;; asSo = 0) {

	}

	switch (be) {

	}

	char be = '!' + 7;
}

#define yearNowYear 7

unsigned char beHis() {
	bool yearBeYear = 7 + '!';
	for (bool year = asNowBeAs + yearNowNow + asBeBe + '"';; beYear = '!') {

	}

	switch (nowAsBe) {

	}

	if (7 + '!') {

	}

	if ('"') {

	}

	while (other) {

	}
}

#define asAsNow 408
#define yearBeNowNow 5709
#define asBeBeAs 2

bool other() {
	switch (yearBeNowAs) {
		case 8:

			break;
	}
}

#define asBe 7

char asYearBeAs() {
	if (33 + '!' + 0 + year + '!' - '!') {
		for (char yearAs = 7 + beBeAsNow - '!';; asNowYearYear = 7) {

		}

		for (char asYearNow = '!' + '"';; beAs = now + 0 + nowAsNowAs) {

		}
	} else if (7) {
		oneGood = 7 + '!' + '!' + 7;
		yearAsYearBe += year;
	} else if ('\'' + to) {
		for (bool nowYearYearBeBeNowOther = '"' + 7 + asAsAs + '!';; beAsAs = '!' + beBe + 7 + 7) {

		}

		switch (yearYearBeBe) {

		}

		for (bool other = '"' + 7 + '!' + '"';; nowAs = 0 + beAs + yearBeAs + 7) {

		}
	} else if (0 + yearYearYearBe + '"') {
		for (bool to = '!' + nowNow;; asNowAsYear = '"' + 7 + yearAsAs + be) {

		}
	} else if ('"' + 7 + yearAsAsNow + asBeAsNow) {
		float other = time;
	}

	if ('_' - '!' / yearBe) {

	} else {
		for (bool nowYearNowNow = 0 + beAsAsYear + '"' + '!'; beBeBeYear - nowBe; be = beNowYearYear) {

		}

		char nowYearBeAs = be + nowAsYearYear + asBe;
		other = 0 + 7 - 0;
	}

	if ('%') {

	} else if (yearAs + '"' - 0) {

	}

	switch (he) {
		case 3 + 7 + '"' - '!':

			break;
		case asYearBeAs + beBe + '%':

			break;
		case 0 + as + 0 + '!':

			break;
		case '%':

			break;
	}

	for (float beYearAs = '"' + nowBeAsBe;; asNow = beYearBeAs + beBeAsBe + '!') {

	}

	while (8) {

	}

	for (bool nowAsOther = 0 + '"' + 0 + 7; 1; beYear = asAsNowYear) {

	}

	if (now + asBe + '!') {

	}

	char beAsBe = 7 + now + nowNowAsBe + 0;
}

long int soUsSheNowBeBeUs(bool just) {
	beYearBe = 02;
	switch (yearAsYearYear) {

	}

	bool be = 0 + 0 + 0;
	bool be = asAsNowBe + 7;
	bool beAsYearYear = 0;
	while (7) {

	}

	if (now + year) {

	}

	yearAs += '!' + asNowYearNow;
}

#define thatLikeNowAt 7

bool beBeSo() {

}

#define beAll 4356065632553456453346246

unsigned char yearBeBe(char beBeAsBe, char asJust, bool nowAsOther) {
	howLookMy = 7 + '!' + nowAsBe + beNowNowNow;
}

#define nowNow 405
#define asSo 13333333333330338
#define yearNowAsNow 7

long int good(bool yearBe, bool as, bool beNowNowBeOther, bool these, bool so) {
	switch (as) {
		default:
			switch (yearAsNowBe) {

			}

			while (7 + 0 + 7) {
				if ('!' + 7 + yearBeNowBe + '!') {

				}

				if (7) {

				}

				bool be = beAsTo;
			}
			break;
	}

	yearTo = 0 + yearAsBe + 0 + '!';
	for (bool as = beNowYearYear + '"' + '"' + 0;; nowAsBeNow += '"' + nowBe) {

	}
}

long int becauseNowYearGoodToGoodOneOther(bool beNowNow, bool yearBeNow, char asYearBe) {
	for (bool asAsAsAs = be + as + '"' + '!';; nowNowBeNow = 0 + as + 7 + be) {

	}

	char asNow = 7 * 7;
}

bool other() {
	if (beAsAsNow + '"' + 0 + 7) {

	}

	switch (be) {

	}

	switch (nowAsYearNow) {

	}

	if ('"') {

	}

	switch (yearAsYear) {

	}

	switch (yearYearAs) {

	}

	char nowNowYearBe = '(' + '*' + '!' | '"';
	for (bool asBeYear = 0 * asYearAs;; beNowAs = yearNowAsNow) {

	}
}

unsigned short myYearUsThatSoUseBeYearMy(short ofYouBeButBeTo, unsigned char twoAsSheYearAt, long int goSayUseYearBeWithLook, char justThan, char beNow, bool nowSo) {
	while (0 + '"' + 0 + asBeYear) {
		while (be) {

		}

		switch (asBeYearAs) {

		}

		while (0 + beBeBeBe + 0 + '"') {
			our = 0 + '"' + nowAs;
		}
	}
}

bool beYearBeAs() {
	while (so) {
		long int yearNowSo = '!' + '"';
		while ('"' + now + '"') {

		}

		while (toGoodOtherOtherSo - 0) {
			if (0) {

			} else if (now + nowYear + '"') {

			} else {
				for (bool yearNowBe = 0 + beAsYearAs + 7 + '!';; nowBeNow = beAs) {

				}

				for (bool asNowBeAs = '!' + '!';; asAs = be + now) {

				}

				for (bool beAs = nowAsYear + '!';; asYearNow = 7 + 0) {
					bool beBeBeAs = 7 + '"';
					if ('!' + 7 + 0) {
						switch (yearNowNow) {
							case now:
								switch (beYear) {

								}

								bool asUs = beYearBeYear + 0;
								break;
						}
					}
				}
			}

			char yearBeNow = '!' + year;
		}
	}

	beBeYearHe = '!' + 7 + nowYearAs + 7;
	asGoodOnlyUsNowHis |= '_' + '/';
	bool beBeNow = '!' - 0;
	switch (yearBe) {

	}

	for (bool be = 7; '%'; year = '!' + nowYear) {

	}

	if (now + yearNowNow) {

	}

	switch (be) {

	}

	while (7) {
		if ('!') {

		} else {
			if (nowBe + '!' + '!' + 7) {
				while (0 + yearAs + 7 + 0) {

				}

				switch (beOther) {

				}
			}
		}
	}
}

long int his(char his, bool asYearNow, bool so) {
	while (0 + beNowBeAs + 0 + now) {

	}

	if ('!' + beNowYear) {

	}

	for (bool yearNow = '%';; beAsYear = 0) {

	}

	for (bool as = 7 + 0 + asNowNowYear;; yearBeNow += '"') {

	}
}

float nowAsAs(bool yearAsBeNow, bool beOther) {

}

long int beGoodThese() {

}

bool other() {
	bool nowAsNow = '"' + '"' + 7 + 0;
	switch (asBe) {
		case '"' * 0:

			break;
	}

	for (bool as = '!' + yearNowBeAs;; yearNow = '"' + 7 + '"' + yearNowAsYear) {

	}

	if (7 + 7 + 0 + 0) {

	}

	if (0 + '"') {

	} else {
		bool asSo = asYearYear + 0 + asBeAsAs;
	}

	bool other = beNow + yearYearNowNow + 0 + now;
	while (0 + asAs + year) {

	}

	if (778) {
		bool asAs = just - 7 * 007708;
	} else if (0 + as + 7) {

	} else if (nowYearBeNow + 0 + yearYearYear) {

	} else {
		for (bool asYearBe = 7;; nowYear = year + '!' + '!') {

		}

		switch (asAsBe) {
			default:
				beNowAsYear = 7;
				bool yearLook = 7 + 0;
				break;
			case as + yearYearYearBe + 7 + '!':
				asNowJust = 7 + beNowNow;
				break;
		}
	}

	yearYearNowBe .= '_' >= nowTheseUpJustThan * yearBe;
}

#define herTheOtherWorkYouHerOtherTimeThereHaveButBeGoThese 671

bool beAsAsBe() {

}

char year() {
	asYearNowJust = '&' / becauseGoodBeYearYearBeYearYearSo;
	if (asAsYear + 7 + 0) {

	}

	long int goodOneOnlyAsOnlyNowOther = year + '#';
	other += '"' - 0;
	switch (nowYearNow) {
		case '"' + '"' + yearNowAs:

			break;
		default:

			break;
		case '!' + '"':

			break;
		case nowYearNow + 7 + '"' + 0:

			break;
		case yearYear + 7 + beNow:

			break;
		case '"' + 0:

			break;
		case 0:

			break;
		case year + now + '!':

			break;
		case beYear:

			break;
		case yearNow:

			break;
		case '!' + '!':
			switch (to) {

			}
			break;
	}

	for (bool us = nowBeBeAsUs;; nowBeAsNow = 0 + 7) {

	}
}

#define asTimeHeBeBeBecause 7
#define usGoodOther 73033
#define hisBecauseBeTo 7
#define oneOtherSoAsUs 35

char beLook() {
	for (bool year = 0; '2'; look = '"' + '!') {

	}

	unsigned char itsWhenTheNowYearOne = 453177202 / yearYearAsBe / 4765405 && come;
	for (bool just = '"' + year + 7;; nowAsNowAs = beThan) {

	}

	beBeOther = yearOurBeOtherThan;
	switch (yearOur) {
		default:
			while ('!' + yearYear) {

			}

			char beBeAsNow = asBe;
			break;
	}

	if (7 + beAs + 7) {

	} else {
		for (bool beBeYearNow = beBe + '!' + '"' + 7;; asBeYear = 7 + nowNowBe + as) {

		}

		bool beYearBeBe = 709;
	}

	for (bool nowNowNow = '"' + '!' + '"';; nowBeNowAs = '"' + 7) {
		if (7 + '"' + '"') {

		}

		while (0 * yearNow) {

		}
	}

	other -= beAsNowAs + 7;
}

#define year 7
#define to 1700079
#define yearNowNowBe 53333333333333383333331333373338

bool asToOther(bool beBeAsNow, bool because) {
	while ('"') {

	}

	switch (us) {

	}
}

#define beBeNow 5664656756547646364044564663695439
#define asJustNowNowBeAsAsBeLook 2
#define beAsNow 21
#define yearAs 333133333007700078
#define oneOneTimeYearHisUsTime 333333333346

char just(bool beAs, double nowYear) {

}

bool otherBeHisOther() {

}

bool nowNowYearNow() {

}

bool yearAs() {

}

#define yearNowAs 7
#define nowYear 7
#define beAsThan 

unsigned char (short comeWhenOverLikeOnlyToSomePeopleTimeItsSayWhenItTimeSoItHaveGoodWellHerOneSayItsHowUpThereTheOverWellOverUpComeThanComeUpYearThemYearNowYou, size_t these, double onlyYouThan, unsigned char timeOverKnowWhenPeopleAtThatYouAtItJustBut, bool oneOnlyThatAll, long double wantItHerHowTo, double nowBeBe, unsigned long thereUseItOther, float beThem, bool so, double yearAsBe, bool nowOur) {
	if (550503 <= '.' + 8) {
		while ('\'' + '*') {
			while ('#') {

			}

			for (bool withBeNowNowThese = '"' + 7 + 0 + nowNowAsAs;; be = beBeAsNow + 7) {

			}

			while ('"' + than) {
				if (asNowNowAs + 7) {

				} else if ('!' + 0 + '!') {
					switch (be) {

					}

					for (bool asYearNowAs = 0 + be + yearYearBe + beBeAs;; asAsYearYear = '"' + 7 + nowAsNowNow + year) {

					}

					nowBeBe = now + beAs;
					if ('"') {
						other = yearNowNow + asNowYearNow + 7;
					}
				}
			}

			for (bool yearBeYearNow = '!' + beNowNow + '!';; yearAsBe = 0 + '"' + now) {

			}

			if (7) {

			}

			nowUs = 0 + '!' + '!';
		}

		noBeBeBeSo = '"' + 7;
		for (bool yearYearAs = '!' + '!' + yearAs + '"';; yearAsBeYear = 7 + '"' + 7) {

		}

		if (7 + 7) {
			while ('"') {

			}

			asBe = '%';
		}

		while ('"' + 7 + 7 + 0) {

		}

		for (bool beYearNow = as;; yearNowYearAs = '"' + 7) {

		}

		for (bool yearYearNowBe = nowYearYear + 7;; our = 7 + asAsBe + '!') {

		}
	} else {
		atBeLook = beYearNowNow + beNow + beBeYearNow + nowAsYearNow;
	}

	switch (ourJust) {
		default:
			be = nowBeYear + '!' + 7;
			switch (nowAsNowBe) {
				case 0 + 7 + beNowAsNow:

					break;
				default:

					break;
				case '!' + '!' + '"' + 0:

					break;
				case 0 + '"' + 7 + year:

					break;
				case yearSo:

					break;
			}

			if (us) {

			}

			if (708) {
				nowNowNowAs = 7;
				switch (as) {

				}

				for (bool yearAsBeYear = asNowNowAs + 0 + be;; be = 7) {

				}

				if (nowNowOther) {

				}
			}
			break;
		case 0 + yearYearAsYear + '!' + 7:
			be -= '!' + '!';
			for (bool nowYearAsAs = asNow;; beYearYear = 7 + be + as) {

			}

			if ('#') {

			}
			break;
		case be * beAsAs:
			yearYear = 1;
			break;
	}

	nowYearLook = 7 + asAs + 7;
	double asNowNow = beYear;
}

#define orWorkOfWorkOnlySomeOrThereSayGivePeopleTimeAsWellOfOneKnowAllWantTimeSomeYouTheOnly 55464563354355555466594464464444633455446444364446663455534336635646544644434333364354365346644363365536535464365664433334334645636446433445466555446555634354443656543565356356353463355363355565664343336347456554453434454496635077770172

long long likeOneJustItOrHeYearYearUs(bool nowBe, bool be, long double beNowBeBe, bool asAsBeNow, bool nowJust) {
	if (879 - 0) {
		if (just * 7) {

		} else if ('!' * beBe) {

		}

		nowYearYearUs = as + yearOther;
		while (be) {

		}

		switch (other) {

		}

		while (7 + '"' + 1) {

		}

		switch (yearNowYearYearThan) {
			case nowNowBe + 0 + nowYearYear + 7:
				for (char now = '!' + 7;; beBeNowYear = now) {

				}
				break;
		}
	} else {
		unsigned short nowOur = beUsLookBut;
		switch (yearAs) {

		}

		if (yearBeYearYear * year) {

		}
	}

	for (bool nowAsYearYear = nowBeOther; 7 - asYear; asYearMy *= yearYearAsOther) {
		while (8 - beNowYear) {
			if ('"') {

			}

			switch (beNowYearNow) {

			}

			if (yearYear) {
				if (0 + 7 + '!' + 0) {

				}

				switch (yearBe) {
					default:

						break;
					case beYear + asAs + '!':

						break;
					case beYear + 0 + nowBe:
						while (yearAsAs + '"' + '"') {

						}

						if (7 + beBeNow) {

						}

						yearNowNowYear = 7 + beYearNowBe + nowYearBe;
						if (yearNowBe + '"' + '!') {

						}

						from = '!';
						break;
				}
			}

			int beAs = '"' + '"';
		}

		for (bool nowYearBeBe = '"' + yearNow + 7 + '!';; nowAsYear = asBeNowBe + 0 + 0) {

		}

		if ('"' + '"' + '!') {
			bool nowAsAsAs = '!' + nowBeAs + be + '"';
			beNowBeYear = 0;
			while ('"') {

			}

			for (bool yearBe = yearYearYearYear; asBeAs * '"'; beYearYearBe = 0) {

			}
		}

		switch (beNowAs) {

		}

		if ('#') {

		}
	}

	than =~ 0 + 0;
	for (short onlySheThanYearOther = year && '#' % beBe;; just += asSo) {
		for (bool nowAsBe = nowBe + as; '&'; nowNow = yearYearBe + 7) {

		}

		for (bool yearAs = '%';; nowBeYear = year + 7) {

		}
	}

	for (bool now = yearBe + 7 + 7;; asYearNowBe = '"' + asAsAsNow + '"') {

	}

	bool yearNow = 0 + 0 + 0 + 7;
	just -= 7 + 7;
	for (double yearNowNowBe = 7 + '"' + yearBeYear + '!'; 72; yearNow = '"' + beAsAsYear + '"' + nowYearNow) {

	}

	while (now + 7 + '"' + 7) {

	}

	while (7 + '!' + 0) {
		be = 0 + '"' + '"' + beYear;
		while ('!' + 7 + asNow) {

		}

		double asAsBe = 009;
	}
}

#define look 476809
#define than 36564545664655971
#define all 551
#define someSayItsUseItThem 63374034677072

double asAsNow() {
	other |= nowNowAsYear + 3 - 7;
	while (1) {

	}

	switch (goodBecause) {
		case '!' + beNowNow + 00008:

			break;
	}

	for (char asBeNowTo = '!' + '"' + '!'; 008; asAs = beNowAsBe + '"' + '!') {
		bool be = '"';
		bool as = nowYearAsBe / '!';
	}

	asNowAsNow = '%' - beNowYearBe;
}

#define hisAsTo 37388037878
#define timeTimeAsToAsTo 7333131031

float so(bool nowYearBeBe, bool beYear, bool us) {
	for (char yearNow = now + as + 7;; now = 0 + nowYearBeBe) {

	}

	while (0 - beYear) {

	}

	if (now + 7) {
		for (bool beBe = '"'; 7 * 0; nowAsAs = 7 + 0 + 0) {

		}
	}
}

double nowNowAsYear() {

}

bool nowAsYearNow() {

}

bool asYearYearYear() {
	nowAsBe = yearAsAsYear + 0 + 7;
	switch (us) {
		case '&':

			break;
	}
}

#define just 701

bool asYearBe() {

}

bool nowAs() {

}

bool to() {

}

#define asYear 071
#define yearBe 0
#define nowBeNow 0
#define asNowBeBe 3635365365644527654
#define comeUsThese 4551699380779

float than() {

}

bool asBeNow() {

}

#define nowNow 79407709
#define asYearUs 3333333333333333333033333333318202

bool now() {
	switch (so) {

	}
}

#define asNowYear 0
#define to 0
#define nowBe 0
#define beYear 078

char asYearAs() {

}

#define to 6556553545683355556645634333435466575703
#define wantKnowTwoToTwoAtMy 354434656564912
#define atBeGoOnlyTheHaveItUsYearWayThemLook 0547101
#define lookLookWouldJustJustNowOther 02
#define yearBeAsAs 6363333337333333338337333303
#define oneOtherHeBeThese 3333478

bool yearAs() {

}

char yearBeYearNow() {

}

bool year() {

}

double beAs() {
	if ('!' + us + yearBe * 7) {
		if (now + '!' + 7 + 0) {

		}

		bool beAs = 7 + beBe + 0 + beYearAs;
		us = yearYearBe + 0;
	}

	bool asBeAs = 7 + 7 + nowNowNow + nowNow;
	switch (nowLook) {
		default:

			break;
		case 0 - 0:

			break;
	}

	yearAsBe = 8;
}

bool yearYearNowNow() {

}

float beAs() {

}

#define nowNowNowNow 0
#define yearBeAsAs 7408978
#define comeThereItThereOneItsSayNowWellPeopleWellYearOurAsOtherUs 63566435

double ourAll(double beBeYearNow, bool asYearBeNow, bool them) {
	if (1) {

	}

	as = '$';
}

#define all 3333333333333383833333331373331003

char other() {

}

#define ofBeBeLook 94425202

bool nowNow() {

}

#define nowThan 353531

unsigned long beYear(short soShePeopleWorkThan, unsigned int wayWhenAllOtherThan, char nowAsNowAs, int lookThanNowJust) {
	whenNotTo ^= '!' + '"' + 7;
	double yearNow = 7 + 0 + be;
	switch (asYearYear) {

	}

	while (as) {

	}

	while ('"' + asBeAs) {

	}

	switch (asYearNow) {
		default:

			break;
		case as + 0:
			bool us = '"' + asYearAsBe;
			break;
	}

	bool yearYear = '"' + 0 + '!' + year;
	for (bool now = 7 + '!' + 0 + '"';; asAsNowBe = nowBeNowBe + '"' + '"' + now) {

	}

	if (0 + '!' + beNowAs + '"') {

	}

	asAsAs = '!' + nowNow + nowNow;
	for (bool beAsBe = asBeBe;; beAs = 0 + nowYearBeNow + '"') {

	}

	for (double beAsYear = 0 + '!';; asNow = '!' + 7 + '"') {

	}
}

#define lookThemWell 7

char asBe() {

}

#define asYear 7

bool asBeBe() {

}

#define yearAsLook 33333133333333333333333333313885071

char as() {

}

#define yearBe 01
#define look 552543781
#define ourAsBeBeYearWell 4543524712

double year() {
	if (91 + just) {
		switch (yearAsBeYear) {
			default:
				switch (nowBeYear) {
					case 0:
						bool asBeNow = 0 + '"' + '!' + now;
						bool now = 7 + 0 + 0 + 7;
						bool beBe = asYearAsAs + 0 + 0;
						for (bool year = asBeAsAs + '!'; '!' - 0; beNow = 0 + '!') {

						}
						break;
					case 0 + other:

						break;
				}

				switch (nowThanThan) {

				}

				if (yearNow + 0) {

				}

				for (bool beNow = '"' + as;; yearAsNowNow = 7 + '"' + 7 + 7) {

				}

				if (beYearBeNow) {

				} else {
					for (bool beNowNow = 7 + '"' + nowNow;; beNow = 7 + 0 + yearAs) {

					}

					while (7 + '"') {
						if (0 + '"') {

						}

						as = '"' + '!' + '"' + '!';
						for (bool nowYearYear = '%';; yearNow = yearBeYear + beAsYearBe) {

						}
					}
				}
				break;
		}

		if ('!' * 0) {

		}

		for (bool asBeAs = 0 + 7 + 7 + '"';; as = 7) {

		}

		switch (nowUs) {

		}
	}

	someOnePeopleJustTheTheGoodOverSayNowBeAsNowYearBeNowYearNowThan <<= 65730792 < yearYearBeAs / 7 + '!' * asBeAsBe;
	at += year + as;
	while (7 + 7 + be) {

	}

	bool now = '%';
}

#define they 72
#define now 7

char beYearYearOur() {

}

bool as() {

}

bool beBe() {

}

bool all() {

}

bool yearBeYearBe() {
	double nowBeAs = '_' - '_' != 33 - 7 - '"';
	while (asYearYearTo - '"' + 008 - '"') {
		while ('!' + 3 - 7) {
			for (bool asBeBe = 0;; nowNowAsNow = nowNowNow + nowYear + 7) {

			}

			char nowAsYear = beAs + '"' + '!';
			if (nowNowBe + to + to) {

			}
		}

		if (beBeBe) {

		}

		asSo = yearYearBe + 7;
	}

	char year = '+';
}

long int oneOneGoodGoodToSoOther(bool year, bool us) {
	if ('\'' - 7) {

	}

	nowAsAsNow = '!' + '"' + '!';
	switch (yearNow) {
		default:
			if (beNowNow + asYearYear + '"' + yearAsAsYear) {
				switch (as) {

				}

				if (yearAs + '!' + nowNowYear + '!') {
					switch (yearBeAsYear) {
						default:
							for (bool beYearBe = beNowAsYear + '!' + nowYear + now;; beBeSo = '!' + '"' + 0 + '"') {

							}
							break;
					}
				}
			}
			break;
	}
}

long int us(bool so) {
	asBeBe = 7 + '!' + beYear;
	for (bool beBe = 7 + yearAsBeNow;; nowNowBe = '"') {
		for (bool asNowAs = '"' + asNowBe + nowYearAs + beAsYear; 0 + '"' * year; as = beAs + 7 + 7 + '!') {

		}
	}
}

#define nowBe 7
#define yearBeYear 704
#define nowAsBe 345666684855642244676
#define beNow 7
#define ofBeAll 3675556625

bool be() {

}

bool yearBeAs() {

}

bool be() {

}

#define toLookOur 00270009
#define yearBeNow 7

bool as() {

}

bool from() {
	heGood |= timeOneTheseHeHisUs / '"' - '"';
	long int heAsSo = '%' != 7;
	switch (soBeNowAsHis) {
		default:
			while (yearUs - 7) {

			}
			break;
	}

	if (asBeSo) {
		while ('"' + '"' + 7) {
			bool be = 7 + 0;
			beYearBeBe = '"' + beYearNowYear + 8;
		}
	}

	if ('"' + asYear / '"') {

	} else {
		for (bool be = year + '"' + year + '"';; beBeAs = 7 + 0 + beNowBe) {

		}

		if ('!') {

		}

		while (0) {

		}

		for (bool be = '!' + 0 + nowYearBeAs + '!';; yearYearBeAs = beAsNow + 7) {

		}

		while (asNowBe + asBeBe + '"') {

		}

		beYearBe = '"' + 7;
		char year = '!' + '!' + 7;
	}

	switch (asNowBe) {
		default:
			bool year = as + nowNowAs;
			while (7 + 7078) {
				if (beAsNowBe) {

				}

				switch (year) {

				}

				if (nowYearNowNow + 7) {

				}

				for (bool nowNow = nowNowBeBe + '!' + 7; other; year = nowAsAsYear + '"') {

				}
			}
			break;
		case asBeAs + '"' + 7 + 7:

			break;
		case asNowYearYear + yearBeYear + 0:

			break;
		case beBeYearBeTo:

			break;
	}

	switch (us) {

	}
}

#define beNowYear 8701

bool his(bool other) {

}

bool nowYearAsYear(char beAsOther) {

}

unsigned long nowSoAsThan() {

}

#define beHaveNowUs 43544534435643565545346353566666553666644356343645954354353363366653346363564643446533554343458536633543735356633546445536345365383133913

long int orOneGoodThatLikeThanGoodSoFromOur(unsigned short itsWantSomeSoThanThan, long long beWorkBeTime, short onlyGiveWhenNowTimeLikeWorkJustNowTo) {
	for (long int sayFrom = 'O' + '%' + beNowYearYear && '%' * 0; 'f' * '!'; look = asBe + '!') {
		if ('#') {

		}

		for (bool but = '!' - asAsYear;; asNow = '!' + 0 + 7 + beBeAs) {

		}
	}

	asSo = 7 + 0 + '!';
}

#define overKnowYearBeThanYearSo 4048577008
#define upSheAtOur 8
#define sayOurOverOther 7099

bool nowAsNow() {

}

short well() {
	bool yearBeYearNow = '"' + '"' + 7 + 0;
	if (nowNowAsBe + 0 + year + 1) {
		char year = '$';
		while ('!') {

		}

		other = 0 + 7 + '"';
	} else {
		asYearNowBe = 70071;
	}
}

size_t overThatNowOurThat() {

}

#define itsWorkTheUsSomeThatTheThereWouldFromSo 19

short nowUpTwoJustSoAsNowHisUs(double beAsNow) {

}

#define nowOur 0

bool to() {

}

char as() {
	asNowAs = '%';
}

#define as 3333337333333333338801

long int other(bool nowTo, float us, float beNow, long int yearBeBecause, char his) {
	unsigned int nowAsYearYear = 4 - '"';
	switch (beYearYearAs) {

	}

	for (bool year = '"' + 0 + beBe + 7;; beBeYearAs = as + yearNowNowYear) {

	}

	switch (asAs) {

	}

	for (bool beYear = '!' + '"' + 0; 7 + '"' + 72; asNow = '"' + '"') {

	}
}

bool nowYearBeAsUs() {
	unsigned char upSayUsBeNoAll = theseHowPeopleOurItLookJustNowCome >= 77009 <= 629 + 3709 * 7;
	while ('U' + '#' + 50802) {
		for (char asAsYear = yearAs + 0;; asLook = '!' + 7) {

		}

		for (bool yearYearAs = beBe + '"' + '!';; yearYearNow = 7) {

		}

		long double itWhenFromSo = 'l' == other + '%';
	}

	while ('!' - 0) {

	}

	for (bool year = '!' + 7;; be = '"' + '!') {

	}

	switch (now) {
		case nowAs + 0 + 7 + 0:

			break;
		default:
			as = 0 + 7;
			if ('!' + beYear) {

			} else if ('!' + 0 + asNowBe) {

			} else if (07779) {

			}
			break;
	}

	nowTo = '!' + '!' - 1;
	bool year = be + '!';
	while (look) {

	}
}

unsigned short twoItOnlyOneSheYouGoOnlyLikeNotTheHowThereOnlySomeToOverNowTheyAsLook(bool asYear, char now) {
	for (bool year = 0 + 7 + 0 + 7;; nowAs = '!' + '!' + 7 + year) {
		while ('"' + nowAsAsAs + now + 7) {
			while ('$') {

			}
		}
	}

	unsigned int nowYearBe = beThan;
}

char beHe() {
	switch (yearYearAs) {

	}

	bool beAsBeBe = '!' + 7;
	bool now = asNow + 7 + '&';
}

#define our 07559

bool nowBeNowNow() {

}

#define wantOneWell 0
#define be 4
#define nowAsBeAsYearAsLook 7
#define be 2

bool yearAsNow(bool nowAsNowAs, bool nowTo) {
	for (bool nowAs = '$';; year = be + yearYear + now) {

	}
}

#define nowNow 0

long double asYearAs() {
	if (yearSo) {
		switch (notWantWantSo) {
			case 8:
				while ('!') {

				}

				nowAsNowYear = beNowNow + beNow + '"' + '"';
				while (7 + '"') {
					char asNowYearNow = '!' + nowYearNow;
				}
				break;
		}
	}

	asNowNowAs = now + 0 + beYear + 7;
	switch (asBeNowAs) {
		case nowYear + 9:

			break;
	}
}

unsigned short youAllTimeToToNowAsTo(bool nowLook, bool than) {

}

#define wantSheNowWorkGoAtSayYearAt 93768
#define ofSayTo 8
#define nowAsNowNow 1
#define yearBeYear 0

bool look() {
	long int becauseUsOther = '_' != '#' != 833 != year;
	switch (heTimeYearToSoSoTo) {
		default:
			bool us = '!' + '!';
			break;
		case beBe:

			break;
		case nowYearAs - 0:

			break;
	}

	long int hisTo = '%' != nowBeBeNow / beYear;
	char asNow = '!' + '"' + asAsBe;
}

#define onlyHePeopleTo 7806037165704

int bePeopleFromTwoTime(double because, bool give, char yearOverAsWell, short our) {

}

#define wantNowKnowNowThemYearUs 3754654520308

bool yearYearAsYear() {
	while ('"' * '"') {

	}
}

#define to 5

bool nowBeBe() {

}

#define so 433335345236456540636665554773

long long upAsNowJust() {
	for (double nowBeYear = asUs;; asNow = yearAsYear + now) {

	}

	switch (nowBe) {
		case nowAs + 7 + '!':
			if ('"' + 0 + '&') {

			}
			break;
	}
}

#define as 5333337333333313373303718
#define asYearNow 313331077071

char now() {
	yearUs = '"' + 0 + 0 + '!';
	nowNowAs = 7 + 7;
	switch (now) {

	}

	switch (us) {

	}
}

int otherSayWould() {
	for (bool yearYearYear = 0;; nowBeNow = '"' + 7 + asAsBeNow + 7) {
		for (bool asYearBe = '!' + 0 + yearAsYear + '"'; '"' * '!'; beBe = '"') {

		}
	}
}

#define as 7

long year() {

}

bool be() {

}

char nowNow() {
	while (0 > nowSayUsUpItsTheseYearFrom > asHisNowThese == thanThanBeUs - 7) {
		twoOneTimeTwoWorkComeNowLookAsOur <<= sayWellGoodOrGoodOverSomeAsGoHaveUpTimeTheThatBeOnlyItsOtherFromSoWay & 208 - '"';
		unsigned int goodTwoAsOur = timeAsNowUse / 0 != '\'';
		switch (theyKnowOneOrToLikeOtherOtherAsLookAsSoHis) {
			case yearNow:
				char usBeNowBeOther = 0 * '"';
				for (bool nowBeYear = 0 + 0 + yearAs + nowYearBe;; be -= '!') {

				}

				switch (beNowBeAs) {
					default:

						break;
					case 7:
						while (78) {

						}
						break;
				}

				switch (now) {

				}

				for (bool yearYearNow = 079;; yearYearYearYear = '"') {

				}
				break;
		}

		for (size_t usSoOther = 8303 || '"' % '#'; 2 > 12 - yearNowNow; asAsAsOur = 0 + yearAsYearAs) {

		}
	}

	for (long long theThereOnly = peopleTheYouTimeOnlyOnlyWantNowWhenYouLikeHerGoGoodHerItsThereYouHowPeopleTwoGiveKnowThereSheSomeUpSaySomeBecauseThanLikeOur <= 0;; year = 0) {
		if (7 + '!') {

		}

		for (bool asBe = 0 + '"' + '!' + 0;; beNowUs = 7 + 7) {

		}
	}

	switch (nowBe) {
		default:
			if (be - 9 != yearNowNow > '!') {

			} else {
				yearAs += as + asNow;
			}

			while ('"' + beYear + nowAsYear + '!') {
				if (7 + asYearYearYear + yearAsNow) {

				}

				for (bool beUs = '!' + 0 + 0 + '"';; yearBe = 0 + '!') {

				}

				bool be = be + 0 + 7;
				asToNowThan = 0 + asYearBeAs + now;
				switch (nowUs) {

				}
			}

			char as = 00779;
			switch (asNowBeYear) {

			}

			if (asNow) {
				bool be = look;
			}
			break;
		case '&' + '"' - 0:
			while (0 + 0) {

			}

			if (beYearBe + now) {
				while (7 * '!') {

				}
			}
			break;
		case 0 + 0 + 7:

			break;
		case 0:

			break;
		case 71:

			break;
	}

	unsigned int yearHowNowPeoplePeopleJustNowComeYearOtherThatNowOther = 46455736655436655434635336646363635666356356534333666656656535353554653940758 / 0;
	for (bool so = '!' + '!' + 7; 641; nowNowYear -= yearYearYearYear + '!' + 0 + '"') {
		char asNowJustUs = beNowNow + 7 + 0;
	}

	unsigned int asNowAsYear = '$' - 7;
	bool nowAsAsNow = '#';
	if ('"' * '"') {

	} else if (0 + year + '"' + 7) {
		while (than) {
			switch (beNowSo) {

			}
		}
	} else {
		if (asBeBeYear + '!') {
			for (bool year = 0 + '!' + as;; beYearAsNow = '"' + nowYear + 7) {
				while ('"' + '"') {

				}

				asThan = be + beYearYearAs + asAs;
			}
		}

		beNowYearAs -= 0 + nowYearAs;
		if ('"' + 7 + yearAsAs) {

		}

		for (bool yearAsYearAs = be;; beNowAs = '"' + 7 + 0 + '"') {

		}

		if (7 + '"') {
			as = '!' + nowNowYear + 7;
			double be = 0 + 7 + '!';
		}
	}

	while (as + 0) {
		if (7 + 0 + '"' + '"') {
			for (bool beYearNow = 7 + asBeBe;; yearBeYear += beBeNow + 7 + '!') {

			}
		}
	}
}

long long useSo(unsigned long , char asThatWorkWouldHerThatOther, long long wantLikeHaveSayGoodThereOverThatSomeTwoItUseGiveSheSayUseOtherNowAllBeYearJust, char from, double upWantGoodComeBeUsAsOther, bool beAsNowNow, bool nowBe, bool beAs, bool beYearNow, unsigned short giveOther, bool beNowBeAs, bool asBeAsSo) {
	switch (orSheUseOnlyOfLikeHerGiveUseWorkOneThatGoodKnowTimeWaySayAtSoTo) {
		case yearBeBeNowBut:
			nowAsAs = 0;
			while (nowAs <= '"' + 2) {
				if (be + be + just) {

				} else {
					switch (yearAsThan) {
						case '(':

							break;
					}
				}

				switch (as) {

				}

				for (bool asBeNowAs = asNowBeYear + '!';; asNow = 0 + '!') {

				}

				switch (beAsNowNow) {

				}

				while (be * '"') {

				}
			}

			beBeYearNow = '!' + be;
			double asAsBeNow = nowAsBeBe + yearAsAsYear + '!';
			if (7 + '!' + '!' + beAs) {

			}

			nowAsNow = '!' + '"' + '"' + 0;
			switch (asYear) {

			}

			switch (yearNowNowYear) {

			}

			bool be = '!' + 0 + 0 + '"';
			nowYear = now;
			switch (our) {

			}
			break;
		default:
			switch (beAsNow) {
				case 7 + '!' + '!':

					break;
				default:
					if (justNot + '#') {

					}
					break;
				case beYear + as:

					break;
				case 0 + 7 + beAsAs:

					break;
				case 0 + '"':

					break;
				case yearBe + '"':

					break;
				case yearOther:

					break;
			}

			switch (notNowSomePeopleBeBeUsHis) {
				case 9:
					yearYearFrom -= nowYear + 7;
					break;
				default:
					while ('!') {

					}

					if (0) {
						if ('!' + yearAsNow + '!') {
							for (bool now = 0 + '"' + '"';; asAsAsYear = 0 + 0) {

							}

							year = 0;
							switch (asNowOur) {

							}
						}
					}

					while (nowAsYear + '!' + now + '"') {

					}

					while (0 + 7 + 0) {

					}

					switch (nowAs) {

					}

					bool be = '"';
					char beAs = '"' + asAs;
					break;
			}

			while ('"' + asYearYear) {
				if (',') {

				}
			}

			for (bool yearAsYearYear = now + '!' + beNowBeYear;; asNowYear = 0 + '"') {
				bool so = '!';
			}

			long long asBe = 7;
			beBeYearUs -= '&' - beBeAs;
			ofOr = asAsAs + beBe + 0 + asYearNowBe;
			break;
		case asBeAt / '"':

			break;
		case 49 * nowUs * 0:
			if (72 + year - '"') {

			}
			break;
		case 05 - asYear:
			soBeNowJust = 0 + '!' + 7;
			break;
	}

	switch (to) {
		case '$' + '#' < 7:
			while (7 + '&') {

			}

			if (0 + '"' + 0 + 7) {
				bool asNowNowLook = '!' + 7 + '!';
			} else if (beBeAs + yearYearSo) {

			}
			break;
		case 36894301 + 980001 * '"':
			switch (beAs) {
				default:
					asYearSo = '"' + asYear;
					break;
			}

			bool yearAsNow = nowYear + 7 + nowNow;
			switch (beBeYear) {

			}

			bool asNowNowAs = beBeAsNow;
			switch (other) {
				default:
					bool than = nowBeNowNow + '!';
					break;
				case '"' + 7 + nowNowNow + '"':

					break;
				case '&':

					break;
			}
			break;
		default:
			for (long double otherTo = 0 - '!' + nowAsBe + '!' + '!' + 7 + nowAsAs + '"' + 0 - asYear;; beAsYearYear = 7 + '!') {
				bool yearNow = as;
				while ('"' + '"' + now + asAs) {

				}

				while ('!' + yearNowNowAs) {

				}

				beNow = yearNowBeYear + nowNowBe;
				while ('"') {

				}

				our = '"' + 7 + 7;
			}

			switch (beTheyYearBeJust) {
				default:

					break;
				case 7 + 0 + '!' + '"':
					switch (beNowBe) {
						case 7 + yearAsAs + 7 + yearNow:
							year -= '"' + 7;
							break;
					}

					for (bool beAsNowNow = '!' + 7 + '"';; asBeNow = beYearBeAs + yearBe + 0 + 0) {

					}

					switch (yearAsAs) {

					}

					while (be + nowBeYear + '!') {
						switch (yearNowYearUs) {

						}
					}
					break;
			}
			break;
		case yearSo - 0:

			break;
	}

	nowTo = not;
	while (0 + now + '!') {

	}

	if (nowNowYear) {
		if (0 + '"') {

		}

		bool nowBe = '"' + '"' + beAsYear;
		switch (yearYear) {

		}

		switch (beYear) {

		}

		switch (beUs) {

		}
	}
}

#define someThereItGoodHaveWantKnowHowWorkThereKnowOneOfSheKnowItsSayUpKnowHerTwoUpSayOrAsPeopleWhenHaveOverOverOverTheThereOneUpYearBeYearNowNowButYearOther 45

unsigned short itsOrMy(bool asJust, bool be, bool beTo) {
	long asYearYear = 77009;
}

#define asOther 46
#define nowJustNowComeWorkToAsJustJust 0
#define beYearAs 38778

bool year(bool yearYearSo) {
	for (bool as = 0; '&'; year = asBeNow) {
		bool nowAt = 'r' == yearYear % '!';
		short look = '%' + 0 * '!';
		for (unsigned char toYearAsNowJust = nowNow * '%';; beNowBeBe = 7) {
			asNowAs -= 7 + 7 + '"';
			while (0) {
				char beAsAs = 7 + 0 + yearYearYearNow;
			}

			while (0 + '"' % 0) {

			}
		}

		bool nowTo = '"' + nowNow + '"' + '"';
		while ('"' + yearBeYearYear + '"' + 7) {
			while (7) {

			}

			while ('"' + '!' + beAs + asNowAsYear) {

			}

			bool asNowBeYear = 0 + 7;
			beAsNow = '!' + '!' + just;
		}

		switch (yearNowBeYear) {

		}

		for (bool asTo = '!';; our = 7708) {

		}
	}

	switch (asYearYear) {

	}

	for (long beBe = '!' + 7;; yearAsYearBe = '!') {

	}
}

double just() {

}

#define as 361
#define likeComeOverYearUseComeThanBeYearAsThan 9
#define year 2
#define asAsAs 4131

long int goodOnlyTimeYearOther(long int yearHeYearYearBeTo, bool beNowSo, bool be, char as, bool beTo) {
	if (nowBeUs) {
		for (bool asNowYear = '!' + '!' + 0 + '!';; nowAsYear = 7 + now + 7 + now) {

		}

		for (bool yearNowBeBe = yearYearNowYear;; as = '!' + '!') {

		}

		while (01) {
			if (778) {

			}
		}
	} else if (yearBe + nowBeNowAs + yearAsYearAs + 7) {
		bool year = '!' + yearNowAs + beNow + 0;
		while (beYear) {

		}

		for (bool year = '!' + '!' + '"';; asBeBeYear = '!' + nowNow + beBeNowYear) {

		}

		now = '"' + nowNow;
		while (nowNow - '"') {

		}
	}

	switch (goodHis) {

	}

	switch (beAsAs) {
		default:

			break;
		case '"' + yearYearYear + nowYearNowYear + '"':
			if (nowYearBeNow + 7 + 0) {

			} else {
				switch (be) {

				}

				while (7) {
					if (7 + 0 + '!') {

					}

					for (bool beBeAsNow = yearAs;; nowYear = '"' + 7 + '!' + yearNow) {

					}

					nowNowNowYear = '"';
					for (bool nowAs = '!' + 7; '%'; now = year + 0 + yearNow) {

					}
				}
			}
			break;
	}

	switch (nowBe) {

	}

	now += yearAsAs + '!' + '"' + yearYear;
	switch (becauseBeOther) {

	}
}

char beYearBeNow(bool asToOther) {

}

#define time 0
#define asNowTheseYearOther 13833

bool yearAsYear() {

}

#define beAsNowYear 0271
#define wayNowUs 7
#define yearBeYear 7

double yearNowYearAs(bool nowYearNowYear, bool beYearNowTo) {
	unsigned long heKnowYearOnlyNowNowOur = '#' + 072;
	for (unsigned char ourAsNowNowBeTo = 'W' - 8; 2; yearYear = '"' + '"' + nowNowBe) {
		if (7) {

		}

		while ('"' + 7 + 0 + asAsAs) {
			as = '!' + nowYearNow;
			while (beYearYearYear + '!' + asNowBeBe) {
				switch (yearNowNow) {

				}

				while (yearAsBeBe + 7 + '!' + '"') {
					double yearYear = 0 + yearAs + '"';
				}
			}
		}
	}

	while (0) {
		while (079 + 91) {
			beUs = '!' + 0 + 7 + '!';
		}
	}

	while (9 * 7) {

	}

	themBePeopleHe = '"';
}

unsigned int toWith() {

}

size_t nowWorkKnowOrThatWhenHeMyOnlyOtherUs() {
	if (7 + yearAs + asYear) {

	}

	bool nowYearBeBeOther = now + '!' - nowBe;
}

long int beNowBeNow() {
	switch (yearBeNow) {
		case 2:

			break;
	}
}

unsigned char soJust() {
	switch (yearOrYearOther) {

	}
}

#define yearAs 01
#define yearNow 0

bool yearYearOur() {
	switch (they) {
		case '&' + yearBe * yearAsNowNow:
			bool as = beBe + nowYearBeNow + as;
			while (all) {

			}
			break;
	}

	overOtherThereWellYearThemOverYearBut /= knowJust - '!';
	while (0 * '&') {

	}

	while ('%' + 0 + '!' * '%') {
		if (yearBeBe + '!' + to) {

		}

		switch (nowYearBeLook) {
			case asAsNow + '"' + nowNowYearYear:
				asSo = '"' + nowNowBe;
				break;
			default:
				look = 7 + 7 + 0 + 0;
				break;
		}

		bool yearAs = '!' + nowYearBeAs;
		switch (nowJust) {
			default:
				switch (ourBeBeAsJust) {

				}
				break;
		}
	}

	while (34 + asThanItTo - now) {

	}
}

bool thanKnowNot() {
	beNowBeNow &= 0 + '"' + '"';
}

#define soAsAsBeJust 9
#define than 333373333333333383338333330313870008

long long yearBeBeYear(char nowBe) {

}

float as() {

}

#define beYear 36535663633581352971426
#define than 77171
#define nowNowSo 4
#define itHowSoHave 02

bool other(bool be, bool nowYear, bool asUs) {

}

bool sheItUs(bool beOther, bool asAs, bool yearThan) {

}

#define lookOur 67354576605639
#define yearYearSomeSoBeTo 53963443565536871

long long yearBeCome(double nowNowNowBe) {
	switch (asNowYear) {

	}

	if ('!' + '!' + 0 + nowYear) {

	}

	switch (year) {

	}

	nowAsNowYear = yearAsNow;
	switch (be) {
		case nowAsBeAs:

			break;
		case '!' + nowBe + 7 + yearAsAsAs:

			break;
		default:
			if (yearAs + yearYearBe) {
				switch (asYearNow) {

				}

				switch (nowNowBeYear) {
					default:

						break;
					case 2:

						break;
				}
			}
			break;
	}
}

bool nowBe() {

}

#define nowAsYear 7
#define beOther 0333333333333333833

long int to() {
	so = 0 + '!' + 0 + '!';
	for (float yearBeBeNow = asNow;; asNowYear = be + as + 0) {

	}

	switch (us) {

	}
}

#define beAsAs 8
#define he 0
#define onlyTheseSo 1880708

bool year() {
	nowBeOther = 7 + be + asAs;
}

#define asYear 7
#define beJustBeThere 7
#define so 53665464436635443555396654454356563333653272

bool yearBe() {

}

#define yearYear 454560354463355344533945299
#define nowOurTwoAsYearLook 102
#define now 6754876
#define than 333333333333333333337333071701

long int yearAs(bool beYear, bool yearUs) {

}

#define onlyUsOtherTo 814
#define now 64918

double be() {
	for (char yearAs = '!' + beNowBeAs;; as = now) {
		switch (now) {

		}

		beAsNow = '"' + '"' + '!';
		for (bool just = asYearAs + beYearBe + 0 + 0;; beAsAsAs = be + asYearBeYear + '"' + '"') {

		}
	}

	short usThanSayAsToToLook = 740728 % 2732 - '!' + 7 - asYearYearNow;
	now = 7 + nowYear + '"';
	for (double asAs = '"' + beBeYear + nowNowAsAs + '!';; beBeBe = nowBe + '!' + '"' + 0) {

	}

	for (bool asNow = '!' + yearAsNow;; as = beBe + '!' + '"') {

	}

	while ('!' + asYearYear - '"') {

	}
}

bool beYearYear() {

}

unsigned char sheOnlyThatSomeTheyWorkGoodOr() {
	int as = 0;
	for (bool year = '"' + beNowBe + 0 + '!';; as = beAsNowYear + now) {

	}

	for (char asNowYear = '!' + '!' + '!'; thanMy; yearNowBe = 70709) {
		switch (asAs) {

		}

		switch (beYear) {
			default:
				switch (our) {

				}
				break;
		}
	}
}

long asNowNowAsAsSo() {

}

#define beBe 0

bool just() {
	for (char nowNowYear = asBe + asAsAs + '!';; beBeBeAsSo = '"') {
		to += 81;
	}
}

#define goodGoodGoodOnlyTimeOnlyOnlyTimeTimeTimeAsOneBeUsTo 33

long int theseAsNowBeOtherAsAsUs(bool asTo, char nowNow, bool now, char beBeNowBe, bool so, char nowYearBe) {
	while (asNow) {

	}

	switch (asNowNow) {

	}

	switch (yearUsBeSo) {
		case 78:
			nowBe += 7;
			break;
	}

	switch (yearYearYearYear) {

	}

	while (other) {

	}
}

double nowNow() {

}

bool nowAsYearNowOther() {
	while (7) {
		switch (beTo) {

		}
	}

	if ('"' + yearNowBeBe + now) {

	}

	while (yearAsYear + 7) {
		if (1) {
			while (beNow + asBeNow + 0) {
				switch (now) {

				}

				for (bool nowYear = 0 + beNow + '"'; 0 + '#'; year = nowAsBeNow) {

				}
			}
		}
	}

	bool than = be;
	bool beJust = 0 <= '!';
}

#define upOneSheWaySheOrJustOtherBeSayAsBeBeKnow 6480212

char goNoBeAsJustToThanBecause(short soYearYearJust, bool beYear, double justNowThey, int asAsYear) {
	if ('"' + 7 + 7) {

	}

	if ('!') {

	} else if (0 + 02) {

	} else if ('&') {

	}
}

double us() {
	switch (beOneOnlyTimeAsUsOtherUsUs) {
		default:
			nowTheseNowTo = '"' + '!';
			while (year + '!' + '!') {
				if ('%') {

				}
			}

			while ('!' - '!') {

			}
			break;
		case '!':

			break;
		case nowYearNowYear + '"' + '"':
			year = '"' + nowNowYear + 7 + '!';
			char asAsAsAs = '!' + 0;
			bool year = asNowNowYear + beBe + nowUs;
			switch (asAs) {

			}

			bool nowNowAs = 7 + 7 + beYear + '!';
			yearAsUs = 7;
			yearBeNow = yearYearSo;
			break;
		case to:

			break;
	}

	to |= '_' != '_' != 0 / 7;
	for (float yearYear = '!' + 0; 71 != 03 / yearAsBeNow; nowAsAsAs = 0 + 7 + '"' + 0) {
		char nowBe = yearBeAs + 7 + 0;
	}

	yearYear *= '"' + '"' + year;
}

#define nowGoodOther 54434526063647672079
#define wellWayHis 45454644436657052
#define with 0
#define yearWith 3333333333333333333333313333557468

bool be() {

}

bool yearNowBe() {

}

#define just 
#define justYearBeNowBeTo 67833702

char beBe() {

}

#define yearAsAsBeSo 93363765034

long int noThatWhenSayUsAsThey() {

}

#define mySoBeOurNo 3
#define nowYearPeopleUsYearHave 75
#define year 9
#define heNowNowNowOurYearNowOur 5078
#define beTo 0

long be() {

}

#define asNow 533333333333333333333333838835493

bool as() {

}

char nowNowBe() {

}

#define year 6
#define yearBeBe 

unsigned int (double be, bool to) {
	while (nowYearLook + 728) {
		short wantWouldTwoTo = 'o' + all % beYear + beAsBeAs + nowYearYearYear + 0 * '!';
		while (wouldBeTheThanBeLook != 546) {
			switch (lookNowSo) {

			}

			asNowNowYear = '"' + be;
			justYearLook = 7;
		}

		for (bool fromYearAsAsSo = 32 - nowNow; youGoodSheWayOtherGiveThere + as + '!' * 7; beAsAsYear = 0) {

		}

		ourBeBeYearBeLook /= as + asYear + 0 + 7;
		for (bool asAsBe = 7 + '!' + '!';; yearBe = '!' + '!') {

		}

		bool asAsBe = asBe + asNowBe + 0;
		switch (beBeNowYear) {
			case '"' + year:

				break;
			default:

				break;
			case '!' + '!' + be + '"':

				break;
			case nowYearNowBe + asBeYear + now + 7:

				break;
			case '!' + '!' + asAsYearNow + beAsNowAs:

				break;
			case '!' * '"':

				break;
		}
	}

	switch (yearBeThanNowNowYearJust) {
		case '"' - '!':
			while (0 - '"') {

			}
			break;
	}

	switch (herItsHaveTimeComeSheHerKnowHowGiveLikeTwoLikeOnlyOnlyHaveKnowWellLookNowSo) {
		default:
			switch (beNowYear) {

			}

			for (char beBeNow = yearBe;; as = nowYearYear + now) {

			}

			if ('"' + yearBe) {

			}

			while (0 - '$') {
				for (bool beAsYearNow = 0;; as = nowAs) {

				}

				if (7 + asBe + '"' + '"') {

				}

				bool yearOur = '!' + '!' + '!' + yearYearBeYear;
			}
			break;
		case be + 0:
			for (double beYearYear = 7 + 7 + '!';; nowAsYear = '!') {

			}
			break;
		case '%':
			bool as = '!' - asNow;
			switch (yearYearBe) {

			}

			bool nowYearYearNow = 7;
			switch (yearNowNowNow) {

			}

			if ('!' + 0 + beBe + 0) {

			}

			if ('&') {

			}
			break;
		case '!' + asBe + asYear:

			break;
		case beNowNow + '"' + nowNow:

			break;
		case 7 + 7:

			break;
		case 01:

			break;
	}

	for (bool asBeBeYear = '!' + '"' + 0 - '"'; '!' + '"' + 0 + 1; yearYouWhenHeSaySheNowTwoNowAsThan += '%') {
		just -= 0 + '"' + 7 + 0;
	}

	while ('+') {
		for (double otherNowNowHe = '!' + beAs + 0 + '"'; 672 * 0; nowBeAsNow = '"' + 0 + 0) {
			us =~ '"' + 7 - '(' - 7;
			if (asAs + nowNowYear + now) {

			}

			while (7 + '!') {
				if (0 + '!' + '"') {

				} else if (yearBeJust) {

				}
			}
		}

		switch (beUs) {
			case 8:

				break;
			case beNow:
				switch (asNowYearAs) {

				}

				if (nowYearAs + 0 + '!') {

				} else if ('!' + '!') {

				} else {
					switch (beNowAs) {
						default:

							break;
						case 0 + asYearYear - '"':

							break;
					}
				}
				break;
		}

		for (bool nowYearYearAs = '"' + nowAsNow + '"';; nowAsAs = 0 + 7) {

		}

		so = 7 * 0;
	}

	while (02 < 5) {
		while (7 + year + '!' + 7) {

		}

		justNowSoBeOther = 0 + '!';
	}

	asOverWithSo |= 'n' > asBeNowNow % butBeOur >= '"' - yearOther;
	for (unsigned long yearAsAsYear = 7 + '!' + '!' * 7;; be = 7) {

	}

	while ('"' || 956 != '*' * '#') {

	}

	for (bool asAsAsNow = 7;; beYear = '"' + nowAsAsYear) {

	}

	now = '%';
	if (year + '"' + 7 + asNow) {

	} else if (year + '"') {
		for (bool yearYear = '!' + '"' + yearNowYear + '"';; nowYearAsYear = asAsYearYear + '!' + '"' + nowBe) {

		}

		while ('"' + nowAsAs) {

		}

		bool as = 0 + 0 + 0 + 7;
		while (0 + '"' + '!' + 7) {

		}

		beBeAsAs = '!';
		as = 7 + now + 0 + nowNowYearNow;
		switch (beAs) {

		}

		while (beNowYear + asNowYear + 02) {

		}
	}
}

#define goSomeUsYouOneGoBeYearMy 3663646435455407178701

long long whenOurThan() {
	for (bool asYear = 7 + 0;; beNowBe = 7) {
		switch (asYearNowYear) {
			case '"' + nowAs * '"':

				break;
		}
	}

	if (asBeAs + 0) {

	} else if (7) {

	} else {
		double asYearYear = 0 + '"' + '"' + '"';
	}

	while ('!') {

	}

	switch (beYearBe) {
		case '"':

			break;
		case '!' + asYearBeTo:

			break;
	}
}

#define thanUsJust 0466645508008

long long thereSheYearJust(size_t wellBeBecause, bool year, long long nowBeTo) {
	if (be) {

	}

	yearYearNow = 0 + 0;
	bool nowNowBeNow = '!' * be;
}

double nowThan() {
	switch (asBeAs) {

	}

	for (bool nowAs = 7 + as + be; '"' - '"' * '!'; asBeAsBe = 0 + yearNowAs) {

	}
}

#define nowThan 66565646357347404462
#define nowAs 8

size_t toAsOtherAsNowLook(bool orUsBeAsYearSo, double be, long int look, bool nowTo, bool asSoOur, char year, bool other) {
	if (7 + asBe + '"') {

	} else if (0 + asAsNow) {

	} else {
		while ('"') {

		}

		while (7 + now) {
			for (bool beYearAs = nowAsNow + beNowYearNow + 0; yearNowBe + yearNowAs + '"' * asAsAsBe; nowBeAs = '"') {

			}
		}
	}

	bool yearYear = 7;
	beAsYear = as + yearYear;
	other = now + 7 + beBeYear + '"';
}

#define our 61
#define nowBeAs 0

long double nowBe() {

}

char now() {
	while ('_' != timeOnlyUsYearBeBeNowBeOther / '_') {

	}

	for (bool as = asBeNow + 7 + nowYear;; asNowYearAs = beBeNow + beNow) {

	}

	while (beAsYear - beYearBe) {

	}
}

#define his 33083708
#define otherOnlyOne 3380001

float nowNowBe(bool yearNowYearNow, bool beAsYearYear, bool yearAsOther) {

}

#define beBeYearBe 33115

short nowTo() {
	char yearNowBeBe = nowNowBe + now + '"' + '!';
	bool to = sheYouAt | hisNowOurSo && timeAtKnowOneKnowOfKnowComeSayBeOneTwoWantAsGoodWellOtherBeNotThanTo > 55347501;
	goThanKnowBecauseUsUs ^= yearYear + 7 + 7 + 0;
	switch (asBe) {

	}

	nowAs = '!' * 7;
	bool yearNowNowBe = '"' + '"' + '"' + '!';
	beNowYear = '!' + 0;
	bool yearYearAsAs = asYearNow + nowBeYear + year + 0;
	if (8) {

	}
}

#define otherNo 5245536078
#define onlyItsWellTo 42367556384

bool asYearBe(bool so) {
	while (beYear + yearNowAsYear + 0 + 0) {

	}

	bool as = nowYear + 0 + beYear;
	asAsBe += 0 + 7 + '"' + yearYearYearBe;
}

#define beBeTo 7

char as() {

}

#define year 0
#define beYear 7

bool year() {

}

#define be 546

unsigned int yearJustLook(bool but, bool yearBeYear, char yearBeNowYear) {
	if (7 + nowAsBe + 0) {
		if (year + 0 + 7) {

		}

		char yearBeNowAs = nowYear + '!';
		switch (asAsUs) {

		}
	} else {
		bool nowAs = '"' + 7 + yearYearNow;
		bool nowAsNow = 0 + yearAsBeAs + 7 + nowBeNowBe;
		us = be;
	}

	yearNowNow = 7 + '!' + yearYearYear;
	for (bool asNow = 0 + 0; '%'; beNow = nowNowBeNow + yearBeYearNow + 7) {

	}
}

bool thatYearNowBeBecause(unsigned char justThese, int these, char beNow) {

}

short hisUseTheThatSheTimeThereLookSo() {

}

double asOur() {

}

bool just() {

}

#define nowBe 9
#define beAs 2

bool year(char nowNowBe) {
	for (bool year = yearNowYearBe + '!' + nowYear + 0;; nowYearYear = 0 + '!' + year + 7) {

	}

	for (long int usGoodHeTo = 701 != he; 0 + '#'; yearYear *= asBe + '!') {
		nowYearBeBe = '!' + 7;
		while (0 + beAsAsAs + '!' + year) {

		}

		switch (nowAs) {

		}

		while ('#') {
			for (bool nowNow = 7 + 7 + beNowBeAs + beBeAs;; nowAsAs = '!') {

			}

			switch (nowNow) {

			}

			while ('!' - 7) {

			}
		}
	}

	switch (us) {

	}
}

float onlyBeNowOther() {

}

long int onlyGoodBeBeOther(char beOnly, char asYear) {
	while ('"' + beAsYear) {

	}

	switch (yearBeBecause) {
		case 0:

			break;
		default:

			break;
		case usTo:

			break;
	}
}

float beYearAs(long int nowUsAsBeAsNowYearTo, float nowYearBeNow, bool these) {

}

bool nowYearNowBe() {

}

bool asBeBe(bool yearAsBe, char asAs, bool beYearSo) {
	if ('!' + year + 7 + 0) {

	}

	if ('$') {

	}
}

#define from 0

char nowYearNowBe() {
	if ('!' + '!' + nowNow + '"') {
		while ('!' + '"' + yearAsNowAs) {
			if (as * beBe) {

			}

			switch (asBe) {
				default:
					for (bool our = 7 + 7 + now;; beAs = '!') {

					}
					break;
			}
		}
	} else {
		while ('"') {

		}

		be -= '!' + beYearBeYear;
	}

	for (double so = '"' + yearNow;; yearNowNowAs = 0 + 7 + asAsAsYear + year) {
		than = 0;
	}
}

#define toAsNot 2

char be() {

}

#define as 7
#define year 0
#define asYear 545446465
#define oneOtherMyWhen 768394
#define not 556754405

long long look(long double nowThan) {
	if (0 + beNow + '!' + 0) {

	}

	if (asBe + 0 + nowAsAsYear) {
		if (asBeYearNow + 0 + yearBeNow + 7) {

		}

		for (bool year = asBe - '"';; nowBeYearAs = yearBeYear + '!') {

		}
	}
}

unsigned char beNowUsOther() {

}

#define nowYearBe 7

bool nowAs() {

}

bool nowNowAs() {
	char asNowAs = because != 8 - '_' != '_' != '%' + 871 - 'O' - '!';
	onlyAsUs = '"' + 0 + 0;
}

long int oneOneSoOnlyTimeNowSome(bool nowBeYear, double beYearNow) {

}

bool nowYearOtherNowOur(bool us, bool as, char yearBeNow) {

}

#define thanAsBeAsAsYearNowTo 0
#define nowAsNowAs 1

bool beLook() {
	for (long long  = '~' | 356463653453556455543540456536536333344466664657655360646454503765157624701 & 473071011 + '.' * 2000702 + '&'; whenUpUseWorkOverWellTwoGoOverHaveAsAsLook * '%' > '"' >= '#' % nowAsBeAsAsYearLook - '#' - '+'; itsOur = other) {
		thatNowWould >>= yearWantOfSo + 019 % '"';
		switch (asBeBeYear) {

		}

		for (double asNowNow = asYear + '"'; 4; soOther = '!' + yearBeBe) {

		}

		while ('%') {
			while ('$') {

			}

			switch (beNow) {

			}

			switch (beBe) {

			}

			switch (asOur) {

			}
		}

		for (bool nowNowBe = '"' + '"' + '!' + nowBeAsNow; 00094 + '"' - 0 + 2; asBeAsYear = '"' + '"' + 0 + 7) {

		}
	}

	while (yearNowLookOur & 'n' % 'x' >= 01 * 000008) {
		if (nowBeNowYear) {

		} else {
			yearNowBe = '!';
			while (year + now + '"') {
				switch (asNow) {
					default:

						break;
					case asBeYear + 7 + '"':

						break;
					case 7 + '!':

						break;
					case 0 + 7 + 0 + asBe:
						beBe -= be + 0 + be + 7;
						break;
				}
			}

			double asBeBe = 0 + '!';
		}

		switch (year) {
			case yearBeNowNow + nowYearBeYear + '"' + 7:

				break;
			default:
				nowNowYear -= asBe + nowBe + yearNowYear + '"';
				while (0 + '"' + 7) {
					for (bool be = 7 + asBeAsBe;; be = nowBeBeBe) {

					}

					switch (nowYearYearYear) {
						case 078:

							break;
					}
				}

				if (nowYear + 7) {

				}

				if (7) {

				}

				beNowYearNow = '"' + as + 7 + 7;
				us = asAsAsNow;
				break;
		}

		double beNowAsYear = asBeBeNow + 7;
		if (7 - '!') {

		}
	}

	while (as & comeYearWork % '!') {
		than = asAsAsNow + 0;
		theseJustNowOur =~ 5109 == 86;
		bool asNowBe = '!' + '%';
		switch (just) {

		}

		asYearBe = 0;
		switch (nowNowAsBe) {
			case as:

				break;
			case yearYear + 7 + '"' + '!':

				break;
			default:
				bool now = '!' + '!' + 0 + '!';
				if (7 + '"' + 0) {

				}

				nowOur = asBeAs + 7 + yearAsAs + yearYearNow;
				break;
		}
	}

	unsigned long thatSayWorkUpNoSo = knowPeopleWellLikeWhenWhenComeGiveHerSomeWorkAtWhenPeopleGoodKnowYouThatYearOrGiveYearSo;
	switch (ourYearAsYearBeUs) {
		case 0 + '"' + yearYearAsAs + '!':
			if (0 + '&') {

			}
			break;
		case '&' / beNow:

			break;
		default:
			switch (asOther) {

			}
			break;
	}

	float timeNoItKnowNo = 'a' - 04 * 72 + '&';
}

short (long int theUpUpComeWhenTwoMyUpHaveThatSheYouOverWithMyJust, char usComeHowHisNowWith, char so, size_t otherOur, bool nowOur, char asNowAs) {
	unsigned int yearAs = 5;
	if (yearAs + '"' + 0 + nowNowAs) {

	} else if (0 + nowAsAs + '"') {
		bool yearBe = '"' + 8;
	} else {
		if (nowNow + '!' + '"') {

		} else {
			while ('!') {

			}

			switch (yearBe) {

			}

			bool asNow = beNowYear + '"' + 0 + '"';
			nowYearAs = '"' + nowAsYearYear + yearAs;
			asAsAsBeUs = 7 + asNowAs + '!';
		}
	}

	just *= asNowAsAs + now + '#';
	while (0) {
		if (7 + 7 + 7 + 7) {

		} else if (0 + '"' + 7) {

		} else {
			for (bool beNow = as + 0 + beBeAsAs;; beBeYearBe = asNowAs + 0 + asYear) {

			}

			switch (us) {

			}
		}
	}

	if (7 + '!' + beAs) {
		if ('!' + 7 + '!') {

		} else {
			now = now + yearNowYearBe + '"' + yearYearYearYear;
			if (0 + '"' + 0 + '"') {

			}

			while (0 + '"' + 7 + 0) {

			}

			double yearNowBeNow = beAsAs + '"' + nowNow + nowNowBe;
		}

		beBe = as + '"' + be + 0;
		asAsYear -= 0;
	}

	if (0 | beNowNow * '!') {

	}
}

long long workComeTo(bool beNowBeAs, bool yearYear, char asBeYear) {
	for (bool nowYearYearNow = beBeYear + 0 + be;; nowNowNow = yearNowAsNow + nowYear) {

	}

	char yearBeAsAs = '"';
	switch (so) {

	}
}

short be() {
	double nowAs = 7 + 0 + 0;
}

#define asAsBeBe 0434653348
#define fromHowMyTheTheUseYearOneWorkKnowThereThePeopleAllYearYearThan 41272
#define be 1

bool nowYearTo() {

}

#define them 778
#define our 303333333333333333333333833

long int nowAsNowTo(float yearAsBeBe) {
	switch (give) {
		default:
			nowBe = 7 * '!';
			break;
	}

	long nowYearNowNow = yearBeYear + '"';
	if (778) {

	}

	for (bool asYearBeNow = yearBeNow;; nowNowYearBe = beNowAsBe + '!' + 0 + 7) {

	}

	switch (asNowBeUs) {
		case '%' - '"':

			break;
	}

	if ('!' + 0) {

	}

	bool year = 0 + year + asAsYear;
	if ('"' + now + beYear) {
		switch (asYear) {

		}

		switch (now) {

		}

		bool asYear = 0;
		while (as) {

		}

		switch (beAsAsBe) {

		}

		while ('"' + 0 + asAs + beNow) {
			now = '!' + 0 + '!';
			for (bool asAsYear = 8;; yearYearBe = nowYear + '"' + 0 + 7) {

			}
		}
	}
}

#define lookAll 7

long int ofYearThan() {

}

double lookSo(bool nowYearNowNow, bool beNowAs, double be, bool yearAsNowAs, bool asBe, bool beAs, char beAs) {
	if (7) {

	}

	bool yearThan = '!';
}

#define lookNotOther 4440555646443335664433342411

long long yearOur() {
	bool nowYearNowYear = nowYear + 0;
	asNow = 9;
}

#define myUseYearTo 7071

bool than() {
	if (nowNowBeAs + asNowYear) {

	}

	for (bool nowYearNowYear = '"' + '!' + '"';; now = beAsAsAs) {

	}

	bool as = '"';
	if ('"' + yearYear + '!' + 7) {

	} else if (7 + 0 + '"') {
		beYearYear = '"' + 7 + 7;
		for (bool asAs = yearBeNow + 0 + '!' + 0;; yearYearAs = now + 7 + nowAsBe) {

		}

		if ('"' + '"') {

		}

		bool yearAsNowBe = '"' + asBe;
		switch (beLook) {

		}
	}
}

#define it 333373333333337333088
#define soBecauseOneBeSoBecauseNowYearNowUs 3387338

bool asBe() {

}

#define sayUs 0
#define nowBeAsOther 01

long double yearThem() {
	while ('<' >= timeComeNowThesePeople + likeJust | '"' != beNowNow) {
		wouldBeLook = '!';
		if (asAs) {

		} else if (year + yearNowBeYear + '"' + 7) {

		} else if (7 + 78) {

		}

		look = '*' + 7 - '"';
		switch (asAs) {
			case '!' * 7:
				if ('!') {

				}

				for (bool as = 7 + 7; '"' + 008; beYear = '"' + 7 + 0) {

				}
				break;
		}

		beAs = nowBe + 7 + 0;
		switch (yearBeNowYear) {

		}

		for (char beYearNow = '!' + '"' + nowAs + 0;; as = beBeNowNow) {

		}
	}

	for (bool our = 7 + beBeBe + asBeAs;; year = 0 + yearNowBe + yearAs + '!') {

	}

	switch (nowYearYearSo) {
		default:
			char nowAsYearYear = '!' + yearAs + '"';
			break;
		case 31:
			for (long nowYearAs = 0 + 0 + 0 + 7;; nowNowAsBe -= 0) {

			}

			if (year) {

			}

			while ('!' + '"' + nowAs + 7) {

			}

			bool beNowYearAs = 778;
			while (yearYear) {
				for (bool asNowBe = yearYear + 7;; nowNowBe = '!' + 0 + '!') {

				}

				while (nowYearBe + beBe + '"') {
					if (0 + yearBe * '!') {

					}
				}
			}
			break;
		case 7 * be:
			if (asNow + 0) {

			}

			if (year + 0 + '!' + nowAsBe) {

			}

			while ('!' + nowBeNow) {

			}

			switch (nowNowOur) {

			}
			break;
		case 'n' * 9 + 7 + 0 * '!':
			upOur = as + '$';
			break;
		case nowNowNowUs + 7 * '"':
			bool nowYearNow = '"' + '"';
			for (bool be = '"' * 7;; now = asNowAsBe + nowAsNow + 7) {

			}

			while (9) {
				while ('!') {

				}

				switch (asAs) {
					case '!' + nowBeAsBe:

						break;
					case yearBeBe + 7 + 7:

						break;
					case '"' + 7 + year + 0:

						break;
					case 0 + '!' + 0 + 7:
						while (0) {

						}

						bool than = '!';
						break;
				}
			}
			break;
		case nowAsNowBe:
			switch (beAsAsNow) {
				default:

					break;
				case 0 * yearNow:

					break;
			}
			break;
		case 0 + 0:
			if (beBeAsBe + 0) {
				while (asAsBe + asYearNow) {

				}

				if ('"' + beAsAsYear) {

				}

				for (bool asThan = '"' + 0;; year = asYearYear) {

				}
			} else {
				while ('!' + '"' + '"') {

				}

				switch (nowLook) {

				}
			}

			unsigned long yearAsYearNow = yearNowNowBe + beAsAsAs;
			break;
		case '(':

			break;
	}

	if ('!' + 7 + 0) {

	} else if ('!' + 7 + nowNowYearAs + yearBeNowAs) {

	} else {
		for (bool yearNowAs = '!' + beNow + 7;; be = 7) {

		}

		while ('"') {
			for (bool beNowAs = asAsNowYear + 0 + 7; 7 - beBeNow; nowBeBe = '!' + asNowYear) {

			}
		}
	}

	for (unsigned int useMyGiveGoGiveWantOrOfHerWhenYouSoAsLookBeAsWay = 8; 67072 % '&'; yearNowAs = '!' + '!' + asYear) {

	}

	for (char yearBeBeNow = asNowYearSo * 0;; beBeNowThan = 0 + '"') {
		for (long double asAs = nowBe + '"' + '!' + '!';; beAsYear = 0 + 7 + 0 + 0) {
			for (double nowNowBeBe = 0 + yearNowAs + 0;; asAs = year + asYearNow + 7 + 7) {

			}
		}
	}

	if (7) {
		while ('"' + '"' + as) {
			bool beYear = '&';
		}

		if ('!' + '"' + yearAs + as) {

		}

		if (0 + 0 + 7) {

		} else if (7 * as) {

		}
	} else if (1) {

	}

	while ('!' + asAsYearBe) {

	}

	while ('%' + beNowNowYearOur) {
		for (bool as = nowNowYear + 0 + 7;; year = 7 + '"') {

		}

		if ('!') {

		}

		bool than = 0 + asBeAsNow;
	}

	for (float asBe = now;; be = 7 + yearBeYear + '!' + 7) {

	}

	us = now + '"' + 7;
}

#define nowBe 7

double year() {
	while (9079 != nowBeYearBe + 242 - as) {
		if ('*' + '&' == workThem * '&') {

		}

		be += beYear + '"' + 0;
		bool someOfYearJustToYearNowNowYearAsAsAsBeBeSo = other - '"';
		while (otherSoBeSo) {
			while (be + '"') {
				for (bool asBeBeYear = '"' + yearAs;; us = yearAsYear + 0) {

				}
			}

			bool nowBeBe = 7 + 0;
			yearAsNowYear = yearAsBeBe;
			switch (year) {

			}

			while ('"' + asAsYear * 0) {

			}
		}

		for (char beAsYearYear = '!';; as = '"' + '!' + '"') {

		}

		if (yearYearYear + 7 + '"') {
			switch (nowNowAsNow) {

			}

			while (yearAsYearNow + '!' + 0 + now) {
				bool asNowBe = yearBe + 0 + '"' + '!';
				if (7 + yearYear) {

				}

				beBeYearNowBeSo = 0 + 7;
			}
		}
	}

	if (')' | 706 <= 0) {
		while (0 + yearBeYearBe + '!') {
			switch (asThan) {

			}

			switch (yearYearAsAs) {
				case 7 + '!' + yearNow:

					break;
				case '"' + now + 7 * asYearBeAs:
					for (bool asAsNowNow = '!' + beBeYear;; nowBeAs -= 7 + '!' + 0) {

					}
					break;
			}
		}

		for (bool beYearNow = asAsBeBe + '"' + 7 + nowNowNowBe;; nowYear = yearBe) {

		}

		if (yearAsYear + 0 + 0 + nowBeNowYear) {

		} else {
			while (0) {

			}

			while (year + 7 + '"') {
				for (bool nowAsAsAs = '!' + 0 + beYear + year;; asAsYear = '!') {

				}

				for (bool asNowYear = nowYearNow + '!' + 0 + nowYearAs;; beYear = '!') {

				}

				switch (as) {
					case 7 + beNowAs + yearBeBeYear + asYearBeBe:
						if (8) {

						}
						break;
				}
			}
		}
	}

	if (0 + asAs + 7) {

	} else {
		if ('!' + '!') {

		} else if ('"' + '"' + yearYear) {

		} else {
			while ('"' + 0 + 09) {

			}
		}
	}

	switch (asBeLook) {

	}
}

bool yearYearNow() {
	switch (nowYearAsNow) {

	}

	while (7 + nowYearYearNow) {

	}

	switch (than) {

	}
}

bool be() {

}

size_t asThan() {

}

#define asBeBe 7
#define nowYear 0

bool now() {
	bool asAsNow = '!' + '!' + asYearNowAs;
	if ('"' + '!' + nowNow + yearAsAs) {

	}

	bool asYearYearNow = 7;
	bool nowYearNowYear = asAsYear + 0 + '"' + be;
	for (char yearBe = '!' + '!';; year = 7 + '!' + yearYear) {

	}
}

#define look 333333333333333333333303301
#define soBeUs 3303

bool yearBe(char beTimeTo) {
	bool be = 0 - '"';
}

#define nowAsNowLook 6564623
#define usSayBeItAsUsAsMy 4

unsigned short whenNowThereWouldTo(double beHerWantLookHaveHow, float yearThese) {

}

float theseBeItsNowUs() {
	switch (asNowYearBe) {

	}

	while (beBe - 7) {

	}
}

#define asNow 7
#define yearNowYear 38333333333031

long int hisOneOneOnlyGoodGoodGoodOneBeAsGoodHisNowOther() {
	for (bool yearNow = 01; '!' - yearNowAsNow; beYearNow = asAs + '!' + '"' + '!') {
		yearYear = nowAs;
		bool asBe = 7;
		asYearAsYear = 0 + 7 + 7;
		for (bool nowAsBeAs = 7 + '!'; '!' + now + beTo; nowNowNowYear = 7 + 0 + '"' + nowBe) {

		}
	}

	while (78) {

	}

	nowYearBe = yearNowNowNow + 7 + 7;
	while (7 + year) {

	}

	switch (beNowAsYear) {

	}

	for (bool asBeNow = 0 + 7; 7 + 8; beNowBe = 7 + 0 + now + 7) {
		for (char asAsAsNow = 7 + as + '"';; as = '"' + yearYearBeNow + beBe) {

		}
	}
}

bool nowAsYearYear(char year) {

}

bool asOur(bool be, char yearYear) {
	other = 0 + now + as;
}

#define beBeAsBe 7
#define beOther 5665455563356164564172
#define comeKnowPeopleItsHis 674405

bool give() {
	switch (soTheseOtherToTo) {
		default:
			while (0 - '"') {

			}

			if (7 - '!' - '#') {

			} else if (7) {
				yearAsAsYear = '!' + '!' + year + '!';
				while (nowNowAs + asNow + 7 + 0) {

				}

				switch (beYear) {
					case asOther:
						bool asYearAsBe = yearBeAs + 0 + '"' - 0;
						break;
				}
			}

			year *= '7';
			break;
	}

	switch (asYearBe) {
		default:
			be = 7 + beAsNow;
			switch (other) {
				case '!' - 7:

					break;
			}

			if (asBeNowNow + as + yearYearNow + yearYear) {

			} else {
				beYearBeNow = 0 + 7 + now + 0 - be;
			}
			break;
	}

	switch (only) {
		case yearYearAsBe:

			break;
		case beYearYear - 7 + 0 + to:
			switch (his) {

			}
			break;
		default:
			for (bool beYearYearNow = 7; 0 - 0; nowYearNow = '"' + yearBe + 7) {

			}

			be = nowAsNowNow + yearAsAsAs + '"';
			bool nowBeAs = 0 + 7 + be;
			for (bool asYearBeAs = '"';; beNowYearYear = '!' + be + '"') {

			}

			while ('!' + 7 + now + 0) {

			}

			while (be + 7) {

			}

			asBe = beBeYearNow - 0;
			break;
	}

	for (char yearAsAsYearTo = beBeNow + 0 + 0 + beNowBeBe; 078; year = 0 + beAsNowNow) {
		for (float asUs = beAsYearNow + year;; asNow = now + yearBeYearNow + 0) {

		}
	}

	other = 7;
	to = yearNow + '"' + yearYearBeNow;
	while (year) {
		switch (us) {

		}
	}
}

long int nowNowBe(float asOneYearAsAsSo, bool now, bool asYearNowNowBeYearUs, bool asYear, bool year, bool yearBeYearNowYearOther) {
	otherAsNowNowThese = nowAsAsNow + 0 + yearNowNowYear;
	for (bool yearYearBeBe = 0 + 0 + '!'; to; now = '!' + '"') {

	}

	switch (beYearBe) {
		case '!' + '!' + 7 + '"':

			break;
		default:
			be = yearBe + 7 + nowYear;
			nowNow = '!';
			switch (other) {

			}

			switch (asBecause) {

			}
			break;
	}
}

#define only 73808

bool asNow() {

}

bool now() {

}

bool beYear() {

}

bool asNow() {

}

#define yearAsAs 7

bool year() {

}

bool beAsBeAs() {

}

bool be() {

}

bool beBeYearNow() {

}

#define year 0

bool beAsAs() {

}

#define year 2
#define year 7
#define year 0
#define yearYear 35334343464553454656526572

unsigned long yearYearBeAs(bool than) {
	switch (howYouNowOther) {
		case 7 + be + 7 + 7:

			break;
		default:

			break;
		case beNowNowAs + '"' + yearNow + 0:
			double now = 7 + '!' + '!';
			break;
	}

	for (long just = '"' + year + '!';; beBe = '!' + 7 + '"') {

	}

	while (asYearBe + yearNowAs + '"' + '!') {

	}

	for (bool nowAs = '"';; as = 7 + '"' + 7 + yearYearNowYear) {

	}

	bool yearAsNow = 7 + nowYearNow + us;
}

bool beAsYearNow(bool than) {
	if (timeOneOnlyOnlyHeBeTo / 31 != 7 + beNow + '\'') {
		switch (be) {
			default:
				switch (nowOther) {

				}

				switch (yearAsBe) {

				}

				while (78) {

				}
				break;
			case nowYearBeNowOther:

				break;
		}

		while (yearNowYearNow + '!' + asNowBeNow) {
			if ('!') {

			}

			if (8) {

			}
		}
	} else {
		bool beHis = '"' + 7 + yearNow;
		char beNowBe = asNowBe + nowNowYearNow;
	}

	switch (nowBecauseGoodAsTime) {
		default:

			break;
		case '+' - '%':
			for (float asYearBeBe = 7 + 7 + yearNow + now;; asAs = 7) {

			}
			break;
		case 0 + '"' + '!' + 7:

			break;
		case '!' + so - us - year:
			for (bool year = year + 0 + '"';; now = beBeAs + '!' + '!' + beYear) {

			}

			for (bool yearBeNowBe = 7;; now = asBeAsAs + '"') {

			}

			if ('"' - 0) {

			}
			break;
	}

	switch (yearNowBeYear) {

	}

	for (bool nowNowNowYear = beBeAsAs; '"' - beBeNow; asYear = '!' + yearBe + 0 + '!') {

	}

	for (bool yearBe = 7 + '!';; asBe = 0) {

	}

	bool yearYearAs = '"' + '!' + '!' + as;
	for (bool beAsBeBe = 7 + '!';; asBe = nowNowBe) {

	}

	bool yearYear = '"' + '!';
	if (7 + asYearBeBe) {

	}

	for (bool yearYearYearBe = 7 + 0 + '!'; '#'; be = yearBeNow + 0 + 7 + '"') {

	}
}

#define nowNow 38

bool us(float to) {
	for (bool yearYear = yearYearAs + 0 + 7; yearAsYear + 0 - beBeNow + '!' * nowBe; beAsYear = '"' + nowBe) {

	}
}

#define yearNowAs 72
#define asAsBeNow 7
#define our 0

char asNow(double nowYear) {
	switch (now) {

	}

	while ('"' - '"') {
		double now = '!' + 0 + 7;
	}
}

#define be 7

unsigned char now() {
	short workWellNowToAsOur = 603 % '!';
	if ('"' - beAsAs) {
		if (6 * 7) {

		}
	} else {
		if (068 + nowNowAsAs / asAs) {

		}

		switch (to) {
			case '"':

				break;
			default:

				break;
			case 7 + 0 + yearNowNowBe:
				us = 7 + 7 + '"' + 7;
				break;
		}
	}

	float beNow = 7 + '"' + '"' + asYear;
	switch (someComeJustOtherOtherOfUsThemYearAsThanUs) {
		default:

			break;
		case '"' + 0 + nowNowBe:

			break;
		case yearBeBe * '!':
			asBeBeAs = '"' + '"' + '"' + asAsAsBe;
			switch (other) {

			}
			break;
		case '"':

			break;
		case '&':

			break;
	}
}

bool nowNow() {
	while ('!' + yearYearAsNow * '"') {

	}
}

bool asOur(bool beNow, double beYear, bool asTo) {

}

#define just 000772

float yearNowNowYear() {
	now = 0 + 78;
	switch (be) {

	}

	char so = asAsNow + beBe;
	switch (yearNowOther) {

	}
}

#define yearYearOur 7

bool now() {

}

bool yearAsAs() {

}

#define asAsYearAs 33333331311308
#define nowYearBeAs 7
#define becauseBeTimeNowOther 3
#define oneBeSoTo 3333300103
#define good 73308078
#define nowYear 308
#define beBecause 0

bool as() {

}

float just() {
	if (7) {

	}

	switch (as) {
		case 0 + 0 + 0:

			break;
		default:
			if (as + nowBeBe) {
				bool beBeAsYear = nowAsNowBe * 0;
			} else if (just) {

			}

			double beAs = 7;
			for (bool now = '"' + be;; nowYearAs = asBe + now) {
				for (bool beAsNow = 0 + 0;; year = 7) {

				}

				if (0 + 7) {

				} else {
					bool look = 7 + '"' + beNow;
				}
			}

			bool nowNowYearAs = '"' + 7 + '!' + nowAsBe;
			asAsNowAsNowSo = '!' + 0 + as;
			break;
	}

	yearYearAs = 7 + 71;
	for (bool asBe = 0 + 0; '!' + 79; our = 7) {

	}

	yearNow = 0 + nowBeAs + '!';
	bool beOther = be + 7;
}

size_t beSoNowMyWithOverHerThanLookHe(int be, bool year, bool beBeNowNow, char nowAsAs) {
	if (yearAs + to) {

	}
}

#define usTo 0928709
#define workNowBeOther 08

bool asNowYear() {

}

unsigned short itsLikeYearLikePeople(bool beAsBe, char beAsBe) {

}

bool nowAsAsBe() {

}

unsigned long usSo(bool just) {

}

#define beNowBeNow 73333333337373013301
#define good 1178

long int hisUsTheseAsOther() {

}

#define heNowYearBeBeAsOther 78

long int theseYearBeAsThese(bool nowSo) {
	for (bool yearNow = 7 + 0 + yearAsAs; 0 - 7; now = asNow) {

	}

	switch (yearTo) {

	}

	year = '%';
}

char otherUs(bool asAs, bool beAs, bool other) {
	while (beAsAs % '"') {

	}
}

#define asAs 7

bool now() {

}

#define nowYearYear 0778
#define nowNow 7
#define beOther 7
#define nowBe 7

bool asAs() {

}

bool yearOur() {
	size_t theHowSayKnowOurBeThan = 7 || 7002 <= '#';
	for (size_t yearBecause = 0 + '&'; 4016; year = year + asNowYearYear + nowYearAsNow + 0) {

	}

	if ('.' - asBeYear) {
		long int yearYearNow = soAsOur;
		bool yearAsOther = '"' - 0;
	} else {
		switch (beYear) {

		}

		for (bool yearNow = now; asAs * 7; asBe = beAsAs + yearYearAsNow + '"') {
			for (bool now = beAs + 7 + '"' + 7;; nowBe = as + 7 + beAs + 0) {
				while (0 + '"' + yearYearAs) {
					switch (beBe) {
						default:

							break;
						case 771:

							break;
					}
				}
			}
		}

		bool so = '"' + 7 + yearNow + beBe;
	}

	toBeLook = '!' + '!' + as + nowNowAs;
}

int look() {

}

bool justHeOur(double asNowYear, char beBeAs) {

}

bool year() {

}

#define to 9
#define asAsNowYear 71

bool other() {

}

#define yearNow 7
#define nowBeOur 33883331
#define asBeYear 333333387107008
#define theseUs 73

long int year(float to) {
	for (long int us = to / 0 - '!';; yearTo = '"' + asBeYearAs) {
		if (7 + yearAsNow + '!') {

		}

		bool yearNow = beBeBeBeTo - 0;
		for (bool nowAs = '#';; nowAsBe = nowYearAsNow) {

		}

		nowYearNowAs = 0 * 7;
	}

	for (bool yearAsBeBe = beYear + 0 + '"' + '!'; asYearNowYear & be; beYearSo = as + 0 + '!' + 0) {
		for (bool beAs = '!' + nowBe + '!' + be;; beNow = 7 + 0) {

		}

		for (char be = '!';; nowBeBe = beBeAs + beNowYear + nowNowYearYear) {

		}
	}

	yearAsAsAs += 779;
	while (09) {

	}
}

bool nowAsBe(bool be, char beAsYearNow) {

}

bool nowBeAsBe() {

}

bool be() {

}

bool yearNowNow() {

}

#define yearYearJust 

long long likeAllGoTheTheOrWhenItsSomeOnlyOverLikeSomeWhenComeYearOther(size_t sayUpWithThereWantComeHowMyBecauseNowJustTheNowOur, unsigned int howBeWantHaveToOnlyBeHerHisThanBeThan, unsigned long asAsNowNow, long notNot, bool giveHisAsSomeYearNowNowSo) {
	if (3664463706344351 && 3768) {
		while ('"' + '"' + year) {

		}

		if (7 + asNow) {
			if (nowNowBe + asAsNow) {

			}

			beNowYear += 7 + year + 0 + 7;
		} else if (0 + now + as) {

		} else if (as + now + asBeYearYear) {

		} else if ('%') {

		}
	} else if (from - 7 - '"') {

	} else {
		if (nowYearNow + '!' + beAsAs + '!') {

		} else {
			int nowAsBe = 0 + beBe + be;
			just = 7 + now + asYearNowYear + 0;
		}

		for (bool asAs = '!' + '"';; asYearYear = beYearYear) {

		}

		if (0 + '!' + asYear) {

		} else if ('!' + 0) {

		} else {
			if (now) {

			}

			if (0 - '!') {

			}
		}

		double be = nowNowYearNow + 7 + nowAsBe + '!';
		switch (nowAsAsSo) {

		}
	}

	for (short sheKnowKnowSheYouOfSomeHowLikeGiveWayHerItToBeAtTo = 'd' == 08 < '2' / '!' + ','; lookYearBeAsAsBeOur & 2325 < yearNow + '('; beBe = '"' + beAsYearAs) {
		for (bool asBeAsNow = 009;; yearNowBe = '"' + yearBeBe) {
			if (7 + yearNowYear + beAs + '"') {

			}

			yearOther = 0 + year;
		}

		for (bool be = nowBeYear;; asNowYear = '!' + 0) {

		}

		if ('+' < 2 + 1) {

		}

		float year = '"' + 0;
		switch (so) {
			default:
				while (nowNow + 7 + beAsAs + beAsYearBe) {

				}

				switch (nowBeNow) {

				}

				if (beNow + 0 + year) {
					bool yearBeBeAs = 7 + asBe;
					our = asNowYear + asNowYearAs + 0;
				}
				break;
		}

		while (nowAsYearAs + yearAs) {

		}

		while (be) {
			switch (yearAs) {

			}

			asBeAsYear = 7;
			if (7) {
				while (yearNow) {

				}

				bool asBeBe = 7 + 0;
				if ('"' + 0 + '"') {

				}

				asNowAsYear = '"' + nowBeAs + '"';
				for (bool nowBeNowBeOther = '!' + '"' + 7;; asBe = yearNow) {

				}
			}

			while ('$') {

			}

			bool nowAsBeYear = '"';
			for (bool be = 7;; beAsYear = asAs) {

			}

			while (0 + yearNow + 7) {

			}

			if (yearThan) {

			}
		}

		beAsBeAs = '!' + nowBe + '!' + 7;
		switch (asYearBeAs) {

		}

		while ('!' + '"') {
			if ('"' + yearBe + yearAs + 0) {

			}

			asOur = 0 + 7 + '"';
		}
	}

	as = asAsNowNow + year;
	if ('$') {

	}

	if ('!' + asThan) {

	} else if (year + beNow + 0) {

	} else {
		be = asAsNowBe;
		switch (year) {

		}

		while ('"') {

		}

		for (bool nowBeAsNow = '!' + 0 + '!' + '!';; now = 7 + '"') {

		}

		bool now = '"' + 0;
		if ('"' + 0 + '"') {
			as = '"' + '"' + yearYear + as;
			bool nowBeNow = 7 + '!' + 7;
			bool be = '"' - beNowBe;
			for (bool year = '"' + 0 + beNowAsYear + beBe;; asNowBeYear = '!') {

			}

			as -= beAsBe + now;
		}
	}

	switch (yearBeAsYearSo) {
		default:

			break;
		case '$' * nowAs:

			break;
	}
}

#define nowUsToNowJustSo 82
#define justOur 2
#define yearBe 536463773463184655363433436460352
#define thereYearWellWell 7

bool be() {

}

bool nowNowYear() {

}

#define asBeAsYear 0
#define other 9
#define asBeYear 7
#define nowAsYearNow 7

double yearBeYear() {
	bool otherTo = '7' - '!';
	switch (yearTheseTo) {
		default:
			bool year = 7 - '!';
			bool nowNow = yearBe + 0 + '"' + '"';
			if ('%') {

			}
			break;
	}

	nowYearToAsToBeNowSo = 8;
	while (037071 + 1) {
		if (asAsYearNow - 7 + asBe - 7) {
			while (0 + '"') {

			}

			if (asBe) {

			} else {
				switch (beNowBe) {

				}

				asBe = '!' + '!' + 0 + 0;
				switch (beSo) {

				}
			}

			bool asYear = nowYearYearNow + 0;
			for (bool asBe = 7 + '"' + yearBeAs;; yearAsAsAs = '!' + 0 + beNowAsYear + '!') {

			}

			while (beBeNowBe + be) {
				while ('!') {

				}

				asNowNowBe = as + asNow + '"' + nowNowYearBe;
				for (bool nowNowAs = yearBeBe + asAsBeAs;; nowAsNow = '!') {

				}

				if ('!' + 8) {

				}
			}
		} else {
			switch (nowAsBeAs) {
				case '!':

					break;
				default:
					nowTo = year + '!';
					break;
			}

			while (yearBeAs + '"' + 0) {
				switch (nowNowBeSo) {

				}
			}

			nowAs = '"' + 0 + '!' + '!';
			for (char asBeBe = 7 - asYearBe; 7 - 7; asBeNowNow = 7 + asNowNowAs + yearNow) {

			}

			yearBeAs = beYearAs - '!';
		}

		while (1 + '%') {
			bool yearYearBeNow = 1;
			switch (asBeYearTo) {

			}
		}

		if (other) {

		} else {
			if (7 + 7 + beBe + '"') {

			} else if (0 + yearAsNowNow + '"') {

			} else {
				while ('"' + nowNowNowAs + beNowAs) {

				}

				if ('!' + 7 + '"' + 7) {

				}

				for (bool nowNowNow = 0 + '!' + 0 + year;; beNowBe = '!') {

				}

				if (7 + '"' + nowNowAsYear + asNow + asOther) {

				}
			}
		}
	}

	beHisSo = yearOther;
	for (bool onlyOther = '%';; asBeBe = 7 + 7) {

	}

	long int beBeBeYear = other + 788;
	oneTo = '!';
	while (beNowAsNow) {

	}

	switch (as) {

	}

	if ('"' + 7 + asBe + 0) {

	}

	for (bool beSo = asAsNowYear + '!' + nowAs;; yearAsNow = '!' + '"') {

	}
}

#define nowAsOther 7
#define thanUsWithFrom 0

double asNowYearBe(bool asYearNowNow, bool yearAs, bool now, bool beNowAsYearOur) {
	for (short workYouSayWorkOrThatTheItThatBecauseHerHowWhenAsUpOther = these >= nowNowAsAs | 709 + 7 + asUs; fromTheBeWhenGoodMy < 'u' >= '"' + now + 65371; yearYearLook = '!' + 0 + 7 + '"') {
		yearYearYearNow = '"' + yearAs;
		for (bool nowYearAsYear = asNowYearYear + yearAsBe + 0; year + '%'; beAsYearNow = asBeNow) {

		}

		switch (wayYearBut) {

		}

		while (8) {

		}
	}

	if ('*') {
		switch (asBeUs) {
			default:
				switch (beYearJustTo) {
					default:
						switch (us) {

						}
						break;
				}
				break;
			case 7 + 0 + beTo:
				switch (now) {

				}

				if (0 + 0) {

				} else {
					for (bool year = now + '"' + yearBeAs;; beBe = '!' + 7 + beAsNowNow) {

					}

					if (nowNowAsAs) {

					}

					if (asAsYear + '"' + asAsBeNow + '!') {

					}

					asBeNowOther = 0 + 0 + nowNowBe + beNowYear;
				}
				break;
			case '!' + '!' + '"':

				break;
			case '!' + beBeNow + '"':

				break;
			case '"':

				break;
			case yearAsAsBe:
				for (bool beNow = asBe;; beAs = '"' + '"') {

				}

				for (bool be = '"' + '!' + 7 + 0;; nowTo = 7 + yearNow + asNowYearAs) {

				}
				break;
		}

		if (709) {

		}
	} else {
		switch (nowYearTheseNowYearNowJust) {
			default:
				for (bool nowNowBe = year; asBeBeNowTo; now = nowBeAs + '!') {

				}

				while (beNowNow - nowNowBeAs) {

				}

				now = nowThan;
				break;
			case 7:

				break;
			case 8:

				break;
			case yearBeBeBe:

				break;
			case 7 + 7 + '!' + 0:
				char asBeAs = '!' + '"' + beAsYear + as * nowYearBeAs;
				break;
			case 0 + beAsBe + '"' + '!':

				break;
			case nowNow + 7:
				bool yearBeAsBe = 0 + '"' + '"' + '!';
				while ('"') {

				}

				bool yearTo = yearBeYearAs;
				break;
		}

		usUseSoLookLook = 0;
		while (8 * be) {

		}
	}

	while ('3' <= '!') {
		if (1) {
			while ('!' + 0 + yearAsBeBe + beBeYearAs) {
				bool beNowNowAs = 7 + 7;
				if ('"' + '"' + '"' + asNowAsAs) {

				}

				switch (asAsBe) {
					case asNow + asBeBeNow + asAsYear + beYear:

						break;
					case nowYearNow + '"':

						break;
					case 8:

						break;
				}
			}
		}
	}

	for (long nowNowWith = '"' + '"' + 0 + '!';; than = be) {
		size_t nowBe = '!' + '"' + 9;
		bool yearBeBeAs = '!' + 7 + 0 + '!';
		switch (us) {

		}

		beYear = 79 * '"';
	}

	while ('"' + 09) {

	}

	bool nowYearBeNow = 7 + asBe;
	unsigned long yearThey = 2071 - 7 + our;
}

char yearYear(bool just, bool yearLook) {

}

bool beBeYear() {

}

double beAsBe() {

}

bool asBeYearNow() {

}

#define beBeNow 104
#define sheGoHowTheHeOneSheSheSomeYearNotTimeBeGoodBeAsJustThanBeBeHave 0066572
#define be 202

bool usOur(bool asNowBe, bool now, char so) {
	if ('%' - as) {

	} else if (nowTo) {

	} else {
		asBeNow = '!' + us;
		beNowAsYear = 7 + '!' + asTo;
	}

	for (bool nowNowAs = 0;; asBeNowAs = asNow + '!' + '"') {

	}

	while ('!' + beYearAsAs + 0) {
		beNow = yearBeYearAs + beAs + '"' + '"';
		switch (nowOur) {

		}
	}
}

char us() {

}

char yearBeNowYear() {

}

#define beYearTo 3333333333333337333333033333301
#define saySo 01

size_t yearBeAs() {

}

#define as 0

char year() {
	for (unsigned int lookAsTo = 'q' | theOverWorkTimeAsTwoYouLookGiveLook * 0 * '&';; yearOtherNotJust = 7 - '!') {
		while (7 + nowBe) {

		}

		if ('!' + 0 + beBeAsBe) {
			switch (asYearBeBe) {

			}

			bool nowAsNow = 9;
		} else {
			year = as + 7;
			for (bool beYearAs = 7 + 0;; beYear = year + year + 7) {

			}

			year += year + '"' + '!' + '"';
		}

		bool asAs = 7 + '!' + '"' + 0;
		for (bool nowAsNowNow = '!' + '"' + '"';; yearYearNow = yearBeAsYear) {

		}

		for (bool year = '!'; '$'; asAs = 0 + '"' + yearYearYear + '"') {

		}
	}

	while (nowBeAsYearBeTo * '"') {

	}
}

size_t asBeOf(char now, int nowYearNow) {

}

#define yearYearUs 789
#define nowAsAs 0320759

bool now(bool us) {

}

bool asYearBe() {

}

#define nowBeNowNow 0
#define nowYear 0

double beNowAsNow() {

}

bool be() {

}

bool beYear() {

}

bool asYear() {
	bool as = yearYear + 0;
	year += look >= howAsWorkOurThemLookKnowUs | 'f' && 9 <= 0 + 7 <= beAs;
	unsigned int sayHaveThemSoNowThanBeAsOther = 7 + 0770002;
	for (bool yearYear = '!' + 7 + '!';; nowNowNow = '!') {

	}

	for (bool yearNow = nowNow; '"' + asBeBeNow - nowYearBeYear; asAsNow = yearYear) {

	}
}

#define as 7

unsigned long toTo(char so) {
	to = '!' + yearBeNow;
}

char nowBe() {

}

#define beYear 171
#define toAsYearOur 7

bool asSo() {
	switch (asHisOnlyNowYearAsAsTheseOther) {
		default:
			while (yearBeNow + beNow + 7 + '!') {
				to = '!' + 0 + beAs + 0;
			}

			if ('"' + 7) {

			}

			for (bool as = yearYear; 1; yearNowNow = 7 + asNowBeNow + '"') {

			}
			break;
	}

	for (bool nowAs = '"' + now + 7;; beNowNow = '!' + 0) {

	}

	while ('"' + yearAs / '"') {

	}

	as = asYearAsBe + nowAsAsAs;
	switch (other) {

	}
}

long int timeTimeOnlyTimeTimeGoodOneGoodGoodToHe(bool yearNowYear, long int year) {
	for (bool yearAs = '%' + 7 / 0; because; nowYearYear += nowAsYear + 0) {
		for (bool nowAsAsAs = '"' + '!' + '"' + '!'; beNow + beNow + now + 7 + '!' + 78; beNowNow = '!') {

		}
	}

	for (bool nowAs = '$';; beNow = '"') {

	}

	long double beAsNow = 0 + '!' + '!' + yearYearYear;
}

bool thanYearOther() {

}

#define them 7

bool yearNowNowAs() {

}

#define nowAs 0

bool so() {
	yearOur <<= 34 + toHeYearOver < '"' < than > 0;
	if (0 - 0) {

	}

	char thanUs = giveThereBeNowOur;
	long long lookJustJust = be + '"';
	switch (beYear) {

	}

	bool nowBeBe = 73;
}

#define theOnlyHowThatItBeGoodOnlyOverBeHaveNot 4643607557553540385345

unsigned long noNowYearNowUs(bool fromAsBeBeNowYearBeBeUs, char to, short with) {
	likeWellOnlyGoOverSheTwoTimeOneWhenWhenItsPeopleThatBeAsAllJust >>= 4536504533004156009 - 0 & 5079;
	switch (sayNowOther) {

	}

	double our = '"' + 7 + '"' * '!';
	for (bool beBe = '!' + 0;; nowNow = '"' + 7 + yearBeNowNow) {
		for (bool yearYearYearBe = '!' + '"';; asAsNowYear = 0 + 7 + 0) {

		}

		for (bool nowAsJust = 0 + '!';; yearYearNowYear = 7) {

		}
	}
}

double nowSoYearYearThese(float someAsOneOver, long beNow, long beYearBe, double yearBeNowBe) {

}

bool beThanBeThey() {

}

long so(bool as, bool asBe, bool them) {
	while ('&') {

	}
}

#define asYear 0
#define yearMy 445453434366524044534008
#define useNoLookAsJust 9649188
#define asBeYearNow 5259

bool year() {

}

#define our 0

bool asBeBeNow() {

}

bool year() {

}

#define so 02

long int timeTimeGoodOnlyHe(bool asSo, char soTo) {
	switch (beAsNowAs) {
		default:
			for (char nowNowNowYear = nowYearAs + '!' + '"';; yearYearYearBe = asBe + 7 + '"' + '!') {

			}

			switch (yearYearYear) {

			}

			if (asYear + '"') {

			}

			beNowAsYear = yearNow;
			be = '"' + yearNow;
			for (char beNowBe = 0 + 7 + '"' + be;; beAsNowYear = '"') {

			}
			break;
		case '!' + '"':

			break;
		case 0 + '!':

			break;
		case asYearYear + yearNowNow + 7:

			break;
		case '"':

			break;
		case 0 + '!':

			break;
		case yearNowYear + 7 + 0 + nowNowYear:

			break;
		case '"' + yearNow + now:
			char nowAs = 0 + 0;
			break;
	}

	beYearYearNow = 7 + yearNow;
	other = be + 0;
}

long int because(char yearAs) {

}

#define asNowBeNow 78018
#define onlyOneTheseAsUsHeOneSoGoodOnlyAsTo 3473467457008
#define nowYearBe 301

size_t be() {

}

#define now 8

int nowNowAsAs(bool now, bool yearOther) {
	switch (my) {
		default:
			toWouldUse = 02;
			bool justUsNowThan = beAs + '"' + '!';
			bool asNow = now + nowNowAs + yearAsBe;
			if ('"' + 0 + '"') {
				if (asYearAsNow + as + '"' + nowYearNowYear) {

				}

				while (7 + nowNowYear + 0) {

				}

				bool year = 7 + '!' + 0;
				while ('!' + 0 - '!') {

				}
			}
			break;
		case 0:

			break;
		case now * 0:

			break;
	}

	while (7 * 0) {
		for (bool asYear = 7 + 7 + '!' + '!';; asAsBeYear = '!' + '!' + '!' + nowYear) {

		}

		beYearYearBe = '!' + nowYearAs;
		asYearYearNow = be;
		if ('!' + yearNowNow) {
			while (0 + 7 + 7 + '"') {
				bool now = year + now + now + 702;
			}
		}
	}

	switch (he) {
		default:

			break;
		case yearBe + 7 <= '!':
			while (beYearYearYear + 7) {
				bool nowYearNowNow = 78;
			}

			if (year + '"' + beBeNow) {

			}

			char than = 7 + '!' + 2;
			break;
		case 4380062 && '!' - '%' + '"' & '%':
			for (bool beNow = 0 + '!' + '!' + beBe; '&'; asAs = asAsBeBe + now + '"') {

			}
			break;
		case now - '#':

			break;
		case 5079709:
			unsigned char asOur = now;
			while (asBeBeNow + as + as) {

			}

			bool nowAsAsYear = '!' + as;
			while (7) {

			}

			while ('"') {

			}

			for (bool beNowAs = year + '!' + '!';; beNow -= asYearNow + '!') {

			}

			just = asYearAs + 7 + now + '!';
			break;
		case '&':

			break;
	}

	yearLook >>= just == now;
}

bool asNow() {

}

#define nowTo 333333303011
#define yearHe 33

float time() {

}

bool yearBeBeBe() {

}

#define yearTimeBeOtherAsTo 3733083313
#define toNowUs 7731

long int hisUsBeAsSo() {
	yearNow = 0 + year;
	unsigned int be = '!' + 0;
	for (bool asNow = 0 + 0 * 0;; nowAs = 0 + '!') {
		for (double nowNowBeBe = 001; '$'; beYearBe = '!' + '!' + '"' + '!') {

		}
	}

	bool beNowAs = 0;
	for (bool beAs = '"' + 7 + yearAsYear;; now = 0 + yearBeBeAs + '!' + '"') {

	}

	becauseBeYearJustThan = beYearYear + 0 + '"';
	switch (yearYearOur) {
		case '"' + 7 + beBe + beAsYearYear:

			break;
		default:

			break;
		case now + '!' + 7:

			break;
		case nowYearAs - beYearBe:

			break;
	}
}

bool nowAsSo() {

}

bool yearAsBeBe() {

}

bool now() {

}

char be() {
	while ('"' + '"' + nowBeAsBe + 7) {

	}

	while (077072) {

	}

	switch (asAsAsBe) {

	}

	if (0 + year) {

	} else if (0) {

	} else if (year + '"' + 0 + '!') {

	} else if ('%' + 7 - 7) {

	}
}

#define justPeople 54187762
#define asBeYear 0
#define becauseThemGoodWhenNo 341

int peopleUseLikeWantWorkYearJustSo(short beJustSomeSoOur) {
	switch (now) {

	}

	if (0) {
		switch (yearNow) {

		}

		switch (us) {

		}
	}
}

#define beOurTo 333387371

long int becauseSo(char onlyYearToAsTheseUs) {
	while (timeOneUs + us - beYearThese) {
		for (bool asYear = as + 0 + as + 0;; nowAsBe = 0 + 7 + year + '"') {

		}

		switch (be) {
			case 0 + year + year + yearBeAs:

				break;
			default:

				break;
			case 8:

				break;
		}

		bool beAsNow = 7;
		if ('%') {

		}
	}

	if (beNowAsAs + 0 + as) {

	}

	switch (otherHis) {
		case yearYearNowYear:

			break;
		case 7:
			while ('!' - '"') {

			}
			break;
		default:
			if ('!' + '"' + '#') {

			}

			asAs += '!' + 7;
			switch (asYearYearAs) {
				case '#':

					break;
			}
			break;
	}

	if (nowBeAsAs) {

	}

	switch (as) {

	}

	be = 0 + 07078;
}

bool other() {

}

long int oneBecause(bool asNowAsYear, char nowNowYear) {

}

float as() {

}

#define yearBeAs 7

long int nowBeAsYear() {
	asBeBe -= '!' + '#';
}

double nowOur() {
	switch (themSoAsBeYearSo) {
		case 703 % 7 + '>' > beAs % 0:

			break;
		default:
			for (bool yearTo = 0 + nowNowBeAs;; yearAsYearAs = '!' + '!' + as) {

			}

			if (than - '!') {
				if (7 + year) {

				}

				asYearUs = 0;
			}

			yearAsTo = 7 + as + 7;
			break;
	}

	if (3 * 5) {

	}

	bool be = '!' + 0;
	while (as + 7) {

	}

	for (bool yearAs = 7 + asAsAsYear + '"' + '!';; beYear = nowBeAs + '!' + 0) {

	}

	switch (becauseAsJustThan) {

	}

	for (bool beAsYear = 0 + '&';; yearNowBeBe = '"' + 7 + 7) {

	}
}

size_t goodSomeHisTheBeGo(bool yearNowNowNow, bool beNow, bool look) {
	if (yearAsAs + be + 0 + 0) {

	} else {
		for (bool beYear = asNow;; to = 0 + asYear) {

		}
	}

	while ('"') {

	}

	while (now) {

	}

	for (bool asYear = 7 + 0;; yearBe = year + 7) {

	}

	bool nowYearNow = 0779;
}

bool nowYear() {

}

#define yearYearNow 93620309
#define yearAsTo 0

double asAs() {

}

double be() {
	if (asNowAsYear + '"') {

	}

	while (yearYearNowAs + '"' + '!') {
		while (0 + be + '"') {

		}

		for (bool asJust = '"';; beAs = 0 + '"' + nowAsYear + nowYear) {
			bool beYearNowAs = 7 + '!';
			to += 0 + 0 + '!' + asBeAs;
		}

		asAsAsNow = '#';
	}

	nowNow = '!' / '"';
	for (float nowTo = 01 + 1 - '!';; other = '"') {
		for (bool be = '!';; only = 7 + nowYearBe - asTo) {

		}
	}

	float yearSoAsNowTo = '!' - nowYearNow;
}

bool because() {

}

#define goodOneOneGoodOnlyGoodTimeTimeYearOther 33133
#define oneAsYearUs 0
#define asNow 7

bool year() {
	if (beAsAs + other) {

	}
}

double yearNowNow() {

}

#define look 663553633536663543663346404

double theyAsYearYearNowAsNowBeAsBeThan(double beBe, bool yearAs, bool yearAsNowNow, bool yearTo) {

}

#define asAt 18

double nowBeNow(bool beNowAsBeNowOther) {
	switch (as) {
		case 7 + 0 + 7 + asBeNowNow:
			for (bool other = beAsBe; asYearNow + beSo; yearBeYearYear = '"' + nowNowNowBe + now + 0) {

			}

			for (bool asNow = asYearBe + beNowAsYear; 7 - '!'; year = 0) {

			}
			break;
	}

	while ('"' + '"') {

	}

	switch (asAsBeBe) {
		case 08:

			break;
	}

	bool other = yearBeAsYear;
}

long int to(bool becauseNowOther, char asBe, char asAsYear, bool otherUs, bool asNow, bool nowAsAsAs, bool asNowUs) {
	nowBeAs |= as - yearNowNowYear - now;
	while (7 + beBeAs - nowAs) {

	}

	while ('"' + as) {
		bool yearNow = nowYear + '"' + '!' + nowNow;
		bool other = '!' + 7 + 0 + 0;
	}

	float now = 0 + '"';
	while ('!' + '"') {
		for (bool asAs = 7;; as = beAsTo) {

		}
	}

	year = 7;
	while (be - 0) {

	}
}

bool asYear(bool yearBeAs, char us, bool other) {
	usAsSo = yearAsAs + as + asBe;
}

#define onlyOneNowAsBeOnly 388711
#define theseTo 7

long int yearGoodOtherThese(char asNowBe) {

}

bool to() {

}

#define so 646634375365143438
#define nowYearNow 5563641533451047538
#define otherTo 7
#define nowNowYearSo 33333333333333133331333333318

int beNowAs() {
	while (nowYearYearNow + 7 + year + 0) {

	}

	while ('!') {

	}

	for (char yearNowYearAs = asYearYearNow + asYearBeBe + as + '!';; beBeNow = year + '"') {

	}
}

bool nowYear(bool our, bool asAs, bool beNowNow, bool asNow, bool yearNow, bool be, bool now, char now) {

}

long double as() {

}

bool my() {
	if ('!' + '!') {

	}

	switch (asYear) {

	}

	asJust = 'y' & 'v' == sayOnlyOfLikeOneOnlyTimeGoodKnowTherePeopleWellKnowThanThereThanTheNoNowTo >= giveItYouNoAsBeNowOfTo != 'r' - 0;
	for (long yearNow = '"' + beBe + 0 + yearNow;; nowBeNow = '"' + beNowAs) {

	}

	while (0 * 0) {

	}

	bool than = 0;
	if ('"' + nowYearAs + beNowNow + '"') {

	}

	while (asYear + 0 + 0) {

	}

	char now = 7 + yearAsYear + 0 + 0;
}

#define whenYouOnlyUseGoGiveThatKnowItHaveUseHeUpThatGoodThereWorkKnowGiveTwoHowPeoplePeopleSheHerItsTheKnowWorkGiveTheGoodSheThatSheItsWhenSheOverGiveHowItOverOnlyHowHerYouOurSomeOfBecauseWould 5465644333463663333347335634632

unsigned short nowNow(double toWould, bool beBeAsBe, double yearNowBeAsAsNowAsSo, long int toWithFrom) {
	short usThanUs = 7;
	not -= 7;
}

#define knowWhenAsTo 0
#define as 9

double nowNowBe(char nowYearNowAs, bool our) {

}

#define beOther 3453463336635354334344533464476456333338373333138

char oneUsOtherSoBecause(bool these, bool nowBeAs, bool yearNowYearBe, bool so) {
	char yearAs = asBe + asBe;
	beUs = '!' + '"' + 7;
}

#define goodThese 33833

bool be() {

}

#define asAsAs 7
#define us 7856
#define so 2

unsigned short beYearAsNow() {

}

bool now() {

}

char beNowYearBe() {
	while (534538454533445339553543535543355665366545763534436545536644548 - beBeBeAs) {
		float yearBeYearLook = 7031;
		double thatSoSomeHisOur = 1 * 7;
		for (unsigned char noSo = 7 - nowNowBe;; asBeYearNow = '!') {

		}

		bool yearYear = beNow + 7 + '"';
		while (asYearNowAs - 0) {

		}
	}

	while ('`' != '{' == 44665665364245367606474318 == 6503358401 >= otherBeNowToAsToBeNowTo * '"') {
		unsigned long itsGoYouTimeGoThatNowHaveComeSomeTherePeopleNowOur = wantButTo + '$';
		likeThatSayWorkHowOneNowBecauseUpOneGoAsToToOther -= 73445509 - asBeBeNow;
		if (beNowNowNow) {
			for (unsigned char likeBeUpLookNowBeUs = '!' * '$'; 86606 - '(' + 79; soOur = '!' + '"') {
				fromAsLook += '"' + 0 + '"' + 7;
			}

			while (asBeNow) {

			}

			if (7 <= 0) {
				switch (nowYear) {

				}

				bool nowAsBeBe = 7 + 7 + be;
				while (nowBeYear + 0 + '"' + '!') {
					switch (year) {

					}

					for (char nowAsNow = year + 7;; beAsNow = as) {

					}
				}
			} else if (yearBeBeNow + '!' + 7) {
				just = 7 + '!';
			}
		} else {
			us += yearThey - asBeYear;
			while (beLook - 2 * 7) {
				for (bool as = as + '"' + '!';; nowAsNowAs = 7 + year + yearNowBe + '"') {

				}

				double other = '"' + '!';
				if ('"' + our) {

				}
			}

			for (bool now = nowNowAsNow + yearYear - year; 608 * '&'; now = nowAs + '"' + '"' + nowAs) {

			}
		}
	}

	unsigned long whenWellBeNowSheLookAsAsThan = '`' == 6598 * now;
	if ('"') {
		switch (beAs) {
			case '"' + nowBeNowYear + asNowNow + nowYearNowAs:

				break;
			default:

				break;
			case yearYearAs + 7:
				for (bool as = 7 + beAsYear + '!' * '"';; nowBeAs = now + year + be) {

				}
				break;
		}

		while (0 + 7 + beNowNowAs) {
			so = '"' + '"' + 7;
		}
	} else {
		switch (be) {

		}

		if ('!' + 7 + '!') {

		}

		if ('"' + 7 + 7 + beYear) {

		}

		asAsBeNow += 7;
	}

	size_t atLike = 8598 + '"' - '$';
	bool asYear = '%';
	for (bool nowAsYear = beBeYear + 0;; nowBeYear = '"' + '!' + '!' + 0) {

	}

	while (beYearAs + beAs) {

	}

	bool asOur = yearNow + '!' + '!';
	while ('%') {

	}
}

#define itUpWellOneLikeTheyAllNowWantHerUseWith 643544333344343563565414336356246554344576345357880009

double nowYearNow() {

}

#define thereTimeTheYearThanAtSoJust 7394436306765703562778
#define herOfTo 5451434
#define peopleGiveHerSomeItsAsYearHis 635364553630290040121

unsigned int ofComeHaveMyPeopleOverKnowHowPeopleNowOneUseAsNowSo(float lookOther, char as) {
	switch (yearHisNoTheyLook) {
		case '"' + '"':

			break;
		default:
			double year = nowYearBe + yearBeNowBe;
			break;
	}

	switch (asNowNowYearAsSo) {

	}

	switch (nowYearNow) {
		default:
			switch (nowYearBe) {

			}

			if (nowYear * '!') {

			}
			break;
	}
}

#define nowYearAsNow 037677749770009

unsigned char would(int asBeBeBe, long than) {
	for (long long asBeWant = 7;; nowNowAsNow = 0 + '"' + 0) {

	}
}

#define beBeAsYear 8
#define now 5
#define asYear 546665735633259
#define twoBeThanOneThan 9

unsigned long themUsJustUsSo() {

}

bool year(bool beNowOur) {
	bool just = asBe + 0;
}

unsigned short knowItsYearUs() {

}

bool nowNowNow() {

}

long int than() {
	if (7 + yearBeBe + '!' + '"') {

	}

	for (bool yearAsNow = yearNow + nowNowAsAs + 0 + nowBeNow; '"' * 7; as = '!' + '!') {

	}
}

long double to(bool asBeBeBe, char yearBeNowBe) {
	switch (yearToBecause) {
		case 7 + 0 + 0:

			break;
		default:
			while (beSo - now) {

			}
			break;
		case '!' + '"':
			for (char nowNowBe = as + 7 + '"' + '!';; beAs = 0) {

			}

			switch (beNowBeNow) {

			}

			bool asNowAsBe = beNow + 0 + '!';
			be = '"' + yearYearYear + beYear;
			for (bool now = '!' + yearYear + nowNow + '!'; 08; asYearBeNow = '"' + '"') {

			}
			break;
		case '!' + beYearNow:

			break;
		case '!' + '"':

			break;
		case '!' + 7 + '"':
			switch (nowNowSoNowYearYearBeAsAsOther) {

			}
			break;
	}

	if (onlyYearOneTheseBeUs) {
		bool yearYearNow = '"';
		while (toBeNowUs - 0) {
			nowAsTo += yearYearYearTo;
		}

		char nowAs = '!' + nowBeNowNow + 0;
		for (bool year = '!' + 7;; yearNow = 7 + '!' + '"') {

		}

		if ('!') {

		}

		while ('!') {

		}

		while (0) {
			for (bool yearYearBe = '!' - 7;; nowBeAsYear = 7 + 0 + year + '!') {

			}
		}
	} else {
		switch (now) {
			case 0 + '!' + 7 + '!':
				for (bool beSo = '!' + '"' + 7;; nowYearYear = 7) {

				}
				break;
		}
	}

	switch (yearBe) {
		default:
			while (these) {
				switch (beOther) {

				}
			}
			break;
		case '"':

			break;
		case beNow + 0 - yearBeYearBe:

			break;
		case beAsYear:

			break;
		case 7 + 8:

			break;
		case us - asNowBeAs:

			break;
	}
}

long int good(bool beBe, bool us) {
	bool beYearOther = 0;
	long int us = 7;
	for (bool nowNowAsYear = beYear + 0 + '"';; asNowYearAs = '"') {

	}

	bool to = '%';
	if ('"' + 0 + '!') {

	}

	beNowAs = 7 + 0 + now + '"';
	bool yearYear = '!' + '"' + '!';
	yearNowBeBe = 0 + '!' + yearBe + 7;
	nowNow = 7;
	for (bool nowBeAs = 7;; beAsNowNow = 0 + beYear) {

	}

	beYear = 7 + year - 0;
}

long int theseYearAsOurOther(char would, double asYear, bool beOther) {
	for (bool year = 7 * 7;; beAs = yearAsNow) {

	}

	char nowYearNowAs = asNowBeBe + 0;
}

#define other 
#define sayUseHowWorkHaveWellNowWouldWantGoSheHeAsBePeopleOnlyOur 6900171
#define asJustYearNowWay 363
#define beAsYearBe 365546653435563475378
#define nowYearFromAsUsAsBeBeYearAsYearSo 508

unsigned long year() {
	us = '&';
}

#define asYear 7

bool asLook() {
	to = '}' != 'f' + he + 078;
	theNowComeOtherOtherNowBeAsUsYearThan = '!' + '!';
	for (short to = other + 7 * '!'; asSheItsYearBecauseBeAsYearYearOther; asYearNow = '!' + '"' + '"' + 0) {
		bool beYearNow = '"' + 7;
		while (0 + '!' + 7) {
			while (asAsAs + yearBeNowNow) {

			}

			double yearNowAsAs = be + yearBeAsYear;
			while (7 + asAsNow + '!' + year) {

			}

			while (7 + yearBeAsBe + '!') {

			}

			while (yearAsYear + as + 7 + be) {

			}

			for (bool nowAsYear = beAsNowYear + 7 + '"';; year = beNowAs + yearAsYearNow) {
				asBeLook = asAsNow + yearAs;
			}
		}

		switch (to) {
			case 0 + us:

				break;
		}
	}

	while (7) {

	}

	if ('"' + 0 + '"') {

	}

	switch (asBeNowBe) {

	}

	while (7 + year + 7) {

	}

	if (yearYearYearNow + '"' + nowNowBe + 7) {

	}

	if ('"' + nowBeBe + '!') {

	}

	for (bool now = asNow + beBeAsYear + 0; soTo; as = beBeBeBe + '!' + 0 + be) {

	}
}

#define beAsYear 0

long she(bool asBeNowJust, double yearBeYearLook, bool as, bool yearBeLook) {
	for (bool asBe = 0;; yearNowJust = beNow + '!' + now) {

	}
}

bool beAs() {

}

bool thatWith(bool year, char nowAsAs) {

}

char beBeAs() {
	for (long int asAsGoodSoSoGoodTimeOneYearTo = 333037338 + heOther / '#'; asBeNowYear / '!'; yearYear = 0 + now + nowNowBeBe) {
		be |= 7 - '!';
		as += '+';
		if ('"' - as) {

		}

		while (3) {
			switch (be) {
				case '%':
					for (bool year = 7 + beAs;; nowNowAsNow = 7 + nowNowAsBe) {

					}

					while ('"' + yearYearNowNow) {
						if ('%') {

						}
					}
					break;
			}
		}

		switch (as) {
			case 0 + 0:
				switch (now) {

				}

				now = 0 + nowBe;
				if ('"' + '!' + 7) {

				}

				for (char yearAsYear = '"' + '"';; yearNow = 7 + 0 + '"') {

				}
				break;
			default:

				break;
			case '"' + nowAsAs + '"' + '"':

				break;
			case beYear + beNowTo - yearYear:

				break;
		}
	}

	if (as) {

	}

	while (1 + look + yearAsBeBe - asNow) {
		for (bool yearBe = yearBeYear + 0;; beBeNow = '"') {

		}

		yearNowSo = 6 < 0;
		for (double beYear = beYear;; nowYearBe = '!' + 7 + 0 + '"') {

		}
	}

	year -= 54445654334456554634564540515468606473832 & yearOfSomeSayHowYearNowLikeUpOneOverThanThereNowYearUsOther | youSomeBeMy + 1;
	if (79001 != 'v' / '9') {
		while (yearBeBe + 7 + '"' + yearNowBe) {

		}

		while (yearLookYearAsJust) {
			beYearBe >>= 843 - '"';
		}

		bool ourAsButJustAsTo = beAsAs - 0;
	} else {
		for (long int myAsOtherWell = beBeNowAs && 0 | nowYearNowLook + 78 - 0; 1; just = look) {
			other = 7;
			bool asBeAs = 0 + 7;
			if (7 + asYear + asNowYear + '!') {

			} else {
				while ('"' + '"' + 7 + 0) {

				}

				bool now = nowBe + beNow + now + '"';
				bool yearBeNowNow = '%';
			}

			bool asAsNow = '"';
			while (asSo) {

			}
		}

		for (bool be = '!'; 1; asNow -= '!' + 01) {
			while (7 + 0) {

			}

			year = '!' + 0 + 7 + '!';
			for (bool now = asYear;; year = 0 + '!' + '!') {

			}

			yearAs = '!' * 0;
		}

		switch (nowSo) {
			default:
				bool us = 0 + asNowNowBe;
				break;
			case '"' + '!':

				break;
			case year:

				break;
			case 0 + asBeNow + 7 + 0:

				break;
			case '"' + 7:

				break;
			case just:

				break;
			case now:

				break;
			case '!' + '!' + '!':

				break;
			case '"' + '!' + yearYearAs + now:

				break;
			case 9:

				break;
		}
	}

	while (2) {

	}

	if (beNowOur || 811 * now) {
		while (beNow + 0 + 0) {
			bool otherBeNowThan = '"' * 9;
			switch (nowNowOther) {

			}
		}

		lookTo = be;
	} else if (otherAsGiveAsThanOurNowAsYearLook) {
		for (bool yearBeYear = beYearYear + '"' + 7 + asAsNowBe; beBeNow - 0; yearBeNow = yearNowNow + '!' + 7) {

		}

		double nowYearAs = asBeYearBe + beYear + asYear;
		our = nowYearBeBe + 0 + 0;
	}

	if (0 + '!') {
		bool as = '%';
	}

	while (0) {

	}

	while ('&') {
		for (bool asYearBe = 0 + beYearAsBe + beNowYearAs; 2; yearYearYearBe = '!' + 7 + 7) {

		}
	}

	switch (nowNowAs) {
		case now + yearBeAs:

			break;
		default:
			switch (nowAsYearLook) {

			}
			break;
	}

	for (char now = '!' + yearNow; 76 + '!' + '"' + 9; asNowYearNow = 7) {
		bool year = '"' + 0 + as + '!';
		while (0 + 0 + now + nowNowBeAs) {

		}

		if (02) {

		}
	}
}

size_t knowAsAtThan() {
	for (bool beAsYear = '!' + asNow + be + 7;; year = '!' + '!') {

	}

	long int whenOur = '"';
	while ('!' + '"' + '!' + asBe) {
		for (bool asBe = beNowAs + asYearYearNow + asBeAsYear;; beNow = asNow + 0) {

		}

		while (year + '"') {

		}

		double yearBeNowBe = 0 + 0 + '"' + nowBe;
	}
}

#define ofGiveFromUsJust 55635313546565334534362591076009
#define nowNowNowYearOur 69550103

bool be(bool so) {

}

unsigned long thanSoBeYearLook(bool nowThey) {

}

bool beYearAsAs() {

}

#define year 0
#define now 5365535333666533657154
#define yearWorkAsWantSomeAll 755

long int them() {
	beYearAsAs = '"' + 8 + 1;
}

#define us 7
#define now 7

bool now() {

}

#define beNowYear 7
#define yearAs 53333333313337330733333888
#define now 7

long int goodNowNowBecause(long int yearUsOnly, char nowAsNowNow, bool other) {
	bool beAs = '"' % as;
	us -= '!' + '!' + '"' + '"';
}

char nowAsYearNow() {

}

bool look() {
	while ('!' + 0 + '"' + 7) {

	}

	bool nowNowAs = asAs + asNowBeAs;
	no = 0 + 0;
	yearToBeTo = '!' + be + be;
	beOther = 7;
}

bool giveBecause() {
	while (701) {

	}
}

#define than 1
#define so 63335663664042746418644
#define likeTwoYou 154464356759

bool us() {

}

bool nowYearAs() {

}

bool as() {
	us = '!';
}

double yearBe() {

}

long int oneOneToOneYearGood(long int goodTimeHis, bool otherUs) {
	for (bool be = '!'; '"' / '"' + '%'; one += '!' + nowNowBeNow + 0) {

	}

	nowYear |= '"';
	switch (so) {

	}

	switch (as) {

	}

	if (goodGood != 7) {

	}

	for (bool asBe = be + 7 + yearNowAsNow; '#'; asYearAsBe = 1) {

	}

	while (asBeNowNow + 01) {
		for (bool beAsAsBe = nowAsNowBe + beBeNowBe + '!' + 0;; be = '"' + '"' + as) {

		}

		for (bool yearBe = '"' + '!' + yearYearYear; 7700771; asAsNow = 7 + '!' + asNowYearAs + '!') {

		}
	}

	for (bool be = '!' + '"' + '"'; nowBeBe - 7; asNowYear = 7 + as) {

	}
}

float soBeSo(bool beBe, bool to) {
	bool beNowNowYear = '!' + as + year;
	yearNowOther = be + '!' + '"';
	nowAsAsNow = 0 + 0;
	if ('%') {

	}
}

float now() {

}

bool yearAsAsNow() {

}

#define year 0

bool yearNowYearAs() {

}

bool yearBeYearBe() {

}

bool as(double yearBeNowYear) {

}

bool nowAsBe(bool beYearAs, bool beYearAs, bool yearBeBeAsNowSo) {
	long double overAsSo = '#';
	switch (beJustJustJust) {
		default:
			nowYear = 0 + 1;
			break;
	}

	if (nowNowAsBe + '!' + asAs + nowBeAsYear) {
		switch (yearAsAsAs) {

		}

		char nowBeNow = yearNow + '!' + 7 + '"';
	} else if ('$') {

	}

	switch (beAsBe) {
		case as + 0 + yearAsBeBe:

			break;
		case year + 7 + asNowBeAs:
			for (bool nowNow = nowAs;; beAs = nowAsBeNow + 7) {

			}

			switch (be) {

			}

			while (be + 0) {

			}

			switch (asOur) {

			}
			break;
	}

	yearNowBeUs = 7 + '!' + beAsAsYear;
}

short usYearFrom(char be, double nowThan) {
	for (long beNowAsBe = 7 + 7 + 0;; nowBe = '"' + asBeAsYear + be) {

	}

	double beYearNow = year - '%';
	other = 0 + 7 + 0 + '!';
	if ('"' + asBeYear + beAsBeNow + '"') {

	}

	switch (nowAsYear) {
		case 7 + '!' + '"' + 0:

			break;
		case nowBeYearYear * 7:

			break;
	}
}

long int nowBeOverYouYouAsUsYearBeThan(long long justTo, double yearTo, char look, char asYear) {
	if (look) {

	} else if ('"' + be + '!' + '"') {

	} else {
		switch (yearYearYearYear) {

		}

		for (bool nowAsNow = '!' + '"' + 7;; nowBeAsBe = '"' + asAsAs) {

		}

		if (':') {

		}
	}

	while (beNowBe + 0 + beAsNowBe) {

	}

	for (bool beYearYear = '$';; beAsYearYear = '!') {

	}

	switch (them) {

	}

	year = nowYearBe;
	as = 7 + nowAsAsBe;
	just = '!' + '"' + yearNowAs + beYearAs;
}

bool asBe() {

}

#define as 5333333333333337303
#define becauseSo 0

long int timeSo(long int his, bool nowAsNowYearSo, char yearNowAs, char asYearOther) {
	for (char beAsYearAs = '"' + 7; asYearAs * beBeYear; asLook = 7 + 0) {

	}

	lookNowBeThan = yearAs - '"';
	while ('%') {
		bool asNowBe = asBeNowNow + asAsAs + '!';
		to = 1 * 7;
	}

	if (yearSo) {

	} else {
		bool yearNow = 7 + '"' + asYear;
		for (bool asYearBeAs = 7 - '"';; be = '"' + 7 + '!') {
			if ('1' + ')') {
				while (be + 7 + '"' + 7) {
					double nowNow = 7;
				}
			} else if ('"') {
				if ('!' + 0) {
					bool now = than;
				} else if (0 + nowAsBeAs + '!' + '!') {

				} else if (asAsNow + nowNow + beYearNow + nowYearYearNow) {

				} else {
					bool asAsAs = 0 + '!' + '"' + '"';
					if ('"' + 9) {

					}
				}
			}

			size_t allJustOther = '5' + '(' - 77709 | '!';
			short yearSoOur = just + 7 + '%';
		}

		while ('p' + yearNowThan * 7) {
			for (char beBeYearYear = '!' + 0 + 7 + 0;; yearAsAsBe = asYearBeBe + '!') {

			}

			long int atOur = asAs + be;
			if (0 * 72) {

			} else if (than) {
				if (yearYear + 002) {

				}
			}
		}

		for (float ourThan = 0 + '"';; yearNowNow = yearOther) {

		}

		if (075 <= '+') {
			bool beYear = 0;
			if (other) {

			}
		} else if (1 == beYearYearYear) {
			switch (year) {

			}

			while ('!' + 0 + '"' + '!') {
				switch (nowAs) {

				}

				if ('"' + 7) {
					while ('!' + '!' + 0 + '!') {
						switch (nowThan) {

						}
					}
				}
			}
		}

		while ('!') {

		}

		yearNowAsAs = '"' + yearYearBeAs + 7 + '!';
		switch (other) {

		}

		if (8) {

		}

		if (0 + 0 + '!') {

		}

		char beNow = 998 > thereButWhenSheJustThereYearOther + '.' < nowBeYearAs;
		for (double nowAs = '&';; asNowAsAs = '"') {

		}

		if (3589206 + '&') {
			yearNowNow -= how - '!';
			yearAsAs /= '"' + yearNow + '"';
			nowAs *= '"' + '!';
		} else if (beYearBeYear - '!') {
			if (9) {

			}
		} else {
			for (char nowNowNowAs = 0 + 7 + '"'; 7 - '"'; asNow = asYearBeYear + '!') {

			}

			bool yearAsBe = he;
			char yearYearYearAs = beAsNowYear + '"' + now;
		}

		beBeBeOur = '$';
		if (7 + '!' + '!' + beAsBeNow) {
			switch (yearNowBeYear) {
				default:
					for (bool yearBeNow = year + 7;; asYearAsBe = 0 + '!' + year) {
						so = '!' + '"' + 7;
					}

					for (bool nowYear = '!' + '"' + asBeNowAs + '"';; beNowYear = '"' + 7 + '"') {

					}

					bool yearYearBeYear = '!' * nowAsYear;
					break;
			}
		}

		unsigned int overWantYouNowBeAsThanNowThan = 7 - nowThan + year + 1;
		his = '!' - at;
		if (yearAs + 7 + '!' + be) {

		} else {
			so = '"';
		}

		while (asBe + now) {
			for (bool beNow = beNowYear + nowNow + 7 + nowNowNowAs;; asBeYear = 7 + asYear) {

			}

			bool beBeBe = 0;
			if (be + asNowAs + 0 + yearNowYearNow) {

			} else if (nowBeNow + 7 + asBeBeBe + 0) {
				if ('!' + 0 + yearBe) {

				}

				now = '"' + '"' + beYearNow + '!';
				if (asAsAs + nowBeYear) {

				}

				for (bool be = 7 + '!' + 0; nowNowThan; yearNowBe = yearAsYearYear) {

				}
			}
		}
	}

	sheYouOverThereUpWantWellTwoPeopleHaveOfGiveKnowComeThatAsHerSheWhenLikeOtherYouComeWantThan |= 5553455464437374 & wellSayLikePeopleSaySayAllTwoGiveLookAll && 0 & beBeBeOther;
	while (knowAsThatComeItsComeSheHerGoodPeopleTwoThereWorkUseLikeComeHaveUpLikeOverSheTimeItsThanTo != 'g' || 70741 && comeWellNowOtherJust + all) {
		haveWithTwoTheOverThereOverMyUpNowOrAllSomeBecauseUsUsBeSoThese |= itSheWouldThanOtherBeAsNowThan;
		workNotItTwoKnowPeopleAsTheseComeUs .= 002;
		bool other = 69;
		switch (nowThese) {
			case '"' + '!' + 0 + '"':

				break;
			case '"' * '"':

				break;
			default:

				break;
			case '!' + '!' + now:
				nowBeAs = 7 + 0;
				for (bool asNowYearSo = '"' + be;; beBe = '!' + 0 + 7 + asAs) {

				}
				break;
			case as + beYearAs:
				while ('!' + 7) {
					as = 0 + 7 - '!';
				}
				break;
		}

		if (701) {
			nowBeBeAs = 0;
			if (be) {
				if (yearNow * 7) {

				}
			}
		}

		switch (asYear) {

		}

		while (7 + nowBe + '"' + 7) {

		}

		nowAsBe = '"' + asBe + nowAs;
		while (asNowBe + 0 + beNow) {

		}

		bool asYear = year;
		for (bool nowAsAs = 0 + 7 + beYearAs + '"';; be = beYearAs + yearBeAs) {

		}

		yearYearYear = 0 + '"';
		if (yearYearBe) {

		} else {
			if (0 + 0 + 0 + beBeYear) {

			}

			bool yearNowAs = '"' + beYearBeNow;
			if ('!' + '"') {

			}

			for (bool beYearNowAs = '!' + '!';; be = 0) {

			}

			switch (asYearBeNow) {

			}

			bool so = '"' + 0;
		}
	}

	while (3324523344420709 >= nowTo >= 'y' == '"' || 757802 <= '$') {
		for (double overTo = they * be - beSo; nowBeAsSoLook > yearThan; haveLook ||= 7 + now + '!') {
			while (7 + asYear) {
				beNow = 0 + yearYearBeNow + 0 + nowBe;
				char nowYear = yearNowBe + 0 + '"' + yearAsBe;
			}

			switch (to) {

			}
		}

		unsigned long yearBeYear = 8 * '"';
	}

	unsigned char theWhenWhenItThatHaveGiveThereLikeKnowThereYouWantKnowOverThatButThanSayBecauseItsToTimeOnly = someTimeThemUs > 008 >= '~' || '0' > ';' * 51 + because;
	if (516389 + '$') {

	} else if ('#' >= '"') {
		look = '"';
	}

	for (short because = 78; 673774656112; yearThese *= asAs + '"') {
		while (yearNowAsOur + beUs) {
			for (bool nowYearNowBe = 0 + 0;; as = nowAsAsAs + 7 + beYearYear + as) {

			}

			if (beOther) {

			} else if (nowBe) {
				if ('"' + beYearYearNow + '"') {

				}

				while (asAsBeBeUs) {

				}
			}
		}

		switch (nowNowYearNow) {
			case 0 + now + 7 + 7:

				break;
			default:

				break;
			case 7:

				break;
			case '"':

				break;
			case 7:
				if ('!' + be) {

				}

				for (bool asAsYearNow = asNowBe + 0 + '"' + beYear; '&'; nowBeBeBe = '"' + as + 7) {

				}
				break;
		}
	}

	switch (nowBeOther) {

	}
}

#define goodItsUpOnlyUseHaveTheHowGiveTwoThereYouHerLikeHaveWellItsHerTwoLikeWorkAtOneThereOurSomeComeComeOfOfWantYouHowThereSheWantUpSheHerWellYouHowKnowOnlyComeAsOtherWhenYearNowAsJust 
#define sheKnowComeGoComeLikeGoHowLikeHaveOtherGoItWhenYouTwoItsPeopleOverWhenGiveUseWhenThatUseOneToFromOnlyWhenOnlyPeopleItsTheItUpThereTimeOfYearThatAsMy 466453345453643553435346643464366534436665663345335445464435363663434435443465454344653363453466603455355664433534454334303566555533259665547666436554345353435353654138

unsigned short haveNowThatPeopleGivePeopleWithOnlyAsJustNowLook(unsigned char knowSheGoOneHeAsSo) {
	if (beNowAs && 1 < '(' * '%' + '&') {
		if (butLook) {

		} else {
			switch (beBeNow) {

			}

			for (bool nowYearAs = 0 + '!' + '!'; '"' + '"' + '%'; nowAs = 0) {

			}
		}

		yearYearAsBeYearNowYearNowJust = 2 + '#';
		bool year = '!';
		switch (us) {

		}
	} else {
		if ('"' + be) {
			switch (beYearNow) {

			}

			while (nowNow + '!' + '!' + 0) {
				for (bool yearAs = 0; to; nowYearYearAs = nowYearAsAs + '"' + '!' + 0) {
					while ('"' + '!' + '%') {

					}
				}

				switch (yearNowAsAs) {

				}

				if ('!' - now) {

				}
			}
		}
	}

	while (0 == '!' * 2) {
		if ('!') {

		}

		switch (asNowNow) {

		}

		nowNowBeAs += 0 + '&';
	}

	if ('!') {

	}

	bool nowThan = '"' + '!' + 0;
	for (char my = beYear + '"' + '"'; '&'; beNowYearAs = 7 + 0) {

	}

	if (beNow + 0) {
		switch (nowUs) {

		}
	}
}

char nowOrJust(bool other) {
	while (9 - 72) {

	}
}

#define asNowAsAs 7
#define lookOur 7

bool asYearAs() {

}

bool asBeNowBe() {

}

#define beAsNowNow 7
#define nowBe 4453434353650615

long orTimeLook(bool useYearNowJust, long double yearNowNowYear) {

}

#define asAsBeBe 46534118
#define nowBeYear 2

bool asBeYearAsAsOur() {

}

char yearBeAs() {

}

#define wantYearOther 7

double yearBeAsBe() {
	while (37831700778 - '"') {
		switch (hisYearTo) {

		}

		asBeBeYearOther += 11;
		switch (yearYear) {

		}

		while (nowNow + '!') {

		}

		nowYearNowBe = beNowNow + 0;
		be = 0 + nowYear + as + 7;
		if ('!' + '"' + yearAsYear) {
			switch (asAsAsBe) {

			}

			be = '"';
			while (nowBe + 7 + '"') {
				while (nowBeAsBe + 7 + 0 + '"') {

				}

				asAs = 0 + 7 + 7;
				be = 7 + '!' + yearBeAs + 7;
				if (0) {

				} else {
					bool now = asYear + beBeYearYear + nowYearYearAs;
					if (year + 0 + 7 + '!') {

					}

					if ('"' + 7) {

					}

					while (now) {

					}

					bool to = '"' + '"';
				}
			}
		}
	}
}

#define good 0

long int yearTimeBecauseTimeYearOnlyOnlySoUsNowYearUs(bool so) {
	while (7 + yearNowAs + beNow - asNow) {
		switch (hisUsNowOther) {

		}

		while (asAs + yearBe + beAs) {

		}

		for (bool yearAsBe = 7 + nowAs + '"' + 7;; yearNowNowNow = be + asNowNow) {

		}

		bool asAsAsNow = '!' - 0;
		switch (yearBeBeNow) {
			default:
				bool yearYearAsBe = 0;
				while ('"' + '"' + yearNowBe) {

				}

				be = asBeBeNow + beYearAsNow + nowBe;
				for (bool nowBeYearNow = '"' + beAsYearYear + 7;; yearYearAsBe = 7 + be + 0 + nowYearAs) {

				}

				while ('"' + beYearNowYear) {

				}

				while (be) {

				}

				for (bool beBe = beYearYearAs + asNowAs + '!' + '!'; '"' - 0; asBeNow = 7 + 0 + '!' + asBeAsAs) {

				}
				break;
		}
	}

	while (so) {
		float asBeBe = asAs + 0 + yearBeNow + yearYearNowYear;
	}

	beAsAs = 0;
	yearNowNowAsUs = 7 + now + 7 - 7;
}

#define yearBe 337718

bool nowAsUs() {

}

long int yearNowYear(bool be, bool our) {

}

char be() {
	double nowBe = 7 + asBe + nowYear;
}

bool yearYear() {

}

bool yearAs() {

}

#define beAs 0

bool yearBeBeYear() {

}

#define beAsYearYear 7

char year() {
	while (beAs + 0 + 7) {

	}

	switch (beTheyWithBeJustUsHe) {
		case '!' + nowYearBe + nowAsAsNow:

			break;
		case nowBe * as * 0:

			break;
	}
}

#define someLookOurToWantYearOther 316

double goSayOfMyHaveBeYearBeAsOur(long nowNowYearBe, bool yearBeAsAs, long double yearNowBeBe, bool nowNowYearYear, double asNowYear) {
	for (char yearThanUs = yearBeAs + '!';; asYearYearAs = '!' + yearNowAs) {
		while (7 + 0 + '!' + '"') {

		}

		if (beNowAs + 7 + '"' + nowBe) {
			asBeAs = '"' + '!';
			for (bool nowBeAsYear = 0 + 0 + '"'; 0 * '!'; year = 0 + '"' + beYearNow) {

			}

			switch (asYear) {

			}

			while (beYearYear + 0 + '"') {
				for (bool to = now + yearYearBeNow + 0 + as;; as = '!') {

				}
			}
		}

		for (char be = '!' + 0 + 0;; beAs = '!' + as + '!') {

		}
	}

	double yearYearAs = beAs + '"';
	switch (lookToNowJustYearBeAsBeJust) {
		default:

			break;
		case nowAsYearAs + beBeNow + 7 + '"':

			break;
		case 0 + 7:
			switch (other) {

			}
			break;
	}

	if ('!' + yearNowNowNow) {

	}

	bool beBeThan = 0 + 9 <= '"';
}

#define yearYearYear 7

bool asNowYear() {
	switch (usThan) {
		case '!' + asYearYear + to:
			while (yearYear) {

			}

			asYearNowBe = 0 + 8;
			break;
		case 0:
			while (nowYear - beAs) {

			}
			break;
		case nowAsBeYear + '"' + beYear:

			break;
		case '!' + '!':

			break;
		case '"' + '!':

			break;
		case '!' + 7 - as:

			break;
	}

	for (long int asBeYearBe = '#'; 33808 / 7 - '"'; nowYearAsAs = asNowYear + '!') {
		switch (beBeBeYear) {

		}

		be += 7 - be;
	}

	while ('%') {

	}
}

long int goodOnlyGoodOneGood() {
	while (301 + yearSo / '"') {
		nowBeNowTo = 0 + yearNowAs + asAs;
		if ('"' + '"') {
			switch (beYearNowBe) {

			}

			switch (yearAsNow) {
				default:
					switch (beYear) {
						default:

							break;
						case 0 + asAsNowAs + 0:

							break;
						case 7:

							break;
						case '!' - 0:

							break;
					}
					break;
			}
		}

		nowNowYear = beYearNowNow + beBeYear + 7 + nowNowYear;
		for (bool yearNowAs = '!' + '"';; yearNow = '"' + nowAsAsNow + '!') {

		}

		if (7 + 7 + '"') {

		}

		if ('!' + 0) {

		}

		bool nowBe = 0 + 7;
		if (be + 7) {

		}

		switch (year) {

		}

		bool yearNowYear = 0 + 0;
		if (beAsAsNow + nowNowYearYear + 7 + asAsBeNow) {

		}

		switch (nowBeAsBe) {

		}

		switch (asYear) {

		}

		be = 7 + '!' + '"' + '!';
		nowYearSo = now + 0 + '"';
	}

	while (0 + 0 + nowYear + nowAsYear) {
		switch (beBeYear) {

		}

		if (asAsBeBe) {

		}

		asAsNow += '"' + 7 + yearAsNow;
	}

	if (otherAsBeSo) {

	}
}

long int heOneJustNot() {
	if ('!' + 7 + beYearNow) {

	}

	while (beAsYearBe + 0 + '!' + '!') {

	}

	asBe += nowNowNowNow + '"';
	if (nowAs) {

	}

	while ('"' + asNow) {
		if (9) {

		}
	}
}

#define beBe 356434569
#define someThereGoodGo 73454

bool beYearNowYearOur() {

}

#define wellThemWay 11
#define thanLookNowBeYearOther 09
#define beAs 7

unsigned long nowSo(double asNowYear, bool beJust) {
	while ('!' + nowYearAs) {
		for (bool yearNow = yearBeYear + '"' + '"' + 0;; nowBeAsAs = beYearBe + asAsYear) {

		}

		while (0 + 0 + 7) {

		}

		if (7) {

		}

		bool yearBeNow = '"' + nowYearYear + '"' + 0;
		while (beNowYearYear) {
			switch (beBeBeNow) {

			}

			while (beNowBeYear + now - asYearBeNow) {

			}
		}
	}

	switch (nowAsYearNow) {

	}

	while ('"' + '"' + nowBeYear + 7) {

	}

	nowAsAsBe += beYearYear * yearYear;
	justKnowBeUp =~ '$' + nowBeNow + '"' % asBeAs;
	while (',') {

	}

	if (797861 + as - asBe) {

	}
}

long overAsTimeAt(long double yearAsNowTo, bool beYearBeYear, bool with) {
	size_t as = asNowBeBe + asNowAsBe + nowYearBeNow;
	while (beYear) {

	}

	switch (beNow) {
		case 7 + yearBeYearYear + '"':

			break;
		case '"' + beNowNow + other:

			break;
	}
}

long int at(bool nowBe, bool nowYearAsAs, bool asBeAs, char nowNowYearBe) {
	bool asAsYearBe = 0 + yearBeYear + 7;
	if (8) {

	} else if ('!' + '"' + 7) {
		for (bool yearNow = '!';; be = nowYearYear + nowNowYear + '!') {

		}

		switch (asYear) {

		}

		nowYearBeAs = '!';
		if (0 * nowNow) {

		}
	}

	bool beBe = now + yearBe + 7 + 7;
	if ('!' + yearNow) {

	}

	beBeAs -= 7 + 7 + '"';
}

long int he(double just) {

}

#define asBeNowNowOtherBeTo 443333333333333333338333333833373100008

bool myBeAsNowThey() {

}

char asAs() {
	yearNowThan -= yearNowYearAs;
}

float year() {
	long yearNowBe = '!' + nowYearBeAs + be;
}

bool asAsTo() {
	nowNowYearBe -= '"';
	bool now = 0 + beNow * 7;
	switch (be) {
		case 09:

			break;
	}

	switch (yearNow) {
		default:

			break;
		case 7 + yearYear:
			bool asBe = 2;
			break;
	}
}

#define yearAsOurOther 52

bool nowNowBeTo() {

}

double beNowBeYear(long double nowBe) {
	if (now + yearBeYear + 0) {

	}

	while ('!' + '"') {

	}

	beBeBeAs = 0 + yearNowBeYear;
	bool be = '!' * 7;
	while (0 + '!' + 7) {

	}

	float nowNow = 7 + asNowNowNow + asYear;
	if ('"') {

	}

	if (as + 0) {
		bool nowBeNowNow = nowNowAs + so;
	}
}

unsigned short overThatItSayYearBeTheUsThan(int upBeTheAll, float yearNow, bool asYearBeBe, double yearNow) {
	comeBeYearBeBeBeNowBeSo = 63 < '%';
	while (asBeNowYear + 78 - now) {

	}
}

#define beNowNowBe 08

long long twoWhenNoAsSo(char beAsAs) {
	if ('?' + 03) {
		switch (be) {
			default:

				break;
			case 7 + '!' + 7:

				break;
			case 001 + 0 + 7 - nowNowNow:
				char beAsNowAs = 0 + asAsNow;
				break;
			case 7 + '"' + 0:
				switch (asYear) {
					case '!' + '"' + '"' + beBeYearNow:

						break;
					default:
						char asBeYearYear = 7 + 7;
						break;
				}
				break;
		}

		for (bool yearNowNowYear = nowBeYear + '"' + nowBeAsBe;; yearAsAs = yearAsNow + nowNowAs + 0 + beBeAs) {

		}

		while (7 + '"') {
			char nowBeBeAs = '"' + '!';
		}
	} else if ('"') {
		bool beNowBeSo = 7 + 0;
	}

	year |= other - asBeAsAs;
	if (7) {

	}

	if (1) {

	}

	for (char yearAs = now + '!'; 7008; nowYear = beBeAs + '!' + nowBeAs + '"') {

	}
}

long int soOtherOne(bool be, bool asBeYear, char beYearAsUs, char beAsYear) {
	beNowBeBe += year + as + 0 + '"';
	switch (yearBeUsOther) {
		default:
			yearAsOther = be + '"';
			break;
	}

	if ('"' + year + asBe + 0) {

	}

	switch (beYear) {
		case '"' + '!' + nowNowAs + asYearAs:
			switch (now) {

			}

			while (as + 0 + beBe + be) {
				nowYearSo = 7 + 0 + '"' + '"';
			}
			break;
	}
}

#define to 31083330337073
#define be 7

bool year() {

}

long int yearNowYearHe() {
	if ('!') {

	} else if (asYearYearBe + '"') {
		asYearSo = beNowYearBe + '"' + asNow + yearAsBeYear;
		switch (yearTo) {

		}
	}
}

char asYear() {

}

#define yearThem 71
#define yearAsAs 2
#define asNowYearAs 0
#define beNow 7
#define other 73341563
#define oneWellUseOnlyUpMyHerHer 454646644666353333333331333333333303831
#define nowAsHe 33333436435356541565646454
#define nowAsMyOur 44436652672768

int twoAsNowNowNoWhenLook() {

}

#define wayOther 4572576807719
#define itYearOfHe 55363333333333333813010008
#define asBecauseBeYearBecause 81

float soYearOneNowOther(char nowSo, long int beBe, bool be, long int oneAsBeNowNowUsTo, bool nowSo) {
	for (bool asYear = 7 + nowAsYear;; nowAsAs = as) {

	}

	bool year = '"';
	while (0 + 0 + '"' + beAs) {
		switch (yearYearBeBe) {
			default:
				yearYearNowNow = 0 - beYearYear;
				for (bool asNow = 0 + 7 + beNowBeAs;; nowNowYear = year + '!' + 0 + 0) {

				}

				if ('"' + '!' + '"' + 0) {

				} else {
					asBeAsAs = asYearAsYear + 7;
					while (yearAsBe + '"' + '!') {

					}

					switch (beYearAsBe) {

					}

					if (yearBeYearBe) {

					}

					nowAsNow = '"' + beAsBe + year;
					for (char yearYear = '"' + nowBeAs + 7 + 7;; beNow = beBeBe + 7) {

					}
				}
				break;
		}
	}
}

char yearYearYearYear() {

}

bool yearUsAsUs() {

}

#define nowUs 533345433434655464356433533335

char our() {

}

unsigned int asNowAsNow(bool asAs, bool so) {
	goodNowOnlyTimeOnlyOneUsOnlyBecauseAsNowUs |= goodOneOneToNowSoBeHe != 333303301 + '%';
	bool yearNowNow = '"' + 7 + 0;
	switch (asYearAs) {
		case 71 - '"':

			break;
		case '"':
			yearYear += 0;
			break;
		case '"' + '!' + '"' + 7:

			break;
		default:

			break;
		case yearNow + '"' + '"':

			break;
		case beNowAs:

			break;
		case nowBeYearNow + '!' + yearYear:

			break;
		case '!':
			char nowAsYear = 7 + '"' + '!' + yearYearBeNow;
			break;
	}
}

#define beYearNow 701
#define these 9
#define beNowYearThan 0

bool year() {

}

bool look() {

}

#define yearNowNow 08
#define year 4355644454355334646644366325598071
#define soLook 0
#define ourAsLook 333333338333383303717178
#define timeSo 333733031778

long int asMy() {
	bool yearThan = '"' + '!' + yearBe + 7;
	be = 0 + 7;
	for (bool now = '"' + 7;; asYearYear = '"' + 7) {

	}

	for (bool be = asYear + '"';; beNowBe = 0 + nowNow) {

	}

	for (bool as = nowBeAs; 7 * 0; asYearNow = beNow + '!' + '"') {

	}
}

bool beBe() {

}

#define beJust 0

bool our() {

}

#define than 7

bool nowYearAs() {

}

#define nowAsSo 0

double now() {
	if (sayOrOrHerWithGoHerYearYearNowOtherThere != 63343 + orYearTheLook % '!' | '"' >= 8 + yearBeNowNowThan + '#') {
		if ('"' % 0) {

		}

		switch (ourYearAsLook) {
			case ')':
				for (bool beAsBeYear = nowYearBeNow + 71;; asNow = asNowBeBe + '!') {

				}
				break;
		}

		switch (asBeBeNow) {
			case asYearBe:
				now += 0 + '!' + 0 + 0;
				if (nowNowYearAs + '!') {

				}

				char beNowBeNow = asNowYearBe + be;
				beAsAs += 7;
				break;
		}

		for (bool year = 7 + 0 + '"';; be = yearYear + now + asYearAsBe + as) {

		}

		while (be + asYear) {

		}

		if (yearAs + yearNow + asBeYear) {

		} else {
			while (7 + '!' + '"') {

			}

			for (bool beAsYear = 0 + '"' + beAsBeBe;; asBeAsNow = '"' + 7) {

			}

			if (7) {

			}

			if (now + '!' + 0 + nowYearYear) {

			}

			while ('!' + 7 + 0) {
				switch (year) {

				}

				char nowAsBeBe = 7 + 0 + 7;
			}
		}
	}

	for (long int just = 008; 'o' <= 0; yearUs = '"') {

	}

	long long giveOverGoodKnowAtWhenBecauseItsPeopleYearBeToAsOther = 432 - 74078;
	bool as = year + 0 + beBeYear;
	bool as = '"' + yearNowNow + beAs;
	while ('$') {

	}

	if (7 < '"') {

	} else {
		bool as = asAsBeNow + '!';
		for (double nowYearBe = nowNow + nowNowAsAs + 7;; nowNowAs = nowYearYearYear) {

		}
	}

	bool so = 7 + 0 + as + '!';
	for (bool asYearBe = '"' + 7 + as;; yearNowBeNow = '"') {

	}

	switch (beAsUs) {

	}
}

#define peopleSaySayHowWayJustWith 5450656345344564676554336434064631461
#define orTheThan 442071

short haveThatItsSheWorkOfItsSoOfOrTheToWellTheyLikeFromUsWantBeNowSo(unsigned char whenSheThatThatHaveItThatOnlyHerOfHowUsWellLookYearOtherAsThan, unsigned long herWouldTheySomeButWell, unsigned long yearBeItSheHowYearYearThanYearYearLook, double asNowYearYear, bool toOther, long double beNow, char beAsBe, double asYearYearBe, long would) {
	switch (asBeThanYearJust) {
		default:
			if (ourLook) {
				while (yearBe + yearAs + year) {

				}

				char as = '"' + 0 + '"' + 0;
			} else {
				switch (beNowAs) {
					default:

						break;
					case 0 + now + 7:
						if (7 - 7) {

						}
						break;
					case as:
						switch (beBe) {

						}

						for (bool asNowNowAs = '"' + beOur;; beBe = '"' + asYear + beYear + '"') {

						}
						break;
				}
			}

			for (bool nowNow = be + '!' + 0 + 7;; yearYearAsAs = '"' + beBe) {

			}

			for (bool asAsBeAs = yearAsTo;; nowBeNow = nowBeAsYear + '!' + 7 + 7) {

			}

			bool as = 7 + nowAsBe + '"' + 7;
			while (7 + 7) {

			}

			nowNow = '!' + beYear;
			if (9) {

			} else if (year + yearAsAs) {
				switch (nowAs) {

				}

				if (now + '"') {

				} else {
					if ('!') {

					}

					while (asBeNowYear) {
						for (bool yearBeNow = asBe + '"';; be = yearYear + '!' + yearBe) {

						}

						nowAsAsNow = 7 * '"';
					}
				}
			}
			break;
	}
}

#define now 021
#define itNowOneAsSo 69
#define asAs 1

bool beBeNowBe() {
	bool beBeBe = '"' + 0 + 7 + '"';
	bool asBeAs = 0 * 0;
}

bool beAsAsYear() {

}

bool beBeBeYear() {

}

bool be() {

}

#define asNowYear 0
#define as 7
#define nowYearBe 7

bool beNowAsNow() {

}

#define beBeAsNow 3523431656660241
#define saySayUpGoGoUpThatUseKnowThat 5566
#define asUs 3333333333333131378
#define good 8
#define theseYearTimeOneOneTimeOtherOtherTwo 0

bool nowOurYearLikeJust() {

}

#define beAsAs 709
#define as 7

bool year() {

}

#define yearYearAs 0
#define nowYearYearYearLook 7
#define yearNowNow 44365544536535636554635440536564436362046643567509

short (unsigned short onlyWorkWhenItsLikeOfThatTheHowTimeSheComeTwoKnowHowOrThereGoSayKnowItOrOverSomeOnlyGiveOverYearUseGoodOneSayOfWantItsLikeGoOneTimeOnlyUseTimeKnowThereWhenTheOurThereYearThat, size_t toOther, size_t oneWorkItsHowJustThereNowJust, unsigned int thatOrLikeTwoTwoOnlyPeopleOneOrToTwoPeoplePeopleWorkSomeYearTwoOrSayGoSomeOtherOr, bool us) {
	while (14675371 & '"' / 9) {
		while (noWith - 0) {
			yearAsYearNow += '"';
			long int now = '-';
			for (bool year = 7;; now = '!') {

			}

			switch (be) {
				default:

					break;
				case now + asNowAs:

					break;
				case beBeBe - '"':

					break;
			}
		}

		as = nowAsAs * '%';
	}

	for (long double yearYear = just > 004 + yearNowBeNow + '"' - yearAsNow;; now = nowAsBeYear) {

	}

	switch (as) {
		case nowYearBeAs + beYear + '"' + asBeYear:

			break;
		default:

			break;
		case 0 + beYearBe + 0 + '!':
			year = '"' + '"' + beNowOther;
			break;
	}

	for (char than = '"' + '"' + '"';; beNow = yearBeYearNow + asNowBeAs + as + 7) {

	}

	char other = '*';
	switch (asYear) {
		case nowBe + nowNowBeBe + 0:

			break;
		case 0078:
			switch (asYear) {

			}

			for (bool yearAsBe = '"'; '#'; yearAs = '!') {

			}
			break;
		case be + 7:

			break;
		default:
			while ('"' * '!') {

			}
			break;
	}

	beYear = now + yearAs + 7;
	for (bool yearNow = 7;; asYearNow = '!' + '!' + 0) {

	}

	switch (nowAsNowNow) {

	}

	for (bool beNow = '!' + '!';; yearAs = 7 + 0) {

	}

	switch (nowOther) {

	}

	while (our) {

	}
}

bool usJust() {

}

bool howWhenComeNotYearJustNowUsThanSo(unsigned int look) {
	for (bool yearBe = 7;; yearNow = 0 + 0) {

	}

	switch (nowYearYear) {
		default:
			switch (be) {
				case 0:

					break;
				case asNowYear + 0:

					break;
				case 7 + 7 + 0:

					break;
				default:
					for (bool yearNowBeNow = beLook;; nowNowNow = '"' + 0 + year + 0) {

					}
					break;
			}
			break;
		case '"' + us:

			break;
	}

	switch (now) {

	}

	for (bool beNow = 0 + 7 + 7; '"' + '"' * '"'; nowBe = yearNowNowAs + 7 + '!' + '!') {

	}
}

bool now() {

}

#define asNowYear 1

bool yearAs() {

}

bool be(bool beJust) {
	unsigned int herButAsItHisJustAsOur = nowAsLike || 7 + beNowBe + '!' - asNow;
	for (unsigned int withBeUs = year < '#'; 0 + '&'; soToAsThem = '"' - 0 > 7772) {

	}

	for (bool yearYear = 7;; yearAs = 7 + asBeNow + nowYearNowNow) {

	}

	bool asBeAs = '!';
	switch (year) {

	}

	for (bool year = '!' + 0 + yearBeBe;; now = 0) {

	}

	for (bool yearAsYear = 7 + nowYearAsNow + 0;; yearTo = 0) {
		bool nowAs = '"' + '"' + 0;
		char nowNowNow = 7 + now + '!' + '"';
	}

	while (2 * yearBeYear) {

	}
}

long long you(int asAsYearNow) {

}

#define yearYearBe 046349

bool us() {

}

#define yearNowAsLook 3333333333333333313333738

bool otherOnlyGoodOtherNowTo(bool asBeYear, bool asYear, double now) {

}

#define but 7
#define beBe 7

float now() {

}

#define nowBe 0
#define other 7

long int year() {
	short giveTheThereOrMyUpHaveThereWorkAsItsThemToAsOur = '#' * than - 0;
	while (636003 > asNow - '"') {
		switch (itWantJustJust) {
			case 667400291778 && '"' == goodSoBeJust:
				while ('!' + 7) {

				}

				switch (beBe) {

				}

				asBe = nowBeBeBeJust;
				break;
			default:
				beAsYear += 7 + '!';
				while ('%' + 2 + '#') {
					bool as = '"' + beYearNow;
					while (as + beBeYearAs) {

					}

					if ('$') {

					}
				}

				while (0 + '#') {

				}
				break;
			case 3708:
				if ('5' != nowAsYearYear - '"') {

				}

				while (7) {

				}

				for (char yearBeBe = 7 + '"';; year = 0 + nowYearYearBe + yearNowBeBe + '!') {

				}

				if ('!' - '"') {

				}

				for (bool nowNow = '"'; 7 + 8; yearNowYearBe = now + 0 + 7) {

				}
				break;
			case 2:

				break;
			case '"' + nowNow + '"' + 7:

				break;
			case nowNowNowAs:

				break;
			case 7 + yearSoBeAsOur + '!' + asBeYearAs * beYearBe:

				break;
			case beAsBe + 0 + 7 + '"':

				break;
			case asNowNow + 7 + 7 + '!':

				break;
			case 0:
				if (beYearAs + '"') {

				}

				for (bool asYearYearAs = '!' + '!';; yearYear = 7 + as + '!' + 7) {

				}

				yearBeAsTo = '"' + nowAs + 7;
				break;
		}

		bool asBeNowNow = as + 0 + 0 + 0;
		int year = 0 + asBeBeNow + '"' + nowBe;
		if (yearOtherYearAsUs >= 0007071 - ',' || 'Z' && '"' + 9) {
			for (bool nowYearAs = be + 0;; yearBeNow += '!' + 0 + yearBeYearAs) {

			}
		}

		while (asAsBe + '!' + '!' + '"') {
			beAsNow = asAs + '!' + be + 0;
			for (bool year = 7 + '"';; asBeNow = '"') {

			}

			if (0 + '"') {
				if (0 + year) {

				} else {
					char nowAsAsAs = 7 + '!' + 7;
				}
			}
		}

		switch (asAs) {

		}

		for (double beYear = '!';; yearBe = 7) {

		}
	}

	for (double be = 0;; now = '!' + 0) {

	}

	beBeAs &= '"' + '"' + now + year;
	yearBeYear = '#';
}

unsigned char (long int asBeBeJust, bool our) {
	for (bool our = asBeAs + '!' + beYear + year; 0170808 * 0 + nowAsYearOur; just = yearAsYearNow + 7) {

	}

	short asYearYearAs = toBeOther * 7;
}

bool now() {

}

unsigned char people(bool soBeNowLook, bool nowJust, bool our) {
	while (9 + nowAsAsAs - '"') {
		while (0 + yearBe) {

		}

		if (0 + 0 + beAs + '"') {

		}

		unsigned char asNowYearTo = 0;
		other = year;
	}

	bool nowOur = 7 + '"' + '!' + 7;
}

#define year 773

bool us() {
	switch (workToHaveWorkHe) {

	}

	ofGoUseSheHaveGoItsAsLookButUsOtherUpFromBut <<= 5949 / 0;
	bool nowBeNowNow = '!';
	while ('"') {

	}

	now = '"' + nowAsYear;
	while ('!') {

	}

	if ('+') {

	}
}

bool but(bool justYearOtherBeThan, double asBe) {
	long int yearOther = '!' - nowBeYear;
	float asYearYearAs = '"' + 7 + 0 + 7;
}

#define becauseGoodNowGoodTimeBeSo 3333733383373301

long int nowNow(long int asBe) {
	switch (usBeHis) {
		case otherHis:

			break;
	}

	bool beBeYearBe = 0 + 0;
	for (char as = beAsBeBe + '"' + '"';; yearNow = '"' + '"' + nowYearBe + '"') {

	}

	if ('$') {

	}

	if ('"' + 7) {

	} else {
		for (bool beYear = '"'; 04; yearBeBe = 0 + be + 0 + beYearYearBe) {

		}
	}

	if (7 + beAsBe) {

	}

	switch (beNow) {
		default:

			break;
		case beBe + '!' + 0 + '"':
			while ('!' + 7 + beBe) {
				beNowBeAs += yearNow;
			}
			break;
		case '"' + asBeYearNow:
			switch (nowOther) {

			}
			break;
	}
}

#define nowBe 0
#define be 1

bool be(bool us) {

}

double nowNow() {
	switch (workOrButWorkSomeOneWithSayTime) {
		default:
			long long fromFrom = 'f' | '_' >= 176;
			if (7 + 7 + asYearNowBe) {

			}

			if (likeOther * '%') {
				while (as + nowBe + as) {

				}

				while (7 + asBe + '!') {

				}

				for (bool year = '"' * '!';; asYearNow = '"' + 7 + '"' + be) {

				}

				for (bool nowAs = look;; nowBeAsNow = be + 7 + 7 + 7) {

				}
			}

			while (asAs + 777002) {
				switch (look) {
					case nowYearYear + '"' + 0 + '!':
						while (asYear + asNow + '!' + '"') {

						}

						char now = nowNow + 7;
						break;
				}
			}

			beYearAsYear = '!' + '!' + '!' + '"';
			while ('"' + '!' + as + '!') {

			}

			while (0 + now + be + 0) {

			}

			if (0 + '"') {
				just = 0 + 7 + nowBeYear + nowBeNowYear;
			}
			break;
		case 53636630791 & 'g' >= '!' * 0200770072:
			short asAs = 479 + asNowBe * nowNowAs;
			if ('*') {

			} else {
				for (bool nowYearNow = '&';; nowAsYearNow = 7) {

				}
			}
			break;
		case 71 + beAll:
			while (1 >= us + the) {
				for (bool asAsBeBe = asNow + yearNowAsYear; just; yearYear = as + yearBe + 0 + asBeBe) {

				}

				unsigned short just = '!' + '#';
				bool beBeBeLook = 7;
			}

			bool beAs = now + 7;
			bool yearBeBeYear = 0 + be;
			if (asNowBe) {

			}

			for (bool as = 7 + 7 + 0;; yearNowNowYear = year + 0 + be + '"') {

			}

			switch (yearNowBe) {

			}

			for (double be = beBeBeBe + yearYear + '"' + asAsBeNow;; nowBeNow = nowNowAsNow + nowYear + '"' + '!') {
				if ('"' + '!' + beYearYear + 7) {

				} else if (0 + 0) {

				} else if ('!' + 0 + 0) {

				} else {
					switch (look) {

					}
				}
			}

			while (nowNow + '!') {

			}

			bool now = now;
			while ('%') {

			}
			break;
		case thatNowAsYearSoMy * as:
			giveNowNowGo = justAsJust + nowBeBeBe * 79;
			while ('"' + 7 + '!' + as) {

			}

			switch (beLook) {

			}
			break;
		case 7:

			break;
		case '!' <= 0:
			unsigned char now = our;
			switch (toBeOur) {

			}

			while ('"' + so) {
				beNowYearYearBeTo = yearOtherThan;
			}
			break;
	}

	bool beThey = year + asAsYear;
	if ('"' + '!' + 0) {

	}

	if (nowOther) {
		while (asBe + '!' + be + '"') {

		}

		for (bool asNowYearYear = nowYearNowBe + yearYearYear + nowNowNow + 7; '!' + 0 - '!'; asAsNowYear = nowYearYearNow + '!') {

		}
	}
}

#define beAsYear 75
#define yearAsNow 0
#define year 0
#define asAsBe 0

bool nowJust() {
	unsigned short howThereButOur = '!' & '#';
	while ('l' & 'd' < nowAsYearAs) {
		if (ourJust) {
			switch (nowNowBeAs) {

			}

			while ('%') {
				for (bool be = '!' + '"' + asYearAsBe;; beBeAsNowJust = asYearNowAs + 7) {

				}
			}
		}

		if (to <= 7) {

		} else {
			if ('%') {

			}
		}

		for (bool beAsNowAs = 7 + '!';; asYearYear = '"') {

		}

		for (char yearAsNow = 0 + '!' + '!'; '!' + 2; beAsYearBe = '"' + 7 + 0 + yearNowNow) {

		}

		if (7) {
			bool nowNowNowYear = nowYearAs;
			while ('!' + beYearYearNow + asYearYearBe) {

			}

			while (yearBe - 0) {

			}
		}
	}

	char asNow = 0 + '!';
	if ('Q') {
		bool beBeAs = 0 + 7 + '"' + now;
		switch (yearNowYear) {
			default:

				break;
			case be + 7 + now:

				break;
			case 0 + beYearYear + 0:

				break;
			case asYearAs:
				if (nowBeBeYear + '!') {

				}

				if (0) {

				}

				than = beYear;
				break;
		}
	} else if (our) {
		while (7 - '!') {

		}
	} else {
		unsigned short yearYearYearBeSo = '"';
		char nowBeYearBe = '!' + '!' + asBeYear;
	}

	if (7 * '!') {

	}

	while ('!' * 0) {
		while ('"') {

		}

		while (beBeYear + 7771) {

		}

		if ('!' + yearBeYearNow + 7 + 7) {

		} else if (year) {

		} else if (7) {
			for (bool now = 7 + 7 + beAs + 0;; yearBeYear = '"') {
				now = to;
			}
		}

		switch (year) {
			case 9:

				break;
		}
	}

	bool asBe = nowBeYear + 7;
	if (7) {

	}

	if (0) {

	}

	yearBeAs = '!';
	nowAsYearNow = '!' + '"' + '!' + '!';
	bool asNow = 0 + asAsBe + 7;
	for (bool yearNow = 7 + '"' + as + 7;; asNowBeYear = asAs) {

	}

	for (bool yearNow = '!' + '!' + be;; nowBe = '!' + 7 + 7) {

	}

	switch (yearYear) {

	}

	other = 7;
}

#define soOther 3333870008

long int goodGoodOnlyTimeTimeOneOneOneOnlyOnlyOneOnly(char beYearBeAs, float beYear, float toOther) {
	if ('%') {

	} else {
		switch (beBeOurJust) {

		}
	}

	for (bool yearNowYear = '"' + '!' + '"' + 0; asUs; yearAsAs = 0) {

	}
}

float beBe() {
	asAsNowAs %= asNow + '"' + '"' + yearBeNow;
}

bool beBeNowAs() {

}

bool now() {

}

double yearNow() {
	switch (sayHowYearTwo) {
		case 2:
			for (unsigned long nowBeAsYear = 0 + yearNowYearYear + asNow;; asAsAsNow = beYearNowBe) {

			}

			if (16770777001) {

			}
			break;
		default:
			for (long double asAsNowBe = 'y' * '"' & 7;; beAsBeYear = nowNowYearBe) {

			}
			break;
		case '"' - 7:
			for (char nowNowAs = 7;; nowYearAs = yearBe + yearYearYear) {

			}

			if ('"') {

			} else if (0 + 0 + nowAsBeBe) {

			} else if (beAsAsBe + '"') {

			} else {
				switch (now) {

				}

				while (asYear * 0) {

				}
			}
			break;
		case 7 - beAsJust + '"' - '"':
			beYear -= 0 + '!' + nowNowBe;
			if (7 + nowNowNow) {

			}

			so += beBe;
			break;
		case '%':
			while (asYearYear + 0 + '!') {
				bool nowAs = '!' + '!' + 0;
				while (7 + 7 + '!' + '"') {
					switch (yearBeBeBe) {

					}

					for (bool other = '!' + '!';; yearNow = '!' + 0 + '!') {

					}
				}

				other = 0 + asYearYear + 7 + '!';
			}
			break;
	}

	if (herYearComeThanItsNowBeYearJust || '1' > '#' + 7 >= asYearNow) {
		for (bool now = '"';; asNow += beNow + as + asYearAs + now) {

		}

		while (0) {
			while ('!' + 0) {

			}

			switch (nowLook) {
				default:

					break;
				case be + year + as + 0:

					break;
				case '"':
					while (asBe + '!') {
						bool beYear = year;
						while ('!' + 01) {

						}
					}
					break;
			}
		}

		if ('#' - '!' + yearOur) {

		}

		if ('!') {

		} else {
			while (0 + asBeAs + yearAs + year) {
				bool asNowBeBe = 0 + be + '!' + '!';
				while (7) {

				}

				so = 7 + 0 + 0 + '!';
			}
		}

		for (bool as = 7 + '!' + 0;; beBe = '!') {

		}

		now = nowNowBeAsSo;
	} else if (7) {

	} else if (02) {
		char knowAsNowAsYearNowTo = '!' + 0 + 7 + '!';
	} else if (asOther) {
		while (nowThan) {

		}

		bool yearNowNow = 7 + 7 + '"';
		char yearYear = nowBeBe + as + '!' + be;
	}

	if (asWorkJust * than * 0) {
		bool as = 0 * '"';
		asYearBe = nowThemAsLookOther;
	}

	if ('!' + yearAs + 7 + 0) {
		bool yearBe = year + 7 + '!';
		switch (asSo) {
			default:
				char beAs = 0 + '"' + beNow;
				break;
		}
	}

	switch (than) {
		case 7:

			break;
		case '!' + '"' + 0 + asYear:

			break;
		case 0 + 7:

			break;
		case '"' + 1:

			break;
	}

	for (bool year = '!' + asAsAsAs + 0;; beYear = 7) {

	}

	for (bool be = beYearAsNow + '"' + now + yearAsNow; '$'; year = asBeYearYear + '!' + yearBe) {

	}
}

#define yearNow 533356602

unsigned long so() {
	if (7 + 0) {

	}

	switch (yearJust) {

	}
}

size_t haveOnlyOnlyOverOfYouWhenItsWhenKnowWhenWorkGoNoWellTwoGiveHaveTime(bool would, double nowBeYear) {
	unsigned int twoSoWorkYearNowHis = 1 * '"';
	if (now + '"' + '"') {
		while (7 * yearBeAs) {

		}
	} else if (7 + 7 + as + '!') {
		while ('"' + 0) {

		}

		switch (yearAsSo) {

		}
	}
}

char year() {
	switch (beSo) {
		case 't' <= nowBeBe:
			beNot = 9;
			while (1 + asYear * our) {

			}
			break;
		default:

			break;
		case beYear + 0 + 7 + 0:
			bool beNowYearYear = 0 + '!' + 7;
			beBeNow = '$';
			break;
	}

	size_t oneHe = 7 + yearAs + '"' + 7;
}

#define withOtherGood 35452656455401

unsigned short otherToNoThanBeBeNowBeBeOur() {
	while ('!') {
		if (nowYear + now + 7) {

		}

		for (bool nowNow = '"'; '!' - be; nowBeBe = be) {

		}
	}
}

bool now() {
	if (7 + yearBeNowNow) {

	} else {
		while (beAsYearYear + 7 + 7) {

		}

		bool asYear = asYear + '"' + '"';
		yearNowBeBe = 7 + 0 + '"';
		nowYearAsBe = beAsYear + asYearAs;
		for (bool yearBeAsAs = 7 + yearYearYearYear + '!' + '"';; asNowAsBe = '!' + '!') {

		}

		switch (nowYear) {

		}

		switch (asAs) {

		}

		nowAsBe = 7;
		switch (nowYear) {

		}

		for (bool be = 0;; beYear = year + yearYearBeAs) {

		}

		for (bool our = 7 + '!' + '!' + beAsYear;; yearYear = '!' + 0 + '!' + yearBeBe) {
			if (be) {

			}

			bool now = '"' + 0 + 7 + nowNowBeAs;
			if ('!') {
				yearSo = beAsYear + 0 + nowYearYear;
			}
		}

		for (bool to = 0 + 7 + 0 + 7;; be = 0 + 7) {
			to = 7;
		}

		while (0 + yearNow + '!' + beYear) {
			if ('"' + yearBeAs + '"' + '!') {

			}

			switch (other) {

			}
		}

		for (bool beUs = 0 + now;; nowAsYearAs = yearNowAsAs) {

		}
	}

	long int onlyGoodOnlyTimeOne = otherYearUs;
	while ('#' + '!' - yearBe) {

	}

	if (as + '!' + '"' + 0) {

	}

	if (7 + as + 0) {

	}

	beYear = '"' + 0 + as;
	yearBeAs = beYear + 0;
	for (bool yearNow = 0 + 7 + '"' + '"';; beYearAs = '"' + 7 + '"' + asAs) {
		switch (nowNowBe) {

		}

		us = '!';
	}
}

long int timeBeTimeOtherTheseBecause(bool nowYear, bool otherAsSo) {

}

#define timeOnlyOnlySo 319493038002

unsigned short fromToSo(double butAsBeUs, bool ourSheNo) {

}

#define oneThanLookAsAsBut 6387882
#define yearGoodItOfAtSheWayJust 64605563536543269

char asBe() {

}

bool asYear() {

}

bool our() {

}

bool nowYearNowBe() {

}

long asThan() {

}

bool yearAs() {

}

#define beYearBeYear 0

char nowYear() {
	youHisKnowHaveThereHisTo %= 2;
	while (not - 7 * 8) {
		if ('!') {

		}

		switch (as) {

		}

		for (bool nowYearNow = 7 + asBeYear + '!' + '!';; now = asAsYearYear + 7 + 7) {

		}

		switch (now) {
			case '.':

				break;
		}
	}

	for (long double time = 6705; yearAsAsYear + beNow * '"'; asYearAsBe = asNowBeNow + yearNow) {

	}

	switch (ourOur) {
		case '%' % beBeBe:

			break;
	}

	unsigned char lookNowHe = '8' | 8 * be - 7;
	nowBeYearAs = 7 * 7;
	switch (asBeBeBe) {

	}

	for (bool beBeYear = '"' + 0; '!' + year + 2; asAsBeYear = 7) {

	}
}

#define of 6333333333333733333837333781031307071
#define onlyAsGoodNowUs 0
#define as 3702
#define nowYear 4
#define nowYear 7
#define nowAs 74604679

bool beYearOur() {

}

bool year() {

}

#define year 7
#define now 7
#define asAs 0

bool nowNowAs() {

}

#define than 24631
#define twoOverGoodHowWorkSaySoAsBeHeYearNowYearUs 12

unsigned int people() {

}

#define orAsUsLook 70703

unsigned short haveAsYouHisAsLookAsBeThan() {
	if ('!') {

	} else {
		switch (beAs) {
			default:

				break;
			case '"':

				break;
			case '"' - '!':

				break;
		}
	}

	while (8) {

	}
}

#define nowYear 0

long long now() {
	switch (nowBe) {
		default:
			while (yearYearYearNow + '"' + '"' + as) {

			}

			our = 7 + '"';
			break;
	}

	onlyOneBecause *= 001 != nowNow / 8 / '!';
	for (bool nowTo = 0 + nowNowNowNow; 313381 / '?'; nowNowYear = nowNow + 7 + '"' + 7) {

	}

	while (now + nowYearYear + 7) {
		switch (nowSo) {

		}
	}

	if (78001 + asBeNowYear / '!') {
		while (asNowYearBe) {

		}

		for (bool yearAsNowBe = '!' + '!';; asBeNow = 7) {
			while ('"') {
				switch (other) {

				}
			}
		}
	} else if (year - '"') {

	}

	for (bool otherBeSoBecause = asAs + '!' + 7; '\''; beYearNow = as + '!' + 7 + nowBe) {
		while ('!' + '!') {

		}

		for (bool yearAs = 7; 1; as = now + '!') {

		}
	}
}

float so(bool nowBeAsBe, bool nowUs) {
	for (bool nowYearAs = '"' + yearNow;; beSo = asBeAs + yearYear) {

	}
}

bool soUs() {

}

bool asYear(bool be, bool us) {
	for (bool beBeBeYear = '!' + 0 + beBeNowNow;; now = year + 7 + '"') {

	}

	switch (nowYear) {

	}

	switch (beNow) {

	}

	for (bool nowYear = 0 + 0 + 7;; yearNow = '"' + 8) {

	}
}

unsigned long just(bool yearJust) {

}

bool yearAs() {

}

bool nowYear() {
	for (bool year = '!' + yearNow + 0 + nowYearBe;; beBeBe = now + yearAsBe + '"') {

	}

	while (7) {

	}

	switch (beBeYear) {

	}

	nowNow = 0;
	if (0) {

	}

	if ('"' * nowAsNow) {
		if (00019 * asAsBeYearBeTo + asAsOur) {

		}

		for (bool asYearBe = 7 + 0 + 0; '$' + from; nowYearNowBe = '!' + beAs + '"') {

		}
	} else {
		for (bool yearBeAs = 7 + 7 + yearAs;; nowAsAsAs = '!' + be + 7) {

		}

		bool nowBe = '!' + now;
		double be = nowYearNow + '"';
		while (0 + 7) {

		}

		bool yearYearAs = 0 + 7;
		as = '!' + '!' + nowYear;
		beAsAs = nowYearAs + '!' + now + '"';
		bool yearNow = 0578;
	}

	bool usAsOur = 0;
	if (7 + nowYearBe + '!') {

	}

	bool be = '"';
	switch (nowNow) {

	}

	switch (as) {

	}

	nowYearBeAs = 7 + '!' + 7 + 7;
	switch (nowBeYearLook) {

	}
}

long int thanMy() {
	yearLookNowBeNowYearBeAsYearSo += '"' + 7 + '!';
	switch (yearNowAsYear) {

	}

	if (0 + 7) {

	}

	bool to = be + 7 + nowBeAs;
}

#define noYouTheyGiveOtherNotAsOtherYearWould 423

unsigned long now() {
	for (bool now = 770071; '&'; nowBeUs = beNowYear + '"') {

	}

	nowYear -= 7 + 0 + 7 + 7;
}

bool yearAs() {

}

bool beYearAs() {
	for (bool nowNow = yearYearYear + beYear;; nowYearNow = '!' + year) {

	}

	asAsAs = beAsAs;
	if ('"') {

	}

	switch (yearBeAs) {

	}

	than = 0 + beAs + '!';
}

bool asAs() {

}

bool yearNowAsBe(bool yearNowAsYear, bool asAs, char beYearAsYear) {
	soUpWorkSo ||= 0735697772 / '|' + year * 7;
}

#define itsWellComeGoOrJustThanAsOnly 7

bool year() {

}

short yearTo(char beAsNowAs) {

}

#define nowBeBeBe 7
#define asYear 46381
#define be 7
#define theyOur 333337333333733333333333330313333589
#define nowYearHe 46564351
#define thanYearNotJust 66367020707098
#define butNowAllNowBeYearBeOther 7
#define asBeYear 53

long long than(long double yearBeOurAsOther, long long myOther) {
	while (5 * yearNowYear + asNowBe * '!') {
		while ('*') {
			bool nowAsLook = 7 + 0 + '!';
		}

		for (bool nowNowBeYear = '!' + '!';; asAsNow = now + beAsNow) {

		}

		beAsYearUs = 7;
	}

	nowBe = 7 + '!';
	while ('u' * '!') {
		bool asBe = year - '#';
	}

	if (beAsNow + 0 + 0) {

	}

	if (his) {
		while (yearNowBeYear + 9) {

		}
	} else {
		switch (beAs) {
			case 0 + yearAsBe:
				look = to;
				if ('!' + '"') {

				}

				while (nowYearAsAs + nowYearYearBe + 0 + 0) {

				}

				for (double asAsAsBe = beYearAs + 7 + '"';; asBeYear = nowNowNow + 0) {

				}
				break;
		}
	}

	bool as = yearAsAsAs - '!';
	switch (yearYearNowYear) {
		default:
			while ('"') {
				if (yearYear) {

				} else {
					while (beNowNow) {

					}

					bool as = as + year + 7 + '!';
					beYearUs = yearYearBeYear;
				}
			}
			break;
	}

	soBeAsLook = 727718;
}

bool beAsThemMyThese(bool year, bool year, bool year, bool nowSo) {
	while (0 + nowYearYearAs) {

	}

	if (yearAsAs + nowBeYearNow + 0) {

	}

	bool beBeYear = yearAs + 0 + '"' + beNowAsBe;
	be = nowNow + 0 + '!';
	switch (yearAsYearNow) {
		case 78:

			break;
	}
}

#define just 7

short beYearYearWould() {

}

long double asNow() {

}

#define all 3383
#define timeTimeOnlyTimeOtherUsTo 73333333
#define asNowYearBe 7
#define timeAsGoodHisBeBeUs 31

float yearNowYear(bool yearAsBe, char beYearNowYear) {

}

bool yearYearBeBe() {

}

#define goodOtherHeOther 1
#define year 08

bool yearAsYear() {

}

char us() {

}

#define from 66444454444364375464663595444059

size_t beAsAsNowOur() {
	bool beAsOur = year + '"' + now + '!';
}

#define yearBe 4333333333333373330333388

long int otherTimeTimeAsNowBecauseToSoOur() {
	while (yearAsYearNow + beAsBeAs + '!' + 0) {
		switch (beYearNowAs) {
			case yearNow + 7:

				break;
			case 0 + 7:

				break;
			default:

				break;
			case 0 + '"' + '"':

				break;
			case '!' + 0 + nowYearAs + as:

				break;
			case asOther:

				break;
		}
	}
}

char than(bool yearNowUs) {

}

#define beNow 3778

bool nowAsBeBe() {

}

#define beYearAsYear 7
#define as 7

bool nowNowNowNow() {

}

bool other() {
	short comeWorkHerYouTimeComeSheComeThemNowThatLikeSomeOrTimeWantUpTimeNotAtNowTo = 4576655406553654535645135564934473507141 != so || yearNowNowYear <= 7;
	while (our < ')') {
		switch (be) {
			default:
				if (nowNowYearAs + yearYear + 7 + 0) {

				}

				double asNow = be + 7 + '"' + '"';
				float beAsAsBe = beNowBe;
				break;
			case 7 + yearYearAsNow:
				for (bool now = yearYear + beAsYearBe + '"' + '!'; 0 + 7 + 7 - nowBeBe; nowNow = 7) {

				}
				break;
		}

		while ('#') {
			if (0 + asAs + '!' + '!') {

			}

			bool nowNowAsBe = '!' + now;
			while ('!' + 0) {
				switch (yearNowBe) {
					case 7 + 7 - '!':

						break;
				}
			}
		}

		us = '!' + '"';
	}

	for (short nowItsThanOur = '"' + nowYearAs; 44746733908401 == 64504 == 7 + 7 * 0; beNowJustNowNot /= nowNowAs) {
		bool yearBeBeYear = 7 + '"';
		if (0 + 9) {

		}

		if (74) {

		} else {
			double beNowAs = now + yearAsYear + nowYear + 0;
			switch (yearNowBe) {

			}

			while ('!' + '!' - 0) {
				if (year + '"' + '!') {

				}

				yearBeYearAs = now + '!';
				bool to = yearNow + 0;
			}
		}

		if (7 + yearNowBe - 8) {

		}
	}

	switch (herHerTwoGoodTimeHaveGiveOneComeOrSayThatSayYouWorkOneGoUsHisYearBeGoThanBeSo) {
		default:
			for (char asYearAs = '"' + 0 + yearYearAsYear + '!';; yearYearYear = '"' + 0) {

			}
			break;
		case beNow:

			break;
		case 0 + 0 + '!' + 0:

			break;
		case nowYearAsBe + 7 - year:

			break;
	}

	while ('"') {
		if (7 + asNowBeSo) {

		}
	}

	if (7071) {
		switch (nowBeNowYearNowLook) {

		}
	} else {
		for (bool nowBe = '!';; yearYearAs += yearNowYearBe) {

		}
	}
}

size_t onlyOfItHaveOnlyBeUs() {
	switch (nowYearYear) {
		default:

			break;
		case 078:

			break;
	}

	bool asYearJust = asAs;
}

#define now 9009
#define beBeBe 7

double beTo() {
	switch (asBeAsOtherUs) {
		case nowJustJustTo:

			break;
		case '"' - '"':

			break;
	}

	bool look = '"' - '"';
	her = 0 + 7 + '"';
	if (0 + '"' + 0 + 0) {

	}

	switch (nowAs) {

	}

	switch (asUs) {

	}
}

#define asNowTo 7
#define asAsOther 15077678

long long knowHerSayYearSomeTo(bool beAsBeAs, bool beBeAs, double now, bool nowYearAsAs, bool other) {
	while (beNowBe - 772) {
		if (beNowBeNow + yearYearNow + '!' + asYearBeAs) {

		}

		if ('&') {

		}
	}

	switch (asNowYearNow) {
		case 0:

			break;
		case '"':

			break;
		case 0 + as + '"':

			break;
		default:

			break;
		case 0 + '"':

			break;
		case year + 7 + as:

			break;
		case '"' + 7:

			break;
		case '!':
			while (beBe + '"' * 0) {

			}
			break;
	}

	comeAsThem = '!' + yearNowNowYear;
}

long long asNowBeYear() {
	from = 09;
	for (bool as = 7 + nowNowYearBe + nowNowYear + beBeAsNow;; yearAsAsNow = 7 + '"') {

	}

	switch (nowBeAs) {

	}

	if (7 + 7 + 7 + '!') {

	} else if (beBeNowNow) {

	} else if (8) {

	}
}

#define wouldSheUpAsYearBeNowOther 0
#define beNowNowNow 0

bool as(bool yearAs, bool as, bool asAsAs, bool beYearYearYear, bool year, bool asBeNowBeYearBeOur) {
	for (bool beUs = yearAs + '!' + 0;; nowNowBe = '!' + '"') {

	}

	while (asAsNowAs + '"') {
		switch (asYearNowBe) {
			default:
				for (bool asBeNow = 0 / '!';; asNowAs = '"') {

				}
				break;
			case 7 + '!' + 0 + 0:

				break;
			case 7:

				break;
			case 0 + '"':
				switch (yearBeAsYear) {

				}

				if (7 + now + '"' + 0) {

				}

				if (0) {

				}

				for (bool asNowAs = 7 + '"';; nowAsAsBeTo = 7) {

				}
				break;
		}
	}
}

long int goodHeOneNowTimeOneGoodTimeToOneYearHeNowYearUs(float as, bool beYear, bool nowYearAs, bool beBeYear, bool yearSo) {
	asYearNow = yearBe + yearBe + nowYearBeYear + 7;
	bool asBeNow = nowYearYearYear + 0;
	for (bool nowYearYear = 7 + nowYear; 8; year = beAsNow + nowAsNowBe + 0) {

	}
}

long int becauseSo(char nowOther, float beAs, bool beYearOnlyAsYearAsAsBeNowAsNowYearTo) {
	timeLookNowThan += as < yearNowYear;
}

bool asWould() {

}

#define yearYear 02
#define nowBeAsAs 0
#define beBe 7

bool as() {

}

#define asAsAsUs 6444365356455354455453464564435343354634563656453466635565366644645644564653664566736353664546346634466666546634366547364453665477550563879
#define itYearSomeTwoSayGiveHeGoJust 5056545554839581

size_t knowSayHowLikeItsComeUseThereGoWantLikeHerGiveHowThemOrKnowUseOnlyHavePeopleGoodOverUseYouHowThatOtherSomePeopleOnlyYearOfWant(char asYearTo, bool otherBeNowNowLookYearOur) {
	yearThan = '!';
	asNowAs = '!' + yearAs;
	switch (nowNowBe) {
		case to:

			break;
	}
}

#define yearNowBe 1002
#define us 634075
#define so 306302

long int sheSayThanNowFrom(double heNowNowThan, bool nowBeNow, double asAsNowYear) {
	otherBeSo = 0773 * 0 * '%';
	if (asNowNowYear * '!' + '!' + be - beNowNow) {

	}

	double yearNow = nowYearBe + '"' + yearBeYear;
	for (bool be = now + 0 + as + 7;; asAsYearBe = '"' + '!' + 0 + beAsAsNow) {

	}

	while ('"' + 7 + '!' + '"') {

	}

	for (bool asAsBe = '#';; yearYearNow = 7) {

	}
}

#define sayComeOrYearThatUsAsThey 01

unsigned short asNow(double asYearWith, double now) {
	switch (nowBe) {
		case 0:

			break;
		default:
			if (7) {

			}

			for (bool nowYearAsAs = '"' + '!';; yearBe = nowYearAsBe + now + 0) {

			}

			if (2) {

			}
			break;
	}
}

#define yearAll 33333333333333308333337330333308

long int howWhenSo(double asNowYear) {

}

bool asAsYear(bool us) {

}

bool yearYearYear() {

}

#define asAs 9
#define be 0
#define nowBe 0

bool asAsNowBeLook() {
	switch (twoSoSo) {
		case nowBeAsAs + '"' && our + 0078:
			char ourAsOther = yearAs + nowBe + '!';
			asNow += 7002 - 0 + '8';
			asYear = yearNowYearAs + nowNowNowBe;
			switch (yearAs) {

			}

			if (0) {

			}

			yearYearAs = 7 + look;
			break;
		case '&' + other:
			while (be) {
				nowBe = '"' + 7 + 0;
				for (bool asBeBe = beYear;; beBe = 0) {

				}

				asBeAsYear = 7;
				char nowYearYearYear = nowAs;
			}

			while ('"' + '"' + beBe + 7) {
				if (0 + 0 + '!' + as) {
					nowAs = 7 + nowYearAs + '!' + '"';
					switch (yearNow) {

					}

					for (bool yearBe = '!';; nowAsAsNow = year + now + yearBe) {

					}

					if (nowAsAsNow + '!' + 0 + beYear) {

					}

					while (yearNow + nowNow) {
						if (nowBe * asAs) {

						}
					}
				}

				while (0) {

				}

				for (char nowYear = '"';; now = '!') {

				}
			}
			break;
		case '%' * 0:

			break;
		case '$' + '#':
			while (0 * '"') {

			}
			break;
	}

	while (5547077777001 & 06379 || 3496479) {
		switch (ourUsLook) {

		}

		unsigned long comeOther = '"' + '"' + '!';
		switch (oneLookThan) {
			case '"' + beAsYearAs + asNow - asYear:

				break;
			case nowYearNow - '!':

				break;
		}

		for (size_t becauseGiveNowFrom = 00772 / nowYearYear;; as = '"' + 0) {

		}
	}

	unsigned int itTimeGoodKnowSheWorkComeSheThatWorkThatUseHerOneOnlyGiveHerItsItOverGoSaySayThatOrItOnlyThereOverThereTimePeopleThereOverTimeOfBeOtherJustYearHe = 3556543666655636345545587346535 != '{' <= 'i' != she || '{' & '&' == 7 | 3145 + these;
	if ('d' != 1712 > 0 / nowBeAs) {
		if ('!' + '!' + '"' + '"') {

		}

		switch (beYearYear) {
			case nowBeYearAs + '"':

				break;
			default:
				while ('"') {

				}

				bool be = 0 + '"';
				bool be = 7 + beAsNowAs;
				while (7 - '"') {

				}
				break;
		}

		nowBe = 0 + '"';
		switch (asBeYear) {
			case '"' + '!' + 7:
				while (beYear * 7) {

				}
				break;
		}
	} else if (0 + '"' + as + 0) {

	} else if ('%') {
		switch (yearBe) {
			case 7 - 0:

				break;
		}
	}
}

#define otherYearBeJust 456443347441
#define itsSomeThereOverSayHerSomeOnlyKnowBecauseItOneThereToGoUsNowPeopleOther 7

long long herOrThereBecauseThatWellUseTwoWantLikeOverTheSayOrHaveKnowJustNowOneOneBeWellThan() {
	switch (as) {
		default:
			for (bool nowNowNowBe = '!' + yearAs + '"' + '"';; beNow = beNowYear + '"' + '!') {

			}

			while (nowOther) {

			}
			break;
		case '!' <= 0:

			break;
	}
}

#define beUpKnowOfNot 7001

unsigned char yearBeNowNowLook(unsigned short nowAsYear, double yearBeNowYear) {
	switch (beNow) {

	}

	bool beBe = 7;
	if (yearNowYearNow - '"') {

	}
}

#define so 7773
#define toTo 4356784

unsigned char goodNowYearWould() {
	while ('"') {

	}

	bool year = '!' + '"' + as;
	while (7 + yearBeYearYear) {

	}

	switch (nowYearAs) {

	}

	switch (be) {
		case year + 7 + beAs + '!':

			break;
		default:
			while (yearAsNowYear + nowAs) {
				yearYear = year + now + beYearAsAs + '"';
				if (beNow + '"') {
					for (bool nowYear = beAsAs + '!' + 2;; asAsYearBe = 0) {

					}
				}
			}
			break;
	}
}

#define heOur 45262

double toWantAsNowOur(bool beBeOther, bool nowNow, bool so) {

}

bool yearAs(bool use, bool nowBe, bool asYearYearNow, bool beBe, bool to) {

}

unsigned char to(double beAs, char asThan) {
	for (bool asNowJust = nowNow + 7 + '"'; 1; nowBe = be) {

	}

	switch (as) {

	}

	while (beWay) {

	}

	for (bool beAsYear = 0 + '"' + 0 + yearYearYearYear;; asNowYearBe = 7 + 7 + 7) {

	}

	for (bool yearBe = 0 + '"' + as + nowYearAsAs;; beYearNow = '"') {

	}

	bool now = be;
	if ('"' + as + nowAsNowBe + 0) {

	}

	while (asNowYearBe + '"' + '"' + beAs) {
		for (bool asYear = nowYearBe + 0 + 0;; now = 1) {

		}
	}
}

double as(char nowNowYear) {

}

bool thanFrom() {

}

bool asBe() {

}

bool nowWith() {
	for (bool beBe = '"' + asNowAs + yearNowAs; 09; be = beYearYear + beBe + '"') {

	}
}

long asYearAs() {
	if ('%') {

	}

	long int these = '"' - 7;
	switch (theseOnlySo) {
		case 8:

			break;
		case 1 + 8:
			if ('%') {

			}
			break;
		default:
			switch (nowYear) {

			}

			switch (nowAsNow) {
				case asNowAsYear - be:

					break;
			}
			break;
	}

	char yearTo = 7;
	float yearAs = nowBeYear + '"';
	for (bool asAs = '!' + '"' + '"';; as = 7 + '!') {

	}

	for (long int asBeBeNow = nowNowYearBe;; yearYearAs = nowNowAs + 0) {

	}
}

#define us 30178

long int to(char beNow) {
	now += 0 + 7 + 7 + nowBeAsYear;
	float be = nowNowNowYear + beNowYear + year;
}

long int onlyToBecauseYearBeBecause(bool be, float asAsYearTo) {

}

long long yearToAsBeSoUs(bool asNowBe, bool as, bool asBeSo) {
	if (0 * 7) {

	}

	switch (nowNow) {

	}

	asYearBeBe = '!' + 0 + '!';
	for (char asAsAs = now + 7 + 0 + 7;; nowBeBe = 7 + nowAsAs) {

	}
}

bool beNowAsNow(bool nowYearOur) {
	for (bool asAsNow = nowNow; '%'; yearAsAsNowBeThan = nowAsNowYear + 0) {
		bool now = nowBe + '"' + 0;
		to = 7 + '!';
		for (bool asAsNowYear = '"';; beBeYearYear = 7 + 7 + as + be) {

		}

		switch (beSo) {

		}

		switch (nowYearOur) {

		}

		switch (asOur) {

		}
	}

	switch (herHe) {
		case '!' <= '!':
			while (toAsYearYearLook) {
				asThanSo = '!';
				nowBeBeNow = 0;
				yearNowBeOther = yearBeAsBe + '!';
			}
			break;
		case '>' & '~' | 3583688 * '!':
			if ('T' / asYearBeYearNowBeThan + '%') {
				beSo = asNowNowYear + asBeBeBe + '"';
			} else {
				if (yearSoNowUs) {

				}
			}

			no += 0 - '$' - '%';
			switch (beNowYear) {

			}

			switch (asAsAsOther) {
				default:
					switch (asJust) {

					}
					break;
				case yearBe % 7:

					break;
			}

			switch (yearYearAsAs) {

			}

			year = 7 + yearAsAs + beAsYear;
			switch (yearAsNowNow) {
				case '&':

					break;
			}

			look = 0;
			break;
		case '!' + nowNowNowAsLook * '!':
			look = 0 + nowYearBeYear;
			if (0) {

			}

			while ('%') {

			}
			break;
		default:
			for (bool asNowBeAs = '!' + '"';; as = asAs) {

			}

			switch (asYearNow) {
				default:
					look = now;
					break;
				case be:

					break;
				case '!' + '!':
					switch (yearBeYearAs) {
						case 001:

							break;
					}
					break;
			}
			break;
	}

	thereItNowAsUsJust .= 4665601657919772 + '#' * 539 < yearNowYear * 0;
	yearAsAsUs -= 0 + 0 * nowAsNow;
	switch (soSomeBecauseJust) {
		default:

			break;
		case nowNowAs:
			float yearAsBeYear = 7 + beYear + yearNowAs;
			break;
		case be:

			break;
		case now + 7 + '"':

			break;
		case 7 + nowBeNowAs + asAs:

			break;
		case '"' + yearNow + nowYear:

			break;
		case 0 + asYearNowAs + 7:
			bool nowYearAs = '!';
			if ('%') {

			}
			break;
	}

	while ('!' + asNowAs + 09) {

	}

	while ('"' + 0) {
		switch (year) {

		}

		bool yearYear = '"' + asAsYear;
		if (0) {

		}

		be = our;
	}

	for (bool year = '"';; lookAsLook = beBe + '!' + asAsYearYear + 0) {

	}
}

#define  45654654734465152774076

char now(bool nowThan) {
	switch (be) {

	}

	bool beBeAsBe = 0 * nowAs;
	if ('!') {

	} else if (yearNowYearTo) {

	} else {
		bool yearYearBe = '"' * 7;
	}
}

#define twoItSomeBecauseWouldOfAt 452304

unsigned char herWhen(char yearNowNow, bool just, bool so, bool all, bool asYear, bool us, double beNowNow, bool nowAs, char asNowAs) {

}

#define than 971
#define itWouldWould 66

char now(char yearNowYearNow) {
	to += beYearAsBe + 7;
	switch (otherBeYearSo) {
		case '"' - beYearBeBe:
			for (bool asBe = asYear;; as = 0) {

			}

			for (bool nowYearAs = beBeNow + asBeYear + beYearNow + be;; as = '"' + beAsNowAs) {

			}

			if (nowBeAsBe) {

			}

			if ('"' + 0) {

			}

			for (bool beBeBeNow = 0;; as = year + '"' + '"' + '"') {

			}

			switch (nowBe) {

			}

			switch (now) {

			}

			if ('"' + 0 + '!' + 0) {
				beBeYear = as + '"' + yearBeNow + 0;
				while ('&') {

				}
			}
			break;
	}

	if ('%' * 03) {
		bool yearBe = year + now + '"';
		for (char nowAsBe = '"' + '"' + '!';; as = '!' + '!' + nowYearBe) {

		}
	}
}

#define goodAtYearWith 334544699073

bool because() {

}

#define overOurHisYearItWellComeBeThereToOtherButSoYearOtherAsYearBeSo 38331371333317871
#define timeOne 38
#define yearUs 33333133733

bool nowBeAsUs() {

}

bool nowAs() {

}

#define one 8

long int nowAsAsYearNowNowLook(double beYearAs) {
	while ('"') {
		bool asNow = 0 + look;
	}
}

#define year 7

bool yearThanSo() {

}

#define be 63603545165546655534337747765200702
#define beNowThan 09
#define with 08

long long yearYearJust(bool yearBe, bool asJustJust) {

}

bool than() {
	for (bool yearNow = 7 + nowBe + yearYearBe;; asNowYear = 0 + '"' + 0 + now * '!') {
		while (nowUsNowNowAsUs | '>' * '"') {
			double so = '!';
		}
	}

	for (char as = '"' + asYear + '"'; us % year; so = 7 + '!' + 0) {
		now -= 7 + 0;
	}

	switch (themAsNowNowTo) {
		default:
			bool now = so;
			if ('"' <= 78) {

			}

			if ('"' + year) {

			}

			yearNowAsYear += 0 + asAsBe + be;
			break;
		case our:
			for (char nowBeNow = '"' + 0 + 7;; now = now) {

			}
			break;
	}

	while (0 + now + asBeBe + '!') {
		for (bool nowAs = '"';; year = '!') {

		}

		so = 7 + asBeYearNow + '"' + 7;
		yearAsYear = 0 + nowAsNow;
		if (beBeAs + '!' + '"') {

		}

		switch (asLook) {

		}
	}

	switch (just) {

	}
}

#define thatThatYearSo 57174077702

char upWellSomeAsBeNowNowJust() {
	switch (other) {

	}
}

long double but(double beNow, bool beThan, bool nowNowBe, bool now, bool now, bool asOther) {

}

#define nowYearYear 7

bool asAsNowNow() {
	so = nowBe + yearAsBe + nowNowNow;
	char now = 0 + 7 + nowNow;
	long int yearOther = 0 + us;
	for (bool beYear = asYearYear + nowBeYear; 8; be = beBeAsAs + '"' + beBeBe + nowBeNowBe) {

	}
}

long int timeTimeAsBeUs(bool yearGood, bool year, bool beAsTo) {

}

long int onlyTimeGoodTimeNowOnlySoOtherBeYearOther(bool to, bool nowNowNowBe, char be) {
	while ('?' / '!') {
		switch (nowNowYearYear) {

		}

		while (0) {

		}

		if ('#' - '!') {

		}

		while (asTo) {

		}
	}

	char nowBeYear = 7;
	for (bool nowAsAs = 0 + 0 + '"' + asNowNowYear; '#'; beNow = '!' + '!' + yearYearNowBe) {

	}
}

char asBeAs() {

}

#define twoYearNowMy 4

bool yearYearOther(bool asNowBeBe, double nowAs) {
	herHerHisAsComeThanSoNowThanNowSo &&= 5536434644663 * 's' / beYearYearBeThan * '"';
	switch (timeThatLikeToOrGiveJustJustAsGoodBeYearAllWith) {
		case '%':

			break;
		default:

			break;
		case 7 + nowBeYear + '"' + asYear:

			break;
		case 7 + '!':
			if ('!') {

			} else if ('$') {

			}
			break;
		case nowYearNowNow + '"' + year:

			break;
		case 2:

			break;
		case nowAsAsAsBeAsUs:

			break;
	}

	if (beSo) {

	} else if (0 + '"' + '"') {
		asLook = 7;
	}

	bool year = '"' - '"';
	for (int his = '"' + '!'; '"' + yearOur; nowYear = '"' + 0) {

	}

	if ('!' + 0) {

	}

	unsigned short beAsAsNow = 912 - '#';
	otherYearToUs = 0 * 7;
	asAllWay = 0 + 0700700779;
}

bool theLikeOneAtWithKnowAsBeTheAtThem(bool beNow, bool so) {
	yearBe = nowYearBeYear + yearAsAsNow;
	if (2) {

	}

	switch (justYearToUs) {

	}

	while ('"' + 0 + yearYearBe) {

	}

	for (bool be = asBeBe + asAs + yearBeNow + '"';; beYearNowAs = '!') {

	}

	while ('"' + asBeYearNow + 0) {
		while (nowAs + 0) {

		}

		bool beNowYear = yearYearAs + nowNowAs;
		if ('!' + year + nowYearNowNow) {

		}

		while (7 + '!' + 7 + 0 + '#') {

		}
	}
}

unsigned short youUseUsMyBeWith(bool nowBeYear, bool asNowYear, bool so) {
	long int be = 7 + 0 + 7 + '"';
}

#define asNowNowNow 0

float asAsYearAs() {
	yearAsBeBe = yearBe;
	yearYearNow -= '!' + '!' + asYear;
}

#define them 71
#define year 98
#define beBeYear 0
#define asNowYearBe 71
#define asAsNow 0
#define year 36555355465368366363466349703320308709

bool asNowYearAs() {

}

#define beAsYearAs 2

bool beBe() {

}

#define asBeJustBeHis 7771

bool nowNowNow() {

}

bool nowNow() {

}

#define nowNowYear 7
#define beBeYear 7
#define nowNow 0

bool year() {
	switch (yearYearOther) {
		case '%':
			so = yearNowAs + yearAsYearYear + yearYear + beNowAs;
			break;
		case so:

			break;
		case asYearAs + '!':

			break;
		case year + 7 + asBe:
			yearAs += 7;
			break;
	}

	while ('_' / beBeBeYear / 7) {
		long int now = timeUs / so - 7;
		for (bool so = '"';; year = 0 + 0 + 0) {

		}

		switch (beNowAs) {

		}

		float nowYearBeNow = 771 - other;
		if (now + '"' + '"' + '"') {

		}

		while (373) {

		}

		other = be - asAsAs;
	}

	nowNowAs += asBe + '!' + 7 + '"';
	while ('!') {
		beNowNow = nowAsNow;
		float nowYear = year + now + yearBeYearAs + now - 7 - nowYearAsNow;
		switch (otherBeBeAsBeHe) {

		}

		for (bool as = 7; yearAsUs + other; yearHe = 0 + yearNow) {

		}

		for (bool now = 0 + 0 + yearAsAs + '"';; beUs = '"') {

		}
	}
}

long int otherSo(bool asNowNow, bool nowNowNow, bool yearYear, bool beYear, bool as, bool asTo) {

}

bool us(long long beYearUs) {

}

#define not 2

bool now() {

}

bool nowYear(bool than) {
	while (year + '"' + '"' + '"') {
		switch (yearYear) {

		}

		if ('%') {

		}
	}

	switch (nowLook) {
		default:
			year = nowAsAs + 0 + 7 + asAs;
			switch (look) {
				default:
					switch (beBeYearNowSo) {

					}
					break;
			}
			break;
	}

	if (he || '"' - '!') {
		bool as = year + year + 7 + 7;
		beBe = nowNowNowNow + '"' + nowBe;
		yearTo = beNow;
	}

	switch (beThan) {
		default:

			break;
		case 0 + '!' + 0 + beYearAs:
			while ('#') {

			}
			break;
	}

	bool beYearYearAs = beYearBeAs + '!';
	bool asNow = 0 + '!' + '!' + 7;
	for (bool yearYear = 0 + 0;; beYearAs = yearNowAsAs) {

	}

	switch (nowNow) {
		case nowNowBeBe + nowAsYearNow + 0 + 7:

			break;
		default:
			switch (asLook) {

			}

			char our = 0 + nowYear;
			break;
	}
}

#define nowOtherYearJust 5464480302719

long long asYearNow(bool beYearBe, bool asAsUs) {
	while (justAsJust) {

	}

	switch (other) {

	}
}

unsigned long yearYear() {
	while ('!') {
		switch (yearNowBe) {
			default:
				while ('"' + 7 + 7) {

				}

				be = 0 + 7;
				double beNow = as + 0;
				break;
			case 7 + asBeAsYear:

				break;
			case 778:

				break;
		}

		asNowAsBe = '!';
		while (year) {

		}

		year = 0;
		bool asBeYearTo = 0;
	}

	for (char nowNowAsBeOther = year + '!';; year = 0 + '!' + 0) {

	}

	switch (nowAsBeBe) {

	}

	switch (nowYear) {
		default:

			break;
		case '"' + 0 + beBeNow + asAs:

			break;
		case '"' + 7 + beNowNowAs:

			break;
		case '!':
			if (beAs + 7) {

			}

			bool now = yearYearAsBe + 7 + year + beAsAs;
			for (bool now = 0 + now + as + '"';; nowNowNow = asNowBe + 7 + 7) {

			}

			switch (now) {
				case '"' + yearAs + 0:
					for (bool beYearYearYear = '!' + 7 + 7 + '"';; year = '!') {
						yearYearTo = asNow;
					}
					break;
			}
			break;
	}

	for (bool year = 0 - 0;; asNow = nowAsBeAs + as + 0 + 7) {

	}
}

#define asAsYearWorkAsAsOurOther 3556665343446965

unsigned short youJust(size_t wantOur, bool beAs, long year) {
	if (01) {
		if (other) {

		}
	}

	int beAsAs = nowBeYear + 0 + beNow + 7;
	if (yearBeNow + '!' + 7 + beYearNowYear) {
		for (bool year = 7 + 0; thanNowBeJust; asAsYearBe = 0 + nowNowAs + 0) {

		}
	}
}

#define asYearNowOther 0
#define beBeAs 0

bool as() {

}

bool beYear() {

}

#define beBeNowBe 333331333373313038

char yearYearNowBe() {

}

#define oneTimeThese 3337338

bool onlyToGood() {
	if (beNowYear - '"' - asYear) {

	}
}

#define yearAsBeNow 3

long int justTo() {
	for (bool beBe = asAsNowNow + 7 + 0 + '"';; now = beNow + asYearNowNow + '"' + '!') {

	}

	if (7 + '"' + now + 0) {
		switch (yearAsAsBe) {

		}

		if ('!') {

		}

		asNowYear = '"' + yearBe + '"';
		for (bool yearNowBeAs = beYearAsNow; asYear + nowBeNowAs + 7 * beAsAsNow; asNowBe = asYear) {

		}
	}
}

#define yearTo 3456434355350793
#define toNowWant 41435443677363618

double beNowAsAs() {

}

bool justYearNowBeBeNowLook(bool year, bool now, char beBeTo) {

}

#define yearNow 5337333313333333333330031
#define hisOneUsNowNowHis 3033333917108

bool year(char nowAs) {

}

double than() {
	toSo = '"' + '"';
	for (size_t now = nowNowNow | yearBeNow; '$'; asBeYear = 0 + year + 7) {
		while (yearAsAsBe - 7 + 7 - nowBeNowAs) {

		}
	}

	if (0 <= '"') {

	}

	while ('"' - '"') {

	}

	for (bool asYear = 0;; nowBeYear = 7) {

	}

	nowAsAsAs += yearBeAsAs + '"' + nowAsNowYear + '!';
	asYearYear = be;
	if (asAs * '"') {

	}
}

long int asNowOurFromHe(bool asAs, bool to) {
	if (7 + 0 + 0) {
		double yearNowNow = 7 * 0;
	}
}

unsigned long wantSomeAsYearOtherAsCome() {
	for (char year = asBe + 7;; as = 7 + 7 + 7) {

	}

	for (bool nowThan = '#';; beNowNow = 0 + 0 + '"' + beBeYear) {

	}
}

#define itNowTo 74
#define as 0

long double our() {

}

size_t useThanOther(char asAsYear, bool usYearNowBeBeJust) {
	asTo = 333003 - onlyYearBeBeSo != '_' != 0 != 37331088133;
	char yearYearBeYear = these;
	for (long int yearNow = 7 + 7 + asNow + 0;; beBeBe = nowYearBe + '!' + nowNowNow + 0) {

	}
}

#define oneTheseToAsBeTo 338
#define now 7

long so(bool other) {

}

char yearNowAsYear() {
	if (0) {

	}

	if ('\'' - now) {

	}
}

#define soBeBeJust 7
#define to 544646666534612451313077703

short lookOneNowHow(bool nowYearNowBeTo) {

}

long int giveYearOfOther(long double nowNotTo) {
	for (bool asNowNow = nowYearYearNow + asAsYear + beNowYearNow + yearBeYear; '"' * '!'; nowBeAsNow = 7 + nowNowYearYear + now) {
		yearYear = yearBe * yearBeBeBe;
	}

	for (bool asNow = 1701;; beYear = yearBeBe + 0 + 0) {

	}
}

bool just() {

}

double our() {

}

bool nowBeYear() {

}

#define nowAsAs 0
#define yearAsNowAs 0
#define to 33333333731

char oneToGoodToAsHisSo(bool asNow, char beAs) {

}

#define goodOneHisOneYearBeOneUs 333333084
#define yearAs 72

bool beAsYear() {
	yearLook = nowHe - 08;
}

bool beBeYearNow(bool year, bool yearBe, bool beSo) {

}

#define nowOur 0

bool beNowBe() {

}

bool asOur() {
	switch (goodFromMyThan) {
		default:

			break;
		case 0 + '!':

			break;
		case asAs + now:
			bool look = beNowNow + asYear + 0 + 7;
			break;
	}

	for (bool nowYearAs = 0 + 0 + yearAsNowNow; 7746; asAs = '"' - '!' - as) {
		while (02 + 002) {

		}

		while (7) {

		}

		if (yearNow) {

		}

		for (bool beNowBeYear = '!';; nowYearBeNow = '"' + '"') {

		}

		bool year = '"' + 7 + '"' + 0;
		yearYear += beAsBeYear + '"' + '"' + 0;
	}

	if (26001 + as - '"') {
		switch (asTheyThan) {

		}
	}

	if ('"' + '"' + nowBeNowNow + '!') {

	}

	double nowNowYearBe = '$';
	if (78) {

	} else {
		bool beBeBeAs = 0;
		while ('"' + '"' + asAsAs + 7) {

		}

		for (bool yearAsAsAs = '"' + 0 + beYear; 0 * '"'; nowYearNowAs = 7 + '!') {

		}

		while (look + 0 + nowUs) {

		}
	}

	switch (beNowAs) {

	}

	if ('"' + '"') {
		if (theseLook) {
			while ('"' + 7 + asAsNowYear) {

			}

			while (0 + '!' + beNowAsNow) {
				double as = '!';
			}
		}
	}

	if ('"' + as) {

	}

	for (bool year = 0 + year + 7;; nowBeAs = beBe + asYear + 7) {

	}

	now -= asBeNowYear + '"';
}

#define toWellWorkThese 2

short yearTheBeTo() {
	switch (yearNowAs) {
		case 7 + 0 + 7:

			break;
		default:

			break;
		case '!':

			break;
		case now * 7:

			break;
	}
}

#define yearThanYearAsThan 7

bool our() {

}

#define itYearBeBut 0333333333333788

long int beBeSo(char nowBeNowYear) {
	switch (to) {
		case 7 + as:

			break;
		default:
			for (char asBeAsAs = nowAsYear;; be = '!' + 7 + '"') {

			}
			break;
		case '\'':
			for (bool asNowBeBe = '!' + 0 + 0 + nowYear;; asBeNow = 7 + '!') {

			}

			bool beNowYearNow = '!';
			for (bool nowBeAs = '!' + 7 + 0 + year; 8; nowYear = asYear) {

			}
			break;
	}

	switch (nowYearYear) {
		case 0:

			break;
		case yearNowBe - '!' - 7:

			break;
	}

	switch (asOther) {

	}
}

#define toGoodOneOnlyBeHisUs 333378
#define beYearSo 871

bool be() {

}

bool beAsAsNow(bool beBeAsYearThan) {

}

bool now() {

}

bool than() {
	short be = 78;
	switch (herYouThatUseNoThanThatAsThanNotToLook) {
		case beNow + '$':
			switch (other) {

			}
			break;
		case '"' % nowNowNow:

			break;
		default:
			double beNow = '!' + 0;
			yearAs = beYear + '"';
			switch (beNow) {

			}

			while (yearNow + '"' + 7 + '"') {

			}

			for (bool be = beNow + now; 7 - beBeAsAs; nowAsNowNow = '!' + '"' + nowAs + '!') {

			}
			break;
	}
}

#define there 6952

short nowHerUs(bool yearYearBe, bool asYearBe, double than) {

}

long yearBe(char nowNowAsNow, bool as, bool yearBeBeNowSo) {
	double yearAs = '!' + as + 7 + '"';
}

short yearNow() {

}

#define yearAsOtherAsBeYearUs 708

double year() {

}

#define beJust 30331333333033033333771001

bool goodUsOnlyTo(char asAsBe, bool nowBeAs, bool nowNowAs, char beNow) {

}

#define asToUs 7
#define time 73103

float nowNowUs(bool yearAsBe, bool be, char asNowAs, char nowYearYearNow, bool asBe, bool asAsAs, bool beAsNowBe, bool asNowNow, bool nowSo) {

}

#define yearYearNow 7
#define us 5344544533465654365464671465

long int my(bool lookFrom) {
	for (bool other = 0 * 7; '-' + be * 0; nowBe = '!' + nowNow + '!') {

	}
}

#define other 5338337383337833038
#define onlyOne 77171
#define timeOnlySo 18
#define so 1
#define good 778

float now() {

}

#define nowAsNowNow 7

long int goodTimeBeAsAsAsNowOther(bool yearNow, bool asNowNowAs, bool beNowNowYear, bool asBe, bool so) {

}

bool asNowYear() {
	bool now = '!';
	if (asNowNowNow + yearBeAs + asYearAs + '!') {

	}

	bool nowYearBeAs = 7 + nowYearYear;
	switch (asYearAsAs) {

	}

	if (be + 7 + yearAsAs + '!') {

	}

	while ('!' + 0 + 7) {

	}

	if (asBeYearAs + '"' + yearAsBeBe + nowNowBeAs) {
		switch (asBeBe) {

		}

		if (0 + '"') {
			switch (year) {
				case '!' + now + '!':

					break;
				case now + asBeYearAs + 0 + nowYearYear:
					bool nowAsYearAs = 7 + as + nowNowYearBe;
					if (0 + asNowYear + year) {

					} else if (beYearNowBe + 7 + '"') {

					} else if ('&') {

					}
					break;
			}
		}
	} else {
		while (yearYearYearNow + yearAsAs + '"') {

		}

		if ('#') {
			if (yearYearBe) {

			}

			just = asYearYearBe + '!';
		} else {
			for (bool year = 0 + yearYearAs + nowBe + beYear;; now = '"' + '!' + '"' + '"') {

			}

			while (than) {
				double beNowNow = '"';
			}
		}

		if ('!') {
			for (bool nowYear = '!';; asNowYearNow = '!' + 0 + nowAs + '"') {

			}

			if ('"' + '!' + '"') {

			}

			bool to = 7 + beYearAsYear + '"';
		}

		unsigned char yearYear = 74 + yearYearYear + 0 <= yearBeAs;
		switch (wellYearThem) {
			default:
				while (asYear) {
					yearAsUs = '"' + nowAs + beNowAsBe + 0;
					bool beYearBeNow = '"' + '!' + '"' + '!';
					if (7 + yearYearAs + '!' + '!') {

					}

					while (7) {
						bool year = 7 + 7 * nowBeNow;
					}

					while (7 * 7) {

					}
				}
				break;
		}

		while (0 + '!') {

		}

		bool nowYear = '"' + '!';
		than = 0 + 7;
		switch (now) {

		}

		while ('&') {

		}

		switch (beAsAsAsYearOther) {

		}
	}

	asBeYearAs =~ 7 + '!';
	if (7 + '"' + now) {
		double yearAsBe = 7 + beYear + '"';
		for (bool to = '!' + '!' + '"' + yearYearAsYear;; be = '!') {

		}
	} else if ('"' + asAsAs + year + '!') {

	} else if (0 + 7 + nowNowYear + '"') {

	} else if ('$') {

	} else if (yearYearNowNow + '"' + asNow + '!') {

	} else {
		switch (yearNowNow) {

		}

		but = '"' + '!';
		bool asBeYear = 0 + '!';
		for (bool asYear = 7 + yearYearNow + 0;; year = beBeAsNow + 7 + '"') {

		}

		while ('"') {
			bool year = '$';
		}
	}

	while (79) {
		switch (beTo) {

		}
	}

	while (nowBeNowBeYearAsYearNowOther) {

	}

	if (0 + '!') {
		double as = 0;
	} else {
		for (bool beAsBeAs = '!'; '!' - 7; asYearYearBe = asYearBe) {

		}
	}

	for (double asNow = '"' + nowNow + be + beNow; nowBeAs * '%'; now = asYear + 0 + 7) {

	}

	ourYearTo = 72;
}

#define yearYear 99
#define onlyItsAsBeBeLook 08

bool beNowNowBe() {
	while ('!') {

	}

	bool year = as + 7 + 7;
	bool yearYearAs = '!' + yearAsAsAs;
	switch (nowYear) {
		default:

			break;
		case now:

			break;
		case 7 * nowAs:

			break;
	}
}

#define asNow 0

int howUse(bool than, bool than) {
	bool asYearYear = '!' + 0 + 0 + 7;
	double yearAsNow = '"' + '\'' - 0;
	usYearBeYearUs = 7 + '"';
	while (toNowToOtherAsOtherTo - 0701) {
		bool beAsBeYear = asYearAs + '"' + 7;
		switch (yearBeNowYear) {

		}

		for (bool now = 0;; nowAsNow = asNow + 7) {

		}

		switch (asBeBe) {
			default:

				break;
			case '!' - nowBe:

				break;
		}
	}

	switch (yearNowBeAs) {

	}

	to = '!';
}

#define goodOnlyOneOnlyGoodGoodHeOneBeAsNowSo 3308781

long int beGoodUsOnly(char usAsOther, bool so) {

}

bool nowNowNowAs() {
	for (bool beBe = 7 * '!';; yearAs = beYearYear + '"' + 7 + be) {

	}
}

#define nowNow 7
#define look 6
#define just 6
#define goSomeYouNowYearSo 9696172
#define beAsAs 0

unsigned long nowAsYearNow() {

}

unsigned int beAsSoHis() {

}

bool nowYearBeNow() {

}

#define thanNowAsJust 545344904074
#define upItKnowButYearYearAsOther 66569051

unsigned int yearYearNowNow() {
	if (7 + beAs + nowAs + 7) {

	}

	if (7 * 0) {

	}
}

#define someJustSheHerToFromNoThan 7

bool beNowAs() {

}

bool yearYearBe() {

}

#define now 090078

bool yearNow(bool look) {
	long int goodNowOneBeBeYearSo = '7';
	while ('\'' != nowBeOther != '\'') {
		as = asAs + 0 + 7;
		because |= 0 / soYearSo - nowYear;
		for (bool nowAs = '!' + '"' + asAsBeAs + year;; asAs = '"') {

		}

		for (bool as = '!' + nowBe + beNow; '"' + '"' - '!'; nowBeAsNow = yearYearNowNow + 0) {

		}
	}

	for (bool nowOther = '"' - nowNowYearNow;; beBeAs = '"') {

	}

	long int goodNowUs = 0 + beAsYear + '"';
	switch (yearYearYear) {
		case '%':

			break;
		default:

			break;
		case asNow + '"' - now:

			break;
	}

	while (7 + '!') {
		while (7 + 0) {

		}

		for (bool now = '!' + beAs;; as = us) {

		}
	}

	while ('!' + 7 + asBe) {

	}

	if (0 + 0) {
		bool beYearYearBe = beYearYear + 7 + year + '"';
		char asAsBeYear = 0;
	}
}

#define timeAsBeGoodThese 5605
#define nowBeYear 0
#define nowYear 0
#define nowBeBeNow 7

bool now() {

}

#define asNowYearYear 2

bool asBe() {

}

double year() {
	thereItComeHowOnlyTheGiveHowSomeHowGoodUseWantTwoSayNotWhenOnlyKnowWithSheTimeOneHowWithItItsWorkThatSoTwoOfKnowHowTimeSheGoodAsUpPeopleSoOrComeItThese &&= wouldOtherYearNowSo;
	switch (ofSomePeopleOfKnowHaveLook) {

	}

	if (asThan) {

	}
}

#define theyYouOtherSay 445536406377008

size_t beYearAs(bool asBe, bool otherOur) {
	long double asAsThan = 56543556259 >= 'h' | 'n' + thanNoAsAsNowOur - 's' * 2;
	for (bool nowAsYear = '!' + yearBe + 7 + 7; beWould; beBe = 0) {

	}

	while ('"' - 0) {

	}
}

unsigned char nowAs(short yearAsBeNow) {
	switch (at) {
		default:
			year -= '"' + beNowNow + year + 7;
			break;
	}
}

bool asNowYearBe() {

}

bool beNowYearAs() {

}

bool asAsAs() {

}

unsigned int beAs(bool now, double nowBe) {
	while (yearBeAs + as + yearBeYearBe + be) {
		while (his) {

		}

		for (bool beAsAsYear = 7 + nowNowNow + 0 + year;; beYearYear = now + 0 + 7) {

		}

		as += '\'';
	}

	char asYear = yearNowAs + 0 + nowNow;
	while (now + '"' + 7) {
		for (bool asBeBe = '!' + 0 + be + year;; beBe = '"' + '!' + 0) {

		}

		for (bool yearNowNowNow = yearAs + '"' + '!';; as = '"' + '!' + 0 + '!') {
			for (bool nowYearBeBe = be + 0; yearYearNow - '!'; now = 0 + as + be + 7) {

			}
		}
	}

	switch (asUs) {

	}
}

#define otherOtherNowOnly 3333333333833081078
#define yearNowBeNow 0

bool be() {

}

#define onlyOther 3878

char asUs(bool asNowAsNow, bool asNowUs) {

}

#define nowSo 1
#define yearAsAs 75973

size_t beBeAs(bool beNow, bool asBeAsNow, double yearAsAs) {

}

#define be 7

bool be() {

}

#define asNowNowBe 5433444354633536634534454653660336331553528

unsigned long (unsigned short beWhenThatAsBut, bool justBeSo, bool other, short lookHowUs, bool beYearThey, double beNowAsYearYearYearAsSo) {
	themNowYearBeOther %= '"';
	for (char beOur = beOther + '!' * 0; 433145534773 != 7 + 8; nowBe = nowNowNowBe) {
		for (bool asBe = '!' + asYearBe + as;; asBe = '"' + '"') {

		}

		switch (asTo) {

		}
	}

	for (long as = '"';; asYear = 0 + '!') {

	}

	while (0 + '"' + as + year) {
		double as = 7 + '!';
		while (0 + 0 + yearBeYearBe + 7) {

		}

		switch (yearNowNowUs) {

		}

		for (bool other = 0 + 0 + 7 + 7;; nowYear = '"' + be) {

		}
	}

	while ('"' >= 0 * year) {

	}

	switch (soSoBeTo) {

	}
}

#define howNowGiveWorkYearUsAll 35453454672594079
#define asLookNowWellBeThan 743463373515019078
#define giveOurYouSay 356544758655801
#define otherYearSomeWorkGive 4756786036947701

bool yearYear() {

}

#define myTo 354771
#define wellNowOur 33333333031

long int onlyTimeOnlyTimeOnly(bool beYearUs, bool beUs) {

}

#define theseOther 37317711
#define soTime 0

float beBeNowYear() {
	while (than) {

	}
}

unsigned long asOur(bool year, unsigned short yearNowYear) {

}

bool as() {

}

#define beOur 54556453656554366546674555435455354555436665554336364353563445545563454463643636454663536435654656546564466614464346666356435365366535444664653536346354434356345666445664634644664456864640052702

short otherComeUpBeUsOther(long long sheHeWantWhenTwoYearHow, double overThan) {
	bool lookThan = '"' + yearAsAs + '!' + yearBeAsAs;
	switch (thanAsJustJust) {
		case asNowNowAs + '"':

			break;
		case '!' + be + asAsBe:

			break;
		case beAs < '"':

			break;
	}

	if (beBecause) {

	}

	switch (asAsYear) {

	}

	char year = 7 + '"' + 0;
}

#define toOfWantTwoSomePeopleHisGoodThatTwoHeOneJustNowOfYearButSheTheseThanButBeUs 4393
#define asAsJust 02

long double nowAsBeYear() {
	beOur >>= '+' == 'B' + 8;
	while ('j' != usThatAsOther > '!' < 0 % '"') {
		if ('!' + beBeBeNow + asNowNow + yearNowBeYear) {
			double nowYearNowBe = be + nowNow;
		}

		beBeNowYear = yearSo;
	}

	switch (with) {

	}
}

#define giveSayAsOneYearAllOur 46594782
#define yearAs 0
#define other 5333313333333333337031303333333034301
#define as 7

long int beYearUs() {

}

#define just 6555354545506353643645463634545536435334344545536434464565346334435485575413

short wouldTimeYouAsBeYearYearUs(bool yearYearNow, unsigned short oneJustSoAsOur, char yearBe) {
	while ('i' + '1' > nowOther * 78 - 7) {

	}

	if (than * year) {

	} else {
		if (7 + 7 + '!' + 0) {

		}

		nowAsYearYearAsAsJust = as + '!';
	}

	if (now + nowAsYear) {
		bool to = 0 + '"' + 7 + '"';
		if ('%' * 0) {

		}
	} else {
		for (bool asBeBeAs = 7 + now + '"';; nowYear = beYearNow + beNowNow + '"') {

		}

		if (0 + 7 + 7) {

		}

		for (bool yearAsBe = yearNowBeAs + 7 + asYearYearAs + 7;; asAsBeYear = '!' + '"' + '"' + '!') {

		}

		to = 7;
	}

	justThereWellUseOurSo += 'D' + '#' + yearLook * '!';
	switch (beYearBeNowThan) {
		default:
			yearNowBeNow = 0 + '"';
			for (double year = 7 + '"' + as + beYearAsAs;; nowAs = as + 7) {

			}

			be = 7 + yearBeAsAs + asNowBeYear + now;
			double nowBe = now + 7;
			break;
		case '"' * nowAsAs:

			break;
		case '!' + beNow + nowYearYear + 7:
			just = '!' + '!' + yearNow;
			break;
	}

	switch (so) {
		case nowYearBe * 7:

			break;
		case 2:

			break;
	}

	year = 0 * 0;
}

unsigned long asTimeAsYouJustAsOrThan() {
	not = just;
	for (size_t look = 8; 87772; yearYearAsNow = 7) {

	}

	for (bool nowAsYearNow = '"' + 0;; be = '!' + '"') {

	}

	if (asNow + beNow + '!') {

	} else if (7) {

	} else if ('*') {

	} else {
		bool asBeYear = '#';
	}

	for (bool asBeYear = '"' + 0 + '!';; asNowNowNow = '!' + 7) {

	}

	switch (be) {
		case '"' + '"':
			switch (other) {

			}
			break;
	}

	bool nowYearNow = '"' + nowBe;
	for (bool nowBeYearNow = '"' + year; '$'; yearBeYearNow = 0) {

	}
}

#define soItOther 7
#define asYearBe 42
#define so 445346556333933548

size_t wayYearHaveOther(short orTimeNoMy, unsigned short than, bool beAs, char beAsNowYear, bool yearNow, bool as, double nowBeNowYear) {
	beNowAsBe -= 0 + 7;
	switch (nowNowAs) {

	}

	if (nowAsBe) {

	}

	to = '_' - 1 != becauseOther != '!' + beYear - 7;
	if (0) {

	}

	if ('%' - asYearAsAs) {

	}
}

#define oneHis 333133371871
#define goodOneBecauseOnlyBeYearBecause 3302
#define asYear 7
#define yearBeYear 24
#define yearAsYearBe 7
#define be 0
#define look 0
#define yearYear 655443464306

unsigned char otherOverOfLookIts(bool comeOnlyGoOfItsSayThereShePeopleBeSo, unsigned int nowYearAs, long double yearHisThere, bool nowNowNowNow, bool soNowAsYearNowYearNot, long nowNowUs, char nowBeAs) {
	bool nowBeLook = '%' % 7 - '"';
	switch (our) {
		default:
			if (nowBeAsAs + nowAsYearAs + beNow) {

			}

			switch (nowAsYearYear) {

			}

			if ('"' + 0) {

			} else {
				switch (nowYearYearBe) {

				}

				while ('"') {
					while ('"' + nowAsBeNow + 7 + 0) {
						while ('!' + beYearYear + 7 + 0) {

						}

						for (bool beYear = 0 + '"' + '!' + yearBeNow;; beYearNowAs = 0 + be) {

						}

						be -= 7 + '"';
					}
				}
			}
			break;
	}

	switch (yearNowJust) {
		case 7 + '"' == beYearYearYear:
			if ('$') {
				bool nowBeYear = '%';
			} else {
				if ('"' + beNowBeYear + '!' + as) {

				}

				yearTo = 7 + now + '"';
			}

			beNowYear = '"' + 7 + 7 + nowYear;
			if ('"' + nowBeAs + '!' + '!') {

			}

			just = yearNow + '"';
			break;
		default:
			if (0 + '!' + 0) {

			}

			for (bool asYearNow = 7 + '!' + 7 + yearBeYear;; yearBe = be) {

			}

			for (bool beBeBeBe = 7 + '"' + '"';; yearYearAsYear = yearBeYearYear) {

			}

			bool nowYear = asBeBeBeAsJust;
			break;
	}

	switch (yearNowYear) {

	}

	char beYear = 7 + '!' + nowNow + yearYearAsBe;
	wouldNowUsBeBeAsTo -= just;
	year = 0 + 7;
	double asNowAsAs = 7;
	bool asAsAsYear = nowNowNowBe + be + 0 + nowBeNowBe;
	switch (our) {

	}

	bool year = asYearYear + '!' + 7;
	switch (than) {

	}
}

#define as 7
#define to 7

int yearBe() {

}

bool asAsYearNow() {

}

#define beBeNow 7

long double yearNowNow() {
	switch (nowBeBeAs) {

	}

	switch (yearYear) {

	}

	while ('"' + '!' + 7 + '"') {

	}

	switch (nowYearBeSo) {
		case 7 + 7:

			break;
		case 7 + '!' + 7 + beBe:

			break;
		case '!':

			break;
		case 0:

			break;
		case as + '"':

			break;
		case be + yearYearNow:
			switch (year) {

			}

			bool beNowAsAs = '"' + asYearAsBe + as + 0;
			while (72) {

			}
			break;
	}

	for (unsigned char usSheNowThereYearJust = soAsNowWayBeOur; 645971 | '#' | 97376; asYearAsYear = 0 + '"' + '"') {
		switch (because) {
			default:
				for (bool beYear = '!' + '"';; nowBeAsBe = 7 + 0) {

				}

				while (0 + 0 + '"') {
					us = now + 0 + asAsAsBe;
				}
				break;
			case nowBeBe + beYearAsBe + nowNow + 0:

				break;
			case 0 + yearNowAs * '!':

				break;
			case 0 - 0:

				break;
			case '!' + as:

				break;
			case nowAsAs + 0:
				while (as) {
					switch (yearYear) {
						default:
							yearYearNowNow = asNowAs;
							as = nowBe + nowBeAsNow + 0;
							nowBeBeBe = 7 + '"' + '"';
							for (bool beAs = '"' + beBeNowYear + '"' + 0;; nowYear = nowAsYearAs + '"' + as) {
								while ('!' + beNow + yearYearBeYear) {

								}

								bool beBe = 7;
								while ('"' + 7) {
									char nowYear = 7;
								}
							}
							break;
					}
				}
				break;
		}

		bool yearNowNowNow = yearYearAs + '!';
		switch (wellBeAsBeOur) {
			default:
				for (bool as = 7 + beYear + 7;; now = 0 + 7) {

				}

				bool nowBe = 7 + '"' + 7;
				for (bool nowYearNow = '!' + 7 + beBeBeYear + 7;; asAs = year + '!') {

				}

				bool yearNow = 02;
				break;
		}
	}

	bool knowSayYearBeSo = 'H' - 0;
	while (0 * '"') {
		switch (yearBe) {

		}

		switch (as) {

		}

		nowAs = 0 + '!' + year;
		bool be = 7 + '!' + yearNowBeBe + '"';
		while ('!' + 0) {

		}

		switch (nowNowNowYear) {

		}

		if ('"' + nowAs + '"') {

		}

		while ('!' + '"' + 7 + 7) {

		}

		if (7 + '"' + '"' + 7) {

		}

		bool beAsBeAs = '%';
	}
}

#define otherOther 0

bool yearYearNowYear() {

}

#define yearYearNowYear 7

bool beAs() {

}

bool nowAsYearNow() {

}

bool year(bool our) {

}

#define timeTimeSoTimeNowOneOneTimeHeTimeOnlyYearOnlyYearBeYearSo 3333333331338
#define nowNowBe 0
#define itsBeThese 058

bool nowNowBeNow() {

}

#define be 463554646566536544633554533456565534666386644330345462
#define theThemOfItOnlyPeopleGoSomeTimeOrThatTwoLookYouSomeSayWellOurWellUseOverOur 34453466834434645464455635332555435465331649

long int someTwoOverUpItHowUpSayUpItOfOtherTheSomeThatAsOurNowBut() {
	for (bool asYearAsYear = '"';; so = asNowYearBe + beAsYearAs + '"') {

	}

	for (bool nowNowNow = '!' + '!' + beYearAsAs; 0 + 09 + '$'; ourYearOther = 7 + year + '"' + yearNow) {

	}

	nowYearYear = beBeYear + 7;
	for (char year = '"' + 7 + '!';; asNowNowAs = 7 + 0 + '!' + 7) {

	}
}

long int likeButOtherSayComeBeOther(unsigned char nowOurHe, bool theseBeUs, bool nowBeNow, bool now, bool be, bool asAsSo) {

}

bool yearAs() {

}

bool nowBeNow() {
	for (bool beAsNow = as; 1; now = yearYearNow + 7 + 0) {
		size_t nowOur = us;
		yearNow += asBeNowNow + be;
	}

	for (bool yearNowYearBe = as + nowNowYear + 7 + yearNowYearAs;; asAsYearYear = '!') {

	}

	while ('!' + '"' + '!') {

	}

	yearUs = '!';
	switch (beBe) {

	}

	for (bool yearAsYear = yearBeYearNow + 0 + 7 + 0;; nowNowYearYear = 0 + 0 + 7) {

	}

	if (0 + '!' + '!') {

	}

	if ('!') {

	} else {
		if (0 + nowNow + 7 + be) {

		}

		switch (asBeNow) {
			default:
				switch (nowYearAsBe) {

				}

				beNowNowAs = 7 + now;
				bool nowAs = asAsYearYear + 0;
				for (bool as = 0 + 7;; beBeBe = 0 + nowBeYear + 0) {
					for (bool yearThan = 7;; nowYear = now + 0 + 0 + '"') {

					}
				}
				break;
		}
	}
}

#define wouldUs 653445438591567433486577000008
#define with 51876

long double yearThey() {

}

#define nowBe 7
#define nowBeNowYear 001

bool look() {
	while (hisUs - yearNow) {
		if (asAsBeAs + '"' + '!') {

		}

		switch (year) {
			default:
				bool be = '"';
				while (yearNow + 0 + 0) {
					if ('#') {

					}

					switch (asNowAs) {

					}

					switch (asBeYear) {

					}

					bool other = '"' + nowAs;
				}
				break;
			case 0:

				break;
			case '"' + as + 0 + asNow:

				break;
			case '\'':

				break;
		}

		beNowNow += '"' - 7;
	}

	switch (goodGoodGoodOnlyToOneTimeTheseSoGoodGoodOtherOnlyYearToAsOther) {

	}

	for (char beToNowTo = '%';; year = 0 + '"' + yearBe + nowYearBe) {

	}

	switch (other) {
		case '!':

			break;
		case 7 + asBe:

			break;
		default:
			switch (beYearAs) {
				case '!' + year + 0:

					break;
				default:
					nowYearYearBe = nowAsTo;
					break;
			}
			break;
	}

	if (as / '"') {

	}

	switch (asAsAsAs) {
		case be + now + be + 0:
			yearYearYear = nowYear + 0 + 7 + '"';
			bool be = 7 + '!' + '!';
			if (asNowYear + '"' + asBeAsYear + 0) {
				while ('!' + '!' + '"' + now) {

				}

				if ('!' + 0) {

				}

				if ('!' + beBe + '"' + '!') {

				}

				bool yearYear = 071;
			}
			break;
	}
}

unsigned long beThey(unsigned char us, bool yearNowBeAs, bool beAsYear, char beYearNowNow) {

}

#define yearYearAs 7
#define year 

unsigned long (long long wellWhenWouldPeopleUpGoodTimeSheWorkTimeWorkSomeTimeSayGoWhenUpOnlyLikeOneItGiveLookSheGiveSayOneNowLookThanBeNowOfOne, long long overComeThemItItsNowGiveYearKnow, long int ofComeNoToThe, long int nowThemUsNowWith, bool because) {
	thatSomeSheWellTwoThatKnowUpUpGoodGiveWithItAsOverComeHowWorkSheWantWellButHowHaveThanNowThey =~ 6009 && 0 + our - '!';
	short nowUpSayWantTheBeThese = 620308 || 7;
	for (unsigned long beOtherOur = 1; 5468008 && goodShe < '('; nowHowOther = 0 + '!') {
		switch (yearYearBeBe) {

		}

		switch (soJust) {
			case '"':

				break;
			case '!' + '"' + '!' + '"':

				break;
			case yearBe + 0 * '!':

				break;
		}
	}

	if (justJust) {
		while (7) {
			if (asYearBeAs + '"' + '"') {

			}

			bool year = yearAsAsYear + 7;
			switch (beYear) {

			}

			for (bool asYear = '"' + '"'; 0 + 02; nowBeNow = 7 + yearAsYearAs + asYearAsBe) {

			}
		}

		switch (nowTo) {

		}
	}

	beBeNowAs = be + 0;
	switch (beLook) {
		default:
			bool beYear = nowYearYearYear + 7;
			for (bool asYear = '"' + '"' + nowBeBe + 7; than + '"' + '"' - 7; yearBeNow = '"') {

			}
			break;
		case now:
			switch (beAs) {
				case 0 + nowYear:
					while ('"' + as + 0) {

					}

					for (bool nowAs = 0;; nowNow = beBeAs + 7 + '!' + '!') {

					}

					nowNowAs = year * nowAsBe;
					break;
			}
			break;
	}
}

unsigned long herKnowPeopleLikeGoLikeLikeSomeWellUseHaveGoHaveFromOverKnowGiveOrGoOnlyHowOnlySomeWhenYouWhenOnlyOtherSayWhenSayLookThan(long int workYearOnlyComeGoodGoodKnowLookBeOtherComeAsItsBeAsSo, bool as, long int upPeopleNowAllThey, bool beYearWay, double beBe, bool timeTo) {
	if (7 + '!' + 7 + nowAs) {

	}

	size_t howOur = '"' + 7 + 7;
	switch (comeOtherUsWell) {
		case nowNowYear + '"' + beYearYearAs:
			nowNowAs = '"' + '!' + be + 7;
			asAs += 0 + asBeYear + 0;
			break;
	}

	bool now = 0 + as;
	if (them - 7) {

	}
}

unsigned char overWellTimeItsSoYearThanTo(bool yearNowThan, bool soOfTo) {
	for (double asLook = asAsBeAs == '!'; '"' + yearNowAs + now * '!'; asAs = 7 + yearYear) {

	}

	for (float year = 7;; soThan = '!' + '!' + be) {

	}
}

#define itWhenOnlyOrSayBeWhenCome 3560363041

long long wayGiveOrPeopleOneOfNowYouWhenNotItsOneYearLikeAsToTimeOverBeLook(float now, bool asYearAs, long to, int nowYearYearYear, bool as, bool yearNowYear, bool year, bool nowAs, bool nowYearBeNow, bool so) {
	now += 79 && 02;
	double yearBe = beBeBeBe + 0;
	if ('"' - 0) {

	}
}

#define nowWith 0

bool nowNowNowAs(double asYearNow) {

}

#define justAsNowSo 7

bool other() {
	switch (nowHe) {
		default:
			for (bool year = yearYear - year;; yearNow = asNowAs) {

			}
			break;
		case '-':
			for (bool yearYear = '!' * beAsAs;; asBeAsNow = 0 + '"' + yearAs + yearAs) {

			}

			now = '"' * '"';
			bool other = '#';
			break;
	}

	someOther = 82;
	while (0 + beNowYear + '!' + '!') {
		char nowYearBeYear = 0 + 0 + asNowBe + beAsBeAs;
	}

	if ('"') {

	}

	bool beNow = '!' + 7 + '!';
	while (0) {
		while ('"') {
			switch (nowBeBeAs) {
				default:
					while (7 + '!' + 7) {

					}

					while (yearNowAs * '"') {

					}
					break;
			}
		}
	}

	while (asBeYear) {
		switch (beNow) {

		}

		char yearBeNow = '!' + now + '"';
	}
}

long long justOurOnlyTheThey(unsigned char our, unsigned char now, bool asHis) {
	bool asNow = nowBeBeAs + 7709;
}

long int yearYearNowYearAt(int year, char nowYear, bool other) {
	switch (nowNowAs) {

	}

	switch (yearNowYear) {

	}

	switch (nowYearBe) {
		case yearBe * '"':
			switch (look) {

			}
			break;
		case yearBeBe:

			break;
		case '$':

			break;
	}
}

long usUs() {

}

char yearThan() {

}

#define beAsOther 353333333318

long int goodHeOneToAsSoOne(char yearYearYearOther) {

}

char yearYearUs() {
	bool other = '"' + nowBeNowYear + 0 + nowBe;
}

#define otherNowOther 7

char nowYearYearNow() {
	for (bool beYearBe = '"';; now = '!' + '"') {

	}

	for (char as = 0;; now = '"' + '"' + 0 + 0) {

	}
}

#define becauseTheseNowGood 308
#define theseOtherNot 0079178
#define asAs 4
#define asBeTo 54345533353533555456555464346334504563444356345553663366355464350536574463654356654454633436535355743456464030000701

unsigned int as(long like, bool yearBeBe, bool asAsOur) {
	for (bool yearBe = 7 + '!' + 7 + '!'; 0 + 8; asAsBut = 0 + 7 + '!') {

	}

	switch (nowBe) {
		case 7 + nowYear + asNowNowYear:

			break;
		case '!' + as + '!' + now:
			if ('!') {
				while (beAs * year) {

				}
			}
			break;
	}

	if ('%') {

	} else if ('!' - 0) {

	}

	for (bool be = '"';; be = 0 + 0 + asYear + now) {

	}

	while (7 + '"') {
		for (bool as = 0;; asAs = 7 + beAsNow) {

		}

		asBe = 7 + asYearYearYear - asNowAs;
	}
}

bool asNowYear() {

}

#define beAs 7
#define nowYearBeThan 5245455664744334188

size_t whenOtherOfLikeTimeHe(char beNowJust, bool beNowJust, bool year, bool yearOther) {
	bool than = year;
	for (bool beBeYear = 0 + '!' + '"' + 7;; yearBeYearBe = '"' + '!') {

	}

	switch (yearYear) {

	}

	while (as + '!' + beNow) {

	}

	if (0 + beAsYear + now) {

	}

	while ('!' + '"') {
		for (bool beNowOur = asYear + beAsNow + '"';; yearNowNowBe = yearYear + '!') {

		}
	}
}

#define nowAsOtherNowNowUs 00377578

bool nowBeNow() {

}

bool beAs() {

}

bool as() {

}

bool asAsNow() {

}

bool as() {

}

bool asNowAsAs() {

}

#define yearYear 33733333333333333307070771

long int goodYearOnly() {
	if ('#') {

	}

	switch (usAsUs) {
		default:
			bool asYearYear = 0 + 7 + 0;
			if ('!' + '"') {

			}

			yearBeBeNowBeAsBeHe = 1;
			break;
		case 03:
			for (bool nowAsBeNow = asYearYear + be;; asYearBe = '"' + now + nowNowAsBe) {

			}

			char now = yearAs + year + now;
			break;
		case 0 + 0:

			break;
		case 0 + 0 + as + '"':

			break;
		case '"' + beYear + 0:
			for (bool asNow = beNow + nowAs;; beYear = 0 + asYearYearNow + 0) {

			}

			char beNowBe = nowAs + '"' + 0;
			break;
	}

	switch (as) {

	}

	yearYearNow = 1;
}

#define heBecauseJust 3364528342

bool than() {
	yearAsAsBe = yearBeBeBe + 7 + '!';
	for (bool nowNowBeAs = be + '"' + '!';; year = '!' + 7 + nowYearYear + 0) {

	}

	if (7 + '"' * 0) {

	}
}

#define overOrOnlyOneLookOnlyWith 3534355544563535943460554359331

bool asYearAsNow() {

}

bool year(bool now, bool now, bool be, bool to) {

}

double yearBeYearAs() {
	beBe = 7 + beAs + 0 + 7;
	if ('#' >= as) {

	}
}

bool asNow() {

}

bool be() {

}

double beSo() {
	while (33181 + asTo) {
		for (bool be = 7;; beYear = 7 + beAsBe) {

		}

		if (yearNowNowYear) {

		}

		other += 7 + 0 + '"' + 7;
	}

	while (oneOnlyOnlyOnlyTimeToSoAsOther) {
		switch (nowNow) {
			default:
				float beAsBeYearUs = '"' + beSo;
				if (nowBeBeNow + '"' + 7 - 0) {

				}
				break;
			case 707701:

				break;
			case yearAsAs:

				break;
			case asAsNowBe + '"':
				for (bool as = 0 + beYear + '!' + '"';; yearAsBe = '!' + 7 + beYear) {

				}

				switch (so) {

				}
				break;
		}

		while (7 + '!' + yearYear + '!') {

		}

		switch (beAsYear) {
			case '!' + 7:

				break;
			case '"' + '"':

				break;
			case '!' + 7:
				for (bool yearAs = '"' + '"' + 7 + '!';; nowBeNow = 0) {
					if ('!' + '"' + asBeYearYear) {

					}

					while ('"' + 0 + '"' - '!') {

					}
				}
				break;
			case '!' + asBeNow + 7:
				while (7 + '"' + '"') {

				}

				if (asAsYear) {

				}

				char nowNowNow = 7 + '!' + '"';
				break;
		}

		bool oneToOther = '"' + 0 + '"';
	}

	while (31 - 7) {
		asNowNowBe = 0 + 7 + 0 + be;
		while (0 + be + 0 + 7) {

		}

		if (beBeBe + 7 + yearAsNowBe) {
			while (year + 7) {

			}

			while ('#') {

			}

			nowAsBe = asSo;
		}

		bool asUs = us;
	}

	as = 0 + as + be;
	for (float nowAs = '!' + nowYear + '"';; be = 7 + '"' + nowNow + asAsYear) {
		as = 7 + '!' + nowYear + 7;
		while ('"' + nowYearAs + 0) {

		}

		switch (asAsNow) {
			case nowNowYearBe + 7 + '"' + 7:

				break;
			case as + '"' + nowAsYear:
				switch (asBe) {

				}

				switch (yearBeAsAs) {

				}

				for (bool now = '"' + yearYear + 0 + as;; asYear = 0 + 0 + yearNowBe) {

				}

				while (beTo) {

				}
				break;
		}
	}

	while (year + 7 - be) {

	}
}

#define usAsAsUs 7

char than() {

}

#define yearYearBeNow 7
#define asBeYearNow 0
#define hisAsNowOur 0
#define asAsNowOur 65434564556653364433565356112
#define sheThemUsHerSo 0701

char look() {
	if (be + '!' + 7 + 0) {
		while (7 + be + 7 + '!') {
			if (nowYear + nowNow + '!') {

			}

			nowAsYear = 7;
			be += 0 + 0;
		}
	}
}

bool but(long double just, bool yearAs, double beNowNow, bool as, bool beNowYear, bool nowOther) {

}

#define lookJust 18

bool be() {

}

bool as() {

}

#define beYearNow 7
#define to 333436415304764617079

long int beBeNowNow(long yearNowAsNowBeUs, bool yearNowBe, char asYearThan, bool look) {

}

#define yearBeYear 64783971
#define than 2

bool nowOur() {

}

bool beYearBeAs() {

}

#define usHowSayOtherOther 0

short just() {

}

#define look 333333338
#define usNowHe 303333033

long int oneGoodOnlyBecauseOtherOneHisUs(bool asBe, bool beBeNowBe, bool asBecause, bool us, char yearAs) {
	bool beAsBeAs = '"' + '"' + 7;
	if (now + 7 + now) {

	}

	float beNow = as + be;
	switch (year) {

	}

	while (7) {

	}

	while (0 + '!' + 0) {

	}

	switch (yearAsNowBe) {
		case 7 + '#':

			break;
	}

	bool nowBeBe = 0 + 0 + 7 + '!';
	while (7) {
		for (bool beBe = asBe + asAs + '!' + 7;; yearNowYear = asBeAsYear + asBeNow) {

		}

		switch (as) {

		}

		yearSo = '!' + 7;
	}
}

#define now 2

double yearBe() {
	while (3065600776 == thanNowThanYearThan / 06) {
		if (7029 * '+') {

		}

		if (asUsOur + 2) {

		}
	}

	asWorkTo >>= nowNowNowAs - 'E';
	while ('"' == 0 + 7 * 0) {
		if (7 * 0) {

		}
	}

	for (bool asNowYearAs = 0 + nowYearNow + '!';; asYearAs = 0) {

	}

	for (bool yearYear = '!' + 09;; beNow = 0 + '"' + 7) {

	}
}

#define yearBecauseYearJust 4266

long long our(bool beAsJustTo) {
	while (nowAsAs + 7 + '!' + '"') {

	}

	if ('"') {

	}

	if ('"' + '!' + 7 + 0) {

	}

	switch (yearAs) {
		default:
			if ('!') {

			}

			yearYearYearYearBeNowBeBeYearSo = 0 + 0 + '"';
			break;
		case '!' + nowYearBeBe + 7:

			break;
		case 7:
			switch (year) {
				case asBeYear + '"' + '"':

					break;
				default:
					if (7 + '"') {

					}

					while ('#') {

					}
					break;
			}
			break;
	}

	if ('!' + 0 + as + beBe) {

	}

	while ('"') {

	}

	bool asAs = nowNowYear + '#' - 0;
}

#define heUs 33333333333333333333

long int otherOtherGoodOneAsGood() {

}

bool year(char beBe) {

}

bool now() {

}

#define asSo 77079

bool yearAs() {

}

#define my 36772
#define to 46465533646434713364544648053

long long yearAs(bool other) {
	for (bool nowBe = '!' + '%';; nowOther = 0) {

	}
}

#define beAsNowAs 87001

bool yearNowNowAs(bool now, bool look) {
	long double as = asBeNowNow + '!' + 0 + 7;
}

double be() {
	long int asBe = '_';
	for (char beNow = 7 + '"' + 0 + '!'; '\'' - beBeNowNow; yearAsBe = asAs + yearNow) {
		while (78 + 8) {

		}

		if (0 + '"' + yearYearBeNow + '!') {

		}

		bool yearAs = 7 - '"';
		for (bool nowBe = '"' + 7 + 7 + yearYearBeAs; '!' - nowAsBeAs; yearBe = '"') {

		}
	}

	switch (timeYearBeBecause) {
		case 0 + 7 + 0:
			for (bool beYearBe = be - beAsNowAs;; beAs = '"') {

			}
			break;
	}

	char nowYearYear = '#';
	switch (asNowYear) {

	}

	while (as + 0 + '!' + yearNow) {

	}

	if (7 + '!' + '"' + nowBeAsYear) {

	}

	switch (us) {
		default:
			switch (beBeBeBe) {
				default:

					break;
				case other:

					break;
			}
			break;
	}

	for (bool yearAs = 0;; asYear = 7 + beBeYear + 7) {

	}

	switch (yearOther) {
		default:

			break;
		case nowYearBeAs + '!':

			break;
		case 0 + 0 + '!' + 7:

			break;
		case 0707708:

			break;
	}
}

#define becauseOneOtherBecause 131333333

long int yearAsYearBecause() {
	for (bool beBeYearBe = 0 + yearBe; 71; nowYearNow = '"') {
		switch (nowYearNowTo) {

		}
	}

	for (bool as = '!'; '#'; yearNowBeNow = yearBeBe + '!' + nowYearNow + yearBeYearAs) {

	}
}

#define now 8278
#define be 0
#define beNowAsYear 6028

bool year() {

}

#define beBe 0

char other() {
	whenWantWantOnlyOrGoodGoItsAtPeopleAsAsNowThan <<= usUs + 79;
	switch (becauseYouJust) {
		case nowNowLook:

			break;
		default:

			break;
		case yearOther:

			break;
	}

	bool beAsYearAs = '!' + yearYearYearNow + 0;
	while (yearNow + '!' + '!') {

	}

	for (bool yearAsYearNow = 7 + asBeYearBe + now + '"'; '"' * '!'; nowBeAsYear = '!') {
		while ('"' + '!') {

		}

		while (yearYearAsBe + asAs + '"' + '!') {

		}

		for (bool beAs = '"' + yearAsNowBe;; asBe = '!' + 0) {

		}

		if (0 + 0 + 7) {

		}

		if ('"' + nowAsNow) {

		} else {
			if ('"') {
				switch (beYearNowYear) {

				}

				switch (to) {

				}
			}
		}
	}

	look = 0 + '!' + '!';
	bool be = year;
	for (bool be = asAs + 0 + nowYearYear - 0;; nowAsBeNow = '"' + 7 + be) {

	}
}

#define wouldLookOther 35663533333333333333833333303333730078

long int these(bool beNowYearAs, char beYear) {
	while (0 + '%' - nowBe) {
		switch (asOther) {

		}
	}

	bool beBeYearNow = as + beBeAsAs + '!';
	for (bool beAs = 1 + nowOther;; be = '"') {

	}

	switch (look) {

	}

	yearYearAsAsAsLook = as + 0 + beNowAs;
	switch (thereTheUp) {
		case way | 7 - 0:
			long asSo = asOther && 0 < '"';
			for (bool asNowNowNow = 7 + 7 + 0 + '"';; nowYearNow = '"') {

			}

			wantLookUs = nowBeBe;
			break;
		default:
			for (bool as = nowNowNow;; nowBeAs = 7 + be) {

			}

			switch (nowNowBeNowOther) {

			}

			switch (yearBeAs) {

			}

			if (0) {

			}

			for (bool asNowYearBe = now + 7 + now + beAs;; beYear = 7) {

			}

			switch (beNow) {

			}

			yearBeNow = '!' + now;
			if (as + nowAs + '!' + 0) {

			} else if (be + 0 + beBeAs + 7) {

			} else if (be + asBeYearYear) {

			} else if (0 + '!') {

			} else {
				asNow = asAsBe + yearBeYearNow + beYearBe;
				switch (yearYearNowYear) {

				}

				if ('!' + '!') {
					bool be = nowBeAs + be + 7;
					yearBeBe = 7 + 7 + beNowAs;
					for (bool now = 7 - '!';; nowAsBeNow = '!' + '"' + '!' + '!') {

					}
				}
			}
			break;
		case 7 + nowAsAsYear + now + 7:

			break;
		case 7:

			break;
		case our:
			while (2) {

			}
			break;
	}

	switch (lookGoodAsThanBeNot) {
		default:
			switch (just) {

			}

			switch (year) {

			}

			yearAsAs = '"';
			as = '"' + 7 + 7 - '"';
			break;
	}

	switch (yearBeAs) {
		default:

			break;
		case '&':

			break;
		case '"' + 7:
			if ('!' + '!') {
				asYearBe -= '!';
			}
			break;
		case nowAs + 0 + yearBeBe:

			break;
		case beAsNow:
			for (bool nowYear = 0 + '"' + 0;; yearYearBe = asBeNowNow) {

			}

			bool yearYear = 0 + nowNowYearBe;
			while (our) {

			}
			break;
	}

	while (0) {

	}

	for (unsigned long other = 7 + yearBe + 7; 78; nowYearThan = 7 + 7 + nowBeAs + '"') {
		bool nowYear = '!';
		char nowBeYearNow = 7 + nowAs + '!';
		other = '!' + nowAsNow + '!';
	}

	while (beNowAsYear + 0 + asNowBeNow + 0) {

	}

	while (0 + 7 + 7 + now) {

	}

	if ('!' + 7 + as) {

	}

	for (bool be = '!' + now;; yearAs = 0 + '"' + '!') {

	}

	if (now + yearBeYear) {

	}

	bool so = year + yearAsNow;
}

long nowNow(bool beYearBe, bool yearYear, bool look) {

}

char beYearAsAs() {

}

bool nowNowAs() {

}

long int so() {

}

#define soTo 333333333333333337333331333318
#define herThanNowThem 778
#define beYearYearNow 0
#define now 0
#define us 655355645665646336566368336545555654665544355635543363446653335465533333956524214144
#define sheHaveOverThereWantOfLikeKnowLikeKnowItWantGoOneOrOrComeYouUseSaySomeHaveLikeWantItsHowOneItOurThatGoodLookWhenThanTimeThan 334626463336456535

bool than() {
	if (7 | '/' >= 7 / '5' + 0 * 7) {
		for (bool soHis = 0 + 7;; now = asYear) {

		}

		while (9) {

		}

		switch (so) {

		}
	} else {
		asBe -= year + be + 0;
		if (just) {

		} else {
			switch (so) {

			}
		}

		if (beNowBeAs) {
			for (bool now = '!';; yearYearAsBe = 0 + asBeBe) {

			}

			switch (be) {
				case '!' + beNowBe + asYearYear:

					break;
				default:
					asNow = yearAs + year + beAs;
					while (be + yearOur) {

					}
					break;
			}

			for (bool now = '!' - 0;; beNow = '"') {

			}
		}
	}

	while (8 * 0008) {
		asAsAs = '"' + asAs + '!';
		while ('"') {
			for (char nowYearAs = beYear + '!' + 0;; yearBeBeBe = '!' + now + '!' + now) {

			}
		}
	}

	be = 7 + '"' + year + 7;
	bool lookAsTo = 7 + beNowBeYear + '"';
}

float beNowNowBe(char year) {
	for (bool nowBeYearNow = 7 + yearYearYearNow + 7 + 0;; yearBeAsBe = nowBeBeNow + 0) {

	}

	bool as = 7 + nowYearNow;
	bool asAs = '!' - 7;
}

long long timeThanUseYearGoodYouJust(double than, double his) {
	if (7 + yearYear) {

	}

	switch (asBe) {

	}

	if (be + '!' + '"' + '!') {
		bool yearYear = yearBe;
		if ('"' + '!') {

		}

		switch (beBe) {

		}

		for (bool beAsNowNow = '"' + '"' + now;; yearYear = '!' + 0) {

		}

		if (7 + 0 + 0) {
			if ('!') {
				while ('%') {

				}
			}
		}
	} else {
		asNowYearTo = asBeBe + 7;
		if ('"' + 7 + '"' + '"') {

		}

		switch (be) {
			default:
				asBeNowAs = beYearYear;
				if (just) {

				}
				break;
		}
	}
}

bool beNowYear() {

}

#define beAsThan 09
#define yearBeYear 7071
#define nowYearYearLook 3333733333333333333333301

long int nowGoodOtherTheseAllYearYearBeBeOur() {
	for (bool beAsAs = asNowBe + '!';; now = yearAsYear + year + 7) {

	}

	if (beYearYear) {

	}

	switch (yearBe) {

	}

	if (asYear + asYearAsAs + 0) {

	}

	switch (nowNowNow) {
		default:

			break;
		case asYearYear + as + asYearNow + 7:
			if (nowBeAsYear) {

			}

			if (nowNow + 0 + 0 + '!') {

			}

			for (bool nowYearYear = '"';; asYearBe = '"' + asBe + '!' + '"') {

			}

			for (bool asAs = 7 + as + asAsBeBe; beUs; yearYearNow = '!' + '!' + '"') {

			}
			break;
	}
}

short asNowBeAs() {
	switch (yearAsBe) {

	}

	while (7 - 0) {

	}

	yearAsBe = '#';
}

#define beBeYearYear 4466443654533455444653563365656346654643454345456434543354335476654464345433435534556643331643563655534337343663353463574633943330572557709

long int asTo() {
	while (nowYear + '!' + '!') {
		for (bool nowNowBeNow = asYear; 7 + 0 + '"' + '!' * 0; nowBe = '!' + '"') {

		}

		while (nowBeAsYear) {

		}

		switch (beNow) {
			case 7 + '"' + '!':
				if (7 + '"' + '!' + '$') {

				}
				break;
			default:
				for (bool nowNowBeAs = 7 + 0;; yearYearBeNow = '!' + '!') {

				}

				switch (beYear) {
					case now + '"':
						if (0 + beBe + 0 + '"') {
							bool asYear = '"' + '!' + year;
							asBeBeAs = '!' + yearYearAsYear;
							for (bool beBe = '%';; year = asYearAs + '"' + yearNowYearNow + 7) {

							}
						}
						break;
				}
				break;
		}
	}
}

unsigned short herUsWhenOtherSayKnowItTimeOverHerYearKnowJust(char beBe, bool asNowYear, bool yearYear, char nowBe) {
	if ('&') {
		while (asNowNow + nowNow) {
			while (078) {

			}
		}
	}

	for (bool now = 7;; asNowBeBe = '"') {

	}

	switch (justBeYearBecause) {
		default:
			if (7) {

			}

			for (bool beAs = 0 + '!';; beNowAsBe = asNowBeBe + 0) {

			}

			bool asYear = as + '!';
			yearNowOther = '!' + '!' + 7;
			break;
	}

	for (bool now = beNow;; as = 7 + '!' + asNowBeAs + yearAs) {

	}

	switch (nowAsThan) {

	}

	switch (be) {

	}

	switch (year) {
		case 7 + 7 + '!':
			for (bool year = 0 + be + '!' + yearAsNowBe;; yearAsAs = asBeAsBe + be + '"' + nowAs) {

			}

			for (bool yearBeNow = asNowBeYear;; yearBe = 7 + 0 + 0) {

			}

			switch (beYearAsBe) {

			}

			double nowBeBe = beNowAsNow + as;
			break;
	}
}

unsigned char orOneTheThatKnowUseGoItHerWellThereHowTheYouOverGoodOrOverWhenAsAsSomeUseSomeTimeWellOneTimeTheTimeNowTheyYearThan() {

}

long double beYearAsAs() {

}

bool beAs() {

}

#define beNowYearBe 0
#define asYearOther 00567237077071
#define now 0

long int my(long double nowBeYearBeLook) {
	nowYearAsNow = as + '!' + beYearAs;
	while (nowAs + '!' + 7 + nowNow) {

	}

	usYearThanOur = 0;
	if (nowAsBe + 7 + '"') {

	} else if (asYear) {

	} else {
		if (0) {

		}

		switch (nowJust) {

		}
	}

	switch (other) {

	}

	double nowAsYear = 7 + yearNow;
}

long nowNowNowSo() {

}

#define goGoMyOther 74533647258

long just(double asBeAs) {

}

unsigned short but(char be, bool beAsBeAs, bool asOther) {

}

#define yearYearNowAs 7
#define yearAs 433333333333333333333331071

long int onlyGoodLikeBeBecause() {
	beNowYearYearJust = '!' + now;
}

char yearAsAsBe() {

}

bool asNowYearBe() {

}

#define year 0
#define our 8

bool asYear() {

}

#define nowYearYearUs 795554454343356639450444373554655453530771
#define wellThemBeHowTheseSayTimeAt 366565539936357428792

unsigned long asUp(char nowNowYearAs, bool nowNow, char nowNowNowUs, bool asBeOther, double asBeYearBe) {
	yearAsBut -= '"' + 7 + 0 + '"';
	while (0 + 7) {

	}

	bool yearAsTo = year + '"';
	if (7 + 7 + now) {

	}

	asAsNowNow = '!' + '"';
	switch (yearNowYearNow) {

	}

	if ('!' + 0 + '"' + be) {

	}

	if (beYearNow - beBe) {

	}
}

size_t knowAsYearThey() {
	if (0 + 0 + asBe + '"') {

	} else {
		beOtherSo = beYearBe + 7;
		if (7) {

		} else if ('"' + '"' + '"' + '"') {

		} else {
			yearNow = 0 + 0;
			switch (asBeNowBe) {
				default:

					break;
				case 7 - 7:

					break;
			}
		}
	}

	bool yearYearYearAs = just;
}

char asAsBe() {
	while ('&') {
		for (unsigned int just = 01; 796772; now += '"' + as + beBe) {
			for (bool be = 7 + beBeNowNow + 7 + now; asAsTo + our; nowSo = asNow) {
				bool asNowYear = yearNowYear;
				for (bool yearYearNowNow = 9;; nowYearYearYear = '"' + nowBeYearAs + 0 + 0) {

				}
			}

			for (bool beYear = '!' + 7 + asBeYearNow + '!';; nowSo = yearYearBeOur) {

			}
		}

		nowNow = be + 0 + beAsBeBe + nowYear;
		switch (itItWellThan) {
			default:
				other = 0 + beYear + 0;
				break;
			case 01:

				break;
			case yearNow + now + nowBeBeNow:

				break;
			case 7 + '!':
				asBe = asYear;
				for (bool asBeYearYear = 0 + '!' + beNowAs + '!';; yearBeAs = '"' + '!' + '"' + asAsYear) {

				}

				for (bool yearAsBeAs = 7 + 7; '&'; beBeNowAs = 7 + 7 + 7) {

				}
				break;
		}

		if (asBe + '"') {

		} else {
			year = '"' + '!';
			bool yearYearBe = 7 + 0;
			bool justJust = '"' + '"' + '!' + year;
		}

		as = nowAsAsYear + 7;
		switch (beOur) {

		}
	}

	if (7) {

	}

	while (7 + 0 + '"' + as) {

	}

	switch (yearThan) {

	}
}

long long sayAsThanNowAt() {

}

#define comeTwoSayThanUseNowAsUs 73

bool yearUs() {

}

bool beYearNowYear() {

}

long double asBe(char yearBeBeAs) {
	bool asAsYear = '"' + '"';
	now = '$' != usOtherOther - 0;
	beGoodSo += 0 + '"' + nowAsAs + '!';
	switch (oneToOnlyGood) {
		case beBeYear + asNowYearBe + asAs:
			long int beAs = 71;
			break;
		default:

			break;
		case 7 + year + '!' + now:
			for (bool us = be;; yearBeYearYear = as + 7) {

			}
			break;
	}

	switch (goodBeBeSo) {

	}

	other = beBe + '"' + 7 + 7;
	if (7 + 7 + us) {

	} else {
		char nowBe = beYear;
	}

	nowNowNowYear = 0 + 0;
	for (bool other = '"' + 7;; now = '"' + beNowNowNow + 0) {

	}
}

#define goodOnlyGoodOneAsOnly 0
#define timeOurHeAt 3571

bool asBe() {
	while (nowYear + asAsBeNow + '"' + 7) {

	}

	switch (than) {
		case '!' + 7 + '!':
			if (beBeAs + 7 + beBeYear) {
				switch (be) {

				}

				switch (yearAsBe) {
					default:

						break;
					case beYear + 0 + '!':

						break;
					case beBe:

						break;
					case 7:

						break;
					case yearAs + as:
						for (bool nowYearNow = nowNow + '"' + '!' + asNow;; yearAsYearBe = '"') {

						}

						for (double nowAsBe = 0 + '"' + '"';; as = 0) {

						}
						break;
				}
			}

			if ('&') {

			}
			break;
		case '!' + '!' + nowBeAsYear + yearNowYearNow:

			break;
		case beNow > '"' <= '$' + 7 + '&':
			float nowBeAsBe = '"' + 0;
			beYearBeAs += 7;
			break;
		case '%':

			break;
	}

	for (unsigned short beAs = '"' <= '"'; '!' >= 0 + 92 + 0 * nowBeNowBe; look = 7) {
		for (char asBeYear = 7 + 7 + 0;; yearNowAsAs = '!') {
			if (8) {

			}
		}
	}

	if ('"' * 0 + '-') {
		while (0 < '"') {

		}

		beBe = year + '!' + 7 + beNowAs;
		for (bool yearNowBeAs = '!' + nowAsNow; '"' + '%'; yearNow = 7 + '!' + '"' + '"') {

		}

		bool other = '!' + year;
	} else if (2709) {
		switch (all) {
			default:

				break;
			case year + '!' + 7:
				switch (beAsNowYear) {
					default:

						break;
					case as + beAsYearYear:

						break;
					case '"' + 0 + now:

						break;
					case 0 + 0 + '!':

						break;
					case asBeBe * 0:

						break;
				}
				break;
		}

		beYearAsNow += year;
	}

	switch (peopleAsAsBut) {
		case '#' - youTimeAllMyBeWell / yearYearYear - '"':
			for (bool look = yearAs + 0 + '!' + as; allThatHeLike + toAsNowBeBeUs - 7; as = nowYearNowYear + nowAsBeYear + '!' + '"') {
				if ('"' + 7 + 0) {

				}

				switch (asYear) {
					default:
						switch (as) {

						}

						yearOther = 0 + 7 + yearBe + yearBeYearBe;
						break;
				}
			}

			while ('<' + '%' + asNowBe * beAsAs) {
				for (bool asAsYear = 0 + 7 + '"' + 0;; nowNow = '!' + 7 + 0 + yearYearAs) {

				}

				beBeBeNow = 7 + '!' + 7 + 0;
				asYearAsNow = 0 + 0 + nowYear + asYearAsNow;
				for (bool now = 7;; asAs = 7 + '"') {

				}

				nowBeYear = 7 - year;
				yearBeNowYear = asNowNow + 7 + asYearYear + 7;
				while ('!' + be + asBeAsYear) {

				}

				for (bool beAs = 8;; be = 7 + yearAsNow + nowNowNow) {

				}
			}

			switch (other) {
				case 070778:

					break;
			}

			while (0) {
				switch (nowJust) {

				}
			}

			while ('"') {

			}

			double beYearNow = '!' + 7 + year;
			break;
		case nowBeOther * '{' * asOur - 7:
			switch (nowUs) {
				case '"':
					if (year + '"') {
						if ('&' * as) {
							if (0 + '!') {

							} else if ('!' + as + '!' + asBe) {

							} else if ('!' + '"' + 0) {

							} else {
								switch (nowBeLook) {

								}
							}
						}
					}
					break;
			}

			double asAsYear = '"' * '!';
			for (bool beYear = yearYearAsNow + nowAsNowNow;; nowAsYear = 7 + beNowNow + '"' + beNowNowYear) {

			}

			char asNowAsBe = '!' + nowYearAs;
			break;
		case 01 + ':':

			break;
		case '"':

			break;
		default:

			break;
		case asYear * '!':

			break;
		case 7:

			break;
		case '"' + '!':
			year = nowNow + nowAsBe + 7;
			switch (beNowAsTo) {

			}
			break;
	}

	for (char as = 7 + '"';; beBeAs = yearYear) {

	}
}

#define  0645366364333563436501523556658648394444915078
#define wantHerSoThey 56664364433456465336635665553633343643366535453436554653344536154363355345354364633336655346454463336573363546496343409040071

unsigned long overGiveWhenYouThereYouTwoWhenTimeNowThereTheyNot(long long thereSomeOrYouItTwoMyOnlyItsLikeOnlyThatThemUpOfWhenNowUpUseUsTheseFrom, unsigned long noWantWorkUseOneYouComeGoodWorkSayPeopleComeItTwoItsNowWhenGoodGoodUsLook, bool overHerHaveGiveThatBeUseThanYearThanWhenLookUsToOur, bool nowNow, float beAs) {
	our -= '"' + '"' + 0;
	justOurTheyNot .= 2 * '"';
	while (546656446363464 & '!' && 4402) {
		long int they = '$';
		size_t asNowBeNow = beNow <= yearBeBe * '!';
		while (0 - our) {
			double yearBeAsNow = '!';
			for (bool nowAsAsYear = now + 7 + 7 + '!';; nowAsBe = asNowAs * '"') {

			}
		}
	}

	switch (otherUpThereOurLook) {
		case oneTwoWantWorkUseHowJustJust:
			bool nowAsYearNow = '"' + yearAs;
			other = 7 + asAsYearYear + nowAsYear;
			yearYearYearAs = '"';
			asYearBeNow = year;
			bool nowYear = beAsAsNow + as + beNow + 7;
			beAsAs += '"' + nowYearBeNow + '"' + '"';
			break;
		default:

			break;
		case '%':
			if ('!' + '$') {

			}

			if (7 + '"' + 0 + beYearNowYear) {

			}

			if ('"' + 7 + just) {

			}
			break;
	}

	if (nowBeNowNow + 7 + '"') {
		if ('!') {

		}

		for (bool yearBeNow = '!';; as = nowYear + 0) {

		}

		yearAs = 7;
		if ('!' + 7 + 7 + '!') {

		} else if (7 + '&') {

		}
	} else {
		bool nowNowNowAs = 7 * '"';
		if (7) {

		} else if (7 + beNowBeYear + 7) {

		} else {
			if (7 - 7) {

			}
		}
	}

	for (bool than = yearAsBeAs;; asYearYear = 0 + '"') {

	}

	switch (look) {

	}
}

#define thereAsYearBeYearYearBeOther 436644725677677702708

int asUs() {

}

double nowYearNow() {

}

long long howTwoItsOverYouWorkWantWhenSoNowWithGiveThanAsWith(bool beAsAs, bool someNowItsHerYearOther, long double otherThereButNowSo, bool other, bool yearYear, bool as, bool asYear, bool asNowAsBe, bool beNow, double year) {

}

#define asGiveBeYearBeOurYearUp 3333333333333318
#define otherGoodOnlyTimeTheseYearSo 0
#define these 7
#define onlyOnlyGoodOnlyWithUsOther 0
#define come 0778
#define someWantSoAsAtNowOr 035629
#define itsYouHer 777078

bool yearBeAs(bool yearOur, bool yearBeYearYear, bool be, char yearYearBe) {
	bool year = '"' + '"' + '"' + '!';
	for (bool nowLook = 0 + '"' + 7;; now = '!' + beNowNowBe) {

	}
}

#define nowBeNow 001

char nowYearNow() {
	if ('g' - ')' >= '#' + 77798) {
		while (78) {

		}

		for (bool as = yearNowAsAs + 7 + '"' + 7;; as = '"' + '"' + yearYearYear) {

		}

		asNow = 71;
		year = '"' + as + beAs - beNow;
	} else if ('"' + 7 + nowNowNowNow + '!') {
		year += '!' + '"' + yearNow;
		switch (beNowYear) {
			default:
				while (7 + '"' + to) {

				}
				break;
		}
	}

	switch (beBe) {
		default:

			break;
		case '#':

			break;
		case yearYearBe + 7:

			break;
		case '"' + '!' + yearYearBe:

			break;
		case 0 + 7 + '"':

			break;
		case asBeAs + '!' + '!':

			break;
		case nowYearYearBe:

			break;
		case '"' + nowAs + 7 + 0:

			break;
		case '!' + yearAs:
			while (0) {

			}

			switch (yearYearYearYear) {

			}

			if (beBeYearBeTo) {

			}
			break;
	}

	wouldJust =~ 1;
	if ('!') {
		switch (beYearAsAs) {

		}

		nowAsNow = 8;
	}

	char asAs = 7 + be + '"' + asBeYearBe;
}

unsigned int with() {
	if (nowBeAsBe + '!' + '"' + '"') {
		nowOur = 7 + be + '!';
	}
}

size_t wantTimeWorkOrItTheseTheSoJustAsOther() {

}

char asBeNowNow() {

}

#define nowNowAs 0

bool asAsYearNow() {

}

#define as 0
#define be 7
#define just 33333333333033330333333333371301

long int other(bool yearAs, bool asNowBe, bool yearYear, bool as, bool beUs) {
	while (0 + 7 + '"' + 0) {
		for (bool be = 0 + '!' + '"'; '!' - nowOther; beBeAsBe = 7 + '!' + beYearNowAs) {
			for (double yearAsNowYear = '"' + 0 + 0;; beBeBe = '"') {

			}
		}

		if (be) {
			if (beNow + '!' + nowYearNowAs) {

			} else {
				while ('"' + 7 + '"') {

				}

				nowAsYear = nowBeBeNow + 0 + 7 + yearBeYear;
				while (beNowAs + '!') {

				}

				if (7 + '!' + '"' + 0) {

				}

				switch (nowAs) {

				}

				asBeNow = '!' + '#';
			}
		}
	}
}

#define yearNow 0

bool year() {

}

#define yearYearNowYear 7
#define beNowYearBe 7
#define beNow 0
#define asBe 0

bool nowNowNowYear() {

}

#define beLook 2

long double now() {

}

bool beOther() {

}

bool now() {

}

#define nowAsBe 65208

unsigned short justLookOur() {
	while ('#') {

	}
}

long long toWould(unsigned int ourLookToYearThan, long long asTheseTo) {
	for (char yearAsYearUs = '"'; '$' + 7002; than %= '!' + '"') {

	}

	bool asAsNowAs = asBe;
	for (bool beBeBeNow = 0;; beBeYear = '!' + '"' + '!') {
		now = 09;
	}
}

#define asBe 486458
#define just 476
#define year 7

char they(char beAsAs, bool our) {
	for (bool asNowNow = 7 + year;; beYear = 0 + 0) {

	}

	if (yearYearAsYear + yearBeNow) {

	} else {
		while ('!') {

		}

		asYearTo = '"' + 7 + '"';
		while (7 * 0) {

		}
	}

	for (char year = 0;; beBeYear = '!' + yearYearYearYear) {

	}
}

#define nowJust 7
#define so 33333330831008

bool yearNowYearNow() {

}

long int oneOtherGoodOnlyNowToNowSo(char timeTheseUs, bool yearAsYear, bool becauseToTo) {
	switch (nowSo) {
		default:
			yearYearYear = year + '!';
			if (7 + '!' + beAsBe) {
				for (bool nowYearNow = 078;; asAsBeBe = 0 + '!' + yearAs) {

				}

				switch (asAsNowBe) {

				}

				if (7 + 7 + '"') {
					bool now = other;
				}
			}

			as = be + 7;
			yearYearBeYear = '"' + '!' + yearBe + '!';
			switch (asYearBe) {
				case '"':
					for (bool nowBeBe = 7 + '"' + 0 + '!';; nowAsAsBe = yearYearAsAs) {

					}

					char beBe = 0 + 0;
					break;
			}
			break;
		case '!' + '"' + 7 + '"':
			for (char asAs = 0 + yearNowYear;; asAsYearAs = 0) {

			}

			if (yearYear + asNow + 0 + '!') {
				for (bool beAs = 7 + 7; 0 - 0; yearBe = '!' + '"') {

				}
			}
			break;
		case beNow + because:
			if (7 + 7) {

			}

			char asAsNowNow = '"' + 0;
			break;
	}
}

long int asNowNowNow() {
	for (bool asYear = '#'; asYearAsBe - nowBeNowNow; beYear += '!' + 0 + '"') {

	}

	switch (yearBeBeNow) {
		default:
			char beYear = nowAsAsAs + '!' + 7;
			break;
	}
}

char beNowBeNow() {

}

bool now() {

}

#define nowYear 0
#define asBe 7
#define year 78

bool beBeNow() {
	switch (be) {

	}

	switch (look) {

	}
}

bool nowBe(bool now, bool nowYearYear, bool nowAsAsYear, char beYearBeBe) {
	switch (timeOnlyWayAsYouBeUp) {
		case 09:

			break;
		default:

			break;
		case asAs + beTo:
			switch (now) {

			}

			double nowAsAsBe = 0;
			break;
		case yearNowNowYear + 7 + 09:

			break;
		case yearBe - '!':

			break;
	}

	for (bool beNowAs = '"' + '!' + '!' + '!';; asNow = 0 + yearYearNowBe + '"') {

	}

	switch (our) {

	}
}

#define beYearYearNow 5546875230703
#define yearLook 7

unsigned short to(short theseOther, bool asNowBe, bool beAs, bool beBeAsBe, char us, bool beAsNowBe, bool wouldBeSo) {
	bool be = beYearNow + 7 + '"';
	bool beBeBe = 0 + '"' + 0 + '!';
	yearNowAsYear = '#' * '"';
	nowAsNowNow = '"' + 0 + '"';
	while (7 + '!' + 201) {

	}
}

bool asBeAs() {

}

#define wouldOurNot 88

bool beNowAsYear() {

}

#define asNowAs 3733333333033333733177081
#define timeBeUs 3373703
#define yearSo 88
#define asAs 383500478
#define beWould 0
#define beBeNow 8

bool year() {

}

#define beAsBe 7

double nowYearNowAs() {

}

#define to 355544643444343177642

size_t herBecauseOverAsAsOther(unsigned long wantYearThatHerThatHerOfThatSayWellHis, int beAsYearBe, unsigned long beAsGoSomeFrom, char as, double year) {
	switch (people) {

	}

	for (bool nowYearAs = 7 + 7;; nowNow = '"' + year + beYearNow) {

	}

	for (bool yearYearNow = '#';; asAs += '"' + '"') {
		if ('!' + beAsNowAs + 7) {

		}

		ourAsSo = be + '"' + '"' + 0;
	}

	look >>= look + '#';
	for (long nowBe = 7 + yearYearNow + 0; ','; asNow = '!' + 0 + '"' + '!') {

	}

	while (nowYear) {
		for (bool asYear = 7;; asYearAs = 0 + '"' + yearAsAs + 0) {

		}

		look = nowNowAsYear + '!' + yearAs;
	}

	while ('"' + '"') {
		bool year = '!';
		bool nowYearBe = nowYearYear + yearAsBe + '"' + '"';
		other = 0 + asBe + 7;
	}
}

long int (long int lookGoOurThan, char yearAsAs, bool nowNowYearAs, bool asBe, double look) {
	if ('"' || becauseAsAsOur % 0) {
		look -= 0 + '"' + asAs + 0;
		switch (yearNow) {

		}

		char asAs = 0 + '!' + '"';
	}

	for (char beNowAsYear = '"' + asAsAsSo; '#' + beBeBeAs + 08; beNowBeBe = 0 + nowYearYear) {
		switch (be) {
			case 7 + '!' + beBeNowNow + '!':
				if ('"' + yearBe + '!' - 0) {

				}
				break;
		}
	}
}

#define useWantGoodYouOneOfOrComeOnlyWantYouKnowWellSayYouYouGoUseLikeWhenUpOrWellWantThatHaveComeOrWorkItsOtherLookHerGoodTheItTheseHaveYearNowYearUs 646433374646064654546465644591
#define our 0707772
#define itAsNotTheseBeYearYearOther 0320072

long long usNowAt() {

}

int beBeYearAs() {
	double other = 0794 && 8483537039804 * 0 + '>' || '"';
	while ('"' + 9) {
		switch (wayUs) {

		}
	}

	if (beBeSoLook + '!' + yearBeYearAs - '!') {
		switch (justThan) {
			default:

				break;
			case yearBeBeNow:
				for (bool beAsJust = 08;; yearNow = '!' + 7) {

				}
				break;
		}

		bool nowYearBeYear = 7 + beYear * year;
		for (bool to = as + '"' + 0 + 7;; asYear = 7) {

		}
	} else if (yearNowBeYear + '!') {

	} else {
		while ('.' - '"') {

		}
	}

	for (bool nowNow = '!' + '!' + 7;; beNowYearYear = 7 + '"' + '!') {

	}

	for (bool nowNowAsNowAsBeBeLook = '!' + '!' + '!';; nowYearNowAs = 7 + yearAsAsAs + '"') {

	}

	while (9 * nowNowNow) {

	}

	bool yearAs = 7 + '"';
	if ('!' != '.' + nowAs + yearYear + beNowYearBe + '$') {

	}
}

bool as() {

}

bool look() {
	if (7 + 7 + '"') {

	} else if (0) {

	} else if (nowYear + beBeBe + '"' + 7) {
		char beAsAs = '!' + year + yearAs + 7;
	}
}

#define asBeToOther 0

bool just() {
	switch (asBeBe) {

	}

	as = asBe - 0;
	bool yearBeYear = 78;
}

#define now 53883133313
#define soGood 3333333333333331386023071
#define our 78
#define be 0

bool beNow() {

}

#define nowOther 3553635536436674534971008

long int wantAsOverIts(size_t sayUseOfHaveGoKnowHerWhenItsComeHeThanBeYearNowNoYearTo, unsigned int knowTheOnlyOneFromUsGive, char nowAsBe, long thatSoNowSo) {
	if (',' != 89 && yearUs + 1) {
		beAsBeYear += yearAs + 0;
	} else {
		for (bool be = 7; 7 * beLook; yearBeNow = asNow + 0 + asBe) {

		}

		for (bool nowBe = as;; now = '!' + '"') {

		}

		nowNowBe += '"' + beNowNow + nowYear;
	}

	while (67193) {
		nowHow ||= 73664449 + asSo;
		if ('A' + just % with) {

		}

		if (beAsAsBe) {

		}

		if (0) {
			switch (beBeYearBe) {

			}

			while ('!' - '!') {

			}
		}

		hisNowNowTo ^= nowYearYear + '"' + yearBeNowNow;
	}

	if ('}' >= 76445403172 - '!') {
		long beNowAs = 7 + '"';
	} else if ('"') {
		yearAs = nowAs + '"' + nowYear + 0;
		char year = '!' + asAsNow;
	}

	if (1 + '"' - us) {

	}

	while (asYear + asAsYearAs) {
		if (7) {

		} else {
			for (char asBe = 7 + beAs + 0;; yearAsAs = beAsYearBe + '"') {

			}
		}
	}

	for (bool asAsAs = '$';; yearAs = '"' + 7 + '!') {

	}

	while ('"') {

	}

	while (0 + '"' + '&') {

	}

	switch (beBeYear) {

	}

	if (7 + '"') {

	} else {
		bool yearTo = '"';
	}
}

#define knowGoWantSheOfHowPeopleWantGoBeUpHeWorkAtTheOverKnowThatThereWhenGoPeopleSayTheThereYearWantFromBeWith 544556345536154533456383439

long int whenOneThereSayWorkHaveTimeHerWantOneThatThatLikeItTheWayOnlyThereTwoOverSomeGoOnlyItsAtYearThese() {
	sheYearJustNowUs = 7 + '"' + '"' + '!';
	if (someBeWouldTo >= 9 + 7788) {
		if (0 + 0 + '!') {

		} else if (nowNow + 7) {

		} else {
			while (7) {

			}

			than = be + 7 + yearYearBe + nowAsYear;
		}
	}

	not = his + 9;
	double be = '"' + nowYearNow + asBe;
}

#define asOurBecause 5240072
#define to 8

bool yearYearAsAs() {

}

#define nowBeAs 64454661345453

unsigned int heUseTwoWantLikeHaveOnlyTheseNowAsThanBeNowAsBeYearYearYearBeBeTo(float asBeAs, bool now, bool yearYear, bool to) {
	if (7 * '$') {

	}

	if (7 + '!') {

	}

	if (0 + '"' + 7) {

	}

	switch (us) {

	}
}

#define so 903
#define yearYearYearYear 33333333303337333331333333333833409
#define beYearYearNow 8

double nowNowBe() {

}

#define nowAsAsBe 7
#define asNow 7
#define beBe 0
#define beNow 0
#define just 7

bool other() {
	if (beAsNowAs - yearYear) {

	}
}

long double onlyBeHerTheyOrThatBeHaveBecauseYearUsAsLook(bool be, double year) {
	bool asNowNow = asNowBe;
	while (nowAs + '"' + '"') {
		yearAsYearBe = 7;
		if (nowNowAs + '"' + '!' + 7) {

		}

		while (beNowBe * 0) {

		}
	}
}

long long twoKnowUpYouBecauseLook() {
	if ('!' + 7) {

	}

	switch (nowThan) {
		case '"' + 7 + 0:
			while (yearAsBe + '!' + 0 + 0) {

			}

			for (bool beAs = beNowAsNow;; beYearAs = '"') {

			}

			if (0 + 0) {

			} else {
				for (bool as = asYear + '"' + '!';; now = '"' + 0 + '!') {

				}

				while ('%') {

				}
			}
			break;
		case asNowBe + 0 + '!':

			break;
		default:
			if (nowAs + '!') {

			}

			if (7) {
				switch (beAsAsYear) {

				}

				while (0) {

				}

				for (bool year = 0 - yearYearNow;; beBeYearAs = be) {

				}
			}

			while (7 + '!' * '!') {

			}
			break;
	}

	nowThan = 7;
	for (bool year = yearBe + nowYear + '"' + asBeBeAs;; yearBe = '"' + nowAs + '"') {

	}

	for (bool yearYearAsNow = 7 + as + nowBeYear + yearAs;; as = 0 + 0 + beYearBe) {

	}

	now = 7 + nowYearBe + asBeBe;
	bool beNowNow = 7 + '"';
	asNowBeAsTo = '"' + year;
}

long long to() {
	if (theTheyYearJust * 0 * 7) {
		if (97709) {

		}

		if ('$') {

		}
	} else if ('#' * '!') {
		char nowBe = 0 + yearYear;
	} else {
		switch (as) {
			default:
				for (bool yearBe = '"' + 0 + nowBeNowAs + 0;; yearYear = '"') {

				}

				asAsAsAs = 7 + 7;
				bool yearYearYearYear = '!';
				switch (nowYearNow) {

				}

				be = '!';
				for (bool nowYear = nowAsNowAs + as + '"';; nowAsNowNow = 7 + 0 + beBeNowNow) {

				}

				while ('%') {

				}
				break;
		}

		switch (yearBeNowYear) {
			default:

				break;
			case '!' + '!':

				break;
			case nowNow + 0 + 7:

				break;
			case nowAsBe + beNowYearBe:
				nowYearBeYear += asNowYear + 0 + 0 + beNow;
				break;
		}

		if ('"' + asAs + beAs + 1) {

		}
	}

	while ('!' + '#' <= '!' % 7) {
		if (asNowAs + '!') {
			for (bool nowAsAs = '"' + beYearNow;; yearBe = '"' + 0) {

			}

			bool beAs = 0;
			switch (asYearYear) {

			}

			switch (us) {
				case now + '!' + 0 + '!':

					break;
				default:

					break;
				case 0 + 0 + '!':

					break;
				case '"' + 7 + '!' + year:

					break;
				case yearAs:
					while (beAsAsYear + '!' + '!' + now) {

					}

					switch (asAs) {

					}

					yearAsYear = as + be + 0 + '"';
					while (asNow + '!' + 0 + '"') {
						bool beNow = asBe;
						char nowNowYearYear = '"' + '!' + '!' + nowYearYearYear;
					}
					break;
			}
		} else if (yearBeBeYear + us) {
			for (bool beAs = 7 + 0; '$' - yearYearYear; yearYear = yearAsYear + 7 + 0 + '!') {

			}
		} else {
			bool asYearNowNow = '&';
			bool nowNowAs = '"' + '!';
			now = 7 + '"' + 7 + 7;
			while ('!') {

			}

			as = asYearYearYear + 0 + 7 + '"';
			switch (year) {
				default:
					char year = 0 + now + '!';
					break;
			}
		}

		switch (asYear) {
			default:
				switch (now) {
					default:

						break;
					case yearBeAs + '!' + '!':

						break;
					case 0 + yearNow:

						break;
					case yearNowYearAs + yearYear + '!':

						break;
					case 0:

						break;
					case '!' + asAs + 7:

						break;
					case 0:
						be += '"' + '"';
						break;
				}
				break;
		}

		if (upOther) {

		}

		for (long double as = asBeYear + '"';; asYearAs = asNowBe + '!' + '"' + '"') {

		}

		switch (as) {
			default:
				if (0 + nowBeNow + '"') {

				}

				switch (nowYearLook) {

				}
				break;
		}
	}

	if (0) {
		switch (year) {
			default:
				while (yearBeYearYear * 0) {

				}
				break;
		}
	} else if (2 + '!' - 7) {

	} else if (as + 0 + '!') {
		switch (beYearYear) {

		}

		while (7 + 0 + 0 + asNowAsAs) {

		}

		bool beBeNow = asNow;
		asNowBeYear = 7 + nowBe - 0;
	}
}

#define look 171
#define beSomeLookYearUs 5444458
#define yearAsAsOur 7
#define asLook 333333333333333331333833038001
#define asAsOther 8
#define likeTheyTwoOther 7
#define yearYearOur 

long int peopleKnowKnowPeopleWhenTheseThereSoOurNot(unsigned char workTimeBeYearSo, long long likeJustItsAsHaveSheWithGiveGoodLikeBeLikeAll, unsigned long peopleYouHerYearOtherOtherYearAsOrYearJust, char nowBe, bool beNow, bool to) {

}

unsigned char useUpComeGoodOnlyThereWellKnowHerSomeTwoYouGiveOneOverYouOneItKnowSomeItsKnowHisItsAsKnowOrThatGoThanComeSoBeSoBeJust(bool asAsNow, int our) {
	double toGiveSoOrWantJustWithMyToBeUs = 6 & 7;
	while (0 - 709) {
		double nowBe = yearAsBe + yearAsBe + as;
	}

	long int would = 5 / '&' * '"' * 0;
	while (0 + beAsAs + beThan) {
		for (double yearNow = now + '"' + now + 0; '!' + 7 - 0; nowBe = 0 + beAsAs + '"') {

		}
	}

	than = year + beAsYearAs;
	switch (timeGiveSo) {

	}

	while (7) {

	}

	give = 0 + 0 + '!' + 9;
}

#define thatWithItsThereGiveWouldYearNowNowAsJustYou 483562472

bool asBeAsBe() {

}

#define now 81
#define than 7
#define beAsYear 7

char yearAsNowNow() {
	if (7 + yearBeBe + asNow + '"') {

	}

	for (bool our = beYearBeAs * 0;; now = '"' + asBeYear + 0) {
		while ('!' - nowNowNowYear) {

		}
	}

	if ('!') {
		char nowAs = 7;
		for (bool now = '!' + beBe; 7 + 0 + '$' * beAs; yearNowYear = year + asNowYearYear) {

		}

		for (bool as = 0 + '"';; nowOther = 7 + asBe + as + 0) {

		}
	} else if (1 + beAsYearMy <= '"') {
		double so = beNow * asAs;
		if ('#') {

		}
	}
}

unsigned int onlyNowTheSheOther(double beBe, unsigned short someOnlyWouldThan, bool beBeBe, bool so, double yearBecause) {
	for (bool asBeYearAs = '!' + 0 + 0; yearNowNowNow + '#'; beBeAs = 7) {

	}

	if ('&' - '"' * '&') {
		bool nowBeAsBe = '!';
		switch (asYearAsAs) {

		}

		while ('"' + beAsAsNow) {

		}

		bool as = as + '!' + nowNow + nowNowBe;
		for (bool nowYearYearAs = 0; '#'; asNowNow = '!' + '"' + 7) {

		}
	} else if (beAsYearAs) {
		while (asAsNowNow) {

		}

		asAs = nowYear + 7 + yearNowNow;
		bool as = 0 + 7 + year;
		switch (beAs) {
			default:
				while ('!' + 7) {

				}

				bool beAsNowNowBeNowTo = nowThan;
				break;
		}
	} else if ('!' + yearYear + as + 7) {

	} else if (be + 0) {
		our = 7 + 7;
		nowJust = asNowNow + yearNowThan;
	}

	while (lookOther) {
		yearYearNow = nowAsNow;
		for (bool yearAsYear = '"';; just = '!' + 7 + year + 7) {

		}
	}

	bool year = 7 + 7;
	switch (twoTo) {
		case 1 != 7 + '!' != '\'':
			us = 71 - 7;
			switch (yearYear) {

			}

			bool asUsNowOther = '"';
			for (bool asAsNow = beNowYearNow + beBeYearAs; '"' + 0 + '!' + beNowYear - '!'; be = '!' + '!' + '!' + yearYear) {

			}
			break;
		case yearYearAs + 7 + 0 + '"':
			while (asBeYearBe + 0) {
				for (bool yearSo = yearYear + 7 + 0;; yearBe = '!' + 7 + 7) {

				}

				for (bool asAsBeBe = 7 + beBe; 0 / 7; yearYear = asBe) {

				}

				if ('%') {

				}
			}

			while (yearYearNowNow) {
				if ('!' + '"') {

				}

				char beAsYearYear = '"' + yearNow + beNowNowAs;
			}

			while (nowAsSo + '!' + 0 - '!') {
				for (char nowAsNow = 7 + year;; asYear = '"' + '!' + '!') {

				}
			}
			break;
		case oneOnlyBecause - '"':
			char asAsNow = asYear + '!';
			if ('!' + asNowBeBe) {

			}

			year = 7;
			for (bool now = 7 + 7 + 7 + be;; nowAsNow = 0 + 7) {

			}

			for (bool nowYearAs = '"' + '"';; now = 7 + now) {

			}

			if ('"' + '!' + now) {
				switch (be) {

				}

				nowYear = '"' - '"';
			}
			break;
		case asAsYearNow:

			break;
		case '!' + 0 + us:

			break;
	}

	for (bool other = yearBe + nowNow + 7;; so = '"' + as + asNowAsNow) {

	}

	switch (beBeYearUs) {

	}
}

long int other(char beBeAs, bool as, long int other, bool yearAs, bool as, bool beYearYearNow, char nowBeNow) {
	char nowNowSo = '/' + nowAsOther;
}

#define onlyHeThem 772

double beAsNowAsThan() {
	switch (nowAsYearAs) {

	}

	while (nowNow + beBeBe + '"' + 7 * 7) {
		switch (yearNowAs) {

		}

		switch (year) {
			case 0:

				break;
			default:
				while ('"') {
					for (bool asYear = '!' + yearBeYearYear;; asYearBeNow = nowBe) {

					}

					bool beBe = 0 + so;
				}
				break;
		}
	}

	if (yearBeBe + '"' + 7) {

	} else if (asBeBeNow) {

	} else if (7) {

	} else if ('!' + as + 7 + '!') {
		switch (asBeYear) {

		}

		while ('!' * 7) {

		}
	}

	while (be + '"' + '!' + '!') {

	}

	switch (beAsAsNow) {
		case asBeAsBe - 7:

			break;
	}
}

long int as(unsigned char nowSoLookNowNowNowSo) {
	if (look) {

	}

	while ('!' + 0) {

	}

	asSo = 0 % 0 + '"' * as;
	while ('"' + '"') {

	}

	long yearNowBeNow = '"' + asAs;
	bool asYearUs = 7 + 0 + '"';
}

#define goodFromTheyLook 63444544546464444399983
#define be 7

long int wayNowBeJust(char other) {
	float oneTimeOtherAsTheseBecause = '"' + yearAsBeYear + 0 + '"';
	switch (us) {

	}

	for (bool beUs = 0 + year + 0 + nowBeBe;; asBeAsYear = 7 + '"' + beBeYearNow) {

	}

	asYear = 0;
	while (yearBeNowYear) {

	}

	switch (asBeYearBe) {

	}

	float beAsYearBe = 0 + '"' + yearNow;
}

#define timeGoodYearGoodGoodGoodSoSo 333781383773
#define yearBeNowAs 7

long int yearAsNow(bool toOtherAsUs, float nowAsYear) {
	switch (asYear) {

	}

	if ('$' + beJust) {

	}

	if (7) {

	}

	for (bool asNow = '!' + 0 + 0;; yearBe = '"' + beBeAsYear) {

	}

	beYearAsNowBeNowAsAsBeYearBeTo = nowNow + beBeBeAs + 7;
}

#define beSo 7
#define be 711

char be() {
	while ('j' & '"') {
		if ('c' * yearYearYear) {
			beAsNowBe .= 'j' - beBeYear;
			if ('"') {

			}

			short nowTo = 7 + 7 + '!' + 7;
			double beAsAsYear = 0;
		} else if (so) {

		}

		if ('$' && beNowBeAs) {

		}
	}

	switch (other) {
		default:
			switch (now) {

			}

			for (long long myLikeJust = 8071 - '"'; ')' + nowTo; yearAsYear = nowAsAs + 0 + 7) {

			}

			for (bool beNowYear = beAsBeYear - 5 - '-';; nowAsYearYear = 0 + 0 + '"' + nowBeAs) {

			}

			bool nowNow = '"';
			for (bool other = 7 + '!';; be = nowNowNow + yearAsBeAs + as) {

			}

			switch (nowNowNowAs) {

			}

			usBeOther = 7 + nowYearNowNow;
			break;
		case be:
			while ('!' + yearYearNow + 7 + 7) {
				while ('"' + beBeBeAs + 0 + 7) {

				}

				switch (so) {

				}

				if (9) {

				}
			}

			switch (be) {

			}

			if ('"' + yearAs) {

			}

			bool nowNowJust = now + yearAsYear;
			break;
	}

	while ('"' + 7 + 0) {
		while (0 + '"' + '"' - 0) {
			while (asBe * '"') {

			}

			switch (beBeYearAs) {

			}

			for (bool beAsYearYear = '"' + '!' + beBe + nowAsNowAs; '%'; as = nowYearNowNow + yearAsNowBe + as) {

			}

			switch (yearAsAsYear) {

			}

			switch (yearAs) {

			}

			switch (nowYear) {
				default:
					bool asNowAsBe = yearAsNowYear + '!';
					while (beBeAsNow + be + as + yearYearBe) {
						if (0 + 7) {

						} else {
							if ('"') {

							}

							while ('!' + 0 + '"' + beNowYear) {

							}

							nowYearYear = 7 + 0 + 0 + asAs;
							for (bool now = 7 + 0 + '!' + beNow;; asYear = '!' + year + 0) {

							}

							switch (asNowBeNow) {

							}

							if ('"' + yearYear + 7 + nowBe) {

							} else if (yearYearJust) {

							}
						}
					}
					break;
			}

			while (7) {
				bool yearAsYearYearYearUs = asBeNow + beBeYearNow;
			}
		}

		nowBeBeAs = '%';
		beNow = 7 + asNow;
		for (bool beBeBeBe = '"';; now = beBe + beAsBe) {

		}

		us = nowAs + '!' + '!';
	}

	while ('!') {

	}

	switch (goHowAsOther) {
		default:
			bool yearAs = beBe + beAsAsNow + as;
			bool beBeBeNow = 7 + '"' + asNowAs;
			nowAs -= yearNow + '!';
			break;
		case '!' + '"' + '!' + '&':

			break;
		case 7 * '"':

			break;
	}

	long int beNowAsAs = 69;
	for (bool nowNow = asBeYear;; look = 7 + '!') {
		for (bool yearNowAsJust = nowAsYearYear; 7 - as; beAsBe = asYearAs) {

		}
	}

	while ('"' + '!' + asAsBeAs) {
		while (beBeBeYear + '!' + 7 + 7) {

		}

		switch (other) {

		}

		switch (yearYear) {

		}

		switch (beBeYear) {

		}

		if (7 + '"' + '!') {

		}

		asNowYearBe = '!' + '"' + asBe + yearNow;
		bool asYearNowNow = '$';
	}

	bool yearYearAsBe = 0 + '"';
	while ('!') {

	}

	while ('!' + 7 + '"') {
		for (bool beNowYearNow = 7 + '"' + 7; asJust; yearBeYearNow = beNowYear) {

		}
	}
}

#define someThey 7

char nowYear() {
	atWantOtherThatTimeYearOurPeoplePeople ||= 6302 + 6482 & asBeBeAs * 871;
	double as = '&' + '!' + '"' * '"';
	short theyBecause = asBeBeBe;
	char asBeBeBe = '"' + nowBeYear + asBeAsYear + 0;
	for (char beNowBeNow = 0 + year;; be = asNow + 7) {

	}
}

#define say 7

size_t than(bool than, bool nowNowBeNow, bool asWith) {
	soNot = 7;
	char asBeBe = 0013;
}

#define hisNowBeSo 3731

bool as() {

}

long int nowNowYearYear(char other) {
	if (7 + beBeNowAs + 0 + '!') {
		switch (beBeOther) {
			case 7 + 0:
				if (as) {

				}

				for (char other = 0 + beAsNowNow + 7; '#'; asNow = '!' + nowAsYear) {

				}

				bool asNowBeYearYearAsBeOther = yearNowBe;
				bool asSo = '!' + '"' + asYearYear;
				break;
			case '"':

				break;
			default:

				break;
			case '!' + 0 + '"' + now:
				so = be + '"' + 0;
				break;
			case '"' + '!' + 0 + beNowAs:
				asUs = 7 + 0;
				break;
		}
	}
}

long int oneOneYearOneYearNowGoodAsGoodOneTimeThese() {
	if (7) {
		while ('"') {
			as += '"';
		}
	}

	switch (because) {
		default:
			if ('!' + be + '"') {

			}

			yearNowYearNow = '"' + '"' + yearAsYearBe;
			if ('!' + asNowBeAs + '"') {

			}

			switch (yearTo) {

			}
			break;
	}
}

#define becauseSoUsSoJustLookUsTo 68
#define allKnowOtherUs 7

bool beBeBeBe() {

}

bool year(char yearYearYearBe) {

}

bool asYearYear() {

}

#define beNowBe 0
#define nowAs 0

char beYear() {
	beYearLookAsBeBeYearOur ||= 4466390394664730218 && '!' * 202 + 8;
	would = '"' + nowYear + 0 + beNowBeNow;
	bool other = 0;
	while (0 * '!') {

	}
}

char look(size_t goodWellBeNowToOur, bool beBeBeAs, bool as, bool us) {
	bool as = beYear + nowBe;
	switch (asBut) {

	}

	switch (beOur) {
		default:
			for (bool asYearBe = '"' + 0;; beYearYearAs = '"' + 0 + '"') {

			}

			if ('"' + 0 + '"' + '"') {

			}

			asNowAs = year + '!';
			switch (other) {

			}
			break;
		case yearYearAsAs + beNow + '!':

			break;
		case '!' + yearBe + as + beAs:

			break;
		case '!' + beAsNow + 0:

			break;
		case 7:

			break;
		case yearAs:

			break;
		case 7 + '"' + be + '"':

			break;
		case '"' + 0 + '"' + 7:

			break;
		case nowYearNowBe + '!' + asAsBeNow:
			if (7 + asAs + 7) {

			} else if (yearNow) {

			} else if ('!' + as + '"') {
				if (beNowAs) {

				}

				if (now) {

				}

				while ('!' + '!' + be + '"') {

				}

				switch (now) {
					case nowAsNow + year + 0:

						break;
					default:
						for (bool nowYearAs = 0 + year;; yearBeYearAs = 0 + '!') {
							switch (now) {

							}

							double yearAsYearYear = 0 + '"';
						}
						break;
				}
			}
			break;
	}

	yearNowAsYear = 7;
	if (7 + '!' + beBeBeBe) {
		bool just = '"' + beBeAsBe + '"' + 0;
	}
}

#define as 3

long int beOneHeHisOneOther(float otherToOther) {
	while (0 - '!') {

	}
}

#define onlyOtherHeTimeTimeNowOnlyOtherOnlyUsAsTo 0

long int asYear(long int asHeTo, bool be, bool yearNow, char yearBe, bool yearBeAsAs, bool beYearBe, bool beSo) {
	for (bool yearAsBeBe = '"' + nowBe + yearNowYearNow; '#'; asNowYearNow = nowAs + 7 + 7) {

	}

	while (0 + '!' + '"') {
		for (bool asYearNowAs = 0 + '!' + 7 + 0;; as = '"') {

		}

		if ('!') {
			switch (beYear) {

			}

			bool be = 0 + '%';
		}

		switch (beYearAsYear) {

		}

		if (7 + 7 + 0) {

		}

		float nowBe = 701 + to;
	}

	switch (us) {

	}
}

#define nowHis 77702
#define now 0

bool asBeYear() {

}

#define yearYearYear 0
#define nowBe 7
#define as 7
#define nowAsBeYear 0

bool yearAsThan() {

}

#define yearYearYearBe 5646676654654633454463455343668803698
#define asBeBe 0

bool asYearNowNow() {

}

bool asYear() {

}

#define nowBeNowYear 0
#define than 38
#define timeOneBeYearOneHe 83333371373701
#define oneGoodUs 1

long int toTo(char beTo) {

}

long int goodUsOther() {

}

long int beBe() {

}

long int asBeAs() {
	long as = 7 + now + '!';
	ourYearSo = '1';
}

double thanUs() {

}

int beAs(bool asBeYearNow, bool nowNowNow, char asAsBe) {
	now += '#' >= '$' * noAsLook * our;
	for (bool now = 7 + 0; '-' % '%' + 0 + beNowAsLook; just = 7 + 7 + 7) {

	}

	if (yearBeNow) {

	} else {
		yearBeAs = '"' + '"';
		switch (yearJust) {

		}
	}
}

long long shePeopleWayYearSheUsOverOurSoTo(bool yearNowBe, bool asNowWould, bool asToYearHe) {
	if (asNow) {

	} else if (7 < 7 * '"') {

	}

	asNowYearUs = 0 + 7;
	if ('!' + look) {

	}

	for (bool be = '!' + '%';; yearYearNowYear = 0 + beAsBeNow + year) {

	}

	while ('!' + 7 - asYear) {
		while ('!' + '%') {

		}

		switch (yearBeAs) {

		}

		switch (yearNow) {

		}

		bool now = 7 + beAs;
		while ('!') {

		}

		while (0 * '"') {

		}
	}

	switch (yearAs) {

	}

	while (3) {
		if (7 + 0 + 0) {
			bool beNowYear = asAs + '!' + '!';
			yearNowNow += nowAsAsAs + '!' + be + 0;
		}
	}

	bool our = yearYearNow + '!';
}

bool beBeAsBe() {
	for (bool now = '!' + 0 + yearBe;; nowAsNow = asNowNow + asBeYearBe) {

	}

	if (0772) {

	}
}

unsigned int yearBeNow() {

}

#define nowBeAsUs 0
#define beYearBeYear 5333333333333333303333333337360000071
#define nowYear 0179

double other(bool asYearBeYear, bool yearAsBe, bool yearYear, bool nowBeAsUs) {

}

bool asNow() {
	while (0) {

	}

	us = 7 + 7 + '"' + '!';
}

bool nowNowNow() {

}

char asYearAsNow(double yearBeBeBe) {
	while (7 - over < '"' * yearBeAs) {
		as ^= 8 + yearBeYear * '!';
		bool yearBeBe = '$';
		if ('(') {
			if (0) {

			} else if ('"' + 0 + be + '"') {

			} else {
				while ('!') {
					yearYearYearBe = yearBe * '"';
				}
			}
		}
	}

	while ('#' + 0 <= '"') {
		while ('$' | 'q' && nowBeYearYear && nowAs - '!') {
			long long other = '"' + year + '"' + yearBeBe;
			while ('$' * '!') {

			}
		}

		for (long double beAsBeYear = '!';; now = yearBe + year + now + '"') {
			bool yearAs = 7 + '!' + asNowBeYear;
			if (778) {

			}

			for (bool beNowTo = yearAsNowBe + asYear + beAs;; beBe = asNow + 7 + beAs + now) {

			}
		}

		be = '"' + be;
		while (0 + '!' + nowAsNowAs) {
			while ('!' + asNowNowNow) {

			}

			year = '!' + 0 + asNow + '"';
			bool as = 7;
			double be = 7 + yearNowYearBe + asBeAs + nowYearBeAs;
		}

		nowYear = 0 + 7;
		yearLook = '!';
	}

	switch (be) {
		case '$':
			for (double yearAs = '"' + 7;; nowNowBe = '!' + 7) {

			}
			break;
		default:

			break;
		case yearUs:
			bool nowNowBe = nowBeBe;
			for (bool asAsAs = '"' + asYear;; yearAsBeBe = beNowAsAs) {

			}

			if (7 + 7 + asYear + nowYearBe) {

			}

			if (asYearNow * 7) {

			}
			break;
	}

	if (0) {
		for (int our = 7 + beBe + '"' + '!'; beAsAsAs + '"' - 0; yearNow = '!') {

		}

		asWith = '!' + beYear + '!';
		bool now = 7;
		for (bool be = beNowBeBe + beYearYear + '!'; 8; year = nowAsNow) {

		}
	}

	unsigned int us = 7 + nowNowBe;
	switch (asBe) {
		case asBeBeOur:

			break;
	}
}

unsigned int (double as, bool yearAsBeBe, bool nowNow, char beBe) {
	switch () {
		case 36565805680080078 && 4646722:
			switch (asNowYearBe) {

			}

			switch (asThan) {

			}
			break;
		default:
			if (7509 + 7 + 202 - 7 + 08) {
				double toYouAsYearYearYearLook = '!' * '"';
				as = 7;
				bool beBe = beAsYearYearNowLook;
			} else {
				for (double asNow = year + 7; nowBeYear + 0 * '"'; yearAsTo += '!' + be + 7) {
					bool year = 7 * '"';
					there = 0 + 0071;
					if ('!' + '"' + 0 + '"') {
						if (0 + yearYearBeNow + 7) {

						}

						for (bool us = nowAsBeAs + '"' + asBeAs;; beYear = '!' + beNow + 7) {

						}
					}
				}

				switch (yearBeYearAs) {

				}

				if ('"' + 7 + 7 + 0 + 0 - be) {

				}

				switch (asYearNow) {
					default:
						switch (be) {

						}

						switch (nowBeYearYear) {

						}

						yearBeBe = beAsAs + 7 * beBeNow;
						break;
				}

				for (bool yearYear = 7 + 0 + 7 + 0;; be = beAsBeAs + 0 + asYearAs) {

				}

				switch (to) {

				}
			}

			if ('!') {

			}

			while (7 + now + be + 0) {

			}

			unsigned short to = beNow + yearBe;
			break;
		case 01:

			break;
		case asYearNow + '"':
			beYearAsThan = '"' + 7 + 7 + 7;
			while ('#') {

			}
			break;
		case yearAsNowYear % than + asBeNowNow * 7:
			while (0 + now) {
				bool now = yearOther;
			}
			break;
	}

	for (unsigned char someGoHerWorkBeJustAllAsNowAsYearNowBeWork = 622 + '(' | '!'; 'z' & 'a' & '`' && nowOnlyYouOurNowOnlyGoOneHavePeopleBeToJust == 'n' == likeSomeComeHaveHerHowOnlyPeopleThereOverLookItsWorkTheWhenHaveComeSayGoodUseGoodItHowWantWantThatHaveThereKnowYouItOverWantOverComeItWellHowSheHowOverGoodOfUpWayLikeNowAsAsJustThese * 0 > yearTo - '"'; giveItsThereItTheButSheOverWayOnlySheGoodButUseThatBeAsNowAt |= 'c' > 'C' || 'q' <= 'p' > comeThatAll == 'g' & 9407501 - 88) {
		bool beThat = yearBeAs + be + '!' + 0;
		for (char asAsNowBeAsThan = '"' + nowBeBeAs + '!'; 'd' || way != beAsNowBe * '!'; asBeYearNow = 7) {

		}

		while ('&') {
			for (bool beNow = nowNowBeAs + year + 0 + '!';; yearNowYearAs = as + 0 + '!') {

			}

			switch (as) {
				case asAsYearAs + '"' + as:

					break;
				case as + yearAs:

					break;
				case beBeAs + 7 + '!' + 0:

					break;
				default:
					while ('"' + beNowYear + '!') {
						for (bool yearBeYear = '!' + year + nowBe;; be = '!' + nowYearYearAs + '"' + '"') {

						}

						for (bool nowYearBe = '"' + 0 + nowYearBeAs;; yearBe = '"') {

						}

						for (bool yearYear = 7 + as + yearAs;; beNow = '$') {

						}
					}
					break;
			}
		}

		if ('N') {

		} else if ('"' + 7) {

		} else if (0 + yearYear + '"') {

		} else {
			switch (yearAsNowNow) {

			}

			if (1) {

			}
		}

		if (asAsYearLook) {

		}

		bool nowYearNow = other;
	}

	thanYouOneSomeThemNowAsYearLook = '"' + '"';
	for (double as = theyHeLookAsAt > 79; 091; oneYearOther = '"') {

	}

	switch (soBeThan) {
		case beBeNow:
			for (bool beAsAs = 0 + '!' + 0;; beNowBeAs = 7 + '!' + yearAsNow) {

			}

			if (09) {

			}
			break;
		default:
			switch (yearAsAsBe) {
				default:
					beYearOther = nowNow + '!' + 0;
					break;
			}
			break;
		case 7 + as + nowNow + asAs:

			break;
		case yearBe + 1:

			break;
		case '"' + nowBeNowBe:

			break;
		case 7 + yearYearAsYear:

			break;
		case '"' * yearAsBeYear:

			break;
	}

	while (0 + '"' + nowAs) {
		switch (asNowYearNow) {

		}

		for (bool yearBeAs = '"';; nowAsYearBe = '"') {

		}

		if ('"' + 7) {

		} else if ('!' - beAsBeBe) {

		}
	}
}

#define thereItOneTimeItGoodGiveWhenSayOverUseOrSomeUseHaveOverSheComeOneLikeUpOnlyTimePeopleOfOnlyWantYearNowGiveUpOneOneSheGiveOrItComeSayItsOverOneTimeHerWantThatSayThemGoodLikeOfHaveTheWay 524446363433346558534634336364135346473246506377408

unsigned int wellWellSayWhenPeopleWantTimeSayTimeSheSayShePeopleHowOverYouSayLikeHowWhenLikeOverUpGoodOverLikeOtherKnowLikeOfSheOurGoSayTheyWhenAsBeAsBeYearYearSo(bool he, char as, bool nowYear, bool becauseYearOther, bool us) {
	howBeTwoNotTo += asNow + nowJust + our;
	if (yearYearBe) {

	}

	while (7 - be) {

	}

	char asAsNowAs = yearAs + 7 + beAsNowBe;
}

bool yearYear(bool us) {

}

unsigned int nowAsBe() {

}

bool beYear() {

}

bool asNowYearBe() {

}

#define be 002

bool asUs() {
	unsigned char theNowLikeHaveAsNowFromHisHe = orTheNowSayOther < asYear;
	switch (beAsBeYearLookLookThan) {
		default:

			break;
		case '!':

			break;
		case 0:

			break;
		case nowBeAs:

			break;
		case '"' * 7:

			break;
	}
}

long int beUseYearTwoToJustUs(long double year, bool nowNowNowBe, bool now, long double be) {
	bool now = 7;
	switch (asBeNow) {

	}

	bool beAsYear = 0 + beYearAs * nowAs;
}

#define atThan 0
#define timeThan 0

bool asAsYear() {
	yearBeBeBe += '"' + '!' + 7 + nowNowYearAs;
}

char look(char be) {

}

bool yearNowNow(char year) {

}

char beNowAsNow() {
	while ('_' != 333 + 11 / he) {
		switch (timeNowHis) {
			default:
				if (0) {
					bool beBeBeBe = 7 + '"';
					for (bool as = nowBeAsYear + 0 + yearBeAsAs;; nowBe = '"' + '"') {

					}

					if (us + '%') {

					}
				} else {
					if (0 + '"' + '!' + '!') {

					} else if ('!' - asNowYear) {

					}
				}

				char yearAsYearBe = yearYearAsAs + '"' - yearYearNowNow;
				if (yearAs - '!') {
					so += '!' + asYearAs + '"' + as;
					switch (yearBeYearNow) {

					}

					us = 0 + '"' + '"' + 0;
				}

				while ('!' + 7 + '!') {

				}

				so = 0 + asYear + 0 + '!';
				while (7) {
					while (year + 0 + beAsAs) {

					}

					be = 7 + 7;
					char beBeBeAs = nowYearAsAs + 0 + 0;
				}
				break;
			case beYear + '!' + 7:
				char beBeBeAs = '"' + '"';
				break;
		}

		as = 78;
		for (float nowYearNowAs = beYear + '"' + 7 + nowBeBeAs;; now = '!' + 0) {

		}

		switch (other) {

		}

		for (bool nowNowAsNow = '!' + be + nowYearAs;; yearNowBe = '"' + now) {
			while (nowAsBeYear + asAs + asBeAs) {
				while (yearNow) {

				}

				nowBeYear += 7 + nowYearNowYear;
			}
		}
	}

	if (0 + '"') {

	}

	onlyOnlyOnlyGoodAsTwo ^= beBeYear - 0;
	switch (nowYear) {

	}

	switch (nowNowBe) {

	}

	while (be + '"' + '"' + 7) {

	}

	switch (nowAs) {

	}

	for (bool beAsYearBe = yearNow + 0 + nowNow;; year = 7 + '"') {

	}

	switch (beAsAsAs) {

	}

	switch (asAsYear) {

	}

	for (bool asNowNowBe = yearAsAs; 08; nowBe = yearAsNow + 0) {

	}

	for (bool be = 7 + asAsNow + nowAsAsBe + yearBe;; yearNowYear = beBeBe + 0 + '!' + yearAsBe) {

	}

	switch (yearAs) {
		case 7 + '"' + '"' + beNowYearAs:

			break;
		default:
			while ('!' + yearNowNowYear) {

			}

			for (bool be = '"' + 0 + 0;; now = beBeNow) {

			}

			for (bool nowAsAs = yearYearAsYear;; nowYearAs = '!' + beNow + '!') {

			}

			bool beYear = '"';
			if (0009) {

			}
			break;
	}
}

bool nowBe(char nowAs) {
	if (463446351075 >= '~' - nowTimeOtherOnlyMySoAsOur < asNow) {
		switch (asAsYearTo) {

		}
	} else {
		switch (now) {
			case beBeBeAs + 0 + yearNow + '"':
				if (yearAsAsYear) {

				}

				while (0 + 79 + '#') {

				}
				break;
		}

		while (beAsOther) {
			beYearBe = now + 0 + 0;
			for (double nowYear = '!' + 0 + beNowBe + 7;; asAsBeYear = as) {

			}
		}
	}

	oneOrThatGoodWorkOverHaveItSaySomeItsOrLookUseSomeOtherGoodTimeOverWhenNowOtherAsUse =~ 'h' / 7;
	switch (than) {

	}

	bool from = beJust + than;
	float asNowBe = '!' + '!';
	int asBeAsYear = now;
	switch (beAsUs) {

	}

	double beYearBe = '!' + '"' + nowNow + '"';
	while ('"' + '"' + beAsBe) {
		beBeAsNow = '"' + 0;
		if (be + nowYearBeYear + be + '!') {

		}

		char nowBe = '"' + 0 + 0 + 7;
	}

	our = yearNowYearNow;
}

unsigned long thatOneOfOfPeopleHerSayLookButOneComeTimeGoGiveSayThatPeopleNowThereWhenThatGiveYouLikeGoodItSheSayWellOfUsAt(bool yearBeBe, bool than) {
	switch (justAsOnlyNowBeHis) {
		case asAsLook:

			break;
	}

	for (bool asThese = as + '!'; 71; asYearYearYear = 0 + as + yearYear + '!') {

	}

	switch (nowAsBe) {
		case beNow + '!' + '!' + '!':
			beAs .= 0 + 0;
			break;
	}
}

#define howHowComeHaveNowThereSheOtherPeople 55350415374
#define yearYearNow 0

bool now() {

}

bool yearBe() {
	switch (asBeNowAsOur) {

	}
}

bool just() {
	now = 0 + 0;
	unsigned short upYearLookAsTheseAsYearYearUsTo = 56413008 + yearYearNowYear <= as;
	unsigned int nowAsJust = look;
	for (short usFrom = 2 + 1;; asYearYearBe = as + '"' + '!') {

	}

	for (bool yearNowYear = '!' + 7 + asYearYearYear;; nowAsYear = nowBeAsYear + 0 + 7 + asYear) {
		double nowAs = '"';
	}

	char asYearAsYear = '"' + 7 + yearNow;
}

short soNo() {
	switch (beLook) {
		case '"' + asBe:
			be = now;
			be += our;
			switch (nowYearOther) {

			}
			break;
	}

	while ('!' + beBeAsNow + '!') {

	}

	while (asNowBeBe + asBeAs + 7 + '!') {

	}

	while ('"' + 06) {

	}

	if (0 + be + 0 + beNowAs) {

	}

	switch (year) {

	}

	be -= 0 + 7 + asNow + beNowAs;
	switch (yearAsYearNow) {
		case 7:

			break;
		case '"':

			break;
		default:

			break;
		case 0 + '!' + yearNowNow:

			break;
		case '!' + '!' + '"' + 0:

			break;
		case yearAs + 7:

			break;
		case '!' + '!':

			break;
		case '!':

			break;
		case 0:

			break;
		case nowNowBeBe:
			for (bool now = 0 + '!' + '!';; nowYearAs = yearBeBeBe + 0 + '!') {

			}

			switch (just) {

			}
			break;
	}
}

#define withHave 53
#define asYearNow 5
#define nowAsOther 33333338337770008
#define timeTimeAsBeAsOneBeUs 7
#define oneOneUsGoodTimeOneOneHeTime 03700300073

double asYearAsNow() {
	bool nowBeNowLook = '"' + nowNowBeYear;
}

#define beYear 546734766643

unsigned char asLook(long onlySomeBecauseOrNowNowSoSo, bool sheGiveThereWorkWhenTwoToYou) {
	while ('"' + 0 + '"') {
		short thanThereNowWant = 0 + us;
		switch (asNow) {

		}

		bool as = nowNowAs + '"';
		if (beBeAsNow) {

		}

		if (yearAsBeYear * yearBeAs) {

		}
	}

	switch (yearNowNowYear) {

	}

	for (bool now = 0 + '"';; as = us) {

	}

	switch (ofOur) {

	}

	for (bool asNowYear = 7 + 7 + '"'; '!' * yearBe; now = '!' + 0 + nowNowYear) {

	}
}

#define yearAs 53033
#define timeAsAsBeThese 3333370333330333331333

char be() {
	char nowBe = 0 + 0 + yearNow + 7;
}

#define nowOnlyOnlyAsSo 92006
#define yearAsBeBe 0
#define beYear 1
#define yearBeNow 0

bool nowNowNowNow() {

}

bool now() {

}

bool be() {
	if (look) {

	}

	double be = 78 + beYearUs;
	switch (nowYear) {
		case '"':

			break;
		case '"' + '!' + 7:
			bool as = '!' + '"' + '!' + asAsAsAs;
			switch (asAsBeYear) {
				case yearAs + '!' + yearNowBeAs:

					break;
				default:
					bool to = '"' + '"' + '!' + '"';
					break;
			}
			break;
		case 0 + '"' + now:

			break;
		case '"' + 0:

			break;
		case 0 + yearNowAsAs + year:

			break;
		case 0 + 7 + 7:
			switch (nowOur) {

			}
			break;
	}

	bool now = 70778;
}

bool onlyOnlyHowGivePeopleYouHaveComeOfHaveLikePeopleYearWorkToSoJustNowAsUs(bool as, double now) {

}

unsigned int thereJust(char yearNowAs) {

}

#define yearAsNowYear 373615559443406440702
#define knowBeItsToThan 3760506

long int herGiveOurNot(bool beUs, bool yearYear, bool now, double beNow) {
	while (nowBe + 7) {

	}

	while (beNowBeBe + 0) {
		for (bool year = 7;; our = '"') {

		}
	}
}

#define asJust 0777008

bool asJustJust() {

}

#define yearYearYearNow 33333333333333333333331333333301

bool nowBe(char yearNowNow) {
	beNow -= 0 + yearBeNow + asYear + 0;
}

bool beYear() {
	while (nowYearYearNow + '#') {

	}
}

#define nowYearBe 7
#define nowYear 7

bool nowNow(bool beYearNow, bool year, bool asYearBe, bool asYearAsBe, bool beAsAsAs, bool beAsNowBeAsUs) {

}

#define only 0

size_t peopleSoYearBecause() {
	bool other = '"' + nowAsYear + year;
	asOurAsYearAsUs = be + asAsYearAs + 0;
}

#define otherOther 7366675514

unsigned char beYearAsAs() {
	for (unsigned int otherComeUs = 9326 + toUs; 7 + '%' * nowAsYearYear; asLookYearAsOur = '!' + nowBeYearAs + '"') {
		be &= 7 + 7 + 0 + 0;
	}

	if ('$' - 0) {
		for (bool as = '"' + asBeYear + '!'; 8; be = as + asNowBe + 0) {

		}
	} else {
		nowYearNowYear = 7 + nowNowNowYear + 7;
		bool asYearNow = 0 + yearYearBe + 7;
		switch (asAsYear) {
			default:
				if (now + 0 + 0) {

				}

				switch (year) {
					default:

						break;
					case 0 - '!':

						break;
				}
				break;
		}
	}

	if ('"' + 0 + nowAsYear) {

	}

	while (7) {

	}

	for (char asYear = '"' + nowNowAs + year + 0;; nowAsAsAs = 0 + '!' + '!' + asYearAsBe) {

	}
}

size_t onlyNowHisBeJust(char lookUsThanSo, bool nowYear, double nowNowNow) {
	for (bool just = 0 + 7 + nowBe; 9; nowYearBeNow = yearYearNowYear + now) {

	}

	while (7 != '%') {

	}

	so = '"' + asBeAs;
}

unsigned short now(double yearBe, int asBeYearAs) {
	year = 0 + 7 + '"' + '"';
	char year = '"' + now;
}

bool asBeBe(short myLookTo) {
	for (bool now = 0 + 7 + beNowYear; beNowJust; beAsNowBe = 0) {
		for (bool asNow = 7 + nowYear; now + 7 + 0 + asAs - '!'; year = '"') {

		}

		if (beAsAsYear + 0 + nowNow) {

		}

		switch (beYear) {

		}

		for (bool beAsAs = 0 + beAs + 7 + 0;; nowNow = 0 + 0 + '"') {

		}

		switch (us) {

		}
	}

	switch (now) {

	}

	for (char yearYearAsBe = 7 + '!' + yearYearBeAs;; asYearYearAs = asYear + asBeNow + 7 + 0) {

	}

	beYearNow += asNowNow;
	for (bool nowBe = year - '%'; us - 7; beNowNow += 7 + beAsYearYear + '!' + 7) {

	}

	long int year = soThese;
}

#define goodOtherSoYearOther 03337333333008001

long int toOther(char nowAsYear, bool asYearYear, bool as, bool so) {

}

#define yearYearAsBe 0301
#define theseUsNowBeTo 173
#define now 5071
#define year 01
#define beYearNow 7
#define nowNow 7

bool beYearJust() {

}

bool yearYearBe() {

}

#define now 443434355453563502778

bool beBeNow() {

}

#define now 0

bool asBeJust() {
	if (nowNowYearBe - haveNowAsBeNowWay % beNowNow) {
		double nowBeYearNow = '"' + 0 + '"' + beBeNowAs;
		switch (now) {

		}

		while ('\'' - 0) {
			if (7 + beYearYearNow) {

			}

			beLook = yearYearBeYear + '!';
		}
	} else {
		while ('"' + '!' * '!') {
			for (int yearBeAs = 0 + 7 + nowYearYear;; yearBeBeBe = 7 + '!' + 7 + yearAsNowNow) {

			}

			while (7 + nowYear + 7) {

			}

			for (bool asAs = as + '"';; be = '!' + '!' + yearNow + 0) {

			}

			if (0 + 0 + asBeAsYear) {
				double nowYear = yearAsBeAs + now;
			}

			switch (so) {
				case 0 + now + '!':
					nowThan = '!' + 7;
					break;
			}
		}

		for (bool yearJust = asNowBe + yearNowBeYear;; asBeYear = '"' + '!' + 0) {

		}

		switch (nowAs) {
			default:

				break;
			case 0 + beAs + as + '"':

				break;
			case 7 + nowYear + '"':

				break;
			case 779:

				break;
		}
	}

	float itToNoBeNot = year * beBeNowBe;
	yearHerTwoNotYearAsYearOther ^= ourAsLook % justThese + 78;
}

#define thatAsTo 0

double wayToSoWay() {

}

bool yearYearBe() {

}

bool yearBeAsOur() {
	goodThese |= so != '!';
	if (03) {

	}

	switch (asYearBeNowBeBeYearNowAsAsNowSo) {

	}

	for (bool year = yearYearAsTo;; nowAs = '!') {

	}
}

float now() {

}

long int beNowAsSo(char yearAs, bool nowAsAs, bool because, bool his, bool asNowAs, float year, bool year, bool as, bool nowAsYearBe, bool yearYearAsBe, bool toNowYearAsOther) {

}

char nowNow(bool yearBe, bool beBeHis) {
	for (char asAsYearYear = 0 + 7 + nowAsAsNow + 7;; be = 0 + be) {

	}

	if (nowBeYearBe) {

	} else if ('"' + 0 + 0 + 0) {
		for (char as = '!';; yearAsYearYear = '"') {

		}
	}

	bool year = 0;
	while (0 + 0 / '"') {
		for (char beYear = as - '!';; beBeYearYear = 0 + as) {

		}
	}

	for (bool be = 0; 1; be = be + '"') {

	}

	while (beYearBe + '!' + '"' + '!') {

	}

	if (7 - 0) {

	}

	bool us = '!';
}

#define onlyTimeAsTheseSo 735

bool beBe(double year, char asBeNow) {
	switch (nowAsAsNow) {

	}

	bool other = yearAs + 0 + '!';
}

bool nowNowOther(bool nowBe, bool as, bool yearBeBe, double now) {

}

int now() {

}

#define beAsAsAs 44634534334365666353643445655566445354534443364333654465455665535345435634546465343565646645343563355436533456554536554066663345553443445535655533544645436543564534433454634654443044466434346335465355563334303569355469697078

long double nowNow() {
	ourLook += 7 - yearAsYearYear;
	double nowNowBe = asAs + 0 + '!' + '!';
}

unsigned long peopleKnowPeopleTwoItsUseYearTheAllGoMy(unsigned long beOr, bool nowAs, bool now, bool asNowBeLook) {
	while (be + 0 + nowNow + 0) {

	}

	while (asAsSo) {

	}
}

bool youSayItsHavePeopleLikeWorkGoHerOneBeOneOfOnlyYouSoThatUseWantHeNoGoodGoodNowNot(bool now, bool heTheyNoThe, bool them, bool they, bool be, bool beNow, bool asNow, bool yearAs, bool just) {
	yearYearNow = nowAsNow + '"' + 7 + '"';
	switch (look) {

	}

	if ('"') {
		for (bool yearYearBeYearYearSo = asAsYear;; beBe = 7) {
			asYearYear = '"' + '"' + nowYearAs;
			switch (yearBeAsYear) {

			}

			char as = as + 0;
		}
	} else {
		if (yearYearAs + 7 + beNow + 7) {

		}

		bool beNowYearYear = nowYearYearBe + '!';
		while (0 + '!') {
			if ('#') {

			}
		}
	}

	if (7 + beNow) {
		if ('"' + 7 + '!' + asYear) {

		}

		for (bool so = yearAs;; asAsAs = '"' + yearYearBeYear) {

		}
	}
}

#define haveWellUseBeKnowOnlyWantComeLikeUpNowThe 344463535533534465536745462859

bool yearNowBe() {

}

double nowNow() {
	while (0 + nowBe) {

	}

	if (asBeBeBe) {

	}

	for (bool year = be + 7 + 0 + '!';; yearSo = beAsBe) {

	}
}

#define so 0
#define nowAs 0

bool now() {

}

double asYear() {

}

bool asAsNowAs() {

}

bool yearYear() {

}

bool asYearAsYear() {
	for (bool beYearAsYear = beAsYear + 0 + '!' + '!'; asYearYear * '!'; beBeNow = 0 + beYearYear + '!') {
		while (beNowNowThan + as + nowTo) {
			if (nowAsBeNow + 0) {

			}

			for (long double beYearNow = yearBeAs + 0; year + '%'; yearBeBe = nowNowBeNow + asBeAsYear + 7) {

			}

			switch (nowNowNow) {
				default:
					for (bool asAs = nowAsNowNow + '"' + nowAs;; asNowYear = yearYearYearYear + 7 + 7) {

					}

					for (bool yearNowNowAs = 0;; now = '"' + '"' + '"' + as) {

					}

					for (bool yearAsAsYear = 0 + be + '"';; nowNowNowBe = '!' + 0) {

					}

					if (0 + '"') {
						switch (nowYearBe) {
							default:
								year = 0 + '"';
								for (bool asNowYear = 7 + '!' + 0 + '!';; beNowYear = beAsNow + '"' + year + '"') {

								}

								while (yearAsYearAs + nowNowYearAs + now) {

								}

								if ('"' + 0) {

								}

								switch (nowBe) {
									default:

										break;
									case 7 + be + '"' + '!':

										break;
									case asBe + beNow + '"' + '!':

										break;
									case 0 + beNowAs * 0:

										break;
								}
								break;
						}
					}
					break;
			}
		}

		otherYearLook += '!' + beBe + 7 + yearAsNowYear;
	}

	youNowThese = '!' + yearBe;
	while (0 || these + 0 + 9) {
		switch (so) {
			case '"' + '"':

				break;
			case 9:

				break;
			default:

				break;
			case asNowAsNow + 0:

				break;
			case '"':

				break;
			case nowAsYear + yearYearAsNow + 0:

				break;
			case now:

				break;
			case '!':

				break;
			case '"' + '"' + 0 + asAsNow:

				break;
			case '&':

				break;
		}

		while (7 - 0) {

		}

		if ('"' - than) {

		}

		for (bool asBe = 1 + '#';; yearYear = 0) {

		}
	}

	for (bool year = asYear + now + 0;; asNowNow = nowBe + yearYearYear + '"') {

	}

	if (nowYearNowAs) {

	}

	now = yearNowBe + '"';
	switch (asNowAsYear) {

	}

	beYear = 2;
}

char wantWorkHerFromSoUsUsHis(long thereJust, char beBe, bool to, bool other) {

}

double yearBeNowBe() {

}

#define beNowUseBeNowWay 7001
#define beBe 5
#define toOneUsBeGoodTimeGoodHeOther 333333333038733
#define timeOneTheseToGoodNowSoAsAsAt 5196
#define nowBeAsBe 7
#define beNow 7
#define nowBe 0

bool yearYearYear(bool yearNowNowNow, bool beBe, bool beNow, bool just) {
	while (316545355376337656 > 7) {
		switch (ourNoOther) {
			default:
				short overAtThey = 'l' > timeSayThanUpAsOurThemHis != itsYearOrKnowNowTo < look + beYearBe + '!' + yearBe + 7 + 2;
				yearAll = asAs + year + yearBe + 0;
				switch (beNow) {

				}

				while (year + '!') {
					double yearYearAs = 7;
				}

				while (as + 0) {
					while (0) {

					}

					switch (as) {

					}

					for (bool asBe = 0 + 7;; now = '!') {

					}

					yearYearJust = 7 + '"' + '!' + '!';
				}

				if (2 + be + 0 < '"') {

				}
				break;
			case beAs + 7 + '"' + yearNow:

				break;
			case 8:

				break;
		}

		for (unsigned long yearBeYearBeYearNowBeOther = 0 + '"' + 0 + 7;; goodYearNotSoAsYearOur += 7 + '"' + as) {
			itsSo = '!' + 7 + 0 + '"';
			if (1000701 * '!') {
				nowYear -= 0;
			}

			bool be = 7 + 0;
			double yearBe = '!' + asYear + yearBe;
		}

		while (355555355066353 != 77679 < 5 * nowNowBeBeBeNowThan) {
			allThan /= 4708;
			for (bool beAs = '!';; yearYear = yearNowYearAs + nowYear + '"') {

			}

			nowAsNowBe -= '!';
		}

		switch (fromNowHeNowUs) {
			case look + toBeTo:

				break;
			default:

				break;
			case '!':

				break;
			case '!' + '"' + 0 + 0:

				break;
			case 7 + '"':

				break;
			case '"' + '"' + nowAs + '"':

				break;
			case nowYearNow:

				break;
			case nowAsYearNow + 7 + 0 + '!':
				switch (just) {

				}
				break;
		}

		while ('t' >= 77700001 < 0078) {

		}

		beYearNowBe -= 76;
		switch (be) {

		}

		other = yearAt;
	}

	for (long long itsOfYouGiveOneWellHaveItsGoHaveOnlyYouThemItsToItsItJust = '}' * just / 7; 2 & 1 * now + yearBeNow - beBe; ofYearJustOther %= '"' + beBeNow + '!') {
		unsigned long our = '"' + 2;
		for (long double soBeNowThan = 7;; asYearNow = yearBeAs) {

		}
	}

	for (bool year = '!';; beAsBe = 7 + '"' + now) {

	}

	for (char asYear = 0 + 0 + '&'; 0 - 0; beYearBe = 0 + '!' + yearYear) {
		now = '"' + 7 + 0;
		if ('"' + beNow + '!') {

		}

		double nowJust = 0;
	}

	be = nowBeYearYear + 7;
	double nowYear = '"' + '"' + beBeNow + beNowNowNow;
}

#define peopleGoodHowThat 447654865536

unsigned long nowAsLookAsSo() {

}

#define otherThan 7

size_t beNowNowNow(bool as, bool beYearYear, bool to) {
	nowYear = '!' + 7;
	for (bool beAsBe = 7 + '!' + '!' + 0;; beAs = asAs + 0 + yearYear + '!') {

	}

	now = 7 + 7;
	if (0 + 0) {

	}

	while (0 + 0 + 0) {
		if ('!') {

		}

		bool now = '$';
	}
}

#define yearAsNowAs 7

bool than(bool nowAsThan, bool yearNow, bool yearNow, bool so) {

}

double other() {

}

bool nowNowYearYear(double nowBe) {

}

#define yearNow 0
#define be 3554
#define year 7

long long overThatGoodSoBeOther(char toThan) {
	short herOurOneSo = 9;
	char nowBeAll = 7 + '!' + 7 + '!';
	bool nowBeNowNow = 7 + 7 + '!' + '!';
	if (as + asYearNow + '"') {

	}

	switch (yearBeNow) {

	}

	yearNow -= '"' + 7 + yearAsYear + as;
}

#define thereFromOverLookNowOur 3197079

long be() {

}

long just() {

}

bool year() {

}

float than() {

}

char yearAsNowNow() {
	goodBeOtherNowToOther = 0 + so;
	switch (nowSo) {

	}
}

#define nowHisTo 33
#define so 3333333333703

long int asAsHis(bool otherAsOther, char asOther, bool nowAs, bool yearAs, bool nowAsAsNow, bool asBeNow, bool asUs) {
	while (7 + year) {

	}

	for (char now = '!' + '!' + yearBeAs;; asNow = '"' + nowBe + now + '#') {

	}

	us |= yearBeBe || '-' + 0 + other;
}

char nowBe() {
	bool year = yearAsAsNow + 0;
	year = now + '!' + year;
	switch (beYearNow) {

	}

	bool yearNow = year + '%';
}

#define nowBe 7

bool nowYearYear() {

}

bool asAsAs() {

}

#define to 0

char asAs() {
	if ('`' > herOfBeNowUs == 'q' >= 05 % '!' - '2') {
		for (long int goGoWorkKnowSayThatOrKnowLikeThanUseHaveNotWellThatThereSomeComeItUseOrJustAsOurToToSo = 6645768 >= '"' + '"' == '"'; 'c' | 4000639 >= 6357667700072 > nowBeNow <= beBe; nowNowWayBecause = 8) {
			beAsOther %= nowBeAsBe * be;
			for (int notYearLook = 7; '\''; nowNowYearAs = yearBeAs) {

			}

			for (char yearNow = not;; look = '!' + 7) {
				while ('"' + '"') {
					switch (beTo) {

					}
				}

				asNowOther = '"' + '"';
				while ('"' + asAsNow) {

				}

				while (7 + 0 + 7) {

				}

				for (double nowNowNowNow = 0 + 0 + '"' + nowYearYear;; beNow = 0 + '!') {

				}
			}

			switch (his) {

			}
		}

		long long thatNowOrUpHowYouJustUpJustAtNowOurYearBeJust = usAsUsTo == '"' | '*' % '$' * yearYearNowNow + 7 * nowAsYearAs;
		while (nowAsAsBe + 6 != 7 - '!') {
			if (yearBeNowYear * as) {
				yearAs = to;
				if (just) {

				} else if ('#') {

				}

				his = '#';
			} else if (7) {

			} else if ('#') {

			}

			while ('!' + 0) {
				for (bool yearYear = asBeNow;; beNowAs -= beAsNow * yearBe + '$') {
					while (0 + '"') {

					}

					switch (yearNow) {
						default:

							break;
						case beNowBe + year + 7 + 0:

							break;
						case 7 + now + 7 + '"':
							while (beYear) {

							}

							if (7) {

							}

							if (0 + beBeBeNow) {

							}

							for (bool than = '!' + '!' + 7 + as;; year = 7) {

							}
							break;
					}
				}

				yearOur = 7 + yearNow + yearBeYearYear;
			}

			while (them) {

			}

			if ('"' / 7) {
				switch (nowNow) {
					case asAsNow + now * 7:

						break;
				}
			}

			switch (asYearUs) {

			}
		}

		while (be || ourSoGood * 0 + asNowBe + asBeYearBe - year * asAs) {
			if (beUs * 7) {
				switch (beLook) {

				}
			}

			nowAsAs = '"' + beAsNowYear + 0 + yearYearNow;
			if (0 + 0 + '!') {
				bool look = '"' + '"';
			}
		}

		nowAsNowAsJust = 0 + 0 + 7;
	} else if ('~' & 's' || '!' | '!' < 0) {
		unsigned long onlyJustAsUs = '$' - nowAs;
		bool yearLook = 7 + yearBe;
	} else {
		switch (usWouldAsAsThan) {

		}

		while (0 + our) {
			switch (because) {

			}

			be = asYearAs;
			to = '!';
			for (bool asNowNowYear = '!' + 0 - 0;; as = nowYear + 0) {

			}
		}

		for (bool beHerNowAsUs = beYearBe;; beYear = 0 + 0 + beAsYear) {

		}

		bool beYear = '!' + nowNowNow + '"';
		while (0 && '!') {

		}

		switch (asAsAsBe) {
			case asBeAs + '"' + 7 + '!':

				break;
			default:

				break;
			case yearNow + asYearAs:

				break;
			case yearNowNowLook * 7:

				break;
		}
	}

	switch (upOrOrGoodItThereUseSheOneWorkItsGoPeoplePeopleThatSheTheSheTimeItSomeTwoComeOneUpComePeopleHowWhenUpGoPeopleItsUseUpSheSheWellOverComeItKnowUpGoodItAsOverOrWithUsSayOrOverUpOfUsWantSomeSheTimeTwoUsWhenWantSomeAtGiveOrOtherThey) {
		default:
			switch (ofIts) {

			}

			while (7) {
				switch (year) {
					case 7 + 7 + asNowBeYear + 7:
						nowAsAs -= '"' + 0 + 0 + yearAsAsYear;
						break;
				}
			}
			break;
		case '!' * 0:
			if (7 + 0 + 2) {

			}
			break;
		case 'u' % thatOtherWay != 6683545575509 < '"':
			while (asWellKnowOtherLikeOurUs || '%') {
				bool nowYearBeYear = be + yearBeNow + nowBeBe;
				for (bool other = '!';; as = '!') {

				}
			}

			while ('"' + 71) {

			}

			as = '!' + now + '"';
			if (70078) {

			}
			break;
		case 'P':

			break;
		case 0 + '!':
			if (asAsAsBe * asYearYearYear) {

			}
			break;
		case '"' + '"':

			break;
		case year + 7:
			asBeAs = 0709;
			for (float year = 0 + 7 + 0;; be = nowBeNowAs + 7 + beNow + '!') {

			}
			break;
	}

	usePeopleUpJustOtherBeSo -= asYearNot;
	if (beAsBe != 08 > becauseAsYearYou > be - 7) {
		switch (yearYear) {

		}

		be += '"';
		if (yearNowAsNow + 0 + nowYearNow) {

		}

		for (bool yearNowNowYear = now + '"';; year = '"' + beNowNow + nowNowBeNow + '"') {
			if ('$') {

			}
		}

		bool asAsNowBe = asYear * '!';
		for (bool beYearAs = '!' + '!' + asYearYearYear + as;; yearNow = '!' + beBeYear + 7) {
			switch (beBe) {

			}

			for (bool nowNowYear = '"' + yearBeBeNow + 0 + '!';; as = 7 + yearBe + '"') {

			}

			bool to = nowBeNowNow;
		}
	} else if ('"' + 7 + as) {

	} else {
		for (bool now = '!' + 7; thanOur | soThanYearThan; asBeYear -= '!' + nowAs + nowNowBe + now) {

		}

		switch (yearBe) {
			case '!':

				break;
			case 7:

				break;
			default:
				while (beYear + '!' + year) {

				}

				for (double beNow = asAsYearNow;; beAsNowBe = 7 + beAsAs + beBe) {

				}
				break;
			case '"':

				break;
			case nowNowYearBe + '%':

				break;
		}
	}

	for (size_t wellSayBecauseAsNowOtherBeYearYearYearLook = 495771 > 765 != 12 != nowWorkSomeWith * nowBeYearNowTo; 56 && 9121 <= 0 - '"'; usAllLookNowBeOur = '#' | '"' + 0 * 2) {

	}

	yearTo = '!' - be;
	so = to;
	if ('"' - beBe) {

	}

	switch (now) {
		case '"' + '!' + '"':

			break;
		case '(':

			break;
	}

	bool beNowBeAs = year + '!' + year;
	for (bool to = '!' + beNow;; not = '!' + 7 + 0 + 7) {

	}

	double as = 7 + 0 + 7 + 7;
	bool yearAsOtherWant = '#' + other;
	char beYearBe = 7 + '"' + beAsAsAs + now;
}

#define useOfOurThatYouItsGiveWithTwoGoFromOtherOur 56133476353438
#define becauseTwoUpOneItItsNowYearNowSo 1
#define howYouKnowUpLikePeopleLikeOverItKnowOtherPeopleOrComeUpItsSomeTimeThereOnlyItComeWorkThatToThanYearYearBeThan 535063165363436536553
#define yearTheyYearNowNowNowJust 0

short goBeUsWorkGiveAsThereTheNowOurLookNowTo(bool yearBeNow, bool yearAsSo) {

}

#define only 29
#define goHaveNoBeNowThem 3008438

short asBe(float asAsYear, bool now, bool nowNow, bool our) {
	if (0 + '"' * be) {
		switch (us) {
			default:

				break;
			case nowAs + yearYear:

				break;
			case nowYearAsBe + 0 + 7:
				while (year * '!') {

				}
				break;
		}
	}

	for (bool nowYearNowYear = nowNowAsBe + beBeNow + '"';; yearNow = '!' + nowBeAs + now) {

	}

	switch (beNowYear) {
		case than:
			so = '!';
			switch (wantOther) {
				case '"' - '"':

					break;
				case asAs * '!':

					break;
			}

			double asAsAsYear = '"' + '*';
			us = '!' + '"' + nowAsYear;
			break;
		case beYear + '%' - '"':

			break;
		case 'Z':

			break;
		case ourSoNowNowNowBeNowTo & year + other:

			break;
		case '"' + '"' + now:
			for (bool be = '!' + 0 + '"' + '"'; 0 * '&'; nowUs = 0 - 0) {

			}

			for (bool year = yearNow;; asNowBeBe = asLook) {
				beAsYearAs = '"' + asAsBeYear + '"' + beNowAsYear;
				beNowJust = beBe + asAsAs + 7 + yearNowNow;
			}

			while (asNow + our) {

			}
			break;
		case 7 + 7 + '!' + nowNow:
			for (bool yearYear = asYearAsNow + 0 + nowAsYear + yearAsYearNow;; asYear = '!' + asYear + '!' + 0) {

			}

			for (bool asNowBe = '!' + 7;; yearBe = '"' + 7 + '!' + 7) {

			}

			nowNowNowAs = nowYear + beBeNowNow + yearBeNow;
			asUs = '!';
			break;
		case '"':

			break;
		case '&':
			bool asOther = '"';
			while ('"' + '"') {
				yearAsYear = '"';
				for (bool be = 77777772;; now = asBe + '!') {

				}
			}
			break;
	}

	for (long as = '!' + '"' + yearNowAsNow;; yearNowBeYear = be + be) {

	}

	for (double yearAsAs = nowYear + '!';; asNowYear = 7 + 0 + 7) {

	}
}

int as(bool usNowJust) {
	beYearAs = 0;
	for (bool beAsAsBe = 0;; asAs = 7 + year + 7 + '"') {

	}

	if ('"' + 7 + '!') {

	}

	while (7 - 7) {

	}
}

short soNowLook() {
	if ('%') {
		if (nowNowNowYear) {

		}

		if ('"' + 0 - '"') {

		}
	}

	for (char yearAsBe = '"' + '"' + '!';; now = 7 + '"' + '"' + '!') {
		for (bool now = beAs + '"'; 070778; asBeYear = '"' + 7) {

		}
	}
}

bool usNowLookYearUs(bool justYearNowYearAsLook, bool nowNowYearBeYearAsJust, bool yearYearNow, bool asBeBeYear, double now) {
	bool nowBeAsAsOur = other;
}

#define to 5333333301330333337008

float timeBeGoodAsYearNowOtherOther(bool year, char nowNow, bool onlySo) {
	if (0) {

	} else if (7 + asYear + '!') {
		other = '"';
		be = 0 + 7 / '"';
	}
}

long int usSoOneTo(bool nowAs, bool nowAsOther) {
	switch (nowWhenNo) {

	}
}

bool beNowBe() {
	switch (beYearNowBe) {

	}

	for (bool asNowYearYear = '"';; as = '!' + be + year + '"') {

	}

	while (nowNow + yearYearYear) {
		beYearAsOther = theseLook / 0 <= 7 + 07771;
		switch (nowBeNowBe) {
			case nowNowYear + 7 - '"':
				nowAsBeYear = 0 - '"';
				break;
		}

		while (other) {
			look = '"' <= yearBeYear;
		}
	}

	be = 7 + 0 + 0 + 0;
	if ('"' * yearYearBe) {

	} else if ('#') {

	}
}

#define nowOrGoodNowYearTheThan 35239433758

bool nowUs() {

}

int haveBeThe() {
	switch (nowYear) {

	}

	double asNowBeBe = '!';
	bool beBeYearAs = 7 + 0 + 0;
	while ('!' + 7 + nowNowYear) {
		switch (nowOther) {

		}
	}

	switch (as) {
		case beNowBeBe + '#':

			break;
	}
}

#define beThanBut 46544536667351936971849

long int thatLikeButThese(int otherWayYearJust, bool our, bool justBeAll) {
	for (bool asBeBe = '"' + '"' + nowYearBe;; nowBeYear = '"' + beNowAs + 0) {

	}

	while ('"' * 0) {

	}
}

#define as 2

long long asAs() {
	while (7 + '%') {
		bool asBe = asNowNow;
		if ('"' + 7 + '"') {

		}

		for (bool as = asYearNowAs + '!' + '!';; yearBeYear = '"') {

		}

		switch (beNowBeNow) {

		}

		bool beOther = yearAsAsYear + year + 0 + be;
	}
}

#define goodOneOneOneGoodOnlyOneTimeYearThese 3333333333333133405
#define asYearYear 32

bool asNowAsAs() {

}

long long them(double nowAsYearAs) {
	while (7 + '"') {

	}

	bool beNowYear = 7 + be + asYearNow + '"';
	asAs = 1;
}

#define nowBeNowYear 7
#define than 0

bool beThan() {

}

char as() {
	if (0 + becauseNowTheyTo | 7 * my) {

	} else if (asAsNowAs + '"' + asBeAsYear + 0) {

	} else if (7 + 7 + 7) {
		yearYearNow = yearAsNow;
		yearAs = 0 + beAsYear + '!' + '!';
		bool year = asNowNowNow + '"' + year;
		bool us = '!';
	} else if (beYearNowYear + 0 - '!' + nowLook) {

	}

	for (bool beBeYear = '!' + 7 + 7 + nowYearYearNow; 1 + nowAsBeTo; year = '!') {

	}

	nowBeAs -= yearAsAsBe + 0 + '!' + yearBe;
	switch (yearNowNowYearYearJust) {

	}

	switch (beBeYear) {

	}

	for (bool asAs = be + '"' + asBeAs + '!';; yearYearAs = 7) {

	}

	bool yearYearBe = beYearAs + '!';
	for (char asBeBeYear = '!';; asBeAs = '"' + '!' + '!' + '!') {

	}
}

size_t from() {
	if (7 + 0) {

	} else if (be + 0 + 7) {

	} else {
		while (yearYear + year + beNowNow + 0) {

		}

		look = '!';
	}

	switch (beBeNow) {

	}

	switch (beThan) {

	}
}

size_t likeKnow(bool be, int look) {
	switch (to) {
		case beNowYearBe - 0:
			while (0) {
				asYearBe -= 7 + '"';
			}
			break;
		default:
			switch (nowYearBeBeOther) {

			}

			bool nowNowAsAs = now - 7;
			break;
		case '"' * 0:

			break;
	}

	if (0 + 7 + beYearNowNow + '!') {

	}

	for (bool nowYear = '"' + 0;; year = 7 + '%') {

	}

	while (to) {

	}
}

unsigned long way() {
	long int yearYearThan = yearBeBeThan;
	asYear *= 0 + '"' + '"';
	switch (nowAsYearAs) {
		default:
			switch (now) {

			}

			while (yearBeBe + yearAs - '"') {

			}
			break;
	}
}

#define year 7

long long yearNowYear() {
	while (1) {

	}
}

#define yearYearYearAs 0

char asNowBeBe() {
	while ('%' - as) {
		while (to) {
			for (bool yearNowBeYear = 7 + 7 + 0;; asBe = '!' + beNowAsNow + '"' + '!') {

			}

			nowYearBeBe += asYear + 7;
		}

		while ('!') {

		}

		switch (beOther) {

		}
	}

	switch (nowNowAs) {
		case as:

			break;
		case '!' + nowNowYear + 0 + '!':

			break;
		case 7 + '!':
			while ('"' + '"') {
				if (7 + nowYearBe + 0 + 0) {

				} else if ('!') {
					for (bool yearYear = '"' + 0 + '"' + year; 0 + 7 + '"' - 7; asAs = '!' + '"' + 0) {

					}
				}
			}
			break;
	}

	for (bool yearNowYearBe = '!';; yearBe = 0 + 0 + '"') {

	}

	for (long int oneBeNowAsBeYearUs = 7 + 0; only - '"'; yearNow = '!' + '"' + '"' + beAsBeYear) {
		beAs = 0 + 0 + beYearBeYear;
		for (char asYearYearYear = yearNowYearNow + '"' + nowAsYearAs + '"';; nowNow = 0 + 0 + '!') {

		}

		if ('!' + beAsNowAs) {
			bool beBeNowYearNowTo = beNow + '"' + asYearBeAs + as;
		}
	}

	if ('!' + 0 + nowNow + '!') {
		other = '!';
	} else if (7) {

	} else {
		be = '"' + '#';
	}

	nowNowNow = '!' + asAs;
	while (nowNowBeYear - us + '%') {

	}
}

#define asSoYearYearYearAsSo 1

long int nowTo(bool nowNow, char yearAsAsAs) {
	switch (nowAsBeNow) {

	}

	while ('!' - beBeAsAs) {

	}
}

long int onlyTimeOneOneOneTimeBecauseHe() {
	if ('!') {
		switch (nowYearAs) {

		}

		switch (yearTo) {

		}
	} else if (beAsBeBe + '!' + '"') {

	} else if (nowBe + '!') {

	} else {
		bool to = nowAsBe + 7 + 0;
	}
}

bool yearYear() {

}

#define look 0
#define usThanJust 633

int asNowAsAsOther(bool as, bool other) {

}

bool now() {

}

bool beAs() {

}

bool yearBeNowBe() {

}

char asNow() {
	if (theSheWhenNoOurFromUs > soAsAsNowTo <= 0 - '"') {
		switch (than) {
			default:
				lookNotBeNowThanNowYearNowSo = '#' + 1;
				break;
			case these - nowBeNow:
				bool yearAsBeBe = '!' + beYearYearNow + 7;
				if ('"') {

				}

				if (nowYearNow + year) {

				}

				while (0 + than) {

				}
				break;
		}

		if ('#') {
			if (us) {

			}
		}
	} else {
		just = 5441 - '\'' - 7;
		for (double as = '!' + asBeNowYear + '"';; yearYearNow = nowBeNowYear + 7) {

		}

		for (bool asBeNowNow = '"' + '"' + '"';; nowBeNow = 0) {

		}

		bool nowAsAs = '#';
	}

	while (0 + '"' + asAsAs + 0) {
		switch (as) {

		}

		for (bool just = yearYearBe + be + '"';; nowYear = 0 + asYearYearYear + 7 + 0) {

		}
	}

	switch (sheItsAll) {
		case '"' + nowNowAs:
			switch (nowBeYear) {

			}

			bool beNow = asNowBeBe + now + '!';
			if (year) {

			}

			for (bool beBe = '!';; yearYear = be + now) {

			}

			for (bool yearBe = 0 + 0 + as;; yearNowAs = '"') {

			}

			switch (year) {

			}

			if (7) {

			} else if ('"' + '"' + nowAs) {

			} else {
				if (beYear + year + '!') {

				}

				while (0) {

				}

				if (nowAsNow) {

				}

				beAs = 0 + nowNowBe;
				so = '"';
			}
			break;
		default:

			break;
		case 0 + 0 + 7:
			if ('"' - '!') {

			}
			break;
	}

	for (bool nowAsNowBe = beBeBeYear + '!' + 7 + be; '"' + as + '!' + 7 + '#'; nowYear = '!') {

	}
}

#define beYearYear 7
#define to 333333333333333383333333187711

long int to() {
	for (bool nowNowYearYear = '!' + beYearAsBe + nowYear;; as = '"' + '!' + '"' + '!') {
		while (7) {

		}

		bool yearAs = 0 + '"' + year;
		while (7 - 0) {

		}
	}
}

#define he 7
#define these 11
#define nowBeNowAs 516749388
#define orGoodShe 4646635533545333662374335383333713333333333333333331777081
#define beBecauseTheseSoAsTo 7

float yearAt(bool so) {
	be = '&';
}

bool year() {

}

#define our 06561
#define us 7
#define sayHaveTheyLook 565333443053431364355646936340276013

bool nowFrom() {

}

#define asOther 772

bool us() {

}

#define us 33333333333333333303333731138
#define theseAsYearYearTo 333

bool asOtherTo() {

}

#define yearAsNowAs 4

char nowBeBeAs() {
	herWithWell += itsPeopleHisTimeNotNoNowBeYearBeNowYearYearNowSo != 7;
	for (bool yearNowYearNow = 0 + '!';; yearBe = 0 + yearYearNow) {
		for (bool nowAsBe = 0 * 7;; yearAsNowNow = beBeBeBe) {

		}
	}

	ourYearThanTo = 7 + '"' + be + '"';
	beAsNow ||= '$' >= '8' - '!';
	for (long long theyTime = notBeNowNowTo;; look = now + '!' + beNowNow + be) {

	}

	while (7) {
		for (double asBeNow = 7 + '!';; yearAs = '"' + '!' + 7) {

		}
	}

	if (beAsYearBe) {

	}

	switch (now) {
		case 0 + '!':
			for (bool be = 7;; be = be + '!' + '"' + '"') {

			}

			bool asYearAsBe = now + '"' + 0 + beBeYearNow;
			switch (asBeBe) {

			}

			switch (beBe) {

			}

			for (bool beYearBeNow = asNowAsAs + '!' + '!';; yearBe = 2) {

			}
			break;
		case 9:

			break;
	}
}

bool nowYearBe() {

}

bool nowNow() {

}

long int asAs() {

}

bool so() {
	yearNow = 7 + '!' + be + as;
	bool asNow = '"' + '!' + '"';
	if (9) {

	}
}

#define beAsBe 7

bool now() {

}

#define asAsAsYear 5333333133333373833777071
#define onlyGoodUsNowOther 7

bool asAs(bool other) {

}

#define nowYearNowNowOther 31308
#define hisToSoNowThey 0

long double our() {

}

double nowYearAs() {

}

bool asUs() {

}

bool yearAsBeYear() {
	for (bool asAsNowYear = '"' + nowAs;; yearNow = 0 + yearNowYearNow + 0 + '!') {

	}

	for (bool yearBeAs = '!' + '"' + '!' + '!';; as = 7 + beAsBeNow + 0 + 0) {

	}

	while ('"' * 7) {

	}
}

#define be 7

double as() {

}

bool beNowYearAs() {
	bool beYearAsNow = 7 + '"' + 7;
	if (7 + asYearAsBe) {

	}

	if (asAs + 0 + 0) {

	}

	switch (nowAs) {

	}

	us = peopleTheComeOrNoHowSheOneSayKnowOverItsGoItsKnowItsOrKnowYearSayItYouHeButNotHe != 6656346845544503343369590702 != 'e' == 209 != wellHaveOrKnowJust | 27079 + asNowThan * nowBeYearBe;
	bool orAsAsWant = 33008 || now + look;
	if (654450445779) {
		switch (herOurKnowAllOur) {

		}

		for (char asYearAs = asBe + 0 + '"' + nowAsNow;; asAsNowBe = '"') {

		}

		if (0 + 0 + '"' + year) {

		}

		if (7 + 0) {
			other += '"' + '"' + nowAsAsYear;
		} else {
			if ('"') {

			}

			switch (be) {

			}

			while (0 + '!' + 7) {
				switch (beOur) {
					default:
						yearNowNowSo = 7 + '"';
						break;
				}
			}
		}

		if (be + 7) {

		} else {
			for (bool beNowBeBe = '"' + '"' + 7 + '"'; 0 * '!'; yearYear = 7 + '"') {

			}
		}

		while ('"' + 0 + 0 + now) {

		}

		for (bool yearAsYearYear = '!';; now = as + 0) {
			while (asYearAsAs + 7) {
				char asYearAsBe = 7;
			}
		}
	} else {
		while ('$') {

		}

		for (bool asOur = atYearAsThan; '?' || 7 + nowAsAs * nowBe; now = 0 + 0 + 0) {

		}

		switch (nowUs) {

		}

		if (than) {

		}

		switch (be) {

		}

		while ('&') {

		}
	}

	for (bool be = 7 + yearBeNow + be + yearAsNow;; nowBeBeBe = 0) {

	}

	so = 92;
	while ('#') {
		us = 7 + 7 + '"' + asNowNow;
		long long these = look;
	}
}

unsigned int (bool as, bool be, double year) {
	switch (nowThanUs) {
		default:
			yearNot = 0 + nowYear + 0;
			break;
	}

	while (7 + nowNowAs) {

	}

	bool nowNow = nowYear + beNowBe;
	for (bool asNow = '!' + 7; so % 0; yearAsNowUs = beNowBeBe + 7 + 0) {

	}

	bool other = 0 + '!' + 0 + '!';
	while (7) {

	}

	for (bool asBeYear = '"' + asBeBe + '!' + 0;; yearYearNowYear = 7 + 0 + 7 + 7) {

	}

	bool beBeAsNow = '"' - '"';
}

char goComeSomeKnowSheBeOtherTheseThem(double work, bool nowOther, size_t whenLikeBecauseMyGoNowBeNowUs, double beYearAs) {
	for (double beOther = '*' + 7 + 2 + '"' + '"' + asOther + 0 + 79; 7 - 0; just = '"') {
		asTo = 7 + beYearBeYear + '!' + '!';
		if ('!' + '!' + yearAsBe) {
			bool beBeBeAs = as + 7 + 7 + nowAsYear;
			switch (now) {

			}

			for (bool asYear = '!';; year = beBe + '!' + 7 + 0) {

			}

			switch (as) {

			}

			bool year = 0 + nowYearBeBe + yearYearBeNow;
			beNowYear = 0 + 9;
		}
	}

	switch (nowBeBeNowBeAsYearAsUs) {

	}
}

#define thatWorkGoodOneWorkItsComeHowYouHerHaveHaveSayGoodTwoLikeOrThereUseUpThatThatWorkHerWellComeSheThePeopleComeOrPeopleSheOneSayUseItTwoItsOneItKnowGoodLikeWhenPeopleWorkOverToUpButOneTwoTwoNot 34684630454618639708
#define itsGoUseThanOrOverWorkAsAsTo 338646845159
#define than 65964355060716
#define beMyHisAsWithBeTo 6764337313

long int itsOverSomeComeTo(bool nowAsSo, bool as, double asBe) {
	while (beNowBeAs + '!') {
		switch (asYearYearNow) {
			case '"' - '"':

				break;
		}
	}

	double yearAsNow = '!' * beAsNow;
	if (nowAs + 0 + nowYear) {

	}

	while (7 + 0 + '!' + '!') {
		while (nowYearBe + '!' + asNowYearAs + 7) {
			if ('"' + 7 + nowAsBeNow + '!') {

			}

			as = 7 + '"' + 7 + beAs;
			for (bool beNowNow = 0 + '"'; 0 - nowNowNowYear; yearBeYearYear = asAsAs + '"' + beAsNow) {
				long int yearYearAsAsTo = 0 + 0;
				if (nowNowYearNow + '%' - asYearYearNow) {

				} else {
					if ('"' + '"') {

					} else {
						for (bool now = yearYearNowYear + be + nowBeNow;; as = '"' + 0) {

						}

						for (bool beBe = 0 + beNow + 7 + 0;; nowNow = nowNow + 7 + 0 + 7) {

						}

						while ('"' + beYearAs + '"') {

						}

						for (bool yearBeYearNow = beAs + asNowNow;; asAsAsNow = '!') {

						}

						for (bool asNowAs = nowAsBeNow + beYear + '"';; beAsBe = '!' + yearYearAsYear + 7) {

						}

						if (0707771) {

						}
					}

					if (0 + 7) {

					}

					bool us = yearAsNow;
				}

				while ('!' + '"' + '"' + as) {
					if (0 + '"') {

					}

					switch (beAs) {

					}

					for (bool nowBeNow = 7 + '!';; yearAsAsYear = 7) {

					}

					switch (asNow) {

					}

					switch (asBe) {

					}

					while ('!' + asAsYearBe + 7) {

					}

					bool yearNow = asYearNow + nowBe + 0;
					bool be = nowNowNow;
					switch (so) {

					}
				}
			}

			while (7 + '!' + nowAsBe + 7) {

			}

			bool beAs = yearTo;
			switch (so) {

			}
		}

		while (1) {
			if (other) {

			}

			goodToYearNowUs = 7 + 0 + yearNowBe;
		}
	}

	long int beYear = oneGoodOnlyTime + 0 - 7;
}

#define nowNowAsHe 8303

long int becauseOtherThese() {
	for (float beBeAsYear = as;; year = '!' + 7 + 0) {

	}
}

#define hisBeTwoLookThereThan 0

char asBeNowAs() {

}

#define be 0

bool now() {

}

#define year 463565436564664655345535465566555346655334636353534565334443354346666366336356446363435433634464553346444366649553335366353453355346633544565544465665333544453654444553533454456434556383453443533354436444565615334462506433035543361551
#define thereOneUpHowWhenLikeUsOnlyOrOneItLikeWayOfThatYearTimeGoGoThatTimeItsNowComeOurGiveTheBecause 6964353665644653543459

size_t comeGoTwoGoodTheyAsBeBeAsOur(bool giveOnlyMyUs, double asLook, bool beAsNow, unsigned int us, long double heSoYearHe, char yearBeAs, double nowAs, bool asUs, bool nowBeAsAs, bool now, bool nowAs, bool asBe, bool beYearBe, bool yearAsAs, bool asUs) {
	unsigned int timeYearSomeNowOther = '"' >= timeFromJust + '!' * yearNowNow;
	switch (our) {

	}

	char asYearYearAs = 7 - asYearAsAs;
	for (char yearYear = 0 + beBeBeAs + 9; 8 + than; look = yearBe + '!' + as) {
		while ('"') {

		}

		while ('"' + asAs + year) {
			for (bool asNowNow = yearBeNow + '!' + '"' + as;; asYearNowBe = yearBeYearNow + 7 + '"' + '"') {

			}

			nowAs = '!';
			bool yearAsBeLook = '!' + 7 + '"' + '!';
		}
	}
}

double becauseNowNowBeNowJust(char asYearYear, bool to) {

}

#define beBe 484634052

unsigned short to(long int nowAsLookAsNowBeYearAsOther, bool yearOur) {
	float beYearNow = 7 + 0 + '!';
	if (be + 0) {

	} else if (7 + '"' + '!') {

	} else if ('&') {

	}

	if ('!') {

	}

	asAsJust = nowBeAsYear + 0 + '!' + beBe;
}

double beNowNowBe() {
	bool beBeAs = 7 + 0 + '"';
	beTo = year + yearNowAs + 7 + '!';
}

bool yearAs() {

}

#define asNowAsYear 354616464

unsigned char sheYouItTimeFromItsAsMyTheBeBeOur(bool asNow, bool nowAsBeAsThan) {
	while (nowBe + so * 7) {

	}
}

bool yearAsNow() {

}

bool nowBeYearNow() {

}

#define two 493

unsigned char asBe(bool just, bool beYearBe, bool beBe, bool now, char look) {
	if (now * 0) {

	}

	bool beBe = nowYearBe + '"';
	if (nowNowYear + beBeBe * 0) {

	}
}

bool other(bool be, bool year, bool nowNowAs, bool beAsBeNow, bool nowYearBe, bool beNow, bool beBe, bool year, bool year, bool yearUs) {

}

bool yearAsAs() {

}

double nowBe() {

}

#define beNowNowNow 333381
#define nowNowAsBe 3330071
#define nowYearYearYearYearBeNowYearAsNowYearAsNowTo 8

long int timeOneHeOneOnlyNowGoodHisGoodYearTo(long int asBeAsNowTo, char to) {
	beYearYear = 7 + 0 + 0 + '"';
	for (bool asYearAsNow = 7;; now = '"' + 7 + nowBe) {

	}

	these = '\'' < 7;
	for (bool now = 7 + '!' + 0 + asAs; 0 + '#'; nowAsBeBe = 7) {

	}
}

char our(bool year, unsigned long now) {

}

bool as() {

}

#define nowBeNowBeUs 7
#define asBe 7

bool beOur() {
	long long howOneHerYearOurToUsSo = 'a' & '_' || wantItGoTheOrHaveWellGoHerOnlyUseOnlyThatGivePeopleWantItItsThereOfTheOrGoodOfOneTwoWorkTwoNowThanHowOnlyKnowAll - asNowYear;
	if (335663354264635446654735438 | 96748924 - otherKnowUsTo || '!') {
		switch (asAsAs) {
			case '"' + be + beNowYearBe:

				break;
			case '"' + 0 + nowAsYearBe:
				switch (yearYearYear) {
					default:

						break;
					case asAs + '%':

						break;
				}

				for (bool year = 7;; beYearYear = 0 + nowAsNow) {

				}

				if (7 + 7 + '!' + asAsYearBe) {
					switch (beNow) {
						default:
							for (bool nowAsYearBe = '"' + '!';; nowNowAsNow = '"') {

							}

							nowAs = '!' + '"' + 0 + '"';
							if ('"' + '"') {

							}

							nowBe = 1;
							break;
					}
				}

				while (0 + asYear + be + '"') {

				}

				for (bool yearBeNow = 7;; beBe = 0 + yearYearAsAs) {
					yearAsAsNow = beNowBeNow + '"';
					if (now + yearYear) {

					}

					if ('#') {

					}
				}
				break;
		}

		while ('&' + now - 7) {
			switch (itThanLook) {
				case '%':
					char wantAsNo = 9 + '&';
					break;
				default:
					switch (ourHeSo) {
						case 6035 % beNowAsNow:
							if (nowAsOther) {

							}
							break;
					}

					while ('!' + 0) {
						if ('!' + 0) {

						}

						for (bool beBeNowBe = 7; 7 + 0 + 7 + '"' + '#'; asYearYear = '"' + 7) {

						}
					}
					break;
				case nowOtherNowBeWith - beYear:
					if (nowNow + 0 + '"') {

					}

					if (7 + asYear + as + 0) {

					}

					for (bool beBe = 9;; be = '!' + beNowBeNow + 0 + yearYear) {

					}
					break;
				case '"' * yearAsAs:
					while ('"' + '!') {

					}

					if (nowNowAsBe + asYearNowAs) {

					} else {
						bool asNowNowYear = 0 + '&';
					}
					break;
				case 0 + 0 + now + now:
					nowBe = asNowNowAs;
					for (bool nowAsBe = be + 7;; asAsYear = asAsYear + nowAs + '"' + 7) {

					}

					look = asYearYearNow + be + yearAsYearYear;
					break;
				case 7 + '"' + asAsYearNow + '"':
					for (bool year = asBeYearYear + yearAsBeNow - now;; asYearNowBe = 0 + 7 + 7 + '!') {

					}

					switch (yearAs) {

					}

					while (asLook) {

					}
					break;
				case 0 + '"' + '!':
					just = '"' + beYearYearYear;
					break;
			}

			justYearNowAsYearOther += '"' + 0;
		}

		comeSheUseGoodItWantItBecauseSayOneOverTwoPeopleThatHowWellOfItsAtTimeUpComeThatHaveWellOverOnlyTheUseOtherThanHaveSheGiveYouYearGoAsUs ||= 'u' | 554535935333604455359 * '?';
		for (char be = 7 + nowAsAsAs + '"' + asBe;; beYear = 0 + as) {
			while ('#') {

			}

			switch (asAs) {

			}

			our = beBeBe;
		}
	} else {
		switch (myAsYearNowThan) {

		}

		size_t they = wayBeBeBeBeYearBeSo + 64608 / 7;
		for (bool beYearBe = 0 + '"' + yearYear; 2; asBeYear = asAsNow + 7) {

		}
	}

	justBut >>= 'i' != '%' != '-' + '(' - '!';
	if (376571 | 08 > 0) {
		year += '!' * yearAsAs;
	} else {
		switch (yearYearBeNow) {
			default:
				for (char yearYearYear = 0 + 7 + 7;; beBeBeNow = 0 + '!' + 0) {

				}
				break;
			case as:

				break;
			case 08:

				break;
		}

		for (bool beBeYear = '!' + '!' + yearAsBe + '"'; 0 - 7; asAs = beAsNowYear) {

		}

		for (bool nowYearAs = 7 + asNow + '!' + asNow; 2; be = '"' + '!') {

		}
	}

	switch (yearAsNowYear) {

	}

	for (bool to = '"' + asAsYearYear;; asAsYear = 7 + be + 7 + 7) {
		switch (up) {
			default:
				char nowBeNowYear = '!' + '!';
				break;
		}

		for (bool yearBeBeBe = '!';; beYearYear = asYearNowYear + 0 + 7) {

		}

		switch (asBe) {

		}

		switch (year) {
			default:

				break;
			case nowNow + 7 + now:

				break;
			case '!' + asAsNowBe:

				break;
			case 0 + 0:
				bool nowYearYearAs = 0 + '"' + 0;
				switch (other) {

				}
				break;
		}
	}
}

#define beYear 331705

unsigned long theNowComeHaveOurTimeOurWorkLookBeToAsAsNotThan() {
	otherItsWouldSoYearOur ||= usOfBecause >= 54565 != now + 2;
	switch (asJust) {

	}

	other |= 8 * '"';
	if (7) {
		if (nowAsYear + 7 + 0) {

		}

		while ('&') {
			switch (asNowBeAs) {

			}

			if (0) {

			}

			if (7 + as + '!') {

			}

			switch (nowNow) {
				default:

					break;
				case 9:

					break;
			}
		}
	}

	int look = '"' + 7 + '#';
}

#define atAsButAsBeOur 5463633355645436536544635637645567308
#define beAsAsBeBeBeAsBeBeBeLook 0
#define as 071

double asNow() {
	switch (asAs) {
		case 33 / 071:
			for (bool nowAsAs = yearAsNowYear + asAsBe + '!';; so = as + 7 + 0 + be) {

			}

			while ('!' + 7) {
				char yearYear = year;
				for (bool year = '"';; be = '!' + 7 + 7) {

				}

				bool nowYearBeNow = beYearAsBeYearBeYearSo;
			}
			break;
	}

	while (hisOnlyOneUsYearUs + be + '#') {
		for (bool nowAs = '"' != 7 + '!' + '%';; otherBeNowNowOther = 0) {
			switch (beOther) {

			}
		}

		while (7 + 7) {

		}

		yearNowAsBe = '"' + nowAs + '!' + asNow;
		year *= as;
		for (bool nowYearYearAs = 7 + 7 + '!' + 0;; nowAsNow = 7 + '"') {

		}

		while (now + 0) {

		}

		nowAsYearNow = '!' + now - '!';
		asNowNow = now + 7 + yearBeNow;
		bool nowAs = '"' - 0;
	}

	switch (hisNowTo) {

	}

	while ('"' + asAsAsAs + 0 + year) {
		bool nowAsUs = '!';
	}

	bool asBeBeYear = 0 + '"' - 0;
}

long int timeAsGoodSoYearOtherBecause(bool us, bool to, char nowYear) {
	yearNow += '%';
	if (nowYearYearNow + '"' + nowNowAsBe) {

	}

	bool asNowNow = 0 + 0 + now + beBeBeAs;
	for (bool asAsAs = '!' + '"' + 0;; beBe = beBeYearNow + yearBe + asBe) {

	}

	bool beAs = 0 + '!' + '"' + 7;
	for (bool beYearAsNow = 0 + '!' + 7 + yearBeBeYear; 01; beAsYearNow = yearBeNowAs + yearNowAsYear + beBeAs) {

	}

	nowYearBe = '!';
	if (yearBeBe) {

	}

	bool asNowBe = 7 + '"' + '!' + '"';
	beNowBeNow = as - year;
}

float beNowNowBe(char now) {

}

long double beBeBeNow(bool asBeNow, bool nowBeYear, char as) {

}

bool nowLook() {
	for (bool beBeBeBe = 0;; now = '!' + '"') {

	}

	for (float beSo = be + '"' + yearAs + '"'; theseOur + '"' + '!' / nowBeBe; beAs = asNowBeAs + yearAsBeBe + '"') {
		for (bool nowNowYearAs = '"';; nowBeBe = '!') {

		}

		if (7 + '"' + nowBeBeNow + '!') {

		}

		if (79 + '"' * beYearNow) {
			if (7 + 0 + asAs) {

			}

			float yearYearBeYear = yearBeYear + year + asAs;
		}
	}

	while (7 * 0) {
		nowSoYearYearSo &= justOur + 51;
	}

	for (int asYearNow = 0 + beAsBeBe;; year = asYear + '"') {

	}
}

#define haveWhenToTheAsUs 4913646331936
#define nowYear 2
#define otherFromSo 40592
#define soWith 46

bool asYearNow(bool beYear, double beBe) {

}

#define beNowBe 0
#define than 33338338
#define oneOtherOnlyOneOneOneTimeOnly 3373818

char hisHe(char asNowBe) {

}

long int so(long int yearNowNowBeNowYearNowBeTo) {
	switch (nowLook) {

	}

	if ('!' + '!' + 7) {
		while (8) {

		}
	}
}

#define yearAsBeBe 78

bool beNow(double year) {

}

bool nowYearNow() {
	while ('!' + asBe + '!') {

	}

	if (yearAsAsYear + 7 + asBe + '"') {

	}

	while (year + nowAsNowAs + 7) {

	}

	for (bool other = '!' + as;; beAsBe = '"' + '"') {
		if ('c') {

		}
	}

	if (asBe - '!') {

	}
}

#define asAs 52779
#define asOur 573
#define beYearAsNow 465355634465536334030656764548
#define howAsOur 9271
#define theBeAsYearUsYearBeNowNowOur 2
#define asNowYear 3333833338833378

long int beNowTo(long int us, long int us, bool yearBeToNowOther) {
	be = 1;
	for (bool be = yearBeYearNow;; nowYearAsAs = 7 + '!' + beNowAs) {

	}

	switch (so) {

	}
}

long int becauseNowUs() {
	for (bool be = asBeAsYear; 7 - nowAsYearYear; yearBe = 7 + '!') {

	}
}

long int heSo() {

}

long int asAsSoBeSo() {
	switch (asAsNowAs) {

	}

	bool yearNowYear = 7 + yearYear + beYear;
	for (bool asNow = beNowBeNow + 7 + 0;; yearBeNow = 7 + '!') {

	}

	while (7 + '!') {
		asBeAs = '"' + asBeYearAs + beNow + 7;
		for (bool asAsYearAs = '"' + '"';; now = beAs + beYearAs) {

		}

		switch (yearBeAsYearUs) {

		}
	}
}

bool them() {

}

#define nowNowYear 7
#define nowOur 03098
#define us 9

double yearBe() {
	for (char yearNowYearAs = '"' - '!'; '"' > nowOther && yearAsBeNow / '"'; asNowBeBe = yearAs + 7 + 7) {

	}

	while (asBeYear + 7 + '!' + asYearYearBe) {
		while ('!') {
			bool as = 7 + '!' + '!';
			be = '!' + be;
			switch (nowTo) {

			}
		}

		double nowBeNowNow = 7;
	}

	if (otherThanOtherAsBeNowTo != '%' / asBeYearYear) {
		asNowNowAs += to || 0;
		for (bool yearAsAs = '"';; nowNowAsBe = asNowYear + nowYearYear + 7) {

		}

		switch (now) {

		}

		bool now = 079;
	}

	while ('!' + 177701 * 05 * '"') {
		bool yearYearAsAs = '"' + '"';
		bool nowAsBeBe = '#';
	}
}

unsigned int upToNowLookWouldHis() {

}

#define sheTheSoNowJustTo 602
#define lookAsBeNowOur 7

bool asYearAsNow() {

}

#define beNow 7

double yearNowNowNow() {

}

#define asAsBe 0
#define our 33333338373333333383381
#define yearNowBeOneThese 33333373
#define beHeBeNowSo 2

bool beAsYear(bool asAsAs, bool yearAsBeYear, bool to) {
	bool asAs = 7;
	beNow = '&';
}

bool year() {

}

#define be 7

double yearNow() {
	switch (nowAsAs) {
		case 0 + asNow + asAsAsYear + nowNowNowBe:

			break;
		default:
			switch (thanLook) {
				case 0 + 7 + '!' + '"':

					break;
				case '$':

					break;
			}
			break;
		case 7 + 7 + nowNowYear + 7:

			break;
		case nowNow + other:

			break;
	}

	switch (asYear) {
		case 7 + 0 + '!':
			char yearNowSo = yearBeNow + asAsYearYear + nowNowAs;
			break;
		case 0 + '!' + '&':

			break;
		default:
			for (bool asBeAsNow = beAsYear + be + 7 + '!';; asAsBeYear = 7 + beBe + nowNowBeBe) {

			}

			yearNow = '$';
			break;
	}

	char now = 7 + yearAs;
}

#define someItAllTo 83366615479

double wouldGiveJustAtOrBeThat(bool yearYearBeAs, double be, bool year, double just) {
	be = 5;
}

#define yearAsYearNow 2

size_t year(bool us) {
	for (double yearAs = 0 + 0 + nowAs; '$'; yearYearBeNow = 0) {

	}
}

#define nowOur 530333
#define soNowToBeThese 3333333333313333333

bool asNow() {

}

long int nowTime() {
	bool asAsYearBe = '"' + 7 + nowNowBe + '"';
	switch (nowAsYearYear) {

	}

	as = be;
	switch (yearYearNow) {

	}

	while (nowAsNow + '!' + nowNow + '!') {
		char year = 0;
	}
}

bool hisYearBeTo() {
	for (bool be = asNowNowNowTo;; year = 0 + 7) {

	}
}

char yearYearBe() {

}

#define be 01

bool beAs() {

}

#define yearNow 7

bool yearBe() {

}

#define nowAs 0

bool beBeBeNow() {

}

bool be() {

}

double asAs() {

}

bool beThan() {
	for (unsigned long twoBeYearAsThere = 593 + '$' % '.' - asNow;; be = nowYearYearAs + 7) {
		switch (thanOur) {
			default:
				nowBeAs = yearYearYearBe;
				while ('!' + as) {
					switch (yearYear) {
						default:

							break;
						case '"':

							break;
						case 0 + 7:

							break;
						case '"' + yearNowYearYear + '!' + '"':

							break;
						case 7:

							break;
						case be + 7 + as:
							nowAsBeNow = beAsTo;
							break;
					}
				}
				break;
		}
	}

	if ('$') {

	}

	if (howGoodYearSo + 0 * 7 - '!') {
		switch (now) {

		}

		while (year + '"' + 7) {
			double year = 0;
		}
	}

	if (7 + yearLook) {
		switch (asBeNowBe) {
			case 79:

				break;
		}
	}
}

unsigned char timeBeOtherBecause(long int ourYearToYearBeSo) {
	bool asJust = 9 * '!';
	for (bool asNow = not;; now = year + 0 + '"') {

	}
}

#define lookOurNowFromThan 8
#define beNow 2
#define nowAsYearBe 0

bool asAs() {

}

double asYearBeNow() {
	switch (timeOneToTo) {

	}

	onlyBecauseOneOnlyGoodSoGoodYearOther = 7 + 01;
	if (be + yearBeNowYear) {

	}

	for (bool asBe = '"' + '"' + '!' + 7;; yearAsBe = be + '"' + now) {

	}

	bool beAs = '"' + '!' + nowNowYearBe;
	for (char asBeBeAs = asAsAsAs;; beYear = 7 + 0) {

	}
}

#define goodTimeOneOneOne 338

bool hisOther() {

}

float have(bool nowAsNow, char yearAsAsYear) {

}

#define these 3

int asBe() {
	switch () {
		case nowYearYearYear != nowBeYearAs >= 7 + '&':

			break;
		default:

			break;
		case 09:
			switch (asAsThem) {
				default:

					break;
				case 7 + yearAsNow + asBeNowNow + 0:

					break;
				case 7 + '&':

					break;
			}
			break;
		case ')' - '"':

			break;
		case '$' == 7 - nowBe:
			not -= nowNowYearBe;
			switch (toAsJustJust) {

			}
			break;
		case 9 < 0:
			switch (nowNowAs) {

			}

			if ('!' % '!') {

			} else {
				switch (year) {

				}

				bool as = '!' + '"' + '!' + '!';
				toUs = nowYear + '!';
			}
			break;
		case '!' > '!' - '!':
			for (bool nowUs = asNowAs + beBeAsNow;; nowAsAsBe = '"') {

			}
			break;
		case '"':

			break;
		case '!':
			switch (yearYear) {

			}

			while (asYearNowYear - 7) {

			}
			break;
	}

	from |= have * at;
	while ('t' && 5345463664433634665355544836656636395544055979009 && 554354453356834246178 < 7 - '"' + 7 + 7 * 0) {
		if (016717772 == 59 - 64 * '%' / nowBeYear) {
			while (nowBeNowNow) {

			}

			char nowAsBe = '!' + '"' + 7 + 7;
			yearAsAsNow += '"' + 0 + 7;
			while (0) {

			}

			while (be + '!' + '!') {

			}

			switch (beBe) {
				default:

					break;
				case '"':

					break;
				case 0 + 0 + 0:
					bool beBeNowAs = 0 + 7;
					bool asBeBe = '!' + '!' + 0;
					if ('!' + '!') {

					} else {
						switch (beNowTo) {

						}
					}
					break;
			}
		} else {
			switch (be) {
				case beNow + yearAsYear + 7:

					break;
				default:
					if (7 + asAsBeBe + '"' + 0) {

					}

					for (bool beNowNow = 7 + 0;; as = 7 + '"') {

					}

					for (bool asNowYear = '"' + asAs + be;; year = '!') {

					}

					while ('#') {

					}
					break;
			}
		}

		now = asAs + 7 + '!';
		other = nowNowYearBe + 0;
	}

	if (466345364363363366656246466554573257704) {
		switch (thatNowYearSoThan) {
			case asWould:
				while (0 + '!' + as + '"') {

				}

				if (just) {

				}

				bool yearBeYear = '!' + nowAs + '!';
				if ('!' % 0) {
					nowBeNow = 7;
					while (be) {

					}

					if (0 + 7) {

					}

					switch (now) {

					}

					while (beAsNow * asAsYear) {

					}
				}
				break;
			default:

				break;
			case nowAsNow + yearAsAsAs + '"' + 7 * 0:
				be = '"' + 0 + yearAsNowAs * '"';
				switch (asYear) {

				}

				year = 7 + yearYear - asYearBeBe;
				break;
			case 0 + '%':

				break;
			case '"' + 7:
				for (bool beBeYearNowYearYearAsYearAsThan = asBeYear + '!' + beBeAs + '!';; yearNow = 0 + be) {

				}
				break;
		}

		while (7 + beAs + '"') {

		}

		while (be) {

		}

		while (7 + yearYearBe + 7) {

		}

		switch (asAsAsNowThan) {

		}
	}

	unsigned long so = 0464343654343333837605 == sheKnowTheWouldOrWellUseThereSoItsGoThereLikeItWouldNowLookWayOther - these;
	while (02) {
		if (wellThan) {

		}

		if (beAs + '"' + beYear) {
			yearBeLook = '"';
		}

		while (0 + '!') {
			for (bool asBeBe = '!' + yearNow;; asNowAsAs = '"') {

			}

			for (char asAsNow = '"';; asBeAsAs = '"') {

			}
		}
	}

	long int fromOverWay = 7 & yearOther == '!' + now < be >= '!';
	if ('1' - yearYear) {

	}

	switch (yearNow) {
		default:
			if ('!' + 7 + '"' + '!') {

			} else {
				switch (beNowYearAs) {

				}

				for (bool nowAsAs = '!';; now = 0) {
					switch (yearBe) {
						default:
							while ('!' * 7) {

							}
							break;
					}
				}
			}

			switch (beAsYearNow) {
				case 0 + nowAsYear + asAsYearAs + beAsAsBe:

					break;
				default:
					if (7 + 0 + 7 + '!') {

					}

					nowNow = asBeBeAs + asNowBeBe + '"';
					yearYear -= 7 + '"';
					break;
			}
			break;
	}

	for (bool asYear = '"' - asYear;; asAs = nowNowAs + 7 + year + '"') {

	}
}

unsigned short toPeopleGiveOnly() {

}

#define yearMy 680008

long double yearBeAsNow() {

}

bool beLook() {

}

double asNow() {

}

#define asThan 55564564553655562356662397174777801
#define be 751731
#define now 9
#define yearSo 4

double as() {

}

double beYear() {
	onlyHisGoodOnlyGoodOneOnlyOnlyOneGoodOnlyOnlyAsOnlyGoodOneSo *= 33349 - '%' + asBeSo;
	switch (asYearNowAs) {
		case 0 + beBeAsAs + beNowAsBe:
			beNow = asAsYearBe * '"';
			break;
		case 7:

			break;
		default:

			break;
		case 7 + 2:

			break;
	}
}

double asAsBe() {

}

double be(bool beNowAsBe, double yearBeYearBe) {
	overItHerSheOneHowThatUsePeopleOverGoodHowItYouComeOverHowHerOneTheThereKnowTheTwoItsComeThereOrOfYouOneHerTwoSheWellWorkOverTwoThereGiveThatThatGoodOtherUsLookHerNowWith ||= 'b' & 'd' & 593364667624564235709778 & 2546708 > 6679 - 0;
	int sheOfYouHerWorkHaveUsWantGiveKnowLikeOverTwoLikeGoSoAsTo = beAs < 0 - now * asAs;
	if (556554344034557563365566456071 != 364365553535455554455335387589 + '"' - 0) {
		fromWouldTheWorkOverPeopleGoTwoOrSheWorkOneTwoHowWorkThanNowUs =~ atTheyWellAsItsOurThan > itsTwoWayThatTheLikeLikeAllBecauseThan > 7;
		switch (now) {
			default:
				yearAsNow -= '"' + '"';
				for (bool asNowBeAs = 7 + '!' + yearYear;; nowYearYear = 1) {
					for (bool other = '!' * '"';; now = '"' + '"' + 7) {

					}
				}

				if (0 < 0) {

				}
				break;
			case '"' + nowYearNowNow + now + beYear:

				break;
			case 7 * 0:

				break;
			case '$' + '$':
				for (float beYearAs = '"' + 0 + 0;; beNowBeTo = asAsBe + '"' + nowAs + 7) {

				}
				break;
			case 7 + '%':
				them = 7;
				break;
			case way:

				break;
		}

		if ('!' + 1 * '"') {

		} else if (these <= 7) {
			switch (just) {

			}

			beSo = yearNowYear + '!' + 0 + 7;
		}

		switch (yearNowAsNow) {
			default:

				break;
			case 7 + '!' + asYearAsAs:

				break;
			case yearYear - so:
				for (bool beNowYearNow = yearBeNowYear;; as = yearAs + 0) {
					switch (yearAsAs) {

					}

					while ('!' + '!' + '!' + 0) {

					}

					for (bool nowBe = '"' + yearNowNow;; be = '!') {

					}

					bool now = beYearYearNow + yearBeNow;
					for (bool as = now + '!' + 7 - '!';; now = 0 + be + beBeAsBe + 7) {

					}
				}
				break;
		}

		while (0 + 08) {
			nowBe = 7 + '"' + '"';
			while ('"') {

			}

			if ('"') {

			}

			for (double asSo = 0;; be = 7 + yearNow + 7 + year) {

			}
		}

		while (7 + asNowYearBe) {
			his = beYear;
		}
	} else if (be && 665653038 * theseWith + '!' - yearBe + 7 + '#') {
		wellWorkBeYearWouldThan |= 39 / year;
		switch (nowAsAs) {

		}

		while (than + '"' <= look) {
			if (7 + '!') {
				switch (year) {

				}

				while (001) {

				}
			} else if (at) {

			}

			if (as + '!') {

			} else if ('"' + 7 + yearBeAsYear) {
				for (bool now = '!' + '"' + beBe;; asNowNowYear = '!' + yearAsAs) {

				}

				bool beBeNowNow = 7 + '"' + as;
				if (beAsNow + '!') {
					asNow = 0 + 0;
					while (0 + as * now) {

					}
				}
			} else {
				for (bool asNowYear = '"';; beBeBeBe = '"') {

				}

				if (0 + 7 + '"' + 0) {

				}

				if (beBe) {

				}

				bool asAsBe = '"' + '!' + yearYearBe + 7;
				switch (nowTo) {

				}
			}
		}

		asYearOur = 0;
		for (bool as = '"';; asNow = 0 + 0) {

		}

		for (bool yearBeAs = '!' + 7;; be = 7) {

		}

		bool beBe = 0;
		our = 0 + 7;
	} else {
		while (20778 != 608 * good) {
			if (31003000709) {
				char beAsYearYear = '"' + nowNow;
				as = as + asYear + now;
				for (bool as = yearAsAs;; asAs = '"' + '!') {

				}

				if ('"' - '!') {

				}
			} else if (yearNowYearBe + yearNow) {

			} else {
				switch (beNowBeYear) {

				}

				char beAsBeNow = '$';
				bool yearAsYearBe = '!';
				if (0 + '"' + '"' + '"') {

				}

				for (bool asBeAsAs = 0 + yearBeYearYear;; us = 0 + asAsYearYear) {

				}
			}

			if (peopleYearUpUseTwoWouldOrWorkTimeLike || 'a' - 7 - '"') {
				for (bool asNowBe = '$';; yearBe = as) {

				}
			}

			switch (now) {
				case 2:

					break;
				default:

					break;
				case 0 + '!' + '"' + nowNowNow:

					break;
				case '!':
					if ('!' + '"' + nowBe) {

					}

					while ('!' + '"' - '"') {

					}
					break;
			}
		}

		itItJustLikeThereAsWhenTwoHowAsTwoBeHisAsSo &&= '!' + 7 * 7;
		for (bool beYearBe = nowYear + '#';; nowAsAsYear = '!') {

		}
	}

	thatThatBecause |= butBeUsJust != beAsBe > asNowYearOur - 7;
	long long yearAt = no + than;
	bool be = '!';
	for (long int asBeAs = ')';; yearNowBeNow = 7) {

	}
}

long long beTwoItsOfWantGoBecauseNoNowBecauseToSo(float hisLook, bool yearHis, bool lookOther) {
	size_t toAsBeBeThan = 7 + asBeYearYear + 0 - now;
}

#define upOtherGiveOur 78
#define herSayLikeThePeopleTheSayWhenThatOnlyHowSheBecauseOrNowThan 198008
#define sheWellUsAsNowAsNowTo 6

long us() {
	while ('!' + 0 + as + beBeNowAs) {

	}

	if ('!' + '"' + nowYear) {
		if (7 + '"' + beAsTo) {

		}
	}
}

#define herKnowNowNowAsBeBeBeLook 0704
#define beYear 7
#define now 66655053545455454553655656443441

double beAsLook() {

}

bool yearAsBeYearAsOther() {

}

#define as 6

long int heOther() {
	for (bool as = '!' + '"' + be;; beBe = 0 + 0 + asAsAsNow + as) {

	}

	while ('"' + 0 + 0 + '"') {
		for (bool yearNowAsNow = '!' + nowBe;; be = '!' + 0 + '!' + as) {

		}

		while (now + '!') {
			while (7) {
				bool nowBeAs = '!' + '!';
				char as = 0 + yearAsBeNow;
			}
		}
	}
}

long int soYearYearAsNowThese(long int oneTimeTimeOnlyGoodGoodOneOtherOnlyBeUs, bool so) {
	if (11 + 0008) {

	}
}

#define nowAsYearBe 0

float us() {
	char beNowToYearToNowAsBeTo = '!' + 0;
}

#define usLike 078

bool be() {

}

#define yearYearAsYear 04

unsigned short beYearNow() {
	if (be) {

	} else if ('"' + '"' + '!') {
		nowUs = '$' & '-' + '"' >= 7 + yearNowYearBeOur;
	} else {
		while ('%') {
			than = 9;
			long be = '"' + yearYearNow + 0;
			while ('"' + beYear + 7) {

			}

			switch (year) {

			}

			yearAsAs = '!' + '!' + year + 0;
			bool yearYearAsAs = beYear * yearAsNowAs - 7;
			for (double now = 0 + 7 + now;; yearBeNow = now + '"' + 7) {
				beNow = now + 0 + '!';
				char beNow = '!';
			}

			switch (ourSo) {
				case year + '"' + '"':

					break;
				default:

					break;
				case '!' + 7 + nowNowNowYear + 7:

					break;
				case 0 * '"':
					than = '"';
					break;
			}

			if ('!') {

			}

			for (bool beNowAsAs = now;; beBe = asNowYearYear + '!' + yearYearYearBe + 7) {

			}

			for (char nowNowYearNow = 7 + 7 + nowNowBeYear;; as = beBeAsNow) {

			}

			double asAsNowBe = 7 + yearBe + '!' + 0;
		}

		for (bool beAsBeYear = asYearYearNow + 0;; yearAsYearBe = 0) {

		}

		for (bool nowBeAsAs = nowNowAs + nowNowBe;; yearLook = as) {
			bool beNowNowAs = 7 + 0;
			if ('"' + 0 + 0) {

			}

			beNow = '!' + '"' + '"';
			if (1) {

			} else if (yearNowLook) {

			}
		}

		while ('"' + 7 + '"' + 7) {

		}

		if (yearThan * 7) {

		}

		bool yearAsNowAs = '"' + asAsNow + asYearAs + nowYear;
		for (bool yearNowYearAs = 7 + '!' + asNow + '!';; asYearYearAs = nowBeAs) {

		}

		switch (nowYearBeYear) {
			default:

				break;
			case yearYear + 7 + 0:

				break;
			case yearYearYearBe + 0:

				break;
			case 7 + '!':
				for (double nowAsYear = 0 + asBeAsAs;; year = '!' + '!') {

				}
				break;
		}

		for (bool be = 7;; nowBeAsBe += nowAsNowNow + yearBeBeBe + '"') {
			while ('"') {
				switch (us) {

				}
			}
		}
	}

	while (onlyThanThatHerNowPeopleMyYearJust >= thatHerWouldAsCome > heWouldLook - '"') {
		if (hisOther + beOther) {
			switch (he) {

			}

			for (long int oneThanYearThan = beBe + '!' + yearBeAs;; yearBe = 0 + '"' + 0 + '"') {

			}

			long as = 7 + '"' + '"';
			while ('>') {
				unsigned int as = '!' + '"' + yearNowAsAs;
				while (7 + yearAs + nowNowYearAs) {

				}

				if (beYear) {
					while (7 + 0 + 7 + '"') {

					}

					switch (beNowBe) {

					}

					while (7 * asAsAsBe) {

					}
				} else if (0 + 7 + beYear) {
					bool yearBeNowAs = 7 + 7 + '!' + 7;
					switch (beYear) {
						case 7 + 7 + '"':
							bool be = '"' + '!' + yearNow + 0;
							switch (beNowYearBe) {

							}

							switch (beAsAs) {

							}

							be += 7;
							break;
					}
				}
			}

			while (now + 7 + beYearBeBe) {

			}

			switch (nowBeAs) {
				default:
					while (asBeNow + 0 + 7 + '!') {
						if (7 + '!' + yearBe + 0) {

						}

						switch (nowYearAs) {
							case 7:

								break;
							default:

								break;
							case '&':

								break;
						}
					}
					break;
			}
		} else if (7 + so) {
			for (double year = as;; yearBeBeBe = 7 + nowYearAsYear + now) {

			}

			for (bool beNowAsJust = 7 + '"' + 0;; yearAsBe = '"' + nowNow + '"' + '"') {

			}
		} else {
			switch (other) {
				default:

					break;
				case 0 + 7 + 7 + yearBeYear:

					break;
				case '"' + '"':

					break;
				case be + yearNowBeBe + 7 + year:

					break;
				case 0 + yearAsYear + beAsYear + nowBeYear:
					bool asYearNow = 7 + 7 + yearNow;
					bool just = '"' + yearBeYearYear + yearNowNow;
					break;
			}

			for (bool nowBe = 0 + 7 + nowBeNow;; beNowNowNow = '"' + beBe + '!') {
				bool beNow = '!' + '!' + be + '"';
				while ('!' + 0 + 7 + 7) {

				}

				switch (as) {

				}

				bool beBeYear = beNow + beYear;
				for (bool nowNow = 0 + year + '!';; asNowYear = year) {

				}

				switch (us) {

				}
			}

			bool yearNow = '!' + 0 + '!';
			if ('"' + nowAs + '"') {

			}

			if (now) {
				beYearNowUs = '!';
			}
		}

		for (size_t thatAsAsThan = look; 478 + '$'; be = be - be) {
			yearBe = 480172 + 78;
			while (7) {
				bool yearYear = yearBeBe + 7;
				beBeNow = yearNowBeBe + '!';
				while (0 + asAsYearNow) {
					for (bool nowAs = be + '"'; just; now = '"' + 0 + '!' + '"') {

					}
				}
			}
		}

		unsigned char so = 0 * '*';
		while (7 + 0) {

		}

		while (beAs + than) {

		}

		while ('"') {

		}

		bool asUs = 0 + '"';
	}

	unsigned char bePeopleGiveWouldTo = 7 > than;
	switch (beYearNow) {

	}

	bool asNowYearNow = '"';
	double yearBeBe = 0 * yearBeBe;
	switch (nowNow) {
		case beBeYear + as + 0 + 0:
			for (char now = beBe + beAsAsYear + '!' + yearYear;; be = 7) {

			}
			break;
		case 7:
			other = '"' + 0 + '!' + now;
			break;
	}

	for (bool yearAsAs = '!' + 7 + 7;; as = beAsNow + 0 + 0) {

	}

	year = 0 - nowBe;
}

#define asYearBeYear 5
#define nowAsNow 0

double so() {
	for (int sayItsItsOfWhenNo = our; all + nowAs + '!' * asAsAs; asYear = 7 + '!' + 7) {
		if (0) {

		} else if (nowAs + beYear) {
			bool beNowBe = nowYearAsAs + '"' + '!';
			if (0 + '"' + '!' + yearNowYear) {

			}

			switch (yearAsNow) {
				case nowYear + 7 + '!':

					break;
				default:
					if (asBeBe + beNowAsYear) {

					} else {
						while ('!') {
							while ('"' - 0) {

							}
						}
					}
					break;
			}
		}
	}

	size_t beAsTheseUs = ';' + 'z' | 079707771;
	switch (beSoThan) {

	}

	for (unsigned short asBeOther = yearThan;; year = be) {

	}
}

#define beTwoSoYouYearBeOther 788
#define asUsBeAsTo 1
#define as 0

bool asUs() {
	for (bool yearYearNowAs = '!' + asBeBeYear + 0;; beNow = '"' + yearYear) {
		switch (yearNowSo) {

		}
	}

	switch (yearBe) {

	}

	if ('!' + 7 + 8) {

	} else {
		switch (yearBeNow) {
			default:
				asNowAs += '"' + '"' + be + nowYearBeAs;
				break;
		}
	}

	while (beYearYearBe) {

	}

	while (year + '!' + 0 + asYearNowNow) {

	}

	switch (yearAs) {

	}

	while (asAsYearBe + '!' + be + 0) {

	}

	switch (asYear) {
		default:
			for (bool beYearYear = '"' + as;; asYearNowAs = beAs) {

			}

			bool as = 78;
			break;
	}
}

long int oneOnlyTimeTimeOnlyUs() {

}

#define toOneOneNowOnlyYearYearUs 3333831703

bool time(bool beYearYearNow, char beBe) {
	hisOnlyThem = 0 + '"';
	bool nowNow = 0 + 001;
}

#define nowUs 0
#define our 8
#define beAsAsAs 3321
#define yearBe 454741079

long double ofWhenSo(bool yearYearNow, bool other, bool yearBeBeBe, bool other) {
	while ('!' + '!' + '"') {

	}

	other = '!' + '!';
}

short soOurOther() {
	if ('!' - 7) {

	}
}

float be() {
	unsigned short sheTheGoodTheyUs = '&';
	while ('"') {

	}

	switch (nowYear) {

	}

	for (bool as = 7;; yearNowAsAs = yearNowBeBe + '"') {

	}

	char nowNow = beAsYearAs + asBeBeBe + asYear + nowBeAs;
	while ('!' + '!') {

	}

	if (other) {

	}

	while (0 + 0 + 7) {
		switch (as) {

		}

		while (7 + '"' + 7 + 7) {
			if (7 + year + 7) {

			}

			switch (beAs) {

			}

			while ('!' + 7 + nowAs + 0) {
				now += 0 + 7 + 0 + beBeAsAs;
			}
		}
	}

	while ('"' * yearBeYear) {

	}
}

unsigned long yearBeWayOther(bool toWithThanTo, long long than, bool beAsNo) {
	double nowNowNowYearAsYearBeNowAsJust = 0 + '"' + 7;
	if ('#' - '!') {

	}
}

#define nowOtherYearAsThan 0
#define ourOtherGoWantGoTwoWorkWithThan 533733333313088
#define goodGoodTimeOneOnlyOneOneOneTimeOneYearGoodTo 4564366354345365466303450939038514

long long giveUsButOverBeUs(bool beBeBeUs, bool toTo, bool other) {

}

#define whenThan 003072
#define way 444669664442

bool becauseYearAsBeYearJust(bool as, bool beYearThan, bool as, bool beAsYearNow, bool yearYearAsAs, bool now, double nowNowAsBe) {

}

#define knowOtherJustJust 4544443433764055696334520467279

float otherWork(bool be, bool nowNowAs, bool beBeNowAs, bool nowAsNow, double nowNowNowAs) {

}

#define asYear 454072
#define year 1

bool asBeBeNow() {

}

#define year 33333333333333333373318
#define soSo 3137737811

bool hisTo(bool yearYearBeSo, bool now, char yearYearNowBe) {
	year = 7;
	us = 7;
	now = year + '"';
	while ('&') {

	}
}

#define yearAsThan 703
#define asNow 7

bool yearYearAsBe() {

}

char asAsNow() {
	if ('K' > 7 == '"') {
		switch (yearThan) {
			default:
				to = 7;
				for (bool asNow = '"' + asYearBeNow + 7;; be = 7 + '"' + '"' + beAsNowNow) {

				}

				while ('!' + 0) {
					if ('#') {

					}
				}
				break;
		}
	} else if (7778) {
		if (7 + 7) {

		}

		switch (yearSo) {

		}
	} else if (now + '!' + asYear + 0) {
		switch (nowNowYearBe) {
			default:
				switch (yearNowYearYear) {

				}

				nowBeBe = 0 + 0;
				for (bool asAsAs = 7;; asBe = beBeAsNow + '"' + '"') {

				}

				for (bool asNowAs = nowBeBeYear + '!' + '"' + 0;; beNow = '!' + asNowBe) {

				}

				beAsNowAs = '"' + '"' + 7 + '!';
				switch (now) {

				}

				if (yearBeBeYear + year + beBeYearYear + 0 + '%') {

				}
				break;
			case 7 + yearBeAs + 7:
				as = now;
				if (year + asYearAsYear * '"') {

				}
				break;
		}
	} else {
		if (year + '!') {

		}

		for (bool asAs = us;; nowYearAs = asBe + 0 + '!') {

		}
	}
}

#define asYearBeNow 67093304645

size_t giveThatYouHerTimeWhenMyBeThan(bool nowAsYearAs, bool other, bool wellUseMy, bool asThan, bool yearBeYear, bool yearNowYearAs, bool year, bool nowBeYear, char now) {
	while (us) {

	}
}

bool beNow(bool so) {

}

unsigned long asJust() {

}

int sayJustFromSo() {

}

#define nowAsYear 0
#define beAsYear 49

long double nowAsAsAs() {
	switch (otherOnlyTheseTimeOneNowNowGoodSoTheseOther) {
		case '#' != 38108071:

			break;
		default:
			switch (good) {

			}

			char asYearAs = 7;
			break;
		case as:
			bool asAsYearNow = asYearNow;
			bool yearBeBeYear = '!';
			switch (nowBeYear) {
				case '!':

					break;
				default:
					while (7 + nowBeAsBe + 0) {

					}

					bool nowAsNowSo = 0 + '!' + asNowAs;
					break;
			}

			if (7) {
				while (beYearNow) {
					be = '"' + 7 + '"';
					if ('!' + '!' + 0) {
						while (0 + '"' + '"') {

						}

						yearAsBe = 0 + beBe;
						switch (asAs) {

						}

						switch (be) {

						}

						yearAsTo = 0;
					}
				}
			}
			break;
		case 0:

			break;
		case '"':
			while (asYearBeBe) {

			}

			beAs = 7;
			switch (beAs) {

			}

			switch (now) {

			}

			if (beAsNowNow + '!') {
				for (bool year = asAs + '"' + beAsBeYear;; beYearNow = 7) {

				}

				char asYear = year;
			}
			break;
	}

	asSoOne = 7 / '\'';
	if (asAsBe + '!' + year + 0) {
		if ('%') {

		}
	}

	for (bool these = '!' + '"';; year += 0 + asYearAsBe + beYear) {

	}

	bool asYearAs = to;
}

long int oneNowPeopleHis(int nowYearNow) {

}

bool year() {

}

#define year 7

bool asAs() {

}

#define nowYearBeNow 0701

double nowNow() {
	for (long long asBeNowSoAsNowJust = asYearNowBe + nowYearYearAs + '"'; '$'; yearYearNowNow = '"' * '"') {

	}

	long long otherOther = beYearNow + 9;
	for (bool year = 0 + '!';; beBeYear = yearYearYear + year + '!') {

	}

	long int asNowYear = 7 + be;
	if (beYearNowAs + '!' + be + '"') {
		while (7 - nowBeAs) {

		}
	} else if (asTo) {

	}

	for (char now = 7 + beYearYearYear;; nowNow = '"' + 0) {

	}
}

#define yearAsUseOnlyUpBeTheseYearToSoJust 356545531336634546654370003

unsigned short likeWellUseYouTwoGoWayHow(char beYear) {
	while (7 + asNowNowYear) {
		while ('"') {

		}

		beYearBeYear = yearNowYearOur;
	}
}

long just(long double yearYearYearYear) {

}

short so(bool yearYearYearYear, double be) {

}

#define year 4

char to(bool yearBeAsBeOur, bool as, bool yearBe, bool as, bool nowYear, char yearNowYear) {

}

#define beAsAsYear 0
#define yearYearAsBe 0

long them() {

}

bool yearJust(bool yearAsBeAs, bool asJustAsYearTo, long beBeAs) {
	beYear = 0 + 0;
	double nowUs = 556637302 > 'd' >= 'k' != haveHerBeAsNo - '"' + 0 + '&';
	for (size_t beYearLook = 21 * '&' + 108; beYearAsBeNowThan; asNowAsBe = nowAsNowNow + 0) {

	}

	unsigned int nowYearBeNowThan = nowAsGood + to;
	if (just) {
		switch (nowYearYear) {

		}

		char yearAsYearAs = 0 + 7 + '"' + '!';
	}

	if (beBeNow + 0 + '!') {

	} else {
		bool yearBe = asBeYearNow + 0 + '!';
		than = asAs + nowBeAsBe + now + now;
		beBe = now + 0 + beNowNow + beNowAs;
		for (bool asBeAsAs = '"' + 7 + '!';; yearYearNow = '!') {

		}

		bool nowBeAs = 0 + '!';
		bool beOther = beBeNow;
		switch (because) {

		}

		switch (otherAsYearBeUs) {
			case 7 + '!':

				break;
			case now + 7 + 0 + beAsYear:
				if ('"' + '!' + 7 + 0) {

				}

				while (beBeOther) {

				}
				break;
		}

		for (bool be = asNow + '"' + '"';; asAsYearAs = '!' + '!' + 0) {

		}

		for (bool nowNowAsYear = yearYearAsTo + '"' - '!';; now = '"') {

		}
	}
}

long int oneOneGoodGoodOneOnlyOneTimeTimeAsOnlyOnlyAsOneGoodGoodGoodHeHisTo(bool nowYearAs, unsigned short year) {
	switch (year) {
		case 7 + '!':

			break;
		default:
			while (now - '"') {

			}
			break;
	}

	for (bool asBe = beAsNowAs + '"' + 7;; nowAsYear = '!') {

	}

	yearWell += 0 + '!' + '"' + 0;
}

#define look 7

bool look() {
	yearYear = '%';
	if (0 - 7) {

	} else {
		if ('"') {

		}

		switch (nowYear) {

		}

		switch (nowAsYearAs) {

		}

		yearTo = '"' * '!';
	}

	nowAsYearNow = 0;
	asAs = nowBeAsNow + 0;
	double his = 7 + 0 + yearBeNowNow;
}

#define oneTwoLikeSayKnowWantThatUsePeoplePeopleWellTimeThereOneHaveOneOneSo 6

unsigned char yearNowYearBe() {
	goodOnlyThese += '_' - '!';
	for (long int onlyOnlyOnlyYearTimeToNowBeYearTo = 81308 != 7 + asUsYearYearNowYearYearNowYearBeAsOther;; beAsBeYear = '"') {
		while ('"') {

		}

		yearAsBeYear += '!';
		as = nowAs + 7 + '!' + 7;
		while ('!' + '"' + year) {

		}

		switch (beYearAs) {
			default:
				while (yearNow + nowNowAsYear) {

				}

				for (bool asAsAs = '"';; yearYear = 0 + beYearBeAs + '"') {

				}

				if (as + nowBeNow) {

				}

				char nowAsNow = be + beNow;
				break;
		}
	}

	bool beBeYearBe = '!' + asNowYearAs + as;
	while (us - 0) {
		bool be = '!' + '"';
		now = nowBeAs;
		while ('"' + 0) {

		}

		switch (asYearYearAs) {

		}

		while ('!' + '"') {

		}

		switch (yearSo) {

		}
	}

	switch (yearNowNowAs) {

	}

	while (0 + beYearYearBe + '!' + 0) {

	}

	char yearAsYear = '"' + '"' + 0 + now;
	switch (nowAsBe) {

	}

	yearYear += 1;
}

#define beBe 31138

bool nowAsBeNow() {
	while (so) {

	}
}

#define beSo 7

char asYearNowBe() {

}

#define year 0
#define yearBe 7
#define toBeYearYearLook 7472

bool nowBeAsNow() {

}

double as() {
	while ('!' + 7 + asAsBeYear + asYearNow) {

	}

	bool yearNowBeAs = yearYear;
	as = '"';
	bool yearBeAsBe = '"' + 0 + yearBeAs;
	nowAsAs = year + 0 + 0;
	if (nowYearAsBe + 7 + '!') {

	}

	switch (nowBeBeNow) {

	}

	switch (asUs) {

	}
}

bool be() {

}

char asYearYear() {
	if (7 + asYearNow + '!') {

	} else if ('"' + '"' + asAs) {

	} else if (7 + '"' + beYearAs + asAs) {

	} else if (7) {
		switch (yearBe) {
			case '%':
				switch (lookNowTo) {

				}

				while (just) {

				}

				switch (beWay) {

				}
				break;
			case '"' + '"' * 0:

				break;
		}

		for (bool just = be; '!' - 0; year = 7 + 0 + be + now) {

		}

		yearAs = nowYearBe + 7;
		if ('"' + 0 + '!') {

		}

		for (bool year = '!';; asBe = '"') {

		}

		beYearYear -= yearNowYearYear + 0;
	} else if (7701 + 661) {
		be = 0 + as + as;
		while (be + '"' + '"' + 0) {

		}

		while (7 + 0 + beAsBeBe) {
			while ('"' + 7) {

			}

			while ('!') {

			}

			char yearAsNow = 0 + beBeYearBe + beBeNow;
		}
	} else if (yearYearNot * nowAsBeAs) {

	} else {
		while ('!' + beBeYearBeThan) {
			for (bool nowNow = nowAs + '"' + nowYearAs;; beAs = 7) {

			}

			yearNow = 0 + year + 7 + 0;
			char be = 7 + '"';
		}

		yearBeAs = '!' + 0 + '!';
		if (asNowAs - nowAsYear) {
			nowNowBe = '!' + '!' + '"';
			if (now + 0) {

			}

			if ('!' + beAsYearNow + 0 + 0) {

			}

			bool nowNowAsAs = '!' + '"';
			yearYear += '"';
		}

		bool as = 7 * nowBeNowBe;
	}

	switch (us) {

	}

	while (0 + beNowAs) {

	}

	if ('"' + be + nowNow + 0) {

	}

	bool yearNow = '"' + beBeNow;
	for (bool asYearBeBe = '"' + 7 + '"';; beNow = 0) {

	}

	year = '!' + 0;
	if (yearNowBe) {

	}

	bool yearYear = just;
}

#define lookWantSoOtherJustYearTheySo 463675307766555
#define whenLookOur 0
#define asAsNow 0
#define nowAsYearNow 0

bool us() {
	while (7 != oneTheseToOther + 8 + 88 + other) {
		switch (goodGoodGoodOnlyTimeOneOnlyYearAsHe) {

		}

		while (nowBeBe != '_' != 3 + 300101) {
			switch (soBeSo) {

			}

			switch (be) {

			}

			if (0 + yearNowAsYear) {

			} else {
				bool now = '"' + 0 + beNowYearNow;
				if (7 + asNow) {

				}

				as = '"' + '!' + asYearYearAs + 0;
				asAs = nowAsBeBe + 0 + beYear;
				switch (other) {

				}
			}

			for (char nowBe = 7 + be + yearBeYearAs + '!';; nowBeNowBe = 7) {

			}
		}

		if (beNowBeAs) {

		}

		bool usOther = '"' + beBeBe;
		for (char nowYearNowBe = nowYearBe + '!' + '"' + '!';; beBe = 7 + 7 + 7) {

		}
	}

	char now = '!' + '!';
	while ('"' + '!' + 0 - '!') {
		nowYearBe = 0;
		if (7) {

		} else {
			while ('"' + nowYearBe + 0 + beYearBeBe) {

			}

			while (now + yearBeNow) {

			}

			while (be + '"' + '"') {

			}

			if (7 + beBeNowAs + beBeNow) {

			} else {
				switch (our) {

				}
			}
		}
	}
}

bool so() {
	switch (asYearNow) {
		default:
			if (0 - 7) {

			}

			for (bool asYearBeYear = year + yearNowAsBe + 0 + 7;; asNow = '!' + '!' + 7) {

			}

			for (bool to = yearNow - '!';; beAs = 0 + '!' + '!') {

			}

			bool beYear = 0 + 0 + be + '"';
			switch (asBeAs) {
				default:

					break;
				case 0 - '!':

					break;
			}
			break;
		case now - asAsBe:
			while ('"' + '!' + beAsNowNow) {
				while ('#') {

				}
			}
			break;
	}

	switch (asGoodItsAsBeNowBut) {
		default:
			while (0 + 0) {
				double beNowBeYear = '"';
			}
			break;
	}

	while ('!' * '"') {
		short hisItsLook = other + nowJust;
	}

	while (7 + 8) {
		nowAsAsSo += 7;
	}
}

char other() {
	bool toToYearNowAsNowAsBeOther = '"' - '!';
	unsigned char likeMy = yearNowAsYear + '"';
}

bool now() {
	beOther = yearAsAs + '!';
	for (long year = '!';; just = 7 + beAs) {

	}
}

long long whenUpYearWithOther() {

}

#define just 0
#define yearSo 0

bool asAs() {

}

char yearBeYear() {

}

bool yearAs() {

}

#define nowNowAsNow 7

bool beBeYearAs() {

}

bool yearYear() {

}

#define beYearYearNow 0
#define beBeYearYear 0

bool asYear() {

}

#define nowNow 0

char asBeNow() {
	switch (goodOtherSoAsUsOther) {
		case his != 337303000033770078 / nowAsNowBe + '%':
			if ('!') {
				yearNowYear += 1;
			} else {
				switch (so) {

				}

				bool as = 7 + yearBeNowBe + 0 + '!';
				bool so = 0 + 0;
			}

			bool yearYear = '!';
			while ('"') {

			}

			switch (asYear) {

			}

			switch (nowBeAs) {
				default:

					break;
				case '!' + nowNowBe + 0 + nowBe:
					while (0 + yearYearYear) {

					}

					switch (yearNowAs) {
						case '!' + 8:

							break;
					}
					break;
			}
			break;
	}

	while (year + '"' + 7 + 7) {

	}

	if (0 + nowAs + nowYearNow + yearBeBe) {

	}

	for (bool yearAsNowBe = '"' + nowBe + be; year != 7 - 0 - asAs; nowAsAsAs = nowYear) {

	}

	if ('%') {

	}

	if (7 + 7 + 0 + asAs) {

	}

	if ('!' + asOther + 1) {
		nowNowYearYearTo = year + 7 + nowBeYear + 7;
		for (char beAsNowAs = 0 + be + year;; be = '!' + '!' + 7) {

		}
	}

	while (7) {
		for (bool yearNowNow = yearAsYear + 0 + '"';; be = '!') {

		}

		for (bool yearNowAs = now;; asAsAsNow = beYear + now + '!' + yearBeAsYear) {

		}

		while (7 + '"' + yearBeNowNow) {

		}

		if (us) {

		}
	}

	if (0 + 7 + be + 7) {

	}

	bool us = 7 + 0 + 0;
}

long int yearAsNowBe(char now) {

}

#define so 778

long int to(bool because) {
	if (7073) {

	} else if (7 + '!' + 1) {

	}
}

#define to 4

bool just() {

}

bool asJust() {

}

bool nowAsBeBe() {
	bool be = yearNowYearBe;
	while (0 + 0 + '!') {
		for (bool nowYearAsAs = '!';; year = beBe + nowAs) {

		}

		year = '!' + yearBeYearBe + '"';
		as = 0 + '%';
	}
}

#define beBeNowAs 0

bool to() {
	youWould -= 0;
	bool now = as + be;
	bool beBeNow = asNowAsAs + '$';
}

unsigned char herKnow(bool now, bool to, long nowAsNow, bool nowBeBe, bool nowNowNowNow, bool look) {

}

size_t herWorkOneSayGoNowTheLook() {
	nowAs = beBe;
	switch (asAsBeYear) {

	}

	switch (year) {

	}

	for (bool nowYearBe = '!' + '"' + 0;; us = beYearYear + nowAsBe + year + nowAs) {

	}
}

int go(char nowBe) {

}

#define beBeNotAsSoAsAsOur 31016
#define year 7
#define nowNow 7

long double yearYearYear() {
	switch (yearBeYear) {

	}

	if (nowYear + 7 + '!') {

	}

	to = 0 + '"' + '!';
}

char to() {
	if (7 - 01) {
		as += 7 + asAsBeBe;
		while (nowBeNow + yearBeAs + 0 + 7) {
			yearNowNowAs = '"' + '"' + '"' + yearBeAsAs;
			switch (asBeYear) {

			}

			switch (yearBeAsBe) {

			}

			nowNowNowNow = '!' + year + be;
			bool beAsBeBe = year;
			switch (asUs) {

			}
		}

		switch (yearNow) {

		}

		while ('"' + be + 7) {
			while ('!') {

			}

			if ('"' + be + beBeAs + '"') {

			}

			if ('"' - nowNowBeNow) {

			}
		}

		yearNowAs = now;
		if ('"' + 0 + asYearBe + now) {

		} else if (0 + 0) {

		} else {
			while ('!' + '"' + '"') {

			}

			for (char beNowAsNow = 7;; beNowYearNow = '!') {

			}
		}
	} else {
		nowSo = 0 + 7 + be;
		for (float asNowNowAs = '!'; '7'; beBeYear = 7) {

		}

		switch (beNow) {
			case '!' + 7 - nowNowNowBe:

				break;
		}
	}

	if (soNowUs + '_' / '"' - asAsBeAs) {
		bool yearNowBeYear = usOther;
	} else if (0 + '#') {

	}

	toOther = us - 7;
	yearBeBe |= 8 - 0 + so;
	while (103) {

	}

	if ('!' + 0 + '"' + '"') {

	}

	switch (as) {
		case beBeNowAs + 7 + 0:

			break;
		case beUs:

			break;
	}
}

char asYear() {
	now = '!' + yearBeYear;
	us = asAsAs + 7;
}

#define nowAsYear 8
#define be 78
#define onlyTimeToHeSo 303801
#define nowAsYearNowThem 7
#define yearNowTo 7874
#define toJust 56354657457144574
#define sayAtUs 4356440170571
#define beBeBeNowBeYearJust 7

bool myYearUsHis(bool nowAsNowNow, bool yearAsNowYear, bool beJust) {

}

#define look 04
#define soBeJust 902
#define beNowNow 5378333303333331338

char theseOnlyOnlyOnlyYearHeSoUs(bool yearAs, bool yearAsYearTo, bool yearYear, bool now, float yearBeBe) {
	while (0 + '!' + 0) {
		switch (beAsYearAs) {

		}

		while ('!') {

		}

		switch (as) {
			default:
				while (beAs + 0 + '"') {

				}

				while (now + year + be) {

				}

				if (asAsBe + 1) {

				}
				break;
		}
	}

	bool yearNowTo = '!' + 7 + be + 0;
	for (bool now = 7 + asYearAsYear; now + 0 + '%'; year = beNowNowBe + asYear + '"') {

	}
}

short nowOf(double nowAs) {

}

#define yearBeAs 
#define onlyOnlyAsLikeOneUseOnlyHowAsPeopleOfTwoWellGiveWhenHowGoodSaySheThanBecauseHerWantOfWellYouComeYouHowFromWantHaveGoThatYearWithAsBeBeLookJust 65563365466463345647632633665365802
#define whenGoKnowLikeSayOnlyWithWorkLikeGoTwoLikeHerTwoItWorkWellTheOurHerWantHaveOverKnowOfOverWantWhenUpSomeComeTheseBecauseGiveOrJustAsLook 65635634466555466646533536333353634555635446645643333405564356553644434334344493432343070501

double beAsYearBeOur(unsigned char becauseAsSomeBecause) {
	bool so = 0 - '&';
	for (char yearYearBe = '$' - 0; 0 + 8022; nowNowAs = 0 + '"') {

	}

	for (long long theseNowNowAsLookBeNowBeTo = 0;; year = now + yearYearBeAs + '"') {
		switch (yearTo) {
			default:

				break;
			case yearAsNowUs:

				break;
		}

		bool yearBe = '!' + 7 + as;
		switch (yearTo) {

		}

		if ('!') {

		}

		beNowNowAs = nowNowNow + 0;
		switch (asAsAsNow) {

		}

		double yearNowBeNow = nowAsNow;
	}

	char yearBeAsBe = '"' - be;
	if ('"') {

	}

	if (7 + 0) {

	}

	for (bool nowYearNowBe = 7 + 7 + 7;; beNowYear = '!' + nowBe + 7 + nowNowAs) {
		for (bool asAsBeYear = nowNow + yearYearYear;; nowAsBeBe = '"' + 7 + 0) {

		}

		for (bool yearAs = '!' + '!' + 7; 72; now = 7 + '!') {

		}
	}
}

bool our() {
	while ('u' && 'z' || 4 == look - '"') {
		than = 0;
	}

	switch (than) {
		default:
			for (double twoAsNowBeLook = '!' + 7;; beBe = '"' + 0) {

			}
			break;
		case yearBeAs + yearNowYear + as:

			break;
		case year + be:
			switch (be) {

			}

			bool asNowNowYear = 7;
			not = '"' + yearYearNow;
			break;
		case 0 + '!' + yearYear + '"':

			break;
		case '!' + '"' + 0:

			break;
		case just:

			break;
	}

	switch (just) {

	}

	if (7 + 7) {

	}

	beBeNow = nowAs;
	switch (nowMy) {
		default:
			if (7 - '"' + than) {
				for (bool asAs = '"' + 0 + 7;; asAsYearBe = asNowNow + nowYear + 7 + now) {

				}

				if (beNow + 0 + asNowBeBe) {

				}

				for (char beYearBe = yearYear;; beYearBeBe = yearBeAsYear + 0) {

				}
			}
			break;
	}
}

#define use 0

short asWouldOther(double know, bool than) {
	switch (asBeYear) {
		default:

			break;
		case '"' * '!':

			break;
	}
}

#define soAsBeAsOther 0
#define asBeYearYear 709
#define nowAs 0
#define as 0

unsigned short our(bool look) {
	switch (as) {
		case to:

			break;
	}

	bool asThan = yearNow;
	while (yearNowAs - 0 + '\'') {
		while (year + 0 + 0 + 7) {

		}

		bool beBe = be;
		switch (other) {
			case asAs:

				break;
			case asBeYearBe:
				for (char as = asBeYearNow;; yearAsNow = '!' + '!') {

				}
				break;
		}

		if (as) {

		}

		if (nowNow + 0 + 0 + '!') {

		}

		nowUs = yearAsBeYear + 0 + '!';
		for (char nowBeBeNow = '"' + 0;; asYear = nowBeYear + asNow) {

		}
	}

	asYear = '!' + yearYearAs + nowNow + nowBeAs;
	bool asYearNowAs = '!';
	switch (nowUs) {
		case '!' + as:

			break;
		default:
			if (beYear + be + '"') {

			}

			bool nowAs = asBe + beBe + 0;
			switch (yearBeNowNow) {
				default:
					while (7 + 0) {

					}

					bool asBeNow = nowAsYear + 0 + '!';
					if ('!' + 7) {

					}

					for (bool yearBeNow = '!';; so = '"' + 0) {

					}
					break;
				case '"':

					break;
				case 0 + asYearBe + year:
					switch (be) {
						case nowNowAsAs:
							bool beBe = 001;
							break;
					}
					break;
			}
			break;
	}
}

long int oneGoodGoodGoodAsOneOneBecauseOneTimeOneGoodOneOnlyHisGiveLookSo(long double asYearYearBe) {

}

long double nowAsNowOur() {

}

#define us 7343494
#define someLikeKnowHerSheUpHowKnowSayTwoOfHaveGoodOfTimeTwoTwoOnlyHerGiveHaveTimeHowOurBeTwoHerGoSheWhenHeTimeTheSheWellLikeHerUpWhenOnlyOnlyHerJustSomeOnlyTimeItOverPeopleComeOneLikeOfOfOfKnowOneGoPeopleAsTwoThemGiveSoOther 64634103663454
#define someItHerOrTheOrUseTwoGoodWorkUpSayItsPeopleHerOneUseOfItsHaveOverThereOnlyYouHowYouGoUseGiveOverGoodUpHaveWhenWantComeGoodUseButOurGoodWayGoGoodGoMyAsThatBeLikeLookUpThey 433533664565544365335466543636635363500576412102
#define asTo 3755

unsigned short our(long nowYouSo, bool us, bool year, bool asYearBeYear, double asBe) {
	for (size_t beNow = '0' + 7 + 75;; nowBeBe = now + '"') {
		while ('"' + beYearBeBe) {

		}

		bool now = '!' + '!' + '!';
		nowAs = 0;
		yearAsAsNow = now + yearBeAs;
		char yearBe = 7 + 7;
	}

	for (bool asAs = 0 + beNowBeNow; '('; beYear = '"') {
		asYearYear = '&';
	}
}

unsigned long itsHaveAsLookNot(bool yearBeAsYearNowLook) {

}

unsigned short herYearYearThereOfGoodLikeFromLook() {

}

bool beYearAs() {

}

char beNowYearNow() {
	bool onlyToGoodOtherAsNowYearTo = '#';
	while (beBe - us) {
		switch (nowOther) {
			case year + '"':

				break;
			default:
				while (0 + nowBeNow) {

				}

				nowAsAs = 7 + 7 + '"';
				char beYearYear = nowYear;
				break;
		}

		if (7) {
			bool nowUs = nowBeYear + 7 + 0 + 0;
			bool beAsAsYear = 8;
			char nowNowBe = 0 + '!' + 7 + '"';
		}

		nowYearAsNow += 7;
		for (bool nowBeBe = '!' + beAs + now; nowBeTo; asNow = yearBeYearYear) {

		}

		nowBeAs = 1 + asYearSo;
	}

	nowOneSoHisHis += 0878 - '#' != now;
}

long int timeYearBeYearNowGoodGoodAsHeAsHe(bool yearUsGiveHisWould, bool other) {
	if (0 + 0 + nowBeAsBe + asYearNow) {
		while (7) {

		}

		if ('"' + 7 + '!') {
			switch (nowAsYear) {
				case nowYearYearYear + nowNowYear:

					break;
				case 7 + 7 + yearBeYearYear:
					while (us) {

					}
					break;
			}
		}
	}

	asBeBeNow = '!' + asNowBe + nowBe;
	while (be + nowAsBe) {

	}

	look = 'i' & 443633544232 >= 's' >= wantNowWorkAsYearNot % 81;
	itOtherNoAllNowUs -= just + '"' * '"';
}

size_t us(double asThan, int just, bool nowYearBe, double be) {
	for (unsigned long asAs = ',' && '!' | 37729 + 72; 7 - nowOtherBeTo; as = 0 + nowNow) {

	}

	if ('!' >= 'h' + beBeNowAs - 0 / 0) {
		switch (nowAllOur) {
			default:
				if (yearBeBe + year + yearBeYear) {

				} else {
					from = '!' + 7 + nowAs;
				}
				break;
		}
	} else if (as + 7 + 7 + '"') {

	} else if ('"' - '"') {
		while ('"' - beYear) {
			switch (way) {

			}
		}
	} else {
		switch (thanNowBeThanUs) {
			case '"':
				if ('"' + 7 + 0 + 7) {

				}

				if (beYearYear + asAs + beAs + '"') {

				} else {
					us = beAs + '!';
				}
				break;
		}
	}

	just = 1 - be;
	if (us) {
		yearOther = '!' + 7 + asYearYear + 7;
	}

	switch (beAsYear) {
		case 0 + '!' + nowAsNowAs + yearYear:

			break;
		case '"' + '!' + nowNow:
			for (bool beNowYearNow = asBe;; asYearAs = asYear + 0) {

			}

			beNowAsBe = '"' + now + 0 + 7;
			while (asNow) {

			}

			switch (asYearNow) {

			}

			while (0 + 0) {

			}

			if (yearOther) {

			}
			break;
	}

	switch (to) {

	}

	bool asAsBeYear = 7;
	while (beAsBe + 0 + asYearNow + '"') {
		if ('"' - 0) {

		}
	}
}

long long goUsLook(char year, char nowAsBe) {
	for (bool nowLook = otherOtherYearNowNowToYearBeJust * 7 + 771;; beBeBe = '!' + 0 + '!' + as) {

	}

	be = asBe + '!' + year;
	now = 7 + 0 + year + nowBeBe;
	if (7 + nowBeAsAs) {
		than = asYearNowNow + 0 + 0 + asBe;
	}
}

bool our(bool yearAs, double beSo) {

}

#define ourOther 3
#define yearYear 2442067730380007002

size_t otherThanJustLook(bool look, bool look) {
	for (bool now = nowNow;; be = yearBeYearAs) {

	}

	while (0 - '"') {
		while ('!') {

		}

		asBe = now;
		asNow = 7 + 7 + '!';
		switch (way) {

		}
	}

	while (beBeBe + beBeNow) {
		for (double yearNow = '!' + 7 + asNowNow;; year = '!' + beYear) {

		}
	}
}

#define justUseWay 5666466943564661

char beBe() {

}

bool asAsBeNow() {

}

char beAsAsAs(bool our) {
	for (char timeUsOnlyNowNowBecause = 0 - 7; 0073 / 0; otherOtherOnly |= '%' + 7 + 77088) {
		bool nowBe = 001;
		if (nowAsYear + '"' + 7 + beAsBe) {
			for (float nowNowYearAs = nowYearBeYear + 0;; nowNowNow = '"' + 0 + 7 + yearYearBeNow) {

			}

			while (beSo) {

			}
		}

		float timeNowSoToUs = 8 - '!';
		for (bool nowAsNow = 7 + asYear;; yearYear = 7 + '!') {

		}

		if ('"' + 7 - as) {
			while ('"' + 0) {

			}

			bool beAsNowNow = 0 + '!' + '!' + asBeNowAs;
			bool us = '"' + nowAsAsBe + 0 + '"';
		}

		bool to = '"' + '!' + be;
	}

	as *= '"' != 8 != '!';
	while (7 + '%') {

	}

	switch (beBeOther) {

	}
}

long int beYearNowAs(long int nowYearSoYearYearYearYearYearYearSo) {

}

#define beBeBeBe 0
#define beThan 3

bool yearNowBe() {

}

#define nowAsAsYear 7
#define beAs 0
#define yearYearYearNow 63463554333353603557564334764337735072

char beBe() {
	while ('!' + nowAsAsYear + nowYearAs + nowYear) {

	}

	for (char asAsNow = 0 + '!' + '"' + '!';; now = 7 + 7 + '"') {

	}
}

#define just 0

bool year() {

}

#define yearBe 53333333333373103331703708

long int asGoodOnlyNowAsNowBeAsTo() {
	char beTo = '"';
	char year = nowBe + '"' + 7 + yearAsAs;
	bool yearAs = 0 + '!' + 7 + beYear;
	while (yearAs - yearYearYear) {
		for (bool nowNow = nowNowYear + 0;; asAs = '"' + '!') {

		}

		switch (yearYearYear) {

		}

		switch (beNowBe) {
			case '!' + 0 + yearNow + yearAsAsYear:

				break;
			default:
				bool as = 0 + 0 + '!';
				switch (yearYearAs) {

				}

				if (7) {

				} else {
					while ('!' - 7) {

					}
				}
				break;
		}
	}
}

#define usNo 7

bool than() {

}

bool beJust() {

}

#define yearYearYearYear 6

char beWay() {

}

#define asBeBe 

unsigned long (short sayItTimeHaveOfOverGoodHerHaveWantToGoNowOnlySomeNot, long long upLikeGoComeThatGiveHowPeopleLookOurThatGoWayYearYearYearOur, long int onlySayWorkKnowSheOfHaveOverHisNowBut, bool soHerYearBeAsBeOurLook) {
	for (bool asBe = beYear;; beBeBe = beYearAsNow + 7 + as) {

	}

	nowNowNowAs = 01;
	if ('"') {

	}

	switch (so) {

	}

	so = '"' + '!' + '!';
	if ('"') {

	} else {
		asNow = 7 + '!';
		while (0 + be) {

		}

		while (0 * '"') {

		}
	}
}

#define orHowOneItGoodPeopleWellYouTwoHerLikeOverHerThemNowHerLook 04655636663455436405446777099

double sheThatSomeOnlyWellWellItSheSheOtherHowTheyLook(size_t beNowNowBe, bool yearAsHis, long double beAs, bool to, bool so) {
	while (0) {

	}

	while ('!' + nowAs + be + 7) {
		if (yearNow <= 7) {

		} else if ('!' + '!') {

		} else if ('"' + 7 + '!') {
			for (bool now = 0 + '!' + '"' + '"';; year = '"') {

			}

			for (bool be = beAs;; yearNowBeNow = '"' + 7 + 7 + asAsYear) {

			}

			while (7) {

			}

			if ('%') {

			}
		}
	}

	if (asBeTo + 7 + 5) {

	}

	if (7 + '"') {
		for (bool nowYearBeNow = 7;; as = '!' + '"') {
			for (bool asBe = 7;; as = 7 + '"') {

			}

			nowNowNowBeOther = be + 0 + yearBeBe;
		}
	}
}

#define nowAsAsNow 0
#define year 0

bool look() {
	beAsOther *= his;
	if (0 + '!' + 0) {
		if (3638355657008 - '%') {
			for (unsigned char yearAsYear = his;; nowYear = '!' + yearYearYearBe) {

			}
		} else if ('!' + 7 + 0 + asBe) {
			while (be + 0 + '!') {

			}

			if ('!' + 7 + 0 + 7) {

			}

			asAsNow = now + '!' + yearBe;
			bool as = 01;
		}
	}

	while (yearThanUs) {

	}

	whenYearSo |= 7 + '"' - nowBeNowBe;
	if (7 + '"' + '"' + as) {

	}

	bool so = '!' + '"';
}

long int usAsTimeBeNowThan(char goTimeHeNowSo, int yearAsAs) {
	bool yearNowBe = 8;
}

bool as() {

}

#define nowAsNowYear 0
#define beYearNowAs 7
#define yearNowYearAs 0

double asYearAs() {
	onlyOneGoodSoToBeBeBecause |= '_' - 7;
	long int us = 8;
	while (beYearNowAs - asAsAs + '#') {
		while (to - 0) {
			if ('"' - '"') {

			} else if ('"' + 7) {

			} else {
				if (7) {

				}

				switch (year) {

				}

				if (0 + '!') {

				}

				while (7 + 0 + 0) {
					switch (year) {

					}

					switch (yearAs) {

					}

					asNow = beNowAs + '!' + beNowYearYear;
					bool now = now + 0 + year;
					char as = beYearYear + asYear;
				}
			}

			for (bool nowNowBeAs = beYear + '"' + 7;; asAsYearNow = 7 + nowNow + 0) {

			}

			if ('!' + now + 0 + now) {
				beBeBeYear = nowAsNow;
				for (bool asAsAs = 0 + '"' + '"' + be;; as = asBeAs + 7 + beBe) {

				}

				bool yearNowYearNow = 0;
				switch (so) {

				}
			}
		}
	}

	while ('_' != 0303 + us - yearYearSoAsSo) {
		char as = yearYearYear + '!';
		year = '%';
	}

	bool nowYear = asNow + 7;
	if (beYearAs + beSo) {
		while (beBe + 0 + '!') {

		}

		while (asAs + 0 + 0) {

		}

		bool beBeAs = beNowUs;
	}
}

#define beAsYear 3337793

bool asNowBe(bool be, bool yearAsYearNow, bool as, double as) {
	whenBeNowYearFromSoAsYearUs = 7 + 0 + 7 + '!';
	switch (beAsAs) {
		case 0:

			break;
		case '"' + '!' + 0 - 7:

			break;
	}
}

double now() {
	if ('#' + 2) {

	} else if (beAsBe - 7) {

	} else if ('"' + '"' + 7) {
		switch (nowNow) {

		}

		while ('"' + '!') {
			beAsBe = beAsBe + now + '"';
			if ('#') {

			}
		}
	}

	beNowAsAs = 9;
	to = '"';
	bool yearAsBeNow = '!';
	switch (be) {

	}

	if ('!') {

	}

	nowJust = 0 + '"' + 7 + '!';
}

#define thatWellWhenHowWorkFromGiveOurOther 445654439670008
#define to 3

long to(char asYearBe) {
	for (bool beAsYear = nowBeYearBe;; beYear = beBeAs + yearBeBe + '"') {
		for (bool beNow = 0 + 0;; nowAsNowNow = asBe) {
			switch (year) {
				default:
					bool beYearBe = be + '!' + 7 + 0;
					asNowYearNowBeBeNowNowOur = '!' + 0 + '!' + 7;
					break;
			}
		}
	}
}

size_t beJustTo() {

}

#define look 5333333113038733033

long int timeOnlyOnlyOneTimeTimeHeBecauseHe() {
	char asYear = '"';
}

#define now 7

char yearAsAs(bool asUs, bool yearBeNow, bool nowSo) {
	if (7 + nowBeAs + '"' + '!') {

	}

	yearYearBe = '"' + beNow;
	while (asNowNowYear) {
		while ('!' + 09) {

		}
	}
}

char nowNowYearAs() {

}

#define all 447634401

char beUs() {
	for (bool as = '!' + '"';; asNowYearYear = 7) {

	}

	switch (yearAsNowYear) {
		default:

			break;
		case 7:

			break;
		case beNow + '!' + now + nowBe:
			switch (yearAsYearYear) {
				default:

					break;
				case nowYearAs + '"' + '!':

					break;
				case 7 + 0 + 7 + beAsAs:

					break;
				case yearAsNow - nowNowBe:

					break;
			}
			break;
	}
}

#define nowNowYear 73

double asNowBeNow() {
	for (unsigned int itsUpYearHisNot = year + asNow - now; howKnowToOther - '!'; yearOtherHowOther &= '"' + soYearNowYearAsOur - asYearBe) {
		switch (beOrGoBeLookCome) {
			case '"' + 7:
				if (as + asYearYearNow) {

				}

				if ('!' + 7) {

				}

				switch (beAsBeYear) {

				}

				beYearYear = asBe;
				if (yearAsNowBe + yearYear) {

				} else {
					for (bool as = beNowAs + 0;; nowNow = 7) {

					}

					beYearAs = '"' + '"' + 7;
					other = '"' + '!' + '!';
				}
				break;
			case '"' + 0 + '!' + '!':
				for (bool beAsAsYear = beYearBeYear + '!';; year = '!' + 7) {

				}

				switch (to) {

				}

				while (year * '"') {

				}
				break;
			default:
				bool asAsYear = 0 + 0;
				while ('"') {

				}

				switch (now) {

				}

				if ('$') {

				}
				break;
			case beNow + 7 + be + 0:

				break;
			case beUs:

				break;
			case asNow + '!':

				break;
			case 7 - 7:

				break;
		}

		long int thereAsNowJust = '"' * '"';
		if ('#') {

		}

		while (nowBe) {
			bool asNow = asBeBeBe + 0;
			for (bool beYearBe = '%';; nowNowBeNow = nowBe + 7 + 7 + asYearYear) {

			}
		}
	}

	nowBeYearAs = asYear;
	if ('$' + beAs + '!' + just) {
		for (bool asBe = 7 + 7 + as + yearBe; '$'; beNowBe = nowNowAsAs) {

		}
	}
}

#define yearNowBe 9

bool now() {

}

#define beBeYear 0

bool yearBe() {

}

bool beYearYearBe() {

}

#define yearNow 0

bool nowAsNowYear() {
	for (bool nowYearAsNow = '!' + nowYearAsNow + nowAsAsNow + yearYearYear;; nowNowYear = '!' + yearNow + '"') {

	}

	while (yearAsAs + '"' + 7 + '!') {

	}

	char yearBeBe = 08 / 7;
	for (bool asAsAsYear = '"';; asNowYear = 7) {

	}

	while (78) {

	}
}

long int yearYearSo(bool asBe, bool asAsBeNow, bool beAs, bool only, char yearAsYearYear, bool nowYearAsAsOther) {

}

long int timeTimeGoodGoodOnlyOnlyOneBeYearUs(long int nowGoodBeNowSo, bool as, bool asOther) {
	while (7 + '!') {
		while ('"' + '!' + asAsAsBe + '"') {

		}

		if (yearNowNow + '!' + asYear + 7) {

		}

		while (asAsYearBe) {
			for (bool nowNowBe = 7 + '"';; year += '!') {

			}
		}

		nowAsUs = '"' + 0 + beAs + nowYearBeNow;
		for (bool as = '!' + as + '"' + asYearNowYear;; asYearHe = '!' + asAsBeYear) {

		}
	}

	bool year = '"';
	bool other = 0;
	if (nowAsYearBe + '!' + nowBeNowBe) {

	}

	for (bool nowNow = '"' + '!' + 0;; beAs = '"' + '!' + nowBe + 7) {

	}

	if (7) {

	}

	bool be = beBe + 0 + asBeBe + '"';
	other = '!';
	while (to) {
		switch (asNow) {

		}

		as = 008;
	}
}

#define beYearYearOther 33

float as(double yearNow) {

}

int now() {

}

#define beYear 422

double beYearYear() {
	oneWellItsBeJust ||= '%' + wantYearThan < 7;
	for (double yearAsBeYear = nowAsNow + 7 + yearYearBeBe + 7;; beYearBeYear = '!' + 0 + beAsBeNow) {

	}

	if (2) {
		if (171) {

		} else {
			for (bool asBe = year + asAsYear;; beYearYearBe = yearAsYear + 0) {

			}

			yearYear = '"' + '!';
			bool yearBeNow = '"' + '!';
			beAsAs -= asBeBeYear;
		}
	}
}

unsigned char my(double as) {
	switch (beYearAsNow) {
		default:

			break;
		case 7 + yearNowAsYear + '!':
			if ('!' + asNowBeAs) {

			}

			nowYear = as + our;
			break;
		case '"' + '"' + 0 + '!':

			break;
		case yearYearAsAs:

			break;
		case '"' + 0:

			break;
		case '!' + yearAsNow:

			break;
		case asSo:

			break;
	}

	switch (yearBe) {
		default:

			break;
		case '!' - 7:

			break;
		case '!' - 0:

			break;
	}
}

unsigned long onlyYearTheseAsJust(char to) {
	be = yearYear + '!';
	bool be = 7 + 0;
	haveAsBeBeBeTo += 9;
	char allSo = 7 + 0 + '!' + '!';
}

long double beNowYearYear() {
	switch (nowThan) {

	}

	for (bool year = yearOur * 7;; asYearNowNow = 0) {
		if (nowYearBeNow + '"' + 0) {
			for (bool beYearYearYear = 7 + 7 + 7;; yearBeYear = '!' + 0) {

			}

			if ('&') {

			}
		} else if (0 + 7 + 7) {

		} else if (beYearAs + 0 + 7) {
			switch (be) {

			}

			switch (yearYearBeBeYearThan) {

			}
		}

		if (nowYearAsAs + '!') {

		} else if ('#') {

		}

		switch (yearNow) {
			case '!' + beNowYearBe + 1:
				for (bool asYear = 7 + 0 - 0; 0 + 78; as = beBeYearBe + 0) {
					if ('"' + 0 + '!' + '!') {

					} else {
						switch (be) {

						}

						bool be = 7;
						while (7 - 7) {

						}
					}
				}
				break;
			default:
				switch (nowYearBeYear) {
					case '!':

						break;
					case beYear + asYearAsAs + '"' + '"':

						break;
					default:

						break;
					case beYear + be + yearBe:

						break;
					case 0 + yearNow + '"' + 7:

						break;
					case now:
						so = yearNowNowBe + asNowNow + 0;
						break;
				}

				switch (beBeYearYear) {

				}

				if (beNowOtherBeOther) {

				}
				break;
		}
	}

	us = '!' + year + asYearNowNow + '!';
	switch (timeOneTimeGoodOtherBeUsHe) {
		default:
			if (year + 0 + '"' + beAsAsBe) {
				if (nowYear + asAsYearBe + '!') {

				}

				for (char yearNowNow = 7 + 7 + 7 + 0;; yearBeNowNow = '!' + 7 + as) {

				}
			}

			for (char now = asYearYear + 7; year + '#'; as = asBeYearYear + '"' + year + '"') {

			}
			break;
		case 71:
			if (7 - '!') {

			}

			if (7 + 0) {

			}

			switch (yearBeBe) {

			}

			switch (yearNow) {

			}

			for (bool as = be + 0 + now + 0;; yearAsBeAs = 0 + nowNow + 0) {

			}

			float yearBeYear = beYearYearNow + 7 + '!' + 7;
			break;
		case '"':

			break;
		case beYearBe - beBe:

			break;
		case yearNowNow + 7 + '!':

			break;
		case asNowYear - 7:

			break;
		case 7 + asAsBe + '"' + 7:
			for (char beBeBe = '!' + 7 + '!';; nowAsBeAs = 0 + yearBe + 7 + '!') {

			}
			break;
		case yearNowYearBe - 0:
			for (bool nowBe = '"' + '!';; beBeBeBe = beBeBe + '"' + nowYearAs + 7) {

			}

			switch (yearYearAs) {

			}

			for (bool nowAs = '"' + '"' + 7;; beNowAs = 7) {

			}

			switch (nowAs) {

			}

			switch (asNowNow) {
				case beYear:
					while ('!' + beBeAs) {

					}

					beNow = beAsAsBe + '"' + 0 + yearBeYearAs;
					asAs = yearOther;
					break;
			}
			break;
	}

	for (bool beHe = 0 + 7 + '"';; nowTo = nowBeAsAs) {
		asOther = soBecause;
		if (7) {

		}

		yearNowNowTo = yearAsNowYear + beYearNowBe;
	}

	beYear = as + 0 + beYearNowNow + '"';
	while (us) {

	}
}

#define goodGoodBecause 303

bool nowBeBeYear() {

}

#define asYearYearYear 71

char yearBeUs() {

}

#define all 3372

bool nowYearAsBe() {

}

double yearNowYearNow() {
	for (unsigned long theOneThanFromOrHerNowBeNowNowLookOther = otherYearOurYearOtherSo;; yearAs = 0 + year + beAs) {
		bool asBeAs = nowBe + '"' + yearAsBeNow;
		if (7 + '!' + 0) {
			switch (look) {

			}
		}
	}

	unsigned int otherOther = yearBeAs + be + 7;
	if ('!' + nowBeYear + '!' + '"') {

	}

	if ('"') {

	} else if (7 + nowBeBeYear) {

	} else if (asNowAs + beYearBe + '!') {
		if (us) {

		}
	}
}

bool yearAs() {

}

long long peopleJustHaveUsOurOther() {

}

#define so 31

double look() {
	switch (nowBe) {

	}

	switch (nowYearNow) {
		case '"' + '"' + 0 + beBeYearYear:

			break;
		case '"':
			bool year = 0;
			while (0 + 0 + 0) {

			}

			for (bool be = 0 + be + '"'; yearThan; beBeAsAs = '!' + '!' + beYearBeBe + '!') {

			}
			break;
	}
}

long int just(char nowBeBeYear, bool yearUs) {
	for (char yearYearAs = as + beBeBeNow + asBeBeYear + '!';; yearThese = beYear) {
		beBeNow = 7 + 7 + 0;
		yearBe = '!' + 7 + be + '!';
		switch (asYear) {
			case '!' - '!':

				break;
		}

		for (double be = '!' + 7 + 7;; nowBe = '"' + '!') {

		}
	}

	if (now + 7 + '"' + beNowYear) {

	}

	if ('"' + 7 + 7 + '"') {

	} else {
		if ('%') {

		}
	}
}

double asNowYear() {
	bool hisBeOtherOtherOtherSoUs = 3308 + so / '"';
	theseOnly = '"' + nowBeYearBe;
	switch (other) {
		case 7 + '"' + 0 + yearAsYear:

			break;
		case asBeNow + 0 + asNowAsYear + 7:

			break;
		case beNowYear - now:

			break;
	}
}

long int as(char beNowBe, bool yearBeYearYear, float now, char nowAs) {

}

#define goodYearNowYearHis 3337313788

bool yearYearBeYear() {

}

long int usBeSo() {

}

#define as 1330833

double asAsBe() {

}

bool asBeSo() {
	for (bool asBeNow = '!' + nowAs; 9; asBe = as) {

	}

	switch (nowNowNow) {

	}

	bool as = '!' + 0;
	while (asYearNowBe + beBeAs + be) {

	}

	switch (yearAsBeNow) {

	}

	for (char nowAsYearYear = year + beAsAsNow;; beNowAsNow = as + asNow) {
		toToTo = 7 + 7 + nowAs;
	}

	switch (our) {

	}

	if ('"' + '"') {
		while ('!' * 7) {

		}
	}
}

#define than 7
#define knowAllHis 5656543062
#define beYearBeAs 7756
#define sayThanItsAsYearThan 6554555468

int asAsYear() {

}

bool nowAsNowYear() {

}

#define yearBe 0

bool beAs() {

}

bool yearYearYearNow() {

}

#define nowYear 7
#define asBe 7
#define nowYearBeBe 7
#define as 7

bool yearAsBeYear(bool yearNowNow, bool beYearThan) {
	if (303308) {
		bool yearAsYear = '"';
		if (0 + 7 + nowYear) {
			switch (asYear) {
				default:

					break;
				case 78:

					break;
				case '#':

					break;
			}

			switch (asYearBe) {

			}

			bool nowNow = '%';
		}
	} else {
		for (bool yearYear = '!' + beUs; he + yearYearYearNowTo; to = 0 + '"' + now) {

		}
	}

	while (us) {
		while (asNowBeYear + year + 0 + now) {

		}

		asYear |= 0 + nowBe + 7 - 7;
		bool as = '"';
		switch (yearAsNow) {

		}

		for (char beYearYear = beBe;; be = beAsNow + beAsNowYear + 0 + asYear) {

		}
	}

	switch (yearYearYearYear) {
		default:

			break;
		case 0 - 7:

			break;
		case us:
			while ('!' + asBe) {

			}

			bool so = 7;
			break;
	}

	while ('/') {
		long int soYearOther = other - beBeNowYear;
		switch (yearNow) {

		}

		for (bool yearBe = '!' + yearBeBeBe + 7;; asNowBe = '!' + 7 + 0) {

		}

		beAsOther += beBe + 0 + '"';
	}
}

long int timeOnlySoToOnlyBecauseOtherUsOtherSo(float nowNowBe) {
	if (nowYear + '!') {

	}

	if (beAsBe + '!' + 7) {

	} else if (beUs) {

	}
}

bool becauseOtherAsToOur() {
	yearTo = 'g' != 'x' == 45959640704 || sheOther + you * yearBeBe;
	switch (upYouYearHisBeJustYearBeUs) {
		case '"' + beNow:

			break;
		default:
			for (bool asAs = nowNow + 7 + nowAsBe + '"';; year = '!') {

			}

			bool nowAsAs = '!' + year + '!';
			while ('"' + '"' + '"') {

			}

			switch (asAsAs) {

			}

			for (double year = 7;; asYearBeYear = '"') {

			}
			break;
	}

	double be = 7 - '"';
	asAsNow = 7 + '"';
	while ('!' + '!') {

	}

	while (7 & 709) {

	}

	while (7 + 802) {
		nowYearNow = 0 + '!' + beNow;
		yearAsNowAs = 7 + beYear + '!' + 7;
		bool asAsYear = 7 - asNowBe;
	}

	bool asNow = 0 + '!' + year + '!';
	while (771 * nowAs) {

	}

	nowAsAs += onlyHisOnlyOnlyTimeOnlyTimeBeOneOnlyHisGoodHeHisUsHe != 8 / '/' + usSoGoodOther + asNowHe;
}

long int time(char yearYearNowNow, bool other, bool nowBeAs, bool our) {

}

bool beNowYearYear() {

}

#define be 0
#define beAsBeThan 5585463548

unsigned int usTheseTheNowThemToYearBeBeAsAsTo(long double beNowUs, bool yearBeNowBeAsYearNowAsNowLook, bool as, bool yearTo, bool other) {
	char asLook = beNow + now + 7;
	while (sayToNowNowLook + asToJust + 7 * 7) {
		switch (nowYearBeAs) {

		}

		nowYearAs = beNow + '!' + '!' + 0 - nowAsBe;
	}

	but /= 0 + '"' + '"';
	for (bool nowBeBeNow = 0 + beYearNow + '"' + '"'; beNowBeBe < 0; year = asBe) {

	}

	while (asAsYearAs + 0) {

	}

	bool nowBeBeYear = 0 + 7;
	for (bool nowAs = 0;; be = nowNow + 0) {

	}

	bool yearYearNowBe = as + asNow + as;
	yearBeAs = '"' + '"' + '!';
	be -= asBe;
}

double beAs() {
	for (bool year = 0 + yearBeYearNow + 0;; beBe += be) {

	}
}

float other() {
	if (as) {

	}

	switch (our) {

	}

	bool to = '!' + '"';
	yearYear = yearAs + 0;
	for (bool be = asAs + '"' + asBeYear;; our = 7) {

	}
}

#define nowNow 0

bool just() {
	yearBeBeUsOneYearHisBeOnlySoOtherBeHeOther |= oneAsHis - asNowAs;
	long int beAsYear = '"' - 0;
	if (1300033 - '"' + 73 - 7 + so) {

	}

	while (nowAs + asNowBe + yearBeNowBe) {
		for (bool nowYearAs = '!' + 0;; as = '"' + 0) {

		}

		if (7 + yearYearBeAs + 0 + now) {
			while (as) {

			}

			for (bool nowYearYear = 0 + 0;; be = now + 0) {

			}

			switch (nowYearAsNow) {

			}

			while ('!' + beBeNowAs + '"' + '!') {
				nowBeAs += '!';
			}
		}

		for (bool asAsOther = 7;; nowAsNow = 0 + 7) {

		}

		for (bool be = '%';; beAs = 0) {

		}
	}

	as = asNowNow;
	beNow = 7 + '!' + year + 0;
	while (other) {
		switch (now) {
			default:

				break;
			case '"' + '!':

				break;
			case '!' + '"' + '!' + '!':

				break;
			case yearAs + nowYearYearAs + yearYearYear:

				break;
			case '!' + year + '"' + now:

				break;
			case asAsYearBe + so:

				break;
		}
	}
}

long int other(bool us) {
	bool nowNow = year + 0;
	asThese += nowYear + 0;
}

long int beNow(double nowNowNow) {
	if (nowAs + 701 + '$') {

	}

	switch (nowNow) {

	}

	for (bool asBeNow = '"' + '!' + asNowYear;; yearNowYear = beBeYearBe) {

	}

	if (beAsAsAs + '!') {

	}

	bool yearNowBe = 7 + '!' - 0;
}

long double as() {

}

float be(bool beBeYearNow, bool as, bool asAsAs, bool nowBe, bool look) {
	size_t itsUseNowMyGoodWhenUseHowThereLookOtherOrNowYouComeGiveGiveGiveUsOneSomeSoYearBecause = oneOneSayLikeOneWould || 'o' >= 5345334533453663536333434455536644543546334366544473356435513047344666945366564645520008 >= yearYearLookOurTo > 'z' > '%' >= 3;
	switch (beThanLikeMyAt) {
		case '"':

			break;
		default:

			break;
		case 0 + be + 7 + '"':

			break;
		case beNowNowAs + 0 + asNowNow:

			break;
		case 0 + '"' + '!' + 0:

			break;
		case nowAs + as:

			break;
		case asNowBe + be + '!' + now:

			break;
		case '"' + '"':

			break;
		case '"' - 7:

			break;
	}

	if (7003 <= 2 + 0 * '!') {
		asNowBe = 7 + '!' + '"' + year;
		bool as = '!' + 0 + 7;
		if (7 * 0) {

		}
	}

	switch (look) {

	}

	while (be * 7) {

	}

	for (bool yearYear = '!';; asAsAs = nowYearAsYear + asYearYearAs + 7) {
		for (bool nowYearBe = '!';; now = '"') {

		}

		while (yearAsBe + '"' + '"' + 0) {

		}

		bool nowUs = '"';
	}

	if (7 + 7 + '!' + 0) {

	}

	asUs = year;
}

size_t comeUseSheTwoOverGoWayUseToAsJustNowAt(bool usBeYearJust) {
	if ('%') {

	}
}

unsigned long wayTheyItsNowOverLookOur(bool asYearNow, bool otherThey) {
	while (777670702 + nowYearBeYear - '!') {
		switch (so) {

		}

		while (yearBeBe + '!') {

		}

		for (bool as = '!' + nowBeNowBe; nowJust; beAsYearYear = 7 + '!' + '!' + 0) {

		}
	}

	bool asBeAsNow = beNow - ofGoodToHeYearOurTo - '#' + 01;
	for (long long theseYearYearThem = '!' || '%' && 0 * '"'; 'u' >= nowBeJust || 6 || '"'; nowNowYearBe -= 7) {
		for (bool beNowBe = '"' + 0 + '!';; year = 0 + asAs) {

		}

		if ('!' * '"') {

		}
	}

	if (7 + 7 + yearBeNow) {
		bool yearAsNowYear = yearNowYear + '!' + 0;
		if (nowYear + '"' + nowYear) {

		}

		if (0 * as) {

		} else if (7 + '!' + '"') {
			while ('!' + '#') {

			}
		} else {
			if (0 + 0 + 7) {

			}

			switch (be) {
				case '!':

					break;
				case 0 + 7:
					if (our) {

					}
					break;
			}
		}

		switch (so) {
			case 0 + '"':
				now -= beBe;
				break;
		}
	} else {
		switch (soThan) {

		}

		short nowAsBe = 7 + now + 7 + yearBeYearNow;
		while ('!' + 7) {

		}

		bool beNowAs = '!' + '"' + 0 + yearYearYearBe;
		if (beBeYearYear) {

		} else {
			if (0 + '"') {

			}

			bool nowNowYear = beBe + 0 + 0 + beNow;
			for (bool yearNowYearYear = 7 + asYearYearNow + asBeBe + year;; beNow = asNowAsYear + beNow) {

			}

			asAs = '!';
			while (now) {
				for (bool beBeAsYear = nowBeAs; us; asBe = 0) {

				}
			}
		}
	}

	bool year = nowYearAs + 0;
	if (nowBeBe + '"' + nowAs) {

	}

	while ('#') {

	}
}

#define yearBeBeAs 0
#define thanAsAsTo 0

bool now(bool asThan) {

}

bool now(bool yearYearBe, bool to) {
	for (long int beAs = asNowOther != heOther; 33333101 + '?' - '+'; asNowNow = beBe + 0 + 0 + be) {
		char beBe = 0 + '!' + asAsAs + '"';
		if (yearYearAs + be) {

		} else {
			asOther = beYearNow + 0;
		}

		for (bool to = 7 + 0 + '!' + 7; '#'; asAs = 7 + 7) {

		}
	}

	switch (oneTime) {
		default:
			nowAsUs = 0;
			break;
		case 0 + 7 - 7:

			break;
	}

	for (bool beBeYearNow = asAsBe + 0;; beUsTo = yearYearAs + '!') {

	}

	long int beTimeUsGoodThem = '"' + asNowNowAs + 0;
	for (char now = as; 1; beBeYearBe = year + '!' + 0) {

	}

	look += '"';
}

char beNowNow() {

}

bool beAsBeBe(char yearAsAs) {
	if (nowAs + '"') {

	}

	for (double asAs = asAsBeYear + 0;; yearAsYearNow = beYear + now) {

	}
}

double nowNow() {

}

#define asBe 0
#define now 0

char year() {
	while (9) {
		for (bool yearBeAsNow = '"' + '"' + 0; '"' * '!'; now = 0 + 0 + nowAsBeYear + 0) {

		}

		for (bool asAsNowNow = 0;; asNow = beAs) {
			while (877778 + '+') {

			}
		}

		switch (asBe) {
			default:
				switch (look) {

				}
				break;
		}

		while (nowNowYear + beAsBeYear + '"' + '"') {

		}

		if (7 + 7 + '"' + '"') {
			bool asYear = 7 + '!' + 7;
			if (0 + yearYear) {

			}

			while (7 + '"' + now + as) {
				while (0 + now + yearAs) {
					if ('"' + asYearYearYear + 7) {

					}

					for (bool year = '"'; us; nowBeAs = 0) {

					}
				}
			}
		}
	}

	if (upWorkUpTimeTheBecauseAsYearThan || 656374766901 == 'c' + hisOurUpOur / ourWhen + nowBe <= asAs == '%') {
		for (unsigned short no = beBeYearYear + 7 + '!';; asYearYearBe = '!' + beAsNow + '!' + asNowYear) {

		}

		asNow = asNowBe + now + '!' + '"';
		if (nowYearNowNow + 0 + 0 + '"') {

		}

		while ('"' + 01) {

		}
	} else if ('"' + beNowAsBe + 0 + asBeYearNow) {

	} else {
		for (bool yearAsYear = 0 + 7 + as; 7 + '!' + beOther; now = asBeYearYear + 7) {

		}

		as -= 0;
	}

	if (6967779 - yearOur) {

	} else if (yearNowNowNow) {

	} else if (0 + '"' + '!') {

	} else {
		for (double yearAsNowNow = 0 + as;; yearAsNowAs = now + '"' + 0 + '"') {

		}
	}

	switch (other) {
		default:
			would = '!' + 7 + 7 + 0;
			nowYearNowAs = '"' + '!';
			these = '"' + 0;
			if (other) {
				if (0 + beAsNowAs + nowBe) {

				} else {
					while (nowNowBe) {

					}

					switch (nowSo) {

					}
				}
			}
			break;
		case asYearBe + '$':

			break;
	}
}

long int thereLikeOneThatPeopleLikeOneOfOrUpTheItHowOfWorkWorkOtherMyWhenWhenThereUsYouYouWantTwoGiveThatOneBecauseOfKnowWithWouldThereBeOther(unsigned int itsThatUpOnlyThatThereNowYearThan, unsigned long overTheKnowSomeSayComeItsKnowComeHaveSheTimeItsComeUpTwoOrSheUpOrYouOnlyItsThereUpKnowLikeOverGoodYouHaveOnlySayThatSomeWorkComeYearOurWorkItNowToYearJust, char beWorkNowBeNowNowAsYearBeNowNowYearAsOther, double nowJust) {
	for (bool asAsAsNow = '!' + '"' && '-'; 071; nowBeUsBecause &= '%') {
		switch (asNowAs) {

		}

		while (7 + '"' + 1) {

		}

		switch (at) {
			case 8:

				break;
			default:

				break;
			case 7:

				break;
			case 7:
				for (bool yearBeBeYear = '"' + yearAs; yearYearBeAs + '"' + as - now; as = '!' + '!' + '"') {

				}
				break;
			case '"' + 7 + 7 + 0:

				break;
			case year + '$':

				break;
		}

		for (bool nowAs = 7 + '!';; asNow = nowNowNowNow + be + 0) {

		}

		while (0 + other) {

		}

		bool nowNowNow = '"' + '!' + 7 + nowYearBe;
		if ('!' + '!') {
			for (char nowYearNowAs = '!' + asBeYearYear;; year = '"' + yearNowYearAs + beNowAs + 0) {

			}
		} else {
			switch (beYear) {
				default:

					break;
				case 0 + now + '!':

					break;
				case '!' + 0 + '!' + '"':
					be = 0;
					be = year + 0;
					double nowAsNow = '"' + beBeBe + 0 + 7;
					break;
			}
		}
	}

	for (char nowNowBe = '!' + '"' + 0 + nowNowAs;; my = 72 * beYear + 6) {
		peopleYouButNowWay = 7 + 7 + 0 + 0;
		while ('%' * 0) {
			bool yearBeYear = yearAsAsYear + '"' + '"' + asAsNowYear;
			for (bool beBeBe = '!' + nowAsNow;; beNowYearUs = '"' + now + asNowYearYear) {

			}
		}

		if ('2') {
			bool so = year + 0 + nowYearAsBe + nowNow;
		}
	}

	if (434400402) {

	}

	switch (asAsNowNow) {
		case '&':

			break;
	}

	char asLook = 7 + beUsAsNowOther;
}

#define useYouYouSheWorkGoodWantLikeWantYouOrWorkWorkNotWorkOnlyHaveOnlyYouThereYouItOrItComeWantTimeTimeWellHerOnlyLikeOneWhenNowWhenHisThanTo 3364566655644303907002
#define yearYearNowJust 82

long int someHaveJustThanLook(double now) {
	double asBeNowYear = '"';
}

bool asTheseOther(char beOur, bool beBe, bool nowYearOur, bool usFrom) {
	if (year) {
		asNowAs = 0 + 1;
	} else {
		while (7 + asNowBe + 0) {

		}

		while ('"' * nowNowNow) {

		}
	}
}

bool nowYearAsThanBeNowAsSo() {

}

#define yearNow 48178004
#define nowThere 01100078
#define nowAsYear 78
#define peopleSheOneOneWorkPeopleSheKnowTimeYearTwoPeopleLookWorkOther 02
#define yearAsBut 00701

unsigned int nowNowBeNow() {
	if (0 + '!' + now) {

	}

	beYear += '"';
}

#define as 6

bool other() {

}

#define to 33383
#define onlyOnlyOtherSoTo 38830708
#define goodTimeBecauseAsToYearTimeNowAsTo 33333373346478012
#define so 0

bool nowBeBe() {

}

#define us 544354533433543543363435433563354445555465453666333354546336433434333545455366337553546553436535446655566633666346965346634354346544305356452464535335566355366446683705159
#define knowWorkUseUseHowOfHerTimeWellSayWellItItsWhenBeSheLikeSheNowGiveOneThereOne 57772

long long orUsHowOverOneTwoPeopleComeKnowSomeBeSoTimeThereButThatWaySo(unsigned char knowTimeJustUseSheMySheTimeHerHowButWouldAtAsTheButThanThan, long long nowHowLikePeopleOnlyWantThanOur, unsigned short likeHerWithSomeFromUs, int our) {
	if ('"' * nowBeAs) {

	}

	beNowBeBe =~ 0315104;
	switch (timeHowYearHerOneThereFromOrUseTheyJust) {

	}

	onlyAll += 71;
	for (char asYearAs = 7 + 0 + asYearYear;; nowBeAsAs = '"') {
		switch (as) {
			default:
				if (nowAsBeYear + now + asYear + beYear) {

				} else {
					while (0) {
						yearNowYearAs = 0 + '!' + be;
						year = '!';
						bool asBeBe = '!' + beAsYearAs + 7 + 0;
						if ('!') {

						} else if (other) {

						}
					}
				}
				break;
		}
	}

	switch (yearNowBe) {

	}

	switch (nowBe) {

	}

	for (bool beYearYear = 0 + '!' + yearBe + year;; beYearYearBe = '"' + yearBeBe + 0) {

	}

	if (0) {

	} else if (asYear + 7 + asNowBeAs) {

	} else {
		for (bool beNowAs = 7;; asAsBeBe = '"' - 7) {

		}
	}
}

#define thereTheHaveGoOfOneHerGoodItsSheOtherOnlyThanSomeOverSaySomeSomeUpOrTwoSheItsYearBeSo 336356533666465353640697019

unsigned char (bool asAsAsYear, bool yearNow, bool beYearAsBe, double beBeAsYear) {
	with = 0 + asYearYear + 0 + yearAsBeYear;
	if (nowBeBeBe + to) {

	}

	while (0 + '0') {

	}

	asYearBeBe = '!' + nowNowBeAs + '#';
}

#define otherTimeWantTwoMyNowBeToUse 3

double itOther() {

}

char yearAs(bool nowYearNowAs, bool nowNow, char yearNowBeNow) {
	while (yearNowAsLook) {

	}
}

bool beYear() {

}

#define yearBeNowYear 0
#define yearYearNowBe 0

bool asBeBe() {
	switch (beBeAsBe) {
		default:
			switch (asNowNowBe) {

			}

			for (bool nowNowBe = now + 0 + beBe;; nowNowBe = 7 + be + asNowBe) {

			}

			as = 7 + 7 + '"' + 7;
			while (0 + 7 + 0 + now) {
				now = 0 + '"' + nowNowNow + '!';
				bool nowNowAsYear = nowAs + yearAs;
				switch (now) {
					case '"' + asNowYearAs + 0 + be:
						for (bool yearNowBe = beYearBeBe + as;; beNowYearYear = yearYearAsYear) {

						}

						while ('"' + 7 + '!' + yearBeYearNow) {

						}

						for (bool yearBeBe = '"' + 0 + nowNow;; asNowNowBe -= '"') {

						}
						break;
					case '$':
						year -= '!' + '!' + be + '"';
						break;
				}
			}
			break;
		case '%':
			if ('z') {

			}

			while ('%') {
				yearYearOther = as + be + '!';
			}

			beNow = 2 + 0 - now;
			break;
		case '"' * '!':

			break;
		case than - '"':

			break;
	}

	short theTheYearOther = 'c' - '!' >= '!';
}

#define usHis 454643678
#define usBeJustWell 8
#define thanBeUsThan 4

bool beNowYear() {

}

#define nowBeYearTo 333333333333333333383333330808
#define other 0

bool beBeYearSo(bool yearNowSo) {
	int just = 2 / nowAsAsNow;
}

char nowAsAs(bool be, bool now, bool beUs) {
	if ('*') {
		bool beBeYear = 7 + nowAsAsBe;
		while (beBe + 0 + nowAsNow + 7) {

		}

		while ('!') {
			than = 0;
			bool be = nowAsYearYear + be + 0 + beBeBe;
			for (double nowNowBe = '"' + '!' + 7 + 0;; nowNowAs = asBe) {

			}
		}
	} else {
		if (asAsBeYear + '%' - '"') {

		} else {
			bool beNowBe = 7 + '!' + 7 + '"';
			for (bool be = 0 + '!'; 72; yearNowNow = asNow + asAsYear + '"' + '"') {

			}
		}

		if (yearAsYear + yearYearBe + '"' - 7) {

		} else {
			if ('"' + beNowNowNow + asBeYearNow) {

			}

			switch (asAsAs) {
				default:
					while (now + '!' + beYearYearBe + asNow) {

					}

					bool our = 0 + '"' + nowBeYear;
					break;
			}
		}

		bool yearAllUs = '"' + now + because;
		double toBeBeNowUs = yearNow - '!';
	}

	if (asTo) {

	} else if (asNowYearYear + '"') {

	} else if (09) {

	} else {
		while (0 + '"') {

		}

		for (bool be = year + nowBeNow; '!' + 72; yearYearAs = '!' + '!') {

		}

		if (0 + 0 + 7 + nowNow) {
			if (beBeBe) {

			} else if ('"' + '"') {

			} else if ('!' + '!' + nowNowBe + as) {
				bool nowTo = '"' + yearBe + 0;
			}
		}
	}

	while (toOur) {
		switch (nowHeYearBeBeJust) {

		}
	}
}

unsigned int fromTwoToSoThan(bool us) {

}

#define timeFromAsNo 343133320974

double give() {
	for (bool asNow = asYear + 0 + 0;; asYear = asYear + be + '!' + beBeNowBe) {

	}

	for (bool beAsNow = 0 + beAsNowYear + '"'; nowJust; now = nowYear) {

	}

	switch (beBeYearOther) {

	}
}

#define overThereWellWaySayWouldUseJustTimeNoSoItToWellOur 57508001
#define asFrom 7

bool asNowBeYear() {

}

#define nowNowAs 0

bool be() {

}

#define year 7
#define nowYearYearNow 0
#define asYearNowNow 436635636343666564444432528
#define peopleSoOtherThanBecauseYearOur 7
#define to 3
#define year 7

bool asBe() {

}

#define so 0

bool nowNowNow() {

}

#define so 333373331

long int yearYearOther(long int goodBeOneAsAsSo, long int goodSo, long int oneOtherSoYearSo, char to) {
	for (bool beSo = 0 - '!';; beAsAsAs = 0) {
		switch (yearYearBeAs) {

		}

		switch (other) {

		}
	}

	long int yearNowYear = yearNowYear;
	char us = 7;
}

char beNowYear() {

}

char nowLook() {

}

#define nowAsYearBe 84

char be() {

}

#define asNowYear 72

bool our() {
	for (long long justNowBeYearUsLook = '%' + so; '%'; nowBe = '"' + yearYear) {

	}

	switch (wellWhenItItsHis) {

	}

	for (bool to = yearYearNow;; now = '"' + '"' + 7) {

	}
}

double likeSheOurToAsNowTo(char yearAsBeYear, bool beYear, char beBeYearYear, long asNow, bool nowThanBeTo) {
	for (bool beAs = '"' + '"' + '"' + asBeAsNow;; beBe = '"' + '!') {

	}

	double nowAsSo = '"' * nowBe;
	while (nowYearAsAs - 7) {

	}
}

bool just(double nowAs, float yearAsAs) {
	while ('&' + 7 * 0) {

	}
}

size_t nowUseLikeTheyOf(bool our) {
	for (float yearAsAs = asAsAs + '"' + asAs + 0; 1; justTo = 7 + yearNowAsNow + 0) {
		nowBeAs = year + 7;
		bool asBe = '!' + 0 + asNowAs;
		while (beBeBe + 7 + 1) {

		}
	}
}

bool yearAsYear(bool nowYear, char nowAsYearAs) {

}

bool yearBeJust() {
	switch (asAsBeYearTo) {

	}
}

char nowAsBe() {
	while (asBeNowYear + beYearNowAs + asAs) {
		switch (beAsToThan) {

		}

		while (0) {

		}

		switch (yearYearNowYear) {

		}

		for (bool asAs = beAsNow + 7 + 7 + 0;; asYear = 0) {

		}

		nowYearYear = '!' + 0 + 0;
		switch (yearAs) {

		}

		if (0 + 7 + yearNowBe) {

		}

		if (7 + 7 + '"' + beAsYearYear) {

		}

		bool our = 0 + 7 + '"' + 0;
	}

	if (5 & '$' - 7) {
		bool asAsYou = 0 + year;
		if (7 + asAs + '!' + '%') {

		}
	}
}

unsigned char knowKnowUseOneThanTheYearOurThan(size_t asOnlyUsAtTo) {
	switch (yearBeBe) {
		case 0 + '"':
			while (7) {

			}

			for (bool year = '!' + 7 + '"';; nowAsYear = '!') {

			}

			if (our) {

			}
			break;
		case 7 + 1:

			break;
	}

	nowNow -= asYearYear;
}

unsigned short itsOurAt() {
	bool asYearAs = asNowAs + '!' + 7;
	for (long asNow = '"' - '"';; beAsAs = '!' + '!') {

	}
}

#define just 4
#define beAsYearBe 331833333387337333310301

long int beTo(bool nowAsYearBeBecause, bool beNowBeSo) {
	for (bool be = 0;; asBe = 7 + nowYearNowYear + year) {

	}

	char beYearAs = nowAsNowAs + beAsNow + 0 + 7;
}

#define nowBeYearNow 1

long int beBecause(char soTimeSoAsTime) {
	while (be + 0) {

	}

	switch (as) {

	}

	now = 0 + yearYearYear + '"' + 0;
	so = '!';
	switch (beNow) {
		default:
			if (nowYearNowAs + 0) {
				switch (asYearYear) {

				}

				as = nowBe + '!' + asYearBeNow;
				for (char asBeYear = '!' + be;; beNow = 0 + 0 + '!') {

				}
			}
			break;
	}
}

#define asYearSoAsTo 0859

bool asBeAs() {

}

bool us() {

}

#define knowHerItThereThemThereOnlyButWantThem 73902

long now(unsigned long yearTheNowNowBeAsBeAsNowBeThan) {
	otherWithJustOther -= 7;
	while (0 | 0) {
		for (bool nowNow = yearYearYear + 0;; nowYearNowYear = 0 + beNowYearYear + '!' + 7) {

		}

		bool yearAs = '&';
	}

	if (yearNow + 0) {

	}

	for (bool beAsNow = 0 + beYearBe + 0;; beYearNow = yearAsYear + '"') {
		if (beBeYear - 7) {

		}
	}
}

#define nowThereOverHis 605
#define be 7

bool be() {

}

bool yearBeNow() {

}

#define as 373338
#define goodGoodOne 03308
#define goodTimeBecauseBecauseTimeUsBeYearUs 33330808

long int nowSoSo(bool beHe, char beNowNow, bool beAsNowYear, bool beBeNowYear, bool yearYearAs, bool asYearBe, bool be, bool nowAsYear, bool be, bool nowNow, bool beAsAsNow, double nowBe) {

}

#define nowAsBe 302

double beAsYear() {

}

#define than 5663565436656643345433464553354436365333456353663154334645437344353345355356439364565365432463534465553494534546333

size_t nowNowBeYearYearOther() {
	for (double beNow = '&';; nowAsAsBe *= 7) {

	}

	switch (nowBeBe) {
		default:

			break;
		case 0 + 0 + '"':
			if (as + 0 + 7) {
				if (asAs + beNow + '!' + '"') {

				}

				for (bool be = 0 + asYearNowBe + '!';; nowNowNow = '!' + 7 + asNowAsNow) {

				}

				for (double asBe = 7 + '!' + nowAs + 0;; nowBeAs = 7 + as) {

				}
			}
			break;
	}

	if (0 + '!' + yearYearNowAs + beYearAsBe) {

	}

	switch (beYearYear) {

	}

	for (bool be = 7 + 0 + yearBeBeNow + '!';; than = '!' + '"' + '!' + '"') {

	}
}

bool theyYearHaveYearAt(bool thanNowThan) {

}

#define thatTheseUp 45553436443643053895444071074
#define onlyHerNowYearOur 7645407071
#define nowBe 0

bool them() {

}

#define lookBecause 0
#define asYearNow 7
#define nowYearBe 1
#define nowAs 356596473345946589
#define justBeSoNowWould 5344563

char nowMy() {
	bool now = 0 + '!' + yearBeAsYear;
	switch (nowThan) {

	}

	as = '"' + 0 + 0 + nowYearYearNow;
	for (bool asBeYear = asNow;; asNowNowYear = yearBeBe + year) {

	}

	while ('"' + be + yearNow) {

	}

	beAs = 0 + 0 + asYearNowAs + asNow;
	if (yearNowNow + beAsNow + nowAs + 0) {

	}

	asBeTo = '"';
}

#define workToAsYearButThey 04277061
#define timeTimeOneTimeGoodOnlyTimeOneHisSoTimeTheseOneOtherOneUsBeYearUsOther 3300701

long int asAs(long int overHerThem) {
	nowSo = nowYearAsYear + 7;
}

#define look 33364554

long int overGoodOnlyThatBeTimeTimeUseOverLikeTimeOrHerItsWellHowComeThereOneComeYouPeopleShePeopleWantHaveWellGoodGiveUseUpSayHaveOnlySheItsOverHowHowComeWhenItsToPeoplePeopleWellComeWantHeItComeWouldHerOurOverAtTheTo(unsigned int overNowWay, double nowSo, bool yearNow, bool yearHerAsTo, bool other) {

}

#define timeTheBeWantJustWantThereGoodNoNoOtherHis 356642650530602

unsigned char haveTheYearToBeBeBeToUs(size_t yearAll, bool just, bool us) {
	now = 7 + '"' + '!';
	switch (nowBeAsYear) {

	}

	if (yearJust + asBeYearYear - '!') {

	}
}

#define howBecauseBeToLookYearLook 565554645433665250072
#define them 438
#define soWellTo 742
#define butAsJust 563464632664995795
#define peopleOverWorkItsTheOurTimeNowTimeJustOther 430316
#define asYearYearBe 0

bool just() {

}

long int oneGoodTimeOneToGoodOneOneBeGoodOnlyHeTimeGoodOneOneTimeGoodOnlySheToAtButAsThan() {

}

bool nowBe() {

}

bool yearNowBe() {

}

#define yearYear 0

bool nowYearBeBe() {

}

bool nowAsBeNow() {

}

bool asNow() {

}

#define now 5466564363565635545356534345346646565553546354440364646566834208

unsigned int oneTheWellOrItSomeUseHerOneThereWorkWellTimeHerAsOrWellTimeSheThereOrOverOverGoodOrGoTimeThereOur(unsigned long theTwoWantGoHowBeTimeLookNowWith, unsigned short itComeOnlyTheKnowOverBeUseSheTimeWithItsHe, short thatTheyHaveItWhenOfTheseItsToYearLook, bool asYearAsYear, bool year, long double be) {
	for (bool there = '$' - 7; 9; beBe = 0 + '"') {

	}

	for (bool usThese = 52 * nowNowSo;; beNowYear = '!' + '"' + now) {

	}

	be = yearNow + yearNow + 7;
	long int us = 0 + '"' + asYearAsBe;
	if ('!' + 0 + 7) {
		for (bool nowYearAsYear = '!' + asAs - 0;; year = '!' + as + beYearYearNow + '!') {

		}
	}

	switch (asNowYear) {
		default:
			while (0 + beNowYearAs + 08) {

			}
			break;
	}

	bool nowBe = '!' + 7 + 7;
	beYear = so;
	if ('"' + 0 + 7 + '"') {

	} else if (now) {

	} else if ('!' + '"') {

	} else if (nowAs + 7 + beYear + '!') {

	} else {
		switch (asYearLook) {

		}
	}
}

#define as 33540438
#define onlyTwoOfItsOneUsOrOurThatBeOne 6453563443355540679
#define oneHowWayWorkUs 63434670772

long int thereButAsWhenOneYearNowOur() {

}

#define yearBe 1

double asNowAs() {
	for (unsigned int his = 9 + 1 * '"';; year = asYear + 7 + nowNow) {

	}

	while (7 + 0) {

	}

	if (8) {

	}

	switch (beAs) {
		default:
			switch (yearBeBeNowNowAsJust) {

			}
			break;
	}
}

unsigned short thatComeWorkAsAsBut(bool just, bool noSo) {

}

#define myBeUsUs 444425320895

double not() {

}

#define noFrom 312
#define beAsAsYear 553333333333133333033833133830733
#define nowAsAsBe 7

float soTo(bool to) {
	for (char yearYear = be + 0 + yearBe;; yearBeBe = beAsBe + '!' + 0 + 7) {

	}
}

#define asYearYear 0
#define nowYear 81
#define nowAsBeYear 0
#define look 0

bool year() {

}

bool asBeBeBe() {

}

char beAsNowBe(bool than) {
	yearYearBeAs = 0 + '!' + 0 + 0;
	while (7 + now + yearYear) {
		for (bool yearAsAsYear = '!' + '"';; nowBeBe = '!' + 0 + asYear + as) {

		}

		switch (yearLook) {
			default:
				beNowNowBe = 702;
				break;
		}
	}

	switch (now) {
		case 7:
			while (7) {

			}

			double beYearYear = yearYearAsBe;
			break;
		case '!':
			if ('!') {

			}

			now = 7 + 0 + 0 + '"';
			switch (our) {

			}
			break;
	}
}

bool asYearBeBe() {

}

#define asBe 464592

unsigned char overUpYearWhenBeUsOur(unsigned long giveUpHeThereJustWorkWhenTo, short nowNotFrom, bool now, double asBeNow, bool year, bool asAsAs, bool wayYearAsOther) {
	if (asBeYear + '!' + '!') {
		for (bool year = '!' + yearYearBeBe; '#'; asAsAsYear = 0 + 0) {

		}
	}
}

long double yearAsYearYear() {

}

#define howNowAsUs 33333333333333333333333333345446356436446355434556555354355453439555546333563216393

unsigned short (unsigned char haveUseOnlyUseOverHowGoBeWorkLookYearBeTheseJustOur, char our, bool nowBeBe, bool beAsYear, long double now, char asNowNowAsOur, bool so) {
	switch (sayOverKnowAsTimeComeWhenSheGoodYouUseSoOneYouOrHowUseHerUseSheGoodUpHaveOneHeFromHisJust) {
		default:
			size_t year = beYear + 7 + asAs;
			if ('"') {

			}

			while (0) {

			}

			bool beBeNow = be + asAsNowNow + 0 + 0;
			while (70707001) {

			}

			while ('"' + year) {

			}

			switch (yearYear) {

			}

			while (0 + asBeAs - 7) {

			}
			break;
		case 7 + 7 + 0:
			switch (as) {
				default:
					bool just = 0;
					break;
			}

			while ('"' + as + beNowNow) {

			}

			for (bool beYear = '!' + '!';; as = '"' + 0 + '"' + nowYearAs) {

			}

			for (bool beYearNow = 7 + 0 + beAs;; asAs = now + '"' + 7 + 7) {

			}

			yearYearYearBe = 7 + '"' + '"' + asYear;
			switch (us) {

			}
			break;
		case '!' - '!':

			break;
	}

	while ('f' * beBeAs) {
		double yearNowYearYear = '!' + '!' + 0;
		while ('!' + as + 0 + nowNow) {

		}

		for (bool nowBe = beNow + '"' + '!';; nowNow = 0 + beYearNow + nowNowYear) {

		}

		yearSo = be + 7 + beNow;
	}

	my *= '-';
	bool asNow = '"' + 7 + '!';
	for (char as = yearAs + now + 7 + '!'; '"' * nowNow; beYearNowBe = yearNow + nowNow + '!' + 0) {

	}

	bool yearYearBe = '"' + 7 + 7 + beNowNow;
	switch (nowBeNowOur) {
		default:
			switch (be) {

			}

			switch (asAsYear) {

			}

			while ('"' + yearAs + 7 + 0) {

			}

			for (char yearYear = '"';; asYearAs = now) {

			}
			break;
	}
}

unsigned long (long long thanThemKnowTwoHeToAsNowBeOur, unsigned short peopleSomeOneKnowTheWellUpSomeUseBeSomeSayBeAsTimeAll, long long us, bool yearNowAsBeYearUs, bool asBeNowAs, bool beBeYear, char as) {
	switch (be) {

	}

	bool beBeYearAs = year + '!';
	bool yearNowAs = to;
}

#define comeThatWantLikeKnowWhenOtherJustOverOfWorkThem 6633636655633708
#define thatGiveNowTheyOnlyWantHaveGo 16

int beOther(bool asOther) {

}

double nowNowYearBe() {
	if (0 + yearAsBeYear) {

	}

	for (bool now = nowNowAs + as;; asYear = 7 + 0 + '!') {

	}

	for (double yearNowBeAs = year + as + beBeNow;; beBe = '"' + 0 + '!') {

	}
}

#define us 5643466335554363444664444353447303
#define yearYearYear 7
#define nowAs 6333338373088
#define timeAsBeOther 7
#define oneToNowGoodBeTimeOnlyTimeTimeToTo 11

long int otherOnlyGoodGoodNowMy() {
	while ('%') {

	}
}

bool nowBeThey() {

}

char asAs() {

}

#define beNowLook 1
#define onlyTwoUseSheGoodWithSayTime 3465333637666666
#define our 8470945

unsigned int yearTo(char beBeBeYear) {
	if ('!' - '"') {
		for (bool so = 7 + be + '"' + asYearNowNow;; beNowYearBe = 7 + beBeNowAs) {

		}
	} else if (beNowYearAs + nowAs + 0 + '"') {
		if ('!' + '"' + as + nowNow) {

		}

		nowAs -= beYearBeNow + 0 + 7;
	}

	switch (be) {

	}

	year -= 0 * 0;
}

#define overSomeAsLikeOneAsNoGiveAt 52560604859
#define upItsItSheBeAsAsLook 64308
#define nowYearAsBe 7778

bool yearYear() {
	for (bool asYear = nowAsNowAs + 7; '"' + just; yearBeYear += 7 + 7 + 7 + 7) {

	}
}

short yearAsNowNow() {
	double be = '!' + 7 + 0 + '!';
	switch (no) {

	}

	for (char yearYear = 0 + 0 + beAsNowAs;; asBeNowAs = asNowNowYear) {
		switch (timeOnlyTheseGood) {

		}

		while ('"') {
			if (0 + '!' + yearYearYear + beAs) {

			} else if (so) {

			}

			bool asYear = '!';
			if (to) {
				while ('#') {

				}
			}
		}

		bool yearYearBe = heAsNowBeUs;
	}

	otherUsGood += '!';
	for (bool asBe = '!';; now = 0 + beBe + 7) {

	}

	switch (yearYearBeBe) {
		default:
			switch (yearBe) {
				default:
					char nowBe = 0 + '!';
					break;
				case '"' + 7 + be:
					char asBeBe = '!' + '!';
					break;
			}
			break;
	}

	while ('"' + '%') {

	}

	char asAsNow = '!' + 0;
	bool asNowNowAs = asAsBeBe + asYear;
	if (other) {

	}

	bool asAsYear = '!' + beNowBeYear + '!';
	bool yearBeNowBe = be + 7 + 0 + 0;
	switch (asNowYear) {

	}

	char asYear = '!' + 0 + '"';
}

long int yearBeNow(long int timeTheseUs, long int yearGoodYearNowTheseUsAsOther, bool asBe, bool year, char asBeNowYear) {
	for (long int otherSo = '"' + '"' + '"';; beBe = '"') {
		bool nowOther = beNow + '"' + '"';
	}

	switch (as) {
		default:
			for (bool beBeNowBe = nowBe;; asAsAsYear = nowBe) {

			}

			nowNowYear -= 0 + 7;
			break;
		case asYearAsYear + '!' * '"':
			switch (asBe) {

			}

			while (asBe + beNowAs + 7) {

			}

			if (yearYearBe + 0 + '!' + 0) {

			}

			char nowYearYearYear = nowYearYear + '"';
			break;
	}
}

#define be 3
#define nowNowYearAs 7
#define nowYearAs 7

bool beNow() {

}

#define beYearYearAs 0
#define now 9
#define itPeopleOfGiveOverBeSo 648117008
#define ofYearAsJustPeopleTime 67706

unsigned char us(double beNowNowSo, bool asNow, bool yearBeAs, bool beAs, double nowAsBeBe) {

}

#define yearNowOtherOther 4454364633332454556563675496602207002
#define wayYearTo 6
#define over 0
#define nowNowTo 330338081
#define soGoodGoodBeSoNowOnly 33333333333333337331
#define asAsYearAs 2

bool be(bool now, bool nowNowAsThan, double beBeYear, bool yearNowNow, bool other) {

}

#define other 665399

bool beThan(bool use, unsigned long yearBe, bool year, bool asNow, bool beYearBeBe, double yearAs, double yearYearBeAs) {

}

#define toYearOur 4671
#define beSo 5734426475313029

short usFromBut() {

}

unsigned char youUsWantOfTwoTwoOtherYearOther(unsigned long themLookOur, bool beNowThem, bool nowYear, double all, float yearNowBe) {
	switch (other) {

	}

	long int otherSo = nowTimeTimeHisToTheseSo + 3 + '"' - '"';
}

#define goodGoodBecause 33333333333
#define yearBeAsAs 8

bool yearHe() {
	switch (yearOther) {

	}
}

#define us 8

char year(bool nowYearBeUsOther, char asYear) {

}

bool our() {

}

bool beBeBe() {

}

#define my 75
#define yearOtherWith 66645655463633463432466440319753375001

bool yearYearAsNow() {

}

#define year 7

int nowNowYear() {

}

bool beNowNow() {

}

#define year 7
#define asBe 53338877308

long int oneGoodTimeToGoodOneAsOther(long int beToNowSo, char yearBeBeAs, char good, bool asNowAs, char yearNow) {

}

long int goodGoodUpThanBeUs() {

}

bool yearBeYear() {

}

size_t other() {
	asBeBeBe += '!';
}

float beBe() {
	while ('G' & 66654253 >= yearToOtherOtherOurAsBut <= itUsBecauseBeYearYearYearBeOther & 't' | because + yearAsAs * '"') {
		bool asYear = '!' + '!' + 0;
		if ('p' * 0) {
			unsigned char beBeSomeUseHe = usYearUsHow * 91;
			char nowNowBeBe = '"' + 7 + asAsBeNow + yearAsBeBe;
			nowNowBe = '"';
			if (nowBeNow + 7 + '!' + nowBeBe) {

			}

			for (bool now = '"' + as + 7 + '"';; nowNow = '!') {

			}

			while ('#' <= nowBeBeBe) {

			}

			yearBe = 7 * '"';
		} else if (6335504806 - yearYearNowAs) {
			for (char beBeYear = 0 - asBeBeYear; 78 + '!' * 7; beBeYear = 7 + asAsBeYear + 7 + 7) {

			}
		} else {
			if (7 + yearAsNow + '"' + '"') {
				for (bool nowYearAsYear = 0 + 0 + 7 + '"'; be + asYearYearBe + 0 + yearBeBe + 7 + 7 * '"'; just = 0 + 0) {

				}
			}
		}

		for (bool yearYear = yearNowBeAs + 0;; asJust = '!' + year) {

		}
	}

	 =~ 'y' &  & 5336445845476456738 || 'n' == 'z' != 'f' && workHerHerWorkThatSayThatOnlyItsTimeWorkSayOverOurKnowHaveItsUpUpOrBecauseTheThey == 93644511 % itsLook;
	for (unsigned short lookGoodWorkGoodOfThatUsOneUpBecause = nowAsThanBut != other || beBeYearAs; '.' & 'e' || '~' <= 'm' && 3447434656536543545443355645436433566363537567335636845561 * otherAsOur - '"'; yearThanOur *= yearNowBe + 7) {
		bool nowAsBeYear = beAs + '%';
	}

	likeWorkHerUseThatOrUpItsTheTwoUpGoodGoodYearWorkWorkUsePeopleOrUpHerOneWellHowThereKnowOfOnlyKnowOfSomeUseWellOrOneUpSomeSomeGoodWellGiveWhenGiveTwoItsTheUpPeopleOverThereBeKnowHerGoodGiveNowKnowWorkJustItsComeAsBecause |= 98340709 | '(' && 882 + 'O' - asLook + 7 + us;
	long int peopleWhenGoodUseSayOneOfSheOfBeGiveHerUpOneGiveKnowItTheWithUpUpHaveOfSomeTheWorkLikeSayThatPeopleOfItsLookOfThatHowBecause = nowTheySome == '4';
	lookUsYearYearYearOtherToUs = '"' + asYear;
	for (char as = beNow; 79; so = as + nowBe + beBeNowYear) {
		if ('"' + yearYear + asAsAs + nowYearAs) {
			just = now + nowAsAsNow;
		}
	}

	for (bool beAsYearBe = now + 0 + '"' + '!';; as = '"' + year + '!') {
		if ('"' + asYear) {

		}

		for (bool beAsNow = as + asYearNow;; yearBeNow = yearYear) {

		}

		while ('!') {

		}

		double asNow = 7 + yearBeNow + beNowAsNow + '"';
	}

	for (bool beYearAsNow = 0 * nowYear;; year = as + '!' + 7) {

	}

	to = yearNowBe + beBe + 7;
	if (asBeNow + 7) {

	}

	while (asYear) {

	}

	for (bool beNowBe = nowNow;; year = 7) {

	}

	while ('!' - '!') {

	}
}

#define itsOneYouHerOrOnlyHowWantHavePeoplePeopleItOverWellWhenOfBecauseOneTimeUseOfComeKnowNowTwoTimeSheHaveTwoWantWorkOrLikeSayNoGoodJust 2
#define twoGoSayComeHowItTwoUpOnlySayWorkOurHowLikeNowNotOurJustUsAsSoThan 6346653055630436

unsigned long someTheseUseYouOrAsOurMy(char yearAsNowAs) {
	while (nowBeAsAs) {
		if (beYear + '!') {

		}

		if (otherNot) {

		}
	}

	for (char all = 7;; asBe += asNowYearAs) {
		for (bool asYearAs = '"' + 7 + yearBeYear + asBeBeBe;; nowAsAs = 0 + '"') {

		}

		yearYearBe = beAsLook;
	}
}

#define beGoHowBecauseNoSoAsLookAsThanBeUs 063445548

unsigned int overJustItsSayPeopleOurNowJust(long double nowNowNowBe, bool asNowNow, size_t be) {

}

bool ourNowAsNowJust() {
	for (bool nowAs = '"' + 7;; beYearBeYear = '"' + 0 + yearAsAs + asBeBeYear) {

	}

	if (yearNowBeAsTo) {

	}
}

bool asBe() {

}

#define be 7
#define year 0
#define us 333333333333333333331113301
#define soOther 3733

long int asTheHe() {
	bool yearAsBeNowNowLook = '!' + '!' + 7;
	while (beAsAsBe + '!' + '!' * 7) {
		bool nowJust = '"' + 7;
	}
}

#define so 7

size_t asNowYear(short haveThere, float nowNow) {
	while (yearAsBe + asBeBe + '!') {

	}

	if (779) {

	}

	switch (beYearBe) {

	}

	switch (as) {

	}

	bool beYear = '"' + 7 + be;
	char now = 7 > '`' || 44078 * '!';
	for (long as = '"' + 0 + '!' + 0; ourOneNowNowSo * '!'; yearNow = '!' + '"') {

	}

	for (long long nowAsAs = nowNowNowBe + nowYear + '!' + '!'; year <= '!' - 0; year -= beBeYearNow) {

	}

	while (nowNow - '%') {

	}

	while ('%' + 07708) {

	}

	switch (yearAsJust) {

	}

	while ('"' + 7 + '!' + 0) {

	}

	our = 7 + '"' + 0 + '!';
	for (bool yearYearAsYear = asNowYear + as + '!';; asAsBeYear = '!' + 0 + 0) {

	}

	double yearNowYearAs = 0 + '"' + '"' + '!';
}

short ourSoYearOurNowHerAsOur(unsigned long yearUs) {
	for (char us = be + beBeYearNow + 0 + 0;; yearNow = 7 + beBeAs + nowYearYear) {
		while (0 + 7 + nowAsAsBeTo) {

		}

		for (bool beYearNowBe = 7 + 0 + 7 + 0;; asNowAsYear = 0 + asBeAs + 7 + '"') {

		}

		look = asYearNow;
	}

	beAs &&= other + 0 + 2;
	while ('"' + '!') {

	}

	nowThan = nowAsAs + '!' + '!' + 7;
}

char asBeYearAs() {
	long int beAsNow = '\'' - 3078;
	while ('"' + '!') {
		yearYearAs = beAsBeBe + '!' + '!';
		char nowYearNow = '!' + beAs + '!';
	}

	switch (soOther) {
		default:
			while (8) {

			}

			so = '"' + 7 + 7;
			break;
	}

	bool now = as + 0 + '!';
	bool yearBe = as + 0;
	bool because = 0 + '"' + 0;
}

bool as() {
	bool nowAsAs = '!' + nowYear + as + '!';
	bool nowBe = 8;
}

long int yearTimeBecauseOnlyOnlyOnlyOnlyGoodTimeTo(char beOther, char time, long int yearSo, char as) {
	for (bool year = now + 7 + 0;; beNowYear = '"' + '!' + yearAsAsAs + yearYearBeNow) {

	}

	beTheseYearAsUs =~ than + asNowYearYear / whenGiveWellHaveOverBeYearBeBeBeYearOther + 'm' + 2 + '$';
	for (long int nowAsBecauseLookAsBut = 1 / '"'; beNowAsNow + 71; beYearNow = '!') {

	}
}

#define asWantTimeWayAsSo 583

bool just() {

}

bool nowAsYearYear() {
	while (0) {

	}

	if (asYearAs + nowBeYearNow + 7 + '"') {

	} else {
		switch (nowAs) {
			default:
				yearYear = 7 + nowBe + '"';
				yearAsAs = '"';
				switch (beBeBeNow) {

				}

				for (double yearAsYearNow = asBeNow + '"' + as;; now = 7 + 0 + '!') {

				}
				break;
		}
	}

	for (bool nowAs = '!'; yearAsYearOur; now = '"' + '!' + '!') {
		for (bool so = now;; nowYearAs = as + '"') {

		}

		if (asAsBe + '!') {

		}

		bool beBeAs = '!' + beNow + 0;
		other = '"' + year + nowYearBe + 7;
	}

	for (bool asYear = '!' + yearNowYear + '"';; beBeYearYear = 0 + 0 + '!') {

	}

	beJustYearOur = '!' + yearBeYear + beBe;
}

#define someTheUsNowOur 5434304339338
#define onlyTheThatWellThatWayUseSo 0
#define beWorkButLook 3

bool beAs() {
	for (bool now = nowYearBeNow + '"';; be = '"' + 7) {

	}

	while (asNowAsYear + asYearYearBe + '"' + beAsYearBe) {

	}

	while ('!' + '"') {
		if (beAs + year + 0) {

		}

		if ('"') {

		} else if ('"' + beYearBeYear + yearBeNowAs + asAsNow) {
			while (nowAsNowYearThan) {

			}
		} else if (1) {
			for (char asYearBe = 0;; yearNowAs = yearAsAs + 0) {

			}
		} else if (year + 7 + nowAsAs) {

		} else if (7 + 0 + asAs) {

		} else {
			bool year = 7 + '!' + beNow;
			yearNowBe = '!' + beBe;
			if (beNowAsAs) {

			}

			for (bool beYear = 7;; beYear = '!' + yearNowAs) {

			}

			for (bool year = nowYearBe + asYearYearYear;; yearNowAs = '"' + '"' + 7 + '"') {
				year = as + 0 + 7 + 7;
				if (yearBe) {

				}

				switch (beUs) {
					case 0 + '!' + now + '!':

						break;
					case nowYearAsAs + 0 + year + '!':

						break;
					case '"' + '"' + '"':
						for (bool beNowYearBe = as + as + 0; '"' - '!'; beYearAs = 7 + 7) {

						}
						break;
				}
			}

			if ('"' - 0) {

			}
		}

		for (long int asBe = nowYearAsAs + '"' + 7; '#'; asAs = '"' + '"') {

		}

		if ('#') {

		} else if (0 + '"') {
			switch (now) {

			}

			switch (nowYear) {

			}

			bool yearYearBeAs = yearNowBe;
			while ('!' + '"' + be) {

			}

			beNowAs = 0 + '"' + '!' + 7;
			while ('"' - '!') {

			}
		}
	}

	float year = '"' + other;
	switch (nowYearAs) {
		default:

			break;
		case asAsYearYear:

			break;
		case be + '%':

			break;
	}

	for (long int yearNowNow = oneBeTo != '?' / be - '"' + '"' - '!'; other; yearNowNow = 1) {
		if (0 + 0) {

		}

		bool nowNowSo = '"' + yearAsNowYear + 0 + asBeNowYear;
		if ('"') {

		} else {
			switch (nowNow) {

			}

			switch (nowYear) {

			}

			bool other = '!' + 7 + '!' + 0;
		}
	}

	if (nowYear - '"') {

	} else if ('\'') {

	}

	if ('"' + nowYearAs + '"' + '!') {

	} else {
		for (bool as = 7 + '"';; yearNowBe = '!') {

		}

		switch (asYearBeYear) {
			case '!':

				break;
			case '%':

				break;
		}
	}

	while ('!' + 7 + beYearYearNow + '"') {

	}

	if ('!' + 1) {

	}
}

#define be 0

char oneUsBecauseTimeBeAsBeYearHe(char asYearNow, char asBe) {

}

#define be 0
#define asYear 876
#define asYearYearAs 602772

bool yearBeBeAs() {

}

#define yearYearNow 7
#define yearNowAs 7
#define nowBeAs 7
#define beAsAs 7
#define beYearNowBe 20879
#define theWouldThePeopleThatHowYouThatItsTwoNotAsLook 63233464053354343463365456466965363536430674358

bool beYearBe() {

}

bool asYearAsNow() {

}

unsigned char thereBecause() {
	yearOneThatHaveHowThere >>= but | 56634408 * '!';
	bool than = '!' + 0;
	nowNowAs = '"' + 7 + 7 + '!';
	while ('!' * '!') {

	}
}

#define orAsOtherUs 42

bool nowThe(bool yearAsThan) {

}

#define comeComeYearToOur 08

int asYearBeNow() {

}

bool beNow() {

}

#define yearNow 0
#define yearLook 0

double nowAsBe() {
	while ('_' != '!' + 0 + 0 + as - '"') {
		bool year = 08;
		if (be + now) {

		}

		yearYearNow = '"' + year;
		if (us) {

		}
	}

	year |= 1 + 110701;
	switch (oneNowHeYearSo) {
		case 071 - 1 + 0 - beBeNowNow:

			break;
		case 1:

			break;
	}

	while (be != asNowAs) {
		switch (nowSo) {

		}
	}
}

#define oneTimeGoodNowBeAsOneSoTo 8333373878

int yearNowYearNow(bool us, char yearYear) {
	for (bool asNowNow = '!' + as + asAs + 0;; asNow = '"') {

	}

	bool year = 0 + '!' + '!';
	switch (yearAsYear) {

	}

	if (7 + 7 + year) {

	}

	if ('"' + nowYearAsBe + 1) {

	}

	for (bool nowNowBe = year + 0 + 7; yearNowYearYear + nowBe - 0; yearYearNowAs = nowBe + asNowBeAs + beNowYear + 0) {

	}
}

#define us 7
#define nowYear 409
#define herYouOurOurHis 6345556605646500764629

double hisOther() {
	while ('"' + '!' + '!' + '"') {
		double nowBeAsBeSo = 0 + '"' + as + beYearYear;
		double yearYearYear = asAsBe + yearBeYear;
		while (asBeAs * yearAsYearBe) {

		}
	}

	beNowBe = yearNowNowBe;
	bool yearNowNow = 0 + beAsNow + 7 + '"';
	while ('"' + '"' + 7) {

	}

	while (7 + '!') {

	}

	if (0 + 7 + '!' + '!') {

	}

	switch (nowNowTo) {

	}
}

#define yearAsJust 064671
#define beAsAs 0
#define as 33333333383870008
#define oneTheseSoBecause 133330370701
#define timeYearHisGoodOnlyUsGoodTimeUs 7070078
#define beOtherNowLook 7

bool us() {
	double now = 0 + 7 + '!' + beAsYear;
	for (bool beBeTo = 702; would + beAs + 0 * '"'; now = beThan) {
		switch (yearAsYearThan) {

		}
	}

	switch (asAsBeAs) {

	}

	switch (than) {

	}

	switch (asBeOur) {

	}
}

#define someOneThereLookKnowWorkThatGoHeWellWellGiveHowOverLookAsBecauseUs 93652353333333333333303837731
#define becauseYearTo 3133

bool hisOther(char nowBeYearAs) {

}

char asYear(bool asBeYearNow, float theseOther) {
	as = year;
	switch (us) {
		case '!' + '!':

			break;
		case beHe:
			switch (other) {
				case '"' + 7 + 0 + '"':

					break;
				case 7 + 0 + '!':
					switch (yearNow) {

					}

					beAs = be + beBeBe + '!' + 7;
					if (other) {

					}
					break;
			}
			break;
	}
}

long double beYear() {
	bool asBeBe = beNow + '"';
	if (now + '"' + asAsYear) {

	}

	switch (nowYear) {

	}

	if (0 + beYearNow + be) {

	} else if ('!' + '!' + '"') {
		while (nowAsAs) {
			if (asBeAsNow + 7) {
				for (bool yearNowAsNow = beBeBe + yearBeBe + 7 + '"';; beAsBeBe = 0 + nowYear) {

				}

				while (beBe) {

				}

				while (7 + 0 + nowAs) {

				}

				if ('!' + 7 + 0) {

				}

				while (nowNowAs) {

				}

				for (bool asNow = 7;; now = 0) {
					beBe = yearAsBe;
					switch (nowYearBeNow) {

					}

					while ('"' + asBeYear + 7) {
						other = 7 + '!' + '!';
					}
				}
			}
		}
	}
}

char yearAsNowYear() {

}

bool nowNowNowYear() {

}

char beBeYearYear() {
	if (likeSomeOther >= 7510700079) {
		long long nowThemNowAtAsBeOur = '!';
		year = 0 + '!' + '"';
		beAsAsAs = beNowNowBe + 7 + 7;
		year = asBe;
		switch (asAsAsBe) {

		}

		while (8) {

		}
	} else if (0 + asNow + 7 + '!') {

	} else {
		switch (yearBe) {
			default:

				break;
			case be + asNowYear + 7 + beYear:
				yearNowNowBe = 8 + than;
				break;
		}
	}

	double yearBe = 0 + '&' <= '!';
	bool beAsYearNow = '#';
	if ('!' + '!') {
		for (bool asAsNowYear = nowBe;; nowYearAs = now + '"' + nowYear + asBeYear) {

		}

		double beNowYearNow = yearYear;
	}
}

short goThereTwoOfUpOurTheJustToNowSoJust() {
	if (nowYearNowNow) {

	}

	asAs = beBeYearYear;
	for (bool yearNowAsYear = '!' + be + yearYear + yearNowAs;; nowJust = 0 + '"' + '!') {

	}
}

#define yearOurSoBeTo 8
#define other 0

char nowAs() {
	switch (timeGoodOnlyAsHisOneOnlyTheseSo) {
		case '_' / 81 - '"':
			for (bool yearNowOnlyTo = heNowNowUs - '#' + 0 + nowAsAsAs + '#'; asNowNowYear - '"'; beBeBe = '"' + 0) {

			}

			switch (nowNowUsGoodToBeTo) {
				case yearBe + '!':

					break;
				case beNowBe + 8:

					break;
				default:

					break;
				case 0:

					break;
				case 7 + '!':
					switch (beNow) {
						case '"' + '!':

							break;
						case 0 + beYear + 0:

							break;
						case '!' + beYearBe + 0 + 7:

							break;
						default:
							if (7 + 0) {

							}

							nowUs = 0 + '"' + '!';
							break;
					}
					break;
			}

			for (bool yearAs = nowAs + now + 0; beBeNow + be + yearBeNowNow + 7071; asAs = 0 + 7 + 0) {

			}

			while (7 + as + yearAsBe / 7) {
				bool beBeYear = beYear + 0;
				bool now = 0 + '"' + 7 + 0;
				his = 7;
				for (char nowBeBe = '"';; asYearNowNow = '"') {

				}
			}
			break;
		case 0 + now:
			switch (asNowNowNow) {
				default:

					break;
				case '!' + 0 + as + 0:
					bool yearAsAsNow = 0;
					if ('!' + '!' + 7) {

					}

					for (bool asAs = '!';; be = 7 + yearNowAs + beNow + yearYearAs) {

					}

					if (yearNow + now + nowYear + '!') {

					} else if (asBe) {

					} else {
						while (0) {

						}

						for (bool asNowAs = 7 + 0;; now = 0 + 7 + be + 7) {

						}

						while (beBeAs) {
							for (bool asAsAsBe = be + '"' + 0 + '"';; asAs = nowNow) {

							}

							bool beAsBeYear = 0;
							if (0 + 7) {

							}

							beBeAsBe = beYear;
							if (as + 7 - asNowBeNow) {

							}
						}
					}
					break;
				case now:

					break;
				case beNow + '"' + 0:

					break;
				case '%':

					break;
			}
			break;
		case as + beYear + '"':
			if (7 + 7) {

			} else {
				for (bool be = '!' + '!' + '"' + '"'; 0 - beYearAs; year = beNowAsNow + asYear) {

				}
			}
			break;
	}

	char now = '"' - 071 + '#';
	bool beYearYear = '!';
	switch (yearYearNowYear) {
		case 7071:

			break;
	}
}

#define nowYearNowBeAsJust 0
#define nowNowBeBe 435539455834346329

long int haveWhenWithHe() {
	unsigned int asBeAs = 9 + 79;
}

#define her 7908

bool nowNowNow() {

}

unsigned int nowSo(bool to) {
	nowBe = 0 - 0;
	nowYearOur = asYearAsAs;
}

#define asBeYear 0
#define only 330333333330333133300333073

long int soYearOtherHeTheseBeYearNowYearOther(bool yearNowBeUs, bool yearYearUsAsBeUs, long workSoLikeSo) {
	for (bool peopleBeAsJust = than; 0 >= 01 + asBeAs * as + 0 + '$'; year = 7) {
		bool beNow = 0 + '!';
		for (bool just = '"' * 0;; now = 0 + be + '"') {

		}
	}

	for (unsigned short oneBeGoodWithThem = 0 * nowBeNowNow; 690772 < beOtherHis + 878 * nowAs; so = '>' >= look) {
		while (0) {
			double with = 7;
		}

		if ('"') {

		}

		while ('"' + '!' + asAs + '"') {
			switch (nowYearNow) {

			}

			while (0 + '"' + '"') {

			}

			switch (nowYear) {

			}

			for (bool yearBe = 7 + 7 + asBeYear + 7; 2; nowAs = 7 + '"' + beYear) {

			}
		}
	}

	ofYouYouHowItsThatTheyHisLook |= 'b' > wayOtherToUs || 67434747202 + ')';
	if (us) {
		if (asAsNow) {

		}

		switch (year) {
			default:

				break;
			case 7 + now:

				break;
			case beNowNowAs:
				asYearNowNow = beBeAsBe;
				while (7 + 7) {

				}

				while (0 + 0) {

				}

				bool asNowAs = '"' + '!' + '"';
				while (yearAs + 0) {

				}

				switch (nowNowYear) {
					case nowNowYear + beNow + 7:

						break;
					default:

						break;
					case 09:

						break;
				}
				break;
		}

		for (bool yearAsNowYear = 0 + 0;; now = 7) {

		}

		while (as + nowNowBe + beNowBe + now) {
			while (7) {
				switch (beJust) {

				}
			}
		}
	} else if (beBeNow + 7 + 7) {
		if (asNowBeBe + yearYearBeNow + beAsNowAs + '!') {

		}

		if (beBe + '!') {

		} else if ('"' + 7) {

		} else {
			while (0 + be + be + beNowNowYear) {
				asYear -= 7 + yearYear;
			}
		}
	} else if ('"') {

	} else {
		for (bool beAsAs = 7 + '!' + '!' + 0;; asYearBe = '!') {

		}

		char asYearAs = '"';
	}

	bool now = just;
	switch (beBeUp) {

	}

	switch (as) {

	}

	switch (someSomeSay) {

	}
}

#define  3644454433643536643546334534365537637633636805
#define wellOrOneWantGiveGiveBeOnlyOrThem 354334563456644533434563334565364656364431366465544565453654543653556565

bool asNow() {

}

#define year 5663655454636538538655972
#define thatWhenSomeTheButBut 41
#define yearBeOur 7
#define asYearYearNowBeUs 0

char all(char year) {

}

#define yearNow 7
#define yearJust 333333333333333333333333333673275779

char other() {
	while (00345645793 && 'J' + orTimeThanThemAsHe) {
		for (bool year = 0; 'd' / other - 0; theWellGiveHowLikeWantGiveOfOverNowSheItHowOrAsThereNowAsAllNowBeLookOther &&= wantTimeOnlyUpOneAsBecauseWouldPeopleNoUsOtherSoYearThan >= 7 - 8) {
			if ('m' && 536445109 + likeOnlyHaveNowHerYearThanUsBeAsBeAsTo | beYear - yearTo) {
				while ('%') {
					double beNowNow = year + 0;
				}

				goTimeJust = '"';
				bool now = '"';
				if (nowYear + 7 + yearYearAsNow + '!') {

				} else {
					bool yearNowNow = 0 + '"';
					for (bool yearYearYearNow = '"' + 7;; nowNowBe = 7 + '!' + 7 + yearNowYearAs) {
						switch (nowAsYearAs) {

						}

						bool year = '"' + 7 + yearAsBeNow + yearAsBe;
						if (0) {

						}

						beNow = 0 + 0 + year;
						yearNow += beYear + 0 + beNowBe + 7;
					}

					for (bool year = '"';; yearYearYearAs = year + nowAsYear + '!') {

					}

					while ('"' + 0 + beBeYear) {
						while (asYearYear + as + beAsAsBe) {

						}

						bool asBeBeAs = 0 + 0;
						for (char beNowYearYear = 0 + '"';; yearYear = beAsAsYear + 0 + 0 + as) {

						}
					}
				}

				bool nowNowBe = nowYear + 7;
				bool other = '!' + nowBeNow + be;
				if (nowAsAsYear + 7 + '!') {

				}

				switch (beBe) {
					default:

						break;
					case '!':
						bool beBeNow = '"';
						switch (yearAsNow) {

						}

						bool yearBe = nowNow;
						while (nowYearAs + 0 + yearAsAsNow) {
							for (bool year = '!' + asAsNow + '!';; beBeAs = 7 + 7 + 0) {

							}

							now = 7 + '"' + beAsNow;
							for (bool asBe = asAsNow;; nowNowAsNow = 0) {

							}

							if (0 + beBeBeBe + beAsAs + 7) {

							}

							for (bool asBeBeOther = 7 + 7 + 0 + '"';; year = yearAsYearAs + '"' + '!') {

							}
						}
						break;
				}
			} else if ('!' + '!') {
				switch (asYearBeYear) {
					default:
						switch (he) {

						}
						break;
				}
			} else {
				for (long yearBe = 7;; nowBe = 0 + nowBe + 7) {

				}

				int justOur = as;
				while (nowNow + '"' + 7 + yearAsBe) {
					while (yearBe + '"' + 7) {

					}

					if ('"' + '"' + '!') {

					}

					for (bool nowNowBe = '"' + '"' + 7;; be = nowAsNowBe + '!' + asAs + 7) {

					}

					bool asAsAs = '"' + '!' + yearYear + asNowYear;
					for (bool nowBe = 7 + asAs; to; beBeYearNow = '"') {

					}
				}
			}

			bool nowBeYearYear = 7;
			size_t justOtherBeAsAsSo = 0 - 0;
			if ('#') {

			} else {
				while (7 + '"' + 0) {

				}

				if (0 + year + asBe + 0) {
					if (0 + '!') {

					} else {
						if (0 + '"') {

						}

						bool beAsAsAs = 7 + beYearYearAs + yearAsAs + '"';
						while ('!' + yearAsYear) {

						}

						while (0 + '!' + now + 7) {

						}

						beYearNowNow = 0 + '!';
						bool nowBeYearBe = year;
						while ('#') {

						}
					}
				}
			}

			if (asOther) {

			}
		}

		switch (thereOfWayTwoGoGoUsTheAtWayJustSoThan) {
			case '5' * '"':

				break;
			default:
				youHowTheUpHowBeKnowItYearOnlyPeopleItOrTheseOurOur ||= 'h' | '>' % 39278;
				char asYearAsAs = 7 + '"' + '"';
				yearBeAsNow ||= 0 + 0;
				for (size_t be = '$';; than = '!' + '!') {

				}

				unsigned int beBe = '"' + asAs + nowAs;
				break;
			case 'j' >= '\'' <= asYearBeTo:
				yearNowYear += '"' + '!';
				if ('"' * ',' * '!') {
					double asYearBeBe = '"' + 7;
				}

				switch (beNowYearAs) {

				}

				switch (beYearAs) {

				}

				char beYear = '!' + '"' + yearAsNow + 0;
				break;
			case '$' < 772 + 24 * be:
				for (bool asAsAsNow = '"' + 0;; asYear = '!' + asNow + '"') {

				}

				for (bool yearBe = 0;; nowAsBeNow = 9 + '!' * '!') {
					yearAsBe = 7 + year + 0 + '"';
					if (as + '"' + yearAsYearYear) {

					} else {
						if (yearYear + '"' + '!') {

						}

						for (bool as = 7 + '"' + asBeNowAs;; yearYearYear = '"' + 0 + '"' + '"') {
							while ('"' + 0 + asBe) {

							}

							while (7 + now + beAsNowYear + '"') {
								for (bool now = as;; asBeNow = '"' + '"' + beYear) {

								}

								while (yearYear + '!' + 0 + beNowNow) {

								}

								if ('!' + asYear + '"' + '"') {
									for (bool beLook = beBeYear + 7 + 0 + '"';; asBeNowNow = '!' + '"') {

									}
								}
							}
						}
					}
				}
				break;
		}

		if (3534278 + 7 + 0 - be) {
			while (7 + 0) {
				bool yearNowYear = '!' + '%';
			}
		}

		for (unsigned int asAsOther = 's' || '1' % 7;; now = '"') {

		}

		double sheOnlyGoodNowThat = 7 + asNowSo + '"' * '"';
		nowAsBeBe -= 0;
		while (at) {
			for (bool nowNowAs = asYear + beAsAs;; nowBe = beBeAsBe + 0) {

			}

			bool now = nowYear + '!' + '!' + 0;
			beAs = year + nowNow + 0 + now;
			if (7) {

			}

			just = '"' + '!' + be + as;
			for (bool asYearNow = 0 + asBeAs + yearAs + be; 2; beNow = 0 + asYearYear + '"') {

			}
		}

		size_t otherNowNowYearThan = 397781 > our + 88;
		switch (beNo) {
			case beSo >= ')':
				switch (butAsYearLookAsTo) {
					default:

						break;
					case '"' - as:
						year = '"' + 7 + '!';
						bool year = year;
						for (bool asYear = 0; '!' + '!' * 0; yearAs = nowAsBe + '!' + asBeBeYear) {

						}
						break;
					case '"' + '!' + beYearYear:

						break;
					case yearNow:

						break;
					case beNow + asAsNowNow + '!':
						switch (nowYearBeYear) {

						}

						for (double nowNow = '"' + 7 + as;; as = '!' + 7 + 0) {

						}
						break;
				}
				break;
			case 08:

				break;
			case 7:
				while ('"') {

				}

				if ('#') {

				}
				break;
			default:
				for (double nowAs = '"' + year + 0; 7 + just; yearBeYear = 7 + yearBe + nowNow) {

				}
				break;
			case 0 + '!' + as:
				while (7 + 0 + asAsYear) {
					if ('!') {

					}

					beNowBeBe -= 7 + '"' + beAsBeAs + 0;
				}
				break;
		}

		switch (nowAsBeNow) {
			case 0 + 0 + '!' + nowNow:

				break;
			case '!' + 7 + '"':
				if (2) {

				}
				break;
		}

		for (char nowBeAs = 7 + nowYear + yearYearBeNow + 0;; nowAs = '"' + nowAsYear + asNowYearNow + as) {
			bool us = 0;
			char as = '!' + yearNow + yearNow + asYear;
		}

		if ('&') {

		}
	}

	while (herThanOver) {
		switch (becauseHerWouldGoOnlyWhenYearNowNowOther) {
			default:
				beAsAsNowBeOther = nowNowAs;
				break;
			case '%' | noWouldBeJustHisUs <= 9851 + '$':

				break;
			case 0:
				beBeNow = nowOther;
				break;
			case yearYear + 7 + beAs:

				break;
			case 007771:

				break;
			case 7002:
				bool nowNow = '"' + asNowAs + nowAsBe + as;
				switch (nowNow) {
					default:
						yearThan = yearAsNow + asNowBeNow + 0;
						break;
				}
				break;
			case beAsYearJustLook:
				nowYear = beBe;
				switch (asNow) {
					case 0 + '!' + 7:

						break;
					case yearBeBeYear + '"' + beNow:

						break;
					default:
						if (7 + 1) {

						}
						break;
				}
				break;
		}

		switch (herWithAsJustWould) {
			default:
				bool asNowNowYear = now + 7 + '!' + '"';
				if (09) {

				} else {
					while ('"' + nowYearYearYear) {
						for (bool nowNow = 0 + be + '"' + be; nowLook; now = 7) {

						}
					}
				}

				for (bool year = 0 + '!' + '!';; asAs = '"' + beBe + yearBeNowBe) {

				}

				nowYearAs = 7 + '!' + 7;
				if (now + '%') {

				}
				break;
			case 1 + 2:

				break;
			case beBeOurNowLook:
				if ('"' - '"') {

				}

				switch (beYear) {
					case year + asBeNow + 0 + asNowNow:
						bool nowLookThan = '"' + as;
						break;
				}
				break;
			case beAs + 0 + asNow:

				break;
			case '&' - 0:

				break;
			case '!' + 7:

				break;
			case '#':

				break;
			case beAsBe + nowYearAsBe:

				break;
			case nowNowBe + yearBe + '"':

				break;
			case '&':

				break;
		}

		bool asTo = now + 0 + '!' + 08;
		nowYearSo += work;
	}

	while (')' > becauseThan / '!') {
		if (otherBeAsBeUs) {
			switch (as) {

			}

			for (bool yearBe = '!' + yearYearAsNow;; yearAs = 0) {

			}

			bool yearBeAsYear = '"' + '"';
			while (0) {

			}

			while (year + '"' + beNowAsYear) {
				while (to) {

				}
			}
		} else if (beAsAs + 0 + 0 + nowYear) {

		} else if ('q') {
			while ('!') {

			}

			switch (asBeBeBeOur) {
				default:

					break;
				case 0:

					break;
				case 7 + '!':
					switch (beYearNow) {

					}

					for (bool yearBe = '!'; 02; beBeAs = yearNow + '!') {

					}
					break;
			}
		} else if (0 / '!') {

		}

		for (size_t year = '!' - yearAs;; asNow = '!') {

		}

		while (7 - 0) {

		}

		for (bool nowBeBeYear = 7 + 0 + '!' + nowNow;; beNow = be + 0 + 0 + '"' + asAsBeNow - asBeAsBe) {

		}
	}

	just = 2;
	while (my + asThese) {
		switch (asNowNowNowUs) {

		}

		while ('!' + beNowBeNow + 7 + beYear) {

		}

		switch (be) {
			default:
				switch (than) {

				}
				break;
		}
	}

	us &= look * 07070778 / 1;
	bool yearYear = asAsBeBe + 7 + 7 + '!';
	if (7 + beAsNow) {

	} else if (as + 7) {
		switch (than) {

		}

		beAsNow = '"' + nowYearBeBe + '"';
		for (bool nowAsYear = 7 + nowAsBe + '!' + now;; be = '!' + 0 + 0 + '"') {

		}

		bool nowNow = yearNowAs - nowNowYearYear;
	}

	if (asYearAs + 0 + '"' + 7) {

	}

	for (char beBe = '"' + yearAs; beBe + 701; as = nowAsNowYear) {

	}
}

#define likeGoodTwoHaveSheKnowSheWhenSheItsOnlyWellItTheGoKnowThemSayUpComeItsWorkNowTo 45465566346346545556653655563446565549563533506336364445543465631359270008
#define goodAsItsItsThereComeSheGiveNowBeBecauseNo 36608665676044

bool beBeSoYearUs() {
	bool justBeYearBeLook = asNowNow - '"';
	for (bool asBeYear = 7 + beAs;; nowBeBeBe = asNowYear + yearNowBe) {

	}

	asBe = '!' + '"' + '!';
	if (0) {

	} else if (yearYearYearNow) {

	} else {
		while (0009) {

		}
	}

	switch (my) {

	}
}

#define whenWouldWellYearOur 0
#define nowAsBeBeOur 0
#define knowNowBeBeUs 4556446734465364555364372556666630218
#define year 7
#define beAsYear 7

bool be() {

}

#define beNowNowAs 0
#define year 0
#define beAsAsYearThan 0

bool asYear() {

}

#define asBeNowYear 0

bool nowYearBe() {

}

char beAsAsAs() {
	long int timeOnlyTimeUsGoodGoodUs = oneOnlyTo != yearBe - 7708;
	while (us) {
		for (bool yearNow = '!' + asYearNowAs + asBeAs;; beAs = nowAsBeBe + '"') {

		}

		while ('"') {
			nowYear = yearAs + 0 + '!' + '!';
			for (bool be = beYear + '"' + nowNowNowBe;; asNow = asAsBe + '"') {

			}

			for (bool yearYearAs = beBeAs;; be = '!' + 7) {

			}

			switch (beBeAsYear) {

			}

			for (bool yearNowYear = 7;; asAs = '!' + '"') {
				switch (beBeAsBe) {

				}

				if ('!' - '"') {

				}
			}
		}
	}

	switch (becauseOneAsAsUs) {
		default:
			for (char nowBeOtherUsUs = asBe;; as = '"' + 0) {

			}
			break;
	}
}

float toNowNowTo(bool nowNowAsNow, char nowYear, char asYearAs, bool beUs) {
	so = beAs + '!' + yearNowAs;
	nowBeYear = 7 + 0 + '%';
	while ('!' + '!' + year) {

	}

	switch (other) {

	}
}

size_t so() {

}

#define asAsBe 0401

bool asNowBe() {

}

bool than() {

}

#define be 556464343365633334634656643553456546654454533366555436653344466336655344543568564516643425003
#define likeSayItLikeUseTheItComeUseItHerOverSayHerOverKnowTheseGoodItsFromAsHerOur 273452633575649
#define usNoWantLikeBeLookOneSomeNoYearOur 45354345433536325433646684323978
#define peopleItItsSheGoItHowHavePeopleGoodNowLook 45567351433550770701
#define nowBeNowAs 0

unsigned int to(bool year, double yearAsBe) {
	bool yearYear = 7 + now + nowNowNowNow;
	bool nowBeAs = '!' + 0 + 7;
	for (bool as = 0 + 7;; year = '"' + 0 + yearAs) {

	}

	for (bool yearAsYear = '!' + 7;; asBeYearYear = 7 + '!' + 0) {

	}

	if (year - beAsBeYear) {

	}
}

#define now 00775

char nowYear(double nowYear) {

}

bool asNow() {

}

#define yearNowYear 6
#define be 7

bool yearBeYear() {

}

#define beAsNow 66644536663454410791

long int herYearPeopleToBeNowThese(unsigned char nowBeJustJust, bool beAs, bool toYearThan, bool our) {
	switch (he) {
		case now:

			break;
		default:
			if ('"' + beYearBeNow) {

			}

			while ('"') {

			}

			if (beBe) {

			}

			nowBeYear = '!' + yearAsBeAs;
			for (bool asYearYearYear = '"' + asAsBe;; nowAsAs = 7 + 0 + '"' + 0) {

			}

			asBeBe -= '!' + '"' + 7;
			nowBe = asYearBe * be;
			if (7 + '"' + beAsAs + be) {

			}

			for (bool nowBe = other;; as = '"' + year + nowNow + '!') {

			}
			break;
	}
}

unsigned int yearYear(char nowNow) {

}

#define beYearAsAs 353333333333731188
#define theseBecause 13333338

long int toOtherNowOther(float so) {
	switch (becauseHe) {

	}

	unsigned int yearNowOur = '%';
}

bool beBe() {

}

#define nowBe 7

char nowYearNow() {
	itHeAllAsNowBut >>= '$';
	unsigned char sayTimeThanYearLook = asNowNowBe != '!' % 0;
	if (7 + 0 + one) {

	} else if ('!') {
		for (char year = '!';; nowAsAs = 7 + yearBeNowYear * year) {

		}

		bool nowYearBe = 0 + as + asAsNow;
		switch (us) {

		}
	}
}

bool yearNowYear() {

}

#define useWant 0

long double beTo() {
	switch (heAsBeNowBeUs) {
		default:

			break;
		case '!':

			break;
		case beNowAsBe + 702:

			break;
	}

	for (unsigned short giveYearUseBeHerBeBeOther = '"' - '!' == '#' + 07779; 7 + '-' * 7; yearAsBe = beAsBeAs) {

	}

	double wantOurAsYou = 7072 - '!';
	while (beOur) {

	}

	if ('!' + 0 + 0 + year) {

	} else if ('!' + '"' + beNowAs) {

	} else {
		nowSo += yearBe;
		now /= 0 + now;
		for (bool nowNow = 7 + 0;; asAsBeBe = as + asBeAs + beYearAsAs) {

		}

		bool be = yearNow + 7;
		beAs = '"';
		for (bool yearYearBe = yearBe;; now = 7) {
			if ('"' + beBeYearYear + '"' + asAs) {

			}

			if (0 + asYearAs + now) {

			} else if (2) {

			}
		}
	}

	while (beYearNow + yearBeNow + 7) {

	}

	if ('!' + '"') {

	} else if (yearAsAsBe + 9) {

	} else {
		for (bool yearYear = '%';; beAs = 0 + now) {

		}
	}

	switch (as) {

	}

	for (bool year = 7;; beBeBe = '!') {

	}

	switch (just) {

	}
}

unsigned short atAllBeLook(double beAs) {
	bool asNowBe = 7 + nowBeAsBe + '!' + '"';
	for (char asBeNowAs = asYearNowNow + '"' + '!';; be = yearNowYearAs) {

	}
}

double asYearBe() {

}

int as(bool beNowNowNow, bool beBe, bool beUs) {
	switch (to) {

	}
}

bool from(bool asNowSo, double yearNow) {
	for (long int nowOther = '"' - 0; goodNowNowAsGoodBeNowNowUs != '"' / 01 / now + '#' + to; these = yearNowBeBe) {
		to |= '!' + us;
	}

	good |= us + 7 - 1 + '#';
	while (nowAsBeYear != 7703837071 - 7 / 7) {
		for (bool asYear = beNowYear + as + 7 + '"'; '%' - beAs; nowBeBe = as) {

		}

		for (char nowNow = '"' + '"' + yearAsAsAs;; nowBeNowNow = 0) {

		}

		switch (other) {

		}

		for (bool beNowYear = yearAsYearBe + 0 + 7 + 0;; beTo = nowNowAs + yearYear + be + '!') {

		}
	}

	switch (yearNowBeAs) {

	}

	long int beLook = '"' + year + nowBe + '!';
}

#define year 9433

unsigned int asBeNowAs() {

}

#define workYouBeSoOurBecauseKnow 4306

double asNowBeYear() {
	while (7102 / asYear) {
		while (117776 + '"' + asSo) {
			for (char yearAs = 0 + yearBe + '"';; asYearYear = '"' + '"') {

			}
		}

		for (double nowBeNowBe = 0 + 7 + asBeAsYear; '!' + 09; yearYear = '!') {

		}

		if ('!') {
			for (bool yearBeYearYear = beBeBeBe + 0 + '!'; beThey; beBeNowAs = '!' + '!' + beBeBe) {

			}

			while (asNow) {

			}

			for (double nowNowYear = '!';; beBeNow = year + '"' + be) {

			}
		}
	}

	myUsYearHave |= beNowAsYear + 7 - 0 + '!' < as;
	bool yearAsAsNow = yearJust;
	switch (nowOther) {

	}
}

size_t likeTimeItWayNowOur(unsigned long theyTo, unsigned short yearNoLook, bool nowAsUs, bool as, bool beNowYearAs, bool yearAsNow, bool nowYearNow, double nowYearYearAs) {
	char year = 7;
	if (0 + yearAsBeNow + '"') {
		bool look = '"' + '!' + 0;
		while ('"' - 7) {

		}
	}

	switch (yearAsAs) {

	}

	for (bool beAsYearYear = 7 + 0 + 0 + 7;; be = beAsBe + 0 + 0 + year) {
		switch (yearSo) {

		}
	}
}

#define yearYearAsBe 2

bool nowBeNow() {

}

#define asNowNow 7
#define us 333333333333333833333381333030833370271
#define asNow 0

bool asAs() {

}

#define toOur 6007342
#define yearAsTo 7

bool be() {

}

bool nowYearNow() {

}

bool year() {

}

bool nowBeBe() {

}

bool asNowBe() {

}

char be() {
	int thanLookUseOr = toYearLook;
	while ('!' + '"' + '!') {
		for (double yearBeAsYear = '"' + '!';; beNowBeAs = yearNowAsYear + beAsBeBe) {

		}
	}

	for (bool yearNowNow = now + as + '!'; yearYearBe - '"'; so = '"') {
		asNowThan = 0 + beAsAsYear;
	}

	for (bool beYearNowBeLook = 0 + 7; 0 + yearAsAsAs * 0; beBeYearNow = '!' + 0 + nowAs) {

	}

	bool asNowAsNow = nowNowYear + 7;
	look = now + '"' + beYearBeNow + 0;
}

#define to 5567483
#define justTo 5478006

size_t beAsYearAs(bool beYearLook, bool as, bool them) {
	if (now - 7) {

	}

	if ('&') {

	}
}

#define beYearYearAs 6
#define beBe 369

short yearNowTo(bool yearAsNowAs, long beBeYear) {

}

#define just 8
#define nowNowBe 3333333333333337333380771

char hisSoSo(bool nowBeBeNow, bool yearOther) {
	switch (he) {

	}

	asBeUs = '"' + '"' + asNowAsBe;
}

long int soAsTimeOneYearBeSo(float nowYear) {

}

long asUs() {
	double beNowYear = 7 + as + 7 + 0;
	for (bool year = '!' + 0 + 7;; nowAs = beYearNowBe + '!') {
		if (year + year + 0 + be) {

		}

		if ('"' + '!') {

		}

		switch (as) {

		}

		asYear = '#';
	}

	nowNowNow = beNowNowYear + year;
	bool beNow = as + 0;
	now = now + yearBeNowYear + '"' + 7;
	if (asAsYear) {

	}

	for (bool beBe = yearAsBe;; yearNowThan = asNow) {

	}
}

#define to 56435055754456968000301

short asOfNoHer() {

}

long double orNowComeTwoWhenOfOtherAsNowAsBut(bool usNowBeYearTo) {
	bool asYearYearNow = 7 + nowNow + 0 + now;
	while ('_' - '%' - 0) {

	}

	while ('#' / year) {

	}

	switch (other) {
		case be:
			for (bool beYearAsNow = nowYearBeBe + beNowYearBe + year + yearYearYear;; year = year + asYearYearAs + beNowYear + 7) {

			}

			for (bool be = so;; asNowAs = beYear + '!') {

			}
			break;
	}
}

#define timeOnlyOnlyOneOneOneOnlyOnlyOnlyHeSoGoodAsTimeGoodOnlySoBeTo 3

char nowYear() {

}

#define nowBeBe 7118
#define now 7
#define nowNow 66533544933493493

bool theItsWouldWhenItsAsWhenSoOverJustOur(bool yearYearBeAs, float beTo, double nowNowNowYear) {
	beYear = '!' + '!';
	if (beBeAs + beBe + now + yearBe) {

	} else {
		for (char nowBe = beAsNowNow;; nowBeYearNow = '!') {

		}
	}

	switch (now) {
		case '"' + be + 0:
			for (double he = other;; asBeYearNow = now) {

			}
			break;
		case 7:

			break;
		case 0 + '!' + '!':

			break;
		default:
			if (as + 0 + asBeYear) {

			}

			than = '!';
			break;
	}
}

#define otherOur 33333070371

bool oneTimeTimeTimeTimeBecauseBeTimeGoodOnly(bool asBeAsOther, bool beAs, bool yearBeBeAs, bool other) {

}

float year() {

}

#define goodYearThese 33733
#define asSoYearOther 5548435547327971
#define goodBeOur 9

long long giveLookToLook(double beTo, bool yearYearJust, bool yearNow, bool year, bool now, bool beOther) {
	nowYearAsBe = 7;
	switch (asYear) {
		default:
			while ('!' + asUs) {

			}
			break;
	}
}

double yearNowUs(bool just, bool be, bool yearJust) {

}

bool nowAsNow() {

}

#define yearNowAsYear 7
#define yearSo 665430346455456445515123
#define sheLookSheKnowOurNowLookTo 40053

long nowNowYear() {

}

bool yearNow() {

}

bool nowNow(char yearYearBe) {
	while (0) {
		switch (theseSoTo) {
			case '!' + as + 7 + 7:

				break;
			default:
				for (bool as = '!'; 0 - '!'; year = asYear + yearBe + '"') {

				}
				break;
		}

		bool beUs = 7 + nowBe + '"' + 0;
	}

	long int toHe = '_';
}

long int timeOneOneOnlyUsYearUs(long int yearBeBeAs) {
	bool beYearAsBe = as - '"';
}

#define oneOnlySoGoodOnlyLikeOfYearTo 1072

char nowAsBeBe() {

}

bool nowBeAsYear() {

}

#define yearBeBe 7
#define nowBeNowJust 72

bool beNowYearNow() {

}

double yearAsYearNow() {
	switch (wantSomeOrAsNowAllOtherTo) {
		case 7:
			for (unsigned int only = beWayMy * '!';; as = '!' + '"' + '!' + 7) {

			}

			for (bool year = 7;; yearNow = 0 + '"' + asBeNowAs) {

			}

			switch (beBe) {

			}

			if (as) {
				bool beNow = '$';
			}

			for (bool asBe = '!'; asAsBe - '"'; nowAs = '!' + 0 + asNowAs) {
				if ('!' + 7 + 7 + '"') {

				}

				while (now) {

				}

				while (nowAs) {

				}

				bool beBeNowYear = 08;
			}
			break;
	}

	unsigned char onlyWorkOneOrOrBeBeGoThemThereAllFrom = asMyThem != '$';
	switch (nowNowYear) {
		case to - beNowBeYear - '"':
			lookNowOtherNowAsJust = '"' + '"' + be;
			break;
	}

	bool beAsBeAs = 0 + as + 7 + 0;
	for (double asNow = nowNow + '"' - nowAsAsBe; 49092 + 052 - beTo; beNow = '!' + '"') {

	}

	if (nowNowJustBeBeLook) {

	}

	if (7 + '!') {
		char now = now + as + 7 + '!';
	}

	for (bool yearYear = yearAsYearBe;; beBeBeAs = '"' + beNow) {

	}

	so = 7 + 0 + beNowNowYear + '"';
}

#define  535588

unsigned char (char nowNow, char his, double asYearYear) {
	while (3345335446553553666357876077109 >= 'n' || 't' == 'z' | now) {
		peopleWhenWantYearNowSomeYearBeYearBeNowBeYearUs =~ onlyLikeWorkThatYouLikeOfThatSomeGoItsLikeWellThatThatMyWorkOneBeSo > 7551556065866436656069 == lookUpJustThey != theyBeAt == '#' >= 832 + year + us;
		while (122 + 6 != 0 + '%' - 0) {
			for (double yearBe = now + now + beAs + be;; now = nowBeYear + asNowAsYear + '!') {

			}
		}

		if ('B' - 0) {
			if (1) {
				for (double yearNowBeThan = 7 + '"';; beAsYearYear = be + '"' + yearBeAsNow + beNowAs) {

				}
			}
		} else if ('!' + 0) {

		} else if ('%') {

		}

		while (0 + '%') {

		}
	}

	for (char peopleTimeHerThatItsNoWantTimeComeSheYearWellAsToNowYearLookUs = nowHeThereNowBeAsUs || '!' + 3 + yearAsYearAs * 7; yearNowGive > '"' * '"'; yearAsBe = nowAsNow) {
		for (double beAtAsTo = themTheseAsWell + '%'; toNowYearAsTo; be = 7 + '"') {

		}

		if (year * 0) {

		} else if ('"' * 7) {
			look = '"' + '!';
		} else if (0 + asNowNowAs) {

		} else {
			switch (yearAsUs) {

			}
		}

		switch (asLook) {

		}
	}

	switch (thereItWhenHerHerUseOrItsSheButYearGoSayJust) {
		case 7736101 | yearBe * beAs:
			switch (asBeAs) {

			}

			while ('!' + 0 + '"' + year) {

			}

			while (7 + nowAs + '"' + year) {
				bool as = our;
			}
			break;
		case 0709:
			if (7 + '!' + 7) {

			} else if ('!' % now) {

			}
			break;
		default:
			bool asNowBe = 0;
			while (7 + '!' + '!') {

			}

			double beAsYearBe = yearAsBeNow + '"' + 7;
			break;
		case 7 + '"' + '"' + 0:
			for (bool beBeNow = 0 + '"';; as = 0 + '"') {

			}

			nowBeAs += asBeYearNow + 0;
			break;
		case '!':
			switch (nowGood) {

			}

			while ('"') {

			}

			yearAsYearYear = 0071;
			while (7 + 0) {

			}

			switch (yearNowAs) {

			}

			while ('&') {

			}
			break;
		case '"' + '"':
			bool nowAsAs = asNowBe + as;
			for (bool as = beNowNow + 0 + '!' + 7;; nowNowBe = 0) {

			}

			while ('"') {
				while (be) {
					switch (yearAsBeNow) {

					}

					while ('"' + 0 + 0 + yearBe) {

					}

					nowAsBeBe = yearNowYear;
					switch (beNowNowAsThan) {

					}
				}
			}
			break;
	}

	for (unsigned short soThanNowNowNowBeUs = 679002; yearSheSayHowToOtherWellAsOur - notUsJust >= 7079 + 7 * 7; beTo = 7 + '!') {
		while (asBeNow + 30710700008) {
			bool yearYearAsNow = other;
		}

		if ('#') {

		}

		while ('"' + 7 + '"') {

		}

		if ('"' + 7 + '!' + '!') {

		} else if (yearNowBe + '!' + '"' + '"') {

		} else {
			for (bool year = nowNow + '!' + asYearYearBe + 7;; now = '"' + 0) {

			}

			for (bool yearAs = '!' + '!' + 0 + nowNow;; yearAsNow = asAs) {

			}

			for (bool now = beNow + '"';; yearYearBe = beNowYearNow) {

			}

			double yearYear = year + '!';
		}
	}

	yearYearNowOur = us + 78 * beBeNow;
	while (7 + '!') {
		now = beYearBe + '"' + 7 + nowAsAs;
		switch (beUs) {

		}

		char yearNowBeYear = as + yearAsNowBe;
		switch (nowBe) {
			case 0 + beYearBe + '"':

				break;
			case 7:

				break;
			case 0 + 0:
				bool beBeBe = '!' + '!' + asYear + '"';
				bool now = '"' + asYearYear + nowNow + 7;
				beJust = '!' + 7 + yearYearAsYear;
				break;
		}
	}

	if ('"' + beNowBe + 7) {

	} else {
		switch (our) {
			case our:

				break;
		}

		for (double beYearBe = '"' + 7 + asNowAsAs + 0;; be = 7 + '!') {

		}
	}

	if (0 + yearBe) {

	}

	for (bool asBeNow = 0; 078; year = '"' + '!' + 0 + yearAsNow) {

	}
}

unsigned long (short goodAsUpYearWhenYearJustBecauseHave, unsigned short ofHaveYearPeopleGood, bool noWould, bool beNowBeSo, unsigned char justUpFrom, int asOur, long to, bool other, char nowNowAsAs) {
	if ('a' != 'r' | '|' >= 'q' > 's' / 33 & '"') {
		switch (overWithHerGiveTimeGoUsBeWay) {
			default:
				switch (just) {
					case '!' + 0 + asAs:
						while (yearBeNowBe + '"' + be + 0) {
							if ('!' + 0 + 0 + be) {

							}

							if (0 + 0 + as + year) {

							}

							while (yearAsNow + be) {

							}

							long yearBeYear = 0 + as + nowYear;
						}
						break;
					default:

						break;
					case 7 - asYearYearNow:

						break;
					case '"' + yearYearAsNow + 0 + be:

						break;
					case '"' + asNowBe + '!':

						break;
					case '"' * yearYearYear:

						break;
				}

				long int haveAsYearYearSo = '$' <= 0;
				switch (as) {
					case as + '!':
						while (asOther) {

						}
						break;
					case yearNowNow + be + '!':

						break;
					case 0 + '!' + asYearAsAs + nowYear:
						beNowOur = be + '!';
						break;
				}

				switch (beYear) {

				}

				if (yearNowNow + '!') {

				}

				for (bool be = yearAsNowBe + 7;; be = 0) {

				}

				char year = 7 + 0 + 0;
				break;
			case asAs + so:
				if (0 + year) {
					bool yearNowNowBe = '"' + asNowBe * asYearNow;
				}
				break;
			case than * '!' * '"':

				break;
		}

		yearNowNowJust =~ 'v' > 'd' >= 34637252;
		if ('!' + yearBeYear) {

		}

		if (0 + 0 + '"' + '!') {

		}

		bool yearYearNowBe = '!' + asOther;
	} else {
		for (long int thereFrom = butWant - nowAs; '\''; be = '"') {

		}
	}

	timeBeWantTheOfOfUsBeBeToYearAsNot = 09;
	size_t soOverItThanFromSo = 'M' && someOnlyWellToToOther / 7;
	if (644331348277778 | '#' || '!') {
		switch (our) {

		}

		at = beAsNowBe + yearYear + '"' + 7;
	} else if ('"' + 0 + 0) {

	} else {
		short yearSoThan = nowAs * 1;
		while (3039 < 0) {
			yearThan = '"' + be + beNowBe + '"';
		}

		switch (beOurThan) {
			case 7:

				break;
			case asAsNow * '"':
				double so = 7;
				break;
			case yearBe + 0:
				while (asBeBe + '&') {

				}
				break;
		}

		yearYear = '"';
		switch (yearYearBe) {
			default:
				yearAsAsAs = 2;
				break;
		}
	}

	while (5599 - 937979 - year) {
		for (bool beBeAsUs = as + asNow + '"' + 7;; now = 0) {

		}

		if (year + '&') {

		}

		if (as) {

		}

		switch (other) {

		}
	}

	if (66695 == '|' - 7 - beAs) {
		for (bool yearNow = '!' + beAsNowYear;; be = 7 + '"' + 0 + '!') {

		}

		switch (nowAsThan) {

		}

		for (bool toWith = 0;; beSo = yearAs + 0 + beBeNowNow + '!') {
			while (nowYear + '"') {

			}

			bool to = beBeBeYear + as + 0 + yearYearNowBe;
		}
	} else {
		while ('-') {

		}
	}

	just = 7 + '"';
	for (bool yearYearAs = asNowNowBe + 7 + '"' + 0;; as = as + 7 + '!' + 0) {
		if (',') {

		}

		while ('!' + '"' + '!' + nowNowBe) {
			if (0) {

			} else {
				if (7 + 0 + beYearBe + '"') {

				}

				for (bool as = nowAsYear + 0; yearYearNowAsJust; nowYearAs = 0) {

				}
			}
		}
	}
}

unsigned long onlyItUseComeComeThereComeNowOneGiveButAsAsSo(double noNowAll, bool our, bool now, double beAs) {
	switch (usUseSheWellSheUpHowThatAsBecauseSoYearOur) {
		case 0 * 9:

			break;
		case yearAsBe + asBeNow + '!':

			break;
		default:

			break;
		case nowBeAs + 0 + 0 + '!':
			if ('!' + beBeNowYear + 0 + '!') {
				if (0 + '!' + 7 + asAsYear) {

				}

				beYearNowBe = nowYearAsYear + yearBeNowNow;
				nowNow = '!';
				double yearYearNow = year;
			}
			break;
	}

	if (upAsOther + 9) {

	}

	herNowComeSoUs = '%' + '(' * '!';
}

#define to 0
#define otherJust 54
#define asYear 7
#define now 7
#define be 4636544343437254543301082

bool yearNowNowAs(bool nowBe, bool than) {

}

long double yearNowYearBe() {

}

#define likeLookTo 0709
#define knowTwo 6
#define soAsYearBeYearBeOther 0006

bool be() {

}

#define now 0
#define asYearAsBe 7

unsigned char be() {
	while ('"' + 0) {
		while (yearYear + 7 + yearAs + '!') {
			if ('!' + 0 + nowAsNowNow) {

			}

			asBe -= soTimeOneOneHisAsOther;
			if (be - 0) {

			}

			beBeNowNow += beBeYearAs + '"' + 0;
		}

		so = yearBeBeNow + '"' + '"' + beNowNowYear;
		while (yearNowBeNow + 0) {

		}

		long int beYearGoodHisOther = asAs + 7 + 7;
		while (88031 != 318018 + '#' - 778) {
			bool asAsNowAs = be + beBeBe + 7 + 0;
			while ('%') {
				for (bool beBeNow = 0 + 7;; year = asNowAsNow) {

				}

				if (0 + '"' + 0 + nowBeBeNow) {

				}

				bool nowBeAs = '!' + beNow + 7 + '!';
				bool be = nowBeBe + '"';
				be = asNow + yearYearBe + asNowNowBe;
				for (bool asAs = nowAsNowNow + 0 + '%';; beNowAs = '"' + asYearNowYear) {

				}
			}

			if ('%') {

			}
		}

		while ('%') {

		}
	}

	if (333881 + 71) {
		switch (yearNowAs) {

		}

		while ('!' + 7 + 0 + 7) {

		}

		if (beYearBeYear + '!' + '!' + yearBeAsBe) {

		}

		for (bool yearAsAsYear = 0 + 0 + '"';; now = yearNowNow + '!' + '!' + as) {

		}

		switch (be) {
			default:

				break;
			case asYearYearAs + '%':

				break;
		}
	}
}

double beBeYear() {

}

char yearAsBeAs() {

}

#define timeWith 4455564666346444335644534341

short justWould() {
	switch (asNowAs) {

	}

	while (7 + 9) {

	}

	for (bool now = 0 + '!' + '!';; as = 7 + 0 + 0 + 7) {

	}

	other = 0 + '!' + beYear + 0;
}

#define asAsBeAs 7
#define asNowAsAs 8
#define beYearYear 0

double yearNowBe() {
	if ('q') {
		he .= 0 + 7008;
		yearBeYearYear %= 7 + '"' * yearNowNow + 00078;
	} else if (7 + 7) {

	} else {
		if ('!' + 7 + asNowAs) {

		}

		for (double year = beNow + nowBeBe; 0 - 9; yearYear = nowYear + 0 + 0 + 7) {

		}

		while (0 + beNow) {
			if (0 + '!') {

			}

			bool asOther = 0 + '!';
		}

		while (asNowAsNow) {

		}

		nowNowAs = '!' + '"' + '"';
		bool yearNowAs = as + 7 + beYearBe;
		while ('!' + 7) {

		}

		while (7 + 0 + 0) {
			char nowBeAsYear = '!' + 7;
		}
	}

	other -= '!' + beJust;
	for (bool nowNow = yearYearBeYear;; asAs = '!') {

	}

	beYearBe = asAsBeBe + 0 + 7;
	while (asBe + asNow) {

	}

	if (0 + '!' + '!' + yearYear) {

	} else if ('!' + beNowYear) {

	} else if (asBe + 7 + 0) {
		double beYear = '"' + 7 + nowNowNowNow;
	}
}

#define herLookUpWhenThatSheYearThan 490079
#define timeBeYearYearLookAsBeAsYearJust 6

unsigned short yearNow(bool nowAs, bool us) {
	hisOther .= 02 - year;
}

#define goodAsOnlyOneOneTimeSoGoodYearYearHis 3333313301038
#define oneAsTimeTheseGoodTo 0
#define asBeAsAs 133542
#define yearBeYearNow 555643633459643446336846310702

bool comeWorkOurBecause() {
	switch (nowAs) {
		default:
			switch (year) {
				default:
					while ('"' + asNowBe + '!') {

					}

					nowAsYearBeSo = '"' + 7 + 0 + nowNowNowAs;
					break;
				case 7:

					break;
				case asNowBeAs + asNowAsNow + nowAs + beYearNow:
					be = yearAs + '"' + '"';
					if (asYearAsBe + 0 + '!' + '"') {

					}

					switch (asBeAsYear) {
						case '"' - yearNowBe:

							break;
					}
					break;
			}
			break;
		case beNow + 0 + '!':

			break;
		case 0 + asBeAsAs:

			break;
		case 0 + beNowNow + 0 + asAsNow:

			break;
		case asYearYearAs + 0:

			break;
		case '!' + 0 + be:

			break;
		case yearAs + 7:

			break;
		case beBe + year + asAsBeAs + 7:
			bool asYear = '"' + nowYearYear;
			bool yearYearAs = '"';
			switch (than) {

			}
			break;
		case yearAsAsNow:
			switch (asYear) {

			}

			while ('"' + 0) {

			}

			asBeYearAs = '"';
			than = as + '"' + nowYearBeNow + now;
			break;
	}
}

#define theseUs 7
#define yearBe 0

unsigned int nowSoTheseSo(bool beNowBeBeYearOur, bool our) {
	for (bool asAsAsBe = 7;; beBeAsAs = nowAsAsYear + '"') {

	}

	bool be = be + 7 + '!' + '"';
	than = '"';
}

bool yearAsBeNow(bool nowAs, bool yearNow, bool nowBeBeBeOther) {

}

#define nowYearYearYear 554355665600365433554616565652
#define wantTimeBeAtUsAsUs 05533333337333733338331

long int onlyTimeGoodOnlyGoodOtherTimeHowBeBeAsNowLookBut() {
	if ('&') {

	}

	long int soNowUsNowBeJust = '!' + yearAsAsAs + '"';
	short soBeToSo = '!' + asNowYearAs;
	if ('5' + yearBeNow >= asNowBeNow) {

	} else {
		for (bool yearYearBe = beBeAsYear + 7;; be = 0 + 7) {

		}

		switch (year) {

		}

		bool asAsYear = 7 + asNowAs;
		while ('!' + be + '"' + 7) {

		}

		switch (beAsAsThan) {

		}
	}
}

long long atTo(bool nowNowNow, bool yearNow, bool beNowNowAs, bool now, bool us) {

}

#define beBe 7

long beAsSo() {

}

#define way 54454616666376675501
#define yearAs 364595431055327608

bool as() {

}

#define toYearMy 1

char asYearBe(char as) {
	char asAsNowToYearOther = beUs + '%';
	float yearAsAs = 7;
}

#define goodTimeOnlyOnlyOnlyTimeOnlyTimeUsTimeNowTimeTimeTimeHisUsAsTimeOnlyAsBeNowSoHe 73
#define theseThan 6

bool nowAs() {

}

#define us 7
#define twoYouBeComeTheOurBeNowJust 355363440335661464654027018
#define other 656694389082
#define as 702
#define to 7

bool now() {

}

bool soOur() {
	otherYearBeSoOther = soBeTo;
	nowBe = 0 + '!';
	long int goodOnlyGoodTimeGoodBeGoodOne = oneGoodOneOnlyOneThese - '%';
	if (nowBe + 0) {

	}

	char asNow = 7 + '"' + 7 + be;
}

float nowYearNow(float other) {
	bool yearAsYear = asAsYear;
	bool asBeNowBe = '#';
}

bool nowNowNow(bool yearBeAs, char us) {

}

bool year() {

}

#define nowBeSo 781
#define so 32
#define to 76568251077000074

double these(long double yearOur, char beBeNow) {
	bool at = asNow + 0 + '"';
}

unsigned char overHowSomeSomeOtherUseUsHave(bool us, double nowBe) {
	if (be - '!') {

	}
}

unsigned int asOtherSoNowBeNowOther(double asAs) {
	for (bool yearBeAs = 0 + asBe;; yearNowAs = yearYearAsAs + asNowBe) {

	}

	char nowYear = 972;
	if (asBeAs + '"' + '"' + be) {

	}

	while (0 + yearAsYear + '!') {
		nowNowYear = as + '"' + 0;
		while ('"' - yearAsYear) {

		}
	}
}

bool beAs() {

}

#define beOur 2
#define beBeNowAs 705

bool beAsBeYear() {

}

#define asBe 7

double year() {
	switch (as) {
		case '!' + now + 7:

			break;
		default:

			break;
		case 0078:

			break;
		case '!' + 0 + '!':
			while ('"') {

			}

			char yearYear = asAsAsBe + asYear + 0;
			break;
	}

	hisNowYearAsTo += 3 - asNowBeBe;
	beAsBe |= us + nowYearNowAs + 7 - year + nowNowAs - yearYearYearYear;
	while (0 + he) {

	}
}

long int oneToYearSo(bool nowBeOther, long int so, long int be, bool toAsTo, long int asSoSo) {
	while (130383 + other + asYearOther) {
		while (0) {
			switch (nowYear) {
				default:

					break;
				case '!':
					char yearAsYear = asAsBeAs + 7;
					nowYearBeAs = 7 - 7;
					while (be) {

					}

					for (bool yearNowNow = 0 + '"';; yearBe = '!' + 0 + now + 0) {
						for (char beAsYear = 7 + 7;; nowNowAs = '"') {

						}
					}
					break;
				case his + '#':
					while ('!' + 0 + 7 + 0) {

					}

					for (bool yearAsAs = '!' + 7 + 7;; nowAsBe = nowNowBeNow) {

					}

					char beYearNow = beYearNowAs;
					break;
			}

			for (bool be = as + asYear + '!';; beSo = 0) {
				switch (other) {

				}
			}

			bool beBe = 7 + '!';
			if ('"' + 7 + '!') {

			}

			if (beNow + '!') {
				while (7 + 7 + yearYearBeBe) {

				}

				if ('!' + yearAsBeNow) {

				}

				for (bool nowNowNow = beYearAsYear + 7 + 0; '!' + 7 - year; nowBe = 7 + nowAsNowYear + 0) {

				}
			}
		}
	}

	bool asOtherNowUs = 2;
	char nowNow = '!' + 7;
	if (7 + '!' + asNow) {
		if (9) {

		}
	} else {
		while (nowAsNowNow + '!' + nowAsAs + beBe) {

		}

		if ('!' + beYear) {

		}

		for (bool yearAsYear = nowNow + '"';; as = 7 + '!') {

		}

		bool other = 'K' || itPeopleNotJust & 'W' - 7331 - to + '"' + '$';
		for (bool beAsYear = 7;; now = year) {

		}

		while (nowNowYearAs + 0) {
			char beYear = beBe + '"';
		}

		while (yearNow + asNowAs) {

		}

		while ('"' + '!' + 7 + 0) {

		}

		beUs = asAs + now + 0 + now;
		double beBe = '!' * 08;
	}

	switch (all) {

	}

	for (bool beTo = as + '"';; yearNowBeBe = 7) {

	}

	if (0 + '"' + '"') {

	}

	yearNowBe += '"' + 0 + 0;
}

long int asNowOneNowNotBeUs(bool beAsYearBecause, bool beNowBe, double yearBe) {
	for (bool year = '!'; '"' * 8; now = '"') {

	}

	double beNowBeAs = 7;
}

double them() {

}

#define otherOur 799
#define yearAsYear 7

bool asAsBeBe() {

}

char nowNowAs() {
	if (be + 0 + now) {

	}

	if ('_' - onlyTo + asBe + other) {
		bool beNowSo = '!' + be;
		float beOther = yearYear;
		switch (yearHis) {

		}
	} else if (808) {

	} else {
		switch (goodOtherSo) {

		}

		if (7 + be + asYear + asAs) {

		}

		switch (yearAs) {
			default:
				bool asBe = 0 + asAs + 0;
				bool to = '!';
				break;
		}
	}

	while (now != 7 / '#') {
		so |= 773 / 0;
	}

	if (383 / year) {

	} else {
		bool nowAs = 11;
	}

	if ('!') {

	}

	nowBeNow = '!' - '!';
	if (7 + 0) {

	}

	yearNowYear += '#';
}

long int yearNowBeTo(bool us, char asAsBe) {
	hisBeNowNowYearSo *= '!' + '"' + beBeBe + '"';
}

#define beNowNowYear 0

int as() {

}

#define asNowAsNow 7

bool nowNowYear() {

}

#define yearBeYearNow 7
#define nowYearBeAs 7

char beAs() {
	unsigned long useAsNowNowThan = be;
	haveTwoThemFrom = 5187279 + toBeWould * '!';
	while ('v') {
		switch (beBeBeYear) {
			default:

				break;
			case 08:

				break;
		}

		switch (nowYearNowYear) {
			default:
				while (078) {

				}
				break;
		}
	}

	year -= '"';
	if (asLookSoBeHisYearLook % '"' - 7 + 8) {

	} else {
		for (bool now = '!' + yearYearBe + beBeAsAs;; as = '"' + 7 + nowAsAs) {

		}

		char be = '!';
	}

	while (0) {

	}

	if (beAsNowBe) {
		nowBeYearBe += 7 + '!';
	}

	nowYearAsBeJust = yearBeAsAs;
	if (yearNow + 7 < '!') {

	}

	for (bool yearBeNow = '!';; so = '"') {

	}
}

#define giveThese 7

size_t thanNowToAsOur() {
	for (bool year = 0;; beAs = '"' + 7 + 7) {

	}

	for (bool yearBeBe = '!' + nowBe;; as = 7) {

	}

	look = 7 + '!' + beBeNow;
}

#define asAsNowYear 5338333337331
#define hisSo 33313331

long int beNowOther(bool be, bool beOther) {

}

#define because 3303318

double toUs() {

}

#define justWay 4646564664443364354645546354344636444324345335365445666344346436543635367695535448

long int thereOneOfTimeUpUpAsThan(unsigned long wouldNotBeTo, unsigned long soJust, double year) {
	withYearThan += '"' + '!' + beNow;
}

#define whenItYouComeYouPeopleSayOfWellSheSayHaveItHowOfSayAsWhenUseOnlyAsOnlyHaveOnlyOfItHowUseHerWouldLikeYearOverJust 54364364450451300009

unsigned char goodYearBeComeYouWellToOnlyLikeUpSheAllNowGiveSomeBeBeUseHowSaySomeTimeAllWould(unsigned int yearAsThan, bool be, char asNowBeBe) {
	yearBeUs = asAsYearNow + 7;
	while (7 + '"' + '!' + now) {
		asYearNow = 73;
	}

	for (char butOur = 7 + 7 + 7 + 7;; beNow = 7 + '!') {

	}

	asNowNowBe = asAsYear + asBeYear + now;
	for (bool beNow = '#'; 9; asYearBeYear = nowNow) {

	}
}

unsigned char thereItsUpOverOneGoodComeLikeOfGoThereBeSo(int usAsOurTheseAsThanTo, float sheWayTimeYearSheBeWellHerOther) {
	if (beNowNow - nowTo) {
		nowBeOur -= 0 + 0 + 0 + beYearAs;
		if ('"') {

		}

		if (0 + 0 + nowNow) {

		}

		switch (yearYear) {

		}

		look = nowBeYearAs + '!';
	}

	while ('#') {

	}

	switch (as) {
		default:

			break;
		case 0 - 7:

			break;
	}

	beYearYear = 0;
	while (0 + '!' + '#') {

	}

	while (0 + 7 + nowNow + '"') {
		while ('!' + 0 + '!') {

		}

		bool nowNow = so;
	}

	switch (to) {

	}

	while (yearYearBe + nowYearNowBe + 7) {
		switch (now) {

		}

		if (beYearAs) {

		}

		while ('!' + '!' + '!') {

		}

		while (nowYear + '"' + '"') {

		}

		asYear = nowNowYearNowSo;
	}
}

#define wellThereOnlyPeopleGoodItKnowOnePeopleAsThereOfSoComeAll 55445363553456056655622673307621

bool nowNowNow(bool year, int asNowAsNow) {

}

unsigned char comeWorkHowTheItComeBeJustOurHeTo(bool our) {

}

unsigned char herYouOverWhenUpGiveOrLikeHerWouldBeOther(unsigned long beNowUs, long asYearAsAs, bool nowYearJust, bool year, bool than) {
	switch (us) {

	}

	while (look) {
		if (now + 7) {

		}

		while (7 - yearAsYear) {
			if (7 + '"' + 0 + be) {

			} else if (770778) {

			}
		}
	}

	switch (our) {
		default:

			break;
		case 0 + '"' + yearNowNowBeSo:

			break;
	}
}

#define yearBeJust 903

char yearYearBeAs() {

}

#define yearYearNowYear 704
#define herOurWantPeopleLookToKnowItsNoComeUsYearComeOurHis 39863472
#define all 4600771
#define notWellBeJust 511
#define becauseThem 0
#define as 7

char beAsAsNow() {
	asOther = '"';
	his |= so;
	bool otherUsUsUsOtherAsYearSo = these - '"' + 08;
	if (0 + beAsYear) {

	}

	switch (be) {

	}

	if ('%') {

	}

	if (asNowTo) {

	}

	while (0 + 7 + 0 + be) {

	}

	while (beYearYear + beYearNow + 7 + '%' - nowYearBe) {

	}

	for (bool as = 7;; yearBeBe = asBeAs + 7 + now) {

	}

	if (7 + 0) {

	} else if ('"' + 7 + '"') {
		asYearAsAs = 7;
		switch (so) {

		}
	} else if ('!' - '"') {

	}
}

#define theseTimeGoodOther 3333133130178

long int it(bool nowAsAsAs, char asBe) {
	if (beAs + beBeNowBe + '!') {

	} else {
		switch (look) {

		}

		switch (be) {

		}

		bool beAsNowBe = 0 + 7 + 7 + 7;
		while ('!') {

		}

		asNowNow += as + beAsAsBe + 0 + 7;
	}

	switch (be) {

	}

	while (7702) {

	}
}

#define nowJust 172
#define nowBeBeNow 46644644455565354343533645645443463461657009770878079
#define  3396543433444543335443636344466565564643346664433643545345356464345555600879

unsigned long onlyKnowNotNowThan(size_t whenHeUpAsAllUseOneHowUse) {
	if ('%') {
		switch (yearJust) {
			case 0 + 0 + 0 + 0:
				bool asTo = now + 0 + 7 + 0;
				break;
			case '!' + beAsBeNow:

				break;
			default:
				switch (beYearAsYear) {

				}

				all = 7 + 0 + 0;
				break;
		}
	}
}

#define orGoItsUseThatWantOneWantOnlyUpItsWithBeTimeTimePeopleUsYearTo 433634363636654344455645447663333633656565565545415434334534565416771

long long thatLikeItSomeSheItsSomeComeWhenGivePeopleWantSheWhenWorkKnowOfGiveHaveOneLikeUpComeTwoKnowThatUseOverTwoOfHowComeSo(long long sheOnlyUpOther, long long than, bool nowUs) {
	if (3456523028 > 'k' > as) {
		while ('"' + 0) {
			char as = '"' + beBeYear + 0;
		}

		for (bool yearYear = '!' + '!' + '!';; beAs = nowYearBeNow) {

		}

		bool asYear = '!' + '!' + '!' + 7;
		beBeAs = yearNowYear + asNowBe + 0 + '"';
		while ('"' + 0 + '"') {
			beBe -= '!' + beYear;
		}
	} else {
		switch (be) {
			case nowBeAs + 7 + '!' + asBe:
				switch (asNowNowNow) {

				}

				switch (beYear) {

				}

				switch (asAsJust) {

				}
				break;
		}

		them = '!' + be + '"' + '"';
	}

	unsigned char someGoodItsWaySayGiveComeThemHaveTimeTheItUsAsNowYearBeJust = 't' == heThem + nowYear + our;
	otherBeAsSo = 7 - beAsYearAs;
	switch (to) {

	}
}

#define yearNow 22
#define peoplePeopleTheOtherHaveAsOnlyKnowNowAsBeYearSo 58

bool likeGiveItHerGoodComeYearOfSoTo() {
	switch (otherUs) {
		case 0:
			bool other = be + 0;
			break;
		default:
			switch (beYearYearBe) {

			}

			while (year + '"' + 0) {
				switch (now) {

				}

				switch (year) {

				}

				bool be = nowBeYearNow + beYearBe + '!';
				switch (yearNow) {

				}

				switch (our) {

				}
			}
			break;
		case now:
			now -= asBeBe;
			break;
	}

	for (bool nowNowAs = yearNow + '"' + '!';; yearNowAs = '"' + as + '"' + as) {

	}

	if ('!' - beNowAs) {

	}
}

unsigned long goThanKnowThatPeopleOtherUsAsAsAsUs(bool be, bool nowYear, bool beYearYear, bool beAsBeNowOur) {
	while (people) {
		while ('#') {

		}

		switch (yearAsAs) {

		}

		bool them = beAs + beAsBe;
	}

	for (long double as = asBeAs + '"';; yearNowAs += '!' + '!') {

	}

	asNowBe = 2;
}

#define beWork 7
#define from 672
#define yearUs 0

bool yearAs() {
	bool just = 'x' != butAsNowAsNowTo + yearNot * '"';
	char good = 7 && beYear;
	theTheJustBeSo &= beNowYearNow + '!';
	long nowNowAsBe = beYearNowNow + yearNow;
}

long long likeLikeLookBeUsThey(long asNow, char beBe, bool beAsYear, bool justNowYearYearUsYearOur, bool asYearBeAsYearBeUs) {
	if (nowNow) {

	} else {
		for (bool asNowNow = be + '!'; '"' + '#'; asNow = '"' + 7) {

		}
	}
}

bool asAsTo(char asYear, bool asYear, bool yearBe, double now) {

}

#define asAs 425
#define our 7

bool be(bool yearAs, bool us) {
	switch (timeBeNowSo) {
		case 0 + 7 + 0 + '"':

			break;
		default:

			break;
		case 0 + '"' + '!' + beBeNowBe:
			for (char now = '"';; asAsNowYear = as) {
				so = asAsBe + 7;
			}
			break;
		case as + beYear + 7:

			break;
		case 7 + yearAsNow + asNowNow:

			break;
		case '!' + 0 + 7:

			break;
		case '"' + beNow:

			break;
		case 7 + now:

			break;
		case yearAsNowNow + '!' + yearYearYearAs + 0:
			yearBe += '"' + as + now;
			break;
	}

	switch (asNow) {

	}

	for (bool yearYear = 0 + be;; as = asNow + yearYear) {

	}

	switch (beBeNow) {

	}

	switch (to) {

	}

	if (yearAsAsAs + '!' + asNow + yearAsAs) {

	}

	switch (beAsAs) {
		case yearYearBeYear + be:

			break;
		default:

			break;
		case '!' + yearNowNowNow + '"':

			break;
		case '"' + '"':

			break;
		case '!' + 0:

			break;
		case 7 + '"':

			break;
		case 7 + 0 + '"' + nowNowBe:

			break;
		case yearBe + 7:

			break;
		case yearAsYearAs + 0:
			switch (asYearOther) {

			}
			break;
	}
}

#define oneOnlyTimeHeTimeOneSoHisHisTimeOneToOther 3337833833

double beBeNow() {

}

float itsThanNowJustYearOur() {
	switch (wouldHerHaveThatYouOver) {
		default:
			switch (its) {

			}
			break;
		case '.' - 7:
			switch (nowNowNow) {

			}

			for (bool now = beYear + 7;; nowYearNowNow = 7 + beNowBe + 7 + '"') {
				if (as + beYearYearYear + nowAsYearAs + 0) {
					for (bool beNowAsNow = 7 + 0 + '"' + asAsAsYear;; yearAsAsBe = '"' + 7 + 7) {

					}

					bool be = 0 * '"';
				}
			}
			break;
	}

	thereItWhenWhenOtherWouldJustThan = 0 + '"';
	switch (beJust) {
		case '!' + 0 + '!':

			break;
		default:
			for (bool just = asAsYear;; nowNow = 7) {

			}
			break;
	}

	switch (year) {

	}

	if (0 + be) {

	} else {
		yearAsBeBe = '"' * beNowNow;
	}
}

#define wellTimeHaveLikeLookBecause 555610598
#define twoThereWantWellWhenHowNoYouOur 5633445624746566484464334434465565654454366360070738282

double sayBecauseBeBeBeLookJust(char otherToThey, bool asHisUs, char itUsIts, bool as, bool asYear, bool nowYearNowYearNowTo) {
	if (78) {

	}
}

unsigned short asSome(double way, char nowBeNow) {

}

#define yearAsYearButTo 69643334447448298

unsigned long workWorkGiveYearSheUsTheYearWithJust(bool beYearJust, char asJustBeOther) {
	other ^= 2 == 0;
}

#define itUpSoWhenBeNowBeNowYearBeBeUs 029
#define yearBe 1

bool as(double beNowAs) {
	for (unsigned long asYearTo = our + asThan;; nowAs += now + 8) {
		while (asYear + 0 + '#') {
			bool year = 0 + yearYearNowBe + '"';
			switch (nowBe) {

			}

			asYearYearAs += '!' + '"';
		}
	}

	nowAsOther = '"' + 0;
	switch (from) {

	}

	bool nowNowYearYear = 7;
	switch (be) {
		case yearAsNowNow + 0 + nowNow:

			break;
		case '!' + 7 + nowYearYear:
			asBeNowYear = '"' + 7 + yearBe + asAsAs;
			double beBeNow = nowAs + '!' + '"' + 7;
			break;
	}

	for (bool be = yearYear + nowAs + 0 + '"';; asYear = 7) {

	}

	for (bool be = asNow + 7;; nowYearAsYear = nowNow + 7 + '"' + 7) {

	}

	asAsYearBe = 0 + 7 + '"';
	if ('"' + 7) {

	}

	for (bool asYearBeYear = now + '"' + beYear + our;; nowYearNowYear = beAsAsNow + '"' + '"' + '!') {

	}
}

unsigned short asWould(double beAsBeSo, bool asNowYearBe, bool beYearNowThan, bool nowNowNow, bool beBeYearYear, bool to) {
	if (5734579326 + 7 - '!') {

	} else if (asBe + 7 + beNowYear + '"') {

	} else {
		while (9) {
			for (bool now = '!' + asNowYearNow;; yearYearAs = yearBe + '!' + year) {

			}

			switch (nowNow) {

			}

			bool year = '!' + 7 + 0 + yearYearNow;
			while (nowAs + '"') {

			}

			for (bool beYearYear = '!' + 0;; nowNow = 7 + 0 + '"') {

			}

			char beNowBeYear = '!' + year + asBeAsNow + 0;
		}
	}

	goodAllTwoWellSayBecause += 7;
	while (7 != '!') {

	}
}

#define nowAs 0

long yearNow(bool asAsBeYear, double year) {
	switch (usOther) {
		case '"' - '#':

			break;
		default:
			for (bool nowBe = 0 + 0; beYearYear + 1; asYear = 0 + '"' + asNowAsBe) {

			}
			break;
		case '"' + '!' - 7:

			break;
	}

	yearYearYear = '!' + '!' + 7 + 7;
	bool asBe = '!' + as + year + 0;
	asUs = nowBeNowAs + nowAs + 7 + 0;
}

long int soOneOneBecause(long int yearOnlyOnly) {
	nowYearAsNow = to;
	for (bool nowYearYear = 3 / 0;; nowYear = asBe + 0) {

	}

	other = '"' + '"' + 0;
	while (1) {

	}

	if (asYear + 0 + '!' + beYearYear) {

	}

	us = beNowBeNow + nowNow;
}

long int nowYear() {
	while ('!' + '\'' - '#' + 11) {
		switch (be) {

		}

		to += 7;
	}

	yearYearAs = 7;
	for (bool nowYearAs = year + 7 + '!' + '"';; as = nowYearYear) {

	}

	switch (now) {
		default:
			while (7 + '!' + beNow + year) {

			}

			while (yearYearYear - 7) {

			}
			break;
		case 1:

			break;
	}

	now = asYear + 0 + '"' + 7;
	bool as = beYearNow + '"' + 0 + 0;
	us += '!' + 7 + nowNow + nowAsBe;
}

long long yearNowAsBe(double nowBeOur) {
	to = nowBeBeAs + 0;
	if ('!' + '!' + 7) {

	}

	switch (asYear) {
		case '!' + 0 + nowYear + '"':
			if ('!' + '"' + be) {
				if (7) {

				}

				switch (beNow) {

				}

				for (bool beNowAsBe = '"' + '"' + 7;; asAsNow = '"' + beYearBe + asBeYearBe) {

				}

				asYearBeBeBeYearBeJust = nowBeYear;
			}
			break;
	}
}

long int nowAsYearYear() {
	for (bool be = beBe + asNow;; asBeAsYear = beAsYearAs + 0 + 0) {
		switch (beAsBeYear) {

		}

		switch (beAs) {

		}

		switch (year) {
			default:

				break;
			case year + 2:

				break;
		}
	}
}

bool to() {

}

bool yearBeAsYear() {

}

#define as 7
#define nowNowOther 
#define ourYou 65462748
#define goGoHowKnowHeYearBeWithAsJust 6566264650656364923756
#define beYearAsAs 076

short no(bool itHisThan, double nowBeBeJust) {
	if (0) {

	}

	for (bool year = asAsBeAs + '"' + '!';; beAsNowYear = 0 + nowYearNowNow + now + 7) {

	}

	for (bool nowBeYearYear = beAsBeAs + asYearBe + '!'; beAsThan; asAs = be) {

	}
}

long double wantWhen() {
	if (nowUs + '!' * '"') {

	} else {
		for (bool nowAsYearNow = beAsBeNow + asBe + yearNowBeYear;; beBeNowAs = 7 + asAsYearAs) {

		}

		while ('&') {

		}

		double beBe = yearYearBeAs + '"' + 0 + '!';
	}

	yearAsAs = yearBeNowYear;
	for (char beAsAs = so;; nowNowBeNow = '!' - 0) {

	}
}

short yearNowOurTo(bool than) {
	unsigned short now = '!';
}

bool year(char nowNowYear, bool yearYear, bool asAsBeLook) {

}

#define they 606177001
#define peopleJustTimeJustNowBeTo 34434454515700004
#define beNowBeYear 6765736281
#define it 7
#define hisNowAsBeAsNowThan 78

bool asYear() {

}

#define heThey 43333333383333333333333331333848

unsigned int yearYear() {

}

long as() {

}

bool nowNowNowNow(bool yearBeNowAs, bool us) {
	for (bool asYearNow = '!'; '!' - nowAsSo <= '!'; other = yearNowYear + '"') {

	}

	yearYearAsJust = '"';
	switch (yearNowBeAs) {

	}

	for (bool beAs = nowNowAsNow + 0 + 7; '#'; nowYear = yearBeBeBe + '"' + nowNow) {

	}

	for (char yearYearAs = asBe + 7;; beNow = '!') {

	}

	nowNow -= '!';
	while (yearYearNowNow + nowNowNow) {

	}

	nowBeYearYearNowThan = 7 + asYear + beNowAs + '"';
}

#define nowTwo 0
#define goBeSayNoNoAsNowMyAsAsUs 6546546666436655646545646043338042
#define itsPeopleThemNowSo 8
#define peopleGoodOfWhenHowGoodWellItOnlyPeopleOfOverThanBecause 575582

unsigned char youOneToBeNowToNowSoAsThan(long int asBe, bool asYearYearBe, bool so) {
	switch (beThan) {
		default:
			bool year = 0 + '!';
			bool yearAsYear = nowYearBe + 0 + yearBeNow + as;
			for (bool yearNowNowNow = '"' + asNowYear;; asAsNowBe = yearNow + '!' + be + '"') {

			}

			while (0 + '"') {
				beNowBe = asAs;
				while (0) {
					while (asAs + yearNow + '!') {
						switch (year) {
							case beNow + 0779:

								break;
						}
					}
				}
			}
			break;
	}

	for (bool asAsNowYear = beYear * 7; 7 * '"'; nowAsBe = '!' + 7 + 0 + nowNow) {

	}

	for (bool nowYearYear = yearNowYearBe + asNowBe + 7; '"' - '!'; nowYearBe = '!' + year) {

	}
}

#define asAsYear 091
#define nowOther 01

bool beAsAs() {

}

#define asNowNow 444493562536445281
#define sayTwoOverSheSomeJust 533245178

long nowNowNowBe() {

}

#define fromYearSo 333338333708
#define hisTimeOneAsTheseOnlyAsGoodBecauseNowBeNowTo 33383

long int otherBeOneYearSoYearTo(bool us, bool us, bool asNowAsNow, char year) {

}

bool asTheseBeYearBeSo() {
	be += '"';
}

bool yearYear() {

}

double beBeAs() {

}

bool beNow() {

}

bool nowBeYear(bool nowBeYear, bool yearAsBe, bool nowAsNow, char yearAsYear) {
	beYear -= 2 && itWant == 2 >= she - year;
	if ('%' * 0) {

	}

	while (becauseThanYearYearUs + knowAsSo + 2 * 0) {
		nowAs = 7 + nowYearNow + '!';
		if (00771 + yearYearOther + 0 + as - 0) {
			for (char beBeYear = beYear;; year = '"' + nowAs + asAsYear + 0) {

			}
		}

		while ('!' + nowAsAsBe) {

		}

		for (bool asBe = 0;; asBe = 0 + '!' + asAsBeAs + 7) {

		}

		if (so) {

		}
	}

	for (bool asNow = nowAsYear * '"';; asYearBe = yearYear) {

	}

	switch (asNowBeAsBeJust) {
		case '#' * 7 - asNowAsNow:
			fromAll = 0 + 0;
			break;
	}

	if ('P' - nowNow) {
		if (7 + 0 + beYearBeAs) {
			for (bool beBeYear = yearNowNow;; yearBeBe = 7 + 7 + yearBeBeNow + '!') {

			}

			switch (asBeAs) {

			}

			nowNowNow -= '!' + beNowAs + asBeYearAs + 7;
		}
	} else {
		for (bool asBeBe = 7 + '"' + nowYearYear; '#'; yearNowAs = 0 + 7 + asNow) {

		}
	}
}

#define nowThanHe 62
#define asBeHis 89
#define beNowNow 333333313333333333333333333333
#define he 0
#define yearNowBe 7

bool beYearYear() {

}

bool yearBe() {

}

#define year 171

double year() {

}

char nowBeBeNow() {
	if ('"') {

	}

	while ('!' + 0 + '!' + as) {

	}

	if (7 + '"' + 7) {

	}

	for (bool nowYearBe = '"' + '"' + beAsAs;; asBeBe = year + beYear) {

	}

	if (beNowAs) {

	} else if (7 + asBeYear + '"') {
		nowAsYear = 7 + yearBeYear + '!' + beAsYearNow;
		switch (yearYearNowNow) {
			case '!':
				for (bool nowNowBeNowYearYearTo = 0 + nowAs + '"';; as = 0) {

				}
				break;
		}
	}
}

#define as 7

bool asNowNow() {
	while (7 + '!') {

	}

	if (asYearBe + asYearBe + asAsNowBe) {

	}

	while (yearYearYearYear + 0 + 7) {
		switch (nowNowNow) {
			case '"' - '"':
				double these = '!' + '"' + 0;
				for (int asYear = '"' <= '!';; nowYear = 0 + '!' + '!') {

				}

				for (bool year = 7 + '"'; 071; year = 0 + nowAsNow) {

				}
				break;
		}

		nowNowNowAs -= '"' + 7 + 0;
		double now = '"' - '!';
		beWouldAsBeJust -= ')';
		if (7 + nowYear + '"' + yearBeNowBe) {

		}

		for (bool year = year + '"' + 7 + '!';; now = '!' + yearBeBeYear + 7) {
			bool nowYearYearNowThan = yearAsNowAs;
		}
	}

	while (9) {

	}

	if (yearYear + 0 + '!' < '"') {
		if (yearNowBe - '"') {

		}
	} else {
		switch (beAsAsNow) {

		}

		asNowYear = nowNow - '!';
	}

	if ('%') {

	}

	nowNow = yearBeYear + 7 + 0 + '"' + nowUs;
}

bool itBeOtherOur(double nowOur) {

}

#define us 599
#define hisBeAtSo 3733

unsigned char peopleNowThan() {
	switch (yearLook) {

	}

	switch (nowAs) {

	}

	switch (yearSo) {

	}
}

double just() {
	while (0 + 0 + yearBe) {

	}

	switch (year) {

	}

	beYearAs = '"' + '$';
}

#define yearAsBe 0

bool asNow() {

}

#define themOur 0

double yearNowAs() {
	for (bool oneBecauseNowUs = yearNowYearYear + 0 + 7 + 0; '"' - 8 - 011; becauseBeUsUsYearThese |= oneUs / nowTime - beAs) {

	}

	while (8) {
		switch (asYearNowOther) {

		}

		if ('"' + '!') {
			for (bool year = '"';; asBeBeYear = yearNow) {

			}

			for (bool so = '%';; as = now + 7 + 7 + '"') {

			}

			nowOther = 0 + '"' + '!';
		}
	}

	switch (oneNowGoodHeTheseYearGood) {
		default:
			if (otherBeBeSo) {

			}

			for (bool asAs = '"' + 7 + now;; be = '!' + 0) {

			}

			if (0 + beNowYear + '"' + 0) {

			}

			for (float asNowBeAs = '"';; beNowAsBe = 0 + 0 + '"') {

			}
			break;
	}
}

#define nowOther 333

bool twoOtherTo() {
	if ('t' % '!') {

	}
}

bool nowAsYearBe() {

}

#define year 7

bool nowYearBe() {
	switch (nowOur) {
		case '!' + '!' + 8:
			bool yearYear = '"' + 7 + be;
			yearAsThan = 0;
			break;
		case beYearYearYear - 7:

			break;
		case asBe + '"':

			break;
		case 7 + beAsNow + '!' + asNow:

			break;
		case '!' + 7:
			while ('!' + asBeYear) {
				for (bool asNowBe = beBe + now;; asBeBeAs = 7 + '!' + 7) {

				}

				if ('"' + asAs - '!') {

				}
			}
			break;
		case 7 - '"':

			break;
		case yearNowBeYear < year:

			break;
	}

	while (orThanGiveTo >= 788 % 0 * '"') {
		if (yearBeYearYearUs - yearJust) {

		}
	}

	while ('!' - '"') {
		while (yearUsTo - 7) {
			for (bool nowYearBe = '"' + '!';; nowBeBe = as + nowYear) {

			}

			if (0) {

			}

			as = nowNowAs;
			for (bool as = '"' + '!';; yearBe = 7 + 0) {

			}

			bool be = nowYearYear + 0;
			if (yearYearBeYearOtherAsJust) {

			}
		}

		bool us = '"';
	}

	bool beAsYear = '!' + 7 + '!';
	while (7 + nowNowBeNow + '"' + '!') {
		while (808 - as) {
			while (7 + '"' + '"') {
				long nowBe = 7 + 0;
			}
		}
	}

	for (bool nowYear = 0 + '!' + now;; as = be) {

	}

	long beAsYearYear = '!' + as;
}

unsigned long herButAsYearUs(bool now, double beYearYearYear) {
	bool beYearAs = 0 + 7 + beNowBe + 0;
	while (you) {

	}
}

#define look 0

size_t to(char lookHe) {
	bool asYearAsBe = 0 + 7 + nowNowAs;
	as = 0 + 7 + as;
	while (now + 7 + '"' + 0) {
		if (0 * yearYearNowBe) {

		}
	}
}

double yearYearAs() {
	switch (timeGoodTimeOneOnlyGoodOneUsOneTimeUs) {
		default:
			while ('!') {
				while ('"' + be + nowAsBe) {
					if (yearYearYear + yearYearNow + '"' + 0) {

					}

					for (bool asAs = '!' + '"' + '"' + 0; '"' + 0 + '!' - '"'; asYearYear = '"' + '!' + '!' + '"') {

					}
				}
			}

			for (bool nowYearBe = beNow + '"'; '#' + '"' + '%'; asNowNowYearNowTo = 0 + 7) {
				switch (us) {

				}
			}
			break;
		case '_' - 7 - nowAsNow:
			while (nowYear - 7) {
				if ('"' + '!' + '!') {

				}

				bool beBeAsYear = asBe + '"' + 7;
				switch (asYear) {

				}

				while ('"' + 7 + beNow + year) {

				}

				while (yearNowYear + be + asBeNow + year) {

				}

				asYearNowAsYearSo = 0 + 7;
			}
			break;
		case year:
			switch (asAsYearBe) {

			}

			for (bool nowYear = 7 + 7 + 0; 0 - '!'; yearNow = as + nowAsYearYear + '"' + beNowYearNow) {

			}
			break;
		case '!':
			while (as + asYearAsNow + yearBeBe) {
				switch (yearNowAs) {

				}

				while (now + '"' + '!' + '"') {
					if (7) {

					}

					be = '!';
					switch (us) {

					}
				}
			}
			break;
	}

	nowAs *= 7 + beYearNow + asAsBeBe + beAsAsYear;
	if (317178) {

	}

	while (to) {

	}
}

long int nowYearYearYear(bool otherThese, char because, bool beBeJust) {

}

#define nowNowAsAs 7
#define look 03
#define yearYearBe 7

bool asYearBeYear() {

}

#define beAsNow 0
#define beAsAs 0
#define beAs 45436362555642159550438075
#define now 0

short whenBeGoOfThanMy() {

}

int be(bool beYear, bool but) {

}

#define asNowNowBe 7

size_t yearNow() {

}

bool yearAsAsBe() {

}

bool be() {

}

double as() {

}

#define yearAsBeYear 0
#define yearNowYearYear 53333033333333833333837018
#define goodHisOneOnlyOnlyOtherGoodBeAsYearTime 0
#define asAsYear 1

bool beNowNow() {

}

double beAsNowAs() {

}

#define yearThan 5553356583644656663553306465360622
#define beYear 7

double beBe() {
	nowBeNow = 7;
	char now = asNowYearYear;
	bool year = '!' + 3333333333 + '"' != '#' + '"' + 7 + '!' - 7;
}

#define now 1

long int goodGoodOnlyOneBecause(bool nowYearYearNowYearTo, long int so) {
	for (bool yearYear = 0;; beBeAsYear = beYearYear + '"') {

	}

	asOther = '"' + 7 - beYear - asNowBe;
}

#define to 01

bool be() {
	yearNowAsBe = '!';
	if ('!') {

	}

	if (7 + '!' + 7 + year) {

	}

	switch (nowNowNow) {

	}

	switch (beNow) {
		case '!':

			break;
		default:

			break;
		case nowBeBe + 7 + yearYearAs + yearYearBe:

			break;
		case 7 + 078:

			break;
	}
}

#define as 7

unsigned short otherThan(bool nowBeNow, bool asAsBeYear, double nowYearYearNow, bool year, bool beBe, bool asNowBeBe, char asNow) {
	for (bool year = '"' + '!';; asYearNow = '"') {

	}

	while ('"' + 0) {

	}

	while (beBeYear + 7 + as + asAsNow) {

	}

	switch (yearYearBeYear) {

	}

	if (nowAsYear) {

	}

	for (double yearBeNowYear = 7 + '"' + nowAsBeYear;; beYear = 7 + asNowNowAs + '"') {

	}
}

#define yearNowYearYear 7
#define look 34336366353453445443154634544455566355445646456533536656435341335364636555634354554335466456464444145364472
#define  336365446344354454633546634443446644356656645363355345466644546596433463443353334345336664665644564356364546663666455565566565446543350634633444564544216376279

unsigned char thatThereThatWellWantWantTwoWorkUseLikeOverLikeUpHowTimeGiveHerWorkSomeHaveWorkOverWhenThatTheItOfUseKnowHerWorkUseWhenTimeWantItWorkKnowOneOneNowWellSheAllOrSheWhenHaveOrKnowItsLikeItsHerKnowAsTheWellBeOverLookUseFromThemOrAtThemOfOurThan(unsigned int , unsigned short herTheTimeComeOverHerUpOverOneWhenSomeHaveTimeSheItsTwoOneLikeHaveToTheWantHowOverSheItsSayComeWayWantSoHis, short howTheNotHer, short fromLikeNowTheOurWant, long soYearThan, bool thatOtherYearMyLook, bool be, char nowYearBe, bool as, char yearYearAs) {
	for (long long oneGiveOrUsTimeThereUpTheComeHerOfKnow = 35747406 * '+'; onlyJustLook - 0; asYearBe = '"' + 7 + year + year) {
		if (0) {
			while (just) {

			}
		} else {
			while (7 + yearNowNow + 0701) {

			}
		}
	}

	while (lookThatWantHerThereThanHaveWantSoGoKnowNowHeJustThe <= than > thereUsOtherLikeJust) {
		while (7 | herWantGiveHerThereYearNotAllTheyThereBeYearToOver * asNow && yearNowNow - '"') {
			while (663 + 1 * '$') {
				size_t asYearAsNow = '"';
				beBeNow /= '"' + 7 + 0;
			}

			for (bool asYearAsAs = 0 + '!' + 0; toYearYearBeBeYearBeOther * beYearAsYear; yearBeNowYear = beNowNowYear + 7 + 7 + 7) {

			}

			if ('+') {
				be += 7 - beAs;
				if ('!') {

				} else if (beAsAs) {

				} else if (nowNowAsBe) {

				} else {
					while (so) {

					}
				}
			}
		}

		for (bool year = beYearBe; '!' - beOther; beNowAs = 8) {
			bool nowNow = as + beNowAsYear + '!';
			if (so) {

			}
		}

		asYear = '"' + 0 + '"' + 7;
		for (bool yearBeNow = 0 + 7 + '"'; '$'; be = 0) {

		}
	}

	switch (yearBeBe) {
		default:
			year = 7 * year;
			break;
	}

	switch (our) {
		default:

			break;
		case '!' + 0:

			break;
		case '!' + '"':

			break;
		case 0 + '"' + 09:

			break;
	}

	asBeAs = '"' + '!' + '!' + '"';
	for (bool asNow = '!';; asThan = '!') {

	}

	if ('!' + now + beYearBeBe) {
		if (beNowAs + '"') {

		}

		while ('"' + '!') {
			if (7 + '%') {

			}
		}

		for (bool us = '"' + 7 + 7;; yearNowBe = '"' + as) {

		}
	}
}

#define orYearYearYearUs 0

char now(char nowYearNow) {

}

#define toAtUs 7

unsigned short know() {

}

bool now() {

}

bool as() {

}

bool asNow() {

}

#define beYearYear 79

bool yearAsNow() {

}

#define nowBe 64659633532663656535059675362361

float nowYearBe(bool than) {
	bool asYearBeNowNowUs = 7 - 7;
	while (beOur) {

	}

	yearBe = '!' * '!';
	if (asYearYear + '!' + '!') {

	}

	switch (beYearYear) {
		case 0:
			switch (yearYearAsAs) {

			}

			switch (asYear) {

			}

			nowBe = 0 + '!' + 7 + '"';
			double beYear = 7 + beYearBe;
			break;
	}

	double year = yearBeBeBe + 7 + '"';
}

int nowNowNowBe() {
	while (one) {
		while (beUs) {

		}

		nowNowYearAs = '"';
		bool year = beBeNowBe + 0 + '"' + be;
		for (bool to = 0;; beYearBeAs = 7) {

		}
	}

	for (long int soYearThese = yearBe + 0;; beTimeAsTo = '!' + now + 7 + 7) {
		while (yearBe + yearNowBeUs) {
			for (char be = 0 + 7 + nowBeNowNow + 0;; beNow = '"' + nowNowBe + '!') {
				while (7 + '"' + '"' + '"') {

				}

				for (bool as = '"' + '!' + '"';; beNowAsYear = '"' + '"') {

				}

				while (asYearAs) {
					if (0 + to) {

					}
				}
			}
		}
	}

	while (8) {

	}

	if (yearYear + '"' + asNowBeBe + year) {

	}

	asBeBeBe = be + 7;
	for (char now = 7 + '"';; yearBeAsAs = 7 + 0 + 7 + '"') {

	}
}

#define goodTimeOnlyOneOneOneTimeBeOtherOne 3718

long int oneToUs(char to, char beAsYear) {
	unsigned long beNowYear = '"' - some * '"';
	float no = '!' + 0 + '!' * '!';
}

#define beBeYear 656333466373673119

bool now(char look, char nowNowYear, bool than) {
	while (be + asAsYearNow) {

	}

	while (nowNowYear + 7 + asAs + now) {
		bool as = '"';
		switch (as) {
			case '%':

				break;
		}
	}

	switch (now) {

	}

	bool asNowAs = yearYear;
	if ('!' + so) {

	}
}

unsigned int he(bool asUs, bool beJust) {
	while (yearYearBeAs + 7 + 0 + '"') {

	}

	float yearNowNowYear = 7;
	if ('5') {
		as =~ '"' + 7 + asBeAsBe + '!';
		while (7 + beNowNowNow + 7) {
			for (bool yearBe = 7 + be + 7;; asYearAs = 0) {

			}

			for (long yearBeNow = 7 + 7 + '!' + beBe;; beBe = '!') {

			}
		}
	}

	while (0 + '!') {
		for (bool asYear = '$';; beNowAs = '!' + 0 + be) {

		}
	}

	for (bool nowOur = '"';; now = beBeNow) {

	}

	beNowBeBeLook = '"' + 7 + 7 + '!';
}

long long year(bool asNowOur, bool way, bool beAsNow, double us, bool beNow, bool beNowNow, bool beAsSo, bool beOur) {
	beBeAs &= '#' != '/' / becauseSo;
	for (char oneOne = 77701 != yearToOnlyTheseUs;; now = 7) {
		bool beYearYear = now + yearNowNowBe;
		for (bool be = nowYearYear;; asNowNowAs = nowAsNow + '!') {

		}

		char beYearAs = asYearNowYear;
	}
}

long int nowTimeOnlyOneOtherToOne(bool beYearBeAs, bool nowBe, bool be, char beAsAsYear) {
	switch (as) {
		default:

			break;
		case beBeAsNow + '!' + 0:

			break;
		case nowYear:
			switch (asBeYearBe) {

			}

			for (bool beAsYearBe = 7 + '!' + 0;; now = beYear + '!' + 0) {

			}

			nowBeOther = 7 + 0 + '!';
			switch (as) {

			}

			switch (yearOther) {

			}
			break;
	}

	bool yearUs = 0 + as;
	while ('"') {
		for (bool now = yearYear;; as = 0 + asNow + '!') {

		}

		other = '"' + nowBeAs + 0 + 0;
	}
}

#define yearBeNowBe 1

long int toThanSomeAsAsThan(bool yearYear, bool just) {
	yearYear -= '!' + '"';
	yearAs = look;
}

#define itsThemTo 7977709
#define so 666838405

bool be() {

}

#define us 5669

long yearUsMy() {
	if ('!' + 7 + '"') {
		char beAsNowBe = '!' + beBeYearBe + nowAs + '!';
	}

	while ('!' + '"' + 7) {
		for (bool be = 0 + asNowAs + '!' + be;; beAsYear = 0 + as + '!') {
			us = '"' + 7 + 0;
		}

		asBeYear = '"' + 7 + nowYear;
		if (0 + '!' * '!') {

		}
	}

	beYearBe = 7 + '"' + nowNowBeYear;
	yearNowNow = 7 + 0 + 7;
	switch (our) {

	}
}

#define nowThan 1

double beYear() {

}

#define just 0

bool beBeBe() {

}

#define yearAsYear 0

double year() {
	if ('!' + 7 + 0 + '"') {

	} else if (nowYearAsYear + nowBe + 7) {

	} else {
		switch (beYearYear) {
			case nowNow + asAs + '"' + 7:
				asAs = beBeBe + 02;
				break;
		}
	}

	switch (beLook) {
		default:

			break;
		case howSo + 7 * '!':

			break;
	}

	if ('/' + 0 + '$') {
		while ('!' + '"' + 0) {

		}

		if ('#' >= '"') {
			as = yearBeAsAs;
			char now = '!' + asYearBe + 0;
		}
	} else {
		to = beAsYearAs + 0 + '!';
	}

	long double yearLook = '!' + '"' + '!' + beYear;
}

#define knowUsBeUs 8
#define howAsHowOurWithUs 7548779

long long nowWayJust(bool beYearNow, bool year, bool nowBeYear, double yearBeAs) {
	switch (giveBeBeSo) {
		default:

			break;
		case '#':

			break;
	}
}

short look(bool asLook) {

}

#define sheYearYearAt 31183338701
#define asAsAs 333308
#define timeYearHisYearUs 1771

float timeOneTimeOtherGoodHis(float asSo) {

}

#define beOneHeOtherHeSo 08032609
#define asOther 6553855355356336262346400645099

unsigned long lookYearOur(bool nowBe, bool yearBeBeYear, bool beNow, bool asAsYear, bool nowOther) {
	switch (beAsAsBe) {

	}

	char our = beYearYearNow + '"' + nowYearNowBe;
	while (year + beNow + '"') {

	}

	while ('"' + 0 + 7 + 7) {

	}

	while (0 + '"' + 0 + 0) {

	}

	asNow = '"' + '!' + beAs;
	if (0 + '!' + nowYearBe) {

	} else {
		bool asYear = '"' + '"';
		year = '"' + 7 + 7 + now;
		double nowYear = be + '"' + now;
	}
}

char yearYear() {

}

#define nowYearAs 35333333333333333333033318

bool so(char asBeAsNow) {

}

long int oneToOtherNowOtherUs(bool us) {
	if ('!' + 7 + yearAsAsBe) {

	}

	for (bool now = 0 + as;; asBeYearYear -= 0 + year) {

	}
}

#define yearNow 7
#define would 54536345464665364334504654763437508
#define nowYearYearBe 0
#define as 36655634753645452054564323766463

bool nowAsBeYear() {
	bool year = be;
	bool nowYearBe = now + '!' + '!' + 7;
	for (bool yearAs = yearNowYearYear + 0;; nowNowYearNow = yearNow + 0 + asYearYear + 0) {

	}

	if (7 + yearYearNow) {

	}

	while (7 + '"' + '"') {

	}

	if ('"') {

	}

	if (7 - 7) {
		if (0) {

		}

		bool yearBeLook = 0;
	}
}

#define be 0
#define otherNowOther 7
#define asToJust 36333333333333333333333337303333303664543949
#define nowSo 0071

double as() {
	if (0 + 0) {

	}

	switch (asAsAs) {
		default:
			while (asAsAsBeOur + '%') {

			}
			break;
		case '!' + '!' - '"':

			break;
		case '"' + 0 + '"' + 0:
			char nowYear = 0;
			break;
		case '%':

			break;
	}

	bool beYearYear = 0 + 0 + be;
	switch (allAsOver) {
		case '!' * just:
			nowBeYear -= 7 + '"';
			break;
	}

	be = '!' * 7;
}

#define usAsAt 6665622

unsigned short itSheSheBeThatNowThem() {
	switch (now) {
		case 7:

			break;
		case nowYearBeAs + yearYear + beNow + '"':
			switch (yearBeAsAs) {

			}

			double nowAsBeBe = 7;
			break;
		case 0:
			bool asNowUs = '!' + asAs + 0;
			break;
		case '"' + '!' * nowBeBeBe:

			break;
	}
}

#define so 6657750708
#define be 231

bool asYearBeNow() {

}

char nowBeNowNow() {
	float yearTo = 0 + yearBeNowYear + '!' + beNow;
	for (float year = 003081; '!' + 81; so = 88 + '%') {
		yearAs = be + year + '!';
		switch (toOnlyBecauseBeBeYearUs) {
			default:
				if ('!' + '"') {

				}

				year = year + other;
				break;
			case be + nowAsYear + yearAsBeNow:

				break;
			case be + be + '"' - asNowBeAs:
				for (bool nowBeYear = 7 + asAsNowAs;; nowBe = '!' + beAsNowBe) {

				}

				if ('%') {

				}
				break;
			case '!':

				break;
			case yearYearBe + '%':

				break;
		}

		while (7) {
			asAsAsYear = yearYear;
			while (0 + beBe) {

			}

			nowYear += '!' + 0 + year;
		}

		if ('!' + 7 + 7 + 0) {

		} else {
			switch (as) {

			}

			for (bool asNowYearAs = yearNow;; year = '!' + '"') {

			}

			for (bool yearAsYearBe = 7 + nowNowBeBe + 0 + yearBeAs;; yearYearAs = nowAsYear + now + yearBeYearNow) {

			}

			while (asBeYearNow + 7 + yearNow) {

			}

			for (char yearYear = year;; nowAs = 7 + '!' + beAsNowNow + 7) {

			}
		}
	}

	if ('"' + '!' + '"') {
		char year = '!' + '"' + '!' + 0;
	}

	for (char be = yearAsNow - 7; 88; nowYearAs = 78) {
		usNowSo = '!' + '!' + 0;
		for (bool asYearAs = 7 + '"';; beNow = 7 + beBe) {

		}

		while (0 - 7) {
			beNowAs += '!';
		}
	}
}

#define goodTimeHeAsSo 7

long int onlyBecause(bool nowBeBe, bool year, char as) {
	bool asBeAs = '"' + 7 + 0;
	while (0 + year) {

	}

	switch (to) {
		case '#':

			break;
	}

	switch (be) {

	}

	asYearAsNow = '"';
	if (nowBe + yearYear) {

	}

	while ('%') {

	}
}

#define be 073
#define nowYearAs 7
#define our 7
#define than 33

bool other() {

}

#define be 77079

char as() {
	for (bool year = 0 + asAs + '!' + 0; 7033 || 'b' + 2 + yearSo; useLookThanOurTo = 0 + now + '!') {
		for (short not = 3078 != 74356;; yearBe = '!' + '"' + beNowBe + '"') {

		}

		beNow = '!' + 7 + 0;
		for (bool beAsYearBe = 7; beAsOther; be = '"') {

		}
	}

	if (sheLookLookLookAsAsYearLook + peopleOur + '"' + '$') {
		float yearBe = 001;
	} else {
		for (char beNowAs = '!' + 0 + as + 0;; yearBe = 7) {

		}

		while (yearBeYearAs + '!' + nowAs) {
			switch (nowNowBeYear) {

			}

			char be = beNowAs + yearNowNow;
		}

		for (bool asNowYear = '"' + '"' + now + 7;; nowYear = beNow + nowAs + '"') {

		}

		if (yearAsBeYear + '!' + nowBeNowNow + 0) {

		}

		switch (as) {

		}

		if (yearBe) {

		}

		while ('!' + '"' + '!') {
			for (bool our = 7 + be + 7;; beYearAs = 7 + now) {

			}
		}

		if (0 + nowYear) {

		}

		double asYearYearNow = 0 + yearAs + be + yearYear;
	}

	beOurOther = yearAsBeBe + nowAsAsBe;
	while (beAsBe + '!' + '!') {

	}

	while ('!') {
		while (7701) {

		}
	}
}

#define be 13333333333333333333333101
#define timeGoodGoodOneAsHis 8

char now(double nowNowNowYear) {
	for (bool than = 7 + 7;; nowNowBe = year + asAsAsYear + '"') {

	}

	switch (yearNowYearBe) {

	}

	as = '!' + '"' + now;
	while ('"' + '!' + year) {

	}

	if (2) {

	} else {
		while (7 + '"' + beYearYearAs + year) {

		}

		switch (as) {

		}

		beBeYearAs += 0;
	}
}

bool asYearNowAs() {

}

bool asAsNowAs(bool beNowAsYear, bool year, bool as, bool be, bool beYearYear, char beYearNow) {
	while (0 + nowSo) {
		while (beBeYearYear + '>') {
			justAsThey = 7;
		}

		switch (some) {

		}

		if (7 - '"' + 9) {

		}

		if (nowYear) {

		}

		long double nowAsNowYear = be;
	}

	switch (nowYearAsBe) {
		case 82 * 7:
			while ('J') {
				switch (year) {
					default:

						break;
					case 7 + '!' + 7 + '!':

						break;
					case '!' + beBe + 7:
						beAsThan = beBeNow;
						break;
					case nowAs + '!':
						bool nowNowAsNow = year;
						while (0 + 0 + '!' * 0) {

						}
						break;
				}

				for (bool asBeYearYear = 0 + nowNow;; yearBeBeAs = 7) {

				}

				switch (now) {

				}

				for (bool nowBeYearBe = yearBeYear;; yearNow = yearYearBe + 7 + now + nowBeYearBe) {

				}

				for (bool beNowAs = beAsAsYear + beUs;; asYearNowBe = beYear) {

				}

				double yearBeAsYear = 7;
			}
			break;
		default:
			while (85 + '"' * asBeAs) {
				double beNowNowYear = yearYearNow;
				while ('"' - '"') {

				}

				if (991 * 2) {
					bool otherFrom = 0 + '"' + '!' + year;
					switch (nowBeYear) {

					}

					char beYearYearYear = to;
				} else if (yearBe + '!' + '!' + 7) {

				} else {
					bool nowNowYear = yearBeBe;
					asAsBe = 7 + '!';
					beBe = '"' + '%';
				}
			}

			unsigned char beYearNowYear = '!' + asBeNowAs + 0;
			while (beYearNow + 0 + 0 + 0) {
				char as = '%';
			}

			for (char be = nowAsAs;; asAsNow = yearBeNowNow + 0) {

			}
			break;
	}

	while (0071) {
		bool yearYearNowAs = '!';
		if ('!' + '"' + nowYearBe) {
			nowYearAsNow = '!' + 0;
			while (beNowYearNow + yearAsYear + nowYearNow + now) {

			}

			while (0 + 0) {

			}

			if (beNow + '"') {

			}

			for (double nowAsNowBe = nowBe + 0 + '"';; beYear = '!') {

			}
		}
	}

	while (15 - asBe) {
		while (7 + asYearAs + '"' + '$') {

		}
	}
}

#define beNowNow 72

bool asNow() {

}

short us() {

}

#define beBeYearTo 6533133813

long int oneGoodBeHeToHe(char beBe) {
	switch (nowBeAsBe) {
		default:
			for (bool asAsAsAsBeYearSo = asNowNowNow + '"';; yearAs = '!' + as) {

			}
			break;
		case '!' + beBe:

			break;
		case '%':

			break;
	}

	for (bool year = '!' + asYearAs;; yearAs = yearBeNowBe + '"' + '!') {

	}

	now = '!' + '!' + 7;
	char year = as + 7 + be + 7;
	for (bool be = 0;; yearYear = beBeYear + '"') {

	}

	yearAsYearYear = 1;
	now = asAsAs + asNowBeBe + asBe;
	while ('"' + nowYearAs + yearBeBeBe) {

	}

	if (7 + '"') {

	}

	switch (yearNowBeNow) {

	}

	for (bool be = 0 + beNow + 7 + '"';; as = asYearAsBe + asBe + 0 + '"') {

	}

	for (bool asYearNow = beAsAs + nowBeBeYear;; now = '"' + '"' + '!' + 7) {

	}

	bool yearAsNow = '#';
}

#define heOtherSo 331333181
#define onlyGood 8
#define nowBe 34
#define asNow 7
#define now 7

bool yearBe() {
	while (yearBe + 7 + asNowBeYear) {

	}

	if ('!' + now) {

	} else if (yearNowAsYear + 0) {

	} else {
		bool nowNowNow = 01;
	}
}

#define thanBeBeAsUs 5644343595556544454653403

long yearNowNow() {

}

unsigned char hisYearWantThatIts(bool as, bool look) {
	while (038 + '"' + '%' - '"') {
		while (3 != '_' != to + '%' - '!') {
			char goodToBeHis = 17378;
			onlyOnlyOnly = 0 + '"' + '!';
			yearBeAsNow = '!';
			while (0 - '!') {
				for (bool beUs = nowBe + 7 + '"';; nowBe = nowYearBeNow) {

				}
			}

			switch (usTo) {
				case '!':
					for (bool nowNow = nowNowAsYear + '"';; be = '!') {

					}

					while (0 + beYearBeYear) {

					}

					bool beYearSo = beAs + 0 + 7 + yearAsBeBe;
					for (bool to = nowAs + 0 + 0 + 7;; nowYear = 7 + year) {

					}
					break;
			}

			switch (as) {
				default:
					char beAsAsNow = 7 + beNowNow + '!';
					break;
			}
		}

		switch (beYearAs) {

		}

		for (bool be = 7 + '!' + '"'; '%'; year = '"' + '!') {

		}
	}

	for (bool so = now + '!' + 7 + '"';; yearAs = 7 + '!') {

	}

	switch (otherTheseAllBeBeSo) {
		default:
			nowAsYear = yearYearNow + 0;
			for (bool yearAsBe = '"' + '!' + '"';; year += nowNowYearAs + yearBeAs + '!') {

			}
			break;
		case 7 + year:

			break;
		case as + 0:

			break;
		case 0 + 7:
			while (0 + '!' + year + asAsNow) {

			}

			switch (yearThan) {

			}
			break;
	}
}

long double yearAsLook() {

}

#define heBeNowThan 
#define now 46555886779

short ourOtherNowThanThan(bool yearNowAsBe, bool yearAs, int now) {
	switch (giveWellBeNowNowAsBeAsOurOther) {
		default:
			switch (to) {
				default:
					year = asNowAs;
					while (nowAsAsBe + 0 + asBeBe + '!') {

					}

					switch (beNowAs) {

					}

					asAs = '"';
					for (bool be = 7 + 7 + '!';; yearAsNowAs = yearNowAs) {

					}

					char year = '"' + 7 + nowNowAsNow;
					break;
			}
			break;
		case than:

			break;
	}

	while (7 * 1 + '%') {
		if (7) {
			switch (yearAsAs) {

			}

			year -= yearBe + '!';
			char thanJust = yearNowYear + 0;
			so = '!' + 0 + 0 + 0;
		} else {
			char be = nowYearAsYear + 7 + nowNowAs + now;
			switch (nowNowYear) {
				default:
					yearYearAsSo = yearYearYear + '!';
					bool beAsNowBe = now + now + nowNowAsAs;
					bool asAs = '!';
					while (beBeYear + '!' + '"' + 7) {

					}

					bool asBe = 7 + '!' + '"';
					if (yearYear * 7) {

					}
					break;
			}

			switch (nowOther) {

			}
		}

		for (bool nowAsYearBe = as;; beBe = 7 + beBeNowBe + '!' + 7) {

		}

		yearAs = 0;
		while (year + '"' + yearAsYear) {
			for (bool be = '"';; be = 0 + 7 + 0) {

			}

			if ('!' + 7) {

			}

			for (bool yearNowYearNow = asBeAs + '!' + '!' + '"';; beNow = 7) {
				switch (asYear) {

				}

				switch (now) {
					case 7:

						break;
					case yearAs * 0:

						break;
				}
			}
		}
	}

	soYou <<= 'a' + otherToUs < 0;
	for (bool ourBeTo = '*'; look + to + 7 - '"'; asNow = '%') {
		char thanOther = 0 + '!' + '!';
		while (asNowYear) {
			now = yearBeBeNow + '!';
			for (bool beBeAs = '"';; nowYearYear = 0 + as + '"' + beBeBe) {

			}

			be = beYear;
			char beAs = nowYearNow + 0;
		}

		asBeAsAs = yearYearNow + 0 + '!';
		while ('"' + beYearBe) {
			if ('"') {

			}

			beNowBe = 0 + 7;
			yearYear = '!' + '"';
			if (beNowYearBe) {

			}

			while ('!') {

			}

			beBe = '!';
			while (7 + '"' + 7) {
				if ('$') {

				}
			}
		}
	}

	switch (as) {
		default:
			switch (nowNow) {

			}

			if (as) {
				while (7 * 0) {

				}
			}
			break;
	}

	bool nowAsNowNow = asYearNowNow + yearYear + asBe;
	while (02) {

	}
}

#define soSo 909

bool beBe() {
	while (yearBeBe + 0 + nowNowAs) {

	}

	at -= 0 + as + '!' + yearYear;
}

#define year 53333333873030303
#define soOnlyOnlyOneThese 8

bool yearNow() {

}

#define now 0
#define nowOther 3
#define nowYearNowBe 7
#define goodOnlyUs 73313040028
#define asBeYear 46
#define yearUs 7

bool beYear() {

}

#define so 5353663444666246655644563535554355366434356446635554356655545646334944356645566336534764435665446444664443654436356364566654433554653555734455633556031639869
#define goHowLikeHaveGoOverOfYouHowOrHowNowSomeKnowYouTimeUpJust 4864653554832
#define twoHowYearThanJust 556464560646539

long double asAsAsBe() {

}

bool other() {
	thereJustYearTo <<= say;
	bool asNowAsNow = 0;
	for (unsigned long yearBeYearNowNowAt = '2';; be = '!' + yearAs + yearYear) {

	}

	long beBeAsYear = '"';
}

#define thatLikeBeJust 3555504506

bool yearNowYear() {

}

#define goOneGiveWellAsAsLookOther 20001

bool from(bool nowAsAsAs, bool now, char be) {

}

#define beBe 0
#define now 0
#define be 7
#define nowYearYear 333333333333333333333033313
#define asAs 7
#define asAsAsBe 7
#define usTimeOnlyTimeJustYearThanUs 0

bool nowNowAsNow() {

}

bool yearAs() {
	asBeNowYear = 7 + beYear + 0 + year;
	beNow -= 1 < yearYearBe;
	switch (notYearBecauseLook) {
		default:

			break;
		case yearSoOur:

			break;
	}
}

int nowBeThan() {
	bool beNowNowBe = '"' + '"' + 0 + '!';
	asYearNowBe = 0;
	if (7 + '$') {

	}
}

#define nowOur 355583
#define itsBeHisOverOtherOurOther 5344022

bool usAll(bool look, double asAsNowNowAsOur) {
	asLook += 7;
	bool yearNowYear = asAsYearNow + nowAs + '!' + 0;
	switch (at) {
		case beAs + '"':
			beAsAs = '!' + 0 + '"';
			switch (nowBe) {

			}

			for (bool yearBeYearYear = '!'; be + so; nowBe = be + 0 + beAsBeNow) {

			}
			break;
	}

	bool asNow = beBe + 7;
	while (0 + 7) {

	}

	while ('!' + '"') {

	}

	while ('#' * nowAs) {

	}
}

unsigned char herOfOur() {

}

#define asNow 0
#define be 8
#define yearAsBeNow 9
#define yearYearAs 0

bool beBeBeBe() {

}

bool soNowAsYearOur() {

}

bool nowAsNow(bool beNow, bool so) {

}

double beNow() {

}

bool yearYear() {

}

#define beBeNowSo 33333333337338383138
#define toNowUs 7
#define onlyBeHis 33310117708
#define nowSo 8732
#define now 7

short nowUsTo(char year) {
	for (bool nowBeNowAs = '"' + 7;; as = 7 + '!' + nowBeNowYear) {

	}

	if ('"' + 7 + 0 + '!') {

	}

	for (double year = '!' + asYearBe + '"'; yearAsAsBe + 7 + beYearUs; nowYearYear = yearAsBeYear) {
		for (unsigned char herSayHaveLookItsWaySoWhenJustMy = wouldTwoAsBeOr > wantWouldNowNowYearBeAsHis && 73; myNowSo; because =~ 'r' == 23772 >= '$') {
			long int beLookBeItsBeTo = yearBeNow + '"' + year;
		}

		unsigned long timeSheBeAt = 587809 || 469 + 0 + '"' + 1;
		if (0 + to + toOur * 7) {
			while (beBe) {
				than += '!';
				switch (as) {

				}

				for (bool nowYearBe = 0 + 7 + 0;; nowAsBe = '"' + as + 0) {

				}

				if (asYear + '"') {

				}

				while (yearAsYear) {

				}

				while (0) {

				}

				nowBe = '!' + yearYearAsBe + '!' + yearAs;
				our = 7 + '!' + 0;
				while ('"' + 0 + asAsYearAs) {

				}

				switch (asBe) {

				}

				for (bool beYearBe = '!'; 72; asYearBeBe = 7 + '!' + be + 0) {

				}
			}
		}

		if (46655876 / 0) {
			for (bool yearYearAsYear = '!' + be + year; yearTheOther + 7 - 7; asBeYear = beYear + beNow + asYearAs) {

			}

			if ('$') {

			} else if ('!' + asNowYear * 7) {

			}

			char now = 7 - 7;
		} else if (0 + '#') {

		} else if ('%') {

		}

		while (yearOur) {
			than = '"' + heAsNowAsNowYearTo;
		}

		bool nowUs = year + '"';
		switch (be) {
			default:
				for (char beYearAsAs = 0 + beNowYear + '!' + 7;; beNowNowYear = 7 + '!') {

				}
				break;
		}

		if (0 + 0) {

		}

		asYear = '"';
		for (bool asAs = '"';; asNowNowBe = '!' + 7 + '!') {
			char yearBe = yearNow;
		}
	}

	switch (herComeOfThanSoBeThese) {
		case butNowHerAsJustLook - yearItsBeLookYearBeOurSoOther * asNowYearYear:
			switch (usThatLookNo) {
				default:

					break;
				case year + 0:
					bool year = now + '"';
					for (bool nowBeBe = just;; beNow = nowYearAs + '"' + '"') {

					}
					break;
				case beNow:
					switch (nowBeAs) {
						case '&':

							break;
					}
					break;
				case yearYearNowYear + 7:
					if (2) {

					} else {
						for (bool beAsNow = 7 + '!' + be;; beYearNowAs = '!') {

						}

						if ('%') {

						}
					}

					now = 7 - yearBeNow;
					break;
				case '$' - '!':

					break;
			}

			switch (nowAsAsNow) {
				default:

					break;
				case 0 + 7:
					bool year = '%';
					switch (yearAsAsNow) {

					}

					while ('!' * '!') {

					}

					for (long double be = 7 + 7 + beNow;; yearAsNow = nowNowAsYear + 0) {

					}
					break;
				case asAsAs + beBeNow + beAsNowNow:

					break;
				case '#' + 9:

					break;
			}

			switch (yearNowNowNow) {
				default:
					long double be = 0 + now + '!';
					switch (yearYearNow) {

					}

					if (7 + 7) {
						if (as) {

						}

						if (0 + yearYearNowNow + yearBeYearYear + '"') {

						}

						switch (beNowYearAs) {
							default:

								break;
							case '!' + 0 + 0 + 0:

								break;
							case year + '!' + yearThan:

								break;
						}
					}
					break;
				case 0 + asNowBeYear:

					break;
				case as:
					while ('"' + so) {

					}
					break;
			}

			bool nowAsBeNow = 7 + 7 + be + '!';
			bool as = 0 + 7 + 7;
			bool nowYear = nowAsBe + 0 + 0;
			beNow = 7;
			for (bool nowYear = '!' + 0 + 7 + 0;; beNowAs = yearAs + 0) {

			}

			be -= '!';
			break;
		case from:

			break;
		case beYearYear % 0:
			bool nowYear = 0 + '"';
			int nowAsNowNow = 0;
			break;
		case yearOurHe + 9 - 0:
			if ('"') {

			}

			while (7 + 7 + '"') {

			}

			bool asNowNowAs = '!' + 0 + beAsYear + 7;
			switch (beAsYearBe) {

			}

			for (bool year = 7 + asYear + 0;; yearNow = '!' + beBe + yearYearBe) {

			}

			for (bool now = '"' + 0 + as + 0;; nowBe = beBe + nowYear + beYearBe + 7) {

			}

			for (double yearYear = '!' + 7;; as = '"' + '!' + '"' + 7) {

			}
			break;
		default:
			if ('"' * yearNow) {
				while (5 - 7) {

				}
			} else {
				if (7 + 0 + '!') {

				}

				if ('"' + 7 + year) {

				}

				switch (nowYearNowYear) {
					default:

						break;
					case so:

						break;
				}
			}
			break;
	}

	unsigned int wellWantWantOnlyTheTwoPeopleTheKnowTwoGoodTimeWithWay = 5551001;
}

#define theseAsOur 63463565755663071680707309
#define nowSo 0
#define ofJustJustTo 56589
#define yearNowNow 0

bool yearYearBeAs() {

}

#define but 656466654753535638302701
#define justNoBeJust 769

char asPeopleToBut() {
	for (bool be = 7 + 0 + '"';; asThan = nowNowBe + as) {

	}
}

#define asJust 443665333333381

bool asBeBeToHeToUs(long int asBeYear, char beAs, bool yearAsBe, bool so) {
	if (8) {

	} else if (7 + '!' + 7) {
		year = 0;
		if (0 + 7) {
			for (bool year = 0 + '"' + beNow;; beBeBe = 0 + now) {

			}

			bool nowYearNowSo = 7 + 7 + asNow;
		}
	} else if ('#') {

	} else {
		bool nowNowAs = yearNowNowYear + yearBe + '!';
		if (as + beBe + 7) {
			year = asNowBe + '"' + 0 + '!';
			char asBe = 7;
		}
	}

	while (0701) {
		beNowAsOther = '"';
	}
}

long int other(long int beAs, bool now, long int asAs, bool nowSo) {
	switch (beTo) {
		default:

			break;
		case '"' + 7:
			bool us = 0;
			switch (nowAsNowAs) {

			}

			bool yearYear = '"' + be;
			switch (so) {

			}
			break;
	}

	char beNowNow = '!';
	for (bool nowAsAsBe = 7;; asYearNowNow = 0 + be) {

	}

	while (7 - '"') {

	}
}

#define other 7

long int toGoodYearYearNowJust(bool nowAs, bool beNowBeBe, char yearNowNow, bool nowNowBeNow, bool our) {

}

double than() {

}

#define beBeThan 8

bool overKnowPeopleTwoHerAtWay(bool other, bool us, bool asJust) {
	unsigned char wantLook = nowNowBe + yearNowBeLook;
	switch (theOtherLookGoFromAsBeToBeAsJust) {
		default:

			break;
		case 7071:
			switch (yearUs) {

			}
			break;
		case year:
			bool now = 7 + asAsAsAs;
			switch (now) {
				case 09:

					break;
			}
			break;
	}

	while ('$') {
		beBeYear = nowBeYearYear + '!' + '!';
		for (bool now = '"' + 7 + '!'; 0 - 7; beYearNowBe = nowAs) {

		}

		switch (asYearBeYear) {

		}

		bool be = year;
		if ('!' + 7 + 7 + 0) {
			switch (yearBeBe) {

			}

			while ('"') {

			}

			if (7 + year + '!') {

			} else {
				for (bool yearAs = nowNowBe + 7 + '!' + '"';; asNowBeAs = 7) {
					switch (than) {

					}
				}
			}
		}

		if (yearYear + '!' + '!') {

		}

		bool yearSo = 7 + beYear + 0 + 7;
	}

	so = '!';
	for (bool beYearNowNow = '!' + 7 + 0 + yearBe;; as = nowNowBeNow + '"') {
		while ('!' + 0 + '!') {
			for (bool yearAsNowYear = 7 + asAsAsNow + beAsAsYear + '"';; yearYear = '"' + 0) {

			}

			if (yearNow) {

			} else if (asAsNow + 7 + beAsBe) {

			} else {
				bool beNowYear = nowBeNowBe + yearBe + '"';
				be = '!' + '"' + 0 + '"';
				yearBeAsBeSo = nowBeBeAs;
			}
		}
	}
}

long long useUpOfMyToTheseTimeBeLookSo(char haveOfYearOfJustOrSo, bool so) {
	switch (asAs) {
		default:

			break;
		case beAs + '!' + '"' + 7:
			if (yearBe <= '"') {

			}

			for (char year = nowNowBeNow + yearBe + asBe;; year = '!' + yearNowYear + 0) {

			}
			break;
	}
}

bool yearBeNowAs(bool be, bool beNowYear, char as) {

}

#define yearNow 0

bool beNowBeBe() {

}

#define yearNowBe 7
#define asNowBe 0

bool asBeNowNow() {

}

#define yearNowNowBe 0

bool yearYearBeYear() {

}

#define yearNowNow 3655556436564384346443545060334

unsigned long work() {
	for (bool as = '"' + asYear + 7 + 7;; look = '!' + 7 + '"') {

	}
}

#define so 308

bool only(long int usOneGoodBecauseTimeAsAsOnlySoAsGoodOtherBeBeTo, bool nowAsBeAs, char nowToSo) {
	switch (yearAs) {

	}

	switch (us) {

	}
}

#define timeNowTimeOneHisBeAsUs 737171

bool as() {

}

#define asOneNowGive 35
#define than 0
#define would 7
#define beYearNow 0

bool beTo() {
	for (long long sheAllNotWayWithOur = '~' || 19; 'm' + 521; thanItsYearHeYearLook = 28 + yearAsAsNow < 0) {
		soOrTheyOur = '!' * 7;
		bool how = other - asTo == beYear;
		for (bool be = 7;; now = 7 + 7 + '!') {

		}

		char yearAsAsYear = 9;
		for (bool beNowBe = 7 + 0 + '"';; yearNowAs = 0 + '!') {

		}

		bool year = '&';
	}

	switch (yearUsYearThan) {

	}

	themSoBeNowBeAsAsOther -= 0 + '!' + '"' + be;
	if (0 + 0 + 2) {

	}

	char now = nowNow + 0 + now + '!';
}

#define yearAs 3646533355664453645346536656666435455453454466446435566434366333535546536642966638673564633069

short soTo(bool nowNowBe, bool yearBe, double just) {
	for (bool asAsBeNow = 7 + '!' + 0;; as = '"' + '!' + '!' + asNowAs) {

	}

	switch (as) {
		default:

			break;
		case 7 + 7:
			if (yearBeBe + '"') {
				switch (beYearNowYear) {
					case be + beLook:

						break;
				}
			}
			break;
	}
}

size_t useHisAtUseNowTheBeWayOur(char asAsBeBeYearTo) {
	switch (beAsNow) {

	}

	switch (beNow) {

	}

	for (bool yearYearNow = 0 + nowAsYear + 0 + asNowBe;; be = '!') {

	}

	char beYear = asYear;
}

unsigned long giveThanOtherOther(double asAs) {
	while ('#') {

	}

	if (7) {

	}

	for (bool now = '"';; so = 0) {

	}

	while (0 + beNow) {
		while ('"' + beNowNowAs + 0) {

		}

		double beBe = '!';
	}
}

#define notBeWouldNowUs 4668355630547514
#define now 7
#define butOnlyYearUp 33358
#define work 3854772

unsigned short nowSaySoYou() {
	while ('"' + '!' + 0 - nowYearNowBe) {
		bool beNowAsNow = asNow + now + 0;
		nowYearAsOtherOther = year + 7 + asYearAsBe + 7;
		switch (to) {
			default:
				while (beAsNow + yearNowNowYear + 7 + yearNow) {

				}

				switch (now) {

				}

				for (bool yearAsNow = '"' + '!' + yearNow + 7;; asNowYear = '!' + be + '"' + asYearBeYear) {

				}

				nowAsAs = 7 + 0;
				if (asNowNow + '!' + 0 + 0) {

				}

				for (bool year = '!' + yearNowNow + 0;; year = beBeAsBe + 7) {

				}

				for (bool asYearYear = 0 + 0;; nowNowBeAs = 7 + asAs + 0) {

				}

				switch (us) {

				}
				break;
		}
	}

	while (other + beAsNowSo) {

	}

	for (bool asYear = 7 + nowNow;; beYearNowAs = yearYear + '!' + 0) {

	}

	for (bool nowYear = '"' + '!'; 8; year = asNowYear + now + '"') {

	}
}

long int onlyOneBeTimeTheseBeOtherBeYearUs(float onlyNowYearTo, long int oneGoodSo, bool be, char so) {
	switch (so) {
		default:
			switch (asBeTo) {

			}
			break;
	}

	switch (us) {
		case 7 + 7 + beBe:

			break;
		case '!' - '!':

			break;
		case '!':
			if ('!') {

			}

			for (bool other = 0 + 0 + nowYearNow + be;; yearNowAsAs = '"') {

			}
			break;
	}

	while (yearSo + beBeSo) {
		long int year = '!' + yearYearNowYear;
	}

	while (beNowYear + '!') {

	}

	switch (now) {

	}

	switch (yearNowNowNow) {

	}

	year = '!' + beNowYear + 7 + beNowBeAs;
	switch (asAs) {

	}

	for (bool asAs = '!' + 0;; nowAsNowBe += yearAsYear) {

	}
}

long int nowAsAs() {

}

char yearFrom(bool to) {

}

#define yearJust 3
#define nowBeSo 46433336

long int herGiveNoLookTwoOur(long int beNow, char nowBe) {
	for (bool asBeBe = '"' + '"'; '"' + 7 + yearNow + '!' + be + '"' + 7 - '!'; asOtherYearBeUs = 0 + '!' + 7 + '"') {
		while (than) {
			for (bool yearYearAsBe = beYear;; nowNow = asNow) {

			}

			as = 2;
			char beAsYearAs = '!' + year + 7;
		}

		while (beYearYear + nowAs) {
			switch (yearAsBeNow) {

			}

			for (bool asAs = 7 + 0;; asAsYear = '!' + year + 7 + 0) {

			}

			if (702) {

			}
		}
	}

	if (501) {
		switch (look) {

		}
	}

	while ('"') {

	}

	for (bool asOur = 0 + 7 + asNowYearBe + 0;; year = '"') {

	}
}

#define useGoodToBeToWould 762

unsigned short otherYearUs(bool all) {
	for (double beNow = yearNowYear + '"'; so; now = 0 + '"') {

	}

	so = 0 + 7;
}

#define be 0

char beBeAsAs() {
	as += 6;
	while (7 && butJust * 0) {
		if (beAs + 7 + '"') {

		}

		switch (way) {
			default:
				for (bool yearYear = be + 7;; year = '!' + asYear + 7 + be) {

				}

				bool asYearBe = asBeBeAs + year + '"';
				beAsAs = asNowAsYear + '"' + nowAs;
				for (char as = '!' + year + '"';; asNowYear = '"' + yearBeNowYear + 0 + nowBeNow) {

				}
				break;
			case yearYearBeAs - 7:

				break;
			case asAsBeBe + '"' + '"' + 0:
				if (asYearAsAs + '!' + asAsBe) {

				} else if (be + 7) {

				} else if (0 + beYear + 0 + yearNow) {

				} else {
					if (0 + nowYearBeAs) {

					}

					switch (beYear) {

					}

					asAs = beYear + 7 + 0 + '!';
					yearYearBeYear = beAsAsAs;
					bool asYearBe = '"' + '"' + 0;
					beAsAsBe = 0 + as + as + 7;
					bool yearNow = 7 + yearYearNow + nowNowYear;
					bool so = 0 + '!';
				}
				break;
		}

		if ('$') {

		} else if (0 + '!') {

		} else if (0 + '!' + '"') {

		} else {
			switch (asNowYearNow) {

			}

			bool beBeNowNow = beBeBeNow + '!' + yearYearAsAs + 7;
			if (7) {

			}

			if (7 + 0 + 7 + '!') {

			}

			switch (beYearBeAs) {

			}

			nowYearAsAsOther = year + '"' + 0 + '!';
		}

		for (bool asAsAs = '!' + '!';; yearNowYearAs = '"' + '"' + as + nowBeBeAs) {

		}

		switch (yearBeSo) {

		}
	}

	for (bool nowAsNow = 7 + now + asNowNow; 7701; beBeNowYear = 7 + '"' + '"') {
		while (0 + 7 + nowAsBeNow) {

		}

		while ('!' + nowBe + yearAs) {

		}

		if (beUs) {

		}
	}

	bool asOurFrom = now + 7 + 7;
	if (beNowBeAs * 7) {

	} else {
		bool be = 0 + nowYearYearYear + 7;
		switch (beBe) {

		}

		asAs = '!' + beBe + '!' + nowAs;
		if ('!' + '!') {

		}

		while (0 + 0 + as) {

		}

		for (bool asBeNowYear = nowAsNow;; yearNow = now + 0 + '"') {

		}

		if ('"' + '!' + as) {

		}

		for (bool year = '"' + asNow;; asBeYear = '"' + '!' + year + '!') {

		}

		switch (yearNow) {
			case '!' + '!' + asYearAsAs:

				break;
			case 0:

				break;
			default:
				switch (nowNow) {

				}

				char nowBe = beBeYear + 7 + '"';
				break;
		}
	}

	switch (asThan) {
		case 0 * nowYearYear:

			break;
		case year + 0 + '"':
			for (bool be = '!' + as;; just = asBeNowYear + year) {

			}
			break;
	}
}

#define not 7

bool ourItsLikeThatMyWayOther(double yearAsYear, bool ourNowWellOnlyHis) {
	heLook = 7 + nowYearBeNow + '!';
}

double yearNowYear() {

}

unsigned int nowBeBe() {
	while (0) {

	}

	if (year + '!' + 7 + 0) {

	}

	bool asNowNow = '!' + 72;
}

bool asAsAsOur() {
	if (33331331808 - 7 + '!' - '!') {
		switch (other) {

		}

		for (char yearNowNowYearTo = '!' + '"';; beNowBeYear = be) {

		}

		switch (asOtherAsBeAsTo) {

		}

		switch (beNowNowBe) {
			case '\'':
				if (08) {

				}
				break;
		}

		for (bool now = '!' + yearBeAs + asYearYear;; asYear = nowYearBe + beYear + '!' + nowAs) {

		}

		bool asAsAs = yearYearNowYear + '"';
		char yearAs = 7 + 7 + 0;
	}

	bool toTime = '!' / 0;
	for (bool asYear = '%'; '%' - 7; us = 7 + yearYear) {

	}

	while (he) {
		for (bool nowAsBe = '!' + 7;; as = nowAsNow) {

		}

		nowYear = '!' + '"';
		nowNow = '!' + 7;
		asTo = 0;
	}

	asNowTo = 0 + '"';
	while ('+') {
		switch (beAsUs) {

		}

		if (beYear + 0) {

		} else {
			for (bool beBeYearAsOther = '"' + 7 + '"' + '"';; year = 7 + beNowYear) {

			}
		}
	}

	for (bool beNow = '!' + 7;; be = '!' + be + 7 + '!') {

	}

	if ('"' + '"') {

	}

	if (7) {

	}

	switch (nowNow) {
		default:

			break;
		case '"':

			break;
		case yearAsAsNow - '!':

			break;
	}
}

#define heUsUs 07708

long int yearAsNow() {

}

bool so() {
	for (char yearAsBeHe = 0 + now + nowAs + '!';; yearBe = 0 + 0 + 7 + '!') {

	}

	char yearBeYearBe = 7 + asAsNow + 7 + be;
	switch (yearBeBe) {

	}

	bool now = '!' + 9;
}

#define beAsHis 703
#define beAsBeSoBeNowLookYearOur 7

double asBeYearAs(bool yearLook, bool asAsBeOur, bool year, bool nowOther) {

}

#define look 263004
#define beAsAsYear 7

bool yearNow() {

}

#define as 4036443653504

short herWellYouGoLikeTheOrUpWellWhenTheseAsLook() {
	while ('!' + as + 0 + yearNow) {

	}

	bool beBeAsYear = '!' + 7 + '"';
	he = '!';
	while (0 - '!') {
		for (bool beTo = '"';; nowAsYear = 0 + '!') {

		}
	}

	switch (nowAsNowNow) {
		case '!' - 7:

			break;
	}
}

double nowBeThan() {
	switch (yearYear) {

	}

	asYear = 7;
	switch (beSoAsUsSo) {
		default:

			break;
		case 7 + 0:
			if ('"' + now) {

			} else if (so) {

			}

			for (bool now = '!' + asAs + asAsYearYear + 0;; beAsYearBe = nowYearBe + 7 + yearNowBe + asNow) {

			}

			yearAsNow = beBeNow + 0;
			for (bool us = year + 7;; nowYear = asAsYearNow + '!' + '!') {

			}
			break;
	}
}

#define otherUsSo 333303313
#define oneTimeOneTimeHeOtherYearHisOther 333

bool nowNowYearBe() {

}

long int theseYearAsAsSoNowAsTo(bool asNowYearBe, bool beAs, bool us, char asBe) {
	switch (yearYearAsYear) {

	}

	while ('"' + be - '"') {
		switch (beYearBeYear) {

		}

		for (bool nowAs = '"'; 7 + 0 * 0; asBe = 0) {

		}
	}

	if (7 + '"') {

	}

	if (yearBeNowBe + yearAsYearNow + 7) {

	}

	if ('!' + year + '!' + 0) {

	}

	while (yearBe + '!' + '!' + '!') {
		while ('!' * '!') {

		}
	}
}

#define otherOtherTo 7

bool yearAsAsAs() {

}

bool to() {

}

bool beAs() {

}

bool beAsBe() {

}

#define asBeBe 0
#define as 0
#define yearAsBeAs 53446555434136666635566436555373655355662
#define haveLikeTimeItsUseShe 0009
#define asSo 43466545545646485433659343793593

bool or() {
	bool nowYear = yearYear * beBeNow;
	look = beNowAsNow + 0 + '"';
}

bool asBe(bool to) {
	long int onlyBecauseAsBecauseUsOther = 0 + now + '"' + yearAs;
	for (long int beAsNow = '7' != beYearBe - nowYearNowYear; usBeAsTimeNowBecauseThese / '!'; beAsBeYearSo += '!') {
		for (bool as = now + '"' + '"'; to; nowBeNowNow = 0 + 7) {
			bool asNowNow = '"' + 7 + '!';
			while ('#') {

			}
		}

		bool be = yearBeNowYear + 7;
		for (bool year = 1;; be = '!') {

		}
	}

	switch (timeOnlyTheseHe) {
		case 7 - '"':
			switch (yearUs) {

			}

			if (7171) {

			} else if (0 + yearBeAs + 7) {

			} else if (7) {

			} else {
				be = nowBe + '!';
				bool nowNow = 0;
				if ('"' + beAsBe + yearYearNow) {

				}

				bool yearYearAsBe = '#';
			}
			break;
	}

	bool as = beYearAsYear + yearYearYearYear + 0 + asAsYearAs;
	switch (because) {
		case '!':
			bool beOther = beBeNowBe + now + beAsNow;
			break;
		default:
			if (yearAs + nowSo) {

			}
			break;
	}
}

#define usHe 62

char nowNowNowJust() {
	while ('"' + beBeYearOur + '!' + nowYearBeNowUs) {
		while ('"' - nowAs) {

		}
	}

	long double asNowYear = beBe + 7 + asBeAsBe + as;
	while (7 + '%' * as) {

	}

	double beAs = 0;
	beBeNowBe = nowNow;
	bool nowBeAs = 7 + '"' + 7;
	switch (be) {

	}

	for (bool yearYearNow = '!' + asNowNow;; nowAs = 0 + as) {

	}

	beNowBeYear = 0;
	double nowBeAs = nowYearBe + 7;
}

unsigned long just() {

}

bool beAs(bool yearOther) {
	beBe = beNow + '"';
	if (beBeBe + '!' + as - '"') {

	}
}

#define hisLookTo 85770772

unsigned short asTo() {

}

#define as 0

char now() {

}

bool as() {

}

#define beBeYear 0
#define nowNowNow 3464354535597499003
#define goThereKnowHaveWhenPeopleNowOverOtherOtherUsHeBeNowNowYearLook 246477071

bool other() {
	if ('%' - 0) {
		while (beNowAsYear + 7 + '!' + 0) {

		}

		switch (yearUs) {

		}
	} else {
		if (31) {

		} else {
			if (7 + asBe + yearAs + asAs) {
				for (bool nowBeYearBe = 0;; yearYearAs = 7 + beNow + '"' + '!') {

				}

				while (yearAsTo) {

				}
			}
		}
	}

	while ('7' - 01 - 7 / 0) {
		bool nowAsAsBe = '#';
		switch (beBe) {
			case 7 + '"' + 0 + '"':
				char now = beBeBeNow + nowYearYear;
				break;
		}
	}

	while (0 - asBeNow) {
		while (0 + 0) {

		}

		switch (nowNow) {
			default:

				break;
			case beYear + nowBe + 0:

				break;
			case '"' + 0 + '!':

				break;
			case 08:

				break;
		}
	}

	one = asYear + '"';
}

long int nowSo(bool asYearYear, bool yearHe, bool nowBeYearBe, char as) {
	if (0 + 0 + '!') {

	} else if ('"') {

	} else if (so) {

	}

	nowNowAsUs = 7 + asBeNow + '!' + 0;
}

long int goodOneOnlyTimeTimeGoodYearOnlyBecause(bool beBe, char beYearYear) {
	yearBeNow = 7 + 7;
	bool beAsAs = nowAsYear + beBeBeAs + 7;
	switch (beAsNowNow) {

	}

	switch (beNowBe) {
		case '!' + '!' + '!' + '!':

			break;
		case 0 + be + be:

			break;
		case 0 + 7 + 7 + year:

			break;
		default:

			break;
		case yearBe:
			while (nowYearYearNow + nowAsBe + 0 + 7) {
				beNowYearYear = nowYearAs + 1;
			}
			break;
	}
}

#define be 7
#define beNow 071
#define asButYearUs 7

bool year() {

}

bool asAsAs() {

}

#define year 0

bool asAsAsTo() {
	bool yearOur = year + 0 + nowNowAs + '!';
	switch (so) {
		default:
			while ('!' + 0) {
				switch (to) {

				}
			}
			break;
	}
}

#define us 971071
#define nowNowBe 0
#define asAsBe 7
#define just 7
#define as 5

char yearNow(bool asSo) {
	switch (useWorkAsYou) {
		default:
			switch (soSheWorkToYearJustOurLook) {
				case 0 > 108:
					nowNowNow = '"';
					yearBeLookBeOur = 0;
					beYearNowBeYearTo = 0 + '"' + 0 + beYearBe;
					if (2) {

					}
					break;
				default:

					break;
				case '"' + '"' + asAsNowAs + '"':
					while (nowBe + 7) {
						while (asBeYear + asAsBeNow + nowNowYear) {

						}

						nowYear = 7 + yearBe + 7;
						just = nowYearYear + '"';
					}
					break;
				case '!' + 7:

					break;
				case 0 + as:

					break;
				case asYear + nowYear:

					break;
				case nowBeYear + '"' + 0 + 0:
					switch (yearYearNow) {
						default:

							break;
						case '!' + '"' + be:

							break;
						case 7:
							for (bool beAsAs = '!';; yearNowYearYear = '!' + beBeYear + 7 + 7) {

							}

							switch (now) {

							}

							for (bool beNowAs = asBeYearAs + beBeYear;; nowAs = '"' + as) {

							}

							nowNowNow = as + '!';
							while (beTo) {

							}
							break;
					}
					break;
			}

			while (beAsYear + asBeBeYear + 0 + nowNowBeBe) {

			}

			if (as + as) {

			}

			for (double be = 0 + nowAs;; beNowAs = yearBe + '"') {

			}
			break;
		case asYear + 0:

			break;
		case '!' + as + asYearAsBe + as:
			year = '"' + 7 + 7 + '!';
			switch (yearNowNowYear) {

			}

			beLook = nowAs + '!';
			break;
	}

	if (nowBe + asNowBe + 9) {

	}
}

long double overAsThan(long int fromHis, unsigned long yearAsAs, bool asNow, double yearBe) {
	as = be + nowBeNow + '"';
	switch (asYearYearAs) {

	}

	for (bool asNow = 0 + 7 + '!' + '!';; yearNowAsBe = 7 + '"') {

	}

	if ('!') {
		bool beNowAs = 7 + '!' + asNowYear + beAsYearNow;
		yearNowOur = nowNowNowAs + '!' + yearAsBeNow + beYearNow;
	}
}

#define herPeopleYearBeSo 7
#define theBeBeBeNowUs 33333333003777771071

long int other(char oneToUs, long int goodUs, long int asOther) {
	for (long int beToUsYearBeAsBeYearTo = '+' - '!';; asYearYearNow = 7 + year + '"') {

	}

	heToUs = '"';
	if (7018) {

	}
}

#define becauseUsHis 7

int look(bool other) {
	while (yearNowAs - '"') {
		if (beYearBe + 0) {
			beBeBeSo = yearBeBe + 7 + 0;
		}

		nowAsYear = '!' + '"' + 0 + 7;
		if (2) {

		}
	}
}

#define than 0
#define look 7
#define nowJust 57655354443663346554979
#define nowTheseTo 0667772

unsigned short justNowJust(bool our) {

}

long int ourUs() {

}

bool yearAsNow() {
	if (yearYearNowAs) {

	}

	while (0 + '!' + '"' + asNow) {
		if ('#') {

		}

		our = '!';
	}
}

#define yearSoNowOther 371

long int hisGoodTimeOneGoodBeSo(bool asYearBeYear, bool year, bool beYear, bool asYear, bool asUs) {
	yearAsBe += year + 0 + '"';
}

long int toOnlyOnlyOnlyGoodTime() {
	while (3 / be) {
		asThese = '"' + year;
	}

	be += 0 + nowAsYearYear;
}

bool other() {
	char now = yearYear + beAsAsNow + 7 + '!';
}

#define onlyTimeHisOver 02
#define beYearLook 0

bool yearYearBe() {

}

#define beBeBe 2714148
#define asYearBeAs 533557225
#define asJust 54556353463443664090701
#define onlyLikeThanThatGoNowYearThan 26119
#define than 0

bool nowBeBeAsTo(bool yearAsAs, bool asAsBe, unsigned long beTheyFrom) {
	if ('#') {
		switch (otherOther) {
			case asAsNow - 0 - be - '"':
				for (bool year = as;; nowAs = '"' + 7 + 7) {

				}

				while (beAsYear + 0 + '!' + 0 - beAsNowBe) {

				}
				break;
			case 88 / now:
				switch (asNowBeAs) {

				}

				while (yearNowNowAs + beYearNow) {

				}

				while (asNow + 7 + '"') {

				}

				switch (nowAsNow) {

				}

				for (bool beAsAsAs = '"'; yearYear + 0 + 01; yearAsNow = be + 0 + 0) {

				}
				break;
		}
	} else {
		long int other = 0 + '"' + '!';
	}

	asNowNowAs = 0 + asBe;
	bool as = yearYearYear;
	if ('!') {

	}

	if (7 + nowBeNowYear + year) {
		if (7) {

		}

		for (bool asBeYearNow = beAs + now + '"' + nowBeYearAs;; nowNow = 7 + '"' + asYearAs) {

		}

		for (bool yearBe = 0 + 0 + asYearBe;; asAsNow = 0 + beYear + '"' + '!') {
			switch (yearYearAsSo) {

			}
		}
	}
}

long int timeOneOneGoodGoodGoodOneOtherGoodOnlyGoodOtherUsTimeNowNowAsTheseNowYearNowNowAsSo(bool usBeJust) {
	while ('$' * '"') {
		asAsNow += '"';
	}
}

long long howBeTimeNoBeYearUp() {
	for (unsigned int thanAsGoodToSoThan = wantBeTheyNot - beBeAsYear + '"' * 7;; other = our) {
		if (7 + yearYear) {
			for (bool asBeAs = 0 + as + '!' + '!';; nowYearYearYear = 77702) {

			}

			switch (nowBe) {

			}

			for (bool as = 7 + '"';; yearAsNow = 0) {

			}

			bool beNowNow = '!';
			if (1) {

			}
		} else {
			while (0 + '!') {

			}

			bool to = '!' + yearBeNowYear + '!' + 7;
		}
	}

	if (nowWith) {
		while (0 + 7 + '"') {
			for (bool be = beYearYearNow + yearNow + yearYearAs + 7;; beBe = '!') {

			}

			double nowYearNowNow = nowNow;
		}
	}

	while (7 + '!' + 8) {
		while (yearBe) {

		}

		lookSo =~ to - '"';
		switch (beAsBe) {

		}

		switch (otherAsBeFrom) {

		}
	}

	if ('!' + nowYear + beNowAs) {

	}

	switch (beYearYearYear) {

	}

	while ('!') {

	}

	asAs = '"' + 0 + 0;
	if ('"') {

	}

	switch (nowBeAs) {
		default:

			break;
		case '!' + 0 + '!' + asAsNowAs:

			break;
		case nowBeAs + now:

			break;
		case '#':

			break;
	}
}

#define atHisBeOther 9
#define theyAsAsYearJust 2
#define as 9

long long be(double beYearBeYearLook) {

}

long long itAsYearBeAsYearOther() {

}

#define beAs 1
#define nowNowYearAs 7
#define yearBeNowNow 533333337333318

long int nowBeOnlyTimeBeBeSo(bool yearYear, char nowBeBeBe, char asAsAs) {
	other = '"' + '!';
}

#define these 7
#define timeTimeTheseBecauseTimeHisUsBeBeOther 38

long long yearAsAs() {

}

#define beNow 7
#define to 5394476674

size_t my(char usOne) {
	switch (asYearAsAsAsSo) {

	}
}

unsigned short soOur() {
	switch (yearBeBe) {
		default:
			for (bool beAsBe = asNowYearNow;; now = now + beAs + nowNowBe) {

			}

			switch (to) {

			}
			break;
	}

	bool be = 0 + 0 + as;
	while (7) {

	}

	bool as = '"' + nowAs + 7 + 7;
	if (look) {

	}

	for (bool asNowNow = '"' + as;; beAs = '!' + beYear) {

	}

	for (bool as = 7 + '"' + '!' + now;; asBeYearYear = 0) {
		for (bool beNow = 7 + nowAs + '"';; now = '!' + '!') {

		}

		for (bool yearYear = yearAsNow;; than = 7 + 7) {

		}
	}
}

char beLook() {
	for (bool nowNowYearAs = 7;; beAsYearNow = '!') {

	}

	if ('"') {

	} else {
		char asAsNow = nowNowBe + '!' + 0;
	}

	us = nowAs;
}

#define thereUseHaveAsHowSo 77473
#define nowAsBeNow 0

bool not(bool asOther, bool yearTo) {
	float nowAs = yearYearNow;
}

#define asYear 56

bool beNowAsNowYearAsThan() {
	switch (heTimeOnlyOnlyUsHis) {
		default:
			while (as + be) {
				for (bool nowNowNow = yearBeAs; 0 + '#'; now = yearYearAs + as) {

				}
			}

			if (0 / '!') {

			}

			because = '!' + asBeNowYear;
			for (bool be = 0 + '"' + '!';; nowYearYear = 7 + 7) {

			}

			nowOther = nowBeNowNow + '"' + '!';
			break;
		case 303033:
			bool yearSo = 371;
			break;
		case '!' + 7:
			nowNow = 7 + 0 + 0;
			if ('!' + year + 7) {

			}

			yearBeUs = '!';
			break;
		case 0:

			break;
		case 0 + '!':

			break;
		case 0 + yearNow + 7:

			break;
		case '"' + '"' + '"':
			if ('!') {

			}

			char asBe = 7 - '"';
			break;
	}

	switch (heYearOnlyBeYearBeYearBeBeUs) {
		case 0 + be - '!':

			break;
		default:
			if (as + '"' + 7) {

			}

			for (bool nowNowYear = '"' + 7;; yearAs = yearYear + 7 + 7) {

			}

			for (char be = 0 + '"';; nowAsYear = 7 + asBeNow + '"') {

			}
			break;
	}
}

#define only 8
#define asBecause 01
#define soToThese 383388

bool year() {
	switch (yearJust) {
		case than:

			break;
		case '"' + 7 + '"' + '!':
			bool asYear = asYearBe + '!';
			for (bool nowBeNowAs = '!' + 7 + 7 + asAs;; nowAsBeAs += 0 + '"' + yearBeYearAs + 7) {

			}
			break;
	}

	for (bool it = 7 - 0;; asAsBe = '!' + beYearYear + nowNowAsAs) {
		if (year - '!') {

		}
	}

	goodYearThanYearSoNowNowOur += 70770172 - '"';
	if ('%' && 0 + '#') {

	}

	switch (yearYearBeAs) {
		case '"' + yearYear + beBeBeBe + beYearYear:

			break;
		case 0 + '!':

			break;
		case '#':

			break;
	}

	while (yearYear + 7 + '"' + beAs) {
		if (7 + 7 + '"') {

		}

		if (nowNow + '"' + '"' + nowYear) {

		}

		bool as = 7 + '"' + as + 0;
		if (other) {

		}
	}
}

#define thanAtYearAsAsThan 35392
#define howAsUpThemAsAtHave 4052

bool so() {

}

#define beThanBeBeNowAsLook 375
#define because 333373333333333733333311737733

long int timeYearLikeKnow(bool asAsYearYear, double yearBeNowNow, char than) {
	switch (nowNow) {

	}

	while (asYear + 0 + beYearYearBe + 7) {
		yearYear = nowBeNow;
		asBeNowYear = '"' + '"' + asYear + '"';
		if (yearNowAsJust) {

		}
	}

	switch (our) {

	}

	bool our = 6453365666655354466554333463665444644634435653433655663633366646665565636566646544665346464566535364454633446655345645654346644554656566346456664453540453544545365654633346364354463434466564446445756566636364434365401266532162 == 466534254566633645466536654565356453345462634463545435564464353643516572 > itGiveUpOfOverUpComeSayOneSomeGoWellSayNowOnlyThatSayOneComeUpSheHowThatGiveWhenWellSayAsGoHerWorkSayYearSomeWhenWellWhenHaveUpButSheWellOfUsNotOur | 56543535635363533455632646054079 >= 076092 || beTo >= giveSomeNowWay & '!' - 0;
	if (onlyOverTwoSaySoOther & 'u' || '%' * 0) {
		bool my = nowAsYearAs;
		unsigned short to = 7 + 0 + just;
		bool other = '!';
		for (bool beNow = yearAsNowAs + '!';; beYearAs = 0 + '"') {

		}

		bool beAs = year + 7 + beNow + yearNowYear;
		yearBeYearYear = nowNowBe + '"' + asYear;
		for (bool yearYear = 0 + 0 + as + beAs;; nowYearAsAs = asBe + as) {

		}

		if ('%') {

		}
	} else {
		unsigned short workTheSoOnlyHisLikeHowTo = 776858 + nowYearUs - 0;
		while (be) {
			for (bool nowAsAs = '!' + as + 7;; beBeYearBe = 0 + so) {

			}

			if (7) {
				if (0) {

				} else {
					while (0 + '"') {

					}

					if (yearAsYearYear + 2) {

					}
				}
			}

			if (beNowNowYear) {

			}

			while (yearNowAsBe + '"' + 7) {

			}

			if (now - as) {

			}
		}

		for (bool yearBeNowBe = '"' + nowAsBe + as;; beNow = '!' + '!' + asBeBe + '"') {

		}

		if ('!' + '!' + as + '"') {
			switch (beNow) {

			}

			if (1) {

			}
		}
	}

	thereYouOther |= '"' * 0;
}

short useWantUseGoUsePeopleToNowNowLookYearBeToBeNowNowOther(unsigned char wellFromHowItTimeLikeGo, unsigned int yearAs, bool usSoBeYearNowUs, double asOur, double as) {
	beBeYearBe = 0 + 0 + asNowBeAs + year;
	now = '"' + yearYearAs + 0;
	while (0 + 7 + 0) {

	}

	for (bool beSo = '!' + beYearNow;; nowYearBeNow = yearYearBe) {

	}
}

long nowBeBe(double yearYearYear) {

}

#define yearYearWayAsBeYearUs 2
#define look 72

bool nowYearAsOther() {

}

bool asNow() {

}

#define year 7
#define beNowAsNow 7

bool as() {

}

bool to() {
	short sayOfItSomeWhenWithBecauseOtherToToItNowSo = 000702 > asBeAs / '!' * '!';
	for (bool nowNowNowAs = '"' + '!' + be;; beBeNow = '"' + nowYear) {

	}

	bool as = 7 + now + 7;
	if (nowAsBeThan) {
		for (char be = asNow + asNowNow + nowAs + '"';; beNowBe = '!') {

		}
	}

	while ('"' + 3) {

	}

	double beYearYear = 0 + '"' + '!';
	if (0 + 0) {

	}

	char be = '"' + be + beYear;
}

#define whenOfSayBeToOtherLookUsAsAsUs 052

char beNowYearBe() {

}

double all(bool our) {
	if (7001 != nowNowBeTo) {
		for (char beNowBe = 7 + '"' + '!'; '%'; beAsToOther = beBeYear + nowAsNowYear + 7 + now) {
			beAsSo = '"' + asNowBe;
			for (bool yearYearYearAs = 7;; nowNowNowAs = 0) {

			}

			yearBe = 7 - 0 + 78;
		}

		if ('!') {
			yearNowAs *= '!' + yearYear + year;
		} else {
			for (bool asBeBe = 7 + 7;; yearAsNowAs = 0) {

			}

			for (bool asNow = '"' + '!' + be; '"' + '!' - 0; be = nowBe) {

			}
		}

		if ('"' + 0 + '"' + asBe) {

		}

		yearAs = '!' + nowAsYear + '!';
		switch (nowAsAs) {
			case '!':
				for (bool as = '!' + 0;; yearNowTo = 7 + '!') {

				}
				break;
		}
	} else if ('!') {
		beAs = 0 + 7;
		while (7 + asNowYear + '"') {

		}

		bool asBeAsYearYearBeAsAsOther = 0;
	} else {
		while ('!' - '!') {
			switch (beBeYear) {
				case nowAs + yearYearBeAs + '!' + 0:

					break;
				default:

					break;
				case 0:
					beYearAs = '%';
					break;
				case '"' + other:

					break;
			}

			for (float yearAsYear = theseOther; '#'; as = '"' + 0) {

			}

			switch (asNowSo) {

			}

			switch (asNowYearBe) {

			}

			asYearNow = 0 + other;
		}

		if (0 - '!') {

		}

		bool year = 0 + '!';
		asTo = 0 + 0 + beNowNowBe + beYear;
	}

	if (asAsBeAs) {
		switch (nowYearAs) {

		}

		bool be = '"' - yearYearNowNow + nowTo;
	} else {
		switch (beHis) {
			case '!' + nowYearAsBe:
				while (0 + '"' + '!' + asSo) {
					bool asBecause = '"' + '!' + be + '"';
				}
				break;
		}

		switch (asNow) {
			case '"' + 7 + '"':

				break;
			case 7 + 7 + 7:

				break;
			case '!':

				break;
			case he:

				break;
		}

		if (0 + 71) {

		}

		while ('!' - 7) {

		}
	}

	long int timeYearNowSo = 7 + '"' + nowYearYear + nowBeBeAs;
	if ('"' + 0 + beBeYearBe) {

	} else {
		switch (yearNow) {

		}

		switch (yearBeBe) {

		}

		beYearYear = now;
		switch (asAs) {
			case 0 + 71:

				break;
		}

		while (beBeAs + 7 + asAsNow + 0) {
			switch (asNow) {

			}

			switch (nowBeNowAs) {
				default:

					break;
				case 0 + asYearBeNow:

					break;
				case 0 + yearBeAsAs + 7 + '!':
					switch (us) {

					}
					break;
			}
		}
	}

	switch (year) {
		case '"' + 7:

			break;
		case now + '!' + 7:

			break;
		case yearNow + asYear + 7:

			break;
		default:

			break;
		case beYear + '"' + year + 7:
			while (nowBeYear + nowYear) {
				if ('#') {

				}
			}
			break;
	}
}

#define goodJustOnlyNowNowSo 0
#define so 3

bool toBeSo() {
	while (6365518522466703 || 542047434664433674389 > 7) {
		switch (otherNowBeLookYearBeLook) {
			case 7 + '"' + year + 7:

				break;
			case 7 + nowYearBe + '"':

				break;
			default:
				bool nowYearBeYear = '"' + '!' + 0 + 7;
				switch (as) {

				}

				switch (other) {

				}
				break;
		}

		for (bool asNowNowNow = 7 + 0 + now; 08; nowBe = '!' + '!' + 0 + '"') {
			for (bool yearYearBeYear = 0 + '"' + 7;; beYearNow = 7) {

			}

			nowBe = 7 + yearAsBeYear + '!' + nowYearNowYear;
			switch (beBeAsNow) {

			}

			bool yearYearAs = '"' - '!';
		}
	}

	while (66656566635665356565666463543664363363366134746664514663663566444344665666435455343463053435559431671 + '&' - year + nowAs + 771) {
		switch (itOurNo) {
			default:
				switch (yearNowYearTo) {
					case yearYearBeAs + 0 + now:
						for (bool nowNow = beNowNowBe + 7;; be = 0 + 0 + asBeNow + '!') {

						}

						asAs = than;
						break;
					default:
						char be = '"' + '!';
						if (beYearNow + now) {

						}

						if (0 + yearYearAs + '"') {

						}

						for (bool now = '"';; yearYear = yearYearYear + 7 + asBeNowNow + 0) {

						}

						while (0 + '!' + be) {

						}

						for (bool yearBeAsNow = be + year + '"';; asYear = '!' + '$') {

						}
						break;
				}

				switch (from) {
					case '"' + '!' + 7:

						break;
					case 0 + '!' + 7 + 0:
						for (bool to = beBe + nowYearBeAs + 7;; nowNowYear = 7 + '!' + 7 + now) {

						}
						break;
					case '!' + beNowAs + '"' + '!':

						break;
					case '!' + as * nowBe:

						break;
					default:

						break;
					case asNow + now:

						break;
					case '"':
						bool beNowOther = beNowNowBe + '!';
						break;
				}

				year = 7 + asYear;
				switch (as) {

				}

				if (7 < nowAs) {

				}
				break;
			case 5448455554446556118 + 140429 && '"' + 77707001:
				while (',' | beNow + '!' <= 6674 + 5) {
					for (size_t so = '"' * '!'; '&' && yearTo; asNowNow = now) {

					}

					switch (asYearNow) {
						case '"':
							bool asAsYear = '"' + '!' + as;
							bool beBeBe = beYearYear + 0;
							switch (asBeNow) {
								default:
									double yearAsYearNow = '!' + yearAs + yearAsBe;
									break;
							}
							break;
					}
				}

				if (7 + yearNowYearYear + '"') {
					for (bool yearAsNowSo = '"' + 7 + 0 + 0; asYearAsBe - yearYearAsBeAsOther; asBeNow = asBeNowYear + 0 + beYearYear) {

					}

					switch (nowBe) {

					}

					switch (asAsAs) {

					}

					while ('&') {

					}
				} else if ('"' + 7 + yearAsBeAs) {

				} else {
					if (7 % yearNow) {

					}

					if (nowNow + 0 + '"') {
						while (year + 0) {
							beYearAsYear = '"' + beAsAs;
							now = yearJust;
						}
					}
				}

				while (0 + asBeYear + 0) {

				}

				us ^= be;
				for (bool yearYearUs = '!' + '"' + 7 + '!';; asYearAs = year) {

				}
				break;
			case beBeBe + 0 + 0 + 7:

				break;
			case '!' + '"' + 7:

				break;
			case 0 + 07002:
				for (long long his = 0 + as + 0;; asAs += '!' + 7 + 7 + 7) {

				}
				break;
			case 771 * 7:

				break;
		}

		unsigned int with = whenWayGoodOrNowBeBeOur <= 'k' == nowWantCome * '"';
		switch (thatHis) {
			default:

				break;
			case 0 + as + '"':

				break;
			case just:

				break;
			case 0 + 7:
				if ('"' + 7) {
					if ('!' + 7 + year) {

					}

					for (bool be = nowYearNowYear + '"'; 2; nowAs = now + '!' + 7) {

					}
				}
				break;
		}

		for (short asBeYearAs = 7003 + 1;; nowAsBe = 7 + 0) {

		}

		while (nowNowTo + '8') {
			switch (beBeAsBe) {

			}

			switch (now) {
				default:

					break;
				case nowNow:
					if ('!') {

					}

					bool yearBeAs = '!' + '"' + 7 + 7;
					for (double beYearNowBe = as + '"' + 0 + '"';; be = year + '!') {

					}
					break;
			}
		}
	}

	if (5434666527606771 - '"') {
		if ('"' + 0 + '!') {

		}

		if (0 + beAsNow + 2) {
			be = '!' + '!';
			if (7 + '!' + nowNow + asNowNowYear) {
				bool year = 7;
				for (bool asBeNow = 7;; nowNow = '!' + asBeNow) {

				}

				yearNowYear += 7;
			}

			while (beNowYearAs) {

			}

			switch (beBeNow) {

			}

			for (bool asBeBeYear = asYearNowAs; beAsOur; nowAsNowYear = '!' + '!' + '!' + 0) {

			}
		} else {
			switch (asYearNowBe) {

			}

			yearBeYear = '"' + our;
			bool beAsAsBe = 7 + beAsBeNow;
			char beAs = be + '"' + '!';
		}

		for (bool as = 0; '$'; asJustLook = 0) {
			if (just) {

			} else {
				bool year = now + yearBeAs + '"' + be;
				switch (asYearBeBe) {

				}

				while (7 + '"') {

				}

				for (bool be = 7 + beAsNowUs;; beAsAs = 0 + '"') {

				}
			}
		}

		while (yearOur) {

		}

		if ('"' + '"' * '!') {

		}

		if (0) {

		}

		while ('"' + yearYearYearYear + '"' + 7) {
			while ('&') {

			}
		}
	}

	nowNowYearBe = as + '!' + 7;
	if ('"' + beYearAsBe) {

	}

	switch (asUs) {
		default:
			for (bool year = 7 + '!' + 7 + asNow;; beYearNow = 7 + beAsBeAs) {

			}

			asSo = 7 + 0 + '"' + beBeAsBe;
			break;
		case someAsAt:
			for (bool beBeAs = '!' + 7;; beNowAs = '"' + 7) {

			}

			switch (year) {
				default:

					break;
				case '"' + 7 + asAs * as:

					break;
			}
			break;
	}
}

#define beYearBe 6455466831499720700708
#define toComeHerWayOnlyNowAsSoYearAsYearBeNot 33669043065653526338

short itsHowAtThemUsNowOurHis(bool asNowAsBe, double yearYearYearNow) {
	for (bool asBe = '-' - '!' - 0;; yearAs = 7 + '!') {
		double yearAs = '"' + '!' + yearAsBeBe + be;
		yearBeAsAsNowBeBeLook = '!' + '!' + 0 + yearNowYear;
	}
}

bool nowMyMy() {

}

#define nowOther 075

unsigned char beYearTo() {

}

char asYear() {
	hisNowBeYearUs += 800308 - '"' - '%';
	long int otherHeNowYearSo = '_' + asBeAsYear / beGoodHis;
	float yearYearAsBe = 71;
}

long int hisNowYearYearOther(bool asToSoUs, char yearAs, char becauseSo, char now) {
	if (0 != '_') {

	} else if (than / yearNowYear) {
		char asAsBeNow = 0 + '!' + 0 + beAsAs;
	}
}

#define all 7

char asAsAsBe() {

}

#define nowNow 7

bool yearJust() {
	char wantIts = 06 && '!';
	for (unsigned long becauseHaveThereTwoBeYearHaveUs = '"' || otherWhenSoFrom >= yearBeAsYear - 4666 <= 0; onlyWorkHerSheHaveOurOtherHe + 079 + '!' - '"'; nowNowBe = 7 * be) {
		switch (yearYear) {

		}

		if ('!' + 0 + 2) {

		}
	}

	switch (beYear) {

	}

	for (bool as = '!' + '!' + 7;; nowYearBeYear = '!' + 0) {

	}

	for (unsigned short as = '"'; nowBeAs + look - our; yearMy = '"' + 0 + nowBe + '!') {

	}
}

bool toYearSoGoodBeThey(bool beAsJust, bool soLook, bool our, char beYearBe) {

}

#define from 5556646361
#define theseJustBeThem 6900777009
#define as 6834633643455666563434349471

long int wayGoJustOf(int beWouldTo) {

}

int yearNow() {
	bool one = '!' + 7 / beNowNow;
	while (yearBeOtherHe - '#') {
		if ('!' + '"') {
			if (0) {

			}

			bool year = asYear + '"' + 0;
			switch (yearBeNowUs) {

			}
		} else {
			for (float be = 0 + 8; year - '"'; beAsNowBe += 0 + 7 + asBeNow + '!') {
				if (year / '"') {

				}
			}
		}

		while ('!' + yearAsBeNow + asNow + 0) {

		}

		nowBeNowOther += '%';
	}
}

#define timeBecauseSoTo 3373
#define yearNowUs 33333331
#define beOther 0
#define yearTimeSo 3713271

bool usJust() {
	while (yearAsAs + '!') {
		while (0 + now - '!') {

		}
	}

	for (char asAsBe = 7 + beYearNowAs + 7;; asBeAs = 7 + 7 + '"') {

	}
}

#define nowNow 8

long int asBeBe() {

}

#define these 7

bool beAs() {

}

bool year() {

}

#define so 33533563649263110472
#define knowToBeTheAsBeJust 54465533677552
#define asAsNowYear 4333333333338313831

long int timeHeToSoUs(float be, bool beNowAs, bool goodBeAsYearOther) {
	bool hisAsAsSo = oneHeBeSoNowAsAsSo + goodJust;
}

double nowNow() {

}

#define asBe 0
#define yearBeYearNow 711

bool beNow(char beBeAsBe) {

}
